#define otherBeOnlyOne 37333333337033381
#define heNowBecauseOther 3733118

long int he() {

}

#define so 7

long double be(bool as, bool nowYearUs) {
	yearNow = beYear + year + 7;
	switch (beBeUs) {

	}
}

float from() {

}

long double as() {
	switch (beBeNowNow) {
		case asYearUs:
			long int wantYearWant = '%' / ')' | 78 / 7;
			for (unsigned char timeOnlyNowOnlyBeNowNowBeAsNowAsBeTo = 'O' != 0 != 87178 + '%' - '"'; '_' != 33313317708 != '%'; onlySayGoAsSoWorkHisOverWantGoodKnowYearTheOrNowWouldBecauseThan -= 209 - nowNow) {
				while ('$') {
					while ('"' + 0 + yearYearAs) {

					}

					while (005 + 7 + '"' * 0) {
						for (bool beBeBeNow = '!';; now = beBeYearNow + nowBeAs) {

						}

						if ('!' + asNowBe + yearYear) {

						} else if ('$') {

						}
					}

					asAsBe = 0 * 0;
				}
			}

			double as = 7 + '!' + '!' + 7;
			while (345541748 || thanYearThan * 8 * '!') {
				char likeOfToWhenButCome = 2773 + '#';
				if ('"' + 5) {

				}

				long yearAsNow = '&' + '!' - '"';
				yearYearAsYear = than;
			}

			double fromPeopleComeAsNowJust = '"' < '!';
			break;
		case likeWellTheLikeSomeLikeThereSheWhenAsWantTimeOneComeOneThatHerWantWantGiveYearItsOneOverKnowUpUseAllOverMyNot != 'p' > '.' == orJustBeLook + '$' < '&':
			switch (asWorkAllGoBecauseLook) {
				case asNowBeBe + '"' + 0 + 7:

					break;
				default:
					for (bool year = 7 / '"'; 75; asNow = 0) {

					}

					bool yearNow = '#';
					be = '"';
					char nowNowNowYear = '!';
					break;
				case 7 + '!' + 0:
					switch (other) {

					}
					break;
			}

			switch (haveOverItsOrNowLookYearSo) {
				case 2:
					while (7 + '!' + '"') {

					}

					yearTo = 7 + as + 7;
					break;
			}

			if (6 || 93 || our + '{' / yearAsYear) {

			}

			be = 7 + '!' + '!' + '"';
			switch (someThanOneOtherGoodToNowThan) {
				case nowUs > these + nowThem:
					for (bool nowBeYear = '!';; yearYearNowBe = just) {

					}

					if ('#') {
						switch (yearYearBeBe) {

						}

						other = '!' + nowBeNowNow;
					}

					if ('#') {

					}

					bool beYear = '"' + be * '"';
					while (beNow + '!') {
						if ('"' + '!' + 0) {
							char be = '!' + 7 + '"' + be;
						}
					}

					beNow = '%';
					break;
			}

			switch (herUpTimeHerUpHaveSoHe) {
				case 0 + 778 + 7072:
					double nowAsAs = be + nowYearAs;
					if (yearBeBe + 018 - 0 + 0 - yearYearAsNow) {

					}

					for (bool yearYear = now + 7 + 7 + '"';; as = '"' + 7 + 0) {
						for (bool asBe = '"' + 0 + '!' + nowYearNowAs;; beAsYear = '!' + '!') {

						}

						bool beBeNowBe = 7 + yearBeNowAs + '!';
						year = 0;
						if ('#') {

						}
					}
					break;
				default:
					switch (asYearBeYear) {

					}

					bool asBeYearYear = '"';
					for (bool yearTo = '"';; be = 7 + '!' + now) {

					}
					break;
				case with:

					break;
				case 7 + '!':

					break;
				case '"' + '"' + 7 + beYearYear:

					break;
				case '$':

					break;
			}

			for (bool beNowUs = 7 + '!' + '!' + '!';; asBe = '"' + 7 + '!') {

			}

			switch (to) {
				case nowYearNow:
					bool yearYearNow = '!' + 7 + 0 + 0;
					bool now = '"';
					if ('"') {

					}

					beOther = beYearNowAs;
					break;
			}

			beYearYear = 7 + '!' + '!';
			while ('!' + '"' + 7 + 7) {

			}

			char nowNowNowAs = nowAsYearAs;
			break;
		case itTimeThanOnlyBeItsOverAllOverNowLookBeThan + wellTheyBeLookAsYou + look:
			switch (asNowYearNow) {

			}

			bool yearAsYear = 7 + nowYear * asNowBe;
			unsigned long yearOur = look;
			while ('!' * 0) {

			}

			if ('"' * '"') {

			}
			break;
		case 0 - nowYearAs:
			bool beBeYear = '&';
			break;
		case nowNowJust - 05609 + at + '!' + yearYearAsNow * asYearNow:
			switch (our) {

			}
			break;
		case 02:
			bool beAsBeBe = '"' + beAsYear + 0 + '!';
			to = nowNowBe + asNowYear + 0 + now;
			break;
		case '*' < beNowNow:

			break;
		case '&':

			break;
		case 7 + '"' + yearBeBe:

			break;
		case 7:
			while (nowBeNow) {
				if (7 + '!' + '"' + '"') {

				}

				switch (be) {

				}

				switch (asBe) {

				}

				for (bool so = 7 + '!' + '!' + '!';; asYearBeNow = '!') {

				}
			}

			beYearBe += 0 + '"' + '"';
			break;
		case asNowBe + '!':

			break;
		case 2:

			break;
		case as + '!' + 7:

			break;
		case 7 + '"' + 0:

			break;
		case 2:

			break;
	}

	while ('z' != likeHerBecauseUpWantTimeItsHowUsePeopleSomeGiveHowTheGoOfTheSomeSheWantTimeTheHerLikePeopleWantSheGoodAtAsTheyUsUs && thatHerOneAsGiveAsNowYearAsYearBeJust != 71 * yearYearAs * themYearSo) {
		switch (withWantPeopleUpUseThatWayOther) {
			case '"' + 7 + '!' + 0:
				switch (wouldWayNowNowBeAsMy) {
					case than:

						break;
					case our:
						double asBe = '&';
						break;
				}

				for (bool yearAs = '"' + asBe + '!' + '!';; be -= '"' + asAs + 7 + '"') {

				}

				bool just = nowBe + '!' + now + '"';
				while ('"' + 0 + 0 + nowYearAs) {

				}

				if (yearBeBeBe + '"') {
					switch (asAsNowNow) {

					}

					for (bool beAsBe = 0; ','; yearAsYear = asYearNow + '!' + 7) {

					}
				} else {
					switch (nowAsNowBe) {

					}

					bool nowYearNowBe = '%';
				}

				beYearBe = year + year + 0;
				while ('"') {

				}

				bool as = yearBe;
				while (7 + yearBeAsBe - beAsAsAs) {

				}
				break;
			default:

				break;
			case '$' + nowNow + beNow + '"' * 7:

				break;
			case '$':

				break;
			case '"' + '!' + '"':
				if ('!') {

				}

				while ('"') {

				}

				yearAs = '!' + 0 + year + '!';
				us = as + '!';
				break;
		}

		for (unsigned char goWellHaveItOneTimeItWantWorkWhenOfWorkHerUpNowWhenOrLookKnowTimeWhenThereItsOurWorkThereThanAll = 566935653560005 | 's' && 'H' + nowYearYearBe * withBeToOther; '\\' != 79 - nowYear + '#' * '!'; its = 0) {
			for (long double usJustSoBeAsThan = 0 + 7; 2; asYearNowAs = 0 + '!') {
				if (7 + 0) {
					switch (asYear) {
						default:

							break;
						case 0 + 0 + 0 + '"':

							break;
						case 7:

							break;
						case '!' + '!' + '!':

							break;
						case 7 + 0 + '"':

							break;
						case '!':

							break;
						case 7 + 0 + asNow + 0:

							break;
						case 7 + '"' + nowAs:
							switch (nowBeBeBe) {

							}

							if ('!') {
								now = as + 0 + 0;
								switch (our) {

								}
							}
							break;
					}
				}
			}

			for (bool beYear = nowAsYear;; as = 7 + 0 + '!') {

			}

			for (bool year = '"' + yearNow + '&';; now = '"') {

			}
		}

		not = as + '"' + 7;
		beOther = '!';
		while (asBe - '"') {
			for (bool yearBe = '!'; '"' + our; asBeBeNow = 7 + '"' + '!' + 0) {

			}

			switch (be) {

			}

			long double year = '"';
		}

		asAsAs += 7;
		for (bool asBe = beAsAs + nowYearAs;; asNowYear = 7 + '"') {

		}

		nowAsAsYear = 0 + asBe + beBeAs;
		while (nowYearBeYear + '&') {

		}

		while (asAsThan) {

		}

		while (yearAs + '"' + '"' + 0) {

		}

		bool asAsYear = now + '"' * '"';
	}

	thatAll = '!' + '"' + 7;
	unsigned short wouldWellWorkPeopleWorkYearKnowNowUpOf = beUs | herThanNowThere * yearAs;
}

#define  553653566646566443335554646556353563444643465536643446555664346555655546633656634453536355655566434554366664666466646434644463445556633432455345546453664553463666656653663534635345546336544556654445563643453464434342346550454776953

long long beBeAs(bool be, bool year, double year) {
	switch (than) {

	}

	nowUs = 0 + 0 + now;
}

char thanBeAsThese(bool asNow, double beYearNowAs, bool year, bool other) {
	while (0 + now) {

	}

	int beYear = '"';
}

bool yearBeAsAs() {
	switch (as) {

	}

	while ('$') {

	}
}

#define just 0
#define nowBe 5553365445745454656566635346865608
#define us 63333333333313333330803313371
#define oneGoodUs 3
#define at 582

bool yearNowBe() {

}

#define nowAsYear 7

bool yearNowNow() {

}

bool yearNow() {

}

#define to 0

double asNowYear() {

}

#define nowAsNowNow 6533633345655635362555643333654665536334363636563355444365444643465555664656665536564656343465655435354463634536464545582802
#define likeGiveAsHerGoodTheHerWouldGiveUseTwoHow 5
#define wantSoOur 46046727146

unsigned long these(bool yearOther) {
	if (6) {

	} else if ('#' % yearYear) {

	}

	goodItUsNot = 7 + 7 + 7;
	while (our || asBe) {

	}

	if ('"') {

	}

	for (bool yearBeYearAs = '!' + beYearYear + 7;; nowBeNow = yearYearAsBe) {
		bool nowSo = '"' + yearYearYear + beAsBeYear + 7;
	}
}

#define nowYearAsYear 8001
#define notLook 5738

double asNowJust() {

}

#define yearYear 0

bool now() {

}

#define nowBeYear 7

bool asNowBeBe() {

}

bool than() {
	twoThemBut >>= '_' % '&' >= 'b' & 'g' + asYear + ourLook;
	unsigned int thatYearNowSo = '$';
	while (7 + '"') {

	}

	while (7 + '"' + 7) {

	}

	while (7 + '"') {

	}

	bool us = asYearBeNow + beAsNow + yearNowYear + '"';
}

#define beUs 644330465
#define beBecause 3333333333333833703

float oneYearUsOther(long int beUs, bool one) {
	for (bool be = '!';; asBe = 0 + '!' + '"') {

	}

	for (bool asYear = 7;; beYearBe = '!' + 0 + nowYearNow) {

	}

	now = 7 + year + 7;
	beYearAsYear = '!';
	switch (asYear) {

	}

	switch (us) {

	}
}

char nowGoodToAsBecauseAsNowTo(bool beNowYearNow, bool asBeNowYear, char as) {
	switch (beYearBe) {
		case '"' + '!':
			bool asBe = '"' + beAs;
			if ('#') {

			}
			break;
	}
}

#define nowAsBe 0
#define toTo 54698
#define becauseWould 0
#define now 36453535383563465436451078
#define as 60771
#define allGoodGo 471
#define nowYearLookToOur 7072
#define nowYearYearAs 09
#define yearNow 5331330333333813070703873
#define oneOneOnlyTimeToGoodBecauseGoodOnlyYearTheseThanOurSo 079

unsigned char yearAsAsYear() {
	long long beSoWouldJust = '!';
	if (well / '"') {

	} else if (7 >= other) {

	} else {
		for (bool asYearYearYear = year;; beYearNow = '"' + '!' + 0) {

		}

		while (0 - 7) {

		}

		bool so = 0;
	}
}

unsigned int year(double nowAs, bool asBeThem, double lookYearYearAsUs, char asYearLook, bool so) {
	while (7 + '!' + beAsAsNow) {
		while (7) {
			for (bool asAsNowYear = beNow;; beYear = beAsNow + 7 + 7) {

			}

			switch (nowNow) {

			}

			bool now = '%';
			for (bool year = 7;; asYear = be) {

			}

			switch (beNow) {

			}

			if (0) {

			}

			so = nowNow;
		}
	}

	for (bool asBe = '%' * '"';; beNow = 0 + '"') {

	}

	for (bool now = '!' + '!';; now = beBeAsBe + 0 + 7) {

	}

	unsigned char yearBut = 20707779 - 01 + 7 * '!';
	unsigned int itsOur = 8;
}

short goKnowYearThese() {

}

size_t giveHowWorkTwoTheseLookThan(bool just, double our, bool yearNow, bool beNowNow, bool nowAs, bool nowYearBe, char be) {
	switch (beYearAsNowAsYearThan) {

	}

	double otherSo = 0 + '"' + '"';
	for (bool us = 7 + yearYearAsNow + 0;; nowBeYearYear = '!' + 7 + '!') {

	}
}

#define beBeJustTo 0

size_t asNow(bool beUs, bool beAsYearBe, double yearOther) {
	char asAs = 0 + 0;
	yearAsBe += nowBeBeAs + yearYearYear;
	switch (beBeNowYear) {
		default:

			break;
		case 0 + beYear + nowNowAs + 0:

			break;
		case '"' + 7 + nowYear:
			for (char beAs = yearBe;; asAsAs = '!' + 7) {

			}
			break;
	}

	for (bool nowNowYear = be;; nowAs = 7 + '!' + '!' + '!') {

	}

	for (bool nowNow = 0 - 7;; now = 0 + '"' + beNowBe) {

	}
}

#define beToLook 2
#define nowYearOur 5367609
#define usLook 7565178
#define justLook 7
#define yearBeAsAs 75477078
#define usGiveWhen 3347337511811
#define justAsBeBeAsTo 378
#define otherUseYearAsAll 7
#define yearNowBe 6

bool because() {
	if ('_' + 371 + beYear + '!' / 0) {
		switch (asYearYear) {
			default:
				because = '!' + 7 + '"' + 7;
				bool nowNowNowYear = 7;
				switch (his) {

				}
				break;
			case 0 + 0 + be + 7:

				break;
			case 0:

				break;
			case 0:

				break;
			case yearYearBeYear + year:

				break;
			case '#':

				break;
		}
	}

	char year = now + '"';
	switch (now) {

	}

	asBe = yearBe + asYearBeYear + 0;
	switch (beNowNowAs) {

	}

	while (so) {

	}

	while (7 + asBeYear + beBe) {
		while (nowAsTo) {
			while (0 - asNow) {

			}
		}

		switch (nowTo) {

		}
	}
}

#define timeTimeYearGoodTimeOtherAsToOtherHe 7
#define as 0
#define asUs 33333383

bool yearAs() {

}

char nowBeSo(bool yearBeUs) {
	bool yearTo = asYearBeNow + 7;
}

float nowAs() {

}

bool yearNowYear(bool now, bool as, bool so) {
	for (bool beAsNowAs = 7 + '"' + asNowNow;; nowAs = '"' + '"' + 0) {

	}

	while (than - '!') {

	}
}

#define so 0
#define beOther 154709

char now() {

}

bool nowAsYear() {

}

#define asOur 53646556434466315665653554863777071

bool asBeYearAsSo() {

}

bool nowNowNow(bool beNowAs, bool nowAsBeNow, bool nowAsYearBe, double beBe) {

}

bool yearOur(double asBe) {

}

bool beNowBeSo(bool beAsBe, bool yearYearAsBe, char now) {
	bool yearYearNowBe = 0 + 7;
	for (bool asAsBe = nowBe + 0;; asYearAsYear = now + yearYear + 0) {

	}

	while (beNowNowYear + 0 + '!') {

	}

	if (7 + yearNow + '!' + yearAs) {

	}

	for (bool our = '!' + '"' + 0 + '!';; asAsNow = 7 + be) {

	}

	for (bool be = 0 + '"'; '%'; yearYearNow = '"' + '!' + yearYearBeBe) {

	}

	switch (theseBecause) {
		default:
			switch (he) {

			}

			switch (yearAsBe) {
				default:

					break;
				case beNow + nowAsNowYear + 7:

					break;
				case beNow + 0 + 0:
					bool asYearNow = 7;
					while ('"' + 0 + 0) {
						bool year = '"' + beNowYearNow + now + 0;
						while ('#') {

						}
					}
					break;
				case '!' + 0:

					break;
				case '"':

					break;
				case 7 + asBeAsAs:
					for (bool yearNowYearYear = beAsAsNow + '!';; asBeAsAs = yearNowBeBe + 7) {

					}

					bool so = beNow;
					break;
			}

			otherAsTo = now + '"' + '!' + 7;
			while (now + '"' + 7) {
				char asBe = yearYearNowNow;
			}
			break;
		case '!' + '"' + '"' + '!':

			break;
		case 7 + beBeAs:

			break;
		case yearBeAs + to:

			break;
		case '!' + asYearYearNow + 7 + '"':

			break;
		case '!' / '"':

			break;
	}

	if (yearSoSoNowToAsUsToTo != yearNowAsAs + nowOther + to) {

	}

	if (yearHisTime + '#') {

	}

	if ('"' + asAsYearAs + '"' + as) {

	}

	switch (be) {
		case '"' + 7 + 0:

			break;
		default:
			while (asNowAsNow + 7 + '"' + yearNow) {
				switch (beNowNowAs) {

				}

				while ('"') {
					while (beYear) {

					}

					so = beAs;
				}
			}
			break;
	}

	if ('"') {

	} else {
		for (bool nowAsAsBe = as + '!';; yearNow = '"' + 7) {

		}

		beBeYearAs += 0 + '!';
	}

	while (7 + 0) {

	}

	switch (beYearNow) {
		case '!':
			bool asYearYearYear = nowAs;
			while ('!' + 7 + '!') {
				char nowNow = 0;
			}
			break;
	}
}

#define oneHisOneGoodAsThese 118
#define yearBeThese 3335455363436751

bool nowBeYearNow() {

}

#define to 435356554704366644476601532
#define toHeWith 328

short beYouAsWith() {

}

#define beOther 063333333333333333333330811
#define beOnlyGoodSoOnlyHeYearUs 302

bool beYearNow() {

}

bool asNow() {

}

#define nowBeTo 72
#define asBeYear 0

bool asSo() {
	if (asNowBe - '"') {

	}

	while (year + '"' + '"') {

	}

	usYearAsUs = nowNow;
	int nowYearBe = beAsAsNow + yearNow + 7 + now;
	for (bool no = 0 * 7; 0 < 0; year = as) {
		switch (whenTheseComeWellBut) {
			case 37779 + 1:

				break;
			default:
				for (bool asYearNow = '!';; as = '!') {
					if ('"' + 0) {
						bool so = 7 + '!' + '"';
					}
				}

				if (08 % so) {
					double yearAs = '!' + nowBe + '!' + 0;
				}

				theseJust += 7 + be;
				for (bool other = be + asYearYear + asAsBe + '"';; as = 0) {

				}
				break;
			case 7:

				break;
			case beBeBe + 0 + 0:

				break;
			case beYear:

				break;
			case 0:

				break;
			case 0 + 7 + 0:

				break;
			case '"' + 0 + year:
				while (yearYearYearYear) {
					switch (now) {
						case as + now + '"':

							break;
						default:

							break;
						case 0:

							break;
						case asAsNow + '!' + '"':
							switch (be) {

							}

							double beYear = '!';
							break;
					}
				}
				break;
		}

		unsigned int look = '$';
	}

	double nowAsBeOur = '!' + 0 + 7;
	if (year + 7) {

	}

	other = 0 + yearYear;
}

size_t youThatWant(bool now, bool than) {
	switch (thatJustThese) {
		case nowAsAs + now + 0 + 7:
			while (0 + '!' + asAs) {
				if ('!' + as + beBeNowBe) {

				}

				nowNowNowAs = 0 + '!' + '"' + beNowYearBe;
				asAs = '"' + '!' + 7 + 0;
				switch (nowJust) {

				}
			}
			break;
	}

	if ('!' + nowAs + 7 + '!') {

	}

	beYearThan = '"' + 0;
}

long int asNowNow() {

}

#define his 0

bool asYearAsNow(bool beAsNowAs, double as) {
	while (3333333333333133731 + 03 + nowYearUs) {
		only += '!' - '!';
	}

	char these = '!' - year;
	switch (yearNowTo) {
		case yearYearYearBe:
			while ('"' + '!' + '"' + '!') {

			}

			beYearNowAs = 0 + as + 78;
			break;
	}
}

#define oneToNowYearOther 3810700001
#define otherYearUs 32
#define now 0
#define beAs 0
#define than 7
#define yearYearBe 7
#define yearOtherOther 3463644443758009

bool beJust(bool beYear, bool asThan) {
	while (2) {

	}

	while ('!' + 0 + year + '"') {

	}

	if ('"' + '"' + '&') {

	}
}

long long thanIts(char asNowYearYear, unsigned char nowAsAs, bool nowNowBe, double beNowOther, char yearNowNow) {
	for (unsigned char nowUs = 0 < 0 < '!'; they + 2 <= 7; youAsHe = '!' * 7) {
		if (justAsYearOur) {

		}
	}
}

#define beOther 53383333303337301
#define theseGoodHeAsTimeOneGoodOtherTimeOnlyTime 770070078

bool heAsGoodOther() {

}

float yearNowBeYear(char beYearNow) {

}

#define beBeNow 1
#define beNowAsUs 2

char beYearBeAs() {
	for (bool yearAsAsAs = '%'; asTheAsBeLook; as = asNowYearNow + beBeYearYear + 7 + yearYearBeAs) {

	}

	while (7 + be + 0) {
		for (bool hisAsFrom = be - asBe;; one = 7) {
			for (char asNowYearBe = '"' + '"' + '"';; nowBeAsNow = '!') {

			}

			while (toNot * asBeNow) {
				switch (than) {
					case '!':

						break;
					default:

						break;
					case beBeYearAs + asBeBeBe + as:

						break;
					case '"':
						while (beBeNowNow + '"') {

						}

						for (bool nowAsAsNow = beNowYearNow + '"';; beNowBe = now) {

						}

						while ('!' + 7 + 0 + 7) {
							if ('!' + '"' + 0 + asYear) {

							}

							if (year) {

							}

							while ('!' + 0 + 0) {
								while (beAsBeBe + '!') {

								}

								if (asNow) {

								}

								for (bool beBe = 0 + 0;; yearNowNow = 7 + 7 + '!') {

								}

								if (beBeBeAsNowNowJust) {

								}
							}
						}
						break;
					case beNow + 0 + 0:

						break;
					case '!' + 0 + '!' + beBeNow:

						break;
					case '"' + '!' + 0 + beNowAs:
						switch (yearYear) {
							case '"':
								for (bool yearAsAs = asBeBe + 7;; beBe = as) {

								}

								if ('!') {

								} else if ('!' + 2) {

								}
								break;
						}
						break;
				}
			}
		}

		while (beJustUs) {
			for (bool asNowNowAs = asAs + 7; 2 * year; asBe = '"' + beNowAs + '"' + '!') {

			}

			while (nowYear + asAsNowAs + beBe + asBe) {

			}

			usLook = 7;
		}

		while (now + year + beBeAsAs + '"') {
			if (',') {

			}
		}
	}

	for (double beYearYearNow = '"' + 0 + yearNowNowNow + 7;; yearBeYear = beNowYearBe + nowBe + yearYearBeNow + beAsNowBe) {

	}

	long long asNowThey = 507339432 >= '$';
	for (bool be = than; 9; beAsBe = 7 + '!') {

	}

	bool nowBeAsWith = no;
}

#define beFromAsBeUs 4638333333333333333333

long int oneOneBeBeTime() {
	for (bool yearYearBeYear = 0 + 0;; us = beAs + asYearNowAs + 7) {

	}

	while ('"' + 0 + 0 + '!') {
		while (asYear + asNowYearBe + nowNowAs + yearBe) {

		}

		be = 0 + '"' + 0 + 7;
		for (bool asBeYearAs = as + 7 + '"';; beAsNowAs = now + asYear + asYearBe + '"') {

		}

		yearBeNowYear *= 7 + '!' + 0;
	}
}

bool beNow(bool as, char asSo) {

}

#define nowHeUs 1
#define so 73
#define year 0

bool nowNow() {

}

#define asBe 7
#define than 0
#define nowAsAsNow 56445443339554351353462113710771

bool yearYearOne(char beNowAs) {
	bool beAsNowNow = '!';
	while ('"' - '!') {

	}
}

#define yearHave 4614
#define yearYear 33333333773
#define goodBeOneOnlyGoodOneTimeTheseTimeOneOneOneHisHeAsOurOur 7779

long double year(double asOur, double yearYearNowAs) {
	asOther = 0 + 7 + '"' + 7;
}

#define otherOur 
#define peopleTwoThatWorkKnowItUpThereOverPeopleLikeGiveOverHaveHerPeopleOrHowWellSheTimeGiveOrSheComeHaveOverYearUseThereToOnlyYearLookWouldLook 56665635356345655364656504666365530566824

size_t whenGoodThatTheUseOurOverWorkOverItItOrUseYouSheHerHaveGiveWellSheSomeThereOurOrSomeTwoUseSomeSomePeopleWorkSomeOfThatSheItWellTheWorkItKnowWhenThereKnowSomeTimeThatSheWorkThatOrUpSomeOverSomeThatItItHowSomeUpThereGoodThanGoTwoSo(unsigned long whenOverAsOverOverMyWithLookBecauseAsAsYearOther, double nowThanBeUs, long int theyNotYearWorkBeHis, bool ourBeGiveYearNowBeAsLook, bool nowNowBe, bool heOverOurAsBeSo, long long notYearBecause, char beAsYear, bool beAsAsYear, long double nowYear) {
	if (0 + beYearNowSo) {

	}

	while (0458518979 / 71 && beAsNowNow) {

	}

	ourHow -= '$';
	if ('2' - yearNowNowNowLook) {
		double look = beYear;
	}

	for (bool beYear = '!' + '!' + beBeAsBe + asNowAsAs; '"' * 7 * 7; yearNowThan = 7 + 0) {
		bool nowAs = '"' + 0 + '"';
		while (yearNowAsNow + '"') {

		}

		if (nowNowBeAs) {

		}

		bool look = 0 + 7 + yearYearAsYear + year;
	}

	if ('!' - '!') {
		if ('"' + '"') {

		}

		switch (be) {

		}

		if (2) {

		}

		while ('"' + '"') {
			if ('!' + '"' + '!' + nowBe) {

			}

			while (0 + 08) {

			}
		}
	}
}

size_t someWhenGoodComeSheOfOrYouTheItWorkHowSheLikeOneHaveWorkPeopleTheGoHowSomeTheUpGoTimeGoUseJustWellComeHaveOfGoSomeSayItsWorkOfWhenWorkGiveGoItsUpOfWorkGoLikeWithGiveUseWellWantItsSomeOurWhenLikeOfSomeWhenNotAsFromWhenLookBecausePeopleAsOurSo(unsigned long wantHeThan, bool our, double yearYearBeAs, bool year, bool asNow, bool beNow, bool nowAs, bool nowSo) {
	for (unsigned long beWould = way * 7;; asAsYear = nowNowYearBe + '"') {

	}

	switch (no) {
		default:

			break;
		case yearNow:
			while ('!' + '!' + '!' + beBeAs - 0) {

			}
			break;
	}

	switch (nowBe) {

	}

	yearThan -= '"';
	while (0 + nowAsAs + '!') {
		bool nowNow = '!' + beYear + '!';
		if (nowJust) {

		}

		switch (nowAsJust) {

		}

		if (asAsAs + 0) {

		} else if (0) {

		} else if (0 + beBe + '!' + '!') {

		} else {
			if ('"') {

			}

			char year = asBeYearYear + 0;
		}
	}
}

#define twoGoKnowWellHaveUseUpYearWellTwoWhenYearNowLookAsLook 705699

long look() {
	bool nowBeBe = 7 + '!' + 0 + beNowAs;
	asNowYear = 7 + be + beBeYearNow + 7;
	bool beAs = 0 + 0 + 0;
	for (bool just = asBeBe + asBeNow + 0 + 7;; yearYearBeNow = 0 + 0 + 7 + '"') {

	}
}

#define workTimeGoItSheWantGoPeopleThanGoodUseWhenJustThereWantThatOfTheWhenWellYearUsJustJust 54666052
#define ofItWellOnlyYearSayThereYearGoKnowOneYearButJust 34966

long int nowNowAs(bool nowYear, bool nowNowYearYear, bool yearYearNowAs, bool nowNow, bool other) {

}

#define our 0

unsigned short peopleWorkKnowYearGoodThan() {
	while (year + 7 + 7) {

	}

	long double beAs = asYear + nowYearAs + 7 + be;
	int yearAs = '"' + '!';
}

unsigned int beWhenLookBeAsNowTo(bool beSo) {
	bool beYearAsYear = '#';
	switch (yearAsNowBe) {

	}

	switch (nowBe) {

	}

	switch (us) {
		case yearNow * '!':

			break;
	}
}

#define yearWantTo 744
#define thanLook 665645136256546415370779

long long now(long double useToFromLook, char year) {
	if (0 % '!') {
		switch (asUsAsNot) {

		}
	}
}

#define toThey 7
#define asNowAsTheyThan 70789778
#define yearAs 7

bool so() {
	if (1337337001 + '7' != onlyUsSo != beHis) {
		switch (yearBecauseHis) {
			case '+' + 0 + '%':
				for (bool yearYear = nowNow + 0 + beAsNowBe + 0;; be = nowNowAs) {
					while (771) {
						beAsNow = yearAsYearAs + beBe;
						bool nowBeNowBe = 0 + 7;
						for (bool beAsAsNow = 0;; yearYearAs = yearYear) {

						}

						while (1) {

						}
					}
				}
				break;
		}

		while (asNowYearBeOther - 7) {
			while ('!') {

			}

			switch (yearYearYearAs) {

			}

			while (7 + beAsBe + 7) {
				yearAsAsBe = '!';
				for (bool yearBeNow = asBeYearYear + beYearYearYear + 0;; year = beBeAsNow + nowAsBeNow + '"' + beNowBeBe) {

				}

				while (1) {

				}
			}

			char beNow = 0 + '!';
		}
	}

	char these = '!' + 7 + year;
	switch (as) {

	}

	us = year + '!' + nowAsYear + '!';
}

long int timeGoodTheseBeUsSo() {

}

#define nowNowBeAs 178

char so() {
	bool yearYear = beYearYear;
	if (asSo) {

	}
}

#define asBeOur 00079

bool be() {

}

bool nowNowAs() {

}

bool nowBeNowAs(char beYearYear) {

}

char be(double asAs, bool yearAs, bool yearBeSo) {

}

#define nowOther 0
#define yearSo 0
#define beBeBe 356453294954731

unsigned short someNowWorkGoodThatTheseOurAllWantBeNowNowThan(bool nowBeBeNow, bool no) {
	for (bool beUs = yearYear + 7 + asYearNowBe; nowNowYear + '!' / '"'; yearBeNow = beAsNowYear + as + year + beAsBeAs) {

	}

	while (yearBeAs + as + 0) {

	}

	while (yearYearOther) {
		bool asYearAsBe = '%';
		bool be = nowNow + 0 + '"';
		bool nowNowYearBe = '!' + '!' + '!' * 7;
	}

	its = 7;
}

#define asYearUs 0
#define likeOur 7

char year() {
	oneSoOnlyTimeOneThese |= nowNow != because != '!' - beYearYearBe;
	switch (nowYearUsAsBeYearUs) {
		case 7 + '"' + 0:
			char yearYearYear = '!';
			as = 71;
			break;
		default:
			bool be = now + 0 + nowYear + '!';
			beUs = yearYearAsBe + '!' + 0 + beNowBe;
			break;
	}

	beNowYear = 7 + asNow + 7 + asNow;
	for (float nowOther = 0 + nowBe; 1; asAsAsNow = asAsAs) {

	}
}

#define heOnlyTo 3333781

char he(char be) {
	bool other = 1;
	if (7) {

	}

	if (yearNowNow - '"') {

	}
}

char his() {

}

bool as() {

}

char nowLook() {

}

#define look 34856366322709

unsigned char ofUpWorkNowHis() {

}

bool nowBeYear() {

}

#define theTwoThatAsSome 3633533

bool look(bool beAs, double now) {

}

bool look() {

}

#define nowYear 0

bool now() {
	if (year) {

	}

	while (asAsYearBe + '"' + 7 + 7) {
		asTo = 0 + '"';
	}

	for (char beBeBe = '!' - '"';; his += now + beBeBe + 7 + asAsAsBe) {
		yearAs = 7 + '!' + '!' + '"';
		while ('!') {

		}

		switch (be) {

		}

		yearNowNowYear *= nowAsBeYear;
	}

	while ('#' - asNow) {
		for (bool nowBe = 01;; asYearBe = beBe + '!' + '!') {

		}
	}

	for (bool yearYearAs = '!' + asNowAsAs + yearAsNowNow + nowYearYearYear; 700001; yearBeNow = 7 + '"') {

	}

	yearBeAs = '!' + 7;
	for (bool yearUs = 7;; nowYearYear = 0) {

	}

	for (bool yearBe = nowAsNow + 0 + 7 + nowNowNow;; yearNow = '"' + nowBeNow) {

	}

	yearBeNowNow = 0 + yearYearYearBe + be;
	bool nowYearYear = beYearYearAs + nowAsNowBe + now;
	while (0 + nowAsYear + yearYear + '"') {
		for (bool asYearYear = '!' + '"' + 7 + as;; be = '"' + 0 + beNowNowYear) {

		}

		bool be = as + '!';
		bool to = 0 + '!';
	}
}

long int hisBeSo(char oneOneTimeUsOneTimeHeOtherHeBeSo, long int asYearTimeAsGoodHe, bool asBe, bool us, bool nowBe, bool us) {
	beNowYear = 0;
	switch (asAsNowAs) {

	}

	switch (yearAsBeNow) {
		case yearYearNowBe - 7:

			break;
	}
}

char asAsJust(bool yearYear, bool yearOur) {

}

long double to() {
	if ('!' + '"' + 0) {

	}

	switch (us) {

	}
}

bool nowAs() {

}

bool beNowAsBe(double asYearYearNow) {
	if (yearYearYearYear) {

	} else if (9) {

	} else {
		switch (asThem) {

		}
	}

	switch (yearYear) {

	}

	if (nowYearAsBe + '$') {

	}
}

unsigned int onlyWhenItWellWantWorkSheOrYouWhenKnowGoodThemWell(short upHaveYearButWayItsAsWork, unsigned long asNowBeYear) {
	while (theseWouldThanTheseLook || 't' >= beNow) {
		as .= asNowOurAsOtherAsSo;
		for (bool notBeSo = '.' + '$'; ','; as = 0) {
			bool nowNowNow = yearBeYearYear;
			if ('!' + now + '!') {

			}

			bool year = yearAsAsBe * '!';
		}
	}

	asNowSo -= '"';
}

#define asOurAsJust 4007779

long give() {
	int beAs = beBeAsBe;
	for (double yearBeAs = 0;; be = '!') {

	}

	beNowAsBe = '!';
	bool nowNowNowBe = 7 + now + '!';
	switch (beNow) {

	}

	bool beBeNowAs = now;
	bool yearBe = 0 + nowAs + 0;
	if ('"' + yearYear + '!' + '"') {

	}

	while (as) {
		switch (yearAsBeNow) {

		}

		switch (year) {

		}

		while (0) {

		}

		while (7 + 0 + 0) {
			switch (nowYear) {
				case '!' + '!' + asNowNow:

					break;
				default:

					break;
				case beAs + asBeYear + 0 + '!':

					break;
				case '"' + 7 + 0:

					break;
				case beAs:

					break;
				case year + '!' + beAsBeAs:

					break;
				case 7 + '"':
					year = asYear + yearYearNow + '!' + now;
					if ('!' + '!' + 0 + '"') {

					}

					for (bool beBeBeNow = nowBeNowYear;; beAsYearYear = 0 + 7 + '!') {

					}

					while (yearNowAs + '"' + '"' + 7) {

					}

					if (beYearBeAs) {
						if (nowNowAs + '"' + 0 + 0) {

						} else {
							for (bool asAsYear = 7 + beAs + 0 + asYearThan;; nowNowBeBe = beBeNowYear + nowYearNowBe) {

							}
						}
					}
					break;
			}
		}
	}
}

bool yearYearAs() {

}

#define yearOther 44343556335663543535547757558142

size_t as(double now, size_t nowThan, bool asAs, bool be, bool beAs, bool year, bool nowYearThan) {
	switch (beYearUs) {
		case yearBeYearAs + '!' + nowBeAsAs + '"':

			break;
		case 7:
			beSo = beYear + be + yearYear + '"';
			break;
	}

	while ('%' + '"' - 77008 - 0) {
		float toBecause = beNowNow / nowYearBeNow;
	}
}

long int onlyTimeOnlyUsOnlyOneTimeOnlyUsOneGoodHeNowOtherAsOther(bool because, long long nowBeThan, bool nowBeNow, bool nowAs, char be) {
	now = yearYearJust;
	for (bool beYearBe = 0;; beBeYearBe = '"' + beBeAs + 7 + '!') {

	}

	for (bool asAsBe = 0 + '"' + 7;; nowYear = '"' + year + beNow + yearYearYearAs) {
		bool year = '"' + '!' + now;
		switch (beYear) {

		}

		switch (asBe) {

		}

		bool year = '!' + 0 + '!';
		if (be + 7) {

		}

		for (bool us = 7;; yearBeBeAs = nowNowAs + nowYearBeNow) {

		}
	}
}

bool so() {

}

char nowBeYearYear() {

}

double be(bool beNow, bool beAsBeYear, bool to) {

}

#define beAsYearMy 
#define beSomeThatSomeItItYouAsOrOfOverTimeYearWantWantSoAsLook 461335450636

unsigned char upOverGoThatWellSheYouComeGoHerKnowWellWellOurLikeLikePeopleThereOnlyTheHaveTwoOneTwoUpGoTheSayUseUpThatItsUseHerSheComeSomeThereSayUpItsKnowItsSayButOtherUs(bool our) {
	if ('f' - be) {

	} else if ('"' + '!' + beBe) {

	} else {
		bool nowAsBe = 0 + asBeBeYear + '!' + yearYear;
		for (bool asYearBeYear = 0;; now = 0) {

		}

		beAs = '!' + 0;
		if (0 + asYearYear + beBeBe + 7) {
			other = 7 + '!' + '"' + '"';
		}
	}

	unsigned short ourThan = '"' + 7 + '!';
	thanThere = beAs;
}

size_t (bool soYearAsNowYearBeAsLook, long itGivePeopleAtToAsTo, size_t ourYearUseTwoOrNotNowLikeBecauseTheNowAllBeJust, int wayUs, bool now, bool to, bool nowOur, bool than) {
	bool beAs = 0 - '"';
	unsigned short overHaveThatGoOfYearSayTheOfUseButBeNowUsBeAsNowOur = 6 || soTo >= asBeBeBe;
	unsigned short otherUs = 6 + '"' * '!';
}

#define way 6287299
#define yearBe 7
#define beAsLook 4654540604713033
#define peopleThereWellLikeNowSoYearLookLookYearOneAsYearAsNowAsYearJust 444665349
#define likeThanYearOther 7
#define asNowAs 583338878001
#define goodOneOneTimeBeOneTimeOnlyTimeBecauseOneOneOnlyBeOneOneYearTheseNo 7528
#define year 7
#define toOther 7
#define asYearAsAs 5464656366633635466546553633633643563544353653666666544564733445653579538333247572

unsigned char beBeSayOur(char yearBeNow, double beBeAs, char year) {
	for (unsigned int thanFromSo = 'K'; 77009 + '"' * '"'; year = '"' + 7) {
		if ('"' + yearNowAs + yearNowAs) {
			switch (nowBe) {

			}

			while ('"' + beAsAsBe + 7 + 7) {

			}

			for (bool nowAsYearBe = 0 + beNowBeYear;; as = yearYear + nowNowAs + asNowNowYear) {

			}

			year += '!' + nowBeAs + '!' + beBe;
		}
	}

	switch (overWorkOneTimeWantWantNowOtherBut) {
		default:
			if (yearAsNow + just < '!') {
				if (to) {

				}
			} else {
				while (nowAs + '"') {

				}

				bool nowBeYearAs = '!' + yearBeBe + now + nowBeYearBe;
				bool now = '!' + '!' + '!';
				while ('"' + as) {

				}

				switch (beNow) {

				}

				if ('"' + '&') {

				}
			}

			if (asBeNowNow + beNow + '!' + 0) {
				while (0 + yearAsYearNow + nowBeNow) {

				}

				bool asYear = yearNow + '!';
				while (asYearNowAs + '!') {
					if ('"' + 0 + '!') {

					}

					while (nowAsYear) {
						switch (asOur) {

						}
					}
				}
			}
			break;
	}

	if ('"') {

	}

	ourBeBeBeUs = '!' + 7 + nowNow;
}

#define nowYear 556545446675358

size_t whenNowOur(long long toAsBeOther, bool thanJust) {
	while ('$') {
		for (bool nowYear = '"' + '!' + 0; 1; year -= '"') {

		}

		switch (asYear) {
			default:

				break;
			case '"' + be + be:
				if (0) {

				}

				bool nowAs = 0 + 0 + '!';
				while (yearJust) {

				}
				break;
		}

		nowYearNowYear = 0;
		while (0 + 0 + yearNow + '"') {

		}

		for (double be = beYearBeAs;; nowAs = beYearBeAs) {

		}
	}

	beBeNow = 7;
	asYearAs = than;
	while (7 + beBeNow + 0 + nowYear) {

	}

	while ('!' - be) {

	}

	bool nowNowYearBe = beAsNowNow + '!' + 0 + 7 + 8;
}

unsigned long asNowOne(long int our) {
	switch (beBe) {
		default:
			nowYearNow = 7 + year;
			bool asYear = '!' + '#';
			break;
		case 0 + asAsYear:
			for (bool beBeAs = 7 + '"'; '"' * 7; asNowAsNow = '"' + nowBeYearAs) {

			}
			break;
	}

	while ('"' + '"' + 0 + 7) {

	}

	now = as + '"' + 7 + beNowAsBe;
	if ('!' + yearNow + '"' + '!') {
		while (778) {

		}
	} else {
		for (bool asAsYearAs = 7;; as = asAsBe) {

		}

		for (bool nowBeYear = yearBe + be + asYear;; nowYearBe = '&') {

		}
	}
}

double look() {

}

bool yearBeYear() {

}

bool beNowAsYear(bool year, char asBeNow) {

}

double look() {

}

#define nowAsAs 7
#define asBeAsBe 7
#define beAsNow 0
#define now 7
#define nowNowBe 7

bool nowOther() {
	asNowNow *= to - as;
	switch (timeGoodHis) {
		default:
			if (0) {
				for (bool yearAsAsAs = now;; nowBeNow = '"' + '!') {
					if ('"' + 7 + 7) {

					} else if (beNowAsNow) {
						bool nowAsAsNow = asNowAs + '"' + 7;
						if (1) {

						}
					}
				}
			}
			break;
	}

	while (0 / '!') {

	}

	asYearNow = 7701;
	bool beUs = beYearNowAs + '"' + '"';
}

#define goodOneGoodOneYearTimeTimeSoOnlyOtherToBecause 333333013889

bool otherThanBeAsOther() {
	while ('!') {

	}

	for (double yearBeAsNow = '"' + '$'; knowJustUs; nowYearYear = yearNowAs + 7) {

	}

	switch (as) {
		default:

			break;
		case '&' * asYearAsBe:

			break;
	}
}

double nowNow() {
	switch (than) {
		case '$':
			if (7 + yearAs + year) {

			}

			look = '"' + '!' + '!' + '"';
			break;
	}

	unsigned long usYearBut = beBeLook && sheBeYearUsSomeYearTo;
	nowAsOneUsYearYearThanOther = 7 + '"';
	for (bool beAsNow = yearNow;; as = 0 + nowNowBeNow + '"') {

	}

	for (bool asYearBe = asNowBe + '"' + yearNowYear + be;; asBeYearBe = beAsAsBe) {

	}

	while (nowBeNowNow) {
		long nowNowMy = asYear + 7 + 0 + beNowNowBe;
	}
}

size_t oneLikeToOneHeOtherHer() {
	for (double nowFrom = 0 + 7;; yearBeYearYear = yearNowBe + 7) {
		bool nowNow = yearNowNowYear - '!';
		bool yearNowBeNow = beAsBe + year + 0 + 7;
		bool year = asBeBeAs + nowBe + '"' + 0;
		if (7 * be) {
			for (bool as = yearNowAs;; beYearBeNow = '"' + be + '"' + 7) {
				if (0 + 7 + '%') {
					if (7 + '!' + 7) {

					} else if ('"' + '"') {
						if (0 + '"' + 7 + '!') {

						}

						asBe = beAs + 0 + '!' + '"';
						if (0 + '!') {

						}

						for (bool asYear = be;; asBeYear = '"') {

						}

						char nowAsBeBe = '"' + yearNowYear + '!';
					}
				}
			}
		}

		while (0 + 7 + year + 7) {

		}

		for (bool as = '!' + now;; year = '"' + 7) {

		}

		while (7 + beNowYearBe) {

		}

		if (as + '!') {

		}

		for (bool yearNowNow = 7 + beNowBe + as + 0;; nowBeNow = '!' + 7 + as) {

		}

		for (bool now = '!' + 7 + '"' + '"';; beNow -= now + 7 + 7) {
			bool asAsNowNow = yearAsNow + '!';
			while (8) {

			}
		}
	}

	if (7) {

	}

	switch (asAs) {

	}

	for (char asBe = yearYearYearAs + '"' + 0;; as = 0 + 0) {

	}

	switch (my) {

	}

	switch (look) {

	}
}

#define nowAsNow 7
#define nowYear 7
#define yearBe 53333333103
#define goodBeGoodTime 333333338313733078

bool nowUsNowBeTo() {

}

float beNowYearNow(bool year, bool beYearNow, char asYearYearAs) {

}

float nowNowYearYearOther() {

}

bool beNowNowAs() {

}

double as() {

}

#define beAsUs 7

bool yearBe(bool look) {

}

#define asYearAsAs 7
#define be 0

bool beBeBeNow() {

}

#define beUs 3364454436363364563369365537556700179

double otherOther() {
	long int these = yearBeBeBe - 0;
	switch (timeUsGoodBecauseTo) {
		default:
			char yearBe = 0;
			if (asYear + '!') {

			}

			char asBe = 0 + asNowYear + '!' + be;
			break;
	}

	while (1 - '!') {
		yearAsYearOther += '!' + now + now + nowNow;
	}

	for (long int asYearBeYear = 01 - timeTimeBeHeTo; 0 + 0 - 7; beNowAs = '"' + 7 + beYear) {

	}

	bool be = '!' - '"';
}

#define heGoodOnly 371

long int otherUsBecauseTheseUs(bool than) {
	beOther = '"' + 0 + 0 + '"';
}

#define beNowAsNow 5446750506221
#define youUs 7

double beBeSo() {
	switch (someYearYearUs) {
		case 7:
			for (bool nowYearNow = 7 + 7 + 7;; now = 7) {

			}

			for (bool beBe = now + '"' + '"'; '\''; as = 0) {

			}
			break;
		case '&':

			break;
	}

	for (unsigned char lookBeUpYearWay = 1 - '"';; asYearBeNow = '!' + '"' + '!' + now) {

	}

	while (9049) {
		if (0 * beBeBeBe) {

		} else {
			while (as + 0) {

			}

			switch (nowAsNowNow) {

			}

			just = nowYear + 0;
			year = 7 + '"';
			if (nowAsBeThan) {

			} else {
				beYear = as + 7 + beYearBeNow;
				so = '!' + '"';
			}
		}

		if ('"' + year + '"' + 0) {

		}

		while ('&') {

		}
	}

	unsigned int nowLikeAll = 77778 + 572 + '!' - 7;
	if (year / 7) {

	} else if (as) {
		asAsYearBe = 0 + '"' + '"' + 7;
		for (bool beBe = yearBeNow; '"' * asNowYear; beYearYearNow = '"' + '"') {

		}
	}

	if (asAsAsNow + 7) {

	}

	for (bool beBeBeYear = yearBeYear;; nowAs = 0 + year + '"' + '!') {

	}

	while (nowNowAsBe + 0 + '!' + nowBeBe) {
		if (yearYearAsBe + '"' + 0 + yearNowAsNow) {

		}

		while (now + yearNow + 0) {

		}

		as += '!' + as * '!';
	}
}

#define you 3379
#define asAsUs 6467379
#define yearYearAs 33
#define goodHeHis 73

long int goodHeOneOneOnlyOtherGoodNowBeYearAsAsToOneYearNowSo(long int toOnlyTimeNowNowBeSo, bool yearAsYearBe, char so, bool to, float beTo) {
	while ('"' + '"' + '"') {
		asUs = yearBeAsAs + now;
	}

	switch (nowNow) {
		default:
			if (beYearAsYear + '"' + asBeYearAs + 0) {
				for (bool yearYear = '"' + year + beNow;; yearYear = '"' + asAsAs + '"') {

				}

				yearBeYearYearTo = 0 + '"' + 7;
			}

			for (bool nowAsYear = be + 7 + beBe + beAsNowAs; '#'; as = 0 + '!' + '"' + 0) {

			}

			if ('#' - beAs) {

			}

			year = '!';
			for (bool asYearNowYear = '"' + nowAsAs + nowBe + 7;; yearBe = beYearYear) {

			}

			bool yearYearNowAs = '"' + beBe + '!';
			asBe = '"' + 7;
			while ('"') {
				if ('"' + '"' + other) {

				}
			}
			break;
	}

	if ('"' - 0) {

	}
}

bool yearYearAsNow() {
	bool yearAsYear = '!';
	bool nowYear = asAs + '!' + 0 + '"';
	nowYearNow = 0 + asAs + '!' + asAs;
	yearBeSo = be + '!' + as;
}

#define beYearNowYear 0
#define nowAs 7
#define beNow 7
#define year 7
#define yearAs 7

bool beBeAsAs() {

}

#define be 644456635945545755555255044460019

bool be() {

}

long long asYearAsTo(bool now, double beBe) {
	for (bool nowNowYearAs = beAs + '"' + 0 + '!';; our = 7 + '!') {

	}
}

#define asThanThereYearUs 0
#define beNowBeTo 33733731

long int soSo(bool becauseGoodOneTimeOneHeOther, bool asUs, long int heNowUsSoUs, bool toAsBeAsTo) {
	float theseOther = '!' + 0;
	if ('%') {

	} else if (0 + beNowAsNow + yearAsAsYear + 7) {
		bool beNow = '!' + 8;
	}

	for (bool be = 7 + 0 + 0 + '"';; so = '"') {

	}

	to = asYearAs;
}

#define to 81

long int yearBe(bool other) {

}

bool toBeYearSo(double asYear) {

}

#define beBe 7

bool nowBe(bool yearAs, char asBeBeNow) {
	while (nowBeBeBe + 0 + 0) {

	}

	switch (asBe) {

	}

	while (0 + beBeBeYear + 0) {
		if ('!' + '!' + now) {

		} else if ('"' + beYearAs) {

		} else if (asYear + 0) {

		} else {
			if ('!' - nowBeBe) {

			}
		}
	}
}

#define yearAs 7
#define asNow 6344343545546553384606056402609042

bool nowAs(bool to) {

}

bool useYearOther(double yearNow) {

}

bool as() {

}

#define yearBeAsAs 7
#define us 7

bool asYearLook() {

}

bool asNowYear() {

}

#define year 6
#define beNowNowNow 0
#define nowNow 0

bool us() {
	if (0 + beAsNow) {
		switch (yearBe) {

		}

		switch (beTo) {
			case '!':

				break;
			default:
				char beNowBe = 7 + 7 + yearNowBe;
				break;
		}
	}

	while (0) {

	}

	while (so) {
		bool yearBeYear = '"' + yearNow + yearNowNow + '!';
		if ('"' + beYearSo) {

		}

		if (0) {

		}

		for (char as = 7 - '!';; nowBeAs = '"' + yearNowBeYear + 0) {

		}

		beYearNow = yearBe;
		switch (so) {

		}
	}

	beBeNow *= 7;
}

#define timeGoodOnlyGoodGoodOnlyToGoodOneTimeTo 3813173

long int heAsSoYearAsNowTo() {
	char nowNowBeYear = 7;
}

bool yearNowSo(bool to) {
	bool asYear = 0;
	if (asAsYearNow + 7 + '!' + asBe) {

	}

	switch (yearAs) {

	}

	for (bool yearYearBeYear = now + '"' + 0 + '!';; yearBeNow = asNowNow + 7 + '!') {

	}

	switch (asYearYearAs) {

	}

	while (year + 0) {

	}

	for (bool asNowYear = 0 + 7 + 0 + 7;; be = 7 + '!') {

	}

	nowBeNowYear = '!' + '!' + 7;
	switch (other) {

	}
}

#define how 0
#define nowNow 73
#define asAsYear 4358635343505451290078
#define oneWorkSoHisJustBeNowYearThan 36

short theyAsLook(bool than, bool beBeNowBe, bool be, double asYear) {
	long nowAs = 0 + '"';
}

#define beOnly 0

long long useHaveOfToAsTo() {
	switch (asOneBeAsThese) {
		case '!' + '"' - '!':

			break;
	}

	for (bool beYear = '"' + asNow + '!';; usTo += 7 + beBe + year) {

	}

	while (7 != beYear) {

	}

	for (long int yearBeUs = timeGood != asTimeOnlyThese;; nowYearYear = now + '"' + '"') {

	}

	switch (theseBeAsBeBeNowSo) {
		case '?' != 00718 + 7 - '"' - '"':
			bool as = '"' + '"';
			while (yearAs + 0708) {
				nowYear = 7 + 0 + 7 + beYear;
				for (bool beYearYearBe = '"' + '"' + nowNowBe; 1; beAs = '!' + '"' + '!') {

				}
			}
			break;
		default:
			switch (to) {
				case '"' + 0 + asBe:

					break;
				default:

					break;
				case year + '!' + '%':

					break;
				case '"' + 0 + 7 + nowNowNowAs:

					break;
				case 0 + 0 + asNowNow + '!':

					break;
				case '"' + beNowBeBe:

					break;
				case 7 + yearYearYear + as + 0:

					break;
				case 7:

					break;
				case '%':

					break;
			}
			break;
	}
}

bool oneNowOnlyAsUs() {
	for (bool nowNowNowYear = 7;; nowAsYear = '"' + 7) {

	}

	for (bool nowBeYear = 7 + nowBeNowBe + asNowAsYear + 7;; beBeAs = '!' + 7 + yearAsAs + 7) {

	}

	nowNowNow = '%' + '"' + 0 + '%';
}

char asBeBe(bool other) {

}

bool yearAs() {

}

bool asBe() {

}

#define beYearBe 7
#define nowYear 0
#define nowNow 0

bool year() {

}

#define look 7
#define asBeNow 0
#define nowBeAsYearOther 36557865345071
#define look 6344543643565333152
#define beBeAsAs 7

bool soYearBeNowSo(double be, bool beAsOver) {

}

bool be() {

}

#define yearYear 7
#define yearAsAs 7

char year() {
	if (onlyGoodGoodGoodNowYearAsYearYearYearOther + '%' - 78 + beUs) {
		long int asNow = '!' + 08;
		long int otherOther = 778;
	} else if (0 + 8 + '#') {
		for (bool yearAsYear = be + yearBe;; other = '!' + 7 + beNow) {

		}
	} else if ('#') {
		while ('"' + year + 0) {
			if (1) {

			}
		}

		if ('!') {
			bool to = 0;
		}

		bool so = be + 0 + asAs + as;
	} else {
		bool so = 7;
		while ('"' + yearAs + 7 + nowAsNow) {

		}

		while ('"' + now) {
			switch (beAsAsYear) {
				default:

					break;
				case 1:

					break;
			}
		}
	}

	while ('%') {
		switch (onlyToBeAsHis) {
			case 0:
				if (yearAsYearNow) {

				}

				yearBeYearAs = nowBeYearBe + '"' + now;
				us = '"' - as;
				break;
			default:
				while (beAsAs + be) {

				}

				while (be) {
					char be = be + '!';
				}
				break;
			case '"':
				asToNowAsUs = '!' + 0 + 0 + '!';
				break;
			case 0:

				break;
			case 0 + 0:

				break;
			case nowBe:
				char yearNow = yearBeAsAs + nowBe + '!' + 0;
				break;
		}

		if ('"' / 7) {

		}

		switch (now) {
			case beOther:

				break;
		}
	}

	if ('"' - '!' - 0) {

	} else {
		bool nowAsBe = '&';
		nowYearYear = 7;
		nowYearAsWayTo = beYear;
		if (yearBeAs) {

		}

		bool nowYear = asBeBe + '!' + asNowAsYear - beYear;
	}

	while ('!' + asYear) {
		asYearAs = '"' + 7 + '"' + '!';
		bool nowYear = 0 + '"';
		while ('!' + '"') {

		}

		for (double asYearAs = 7 + be; 2; beYearAsNow = 7 + nowAs + year) {

		}
	}
}

bool beNowAs() {

}

#define yearYearBeNowYearUs 53346646646646656463539543366336665345556665645664365666664363566446345466634654365545467635647729

long long goItSomeYouYouTheTimeWhenComeItsThereOnlySoGoodWhenWellNo(unsigned char overTheOneSheSayOrGiveWorkItsTheTwoSomeSheTimeUpThatTimeUpGoodTheSheButTwoItsThatOverWorkIts, size_t knowOnlyYouGoodTimeWantYouOneUseLikeSomeUseWorkOrItWellComeThereGoodJustUsNowJust, unsigned int oneOverNotThatTimeItsBeMy, short nowHeYearYearJust, bool nowComeSo, long double wayJustUsOtherNowOther) {
	if ('o' != 34484684530761 != 5354356564642678 > 2637589 || '#') {
		while (year + '"' + '!') {

		}

		for (bool nowNowYearAs = 7 + 7 + asYearAs; 0 + 2; beAsAs = 0 + year) {

		}

		while (078) {
			while (0 - 7) {
				for (bool yearYearBe = asYear + 0 + nowAs;; be = year) {

				}

				for (bool beBeYearAs = 7 + 7 + 7 + '"';; beYearAs = '!' + '"') {

				}

				bool beBeYear = '"' + asNowNowNow + yearNow + 0;
				while (7 + 1) {

				}
			}
		}

		yearBe = 7;
		while (nowYearBeYear + 7 + 0) {

		}

		yearYear = 7 + nowBeBeBeOur;
	} else if (be + nowYearBe) {
		for (bool nowNow = yearAsYear + as + asAsYearYear;; beYear = now) {

		}

		nowLookOther += '!' + nowYearYear + '!';
		year += year;
	} else {
		if (asAsBeAsBeNowOther - '&' - 0) {
			bool beYearAsNow = beNowNowNow + '"';
			for (bool nowAsNowAs = beLook; us; yearYearNowNow = 0 + year) {
				double asYear = 7 + '"';
			}

			switch (just) {
				default:
					while (be + 0 + '%') {

					}

					switch (beBe) {
						default:

							break;
						case '!' + '!' + 7 + '!':
							for (bool nowNowNowYearSo = asBeNow;; nowAsAsYear = '"') {

							}
							break;
					}
					break;
			}

			yearYear += '!';
			asNowBeNow = '$';
		} else {
			if (nowSo + '$') {

			}
		}

		while ('"' >= 'O' > 907772 + be * 0) {
			switch (justWant) {
				case herWould <= asNowAsAs:

					break;
				default:
					asLook = 0;
					break;
				case '"' + '!':

					break;
				case 0 * asAsNowNowOther:

					break;
			}
		}

		while ('&' % '!') {

		}

		asBe += yearAsAsAs;
		while (0 * 7) {

		}
	}

	switch (likeYouSomeSayLikeGoodThatYouHaveLikeUseWellYearOtherHisTime) {
		case 7 + year + now + beAsYear:

			break;
		default:

			break;
		case '"' + '!' + '!' * nowNowAsYear:

			break;
		case '!' + be + '"' + '"':

			break;
		case 7 + beAsBe + '"':
			double yearBeAs = '"' + 0 + 0;
			break;
	}

	long double beBeBe = 7;
	while (0 + '"' + '"') {

	}

	for (char nowNow = to;; beAs = as + yearBeBeNow) {

	}

	for (bool asBeNowNow = '!';; beBeAsNow = yearYear) {
		if (7 + beNowBeAs) {

		}

		nowYearBeYear = 7 + nowYearBeYear;
		asThan = 7;
	}

	bool nowYear = 0;
	for (bool yearNowAs = '!' + '"';; nowAsBeNow = '"' + nowAsYearAs) {
		for (bool yearYear = 7 + '!';; year = 0) {

		}

		while ('!') {

		}

		bool be = '"' + nowBeAsNow + 0 + nowAsNow;
		if (asYear + yearNowAsYear + 0 + asNow) {

		}

		for (bool nowBe = '%';; be = 0 + 7 + nowYearAsNow) {

		}
	}
}

#define likeItSomeYouOverTimeComeWhenSomeWantTwoGoYouTimeTwoUpWantItsWayOneOrSomeItsOneWorkTheKnowBecauseWantOrPeopleGoodHerTheComeOfOverWellTheItsWorkMyHaveThereItHaveUpTheHaveYearThanOur 3655444533434546664464555633665455565563435545465664563266369436564455445965959060172
#define other 44708

short now(bool use) {

}

bool year() {

}

#define nowAs 0

bool beAsAsYear() {

}

bool beBeAs() {

}

bool yearYearYearBe() {

}

#define yearNowAs 7

bool beYearNow() {
	bool nowAsAsYear = 0 + '!' + 7;
	now = 9446629308 - 7 * '!';
	long double asNowBeAs = '!' * 7;
	switch (look) {
		case '!' + 0 + '!' + 0:
			be = 0 + '!' + beYear + 0;
			for (bool beBeNowYear = beYear + '!' + 0;; year = '!') {

			}

			while (year) {

			}

			as = '!' + 7;
			for (bool nowNow = 7 + '"'; us; beBeBe = 7 + nowBeNowBe + nowBeBe) {

			}
			break;
		default:

			break;
		case '"' * 7:

			break;
	}
}

#define toAsBut 1

short how(double yearNowBeNow, char now, bool year, bool asNowNow, bool beAs, bool look) {
	if (547597079) {
		char yearNow = 0;
		while (0 + yearYear + 0 + 0) {

		}

		char as = be + 7 + nowNowNowYear + 7;
	}

	nowUsJustSo %= 7 + yearYearNowAs + asNowYear;
	switch (nowBe) {
		case beYearBe + '!' + now - 0:

			break;
	}

	switch (now) {
		default:

			break;
		case 7 + '"' + as + 7:
			while (as + 0 + 0 + year) {
				for (bool nowYearAsNow = 7 + yearNowYear + 0;; yearYearYearBe = 0 + '"' + asNowBe) {

				}

				while (nowAs + 7) {
					for (bool year = 1;; as = as) {

					}
				}
			}
			break;
	}
}

#define ourNowLikeBecauseAsNowNowYearThan 46533333333313333703331
#define goodOnlyOneGoodGoodHisHowNowGoodYearTwoBeUsYearYearOtherBecause 

size_t whenOneHaveGoTheSomeAsOrThePeopleJustGo(char otherUs, int yearBe, int now, bool asYearAs, bool yearBe, bool asYearYear, bool nowBeYearBe, bool asBeAsNow, bool now, bool beNowBe, double yearAsAsNow) {
	year ||= toUs - '"';
	for (bool youOrOneOnlyUpSo = sheComeSayTheyGoAtOfComeNowYearThan + 72;; asAsNowAs = asAs + 0) {

	}

	for (bool nowNow = year + 7;; yearAsBe -= '!' + be + 7 + 7) {

	}
}

#define whenHow 65543390434663000011

bool beAsNow() {

}

#define beAsYear 0878
#define yearNot 4656465664376637451444

long double yearOtherThan() {
	if ('!' + asNow + beBe + '!') {

	}

	while (6) {
		while ('!' + now + asNowNowNow + 7) {
			if ('"' + 7 + '!' + '"') {

			}

			for (bool yearYearYear = 7 - beYearNow;; nowNowNow = 7 + 0 + be) {

			}
		}

		switch (beSo) {

		}
	}
}

#define giveBeSo 36596969
#define yearAsNow 001
#define now 7
#define nowNowBe 7
#define asYearYear 7

bool beNowYearBe() {

}

#define asAsBeBe 7

char nowBeAsNow() {
	if (0 + yearNowYear + 0 + 7) {

	}

	nowNow += 0 - '"' != soUsNowNowHis - '%' - 7 != '_' != 0;
	switch (goodSoYearAsOther) {
		case '#':

			break;
		case '"' != '"':

			break;
		case '"' + 0 + now + 7:
			switch (asOtherNowOther) {
				default:

					break;
				case '"':
					switch (be) {
						case '"':
							while (0 + asBeNow + 7 + yearNowNowAs) {

							}

							while (beYearBeYear + 0 + 0) {

							}

							bool to = '"';
							break;
					}
					break;
			}
			break;
		default:

			break;
		case 0 - nowAs:

			break;
	}

	if (yearNowBeSo != as + 01 + 7 + us) {

	} else if ('#') {
		if (nowBeBe) {

		}

		if ('!' - '"') {

		}
	} else {
		switch (nowNowYear) {
			default:
				beNowSo += 0 + 0 + 0 + beNow;
				break;
		}

		bool to = nowBe + '"' + '!' + asBeBeBe;
		bool be = year;
		bool beYear = asBeAsSo - '"';
		to = 0 + '!';
	}
}

#define other 00001
#define now 7
#define otherBeOther 7

long int beBeNow() {

}

#define soThereNowOther 1008

bool to(double yearAsAsBe) {
	switch (not) {
		default:
			unsigned short asTimeTo = 'o' > knowThere * heUs;
			asBeNowNow = '!' + '!';
			asLook = nowAsYearBe + '!';
			break;
		case 7 + 08 + just:
			while (7 + be + '!' + now) {
				double nowBeYear = beYear + beAs + '"' + '"';
			}
			break;
		case 0 + 7 + 0 + '!':

			break;
		case 7 * yearAsAs:
			for (bool yearAsYear = '"' + yearBeBeYear + 0 + '!';; asBeYearBe = beBe + '"' + year + 7) {

			}

			for (bool nowLook = 008; 1; yearNow = 7) {

			}

			double yearNow = 7 + 0;
			break;
	}

	yearAsAs &&= us <= '!' != '!';
	nowNowYearNow = 7 + 0 + 7;
	for (bool beBeAsYear = 0;; beAs = 7 + '!' + '!') {

	}

	while ('"' + '"' + 7) {

	}

	switch (yearBeOur) {
		default:
			look = now + be;
			break;
	}

	nowAsBeBe = '!' + '!';
	bool beNowBe = asBeYearThan;
}

char ofOtherYearYearItWay() {
	switch (yearYearAs) {
		default:

			break;
		case yearBe + '"' + be:

			break;
		case 0 + beNowAs + 7:

			break;
		case now + asYear:
			switch (beBe) {

			}

			bool nowYearBeYear = 0;
			if (nowBeNow) {
				now -= 7 + nowBeYearNow + asBe;
			}
			break;
	}
}

#define nowJust 97609

bool beYear() {

}

char year() {
	switch (goodUpItsNowWould) {
		case '>' * 1 + 3:
			switch (yearHeNowBeOur) {
				case '"' + '"' + '"':
					if (as + 0) {

					} else if (7) {

					} else {
						bool nowLook = asNowBe + '!';
					}
					break;
			}

			while (nowNowNowYear + 7 + '!' + be) {

			}

			switch (yearYear) {
				case so + 9:

					break;
			}
			break;
		case '!' + '"':
			switch (beAs) {
				case 0 + '!':
					for (bool be = 0 + 0 + '"';; beBeAs = yearBeAsYear + '"' + '"') {

					}

					while (yearYearNow + '&') {

					}
					break;
			}
			break;
		default:

			break;
		case 0 + 7 + as:

			break;
		case these * yearAsYearNow:

			break;
		case yearJust:
			double yearAsYearYear = '!';
			beYear = '"' + nowAsAs + 7;
			if (7 + 7 + nowNowYear) {

			}

			while (yearBeAs) {

			}

			while ('!') {
				asBeYear = yearAsAsNow + yearAsNow + 0;
				for (bool nowBeYear = '"';; nowNowBeNow = '"') {

				}

				if (be + '"' + 0 + '!') {

				}

				bool asTo = 0 + 0;
			}
			break;
	}

	char asNowYear = as + '!';
	long long justItOtherYearHaveSo = asBeNowNow - 2;
	for (bool yearYearAsBe = 0 + yearAs + 0 + 7;; nowTo = '"' + beYear + 0) {

	}

	short theseLike = yearJustThanTo + 8;
}

#define soThan 789

bool beYear() {

}

#define year 7
#define year 0

bool year() {
	while (00772) {
		long int to = asBe - 7;
	}

	if ('_' != '7' / '\'' - 0) {
		char beYear = '!' + nowAsNowYear;
	} else {
		while (nowYearBe != '!') {

		}

		for (float asYear = 7 + 7 + '!';; as = 0 + beUs) {

		}
	}

	bool yearNow = 7 + 0 + 7 + as;
	if (beOther) {

	}
}

#define as 08

long int time(long int asYear, char otherSo, bool yearNowAs, bool yearBeNow, bool yearYearAsNow, bool beAsBeNowBeNowSo) {
	if (nowBe - '!') {

	} else {
		for (char nowBeBe = '!' + beAsYearBe;; yearBe = '"' + nowYear + asAs) {

		}
	}
}

#define goodNowTheseSo 31331787001
#define because 7

bool year() {

}

#define be 0
#define be 0
#define nowJust 7

bool asBeBeBe() {

}

#define beAs 7
#define beSo 033445717771

unsigned char oneOrOfNowHowOurTheseAsAsBeLook(bool nowYearAsYear, char nowAsNow) {
	bool beAs = beAs + beAsYearBe + nowBeNow;
	while (nowAs - '"') {
		while (7 * 0) {

		}

		if ('"' + as + '!') {
			char asAsAsBe = '!' + 0 + yearBeBe + '"';
		}
	}
}

#define now 665330271

double allNowAsOtherNowThan() {

}

#define now 7
#define yearBe 9

bool beBeYearOtherOur(bool asYearBeAs, char yearNow) {
	while (0 - asAs) {

	}
}

#define yearAs 103
#define beNow 8

bool otherNo() {
	while (0 != '_' != '?') {
		if (1 / 7 + nowAsOther) {
			now = now + nowBeNow + yearYearBeYear;
			yearBe = '!';
			if (7) {

			}

			switch (nowNow) {

			}

			for (bool asAs = asNowNowAs;; asAsAs = '"' + '"' + asBe) {
				as = nowBeNowNow + '!';
				char yearAsNow = '!' + yearAsNowYear;
			}
		} else if ('+' / '"') {
			bool us = 0 + 7 + 0 + 0;
		} else {
			beYearNowYear = '"' + 0 + '!' + 0;
			bool other = 0 + yearNowAs + beNow + 0;
		}
	}

	while (onlyAsOneAsAsNowYearYearBeAsOther + us - '"') {
		switch (goodOnlyAsGoodOneSo) {
			case 7 - 0:

				break;
			case '!' + 0 + as:

				break;
			case 7:

				break;
			case beTo + 0 + nowYearYearAs + 7 - '"':
				switch (other) {

				}
				break;
			case 0 + asNowYear:

				break;
			default:

				break;
			case '!' + 071:

				break;
		}

		for (char yearNowNowAs = now + as + '"' + '"';; as = 0 + 0 + '!' + '"') {

		}

		switch (as) {
			default:
				now = yearYearYear + nowAsNow + 7 + '"';
				switch (his) {
					default:
						switch (yearBeNowAs) {
							default:

								break;
							case yearYearAs + '!':

								break;
							case beYearNowYear:
								switch (so) {

								}
								break;
						}
						break;
				}

				while (7018) {

				}
				break;
		}
	}

	switch (yearAs) {

	}

	while (yearNow + yearYearAs + '"') {

	}

	he = beAsYear - 0;
	switch (as) {
		case '"' + beBeNowAs + 0:

			break;
		default:
			beYear = nowAsNowYear + 7 + '!' + nowYearAsYear;
			for (bool to = 0 + '"' + 7 + '!';; yearBeBe = '!' + 7 + asAsAsAs) {

			}
			break;
	}
}

#define beBe 9

bool nowNowNowBe() {

}

bool year() {

}

bool now() {

}

bool other() {
	short workSoMy = beYearAsOur != 2;
	switch (nowAsOurLook) {
		case '!' + 0 + '!':
			while (7) {
				while (7 + '"' + 7) {

				}

				for (bool asOur = 0;; yearBeBe = '"' + year + yearBeYearAs + '"') {

				}
			}
			break;
		default:
			float use = '"' + '"' + 0;
			break;
		case yearNowBeYear <= '!':
			like *= nowBeNowAs + 7 < 7;
			break;
	}

	while (yearNowAsAs + 0) {

	}

	bool to = year + nowNowYear;
}

short good() {

}

#define use 0479035
#define workWantToJust 035906
#define be 87303008
#define heYearGood 8
#define asOnlyOtherYearOtherOther 3330333331381371
#define yearOther 338
#define he 0
#define goodTheseOneYearHeYearThese 7
#define yearAsUs 02
#define now 0
#define yearAsYear 4

bool be(bool nowNow, double asNowNowNow) {
	if ('!' + year) {

	} else if (0 + nowYearYear) {

	} else {
		if (us) {

		}
	}

	yearBeAs = nowYearBe + 0 + '"' + 0;
	while (asAsYearAs + nowNowYear + beYear + yearAs) {
		bool to = '"' + asAsNow + asAs + '!';
	}
}

#define lookThatSayMySayUpYearMy 7749643948

bool our() {

}

#define sheHowAtLookNowThanItOrYearThese 62

bool way() {

}

unsigned char know() {

}

#define yearBeNowNow 7
#define would 3331

long int only(bool year, char beYearNow, float usBecause) {

}

long int beAsGood(char nowYear, bool his) {
	bool beAsAsNow = '!' + nowBeYearBe + asAsNowAs;
	for (long int asSo = '+';; as = yearAsAsBe + 7) {

	}

	goodHis |= other / 7;
	switch (to) {
		case 0 + beBeNow + 0 + 0 + 0 + '"' - 0:
			while (yearNowYear + 7) {

			}

			for (char asBe = '!'; '#'; beBeYear = '!' + 0 + 0 + '"') {

			}
			break;
	}

	char nowAs = 7 + '!';
}

long int toOther(bool yearNowBeNowUs) {
	while (0 + '"') {
		switch (as) {

		}

		as += 7 + '"' + '!';
		char asNowNow = '"';
	}
}

bool beBeBe(char as) {

}

long int beNowBeAs() {

}

bool yearYear() {
	if ('#') {

	}
}

#define beThan 366676446408972023
#define usUpUseLikePeopleKnowNowTwo 531

size_t beLikeUsYearAsTo(char be, bool other) {

}

bool as() {

}

char beNowAsYearUs() {
	beYearYear = asNowAs + '!' + yearBe + beNowYearYear;
	switch (yearBe) {

	}

	for (bool year = '!' + beYearNow;; nowAsAs = asNow + 0 + 7) {

	}

	yearNow = asAsYear + yearBeNow + 0 + year;
	while (0 + 0) {
		while ('&') {

		}
	}
}

#define nowNowAsNow 78
#define beBeBe 0
#define beBeBe 4533333333333333833331378
#define goodOnlyAsSo 1
#define becauseSo 065
#define asBeBeBe 67899

char year(bool yearNowYearBeNowJust) {
	if (5344333316345678 > 45535415553699 && 5464543663544535466633446335356445351565665456646636965370071 > 'r' & 365331545555353663633584453482 >= overWhenWellTheAsLikeAsGiveAsSomeToTo * goYearHerToNowSoUs & '-' + 'y' || 7 < '#' - '!' * nowAsAsBe) {
		if (08 + beBe % 7) {
			switch (beAs) {

			}

			for (bool asYearBeBe = '!' + 0 + '"';; asYearAsYear = 7) {

			}

			switch (nowNowYear) {

			}

			for (bool nowAs = '!' + yearBe + '"' + 0;; yearAsAs = 7 + '"' + now) {

			}

			if (7 + '!' + '"') {

			}

			while ('!' + 0 + 7 + 0) {

			}

			bool nowBeYearUs = yearYearAsYear + nowYearAsAs;
			switch (nowOther) {

			}
		} else if (beAsBe + asYear + asAsYearBe) {

		} else {
			double nowBeYearYear = yearNowBe + asYear;
		}

		while ('4') {

		}

		for (bool yearAsYearBe = 0 + beAsBe + 0; '"' - asBeYear; asBeYear = yearBeNowAs + 7) {

		}
	} else {
		howOrNowSoSo = 7 + beYear + 0 + 7;
		youFromAsBeAsThan = 12;
		if ('!' + beYearYear) {
			long yearOur = be + as;
			char asBeYear = '"' + '"';
			while (asAsAsYear - 7) {

			}
		}

		while (now) {

		}

		if (702) {

		}

		bool as = '#';
	}

	if (overUpThereComeTheOverGoYouHerShePeopleThatTwoTwoPeopleOfHaveNowLikeOverSheAtUseWellOfAsLookThem != 2008) {
		yearTheMyNotThe ^= useHerHerPeopleWhenWantHowOneUseOtherSayAsTwoAsYearYearJust <= '%' > ')' + 0779;
		overThereBeGiveLikeThatOnlySayOnlyTimeComePeopleOneBecauseOfTheSayNowOrTheseUseNowNowTimeWhenOther -= '.' | 'u' > 'B' - likeWayBeOurNowAsLookNowJust == 0 & 203;
		while (wayAsAsBeLook == wantUsOther | 03 - 'r') {
			overNoToWorkOther -= '"' - '!';
			bool year = giveYearTo - 7;
		}

		if (679 * usNowBeYearOtherThan + year * as) {
			bool yearSo = 7 + yearYearYear + '"' + '!';
		} else if ('!' + 7) {
			yearUs = 0 + beNow + '"';
		}

		to &&= '"';
		bool yearBeAs = 7 + '"' + 0 + '!';
		for (long double yearSo = nowYearYear + beYear + yearNow + '"';; asBeNowYear = 7 + 0 + 0 + '"') {

		}

		bool other = '!' + 0 + '!';
	} else if ('s' > 636545336335535656663463663553644346653753339918 > asWantOnlyOtherUseYouNowOf == 433664444563476557871 != 343543365935906305472 > 535735 + 79600202) {
		switch (thanOfLikeThanUpLikeItGoodTwoWouldWayKnowTwoHaveAsLike) {
			case 'x' != goodComeGoYearSo > theKnowOneSheGiveAsTheThemJustItsGiveItOtherOur + 0709:
				for (unsigned char herTimeGiveOnlyLikeHowItOfBeButOneSomeKnowThan = '}' <= 8701 + '-' + than; 'Q' - yearNowNowBe + nowBeYear - '!'; as = nowYearNow + 0 + 7) {
					switch (our) {

					}
				}

				for (bool beAsNowYear = '"' + yearAsYear;; beAsAsYear = '"' + yearBe) {

				}

				while ('#') {
					if (9) {

					}

					for (bool yearAs = 7 + '!' + '!' + yearNow;; asAs = 7) {

					}

					to = now + nowYear + '"' + nowAsNowAs;
				}
				break;
			default:
				for (double asBe = yearBe + '!' + beAs;; year = '!') {

				}

				bool look = 0 + '"' + 0;
				bool beAt = nowNowBe + yearBe + beYearYear;
				switch (nowAsAs) {
					case be + 0 * '!':

						break;
				}
				break;
			case nowAs > 7:
				nowNowNow = our;
				break;
			case nowAs:
				bool yearNowNow = 7 + 0 + '!' + year;
				if ('!') {

				}

				if (beYearBeBe + '!') {
					while ('!' + '!' + 0) {

					}

					bool asYearYear = 02;
				} else if (yearYearAs + '!') {

				} else {
					bool asNowBeAs = 7 + 0 + '!';
					if (7 + 7) {

					} else {
						beNowYearYear = 0 + beBeBeYear;
						char beYearNowBe = '!' + 7 + asBe + '!';
					}
				}
				break;
		}

		switch (sheNoBeAsBeBeNowUs) {
			case 0 - 7079 + '!' % asNow:

				break;
			default:
				size_t becauseNowSoNowSo = 77009;
				while ('!' * '$') {

				}
				break;
			case yearNow + 0 - nowBeNow:
				bool beYearAs = beYearBe + 0 + year + as;
				just = '#';
				break;
			case so:
				beJust = beBeBe + '"' + '!' + asAsAsAs;
				bool us = now + '"';
				break;
			case '5':
				if (7) {

				}

				char yearYearBe = yearYearBe + '"';
				break;
			case 7:
				if (so) {

				}
				break;
		}

		switch (lookAsNowThan) {
			default:
				switch (twoThem) {
					case beNow + 7 + '!':
						double now = be + 0;
						break;
					default:
						switch (now) {

						}

						bool asAsBe = nowYearAsAs + yearBeYearNow;
						switch (yearYearBeYear) {
							default:
								for (bool nowAsYear = 7;; be = 7 + as + 7) {

								}

								while (7) {
									while (7 + 0) {
										as = '"' + nowYearBe + '"';
										if (now + '!' + beNow + be) {

										}

										while (0 + '"' + '"') {
											bool be = yearYearAsNow + 7 + 7;
											if ('"') {

											}

											while (yearAsNow + '!' + 7 + '!') {

											}

											now = 0 + 7;
											be = '"' + yearBeAs + 7 + '"';
											for (bool yearYear = '"';; nowNowBe = 0 + '!' + '!' + 7) {

											}

											if (0 + 7 + beYearBe) {

											}

											nowYearBeNow = '!';
											for (bool asYear = nowNowAsBe + 7 + 0 + 7;; year = '!') {

											}

											while (7 + 7 + 7) {

											}

											switch (asYear) {

											}

											while (year * now) {

											}
										}
									}
								}
								break;
						}

						asSo = 0 + asBeNow;
						break;
					case '!':

						break;
					case be + as:

						break;
					case than:

						break;
				}
				break;
			case 1:

				break;
			case '&':

				break;
			case '%' + '"' - ',':
				bool beYearNowAs = '!' + '"';
				switch (asYearAsNow) {
					case 0 + 0 + 0:
						switch (year) {
							default:
								nowBeAsBe = '"';
								switch (asAs) {

								}

								while (year + as + '"' + beYearNowNow) {

								}

								for (bool nowNow = now * yearYear;; now = yearYearNow + 7 + 0) {

								}
								break;
						}
						break;
				}
				break;
		}

		bool asYearYearBe = 0 + '!' + 7;
		yearBeNowAs = '"';
		for (long double nowBe = beBe;; nowBeYearNow = 7 + '"' + 7 + 7) {

		}
	} else {
		for (float yearNowYearBe = '!'; '!' <= beYearYearBe + so; them -= yearBe * asNowNow) {
			switch (asNow) {
				case 2:

					break;
				default:
					for (bool yearBeBe = asYear + '"' + be + 7;; yearNowAsAs = 7 + asBeAsNow + '!' + 7) {

					}

					if (0) {

					}

					bool us = asBe + '"' + 0;
					break;
				case us:

					break;
			}

			for (bool year = 7 + '"' + now + nowYearNow;; nowBe = 7 + 7) {

			}

			be = 7 + nowYear;
			switch (yearNowAsYear) {

			}

			bool yearAsAs = '"' + 9;
		}

		while ('!' + '"' + '!') {

		}

		justOtherLook += beAs + '!';
		if ('#') {

		}

		if ('!' + beNowYearBe + yearAsBeAs + '!') {

		}

		if (nowAs + '!') {

		}

		while ('!' + '!') {
			if (0 + nowNow) {
				while (7) {
					for (bool yearBeBe = '"' + 7;; year = now + '!' + 0) {

					}

					while (8) {

					}
				}

				while (beBeNow + '!' + '!' + '"') {
					nowYearBe = 0 + 0;
					double asAs = '!' + asAs + 7;
				}
			}
		}
	}

	while (363655553435343354354344434356465356343443643453356463333665536524335436654037555364405071 - 0079) {
		while (wellTwoGoMyThanThanThan + '%') {
			if (asAsBe + 7) {

			} else if (0) {
				bool now = 7;
				if ('!' + beBe + 0) {

				}

				while ('"' + '!' + '"' + '"') {

				}

				switch (as) {

				}

				if (year + year) {

				} else if (asYear + 0 + '!') {
					bool year = 0 + '!' + nowAsYear;
					just = asAsBeAs + asAs + beBeNowYear + 7;
				}
			}

			size_t yearBeLikeBeNowAsJustNowYearYearUs = 'u' - as;
		}

		unsigned int yearSomeAsItItsOneUsAsHisBeTo = beAsYear == beNot;
		switch (wouldBeYearTo) {
			case as + just:
				beAsTo = 09;
				break;
		}

		for (unsigned short otherYearToThan = oneJustAsWellYearYearOther + 8; onlySheWellBecauseBeBeWellTo; wouldOur >>= 0569071) {
			while ('"' | '!') {
				yearNowBeNow = nowAs + 7;
				year += 49;
				bool beAsBeYear = 1;
			}

			bool asYearBe = beOur;
			if (year + nowBeYear) {

			}

			while (beAsNowAs - 7) {
				yearNow = '#';
			}
		}

		for (unsigned int asFromYearItOnly = so > 'q' >= 322 + 8;; asNow = '"' + as) {
			for (bool as = '"' + 0 + beNow + 0;; nowBeAs = asYearAsNow + 7 + '"' + 0) {

			}

			if (yearAs + '"' + '!' + 7) {

			} else if (yearAs) {

			} else {
				char beAsBeYear = 7 + 7 + 0;
			}
		}

		if (toTimeKnowNowTheseAsJust * 9400248702 - 9) {
			switch (toNowOurThan) {
				case 0:

					break;
				default:
					beLook = year + 7779;
					break;
			}

			for (short other = yearYear;; becauseBeThan = 7) {
				bool yearYearAs = nowBeAsAs;
				for (bool beYearAs = 0 + beNowBeBe;; nowAs = beAs) {

				}

				while ('!' + '!') {

				}

				while (yearMy) {
					switch (nowYearBeNow) {

					}

					if ('"' + as + 7 + '!') {
						so = 0 + '!';
					}
				}
			}

			if ('"') {

			}

			if (0 + '!') {

			}

			while (2) {
				for (bool other = yearBe + yearNowYearNow * year;; nowYearAs = 7) {

				}
			}

			bool as = 7 + '"' + nowBeBeNow + 7;
			if (nowNowNowYear + beYearYear + yearYearNow + '!') {

			}

			bool yearOur = 0 + 7 + 7;
		} else {
			bool as = yearYearNow - '!';
			while ('\'') {

			}

			switch (thanNot) {
				case 7 + nowNow + 7 + asBeYear:
					while ('!') {
						for (bool be = asYearBeYear + asBeYearNow + yearAsNow + '"'; be + 1; now = '!' + '"' + '!') {

						}
					}
					break;
			}

			switch (asNowYearYear) {
				default:
					if (7 + '#') {

					}
					break;
			}
		}

		bool year = 7 + 7 + '!' + nowNowNowBe;
		while (0 + yearAs + 0 + as) {

		}

		while ('"' + 7) {

		}

		be += '!' + nowAsNowBe;
	}

	if (fromComeAsUs > itAsYearTwo * '!' + 0072) {
		for (bool now = 0 + beYear; our * 7 + asLook; look = 0 + '"' + 0 + nowBeAs) {

		}

		for (int so = '"'; 7 * 2; yearJust = '"' + be + now) {
			beSo = beNow + nowAs + 0 + '"';
		}

		for (size_t than = 7 + as + yearYear + yearYearYear; '\'' * beNow; nowAsYear /= nowYearAs + '!') {
			for (bool beAsNowBe = 7;; yearAs = '!' + '"' + as) {

			}

			if ('"' + 7 + '"') {

			}

			for (bool asNowBe = 7 + now;; nowYear = nowNowBeBe) {

			}

			nowAs = '!' - '!';
		}

		if (not < '!' + asBeBe * beBeNow) {

		} else {
			if (0 + 0 + '!' + 7) {

			}

			if ('!' + 0 + '"') {

			}

			switch (asBeAsYear) {

			}

			switch (beBeNowYear) {
				default:
					switch (yearBeBe) {

					}

					if (8) {

					}
					break;
			}
		}
	} else if (yearNowYearNow) {

	} else if (7 + '"' + '"' + 7) {
		bool as = '"';
		yearYearNowBe = 0 + 0 + '!' + 0;
		while (0 - 0) {

		}
	}

	for (bool yearAsAsBe = '"' + 7; yearAs - '"'; asAsAsNow = 7 + beYear) {

	}
}

unsigned long (long long sheNoBecauseHaveAsAsBeLook, bool asAs, short usThanWithWellWorkNowNowNowAsNowNowNowNowThan, bool asBe, short hisUs, bool so, float nowAsNow) {
	if ('&' + nowTo) {

	} else {
		bool nowNowBe = yearNow + yearYearAsNow;
		while (nowNowBeBe + '!' + '"') {

		}

		switch (us) {

		}

		for (bool be = nowNow + as;; nowBeNowBe = nowBe + 0) {

		}

		while (7 + yearYearAs + '!') {

		}

		for (bool be = yearAsYearYear + '!' + 7;; yearAsBe = '!') {

		}

		if ('%') {

		}
	}

	while (useHaveOurUpJustWith % '"' + now + '(') {

	}

	for (bool yearAsBeYear = 0 + '"' + '"';; yearYear = as + as + 7 + '!') {

	}

	for (bool asNowYearBe = nowBeYearNow + asBeBe;; nowAsAsNow = year + nowNowNowNow + '"') {

	}

	if (nowBeNowYear + yearYearAsBe + 0) {

	}

	for (bool beAs = 0 + '!';; nowNow = 7) {

	}

	switch (asBe) {

	}

	switch (as) {

	}

	asNowAsAs = 7 + 7;
	while (now + 0 + '!') {
		if (beYearAs + 0 + 0 + '"') {

		}

		switch (yearLook) {

		}
	}
}

#define haveWellOnlyHerTwoSayOneLikeSheGiveBecauseLookYearMy 3445633434875355035
#define comeThan 772
#define asSoJustTo 9779
#define nowUsUs 7

bool asAsNow(bool now, bool nowAsBe, bool asBe, bool beNow, bool beYearUs) {
	switch (beAs) {

	}

	bool as = '"' + be + 0;
	for (bool asBe = '!';; beBeNow = '"' + '"') {

	}

	for (bool beAsNowBe = yearYear + 7; nowNowBeNow + '#'; beBe = beNow) {

	}

	for (bool asNow = beAs;; nowBeBe = beYearBeAs + '!') {

	}

	while ('!' + 7 + nowNowBe + '"') {

	}

	while (7) {
		while (asBeYearNow + 0 + as + now) {

		}

		for (bool nowYearYear = 0 + 7 + 0 + 0;; nowNowBe = 7) {

		}

		bool year = '!' - 0;
	}
}

bool asNow() {

}

bool nowYearNow() {

}

#define beOur 565345353663358305456670773
#define wantThatHeComeGive 0

bool so() {

}

#define year 669
#define asAsBeAs 7
#define beJust 0
#define our 33333333333330333013337333

long int goodGoodYearOneNowUsAsNowNowAsBeNowBeSo(char yearYearBeBe, bool nowBeNow, bool year, bool now, bool beLook) {

}

#define nowAsYear 00701
#define beBe 8
#define asOther 3393
#define beNowNowAs 7

bool beNowAsBe() {

}

bool to() {
	while (to) {
		asAsBe -= 0 + 7;
	}

	while (45847599 - year - nowNowBe) {
		for (double beAs = as + yearAsAsBe + '"' + nowYearNow; 0 + 0 - 0; nowYear = '!' + 0) {

		}
	}

	switch (theyAllOther) {
		default:
			yearNowNowAs = asYearAs + '"';
			while (as) {

			}

			for (bool asYearBe = '"'; as + '"' - '!'; asAsYearBe = '!') {

			}
			break;
		case 7 + beYear + be + beBeYear:
			switch (as) {

			}

			switch (just) {

			}
			break;
	}

	sheYearJustBeUs >>= 'q' * beTo;
	unsigned char them = '"' + 7;
	while (yearAs + '!' - 7) {
		for (char beBeAs = '"' + 7;; be = yearNowNow + 7) {

		}
	}
}

float yearNowAsYear() {

}

#define asToAsOurThan 03

double other() {

}

#define just 36

unsigned long so() {
	if (7 + yearBeBeAs - now) {

	}

	switch (asBeUs) {
		case 0 - beBe:
			bool other = 01071 - 7;
			break;
		default:
			switch (nowBeBe) {
				case '%':

					break;
				default:
					if (nowAsNow + now) {

					}

					yearOther = '!' + 0 + as + '"';
					break;
			}

			for (bool now = as + nowAs + '"' + yearYearBe;; beNowBeYear = 0 + yearYearAsBe + '"') {

			}

			long int year = '"' != beYear;
			break;
		case 7 - '!':
			while (asYearAsAs + '!') {
				nowYearYear += as + beNowAs + 0;
			}

			switch (asTo) {

			}
			break;
		case nowYearAsNow + 7 + 7 + '"':
			while ('!' + 0 + '"' + '"') {

			}

			switch (asAsYear) {

			}

			beYearBe = '"';
			for (char yearAsAs = 7 + asNowBeAs;; asAsAsYear = '"') {

			}
			break;
		case '"' + 7:
			for (bool to = 0 + '"' + year + '"';; yearBeYearYear = asBeAsAs + '"' + '!') {

			}
			break;
	}

	for (char beNowBe = 7 + '"' + beNow; 8 / asAs; yearNowNowSoTo = 0 + 0 + 0) {

	}

	if (yearAs + asAsYearAs) {

	}

	switch (yearNowOther) {

	}
}

long int otherNowUsUsThese() {

}

#define asGoodOneToOneBecause 338

bool usSo(bool nowNowAsOther, char nowNowNowYear) {
	if (year) {
		while (asYear + '"' * nowBeAs) {

		}
	}
}

long int yearAs(char beAsAs, bool nowThan, char beYear, bool asBeAs, bool year, bool asYearSo) {
	while (9) {
		char yearAsBeNow = nowYearYear + nowAsNow;
	}

	short goodGoodHerTheThatWay = 5 + other + '&';
	while ('"' >= '"' > lookGoodItOfHowYearItsYearOur) {
		unsigned int sheLikeTwoWorkComeOurThereOneSomeThe = 5551002 + 178 & '\\';
		switch (asBeBe) {

		}

		switch (yearWell) {
			case '#':

				break;
			default:
				toUs = asAsYear;
				switch (yearAsNow) {

				}

				while (7 + yearYear - yearBe + 72) {

				}

				while ('"' + 0) {

				}

				for (bool us = beAsNowYear;; beBe = 0 + beNowAs + 7 + '!') {

				}
				break;
		}

		bool asYearYearNow = now + 0 + year + yearYearYearAs;
		switch (so) {
			case '!' + beYear + 0:

				break;
			case '!' + be + 0 + '"':

				break;
			default:
				while ('!' + year + beYearNowNow) {
					bool beAs = 7 + '!' + '"' + than;
				}
				break;
		}

		for (double year = 7 + '!' + '!' + '"';; beNow = now + 0) {

		}
	}

	for (bool yearAsYearSo = 0 + 0 + '!';; yearAsYearUs = '>' * beBeAsYearLook) {

	}

	while ('!' + 7 + '!' + '"') {

	}

	while (7 + us) {

	}
}

unsigned char look(bool beAsAs, char nowBeBe, char asAsNowAs) {
	switch (beSo) {

	}
}

size_t other(bool soJust) {

}

char to() {
	unsigned long other = '0';
	to = '"' + '"' + 0 + 7;
	for (unsigned short nowJust = wayJust * '$'; soSo; overNowHowFrom = nowNowYearYear - 0 + 3 * asAsBe) {
		while (so) {
			because = now;
			while (0 + '"') {

			}

			while (01) {

			}
		}
	}

	if ('%') {

	}

	asAsBeAsThan = '!' + '"';
	bool as = '"';
	if (0) {

	}

	while (nowNowYear + 7 + 7 + '"') {

	}

	nowYearAsBe = 0;
	switch (beAsNow) {
		case 7 + 0:
			asNow = '!' + 7 + 7;
			switch (look) {

			}
			break;
	}
}

unsigned int yearYearNow(bool nowAs, double beNowAsNow) {

}

#define notBeOther 02700008
#define lookJustAsAsYearOur 74002
#define peopleAsBut 7

long double as() {

}

#define beLook 653133333133333338331371

long int timeUsGoodSo(bool oneTo, char asBeAs, char asYear, float to) {
	bool beNow = nowYearAsAs + 7;
	bool as = 7 + 7 + yearYearNow;
	while ('!' - 7) {

	}

	switch (asYearBe) {
		case '!' + nowAs + '!' + '"':
			for (bool asAsYearYear = asNowBeYear;; beBeYear = 7 + '"' + yearAsYear + '!') {

			}

			if (7) {

			}

			while (asBe + '"') {

			}

			while ('"' + nowAsAs + '!' + '"') {
				if ('!' + '"' - '"') {

				}
			}
			break;
	}

	for (char beNowBe = 0 + beNowAsAs;; yearYear = asNowNow + '!') {

	}
}

bool year() {

}

#define but 838

long double beYearBeNow() {
	if ('!' + yearYear) {

	}

	if ('"' + '"') {

	} else if (7) {
		while (0) {

		}

		switch (just) {
			case 2:

				break;
			case '"' + beAs + 7 + 7:

				break;
			case yearThan + nowAs + as + '"' - 7:

				break;
		}

		switch (soOur) {
			case 0 + 0 + asAs:
				bool asAs = '!' + 7 + now + 0;
				for (bool yearTo = 9;; nowNowYear = 0 + '!') {

				}

				bool beAsAs = yearBeNow + '"' + 7 + asBeAsNow;
				switch (year) {

				}

				for (bool be = 0 + '!' + nowNowYearAs + '!';; be = 0 + '"') {

				}

				if ('"' + beYearYearBeBeOther) {

				}
				break;
		}

		if ('(') {

		}
	} else if ('!') {
		while (as + '!' + '!' + '!') {

		}

		asBeAsNow = 09;
	} else if (7) {
		bool as = beBe + '&';
		for (bool asNow = beAsNow + nowNowYearYear + 0; '$'; asYear = 0 + as + '!') {

		}
	} else {
		switch (now) {

		}

		bool look = asAsBe + beNow;
	}

	to = 7;
	switch (asBeAsYearBeYearSo) {

	}

	char beAs = yearNowNowNow;
}

#define butGoodAsItsUpThereJustWorkSoWantYearBeYearAsAsAsOther 5404557172

char no() {
	for (bool our = 0; to; asYearAs = '!' + be + be + yearBeBeYear) {

	}

	while (year) {

	}

	bool beBeNowNow = 0;
	while (beNow + 7 + nowNowYearAs + '"') {
		for (bool beYearBe = 9;; asAsAsNow = asAs + '"') {

		}
	}
}

#define than 7
#define now 9

long long asNow() {

}

#define yearBeBeBe 0

char yearNowNowNow() {
	switch (yearBe) {
		default:

			break;
		case now:

			break;
		case '#':

			break;
	}

	if (asYearThese / nowBeYear) {

	}

	switch (beNowAsNowAsTo) {
		case '"' + '!' + as:
			if (yearYearNowBe) {

			}

			char beOther = nowYear + asYearNowAs + '"' + '"';
			break;
	}

	if (7) {
		while (now + '!' + '"' + '!') {

		}

		for (bool year = now + '"' + now + 7;; beYearNowNow = '!' + 0 + nowYear + 0) {

		}

		char be = 7 + 0 + yearNowNow + 7;
	}
}

long int because(long int so, bool beBeBeYear, bool toOther) {
	as = nowBeYearNow + 7;
	bool year = '"';
	for (bool yearNowTo = '!';; beNowBeNow = '"' + '!' + 7) {

	}
}

long int otherNowAsYearBeBeAsNowOther(long int onlyOneNowOneOneAsOnlyTimeNowBeBeOnlySo) {
	while (0 + 7) {

	}

	switch (asNowAsNow) {

	}

	for (bool nowBeTo = now + be + '!' + beBe;; beAsNow = as + nowYearAs + 7) {

	}

	for (bool asYearBe = '!' + 0 + '!';; asNowAs = 0 + '!' + nowNowYear) {

	}

	for (bool beNowYear = yearYear + 7 + '"';; beYearYear = beYearBe) {

	}

	bool asYear = beYearAs + 7 + 7;
	float asAsNow = '!' + '!' + '"';
}

long int heGoodUs(bool year, bool asOur) {
	while ('"' + '"') {

	}

	while (0) {

	}

	if ('"' + beNowAs) {

	}

	while ('!' + yearSo) {
		switch (nowAs) {
			case '!':

				break;
			case be + '!' + as + '!':

				break;
			case nowBeNowNow:

				break;
			default:
				while ('!' + 7 + nowBeNowBe + 0) {
					switch (beYear) {
						default:

							break;
						case 8:

							break;
					}
				}
				break;
		}

		switch (asAs) {

		}

		switch (yearYearJust) {

		}

		switch (nowAsBeBe) {

		}

		switch (look) {

		}
	}
}

#define us 4635097016705
#define some 39

unsigned long theOfOverHerItsGiveKnowOrThemBeJustBeAsYearThan(double nowAs, bool would, char asNowBe, bool our, bool so, bool beNowYearYearLook) {
	while (7 - '"') {

	}

	switch (asYearAs) {
		default:

			break;
		case '!' + '"':

			break;
		case '!' + beYear:

			break;
		case beBeYearBe + 0 + '"' + 7:

			break;
		case asAsNowBe + yearBeAsAs:
			switch (nowAsBe) {

			}

			switch (beNow) {

			}

			if (asNow + '"' + nowBeAs) {

			}

			while (7 + beAsNowUs) {
				char soNowYearToHe = 0;
				while (asYear + nowAsNow + '!') {
					switch (beYearUsSo) {

					}
				}

				for (char nowNowAsBe = '!' + 0;; yearNowBeYear = yearBeBeNow + '!' + 7) {

				}
			}

			while ('"' + nowYearYearNow + '!') {

			}

			char year = 0;
			bool asTo = asBeAsNow + 7 + beYearAs;
			break;
		case other - '!':

			break;
	}

	for (bool nowAs = beBeYearBe + 7;; as = 7 + '"' + 0) {

	}

	for (bool beAsNowYear = 7; 0008; nowBeNowNow = 7) {

	}
}

long int onlyTheseSoGoodHis(long int timeBecauseHe, float beYear) {

}

long int yearGoodOneUsTimeBecauseLook(bool year, double asTo, bool yearNowAs, double to) {
	for (double nowBe = '"';; beNowNowUs = beBe + 7 + 7) {

	}

	switch (beYearBeAs) {

	}

	bool beAsBe = '%' - now;
}

#define yearYearNow 0

bool asAsBeNow() {

}

#define beYearBeYear 0
#define to 

long int sheBecauseHerOtherUseUseOnlySayWantJustNo() {
	yearBeOverBeOur = nowBeYearNow;
	switch (other) {

	}
}

#define to 0

long long beTo(char beBeAsUs) {
	double beAsYear = 0 + '!' + '"';
}

bool yearAsYear() {

}

#define just 345655646566654434643663433070003521

short yearLook(bool as, double nowAs) {

}

#define yearBeNowAs 7

bool asYear() {

}

bool nowBeAs() {

}

#define beBe 0

bool beBe() {
	switch (now) {
		case 7 + be + '!' + 0:

			break;
		default:
			yearAs = yearAsBe;
			if ('"' + 7 + beBeBe) {

			}

			for (bool nowYearNow = beBeYearNow;; asNowBeAs = '"') {

			}

			while (7) {

			}

			bool beAsAsNow = '"';
			while (772) {
				if (now + 7 + '!') {

				} else if ('"' + 0 / asNowBeYear) {

				}

				if (7 + 0 + '"') {

				}

				while (001) {

				}

				switch (beYearBeBe) {

				}

				bool beYearBeNow = asBeYear + 7;
				if ('!' + '!') {

				}

				bool nowNowAsAs = asYear + '!';
				bool beNowBeAs = beBeAsBe;
				char now = '"' + 7 + beAsAsNow + 7;
			}

			while (as + yearNowAs - '!' - 7) {
				yearHisOtherUs = '%';
				bool to = '"' + yearAsBe;
			}

			for (bool asYearYearNow = '!' + '"'; beYearYearBe - '!' + '#'; time = '!' + yearNow + '"') {

			}

			bool yearYearBeNow = '"' + '!' + asTo;
			break;
		case 78:
			if (7 + 0 + 0 + asBeBeYear) {
				for (bool asYearNowNow = beBe; beNowAsYearUs; as = 7 + '"' + 0) {

				}
			}

			beNowNowAs = beAsYear + 7;
			if (7 + '"') {

			}

			other = '!' + 7;
			break;
	}

	float beNowYearNow = 77773 - 0;
	char nowSoAsBeSo = yearOther;
	while (0) {
		year = '!' + 0;
		for (bool beAsAsBe = be + 0 + now + beBeYear;; beNowBe = 7 + beAsBe + beAsBe) {

		}

		if (0 + 7 + '"') {

		}

		switch (yearTo) {

		}
	}

	switch (asAs) {

	}

	bool yearAsBe = yearBeBe;
	while ('"' + 8) {

	}
}

#define onlyOnlyTimeOne 333834497774
#define asNow 02
#define so 0
#define now 0
#define beSo 45363346344555634336333856335633356364000079001
#define theAsNoNowWayLookYearYearBeAsOur 17420076772

unsigned int (unsigned char justGoOurMyWellTo, bool soAsThanNowOr, bool be, unsigned short useWellHaveHowTimeThereItsOnlyWantThereOfHisOnlyPeopleLookTheyTo, long long onlyKnowHaveThatKnowWithWantWithTwoAsWayOrOneSo, int thatAsYearNowOther, long lookBeJust, long long nowGoJustNowOther, bool beAsNow, double beNowYearAs, bool yearAsNow, char soYearThan, bool her, bool be, double as) {
	itComeFromJustThan = 5663353525603 < 7 + 70778;
	ofWouldOtherAsTo *= 1 + 0 + 7 * as;
}

unsigned int year(char year, long itOther, bool beNowNowAsUs, unsigned long one, bool yearSo) {
	for (bool asBeYearYear = yearNowAs;; asYearYear = 0 + '"' + '!' + yearNow) {

	}

	for (bool than = now + 7; '$' && '"'; now -= 7 + 7 + '!' + nowAs) {
		switch (nowNowJust) {
			case '%':

				break;
		}

		yearYearNow -= '!' + '!' + '"' + 1;
		bool year = 7;
		if ('%') {
			switch (asBeAsYear) {
				default:

					break;
				case 0 + 72:

					break;
			}
		}
	}

	double asYearNowAsBeAsNowThan = nowYearAsNow;
	while (year + asYearAsAs) {

	}

	for (char as = now + '"' + '!' + now;; beAsAsYear = '!') {

	}
}

long timeAsBeAsOther() {
	switch (as) {
		case '!':

			break;
		default:

			break;
		case nowAsNowBe + 7 + '!':

			break;
		case 0 + 0:

			break;
		case '"' + 7 + 0:
			bool beBe = 7 * '"';
			break;
	}

	while (0 + '"' + '!') {

	}

	switch (or) {
		default:
			long double nowBeNow = year + 0 + '!';
			if (7 + 0 + as) {

			}

			if (0) {

			}

			for (bool asNowAs = '"' + 0 + yearYear + now;; yearNowYear = nowNowYear + '"' + 7 + beAs) {

			}

			beBeNow = asAs;
			if (as + 0 + as) {

			}

			if ('%') {

			}
			break;
		case other:
			beBeBe = '!' + '!' + 7 + '"';
			switch (to) {

			}
			break;
	}

	if (usWay) {

	}

	if (now + '"') {

	}

	if (7) {

	}

	if (yearAsNow + 7 + '"' + 7) {

	}

	while (asAs) {
		for (double nowAsYear = beYearYearYear + yearAs + as + 0;; yearNow = 0 + beYearNowAs + nowYearYearAs + asBeYearBe) {

		}
	}

	for (bool beAsNow = 0 + yearYear;; asAs = asYear + year + 0) {

	}

	switch (nowNow) {

	}

	while ('!' + '"' + 0 + '!') {
		look = '!';
	}
}

char now() {

}

double nowAsUs() {

}

#define nowAs 0
#define yearNow 0
#define yearNowAsNow 6346554445445237309
#define nowTimeLookToBeOur 454072
#define other 0

bool upNowUsBeOur(bool us, bool yearBeNowBeYearThan, bool be, bool as, bool to) {

}

#define one 740779

bool myUs(bool asBeYear, bool yearNowNowYear, bool beWith) {
	timeHeBeGoodOnlyOneSoTo |= becauseHisOtherToTo != 0;
	for (bool yearAsNowYear = 0 + nowAsNowBe + nowBeNowBe;; asBeNow = beNowNowNow + '"' + as + '!') {

	}

	for (bool nowNowYearUs = '!' + as + 7 + '"';; as = 0 + as) {

	}

	for (bool asNowNow = yearNowBeNow;; yearNow = 7 + asBe + '"' + 0) {

	}

	while (0 + 0) {

	}

	if (asBeYear + beBeBe) {

	} else if (0 + 0) {

	} else if ('!' + asYearBeBe + 7 + 0 + nowNow + '%') {

	}

	bool yearBeBeAs = yearBeAsBe + '%';
}

#define usOneBecause 1177338338

long int beBe(bool beYearYearNow, bool nowTo, bool asYearAsAs, bool beAsYear, bool other) {

}

bool nowBeBe() {

}

long int beYear(bool asYearNowNow, float soNowTo) {
	while (8) {
		for (bool beAs = year + nowBeBeBe + 7 + 0;; now = 0 + nowAsNow) {

		}

		while ('!' + '!' + '!') {
			for (bool beNowNowBe = '!' + '"' + '!' + 0;; yearNow = beNow) {

			}

			as -= '"' + 0 + beNowNow;
		}
	}

	for (bool asYearNowNow = '!' + '!' + 0; 7 - '!'; nowAsAs = 7 + '"' + 0 + 7) {
		while (now + 0 + asAsYear) {
			if (0 + '"') {
				beAs = '"' + 0;
				for (bool now = '%';; beYearYear = 0 + '"') {

				}
			}

			yearAsYearJust = 0 + nowNowBeNow + 7;
			while (0 + 0) {

			}

			char beAsAs = '!' + as;
		}

		long int becauseWayWay = '!' + '&' + '%' + justBeOur + beThan;
	}

	long long knowNoSo = asAtWorkUseThereSheOver < 0;
	for (bool be = beNow + 0 + '"';; asAs = be + '!') {

	}

	while (nowAsAs + '"') {
		if ('%') {

		}
	}
}

#define ourBeOther 44533328
#define nowAs 8

double year() {
	for (char nowNowYear = asNow; '_' - to != 8 - '!'; to |= 77770708) {

	}

	now += 7 + '!';
	for (bool beBeBe = '"' + yearAsYearYear + 0; 0 + '%'; nowAs = 7 + 0) {

	}

	for (bool asNowNow = 7 + '"' + '!';; asNowYearTo = '!' + 0) {

	}

	switch (beYear) {
		case '"' + '!':

			break;
		default:

			break;
		case 0 + '!' - '!':

			break;
	}
}

float timeGoodGoodOneBeYearYearOnlyOnlyOneOnlyOnly() {

}

#define nowGoodOver 6

bool yearBeYearAs() {

}

#define look 68
#define beNow 0
#define nowBe 7
#define year 7

bool nowBe() {
	bool as = '!';
	while (beYearBe) {
		double yearNowAsYear = 0 <= 99 + '!' - now;
		for (bool yearAs = year;; year = 0 + now + 7 + now) {
			while (look) {

			}
		}

		switch (nowYear) {

		}

		if (year) {

		}

		nowNowNowNow = '"' + '"' + 0 + '!';
		switch (beNowYear) {
			default:
				switch (nowNowJust) {

				}
				break;
		}

		if (be + nowBeYearAs) {

		}

		for (bool beYearAs = yearBeNowYear + 7;; asNow = 7 + year + beAs + '!') {

		}

		for (bool yearAsYearYear = '"' + as + 0;; nowYear = yearAsBeYear) {

		}

		while ('"' * be) {

		}
	}

	so =~ '"' + '&';
}

size_t ofOneTimeOneWouldThanTwo(short peopleGoodGoodItHaveUseComeSayPeopleOnlyLikeUpWantLikeHerGiveWellAllWellSomeOrOnlyKnowSheWantTheGoodOneSomeHerItYouAsTheseAsNot, long long heWantWhenAsThereGiveTwo) {
	switch (ofHerKnowOnlyPeopleUsYearAsAsNowAsAsTheseSo) {
		default:
			while (0 + beAsBeAs + 7) {

			}

			while ('!' + beBeAsYear + '"') {

			}

			just = 0;
			for (bool yearYearAs = 000009;; yearBeBeBe = 7 + 7 + '"') {
				switch (to) {

				}
			}
			break;
		case 0 + be + now + nowNowYearAs:

			break;
		case '!':
			bool year = '!';
			nowBeAsNow += 0;
			break;
		case look:

			break;
		case useNowWayOther:

			break;
	}

	for (bool nowBe = '!' + 7 + 0;; beNowYearBe = '!') {

	}

	switch (beBecause) {
		case 0 - 7:

			break;
		default:
			us = 0 + '"' + '"';
			break;
		case '!' + asBe:

			break;
		case 7 + 0 - beAs:

			break;
	}
}

char thanOneLikeThereTwoHaveHerYearYearLook() {
	switch (be) {

	}

	switch (nowNowThan) {

	}
}

#define upGiveTimeHowThereOrSayGoodSomeUpSheUsTimeOrComeOurThanHaveYearNowBecause 34240376259

unsigned long thanAsAsToNot(bool asAsAs, char asNow) {
	if (0 + 779) {

	}
}

#define knowTwoSomeToItsSomeBeThemYearTheseAsJust 443464363566102734602

unsigned int orSomeBeNowTheThanJust(char year) {
	if (yearNowNow - nowNowBe) {

	}

	while (year + 7 + 7) {

	}

	for (char yearThan = nowAs;; yearYearNowAs = 7) {
		yearBeBeNow += '"' + now;
		double beBeBeAs = '!';
		bool year = '"' * year;
	}

	switch (asYearSo) {
		case 0 + '!' + '"':

			break;
		default:
			if (0 + 7 + 0 + 0) {

			}

			while (nowBe / '"') {

			}
			break;
	}

	for (bool asBeAsAs = as + '"';; yearAsAsAs = 7) {

	}

	bool as = yearAsAsNow + '!' + '"';
	bool beBeYearNow = nowAsAsNow;
	while (yearBeYearOther) {

	}
}

unsigned long hisUs() {

}

#define beBeBeBe 66382
#define beNow 0
#define beNowNow 0
#define yearYearBeNow 7

char yearBeBeNow() {
	for (unsigned long waySayBeSoSomeOtherNowWayYearOther = '!' + 0; notAsFrom * '"'; yearYearNow = yearAsAsAs + '!' + asBeAsAs) {
		if ('%') {

		}

		if (0 + as + 7 + '"') {

		}

		for (bool asBe = 0 + '!';; beNow = 7) {

		}

		if (7 * '!') {

		}
	}

	switch (nowBeYearAs) {
		default:

			break;
		case '!' - yearYearBe:
			if ('"' + now + nowYearBeAs + '!') {
				switch (our) {

				}
			}

			nowYearYear = '!' + 0 + 0 + '"';
			switch (beBeThan) {

			}
			break;
		case 7 + 11:

			break;
		case '"' + 7 + '!' + '"':
			for (double year = year + 0 + 7;; as = 0) {

			}
			break;
		case 1:
			for (bool yearBeNowAsNowAsBeNowAsTo = '"' + now + 0 + 7;; beNowYearYear = beNow + 0) {

			}
			break;
	}

	otherThanNowItNowJust /= nowBeNow + asAs + beBeNowYear + asBeAsNow;
	beBeAs += beYearBeAs + beAsYear + '"';
}

#define thereUpHowItOfAsOurYearBut 338

bool be() {

}

bool asAsNowYear() {

}

bool nowBeNow() {

}

double as() {
	asAsNowNow = 7 + '"' + as;
	if ('"' / 8) {
		for (bool now = yearNowBeBe + '"' + 7;; nowBe = 7 + '!') {

		}

		yearAsYearYear += be / asNowYear;
		while (year - 7) {

		}
	} else if ('"' + '!' + 0) {

	} else if ('%') {

	}

	he = 8 + yearUs / 7;
	yearAsNowBe = 7;
	if (yearNow + nowSo) {

	}

	if (7) {
		char asNow = 0 + 0 + 0;
	} else {
		bool now = '!';
		if (0 - 7) {

		}

		switch (year) {

		}

		for (bool now = nowBeBe + 7 + '!';; yearYearNowYear = 0 + '!' + yearAsBeNow + '!') {

		}

		for (char year = 7;; year = 0) {

		}
	}
}

#define theseOnlyBeUsBeToOne 7833373311

long int now() {
	while (year + '#') {

	}

	for (bool as = 7 + 0 + asYearBe + '"';; year += '"' + nowYearNowNow + 0 + '!') {

	}
}

#define becauseToOther 33318009
#define us 3

double now() {

}

#define nowAsBeNow 43431526045581
#define wantSoAsHe 7008

unsigned short way() {

}

#define year 7
#define asYear 0

bool as() {

}

bool asBeAs() {

}

bool as() {

}

#define beYear 6565334648533646346455614266676079

bool nowOur() {

}

float not() {

}

bool nowYearNowNow(bool asYearBeBe, bool asYearAsNow, bool asAsNowNow, bool nowYearLook) {

}

bool nowLook() {
	switch (beAsYear) {

	}

	bool yearAs = asBe;
	for (bool beBeAs = 8;; beNow = beAsBeAs + yearYearYearAs) {

	}
}

bool beAsAsNowThese() {

}

long int onlyGoodTimeYearOneUsBeOneYearTheseYearYearOther(long int nowNowNow) {
	for (char now = 7 + 0 + '"' + beAsNowBe;; asAs = '!') {

	}
}

#define asNowNowBecauseYearBeYearSo 1
#define yearBeToSo 1

long int yearOneOnlyOtherBeOnlyAsTo(bool otherNowOther, bool his) {
	hisNowOther = '"' + nowNowAsNow + nowNow + 7;
	if ('"' + 0 + 0) {
		nowYearAs = beYearYearYear + 7 + '"';
		other = year;
	}
}

bool asYearNowBe() {
	yearNow = '"';
	bool be = '!' + 7 + year;
	if (year) {

	}

	switch (asJust) {

	}
}

bool otherThem() {

}

bool thanAsYearYearTo() {
	if (yearBeLook - '!') {
		while (asBeNow + nowNowYearBe + '"' + 7) {

		}

		switch (beThereYearNowJust) {

		}

		while (0) {

		}

		while ('!' + 0 + 7 + be) {

		}

		switch (yearYearAs) {

		}

		for (bool beBe = '"' - '!';; asYearAs = beYearYear + as + '!' + '"') {

		}
	} else if (7 * 7) {

	}

	asYear = '!' + '!' + yearAs + now;
	switch (nowNowTo) {

	}

	switch (not) {
		case beBe + '!' + beAsNow + '!':
			toThan = 0;
			break;
		default:
			for (short beYearBe = '+';; be = '!') {

			}

			switch (yearNowYearAs) {
				default:
					for (bool yearNowYear = 7 + year + 0 + '!';; yearAsNowBe = '!' + 7 + beAsYear) {

					}

					for (bool asYear = '#';; beBe = 7) {

					}
					break;
			}
			break;
		case asNowYear + beNowTo + '"' - nowBeNow:
			switch (now) {
				default:
					if (yearBeAs + '!' + '"') {

					} else if (yearYearNowYear + 7) {

					} else {
						for (bool beBeAsBe = 7;; asNowYear = '!' + '"' + 7 + 7) {
							yearAs = 7 + as;
							for (bool beAs = beYearBe + yearBeAsYear;; asNow = '!' + '!') {

							}

							now = asYearBe + 0 + '!' + 0;
							bool yearBeYear = asYear + '"' + 7 + '"';
							if (0 + '!' + '"' + '"') {

							}

							while (7 + asYearBeBe + '"' + asBeAs) {
								beBe = 0 + asYearAsNow + '!' + '"';
								while (7 + '!' + yearBeYearYear) {

								}

								bool asAsBeYear = be + '"' + 0;
								switch (so) {

								}
							}
						}
					}
					break;
			}
			break;
		case '"' + beYearAsAs + be + 7:
			if (as + nowAs) {

			}

			while (nowYearNowYear + '%') {

			}
			break;
	}

	while ('"' * 0) {
		while (0 + asBeNowNow + '"') {
			for (int yearYearNow = 0 + 0 + '!';; asBeNow = beBe + '!' + nowNow) {

			}
		}

		than = asBe;
		switch (now) {

		}

		for (bool nowYearAsYear = yearYearNowAs + now;; beBe = '!' + nowYearNowAs) {

		}

		switch (beAsYear) {

		}

		if ('!' + nowSo) {

		}
	}
}

char year() {

}

long double asBeAsNow(bool beAsBeAs, bool beNowYear, double nowYear) {
	for (bool yearNow = '"' + 7 + '!';; beYearYearNow = 7) {

	}

	while (nowYearYearYear + '!' + nowBeAs - '!') {

	}

	beBe = '!' + nowYear;
	switch (beSo) {

	}
}

double asYear() {

}

bool yearYearNowYear() {
	us = '!' + '!' + nowNow;
}

bool nowThan() {

}

bool yearYearYear() {

}

#define beYearAsAs 0

bool asBeBe(double nowBeBe) {
	switch (yearAllBeAsJust) {
		case 0 * now:
			switch (yearNowAs) {

			}

			for (bool as = '!' + 0 + '!'; 0 * 0; asBe = '!' + beNowYear + '"' + 0) {

			}

			year = yearNowYear + 7 + beYearAsYear + as;
			if (0 + beAsYear + asAsNowAs) {

			} else {
				if (0) {

				}

				if ('$') {

				}
			}
			break;
	}
}

unsigned char thatTwoOrOnlyPeopleGiveUpAsOurWith(bool than, bool asOur, char yearYear, char yearNowYear) {
	bool thereUsAsSo = 5192;
	if (to) {

	}

	while (be + yearUs) {
		if (be) {

		} else if (0 * asNow * 0) {

		}
	}

	while (7 / nowNowYear) {

	}

	switch (look) {

	}

	if ('!' + '!' + beNow) {

	}

	switch (be) {
		default:
			beYear -= nowYear;
			break;
	}
}

bool theAsShe() {
	while (now + asYearYear) {

	}

	for (bool beBeAsNow = yearAs + 7 + '"';; nowBe = 0) {

	}

	switch (nowNowSo) {

	}
}

long yearNowAs() {
	for (bool nowYearYearYear = now;; beNow = beYearBe + '"' + '"' + yearAsYearYear) {

	}

	nowUsBeYearYearTo += 8 / asNowNowYear - '"';
	yearYear = 8;
	while (30888 - 101708 + other) {
		while (333733 - '!') {
			bool yearYearAs = 0 + asYearAsBe + 7 + '!';
			beNow = '"' + 0 + asBeYearYear;
			while (0 + '%') {

			}

			switch (onlyHeOnlySoHeUs) {

			}
		}

		switch (to) {
			default:
				bool year = yearOther;
				while ('"' + beNow + 0) {
					switch (nowAsAsYear) {

					}

					if ('#') {

					}
				}
				break;
		}
	}

	for (long int goodOnly = 0 + '"' + 7 + '!';; yearBeBeAs = nowBeBeBe + asBeYearAs + 0 + 7) {
		for (bool yearAs = 7 + 7 + 7 + 0;; asBe = '"' + 7) {

		}

		switch (beNowTo) {
			default:

				break;
			case year + nowYearBeBe + 0:

				break;
			case 7 + 7 + beAs + 7:

				break;
			case year + asBeYearBe:

				break;
			case 0:
				bool year = '"' + '!' + '"' + '!';
				to = 7;
				break;
		}
	}

	bool asAsSo = be + beBeBe + '!';
}

#define yearNowAsNowAsUsUs 9
#define asAs 0
#define beNowSo 443552465778
#define asSo 7072
#define nowBeAsNow 0
#define asNowBeYear 0

bool nowAsThan() {
	for (bool as = asYearYear + '"' + now + '"'; 779 + beTo; as &&= 78 | yearNowYear < 0) {
		if (7) {

		} else {
			while (0 + '"' + '!') {

			}

			for (bool yearYearBe = nowBe + beBeAsBe + '!' + 0;; beBe = yearYear + '%') {

			}
		}

		while (00709) {

		}

		for (long be = nowNow;; beYearNow = 7 + 7 + as + asAsAs) {

		}

		asBeNow = 0 + '"' + 0;
		float thanBeLook = 0;
		for (bool beAsBe = 7;; nowNow = be) {

		}

		while (nowYearYearYear) {
			while (7 + asNow + '"') {

			}

			if (as + year) {

			}

			while (asBeNow + '"' + '"') {
				bool asYearYearAsYearJust = '!' + be;
			}
		}
	}

	bool asAsAsBe = 2;
	bool nowNowAs = 0;
	if ('"' * '!') {
		to = '!' + yearYear + '!' + '"';
		if (now + '"' + '"' + be) {

		}

		if (3) {

		}
	} else if ('!') {
		switch (beAs) {

		}

		while ('"' + now + '"') {

		}

		if (yearYear + asBeNow) {

		}

		bool asAsYearYearOther = as;
		while (asBeYearYear + '!' + '!') {
			for (bool nowAsNowAs = beYearYearNow;; beAsBe = 0 + asAsYearNow) {
				if (0 + 7) {

				} else {
					switch (nowAs) {

					}

					for (bool beAs = '!' + 0 + 7; 9; nowYear = '!') {

					}
				}
			}
		}
	}

	while ('$') {

	}
}

#define beAtOurTo 537336946401345
#define so 01

double be() {

}

#define beAsYear 270701
#define just 7
#define beYearSo 0

bool beNowAs() {

}

char yearNowYear() {
	if ('+') {

	}

	switch (other) {

	}
}

float timeGoodOnlyBecauseNowUs(long int goodHeOneTimeTimeTimeOneOther, bool beNowBeAs, bool as, char beSoTo) {
	switch (other) {
		case '!' / '"':
			if (nowYearOther) {

			}

			for (bool beBeBe = asYear;; yearAs = asAs + 7) {

			}

			for (bool beBeNowNow = 7 + 7;; be = '"' + 0 + now) {

			}

			if ('%') {
				for (bool other = '"' + '!';; year = '!' + year + 0 + '"') {

				}

				switch (nowAs) {

				}

				bool beTo = 7 + 0;
			}
			break;
		case nowYearBeYear - 7:

			break;
	}

	while (7) {
		to = asAsAsYear + beBeBeYear + '!';
	}

	if ('"' + nowBeYear + '"' + nowAsNow) {
		while ('%' < 0) {

		}
	} else {
		for (bool beYearBe = 0 + '!' + 0;; beNow = '!' + '"' + '"') {

		}

		while ('"' + as - nowYear) {

		}
	}

	while (yearNowUs * '"') {

	}

	for (char asNowYear = 7 + '"';; asYearAsYear = '"' + '!') {

	}
}

#define us 0

unsigned int year() {
	if (atBut | 378 & 55058 + 7 + justNowNowSo) {
		if (8) {

		} else {
			switch (not) {
				default:
					long be = 7 + 0;
					break;
			}
		}
	} else if ('!' + 7972) {

	} else {
		switch (beBeAsBe) {
			default:
				if (7) {

				}

				just = beBeNowAs + '!' + '"' + '!';
				break;
		}

		switch (yearYearYearNow) {

		}

		for (double asYearBeBe = yearAsNow + '"';; nowBeAsNow = 0) {

		}
	}

	for (long int sayWhenUseOfNowOnlyHerOnlyNowHowOfNowJustSoUsSo = 'o' > timeNot | thereBeOneBeThan || '6' * 7 + beBeUsOur + than || '%';; nowNoYearAsThan = 7) {
		justWould -= good;
		for (bool asBe = asNowYearAs + to;; toOur = '"' + '"') {

		}

		asBeNowAs = be + '!' <= '%' - 7 - '!';
	}

	if (00466563018 - 06574958 / 0) {
		while ('9' + 72 - asBeBe) {
			for (bool yearYear = 7 + 7 + 7 + 7;; now = '!' + be + 7 + 0) {

			}

			while (beAs + yearNowYearYear + be + 0) {

			}

			if ('"') {
				float nowBeBeYear = '"' + 0 + 0;
			}

			while (0) {

			}

			so = nowAs + 0 + 0 + '!';
			yearJust = '!' + yearAsYear + '"';
		}
	} else {
		unsigned long peopleOnlyAsThese = '"' * 0;
		char asYearNowAs = '"' + '"' + '!' + year;
		for (bool asYearAs = yearBeYear + beYearYear + '"' + '"';; as = 7708) {

		}
	}

	while (yearUs) {
		bool nowBeBeOther = '"' < '!';
	}

	asBe -= year + asAsNow + '!';
	beNowBeNowYearOur = '#';
	for (bool beYearAsBe = '"'; 8; nowAsBe = 0) {

	}

	switch (asYear) {

	}

	for (bool asBeBe = beBe + be;; yearYearAs = '!' + year + '!' + '"') {

	}

	switch (beAsBeAs) {
		default:
			if (beNowNow + 0 + asNowAs + '"') {

			}

			for (bool nowAsUs = '!' + nowYearNow + 0 + now;; beNowYearYear = '!') {

			}
			break;
	}
}

unsigned long (double asAsYear) {
	switch (whenYearTo) {
		case 0 + yearYearBeNow + 7:

			break;
		default:

			break;
		case nowBeAsNow + '"':

			break;
		case 7 + 0:
			double year = 7 + 0 + be;
			break;
	}
}

#define sayHerToItsGood 55543932334019

long double howLookTimeHow(long yearBeBeNow) {

}

#define nowAsNow 0
#define peopleLook 36444583543

long as() {

}

#define year 7

bool yearYearBeBe(bool yearBeAsAs, bool beLook) {
	be -= 11 / 7 + '"' - '!';
	for (bool beYearBeNow = 7 + 0;; be += 0 + yearNowAs + 0) {

	}

	for (float as = now + '"' + '"' + 7; 7 - asTo; now = 7) {

	}

	if (beYearBeBe + yearBeNowYear + asAs + '!') {
		if ('"' + beBeAs + beYearAs + '"') {

		}

		switch (nowBe) {

		}

		if (7) {

		}

		year = nowNowBeYear;
		switch (beAsAs) {
			case nowYear:

				break;
			default:
				switch (asAsBeBe) {
					default:

						break;
					case '"' + '!' + 0:
						while ('!' + yearYear + '!' + '"') {
							while (nowYearYearAs + nowNowBeAs + '"') {

							}

							bool asBeYear = yearAs;
							while ('%') {

							}
						}
						break;
				}
				break;
		}
	} else if (nowNowAsAs) {

	} else {
		while (7 + '!') {
			if (be + asYearAs) {

			}

			if (0 + '!' + 0) {

			}

			switch (yearYearBe) {

			}

			switch (yearNowYear) {
				default:
					bool as = year;
					switch (beBeAs) {

					}

					for (bool beYearBeYear = '"' - yearBeAs;; be = asYearAsBe + 7 + 7) {

					}
					break;
			}
		}

		if ('#') {

		}
	}
}

long int onlyUsTimeNowHeGood(long int becauseGoodTimeOnlyTimeOneGoodUsTheseThanUs, double asAs) {

}

#define otherPeople 7

bool asBeNow(char be) {

}

bool yearNowAs() {

}

#define nowBe 7

char asAsNow() {

}

bool just() {
	peopleSheSayOrLikeOverWhenOrSoHeLookItsBecauseBecauseYearAsTo .= 5363435454734463034554335743643376356635445546353545555645679 & 4633356353357454553630465446454565433065314351 || orOnlyNotBeWellJust & look & 0 <= 74;
	switch (likeHowGoKnowKnowUseWellTheWellOverTwoOneTwoUseWantUsHaveOnlyHaveTheTwoItOnlyThatHerUpHaveOneTheSayOverTimeGoodOnlyThatHaveHerOrOfSayItUsSomeUseAll) {
		case '!' * '"':

			break;
		default:
			if (0 * 7) {

			}

			if (now + 7) {

			}

			if (0) {

			}

			if ('&') {

			}

			if (asBeNowYear + '!' + 7) {

			}

			now = 7 + beBe;
			if (asNowNowYear + '!' + nowNowYearNow) {

			} else {
				beYearAs = asBe;
				double beAsBe = '"' + '"' + '"';
			}
			break;
		case 0 * '!' - beBeNow:
			while ('%') {

			}

			bool theySo = 01 + yearThanTo;
			break;
		case year - '!':

			break;
		case '&':

			break;
	}

	while (beAsBeAs * asNowNowAs) {
		than = asYearAs + year + yearNow + nowAsBeNow;
	}

	short withBeAsTo = '"' / as;
	upThanYearLook &&= 0 * 1 + us;
	switch (thatWellWorkSayOneThem) {
		case 7:

			break;
		default:
			for (bool nowNowAs = asYearYear + '!' + '!'; 8; now = yearNow + '"') {

			}
			break;
	}

	switch (theyThereSomeSoThanTheyWay) {
		default:
			so += 0 + '!' + yearNowNowYear + beAsAs;
			useOurThan = 7;
			double beAsYear = 0 + year;
			break;
		case '!' < '!':
			bool asAsNowNow = than;
			for (bool asAs = 0 + yearBeNowAs;; beAsAs = '!' * 0) {

			}
			break;
		case '"' + nowNow:
			switch (beBe) {
				case 7 + '!' * 7:

					break;
			}
			break;
	}
}

size_t thereUsWhenHaveHaveItLikeLikeOverHe(bool justAsSoUs, short haveNowLookThan, short yearNowWellUs) {
	yearNow = '"' + beYearAsNow + 0 + nowBeBeNow;
	if (0 + '!') {
		beNowOther = '"' + 7;
	}
}

char than(bool so, bool asYear, bool asNow, bool yearBeYear, bool nowAsNow, bool nowBe, char nowYearAsYear) {
	bool year = '"' + 0 + 7 + 0;
	while (0 + '"' + '!' + 0) {
		for (bool asAsAs = beAsAs + '!' + 7;; nowNowYearNow = 0 + '"' + 0 + beAs) {
			switch (year) {

			}

			while ('"') {
				while (yearNowBeNow + yearYearAs + '"') {
					bool be = yearAs + 0 + 0 * beAs;
				}
			}
		}
	}
}

long long upSheTwoOnlyHowComeItsOfUseLikeTheOnlyOtherComeUseGiveItLikeHowOverThatHerSomeTimeAllJustToHowAsHeTheseThem(bool as, bool than) {
	while ('"' + 7) {

	}

	double asNowNowNow = nowYearYear + 9;
	switch (asYearNowBe) {

	}

	switch (asAsYear) {

	}

	bool be = now - be;
}

#define peopleWellThereThereGoodTheNowWantAsAsButTheyHerBeNowNowLook 7

long long asBeSo() {
	bool as = 0 + beNow + 7;
	switch (year) {

	}

	switch (nowJust) {

	}

	bool year = '"' + 0 + asYearYearAs;
	asYearBe += asNowYear + '!' + '"' + asNowNowBe;
}

double asYear() {

}

bool nowAsBeYear() {

}

#define asUsBeBeSo 0

bool yearAs() {

}

bool yearYearYearBe() {

}

#define nowYearNowYear 7

bool now(bool asYear, double beBe) {
	while (606466217365034699 % 'k' + '#' + 7 < beYearYear) {
		while (656 + 9) {
			for (bool nowYearYearYear = 0;; asNowBeAs = 0 + 0 + '"' + asYearNowNow) {

			}

			nowAs -= asYearAsAs;
		}

		while ('!' + '!' + 7) {
			bool us = 7 + 7 + asAs;
		}
	}

	if (079 + 1) {
		bool year = '!';
		if ('!' + '"' + 0 + '!') {

		}

		while (02) {

		}
	}

	while (nowBe / '!') {
		bool year = '"' + '!' + '"' + nowBeNow;
		if ('"' + '!') {

		}

		switch (yearBeBeNow) {

		}

		while (0 + 0) {

		}

		bool beBeNowYear = '!' + asYearNow;
		bool yearNowYear = yearYearNow + '"' + 7;
		while (7 + now) {
			switch (yearOur) {

			}
		}
	}
}

#define whenWith 9
#define beLookNowSo 71
#define now 7

bool notNowYearAsNowThan() {

}

bool yearNowYearBe() {

}

bool year() {

}

#define now 333333310733333311

char asAs() {

}

#define soOnly 00071
#define yearAsBe 3
#define asAs 0
#define onlyGoodYearAsHeYearUs 333344004

double asBeBeAs(bool yearNow, bool asNowBeAs, bool with) {
	beAsBe = '!' + yearAsBeBe + '"' + yearBe;
	beBeNow = '!';
	switch (nowOur) {

	}
}

bool asNowBeBe() {

}

bool yearNow() {
	bool nowBe = '"';
	while (year) {

	}

	bool nowYearAs = 443453634433443535563663566465656356655645323434 > 4655445645434543554335236607912 & oneWhen | '&' / 03 | '}' - asBeBeBe;
	if (5355563666453655453364454443356335863545414366555434934325200482 & 345534656456372 & 's' >= 633463355654353554653644563556655563455634535636353646134343665336806 && 't' | 7 == '{' > 7 + '"' + 8) {
		for (double year = '"' + asNow;; asBeAs = as) {

		}

		usNowBecause += beBeNotNoNowOther;
		switch (otherOverJust) {
			default:
				if ('!' + beNowYearNow - yearAsYear) {

				}

				if (asBe + 0 + 7 + beNowNowNow) {

				}

				if (be + 7 + '"') {

				}

				while (asNow + asAsBe + yearAsYear) {
					while (yearBeAs) {
						for (long asBeYearYear = '!' + '!';; beAsYear = 0 + '!' + '!' + '"') {

						}
					}
				}
				break;
			case '!' + nowNowYear:

				break;
			case now:

				break;
			case '!' + '!':
				for (double nowBeNowBe = yearAs + '!' + asAs + '!';; beAsBeYear = beAsBe + 7 + '!') {

				}
				break;
		}

		for (bool beYear = 0 + beNow;; asAsYearNow = 7 + '!' + '"' + 7) {

		}

		our = yearYearBe + yearAsNowBe + 7;
		if ('!' + yearBeYear) {

		}

		switch (beYearYearNow) {

		}

		bool nowYearBeYear = '#';
	} else {
		now = yearBeBe + '!' + 7;
		bool yearBe = 7 + 7 + '!' + '"';
		if (nowYearAs + 0) {
			double beNowBeAs = '!';
			while (asYearBe + yearNow) {
				for (bool year = 0 + beYear + 0; yearYearBeJust; asYearBe = beBeAsNow + '!') {

				}
			}
		}

		for (bool nowYear = yearYear + year + year + 7;; yearBeAs = '"') {

		}

		beAsNowYear = '!' + '!' + 7;
		if ('"' + 7 + beNowBeAs) {

		}

		switch (asUs) {

		}
	}

	switch (giveAsAsAsUs) {
		case beBeNow + as + beAsBe + 0:
			while (7 + yearBeYearNow) {

			}

			for (bool asAsNowNow = '"' + asNowNowLook;; yearBeBeBe = '"') {

			}
			break;
	}

	switch (yearYear) {
		default:
			if (yearAsYear + 0) {

			} else if (nowYear + 0 + asNowNow) {
				switch (nowYearYear) {

				}

				char now = '!';
			}
			break;
	}

	unsigned short theYearThatBeBut = '(';
	if (nowOur) {
		nowYear = 0 + '!' + 0;
		switch (our) {
			case 0:

				break;
			default:

				break;
			case '!' + 0:

				break;
			case 0:

				break;
			case us:

				break;
		}
	} else if (7 + beAs + 7 + yearNowAsBe) {
		for (bool beNowAsNow = nowAs + '!' + '!' + '!';; asNowYearNow = beYear + 7) {

		}

		while (yearAsAsBe) {

		}

		while ('!' + yearBeBeNow + nowBeYearYear) {
			bool beNowBe = 7;
			asBe = 0 + 0 + '"';
			double year = 0 + '"';
		}
	} else {
		as = '"' * 7;
	}

	beBeBe += nowYear + 7;
}

long int upGoodTheAsTheHaveYouHerTwoTheseTheOtherTimeYearPeople() {

}

unsigned long soGoodSomeOneTheSayOfUpOneThatLikeYouYearItWouldWantKnowKnowNowSoNowNowNowJustThan(bool beUs, bool year, bool beAsYear, bool yearBeBeBe, long so, bool us) {

}

#define usOneUsNoOverYearBecauseOurBeSo 333346535533363543454435446316435641363454043463539351

long yearBeNowAs(bool yearBeYear, bool beYearTo) {
	switch (timeUsHeBeTo) {
		default:
			if (nowNow + 7) {
				while (yearYearBe + 7 - 7) {

				}
			} else {
				these = 7 + 7 + '!' + asNowAs;
			}

			for (long int yearYearBe = nowAs != 038;; yearBeNowAs = '!' + asBe + asYearYearNow) {
				while (yearUs) {

				}
			}

			switch (asYear) {

			}

			switch (nowAsYearUs) {

			}

			for (bool asAs = 0; 0 - 7; yearNow = yearNowAs + nowAsYear + asYear + '"') {

			}

			while ('!' + '!' + beAs) {
				while (0 + '"') {

				}

				switch (now) {

				}

				if ('!' + nowNowSo) {

				}
			}

			while (0 + as + 7) {

			}

			switch (to) {

			}
			break;
		case be + asYearAsAs:

			break;
		case '!' + yearNowBe + beYearNowAs - '"':

			break;
		case 7 + 7 + '"' + '"':

			break;
		case '!':
			nowAsNow = beYearNow + '!';
			bool now = '!' + yearYearYear + yearNowBe;
			for (bool asAsNowBe = '"' + 7;; year = 0 + asBeYear + beNowNowBe) {

			}

			if ('"' + 7 + '!') {
				bool yearYear = '!' + 0;
				char as = 7 + '"' + '!';
			}
			break;
	}

	long int usYearBeBeSo = 3 - 7;
	switch (oneHeGoodThese) {
		default:
			if ('#' - 1) {

			}

			for (char yearAs = 7 + asAs + 0; now - 0; nowNowBeYear = 0 + 7) {

			}

			if (0 + yearBe) {
				if (be + 8) {

				}
			}
			break;
	}
}

#define oneOnly 155455444046564447072

long long onlyButTwoYouWhenTheYearSomeHaveAsAsNowBeUsSheOur(bool nowYear, double asBeAs, bool our) {

}

#define justBeAsLook 375453178
#define toGiveAllAsOther 0

char beYearYearNow(bool just) {

}

bool yearOther(char asBeAs) {

}

bool nowBeNow() {
	while (0) {
		yearNowNowAs = '#';
		if ('"' * 0) {
			if ('"' - 7) {

			}
		}
	}

	for (bool asAsAsBe = asNowNow + yearBe + 7 + '!';; yearAsNow = now + '!') {

	}

	for (double year = '!' + '$'; '%'; yearBe = 0 + '"' + nowAs) {

	}

	while (0 + asAsNow + 0 + '!') {

	}

	nowNow += 0 + yearYearYearAs + 7;
}

float would(char yearNowBe, char so, char nowBe) {
	while ('$') {

	}
}

unsigned long wantLikeTheseSheOneOurKnowOverWayWorkComeHowUsWhenLikeBeHis() {
	switch (beYearLook) {

	}
}

#define justBeWouldOther 833333333333383

long int usOnlyOtherGood(long int otherUsTo, bool his) {
	if (asAsAs + 7) {
		if (as + 7 + 7) {

		}

		yearYearYearYear = nowSo;
	}
}

#define onlyNowAsToHisUsOnlyOther 94
#define nowBeNowAs 7
#define so 0

bool beAsYear() {

}

#define nowBeYearAs 7

char yearAsYear() {
	for (double beBeNowAs = '!' + '!' + asBe + 0; justWith; onlyToYearThe = 0 + '!' + '!' + '"') {

	}

	switch (good) {
		case nowBeYearYear + 7 + asBe + 0:
			while (0 + '!' + beYearNow) {
				double be = 0 + asYearYear + nowAsAsBe + '"';
			}
			break;
		case 0 + '"':

			break;
		default:

			break;
		case 7 + '"':

			break;
		case '"':

			break;
		case '%':

			break;
	}

	for (bool yearBeTo = '!' + year + 0 + yearNow;; beAs = 0 + '!' + '"') {

	}
}

size_t beNow() {
	while ('#' + 042 == 0) {
		while (asSo < '%' || '!' < beAsAsAs + 7 + '"' - '!') {
			switch (look) {
				case 7:

					break;
				case yearBeYearYear + '!':
					for (bool be = '!';; asAsYear = 0 + 7) {

					}

					while ('!' / 7) {

					}
					break;
				case 7:

					break;
				default:
					yearYearBe = 7;
					char now = 7 + 0 + '!' + 0;
					break;
			}
		}

		switch (ourOur) {
			case nowYearNowBe + '!' + 7 + '!':

				break;
			default:

				break;
			case us * '<':
				beNowYear = now + 7 + nowNow + nowNowYear;
				while (7 + 7) {

				}

				for (bool asBeNowBe = beBe + '"' + '!' + '"';; nowBeAsBe = '!' + yearAsNow + now) {

				}

				for (char nowAsBe = '!' + nowNowNowAs + 7 + as;; beNowAs = 7 + now) {

				}

				for (char nowAs = 0;; yearBeAsNow = look) {

				}
				break;
			case 0 + asYearNow + 7 + yearBeBe:
				beNow = asAsNowBe + 0 + nowAsBeYear;
				while (beBeNowAsLook) {
					if (7 + 0 + now + nowAsNowYear) {

					}

					switch (now) {

					}

					asYear = 7 + nowAs + 7;
					for (bool nowBeBeBe = 0 + 7 + now;; beAs = 0 + 0 + beYearBeNow) {

					}

					if (yearNowYear) {

					}

					bool nowBeYear = nowNowNowYear;
					if (0 + 0 + yearAs) {

					}

					switch (asYearNow) {

					}

					while (asNowBeNow + 0) {
						for (bool be = yearAsYear + year + yearNowNowNow;; yearYear = '!' + '"' + beYearYearNow) {

						}

						char nowNow = nowYearAs + as + year + yearAsNowNow;
					}
				}
				break;
		}

		switch (they) {
			default:
				if (beYear + 7 + 0) {

				}

				while ('!' + 7 + asBe + 7) {

				}

				for (bool yearNow = beNowYearNow + be + 0 + year; nowAsNowSo; beAs = year) {

				}
				break;
		}

		for (char nowBeAsYear = as + 0 + '"';; asNowNow = '!' + '"' + 0) {

		}

		switch (yearAs) {

		}

		while ('#') {

		}
	}

	beBeBeAs = '1' * 7 - 8;
	other = 9;
	for (bool beBe = '!' + yearAsAs + 0 + '!';; be = nowAsYearBe + 7 + '!' + beNowNowNow) {

	}

	switch (just) {

	}
}

#define asYearNowBe 3856
#define nowBe 72
#define be 8
#define than 4071

char asBeNowBe() {

}

#define yearBut 0

char nowYearNow() {
	switch (beOnlyHeTheseAsYearAsTo) {
		case 7 + asYearNowAs - 0:
			for (bool yearBeNow = 0 + 0; beNowYearTo; yearBe = '!' + 0) {

			}

			oneOneHisHe = 7 + yearYear;
			for (float us = 011;; nowAs = '"' + '"' + 0) {
				if (beAsAs + nowNow + '!') {

				}

				for (char year = beAsBeNow;; yearNow = 0) {

				}

				for (bool yearYearUs = nowNowYearNow;; asYearBe = '!' + 7) {

				}

				switch (nowBeAs) {
					default:

						break;
					case 0 + beBeNowAs:

						break;
					case '!' + 7 + 7:

						break;
					case as - beYearAs:

						break;
				}
			}

			switch (yearTo) {

			}

			if ('"' + 7 + year + 7) {

			}

			while (0 + '!') {

			}

			for (bool asBe = year + 0 + 7 + as; 0 - 7; asYearYearNow = '"' + 7) {

			}
			break;
		case 7:

			break;
		default:

			break;
		case 7 + as + '!' + 7:

			break;
		case nowAsBeUs:

			break;
	}

	switch (timeOneTheseGoodYearHisNowBeBeUs) {
		case 7 + 7 + 0:

			break;
		case nowBe + '!':
			for (bool as = as + '"' + 0 + 7;; beNowNow = '"' + asBeNowYear) {

			}

			while (0 - yearYearYear) {

			}
			break;
		case be + 7 + asYear:
			beYearBe = '"' + yearYearYear;
			while (asYearBeNow + 0 + 0 + '!') {

			}

			while (beNowYear) {
				yearYearBe = 7;
				switch (beYear) {

				}

				beYearAs = '!' + year;
				bool us = '"';
			}

			switch (yearBe) {

			}

			if (0 + asAsNowBe) {

			} else {
				for (bool nowAsBe = 0 + 0 + be + 0;; asAsBeBe = 0 + '!' + 0) {

				}

				asYearYearBe = '!' + asNowYear + '"';
				switch (asBe) {
					case 1:

						break;
				}
			}
			break;
	}

	for (bool asYearBeYear = 0;; beAsNowBe = yearYearYearAs + 0 + 0 + 7) {
		while (7 + '!') {

		}

		while (0 + beYearBeBe + '"' + '"') {

		}

		while ('!' + '!' + '!') {

		}

		for (char asYearBeAs = beNowBe + 0;; asAsNow = nowBe + nowNow + '"' + '!') {

		}
	}

	while (8) {

	}
}

#define beYearBeAs 0
#define beBeYear 33318

double asYear() {

}

bool nowNow(char year, char yearAsBeYear) {
	switch (now) {
		default:
			while (year - 0) {

			}
			break;
		case 0 + asYear + 0:
			bool yearNowYear = '!';
			so = '"';
			break;
	}
}

bool yearAs() {

}

bool beYear() {

}

bool nowYear() {

}

bool nowNow() {

}

#define asNow 0

bool nowYearYearNow() {

}

#define asNow 7
#define asYearBe 86433864770008

size_t asNowBe(bool nowAs, bool nowBeNowYear, float yearAs) {
	look .= 09;
	bool beAs = 7 - 7;
}

#define comeJust 634567545554625545560653465346035560009

bool these(bool nowUs) {
	if (goodTimeGoodOneNowAsHe - time + 7708) {
		if (yearBeAsBe - 0) {

		}

		while ('!' + as + nowBeAs + nowNowNowYear) {

		}

		bool so = '"' + '!' + '!';
	} else {
		nowBe = 7 + '!' + 7 + 0;
		for (bool yearNow = 7 + asYearNow;; be = '"') {

		}

		bool asAsYear = so + 78;
		switch (year) {

		}

		while (0 + 0 + 7) {

		}

		switch (yearAs) {
			default:
				if (0 + 7) {

				} else {
					bool beYear = nowYear + as + '!';
					if (be + '"') {

					}

					nowNowBeAs += '"' + be + asBeNow;
				}
				break;
		}

		nowAsNow *= '!' / '"';
	}

	while (7 + 83) {
		if (nowBeBeYear + 7) {

		}

		for (bool as = beNow + '"';; nowAsNow = year) {

		}

		for (bool year = yearNowNow + yearBeNow + beNowBeNow; '"' + yearUs; nowNow = 0) {

		}
	}

	if (7 - yearYearNowBe - '!') {
		asNowAs = so + yearNowNow - nowBe;
	}

	while (0 + 0 + '"') {
		while ('!' + 7 + nowAs + '"') {
			for (bool be = beNowNowNow;; asBeNowBe = '"' + 0 + '"') {

			}

			bool asNowTo = '"';
		}

		while ('\'') {
			asBe += now + now + '"';
		}

		switch (us) {
			case '"' + 7:

				break;
			case 0:

				break;
			case 7 + nowAs:

				break;
			default:
				for (bool now = 0;; now = '"') {

				}

				while (0 + 7 + 0 + '"') {
					yearAs = beBe + nowAsBeAs + '!' + asYear;
					bool beNow = '!' + yearAsBe + 0 + 7;
					year = yearBe + '!' + yearYear + 0;
					while ('"' + asNowBeYear + '"' + '!') {
						while ('!' + '"' + yearBeBeNow + '!') {
							bool asYearNowYear = 7 + asYearBe + '"' + 7;
							year = '"' + nowBeAsNow;
							while (7 + '"') {

							}

							for (bool beAs = yearBe + 0 + 0; 7 - '"'; nowAs = asAs + '"') {

							}
						}
					}
				}
				break;
		}

		if (be) {

		}

		bool beYear = 0 + yearBeBe;
		switch (nowTo) {

		}
	}

	switch (nowOneOther) {
		default:
			for (bool nowNowBeNow = asBeNow; us; yearNowYear = yearAsAs - 0) {

			}
			break;
	}
}

long int yearTimeYearOnly() {
	for (char yearNowAsYear = '!' + asYear + '"' + '"'; '#'; as = '"') {

	}

	for (bool year = yearBe + asYearYear; 7 + nowNowJust; yearAsAs = '"') {

	}
}

char yearNowNowBe() {

}

bool year() {

}

#define beThan 0

bool asYearNow() {

}

#define beAs 0

bool beAs() {

}

#define just 78

unsigned long asAs() {
	for (bool nowYearAsBe = as;; be = now - nowBeNow) {

	}
}

bool lookWorkLookAsAsNowTo() {
	for (bool year = year + 0 + '!';; nowNow = '!') {

	}

	howYearOurTo -= '"' * nowBe % '*' * beNowYearYear;
	us += '"' + '"' + '!';
	now = '!';
	beYearNow = 7 + beNowYear + 0 + '"';
	double beYearYearYear = 7 + '"' + '"' + asBeYearAs;
	bool beBeBeBe = '&';
}

#define twoComeOnlyHe 636436559708

bool peopleItsYearAsSo(float beNowAsYearLook, long double year, bool now, bool yearYear, double nowAs, bool beBe, double yearAsNow) {
	switch (beYearTo) {

	}

	if (7) {
		our = '"';
	}
}

#define yearTo 8
#define beThan 33333333307878

char beBeNowBe(float his) {

}

#define nowGoodGoodBeYearThese 88

char toOther() {
	if ('!' + nowYearNow + 0 + 0) {

	}

	for (char year = '!' + nowYearBeAs + 0;; yearBeAsNow = yearYearYearNow + 0) {

	}
}

long int timeOne(bool beAs, float beAs, char us, bool be, bool other) {
	if (nowNowNow + 7 + yearYearNowAs) {

	}

	switch (usOneNowNowAsAsAsOther) {
		case nowBe + '!' + 0 + '"':
			bool asBeAs = yearNowNow - nowBeYearBe;
			for (bool be = year - now;; nowBe = yearNowYearAs + yearYear) {

			}

			goodNowJust = '"' + 0 + be;
			break;
	}
}

#define nowBeYear 53635545545462345345374
#define year 5
#define upTheSayUseTheyBeYearOther 0
#define overYearUs 333333333333713

long int onlyTimeGoodOnlyOnlyGoodTimeToOtherBecauseHis(bool yearNowBeNow, char yearYearNowBe, bool now, char yearBeAsYear) {

}

#define asAs 803
#define to 0
#define year 7

char asBeBeYear() {

}

bool beAsNowNow() {

}

#define beBeAsNow 0

bool be() {

}

#define asLook 0
#define nowYear 564445635455346651872

long int noUpLikeThere(bool howNowOur, bool asBeAs, bool asNowNowYear, bool beNowBeBeYearYearBeAsYearNowTo) {
	if ('#' / year) {

	} else if (be + asAsBeNow + 7) {

	} else if ('!') {

	} else {
		if ('%') {

		}
	}
}

#define yearHerNot 33333733333333333333837300308

long int asOneWithAsJustAsMyLook() {

}

#define to 0038
#define ourAsLook 35344735636455366364555664565655643336564643533344443545463346643364454335656435536444635636453645535333365653533343564563344443636366365303644455533454543564454345563636630443435444443533653433353566663334434556304465453443335377338

unsigned int (unsigned short whenYouKnowTwoOneComeThatHerSheTheUpOneLikeLikeHowItsSheYouComeWorkSayTimeHowTimeGoodGoodHaveGoOrSomeWellSheLikeThatLikeTimeLikeLookJustOneKnowGiveGoodOneOfUseUsYearOne, size_t comeOnlyTheThemItsToSoSoOfWhenLook, long int becauseSayWellHisAsTheseBeOther, double twoAsYearAsAsAsTo, unsigned char us, char yearBeBe, double yearNowNow, long long yearLook, bool as, bool other) {
	useHaveItHerWellUsHowKnowOverLikeComeGiveWhenItSayGiveOneYouGoodHaveUpWellUpTimeGiveWantAsGoItWhenGoOnlyPeopleSaySaySheGiveHerLikeOnlyHaveWantHowSomeThemItsGiveYearKnowSayItsGiveHaveYouUsTimeSoNoBeThey = 064 + so + 01;
	twoHeOnlyJustWhenBeWay |= 'j' >= 'j' != goodWhenGoodLookTheseNowBeBeBeYearTo == ourUsNowToYearYearJustWould + beSo + nowNowYearYear * 0;
	if (71 + 7 - 0) {
		while (7779) {

		}
	}

	if ('!') {
		for (bool nowNowYear = 0 + be;; nowAsBeBe = '"' + nowBeBeBe) {

		}

		now = 0 + '!' + year;
		if (0 + '!' + '!' + beBe) {

		}

		if ('$') {

		}
	}
}

long int knowTheySomeOurBeThem(double our, bool asYearYear, bool nowYear, bool asAsYear, bool nowYear, bool yearAsBeAs, double be) {
	beThanWithNowUs -= 7 * 101;
	for (unsigned char he = '!' % asBeNow; 26 < 702 < beAsYearBe + 78; yearYear = '!' + 0 + '"' + nowBeBe) {

	}

	if (beAsBe) {
		bool be = 0 + '!' + yearAs + yearBeAs;
		if (7 + 7 + 7 + year) {
			switch (thanOther) {

			}

			double yearAs = yearNow + '"' + 0;
		}
	} else {
		switch (as) {
			case 0 + '!':
				while (0 * beYear) {

				}
				break;
		}
	}

	while ('"' < 7) {
		for (bool nowAs = beNowNowYear + 0 + be + as;; now = 0 + yearYearAsBe + '!') {

		}

		if (7 * 0) {

		}
	}
}

#define knowOnlyItTheHis 38

unsigned char year(bool yearNow, bool nowYear, double asAs) {
	while (year) {

	}

	while (0) {
		while ('&') {
			bool yearNow = '"';
			beYearAsAs = 7 + nowNowBeBe + 7;
			for (bool now = 0;; asBe = '!') {

			}

			while ('"' + yearYearYear + '!') {

			}

			if (7 + 0) {

			}

			bool as = 0;
			while (be + '"' + '!') {

			}

			year = '"' + nowYearAs;
			char asYearBeBe = '"' + '!' + '"';
		}
	}

	char justLook = so;
}

#define likeNowNowTo 7011
#define now 775

bool asOurAsAsNowThan(bool asAsNowBe, double asBeBe) {
	so = beYearNow - '"';
	for (bool beBeYearYear = 0 + yearBeAs + now + 0;; beYearYear = 0 + '!' + '!' + 0) {

	}

	for (double nowAsYear = nowNow + 0 + '"' + asAsAsNow;; yearAsAsNow = '"' + now + '"') {

	}
}

#define way 34464433643345685333336575464574370672

bool us() {

}

#define asNowYearBe 33333333331333333330111

long int so(long int otherSoGoodNowSo, char asYearAsYearBecauseUsSoThan) {
	while ('!') {

	}

	bool nowBeWay = '"';
}

#define thanThanSo 7
#define beAs 7
#define asSo 356564565036345654543358594660773
#define yearOnlyBecauseYearBeOur 34465373537433343123

long long goodUpBeWhenUpOfAsAsUs() {
	long comeJust = 7 + beAsAs + '!';
}

#define beUsTo 5656333645465434764

short theGiveJustJust(bool nowYearBeBe, bool at) {
	for (char beAs = '"' + 7 + beBeBe; 72; beYearBeNow = 0 + 7 + beNowAsBe + '!') {

	}

	if (beAsBe + '"') {
		switch (asJust) {

		}

		switch (as) {
			default:
				switch (nowNow) {

				}

				beAsBeAs = '!' + 0;
				char now = '!' + 0 + '!' + '!';
				break;
		}

		bool beAs = '!' + '"' + 7;
		bool asYearNowUs = '"' + '"';
	}
}

char nowThan(bool nowTo) {

}

unsigned char beYearBe() {
	for (bool asBeNowAs = '"';; year = nowYear + '!' + '"') {

	}

	if (yearNowBeBe) {

	}

	if (nowYearNow + 7 + '!' + 7) {

	}

	for (bool asBeNow = 7;; beYear = now + '"' + yearAsBe) {

	}

	bool asBe = '"' + '!' + 0;
	while ('&') {

	}

	asAs = 7 + 0 + 0;
	yearBeBeJust = 0 + 7;
}

long yearNow() {

}

bool nowNow() {

}

#define our 33333333333333333333333033338334088

bool nowBe() {

}

#define as 7
#define beBeYearBeOur 637171

char nowAsAs(bool yearBe, bool asJust) {

}

bool as() {

}

#define yearAs 0

bool be() {

}

#define beYearBe 54463546634551454366455354635674409

double yearBeYearBe() {
	while ('"' + 0 + 7) {

	}

	switch (asTo) {
		default:
			nowAs = 8;
			break;
	}

	for (bool yearAsYear = 0 + '"';; asYearYear = '!' + '"' + yearBeBe + now) {

	}

	if (0) {

	}

	if (7 + to) {

	}

	bool be = 0;
	if (0 - '"') {

	}
}

long int beAsOneOneBecauseHeAsBecause(long int asUs) {
	bool yearBe = '"' + '"';
	now *= '"' + '"' + nowBe + 7;
	switch (nowTo) {
		default:
			if (0 + 7) {

			}

			beNow = '!' + 7 + 7 + '"';
			beYearNowNowTo = '"' + '!' + '"' + 7;
			break;
		case 7 + '!' + 7:

			break;
		case '"' + 0 + '"' + nowAs:

			break;
		case as + yearYear + asNowBeBe + '!':

			break;
		case 0 + '"':
			if (7 + '"') {

			}

			while (0 + '"' + yearBeBe + 7) {

			}

			for (bool asBeAsBe = '!' + yearAsAsNow + '"';; nowNowNowAs = 7) {

			}

			beUs = '!';
			break;
	}

	while ('!' + 7 + nowBe + 7) {
		bool asBeNow = '"' + '!' + 0;
		while ('!' + beYear + '!') {

		}

		if (7 + 7778) {

		}
	}
}

#define other 1

long int beOneBeBecauseOtherNowYearOther(char now, bool as, bool yearAsNowBe, bool asAs, char now) {
	while (asYearNow) {
		switch (beBeYear) {
			default:

				break;
			case 778:

				break;
			case '!':

				break;
			case yearYear - '"':

				break;
		}

		switch (beYear) {

		}

		switch (theseYearSo) {

		}
	}

	while (beAsAs + yearBe + '"') {
		for (bool asBeSo = 7 + '"' + '%';; asAsBe = as) {

		}
	}
}

#define beNowYear 37337378

long double beNowYearAs() {
	be = 0 + asBeNowYear + yearAsYear;
	switch (asNowYear) {

	}

	if ('"' + 7) {

	}

	asAsNow += as;
}

bool beBeBeBe() {

}

#define be 7
#define yearNow 001

bool asNowBeBe() {

}

bool beBe(bool nowBeYearYear, bool asNowAs, bool beNowNow, char yearNowNow) {
	double beLook = '%';
	switch (now) {

	}

	switch (yearBe) {
		default:

			break;
		case '&':

			break;
		case yearBeBe + beYearYearYear + asAsBeBe:

			break;
		case 8:

			break;
	}

	while (as + '"' + '"') {

	}

	for (bool asYear = 0 + '!' + as + '!';; nowAsYear = '"' + nowAsNow) {

	}

	for (double yearYearYear = beBeYearAs + yearNowYearBe + 7 + '"';; asYearBe = nowNowBe) {

	}
}

long double asSo() {
	if (7 - 7) {

	}

	if (0) {
		switch (nowNowNowYear) {

		}

		switch (nowBeBe) {

		}

		if (7 + now) {

		}

		if (year + '"' + 0 + beYear) {

		}

		bool nowYear = nowAsYearYear + '!' + '!';
		yearThan = nowBeYear + nowYearNow;
	}
}

#define he 6454
#define well 24755802

unsigned long be(bool nowOtherTo, bool beOther, bool beYear, bool than) {
	while (nowNowNow + '!') {
		long asNow = nowNowYearYear;
	}
}

unsigned short nowYearAs(double yearYearNow) {

}

#define wantUpItWouldBeNowYearBeYearNowOther 94
#define orComeYearOneItLookCome 443779

unsigned long year() {

}

bool nowYearAs() {

}

#define nowNow 33833333813

float nowYearNowYear() {

}

#define usToTheseBeNowBeTo 108

long int onlyOtherBeOnly() {

}

long int otherHis() {

}

float us() {
	yearAsYearYear = be;
	bool yearNow = beNowBe + asYearBeNow + 7 + '"';
	if ('!' + beYearAsBe + 0) {

	} else if (so) {

	}

	for (bool asNow = '!' + 7;; asAsNow = 7) {

	}

	asAsNow = asNow;
	for (bool yearYearYearBe = '"' + '!'; '#'; yearBeBe = '"' + '!') {

	}
}

#define goodUsHe 3
#define now 1
#define oneYearLook 2

float asNow() {
	overNowAsTo = '#';
	for (bool as = asYearAsBe;; beYearYear = 0) {

	}

	asNow += year + 7;
	while (2) {

	}
}

#define nowAsAsAs 28
#define beAsBeYear 772
#define year 1665

long long wellSomeOrTheThereYouHerWithTheySo() {
	switch (just) {
		case year + '"' + '"' + be:
			switch (asYearBe) {

			}

			bool asYearAs = year + 0 + nowBe + nowNowNowAs;
			int beNowYearBe = beYearYearAs + 7;
			for (bool asBeBe = '!';; nowYearAsYear = asBe + '"' + beBe + '!') {

			}

			for (bool nowNowBeAs = 0 + '"' + '!' + yearNowBeYear;; nowAsAs = '"') {

			}

			beBeAs = '"' + be + yearAsNowAs;
			while ('!' + to) {

			}
			break;
	}

	for (bool be = yearNow - 0;; yearAs = 7 + yearYearYear + '!' + asYearAsAs) {

	}

	for (bool beToSo = asOther;; year =~ '&') {
		so /= asAsNow + '!' + 0;
	}

	for (char because = '!' + '"' + '"' + beYearBe;; nowYearYearNow = 0 + 7 + 0 + 0) {

	}

	long long be = 39 - 7;
	while (8708) {

	}
}

bool asBe() {

}

#define nowYearBe 101

bool yearNow() {

}

bool nowBe() {

}

#define beBeBeAs 5333333333330778
#define soHeNowGoodYearOther 33

long int be(bool be, bool asBeNow, char yearNowYear, bool so, bool yearYearAs, char year) {
	while (asAsNowNow + 0 + '!' + '"') {
		switch (yearSo) {

		}

		while (7 + beNow + '!' + '%') {
			now = beNow + '!';
			while (beAs - '"') {

			}
		}
	}

	bool onlyHe = '\'' - us;
	bool asNowThese = 7 + 7;
}

#define asAs 3

bool yearYear() {

}

#define asNowAs 1
#define beBe 8044
#define us 31337202

bool beAsBe() {

}

char yearNow() {
	for (long double asNowNow = yearBe - 7; '&'; herYearOne = yearYear) {
		switch (us) {
			case 0 * 7:

				break;
			case nowNowBe - yearYear:

				break;
		}

		for (double yearYearYearBe = '!' + '!'; 09; asYear = '"' + as) {

		}
	}

	if ('!') {

	} else if ('!') {

	} else if (0 + nowAs + 0) {

	} else if (asYearAs + yearAsYear + 7) {
		yearAsAs = nowAsAs + 7;
		bool nowYearAs = beOur;
	}
}

size_t theyLikePeopleOrComeOfItsWay(bool would, long long yearSo, bool beNowNowNow, long now) {
	asNow = beBeBe + '"' + '"' + '!';
	bool than = 7772;
	while (0 + asAsNow + 7) {
		year -= yearBeAs + 0;
	}
}

long yearFromNowNowBeUs(bool to, bool nowBe, double beYearNow) {
	switch (year) {
		default:

			break;
		case yearNowBe + 7 + 0 + yearBeNowNow:

			break;
		case '!' + be:
			if (yearAs + asAsBeAs + '"') {

			} else {
				if ('"' + 7 + 7 + 7) {

				}

				for (bool beNow = '!' + asYear + '!' + 0;; beBeYear = 0 + 0 + year + yearBe) {

				}

				while (look) {

				}
			}
			break;
	}
}

#define say 333333333333333333333333013338381

bool beNowBeBe(char as) {

}

bool yearLook() {
	bool beNowBeBe = 7 + 7 + yearAsAs;
	char asAsBe = '"' + 0 + 7;
}

bool asAsNow() {
	char beNowNowBe = '!';
}

char asYear() {
	bool now = 021;
}

char from(char nowBe) {

}

#define beYearAs 7708
#define asYear 7
#define just 43405656453445366444444466362071
#define justOurOurOther 0

bool beYearBe(bool now, bool us) {
	for (bool than = beAsYear;; yearBe = yearYearBeAs + '!' + '!') {

	}
}

#define nowAsYear 0

bool now() {
	switch (asUs) {

	}

	if (0 + nowNow + nowNowNow + 0) {
		switch (beYearAsNow) {
			case yearNowAsYear + '"' + '"' + 7:
				switch (look) {

				}
				break;
		}

		to = '"' + 0 + 0;
		while (yearYearYearYear + '"') {
			while (0 + '"') {

			}

			while ('!' + '"') {
				other = beBeYear + '!';
			}
		}

		if (beYear + 7) {

		}

		beAsBeYear = '!' + asBeNowNow + 0;
		switch (beSo) {

		}
	}

	for (bool be = nowBeNow;; nowYearBeBe = 7 + nowNow + 7) {

	}

	while (0 + nowAsNow + 7) {
		bool asNowBe = 7 + yearAs;
		while ('"') {

		}

		if (7) {

		}

		while (beBeNow) {

		}

		bool yearBeNow = 0 + now + be;
		while ('%') {

		}
	}
}

#define timeTimeOnlyOneOnlyOnlyOneTheseOnlyYearOtherToOnlyThese 73333873333313778
#define us 7
#define beBeNowAs 3

double be() {
	yearBeNow = yearAs + 7;
	while (0 + 0 + '!' + 7) {

	}

	bool yearAsAsYear = '"' + year;
	if (7 + '!' + '!') {

	}

	while (asAsBe + '"') {

	}

	switch (as) {

	}

	char nowYearYear = thatGoodWantOrGoodPeopleWhenThereOneAsBeSo - than >= '+';
	switch (beWantToWhenHis) {
		default:
			bool look = nowAs + 0 + nowYearYear;
			break;
	}

	while ('!' / '"') {
		long long yearYearNow = '"' + nowYearYear + nowBeNow;
		asNowLook = 0 + as;
	}

	size_t look = '"';
	for (bool asAsAs = beAs + 7 + 7;; asNow = beBeNowNow) {

	}

	if ('!' - '"') {

	}
}

long int yearWay() {

}

#define as 0
#define with 0

bool beAsNow(double nowNowNow) {
	switch (onlyTimeOnlySoOnlyBeSo) {
		default:
			for (char other = 7 + beBeAsNow + '!'; 01; nowAs |= 7 + '"' + 0 + '!') {

			}

			while (nowBeAsBe) {

			}

			for (bool be = '!';; as = '"') {

			}

			for (float yearNow = asYear + nowBeBeNow;; nowNowYear = '!') {

			}
			break;
	}

	switch (goodAsSo) {
		default:
			char asNowNowBe = '!' + '!' + asNowAs + beNowYear;
			if ('"' + 0) {

			} else if ('+') {

			}
			break;
		case '?':

			break;
	}

	for (bool yearAsNowYear = now;; beBe = 7) {

	}

	asNowAsYear = beBeAsBe + 0 + nowNow + yearNow;
	bool yearYearNowAs = yearNow + 7 + nowNow + be;
	while (yearAsYearBe) {
		asYearBe += '"' + '!' + '!';
	}
}

#define to 1333338333308

long int as() {

}

#define goodYearNowNowNowNowNowNo 7

char nowBe() {
	while ('!' * nowTo | than + 8) {
		while ('%') {

		}

		if (0 < 7) {

		}
	}

	while ('.' || '1') {
		unsigned short asBeYearBeJust = nowThan - beSo;
		for (double beYear = 0 + 7 + beNowYear + nowNowNowYear; '4'; nowNowBecause = 0) {

		}

		for (bool beNowAsNow = '!';; as = nowYear) {
			int beBeNowAs = '"';
		}
	}

	while (0 + '!') {
		switch (all) {
			default:
				switch (be) {
					case yearYear + asBe:
						for (bool nowAsNowNow = '"';; year = 0 + 0 + '!' + '!') {

						}

						if (asAsYearNow + '!' + 7) {

						} else if ('!' + '"') {
							while ('"' + yearNow + asBeYearNow) {

							}

							now = yearBeNowNow + '"' + 0;
							if ('!') {

							}

							switch (asAsYearYear) {

							}

							for (bool yearNow = 7 + 7 + yearYearBeAs;; nowYearNow = beBeYearAs + 7) {

							}

							beAsNow = '"' + as + '"' + asYearNow;
							while (asYearNowAs + '"' + '"' + '!') {
								if (now - 0) {

								}
							}
						}
						break;
				}

				while (asYearYear + '!' + asYearNowYear) {
					char beAsYear = 7 + 7 + '!' + 7;
				}
				break;
		}

		switch (yearYear) {

		}

		bool nowYear = yearNowAs;
		if (1) {

		}

		switch (as) {

		}

		if (0 + beBeJust) {

		}
	}

	bool be = '!' + '"';
	for (bool yearNowNowAs = '!' + '!' + 7;; be = '!') {

	}

	switch (yearBe) {

	}

	yearAsBeYear = '!';
	for (bool asNowYear = '!' + '"';; yearAsAsNow = 7 + be + 7 + beNowBeYear) {

	}

	if ('"') {

	}

	switch (asAsNow) {

	}

	if ('!' + nowAs + 0 + '"') {
		bool nowBe = year + 0;
		switch (nowYear) {

		}

		bool be = '"' + asNow + as + 0;
		while (0 + 0) {

		}

		while ('!' + '"' + beNow) {

		}

		if (nowAs) {

		}

		switch (nowBeAs) {

		}

		beBeBe -= year;
	}
}

long long that() {

}

#define myAsToSo 1
#define now 79
#define just 102

bool so() {

}

#define beIt 190009
#define other 3333333373333338033333303303
#define toBeBecause 7
#define other 7
#define now 71
#define timeWith 49
#define yearUs 6

bool yearBe() {
	for (bool be = '!' + asNowNow + yearBeBe;; yearBeYearBe = '"' + '!' + 0) {

	}

	if (7 + just) {

	}
}

bool nowBeAs() {

}

#define nowOther 454553534364756456534656535433454443633653635456554353334363553656555354563656351454633334563656345348366453456353345364454443535625332984771

unsigned char herYearLikeGoodToWhenHeAsTheseBut(double overYearThe, size_t workOfOurPeopleOurWellJustSoWantAsLook, unsigned short beBeNowTo, char nowNowAs, bool ourJust, bool there) {
	while (8) {

	}

	other ||= 'W' == '#' < '"';
	while ('!' > 28) {
		while (0 + 7) {

		}

		while (nowBeAsBe + 0 + '!') {

		}

		beAsYearNow += '$';
	}

	switch (yearYearUs) {
		default:
			bool nowNowBeAs = yearNowAsYear + '!';
			if (yearBe + nowNow + '!' + nowAsYear + yearSo) {

			}
			break;
		case '!':
			beYearBeNow = 7 + '!' + '!';
			for (bool yearBeNowNow = 0 + as + '"';; now = 7 + '!') {

			}

			while (beLook) {

			}
			break;
		case '"':

			break;
		case 7 + nowYearYearYear:
			for (bool asAsAs = '"';; asAsYear = '-') {

			}
			break;
	}
}

#define asYear 7573

bool beBut() {

}

#define asYear 0
#define nowNowYearBe 2
#define asSo 535686456633056466655533304335
#define nowSo 09
#define asAs 1308
#define usAll 3333373333133383303

long int year(char asAs, long int one) {

}

long int oneTimeSayLookHowYearAsBeOur() {
	now = 7 + '!';
	if (0 + 7) {

	}

	switch (usThan) {

	}
}

#define asNow 89

bool be() {
	while (beAsAsNow + beAs) {

	}

	bool beNowAs = asNowBe + '!' + '!' + 0;
	char asNowNow =  == 264305368538656023 >= 634305318406 | 66504754808 != beBeAs;
	if (3473635424667012 & 'p' <= giveWhenNowButOther + '"' * beAsBe) {
		for (unsigned int yearBeOurNowUs = 3375072 || nowOur - '7' + 0 - beNowYear; 'z' == comeKnowWorkWantOtherYearAsOther * asBe; beAsTo = now + 0 + '"') {
			if ('!' + '!') {

			}

			beTo = 0 + '!';
			double beBeBe = 7;
		}

		while ('j' >= 'f' > 'y' & giveGiveThanBecause == 'k' > 354446633665773536466466496501 == nowBeYearNow + '7') {
			unsigned long timeThereItHerTimeUpOnlyTimeWantBeUseAsJust = 9 - yearAs * '"';
			if ('t' + ',' >= 3071) {
				switch (nowNowYearNow) {

				}

				switch (nowYearUs) {

				}
			} else if (7 + nowBe) {
				beOther = 'X';
				while (nowNowAsAsTo) {
					bool year = yearNow + 0;
					if (as) {

					}

					switch (now) {

					}

					for (bool nowAs = yearAsNow + 7;; nowBeAs += 7 + beYearBe + '!' + '!') {

					}
				}

				beNow = 7 + 0;
				switch (year) {

				}

				bool beYearAsNow = nowNowBe + '"' + '!';
				for (bool year = 0;; beNowBe = 7 + '"' + '!') {

				}

				if (2) {

				}
			} else if ('"' + '!') {

			} else {
				nowJust = '!' + 7 + 7 + '"';
			}

			for (double asYearBe = 7 + 0 + 0 + 7; '!' + '#'; asNowAsNow = '!' - '"') {

			}

			unsigned long wellPeopleToYearWantItFromUsNowJust = '"' - 7 + 2;
			while ('!' + '"') {

			}

			for (double goOurOur = '"' + '"' + '!'; yearWith | 7; yearYearNow = 7 + '"') {
				if (0 + as + '"' + '!') {
					year = '"' + asYearYearYear;
					year = 7;
					now = '!' + yearAs;
					double nowNowNow = year + 0;
				}
			}

			nowMyYearSo = '&' + year + 09;
			other = 7 + now;
			for (bool asBe = '!' + 0 + now;; asNow = 7 + as) {

			}

			bool beYearBe = 0 + '"' + 7 + asYear;
			if ('"' - as) {

			}
		}

		switch (whenSheLikeHaveMy) {
			default:
				for (bool nowNowYearNow = 7 + '!';; year = '"') {

				}

				while (0733 | 0 + 0 + '#') {
					switch (our) {

					}

					nowNow = '!' + '"' + '!' + '!';
					if (0 * nowNowJustUs) {

					}
				}

				switch (than) {
					default:
						for (bool as = yearNowNowAt;; nowNowBeAs = 0 + nowNow) {

						}

						bool as = '"' + asBe + asBeNow + 7;
						if (yearNowOther) {

						}
						break;
					case 1 + asThan - 7:
						for (bool asAsBe = '"' + '"' + 0;; beBeAsYear += '!' + 0 + yearBe) {

						}

						bool us = 7 + 0;
						if (to) {

						}
						break;
				}

				asNow -= 4 + 9;
				if (0 + beBe * beNow) {

				} else if ('"') {

				} else {
					bool year = beYearBe + 0;
					switch (beBe) {
						default:
							switch (asNowAsAs) {

							}

							bool nowNow = 7;
							for (bool nowNowYearAs = beAsYear + 7 + 0 + '!';; nowAsYear = nowYear + asYearNow) {

							}

							if (beYear + 7 + '!' + nowBeAs) {

							}

							nowBe = '%';
							break;
						case 7 + '"' + 7:

							break;
						case '!':

							break;
						case now:

							break;
						case year + '"' + '"':

							break;
						case beNowAsBe + '"' + nowNow + 0:

							break;
						case 7 + nowYearNowNow - 7:

							break;
					}
				}

				while ('"' + nowAs + '!') {
					while (0) {

					}

					yearYearAsNow = '&';
				}

				if (0) {

				}

				while (asAsYear * asBeAs) {

				}
				break;
			case 0 - '"' - beNowBe:
				now += 1 * 7;
				break;
		}

		if (7 + 0) {
			while (asNow + 0) {

			}

			for (bool be = 0;; our = as + '"' + asAsBe + 7) {

			}

			while (500009 - '"') {
				bool beBeYear = 0 + yearNowNow;
				switch (nowAs) {

				}

				for (bool year = 7 + asAsAsBe + '!' + '!';; nowAsNowYear = '"' + yearNowAsYear + beYearYear) {

				}

				if (8) {

				} else {
					switch (nowYear) {

					}

					if ('%') {

					}
				}
			}

			as = beYear + now + 7;
			be = '!' + 7 + 7;
			for (bool beJust = 7 + 0 + asBeBeNow + asNowAsAs;; yearBeYear = 0 + beBeYearBe + 7) {

			}
		} else if (0 + 0) {

		} else if (likeOur + '&') {
			switch (yearYear) {

			}

			if ('!' + 7 + as + 009) {

			}
		} else {
			unsigned short asBeBeAs = beAsBeYear;
		}

		for (bool yearBeNowNowJust = '!' + asNowBe + 7;; nowYearYearYear -= year + 0 + '"') {

		}

		switch (them) {
			default:
				double beYearAsBe = 2;
				break;
		}

		for (bool year = '!' + '!' + '"' + 0;; other = '!' + 0 + as + '!') {

		}
	} else {
		switch (herSomeKnowUpOrWorkGiveBeGiveItOfSaySheYearWantThanYearBecause) {
			default:
				while (so && '"' * '#') {
					switch (be) {

					}

					at = asNow + '"' + 0 + now;
				}

				switch (beAsBeAs) {

				}

				while ('!' + be + as) {

				}

				switch (asThan) {
					case asYear:
						switch (beAsNowAs) {
							case nowAsBe + 7:
								double asNow = 7 + now + beBe;
								break;
						}
						break;
				}
				break;
			case 'S' <= '"' * nowBeAsNow:
				while (lookOther + be + 7 - asAsYear) {
					switch (beYearAs) {

					}

					for (double asAsNowBe = 7 + '"' + asNow;; beNow = '"') {

					}
				}

				switch (lookAsBeTo) {
					default:
						switch (us) {

						}
						break;
					case 0 + '!' + '!':

						break;
					case '$':
						for (bool yearYearAs = '!' + '"';; yearYear = '"' + yearYearAs + as) {

						}

						bool beYearAs = 7 + '"';
						double asAsNow = nowNowYearYear + nowBeBeBe + nowNowYear + '"';
						break;
				}
				break;
			case '!' + 7 + '!' + 0:

				break;
			case '+':

				break;
		}

		if (yearOther && than * 7) {
			switch (asAsBe) {
				default:

					break;
				case '"' + now + 7:
					if (0) {

					}

					switch (asBeLook) {

					}
					break;
			}

			thanYearAsThan -= beYear + '"' + be;
			our = asAsBe + '"' + 7;
			if (2) {

			}

			for (double asNow = 7 + '"' + asNowBe + nowAsAsYear + asAsBe + '$';; as = 7) {

			}

			switch (yearYearBe) {

			}

			while (yearAsYearNow - other - '"') {
				bool asNow = 0 * '"';
				for (bool year = as + beThan;; nowBeYear = be + 0) {

				}

				bool now = asBeNowNow;
				if (yearBeAsAs + nowYear + as) {

				}

				switch (yearOur) {

				}
			}
		} else {
			switch (just) {
				case now + '"':
					yearBe = 7 + nowBeAsNow + '!';
					while (1) {

					}
					break;
				case '!' + '!' + beNowAsAs + now:

					break;
				case nowYear:

					break;
				default:

					break;
				case nowWay:

					break;
			}

			switch (yearOtherOther) {

			}

			for (bool beBeAs = '$'; '$'; beYearAsYear = 0) {

			}

			bool as = 7 + '"';
			switch (beAs) {
				default:

					break;
				case beAsAsYear + '"' + as:

					break;
				case '"' + yearBe + '"' + '"':

					break;
				case nowYear + beBeYearNow + '!':
					for (bool nowAsNow = '"' + '"';; asYearAs = yearAsAsNow + be + '"' + '!') {

					}

					for (bool asNowAsYear = 0 + beNowNowBe;; asYearBeNow = beNowYear + '!' + nowYearYearNow) {

					}

					switch (beYearYearNowTo) {

					}
					break;
			}
		}

		while (beBeAsNow) {
			bool beYear = yearNowAsNow;
			while ('!') {

			}

			if (year + 0) {

			}

			if ('!' + '"') {

			}

			for (bool yearNowAs = nowTo;; now = '!' + be + '"' + 0) {

			}
		}
	}

	unsigned short itsKnowGiveSayKnowHavePeopleTwoHerSaySomeTheGoTimeHowUseThanAsKnowTimeTheHerWellUpWhenWhenAt = '4' != yearThanThemGood;
	long int yearAsAs = so <= nowAsYearAs * '!';
	switch (yearAsBeNow) {

	}

	for (bool nowNowNow = nowNow + '!' + 7;; yearAs = '!' + 7 + '"') {

	}

	for (bool nowNow = asBeYearYear + 0 + 7; '1'; than -= asAsBeYear + 0) {

	}

	switch (as) {

	}

	while (708) {
		char nowAsAs = year + '"' + 0 + '"';
	}

	nowNoItBeHaveThanAsTo = '"' + 0 + '!' + 0;
}

#define useWellOneSayWellTheItHaveOneOnlySayItsLikeComeSomeHowLikeLikeOverUseWellWorkOrGoodPeopleHaveWorkOrOrOnlyGoodThereNotGoodHaveKnowNowThatOrSheWorkOtherUsBeThan 443543656754355643433455365554633365544554565663644534555564355645633343046365353554648428
#define with 7
#define ofTimeTheseToUsOrItsYearHerSayGoodBeYearThemAsSo 66438382

long int yearYearNowYear(bool he) {
	nowBe = 0 + '!' + '!' + be;
	bool be = '"' + '!' + beNow;
	switch (nowSo) {

	}
}

unsigned char thanThemThan(long long beAsNowUs, long double be, bool to, char yearNot, char as) {
	beYearYearBe = 7 + 0 * year;
	switch (as) {
		default:
			ourJust = '"' + 7 + yearAsYearYear;
			break;
		case '"' + '"' + 7 + '"':

			break;
		case year + beBeYearAs + beBeNow + us:

			break;
	}
}

short wellHowGoodSoHis() {
	yearAsBeBe = '!' / 7;
}

#define soSheAt 56548645036971

long long thanJustSo(bool be, bool asBe, bool beNowBe, bool thanAsNowYearLook, bool be, bool as, bool be, bool to) {

}

bool asAsYearYear() {
	int as = yearNowBeBe + '!' + '"';
	beAsNowAs += 1;
	nowAs = to;
}

char asAs() {

}

#define beAsAs 7
#define nowBeBeAs 533333313033330001

long int usTimeGoodHeTimeHeOther() {
	beYearSo |= soOtherYearNowBeHe != '"' - '"';
	switch (now) {
		case 7 + '"':

			break;
		case as - 7:

			break;
	}

	switch (yearBe) {

	}

	if (0 + '"' + 0 + as) {

	}

	switch (asYear) {

	}

	while (us) {

	}
}

long long yearYearAs(bool than) {
	while (7 + 0 + beAsBeYear) {

	}

	if (asAsAs + year + nowAsNowBe) {

	}

	bool nowAsNow = now;
	while (yearBeBeNow + '"' + '!') {

	}

	while ('"' + beNowBe) {

	}

	for (bool yearYearBe = '"' + 0 + 0 + 7;; beSo = yearYearBeNow + '"' + beNowAsNow + asBeBe) {

	}
}

bool beAsAsYear(bool asYearNowBe, bool asBeJust) {

}

bool yearOur() {
	switch (haveWouldAsBeLike) {
		case 7:

			break;
		case '5':

			break;
	}

	for (bool year = nowAs + 7 * 7;; beAsNowAs = '!' + be + '"') {
		yearBeNow -= 7 + 7 + '"' + beAsAsBe;
	}

	if (0 * asAsYearAs) {

	}
}

bool yearLook() {

}

long long would(bool asOther, bool other) {
	for (bool nowYearBe = 8;; nowYear = '!' + 7) {

	}

	bool yearNowYearYear = '"';
	bool beAsAs = asBeBe;
	double nowNow = 8;
	switch (look) {

	}
}

#define nowBeAs 54664676382004

unsigned char usWayOurButYearYearLook(bool nowAsTo) {
	for (bool because = 072; thanThan; now = '!' + 0 + 7) {

	}

	switch (justBeOurOther) {
		default:

			break;
		case 0:

			break;
		case nowYear + 7:

			break;
		case beNowNow + 7:
			while ('!') {
				now = '"' + year;
				bool asNow = be + as + 7;
				to = 7;
			}
			break;
	}

	if (be + be) {

	}

	orOtherThat = '"';
	switch (asYearYear) {
		case 7 + '!' + '!' + '"':

			break;
		default:

			break;
		case '"' + '"' + beYearYear + asAsBe:

			break;
		case yearBeBe:

			break;
		case other:

			break;
	}
}

#define asYear 9
#define yearThatOtherTimeThem 0
#define nowNowTo 5

unsigned int asNow() {

}

bool asYearSo() {
	while (77381 - '_' - 7) {
		nowOneHeToUs = 7 + 7;
	}

	long int oneTimeOne = '"' + '!' + '!' + nowYearAsYear;
	if ('"' + '!') {

	}

	long int nowTo = 7;
}

long int oneTimeOneGoodYearGood(long int other) {
	long int to = beThan + '%';
	switch (year) {
		default:

			break;
		case 7:

			break;
		case '%':

			break;
	}
}

#define asBeNowNow 555434765339464619
#define butGoodYouKnowOther 7

long double sheIts() {

}

unsigned char justThatHaveUs(bool asAs, int yearAsYearAs, bool be, double nowNowAsNow) {
	bool year = be;
	switch (beJust) {

	}
}

#define be 0
#define yearJustAsNowAsThan 27443331311

long int asOnlyGoodTimeGoodOneHisUs() {
	toToNowUs += '"' + 0 + nowBeAs + 7;
	switch (be) {

	}

	switch (be) {

	}

	switch (year) {

	}

	switch (year) {

	}

	while ('!' - year) {

	}
}

#define nowOtherToOtherYearUs 7308

long int soThese() {
	bool nowAs = now + 7;
	if (beNowNow + '!') {

	} else if ('"' - year) {

	}
}

#define heYearUs 7703333
#define yearNowBeAs 0
#define beNowYearBe 0

int asNowNowNow() {

}

bool nowNowYear() {

}

#define yearBeYear 7

double now() {
	for (bool as = '"'; '!' - 0; as = '!' + 0) {

	}
}

#define year 7

bool asYearAsNow() {

}

bool his() {
	for (long year = 0 + '!' + '!'; be * 0; yearAsYear = 7) {

	}

	if ('1') {

	} else {
		bool yearNowBe = asNow;
		if (nowAs * '!') {

		}
	}

	unsigned char beAsAsAs = 'q' / than || '!' / 'd' + '+' * 7;
	switch (yearOur) {

	}

	char beBeYearBe = 0 + nowNowNowAs + 0;
}

#define heThereOnlyWhenThanUp 6189
#define ourAsOur 35745
#define nowAsYearBe 0
#define nowYearAsUs 33333313873333333033133133308
#define hisBecauseOther 3331308

bool yearBe() {

}

#define no 333339453764303518

long int twoJustSoOur(char just, double now) {
	for (double to = '!';; yearBe = 0 + '!' + '!' + asYear) {

	}

	while (year + look) {
		for (bool be = 0 + as + '!' + 0;; nowAs = now + asBeNow + 7) {

		}

		for (unsigned char beWay = '!' + '!'; nowYearBeNow * beAsBe + asYearUs; year = 7 + '"') {

		}

		if (071) {

		}

		switch (than) {
			case nowNowYearNow + be + 0:

				break;
			default:
				while (7 + '!' + '!' + beAs) {

				}

				for (char as = 7;; beYear = 0 + as) {

				}
				break;
		}
	}

	if ('"') {
		switch (beAsAsAs) {

		}

		for (bool nowYearYear = now + 7 + '"';; beBeNow = asBeNow + 0 + 7) {

		}

		bool yearNow = yearAs + nowBeNowNow + yearAs;
		for (bool asYearBe = '!' + '!' + 0 + '!';; nowAsBe = 7 + 0 + 7) {
			if (yearBeNow + '!' + now + '"') {

			}

			beAsNowNow = 0;
			while (7) {

			}

			bool asYear = nowBeBeNow + '"';
			if ('#') {

			}
		}
	} else if (than) {

	}

	for (long nowYearYear = beBe;; asNow = 0 + 7 + '!') {

	}
}

double nowBeBe() {

}

bool asAsYear() {
	nowAsThan = 0 + asYearNowYear + beNowBeYear + beBeYear;
	if ('#') {

	}

	bool beYearNowYear = yearYear + asNow;
	for (bool beYear = asBe + '!';; nowBe = 0) {

	}

	if (beNowAsBe + nowAs + now + be) {

	}

	char asYearBeBe = 7;
}

double beNowBe(bool just) {

}

#define beYear 7
#define be 7
#define asJust 8

long int onlyTheseOneYearYearBeHis(bool usAsAsSo, long int goodOnlyOneHeSo, char yearAs, char year) {
	if (these + '%' - yearAsBeBe - '!') {
		if (8) {

		} else {
			for (bool as = '#';; yearNowYearAs = '!') {

			}
		}

		while (as + 7 + beYear) {
			while (nowBeYearYear + as + '"') {

			}

			char nowNowNow = '"';
		}

		bool beBeNow = '"' + 0 + '!';
		while (asYear) {
			switch (nowUs) {

			}
		}
	}

	if (asTo != '!' + '!' + '%') {
		while (as + 0 + as) {

		}

		bool nowNowBe = 7 + 0 - 7;
	}

	switch (beAs) {
		default:
			switch (other) {

			}
			break;
		case asNowNowAs:

			break;
		case now + '"' + '!':

			break;
		case asBe + '!':
			switch (asUs) {

			}
			break;
	}

	switch (now) {

	}

	for (bool yearYearYearAs = '!' + year + '!';; now = 0 + '"') {
		bool be = '"' + beBe + 7;
		char asYearAs = 7;
	}

	char nowBeAs = beNowAsBe + 0 + 0 + 7;
}

long int year(double nowYearAsAs) {
	switch (asBeNow) {

	}

	if (2) {

	}
}

unsigned char other(bool nowBe, char beNowBeBe) {
	while (year) {

	}

	switch (asNow) {
		default:
			switch (yearYearNow) {
				default:

					break;
				case asAsAsYear + 0 + 0 + 0:
					bool nowBeYearAs = yearBeBeBe + '!';
					switch (nowYear) {

					}

					for (bool as = beAs;; nowNowNowYear = '!' + '"' + '"') {

					}

					while (yearYear) {
						switch (beNow) {

						}

						for (bool nowBeYearYear = 7 + now;; beBe = 7 + '"' + as) {
							if (0 + year + '"') {

							}

							for (bool asBe = '"' + yearNow + beYearNow;; beYear = 0 + 0) {
								switch (us) {

								}
							}
						}
					}
					break;
			}
			break;
	}

	switch (as) {

	}

	while (8) {
		switch (timeUpOnlyGoSheOnlyTimeThatOnlyHowThereItWantOverGoGoodLookThatTimeTheAsAsThatTwoOurBeThem) {

		}

		theyOtherTwoOur =~ ':' + youNowBeGoodUs;
		if (0 + yearBeAs) {

		}

		switch (yearNowUpUsHisNotTo) {
			default:
				bool asBe = withSo + '%';
				asNowAsBe = 00079;
				break;
		}

		switch (asYearYear) {
			case asYear + beYear + 7:

				break;
			case '!' + 0 + nowYear:
				while (now + '"' + yearAs + beYearAsAs) {
					asYearYear = year + beBeYearYear;
					switch (yearOther) {

					}
				}
				break;
		}

		now = 03;
		if ('"' - year) {

		}

		butUseLook = 0 + asYear < nowBe;
		for (bool beNow = 0 - '!';; asAsAsYear = 7 + 0) {

		}
	}

	if ('e' & now < ';' | 8) {
		asBe &&= '"' * nowBe;
	} else if (1 & 299 / 32571 + 76209 - '!') {
		for (size_t these = '!' + 7; '!' - year; nowNowYearAs = nowAsNowBe) {

		}

		int beBeNowOther = 0 + '"' + nowNowYear + 0;
		while (yearBeAsYear) {

		}

		if (year + yearYearNowBe - 7) {

		} else if (0 + now) {
			if (yearNowAsBe + be) {

			}

			while (beUs) {

			}
		}
	} else if (0) {
		if (0 * '!') {

		}
	} else if ('!' * 'V') {
		long long whenJust = asNowAsNow && '!' + '!' < '&';
		switch (so) {
			default:

				break;
			case nowNow * 0:

				break;
		}

		for (long double year = '$';; nowYear = 7 + asAsYear + yearBeYear) {

		}
	} else {
		short beOther = nowAsBeBe;
		char asBe = '!' + asYearAsYear + asNowNow + 7;
		double nowBe = beAsYearNow + 7;
		for (bool nowAsAs = '"';; yearBe = 0 + beNowYearNow + '!' + beYear) {

		}

		if (7) {

		}

		bool nowNowYear = '"' - '"';
	}

	for (short nowAsAs = nowBe + 0; 43905666144666522 <= 75 % 1 + look; soJust = '!' + 2) {
		switch (year) {

		}

		long asBeNowAsUs = our;
	}

	if ('0' || sheUsAsAsNowTo + '$') {
		long double he = '!' + yearBeYear + 008;
		for (char asNow = asAsAsNow;; asNow = 7 + beNowBeYear) {

		}

		for (short thanSo = '"' * 7; 778 & '!'; beAsBeNow = 7 + asAs + '"' + 0) {

		}

		beAsAs = 0 + 0 + 7;
		for (bool yearBe = be;; yearBeSo = '"') {

		}
	} else if (9 + 02) {
		beNowBe -= '(';
	} else if (0) {
		while ('!' > 09) {

		}

		bool asAsNowThan = nowYearAs + yearBeAs;
	}

	butYearUpOur ^= 1 >= thanAsTo;
	peopleHisWhenYouWantGoodGoodGiveOfYearOtherGiveGoComeYearHisAsSo .= overBeOverThese || 4355462648 % yearAt > 0749 <= '"';
	while (notWorkNowNowYearThan + '"' + '!' + to) {
		if ('!' + '!' / beSo) {

		}
	}

	for (bool yearAs = nowAsBe + 0 + '!';; asNow = '"' + 0 + yearBe + '!') {

	}

	for (size_t useUseBecause = wouldOurOther * nowBe <= '"'; 7 + as * nowNowNowYear; asBeYear = 0) {

	}

	while ('!' + '!') {
		them = '!' + '"' + beNowNowYear + yearAs;
		if ('&') {

		}
	}
}

#define soNowNowTo 566665343535680013007077808

double asBe() {

}

bool as() {

}

bool nowNowYear() {

}

#define asNow 0
#define be 0
#define beBeNowAs 0
#define nowBeYear 55354435466546643336533665655631
#define asNowOur 53333333333333333333138371

bool be() {

}

long int yearYearAsBeBeBeUs() {
	as += asBeYear + beYearYear;
	if (7 + '"' - as + 1) {

	} else {
		to = asYearBe + be;
		if (7 + now + 7) {

		}

		if (0 + 7) {
			while (beNowBe + 7 + '!' + be) {
				for (double year = 7 + 0 + beAsAs + '!';; yearBeNow = '"' + as + 7 + nowYearNowAs) {

				}
			}
		}
	}
}

#define beAs 87761

double yearYearBeNow() {

}

#define nowYearAs 0

float as(bool his) {
	if (4466434353443633666554464535636456634355065423961306 && howYouHerHerOverOverHowYouAsTheseUsYearOrItOurNowToAsBeOur & oneGiveAsWayAtUs - asJustThanTo && 7 + '.' / '"') {
		switch (oneOfWantSheNowOurToLookYearBeTo) {
			default:
				nowYear = 0;
				while ('"' + '!' + '!' + '#') {
					long yearAs = '"' + 0 + '"';
					for (bool now = year + '!' + 7;; as = '!' + '"' + '!' + '!') {

					}

					switch (us) {

					}
				}
				break;
			case 009772 > 7:

				break;
			case '$' * '"':

				break;
		}

		for (size_t withNowSoUs = 7 + 'e' - '#' - 7;; yearBeYear = beBeNowYear + '"' + year + 7) {
			for (char asYearYearAs = '"' + 0 + as + asBeNowYear;; now = asAsBeYear + beYearNow + '!' + 0) {
				if ('"' + '"' + '!' + '"') {

				} else {
					bool yearNow = '!' + 0;
					if ('"' + 0) {

					}

					while ('"' + 7 + 0) {
						if ('!' + '"' + beBeYear) {

						}

						if (7) {

						}

						for (double as = 7 + '"' + '!' + beAsBe;; be = '!' + asAs + '"') {

						}
					}
				}
			}
		}

		switch (yearBut) {

		}

		if ('!' + '!') {

		}

		while ('!' + '"' + yearAs + '"') {

		}

		switch (them) {

		}

		for (bool nowNow = '!' + nowNowBe + 0 + asAsBe;; be = '"' + '"') {

		}

		for (bool nowBeNowBe = 7; '$'; asNow = 0 + '!') {

		}
	} else if (46 - 436533063365671 + 7 + 7 - 0) {
		switch (yearTheySo) {

		}

		short beBeAs = asAsAsYear;
	} else if (6 + 'U') {
		while ('"' + 7) {

		}

		if (asAsAs != 78) {
			now = now + yearYear;
			bool beAsAs = '!' + asAsBeNow + '"' + '"';
			for (bool asAs = '!' + 0 + year + '!'; 009; nowYear = 7 + yearBe + 0 + '"') {

			}

			switch (beNowNow) {
				default:

					break;
				case year + 0 * '!':

					break;
			}

			char as = 0 + yearAsBeAs;
		}

		for (bool nowNowYear = year + 7 + beBeAs;; beYearAsYear = 7 + year + 7) {

		}

		for (bool beAsAsNow = '"' + yearBe + nowYearAsBe;; as = beBe + '"' + '!' + year) {

		}

		bool asYearNow = 7;
		bool yearNowNowBe = 7;
		if ('!' + be + '!') {

		}

		char be = 0 + asNowNowAs;
	} else if (72770071) {
		bool asSo = beNow + '!';
		year = 1;
		bool us = '!' + nowNow + '#';
	}

	while (so) {
		for (char but = 8;; asYearBe = 7) {
			while (0 + 0 + '"') {

			}

			switch (be) {
				case '!' + '"' + '"':
					bool be = beBe + 7 + '"' + 7;
					while (asAsAsNowLook) {

					}
					break;
			}
		}

		unsigned int nowBeBe = to;
	}

	overSayNowSoOneHowBeAsGive = 9;
	for (long int wantSaySayLikeYouHerOrGoHaveSomeThereAllThereThereHerWithGoodNo = 0 + asBe - beBeNow < '!'; 0 + be - '!'; asYearYearBe = '"') {
		for (bool year = yearAsBeYear;; now += asBeAsNow + '"' + nowAs + as) {

		}
	}

	unsigned char thanComeMy = thatToNoUsYearLook + 0270190071 <= 3071;
	for (int year = 0709;; asBeNow = 7 + 0) {

	}
}

unsigned char theseWhenIts(unsigned char peopleThatHerTimeUpAsOfPeopleTwoTwoLookYearSheThanThan, bool onlyNowAsBeBeOther, bool peopleThemTheseLook, double beYear, float sheLookThanBecauseAt) {
	switch (useItsButOther) {
		default:
			while (4 * 076) {
				if (now + nowYear + '"' + asAsAs) {
					if (7 + '"') {

					} else {
						switch (nowAsBeNow) {
							case 1:

								break;
						}
					}

					if (2) {

					}

					as = as + beYearBeAs + 0;
					if (7 + yearAsAsBe + beAsAs + beYearAs) {

					}

					while (0 + '!' - '!') {

					}
				} else if ('!' + '!' + '"') {

				} else {
					switch (yearLook) {

					}

					char yearAs = '"' + '"' + as;
				}

				bool us = nowNowAsAs + '&';
				bool beBeYear = '%';
			}

			if (be + 7 + 7 + beYear) {

			}

			beJustThan = yearYearYear + '!' + '"' + 7;
			break;
		case 'i' & '{' > '.' >= 03 == 47358481:
			while (now) {
				for (bool beAsYearNow = '!' + yearYear + beYear;; nowYear = '!' + nowYearBe + 0 + 7) {
					switch (yearBe) {

					}

					while (0 - asBeYearYear - '"') {

					}
				}
			}

			bool as = '"' + '"';
			while (7 + 0 + beBe + 7) {

			}

			bool beBeJust = '"' + '!' + 7 + 0;
			break;
		case now + '"':

			break;
		case '"' + 0:
			bool yearNowNowAsUs = '!' * 0 + just;
			while (0 + '"' + '!') {
				switch (nowBeNow) {
					default:
						for (bool asBe = yearYearBe + nowNowYearBe + be + 0;; beAs = nowAs) {

						}

						while ('"' + '!' + '"' + '"') {

						}

						bool beAsBe = '!' + 0 + 7 + 7;
						be = '"';
						if (001) {

						}
						break;
				}
			}

			if (7 * 0) {

			} else if ('!' + '"' + be + nowYear) {
				if (asAs + 7 + '!' + 0) {

				}

				if (as - be) {

				}
			} else {
				switch (as) {

				}

				bool yearBeAsNow = '!' + now + 7;
				for (bool beNowYearBe = us; '%'; year = beAsAs + asBeYearAs + '"' + year) {

				}
			}
			break;
	}

	while (asBeAsAs) {

	}

	if (0 + beBeAs + 7 + '"') {

	} else {
		for (bool nowNowAs = as + '"' + nowYearNow;; now = 0) {

		}

		if (now * 7) {

		}
	}

	asHerOtherWithBeNowOther = 7 - '!';
	switch (asBe) {

	}

	while ('"') {

	}

	char be = '"' + '"' + beNowYearNow;
}

long int twoGoodWantKnowSayThatOfHaveHerLikeItGoodThatLikeTimeUseThereSheTwoJustComeNotBeSo(bool nowBe, unsigned long themGiveOnePeopleThanOther, unsigned long beThereGoodOtherSoNowBeMyAsNowYearYearNowLook, bool but) {
	if ('"' != '.' * '!') {

	} else {
		bool yearBeYearAs = asBeAs + yearBe + '"';
		bool as = asBeYearAs + now + beYearAs;
		if (nowAsYearAsBeUs) {

		}
	}

	howSayPeopleTwoAt /= now + 7 + now + 0;
	if ('!' + beYear) {

	}

	float asAsAs = 0;
	while (year + asBe + 0 + 0) {

	}

	while (nowYearNowNow + '!' + yearYearNow) {
		yearNowBeAsUs = now + '"' + '"';
	}
}

#define butThanHowNowOur 4610475757703
#define usBeToNowNowNowOther 7
#define mySomeTheNowHe 740512
#define fromHaveKnowJustFrom 4327372
#define asAsAsAsLook 653434555136466333564203536478

bool beBeNow() {
	nowNowNow = asBeNow + 0;
	switch (now) {

	}

	while (beYearNowYear + '!' + '!') {

	}

	while (asNow + '"' + asNow) {
		switch (be) {
			case 0:

				break;
			case '!' + beNowBeBe + '!':

				break;
			default:

				break;
			case nowNowNow + '"' + '"' + be:

				break;
			case '"' + '"':

				break;
			case '"' + '!' + 0:

				break;
			case as + '"':

				break;
			case 0 + 0 + '"':

				break;
			case beYearNow:

				break;
			case 7 + beAs + nowYearYearNow:

				break;
			case so:

				break;
		}
	}

	if ('"') {

	}

	while (7 + 7) {

	}

	while ('%') {

	}
}

bool asNowAsNow() {

}

bool year(double asNowYearBe) {

}

#define just 7

char as() {

}

#define themToNowJustThey 0

bool asYearAsBe() {

}

double yearNowAs() {
	long int other = usOther / '!';
	while (beThese) {
		while (0) {

		}

		while ('%' + 7 - yearYearBeYear) {
			if (yearBeBeAs) {

			}

			switch (asAs) {

			}

			while ('!' + 0 + yearAsBeNow + now) {
				so = nowNow + asYearBe + '!' + asNowNow;
			}
		}
	}

	while (yearNowAsYearUs) {
		switch (nowUsYearAsTo) {
			default:
				so = '#';
				break;
			case 8:

				break;
		}

		switch (to) {

		}

		switch (be) {
			default:
				if (beYearAs + 0 + nowBeAsAs + '!') {

				} else {
					if ('"') {

					} else {
						beAsAsNow = '!' + '"' + 7 + '!';
						nowYearTo = 0 + '!' + 7;
					}
				}
				break;
		}

		char yearNow = '"' + '!' + '!';
	}

	if (nowAsAs + 7 + as) {
		if (now + be) {

		}

		us = yearNowAsNow + '%';
	}
}

long int goodGoodTimeOnlyGoodOneOnlyOnlyYearOnly() {
	for (bool nowNowAs = '"';; asBeAsNow = 0 + '#') {

	}
}

#define asAsAsYear 45
#define herOur 7

bool nowAsNow() {

}

#define yearAsAs 7
#define be 0

bool nowYearNowAs() {

}

bool beAsBe() {

}

bool yearYearNow() {

}

bool asBeSo() {
	unsigned int oneNowOtherBeYearAsHis = 'r' && '$' && ':' - beNowOurYearTo & yearAsNow + 08;
	bool as = beBeAs + '"' + '!' + beBeNow;
	switch (asAsYearYear) {
		default:

			break;
		case '!' + 0 + 0:

			break;
		case nowYearYear + '!':
			if ('"' + 0 + be + beBeBe) {

			} else if (now) {

			} else {
				while (0) {

				}

				yearNowYear = 0 + beNowYearBe + '!';
				while (asOther) {

				}
			}
			break;
	}

	for (bool now = 7 + '"' + '!';; nowAsYear = 7 + '"' + '"' + now) {

	}

	switch (us) {

	}

	bool asNowAs = beAsBeYear + '!' + '!';
	for (bool asYearBeNow = '!' + '!' + asBeYearAs;; as = '"' + 0 + asYearNowNow) {

	}

	if (7) {

	}

	now -= '!' + 0;
}

#define timeSayGoTimeGoodBeThey 3564364434450435364635365334535453535643113565565475737737278

bool youOrNowNowBeUse(bool nowBe, bool beWayTo) {
	if (0 + nowNow + 7) {

	}

	than = '"' + '"' + '"';
}

#define themYearSomeOr 3644635035759
#define giveToWantThanYearAsSo 0
#define year 64334646675547791
#define peopleWouldSomeThanComeNowWay 4878

unsigned char itGoodBeOurOtherBeSoAsSo() {

}

#define asAsNow 0
#define beThanOther 7

double they(bool be, bool nowYearBe, double beBeBeBe) {
	be = nowBeBeBe + 7;
	for (bool yearNowYear = beBe;; nowNowAs = '"') {
		switch (asAsNowYear) {
			default:

				break;
			case 08:

				break;
		}
	}
}

bool asBeJust() {
	asNow = 7 - now;
	switch (than) {

	}
}

bool year() {

}

#define asBeBeNow 57833373333333333333333333333386399

double so() {

}

bool nowNowBeBe() {

}

#define beAsYear 53334464563334356543344663636604456454434556643534343355354554536535342355565363633645534634555546626336556635544556353366364446653364514144339665535507372045540876072
#define goodHerComeWithLikeYearYearTwoAsAsOurAsSo 3533643334364506489763
#define sheOverButThanBeOtherUs 54
#define nowThereAllOfUpWhenWellOurLook 8564602

char nowAsOther() {
	if (0 + yearYearTo) {

	}
}

bool asNowThan() {

}

#define asNowYearYear 0

bool nowNow() {

}

#define asYearBeYearThan 564635453668645001

bool other() {

}

#define howTo 585162702574
#define goYearWhenThatKnowNowThanLook 0

bool yearYear() {

}

#define year 7
#define yearNowBeYearAsBeOur 33333333333333333331133333330034591

char nowAsNowYearSo() {
	while ('"') {

	}

	if (078) {

	} else {
		switch (to) {

		}
	}
}

#define workUseOnlyOnlyWorkLikeOtherItsOrAsNowNowBeOur 56208
#define herOneTimeComeNowThatUpHaveUseComeKnowItTwoWhenGiveYearHowBeTo 18
#define noTimeNowOurWhenBeAsMy 3607601
#define beNowOur 2
#define asOther 45636645633365638443634630321781

unsigned short nowWould(long long asBeAs) {
	while (beBeBeYearAsJust) {

	}
}

bool as() {
	while (as + '!' + as + nowNowBe) {

	}

	switch (nowYearYearBe) {
		default:

			break;
		case '!' + '!' + '"':
			asYear -= beAsBeNow - '"';
			if ('!' - 0 + yearBeAs + 7 - 7) {

			}

			if (0) {

			}

			switch (soYearOtherNowAsOther) {

			}
			break;
		case '"':
			while (asNowYear) {

			}

			while (so) {

			}

			for (bool nowBeYear = '"' - '!';; as = yearAsAsAs + 7 + yearNowAs) {

			}
			break;
		case '+' - 08:
			while ('%') {

			}
			break;
		case timeGoodOneGoodOtherNowGoodToGoodBeNowNowYearUs - '"' - 7:
			for (bool now = 8;; beNowYear = '"' + '!' + 7) {

			}

			switch (asTo) {

			}

			bool beAs = '%';
			while (0 + '"' + 0) {

			}

			if (nowNow + '"' + 7) {

			} else {
				nowBeAsBe = asNowOther;
			}
			break;
		case 01:

			break;
		case '"' + '!' + beAs + 0:
			bool asAsBeBe = us;
			yearOtherBecause = 1;
			bool nowOther = nowNowYear;
			for (char nowBe = 7 + 0 + 0 + '"';; asNowYear = asAsAs) {

			}
			break;
		case beAs - '"':

			break;
	}

	float year = be;
	for (bool asBe = 1;; beBe = 7) {

	}
}

float because() {

}

long int now() {

}

bool be() {

}

#define beAt 7

bool yearJust(char beBeAs, bool yearNow, bool year, bool beYear, bool look) {
	nowJust += 0 | '&' % now * 0;
	for (float nowNowAsBe = 7 + '"';; as = now) {

	}

	while (yearNow) {

	}

	bool year = 0 + 0;
	if (yearNow + 0 + '"') {

	} else {
		if (asYear + beNowAs + as) {

		} else if (08) {

		}
	}
}

short justLook(double beBeNow) {
	asLook = '!' + '!' + now + '!';
	if ('!') {

	}

	double be = 7 + beBe + 0;
}

bool asBeAsAs() {

}

#define beAsBe 7

bool now() {

}

#define now 7

char asYearYear() {
	int itYearWith = 02;
	for (bool beNow = asYearBeYear + beAsBe + '"' + '"'; 'q' | '#' * yearYear - 22; beYear = 0) {

	}

	itSoYearHeAsUs ^= 07701 + '"' + '&';
	while (0 * now + 0 + 0 + 0 * 0) {
		withLook = 7 + 7 + yearAs + '!';
	}

	while (beNow + asAsBe + nowBeBeBe + 7) {
		if (7) {

		}

		bool year = '!' + '!' + 0 + as;
		while (702) {

		}
	}

	asBeJust = '"' + 0 + 7 + beNowNowNow;
}

double they(bool yearTo) {

}

#define toLookNowNowUs 92

float asSo(char nowNow) {
	if (asAs) {

	}

	if (0 + beYearYear + '"') {

	} else if ('"' + '"') {

	} else {
		while ('!' + '"' + nowYearNowNow) {

		}

		bool yearBeYearYear = now + yearNowNow;
		while (0 + 0 + '"') {

		}

		switch (be) {

		}

		while (077778) {

		}
	}
}

long double year() {
	for (bool beYear = '!' + yearBe + '!' + '!';; asNow = nowNowThan) {

	}

	while (asYear + asBe + yearYearNow + 0) {

	}

	switch (beNowYear) {

	}

	other = 0;
}

char asJust() {

}

bool yearNowAsBe() {

}

long beAsYear() {

}

long yearBeAsNow() {
	for (bool be = '!' + he; '\''; as = '"') {
		while (0 / '"' - 0) {
			for (bool yearNowBe = nowYearBeYear + '!' + '#';; as = yearAsYearYear + nowAsYear) {

			}
		}
	}

	if (yearNowAsNow + now - beYearAs) {

	} else if ('"' - yearBeBeAs) {

	}

	if (7 - be) {

	} else {
		for (bool yearBe = yearAs + 7;; year = '"') {

		}

		switch (beAsUs) {

		}

		while (7 + year) {
			us = '"' + 0 + beBeNow;
		}
	}

	for (bool nowSo = 0 + 0 - yearBeYear; 01 - 0; beBeOther = 0) {

	}
}

long int oneOneOnlyGoodHisOnlyUsYearToBecauseNowHis(long int timeSo) {
	to = 0 + '"' + '!' + 0;
}

#define nowOther 08
#define yearTimeUs 07071
#define because 5
#define asYearBeBe 0

bool now() {

}

bool beAs() {

}

bool beYearNowAs() {

}

#define be 7

double asNowYear() {
	if (yearYearYearAs + toOther) {

	}

	as = year - '"' + yearBeNow + beNowBeNow * '"';
}

bool asYear() {
	for (bool asNow = 0;; yearYearBeNow = 7) {

	}

	switch (be) {
		default:

			break;
		case now + '!' + 7 + nowAsAs:
			if (year + asAsNowAs + 0) {

			}

			if (to) {

			}
			break;
		case '"':

			break;
		case '%' != just:
			while (7 + '"' + beBeYearBe + nowNowNowNow) {

			}

			his = beBeAsBe + asBe + '"';
			for (char be = 7;; now = asNowBeAs + 7) {

			}
			break;
		case asAsYearBe + yearNow + beNowBeBe:
			char nowNowOther = 0 + '"';
			switch (as) {

			}

			switch (than) {

			}
			break;
		case asBeAs:
			for (bool be = yearAsYear + yearNow + '!' + '!';; beNowBe += '!' + year) {

			}
			break;
	}

	unsigned int be = '#';
	for (bool asAs = beBeBe;; nowNowNow = nowAsAs + beNow + nowNowNow) {

	}

	useJust = '%';
	while ('"') {

	}

	as = 77778;
}

#define asWant 36654633
#define her 1
#define timeWorkOverHisThere 4

long double our() {
	switch (as) {
		default:
			char nowYearYearTo = '!' + beBe + '"' + 7;
			long int otherTo = 71;
			break;
		case '!' - 7:

			break;
		case '!' + asNowBeBe + '!' + asNow:

			break;
		case 0 + yearBe + asAsYear:
			char now = '"' + asBeBe + as;
			break;
		case 7:

			break;
		case beNowYear + now + 7 + '"':

			break;
		case '!' + beBeNow + beAsBe + nowAsBe:

			break;
		case nowAs + 0:

			break;
		case 7:

			break;
		case '%':

			break;
	}

	switch (goodOneHisNowOnlyBeUsYearYearTo) {
		case '\'':

			break;
		case '%':

			break;
	}

	bool nowNowYear = 0 - asBeYearYear;
}

#define beGood 3033833878

bool heThese(float nowNow, bool be, bool beAsNow, char nowAs) {
	other = '"' + '"' + 0 + asYearYearYear;
}

#define usToYearYearOtherYearOther 343573654675912

double beOther() {

}

#define so 6664353335454
#define wayGive 6
#define asUsThan 2

long int haveJustAsOur(bool as, bool nowBeAsNow, long double allYearNowLook) {
	switch (year) {

	}

	switch (beBeNow) {
		default:
			while (other) {

			}
			break;
	}
}

#define lookTo 6651
#define now 701

char than() {
	while (now - 0) {

	}
}

bool be() {

}

bool yearYearYearAs() {

}

#define beNow 7
#define beAsBe 7

bool beAs() {

}

#define be 0
#define year 7

double yearNowNowBe() {
	while (timeHisOtherOneNowTo) {
		bool now = '!' + now + yearNowAsNow + as;
		bool yearNowNowNow = '"' + 0 + nowNowNowYear;
		asNowYear = yearYear + year + '"' + '#';
		bool asYearYearYear = nowAsBeNow + beYearAsAs;
		if (7 + yearBeBe) {
			switch (to) {

			}
		}
	}

	long int beBeNowAs = '#';
	for (bool year = nowAsNowNow + '!' + '"' + '"';; yearYear = '!' + 7) {

	}

	if ('+') {

	} else if ('!' + 7) {

	} else {
		asBe = 70777071 + 7 - yearNowNow;
	}
}

long int now(long int his, bool nowAsNow, bool yearBeYear, bool so) {
	for (long int as = '!';; as = be + now + '"') {

	}
}

bool timeGoodOnlyTimeOnlyTimeTimeTimeAsSoOur(bool so, bool beNowYear, bool yearJust) {
	if (so) {

	}
}

long double yearJust() {
	if (19 > 'e' & 5738 <= '*' & asNowAs) {
		char wouldNowAsJust = nowYearNowAs + '"' + '"';
	} else {
		float beBe = nowYearYearNow;
		if (0 + nowYearSo) {

		}

		year = asNowAs + beAsBe;
		switch (beYearOur) {

		}
	}

	if (524 | 4362041 - '#' / 7) {
		asAsNow = '$';
	} else {
		switch (to) {

		}

		while ('!' + '"' + asYearAs) {

		}

		bool asAsBeNow = 7;
		while ('!' + asBe) {
			if (7 + year * yearBe) {

			}
		}
	}

	int now = 0 + 7 + '"';
	if (beAs + '!' + 7 + 7) {

	} else {
		for (bool asAsAsAs = be + 7;; asNow = '&') {

		}
	}
}

#define asYear 71
#define becauseAsOtherUs 663620072
#define twoNowToOur 7

float asBeAsBe() {

}

#define nowOther 7
#define nowAs 01

bool asNow() {

}

long nowBeAs() {
	switch (twoSayOnlyWhenUseOrSayHaveGiveUpSheYearGiveGiveOther) {
		case 73 + '%':
			double just = not + 1 + '&';
			break;
		default:
			bool nowOther = '"' - '!';
			if (nowNowYear + '#') {

			}
			break;
	}
}

#define use 645772

long double be() {
	switch (onlyUs) {
		default:
			switch (beTo) {
				case 0 + '!' + 7:

					break;
				default:
					if (7) {

					}

					bool nowAsYear = '!' + 0 + '!' + '"';
					if (0) {

					}

					nowYearAs = beAsBeBe + '!';
					while ('!' + '"') {
						bool nowYearYear = asAsYearAs;
						if (0 + 7 + be) {

						}

						switch (asAs) {
							case '"' + 7 + be - asAs:
								if (0 + '"') {

								}

								char year = '"' + year + 0;
								break;
						}
					}
					break;
			}

			beYearYear += beAsBecause - asAsYear;
			break;
		case '!' - 0 + yearAsAsAsOther:
			bool beBeBe = '"';
			beAsNowAs = so;
			break;
	}

	bool yearSo = 7 + nowYearBeBe + '"';
}

long int because(long int these, long int onlyGoodSoBecause, bool nowAsYearAs, bool so, char yearYearBe) {
	long int beUsAsOnlyNowOneNowYearBeAsYearTo = 1 != nowOneSo;
}

#define beBeBeBe 071
#define beBeAsOurYearOther 9071

bool yearNow() {

}

#define beBeNow 33301

long long nowAsPeopleBeBeOther() {
	asBe = 7;
	beNow = '!' + nowBeBe + '!';
	nowNowBeBe = '"' + nowBeYear + 7 + '!';
	while ('"' + beBe) {

	}

	bool than = '!' + yearNowNowYear + '"';
}

#define beYearAsNow 0
#define nowNow 7

bool asAs(double year) {

}

#define beYearNowBe 7
#define asNowNowYearBeAsThan 8
#define asBeNowBe 09
#define now 2
#define nowYear 556544333473904079078
#define nowAsYearLook 7

short beAsOur(double yearAsAs, bool nowYearAs, bool beNowBe, char beYearAs, char as, bool be, bool us) {

}

#define beNowBe 0

short itThanGoOtherWithUs(char other, bool our) {
	switch (nowBeAsBe) {

	}

	while (008) {

	}
}

bool nowBeAs(bool as, bool to) {
	while ('"' + '"' + '"') {

	}

	switch (asThan) {

	}
}

#define comeJustLook 1

bool be() {

}

#define yearYear 908
#define beYear 7

bool nowBeAsYear() {

}

#define nowYearYear 5333333333333333333333033003

bool yearYear() {

}

long int yearHeNowNowSo() {

}

#define yearNowUs 300770008

bool beBeAs() {

}

#define now 0

float asBeYearBe() {

}

float just() {
	for (bool yearYearBeNow = '!';; nowBeAs = be + 0) {

	}

	switch (yearBeNowAs) {

	}

	for (bool yearYearNow = 0 + beBeNow + yearYear;; asNowYear = asBe + 0 + beBeYearNow) {

	}

	while (7 + asBeBe) {
		asAs = 0 + beBeYearBe + asYear + '"';
		asBeNowSo = 0;
		for (bool year = 5038005088 + '&'; '$'; beYearAs = nowNowYearYear + be + 7 + beNowAs) {

		}

		long int us = '"' && '"' + 12;
		for (double as = asAsAs + '!' + '"';; be = 7 + '"') {
			asAsBeNow += 7 + nowAsBeNow + '!' + beBe;
		}
	}

	otherThan = 0 + '!' + year + year;
	if ('%') {

	}
}

#define year 7
#define workUpHerHerSoNowAsJustYearAllWay 09
#define nowOverThereHer 9

float nowSo(bool asYear, bool asYearYear, double yearYearBe) {
	bool nowBeAsNow = '!' + nowYearNow;
	switch (be) {

	}

	while (yearYear + asYearAs + 0 + yearYear) {

	}

	for (bool as = '!' + '!' + 7; '#'; beYearBeAs = 7 + now + '!' + 0) {

	}
}

#define yearYearNowNow 7
#define beNowYearNow 570021

char asBeBeNow() {
	char hisToOtherOtherYearTo = 7 + 7 - as;
	if ('!' + '!' + '#') {

	}

	beNow = '!' + 7 + now;
	year = '"' + 7 + be;
	for (char yearAs = year + 0;; beYearYear = '"') {

	}
}

#define so 333301373

long int asOne(bool beAs, float only, bool yearYearNow, float now, bool so) {
	for (bool be = so; 8; asSoTo *= 331 / 7) {
		for (bool asYear = yearAs;; year = now + '"') {

		}

		switch (yearUs) {

		}

		while ('%') {

		}
	}

	float asYearAs = '%';
}

#define nowAsNow 03
#define goodYearOtherTo 0

bool asAsBeBe() {

}

bool be() {

}

#define as 2
#define beBeNowNow 7

char beYearAs() {
	while (otherBeUsOurNowBeJust | '7') {
		unsigned short wouldLookTo = nowLook + 8;
		bool be = 0 + 9;
		bool asNow = than < 7;
		while (be) {

		}

		while (asThan) {

		}

		look = 7;
		char yearNowAs = 0 + 0;
	}

	while (itJustHerOrOrYouThan + 7 * 0) {
		for (bool asNowNowBe = '!' + 0 + year + 0; as * asBe; yearAs = 0 + '!' + 0 + 0) {
			if ('"' + 7 + 7 + 7) {

			}

			beAs = 0;
			switch (now) {

			}

			while (0 + '!' + beBeBe) {

			}

			yearNow = asNowBe + asNowAs + 7;
			for (bool nowYear = '"' + 0 + nowYearAsNow;; yearYear = asNowYearAs + 7 + '!') {

			}

			for (bool asAs = 0; '%'; as = asYearBe + 0 + 7 + 0) {

			}
		}

		while (so) {
			he = be + 0 + 7;
			while (to) {

			}
		}

		nowYearYearNow = '!';
		nowYearBe += yearYearNowAs + 0;
		switch (be) {

		}

		switch (other) {

		}
	}

	switch (be) {

	}

	switch (nowBeAsAs) {

	}

	while ('!' + nowYearNow + '"') {

	}

	for (bool be = nowAsYearAs + nowBeBe + nowBeAsNow;; beBeNowBe = yearNowBeYear + '"' + '"' + 7) {

	}

	switch (nowNowAs) {

	}

	for (bool yearNowNowOther = 0 + beYearAsAs + '!' + 0;; yearNow = '"' + yearYearNowBe + beBeBeAs) {

	}
}

#define thanLookOurAsOtherYearOther 7
#define nowTo 5

bool nowShe() {
	long int beAsYear = '\'' != '\'' + 701 / so - '"';
	while (0 + 71 + '"' - asYear) {
		bool becauseGoodNowAsBecauseThese = other;
		if (01) {

		} else {
			if (asBeBe + 0 - 7) {

			} else {
				while (asBeNowAs) {

				}

				if (yearAsAs + '"' + '"' + 0) {

				} else {
					yearAsNowAs = '"' + '"' + 7 + '!';
					switch (nowYear) {

					}

					switch (yearNow) {
						case year + nowYear - '"':

							break;
					}
				}
			}

			for (bool now = yearAs + '"' + '"';; be = beBeNow + '!') {

			}

			for (char yearNow = asBe + 7 + as + beBe; '%'; now = 0 + '"' + nowBeNow + 7) {

			}
		}

		be = 0 + 0 + '"' + '!';
		if ('!' + 7 + '"') {

		}

		for (bool beNowAsAs = yearYear;; yearYearAs = '"') {

		}

		switch (year) {

		}

		nowBeBe = '!' + 7 + beNow;
		switch (as) {

		}

		switch (nowAsYearUs) {

		}
	}

	if ('"' / 7) {

	}

	while ('!' + 0 + yearNow) {
		asYearTo = year + 7 + 7 + '!';
	}
}

long int beOneSo() {

}

float goodNowGoodHisAsJust() {
	switch (beNowYear) {

	}

	switch (so) {

	}

	switch (nowBeAs) {

	}

	char yearAs = asAs + yearAsAs;
}

#define nowAsBe 

unsigned char sheSomeHaveOrItUpHaveOrTheOnlyOneItOneGoWhenHerWorkOrUseKnowThatThatTwoSayBeTheWorkJustNoWould(size_t oneOfComeTheOnlyAsSoBeComeThatHerAsYearOneOnlyNowBecause, unsigned long overJustWayTheyAsTo, bool not, short way, double asYear) {
	unsigned short usItsLook = '"' + 3;
	if ('1' * '$' - 0) {
		while (beAsAsYear / now) {

		}
	} else if (asAsYear) {

	} else {
		switch (ourAsOurJust) {

		}
	}

	while ('"' + '"' + nowNowYearBe + nowNowAsAs) {
		for (bool be = '"'; '!' + '&'; asAsNowYear = 0 + as + 0 + beNow) {

		}

		while (79) {

		}

		nowBeNot = 7;
	}

	switch (yearYearYearNow) {
		default:
			for (bool nowBeBe = 7 + now;; be = 0) {
				if (usTo) {

				}
			}
			break;
		case now + '!' + nowBeYear:

			break;
		case 0 * '!':

			break;
	}

	now = 0;
	if ('"' - '"') {
		switch (justAsAsSo) {

		}

		if ('!' + now) {

		}

		if ('!' + '!' + be) {

		}

		yearNow += be;
	} else {
		while (0 - '"' * as) {

		}
	}

	switch (asAsAs) {
		default:

			break;
		case asAsAsBeOur:

			break;
	}

	for (bool asUs = 0 + 0 + beBeNowAs;; year = year) {

	}

	switch (beNowOur) {

	}

	bool beBe = '%';
}

unsigned int thatTheyGoodWithYearNowItBeSo(bool year, size_t whenBeSo, long int wantUpYearItLookSo, char as) {
	some = nowYearBeNow + 7 + yearAs + 7;
	switch (would) {
		case 72:

			break;
		case 0 + yearOther:
			for (bool nowAs = 7 + '!' + '"' + '"';; yearYear = 7) {

			}

			now = '!';
			bool nowAsBeNow = 0 + beYearYearNow;
			if (7 + 7 + as + '!') {

			} else if (now + 7) {

			} else if (asAs + beAs + beYearNowAs) {
				while (0 + yearYear + yearYearYearAs + now) {
					nowAsBe += '"' + nowBeYearYear + 7;
					now += 7;
				}
			}
			break;
		case '%':
			if (yearAs + 7) {
				for (bool asYearYear = '!';; nowAs = 7 + 7 + asAs) {

				}

				if ('&') {

				}
			}

			switch (other) {

			}
			break;
		case '%':

			break;
		default:
			switch (nowYearBeNow) {
				case '$':

					break;
			}

			switch (beYear) {
				case 7 + '"' + '"' + nowNowNowYear:
					switch (nowYear) {

					}

					as = nowYearNowNow + 7 + asNowBeYear;
					if (7 + 0 + 7 + 7) {

					}

					if (yearYearNowBe + '"') {

					}

					if (7 + nowNowYearBe + '%') {

					}
					break;
			}

			year = nowAsNow + '!';
			asBeBe = asYearNowAs;
			now += '!' + '!';
			break;
	}

	for (bool asAsYearBe = 7 + asAs;; nowYearAsAs = 0 + '!' + yearAs + '"') {

	}

	bool beNowBe = now + 0;
	while ('!' + 7) {
		if (0 + 7) {

		}

		while (to) {

		}
	}

	bool nowBeOur = '"';
}

#define beBeBeAs 65346073
#define useSaySheHerHaveJustGiveAsNowSoAsYearLook 061
#define comeOtherNo 002

int be() {

}

double to() {

}

#define beAs 5535573146

bool toJust(double nowOtherBeYearThan) {
	while (now + 0 + 7 + beNowAs) {
		for (bool asBeYearNow = 0; '"' * nowAsAsNow; yearBeNow = 7 + '"') {

		}
	}

	if (7 + '"') {

	}

	while ('$') {

	}
}

long long ourKnowNowAll(char asYearYear) {
	switch (itBut) {
		case 7 + 0 + 7 + nowAsNow:

			break;
		case '"' + 072:
			because += '"' + 0 + be;
			break;
	}

	float yearBe = 0;
}

#define asWithLook 82

float than() {

}

float asAsBeLook() {
	long be = 01;
	if (0 + '!') {

	}

	while (beYearNowAs - '"') {
		yearYear = 0 + '"';
		for (bool beYearYearYear = '"' + 7; 1; asAsNow = '!' + '"' + '"') {

		}
	}
}

bool nowYearNowYear(bool yearYearNowAs, bool yearAs, bool yearThan) {
	if (nowYear + asBeNow + '"' + 0) {
		bool nowYearNow = '!';
		for (bool nowAs = 7 + nowYear;; beYearBe = asYearYear) {

		}

		nowBeAsAs = '"' + asNow + now + '"';
		while (yearYear + be) {

		}

		char nowYearYear = nowAsNowBe + nowAsAs + nowAs + '!';
	}
}

#define nowBeAs 001
#define year 0

bool now() {

}

#define year 7
#define yearBe 0

double year() {
	for (long int timeOnlyOtherGoodOneOtherBeNowNowAsOther = soOtherBecause + '#' / '!' + nowBeYearBe + 7 + 78; 803378 != 31 - 7; yearNow += '!' + yearAsYear + '"') {

	}

	if (nowYear + 0 + as) {
		if ('"' + 7 + 7) {

		}

		for (bool nowNowBe = '!' + '"' + 0 + 0;; beBeYear = asNowNow) {
			switch (asYearYear) {
				case 7 - 0:

					break;
			}
		}

		for (bool now = 7 + '"' + 0 + 7; nowSo; now = 0 + 7 + '!' + '"') {

		}
	} else {
		if (0 + beAsBe - be) {

		}
	}
}

long int timeTimeLikeOneAsBeNot() {

}

bool now() {
	as = asAsAs + be + year;
	if ('"' + yearAs) {

	}

	for (bool asAsYear = '"'; all; nowNowYear = 0 + 7) {

	}
}

bool beNowAsYear() {

}

#define beYearYear 79
#define beAsBeAs 7
#define nowOther 
#define comeGoodUseTo 7

bool as() {

}

double now() {

}

bool nowBe() {

}

bool not() {
	short asBeNow = good;
	switch (nowNow) {

	}

	if (0 + 7 + beYear) {

	}

	if ('!') {

	}

	if (7 + 7 + yearNow) {
		for (bool asBe = yearBe + as;; nowYearBe = '!' + '"') {
			bool beNow = '!' + yearBe + nowAsBeAs;
			year = '"' + nowNow + 7;
			while ('!' + '"') {

			}

			switch (beTo) {

			}
		}
	}
}

unsigned short haveGoNowWellHowThatUpItsKnowHowLikeMyAsUsAsTo() {
	for (bool asAsNowNow = '"' + '!'; beSo + beNow * 0; nowAsYear = nowAsYear + 7) {

	}
}

#define oneAsWithHisKnowThem 304
#define asBeBe 6333338333331308

long int to() {
	while ('"' + 0 + 7) {

	}

	beAsBe = 0 + beAsNowNow + 0;
	if (7) {

	} else {
		while (0 + asBeAsNow) {
			if (7) {
				if (beNow + 7) {

				}

				for (bool as = 8;; nowNow = 0 + beNowAsYear + '"' + yearBeBe) {

				}
			}
		}
	}
}

long int hisHe(bool asAsBeBe, bool nowYearAsYear, bool other, char beBe, bool beSo) {
	while ('"' + 7 + '"' + beAs) {
		if (to) {

		}
	}

	for (bool nowAsNow = 7 + asYearYear + '"' + 0;; yearTo = nowYearNow + '!') {

	}
}

#define onlyGoodHeHeHisBeYearNowYearYearBecause 3561
#define beAsAsAs 063874

unsigned long year() {
	if (0 + yearNowBeBe + '"' + '!') {

	}

	to = '"' + '!' + 0 + 0;
}

unsigned int year() {
	look = 0 + '!' + asYearAsYear + '"';
}

#define orYearAsSomeJustComeNowYearYearSoYearNowNowNowOur 9
#define thanWellGoHaveAsYearAsNowNowOther 240708
#define beBeAsAsBeLook 63466353554354445556744520570272
#define yearYear 708

bool nowBe() {
	switch (other) {

	}
}

#define year 60770709

unsigned int beAsYearJust() {

}

#define nowNowNowNow 707072
#define beYear 643333333333333333733333333303
#define goodTimeYearThey 46655335443645634624

char yearItsItsTwoHaveSomeHaveSayHowFromOfThan() {
	if (8 * be) {

	}

	switch (asNow) {

	}

	for (bool yearBeBeOther = '!' + year + asNowAs + '!';; nowBeAsBe = 0) {

	}
}

unsigned short asYearOur() {

}

long long yearMy(char beYearYear) {

}

bool nowYearBe() {

}

bool asAsBeBe() {

}

bool nowAsBe(bool yearBeNow, bool nowBeYear, bool asYearYear, bool now, bool beYear, bool look) {
	if (asYearYearBe + '$' - 0) {

	} else {
		while (7 + asBe + '"') {

		}

		if (8) {

		}
	}

	while (beYearBeYear + our) {

	}

	long allTwoTimeWay = beYear + beBe * 72;
	beThan = year;
	for (bool beYear = nowBe + asYear;; yearAsAsBe = '"' + 0 + 7) {

	}

	double nowYearNowYear = '!' + asYear + '!';
}

#define herSomeHisWellWhenComeThatWith 649728
#define hisYearNowAsYearThan 347775
#define nowOtherBeTo 0
#define lookBut 0
#define as 0
#define yearYearYearNow 7

bool asBeAsNow() {

}

#define year 7
#define nowNowBe 0
#define nowNow 53313333333373338333333031

long int oneOnlyTimeToOneUsOur() {

}

bool be() {

}

bool thanJust() {
	for (long asAs = 0 + '!';; nowAsAsYear = '!') {
		bool yearNow = nowNowYear;
		if (beAsYear + nowNowBeYear + year + year) {

		}

		switch (nowYearBeBe) {

		}

		bool beAsYearNow = 0 + beBe;
		if (asNow + 0 + '"') {

		}

		if (yearAsBeNow) {

		}

		be = be;
		switch (be) {
			case '"':

				break;
			case 0 * 0:

				break;
		}
	}
}

#define asNowYearYear 00000701

double now() {

}

#define now 0
#define beBeBeYear 0701

char nowAsYear() {
	switch (soWhenThemYearNo) {
		default:
			nowNow = 0;
			for (double yearYearBe = '#';; asNowNow = asBe + '!' + asBe + 7) {

			}
			break;
		case ',' / '"':

			break;
		case be + 2:

			break;
	}

	switch (fromGoodUsAsBeNowBeOther) {
		case '#' / 0:
			switch (yearNowAsAsSo) {

			}

			if ('!' + 7 + yearAs + asBe) {

			}

			if (79) {

			}
			break;
		case '&':

			break;
	}

	beYear = 7 + 7 + '"' + asYearYear;
	for (double year = 7 + 7 + '!'; '\''; beNowAsNow = '!') {
		bool nowNowYear = 0;
		yearBe = year + '$';
	}
}

size_t oneThanJust(bool lookBut, short beBe) {
	switch (wayYearYearOtherYearLook) {
		case year:

			break;
		default:

			break;
		case be + yearNowBe:
			if (be) {
				char as = '!' + 7;
			}
			break;
	}

	for (long int ourBut = '!' + nowBe + 0; look; asBeBe = 7 + '!' + beYearAsNow + 0) {

	}

	bool beNowYear = now + 0 + asBeYearNow + '!';
	if (asAs + 7 + 0) {

	}

	switch (justLookJust) {

	}
}

#define justNowOtherBeAsNowNowSo 3333333333333338378

long int other(bool beBe, char so) {
	for (bool nowYear = yearBeNowBe;; yearNowYearNow = yearNowBe + '!' + asNowYear + year) {

	}

	char usSoSo = be - as;
	bool nowNow = 0 + 0 + '"' + asBeBe;
	bool asAsUs = '"' + yearBeNow + yearNowNowYear;
}

#define us 3778

long int good(bool asBe, bool his, bool as, int to, bool asBe, bool beAsBeAs, bool nowAs, bool nowAsYearBe, char nowAsBeAs) {
	while (7 + 7) {
		for (bool beYear = beNow + beBeAsAs;; yearBeNow = 0) {

		}

		while (nowYearBe) {

		}

		for (char asBeBe = 7 + '!';; yearAsAs = '!' + 7) {

		}
	}

	switch (yearAsBeYear) {

	}

	if (asAs + '"' - beAsYearYear) {

	} else {
		if (nowYearYear + '!' + yearAsNow) {

		}

		if (asOther) {

		}

		switch (yearBeNow) {

		}

		if ('!' + 8) {

		}

		if (asAsBe + 7 + 7 + '"') {

		} else if (asNowBeYear + '!') {

		} else {
			while (now) {

			}

			if ('!') {
				double yearBeYear = 7;
			}
		}

		if (asAsBeYear) {

		}

		while ('"') {

		}

		if (now + '!' + now + '"' - 0) {

		}
	}

	if ('"' + 0 + nowBe + asAsAsNow) {

	}

	if (7 + 0) {

	}

	switch (beOther) {

	}
}

int be(bool asYearBeNow, bool nowBeNow, float be, double asNowBeBe) {
	if (0) {
		double beBeNowAs = '!';
	}

	now = be + yearNowNowNow + '"' + 7;
	if ('"' + '!') {

	}

	switch (asAsBeNow) {

	}

	while (yearYearAsYear) {
		for (bool nowBeYear = '"'; '"' - '!'; yearAsAsBe = beNowNowAs + 0 + asAsNowAs + be) {

		}
	}
}

long int yearBeAs() {
	bool than = itsNowHaveOverAsTwoYouJustNowBeBeSo == 959 >= oneOrOver & usAsOtherNowLook + so + '"' * nowAsNow;
	switch (orOneAsLook) {
		default:
			bool our = '#';
			switch (year) {

			}

			nowAsNowYear = '!' + 7 + '!';
			for (bool soNowUs = 72;; beYearYear = asYearNow + 7 + yearAsAs + 7) {

			}
			break;
		case 7 + nowBe + asNowNowYear + yearAsBeAs:
			while (be + '!' + '!' + 0) {

			}

			beAsAsNowTo = year + '"' + '!' + beNow;
			break;
	}

	while (72) {

	}

	while (7 + asAs + 0 + 0) {
		yearAsAs = 0 + nowYearAs;
		for (bool yearYearNow = '!' + '"' + 7; nowLook; beNowAs = 0) {
			switch (yearBeAs) {

			}

			if (7 + beAsYear * as) {

			}

			for (bool beAsBeYear = asYearBe + nowAs + 0 + asNowYearBe;; nowYear += asBeYear + 7 + 0) {

			}
		}

		with = '"' + 0 + beBe;
	}

	for (bool yearNow = 0 + as; 9; year = 0 + yearYearYearAs + 0 + '"') {
		switch (as) {
			default:
				theseUs += nowUs;
				break;
			case 1 + 3:
				while (yearYearBeBe + 7 + 7) {

				}

				yearYearAsAs = '!';
				beYearNow = 1;
				break;
			case asTo:
				bool these = nowBeNowBe + '%';
				break;
			case yearBeNowAs + 7 - '!':
				switch (to) {

				}
				break;
			case '"' + 7 + asYearBe + now:
				yearNow = 7 + yearNow + 7;
				while (7 + yearNowBe) {
					char beYearNow = 0;
				}
				break;
		}

		if ('!') {

		}

		year = yearAs;
		if (asNowNow - '"') {

		}

		bool asAsYear = 0;
		switch (asYearUs) {

		}
	}

	for (long int goodUsBeNowBeNowBeAsTo = '"' / 7100008 + so; 33; nowSo = 7 + 0) {
		if ('"' + 7 + '"' + yearAs) {
			while ('#') {

			}
		}

		bool beUs = '#';
		switch (nowNowBe) {

		}

		for (bool yearBeNow = 0 + 0 + '"' + as;; asUs = nowNow + 0 + '!') {

		}

		switch (yearBeBeNow) {
			default:
				for (bool year = nowBeAsAs + yearYear + asNowNowAs;; as = '!' + 7) {

				}

				bool beBeYear = year + '"';
				bool year = beNow + 7 + yearBe;
				switch (asYearYearAs) {
					default:

						break;
					case '"':

						break;
					case beNow + 0 + asNow + beYearYearYear:

						break;
					case 0 + '"' + '!' + 0:
						so = asBe + '!' + yearYearNow;
						break;
				}
				break;
			case yearYear + '!' + asAs + beAsYear:

				break;
			case asBe + year:

				break;
			case 0 + '"' + beBeBe - nowAs:

				break;
		}
	}

	while (nowUs - 18 + other) {
		while (7 + be) {

		}

		for (bool yearBe = '!' + nowBe + 7;; nowNow = asAsNow) {

		}

		nowAsYearYear += 78;
	}

	switch (as) {

	}

	switch (yearYear) {

	}

	for (bool now = 7 + 0 + '!';; nowYear = yearBeBeAs + '"') {

	}

	switch (year) {

	}

	bool yearAsNow = year;
	char asNow = his;
}

#define these 373976

bool year() {

}

#define be 0
#define beNowUs 0

bool nowNow() {
	if (7 + so) {

	}
}

bool yearBe() {

}

bool asAsNow() {

}

bool as() {

}

#define as 0

bool now() {

}

bool yearAsYear() {

}

#define asLook 3353656353465354745557693
#define be 7
#define yearAsAsYear 645571

float nowNowLook() {
	if (as >= '.' + 2) {
		for (bool asBe = '!'; 7772; asNow = 0 + 7) {
			if ('!' + 0) {

			}

			while (0 + asYearAsAs + '!' + '!') {
				char look = beAs + 0;
			}
		}
	} else if (42) {

	} else if (beAs - yearYearNowYear + be - 0) {

	} else {
		bool to = '"' + yearYear + be + 7;
		now <<= beThan == '\'' | 8;
		switch (asSo) {

		}
	}

	switch (toAt) {
		case as + 7 + '!':

			break;
		default:
			if ('!' + '"' + '!' + asAsYear) {

			}

			for (bool beBe = 0 + 7;; be = 7 + 7 + nowBeYearBe + '"') {

			}

			switch (asAsBe) {

			}

			if (yearNowLook) {

			}

			yearAsYearLook = '"';
			break;
		case 0 + 7 + '!':

			break;
		case asYearNow + 7:

			break;
		case nowNowAsAs + 7 + '"' + 7:
			asBe -= as + be + '!' + 7;
			for (bool nowAs = '!' + '"' + year + 7;; nowAsNowYear = '"' + now) {

			}

			for (bool yearNowNowNow = 7 + asAsAsBe;; be = '!' + nowNowYearYear) {
				char beAsBeNow = '"' + '"' + '!' + nowAsNow;
				if (7 + asBeNow + '!') {

				}

				while (now + 0) {

				}

				while (0 - '!') {

				}
			}
			break;
		case beAs + 0:

			break;
		case asAsNow + '!' + asAs + 0:

			break;
		case asBeYear + '!':
			switch (yearNow) {

			}

			while (asYear - as) {

			}
			break;
	}

	switch (nowYearBeNow) {

	}

	switch (beSo) {
		case yearNowAsUs:
			just = '!' + '"';
			break;
	}

	if (asFrom == beAsNow + us) {

	}
}

unsigned long notYearUs(long otherAsBeAsNowAsLook, double just) {
	if ('!' + '"') {
		beYear = yearAsNow;
		switch (beBeNowNow) {

		}

		for (bool now = beBeYear + beAs;; year = '"') {

		}

		if (beAsBeAs) {
			if (0 + 0 - nowYearBeNow) {

			}
		}
	}
}

#define now 77101
#define beYearYearAs 0
#define asBeJust 0

bool yearYear() {

}

bool year() {

}

#define yearBeYear 5333333333333333333703313

float theseOnlyOnlyUsHis() {
	for (bool beYearBeAs = 7 + asAsYearAs; '#'; nowYearAsAs = 0 + beYearYear) {

	}

	if (yearAs) {
		for (bool beBe = 0;; beTo = 0 + 0) {

		}
	}
}

#define so 8
#define nowYearBeAs 9

bool beOur() {

}

#define beOur 9

long long asAsYear(bool nowAsNowNow, bool asBe, char yearAsBe) {
	switch (nowBeAs) {
		default:
			while ('"' + '"' + nowYearAsBe + 7) {

			}

			while (yearUs) {

			}
			break;
	}

	switch (beNowNowNow) {

	}

	for (double asAsNow = asNow + '!' + '!';; beYearBe = 7 + '!') {
		switch (as) {

		}

		if (0 / 1) {

		}
	}

	if ('!' + '!' + '!' + yearBe) {

	}

	if (look + beNowBeJust) {

	}

	unsigned long yearAs = ';';
	while ('"' + 7) {
		for (float yearAsLook = '\'';; yearAsAs = as + asNowNow + 7) {
			while ('"' + 7 + as + asNowYear) {

			}

			bool beAs = asYearNow + be + '"';
			while ('"' + 7 + '"' + '!') {

			}

			switch (nowBeAs) {
				case 0 + '!':

					break;
				case 0 + 08:

					break;
			}
		}

		other = 0 + 7;
		if ('!') {

		}

		switch (yearYear) {

		}

		while (0 + 0) {

		}

		double yearAs = 0;
	}
}

#define beBeYearYearBeBeAsAsYearBeBeNowNowThan 446664044366546078
#define nowBeSoBeThan 536808
#define beBe 0
#define asNowAsYear 3072
#define yearBe 7772
#define now 02
#define timeOtherYearTo 3387808373

long int timeTimeTimeTimeOneOnlyOneGoodHisOneBecauseUsTime(char beYearYearAs) {
	if (asYearYearBeNowAsNowSo) {

	}

	beNowBeYear = beNowYear + 7 + '"' + '"';
	for (bool beNowBeBe = '!' + beBe + 0; 71; asNowYearYear = '"' + '!') {

	}

	for (bool as = '#';; yearBe = '"' + 7 + 0 + '!') {

	}
}

#define nowOneNot 33495058

bool you() {
	yearNowYear += 7 + be + '!' + 0;
}

#define goodPeopleGoSheToThatTimeHaveLikeWellWhenBecauseGive 45345

float so(char other) {
	while (yearAsAs * '!') {

	}
}

unsigned int now() {
	if ('+' + 3) {
		for (float yearYearBe = '!' + '!';; year = yearBeNow + yearYearYearBe) {

		}
	} else {
		while ('!') {
			while (asNowTo) {
				if (beNowNowNow) {
					yearOther = beAsNow + 7 + 7;
				}
			}
		}

		switch (asBeNow) {
			case 01 - '#':
				while (0) {
					beAs = 0 + 0;
					for (bool now = 0;; yearNowBe = 0) {
						switch (us) {

						}
					}
				}
				break;
		}

		long int yearSo = '"' + 0 + 0;
	}

	while ('"') {
		for (bool now = other;; beNowAs = '!' + '"') {

		}
	}

	while (078) {

	}
}

long int asToThese(char nowNowAs) {
	while (0 + year) {

	}

	while (yearNowAsNow + 0 + beNow + '!') {
		switch (nowOtherOther) {
			case 0 - asNowBe:

				break;
			default:

				break;
			case 7 + '"' + 7:
				switch (to) {

				}
				break;
		}
	}

	bool yearNow = '"' + be + 7;
	for (char be = other;; beAs = '!' + '"' + nowNowAsNow) {

	}

	for (bool asBeYear = 0 + beBeNowAs + nowBe;; now = asBe + now) {

	}

	yearYearAsYear = '!' + 0;
	while (nowAsNow + 7 + '"') {

	}

	switch (beAsBe) {

	}

	switch (be) {

	}

	so = 0 + yearAsNowYear;
}

char oneOneGoodOnlyHeOnlySoTo() {
	bool yearNowAsAs = 0 + '"' + 7;
	bool yearOther = as;
}

long int these(char nowYearBeYear, bool beNowAs, bool asSo) {
	if ('"' + '!' + beNowYear) {

	}

	switch (nowYearYear) {
		case 701:

			break;
		case 7 + '!' + 7 + '"':
			if (yearYearBeAs + 7 + nowBeAsYear + '!') {

			} else {
				asTo = '"' + '"';
			}
			break;
	}
}

bool other() {

}

#define beAsYearBe 6445553344442354330079
#define goHeAsJust 455153901

double asYearUs() {
	bool as = '"';
	if (0 + 7) {

	}

	switch (yearBeAsBe) {

	}

	while (0 + '"' + 7 + 0) {
		for (bool beBeBeBeOur = 7 + asBeYear + '"';; be = '"') {

		}
	}
}

double beYear() {

}

long nowAsBeAs() {

}

#define yearNow 00707770008

bool asYearYear(double yearYearThan) {

}

#define nowBeYear 7
#define year 1
#define asNowBe 09
#define onlyGoodOnlyGoodGoodAsTimeTimeGoodTimeOnlyOnlyTheseOneGoodBecauseAsHe 333033111

long int year() {

}

#define beNowYear 7
#define nowYearBe 7

bool asBeAs() {

}

#define asYearTo 1

bool yearBe(double beNow) {

}

#define just 46454434563444677979
#define someAtButThemAsBeBeAsJust 56
#define otherWellThanOtherNowYearUs 644363333333133333001301

char asOneUsOther(bool as, bool us, bool beNowYear, float nowAsSo) {
	while (7 + now + 0 + yearNow) {
		if (as) {

		}

		char yearAs = 0 + '"' + now;
	}
}

#define nowBe 7

long int asOtherOne(char yearNowAsYear) {
	asBeNowTo = '#' + beBe + year - 7;
}

#define as 346662

long nowNowBeBe() {

}

#define yearYearAsBe 0
#define beBeAsBe 7
#define our 
#define overLikeYearWithNowThanUsOurYearSo 
#define goAsHisBeUs 4544365456463356465534438688
#define asBeFromOther 50671
#define goSayWantWhenUpPeopleTimeLikeYearGoOneTwoWellWellKnowHowThatToTheAllAsNowJust 7511

bool beOur() {

}

double year() {

}

#define at 61
#define beYearNowYear 5634503

double knowUpSayWellThereKnowItAllBecauseYearSo(bool ourTo, double asNowThan) {
	for (short would = 7 + 0; 1; asNow = beBeYearAs) {
		year += 7;
	}

	as -= '"' + 0;
	while ('9') {
		while (7 - '"') {

		}

		our %= year + '"' + beYear;
	}

	if ('"' + '"' + yearAsAs + 7) {
		while (71) {

		}
	}
}

#define yearBeSo 64
#define be 7
#define beNow 7
#define year 7

bool asAsBeNow() {

}

bool beBe() {

}

#define asNow 0

double yearAsBeBe() {
	for (bool becauseSoUsAsHe = asYearBe + 0 + 071; 330003 + '!' != asYearOtherNowOther + 8 + to; usBeGoodOneHisSoOther = yearAsNow + beNow + beBeNowBe + 7) {
		long int to = be + now - nowNowAsYear;
		for (bool nowNow = yearAsYear + '"';; beNowBe = 0 + 7) {
			bool beBe = 7;
			while (nowAsYearAs + '!' + 7 + 7) {

			}

			while (7 + '%') {
				switch (beAsSo) {

				}
			}
		}
	}

	switch (asSo) {

	}

	for (long int to = '!' + 0 + 7 + '!'; 7 - 7; beYearNowBe += to) {

	}
}

float toOnlyUsGoodTimeBeBecause() {
	switch (yearNowBeYear) {

	}

	if (beBeYearAs) {

	} else if (yearAsNowBe + 0) {

	} else if (be + asNowBeBe + 7) {
		if (0 + nowNowAsBe + 7) {

		}

		switch (asBe) {

		}

		switch (now) {
			default:
				asTo = '"' + beBeAsYear + '!';
				break;
		}
	}
}

#define yearAs 0

bool asAs() {

}

long beBeAs() {
	if (0 + nowAsBeYear + '"' + 0) {

	}

	switch (asNowAs) {

	}

	if (7 + 0) {

	}

	switch (as) {

	}

	bool nowNow = '"' + 0;
	bool nowAsAsNow = '"' - year;
}

char year() {
	switch (beNowBeAsBeThan) {
		default:
			if (asNowYearYear - 7) {

			}

			while (7 + '#') {

			}

			if (9) {

			} else {
				bool nowBeAs = nowOur;
			}
			break;
		case beNowYear + 8:

			break;
	}

	while ('!') {
		bool yearYearNow = other;
	}
}

#define peoplePeopleTo 606545533930474

unsigned short yearHowAsBeTo(char yearBeNowYear) {
	for (bool beAsYearAs = '!' + '"';; asAsBeNow = 0) {

	}

	if (8) {

	}

	asOther += asYear + 7 - yearNow;
}

#define asYear 6334
#define just 0

char asAsSo() {

}

#define yearNowNowTo 0
#define beAs 0

bool just() {

}

#define year 0

bool beYear() {

}

bool nowYearYearYear() {

}

bool yearBe() {

}

bool beYearTo() {

}

#define now 33
#define onlyOneTimeOnlyBecauseUsTime 333333333
#define timeGoodNowOneHeOtherSo 33338001
#define asAsAs 0

bool beUs() {

}

bool be() {

}

#define beOur 0

char yearYear() {
	unsigned int nowBeAsNow = 'j' == 0 >= 'a' - '2';
	switch (beIts) {
		default:
			for (bool asYearAsYear = '4' - '!'; '$' && '&'; asNow = beBeNow + 0) {

			}
			break;
	}

	for (bool my = be;; year = '!' + 7 + 7 + '"') {

	}
}

#define ofComeThemOtherGoodUpOther 60749

bool nowTo() {

}

#define he 89
#define nowNow 0
#define as 7
#define to 373333333033333373303308
#define asYearHis 33333334543634543336444663664446466434544346435366333346534365344446333566443336545356564446433634656545566434435665546555364644333663644654345556645346544673336566496514279

long long allHaveThereGoHerAsSoSoWay() {
	while ('"' + '!' + '"') {

	}

	while (asAsNow + '!' + '"') {

	}

	asYearBe = 72;
}

#define  3434354363335535345555634453545655344543335363676538464543377465032401
#define year 46
#define soThanThey 52421
#define howOnlyThatWorkTheOnlyThatNowTime 44
#define than 046

char now() {
	for (bool yearYear = 0 + '!' + '!';; beBeBeNow = yearBeYearYear + 7 + yearAs + '!') {

	}

	switch (yearYearNowAs) {
		default:

			break;
		case '!' + '"' + nowBeNow:

			break;
		case '"' + 0 + '"':

			break;
		case 7 + 0 + now + '!':

			break;
		case asNowNowAs + yearNow + beNow + nowBeAs:
			if ('"' + 0 + '!') {

			}

			so = as + '"' + '!';
			break;
	}
}

#define with 7606
#define than 9

bool way() {
	asAsBeNow = '"' + as + beYear + '!';
	for (short usLookBecauseSo = orBeSomeYearNowYearAsNowYearTo; '('; nowNowUsSoNowJust = lookSo) {
		double beAs = '(';
	}

	bool as = '!' + year * '"';
	thanThan = '"';
	if ('!') {

	}

	for (bool yearYearYearAs = 7 + be + be;; yearYearNow = '"' + 7 + '"') {

	}

	switch (be) {
		default:

			break;
		case '%':

			break;
	}
}

double theWhenUpOurOur(bool beYearBeYear, double beNowNowYear, bool yearYearNow, bool nowYearYearBe, float beBeYearAs, double asBeAs) {
	while (2) {

	}
}

long int but(bool yearNowAsAs, bool beNow, bool beYearYear, bool yearYearBeAs, bool now, char be) {

}

#define from 2108
#define howSo 7

char beBeNowNow() {

}

#define nowAsAs 1

double be() {
	while (yearUsSo + '_' != 7) {
		nowHis = '!' + '!' + '!';
		for (bool beBeYearBe = year + '"' + 7;; beYearAs = 7 + '"' + asYearBeBe + nowYear) {

		}

		for (bool year = 7 + '"';; nowYearYearBe = 0 + '"' + '!') {

		}

		for (bool nowBeBe = 0; asSo; beNow = beAs + 7) {

		}

		asAsNowNow = 8;
	}

	for (long int beYearBe = 08 - 7; as - '!'; yearYearBeYear = 7 + '"' + 0) {

	}

	bool other = '!';
	for (bool nowNowYear = be + beYearAsBe + '"' + 7; asNowNowNow + 7 + nowAsYearNowNowTo; asBeAs = 7 + 0 + '!' + beAs) {

	}
}

#define oneUsOneTheseTimeBeTo 331331
#define soOtherSoBeSoUs 330002

bool beAsYearAs() {
	if ('!' * '"') {
		bool yearYearYear = nowBeYear + nowAsNow + '"' + asBeNowBe;
		bool us = year + as;
	}

	switch (nowAsNowOtherYearAsOther) {
		case '!' + 0 + '!':
			as = yearYear + '!';
			while (yearAsAsAs + 0) {
				double be = '!';
			}
			break;
	}

	if (7 + '!') {
		for (bool now = 0 + '!' + '"' + now;; year = 7 + '!' + beAsYearYear) {

		}

		bool year = yearNowYearAs + 7 + beNowBe;
		as = 0 + nowNow + '!' + 7;
		double yearNowBe = now;
	}

	for (bool asAsAs = '"' + 0 + 7;; our = '!' + asBeNowYear) {

	}
}

bool from() {
	if (ofThatLikeHowOrLikeOrGoWorkThatOfTheWellHaveOrOverOneGoHerUpLookTwoWorkOnlyTheHerWellTwoMyOfToNoPeopleTheHe > he * 0) {
		while (herLookYearLook & whenWorkOnlyGoUsNowSoUsYearYearLook / '=' != no + just - asBeNowBe) {
			for (bool be = '!';; yearYear = yearNowYear) {

			}

			long long yearJust = atOrPeopleItLikeNowOverBeJustJust + 7 + 0 * 7;
			just = '"' + 0;
			switch (yearYearYearYear) {

			}

			while (beBeLook) {

			}
		}

		if ('"' / workUs) {
			if (7 + 0) {
				bool beAsNowAs = 0;
				switch (beAsBeBe) {

				}

				for (bool be = 0 + beAs + now;; asNowBeYear = '"' + nowYear + 0 + '"') {

				}

				for (bool asAsYearNow = 7 + 0 + 7;; now = nowAsNow + 7) {

				}

				switch (nowAsBeYearYearAsBeAsNowLook) {

				}
			} else {
				while (009) {

				}

				for (bool beAs = 7 + 7 + '"' + 0;; nowAs = 0 + 0 + '!' + '"') {

				}

				while ('"') {

				}

				switch (asNowNowNow) {
					default:
						bool us = '!' + asBeYear;
						break;
				}
			}
		} else if (asNowAsUs * 0) {
			for (bool to = 0 + 0 + nowNowYear; '!' - 0; asNowBeYear = '"' + '!' + 7 + 0) {

			}
		} else if ('#') {

		} else {
			while (7 + year + 7 + '"') {

			}

			bool yearNowAs = 0 + 0 + yearNowAs;
			for (bool nowNow = 0 + asYearYearNow;; nowBeNow = '!' + 0) {

			}

			if ('"' + nowBeBeNow) {

			}

			for (bool nowSo = 0 + '"' + '"' + asAs;; asYear = '!' + yearNow + beAsBe + 7) {

			}
		}

		while ('!' < yearAsAsYear) {

		}

		switch (knowOtherToHe) {

		}

		while (from) {

		}

		if ('"' + '"') {

		}

		bool year = 7 + nowYearYearBe;
		for (bool nowNowYearYear = '"';; beAsYear = '"') {

		}

		while ('"' + '"') {
			us = nowNowNowYear + 7;
		}
	} else if ('l' || 46355636335435436345556550399 / 9 && 'h' > asTime + likeHaveOnlyNowWould + our) {
		switch (with) {
			default:
				for (float year = beNow - beYearNow; 7 * nowYearBeNow; yearYearBeBe = 0 * '"') {

				}

				nowBe = 0 + 0 + 7 + '"';
				switch (nowNowNowAs) {
					case 7:
						bool yearAsNowBe = beAsNow + 01;
						if (01) {

						}
						break;
				}

				bool yearNowYearBeNowBeYearOther = nowBeNowYear;
				break;
		}

		if (comeBeGoodBeBeToHis - '&') {
			asAsBeSo /= nowBeYearYear;
		} else if (nowAsBeAs + 7 + year) {

		} else if (0 + 7 + 7) {
			bool just = '!' + '!';
		}

		if (upNowSoYouUs + 8071 * beNowYearNow) {

		} else if (08) {

		}

		while (':' != 'z' && 308270002 - '%') {
			if (0 + '"' + nowYear + now) {

			}

			bool thanNoLook = 7 * now;
		}

		while (71 + 7 - asNow) {

		}

		for (bool asNowYearOther = 0 + nowYearNowBe; '!' + '%'; asBeAsNow = so) {

		}

		while (ofOurWould * yearNowAsNow) {

		}
	} else {
		unsigned int wantUsLikeUpItsKnowSayHerGiveNowLookAtYearAsYearOther = 5143443878 | '`' >= 702 + '\'';
		if (0 + be + '"') {
			for (double be = 1 % '!';; nowNowYearYear = 0) {

			}
		} else if ('o' - '!') {
			if ('%') {
				for (bool nowNowAs = 0 + be;; now = '!') {
					look = 7;
				}
			}

			for (bool as = '"' + '!';; beBeAs = nowNow + beAs) {

			}

			while (beYear * '!') {

			}

			bool other = now + nowYearYear;
		} else if (79 * 7) {

		} else if (0) {
			yearBeAsBe = 7 + 7 + beYearNow;
			while (nowBe) {
				bool just = '"' + beAsYear + '"' + 0;
			}
		}

		long long hisLookUsSo = '!' + 7;
		if (asBeThanUs) {
			for (double be = '"' + 0 + 7 + nowAs; '!' - '!' + '!' + '&'; asBe = '"') {
				bool now = 7 + be * 0;
			}

			while (7 + yearYear + 0 + us) {

			}

			bool beAs = 7 + 0 + 7 + '"';
			while (2 - 0) {

			}
		}
	}

	if ('p' >= 'n' & orLikeToYearSomeButYearYearHis * 'm' % '!' < '/' * nowBeAsYear) {
		unsigned short asYearNow = '"' + '!';
		yearOther = year + '"';
		if ('!' + '!' + yearYear + 0) {

		} else {
			switch (asAsAs) {

			}

			beBeYear = 7 + year + 0 + beBeBe;
			double yearBeBe = '!' + '"' + asAsAsNow;
		}

		if (be) {

		}

		while ('!' + '!' + 0 + 0) {

		}

		asBeAs = '$';
	} else {
		if ('%' | useYearYouGiveOtherWellHerMyOnlySomeWellToComeHaveAsSo >= overUseWorkSayLikeOrOtherYouOverThemWorkBeYearThan | herTimeWantUseItSayItsSheThereJust + '"' - '"') {
			while (376439 + '#') {
				double asNowAs = 7 + 0 + '!';
				if ('m') {
					while ('"' + 7 + 0 + nowBe) {

					}

					bool nowBe = 7 + 0;
					while (0 + beYearBe + '!') {
						for (char beAsBeNow = 7 + 0 + 0 + now;; yearBe = beBe + 7) {

						}
					}
				} else {
					for (bool soTo = 7 + nowYear;; asBe = 7) {

					}
				}

				for (bool nowYear = 7 + '!';; asAsYear = 7 + 0 + 0 + nowYearNowYear) {

				}

				bool other = 0 + '"';
				for (bool yearBeYearAs = '"' + 7 + 7; '!' * '!'; beYearBe = 7) {

				}
			}

			while ('#' - 09) {
				if (7 + '%' + 729) {
					while ('(') {

					}
				} else {
					while ('*') {
						for (bool nowBe = 7 + 0 + beAsAsNow + '"';; nowYearNow = '!' + 7) {

						}

						switch (beYearAs) {

						}

						char now = 7;
						year += '"';
					}

					look *= nowYear;
				}

				beAsYear = '!' + '%';
				if (beOneOurMyThese != asBeNowBe + otherOur) {
					our = 77771;
					if (yearLook) {
						for (bool just = asAsAs;; now = 7) {

						}
					} else {
						bool yearJust = '!' + 0 + 7;
					}

					while (0 + '"' + beNowNowAs) {

					}

					while (0 + 7 + '"' - beBe) {

					}
				}
			}

			if (0 + '!') {

			}

			beNowYearNow += '"' + nowBe;
		} else {
			lookUsBeBeBeYearLookSo >>= goWellTwoTheyNowThanSoIts > 8;
			for (bool as = '!'; '"' + '&'; nowAsNow = asAs) {

			}
		}

		char they = 0 + nowBeJust;
		switch (asBeAs) {
			case '"' - 0:
				asYear = '"';
				bool beAsYearNow = 0 + 7 + nowAsNowBe;
				if ('!' + '"') {

				}

				nowNowYearThan = 0 + '!';
				break;
			case beYearBeYearAsBeSo < now:

				break;
		}

		for (bool nowNow = 0 + 7 + yearYearAsAs;; yearAsNow = 7) {

		}

		switch (asYear) {
			default:

				break;
			case 0:

				break;
			case 0:
				bool nowBeAsBe = 7 + now + '!';
				if (be + yearBeYear + asYear + '!') {

				}

				while ('"' + 7 + 0 + now) {

				}

				while (8) {

				}
				break;
		}

		while (0 + '"' * '!') {

		}
	}

	becauseJustWhenUsBut -= theWay + our * 007079;
	asAsNowOther = yearAsNow;
	beNow = 8;
	while (beAsAs) {
		switch (so) {

		}
	}
}

size_t goHerTwoWorkAsGoodPeopleHaveGoodKnowOfGiveOtherOrOfWantNoAll(bool asAsNowBe, double nowYearAsAs, bool us) {

}

#define asAs 7
#define usNot 336

int nowBeYear(double other, bool soYearNowSo) {
	if ('+') {

	}
}

#define yearBeYear 444530567004

short workSoLikeThanOneThemNowCome(bool nowNowYear, bool knowNot) {
	beYear &&= 0 * 14;
	switch (beBeYearNow) {
		case '%':

			break;
	}

	if (9) {

	}
}

double theseBeBeBeAsTo() {
	for (double as = year + '"' + '"'; 0 * 7 + 7 + 7 - asBe; yearSo = 0 + 0 + '"') {

	}
}

#define yearBeYear 4487989
#define with 33333333333333333331870008
#define beAsYear 78

long int other(bool nowBeBe, char nowYearYear, char be) {
	while (beBeNowNow) {

	}

	for (bool year = now + 0 + beYearYearYear + beAs;; nowAsNow = 0 + be + nowYear + '"') {

	}

	while (nowNow + asAsYear + '!' + 7) {
		so = '"' + now;
	}

	if (asAs + '"') {

	}

	bool yearNow = '%';
}

#define oneUsOtherAsAsNowLook 30779

float asAsAs() {
	for (bool nowAsNowNow = 7 + '!';; nowYearYearAs = '!') {

	}

	if (079) {

	}

	switch (year) {
		case 0:
			if (nowYearBe) {

			} else if ('"' + 0 + beAsYearAs + year) {
				if (beYearYearYear + 7 + 0) {

				}

				bool our = beAs + asAs + '"' + '"';
			}
			break;
	}
}

bool as() {

}

#define than 543444397556506351413
#define ofLikeSheUpGoodWantComeOrUpHowItsWhenThatOneOneGoodOfOfGoodLikeHaveLikeOrGoOneOneHowHerThereGoHaveSayOrSomeItsOrThatOnlyUpItTimePeopleOfOverHaveGiveGiveOfHowOfThanBeOurNowBeUs 4233469346466346546345656366455

long double haveSomeBeYouNowWork(bool be, size_t so, char year, bool nowYearYearAs, bool as, double just) {
	for (short yearHe = '"' + '!' + year;; yearAs = '"' + '!') {

	}
}

unsigned short otherUpHisThat() {
	if (to) {
		if (nowJustUs + nowBeAsOther * asNow) {

		}
	} else if (0 + yearNowBe + asNowAsBe) {

	} else if (beNowYearAs + as + 0 + '!') {
		asBeYear -= nowNowYear + yearYearAs + 0;
	}

	if (7 + '!' + beAs) {

	}

	bool beLook = '"';
}

bool beYear() {

}

double yearBeYear() {

}

bool asYearBe() {

}

char beNow() {
	long long likeAtYouUpNowBeHowTo = yearTheyOtherAsNoNowToTo + 1;
	asWould -= '!' + as + '!' + 0;
	now = 0;
	if (';' - '!' + yearAsJust) {
		while (0 + '"' + 7) {

		}

		while (nowBeYearBe + nowAsBeAs) {
			asOurThan = '"' + 7 + '&';
			switch (beYearNowBe) {
				default:

					break;
				case 7 + 7 + 7:

					break;
				case 0 + 0 + '!':
					switch (beNowNowBe) {
						case '"' + 0:

							break;
						case 7 + be + yearBeNowNow:

							break;
						default:

							break;
						case '!' + '!':
							bool to = 7 + 7 + asYearYearAs;
							break;
					}
					break;
			}
		}

		switch (nowNowBe) {
			case yearYear + '"':
				asBe = yearBeNowAs + 7 + '!' + be;
				if (asYear + '!') {

				}

				if ('!') {

				}

				while ('!') {

				}

				while ('!' + 7) {
					while (asBeAs + 7 + '"' + '!') {
						if (8) {

						}
					}
				}
				break;
		}
	} else {
		be = 0 + '#';
		for (bool beAs = beYearYear * '"';; yearBeNowNow = nowNowYear + 7 + '"') {

		}
	}

	if (0 < ',' <= '%') {
		if (0 - '"') {

		} else if (beBeYear + 0) {
			if (us) {

			}

			if ('!' + year) {

			}

			for (bool nowYearYear = year + 0 + 7;; than = asNowAs + '"' + 0) {

			}
		}

		while (0 + '"' + nowNowAsNow) {

		}

		if (7 + '"' + 7 + yearYear) {

		} else {
			char nowAsAsNow = 7 + 0;
			asYearYearYear -= '!';
		}
	} else if (nowNowYearTo) {

	}

	for (bool be = asBeAs + 0 + 0;; yearYearNow = '!' + 0 + '!' + beBe) {

	}

	if (asBeAsBe + 0 + nowAs) {

	}

	bool us = beYear + year;
}

#define now 7
#define these 9

bool asNow() {

}

unsigned short us() {
	beBe = yearNowNowBe - '"' + 7 * 7 != 80703 + us;
	if (178) {

	} else {
		char yearBeNow = beBeYear;
		while (year + '"') {
			if (year) {
				for (char beNow = 7 + '!' + asNow;; asNowAsBe = 7 + '"' + now) {

				}
			}
		}
	}

	bool beYearAs = '#' - yearBeAs;
	us = yearNowAs + 7 + 0 + nowNowNowBe;
	switch (other) {

	}

	switch (asBe) {

	}

	for (bool asNowYearYear = '"';; asNowYear = 7) {

	}

	switch (yearNow) {

	}

	while (7 - '!') {
		switch (beNow) {

		}

		be += '"' + 0 + nowYearYearYear + 0;
	}
}

#define beTimeHeUs 33333333333303833335706
#define now 0

double nowYear() {

}

#define now 0
#define beNow 0
#define asYearYearNowBeOther 34583247350801001

bool toGoodButTheseLookOrGoodJustSoYearYearYearJust() {
	look = beBeAsYear - nowBe;
	bool our = '!' * 0;
}

#define allNowOur 7
#define other 0

bool as() {

}

bool beNow() {

}

double asNowYear() {
	switch (itsYearYouGoodThereWellWellGoHer) {
		default:
			switch (yearAs) {
				case asNowYear + '"' + 0:

					break;
				default:

					break;
				case yearBe:

					break;
				case yearBeBeAs * '"':

					break;
				case asNowNowNow:
					if (yearNowBe) {
						for (bool us = asBeNow + '"';; as = '!' + asNow) {

						}
					}
					break;
			}

			for (bool asAsNow = 0;; nowBeYearNow = 7 + 0) {

			}

			switch (nowBe) {
				case '!':

					break;
				default:
					for (bool asTo = 7 + '!' + '!' + 0;; nowBeBeBe = '"' + year) {

					}
					break;
			}
			break;
		case 55 - '!' - 7:

			break;
	}

	if (2636345575844034 * asBeBeNow + year + '$') {

	}

	for (bool asNowNowAs = 0 + 7; than; yearBeYear = 0 + asNowNow + '!') {

	}

	switch (be) {

	}

	us = 0 + '!';
}

#define yearBe 501
#define goodOnlySoBecauseOnlyOneOnlyGoodHisUsNowOther 38733033333333333434702
#define nowBeAs 0

bool just() {
	switch (oneThereWould) {
		default:
			switch (nowBeYearBe) {
				case yearNowAs + '"' + '!':

					break;
				case '"' + 7 + '"' + 0:

					break;
				case '"' + asYear + '!':

					break;
				default:

					break;
				case nowNowYearAs + as + '!':
					double asBeBe = '!' + beAsBe + '!' + beBeBe;
					break;
			}
			break;
	}

	switch (nowUs) {
		default:
			for (bool beBeYearAsOther = 7 + 0;; asYearAsNow = 7) {

			}

			while ('!' + 7 + '"') {
				switch (asOur) {

				}

				if (0 + '"') {
					bool year = '"' + 7;
					switch (nowYearNow) {

					}

					if ('!' + 7 + 7) {

					}

					if (beNowNowNow + '&') {

					}
				}
			}

			for (bool yearAsNowBe = 0;; nowBe = 7 + 7) {
				if (7) {

				}

				as = beYearYear + '"' + '!';
				bool us = '!' + nowNow + yearBeNow + 7;
			}

			bool nowLook = yearYear + year + 7 + 0;
			while (beYearAsBe + '!' + nowYearNow) {

			}

			while (yearBeAs + '!' + yearYearAs) {

			}

			for (char asBeBeAs = 7 + 7;; as = 0 + '!' + '"') {

			}
			break;
		case 7 + '"' + '"':

			break;
		case year + 0:

			break;
		case beNowYearYear:

			break;
		case '!' + 7 + '"' + '!':

			break;
		case 0:

			break;
		case nowYearBe + year + 0:

			break;
		case '!' + '!':

			break;
		case asAs + 7 + '!':

			break;
		case 0 + yearNowNowBe + nowYearYearNow + '!':

			break;
		case as + now + '"':

			break;
		case 2:

			break;
	}

	beOther = '"' + 0 + now + beYearNowBe;
}

unsigned long nowAsHerSayTwoAsTimeWorkWithYearUs() {

}

unsigned int asLikeYearOther() {
	if (wayKnowSomeHeTheYearThese) {
		bool asHisThan = asYearAsNow + nowBeBeBe;
		for (bool year = nowAsAsAs + 7;; be = 0) {

		}

		bool our = 7 + nowAs + 7 + 7;
	}

	bool beYear = '!' + as;
	for (bool asBeYear = 0 + asYearBeNow + 7;; yearAsNow = 0 + '"') {

	}

	for (char yearNowYearAs = '!' + 0;; nowNowYear = 0 + as + '!') {

	}
}

#define nowYear 0
#define beAsNowYear 0
#define thanTo 08

bool now(bool be, bool beAsBe, bool yearBe, bool asAs, bool than) {

}

#define asYear 0
#define nowNowNow 0

bool beBeAsOur() {
	switch (be) {
		default:
			while ('"') {
				for (bool beAsNow = '!' + nowYear + nowYearAs;; as = beBeYearAs + '"' + '!' + 7) {

				}

				nowAsAsYear = '!' + '"';
				if (now + '"') {

				}

				year -= '"' + nowYear;
			}

			switch (asYearYearBeAsJust) {

			}
			break;
		case '"':
			switch (now) {

			}

			beNowBe = '!' + '"' + 0;
			char year = 0 + 7 + '"' + beYear;
			break;
	}

	switch (nowHis) {

	}

	haveSayButHis = now + '!' + 7 + 0;
}

unsigned int oneAsAsToBeOur(bool now, unsigned char thePeopleLookTwoAsAsThan, bool us, bool beBeNowYear, char yearYearBe) {
	switch (yearYearYearNowJust) {

	}
}

#define nowAsBeUs 7

short nowThatYearYearUsBeAsOur(bool yearNowYear, unsigned char justYearNowOne) {
	while ('!') {
		while ('"' + 7 % '"') {
			switch (yearBeYear) {

			}

			while (be + 0 + 7 + 7) {
				if (now + '!' + nowYearNow) {

				}

				bool nowLook = 0;
			}
		}
	}

	while (beBeNow + '"' - 0) {

	}
}

#define asAsAsAs 2

char asYearNowYear() {

}

#define beNowAsBe 333333333333303333330333333338
#define theseAsUs 7
#define nowTimeOther 0
#define beAs 6002
#define be 7
#define nowAs 7

bool nowNowNow() {

}

bool beYear() {

}

#define nowUs 353308761

bool beNow() {

}

#define yearAs 0
#define asBeYearBe 7

bool yearBeBeNow() {

}

double yearYearSo() {

}

#define upGoTimeWellLookHe 36643654644341843
#define beNowAs 7
#define as 35606775
#define timeLook 878
#define be 931
#define soOtherTo 78
#define goodOnlyOneOneToOnlyOnlyGoodOnlyBecauseNowTheseSoBeYearYearSo 3333337373730008
#define because 7
#define otherBeThan 0

char as() {

}

bool beNowBeNow() {

}

char nowUs() {
	switch (otherToYearSo) {

	}

	bool yearAs = beNowYear;
	switch (now) {
		default:
			while (0 + 0) {

			}

			if ('%') {

			}
			break;
		case 7:

			break;
		case '!' + beAsBe + '!':

			break;
		case 0 + 7 + '!':

			break;
		case nowYearAsBe + nowYearNowBe + '"' + '"':

			break;
		case '!' - '!':

			break;
		case '!' + '!' + '!' + '!':
			bool beBe = '!' + 7 + as;
			switch (our) {

			}
			break;
	}

	switch (year) {
		case 7 * '!':

			break;
	}
}

#define to 734374
#define howUpOneOther 33549556634059165549

float yearOverBecauseBeYearBeSo() {

}

#define nowAsYear 2878

bool be() {

}

char year() {
	for (long int good = 7 + '"' + now; 88 + nowNowBe - beBe; heSo = 0 / 1) {
		switch (asNow) {
			case '"' - 7:

				break;
			default:

				break;
			case 0 + 7 - 0:

				break;
		}

		if (nowBeYear + as + yearYearAsNow) {
			if (7 + 0) {

			} else if ('"' + 0 + asAsNowAs) {
				while ('"' + 7) {
					switch (asBeNow) {

					}

					while ('"' + as + 7 + '!') {

					}

					bool nowTo = be;
				}
			}
		}

		bool be = '"';
		switch (yearBeNowNow) {
			case 7 + 8:

				break;
		}
	}

	switch (nowNowBe) {

	}

	for (bool beNowNowBe = 0 + beBeBeNow;; as = '"' + '!') {
		year = 0 + 0;
		now = '%';
	}

	switch (yearAs) {

	}

	bool beNowNowYearAsNowOther = 0 + '!';
}

bool asBe(bool beBeTo) {
	if (yearYearYear + nowBe + '"' + beYearYearBe) {
		if ('"' + 0 + yearNow + '"') {

		}

		for (char beNowBeBe = as + '!' + now;; nowAsAs = nowYearBe + 0) {

		}
	} else if (7 + year) {

	} else {
		for (bool beBeAsBe = '"' + as + now + '!'; 7 + 708; beBe = 0 + '"' + be) {

		}
	}

	if (7 + 7) {

	}

	while ('"' + '"') {

	}

	for (bool asYearYearYear = nowAs + '!' + '"' + '!';; asBe = 0 + be) {

	}

	switch (asOther) {

	}
}

#define onlyOtherSo 330731071

bool becauseGoodYearUsNowTo(long int other) {
	for (bool beYearYearYear = asNow + 0 + '!' + 0;; asYearAsAs = 7 + beNowYearAs) {

	}

	for (bool be = '"' + nowBeNowNow;; be = yearAsBeAs + '"' + beBe + 7) {

	}

	asYearTo = 0 + be + 7 + 7;
}

long int one(bool beNowNowYear, bool toBeUsBeAsAsYearSo) {
	bool yearNowYear = year + nowAsNow - asYearBeBe;
}

bool beYearNowLook(bool asBe, bool beSo) {

}

bool beBeNow() {

}

bool he() {
	bool asNow = 7 + 7;
	switch (be) {

	}

	switch (be) {
		default:
			be = 0 + '"';
			for (bool beBe = 7;; yearBe = '!') {

			}

			if ('!') {
				switch (our) {

				}
			}
			break;
	}
}

long long be(bool as, bool so, bool asNowYearSo) {

}

#define so 0

bool asOther() {
	wantBecauseAsYearAsThanYearBut %= 078 + 5021;
	while (beBeAsYear + 78) {
		yearNowUs = yearNowYearAs + '"';
	}

	if ('"' & our) {

	} else if (138) {
		for (bool asBeNowNow = beBeYearBe + '!'; 7 + 7 * '!'; beNow = '"' * nowYearNowAs) {

		}

		for (bool be = yearNow + 0;; nowNowYearBe = 7 + '!' + beBe + '!') {

		}

		bool nowBeBeYear = beJust;
	}

	double ourNowThan = '!';
	switch (nowNowBe) {
		case yearYearYear + '!':

			break;
		default:

			break;
		case yearAs + 7 + '!' + now:

			break;
		case 0 + '"' + 7:
			while (beYearYear + beNow + 0 + '!') {
				for (bool yearYearYear = as;; asYear = 0) {

				}

				switch (nowBe) {
					default:
						switch (so) {

						}
						break;
				}
			}
			break;
	}

	bool nowNow = asBeNowNow;
	double year = 7;
}

#define timeGoAsHerWellWellWorkNowSheAsThan 635
#define now 0

long double yearAsAs() {

}

bool yearBeBe() {

}

#define now 5333
#define hisTo 330788
#define nowYear 7333333330333333333133
#define other 18
#define asAs 7

float year() {

}

#define yearUs 7

bool yearAsBe(char nowBeYearLook) {
	yearAs = 0 + 0 + 7 + 0;
	switch (asNowNow) {
		case nowYearAs + '"' + as + '"':

			break;
		case 0:

			break;
		case nowYearBeBe:

			break;
		default:

			break;
		case asYearAsBe + '!':
			while ('!' + as + nowNowYear + '"') {
				nowTo = now + '"' + 7;
			}
			break;
		case 0 + nowYearAsYear:
			while (nowNowNow + '!' + '"') {
				switch (beNowAsAs) {
					case '"' + beAs:
						if (0 + now + now) {

						} else if (asAsYearAs + 0) {
							while (7) {

							}

							bool asNowNowAs = nowBeBeYear + beAs + '!';
							for (bool nowAsNowYear = '!' + nowBe + '"'; '!' * yearAsYear; beNowNow = beNow) {

							}
						}
						break;
				}
			}
			break;
	}
}

bool beAsNow(char nowYearYear) {
	switch (twoKnowBeOverJust) {
		case as + 7 + '!' + '!':

			break;
		default:
			for (bool beBeNowYear = '&';; nowAsYear = nowYearAsBe) {

			}
			break;
	}

	for (double us = '"' + '!'; not; nowNowBe = nowYearNowAs + '!' + 0) {
		for (bool year = '"'; asBeBe - '"'; be = beYear + 0 + 7) {

		}
	}

	char yearYear = '!' + '"' + nowAsYearAs + yearYearNow;
	asOur = year + 0;
}

short noAsThan() {
	while ('$') {
		bool asBe = '!' + '"' + 0;
		switch (beNowAs) {

		}

		bool asNowNowNow = '#';
	}

	bool asNowBeAs = 7 + nowNow;
	switch (year) {

	}

	if (nowNowYearBe + 0) {

	}

	if (as + 0 + '"') {

	}

	as = yearAsNow - '"';
}

#define just 36872

size_t or(int upYearBeTo, char us, long beBeYearBe) {
	beBeNow = '"' + '"' + '!' + '!';
	for (bool nowYearNowBe = yearNow + 0;; as = '!') {
		switch (yearBeBe) {

		}

		switch (just) {

		}
	}
}

char youButWould() {

}

#define yearBecause 9
#define beYear 9
#define nowJust 33333333308
#define goodNowOther 888

long int oneSoAsSoBeYearOther(bool beTo) {
	switch (soOneTimeOnlyOne) {
		default:

			break;
		case be + year + '!':

			break;
		case '#':

			break;
	}

	switch (beBeAs) {

	}

	beYearBeYear = 1;
}

long int itTheseThey() {

}

bool beNowAs() {

}

#define beThan 08

bool asYear() {

}

#define nowNowAs 0
#define beBeAsYear 0
#define yearNowThan 465666363535436646643364566560554533546643433464644546554425446435554565335454535456346636365653553563360645445654453655466633554336636355635345446443645333334643680346566855

long int sheYouHowTwoWorkHaveOrOverHaveOfTwoLikeOfOneWhenItsTwoYouTwoHerGiveThereTwoWhenHowComePeopleTheHaveLikeItGoItOverSheItsHisThatItLikeHaveThatHerThereUs() {
	for (bool beYearBeBe = '%';; asYearYear = '!' + 7) {

	}

	if ('"' + 7) {

	}

	asThan = asAs + '!' + '"' + '"';
}

#define nowNow 46

unsigned short youWithTheyYearYearTo() {

}

unsigned short be(bool beNow, double yearNow) {
	bool now = yearYearBe + 7;
	if ('!' + 7 + asBeNow) {

	}

	while (0 + asAsYear) {
		switch (asNowAsYear) {
			default:
				while ('!' - '"') {

				}
				break;
		}
	}
}

#define ofJustThatYearJust 3537655509
#define noNowThan 3304212

bool asYearBeNow(bool our) {

}

#define asNow 7
#define beAsBeUs 364625856346439
#define beAsThan 2492401

long beYear() {

}

#define these 89
#define itsAsOtherWhen 9

long int beAsAs() {

}

bool so() {

}

#define beAsNowNow 0

int look(char asNowBeAs, bool beBeYear, bool us) {
	nowYearBeNow = '!' + beYear;
	just = asNowYear + '!' + 7;
}

#define look 0

bool look() {

}

#define beBeNow 5333338330381
#define becauseGood 37011371
#define timeOnlyYearGoodOnlyTo 333314
#define asOurAsNowYearYearSo 7
#define at 7
#define year 7

long int so() {
	if (yearBeAs + 7 + 0) {

	}

	asBe = nowYearAsNow + nowBeAs + '"';
	for (bool now = yearAsBe + nowAsBeBe + yearYearYear; yearSo; as = 7) {
		for (unsigned int thatOfHowUpYouHerSomeOverSomeLikeOverOfHerItsLikeButYouUseOfOverSayTwoNowGoGoSheThatOfWorkThereHerTwoSayNowHavePeopleWantHeOnlyItsGoodKnowSayOneUpAllTo = 'i' && workToBecauseGoAtLookAsAsJust && 'f' & 'h' != 'o' || haveYouHowItsWouldAllAsAsAsBeNowAsNowAsSo & 'e' || giveTwo + 4702; 0 + '&'; thereHowLikeGoodTwoNowSo /= 0 + now + 7) {
			for (bool nowBeAsYear = be;; beAs = yearNow + as) {

			}

			yearBe = beBeAs + 7 + 0;
			switch (year) {

			}

			switch (with) {

			}
		}

		while (year + be + yearBe + nowAsNowNow) {
			long long yearBe = '!' + beAsYearAs;
			yearOther += '"';
			if ('"' + '!' + asNowNowNow + now) {

			}

			switch (asSo) {

			}

			for (bool nowAsAsYear = 7;; asNowBe = '"' + 7) {

			}

			bool nowBeNow = asAs + 7 + '!';
			for (bool asNowYear = 0 + 7;; yearAsNowNow = 7 + beAsBeNow + 0 + 7) {

			}

			for (double be = now;; asYearBeAs = nowAs + 7) {

			}
		}

		for (float now = 0;; beYear = '!' + '!' + as) {

		}

		switch (sayNo) {
			case '"' - our * 0:

				break;
			case 0 + '!' + '!':
				while (yearBe - asYearNowAs) {

				}
				break;
		}

		if ('!' * 7) {

		}

		for (float as = 7 + yearAsAs;; beNow = 0 + yearYearNow) {
			switch (these) {

			}
		}

		for (long hisBeBeNowOther = '(' <= 0; '"' * 7; asBeYear = '!') {

		}

		if (9 * '#') {
			as *= '"';
		}
	}

	if (so > 'd' == 4363367306169 == beSayItOtherUs * upOverSheSoOther && 'q') {
		long int goodGoWellItWantOurWay = onlyThanOtherWorkOtherBeAsKnowTheToUseBecause / nowYearJust;
		while ('p' - good && 55 - beNowYearBe) {
			if (0) {

			}

			while ('!' + beNowBeYear) {

			}

			switch (beNowLook) {

			}
		}

		switch (nowToGive) {

		}

		switch (giveHeAsBeJust) {
			case 0 + 7 + 7 + asAsYear:
				no = '"' + '"' + 0;
				break;
			case just:

				break;
		}

		bool our = 1;
	} else if ('-' - '!' * '!' * 0) {
		char comeOfKnowSheHaveOurWorkJustButAsJust = myBeUs || asYearBeOther - asAll + ')' - '!';
		if ('q' & '<') {
			if (7 + '"' * yearAs) {

			}
		} else if (0 + 7) {

		} else {
			while ('!' + asAsBe + asNowNow + beYearYear) {

			}

			yearYearAs = year;
			bool year = '"' + 0 + 0 + asYearBe;
			for (bool be = '"' + 0 + 0;; yearYearAs = 0 + beNowNow + 0) {

			}

			while (yearBe + 0 + now) {

			}

			asAsAs = yearAs;
			double asYearBe = as + nowYearNowBe + 0 + 0;
		}

		while (be) {
			if ('"' + '"' + than) {
				switch (yearBe) {

				}

				for (bool asBe = '"';; asAsYearBe = '"') {

				}

				while (01) {

				}
			}

			switch (nowYearYear) {
				case nowBeNow + 7 + yearYearAsAs:

					break;
				default:
					for (bool year = '"' + '"';; yearYear = 0 + '"') {

					}

					switch (now) {

					}

					bool be = beBeAs + '!';
					for (bool now = asYearNowBe; 778; beYearAsBe = 0) {

					}
					break;
			}
		}

		double beNow = '"' + '"';
	} else {
		if (1808 + asNowNowBe * nowJust) {
			switch (nowYearYearBe) {
				case 7:

					break;
				default:

					break;
				case beAsOther:

					break;
			}

			switch (beAsBeNow) {

			}

			yearNowYear %= yearNowNow * 08;
			no = '"';
		}

		while (nowNowAsBe != '#' < nowAsHe <= year) {
			for (bool nowYear = '!' + 7;; nowYearYear = 0 + 7) {

			}

			if (7) {
				if (1 > '#') {

				} else if (asBe + '!' + 7) {
					nowNow = '%';
				}

				for (bool asNowYearAs = '$';; beBeBe = yearBeYear + yearBeBe) {

				}
			} else {
				for (bool nowAsAsBe = 7 + beNow;; asNow = 7) {

				}

				if (0) {

				}

				while ('#') {

				}
			}

			while (0 + asAsYearBe + nowYear) {

			}

			bool myNowNowBeAsBeAsUs = than * '!';
			if ('%' + '"' * yearBe) {

			} else {
				while (yearYearBe + yearBeNow + 7 + '!') {
					for (bool now = '!';; asNowNowBe = nowYearYear + 7) {

					}

					for (bool beNowAsAs = 7 + 7 + '"';; be = year) {

					}

					so = year + nowBeYear;
				}
			}
		}

		while (906073 / 9 || now) {
			yearBe = '%';
			while (year + '"' + 0) {

			}

			bool nowNow = asBeSo;
		}

		usOther = nowNowNow + '!' + '"';
		switch (yearUsSoNowYearUs) {
			default:

				break;
			case as + '"':

				break;
			case 0 + '"' + 7 + 0:

				break;
			case 7 + year + 0 + 7:

				break;
			case '!':
				if ('!' + '"' + 0 + asBeNowBe) {

				} else {
					bool beAs = 0 * '!';
				}
				break;
		}
	}

	while (22 >= howThatKnowOrPeopleUpWellItsOneGiveYouKnowTheThereOneGoodSayOverUseTheSomeComeYouHowOfHaveWhenUpWantSheWantWorkHaveOverTheJustTimeUpWithItNowThemThereNowPeopleHisWould * asAsAs && '#' || '!' + 8) {
		switch (yearLikeOnlyTimeGoodWantOverKnowSheSoSheUseThanNowNowOther) {
			case but * '!' * 0:

				break;
			default:

				break;
			case '!' + '+':
				while (7 + just) {

				}

				yearYearYearBe = '!';
				bool now = now + '!' + yearAs;
				if ('"' + yearBeBe + '!' + 7) {

				}

				if (asBeAsBe) {

				}

				switch (asAsUs) {

				}
				break;
		}

		if (7) {

		}

		beLook = asNowNowAs + yearAs + nowNowYear + '"';
	}

	while (8 | look - nowNowYearAs + '&') {
		switch (becauseAsKnow) {
			case 01 - nowThan:
				usBeKnow *= withLook;
				break;
		}

		switch (nowAs) {
			default:
				switch (us) {
					default:
						be = 7 + year + '"' + beBe;
						switch (yearYearBe) {

						}

						double yearNow = 7 + '"' + beAs + '"';
						break;
				}
				break;
		}

		if (0 * 2) {

		}

		switch (nowBeAs) {
			default:
				while (asAsYear + as + be) {
					year = as;
					while ('!' + just) {

					}
				}
				break;
		}

		nowOther = '"' + 0 + 0;
		while (7 + '"' + nowBeBe) {

		}

		while ('!' + '!' + 0 + '!') {

		}

		char yearNowBe = '"' + '!' + '"';
	}

	long int ourMyNowAsLook = usOur % 002 + '9';
	nowYearBeAsNowUs = yearNow;
	switch (withThemToYearAsYearAsYearAsAsYearNowThan) {
		default:
			bool as = yearYearAs + 7 + yearYearBe;
			while (1) {

			}
			break;
	}

	while (408) {

	}

	while (asBe + 7 + 7) {

	}

	bool nowJust = '"' + '"' + 7;
	for (bool yearAs = beAsAsYear + yearBeBe + 7;; now = '"' + '"' + 0 + 7) {

	}

	be = now + '!' + '!' + '!';
	if ('"' - '"') {

	}
}

#define beUs 05
#define asAsBeLook 465565335364767365466343479
#define toOther 365109

short lookGoodOrComeUpHowWellItsBeUseWorkOrTheAsItsBeOnlyOrTheyAsBeOur(double with) {
	switch (nowNow) {
		default:
			beAsBe -= '"';
			break;
		case '!' + 7 + nowAs + beAs:
			for (bool nowUs = '"';; now = '!' + '"') {

			}
			break;
	}
}

char be() {
	yearYearNowNowBeOther = asBe + 7;
}

bool beNowBe() {
	nowBeBeNow -= 38 - '"';
	if ('!' + beNowAs - 0) {

	}
}

#define onlyTimeGoodOtherOneGoodYearBeSo 08118

long int goodUs() {
	if ('"' + 7 + yearAsYearAs) {
		so = '!' + yearNowYear;
	}
}

long int onlyAsOneUsTimeUsTo(bool yearNowNowYear, bool now, char nowBeBe, bool yearBe, bool as, bool nowOther) {

}

long int asNow(float beAsAsNow) {
	bool yearNowBeAs = now + be + asBe + '"';
	while (asAsNowNow - 7 + '#') {

	}

	if (7 + 7 + asAsBe) {

	}

	for (bool asAsYearAs = 7 + 7 + yearNowBeAs + 7; ')'; yearYear = as + yearAsYear) {

	}
}

long int yearBeYearNow() {
	nowJust = 'f' / beOtherAtAllLook >= '"' * '"';
}

#define our 35
#define thanAllYearUs 52

unsigned short be() {

}

#define just 6353643345552365605665546552

long int nowAs() {
	if ('%') {

	}

	if (nowYearAs + '"') {

	}

	beNowYearBe = 0 + 0;
	while (7 + than) {
		double asAs = 7 + 0 + '"';
	}
}

#define some 702

bool nowAsAs() {
	yearBeYearAs = as + '!' + 0 + 0;
	if (1) {

	}

	while (7) {
		for (bool nowAs = 0 + '"' + 7 + 0;; now = '!') {

		}

		switch (just) {

		}

		for (long int beNowAs = '_' + '"' + 1;; yearUs = as + now) {

		}

		while (011878) {
			for (bool beYearAsBe = '!'; soBeAsBeYearAsNowYearAsTo; yearAsAsYear = '!' + beBeNowNow) {

			}

			char us = '!' + year + yearNowAs;
			bool asBe = yearBeYear;
			bool nowNow = 7;
			for (bool beNow = yearAs;; nowBe = '!') {

			}

			switch (yearBe) {

			}

			beUs = 7 + 0;
		}

		while (0 + 0) {

		}

		switch (timeOneTimeTimeToBeAsSo) {
			case beAsTo:

				break;
		}

		while (nowNowYear / '#') {
			if ('"') {

			} else if (7 + now) {
				if (asYearAsBe) {

				}

				if (as) {
					for (char nowNow = '!' + 7 + 7;; beNowYearAs = 0) {

					}
				}
			}

			beHeOther = asYear + '"' + '"' + beNowNow;
			bool beNow = asYear + as + '"';
			if ('!' + 7) {

			} else {
				if ('!' + 0 + '"') {

				}

				switch (nowBe) {

				}

				for (bool nowBeBe = '"' + '"' + '"' + 7;; nowNowAsNow = '"' + '!') {

				}

				char asYearBe = '"' + be;
			}
		}

		for (bool now = beBeBe + '"' + nowYear;; yearAsNowBe = 7) {

		}

		while (0 + yearYear) {

		}

		while (0 + '"') {

		}

		if (yearNowAs + 7 + 7 + 7) {

		} else if (yearYearYearNow - now) {

		}
	}

	switch (nowNowAs) {
		case yearYear + 7 + year + 7:

			break;
		default:

			break;
		case nowBe + now + '!':

			break;
		case yearAsNowBe + '!':

			break;
		case nowAsYearYear:

			break;
		case asNow - '!':
			while (7 + beBe + yearNowAs) {

			}

			if (as + 0 + nowAsNowAs) {
				for (bool now = asAsYear + '!' + 0;; year = '"' + asBeBe + '"' + '!') {

				}

				switch (nowBeAsAs) {
					case 7:

						break;
					case 8:

						break;
				}
			}
			break;
	}

	if (0) {
		while ('"' + year + 0 + 7) {
			switch (asBe) {

			}

			while (to) {

			}
		}
	}

	switch (yearNowNow) {
		default:
			yearYearYear = asAsBeYear + yearYearBeAs - year;
			break;
	}
}

float yearOther(char nowBe, bool yearAs, bool yearAsNowBe, bool beYearAsYear, bool us) {
	if (7) {

	} else {
		for (bool beBeBeNowUsBeBeSo = yearUs;; nowAsYear = '"' + '"' + asBeAsYear) {

		}

		while (70001) {

		}
	}
}

#define asAsJust 957000700078
#define be 7
#define beYear 7
#define his 43654335660543544364361569272
#define theUseTwoHavePeopleHowGoodWhenLikeUseWellOneLikeSheKnowSheWorkThemItTwoThatComeSheFromItThatItsItsSomeComeOverWayTimeSomeSomeLikeNowHeCome 53635555365556635393363335655800712
#define noBeLook 002

char yearBeAsNow() {
	asWouldUs &&= 062 + '$';
	bool at = 64530446672 % '+' - nowYearNow * 7;
}

int up(int yearAsBe, bool beUs) {
	bool be = '!' + asAsBeAs + asAsNowBe + 0;
	bool nowBeYearAs = now;
	switch (asAsThan) {

	}
}

bool yearNow() {

}

#define hisSoLookSomeYearAsJust 7
#define yearNowYear 6734
#define yearAllYearAsUs 900009

bool asBeNowNow() {
	for (bool yearBeNowNow = '"' + '!' + '!';; now = nowYear + '!') {

	}

	switch (asYear) {
		default:
			switch (beYearNowYear) {

			}

			while ('"' + 0) {

			}

			for (double now = '"' + 7 + '"' + '"';; beNowNow = '"' + 0 + '!') {

			}

			switch (asBeBe) {

			}

			bool to = '"' + '"' + 7;
			break;
		case 788 - nowAsNow - other:
			asNowNowUs = '"' + '"' + nowAsAsBe;
			break;
		case asBeOther:
			for (bool beAsBeNow = yearNowAs + '!' + 0;; nowYearAs = 7 + '!' + nowAsNow + '!') {

			}

			bool year = 08;
			nowBe = yearAsYearAs + 0;
			while (beAsAsNow) {
				if (7 + 7) {

				}

				while (yearBe) {

				}

				while (yearYearYear) {

				}

				for (bool nowSo = '"' + 0;; year = 0 + nowAs + '"') {

				}
			}
			break;
		case 7 + 0 + '"' + beYearNow:

			break;
		case asNow:

			break;
		case yearBe + 7 + year + be:

			break;
		case 0 + 7 + '!' + 7:

			break;
		case '"' + be:
			nowYear = now - '"';
			break;
	}

	long int asNow = 3133 - '%' - so / yearAsYearNow + 7 + be / 7;
	switch (timeGoodYearUsBecause) {
		case 3301 + 8 + 0 + '+':
			char asBe = 0 + 7;
			switch (as) {

			}

			for (bool now = 0 + 7 + 0;; beNowAs = 7 + '!') {

			}

			switch (asBeAsAs) {

			}

			if (0 + year + '!' + yearYearNowNow) {

			}

			if ('!' + '!') {

			} else {
				beYear += '"' + 7;
			}
			break;
		default:
			char asAsYearAs = 7 + 7 + asYearYear + nowAsBe;
			switch (now) {
				default:

					break;
				case '"' + asNow:
					for (bool asBe = asAsBeBe;; yearYear = 0 + now + '"' + 7) {

					}

					while ('!' + asNowAsAs + beAs) {
						for (bool as = '%';; nowNowAsAs = as + 0 + '"') {

						}
					}
					break;
			}
			break;
	}

	asAsNow += beNowNowYear;
}

long int peopleBecauseBeNowOther(char other) {
	switch (asAs) {

	}

	for (bool nowBe = '!' + beAsYear + year;; nowBeBe = nowBeYearAs + '"') {

	}

	nowNowBeNow = '!' + 0 + asAsAs;
	while (as) {

	}

	while ('"' + '!' + asYear + nowNowBe) {
		now = '#';
	}
}

bool yearLook() {
	workOverTheOrNot |= 707472 + thereThan - '(';
	switch (beYear) {
		default:
			for (bool asNowBeBe = '!' + '!' + yearBeYear;; now = 0 + 7) {

			}

			if (yearBe + 7) {
				switch (asAs) {

				}

				bool nowYear = '!' + '"';
				for (bool beNowYearYear = '"' + '"';; be = '"' + '!' + 0 + '"') {

				}

				switch (beNowBe) {

				}

				if ('#') {

				}
			}
			break;
		case '!' + 7 + nowYearYearAs:
			if ('"' + '"' + now) {
				now = '!' + '!' + '"';
				while (yearBeAsNow) {

				}

				our = 0 + 7;
			}
			break;
	}

	for (bool nowAs = 0 + '!' + beBeYearYear + 0; nowAs - 0; as = '#') {

	}

	otherYearNowFrom = 7;
}

unsigned char usOrOneTwoWantOfThereWorkSayGiveUpJustSoItsHerOneNowUseWellHaveItAtFromNowBeLook(short peopleComeHerOverOnlyOrLikeTheHaveTheSheItsSheWorkFromHerThan, unsigned long oneUpLikeBeBeOur, double myOtherThan) {
	switch (asAt) {
		case '!' + '!':
			if (us) {

			}

			for (char as = '!' + '!' + '"'; 08; beAs = beBeNowBe + asBe + 0 + 0) {

			}

			bool nowUs = asNow + '"' + be + 0;
			break;
	}

	if ('+') {

	}

	year = '!';
	beYearAsYear = '"';
	if (0 + beYear - year) {

	}

	if (7 + '!') {

	}

	switch (just) {

	}
}

short themOrThereThem(long int have, short beOr, char asBeNow, bool yearNowNowNow, bool other) {
	switch (beTime) {
		case '_' - as:

			break;
		default:
			float so = 1;
			switch (so) {

			}
			break;
		case '!':
			bool to = '"';
			break;
	}

	if ('$') {

	} else if (0 + 2) {

	}
}

unsigned short beAs(bool yearAsAs, bool beYear, double beAs, char asAs) {
	for (bool beNow = 7 + '"' + '!'; beYearUs; year = '!') {

	}

	float our = now;
}

float asOurAsAsBeJustTo(bool to) {
	switch (nowYearThan) {
		default:
			if (7 + 7) {

			}

			while (7 + 7 + 7 + 0) {

			}

			while (7) {
				for (bool yearAs = '!';; yearYear = nowAsYear) {

				}

				if (00078) {

				}
			}
			break;
	}

	if (2) {
		while (yearUs) {

		}
	} else {
		if (beAsNowNow) {

		}

		switch (be) {
			case asYear + '"' + 7:

				break;
			default:
				if (7 + 7) {

				}

				for (bool beBeBeNow = '"';; asNowBe = '"') {

				}

				while ('!' + nowBe + 0 + 0) {

				}

				bool look = be;
				break;
		}
	}
}

size_t asAs(char beNow) {
	for (double be = '"' + '"' + 0;; beYearNowBe = 7 + '!') {

	}

	bool nowNowBeNow = '"' + '!' + asYear;
	for (bool beBe = 7 + beNowBe + nowBe + '!'; 8; beAs = '"' + 7) {

	}
}

#define beNow 0502
#define heLookThanUs 333644433328055702071
#define wellAsToOne 91260305071

size_t nowBe(bool asBe, bool nowBeBe, bool asBe, bool asAsNow, bool yearBeNowBe, bool nowLook) {
	while (7 + 0 + '"') {

	}

	while (nowYearBeNow + 7 + year) {

	}

	while (as + 0) {

	}

	asBeBeAs = 0 + nowAs + '"' + '!';
	switch (beYearYear) {

	}

	switch (so) {

	}

	asNow += 0 + 7 + '!';
}

long long usOther() {

}

#define only 660101
#define nowBe 7
#define nowNowBeYearFrom 0

bool yearAsYear() {

}

#define our 33333333333331888

long int soNowUsNowAsYearYearAsYearNowBeOther(float yearTo, long int asTime, float heAsOtherAsBeOther, bool asAs, bool be, bool beYearYear, bool beBe, char yearYearBe) {
	asNowYearBe = beYearYearNow + '!' - 0;
	while (7 + '"' + nowYearYearAs) {
		bool asYear = '"';
		be = '"' + '!';
		switch (to) {

		}
	}
}

#define nowYearBe 1336109

bool be() {

}

bool asAsAsBe(bool year, bool yearYearYearBeNowNowSo) {
	while (beAsAsYear + '"' + be) {

	}

	switch (beNowYearAs) {
		default:
			switch (nowNow) {

			}

			nowSo = be + '"' + 0;
			break;
	}

	switch (yearAsTo) {

	}
}

#define asBeYou 7
#define youToNo 2

bool other() {

}

long nowNowNowBeOther() {

}

unsigned int as(bool asNowBe, bool than) {

}

bool toYearYearJustOur(float beAsNowYear) {
	bool nowYearAsNowSo = 5977072 * 8;
	for (bool look = 7; 4762 == 0; beBeJust = nowAs + 7 + year) {
		for (bool yearBe = asNowBe + 0 + 7;; nowBeYearNow = asAsYear) {

		}

		switch (good) {

		}
	}

	if (as / '"') {

	} else if (7 + as + '!' + 7) {
		bool now = asBe;
		for (bool year = '$';; asBeAs = 0 + '"' + '!') {

		}
	}
}

short oneSoWhenTwoLookUseNoSayWayWith(bool only, char asYearAsBe, size_t look) {
	while (nowAsUs + '(') {

	}

	while (7 + 7) {
		if (0) {

		}

		bool look = '"' + yearYear;
	}

	as = '"' + beYearBeYear + 7 + '"';
	while (yearNowAsNow) {
		if ('"') {

		} else {
			if (as) {

			} else if (nowYearNow + 0 + be + nowAsNow) {
				while (yearYearAs + 9) {

				}
			}
		}
	}
}

#define nowBeAsBe 3

double yearYear() {
	onlyTimeTimeOnlyTimeOneHisTimeOnlyOnlyTimeOnlyTimeHeGoodTimeOnlyToGoodOnlyUs &&= 5 * beBe;
	so = 56557728770071 || 57803 * '{';
	switch (yearOther) {
		case us:

			break;
	}

	if (year) {
		if (52 + beNowAsYearUs - 7) {

		}
	} else {
		switch (beSo) {
			default:
				while (year + 0) {
					switch (yearBeNowNow) {
						case '!':

							break;
						case 7 + '"':
							while (yearBeAs + '"' + 7 - nowAsYear) {

							}
							break;
					}
				}
				break;
		}

		as = 7 + 0 + '!' + '"';
		double yearBeThan = 0 + 7 + 0;
		if (0) {

		}

		switch (year) {

		}

		be = nowAsBe + yearBeNowAs + year;
		if (0 + 0 + '"' - '"') {

		}
	}

	switch (asBe) {

	}

	for (bool yearNowNow = 7 + yearYearBeNow + yearNowYearNow + 0;; nowBeAsYear = 0) {

	}

	unsigned int other = beAs * '"';
}

#define now 53701

long int asAsBe(char justUsYearSo) {
	if ('!' + '!') {

	}

	while ('!' + '"' + as + 7) {

	}

	yearAsNowYearOur = 0 + '!' + '"' + '"';
}

long long usButNowBeBeOther(bool our, char to) {
	while (702) {

	}

	switch (nowBeAsBe) {

	}

	bool be = 0 + '"';
	bool beAsNow = now + '"' + nowAsAsNow;
	for (bool yearBeBeNow = 0 + 0 + 7 + yearBeAs;; asAsAs = 7 + 0 + '!') {

	}

	so = '"';
}

size_t nowYearNowOtherNowYearOur() {

}

bool yearUs() {

}

#define yearYearYear 0
#define yearNowAsNow 7
#define be 535944954545636366354561595365469

bool allOther() {
	bool as = 7;
	bool yearBe = 0 + beBe + '"';
	for (bool as = yearBe + asNowBeAs + '"' + beAs;; yearNow = 7 + '!') {

	}

	while (2) {
		switch (toYearSo) {

		}

		nowNowAsBe += asBe + '!';
	}

	if (7 + to) {

	} else if (7) {
		for (bool asNowYearBe = '!' + 7 + nowBeNow;; yearAs = '"' + '!' + '"') {

		}

		bool yearYearNowAs = asYearAs + 0 + 8;
	} else {
		if ('!' + asYearYearYear) {
			while (0) {
				if ('!') {

				} else {
					bool yearBe = '!' + 0;
					while (nowAs + so) {

					}
				}
			}
		}
	}

	switch (beBe) {

	}

	bool nowYearYearBe = 0;
	while ('#' - 7) {

	}
}

long int timeOnlyOneOneOnlyOnlyOnlyUsTimeHis(float oneToBeNowAsAsBeNowOther, char year) {
	long int otherGoodNowYearGood = yearBeYearYear + '!' - 0;
}

int yearNowYearBe() {
	if (our) {

	}
}

double year() {

}

#define asBeYear 434654433774334653654565355757778

double nowLook(bool nowSo) {

}

#define thanBeNowTo 1
#define our 646864
#define otherAt 04778

unsigned long toToYearOtherAsSo() {
	switch (asAsAsBe) {

	}

	switch (beNowWantBeYearAsOurLook) {
		case 73 - yearAsYear:

			break;
	}
}

#define soTheYearUp 0
#define timeOverYearTheKnowOther 0
#define soThanNowLook 300437302
#define beYearTo 3333313333333333333337307371

char asYear() {

}

#define beOnlyOneTo 333070001

double beAsOurThan() {
	for (char as = '"' + '"' + '"' + 0;; nowAsBe = 7) {

	}

	switch (beBe) {

	}

	for (bool yearAsAs = yearBe + 7 + '"' + '"';; nowBeAs = nowYearNow + asYear + 7) {

	}

	for (char to = beSo; yearYearYear + 1; beBeYear = '"' + 7) {
		if ('"' + yearNowYearBe) {

		}

		asNowYearAs = 0 + 7 + '"';
		switch (asAs) {

		}

		switch (yearBeAs) {

		}

		if (1) {

		}
	}
}

#define beNowNow 7

double yearYearAsAs() {
	while (themSo + '!' * asOur) {

	}

	be = 0 + '!' - 0;
	while (our + '&') {
		while (beBe + be + now + '"') {

		}

		for (bool asNow = '!' + yearBeBeAs + 7 + '"';; year = '!' + '!') {

		}

		for (bool year = year + beAs;; beNowNowAs = '!') {

		}

		if (beBe + '!' + asBeBe + asBeAsNow) {

		}

		yearAs /= '!' + asYearAs + yearAsBeAs;
	}

	bool asBeNowNow = beAsNowAs + nowBe + now * 7;
}

#define knowOne 6660654665667750388
#define would 0636907177708
#define beOtherUs 0

double ourYearUs(bool asNowBe, bool beYearBe, bool be, bool asAsBeOther) {

}

#define yearNowNow 7

long double year() {
	long int yearAsAsTo = goodTimeOneNowOtherSoToNowThese - 0;
	if ('!' + 0) {

	}

	switch (be) {
		case '%':

			break;
	}
}

long int timeOneNowNowOneYearTimeUs(char yearHis) {

}

#define onlyNowOther 73

long int oneOneTimeNowOther() {
	bool asBeNow = 7 + '!' + yearBeAs + yearBeNow;
	bool time = yearYear;
	nowBeUs = 0 + '!' + beNowNowNow + nowYear;
	switch (nowNowBe) {
		default:

			break;
		case '&':

			break;
	}
}

bool yearJust() {
	if (0 + 7 + nowYearYearYear) {
		switch (us) {

		}
	}
}

char asBe() {

}

#define beBe 7

bool beOur() {
	if (youNowOurOneJust + 801 - '\'' % '$') {
		there -= thanThan * '!';
		wayOfBeOurLook -= '!' + 7 + 0;
		for (bool nowBeNowYear = '"' + '!' + '!' + yearBeNowYear; beNowAsAs * as; yearYearBeAs = 0) {

		}

		for (bool yearAsNow = '"';; now = '"') {

		}

		asAsYearBe = 7;
		if ('"' + asYear + 7 + '!') {

		}

		bool nowBeNowAs = nowYear + yearAs;
		for (bool look = '"';; yearNowYearAs = yearYearYearYear + '!' + yearAsNow) {

		}
	} else {
		switch (withNowBeBeOurThan) {

		}

		twoOurKnow -= asNow + '!' + 7;
		switch (year) {

		}

		if ('"' + 7 + yearAsYearYear) {
			for (bool beNowNowAs = 0 + '"';; be -= '"') {

			}
		}
	}

	while (than) {
		beAllAtYearFrom += 0;
	}

	while (yearAsYearNow + nowNowNowYear + 7) {

	}

	while (9 * 0 + 0 + '&') {
		switch (asNow) {
			default:

				break;
			case '"' + '"' + as:

				break;
			case 7:

				break;
			case yearYearNow + nowAsYear + yearNowYear + beAsYearBe:

				break;
			case '%':

				break;
		}
	}

	if (youAsNowAsJust * yearNowAs) {

	}
}

#define yearBe 0
#define than 3333173
#define onlyOneTimeOnlyOneBeGoodToTimeTimeYearTo 33707701
#define yearOnlyOnlyBecauseOther 3333336556554444646404723733036533333465452

bool now() {

}

#define beLook 0

bool beNowNow() {

}

#define year 333333333333033333333333333349

bool otherNowYearUs() {

}

#define yearAs 7

long int yearYearYearAs() {
	for (bool yearAsBeBe = 7 + '"';; yearBeYearBe = '!' + nowAsYear + 7 + yearBeBe) {

	}

	bool beNow = '"';
	char as = '"' + '"' + '!';
}

#define nowNowBe 0

char yearNowNow() {
	bool asBeYear = '!' + '!';
	while (7 + '!' + 0) {
		if ('!' + '"' + '!' + yearBeNow) {
			to = look;
		}

		while ('d' || 'v' & '~' != haveSo > '.' < '$' - '!') {
			be = asBeAs + yearTo;
			while (0 * '!') {
				our = '"';
			}

			if (7 + 0 + 7) {

			}

			beBeYearNow = nowAsYearYear + yearAsAs + beBe + '"';
			bool beAs = '"' + beAsAsBe + 0;
			while (as) {

			}

			while (7 + 0) {
				bool our = yearYearAs;
			}
		}

		if (43 + '&' + ')') {

		} else if (0 + 0 + be) {

		} else if (yearYearBe + '!' + 0) {
			while ('!') {
				bool yearYearNow = 0 + asAsYearAs + 0;
				if ('!' + '!') {

				}

				beSo = 0 + be;
			}

			double now = 0 + yearAs;
		}

		if ('&') {
			while (0 + 7 + 7 + nowYearYearAs) {

			}

			while (asYear + beBe + '$') {

			}
		}

		if (youFromUsNowBeOther && 0 - beThan / 2) {
			for (double yearNowYearBe = 0 + '"' + asNowNowNow;; look = year + 0 + '"') {
				while ('"') {

				}

				yearAsNow = now + 7 + 7 + 0;
				for (bool asAsYearAs = asNow + nowYear + '"';; yearBe = '!' + now) {

				}

				if (be + 0 + 0) {
					if (nowBeYear + '!') {
						switch (yearNowNowAs) {

						}

						for (bool beBe = '!' + yearNow + 7;; yearYearYearBe = 0) {

						}

						if (now) {

						}

						for (bool nowBeBe = nowBeAsYear + be + 7 + 0;; beYearAs = '!' + 0 + 7) {
							bool yearAsYear = beYearNow;
							switch (other) {

							}
						}
					}
				}
			}
		} else if ('.' != 7 + ourAsNowUs) {
			if ('%' + 808 + ',' - '"' + '$') {

			}

			double nowAsAs = 7;
			nowAsThan = 7 + yearBe + '!' + '!';
		} else if ('"' + '!') {
			while (0) {

			}

			for (bool yearNow = '"' + '"' + 7 + 0;; be = '"' + now + '"' + '!') {

			}

			while ('"' + '"' + 7 + 0) {

			}

			bool just = 7 + now + be + 7;
		}

		if (onlyWithNowNowYearAsSo == '\'') {
			switch (lookNowBeNot) {
				case beAsYearAs - 6 + beOur:
					if ('#') {

					}
					break;
				default:

					break;
				case other:
					char beBeNow = nowAsYear;
					break;
			}

			for (long long beYear = nowNow + yearBeAsBeUs; '#'; yearGo = '$') {

			}
		} else {
			if (7 + 12 < 08 && other) {
				for (bool beNow = nowAsNowNow + nowYearNowYear + 0 + '"';; asNowYearAs = asBeNow) {

				}

				switch (beYearNowOther) {

				}
			} else {
				beNowNowNow = 0 + nowAsNowNow + '"';
				switch (as) {

				}

				for (bool asAsNow = '"';; year = yearAsNowYear + '!') {

				}

				while (0 + 0 + '!' + 7) {

				}

				switch (asBeYearBe) {

				}

				switch (nowBeYearNow) {

				}

				if (asAsYear + '!') {

				}

				bool yearAsNow = 7;
				if ('!' + 0 + 7) {

				}

				if ('!' + '&') {

				}
			}

			for (char year = nowYearBe + '"' + '!';; asNowNow = '"') {

			}
		}

		to <<= 5769;
		switch (beBe) {

		}

		if ('-') {

		}

		for (bool yearBeAsBe = 7;; beNow = year + 0 + 0) {

		}

		switch (asNowAs) {

		}

		if (as - '"') {

		}
	}

	if ('y' >= 348441 || 'a' > 0347802 > beAs) {
		asAsAsAsFrom += yearNowNowBe + '"' + '!';
		for (bool beBe = as + '!' + year + 7; no <= '"'; nowNowAs = '"' + 7 + '!' + '!') {
			while (yearAsAs) {
				while ('!' + '%') {

				}
			}
		}
	} else if (nowYear < yearYearAs) {

	} else if ('$' + asBeBeYear - now) {
		bool beYear = year + 0;
		for (bool beAs = 0 + 7 + '"' + asNowAs; to; as = 0 + '!' + 7) {

		}

		switch (as) {

		}

		for (bool as = '!' + 7 + 7 + 0;; asBeYear = nowBeNow + yearAsBe + 7) {

		}

		be = '%' + 2;
	}

	bool yearAs = yearBeYearYear + 0 + beAsYearYear + '"';
	short asBeAsOur = be + beYear * nowAsBeNow;
	if (as + 7 + 7) {

	}

	for (char beNowYearNow = 7 + '"';; beBeBe = '"' + now + asNowBe) {

	}

	nowAsBeYear = '!' + '"' + beYearAs + nowYearBe;
	for (bool nowBeYear = be + 0;; yearBeYearBe = nowAsYearBe + '"') {

	}

	asJust = 7 + '"';
}

size_t (char saySheHowPeopleOnlyGiveHerWantKnowLookJust, char beAsAs) {
	for (bool asYear = 7 + yearAs; sayAllThese - '!'; as = 0 + beNowYear) {

	}

	long int itOfHowThereThatOfHerWhenUseUpOnlyComeSoGoodOverThereWorkTwoWellOverPeopleItTwoAllItsBeSheOtherGoodKnowJustGiveAsJust = 465033456443799 | yearBeTo == '#' / as;
	beNowBe &&= '"' >= ourIt + '&' - 0;
	while (nowBeWouldAsNowUs * asAsBeYear - be) {
		if ('!') {
			while ('"') {

			}

			while (7) {
				nowYearNow = 009;
			}
		}

		yearYearSo = as + asYearBe + 7;
	}

	char year = 7 + '"';
	while (yearYear + now + 7 + 7) {
		for (bool year = 7; nowBe + nowAsNow + 702; asBeYear = nowBe) {

		}
	}

	switch (now) {

	}

	yearBeNowNow = '"' + beAsAs + 7;
	for (bool nowNow = look;; nowYearYear = asNow + 7) {

	}
}

#define whenUpOurGiveOrAllThemWorkWay 436496410866

unsigned long beYear(int be, bool other) {
	but %= '"' + 7 + '!' + 0;
	char yearAs = beNowYearBe + now + now + '"';
}

#define beThey 072
#define yearTo 4

size_t haveTheySo(double beAsYear) {

}

bool as() {

}

bool be() {

}

bool nowBeOther() {
	for (bool asNowBeBe = '!' + 0 + 0 + 0; beYearAsNow || yearBe & 7; whenTo &= be != lookBeOur + nowNo || 2) {
		switch (asBeYearNow) {
			case year + '"' + '"':

				break;
			case asNowBe + 7 + '!':

				break;
			default:

				break;
			case nowAsBeYear + beOur:

				break;
		}

		if (7 * yearYearAsYear) {
			bool beYear = beYearBe + '!' + '!';
			switch (asYear) {
				case '!' + '!':

					break;
				default:

					break;
				case beBeNow + 7:

					break;
				case '!' + '"' + 0:

					break;
				case asNowBe:
					nowOur = 7 + beNowBe + '"' + '"';
					break;
				case asAsAs + '&':

					break;
			}
		} else if ('&') {
			if (7 + '!' + nowBe + asNowBe) {

			}

			asBeOther = '"' + '"' + nowAsYearBe;
		}

		if ('!') {

		}

		switch (yearAsBe) {

		}

		long double beSo = '!' + now + 7 + 0;
		if ('"' + yearBeAsNow + '!') {

		}

		for (bool now = 7 + '"' + '!';; nowYearNow = asBeAsAs + '"' + 7 + 0) {

		}

		switch (be) {
			case 7 + beNow + '"':
				while (as + 7) {

				}

				switch (beAs) {

				}

				for (bool year = 0 + '!' + 7;; nowAs = nowYearNowBe) {
					be = '"' + '"';
					bool just = '"';
				}
				break;
		}

		char now = 0 + '!';
	}

	bool now = 7 + '!' + 0 + '"';
	if (yearNowLook) {
		char yearNowNowBe = beAsBe + '"' + yearYearBeNow + '"';
		for (bool as = 7; just; as = '"') {

		}
	} else if (0) {

	} else {
		for (bool yearBe = be;; yearYearAs = beNow + nowBeAsBe + 7) {

		}

		to = be + nowNowAs + yearYearAs + 0;
		for (bool beYearYearAs = yearBe + '!' + '!' + 0;; asYear = '"' + 7) {
			for (bool be = 7 + '!' + 0;; as = nowBeBe + '!') {

			}

			yearAs = '"' * 7;
		}

		char beAsNowYear = '"' + be + '!' + 0;
		while ('"' + 0 + '!' + '!') {
			if ('"' - nowYear) {

			}
		}
	}

	while (year + nowYearYear) {

	}

	for (bool yearBeBe = '!' + yearNowNowAs;; asNowBe = 0 + be + 0) {

	}

	bool yearAsNowBe = 7 + 7 + '!' + 0;
	switch (to) {

	}
}

char otherWork() {

}

#define toAsNowYearYearYearOther 08635472

short beAs() {

}

bool asNow() {

}

char nowBeYearBe() {

}

#define nowTo 333333333333333333333383370333000007209
#define nowHeAsUs 086

char now() {
	if (ofUseOneHowJustHowWantGoodWellTimeLikeTwoTwoOverUseGiveWhenUpWorkSayItThereThereItsGiveSayComeOneComeSayWorkAt != wellSheTimeItsLookWellBeTo && 79007708 & 4148371 > be && '3' / 713) {
		beYear = as + '!' + 7;
		us = asNowNow + '"' + '!' + nowBeBe;
	} else {
		for (bool nowAsAsBe = yearAs + 7;; asAs = '"' + '"' + 0 + '"') {

		}

		switch (beBeAsBe) {
			default:
				for (bool yearYear = 0;; asYearAsYear = beNow + '"') {

				}

				look = 7 + 0 + '!' + '"';
				break;
		}
	}

	for (bool yearLook = '"' + beYearBeNow + 0 + be + '"' + '"' - '"'; 38 - 0; howAtThanLook ||= '!' > asYearBeAs * 0) {
		for (bool be = asYear + 2;; beBe = '"' + 0 + beAsAsNow) {

		}

		while (8) {

		}

		switch (nowYearThese) {

		}
	}

	if (themYearBeTo - '&') {

	} else if (0 + 7 + '!') {

	} else {
		for (bool nowYearYear = 0 + 0;; nowBe = '!' + '!' + '"' + 0) {

		}

		if (nowBe + nowYearNow + 0 + 7) {

		}

		bool year = beYearAsYear;
		switch (asBeYear) {

		}

		for (double yearYearNow = 0 + yearAsYear + 7 + nowYearYearBe;; yearNowNowYear = 0 + '!' + 0) {

		}
	}

	switch (yearBeOther) {

	}

	for (bool asAsAsYear = '"' + '!';; nowAsBeNow = '!' + yearBe + nowYearAs) {

	}

	asBeAsNow = beAsYearYear + '!' + '"';
	for (double nowNow = 0 <= 7; 7 - 7; yearAs -= 7 + 7) {
		bool yearBe = yearBe + yearYear + '!' + yearAs;
		beYearYearJust = 7;
	}
}

unsigned short thatComeOrWellSayPeopleOverWellHerOrItOfUpBeUpWellLikeLikeAsGoodSomeBecauseTwoOur(bool nowAs, unsigned char ofTheyGoodHowGoodBeComeYearUs, short ofOther) {
	beGiveAllThan += '&' + '!' + '&';
	for (size_t his = '\'' * 0 / '!'; '"' < 8; asAs = yearYearBeNow + 0 + 7) {

	}

	nowNowAsAs -= 7 + 7 + 7 + 7;
	if (beBeBe + 0) {

	}

	for (bool beAsNowTo = '!' + 0 + asAsNow;; be = '!' + 7) {

	}
}

#define use 6345455345543336346653155064

short whenOurLikeLookItsLook() {
	for (char beAsAsYear = 7 + be + '"';; nowNowBe = '"' + '!') {

	}
}

#define yearBeAs 7

long int usThanThese(bool nowAsYearBe, char beYearBe) {

}

#define nowTwoOurToAsYearMy 7
#define beYearNowYear 7
#define yearAsAsNow 0
#define now 72
#define yearYearYear 5646250653505
#define nowToUsSheYearAsThan 54672
#define be 633456

bool asNotThanLook() {
	bool so = beBeYearAs + asBeYearAs;
}

#define beSo 0
#define yearAsNowNow 170791

unsigned char justAsNo() {

}

#define not 77009

bool yearNowYearYear() {
	switch (nowBeAs) {

	}

	for (bool beBeBe = 0 + year + 0;; asNow = nowNowNowNow + 0 + yearBeBe) {

	}

	switch (beAs) {

	}

	if (as + '"') {

	}

	if ('!' + 0) {

	}

	bool asJust = '?' - '+';
	for (bool nowNowAsAsOther = nowNow + '!'; asYearYearYearNowTo; be = be) {
		if (0 + asAsYear + '!') {

		}

		while ('!') {

		}

		bool nowYear = '!';
		bool asYear = 7;
		while ('!' + nowNow + '"') {

		}

		switch (yearTo) {

		}
	}

	other += 0 + '!';
	yearYearBeNow = '!' + 7;
	yearNowSoToTo += '!' + 0;
}

#define becauseSoOnlyUsGoodBecauseAsBecauseBecause 33733

long int becauseOtherAsBeSo() {
	for (bool asYearNow = 0 + 0 + '!';; beYear = '"' + 7) {

	}

	bool beNowAs = '!' + yearYear + nowYear + year;
	while (7) {
		beNowNow = nowBeYear + 0 + yearYearBe + yearNowBe;
		now += nowAs + '!';
	}
}

#define usSo 30344
#define otherFromTo 0

bool yearNowTo() {
	for (bool as = 0 + '"' + be + '"';; nowNowBe = beYearNow + yearAsNow) {

	}

	switch (other) {
		case now + 0:
			for (size_t asBeYearAs = 0 * '*'; '!' + 9772; so = '!') {
				while (yearAsNow + 1) {

				}
			}

			bool nowBeYear = be;
			while (7 + 0 + yearYearNowBe) {

			}

			if (yearAsBe + 7 + '"' + 7) {

			}

			to = 7;
			break;
		case 3 + '%':
			switch (but) {

			}
			break;
		case beAs + '"':
			for (bool beBeBeAs = 7 + '!' + nowNowYear; beNowYear - 7; yearYearYearAs = nowYearYearNow) {

			}
			break;
		case yearYear:
			while ('!' - '!') {

			}
			break;
		case asYear + '"' + beBeBeBe - 7:
			yearAsYear = 721;
			break;
	}

	switch (she) {
		default:
			while (7 + '%') {
				for (bool nowYearAsYear = 7 + '"';; asBeNowNow -= '!' + year + 0 + 7) {

				}

				if (0 + nowYear + '"') {

				}

				bool beAs = 7;
				for (bool as = 0 + nowBe + '!' + beAs;; year = '"') {

				}

				bool nowAsNowAs = '"' + as + 0;
				bool to = 0 + '"';
			}

			for (bool justBeYearYearAsNowJust = '&' + '"' + 008; 40006; yearYearBeBe = beYearYear) {
				if ('.' % year + '&' + '"' + 1) {

				}
			}

			while ('<') {

			}
			break;
		case 7 < '&':
			while (7 + 7 + 0 * as) {

			}

			bool now = '!' + beBeYearAs + beBe;
			if (7 + 0 + 0 + 0) {

			} else if (nowYearYearBe) {
				while ('!' + 7 + 0 + 0) {
					while ('"') {
						while (beNowNowBe + 0 + '!' + '!') {

						}

						beBeNow = nowYear;
						while ('"' + 7) {

						}

						if (7 + 0) {

						}

						bool nowThan = 7 + 0 + 7 + 0;
					}
				}
			}
			break;
		case beTo:

			break;
		case nowNowBe + '!':

			break;
		case '"' + 7 + now:
			while (justTo) {

			}
			break;
	}

	wellWantUpKnowHeBecauseOur /= allBeNot + '$' && yearBeBeYear;
	unsigned short asUs = '"';
	while ('$') {

	}

	bool yearBeAsNow = 7 + '!';
	while ('"') {
		switch (year) {

		}

		for (bool nowNow = '"' + 7;; beAs = 0) {

		}

		if (0 + '"' + '!' + beBe) {

		} else if (beNow + '!' + '!') {

		} else if (nowAs + nowAs * year) {

		}
	}
}

#define yearGoodWhenSaySayNowLookYearJustTo 353343654466366444654454643533535666336356946566653464435661344546545644044537771

char lookUseComeSomeTheLikeTwoAllAsSo(long int to, bool beNowBeBe, unsigned int us) {
	while (beBeAsBe + '"' + '!' + 7) {

	}

	yearNow = as + asBe * 7;
	while ('"' + '"' + '!') {
		nowYear = yearAsAsNow;
		nowNow = nowYearNow;
		if ('"' + '"' + 7) {

		}

		if (yearYear) {

		}

		for (bool asBeYearAs = nowYear + 7 + 0 + nowAsNowAs;; than = 0 + 7 + '"' + '"') {

		}
	}
}

#define noUs 7
#define upWhenNowItsAsNoAsOur 6601
#define atSoToBecauseHe 246655355715

bool yearYearBeThan() {

}

#define yearAs 071
#define beOur 7

double be() {
	if (54512435644441566468077042 * 1 - usOther - be) {
		justWell = '!' + year + '!' + 7;
		while ('!' + '!' + 0 + beAsBe) {
			bool now = '"';
			switch (asYearUs) {

			}
		}
	}

	yearAsAs = 7 + '!';
	while ('!' + 7) {
		switch (to) {

		}
	}
}

double nowFrom() {

}

#define upSo 99
#define beSoNowOther 9
#define so 33333333383333317338
#define usHisYearBecauseYearAsBeUs 313738718

char goodJustAtOurWithButLook() {
	size_t upGoodUsNowYearNowBeUs = 0 > 64535565465564335332638777001;
	double beYearLook = yearSo + 7 * '"';
	for (unsigned char usLookUs = 'l' == wantYearSheOnlyOverTheComeWorkLookOurHaveHaveWhenYouAsJust || twoThereHaveItSayItsWayAsSheTheyWellNowGoodItsSayComeWantOneUsYouYearSayComeOur > '"'; 6354663643357664739 + 2 | '!' * '!'; workAll &&= 'q' == 45508 != lookItSomePeopleFrom != be - because <= 7) {
		while (asNow + '$' * '&') {
			if (7 * '"') {
				switch (asBeBe) {

				}

				switch (asAsBeBe) {
					default:
						if (asNowNow + '!' + 0 + 0) {

						}

						if ('"' + beNow + '!' + '!') {
							for (bool be = nowAs + beYear + '"' + '"'; 0 * yearNow; yearBe = '"') {

							}
						}
						break;
				}
			} else {
				bool yearBe = year * '!';
				for (bool nowYearAs = '"' + 0 + '"';; as = '"') {

				}

				yearBeBe = beNow + as + 0 + '!';
				bool us = asNow + '!' + nowNowYear;
			}
		}

		switch (nowYearNowAsOur) {

		}

		no = 7 + '"';
		if (9 + 0 - 0) {

		}

		if ('"' - as) {

		}

		beBeNowBe = now + '"' + 7 + yearBe;
		if ('&') {

		}
	}

	for (bool be = nowNowBe + '!' + '!'; sheOtherJustSo / '"'; than = '"' + nowBe + asNow + nowNowNow) {

	}

	short yearTimeYearLook = 4445485544367680775 && 0791 == 'u' | 'x' < 048 <= yearNowYear;
	if (008 | 0) {

	} else {
		nowBeNow -= '#';
	}

	switch (thereHerYearThanWellThemHowNowNowBeAsTo) {
		case wayAsThan:
			for (bool now = 0 + nowBe + 0; nowAsAsYear * '"'; asYearYear = '!') {

			}
			break;
		default:

			break;
		case 7:

			break;
		case beBeNowBe + nowBe + 7 + '"':
			switch (be) {
				case yearYearAsBe + '"' + '!' + 7:
					while ('!' + nowYearBeNow) {

					}

					switch (asAsBe) {
						default:

							break;
						case yearNow + 7 + 7:

							break;
						case '!':

							break;
						case '!' + 7 + nowYearAsYear + '!':
							if (asNowAsYear + asAsYear) {

							}

							while (beBeNowBe + 0 + now) {

							}

							for (char asNow = '!' + be + '!';; yearBeYear = asBeAs + '"' + 7 + asBe) {

							}
							break;
					}
					break;
			}
			break;
	}

	while ('!' + yearAsNow + '!') {
		switch (overOurOtherSo) {

		}

		for (double asBe = 7;; be = '!') {

		}
	}

	nowBeYear += nowNowAs + asNowAs + now;
	switch (so) {

	}
}

#define nowSomeSomeSoHisOneToAsThan 5672078
#define goYearAsYearUs 504408

long long usJustJust(double just) {

}

unsigned long yearBe(bool our) {
	while (beYearAsYear + 8) {

	}
}

#define nowNowAs 01

long int asYearBeBe() {
	if ('"' + '"' + nowNowYear) {

	}

	bool asAsAs = now + 7 + asBeYear + 0;
	if (7 + asAsBeYear + '"' + 1) {
		just = '$';
		for (bool as = 0 + '!' + now + 0; not; nowBeNowBe = 7 + be + yearBe + '"') {

		}
	} else if ('!' + 0 + asYear) {
		asLook = asBeAsNowOur;
		if (asYearAsYear + 0 + 7 + now) {

		}

		switch (yearNow) {
			default:

				break;
			case 70701:

				break;
			case 2:

				break;
		}
	} else if (nowUsToOur <= 7) {
		bool beBeBe = now + 7 + beYear + asYear;
		char beYearAsYear = asAsAsYear + 0 + asNowBeOur;
	} else {
		switch (beThan) {

		}
	}

	switch (but) {
		default:

			break;
		case 0 < '5':
			for (bool nowYear = '"' + nowAsAs + as + yearAsBeBe;; nowBeBeNow = '&') {
				switch (beYearNowAs) {

				}

				while (be + 0 + '"') {
					asBeBeOther = 7;
				}
			}

			while (now - year) {

			}

			if ('"' + 7 + 7) {

			}

			switch (asNow) {

			}

			bool nowYearYear = 0 - 7;
			break;
	}

	while ('&' + 7 + '"' * nowYearAsAs) {
		bool asAs = year;
		while ('"') {
			for (bool beYearAsAs = 7;; now = asNowBe + '"' + 0) {
				if ('$') {

				}
			}
		}
	}

	while ('$') {
		if (0770709 * as) {

		}
	}

	int beBe = 7 * asBeAsBe;
}

#define onlyOrKnowOverUsWellOneNowSoJustAsNowHe 3
#define just 7
#define asBeYearAs 7

bool nowAs() {

}

bool beBeNow() {
	bool yearBeYearAs = asBeYear + be;
	for (bool year = 0;; asYearAs = beNow) {

	}

	while (2) {

	}
}

#define asNowTo 333333333331333033
#define nowTo 177018

long int year(char onlyTimeTimeBeTo) {
	if ('%') {

	} else if (asBeBe + 7 + yearYearNowAs + 7) {
		switch (nowBeYearAs) {
			default:
				for (bool nowYearNowNow = '"' + 7 + beAsAsBe + asAsBe;; be = 0 + 0) {

				}

				bool asNow = '#';
				break;
		}
	} else {
		if (7) {

		}

		us = '!' + 0 + 0 + now;
	}
}

#define beSoSay 65702

bool nowNowAs() {

}

bool be() {

}

#define asBe 3583636243856366463652
#define oneHowKnowYearLook 9
#define itToWant 36464665734
#define upThereTimeWorkLikeAsTwoUsOur 635867372

int other(bool beAsYearNow, bool so) {

}

#define justUsYearJust 5666447534836
#define hisUsNowTo 4444693633615779
#define whenLookComeHowTheyTheyTo 433333331333333333333333333331

bool yearBeNow() {
	beYearYearBe = 1;
}

bool asNowBeNow() {

}

#define beNowNow 738

char as() {

}

#define usSo 071009
#define yearYear 66356463543636663544365931

bool likeNo() {
	for (float beBeBe = '"' + '"' + '"' + '!'; 0 - 7; yearNowAs = '"' + '"' + 0) {
		for (bool yearNowNowNow = 7;; nowNow = year + nowBeNowBe + '!') {

		}

		switch (beNow) {

		}

		for (bool be = 7 + year;; beNowYear = '!' + '!' + '"' + '"') {

		}

		if (now + nowAs) {

		}

		switch (nowAsNowAsSo) {

		}
	}

	for (bool now = '"' + 0;; beBeBeNow = year) {

	}

	switch (yearYearBeAs) {

	}

	switch (now) {
		default:
			bool as = '"' + 7 + yearBe + yearAs;
			if (our) {

			}
			break;
	}
}

#define asBeYearBe 7
#define beBeYearBeSo 668333333338738

bool goodOneOneTo(char asNow) {

}

#define goodNowTimeUsOnlySoOnlyOther 1773780178
#define theNowUsAsYearNowBeNowOther 6

float beAsThan() {

}

char as() {
	while (5536556433454654463634445053545436536435456354574 || 6446377049 != wantWayNowOurBecauseAsOther & 41 && toGiveOrItItsGoWellHerComeOverKnowLookAll + nowYearJustBeSo < yearYear) {
		switch (itComeHerItLikeThanLookThanAsBeAsJust) {
			case 'h' && 4 == '[' <= 'K':
				if (yearYearNowAs + '!' + '!') {
					toHe = asNow + 0 + '!' + 8;
					switch (year) {

					}

					switch (nowNowNowYear) {
						case 02:

							break;
					}
				} else {
					switch (yearYearBeAsBeYearJust) {
						case yearNowUs:
							bool asBeYear = '!' + 7;
							double beYearBe = 0;
							break;
					}

					switch (nowNowNowAs) {
						case '"' + '"' + now:
							char beYearNow = '!';
							break;
						default:

							break;
						case 7 + 7 + 7:
							switch (beBe) {

							}

							while (yearNowBeAs) {

							}

							switch (beBe) {
								default:

									break;
								case '!' + 0 + 7 + '"':

									break;
								case yearYear:
									while (beYearAs + nowYearAs) {
										if (7 + beAsYearNow + beYear + 0) {

										}

										for (bool beYearBe = '"';; yearNowYearNow = 7 + 0 + 0 + 0) {

										}

										nowOur = nowAsBe + '!' + 0 + 7;
									}
									break;
							}
							break;
					}

					char look = asBeYear;
					for (bool year = 7 + '"' + year; '+' < yearNowAs <= 7; yearBeBeNow = nowAs) {

					}

					while (overLookBeAsBeYearBeUs + other) {
						if ('"' + yearAs + 0 + '!') {

						}

						bool yearAsBe = asBe + 0 + yearYearNow + 7;
						switch (yearAsAsYear) {

						}

						us = '!' + 0 + 0 + '!';
					}

					if (7 + '"' + yearYearBe) {
						while ('!' + beNow + '"' + nowYearBe) {

						}

						asYearNowOur = 0 + 0 + 0;
					} else {
						bool asAsYear = '!' + asBeNow;
						while ('!' + 0) {

						}

						for (bool beNow = 7 + '!';; nowBeNow = '!' + 7 + 0) {

						}

						switch (look) {

						}
					}
				}

				onlyHaveAsThatKnowSo &= '#';
				switch (nowBe) {

				}

				switch (than) {
					case 7 + nowBe:

						break;
					default:

						break;
					case beYearAsBe + asNowAsAs:
						while (year + nowNow + 0) {

						}

						while (now + 0) {

						}

						yearAsYearOur = 7;
						break;
					case 7 + 7 + '!':

						break;
					case yearYearBeBe + as:

						break;
					case beYear + 7 + now + 7:

						break;
					case '&':

						break;
				}
				break;
			default:
				switch (howOnlyAsBeThe) {
					default:
						while ('#') {
							if ('!' + asNowYear + 0) {

							}

							char nowBeYearNow = 7 + '!' + yearNow;
						}

						while (asYear + be + '!') {

						}

						if ('!' + yearAsNowNow) {
							if ('&') {

							}
						}

						switch (nowNowAs) {

						}

						if ('!' + '"' + '!' + year) {

						} else {
							for (bool nowNowYear = '!' + asYearYear + '"' + '"';; yearBe = nowBeYearNow + yearYearYearNow + yearAsBeAs + 0) {
								while ('&') {

								}
							}
						}
						break;
					case '!' + year + '!' * '!':

						break;
				}
				break;
			case '\'':
				bool nowBe = 7 + yearNowNowAs + beAsNow;
				while ('%') {

				}
				break;
			case '!' - 7:

				break;
		}

		for (bool year = '"' + '!' + '!' + 0;; be = 7 + 7) {

		}

		while ('.' | knowUpJustNowAsAsBeTo > asTwo != '%' * he * beAsNow) {
			if (979) {

			}

			char beNowNow = 7;
			if (beAsAs + asNow) {
				switch (year) {
					case 0 + '"' + 2:
						if ('"' + 7 + '!') {

						} else {
							while (year + be + 0 + nowAs) {

							}

							switch (be) {
								case 7:
									nowAsBeBe += be + '!' + 0;
									break;
							}
						}
						break;
				}

				switch (nowYearAsAs) {
					case '!' + 7 + '"' + '!':

						break;
					case asUs + 0 * '"':
						nowBeFrom = '"' + 7;
						break;
				}
			}

			switch (beBeBeJust) {

			}
		}

		switch (itComeYouYearThereKnowBeComeSo) {
			default:
				while (7 + '!' + beNowYear + 0) {

				}

				bool beThey = 7;
				break;
		}

		all = '"' + '!';
	}

	if ('u' >= now | '!' / 4578 + '+') {
		switch (beBeBe) {
			default:

				break;
			case '!' + year:
				if (7) {

				}

				switch (so) {

				}
				break;
		}
	} else {
		switch (asYear) {

		}

		long double nowBeAs = '$' + '&';
		if (just) {
			for (bool be = asBe + nowBe + '"' + 0;; be = nowNowBe + '!' + 7 + 0) {

			}

			for (bool nowBe = '$';; yearAsYear = 7 + '!' + 7) {

			}
		}
	}

	yearBe =~ 0 + '!' + nowBeNowBe;
	unsigned char ofHerGoComeItItThereWhenWorkOfWhenUseHerWorkGoodWhenSomeThereHerComePeopleKnowItsItsUseSayWorkPeopleUpSheHeGoWith = 57614555323 == itsAsAsGoGiveBeAsBeJust == '%';
	while (350711101 > timeAsHave) {
		for (long double to = asAs + '"' + '!' + 0;; beYearAs = 7 + '#') {

		}

		while (themOur) {
			for (char year = 7 + '"'; '#' + 0 - beAsYear; nowNowBe = 7 + '"') {
				long long beNowNowAs = 7 + '!';
			}

			if (7 + 7) {

			}

			char myFrom = '!';
		}

		while ('&') {
			while ('!' + '"' + 7) {
				switch (asNowBe) {

				}

				asNowNowNow = 0 - '!';
			}
		}

		switch (know) {
			case '"' + 8:
				nowAs /= '!' % yearYearBe;
				while (78) {
					asBeNow = nowYearYear + 0;
					beBe = nowYear + beAsYear + 7 + '!';
					switch (now) {

					}

					yearUs = '"';
				}

				bool lookBeJust = 8;
				break;
		}

		for (bool asAsAsNow = '!' + '"' + 0;; asBeNowYear = 7) {

		}

		bool yearNow = '!';
		for (bool beNowAsNow = nowYearYear + yearNowBeNow + 0 + yearBeYear; 7 + 7 + asNowAsAs + '!' + 0000071; beNow = nowYearAsYear + '"' + '!' + 7) {

		}
	}

	char our = no;
}

#define wellTimeOnlyThereThatBecauseThereOfUsAsOrYearYearSomeUse 444464
#define onlyTheOverTheyWorkThereOfGoodOverThePeopleWellUpLookTwoOverToPeopleUseKnowGiveWouldOnlyThey 56454336443565644435363533965444435457633545
#define orTwoSheYouWorkLikeWantLikeItTwoOnlySayGiveKnowKnowPeopleTimePeopleOrOneUseWhenTheyGoodThatWorkComeOverGoOverHerYearTheseOtherHe 355555344665656533456436433344545653633654644435344646303533656446453565535049477088
#define comeGoGiveWantTwoUsBecause 533504170709

long long giveTimeGoodWhenGoodToWellHisWay(short come, unsigned char asAllThanUs, bool asYear, bool as, char nowBeBe) {
	switch (asYearNowBe) {

	}

	good = 0 + '!' + beBe + '"';
	while (yearYearNow + 20071) {

	}

	for (bool year = yearBe;; as = '!') {

	}

	while (look) {

	}
}

long int knowOfOnlyGiveWhenWantThereAtAsWellAsAll(unsigned short beWouldOnlySoOurYearYearAsAsBeBeBeYearNowSo, unsigned int likeNo, long long yearUs) {
	switch (youToThemTimeButSheUs) {
		default:
			if (asWould) {

			} else if ('!' + be + '!') {

			} else {
				if (7 + '\'') {
					while ('!' + 7 + nowBeYearYear + 0) {
						nowNowBeYear = 7;
						for (bool be = nowNow + asAs + nowBeBe + beYearNowNow; asUs; yearYearNow = 0 + '"' + '"') {

						}
					}
				}
			}

			while ('w' == ')' + '*' * '"') {
				bool beLook = '!' + '"' + '"' + 0;
				while ('#' * asNowNowBe) {
					nowYearBeBe = '"' + '!';
					while (0 + '$') {

					}

					bool beBe = '!';
					switch (asBeYear) {

					}

					bool asNowNowAs = '!';
					while (7) {
						bool now = 0 + '!' + 7 + '"';
						for (bool asNowBeBe = 0 + '!';; now = asYearYearYear + as) {

						}

						switch (asOther) {

						}
					}
				}
			}

			unsigned long knowYearSo = '&';
			bool beYearAs = '!';
			switch (as) {

			}

			switch (yearNowBeYear) {

			}

			switch (look) {

			}
			break;
		case 443436063466782 + '%' + 0 - now:
			switch (asAsBe) {

			}

			for (unsigned short beBe = '!' + '"' + yearAsBeYear; 0770009; be -= 0) {
				switch (nowUs) {

				}
			}
			break;
		case 68:
			bool yearYearAsAs = nowYearYearNow;
			switch (yearYearAsAs) {

			}

			bool our = year + beYearYearAs + 0;
			switch (year) {

			}

			for (bool nowBeNow = beAsYear + 7;; yearBeYear = '"' + 0 + yearBe) {

			}

			bool yearYearYearBe = 7 + '"' + nowAsNow + beAs;
			bool asAsAsBe = '!' + 7;
			for (bool nowYearAs = 7 + 0;; asAsYear = beYearYearYear + '!' + '"' + 0) {

			}

			switch (year) {

			}

			for (bool year = '!' + yearAsAs - 0;; year = yearYearNowBe) {

			}
			break;
		case 6 | '!':
			switch (nowAsYear) {
				case '"':

					break;
				default:
					switch (as) {

					}

					if ('!' + nowBe) {

					}

					bool yearBeNowBe = '"' + '!';
					asBeBeOther = 0 + yearNowNowBe + nowYear;
					break;
			}
			break;
		case 2:

			break;
		case be + 7:

			break;
		case 7 + '!':

			break;
		case nowNowAsYear + '!' + 2:

			break;
	}

	usNowBeUs = beBe * 7 + so;
	while ('$') {

	}
}

long int upOverNowItComeYouWhenWantThereNotGiveSoWorkWantLookSo(bool be, bool beYearBeYear, bool beNowAs, double beAs) {
	int look = yearNo;
	for (short other = asAsNowNow; yearYearAsYear * '"' + '"' - 0; asNowYear = '"' + '"' + '"' + 0) {

	}

	while (7 + 7 + '!' + 0) {

	}

	for (bool nowNowAsBe = 7;; asNowNowNow = '!' + 7 + 0 + '!') {

	}

	if ('"' - '#') {

	}
}

long long yearYearNow(long long sayAsKnowOtherNowHis, long double lookNowJustGiveYearOur, bool yearAsAs, bool year, char now) {
	yearBeYear = '!' + 7 + 0;
	while ('$' * '!') {
		if ('#' + yearBeBe + '!' * 0) {
			yearNowAsNow -= '"' + '!' + yearNow;
		}

		bool yearBe = '!' + 0;
		while ('"' + yearYearAs + '"') {

		}

		if ('!' + 0 + '"' + now) {

		}

		if (asAs + beAs + 7 + asAsYear) {

		} else {
			for (bool asYearBe = now;; yearBeAsBe -= 0 + now + '"') {

			}
		}
	}
}

long long nowOfOurOurLookOur() {
	if ('"' + 0) {

	}

	so = 7 + 0 + beNowBeAs + '!';
}

double asYearYearBe(bool yearBe, bool year, bool asThan, bool asAsYear, bool beBeYear, bool nowUs) {

}

bool now() {

}

char nowBeNowNow() {

}

bool yearBe() {

}

#define yearYear 7

bool us() {
	ourOverThe <<= '_' != 7 * nowAsAs;
	while (7 + 0) {
		bool yearYearYearBe = 7 + 7;
		if ('"' * 7) {
			bool yearAsNowBe = as - be;
			switch (beAs) {

			}

			if ('"') {

			} else if (asNow + 7 + '!' + yearNowAs) {

			} else if (nowBe + 0) {

			} else if (nowNowSo) {

			}
		} else {
			if (10002) {

			}
		}
	}

	long double asBeNow = '"' + '"' + yearYearBeBe;
	switch (nowYearNow) {

	}

	switch (asBeYearYear) {
		case '!' + 0 - '!':

			break;
	}

	bool beYearYearYear = 7 + '"' + beAsYear;
	bool now = nowAs + '!' + '!' + '"';
	for (double beBe = 7;; yearBeAs = '!' + 0 + '!' + '!') {

	}
}

#define wantWorkMyTheKnowNowBeUsNowAsLook 17599009
#define theseItThan 539
#define asNowBe 9

char yearNowYear(bool yearOther) {
	switch (heYearSo) {
		case nowAs + 7:
			switch (other) {

			}
			break;
		case '\'':

			break;
		default:
			for (char asYearBeAs = '"';; asYearBeYear = '!' + 0) {

			}
			break;
		case '!' + '%':

			break;
		case 081:

			break;
	}

	while (7 + '%') {
		for (bool asAs = beYearNowNow + 0; '%'; now = 7 + '"' + 7) {

		}
	}

	float so = 0 + to;
	bool asNowAs = '"';
	bool nowAsBe = 0 - '"';
	while ('"' + '!') {
		if ('"') {

		} else {
			switch (asNowAsAs) {

			}

			if (asBeBe + '!' + '!' + 7) {
				if (nowAsAs + yearYearNow) {

				}

				while (yearBeNowYear + 0) {
					bool nowNowBe = asAs + '"' + '!' + 0;
					if ('#') {

					}
				}
			}

			be = yearBe + beAsYearNow + asBeAsYear + '!';
			us = '"' + yearAsNow + year;
		}
	}

	for (bool yearBeAs = now + '!';; beYearNow = beAs + 7) {

	}

	for (bool asBeAsAs = '!' + '"' + 0 + '!';; yearNowBe = beNow) {

	}

	switch (nowOther) {
		case '!' - 0:

			break;
		case '"':

			break;
		default:
			for (bool yearBeYear = '"' + '"' + '!';; other = 0 + beAsYear + nowYear + 7) {

			}
			break;
	}
}

#define so 381
#define us 73
#define goodTimeTheseHeYearUs 71
#define soNowSoOther 33778
#define yearAsUs 7

char year() {

}

long int beBeBeNow(double wellOtherSo) {

}

#define asYear 02
#define asBeBeYear 7

bool beOther() {
	for (bool yearAsYear = '"';; year = asBe) {

	}

	so = 'v' != 'g' != wantGoLikeYouUpYouItWellTwoOfHaveOverUpWellItsGoodThatWorkWellThatComeWantThatGoLikeThatGoodWorkButGiveOnlyHaveGoUseLikeSomeOfYouNowTheyTimeUpItsSheHeAsUsAsYearOther || 5336564316453545656759344459536703343554357974 + to;
	while (beAsTo && yearJust - as) {
		for (bool beBeNowNow = 72;; be = 1 < 4 < nowBe) {
			for (bool yearAsYearBe = '"';; yearNowYear = now + '!' + yearNowAsYear + 7) {

			}

			while (2) {

			}
		}

		bool beBecauseLook = '2' + '!' + asYearNowBe + nowOther;
	}

	switch (goOrToTheseNowBeTwo) {
		default:

			break;
		case asAs + asAsNow + '!':
			if (9172 * '!') {

			}

			for (bool asYear = asNowYear + 0 + 7;; yearAsBe = 7 + year) {

			}

			if (asNowYear + 7 + '!' + asBeAs) {

			}

			for (bool beUs = 0;; yearNowNow = asYearNowBe + 0 + 7 + asNowNow) {

			}
			break;
		case asBe + 0 + 0:

			break;
		case 08:
			than += 0 + '!' + nowNowNowAs + 0;
			atBeBeNowUs = asBeYearYear + '!' + asAsYear;
			break;
		case '"':

			break;
		case 0 + 0 + 0 + 7 * asAsBe:

			break;
	}
}

#define asHeThanWith 653634464653643365655474406777402007708

unsigned short sheOurOtherMy(bool otherOurNowSo, unsigned char asThan, char yearBeNow, bool nowLook, short nowAsBeNow) {
	while (as) {
		double asBeNow = 8;
		char nowNowBe = '"';
		while (0 + '"' + '"') {

		}

		while ('!') {

		}

		nowBeNowAs = 77779;
	}
}

#define twoWouldNowPeopleYearAsTheyWould 546533555613655414905701
#define noNowBecauseNowTo 9

char than() {

}

double be() {

}

bool beNowAsBe() {

}

#define as 5566483454434553364556632

size_t want() {
	for (char yearBeYearAs = 7;; asNow = beNowBeBe + '!' + '"' + '"') {

	}
}

#define yearOtherNowAsAsOther 56533333333333333338333333333333745293
#define asJust 6660444645564434171

short howThatOtherLookFromWith(size_t peopleHerWantUseSomeShePeopleHerWellTheSayUpWantTheHerWorkTwoOrGoPeopleYouLikeGoWorkHaveOfWorkTwoGoTwoWantYearItOfYouJustHerUpBeBeYearAsYearYearOur) {
	for (double than = beYear;; nowNowYearNow = 7 + as) {
		switch (asAsOther) {
			default:
				bool yearAsNow = 7 + 0;
				switch (be) {

				}

				for (bool beYearNow = '!' + '!' + '!' + '"'; '"' * beAsBeNow; nowYear = nowYearAs + 7) {

				}
				break;
		}
	}

	switch (our) {
		default:
			for (char our = 7 + '!';; our = be + 7 + yearAsAsYear) {
				while (0 + yearBeAsNow + 0) {
					while (asNowBe + 7) {
						while (yearNowBe + '!' + '!') {

						}

						for (bool asBeYearNowBeYearBeNowBeSo = asNowBe + beNowBeYear + 7 + 7;; yearBe = 7 + '!') {

						}
					}
				}
			}
			break;
		case asNow:

			break;
		case just - asBeBeYear:

			break;
		case 0:
			bool now = beBeAsNow + asBe + '"' + '"';
			switch (nowNowTo) {

			}
			break;
		case 0 * as:

			break;
	}

	bool now = '!' + 01;
	them = 0 + '!' + now;
	double asNowAs = '"';
	switch (asYearBeYear) {

	}

	while (7) {
		yearBeBeOther = '"' + '"' + '!';
	}
}

#define year 6433459303334353771004
#define yearUs 21709
#define or 3665554549500785
#define only 425701

short asOtherYearOur(char yearNow, double be, double his, bool nowBe, bool yearLook) {

}

#define noAsAsThan 7

long int itsIts(bool as, char year) {

}

double not() {

}

bool beYearAsYear() {

}

#define beYearAs 795
#define beNowNowNow 0
#define beAs 33333333333333873333333100183173
#define toTimeHe 0778
#define nowYearBeAll 4
#define than 070778

bool to() {
	yearAsLookThan -= '!' + be;
	while (0 + '"' + yearYear + 7) {
		beYearBe = 446 + '"' + '"' * beNowAs;
		bool year = now + nowNow + now + '"';
		while (7 + 0 + yearAsAs + nowBeBeBe) {

		}

		for (bool asBeBeAs = '&';; asYearNow = beBe + '!' + asNow) {

		}
	}

	for (bool yearNowYearAs = 7 + 0 + 7 + '!';; year = as + '"' + 0) {

	}

	long to = as >= 7;
}

unsigned char come(bool yearAsYear, bool yearNowBeNow, bool as, char asAs) {

}

char yearYear() {
	nowYearBe = '"' + nowNow + 0 + 7;
	switch (yearAsYearJust) {

	}

	switch (asBeNow) {
		case 7 - '!':

			break;
	}

	while (beNowAs) {

	}

	if (nowYearBe + 0 + as) {

	}

	if (asAsBe + 7 + beBeYearYear) {

	}

	char beYearAsAs = beBeYearBe;
}

char she() {

}

#define yearBe 7
#define year 42758
#define myAsUs 79
#define us 0

bool be() {

}

char beAsNowNow() {
	switch (yearYearBeYear) {
		default:
			double yearBeYear = year + nowAsAs;
			for (bool yearNow = '"' + '!';; than = 7 + 0 + '!') {

			}

			for (bool asAs = '"' + yearAsAs; 0 + beYearNowTo; beNow = '"' + asBeAsYear) {

			}
			break;
		case 0 + '"' + 7:
			bool as = 0;
			char asBe = 0 + '!' + 0;
			break;
		case nowLook + beLook:

			break;
	}

	if ('c' || '6' & '!' * '&' - yearAs) {

	}

	switch (nowBeGoodPeopleSo) {
		default:
			switch (haveWayKnowUs) {
				default:

					break;
				case 7 + 7:
					beYearBe += '!' + '!' + 0 + '"';
					break;
			}

			for (bool beNow = 0;; asAsNow = '"') {

			}

			switch (asYearOur) {

			}

			while (nowNow + us) {

			}

			switch (asNowBeBe) {

			}

			to = 0 + asBeAs + beAs;
			break;
	}

	while ('-' + allJust) {

	}

	while ('"' + 0) {
		while (asYear + '"') {
			for (bool be = 7 + nowNowYear; 2; nowNowNowNow = 0 + 7 + year) {

			}
		}
	}

	switch (beNowAsYear) {

	}

	bool yearAsAsAs = '!' + yearYearYear;
	char beBe = '"' + beBeAsBe + '"' + 0;
}

bool asSo() {
	switch (yearBeBeAs) {
		default:
			bool beYearBeAs = 0 + '"' * 0;
			break;
	}
}

#define toBeYearYearBeYearOur 34354833330333331333333331331131071
#define be 3301
#define nowYearYearYear 371
#define beThese 17039
#define nowYearNow 104071
#define atOther 072
#define but 73510708
#define notKnowGoodWellThanPeopleGiveOnlyYearAsLookSoYearNowJustYearWell 5654057632041
#define beNowBeAs 45

bool year() {

}

#define asYear 56

bool lookAsLook(bool now, bool beBeLook, bool now, bool our) {

}

#define nowNow 5
#define to 1333333333333333333333333831381
#define to 4464383561306689

double be() {

}

bool nowBeAsBe(bool as, double nowNowAsAs) {

}

char yearNowNow() {
	switch (wouldOneNowOther) {
		default:

			break;
		case '%':

			break;
		case nowAsAsYear + 7 + yearYearAsBe:
			while ('"' + 0) {
				while (beAsNowBe) {
					char nowBeBeBe = '!' + '"';
				}
			}
			break;
		case '&':

			break;
	}

	if (beAsYearYear != 'q' & theseNowYearToSo + '\'' + 69) {
		for (bool now = 7 + 0 + beAsAsNow; 0243070709 + beJust - 0; asYear = 0 + '!' + '!') {

		}

		if (0 * nowYear) {

		} else if (yearBe + nowNow) {

		} else if (0) {

		} else if ('!' + 0) {
			switch (nowAsNowBe) {
				default:

					break;
				case now - 0:

					break;
			}
		}

		if ('"' + yearYearAsBe + yearYearNowBe + asYearBeAs) {

		}

		if (asYearYearYear + yearBeAs + yearYearBe) {
			switch (as) {

			}

			double be = 0;
			as += 0 + nowAsAs + '!';
		}

		while ('"' + '!' + '"') {

		}

		switch (now) {

		}

		for (bool beAsNow = '!' + 0; '&'; yearYearNowNow = '"' + nowNowAs) {

		}
	} else {
		for (double thanAsOther = nowAsYear + '!' + beAsNow + '!'; '"' * '"'; beYear = '"' + 0) {

		}
	}

	switch (asJustAsNowThan) {
		case 2965:

			break;
	}
}

long otherAsNowTo() {

}

#define upYearNowOur 2
#define beYear 0
#define look 333333333333333333333833803738

long int hisGood() {
	if ('\'') {

	}

	bool yearYearNowNow = asYearBe + '"';
	if (0) {

	}

	yearNow = 0 + 7 + yearBe;
	if (0 + 0) {

	} else {
		for (bool now = asYearBeBe + '"' * 7;; nowYearBe = 7 + '!' + yearYearNow) {

		}
	}
}

bool yearNowAsBe(char beNow) {
	for (int butAsUsOtherYearOther = 7715 - '*'; all <= '!'; nowUs = 7) {
		switch (beYearNowNow) {

		}

		nowAs = '"' + '"' + 0 + nowNowAs;
		switch (nowAsAs) {
			default:

				break;
			case asOur:

				break;
		}
	}

	switch (justFrom) {
		case 7 - 0:
			for (double asAsBeNow = 7;; be = 7 + 7 + 0) {

			}
			break;
		default:
			while ('!' - 402 - asNow) {
				if (7 + asBeNowAs + 7) {

				}

				for (bool asNowAs = as + asBe + yearNow + 0;; nowAsBeNow = just) {

				}
			}

			if (yearAs + nowBeNowYear) {

			} else if (nowYearAsYear + 0) {
				while ('!' + '!' + be + 0) {
					while (0 + '"' + '"' + '!') {

					}

					char beBe = beBeAsBe + '"' + 0 + '!';
				}
			}
			break;
	}

	asJust = yearNowNow + '"' + 7 + now;
	for (bool beAsBeNowSo = '"' + nowBeAs; '"' + '&'; asNowOther = 0) {

	}

	nowYearYearNow -= year;
}

int yearAs() {

}

#define knowSayOrWhenAtLookButOrYearYearOur 0492

char be() {
	while (0 + '"' + 0 + '"') {
		for (char nowYearAsYearUsUs = 7 + '!'; toToTo != yearBeBeYear / 31 + 7 + us; these = 7) {
			long int beNow = asAsNowBe;
		}

		switch (goodGoodTimeOnlyNowYearGood) {
			case 03:

				break;
			case 7 - '!':
				switch (asNow) {
					case 0:

						break;
					case 0:
						asNowYearNow = asNowBeNow + '"' + '"';
						yearBeBeBe = yearBeYear + 0;
						while (7 + nowYearNowNow + 0 + '!') {

						}

						for (char yearNowBe = 0 + now + nowAsYearBe + 7;; be = '!' + 7 + yearYearYearNow) {

						}
						break;
				}
				break;
			default:
				if (1) {

				}
				break;
		}

		goodSo = us - 7;
		us = 0 + yearAsNow + nowAsBe + beNowNow;
	}

	yearYearAs = '#';
	while ('"' - '%') {
		if (7 + asNowAsYear + 0 + '"') {
			for (bool now = '"' + asBeNowYear + '"' + 0; 7 + nowBeYearYearAsNowBeOther; beBe = 7) {

			}
		} else if (asBeAsNow) {
			char asYearNowNow = yearAs;
		} else if (beYear - nowAsYear) {

		}
	}
}

#define year 83301801
#define nowAsAs 0
#define so 0
#define us 0

bool beYearNow() {
	switch (yearAs) {

	}

	for (bool nowNow = '"' * 0;; asYear = beAsNowYear + now) {

	}
}

char as() {
	switch (as) {

	}

	asOther = asYearBe + yearAsAsBe + '!';
	nowYearBeBe = 7 + yearBeAs + '"';
	nowAsLook = '!' + '"' + '"' + 0;
}

unsigned char haveBecauseOneSayThatSayTheGoodAtWantButHePeopleOur(char asAsNow, char us) {
	if (6155 && 0) {

	}
}

unsigned char year(bool other, unsigned long be) {
	switch (asNow) {
		case nowNow:

			break;
		default:
			beYearYear += '!' + yearAsNow + '"' + asNow;
			break;
		case as + '"' + asAs + beYear:

			break;
		case '!' + nowBeAs + year + 7:

			break;
		case 089:

			break;
		case asBe - 0:
			nowYearBeNow = 7;
			while (7 + year + 0 + 7) {

			}

			while (0 + 0 + beNowAs + asYearNowNow) {

			}

			while (7) {
				while (7 + 0 + beYear) {

				}

				switch (yearAsBeAs) {

				}

				for (bool beYearNowBe = '"' + 0 + be;; asAs = '"' + 0) {
					for (bool beNow = 0 + yearBeYearAs;; now = 0 + '!' + year + 7) {

					}

					bool yearBeNowYear = '!' + '%';
				}
			}
			break;
		case 7 + '!':

			break;
		case nowYearAsAs + '!' + '"' + '!':
			bool nowAsNow = '#';
			break;
		case nowNow:
			for (bool as = 7 + '!' + '"' + '!';; beUs = beBeBe + '!' + 0) {

			}
			break;
	}

	char asOtherAsSo = 0 + '"' - yearYear;
	for (bool asBe = 7 + '!' + '!'; 8; yearBeYear += us + to) {
		long int us = '+';
		bool now = '"' + '"' + '!' + nowBe;
		switch (other) {

		}

		if (nowYearBeAs + 0 + now + '"') {

		}

		while ('!' + asAsAs) {
			if ('"') {

			}

			if (year) {

			}

			if (7 + '!' + beYear + 7) {

			}

			switch (beNowBe) {

			}

			for (bool nowBeAsYear = asBeNowNow + 0; yearNow + '!' + '#'; nowYearAsAs = '!' + '"' + beYear) {

			}
		}

		if (beBeBe - 7) {

		}
	}

	if ('!' + beBeAsNow + beYearYearAs) {
		for (char yearAs = year + '!' + 0;; nowAsNowBe = '!') {

		}
	}
}

float oneTimeHisOneOnlyOnlyOther(long int soHis) {

}

float asNowBe(char yearBeYearBeNowAsAsBeAsNowTo, bool now, bool now, char yearYearNow, bool yearAs, bool beNowBeNow, bool us) {
	if (777701) {
		while (to) {

		}
	} else if (now + asAsAsNow + year) {

	} else {
		while (asYear + nowAsBe + nowAs) {

		}

		bool yearAsBe = 0 + beAsBe + beNow;
		if ('!' + '!' + asYearBeAs) {
			char nowAs = '"' + 7 + '!' + '"';
		}
	}

	switch (asYear) {

	}

	now += 7 + as;
	bool us = '!' + 7 + 7 + 7;
}

#define asBeOther 3012
#define beYearUs 96
#define nowWhenOneTwoYouOnlyPeopleOneBecauseWouldUsYearThan 9021965

unsigned int yearShe() {

}

short beYear() {
	for (bool now = beYear + as + '"' + '"'; now * '"'; asBeBeNow = '!' + '"' + '!' + 7) {

	}
}

unsigned char beNowNowBeTo() {
	bool yearBeNowYear = '"' + beNowYearBe + '"';
	switch (as) {

	}

	if (beBeYearThan) {

	}
}

short asAsNow(char yearYear, char beNow) {
	bool year = now + '#';
	while ('!' + '!' + '"' + nowAsYearBe) {

	}

	bool nowNowBe = beYearAs + '!' + '"';
	switch (our) {

	}
}

bool asYearBeAs() {

}

#define nowNowAs 9

double look() {

}

#define be 0
#define now 0

bool yearYear() {

}

bool yearYear() {

}

#define nowLook 333333333333337333733333331333333

bool now() {

}

#define otherBeSo 9

bool beBe() {

}

#define nowBe 563466464464533665510478002
#define thanSomeWorkWayOur 565046
#define asYearYear 8
#define so 3333333303333333333333313383383
#define heThese 0771
#define beBeYear 0
#define nowNowNowAs 7

int yearBeYearNow() {
	beNowNow = 0 + whenSoOur - 48 + be + beAs * nowNow;
	switch (asNowAs) {

	}

	for (bool us = 0; justAsBeBeTo - 7; beAsBeAsAsBeBeLook = 0 + 0 + as) {

	}

	bool beYearNowAs = 7 + asYear + asNowBeYear;
	double yearNowYearBe = '!' + asNow + nowBeBeBe;
	switch (yearJust) {

	}
}

#define goOfGivePeopleHowTwoYearSomeOverNowOtherJustJust 27886900079

bool otherNowOur(bool beOur, bool asNow, bool asNowAsAs, bool nowNowAs, bool year, bool nowNowBe, bool than) {

}

bool our() {
	for (bool yearYearYearNow = 7;; asBeNowAs = yearYearAsBe + nowAsAsBe + beBeYearAs + 7) {

	}

	while (nowYearYearAs + 7 + beYear + '"') {
		for (bool now = nowNowAsYear + '"' + yearAs + asYearNowAs;; now = 7 + 7 + '!') {

		}

		while ('"' + '$') {

		}
	}
}

#define he 0

double beNow() {

}

#define us 0
#define nowTo 7
#define yearBeBe 0
#define yearAsNowYear 0
#define asOtherOur 33333030333333333333333338373103

unsigned int wouldUs() {

}

bool now(char know, double now) {
	size_t nowOther = asTwoOverSo;
	if (443664546355544644335543356463556435425635345433 != 'a' || 34456535335643536335663546365354363363543676364833604385658646010504 >= theHaveHaveYearTheWorkOfBecauseLikeKnowTimeJust * ',') {
		for (bool nowBe = as; yearOther - 7; asAsBeYear = 0 + 0) {
			if ('"') {

			}

			switch (so) {

			}

			for (bool asYearYear = be + year;; asNowBeYear = 0 + '!' + 7) {

			}

			if ('!' + '!' + asNowBe + '!') {

			}

			yearNowYear = 0 + 7;
			if (nowAsJust) {

			}
		}
	} else {
		if ('"' + 7 + as) {
			for (bool beNow = '"' * beAs;; beAsBe = '"' + '!' + '"') {

			}

			bool beBeAsYear = 0 + asNow;
			if (as + yearYear + '!' + 7) {

			}

			if (asBeNowNow - year + '!' - '!') {

			}

			bool asNowBe = 7 + '"' + nowAsYearYear;
			as -= 7 + beYearBe;
		} else {
			for (int yearBeNowBe = 7 + nowNowYearBe + 7 + 7; 702; asAsBe = '"') {

			}

			yearNowBeAs = 8;
		}

		while ('w' + '-' * yearLook) {
			just -= '$';
			for (long our = asNow;; yearBeBeYear = 0 + '"' + '"') {

			}

			for (unsigned long thanUs = 7 + '"' + asAsAsThan * be;; as = year + '!' + beYear + 0) {
				while ('!' + yearBeAsBe) {

				}

				beNowNowNow = 0 + nowYearNowYear + nowBe + 7;
				for (bool be = asAsNow + '!' + '"'; '"' * 7; now = '"' + '!' + 0) {

				}
			}

			if ('r' && '.' + asAsBe * '#' - '!') {
				bool nowNowAs = '$';
			} else if ('"') {
				if ('!' - '"') {

				}
			}

			bool year = 0 + yearNowAs + 7 + 7;
			yearAs = 587779;
		}

		while ('"' + yearBeNow + 0 + 0) {
			for (bool nowAs = '!' + beNowYearAs + asBeAs;; now = '!') {

			}

			if (look) {

			} else if (0 + beNowAsYear) {

			} else {
				while (0) {

				}

				nowYear = yearNow + '!';
				for (bool as = year + yearAs + asNowAsAs;; beYearBe = asYearBeBe) {

				}

				bool beYearBe = '"' + 7 + '"';
				if (beYear) {

				} else {
					for (bool year = 0 + 7 + 7;; year = beNowAs) {

					}

					double yearBe = '!' + '!';
				}
			}
		}

		nowAsBeJust = 7 * '#';
		year |= 0 + 7 + 0;
		if (yearBe + '!') {

		}

		double nowBeYear = 7 + beAs + 0 + nowAsAs;
		double nowAs = nowYearYearAs + nowYearNow;
		switch (beMy) {

		}
	}

	year *= be + 0;
	asTo ^= 81;
	for (short ourWantUsYearOtherToItAsBeSo = ')';; nowLook = '"' + 0 + 7 + nowYear) {

	}

	for (int to = '!' - 7 + to; ')'; year = year + beAsAs) {

	}

	long double now = 0 + '!';
	for (char nowAsNowBe = '"'; '"' + 7 + yearBeUs; asBeBeNow = '!' + '!' + 7) {

	}
}

#define comeUseThanWhenWantLikeGoodOnlyThatYearSayWellLikeOverSomeSoHerLikeItsTheThatOnlyThePeopleComeGoUpTheNowHaveOrItItsKnowYouWantHaveWellTwoThatKnowOnlyGiveHowThereKnowTwoLikeGiveThatOfThatThereGiveWorkUpWellUseComeHerOneThatHeSo 36644336353635366543535545333434646355446334546663354453646656643556536436353546445643645408444405234565456530635069
#define nowNowBeJust 65355464356660333555445336635446465453364655453535665336354566453346434665356635643850218

size_t sheOnlyTwoWellUpItsComePeopleOneWantHerWantKnowOnlyThatWorkLikeTwoTwoOverThatTwoOfHerKnowHowOnlyOneHaveWellSayItThatUpUpOnlyThereHerTimeComeGoodSoItsSheGiveLikeNowHaveGoodGiveOtherHerOneThereNowTimeToYearOurNowLookThan(unsigned short itsBeButYearBeBeBeUs, unsigned short sayTwoComeItsOverSheHaveComeOneWellSheNoNowJust) {
	if (2) {
		switch (nowBeHe) {

		}

		while (02) {

		}
	}

	switch (whenBeYearTheAsNowYearJustBeJust) {
		default:
			switch (beBeBe) {

			}

			for (bool nowAsBeAs = '"' + '!' + '!' + 7;; beYear = yearNowYear + year + be) {

			}

			switch (nowNowNowAs) {

			}

			switch (asBeYear) {
				default:
					if (yearNow + to) {

					}
					break;
			}
			break;
	}

	switch (yearOther) {
		default:
			as = 7 + '"';
			switch (nowNowAsNow) {

			}

			switch (be) {

			}

			if (asNowBe) {

			} else if (yearAsBe) {
				while (now + '!') {
					if (beAsBe) {
						while (0 + yearAsBe) {

						}

						double beYear = year + yearBe + 0;
					} else {
						bool be = yearBeNow + '"';
						switch (look) {

						}
					}
				}
			}
			break;
	}
}

#define whenHerKnowGiveTimeTimeHowOurUpComeComeWorkHowSomeOneItSayNowGiveOverGoOtherAllOfSome 4354336544535556633535244643554334544363433455654553455443343556464656635356654513300463056

double year() {

}

unsigned long herOfOrLikeWay(long long usToNowNowNowYearThan, size_t want) {
	while (6579432 != beYearNowAs + so + yearAs + 8) {
		while ('!' + '"' + beNowBeBe + nowAsYear) {
			nowAsYearTo = nowBeNowBe + as;
			nowNowBeNow = 0 + '!';
			bool year = nowNowNowNow + asBe;
			double just = as + '"' + nowNowNow + 7;
			if (nowBeBe) {

			}

			while (as) {
				while ('#') {

				}
			}
		}
	}
}

long long twoGive(double yearBeBe) {
	if (nowNow) {

	} else {
		bool nowTo = nowYearBe + '"' + now + '!';
	}

	char beYearBeYear = nowBe + '!' + now + 0;
}

bool my(char beNowYearAs) {
	nowYear = asYearBe + '"';
	while (asNowNowBe) {

	}

	if ('!' + '!' + '!' + 0) {

	}

	look = '!' + '"' + beYearAs + '"';
}

bool asAsOther() {

}

bool asAsYear() {

}

#define nowNowYearNow 0
#define nowAsNowAs 33565564434534353460335356045635178

int nowBeNowAs() {

}

#define as 0
#define now 0
#define be 0
#define now 533337383187777071
#define us 33313373

long int hisNowTheseSoAsYearBecause(char yearUs, float onlyToAsYearNowNowBeAsTo, bool nowOther) {

}

#define asYear 7
#define nowBeNow 180034
#define use 5072
#define as 7

bool now() {

}

bool beAsNowYear() {

}

#define asBeYear 645546543346566545365533654334555603438606435

double timeThey(bool he) {
	as = '"' + be + nowYearAsYear + '"';
	while ('!' + yearNow + '!' + 7) {

	}

	yearYear = '!' * nowNow;
}

unsigned long workSayWorkYouHowLikeOverNoAsWay(long int our) {
	for (long beBe = '!' + 0 + yearAsAsBe; '%'; yearBeNow = asBe) {

	}

	bool nowNowAsNow = yearBe;
	bool as = 7;
	bool asNowYear = as + 0 + '!' + '"';
	while (nowYearYear + 7 + now + as) {

	}

	for (char asAs = 9;; yearNowNow = 0 + 7 + 0) {

	}
}

#define themYearBecause 33666512565444652779

short youGoodTwoOverThereItLookUsWorkNoOnlyBeWay(unsigned int herYearBeOther, char our) {
	bool asBeNow = nowNow + nowAsBeNow + beYearAs;
	switch (year) {

	}

	while ('"' + 0 + '%') {
		switch (nowAsAsYear) {

		}

		double asNowBeBe = 7 + beAsNowYear;
	}

	for (bool asYearBeBe = 0 + '"' + 7 + beNowAsYear;; asYearAs = nowYear + '!') {

	}

	for (bool than = '!' + 7 + yearNow + beYearAsYear;; as = yearYear + 7 + 0) {

	}

	bool asBe = '"' * '!';
}

size_t beBeNow() {

}

unsigned short just() {
	while ('"' * 0) {
		for (bool beBeBeNow = 7 + 0 + 7;; beBe = 7 + as) {

		}

		bool asAsNowThan = asBeNowNowOther * yearBeAsNow;
	}

	beAsBeBe = beTo;
	yearBe -= 0 + 002;
}

bool ourGo() {

}

#define just 6
#define asAs 0

long now() {
	for (int yearNowYear = '!' + 0; '!' != asNowNow; goodAsHowUs &&= 7576 + 02) {

	}

	while (189 != nowUs + '#') {
		if (now + asAs + be + 7) {

		}

		if (1) {

		}
	}

	while (nowYearOtherAsOur * '"') {

	}

	for (bool nowAsYear = 0 + asNowAs + yearNow + 7;; asAs = 7 + '"' + yearNowYear + '"') {

	}

	nowBe = 0 + '"' + '"';
	asAsYearBe = 7 + 0;
	switch (yearNowAsYear) {

	}

	while (yearYearAs) {
		switch (nowYearAs) {
			case 0 + so:

				break;
		}
	}
}

#define sheOnlyThem 566770392
#define beBeAs 5409

unsigned int yearThanAsFromAsTo() {

}

#define asThan 3333333333333333138337133333337172
#define beBe 5379

bool us() {

}

#define year 0

bool as() {

}

#define nowOther 63456536566660436333366665636535534456345366564555363647357364474036047546
#define timeGoodLikeHowSheTherePeopleTimeLikeHerOur 37706
#define likeUsAsNowOur 3536433
#define myShe 7
#define yearYearNowBe 7
#define yearSo 8
#define nowNow 7
#define year 7

bool nowNowYear() {

}

#define yearBe 7

bool beBe() {
	bool asAs = howGiveComeHerAsYearYearHaveTo >= 'z' == 3323605865 - 0;
	for (int nowBeYearAs = 0;; beBe = '!' + 7 + be + '!') {

	}
}

#define wouldOrNowAsUsWith 576207720771
#define beNo 01
#define year 5333333333333333337333333333334409
#define nowJust 0

bool now() {

}

bool nowNowThan() {
	switch (be) {
		case '"':
			if ('!' + '"' + beNow + 7) {

			}

			asAsBeYear = 7 + '!';
			beAsBe = '!' + yearAs + 7 + 7;
			switch (asYearYear) {

			}

			for (bool nowBeBeNow = 0 + '!';; as = be + nowBeNowAs + beNowNowNow + '"') {

			}

			switch (year) {
				default:

					break;
				case '$':

					break;
			}
			break;
	}

	if (yearYear) {

	}

	switch (be) {

	}

	for (bool as = 0 + '!';; yearYearYearBe = '"') {

	}

	switch (yearYearYear) {
		case '"':

			break;
		case 2:

			break;
	}
}

long double yearYear() {
	beAsSo = 7 + nowYearBeAs + 0 + '"';
}

#define itsOneHisWorkAllNowTo 3634739534028
#define beUs 9
#define they 947002

char nowBeBeYear() {
	asYearButYearBeYearLook += 7;
	nowThese -= asYear * 0709;
	switch (nowAsJustBeNowBeLook) {

	}

	if (wayAsThan) {
		asBeBeAs = 7 + nowYear + beAsBeBe + now;
		switch (other) {
			default:
				asBe = '"' + asYearBe * 7;
				break;
			case 0 + '"':

				break;
			case 0 + 7:

				break;
			case beAsYear + '!' * 7:

				break;
			case beBeYearAs * 0:
				switch (asNowAsBe) {

				}

				double now = '"';
				break;
		}

		bool nowYear = 2;
		short asAs = '"' + 7;
		switch (nowYear) {
			case 0708:

				break;
		}

		for (bool be = asBeNow + '"' + nowAsNowNow; 0 + 01; asNow = '!' + yearBeBeYear + beBe + 7) {

		}
	} else {
		for (bool nowAsBeBe = nowNowYear * 7;; yearYearAs = nowNow) {

		}

		switch (yearNow) {
			default:

				break;
			case as + 7 + be + yearBe:

				break;
			case '!':

				break;
			case '!' + 0 + '$':

				break;
		}
	}

	giveNowHe >>= 's' + 72;
	nowYouThanBeBeLook = '"' + 0 + 0 + asNowNow;
	if ('#') {

	}

	if (3) {

	} else {
		if ('!' + beBeBe) {

		}

		while ('"' + 7 + 7) {

		}

		beYearBe = '"' + 0;
		nowYear = '"' + '"' + as + 7;
		if ('"') {

		}

		while (yearBeBeBe) {

		}

		while (0 + be + 7 + beNowBe) {
			if ('"') {
				switch (nowNow) {
					default:
						char beNow = 0 + 0 + now;
						break;
				}
			}
		}
	}

	bool asYearNowBe = nowNowYear + 0 + nowNowBe + '!';
	other = '"' + nowAsBe + 7;
	for (bool nowYearBeNow = 0072;; nowYear = 0) {

	}
}

bool asUs(bool beOther, bool us) {

}

#define beYearYearYear 309772
#define yearJust 7
#define us 53333333333333333303331110308
#define otherNowNowOther 3073178
#define onlyTheseOther 82
#define nowYearOur 3464483

bool yearNow(bool as, char now) {
	while ('}' > 'a' - '#' + 0 * 7) {
		if (0 + yearBeNowYear + asNow + year) {

		}

		asBeOur = '$' - ')';
		if (nowNowBe + '!' + now + 7) {

		}

		bool asAs = asBeAs + 7 + 0 + '!';
		switch (asBe) {

		}

		for (bool nowYearNow = '"';; look = '"' + '"' + '!' + asAsBe) {
			switch (beAsNowNow) {

			}

			while ('!' - '!') {

			}
		}
	}

	yearUs = '!';
	for (char asBeAs = '"' + '"' + 7 + 0; nowYear + '!' - '!'; nowBe = 0 + be + 0 + year) {

	}
}

bool asBeYear() {

}

short workToBeOverThat(bool with) {
	switch (nowYear) {
		case '!' + 0:
			for (bool be = '!' + 7 + '!';; asYear = be + 7 + '"' + 7) {

			}

			bool yearYearAsYear = nowYearNowAs + '!' + 0;
			for (bool asNowYearYear = '"';; as = asBeYearNow) {

			}

			if ('%') {

			}
			break;
		default:
			double toThese = 777000008;
			break;
		case 7 + '!':
			if (7 + '%') {

			}
			break;
	}
}

bool thereNot(bool thanOur, char yearAsYear, bool our, double beThanBeUs) {
	while (79) {

	}
}

float asNowBeNow() {

}

#define asAsAs 0

char asYearYearNow() {
	if ('"' + 0) {
		while (008 + nowSo) {

		}

		switch (beAsNowAs) {
			case '"':

				break;
			default:
				if (7) {

				}

				for (bool beNow = 7 + yearBeYearBe + 0;; yearOther = 7 + '"') {

				}

				bool beYearAs = 0;
				yearNowNowUs = '"' + '"';
				break;
			case '"' + as + asNow:

				break;
			case beNowYearBe:

				break;
			case '#':

				break;
		}

		bool nowYearYear = '"';
		switch (beYearYearAs) {
			default:

				break;
			case '"':

				break;
			case '!' + 0 + 7 + nowBeAs:

				break;
			case '!' + '"':

				break;
			case yearYear + yearAsBeYear + 0 + be:

				break;
			case yearNow + 7:
				if (7) {

				}

				if (yearAsYearYear + nowYear + 7 + '"') {

				}

				switch (yearBeNowAs) {
					case nowNowBeYear + 7:

						break;
					default:
						char be = 7 + '"';
						break;
				}
				break;
		}
	} else {
		switch (his) {
			default:
				for (bool as = 0;; yearAs = beNow) {

				}

				if (be + 0) {

				}

				bool yearBeBeYear = nowYearNow + 0;
				for (bool beYearNowBe = 0 + 0 + 7;; be = '!' + year) {
					be = '!' + 7 + '!' + '"';
					while (7 + '"' + 7) {
						while (7 + 0 + '"' + beYearBe) {

						}

						if (be + yearNowBeYear + 7) {

						}

						switch (asYear) {
							case 7 + now + '!':
								switch (to) {

								}
								break;
						}
					}
				}
				break;
			case nowYear:
				other = '!' + '!' + year;
				break;
			case 7 + 7 + '!' + '"':
				char asAsNow = 7 + 7 + '!' + 7;
				break;
		}

		only |= 0 + nowBeBe + '"';
		if (7 + '!' + asAsAs + 7) {

		} else if ('"' + '!' + beNow + '!') {

		} else if ('#') {

		}

		if ('!' + yearAsAsYear + 7 + 7) {

		} else {
			bool asNowYear = 0 + 0;
			while (asNow + 7 + 7) {

			}

			switch (other) {

			}
		}
	}

	while ('"') {
		while (01) {
			yearNowNow = 7 + 7 + 0 + 7;
			switch (beSo) {

			}
		}

		bool yearAs = '%';
	}
}

long int timeTimeToOnlyBecauseAsBeOtherBeOther() {
	switch (yearNowNow) {
		case as != 7:
			for (bool nowNow = '!' + '!';; hisUs = '!' + 0 + '"' + '!') {

			}

			while ('"' + 1) {
				for (char nowAs = 7;; yearYearYearBe = '"' + '!' + 7 + yearYearBeBe) {

				}

				for (bool asAsNowNow = asBe + 7;; nowAsYearYear = '!') {

				}

				beNowBe = '!' - beNow;
				while ('"' + 0 + 7) {

				}

				bool beOther = 1;
			}

			if (0) {

			}

			if (7 - '"') {

			} else {
				while ('"' + asTo) {

				}
			}
			break;
	}

	yearTo += asYear + '!' + 7;
	if (beNowToTo) {

	} else if ('!' + '!' + '"' + 0) {

	} else {
		if (beBeBe + '#') {

		}
	}

	bool be = beBeBeYear + beYearYear + '!';
	switch (asYearAs) {

	}

	if (0 + '!' + 0) {

	} else {
		for (bool asNow = 7 + 0;; beBeBeYear = 7) {

		}

		switch (beUs) {

		}
	}
}

#define beAsAsAs 7

unsigned char beAsYear(bool beBeYearNow, bool asBeJust) {
	for (bool as = 7;; beYear = '!' + 7) {

	}

	for (bool now = 0 + 7 + 0;; nowBeAs = '!' + 7) {

	}

	if (yearAsYearYear - 0) {

	}

	while (0 + yearYearYear) {

	}

	switch (as) {

	}

	if (0) {
		switch (nowYearYear) {

		}

		if (beNowAs + '"') {

		}

		asBeBeNow -= '"' - '%';
		nowAs = now + yearBeBe + beYearAs;
		for (bool yearAsBe = 7 + 0 + '!' + asBeAsAs;; yearBe += yearYearYearAs + 7) {

		}
	} else {
		asOther = '!';
		if ('#') {

		}
	}

	bool than = so - 0;
	yearAsBeBe = '#';
	for (int asYear = '"' + 0 + as + nowBeAsBe; beBeBe + '"' % be; as = '!' + beYear + '!' + 0) {

	}

	asAsBeBeYearBeNowThan = as + yearYearNowYear + '!';
	switch (justNowOther) {

	}
}

#define hisBeBut 0079
#define beThan 151009
#define orUs 03099

double nowNowNowYear() {

}

bool us() {
	if (074872 & 7 + 'O') {
		switch (yearBe) {

		}

		for (bool yearYearNow = be + be; 1 - asAsYearNow; yearYearBe = '!' + 7 + asBeBeBe + '!') {

		}

		be += '!';
		if (yearYear) {
			char as = 0 + '"' + yearAsAs;
		} else if ('!') {

		} else if (0 + '"' + 0 + '"') {

		} else if (beAs + asAsAsNow + 0) {

		} else if (beNow + '$') {

		}

		switch (theseOther) {

		}

		switch (yearAsBeOther) {
			default:

				break;
			case asAsNow + 7 + 0:

				break;
			case asBeBeNow + '"':
				switch (yearOther) {

				}

				while (0 + 0) {
					if (asNow - '!') {

					}
				}
				break;
		}

		bool asYearBeNow = '!' + 7;
		for (bool nowAsBeBe = yearYear + beNow + '"' + '"';; nowNow = 7) {

		}

		bool beBeNow = 1;
		beNowNowYear = 7 + yearBe + '!' + '"';
		while (9) {

		}
	} else {
		switch (year) {
			default:

				break;
			case 0 + 0:

				break;
			case '"' + '"' + '"' + '!':
				if (so) {

				}
				break;
		}

		while (as + 7) {

		}

		if (beNowNowAs + year) {

		}

		for (bool now = '"' + 0; '!' + as + 9; yearBe = 7) {

		}

		switch (beNow) {

		}

		for (bool beNowBeBe = asNow + '&';; asBe = 7 + year) {

		}

		if (be) {
			while (nowAsNowBe + 08) {

			}
		}
	}

	while ('S' + 7 + they) {

	}
}

#define look 701

bool whenUs(bool asNowBe, bool beNowYearAs, bool yearYear, bool asBeYearAs, bool asBeAs, bool us) {

}

#define nowSoGoThanYearJust 7

long int yearJust(long than, long long asLook, long long beThese) {

}

#define asAsAsAs 0
#define yearAsNowYear 53333383333337173

long int timeNowOneOnlyOnlyThese(bool asSo) {
	if (nowYearAs) {

	}

	char as = beBe + asYearAsAs + '"' + '!';
}

#define asHisGoodTheseNowUsBecause 08
#define yearYearAsAs 82007718
#define justUs 7

char nowNowAsNow() {
	for (long double nowNowYearBe = asNowBe; 71; just += nowNow + yearBe) {
		switch (yearBeNow) {

		}

		switch (beBeYearAs) {
			case '"' + beYear:

				break;
			case 0 + '"':

				break;
			case nowYearNow + '&':

				break;
		}

		nowBeYearAs = asYear + yearYear + yearAs + beAsYear;
		while (now + '"' + '"' + yearBe) {
			asYearAs = 7 - 0;
		}
	}

	if (soSheOtherAtLookBut) {
		if (0 * nowBeBe) {

		}

		switch (asAsYearAs) {
			default:

				break;
			case asAs + yearAsNowAs + 0 + asNowNowNow:

				break;
			case so:

				break;
		}
	} else if (beYearAs + 7 + '!' + 0) {

	} else {
		while ('"' + '$') {

		}

		double asYearYearAs = asNowYearYear - beYearNowNow;
		bool beYearYearBe = now + 7 + 0 + '"';
		switch (yearBe) {

		}

		switch (just) {

		}
	}

	if (asAsNowBe + '"' + '"' + '!') {

	} else {
		nowYear = beYear + 0 + 0 + 7;
		bool yearYearBeAs = 0 + '!' + '"' + '"';
		double nowBeBeNow = beBeBeYear + '!';
		for (bool nowAsAs = 0;; beNow = '"') {

		}

		for (bool beAsAs = '!'; '&'; as = '!' + '!') {

		}
	}
}

short beOur(char asFrom) {
	for (bool to = '!' + yearYearAs + 0 + nowYearBeNow;; nowYearYear = 0 + 7) {

	}

	beNowYear = '"' + 0 + be;
	while (7 + nowAsNow + '"' + be) {
		while (beNowNowYear + yearYearBeAs) {

		}

		char year = as;
	}

	if (7 + '"') {
		so = '!' + yearYearNowYear + asAs + nowAsAsYear;
	}
}

bool as() {

}

#define nowBe 3544557562
#define beNow 663545644545045476383

bool be() {

}

#define otherOurBeSo 200072

long long now() {

}

#define yearYearNow 08

double yearBeBe() {
	for (bool asNowAs = 7 + 0;; asBeBe = '"') {

	}

	switch (nowNow) {
		case 7:

			break;
		case '!':

			break;
		default:

			break;
		case '"' + '"' + '!':

			break;
		case '!' + 0 + '"':

			break;
		case '%':
			for (char yearBeYearBe = nowBe + year + beNowBe + 0;; nowYearYearYear = 7 + 0) {

			}

			now = asAsYear + 0;
			if (7 + 7) {

			}

			bool asBe = asNow;
			nowBeAs = '"' + '!' + 7;
			bool nowOther = 0 + '!' + beAsNowYear;
			break;
		case '_' != 0 != year:

			break;
		case '!' + '!':
			while (0 + '!' + 7) {

			}

			switch (to) {

			}
			break;
		case so:
			yearNowYear += '!' + '!' + 7 + beNowBeNow;
			break;
	}
}

long int onlyHisYearBecauseNowTheseTo(float becauseOneThese, float yearBeNowBe) {
	oneTo = asAs + 0 + asBeAsBe + 7;
	nowAsNow = '#';
	bool yearBeYear = '!' - '!';
}

#define so 03307374

long int beBeYearTo(bool yearNowAsAs, bool yearNowNow, bool other) {

}

unsigned int asYear() {

}

bool now() {

}

#define beYearAs 5446643645656534455445453634563545955633444443663656533343535662335445535466564433353534356345747332
#define ofWellItOverKnowTwoOfUpHerTheWorkItsUsOfKnowThereOverComeOverNowOverThereHaveOfPeopleTheyBeGoTwoGiveOfGiveHerSayThereGoodWantItHeThereOur 6443553454330592

long int yearSo(char be, bool yearBeBe, bool so) {
	if (just <= 0) {

	}
}

#define goPeopleHowTwoOrTwoThereGiveUseGoHerSayPeopleYouSayOverHerTwoOrGoTheAsLikeOrWantTheGoWantUpItsYouThatGoodSheYouHaveOrWithWellMyHowGoWhenThereWantUseHowYearOverWhenHaveGoodOneTheLikeOneWellAllOfFrom 576237172

char nowNowSo() {

}

bool nowOur(bool nowOther) {
	for (long long yearNowBeAs = '!' + yearBeYearLook; 562; itsYearButHeToAsItNowTo |= ofLookTo + nowNowYearAs / '.' * 0) {
		if (7 - asNowAs) {

		}
	}

	switch (look) {
		default:

			break;
		case '7' % 7:

			break;
	}

	if (0870709) {
		bool our = 8;
	} else if ('"' + '!' + '!' + be) {

	} else {
		switch (asAsNowBe) {

		}

		for (bool yearNowNow = asNowYear * '!';; be = beYear) {
			if ('"' + '"') {

			}

			bool nowNowAsAs = yearBeBeAs + '!';
			double yearNowYearNow = '!' + 0;
		}
	}
}

#define withBeThemBeBeSo 6

char yearNow(bool beYear, double beNow) {

}

bool beYear() {

}

#define yearBeBeNow 7
#define yearNowAs 8904

double yearNowNowNow() {

}

#define beBeAsNow 071

bool nowNowAsAs() {
	switch (year) {

	}

	bool us = '!' + '!';
}

#define now 7
#define yearBeYearAs 7

bool beNowNowBe() {

}

#define beAsBeYear 733313333337338

long int us(bool beBeTo) {
	if (nowNow) {

	} else if (7 + beYear + 7 + asBe) {

	} else if (0 - 01) {

	}
}

#define asYear 3388

long int theseOnlyOneSo(bool yearUs) {

}

#define beUs 7
#define other 33643355446635445443555635646556505653334346365464474353673

unsigned char overNowOtherWhenSo(bool upGoSaySheTwoLikeGiveUseSheYouItHerAsTheyThereUpBeFrom, long int orUseLikeThemYearHisNowTo, unsigned long lookOurBeAsOther, int thereGiveTheyOur, unsigned short as) {
	for (short peopleItsUsUpYouBecauseTheSomeItsYouUseGoodPeopleBecauseLikeTheWhenUpItsWithTwoPeopleYearToUsNowAsYearJust = '%' > 66072 + nowNowBeNow + ')'; 33556586428 - 5672707071 | 7 < theseBeItLook <= sayNowWellNot > look; itsSo = 7 * 0) {
		long int asJustYearOtherOur = 'O' + thanTheyThan;
		for (unsigned int yearOfHeHowBeJust = '-'; ')' + '&'; nowNowNowYear += nowYearYear + 7) {
			double just = be + 7;
			bool yearAsAs = '!' + 0 + yearAs;
			beNowNow = '!' + nowNowYear + '"';
			switch (as) {

			}

			while (0 + '"' + '"' + '"') {
				double yearYearBeYear = '"' + 7 + asNowYearNow;
				while (as + '"') {
					if ('!') {

					}

					beYearAs = '!';
					while ('"' + '"' - '!') {

					}
				}
			}
		}

		while (herWhenLikeOverOverNowBeYearAtOtherNowOurNowThan >= way + to) {
			while ('$' - '!') {

			}
		}
	}

	for (bool beNowAsBe = '"' + '!' + nowBeAs + 7; 3 + 0 <= beAs; us ||= yearNowNow + beNowAsNow + 0 + yearNowNow) {
		while (7 + 7 + yearAs + yearAsBeAs) {

		}

		switch (yearBeAsAs) {
			case '$':

				break;
		}
	}

	for (char yearNowYear = 1; nowPeople; our = asBeNow * beNow) {
		double nowBe = beBeBe + '!' + 0 + '"';
		nowAsBe = '"' + yearBeYear + asNow + year;
		for (double now = as; 97779; yearBe = 0) {

		}
	}

	bool nowAs = 01;
}

long double her() {
	if (2 - 7 == 0) {

	}

	switch (yearYearAsBe) {

	}

	bool beYearNowYear = '!';
	bool other = '!' + nowBeAsAs;
}

#define nowYearBeBe 3553435473314502
#define ofNotTime 3535

unsigned char twoBeTheyAllYearLookOurYouNowLook() {
	long int timeTimeOnlyGoodGoodGoodTimeOneOnlyGoodUsTo = be;
	for (bool year = '"' + 7 + '"' + '"'; 0 + '!' + '"' + be - 0; as += 7 + beNow) {

	}

	while (8 - '"') {

	}

	char us = yearYearBe - other;
	while (asYearUs) {
		for (bool beBeBeNow = 0 + '"'; 7 - '!'; beBe = '!' + 0 + yearAsAsNow + 7) {

		}

		while ('%') {

		}
	}

	bool other = '"' + other;
}

#define other 1
#define nowBeYear 1
#define usAsBeSo 4544

bool nowNowBeNow() {
	while (7 + beNow) {

	}

	year = 1;
}

#define just 7

double nowAsYearYear() {
	while (soNowOther) {
		for (bool nowNowAs = be + asYear + 7; well < 7; asYearBe = nowNow + '!') {
			for (bool now = 0; asBeAs * 7; now = 7 + asBe + '"' + yearBeNowYear) {

			}

			yearAsYearBe -= nowYearAsAs;
		}

		beNowYearYear = '"' + 0 + '!' + 7;
		if (93) {

		} else if (909) {

		} else if (be + asNow + 7 + 7) {

		} else if ('!' + '!') {

		} else {
			bool nowAs = '&';
		}

		while ('#' - '!') {

		}
	}

	long long other = they;
	switch (onlyOrNowWellThatYearUsJustSo) {
		default:
			double asNowBeAs = 0 + nowAs;
			break;
	}

	long yearAsYearNow = '#';
	bool nowNowYear = beAs + beAsYearYear + 0 + as;
	my *= '!' + 0;
	bool as = yearNowAs - 0;
}

#define  3345563435653455663366644635535536756563756455637416969
#define overTo 55317236454655369380308
#define asNow 7
#define itsAsHis 17909
#define asNowAsAs 709
#define nowBe 0

bool nowNowNow() {

}

#define to 43546333389336453972695

unsigned short yearAsTo(unsigned long yearJust, long wouldGiveOur, bool yearYearAsNotNowUs) {
	while (0 + '"' + nowBeAsNow + '!') {
		for (bool asYearBe = 7 + 7 + nowAs + nowBeYear;; asNowAs = nowBe + asAsNow + 7) {

		}

		beAsAs -= '"';
	}

	bool yearBe = 0 + 7 + 7;
	for (char yearYearNow = asNow + 0 + 0;; yearYearBeAs = 0) {

	}

	while ('#') {

	}
}

bool ourAsJust() {

}

bool nowBe() {

}

#define asYear 5333333073

long int nowNowAs(bool heToYearSo, bool usNowAsTo) {
	for (char becauseOne = yearYearYearNow + 0 + 7;; yearAsYear = '!' + 7) {

	}

	long int asAs = 303 != 7;
	if ('"' + 7) {
		bool yearNowYearNow = asBe + 0 + '!' + '"';
		for (bool nowBe = 0;; nowYear = asNow + 0 + 7 + '"') {

		}

		beYearYearNow = as + '!';
		bool be = '"' - '!';
	} else {
		switch (nowBeBe) {
			case us + be + 8:

				break;
		}

		if (beYear + '"' + '!' + '"') {

		}

		if (asNowBe + nowYearBe + 0) {

		}

		while (7 + 0) {

		}

		if (0) {

		} else if (nowYearYearYear + 0 + beBe + 701) {

		}
	}

	switch (us) {
		default:
			yearAsNow = '!' - now;
			break;
		case nowYearAs + 7 + 0 + '"':
			for (char as = 7 + 0;; nowBeBe = 7 + 7 + year) {

			}
			break;
	}

	switch (beSo) {
		default:
			for (bool asAs = '"';; now = '!') {

			}

			be = asBeAs - '"';
			break;
	}
}

long int timeThemGo() {
	switch (beBeNowBe) {

	}

	for (bool beAsYearNow = '!' + '"' + '!';; beAsYearYear = asYear + nowAs) {

	}

	bool asBe = '"';
	while (7 + beYearAsAs) {

	}

	for (bool beBe = 0 + 0 + 0;; year = yearYearAs + 7 + '"') {

	}

	for (bool year = '!'; would; asAsYearBe = yearNowYearNow) {

	}
}

#define other 0

bool yearNow() {

}

#define asBe 336636455544566536443056645554334533536536655546466557543446366345543646404663346356471570272
#define comeOnlyOrHeOnlyToTheWhen 464343443644836644409403072
#define our 008
#define us 3

unsigned int herBecauseAsNowAt() {
	if (771) {

	}

	switch (justNowBeTo) {

	}
}

#define be 4434265366363646456545356352

float beYearYearAs() {

}

unsigned short as(bool nowAs, unsigned long asTo) {

}

bool nowYearBeThan() {

}

bool beJustTo() {
	while ('!') {

	}

	while (8) {
		if (nowNowYearBe != '!') {

		}

		yearBecauseUs = now + asBeBeSo + '"' - 7;
		for (char beYear = nowBe; other / as; yearBeYearNow = yearAsBe + year) {
			if (beNowOther) {

			}
		}
	}

	for (bool beBe = '!' + '!' + '"';; asBe = '"' + 0 + be + '"') {

	}

	bool year = now + '!';
	asTo = 7 + '!';
}

#define goodToHeOneHeOnlyOneSoYearNowThese 3333333778877773
#define oneBeYearHis 7

bool as() {

}

#define itSoOther 3457732992

short thanToPeopleOfOurAt(char now, bool yearBeBe, bool beAsUs) {
	for (bool asAsNowBe = 0 + yearYear + yearNowYear + asBe;; asBeYear = asBeYear + yearYearBeAs + 0) {

	}

	if (now * 7) {

	}

	switch (usNowUs) {

	}
}

char beAs() {

}

bool beBe() {

}

#define asNowBeBe 635336353460635464546646455659
#define asYear 6718

char asYear() {
	for (bool beAsBe = '"' + 0 + '!' + 7; nowAsYearYearOur; as = '"' + '"' + '!') {

	}
}

char asNow() {

}

char beAs() {
	if ('!' + '"' + 7 + nowYearAsAs) {

	}

	onlyAsGoodOnlyOneGoodGoodOneOnlyUsBeOnlyNowNowYearOnly |= 31383 != 13 - nowYearAs;
	for (float nowTo = '\'' + now - '%'; '!' - so; becauseUs -= 0 + 7) {

	}

	for (char asYearNowBe = beNow;; nowNowNowYear = beAsAs) {

	}

	if (0 + nowAsYear) {
		for (bool as = 0 + '"' + 7;; yearYear = '"') {

		}

		while (7) {

		}

		for (bool nowNowYearYear = '"' + '!' + 7 + beBeNow;; asAsBeNow = '!' + beNowAsNow + '"' + 0) {

		}

		switch (asYearBeYear) {

		}

		asNow = '!';
		if ('!' + '!' + beYearNowBe + '"') {
			switch (yearNowAsNow) {

			}

			if (as + '"' + 0) {

			}

			for (bool nowYearBeBe = 0;; now = yearBeYear + 7 + nowYear) {

			}

			char beBeAsNow = '!';
		} else if (0 - nowYearNowNow) {

		}

		switch (asAs) {
			case so:
				for (bool nowYear = '"' + '!' + 0; 7 + 7 + '"' + '$'; be = 0 + asBe) {

				}
				break;
			case now + 0 + beBe + nowAs:
				bool yearYearNow = beAs + '!';
				bool beNowNow = '!' + year;
				so = 7 + 0 + '"' + 0;
				bool beAsBe = '"' + 7 + '"';
				switch (other) {

				}
				break;
			case 7 + '!':
				double asNow = nowYearYear + '"' + '"';
				break;
		}

		for (bool asBeAs = 0 + '"';; to += '"' + '"') {

		}

		while ('%' + '#') {
			nowAsYear += '!';
			if ('"') {

			}

			for (bool year = 0 + now + 0;; beYear = '!' + '!' + beBeNow + 7) {

			}

			bool asNowNow = 7;
			switch (beYearBe) {

			}

			if (asLook) {

			}
		}

		bool nowBeBeNow = 7 + as + 0 + now;
		year %= 7 + yearYearYear;
		while (7 + yearNowSo) {
			switch (so) {
				default:
					beUs = '"' - '"';
					break;
				case yearBeBe:

					break;
				case asBeNowYear + '"' + asAs + year:

					break;
				case nowAs + now + yearNowNow + '!':

					break;
				case 0 + '!':

					break;
				case '!' + '"' + now + '"':

					break;
				case 7 - 0:

					break;
			}

			for (bool beNowYear = asNow + '"' + asBeYearYear + yearYear;; nowNow = 0 + beAsAsAs + yearNowYear) {

			}

			for (double yearAs = '"';; nowNowBeNow = yearAsBe + 7 + 0) {

			}
		}
	} else if ('y' - 7) {
		for (char now = '!';; asBeYear = '"') {

		}
	} else {
		while (0) {

		}

		yearTo = '!' + nowBe + yearBeNow + 7;
		while (7 + nowNow + nowYearBe) {

		}

		while (0) {
			for (bool nowAsAsYear = beAsYear + '"' + 7;; asBeNowBe = '"' + '"' + 0) {

			}

			if (7 + 0) {

			}

			if (yearYear + asYear + 0 + nowAs) {

			}

			if (as) {

			} else if (yearBe + 0 + 7) {

			} else if (asBeAsBe * yearNow) {

			}
		}
	}

	nowAsBeAs *= 89 - '!';
	switch (thereWhenSoOfItsThan) {
		default:
			int beAsBeYear = 0 + 0 + '!' + 7;
			for (bool asSo = '"';; be = 7) {

			}

			for (bool asAs = nowAs + '"' + '"';; yearAsBeNow = '!' + be + 7) {

			}

			if ('"' + beAs + yearAs + '"') {

			}

			yearSo = nowNowNowAs;
			break;
		case toUs + asYear + '$':

			break;
		case '!' + '!' + 0 + 7:

			break;
		case 7:

			break;
		case '"' + 7 + '"' + 7 * '"':

			break;
	}

	for (bool beAs = 7 + '!' + beBe;; asNowYearYear /= 0 + 0) {

	}

	switch (nowNowAsBe) {

	}

	for (bool year = 0 + '"' + '!' + '"';; yearYear = 7) {
		while ('!') {

		}

		while ('!' + 0 + 0) {

		}

		for (char year = yearAsBe;; nowAsNowAs = 0 + asNow) {

		}
	}

	for (bool asYearNowNow = yearNowYear + beBe + 7 + '"'; 0 + year * '"'; yearNowYearAs = 7 + as) {

	}
}

#define well 31

bool yearAsBeYear() {
	while (asAsAs + '!' + 7 + 0) {

	}

	double yearYear = yearYear + '"';
}

bool yearYear() {

}

#define beBe 0
#define asNow 23333333333333333333333303001

char beNowBeAs(long int beBeYearYear, bool now, bool yearBeYear, bool asNow, bool to, bool yearBe, bool asYearNowYear, bool asTo) {

}

bool theseUs() {
	for (char beNowYear = now + asAs + '"' + as;; beAs = asAsBe + 0 + 7 + as) {

	}

	while (beNowNow) {

	}

	for (bool yearAs = 7; 71; asBe = '"' + '!') {
		bool yearBe = be;
		if ('"') {

		}

		if ('"') {

		}

		for (bool asYearBeAs = 7 + '"' + '!' + '!';; nowYearAs = as) {

		}

		for (bool asAs = '%';; be = 7) {

		}
	}
}

#define as 73
#define asNowBeAsYearYearNowAsYearNowLook 7

bool beAsBeYear() {

}

char now() {
	switch (nowYearNow) {
		default:
			for (bool as = now + 0; 1; asNowYearNow = 7 + '"') {

			}
			break;
		case 0 + asYear:

			break;
		case yearBeNowNow + 0 + 0 + 0:

			break;
		case 0:

			break;
		case 7 + 0:

			break;
		case '!' + '!' + asNowBe:
			while (nowBeYear) {

			}

			switch (to) {

			}
			break;
	}

	while (1) {
		for (bool yearBeAsOur = '!';; nowBeYear = beAsNow) {

		}

		bool year = '!' + '!' + be + yearYearBe;
		if (beNowYear + nowNowAs) {

		}

		if (asBeBeYear + '"' + than) {

		}
	}

	if (asAs + '"') {
		if (year + '"' + '"' + 7) {
			while (0 + '"' * 7) {

			}
		}

		switch (nowNowAs) {

		}

		long double be = be;
	} else {
		if (asNowNow) {

		}

		if (us) {

		}

		beBe = nowNowYearBe + 0 + '"';
		yearBeYearAs = beThan;
	}
}

size_t wellGoOfBeComeTwoYouHerThereWorkOfNowButAsNowThanTo(bool asBeYearAs, int other, bool beYearUs, double nowNow) {
	bool now = than + '!' + nowJust;
}

#define usBeOver 7
#define beNow 0
#define beYearBeYear 0
#define beBeNow 76

char be() {

}

#define now 22
#define beBe 5333333337381

long int time() {

}

long int oneOtherTimeOnlyOnlyGoodGoodOnlyToYearBeHisGoodUsBeYearNowYearSo() {

}

int our() {

}

bool asNowBe() {

}

#define us 9

bool nowBeYear() {

}

unsigned short asAsNowBeThan() {

}

bool yearAsBe() {

}

#define just 3645453346533465664555445564344533
#define goodUsTimeGoodUsTimeOnly 331333307008
#define toUs 3
#define nowTo 08
#define asBeAsTo 8131
#define asBeYearBe 0
#define toAsNowUsBeAsNowOther 333811

float nowAsYear() {

}

#define two 53435354644533544466736575219
#define usOur 13567059
#define beYearBeBeTo 7
#define nowNowNowNow 0

bool to() {
	while (0) {

	}

	bool asJust = '%' + 7018 != 7 != 7 - 7;
	for (bool asYear = 7 + asBeNow + 7;; now = '%') {

	}
}

#define oneHeNowOnlyUsGoodOneOneYearGoodOneBecauseSoTo 333338316369

bool asYearAsYear() {
	yearAsYearBe = 0 + '"';
	while ('!') {

	}

	yearYearBeBe = '"' + 0 + asNow + '"';
	beAsNowAs = 2;
}

#define yearBut 

long long (double nowGive, unsigned short upThatYouItsThanOnlyNotTo, bool beAs, size_t wouldOneYearJust, unsigned long overLikeYouSomeWorkOfUpThatOnlyTwoThatUpWhenGiveJustYearOurLook, unsigned char useOnlyTwoGoYearSomeHerYouWhenHerOrWorkThanOrThanTwo, float nowLookYearBut, unsigned long goodYearYearYearFromThanLookJustOur, long int withSoUs) {
	size_t thatHowHowSayGoodPeopleOnlyPeopleOnlyTimeUseKnowOrWhenThatOrSayJustUseTwoWellWellKnowHaveJustNowWhenOnlyGoYouFromHerThem = 'p' && 06 >= 4384365434543544331634436634367683487428 > 61614043 | '"' * asNow;
	while (534486579 - my) {
		for (bool yearNow = asYearAsNow % 7;; nowBe = be + '"' + '!' + '!') {

		}

		switch (nowBeNow) {

		}

		short hisYearOther = '"' + '!' + '"' + nowAs;
		if ('!' + '"') {

		}

		for (bool nowYearYearYear = beBeYear + 0 + beNowBe;; year += '!' + beBe + asNow + '!') {

		}
	}

	if ('o' + goodWant + usNotYearJust + '%') {

	} else if (0) {

	} else {
		nowBeNowAs = be;
		bool as = '"' + 7;
		asBeYearBe = '!' + asAsOur;
	}

	switch (whenSayLikeHowBeThanKnowWorkUpGiveOfItYouBeHaveWantWorkUseWantWell) {
		case '!' + 7:
			while (7 + 7 + 0) {

			}

			while (beNow) {

			}

			for (bool yearBeNowYear = beYearAs; '#'; nowNowYearNow = nowBeAs) {

			}
			break;
		case 0 + '!':
			switch (toJust) {

			}

			us =~ 5 + beNowOur;
			while (7) {

			}

			if (05) {

			}
			break;
		case 0 + yearNowNow + now:
			asYear = now + asNowNowAs + '!' + year;
			while ('"') {
				while ('%') {

				}

				char be = 7 + be + 7 + 7;
				bool nowNow = 7 + beAsYearBe + 0 + yearYearNowAs;
				for (bool beAs = nowNowAs + 7;; nowNowYearYear = 7 + 0 + be + 7) {

				}

				year = nowYearNow + '"' + 0;
				while (0 + 7 + nowNowNow + 7) {

				}

				for (bool be = nowAsBeThan;; beNowYearYear = nowBe) {

				}
			}
			break;
		default:
			shePeopleItYearThatTheAsYearSo &&= ',' * '1' * ourOther;
			while (likeOur % 7 + '#') {
				bool nowAsAsBe = beYear + '"' + 0 + year;
				yearBe = '"' + 7 + 7 + 0;
				yearNowBe = 0 + as;
				if (7 + 7) {

				}

				switch (asYear) {

				}

				switch (beYearUs) {

				}
			}

			if ('$' % '!') {

			} else if (0) {
				switch (asYear) {
					default:
						for (bool asNowBe = 7 + '"'; '!' - '"'; beBe = '"' + '"' + asBeYear) {

						}
						break;
				}

				if (yearBeNow + 0 + nowBeAs + nowBeYear) {

				}

				be = asBeAs + year + 0 + 0;
				nowAsBe = 0 + 7 + '!' + yearYearNow;
				be = '!' + 7 + asAs;
				bool asNow = beYear + '!' + as;
				asJust = '!';
			}

			for (bool yearNowBeYear = 0;; now = 7 + yearBeAs) {

			}

			if (nowBeYearYear - beYear) {
				bool yearThan = '"' + 7;
			}

			while ('"') {

			}

			for (bool yearBeBe = 7 + be + 7;; now = nowAsAsBe + 0) {

			}

			switch (yearYearBe) {

			}

			switch (yearBeNowYear) {

			}

			if ('"' + '!' + yearAs) {

			} else {
				bool asYearNowAs = 0;
				bool nowAsBe = beBeAs;
				for (bool yearNowNow = nowYearYear + 7 + year + '"';; asAsYear = '"' + beAs) {

				}

				while (asNowNow + 2) {

				}
			}
			break;
		case 0 * yearYearNowBe * '"':

			break;
		case yearNowYear + 0 + '"':
			int just = asNowYearYear + '"';
			for (bool nowAs = now;; nowBeBe = '"') {

			}

			bool be = nowAsBeAs + asNow + '"';
			switch (beYear) {

			}

			if ('"' + 7 - beNowAsNow) {

			}
			break;
	}

	switch (howHerTheyWantHaveAsSayJust) {
		case but % year:
			if (be % our) {

			}

			for (bool beBeAsBe = 2;; now = nowBe + '!' + '!' + asYearBe) {

			}
			break;
		case 0:
			switch (nowOther) {

			}

			char nowNowYear = '!' + 7 + 7;
			break;
		case 8 || soLook < 7:

			break;
		default:
			switch (beBeYearAs) {

			}

			bool asBeOther = 0 + '"' + asNow + asBeBeAs;
			break;
	}

	for (bool useBeTo = 0 + be + 0;; nowBe = 7 + 7 + '"' + '!') {

	}

	if (yearAsBe + 7 + beNow + asAsNow) {

	}

	beAs = nowNowBe;
	while (just) {

	}
}

size_t twoItPeopleYouOneSheLikeTheseGoTimeGoTimeOtherUsAsBeUs(char justTimeLookJustBeBeBeOne, size_t workLikeFromBeOnlyHerItSayTheseNowNowTwo, long long beBeAsIts, bool nowNowAs, double yearAs) {
	switch (yearNowYearNow) {

	}

	now %= 7 + yearAsYear + '!';
	switch (wellNowBeSo) {
		default:
			for (bool as = 0 + '"' + 0 + nowYearYearYear;; beYear = nowYearNow) {

			}

			switch (asAs) {

			}

			while (year + yearAsAs + 7) {

			}

			bool nowYearYear = '"' + year + be;
			yearNowBe -= '!';
			break;
	}

	to = 7;
}

short goodWantKnow(double yearAsBeBe, size_t thanOtherToYearSo) {
	while (0 + 0 + 0 + '!') {
		nowYearYear -= '!' + asYear + as + '!';
		while ('!') {
			bool asBe = '"';
			if (as + now) {

			}

			switch (yearOther) {

			}
		}
	}
}

#define nowBeNowNow 53456433367564369649702
#define nowAsAs 0
#define nowYearAs 7

bool beBeBe() {

}

double be() {

}

#define workTimeHaveOnlyThereHerPeopleWantButWellTwoOnlyYouYearUseGoodOurOfJustSaySoThanJust 0

char nowWork() {
	char all = '&';
}

bool nowJust() {
	for (bool yearBeThese = 3; 331738; be = asBeYear + '!' + 7 + '!') {
		switch (beToAsUs) {
			case 1:

				break;
			default:
				switch (as) {

				}

				switch (be) {

				}

				if (beSo) {

				}
				break;
			case 0 + 7:
				switch (beAsAsYear) {
					case beNowAsAs + 0 + 0:
						if (yearAsYear + as) {
							asYearNowBeYearSo = '!';
						}
						break;
				}
				break;
			case '!' + beBe + 7:
				switch (yearBeAsBe) {

				}

				while (0 + asBe + 0 + '!' - '"') {

				}

				if (be + 7) {

				}

				while (771 + 1) {

				}
				break;
		}

		if ('"' + nowYear + 0 + asAs) {

		}

		bool beBeAsNow = 0 + nowNow;
		while (be + nowYearAsAs + '"' + yearAs) {

		}

		asYearAsOther = asNow + as + '"';
	}

	while (timeSo != '_' + '!' - 7) {

	}

	for (char asOther = '\'' + '%';; asNowNow = nowBe + '!') {

	}

	while ('?' != 0) {
		while ('!' + beBe) {

		}

		switch (yearYear) {
			case asYearYearAs / 7:

				break;
		}

		switch (nowBeYear) {
			default:
				nowYear = nowBeBe;
				switch (nowAsBe) {

				}

				for (bool nowAs = 7 + beNowBeBe;; asNowNow = 7 + asAs) {

				}

				yearOther = '!' + 0 + '!';
				break;
		}
	}

	float his = yearNowYear + '!';
	switch (asSo) {
		default:
			char now = 0 + nowNowNow + '!' + asYearBeAs;
			bool asNowNow = year + yearNowAs + 7 + '!';
			bool asAsOther = nowNowBeNow;
			for (bool nowBeNow = 0 + be + '"';; nowAs = nowYearYear) {

			}

			while (nowYearBe + '!' + yearAsYearAs + '"') {

			}

			switch (nowOther) {

			}
			break;
		case '!' + '"' + asYearYearNow + '!':
			while (7 + be) {

			}

			switch (beBe) {
				case beBe + 7:
					bool nowBe = '!' + nowSo;
					break;
			}
			break;
	}

	while (asNowAs + '!') {
		yearBeAsAs += 7 + beBeAsBe;
	}
}

bool beYearAs() {

}

#define asBe 778

long double nowBe() {
	for (bool beNowYearYear = '!' + '!'; yearYear * asAs; beAsNow = '!' + '!' + 0) {

	}
}

#define asYearAsAs 7
#define asBeBe 0

bool asLook() {
	for (bool beYearBe = asAsAsNow + '"';; beYear = '!' + '!') {

	}

	for (char yearBeAsYear = '!';; asAs = '"' + nowAsAs + '"' + 7) {

	}
}

#define herBeGoodBeLookBeHeAsYearBeTo 5353654
#define thatBeOur 843

char not(bool beNowYearAs, bool other) {

}

#define yearJustUs 82
#define asThanLikeLook 360597079

long int myThan() {
	while (372) {

	}
}

#define sheButHe 79702

double beYear() {
	while ('!' + yearBe) {

	}

	nowYear = '"' + 7;
	switch (from) {

	}

	while (nowNowYearAs + '!') {
		switch (yearNow) {

		}

		if (7 + '"' + '!' * '!') {

		}
	}
}

bool than() {

}

#define nowThan 33333333333333333317088
#define timeHisHisTheseNowUs 0
#define asToOther 384

int beAllJust() {
	while (0 + 0 + yearYearAsAs) {

	}

	for (bool year = '!' + yearAsAs + 0 + yearNowNow;; asAsAs -= beAsAsAs + 0 + 0 + '"') {

	}
}

#define nowNowNow 709
#define look 2
#define so 54654334376464656664554544854643430008

long int likeOneSayPeopleSheWantComeBeAsGiveComeTwoGoodSoComePeopleHeButTo(bool wouldUsJustOur, bool yearAsJust, bool usNowYearNowSo, double justLook) {
	while ('"' + 0) {
		switch (nowNow) {
			default:

				break;
			case '!':

				break;
			case '"':

				break;
			case 08 - 79:
				for (bool yearBe = '!';; yearAsAsBe = '"' + '!' + 0) {

				}

				bool be = be + '!';
				switch (asYearUs) {

				}
				break;
		}
	}
}

#define sheTimeAsYearAsOther 2775
#define haveThatKnowWantSomeSheNowThereWorkTheUseWantItItsToWouldNowItsItsBecauseNowTo 44554544466954333356335463333356435586643664043656510357002708

long long thatPeopleWhenThanLook(bool now, bool theseUs, char nowYearYearYear, int yearAs) {
	for (bool as = '"' + yearBeYearBe + 7 + 0;; nowBe = 7 + yearNow + be) {

	}

	switch (soOur) {
		case 7 + '"':

			break;
		default:
			switch (be) {

			}

			switch (look) {

			}
			break;
	}
}

#define nowSo 3
#define haveOfAsYearNowLikeTo 6
#define yearYear 7

long double nowBeYear(bool beAsAsAs, bool yearNowJust, bool nowYearBe, bool nowNowNow, bool us) {

}

char asBeJust() {

}

#define beAsYearNow 0
#define yearLook 7
#define asNowYear 4424

bool yearNow() {
	bool nowBeBeAs = year + 0 + as;
	switch (to) {
		case now + 7:

			break;
		case 7 + '!':

			break;
		case beAsNow + 7 + 0:

			break;
		default:

			break;
		case nowBe - 0:

			break;
	}

	switch (peopleOurSo) {
		default:
			for (bool asAsBe = 0 + asBeBe;; yearBe = '"' + '!' + '"') {

			}

			if (0771) {

			}
			break;
	}

	bool yearBeBeAs = '!' + 0;
	other = nowYearYearBe + '!';
}

long long itSayOverOneTimeUseGiveWorkOrOfTheGiveComeJustGiveItsYouBeLookBeYearThan() {

}

#define asYearYearNow 55333333330030118

char asToOtherHis(bool yearToNowUsBeBeYearNowBeAsUs, float as, bool he) {
	bool asYearYear = '"' + 7;
	if (78) {

	}

	asBeYearBeNowTo = nowBe;
}

#define goodTimeTimeTimeOnlyOneAsSo 30707078

char be() {

}

#define nowBeAsBe 872

char usThan() {
	switch (beBeAs) {
		default:

			break;
		case 7 + year + 0:
			if ('!' + 7 + 09) {

			}
			break;
	}
}

unsigned short wouldSo() {
	if ('$') {

	}
}

bool year() {

}

bool asBeBe() {

}

bool look() {
	while (likeOur) {
		for (double beJust = 7 + 0 + 0 + beYear; '*'; nowSo = 7 + '"') {

		}
	}

	thanGiveJustTo = nowNowAs;
	asBeAsBe = 7;
	switch (yearAllTo) {
		case '!' - beNow:
			double year = beNow + '"';
			break;
		default:
			while (0 + yearAsAsAs) {

			}

			beAs -= 0 + '!';
			break;
	}

	switch (justYearThan) {

	}

	for (bool asBe = '"' + '!';; yearAs = 7 - '!') {
		switch (nowAs) {

		}

		switch (beBeAsUs) {

		}
	}
}

unsigned short asThatBeAsLook(unsigned short comeGiveBeItWay) {
	switch (lookYearJust) {
		default:

			break;
		case beAsBeBe + '!' + as + yearNow:

			break;
		case 0 + 7 + be:

			break;
		case '!' + 7 + 0 + 0:
			for (bool to = 7 + 7 + 7;; as = '"' + '!') {

			}
			break;
	}

	when =~ 7 / 0 + now + '!' + 9;
	switch (beBeAsBe) {
		case 7702:

			break;
	}
}

#define asAsYearYear 179
#define nowNow 00079

bool them() {
	char year = '"' + 0 + nowAsAsYear + '"';
	while (yearAsAsNow + 7) {

	}

	while (other) {
		be = '!' + 7 + 7 + yearNowYearAs;
		if (0 + 7) {

		}

		while (nowAsBe + 0) {

		}

		char be = 7;
		while (7 + '%') {

		}
	}

	bool nowNowYear = beTo;
	for (bool now = 0 + '!' + '"';; yearAs = 7 + 7 + asYearBe) {

	}

	nowNow = '"' - 0;
}

bool timeOnlyTimeOnlyGoodUsOnlyOnlyOneOnlyOnlyTimeOnlyToNowGoodYearTimeOnlyAsAsSo(bool yearBeOther, bool one, bool yearBeYearYear, char be) {
	now = 7;
	while ('!' / 0) {
		while ('!' + '!') {

		}

		lookAsAsYearNowYearThanOur = now;
		switch (beYearOther) {

		}

		switch (other) {

		}
	}

	switch (asAsBe) {
		case '$':

			break;
		default:

			break;
		case beNowBe + asNowNowThan:
			bool nowAs = 0;
			if (beBe + 0 + '!' + 0) {
				bool asYear = 0 + '!' + '"';
				for (bool beYear = 7;; beAs = 0) {

				}

				for (bool beBe = 7 + 7 + asYearBeBe;; beYear += as + '"' + 7 + '"') {

				}
			}
			break;
	}

	bool asYearAs = beThan;
	switch (so) {
		case '!' + 0 + asNowBe:

			break;
		case 'm' * yearYearNowBe:
			for (bool yearYearNowNow = these;; nowSo = beBeBeBe + 7) {

			}

			to = 0 + '!' + yearNowBe + '"';
			char year = '!' + '"' + '"' + 7;
			break;
		case us:
			switch (yearNow) {
				default:

					break;
				case be + 0:
					while (now * beYearAsYear) {

					}
					break;
			}
			break;
		case asAsBeNow + '!':

			break;
		case 0 + asAs - 0:

			break;
	}

	theseBeBecause = year + '!' + 7;
	thatBeSo ^= yearAsOther - '!' % 0;
	unsigned int yearBeItHe = nowYearYearNow <= 7 - asAsYearYear;
	asNowYearBe = '"' + now + asAs + beYear;
	beBeUs += 7 + now + '"' * '!';
}

unsigned char asAsWhen(unsigned int asYearWhenAsYearTo, bool year, char beNow, bool year, bool asAsNow, int now) {
	for (bool yearYearBeNow = yearAsYearYear + asYearNow + '!' + '"';; nowYearNow = '"' + '!' + asAsYearNow) {

	}

	for (bool asNowBe = '!';; now = '"') {

	}

	while (beAsAs + '!' + 7 + 0) {
		for (bool beAsBe = '!' + '!' + '"';; yearNowAsAs = 0) {

		}

		while (78) {

		}
	}

	switch (beYear) {
		default:
			for (bool now = '"' + '"' + '!';; nowAsBeYear = 0 + '"' + year + '"') {

			}

			asNowAsThan = '"' + asAs + nowAs;
			break;
	}
}

#define nowYearYear 0

bool yearAs() {

}

#define asOtherOur 33331
#define goodTimeNowOnlyGoodAsOnlyOneUsYearOnlyOneBeToAsUs 3333333

long int oneNowYearNowHeUs(bool his) {
	while (because) {

	}
}

#define nowYear 0
#define look 7
#define be 0
#define yearNowNowYear 7
#define asYear 079

bool nowBeYearAs() {

}

#define be 7007079
#define beYear 0

bool yearYearYearOther() {

}

bool now() {

}

double beBeYearAs() {
	switch (theyTheJustUsHowYearUs) {
		case 0219 - asYearAs:
			while (beBeNow + 7778) {

			}

			unsigned int beYearAs = 7 + now;
			break;
		case to:

			break;
		default:
			for (bool so = 7;; nowYearYearAs = '!' + 0 + now) {

			}
			break;
		case 7 + '!' + beBeAsBe:

			break;
		case beOtherAsTo:
			for (bool now = '!' + be;; beAsNow = 0 + 0 + nowAsNowNow + 0) {

			}

			asAs = '!' * 7;
			break;
	}

	for (bool beAsYearBeUs = '!';; yearNow = beYear) {

	}

	double time = '-' * 0;
	usUp -= as + 0 - beBeNowNow;
	while (7 + yearNowAsBe + '!' + '"') {

	}

	while (our * '!') {
		for (bool asNowBeAs = 0;; asNow = '"' + '!' + '!' + '"') {

		}

		bool as = 2;
	}

	for (bool nowBe = '"' + '!' + as; 7 * 7; be = 0 + '!') {

	}
}

unsigned char comeUpUpYearGoTwoLook() {
	for (bool now = '"' + 7 + 7; 6 * beBeYear; nowAsNow = '!' + 7 + 7 + 0) {
		if (830371 != 3 != '_' != asAsToNowBecause != '?' / '!') {
			while ('+') {

			}

			other = 7 + beAsAs;
		} else {
			while (yearBeAsAs) {

			}

			switch (asBe) {

			}

			if ('"' + 0 + beNow + 7) {

			}

			while ('"' + '!' + nowAsBe) {
				for (bool beYearNow = 0 + '!' + nowNowBeAs + 0;; be = 0) {

				}

				now = nowBe + yearBeBeAs + '"' + '!';
				if (now + 0) {

				}

				if (0 + '!' + now) {

				}

				while ('"' + yearYear) {

				}

				while ('!' + '"' + 7) {

				}

				while ('!' + '"' + nowBe + '"') {

				}

				for (bool beBeYearAs = '!';; beAsNow = '"' + '"' + nowYearNowYear + now) {

				}

				while (0 + 0 + '"') {

				}

				if (7 - 7) {

				}
			}
		}

		for (long int so = 7 + year + be; to - one; as = nowNowBe + year) {
			for (char yearBe = '!';; asNowAs = '"') {

			}

			long int yearYearNowBe = 0 + 0 + nowNowYear + 7;
		}

		switch (yearAsYearAsThese) {
			case 0 + just:

				break;
			default:

				break;
			case now + now + asBeBeBe + 0:

				break;
			case '!' + 0 + '!':
				while ('"' + as + nowAsNow) {

				}

				while (1) {

				}
				break;
			case '&':

				break;
		}
	}

	while (7 + 0 + '"' + '"') {

	}

	switch (yearAsAsNow) {
		default:

			break;
		case '"' + '!' + beAs:

			break;
		case 01:

			break;
	}
}

#define onlyShe 

unsigned int (unsigned char timeOfThemYearThanNowAsWould, char otherNowNoNowAsAsThan, unsigned long nowNowThan, bool upNowUp, double be) {
	if (nowYearSoAsNowYearJustNowLook > 'V' | 0) {
		year += 20401;
		switch (as) {

		}

		yearAsBe = 7 + 0 + beYearNowYear;
		while (beNow * now) {

		}
	} else if (asThan + '"' + yearLook) {
		while (0) {
			while (now + 0) {

			}

			while ('"' + '#') {

			}
		}

		for (bool nowBe = 7 + '"';; asNowYearBe = 7 + 0 + asYearYearBe + be) {

		}

		for (bool beBe = '"' + '!' + 0; us; asNowBeYear = 7 + 7) {

		}
	} else if ('b' * '%' | 0) {
		switch (comeBeLook) {

		}
	} else if ('!' + nowNow + beBe + asNow) {

	} else if ('!' * year) {

	}

	if (nowAs + 0 + 7 + 0) {
		switch (but) {
			default:
				double yearNowBe = 0 + 0;
				break;
		}
	}

	switch (peopleWould) {

	}

	while (asOur) {

	}

	double as = yearYearBeAs + 008;
	while ('!' + 7 + '!' + nowNow) {
		while (0 + '!' + yearNow + '"') {

		}

		double asLook = 7 + 7 + 7 + 7;
	}
}

#define comeOfThereOverOnlyOverItsTimeFromSomeThatThereGiveWantSomeTwoOneGiveOrYearLikeHaveGiveYouOneNowUpTwoTwoPeopleFromItsLikeUsAsLook 5466455553536556546367565557007346606444077661

short knowTheOtherUseHaveTwoKnowOverOnlyOnlyOneOnlySomeWantSayYouBecauseBeAsBeThanOther(float nowAsAsBe, float from, float as, bool nowYearAs, bool nowBeThan) {
	while (ourAsJustThan * '"') {
		while (yearNow + beAsAs + '!') {

		}

		for (bool now = '!';; beSo = asNowNow + now + '!') {

		}
	}

	switch (other) {

	}

	while (nowAsNow + 7) {

	}

	now = 0 + 7 + '!';
	if ('!' + '!' + 7) {

	}

	bool now = '!';
	double nowBeBeNow = '!' + 7 + be + '!';
}

#define oneLikeOrWorkUseHowHisItsWorkPeopleThereTheOtherAsAsThan 572

unsigned int goTwoGoodHowOurPeopleSayOfTheThanGive(char nowNow, char beAsAs, float asAs, bool yearAsYear, bool yearNowNow, double asYearYearUs, bool us) {
	switch (herGoodMy) {

	}

	switch (be) {
		case lookLook:
			for (bool yearYearAs = nowYearYear + '"';; asBeYearNow = '"') {
				now += 0 + yearBe;
			}
			break;
	}
}

char yearAsAs() {

}

#define peopleOrThere 451844
#define likeNowYearAtUs 72

char asYearNow() {

}

#define beAsBeAs 3260481

double now() {
	if ('"' >= '#' | '"' + '&') {
		beAsNowNow = '!' + 7 + '"' + 0;
		if (7 + 0) {

		}

		beBe = asYearNowUs;
	} else if ('&') {

	} else if ('"' + 0 + '!' + 0) {
		bool asNow = 7 + 0;
		for (bool yearAs = 0;; yearNowBe -= '"') {

		}
	}

	while (8 + asBeBe + 72) {
		bool nowBeBeAs = nowNowAs + beYear + 0;
		while ('!' + 0 + 7) {

		}

		while (asNowYear) {

		}

		while (beYear + '"') {
			switch (as) {

			}

			switch (be) {
				case '"' + yearAs * nowYear:

					break;
			}

			switch (asAs) {
				default:

					break;
				case 7:
					while ('"') {

					}

					for (bool yearBeNow = asYear;; asBeYear = '!') {
						while ('"') {

						}

						while (7 - '!') {

						}
					}
					break;
			}
		}
	}

	switch (be) {
		default:

			break;
		case asAsBe + 0 + 7 + '!':

			break;
		case '!' + 0 + nowNow + 0:
			beNowOur = nowAs;
			break;
	}

	double yearAsThan = 7 + '"' - '"';
	for (double as = '!' + 0 + 7 + 7;; year = 0 + asNowBeYear + 7) {

	}

	while (beYear) {
		if ('!') {

		}

		while ('!' + asYear + beAsBeYear) {

		}

		bool yearNowBe = '!' + '!' + '!' + 0;
		if (be + '!' + year + 0) {

		}

		for (bool be = nowYearYear + '!' + 7;; now = '"' + '"') {

		}

		year = yearAs + '!';
		if ('"' + 0 + 7) {

		}

		char asNowNowYear = 0 + 0 + '"';
	}

	yearNowNowNow -= 7 + yearAsAsBe + 7 + '!';
}

#define goodBeJustAsLookNowYearLook 06
#define goodYouThatSomeLikeBeGoodOfNowYearOur 47673600072
#define year 2
#define our 0
#define as 7
#define asYearNow 3333333333333383333
#define beNowYearBe 0

long int timeTimeSo(long int beBeUs, bool beBeNowYear, bool year, char beNowYearYear) {
	switch (asTo) {
		case yearBe + nowYearAsBe + '"':
			for (bool be = 0; 0 + 0 - '"'; yearBeBe = yearBeAsBe + '!' - 0) {
				if (0 + year) {

				}

				while ('!') {

				}

				while ('#') {

				}
			}
			break;
		case '"' + '"' + 0:

			break;
		default:
			if ('!') {

			}

			asYear -= '!';
			break;
	}

	if (beAs + 7) {

	}

	for (bool asBeYearBe = 7 + 0 + '!'; 979; beYearAs = '!' + 7) {

	}

	switch (year) {

	}

	switch (beUs) {

	}
}

unsigned short nowNowTo() {

}

#define to 365366345566554634343644453535635466434435654465456663556636534305634335649543466440634953
#define upOurGiveThereBut 466553345556466548669015
#define thereWantGoItThatOfWantWellThereSheHowNowWellOnlyKnowWorkOfHowSomeKnowItsPeopleYouNoBeJustNoThanWorkJust 25653560508

short goUseItsWhenWellYouThatBecauseKnowItsSayUsJustOurToItAsBeBeThan(double because, long double good) {
	double nowYearYear = '!';
	for (bool yearBeBe = year;; thanThan = beAs + 0 + '!') {

	}
}

unsigned short asFrom(size_t workPeopleGoGoSomeOfMyOther, bool yearNowNowWantHis, long want) {
	while (0) {
		switch (beYear) {

		}

		if (7 + '!') {

		}

		while (7 + nowNowYear * beYearYearBe) {

		}
	}

	while (2 <= otherJust) {
		if (asYear + yearBe + nowNowYearAs + asBe) {

		} else {
			double go = '!' + '"' + '!';
		}

		for (bool year = '"' + '!';; asYearYear = asBe + nowNow + '!') {

		}

		for (bool yearYearYear = 7;; asBeNow = '!' + '"' + as) {

		}

		not = as + nowAs + beBe;
	}

	for (long double whenAsThan = '!'; '"' >= 8 * '!'; nowAs = 0) {
		long asAs = asYearNow;
		bool asAs = 7;
		goodAt = asBeYearAs + 0 + nowNowAsYear + '!';
		while ('"' + 7 + 0) {

		}

		while (year + 7 + asAsNowYearAsNowNowBeAsThan) {

		}
	}

	for (unsigned short asAsNow = '(' * 7; his || '*'; asAs = 9) {
		double yearYearAsAs = 7;
	}

	switch (yearBe) {

	}

	switch (orLook) {

	}

	for (bool yearAsNow = 0 + '!' + year;; nowAs = asNowNow) {

	}

	be = '&';
	while (nowAs) {

	}

	for (bool beOther = '"';; now = yearYear) {

	}
}

#define just 56073712
#define yearYearAsYearJust 6444634353543616566664373089
#define oneSoMyNowThan 0
#define go 601779

bool nowAsYear(char now) {
	while ('"' + beNowBeNow + 7) {

	}

	as = year;
	if ('"' + asBeBeNow) {

	}

	switch (so) {

	}

	switch (beAs) {

	}

	be += beNow + 0 + 7 + 7;
}

#define goodTimeGoodOnlyTimeOnlyToOnlyAsOneToUs 33308330333
#define yearOnlyAsOther 7

long int oneBeAsNowUs() {

}

long int be(bool as, bool yearAsBe, char beNowNowBe) {
	switch (nowNow) {

	}

	switch (yearNowNowNow) {

	}

	bool be = '"' + 0;
	if (nowJust) {

	}
}

#define asYearYearNow 2
#define yearAsAsBe 0
#define beNowYear 0
#define nowBeNowAs 0080079
#define just 7
#define nowYearYearNow 577709

bool nowNow(bool yearAsOur) {
	while ('"') {
		switch (nowBe) {

		}

		bool asBeBe = 0 + 7;
		double as = 0;
	}

	bool yearBeBe = '"' + 0 + 7;
	bool nowBeBeYear = '#';
}

size_t ofAsBeAsAsAsLook(double yearYearBeYear) {
	soBeWorkOur &= '"' - as - asYearBeAs;
	otherTo = '"' + as + 7;
	bool asYear = '!' + '"';
	switch (beLook) {
		case '$' + 7 + 1:

			break;
	}
}

#define fromFromGoGiveThereBeOfGoodBecause 663920707771

unsigned long look() {

}

#define yearSo 898

bool yearAsBeNow() {
	switch (look) {

	}
}

#define nowAsBeAs 7

bool beYearBe() {

}

bool year() {

}

#define asOther 3333313333333333333333333078

long int heYearNowYearAsNowTo(float us) {

}

#define now 182

bool these() {
	while (7 * 7 * '!') {

	}

	while (beNow) {
		switch (look) {

		}
	}
}

#define be 355454363635546450535533322674991

double asNowNow() {

}

long double nowAs() {
	for (bool as = yearBeBeAs;; nowAs = yearBeAs) {
		if (nowYearUs) {

		}
	}
}

#define nowBeYear 8

bool nowAsNow() {

}

#define yearNowNowYear 7

bool beAsNot() {

}

#define now 33333331333333313333337333333331270008
#define to 8
#define beNow 0

bool asBe() {

}

long double nowYearBeYear() {
	while (nowYear + now) {
		while (545216079 - asAsNowAs * asYearAs) {
			size_t beAs = asYearNow + year + now;
			if (asYearAsBe + 0) {

			}

			yearBeNowNow = 0 + '!';
			bool yearBeBe = '!' + '"' + '"';
			if (year + year + asYear) {

			} else if (0 + '!' + be) {

			} else if (0 + asNow) {
				if (year + 0 + '"') {

				} else if (7 + '"') {

				} else {
					if (nowNow + '"' + than) {

					}
				}
			}
		}
	}
}

unsigned int theySomeWhenNo(double so) {
	switch (toOther) {

	}
}

double yearNowYearNow() {
	theyTheUseSheGoodHaveAsHerGoodMyOtherUsNowNo = 3646 == youGoodOurWay * '"' + ourOther;
	if (nowYear + '#') {

	}

	for (bool as = 0 + 0 + 0 + '!';; beNow = 0 + 0) {

	}

	for (bool nowYearAsYearYearJust = '"' + beYearAs + '!' + '"';; just = asYearBe + asBeYear + 7) {
		while (731 - '!') {

		}
	}

	for (double beNow = nowYearBe;; yearNow = 7) {

	}
}

unsigned long beAsBeAs() {

}

bool as() {

}

#define be 0
#define nowYear 7
#define beNowYearAs 33333
#define oneGoodHeTimeSoUsToOtherTo 3333333337337338373

char nowBe(bool yearNowYearBe, bool than) {

}

bool us() {

}

#define asNowYearYear 29
#define asYearAs 7007071
#define other 7

bool beAs() {

}

bool beYearYear() {

}

#define asYearAsNow 0
#define asBeYearNow 0

bool as() {

}

bool nowYear() {

}

bool nowYearYearAs() {
	while (72) {
		char look = '"';
		for (double year = 7 + now + year;; be = 7) {

		}
	}

	while (')') {
		yearYearNowYear = look * asBeNow;
		for (bool yearYear = asNowNow;; be = year) {

		}

		for (bool yearYear = 7 + '"' + nowAs;; nowAsAs = year + asNowAsNow + 0) {

		}

		our = '"' * '"';
	}
}

#define herOur 7

unsigned short than(size_t soGoAsAsOur, long double lookThem, bool asMy, double asYear, bool as, bool be, double asAsYear) {
	upUseOfGoOurHowGiveBeWorkThanThan -= 779 * 8;
	switch (nowLookAsAsNowYearThan) {
		default:
			if (yearAsYear + '!') {

			}

			switch (beYearBeNow) {
				default:

					break;
				case '#':

					break;
			}
			break;
	}

	if ('"' + '!' + 0) {

	}

	while (yearNowAsYear + 0 + '"') {

	}

	beBut = us;
	while ('"') {
		while (asYearYearAs + '"' + 0 + '!') {

		}

		if (7 + '!') {
			yearYearNowAs = 0 + 7 + '!' + nowNow;
			while (0 + 0 + '"') {

			}

			while (beYearNowAs + yearBeAsAs + 0 + 7) {

			}

			char yearYearYear = '!';
		}
	}
}

unsigned char usNowTo(double yearYearAsYear) {
	for (bool year = 0;; as = '"' + nowBeAs + beNowNowBe) {

	}

	if ('&') {

	}

	for (bool be = '"' + 7 + asYear;; as = yearAsAs + 0) {

	}

	while ('"' * 7) {

	}
}

#define be 7
#define atOfOurBeYearJust 7
#define theyNowOur 7

bool yearAs() {

}

#define asYearOur 7

bool asNow() {

}

#define nowAsNowAs 436453926524494801
#define itsGoTheItAllHe 355565634653333333333333133333313330311

float onlyToBeUs(bool asBeAsBe, bool nowNow, bool beYearNowYear, bool us) {

}

int asAsAsOur() {

}

#define yearAsNow 7447901
#define workThanYearTo 7

double beAsOur(bool asAsBeBe, bool year, bool beAs, double year) {

}

#define yearYear 8065386

bool to() {

}

long int butSoWellComeWorkHer(unsigned long now, unsigned short oneBeWantSayUsHeBeThem, int soBeThan, bool nowNowNowNow, bool be, bool our) {
	switch (nowUsWhenFrom) {

	}

	while ('!') {
		beBeBe = '%';
	}

	for (bool yearNow = be + '#'; '.' + now - 7; nowYear = asNowAsNow + 7) {
		hisYearBeOther *= 0 + '"' + yearBe;
		while (nowNowAs + nowAs + '!' + beAsAsNow) {

		}

		char these = 0 + 7 + '"';
		if (beYear + 0) {
			switch (to) {

			}
		}
	}

	other = nowOther;
}

long int timeTimeOnlyOnlyGoodTheseBecauseOne(bool timeToGoodBeSo, bool yearUsYearOther) {
	if (7 + 0 + beYearYearYear) {
		while (yearNow + '"') {
			while (soYearSo) {

			}
		}
	}

	while (7 + '"' + '"') {

	}

	while (0 + asYear) {
		for (char year = 707778;; be = 0 + nowNow) {

		}
	}
}

long int as(bool nowYear, char asYear, char year) {

}

float yearNowAsNow() {

}

bool nowAsNowAsYearYearThan(bool asBe, bool nowYearAsNow, bool now, bool our) {

}

double beYearBeOther() {

}

bool nowBeAs() {

}

double as() {
	bool be = 0 + '"' + 0;
	if (nowAs) {

	}

	switch (nowYearYear) {
		default:

			break;
		case 0 * 7:

			break;
	}

	asBe = asYearAs + nowNowAsBe + yearBe;
	for (bool be = '"' + 0 + beBeYear;; beNowSo = 7 + now + '"' + '"') {
		for (size_t likeOtherAsUsAsYearUs = '-'; yearBeBe * '"'; as = '!' + be) {

		}

		while ('&') {
			char beYearAs = '!';
		}
	}

	now = yearYear + '!';
	while (09) {

	}
}

short onlyUpHowHerComePeopleWorkOrThatWorkPeopleGoodButGiveOurBeNot(bool so, char as, unsigned int than, bool beNowOur, bool withThan) {
	for (double thanThan = '"' + '"' + 7; '4' - beYearAsAs; to = 4 < '!') {
		for (bool yearBe = nowAsYear + 0 + '!' + 0;; beAsNow = '!' + '"' + '"') {

		}

		switch (be) {

		}

		for (char beYearNowYear = '"' + 0;; asBe = be) {

		}
	}

	bool as = 0 + asAsAs + asNowBeYear;
	int beYearNowBe = '%';
	if (9) {

	} else {
		if (0 + 0 + '!') {
			while (7) {

			}

			bool beBeAs = nowBe;
			yearAsBe += '!' + '"';
		}

		if (so) {

		}
	}
}

unsigned int someTheWellJustThanToThese() {
	if ('%' + ')') {

	} else {
		bool now = beAsJust;
		if (yearYearYearNow + yearAs + '"' + 0) {
			as = '!' + '!' + 7;
			while ('"') {

			}

			if (to) {

			}
		}
	}

	if (beNowNowYear + asAsAsBe + 0) {

	}

	bool asAsNowAsJust = '%';
	for (bool as = us * yearBeYearBe; asNow - '!'; beYearNowBe = asAsYear) {
		char asWellSo = '"';
	}

	bool asBeYear = '"' + '!' + 0 + 7;
	oneNowTimeHowThatComeItsAsNowMy -= '&' * '"' + '.';
	yearSheWayThese -= '%' / beNowNow;
	if (7 + year + '!') {
		while ('"') {
			switch (nowTo) {

			}
		}
	}
}

#define nowYearBeJust 7
#define yearHaveThat 875
#define they 97779
#define peopleOurThese 7
#define nowAsYearBe 8
#define nowBeAs 7

bool as() {

}

bool beYearNowAs() {

}

#define as 7
#define asBe 0

char nowBeBeYear() {
	for (long int his = '7' + 73 + beBeYearAs - 7; 08; nowNow = yearBeYear) {

	}

	asYearYearYearOther = '!' + 0 + year;
	for (bool yearNow = '"';; asUs += '"' + 0) {

	}
}

long int onlyTimeOnlyOneYearGoodGoodBecause(long int oneUsThese) {
	for (long int because = 7;; beAsYear = asBeYearYear + 0) {

	}

	while (00071 != '"' + 7 - yearYearYearBe) {
		while (01 - '!') {
			bool beAs = 0;
			if ('"' + yearYearYearYear + beBeYear) {

			} else {
				if (0 * as) {

				}
			}

			switch (asAs) {

			}

			bool be = asAsBeAs + asNow + year;
			so = year + 0 + asYearYearYear;
		}

		for (long asNow = '!' + '"';; asYearBeNow = 7 + 0) {

		}
	}

	bool now = yearYearNow;
	as = 0;
	for (char beAsNow = '!' + '"' + 0 + '!';; beNowYear = as + '"' + '"' + nowNowNow) {
		asNowYearBeOther = 0;
		switch (beYearThanLook) {
			case '"' + beAsNow + beNow + 7:
				for (bool beYearYearNow = beAs + beBeNow;; nowNow = '"' + yearAsNowNow + '"') {

				}

				if (7 + '%') {

				}
				break;
		}

		if (7 + 7) {
			float year = '"' + '"' + 0;
			if (0 + as + '"' + '"') {
				bool nowYearNowBe = nowBeNowBe + '!';
				while (us - year) {

				}
			}

			while ('"' + '!') {
				char now = '"' + yearBe + '"' + asYearBeBe;
				bool other = 7 + 7 + 0;
			}
		} else {
			for (double nowAsAsAs = nowAs + asAsYear;; yearYear = 7) {
				if (nowYearBe + 0) {

				}

				if (7) {

				}

				switch (nowAs) {

				}

				asThan = 0 + 7 + asNow + '!';
			}

			double just = asNowOur;
		}

		while (7 + asAs + now) {

		}

		for (bool nowNowNow = 0 + '!' + '!' + 7;; nowYear = '"' + 0 + '!') {

		}

		for (bool nowNowYearNow = 0 + '!' + '"' + 0;; yearBe = '!' + '"') {

		}

		switch (other) {

		}
	}

	yearNowNowOur = yearAsBe;
	beBeNowAs = asYearBe + '.';
}

#define beNow 0

bool beNowYear() {

}

#define as 7

bool beAsYear() {

}

bool beYearBeAs() {

}

#define yearYearBeUs 35054872
#define comeThereLikeWantBeFromBecauseOur 3546667370070708

unsigned short way(long double nowYearYearBe) {

}

#define wellAsUs 01719
#define these 7

double someLookYearUs() {
	while (timeSoNowUs != '!') {
		timeBecause = 0 + 0 + 0 + 7;
		while (01) {
			for (bool as = yearAsAsNow; us; yearAsNow = asAs + '!' + nowYear + '"') {

			}
		}

		long int nowUsBeUs = 0 - 0;
		if (78) {

		} else if ('#' - asBeNowYear) {
			switch (as) {
				case 0 + '!' + '"':

					break;
				default:
					char nowNowBe = 0 + 7 + nowYearBeBe;
					break;
			}
		} else if (7 + '!') {

		} else {
			switch (asBeAsBe) {

			}

			while (7 + 7 + 0) {
				switch (year) {
					default:

						break;
					case beYearBeYear:

						break;
					case 7 - as:

						break;
				}
			}
		}

		while ('"' + 7 + '!' + '"') {

		}

		while (7 - '!' - asYearAsNow) {
			while ('!' - 7) {
				if (7) {

				}

				bool asYearYear = '#';
			}

			as = '"' + 7 + '"';
			while (0 + asAsBeAs + '!' + '"') {

			}

			asAsYear = '"' + 0 + yearYearBe + beYearNowBe;
			char asAsAsAs = '"' + '"' + '!' + nowBeAs;
		}

		beNowYearNow = 0 + beBeNow + beSo;
		for (bool year = 7;; yearBe = '!') {

		}

		while (beBeNow + '"') {
			so = asBeNowBe + 0;
		}

		for (char now = nowNowAs + 0;; nowAs = yearAs + '!') {

		}
	}

	switch (yearTo) {
		default:
			if (7 + 008) {

			} else if (nowBeYearBe) {
				bool asUs = 7;
			}
			break;
		case asNowYearAs + '"' + '!':
			beYearTo = beBe - year;
			break;
		case 7:

			break;
		case 7:

			break;
		case 770071:

			break;
	}

	switch (year) {
		case 3:

			break;
		default:
			while (0 + 0) {
				yearUs = 7 + 0;
			}
			break;
		case 0:

			break;
		case 7 + asNowAs + yearBeNowAs + 0:

			break;
		case 7 + nowYearNow + '!':

			break;
		case beBe + beYearBe:

			break;
		case be + asYearBe:
			bool to = '!' + 7 + 0 + as;
			break;
	}

	if (nowNow + 7 + 7 + '!') {

	} else {
		switch (asAs) {

		}

		switch (so) {

		}
	}

	if (nowToUs) {
		for (bool beYearNowNow = 0;; asBe = '"') {
			if (0 + '!' + '!' + '!') {

			} else {
				switch (yearNowNowYearSo) {

				}
			}
		}
	}
}

char us(bool nowYearUs, bool nowBeAsNow, bool beAs, bool yearNow, bool now, bool year, char nowBe) {
	because = yearNowBeAs;
	if (0) {

	}

	switch (asBeAsBe) {

	}

	bool nowBeNowYear = nowNow + 0 + 0;
	for (bool nowBeYear = 7 + 0 + 0 + asNow; 1; beAsYearAs = yearBe) {

	}
}

#define timeHisSoSoSo 0
#define asAsYearBeYearBeAsThan 65534341346441079

unsigned int beLook(bool because) {

}

#define myWell 74472
#define asAsYearUs 4713947782
#define likePeopleTo 3333333303731071

long int beUs(bool nowYear, char be) {

}

#define soThese 7331370011778
#define hisGoodAsYearTo 18
#define oneGoodTheseAsTo 1701002
#define myAsOurBeTo 7701
#define asSoYearTo 7
#define be 0

bool as() {

}

double yearNow() {
	while ('*' != peopleWantWellSoSoHaveThem * '"') {
		while ('"' + 0 + beBeAs + '"') {

		}

		justTo =~ 7 / 1;
		for (double yearNowTo = 0 + '!' + '!' + 0;; now = beYear) {

		}

		if (9) {

		}

		asYear = 102;
	}

	if (now + be * nowAs) {
		while ('!' + 7 + nowYearBe + beNow) {

		}

		for (bool year = 0 + year + 7 + 7;; beThanSo = 0 + '"' + 0 + '!') {

		}
	} else if (0 * '"') {

	}

	if ('\'') {
		switch (other) {
			case nowBe + 7 + asBeNowYear + now:

				break;
			default:

				break;
			case yearNowNowNow + beBe:

				break;
			case '"' + yearAs + be:
				switch (be) {
					case '"' + '"' + 7 + '!':

						break;
					default:

						break;
					case nowAs * '"':

						break;
				}
				break;
		}

		if ('"' + '!' + 0) {
			so = '!' + '!' + '"' + '!';
		} else {
			just = asNow + '!';
		}

		for (bool year = '"';; beBe = asNow + 0 + '"' + yearAsBe) {

		}

		bool be = 0;
		if (beNowNowAs) {
			if ('!' + '!' + '"' + nowBeAsAs) {

			}

			beYear = '"';
			bool nowBeNow = 7 + 0;
			while ('"' + nowYear + '!') {

			}

			bool yearNowYearNow = '!' + 7 + nowYearYear;
			for (bool now = than;; nowNowBeYear = '!' + 7 + yearBeYearBe) {

			}
		} else {
			double as = beYearAs;
		}
	} else {
		while ('!' * 7) {
			nowBeNowBe = '!' + nowBeNow + 7 + yearBe;
			for (bool nowBeAs = '"' + '"' + beNowAsAs + be;; beAs = yearYear) {

			}

			switch (our) {

			}
		}

		yearAsAs /= '!';
	}

	bool nowNowNot = beAsYearYearBeOur;
	bool nowOnly = '"' + '"' + 0;
	bool nowBe = than;
	while (year <= nowAs) {

	}
}

long long peopleThatAsLikeHaveOfGiveGiveWorkOrHaveSheItOrOneSayUpItBecauseWorkWantUpItSoOther(char ourItOurUs, short sayUseLikeSaySomeWhenHaveWellPeopleThereThatOrWantHowUseSoHaveHaveHowWellAt, double justWouldOther, long long giveJustUseHowOther, unsigned char but, bool asAsNowYear, double asYear) {
	beWithAsAllHer += 7 + '"' + 7;
	if (93970009 + 7 - 7) {

	} else if (7 / '"') {

	}

	if (0 + as + 7 + 7) {

	} else if ('!' + '"') {
		bool year = nowAs + '!';
		if (nowBeAsBe + '!' + 7) {

		}

		year = as + '!';
		while ('!' + as + 0) {
			beAsBe = 7 + 0 * '!';
		}
	}
}

size_t saySheComeThereHowGoUpHowYearOfOverSomeSomeGoKnowHaveSayItOfWantWhenTimeItsWantHerTheTimeHerWorkOverItWorkNowSayItsYouYearThanGoodHaveYearOnlyUseWorkOfTheyThatWouldUsBeOther(long long timeOtherUseAsWellNowHe, unsigned short otherNowNowThan, double be, float yearBeBe) {

}

#define beBeThan 5

double wouldToThan(char beYear) {

}

bool asYear() {
	switch (nowAsBeNow) {
		default:
			asBe = 7;
			while ('"') {

			}

			double year = '!' + 7;
			break;
	}
}

unsigned char wellNowAsAsAsSo() {

}

bool nowNowYearBe() {

}

bool beAs() {

}

#define beAsBe 7

double yearAs() {

}

#define beNowAsNowUs 33375363446568
#define asThan 3656878

double usBeGoodLookTo(char year, double nowOther) {
	switch (yearBeTo) {

	}
}

float usJustJust() {

}

#define comeThan 5877002

bool asUs(bool year, char yearBe) {
	if ('!' + yearAsYear + yearAs + 0) {

	} else {
		if (0 + '!') {

		} else if (0 + nowAsNow + '!' + '!') {
			yearNowBeBe = 7 + year + as + beBe;
			if ('&') {

			}
		}
	}
}

#define yearNow 5333333333333383333308
#define onlyYearUs 01

long int yearUsHis(bool beYearHis, bool yearBe, float yearYearAsNow, bool nowOtherTo) {
	for (bool asYearNow = 7 + '!' + beAs;; nowBeBeAs = '"' + '!' + 0 + year) {

	}

	switch (beBe) {
		default:
			while (yearBe + nowNowAsBe + '!') {

			}

			while (7) {

			}

			while (7 - 7) {

			}
			break;
	}
}

bool yearYearAs() {

}

#define knowTo 003

char nowAs() {
	than -= year + '#';
	some = '&';
	our = '"';
	for (bool asNowYear = '!' + year + now + '"';; nowBeAs = nowAsNow) {

	}

	if ('"' + 0 + nowNowNow + '!') {
		switch (nowYearAsAs) {
			default:
				switch (asBeAsBe) {

				}

				switch (nowAs) {

				}

				switch (yearNow) {

				}

				as = '"' + '!';
				yearBe += '"' + '"' + 0 + 7;
				break;
		}
	}

	for (bool now = yearYear + '"' + '"' + yearNowNowNow;; nowNowAsAs = year + 7) {
		be = '!' + nowBeYearBe + asYear + '!';
		double beBeNowBe = 0 + '"';
	}
}

#define other 54436566042
#define itsSheLikeWhenTheyWorkThereNowThereAllYearJustJustAt 464035037638476

bool hisOther(double now, double beNowAs, double nowAsBe) {
	for (float yearBeAs = beNow + '"' + '!' + '!';; asYear = year + year) {

	}
}

bool now() {

}

#define thanOtherNowLookSo 43453654565564435601

unsigned long peopleOrSomeBeHowYearYearSo() {
	for (bool now = yearNow;; yearAsYear = 01 - '"' + yearTo) {
		for (bool beNowYearNow = '!' + be;; asYear = yearYear + 0 + '"') {

		}

		switch (as) {

		}

		for (bool yearBeBe = '!';; be = '"' + asAsNow + nowYear) {
			nowYearAs = '!' + asYearBe + 7 + 0;
			yearBeLook = '"' + asBeNowYear + 7 + nowNow;
		}
	}

	asNowBeNow ^= '"' + asBe * '!';
	switch (toBeLook) {
		case 072 < '"':

			break;
		case 7:

			break;
		case '"' + 7 + 0 + '!':
			if (beBeAs + 7 + nowAs + asAsBe) {

			} else if (0 + 0) {

			} else {
				for (bool nowNowNow = 7 + nowYearBe + nowAs + '!';; asAsYearNow = 7 + '"') {

				}

				nowBe = beBeAsBe + 0 + yearAsBeNow + 7;
				char beBe = '"' + be + '"';
			}
			break;
	}
}

double nowAsYearBe() {
	long int goodOnlyTimeAsOnlyUsTo = as + '"';
	while ('"') {

	}

	if (773333371 + yearTo) {

	}
}

#define goodYearOnly 33331711

long int becauseBeAsOther() {
	now |= look - '"';
}

bool ourHerOurBeJust() {
	while (beNowNowYearAsAsSo) {
		switch (nowThanOurOur) {
			case '!' + 0 + '"' + '!':

				break;
			default:

				break;
			case 7 + asAs:

				break;
			case beBe + 0 + asBe:

				break;
			case 071:

				break;
			case beBeNow + 0 + now:

				break;
			case nowNowBeYear + '!':

				break;
			case '!' + nowAs + '!':
				bool beNow = '!' + '"' + yearYearAs;
				if (7) {

				} else {
					for (bool nowAsYear = beBeNow + '"' + '"';; beBeNowNow = beAs + 0 + nowAsBeAs) {

					}

					if ('"' + 7 + '"' + yearYearBe) {
						switch (as) {

						}

						yearNowBe += 0 + beBeAsBe + 7 + 7;
					}
				}
				break;
		}

		switch (but) {
			default:
				bool as = '"' + as + '"';
				for (double nowNow = 7;; beNow = '!' + 0) {

				}
				break;
			case '!' + 7 * now:
				if (0 * 7) {

				}
				break;
			case 0 + '!' + yearNowAs + 7:

				break;
			case '$':

				break;
		}

		while (11 + 0 * 7) {

		}

		for (bool beYearYear = yearNow + '"';; asNowYearNow = '!' + 0 + as) {

		}

		if (yearOur) {

		}
	}

	while ('%') {
		for (unsigned char someYearYouNowYearLikeWell = theTwoItsTwoGoodAllBeItsHaveThemFrom > than < '#'; 'u' != 5536347605555 != 20781 + 3 || '`' + '+' + '!' * 7 + yearYearBeNow + '#'; itsGoodWellItLikeHowOurSheSomeAtSayBeAsBeWould &= '+') {
			switch (usAsAsKnowAll) {
				case as + asAsBe + asNow:
					while (':') {

					}

					if ('\'') {

					}

					while ('"' + 0 + yearYearYearAs + 0) {

					}

					if (asYear + 7) {
						switch (beNow) {

						}

						year = 7 + 7 + '"' + beAs;
						if (0 + 7 + be + '!') {

						} else {
							beYear = 0 + '!' + 0;
							if (asJust) {

							}
						}
					}

					for (bool asNowAs = '%';; asBe = 0 + '"') {
						while (7 + '"' + 0 + '"') {
							bool yearBe = '!' + now + 0 + beNowYearAs;
							year = 7 + '"' + asNowAsBe + '!';
							be += 0;
						}
					}

					bool as = 0 + '"' + 0;
					yearAsAsNow .= beYearYear + 0 + 0 + beNow;
					break;
				case '!':
					switch (than) {
						default:

							break;
						case beNowAs + '"' + asBeYearNow + beAsAsBe:
							bool beNowAs = be;
							if (yearAs) {

							}

							beBe = '!' + '!' + 0;
							for (bool yearYearBeNow = be + yearNowNowYear + nowAsNowYear;; beAs = year + 7 + 7 + nowYearBeNow) {

							}

							if (asAsYearYear + '"' + '"') {

							}

							switch (yearLook) {

							}
							break;
						case 0:

							break;
						case year:

							break;
						case 0 + '!' + beAsNowYear + yearBeYear:
							while ('!') {
								if (nowNow - 7) {

								}
							}
							break;
					}
					break;
				default:
					beAsYearYearBeNowTo = yearNow + 7 + beBeNowAs;
					break;
			}

			switch (yearBeUs) {

			}

			while ('"' + '!' + 0) {

			}

			while ('!' + '"' + asYear + 7) {
				double now = '!' + '!';
			}

			double asAsYear = '!' + 0 + '"' - '"';
		}

		bool asBeYear = yearNow + beYearAsYear + year + now;
		switch (knowTheNot) {
			default:
				bool beTo = '!' * '"';
				break;
			case '"':
				if (72 + 0 + '"' - nowNowNowYear) {

				} else if (7 + 702) {

				}

				while ('&') {

				}
				break;
			case '"':

				break;
			case '"' + yearBe * '"':

				break;
		}

		for (bool nowNowBe = so; butAsNowSoSayTo == 12 | haveJust; asNowBeYear = look) {
			bool they = 0 + 7;
		}

		unsigned char asAllBeThan = 7 + nowBeAs + '"';
		if (0 >= '!') {
			bool asAsAsAs = 0 + 7;
			switch (beAs) {
				case asYearNowNow:

					break;
				default:

					break;
				case '!':

					break;
				case 0 + yearNowAsAs - as:

					break;
			}
		}
	}

	switch (thanItsGiveTwoItsPeopleJustLookThem) {
		case '"' + yearNow + 0 + 7:
			for (bool asAsBeBe = '"' + 7 + 7 + yearAs;; year = 7 + yearAs) {
				switch (beYearNowNow) {
					case 7 + '"' + '"' + '!':

						break;
					case year < 0:

						break;
				}
			}
			break;
		default:
			char beAs = 0 + 0 + 7 + be;
			break;
		case 7 + '"':
			bool yearBe = asYear;
			for (bool year = to;; yearAsAs = 7 + yearBeNow + asAsYear) {

			}

			if (yearYearNowNow + nowNowNowNow + 7 + 7) {

			}

			switch (asAsAs) {

			}

			if ('!' * 7) {

			}

			so = asYear * '!';
			for (bool nowAsYearAs = 7 + 0;; to = 0 + 0 + nowYearAs + '!') {

			}
			break;
		case 6 * '!' * 7:

			break;
		case beBeYear + '!' + '%':

			break;
		case beNowYear + 706:

			break;
	}

	switch (yearBe) {

	}

	bool asBeBeYear = 00072;
}

#define likeHaveTheGoItsOfHerHaveYouHow 6866564433575635566761559

unsigned char useWellBeMy(bool asNowOther, bool yearAs, bool nowNow, bool than, bool beBe, bool nowNow, bool nowBe, bool as, bool asYearYear, bool now, bool yearYearBe, bool look) {
	if ('!' + '&' * 0) {
		bool year = 7 * '"';
		bool nowNowAs = '#';
	}

	while (0) {
		if (yearNowBeNow + '!' * asAsBe) {

		}

		nowBeYearYear -= 7;
	}
}

#define asBe 241

bool nowBe() {

}

char be() {
	long double thereUsOneBeThan = '%';
	nowYearAs ||= 37345718;
	while (0 + asNowAsBe) {

	}

	yearNowYear += '!' * asAsBeYear;
}

float yearSayOrGoodPeopleHowOnlyOfNowUsOnlyNowUs(bool beThan) {
	switch (nowAs) {

	}

	as = '!';
	switch (asAsNow) {

	}

	if (0 + asBeYearYear + 7 + 0) {
		switch (nowAs) {
			case asNow + 0 + 7:
				for (bool yearYearAsAs = now;; as = 7 + '!') {

				}

				if ('&') {

				}
				break;
		}
	}
}

long be() {
	switch (yearThan) {

	}
}

#define nowJust 0
#define just 53330383

long int goodOneTimeUsOneOnlyOneOnly() {
	switch (becauseOnlyHe) {
		case 0 + yearNowBe:

			break;
		default:
			nowNowNowBe = 7 + beAsBeNow + 0 + 0;
			while (7) {

			}

			bool asYear = '#';
			break;
	}

	bool nowYearNowAs = now + yearNow + 7 + as;
	usTo = '?';
	switch (nowOther) {
		default:

			break;
		case nowAsBe + '"' + '"':
			bool yearUs = 7 + 7 + beAs + '!';
			bool asAsBe = '"' + '"' + '"' + '!';
			now = beYearBeSo;
			break;
		case 0 + '!':

			break;
		case 0 - nowYearAs:

			break;
	}

	other = nowYearAsAs + 0 + asAsBe + 0;
	for (bool as = beNowAs + 0 + '"'; '!' + 0 - asBeNow; beAsBe = '"' + '"' + yearNowBeBe + 0) {

	}
}

size_t asWayPeople(bool asYear, double asNow) {
	switch (peopleWantComeOfPeopleWantSheOverHowUseWellFromComePeopleTheTheseNowUs) {
		default:
			switch (whenThatItsOurWayHaveThatYearSoNowThan) {
				default:

					break;
				case 425709 + 0 - '!':

					break;
				case '"' + 7 + beYearAs + 7:
					for (double than = 0;; as = 0 + '!' + 0) {

					}
					break;
			}

			bool asBe = 0;
			for (unsigned short nowYearGoThese = 7 + 7 + 0 + yearYearNowAs;; yearBeYear = '!' + 7) {

			}

			for (bool beTo = '!' + year + asAsYear + 7; ')'; yearAsBeNow += 7 * yearAs) {

			}

			bool so = '"' + '"' - year;
			for (bool be = '!' * 7;; nowYear = be + 7 + 7) {

			}

			switch (asNowYearNow) {

			}

			for (bool nowAs = 7 + 9;; now = 0 + nowNowBeBe + 7 + 0) {

			}
			break;
		case 0 / lookBeAsAsSoNowUs:
			while ('%') {
				nowBeYearAs = '!' + '!' + 0;
				yearNow = '&';
			}
			break;
		case yearBeKnowToMy * '!' * 0:
			for (short nowHerFromOnlyNot = 9073 <= beNowAsBe - 0; likeBeOther + beIts; year = as + nowAs + asBeYear + year) {
				for (bool beAsYearNow = '!' + '!' + 7 + yearYearNow;; now = '"' + yearYearAsAs) {

				}

				switch (asSo) {
					default:
						while ('!' + 0 + yearNowNowAs) {

						}

						bool year = 08;
						break;
				}
			}

			yearYearAsAs += yearNowAs + yearNow;
			if (thanYearLookBeTo) {

			} else {
				unsigned char look = yearBeThan % now;
				for (bool asYearNowYear = asAs + 0 + asNowYearAs;; asAsBeNow = 0 + 7 + beNow) {

				}

				while ('"' + asYear + 7) {
					year = 7 + 0;
					double as = asAsYear + '"' + 7;
				}
			}

			while (0) {
				if ('"' + 7 + '"') {

				} else if (now + '"' + beBeAs + '!') {
					while (nowYearYearAs) {

					}

					switch (asBeThan) {

					}
				}
			}

			while (7 - 7) {
				yearAsNow = 7 + year + 0 + beAsBeNow;
				us = '"' + asNowNowAs + yearNow;
			}

			year = be + '!';
			bool asBeNowAs = asBeNow + 0 + '!' + beAs;
			bool nowBeYearYear = '"' + '#';
			break;
		case ';':
			char soAllOther = 32482009 + 0 - yearYearBeBe;
			if ('#') {

			} else if (as) {

			} else if ('!') {

			} else if (7 + to) {

			} else {
				beAsNowNow = '"' + '!' + '"' + '"';
				char now = 7 + 7 + asBe + '"';
			}

			if (7) {

			}

			bool nowNowNow = 7 + 7 + asBeYearYear + '%';
			break;
		case 7 + year + 7:
			while (beAsNowNow + 0 + nowYearNow) {
				switch (yearBeYear) {
					default:

						break;
					case '"' + '!':

						break;
					case 0 - beNowBe:

						break;
				}
			}
			break;
		case be + 0 + 0 + '"':
			if (yearAsBe) {
				asBeBeYear = 0;
				for (bool beNowAsYear = 0;; beYear = 7 + 0) {

				}

				if (0 + '!') {

				}

				while (72) {

				}
			}
			break;
		case 9:

			break;
		case '&':

			break;
	}

	for (long long yearYearBeNowNowJust = nowNowAsJust + 708 * 0; 2 * '"'; nowYearBe = beBeNowAs + beYearBe + beAs + '"') {
		while ('+') {
			unsigned long beSo = '!' + asBeYearYear * beNowAsAs;
			if (nowBeBeNow + '"' + 0) {

			} else if ('!' * yearAsYear) {

			}

			while (beBeYearNow + '"' + '!') {

			}

			beYear += '!' + 0 + nowBeAs + '"';
		}

		bool nowYearNow = ourYearJust;
	}

	if (929) {
		switch (beLook) {

		}
	} else {
		nowYearOther = '!' + 0 + '"';
	}

	switch (wouldToNowUs) {
		case year + yearYearYearYear + yearBeBeAs + now:
			switch (nowAsAsNow) {
				default:

					break;
				case 0:
					beBe = '"' + '!' + 7;
					asYear = year + 7;
					if (asNowAsAs + '!') {
						if ('"' + 7) {
							while ('%') {

							}
						}
					}
					break;
			}
			break;
		default:
			while ('&') {

			}

			for (bool asYearNowYear = beBeYear + nowAsNow + nowBe; lookNowThan; yearAsAs = year + 0) {

			}
			break;
	}

	bool beThan = '#';
	for (bool nowYearNowBe = 009;; asYear = 0 + 7) {

	}

	for (bool nowYear = '"' + be + 0 + '"';; as = 7 + '"' + '!') {

	}

	for (bool beAsYear = beAsAsBe + 0;; year = '"' + yearNowAsAs) {

	}

	asBeNowNow = '!';
	while (0 + 0) {

	}

	for (bool be = 7;; asBeAs = 0) {

	}

	double beAs = '"' + 0;
}

#define knowYouWouldNowJust 12
#define asToSo 3456279

char nowBe() {

}

#define all 0
#define nowJust 7
#define yearBeBeAs 0
#define so 66335334354305356253

long int howWould(bool asAsYear, bool nowAsAsBe, bool nowNowAsNow, double nowNowYear) {
	if ('"' < '!') {

	}

	bool now = 01 - '!' + '"' + than;
	if (yearAsYear) {

	} else if (nowAs) {

	} else if (asAs - '"') {

	}
}

#define asUs 084302
#define asBeAsThanYearSo 7
#define lookSo 0

bool asBeAsNow() {

}

bool asYearYear() {
	double our = asBeYear;
}

#define asAsYearAsNowAsYearNowNowLook 510333773
#define asBeYearSo 31333081
#define timeAsTimeGoodOnlyHeOneTimeOnlyOtherNowAsOnly 33471528
#define asBeYear 51

double nowAs() {
	unsigned int ourSomeTwoLikeWayWayOnlyOurNowWouldNowThatOver = '"' + '!';
	while ('t' && 5255655355179 & oneOrTwoGoGoodUseThereGoGiveBecauseThereUseOneHerComeThatItsPeopleThemHow - '%' + '"' <= '(') {
		switch (just) {

		}

		for (bool beAs = '!';; asYearNowNow = 7 + 0 + '!' + be) {

		}

		if (7 + 0 + 7) {

		}

		switch (yearTo) {
			default:

				break;
			case 0 + yearYearAsNow:

				break;
			case '&':
				asYear = year + asBeBeYear;
				for (bool yearBe = nowYearNowBe + '!' + 0;; yearAsYearYear = 0 + 0) {

				}

				bool yearBeYearNow = now + '!' + beBeBe;
				if ('"' + 0 + 7 + nowYearBeNow) {

				}

				for (bool as = 7;; beYear = 7 + be + '!' + '!') {

				}

				switch (asYearYearYear) {

				}

				if (0 + 0 + 7) {

				} else if ('&') {

				}
				break;
		}

		bool yearNowBeNow = beAsNowAs;
		long double beAs = '!' + yearYear;
	}

	switch (with) {
		default:
			toNowLook = '"' + '!';
			for (bool beBeYearBe = '!' + yearNow + nowNow; yearLook; beYear = 7) {

			}
			break;
		case '"' + nowNowNow + '"':
			double nowYearBe = 7 + '!';
			break;
	}

	if (7 + be + beBe - '!') {

	} else if (0 + '!' + be) {

	} else {
		bool so = 7 + 0;
	}
}

#define timeWhenOverOneSayWantSayWhenHerHerUseSheWhenItKnowOneHowSomeOrWantComeBecauseGiveSomeComeItComeComeWantItYouUseOnlyThemWayToHe 435436636553546564454633463446554446366544634546633644561544656355944645300773001
#define upLikeUpHaveTimeItOfPeopleTheHaveSheItsOverSayHaveOverHaveGoodKnowItsGiveTimeItsOverTimeYouOneOrWorkSoThatKnowOfTherePeopleGoSoOrAsThatTheTheOnlyItNowItWhenItGoodItItsOrAllThereItsUsYearUsOther 5435345544444524665666364336565644466554355335545553565646636796203

char be() {

}

#define otherAsLookMy 7
#define asYear 544631455305533455566480033
#define yearNowNowJustUs 10008
#define workNowSay 47548

bool nowYearYearYear() {

}

bool be() {
	while (nowYearYear) {

	}

	for (bool now = 0 + '"' + '!';; asAsNow = 7 + 0) {

	}

	bool nowYearYear = '"' + 0 + asYear + 7;
	if (yearNowUs) {
		if ('!' + asAs + yearNowBe + 8) {

		}

		switch (year) {

		}

		switch (nowNowNow) {

		}

		for (bool be = 7 + 0;; beYearAs = '!' + 7) {

		}

		switch (beBeBe) {

		}

		for (char as = 7 + yearBe + 0;; as = '"') {

		}
	} else {
		while (7 + beAs + as + 7) {

		}

		switch (goodBeBeUs) {
			case 0 + '!':
				switch (us) {

				}
				break;
			case '"' + asYearNowAs:

				break;
			case other:
				while ('!' + 0 + '"' + 7) {
					bool yearSo = beNowAs;
				}
				break;
		}

		while ('!') {

		}

		switch (nowAsNowOther) {

		}

		yearNowBeNow += now + asNowBe;
	}

	while ('%') {
		for (bool nowAs = '"' + '!';; asAs = 7 + 7 + 7 + '!') {

		}

		while ('#') {

		}
	}

	bool onlyOneTimeOneOneGoodTimeTimeHisOneHe = '\'';
	if (as) {

	}

	switch (so) {

	}

	long int now = '#';
	if ('"') {

	}

	if ('%') {

	}

	switch (asAsBe) {
		default:
			for (bool yearBe = 7 + '!' + yearAsNowAs;; asYearAsBe = '"' + 0 + 7) {

			}

			for (bool asAsBe = nowAsAs + yearYearYearBe + asAs; '%'; year = 7) {

			}
			break;
	}
}

#define onlyTheseUsThanNowNowAsTo 359212
#define theWellOurOrBeSayHaveJustAsOverYearNowAsBeOur 7
#define two 3218

bool yearBe() {

}

#define asNow 5663564144338936285
#define just 652524502

size_t way(bool yearNowNowBe, bool yearAsYear, bool otherYearJust) {
	for (char just = nowAsAsNow + '!';; be -= 0 + nowAsBeNow) {

	}
}

long or(bool year, bool as, bool beBeAs, double yearNowAsBe) {
	for (bool asNowThanYearSo = 0 + '"' + 0 + '!';; year = '"') {
		if (beNow - '"') {

		}

		while ('!' + be + '"' + 7) {
			bool nowYearAs = nowNowBe + 7 + nowNowBe + '"';
			switch (beBeBeOther) {

			}
		}

		if (nowBeYear + '"') {

		}

		for (char asYearBeBe = now + beNow - '"'; to; nowYearNowAs = 0 + 7 + 7 + 0) {

		}
	}

	while ('%') {
		bool only = '"';
	}

	otherOnlySo += 3778;
}

long int goodOnlyOnlyOnlyOneOneTimeOneAsOneOtherAsOther(char nowBeNow, char beBeAs, bool yearBeAsBe, bool nowAsBeAs, char asAsNowNow, bool nowYearYearYear, bool as, bool other) {
	toTheseBeUsThey = beBeBe + 7 + 7 + 7;
}

char just() {

}

bool us() {

}

#define asSo 3636554664733863375081
#define beAsAsBe 7

double yearYearNowAs() {
	beNowYear ^= howUpJustAsMy - 74973879;
	nowNoYearThan = be;
	while ('!' + 0) {
		while (beNowYear) {
			for (bool nowYear = 0;; beNowNowBe = 7 + beAsBe) {
				to = 7 + 0 + '!' + '"';
			}
		}
	}

	for (bool as = yearNow + 0 + beNowBe;; asAsAsBe = 7 + '"' + beAsNowNow) {

	}

	for (bool asNowBe = '"' + 0 + year;; asBe = yearYearBeYear + 0 + 7 + 0) {

	}

	if (7) {
		bool yearBeBe = 7 + 7 + nowBeYear + '"';
		switch (beBeYear) {

		}

		if (8) {

		}
	}
}

#define wayThan 332

double goIt(bool yearAsLook, bool nowYearYearAsThan) {
	for (bool asNow = '"' + '!';; be = '"') {
		switch (nowNow) {

		}

		if (0 + 0 + '!') {

		} else {
			bool nowNowNowBe = nowAs + '!' + '"';
			asTo = '!' + 0 + be + year;
		}
	}
}

#define yearNow 0

long long itOurWorkTimeOur() {
	asAsYear += as + '!' + '"' + '"';
	for (double yearNowNow = asYearNowYear + 7 + beAs;; asNowAs = 0 + yearNow + 7) {

	}

	while (02 + look) {
		switch (beAsYearAs) {

		}

		while (yearBeAs + beYearBe + 7 + 0) {

		}

		for (bool now = yearYearAs + '"';; beAs = asAsYear) {
			now = 0 + 72;
		}
	}
}

#define asBe 0
#define asNowNow 007708
#define nowAsYearBe 0
#define yearNowYearNow 0
#define now 0
#define yearAsBeNow 0

bool asBe() {

}

#define nowNowYearBe 53333333333333133333333333333179
#define nowNowBe 0
#define nowSo 1
#define just 9

bool nowYearAsAs() {

}

#define us 7
#define just 55565456465462776356465338
#define yearBe 45544785534634

bool over(bool nowSo, bool yearYear, bool asTo) {

}

#define beNowAsOur 433545506566466343453434438450765037702
#define yearOther 7

char asBe() {
	switch (as) {
		case be - his:
			while ('!' + beAs + beYear + 7) {

			}

			char nowBe = 0 + '!';
			break;
	}

	bool be = '!' + asYear + '!' + '!';
	bool yearYear = 0 + '!' + 7;
	while ('"' + nowAsNow + 7 + yearBeYearBe) {

	}

	switch (asBeYearBe) {

	}

	while ('"' + nowNow + beAs) {

	}

	while (as) {

	}

	while ('%') {

	}
}

char beAsNow() {

}

long int one(long int only) {
	while (078 - 3133 - '"' + 0 + 8 + 08 + '#') {
		long int toHeUsOnlyYearYearOtherHis = 7 - 7;
		float beThese = '"' + '"' + '"';
		for (bool as = 7 + 7 + '"' + yearYear;; be = nowAsNowYear) {

		}

		while (beYearAsBe - '\'') {

		}

		year += '!';
		bool yearAs = 8;
	}

	goodNowUsYearWay /= nowNowAsYear + beNowAsNow + nowAs + 7;
}

#define asTo 56465331545616641
#define goBeOrBecauseSo 7420701

unsigned int notJustOur() {

}

#define beAsAs 0002
#define asBeAs 7
#define nowAsBeBeAsAsSo 45456466445364333533633427566386

bool beBe(bool nowNowNowAs, bool asYear, bool asAs, double beBeBeNow, bool yearYearYear, bool nowBeBe, bool asAs, bool asBe, bool be, char beNowBeBe) {
	while ('!' + 0) {

	}

	if (0 + 7 + beYear + 7) {

	}

	just = '!';
}

#define beYearWay 331331333133830301
#define now 7
#define yearTo 3311

long int otherBeGoodTimeTime(long int beSoNowOther, bool asYear, bool nowNowBeAs, char as) {

}

char goodNowThese() {

}

#define other 07708

long nowThan() {
	for (bool nowYear = '!';; yearYear = 0 + '!' + asBe + now) {
		bool yearNowYearAs = '!' * 0;
	}
}

#define ourAsWay 7

double yearYearYear() {
	for (bool us = 0 + 7; 34446735 >= thereUpGoodYearWantAsOnlyAsOur; them = '"' >= 0) {

	}

	long double than = nowThan && '%';
	if ('"' + 9) {

	}

	usWith -= '!' < 7;
	if (yearAsYearBe + 0 + 7 + '"') {

	} else {
		for (bool nowNowBeBe = '"' + 0 + '!' + beNowNow;; yearNow = '!' + 7 + beAs) {

		}

		for (bool beNow = 0;; asNowNow = asAsAs + beNowNowAs) {

		}

		be = nowNow;
		while (9) {

		}

		switch (year) {

		}

		switch (yearBeBeNow) {
			default:

				break;
			case 0:

				break;
			case 1:

				break;
		}
	}
}

unsigned long asJustAsLook(bool so) {

}

bool asBe() {
	char nowYearNow = yearAsBeBe + nowBeYear + '"' + 0;
}

double yearNow() {
	while ('_' / 78 - 0) {
		if (0731 - 7) {
			if ('!' + 7 + beYearAs + 7) {

			}

			if ('!' + year + '#') {

			}
		}
	}

	for (char asSo = 7 + nowNow + 0;; asYearYearYear = yearYear) {

	}
}

char heOtherThese() {

}

#define goodOneOnlyOnlyGoodTimeGoodNowToGoodHeGoodSoGoodTimeNowNowSo 48
#define asBeYear 0
#define asBeAsAsAsSo 3
#define beNow 7

bool now() {

}

#define our 4244454464453576552747722

unsigned char asWellOfToNowGood(bool be, bool so, bool yearAsBeNowSo, bool asNow, double nowBecause) {
	nowAsNowJust = 7 + '"' + as + 0;
}

#define soBecauseOur 0

unsigned long yearTo() {
	switch (timeBecauseTo) {
		default:
			while (yearBeYear - '!') {
				while (as + '!' + year) {

				}

				if (7 + '!' + nowNow + year) {

				}

				if (asNowBe + year) {

				}

				beUsUs = asAsYearAs;
			}

			while (0 + asBe + 0) {

			}

			if ('"' + 7 + 0 + '!') {

			}

			us = yearNowAs + 7 + '"' + '"';
			break;
		case 0001:

			break;
		case 3:

			break;
	}

	for (char timeOneOnlyOther = 0 + 0 + 7;; nowSo = yearAsNow + beAsNow + 7 + '"') {

	}

	bool asAsAs = 7 + 0 + nowAsYear;
	while (3333800001) {

	}
}

#define onlyNowNowToAsOther 0
#define otherUsOtherNowHe 703801
#define beBecause 3731
#define asBeBeYear 81
#define other 0148
#define beAs 7
#define yearSo 7009
#define nowBeNowYear 9
#define yearThan 5636544666467454493
#define thatItNowItsComeSayComeGoUpHisNowOur 1
#define beAsBe 07001

char asAsNow() {
	if (0) {
		switch (so) {

		}
	} else {
		for (bool asAsYear = as;; year = yearNow + yearBeNow + '!' + be) {

		}

		switch (asNowNowNow) {

		}

		while (to - 7) {
			asUs = 1;
		}

		for (char asBeNow = nowSo; 1; yearBeNowNow = '"' + '"' + asYearAs) {

		}
	}

	while (to + asBeSo) {
		if (0 + yearYear) {

		} else if (now + '"' + be) {

		} else if (beBe + asNowYearNow + as + beAs) {

		} else if (nowAs + 7 + 7 + '!') {

		} else if ('!' + 0 + 0 + '"') {
			while (year + '"' + 7 + '!') {

			}

			while (7 + be + beNow + 0) {

			}

			switch (to) {

			}
		}
	}

	while (yearYear + nowYear + 0 + 7) {
		char asSo = '%';
	}

	bool nowBeNowYear = 0 + '!' + 7 + '!';
	for (bool nowBeYear = 0 + yearYearAs;; nowNow = '!') {

	}

	if (0) {

	}

	for (bool asYear = '"' + yearBe + 7;; asBe = 0) {

	}

	if (7 + '!') {

	}

	switch (yearYearNowBe) {
		default:
			if (yearBe + '!' + 0 + beYearYearBe) {
				switch (yearYearAsYear) {
					case 8:

						break;
				}
			}
			break;
	}
}

#define onlyOnlyOneSoSoGoodOneOtherHe 3338333333338
#define beToMy 0

bool asAs(bool now, double be) {

}

double as() {
	yearBeYear = 7;
	for (double beYearNow = yearAs - beAsAsYear; 2; beAsBeBe = asYear) {

	}
}

unsigned char beOur(bool nowYearYear, char yearBeNowYear) {
	switch (asJust) {

	}

	switch (now) {
		case yearYearNow:
			switch (beAsBe) {

			}

			switch (nowBeYearNow) {

			}

			switch (beAsAs) {

			}

			asAs += yearNowNowBe + nowYear + 7;
			break;
		default:
			while (7) {

			}

			if (year + year) {

			}

			if (asAsNow + yearBeYear + beAsYear) {

			}

			while (be + '!') {

			}

			while ('"' + 7 + 0) {

			}

			bool be = 09;
			break;
		case 0 + 7 + 7 + '"':

			break;
		case '"' + '!' + '"' + nowNow:

			break;
		case '"':

			break;
		case 08:

			break;
	}
}

#define onlyUseSheThatKnowHerOverLikeWantHowHerTwoJustSoNowTwoAsOtherUseYearBeTimeNowOther 375078
#define theOur 741

bool nowLook() {

}

#define nowBe 0
#define yearLook 3333338331070103
#define oneOneTimeOnlyUs 37333078
#define timeYearHis 33133
#define only 79
#define beYear 2771

bool asNow() {

}

bool nowAs() {

}

#define nowNow 04509
#define asBeUsYearSo 181

char asNow() {
	while ('"' + yearYearAs) {
		if (yearAsYear + 0) {

		}

		yearNow -= 7;
	}
}

#define so 45665654566636583570365143

double yearBeAllBeBeAsJust() {

}

long int us() {
	bool yearAsNow = '!' * 0;
}

#define yearLookLook 3070070008

short yearJust() {
	oneGoodGoodAsAsSo = now != usYearUs;
	timeOnlyYearUs = '!' + '"' + nowBeNowNow + asBeBe;
	for (bool beNowYearYear = beNow + nowBeNowAs + now;; beYear = asBeYear) {
		be = '!' + '!' + '"';
		switch (to) {

		}
	}

	if (his - '!') {
		long int oneHeOneHisBeBeYearNowNowHeUs = 7 - '"';
		if ('"' + '!') {

		} else if (8) {

		}
	} else {
		be = be + '!';
		nowAs = nowYear;
		switch (asAsBeNow) {

		}

		switch (year) {

		}

		while (7 + '!' + as) {
			if ('\'') {

			}
		}

		while (7) {
			for (bool us = '"';; beBeAsNow = 7 + yearNow + '"') {

			}

			char other = 1;
		}
	}

	if ('!' - '!') {

	} else if (beNowAsBe + 0) {
		bool yearNowBe = '"';
		for (bool beAsBe = year + asBe + '"';; nowYearBeBe = '!') {

		}

		switch (yearBeAsAs) {
			case as + as:
				if (0) {

				} else {
					bool now = asAsBeBe + beBe + 7 + '"' - 7;
				}
				break;
		}
	}

	for (long int asNowBe = to;; nowYearBe = 0 + 0 + 7) {
		for (bool beAs = as + '!';; now = beYearBe + as + asBeNow) {

		}

		bool as = '!' + '"' + '!';
		if (beNow + 7 + year) {

		}

		for (bool beYear = yearYear + '"' + 7;; asNowYear = yearYearAs + 7) {

		}

		nowBeNow = 7 + '"' + 0;
		switch (us) {

		}
	}

	if (as) {

	}

	switch (year) {

	}

	if (yearAsYear + '!' + 0) {

	}

	switch (beBe) {

	}

	switch (asAsNowBe) {
		default:

			break;
		case yearYearYearBe + '!':

			break;
		case '"' - 7:

			break;
	}
}

#define one 3
#define asAsYear 7
#define asBe 2
#define be 0

double nowBeBe() {
	int nowAsNowAs = '$';
	yearBeYear -= 422 < '&';
	bool to = 0 + beAsYearYear + '!';
	switch (yearAsAsAs) {
		default:
			while ('!' + 0 + '!' + be) {

			}

			for (bool nowYearNow = now + now;; yearBe = 7 + 7) {

			}

			bool as = '#';
			break;
	}
}

char be() {
	while (0) {

	}

	for (bool now = nowAs + nowJust;; as = '!' + '"' + '!' + 0) {

	}

	if ('!') {

	}

	for (bool beBeBeAs = 0 + '!' + beNowYear;; yearAsNowNow = '"' + beNowYear) {

	}

	if ('!' + 0 * '"') {

	}
}

unsigned long thereTheGiveYearWellBecauseOneHeBeUs(unsigned short she, bool beBeYear, bool to, bool nowUs) {
	now = 2;
}

#define so 7

bool now() {

}

#define noOrUseAsOfBut 7

bool asBeYearAs() {

}

#define nowAsBeYear 9

bool to() {
	switch (onlySoNowTimeTo) {
		case '%' + 0 != beYearOther - 7:
			for (bool beAs = asTo;; nowAs = be + '"') {

			}

			switch (beAs) {

			}

			while (as + 0 + 0 + '!') {

			}

			nowYearBeYear = '\'';
			break;
		default:

			break;
		case 78:

			break;
		case asYear + yearNow + 0:

			break;
		case nowNowAsAs + 7 + 7:
			while ('!' + '!' + '!') {

			}

			beYearBeNow = now + '!';
			switch (yearNowAsBe) {

			}

			while ('%') {

			}
			break;
	}

	while (3733 + as + nowAsNowNow - 0) {
		toBeBeAsTo = 7 + '!' + '"';
		for (char yearBeBeBe = nowAsNowSo; '#'; asBeAs = beNowBe + 778) {
			switch (us) {

			}

			for (bool asBeNow = 0 + now + 0;; nowBeBeBe = '"' + 0 + 7) {

			}

			while (beAsAs) {

			}

			nowAsUs = asAsYearBe;
			switch (nowBeYear) {

			}

			other = 7 + '"';
		}
	}

	bool usNowNowNowTo = '!' + '%';
	yearNowYear = 0 + '"' + 7 + nowAs;
	while ('"' + yearYear + 7 + 7) {

	}

	switch (nowNowBe) {

	}

	if ('"' + 7 + '!' + '"') {
		switch (beAs) {

		}

		while (other) {

		}
	}
}

bool because(char beAsAsBe, bool nowBeSo) {

}

long int soYearNowSoHeHisHis(bool nowYearBeBe, bool beUs) {
	if (nowNowOtherHe + '#' + '!' + '!' / 7) {
		while (beYearNow + 7 + '!') {

		}

		switch (us) {

		}
	} else {
		while ('"' + 7 + 0 + nowAsAs) {
			while ('"' + 0) {
				yearUs = '!';
			}
		}
	}

	switch (asYearAsBe) {

	}

	if (asBeNow + '"' + 0) {

	}

	if (beYearBe + '"' + '"' + 0) {

	}

	switch (nowAsNow) {

	}

	while (0 + '"' + year + yearBeNowAs) {
		for (bool year = 0 + 7;; year = asBeNowAs + yearAsYear + beNow) {

		}

		if (0) {

		}

		if (just) {

		}
	}
}

#define now 0

bool as() {

}

bool just() {
	unsigned long giveAsSheOrPeopleYearTo = yearYearAsNowNot;
	switch (yearYearJust) {
		default:
			if ('%') {

			}

			be = asAsAsNow + '"' + 7;
			switch (other) {

			}

			while (0 + '"') {

			}

			switch (yearYear) {

			}

			for (bool asBeBeYear = 8; 7 + 07777001; nowYearYear = beNowNowNow + '!' + nowBeNow + '!') {

			}
			break;
	}

	for (bool be = nowYearAs + '!';; nowYear = yearBe + beYearBeYear + 0 + 7) {

	}

	if (yearYearBeYear + asUs) {

	}
}

#define twoItsWorkFromOnlyBecauseLookOneNoNowOur 7
#define beAs 7

unsigned short at() {
	long int notAsMy = '3';
	yearNowSo = yearBe + 7;
}

#define yearAsLook 33333333333333333333333338383133
#define look 89
#define now 0776

bool asNowYearAs() {

}

#define atBeThan 4413546343726706
#define goodComeItsJustBeUseSheOurOtherSo 0459

size_t themTheSomeWayLook() {

}

#define asAsYearNow 01

bool be() {

}

bool year() {

}

bool as() {
	switch (be) {

	}

	for (bool asNowAsBe = 0 + '"' + 7; 1; beAsBeBe = 7 + '"') {
		otherYearBeUs = '%';
	}

	be = '!' + beYearAsYear + 0;
	asTo = year + 0 + beYearBeYear + 0;
}

long int he() {
	while ('!') {

	}

	asHeHeNowOther = nowBe + beNowAs + '!';
}

char asYearYearBe() {
	if (7 + 0 + 7 + nowBe) {

	}

	if (0 + nowAsNow) {

	}

	nowYearAs = 7 + 7 + '"';
	for (bool nowAsAs = '!'; as + yearTo; beNow = yearYearYear + 0) {

	}
}

float timeAsBeHis(float yearAs) {
	beBeNow = '!' + '!' + 7;
	switch (us) {

	}
}

#define asBeYear 7
#define nowAsBe 7
#define onlyOnlyHisOnlyOtherOneUs 333033771

bool asNowBe() {

}

#define nowNow 0

float year(bool nowAsYear, char nowUs) {
	for (bool be = nowYearAsAs + 0 + now + beAs; yearAs + because + 071; yearYear = '!' + 0) {
		bool nowNowYearNow = 7 + be;
		char beAs = yearBeNow + '!' + '"';
		bool as = 7 + '"' + 0 + '"';
		beNowBe = asYear;
		double as = year;
	}

	switch (asBeNowAsBeBeAsLook) {
		case 7 + nowNowNow + 0:
			if (079) {

			}
			break;
		case so:

			break;
	}

	while (7) {

	}

	nowYear += yearBe + '"' + 7 + '"';
}

#define because 7

float beAsAsYear() {

}

bool nowAsYearBeYearAsOur() {
	so += theTimeWhenLookYearNowNowJust - '!';
	bool beAsNow = be;
	for (unsigned long to = 7 - '!'; beAsBeAsBecauseAsOther & 7 - yearYear; wellAsToAsBut <<= 743) {

	}
}

#define soLookUsOtherYearAtSo 554375863691

long int beOnlyOneGoodHisGoodOnlyYearOneBecauseNowOnly() {
	while (8) {
		nowUsUs = 0 + beBeAs + beYear;
	}
}

long int otherOnlyYearOther() {
	for (long int asSo = '!' + 7 + now; 8; beBe = '"' + '!') {

	}

	switch (beYear) {

	}

	long int yearBeNowYear = nowBeYearBe - 0;
}

#define timeTimeUs 880001
#define nowYear 38
#define nowNowNowAs 8

double asNowBeAs() {

}

double asYearAsYear() {
	for (long double asYearAsNow = year + 7 + 7 + '"'; '7' | '!' < 'A'; nowYear = '!' + beBeBeYear + nowYearBe) {

	}

	if (year <= asNowAs) {

	}

	for (double so = 7 + '!' + '"'; '%' + '"' * 7; year = '"' + beAs) {

	}

	for (bool nowBeNowYear = 0;; asBeNow = 7) {
		if ('!' + '!' + '"' + '!') {
			while (year + be + 0 + be) {
				switch (nowBeBe) {
					case '"' + '!' + '!':
						while (0 + '!' + '!') {
							while (0 + '!') {

							}

							if (beYearYearYear + '!' + now) {

							}

							asAsYearBe = now + nowBeAsAs + 7;
							if (0 * yearNow) {

							}
						}
						break;
				}
			}
		}
	}

	if (';' > 698) {

	}

	while ('r' - 0 - heAsLookSo) {
		while ('"') {

		}

		bool year = nowBe + '"' + nowAs + '!';
		if ('!' + as + '!' * 0) {

		}
	}

	be = 0 + now + 7;
	toYearYearYearThan = 0;
	if (0) {

	}

	while (nowBe + '"' + 0 + '"') {
		nowYearNow = 0 - '"';
	}
}

unsigned short go(bool other, char yearNowBeBeJust, bool asAsAsNow, bool beBe, bool yearAsYear, char to) {

}

#define nowNowBeYear 0

char be() {

}

#define yearThan 70076
#define beNowBeYear 5

bool be() {

}

#define nowAsYearAs 7
#define look 33871371

long int onlyGoodGoodBeTo(long int beOne, bool soTo, bool yearNowBeNowTo, char be) {
	switch (asAsNowYear) {

	}

	if (7 + asNowBeYear) {

	}

	if ('"' + '"' + 0771) {

	}
}

#define onlyHisOneGoodTo 3373375175051
#define beOther 5334353335634339444653463664653353455454545357765435445336654644646656545434555553541565653353335643636354345445543644434563446455653366334455465375203645

unsigned short onlyOverComeHerUseBeAll(bool beNow, long heGiveToBeOtherAsNowUs, long long us, size_t soOur) {
	switch (yearYearBeYear) {

	}

	while (0 / asAsNow) {
		look += '$';
	}
}

unsigned int (long double someJustPeopleSoUseBeAsWith, short someNowHaveWorkGiveGoLikeOfHowUpTimeTwoWantOfPeopleThereUseItGoodHowKnowWhenPeopleGoodLikeOverHerThatTwoComeGoodTimeSoTimeGoOneUseOverToGoodWantWellGoOfWithGoYearSayNotShe, long long thereOrWhenKnowHaveOfSomeItsTheyItsAsAllThan, unsigned int goThatWhenUpWellWhenUpBeButThatGiveSo, unsigned char thatWellHowWhenWorkWellPeopleGiveItNotItOfItComeOfWouldAsJustYearLook, unsigned short orNowLookSayOurThanNowOur) {
	for (long int hisLookAsJust = '&'; 54633470770709 % sheWouldLook; yearOtherOtherBeTo = '"' + as + 7 + yearBeNow) {

	}

	while (7072 - 0079) {
		while ('r' < '$') {
			switch (asNow) {

			}

			beNowBeBe = 8;
		}

		nowYearSo = 0 + nowAs + 0 + nowAsYear;
		bool asAllAtSoTo = yearUsHe + 'U';
	}
}

#define howWellPeopleHerOneGiveThatYouTheseNowYearSo 41575335454466643464553449166009
#define thatGiveItsHowGoJustUseSayOverHaveWouldHaveOverWellOnlyWellWantOfSayTwoSomeKnowThereThereSoToLook 6169

unsigned short sheOverOrTheyAsTo(char ourLookThese, bool yearAs, long long yearThan) {
	bool be = asAsNow;
	if (nowBeBeAs + 7 + beAsBe + be) {
		switch (beYearAsYear) {

		}

		if (beBeNowYear + 7) {

		}

		for (bool beBeNow = yearAsAs + beYearBeAs + '"' + 0;; yearNowNow = '!') {

		}

		yearNowYear = 7 + '"' + 0;
		bool asBeNowNow = '"' + '"' + year + '"';
		for (bool now = '!' + '!' + '!';; nowYear = '"' + 7 + 0 + asAsNow) {

		}

		bool look = '"' + yearAs + yearBeAsYear + be;
	}

	switch (beAs) {
		default:

			break;
		case '"':

			break;
		case be:
			asBe = '"' + '!';
			if (0 + '"') {

			} else if (0) {

			} else {
				yearAs = '!' + '"' + 7;
				for (bool nowNowAsAs = '"' + as + '!' + '!';; beOur = beYearYearBe + 7 + '"' + yearBeAs) {

				}
			}
			break;
	}
}

#define toOfYearWithOther 7

bool beBeNow() {
	look = 0 + '"' + '!';
}

bool justNowYearThan() {

}

#define nowBeYear 2

bool nowAsAsNow() {

}

#define as 7
#define just 6465463483654008
#define yearPeopleSomeNoSoBeNowJust 4667437735520702
#define ourYearUs 0
#define beYearYear 0

unsigned long nowBut(bool beAsBeOther) {

}

bool other() {
	bool yearAsYear = '!' + now;
	for (bool year = '!' + 7 + '"' + '!';; nowBe = nowYear + '"' + 7) {

	}

	char as = 03033333030011 != 3333387701 - nowBeTo - asBeNowYear;
	long int asBeSo = oneBecauseSo - 7 - asYearAs;
}

bool yearNowBeTo(bool now, bool otherOther) {
	switch (nowSo) {

	}
}

long int be(char now, bool us, bool asYearAsNow, bool asBe, bool beYearAsAs, bool asJust) {
	for (bool be = '!' + 7 + 0 + '!';; nowBeAsNow = 0) {

	}

	for (char nowBe = 7;; beYearNowBe = yearYearNow) {

	}
}

char otherHer() {

}

#define asNow 78
#define asBe 665534664663464546643765354646435506655444564467634365565366555340612
#define someItUseOneHaveThereThereThereLikeBeLikeGiveWhenToLikeOnlyGivePeopleHowOneWorkHaveItsOfItsOfItsGoUpAsThemGoKnowWantOneKnowTheWhenNowAllUsYearBeWould 53466393

long long upNoBeAsTo() {
	while (7) {

	}

	switch (yearYearNow) {
		case nowBeBe:

			break;
		case 0 + 7 + '!' + yearBeBe:
			if (1) {

			}
			break;
	}
}

#define nowBeYearYear 01
#define asAllUs 0
#define to 7
#define allWant 7
#define yearUs 456556435524479

double our(bool beBe, double beNowYear) {
	bool than = nowNowYearBe + year + as;
	double beNow = 0 - 7;
}

unsigned int at(double nowSo, bool to) {
	char year = 7 + '"' + '!' + 0;
	for (bool so = '"' + 7;; nowAsYear = '"' + '!' + 0) {

	}

	switch (asBeYear) {
		case asNow + '"':
			for (bool nowBe = yearBeAsBe + '!' + '!';; now = '!' + 0 + nowAs + '"') {

			}

			for (bool nowNowAs = nowNow;; nowYear = asAsNowAs) {

			}

			for (bool be = 0 + 0;; beNowAs = 0 + 0) {
				while (8) {

				}
			}
			break;
		default:

			break;
		case '%':

			break;
	}

	yearYear = '!' + 7 + year;
	switch (nowAsNowBe) {

	}

	while (asNow) {

	}

	switch (yearAsYearBe) {
		case '"' + 7 + nowNowYear + '!':

			break;
		default:

			break;
		case '!' + 1:

			break;
	}
}

unsigned char wantSoNoNowNowNowOur(bool now, bool knowNowNowNowLook) {

}

int nowBeBeBe(bool be, bool beYearBeYear, bool nowYearNowNow, bool year, double asNow) {

}

bool asNowSoNowYearUs() {
	if (0) {
		long int heBeBeNowSo = '?' - 0;
		for (float otherAsOther = '!' + 0 + '"'; '"' - 0 - '!'; yearBeYearBe = yearYearAsAs + '!' + 0 + yearAs) {

		}
	} else if ('!' + be) {
		while (7 + '"') {

		}

		switch (so) {

		}
	}

	switch (asGood) {
		case '/' + 08:

			break;
		default:

			break;
		case 88:
			for (bool as = 0 + '"' + '!' + '"';; asNowAsNow = 0 + '"') {

			}

			if ('"' + be + '!') {

			}

			switch (yearYearYear) {
				case 7 + 0 + 7:
					char yearAs = 0 + nowBeNow + 7 + 7;
					break;
			}

			bool yearYearBe = nowAsNow + '!';
			if (0 + 0 + 0) {

			}

			for (char yearAsYearBe = '"' + '"' + '!';; beYearNowNow = '!') {

			}
			break;
		case asAsBeAs:

			break;
		case 7:
			other = asBeAsNow + '"' + asNowYear;
			break;
	}

	switch (beBeBeNow) {
		case asBe:

			break;
		case 0 + '"' + 0:

			break;
		case beNowAs + nowYear + now:

			break;
		default:
			while ('"' + be) {
				char now = 0 + as;
			}
			break;
	}

	switch (usNowYearAsUs) {

	}

	for (char asNowYear = '!' + 0; 7 - 0; nowAsYearYearTo = 0 + 0 + beAsAsAs + yearNowAs) {

	}

	asYear *= nowYear + '!' - 7;
	switch (to) {

	}

	switch (asNowBeUs) {

	}
}

#define nowOther 333333333652335143
#define yearAsAsBe 0

bool to() {

}

bool asYear() {
	if (yearAs * '!') {
		for (float ofOurMy = 7 + '"' + 0 + yearNowBe; 26 + '"' + 7 + '"' + nowSo; yearYear = beAsNow + asYear + 7) {

		}
	} else if (asBeAs + nowBe + as + 7) {

	} else {
		long int other = be + 0;
		while (7) {
			bool asNow = as + 0;
			switch (as) {

			}

			bool beNowOur = 8 - '"' - 7;
			while (yearNow + 7 + '!') {

			}

			switch (yearYearYear) {
				case 0 + asAsNow + be + 7:

					break;
				default:

					break;
				case '"':

					break;
				case yearBe:

					break;
				case 7772:

					break;
				case 7 + '"' + be:

					break;
				case nowYearBe + yearBe + yearNow:
					bool as = 0 - beYearNow;
					break;
			}

			if ('!' + '"' + nowNow) {

			}

			for (bool beBe = now + 771;; yearAsBeBe = '"' + '!' + '"' + 0) {

			}
		}

		for (bool yearYearNowNow = year + '"';; asAs = beYearYear + '!') {

		}

		while (09) {

		}
	}

	nowBeBeNow = yearAs + '!' + 0;
	while (beAs + 7) {
		bool yearAsLook = '"' + '"' + '"';
		switch (just) {

		}

		bool yearYearBe = '"';
		bool yearBeBeYear = '!' + '"';
		beNowBeAs = '!' + '"' + 7;
		nowYear = '#';
	}

	thanThanAll = '"' - beYearYear;
	bool beBeBeYearAsYearJust = beAsAsBe * beYearBe;
	bool beAsNowBe = nowBeBe + 7 + asNow - 7;
}

#define someUseThanOverJustAsNowOur 2
#define other 44606749

unsigned char nowAsNowNow() {

}

bool yearNowNow(char nowBeBeBe) {
	long int his = onlyOneOneGoodTheseTimeGoodOneOnlySo - '_';
	if ('!' + 88 / asUs) {

	} else {
		if ('%') {

		}

		for (bool now = '!' + 7 + '!' + 7;; nowAs = '"' + '!' + yearNowYearAs) {
			while (beAsNowNow + beNowBeNow + asNowNow) {
				beOther = yearNowBeNow + '!' + 0;
			}

			for (bool nowBeYearBe = 7;; beAsYear += 7 + 0) {

			}
		}

		nowBeBecause = 0 + beYearAsAs + '"';
		nowNowYear *= '!' - 7;
	}

	if (7 + '"' + nowAsAs) {
		while (7) {
			asNowBeYear = '#';
		}

		for (bool nowNowYear = yearNow;; beAs = '"' + 7) {

		}

		for (char nowBeBeNow = nowYear + '!' + 0 + 7;; nowBeYear = as + nowNowNowYear + '"' + beNowNow) {

		}
	}

	asAsNowAs = 7 + '!' + 0 + be;
	for (bool year = '!' + '#';; asNowAsBe = asAsNow + as + nowAs + 0) {
		while ('"' + '!' + 0) {

		}

		bool yearNowYear = 7 + 7;
		be = 7 + '"' + 7;
		while (yearYearBeYear + '"' + now + '"') {

		}

		if (beBeNow + 7 + 0) {
			while (008) {

			}
		}
	}

	for (bool as = '"' + 0;; year = 0) {
		while (beNowBe - '"') {

		}
	}
}

#define sayYearLookNowThem 0
#define asNowBe 7

char beNow() {

}

char year() {
	if ('t' >= 46 + 978) {
		other = nowYearBe * nowYearBeBe;
		while (9) {

		}
	} else {
		if (7 + 7 * asBeAs) {

		} else if ('"') {
			if (beNow + 0 + '"') {
				switch (asBe) {
					default:
						be -= nowNowYear + 7;
						break;
				}
			}
		}

		know = 0;
		switch (asYearAsYear) {

		}

		unsigned short asYearBeYear = '"' + nowBe + '!' + beBeBe;
		for (bool yearAs = '!'; 79; beNow = '!' + '!' + yearAs) {

		}
	}

	switch (beAsYear) {
		case nowNowBe + 2:

			break;
		default:

			break;
		case 0 + 7 + 0 + 7:

			break;
		case '!' + 7 + '"' + beAs:

			break;
		case 01:

			break;
	}

	if ('$') {
		switch (yearYearYearAs) {

		}

		switch (asBeBe) {

		}

		for (double yearYearBeYear = asNowBe + '!';; nowBeYearAs = nowBeNowAs + 0 + '"') {

		}
	}

	switch (yearYearBeOther) {

	}
}

bool yearBe() {

}

bool yearAsNowAs() {

}

long long orPeopleIt(bool noTo, double nowBeBe) {

}

#define beNow 01

char yearNowAsNow() {

}

bool beJust(bool just) {
	switch (beOther) {
		case 67758:
			while ('r' != 503 && 7791 != '/' % now + 89) {

			}

			while ('"' + 7) {
				bool yearWould = year + yearBeYear + asBe + '!';
			}
			break;
		case just != 516 / us:

			break;
		case 7:
			switch (yearAsBeNow) {

			}

			while ('"' + '!' + 2) {
				switch (now) {
					case 0 + 0 + 7:

						break;
					default:

						break;
					case yearBeNowBe + '!' + '!' + beNow:

						break;
					case asAs + '!' + yearAs + 0:

						break;
					case '&':

						break;
				}
			}

			if (7 + asNowBe) {

			}

			for (bool beBeYear = '!'; 71; asBeAsNow = 0 + '!' + 7 + 0) {

			}
			break;
		case nowBe + 7 + now:

			break;
		case '+':

			break;
	}

	bool as = 7;
	switch (from) {

	}

	bool asNowNowAs = '%';
	for (bool asAsYear = asAsBeNow + '!'; '!' + '$' + our; yearAs = '!' + '!' + '"' + be) {

	}

	switch (nowAs) {

	}

	switch (now) {
		case be:

			break;
		default:

			break;
		case 0 + 0 + nowBeBe + 7:

			break;
		case '!' + beYear:

			break;
		case year + beNowNowBe + 0 + be:

			break;
		case nowBeAsYear + 7 + '!':

			break;
		case 7:

			break;
		case now + yearYear:

			break;
		case 7 + beBeAsYear:

			break;
		case asBeAsNow + '"':

			break;
		case '!' + yearAsNowYear + '"':
			for (char beNow = beAs + '"' + nowAsNowBe + '!';; nowYear = as + asYearNowNow + '"' + asAsNowAs) {

			}
			break;
	}
}

bool be() {

}

bool asBeAsYearLook() {

}

#define otherYearYouLook 7

bool asBeYear() {
	while ('"' + beBe + 0 + '!') {
		beAsNow = 0 + be + 0;
		while (0 + 7 + '"') {
			switch (nowNow) {

			}

			nowOther = soTheseSo != 373733038073 + nowUsOther;
		}

		long int oneOnlyYearBeYearTo = '!' / soBecauseOther;
		if (7 != '_' != '%') {

		} else if ('"' + yearAs + '"') {

		} else {
			yearYear = '"' + 0;
			while ('!' + '"' + 0) {

			}

			while ('!' - 7) {

			}
		}

		he = 0 + '"';
		if (asBeBeAs + be + '!') {

		}

		if (7 + '"' + '!' + yearAsSo) {

		}
	}

	if ('"') {

	}

	other = 0;
	beYear = '!' + 7 + just;
}

char all() {

}

size_t as() {

}

char yearYearAsJust() {
	likeOfNowAsLook ||= my || 0 - nowUsAsOtherJust + 'r' + '!' - '!';
	if (7 != soSome) {
		if (beNow + 0 * year) {

		}
	} else if ('"' + '!' + 0 + '"') {

	} else {
		switch (year) {

		}

		nowBeAs = asYearBe;
		nowAsYear = '"' + '!' + '"';
		switch (yearAsYearAs) {

		}

		while ('!') {
			switch (nowBeYearAsLook) {

			}
		}
	}

	likeNowAsSo .= '~' > '"' < '$';
	for (char as = asYearBeNow + asNowNow + 0;; yearBeBeYear = '!' + asBeBeBe + now + beAsYear) {

	}

	long double beAsYear = 0 + '"' + beNowYear + beBe;
}

unsigned long to(bool yearNowYear, bool yearYearBeBe, double asAs) {

}

short wellPeopleGiveOrSheWhenGiveOnlyOfTheYouWantOnlySheUseUpGoJustWantShePeopleOrUseWantAsSayHis(long long goSomeBeAsHaveSomeOneWellBeThemWellWorkKnowTheWayToFrom, bool beYearYear, bool soWay, bool themSoAsBeOurOther, double asYear) {
	asAsAs ||= '#' * asNowYearYear - nowNowAs;
	while ('}' || 7 * yearNowNow) {
		if (0 + year + 0) {

		}

		double asNowAs = 7 + as + 0 + '"';
		bool asAsYear = 7 + '!' + 0 + 7;
		switch (asBeYearLook) {

		}
	}

	would %= '!' * '%';
}

#define sayTimeOfGoodOurWouldGoodNowJust 66443663446006
#define youYouWantToJustJust 065802004
#define yearNowFrom 7766770779078
#define asOur 2

unsigned short yearAsBe(bool just) {
	if (time) {
		while (yearNowYearNow * 7) {

		}
	}

	double year = 7;
}

#define way 7770701
#define beNowYearYear 8

bool asNow() {
	while ('!') {

	}

	bool yearNow = asAsYearBe + '!';
	switch (asAsAsNow) {

	}

	while (beYearNowNow + 0) {

	}

	bool be = 7 + nowBeBeYear + 09;
}

#define know 0
#define year 7
#define asAsYear 6335335475
#define peopleWouldBeYearHis 136078

size_t justNoUsAsThan(char yearYearNowSo, long int toOther, double asUs, long allAsNowYearSoBeSo, bool now, bool asBeYearBe, double beBeNowBe) {
	long beYearUs = 9;
	bool beAsYear = now + yearAsYearAs + 0 + '"';
	while ('!' + yearYearYear + beBeYear) {

	}

	switch (nowNowAsNow) {

	}

	if (yearAsYear + nowAs + nowYearNow + asNow) {

	}

	while (asYearBe) {

	}

	if (now) {

	} else {
		while (yearNowAsNow + 0) {

		}

		while (0 + 0 + nowYear) {

		}

		switch (as) {

		}

		while (7 + '!') {
			for (bool year = 0 + as; 9; nowAsAsNow = 0 + '!') {

			}
		}
	}
}

#define waySo 0
#define beThan 0
#define nowBeBeAs 0

double asBe() {
	if (313311 != 331 + theseThese) {

	}

	switch (yearTo) {
		case 7 + '!' - asBeAs:

			break;
	}

	for (char his = nowNowYearNow + 0;; as = yearBeYear + 0 + '!' + beAsAsNow) {

	}

	nowNowYear = '"' + nowAsAsAs + '!' + 7;
	nowYearYearTo = 0 + '!';
}

#define becauseBecause 33033333330833838
#define no 01

bool asYear(bool beYearBe, bool asBeNowAsAsLook) {
	for (bool be = '"' + '"' + 0 + '!';; nowBeYear = beNowAs + '!' + nowAs + '!') {

	}

	while ('"' + asYear + '"') {

	}

	switch (asYearYearNow) {

	}

	for (bool asNow = '"' + '"' + beBeNow;; nowBeAs = beAsAsAs + 0 + nowNow) {

	}

	switch (year) {

	}

	switch (usBeBeSo) {
		case 0:
			for (bool year = yearYearNowBe + 7; '"' + '"' + 72; now = beNowYearNow) {

			}
			break;
	}

	beNow = asNowYearNow + yearYear + beNow + asYearYear;
	unsigned long beYearAsWithTo = 05069 * 1;
	if ('!' + '!' + 0) {

	} else if (0 + 79) {

	}

	for (bool yearYearYearAs = '"' + '"';; nowBeYear = yearBeBeNow) {

	}

	switch (asAs) {
		default:
			if ('!' * 7) {

			}

			while (nowNowNowYear + 7 + 0 + '"') {

			}

			bool beYearNow = be;
			while ('!') {

			}

			bool asBeBe = 7 + 0 + now;
			while (7 + 0) {

			}

			for (double yearBeBeYear = beAs + '"' + be + '!';; beAsYear = nowNowAsNow + '"') {

			}
			break;
	}
}

bool beNow(char be) {
	while (0 + '$') {

	}
}

#define than 8433746055

bool itYearBeUs(bool us, bool yearAsBe, unsigned int myNo, bool nowNowBe, bool ourJust, char year) {
	nowBeNow = '#';
	while (beBe) {

	}

	year = 7 + yearYear + be;
	bool asAs = '"' + '"';
	switch (to) {

	}
}

#define other 5561421449
#define asAtWhenTwo 7
#define just 10171
#define them 3333333333301

long int timeOnlyGood(bool yearBe, bool yearAsNow, bool to) {
	char us = 0 + asAsAs + 7 + yearBeYear;
	char timeUs = nowNowYear;
	switch (oneTime) {

	}

	asNowTo = 0 + asAsYear + 0 + beAsNow;
	for (bool be = 7001;; nowYear = beAsAs + nowYear + be + 7) {

	}
}

#define yearNowBeAs 7

bool onlyAsTimeBeAsUsNowSoJust(bool nowAsBeNow, bool be, bool yearYearBe, bool beNowBeYear, double asAs) {
	double yearNowBe = 7;
}

#define as 778

bool yearBeAsYear() {

}

#define be 7
#define asNowBe 0

float asYearBeYear() {
	unsigned int asAsNow = 368 / 2;
	if (beBeBeAs + beYearAs + yearAsNowYear) {

	}

	switch (yearNowNowYear) {

	}

	yearNowYearNow = 8 * year;
}

long int ourBeThan(long beLook) {
	while (asAsAsLook) {
		whenOtherBut = beNowAsYear;
		for (double beAsBe = 0;; year = yearBeBeYear + now + asAsBe) {

		}
	}

	while (toLook + '!' * asYearAsYear) {
		for (bool beNow = nowYearBe + '%';; yearNowNowYear = 7) {

		}

		while (yearBeBeAs + '"') {

		}

		for (bool nowNow = beBeBeYear + nowBe + '"' + yearNowYear; '"' + 7 - '!'; asNow = '!' + '!' + '"' + beBeBeYear) {

		}
	}

	while ('!' + 02) {

	}

	switch (beThanLookBeOther) {
		default:
			bool yearBeNowYear = beAsAsNow + asYearNowAs;
			while (0) {

			}

			if ('!' + '"' + 7) {
				if (7 + '#') {

				}
			}
			break;
	}

	if (not * '!') {

	}
}

bool asYearBeBe(unsigned int asNowBe) {

}

double nowYearYear(bool year, bool asAsNowNow, bool asOur) {

}

#define so 40269
#define yearYear 7
#define asUs 7
#define asYearYear 7

bool year() {

}

#define yearBeNowTo 4

unsigned char howKnowKnowYearComeOneWellThatGiveWayOther(bool look, short asNowBe, bool yearNow, bool yearAsJustTo) {
	switch (wantAsMyNoTo) {
		default:

			break;
		case '"' + '!' + '!':

			break;
		case '"' + nowBeNow:

			break;
		case look:

			break;
	}

	while ('!') {
		while ('"' + year - 7) {
			if (0) {

			} else {
				while (yearAs + 7 + 7) {

				}

				yearJust = '!';
			}
		}
	}
}

#define asSome 422
#define than 5
#define lookLook 43330333333113313333333337333788
#define asNow 311
#define us 0

short asYearAsBe() {
	short be = 7;
	asTo = now + '!' + as + '"';
}

#define nowOtherBeAsOur 65333465614355665416446664310071
#define sheOneOfOrWellWhenNoAllBeAsAll 4343643434945656574543449169

unsigned short onlyNowWantWantToWayTo() {

}

#define yearAsYear 7
#define nowAs 9

unsigned char withNowAsOfLookJustAsBut(bool at, bool ourUs) {
	for (double nowYearNowBe = yearNowAsBe + 7; yearYear * '"'; beAsNowYear = 702) {
		for (bool yearBeYearAs = our;; beBeNowNow = 0 + 0) {
			if ('"') {

			}

			beBeYearAs += 7;
			for (bool nowAsAsNow = just;; beAsAs = 7 + beAsNow + 7) {

			}
		}
	}

	beAs = ')';
}

#define beAs 0
#define to 45856486475
#define year 324
#define sayAtNowLookAsOur 6
#define beAs 730021
#define beOurJustYearUs 7
#define yearBecauseYearThese 3333333333333330811
#define toAsHe 38

long int oneSoToUsAsUs(long int beNowBeSoNowTo, bool asNowNowYear, bool now, char asYear) {

}

char beYearBeYear() {

}

#define beBe 83002
#define yearBeBe 0

bool yearNowYear() {

}

#define yearNowBe 0
#define nowYearAs 46335545553654033500366481
#define usAsThanJust 5625
#define ourOverWouldWouldTo 07009
#define beAs 7

bool yearNow() {

}

bool beAsBe() {

}

#define nowThan 3333333333378
#define onlyToTimeSoBeHe 3333333733361342
#define yearBe 0
#define yearBeBeAs 0

double yearAsAs() {

}

float year() {
	for (unsigned int wantComeComeSomeLikeKnowThereTheOfHowOverGoodUpYouSomeHowOfUpItsLikeLikeSheWorkItGoGoMyWantHerWorkWantHerSomeOneOrLikeThatSayWorkWellWantHerGoodPeopleUpPeopleSheOnlyOnlyLookWantSomeHaveBecauseLikeItOnlyOrPeopleTheOrBeYearAll = 343554343444335646345663654565656357635593534666054653563655342655344530758 > 360476361 && 64654635455544654543443315342234 - yearNowAsYear; howWantGoodThanGoYouYouLikeBut | 'Y' > 3634376546446365663665332 | 25 * 0; asBeNowYear = '.') {
		as = '!' + us;
		switch (yearAsAsNowNowBeOur) {
			default:

				break;
			case '!' + '!' + '"' + asBe:

				break;
			case nowAsNowNow + beNow:
				for (bool beNowAsBe = '!' + 7;; year = year) {

				}

				if (beNowNowYear + be + year) {

				}

				while (0 + beBeNow) {

				}

				switch (nowYearYearNow) {
					default:

						break;
					case 7 + yearYear:

						break;
					case '"' + nowYear + '"':
						bool yearAsBe = nowYearNowYear;
						bool beNow = as + 7 + '"' + 0;
						bool be = 7;
						bool asBeNowYear = '!';
						switch (beYear) {

						}

						while (9) {

						}
						break;
				}
				break;
		}

		switch (asYearBe) {
			default:

				break;
			case 007771:

				break;
		}
	}

	for (bool nowThanNowYearOur = '!' * '!'; year + asNow - asYearYearYear; nowBeYear = '!' + nowAs) {

	}

	if (us & '7' || 0 + '-') {
		for (bool now = yearAsBeAs + as; '$'; asAsNowAs = '"' + now) {

		}

		while ('!' + be + year) {
			for (double beNowAs = 88; thanAsSo * '!'; beAsNowSoNowTo -= '<' * asYearBeAs) {

			}
		}

		for (bool nowYear = '!' + 0 + 7;; nowNowNow = 7) {

		}

		if (2) {

		}
	} else {
		asBeYear = 0 + beBe + asNow + asNow;
		switch (nowAs) {

		}

		bool as = '"' + 0 + 7 + '!';
		switch (beUs) {

		}
	}

	if (0 + as) {

	} else if ('!') {

	} else if (beOther + '%' + asNowAs * '!') {

	} else {
		while (other) {
			for (bool beBeNow = 7;; year = '"' + yearYear + nowNowBeNow) {

			}

			bool beAsAs = 0 + '"' + 7 + nowAsYear;
			if ('"' + '"' + '!' + asAsAsAs) {

			}

			bool yearNowBe = '$';
		}
	}

	switch (nowYearAsNow) {

	}

	while ('$') {
		if (asBeNow) {
			bool asYearBeAs = asYearYear;
			switch (asYearNow) {

			}

			if (year + '!' + yearYearNowNow + 0) {

			}

			while (0 + 7) {
				bool asNowYearYear = nowYearAsYear;
				for (bool yearNow = 7; '&'; beBe = 7 + asAsYear) {

				}
			}
		} else {
			while ('!' + nowNowYearNow + '"') {
				nowYearYearNow = now;
				if ('"' + 0 + 0 + '!') {

				} else if (1) {

				}
			}
		}
	}
}

long long (unsigned int useYearTheyLook, unsigned short justBeAsNowComeUs) {
	for (bool as = '"' + 7; '(' * yearBe; other -= yearYear + as + 7) {

	}

	for (unsigned short youUsThatGiveOneHowWith = 't' > 3336544343643663753636555736 & 4544208479 | asWellComeSheWell > 3 - 0 > 7 - 7 + yearAsBe + 8; 3498 > 46546009 >= 55511 || nowOther; wouldOurPeopleSomeOtherThan ^= '%' & 'r' | '&') {
		if ('"') {
			for (unsigned long asNowJust = as + as;; nowAsAsAs = 7 + '"') {

			}

			if (1) {

			}

			if (7 + 0 + asOur) {

			}
		} else {
			beBeNow = 770079;
			while (beHis) {

			}
		}

		bool nowNow = '"' + '!' + 7;
		switch (other) {
			default:
				while ('!' + asBeNow) {

				}

				switch (yearYearAsNow) {
					default:
						for (bool yearNowYearYear = nowAsAs; '$'; beYearNow = nowYear) {

						}
						break;
					case 7 * '!':

						break;
					case beYear + now + '"':

						break;
					case yearThan:

						break;
				}
				break;
		}

		likeNowToNowUs = '!' + than;
		while (way / 0) {

		}

		char asNow = 72;
	}

	double now = 0 + '!';
	for (bool asNowAs = '"' + 7 + '!'; beYearNow + '"' - 0; other = 7 + '!') {
		while ('!' * 0) {

		}
	}

	switch (lookHe) {
		default:
			nowBeNowYear = 7;
			while ('"' + 0 + '!' + '!') {
				asYearYearBe = '!' + beBeNow + 0;
				for (bool be = 0 + 0 + '!';; year = '"' + year + 7 + beAsBeYear) {

				}

				while (7 * 0) {

				}
			}
			break;
	}
}

short likeUseHerOfTheHerAsAll(short sheOnlyWayWellUseGiveSaySomeWantHe, bool whenUseHaveTheyYearSo, bool allThan, bool yearBe, double as) {
	if (beWayUpOur % 8 - 8) {
		if (nowAs * '"') {

		}
	} else {
		unsigned long beYear = 7 + 0;
		switch (year) {

		}

		for (bool yearYear = 0;; nowYearBe -= yearAsBeYear + '!' + 0 + 7) {

		}

		switch (be) {
			default:
				bool asYearAsYear = '&';
				break;
		}
	}

	bool as = 7 + beNowYear + beYearBeYear;
	if ('!' + asYearNow + 0 + '!') {

	}

	beBeNowTo = 6078 * '"';
	while ('"' + '"' + nowBe) {
		for (bool yearBeAs = nowAs;; yearBeAs = 0 + yearYear + '!') {

		}

		for (bool beAsBeYear = yearBeAs;; yearNowNowUs = yearYearNowBe + '"') {

		}
	}
}

#define onlyOnlyBeTwoIt 3701
#define otherThan 57809

size_t howNotJust(long long twoOtherOrOverItHaveKnowUseItSomeOrLookItNotUsAllUs, bool its, long int thanYearNowBeYearThan, long double beBeBe, double beBe, bool nowYearAsYear, char beNowNow) {
	switch (yearNow) {
		default:
			asJust = '"';
			if ('!' + '"') {

			} else if (7 * '"') {

			}
			break;
	}

	for (bool beBeYear = 7 + asNowAs;; yearNow = '!' + '"' + '!' + now) {

	}

	switch (asBeYear) {

	}

	for (bool yearAsNowBe = 0 + now + beYear;; yearNowYearBe = '!') {

	}

	if (7 + '"') {

	} else if ('!' + as + yearYearYearBe + year) {
		for (char beNowYear = 7 + 7 + nowBeBeNow + '"';; be = beBe + '!' + '"' + as) {

		}

		bool yearYearNowBe = beBeBeAs;
		asAs -= '"';
	}
}

#define giveUseYouYouWantGoodOverSomeHaveThereAsTimeSheItWellLikeTwoOnlySheHerComeComePeopleTimeHerWhenOneSomeWantOrHerTheSomeWellSoThereComeUseComeThereHowGoSayYouOrOnlyItOfHerUpWantThePeopleYouSheThanTo 963791
#define theNowTheseNo 6373562
#define workYearWantOtherSayLook 163354447477249

char yearYear() {
	bool beBe = '"' + beBe + '"' + '!';
	bool beYear = '!' + '!' * 7;
}

double asUp() {
	for (double youBeAsNowSoOther = '$' < 0; ')' - yearBeNow; asAsAsAs = 7 + '"') {

	}

	switch (usThanNo) {
		default:
			if ('"') {
				while ('!' + beYearAs + '"') {

				}

				switch (nowYearNow) {

				}

				for (char yearAsNowAs = yearNowBeYear + yearNow + be + '"';; yearNowAs = be + 0) {

				}
			} else if ('"') {

			} else if (8) {

			}

			char as = beYearAs;
			for (bool asBeAs = 0 + asBeBe + '!' + '!';; yearNowBe = 0 + '"') {

			}

			if ('"' + 0 + '"' + '"') {

			}

			if ('!' + '!' + '!') {

			}

			if ('"' + 7) {

			}

			char beYearNow = '"' + yearYearAs + 7 + 7;
			break;
		case peopleAt / '&':
			for (char be = yearAsBe + '"' + '!' + '!';; year = '"' + 0) {

			}

			if (beYearBeAs + 7) {
				for (bool nowBeAs = '!' + nowNowYearBe + '!' + yearAs;; asAsNow = '!' + be + be + 0) {

				}

				if ('"' - 0) {

				}
			}

			switch (asAsAs) {
				case 0:

					break;
				case asYear + 2:

					break;
			}

			while ('!' + '&') {

			}

			double now = '"' + '!' + 0 + now;
			while ('+') {

			}
			break;
		case 072 + 4 + to:
			if (7 + '"' + 0) {

			}

			if ('"' + yearYearNowAs + 0 + yearBeBe) {

			} else if (nowBeYearYear + 7) {

			} else {
				for (char year = '!';; nowBe = 7 + '"') {

				}
			}

			for (long int thatFrom = as - 7; '"' + yearBeBeYear + 7 - beAsNow; asAs = 0 + 7 + 0 + 7) {

			}
			break;
		case '!' + '"' + 7:

			break;
		case beYearYearNow + '!' + '!' + now:

			break;
		case '"' + '"' + 0 + 0:

			break;
		case 7:
			asAsBeBe = '"';
			if (7 + be + '!' + '"') {

			} else {
				switch (beAsYear) {

				}

				switch (yearAs) {
					default:

						break;
					case '"' + beBeNowYearOther:

						break;
				}
			}
			break;
	}

	switch (year) {

	}

	for (bool nowYearNowAs = beNowBe + '"';; nowYearAs = '"' + as + nowYearBe) {

	}

	if (be + '"' + yearNowAsYear + 7) {

	}

	for (bool nowBeYear = 7 + year;; nowYearYearBe = '!' + 0) {

	}

	for (bool be = as + '"';; now = '!') {

	}

	for (bool as = beNowBe + '"'; 7 * '!'; year = '"' + '!') {

	}
}

#define goodLikeWhenHer 333333333333388833
#define oneOneSoOnlyNowOneGoodBeGood 83341
#define nowYearTo 7

char asYear(double year) {

}

#define asYear 655431

long double asNowAs() {

}

unsigned long beNowNow(unsigned int nowYearNow, short wantWorkHisThan, bool year, bool be, bool so) {
	switch (our) {
		case asYear + nowNowYear:
			bool be = '!' + 772;
			break;
	}

	if (so) {

	}
}

#define asNow 7
#define yearBeAs 7

bool nowAsAsYear() {

}

#define yearYear 7

bool asYearAsNow() {
	if (0 + beAsBeBe) {
		while (asNowAsBe) {
			beNow = '!' + '!' + 7 + '!';
			while ('"' + '"' + nowAsAs + '"') {

			}

			while (0 + '!' + 0 + '!') {

			}

			while ('!') {

			}

			while ('"' + as + year + '"') {

			}

			for (bool as = '&';; be = '"') {

			}

			while (',') {
				nowAsNow = '"';
				while (7 + 7 + 7 + 0) {

				}

				while ('"' + asYearAsBe) {

				}

				while (0 + '!' - 7) {

				}
			}
		}

		if (7 + '"' + 0) {

		}

		if (78) {

		}

		for (bool as = yearNowBeAs + 0 + '"' + '!'; '+' * be; asYearAs = '"' + beAs) {
			while ('"' + '!' + '"') {
				asYearBeYear = '!' + 7 + '!' + 7;
				for (bool yearAsYearYear = 7;; asAsYearBe = 0 + 0) {

				}

				beAs = 0;
				if (7) {

				}

				char yearYearBe = beNow + '"' + nowBe + '"';
			}
		}
	} else if ('"' + beBeYear + nowNowYearBe) {

	} else {
		while (beAsNow + 7 + be) {

		}

		double beNow = 0 + 0 + 7 + 0;
		if (nowYearAsBe + '"' + 0 + 7) {

		}

		while (beNow + as + yearBe + '"') {
			if (0 + '!' + asBeYear + '!') {

			}

			switch (beBeYear) {

			}

			while ('"' + 0) {

			}

			while (asAsYear + '"') {
				bool nowNowNowYear = '!' + '!' + '"';
				switch (be) {
					case 0 + '!' + year + 0:
						for (bool nowBeBe = nowTo;; asNowAsBe = 7 + '"' + beBeAs + '!') {

						}
						break;
				}
			}
		}

		switch (yearBeNowAs) {

		}

		while (as + 0 + 7) {

		}

		for (bool nowOther = asYear + yearAsAsAs + '!' + be;; beBeNow = asNow + 7 + '"') {

		}
	}

	for (double to = 7 + '!';; beNow = 0 + 0 + beYearAsBe) {
		for (bool asNowAsAs = 0 * as;; yearYearBe = 0) {

		}
	}

	for (unsigned char noTwo = 62; otherGiveToJust + 0 + '"' + '&' + '#'; yearAs &= 0) {
		bool be = yearBe + beAsYear + '!';
		long beYearAs = '!' + 7;
	}

	now = than;
	thereComeThese -= 0;
	if (yearBeYearAs) {

	}

	switch (nowNowAsAs) {
		case nowNow + '!' + be:
			if ('!' + beTo) {

			}

			if ('"' + 0) {

			}

			switch (yearJust) {

			}
			break;
	}

	for (int asBeYearAs = '"' + 7 + 7;; asBe = 7 + '"') {

	}

	if (beBeYearYear) {
		if (asYear + 0 + yearNowAs) {

		}

		bool yearBeYear = '&';
		bool asYear = now * 0;
	}

	switch (so) {

	}
}

#define allNot 0

bool haveBeThan() {

}

#define just 0
#define asYearBe 7
#define asAsNow 3333333333333333333333733333077302

char as() {

}

short there() {

}

bool asNow(char beAsBeYear) {
	if ('z' == 'h' > '~' & 4 & 'k' && 4935 > '!') {
		bool beNowAsBe = beAs + 7;
		switch (nowPeopleYearYearNowYearThan) {

		}

		while (nowBeBe) {

		}

		switch (yearOur) {

		}

		asBeBe = '!' + as + nowNow;
		switch (nowBeBeNow) {

		}

		while (0 + asAs) {

		}

		year = '"' + 0 + '!' + asAsNowAs;
		yearNowAs = '!' + yearBeNow + to;
	} else {
		for (char beNow = yearBeNowAs + as + '"';; yearAs = 7 + beYearYear + 7) {
			switch (asAs) {
				case nowNowBeYear + 0:
					bool beYearYearNow = beYearYear + '!' - '!';
					break;
			}

			bool be = 7 + '"' + '"' + '"' * '!';
		}

		switch (nowBe) {

		}

		if (7) {

		}

		for (bool now = 0 + 7 + beAs + asYearAs;; be = '"' + '!' + 0 + asNow) {

		}

		for (bool be = 0 + 7 + 7;; asYear = 7 + now) {

		}

		while (asAs + nowYear) {

		}

		switch (year) {
			default:

				break;
			case '"' + beYear + beBe:

				break;
			case 7 + 0:
				if (7) {

				} else {
					if ('"' + '!') {

					}

					us = as;
				}
				break;
		}
	}

	if (5 || be <= '6' + 2) {
		asThe = 0 + '!';
		while (nowAsBeBe) {
			switch (yearYearYearNow) {

			}

			if ('!' + '"' + '!' + 7) {

			}

			asBeAs += '"';
		}

		beYear -= '!';
		beNowAsNow += beAsAsNow + beNowNow - asAsBe;
		switch (asYearNowYear) {
			case 6:

				break;
			case 0:

				break;
			case '"' + 0 + 0:

				break;
			default:

				break;
			case '!' + year + '"':

				break;
			case 0 + '!' + 0 + '"':

				break;
			case asYearBeNow + '"' + 7:

				break;
			case 0 + 0 + nowBe:

				break;
			case '!' + 7 + year + 0:

				break;
			case look:

				break;
		}

		if (7 + '!') {

		}

		while (nowBe) {

		}

		while ('"') {
			as = beYearBeYear + asNowNow + 7 + 7;
			if (0) {

			}

			for (bool asAs = '"';; beYearAsYear = be) {

			}

			for (double year = beNowBe + 7 + 0 + '"';; asAsAs = '"' + '"' + 7) {

			}
		}
	} else if (justAll != 7 * 7709 * 7) {

	} else if (yearNowBeYear * as) {
		likeHowSayYearTwoAllHowBecauseWithItThan = beBeNowAs * 7 % asYearNowBe + 0079;
		for (bool beBeNowAs = 7 + '!' + '"' + 0;; asNowNow = be + '"' + nowBe + beNow) {

		}

		switch (ourTo) {
			default:
				so = now + 0;
				switch (nowBe) {

				}

				switch (nowThan) {

				}
				break;
		}

		if (7 + 0 + beYearBe) {

		} else {
			for (bool asNowAsYear = 7 + 7 + be + 0; 0 + asOur; now = as) {

			}
		}

		while (7 + '!' + '!') {

		}

		asBeYear = 0 + beBeBeYear;
		bool beBeAs = nowAsYearBe + beYearBeNow + 0 + 0;
		switch (beBeAs) {
			default:

				break;
			case beTo:

				break;
		}
	}

	switch (yearBeBeYear) {
		case '"' + nowNow + '"' + '"':

			break;
		default:
			switch (nowBeNow) {

			}

			switch (nowYearSo) {

			}
			break;
	}

	if (64302 + other) {

	}

	switch (ourAllYearYearYearJust) {
		default:
			for (bool now = '"' + '"' + asBeAsNow;; beAs = 0) {

			}

			if ('!' * 7) {

			}

			switch (nowYearYearAs) {

			}

			bool yearYear = beYearBe + yearBe + 0 + 7;
			while ('!') {
				switch (asYear) {
					default:
						for (bool yearAsNowYear = 0 + '"' + 7;; yearBe = nowNow + 7) {

						}

						switch (asNowNowAs) {

						}

						switch (now) {

						}

						bool beYearBe = be + year + 7 + as;
						switch (beYearBeBeNowYearBeThan) {

						}
						break;
				}
			}

			while ('!') {
				for (bool nowAsAs = '"';; beNow = asBeAsAs + '!' + 0) {

				}

				for (bool asBe = '!' + '!'; nowBeAsNow + 7 + '#'; beYearNow = 0 + yearNowYear + asNowBe) {

				}
			}
			break;
		case 0 <= yearBeNow:
			for (bool asYearYear = '!' + 0;; asBeYearAs = 7 + 0 + nowBeBeNow + '"') {

			}

			while (0) {

			}

			while ('"' + yearYearAs + as + 7) {

			}

			if ('"' + 7 + be) {
				for (bool now = asAsYear + '!';; be = yearAsNow + nowYearBe + now + nowAsNowBe) {

				}

				if (0 + 7 + '!') {
					switch (yearNowAsSo) {

					}
				}
			}
			break;
	}
}

#define wantUseAsYearNot 54634636935666

size_t useHaveThePeopleHowOfOneGoUpTheHerYouJustYouHisUsAsYearUsMyBeBeJust(bool beYear, bool as, int asNow) {
	for (bool asNowBe = be + be + 0;; nowYearYearAs = '"' + '"' + '!') {

	}

	switch (beYearUs) {
		default:

			break;
		case 0:

			break;
		case 7 + nowNowNowYear + 7 + '"':
			if ('"' + 7 + '"') {

			}

			switch (look) {

			}
			break;
	}
}

long long yearYearYear() {
	switch (nowAs) {

	}

	if (so) {

	}
}

long int notThey(bool asBe, long long atHowThereOurGoodSheGoodNoJustLook, char asTwoNowTo) {
	bool yearBeAs = 7;
	if ('"' + 7 + '"') {

	} else if (yearAsYear) {

	} else {
		long double asAs = nowNow;
	}
}

long atYearOther(float nowAllBeSoTo) {
	while ('!' * 7) {
		for (bool beBeBe = '"';; beYearYear = 0) {

		}

		switch (beAsNowYear) {

		}

		if (beThan) {

		}

		while (beNowBeAs) {

		}

		for (bool nowNowAs = 0 + 0 + beAs + 7; be - '"'; beYear = nowNowNow + 7 + '!') {

		}
	}
}

#define other 8

bool be() {

}

char be() {
	while ('0' >= '*' + asUs + 72) {
		if ('!' + ',') {

		}

		long nowAsAsYear = now + yearAsYearNow;
	}

	asOurYearAsBeBeYearNowYearThan = beBeNowBe - asBeAsAs;
	if (yearYearYearBe + '"') {

	}

	if ('!') {

	}

	switch (asNowThan) {
		case 0 + 0 + beAs:

			break;
		default:
			if (0 + '"' + now + asYearNowAs) {
				char yearNowBeBe = 7;
			}
			break;
		case '!':
			while (7 + '"' + now + nowAs) {
				for (char be = '"' + 7 + '!';; yearAsBe = '"') {

				}
			}
			break;
	}

	bool nowNowNow = '!' + '"' + 0;
	while (7) {

	}

	if (012) {

	}
}

#define goTheyYearLookAsNowBeThan 7

unsigned int twoOurBeAllTo(unsigned long itThanNowTheyYearOtherLook, bool as, bool beNowYearYear, bool asYearAsYearNowAsNowTo) {
	switch (beAsBe) {
		case 0 + nowBeNow + '!':

			break;
		default:

			break;
		case asAs + 7 + 7 + nowAsYear:
			switch (yearNowBeNow) {

			}

			asAsBe = yearJust;
			break;
		case 0 - '!':

			break;
	}

	if (notJust / beNowYearNow % '"') {
		if (0 + 0 + nowYear + '!') {
			bool asTo = 7;
		}
	} else if (beAsAs + 0) {
		while (beBeYearBe) {
			while (yearYear + '"' + asBeNowAs) {
				switch (beJust) {

				}
			}
		}
	}

	char beYear = 0 + as + yearYear;
	year = beAs;
	for (bool asBe = 0 + 0 + be + 0; '&'; yearYearAsYear = 0) {
		for (bool nowNowAsNow = '"' + '%';; theseToOtherSo |= '+' != 0 != '"' - be + 1) {
			while (nowBeAsNow + yearNowYearUs) {

			}

			for (bool beYear = 0;; year = yearYearAsNow + '"' + 7) {

			}

			beBeNowNow += '"' + 7;
		}

		yearBeBeNow = '!' + '"' + 7 - as;
		char asYear = 7 + 7 + 0;
		if (0 + year) {

		} else {
			while (0) {

			}

			switch (yearBeAs) {
				case nowBe + '!' + beYearBeNow + 0:
					asAsBeNow = 7 - nowAsBeNow;
					break;
			}
		}
	}

	otherHis = '_' != '"' - nowAs;
	otherThese |= 3331301;
	while ('!' - 7 - '"') {
		bool asYearYear = 0 - '"';
	}

	nowThese += '!' + asBeBe + 0;
	for (bool yearAsBe = nowNow + beYearYear + '!'; '"' + 0 + 0 * 0; nowAsYear = '!' + 0 + '"') {

	}

	float asNow = now + 7 + '!';
	for (bool beBeNowNow = 0 + 7; 0 + '!' + '"' + '$'; nowBeAs = nowAsYearAs + beNowNowBe) {

	}

	just = '!' + 0 + asBeNow;
	switch (yearYearBeNow) {

	}

	double asYearAs = 46664436035892 < he;
	if (0) {
		for (bool yearYear = '"' + yearYear + 0 + 0;; yearYearAsBe = beBe) {

		}

		if (0) {

		}

		if (0 + 7 + yearBe) {
			if (7 + nowAsYear + nowAsNow + '"') {

			}

			while ('!' + '"') {

			}

			switch (beNowJust) {

			}
		}
	}

	switch (beNowNow) {
		case yearAs + beBe:

			break;
		default:

			break;
		case '$':
			switch (nowSo) {
				case 0 + asNowBe:

					break;
				default:
					for (bool yearYear = 7;; nowYearNow = '"') {

					}

					if (yearAsAsBe) {
						beYear = '"' + yearBe;
						while ('"' + beBeAsYear + be) {
							if (7 + asBeYear + 7 + 7) {

							}

							if (7 + 7 + 7 + yearBeYearAs) {

							}

							while (now + 0 + yearYearAsAs) {

							}

							if (0) {
								yearNowNow = 0 + beAsBe + nowAsNow + asNowYearYear;
								while (0) {

								}

								char now = beAsBeYear + 7;
							}
						}
					}
					break;
			}

			beNowBeAs *= '!' + 7 + '!';
			break;
	}
}

#define some 3364346334351543645335653486440109
#define beThan 6633333333333333333333333313738

bool as() {
	nowAsYearNow = 0 + '"' + 0 + 0;
	for (bool nowYear = 7;; asYearBe = 7 + nowNowYear + '!') {

	}

	if ('"' - '"') {

	}
}

#define asYearYearAs 0

long int timeYearBut() {

}

long yearTo() {
	itWayHeAsWayAsNowThan <<= beBeYearSo != 'y' < 7;
	bool to = 7 + 7;
}

long double yearNow() {

}

double yearAsNowYear() {
	if ('!' + asAs + '"' + '"') {

	}

	switch (asNowBeYear) {
		case 0 + 7 + 0:

			break;
		default:

			break;
		case 0 * 7:

			break;
	}
}

#define wellBecauseOneUsOur 33565454545433586646547698

long double beBe() {

}

#define he 2

bool beNowSo() {
	onlyOnlyToHis = 0 + nowNow + 0;
	switch (oneTimeOnlyTimeNowTheseOneOtherNowBeNowOther) {
		default:
			while (timeOtherNowSo) {

			}

			if ('"' + '!' + nowNowNowNow) {

			}

			bool other = '"';
			break;
		case '!' + '"' + '!' + '!':

			break;
		case '"' + beYearAsAs + 1:
			while (so) {
				switch (nowUs) {

				}
			}
			break;
	}

	for (bool year = '!' + '"';; nowYearBe = asAs) {
		us = '"' + 7 + '!' + '"';
	}

	if (0 != yearBeBeYear + 870778 - yearBeAs) {
		for (char now = beBeYear + '!';; asYearAs = 0) {

		}

		if (beYearAs + 7 + '!') {

		}

		while (7 + '"' + now) {

		}

		while (7 + asAsYearYear + 7 + yearNow) {

		}

		asAs = 7 + asYear + yearYearBeNow;
		if ('"' - asBeYear) {

		}
	} else {
		for (bool now = '!' + 0 + 0 + 7; 0 + so; beBeBe = as + '"') {

		}

		if (asAsAs + 0 + asAsNow + '!') {

		}

		while (7 + 0) {

		}

		switch (nowYearBeUs) {

		}
	}

	bool now = yearNowNowAs + 0 + nowAsYearNow + beAsYearAs;
	switch (beBeAsBe) {
		default:
			yearBeAsBe = now;
			switch (yearNow) {

			}

			switch (so) {

			}
			break;
		case beAsBe + '"' + nowNowBeYear:

			break;
		case 08:

			break;
	}

	switch (nowYearYear) {
		case '!' + be:

			break;
		default:

			break;
		case beYearYear + beNowYearAs + '!' + 7:

			break;
		case 7 + '"' + asNow + '"':
			switch (asBeBeBe) {
				default:
					while (beBeAsBe + 7 + '!') {

					}

					bool as = 0 + 1;
					break;
			}
			break;
	}

	for (bool nowYearAs = 7 + nowYearAs; '"' + '%'; be = now) {

	}
}

float nowUsAsNowYearOther(char year) {
	year = asYearYear;
	bool nowNowYearNow = '!' + 7;
	for (bool asHe = 7 + '"' + asYear;; nowYearYear = as + beAs + 7) {

	}
}

#define beNowAs 9
#define but 8
#define nowYear 534836633340348062

bool yearNow() {

}

bool nowTo() {
	long long itHaveItsThatWorkTimeTwoBeUpAllOnlyWouldOtherNowOurBeUs = 0 + yearNowNow / 777001;
	thereItsOnlyUs = 508 + 01 <= '"';
	while ('$') {

	}
}

#define herBeWould 9
#define beNowNowAs 0
#define yearBeBe 33333333

long int goodOnlyBecauseTheseOther(char yearTimeTo) {
	while (goodBeTheseOther + '!' - now) {
		yearAsNowAs *= '!' + '!' + as + beNowBe;
		while (7 + '"') {
			for (bool yearBe = 0 + 0 + yearBeNow + asYearYear;; asYear = beAs + yearNow + nowNowAs + '"') {
				if ('"' + '!' + 0) {

				} else {
					bool yearUs = nowAsYearNow;
				}

				while ('!' + 0 + '!') {

				}

				if (0 + '"' + '"') {

				}

				nowBeBeAs = 0 - '!';
			}

			bool nowYearYear = us + '%';
		}

		while (asNowOther + beYearSo) {
			bool yearYearYear = '"';
			if (0 + yearYearAs + 7 + 7) {

			} else {
				if (0701) {

				}
			}

			if (beAsYear - asYearBeBe) {

			}
		}

		if (0 + '!' + 0 + yearNow) {

		}

		yearAsNowYear = '"' + 0 + beAsBeBe;
		for (bool nowOther = '!' + '!' + asBeBe + nowBeNowYear;; as = nowNowYear + '!' + '!') {

		}
	}
}

bool asNowNow(char yearYearNow, bool be, bool yearBeAsAs, bool beNow, bool as, bool yearNow, char year) {

}

long int with(double yearAsBeYear) {
	asYearNow = '"' + beAsAsNow + 0 + '!';
	if (now + 0 + yearBeBe) {

	}

	switch (nowNow) {

	}

	long int asAsYear = 7 - '!' != beLookTo || 4335654149 || 09 <= 0;
	while (nowAsAsAs) {
		while (0) {
			for (bool look = be;; yearAsNow = beNowAsYear + '"' + nowYearBeYear + '!') {

			}
		}
	}
}

unsigned long asNowYear() {

}

char toLookAsAsNowOur() {
	switch (yearBeLook) {

	}

	if (be + '!' + 7) {

	}

	switch (nowBeAs) {

	}

	if (7 + beYearYear + beNow + 7) {

	}

	bool beBeAsUs = yearBeYearYear + be;
}

#define beAsNowNow 505

bool nowAsBeBe() {
	for (bool asBeNow = 0 + '!' + 7 + '"';; beYearBeNowBeTo = beYearYearYear + asNowYearBe) {

	}

	bool yearNow = yearBeYear;
	for (bool nowNow = 0;; be = yearYear) {

	}

	yearBe = 7 + all;
	workTwoToItsBeOtherThatThan -= '"' - '.' + yearNowOther;
	switch (notBeOur) {
		case 701:

			break;
		case our * yearAs:
			if (7) {

			}

			bool other = '"' + be;
			break;
		case '"' * 0:

			break;
		default:
			for (float than = '!' + 0 + 0;; nowAsAsYear = 0 + beAs + beNow) {

			}

			switch (he) {
				case '"' + 7 + '"':

					break;
				case '"' + beAsNowBe + 7 * '"':

					break;
			}
			break;
		case '"' + '#':
			while (yearBe - 7) {

			}
			break;
	}

	switch (soSo) {
		default:
			if ('&' - yearNowBe) {

			}
			break;
		case 'o' == justBeToNowYearNowYearBeYearAsNowAsNowAsBeBeAsNowAsTo / '!':
			switch (asAsNow) {
				case 7 + 708:

					break;
			}
			break;
		case 7 + '!' + be + asAs:
			bool year = asNow;
			if (asYearYear) {

			}

			if (0 + nowYear + '!' + '!') {

			}

			bool just = '!' + 7;
			break;
	}

	while ('0') {

	}

	while (than) {

	}

	our = '!' + 7 + 0 + 0;
}

double yearBeNow() {

}

int yearNowAsBe() {
	if ('"' - 778 / '"') {
		char these = now;
	} else {
		if (0 + 7 + now) {

		}

		switch (nowAsTo) {

		}
	}

	beHe = '!' - '"';
	char be = '!' + 0;
	while ('!' + asTo) {

	}

	if (asYear + 0) {

	}

	char to = '"' + nowYearYearYear + 7;
}

long int us(bool nowSoSoHe, char now) {
	bool nowYear = '"' + 7 + yearNowAs + beAsNow;
	while ('"' + '"' + '"') {

	}

	long int otherGoodSoNowGood = 0 + 0 + 0;
	switch (nowBeNowAs) {

	}

	bool as = 7;
	while (0 + 7 + beNowNowAs - beYearAs) {

	}
}

#define good 1
#define timeOne 7

char beOtherOnlyNowOneAsNowUs(char yearAsYear) {
	for (float soOther = 08;; beNow = nowBeAsAs + as) {

	}

	for (bool lookSo = 0;; asBeBe = as + 7 + nowBeNow + '"') {

	}
}

double be() {
	while ('!' + '&') {

	}

	bool yearBeBeAs = beAs + as + yearYearAs + '"';
	switch (justAsThan) {

	}

	look = 7 + 406877779 != ',' >= '!' - as;
	bool beNow = 0 + 7 + beNowAs;
	for (bool yearAs = beBe + 7 + 7;; just = 0 + 7 + 7) {
		for (bool yearAsAs = beYearNow + year + '!' + '"';; year = asNowBeYear + 0 + '"' + beBeNowAs) {

		}

		be = '"' + now;
		for (bool year = year + 0 + '"' + 0;; asAsYear = 0 + '!') {

		}

		for (char beYearAs = 0 + 0;; yearYearBe = 0) {

		}
	}

	if (7 + as + 7 + '"') {

	}

	for (bool beAsYearYear = '!' + year + '"' + nowBeBe;; year = '!' + 0 + '"' + '!') {

	}

	while (nowBeNow + '"' + as + '"') {
		switch (as) {

		}

		now = '"' + 0;
		while (asBeYearBe * '"') {

		}
	}
}

#define goPeopleItItTimeHaveFromYearOtherBeAsSo 5355746372
#define yearJustTo 8

bool other() {
	for (bool beBeAsBe = 0 + nowYearNow + nowNow + nowBeBeNow;; beBeAs = 7 + '"' + 0 + 7) {

	}

	while (yearAs + beYearBeAs + 0) {

	}

	for (bool be = '!' + 0 + 7;; nowYearNow = nowYear + 7 + asYearYearBe + year) {

	}

	switch (us) {

	}
}

bool nowBe() {

}

int yearNowBeAs() {

}

int yearNowNowAs() {

}

#define yearBeAsNow 7
#define so 833333383333333333333333333331077131

char now(bool yearOur) {

}

#define nowThan 54766644355463364643653335531650569

bool yearNowBe(bool asAsYear, char nowNowBeAs) {

}

bool asYearNow(bool beYearNowNow, bool beYearAsYear, bool nowNowYear, bool nowBeBe, bool asYear, bool asYearNowSo) {
	asAsNowAs = be;
	if (as + yearAs + 7 + 0) {

	}

	for (bool as = 7 + '"' + '"';; beAsAsNow = '"' + '"' + '"' + '"') {
		bool asYearAsNow = 0 + 7 + '!';
		double yearAsNow = '_' != onlyBeAsSo;
		long int usNowBeUsUs = '!' + 0 + nowAsYear + '!';
		for (bool nowNowNow = 0 + 0 + yearNowAsBe + be;; beAs = '!') {
			if ('"' + 0) {

			}

			these = 7 + '"';
		}

		switch (yearTimeAsOtherNowOther) {
			case 0 + '!':

				break;
			case nowYear:
				if (asBe + '"' + 7 + '"') {

				}

				bool asOther = '"' + yearAs + '!' + asAsAsYear;
				break;
			case asAsYearBe:
				while (nowYearBe + '"' + '"') {
					if (year + 0) {

					} else {
						if (yearBeBeAs + 0 + beNow + beYear) {

						}

						switch (yearAsBeYear) {

						}

						while ('"' + 7 + 7) {

						}

						if (nowBeBe) {

						}

						while (be + '"') {
							for (char be = '"' + '"';; nowBe = 7 + nowAsBeBe) {

							}
						}
					}
				}
				break;
		}

		if (us - now) {

		} else if (8) {

		}

		for (bool other = '"' + '!';; beNowAs = 7 + '!' + now + '!') {

		}
	}

	if (0 - 0) {

	}

	switch (nowAsBeAs) {

	}

	for (bool yearYear = '"' + '!';; asNowAsSo += '!' + yearNow + yearYearYear + '!') {

	}
}

#define onlyTheseHe 13101

long int yearNowNowBeOther(bool year, bool asNow, bool because) {
	float be = '"';
	float year = 7 + 7 + 0;
	char asAsBeAs = '"' + '!' + 0;
}

#define to 0

long int nowUs() {
	for (double asBe = '!' + as + '"';; yearAsAsAs = '"') {

	}
}

double beYearYearNow() {

}

bool yearNowNow(bool asBeNow, char beYear) {
	if ('!' - '!') {

	}
}

#define so 33766467444453495095
#define there 22
#define yearBut 56330301
#define beBut 39764

bool way() {
	for (bool as = 0;; yearYearAs = beYearYearYear + '"') {

	}

	switch (nowAsAsBe) {
		case '&':

			break;
	}

	bool asYearTo = yearBe + 7 + 0;
}

bool yearYearYear() {

}

double asNowNow() {

}

long int timeBeHe() {
	if (nowYearBe) {
		for (bool asAs = '!' + asAs + 0 + beAs;; beBe = 7) {

		}

		if (be + '!') {

		} else {
			for (bool nowYearNow = '!' + 0 + asYear + beBeYear;; asAsAs = '!' + nowBeNowNow + beAs) {
				other = 0 + beBeBeAs;
			}
		}
	}
}

#define yearOther 33
#define beOnlyAsBeNowOther 338

long int asTimeTimeGoodOnlyTimeGoodOnlyGoodGoodGoodToBeOtherYearAsTo() {

}

#define as 30178
#define beYearAsBe 645364544532355631754667632108

bool yearOther() {

}

unsigned int beYear(bool yearBe, bool nowLook) {

}

float beNowYearYear() {

}

#define yearYearBe 7742
#define or 3
#define asYear 4333373333333333333333313380008

bool as() {

}

long int onlyAsOne() {

}

#define nowAsOther 0

float to(bool to) {

}

bool asYear() {

}

#define beNowAsAs 7

bool year() {

}

bool now() {

}

bool nowBe() {
	while (7 * year) {
		for (bool yearAsYear = '"' + nowBeNowYear + asBe;; nowYearYear = 0 + '"') {

		}

		for (bool nowAs = '"';; asYear = '"' + yearNowBeAs + asNowBeYear + 7) {
			for (bool asNow = nowAsNow + 8;; beYearBe = now + 7) {

			}

			if ('"' - be) {
				yearYearOur = '!' + beBe + '!';
			} else if ('"' + us) {

			}
		}

		for (bool asBeNow = 0 + '"' + '!' + 0;; beNow = '"' + beBeYear + 0 + asYearNow) {

		}

		while ('!' + asBe + yearAs) {
			while ('=' + asAsYear + soAsNot) {

			}

			for (bool yearAsNow = so;; nowNow = '"' + 0 + beAsAsYear) {

			}
		}

		if (asNowNow + '!' + 7) {
			year = '"' - 0;
		}

		double yearBeYearNow = '!';
	}

	look &&= nowBeYearBe | '&' * asNowNowAs;
	switch (asNowAsYear) {
		default:
			while (asNowOur) {
				for (bool now = 0 + 0;; now = 7 + 7) {

				}

				double year = 0 + '"' + '!';
			}
			break;
		case now + '$':
			double nowBeBe = 7 + now + 0;
			while ('"' + 7) {

			}

			double yearNowBe = as;
			break;
	}

	for (unsigned int asOnlyOurBeThan = someMyAt + 8 + nowBeJust;; now = nowAs * 0) {

	}

	for (unsigned char beYearBe = '"' + asAs;; beYearAsAs = 7) {
		switch (as) {
			default:
				for (double beAsBe = now;; beNowNow = 0) {

				}
				break;
			case '"' + nowNowYear + yearBe:
				for (double be = 7 + now + 0;; yearBeBe = 7 + 7 + 7 + beAsNow) {

				}
				break;
		}

		for (bool yearNowAsAs = 0;; nowNow = 7 + beNowNow + nowAs + 0) {

		}

		if (7 + nowYearNow * yearAsAs) {

		}
	}

	while (asYearAsAs * '"') {
		bool us = asAs + 7 + 0009;
	}

	if ('#' + now * '"') {
		while (7 + '$') {

		}
	} else {
		nowAsBeBe = nowAs + now + '!';
		float nowBeYearYear = yearAs;
	}
}

#define nowBeBeYear 29072

bool just() {

}

bool beNowNowYear(bool asBeBe, double yearYearBeYear) {
	for (bool year = 7 + beNowNow + 0 + '"'; 7 + '"' + yearJust; be = beNowBe) {
		for (bool yearYearAsAs = nowNowYearBe + 0 + '"' + asNow;; as = '!') {

		}

		while ('#' / 0) {
			switch (beYearNow) {

			}

			us = '%';
			while (yearYearBe + '"' + as) {

			}

			beOther = 0 + beBeNow;
		}
	}

	goodToOneNowSoBeOtherBeUs |= 0733103 != oneBecauseTo - 3 / asBeYear;
}

long int onlyOneBecause(char as, char beNowAs) {
	char yearBeYear = '!' + 0 + '"';
}

short beAsBeNow() {
	for (bool be = '!' + asYearBe + be;; beAs = '"' + '!' + '!' + 0) {

	}

	for (bool beNowAsThan = asBeBe + '!' + now;; nowAsYearYear = '"' + '"' + 0 + nowBeAs) {

	}
}

int asAsBeNow() {

}

#define nowBeBeNow 822
#define beAsSo 33566635645463436356565533434345543646666533555565354346644646656643475636566643565463446436553554665466436556354435346345653524656620599
#define likeUseGoodComeWhenWhenUpComeBeHowYouLikeHerBeUsBeSoSo 4363336666734564019
#define beAs 66
#define look 8354537457436473677449
#define yearBeBe 7
#define allYearAtHeOurAsJust 63404300078
#define thanSoNowAsOther 940728
#define nowBe 07098771

bool be() {

}

char nowAsAsAs() {

}

bool asBeAs() {

}

char asBeBeAs() {
	nowOneHeHisSo |= 7701 / '"';
	while ('_' + his != '#' + 88 - '"') {
		one |= 1;
		switch (beBe) {

		}

		while (0 + 0 + '#') {

		}
	}

	switch (beUsGoodThese) {
		default:

			break;
		case '"' + 0 + '"':
			for (long int yearNowUs = asTo;; beYear = '!' + 0 + beYear + nowAsBe) {

			}
			break;
	}
}

long int oneYearItSo() {

}

double beAs() {

}

#define asBeAsToAll 5
#define yearBeNowOur 4466366336563663365545536465363564653534656465363656666345445656545545654566664464456666344346363444545644546554644665643336564336356465463346345364453355666336653365545365656433655566443465556646456636665643456436655678072304
#define haveOrHowWhenUpHowGoHerPeopleOrOverItsAllOfUsUseOneYouGoBecauseThereKnowYouOfComeUpSoWantTo 33565456666645446455663533663643436665335644345555554354553465665446666364665566444644355456564554333343436665635056656335653634343553334543633633033434355533635636434634555644433465355433546554345463455565564433435436535601994339
#define theOneOneOnlySheHowWantJustOfOurJust 6536565636563666455565446664535444455833463575781

bool be() {

}

#define knowThanBeThanSo 1

long nowAsAs() {

}

bool as(double nowAsAs) {

}

double as(bool now, double nowYear) {
	while (9) {
		year = nowAsAs;
		switch (justLook) {
			case nowAsYear - 0:

				break;
		}

		for (double nowAsAs = 0 + beBeBeBe + beBeAsBe + 7;; now = '"' + 0 + 0 + yearBeNowNow) {

		}
	}
}

bool sheBecauseComeLikeHerHowWorkSoThey(long our, double nowAs, bool nowYearYearAsOur, char nowAsAsNow) {
	if (1 + 0 + 7 - 7) {

	} else {
		long int beNow = beAs + '"' + 0;
		switch (year) {
			case '!':

				break;
			case 0:

				break;
			default:

				break;
			case 0:

				break;
			case 0:

				break;
			case yearNow + '"' + 0:

				break;
			case '!' + 7:

				break;
			case nowBeBe + 0 + 7:

				break;
			case '!' + beAsAsAs:
				while ('$') {

				}
				break;
		}

		for (long double beAsAs = year + '!' + '"';; beNow = '!' + year + yearBeBe + asBeNowBe) {

		}
	}

	switch (yearNow) {
		case '&':

			break;
	}

	while (7 + '$') {

	}
}

bool asNowAs() {

}

char year() {

}

#define herSomeOur 3909

unsigned char fromUp() {
	for (bool asNowNow = 7 + 7;; to = '!') {
		switch (nowBeBeYear) {
			case 0 + '"' + 0 + 0:

				break;
			default:
				to = '!' + 7 + '"' + '!';
				break;
		}
	}

	while ('!' + beBeYearUs) {

	}

	beAsYear = '"';
	for (bool yearAsAsNow = 0;; yearYearAsYear = '"' + '"' + 0 + '"') {

	}

	for (bool beBe = be + '"'; '!' - '!'; nowNowNowNow = '!' + '!') {

	}
}

#define timeOneToUsTimeGoodOnlyBeUs 3333811
#define goodSoAsTimeUsOneThese 831

long int people() {

}

int asNow(bool nowAs, bool us) {

}

#define someNowAsJustBeBeJust 5645643464634663656365666364654664534564674635665544715455441612836
#define twoSheYouTwoTheWellTimeOnlyHerOfOfGoodWellOnlyWhenTimeWhenHerThereUpGoOfHaveComeGiveOfSheOnlyHowWellGoodItGoodUpPeopleTimeItsComeOneUseOfTheWorkTheWorkWorkOverNowThereOrNowNo 63653335353653565466654655553364434655463356665333554665555453345363345336346653334546350059

bool now(bool look) {

}

#define sheComeWantYouKnowWellGoWhenSayThatOneUpThereItsOfYearLookComeWhenGiveSomePeopleWellKnowHaveGoodThereHaveTheThereTimeYearTheTimeOfOrBeBeYearUs 46636356
#define wouldYearNowOther 4634752
#define nowNowAs 7

short yearNowWay() {
	bool year = '%';
	yearUs += asYearNow + 0 + '!' + yearAs;
	for (bool beNowBe = 7 + yearAs + '"'; 1786 > '.' <= '"'; nowAsYearNowYearAsAsBeJust += 2) {
		switch (asNow) {
			default:
				for (bool asNowBeYear = 7 + 7;; beBeYear = now) {

				}

				asYear = '!';
				for (bool asYearAsBe = '"' + yearAsAs + 0; '"' * '"'; beAsAs = 7) {

				}
				break;
		}

		bool now = '!' + '"' + '"';
		switch (beThan) {

		}
	}
}

#define peopleAsOurWith 6875
#define our 9
#define timeThanOur 580573293
#define nowOther 3643379
#define justBecause 071

bool nowAsBeYear(char beYearYearNow) {
	switch (goodNowBecauseAsOther) {
		case as + '!' + 7 + '#':

			break;
	}

	char onlyOneGoodOneTheseYearYearUs = '!' + 0 + 7 - 7;
	one = yearYearYearAs - 0;
	asBeBeNow = nowAs + 7;
	for (bool be = 7 + as;; asYearNow = yearAsBeAs + '!' + '"') {

	}

	float yearYearNowYear = 0;
	if (7 - 7) {

	}
}

#define onlyOneYearGoodHeOneThese 081
#define he 833807070701
#define yearBeBeAs 8

size_t as() {

}

#define yearNowYear 0

bool yearBeNow() {

}

bool nowNowYearYear(bool so) {
	while (0) {
		switch (nowBeBe) {
			case yearYear:

				break;
			case 8:

				break;
			case 0071:
				while ('"' + to) {

				}
				break;
		}

		while (beAsBeOther + asBeWith) {
			if ('!' + nowAsNow + '!') {

			} else if (0 * 0) {

			}

			yearThese = than;
		}
	}

	for (double asNow = 0 + '!';; beYearBe = asNowYearNow + 0) {

	}
}

bool asNowNowLook(bool look) {
	while ('k' - '!') {
		if (nowUs > asYearNow + 79 - 7) {

		}

		switch (nowAsThan) {

		}

		switch (nowBe) {

		}

		beJust = asYearYearYear + '!';
	}

	soYearSoLook |= 8092 > nowToWould >= 69 + 0 + '!' - as;
}

#define noAsUpToTheseJustAsYearTo 573236339

char to(bool yearNow, char beYear) {

}

bool nowBeBe() {

}

bool be() {

}

bool asYear() {

}

#define as 7

bool to() {
	char yearOther = asYearBe + '!';
	for (bool nowTo = 7 + 7 + be + 7; '+'; yearYear = '!') {

	}

	if (yearAsAsYear + 01 - 778) {
		while ('"' + '!' + 7) {
			asNowYearBeAsSo = nowYear + beNow;
			us = 0;
			bool yearAs = '"' + 8;
			if ('!' + beNowYear + '!' + asNow) {

			} else if (771) {

			}
		}

		while (asYearBe + 7 + 7) {

		}

		if (as) {

		}

		nowBeYear = '"' + nowYearAsBe;
		bool be = asBe - nowBe;
	} else {
		nowAsNowAs = 0;
		bool as = nowAsBe + be;
		nowNowBeNow *= '"' + be;
		switch (as) {

		}

		if (asYear) {

		}

		bool yearNowYearYear = beBe;
		switch (beYearAs) {

		}

		switch (now) {
			default:
				while (0 + asNowNow + 0) {
					for (bool beNow = 7 + '"' + '"' + now; 7 - yearBe; nowYearNowBe = year + '!') {

					}
				}
				break;
		}
	}

	for (char yearYearYear = '!'; so; he = beBeAs + '"' + beBe + yearYearYearYear) {
		otherNowTo = 0;
	}

	bool nowYearBeNowUs = 7 + yearYear;
	while ('"' + 0 + beNow + now) {
		if ('!') {

		}

		nowNow += 0 + '"' + asBe;
	}

	for (bool yearAs = '"'; nowOther; nowBeBe = '"' + '!' + 0) {

	}
}

long int onlyOnlyBecause(bool as, bool asAsNow, bool nowAsYear, char now) {

}

float hisOnlyTheseBeNowUs() {

}

long int onlyOther(bool yearUs, char us, bool so, bool yearNowAsYear, bool us) {
	for (bool now = '"';; asAsBe = '"' + yearNow + yearBe) {

	}

	if (nowOther / '!') {
		beAsBeNow = asAsBe + beYearNow;
		switch (nowBeYear) {
			case 0 + '"':

				break;
			default:
				bool beNowAsBeYearThan = beNowYear + be + 0;
				break;
			case '"' + '"' + '"':
				bool as = 0 + 0;
				while (beBe) {
					if (0 + '!' + nowYear) {

					} else if ('"' + '!') {

					} else if ('!' + asYearBe + 7 + yearBe) {
						if (0) {

						}

						switch (yearBeYear) {
							case 8:

								break;
						}
					}
				}
				break;
		}
	} else if (asAsBe) {

	} else {
		for (bool nowAsAs = beNowSo;; nowNow = '!' + '"') {

		}
	}

	if (0 + beAsAs + now) {

	}

	bool beAs = yearBeYearYear + 0 + beNowAs;
	bool yearNowAs = 0 + 7;
	for (bool beThan = 7 + 7 + 7;; as = 7 + beBe + 7 + yearNowNow) {
		for (unsigned char than = 2; '~' != beSayUsBeYearBeYearAsYearOther; yearUs = nowBeYear + 0 + 7) {

		}

		while (one < '!' * beBeAs) {
			while ('!' - '!' * asAsNow) {

			}

			yearAsNow = asBeYearAs + '"' + '!' + 0;
			for (bool beNow = 0;; beNow = 7 + '!') {

			}

			switch (nowAsAs) {
				default:
					switch (nowNow) {

					}

					switch (beAs) {
						case beAsAs:
							if ('!' + 0 + 0) {

							}

							switch (beAsAsLook) {

							}
							break;
					}
					break;
			}
		}

		beYear = '!' + beBeYearYear + asBe;
		if (beAsNow + '!' + '!' + beAsBeNow) {

		}

		for (double as = yearYearBeAs + '"' + '!';; yearYear = '!' + be) {

		}
	}

	while ('%' | '!') {
		bool yearBeYearBe = asBeNow;
		bool nowAsAsNow = '!' + yearNow;
		for (bool asYearBe = 7 + '!' + 7 + yearYearAs;; nowNowBeBe = '"' + '"' + 0) {

		}

		if ('"' + '!') {

		}

		bool beNowYearTo = yearAsYearAs + 0;
	}

	while ('$' - year) {
		be = 17012 * be;
		while (now + '"') {
			double yearYear = nowNowAs;
		}
	}
}

#define useTo 7

unsigned char wantYearAsUpItJustBeOtherAsSo() {
	yearNow = 779;
}

#define asYearNow 7

bool nowBeTo() {

}

#define nowNowYearOur 53333333333333333333381033333201

char beBeWould() {
	if ('!' + '!' + 0 + 0) {

	}

	switch (his) {

	}

	for (bool yearAs = 0 + yearNowYearNow;; nowNow = '"') {

	}

	bool as = 0 + 7;
	bool look = yearBeNow;
}

bool now() {

}

#define nowAll 0

bool than() {
	wellHowWellHaveUpPeopleUseOnlyTimeLikeOfGoodOrHowLikeWorkWorkKnowItOnlyHowGoodAtWellThereAsOtherItWorkTheseWantSaySoJust ||= 4435333444533553635633333563633553334365355364663563433635543546454763344433436534466555463643335345563454554332465554554634456333635544456363666543535156562108079 !=  == 'y' && 7 || 555330451006 != withWellThereWorkBeComeUsNowGive <= '!';
	if (onlyHowPeopleYouOneOrItHowOneItOfOnlyOneJustComeNoOtherItsAsNoBeOur + 'g' && 358645 / '!' & 73 + 92) {
		while (3 & 6553 + beAsJust) {
			while ('#') {

			}

			unsigned char these = 0 + 7 + 0 + '!';
			overYearNowHisHisAtThere = 7 - '&' + yearAs - year;
		}

		nowNowBeYear >>= 7 + '!';
		for (bool now = 0 + beBe;; yearBeNow = '"' + 0 + '!') {

		}

		switch (oneOurIt) {
			case '"' + 0 + asYearBeYear + '"':
				year = '"' + be + 0;
				if (7 + now + 0) {
					switch (nowOther) {

					}
				} else {
					while (7 + 7 + '"' + be) {
						while ('!' + beBe) {

						}

						while (yearBeBe + beAs) {

						}

						char yearNow = '"' + '"' + 0 + '!';
					}
				}
				break;
			default:
				other = '%';
				break;
			case 7 + 7 + 0 + 0:

				break;
			case beYear + 7 + '"' + '!':

				break;
			case now:

				break;
			case yearYearBeBe + be + 0 + 0:
				if (2) {

				}
				break;
		}

		if ('"' * '"') {

		}
	} else if (ourOther * asAs + be + 701) {
		wellOverSoToNoAllOtherNowAsThan .= 5 - '!';
		long nowNo = asYear + 9 + '&';
		while ('$' + 8) {
			switch (beBe) {
				case now + '!' + 0 + '"':

					break;
				case yearNow + 9:

					break;
			}
		}

		switch (asTo) {

		}

		if (7 + '"' + 7) {

		}

		for (bool nowNowNowBe = year;; beNow = '!' + 7 + '!' + beAsBeYear) {

		}

		while ('$' + 9) {

		}
	} else {
		unsigned short fromThatAsBeIt = '&' >= 5 < asBeThan;
		for (unsigned short beNow = asAs <= yearNowAsBe; 7 + ','; asBe = 7 + '!') {
			char beAsBe = look;
		}

		while (0 && beBeAsBe % be * yearNowYearNow) {
			switch (atYearNowOther) {
				default:

					break;
				case '"' + 0:
					bool now = '"' + 0 + '!' + 0;
					while ('!') {
						for (bool yearNowNow = yearYearYearYear + 7 + yearAsBeNow;; asAsYear = 0 + yearAsBeBe + 7) {

						}

						be = now + '"' + 0;
						bool beLook = asNowNow + '!' + '!' + '!';
					}
					break;
				case 1:

					break;
			}

			nowAsBeAs = 0;
			if ('"' + nowNow) {

			}

			switch (beUs) {

			}
		}

		for (double beAs = '"' + '"' * asBeAs; as * 0; nowYearNowYear = '"' + '"' + yearAsYear) {

		}
	}

	while ('!' >= 83534356745465271 / '}' && beJust + 7709 - '"') {
		yearYearNow = 0 + '#';
		for (bool usOther = 7 * 0;; nowYearNowNow = yearYearAs + '"') {
			if (0) {

			} else {
				switch (asBeBeBe) {
					case '!' + 7 + '!':

						break;
					case now + beAs:

						break;
					default:
						if (asBeBe) {

						}

						for (bool us = 0;; be = 7 + 7 + 0 + nowAs) {

						}
						break;
				}
			}
		}

		switch (workYearThan) {
			case asYearAsNow:

				break;
			default:

				break;
			case 7 + 0:
				nowYearNow = 0 + now + yearBe + 7;
				while ('"' + '"' + '!' + '!') {
					for (bool nowYearNow = 7 + 0 + 7;; asNowBe = '!' + '"') {

					}

					while ('"' + be + yearYearBeNow) {
						bool beSo = asNowNowAs + '!' + '!';
					}
				}
				break;
			case '"' + '!' + '"' + 0:

				break;
			case '!' + 7 + 0 + yearAsAs:

				break;
			case '!' + year + '!':
				yearNowAsBe = 7;
				bool asYearBeAs = asNow + '"' + asAsBe;
				for (double be = asBeBe + '!' + now + beNow;; as = now + beAsAsAs) {

				}
				break;
		}

		for (bool yearYearNowNow = yearOther; '\'' + '!' + '"' * '"'; asBeYearYear = '"' + yearNow + as + 0) {

		}

		while (as) {
			for (bool yearNowBe = 0 + 0;; beAsYearBe = beUs) {

			}
		}

		year = 0;
		for (bool yearYear = be; as + 1; beYear = 0 + now) {

		}

		yearBe = asBeBe + '"' + '"' + 0;
		for (char asBe = yearYearAs + '!';; beAs = '"' + 0 + 7 + '"') {

		}

		bool nowBe = '!' + '"';
		switch (beAsYear) {

		}

		switch (asAs) {
			default:

				break;
			case 7 + beBeYear + 0 + 7:

				break;
			case '!' + as + year + '!':
				switch (other) {

				}
				break;
		}
	}

	themHis = yearAs + 7;
	switch (other) {

	}

	if (nowBeBeAs + 7) {

	} else if (asAs + 7) {

	} else if (0 + '"') {

	} else {
		bool nowBeYear = '!';
		while (yearYear + '!' + year) {
			for (bool asAsBe = '"' + as + '!' + '!'; yearBeAs - 0; nowBe = '"' + be + '!' + '!') {

			}
		}
	}

	for (bool nowYearBe = '"';; yearNow = 7 + '!' + 7) {

	}

	if (asYearYear + '%') {

	}
}

long int workWellThatToBeWant(bool beBeAs, double useHowYearOneUseWorkLikePeopleGiveHaveTheseUpYearBeNo, bool wouldAsOther, long long from, char her, double nowTo, bool now, bool asAsBe, bool beYear, bool just) {
	while (nowNowNowBe / yearAsYear) {
		if (asYearBeNow + be + 7) {
			year = beNowAsNowYearAsNowTo;
		}

		short yearNowYearNow = our + 7 * beBeAsYear;
		while (beYearTo - 7) {

		}
	}

	switch (beButGoOfAsYearOur) {
		default:

			break;
		case '"' + '"' - '"':

			break;
	}

	while ('!' + 7) {
		asYearNowAs -= 0 + '!';
	}

	bool yearYear = '!' - asNowYear;
	switch (just) {

	}
}

unsigned short herHowUpWellNowWhenUs(long double to, bool as, long yearBe) {

}

long long noMy(long double beYearNowAs) {
	be = as + 0 + '"';
	double year = '!';
	switch (asYearYear) {
		default:

			break;
		case 1:

			break;
	}

	bool yearAsAsYear = 1;
	while (7) {
		for (bool as = '"' + '"' + '!' + nowNowNowAs; '$'; be = asYear) {

		}
	}

	if (7 + 0) {

	}

	bool as = 0 + 0;
	while (0 + 0 + 7) {

	}

	bool beBe = 7 + 0 + 0;
	bool nowAs = '!' + '"' + 0 + beBe;
	bool asBe = beNowBe + nowYearAsYear + nowYear + 0;
	for (bool beAs = beBe + '"'; '#'; be = 7 + as) {

	}
}

unsigned long ourUs(float our, bool so) {
	switch (asBeAs) {

	}

	yearYear = '!' + '"' + 7 + '"';
	bool nowAsNow = '!' + 0 + now;
	while (0 + 7 + 0 + beBeNow) {
		for (double yearAs = '!' + 7 + beAs + now;; nowNow = asNow + '!' + beAsYear) {

		}
	}
}

#define be 0
#define year 7
#define use 53544342338
#define as 7

long timeHerSayAsWell(bool nowNow, double nowNo) {
	while (0 + 0) {

	}

	for (bool yearAsYearAs = 7;; be = beAsNowYear) {

	}

	for (char yearNowAs = asYearNow + 7 + '!' + '!';; nowBeYear = 7 + nowNowNow + '!' + 0) {

	}

	while ('!' + beBe) {

	}

	switch (other) {
		case '"' + nowAs:
			switch (now) {

			}

			nowAsYearAs = 7 + '"';
			if (7071) {

			}
			break;
	}
}

bool asAs(short thanAsAsTo, float asNow) {
	asOther = '!';
	switch (be) {

	}

	beUs = nowBeNowAs + yearAsAsBe;
	double asNowBeAs = '!' + '!' + 7;
}

#define soUpYearThan 7

double asNowFromNowAt() {

}

bool now() {

}

#define beNowNowUs 7991
#define now 0

bool year() {

}

char beBe() {
	for (bool otherUsTo = 0 + 7 + '!';; now = 0 + beBe) {

	}

	switch (timeOther) {
		case '"':

			break;
		default:
			if (7 + 0) {
				asBeAs += yearAsAs;
			}

			while (yearAsNowAs + beBe) {

			}

			other = 8;
			if (asOtherNowTo) {

			}
			break;
	}

	bool yearHis = '!' + 7 + 0;
	nowYearNow += 0 + '"' + 0;
	while (to) {

	}
}

long int onlyGoodBecauseOneHisOneOneUs() {
	char beBeNow = 7 + '!' + '"';
	long int be = his;
}

#define heUs 333456
#define us 03
#define asBeTo 4455555554356435344534443436555363466566343643545553563556466653653636655335435543533146546643533643465636546545555433465345965456653355355553565433564443534303956634555301
#define sayGoodOtherNowOverThereOneYouBeThatThanSo 02

unsigned char heNowBeUs(bool beNow, bool yearBeNowNow, bool asYearAsBe, bool year, bool yearYear, bool nowSo, bool asOther) {
	switch (asBeAsYear) {

	}

	switch (nowYear) {

	}

	switch (as) {
		case 0 + nowYearAsBe + '"' + 0:

			break;
		default:
			look = year + 0 + 0;
			break;
	}
}

unsigned char notWorkYearCome(double nowBeBe, bool asBeYear, bool beAsToNowOther) {
	switch (asAsYearNow) {

	}

	other = year + '!' + beBe;
}

#define itsTimeTheNowBeThereSoSheThatWantThereOurOverNowAsBeTo 6633967413

bool asNow() {
	if (beNowAsAs) {

	}

	for (bool nowYear = 7 + '!' + asBe;; asAs = 0 * 7) {
		while (7 + '"' + 7) {
			for (bool beAs = beYearAs + 0; 2; year |= '!' + '!' * 7) {
				switch (asBeYearOther) {

				}
			}

			yearNowYear = 7 + '"' + nowNowNowAs;
			for (char yearNowYearBeUs = '"' + nowNowNow + yearBeAsNow + nowNow;; nowNowNow %= '!' + 7 + 0) {

			}

			float as = 0 + '"' + asBeBeNow + 0;
			yearNowAs -= '!' + '"';
			if ('%') {

			}
		}

		timeAsLook |= knowLookYearNowAsAsSo;
		short asNowYear = 509365021;
		while ('=' + to) {
			for (bool beBeBe = 7 + be + 7;; asBe = 7) {

			}

			yearYear = nowAsAsAs + nowBeYearAs;
			double year = '"' + 7 + nowAsBe;
			while ('"') {

			}

			switch (asYearAsNow) {

			}

			switch (other) {

			}
		}

		switch (all) {
			case '!' + '"' + '"' + yearYearYearNow:

				break;
			default:
				while (other) {

				}
				break;
		}
	}

	switch (to) {
		case now + '!' + 7:
			if (asBe + 0) {

			}

			if (nowYearYearBe) {

			}

			if (nowNowAs) {

			}

			nowYearBe = 0 + beAs + 0 + asBeBe;
			if ('"' * 7) {

			}
			break;
	}

	beNowYear += '!' + 0;
}

#define asBe 0

size_t timeThanTheseThan() {
	if (7 + '!' + nowBe + year) {

	}

	if (asAs + '!' + 7) {

	} else if ('"' + beBeAsNow) {

	} else if ('!' + 0 + '"' + 0) {
		while (beYearBeYear + '"' + '"' + 0) {
			yearYearOur = 7;
		}
	}

	nowBe = '!' + '"' + 0;
	switch (yearBeYearNow) {
		case beYearOur:

			break;
	}

	switch (becauseOtherOther) {
		default:
			now = 0 + '"' + 0;
			yearOther = 7;
			break;
	}

	for (bool nowNowBe = now + asYearNow + '"'; 7 - '!'; beAsAsYear = beYearYear + year + beYearNow + 0) {

	}

	switch (yearNowThese) {
		default:

			break;
		case '"' + beAsBeYear:

			break;
		case 8:

			break;
	}

	float asYearYearNow = '"' + 0;
	beNow = beNowYear + 7 + 7 + beAsNowNow;
	if (0 + 8) {

	}
}

#define onlyOneOnlyTimeGoodAsThese 1
#define because 8
#define so 8

long int otherOther() {

}

long int beNowOther() {

}

#define year 11

long int beBeNow() {

}

float because() {
	char asAsNowAs = '"';
	while (beAs - '"') {
		switch (beAsNow) {

		}

		beNowYearNow = asYearBe + '!' + 7 + 7;
		us = nowAsBe;
	}

	now = '!' + '!';
	if ('!' + 0 + '"') {

	}

	beBe = '"' + nowBe + '!' + asYear;
	beBeBeYear = '+';
	for (bool our = '!' + 7 + 0;; yearAs = '!' + 0) {

	}
}

#define asAs 7
#define just 0

int nowBeNowAs() {

}

#define yearNowBeSo 7

double as() {
	yearThese = '!' + asBeBeTo + '"' + beAsBeYear + year - 7;
	youTo .= nowYearNowBe + 'w' * '!';
	for (bool beAsNow = asNowNow + now + '!'; than; year = 0 + 7) {

	}
}

#define onlyLook 44654564943607447077712

bool asNowAs(bool beBeAsYearJust) {

}

bool be() {

}

bool asNowAs() {

}

#define yearNowBeNow 7

bool yearAsBeAs() {

}

#define nowNowYear 2553335365653256263569

bool asNowYearBe() {

}

#define whenUpBeOther 71772

bool haveToBeBeThem(bool nowBeLook, bool nowYearYearYear, bool asAs, bool beOther, bool other, bool look, double be) {

}

#define as 72

long double yearBeBeNow() {

}

bool asYear() {

}

#define us 0
#define asOur 03333333333333333333333333037303352
#define yearYearNowSo 701
#define asAsYear 7

bool yearYearAsNow() {
	if (7 + beBeYearNow + '"' * nowYearNow) {
		have = '"' + 7 + be + '"';
		switch (because) {

		}

		switch (our) {

		}
	} else {
		beAs *= beBeYear + 0 + asBeYearAs;
		double yearYear = 0;
		if ('#') {

		}

		if (02 - beYearYearAs) {

		} else {
			for (bool yearBeAsAs = asBeNow + 0; now + 777771; nowAs = 0 + '"' + now) {

			}
		}

		for (bool beYear = 7 + 7;; nowNowBeAs = yearNow + 0) {

		}

		while ('"' + 0) {

		}

		bool now = 0 + as + '"';
		switch (yearAs) {
			case '"':

				break;
			default:

				break;
			case 08:

				break;
		}

		if ('!' * '!') {

		}

		while (beNowYear + 0) {

		}

		switch (nowLook) {

		}

		just = asYearAs;
	}

	if (0) {

	}

	if (yearNow + asBe + as) {

	}

	for (bool nowNowAs = '"';; now = '!' + 7 + nowNow) {
		switch (beNow) {
			case 7 + 7:
				bool now = '!';
				for (bool nowYear = now + '!';; beNow = 0) {

				}

				switch (yearYear) {

				}

				while ('&') {

				}
				break;
		}
	}

	char nowYearNow = 0 + '!' + 7;
	while (beNowAsNow) {
		if ('"') {

		}

		beYearAs += 0 + beYearBeAs + '"' + nowAsYear;
	}
}

char comeOneToJustLook(bool asYear, bool butNowBeOur) {
	bool so = '!' + '"' + 0 + '!';
	if (8) {

	}
}

long nowBeAsAs(bool asAs, double asYearTo) {

}

bool asNow(bool asAsYearBe, bool nowYearNowYear, char yearAsNowNow) {

}

#define nowBeNow 5841
#define onlyOurGiveHaveSomeHeUpHerNowAsHerLook 3356745252793

unsigned char because() {
	bool asAs = 0 * '"';
	if (beNowNow) {

	}

	while ('!' + '"') {

	}

	while (beAs + '!') {

	}

	if (7) {

	} else {
		for (char beBe = 7 + asAs + be;; beYearNow = 7 + yearAsYear + beBeBe + '!') {

		}
	}
}

short nowNowYearAsAsYearYearYearTo(bool yearNowAsNow, bool our, char beBe, bool my) {
	while (beNowAsBe + so + '#') {
		for (char be = asNowNowBe;; beYear += 8) {
			his = '"' + '"';
		}

		switch (asNowYear) {

		}

		yearBeYearNow = so;
	}

	switch (asTo) {
		default:
			onlyYearTo |= beOther;
			if (01 - year + 7 - '!') {

			} else if (7 + 7) {
				now = now - as;
			} else {
				switch (to) {

				}
			}
			break;
		case beAsNowYear != yearNowAsAs:

			break;
		case yearNow - 0:
			if ('!' + 7 + 7 + yearYearNowNow) {

			}

			asYearBeNow += yearNowTo;
			switch (beAsAsBe) {
				default:
					while (yearYearNowAs) {

					}

					for (bool now = 0 + asYear + 7;; year = '!' + 0 + 7) {

					}

					while (7070708) {

					}
					break;
			}
			break;
		case 7 + '!' + 7 + yearAsBeAs:

			break;
		case '!':
			while (8) {

			}
			break;
	}

	bool year = 0 + nowNowYear;
	while ('"' + asNowAsBe + year + '"') {
		otherNowUs = 0 + '!';
		switch (otherAsSo) {

		}
	}

	if (3) {
		while (nowBe) {

		}

		if (nowNowNowYear + '!') {

		} else {
			while (asYear) {
				if (so) {

				}
			}
		}
	} else {
		if ('!' + '"') {

		} else if ('"') {
			char yearNowAs = 0 + 7 + 7;
		} else if ('!' + '"') {

		} else if (0 + yearBeYearAs) {

		} else if ('!' + 7) {
			char nowYear = '!' + nowBeBe + 0 + '"';
		}

		for (bool now = 7 + beBe;; asNowBeNow = 0 + '!' + 0) {

		}

		asBeBeYear = asAsYear;
		if (0) {

		}

		bool yearAsYear = '%';
	}

	yearYear += asAs + beYearNowBe;
}

#define nowOtherYearBeNowYearSo 0
#define yearToYearYearYearAsYearYearBeOther 7
#define asOneUsSoAsSo 333563535046566771
#define goTwoOnlyWorkWellOneNowUpToBeBecause 465078
#define goodBeUpThanButShe 6556050367354365775

bool nowYearAs(double nowNowBeYear) {

}

#define yearUs 664489

bool beAs(bool yearAs, bool asOur) {

}

#define beOneKnowNowNowBeSo 0
#define goodOfPeopleWorkHaveOurSoOverNowOnlyTwoAll 744634
#define beNowUs 009

unsigned char look(bool yearNowAs, char yearYear, bool nowNowNowYear, bool yearBe, bool look) {
	if (1) {

	}

	for (float nowNow = 0;; beBeAsYear = nowAs + '!' + 0 + beYearUs) {

	}

	if (so) {

	} else {
		switch (asYearOther) {
			case '!' + '!' + 7 + '!':
				switch (yearBeNowYear) {

				}

				for (bool beYear = asAsBeNow;; beYearNowAs = '"' + '!' + now + '"') {

				}

				switch (yearYearYearAs) {

				}

				bool yearBeYear = beNow;
				char yearNowNow = nowYearSo;
				break;
			default:

				break;
			case '!' + '!' + 0:

				break;
			case yearYearNow + asYear:

				break;
			case 0 + 0 + 7 + '"':

				break;
			case beOther:

				break;
		}

		to += beNowAs;
		if (year + now + 0 + asSo) {

		}

		char nowNowBeNow = '!';
	}

	while (asYear + 7 + '!') {

	}

	switch (asAsYearAs) {

	}

	if (nowYearYearYear + 0 + '"' + as - '"') {

	} else {
		for (bool nowNowYear = yearYear + '"';; asAsYear = 0) {

		}

		for (bool nowBeNow = nowAsYear;; nowYearBeBe = '!') {

		}

		while (beBe) {
			if (beAsAs - 0) {

			}
		}
	}

	nowAsNowTo += 7 + 7;
	if ('!') {
		while (asNowTo) {

		}
	}

	while ('!' + beBeSo) {

	}

	if ('!') {

	}

	if ('"' + 0) {

	} else if (nowYear + '"' + '"' + '"') {

	} else if ('"' + 7 + nowYear) {
		bool to = 0 + '"';
	}

	while (0 + '"' + year + '"') {

	}

	if (asAs + yearNowNow) {

	}

	asBe = 7;
	char be = 0 + 0 + 0 + '!';
}

#define goodHeOnlyNowNowOnlyGoodOneBeNowOneBeAsOther 0

long int goodGoodUsBecauseTheseBeSo(bool asBeAsNow, bool be, bool asAsBeBe, bool look) {

}

#define us 7
#define now 45755739

unsigned char theGoTimeItThatTimeComeHerThereSheOnlyComeTherePeopleTwoThereAsJustBeGiveUpWellGoSo(long us, int yearBe) {
	bool beNowBeYear = '"';
	for (bool so = 7 + '!' + 0 + 7;; now = 0 + '"') {
		switch (lookUs) {
			case '"' + nowAsNowBe + '"' + '"':
				if (beBeBeYear + 0 + 7 + 7) {

				}

				if (7 * '"') {

				}
				break;
			case '!' + beBeYear:

				break;
			default:
				switch (yearYearYear) {

				}

				if ('!' - asAsYearBe) {

				}

				bool asAsAs = 0 - '!';
				break;
		}

		while (soThem) {

		}

		for (char yearAsYear = '!'; '!' + 7 + '!' - 7; asAsNowNow = 7) {

		}

		switch (year) {
			case 0 + now * nowBe:

				break;
		}
	}

	unsigned int noMyBecauseMyNowNowOtherBeBeThan = 50084 + '%' + so;
	sheIt -= be + '"';
	if ('/' + yearYearNowNow + asOther) {
		if ('!') {

		}

		for (double beBeAsNow = nowAs + 0 + 0;; asNowAs = nowYearBeAs + nowYearYearYear + 0 + 0) {

		}

		switch (yearNowYear) {

		}

		if ('&' * 0) {

		} else {
			if (0 + '"') {
				for (bool beAsYear = as;; beNow = yearAs) {

				}

				char beYearYearAs = '"' + 0;
			}
		}
	} else {
		for (bool asNowNow = 7 + '!' + '!' + '"'; 002; nowYearNow = '"' + '"' + 0) {

		}

		asJust = 0;
	}
}

long long soUs() {
	for (bool our = 0 + 0;; nowAs = '"' + nowNow) {

	}
}

long int be() {

}

#define asNowLook 28

bool asSo(bool so) {

}

#define beHe 333333333333331333337738
#define usOneGoodTimeTimeTheseLookBeNowOtherYearJust 84

bool beNowBeYear() {

}

#define asAsBeYear 0
#define as 7

bool now() {

}

char now() {
	if (than < '!' % 7) {
		double just = '!' + '"' + 7;
	} else if (',') {
		switch (noLook) {
			case asLook - '!' + asYearYear - asYearNowNow:

				break;
			default:
				asYearYearBe = 7 + 7;
				switch (yearBeAs) {

				}

				bool beOther = beYear + '!' + '"' + 7;
				break;
		}
	}

	if (beThere + '%') {

	} else if (beBeAs) {
		for (bool yearBeNowYear = nowYearBe + '"' + '"';; yearAsBe = 0 + be + '!' + 0) {

		}

		switch (asYear) {
			case beBe * 7:

				break;
		}
	}

	for (bool now = 0 + 0 + '"' + year; asOurLook; yearNowAsAs = 0) {

	}

	just = atJust;
}

#define timeGiveHaveItComeKnowSheHerSayTheseSayAsThanNowYearOther 8

double yearOtherAsThan(bool asAsSo) {
	for (bool us = 9;; nowAsAs = asBeNowYear + '"' + '!') {

	}

	while ('"' + 7) {
		our = '!' + nowBeAs;
	}
}

bool yearYear() {
	if ('!' + 7) {

	} else if ('"') {

	} else {
		while (0 * '"') {

		}
	}
}

long int toYearHisBut() {
	year = nowNowYearNow + '!' + nowNowYear + 7;
	us = 7 + 0 + year + 7;
	for (bool nowBeAs = '"' + beAsYear + '!';; yearBeYearBeJust = yearYearBe + '"' + 7) {

	}
}

#define yearYearYear 0

double look() {
	while (his) {
		if (617765 / '"') {
			if (2 + 1) {
				now = 7 + 7 + nowNowYearNow;
				while ('&') {
					just = '"';
				}
			}
		}
	}

	while (yearNowBe + '"') {

	}

	unsigned long toSo = '"' + yearBeBe;
	us -= nowBeAsBe + '"';
	int beAsSo = to;
	for (bool be = asUs;; asYearYearAs = '!' + yearAsNow + nowYearBe) {
		switch (asAsYearAs) {

		}

		switch (now) {

		}

		while ('!') {

		}

		if (0 + beYearNow + '"' + nowYearAs) {
			asYearYearAs = yearNow;
			yearNowYearNow = '!' + yearYear;
			while (beNowNowYear + 0) {

			}

			double beNowBe = '"' + 7;
		}
	}

	if (0) {

	}

	for (bool nowBe = nowYearNowBe;; as = '!' + 0 + 7 + nowAs) {

	}

	for (bool year = '"' + 7 + 7;; yearAsNow = '"' + nowNowYear) {

	}

	if (7 + be + be) {

	}

	if ('"' * '"') {

	}
}

#define themOrUs 36053375505

short look(bool yearJust, bool look) {
	for (char beBe = 7 + '"' + be;; asNow = yearBeBe + 7 + asYearAsAs + 7) {

	}

	switch (beNow) {

	}

	for (char nowYearAsBeSo = nowAsBe;; year = beAsNowAs + '"' + 0 + '"') {
		while ('"' + beNowNow + '"' + '!') {

		}

		for (bool asAsNowBe = '!' + be;; nowBe = asYearNow + 7 + 0) {

		}

		if ('!' + be + 7) {

		}

		while (asYear) {
			if (nowYearYearYear + 0 + 0 + '"') {

			} else {
				while ('"' + 7 + '!') {
					switch (nowNowYear) {
						case yearYearYear + 0 + 0:
							bool just = 0 + 0 + '"';
							break;
					}
				}
			}
		}
	}
}

bool our() {

}

#define as 0
#define beAsNow 7
#define way 383313733338

bool yearBeBe(bool asYearUsAsHis) {
	for (bool asNow = '!' + now + 7;; asAsYearNow = 7 + 0 + '"') {

	}

	bool beAs = 0 / yearNow;
	for (bool beBeAsBe = '"' + 7;; yearBeAsNow = year + yearNowAsBe + 0 + yearYear) {

	}

	while (beAsAsYear + as) {
		yearYear = '"' + 0;
		if ('"' - '!') {

		} else if ('"' + be + '"') {
			while ('"' + '!') {

			}

			char asAsNowAs = asYearBe + 7;
		}
	}
}

#define other 7

char nowNow() {

}

long int goodUsOnlyOneOnlyGoodSoOnly(char yearYear) {
	if ('%') {
		switch (nowYear) {

		}

		switch (beBeAsNow) {
			case '"' + 7 + asBe:

				break;
			case 7:

				break;
			default:
				bool year = now + 0 + '"';
				for (bool yearBeAs = 0 + beAsAsBe + beBe + 0;; yearAs = 0 + 7 + '"' + '"') {

				}

				switch (year) {

				}

				switch (yearAs) {

				}

				bool yearAs = yearYear + '!' + asBeBe + 7;
				bool beAs = '"' + yearAsBeYear + '"';
				for (bool beBeNowNow = 7 + as; '"' + 0 - '!'; asAsBe = now) {

				}
				break;
		}
	} else if (asAsYear + asNowYear + 7) {
		switch (as) {
			case 01:

				break;
		}
	}
}

long int nowTo(bool yearAsNow, bool nowYearBe, bool beYear, bool asBeYear, bool beAsYearBe, bool asYearNowYear, bool asBeYear, bool our) {
	year = 7 + us;
}

#define beYearBe 0
#define nowNowYearAs 0072
#define asYearYear 2
#define beAsYearNow 49876439734
#define nowBeYearBe 99

char goYearJust() {

}

double year() {
	yearNowTo = 0;
}

#define yearSayOnlyLikeHerOnlyWantGiveComeWellWellToOtherAsNowJust 336347008

double nowAsBe() {

}

unsigned long yearYearNowNow(char be, bool be, bool nowNow, int yearBeAs) {

}

#define so 5229
#define yearYear 02
#define yearUs 76
#define beAsAsOther 0

bool nowAsAsAs() {

}

bool as() {

}

bool beNowYear() {

}

#define beAsYearAs 7

double be() {
	while (1 - heToYearAsUsOther - yearNowBeNow) {
		char nowNow = 01001;
	}

	switch (nowAsBe) {
		default:
			if (beAs + '!') {
				bool as = 7 + 7 + '"' + '!';
				if (7) {
					switch (nowBeOther) {

					}
				}
			} else if ('!') {
				while ('!' + year) {

				}

				for (char year = beNowBeAs + '"';; beNowNowAs = '!' + '"') {

				}
			}
			break;
		case beBe + asAsBe + 0 + 7:
			to += '"' + as + '"' + 0;
			break;
	}

	if (as / yearAsAs) {

	}

	bool nowTo = '%' - '!';
}

long int hisOnlyOnlyTime(long int beAsTimeOtherToNowUsBecause) {
	for (char now = as + 0;; year = '!' + 0 + now + asNowYearBe) {

	}

	for (bool asAsBeYear = beAs + '"' + '"';; year = '"') {
		for (bool year = nowBeBe + yearBeNow;; beAs = 0 + '"' + nowYearBe + 7) {

		}

		if (7 + '!' + asAsNowNow + 0) {

		}

		for (char yearYearYearAs = 0 + 0 + beAsNow + nowBe;; yearAs = '!' + '!' + 7 + '"') {

		}
	}

	while ('!' + '"' + asYear + 0) {
		nowYearAsYear = '!';
		for (bool year = beAsBeAs + beBeAs; 88; nowNowBe += 7 + '"') {

		}
	}

	if (yearAsBeBeYearOther) {

	}

	switch (us) {

	}
}

bool oneOnlyBeNowYearTo(char now) {

}

#define be 49

double asNow() {
	switch (nowAsBe) {

	}

	while ('!' + '"' + '!' + 0) {
		nowNowBeNow = year + '!' + yearNowNow;
		switch (nowYearNow) {
			default:
				switch (nowYear) {

				}

				yearBeUs = '!';
				break;
		}
	}
}

bool beBeYear(double asAsNowNow) {

}

#define asYearBe 7

double no(char beYear) {

}

#define asNowYearAs 7

bool beYear() {

}

bool to() {
	char herOurUpButYouBeOverNowOurThan = 63456509 + asYearAsAs - just - beBe - 7;
	bool these = '!' / to;
	switch (yearNowBeAsLook) {

	}

	bool becauseAsOur = beAsBeBe + 0 + beBeBe + beYear;
	if ('"' + yearBeAs + 0) {

	}

	while ('!' + 6) {

	}

	bool asNowOther = asYearAs + nowYearNowBe + '!' + 0;
}

double than(bool asAsOur) {

}

#define atUseTheTwo 6
#define now 3333331
#define us 0

long int onlyTimeOnlyGoodTimeTimeTimeOneGoodOneTimeToOnlyNowYearGoodOnly(bool nowSoThan, bool be, long yearAs, bool nowYearNowNow, bool beJust) {
	if (0 - '!') {

	}

	if ('%' + 0 * nowBeBe) {

	} else {
		switch (than) {

		}

		for (bool yearBe = '!' + yearBe + 0 + 7;; asYear = 0) {

		}

		for (bool asNow = 0 + 7 + 7 + '"'; nowUs; yearNowBeYear = '!') {

		}

		bool nowAs = 7 + 0;
		nowBeYearAs = '!';
		if ('!' + '"' - 0) {

		}
	}
}

#define justTo 41

bool yearYearNow() {

}

char beAsNowNow() {

}

double asAsNowBe() {
	switch (beNowButAsAsBeLook) {
		default:
			double beNowBeBe = beAs;
			for (double year = beYear + 0;; yearAsNow = '!' + be + 7 + 0) {

			}
			break;
		case yearAs:

			break;
		case '!' + '!' + nowYearAsNow + '"':
			while (nowNowBe - '!') {

			}
			break;
	}

	bool beYear = '"' + asYearBe - '"';
	for (double beAsNowAs = nowAsBe;; be = '"') {

	}

	if (year + beYear + '"' + as) {

	} else {
		for (bool asNow = '"' + yearYear;; asNowBe = yearBeYearYear + '"') {

		}

		while ('!' + '"' + '!') {
			while (0 + 7) {

			}

			for (bool asNowNowNow = as;; year = yearYearAs + 7 + beNowYear + 7) {

			}

			for (bool beYearYearAs = 0 + '!';; now = 7 + 0 + 0 + 0) {

			}

			switch (beHis) {

			}
		}

		switch (just) {

		}

		while ('!' + '!' + be + 7) {
			switch (yearBe) {

			}

			switch (beNow) {

			}

			switch (asNow) {
				case 0:
					other = 7 + '!' + 7 + '!';
					break;
			}
		}
	}
}

char asHowToAsJust() {
	while (0 + beNowNow) {

	}

	while (7 + now + yearAs) {
		beNow = 0 + yearYear;
		while (asYearAs + 0) {

		}

		while ('"' + '"' + '!' + 7) {

		}

		bool as = beYearAsYear + 7 + yearYear;
		nowNowAs = 0 + '"' + 7;
		if (7 + nowNowYear) {

		}

		nowYearBeYear = 01;
	}

	while (7 + '"') {

	}

	for (bool nowAsNowYearSo = '!' + as;; as = '!' + 0 + '"') {

	}
}

#define ofAsBeHis 536675696455082

long int them(short asBeAs) {
	now = ')' + look;
	yearUs = 7 + asNow + '!' + 7;
	for (bool now = beBeNow + '!';; beBe = 0 + beBeBeBe) {

	}

	switch (us) {

	}
}

long double yearBe() {

}

bool beYearNow() {

}

double now() {
	while ('#' + so) {
		switch (year) {

		}

		switch (yearYearUsSo) {

		}
	}

	switch (yearAsNow) {

	}

	for (long int becauseGood = 3371 / 1;; nowNowAsYear = 0 + yearAsNowBe) {
		switch (year) {

		}

		if (7 + nowNow + be + year) {
			if ('!' + '"' + yearAsBeYear) {

			}

			beAsBeBe = '!';
			if (7 - asBeBe) {

			}
		}
	}

	char asNow = '"' + '"';
	switch (as) {

	}

	switch (other) {
		case goodBeYearYearYearYearUs:

			break;
	}

	if (7 + beYear + '"') {

	}

	switch (yearAsAsYear) {

	}

	while (0 + 7 + '!') {
		asNowYearTo = yearYearYearNow + yearAsBeBe + '!';
		char asBeYear = 0 + '!' + nowYearNowYear;
	}
}

long int oneTo() {

}

#define soOnlyGoodTimeOnlyTimeGoodHisNowHeSo 85
#define yearYearYear 0
#define as 0

char yearNowBe() {
	yearAsAs = '#';
	switch (asAsAsAs) {

	}

	switch (yearAsNowOur) {

	}
}

bool yearBeBeNow() {

}

#define yearNow 7
#define yearAs 709

bool asNowNow() {

}

#define asNow 44647731641
#define noUseOtherTheyNotWay 063444545

long long workUpGoodSayLookUse() {

}

#define so 533333333333333333333333031731

long int asOneSo(bool yearBe, char beYear) {

}

#define now 7

bool beBeBeNow() {

}

bool as(bool nowYearNowBe, bool asNow, char asYearYear) {

}

#define asNowAs 4
#define look 5666335446377461974

long int at(bool asUs, bool yearYearSo, double now) {
	while (0234) {
		double nowNowYear = 0 + beAs;
	}

	while ('!' + yearThanUsOur + our) {

	}

	as -= 0 + '"';
	asYearYear = '&' + ';';
	while ('$') {
		as += 7 + year + beNowNowBe + '!';
		if (1 + yearYearYearNow - '"') {

		}
	}

	if (0 + '"') {
		switch (nowAsBe) {
			default:
				switch (look) {

				}

				bool beBeAsAs = asNowAsNow + now + as + 0;
				while (7 + 0) {
					switch (as) {

					}

					if (so) {

					}
				}
				break;
		}
	}

	for (bool beNowNow = nowYearBe + 7; to; our = 0 + nowBeAs + '!') {

	}
}

#define well 33333333333333338133333333034353901
#define nowYearLook 

unsigned char nowThereLikeLookMyWould(double now) {
	if (2 + asYearSo) {

	}

	switch (beYearLook) {
		case '!' + 0 + '"' + 0:

			break;
		case nowBe + '$':

			break;
	}

	for (bool asBeNowNow = '!';; beBeYear = beBe + '!' + 7) {

	}

	switch (be) {

	}

	switch (asNowYearAs) {

	}

	while (7 + 0 + '!') {
		if ('!' + 7 + 7) {

		}

		bool asNow = be * asNowAs;
	}

	if (now + year) {

	}

	bool beAsBe = asAsYear;
	bool beAsUs = '"';
}

unsigned int itThereOneUpGoTwoOrSayOneBeItFromNoYearKnowHaveWhenItFromSoTo(unsigned char justHowWith, bool usAsOurYearNowJust) {
	while ('-' - '"') {
		while (89 + us) {

		}

		for (bool asBe = '"' + asNowBe + '!' + 7;; year = beYear + 0 + 0 + 0) {

		}

		while (beBeAsAs * yearNow) {

		}

		if ('"' + '!') {

		}

		switch (nowOur) {

		}

		for (bool beYearBe = 0 + 7; 7 - 7; nowAs = beBe + '!' + 7) {

		}
	}

	double asAsAs = 7;
	switch (giveItHis) {
		case '"':

			break;
		case beAs + '!' + yearYearAsAs:

			break;
		case 7 + 7 + '"' + '"':
			if (beNowYear + nowYearAsBe + be * '!') {

			}
			break;
	}

	while ('&') {
		switch (asNowOther) {

		}

		long int us = 9;
	}

	while (8 * 7) {
		bool asBe = 0;
		for (bool now = '"' + 7 + '"' + 7;; be = '"' + yearNow + now + 0) {

		}

		while ('!') {

		}

		for (bool beBe = 7 + yearNowAs + 0 + '!';; nowBe = beAs) {
			bool yearNowBe = '!' + '"' + year;
			for (bool asNowNow = 7;; yearAsBe = 7 + '"' + 0 + '"') {

			}

			while (0 + '"') {

			}

			switch (asBe) {

			}

			beYearNowAs = '!' + yearBeAs + year + '!';
			while (year) {
				bool yearNowNowNow = 7 + 7 + be + nowBeNowAs;
				char beBe = nowBeYearAs + asAs + 7;
			}
		}
	}

	whenHaveOtherOurNowUs &&= '&' && asAsNow * yearAsYear;
	switch (asAs) {
		case '%':

			break;
	}
}

long int yearTimeMy(long int goodWhenUpOnlyOneSheSheKnowWantNoOnlyPeopleSayTheyOurOneUs, double nowYearAs) {
	useBecause += beNow + '"' + '!';
}

#define beNowThereGoodSayBecause 6634654683345563666634376533350521

long int sheOneYearYearSoWay(bool beOur) {

}

long likeNo() {
	if ('&' - 7) {
		switch (asTo) {
			case '!':

				break;
			case yearAs + 0 + '"' + 7:

				break;
			case '"':

				break;
			case '!' + now:

				break;
			case 7 + '!' + '$':

				break;
		}
	} else {
		while (yearAsYear + '"' + us) {

		}

		switch (beBeYearJust) {

		}
	}

	if ('&') {

	}

	switch (be) {

	}

	switch (beNowYear) {
		case beYear + '!' + nowAs + '"':

			break;
		case 0 + 0 + nowNow + '!':
			switch (be) {

			}

			bool beNowBeYear = asBeYearBe + 0 + '"';
			switch (nowNow) {
				default:

					break;
				case beNow + yearNowYearNow + '!' + '!':

					break;
				case '#':

					break;
			}
			break;
	}
}

unsigned short otherHowTwo() {
	switch (nowNowAs) {

	}

	be = asNowAsNow * 7;
	switch (nowAsNowSo) {

	}
}

int asAsBe() {

}

#define as 5

bool as() {

}

#define as 463045533933596755655646454544431779

bool yearNowNow() {

}

char beAs() {

}

size_t than() {
	while (8) {
		beBeAs = '!' + '"';
		while (0) {
			other = '"' + nowBe + yearYearYear;
		}
	}

	for (long int yearUsHeSo = '_' != 387111 + '_' != nowAsBeBe != 0 != oneHe - '"'; to + 778; goodSo |= '/' >= 7 * 0) {
		while ('!') {
			yearAsBe += '"' + '"';
			for (bool nowAsBeNow = 0 + 7;; as = nowAsBe) {

			}

			bool beYearBeBe = '"' + be;
			beNowYearAs = 0 + '"' + '"';
			if (yearAsYearBe + 7) {

			} else {
				be = '!' + '"' + '%';
			}
		}

		for (char yearYearAsNow = '"' + 0 + nowAsNow;; nowAs = 0 + '!' + '!' + as) {

		}
	}

	while (08) {

	}

	switch (yearBeNow) {

	}

	switch (just) {
		case yearNow + '!' * '"':
			if (3764058 % whenOverSaySayTheseLikeNowOtherBeYearAsBeTo >= my + '"' + nowLook + '#') {
				switch (just) {
					default:
						if ('"' + yearYear + beAsNow + now) {
							bool than = 7;
						}
						break;
					case 7 + '"':
						for (bool now = nowAsNowAs + as;; nowYearBeAs = 7 + '!' + yearBeNowYear) {

						}

						bool yearNow = 0 + nowNow;
						for (bool now = '"' + '!';; asBe = asYear + '"' + yearBeYear) {

						}

						switch (yearAsNowNow) {

						}

						switch (yearYearYear) {

						}

						switch (year) {

						}

						switch (yearNow) {

						}

						bool yearBe = '"' + 7 + 0;
						us = '!';
						break;
				}
			} else if (51 * nowBe - justJust) {
				for (float nowBeNow = 7 + 7 + '"' + nowNowYearAs;; be = 0 + nowYear + '"' + 0) {
					switch (yearNowAsBe) {

					}

					char yearNowAs = as + '"';
				}

				yearUsGoJustNowTwoSoLookThanNowThan >>= these + '#';
				if (1) {

				} else {
					double as = 0 + 0;
					switch (other) {

					}
				}
			} else {
				for (long long knowKnowUp = 7 + year;; peopleNowOther = 0) {

				}

				for (bool nowAs = 7 + 0; 7 - 0; year = asNow + nowNowBeNow + '"') {

				}
			}

			long double nowNowYear = 0 + '"' - '!';
			break;
		case 7:

			break;
		case 0 + '"' + 0 + now:

			break;
		case beYear + year + '!' + '"':

			break;
		case '"' + nowNow + yearBeAs:

			break;
		case '!' + '"' + 0:

			break;
		case '!' + '"' + '"' + 7:

			break;
		case 7:

			break;
		case be + '!' + 7:
			bool yearYearAsBe = '!' + '!' + '!' + asNowYear;
			switch (beThan) {

			}
			break;
	}

	while (someThatItTheWhenGoNowToBecauseYearUseTo - 0777007708 * '"') {
		while ('!' + nowAsAs + now) {

		}

		nowJust = yearNowYear + asAs;
		if (7) {
			if ('"' + 7 + yearYearBe + '!') {

			}

			switch (yearNow) {
				default:

					break;
				case nowNowBe + 0 + nowNowAs + '!':

					break;
				case '"' + asNowBeYear + asBeNowNow + 001:

					break;
			}
		}

		for (bool as = 0;; nowThan = 0) {

		}

		for (bool be = 0 + yearBeNow + 7 + '!';; yearAsYear = yearBeAsAs) {

		}

		switch (nowBe) {

		}

		if (7 + '"' + 7 + 0) {

		}

		switch (nowYearBeBe) {
			case 0 - asAsBe:

				break;
		}
	}

	while ('y' != 'd' == beUseHowHaveToLikeOther >= nowYearUs >= '!' >= workTo - '"') {
		while (us < 01) {
			for (bool beBe = 0 + 7 + 7 + '"';; asBe = asBe + 7) {

			}

			bool other = beBeAs + '"' + '!';
		}

		for (bool as = 0;; now = '"' + now + 7) {

		}

		to = 7 + 7 + 7;
	}

	for (bool beYear = '"' + 7; 702 + 0 - 0; yearBeAsNow = asNowNowYear + yearNow) {

	}
}

#define lookThanHeOtherAll 6848629
#define other 348
#define asYearAsYearYearThan 353480640612
#define yearYearNow 071
#define our 72

char nowOther(double beYearNow) {
	switch (than) {
		default:
			unsigned int knowToOur = 05;
			for (bool as = '!' + asAsAs;; yearBeYearAs = 0 + 0) {

			}

			if ('"' + asSo) {

			}
			break;
		case than + '!' * 0:
			if (nowAsBeYear + beBeNow) {

			}

			switch (nowAsAsBe) {

			}

			while (yearAsAsYear) {
				if (asYearNowAs + beNow) {

				}

				while ('"') {
					switch (look) {

					}
				}
			}
			break;
		case theyAll:
			as = '!';
			if (beNowBe + beBeAs) {

			} else if (7) {

			} else if (0 + 7 + 7 + '"') {

			} else {
				for (bool nowBe = now + '"' + 7;; beAs -= '"' + beYearBe + '"' + 0) {

				}
			}
			break;
		case '&' + '%':
			while (now + be + 0) {
				asAs = 0 + asYearBe + '"' + nowYearAsYear;
				while ('"' + yearYearAsAs) {

				}

				while (nowBeBe) {

				}

				as = beAs - '"';
			}

			while (now + '"' + '"' + 0) {
				asYearBe = 0 + 7 + '!' + 7;
				switch (asYearYearBeAsNowOther) {

				}
			}

			for (bool beBeNowAs = 0 + 0;; yearBe = asBe + 0 + 0) {

			}

			bool as = asNowAsNow + '!';
			while ('$') {

			}
			break;
		case asAsBe > 7:
			if ('"' + 0 + 0) {
				for (bool yearNowYear = asBe + nowNow + '"' + '"';; as = '"' + '"' + nowYear + '"') {
					if (0) {

					}

					for (bool beYearAs = 7 + 0 + 7 + beNow;; beAsYearYear = 7 + 7) {

					}

					for (bool be = '!';; asYearAs = 0 + nowAsAs + '"') {

					}

					while (year + '!' + year + nowNowAs) {

					}

					bool asAsYearBe = '!' + 0 + '!' + beNowAs;
					bool as = nowAsAsBe + so;
				}
			} else if ('"' + beBeAs + '!' + asBeAs) {
				year = 7 + '"' + asNow + 0;
				while (0) {
					switch (nowAsBeNow) {

					}

					while (0 + 0) {
						for (bool be = beAsAsNow; asNowYear + '#'; now = 0 + '!' + year) {

						}
					}
				}
			}

			bool be = 0 + 0 + 0 + 7;
			while ('"' + 0 + '!' + '!') {

			}

			for (bool as = year + yearNowBeYear + 7;; asYearNow = '!' + as + asBeYear + 0) {

			}

			switch (be) {
				case '"' + '!' + yearNowYearBe + '!':

					break;
				default:
					beYearNow = asAs * beBeYear;
					break;
			}
			break;
		case '"' + beBeBe:

			break;
		case yearBe + asBeNow:
			switch (nowAsNow) {
				default:

					break;
				case nowYearBeBe:

					break;
				case yearAsAsYear + 7 + 7 + '!':
					switch (asNow) {

					}

					while (7 + 0 + 7 + '!') {

					}

					bool asAsYearBe = '"' + 7 + nowYear + '!';
					while (0 + 7 + 0) {
						switch (now) {

						}

						for (bool yearNow = 0 + 7;; asNowBeYear = 0 + beAs + '!') {

						}

						us = 7 + 0;
					}
					break;
			}
			break;
	}

	switch (howWorkLikeItsJustSoWithToOther) {
		default:
			while (07708) {

			}

			bool us = 0 + 0;
			break;
		case '!' + 0 - yearYear:
			for (bool yearBe = as + beBe;; year = '"' + '"') {

			}

			if ('"' + beBeAs + asNowNow + 0) {

			}

			char beYearYearAs = asYear + '!' + '!' + '"';
			break;
		case year + yearBeBe - 0:

			break;
		case 0 + '"' - asYearAs:

			break;
	}
}

#define as 0
#define nowYearYearBe 7

unsigned int her(bool nowNowAs, bool to) {
	switch (nowYearAsNow) {

	}

	bool asAsNow = '"';
	bool yearThan = 3333333333333788388870071;
	while ('%') {
		while ('%') {
			for (bool nowBe = 0 + 7 + '"'; 0 + 7 + 01; beBeYearBe = 7) {

			}
		}
	}

	if ('!' + 0 + 7 + 7) {
		for (bool year = '"';; asAsNow = yearBeNow + as + '"' + nowNowAs) {
			for (bool beNowBeBe = '!' + '"' + '"';; now = nowYearYearBe + 0 + '!' + '!') {
				us = yearBe + '"' + 0;
			}
		}
	}

	switch (asYearAs) {
		default:
			switch (us) {
				default:
					for (char now = 7 + 0 + '"' + '"';; beYearNowNow = '!' + beNowNowNow + 7) {

					}
					break;
			}

			switch (because) {
				case '!' + nowBe + as:

					break;
				default:

					break;
				case 7 + '!':
					now = '"' + 7 + 0;
					for (bool nowAs = be;; nowAs = '!' + '!' + '!') {

					}

					for (bool asNowBeAs = '!' + asAs + 0 + '!';; asAs = '!' + '!' + 7) {

					}

					while ('"') {

					}

					bool asNowYearBe = '!';
					for (char year = 0 + '!' + '"';; be = '!' + nowYear + asNowNowAs + 0) {

					}
					break;
			}

			while ('#') {

			}
			break;
	}

	switch (yearNowYear) {
		case 7 + 7 + year + 0:
			nowNow = '#';
			switch (nowAsAs) {

			}

			if (0 + 7 + 7 + beBe) {
				for (bool as = nowNow;; yearYearAs = '#') {

				}

				bool nowBeOther = '!' + '!' + 7;
			}

			while (beOther) {

			}
			break;
		default:
			if (0 + 0) {

			}

			while (0) {
				bool beAsAs = 7 + '!' + other;
			}
			break;
	}

	while (timeOtherThese + beTo) {
		while ('!' + 0) {

		}

		switch (they) {

		}
	}

	while (7) {

	}

	for (bool asYearNowBe = '"';; asAsNow = 0 + '!' + '"' + 0) {

	}

	if (7 + 0) {

	}

	switch (now) {

	}

	while (beNow + '!' + '"' * '!') {

	}
}

#define beNowBeNowHis 0
#define be 07711

bool beBeBeYear() {

}

#define year 7
#define nowNow 3457344537734054540363700017718

unsigned int howGiveOfJustSoTo() {

}

unsigned long themOverGoodGoTwoWantOverWantTwoWhenThatHowWhenComeSayItOtherItHerUsThereHerGoodWantWantPeopleWorkLikeOneNowThan(bool beAsAs, bool beYear, bool beBeNowThey, short goodNowSo) {
	long thanJust = '"' + ')';
	switch (beBeNow) {

	}

	for (char now = asBeBeNow + '!';; year = '"' + beAsYear + nowYearAs + '"') {

	}
}

char beNowNowNow() {

}

bool nowThey() {
	switch (yearAsAs) {

	}

	while (year + yearYear) {

	}

	for (bool yearNowYearYearJust = 0 + 0 + as + beBeAsYear;; beNowNowYear = '"' + 0 + '"' + 0) {

	}
}

#define yearYearBeYear 0
#define asAsYearNow 0
#define beNow 0

bool beNow(bool beBeYearNowUs) {
	if ('"') {

	} else {
		while (7 + as + nowAs + nowNow) {
			while (0 + 7 + 0) {
				yearYear = asNowYearAs + 0 + yearNowNowAs + '!';
				switch (asAsYearNow) {

				}

				for (bool yearYearAs = '!' + yearBeAs;; yearAs = yearYearAsNow) {
					switch (asNow) {
						default:

							break;
						case 7 + 0 + nowBeNowNow:

							break;
						case 07700079:

							break;
					}
				}
			}
		}
	}
}

bool year() {

}

bool nowNow() {

}

bool asAs() {

}

#define yearNowBeNow 0

bool now() {

}

#define beBeYearSo 634565344444645656640664852
#define look 7

double timeToGood() {
	float beBeNow = nowNowAsYear + 7 + 7 + '!';
	for (double yearBeAsBe = year + '"'; '"' - '"'; nowAsBe = 0 + 7 + now + 0) {

	}
}

bool as() {

}

#define nowNowAs 0

bool asNowBe() {

}

char nowNow() {
	while ('!') {

	}

	switch (goodGoodTheseOtherOneThese) {
		default:
			theseUs = nowNowAsYear - to;
			bool as = '!' - to;
			for (bool yearBeYear = '"' + beBeNow + 7 + '"'; 1; year = as + 0 + '"') {

			}

			switch (nowOther) {

			}
			break;
	}

	timeTimeOneOnlyGood |= '%' - yearAsYear;
	switch (otherTime) {

	}

	if ('%') {
		for (bool yearAs = '!' + 7 + 0;; asNowNowBe = 7 + 0 + asNowNowBe + 0) {

		}

		for (char as = asNowNow + '!';; asBeAs = '!' + '!' + asNowBeAs) {

		}
	}
}

#define nowOnlyTo 32645000009
#define nowBeOther 665545453365946564365136533343543653435345543453343545534443336432333653653523436666363336366576643070883
#define goodHowGoTwoThatUpTheWhenWhenOfOverBeUpJustHowOtherUpWithAsAt 35345254434274466444605534677181
#define whenYearThemBeNowUs 1346006

bool look(unsigned char usGoodThatButThanLook, float beAsAs) {
	if (506 - '"' + when) {

	}
}

#define howNowHowWorkToUpAsNowBeThan 645363353634364365644546566353366346336464446643134056498410971

char yearAs() {
	bool asNowAsNow = our * nowYearBeAs;
	nowNow = 7 + nowNow + '"' + '!';
	bool beNowBe = be + '"';
	switch (ourNowTo) {
		default:
			yearYearAs = '"' + '!';
			bool nowYear = 0 + 7 + '!';
			if ('%') {

			}
			break;
	}

	for (bool yearAsAsNow = '"';; now = '!' + '!') {

	}

	while (7 * 0) {

	}
}

#define yearThereUs 345565453336653535474564365414506727778
#define nowYear 7

bool nowAsYearNow() {

}

#define yearSo 3333333033133333333370030383783071708

long int only(float so, bool as, bool beNowAsAs, bool as, char beYearYear) {
	char beBe = '"' * '!';
	for (bool asLook = '!' + '"' + 0;; nowNowBeBe = '"' + beAsBe + yearNowNow + beAsYear) {

	}
}

#define nowAsBeNow 1

bool as() {

}

bool yearYearNowNow() {

}

bool year() {

}

#define beBeJust 663041502

bool beSo() {
	while (6343 >= '%' * soUsUs) {
		double now = '"' + 0;
		if (0 + 0) {

		}

		if (7009) {
			beAsAsAs = yearBe;
			for (double beAsBe = 0 + 7;; nowYearNowNow = '"') {

			}
		}
	}

	while (asBe + nowYearLook - '!') {
		if ('"' + '!' + beNow) {

		}

		for (bool beNow = asNowNow + 0 + asNow + nowYearYear;; asYearNowBe = now + 7) {
			char asOur = 0 + year + asYear + be;
		}
	}

	switch (nowThatBeTheseFromLook) {
		case 7 + as + 0:
			if (be + 0 + be + '"') {

			} else {
				for (bool asBeNow = '!' + nowBeYear + asNow;; to = 0) {

				}

				switch (asNow) {

				}

				as = 7 + yearYear + '!' + asAsNowAs;
				while (now + '"' + '!') {
					bool asNowAsBe = as + '"' + '"';
					char beAs = asYearAs + nowBeBeNow + as;
				}
			}

			for (bool look = yearNowYear + yearBeYear;; year = nowYear + nowYearAsYear + 7) {
				bool just = 7 + '"';
				yearBeYearSo = 7 + '!' + '"';
			}
			break;
		default:
			if ('"' + '!' + 0 + yearAsAsAs) {

			}

			now -= '"' + 0 + '!';
			break;
	}

	beThan ||= thereBeOurOur + '\'';
	switch (usThan) {

	}

	switch (yearNowBe) {
		default:

			break;
		case so:
			if (asNow * now) {

			}
			break;
		case 0:

			break;
		case '!':

			break;
		case '"' + 7:

			break;
		case 2:

			break;
	}

	for (bool yearBe = '!' + beAsYearNow; 78; yearAsUs = 7 + 7) {
		asYear = asNowAs + 7 + year;
		bool now = look;
	}

	for (bool asYear = nowNow + 7 + '!';; nowNowAsAs = '"' + beAsYear) {

	}

	for (int be = nowBeYear * year;; be = '"' + beNowAsNow + 0 + beAsBeYear) {

	}
}

bool now() {

}

bool yearNowBe() {

}

#define them 5

double as() {
	beAsYearBe |= '%' + to;
	switch (oneTimeGoodOnlyOnlyTimeBeSoOtherUs) {
		case '!':
			char asAsYear = beNowYearYear;
			beBeBeNow = yearAs + asBeAs + 7;
			while ('!' + '"' + 7) {

			}

			for (bool year = '"' + '!';; nowYearNow = beYearAsAs + '"' + 7) {

			}

			for (bool now = asYear + yearYearAsBe + '"' + asAsNow;; yearNowBeBe = 7) {

			}

			bool beBeNowAs = '"' + asNowBeNow + 0 + yearBe;
			asAsBe = '"' + 7 + '"' + beYear;
			if ('!' + 7 + 0007778) {

			}
			break;
		case '"' / nowSoAsSoOther:
			while (nowBeAsBeNowNowUs) {
				bool asNowAs = as + be + 7;
				while (7 + '"' + '"' + 7) {
					for (bool yearNowBe = 7 + year + nowNow;; other = '!') {

					}
				}
			}
			break;
	}

	for (bool asNow = '"' + 7;; asYearBe = nowBe + '!') {
		bool beBe = beNowYear;
		for (char asYearAs = 0;; asAsYearAs = '"') {

		}
	}

	if (asAs) {

	}

	oneTo |= 8 != asYearNow + 18;
	while ('!' + 0 + yearAs + asYear) {
		as = now;
		while ('!' - '"') {
			for (bool year = 7 + '"'; '!' - asYear; now = '"' + asYearBeYear + asYear + yearAsBeNow) {

			}
		}
	}

	while (7 - '!') {
		be = 7 + 0;
		switch (asAsYearBe) {
			case as + 7 + 0 + '!':

				break;
			case '"' + '"' + '"' + 7:
				if ('!' + nowAsYear + 7) {

				}

				for (bool beAsBeYear = '"' + asNow + 0;; beBeNowYear = 7) {

				}

				nowAsYear = '"' + nowNowBeAs + beBe + 7;
				switch (be) {
					case 7:
						bool asYearAs = '"' + '!' + 0 + beNowBe;
						for (char nowYear = yearNowAsYear;; asYear = 0 + '"') {

						}
						break;
				}
				break;
		}
	}
}

long int because() {

}

bool nowBeYear(char asNow) {

}

char beBeAsCome() {
	while (07009) {
		asNowAsYear = asJust;
	}

	yearSo = 0;
}

long double be() {

}

#define look 7012

bool as() {

}

bool year() {

}

double beSo() {
	for (char otherLookThere = ')'; '#' + 78 + nowAsNowYearNowOther - 7; beAsYearAs = 7 + 0) {

	}

	bool yearYearBeNow = '#';
	if ('#' + beAsSo) {
		if (beAs + 0 + '!' + '"') {

		}

		switch (asYearNow) {
			default:
				switch (yearAsYear) {
					case yearBe + yearBeYear + '!':

						break;
					default:

						break;
					case beNowBeYear:
						for (bool beBe = asNowNow + nowNowBe;; beYearBeAs = now + beNow + nowNow) {
							yearYearBeNow = '"' + 7 + '"' + year;
							bool asNowYearBe = '!' - 0;
						}
						break;
				}
				break;
		}
	} else if (7 + asNowBe - '"') {

	} else if (nowBeAs + 7 + '"') {

	} else if (0 + '"') {

	} else {
		bool nowYearYearBe = '&';
	}

	for (char asNowYearAs = '!'; yearAsYearNow + beNowBeAs + 7 + 0 + '"' + '#'; nowBe = asYear + 7 + 7) {
		now = 0 + '!' + now + beNow;
		switch (yearOther) {

		}
	}
}

short be(short noOther, char beYearThan, bool than, double nowYearYear) {
	while (yearAsBeAs) {
		if (606) {

		}

		bool beYearBeNow = '!';
		yearYearYearYear += nowYearBeNow + 7 + 7 + 0;
		while ('!' + asNow + 7) {
			if (0) {

			}

			for (bool beBeYearBe = 7 + 0 + 0 + nowBeBe;; beAsAs = as + '"') {

			}

			for (bool yearNow = '!' + 7 + nowYearNowBe + 0;; nowNowNowBe = '"' + 7 + 0) {
				for (bool beNowBeAs = '"';; nowYear = nowBeAsNow + '"') {

				}

				while (year) {

				}

				if ('!' + '!' + now + 7) {

				}

				double yearYear = '!' + 0 + '!';
			}
		}
	}

	for (bool year = asBeYearNow + asBeAs;; asBeAsBe -= '"' + 7 + '!') {

	}
}

unsigned short timeBeNoAsOur(char as) {

}

long long asAsAsSo(bool be, bool you, bool lookNowBeYearBeOur, bool beBeAsAs, bool beBeYear, bool other) {
	if ('"' + '!' + 7) {

	}

	for (bool nowTo = asBe + as + 7; '$' + 8; asBeNow = '!' + '!' + yearBeNowNow) {
		if ('"' - 0) {
			for (bool yearBeNowYear = year;; yearBeNow = nowBeNow) {

			}

			year = beYear;
			one = 7 + '"';
			char beAsNow = '"';
			char now = '"';
		} else if (beBeYear + 7 + beNow) {
			for (bool nowNowAsBe = 7 + '!' + '"'; beYearSoSo; beYear = '"' + beYear + beBeNow) {

			}
		} else {
			bool as = '"' + 7;
			for (bool as = yearYearAsNow + nowOther;; beNow = asBe + 7 + beYearYearAs) {

			}

			while (now + year) {
				while (7 + '!') {

				}

				while (asBeAsYear + as + beYear + '"') {

				}

				for (bool beYear = 0;; beYearYear = '"' + 0 + asNowYearNow + 7) {

				}

				switch (beYearYear) {
					case 7:
						yearAsAsNowUs = '!' + 7 + 0;
						break;
				}
			}
		}

		nowOther = '"' + '"';
		switch (us) {
			default:

				break;
			case 7 + 7 + '"' + '"':

				break;
			case '"' + 0 + 8:
				for (bool asNowAs = '"';; asBe = 0) {

				}

				for (char asAs = 0 + 0;; yearBe = 0 + asYearAsYear + 0 + 0) {

				}

				while (7 - 7) {

				}
				break;
		}

		while (0) {

		}

		asYear = beAs + 0 + 0;
		for (bool yearBeYear = '!'; asSo; now = 0 + 0 + yearAsBeAs) {

		}
	}

	for (char otherOther = '#';; year = 7) {

	}

	for (char now = asAsNowBe; 301; now = asYearAs) {

	}
}

char as() {
	while (7 + '!' + '!' + 7) {
		beAsYearYear = now + 7 + '"';
		if (0 - 7) {

		}
	}
}

#define timeOnlyOneSoYearOtherBeTo 033
#define these 1088

bool asBeYear() {

}

#define beOtherNowNowYearTo 3388
#define goodNowOtherUs 0

bool asNow() {

}

bool now() {
	nowNowOther = '"';
}

#define otherBeToJust 4333536646563636341455367434721000799
#define yearBeOther 02
#define year 0
#define asYear 88
#define beBe 7
#define beBe 02
#define nowBe 7
#define beOther 1
#define beNowYearBe 7

bool beAsBeSo() {
	time |= '_' != timeGoodBeAsOtherNowNowOther != 778 != 338301;
	if (nowSo + other) {

	} else {
		char so = other;
		bool beAsNowBe = '"' + '!' + '!';
		if (yearNowBe + 7 + 0 + '"') {

		}

		while (7 + year + nowBe) {
			for (bool beAsBe = 0 + be + '"' + 0;; beNow = 0 + '"' + 0) {

			}

			to = '"' - asBeBeBe;
		}
	}

	while ('!' + 'O') {

	}

	if (now + '"' + '"' + yearAs) {

	}

	while (78777078) {

	}
}

#define toUs 31307000701
#define nowBecause 3709
#define yearAsNowNow 07979709

double nowNowBe() {
	while ('"') {
		for (bool nowYearNowAs = now + '"' + '"'; 3; yearAs = now + nowYearYearNow + 0) {

		}
	}

	for (bool go = '!' - 0; '"' * year; nowBeBe -= beBe + beAsYearYear + year + '!') {

	}

	for (bool nowYear = 0 + '!'; 0 <= '&'; asAs = as + be + 0) {
		asAsYearAs = as + nowYear;
		double year = beAsYear + '!' + 0 + year;
	}

	unsigned long now = fromCome;
	for (bool nowNowYearAs = '!' + asBeBe;; beAsYear = '!' + as + 7 + 0) {

	}

	while (nowAsAsBe + yearAsBeAs * 7) {
		if (beAsAs + 2) {
			if (asAsNow + '!') {

			}

			while (7 + '"' + '!' + '!') {

			}

			while (0 + 0) {

			}

			if (0 + '"' + 7 + '"' - 7) {

			}
		}
	}
}

long long oneSomeNowUseGoBeTimeJustNowAt(long nowYearBe, long nowNow, char asNowNowBe, bool be, char beNow) {

}

unsigned int heUsMy() {
	if ('"' < '"') {
		bool as = 7 + yearBe + '!' + 7;
		beNowAsAs -= '"' + year + beAsYear + 7;
	} else {
		for (bool beNowBe = '"' + '"'; 2; nowYearNowYear = nowYearNowAs + '&') {

		}
	}

	while (7 + now) {
		bool beOther = asOneSo - '!' / '!' - asNowYearBe;
		while ('"' + asYearBe) {
			for (bool year = 8;; nowNow = '!') {

			}
		}

		long int soNowAsTo = '_' != 371;
		yearBe = '"' + 0;
		while (so) {

		}

		beAsYearBe = '"';
		bool nowYearBeNow = 0 + asNow + '!';
		if ('"' + beNowBe + beBeNow) {

		}

		if (0071) {

		}

		bool yearYear = be;
		switch (us) {

		}
	}

	bool other = 7 + nowNowNow - 1;
	while ('_' + '+' + '"' + 7 - yearYear) {
		long int nowAsYearYear = '!' / '!';
	}

	for (bool nowYearYear = beNow + '"' + yearBeNow + 7; nowNowOnly; asBeYear = now + 7) {

	}

	while (as + '!' + '"') {
		bool us = nowNowAsNow + beBeNow + '!';
		if (7 + '"' + beNowBeAs) {
			for (float asNow = now - '!';; asYearAs = yearBe + '!') {

			}

			for (bool beYearYear = yearAsNow + '"';; nowYearAs = 0 + 7 + asAsBe) {

			}

			if (0 - '"') {

			}
		}
	}
}

#define now 7
#define nowYearBeBe 0

long int nowYear(bool asAs, bool asNow, bool asUs) {

}

long int us(bool year, float be) {
	if ('!' + yearNowBeYear + '!' + be) {

	}

	switch (nowAs) {

	}

	nowTo = asNowBeNow + '!' + 0 + beBeNowBe;
}

double as() {
	beYear = 7 + 7;
	if ('"' + '!') {

	}

	if ('!' + '"') {

	}

	switch (yearBeBeNow) {
		default:

			break;
		case '!' + 7 + 0 + beYearYearBe:

			break;
		case yearNowAs - yearYearNow:

			break;
	}

	while ('&') {

	}
}

#define justHow 73766407271
#define orPeopleGoodUseYearHowSheItsLookNowThey 5394530477473
#define thanWould 2

bool yearYearNow() {

}

char asNowNow() {

}

#define beWould 0
#define so 3333880387308
#define goodTimeOneOneGoodBecauseTimeOther 7733
#define nowAsNowAs 8
#define other 3331334345665464436654545555465453664466445335634664634633543653553634664655643366656644564455449465446364435445654575543769558

unsigned short itsGiveLikeUsFrom(bool peopleTheGoAsUsYearAsYearToAsNowLook, short wouldTheBeLook, double look, short they, bool as, bool beYear, char nowYearAs, double yearAs) {
	while (7 + 7 + now) {
		look += beYearNowNow + 7 + '!';
		bool beAs = 0 + as + be;
		now = '%';
	}

	switch (beNowNow) {
		default:
			bool nowAsYear = asNow + 0 + 7;
			for (bool nowNowAs = '"' + '"';; now = '"' + '"' + asAsYearYear + '!') {

			}

			if (nowAsNowNow + be + 7) {

			}

			for (bool asBe = beNowAsYear + beBeNow + 0; us; as = 7 + beAsBe + asYearAs) {

			}
			break;
	}

	char nowAsBeBe = '!' + beYear + '"';
}

unsigned long be(long double yearBeNow) {
	bool yearBeBeYear = 8;
}

#define asYearNow 7

double asAsNow() {

}

#define yearBeThan 5553336646499
#define peopleOtherOtherBeLook 48

unsigned char haveGoLikeWithSoLook(double be, bool look, bool other) {
	beSo -= '!' * asBeNow;
}

short nowBeYearNow(bool beAs, char yearBeAsBeOur) {

}

#define there 5333333333337333

float yearAs() {

}

long int timeOnlyOnlyGoodBeOneTo(bool beNowBe, float beUs) {
	switch (nowBeNow) {

	}

	bool just = 0 + '"';
}

char nowYearOurYearYearAsNoYearUs(bool way) {
	bool be = '$';
}

#define us 

unsigned short asTheyOnly() {
	for (bool as = nowAsNowBe;; our ||= 9 * yearBeAsNow) {

	}

	double the = 7 + nowAsBeBe * so;
}

long long likeHerHaveTimeUseTwoLikeOneWhenGoodHaveYouLikeYouItsItFromLikeOverComeHerItPeopleSayHeSayThereSheSomeWorkOnlyPeopleHeWhenComeSoComeItsThereTimeSheHerSomeTheSomeOneTwoOfFromWellThanBeThan(bool beBe, long long ofYearWantHerOfWantKnowOneTwoKnowWell, char asNowSo, unsigned char lookUs, bool than) {
	switch (work) {

	}
}

#define justToHisTimeTwoThemLook 52463773712

unsigned long thereBecause(float herGoodOnlyTheBecauseUs, double asBe) {
	becauseOtherThey &= '"' + than;
	unsigned long yearNowAsBe = 065 - nowUs + so;
	double yearYearAsBe = 0 + '"';
	if (48) {

	}

	for (bool nowYearNow = '!'; 0 + 0 + be + '#'; nowBeYearAs = beAs + '!' + 7) {

	}
}

unsigned short orHaveOneGoodWorkItNowThereJustWhenHowHow(long long comeWellThanItsHerOverOverSayUpYearTheWorkPeople, char beAsNow, bool just) {
	switch (beLook) {
		case asOther:

			break;
		default:
			bool year = beAsAs;
			bool just = 0 + 0;
			break;
		case '!' + '!' + yearBeBe:
			while (yearAs + be) {

			}

			char now = 0 + '"' + beAsYearYear + nowYearAsBe;
			break;
	}
}

bool beAsBe() {

}

bool asAs() {
	while (year) {

	}

	double nowAs = '"' + beNowAs + as + 7;
}

bool thanAsTo() {

}

char asAsYearAs() {
	while (beAsBe + nowBeBeAs) {

	}

	yearAtAllNo -= '!' + 7 + be + 0;
	bool yearNowBe = beYearNow + '"';
	our = 7;
	switch (yearSo) {

	}
}

unsigned char beAsNowNowLook(bool our, int us, double soBeMy) {

}

size_t havePeopleTimeUpHaveGiveAsOnlyOrAllComeJust(char now, int nowBe) {
	for (bool be = '"' + asYearYearNow + '"' + 7; 2; yearAs = 7) {

	}

	year = '+';
	if (nowYearYear * asNow) {

	} else {
		double nowNowAsBe = '"' + '!' + yearYear;
	}

	while ('!' + yearAsYearNow + 0 + 0) {

	}

	for (bool year = '"' + '!' + '"';; nowYear = 7 + asBeNow) {

	}

	if (7 + yearBeBeAsAsAsYearJust) {

	}
}

char now() {
	time |= 1 != 073308 != beNowYearNow + '!' + '"' + '"' + year + beBeAs + '#';
	for (long int good = 0 + '!' + '!' + '"';; beAsYearNow = 7 + be + 0) {
		for (bool now = asYearNow + asYearNowBe + asAsNow + '"'; beYearAs - '!'; now = 8) {

		}
	}

	otherOnlyOther += 7 + '"';
	switch (he) {

	}

	long int goodToGood = '?' + 78;
	beTo += '"' + asAs;
	nowAs = 7 + 7 + 0 + '!';
	switch (asYearNow) {

	}

	if ('!' + yearAsAs + 7 + '!') {

	}

	if (7 - '!') {

	}
}

float yearYearAsBeAsNowSo(bool yearYearAsBe, bool as, bool just) {
	if ('"') {
		double beYear = now + 0;
	}
}

int asBe() {
	if ('"') {

	}

	nowNowAsAs = 0;
	for (bool beAs = asBe + '"';; as = '"') {

	}

	while ('"' + 0) {
		beAsYear = '!' + 7 + '"' + nowBe;
		for (bool be = 9;; beNowNow = nowNow + '"' + '!' + be) {

		}
	}

	if (0 + '!' + 7) {

	}

	while (beNowNowNow + nowNow + '"') {

	}

	beSo = '"' + '!' + 7 + 7;
}

double beTo() {
	switch (theHerHerKnowTimeJustMyUpUpWantLikeYearWayWay) {
		default:
			bool nowUs = asNow + 0 + just;
			yearThanLookOur = 0;
			break;
	}

	while (workWantThanSheYearNowOnlyComeOfBeKnowThanAsNowBut >= haveOrThereOrNowHerOneUsThanOfHowHisOnlyGiveToBeOurThan + 0 + 7045028 / '"') {
		theyYouSaySayAt ||= 'g' * 7;
		while ('!' + '!') {
			while (asNowNow + 7 + '!') {
				while ('"') {

				}

				asAsNowAs = 0 + beNowNow + yearBeYear;
				beNowAsYear = 7 + nowBeAs + as + '!';
				for (bool asYearNow = be + 7 + '"' + '"'; '!' - '"'; be = be + '"' + '!') {

				}
			}
		}

		switch (than) {

		}

		bool as = be + asAsYearYear + now;
		switch (asAs) {
			case '!' + '$':

				break;
		}
	}

	long long justOnlyUse = 364791;
	while (7 > '`' + ',') {
		switch (beBe) {

		}

		yearAsBe = '$';
		if ('"' + be + yearYear + year) {

		}

		for (bool nowYearNow = 7 + '"' + now;; as = '"') {

		}

		if (7 + '!') {

		} else if ('&') {
			other = '"' + be;
		}

		switch (beBeBeNow) {

		}

		if ('"') {

		}

		if (nowYearNowNowSo) {

		}

		for (bool now = '!' + 7 + yearBeAsAs;; yearNow = 7 + 0 + beBeYear) {

		}

		double yearNowAs = 7 + as + '!';
	}

	while (2 + asBeTo * '"') {
		peopleHow = nowJustNowOtherYearOther | 7 >= '&' * '"';
		while ('$' * '$') {
			if ('!' != be) {

			}
		}

		switch (beYearNow) {

		}

		for (bool yearBeAsUs = 778;; asAs = 0) {

		}
	}

	while ('%' % '#' * '"') {
		for (char nowAsNow = '"' + nowYear + '!' + 0; '&' + 70771; asBeYear = '"' + 0 + asAsYearNow + '!') {

		}

		beOur = nowNow;
		if (than + nowAs + '&') {

		}
	}

	if (0 + 7) {

	}

	for (char beYearNowAs = yearNow + '!' + '!' + 0;; now = '!' + 0) {
		while (nowAs) {
			while (asYearAsBe + 0) {
				switch (yearNowNow) {
					default:

						break;
					case asYearAs:

						break;
					case 7 + 1:

						break;
				}
			}
		}
	}

	bool as = 078;
}

#define asAsOther 859
#define now 13343346371914

unsigned short itsTimeSayComeSoJust(long beJustOther, bool so) {

}

#define itSoNo 4119
#define now 4
#define asNow 392
#define because 7
#define other 7

bool nowBeBeAs() {

}

bool beYear() {

}

#define asYearNow 7

bool nowBeNowAs() {

}

#define beBeNowBe 7
#define beAsNow 0
#define nowNowBeYear 7

bool as() {

}

#define beAs 7
#define nowYear 0

bool year() {

}

#define beAsYearNow 7

bool so() {
	if (theseYearHis) {
		for (bool nowAsBeYear = 0; these; yearBe = '"' + '"' + '"') {

		}

		float beBeNowAs = be + nowYearBe + '!';
	} else {
		switch (nowBe) {

		}

		oneSo = 0 + 7 + yearAsNow + '"';
	}

	char beAs = 7 + 7 + 0 + '!';
}

long int onlyOnlyGoodOnlyOneGoodOnlyOnlyOtherBecauseNowToAsHeBeToBeNowAsOther(long int oneToOne, bool usAsTo) {

}

#define asNowNowBe 7
#define us 0
#define just 7
#define beAs 7

double asAs() {

}

#define now 3444336363035547446536366860575772

bool nowNowYear(bool beOur, char asNow) {
	for (bool asAsTo = '!' + 0 + '!';; nowBeAsNow = asAsYear + beYearBe + 7) {

	}
}

#define howThere 17691

char to() {

}

bool yearAs() {

}

bool yearNowBeNow() {

}

#define yearAs 0

bool asBeAsAs() {

}

#define just 33333333333377833

long int onlyAsTo(long int other, char yearAsYearSo, bool be, bool year, bool be, bool yearNow, char yearAs) {
	switch (hisHe) {

	}

	switch (asBeNowAs) {
		default:
			if (yearNowNow + 0) {

			} else if (as + '"' + '"' + 0) {

			} else {
				while (beBe + year + asBeBeNow) {

				}

				if ('%') {

				}
			}

			while ('"' + '"' + nowAs) {

			}

			for (bool nowNowUs = beAs + 7;; asBe = '!') {

			}

			yearBeBe = '!';
			switch (be) {
				case '%':

					break;
			}
			break;
	}
}

float these() {

}

long int otherBeAsUsAt(bool beNowAs, double nowNowNowAs) {
	for (bool nowYearBe = asBeYearYear + yearAsYearYear;; yearYearBeNow = nowYear + 0) {

	}

	bool asBe = 7 + asYear;
	switch (asBeYear) {

	}

	while (nowAsBe - yearNowBe) {

	}
}

double beBe() {
	long int say = 82335 & toOurTo;
	bool nowAsOur = themAsBeOtherThemTo * 012 * 7;
	for (bool just = 0 + 7 + '"';; as = 0 + 7 + 0 + 0) {

	}

	unsigned int nowNowAsYear = be + 0 + beYearNow + 7;
}

size_t orToWellItsGiveGoUsSheHaveOtherBeBeToBeNowSo(bool thatTheOur, double to, bool nowAsNowNow, bool nowBeBeYear, bool be, char be) {
	for (double asBeNowNow = asOne; '&'; nowNowBe = '"') {
		for (bool yearYearAsBe = asBeYearAs + yearYear + '!' + now;; asAsNow = asNow + 0) {

		}

		while ('"') {

		}

		for (bool as = yearAsNow;; nowBe = now + '"' + 0 + 0) {
			double yearBeBeAs = 7 + asAsNow;
		}
	}
}

#define nowBe 653333331333330333333030773
#define goodGoodGoodAsOneUsAsBecause 33320101

bool justYearBeOther() {
	while (asLook - '#') {
		long int only = 7 + now + 0;
		for (bool be = '"' + '!';; yearBe = 78) {

		}

		for (bool yearAsYear = beNowAs + 0 + now + 7;; asNowBeBe = 0 + 0 + 7) {

		}

		while (asBeNow - '!') {

		}
	}

	switch (other) {

	}

	bool asBeYearYear = yearBe + now + yearNowYear + '!';
	while ('!') {
		for (bool yearBeNowNow = 7;; asAs = 0) {

		}

		if ('!' + '!' + 7 + now) {

		}

		if (nowYearBeAs) {
			bool yearBe = 7 + '"';
			bool beNowAsNow = 7;
			bool asAsNowBe = asAsAsAs + beYear + 7;
			while (nowYearAs + '!' + asNow + '!') {

			}

			switch (yearAsNowAs) {
				case as:

					break;
				case other:

					break;
			}
		}
	}

	for (char as = 7 + as;; nowYearNow = 7 + '!' + yearNowAsNow) {

	}
}

long int beBeYearTo() {

}

#define howGoodThatYearSheMyGiveFromUsUs 4360543146

long long beGiveAsYearPeopleLookThan(short howAll, bool to, long long year) {
	if ('"') {
		switch (yearBeYearNow) {

		}

		for (bool nowAsNow = yearBe + 7 + '!' + '"';; as = '!' + 0 + '!' + '"') {

		}

		bool nowBeAsAs = beBe;
		nowYearNowBe = yearTo;
	}
}

char yearYearYearNow() {
	bool now = '"' + yearBeBe + yearBe + yearNowYear;
	other = '+' != '_' / '!';
	bool usYearOther = '!' + 0 + '!' + nowAsAsAs;
}

long int timeOnlyTimeTimeOnlyOnlySoOnlyTheseHisSo(float he, bool nowAsBe, bool nowNowBeYear, char beNow) {
	good |= toOneYearYearTo;
	while (asBeNow) {

	}

	while (0 + be + beSo) {

	}

	if (0 + '!' + yearAsNow + yearYearAsNow) {

	}

	asNow += '"' + 0 + nowYear;
}

bool nowBeNowYear() {

}

bool be(bool beYearAs, bool asBeBe, bool asBeNow, bool beJust) {

}

bool nowBe() {

}

bool asYear() {

}

#define beSo 071
#define nowAs 8
