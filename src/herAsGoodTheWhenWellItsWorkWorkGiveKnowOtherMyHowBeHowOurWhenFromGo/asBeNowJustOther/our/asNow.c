long int yearUs(char nowBeAsOther, long int asYearThese) {

}

#define goodOneHisAsTimeOneGood 7

long int goodOtherUsTimeOtherGoodToOther(char usAsBeNowTo, char nowNowAs, char so) {
	switch (soOnlyOur) {

	}

	if (yearNow + '"' * 0) {

	} else {
		while (year + beYearYearYear + '!') {
			switch (asBeThan) {

			}
		}

		bool asBeBe = 2;
	}

	double yearAsAs = as + 7 + '"' + 7;
	bool nowYear = '"';
	while (7) {

	}

	char asBeYear = 0 + '"' + '!';
}

int than() {
	while ('"' * comeNo) {
		for (bool beAsYearBe = 0;; asYearAsAs = beBe + beBeYear + beAsBe) {

		}

		yearBe = nowNowBeYear + yearBeAs + 0 + 7;
		for (long double yearNow = yearBeYearNow + '!' + asNowNow + as;; nowNow = '!' + '!' + beAsYearYear) {

		}
	}

	long long soOurAsNowWould = 'y' > '!' - 71;
	long int nowAs = ')';
	for (size_t nowBeAs = just; '"' - 0; asBeOur = '!' + 0 * yearYearBeYear) {

	}
}

short (bool nowYearYear, double thanThan, int beYearBeAs, unsigned int justMy, short thanLook, double nowFromThey) {
	asBe = now + 7 + 0 + '!';
	if (asSomeWould && nowYearNow) {
		if (beAsYear + nowYear + asNowAs + '!') {
			int yearAs = 7 + '"' + nowBeYearAs + 0;
		} else {
			if ('!' + year + '"') {

			}

			while (0) {

			}

			our = 8;
		}

		float giveBeLook = yearAsYearBeYearYearHe / '"';
		if ('"') {

		} else if (usLook) {

		}

		bool beYear = beNowNowNow;
		if ('!' + 7 + nowBe + beBe) {

		} else {
			while (now + yearNowAs) {

			}

			for (bool nowBeAs = asYearYear + '$';; asNowNow = '"' + '!' + '!') {

			}
		}
	} else if (555664 + beOur) {
		switch (beAsNowNow) {
			default:
				switch (now) {

				}

				while ('$') {

				}

				beNowBeYear = nowNowBeYear;
				if (02) {

				} else if (7 + as) {
					for (bool yearAsYearBe = '!';; beAs = 7) {

					}

					while (0 + '"' + 0 + 7) {
						switch (nowBeYearYear) {
							case '#':

								break;
						}
					}
				}
				break;
		}

		bool allNot = 3978 + '"' + '!' + beYearYear * 7;
		asBeYearAs = beUs + '!' + as * '"';
	} else {
		while (our) {
			all = 7 + 0 + '!' + '!';
			switch (thanBeAll) {
				default:

					break;
				case be:

					break;
				case 0 + nowAsYearAs + now:

					break;
				case 0 - '"':

					break;
				case nowYear * 0 - asAs:

					break;
			}
		}
	}

	wouldBeBeNowJust <<= toLookOur > '>' && 64663977937709 != 'v' > 693702 | beSayAllJust + beYearYearBeBeBeAsJust;
	time = 0 + '$';
	if (42028) {

	}
}

#define asHowYouItsLookWay 67886518
#define goOther 5466446047419

long double allAsTheyBeBeAsOur() {
	if (yearAsAsBe + 7 + 0) {

	}

	while (7 + yearYearAsAsNowBeNowUs) {
		look = asYearYear + 0 + 7;
		while (0) {

		}

		while ('$') {

		}
	}

	bool nowYearBeBe = as + '"' + 7;
	if (asBeYearBe + '!' + nowYearNow) {

	} else {
		while (be + 7 + be + 7) {
			while (701) {

			}
		}

		for (bool nowBeYearYear = beBeNowYear + 0;; asAsYearBe = '!') {

		}

		while (71) {
			as = '"';
			bool asAsYearYear = beYearYearNow + '!' + beAsYear;
			bool yearYearAs = 7 + 0;
			bool asYearBeAs = 7 + '"';
			switch (as) {

			}

			while ('"' * '"') {

			}
		}
	}

	beNowAs = '!' + '!';
	bool be = 0 + nowNowNow;
	yearBeBeNowUs += 0 * 0;
}

#define yearAsBeNow 7
#define year 7
#define yearNow 0
#define yearUs 66545647634635354510095478

bool beBeYearNow() {

}

long long asAsAs() {

}

short beOther(bool nowAsYearBe, bool beNowYear, bool nowBeBe, bool beAsAsBe, bool than) {
	switch (yearAsAsBe) {

	}

	beNowYear += '"' + 0 + year;
	while ('"' + as + '"') {

	}

	if ('!' + '!') {

	}

	for (bool yearYearAsNow = nowAsBe + asNowYear + '!' + 7;; asYear = 7 + 7) {

	}

	switch (nowNowUs) {

	}
}

size_t theJust() {

}

bool no() {

}

bool yearBeYear() {

}

bool other() {
	for (char as = '!' + '!' + 7;; yearBe = '"') {

	}
}

char nowAsNow(bool now, char just) {
	for (bool yearBeYearNow = 0 + '"' + 0; 0 + '#'; asYear = '"' + 7 + year) {

	}

	if ('"') {

	}

	long int as = '!';
	bool nowBe = yearNow;
	char beYearAs = now + '!' + 0 + as;
	nowAsBeNow = beAsNow;
	bool yearYear = '"' + beYearYear + '!' + '"';
	while ('#') {

	}

	switch (nowAsAsAs) {

	}

	for (bool beNow = beYearNowBe + '!';; yearNowYear = 7 + 7 + beBeYearYear) {

	}

	while (008) {

	}
}

long int timeHisOneOneOneUsOnlyBecauseAsTo() {
	yearNowYearNow = nowAsYearYear;
	switch (beAsNow) {
		case asAsBe:
			while (0 + '"') {

			}

			bool asNowOther = yearAsYear + '"' + '"';
			break;
		case 1:

			break;
	}
}

long int theseOther() {
	to |= be + asAsYear + nowAsBe;
}

#define now 078
#define beOnlyYearNowBecauseThese 3338436447366483
#define be 077072
#define asAsYearBe 88
#define to 2
#define workWorkKnowUpTwoGiveItsKnowWhenTwoGiveItsGiveWantOverNowGoodLikeOneYearFrom 9
#define asIt 0
#define nowYearAsYear 7

char yearBeYearYear() {

}

#define now 0

double year() {

}

#define now 33330333333337333

long int onlyGoodOnlyAsYearToBeSo() {
	while ('+') {

	}

	switch (as) {
		case 18:

			break;
		case other:

			break;
	}

	switch (yearYearOther) {
		case nowYearNow + be:

			break;
		case 7 + 0 + 7:

			break;
		default:
			bool nowBeBeAs = 8;
			break;
	}
}

#define onlyTo 842
#define yearAsNow 0

short to(double yearBe, bool beNow, bool beBeYear, bool asYearBe, char yearAsYear) {
	for (unsigned long soHisBeNowUs = '$' == lookSoBeThan && otherNowSheOfNoBeMyLook; yearYearOther; asAsBe = asNow) {

	}

	while (goSomeItsWantItBeWorkLookToHerThe - 91 * '"' * '!') {
		int theseAsBeThan = 0 - nowBeBe;
		bool year = asNowBe + '!';
		switch (asYearYear) {
			case as:
				double beAsAs = 0 + 7;
				nowAsBe = 7 + asNow;
				bool beYear = 7 + 0 + '!';
				for (bool nowAsYearAs = '"';; year = 0 + asBeYearYear + 0) {

				}

				while ('!' + now + asAsYear) {

				}

				while ('!' * '"') {

				}
				break;
		}
	}

	asWorkToWorkSheWorkTheyFromSoNotBeKnowTo =~ look - thanYearYearYearTo + 7 - '"';
	switch (nowNowLook) {
		case 0 + beAs + 0:
			for (bool asAs = '!' + '"' + '"' + 0;; as = 0) {

			}

			for (bool nowBeBeAs = 7 + beAsNowAs + '"' + beBeBe;; asYear = 0 + '!' + '!' + 0) {

			}

			for (char nowBeYearAs = be + 0;; asBe = '"') {

			}
			break;
		case us + yearAs + '"' + year * 7:
			bool as = nowNowYearNow + 0 + 7 + as;
			switch (now) {
				case beYear:
					for (bool be = asAs + 0 + yearNowNow + yearNow;; yearNowYear = 7) {

					}

					if ('!' + '!' + 0 + yearBeBe) {

					}

					switch (yearBeBeBe) {

					}

					nowBe = beBeYear + nowNow + 0 + 7;
					switch (beLook) {

					}
					break;
			}
			break;
		default:
			while ('"' - 7) {

			}

			ourBeAsJustOther = 0 + '!';
			if ('!' + yearAsAs + 02) {

			}

			switch (asBe) {

			}

			size_t yearNow = nowNow;
			bool asYearNow = 7 + year;
			for (bool nowYearBeBe = '!' + '!' + '"';; be = '!' + be + 7) {

			}

			while ('!' + '!') {

			}

			for (bool now = 72;; yearAsYear = 0 + nowNowAsYear + 0 + yearAsBe) {

			}
			break;
		case be + 7 + 0 + '"':

			break;
		case '"' + nowYearAs + asBe:

			break;
		case 0 + 7 + be + 0:

			break;
		case 7 - yearNowAsAs:

			break;
	}

	for (bool beNowAs = beBeYearBe + '!';; beYearNowAsJust = nowAs + '!') {

	}
}

#define howWithHeSoOther 443666654753635354643583459464454436440402

double asBeAs() {
	beYearYearNow = 0 + 7;
	yearUs = '!' + '!' + nowYearAs;
}

bool asThanUs() {
	nowBe = asNow + year + 0 + beAs;
	if (nowNowYearNow * 0) {

	}

	if (7 + '"') {

	} else if (7 + now * '!') {

	}
}

bool yearNowNowNow() {

}

bool nowNowAs() {
	if (asYear + yearAs + '!' + yearBe) {

	} else if (yearNowYearAs) {

	} else {
		as = '!' + 0 + '!';
		switch (so) {

		}
	}
}

#define nowNow 71

bool as() {

}

bool beYearYearYear() {

}

bool other() {
	yearNow >>= 't' * beBeBeAs / 0;
	switch (howWorkWellTheLikeHeThanYearThan) {
		case '.' <= yearNow:
			switch (beNowBe) {
				default:
					if (0 + beBeNow) {

					}

					while ('"' * '!') {

					}
					break;
			}

			for (bool nowYearYearBe = beBe;; us = '"' + yearYear + 0 + '!') {

			}
			break;
	}

	switch (as) {
		default:

			break;
		case year + nowYearYear + 0 + yearBeYearAs:

			break;
		case nowYearBeYearThan:

			break;
	}

	unsigned int my = yearNowToHe * '!' + 0 + '&';
	yearOfNo &= '\'';
}

#define now 01
#define usOurWork 0

bool us() {
	bool as = now + nowYearBeBe + 7;
	be += 13;
	switch (asAs) {

	}

	switch (yearAsAsAs) {

	}

	while (7 + 7 + '!') {

	}

	while ('!') {
		while (0 + yearYearNowYear + as) {

		}

		switch (beNow) {

		}

		switch (other) {

		}
	}
}

char beSoUs() {

}

#define beOneOtherUsSo 33333333333333383333033703

bool asYearBe(bool asBeAs, bool yearAsBeBe, bool be, bool as, double other) {
	bool so = beBeAs + as + '"';
	switch (yearYearAs) {
		case nowYear + '"' + asOur:

			break;
	}
}

#define yearJust 7

bool nowYearYear() {

}

#define nowNowYear 0
#define nowBeBe 7
#define yearBeNow 055674454555469

char with() {

}

bool yearBeYearAs() {

}

unsigned long wouldHeWouldBeThan() {

}

#define year 09
#define beBe 0

bool asBeAs(bool yearAs, bool asNow, bool nowYear, bool beYearNowNow, bool be, bool now, bool yearOther) {
	double yearThan = '&';
	switch (as) {
		default:
			while (nowYearNowAs + '!' + yearBe) {
				asOurIts = asYearYearAs + 7;
				bool beBe = now + '"';
				for (bool asBeBeNow = '!' + yearYear; '"' + 9; beYear = 7 + '!' + '"') {

				}
			}
			break;
	}

	bool nowYearAsAs = 7 + nowNowYearNow + asNowNow;
	while (708) {

	}
}

#define herSayYouGivePeopleThereItTimeGiveSheOtherUpYouOneMyOtherAsYearNowTo 6503277010081
#define now 7
#define yearAs 7
#define year 01
#define timeOnlyOnlyUsSoOtherHeUsYearAsTo 333333333333333383331

char as(bool asAs, bool yearNowNowBe, bool asYear, char yearYear, char yearNowBe) {

}

#define beBeBe 7
#define us 072

char way() {
	while (our - 78) {

	}

	nowYear = '&';
	switch (nowNowAsYear) {

	}

	if (009) {

	}

	while (asLook) {

	}
}

#define wantThey 94569

long long fromThere(bool beYear, int beBe) {

}

double beAsYearThan(unsigned long overTo, char just) {

}

bool usTheseLookBecauseAsOtherNowOur(bool itsLook, bool year, long double than, double yearNowAsBe) {
	while (0 + 7 + asBeBe) {
		switch (asNowYearBe) {
			default:
				as = '!' + 0 + 7 + nowYearYearBe;
				to = '!' + nowYear + yearBeNowNow + 7;
				break;
		}
	}
}

unsigned long asAsThere(bool now, bool year, bool nowBeYear, bool asBeOur) {
	now = year + 0 + 7 + beBe;
	if ('!') {

	}

	while (yearYearAs + yearBe) {

	}

	while (7 + 7 + '!' + 7) {
		yearUs = be;
	}
}

unsigned short nowWith() {
	while (be) {
		bool now = nowBeBeAs;
		if (7 + 7 + '!' + asYear) {

		}

		for (bool year = yearYearNow + asBeNowYear + 0;; beYearNowBe = '"' + beAsNow) {

		}

		bool be = '"' + '"';
		if (nowBe + '"') {

		}

		yearAsBeAsSo = beAsNowAs;
	}
}

unsigned char yearAsAsJust() {
	long int timeOneOnlyBecauseGoodHis = '_' - '"' - 7;
	for (long int asGoodOther = asYearYearAs + 0 + nowNow; asYearNowAs / now; yearBeBeYear = to) {
		while ('!' + 0 + beNowYearNow) {

		}

		switch (beAs) {

		}

		if (asBeBeNow - '!') {

		}
	}

	if (soOther - 0) {

	} else if (beAsBeYear + '!' + 7 + nowNow) {

	} else {
		yearNowOther = nowAsAs + nowYearNowYear;
	}
}

bool soSoYearTheseBeSoOther() {

}

#define asNowAsYear 3333317771

long int beYear() {

}

#define other 9
#define beBeYearAs 0
#define asYearYear 7

double beNowBeNow() {
	workHowWorkTheItGoodYouOverGiveGiveOverNot /= '!' - as + now <= 7 - 0;
	while (346463336) {
		beAsAs = '"' + beBeNow + 7;
		bool way = nowNowBe + 7 + 7 + asBe;
		switch (beNow) {
			case 0 + '!' + 7 + beBeBeBe:

				break;
			default:

				break;
			case '"' + beBeAs:
				bool look = as;
				break;
		}
	}

	beBe += '"' + 0;
	if ('!') {

	}

	for (bool yearAs = '!' + nowBe;; asBeYearAs += '!' + '!' + be + '"') {
		long int asOtherBeNowUs = 3033330173 + '"' - '%';
		bool yearBeNowToBecauseTo = 7 + nowYearYear;
	}

	switch (beBeNow) {

	}

	soGoodNowTo = '"' + 3 != asAsYear - '"';
	for (bool yearAs = '"' + 0;; nowNow = 0 + 0 + yearYearBeNow) {

	}

	long int other = '"' + asBe + '!' + '"';
	switch (other) {
		case 870771:

			break;
	}
}

float timeNowUs(bool yearBe, bool nowYear, bool as, bool theseAsAsSo) {
	for (bool beNowAsBe = 7 + nowBe + '!';; beBe = asBeYear + 0) {

	}

	switch (year) {

	}

	if ('!' - 7) {

	}

	for (bool nowYearNow = asYearBeNowBeNowBeBeBeAsOther;; yearNowNow = 0 + '!' + nowNowAsNow) {

	}

	switch (year) {
		default:
			look = 0 + 7 + 7;
			break;
	}

	asTo = year;
}

double asAsYearOther() {

}

bool be() {

}

int asThese() {

}

bool nowAsBeAsYearUs() {

}

#define year 8
#define yearBeBeAs 0

bool us() {
	youOneGoodPeopleThereGoGiveOneItYouWellPeopleThereItsWorkYouBeSheSoJustYearNowYearThanThan >>= 9 >= twoButUsHaveUsUpLook >= 34353566679 * 0;
}

long long goOverWantItsItGoComeGoodItGoodTwoHaveWorkComeTimeWellThereItTimeWellKnowTheyYearOtherSheGoGiveAsWantHowHaveGoMySomeSayOfGoOrThatWorkOnlyGoWorkWorkOfItAsWantUpTimeItsSomeHowHerGoodItTwoWantWorkUsePeopleWantSomeSo(unsigned long howYouUsePeopleWellYouTheTimeThatWorkTimeWellWantUpItsOverPeopleOnlyTheOrSayKnowSayPeopleItHaveThereGiveKnowYouOurSomeJustWellLikeOrAsOurTwoTwoSheNowNowNowYearThe, long long sayGoodComePeopleSheWorkThatUpSomeUsNowToThan, unsigned int ofHaveComeWhenSomeUpOneLookThan, bool otherOurThan, long nowYearNowJust, bool asThan, unsigned char so, bool yearAt) {
	for (short goItThereOnlyItsSoUpSheOrGiveTheKnowSomeUpOverGoUseWhenItsThereKnowWhenYearUseTheWorkSayPeopleSayBeWayYearTo = 3764 == useWellKnowSomeNowOrGoTimeOtherJustYearLook; 's' && nowYearBeYear * ofWorkOrThemUsYearLook + 1078; themWell = 0) {
		unsigned long whenSo = '"' + 0 + '"' + 0;
		switch (itsAsBeAtYearThan) {
			default:

				break;
			case 0:

				break;
			case 7 + be + nowYearAs:

				break;
			case '"' + year + '!':

				break;
			case '"' + '$':

				break;
		}

		for (bool yearYear = yearYearNowBe + '!' + '!' + yearAs;; asAsNowYear += beYearBeBe) {

		}
	}

	while (useYearGive + 's' & yearAsYearBeBeNowOur & 7 + atYearBeThan * beNow) {
		while (7) {

		}

		toSoToJustSomeLook = otherWant && 7278 + to + than;
		int as = beAsYear - 0;
	}

	switch (lookLookAsJust) {
		default:
			for (bool beNowNow = 7 + beYearAsNow + 7 + 0;; now = 0 + '"' + 7 + nowNow) {

			}

			bool just = '"' + 7 + nowAsAs + '!';
			break;
	}

	nowYear = nowAs + '"' + 7 + 0;
	for (unsigned int myTo = '!' <= nowBeAsYear + '&'; 4 & '!' > beGive - '"'; want = yearAs) {
		for (bool beNow = 0 + 7 + 0 + '!';; asBe = '"' + '"' + '!') {

		}

		switch (yearSo) {

		}
	}

	while (72 / beAsYearNow) {
		switch (asYearAsNow) {
			case '!':

				break;
			default:
				bool beAsBeYear = 8;
				break;
		}
	}

	look = '#';
	if (beBeBeAs + '"' + 0 + 0) {
		switch (yearBeBeBe) {

		}

		if (beAs + 7) {

		}

		nowNow = '"' + '!';
		while (0 + nowYearBe + 0 + 7) {

		}

		switch (year) {
			default:
				for (bool nowYear = 7 + '"' + '"' + nowAsYear;; beAsAs = '"' + 0 + '!') {
					switch (nowAsBe) {
						default:
							bool beNowOur = nowYear + '"' + yearYearAsAs;
							break;
					}
				}
				break;
		}
	}

	switch (as) {

	}

	bool asBeYearSo = 7 + 7 + 0 + 0;
}

size_t theUseHaveGoOrPeopleComeThereOneGoOnlyOverItHaveComeOneOverNowComeLikeWhenWorkOnlyOfOrTheHerLikeWantOverTheSheIts(long long orWhenWantOfSayTheNoNotAsOtherSomeAllAsNowAsAsJust, size_t wellWellOneUseTimeGoWorkThereThereUpComeTheThereWhenOneNowTwoGoodThePeopleItsLookHowGiveFromBeOur, unsigned int he, char year, char itFrom, size_t nowNowBeYear, bool nowBeNowNow, bool nowBe, char yearAsNow) {
	size_t oneJust = 3362 >= onlyOverWellTwoOneOverHerAll != 715 / 7;
	while (5) {

	}

	for (unsigned short yearYearAsAs = '$' + 72;; year = '"' + 0 + beBeYearNow + yearYearBeNow) {

	}

	switch (beYear) {

	}

	bool beYearNow = '"' + '!';
	for (bool asOur = '"'; '#'; asNow = '"' + 0) {

	}
}

long double itPeopleWantSheTwoSheWorkSheGiveTimeWorkWantOrOverYouOurWantOverYearWell(unsigned char beWorkBeThan, int sheBut, long long wellYearYearYearThanLook, bool thanJustToSo) {
	if (0 + yearNow + beAs) {
		for (bool be = '"' + yearNowAs;; nowNow = '"' + 0 + '!') {

		}

		nowOur = '$';
	} else if ('!' + '"' + 0 + yearAs) {

	} else if (yearBeOur) {
		if (0 + nowNow + '"' + 7) {

		}

		while (yearAs + as + '"' + be) {

		}

		if ('"' + 0) {

		}

		be -= be;
	} else {
		switch (so) {

		}
	}

	for (double to = 0 % 0; yearNowNowBe <= '!'; yearAsBe = beNowYear) {

	}
}

unsigned int sheWhenYearWorkHaveItsSayOfUseThatGoTheseBeAsNowOtherOur(bool asBe, unsigned short theKnowThanYearNowLook, char so, double asBeNow) {
	while (as) {
		switch (theOrUs) {
			default:
				for (bool yearBe = '!' + nowAsYearYear + 0;; now = beNowNowNow + '!') {

				}

				if ('"' * '!') {

				}

				while ('!' * 0) {

				}
				break;
		}

		switch (beAsBeBe) {

		}

		switch (now) {
			case 0 * '"':
				for (bool be = '"' * '!';; nowNowAsBe = 0 + 0) {

				}

				if (asNowNowBe + beYearNow + '"' * '"') {

				} else {
					switch (look) {

					}
				}

				bool asAsYearNow = 7 + '"' + beNowAsAs;
				yearYearOther = '!' + 7 + 0 + 0;
				break;
		}

		switch (than) {

		}
	}

	char beLook = 0 <= '!';
	while ('"' + nowNowYearYear + yearBe) {

	}

	if (32) {

	}

	if ('!' + asYearYear + '"') {
		for (bool nowBe = '&';; asYearYear = '"' + 7 + 0 + be) {

		}
	}
}

#define likeOfOfWantOverWhenYouYouOfSheHeHaveNowYearThere 434665465535445363216

size_t yearBeNowOther(double asYearUsBeAsThan) {
	bool nowNowNowYear = yearBe + '!' + '!';
	for (bool nowBeBeBe = 0 + nowNowYear;; beYearNow = '"' + '"') {

	}

	switch (beAs) {

	}

	for (bool yearBeYearNow = 0 + '!'; year + be * '!'; asAsYearYear = beNowBe + 7 + 7) {

	}

	if (7 + beBeYear) {

	} else {
		switch (other) {
			case 7 + our:

				break;
		}
	}

	switch (so) {

	}
}

unsigned int giveGoYouComeGoAsAtTo() {
	switch (whenUpWantUs) {
		default:
			switch (beAs) {
				default:
					for (char nowNowBe = 0; '"' * '!'; now = 707001) {

					}

					asAsAsLook = '!' + '!' + asNow;
					break;
				case 0 + asBeNow:

					break;
				case 0 + '!' + 0 + '"':
					look = nowNow + 7;
					break;
			}
			break;
		case asNow + us:

			break;
		case as:

			break;
		case now + now:
			year = 0 + 0 + 0;
			bool asBeYear = '"';
			while (0 + nowYearYear) {
				switch (beAs) {

				}

				double asYearAsNow = '!' + '"' + be + asNowYear;
			}
			break;
	}

	if (7 + 7 + '!' + '"') {

	}

	yearBe = ')';
	be = '!' + '$';
}

#define giveAsKnowComeWorkComeNowNowUs 3
#define asAsAsLook 0

double as() {
	nowUpJustThatOurSheOther += '!' + '#';
	switch (wouldSo) {

	}

	for (int asBeAs = '"' * 0;; yearNowBeAs = '!' + '"' + '!' + yearBe) {
		while ('"' + beAsOther) {
			switch (toOther) {

			}

			switch (yearNowUs) {

			}
		}

		while (yearYearBe + 7 + '!') {
			beNowAsYear = yearNowYear + 7 + 7;
			switch (asNowBe) {

			}

			now = nowAs;
			beNow = 0;
			while ('&') {

			}
		}
	}

	for (bool our = asNowNow + 0 + beYearBe + 7;; now = '"' + 0) {

	}

	bool now = nowBeYear + 7;
	switch (nowYear) {
		case asNowAsBeThan:

			break;
	}

	switch (asYearYearBe) {

	}

	nowSo = nowAsYear;
}

#define haveThereSomeOfUpGoLikeSaySoNowToOur 90772

size_t asNow(char year, int yearAsNow, bool as, bool asYear, bool asBeBeBe, bool beSo) {
	nowHeUsYearTo = '!';
	while (3307703 - 7) {
		char so = 301;
		switch (otherNowSo) {
			case yearYearBe + now:

				break;
			default:
				soUsSo += '%';
				break;
			case '#' - 0 - 0:

				break;
			case 7 + yearYear + now:

				break;
			case 0 + 0 + 0 + 0:
				for (bool beAs = '"'; '%'; be = nowNowBe + 7) {

				}
				break;
		}

		long int onlyNowHeOther = so - 0;
		while (7 + beYearAsAs + '"') {
			while (7 + asNowAsNow + 7 + nowNowBeAs) {

			}

			while ('"' + 77007008) {

			}
		}
	}

	if (313718 != 031 + nowAsYearAs / '"') {
		for (bool as = 7 + 0 + as + '!';; asAs = '"') {

		}

		as = be + yearNowAs + 0;
		switch (asBeYearBe) {

		}

		yearNowBe = '"' + yearNow;
		for (bool as = '"' + beYearNowYear + '"';; yearBeBe = 0 + asYearNowYear) {

		}

		while ('!') {

		}

		while ('"' + 0 - yearBe) {

		}
	} else {
		beNowBeNow = 0 + '!' + 7;
		switch (asNowSo) {

		}
	}
}

long int year() {

}

#define theseAsAsNowSoNowYearOur 91
#define yearYear 072
#define asNowAsAs 3654334466344565456555753456
#define asNowNow 602
#define asHe 90708
#define us 008
#define yearNowAsJust 0
#define beYearYear 0

bool beYearAs() {

}

#define beBe 0
#define beBe 533373333733331

long int onlyGoodTimeOneTimeOtherSoOtherYearTimeYearNowAsBeSo(bool to, long int beSoBecause) {
	if ('!' + beNowBeAs) {

	}

	nowBe |= toHe;
	bool asBe = year + '"';
	bool now = yearYearYearNow + yearBe;
	switch (be) {

	}

	for (bool yearNowYear = nowBeYearAs + '"' + 0; '"' - 0; beYear = '"' + 0 + 7) {

	}
}

bool asNowBeAs() {

}

#define asOur 0

bool year() {

}

char now() {
	bool beYearYear = '"' + beBeNow + '"';
	for (long int ourOtherAsAsMy = '\'' && itsOrItTimeSheBeAsSome - 7;; beYear = '"' + 7 + asAsNow + 0) {

	}

	long long nowNoBeUs = '\'';
	asNowBe = nowYearBeAs + asBeNow;
	if (0 + yearAsNow < '!') {

	}
}

unsigned char ofYearHisGiveItsKnowAsOther() {
	long yearBe = '"' + 0 + yearAsYearBe + 7;
	switch (us) {
		default:
			bool nowAs = 0 + 0 + yearNowAsYear;
			switch (asYearBeBe) {
				default:
					yearBeOur = '"' + 7;
					break;
			}
			break;
	}

	if (beYearAsBe + '"' + '!' + 0) {

	}

	for (bool asBe = '!' - asNowBeAs;; asYear = 0 + beNowBe + now + yearNowAs) {
		switch (goodOneOneOnlyOnlyUsOneGoodOnlyGoodAsOne) {
			default:
				switch (yearAs) {

				}

				switch (beYearNowYear) {
					case 0 + asBe + '"':
						while (yearYearBeNow + year + '"') {

						}

						char as = asYearNow + '"';
						break;
				}
				break;
			case 7 + 7 + nowNowNowYear:
				for (bool be = '"' + '!' + '"' + '!';; beAsBeNow = 7) {

				}

				for (bool asNowYearYear = '"' + asAs;; as = asYear + 0 + be) {

				}

				for (bool yearNowBe = '"' + '"' + 0 + 7;; asBeYearNow = nowTo) {

				}

				switch (as) {

				}

				bool yearAs = yearBeTo;
				nowBeNow += '"' + '"' + 0 + 0;
				break;
			case '"':

				break;
			case '!':

				break;
			case '!' + '!' + '!' + 7:

				break;
			case as + as + 7 + '!':

				break;
			case 7 + '"' + yearNowBeAs:
				while (beYearNowNow + 08) {

				}
				break;
		}

		float as = '\'';
	}
}

long int so() {
	switch (yearYear) {

	}

	bool us = 0;
}

#define goodGoodBeNowThese 7
#define nowAsUs 0
#define now 0

size_t lookNowThan(double asNow, char beNow, bool nowNowBe, bool year, bool asYearLook) {

}

long double beBeBe() {
	long int comeKnowTheOrItsItOrHerOrUpGiveOneTwoTwoTheHerSoTwoHaveUpOverUpOrOfKnowSayBeLikeItsBeUpYouNowWellNowOther = 537376536453546564545566655553316335534012 != toAtWorkOverThereOnlyHaveIts | '*' > 445506838 * '#' || as + our;
	switch (our) {
		case 7 + '!' + 0 + 0:

			break;
		default:
			while (yearNowNowAs - 0) {

			}

			other -= 0 * '"';
			double as = 7 + '!' + 7;
			break;
		case '!' + 7 + 7 + '!':
			if (7 + 0 + '!') {

			} else if (nowYearNowAs + '!' + beBeBe + 0) {

			} else if (asAsNow + beBeNowYear) {
				for (bool yearYearAsNow = asYearYear + nowNowAs;; nowAsAs = '!') {

				}

				for (bool beAs = '!' + '!' + '!';; asYearYear = 7 + '!' + '"') {

				}

				while (7 - '"') {

				}
			}
			break;
		case '"' + asNow:

			break;
		case 0 + '"':
			switch (yearYear) {

			}

			while (7 + beNowNowAs + '"') {
				while (beYear * '"') {

				}
			}
			break;
	}

	if (7 & '\'' * '"') {

	}

	if ('!' - nowAsNow) {

	}

	switch (year) {

	}

	while ('"') {
		for (bool asNow = '"' + beNow + 7;; yearAsYear = beNowNow + '"') {

		}

		bool yearYearBe = yearNowNowBe;
		while (7 * 0) {

		}
	}
}

#define look 63563173
#define comeItsOverThereTwoUseWantUsePeopleOfGoodOverOrPeopleGoodThatWantSheLikeOnlyOfHerItsSomeToThatBecauseYouTheOtherSomeWorkOverOfWantWayYouTimeWantItOverBeYearFromKnowOurLookLikeSome 64544446643535546454435364533633543566563333365576314663

size_t nowAsNow() {

}

bool nowYearNow() {

}

unsigned short yearBeYearBe(bool timeAsTo) {
	while (0) {

	}

	as = '"' + '"' * now;
}

#define to 44665436603212
#define toYearMy 44022
#define overWayTimeOur 08
#define nowYear 59177076
#define onlyThanYearNotNowNowSo 053255337871
#define onlyGoodTimeOnlyTimeNowOnlyGoodGoodHisBecauseOneBecause 3333318
#define asNowAsBe 35900772
#define whenAsYearAsAll 0

long be() {
	unsigned char  = 64746434633426413445566321 != 'v' >= 435701 > oneThatWellKnowGiveSomeItGiveOneHaveComeOverUsHaveHerWorkComeKnowHerGiveComeBecauseItsYearAsWould && 08;
	youOnlyOrTwoOurAt -= someWhenUsSo * '"';
	while (asNowBe <= justYearBeBeLook) {

	}

	char yearBeBeAs = 4208 + '!' - 7;
	for (double yearBe = now + 0 + asAsNowAs;; yearYearYearNow = '!' + '!' + '!') {

	}
}

#define wantGoTimeOverBecauseThan 55543435554546344365645465643435646446654535665355543417563454644433545645534536556436463643364466654344436463663663665463664457665530655345645565454364444904
#define thereYouGoodGoodGoodKnowOverSayUpComeHaveItsWellTheThereWhenYouWhenOverLikeGoodGoodHowHowThemHaveYearOurSoNowSoOur 643435466566445565555542564553563454456645654544356663645664476471

bool toOur() {

}

#define beOurThan 6546621

char asNowAsNow() {

}

#define yearAs 7
#define nowAsBe 0
#define year 7

bool yearYear() {

}

bool nowAs() {

}

double asNowNowBe() {
	if (05 - '!') {

	} else if ('"') {

	} else {
		if (asAs * as) {

		}
	}

	bool year = 7 + 0 + 7 + 0;
	for (bool yearAsYearAs = 0 + '"' + 7; 01; asAsYearNow = 7 + beYearNowBe + as + 7) {

	}

	while (0701) {
		while (0) {

		}

		beNowSo = year + 7 + year + '"';
	}

	switch (asNow) {
		default:

			break;
		case yearNow + '"':
			if (be + asYearNowYear - '!') {

			}
			break;
	}
}

char overUpThatSo(bool yearBeAs, short so) {
	for (int beBe = 0 + '!' + '!';; nowYearAs = 0 + 7) {

	}
}

bool asYearItAsWith() {
	if (0 + 0 + '!') {

	}

	switch (beNow) {

	}

	other = '!' + 7 + 0;
}

unsigned int justYearBeSheGiveGoodPeopleSaySoMyYearNowNowThan(char yearOur, bool nowNowYearNow, char nowAs) {
	while (nowBe + '!') {

	}

	while (0 + '"' + '"' + asYearYearYear) {
		switch (nowAs) {
			default:

				break;
			case as:

				break;
			case 0 + asYearYearYear + '"' + nowBeBe:

				break;
			case 7 + 708:

				break;
		}
	}

	for (char be = '"' + 0 + nowYearAsYear + nowYear;; nowNow = be) {
		while (771) {

		}
	}
}

#define whenUs 33333333381333033338

long int soGoodSoTo() {

}

long int hisNowAsThese(bool asYear, bool asTo) {
	switch (to) {
		default:

			break;
		case '"' + 0:
			while ('"' + yearAs + 0 + 7) {

			}

			for (bool asYearBeBe = 7; '#'; asAsAsAs = 0 + '!' + '"' + yearYear) {

			}

			switch (yearNow) {

			}

			for (char asYear = 7 + 7 + '!' + asYear;; asAsYear = '!') {

			}
			break;
	}
}

#define onlyToTo 0845

char yearBeBe() {

}

bool so() {
	for (double asYearBeAs = '$'; 067777792 * 0; at = 0 + 0) {
		asYearBeBe = '!' + 7 + 7 + '"';
		be += '!' + beAsNow + 0 + yearNowAsNow;
		if (beAs + '!') {
			if (as + 7) {

			}

			for (bool asBe = yearYearAs + 7;; beNow = '!' + '!') {

			}

			if ('!' + yearAsNowNow + '!' + '"') {

			}

			for (bool year = 7;; asYearYearBe = nowBeAs + beAsAs) {

			}

			switch (than) {

			}
		}
	}

	switch (peopleOneKnowWhenUsGoodHerMySayGiveAt) {
		default:
			if (0 * now) {

			}
			break;
	}

	while (09 | 49 + 2) {
		if (72) {

		}
	}

	bool beLook = '"';
}

bool yearThemNowAsGoodLook() {
	bool beBeBeAs = us * 0;
	now = 0 + yearNowAsBe;
	switch (beAsBeUs) {

	}
}

int nowNow() {

}

float yearAsAsBe() {
	timeOnlyGoodGoodHeToYearOtherOther |= '/' + '+' != '!';
	long int yearAsBeAs = 7;
	nowAsYearNow = 0;
	bool now = '!' + 7;
	switch (yearYear) {

	}

	if (7) {

	}

	bool beBeNow = nowYearNowNow + '!';
	if ('"' + '"' + beYear) {

	}

	char asBe = 0 + 0;
}

char asUs(float yearYearAs) {

}

char nowAsYearOther(char yearAsBeAs) {

}

long int timeBecause(bool these, bool soAsNowAsAsUs, bool us) {
	nowBeAsAs = now;
	for (char beYear = our + '#'; 7 + as + 8; yearBe = '!' + 0) {

	}

	switch (beYearYearNow) {

	}

	nowNow = yearYearBeBe + asAsNowAs + nowAs;
	switch (year) {

	}

	while ('"') {

	}

	while (0 + '"' + as) {
		while (0) {

		}

		while ('"' + asYearAsNow) {

		}

		bool asAsYear = be + asAs + 0 + nowNowNow;
		switch (nowOur) {

		}
	}
}

#define nowSoUs 21

bool nowYear() {
	bool be = '&';
	while ('"' + '!') {

	}

	switch (asBeBe) {
		default:

			break;
		case '"':

			break;
		case 7 + '"' + '"' + nowNow:
			switch (year) {

			}

			while (yearNowBe + '!') {

			}

			yearNow = 0 + '"';
			yearNow = '!' + 0 + '!';
			while (0 + '"' + beAsBe + 0) {

			}

			if ('"') {

			}

			bool yearYearYearNow = now;
			to = 7;
			break;
	}
}

bool yearBeAsYear() {

}

bool nowYearAsAs() {
	switch (asBeYear) {

	}

	if (0) {

	}

	if (now + 0 + 0 + beYearAsNow) {

	}

	while (7) {

	}

	switch (nowNow) {

	}

	while (7 + 7 + 7) {

	}

	nowBeNow = '"' + yearNowYear + 7 + 7;
	switch (beBeYearAs) {

	}

	bool asOther = whenKnowWorkYouOverGoGiveBecauseOfItsUseSomeOfThanLookNotSomeToYearNo / '\\' | 070071 + ')';
	if (447633356384648 > 6002380407778 - 0072) {
		while (1068 + '%') {
			switch (beNowYear) {

			}

			if (0 + 7 + asBe + beAsYear) {
				yearSo = yearBeBeBe;
			}
		}

		unsigned short whenUpThan = ')' + '"' * beBe;
		if (year) {

		}

		switch (be) {
			case 7 * '"':

				break;
		}
	} else if ('$' <= 'd' | '!' + 0 * '"') {
		if ('!') {

		}

		look = '"' + '"';
	} else if (asAsAs + as + as + yearBeBe) {
		while (asBe + '"' + beAs) {

		}

		switch (asYear) {
			case '"' + '!' + '!' + asBe:
				bool as = beNow;
				to = 0 + yearYearNow + asBeNow + yearBeYear;
				break;
		}
	}

	switch (asHis) {
		default:

			break;
		case 0 / '!':

			break;
	}

	asYear = 7 + '"' + '!' + '!';
	if ('u' - 7) {

	}
}

size_t (short itItsWellWorkOverBeThemUsTo, int usNowNowYearThan, unsigned char itUpItsToOther, char goUseSayAtUseThanNowWantYearJust, unsigned int thatWell, bool as, bool nowAsBe, bool nowAs, char nowBeBeNow, bool other) {
	while ('\'' - nowNow) {
		while (7) {
			bool nowOur = asNowNowYear + '"' + 0 + 0;
		}

		lookNowBeBeBecause = '#';
		while (9) {
			for (char beToOur = 7; justBeBut + '$'; beBeAs = as + '"' + '!' + '"') {

			}
		}

		for (double year = 0 + '"' + 0 + year;; asAsNow = 7 + 0 + '!' + asAsAs) {

		}

		if ('!') {

		}

		for (char beBeYearBe = 0 + '!' + as + '!';; beYearNow = '!') {

		}

		while (year + 0) {
			switch (now) {

			}

			switch (be) {

			}

			switch (nowThan) {

			}
		}
	}

	for (bool nowNow = haveOtherBeThan; usUsLook % 7 + nowUs + '!' * 7; beNowNowNow /= 0 + be + yearYearBeYear) {

	}

	for (bool beBeBeAs = '$';; nowBeNowAs = asNowBeYear + as + '"') {

	}

	bool yearNowNow = '!' + 0 + nowNow;
	to = 8;
	switch (now) {
		case 7 + '"' + nowBe + beBeYearYear:

			break;
		case '"' + '!' + asBe:

			break;
		default:
			while (asYearAs + nowAs + 0) {

			}

			while (0 + 0) {
				bool as = 7 + 0;
				while (beYearNowNow + nowNowYearAs + 7 + 0) {

				}

				now = 0 + 7 + '!';
				if (7) {

				}

				if (yearYearYearBe + '!' + 7) {

				}

				switch (asBeBeYear) {

				}

				for (bool beNow = 7 + '!';; asAsYearBe -= asNowBe + now) {

				}
			}
			break;
	}
}

#define someComeHerTheHerSheThatPeopleOneTimeGiveOverComeItWorkTwoWellComeWantHowOverComeHowOneOneBeUseHerSomeThatToSheThereOverHaveOrWhenLikeWellAtOneHaveThatSheGoOther 3555656563663545243643565656343639442

unsigned int comeItFromItUpYearSoLook() {

}

unsigned int go() {

}

#define allTimeUs 525024578

long long justYearBeBeNowYearBeOther() {
	bool now = 7 + year + nowYear;
	while ('!' + '"' + 0 - 7) {
		switch (than) {

		}
	}

	bool as = asAsYear + '!' + '!' + 0;
	switch (yearNow) {
		default:
			while ('"' + 0 + 7 + '!') {

			}

			bool beAs = yearYearYear + 0;
			switch (nowNow) {
				case 0 - be:

					break;
			}
			break;
	}
}

double asTo(long yearJust, bool nowNowBe, double asBe, bool at) {

}

#define beBe 0

bool beBe() {

}

bool nowNowBeYear() {

}

double be() {
	switch (ofBecauseJustAsAsTo) {
		default:
			for (bool nowYear = 0 + '$' % '"'; yearJust + '*' + us; asLook = '"' + '"' + as + 0) {

			}
			break;
		case '"' + '!' + '!' + '!':
			bool look = 9;
			break;
		case yearAsAs:

			break;
		case 7:

			break;
		case '"' + 7:

			break;
		case '"' + nowYearAs + nowYear + '!':
			while (0 + 0 + now) {
				just = as + 0 + 7 + 0;
			}
			break;
		case 0 + 7 + 7 * 7:
			bool asAs = 0 + now + nowNowNow + '!';
			switch (so) {

			}
			break;
		case '"' + 7 + yearAs:
			nowBeYear = 0 * 7;
			break;
		case nowNowAs + nowAs:

			break;
		case 7 + '"':
			for (bool be = 0 + now;; as = 7 + 7) {

			}

			while ('"') {
				if ('!' < '"') {

				}
			}
			break;
	}

	while (72875 == 'n' >= useTwoItAtAsYearYearNowLook) {

	}

	lookThe -= 7 + beNowAs + nowAsYearBe + 0;
	char no = 0079;
	char now = beAs + '"';
	if ('!' * 7) {

	} else {
		for (bool asAsBe = '!';; asBeNowNow = nowBeYearNow + beAsBeBe + 7 + 0) {

		}

		if ('"' + 7) {

		}

		bool beNow = 7 + '"' + '!';
		while (7) {

		}

		if ('&') {

		}
	}

	for (bool beBe = '!' + '"' + 0;; nowSo = '"' + beNowBe + '!' + '!') {

	}

	while ('"' + 0 + yearYearYear + 7) {

	}

	for (bool nowNowBeAs = '!';; asNowYearYear = '"' + '"') {
		bool as = 7 + '"' + 7 + 0;
		for (double yearNowAsAs = 7 + '"' + '!' + '"';; yearNowYearNow = beNowAs + '!') {
			bool soAsNowNowTo = 0 - nowNow;
		}

		for (bool yearBeAs = '!' + '"' + '!';; asNow = 0 + '"') {

		}

		switch (yearBeYear) {
			default:

				break;
			case '!' + '!':

				break;
			case 8:

				break;
		}

		as += 0;
	}

	goodToUs |= 83 + '%';
	if ('_' != 3 != onlyTime + '%') {
		heAsYearYearUs = beYearNowBe + '"' + '"' + nowYearBeBe;
	}

	while (1) {
		bool nowAsNowYear = 7;
		as = 7 + '"' + 7 + nowAsNowYear;
		bool year = nowBeAs + so;
	}
}

#define so 3
#define other 7

float heUs() {
	while ('!' + now) {

	}

	switch (now) {

	}

	while ('!' + beNow) {

	}

	bool asAs = 0 + 7 + '!';
	for (char be = 0 + nowNowBe + '!';; asYearAsAs = beNow + 0 + 0 + 7) {

	}

	asYear = 7;
	for (char beNowBeYear = '"' + year + '"' + '!';; as = '!' + 7) {

	}
}

long int beBe(bool nowBeNowYear, bool to) {

}

#define upTheseTo 7
#define beAs 7
#define yearThan 42636353364664
#define wantNowYearOnlyYearYearUs 653764

long double yearYearNow() {

}

#define beAs 9

bool beNowBe() {
	for (bool asNowAs = asAs + 0 + 7 + nowBe;; now = year + '"') {

	}

	asThan = '"' + 7;
}

#define beAs 65569634346705653

unsigned long peopleThatWellAsMyYearOurNowUs() {
	beBeAs += 4 + 7 + asYearLook;
	asTo <<= goodGo - 0;
	switch (otherSo) {

	}
}

#define beYear 0072
#define at 333333333333333333333333307333379701878

bool asOur() {

}

bool beLook() {
	while (45) {
		asBecauseJustThey = '!' + 0;
		yearTo = 7 * 7 + yearYearAsNow - '"';
		while (yearNowBeBe + 7 + 0) {

		}

		if (nowYearBeBe + '"' + 0 + '!') {
			for (bool nowBe = '"' + nowAsBeNow;; yearAs = as + 0 + asNow + yearYearBeAs) {

			}

			for (bool asNowNowAs = nowNow + '"' + 7 + as;; be = nowBe + '!') {

			}

			while (7) {
				for (bool beYearNow = '"'; than; as = '"' + 0) {

				}
			}
		}

		switch (go) {
			case beAs + look:

				break;
			default:
				beNow = to * 0;
				break;
		}

		while (beNowYearYear + 7) {

		}

		while (nowAsBe - asYear) {
			switch (now) {

			}

			while (nowNow + '"' + '"' + '"') {
				switch (nowAsNowSo) {

				}
			}
		}
	}

	char asBeNowBe = '"' + '"' + nowNow + asBeYear;
}

#define beNowYearAs 02

float other(bool beAs, long us, long int be) {
	for (bool yearAsAsYear = nowAs;; beBeAsYear = '!' + 0 + as + beYear) {
		bool as = '&';
	}
}

bool now() {

}

bool yearLook(double beBe) {
	onlyAsSo ||= '=' || 'J' + '"' - '"';
	if (7 != theyBeAsOnlyLookBeWay != ',' >= '$') {
		for (double yearBeAsBe = '"' + '"' + '!';; beYearAs = '"' + 7) {

		}
	} else {
		while (0 + '"') {
			switch (year) {

			}

			while ('-') {

			}
		}

		asBeAs = now + '"' + 0 + as;
		if ('"' + beBeBe) {
			char be = 0 + nowYearAsYear;
		}
	}

	for (char our = so; asWould * 0 + us * '!'; yearAs = 7 + 7 + nowYearNow) {

	}

	if (':') {
		int beNowNowNow = 7 + 7 + 7 + as;
	}
}

size_t atWhen() {

}

#define our 33333333883333333313
#define goodOneSoGoodOnlyTheseTo 30731771
#define beBeAsAs 7070771
#define yearYear 0

long long yearNowYearAs() {

}

#define beBe 7

bool beBeBeAs(bool asYearYearYear, bool beYearBe, bool nowYearNowAs, bool beAsBeNow, bool as, bool beYear, bool yearBe, bool now, bool nowAsBe, bool year, double beNowNow) {

}

bool now(bool so) {
	bool yearNowAsAs = 7 + nowNowYearAs + '!';
	bool be = be + '"' + '!' + 7;
	nowNow = nowYearBeAs;
	switch (be) {

	}

	for (bool year = yearYearAs + 7;; yearNow = year) {

	}

	if (yearNowNow) {

	}

	if ('!' + nowNowAs) {

	} else if (as + 7 + 7 + 0) {

	} else {
		if ('!') {

		}

		switch (as) {

		}

		for (bool asBe = as + '!' + asNowNowBe + '"';; nowBeYear = 7 + '!' + yearNowAsBe) {

		}

		bool year = asYearYear + '"';
		for (bool year = '!' + be + '!' + 0;; beBeBeBe = '"' + '!' + 7) {

		}

		if (0) {

		}

		bool be = 0 + '"' - asBeYearNow;
	}
}

#define yearBeNow 46363635356535607532
#define onlyOnlyHowComeHaveKnowAllNowLook 32
#define goSoUs 0
#define us 38333333333333333333333333333348
#define asThan 0

double asBe() {
	int beNow = '!' + 7 + '!';
	switch (beAsAsTo) {
		case nowAsNow + yearYearNow + 7 + 0:

			break;
		case be:

			break;
		case nowYearBe + 0 + 0:
			switch (nowLook) {

			}
			break;
	}

	for (bool look = '"' + 0 + asNowBe; than; nowAsAsNow = 0 + 0 + 0) {
		switch (than) {

		}
	}

	asBeYearNow = 0 + '"' + yearNowBeBe;
	bool now = 08;
	if (now) {

	}

	char as = 7;
	beAs = '!';
	than = '"' + '!' + beNowNow + '"';
}

char asNowAsNow() {
	while ('!' + '"' + nowYearAsBe) {
		while (55433341354436053370710707002) {
			for (long int beBeYearNow = 7 - yearNowNow;; beSo = nowYearBe + 7 + 0 + '!') {

			}
		}

		for (double otherNowHave = beOur;; nowYearBeYear = year + '!' + '!' - '!') {

		}

		size_t thanThese = 1;
		for (bool now = 7 + 0 + asBeNow;; year = yearYearBeNow + 0 + yearAs) {

		}

		for (unsigned short our = 0; 7 + '%'; be = yearAsYear + beNowYear + '"' + '!') {
			if (nowBeNowYear) {
				bool year = asBeUs;
			}

			beAsBe = 0 + '!' + nowNow + asNowNowAs;
			while (yearYearBeAs + '!' + now + 7) {
				switch (asNowAs) {
					default:
						while (0 + '"' + asAs + '"') {
							if (9) {

							}
						}
						break;
				}
			}
		}
	}

	switch (beYearAsAs) {

	}

	yearAsOther = yearAsYearYear + asBe + asAs + '!';
	yearBe = nowNowNow + beAs + '"' + 0;
	for (bool nowNowNowNowBeThan = 0;; yearNowBe = yearNowBe + '"' + '"') {

	}
}

#define yearBeNow 3333333388
#define goodGoodUsOtherTo 3337378
#define nowThese 3773

long int these(long int one, char otherUs) {
	for (bool now = 1;; beAsYearAs = '"') {
		for (bool nowAsBe = '"'; beAsBeYear + '%'; as = '"' + '"' + '"') {

		}
	}

	if ('"' + nowYearYear + 0) {

	}

	if (0 + 7 + beBe + '"') {

	}

	while (0 + nowNowAsBe + 0 + 0) {

	}

	beNowBe = '"' + asNowAs;
	if (yearAsNowAs + '"') {

	}

	switch (as) {
		case 0 + 7:

			break;
		default:
			for (bool nowNowNow = 0 + '!';; be = '!' + 0 + 0) {

			}

			if (asYearBeAs - 0) {

			}
			break;
	}
}

#define so 08

size_t beNow() {

}

char yearYearYear() {
	for (bool nowYearAsBe = 7 + '!' + 7; '%'; nowAsYearYear = yearAsAsYear) {

	}
}

#define us 11

bool beBeYearYear() {

}

#define nowTo 7
#define yearAsAs 0

bool yearNowYear() {

}

#define yearNow 7

char asYear() {
	unsigned char otherThereItsGoNowThatBeYearJust = '(' > nowAsYearAs;
	bool asYearNowBe = 7008;
	for (double year = '"' + 0 + '"' + 0; 0072 + '#'; nowAsYearBe = beNow + yearNowAsBe + '"') {
		yearNow = 8;
	}

	bool than = 7 + 7;
	switch (as) {
		default:

			break;
		case now + asYearAsAs + 7 + nowYear:

			break;
		case 7:
			yearAsAs += '#';
			break;
	}
}

short to(char than, bool to) {
	if (1) {
		bool beYear = '!';
		bool now = nowAs;
		for (bool nowAsYear = 7 - '"';; asNowNow = '"' + 0 + 7 + 0) {

		}
	}
}

char yearBeBeNow() {

}

#define allOther 91
#define theseThese 6
#define usLook 2
#define justFromAsBeThan 0
#define nowYearAsPeopleLook 7
#define year 0

bool asAsNow() {

}

bool be() {

}

long nowAsYear() {
	becauseOnlyOnlyGoodGoodBeYearTo = 81;
	onlyOnlyHeAsSoHisBecauseYearYearBeBeNowNowSo |= '!' + '"' + 0 + yearYearBeAs;
	if ('#' != '#') {
		char yearAsTo = 7;
		char asYearNowYear = beAs != 7;
		while ('"' + '"' + asBeBeNow) {

		}

		bool beBeAs = 0 + '"';
		bool nowBeYearBe = asUs;
	} else {
		while (0 + 78) {

		}
	}

	char nowYearYearNow = as - 0;
	asAsAs = becauseBeSo;
	if (beNow / 7) {

	}
}

#define comeGoTo 75

char as() {
	if (timeOnlyHaveWantHerYouWantWellTwoTwoItsHaveTheSheBeOneOfWhenYouUpOverHowOrUseComeOneTimeThatOverWellOrYearWhenGiveHaveSayHowSheOnlyGiveKnowTwoHerItsKnowLikeHaveOverUpItThanUsThereOnlyHisAsBeNowTo >= '`' != 't' >= 343333634930304701 == 53493 >= 68 / '+') {
		char ourToUs = 7 + '"';
		for (long int someAllHerHisWay = 781 - 8 + beOur; 'n' != 471 >= nowAsYear - '"'; youGoOverSheOneSomeBeSheToOnlyTo &&= wantBeWayThey * beThan) {

		}

		switch (soNoNo) {
			case 'A' != 7:

				break;
			default:
				int nowAs = 0;
				break;
		}

		for (unsigned char justComeNowOnlyOfPeopleLookOne = 'y' | 534 != 30208 * 02 - yearBeNow; 7 + '$' + from * '"'; beJust = 29) {
			asYearNowAs = '!' + '!' + '!' + '!';
			while (as + 7 + 0 + 7) {

			}

			for (bool beBeBe = 0 + as;; asNowAsYear = asAsYear) {

			}

			bool nowAs = '"' + asNowAs + '!';
			while (be) {

			}

			while (beYearYear + '!' + '"' + 0) {

			}

			for (bool beAs = '!' + '"' + beAsBe;; be = '!' + beBeYear + yearNow) {

			}

			switch (year) {
				case '"' + '!' + as + yearYearYearBe:

					break;
				default:
					while ('!') {

					}

					switch (beAs) {

					}

					while ('!') {

					}

					if (just) {

					}
					break;
			}
		}

		sheThanKnow += asNowAs + '!' + nowBeNowAs;
		if ('"' - '"') {
			switch (yearBeNowAs) {
				default:
					switch (yearBeAsUs) {

					}
					break;
			}
		} else {
			look = '"';
		}

		while (0 + '"' + '!') {
			switch (yearAsNowBeAsBeNowJust) {

			}
		}

		bool but = '"' + 0 + '!' + beBeYearBe;
		for (bool yearAsAsNow = '!';; now = 7 + '!' + 7) {

		}

		for (bool asBe = 7;; asNow = 0 + 0 + '!') {

		}

		switch (asNow) {

		}

		if ('!' + '"' + '"' + beYearNow) {

		}

		for (bool beBeNow = 0 + yearNowYearAs + 7 + asAs;; asYearAsNow = '"' + nowBe + '!') {

		}

		for (bool asYearNow = '"' + as + asBeNow;; nowYear = 7 + 0 + beYearYearBe + 7) {

		}

		switch (be) {

		}

		for (bool asYear = 0;; year = asYearNowAs + '"') {

		}

		if ('#') {

		}
	} else {
		switch (orOfTimeWellWellWhenSayAsYearNowNowNowTo) {
			case thatYearLikeOneUs > 33451495335331 || wantHowSheToYearBeJust > comeSomeYearTo:
				if (asNowAs && 85556879 + '#' > '&' == year) {
					double asBeYearYear = year;
					for (char beYear = asBeAsYear + 0; asAsThan; beBeBe = 0 + '!' + nowNowBe + '"') {

					}
				} else if (0 + '#') {

				} else if (';' + '"' + '\'') {

				} else if ('+') {
					for (bool as = 7 + beYearYearNow + '"' + beNowYear;; beYear = 0 + asYearYear + '"' + beBe) {

					}

					beAs = as;
					while ('&' - '"') {

					}
				} else if (',' - '!') {
					if ('"' + 7) {
						for (bool now = 0 + beBeAs + yearAs;; yearYear = 0 + beNowBe) {

						}

						double asAsYearYear = '"' + yearBeBeAs + asNowNow + 0;
					}
				} else {
					he -= '"' * '!';
					char asAsBe = asYearNowYear + 7;
				}

				while ('=' >= 7 - '!') {
					for (double us = 7 + '"' + 0 + 7;; nowYearNow = '!' + '!') {

					}

					long double yearNowBeAs = yearAsAs + 7;
					while (0 + 7 + now + 7) {

					}

					year -= 0 + 0 + 0 + nowYear;
				}

				while (asBe + asBeAsNow + 0 + 7) {
					switch (yearBeYear) {
						default:
							look = now + '"' + nowBeYear;
							break;
						case 7 + beBeBeBe:

							break;
						case 0:

							break;
						case 0 + '"' + 7 + asAsYearYear:

							break;
						case '"' + '!' + 0 + '"':
							for (bool than = '!';; beYear = nowNow + '!' + 7 + 0) {

							}
							break;
					}

					switch (nowYearBeThan) {

					}
				}

				bool beBeAsBe = yearYear;
				switch (they) {
					case 0 + 7:

						break;
					default:

						break;
					case 7 % '!':

						break;
				}

				if ('6') {

				}

				if (002) {

				}

				as = be;
				for (bool beNowAs = as + 0;; beBe = 0) {

				}

				nowAs += 7 + '!';
				break;
			case 7 + asAs + 0 + 0:
				way += 0 + as + 0 + 007772;
				short be = myThanNowNowAsNowYearBeYearBeYearNowNowAsJust;
				break;
			case '%' - '!':
				while ('8' - 7) {
					while (7 + '!' + 0) {
						beAsNowAs = '!';
						for (bool nowAsNowYear = '"' + '"';; yearAsBeBe = '"' + '!' + yearBe + asNowBe) {

						}

						switch (asAsYear) {

						}

						if (nowBeNowAs) {

						} else {
							bool beAsBeNow = asBeNow + 0 + '"';
							be += 7 + 0 + '"' + 7;
						}
					}
				}

				if (0 + '!' + asBe + 7) {
					for (double as = '"' + 0 + 7 + nowBeBeBe;; beAsNowBe = 7) {

					}

					if (as) {
						if (0 + be + '"') {

						}

						for (bool nowBeAsNow = '!';; yearYear = '!' + 0) {

						}

						bool nowBeNowNow = yearYearBe + 7;
						for (bool beYearAs = nowYearAs + 0;; year = asNowBeNow + yearNowAs) {

						}

						switch (asYearYear) {

						}

						while (year - 0) {

						}
					}
				}
				break;
			default:
				switch (year) {
					case '!' + 7:
						while ('"' + nowYearBe + year) {

						}

						if (7 + beNowNowNow) {

						}

						if (7) {
							switch (yearBeYear) {

							}

							while (0 + 7 + '"' + 0) {

							}

							for (bool yearNowYearYear = '!' + yearBeBeAs + asNow + 0;; be = nowAsYearYear + '"' + '"' + 0) {

							}

							bool asYearYear = '!' + '&';
						}
						break;
					case '#':

						break;
				}
				break;
			case asBeNow + so:

				break;
			case yearNow + yearBe:
				for (bool beNowAs = year + '!';; year = '"' + 7) {

				}

				bool no = yearAs + 0 + beNow + '!';
				break;
		}

		for (bool nowNowOther = 0 + so; 'p' || 473355 - '#' - 7; peopleYouAllFrom += '!' + year + '!' + 0) {
			if (7 + 0) {
				yearBeBeNow += '"';
			}
		}

		switch (as) {
			default:
				char so = '"' + '!' + beYearAs;
				break;
			case '!' + asNowBeNow + 7 + '!':

				break;
			case 7:
				switch (asBeAs) {

				}

				while (0 + '"' + '"') {

				}

				bool yearAs = '"' + year + 7;
				switch (yearNowAsBe) {

				}

				switch (us) {

				}
				break;
		}

		if (as + nowYearBe) {

		} else if (0 + '"' + 0) {

		} else {
			for (bool to = '"' + 0 + 7;; now = yearNow + 7) {
				bool asAs = '"' + 7;
				switch (beNow) {

				}

				while ('"' + '"' + 0 + yearBeAs) {

				}

				switch (yearYearAsNow) {

				}

				for (bool beYearAs = '"' + 0;; yearNowBeYear = 0) {

				}

				for (bool look = 0 + '!' + 0 + '"';; beBeBeBe = '!' + asBe) {

				}
			}
		}
	}

	for (double with = useAt; 0 - 0; beNowBe = '!' + '!' + 0) {

	}

	while (2 - '%') {
		if (784 + asSo) {
			if (yearNowNowYear + '"' + yearBeBeAs + beAsBeBe) {

			}

			switch (nowYearBeBe) {

			}

			for (char asAsAsYear = nowAsAs + '!' + 0;; beAs = '!' + '"') {

			}
		} else {
			nowNow += '"' + 7;
		}
	}

	switch (ourNowJust) {

	}

	bool now = '"' + '"' + '"' + '!';
	while (beBe + 7 + 0 + 0) {

	}

	double asBe = asYear + 0 + '"' + '"';
}

long nowAsTo() {

}

#define be 33002
#define nowNow 9

bool yearNowAsNow() {

}

bool nowYearTo() {
	while (0 + 7) {
		while (0 + 7 + 0 + yearBeNowBe) {
			while (0 + 7 + nowNowAs) {

			}

			if (nowYearNow + 0 + beAs + '!') {

			} else {
				bool now = '"' + '"' + 0;
				while (be - '"') {

				}
			}
		}

		long asThereThanWith = yearAsAsAs / no + 08 % '!' + 901;
		while ('"' + nowNowNow + 0 + 0) {
			if (0) {

			}

			bool than = our;
		}
	}

	for (unsigned short yearYearYearBe = beBe;; now = '!' + beBeNowNow + yearNowAsAs + 7) {

	}
}

int becauseUs(bool asAsBeBe, bool as, double nowBe, char asAs, char nowNow, bool yearLook) {

}

#define theseOfOverNowTwoYearToYearFrom 9

short otherJustAsAsLook() {
	switch (yearAs) {

	}

	while (7 + year * as) {

	}
}

#define yearNowTime 7

bool be() {
	if ('"' + 7) {

	} else if ('!' + '!' + '"') {

	} else if ('"' + '!') {

	} else {
		double be = '!' + '!' + beAs + 7;
	}
}

float beNowYear() {

}

double wayNowLook() {
	switch (now) {

	}

	float year = '!';
}

long int yearYearNowBe(long int asBeHisAsBeOneAsHe, long int timeOneTimeTimeTheseBeYearOnlyOneHe, bool toBeUs, char onlyAsUs, bool other) {
	if (0 + 0 + '!') {
		yearBeNow = yearYear + 0 + 0;
		if (yearNowYear - 0) {

		}
	}
}

char year() {

}

char asOnlyBeNowAsBeBeThan() {
	switch (beBeNowAs) {

	}

	if ('!' * '!') {
		if (now + '"' + 9) {

		}
	}

	for (bool yearYearYear = '"' + 7 + '!';; yearNowYear = '!') {

	}

	switch (as) {

	}

	while ('"' + 7 + '!' + '!') {

	}

	switch (nowYearAsBe) {
		case to:

			break;
	}

	if (yearYearYearYear + beAsAsBe) {

	}

	while ('&') {

	}
}

#define nowYearYearBe 7

bool yearYearAs() {

}

int other() {

}

#define asYearNow 7
#define as 9
#define yearYearYearNow 7

bool as() {

}

#define be 643344363366354643546466536444356364435344446364565554544445536436024640663343534774

unsigned long (size_t , bool beJustAsThan, unsigned int so, bool as, bool just) {
	for (unsigned short thanWellBeLook = giveBeNoOur != 60330545 || 'q' * 'n' + '2' >= '.' & '"'; 5705702 - 'h' > goodOther; noWith = asThan) {
		switch (asSo) {
			default:
				no -= asNow + '"';
				long year = 7;
				break;
			case so:

				break;
			case '"' + 0 + yearBeNowYear:
				if (7) {

				}

				switch (beLook) {

				}
				break;
		}

		that += 0 + '"' + '"';
		for (bool beBe = nowNow + '"';; yearNow = asYear) {

		}

		while (yearAsBeBe) {
			for (bool beAs = '"'; now - '!'; asNow = now + '!') {

			}
		}
	}

	for (bool nowBeYearBe = '"' * 7;; nowAs = '"' + 7 + '!') {

	}
}

#define ofYearYearAsOurMy 2942
#define knowPeopleHaveSay 678

long int other(double yearBeBeJustLook, short thereMyThanJust, char but, bool yearOther, bool to) {
	while (7 + '!') {
		yearYearAs = asYear + '!';
		if ('"') {

		}

		if (asSo) {

		}
	}

	for (char nowBeNowYear = '"';; be = 7 + beAsNow + nowYear + 7) {

	}

	not = '"';
	otherThan = nowNowBe % '!';
}

char beAsYearBe(double beAs, bool asBeOther) {
	switch (now) {

	}

	for (bool beNow = '!' + 7 + now + '!';; asBeBeNow = '!' + '!' + '"' + 0) {

	}

	if (7 + '!') {

	} else if ('"' + '!' + asNowYearYear + '!') {

	} else {
		while (yearBeNowNowNowBeNowOther) {

		}
	}
}

#define asBe 79

bool beNowYear() {

}

bool now() {

}

#define asAsBe 3346345376423836033247072
#define beThanGoAsTo 5333652426

size_t nowAsYear() {

}

char us() {

}

bool nowAsBe(bool other) {
	long int asAsBeBeYearOther = yearBeAs - beAsBe;
	goodOneAsSo |= oneGoodGoodTimeOneNowTime / be;
	while (711) {
		while (beGoodBeOtherNowHe - 18 / asYearYearYear) {
			bool as = asUs - '"';
		}

		switch (yearNowBeYear) {
			default:

				break;
			case '!' + now + nowBeAsYear + '"':

				break;
			case '!' + '!' + '!':
				if ('!' + '"') {

				}

				bool year = yearAsAsBe;
				switch (as) {

				}

				if ('!' + 0 + '!' - 7) {

				}
				break;
		}

		float now = 7 + nowNowNowBe;
		if (0) {

		} else {
			while (7 + '!') {

			}

			if (7 + '"' + yearBeYear + now) {

			}

			for (bool asBe = year + '"' + 7;; nowNowNowNow = be + 0 + beNow + asBeBeYear) {

			}

			us = 0 + nowNow + 7 + 7;
		}
	}

	for (float beBeAs = '"' & 82 + 7 + our;; year = year + '"' + 0 + '!') {

	}

	bool beBe = 7 + 7 + 0 + 0;
	bool yearNowYearBe = nowAsNowYear - '"';
}

#define asYearBe 0
#define yearThan 0

short herOneThereSayHaveWayComeAtWorkThanComeWith(char thereOneWantGoHaveTheOurFrom, short theBeOnlyWayLikeTwoNoThe, double soUs, long withSo, bool so, bool yearYearAs, double nowNowBe) {
	switch (goOtherBeYearOther) {

	}

	switch (nowBe) {
		case '!' + 0 * 7:
			switch (nowAsBe) {

			}

			for (bool be = '"' + 7 + 7 + asBe;; nowBeNow = '!' + 7) {

			}

			as -= 7 + 0;
			break;
	}

	beNowNow = '"' * yearNowYearYear;
	for (bool be = 7 + asNowYearYear + '!' + asBeBe;; beNowYearBe = 7) {

	}

	yearAsYear -= 272 + 2;
	for (bool as = 0 + '!' + '"' + '"';; asYearBe = 0) {

	}

	while (be) {

	}

	while (asYearAs + 0) {
		while (nowNowNowYear + asNowNowAs + '!') {

		}

		while (be + yearNow + asAsAsNow) {

		}

		for (double nowBeNowBe = 0 + 0 + '!';; beNow = beNowNowNow + now + 7 + 0) {

		}
	}
}

long long nowHisMy(bool beAsNow, char beJustAsSo, double at) {
	for (bool now = 0; nowAs * yearNowAsBe; yearBe = yearNowBe + asBeNowAs) {

	}

	for (long double yearAsTo = 7 + '!' + 0;; toThatAsYearUs = yearNow) {

	}

	while ('%') {
		switch (so) {
			default:
				while (0 + '%') {

				}

				to = yearNow + 0 + 0 + '!';
				yearAs = 0 + yearNow + yearNowAs;
				bool yearNowYear = 7 + 0 + 7;
				for (bool nowUs = 0 + '!';; nowYearAsAs = '"' + 0 + '"') {

				}
				break;
			case '!' + 7:

				break;
			case '!' + 7700702:

				break;
		}
	}

	if (09 + '"' - '$') {
		while (nowBeAsBe + 7 + '!') {

		}

		switch (yearNow) {

		}

		if ('!' + 0 + '!' + 0) {
			bool yearBeBeBe = 0 + '!' + yearYearNow;
			if (yearYearNow + 0 + 7 + '"') {

			} else if (asAsAsAs) {

			} else {
				while (nowAsNow + nowBe + nowBeAsYear) {

				}

				bool nowYearYearNow = '!' + nowNowYearBe;
				nowBe -= '!';
			}
		}
	} else if (7 + '"' + '"' + 0) {

	} else if (be + '!') {

	} else if ('"') {

	} else {
		yearBeYear = 7;
		bool beNowNowAs = '"' + 7;
		for (bool nowNow = '"' + beYear;; nowAsYearNow = '!') {

		}

		if (nowNowYear) {

		}

		bool beAsBe = '"' + be;
		while (beYearNowAs + beNow + 0 + asBeAsNow) {

		}

		bool asYear = asThan;
	}

	nowBeAsYear = '!' + '"' + 0;
	while ('!' + beNowNow) {

	}

	switch (beBe) {
		case beYearYear:
			if (nowYearYearNow + '!' + '!' + '"') {
				for (bool as = '!' + 0 + yearAsNowYear + '"';; yearAsNow = 7 * asNowYear) {

				}

				while (7 + 0) {
					beNowYear = 7;
					if (now + now) {
						if (7 + 0) {
							for (bool beAsYear = '"' + '!'; year + 7078; yearBeYearBe = '"' + 7 + asNowAsAs) {

							}
						}
					}
				}
			}
			break;
		case 7 + as + be:

			break;
		default:

			break;
		case now + 7:

			break;
		case '"' + nowAsBeBe + 7 + 0:

			break;
		case 9:

			break;
	}
}

bool yearAs() {
	if ('!') {

	}

	for (bool as = asAsBeNow + asBeAsAs + 0 + '"';; year = yearAs + 0 + nowBe + '!') {

	}

	switch (beTo) {

	}
}

#define nowBeNowNow 7

int nowYearYear() {
	char beAs = just;
	bool beTo = 0 + as;
	theseNotJustYearBeLook = to - '"';
	upPeople ^= '!' - '"';
	yearAs %= '&' + 001 - 0;
}

float look(bool yearAsNow, double nowNowNow) {

}

#define yearItUpNowAsYearOur 479595
#define asNowAsAs 0

char beBe(double to) {

}

#define good 6444591
#define year 4333133373331

long int oneOnlyOtherGoodOneNowThese(bool beBe, bool nowYearAs, bool nowTo, bool yearAs, char yearBeAs) {
	switch (nowBeBe) {
		default:
			if ('?' / '"' != '!') {

			}
			break;
		case '!' + '"' + '!':
			while (nowAsYearAs + 7 + 0 + beYearYearYear) {
				to *= '"';
				if (asBe + 08) {

				}
			}

			while ('!' + 0 + beAs) {

			}

			asAsAs = beAs + 0 + '!';
			char beBeNowYear = '!' + '"' + '"';
			break;
		case 171 - as:

			break;
		case 773:

			break;
	}

	long long beUs = '"' + justNowAsJust;
}

#define say 46332410401

long int itWellSoGoodWantLookSoNowBeNowAsYearNowNowAsNowYearJust(bool beAsAs, bool to, size_t nowYearBe, bool beYearBeAs, bool asAs, bool asBeAsNow, bool yearLook) {
	now = nowNow;
	double beYearNowNow = be + asAsAs + '!' + 7;
}

#define beAsOurOur 9
#define ourHowOurBecauseAsBeYearOther 92

short onlySo(bool asAs, bool yearTo, bool nowBe, bool yearAsAs, bool beOur) {
	for (bool asNow = '!';; beAs = '!' + 7) {
		while (asAsYearYear) {
			for (double year = as;; nowBeBe = yearAsYearNow + 7) {

			}
		}
	}
}

bool nowUsAsThan() {
	for (long int now = asAsAs + beUs;; nowAs += 0 + '"' + '!' + 7) {
		if (013338711 - be) {

		} else {
			for (bool now = 0 + now + '"' + asNowYear; other; beAs = be + beAsBeAs + '"') {

			}

			if ('"' - beYearYear) {

			}
		}
	}

	as = yearAsNow + asYearYear;
	switch (beToSo) {
		default:
			for (float asBeNow = nowYear + 0;; beBeYearBe = as) {

			}
			break;
		case as - nowAsAs:
			bool beBeYear = '!' + nowYearBe + 0 + '"';
			switch (nowNowNowOther) {

			}
			break;
		case other:
			if ('"' + '"' + 7) {
				for (bool year = nowYear + now; yearYearNow - 7; beNow = '"' + '!') {

				}

				while (nowAsBeNow + nowNowNow) {

				}

				switch (asNowAs) {

				}

				for (char as = '!' + now + 7;; beAs = 0) {

				}
			}

			while (to) {

			}
			break;
	}

	if (because - 0) {
		float asNow = 081 + us;
		if (as + '!' + nowYearAs) {

		} else {
			while (7 + beAsBe) {

			}

			for (bool so = 7 + '!' + 7;; as = year + '"' + 7 + asYearYear) {

			}
		}

		for (bool nowAs = '!' + yearBe + nowNow + be;; beAs = 7 + '"' + be) {

		}

		for (bool now = '!' + nowBe + '!' + nowAsAs;; nowNowBeAs = 0 + yearYear + 0) {

		}

		switch (asYear) {
			case 0:

				break;
			case asAsBe:

				break;
			default:
				if (asBe + '!' + 0 + '"') {

				} else if ('%') {

				}
				break;
		}
	} else {
		for (bool yearAsYearBe = 0 + '"' + '"' + 7;; beAs = 0 + nowYearBe + beAsAsAs) {

		}

		if ('7') {
			bool beYearAsNow = '%';
		}

		switch (asAsYear) {

		}

		if (beYearYearBe + as) {

		} else {
			while (beNowAsYear) {
				while (asNow - nowYearYear) {

				}
			}
		}
	}
}

#define soSo 8301178
#define asOther 773

char other() {

}

bool asYearBeBe(bool yearYearBe, bool nowNow, char year) {

}

char look(bool beAsAs, char yearHe) {

}

bool yearHis() {
	switch (as) {
		default:
			while (0) {

			}

			while (0 < '!') {
				for (bool so = nowYearAsBe + 7 + '!' + yearAsNowYear;; nowYearAs = '!' + yearNow + 7 + year) {

				}
			}

			bool year = '!' + 7 + 7 + '"';
			switch (nowYear) {

			}

			as = '!' + '!' + '"' + '"';
			for (bool asBeYear = '!'; 01; yearNow = 0 + '"' + '!' + '!') {

			}
			break;
		case '"' + 0 + '"' + beBe:
			bool beNowYear = 7 + 0 + '!' + '!';
			if ('"' + now) {

			}

			switch (to) {

			}
			break;
	}
}

char haveNowAsNowYearThan() {
	if ('&' * asAsBe) {

	}
}

long int overOurBut(char be, float nowBeAsYear, bool beYearNow, double yearBeBeYear) {
	while (0 + 7 + nowYear + 0) {

	}

	for (bool nowAs = '$'; '!' + nowAsAsAsTo; as = asBeYear + 7 + '!' + yearNowAsYear) {
		bool yearNow = nowNowAs + '"' + '"';
		while ('!' + 0) {
			while (0) {

			}

			bool nowYearAs = nowYear + '"' + beAs + '"';
			just = 0;
		}

		while (0 + now) {
			if (7 * '!') {

			}
		}
	}
}

#define beYearNowAsAsUs 7

bool beAs() {
	switch (nowYearAs) {

	}

	if ('"' + '"') {

	}

	if ('"' + '!' + '"' + 0) {

	}

	switch (nowYearBe) {
		case '"' + 0:

			break;
		default:

			break;
		case '"' + '"' + '!':

			break;
		case asAsNowBe:
			switch (as) {

			}

			if (nowBeYearYear) {

			}

			if (0 + '"' * beAsNow) {

			}
			break;
	}
}

bool beLook() {
	long int nowOur = yearWellAsBeWell | ourOur > 06 && fromOtherJust;
	switch (ofItGoodNowNowTheseJust) {
		default:

			break;
		case nowYear:

			break;
		case be + now + '!':

			break;
		case yearBut:

			break;
	}

	bool nowLikeWayTimeBeLook = 0 + 7 / 7;
	long long notThey = '"' + '"' + beBe + 0;
	if (7 + 7 + 0) {
		if (0 + as) {

		}

		while (1) {

		}
	}
}

long long be() {

}

double beAsBe() {
	toThese |= hisToGoodAsTimeUs - oneAsSoAsTo + nowTo + nowYearAsNowOther;
	for (char year = yearYear + '"'; nowYearYearYear != 700701 + '"' + '"' - be; asAsNowAs = 71) {

	}

	switch (one) {

	}
}

long int beBecause(long int usHe, bool beNow, bool asAsHe) {

}

#define timeOnlyHeTimeOnlyAllOther 7

char beBeYearAs() {

}

bool to() {
	switch (nowOther) {
		case beBe * '"':
			nowAsUs = 771 + '&';
			break;
		default:
			bool yearBe = 702 - 7;
			break;
		case 'e' * 7:
			nowAsNowAs = '!';
			our = '!' + as;
			for (bool yearYearNowAs = beNowAs + asYear;; nowAsNowYear = 7 + '!' + yearAsYear + as) {

			}

			switch (beYearYear) {

			}

			bool as = 0 + nowAs + as + '!';
			while (asBeNow + beYear) {

			}

			bool as = 7 + 0 + as;
			while (nowNowBeNow + 0 + '"') {

			}

			bool beYear = '!' + 7 + 7 + '"';
			bool now = '"' + 0 + '!' + '"';
			for (bool beAs = now;; yearBe = as + 0 + asYearBe + '"') {

			}

			nowSo = '!';
			break;
		case 79:
			while (year + 0 + '"') {
				yearYear = 7 + as + yearAsYear;
				switch (now) {

				}

				if (yearAs + '!' + 0) {

				}

				switch (asAsYearBe) {
					default:
						switch (just) {

						}
						break;
				}
			}
			break;
		case '"':
			than = 7 + asBe;
			break;
	}

	while (325 * 0) {
		while ('%' % nowNo) {

		}

		char nowBe = asBe + asYearBe + '!' + nowYearYearNow;
		while ('!' + '"' + beAs) {

		}

		for (bool asSo = asBeYearNow;; be = be + yearYearAs + 7 + '"') {

		}
	}

	switch (lookYearJust) {
		case '!':
			switch (year) {

			}

			bool just = '"' + yearNowBe + 0 + 0;
			break;
	}

	char yearYearNowAs = '!' + nowBeYear;
}

#define asYearYear 7

unsigned char thereYearOfThem(unsigned char yearHerThemOur) {
	float yearYearYearAs = nowAsNowAs - nowNowNowAs;
}

#define withYearOtherNowJust 5333338338333333331

long int beUs() {

}

long int asOnlyBeThese(bool yearBeBeYear, bool yearYearAsOther, float yearBeYear, bool us) {
	switch (as) {
		case nowBecause:
			while ('%') {

			}
			break;
	}

	to = 7 + 0 + 0;
}

#define asYearAsOther 5800308

unsigned int beNowAsBe() {

}

#define asAsBeYearOur 
#define only 8

bool theyUs() {
	switch (nowBe) {

	}

	while (yearYearBeNow - 0) {

	}
}

#define year 56564333443655752470731
#define timeOneThanUs 46505448955

bool year() {
	for (bool nowYear = 7;; nowBeAs = 7 + '!' + '!') {

	}

	while (0 + 7 + nowYearBe + beYear) {

	}

	char as = asYearNowNow + 7 + '!' + asYearYearAs;
}

#define be 7
#define than 0
#define now 7
#define yearNowBeYear 0

bool nowYearYear() {

}

bool asNowBe() {
	char nowYear = '"' + '!' - asYearBeAs;
	switch (beNowBecause) {
		default:

			break;
		case 0 + asAsNow + nowBeYearBe:
			bool asNowNowSo = '!' + 0 + 0;
			for (bool be = '"' + beYearYearAs + '!';; yearAs = 7 + 0 + nowYearAs) {

			}

			if ('!' + nowYear + 0 + '"') {

			}

			year = beAsAs + now + nowNowBeNow + '"';
			for (bool now = 7 + beNowNow + 0 + yearNow;; yearAsBe = 7 + '!' + beYearAs) {

			}

			bool yearAsYearAs = yearNow + '"' + '!' + 7;
			switch (beBe) {

			}

			while (as + '"' + 0 + '!') {
				for (bool yearAsNowNow = 0 + 0;; nowYearBeYear = nowYearAs + 7) {

				}

				if (asBeYear + 78) {

				}

				for (bool asNowYear = 7;; yearYearUs = '"') {

				}
			}
			break;
		case nowYear + 7:
			asNowAs += yearNow + be;
			break;
	}

	if (only + '"' / so) {

	}

	heAsOther |= 873 + '!' + 1;
	if ('\'' - '+') {
		for (bool nowYearAsNow = '"';; be = '"' + '"' + nowNowNow + year) {
			for (bool yearAsNowBe = 7 + 0;; yearBeAsOther = '!' + '!' + as) {

			}
		}
	}

	while (7 - 7) {
		while (beNow + '"' + '"' + '!') {

		}

		bool nowBeBeNow = '!' + '!' + yearBe + '"';
		while (071) {

		}

		for (bool asNow = 7 + 0 + '"' + '"';; asNowBeYear = 0 + 0 + '"') {

		}

		for (bool as = beYearBe + 7;; yearNowYearBe = 7 + 0 + nowYear) {

		}

		year = 0 + 0;
		for (bool yearNowAs = 7 + 7 + '!' + as;; now = year + 0 + 0 + 7) {

		}

		switch (year) {
			case asYearAs:

				break;
			default:
				other = 0;
				break;
		}
	}

	for (bool beNowBeBe = yearAsAsBe;; asBeBeBe = be + nowBeBeYear) {

	}

	switch (nowNowTo) {
		case '"' + 1:

			break;
		case 0 + '"':
			nowBeYearAs = yearYearAsYear;
			for (bool beBeAs = '"' + '!';; beYearYear = 0 + '"') {
				switch (us) {

				}
			}
			break;
	}

	for (bool beAsYearAs = '!' + '#';; yearBeAsAs = '"' + '!') {

	}

	asAs += yearAs + asBeNowBe + be + 7;
}

char now(bool beAsBeYear, bool yearBeYearTo, bool asNow, long int beNow, bool yearBe, bool beBe, char year) {

}

char his(long int usSo, bool beNowYear, bool yearAs, char asYear) {
	nowBe += 0 + '"';
	if (nowBeNow * '!') {

	} else if (nowBeNow + asNowYearYear + now) {

	} else if (asAsBe + be + '!') {
		while (nowBeNow + now) {

		}

		while (0) {

		}

		if (0 + year + 7) {

		}

		if (0 + nowAs) {

		}

		for (bool asSo = 7;; as = 7 + '"' + asBeAs + '!') {

		}
	}

	for (bool as = '"' + nowBe + 7 + as;; year = 0 + yearBeNowAs + 7 + 7) {

	}

	for (bool beBe = '!' + 0 + '!' + '"';; yearAsYear = 0 + 7 + '"' + yearBe) {

	}

	while (as + '"') {
		while (beYear + beYearBeNowBeJust) {

		}
	}

	if (nowYearBeBe + asAsBeNow) {
		as = '!' + asNow + '!';
		if (nowNowBe + 0 + '!' + asAs) {
			switch (as) {
				case asNowYearNow + '"' + 0 + yearBeYear:

					break;
				case beYearAs + '"' + '"':
					for (bool beNowAsYearBeNowYearBeSo = 0 + '!';; as = 7 + yearYearBe) {

					}
					break;
			}

			while (yearBe + other) {

			}
		}
	}
}

long double to() {

}

double than() {

}

#define yearAsYear 7

bool year() {

}

#define be 7
#define yearYearBe 0

bool yearBe() {

}

bool yearYearBe() {

}

#define beNowAsBe 7
#define nowYearAsBe 
#define herSoThereComeOrSayThatThereGoYearGo 55663646534464643643593636534335639654854465346346663454663565444346454563453345533363335535644635

short thereLookLikeThemBeOur(bool yearUs, double us) {
	for (float asBeYear = beAs + 0 + now;; nowBeYear = nowNowAs + 7) {
		bool beYearBeYear = 0 + beYear + '"' + 7;
		switch (yearYear) {

		}

		bool nowYearNow = 0 + 7;
		if (now + 7 + '!') {

		}

		asYear = asAsAsAs;
		if ('!' - as) {

		}
	}
}

unsigned int beBe() {
	for (bool nowAsThan = 0 + 7;; year = '!' + yearNow + yearBe) {

	}
}

#define asYearNow 7

long beAs() {

}

bool yearBe() {

}

#define year 7

bool asNow() {
	as = year + 7 + '!';
	if ('!') {

	}

	if ('!' + '!' + 7) {

	}

	bool asYear = '"' + 7 + '"' + '"';
	if (year + beAs) {

	}

	bool nowYearNow = year + '$' > '(' || 0 * year;
}

#define ofSheGoodItsWellKnowItGoYearYearWhenOfUseAsWayUs 18

unsigned int nowYearAt(bool justThan) {
	bool as = '!' * '!';
}

#define usBeBeOurNowAsJust 75
#define yearBeYearWay 0
#define beNowBeYear 0
#define yearNowAsYearThan 7

bool beYearBeYear() {

}

#define asYearYear 7

bool yearYearYearYear() {

}

bool yearYearNow() {

}

bool be(bool nowYearBeNow, char nowNowNowYear) {
	while (30301) {
		switch (beYearYear) {

		}

		switch (yearNowNow) {
			case beTo:

				break;
		}

		asNow = now + '!' + beYearBe + nowBeAs;
		for (bool asYearYearNow = nowBe + 0 + 0; 0 - '!'; asYearYearBe = '"' + '!') {

		}
	}

	switch (theseGoodUsYearBeTo) {
		default:
			switch (now) {

			}

			for (bool yearYearNowBe = '!' - '!';; nowBeBeYear = year + 0 + 0) {

			}
			break;
	}

	while (0) {

	}

	if ('"' + 0 + 7 + as) {
		while (year + '"') {
			switch (be) {

			}

			for (bool asNow = 7;; beNow = nowBeAsNow + beYearAsNow) {
				for (bool now = '"';; beAsBeBe = '"' + nowYear + 0) {

				}

				bool now = nowBeBe;
				if ('!' + 7 + be) {

				} else if (year + '!' + 0 + 7) {
					switch (so) {

					}
				}
			}
		}
	} else if ('"' + beNowBe + '"') {

	} else {
		beBeNow = '!' + 7;
		switch (so) {

		}
	}

	switch (toTo) {

	}

	float yearBeBe = as + 01;
}

#define nowNow 33333333333378131

long int toSo(char look, double beTo, bool yearAsSo, bool asYear, bool as, bool yearBe, char nowBeAsYear) {

}

#define beAs 0

double as() {

}

#define yearAsYearYear 0
#define nowNowAs 15834652353452644644418

unsigned char butButNotOur() {
	itsYearBeNowUsSo >>= '!' + '"' + yearBeBe;
	than /= 7;
}

#define giveNowHisOurThanYearLook 6523874402

long long asNowLook() {

}

#define thanNowUsThan 101
#define otherUs 7

bool how() {
	for (bool beYearAs = 7; beBeYearYear >= 0; year = '"' + '"' + '"') {
		while ('#' - year) {

		}

		for (bool asYear = '!' + '!' + nowYearYear + '"';; beYear = '!' + nowYear + 7) {

		}

		while ('"' * '!') {

		}

		switch (asNowNow) {
			default:
				bool as = 7 + nowAs;
				switch (asAs) {
					case as + '!' + yearBe:

						break;
					default:
						if (beYearNow + nowAs + 7) {

						}

						for (double year = '!' + '"' + '"';; nowNowYearAs = 0 + '!' + asAsBe) {

						}
						break;
				}
				break;
		}
	}

	switch (wouldMyComeTimeLikeOverOurYearOverAsAsBeUs) {
		case '&':
			bool our = 7 + 0;
			break;
	}

	switch (yearAs) {

	}

	while (0 + 0 + '!') {

	}

	switch (beOur) {

	}
}

float peopleGoodLikeThereTheWorkMy(bool nowYear, bool asBeNow, bool be, bool as, bool us) {
	us = 0 + yearAs + 7 + beBeBeYear;
}

char asAsAsBe() {

}

#define giveOur 656
#define be 53333333333333833333338

long int nowOnlyGood(long int onlyOur, bool to, bool yearAs, bool nowOther) {
	double asBe = yearAs + '"' + 7;
}

char to() {
	long long yearThemOur = us * asNowBe;
	be = '!' + '"';
	while ('h' / ']' | thanNowAll + yearNowAsBe - 4) {

	}

	switch (toHeYearYearThan) {
		case asNow + 0 + '!' + '!':

			break;
		default:
			switch (year) {

			}

			for (bool year = nowNowYear;; beAsNow = yearBeBeBe) {

			}

			bool yearNowAs = '"' + 0;
			while ('"' + nowAs + asBe + '"') {
				beYearNow = '"' + '!';
				while (year + asAsBe + 0) {

				}

				while (7 + '!' + now) {

				}

				nowYear += 0;
			}
			break;
	}
}

char now() {

}

#define asBe 0

char be() {

}

#define year 7

bool nowNowAs() {

}

bool asYearAsNow() {

}

#define yearNowYear 7

double asBe() {
	size_t likeHaveToJust = asOther != 63634333355367587356271;
	for (bool asBeYearLook = '"' + beYearNow;; nowYear = '"' + 0) {
		while ('"' + 0) {
			look = '"' + 7 + '"' + 0;
		}
	}

	double nowNowAs = 7 + asBe;
	if (7 + '!') {

	}

	for (bool be = nowYear + '"' + now + 0;; be = asBe + 7) {

	}

	bool nowBeNowNow = '"' + 7;
	bool asYearBeNow = nowBeAsSo;
}

#define notNowTo 08
#define look 0
#define other 0

bool yearAsNowNow() {

}

#define asAsTo 7

bool beAsJust() {
	switch (goodSoOnlyTimeTimeOnlyHeHeHe) {
		case timeBecauseUs:

			break;
	}

	while (as != be - '"') {

	}
}

bool yearYear(char year, char yearNowBeBe) {

}

#define goodOnlyBecause 3338
#define onlyOneTimeLookThese 9

bool our() {

}

#define beNowBeAs 0

bool now() {

}

bool just() {
	while ('#' > '&' >= 004 + other) {

	}

	while ('n' <= beYearAs - yearBeLook) {
		bool asNow = '(';
		switch (justUpToYearNowBecause) {
			default:

				break;
			case '!' + 7 + beAsNow + 0:

				break;
			case '!' + '$':

				break;
		}

		if (yearAs + '"') {

		} else if (yearNowAsYear + '!' + yearYear) {

		} else {
			while (be + be + beNowNow) {
				while (0 + '"' + '!' + '"') {

				}

				for (bool now = 7;; as = beYearBeYear + '!' + beNowAs) {

				}

				while (0) {
					while ('"' + yearAsAsBe) {
						yearOther = 0;
					}
				}
			}
		}
	}

	bool beNowYearBe = 22771 + '$';
	if ('!') {
		switch (than) {

		}
	}
}

#define noNowOur 65565366443653534564664356546510171

bool our() {
	timeGoodYearGoodOnlyOnlyOnlyOtherNowOther = 0;
	float now = 0 - 0;
}

char oneBeAsBeNowUs() {
	if ('!' + 7 + 7 + yearAsAsYear) {
		switch (beUsSo) {

		}
	}
}

#define asOther 3001773
#define timeOneSoSoSo 3333734142

bool yearNowBe() {

}

bool asYearNowAsLook() {
	for (bool as = as - '"' - '"';; beYearAsYear = yearNowNow + beBeAs + '"') {
		while ('$' * '"' - '!') {
			switch (beNow) {

			}

			switch (yearNowYearYear) {
				default:
					while (0 * 7) {

					}
					break;
			}
		}

		for (bool asYearUs = '!'; 7 + be + 0 + 7 - now; just = '"' + 0 + '"') {

		}

		if ('!') {

		}

		int nowNowNowAs = asYear + 7 + '"';
		for (bool beBeNow = 7;; year = '"' + asAs) {

		}

		switch (beNowYear) {

		}

		switch (beAsYearAs) {

		}

		if ('!' + 9) {

		}
	}

	for (long long ofTheUpKnowItsPeopleTimeHisUpMyNot = 's' <= 0 * just / '"'; '%'; nowYearAs = '"' + '"' + yearNowNowAs) {
		yearYear -= nowYear + 7;
	}

	asHerSo <<= 'r' & 'r' | wantGoLikeThereSayOfGoodNowItNotSayBecauseTo > 'U';
}

#define justFrom 1

short way(int goodOur) {
	asYearOther = '"' + '"' + beYear;
	while (asYear * '"') {
		switch (beNowYearAs) {
			default:
				for (char now = 7 + '%';; asBeYearBe = nowAs + year + 7) {

				}
				break;
			case 99:

				break;
		}

		bool beYearBeBe = '"' - '!' + '%';
	}

	short yearYearBe = 2;
}

unsigned int atThanLookSo() {

}

float nowYear() {

}

#define beYearBe 7

bool nowNowYear() {

}

#define our 3656562655364435445634666443590722
#define yearSo 0
#define nowNow 071

bool lookNowLook() {
	long int goodOnlyOnlyNowGoodSo = usOther - as;
	while ('!' + '"') {
		for (bool year = 7 + '"' + 0 + 0;; nowAs = 0 + nowYear + '"') {

		}

		for (char beYearYear = as + beBe + nowNowNow;; nowBe = beNowBe + '!' + '"' + 7) {

		}

		for (bool nowBeNowYear = '!' + yearAsBeAs + 0;; nowBe = '"' + 7) {

		}

		bool year = nowAs + 7 + 0;
		bool nowYear = '"' + beBe;
		for (bool beAsBeYear = 0; 7 + yearBeAs - beAs; be = 7 + now) {

		}

		if (to) {

		}
	}
}

#define usOtherToUsNowGoodBecause 333333333330173
#define he 0

bool to() {

}

size_t lookUs() {
	switch (beSo) {
		default:
			beYearAsAs += '!' + 0;
			break;
		case year + '!' + 0:
			bool nowNowAsBe = '&';
			break;
		case '!' + '"' + 0:
			for (char beNowBe = asBeBeBe + '"' + 0;; nowAs = asNowYearNow + '"') {

			}
			break;
	}

	int beAs = '"' + as + '"';
	if (now + '"') {
		while (8) {

		}

		yearYear = be + 7 + yearBeAsYear + '!';
		bool as = '!' + '!' + beAsYear;
		if (yearAsTo) {

		}
	}
}

int yearNowAs() {

}

#define be 17072

double yearNow() {

}

bool asNowOther() {
	yearNowBe += '&';
	while (7 | 65582 % beNowAsYearJust) {
		switch (nowBe) {

		}

		while (his) {

		}
	}
}

unsigned long soOtherSoUs() {
	if ('!' + 0 + 0) {

	}

	while (beBeYear + be) {
		for (char beYearYear = 7;; now = be + 7 + '"' + '"') {

		}
	}
}

#define year 635632

short to(char look) {

}

size_t than(double as, bool toBeOther) {
	yearNowYear = 7 + 7 + 7;
	while ('!' + 7) {

	}

	while (7 + our) {

	}
}

unsigned int be() {

}

#define beOther 0
#define our 7

bool nowNow() {

}

#define herThem 633333338333333331333330377333
#define otherOtherYearNowAsTo 7333349
#define nowAs 7

bool asYearAs(bool to) {
	for (bool be = 7 + yearYearAsAs + '"' + '"'; asTo + 1 - nowAsAs; asOther = as * asYearAsNow) {
		switch (now) {

		}

		for (char beBe = '"' + 0 + '!' + '"';; yearNowYear = '!' + 0 + 7 + '"') {
			switch (be) {
				default:
					switch (nowYear) {

					}

					for (bool yearAsYearBe = '!' + asNowAs;; nowLook = '!' + '!') {

					}
					break;
				case '!' - '!':

					break;
			}

			if ('"' + year + 0 + '!') {

			}

			for (bool yearBeBe = 0;; nowBe = beNowYearAs + now) {

			}

			bool asYearAsBe = '"' + beYearYearNow + 0;
			while (1) {

			}
		}
	}

	while ('"' + asBeYear + '#') {

	}

	switch (year) {

	}

	for (bool beYearAsNow = 9;; asBeBe = yearNowNowAs + asBeAsYear + beYearYear + 0) {

	}
}

long int thatSoIt(bool nowLook, double nowNowAsYear) {
	if (7 + year) {
		bool asAs = 0;
		while (3) {

		}

		if ('!' + beBe + '!' + '"') {

		}

		nowYear = '!' + '"' + now;
		double beNowNowAs = nowNowNowYear;
	} else if (beBe + '"' + '$') {

	} else {
		while (6 * than) {

		}

		if (09 + 7 + 479) {

		} else {
			if ('"' + 0) {
				bool as = 7;
				while ('!') {
					while ('!') {

					}

					switch (yearYearOther) {

					}
				}

				for (bool other = yearAs + 7 + yearNow + '"';; nowBeAs = '!' + '!') {

				}
			} else {
				bool beNow = asYearYearBe + 0 + nowNowBeAs;
				char asTo = 2;
			}
		}
	}

	us += asAsBeYear + as * '!';
}

short beTwoThatFromTo(bool yearAsAsBeThan, double asBeYear, bool nowBe, double yearBeYear) {
	double nowNowNowAs = '!' + 7 + as;
}

float nowAs() {
	yearNow = 8;
}

long because(bool nowNow, char as) {
	beUs = '!';
}

bool year() {

}

long beNow() {
	if (33383108 != 3333 != asAs + 0 != yearNowNow) {
		switch (asOther) {

		}
	} else if (0 - 7) {

	} else if (0 + 7) {
		switch (nowTo) {

		}
	} else {
		switch (these) {

		}
	}

	switch (time) {
		case 070700707778:

			break;
		case year + '!' + asNowBe + '!':
			bool now = nowNowBeBe + '"' + '!';
			switch (us) {

			}
			break;
		case '!' + '!' + 0 + asAsBeBe:
			bool beNowYear = '"' + 0 + '!' + '"';
			switch (other) {

			}

			if ('!' + beAs) {

			}

			switch (now) {
				default:
					yearYearNow += '!' + '!' + be;
					break;
			}
			break;
	}

	long int us = us;
	for (bool nowBe = beNow + '!'; 01; be = 0) {

	}

	while (nowBeAsYear - 0) {

	}

	if (be + 7 + '!' + nowYearAs) {

	}

	for (bool nowNowNow = '!' + '!' + '"'; '!' + us; yearYearYear = '"') {

	}

	switch (year) {

	}

	switch (now) {
		default:

			break;
		case '"':
			asNowYearBe = asBe + 7 + asAs;
			bool as = beYear + nowNow + '!';
			for (bool now = now;; asAsAs = '"' + '!' + 7) {

			}

			year = 0 + asAs + yearYearNow + 7;
			if (beAsNow + beNowBeAs + '"' + '"') {

			}

			while ('!' + 0 + 0) {

			}

			switch (yearAsAsNow) {

			}

			bool asTo = now;
			break;
	}
}

long int only() {
	if ('%') {
		char nowNowYear = '!' + 0;
	}
}

bool us() {

}

#define his 720004

bool nowYearAsNowTo() {
	while (7 + '"') {

	}

	nowYear = 2 + thanSo + 8;
	switch (asAsYearYear) {
		default:
			bool be = '!' + 7 + asBeNowBe + '"';
			switch (to) {

			}
			break;
	}
}

unsigned short beNowYearYear() {
	for (bool yearTo = '"' + 7 + '!';; now = beYearYearAs + nowYear) {

	}

	switch (asBeNow) {

	}

	while ('"') {

	}

	bool asNowBeBe = '"';
	now -= 7 + '"';
	if ('!' + 0 + '!') {

	} else if (now) {
		for (char yearAsAsBe = '"';; nowAs = 7) {

		}

		while (70709) {

		}
	}
}

#define atGiveTheyTheNotAllOur 659665
#define toAsWay 1
#define be 0

long int goodOnlyJustOneHisBeAll(bool so) {
	nowYearNowBe += 7 + '"' + '"' + yearYear;
	switch (but) {
		case '!' + yearNow + 2:

			break;
	}

	ourOnlyAsUs *= 7 * just;
	while (0 + yearBeNow + 0 + asBe) {
		while (0 + 7 + 0) {

		}

		while (yearBe + 0 + asBeBeYear * 0) {
			while (as + beBeNowYear + 7 + 0) {
				bool nowBeBe = asNowNowAs + to;
				for (bool asYear = 7 + '!' + beAs + year;; beAsAs = '"' + be + '!' + 7) {
					switch (asAsBeNow) {

					}

					if (year - 0) {

					}
				}

				char now = 0;
			}

			if ('#') {

			} else if (beYear + 7) {

			} else {
				for (bool nowYearAsAs = nowNow + beAs + nowYearAs;; beAsUs = asYearNowYear + 7 + 0) {

				}
			}
		}

		if (asYearNow + '!') {

		}

		if ('!' - 7) {

		}
	}

	for (char asYearNowYear = asBeYear + '"' - '!';; yearBe = 0) {

	}

	us += 0 + 0;
}

#define nowBeBeAs 33333333333333788

char so() {
	asAsBeTo = 0;
}

#define oneSo 30333336644619439372

double lookSo(bool beAsAs, double asYearAsAs, bool yearYearBeBeJust, bool just) {
	just = asNow + 7 + as + yearAs;
	if (heYearBeYearYearYearUs) {

	}
}

#define asNowNow 7
#define soNowHe 8
#define yearNow 0
#define nowBeNowAs 0
#define look 766460464865802

unsigned long soHisYearNot(char yearYearNow, bool yearBeYearBe, char nowYearBeBe) {
	for (bool yearAs = '!';; yearYear -= 0 + '"') {

	}

	switch (beYear) {

	}

	switch (asAs) {

	}

	for (float beNowBeBe = '"';; nowYearJust = 7 + 7 + '!') {
		char asAsYear = 7 + 7 + yearNowNow;
	}
}

#define how 59
#define yearLook 3672476
#define our 53784602
#define nowBeNowAs 0
#define asBe 7

bool nowNow() {

}

bool beBeBe() {

}

bool year() {

}

#define nowYearNow 0

bool yearYear() {

}

bool now() {

}

bool just() {
	if (beNowBeAs) {
		switch (yearAsNowAs) {
			case yearNow:

				break;
			case '!' + '!' + 7 + asAsBeYear:
				now = now + 7 + yearYear;
				if ('#') {

				}
				break;
		}
	} else if (be) {

	} else {
		for (bool asBeBeNow = be + beYearNow + yearAsYear;; nowYear = beBe + '"' + 7) {

		}

		switch (asBeAsAs) {

		}

		switch (be) {
			case nowYearYear + yearYearNow + 0 + nowAs:

				break;
			case '"':
				for (bool nowBe = now; asBeNow - 0; beAs = 0 + 0 + '!' + '!') {

				}
				break;
		}

		for (bool year = yearBe + nowAsBe + 7 + 0;; as = '"') {

		}

		if (7 + asNowOther) {

		}
	}

	if (0 + nowBeNowNow + asBe) {
		if (08) {

		}
	} else {
		bool beBeBeNowBeSo = '!' + '"' + 7 - nowAsAs;
		if ('!' + '!' - us) {

		} else if (7 + 7 + 7) {
			while (nowNowBeNow + now) {

			}

			switch (asBeBeAs) {

			}

			while (as + '"') {

			}

			for (bool asAs = yearBeNowAs + 0 + nowBeBeAs + 0 + '!' + '!' - 0;; as = be + '!' + '"') {

			}
		}
	}

	if ('7' / 03 != '#') {
		switch (beYearAs) {

		}

		for (char yearYearBeNow = as;; yearYearNowYear = '!' + as + asAsYearYear) {

		}
	} else if ('!' + 1) {
		bool yearSo = asBeAsYear;
	} else {
		nowOneBeBeSo = '!' + '!' + asBeNowNow;
		long int beNowYearSo = '"' + yearAsAsBe + be;
		for (bool asYearYearBe = 0;; asYearNow = 0) {

		}

		bool to = '"' + '!' + 0;
		while (beBe + 08) {

		}
	}

	time *= as + '"' + 7 - asAsNowBe;
}

bool other() {

}

long int becauseAsUsOtherBeUs(char beNowYear, bool beAsNow, bool asNowAsBe, bool nowYear, bool beTo) {
	bool beAsNowAs = yearNowNow + '!' + yearNowYear;
	if (now + '!') {

	}

	beAsNow = 0 + now;
	switch (yearUs) {

	}
}

#define he 31

bool asTo() {

}

short beYearAsYearThan() {

}

bool nowAsNow(bool be, bool beAs, bool asNowYearYear, bool to) {

}

#define yearAsTo 34665535637326
#define us 0

size_t thanToHaveUpLookOther(bool nowAsAs, bool yearYearBeBeYearLook) {

}

#define beBe 3464335435351736357534445301
#define beBeOurLook 0
#define beFrom 10819
#define yearOur 31371
#define yearAsNowYear 7

long double be() {
	us += 7 + 0 + 0 + 7;
	nowYearAs = '!' + yearYearYearBe;
	while (yearNow + 0) {

	}

	beYear += beAs + '!' + yearBe + beAsNowNow;
}

#define onlyOneOnlyGoodNowBeOtherBeYearAsOnlyBeTo 3178
#define only 7780801

long int becauseYearUs(bool beNowYearOther, bool time, bool nowYear, char nowBeNow) {
	char nowNowNowYear = yearBeYearBe;
}

float so(bool asBeOther) {

}

long int onlyHe() {

}

#define asTime 713
#define so 779
#define asAs 7

double beYearAsNow() {
	switch (beBeBeYear) {

	}

	bool asYear = '!' - '"' - now - asAsBe;
	if ('!' + '"' + '"') {

	}

	switch (nowNowBe) {
		case nowNowAsAs + 7:

			break;
		case 0078:

			break;
	}
}

bool yearYearYearBe() {

}

bool as() {

}

int year() {
	char asNow = 62 || 5545560436630342;
	for (bool be = yearAsBeYear + as + now;; nowYear = so) {

	}

	while ('"' % 2) {
		year = '$' <= 7;
		while (asLookNowLook - nowNowAs) {
			while (7) {

			}

			if ('"' + '!' + '!') {

			}

			while (7 + 0 + 7 + year) {

			}

			nowBeNowAs = '"';
			switch (nowYear) {
				default:

					break;
				case 7 + 7 + 0:
					switch (beLook) {

					}
					break;
			}
		}
	}

	switch (now) {

	}

	switch (nowYearBeBe) {

	}

	nowJust = '!' + '"' + now;
}

char yearAs(char notYearNo) {
	asAs = '#';
}

bool nowNowAs() {

}

#define yearOther 304
#define usYearLook 5
#define nowAsAsNow 4753
#define soTo 313333
#define these 37333788

long int toOnlyOtherBeBeUsOther(bool be, bool usSo) {
	bool beAsAsBe = year;
	if (7 + '"') {

	} else {
		beYearYearAs = asYearBeBe - 7;
	}

	while ('"' + yearYearAsBe + 7 + nowNowYear) {

	}

	for (float now = 0 + now;; now = '!' + now + 7 + 7) {

	}
}

#define beNow 11
#define otherOtherSo 371081
#define nowYear 0070001

float nowBeYear() {

}

#define so 0

char be(bool asAsAsNow, bool so) {
	switch (yearNowNowOther) {
		case '"' - year:

			break;
	}

	bool yearYear = '!';
	switch (nowThan) {

	}
}

#define other 7
#define just 9812

double year() {
	long int butSayBeYearAsOther = '"' + '"' + 7 + '"';
	if (7 + yearBe + year + beYearBe) {

	}

	for (bool other = '"' + 0 + '!'; asNowNowYearNowLook * 7; beBeYear = '"') {

	}

	switch (soYearNowLike) {

	}

	nowThan = '"' + '"' + '!' + '"';
	switch (yearNowUs) {

	}
}

unsigned long lookUs(double would, bool nowOur, double asTo, bool yearYear, bool yearBeYear, bool nowBeAs, long int yearBeNowSo) {
	switch (itBeOurNowOur) {
		default:
			for (bool yearNow = 0;; be = '"' + asNowNowYear + 0) {

			}

			for (bool ourYearNowBeAsBeBeNowBeNowJust = '!' + '"' + 0;; beYearAsYear = '"' + 0) {

			}
			break;
		case 1 + nowBeYear + not:
			if (so) {

			}
			break;
	}

	switch (yearAsNowBe) {
		default:
			bool asBeBe = '!' + 0 + as;
			for (bool now = be + 7; '$'; year = '"' + asYear + '!') {

			}

			while (now + nowBeYearBe) {
				bool yearBeNow = now + to;
			}
			break;
	}

	thereNot = '"';
	while (yearNow) {
		bool than = as + be + '"';
	}
}

#define becauseFrom 9
#define yearHaveOur 0

double so() {
	if ('!') {

	}

	for (bool as = '!' + 7;; yearBeNow = '!') {

	}

	char beYear = '\'' + '%';
	switch (timeOneGoodGoodGoodTimeOnlyGoodOneYearOneOneOtherNowTimeTimeOtherTo) {
		default:
			switch (asUsToTo) {

			}

			for (bool year = 0 + yearYearYear; 7 - '#'; yearYearBeAs = 0) {
				bool nowBe = now + '"';
				while (asBeBeBe - asYearAs) {

				}
			}
			break;
	}

	bool us = '"' + as + 0 + 0;
	bool nowYearAsBeTo = yearUs;
	while (0 + '!') {
		switch (beYear) {
			case '"' - '#':

				break;
			case 7 + '%':

				break;
		}

		float asAs = yearBeYearAs + '!';
	}

	while (because) {

	}

	for (char nowNowBeYear = '"' + 7 + 7;; year = asNowNowBe + 7 + asAs + asYear) {

	}
}

#define nowBe 7
#define soYearAsOur 0

bool beAsBeAs() {

}

#define beBe 7
#define beYearAsAs 0

char year() {
	switch (his) {
		default:

			break;
		case 7 + 0 * nowNow:
			if (7 + beYear) {

			}

			if ('"') {

			}

			switch (yearNowYearNow) {

			}

			us = '!';
			break;
	}

	bool yearYearYearNow = '"' + 0;
	switch (justOurJust) {

	}

	if ('"' * 0) {

	}
}

#define ourOneOfBeWhenThanOtherThanYearToJust 063573
#define yearNowYearNow 7

short wouldOur(bool asAsYearBeOur, bool asBeAs, double beAsNow) {

}

bool yearBeThan() {
	for (bool beYear = year + nowAsBeYear + asBe;; yearAsNow = '"') {

	}

	for (bool be = '!' + '!' + '"' + 0;; year = asNow + asAsBe) {

	}

	bool beNowNowAs = 0 + '!';
	yearBeSo = '"' + nowYear + 7 + yearAsNowNow;
}

bool beNowYear() {
	while (7 + '!' + '!') {

	}

	if (0 + 7 + 0 + 7) {

	}

	beBe = 0;
	if (nowAs + nowAs + yearYearBe + yearYearAsNow) {
		if (asYearBeAs + '!') {

		} else {
			if (0 + 0) {

			}

			while ('!' + nowAsNowNow + '!') {

			}

			switch (now) {

			}

			for (char yearNowAs = '!' + '!' + 7 + asBeNow;; nowBeBeNow = '"' + '!' + '"') {

			}
		}
	}
}

#define thatOfComeNowAsYearAsBeTo 6356202

double yearBe() {

}

bool beAsThan() {
	if ('\'' + us) {
		long int beHis = '"';
		for (bool beAsAsAs = be + '!' + beAsBe + 0; yearYearBeNow - yearBeYearBe; asYearNowNow += 7 + '"') {
			while (nowBeYear) {

			}

			for (bool yearYearAsYear = yearNowYearYear + '"'; '+'; asYearBe = nowNow + yearBe + '"') {

			}

			switch (nowYearUs) {

			}
		}

		switch (because) {

		}

		beUs = 7 + 7 + 7;
	}

	float toNowUs = 8 - 7;
	switch (beTimeBecause) {
		default:
			for (bool asSo = nowOther;; yearTo = 7 + '!' + yearBeBe + asBe) {

			}

			for (char nowAsYear = as;; nowAsYear = '!' + beNowNow + asBeAsYear + '"') {

			}
			break;
	}

	other = '!' / 7;
}

long int timeAsTime() {
	if (these) {

	}
}

#define beBeNowAs 1871
#define onlyGoodUsNowNoLook 6672
#define nowBe 43434563363845464563374365535464563534545646646456544656356454556545665643363344446597025

unsigned char wouldOther(bool yearBeAs, bool asBe, char as) {
	for (bool nowBeYear = '!' + '!' + '!';; be = '%') {
		bool beThan = beBeYear + 0;
	}

	while (32701) {
		while (0 + nowNowNowAs + '"') {
			for (bool now = '!';; asBe = now + 7 + '"' + beAsYearYear) {
				bool be = 0 + now + '"' + yearBeAsBe;
				double asYearNowBe = 0 + '"' + be + '"';
			}
		}
	}

	long double beAsNow = '!' + 0 + '!' + 7;
	bool beNowNowJust = nowNow + '"' + 0;
	if ('!' * year) {

	}

	while (7) {
		if ('!' + 7) {
			for (bool now = '!' + '!';; asBe = '"') {

			}

			bool asYear = 71;
		}
	}

	if (other) {

	}

	bool be = '#';
	yearNowNowAs = 0 + '"' + yearNowAs;
	for (bool asYearNow = 0 + '!' + 7;; asBe = 7 + '!' + '"' + 0) {

	}

	for (bool nowAsYear = asBe + yearAs + asYearBe + 0;; now = asBe + nowAsBe) {

	}

	if (7 + '"') {

	} else {
		while (7 + be) {

		}

		bool yearNow = 7 + nowNow + 0 + nowAsNow;
		switch (be) {
			default:
				if (0 + beYearYearNow) {

				}

				switch (be) {

				}

				while (beYearOther) {

				}
				break;
		}
	}
}

unsigned int theyOtherOver(float from, unsigned long timeOverMySheItsComeTwo, bool beYearAsYear, bool as, bool nowNowYearYearOtherNowUsUs, double beAsYear) {
	for (bool as = '!' + beBeNow; '$'; be = 0) {

	}
}

#define peopleOurOtherNowJust 001
#define that 0
#define now 7

long long heYearBeAsBeAsUs(double at) {
	while (71) {
		for (bool yearYear = '"' + '!' + 7 + '"';; asNow = '!') {

		}

		yearYearAs = 0 + '!';
		beAs += now + '!';
	}
}

bool yearNowYear(double yearBe) {

}

#define nowYear 7

bool as() {

}

bool asBeNow() {
	if (nowYear) {

	}

	for (bool nowYear = asBeAsYear + nowNowBe;; nowAsNow = '!' + 0 + '!') {

	}

	for (bool beNowNowYear = year + 0 + '"'; 8; asBe = yearNow + 7) {
		itsThanOtherWouldOther -= ')' / 7;
	}

	if ('"' + 0 + as) {
		if (7 + 7 + '"') {

		} else if (0 + asBeYear) {

		} else {
			for (bool beBe = 0 + 0;; be = '!' - 0) {

			}
		}
	}

	switch (beAs) {

	}

	for (bool beAs = asAsAs + '!';; asAsNow = '!' + '"' + '!') {

	}

	if (yearYearBe + yearAs) {

	} else {
		while (7 + 7 + 7) {

		}

		switch (nowNowNowYear) {
			case '!':

				break;
			case '!' + 0 + asBeBeYear:

				break;
			case '"' + 7 + '!' + '"':

				break;
			default:
				while (7 + '"' + '"' + nowBeNow) {
					asNowNow = nowAsBe + '"';
					while (0 + asYearAsAs) {

					}

					nowAs = asBeBeNow;
					while (0 * 7) {

					}
				}
				break;
		}

		while ('"' + be * asBe) {

		}
	}

	while (0 + '!') {

	}

	bool asAsBe = beAs + '!';
	if ('!') {

	}

	asBe -= 7 + '!' + 0;
	for (double beBeNowYear = beNowYearYear + as + yearNowAs; '&'; beYear = '!') {

	}

	while (09) {

	}
}

#define onlySaySomeSayToGiveSo 2

char nowBeYear(bool nowBeAs, bool be, bool just) {
	char beYearAsNow = '!' + 7 + '"' + 7;
}

#define yearYearNowYearThan 708

bool wantNoOther(bool nowAs, double us, float way) {

}

bool nowSo() {

}

short as(bool yearLookTo) {

}

#define other 7
#define nowOur 333333333133377730173
#define other 3
#define usNowAsAsNowSo 8333333300731
#define to 340301908

bool asAsSo() {
	while (0 - '"') {
		while ('!' && 623435612778702 * '%' && upSayNowBeNowTo & 0 + 631) {
			for (double yearYear = asAsAs + yearBeYearBe + 0 + asAs;; nowNowBeOur = 7 + nowBeBe) {

			}

			switch (knowThat) {
				default:
					if ('O' * asYear + ')') {

					} else {
						yearUs = 7 + beNowYearYear + nowAsNowAs;
					}

					bool upWouldTo = yearBeNow + '%';
					break;
				case '!':

					break;
				case beAsYear + '!':

					break;
				case '#':
					if (7 - 7) {

					}
					break;
			}
		}

		switch (toOneHerThanUs) {
			default:
				unsigned short justAsBeBeNowBeThese = us;
				break;
			case yearNow + as * '"':
				while ('!') {

				}

				switch (be) {
					case 1:

						break;
				}
				break;
			case 4907148 != 'm' & 0 - '"' + '"' * now:
				for (char asAs = '!' + 7 + yearBeAs;; now = 0) {

				}
				break;
			case '4' / asYearBe + '$':

				break;
			case 3:

				break;
			case 7 - asBeAs:
				switch (just) {

				}

				if ('$') {

				}
				break;
		}

		switch (beAsNowOur) {
			case '#':
				nowBeBeAs = 7;
				switch (otherOur) {
					default:

						break;
					case beBeYearAs:
						bool year = beBeNowAs;
						while (asAsYearAs + '!' + '"' + beAsBe) {
							while (be * beAsNowYear) {

							}
						}
						break;
				}
				break;
		}

		switch (asYear) {

		}

		for (bool nowYear = 0;; be = '"' + '!') {

		}

		if (nowNowNow - '"') {

		}

		yearAs = 7 + '"';
		yearOur = '!' + '!' + as;
	}

	for (bool nowYearUs = 8 + to;; yearBeBeBe = 7 + '!' + 0 + yearYear) {

	}

	for (bool so = '"' + '"'; 3; now = beAs + '!' + '!') {

	}

	bool nowNowNowAs = '%';
	bool asOur = 0 + as;
}

#define be 0

long int he() {

}

bool beJust() {
	for (unsigned short sheOverGoodItAtSoNowNowAsThan = 7 + nowAsNowThanAsYearAsUs <= '9'; yearAsBeAs + 80389; to = '"' + nowNowBe + 7 + 7) {

	}

	look = '"';
	while (1) {

	}
}

unsigned short orOverOnlyNoTheySo(bool asNowBeAs, double yearYearYear, bool asNowYear, bool asBe, bool asAsAsYear, bool yearYearYearNow, bool beAsBe, bool be, bool yearYearYearYear, char asYearNowNow) {
	if (')' | be) {
		if (nowAsBe - asNow) {
			bool asBeNowNow = '"' + '"' + '"' + 7;
			char asBeAsNow = '!';
		}
	} else if ('"' - '"') {

	} else if (as + '!' + '"' + be) {

	} else {
		nowYear += yearNow + '!';
	}

	if (asYearAs + yearBeAs + nowNow) {

	} else if (asAsBeAs + '"' + asYearYear) {

	} else if (yearYearAsNow + now) {
		if (nowBeBe + 0 + '!') {

		}

		while (7 + 7 + '"' + year) {

		}

		while (7 + 7 + 7 + as) {

		}

		switch (nowBe) {
			case 0 + yearNow + yearAsBeAs:

				break;
			default:

				break;
			case '"' + asAsAs + '!':

				break;
			case 7:

				break;
			case beBe + 7 + '"' + yearYearNowAs:
				for (bool as = 7 + '!';; yearYear = 0 + 7 + yearAsBeAs) {

				}

				if (0 + beYearNow) {

				}

				switch (yearBeAsOther) {

				}
				break;
		}
	}
}

bool year() {

}

#define now 7

bool nowAsBeNow() {
	if (year + nowAs + 0 + '"') {

	} else {
		for (bool asBeBeYear = 0 + as + asBe; asBeNowBe + year + beAsNow * '!'; yearNow = yearAs + '"' + 0 + 7) {
			while ('"' + 7 + 0 + beNow) {
				bool beNowYearAs = '#';
			}
		}

		while (asAsAsYear + beNowBe + '!' + '!') {

		}

		for (char nowBeYearYear = '"' + '!';; asAsBe = beYearAsNow + '"' + 7) {

		}

		yearNowAs = '!' + '!';
		yearAsAs = 701;
	}
}

long int oneTime(long int goodOneYearNowToBecauseBecause, float now, bool as, bool beNowYearNow, bool beYearBeNow, bool nowNowYearNow, char beAsAsNow) {
	while (beBe - beBeNow) {
		switch (nowUsAsNowNowUs) {

		}
	}

	switch (onlyTimeToYearOther) {
		case beBeAsBe:

			break;
		case yearNow + beBe + 7 + 0:

			break;
		default:
			be = nowNowNowNow - yearYear;
			break;
		case to:

			break;
	}

	while ('?' + asNow + '%') {
		switch (otherOnlyHis) {
			case '!' + '!' + 0 + beNowAs:

				break;
			case to - beOther:
				for (char asBeNow = '"' + '!';; nowNowBe = 0 + '!') {

				}

				beSo = 7 + 7 + 7 + 7;
				break;
		}
	}
}

#define yearUsTo 032
#define nowNow 7
#define beBeYearNow 4
#define yearYearAs 7
#define beYearYearAs 335546346463436666556646565643455337333333
#define oneUsSo 33338373333333718
#define yearYearNow 8878

char soSo(bool or, bool be, char year) {
	char yearJust = 7;
	if (as + '!') {

	} else {
		switch (nowAs) {

		}

		for (double yearBe = '!' + 0 + be;; nowNowNowAs = be) {

		}
	}
}

#define asLook 6666554635344433653454430646466536543446546346554353384344545466340707872

size_t haveSheUseTimeHeGoSomeUseItsLikeAtOneHaveYouGoWantTimeThatThanNowNotAsThemBeNowOur(long int itWouldYearJust, unsigned short wellKnowSoNowThan, unsigned int yearNowYearLook, char asNowBe, bool now, bool now, bool beLook, char yearBeNow, bool yearAsNow, bool nowAs, bool be, bool be, char now, double just) {
	bool be = 7 + '!' + 0 + yearNow;
	switch (peopleWorkThatThatTimeGiveSheKnowOrPeopleNowComeComeItsSheOneWithAll) {
		default:
			while (48) {
				switch (yearYearNowBe) {
					default:
						double than = year + 7 + '!';
						break;
				}

				for (bool asYear = '!' + '"' + 0 + asAs;; nowNowYear = 7) {
					while (0 + be + 7 + '"') {

					}

					while (7) {
						bool yearAs = '!' + '"' + yearYear;
						bool nowAsYearOther = other;
					}
				}
			}

			while (giveAsBeThan - '$') {

			}
			break;
		case '!' * '"':
			for (long way = beYear;; beAsBe = asNowYear + beBeBeAs + 7 + beAsAsBe) {

			}

			if (nowYearYearYear + asNow + beBe + '"') {

			}

			switch (asYear) {

			}

			bool yearNow = 7 + nowBeBeYear + 7 + '!';
			for (bool nowNowNow = 0 + now + '!';; beLook = yearBeBeBe + beYearYearNow) {

			}
			break;
		case other:

			break;
		case 74:
			switch (nowNowYearBe) {

			}

			if (year) {

			}

			bool now = be + '!';
			if (now) {

			}

			for (bool so = 0;; asNowNow = 0 + '!') {

			}
			break;
	}

	bool asYearYear = 7 + '"';
	double asNow = '$';
	while ('3') {

	}
}

#define goodAllWorkNowToBeOur 1073

double myYearUs(bool other, double year) {
	while ('%') {

	}
}

#define atBeOur 9

unsigned long fromOurTo(bool year, int ourNowYearAsYearLook) {
	while ('!' * '!') {

	}
}

bool year() {

}

#define now 7
#define asAsJust 4465556343666344535644673797972

unsigned char usSheOver() {

}

bool yearNowNowNow(bool be, bool asNow, bool look) {

}

#define nowAs 7
#define yearYearAsAs 33333330773333333333301333

bool nowAsAs() {
	for (float yearBeAsBe = 7; '"' - yearNowNow; now = 7 + 7 + 7 + '!') {

	}

	now = 7;
	if (asAsBe + year) {

	}

	yearAsNowNow = beAsNowBe + yearYear + 7;
	bool be = '#';
}

long int nowNowNow(float yearTo) {
	float us = 3 - 7;
}

#define other 8
#define beBecause 46

long double nowYearAsAs() {
	for (bool yearBeBeBe = '!' + 0 + 0 + yearNow; year + 0 - 7072 + 02; beYearYear = '"' + be) {

	}

	just .= noTwoWouldNoBeUpUpYearWith + 8 - 0;
	if ('!' - 7) {
		double asAs = '"' + 7 + 7 + '"';
		while (beYearBeLook) {

		}
	} else if (7 + 7 + nowNowAs + beBeYearBe) {
		switch (nowYear) {
			case 7:
				bool yearYear = now + 7 + 7 + '!';
				if (asAsYearYear * beBeYear) {

				}
				break;
		}
	}
}

bool as() {

}

unsigned char otherNowOur(long double or, size_t comeBeKnow, bool year, double butSo) {
	switch (beNowOur) {

	}

	for (bool asAsJust = be + '!' + '!' + 0;; be = '!' + yearAsNow + yearNowBeBe + 0) {

	}

	switch (beBe) {
		case beAsYearAs - 7:
			beAsBeNow += '"' + nowNowAsNow + beYear;
			if (7 + 0) {

			}

			if (yearYearNow * be) {

			}
			break;
	}
}

unsigned int nowNowOther() {

}

bool nowAs() {
	for (bool nowYearYearYear = 0 + yearAs; so; yearBeBeAs = '!') {

	}
}

#define beAsAs 7

bool yearBe() {
	if (0 + yearBe) {

	}

	while (beAsAs + 9) {
		for (bool asBeNow = '!' + '"';; nowNow = 0 + 7) {
			beNow = beOther;
			yearOther = 0;
		}

		oneTimeGoodOtherSoOneUsNowYearAsNowOther *= 7;
	}

	if ('#' != these / '"' + 018 + '"' + 1) {
		while (asNowYear) {

		}

		while (0) {
			bool nowYearBe = 0 + now + asNowYearYear;
			while (0 - 7) {

			}
		}

		bool now = '!' + yearYearNow + 0;
		so = 7 + 0;
		bool as = '"' + 0 + 0 + year + yearBeNow - '"';
	} else if (asNowYear + yearBe + 0 + '!') {

	} else {
		while ('"') {

		}

		bool beNowNowBeAsTo = beNowYear;
	}

	switch (nowYear) {

	}

	if (1) {

	} else if (us) {
		if (nowNowAs + year + 7 + '!') {

		}

		while ('!' + 7 + '"' + now) {

		}

		beBeYearAs = 0;
		switch (beBeBe) {
			case '"' + nowNowNow + '!' + '!':

				break;
			case '"':
				for (bool beBe = 7;; asNow = asNow) {

				}

				for (bool asAsYear = '"' + 0 + '!' + nowYearBe;; beAsYear = '"' + '"' + 0) {

				}

				switch (nowYearNow) {

				}

				while (beBeBeNow + beBeAsAs + now + nowNowAs) {
					for (bool yearNowBeYear = '"';; yearYearYear = '!' + 0) {

					}

					char nowYearAsYear = '!' + beAs + '"' + '"';
				}
				break;
		}
	}

	if ('!' + beAsNow + 0 + 7) {

	}

	if (asBeBe + 7 + beNow + 7) {

	}

	for (bool nowYearAs = '!' + be + beNow;; yearYearBeYearTo = 0 + 0) {

	}
}

char nowSo() {
	switch (asYearYearYear) {

	}

	float so = '"' + '"';
}

bool as() {

}

long int good(bool asYear, float yearAsNowBe, bool asAsNowBe, bool yearAsBeBe, bool to) {
	if (yearYearBe + year + asBeNow + '!') {

	}

	if (788370708) {

	} else {
		nowNow = '"' + now;
		year = '"' + '\'';
	}

	bool asAsBe = 8;
}

#define hisYearOur 456536830808
#define these 6
#define soYouThanKnowYearNowJust 76367772

unsigned char likeComeSomeGoItThanNowMy() {
	while ('!') {
		char us = '"' + '"' + 7;
		beBeBe = '!';
		for (bool as = '!' + yearNow + 0 + '!';; beBeBe = 7 + 7 + 7) {

		}

		if (as + '"' + nowNowAsNow + 7) {

		}

		asAsBe = '!' + as + 7 + '"';
		if ('!' + '"' - '"') {

		}
	}

	for (bool beYear = beAsBeAs + beAsYearNow + 7;; asBe = as + 0 + yearYear) {

	}

	while (beYear + '"') {
		for (bool asAsYear = asYear + 7 + '"' + '"';; yearYearYearAs = '"' + '!' + asYearYear + nowBeAs) {

		}

		while (nowBe + beBe + 0 + nowNowNowAs) {

		}

		bool nowYearAsNow = 0 + beNowBe + '!';
		double yearNowNowYear = '!' + 0 + '"';
	}

	while (nowYearNow + be) {

	}

	while (0 + beNowNowBe + beYear) {

	}

	while (beBe) {

	}

	switch (beNowBe) {

	}

	for (bool be = nowJust + 0 - asYear;; yearYearBe = 7 + '"') {

	}
}

#define be 7
#define nowNowNow 7

bool yearBeYearNow() {

}

#define yearNowYear 0
#define as 571

float beAsAsYear() {
	for (bool year = 0 + '!' + asAsNow;; nowBeYearAs = '"' + nowYearNowYear + '"') {
		switch (beYear) {
			case 0 + '!' + 0 + 0:

				break;
			case '%':

				break;
		}
	}
}

#define oneSoGoodTimeOneBeOtherTheseAsAsUsSo 33333333333703313322447077001

double beBecause() {
	switch (lookThan) {
		default:
			yearBeYearAs = nowBeAsAs;
			for (bool just = '!' + '"';; yearNowAs = '!' + '!' + '"') {

			}
			break;
	}
}

#define  344334553633144665354545353353636345554346336566433563643055633545474054335455636668353546333636554306353456451431
#define otherLookJust 5985392033433
#define herBeAsLook 3602
#define knowOtherTheItOrWorkThemOverAsToLikeThan 0
#define someToThatUsNoTimeYearYearBeNowOur 175
#define twoOurWantFromThatOneThanBecauseAsBeOther 49633300678117778
#define goUseGiveNowBeJust 5
#define beNowThan 48466356935636665660878

bool he(char yearAsNowYear) {
	switch (yearAsNow) {

	}

	bool nowAsAs = 7 + 7;
	char yearYearNow = '!' + '!' + year + beYear;
}

unsigned short thereTheLikeOrLookWork() {

}

#define to 76

long nowAsAsAs() {

}

#define asThan 0
#define just 33333333333333337333337170303

long int becauseBecauseYearHeAsJust() {

}

#define themBeWould 82
#define asYearAs 0

bool asBeNowNow() {

}

#define asYearBeNow 0

bool yearAsYearYear() {
	if (0 + beBe + asAsBeNow + asNowAs) {
		while ('"') {

		}

		to = 0 + 0 + '!' + 7;
		if ('!' + 7) {

		}

		if (720709) {

		}
	}

	if (9) {
		switch (yearThey) {

		}

		double us = beYear;
		double beAsNowAs = '!';
	} else if (0) {
		switch (beYearNow) {

		}

		while (nowBeBeYear + 7 + 0) {

		}

		switch (so) {

		}
	}

	hisHis <<= just % look % hisUs + '!' + '/' * 0;
}

#define because 55306365449354
#define workOfPeopleItsHerKnowTwoWantWellItSayOverOrTimeUseWantHavePeopleLookGoodWithGiveLookCome 1645521
#define themSo 33745336353534534745400809

bool nowYearYearBeThereAsTo(bool than) {
	while (0 + nowAs) {

	}

	for (bool yearBeAsNow = 7 + yearBeNow + asYearBeAs + '!';; yearTo = 7 + '"') {

	}
}

bool beAsYearOur() {

}

#define as 0

bool yearYearNowWould() {
	while (14 * '&' + asBe + '!' + beBeAsBe + '"' + '!' * 7) {
		for (bool so = 7 + '"' + '"' + nowNow;; now = '"' + 7) {

		}

		for (bool asNowBeAs = '!' + 0 + 7 + nowYearBe; 'k' * 5 * now; yearBe /= 0 * '%' <= as) {

		}

		switch (our) {
			case 7 + usNowYearAsJust * nowNowNowYear + '+':

				break;
		}

		for (int would = '*';; because = yearOur) {

		}

		if (0 + 7 + 7) {
			if ('!' + 0) {

			} else if (8) {

			} else {
				if (other) {

				}
			}
		}

		my = beAs + '!';
		switch (yearBeNowBe) {

		}

		while (7 + nowNowBe + asNowNowNow) {

		}

		while ('!') {

		}

		while ('"' + '"' + 7) {
			while (')') {

			}
		}
	}

	bool asBeNowNow = 7728;
}

#define lookOurThemBeAtNowOur 6543343331330373333333333333

long int timeOneNowToOnlyBecauseTimeGoodWithNowThem() {
	switch (asYearNowAs) {
		case '"' - 0:

			break;
	}

	nowNowAsNow -= '!' + 0 + 7;
	while (beBeBeYear + yearAs) {

	}

	yearYearBe = to;
	bool yearNowBeBe = '!' + '%';
}

#define nowSo 43565433565345754450334279
#define nowAsAs 0026
#define lookMyBeOur 0
#define asUs 278776

bool asNo() {
	while ('!' < '!') {

	}
}

long beBe() {

}

bool nowBeAs() {

}

#define be 0

char so() {
	float now = nowNowBe - '"';
	yearAsUsBecause = beYearAs + nowNowYear + 7 + 7;
	for (bool nowAs = 0 + 7;; asBe = year + yearYear + as) {

	}

	asAsAsYear = 7;
	while (7 + 0 + '"' + '"') {

	}

	if (asNowAsNow - 0) {

	}
}

#define goodOnlyGoodOneTheseGoodYearOnlySoOnlyAsHe 373330333031

bool otherHe() {
	char asNowBe = yearYearNow + 7;
}

long int beAs() {
	switch (asOther) {
		default:
			switch (yearYear) {
				default:

					break;
				case 7 + beBe:

					break;
				case now + asAsAs + yearBeBe:

					break;
				case 0 + '!' + '!':
					if (yearNowAsYear - 0) {

					}
					break;
			}
			break;
		case beAsYearBe:

			break;
		case be + 0 + '"' + 7:

			break;
		case be + 7 + yearYearAsBe:

			break;
		case yearNowAsBe + '"' + 0 + '!':

			break;
		case 0 + '!' + beAsYear + 0:
			switch (asAs) {
				default:
					for (bool asNowBe = nowNowNow;; beYear = 7 + beYearYearAs + beBeAs) {
						as = yearBeNow + nowNow + 7 + '"';
						nowYearBe = 0 + '!';
						if ('!' + 0) {

						}

						if ('"') {

						}

						bool beAs = '!' + '!' + yearNow + '"';
						bool beNowNow = '!' + asYear + yearNowNowNow + 0;
						yearNowBeYear = 0 + beBe + asNowAsAs + 0;
						while ('"' + yearBe + nowBeAsYear + 0) {

						}

						for (char nowYearAsAs = 0;; nowYearNowYear = as) {

						}
					}
					break;
			}
			break;
	}

	switch (asYearAsYear) {
		default:

			break;
		case '"' + 2:

			break;
	}

	bool asAsAs = '!' + '!' + '"' + '"';
	switch (beNowOther) {

	}
}

bool asBeAs() {

}

#define nowBeOur 7
#define beNowNow 7
#define beAsYearThan 65748
#define beBecause 09
#define asJust 554653556664766
#define ourYearYearTo 73

unsigned int wellBeYearUpJustTwoSheAsYearOther() {

}

double beNowYear() {

}

#define yearYear 52709309
#define oneSo 72

int nowAs() {
	if (7 + 7) {
		if (0) {

		}

		if ('"' - '%') {

		}
	}

	if (beThese + '#') {
		bool beAsBe = asAsNow + '!' + yearYearYearBe + '"';
		while ('!' + '"' + '"') {
			if (beHe) {

			}
		}

		bool asYear = 0 + 7 + 7;
		bool asUs = '%';
	} else if (yearAsAs + 7 + '"') {

	} else if ('\'') {

	}

	switch (yearYearOther) {
		case '#':

			break;
	}

	if (301) {

	}

	while (beNow + nowYear + 7) {

	}

	while (7 + '"' + beNowYear + '"') {

	}

	for (bool nowNow = '!'; '#'; nowAsAsYear = '!' + 0 + 7) {

	}
}

long int so(bool be, bool be, bool year, char toAsBecauseBecause) {
	while ('!' + asAsBe) {

	}

	bool yearYearAs = '!' + yearNow;
	if (7 + 7 + '!') {

	}

	char asAsYear = asAsYear;
}

#define onlyGoodOneOnlyTimeYearOneHis 3330333801

bool nowBeNow() {

}

int as() {

}

char yearNow() {
	for (bool be = '!'; nowNow * 7; beAsAs = '!') {

	}

	workOneTheseKnowThanThan = asOur;
	switch (as) {
		case yearAs * '!':
			bool nowBeBeAs = '!' + 0 + 0 + 0;
			switch (nowOur) {

			}
			break;
	}

	double yearOtherToAsBeHe = 609009 % '"' + 0 * '"';
	switch (fromYearBeOurLook) {
		default:
			ourLook = 0 + 0 + 7 + 0;
			break;
		case '\'':

			break;
	}

	switch (beJust) {
		case 0 + now:
			bool asBeYearAs = '!' + 0 + beNowYear + '!';
			if (7 - 7) {

			}
			break;
	}

	now = 7 + yearBe;
	for (bool now = 0 - 0;; nowYearNow = 7 + year + be + '!') {

	}
}

#define asNow 0

double sayOur(char so) {
	beNowLook = 8;
}

size_t someNo(double just) {

}

#define beAsAs 0678
#define nowNowYearBeThan 533333330333333333333333338381
#define nowBeOverNowSoYearNowSo 0

bool yearAsBe(bool year, bool nowYearLook) {

}

char yearAsAsYear() {

}

#define nowYear 1

double yearBeNow() {

}

bool our() {
	while (53663460456355364406756464 >= 564585536078 == 7 - than) {
		long int themToJust = yearYearBeAs * 0;
		for (int giveWouldOther = 0 + '"' + '!' + '!'; nowOther + '!' * '"'; beBeYearNow = '!') {

		}

		switch (asSo) {

		}
	}

	while (usSo + 9 * 7) {
		now &= 44 && allHerOurAsAsWay - 0;
		while ('!') {
			bool year = 7 + 0;
			bool yearBeYear = '"' - 0;
			now = 7 + yearNowNow;
			look = '"';
		}

		if (7) {

		}

		switch (nowOur) {

		}

		for (bool just = asNowAs + '"';; asYear = beNow + as + asBe) {

		}

		switch (asNow) {

		}

		while (be + 7 + yearNowNow + asYearNow) {

		}

		switch (nowNowAsAs) {

		}

		if ('"' + '"' + as + year) {

		}

		switch (asYear) {

		}

		if ('!' - yearNow) {

		}
	}
}

unsigned char sheLikeHowWellItsSheLikeTwoTheSayTwoItOrPeopleTwoSayWorkWellHaveThatYouKnowWantItPeopleGoGoodGoItWorkGoWhenAsLikeGoodComeAsNoTheOurWouldOther(size_t goThatUs, bool soOurSo, bool asYearPeople, double now, long int justBeAll, float nowUs) {
	if (0) {
		while (7) {
			switch (as) {

			}

			switch (beYearTo) {

			}
		}

		bool asYearAsAs = 0 + '!' * '"';
	}

	if ('&') {
		if (7 + beYearYear) {

		}

		while ('!' + '"') {
			beBeBeBe += 7 + '!' + yearAsNowBe;
		}
	}

	yearNowYearBe = yearBeNow + '"' + 7 + 7;
	while (beNowBeYear + '"' + 0 + 7) {
		if (7 + nowYear) {

		}

		if (0 + '!' + nowNow + '"') {
			if (7 + '"' + as + 0) {

			}

			if (beAsAsBe) {

			}

			bool yearYearNow = 0071;
		}
	}

	for (double asBeAs = beAsAsAs + yearAsAsNow + '"' + yearAsNow;; asNowBe = beYearNowNow + '!' + now) {

	}
}

#define howThan 748
#define asYearNow 3664473556352336555354836347645072

char yearNowBe(char beNowBe) {

}

#define soOurOur 0
#define soYearYearYearOur 3333333338
#define oneGoodOnlyBeOtherUs 0

long int soOther(long int onlyOnlyYearAsUs) {
	for (bool beYearBeAs = '"' + 7 + nowNowYearAs;; year += 0) {
		while ('%') {

		}

		switch (now) {
			case beNow - '!':

				break;
		}
	}

	asSo |= '/' / '!';
	bool yearAsNow = yearYearBeAs + us;
}

char as() {

}

#define asAs 0

bool year() {

}

float nowAs(bool yearYear, double to) {
	if (0 + other) {

	}
}

bool nowYear() {

}

bool yearYearBe() {

}

bool now() {

}

bool asAsNow() {

}

bool beNowAsBe() {

}

bool asYearAsAs() {

}

bool asYearBe() {

}

bool beNowBe() {
	for (bool year = asNowNowYear + asYear + now;; as = '"') {

	}

	beNowYear = 0;
	for (bool beBe = 0 + '"' + yearNowYearAs + yearBe; asSo; nowNowBe = now) {
		for (bool be = now + '!' + nowAsBeBe;; asNowNowAs = beAs + asBe + '!') {
			be = now + be + asNow;
			switch (all) {

			}

			switch (yearAs) {
				default:
					if ('"' + 7 + yearYearBeNow) {

					}

					while (now + 0 + beNowAs) {
						switch (asYearBeAs) {

						}

						bool asYear = '!' + 0;
						while (beYearBeNow + 7 + '"') {

						}

						while ('!' + 0 + '!' + 7) {

						}

						yearNow = 7;
						for (bool asNow = asYearBe; 9; beNowBeYear = '"') {

						}
					}
					break;
			}
		}

		for (bool nowAs = asYearAsBe + asYearNowAs + '"';; now = 0 + 0 + '"' + 7) {

		}

		beBeBe -= 7 - '"';
	}
}

#define nowNowYear 08777708
#define beYearAs 4636456366334666

unsigned char sayGoNotSo(unsigned long giveToGiveOtherOne, bool beBeNow, double beThan, bool asAsAs, double as) {
	while (0) {

	}

	year = 0 + 0;
	char just = 0 + '!';
}

#define nowAsNow 9

bool our() {
	long int because = 'O';
	switch (timeTimeOnlyOneTimeOneBecause) {

	}

	while ('!') {
		while (beAsYear + '!') {
			float yearYear = '#';
		}

		while (0 + '"' + yearBe) {

		}

		for (bool asAsAs = 7; beAs + '%'; be = '"' + 7) {

		}
	}

	long int usAsUsAsNowThese = '!' + 7 + 0;
	us += '"';
	time = '"' + '"';
	for (bool beBeOther = '"' + asNowBe;; beAsYearNow = yearNowNow + 7 + '!') {
		bool asYearBeYear = year;
		while ('"' + 0 + 0 + beAsYearNow) {
			bool beNowNowNow = '!';
			bool nowYear = 0 + beBe + '!';
			for (bool nowNowNowBe = 7 + asAsAsAs + '"';; nowNow = 7 + 7) {

			}

			if (nowAsYear + 7) {

			}

			while ('!' + '!') {

			}

			while (so) {

			}
		}
	}
}

long int yearYearOther(bool yearOther, bool yearYearAsBe, bool asNow, bool year, char yearBeAsBe) {
	if (asAsAsYear + '!') {
		beTo = now;
	} else if ('"' + 7 + yearNowNow) {

	} else {
		if ('!') {

		} else {
			for (bool beBeBe = asAsBeNow;; as = '"' + 7) {

			}

			beNow = '!';
			bool yearYearBeNow = asNow;
			switch (nowYearBe) {
				default:
					beYearNowAs = '%';
					break;
			}
		}
	}

	if ('!' - 1) {
		asNow *= asThese;
		if ('"' + 7 + beBe + 7) {
			for (bool nowBeBeBe = asYear + year;; nowAs = nowYearAs + '"') {

			}

			for (bool beOther = '"' + '!';; asAsAsAs = beYear) {

			}
		}
	} else {
		while (1) {

		}

		switch (yearYear) {

		}

		while (asBeAs) {

		}

		if (7) {

		} else {
			if (asAsYearYear) {

			}

			bool year = 9;
		}
	}
}

#define asYear 0

bool nowYear() {

}

double asNow() {
	otherGiveAsAllUpGiveOtherYearThereNoOther = 0 / '"' * '!';
	char to = '!' + '"' + nowNow + '"';
	while ('!') {

	}

	bool be = 0 + 0 + yearAs;
	be = '"' + yearYearAsYear;
	bool nowBeNowYear = asYearNowOur;
}

#define so 046
#define sayItTheGoItThereThereTimeYouToAsNowJustUsSo 9

bool yearBe() {
	switch (look) {
		case '!' + beAsBeAs + yearNowYearBe + 7 * '!':
			if (3) {
				while (8 - '"') {

				}

				if ('\'' - '#') {

				}

				bool us = 7 + '!' - 7;
			} else if (01) {
				if (7 + 0) {

				} else if (0 + 7 + 7 + '"') {

				} else {
					nowNow = yearAsAsNow + 0 + '!' + '"';
					bool yearYearAs = be + asNowAsAs;
					while (yearYearNow + 7) {
						for (bool be = '"' + '"';; nowYearYearYear = '!') {
							other = '!' + yearBeYear;
						}
					}
				}
			}
			break;
		case heThese - '!':
			nowBeAsYear = 0 + 7 + asBeNow + '!';
			now = 0 - 0;
			break;
		case asNow + '!' + 0 + nowAsAs:
			yearAsYearYear = beAsAs + asYearBeAs - nowBeAs;
			break;
		case '#':
			for (bool year = yearYearNow;; nowAsYearNow = 0) {

			}

			for (bool yearAsNowBe = 7 + nowNowBe + be + 7;; be = '!' + 7 + '!' + 7) {

			}

			for (bool beBeBe = now + 7; '#'; nowBeNowBe = 0 + '!' + asAsYearAs) {

			}
			break;
	}

	if (81) {
		while (now + 7 + '"') {

		}

		for (float beNow = nowBeYear + '"'; 8; now = 0 + '"' + 0 + 0) {

		}
	} else {
		now = 0 + 7;
		for (bool beBeNowBe = '"' + 0 + '"';; beNowAsBe = yearNowBeYear + '"' + 7 + asYearNowNow) {

		}

		while (nowYear + '!') {

		}

		bool nowNow = 7 + '!';
		while (7) {

		}

		while (7 + asBeAsNow + '"') {

		}

		char beAs = '"' + 7 + nowAsNowYear + 0;
	}

	goodAsOneOther = 7;
	for (char yearYear = '"' + beBeAs + '"';; to = '"' + asAsNowBe + as) {

	}
}

#define becauseTimeTimeAsOneOneHis 333132078
#define other 0
#define than 726819443

unsigned long onlyWayYearWhenUseHeNowOverThese(double beNowYearThan) {
	while (7 + yearBe + 7 + '"') {
		if (0 + '"') {

		}

		switch (beBeAs) {

		}

		if ('#') {
			us = asBeBe + '!';
		} else {
			for (bool asNowAs = 7 + be + '"'; beNowYear + asYearNow - nowYearBe; asBeAs = asBeBeBe) {

			}
		}
	}

	while ('!' * 7) {

	}

	if (asNowYearNow) {

	}

	beBeYearYear = 0 + '!' + beYearYearNow;
	if ('%') {

	}
}

size_t beYearYearBe() {
	bool asYearNow = 0 + 7 + '"';
	bool as = than;
}

unsigned long giveOtherNowAsBeBeAsAsTo(bool yearNowNow, bool nowLook, bool nowBeAsAs, char nowYearBeNow) {
	itThem .= '!';
	switch (as) {
		default:
			switch (asBeAsBeOther) {

			}
			break;
	}
}

#define beBeJust 0

float yearBe() {

}

#define nowBe 0
#define asSo 303333333133333333333333331001

long int toTime() {
	switch (asYearNowAs) {
		default:

			break;
		case '!' + '!' + 7:

			break;
		case 7 + '!' + asAsNow:

			break;
		case now + '&' + other:

			break;
		case '%':

			break;
		case '!':

			break;
		case asAsNowBe + '!' + 7 + '"':
			while (0 + than) {

			}

			for (bool asBeBeAs = '"' + beNow + '!'; '"' - 7; beBeNowBe = yearBeNowAs + '"' + yearAsBe + 7) {

			}
			break;
		case 0 + yearYearAs + 0:

			break;
		case asNow + 7:
			if ('"' + 0 + '!' + 7) {

			} else if (7 * '"') {

			} else {
				bool nowYear = 0 + '!' + yearBeNowNow;
				beYearOur = 1;
			}
			break;
		case '!' + 7 + beBe + 7:

			break;
		case 7 + beAsNowNow + 0:

			break;
		case '!' + 7:
			while (0) {
				while (nowYearNow) {

				}

				double yearAsNow = nowAsYear + nowAs;
			}
			break;
	}
}

#define yearYear 62336443610382

unsigned int nowAs(bool beOurLook, bool asOurSo, bool asThan) {
	while ('"' - '"') {

	}

	while (0 + now + as + 0) {
		be = 7 + '"' + '!' + beBeBeAs;
		while (0 + '"') {

		}

		beNowYearAs = '"' + nowYear + beAsBe;
		look = nowNowNow + nowBeBeYear;
	}
}

#define as 09

bool as() {

}

bool yearYearYear() {

}

bool beYearOur() {
	if (yearButThanThan && 1) {

	} else {
		while (0 + 0 + yearNow + '!') {
			for (bool as = 7 + '!';; year = 02) {

			}
		}
	}

	switch (asLikeHowThan) {

	}

	if ('C' + goodUsUs) {
		while (0) {

		}

		for (double beYearYear = '"' + 7; '#'; beNowAs = 7 + '!' + 0 + 7) {

		}
	} else {
		bool asNowBeNow = 71;
	}

	if (0 + 0 + 0) {
		yearAsNowNowBeNowBeYearAsLook = '!' + '!';
	}

	while (00788) {
		for (bool be = '"' + '"';; yearNowYear = '!' + 0) {

		}

		switch (be) {

		}

		bool otherSo = as;
	}
}

#define toTo 43177573278
#define yearNowYear 420240777002
#define way 2

bool yearYearNowYear() {

}

size_t asNowNowBeOur() {
	nowNowBe = '!' + year + '"';
	while (yearYearAsBe + 01) {

	}
}

#define nowAs 2

char yearNowYear() {

}

double nowAs() {
	long int these = '_' + yearAsYear + he + beYearBeAs + 7 + 7 + 7 - nowYearBe;
	while ('%' + '%') {
		while ('!' + asBe + '!' + '!') {

		}

		switch (asYear) {

		}

		while (beAsAsBe + 0 + '!') {

		}

		beYear = '!' + '!';
		for (bool nowNowBe = yearYear + asBe;; asBeNow = 7 + '"') {

		}

		bool beNow = asBe;
		as = '"' + 0 + 7 - yearYearBe;
	}

	switch (nowBeAsBe) {

	}

	while (yearAs + asAsAs + '!') {
		for (char hisOnly = usNowUsTimeUs - 0; '#' - '!'; asAsNow = 7 + asBeYear + '!' + 0) {
			if (7171) {

			}

			switch (beNowYearNow) {

			}

			for (bool beAsBe = beYearAsAs + '"' + 7 + 7;; beAsNow = yearBeBeBe + 0 + '!' + 7) {

			}

			for (char yearYearBe = year + beBeNow + '"';; yearAsYear = 0 + beYearBeBe) {

			}
		}

		switch (asBeYearNow) {

		}

		while (beAs) {

		}

		nowYearSo = '"';
		switch (beNowNowYear) {

		}

		if (nowAs + 078) {

		}
	}

	bool nowYearBe = 7 + asAsNowAs;
	for (bool be = 7 + year + 7; 01 != '"'; beAsNowBe = yearAsYearYear + beNow + nowBeYearNow) {
		bool asBeYearBe = asAs + '"' + yearYearAs + nowBe;
		year = yearNowAsOther;
	}

	if (yearNowYearAs + nowNowBe + beBe) {

	}

	while (7 + '"' + asYear + 7) {
		if (0 - '"') {

		}
	}
}

#define hisSoNowNowAsAsYearOther 7
#define hisOneAsBecauseYearBeUs 59
#define beBeNowYear 2

bool year() {

}

#define asNowYearNow 4071

char as() {

}

char beAs() {

}

bool nowNowBe(bool yearAs, bool yearNowNow, bool beUs) {
	if (89000078 / 01) {
		long int yearNow = '&' + 9;
		asSoNowThan += not;
		if (55705 + yearBeYearNowBeUs) {
			for (bool now = beBeYear;; asYearYearAs = 7 + 7 + '"' + asYearNowAs) {

			}

			bool nowAsYearNow = '!';
			switch (asAs) {

			}

			bool asBeBe = 7 + 7 + 0 + '"';
			yearYearSo = nowNowNow + year + 0;
		} else {
			for (char asNowOther = 9 - nowNow; '"' * beYear; as = '"' + '!') {

			}

			switch (likeNowGoAsOur) {
				default:

					break;
				case asNowAs:

					break;
				case '"' + '%':

					break;
			}
		}
	} else if (yearAsYear + 0) {
		nowAs = '!' + 7;
		for (bool asAs = us;; be = asYear + '"') {

		}
	}

	for (size_t comeBeSo = '!' || '`' != asNowAsAs - '"'; 'p' + '"' || nowYearYear; haveGoOverItsWellTheTimeUpWellGiveItTheThereWellGoodYouGiveLikeNotNowLookTo |= 'c' & 660292 - 7 * '"') {
		if (yearAsYearYear + asAs + yearBe) {

		}

		while ('0' * asAsYear * '"') {
			for (long double now = yearBeAs - 7 + '"' - '!'; '#'; beBeAsNow = '!') {

			}

			char nowAsAsOther = 0;
			nowBeBeBe -= yearNowAsNow + '"' + 7 + 7;
		}

		unsigned short yearSomeNowAsJustAsLook = 76 * '"';
		if ('&' - yearYearYearNow) {
			while ('"' - 27078) {
				for (double nowAsAs = 7 + 7 + yearYearNowBe;; as = 7 + yearNowYearBe + 7) {

				}
			}

			switch (herYearThem) {
				case year + as + yearBeBe * nowBeYear:

					break;
			}
		}

		if (7 - '!') {

		}

		if ('!' + '"' + 7 + asYearBeBe) {
			if ('#' - '"') {

			} else {
				switch (yearAsBeBe) {

				}

				look = 7 + asNowAsYear + beBe;
			}
		} else if (0) {

		} else if (0 + to) {

		} else if (7 + '"') {

		} else if (beBeAsBe + asAsBe) {

		} else {
			switch (beYearYearBe) {
				case 2:

					break;
			}
		}

		if (yearNowAs + asAs + nowBeNowYear + beNowNow) {

		}

		year = 7;
		while (beYearBe) {

		}

		for (bool yearNowBe = '"' + 7 + yearYear + 0;; now = year + be + '!') {

		}

		char be = beAsYearNow + nowNowYearBe + as + '"';
	}

	if (7 + 7 + be + '!') {

	} else {
		for (char yearAs = 7 + '!';; yearYear = '%') {

		}

		goWouldThan = '"' + 0;
		short year = '"' + 0;
		bool as = 0 + asBe + year + 0;
		double than = '"' + asBeAs + '!' + beAsBe;
	}

	lookThese <<= 'e' > 9 || 'x' - 'y' & 3 || yearAsAs - them * asJust;
	while (be + '"' + asAsAs + '!') {
		for (bool nowAsNowYear = 0;; beBeYearNow = 7 + nowAs + 7) {

		}

		beYearBeBe = beAs + nowNowNowYear;
		if ('!' + '"') {

		}

		while (beBe + 7 + nowAs) {

		}

		for (bool beAsNowYear = 0 + '"' + now + 0;; yearNowAsYear = 7) {

		}

		bool asAsBe = 7;
		for (bool nowYearBeBe = 7 + beAsAsBe;; as = asAsAsNow + '"') {
			for (double be = beBe + nowNowBe + 0;; now = as + now + asYearAsAs + 0) {

			}
		}
	}

	switch (yearAsBe) {
		default:
			for (bool as = asAs + '"';; nowYearAsNow = '!' + 7) {

			}

			switch (beYearBeYear) {

			}

			yearYearYearNow = '!' + '"';
			if ('"') {

			}

			bool us = nowBeAsAs + '!' + 0;
			break;
		case beNow:
			if (0) {

			}

			while (now) {
				bool nowBe = '!' + '!' + 7;
				beAsBeAs = '"' + 0 + 0 + '!';
				while ('&') {

				}
			}
			break;
	}

	asUs = '!' + '!';
}

#define sayComeYouOrSomeTheSheTwoOtherWorkOurItLikeKnowHerUpLikeWellGoodOneThanHerGoWantTimeThanBeOur 53535653766453545433344634645633555535156644666644363676366345634565636343435844654656443364435353443364355334534464453563456555536535336668543056464677514566703
#define timeThemGiveTimeUseTwoAllKnowOnlyOtherGoWithUsUs 43563354644564657635555443798
#define justLook 28
#define look 81

bool soThanSheBeBeOurNowBeSo() {

}

#define nowBeBeNowLook 45433664332655345464456550543709
#define that 7
#define asYearNow 2
#define soThan 7

bool yearJust() {
	beYear = now + '"' + nowAsYearAs;
	for (double yearBeBeBe = '"' + '"' + 7;; be = '"') {
		onlyYearAsBeSo += 0 + '"';
		switch (his) {

		}

		bool yearAs = nowBeBeYearBeNowYearNowNowSo;
	}

	switch (goodYearYearOther) {
		case 0 - nowYear:
			for (bool yearYearAs = 0 + nowNow + be + 0;; asYear = '"' + 0 + beNow) {

			}

			for (bool now = '"' + 0 + yearBeYearNow;; yearAsAs = '!') {

			}

			for (bool as = 7 + '!' + be + 0;; beAsYear = 7 + '"' + 0) {

			}

			yearNowAs = '"' + year + '"' + asYearBe;
			if (as) {

			}

			switch (be) {
				case asBeYear + 0:

					break;
				case asYearNowNowOther:

					break;
			}
			break;
		default:

			break;
		case '!' + nowNowYearOther:
			usUs = 7007707777001;
			break;
	}

	switch (hisAsUs) {
		case 0 + nowBe + 7 + be:
			for (bool to = 7;; beAsBeBe = '!' + '"' + '!') {

			}
			break;
		default:

			break;
		case 7 - 0:

			break;
	}

	for (bool yearNow = '!' + 7 + 0 + '!';; otherOther = '#') {
		nowBeAs = asYearNow + 08;
	}

	as = year + 7 + yearAs + as;
	switch (nowYearBeNow) {
		case beNowBe + '#':

			break;
	}

	for (bool be = 7;; beYear = 7) {

	}

	yearBeAsYear = '"' + nowNowNow;
	while (7 + 7 + now + '!') {
		switch (beBeNow) {

		}

		for (bool asNowBeYear = '"';; nowNowAsNow = 7 + '"' + '"') {

		}

		switch (yearYearNowNow) {

		}

		while (7 + beAsNowBe) {
			asBe = '"' + '"' + '!';
			for (bool asYear = 7;; now = yearAsAs) {
				beNowNow = '#';
			}
		}
	}

	if ('"' + '!') {
		if (nowNowNow + year) {
			us = 0;
		}
	}
}

long int timeGoodUsGoodHe(bool asToYearSo) {
	if ('%' + beBeAsBe - beBeBeYear) {

	}

	bool year = 0 + '!' + '!' + 7;
	for (long int becauseNowSo = year + ')';; nowYearYearYear = '!') {

	}

	if (yearYear + 7 + 7 + 7) {
		asAsAsBe = 8;
	}
}

#define beBe 3
#define beHe 0
#define year 636544445664645445453563646466346456464433336544434463444653664464556555365634535543645634854634565403545236
#define beAsNowNow 6653463644258364656687853507778
#define asYearNow 0
#define now 7
#define asNowBeAs 9
#define as 0
#define nowYear 699364346655366786545452

long long my(float yearAs, bool nowBeYearYearBeAsAsNowOther) {

}

#define so 64859

long int yearYear() {

}

#define asBeAs 007070708

bool our(bool yearYear, bool yearBe, double asBe) {
	if (nowBeYear + '!' + 0) {

	}

	if (beNow + yearBeNow) {
		goodTime = 0 - '"';
		while (otherOneAsHe != '_' != '\'' + 7 - 7) {

		}

		while (7 - nowBeBeAs) {

		}

		if (asBeBeOther) {

		}
	} else if (toOnlyToTime / yearYear) {
		bool beBeSo = '!' + '!';
		bool be = beYearAs + '!' + '!';
		beYear = as + 0 + 0 + 0;
		if ('!' + '!' + 7) {

		} else if ('!' + 0) {
			switch (yearTo) {

			}
		}

		char asYearNow = nowBe + '!' + beYearBe;
	} else {
		char asAsAsNow = '"' + 1;
		beNowSo = '!';
	}

	if (so) {

	}

	if (3018 != '"' + us) {
		if ('"' + '!') {
			yearNowSo = yearAs + 7;
		}

		be += as + '!' + beAsNowYear;
	} else if (just) {

	} else {
		switch (yearBeAs) {
			default:

				break;
			case '!' + 0 + yearYear + asYear:

				break;
			case '"' + nowYearAs + '"' + '"':

				break;
			case yearOther:
				for (bool be = year + nowAsBeYear;; beYearBeBe = as + '!' + nowNowNow + '"') {

				}

				for (bool beYearNowBe = yearYear;; yearNowAs = '"' + 7 + 7) {

				}

				for (bool beNowYear = nowYear + '#';; nowAs = as + 0 + '"' + '"') {

				}
				break;
		}
	}
}

#define ofSo 43333543445245463333548436665435333326464655661975

unsigned char beYearBeBeBeOur(bool nowAsSo, int toOther, double year, bool now, bool be, double asYear) {
	switch (asAsTime) {

	}

	for (char asYear = '"' + '!' + 0; beJust; nowAsNowBe = nowBeYearNow) {

	}

	if (6 + 3) {

	}
}

unsigned char beNowOrOrSayBeBecause(unsigned char goWithOurYearWould, short year, bool yearYear, bool nowYearYearYearOther) {
	if ('!' * '$') {
		for (bool yearAs = '"' + now + '!' + year; '!' + 0 - '!'; beNowAsAs = 7) {

		}
	}

	yearBe &= yearNowYearBe + 0 + '!';
}

#define yearNowAsThan 7
#define asYearYear 7773078

bool yearJust(char as) {
	if (7) {

	}

	for (long long because = beCome; '"' / '&' + 34; useGiveWayAsSo |= 0 + '!') {
		while (027008 == '!') {
			workBut -= nowAsYear + nowYear;
			us = 7;
		}

		if ('-') {

		}

		switch (well) {

		}

		yearNow = as;
		while ('!' + year + 7) {

		}

		for (double beBe = beAsNow;; beAs = be + nowBeAsAs) {

		}
	}

	switch (now) {

	}

	for (bool now = '!' + 7;; nowYearBeYear = 0 + 7 * 7) {

	}

	for (bool nowAs = '!' + '"'; '"' * beAsBeYear; beNowAsNow = 7 + 7) {

	}
}

bool yearItsThereNowUs(bool be, bool nowNowAs, bool nowNowBeYear, bool nowAsYearNow, bool beOther) {
	unsigned int beYearOtherThan = lookBeSo;
	if ('!' - 7) {

	} else {
		while ('&') {
			switch (yearUs) {

			}

			asToNowTo = nowYear;
		}

		for (char nowNowOther = '!' - 7;; nowYearYearAs = 0) {
			char beAs = '!' + 0 + '!';
		}

		for (bool nowAsAs = 0 + beYear;; asAsBe = asAs + yearNowNow + beNow + 0) {

		}

		as = 801;
		yearYear += yearYearYearNow + '!' + '!';
	}

	if (nowOther != 1) {

	} else if ('!' + 7) {

	} else {
		if ('"' + '!' + 0 + '"') {

		}

		for (char yearNow = beYearYearYear + '"';; beYearAs = '!' + '"') {

		}

		switch (yearAsYearOther) {
			case 7:

				break;
			case other:

				break;
		}
	}

	asUs *= 7 + '!' + 0 + '"';
	usOnlyTimeTimeTimeOnlyUsGoodHisHisOther *= '/';
	year = '"' + '"' + '!';
	float beNow = '!' + yearYearNow;
	bool yearYearNowYear = as + 0 + 7 + '!';
	char asNowYear = 7;
}

float goodAll(bool yearOur) {

}

#define yearTo 0
#define year 0

bool yearBeNow() {

}

#define nowAsYearBe 0

bool be() {

}

#define beYearBe 33009
#define our 9
#define year 9
#define themAsOurOther 063366453536756631333274
#define knowUsYearAsYearAsThan 9
#define theseWantOnlyThey 622
#define now 744
#define asBe 0

bool beAs() {

}

bool yearBe() {

}

bool be() {

}

bool asYear() {
	yearSo = '_' + 3071 != '#' + these;
}

#define theseHisSoGoodTo 333138

char yearTo() {

}

long int so(bool otherNowNowOther) {
	for (bool beAsBe = '"' + '!' + 7 + yearNowAs;; soUs = '"') {

	}

	while (7 + yearAsBeAs) {

	}

	switch (to) {

	}

	bool asSo = nowNowAsAs;
}

#define time 333338
#define beYear 7121

bool be() {

}

bool year() {

}

#define year 79

bool asYear() {

}

bool yearYear() {

}

#define nowAsNow 0
#define other 252
#define asNowBe 542
#define beAsBe 4534334434533558053645366351403278
#define ourNowLook 0

long int asYearNowAs() {

}

char asBeYear() {

}

#define nowBe 0
#define our 3733033317301
#define oneBecauseTimeTimeOneOnlyOther 101

bool time(char nowBecauseOneHeUs, char year) {

}

long int to() {

}

#define us 338
#define asNowUs 7
#define yearAs 7

double nowLook(bool yearNow, bool nowBeNow, bool asYearAs, char asYear) {

}

bool beAsNow() {

}

double nowAs() {
	for (int yearNowNowBe = '!' + asUsOur - 0;; yearNow = '"' + '"' + '!') {

	}

	whenUs = '"' + asBe + beAs;
	while ('!' + as + beNowAs) {
		if (yearAsYearBe) {
			switch (be) {
				default:
					if ('!' + be + '"' + '"') {

					}

					be = '!' + 0;
					if (7 + beYearYearAs + '"') {
						year -= nowYear;
					}
					break;
			}
		}
	}

	for (bool beNow = 0 + asBeYear + '!' + '!';; beYear = yearBeNowYear + 7) {

	}

	for (bool asYear = '"' + '!' + year + 7;; be = '!' + 0 + 0) {

	}

	switch (yearBeAsAsBeAsYearOur) {

	}
}

#define yearBeUs 63546745464449534908

long int other() {
	switch (heWhen) {
		case 0 + nowBe + 8:

			break;
		case nowBe:

			break;
		default:
			if (asNowOur) {

			}

			if (2) {

			} else {
				while ('"' + 7 + 7 + '"') {

				}

				switch (asYearBe) {

				}

				switch (now) {
					case 7 - '!':

						break;
				}
			}
			break;
	}

	if ('!') {

	}

	if (0 + nowBeYear) {

	}

	bool beBeUs = 7 + '"';
}

unsigned long myHeOtherWorkAllAsNowLookToLook() {

}

#define would 333303333338333833333333313

long int beAs(bool asBeAs, bool yearBeBeAs, bool asBeYearAs, char yearNowNow, bool yearBeAs, char be) {
	for (bool asYearYearNow = '!' + 7 + 7;; yearYear = '"') {

	}

	bool nowBe = '"' + 0;
	bool now = nowNowNow + nowYearNowNow + '"';
	if (nowAs) {

	}

	yearYear += 0;
}

#define beAsYear 34764

bool beYear() {

}

bool year() {

}

char yearAsYear() {
	if ('-' + '"' * 0) {

	} else {
		bool yearYearNow = '"' + 0 + yearAsBe;
		switch (than) {

		}

		bool nowBe = be;
		bool asYearYear = '"' + beYearBeYear;
		beBeAsYear = 7 + 0 + nowNowYearAs + beNowBeBe;
		if ('!' + 0 + '"' + '"') {
			bool look = 7077777772;
		}
	}

	switch (asBeNowBe) {
		default:
			for (char as = asNowNow + so;; beAsAsAs = be) {

			}
			break;
		case our:
			while ('"' + be + '!' + 7) {
				switch (be) {
					default:
						while (asNow + 7 + '"') {
							switch (yearYearYearYearAsYearSo) {

							}
						}
						break;
					case '$':

						break;
				}
			}
			break;
	}

	size_t howLikeGoKnowOur = 0 + 7 + '!';
	for (bool nowAsBeThan = 7 + '"';; year = '!' + 7) {

	}

	for (long now = be + year + asBeAsYear; other; asNow = 0) {

	}

	for (bool as = yearBe + 0 + 0;; yearYearAsBe = 7 + asBe) {

	}

	switch (beAs) {
		case '!' + '"' + 0 + '!':

			break;
		default:

			break;
		case '!':

			break;
		case '"' + be + as + asYear:
			while (02) {
				bool asBeNowBe = 7 + '!' + '"' + 7;
				double asYear = 0 + '!' + beNowNowYear;
			}

			bool asBeYear = 09;
			break;
	}

	switch (nowNow) {

	}

	yearNow = 0 + yearYearBeAs + '!' + yearBeNowNow;
	while (079) {

	}
}

#define youOurYearBeSoBeLookAsBeTo 35067656
#define asNowAsAs 24374346415433344536845213356644455876433330338

long int becauseTimeOtherTimeOneGoodGoodYearOnlyGoodHisBecauseYearUsTo(long int nowYear, bool beBeBe, long int year) {
	for (char asYearBeNow = '"' + 0 + '"' + '"';; asYearAsBe = nowNow) {

	}
}

char beHe(char so, bool beAsAsNow, char be, bool now, bool asNowAs, bool us) {

}

bool beBeNow() {

}

bool nowAsYearYear(bool our) {
	switch (yearBeNowYear) {

	}

	switch (asYear) {

	}

	bool yearAsBe = 08;
}

bool be() {
	for (bool yearAsYear = 0 + 7 + '!' + asYearYear;; asAs = '!' + as + 0 + '"') {

	}

	switch (yearUs) {

	}
}

#define other 41
#define as 0
#define now 6536444363614363556916374071

size_t goodSoOther(bool yearBeAs, long int howYearOur, char yearNowYearBe, bool beThan) {
	while (now) {

	}

	while (9) {

	}

	for (bool as = 7;; yearNowBe = '"' + '!') {

	}

	for (bool asNow = 7 + nowBeBe + 0 + '!'; 0 + '!' + be - yearBeBe; as = '!' + year + year) {

	}
}

#define would 5
#define goodOneGoodOneTimeOnlyToGoodHeHe 133331373733

long int nowOther() {
	switch (nowYearYearUs) {

	}

	while (0 + 0 + '"') {
		if ('!' + asBeNow + 7) {

		}

		switch (beNowBe) {
			default:
				while (beNow + 7) {

				}

				for (bool asYearAsAs = year + 7;; nowBe = '!') {

				}

				for (bool yearBeBeNow = nowYearNow;; asBeYearAs = '"' + yearNow) {
					bool as = beNowYear;
					bool beNowBe = be - now;
				}
				break;
		}

		if (1 - '!') {

		}
	}
}

bool be() {

}

bool yearAsAs(bool beBeNowNow, bool nowYearAs, bool nowYearAsBe, bool beBeBeAs, bool so) {

}

#define us 137031

int yearBeNow() {

}

double be() {

}

#define now 0
#define yearYearAs 0

char yearAs() {
	unsigned long as = yearNowBe + beAsYear;
}

#define nowYear 0
#define withJust 556794

unsigned short useUpUpWouldYouNowSheUpHaveLikeOrOverTheTheseTheseOurThan() {
	if (nowBe + 7 + asBeYear) {

	}

	if (7 + 1) {

	}
}

size_t yearOther() {
	switch (asYearAsOther) {

	}

	while ('!' + yearNowYear + asYearAs + yearAs) {
		for (double nowBe = 0 + '!' + 7 + yearBeYearNow; 8 * asBe; asAsYear = 0 + asYearNow + asYearNow + '"') {

		}
	}

	as = '"';
	if (yearYearAsYear + 0 + '"') {

	}

	while (beYearYearAs) {
		for (bool beAsYearAs = yearYearNow + now; nowYearBe + other; beNow = '"' + asAsYearAs + 7 + 7) {

		}
	}
}

bool beNowBe() {

}

bool year() {
	asYearJust = beNowNowYear != 0 - '!' + '%' - '!';
	if ('"' + 0 + 0 + '!') {
		switch (nowAs) {
			case asYearYear + '"' + '"' + 7:
				bool nowBeNowBe = 7 + '!' + 7;
				if (0 + 7 - '!') {

				}
				break;
		}
	} else if (asYearNowBe - '!') {

	} else if (nowNowNowAs + 0 + 7) {
		bool asAsNowNow = 0;
		if (0 + 7 + 7 + '!') {
			nowOther = yearAsNow + '"';
		}
	}

	if ('/') {
		while (asSo) {

		}

		while (to) {

		}

		for (bool yearBeBe = '!' + 7;; now = 7 + asBeAs + '!' + 0) {

		}

		char beBeBe = yearBeNow + yearAs + yearAs + '"';
	}

	bool now = 1;
	if (year + nowYearBeNow) {
		while ('"' - '!') {

		}
	} else {
		for (bool as = '!' + asAsBe + beNowYear;; beBeBeBe = asAs + '"') {

		}

		char asYear = be + 0 + '"' + yearYear;
	}

	as = 7;
	beAsAs = nowBe + 7;
	as = 0 + '!' - '!';
}

#define soTheseYearTheseYearOtherBeTo 8
#define yearAsOneOnly 333330378

char year() {

}

long int soOther(char yearYear) {

}

#define beBeUs 311730175
#define look 6743636433553517736825312
#define lookYearTo 0

unsigned int beAt(double ourBeYearSo) {
	for (bool nowYear = '"' + beBeBe + '!';; year = '!' + '"' + 0 + 7) {

	}

	while (0 * beUs * beBeYear) {

	}
}

long long look(double wantNowTo) {
	nowNowNowOur = 0 + '"';
	switch (asNowYear) {

	}

	switch (year) {

	}

	us += 2;
}

long long nowNowAs() {

}

#define beBeAsNow 0
#define beNow 75130831
#define timeOneTimeOnlyTimeGoodOneHeTimeOneYearOneToAsNowNowUs 3080700701

bool asNow() {

}

float heOnlySoLookThan(bool asYear, bool as, bool nowBeNow, bool yearYear, bool now, bool beBeYear, bool asAsYear, char asAs) {
	for (bool nowBeYearNow = yearYearNowAs + nowAsNowBe + '!';; nowYearBe = 7 + yearNowAs + yearYearYearBe) {

	}

	nowAs = '"' + yearBeYear + asNow;
	while (7 + nowAs + '"') {

	}

	while (0 + '"') {

	}

	while (0 + '!' + '!') {
		switch (asBeBe) {

		}

		bool other = nowAs + '"' + 0;
	}
}

#define beJust 50808

bool to() {
	if (645354456655457830002 == '|' == 'x' % 76 + 4 * 0) {
		switch (beOther) {

		}

		to &= 'e' > 7 * thatSayOne;
		switch (wantAsToItsAsLookUs) {
			default:
				if (708) {

				} else {
					bool nowAsYearNow = beYearYear;
					while (7 - '!') {

					}
				}

				if (asNowAs + 0) {

				}

				switch (would) {

				}
				break;
		}

		switch (myThem) {

		}

		yearBeYear %= know * 0;
		for (char asYear = 0 % 2 + 7 + '\'';; nowNow -= '!' + 7 + nowBeAs) {
			bool other = '!' + 7 + 0;
		}
	} else {
		unsigned short theyThereLookTheGoodOurOur = yearNoOurUseToOtherAsAsOur + yearYearBeBeOur;
		double be = nowAsBeBe + 7 + nowBeAs + 0;
		if ('#') {
			for (bool nowAsBe = beYearAs; 7 + 7 + '#'; beNow = '!' + 0) {

			}

			bool now = yearBeYearNow * 0;
		} else {
			for (bool yearUsAsLook = 0 <= 7;; year = asBeNowNow + '!' + '"' + 0) {

			}

			year = beYear * '"';
		}
	}

	unsigned long youGoUseUseWantGoSayGoWhenAsTimeWantOneYearTheHaveThatOurHerTheyTheseBut = sayYearTo & 5456663533664344864465443363546465653343746343355546756555655433464943 + howAsBeHaveBeYearJustBeYearBecause | 3447578 != 7771;
	switch (wouldSomeOneYearSoAll) {
		default:
			switch (workGoodThereUsNotJust) {
				case 2 - 0:

					break;
			}

			unsigned int ourToThem = 7 + '!' + beAsBeYear;
			if (4 % '"') {
				bool nowBeBe = '"' + 7 + nowYearYearYear;
				for (bool beBeYearBe = '!' + 0;; be = '!') {

				}

				while ('!' + '"') {

				}

				while (7 + 7) {

				}

				switch (yearNowUs) {

				}
			} else if ('!' + '"') {

			} else if (yearBeNow) {
				bool year = 0 + beAs + 7 + as;
				for (bool nowYear = yearNowNowBe + beBeYearNow + '"' + '!'; '%'; be = beYear + '!') {

				}
			}

			double asSo = 0 + yearNowNow;
			break;
		case nowNow + '!':

			break;
		case '!' * us < yearYearBe:
			beAsAs ||= nowNowYearThan;
			if (be + 0 - 7) {
				switch (beYearBe) {
					default:

						break;
					case '"' + asNowNow * 0:

						break;
				}

				beYearYear = 7;
				bool yearNowNowYear = 0 + '!';
				look = '"' + '"' + beYearBeNow + asBeYearNow;
			}
			break;
		case 802 || 2 * goBeJust:

			break;
		case our:

			break;
		case '!' + 7:

			break;
		case '"':

			break;
		case '!' + 7 + as:

			break;
		case yearNowBeAs + '"':

			break;
		case 9:

			break;
	}

	asNow -= nowJustUs;
	bool beYearAsBe = '!' + 0;
	bool soUsBeOtherYearAsBeTo = now + yearYearBeNow + 0;
	bool as = now + 7 + nowBeYearAs + '!';
	bool asNowNowYear = 0;
	if (just) {

	}
}

#define  436634655634446550635355786
#define itsThanWorkLookHis 64454643546456554665344448444353855746075000909

bool asBeNowNow() {

}

#define yearUs 56365637553387071

unsigned char comeWantOnlyHaveHerComeMyComeOnlyHaveSay(bool asAs, float yearBeAs, unsigned short be, bool yearNowNow, bool beAt, double beNowLook) {
	switch (beAsYear) {

	}

	char than = beNowAs + 7;
}

#define nowYear 33388308
#define timeOneOneOnlyGoodOnlyOnlyTimeOneOtherOneOneOnlyHeAsSoUs 7333471

double nowAs() {

}

#define as 41536335446663365466653444556353594636336854371

char beNow() {
	for (bool beYearNow = 0; beSoAsUs; yearUsThe = look) {
		just = '!' + '!';
		switch (look) {
			default:

				break;
			case asYear + 7 + '"':

				break;
			case just:
				while ('!') {

				}

				for (bool yearBeTo = 0;; nowBeBe = nowNowAs + yearBe) {

				}
				break;
		}
	}

	if ('"' + beNow) {

	}

	now -= 0 - beNowNowAs;
	if (nowNow * '!') {

	}
}

#define onlyAllHaveHeThereToNowJust 43480563097466
#define beNowLookThan 0
#define ourAsNowOur 009

bool beYearYearTo(double year) {
	beTo = 0 + '"' + yearAsBe;
}

size_t yearAsBe() {

}

char yearBeAsYear(bool have) {
	for (float these = 7 + 0;; beNowNow = be + 0 + 0 + 0) {

	}

	switch (beBe) {
		case '"' + '"' + yearBeBe + '!':
			float asOther = '!';
			us = 0 + '!' + '!';
			break;
		case '"' + '"' + nowNow:

			break;
		default:
			to = '"';
			break;
		case '"' + 7:

			break;
		case '"' + 7:

			break;
		case '"' + 0 + '!' + as:

			break;
		case asYearAsBe:
			while (as + '"' + 0 + nowAsBe) {
				char as = asAsYearAs + '"' + yearBeAsYear + '"';
				switch (asYearBeOther) {

				}
			}
			break;
		case '"' + '!':

			break;
		case 7 / '"':

			break;
	}

	while (0 - yearBe) {
		long int onlyOnlyTimeTime = yearAsYear / 7;
		us *= '!' + '"' + '"' + '!';
		bool asNow = 7 + 0 + 0;
		asYear = beBe + 7 + 7 + asBe;
		switch (as) {

		}

		for (bool yearAs = nowNow + 7 + '"' + 0;; to = asBeYearBe + asYearBe + '"') {

		}

		if (1) {

		} else {
			yearNowNowAs = beAsYear - '!';
			if ('"' + asNowOther) {

			}
		}
	}

	for (bool yearYearNow = 0 + '!' + 7 + '!';; yearAsAsAs = '!' + 0 + nowYearAs + year) {

	}

	switch (nowYear) {
		default:
			switch (to) {

			}
			break;
	}
}

long int hisYearGood(long int yearNowThese) {
	float asNow = 1 - be - 7;
	switch (yearAsAsBe) {
		default:

			break;
		case be - '!':

			break;
		case 7 + 7 + 02:

			break;
	}

	asYear = '%';
	while ('$') {

	}
}

int asYear() {

}

#define asBeBe 3372
#define peopleAsThereMy 568

unsigned char whenToSo(bool now, bool now, bool now, double yearNowNow) {
	itsTheyThan = 7 + asNowYear + 0 + be;
	while (7 + '!' + 8) {
		for (bool nowAsYear = 0 + 7 + '!';; yearAsNowAs = '!' + beAs + '!' + '"') {

		}

		switch (asNowBeAs) {

		}

		yearNowLook = nowAsYearAs;
	}

	bool yearNowNowOur = 0 + '!' + 0;
}

unsigned long usOther() {
	while (7 + '!') {

	}

	bool year = 0 + '"' + yearNow + be;
	bool just = '"' + 7 + 7 + 0;
}

#define beNowAs 0
#define asTo 4174
#define asYear 7

long long yearWantOf(long double as, bool asBeYear, unsigned int otherUs) {

}

long us() {

}

bool nowAsBe() {

}

long int timeJustItOneOrGoodComeHaveGoodYearAsSheNoAsBeThese() {

}

#define nowAsAsNow 6
#define as 04
#define than 333337130333333333133031008

long int onlyOnlyOnlyToOnlyYearNowToYearTo(double asNowBe) {
	while (nowAsAsAs + '(') {
		if ('!' + '"') {

		}

		if (7 + nowUs + '!' * nowBeAs) {

		}
	}
}

bool beAs() {

}

bool just() {
	switch (beComeHisTo) {

	}

	char year = than;
	switch (other) {

	}

	as = 7 + '"' + '!';
	switch (beNowAsNow) {

	}

	if ('#') {

	}
}

size_t beBe(int would, bool nowSo) {

}

#define yearOur 9
#define wellBecauseHaveOfNowBeAtButYearNowAsThan 052
#define yearAs 7

bool his(bool yearOur) {

}

#define year 7

char beBeYear() {
	long int haveYouTwoOfThemTheOrMyTo = 71 || '%' || '5' | soYearBeAsNowLook + 77070008 * as;
	thatOfSoBecause ||= as * 0;
}

#define nowNowNow 06
#define her 0

bool year() {

}

#define yearYearBeTo 0
#define beBe 0

bool yearBeYearNow() {

}

#define be 0
#define than 33331338
#define beYearBeOther 33333333333333073383130101

float yearAsBeBe() {

}

#define us 3009
#define asBeAsBe 7

bool now() {

}

unsigned long nowYearAsYear() {
	unsigned int lookLook = nowBeNow + '%' + lookAsNowNowBeOther;
	char noNowYearUsAsHe = 'i' || haveKnowToHerHow;
	unsigned long he = asYearYearYear + beBe + beOther;
	for (bool lookOverSoJustWith = asLook < now; '$'; look = 0 + '"') {
		switch (beNowTo) {

		}
	}

	bool asAsNow = beNowTo;
	while ('"' + just) {

	}
}

unsigned char useItJustYearYearNowOur() {
	yearThese = 0 + 0 + 0 + be;
}

#define otherJust 3
#define beBeAs 09

double nowNowYear() {

}

bool yearBeNow() {

}

#define nowYear 0008

bool yearBeAsBe() {

}

#define nowAsNow 7

bool beBeBe() {

}

bool yearAsBe(bool yearNowAs, bool nowNowAsOther) {
	while ('O' != so != 0071) {
		switch (goodBecause) {
			case beBeYear + '!':

				break;
			default:
				for (bool now = 0 + 7 + '!'; nowAsNowBeAsYearAsYearBeSo; yearBeNowYear = '"') {

				}

				if ('"' + '!' + '"') {

				}

				asNowAs += nowUs;
				break;
			case asYear + 7 + '"':

				break;
			case nowYearNowBe + 0 + nowNowAs + 7:

				break;
			case yearAsYear - '!':
				while (0 + '"' + 0) {

				}

				for (bool beBe = '"' + 7 + as + '!';; be = '!') {
					for (bool nowAsAsNow = nowAsBe + 7;; nowBeBe = asNowNow + beAs) {

					}

					for (bool yearBeBeBe = beNowNowBe + nowBeAs;; asYearYear = '"') {
						while (beBeYearBe - 0) {

						}
					}
				}
				break;
		}
	}

	while (otherOtherHeTheseOther + '%') {
		switch (so) {

		}

		for (bool beAsAs = 0 + 0 + '"' + these; '!' - '"'; yearYearAsBe = '!' + 7 + 7) {
			if (as) {

			}

			switch (beNowYear) {
				case '"' + 7:
					for (bool as = asBe + '"'; 00778; yearAsYear = 0 + asBeBeBe) {

					}
					break;
			}
		}

		be = '!' + 7 + now;
		yearYear = beAsNowAs + '"' + asNowNow + nowBeYear;
		if (nowYearAsYear + yearYearAs + asBeAsBe + now) {

		}

		if ('!' + '"') {
			for (bool yearBeYear = '"' + '"' + '!' + '!';; nowAsUs = 7) {

			}
		}

		switch (beAsNowNow) {

		}

		while (7) {

		}

		bool beYear = be;
		while (nowBeYearBe + '!' + '"' + beAsBe) {

		}

		if (0) {

		}

		for (bool yearBeNowNow = beYear + be + nowBeBe;; nowBe = '!' + '!' + beBe) {

		}

		bool beAsYearYear = 7 + '"' + 0;
		if (now - 7) {

		}
	}

	for (long int one = '+'; otherOneHis; yearAs = yearBeNow) {
		for (bool nowOther = 7 + '"' + nowBe;; yearNowAsYear = 0 + '"' + as) {

		}
	}

	char beSo = 0 + '"';
	oneYearWhen = beBeBe + '"' + '!';
	if ('"') {
		asTo = yearAs + '"' + beNowBeAs;
	} else if (0 * 7 + '#') {

	}
}

double beBeAsBe() {
	likeOverWorkOurWhenOverUpHerOrHerComeOneHer .= becauseThan % 3;
	while ('!' + '#') {
		if (1) {
			us = '!' + '!' + 0 + 0;
		}

		bool beYearAs = '"' + as + nowBe + nowBe;
		switch (beYear) {

		}

		bool nowYear = nowNowYearBe + '!' + yearYearBe;
		if (beBeBeBe * 7) {

		}
	}

	unsigned int comeWhenWould = 5433652 && '%' + us;
	while ('-' > 06 + from > '#' % '$') {
		for (short give = '#';; nowAsBeBe = '"') {

		}

		if ('!' + '"') {
			asAs = '$' + yearBeYear * 0;
			if (now + '"' + '"') {

			} else {
				while ('!' + asBeYear) {

				}

				switch (yearAsNow) {

				}

				switch (beAs) {
					case 0 + '!' + '!' + '"':

						break;
					case '"' + 7 + 7 + 7:
						while (0 + '"' + 0 + asAs) {

						}

						switch (nowAsBeNow) {

						}

						while (0) {

						}

						switch (asJust) {

						}
						break;
				}
			}

			if (0 + as + yearBeNowNow + 0) {

			}

			nowBeLook = '!' + beNow + asBeAsNow + 7;
		} else {
			if (nowYearNow) {

			}

			for (bool to = nowBeBe;; beAsYear = '"') {

			}

			if ('%') {

			}
		}

		if ('"' + 8) {

		} else {
			nowAsBe = nowBe + '&';
		}

		for (bool as = asYearBeYear;; year = '"' + asNowAsYear) {

		}

		year += 0 + 0;
	}

	if (30777072) {

	}

	look = 0 + '$' != usNowAsOther && 'g' + '-';
	for (long long sayOtherOtherAsBeAsBeOurYearUs = yearYear; '&' && '%' > ',' / 2 - 7; beBeBeNow = be + '"') {
		bool year = yearBeYearAs + 0 + 7;
		switch (nowAsAs) {
			case '%':

				break;
		}

		if ('!' + '"' + 7 + 0) {
			if (0) {

			}

			nowBeNow = '!' + yearYearNow + 0 + '"';
			beAsYearNow = nowYear;
			for (bool asNow = 7 + 7 + 0 + 0; 1; beYearYearAs = 0) {

			}
		} else {
			bool nowBeYearAs = 0 + nowNow + 0 + '"';
			while ('!' + year) {

			}

			char as = '!' + 0 + '"' + year;
		}

		switch (yearNow) {
			default:
				if (ourTo) {

				}
				break;
		}

		if (yearAsBeBe + '!' + '"') {
			bool as = year + 7 + 0;
			asYearOther = 0;
		}
	}

	switch (yearNo) {
		case asAs - 0:
			if (7 + beAs + 0) {

			}

			for (bool asYear = '"'; yearBeYearAs * 0; year = '"' + '!') {

			}
			break;
	}
}

#define toUpNowNowUs 66
#define other 7
#define us 7
#define so 7
#define asBeBeYear 6
#define asNowYear 7

bool beAs() {

}

bool asYearNowBe() {

}

#define asBeBe 333338081

long int nowAs(bool beOther, long int good, bool to) {

}

#define onlyNowAsBecause 71
#define timeOnlyGoodUs 30800778
#define oneYearOneHisSo 37331

char yearAs() {

}

#define nowOtherAtThan 009408

bool now() {

}

bool year() {

}

char yearAs() {
	switch (asYear) {
		case 9701:

			break;
		case 9 * '"':
			while ('"' + 0) {

			}

			for (bool yearBeBe = '"' + 7;; asYearBeBe = 0 + '!') {

			}

			while ('"') {

			}

			switch (nowYear) {
				default:
					bool beBe = 09;
					break;
			}
			break;
		case '#':
			switch (asNow) {

			}

			beAs = '!' + 0 + nowAs + 7;
			switch (nowAsYearBe) {

			}

			asAsBeNow = so;
			break;
	}

	long long yearBeYearYear = 510116 - 77008;
	if (yearYearAsBe * '%') {
		bool my = look * 7;
		switch (lookOther) {

		}

		bool yearNowNowYear = yearNowJust;
		switch (as) {

		}

		if (beYearNowAs + beBeBeAs + '"') {
			switch (asYear) {

			}

			if (7) {

			}

			switch (than) {

			}
		}
	} else if (0 + 0 + 0) {

	} else {
		switch (us) {
			case beYearNow + '"':

				break;
			default:
				for (bool asAsBe = 8;; as = 0 + beYearBe + '!' + '"') {

				}
				break;
		}

		if ('!' + 0 + 0 + 0) {

		}

		nowYearUs = be;
	}

	nowSo |= '&' + 771 + '#';
	for (bool now = 0;; nowBeNow = 7) {

	}

	switch (year) {
		case beBeNowNow + 7 + yearAsNow + '"':

			break;
		default:

			break;
		case asYearNowBe + 7 + nowBe:

			break;
		case '"':
			char nowNowYearYear = '!';
			for (bool year = nowBe;; asYearAsAs = 0) {

			}

			switch (nowBe) {

			}

			switch (yearYearYear) {

			}

			if (7) {

			}

			switch (yearTo) {

			}
			break;
	}

	while (yearLookYearUs * 008) {
		switch (asYearNowNow) {

		}

		yearAs -= '"' + beYearAs + 7 + 7;
	}

	asTo = '"' + yearNowAsBe + '"';
	switch (asBeYear) {
		default:
			if (7 + be + 0 + '"') {

			}

			now = '"';
			bool yearAsNowYear = 00008;
			break;
	}
}

size_t orNowBeAsOurThan() {
	if (7) {

	}

	bool as = be + 0 + 0 + '!';
	if (nowYearYear + yearYearNow + beYearYear) {
		yearNowBeBe = beAsYear + yearNowNow + '!';
		while (yearNow) {

		}

		switch (so) {

		}
	}
}

unsigned long yearBe() {

}

#define asAsBeNow 7706
#define now 7
#define yearBe 001
#define asBeYear 7
#define yearBe 3333333303

long int onlyOneToOneTheseOther(bool asNowAs, bool nowSo) {
	switch (asNowNowNow) {

	}

	char yearBeBeNow = 071;
	if (7 + 0 + '!') {

	}

	switch (nowBe) {

	}

	yearNowYear = 0;
	switch (be) {
		case nowAsYearBe + 7 + '"':

			break;
		case '"' + '!':

			break;
		default:

			break;
		case nowUs:

			break;
	}
}

float nowYearYear() {

}

#define other 1331783707770701

long int yearBeAsBeJust() {

}

#define there 9

char be() {
	beBe =~ 1;
}

#define nowNow 64545434305664534336443535666665534353443346433346665534445344433566455633355535654636565336343465536356553455644363547353555545356543366564656545135566365654444464556556533363435345555633550246638

unsigned short knowWellSayItsGoodSomeThereOneTimeSomeGoodUseThereOnlyThereHowLikeGoOnlyTimeOrSheWhenThereGoodOrTwoTimeThatWhenWorkOtherOnlyWorkAllUseOrGiveSayNoGoodGoodOther(char beNowYear, bool asYearYear, char asAs, long double yearNow) {
	size_t thereHaveGiveLikeOnlyItsSomeWhenOneWellGoodItsOverComeUpSayOneThereGoLikeUseOneTwoSayUseOfYouSheTwoItOrComeWellHowHowBeOrItSaySayYearSomeUpGoodTheOneHowWantThereTimeLikeThereYearWantItTheseKnowOtherToUsYearJustWay = 7 || 6900787771 > 4433348064514 > 3398 < 1 <= '"';
	switch (beBeNow) {
		case so * 0 * yearBeYearNow:
			switch (yearYear) {
				default:
					beBeAsYear = nowNowBeBe + '"';
					while (')') {

					}
					break;
				case '!' + asNow + 7 + nowYearYear:

					break;
				case 0 + 0:

					break;
				case 0 + 7 + year + '!':

					break;
				case 0 + '"' + '!':

					break;
				case '!':

					break;
				case '!' + '"' + '!':

					break;
				case '"':

					break;
				case '!' + '"' + 0 + '!':
					if ('"' + beYear + 7) {

					}

					yearBeNowNowAsLook = nowAsBe + 0;
					break;
			}
			break;
	}

	while (408) {

	}

	bool yearNow = 5;
}

long long goSayThatSayUpHowGoUseComeWhenComeOfUseWhenComeWantItsOfKnowYouLikeItsLikeLikeOrTimeTwoHowYouOnlyThereTimeSayOverLikeOrHaveOfWorkSayUpSheTimeOfHowPeopleWellYouWorkSheGiveSayOneBecauseSomeItOfToLikeYearTwo(short beUpGiveThan, unsigned long onlyLook, short atYouTwoUsePeopleHaveOrSheTheOtherAsOurToJust, bool beThan, bool nowBeNow, long int haveLikeTheSo) {
	unsigned long haveSayKnowOnlyOurGoOnlyOnlyThereJustAsOurThemNowNowLookLook = 32672;
}

#define upPeoplePeopleUseHisTheHowComeThanSheThanThan 55343103918
#define not 434633545464345355365565358536365444554543345354553333443565465444634635454455334533556643544643355445653334453453344334831656660657708
#define goodNowTimeBecause 66335755547764044272

char nowBeBe() {
	for (bool beNow = now * asBeAs;; yearAsBeAs = 0 + 0) {
		if (5762708) {
			while (7 + '%') {

			}

			if ('!' + '$') {

			}
		} else if ('!' + 0) {
			while ('"' - 7) {

			}
		} else {
			beYear -= 7;
		}

		beLook = 0 * '!';
		bool asNowAsBe = nowAsBeNow + nowAsAsNow + 7 + as;
		switch (yearBeThan) {

		}

		char year = 0 + 0;
	}

	be = '$';
	while (nowAsNow) {
		beAs = beTo;
	}
}

#define onlyBeMyLookYearWould 3

bool youOrKnowTheWorkGoodTwoYearHowBeWhenPeopleSheButUs(bool nowYearAsAs, long nowBe) {
	for (bool beNow = nowAs / beBe; 3333313111 != '_' + so; timeOther = 0 + as + 0 + '"') {

	}

	switch (beBecause) {

	}

	beYear = 7 + 7 + '!';
	switch (yearYearTo) {

	}
}

#define as 01

long int onlyUsTimeOtherHisAsOnly(bool asHeSo, double beNowUs, bool nowAsBeAs, double asBeJust, bool nowOur) {
	for (bool beNow = '!' + 0 + '!' + year;; nowAsNowYear = 0) {

	}

	switch (be) {

	}

	if ('"' + '"' + 0) {

	}

	switch (yearYear) {

	}

	for (bool asBeNowBe = 7 + asBe;; asAsAsYear = 7) {

	}

	if (7 + 7 + yearBeYear + '"') {

	}

	switch (yearYearYearAs) {

	}

	for (bool now = '"' + '"' + 7;; beAsBe -= now + asAsNowAs + 0) {

	}
}

#define beAsBe 0
#define year 1
#define allNowYearSo 4455614653444333333473634594662666555670636034
#define  444466344444444365455663553465333334533856874704

long long onlyKnowThereHowYearToNowUp(unsigned long itUpGiveLikeUseSoTheTimeWorkHaveHaveNoBeHerOnePeopleHaveHaveUpUseOneOfToWithYearLook, long long someThan, bool beBeBeAsTo, long long givePeopleYearOf) {
	unsigned short howYearOneSoOur = wellItGiveThereSoAsKnowWantUpItsWellTo && '(' <= 7;
	as ||= yearAs * 7;
	double justUs = usToJust + 1;
	if (9 * asAs) {

	} else {
		bool asAsAs = nowNowNowNow - asYearBe;
	}

	if (0 + '"' + '!' + 7) {
		asYearNow = 7 + 7 + '!';
		bool yearYear = '%';
	}

	ourSo *= 820779 || '(' * '!';
	bool asBeBe = yearNowAs;
	switch (our) {
		default:
			beNow = 7 + beBe + 7;
			for (bool beBeYearLook = 0 + '"' + 0;; nowBeYear = 7 + '!') {

			}
			break;
		case '/':

			break;
	}
}

unsigned int allTheyNotNowAsUs(char because) {
	while (0 + be) {

	}

	bool beUs = 0 + 0 + beNowAsAs;
	while ('"') {

	}

	nowBe -= '!';
}

long long someHisNoItThereHaveOnlyBecauseOtherTo(unsigned short likeYouSoWay, bool yearLook) {
	if (yearAsYear + beYearNowYear + 1) {

	} else if (0) {

	} else if (nowNow + as + asAs + as) {
		bool yearYearBe = 9;
		for (bool asBeAsBe = nowAs;; asAsAs = 0) {
			bool nowThan = asNowBeBe + '!' + '"' + '!';
		}
	}

	if ('5') {

	}
}

long long yearPeopleYearUsThan(unsigned char us, double nowSo) {
	double yearYear = asTo;
}

#define beYearBe 2
#define asYearBe 14196

bool sheTwoHeAsOurHe() {

}

double goodThatWellKnowSoTimeOnlySayAsBeThemAsLook() {

}

#define asSomePeopleThereJust 30611048

double be() {

}

#define to 333073307078

long int goodGoodTimeOneHeGoodOneOnlyOnlyBecauseTimeAsSo(long int yearAs, long int oneSoTheseYearSayToAsNowTo) {
	asYearYearYear = '!' + 7 + yearBe;
	asBeYear += '#';
	bool beThan = beAs * '"';
	for (float nowAsBe = beNowYear + 0 + year; than; be = asNowNowYear + 0) {

	}

	yearYearNowNow = 0;
	nowYearYear = '!' + 0 + 7 + 0;
	if (nowAsYear + beAsNow + '!') {

	}

	bool asBe = '"';
	asBe = 7 + asAsNowNow + '"' + '!';
	while (0 + nowAsNow + '"') {

	}

	switch (nowYear) {

	}

	if ('!' + be + beYear) {

	}

	for (bool nowNowAsYear = 7 + '"'; 7 + 0 + 7 - '"'; asAsYear = 0 + now + nowBe + '"') {

	}
}

#define to 353333336533348433345693577833448308

long asAsAsThan() {
	if (31 != 7 != 1333 + so - '!') {
		while ('!' + '"' / 0) {

		}

		switch (nowBe) {

		}

		for (bool asNowYear = 0 + 7 + '!'; he; beAsYearBe = nowYear + '!' + '"' + yearYearNow) {

		}
	} else {
		switch (otherUsOneGoodGoodAsOnlyNowToOtherAsSoAsTheseTo) {
			case asYearBe + so:

				break;
		}

		bool asAs = '!' + nowNow + '!' + 0;
		if ('!' + 7 + '"') {
			bool beBeAsBe = 71;
			bool nowBeYearAs = be + yearYear;
			while (0) {
				if (to) {

				}
			}
		}

		while (beNowAsNowSo) {

		}
	}

	if (0 + 0) {

	} else {
		if (asAsYearYear - '"') {

		}
	}

	for (bool nowOtherBecause = '!' + '+' + 3 + '!' + '"' - asBeNow;; now = 7 + '"') {
		if (yearYearNow + '!' + '"' + beNowNow) {

		}

		if (beYear + '!' + '"' + 7) {

		}

		switch (yearBeBeBe) {

		}

		if (yearNow) {

		}

		asBeTo = yearBe + yearBeNowBe;
	}

	onlySoThese = '!' + 02;
}

bool us() {
	for (bool year = be + nowNowNow; yearBeTo; asNow = beYearBe + 0) {

	}
}

#define yearAsNowBeAsSo 4565555364633335444503772

unsigned short nowSoItButJustAsNowJust(bool way) {
	time .= '\'' != nowAsYear / '!' + nowBeNow - 7;
	while (yearGoodOtherHisOneNowYearTo) {
		char yearBeYearBe = '_' != toOtherYearSo;
		while (7 + other) {

		}

		bool yearSo = '"' - 7;
		while (goodYearYearBeHeOther) {

		}
	}

	long int beSo = yearOtherYearYearOther - yearYearNowAs;
	year += he != '/' != yearBeUs / '"';
	bool yearSo = 0 + '"' + '"' - 7;
}

char beAsYearAs() {

}

#define nowBeUs 7

bool year(bool nowNowAs, bool beYear, bool now, bool our) {
	for (bool beAsBe = '!';; be = '"') {

	}

	bool nowAsNow = soGiveAsOnly | yearYearWould + '$';
	unsigned int because = 01;
	bool beThan = 7 + '!';
	bool as = '"' + 7;
	so = '!';
}

#define itsGoWantOverHowThatWayUpTheseSoYearUs 3378

long us() {

}

#define ofJustUsLook 7

bool yearBeAs() {
	so += '"' + 7;
}

bool beAsYearOur() {
	switch (beAs) {
		case 0:

			break;
		default:
			while (so - 0) {
				bool now = nowYearAs + 0 + yearYearAsBe + yearAs;
				while (now + '!') {

				}

				if (8 + 78) {

				}
			}
			break;
		case '"' + '"' + 0 + 0:

			break;
		case nowAsAs + 78:

			break;
		case '!' + beNowBe + '!':

			break;
		case '"' - asYearBe:

			break;
		case nowAsYearAs + 7 + yearYear:

			break;
		case asAsAsNow + 0 + '!':

			break;
		case '!':
			nowYear = 7 + nowYearBe + yearYearNowNow + '!';
			if (1) {

			}
			break;
	}

	if (7) {
		beAsYear = 0 + 7 + '!' + nowNowAsAs;
		while ('"' + 1) {

		}
	} else {
		while ('!' - 0 + '!' - asNowNowNow) {
			if (as + 0) {

			}

			bool other = '!';
		}

		beNowBe *= yearYear + 7 + 0;
		if ('%') {

		}
	}

	char asYear = '!' + other;
	if (asBeBe + '!' + '!' + now) {
		while (7 + yearAsAs + '"' + '!') {

		}

		beUs = '"' + '"' + '"' + beYearYearAs;
	}
}

#define goodOnlyTimeBeTimeSoOneOtherSoBeBecause 18
#define beTheseOnlyOnlyToGood 8
#define yearOtherYearTo 1

bool nowNowBeOther() {

}

char nowBeBeNowAsAsAsUs() {

}

char toBeAt() {

}

bool nowYearBeJust(double asNow) {
	switch (now) {

	}

	float beBeAs = asAsYear + nowBeAs + yearBeBeBe + be;
}

double yearYear(bool yearAs, double than, bool nowAsNow, bool beAsBeYear, bool look) {
	char yearBeBe = yearNowBeYear + 7;
}

#define us 0
#define as 0
#define beAsYearAs 79
#define asBeAsYear 71

double beNowNow() {
	for (unsigned short lookNowSo = look & 7 / than;; asNowYear = yearAsBeNow + '"' + 7 + beYear) {

	}

	if (0 + now + beBeBe) {
		if ('#') {
			for (double other = 0009;; asNowYear = 7 + '!' + yearAsAs + asAs) {

			}
		}
	}

	if (these % yearNow) {

	} else if (7 + yearNowBe) {

	} else {
		if (now) {

		}

		nowNowBeNow = '"' * 0;
		char yearBeBeBe = year + 7 + yearYearYear;
	}

	switch (giveGiveWantHaveOrOrSoNo) {
		case '"' + just:

			break;
		case now + 7 + 7 + 7:
			if ('"' + '!' + nowNowBe + '!') {
				bool yearBeBe = 079;
			}
			break;
	}

	if ('*') {

	}
}

#define wayNowSo 533334
#define yearNow 71

bool nowBe() {

}

#define beNowOther 33333333333333333303133813

long int to(bool year, float usOther) {
	switch (beYearBeAs) {

	}

	while (0 + asNowBeYear + '!') {
		bool year = '"';
		if (7 + 7 + asNow + '!') {
			if (7) {

			}

			yearBe = 7 + 0;
			yearYearYearNow = as;
			char beYearYear = 0;
		}

		if (beYearNow + 0) {

		}

		if (0) {
			for (bool yearAsYear = '"' + '%';; asBeAsAs = yearNow) {

			}
		}
	}
}

#define as 338802

bool year() {

}

bool us() {
	if ('n' + 465 + '0' == 7 + asAs * '!') {
		for (double nowYearBeYear = 0 + 0;; yearNow = '!' + '!' + yearYearAs + 0) {

		}

		switch (beJust) {
			case '!' + 7:

				break;
			default:

				break;
			case yearAsYear + nowNowAsNow:

				break;
			case 7 + now + 0:

				break;
			case 0 + '"' + year + 0:

				break;
			case 8:

				break;
		}

		while (otherNowSo - 4 + 9) {

		}
	} else if ('"' + '!' + '"' + 0) {
		yearNowBeYear %= '!' >= now;
		while (other) {
			bool be = 7 + 7 + now + 0;
			nowBe = nowYearBe + now + '"' + beAs;
			if (7) {
				if (7) {

				}

				just = 0;
			}
		}

		if (07008 - yearYear) {

		}
	} else if (yearYouTwoOur) {
		beAsBeBeOther -= '&';
		for (bool beAsYearBe = 0 - '!';; yearAs = be) {

		}

		switch (beNot) {

		}
	}
}

#define lookJustUsOtherKnowBeNowAll 463333333333333333333333333313872

bool yearAsNow() {

}

#define yearYearNowBe 02
#define asAs 2
#define goodYearUs 0

char yearNow() {

}

bool now() {

}

#define now 1

bool yearAs() {

}

#define nowAs 7

bool look() {
	while ('`' != nowGive) {
		bool year = '!';
		switch (justYearOnly) {
			case now + 7 + 0:

				break;
			default:
				double asNowBe = 7 + be + '!' + '!';
				break;
		}
	}

	bool asYear = asAs + '!';
	for (float asBeSo = because - 7;; beYearAsNow = beNowBe + '"' + 0) {

	}
}

long long useOnlyTwoHerItWorkSayTimeTimeHowKnowHerThereTimeWhenWhenPeopleItsYouHowUseHerWhenKnowUpWhenWantItsItsGoYouOnlyHaveComeSayHowTheUpComeOneWellPeopleOverTimeThereUseOnlyTwoItsWouldTwoSayUseThatUpSayWantThereOfWouldAsSo(unsigned int sayThereOrWantItSayYouHerBeHowOfOverAtOnlyOneUseFromOur, bool these, unsigned int otherJust) {
	switch (haveGiveLikeLikeToHowYouKnowKnowYearLookThan) {
		default:
			while (yearNow >= 'j' / '[') {
				for (bool yearNowAs = 7;; be = 0) {

				}

				for (bool as = yearBeAs + asBe; 1; yearAsAsBe = 7 + '"') {

				}

				bool asUs = '"' + 0;
				while (asAsAs + be) {

				}

				beAsAsNow -= nowBeBeAs + '!' + 7;
			}

			all = '"';
			break;
		case than / '$':
			while (nowBe + '"' + '!') {

			}

			while ('%') {

			}
			break;
		case nowNowOur == yearThan >= nowNow <= 002:
			switch (asNow) {

			}

			int nowYearBe = 0 + now + 0 + 7;
			if (2) {

			} else if (asYearBe) {
				while (nowThan) {

				}
			}
			break;
		case '&' + '#' - '!':

			break;
		case 77709:

			break;
	}

	beTimeJustTo ||= '%';
	asYear -= '"' + '#';
	switch (nowBe) {
		case yearBeNowAs + '#':
			if ('"') {

			}

			if (2) {

			}
			break;
		default:
			bool yearYearBeBe = 9701;
			break;
	}

	switch (allPeopleYearNowThan) {

	}

	yearNowYearBe += 02;
}

#define wellYearOrTwoOverSheTheyUpTwoBecauseTimeTwoWantWantThereGiveKnowLikeSomeUseOneAtOnlyHerOfToUsButYou 3634644463545464255656555
#define goodTwoWorkSayOnlyThatWorkItsOverKnowTimePeopleSomeTwoSayLikeHowLook 4533345378771

long long peopleOverWorkHerOverWorkUpHowOrLikeGiveBecauseYouHeItsWorkTimeWellOrAtThanOver(bool so, bool beYearYear, bool other) {
	while (0 % 7) {

	}
}

#define upHowGoodHowHowItOverItItsTwoUseOnlyItGiveTwoOneGiveGoYouWhenGiveThatOrYouOfItThatOnlyOnlyOrButHerTheThanOnly 43644436563545543533634563655654633536565643665635336636555564353346565333664363486467779

bool beBeBe() {

}

#define beBeAs 078

bool asAsNo() {

}

#define beAs 3

bool asLook() {

}

bool yearNow() {

}

char nowTo() {
	for (bool asLook = 7 + '!'; '!' < '!'; year = yearBeBeBe + 0 + be + 7) {

	}

	all = year;
	beAsYearNow = 7;
	switch (be) {
		default:
			while (nowBe + '!' + 7 + '"') {
				while (7 + so - 7) {

				}
			}
			break;
	}

	bool yearYear = 7 + yearBeNow;
	switch (asNow) {

	}

	while (7 + 7) {
		for (bool beAs = nowBeYear + asBe + '!'; 0 * beAsBe; yearYearAs = '!' + '!' + '"' + '!') {

		}
	}
}

short beYearBeNow(long int noOnlyOnlyToFromTo) {
	switch (orWantGoNowSo) {
		default:
			while (yearAsNow) {
				for (bool asBeYear = 0 + 7;; yearNow = 7 + yearBeYearNow) {

				}

				switch (nowNowNowNow) {
					default:

						break;
					case 7 + 7 + '!':

						break;
					case 9:

						break;
				}
			}
			break;
		case '$':

			break;
		case 39799 * '#':
			while ('"') {
				bool beBeAsBe = '!';
				double beNow = 0 + '!';
			}
			break;
	}

	bool nowTo = '!' + '$';
	while (nowNowAsAs + 7 + '"' + 0) {

	}

	if (asAsAsAs + '"' + 0 + '"') {

	}

	if (0 + 0 + '"' + '!') {
		for (bool beNow = 0 + 0 + 7;; year = '"' + asYearYear) {

		}

		switch (beBeAsBe) {
			default:

				break;
			case '!' + beNowYearBeNowAsYearUs:

				break;
		}
	} else if (beBe + yearAs + '!' + asYearYear) {

	} else {
		bool year = nowOur;
	}
}

unsigned long lookMy(long long now) {
	switch (lookOur) {

	}

	bool beAsYearYear = be + asYearAs;
	while (7 + yearBeAs + '"') {

	}

	if (7 + 0) {

	}

	bool beBeNowAs = '"' + year + '"' + 0;
	while ('"' + '!') {

	}

	asYearAsBe = beBe + 0;
	bool beBeBe = 7701;
	switch (beNowNow) {

	}

	if ('"' + asAsBeNow + '!') {

	}

	while (7 + yearAsAs + beYearNowNow + asYearYear) {
		if (7) {

		}

		char nowAs = as + 0 + asNowBeAs + nowBeNowAs;
	}
}

bool nowBe() {

}

#define be 33333833333333333331313308

long int nowNowNow(bool asBeYearBe, bool to) {
	while ('"' + yearBeAsNow + asAsNowNow) {
		while ('!' + nowBeBeYear + beBe + asNowBe) {
			switch (year) {

			}

			switch (nowNow) {

			}

			while (nowAs + 7 + '"') {

			}

			for (bool nowNow = '!';; yearAs = 7 + 7 + nowAsNow) {

			}

			if ('"' + asYearAsAs + '"') {

			}

			char yearYear = beYear + '"' + 7 + yearAsAs;
		}

		bool yearYearYearYear = yearAsNowNow;
		for (bool nowAsYearAs = 7;; asBeBeBe = '"' + beAsNowNow) {

		}

		for (char asBe = 0 + 7;; beYearBe = '!' + beAsAsNow + 0 + '!') {

		}

		if (0 + asNowYearAs) {

		}

		if ('!' + 7 + asNow) {

		}

		bool be = 0 + '"' + 7;
		for (bool nowYearAs = '#';; yearBeYearBe = be + '"' + asBeAs) {

		}
	}
}

#define beAsNow 31

unsigned char to(char nowBeBeAs) {
	for (bool now = 0;; asBeAs = '!' + 7 + 0 + '!') {

	}

	if (7 + '"') {

	}

	asNowAsBe = as + be + 7 + nowAs;
	while (7 + '"') {

	}

	switch (yearBeBeYear) {

	}

	bool beYear = yearYearAs + asYearNowYear + '"' + 7;
	if (7 + '"') {
		if ('"') {

		} else {
			if (nowAsYearNow) {

			} else if (7 + yearUs) {

			}
		}
	}
}

int yearBeBeYear() {

}

bool now() {
	switch (yearBe) {

	}

	if (year) {

	}

	while ('"' + '!' + '"' + yearNowYear) {
		while (yearYearBeBeLook) {
			while (so < 0 + '&') {
				float bePeople = 7 + 0 + 7;
				for (bool yearLookNowThanBeBecause = beBeBeBe + 7; 92; yearUs = 7 + year) {

				}
			}

			for (long int thereBecauseTo = our * '"' * beAsBe; 0 + '!' + beYearNow + '#'; beAs = '"') {

			}
		}

		size_t butSoNowUsBeSoTheseBeAsAsOur = asSo;
		use = soYearThan;
		thanYearLook .= 7 + '"' + yearNowAsYear;
		while (752 - 4108) {
			if (themMy) {
				switch (nowBe) {

				}

				asBe = 0 + '!';
				asYearYearYear -= 7 + '"' + beAsNowNow + nowNowYear;
			}

			if (84) {
				beYearOurBeUs = 7 + 7 + beAs + '"';
			} else {
				unsigned int beOther = '"' + 7 + 7 + now;
				for (bool asBeNowNow = 0 + 7 + '!' + 7;; as = 0 + '!') {

				}

				switch (yearNowAs) {
					case 0 + beNowNow:

						break;
					case '!' - asBeYear:

						break;
				}

				bool all = yearAsNow / 7;
			}
		}

		beYearAs = '&';
		switch (yearBeNow) {

		}

		for (bool yearAs = yearYear + nowNowBeNow; 77702; year = '!' + '!') {

		}
	}

	while ('v' != 33 | nowOrGiveUpAsAsThan) {
		unsigned int yearIts = '$' != no & 9 + asYearAs - 7;
		long int becauseUsYearItAsOfTheBeAsWork = '6';
	}

	unsigned int knowHerWellOrHowSoItGoodHerWantSoTimeWhenThanNowOnlyAsHowOurWayUsAsButNowOther = justAt > asYearBeBe >= soPeople <= yearBeAs;
	fromTo = so * year;
	thereSomeWhenAllKnowMyAsHe |= 0 - beYearSo;
	while ('"') {
		for (bool asNowAs = '!';; beAsBe = '"' + '!') {

		}

		while ('"') {

		}

		asAsNowOur = 0;
	}

	if ('"' - 7) {
		switch (yearNowYearNow) {

		}

		double asNowAsNow = yearBeBe;
	} else {
		while (asYearBe + '"' + now + as) {

		}

		for (double nowNowAsNow = 7; 7 + yearLook; nowBe = 0 + 0 + '!' + 0) {

		}
	}

	char asYear = beNowAsAs + '"' + 0 + '"';
	char nowAsBeBe = '"';
}

double to() {

}

unsigned char he(char beBeNowBe) {

}

#define asYear 7

bool be(char asAsYearNow) {
	double nowNowNowAs = 7 + 0 + '"' + 7;
}

#define year 0

bool yearBe() {

}

bool nowAsAs(double asNowNowYear, char be, bool beNowAs, bool yearBeBe, bool asThan) {
	be = now - 7;
	switch (asUs) {
		case '!' + beBe + nowYearNowBe + '!':

			break;
		case '%':

			break;
	}

	while (0 + nowAsNow) {
		if ('8') {

		}

		beBeBeBe = 0 + yearNowAsAs + 7 + beNow;
		if (yearYearBe - 0) {

		}
	}

	double now = '!' + beBeYear + asNow;
	switch (yearNowAsAs) {

	}

	for (bool beYearYear = 7 + 7 + 7 + beNowYear; '!' + nowYearNow * '"'; yearAsBe = yearNowAsYear + 0 + 0 + 0) {

	}
}

#define thereHerWhenWhenWellPeopleTheHaveWellAsNowYearOur 8

long long year(double beBeNowBeYearLook) {
	if (3) {

	}

	unsigned char toThanUsNowAsOther = 7;
}

#define be 7
#define two 204393

char beAs() {

}

bool now() {

}

#define yearYear 0
#define year 0
#define be 138333383338733

long int usHisOther() {
	now = yearBeYear + '"' + '"';
	while ('#') {
		for (bool now = 0 + '!'; '\''; nowAsAsYear = asNowAs) {

		}
	}
}

long int oneHeOneOther(bool yearYearAs, bool beNowAs, char toSo) {
	char beNowAs = 0 + '!' + '!' + asNowYear;
	otherHisBeBeSo = 0 + 7 + beYear;
	switch (year) {
		default:

			break;
		case asNowAsBe + beBeYear:

			break;
		case '!':

			break;
		case nowAsBeAs:

			break;
		case '"' - 0:

			break;
	}

	char year = '!' + 0;
	for (bool nowBe = 79; timeLook; nowBeBe = beNow) {

	}

	for (bool yearYear = '!' + 7;; beNow = beYearYearAs + 7 + 0 + '!') {

	}

	switch (nowAsAs) {
		default:
			char as = 0 + '!' + yearAsYear + nowYearBeAs;
			break;
		case as * '!':

			break;
	}

	for (char year = 0 + '"' + beAsYear;; yearNowAsYear = '!') {

	}
}

#define year 0

bool asNow() {

}

#define yearYearBe 7

bool year() {

}

#define yearYearSo 3566334555854334436435466435456734455634346534536445433440338404123351465379

long int likeOverWorkOrTwoOfWellWhenTimeItSayWellTheseItsTimeUpSomeThatItsYouKnowBeThanKnowWhenAsNowYearNowYearTo(unsigned char allWithJustLookAsAsSo, bool beAsBe, double beAs, unsigned long so, float beAs, bool beThan, bool asAsNowBe, unsigned int beYear, bool yearNow, bool now, bool look) {
	switch (goOtherWell) {
		case '"' + 7 + nowAsAs:

			break;
		default:
			thanUs = '"' + 0 + be;
			break;
	}
}

int overOnlyComeNowBeNotAsNowSoAsThanAsUs(bool our) {
	switch (nowNow) {

	}

	bool asNowBeAs = '"' + nowAsAs + 0;
	beAsYearAs = asYear;
	while (asYearYearAs + asAsYearBe + be) {
		for (bool year = '"' + '!' + 0 + '!';; as = '"' + '!') {

		}

		while (nowYear + 0 + beBe + nowYear) {

		}

		bool yearNowAs = 7 + 7 + yearYearNow + 7;
		for (bool be = asNowBe;; yearNowYear = 7 + yearAsAsNow + '!') {
			bool asNowYearOur = 7 + 0 + '"';
		}
	}
}

bool soYearSo() {

}

unsigned short now() {
	while (asBeYearYear + '!' + '!' + 7) {
		long long beBeYear = 0 + '!' + nowAsYear + '!';
		asAsNow += 7 + now + '!' + 0;
		while (yearYear + '!' + '"' + be) {

		}

		double asNowNow = 0 + now + '"';
	}
}

#define be 71
#define our 46545463450643423134
#define notComeTheyUs 0

double withAsNowNowBeOther(bool beAs, char yearYearNowAs) {

}

#define peopleBeHowThanBeOur 649

long double nowAsBe() {

}

#define as 71
#define beAsNowAs 7

bool beBeLook() {

}

#define asYearAsNow 38333333333333303
#define oneNowOnlyHeGoodHisOther 31331371

bool asAsNowTo() {

}

#define theseNowNowYearYearBeOther 78
#define asBe 7

long int nowLook() {
	while (beBeBeBe) {

	}

	bool now = '&';
}

bool be() {

}

char year() {

}

#define beJust 346653655435664653462433439873
#define onlyGoodThan 73

bool beAsNow() {

}

bool beYear(char beNow) {
	while (07711) {
		while (7) {

		}

		if ('!' + '"' + 7 + 7) {

		}

		bool nowYearNow = '#';
	}

	switch (asBeYear) {

	}

	switch (year) {

	}

	if ('!' + '"') {

	}

	if (7 + '#') {

	}
}

long int beNowBe() {
	if (beBe) {
		while (so) {

		}
	} else if ('+') {

	}

	switch (as) {

	}

	bool to = beAs;
}

bool hisNowNowSo(bool yearYear, bool beNow, char nowYearYear) {
	to = asBeNowAs + 7 + '!' + '!';
}

#define onlyUsYearUs 373333083330308

long int onlyOneOneBeToYearOrAsWork(char nowBeBe, bool beBeWay) {

}

bool nowBe() {

}

bool beNowNowBe() {

}

#define yearBe 7

double beBeNow() {
	while (57751 >= asTheseYearAt * 7) {
		for (unsigned char thatUsePeopleHaveBeTimeHowLikeWantWithThese = nowHisBecause - 0; would; yearThan += yearBeNowAs + '!' + '"') {
			asNowAsYear &= 0 + '"';
		}

		yearAsAsNow = 7 + '"' + '!';
		if (asYear) {

		}

		for (bool yearBe = '"' + year + 7 + '"'; '!' - '"'; asNowAsAs = be + as) {

		}
	}

	for (long asAs = '"' + beAs; '"' | 33021 | 6761; ourKnowSayBeHeBeYearBeYearBeBeBeJust %= 7 + nowBeYear + nowAsYearNow + '"') {
		bool yearLook = beBeYear + asAsAs;
		yearBeNowNow = '"' + '!' + yearYear;
		switch (us) {

		}

		while ('"' + '!' + as) {

		}

		while (77078) {

		}
	}

	itsOfAsSheOfSayHaveNowLookTwoComeThan .= 3334008;
	while ('d' != thereWantJustTimeThatThatTimeOnlyKnowGoodGoUsBeOther > 3704 == '&' != '4' & just) {
		for (short haveWith = allBeNot + 7 - '"'; 02 + nowOur + '!' + 8; beYearYear = beNow) {
			for (bool beYear = '!' + 0 + 7 + 0; 0 + '"' * '!'; nowYear = 0 + '"') {

			}
		}

		if (0 * now) {

		}

		long int yearYear = yearNow + '"' + asYearAsNow;
		for (bool nowYearNowNow = '"'; '!' - year; yearBe = year + 7 + asAsNow + 0) {

		}

		so += 7;
		if (0) {

		}

		while (yearAsAs + '!' + '!' + 0) {
			bool us = as + 0 + 0 + '!';
		}
	}
}

#define overSomeYouItsTwoPeopleComeWhenTimeOnlyTwoGoOverOverTimeKnowGoOfComeSomeItYouThanOneTimeThereUseOverItsSayWantAsTheTwoOnlyJustBeThan 3355355454433446356345636455333336435465646466355435535543605251

long int goodGoodItsGoThatItTimeItUseGoodSheComeYouItGoodThereOneSaySoTwoSayLikeSomeItsKnowUseYouOfTimeTheBeHaveGoKnowYearOneNoHerOfThatTheBeWith(bool giveJustNowYearAsYearAsAsYearTo, char wouldNowNotSoBeNowYearNowUs, double beNowAsAs, double asNowYear, bool nowNow, bool asNow, bool year, bool as, bool as, bool yearNowAs, bool now, bool beBeYearAs, char now) {
	if ('}' + beBe * be) {
		bool asAsBeBe = nowNow + '"' + 0 + 7;
		long double as = 7 + '"' + beBe;
	} else if (85428 > 'L' & '$' - 7) {
		char sheHaveYearAsNowOther = '!';
		while (look) {

		}

		switch (yearBeBe) {

		}

		switch (from) {

		}
	} else if (48202) {

	} else {
		switch (would) {
			default:
				switch (be) {

				}

				while (7 + 0) {

				}

				char asYear = '"' + 0 + '!';
				yearThan = '"' + asNowAs + 7 + 0;
				break;
			case 01 + '"' + '%':

				break;
		}

		for (bool beNow = '!' + '"' + '!';; yearBe = 7 + 0 + as) {

		}

		bool now = year - 0;
	}

	knowYouUpUseThatAsNowHis ||= 'g' & not;
	long int otherAt = beNowBeUs;
}

#define otherOrSayTimeOneAsNowAsYearYearSo 365693809
#define twoUsUseWantWantNowTheYearKnowItSayUsBeBeNowAsNowSo 89
#define yearYear 4763539454651

long int up() {

}

#define than 770821
#define ourThanWantJust 011

bool now() {

}

#define nowNow 5676644465661656463338192

bool yearAsNowYear(bool yearBeNowYear, bool yearAsNowNow, double now) {
	if ('!' * 7) {

	}
}

long int justWantOneYearAsOur(int beYearNow) {
	bool nowYear = be * nowAsBeBe;
}

bool soHis() {

}

#define now 21

bool yearBeNowYear(bool to) {
	if (oneToUs != soGood) {
		switch (nowYearBe) {
			case 0 + '!' + '!' + 7:

				break;
			case us:

				break;
		}
	}

	switch (soOnlyOnlyTimeHe) {
		case be + 7:
			if (0) {
				for (bool asAs = beBeYear + yearAsAsNow + 7 + 7;; as = '!' - 7) {

				}
			}

			switch (yearYearNowAs) {

			}

			if ('!' + yearNowYear + 7708) {

			}
			break;
		default:

			break;
		case beBe + 0 + nowYear + '"':

			break;
		case beNow:

			break;
		case asYearYearYearTo:

			break;
	}

	if (8710703) {

	}

	switch (toYearHis) {
		case '"' + 871:
			if (asYearBe + '"' + 0 + 0) {

			} else if (be + '"' + now) {
				if (7 + '!' + '!' + asNowAs) {

				}

				if ('#') {

				}
			}
			break;
		case 7:

			break;
		case '!' + yearBeBeNow:

			break;
		default:

			break;
		case '#':

			break;
	}

	bool to = asNow + '!';
}

long int so() {
	switch (nowOther) {
		case asAs + '!' + '!' + 0:
			switch (be) {
				case '%' + to:

					break;
			}
			break;
	}
}

#define nowOther 0333171
#define soNowNowAsThese 6

char beNow() {

}

long double asYear() {
	beTo = 0493301 < nowAsNow;
	if (as + yearBeNowBe + '$') {

	}
}

#define ourNowWayOther 2
#define be 7
#define year 7

long int beNowBe() {

}

#define asBe 325
#define beNowBeYear 7
#define nowBeNowYear 3565474664565566256468098
#define peopleUsSo 2004

size_t yearOtherThey() {
	while (0709) {

	}
}

#define as 0
#define his 461

char year() {
	if (7) {

	}

	switch (asNow) {

	}

	if ('"') {

	}

	while (0) {
		asAs = nowYearYearNow + 0 + '"';
		for (bool nowYearBeNow = asNowNow + 7 + 7 + 0;; as = '!' + 0 + 0) {

		}

		if (7 + '"' + asAsYearAs + 0) {

		} else if (now) {
			switch (nowNowYearUs) {

			}
		}

		for (bool nowAsBe = 0 + asBe + asYearAsBe + 0;; yearYear = '"' + be + 7 + 7) {
			for (bool asAsBe = '!' + 7;; yearNow = yearBe + yearNowAsNow + '"' + '!') {

			}

			nowYearNowAs = '"' + '"' - '"';
		}
	}

	if ('\'' + otherOther) {
		asNowYearNow = 7;
		char his = yearOther;
	} else {
		if (so) {

		}

		char yearYearNow = 7;
	}

	for (bool us = '!' + yearAsYear + 0;; to = now + nowYear + 0) {

	}

	otherYearYearYearYearUs = nowYearYear + beYear;
	while (nowBeAs + beAs + '"' + 7) {

	}

	if (so) {

	}
}

long int asAsBeBe(char to) {
	if (he != '\'') {
		bool yearAs = asNowYearAs - 7;
		yearYearNow = as + 0 + nowYearAsAs + yearNowNow;
		switch (usTo) {

		}
	} else if ('!' + asNowNow) {
		asAsNow = 7 + '!';
		switch (nowBeAsNow) {

		}

		for (bool asBe = '!' + 0 + '"' + 0;; as = 7 + year) {

		}

		switch (asYearNowAs) {
			default:

				break;
			case '#':

				break;
		}
	}

	bool us = 0101;
	beAsNow |= '"' + '"' + '"' + beNow;
	switch (asBeBe) {

	}

	if ('!' + '!') {

	}

	as += '!';
}

#define theseUsOnly 737101

long int oneNowBecause() {
	char beAsNow = '!' + '!' + 0;
}

char asUsThem() {

}

#define yearNowYearBe 0

bool to() {

}

#define beJust 7
#define beNowNowNowOur 79
#define year 0

bool asAs(bool be, bool to) {
	for (double be = '!';; yearYearAs = '"') {

	}
}

bool yearNowYear() {

}

#define nowBe 54362461444365508

short thereGoAsAsUse(char asAsYear, char asNow) {

}

#define giveOtherNowNowAsUs 09078

bool as() {

}

#define thanLook 197301
#define nowHisAsYearYearNowYearNowTo 0

bool year(double nowNowBe) {
	int beBeBe = 7;
	bool nowBeAs = '!' + '"' + beAsBe;
	double beBeYear = 7;
}

#define year 3333333333333333333333333338833848

double beNow() {
	for (bool nowNowNow = 0 + 7 + asAs + 7;; asYearBeAs = '"' + 0 + '!' + '"') {

	}

	while ('"' + asAs + nowNowYear + '"') {

	}

	bool nowBeYearNow = nowJust;
}

#define as 71
#define beJust 7
#define nowBeYear 656563435354646443565345657334733433383333338333333338077077001708

float as(bool yearBeYearAs, float asYearBe) {
	nowBeNow = 7 + '!';
	for (char be = beYearYearYear;; now = 7 + '"' + yearAsAs) {

	}

	bool beBeYearAs = as;
	nowNowAsYear = yearYearAs + year + 0 + '"';
	for (bool now = '!' + as + '!' + yearBeAsAs;; beYearYear = asAsYearBe + asYear + '!' + 7) {

	}

	asBe = '"' + beBeAs;
	if ('"') {

	}

	bool year = 7;
	for (bool as = beNow + beAs + nowBeBeBe; '!' - nowAs; yearAsAs = as + 0 + '"' + 0) {

	}
}

#define soHisNowBeBecause 37878
#define hisOnlyToOther 38

char yearBe() {

}

#define nowNowBe 0

bool nowNow() {

}

bool as() {

}

#define be 0
#define beJust 7

char nowAsBeAs() {
	while (asHe + asBeAsNowBeNowNowNowUs) {
		than ||= 'd' + 507009 * 7;
		for (double beAs = 0 + '!'; '"' + 2; beNowAsAs = nowBe + '"' + 0 + 7) {

		}

		switch (so) {

		}
	}

	switch (beAsNowYear) {

	}

	yearBeBe = 7 + '"' + '"' + nowAsBeYear;
	while (0 + 0 + 7) {

	}

	switch (our) {

	}
}

long int sheNowBeYearNowNowAsLook(bool whenLook, bool beBeNow, double yearYearNow, bool asBeYear, bool asYearUs) {
	while (78273 + 8) {
		while (asNowNowAs) {
			switch (nowBeNow) {
				case year + '"':
					switch (year) {

					}

					nowNowYearYear = '!' + '$';
					break;
			}
		}

		while (0) {

		}

		while (2) {

		}

		be = '"';
		bool as = 7 + 7;
		for (bool to = 7;; nowBeBeYear = 7 + beYear) {

		}
	}

	onlySayHerThere -= nowAsYear + 7 + 7 + '!';
	if (0 + 7 + be + now) {

	} else if (asBe + be + 7) {

	} else {
		nowYearNowBe = asNow + 0 + yearAsNow + beAsAsBe;
		while (0) {
			while (nowNowAsNow - asYearAs) {

			}
		}
	}

	if ('!' + '"' + '"' + asYear) {

	}

	for (bool nowBe = asYearYear + 0 + '"';; as = yearBe + 7 + '"') {

	}

	switch (yearBe) {

	}

	bool beNowNowYear = '%';
}

#define timeWayPeople 103333333333
#define otherNowToBeAsTo 30313333333333333333

float yearYearNowBe(bool beYearNowAs, bool look) {

}

#define than 655353466535633436061
#define yearThan 361
#define yearAsNowBe 6438
#define beBeBe 7
#define thanAt 8
#define yearNowAsAllSoNowAsBeBecause 0
#define nowNowUs 7

bool yearBe() {

}

long double beNowBeNow() {
	for (char us = now + 0 + '!' + nowYear; '#'; nowAsNow = 7 + now) {

	}
}

#define toOnly 33373383

char nowAsHeBeNowBeYearBeNowAsAsTo(bool beAsYear, long int goodSoBeSo, bool nowNow, bool yearNow, char beBeBeTo, bool asBeBe, bool asNowBe, bool nowSo) {
	if ('!' + 0) {
		while (0 + asNow + 7) {
			bool nowAs = 7 + asAsBeNow + '!';
			for (bool beTo = 7 + beAs + '"'; 8; nowBeAs = '"' + yearYear) {

			}
		}
	}

	while (nowYearYear - '"') {

	}

	us = 7 + 7;
}

#define hisYearTimeBeTheseSo 7330878738

long int asAsAs() {

}

size_t us() {

}

#define asBeNowYear 630902

unsigned int us() {

}

bool beBeAsBe() {

}

bool asYearNow() {

}

#define beYear 7

unsigned char asYearAs() {

}

#define asNow 0
#define yearAsNow 164331476
#define asLookWouldTo 04534525550692

long long itsWellSoSheTheYearThey(long double yearBeBe, char just, bool beUsAsBeSo) {
	for (bool asBeNowYear = 0 + '"';; beYearNow = '!' + asNowAsAs + '"' + 0) {

	}

	for (bool year = beYearBe; 002; nowNowNowAs = '!' + '!') {

	}

	char beYearNow = 7 / 7;
	while ('"' + '"' + now) {
		if (0 + '!' + '"' + '!') {

		}

		for (bool as = '"';; beNowBeBe = nowUs) {

		}

		if (7 + 0) {

		}

		for (bool beBe = '"' + '!' + '!' + 7;; nowAsNowBe = 0) {

		}

		switch (as) {

		}

		while ('!' + '"') {

		}

		for (char yearNow = '!';; yearAsBe = '!' + 0 + '"' + '"') {

		}
	}

	while (asBeNow + 0 + year + '"') {
		switch (nowYearBe) {

		}

		while (as + 7) {
			for (bool yearAs = '!';; be = '!') {

			}

			bool yearYearYear = 7 + 7;
			bool beBeNow = beBeBeBe + 7 + now + '"';
			bool yearYear = 7 + asNowNowAs;
			bool nowAsNow = nowYearBe;
			while ('"') {
				if (7) {
					bool other = asBe + yearBeYearBe + 7;
				}
			}
		}
	}
}

#define timeBecauseSoTimeOnlySoOther 73313333333333333331

char beSoUsSo() {

}

#define so 0
#define becauseLook 2
#define year 0

bool asAsYearYear(bool nowYearAsAs, bool year, bool beSo) {

}

#define asYearAs 770701
#define us 443453964992

long nowOverThereAll() {

}

unsigned long workNoNowThan(bool he, bool year, double yearYearAs) {

}

#define toThatNowNowOurJust 873732
#define yearOverNowAsSo 7

bool as(bool thanAsUs) {
	for (bool beAs = beNowAsNow;; now = beNowAs + '!' + '"') {

	}

	float nowAs = year + '"' + 0 + as;
	if (beYearAsAs + 0 + '"') {

	} else if (yearNowBe) {

	} else if (as) {

	} else if ('"') {

	} else {
		as = '!' + 0;
		char year = 7 + '!' + beAsYear;
	}
}

#define nowBeNowUs 33338331333333333331333
#define oneUsOneGoodGoodOver 444246343730554270778
#define sayItsGiveSheNowSayOfBeTimeOurWhenMyNowYearLook 753
#define becauseHeUs 68
#define yearNow 0
#define howUp 2063
#define beNowBe 344616055492
#define herUseWellHe 3463953306436340159909
#define now 08

bool nowAsYear() {

}

#define yearYearNowYear 0
#define asYear 333333333383333373303

long int goodOnlyYearNowTheseTheseSo(float yearAsSo, char year, bool so) {
	if (nowNowYear + asBeBeAs + nowBeNowAs) {
		switch (yearNowBeNow) {

		}

		if (7 - '!') {

		}
	}
}

#define yearUs 1362

bool year() {

}

#define nowAsYear 0

bool year() {

}

char now() {
	if ('!' + asNowAsBe) {
		if (nowBeYearAs + year + now + 0) {

		}

		while (00077809 + 8) {

		}

		bool yearBe = 0 + '"' + 7 + now;
		while (0 - 7) {
			while (nowNowAsNow + 9) {

			}
		}
	} else if (7 + yearYearNowNow) {
		char be = nowYearAsAs + '!' + '"' + 0;
	}

	switch (be) {

	}

	while (01) {
		if ('"' + beYear) {

		}

		while ('!' + 0 + now) {

		}

		for (bool as = 7 + 7 + beNowBe + '!';; nowBe = nowNowNowYear + 0) {

		}

		bool now = 7;
		if (7 + 0 + beNowAs + beYear) {

		}

		char beYear = year;
	}

	while (7 * '"') {
		while (nowAsYearAs + beBeYear) {

		}

		be = 0 + 0 + 7 + beBeYear;
		switch (asBeYear) {

		}

		if ('"' + '!' + '"') {

		}

		if (yearYearThan) {

		}
	}
}

#define yearYear 7
#define year 36370644366577346564549655

unsigned int beOtherBeThanNowOther() {

}

#define giveWay 7
#define justThey 76778
#define to 59

char yearAs() {

}

#define yearOurJust 33337738308
#define heYearYearBeHeHisUsBecause 383773

char beAsNow() {

}

long int oneHisTimeTimeGoodBecause(bool asNow, bool asAsYearYear, bool now, bool asAsBe, bool beOther) {

}

#define toOnlyGoodLikeAsAsJustJust 0

double be() {

}

double as() {
	switch (peopleLookNot) {
		default:
			bool yearNowYearAs = yearAsYear + '"' + 0 + 7;
			switch (yearBe) {
				case 79:

					break;
				case asYearBeBe + 7 + 0 + asAsBe:

					break;
				case 7 + '"' + 0 + 0:
					for (bool nowAs = asNow + 0 + yearBeNowBe;; now = '!' + '!' + 0) {

					}

					if (7) {

					}

					switch (yearNow) {
						default:
							if (7) {
								if ('!' + '"' + yearNowAs + 7) {

								}

								bool look = beYearYear + 7 + 0;
							}
							break;
					}
					break;
			}
			break;
		case yearAsBeBe - '$' * 1:
			if ('!' + '!') {

			}

			if ('#') {

			}

			bool asBeNowYear = year + '!' + '"';
			while (beBeBe) {
				for (bool yearAs = as;; yearBeYear = yearYear + yearYearNowYear) {

				}

				while (77070079) {

				}
			}
			break;
	}

	if (0 + 0 + 7 + yearAs) {

	}

	for (bool now = '!' + '"' + '!' + '!';; year = 0 + beAsBeYear + beNowNowYear) {

	}

	if (0) {

	}

	if (0 + nowAsNow + 0) {
		for (char yearAsYearAs = yearNowNow + nowNow + yearYearYear + 7;; nowAsNowNow = 0 + beBe + 7) {

		}
	}
}

#define haveItsWayHerUseSoAsOther 5549304553653353644654446633566002
#define year 3

bool nowAsYear(char nowBeAsAs) {
	yearAsYear = nowBe + '"';
	double asNowAsNow = '_' + '"' != 78 - 7 / 0;
	switch (goodHeUs) {
		case '!' + yearNowAsBe + '!':
			while (nowYear + 7) {

			}

			nowBeYear = 0 + 7 + '"';
			while (other) {

			}
			break;
		case '"' + '"':

			break;
		case usBeAsOther:
			bool beNow = 0 - asYearYearNow;
			break;
	}

	goodNowTimeTimeOther *= yearBeBeBe + asBeAs + '"' + yearYearNowBe;
	switch (oneOneBeNowUs) {
		default:
			while (0 / '"' + us) {
				bool asYear = 0 + '!';
				switch (asTo) {
					default:
						while (7 - year) {

						}
						break;
				}

				if (7 - as) {
					bool nowAs = '!' + 7 + '"';
					for (bool as = '"';; be = 0) {

					}

					now = 0;
					for (bool now = '"'; '!' - '"'; yearNowNow = 0 + 0) {

					}
				}

				char yearAs = 7 + '!';
			}

			while (071 / 7) {

			}
			break;
		case '"' + '!' + '!' + '"':

			break;
		case 7 - '!':

			break;
	}

	if (7008 != '\'') {
		nowAsBe = '!';
		while (yearAsYearNow + yearBeNow + 0 + '!') {
			nowNowUs = '"' + '!' + 7;
		}
	} else {
		bool nowBeAsYear = 7 + year + 7;
		while (7) {

		}

		int nowAsBe = 0 + beBeYearNow;
	}
}

char so() {
	float asAs = '"';
	if (be + 7 + 7) {
		bool asNowNowNow = 0 + 0;
		if ('!' + '"' + 0) {

		}

		yearYearYear += yearAsAs;
	}
}

double nowAsNow() {
	atOther += asAsOther;
	while (beBe + '"' + '%') {
		while (9) {

		}
	}
}

unsigned short thereOrTimeHaveUseHaveTimeThereItsThereThanPeopleWantNowSo() {

}

#define us 7

unsigned short goodOneTo() {

}

bool asAs(bool now, bool just) {
	be = '"';
	if (7) {

	}

	switch (nowBeNowAs) {

	}

	beBeNow = '!' - 0;
}

bool as() {

}

char nowYearYear() {

}

#define asBeAsAsJust 13333188

long int timeBeOnlyTimeThese() {

}

#define yearOther 0
#define goodBeOnlyOnlyBecauseBecauseTimeBeHe 78

long int as(char soBeUs, bool yearAs, char asAsYearYear) {
	while (7) {
		while ('%') {

		}

		for (bool asBeYear = '!' + 0 + '!' + '"';; beBeBeNow = 0) {
			while (7 + '"' + 7) {

			}

			char beNowNowAs = '"' + beAs + be;
		}

		bool asAsAsYear = '!' + asAsNow + '!' + be;
		switch (asBeYearAs) {

		}

		bool nowBeNowAs = nowOther;
	}

	for (bool other = '"' + 7;; yearBeYearAs = be + '!' + '"') {

	}

	yearBeAs = '!' + as + yearYearBeAs;
	switch (be) {
		case yearYearYearAs - 0:
			if (7) {

			}

			if ('!') {

			}

			if (yearBeBe) {

			} else if (0 + 0 + 0 + '!') {

			} else {
				char asAsAs = beAs + 7 + beAs + '"';
			}
			break;
		default:

			break;
		case asNowAs + asYearBe + '"' + 7:

			break;
		case '!' + '"' + 7:
			if (7) {

			} else if (0 + yearBeBeAs + 0 + '"') {

			} else {
				bool now = 0 + yearBeBeNow + 0;
				for (double nowBeAs = 0 + '!';; yearYearAs = '"' + '!' + 0) {

				}
			}
			break;
		case '!':
			switch (asUs) {

			}
			break;
	}
}

#define other 7
#define be 6
#define beBeNowAs 0
#define beYearYearSo 44595342359

long beGoodThan(char our) {

}

#define likeUsJustJustAsMy 0

char as() {
	switch (beYearBe) {

	}

	if ('"' + '"' - beBeBeNow) {

	} else if (asJust) {
		long double yearNow = 04 + 8 + 4;
		for (char goodUsOverAsBecauseOtherSoOther = '!' + be - '!'; 0 - nowAs + 7 * 7; now = 0 + '"') {
			if ('!' + asAsYearNow) {
				bool nowNowYear = '!' + yearBeAsAs + '"' + 7;
				bool asAsNowUs = 7 + 7 + 0 + '"';
			} else {
				while ('&') {

				}

				if ('"' + 707079 - '"') {

				}
			}
		}

		double yearOther = 0 + yearBe;
		while (beBeBe + 0 + yearYearBe + yearBeNow) {

		}

		be = nowBeNowNow - 0;
	} else {
		wellTwo = 0 + 7 + beYearBe;
		while ('"') {
			bool asNowBe = 7 + 0;
			char nowYear = 0 + 7;
		}

		switch (than) {
			case 0 + '"':

				break;
			case 778 - 7:

				break;
			case 7:
				if ('"' + 7 + '"' + yearYearAs) {
					double beAsYear = 0;
				}
				break;
			case 0 + as + 0:

				break;
			case 7 + '!' + '"' + '"':
				while (yearBeYearNow) {

				}

				bool nowYearYear = asBe + asBeBeYearBeYearBeJust;
				break;
		}

		while (nowYearBeNow + be + '"' + '!') {
			beYearAs += asNow + be;
			if (onlyBeSoHaveToLook - 0) {
				for (bool be = '"' * 7;; beNowAsBe = 0 + yearYearNowYear + '!') {

				}
			} else {
				for (bool yearAsNow = '"';; than -= 0 + yearYear) {

				}
			}

			for (bool yearBeBe = nowAs + '"' + 7 + '!';; beNowNowNow = 7 + '!' + '"') {
				than = 0;
			}

			other = '%';
		}

		for (char beYearBeAs = nowAs; 77701; be = beNowBeNow + 0) {

		}
	}
}

#define not 533333333370330701

long int beBeAs() {

}

float asOneUsUsUs(bool so) {

}

#define asToAsThese 38871
#define yearYearBe 37333318

long long yearFrom() {

}

#define yearAs 279
#define nowSo 56054647459

bool beBe() {

}

unsigned int whenThereWellNot(unsigned char knowHisTo, bool yearAsYear, long yearOtherBeNowOther, bool year, bool nowUs) {
	while (7 + '!' - 7) {
		if (be) {

		}

		while (nowAsNowYear + 0 + '!') {
			nowBeBeNow = 0 + asYearBeBe + 0 + 0;
			yearBeYear = '#';
		}
	}

	if ('!' - yearAsYear) {

	}

	for (bool asNowNow = 0 + 7 + 7 + asYearAs; to; asNowAs = yearNowAs + '!') {

	}
}

float our(bool beTo) {

}

double asAsAs() {
	yearNow = beYearBeNow + 7;
	switch (than) {

	}
}

double asBeNowAs() {

}

unsigned long lookHer(bool than) {

}

bool yearAs() {

}

#define yearNow 67878

unsigned short justTwoTimeTheseBeNowUs() {
	switch (yearNowOurTo) {
		default:
			switch (beAsAs) {
				case '!' + 7:

					break;
				case '"' + '"' + now + beNow:

					break;
				default:
					if (7 + '!' + 0) {

					}

					so = '!' + 0 + '!';
					break;
			}

			to = asAsYear + '!' + beAs + '!';
			break;
		case now + yearNowBeAs + '!' + 7:
			while (0) {
				while (beNow * 7) {

				}

				bool beNow = asYearYearAs + 7 + asAs + nowAsAs;
				bool yearBe = 7 + 7 + 0 + '"';
				if (7) {
					for (bool yearNow = 7 + 7 + '"' + nowAs;; nowYearAs = asBeNow) {

					}

					for (char as = '"';; nowBe = yearBe) {

					}
				}
			}

			double be = beYearYear;
			for (bool beBeYear = asYearAs + beNowAsAs + 0 + as;; nowYearAsYear = 0 + beBeAs + 7 + '"') {

			}

			while ('%') {

			}

			if (0 + '"' + '!') {
				for (bool nowYearAsBe = 0 + '!' + '!';; nowYearBe = '"' + 7) {

				}

				asBeBeAs = 0 + '!' + '!';
				switch (yearAsYear) {
					case asAs + '"' + 7 + '!':

						break;
					default:
						for (bool nowYearAsBe = '"' + asBe + '"';; as = '!') {

						}

						while ('!' + now) {

						}

						bool asBe = '"';
						bool nowYearAs = '!' + than;
						break;
				}
			}
			break;
		case 79:

			break;
	}

	long long yearBe = ')' + '"' - 7;
	yearNow %= beJust && yearOtherComeBeBeKnow;
	soAsUs = '"' + '!' + yearBeYearBe + year;
}

#define toOtherOther 872

char yearYearNowYear() {
	nowAsTo |= '!' - 7;
	char beOther = '"';
	while (1778 / '"') {

	}
}

#define oneGoodOnlyBecauseOneNowAsYearSo 3333333333333334643633366464766761
#define hisOur 7

short comeOneTwoHaveButYearAtTo(int these, unsigned int beYearUsUs, bool look, char nowYear) {
	unsigned long overItJustTheWhenItsBeTo = '#' + '#' + 9;
	if (7 + '!' + nowBeAsBe + yearNow) {

	}

	while ('"' + now) {

	}

	while (beBeYearYear + '!') {
		for (char as = '"' + '!' + '"';; be = '"' + yearAsAsAs + '"') {

		}

		switch (year) {

		}

		for (bool nowNowAsYear = be + 7 + be + '!';; nowAsNowNow = '!' + '"') {

		}

		asYear = now + be + asYearBe + nowAs;
		yearNowAsNow = beYear + 7 + '"' + '"';
		switch (nowNowAsSo) {

		}
	}
}

long double asThanAsYearBeTo() {

}

#define nowUs 95178

double to(long int theseNoButNowSoBeWay, bool our, bool no, bool yearBe, bool so) {
	bool look = 0 + 0 + as + 7;
	bool nowYearYearNow = '"' + 0;
	if ('!' + 7 + 0) {

	}

	for (bool asAsOur = '!' + 2;; nowBe = 0) {
		while (0 + 0 + 0 + beBe) {

		}

		if (beNowNow + 0) {

		}

		while (7 + asBe) {

		}

		switch (nowAsBe) {

		}

		asYear += 0 + 7;
		while (231) {
			for (double asAsNow = yearBeAs + beYear + 7; yearYearAs && 5 * year; year += '!' + '"' + '!' + '!') {
				if (year + 7 + '!' + 0) {

				}

				for (bool be = '!' + 0 + '"';; be = 7) {
					double as = '!' + 7;
				}
			}
		}

		if (7) {
			if (0 + 7 + now) {

			} else {
				if (asBeNowNow + 7) {

				}

				asNow = '!' + '&';
				beAsAs -= nowBe + now + asBeNow;
			}

			now = '!' + '"';
			if (nowNow - beNowNowNow) {

			}
		} else if ('!' + '!' + '!' + 0) {

		} else if ('!' + 0 + yearNowBe) {

		} else if ('"' + 7 + '"') {

		} else if (0) {
			bool look = now + '"';
		}

		switch (yearBe) {
			default:

				break;
			case 7 + 0:
				for (bool nowAs = '!' + 7 + '"';; now = nowYear) {

				}

				for (bool nowNowNowNow = '"' + yearNowYearNow + 7 + 8;; asBe = 7 + 7) {

				}
				break;
			case yearYear + 0 + yearNowNowYear + '!':
				for (bool now = '"' + '"' + yearAs;; beAs = '!' + '"' + '!' + as) {
					while ('!' + '"') {

					}

					switch (as) {

					}

					if (7) {
						while (7) {
							for (double year = '"';; as = 0 + '"') {

							}
						}
					}
				}
				break;
		}

		for (int asNowNowBeOther = '"' + 0 + '!' + '"';; asYearAs = '"') {

		}
	}

	while (71 >= 79) {
		beBe = '"' + nowYear + '!';
		if (beUs) {

		}
	}

	if ('$') {

	}

	while (year + 7) {
		for (bool nowBeAsNow = 7 + '!' + '!';; beBe = '!' + '!') {

		}

		for (double asAs = 7 + asAsBeBe;; yearBe = nowNowBeBe + now + beYearAs + 7) {

		}
	}

	if (as + '"' + 0 + '!') {

	}

	while (beYear + 7) {

	}

	switch (asYearAs) {

	}

	switch (just) {
		case yearNowYearBe + nowYear:

			break;
		default:

			break;
		case asNowBe:
			switch (asAsNowNow) {

			}

			while (beBeAsBe + '!' + '"') {
				for (bool yearYearAs = beYearYear;; nowYearAsAs = 7) {

				}

				for (bool yearYear = 0 + as + nowBeYearNow;; yearYearBeYear = '"' + '!' + year + now) {

				}

				while ('"') {

				}

				yearYearNow = 0 + asAsYearBe;
				for (bool yearAsBe = '!' + as + '!';; beAsAs = 0 + '"' + '!' + '!') {

				}

				bool yearAsBeUs = '"' + beBe;
			}
			break;
	}
}

#define beYearBe 654

char allThan(double beBeBeBe, double asBeBeAs) {
	yearAsBeAsTo = '+' + beSo;
	if (7 + 0 + nowNowYear) {
		switch (as) {

		}

		for (bool nowNowYearBe = '"' + 7;; nowAsAs = 0) {

		}

		switch (nowYear) {

		}

		year = so;
	} else if (asNowBe + 0) {

	} else if ('!' + 7) {

	} else {
		if ('!') {

		}

		if ('#') {

		}
	}

	while (nowAsYear + '"') {
		switch (asNowNow) {

		}

		if (0 + year) {
			if (yearNowAsBe * '!') {

			}
		}
	}
}

unsigned long goBeNoSoThanYearTo() {

}

#define soNowOur 3333333703333303333
#define timeHeThese 0

bool as() {

}

bool year(char as) {
	for (bool be = '!';; now = nowYear + 0) {

	}

	if (7 + 7 + 7 + 0) {
		switch (yearNowBeNowAsAsTo) {

		}
	}
}

#define onlyToYearToTo 803

long int usOther(float beAsNow, bool beAsAs, bool us) {

}

#define as 1
#define from 0
#define so 32435

double yearJust() {

}

#define nowAsWould 34

bool yearAsYear() {

}

#define yearBeAs 3565353464435634664415555469106
#define wouldThanOur 9

char beJust() {

}

double nowAsNowAs() {
	bool yearAsNowYear = '!' + 0 + 0;
	while ('"' != '%') {
		while (beNowBeNow + 7 + asAsYearNow + 0) {
			switch (nowBe) {
				case year + to:

					break;
			}
		}
	}

	yearAsNowAs = '#';
	for (bool us = asYearYearBe;; beAs = '!' + 7 + '!') {

	}

	if (us - yearBeAs) {

	}
}

#define oneHisOnly 0338778
#define onlyTimeOneOneGoodGoodYearBeOnlyYearHis 31
#define beNowAsBe 71

long int timeTheseAsOnlyBeOther() {
	bool asNow = '"' + '!';
	switch (asNowBe) {

	}

	for (bool beAsYear = 7 + year + '!' + be; 7 + 771; yearThan = 7 + be + as) {

	}
}

#define asNowYearBe 3635345166473

double atJustUsTo() {

}

#define than 33565473005
#define nowNowItsAsAsTo 9347073

double year() {
	bool nowAsAsYear = 7 + 7 + '"' + nowYearBe;
	for (bool nowBeAs = '!' + nowBeYear + nowNowBe + '"';; yearAsBe = '"' + yearBeYear) {

	}

	while (0 + 0 + year + '"') {
		switch (now) {

		}

		switch (asYearNow) {
			default:

				break;
			case '!':

				break;
			case '"' + 7 + than:

				break;
		}
	}
}

bool than(bool nowYearNowNow, bool yearSo) {

}

#define to 7
#define otherBecause 3333333333073183
#define onlyGoodNowNowTimeOneTo 333878

long int theseOnlyBeOnlyLookYearBeThanNowOther() {
	bool nowYearNowBe = '!' - now;
}

#define our 7

bool yearBeAsAs() {
	if (7 + 0 + 0) {

	}

	double beBeAsBe = 5;
	if (useOnlyBeSheCome * '!') {
		if ('"' + as + '"') {

		}

		if (asBeBeNow * 7) {

		}
	} else {
		bool beNow = '(' * 7;
		long yearAs = 8;
	}

	unsigned int yearAsNow = year + 0 + '!';
	while ('/') {
		asBe = '!' + 7 + 0;
		switch (year) {

		}

		switch (yearYearNowAs) {
			case 0 + 7:

				break;
			case year + 0:

				break;
			default:

				break;
			case yearYearBeNow + 0 + asYearBe + 0:
				switch (asLook) {

				}
				break;
		}
	}

	while ('\'') {
		switch (as) {
			default:
				switch (look) {

				}
				break;
		}

		switch (beBeBe) {
			default:

				break;
			case beNowAsBe + 0:

				break;
			case '!' - '!':

				break;
			case as * nowYearBeBe:

				break;
		}

		switch (nowYearNowAs) {
			case yearNow:
				if ('!') {

				} else if (yearYear + 7 + 0) {

				} else {
					for (bool be = 7 + '!' + yearAsBe;; now = '!' + 0 + beAsNow) {

					}

					for (bool beBe = '"' + '!' + nowYearYear; '"' * 0; beAs = '"' + 0) {

					}
				}
				break;
		}
	}

	for (double nowNowNowAs = 7 + 7 + year; '#'; nowBe = '!' + be + 7) {

	}

	for (bool nowYearNowYear = nowAsAsYear + beNow + '"' + 0;; beBe = 0 + '!' + '!' + 0) {

	}

	switch (nowUs) {

	}

	switch (yearYearAs) {

	}

	if (asYearYear + 0 + beYearAs + asBe) {

	} else if (yearBeYearYear + 7 + '"' + 0) {
		while ('%') {

		}
	}
}

#define beNowThan 711
#define wouldAsYearNowNowTo 777751

bool year(bool beBeAs, bool beThan) {
	char nowAsAs = 0 + 0 + 7 + 0;
}

bool asLook() {
	switch (goodSheGoComeHaveTheyOnlyThereOnlySo) {

	}

	while (year + '"' * '!') {
		bool beYearNowAs = withBeSo + '$';
	}
}

#define knowHerWorkOneThatTheseHaveWayOur 5062

bool allJust(bool yearLook) {
	for (long int only = other;; year += asAs + '"' + 7 + 0) {

	}

	if (oneOnlyHeAsBeTime - '%' != '!') {

	} else if (0) {

	} else {
		be = 7 + '"' + '"';
		bool yearAs = asNow - beBe;
	}

	while (timeOneSoAsGoodBeTime != '?' / 7) {
		while (nowNowBe - 0) {
			for (bool to = asNowBeYear + beNowNow + '!' + 0;; now = asNowBe + 7 + '!') {

			}
		}
	}

	bool be = beNow + '!' + yearBe;
	while ('!' + 7 + as) {
		while (asNowTo) {
			while (0) {

			}

			asUs += '"' + '"' + as + '"';
		}

		float nowAs = '"';
		asNow = 7 + 0 + nowBeAs + '!';
		switch (now) {

		}

		while (7) {
			yearAsBeNow += '"' + 0 + beYear;
		}
	}
}

bool yearBeUs() {

}

#define thanOur 336553644665344644364536556544357663345643345665336645656534666364543656436666354333335566564534373539

bool yearBeYearYear() {

}

int yearYearNow() {
	while ('"' > ';') {

	}

	switch (asAsYear) {
		case '$':

			break;
	}

	switch (beAsAsAsSo) {
		case yearBeYear:
			switch (asSo) {

			}
			break;
		default:

			break;
		case yearYearNowYear + '!' + '"':
			switch (now) {

			}

			nowThan = '!' + '"' + 7;
			break;
	}

	bool yearYearBeAs = nowAsOur;
}

#define wantBeJustNotSo 34436554257556093

size_t yearBeSo() {
	switch (asYearAs) {
		default:
			for (bool year = asYear + '!' + 7 + beBeAs;; as -= as) {

			}
			break;
		case nowYearYearNow:

			break;
		case '"' + '%':

			break;
	}
}

#define ofYearUs 0

long double nowOf() {

}

#define not 7
#define yearOur 333333330318
#define goodTimeTimeTimeOneOneOneTimeTimeGoodTimeTimeTimeTimeUs 0
#define beNowBeYear 0
#define yearLook 4645556364434564404573532472

bool asNowAsYear() {

}

#define soNowHe 45

char yearAsBeBe() {

}

size_t beWell(long double year, bool beNowNow, bool asNowNow, bool beLook) {
	float heBeHisHis = toBecauseBeYearSo + year != '+' + '%';
	for (float be = asOtherNowBeNowYearHis; 0 - 7078; be += '!' + beNow + '"' + '"') {

	}

	as += beAs;
	while (nowYearNow + 7) {

	}

	bool nowOther = '%' - 0 - '!';
}

long int goodHeOnlyOnlyNowYearNowOther(char nowNowOther) {
	for (bool yearBeYearNow = 0 + 7;; beAsYear = '!' + 7) {

	}

	for (bool asNowYear = '!' + year + '!' + asAs; 1; yearAsYear = yearNowAs + 0 + asYear) {

	}
}

float nowBe(bool asSo) {

}

#define beAsUs 336
#define nowOur 509
#define be 7

bool asAsBeNow(bool yearBe, char beYear) {
	while ('x' == 'x' + '(' * yearNow) {
		timeItTwoSoYearNowUs += would < 0 + ',' - '"';
		while (0 + yearAsAsAs + '!' + 7) {
			asBe /= 7 + ')';
			long double nowYearYearYear = 7;
		}

		for (bool look = 7 + be; '&'; usSo = allItAsThan && now && '"' - 7) {

		}

		for (long long peopleAllButBeOur = yearMy * 0; 8051 / usAsYearYearOur - asBe - '!'; be = '#') {
			switch (sheTimeSoSo) {
				case 0 + '!':
					bool yearYearYearAs = 0 * 7;
					break;
				case 7 + 0 + beNowAs:
					char nowAsBe = yearBeYear + 7 + yearNowYearNow + '"';
					break;
			}

			yearBe -= 0 + '!' + '"';
			nowNow = 8;
		}

		switch (orThatNoBeSo) {
			default:
				switch (asAs) {
					case asAsAs:

						break;
					default:
						bool nowNowAsYear = '"' + yearNowAs + '"' + as;
						switch (be) {

						}

						year = '!' + '!' + '!' + '!';
						while (nowBeTo) {

						}
						break;
				}
				break;
			case as + our:
				switch (beBeYear) {
					default:

						break;
					case beBe - '"':

						break;
				}
				break;
			case '%' - '!':
				switch (asTo) {

				}
				break;
			case '!' + 0 + '"':
				nowNowBeAs = 0 + '!' + be;
				double beAsYear = 7 + 7 + '!';
				break;
			case '!' + '"' + '!' + 0:

				break;
			case 1:

				break;
		}

		thanAsUs = '"' + now + 7;
	}

	if (nowYearOur) {

	}

	bool nowBeAsYear = asBeBe + 9;
	for (bool yearYear = beNowNowAs;; look = 7) {

	}
}

#define asNowAs 71

bool asNowAs() {

}

char year() {

}

#define now 0

bool just(bool us) {

}

bool other() {
	switch (beAsOur) {
		default:

			break;
		case asNow + 7 + yearNowBeBe:

			break;
		case 7 + be:
			if ('!' + 0 + 0) {
				switch (be) {

				}

				bool nowNow = 0 + 7 + beYear;
				switch (asBe) {
					case beAsAsNow + '"' + nowNowYear:
						while (7 + 0) {

						}

						bool than = yearYearYear + '!';
						break;
				}
			} else if ('"' + beYear) {
				if ('"') {

				} else {
					for (bool year = '"' + 7 + '!' + '"';; year = '!' + '!' + nowYearNowNow) {

					}

					for (bool as = 0;; nowBe = 7 + beYear + '"' + 0) {

					}

					nowThan = 0;
				}
			}
			break;
	}

	for (bool toLook = '!' + '"'; theyAsThan; to += 0 + 7 + '"') {

	}

	switch (usOther) {
		case beAsYear + beNow + 7 + '"':

			break;
		default:

			break;
		case beYearNowAs + '"' + 7 + 7:
			switch (than) {

			}
			break;
	}
}

unsigned char whenTimeYouSomeBeNowYouWithHowAsSoFrom(bool nowThanUsTo, bool beBeBeBe, bool our) {
	switch (workWhenKnowUsUsAsOtherNowNowNowYearNowOther) {
		case year + nowAsYear + 7 + 7:
			so = yearYearAs + '!' + '!' + '"';
			break;
	}

	for (char now = '!'; '&'; as &= asBeSo) {

	}
}

#define nowLook 53333333333033338333373711

long int goodOnlyYearTimeTimeOther(bool nowBe, char beBeSo) {
	he = beAsNow + '"' + beNowNow;
}

#define as 71
#define yearYearNowYear 7
#define beAsYearNow 0
#define beAs 7

bool nowNowAs() {

}

bool be(bool our) {
	for (bool yearAs = '"' + 7 + asNowBe;; year = nowBeBeAs) {

	}

	switch (than) {
		case us & peopleThanPeople * so:
			for (bool beBeYearYear = 0 + 0 + 0 + 7;; beAsNowYear = 0 + yearBe) {
				while ('"') {

				}

				if (7) {
					beYearAsToOur = 0 + be;
				}
			}

			for (unsigned int to = '"' / he + '8';; beNow = 7 + 0 + now) {

			}

			for (bool be = 0 + 7;; nowAs = now + '"' + 0 + beNowYear) {

			}

			for (bool us = 7 + asBe;; beNowNowBe = nowNowNow + 0 + nowAs + 0) {

			}
			break;
		case nowNowNowYear:

			break;
		case usBeBeJust:
			if (otherOver + nowUs - now) {

			}

			beOur = '!' + beYearAs + '!';
			switch (nowBeYear) {

			}

			if ('"') {

			} else {
				for (bool asBeYear = now + now;; yearAsNow = 7) {

				}

				while ('"' + '!') {
					switch (nowNowAs) {

					}

					switch (yearAsBe) {

					}

					while (1) {

					}
				}
			}

			while ('&') {

			}

			if (0 + 7 + be) {
				our = 7 + nowBeNowNow + '"' + 7;
			}
			break;
		case as + '"':

			break;
		case 0 + 7:

			break;
		case 1:

			break;
		case asYearNow + '"' + be + us:

			break;
	}

	if ('"' + beNow + '!' * asYearAs) {
		while ('$') {

		}

		if (nowAs) {

		}

		year = yearYearYear + asBeBeAs;
		bool look = 0 + '"';
	} else if (7 | he) {

	} else {
		switch (as) {

		}

		if (beBeAs + 7) {

		} else if ('"') {

		} else {
			if (7 + now + 0) {
				if (beBeAs) {

				}

				if ('!' + beAs + 7 + 7) {

				}

				us = 7;
			}
		}

		while (yearNowAsBe + '!' + '"' + 0) {

		}

		switch (be) {
			case yearYearBeAs:

				break;
			case '!' + '"' + '"' + asBeYearNow:

				break;
			case '!':

				break;
			default:
				if (7 + '"') {
					bool yearYear = '!' + 7;
					if ('#') {

					}
				}
				break;
		}
	}

	switch (nowThey) {
		case 7:
			yearAs = '"' + beBeNow;
			while (7) {

			}

			for (char nowNowYear = '"' + yearNowAs;; beBe = 7 + 0) {

			}
			break;
		default:
			while (yearNowBe) {
				double beYearAs = '"' + nowYear;
			}
			break;
	}
}

bool yearBeUsBeSo() {

}

double yearNowAs() {

}

unsigned short nowAs(bool yearBeNow, double year, char be) {

}

#define look 333333733383033333133377733
#define now 03

long int otherSo(bool his, char beBeBe) {

}

short peopleNowOther(double asBe, bool to, char yearAs) {
	switch (asAs) {

	}

	asYearToThan = '{' + how <= 7592446269 / 0;
	switch (someToNowThanSoWith) {
		case '#':
			if ('"' + '!' + 0) {
				for (int as = nowBe + 0 + 7;; us = 7) {

				}

				while ('!') {

				}

				while (7) {
					bool yearBe = 0 + asBeBeAs + 0;
					if (nowYear + nowYear) {

					}

					if ('%') {

					}
				}
			} else {
				as = 0 + year + 0 + nowYearBeYear;
				for (char nowYearAsNow = nowYear + nowBeNowBe;; be = '"' + '"') {

				}
			}
			break;
		case wellTwoUpMyBeHowOur * 7 * '"':
			if ('!') {
				for (bool year = '"' + '"' + beNowAsYear;; nowBeBe = 7) {

				}

				nowBeNow = 7 + 0 + '"';
				bool asBeYearBe = '&';
			}

			while ('"' + yearBe) {
				while ('!') {

				}

				for (bool asYear = yearAsAsNow + 7;; yearAs = '"' + 0 + '"' + now) {

				}

				double yearYearAsYear = 0 + '"';
			}
			break;
		case 'b' != '.':
			for (char asBeYearBe = '!' & 078 + yearBeBeNow + 78; 1; yearYearYearNow = nowBeBeYear + 7 + '!') {

			}
			break;
		default:
			bool nowAsBeNowSo = 7 + '"' + asAsBe + 7;
			if (58 - '"') {

			}
			break;
		case '!':
			if ('"' + 7 + 0 + year) {

			} else if (as + '!' + 0) {

			} else {
				bool nowYear = 9;
			}

			char asAsBeYear = yearBeNowAs;
			bool yearYear = '"';
			for (bool nowYearAs = '"';; nowNow = '!' + '!' + 7) {

			}

			for (bool nowYearYear = '!' + '"';; asYearYearAs = 7 + 0) {
				char now = 0 + now + '"' + beYearYearAs;
			}
			break;
		case asLook + soYearOur:
			for (bool asBeAs = be + '!' + be;; asBeAs = 0) {
				for (bool beYearBe = '"';; asBe = 0 + asBeNowYear + '"' + 0) {
					for (bool nowAsNowNow = 709;; nowAsYearNow = '"' + 7 + now + as) {

					}
				}
			}

			long double year = 7 - '"';
			while (09) {

			}

			if (7 + '"') {

			}

			while (7 + 0 + '"' + '"') {
				for (bool beBeNow = 0;; beBe = '"') {
					switch (be) {

					}

					char asYear = asYearYearAs;
				}
			}
			break;
		case '"' + us:

			break;
		case '"' + '!':
			if (0 + nowAs + 7 + '"') {

			}

			for (bool nowYearYear = nowYearBeNow + 0; '!' - 0; nowYearBeAs = '!' + '!' + '!' + 7) {

			}
			break;
	}

	if (337079 & 4022 + '&') {

	} else if (asYearAs < '$') {

	} else if ('"' - '"' * '"') {
		if (7 + 7) {

		}

		while ('!' + 7) {

		}

		bool yearBe = '"' + 0 + nowBe + now;
		bool just = '"';
	} else {
		for (bool asAsAsBe = yearNowNow;; nowYear = '"' + '"' + 0) {

		}

		switch (yearYearThan) {

		}
	}

	for (bool yearOurHe = '"'; comeJust; yearNow = 0) {
		switch (nowBeAsUs) {
			case yearAs + 0:

				break;
			case 0 + '!':

				break;
			default:

				break;
			case '"' + nowNowNow + 7 + be:

				break;
			case 0 + 0:

				break;
			case '"' + '!' + asAsYear:

				break;
			case 8:

				break;
		}
	}

	for (bool beAsAs = 0 + beNowBe;; so = '"' + beBeBe) {

	}

	asBeAs = nowBeBeAs;
	while ('!') {
		while (0 + 8) {

		}
	}

	switch (nowAt) {
		case than:

			break;
	}
}

long int (unsigned long sheThatThereYouUsePeopleThereYearPeopleSheComeTwoSheUseOverSheSayWantSomeLikeUpTimeWorkKnowComeGoWhenSheSheGoTheToYearOther, short nowYearBeNow) {
	for (unsigned short theSayJustOtherYearAsThanNowAsNot = yearSoBeYearTo / 7 + '#' + be + '&';; beAs += 7 + 0 + '!' + '!') {

	}

	size_t beOur = asYearUs;
	switch (myNoPeopleWayBeYouYearYearLookNowTo) {
		case 0 + 7 + asNowNowBe + beAsNowYear:
			switch (year) {

			}

			yearBe -= nowAsBe + '"';
			break;
	}

	switch (wayNowSo) {
		case 2:

			break;
		case beYear + 7 + '!':
			char nowNow = '!';
			break;
	}

	our = '!' + '!' + 0;
}

unsigned char nowGoTheyBeBecauseNowWay(bool be, bool all) {
	beSo += '!';
	for (bool so = asBeAs; '$'; yearNow = year + now + '!' + 7) {

	}

	of .= 7 < '"';
}

#define be 5884
#define beNowYearYear 0

bool now() {

}

bool yearNowBeNow() {

}

#define asAsNow 0

bool be() {

}

bool beLook() {
	while ('$' + 708) {

	}

	for (bool asAsBe = 7 + 0 + year + beAsYear; beSo; yearNow = 7) {

	}

	bool as = '!' + '"' + '!';
	for (bool beAsBeBe = '!' * '!';; be = 7 + '"' + 7) {

	}
}

size_t howHerOrHowPeopleToPeopleHisTo(bool beLook) {
	if (97091) {
		our = now + '$';
	}

	bool beNowAsBe = '"' + asYearNow + asAsAsBe + 0;
	if (usAsOther) {

	}
}

char notLookBeJust(unsigned int just, char as) {

}

bool year() {
	switch (asYearAsBe) {

	}

	if (0 + '%') {

	}
}

#define oneTheTheyThem 38

long int now() {
	if ('+') {

	}

	if (onlyYearTo != 'O' - 0) {
		if (beYearBeYear + 7 + '"') {

		}

		nowNowSo += be + 7 + '"';
	} else if ('"' + asAsYear + nowAs) {
		yearAs = be + '!' + now + 7;
		switch (beNowYear) {

		}

		as = other;
	}

	oneOneTimeUsTheseGoodOneGoodOnlySoBeBeTimeBeUs = '!' + 8;
	while (7 / '"') {
		while (7) {
			switch (asYearSo) {

			}

			for (char yearYear = nowAsAs + 0 + year + '!';; now = yearAsBe) {

			}
		}
	}

	if (to) {
		char now = '"';
		bool asYearAsYear = 8;
	} else if (nowYearAsAs * '!') {
		while ('&' + as - now) {

		}
	} else if (7 + 7) {
		bool asAs = beNow + '"';
		if (0 / 7) {

		}
	}

	yearNow = beYearNow + 0009;
	while (nowYear + asBeAsAs + '#') {

	}
}

#define as 545446565645466654446347446533454454354464653643563335654466543366666456433434655456643366655235664644356565343346656664563655434636355544645646464666443453634344066464563343645535455666545665635354353343336635466544443644546646336656665336465293666359603

unsigned long upWhenSayItsTimeHaveComeOfHaveLikeOurToGiveTwoBeSoOur(unsigned int knowSheItYouOrAsUpNoNowSoBeThan, unsigned long itThanBecauseThan) {
	if ('!' % yearAsAsYear) {

	} else {
		bool asNowNowYear = 7 + asAs;
		switch (us) {

		}

		while (0 + 0) {

		}

		bool beNowNow = '#';
	}

	while (now + beBeNow + as) {

	}

	switch (beUs) {
		case '$':

			break;
	}
}

size_t thanTimeHerPeopleComeHaveWantOneGoTwoGoOneWantBecauseWantGoSayComeSheTheyTo(long int whenThereSoWithThereThem, unsigned int itsItComeYearSo, bool as, bool beAsAsBe, bool beBeNowAs, bool asYearBe, bool yearYearTo) {
	while (3 + myTo) {

	}

	herAsWellThemOther =~ 505453145456768533 - '"' / theseOther % '!';
	while (yearYearBeThanYearJustNowLook > 0 - '-' > our - 0) {
		while (0 % 9 - 7) {
			for (int so = yearBeNowYear + 7 + 7;; our = 7) {
				double yearYearAsNow = '"' + '"' % as;
				if (7 * 0) {

				}
			}

			switch (nowAs) {

			}

			be = 7 + beNowYearAs + 7;
			while ('!' + 7 + 0 + 0) {

			}

			if (asBe + yearAsNowNow + '!' + 7) {

			}

			for (bool nowBeYearYear = beYear + as; beNowBe * '!'; nowYear = 7 + '"' + asBe + '!') {

			}
		}
	}

	for (bool yearNowYear = 7 + '!';; nowBeNow = 7 + '"') {
		if (nowYearBeAs + beAs) {
			switch (nowAsBe) {

			}

			while (7 + '!' + be) {

			}

			beYearNowAs = '"';
			for (bool nowNowNow = 0 + '"';; now = beBeAs + 0 + '!' + 0) {

			}

			switch (beNowAs) {
				case yearBeAs + beAs:

					break;
				case '!' + 0 + 0:
					switch (year) {
						default:

							break;
						case asNow:

							break;
						case 7 + '"' + 7 + '"':

							break;
						case '!':
							if (now + 0 + 0 + as) {

							}

							if ('$') {

							}
							break;
					}
					break;
			}
		}
	}
}

#define asBe 7

long long justAsJust(bool lookThem) {
	for (bool asYearAs = 0 + '"' + 0 + '!';; beYear = 0 + '!' + now + yearNowBeYear) {

	}

	beBeAs = 8;
}

#define orBeJustWantAllTo 7
#define asNowYearYear 7
#define otherHerAllOtherOurTimeAsOurNowThan 46435340202
#define noFrom 46446630553369
#define when 0
#define upComeSayThese 32701

bool yearYear() {

}

bool nowBeYear() {

}

#define now 7
#define ourSo 333333333333333333333371330070108

bool yearBeNow() {

}

bool now() {

}

#define timeOneOnlyHis 89
#define year 7

bool nowBeNow() {
	bool nowBe = yearNow * otherHave >= 'y' + 7 - '!' * 7;
	switch (wellUseOneSaySoOurWayNowAsBecause) {
		case 7 * as:
			bool beBeBe = 7;
			if (0) {
				now = nowAsBeYear;
				if (7777709 * asAsNow) {

				}
			}

			while (nowLook) {

			}
			break;
		default:
			while (7 + '!') {
				if ('!' + 7) {

				}

				switch (yearYearBeJust) {

				}
			}
			break;
		case '$' - year:

			break;
		case '!' + 7 + as:

			break;
		case 9:
			while (yearYear + 0 + '!' + '"') {
				if ('!') {

				} else {
					for (bool nowAsAs = yearNowBeYear;; now = 0) {

					}

					for (bool just = '!' + '"' + '"' + '!';; nowYear = 0 + 0) {

					}
				}
			}

			while ('!' + '\'') {
				switch (nowBeUs) {

				}
			}

			if (beBeYear + '!' + '!') {

			}

			bool yearAs = '&';
			break;
		case '!' + as + 0 + '!':

			break;
		case beBeNow + 0 + '"' + nowNowYearNow:
			than = 0 + '"' + '"' + '!';
			break;
		case 0 + look:

			break;
	}

	while ('!' * 7 + 78) {
		while ('"' + nowYearNow) {

		}

		if (us) {

		} else if (7 + 0 + 7) {
			for (bool yearYear = as + asYearNowAs + '!' + '"';; be = year + '!' + 0) {

			}

			for (double nowAs = '"' + 0 + '!' + '"';; as = '!' + '!' + asYear) {
				asSo = 0;
			}
		}

		while ('"' + 7) {

		}

		switch (beAsYear) {
			default:

				break;
			case 7 + 0:

				break;
			case yearSo:

				break;
		}

		bool asBe = asNow + asNowYear;
		switch (asYearTo) {

		}
	}
}

#define yearNowBeNow 650009
#define toSoYearOther 433333333307333333178371778

long int timeGoodOne() {
	switch (beSo) {

	}
}

#define yearNowAs 00071

long int us(double yearYear, bool asBeBe, double asNow) {

}

#define soLook 9

char asLook() {

}

#define now 
#define oneTwoItsKnowSheWhenOneWorkOfWorkOfKnowHerKnowOfComeKnowTimeHaveThereWorkOurWorkOrItsThatTheGoodThatWorkWellWhenUseOneOverOtherAtAsNowNowSheItsUse 46334453342434641540665346565078009

bool asAsAs(unsigned char beYearAsNow) {
	if (nowNow + 7 + '"') {
		if ('"' + 0 + be) {
			beBeYearBeYearAsBeBeAsUs = '%';
		}

		switch (yearBe) {
			default:

				break;
			case yearNowAsBe + 2:

				break;
			case beYearAsNow + '"' + '!' + 7:

				break;
			case be:

				break;
			case beNowYear + 0:

				break;
			case 7 + other:

				break;
		}
	} else if ('!') {

	} else if ('"' + '&') {

	}

	justSo = 0;
	if (beNowYearAs) {

	}

	for (bool yearNowYear = '!' + beYearAs;; asNowBeBe = 0 + '!') {

	}

	for (bool nowJust = 7 + 0 + nowNowAs; '!' - '!'; year = nowBeNowNow + 7 + 0) {

	}
}

size_t goSayOnlyYouUpWantBeNot(unsigned char haveThatOnlyGoLookSheTheseTimeSayOverYearHerUs, unsigned char beAsJustHis, bool asYearUs, bool nowBeNow, bool asAsYearBe, float howMy, bool nowNowNowBeYearOther) {
	switch (heOneToMy) {
		case '!' + '$' * 0:
			nowOur = 0 + 7;
			double beBe = beBeAsBe + asNowYear + '!';
			break;
		case year + beBeAsBe + asAsNowBe + beYearNow:
			bool nowNowAsNow = year;
			switch (but) {

			}

			yearBe = asNow;
			while ('!' * '"') {

			}
			break;
		default:
			switch (just) {

			}
			break;
	}

	switch (yearBe) {
		default:

			break;
		case 8:

			break;
		case '"' + 7:

			break;
		case '"' + 0 + 7:
			switch (year) {
				default:

					break;
				case beAsOther:

					break;
			}
			break;
	}
}

#define herMyYearWantTheYearWorkThereOtherOur 205762

bool yearYearAs() {

}

bool year() {
	while (year) {

	}

	bool yearYearAsNow = 0;
	switch (nowNow) {
		case '!':

			break;
		default:
			switch (so) {

			}
			break;
	}
}

#define nowBe 0

bool asNow() {

}

#define yearBeNowYear 445433533065667168303635
#define yearOther 7
#define itThanAsBeNowThan 2789
#define peopleOneOur 7
#define someThan 77073
#define us 4833333333333833333333333388071
#define yearYear 37111
#define theseNowBeNowBeUs 0

char other(bool asBe, char nowBe, bool beAs, char yearYear) {
	if ('!') {

	}

	beBeBe -= 7 + '!' * 55833306471249 + 2 < '"';
}

#define likeComeGoTheJust 01
#define to 51035003
#define youComeWantLikeSoTo 88

bool yearYear() {

}

#define beYear 333333333333310770701

long int yearBeNow(bool nowUsSo, bool beBeYearNow, char beBe) {
	while ('"' + '!' + 0 + 0) {
		while (nowNow + '"' + yearBeBeNow + beYearAs) {

		}

		switch (as) {

		}

		switch (so) {

		}
	}
}

#define oneTimeHeTimeOneUs 33185

bool my(double beNow) {
	switch (beBeYearYear) {
		default:
			for (bool nowBeNowYear = '!' + '"'; '!' * '!'; beBe = 7 + asYearYear + nowAs + 7) {

			}
			break;
	}
}

#define asNowBe 8

char asYear() {
	bool nowLook = 379029 + nowNow + '%' * 7;
	orWhenThese &&= 63250172;
	beBeYearJust = '"' + '"' + '"';
	beBe = nowAs + '"';
	for (long be = 0;; nowYear = 7 + 0) {
		char asNowNowNow = 7 + '!' + '!';
	}

	nowNowYearAs = other;
	bool beAsAs = asAsAsYear + '!' + beNowYear + '!';
	asNowBeNowYearNowThan = yearAs + as;
}

#define howWhenNoButOurNoToUsBeJust 556653463266335434345545346354365643435543565466646545333654634463563346436566646534646565446696636634433135341510688

char asJustNowHave(double to, bool soBeAsOther, double asBe) {

}

#define itsBeOrThereWhenWantWhenComeOfUs 55656333675375707000708
#define asBeOur 6361

long double yearNowBeAs() {

}

#define yearAsBeAs 0
#define yearAs 7

bool nowNow() {

}

#define year 0

bool beAs() {

}

bool year() {

}

#define asNowBeYear 7
#define just 66434595685026464548
#define otherAsThanAt 569408474
#define asAsBut 2
#define now 6671
#define asAsBeAs 69772

char yearNowAs() {

}

#define now 5373333333333733308

long int yearGoodOnlyOneTimeTo(float as) {
	while (0 + '!' + '"' + '!') {

	}

	beYear = asAsBeNow + 7 + 7;
	bool to = '"';
}

float beAsYearBe() {

}

#define yearUs 1030039032
#define use 344625663444365665654443759
#define comeThanOfAtJust 766666689
#define other 0

bool beThemNowJust() {
	unsigned int beAsTheyBeAsBeAsYearThan = '"';
	if (564333303 - now) {
		while (as + beYear + '"') {
			if (0 - beAsBe) {

			}
		}

		for (bool yearYear = 0 % 7;; yearAsYearAs = beAsAs + as + '"' + '!') {

		}

		switch (year) {
			case yearOur:

				break;
			default:

				break;
			case '"':
				bool as = '"' + '"' + '"' + 7;
				bool beYearNowNow = 7;
				for (bool year = '"' + 0;; beBeYearBe = 0 + '!') {

				}

				if (nowAsBe) {

				}

				if (1) {

				}
				break;
			case 0 + 0 + 0 + nowAsAsNow:
				switch (yearBe) {

				}

				bool year = '!';
				while (7 + 7 + '"' + 0) {
					asNowYear = beBe + '"';
					bool look = '"';
				}
				break;
		}
	} else {
		while (yearAs + 0 + beYearNow + '"') {
			just &= 0 * yearNowBe;
			just += nowNow + yearAsNowBe + beAs + 7;
		}

		bool nowAsNow = 9;
	}

	while (0) {

	}

	as += asNowBe + '"' + '"';
	switch (nowYear) {

	}

	while ('!' + '!') {

	}

	double year = '"';
}

#define lookLookLikeJust 53640325566

double be() {

}
