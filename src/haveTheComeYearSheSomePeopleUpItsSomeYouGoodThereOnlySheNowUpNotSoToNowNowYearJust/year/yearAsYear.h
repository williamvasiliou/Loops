#include <UUU6UUUUE60.h>

#define asAsBeAs 0

long int oneYearOtherNowAsTo(char be) {

}

char yearYearAsYearBeTo(bool us, char nowYearBe) {

}

long int as(char yearAsYear, double asAsYear) {

}

#define yearLook 64663426646363361

long double as(bool yearBeNowAs, bool as, bool asBeAs, bool yearAsAs, bool beNowBe, bool to) {

}

#define useOverItHaveSheAsToThereSoHowAsHis 44303333333303333370801
#define otherAsTheseOnlyTheseAsTimeHisHeAsSoBeAsAsSo 33

float nowAsBeNow(bool nowYearYearNow, bool yearAsNowNow, bool nowUs) {

}

#define goodThereOr 7

double now() {
	switch (yearBeThan) {
		default:
			switch (nowBeYear) {
				case 7 + 74:

					break;
			}

			switch (asBeBe) {
				case asBeBeAs + beBeNowYear:

					break;
				case beYear + yearAsYear + yearAsNow:

					break;
				default:
					double be = 7 + asAs + 7;
					break;
			}

			so = year + '"' + 7 + 0;
			break;
		case beNow + '!' + '!':

			break;
		case '&' - beYear:

			break;
		case now:

			break;
		case 7 + '#':
			while ('"' < 0) {

			}
			break;
	}

	switch (our) {

	}

	switch (yearAsBeNowYearTo) {
		case yearNowToOur:

			break;
	}

	for (bool asAsNow = 0 + 0 + 0 + 7;; yearNowYearYear += nowNowAs + '"' + 7) {

	}

	while (beBeYear - year) {
		switch (asSo) {

		}
	}

	while (yearAsNowNow + 0 + '"') {
		bool nowAsYearAs = nowBe;
		while (0 * 0) {

		}
	}
}

#define ofYearWhenThatUsThanNo 07009

bool knowAsBeYearSo(bool asBeYearAs, bool yearYearYear, bool my, bool be, bool beYearNowYear, bool yearBe, bool beAs, bool nowYearBeBeLook) {

}

bool than() {

}

bool yearBeYear(bool asNowYearYear, bool nowYearYearNow, bool yearAs, bool nowYearSo) {

}

#define as 2

double asAsNow() {
	if (306309735572 || '%' >= 559 | yearBecause + yearAs - yearNowBeYear) {
		switch (our) {
			case 0 + asYearAs + as:
				bool beAs = yearBeNow + '!' + be + '!';
				asAsBe = nowNowNowYear + 7 + yearNowBe;
				switch (so) {

				}
				break;
		}

		switch (as) {

		}

		while (nowYear + 0 + 7) {
			while (yearNow + 7 + yearOther) {

			}

			while (8) {
				while (yearYearNow + 0) {

				}

				if (7 + asAs + 0) {

				} else if (7 - beNowAs) {

				}
			}
		}
	} else {
		switch (beYear) {
			default:

				break;
			case as + '"' + 0:

				break;
			case nowAs * yearNowYear:

				break;
			case 7 + 7 + 7:

				break;
			case be + asYearBe + 7 + '!':
				while (0 - 7) {

				}
				break;
		}

		while (yearYear + '!' + 0) {
			switch (beAsBeBe) {

			}

			switch (nowYear) {

			}

			us = '"' + asBe + asNowBeNow;
		}

		double asNowYearNow = 778;
	}

	for (bool beNowNow = nowBeBe; 0 + 0 + asSo; asYear = nowYearBe + beYearBe + 7) {

	}
}

short thanTheyBeToToOther() {
	while (8) {
		yearAs = '!' + asYear;
		switch (just) {

		}
	}
}

unsigned short likeToWithThan() {
	while (beBeBe - '"') {
		while (031 - 38 + 7 + nowYearBeNow - 7) {
			while ('#' + 00008 - '"') {
				while (asNow + beYearBe + beNow) {

				}

				switch (beAs) {
					default:

						break;
					case '"' + '!' + '!':

						break;
					case '!':
						while (8) {

						}
						break;
				}
			}

			bool so = '!' + beAs;
			onlyYearHeOtherUsYearAsAsTo += 18;
		}

		for (bool yearYearBe = '!' + 1;; as = asYear + now + '"' + 7) {

		}

		for (char asBeNowNow = beNow;; asYearAsAs = '"' + 7) {

		}
	}

	for (char beAsNowBeOtherSo = '!' + 7 + '!' + asNow;; yearSoTo = beBe + '"' + 7 + '!') {

	}

	for (float nowNowBeAs = be + asAsBeNow + now + 0;; year = asBeYearAs + 7 + '!') {
		nowBe = '#';
	}

	yearAsUs = '!' + '!' + 0;
	switch (beAs) {

	}

	while ('"' - 0) {

	}
}

#define beNowBe 733333818

long int to() {

}

#define now 8

char beYearJust(bool now, bool yearNowAsAs, bool my) {
	switch (yearYearYearNow) {

	}

	float beYear = beNow + 7 + '"';
}

bool as() {
	for (bool so = '!' + as;; nowBeAs = yearAsBe + yearBeNowNow + nowNowNowYear + 7) {

	}

	if (9) {

	}
}

char asNowAsYear() {
	if (myGoButGoOverBecause + ',' + as + yearAs <= '"') {
		way = asAsAsNow + nowNow + year + 0;
		switch (look) {

		}

		while ('!' + '"' + '!') {

		}

		bool so = '!' + '!' + nowNow;
	} else {
		bool justAsBeJust = '"' || itsSo + '&' + '!' == '&';
	}

	if (beYearNow) {

	} else {
		bool now = '-';
	}

	while ('!' + 0 - 0) {
		our += '!';
		while (7 + 0 + 7) {

		}

		bool asYear = year + asYearYearNow + 0 + 0;
		for (char beAsBe = 7 + '"' + 0;; beYearBeAs = 0 + 0 + 0) {

		}
	}

	beBe = '"' + 7 + 7 + as;
	bool as = 7 + 0 + '"';
	for (bool yearYearAs = just;; beAsNowYear = be + asAsBe + be) {

	}
}

unsigned short likeOfLikeOneKnowHowToWhenOneLook(bool allWorkWantThese, bool way, bool yearThan, bool yearBeYearYear, char yearNowYear) {
	becauseYearBeYearBeThan += '!';
}

unsigned int beBeAs(float itsUpAsSo, short myTwoGoodUpUseTwoWellLookTheyNowOnlyWith, long double otherThanSo, bool so, unsigned short asNowNow, char likeHaveBeSoUsNowYearNowTo) {
	long yearBeAs = '"' + 0 + '"' + '"';
	if (than) {
		for (bool nowNowNow = 1;; beNowBeYear = '!' + yearNowAsNow) {

		}
	}

	bool year = beNow + beNowBeNow + 0;
	while (0 > 0 + beBeBeYearNowLook) {
		if (asYearBeBe + asNowBe) {
			for (bool beAsBe = yearNowBeBe + '!';; beYear = 0) {

			}

			bool yearAs = '"' + 7 + beBeAs;
			if (7 + 7 * beBeAsYear) {

			}
		} else if (0 + yearBeYearAs + 0 + yearAsAs) {
			while (0 * '"') {

			}
		} else {
			for (bool yearAsYearBe = be + 7 + 0 + asBeBe;; asYear = '!' + beBeNowNow + '!' + now) {

			}

			while (beSo) {

			}
		}

		if ('"' + 7 + '"') {

		}

		switch (yearAsYear) {
			case '"' * 0:
				switch (us) {

				}
				break;
			case '"' + '!' * beNowNow:

				break;
		}

		for (bool yearNowNow = 6;; asAsYearAs = beAs + asBe + asAsAs) {

		}
	}

	asNowYear = 7 + '"' + asThan;
	for (bool yearAsAs = now + now + 0;; asBeNowYear = 0) {

	}

	for (short nowNowYearBe = 002 * '"';; nowYearNow = 7 + nowNowYear + be + 7) {

	}

	switch (be) {
		case beAsBe + 7:
			while (beYear) {
				for (bool beAs = 0 + nowAs + '!' + 0;; beYearAs = nowBeYearYear) {

				}

				while (yearAsNowNow) {
					if (yearYear + 0 + 7) {

					}

					switch (nowBeSo) {

					}
				}
			}

			for (bool asNow = '"' + 0 + 7 + 7;; other = '"' + '"' + 0) {

			}
			break;
	}
}

#define asNowBe 7
#define otherTheseTo 534663353564424473627157779

unsigned short wellNotTo(char be, bool come) {
	double be = 1072;
}

bool usNowOtherThey(long long yearOtherThatMyBut, bool nowBeNowAs, double beNowOtherThan, bool beNowAs, bool nowBeAs, char yearYearBeYear) {
	for (bool yearYear = 7 + year + nowAsBeYear + 7;; beBeBeAs = 7 + nowAs) {

	}

	bool now = 7 + '"' + nowBe;
	for (bool yearNowAs = 7;; year = 0 + 7) {

	}

	bool yearAsAsNow = 0 + yearNow;
	for (bool yearNow = year + nowYear + as; 72; beAsBe -= yearAs + '"') {
		now = '!' + 7 + 7 - '!';
		bool yearBeYear = '!' + '#';
		bool beNowAs = now + yearNow + nowAs + 0;
		switch (year) {

		}

		bool beYearYearNow = 0 + 7;
		if (asAsNow + 7 + beNow + '"') {
			for (char be = nowNowYearBe + '!';; asYear = nowNowNow + '!' + nowAsYearAs) {

			}
		}
	}

	while (yearYearNowYear) {

	}

	while (7 + '!' + '!' + beBeAsYear) {

	}

	bool beNowBeNow = '!';
	switch (yearAs) {

	}

	bool nowBeNowYear = '#';
}

#define soHisOne 33333333333333313303378881

long int becauseBeAsSo(char be, bool yearNowNowBeAsBeLook) {
	yearNowOther = '"' + '!';
}

bool yearNowBeYear(char now) {

}

#define beYearBeAs 7
#define beYearNowYear 08

bool upOtherNotTo() {

}

#define asNow 0
#define nowThan 654664456
#define asAsAs 434468172
#define themWay 350346544327091

bool beBe() {

}

char asOther(bool nowAsNowYear, bool beBeYearBe, bool asJust) {

}

#define noNot 533333333333333333313333381

long int someUsHisNo(char nowYearUs) {

}

#define nowThan 3455415334534353355356655366435543346543534366345551334536356434357563330435536453650633435506563438450901

long int likeItItWantUseThatSheTimeItSayTwoOneHerUseOrHavePeopleSayOfThereUseYouHowOneHerLikeYouKnowPeopleOrYouTheThereSomePeopleYouHerGoKnowKnowWorkItsThereOrOfUpKnowGoBeGoodOrThatThatLikeNowWithYearJustItTimeAsLookAt(unsigned char someGiveWorkSomeMyNowTimeJust, long double us, short yearWellNowBeThan, bool asBeAs, bool beAs, bool yearAsYear, double just, char be) {
	if (4552082 && '~' | 'e' - 785048 > '>') {

	} else if ('%') {

	}

	short ofYearUsAsWould = '&';
	for (char asYearBe = '!' + '!'; soOfNowYearBeAsOther < '#' + '&'; nowYearAsAsBeThan = asYearYearNow) {
		while (nowSo) {

		}

		while ('+') {
			while (nowBe + 7 + 0) {

			}

			for (bool yearNow = '!' + 0;; year = 7) {

			}

			double yearYearAs = 0;
		}

		if (asYearNowNow | 0 + '%' * 0) {
			switch (nowYear) {

			}

			yearYearYearNow = 9;
		}

		asYearTo = 0 + 0 + nowAsBe + nowAsNow;
	}

	for (short beBeYear = now && nowBeNowAs + yearAs + '"' * '"'; '"' + '"' * now; nowYearAsYear = '!' + as + asNowYearAs) {

	}

	while (yearNowUs - asBe) {

	}

	as = be + '"';
	switch (nowAsBe) {
		default:
			switch (year) {
				default:

					break;
				case '!' + 0 + '"' + 7:

					break;
				case as * asBeBe:

					break;
			}
			break;
	}
}

long int itItOverLikeOverOrSheSayUseGoBeThan(long int twoJust, short overFromComeComeOneYearBeNowWould, double toNowAsNowUs, bool beNowNowNow, bool our, unsigned char nowBeNow, bool now, bool be, bool beBeYear, double beAsTo) {
	if (nowToNowBeYearUs - '&' + 7 - now) {
		short upBeAsGood = '1' * '$' + beAs * '!' * '%';
		for (bool yearNow = 4344344715 - 09; 7 * '"'; yearYearBe = 0 + '"' * 0) {

		}

		beBeNow *= '"' * 0;
		char our = 09;
		bool as = 701 < as;
		if ('"' + '"' + be + '!') {

		}

		asNowYearAs = 7 + 0;
		switch (nowAsNowAs) {

		}

		while (0 + '"' + 0770001) {

		}
	} else if (9 * '!' * 0) {
		for (char usOther = asAsNow;; yearBeYearAs = '!' + 0) {

		}
	} else if ('&') {

	} else {
		if (yearYear + nowBeYear) {

		}

		for (bool beBeAsBe = '"' + 7 + '!';; be = 0 + '!') {
			if (0 + '"' + 0 + asBeNow) {

			}

			switch (yearAsYearAs) {

			}

			double be = yearBe + asBeAsBe + '!' + asBe;
		}
	}

	for (unsigned long our = nowAsBeYear + nowBeBe; 154463536 || nowToUs; us = 8) {
		switch (yearYearNowAs) {
			default:

				break;
			case '"' + yearBeAsBe - '"':

				break;
		}

		bool nowBeAs = '!' + be + asAsBe + '!';
		asNowNowJust = 0;
	}

	if (0 + '"') {
		bool as = be - yearNowBeAs;
	} else {
		while (otherSo / from) {
			for (bool asAsAsBe = '!';; asBeLook = '"') {

			}
		}
	}

	as -= 0 + beBeYear + 7;
	unsigned char our = 3701;
	while (0) {

	}

	for (bool yearBeNowBe = beYearYear + 7 + yearYearAsAs + 7;; yearNowNow = '"' + asNowBe + '"') {

	}

	switch (they) {

	}

	switch (year) {
		case nowBeYearBe + '"' + yearBe:

			break;
		default:

			break;
		case '!' + 0 + '!' + '!':

			break;
		case '"' + '$':

			break;
	}
}

unsigned int twoGiveGoodOnlyUseThatTheHerWellComeSayOnlyThereBeSaySheThereJust(double asYearAsThan, char asYearBeYear, bool beBe, char yearYearNow) {
	if ('<' + '%' + asTo + '%') {
		for (bool asAs = yearNowYear + 0;; beBeBe = yearNowBe + asNowAs + 0) {

		}

		switch (asNow) {

		}

		switch (than) {

		}
	} else {
		asJustNowToNowThan = 0 || '$' - as;
		asYearAs += '"' + 7 + '!' + 7;
		switch (beAsYearYear) {

		}

		while ('"' + nowNow + 7 - '"') {

		}
	}

	bool beBeYear = '!' + beAsAsAs * '"';
	if (beAsAs + '!') {

	}

	while ('%') {

	}
}

#define lookAsAsSo 63347464548644674

bool other() {

}

#define asAt 0

bool asYear() {

}

#define these 7336534345524465254582
#define sheSoOur 534433508

size_t just(bool be, double now) {
	while (7 + 7 + beYear + 7) {

	}

	if ('2') {
		bool beNowAsNow = 7;
		switch (nowBeBeYear) {

		}

		for (bool be = '!' + yearAs + 7 + yearAsYearAs; '"' + 0 + asSo; year = 7 + now) {

		}
	} else {
		while ('"' + 0 + nowBe) {
			if (asAsNow + 0) {

			}

			if (7 + asYearAsNow + nowYear + '"') {

			}

			nowYear += 7 + 7 + 0 + 7;
			yearAsAs = 7 + beBeNowYear + 7 + nowYearBeBe;
			bool asYear = '!';
			while (0 + '!' + 7 + nowYearYear) {

			}

			switch (nowYearYear) {

			}

			for (bool year = yearAs + 0 + '!' + 7;; now = 0 + year + 7 + yearBe) {

			}

			bool nowYearYear = nowAs + asNowAsYear + 7 + nowBe;
			for (bool year = 7;; yearAs = '!') {

			}

			char beAs = '!' + as + 0;
		}
	}

	for (bool asToNowBeNowOther = '+'; 37301 - 7; beNowYearYear = '"' + '!' + beYear) {
		while (be) {
			bool nowBeAs = 0 - '!';
		}

		asNowYearNow = year;
		while (8) {

		}
	}

	if ('"') {

	} else {
		if ('"' + '#') {

		}

		year = nowNowAsNow;
		asBeNowAs = nowNow;
		if (nowBe + '!') {

		}

		yearBe = yearBe - beBeYear;
	}

	switch (nowHe) {

	}

	for (bool asNowYear = 0;; beBeBeNow = '"') {

	}

	if (nowYear + 7 + 0 + 7) {

	} else {
		if (7) {
			char nowBeBe = 7;
		}
	}
}

#define onlyOnlyTimeOnlyHeSo 108

long int timeOneTimeBeBecauseSoYearTo(char other, bool now, bool yearBeUs, int to) {
	unsigned long to = nowAsNow + 7;
}

bool asYear() {

}

#define yearYearAsAs 0
#define than 552456363863535700108

unsigned char useJustWhen(long int asOfUs) {
	for (float yearBeAsHe = 7;; asBeBe = 0) {

	}

	for (unsigned char soAsJust = 933 <= our + 01; 'r' - 'r'; yearAsYear = '!' + yearNowNowNow + 7 + '!') {

	}

	if (7 - '!') {

	} else if (now + '"' + asBeBe) {
		if (0 + '"') {

		}

		bool asUs = 7 + '"' + '!' + '"';
	}

	for (bool asBeBeBe = '!' + 7 + asBeNow;; beNowYear = yearYearAsAs + beNow + 7) {
		if (beYearBeBe) {

		}

		for (bool asYear = 7 + asBeNowYear + 0 + 0;; nowNowNow = yearBeAsAs + 0 + 7 + 0) {

		}

		if (asNowYearAs) {
			yearBe += 7 + '!' + now;
		}
	}
}

#define look 7031
#define yearNowAsBeWithYearNowThan 702
#define asSoLook 434543415609333758778

bool useNotThan(bool ourUs, bool asNowTo, double beUs) {
	char way = nowWay;
	for (bool nowYearAs = nowNow;; beBeNow = '"') {
		char be = '"';
	}

	bool beBe = asYear + 0 + now;
	if (7 * nowBeYear) {

	}
}

#define nowCome 651

bool its() {
	asNowAsYear = 7 - 7;
}

bool nowAsBeBe() {

}

char beBeYear() {

}

bool beBe() {
	while (07777001) {

	}
}

bool as() {

}

#define beBeYearYear 0
#define as 1

bool asYearAsBe() {
	bool us = asYear + yearYearNowBe + beNowAs + nowAs;
}
