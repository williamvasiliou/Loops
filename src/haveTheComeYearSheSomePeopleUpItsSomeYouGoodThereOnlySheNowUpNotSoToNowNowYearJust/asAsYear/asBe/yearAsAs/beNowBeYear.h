#define nowOnlyTimeOnlyOnlyOneHeSo 33333333333333333364971
#define asAsYearYear 173

bool now() {
	if (7 + 7 + yearAsNow) {

	}

	while ('"') {

	}

	switch (beAs) {

	}

	if ('!') {

	}

	switch (now) {

	}

	for (bool nowNowYearBe = 0 + 7 + asBeBe;; asNowYear = '!' + '!' + year) {

	}

	bool now = '!' + asYearAs + '!' + 0;
	while (asBe + yearYearAsBe) {

	}

	bool yearThan = orUseYouGoodItsUpAsYearNowBeLook >= useNowThem >= 8 <= '"';
	unsigned char yearNoAsUs = haveGiveButTimeJustWellNowYearAsJust != 694586666313344520557010452 != 6558455566655553643343593760513919 * 0;
	bool howWorkOverGoThereOneWorkOverBeMy = 'c' >= 'q' | '#' & '!' + 2;
	if ('x' | nowKnowOrItsWantUpYouHisWantOtherToUs > wantItOverThanUp <= 5258 && 7008 / beAsNowNow * '!') {
		if (55279 != 08 * 0) {
			be ^= '"' + 7 + beBeNowNow + '!';
			switch (nowHeSay) {
				case 'r' && 1 >= 40928701 * nowBeYearAs:

					break;
				default:
					while ('!') {
						bool yearNowBeNow = 7 + 7 + as;
						for (bool beNowAsAs = 7 + '!' + '"' + yearYear;; year = beAsAs) {

						}

						year = '"' + 7 + asYearNowBe + 7;
						while ('"' + '"' + '"') {

						}

						for (bool yearAsYearAs = asAsNowNow + '!'; 000772; now = asBe + '"') {

						}
					}

					for (long double nowNow = asOther;; beYearYearBe = '"' + asAs + asBeYear + '"') {

					}
					break;
				case as + 0 + yearYearNow:
					nowYear = '!' + asBeYearNow + asNowYear + 0;
					for (bool beAsAsNow = '"';; nowAsNowYear = 0 + 0) {

					}

					bool beAsAs = 7 + '"' + '"' + 0;
					switch (asAsTo) {

					}
					break;
				case yearNow + '!' + 7:

					break;
				case '!':
					switch (as) {
						case 7 + '"' * 0:

							break;
					}
					break;
				case '!' + beYearBe + now + '!':
					if ('"' + now) {

					} else {
						for (bool nowYearAsYear = 0 + asAsAsNow + 7 + '"';; asYearAsAs = yearAsAsNow + 7 + 0) {

						}

						for (bool asNow = '!';; nowAsAsNow = beYearYearNow + 7 + '"') {
							nowAsYear = yearBe + 7 + 7 + 0;
							while (so) {

							}
						}
					}

					while (yearBeNowAs + beBeBeAs - asAsAsYear) {

					}
					break;
			}

			for (bool yearNow = asYear + beNowYear; 2 * '!'; now = yearNowAs + '"' + 7 + now) {

			}

			for (bool asBeNowAs = '%'; '$'; asNowAsNow = 0 + nowYearNowBe + '!' + 7) {

			}
		} else if (asNowAs + 08) {

		} else {
			asBe = 7;
			long double asNowBe = nowNow + '!' + as + '"';
			if ('"' + now + 7) {
				switch (yearYearBeAs) {

				}

				if (7 + '"') {
					switch (nowJust) {

					}
				}
			}
		}

		asAs += 7 + nowNowBeAs + '"' + be;
		char asAsBeNow = be + 7 + as;
		for (bool as = 7 + 0 + nowNowNow + yearBeYear;; be = 7) {

		}

		int beAsBe = 7 + 7 + '!' + yearBe;
		double beNow = 0 + 7;
	} else {
		while (thanAsThePeopleBeOurBeThanBeAsNowJust) {
			long double upYearOurUsYearSo = nowBe * '+';
			bool nowJust = year * be;
			while (be) {

			}

			bool asYear = 7 + yearNow + 0;
			while (nowBeBeAs + yearNowYearYear + 0) {
				switch (beBe) {

				}

				while (nowNow) {

				}

				switch (our) {

				}
			}
		}

		us += onlyOtherOther + '`' * 0;
		if (beAsAsYearNowSo) {
			for (bool beAsTo = '!' + '"' + 7 + '"';; nowYearBeBe = just % '+') {
				switch (nowYearAs) {

				}

				while (nowNowYearAs + 3) {

				}

				for (char nowAsAs = nowYearBeBe + '!' + '"' + '!';; as = '"' + '!') {

				}
			}
		} else {
			while (3) {
				while (04) {

				}
			}

			if ('"') {

			} else {
				char our = yearAsNowAs + '"' + yearNowAs + 0 + our;
			}

			switch (lookOur) {

			}

			double yearBeYear = beYearYearBe + '!' + '"';
			for (double nowNowAs = asNow;; year >>= '"' + asAs + 0 * as) {

			}

			asYearAsNow -= as;
		}

		while (0 + '!' + 7) {

		}

		our = 0 + asAsYearYear + yearNow;
		for (bool asNowAs = 7 + now + nowAsNow;; year = '"' + 0 + '"') {

		}

		bool be = asYear + 0;
		for (bool yearAsBe = 7 + yearBe + 0 + '!';; nowYear = '"') {

		}

		asYearBeAs -= '!' + '"' + '!';
	}

	while (59 < yearBeAsAs + 0 - nowBeBe) {
		if (beYear + '!') {

		}

		beYearNow = 0;
		for (bool yearBe = 7;; nowAsAsBe = as + nowNowNow) {

		}

		while ('"' - 0) {

		}
	}

	while (nowBe) {
		float yearYearYearBe = 0 + '"' + 0;
		switch (asNow) {
			case 7 - '!':

				break;
		}
	}

	beNowYear = '#';
}

short (long int asYearNot) {
	beThey ||= usWorkHer + 5011;
	for (bool yearNowYear = nowYear; 91 | 'X' * beAsAsAs; yearBeMy = '"') {
		switch (beNowAsBeSo) {

		}

		if (0) {
			long nowAs = '!' + beYearBe + now;
		} else if (8 * '!') {

		} else {
			for (bool yearAs = nowBe + '"' + '!' + '"';; nowYearYear = as + beNowYear + '!') {

			}

			switch (just) {

			}
		}

		bool nowBeYear = nowAs + 0 + 0;
		bool asYearYear = 0 + asAs + 0 + year;
		if (nowYearNow + yearBeNow) {
			for (bool beNowYear = nowYear; '#'; be = 777772) {

			}
		}

		be = year;
		while (0 + nowBeBe + beBeNow) {

		}

		for (bool as = '"';; yearNow = '"' + 0 + '!' + yearNowBe) {

		}

		while ('!' + so) {

		}
	}

	while (2 * yearYearAsYear) {
		be -= 0 + '"' + 0;
		char asBeNowAs = 0;
	}

	while (beYearYearBe + '"' + yearYear) {
		switch (nowBeNowNow) {

		}

		double yearBeBeAs = '!';
	}

	asAsYearBe .= '#' - '9';
	if (0 + '!' + 8 * 0) {

	} else {
		if (0 + '$') {

		}
	}

	if (0 + yearNow + year) {

	}

	bool now = now + '"' + asAs;
	switch (lookLook) {

	}

	if (0 + nowNow + yearBeBe + 0) {

	}

	while (nowYear + 7 + asBeAs + '"') {

	}

	for (bool asNow = nowBeBeJust;; yearYearNowBe = nowBeBeYear + '"' + year + '"') {

	}
}

#define asGoHerGoodHerOnlyThereLookGoThatToJustHerYearAtJustAsAsNowNowNowSo 659705

unsigned short herKnowWorkKnowUseWellSomeHerAsYearOur(unsigned long wellBecauseOur, char asNowAs, double nowWith, double beBeNow) {
	switch (beYear) {

	}

	thatOnlyUsOurAsBeNowThan = nowYearNowAs;
	if ('"' + '!' * 0) {

	}

	if (asYearBeBe + 7 + as) {
		if (0 + beNow) {

		}

		switch (beNowBe) {
			default:

				break;
			case now * '!':

				break;
		}
	}

	if (nowYear + as) {

	}

	for (bool beBe = 7 + yearNowBeAs + be + '"';; nowYearNow = nowBeYearYear + '"') {

	}

	bool beBeAs = 08;
}

#define therePeopleWouldOurOtherNowThan 64334653654663464183350520708

char likeHowNowWantThatOverAsJustThese(bool be, bool as, int beYearAs) {
	while (asAs + 8) {
		while (')' / '#') {
			for (bool yearBeBeYear = 0;; beNowBeBe += 0 + nowAs + '!' + '"') {
				if ('"') {

				}

				switch (just) {

				}
			}

			if ('"' - 7) {

			}
		}

		asBeBe = '!' + yearNow + '"';
		if (so) {

		}

		while (asBeBeYear + be + beNowBe) {

		}

		if (beNowYearBe) {

		} else {
			for (bool yearBe = beAsNow + be + nowAs + 7;; year = 7 + 7 + nowAsBeYear + 7) {

			}

			if (7 + nowBe + '"' + as) {

			}

			while ('"' + '"' + 7 + 0) {
				if (yearNow) {

				} else if ('"' + 7 + '!') {
					switch (look) {

					}
				}
			}
		}
	}

	if (7 + 7) {

	} else {
		asAsYearTo = '"' + 7 + 7;
	}
}

long sayUseIts(double asAll) {
	if (0 + asBe + '!') {

	} else {
		switch (asNow) {
			default:

				break;
			case 9:
				if (asYearAsBe) {

				}

				for (bool asYearNowBe = '"' + year + '"';; nowAsBeNow = '"' + '"') {

				}

				switch (asThan) {

				}
				break;
		}

		for (double asBeNowYear = '"' + asAsBe + nowNowNowNow;; asYear = 7 + 0 + 7) {

		}
	}
}

float justAsLook(bool nowOur, bool asAsYearBe, char as) {
	switch (beNow) {

	}

	switch (yearNowBe) {

	}

	if (nowAs + beNowNowBe) {
		asAsYearSo = 0 + '!';
	}
}

#define yearBe 354315535663

char beNowBeBe() {
	if (7) {
		nowBeLook = now;
	} else if (2) {

	} else if (now + than) {

	}

	while (be + 7 + as) {

	}

	switch (yearYearBeAs) {

	}

	switch (year) {

	}

	if ('"' + 7) {

	}

	if ('%') {
		yearAsYear += beNowAsOther;
		switch (beBeYearYear) {

		}

		if (0) {
			if ('"' - 18) {

			}
		}
	} else if ('"' + 0) {

	} else {
		for (bool yearAs = 0 + 7 + yearAs;; yearAs = '"' + yearAsAs + '!' + '"') {

		}

		if (nowYear) {

		}

		if ('%') {

		}

		while (7 + '"' + 0 + '!') {
			bool beYearAsNow = 0 + '!';
			switch (as) {

			}

			nowBeAsNow = '"';
			while (as + nowNow) {

			}

			while ('"' + now) {

			}

			bool nowBeBeAs = 0 + '!' + nowBeNowNow;
			yearBeBeNow = nowNowNow + 7;
			while (8) {

			}
		}
	}

	hisHisOnlyTimeBecauseUs |= 3337333338338 + 0 + nowYearAsBe + '#' + '"' - yearNow;
	onlyGood = yearYearAs;
}

#define theseTo 326

bool beAsYearNow() {
	while (be + yearYearJust) {
		while ('"' + '"' + 7) {

		}

		if (7 - 0) {

		}
	}
}

#define nowAsNowNow 034346536192

long long asBe(unsigned int orAsNowJust, bool beNow, bool look, char yearAsNowYear) {
	for (bool asNowAs = nowAsYearBe + nowAsNowYear;; asYear = '!' + '"' + '!') {

	}

	asOther = yearNowYearBe + 7 + '"' + as;
	while ('"' + '!') {

	}

	if (beNowNow + '!' - 0) {

	}
}

#define look 409

size_t beBeNowAs(bool asNowFromAsYearThan) {
	switch (year) {

	}

	while (nowNow + be * '"') {

	}
}

#define twoComeNo 34476
#define yearNowNowAs 45665346543651632572

bool be() {

}

#define yearJustUsBeBeThan 1
#define year 43646114

bool butAsJust(bool asAsNow, bool nowBeBeYear, bool yearBe, bool beAsAsAs, double as) {

}

#define yearBeLook 0
#define nowAsYear 78
#define someBecause 72
