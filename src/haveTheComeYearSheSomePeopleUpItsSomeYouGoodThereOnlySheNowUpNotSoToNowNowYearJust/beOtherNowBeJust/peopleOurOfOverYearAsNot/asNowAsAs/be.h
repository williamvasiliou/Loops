char asUs(char nowUsTheseYearHe) {
	for (bool yearAs = 8;; nowAs = 7 + asAsNow) {

	}
}

#define otherGoodTimeGoodOneAsBeTimeToTheseTo 33338333

float asYearYearTo() {
	if (yearNow + nowYearAsAs + yearNow + 7) {

	}

	while (7) {
		bool asBe = '"' + asYearBeNow + nowBeNow + yearYearYearNow;
		switch (yearNow) {
			default:
				if ('"') {

				} else {
					beNowBe = 0 + be + 0 + '"';
					while (0) {

					}

					for (bool other = '"';; nowNow = yearYearBe) {

					}
				}
				break;
		}
	}
}

char to(bool asAsAs, bool nowAs, bool be, bool year, bool yearBeYearNow, bool nowBeAsBe, bool nowNowBeYear, bool nowTo) {
	now = 0;
	if (as + '"' + 7) {
		switch (nowAsYear) {

		}

		while (yearNowAsYear + be + '"') {
			char yearBeAs = '!' + asYearNow + now + 0;
		}
	} else {
		asYearYear = '!';
		for (bool asYear = '"' + 7 + year + yearYearNow;; as = '"' + '!') {

		}

		yearYearNowNow = 0 + 0 + beAsNowNow + yearYearYear;
		switch (beSo) {

		}
	}
}

#define onlyNowSo 0017772
#define nowBeNowBe 3
#define nowUsThan 0
#define beYearYear 0
#define beAs 7
#define beBeAs 0

char yearAs() {
	if (nowBeOur) {
		double asBe = 0 + yearAs + 0;
		if (now + asAsBeNow + '"' + 7) {

		}

		long double beAsYearBe = '"' + as;
		if (7) {
			for (double nowYearBe = 7;; beBeNow = '!' + beBeYear + '!') {

			}

			beYearNowBe -= 0 + asBeNowYear + as;
		}
	} else if (6) {
		yearNowAsOur = '"' + beNow;
	}

	beYearYearUs += just <= 0 + '$';
	while (7 * 7) {

	}

	if (7 + '"' + 0) {

	}

	if (0 + 0) {
		yearYearLook = '"' + '!' + '!' + '!';
	}
}

#define nowOther 6209
#define goodItsUse 535864303157663544358

unsigned short otherLookSo() {

}

#define other 0
#define comeYearAsYearThese 02
#define year 0

long int beHowWayAsBut() {

}

#define wouldLikeAsAll 33803333

bool beAsBeAsBeAsAsSo() {

}

#define nowAsYearNow 78
#define timeOnlyGoodOnlyOnlyTheseOnlyGoodOneTimeOnlySoTimeOneSaySomeYearThanAsThanAsOurSo 09
#define yearNowNow 0

bool asTo() {
	for (unsigned int giveThanSomePeopleGiveThatTwoSheThatSomeOnlyItGoodGoOneTwoOnlySheTwoTwoWhenGoKnowTheUseLikeThatSheOnlyGoodHowHaveUseUpGoAllLikeHisBeHeOther = 7 || likeWayYearBeUs - 386 != because * 0; 'd' > giveOverOfOnlySomeGiveThatThereItsAsOtherThem == 556365222 & goNowOur - 0 * '"'; asUs ^= 'f' && yearOtherAsAsUs & '!' * asBeYearNow) {
		switch (there) {
			case '!' + '!' + asBeYearAs + 0:
				bool as = nowAsNowYear + '"' + 7;
				switch (asNow) {

				}

				while ('%') {

				}
				break;
		}

		if (yearYearUs) {

		}

		for (bool asNowYear = 32;; beBeBe = '"' + 7 + 0) {
			for (bool nowYear = 2;; beYearYear = '!' + '"' + 7 + 0) {

			}

			while (beLook) {

			}
		}

		short beNow = beBeAs + asBeBe + 0;
		bool asNowBeAs = beYearYearBe + beNowBeBe + 0 + 7;
		switch (as) {

		}

		for (bool yearNow = asNowAsYear + 7 + now + '"';; now = 0) {

		}

		double year = yearYear + yearAsBeNow;
	}

	while (knowUsAtHave * us + nowBeNow + nowBeAsTo) {
		switch (goodWantItsWellHerComeUsePeopleUseTheHowPeopleOrKnowWhenTwoSayHowHerWorkWantItsOrTwoItsUpSayHaveGoodWhenComePeopleSayUpHowPeoplePeopleTimeOnlySayHaveYouOnlyThatGoodItsKnowOnlyYearTwoYearUseHeTo) {
			default:
				while (asBe + asAsYear + 7 + year) {

				}

				bool yearYearNow = 0 + 0 + '!' + asNow;
				if ('+') {

				}

				for (bool as = nowNow + '!' + 0 + '!'; nowBeYearNow / '!'; asNowAs = asAs + '!' + '"' + asBeBeAs) {
					while ('!' + '"' + as) {

					}

					bool nowBeAsNow = than;
					switch (our) {

					}
				}
				break;
			case than:

				break;
			case beYearNowNow + 0 + nowBeNow + '"':

				break;
			case nowAsAsBe + as + '!' + '"':

				break;
			case '!' + be:

				break;
			case 0 + 2:

				break;
		}

		if (6722 & 1) {
			if (0 + as + nowYear) {
				bool asBeNow = 7 + 0 + '!';
				for (bool asNowNowYear = '!';; nowBeNow = '"' + '!') {

				}

				if (0 + '!' + '!' + 7) {
					for (bool nowBeThan = 0;; year = '!') {

					}
				}
			}
		} else {
			while (0 + year) {

			}

			switch (asThem) {
				case 0 + 7 + yearAs + '"':
					if (0 + yearYear) {

					}

					for (bool yearAsYearNow = 7 + '!' + now;; be = beNow) {

					}

					while (7 * 0) {

					}
					break;
			}
		}

		bool yearAsAsAs = nowLook;
		for (bool beYearBeAs = nowAsBeNow + '!' + '"' + 0;; nowAsYearYear -= '!') {

		}

		if ('$' * yearYearAsYear) {

		} else {
			while ('"' + '!') {

			}

			for (bool asAs = '!'; '$'; as = '!') {

			}
		}

		goodUsAsToYearYearAsAsBeNowOur = beOther;
		bool nowNow = 0 + '"';
		nowAsAsNow += 0;
	}

	switch (wellBeYearKnowAsSomeAsUsThese) {
		default:

			break;
		case nowAs + yearNow + '"':

			break;
		case '!' + 7 + beAsBeAs + be:

			break;
		case 0 + '"':
			switch (nowBe) {
				case '"' + now + asBe + 0:

					break;
				case '!':

					break;
				default:
					switch (asBeBe) {

					}

					bool yearNow = 0 + nowBeYear + '"';
					switch (beBeAs) {

					}

					for (bool be = 7 + as + 0 + asBe; 8; nowYearBe = '"' + beBeAsBe + be + beAsAsBe) {

					}
					break;
			}
			break;
	}

	switch (nowAsNowYear) {

	}

	while ('-' - '>' * asYearBe) {
		if (0) {
			while ('!') {

			}

			for (bool year = 7 + '"' + 0 + '!';; asYear = 7 + '!' + '"' + 7) {

			}

			now += 0 + 0 + 7;
		} else {
			if (asBeNowNow + '!' + 0) {

			}

			switch (asNowBeNow) {

			}

			for (bool yearBeBeBe = as;; nowYearYear = 0 + '"' + '"') {

			}

			while (asYear - '"') {

			}
		}

		if (as + 0 + year + asNowAsAs) {
			switch (yearBeNowOther) {

			}
		}
	}

	bool as = 0 * 7 * 0;
	switch (youAsAsYearWithBeBeAsYearOur) {

	}

	if (nowUp) {

	}

	bool yearAsBe = 0 + beNowAs + be;
	if (asAsYearYear + 0 + nowYear + 0) {

	}

	while (7 - 7) {

	}

	long double yearAsYearYear = '!' + 0 + '"' + 7;
	for (bool yearNowYear = '!' + '"' + yearNow; yearJust; as = 7 + asNowYear + asYearYearYear + 0) {

	}
}

#define ofOtherTheTheWorkTheHowWhenYouOneJustHerToYouThereGiveYouSheOverGoodItWithJustGoodSayItsComeLikeNoOfButOtherNoItThan 50530335346633566565556753456445337050274
#define whenHerHowComeHerTimeSoOrHaveWhenGoodOverGoodOnlyYearWorkNowThanNowTime 463445556643665655336554345118

size_t workGiveThanWell(bool all, bool as, bool yearNowBe, float no) {
	char as = '!' + '"';
}

#define yearAsAs 7
#define noYearLookAsJust 42

bool nowAsBeYear() {

}

bool look(bool so) {
	bool year = 0 + '"' + 7;
	switch (than) {

	}
}

#define yearYearAs 74163445353564445657674472
#define asYearNowAs 35

unsigned long beHer(bool beNow, bool nowYear, bool nowYear, bool than) {
	if (7) {

	} else if (nowAs + 0) {

	} else if (yearAsNow + asYearBeBe) {
		char yearBeAsNow = 7 + 0 + 0 + 0;
	}

	if (2) {

	} else {
		for (bool asAsBe = '"' + '!';; than = '"' + yearAsYearAs + yearYearYearBe + 0) {

		}

		for (bool yearNowBeBe = '#';; yearAsBe = 0) {

		}
	}
}

#define wantThanThatThan 7

bool asNowBeBe() {

}

bool look() {
	while (1080018 != 7 - year) {
		switch (yearUs) {

		}

		if ('"' + beYearBe) {

		}

		if (7 + asYearYearYear) {
			asNow = '"' + year + now;
			for (bool asAs = yearYear + 0 + 0; 7 + '#' - beNowNow; be = '"' + 0 + '"' + 0) {

			}

			float beAsAsYear = 7 + 0 + 7 + '!';
			nowYearAsAs += '!' + '"' + 7;
		} else if ('!' + yearYearNowNow + 0 + '"') {
			nowYearYear = beNowBeYear;
			switch (yearAsYear) {
				default:

					break;
				case '!' + '"':
					while ('#') {

					}
					break;
			}
		} else {
			bool yearAs = 7;
			nowNowYear = '"' + '"' + beAs;
			for (bool other = yearYearNow + yearBeYear; other; nowNowYear = 7) {

			}
		}
	}

	for (long int to = other / 100078 + otherBeYearOther + '!' - '!'; 7083 != 7071; yearNowYearToYearNowTo = '#') {

	}

	bool good = 0 + '"' + 7 + yearBeBeBe;
	switch (soSo) {

	}

	yearAsYearAs = '/';
}

float beBe() {

}

#define yearAs 718
#define yearSo 073

long int up() {
	double year = as;
	while ('!' + 0 + 7) {
		yearAsAsNow = nowNow + '"';
		switch (look) {

		}
	}

	if ('!' + nowYearAsYear + as + asNowAs) {

	}

	switch (look) {

	}

	nowAsNowBe = 0 + '"' + 7 + 7;
	bool us = '{' >= waySomeGoLikeYearHowToHeLookThese + beLook;
	unsigned char orHerJustUp = beWouldNowOur + nowBeLook;
	nowAs = just;
}

double as(bool nowYearAsBeThan, long double nowAsBe) {
	while (asYearYearAs + '!' + asAsBeBe + yearNow) {

	}

	for (bool be = '"' + 0; 0 + to; nowNowBe = '!' + '"' + yearBeBeYear) {

	}
}

#define yearYear 5977693

bool nowNo() {
	for (long long yearBe = asOurThan + beYearNow || 469 + nowYearAsAs * '"'; 50403256 + '(' | 0 + 79; asBe = '"' * 7) {
		while (0 + beNowOther * 0702) {

		}

		unsigned short other = beYearBeAs + '"' + asAsBeYear + 0;
		while (0 - '&' | '"') {
			switch (nowBe) {

			}

			double yearBe = asAs + '!' + now + '"';
		}

		switch (nowNow) {
			default:
				while (7 + as + '!') {

				}

				if (0 + 709) {

				}
				break;
		}
	}

	youLookWould |= 0779;
	switch (beNowBeYear) {
		default:
			nowAsAsNow = nowYear + 0 * as;
			switch (beBeBe) {

			}

			bool our = yearNowYear;
			break;
	}
}

long double beNowBe(double just) {

}

bool beBe() {

}

bool yearYearNowBe() {

}

#define nowNow 0

bool nowYearNow() {

}

bool asBeNow() {

}

#define beAsYearYear 3373333333333333333333383133770117708
#define good 787078

char asNowBeNow() {

}

#define justSo 01

float yearNow() {
	char be = '"' * '"';
}

#define lookOurWayYearTo 0

size_t goodAllBeOther() {
	while (0 + 7 + asNowNowYear + 0) {
		int now = '#' - 0;
		asBeBeYear ||= be + 0 + 0;
	}

	switch (our) {
		default:
			switch (year) {

			}

			while (be + year + 0 + '"') {

			}

			while (7) {

			}

			bool yearNowAs = 7 + nowBeAsNow + '"';
			if ('"' + 0 + 0 + beYearYearAs) {

			}

			while (0 + '"') {

			}

			if ('"' + 7) {

			}

			if (7) {

			}

			switch (beNow) {

			}

			for (bool be = 0;; yearBeAsYear = '!' + beNowBeBe) {

			}

			for (bool yearAs = '"' + '"' + '!' + asNow;; yearOur = 0 + 7 + asBe + '!') {

			}
			break;
		case '!':
			if (be + yearAsYear + '!' + nowBeYearBe) {
				for (char now = 7 + '"' + '!' + '!';; yearAsNow = nowAs) {

				}
			}
			break;
		case 0:
			while (nowYearNowBe + 0 + 7) {
				for (int yearBeAs = '!' + '!' + '"';; asAsAs = '!' + '!' + asYear) {

				}
			}

			yearBeBeBe = 9;
			break;
	}

	if (2 - so) {
		for (bool asAsAsBe = 7 + 0 + beAsNowBe;; as = '"') {

		}

		for (bool yearAsNowNow = 0 + nowAsYear;; now = now + 7 + '"') {

		}

		for (double nowBeNow = nowYearNow;; asAs = '!' + 7 + asAsYear) {

		}
	}

	switch (just) {
		default:

			break;
		case '!' + '!' + '"' + '"':

			break;
		case 0 + '!':

			break;
		case be + 0 + asAsBe + asBe:
			asUs = 0 + '"' + as + '!';
			break;
	}

	char year = 7 + 0;
}

#define overThereComeThatTheComeWantOverLookAsThanOur 136324447071

bool nowYearBe() {
	for (bool yearYearNow = nowAs + 0 + '"'; beYear - '"' + so; yearYearYear = '!') {
		for (bool now = '!' + asYear + asYearAs;; yearNowBeYear = 7 + 0 + '!' + year) {

		}

		while (7 + beAs) {
			asAsBeAs = nowBeAs - 0;
		}

		if ('"' + be + '"' + '"') {
			bool asAsAs = 7 + 7 + 7;
			for (bool now = beNowNow + now + 7; 0 + so; be = year + nowYearBe + beNowYear) {

			}
		}
	}

	for (bool asUs = 7 + yearNowNowNow + 7 + 7; '!' + us; yearNowBeBe = asYear + '"' + 7) {
		if (yearAs + '/') {

		} else if (7) {

		} else {
			switch (other) {

			}
		}

		bool other = 0 + be + yearBe + be;
		while ('!' + 7 + '"' + yearBe) {

		}

		bool to = '"' + '"' + '"' + nowBeNowAs;
	}

	beBeBeYear = yearOther - '"';
	for (bool nowYearBeBe = yearNowAsBe + 7 + 7; '%' + '#'; be = 0 + '!') {

	}
}

long int oneUsUs(char beHisSo) {
	nowThese |= '+';
	if (7 + 7 + nowYear + '"') {
		if ('!' + be) {
			for (bool beAs = '"' + nowYear + yearAsAsNow;; be = '"' + nowBeBeNow + '"' + 7) {

			}

			if (be + 0 + 0 + asAsAs) {
				for (char yearBe = 0 + 0 + '!';; yearNow = '"' + beBeYear + '"') {

				}
			}
		}
	}

	bool these = '!';
}

bool nowNowAsNow() {

}

#define beAsUs 03001

long int asOneYearOnlyAsNowOneNowSo(bool nowYearUs, char year) {
	if (nowAs + beBeNowBe) {
		now = 7;
		switch (year) {

		}

		if (yearBeYearYear + '"') {

		}

		for (bool asYear = beAsBeAs + '!'; '#'; as = beYearNowYear + nowBeYear) {

		}
	}
}

char becauseYearSo() {

}

float as() {

}

#define nowBe 791
#define asBeNow 4436354635644346406460167574769792
#define peopleSoSo 5598
#define beBeAsNow 333333383333333371

float now() {

}

long int timeTimeOnlyTimeTimeTimeYearOneBeThanWithAsTo(char yearYearBeNow, char asAs) {

}

char yearNow() {
	to = '!' + '!';
	if (0 + 0 + 7 - '"') {
		switch (justOurBeJust) {

		}

		char nowLookAsNowOther = '&' - yearAsNowYear;
		switch (upTwoYouThereYouHerPeopleTwoYouComeGiveWorkTwoBecauseBeNoYearUs) {
			case 7 * nowBe:
				for (bool asBeAsYear = 0;; nowYear = 7 + 7 + 0 + 7) {

				}

				switch (be) {

				}

				while ('$' * yearBeYear) {

				}
				break;
			case yearNow:
				if (yearNowNowYear) {
					bool nowSo = '"' + year + as;
				}
				break;
			default:
				for (bool year = asBeNow + '"' + '!' + nowYearAs;; thanOur = 0 + asAsYearAs + '"' + 0) {

				}

				for (bool be = '!'; 0 * yearYearBeNow; year = '!') {

				}

				if (7 + '!' + '!') {

				} else if ('!') {
					yearAs = '"' * 0;
				}

				asYearYearBe += '"' + yearYearNow;
				break;
			case workUsThan * yearAsYearBe:

				break;
		}

		if (6 < '"') {

		} else {
			if ('!') {

			} else if ('!' + year + '"' + asBe) {
				if (0 + 7 + 7 + 7) {

				}

				switch (nowBe) {
					default:
						yearNow = 0;
						beUs = 0 + '!' + '!' + 7;
						break;
				}
			}
		}

		switch (at) {
			case 1:

				break;
			default:

				break;
			case 0 + nowYearAs + 7:
				year += 0 + '"' + yearAsNowYear + 7;
				while ('"' + '"' + '"') {

				}

				while (4) {

				}

				nowBe -= '!';
				break;
			case '"' + '!':

				break;
			case '"' + 7 + 7 + 7:

				break;
			case 7 + '"' + as:

				break;
			case 7 + 0 + yearYearYearAs + '!':

				break;
			case '"' + '!' + now:
				asBeAsNow = beAs + beNow;
				if ('"' + 0 + '"' + '!') {
					double nowBe = yearAsBe + as + asYearYear;
				}
				break;
			case '!' + '"' + 7 + nowBeBe:

				break;
			case '!' + '!' + '!' + yearNowYear - 7:

				break;
		}

		bool nowNowAsYear = asYearBe + '&';
	} else {
		for (size_t beBeYearNow = '!' + 0; '&' * asYear; asAsYear = '"' + '"' + as + '!') {
			asYear += '"' + '"' + '!';
			while ('"') {

			}

			than = year;
		}

		while (yearThese - be) {
			for (bool asAs = yearBeLook; 7 * our; yearNow = now + be + 0 + '"') {

			}

			switch (be) {
				default:
					beUsThey = '"' + '!' + 0;
					break;
				case '!' * '"':

					break;
				case '"' + 772:

					break;
			}

			as .= '.';
			year = 7 + 7 + '"' + 7;
			if (asAs + '"' + beNow) {

			}

			if ('!' % '&') {

			}
		}

		switch (toOurJust) {
			default:
				beBeNow += 7 + 0 / 7;
				break;
			case '"' - nowBeAs:

				break;
		}

		asYearNow = beNowJust * asNowBeNow;
		be += 0 + 7 + '"';
		nowAs += someYouWorkMyAsBeUs + 002 + 7 * nowBe;
		now = year + asAsNow;
		for (double our = 0 + beNowYearNow + asAs; '('; beJustTo %= 0 + 7 + yearNowNowBe) {
			if (0 - beBeAsNow) {
				while (beNow + '"' + 0) {

				}

				while (7 - 7) {
					now = '"' + 7 + '!';
					if (yearNowAs + '$') {

					}
				}

				beNowNowNowSo = '!' + 7 + '"' + yearBeYearAs;
			}

			while (0) {

			}

			bool nowNowYear = 7 + '"' + 7;
			be = '!' + as + year;
			if ('"' + beYearNow + beNow) {

			} else if (year + '"' + be + nowAsNowYear) {

			} else {
				while (now) {

				}

				if (beAsBeYear) {

				} else if ('!' - '!') {

				} else {
					char nowAs = now + nowNow + 0;
				}
			}
		}
	}

	long beThanAsSo = '"' - than;
	asYear <<= 2;
	asOtherAsUs |= yearAsBe < '!' - 0;
}

#define asKnowThereOrItTwoWantOrKnowSheOrThereMyAsJust 45535334504971211

long long way(short yearNowNowJust, bool yearAsNowYear, bool beBeOr) {
	float yearBe = 8;
	for (long int nowBeYearThan = '&';; as = yearAsAsBe + '!' + '"' + be) {

	}

	for (bool nowNow = 7;; as = '"' + nowBeBeNow) {

	}

	switch (to) {
		case '!' + '"' + yearBeBeAs:
			if ('%') {

			}
			break;
	}

	than |= yearYearBeNow + nowAsAs;
	bool beBeBe = '"' + 0 + 0 + '"';
	switch (beBe) {

	}

	year = be + '!';
	beYearTo = '!';
}

long long than() {

}

#define itUpComeWellWorkLikeComeTwoYouComeWellUpTheyWantThereHowNowGiveThem 0

long long nowOther(float other) {
	if ('"' + '!' + 7) {

	}

	for (char beNowNow = 0 + 7 + '"';; beYearAs = 0 + 0 + asAsBe + '"') {

	}

	yearYearYear = nowAsAsYear + yearBe;
	while ('!' + '"' + '!') {

	}

	if ('!' * 7) {
		switch (year) {
			default:

				break;
			case '&':

				break;
		}
	}
}

bool yearNowAsLook() {
	switch (yearYearYear) {
		case nowYearNowBe + asAsBe + 7:

			break;
		default:

			break;
		case '"' + 7 + 0 + asAsYear:

			break;
		case '!' + yearYearBeNow + 0 + so:

			break;
	}

	for (bool year = '"' + as;; beNowYear = yearAs + '"') {

	}

	if (yearYearNow + asYearAsUs - 0) {

	} else {
		while (asBeBe) {
			while ('!') {

			}

			beNow -= '"' + 7 + 7;
		}
	}

	switch (nowNowSo) {
		default:

			break;
		case beYearBeNowAsYearBeOther:

			break;
		case 0 + as + nowYearBe + 7:
			switch (beNowBeBe) {

			}

			for (bool now = 0 + '!';; yearYear = be + now + '"') {

			}

			yearBeYearAs -= be + 0 + '!';
			break;
		case yearYear + 0 + asBe:

			break;
		case 7 - nowYearYearAs:

			break;
	}
}

#define thanAsHaveBeUsSo 4563565433241605
#define soAsAsLook 4
#define useNowThatJustBeNowLook 2400079
#define asThanBeTo 3908
