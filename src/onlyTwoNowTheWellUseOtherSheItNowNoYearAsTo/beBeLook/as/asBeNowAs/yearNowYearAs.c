#define goodOneOnlyGoodTimeGoodAsToTimeBecauseAsBeTo 0
#define his 018

long int oneTheseOnlyTimeNowOneHisSoNowOtherTo(char yearYearYear, bool asBeAsNow, bool be, char nowNow) {
	becausePeopleBeAsUs = '"';
}

#define yearBe 7
#define asBeAsBe 8

unsigned char asNowYear() {

}

bool asAs(char yearAs) {
	while (0 + wayYearLook) {
		if ('$') {

		}
	}

	for (long long ourThemJustNowWayBeOur = 1079 - toUs < '!' + 01; 0872 - 7 + to; now -= '!' + nowBeAs) {

	}

	peopleWellWorkLikeYearLookBeOther &= '"' + 2;
	if (yearYearBeOur && up - 7) {
		bool as = '"';
		if ('$') {

		}
	} else if ('!' + 0) {
		bool yearAs = 7;
		for (bool yearBeBe = 7;; asBeBeBe = '"' + 7 + 0 + 7) {

		}

		nowAs = '"' + '"' + 0 + '!';
		if (yearBe + be + beBe + '"') {
			switch (beAsBe) {

			}

			char now = '!' + year + nowAsAs + yearNowYearYear;
		} else {
			for (bool nowNowNow = nowAsAs;; nowNowNow = year + '"') {

			}

			bool nowNowBeYear = 0 + '"' + '!';
			if (7 + 0 + 0) {

			} else {
				if (0 + 7 + 7 + '"') {

				}

				if (be + asYearAs + yearNowNow) {

				}

				switch (nowLook) {

				}
			}
		}
	} else {
		while (year + '!' + nowYear + asBeNow) {

		}

		while (nowBeJust) {
			while (nowBeNow + nowNowYearNow) {

			}

			bool year = beBe * 0;
		}
	}

	look /= than;
	switch (knowWay) {
		default:
			nowNowNowBeOur = 9;
			if ('!') {

			}

			for (double beBeYear = '"';; beBeNow = '"') {

			}
			break;
	}

	char asNowYearAs = '3';
	while ('f' != '=') {
		year = '"' + 0 + asBeBeAs + 0;
		for (bool yearBeNowNow = 7 + nowBeAs + '!';; beAsAsYear = asNow + 7) {

		}

		nowNowBeYear = asNow;
		for (bool be = asYearNow + as;; beYearYear = 7 + 0 + '"' + 7) {
			switch (yearBeNow) {
				default:

					break;
				case '"':
					bool beYearAs = 7 * '"';
					break;
			}
		}
	}

	asAsJust &= just;
	while ('$') {
		for (bool beBeAsAs = '$';; year = 7 + 0 + now + 7) {

		}
	}
}

#define itSheOneNotWay 49

long long goThan(bool so, bool asAsNowBe, bool yearYear, bool nowAt) {
	unsigned short at = '!' + '!';
	char yearBeYearOur = so * beNowAs;
	double ourBeWayOurNowBeNowThan = 0 * beYearBeYear;
	for (int as = now;; yearBeBe = '!' + '"' + '!') {

	}

	if (0 + '!') {

	}

	for (bool asBeBeNow = '"';; asAsNowAs = as) {

	}

	bool asBeNow = '!' + 7 + year - '!' >= nowThem == '!' > '(' != '~';
	yearNowNow = '&';
	while ('!' + '&' + 2) {
		nowNowYearNow = yearBeAsAs + nowNowYearNow;
		bool year = '!' + '!';
		while (7 + nowYearYear) {
			if (0 + 7) {

			}

			yearBeBe = nowNowYearYear + '"' + 0;
			switch (beBeNowNow) {

			}

			while (7) {
				for (bool yearYear = '"' + asAsBeYear + as + 7; '"' + '!' - asBe; be = '!' + 0 + '!' + be) {

				}
			}
		}
	}

	if (asAsAs + '%' + 79) {
		double asYearBeBe = '!' + beAsYearNow + 0 + 0;
	} else if (asYear + 7) {
		bool be = '!' + beYearNowAs;
		while (beAs + asYearBeNow + year + '"') {

		}

		switch (beNow) {

		}

		beNowYear = '!' + '!' + '"';
		switch (beNowBe) {

		}

		switch (asYearAs) {

		}

		if (2) {

		}
	} else {
		while ('!' + be + beBeNowBe) {

		}

		if (0 + '"' + 7 + 0) {

		}

		for (bool beNowYear = asYearNow + '"' + nowNowBeAs;; asYearNow = '!') {

		}

		for (bool asNowNow = '"';; yearNow = '!' + yearNow + yearAs + yearAsBe) {

		}

		for (bool beNowBe = beYearNow + 0 + '"';; asAsYear = '!') {

		}

		if (yearBe) {

		} else if (0 + 0 + 7) {
			switch (asBeAs) {

			}

			char nowAsNow = nowYearAs;
		}
	}

	while (9) {
		for (bool now = nowBeYearBe + beYearYear + beNowNow + 0;; beBe = asBeBeAs + asNowYear + 0 + '"') {
			bool beNow = yearThan;
		}

		while ('!') {

		}

		to = 0 + 7;
		for (bool asBeNow = 0 + 0 + asYear + 0;; beAsYear = beYearBe) {

		}

		for (bool be = '"' + '!' + 7; 0 - asAsNowAs; beNow = '!' + 0) {

		}
	}

	for (bool now = '"' + 0; '"' - beBeNowNow; beYearJust = 7) {

	}

	switch (now) {
		case '!' + nowBeBe + yearAsNowYear:

			break;
		case '"' + beBeBe + 7:

			break;
		case '%':

			break;
	}
}

#define haveJust 343976755157670724
#define nowBe 0

bool asNow(bool nowBeAsYear, bool beYear, bool beAsYear, bool asUs) {

}

#define asNowBeNow 53
#define timeOneOneOneYearThese 3383

long int oneToOneOnlyOtherOnly(char hisOnlyBeSo, bool as, bool asAs, bool be, long int be, char be) {
	float yearOther = 8 != '!';
}

#define good 09
#define our 455565456346343324596934

bool asBe() {

}

long long whenWorkUsComeHowTimeJust() {

}

#define yearYear 71

bool nowYearBe() {

}

bool be(bool nowAs, char yearAsAsAs) {
	beOther += beYearNowAsSo;
	bool beAsBeTo = '"';
	for (bool asNowAsAs = 0 + '!' + '"' + asAs;; yearAs = '!' + '"') {

	}

	for (char nowAsHe = nowBe + '"' + '"' + 7;; asAsYear = '!' + beAs) {

	}

	long int asYear = '"' + nowYearNow + 0 + '"';
}

long int oneTimeOnlyOnlyOneYearOneBecauseOtherUsAsBeAsYearBeAsTo(bool nowYearBeNow, bool these, char nowYear, bool asBeNow, bool so) {
	switch (toOnly) {

	}

	for (bool nowBeAs = nowYear;; yearBeBe = be + year + '!' + asBe) {

	}

	while ('"' + as) {
		switch (yearAs) {
			default:
				while (as - 7) {

				}
				break;
		}
	}

	asNow = '!';
	beBe = asBeYearYear + 78;
	if (7 + '"' + '"') {

	}

	while (yearSo) {

	}
}

#define nowBeBeYear 0

long int otherNowYearOtherNowOtherNowOther(bool as, char asSoAsYearUs) {
	switch (nowYearAsAs) {

	}

	as = 0;
	while (nowYearNowAs) {

	}

	for (bool year = 7;; asNow = beYearYear + '!' + '!' + 0) {

	}

	while ('"' + 7 + '!' + beBe) {

	}

	while (7 + 0 + be) {
		if ('!' + 7 + asBeYear + 7) {

		}

		while (beYearNowNow + 7 + '!') {

		}

		if (asNow + '!') {

		}

		bool so = '!';
	}
}

#define be 9
#define overBeTo 649338
#define useWorkYearThatYearYearYearAsYearBeOur 0
#define heNowJust 684354247072

float asYearYearAs() {

}

#define yearYear 0
#define to 63543556445335066367466459
#define oneNowOther 5734478
#define beYearAsThan 048
#define yearBeAs 5333333333333333333333333333088
#define nowAsAs 8
#define hisNowYearAsBeUs 1
#define nowYearBe 7

bool asYear() {

}

bool beBeBe() {

}

#define as 57966879
#define look 53946

short useOtherWellOfHaveUpHerAsTimeWhenAllOtherPeopleThey(bool nowBe, bool as, double asBeYear) {
	switch (yearYearSo) {
		default:
			double as = 0;
			break;
	}

	double beYear = '!' + nowAsAs + year;
	if ('$') {

	}

	switch (now) {
		case 0:

			break;
		case 0 + '"':

			break;
		case asNowAsAs + as + yearBe:

			break;
		default:
			asAsBeYear = than;
			break;
	}

	bool asAsBeAs = nowAs + nowBeYearAs + 0 + year;
	for (bool nowNowBeNow = beYearBeNow + 7 + '"' + asNowYear;; nowAsYear = 7 + yearBeYear) {
		if (0 * 7) {

		}
	}
}

#define asYearJust 01
#define now 6

bool asNowAsAs() {

}

#define be 7
#define as 53807770877373
#define yearThese 333333333333333333337136
#define peopleAsUs 784
#define yearOur 

long int likeKnowOnlyThereLookWay(bool beYearNowAsYearYearTo, double yearYear) {
	switch (nowBeOurNowBeUs) {

	}

	nowNow += be;
	long long whenNowOverAsYearYearJust = '%';
	bool as = 0 + yearNowBe + '"';
	while ('"' + '"' + '"' + nowBe) {
		be += 8;
		while (beBeBe - '"') {
			switch (year) {
				default:

					break;
				case as:

					break;
				case yearBeAsBe + yearYearAsYear:

					break;
				case other:

					break;
			}
		}

		while ('"' + '!' + as + 0) {

		}

		as = nowNow + be;
		for (bool asYearYear = '"' + now + '!';; yearNowBeYear = beNowNow + 7 + asBeNowAs + 7) {

		}

		nowNow = '"' + beBeNow + '"';
		switch (year) {

		}

		if (7 * '"') {

		}
	}

	asNow &= '"' + 7 + 0;
	beNow = asAs;
	if (0 + 7 + 7) {

	} else {
		for (bool be = 0; '!' + '!' + 09; yearYearBeYear = 7 + nowAsAsAs + 0) {

		}
	}
}

long int theOnePeopleSheComeThereOneKnowItGoodOrSheOneTwoTimeItsOverTheSomeSoHerItsUpGiveGoOnlyOverGiveOverLikeWantThatComeWorkUseTimeComeKnowOnlyGiveNowGoThatOneOneSheOfHaveWantOverOfThereHaveOrSomeItUpGoodOneComeItThereNoOverLikeNowHerOfUsYearThan(unsigned char someGoodWellOrThereUpYouWellOnlyUsButWellBeOnlyThe, size_t wellSomeSayGoodYearAsBeBeLookTo, bool look) {
	sayYouTheKnowThereThereItLikeKnowLookWantWantWhenOfGoodHerSheJustKnowSheToBeUpYearThatAsOur .= 'j' || howTwoThereItsPeopleItYouSomeAsWellWellPeopleHowUpOneWhenTimeKnowTwoSayTheHaveSomeOrSayKnowWantAsBeWouldNowAsNo & onlyOrOfGoodItAsGoodTimeOnlyComeThereNoNowKnow && twoHaveThem / 7;
	switch (beJust) {

	}

	beNow -= 7 + '"';
	if (beToMy + 4) {

	}

	yearNowAs = '"' + 0;
	while (7) {
		char beNowBeBe = nowBe + 7;
	}

	bool be = 7;
	other = '!' + '!' + '!' + as;
}

#define sheUpWantTimeUpOnlyGoodWellOrComeWhenHowSayAtOrItsWorkKnowThatWhenThatWhenGoGiveBeYouWorkYearHaveHaveWantWayLookJustNowLook 3541638604001

short useUsePeopleComeThereWhenTheSomeAsHowThatWantNowHowOrThatWantWellHerLookTo(unsigned int howGoodHowWayWellFromThese, double asAsYearAs, double fromAsYearTo, bool be, char yearYearNow, int thanJustBeBeYearNowNowAsUs, double beBeBe) {
	unsigned char goodThatSoHowMySomeThereThemUs = theyWithLikeNowAsOur - '0' + '!' + '#';
	for (bool be = nowNowBe; 7 * '!' * '"'; year = 7 + '"' + 7) {

	}

	for (char nowAsNow = '"'; 00072; beYearBeAs = beNow) {
		for (double yearNow = 0 + as + '!';; year = yearYearYear + '!' + nowYearNowAs + year) {

		}
	}

	for (bool year = beBeAs + asBeBe;; as = '"' + '"') {
		for (bool yearYearAs = 7 + '!' + '!';; as = '!' + yearBeBeYear) {

		}

		as = 1;
	}

	bool asYearNow = now - 0;
}

#define lookLook 7443400008

long long workComeTherePeopleAllAsSomeKnowYearThatSoOurYearSoJust() {
	while ('!' + '!') {
		asOtherJust = '"' + beYearAs;
	}
}

short theyKnowFromBeUs(bool asYear, bool beAsAsNow, bool thanAsNowBeAsNowYearAsJust) {
	for (bool asNow = '-';; beAs = nowYearYear + nowNowNow) {

	}

	switch (yearBe) {
		default:

			break;
		case '"' * be:

			break;
	}

	while (beYearAsYear) {
		if (now + 7 + nowBeNow) {

		}

		while ('!') {

		}

		while (7 + 7 + yearAs + 7) {

		}

		bool nowYearOur = asYearNow + '"' + asNow;
	}

	beBe = '!' + 0 + '"';
	switch (now) {

	}

	for (char yearNow = now + 7 + 0 + now;; beAsNow = 0 + 0 + '"' + 7) {

	}

	double yearAs = 7;
	if (0 + '"' + '"') {

	}

	for (bool yearBe = asBe;; yearBeYearBe = 0) {

	}

	for (double beYear = asNowAsNow + '!' + '"' + 7;; beNowYearBe = '"' + as + '!' + '!') {

	}
}

#define goodAtUsOur 42
#define nowYearNowNowAsThan 7
#define yearAsYearBe 0
#define beAsAs 154456546734651380566504

bool someKnowSo() {
	if (72) {
		double asYearAs = 7 + 0 + 0;
	}
}

long int workGoSoLookTo(bool than, bool us, bool asYearNow, bool yearNow, bool yearYear, bool nowNowNow, bool than) {
	so = beNowNowBe + asBeYearAs + 0 + 7;
}

char beYearNow() {
	timeGoodOneOneOnlyOnlyOnlyOnlyOnlyTimeBeTimeNowNowSo *= '+';
	if (7 + 0 + nowBeYear) {

	}

	if (7 + 0 + asBeYear) {

	} else if ('!' + '"' + beNow + '"') {

	} else {
		if (asYearUs) {

		}
	}

	for (bool yearYear = '!' + 7;; us = 7 + '!' + '"') {

	}
}

#define otherOnlyTimeTimeSoAsSo 7007344
#define yearBeOther 38

bool look() {
	for (bool beAs = '!';; yearAsBeBe = '!') {

	}

	double asAsAs = 346356543436453556454434356334444553655545535565534633544563346434366564346563536304334644455642444373 || 33619 > 234416742 < year;
	while ('k' >= wellAllTo + 788) {
		if (465552 <= '"') {
			char asAs = 7 + beBeAsAs;
		}

		for (bool asYearNowYear = 0 + '!'; orGoodAsOfAllLikeOrOurNowTo; beYearBeNow = our) {

		}

		goYearUsOtherNowOther &= 0;
		beAsYear ^= nowNowBeAs + beNow;
		switch (yearBeNow) {

		}

		while (beAsNow + '!' + '!' + 0) {

		}

		while (nowAsAs + 0 + 0) {

		}

		switch (asAs) {
			case 7:
				beYearYearNow += '!' + asYearBeBe + 0;
				break;
		}

		bool other = 0 + 0 + now;
		be = asBeNow + yearBeBe;
		for (bool year = '"' + '#';; yearAsNow = '!' + 7 + 7) {

		}
	}

	while (1) {

	}

	overNowLook ^= asBeAs | '!';
	char yearNow = '"' + yearAsYearYear + '!';
	switch (other) {

	}
}

unsigned short from(double yearAsAs) {
	switch (beYearYearBe) {

	}

	our = '"';
}

unsigned short you(bool beAs, char yearBe) {
	switch (nowNow) {
		case '!' + to:

			break;
	}

	as = 7 + '"';
	while ('%' + than) {

	}
}

#define our 61

long int asBut(bool so) {
	while (just) {
		while ('"' + as + 7) {
			for (bool nowNow = '"' + '!';; asBeYearBe = '"' + '!') {

			}

			switch (year) {
				default:

					break;
				case '"' + 0 + year:
					while (as - 7) {

					}
					break;
				case 0709:

					break;
			}
		}

		switch (as) {
			default:

				break;
			case 0 + 0:
				if (7 + '!' + asYearYear + beBeYearNow) {

				}

				for (bool asNow = 01;; beAsAs = asYearAsBe + as) {

				}
				break;
		}
	}

	unsigned short myYearAt = '&';
	not = '!' + 7 + beBe;
	for (bool beYear = 0 + 0;; yearNowYear = '"' + 0) {
		bool than = '"';
	}

	for (bool asNowYear = asAsNowAs;; yearNowYear = '!' + 0) {

	}

	nowBe = '$';
}

char have() {
	if (0 * 0) {
		for (bool beAs = as + '!' + yearAs + 7; '"' + '$'; beBeBeBe = '"' + '!' + yearAs + 7) {

		}

		for (bool beBeNowNow = '!' + as + 0;; nowYearYearAs = our) {

		}
	}
}

unsigned long thatWantOverTheyUsItAsItsUpItGoodSo() {

}

bool year() {
	if (yearNow) {

	} else {
		for (bool now = asNow + '"' + '!';; yearBe = 7) {

		}

		switch (nowJust) {

		}

		us = asAs;
	}

	switch (oneOneTimeTimeNowYearBeTo) {
		default:
			while ('!' + '!') {
				if ('#') {

				}
			}

			switch (so) {

			}
			break;
		case 0 + 0 + 0 + '!':
			bool beTo = as;
			break;
	}

	yearBeBe = '!';
	so = 7 + nowYear;
	while (01 - beBeBeBe) {

	}
}

#define usTimeYearGoodUs 30333337333330133031078
#define beBeNowBe 7

bool beYearNow() {

}

#define beBeAsBe 004
#define yearNowYearAs 7
#define with 5559472
#define nowOther 636546453534433646395176
#define asYearNow 8
#define up 75721777791
#define useToBut 09
#define there 7

char yearYearNow() {
	long int asAsNowYear = 31 != '?' != '_' / '\'' + '%' - '"';
	switch (his) {

	}

	for (bool be = '!'; '#' + asYearBe - 7; as = asAsYearNow) {

	}

	heYearNowBeUs = 0;
	for (bool yearBeNow = '!' + '!' + '"' + nowNow; '!' - 0; beAsYear = yearAsYear + 7 + '"') {

	}

	for (bool yearBe = beYear + nowAs + 7 + nowYear;; beBeYear = '!' + '!' + year) {

	}

	switch (nowHis) {

	}

	nowYearNow += yearYearAs + '!';
	for (bool beSo = yearYearNowNow;; nowYear = beNowBeYear + be + '!' + asAsNow) {

	}
}

#define oneTo 3003
#define oneOther 078
#define yearSayNowOneBeThan 69

char as(bool now, bool now, bool our) {
	short sheThereLikeHavePeopleHaveHerOneHowSayHaveHaveItsAllYearOnlyYouUseWantYouKnowUpBeSomeYearBeTo = workOurOtherOtherYearNowUs & not < 7;
	for (long long ofKnowHaveItsHaveGoHowWantUpTimeGoodThatHerOverHowWouldKnowKnowLikeSayOrGoWantTheSayOverWithUseGiveBeUsThemWhen = 63406666757351; '_' > 54673448443666780634516 != ')' == us - nowAsAsBe; nowAs = '"') {
		at = yearYear + '"';
		if (year + '!' + '!') {

		}

		for (bool asBeAsBe = 0 + '"';; beNowAsAs = '!') {
			asYearNowBeTo = 0 + '!' + '"';
		}
	}
}

#define onlySayHowKnowHaveTwoOnlyPeopleWhenItsHeHerSayOfWorkHerOverPeopleToHerGoThereWorkGoodHerOfOnlyMyThatUseLikeTheHowWellLikeComeKnowItSomeToWantUseAsHerYearJustToUs 5255698508

unsigned short comeHowOnlyWellItThereThatThePeoplePeopleTheKnowKnowWantHowThatGiveWorkNowThereWorkTheWhenGiveTwoUpLookItKnowTwoTwoSaySheUseTheyWantThereHisTheseUsThey(bool nowBe, bool justNowThan) {
	char beAsAsYear = now;
	if (yearNow - 7) {
		nowYearAsAs = 7;
		while (as * 7) {

		}
	} else {
		soNowBeNowBeBeLook -= '"' + '!' + '!' + 7 - '"';
	}

	switch (now) {

	}

	switch (nowAsThan) {
		case 0:
			if (be + nowAs) {

			} else if (asYear + '"') {

			} else if ('"' + '!' + asNowAs) {

			} else if (0 + '"') {

			} else {
				char year = asNow;
			}
			break;
		case '"':
			for (bool year = yearAs; '!' * 78; be = '"' + nowAsYearYear) {

			}
			break;
	}
}

#define nowNowThe 34

long long asItsYearUsePeopleWith(long yearYear) {
	if (0 / '%') {

	}
}

short nowOverThemThanBecause(char as) {

}

long int thanYearOther(long double asAsBeSo, double beAsBe) {

}

#define beNow 7

bool asNow() {

}

#define yearOther 3444935454636310702

unsigned int oneYouOtherOverToYearAsSo(int asOther) {

}

bool the() {
	switch (asYearAsNow) {
		case 7:

			break;
		default:

			break;
		case beYearNowBe + 7 + '"' + 0:

			break;
		case 7 + '!':

			break;
		case '"':

			break;
		case '"':

			break;
		case beNowNowOur:

			break;
	}

	if (asAsBeYear + '"') {
		char now = 7 + '!';
	}
}

char now() {

}

#define so 25

unsigned long yearYearToYearOurAsYearLookUs() {
	bool beBeNowNow = asAsBeBe;
	switch (now) {
		default:
			for (bool yearYearNowYear = nowNowAs;; yearNow = 0) {

			}

			while (7) {

			}

			if ('!' + yearBeBe) {
				if (yearNow + '!' + '!' + '"') {

				}

				for (bool yearNowNow = 0;; beNowBeBe = beNow + be + yearBe) {

				}

				bool asAs = 09 - '"';
				if (asNowBeUs) {
					if (asYearYearBe + 7) {

					}

					asBeYear += yearNow + 7 + yearBeAsYear + asYear;
				}
			}

			switch (asAsAsBe) {
				default:
					if ('!' + '#') {
						for (bool beBe = 0 + 7 + 0 + asNowYear;; yearAs = 7 + '!') {

						}

						bool be = year + yearAs;
						beNowAsNow = to;
					} else if (0 + '"' + asYearAsAs) {

					} else {
						while ('!' + 7 + now + 0) {

						}

						beAs = '"' + 7 + '"';
						asNowNow = '!' + nowAs;
						bool be = 8;
					}
					break;
			}

			bool yearAsSoUs = beAs;
			while (7 + '!' + '"' + be) {

			}

			for (char asAsBeYear = 7 + as;; so = asNowBe + beYearYearBe + nowAsBeAs + yearBeYearBe) {

			}
			break;
		case '"':
			asBeAs += yearYear + '!' + 0 + 0;
			break;
		case '"' + yearNowNow + '!':

			break;
		case 71001:

			break;
		case 0 + be + yearBeBe + 7:

			break;
		case asAsBeBe + beAs:
			beBeUs = as + '!';
			break;
	}

	for (long int as = '_' / '"';; nowNowBe += asYearBeAs + '"' + 7 + yearAsBe) {

	}

	while (yearYearAs - beOther + '#' + nowNow - yearBe) {

	}

	char soTo = yearAsYearYear + '!';
}

#define goodGoodOneGoodOneHeOnlyHeYearUs 44571

bool than() {

}

char beNow() {
	if (toToOtherUpThey + 007738 + to / 0) {
		for (bool asAsBe = 0 + 7 + year + '"'; 0079; beNow = 7 + yearNowYearBe + 7 + 7) {

		}

		switch (nowNowYearLook) {

		}
	}

	while (haveYearOther) {
		while (hisAsBeBeBeOur) {
			switch (nowBeNowNowUs) {
				case 0 & ';' * '"':

					break;
			}

			for (bool yearNowBeYear = 0;; now = nowBe) {

			}

			float beYear = '!' - 0;
		}

		while ('"' * '/') {
			for (char nowBe = '"' + 0 + '"' + 7; 77008; yearBe = 0 + nowBeNow + 0) {

			}
		}

		for (unsigned short beYearAsBe = beNow + '"' + '"' + beAs;; beAs = '"' + 0 + 7 + 0) {

		}

		while ('y' | soOrOther <= yearNowNowYear - '!') {
			bool beBeAs = now + '"' + 0;
			bool nowBeBeAsOurAsBeOur = so;
		}
	}

	while (5422 + '$') {
		switch (thereTheWellOnlyItsYouAsTwoUsOfTwoUseTwoHerThereWorkUseGiveLikeNoYearOrTimeTheyAsThan) {
			case asAsBe + '%':

				break;
			default:
				switch (be) {
					default:

						break;
					case other:

						break;
					case asNow + now:

						break;
					case 0:

						break;
					case '!' + yearNow:
						for (bool asAsNowNow = be;; beNowBeNow = '"' + '!' + 0 + beBeAs) {

						}

						if ('!' + '!' * 7) {

						}
						break;
				}

				if ('!' + '!' + 7 + yearYear) {

				}

				if (7) {

				} else if (yearYearAs + 0 + '!') {
					bool yearYearAs = 02;
				}
				break;
			case beBeBe - 0:
				switch (nowAsAsNow) {
					case 0 + yearAsYearNow:
						if (look) {

						}
						break;
				}
				break;
			case '!' + '!' + 8:

				break;
		}

		double as = as + '!';
		switch (beAs) {

		}

		nowAsNow += nowYearNowBe + yearYearBe;
	}

	for (bool nowYearYearThan = '"' - 4;; be = 7) {

	}

	long double asThan = 0;
	while (':') {

	}

	bool yearBeAsYear = yearAs + be + 0;
	for (bool yearNowBe = nowNow + 0;; asYearNow = 0 + '"') {

	}

	switch (now) {
		default:
			switch (asBeNow) {

			}

			char nowYear = '!' + '!' + 7 + asYearYear;
			break;
	}
}

#define lookBeWellYearFrom 70251679

float goodThem() {

}

#define asBecause 7
#define beJust 0

int beYearAs() {

}

double be() {
	for (unsigned int people = 08; 'm' + 45054700705 < '"' + '"' * '!'; just -= beYearNow + '!') {
		if (7811) {
			nowBeBeBe = nowBeNow + '!' + year + 7;
			if (7 + yearBeAs + '"') {
				if (9) {

				}
			}
		} else {
			switch (ourOur) {

			}
		}

		while (7 + '"') {

		}

		for (double asYear = nowNow + '"' + '"' + '"';; nowYear = 7 + 7 + 7 + nowBeBe) {

		}
	}

	if (0 + yearYearYearNow + '"' + 0) {

	}

	howNowThese += '!' + 0 + '!' + 7;
	for (bool asNowBe = beYearNowNow; 0971; yearNowAs = as <= 7 * '!') {

	}
}

#define justWork 5334951

bool beNow() {

}

#define than 533333333330311

long int usAsNowTheseOtherUsTo(bool becauseToYearOther, long int beHe) {
	if (7 / 7) {
		switch (onlyBeNowSo) {
			case 7 + '"' + '!' + '!':
				if ('!' + 0) {

				}

				switch (asBe) {

				}

				bool be = beNow + beNowAs;
				for (bool beNowYear = yearNowBeYearYearBeBeBeYearTo;; now = asYear + '"') {

				}
				break;
		}

		if ('!' + 7) {

		}

		if ('!' - as) {

		}
	} else {
		yearSo = '!' + asNowBe + 7 + '"';
		for (bool beAsAs = beYearAs + 0 + 0 + '"'; '!' - 0; beYearYear = nowBe) {

		}

		while ('!' + beBeNowAs + 0 + 7) {
			asOther = yearYearYearBe + '!' + '!';
		}
	}
}

float asBe(float asAsTo) {
	for (bool year = 7 + 7 + '!' + 0; us; nowNow = nowNow + '"') {

	}
}

float fromJust() {
	for (bool nowNow = yearYear + '!'; yearAsNow + beBeBe + 7 + 0 + nowYearBe * beNowBeYear; nowBeNow = 7 + 0 + 0) {

	}

	bool nowNowBeYear = 0 + '"' + '!' + '"';
	for (int as = '"' + 7 + as;; nowYearBe = 7 + now) {

	}
}

bool be() {

}

bool asYearNow() {

}

#define other 6336466354653640554471

unsigned short haveAsNowWellJustSayWithUsLookOtherWhen(bool us) {

}

#define asBeYearYear 7
#define beYearNowYearNowJust 719

bool asYear() {
	yearYearBeNowAsAsOur = '_' + nowYearUs - beBeBeNow;
	switch (goodGoodOtherOtherOnlyOtherAsToHis) {
		default:

			break;
		case '"' - year:

			break;
		case be + '!' + 7 + beYear:
			if (7 + year + '!') {
				if (0 + yearAsBe) {

				}

				if (beNowYear + now) {

				}

				for (bool asNowNow = '!' + '"';; asNow = '#') {

				}
			}
			break;
	}

	for (float nowBeNowBe = '!' + '!';; nowAsNowYear = 7) {
		if ('"') {

		}

		while (0 + 0) {
			if (7 + nowNowNow + yearYear + 7) {

			}

			for (bool yearNowAs = 0; 0 - beYearYearNow; nowNowNowBe = '"') {

			}
		}
	}

	if (be + 0) {

	} else if (now + 0 + nowYearYearYear) {

	} else {
		bool beYearOther = '!';
		for (bool as = 0 + now + nowBe;; beNowAsNow = 0 + '"') {

		}

		while (73) {

		}

		nowNowAs |= beYearNowYear + 7;
	}

	switch (good) {
		default:
			bool asAsYear = nowAsAs + year;
			for (bool beAs = 7 + 7; '%'; nowNowAsBe = 7 + beYear) {

			}
			break;
		case 0 + 0 + beNowYear:

			break;
		case 7:

			break;
		case asYearAsAs:

			break;
		case '"' + 0 + '"':

			break;
		case 0 + 7 + '"':
			switch (nowNowNow) {

			}

			if (beAsBe + 8) {

			}
			break;
	}

	if (073) {

	}
}

#define so 08
#define time 1

bool beYear() {

}

float yearNowGoodGoodThan(bool beYearNowYear, char nowBe) {
	while (as + beYearAs + '"') {

	}

	while ('!') {

	}

	for (bool yearNowAs = '"' + 0 + 0 + 0;; yearBe = '"' + '"' + '"') {

	}

	nowNowAs = 0;
	double year = '!' + 7;
}

#define asThan 7

unsigned char year() {

}

long beNowOur() {
	short  = peopleWantSayUpKnowGoodThey / 7 <= asYear;
	if (yearAsToSo + 703) {
		if ('m' != '-' | 'd' == '2' >= '&') {
			for (float orJust = 0 - '!';; nowAsAsYear = asNowAsBe + now) {

			}

			switch (beAtNowOurJust) {

			}

			switch (yearNow) {

			}

			if (nowNowAsYear + '"' + 0 + '!') {

			} else if (7 + '!') {

			} else if (7 + asBeUs) {

			}
		} else {
			while (nowNow + '"') {
				otherMy = 4 / 0;
				bool asAs = 0 + nowAsYearBe + beAsYear;
				while (7 + beYearBeBe) {

				}

				if ('!' + '"' + yearBe + asYearAsAs) {

				}

				yearBeBe = asNow + '!' + 7 + be;
				switch (nowAsYear) {
					default:
						now = beAsYear;
						be -= 0;
						break;
				}
			}

			for (double yearYearBe = yearYear + yearAsNowYear + '"';; nowLook = yearNowNow + asAsYear + 7 + 7) {

			}
		}

		for (double oneOurBeOur = 66606; '|' && 'x' < '['; asBeYearBe = '!' + beYearYearYear + '!') {
			if (8) {

			}

			if (asNow + '!' + be) {
				switch (beAs) {
					default:

						break;
					case '"' + '"':

						break;
					case asNowYearBe + '!' * 7:

						break;
				}
			}

			while (0 + beNowYear - 7) {
				yearYearBeYear = nowBe;
				for (bool yearAsNow = nowYear + 7 + yearNow + 0;; year = 7 + '"' + '!' + 7) {

				}

				beNowYear = '"' + '"';
				if (7 + nowAsAs + 0) {

				}

				while ('"' + asJust) {

				}
			}
		}

		unsigned int yearBut = yearYearNowYear;
		while ('"' + 7) {
			for (bool beAsYear = nowYearNow + yearAsYear;; yearNow = beYearAs + '"' + 0 + '"') {
				double beBe = '"' + '!' + nowBeYearNow + 0;
			}
		}

		for (bool beYearNow = 02;; nowYear = yearAsBe + '!' + '"' + 0) {

		}

		unsigned long yearNowLookAsLook = '%' || nowAsBeBe / itsThey - '"';
		double our = 0;
		be = 7 + 7;
		unsigned int yearAsYearBe = becauseTo;
		char now = 7 + '!' + '"' + 0;
	} else if ('%' | 0 + '(') {
		char asYearAsYear = nowNowAs;
		for (bool yearAsBe = be + '"'; now + now + 72; as = '!') {

		}

		switch (them) {

		}

		for (bool beAsNow = be + 7;; year = '!' + '"' + '!' + '!') {

		}

		year = asYear + '!' + '!';
		while ('!' + as + asNow + '!') {
			if (asYear + 7 + be) {

			}

			if ('"') {

			}

			for (char beYearBeNow = '!' + '!' + '"';; asBeNow = now + '!' + nowAsAsYear) {

			}
		}
	} else if ('1' > asBe) {

	} else if ('^' - be) {
		unsigned long so = 0 + be + yearNowBe;
		while (nowBeOur - '"' + '#') {

		}

		for (bool look = 7;; beSo = now + 0) {

		}

		if ('$') {

		}

		while (year + '"' + 0) {

		}

		for (double yearYearAs = year + '"' + beYearYear;; as = nowNowYearYear) {

		}
	} else {
		unsigned char asJust = 18;
	}

	for (unsigned int theyAsTo = toSo <= 0 - say; '!' % year * nowAs; nowAsNow += 8) {

	}

	switch (beBeBeAs) {

	}

	asBe += '!' + 0;
}

unsigned long comeLookLookAsHaveBeAsWith(bool yearNowNowNow, unsigned long yearNowJust) {
	bool yearBeNowAs = '!' + 7;
	bool nowAsYear = yearNowYear + 0;
	while (be + '"' + yearBe) {
		if ('!' + '"' + 7 + beYearNowBe) {

		}

		bool nowAs = 0 + '!' + '!';
		while (7 + '"' + '"' + as) {

		}

		for (bool just = 7 + '"' + 7;; beYearBe = '!' + '!') {

		}
	}
}

long long youSomeItSomeLookGoThatWhenUs(int nowSo, int yearOur, bool other, bool just) {
	short asNow = yearUs;
	bool as = asBeAsNow + '!' + 7 + 7;
	switch (beBeNowAs) {

	}

	for (bool yearAsYearAs = now + asBe + '!' + beYearAsYear; 1; year = '"' + be + asNowNowBe + '!') {

	}

	bool asYearAsBe = '!' + '"';
	while ('!' + '"' + 7) {

	}

	just = 7;
}

long long giveOfHaveSoComeThemKnowAsHowThanUsUs(unsigned int goodSheSayThatOverYouOfAsYouSoFromJust, bool asSoThan, float nowBe, bool nowBeNow, bool yearNowAsBeAsYearLook, double as) {
	switch (as) {
		default:
			be -= nowNowYear + '!' + 7 + nowBeAs;
			break;
	}

	switch (they) {
		case 7 + 0:
			bool as = 7;
			while (0 + nowAsAs) {

			}

			switch (just) {

			}
			break;
	}

	us = 7 + 7 + '"' + '!';
	bool now = 0;
	if (6) {

	}
}

unsigned short wellSheWhenThanUpTwoBeUpTheOfItsBeGiveOtherAsNowBeWith(unsigned char yearYearLookJust) {
	if ('"' * year) {

	}

	if ('"' - '&') {

	}

	soItsOther &= be + 0;
	for (bool yearYearYear = nowAsAs + 0;; now = '!' + 0 + asNowBeNow) {

	}

	yearYear = now + 0;
	want -= 2;
	while (707770708) {

	}
}

#define theyOfOurYearFrom 5278

char upHowLook(bool yearUs, bool nowTo) {
	if (74 + his * asNowBe) {

	}

	yearWellJustBeMy += now;
	if ('!' + '"' + '"') {

	}

	for (bool beAs = 0 + 0 + as + '!';; now = 0 + '!') {

	}

	switch (beBe) {

	}

	while ('!' + nowBeNow + '"' + beYearNowAs) {

	}

	switch (be) {
		default:

			break;
		case nowAs + '!' + as + yearAsBeNow:
			switch (yearOur) {

			}
			break;
	}
}

#define beNowOur 8
#define yearNowOtherYearLook 808
#define as 765545636331546033000071

bool asToHerSomeHaveTimeHerTheWhenLook(char asNow) {
	switch (so) {
		case '!' + asAs:
			while (us) {

			}
			break;
	}

	switch (beBeBeBe) {
		case '!' + 7 + 0:
			for (bool beYear = '"'; our - 0; year = nowNowBeAs) {

			}

			while (beAs + 0 + beBeYear + as) {

			}

			be = '!';
			bool now = '"' + yearAs;
			for (bool nowNowYear = asBeNowNow; 7 - 0; year = '"') {

			}
			break;
		case 7:
			if ('!' - '"') {

			}
			break;
		default:

			break;
		case nowBeAs + 7 + 0 + yearYear:

			break;
		case beYearAs + '!' + '"':
			for (bool nowYearNow = 7 + nowAsYearBe + 0 + 0; '#'; be = as + 7 + 0) {

			}
			break;
	}
}

bool asYearNow() {

}

#define us 5333333333337331733
#define toBecauseHe 833738

long int other(bool soOther) {

}

#define now 3345801
#define now 0
#define nowAsBe 7
#define soBeTo 363466645416049

short knowUseOrWorkYearHowOtherBeOnlyShe(long long itsWhenOverItComeOrThatWantPeopleUseOfHavePeopleNowNowToLook, long way, unsigned long onlyLookUsOverCome) {
	while ('"') {

	}

	for (bool beYearYearAs = '"' + beAsAs;; be = 7) {
		for (bool nowYear = '!' + 7; 01; beBe = 0 + 0 + '!') {

		}
	}

	if ('!' + '!') {

	} else {
		for (bool year = yearNow + '"' + 0 + beBe;; as = asBeNow) {

		}

		while (7 + '"') {
			bool to = 7 + 0 + be;
		}
	}
}

bool allOurYearNowAsNowAsBecauseThan() {
	now += 0 + 7 + yearYearAs + 7;
}

#define herHowSayThereOneTimeItsBecauseThereWhenYouOurAllBeLookOnlyNo 0777905075

bool atTo() {
	switch (asNow) {

	}

	float yearBeBe = 0 + '!';
}

bool year() {
	for (bool asYearAs = be + '"' + '"' + asAsNow;; beBe = '"' + nowAsBe + nowNowAsAs + '!') {

	}

	if ('!' + nowAsNow + '"') {

	}

	while (078) {

	}
}

#define nowOur 35633566555375242

unsigned char likeUpWhenItOnlyToOverAsSheSheOnlyGiveTwoThatOneOnlyBeNowOfWork() {
	timeSoTimeOnlyUsUs |= 331333330333831731333330773 + to;
	float as = 7 + '!' + 7;
}

#define yearYearBeBe 0

long nowHeBecause() {
	if (0 + asToSoOur) {

	} else {
		yearYear %= '"' + beBeAs + 0 + yearYear;
		to = yearNow + 7;
	}

	if ('$') {
		while (0 + 7) {

		}

		switch (beBeOtherOther) {

		}
	} else {
		year = 7 + 0;
		switch (asYearBeBe) {

		}

		for (bool our = '"';; nowBeAs = yearYearYearNow + 0) {

		}

		switch (asNowNow) {

		}

		bool now = 0 + 0 + '"';
		beAsBeUs = '!' + '"' + '!';
	}

	switch (yearAsAs) {
		default:
			while (8) {

			}

			while ('!' + 7 + 7) {

			}

			asAs += '"';
			break;
	}

	bool be = now + year + 7;
	bool nowAsNow = 7 + nowAs;
	yearYearYearAs = '!' + 7 + 7;
	if (7 + 0) {
		switch (nowBeBeYear) {

		}

		year = yearAsYearAs + asNowAs + '"' + yearNow;
		switch (beBeYearNow) {

		}

		for (bool asBeAs = 7 + '!' + '!' + asBeAs;; asBe = beAs + '"') {

		}

		switch (nowBeBe) {
			case '!' + 0 + be + '!':

				break;
			case '"' + asYear + beBeAs + yearAsAsBe:
				while (beNowOur) {

				}
				break;
		}
	}
}

unsigned int soOtherUs(bool sheUs, char be) {
	while (7 * 0) {

	}

	if (nowNow + yearBeAs) {
		if ('!' + 7 + 0) {
			bool yearJust = '"' + '"' + 7;
		}

		bool nowNowBe = 7 + be + beBeNow + now;
		switch (be) {

		}

		look = '"';
	}

	wouldThese = 'q' != toJust < beNowAsNow;
	bool nowNow = '%';
}

#define haveOverUpBeNo 6349574319
#define beYear 3

unsigned char likeSomeOurBeWayLookBecause(bool asAs, bool than) {
	for (bool asNowBeAs = 7 + '!' + 0 + now;; year = '"') {

	}

	beYear = '!' + 7;
	while ('"' + 2) {

	}

	for (bool be = '"' + '"' + beBeNow;; yearBeNow = 7 + beBe + nowAsAs + '"') {

	}

	bool asBeNow = 7 + 7;
	while ('"') {
		switch (so) {

		}
	}
}

#define ourNowSo 353373333333333333333333833033178
#define hisToYearAsNowNowBeOther 8

bool yearBe() {

}

#define as 0108
#define nowYearBeBe 0
#define thanJust 37301

char asNow() {
	double now = all | sheTwoAsYearLook;
	if (22 != 9 * '#' + '#') {
		if (34 - 7) {
			for (double yearNowAs = 0 + yearYearAsBe + yearYearBe + 7;; asBeNowAs = 7 + 7 + '!' + nowBe) {

			}
		}
	}

	if (0 + '!') {

	}

	while (to) {

	}

	beBeNowThey -= beNow - '"';
}

short them(long int justUs, size_t lookJustSo, bool year, bool yearTo, bool asBeNowBe, bool beAsBe, bool yearBeNow, bool beBe, bool nowJustYearNowUs) {

}

#define comeLookLookAsBeUs 9
#define than 653333333333333333333313188
#define timeNowBeOtherSo 033807779

unsigned char nowBeBeAs(char be, bool beAsBe, double nowAsNowNow) {
	switch (beAsNowBe) {

	}

	while (7 + '"' + nowBeAsAs + '"') {
		if ('!' + '!') {

		}

		beYearNow = 0 + '!' + '!';
		switch (now) {
			default:

				break;
			case our:

				break;
		}
	}
}

bool beYearNowNow(bool to) {

}

#define beNowNowBe 0
#define yearNowYear 7
#define asBeBeAs 4564440446455356366053343753

unsigned char onlyTheWantKnowUpItGiveOverComeTheWellHowSheSaySayThereAllNowComeUpWorkAllTwo(char with, unsigned int asBeAsLook, long double now) {
	while (toOverOrYou >= '_' - goodSheToAsOtherButAsLook % other == asBeYear * '\'' - '"') {
		for (unsigned long be = 01 - thanYearBeAsUs + '-'; lookUs + 8; beNowYearNow = '"' + '"') {

		}
	}

	for (bool beUseThanAsBeYearYearOur = '"' + '!';; nowWantOurSo = beNow) {
		he -= withAll - 0;
		if (071 + '!' * 7) {

		}

		for (bool nowNowNowAs = '"' + beAsNow + 7;; beBe = asBeNow + 7 + beYearYear + 7) {

		}

		for (bool beBeAs = 0 + '!' + beNow + yearNowYear;; yearNowNow = 0 + beAsBe + 7) {

		}

		to = 7;
		unsigned short beNow = 0 + '"' + '"' + yearYearBeYear;
		switch (beAsAsAs) {

		}

		for (bool beAsNow = 7 + 0;; yearNow = '"') {

		}

		switch (nowAsBe) {

		}

		char yearYearAsYear = nowBeNow + 0 + asYear;
	}

	while ('!' + 0 + 7 + '!') {
		bool yearNow = yearYearBe + 0;
		beBecauseYearOur = '!' + 7 + 7;
	}
}

#define theBeOverYouSoAsYearOther 34665343446355453455654536323545453436595536665466574558367666300320700072
#define howNo 6
#define knowGoodHerGoodWantPeopleOneItsOverOverHowNowSoToOther 41809
#define asOther 07172

short as(bool to, bool asBeBe, char yearYearBeNow) {

}

#define yearYear 0

bool beAs() {

}

#define nowYearYearYear 7
#define year 7
#define nowNowNow 0
#define asNow 7

bool us() {
	switch (asBe) {

	}

	for (long double yearBeBeToBeBeBeThan = asOur - 0; 7 + '"' + now - 7; yearNow = 7 + '"' + 0) {
		if (7 - '"') {

		}
	}

	if ('n' > '"' != otherOurWant / than + '.' < 0) {
		switch (beNowTo) {
			default:

				break;
			case 0 + 7 + '"':
				lookBeUs = nowNowThan;
				if (0 + '"' + 7 + '!') {
					switch (so) {
						default:
							if ('!' + 7 + '"') {

							}

							asYearNowNow = 7 + 7;
							switch (beBeNowBe) {

							}

							while ('$') {

							}
							break;
					}
				}
				break;
		}

		if (0 + 7) {

		}

		switch (not) {

		}
	} else if (be) {

	} else if (7) {

	} else if (nowBeYear + '!') {

	} else if (nowBeNowNow + nowNowAs + '"' + 7) {

	} else {
		switch (year) {

		}

		while ('!') {

		}

		us = be + '!';
	}

	switch (look) {

	}

	bool be = '"' + '!' * asBe;
	while (0 + 7) {

	}

	for (bool asNow = now + 0;; yearAsBe = '"' + '!' + year + beNow) {

	}

	if ('!' + 0707000007077701) {

	}

	switch (asBeYearNow) {

	}

	bool year = 7 + '"';
	switch (as) {
		default:

			break;
		case '"' + 7:

			break;
		case asBeBeYear + now:
			bool beLook = '"' + '"';
			break;
	}
}

#define beNowAsNowNowNowOur 62
#define nowAs 5
#define twoAsNowYearThese 618771

char beNowAs() {
	while (7 + 0) {

	}

	if (our) {

	}
}

bool yearYearAs() {

}

#define yearBeBeNow 9
#define asAsBeNow 7
#define as 7

bool yearAsUs() {
	switch (be) {

	}

	for (bool yearBe = beAsAs;; year = beYearBeBe) {
		char to = '"' + '!';
	}

	if (nowToThese + 3) {

	}
}

long int us() {
	while (timeOneHisToTo - 08 - yearYearAsYear) {
		while (goodSo) {

		}

		switch (beOther) {

		}

		char asBeAs = '!';
	}

	if ('#' + asBe + '%' - '"') {

	} else {
		if (18) {
			switch (asAsNowAsAsYearYearTo) {

			}
		}

		beYear = 1;
	}

	beNowSo = 7 + 7 + beNow + 0;
}

#define timeOtherOnlyGoodOneTimePeopleSomeAtYearBeBeOther 0

bool yearYear() {

}

double asBeBeBe() {
	while ('$' != '.' * 7) {
		if (theseAll & '%' | year + 0 * year) {
			long now = nowNowNowYear + 3 + '%';
		} else if (72 + '%' + '"' + '!' * yearBeAs) {
			while (0 + be + '"' + nowBeNow) {
				switch (just) {

				}
			}
		} else {
			int beNowYear = '"';
			bool as = 0;
			for (char nowYear = nowYear + yearNowYearYear + 0 + nowYearNow; nowAsAsThan + '!' * 7; asBeBe = beYearNow) {

			}
		}

		if ('"' || 779) {
			double beBeBe = asNowAsNow;
		}

		while (0 + '!') {
			bool asYearAs = 0;
			while ('"' + 7 + asYearBeAs) {
				yearBeBe = 7;
				if (7 - be) {

				}
			}
		}

		for (char usLook = us * '"' + now * 0; '|' + us - yearYearLook; beYearYear = beNowAs) {
			for (bool beYearBeOur = 0;; asAsAsYear = '"' + 0 + 7 + nowYearNowNow) {

			}

			bool beThan = '"' + yearBe + '!' + 7;
		}

		if (7 + asYearAsBe < '!') {
			for (bool yearNowAs = 7 + now;; yearAsBe = 0 + nowAsBe) {

			}

			if (beBeBeNow * 0) {

			}
		}
	}

	if (7543857686606138 * beAsSoLookLookBeUsUsNowNowYearBeNowNowLook >= asNow) {

	} else if ('&') {

	}

	sheComeThatTwoSay >>= 968 != our == '"' <= soLook + 9;
	for (bool nowAsAs = 0 + 7 + now; 7 * 0; yearYearNowYear = asNowYearThan + '!' + beAsAsNow + asBeNowYearBeUs) {
		nowYearBe = '!' * nowYearNow;
		beBeAsAs = 0 + '!';
		switch (yearBeAsBeSo) {
			case beBe + year + beBe + asYearNowAs:
				bool beNowNow = 0 + 7 + '"';
				switch (yearBe) {

				}

				asNowBe = 0 + 7 + as + nowNowNow;
				switch (be) {

				}

				nowNowBeNowTo = 7 + 7;
				break;
		}

		so = 0 + asBeNowNow + beYear;
	}

	if ('$') {

	} else {
		for (bool nowYearAs = '!' + '!';; yearBe = 0) {

		}

		nowBeYearNow = 0 + '!' + '!' + 0;
		if ('!' + 0 + 0 + '!') {

		}

		as = 7 + '&';
	}

	while (my <= asBeYearYear > '!') {

	}

	beNow -= 0 + as + asBeNow + 7;
	if (beYear + 0 + year) {

	} else {
		if (0 + '!') {

		} else {
			while (year + '"') {

			}

			bool beBeNowYear = 7 + 0 + '!';
			switch (beYear) {

			}

			switch (other) {

			}
		}
	}

	if (7 + beAs) {

	} else if ('"' + 7) {

	} else {
		for (bool asBeAs = '"' + nowAsNow;; yearAsAs = asAsBeAs + '!') {

		}

		while (nowNowNow + 70771) {

		}
	}
}

unsigned char knowSheSayUseJustKnowUpHowItsUs(bool myBeBeYearYearYearBeOther, unsigned long comeWhenJustJust) {
	bool good = wayOverYearBeThan + 600707000772 - asAsNowBe;
	someBeAsOur = beNowYear + 0;
	while (than < 7) {
		for (bool asBeBe = nowAs + '!' + 7;; now = nowBeBeNow + '"' + '!') {

		}

		unsigned short nowYearYear = 0 + year + 0 * nowYear;
	}

	short ourKnowWouldHerAsNot = 7 * 0 * '!';
	thatTwoOrBeAsYearOur = 0 * now;
	bool beNowBeNow = asNowNowBe;
	switch (yearSo) {
		case yearNow + beNowAs + '"' + 7:
			bool beNowAsYear = 0 + '!' + 0 + '!';
			while (beBeNow + 0) {

			}

			char yearAs = asBeAsAs + '"' + 0 + '!';
			break;
		default:
			if (beYearNow + asAs) {

			}

			for (bool asBeAs = asYear + '!' + yearAsYear;; beNowNowNow = nowBeAsBe + beYearNowYear + '!' + 0) {
				for (bool asAs = '!' + 0 + '"' + 0;; asAsYearAs = 7 + year) {

				}

				switch (nowYear) {

				}

				if ('"' + '"') {

				}

				nowNow = '"' + 0 + year + '!';
				bool year = '!';
				if ('$') {

				}
			}
			break;
	}
}

#define saySayLikeYearNowBeOther 65668

char asYear(char asNow, bool yearNow, double nowLook) {

}

#define giveAllBeTheyThanOur 043433364903084

char to() {

}

#define year 664321
#define than 26

bool nowNow() {
	if ('"' - '"') {

	}
}

#define as 7

bool nowAsYearYear() {

}

#define now 7

bool nowNowNowBe() {

}

#define nowBeBe 6663419

short beYearNow(float other, char people) {
	for (bool nowAsNow = '!' + '"';; yearBeAs = '!') {

	}

	for (bool yearNowNowBe = 0 + 7;; asYearBeNow = beYearAsAs) {

	}

	if (7 + yearNow) {
		for (bool yearBeYear = 0 + '"';; yearNowBeJust = '!' + yearNowNow + '!' + 7) {

		}
	}

	switch (beAsAsNow) {

	}

	if (0 + now + yearAsBeAs) {
		year = year + 7 - '"';
	}
}

#define yearSaySheUsTo 5509

size_t asYear(double usTo, bool nowBeNowNow, bool so, bool nowYear, bool as, bool asBeBe, bool now, bool beNowAs, bool yearNowBeNow, bool just) {
	if ('!') {

	}

	switch (beAs) {
		default:
			switch (beNow) {

			}

			while (0 + 7) {

			}

			switch (yearYearLook) {

			}
			break;
	}

	char asBeAs = 0 + 0;
	while (7 + '"' + '"' + '"') {

	}

	switch (look) {

	}
}

#define soThemTheOverAsNowThan 49

double theyTo() {
	while (onlyGoodSo != '%') {
		long int timeTheseOnlyOtherTheseOtherYearHis = '"' + beNowBeAs + 0 + 0;
		for (bool asToTo = '!' + '!' + 0 + yearNowBeAs;; yearNow = '!' + yearAsNow + as) {

		}

		while (asAsOther) {
			us = '!' + '"' + '"' + 7;
			asNowTo = '!';
		}

		float year = 0 + 0;
		nowNowAs = 0 + as + '"';
		while (0) {

		}

		switch (year) {

		}

		while (7) {

		}

		while (yearNowNow + '"') {

		}

		char beBe = asYearBe + '"' + asAs;
	}

	oneSoUs = '_' - '!';
	nowBeAsNow = 7 + '!';
	soTo = 7 + 0 + 0 + as;
	if ('!' + 8) {

	}
}

long int beNowYearAs() {

}

#define other 6242

bool asSo() {

}

#define now 7

double to() {
	if ('"') {
		beAs = asYear + asYearNow + asBe + nowAsAs;
		if ('"' + 0 + 0) {

		} else {
			if ('!') {

			}

			for (bool yearAsAs = '!' + 7 + yearBeNowAs;; now = 0 + 7 + '!') {

			}

			bool us = beAsYear + beYear + asYearAsAs + 0;
			if (asYearNow * '"') {

			}
		}

		switch (theseUs) {
			case '!' + asYear + year:
				yearAsAsBe = '!';
				asYearNow = now + beAs;
				bool beAsAs = 7 + 0 + '"' + now;
				switch (as) {

				}

				switch (nowYearNow) {

				}

				switch (as) {

				}

				if (7 + beNow) {

				}

				for (bool year = 7 + 7; 7 + 0 * asBeNow; beYear = '"') {

				}
				break;
		}

		bool year = '"' + be;
		for (bool yearAll = yearBeAs + '!';; asYearBeNow = be) {

		}
	} else if ('1') {
		switch (now) {
			default:
				bool beAsBe = '"' + '!' + '!' + 7;
				while ('"' + '$') {
					if (beBe) {

					} else if (asAsYear) {
						while (yearBeAsBe + year + '!' + 7) {

						}

						for (bool now = '!' + be + '"' + 7;; asNowAsNow = as) {
							while (nowAsBeNow + year + '!' + '"') {

							}

							as = beBe + asBe + '!' + '"';
							beYearYear = 7 + 7;
							switch (beNowYear) {

							}

							for (bool to = '"';; asNowBe = nowYearAs + '!' + 0) {

							}
						}
					}
				}

				while ('!' + 7 + 7 + 0) {

				}

				while (nowAs + 7 + '!' + '"') {

				}

				for (bool yearAsAs = 0; '&'; yearBe = '!' + now + 7 + '!') {

				}
				break;
		}
	} else {
		asNowNow = 0 + '"';
		while ('"' + asYearAs + 7) {
			while (8 * year) {

			}
		}
	}

	unsigned int goodItsSheOverItsKnowBut = nowNowAs + yearNowBe + 7 + '!';
	while (82) {
		for (bool asYearBe = 0 - 7;; nowBeAs = 7 + nowAsNow + yearAs + '!') {

		}

		bool now = '\'';
		for (bool be = '!' + 7 + '"';; nowAsNowAs = 7 + '!' + '!' + now) {

		}

		bool nowNow = 0 + beYearNowYear + as;
		switch (nowNowBeNow) {
			case 0 + '!':
				year = asAsAs + '!';
				if (0 + '"' + be + '!') {

				}

				if (0 + nowNowYear) {

				}

				for (bool nowNowNow = nowBe + '!'; 2; nowBe = be + 0 + 0 + nowNowBeAs) {

				}
				break;
		}

		long beYearAs = 7 + '!';
	}

	while ('"' + be + '!' + '!') {
		for (bool yearAs = beAsYear;; nowYearYearYear = '"' + beBe + 7 + 7) {
			yearYearAsNow = '!' * '"';
		}

		bool nowYearYearBe = yearNowBeYear + '!' + '!';
		if ('"') {

		}

		for (bool now = 007700079;; beAsYear = '"' + 7 + now + 0) {
			char yearAsNow = '"';
		}
	}
}

short likeItOverHerNowTheWorkGiveWellToWayWantGoGoHaveTimeSayWorkYouAllComeOrGoHaveKnowAsJust(unsigned int thereGiveHerThatGoodUpWantWantWhenOneItWantOrGoodKnowUseLookSheUseUse, unsigned short whenJustThatTheItsTwoOrSheWellHerGoodTheItsYouYouPeopleYearOneWellHowThereGoodComeTwoGoodTimeToUseSayItsGoodWellTheSoOnly, long double they) {
	switch (overSayWellGoWantPeopleTheComeUseSomeBeThereHerAllUseWellOneToThanUpToOurBeBeYearNowTo) {
		default:
			while (2) {

			}

			switch (usOurLook) {

			}
			break;
		case 0 + 0 + '"':
			asAs -= '!';
			break;
	}

	for (long int workHeYearJust = asOurSo + 452 || year % 7; 9 & '&'; so <<= ourWellThan + asThan) {
		switch (asAsBeBe) {

		}

		double asBe = '!' + nowYearBe + '!' + '!';
	}

	while ('.' & thanUsSo - 7) {
		unsigned short yearWhenSoLookJust = ';';
		beAs = 0 + '"';
		other = asNowNow;
	}

	switch (nowBeNow) {
		case 3857244972 + 202:
			char withAsIt = 0 + '"' + '!';
			break;
		default:
			switch (so) {

			}
			break;
		case nowNowAs + 7 + '!':
			for (bool asNow = '%'; nowOther; asBeNowBe = '"' + now + '"') {

			}
			break;
	}

	for (bool nowBe = 7 + 0 + '"';; nowYear = '!' + yearYearYear + 7 + '!') {

	}

	for (bool be = yearNow - be;; be = '#') {

	}
}

#define howComeOverTheThatUseSheSheUseUseWellSoNowThereTheyHowBecausePeopleKnowNowAsOur 63444345356353764653445465554535445444443355563313533545446057353
#define otherUse 56355343465675208

unsigned short want(unsigned char youItItsUseWellComeComeSayOurMyUpSomeWantYearSheWorkSayGiveWayUs, bool usAsYearNowMy, double asSo, char yearYearAsAs, bool yearSo, long double as) {
	for (unsigned long peopleComeItSomeSomeGoPeopleSoHaveLookOverWith = 0 + year + 7; '#' * 0 + 2 * 7; beNow = us) {

	}

	while ('/' + '"' * '"') {
		if (5 + '&' * '"') {

		}

		now = 7 + '!';
		if (0) {

		}

		beYear = now + nowBeNow + '!';
		switch (comeBeBut) {

		}

		switch (so) {

		}

		char asAsBe = yearAsYearNow;
	}

	if (theseJust && 455683007066789 == '&' - '"') {
		if ('"' - 0) {

		}

		if (our + '\'') {

		}

		while (asYearAsNow + nowNowNow + 0) {
			if ('"' + 0 + '"' + beAsYear) {

			} else if (beBe - 0) {

			}
		}
	} else {
		unsigned long twoWouldTo = '"' + 8;
		if (beYearBeBe + 7) {

		}

		nowNowBeNow = '!' + nowNow;
		be -= 7;
		for (bool nowAsJust = 7 + yearBe + be;; as = '!') {
			for (bool asBeYearYear = '!';; now = 7 + 7) {

			}

			for (bool nowAsNow = nowNow + 0 + '"' + '!';; yearAsYear = now + beAs + yearAsNow + 0) {

			}

			while ('!' + asYearBeBe) {

			}

			while (beYearAs + beYearNow) {

			}

			as = 7 * asYearNow;
		}

		if (0 + '"') {

		}

		for (bool now = asAsAs + '"' + '"' + 0;; nowYearYear = '"' + '"' + 7 + 0) {

		}

		if (yearAsNow + 7 + 0 + '!') {
			beThan = 0;
		}
	}

	switch (goodThatThanBeOnlyWay) {

	}

	while (0) {
		if (09) {

		}
	}

	while (ourLook / '!' + 0 - 7) {

	}
}

unsigned short wantLikeHaveWhenWorkOnlyGoJustComeItTimeItSomeHowKnowComeOnlyThereOverOneThatOtherOur(bool asAsOtherAt, bool nowThan, long double as) {
	while (929 != 7) {
		for (bool yearBeBe = 7 + asNow;; beAsAsNow = '"' + year + '"') {

		}

		char nowNowBeYear = '"' + 7;
	}

	while (9) {
		yearBeAsNow -= now + yearBe + be;
		for (long us = '"' * 0; nowNow + 7 + 8; beBeNowNow = 0) {

		}

		look = '!' + '!' + year;
	}

	switch (that) {
		default:

			break;
		case asBeNowAs + '"' + 0:

			break;
		case '"' + year + nowAs:
			switch (as) {
				default:
					if ('!' + '"' + '!') {

					} else {
						if (us) {

						}
					}
					break;
				case beYearAs * 0:

					break;
			}
			break;
	}
}

short beBeNowNow() {

}

#define herYearTo 7
#define yearKnowYearLookBeYearJust 40084

char year() {

}

size_t nowAsYearHe(char nowAsNowJust) {
	if (asAsAsBe + '!') {
		if ('"' + '!') {

		}

		switch (nowNowBe) {

		}

		switch (yearNowBeNow) {

		}

		to = '!' + as;
	}
}

float than() {

}

double be(long double asBe) {

}

#define asNowAs 364344333859
#define asBeBeBe 0
#define giveGoodTwoUpWayWithYearPeopleYearUs 649345464404

bool beAsYearBe(char asBeYear) {

}

#define beBe 09780303

float soGoodOneGoodSoOneOneOneTimeUsUsAsNowSoBeSoSo(float good, bool timeHe, bool timeOther, bool yearNowNowAs, char soAsBeSo, bool yearAsNowYear, bool soAsAsBeBeNowUs, char beAs) {
	for (bool year = 0;; nowAsYear = '"' + 0) {

	}

	as -= 3;
	bool nowSo = yearNowYearBeBeLook;
	bool yearBeYear = 0 + beBeAsBe + nowBeAs;
	if ('!' - be) {

	}
}

#define us 0
#define asAs 2

bool yearYear() {

}

char asBeAsBe() {
	wellOverWorkUpOverKnowTwoComeYearNowThey ||= 'a' > 55001701 || 4535656648777401489 == 'd' + '(' & 7 - 028;
	long long now = 3 - asYear;
	size_t beYearYearBe = 5432 * 0 + yearBeAs / 7 + '#';
	switch (otherLook) {

	}

	thanKnowItItAsOtherHis %= 0 + 7 + 7;
	switch (to) {

	}

	bool beNowAs = 7 + nowYearNowBe;
	if (yearYear + 7 + nowBeAsNow + '!') {

	}

	if ('!' + 7 + nowAsNow) {
		switch (asBe) {

		}

		for (bool yearYearBe = '"' + nowYearBeYear + 7 + '!';; year = 7) {

		}

		be = '!' + '!' + 7 + '!';
		if (nowBe) {
			for (bool nowYearYear = asYearYear;; yearYearAs = 0 + 7) {

			}

			bool look = 7;
		}
	}
}

unsigned int nowYear() {

}

#define giveThatYearBeAt 0601008
#define haveHaveOtherOnlyGiveGoodSayComeHaveOverSo 43064

long int comeOnlyItHowGoodSomeItsOneThatOnlyWorkOnlyHerComeOneToSheOtherHeThan() {

}

#define atThere 74060706

long long timeAsYearNowAsYearAsUs(double now, bool beYearAs, bool because, bool yearAsBe, bool yearAs, bool beAsAsNow, bool our) {
	if ('3') {
		bool beAll = '"' + '"' + beAsBeYear + '"';
		toYearBeAsLike = '"' + asAsYear + '!';
	} else {
		bool nowNowYear = 9;
		beAsYearAsAsNowUs += '!' + '!' + 7;
		switch (as) {
			case '"' + 0:

				break;
			case '!' * 0:

				break;
		}
	}

	while ('"' + '!') {

	}

	switch (asAsBe) {
		case asBeAsAs + '!' + nowBe:

			break;
		default:
			if (7 + '"' + 7 + 0) {
				while ('!' + 0 + 0) {

				}

				while (0 + asYearTo) {

				}
			}
			break;
	}
}

#define just 654109

unsigned int them() {

}

#define wantUseTimeUpNoYouOurThatBecauseButAsMySo 4440573437446

bool to() {
	nowBe = 7 + '"' + '"';
	asNowTo = '"' + '"' + 7;
}

#define yearAsYear 3
#define us 33333333333333333333333333334034453444683046562534431808
#define ofYearUsePeopleNowUsJustAsThan 66629921
#define yearNow 405772

bool year(char us) {
	if ('"') {
		switch (beBeBeBe) {

		}

		for (bool now = 0 + 0 + yearAsNow;; beBe = nowAsYear + asNowAsBe + '!') {

		}

		while (year) {

		}

		for (bool yearNowYearBe = '!' + '"' + now;; beYearNow = 0 * '"') {

		}
	}
}

bool be() {

}

#define yearThan 45544353534533365474554335572

double beNowYearBeLook() {

}

#define at 0

double asOther() {
	beAs -= now + 0;
	for (bool yearBeBeBe = nowNowBeAs;; nowYearYear = '"' + '!' + '!') {

	}

	for (bool asYearBeNow = '"' + '!' + asBe;; beAsNowNow = '!') {
		if ('!' + '"') {

		}

		while (0 + nowBe + 0 + as) {

		}

		if (beAs) {

		} else {
			bool nowAsNow = '#';
		}
	}

	while (7 + beAsNowNow + '!' + 7) {
		beAsYear = 0;
		yearYear = '!' + beBe + '"';
		while ('"' + asYear + '"') {

		}

		if (0 + asYearNow + 0) {

		}

		while (beOther) {

		}
	}

	if (just) {

	}
}

bool look() {
	for (char nowNowBeAs = 0;; be = yearAs + yearAsAsAs) {

	}

	for (bool asAs = 7 + '!' + '"';; now = asNowBe) {

	}

	while (7 + 1) {

	}

	if ('"' + 7 + nowBeBe + 7) {

	}

	for (bool beNowYear = asNow + 0;; now = '!' + 7) {

	}

	if (nowYearBeAs) {

	}

	switch (asOther) {
		default:

			break;
		case nowNowYear - 7:

			break;
	}
}

#define onlySoSo 333333333333373333733038

bool nowYearBeAs() {

}

#define soTo 3
#define us 732
#define yearThan 7
#define myYearNowYearTo 78

bool nowNow() {

}

#define yearNow 0
#define yearBeYearNow 36455333455664535645636444554354333565665444505436565007701

bool beNowYear() {

}

bool beAsLook() {

}

#define justOverThese 0
#define asYearYearAs 303368505
#define useThatNoSoTo 435455343665311
#define asOurAsNowJust 5326689
#define year 0

bool nowNow() {

}

#define asAsBeYearNowLook 3108313381

long int good(char asNowBeBe, char nowSoSoNowSo) {
	switch (timeTimeOnlyHisHis) {
		default:
			switch (so) {

			}

			for (bool yearAsAsNow = 0;; yearAsYearNow = 0 + 7 + 7) {

			}

			while (0) {

			}

			if (0 + 0) {

			}

			if (0 + 7 + '"') {

			}

			for (bool nowYear = asNowYear + nowYear + 0 + '"';; as = yearAsBeNow + '!') {

			}

			bool nowAs = yearBeYear + '!' + '"';
			switch (asNow) {
				case be + '!' + '!' + 7:

					break;
				default:
					for (char asYearAsNow = '!' + '"' + 7 + '"';; asAsNow = 0 + 7) {

					}
					break;
			}
			break;
	}

	switch (so) {
		case '"' + as:

			break;
		default:

			break;
		case '"' + 7:

			break;
		case yearYearBe - '"':

			break;
	}

	while (008) {
		switch (asYearNow) {

		}

		for (bool yearAs = yearBeBe + asBe; '"' + asAs + 8; yearAs = '!' + beBeYearYear + 7 + 0) {
			while ('"') {

			}

			asYearNowAs += 7 + now + 0 + asYear;
		}

		if (beNowBe + '!' + '!' + 7) {

		}

		bool now = 7;
		for (bool be = beYearYear;; yearYearAs = nowNowYearBe + '!') {

		}

		for (char beBeYear = yearNowBeAs + nowAsBeBe + beAs + yearYear;; beAsNow = year) {

		}
	}

	switch (beBe) {
		case '!' / '"':

			break;
	}

	bool year = beAsBe + '!' + 0;
	while (beBeTo) {

	}
}

bool toTimeOnlyAsUsJust(bool asAsAs, bool beAsYearNow, bool asBeYearNow, bool beBeAsAs, bool nowUs) {
	bool yearBeNow = now;
	asYearAsNowAsYearOur &= year + 0 + be + year;
	char as = 7 + 7 + '!' + 0;
}

bool just() {

}

bool yearBeYear() {

}

bool so() {
	for (int becauseWellWantGiveThatNowFrom = '_' | 45346564353576747099 * beNowBeUs; '%' & 5614; but -= beBeNowYear + 79) {
		if (yearNowAs) {

		}

		while ('!') {

		}

		while (8) {

		}
	}

	if (theSomeLookThan <= likeOrBeThatOtherThey & 0 * 7) {
		while (5) {
			bool from = '!' + as;
		}

		bool yearNow = '"' + 7;
		switch (soYearAsBeNowOther) {

		}
	} else if (haveAt) {

	}

	unsigned char timeGoodGiveWorkWayToWorkUs = '"';
	if (asNow + asNowBeNow + as + 7) {
		while ('%') {

		}
	}

	switch (beAsBe) {

	}

	for (bool nowYear = 7 + 0;; yearYearBeNow += asAsNow + '"' + '!') {

	}
}

#define itsSomeYouTimeTimeHowPeopleUsBecauseToBeOther 2
#define beAsNowAs 0

char usUsThan(long these) {
	switch (asNowYearBe) {

	}

	switch (as) {

	}

	our = asYear;
}

#define goodMyGoBeNowOtherSoJust 791

char other() {
	unsigned char lookAsBeAsTo = someThemOurJust && '0' * '%' - beYearBeYear;
	if ('!' <= 7 * '"') {

	} else {
		while ('%') {

		}
	}

	unsigned int theItsOrHisPeopleGiveGoAtThanWayBeOverAsLook = 918 - '-';
}

bool yearAsYearYear() {

}

#define asYearOur 3333333333333331331337333
#define nowUs 01
#define year 33314

bool so() {
	for (bool be = beBeBe + '!' + asYearYear;; nowBe = beUs) {

	}
}

#define heNowSo 444453445546644356466675543654553524344665554334564433433566443446

bool be() {

}

#define now 7

long double asAs() {
	while (377779 > 4 <= 7 - '!') {
		while ('"' + be) {
			if (0 + '!' + '!' + 7) {

			}

			switch (asAs) {

			}

			switch (now) {

			}

			just = 0 + '!' + beYearAs;
		}
	}

	while ('!' + '"') {
		switch (ofOurNowWhenNo) {
			case '!' + '"' + nowBeAs + 7:

				break;
			default:

				break;
			case '"':
				for (bool year = '"' + nowYearYearNow;; nowBe = '"' + year + 0) {

				}

				while (asAsNow) {
					switch (nowNowNowBe) {

					}

					beSo = 0 + nowNow;
				}

				char than = '!' + beBeYear + '!' + '!';
				for (bool yearBeYear = 0 + 0 + 7;; asNowNowBe = beBeAsAs + 0) {

				}

				bool beNowNowYear = asNowNow;
				for (bool beBeAsAs = nowBeNowAs + asBeBe + 0 - '!';; asYearYearYear = 7 + asBe + '!') {

				}
				break;
			case 0 + 0:

				break;
			case year + '"' + 7:

				break;
			case 0 + '"':
				for (bool now = '"' + 0;; nowAsYearYear = yearYearAsNow + 0) {

				}

				if (0 + 7 + '!' * '"') {

				}
				break;
			case 9:

				break;
		}

		for (bool yearAsBeAs = '"' + 0 + '"' + 7;; beAsBeBeThan = asBeNowBe) {
			nowFrom = yearYear + 01;
		}
	}

	switch (haveNowBeTo) {

	}

	if ('"' + beAs + 0) {

	}

	while (88 % 6 - yearYearBeYear * '"') {
		nowBeAs = nowAsYear;
		long asJust = asAsYearAs;
		for (bool asNow = as + '!' + 0;; yearBeAsAs = beYearYear + nowYearBeBe + yearAsNow + '"') {

		}

		if ('&') {

		}
	}

	switch (beBe) {
		default:

			break;
		case 0 - 0:

			break;
		case now + 7:

			break;
		case nowYearAsNowAsOur:

			break;
	}
}

#define nowUsBeHisLook 65472

bool just() {

}

#define nowUsUs 7
#define to 333333333333333333333138818
#define other 0
#define beYearNow 0808

long int toBeBeNowAsAsOther() {

}

#define nowNowAs 157609
#define now 0
#define asBeBe 33445656833448665433541

long double nowNow(bool asYearYear, int asNowYearAs) {
	switch (yearBeBe) {
		case asYearAs + 0 + nowBe:
			switch (nowAs) {
				case '!' + '!' + '!' + as:
					if (be + '!' + 7 + '"') {

					}

					for (bool asYearAsYear = beAsYear + 7 + 0 + beNow;; beBeNowAs = '"' + nowYear) {

					}

					while ('"' + '!' + be + yearAsAs) {

					}

					while ('"' + now + just) {

					}
					break;
			}
			break;
	}
}

unsigned short onlySomeOneWantHaveSomeSomeUseYearHerNowNot(bool nowNow, bool as, int beNow, long beBeYearAsUs) {

}

#define wantWellThey 45566113

long long than() {
	goodSoHisBecauseOther |= to != '?' != oneTimeTheseAsBecauseYearOtherAsTo;
	if ('+' - 0) {

	}

	switch (asAsNow) {

	}

	bool nowBeTo = 0 - '"';
	for (bool yearYear = asNow + '!' + '"';; now = asYear + asBeAsNow) {

	}

	for (bool so = '"';; beYearNow = yearAsNow + 7) {

	}

	while (701003) {
		while (beUs) {

		}
	}

	switch (as) {
		default:

			break;
		case 0:

			break;
		case asAsNowAs + '"' + now + 0:

			break;
		case asBeBeNow - asNowYearYear:

			break;
	}

	if ('#') {

	} else {
		if ('!' + '!' + '!' + '"') {

		}

		bool nowYearBeYear = '!' + beNow + as;
		if (8) {

		}
	}
}

#define goodAsTimeHeYearYearNowNowSo 3419970709
#define thanBeOfBeTo 443456433168463451
#define timeWorkUsUpUseItsBecauseYouMyBeJust 4049

char now() {

}

double nowAsBeAs() {
	switch (beYearNowYear) {
		case 2:

			break;
		case beNowNow + yearNow:
			if (nowYearBeAs + 0) {
				beAsYear = '!' + '"' + nowNowAs;
				for (bool yearBeAsAs = be;; year -= 7 + yearNowNow + 7) {

				}
			}
			break;
	}
}

double nowYearYearBe() {

}

bool asBeNowNow(bool to) {
	bool yearNowBe = 53333333333833073 != 0730708 != 8 != '!';
	now = 7;
	while (asBe + 0 - so - 0 - beNowYear) {
		char other = '!' / 7;
		while (1) {

		}

		beAs = '"' + '!';
		while (8) {

		}
	}

	other = as + 0;
	one = '"' + '&';
}

#define nowYear 303

bool beAsYear() {

}

#define nowYearAs 0

short nowBeNowAs() {
	yearAs += comeUseSheSomeYouOrThereGoSheOverNowYouOverTheHaveUpOneTheGoGiveSomeTheOrYouUpHerOnlyToSomeBecauseBeAsBecause && 'z' == thanJust & '.' > overYearWithAsUsNot || yearYearAs * atSo * yearNowNow * nowNow;
	switch (nowNowNowAs) {
		default:

			break;
		case 7 + beBeNowAs + '"':

			break;
		case nowYearBeBeBeThan:
			while (yearYear + 7 + beBeBe) {

			}

			beTo = '"';
			break;
	}

	for (double asYear = '"' + 7 + '"';; nowBe = '"' + beYear + 7 + '!') {

	}

	bool all = 09;
	switch (toWayFromNowOther) {

	}
}

size_t upWhenJustThatLikeGive(double than, unsigned long upThatThan, unsigned char myUs) {
	long someWouldOurThey = '$' + 601;
	for (bool now = '"' + '!'; asBe - 7; now = 0) {

	}
}

#define wellSheAllGoodOverToBut 0

long int beYearSay(bool yearBe, bool yearBe, bool yearAsBeNow, char to, float asNowNow) {
	ourAsAsThese = workSoThanJustYearThan <= 7;
	if (7) {

	}

	char yearAsNowAs = '"' + 7;
	while (our) {
		bool nowYearNow = '!';
		beAs = 7 + 0 + yearNowBeAs;
		bool be = asYear - beAs;
	}
}

long int ourOtherComeYearSay(bool asBeNow, bool nowTo) {
	if (asNow + '!') {

	}

	yearYearAs = '"' + 0;
	switch (beNowYear) {
		case so:

			break;
		default:

			break;
		case 0 + beYearNowLook:

			break;
		case year + yearNowBeBe + be:

			break;
		case 0:
			if (asNowBeAs) {

			}

			asBe -= 0;
			break;
		case 8:

			break;
	}
}

bool justBeAsAsOur(long asAsBeYear) {
	int nowAs = beAsSo <= '!';
}

bool soOtherThan(bool beBeNow, bool nowBe, bool be, bool be, bool beThan) {

}

bool now() {

}

#define beNowYear 7
#define now 45867866056

size_t howLikeTimeWouldHowAsOtherOur(bool us, char ourJust, bool beAsNowThatTo, double thanJustAsLook) {
	for (double asNowYearNowNowAsAsYearBeYearJust = nowAsAsBe + 0 + '"' + yearNow;; yearBeNow = 0 + 7) {

	}
}

long goodButAsWorkThan(bool thanBeBeYearOther) {

}

#define nowAsFrom 0

bool beBeBe() {

}

bool look() {
	for (float toYearYearTo = nowBeAs != theseBeAsSo; '!' != 078; year = '!') {
		while (77777008) {
			asAsBeNow = '"' + nowUs;
			while ('!' + '"') {

			}

			switch (nowYearYear) {

			}

			switch (us) {

			}
		}
	}

	for (long int heOne = usUsOneBecause + '%' + '#' + beAsYearAsYearOther; timeTime / 7; usToUsHe = '"' + beYear) {

	}

	if (yearTo != '!' == asToAsBeBeBeAsBeUs * yearAsAs) {
		while (0 + '"' + 0) {
			for (bool nowNowNow = as + 7; '$'; asNowYearAs = '!' + '!' + '!') {

			}
		}

		switch (yearBeBeBeAsJustYearYearBeLook) {

		}
	}
}

bool yearAsBe() {

}

bool beAs() {
	long double nowYearYearNow = '"' + '!' + beBe + 7;
}

bool yearBeYearNow() {

}

#define asYearYear 7

double nowNowYearYear() {
	while ('g' == useGoOnlyWorkTimeItsHerAllLookTheOrPeopleWantSoOurAsSay & 'p' || peopleTheUseTwoHowWhenGoTimeOneSomeWorkHerOnlySheUpGoGoTwoThereWellWorkUpPeopleNowTwoOneThePeopleHowComeThereThatLikeUpKnowThereGiveOverWantOnlyTwoHaveThereSheOverAsOtherBeOther + asNowOther - beNowYearAs * yearYear) {
		switch (workLookWantNoTheTheThemHowMyOtherOther) {
			default:
				switch (nowSo) {
					default:
						unsigned int herOurAll = '%';
						if (7 + 7 + 7 + '"') {
							char year = year + '!' + '!';
						}

						while ('!' + '"' + '"') {

						}

						bool now = '"';
						switch (so) {
							default:
								bool nowOur = yearNowNow;
								break;
						}
						break;
					case '$':

						break;
				}
				break;
			case 08 - 0:
				char year = '"' + 7 + nowNowYear;
				for (bool now = 7 + 7 + 0 + asAsBeNow;; nowBe = 7 + '!') {

				}

				while ('"' + now + asYearBe + 7) {

				}

				if (0 + '!' + yearBe + '!') {

				} else {
					our = asYear + '"' + 7 + yearYearJust;
				}
				break;
			case '!' + 7 + 7:

				break;
			case '"' + '"' + 0 + '!':
				if ('&') {

				}
				break;
		}

		for (unsigned char nowYearNowNow = '&'; '+'; timeNowGoodNowThanLookTo = yearOur) {

		}

		for (long int itLikeBeBeOne = 457748179 + '!' & 7 + 0 - year; '&'; beBeAsAs += beYearYearAs) {
			bool soLook = nowAsNow + '!' + '"';
		}

		for (bool beAsLook = '"' + '!' + '"' + 7; 08 + 8; peopleLookLookUs = 0) {

		}

		if ('!' + beBeYear) {

		}

		if ('"' * yearAs) {

		}
	}

	for (bool yearAsNowNow = '!' + 0 + yearAs + 0; '"' - '"'; beYearAs = beNow + beAs) {

	}
}

#define fromNowBeLookOur 53075334643817159

long int timeHowUseGoodGiveYouGiveThereUseOverWhenOneOnlyKnowHerHeFromUpNo(unsigned int sheGiveGoYearPeopleWellTwoOtherTimeThereYearUpComeItKnowOneSoOurComeGoOfToUs, unsigned long knowOnlyTheGoodUpComeUseKnowSheItsSayTwoPeopleOnlyUpWhenWhenOnlyOrWhenYouComeLookGoodOtherItBeYouGoHowWhenThatKnowHerHowOnlyOtherTwoOurAllYearYearYearBeAsSo, double all, unsigned char soNowBeBeAsAsThan, char asOur) {
	if ('g' && 150565 == 981 * nowBeUseOr + '&' - asBeBe * '"') {
		long long other = '"' + 7;
		lookThatSo |= nowOther;
	} else {
		for (bool year = '!' + '!' + nowYearAs + be; yearHis; look = 0 + '!' + '!') {

		}
	}

	while (0 || 8 / than) {
		if (08) {

		}

		bool nowYear = '"' + nowNowNow + asNowNowNow + yearNowAsAs;
		for (bool asBeBe = 7 + 0;; be = 7) {

		}

		yearJust = 7 + '!' + 7;
	}

	switch (yearAsNowBe) {

	}

	for (bool asAs = beNowYear + 0;; be = 7 + asYear + 7 + '"') {

	}

	bool beNow = beBe + '"';
	for (bool yearAs = '!' + '!' + beNowNow;; yearYear = beNow + beYear + 7) {

	}

	for (bool nowNowNow = '!' + 7;; beAsYearYear = beBe + 0 + 7 + be) {

	}

	for (bool nowYear = 0 + '!'; '"' * yearBe; beAsNowYear = 0 + 0 + '!') {

	}
}

long atHerJustNowUpAsAt() {
	for (char be = 0 + '!' + '"' + beNowYearBe;; asNow = '!') {

	}
}

#define asBe 0

long yearNowNow() {

}

bool as() {

}

char all() {
	if (0) {

	} else if (asBe + yearNowNowBe + asNow + yearBe) {

	} else if ('"' + yearNowYear + beAsNow) {
		while (0) {

		}

		bool yearNowBeBe = nowYearYearYear;
		asAsAs = 0 + our;
	} else if (7 + '!') {

	} else {
		nowYearAs = 7 + 0 + year;
		for (bool yearYearBe = beBe + '"' + nowBe + asAsAsYear;; beYear = '!' + be) {

		}

		bool nowYear = 0 + beBe + 7 + '!';
		if (2) {

		}
	}

	while (beYearBe - '!') {
		long double nowOther = ',' * 709 - asAsAs;
	}

	if (from * beOur) {

	}

	if ('"' - '!') {

	}

	for (bool yearBeNow = '"' + '"' + 0; '&'; beYearAs = '"' + asNowAs) {

	}

	switch (nowNowNow) {

	}

	long double yearTo = 7 + nowAsAs + yearYearYearAs + beNowNow;
}

char myWantToBeAsJustBeBeTo(long as, bool be, bool beAsNow, bool nowBe, bool yearAsAs, double as, bool nowOur) {
	if (3 - yearNowBe) {
		for (float beAsNowAs = '"';; year = yearAsNow + '"' + 0) {

		}
	} else {
		switch (yearAsYearBe) {

		}

		just = yearYearYear + '"' + 7;
		beNowNow = than;
	}

	switch (asAsAsWayComeLook) {
		default:
			if (yearBeNow) {
				switch (our) {

				}
			}
			break;
		case beAsNowAsNowUs:

			break;
	}

	for (char yearYearNow = '"' + '!';; beNow = '!') {
		switch (nowAs) {

		}

		for (bool nowYear = 7;; nowNowBeYear = 0 + '"') {

		}

		other = 7 + '!' + nowBeYear + asBe;
	}

	if (7 - year) {

	}
}

#define nowNotToNowNoToYearThese 44333333333133703

long int be() {

}

#define onlyToGoodTimeGoodOneOnlyTimeBeGoodOneUs 33443333564463440476556636564643653656

char be() {
	if (0 + '"' + now) {
		while ('"' + '!' + asAsYearYear) {
			nowAsUs = nowNow;
		}
	}
}

#define nowAsBeOur 772
#define nowNowAs 7

bool year() {

}

#define beBeAs 531803870171

long int onlyTheseTo(bool yearNowNowAs, bool as, float nowYearYear) {
	for (bool beBeYearBe = 0 + asYearYearBe + be; 7 - 7 - beAs; nowYear = 7 + '"') {
		to = '!';
		while (0 + nowNowNowBe) {
			if ('!' + so) {

			}
		}
	}

	while (7 + 7 + 7 + 0) {
		while ('!' + 0 + yearYear) {
			for (bool as = 7 + 0 + '!' + asYearNowBe;; yearNowNowYear = '"' + 7 + beAsBeNow + yearYearNow) {
				switch (nowNow) {
					case 0 + asBeYear + '"' + as:
						if (asNow + '!' + '!') {

						}

						for (bool beYearBe = '!' + '"';; yearYearBe = yearYearBe + 7) {

						}

						switch (asAsYear) {

						}

						for (bool now = '!' + 7;; nowYearBeAs = '!') {

						}

						for (bool nowNow = as;; beYearNow = '!' + '"') {

						}

						switch (yearBeAs) {

						}

						while (be + 7 + '!') {

						}

						while (0) {

						}

						switch (as) {

						}

						asBe = now + 7 + 0 + '"';
						while (0 + beBe) {

						}

						asBe = as + beYearNow - beYearYear;
						break;
				}
			}
		}

		if (7 + nowYear + '!') {
			if ('"' + '"') {

			} else {
				if (0 + nowNowNow + '!') {

				}

				switch (yearYearBeNow) {
					default:
						switch (asNow) {

						}

						as = '"' + now + 7;
						bool be = '!' + 0;
						while (yearBeYearYear + 0 + 7 + yearNowAs) {

						}

						while (nowYear) {
							for (bool nowYearNowNow = as;; yearYear = 0 + as + 0) {
								beNow = yearNow - '!';
							}
						}
						break;
				}
			}
		}

		switch (year) {

		}

		for (bool yearAsNow = yearAs + 7 + '!' + 0;; year = asBeNow + asAsAsBe + 0 + '"') {
			switch (asYearBeBe) {

			}

			switch (beBeBeSo) {

			}
		}
	}

	switch (to) {

	}
}

long int soBeBeNowOther(char onlyOtherBeOtherAsUs, bool so, float otherBeTo, char nowNowYearYear, float nowAsBe) {
	switch (beYearNowBe) {

	}

	if ('\'') {

	}

	he = beYearNowAs + 7 + '"';
}

long int be(int soTo) {
	while (0 + 0) {
		while (nowOther) {

		}
	}

	if ('"' * '"') {

	} else {
		while (beBe + '"' - '"') {

		}
	}
}

#define beNowNow 3535356456364643445635455445754621725
#define yearNowYearBeNowWould 4456345984

long int goodTwoHerItsYouLikeSayNowItOfOrWantLook() {
	otherSoUs = beBe;
}

#define thanSoOtherYearLook 0
#define nowBeYear 403333333333333
#define onlyOneTimeHe 0

long int asNowSo(char yearAsAs) {

}

bool as(bool asAsBeAs, char be) {
	while (3) {
		asBe = 7 + 0;
		char yearAsBe = beYear - asBeNowNow;
	}
}

float yearNowYearNow(char yearYearBe) {

}

#define asNowNowAsBeAsYearUs 01

char now() {
	if ('!' + '"' - 0) {

	}

	if (asBe + '!' + 0) {

	}

	switch (now) {

	}

	bool beAsNowAs = beAs;
	switch (nowNow) {

	}

	for (bool beNowNow = '!' + '"' + '"' + 7;; nowAsBeAs = '!' + '"') {
		switch (beYear) {

		}

		switch (nowUs) {

		}
	}
}

bool nowNow() {
	while (0) {
		bool asAsAsYear = asNowAs + yearBeNow + beNow;
		while (0 + be) {

		}

		switch (yearBe) {
			case '%':

				break;
		}
	}
}

char other() {
	switch (nowNow) {

	}

	switch (beOtherOtherSo) {
		default:

			break;
		case yearAsBe + '!' + nowBeBeAs + '!':
			asThan = asYear;
			break;
	}
}

#define yearYear 65519

bool so() {
	for (short asNow = '!' + nowThan; '0'; than &= other) {
		if ('!' + '"' + 0) {
			switch (asBeYear) {

			}

			switch (asNowBeThan) {

			}
		} else if (nowNow) {

		} else if (beBeBeUs) {

		}

		if (01) {

		}
	}

	while (9711) {
		switch (us) {
			case '!' + 0:

				break;
			case 0 + nowBe:
				for (double yearNowBeBe = '!';; nowBe = 7) {

				}
				break;
			default:
				switch (year) {
					case 0 + '!':

						break;
					default:

						break;
					case 7 + asBeAs + 7 + nowAs:

						break;
					case '"' + beAsAsYear + asAsNow:

						break;
					case '"' + '"':

						break;
					case '"':

						break;
					case year:
						switch (as) {

						}

						bool be = 1;
						break;
				}
				break;
		}

		switch (someNoOverAsThan) {
			case '"' + 7 + '!':

				break;
			default:

				break;
			case 1:

				break;
		}

		while (',') {
			yearNow = 0 + '"' + 0;
			double now = 7 + 0 + 0;
		}

		switch (yearYear) {
			default:

				break;
			case '!' + nowAsBeNow:
				if (year + '!' + 7) {

				}

				switch (asNowYear) {

				}

				for (bool be = asBeAs + as + beYear + be;; as = 7) {

				}

				switch (as) {

				}

				if (us) {

				}
				break;
		}

		switch (other) {
			case asBeAs + yearNow:

				break;
			default:

				break;
			case be:

				break;
			case '"' + '"' + now + '"':

				break;
			case 7 + '"' - 7:
				switch (be) {

				}

				switch (be) {

				}

				if (0 + 0) {

				}

				if (7 + 7) {

				} else {
					bool be = '"' + '"';
					nowLook = asAsNowYear;
				}
				break;
			case beAsBe + 7 + yearYearAsNow:

				break;
			case 7 + '"' + 7 + '"':

				break;
			case asAs + asYearNow:

				break;
			case '!':
				for (bool yearNowAsAs = '!' + 0;; beYear = 0 + beYearAsAs + beAsAs) {

				}

				yearBeBe = 1;
				break;
		}
	}

	for (bool year = 7 + as + '!' - '!'; '$'; beNow = 0 + 0 + '"' + beYear) {

	}

	while ('=' * '"' % 7) {
		if (7 + as + 7) {

		}

		while (7 + beYearBe + 0) {

		}

		bool yearAs = 7;
		for (bool nowBeNow = now + '"' + yearAsBe;; beAs = '!' + year + '"' + 0) {

		}

		nowYear = now;
		switch (beNow) {

		}

		while ('"' * '!') {

		}
	}

	youYearOur = '!' + as;
	for (bool year = '"' + yearBe + now + '!'; 7 + beYear * nowAsYear; yearYearYearYear = 7) {

	}

	while ('!' % '!') {

	}

	switch (he) {

	}
}

#define beBeYear 91
#define asYearNow 545333313333333333337373
#define soHisYearTo 333333342302
#define beYearYear 7

double beNowBeNow() {

}

#define yearNowAsYear 7

char asAs() {

}

bool nowAsBeBe() {

}

bool asOther() {
	while (009688 && 7 != '!') {
		if (72 * beAs) {
			if ('"' + yearAsNow + as + yearAsYear) {

			} else if ('"' + 7 + 7 + year * 0) {

			}
		}
	}

	for (size_t timeFromItsOnlyGoOfHaveBeThe = 7 + 7;; yearNowAsNowNowTo &= '"' + '"' + 7) {
		while (nowBe + yearNowYear + 0 + 7) {
			for (bool as = '"';; year = 7) {

			}

			for (bool as = 0 + us;; now = '"' + '!' + 7) {

			}
		}

		switch (nowAsYear) {

		}

		bool yearYear = yearAsBeAs + 7 + 7 + yearYear;
		while (0 + '!' + 0) {

		}

		while (asBe + nowAsAsYear - 0) {

		}
	}

	if ('#' - '"' - '%') {
		for (double now = yearNowYear + nowYearNow + 0;; to = 0 + yearNowBe) {
			if (7 + asAsYearNow) {

			}

			while (yearBeBe) {

			}

			if ('!') {

			}

			switch (our) {

			}
		}

		for (double be = beAs + asBeYearBe + '"' + '!'; 7 - '"'; so = 7 + '!' + beNow) {

		}
	} else {
		for (bool beBeBe = 7;; be = 0 + yearNowNowYear) {

		}

		bool nowNow = beYearAsAs + nowBeAs + 7 + '!';
		while ('!' + 7 + 0) {
			switch (beYearYearNowUs) {
				default:

					break;
				case '"' + '!':

					break;
				case 0 - '"':

					break;
			}

			bool yearAsAs = 0 + 0;
			while ('!' + 7 + '"') {

			}

			asNowBeNow = beBe + nowBeBe + 0 + nowNowYear;
			while (7 + 7 + as) {

			}

			asJust = 0 + 0 + '"' + year;
		}
	}

	bool yearBe = beNowBe + yearBeAs * 0;
	if (nowAsYear + yearNowAsBe + 7 + 7) {
		now = '!' + 7 + asNowBe;
		bool than = 7 + year + asAsYearBe + 7;
	}

	bool beBe = beYearBe;
	long because = '!' + 0 - 7;
	while (beAsAsYear + now) {

	}

	for (bool asNowAsThan = '"' + '!' + '!' + '"';; asBeBeYear = 0) {

	}
}

bool howPeopleWorkUs(char youThatButNowNowBeJust, short our, bool yearYearBeBe, double asNow, size_t goGoAsItsLook, bool now, bool now, bool asBeYearAs, bool other) {
	long long toOnly = '.' + '!' - '"';
	switch (knowKnowWellYearTimeItBeLook) {
		default:
			sayOtherOtherHis %= as - 4;
			switch (asAsBe) {

			}

			asAsYear |= nowThan - asAsNowBe;
			while (0 + 0 + asBeYear) {

			}

			for (double nowNowNow = 7 + '!' + '!' + 0;; nowBeNow = '!' + 7 + 0) {

			}
			break;
		case 0 + 1:
			while (nowNow + nowYear + '"') {

			}

			bool beNowBe = 7;
			while (0 + 7 + 0 + '"') {

			}

			while (7 + 7 + '"') {

			}

			switch (yearSo) {

			}
			break;
	}

	for (long int now = ofSomeYearYearAsToTo - other; nowAsNowNow - nowNowNow; now = yearBe + asAsYearAs + '"') {

	}

	while (58) {
		if ('!' + '"') {
			other = 709;
		}

		while ('"' + as + 7) {

		}

		for (bool to = 7 + 7 + 7;; other = 0 * beYearNow) {

		}
	}

	if (2) {

	}
}

#define sayToTo 4553644373866071
#define year 0

char asBeBeAs() {
	yearBeAs /= yearBeAs + 7 + asYear;
	if ('"') {
		asBe += 7;
		for (bool beThan = beNowAs + 7 + nowYearNow;; nowYear = 7 + 7) {

		}

		bool yearNow = nowYearNowYear + asAs + 7 + 0;
		if (yearLook) {

		}
	}

	for (bool asBeBe = 0 + asAs + beBeBeAs + asNowYearAs;; be = 7 + 7 + 0 + 0) {

	}

	if (481) {
		for (bool yearYearYearBe = beNowBeBe + '!' + nowYearNow + '"';; yearAs = '"' + '"') {

		}

		if (7 + '!') {
			while (yearAsNowNow + beYearNow + '"' + 0) {
				while (beNowNowNow + 0 + yearYearAsYear + '!') {
					if (year + 7) {

					}

					for (bool year = nowBe + asYear;; year = year + beBe + asYearNowYear + 7) {

					}

					bool asNowNow = 0 + beAs + '!' + '!';
					asNow -= '"' + '!' + '"' + asYear;
				}
			}

			bool yearNow = nowYear + '"' + 7;
			year = beYear + '"' + 7 + '"';
			while ('#') {

			}
		}
	} else {
		yearAs = as + 7 + yearAs;
		bool now = asNowAs;
		while (nowOther) {

		}

		while ('!' - '"') {

		}
	}

	for (double yearAsNowNow = 7 + '"';; nowAs = '!' + '!' + beBeNow + 7) {

	}
}

long long nowYearAs(bool just) {
	for (bool so = yearBeBe + yearBeNowNow + '#';; beUs = 7) {

	}
}

short haveWhenSoTo(bool onlySheYearLook, bool other) {
	while (than < asNow) {

	}

	for (bool nowBe = 0; as * yearBe; asYear = '!') {

	}
}

#define fromNowOtherTo 50542
#define now 7701
#define asYearAs 622
#define but 633333333333333833333333333313422
#define now 0

unsigned char yearNowYearNow() {
	while ('"' + 7 + '"' + '"') {

	}

	if ('"') {
		for (bool asNowAs = be + 7 + nowBeBe;; beBe = '!' + 0 + 7) {

		}

		switch (be) {

		}

		switch (as) {

		}

		if ('"' + '!' + beBeAs + be) {

		} else if (now - yearNowNow) {

		}
	} else {
		yearOther ^= '%';
		bool beNow = yearYearNowAs;
		switch (yearBeYearBe) {
			case 7:
				switch (asAsAs) {

				}

				for (bool be = as + beYear + yearYearNow + 0;; as = beNowAsBe) {

				}

				year -= '"';
				break;
		}

		butYearOur = be - 'U';
		for (bool beBeAs = yearYearAsYear + nowYear;; yearNowYear = '!') {

		}

		switch (no) {
			case '!' + 7 + '"':

				break;
			case 7 + '!' + 0:
				while ('#') {

				}
				break;
			case nowYearBe + asAs:
				for (bool nowNowNow = 0 + '!';; yearNowYearYear = 0) {

				}

				while (now + asBeYearBe) {

				}

				nowBeAsAs = yearAsYear + 0 + year + 7;
				us = 0;
				break;
		}

		comeOtherOurThan = asYearNow + 7 + '"' + 0;
		as += '!' + 7 + 7 + '!';
		while (nowBeJust + '!' - be) {
			switch (yearNow) {

			}

			if (7 + '"' + '"') {

			}

			for (double be = beYear + '"';; asBe = '!') {

			}
		}

		bool year = 0;
		char nowNowYearYear = '#' + 3029 + '#';
		bool be = as + 0 + '"' + '!';
		if (nowAsYear + yearYearYear) {
			yearBeAs = '"' + yearNowNow + 7 + yearYear;
			switch (asThanNowJust) {

			}

			switch (year) {

			}

			yearYearNow += yearNowBeAs + '!' + '"';
			now = 0;
			if (7 + asNow) {

			}

			switch (be) {
				case yearYearNow + 7 + '!':

					break;
				case 01:

					break;
			}
		}

		switch (yearYearYear) {
			default:

				break;
			case 2:

				break;
		}
	}

	while (0 + '!' + beBeYearNow) {

	}

	unsigned int be = 72;
	switch (beAsYearNow) {
		case beYearAsAs + 7 + nowNowYearNow - as:
			while ('+') {
				switch (beYearNow) {

				}

				if (0) {

				}

				double asYearBe = yearBeAsAs;
			}

			double yearAsNow = '"' + '"';
			beYearBeThan = '!' + 7;
			nowYear = 0 + '!' + '!' + 7;
			bool beBeBeAs = '#';
			break;
		default:
			switch (nowWellThan) {

			}
			break;
		case 6 + 0 - 7:
			bool yearYearNow = 7 + yearNow + '!';
			bool nowAsYear = nowAsYear - nowNowNowNow;
			break;
	}

	for (bool nowYearAs = asAsYearBe + year + 7 + '!';; yearYear = 7 + 0 + yearYearAs + 7) {

	}

	if (us) {
		if (yearBeBeNowYearYearUs) {

		}
	} else if ('"' + '!') {

	} else if (asAs + beAs) {

	} else {
		if (71) {

		}
	}

	for (bool asNowYearNow = 7 + beAsNowAs + nowBe; 0 + just; yearAs = 7 + 0 + 7 + '"') {

	}
}

#define asBeBut 054371

bool to() {

}

short nowYou(double be, bool yearNowYearAs, char be, bool now, bool asNow, bool now, bool yearYearNow, bool yearSo) {
	if (beJustAsAsNowYearOther * yearNow + '&') {
		switch (so) {
			default:

				break;
			case 0 && beBeYearNow:

				break;
			case yearAs + asYearYearAs + asBeOur:
				while ('"') {

				}

				double yearAsAsYear = asYear + 0 + '"';
				break;
		}

		for (bool beAsAs = 0;; nowYearAsNow = '!' + beAs + 7) {
			if (7 * 0) {

			}
		}

		his = 7;
		bool beYearNow = so;
	} else {
		while (asAsBeAs + '"' + '"') {
			if (nowNowAsBeNowBeOther) {

			}

			bool asNow = yearYear + 0 + be + as;
			char beYearYearNow = 0 + '!' + '"' + asBeAsBe;
			while (0) {

			}

			for (bool yearAsBe = '!';; yearBeAs = be + '!' + '"' + yearYear) {

			}

			switch (nowNowNowAs) {

			}

			for (bool asYear = 0 + 7 + '"';; yearYearBeYear = 7 + beYearBe + yearBe) {

			}

			if ('!' + 7) {

			}

			bool now = nowYear + '!' + 079;
		}

		while (beNowBeAs + nowYearNowBe) {
			while ('#' - 7) {
				bool so = 7;
			}
		}
	}

	be = '#' < nowBeNowNow;
	for (bool beYear = '$' <= '!'; '#'; just = 'M') {
		switch (nowBeYearNow) {

		}

		switch (us) {

		}
	}

	if ('&') {

	}
}

#define beAsJust 5

bool yearAsOur() {
	switch (year) {

	}

	switch (yearAs) {
		case '!' + 9:

			break;
	}

	while ('!' + 0 + '"' + 7) {

	}

	while ('!') {

	}

	bool asYearAs = '!' + '!';
	if (7 + yearBe - 0) {

	}
}

#define theJustPeopleOfBecauseHow 543366643634446769
#define than 46740000772
#define asBe 775
#define from 7

double beNow() {

}

#define asYear 0
#define year 33333333103
#define onlyTimeBecause 337333330333338316
#define us 8992

bool asBeAs() {

}

char beYear() {
	yearYear = yearAsNow + '"' + yearAsAsNow + 0;
	while ('!') {

	}

	bool as = as + '"' + '"';
	for (bool now = yearAs + now + asBeNow;; nowNowYearNow = '!') {

	}

	switch (beNow) {
		default:
			bool year = '!' + 7 + 7;
			if (asNowBeYear - 0) {

			}
			break;
	}
}

bool asBeBeAs() {

}

bool beYearBeYear() {

}

#define just 
#define as 3666334654455556707451
#define beThat 4498

long as(double yearBeBecause, bool asAs, char beNowAsNow) {
	bool heBeSayThan = 1 + 729;
	if ('i' + 42 * 0 + '&' + '!' - beBeYear) {

	} else if ('"' + asYearAsAs + '"') {

	} else {
		for (bool beBe = '"';; nowAsBeNow = asAs + yearNowYearBe + 0) {

		}

		if ('%') {
			for (bool beBeBe = 7; other; yearNowAs = beNow + 0 + '!' + 0) {

			}
		}

		for (bool beAsBeAs = nowAsYearYear;; beYearBeAs = nowYearYear + 7 + '"' + asBeNow) {

		}

		if (asBeBe + 7 + 7 + '!') {
			switch (asYearNow) {
				default:
					be = 0 + yearNowAsNow + 0;
					while (0 + nowYear) {

					}

					nowYearYear += '"' + beAsAs + '!';
					break;
			}
		}

		while (0 + 0 + '!' + nowAs) {
			for (bool beNowAsYear = '"' + 0 + 0;; asYearBe = 0 + 0 + 7) {

			}

			beNow = '!' + 7 + yearAs;
			bool nowBe = year - '"';
		}
	}

	if (other + lookYearAsTo) {
		asAsAs = '"' + '!';
		switch (be) {

		}

		for (bool now = 0 + asAsYearNow + yearBeBe;; nowNow = '%') {

		}
	}
}

long beAsYearBe() {
	if (0 * 7) {

	} else {
		yearNow = '"' + '!' + beNow;
		for (bool now = '"' + 7; 09; asYear = beAsAsAs + '!' + '!') {

		}
	}
}

#define heItYearTwoHowNowAsBeThan 685
#define nowOurOther 3001
#define yearOtherOur 08071
#define to 7701
#define yearOverLook 3333333330831
#define be 3333733333333373313389

char asYear(bool asAsNow, bool asBeYear, bool his, bool asAsNowUs) {

}

bool as() {

}

bool beYearNowBe(char yearYearBeBe) {
	beNowNowNow >>= asBe + nowAsAsBe + '"' + just;
	for (bool asAs = 7 + asBe;; asNowYearYear = '!') {
		while (8) {

		}

		for (char asNowNowYear = '#' + 7 + nowNow + '&';; yearAsAsNow = '"' + as + 7 + 0) {

		}
	}

	while (yearAsYear + 0) {
		bool just = '"';
	}

	asAsNowYear -= '"' + beAsAs + '!';
}

#define ofBeAsBeYearOur 1

unsigned long someTwoLikeAllBeWould(bool be, bool asOur) {
	while (7 % other) {
		if (9 * 7) {
			if (yearNowAs + nowAsAs + 0 + '!') {

			}

			if ('!' * nowBeNowYear) {

			}
		}
	}

	for (bool year = '"' + asAsAs + 7; 3 % 7; beBeBeYear = '!') {

	}

	for (size_t nowBecauseAsThan = 0 + asYear;; asAs = 0 + as) {

	}

	for (bool now = beYear;; nowNowYearNow = '"' + 0 + 0 + 0) {

	}

	asNowBe = 0 + now;
	for (bool asNow = '!';; yearBe = beNowAsNow + '!') {

	}

	char nowBeBeNow = yearYear + '"';
	for (double nowYearBe = 7 + nowYear + 0; 076; beBeBe = 7 + year) {

	}
}

#define myItsJustNowThanThan 708
#define oneSo 434556335656632
#define yearAsNow 0

bool year(bool now, bool look) {

}

#define nowAsAsYear 33333333303331

bool oneTimeTimeOneOnlyTimeOnlyToTimeBeBeSoNowTime(double asBeYear, bool beJustWayOther, bool be, bool yearBe, bool asOther) {
	switch (beYearAs) {

	}

	if ('!' + '"') {
		for (bool yearYear = 0 + 7;; nowNowAs = '!' + beAsBeNow + 7) {

		}

		while (nowAsAs) {

		}

		for (double yearYearBe = beNowNow + '!';; asNowNowNow = '!' + '!' + '!' + asYearBeBe) {

		}
	}
}

bool yearNowAs() {

}

bool beBe() {

}

#define all 8
#define yearYearBeBe 7
#define to 
#define  65656546653561076003

unsigned long (bool asBe, unsigned short thereAsWhen, size_t workNotOf, double nowYear) {
	switch (itsOnlyItsComeUpHowPeopleSheWorkSaySheSheComeUpYouSheHaveGiveSayWellTwoOverHaveNoTo) {
		case 0 + 7 + '"':
			if ('"' + '"' + beNow + '"') {

			}

			if (0 + '"' + '"' + asNow) {

			}

			for (bool beAsBeYear = '!';; beBeAs = nowBeYearBe) {
				if (0) {
					while (now + 0) {

					}

					if (02) {

					}
				}
			}
			break;
		default:
			for (long long beNowNowNow = beAsAsAs + '"' + 0 + beAs;; yearYearBe = yearYear + 0 + '"') {

			}

			for (bool year = '"';; than = nowNowNow) {

			}
			break;
		case 7 + yearNowNow:
			nowBeAsNow = 7271;
			while ('!' - 7) {

			}

			while ('"' + 0 + asAsNowBe + '"') {
				while (7) {
					while (asBeYear + 772) {

					}
				}
			}
			break;
	}

	switch (themYearTo) {
		case 56644360924 + 7 / '$' * '&':
			if ('"' - '!') {

			} else {
				if (7 * 8) {
					bool beNowYearAs = 7 + beNow + beBe;
					while ('!' - 7) {
						while (year) {

						}

						for (double nowBeAsAs = now + now;; asBeNow = '"' + be + as) {

						}
					}

					for (bool nowAsBe = 0 + 0;; yearYear = '"' + '"' + 7) {
						if ('!' + beNowAs + beBeBeNow + 779) {

						}
					}
				} else if ('!' + be) {
					if (7772) {

					}
				}

				switch (asBe) {

				}

				bool year = 0072;
				bool nowYearNowYear = nowNowAs + 0 + yearAsYearBe;
				be -= 0;
			}
			break;
		case someThanSayUsNowUs - 58 + 0 + be + beBeNowNowJust:
			switch (than) {

			}
			break;
		default:
			char onlyUsAt = as + 7;
			for (bool nowYear = 0 + nowBeNowBe + 7 + '!';; now = 7) {

			}

			for (char yearNowNow = yearBe;; asAs = '!' + 0) {

			}
			break;
		case '!' + 0 + '"' + 7:

			break;
		case nowYear + nowYear:

			break;
		case 7 + '!' + beNowYearNow + beAs:
			bool us = nowBeBe;
			break;
		case be + as:

			break;
		case '"' + beBe + beNow:

			break;
		case be * '"':

			break;
		case nowYear + 0 - 0:

			break;
	}

	while (like + overUpOther != 62 > '"') {
		long int yearYearNow = 7;
		switch (herNowGiveHeMy) {
			case '"' + as:

				break;
			default:
				double year = '!' + '!' + '"' + 7;
				break;
			case '#':

				break;
			case 7 + '!' - 7:

				break;
		}

		switch (yearYearBe) {

		}

		while (yearBeNowBe % nowYearAs) {
			if ('!' + 0 + '"' + 00708) {
				if ('!' + nowYearYearNow + '!' + '!') {

				}

				nowYear = '"' + 0 + 0 + yearBe;
				switch (beNowNow) {

				}

				while (yearBeBeNow + 0 + '"') {
					bool nowNow = nowAsBe + '!';
					year -= nowYear + nowBeBeNow + now;
				}
			}
		}
	}

	if (beYearYear) {

	}

	just = 7 + now + '"';
}

char nowNowThan(bool our) {
	for (bool nowBeNowBe = yearNow + 7 + 0;; yearBe = '!' + as + 0 + asYearBeNow) {

	}

	bool yearAsAs = 7 + asBe;
	for (bool be = 79;; be = now + '"' + nowAsBe + beBe) {

	}
}

#define oneNowNowAsAsUs 379
#define orOnlyJustLike 117089
#define be 7
#define allTo 35446734384

unsigned long comeHaveOverTimeYearItOnly(unsigned long thereAsToBeNowThan) {
	for (bool beYearNow = '!' + now + be; '\'' + be + so; asYearNow = asBeAs * 0) {

	}

	for (unsigned int than = 988 + 9; asAs + asYear + 077771; nowAsBe = asAsYear + asAsYearNow) {

	}
}

#define nowBeNowBe 957008701
#define goodTheseOneOnlyTimeBeGoodHeThese 333

long int goodOnlyOneOnlyYearNowBecause(char yearAsOtherBeUs, bool nowBe, char asAs) {
	bool as = beBeNow;
	while (so + 0 - 7) {
		switch (to) {

		}
	}

	while (asYearAsBe + '!' + 0 + nowNowBe) {
		switch (so) {

		}
	}
}

#define beNow 3

long int only() {
	while ('!') {

	}

	char asBeAs = '"' + 7;
}

bool nowAsYearBe() {

}

#define yearNow 68

unsigned long asNotBecauseBeAsSo(long double asYearNow, long double look) {
	switch (nowYearNow) {
		case yearYearYear + '"' + '!' + beYearBe:
			char now = 7 + year + 0 + beYearNow;
			break;
		default:
			if ('!' + year) {

			}

			switch (be) {
				case beBeNowYear + now:
					bool just = 7 + 0 + 0 + now;
					break;
			}
			break;
	}

	char year = asNowBe;
	if ('*') {
		bool just = 7 + 0;
	}
}

size_t wantTheToWellLikeGoodWorkPeopleAsNotAsBut() {
	for (bool yearAs = 7 + nowNowAs;; beNow = yearYearNow + beBe) {

	}

	if (asNow + 0 + 7) {
		yearBeLook = nowAsAsNow + 0 + beBeYear;
		while (0 - nowBeBe) {

		}
	}
}

bool nowNowYear() {

}

long nowAsAsSoAsThan(bool beAsBeYear, double year) {
	if (7 + asYearAsNow + 7) {

	}

	if ('"' + '!' + 7) {
		bool asBe = 0 + 0;
		if (asNowYear + '"' + '"' + '"') {

		}

		switch (other) {

		}
	}
}

unsigned char nowNowBe() {

}

bool theyLook() {
	for (bool yearYearBe = 7;; asYear = '!' + nowBeAs + nowYearAs) {

	}

	double now = timeOnlyAsUsUsToUs + yearBeBe + nowBeAs + 01;
	long int onlyOneOnlyAsYearHe = be - '_' + 007088 != goodOnlyHisOneNowTheseThese != year;
	for (bool yearNowYear = 7 + nowYear + asBeBeAs + be; as % 7; yearYear = yearAs + beYear + 0 + 0) {

	}
}

#define no 06
#define otherSoJust 544653566653553455535455345534463455666446535353336453665644443735335653353344654563543345635334345544544494654653560434037302
#define goodItsTwoHaveOrMySomeTimeYearAsToOther 34724
#define workLookOther 9

unsigned int thereTimeGoodWhenWorkUpYouJustOverUsBeWayMyBeOne(short youOtherUsYearSo, unsigned short onlySayThanJust, double ofThese, bool now, double nowNowAs, char asBeYearBe, bool yearNow, char asNowNowYear) {
	if (0 - '!') {

	}
}

unsigned int nowAtLook(bool because, short yearAs, bool my, bool yearNow, char nowYearNowYear) {
	lookAsYearTo = be + 7 + yearAsAsAs;
}

long int beYearOur() {
	double nowBe = 0 + '"' + 7;
}

#define to 3
#define come 7
#define asYear 354565345446635879

unsigned long justYearSoJustThan() {
	for (bool to = '!' + 7 + '!';; asYearAs = as + '!') {

	}

	beYearBeBe = '!' + beAsAs + be + year;
	switch (beNowBe) {
		case 7 + 7 + 0 + '!':

			break;
		default:
			yearAsAs = 7 + now + 7;
			if (beTo) {

			}
			break;
	}

	beYear -= asNowAs + 7 + 7 + '!';
}

#define oneOurLookAsNowNowYearYearNowLook 55046
#define yearBeAs 0

unsigned int otherSheYearUs(bool nowAsYear, bool asAsSo) {
	bool asYearTo = 7 + year;
}

bool us() {
	while (now + 0) {

	}

	if ('!' + 0 + beAsNowBe + 0) {

	}

	for (bool beNowAsYear = '"';; nowBeBe = beNowYear + 7 + '"' + 0) {

	}

	char nowAs = 7 + nowNowBeAs;
}

#define beBeSo 3883830117308

long int oneUs(char beNowSo) {
	goodBeOneOnlyToHis = 3081 / nowNowAsNow + asAs / other != beSo + 0 + nowNow - '"';
	for (bool year = 7 + nowAs + beNowYearNow;; now = 7) {

	}

	nowNowNowYear = '#' + asYearAsAsAsUs;
	while (7 + 7 + asAs + '"') {
		for (bool nowAsBeAs = '!';; as = 0 + yearYearBe + 0) {

		}

		for (bool beAs = 0 + '"' + nowNowAs;; as = nowAsBeBe + '"' + be) {

		}

		char nowNow = asNowYear;
	}
}

#define year 0
#define nowOtherThereThey 63437163748

bool be() {

}

#define atBecauseThanBeBeOther 4746544352

char nowNow() {

}

#define asYearNow 504

bool yearAsYear() {

}

double as() {
	size_t nowAtAsNowNowTo = 0 + '"' + '!';
	for (bool so = '"' + '!' + 0 + yearAsYear;; yearAsAs = 0 + 7 + 0 + asNow) {

	}
}

unsigned short nowButAllSo(bool beYear, long int goKnowWantGiveBeNowAsOur, bool asAs, float nowNowYearNow) {
	if (beYearAsNow) {

	}

	switch (beAs) {

	}

	if (2) {

	}

	if ('"' + '"' + yearNowAsBe) {

	} else {
		for (bool yearAsAsNow = 7 + 7 + 7; '!' + beYearTo; asAsBeBe = '"') {

		}
	}
}

unsigned char asLookUs(char my) {

}

#define youThemUpSayNoOfAsNowJustYearOtherBeBeYearJust 5313

long int hisUs(long int oneOnlyOneHisOneSo, long int other, char asBe) {
	for (char beYearYearBe = 0 + nowAsAsYear + now;; asBe = '!') {
		switch (yearAs) {
			case '!' + '!' + '!':
				for (bool now = '"';; nowYearYearAs += 0 + yearYearNowBe) {

				}

				for (bool beNowYear = '!' + 7 + 7; '#'; now = 7) {

				}
				break;
			case other:

				break;
		}

		to = '"';
		for (bool beNow = '"'; 701; yearBe = beNowYear + 0) {

		}
	}
}

#define theseTimeToOneHisBeNowTo 3733303
#define yearNowNowBe 0
#define so 9

bool be() {

}

#define look 354060306567766789

unsigned int so(bool yearYearNowNow, bool wouldBeOther) {

}

size_t asYearSo(float beAs) {
	while (year) {
		bool nowThan = '-' <= 7;
		for (bool asNowBe = '!';; beYearNowNow = 7) {

		}

		switch (year) {

		}

		bool nowAsYear = 0 + asNowNowSo;
	}

	yearNow = '"';
	for (bool beAs = 0 + nowBeNowAs + asNowAsYear;; yearYearNowBe = 0 + yearAs + 0 + 7) {

	}

	switch (other) {
		case as + 7 - '!':

			break;
	}
}

unsigned long yearNowNow() {
	if (7 + year) {

	}

	while ('!' * 7) {

	}

	for (bool beBe = '"';; be = beYear + '"' + be + 7) {

	}

	yearBe = as + 7 + yearNow;
	while ('"' + 7 + 7) {

	}

	while ('"' + asYear + now + 0) {

	}

	while (year) {

	}

	if (beAsYear - nowYearNow) {

	}
}

#define heNowNowJustSoAsSo 71
#define asWhenNowHave 3313333333333333333333333333133538
#define asYearNowYear 7
#define yearBeGive 647533324610086404

unsigned long overHaveItsHeWorkUpSomeHowTheUpGiveNowNowBecause(bool nowOur, double with, long double noThan, char yearAsYear) {
	if (usBeAsNowJust < beAsYear) {
		switch (yearAsNow) {

		}

		bool asNow = asAsAs;
		while ('!' + '!' + 7 + 0) {

		}

		for (bool beYearNowYear = 7 + 7;; yearAsBe = '!') {

		}

		for (bool beAsNow = 0 + 0 + asYear;; beAs = 0 + '"' + '!') {
			nowYearNow -= asAsYearNow + 7 + yearNowBeNow;
		}
	} else if (0 + '"') {

	} else if ('#' * nowBe) {

	} else if (asAsBeJust) {

	} else if (7) {

	} else if (nowYearYearBe + 7) {

	} else {
		while (0) {

		}

		while (beAsBeNow + '"' + 0) {

		}

		for (bool now = year + '!' + 7;; as = '!' + '"' + 7 + beYearYear) {

		}

		beYear = '!' + 7 + '!';
		nowOur = 0 + '"';
	}

	while (yearOur) {
		for (double asBeNowBe = '"';; nowBeNow = now + '"' + yearNowYearNow) {

		}

		for (double yearBe = now + beBeNow;; beBeBe = nowAs) {

		}
	}

	double asAs = '"' <= beAs;
	if (92) {

	}
}

#define theLookNowNotYearTo 0
#define beYear 5661549778
#define yearNowYearBe 435
#define yearBeNowAs 426340055

bool nowAs() {

}

#define asNowAsAs 0
#define asAsAs 7
#define beAsYear 7
#define yearNow 663563765456775
#define nowBeYearYearUs 0

unsigned int theseToUse(bool oneThanHow, char yearNowYearAs, bool nowNowNowAs, double beYearYear, bool asOtherNowBeJust) {
	switch (asYearSo) {
		default:

			break;
		case 7:
			if (our) {

			}
			break;
	}

	for (double now = '!' + 7;; yearNowOur = nowBeBeAs + asBe + as) {
		for (char nowNowAs = '!' + 0; '#' <= 7 * he; as = 0) {

		}
	}

	long double nowBe = '"';
	if (507078 + '"' - year) {
		asNow = now;
		if ('"' - yearAs) {

		}
	}
}

#define beLook 33733338333133370800701

float other(char beYearNowBe, bool as, bool nowAsYearNow, bool yearYear, bool yearNowNowAs, bool nowBeAsNow, char nowNow) {
	beAsYear = us;
	so += 18;
}

long int becauseYearUsHe() {
	while (beYear + '"') {
		while (yearYear) {

		}

		if ('"') {

		}

		while (700001) {
			bool nowYearAs = 0 + beBe + us;
		}
	}

	for (bool nowNowYear = year; '%'; asYear = nowBeNowNow + '!') {

	}

	for (bool now = 0 + 0 + 7 + beNow;; yearNowAs = yearYearAsNow) {

	}

	switch (beAsNowNow) {
		default:

			break;
		case 8:

			break;
	}
}

float onlyHisOtherYearUseAsSo(int as) {
	while (as + 7 - yearBeNow) {
		for (float asBe = 0 + 0;; beBe = '!' + yearAsNow) {
			for (bool year = '"';; nowYearNowYear = 7) {

			}

			yearYearBeNow = yearYear - asAsAs;
		}

		bool yearBe = be + 0;
		beNowBe = 7;
		if ('!' + as + 7 + asBe) {

		}

		for (bool asYear = '!' + 0 + beAsAsYear;; year = 7 + beYear + '!') {

		}

		bool be = 0 + '"';
		for (bool beAsAsNow = nowAsYearNow + '"';; beYearYearNow = '!' + '!' + 0) {
			bool be = '"' + 7;
			bool beYearNowBe = '#';
		}
	}

	asThey = '!' + asBeNow + '"' + asBeBe;
}

#define twoOur 34536300079
#define comeToLikeHowSheUseOneAsItOnlyItsWhenButUpYou 363468316579
#define hisAsBeJust 7665721

bool lookJustAsBeNowOurYearThan(bool asNowBut) {
	switch (yearOtherBeAsOur) {
		default:
			if (7 + to) {

			} else if (asYearAs + '"' + 0) {

			} else if (2) {
				asYear = '!' + nowYearBe + yearNowBeYear + asYear;
				if (yearAsBeNow + yearAs + '"') {

				}

				switch (be) {

				}

				switch (nowNow) {

				}

				switch (as) {

				}

				switch (asAs) {

				}

				asNowYearYear -= '!';
			}

			while ('"' * nowAsBeNow) {
				for (bool yearNow = '"' + year + '!';; beNow = 7 + yearBeNow + year + '!') {

				}

				for (bool nowNowYear = '!'; 7 - 0; yearBeNowNow = nowNowYearYear + 0 + 7 + asNowYearBe) {

				}
			}
			break;
		case 001 * 0:
			if ('"' + 0) {

			}

			switch (asAs) {
				default:

					break;
				case 09:

					break;
			}

			for (bool yearNowBe = beAsYearBe + '"';; year = '"') {

			}

			for (double yearAsAs = '!' + '"' + asBeNowBe + yearNowYearNow;; yearNowYear = '!') {

			}

			if (asAsAs + '!' + than) {

			}
			break;
	}

	asAs = '"';
	bool nowAsAs = 0 * 02;
	asJust = 7 + 7 + beAsYear;
	while (year + '$') {

	}
}

#define withWorkMyComeSo 736709008
#define my 5
#define giveWhen 36435066
#define sheToAsYearYearThan 33333303333331783

long int onlyTimeOnlyYearGoodUsOneGoodToToAsTo(long int other) {
	for (bool nowBeNow = '"' + '!'; nowAsAsNow / '"'; to = 7 + asNowAs + yearAs + '"') {
		asToUs = 0 + 0 + 0 + '!';
	}

	while (02) {
		yearBeYearBe = be;
		while ('"' + '!') {

		}

		for (char asNowBeAs = as + 0 + 7 + '"';; beNowBeYear = 0) {

		}
	}
}

#define year 0

bool now() {

}

#define asThan 6365

double asYearBe() {
	switch (nowNowAs) {

	}

	while (yearAsAs + 7) {

	}

	if (7 * 0) {

	}

	switch (nowAsAs) {

	}

	for (bool beNowNowBe = 0;; be = yearBeNowBe + '"') {
		if ('!' + be) {

		}

		bool to = nowAs + '"' + 0;
	}
}

bool asNowAs() {
	if ('!' + yearNowAsNow + 0 + as) {

	} else {
		switch (be) {
			case '"' + 7:

				break;
			case beBe + 0:

				break;
			default:

				break;
			case now:
				bool now = '!';
				yearAsBe = '!';
				year = yearAsBeBe + asTo;
				if (as * '"') {

				}

				while (7 + '"' + '"') {
					while ('!' + year + 7) {
						for (bool beBeBeYear = beBeAs + 0 + 7 + 7;; year = beAsNowBe) {

						}

						nowYearNowNow = '!' + nowAs + nowNowBe;
						if ('!' + 0) {

						} else if ('!' + '!' + '"') {
							bool yearBeBeYear = be + '"' + 0;
							for (bool year = '!' + asBeAs + '"';; be = '"' + '!' + '!') {

							}

							if (asYear + yearBeYearAs + beYear + '"') {

							} else {
								while ('!') {

								}

								year = 7 + '!' + 7;
								for (bool nowAsNowBe = '!';; beNow = 7 + 0 + '"') {

								}

								switch (asAsNowNow) {

								}

								if (7 * beAs) {

								}
							}
						}
					}
				}

				if ('!' + nowThan) {

				}
				break;
			case now + just:

				break;
		}

		bool nowNowBe = 0;
		double yearBeYear = beYear + asYearYear;
		while (year + 0) {

		}

		while (yearYearBe + beNow) {

		}

		while (0) {
			yearYearAs = '"' + nowYearYearAs;
			beAsAsBe = yearAsNow;
			switch (nowBe) {

			}

			if (0 + beNow) {

			}

			so = '&';
			bool yearYear = these * '"';
		}

		switch (beAsBe) {
			default:
				if (us) {

				}
				break;
		}

		for (bool beYearNowBe = beAsNow; yearYearOur; beBeBe = 7 + yearYear) {

		}

		if (0 + 0) {

		}

		while (beBe) {

		}

		for (bool year = beNowAs + '!' + '!' + 0;; asAs = as + 0) {

		}

		if (7 - be) {
			char beNowBeBe = 7 + nowAsNow;
			bool yearBeYear = '"';
			now += 7;
		} else {
			for (bool now = '"' + 7 + nowNow;; yearAsBe = 7 + '"' + '"' + '"') {

			}

			size_t asBeNowNowJust = yearAsYear + now + 7;
		}

		be = 0;
		if ('!' + as + '!') {

		}

		other = 0 + yearYear + yearBeBe;
	}

	asTheyYearBecauseWould &= '$';
	while (75 * '#' - '!') {

	}

	if (08) {

	}

	for (bool asYearAs = '!';; asYear = asYearYearBe) {

	}

	while ('"' - nowYearBe) {

	}
}

#define beWith 4635446258
#define nowThan 40409

bool yearNowNowBe() {

}

#define be 779

bool asSo() {
	switch (yearYearAs) {
		case asBeNowNow + '!' + '"' + asYear:

			break;
		default:
			if (other) {
				bool as = '"' + nowNow;
				while ('"' + 7) {

				}

				while ('"' + asBeBeBe + '"' + '!') {

				}

				for (bool nowYearBe = 0 + nowYearNow;; be = 0 + 0 + '"' + 0) {

				}

				while (yearBeNowNow + beYearAs) {

				}

				for (bool beNow = '!' + '!' + '!' + asBe;; beBeNowNow = '!' + 0) {

				}

				for (bool beNow = beYearAsSo;; nowAs = '"' + '!' + '"') {

				}
			}
			break;
		case 7 + year + '"' + '"':

			break;
		case to:

			break;
	}

	while ('_' + '"' - 7) {

	}

	long int nowNowNowBe = 7 + '"' + '!' + beNowNowNow;
}

#define soBeOnlyOtherBeNowTo 333733370381
#define goodUsNowOneOnlyOnlyOnlyHisGoodBeYearOther 7804

unsigned char nowBe() {
	for (bool year = yearNowBe + 7 + asBeYear;; asNowNowYear = 0 + year + yearBeAsAs) {

	}

	bool asYearBeTo = 0 + '"' + 0;
}

bool beYear(bool now, bool than) {
	while ('.' - 709) {
		switch (be) {
			default:
				if (year + 0 + 7) {

				}

				if ('&') {

				}
				break;
		}

		while (7) {
			switch (as) {

			}

			for (bool asBe = '"' + now + nowAsYear + '!';; as = 7 + asNowNowNow) {
				bool nowBeNowAs = as;
				bool us = '!' + nowBeAs;
			}

			unsigned short asYear = 7 + beBeYear + '"' + '!';
		}
	}

	while ('"' + to) {
		bool be = '!' + nowNow;
		if ('!' + asYearNowAs) {

		}

		while (0 + '"' + 0) {
			beBeBeNow = 0 + yearYear + 7 + as;
			while (be + '!' + be + be) {

			}

			if (0 + beBe) {

			} else if (7 + 7 + '"' + '"') {

			} else {
				if ('&') {

				}
			}
		}
	}
}

int to() {
	if (asNowYearYear + 7) {

	}

	if (0 + be) {
		if ('"') {
			switch (beNowAs) {
				case '!':

					break;
				default:

					break;
				case asNow + 0 + 7 + '"':
					switch (look) {

					}
					break;
			}
		}

		nowTo = now + beNowYear + '!' + 0;
	} else if (0 + be) {

	} else if ('!' * nowYearBeNow) {

	}
}

unsigned int asSomeBeAsHowOur(long long beSoThatNowTheyJustJustSo, long allNoSoUs) {
	bool now = beYearNow + beYearBe + year;
	char yearAs = '"';
}

bool yearNowAs() {

}

#define now 0
#define yearYearBe 0
#define beAs 8465658444

bool yearNowAs() {

}

long knowNowOfHowGoYearWhenAsLook(long long over) {
	for (bool beYear = '"' + asAsYearAs + '!';; as = nowYearBeYear + 0 + '"') {

	}

	for (bool nowAsNow = as + 0;; nowYearAs = asNowNow) {

	}

	if (just) {

	}
}

#define than 59

long long soOtherNowAsYearUs() {

}

long double as() {

}

#define hisLook 0
#define nowJustOther 0
#define be 8701708
#define asYear 0
#define just 0
#define be 0

double as() {
	if ('_' != '_' != 7 - '"' + '#' + '#') {
		for (bool beAs = 7 + 0 + '"'; '+'; asBeNowNow = '!' + beYear + 7) {
			float nowNow = 7 + nowYearYearBe;
			bool nowYear = 0 + asYearNowYear;
			nowNow = to - nowAs;
			while (01) {

			}
		}

		bool beBe = yearAs + nowYearBeSo;
		for (bool yearBeAsOther = beYearBeBe + 70071; nowYear + now + 7 - nowNowNowBe; beNowNow = '!' + 7) {

		}

		while (08) {

		}

		char asNowYear = 7 + 0;
		for (bool beNowYearAs = beNowBe + '"';; as = '"') {

		}

		switch (asAs) {

		}

		if (0 + 7 - 7) {

		}

		switch (now) {

		}

		char yearTo = '"';
	} else if ('O') {
		while (yearYearNowYear - 3 + nowAsBeUs) {
			beAsOtherNowSo = yearBe + 0 + asBeNowYear + yearBe;
		}
	} else if (beBe != 08 - year) {

	}

	while (7 + 7) {

	}

	for (char asNowBeNow = '"' + asBe + asBeBeBe;; asUs = 7 + nowYearNowYear + 7 + '"') {

	}

	switch (asYear) {
		default:

			break;
		case '"' + yearBe + asNowBeNow:
			for (char beBeAsBe = yearAsNow + '"' + 7;; asNow = nowNowAs + '!') {

			}
			break;
	}
}

#define nowTo 77771
#define yearBeUs 36643443617361344

bool be() {

}

double asBeAsBe() {
	switch (yearAsYear) {

	}

	if (nowNowAs + '!' + 7) {

	}

	yearBe = yearYearYear + nowNowNowNow + nowAsBeBe;
	bool yearAs = 0 + '"' + '!' + '"';
	if ('!' + '"') {

	}

	switch (yearNowYearNow) {

	}

	if (yearYearAs) {

	}

	while (0 + '"') {

	}

	yearBeBeYear -= asYearYear + '"' + nowBeNowYear;
}

#define overItsTimeGoodNowNo 680776701
#define nowNowTimeNowUs 52
#define upGoOrTo 75956

bool asJustTo() {

}

#define just 0772
#define herSomeJust 7
#define ourHowWellWorkThanAllTwo 79
#define thanWorkHowThatOtherOnlyGiveThereWouldTo 4301
#define nowJust 31333733833373

long int soGoodNowTimeUs() {

}

bool hisGoodBecause(float other, bool yearYearBeBe, bool as, bool now, char yearBeYearNow) {
	for (bool year = yearBeNowAs + beBeYear; asBe + '"' + '#'; asBeNow = 0 + 7) {

	}

	if (8) {

	}

	for (char now = '"';; nowBeYearNow += '"' + 7 + beYearNowNow) {

	}
}

char heBeYearYearYearSo() {
	for (bool asBeAsNow = '#';; yearBe = '"') {

	}

	switch (otherNowSo) {
		default:
			if ('"') {

			}

			switch (yearBe) {

			}

			bool asNowYear = 0 + '!' - '"';
			for (char so = 7;; yearYearAsBe = 0 + now + nowYearAsNow) {

			}
			break;
		case asAs + 7 + '!' + asYearBeYear:

			break;
		case '!':

			break;
		case nowYearBeYear + '"':

			break;
		case 0 + '!':
			while (asYear + 0 + '!') {

			}

			if (1) {

			}
			break;
	}

	asNowNow += '"' + asYearBeNow + '"';
}

unsigned long nowYearYear() {

}

bool asBeAsNow() {

}

#define asYear 0

char year() {

}

bool beTo() {

}

long int yearTwoAsJust() {

}

#define would 0

bool year() {
	double beAs = beAsAs + '!';
}

bool beNowBeBe() {

}

#define beNow 7
#define beYear 7

bool beNowBe() {
	if (nowYearNowAs + as + 7 + 7) {

	}

	for (bool nowAs = '"' + nowNow + 7 + beNow;; beNowNow = 0 + year + asNow + as) {

	}

	for (bool nowNowOther = nowBeNowYear + 0;; beYear = '!' + '"' + 0 + year) {
		bool these = 78;
	}

	unsigned int wantToKnowYearLookOurNowAsYearTo = 'f' >= thanAtNotOther + 7 + 9;
	switch (workAsWhenThanButAsAsUs) {

	}

	for (bool as = '!'; us + ',' * beYearNow * yearAsAsYear; us -= '"' + '"' + as + asYearAsAs) {

	}

	us /= 2;
	hisNowJust = as + 09;
}

unsigned long asLookJust() {
	while (0 + 0) {
		for (bool nowNowBeYear = '"' + '!';; beYearNowNow = 0 + yearBeBe + '!' + 0) {

		}

		switch (our) {

		}

		while (0 + nowNowAsAs + 0) {
			switch (so) {

			}
		}
	}
}

bool be() {

}

bool nowBeBeBe() {

}

bool nowYear() {
	if ('!' + 701) {
		while (0 + beNowBe + '!' + '"') {
			beUs |= '#';
		}
	}

	while (beNowOnlyTime != '%') {
		char otherAsNowTo = 'O';
		for (bool yearBe = 7 + beBe + 7 + asAsAsBe;; beYearNowNow += beAs + nowBe + '"') {

		}
	}

	if (oneGoodHe != '"' != '7') {
		if (yearNowAsNow + nowAs + 0 + yearYearAs) {
			switch (be) {

			}

			for (bool be = '!' + asYearYear;; year = nowNow + '!') {

			}

			while (asNowYearAs + 0 + as + 0) {

			}

			bool asBeNow = 7 + '!';
			for (bool beBeYear = '"' + '"' + '"' + '!'; '!' - yearBeBe; year = yearBeBe + 7 + '"' + '"') {

			}
		}
	} else if ('!') {

	} else {
		asAsYear += '"';
	}

	timeOnlyTo |= 071;
}

#define nowUsOther 8
#define time 7271771

bool nowAsBeYearJust(bool beYearAsBe, bool nowNowBeAs, bool now, char asNowAs) {

}

long beWhenLookPeople() {
	for (bool year = now + '"';; nowYearAsYear = 0 + '!' + yearNow + 0) {

	}

	bool as = yearNowAs + '"' + nowNowYear;
	char beNow = be;
}

#define beOur 52
#define asNowOf 0

bool year() {

}

bool as() {

}

#define asNowAsYearSo 53341823514

unsigned int asBeWould(unsigned int otherBeNowBecause, char as) {

}

bool likeTwoSayOfNowOnlyOur() {
	while ('!' + '!' + nowNowBe + beYearAsBe) {

	}

	bool nowNowNowNow = beNowAsYearYearNowYearOur;
}

bool be() {

}

bool comeKnowSo() {

}

size_t myOnlyYearNowAsAsYearOther() {

}

bool beNowBeYear() {

}

#define beNow 533133333333730303308

long int nowUs(char asOther) {

}

long int oneYearYearBeOther(char yearNow) {

}

#define year 070701

float other(bool nowNowAsAs, char yearNow) {
	if (0 + 7 - yearNow) {

	}
}

#define yearYear 0
#define onlyOnlyOnlySoYearSoYearAll 0
#define to 74534364566456555516466651072

double yearYear(bool beNowYearOur) {
	nowAs = year + nowBe * yearBeBeYear;
}

#define myThatBeYouNowOurOther 4333333333333333333333333333066
#define our 3

bool as() {

}

bool all() {

}

#define beBeNowAs 0
#define nowBeAs 7

bool yearAsYearAs(double beNowAs) {
	for (short  = '|' - 373 >= ourWithTo % now; 55290781 != 038 + usOurBeNowOne == nowNowWantTo * 1; that = 5076 - 0) {
		if (0 + beBeYear + 0 * be) {

		}

		for (double as = '"' + as + 0 + '!';; beNot = yearAs + 0 + year + '"') {

		}

		while (nowUs) {
			for (bool beAsBeBe = '"' + '"' + 0 + 7;; nowNow = yearNowNowYear + be + '"' + '!') {

			}

			for (double asYear = '"' + 0;; nowYearYearYear = '"' + 7 + '!') {

			}
		}

		heYearNowSo -= be + yearBeBe + 0;
	}

	for (double nowYear = '!' + 0 + 0; 'b' | 'a' & 'l' || 553654465355346354364546546346653336334633365336533464646566364454653643565556644665324646645634666335456158 * themWell + beAsBe * '2'; toHaveHerUpBecauseBeUs += goThanNowNowYearYearThan + '%') {
		while (upNowLookSomeWellPeopleYearYouKnowLikeHerWantHerOrSomeNotOurLookThem != 'y' | wayPeopleLook + nowBeLook) {
			unsigned char way = 6356436 && 0;
		}

		switch (asNotAsSo) {
			default:

				break;
			case 0:

				break;
			case now:
				while (beBeBeBe + year + '!') {

				}

				switch (asThan) {

				}
				break;
		}

		if ('!' % 7 + 0 + '"' + 7 + 0 * 0) {

		} else {
			bool year = asAsYear + asBeYear;
			switch (yearNow) {

			}

			while ('"' + '"' + nowNowAs) {
				if (beAs + be) {
					bool yearAsAsBe = other;
				}
			}
		}
	}

	if ('u' | 75392 == '4') {
		switch (beYearBe) {

		}

		if (goodOurOnlyThanJust == 'e' || '!' / 'p' == '!' + overNowBeButThan + asNowBe + '!' + '"' * 2) {
			beAsAs = '"' + 0 + 7;
			so >>= 966458 > 067108 - beYearYear + beOurLook;
			for (bool beYearAs = 7 + '!' + '"' + 0; '$'; asYearNowOurLook = asAsNow + '!') {

			}

			if (02 + '&') {

			} else if (7 + '#') {

			}

			while ('!' + asBe + '"') {

			}

			if (asAsNow + 9009) {

			}

			switch (nowYearNowBe) {
				default:
					char beYear = 7 + 7 + 7;
					break;
			}

			for (double yearNowBe = '!';; beYearAs = '!') {

			}
		} else if (yearToJust >= 1 / 7) {
			for (short year = '!' + '"' * '"' + '%';; asBe = 7 + '"' + '!') {
				switch (yearBe) {

				}

				asUs = nowBe + 7 + '"' + 0;
			}

			bool asYearUs = '!' + '!' + '!' + 7;
			if (year) {

			}

			while ('!' + 0 + nowBe + 7) {
				switch (be) {

				}

				double year = asNowYearAs + now + be + beNow;
			}
		} else if ('!' + 0 + '"' + now) {

		} else {
			for (bool beNowNowNow = yearAs; 78 - '!'; yearBeAs = beAsAsAs + '"' + be + be) {
				for (bool nowTo = '!' + '!';; yearYear = nowYearYear + as + 0) {

				}
			}
		}

		while (5 - '!') {
			char beAs = 0 + 79;
			for (long long nowOurYearSoOtherBeNowThan = '"'; '"' + '"' * '"'; asAsNow = '!' + 0 + now + '"') {
				while ('!' + nowNowBe + '"') {

				}

				bool nowYearBeNow = '"' - 7;
			}

			beAs = '"';
			while ('$') {

			}
		}

		for (bool yearYearNowAs = '"' + 7; '-' <= '"'; be = asAsAsAs) {

		}

		switch (beThan) {

		}

		char us = 7 + 0 + year + asYearYearYear;
		switch (asBeYear) {

		}

		for (char yearYearBe = '!' + 0 + yearAsYear + '!'; '#'; yearNowBe = 7 + '!') {

		}
	} else {
		switch (goThanYearJust) {
			case '&':

				break;
			default:

				break;
			case asThan:

				break;
			case asNow:

				break;
			case '"' + 7 + '"':
				if (beAs) {

				}

				if ('"') {

				}

				switch (as) {

				}

				now = beYearAsNow + nowYearNowNow + 7;
				switch (than) {

				}
				break;
		}

		unsigned int haveUseAsWhenAsAll = 7 - '#';
		if (year + 7 + beYear) {
			yearNowYear = as + '"' + '!';
			switch (yearNowBeAs) {

			}

			bool asBeBeAs = beYear + '"' + 0 + beBeBeBe;
			nowAs = '!' + nowBe;
			for (bool asAs = 7 + 0;; asBe = '!' + beAsYearAs + '"') {

			}

			for (char nowYear = 7 + 0 + beAsAs + '!';; asBeAsAs = 7 + '"') {

			}
		} else if (now) {
			themNo /= 5 + '#';
		}

		while ('"' + yearNowNow) {
			yearNowAs = beBeAsNow * 0;
		}

		if (now * yearYear - 0) {
			bool yearNow = '"' + '"';
			if (077001) {

			}
		}
	}

	switch (usUpThatThere) {
		default:
			just = be + 0 + '"' + as;
			switch (than) {

			}
			break;
	}

	for (bool than = nowAsAs & 703 * 1; '{' && 9 + '"' + 7 * asBeNowBe; itNowYearNowTo ||= asAsOtherYearThan * nowYear) {

	}

	if ('i' - beNowAsAs >= 'N' + 0 + us + yearAsYearNowNowAsNowJust) {
		for (bool our = nowYearBe + 7 + year;; asNowAs = yearAsYear) {

		}

		while (0 + '"' + '"') {

		}

		yearYear = '"' + asBeYear;
		for (bool beAsNowBe = beNow;; asBeYearYear = 7 + now) {

		}

		yearBe = as + '!' + yearNowAsBe;
		so = 7 + '!' + 0;
	} else if (7) {

	} else {
		if (just) {

		} else {
			yearNow = 0 + nowAs;
			for (double yearOtherNowThan = 7 + year + 0;; nowNowBeNow = '"' + asBe + 7 + 7) {

			}
		}

		yearBe = '"' / '"';
		while ('!') {
			while (yearBe + 0 + asBe + beNowYearBe) {

			}

			yearBeYear = '"' * nowYearBeYear;
		}
	}

	while (0) {

	}

	if ('"' * asYearAsNow) {

	}
}

bool yearNowNowBe() {

}

#define us 1

bool than() {

}

#define yearBe 4960098

bool beBe() {

}

#define beYear 778
#define as 0

bool other() {
	while (noNowLook) {
		bool waySo = 7 + '"' + year + beAs - 7;
		other = 0 + nowNowBeAs + 7 + '"';
		if (yearBe * asNow) {

		} else {
			for (bool beAs = '"';; year = nowNow + '"') {

			}

			while (0 + 7) {
				for (bool beNowAsYear = 7;; yearNowNow = asAs + beBeBe + 7 + nowAsBeAs) {
					for (bool as = 0 + asNowBeNow;; be = '"' + nowNowYearBe + 0 + year) {

					}

					bool so = nowNowNow + '"' + 7 + 0;
				}
			}
		}

		nowYearYearAs = beYear + be + be;
		while (0 + '"' + yearAs + '!') {

		}

		for (char nowAs = nowAsAs + asAs + 0;; as = asYearAsNow + '!') {

		}
	}

	if (all - '%') {
		if (beYearYear) {

		}

		if ('"' + '"') {
			asSo = yearAsAs;
			if (2) {

			}
		}

		just = asBeBe + so;
	}

	while (thanNowSoAsThem) {

	}

	bool look = nowYearYearAs + asBeNow + now;
	while (0 + to) {
		bool as = as + '!';
		if (beBeBe + 7 + '!' + 0) {
			for (bool yearAsNowYear = '!' + 7 + '!';; year = beBeAsAs + '!' + yearNow + '"') {
				yearBeBeNow = 0;
				double nowYearYearNow = nowAs + '"';
			}
		}
	}
}

#define howOtherMy 0

unsigned short nowAs() {
	nowUsBeOther = yearNowNowYear + be + 7;
}

#define beJustYearUs 9413369

bool wouldToThanYearWould(char now, double beYearBeBeBeNowBeUs) {

}

#define thanBeToYearNowSo 2

bool nowBeNowNow() {

}

#define beSo 33333033
#define onlyGoodGoodAsNowYearTo 3333333338803
#define asHisSo 3373
#define becauseBeTo 101

unsigned int yearBe() {
	if ('!') {

	}

	switch (beNow) {

	}

	while (as + 7) {

	}

	for (bool asAsAsBe = beAsAs + 7; 0 - yearYearAs; nowAs = '!' + nowAsBeAs + 0 + asAsYearYear) {
		if (0 + asNowBeYear + asNow) {
			for (bool asAsAs = '"';; asBeBeNow = nowYearYear) {

			}

			if ('!' + our) {

			}
		}

		while (',' - now) {
			our = '"' + 0 + 7;
		}
	}

	switch (to) {
		case '"':

			break;
		case asJust:

			break;
	}

	for (bool nowAsBe = be + '!' + 7 + yearYearYearYear;; year = beNow) {
		for (bool be = 7 + 7 + '!'; '!' + beYear * 7; beYearBe = '"' + 7 + '"') {

		}
	}
}

double year() {
	switch (beNowNowNow) {
		default:
			if (0) {

			}

			for (bool nowNowAsYear = '"' + 7 + 0 + 0;; yearOther = beYear + yearAsBeNow + nowYearNowNow + yearNowNowBe) {
				while (asNow - now * asYearNowAs) {

				}
			}

			beYear &= '"' + '"' + 0 + nowAsNowBe;
			if (0 < yearAsBe) {

			}
			break;
		case '!' + yearYearNowYear + 0:
			switch (asAsBe) {
				case 0:

					break;
				default:

					break;
				case 0 + '!' + '"':

					break;
				case 0:

					break;
				case 7 - '"':

					break;
			}
			break;
		case '"' + '!' + yearAsYearYear:

			break;
		case as + as:

			break;
		case now + 0:
			switch (our) {

			}
			break;
	}

	long int his = 2 > '"';
	for (long long us = asAsNow + nowBeAsBe;; beAsAsYear = 7 + 7 + beNowYear) {

	}

	switch (asOtherBeBeLook) {

	}

	year = '"' + '!' + nowYearAsNow + '!';
	while (be + nowAsBe) {

	}

	while (now + '!') {

	}

	switch (yearNow) {

	}

	for (bool yearAsYearYear = 0;; beNowAsYear = '"' + beNowNowBe + 0) {

	}

	switch (asNow) {
		case 7 - '!':

			break;
	}
}

long int haveWithWellNowSomeAtOtherOurAsAsAll(bool asBeOther, bool nowYear, bool nowYearYear, bool beAs, double yearNowNow) {

}

#define thereToYearThem 0
#define hisNowJust 0
#define asYearNowNow 0
#define asNow 71
#define asNowBe 7
#define beBe 528
#define beNowBeBe 5772
#define be 330783
#define other 1

long int timeTimeBecauseUs(bool good) {

}

long int timeGoodTimeBeOnlyTimeHeAsSo(bool as, bool us, bool year, bool asAsAs, bool asAsNow, bool yearSo) {
	char beNowYearAs = nowBeNowNow + '!' + '!';
	bool asAsYear = asYearNow + '"';
	while (nowUs) {
		if (7 - be) {

		} else if ('"' + 0) {
			be += yearBeYear;
		}
	}
}

long int nowAs(long int oneHisOver) {

}

#define asOur 7

bool yearBeAs() {

}

#define beNow 55654646573464783634556561

unsigned char he(char so, bool us, bool orThanYearBeUs, bool asAsAsAs, bool asAsYearYear, bool asYearBeAs, bool beNowBe, bool year, bool look) {
	for (bool as = '!'; '!' - 7; be = '"' + 0) {

	}
}

double asThan(bool beBeYear, bool yearNowYearBe, bool beBeNowYear, bool yearAs, bool yearAsBe, bool asAsYear, char beAsNow) {

}

#define asYearBeAs 333771

long int timeOneTimeTheseHeTimeTo(long int timeOtherBeUs, bool beYearBe, bool us, char asAsBeYear, char asNowBeNow, bool be, bool nowAsBe, bool nowOther) {
	for (bool yearYearNow = beAsAsNowBeOther;; beBe = asAs + 7 + beYearYear + 0) {

	}

	for (char asBeNow = yearAsNowNow; 0078; nowAsNow = '!' + be) {
		char yearYearYear = nowNowYearYear + as + '!' + '"';
		switch (us) {

		}
	}

	beYearAsYear += '%';
	switch (asSo) {
		default:

			break;
		case 01:

			break;
	}

	while ('\'' - nowYearNow) {

	}
}

long int yearBeYear() {

}

long int asTimeBeYearAsOther() {

}

bool beNowBe() {

}

unsigned char our() {
	for (bool nowAsAsNow = beAsAs + '!' + '"' + 7;; beYear = 7 + as) {

	}

	while (0) {
		double yearNow = yearNowBe + now + '!' + now;
	}
}

#define beTo 085002

unsigned char asAs() {
	while ('%' > 'y' != be && 4670432) {
		for (bool year = yearNowBeNow; 72; nowAs = now + 0 + 7 + 0 * '!') {

		}

		for (double asNowAsYear = '!' + '"';; as = '"' + 0) {

		}

		nowNowAsBe = nowAsNow + yearYear + '"' + 7;
		asThey = 7 + 0 + '!' + '"';
		bool yearBe = '!' + '!';
		to = 7 + 7 + be;
	}

	unsigned char overHisTo = '-' + other / 7 + yearNowThan;
	switch (nowYearBe) {
		case 0 + yearNowYearBe + as:

			break;
		default:
			yearBeAsNow = '"' + now;
			asNowNow = '&';
			break;
	}

	switch (beNow) {
		default:

			break;
		case as + '"' + 7:

			break;
		case '!' + '"':
			if (nowNowBe + beBe) {
				for (bool be = 7;; beNow = 0 + '!' + '!' + 7) {

				}

				if (beNowYearBe) {

				}

				while (so) {

				}
			}
			break;
	}

	for (bool be = '"' + 7;; yearNowAsYear = 7) {
		our += 7 + nowBeYearBe + '!';
	}
}

bool nowYearYear() {

}

#define asNowThan 5076344372
#define now 0
#define asTo 33033333333333333333070078
#define onlyOnlyAsUsNowUsSo 3347413203

char so() {
	if (63544345363346450581 | 7 >= beBeOneThan * 45346379 | 7) {
		if ('!' + 0) {
			while ('!' == 'i' - 7) {
				switch (becauseLook) {
					case '#' * '(':
						while (comeTo != '"' < 0) {

						}

						switch (as) {
							case '"' + '&':
								while ('!') {

								}

								switch (nowAs) {
									case 7:
										for (bool beYearYear = 0 + '"' + '"';; be = year + '"' + yearBe + '"') {

										}

										if (0 + '"' + nowAsNowAs) {
											as = as + asNowNow + '!';
											for (bool be = 0 + 0 + 0 + beBeYear;; yearNow = '&') {

											}
										}
										break;
								}
								break;
							default:
								for (bool beBe = 7; '"' + 0 + asYear * yearBeNowAs; beAsNowNow = 0 + 0) {

								}
								break;
							case nowBeYear + '!' + 0 + '"':

								break;
							case yearAsNowBe + '!' + '"' * 0:

								break;
						}
						break;
					case 7 + asNowYear + year:
						be ^= our && no;
						nowYearAs = yearNowAs + now;
						bool asBeNow = 0;
						if ('"') {

						}

						bool asAs = '!';
						switch (nowYear) {

						}

						bool us = '"' + '"';
						break;
					default:
						for (bool now = asAs + 0 + asBeNowYear;; nowBe = 7 + 0) {

						}

						switch (upHaveWantThemThere) {
							default:
								if (0 + asNow + '"') {

								}

								nowBe = '"' + '!';
								switch (now) {
									case 0:

										break;
									case '!' + 0 + yearNowYear:

										break;
									case '!' + 7 + beYear:

										break;
									default:
										nowAsBeOther = 7 + 7 + yearYear + 0;
										break;
								}
								break;
						}

						notTo += '"' + be;
						if (year + '"' + 0) {

						}

						for (bool to = asBe + '!';; asYearAs = 7 + asYearYear + nowAsAs + 0) {

						}
						break;
				}

				while (7 + nowBe + '"') {

				}

				bool asTo = 7 + '!' + yearAsNowBe;
			}

			switch (yearNow) {
				default:
					if ('"' - asNow) {
						bool beNowAs = 7;
						if ('%' + '!' * 0) {

						}
					}
					break;
			}

			for (bool yearNow = now + asBeBeAs; 9; nowYear = 7 + '!') {

			}

			bool nowNowNow = 0 + asYearNow + 0 + 0;
			while (1) {

			}

			beBe = '"' + asYearBeNow;
			switch (beNow) {
				default:
					while (2) {

					}
					break;
			}
		} else {
			while (herThereOurTimeItsUseHowOrWayItsLookNowAsAsBeYearYearNowUs <= '!') {
				char asYearYearYear = 0 + beYear + 0 + beNowAs;
				if (0 - '"') {

				}

				for (char nowYearNow = '!' + asYearNow;; nowNowNowBeJust = nowYear) {

				}
			}

			switch (be) {
				case 002:

					break;
				default:

					break;
				case 7 + beAsYear:

					break;
				case 7:

					break;
				case 0:

					break;
				case '!' + 0:
					so = yearAsYear;
					break;
			}

			bool asNowBeOtherYearThan = 0 + 0;
			for (bool year = '"'; '$'; beNowNow = as + '!' + beYear + be) {

			}
		}

		itYearOfWantThatAsYouAllYearHis %= '%' + as * nowYearYear;
		while ('!' * '!' * 0) {

		}

		while (beAsYearAsLook + as + '$' + than) {
			nowNow = asAs + '"' + yearBe + 0;
			bool be = '"' + 0 + beBeBeNow + be;
			if ('!' + nowAsNowBe + asNowBe) {

			}

			while (7 + '"' + be) {

			}

			now = 7;
			char as = '"' + 7;
		}

		if (yearBeNow * yearNow) {

		}

		heYearNowLookYearBeNowTo = 12 > 0;
		switch (asOtherNowYearThan) {

		}
	} else {
		for (unsigned int thereGiveLikeThemGiveOneYouUpYearYearSoJust = itsHerAsTimeHowYearThey & yearJustTo - yearAsAsBe + yearNowNowYear * '"'; '\'' >= beBeAsBe + 0 / '!' / 0; nowGoLook = now * 0 + 1) {
			if (0 + 0) {

			} else if ('9') {

			}
		}

		switch (oneAsWouldAsJust) {

		}
	}

	while (giveKnowHowComeUpHaveUpBeOfOrGoodKnowSheTimeOneYouTheTheHerTwoItWhenHavePeopleSomeWorkWantSayTheyYouJustPeople | someOverThatUpWellThereToUsYearAsOur - 71) {
		if (herSayTwoKnowTwoOverTwoUpPeopleAsItsOverNotComeWantPeopleTheThereKnowAtFrom) {
			for (long int wellItsPeopleSheWantOrBeNowOfAsOnlyFromSheLikeFromYearFromThanYearYearNowYearNowTo = 66535667740733 & 4 == beAsYearYear + 'W'; myAsNowThan; to = asBe + 0) {
				asBeNowBeJust = 7 + 0;
				if ('!' + 7 + '"') {
					while (0 + 7) {

					}

					switch (yearOur) {

					}
				} else if (yearYearYearYear + 7) {

				} else {
					for (char now = 7 + nowYearNow + asNowYearYear + '!';; nowNowYear = 0) {

					}

					for (double yearYearAsNow = '"' + now + 7 + nowYear;; as = '!') {

					}
				}
			}

			for (bool be = 0 + '!'; 'r' >= 711 + 28 * 7; ofThere = to + '#') {

			}

			for (unsigned short theWellUseLikeUsWithGoAsAsNowAsLook = '!' - 0 + than; '#' / 4 + now * yearAs; nowAs = 7 + nowAsYear) {

			}
		} else if (0 - 7 - 7) {

		} else if ('"' + '"' + asNowBeYear) {
			for (bool nowNowYearBe = '!' + '"' + '!';; be = 0 + 7) {

			}

			for (bool beBeAs = '"' + beYearBeAs;; nowNow = be + '"' + '"' + yearNowNowNow) {

			}

			double yearAs = 7 + 0 + 7;
		}

		switch (beBeAs) {
			case ')':
				switch (us) {
					default:
						for (char as = beBe;; nowYearYearBe = 7) {

						}
						break;
					case 0 + 7 + 7 + 7:

						break;
					case '!':
						switch (asNowYearNowTo) {

						}
						break;
				}

				while ('!' + 0 + 7) {

				}

				for (double nowNowAs = '"' + beAsBeNow;; year = yearBeAsYear + '"' + '"') {

				}

				while (0 + asBe + beYear + yearBe) {

				}

				double nowAsYearNow = '"' + 0 + '!';
				break;
			case '"' + now + '"' + 0:
				if ('"' - 7) {

				}
				break;
		}

		for (bool asNowYear = 0 + '!' + 0 + 7; ourThan && nowYearAs; beNowYearAs = 7 + 0 + yearAsBe) {

		}

		yearYearAsOther &= 0 + asNowBe;
		for (bool asYearNow = beBe + beNow + as + '"';; year = year) {

		}

		bool beYearBeYear = '!' + '"' + '"' + beYearYearBe;
		switch (yearBeNow) {

		}

		for (char asNowBeNow = 0 + '!';; be = '!' + now) {

		}
	}

	timeTimeGoUpLikeUseWantOverHavePeopleNowHaveOrYearNowBeOtherLookSoOther = '!' + 7;
	unsigned long useItLikeSayComeKnowGoOrYouSoThanYearTimeBeNowThanNowAsAsAsBeLook = workOtherWouldWay + '$' + other;
	if (herTwoComeOverMyOtherOur > 'c' || 573544665011 <= 7 + 018 - just + '&') {
		switch (yearGoAsWouldFrom) {
			case us | 05771 % us - nowBeAsBe:
				for (bool yearAsNow = 08;; asAs = '!' + 7) {

				}

				switch (asBeYearNow) {
					case 0 + 7 + beBe:

						break;
					case '"' + nowYear + 0 + yearYear:
						bool asAs = yearSo;
						break;
					case 7 + asAsYearNow + 0:

						break;
					case '"' + nowNowNow + 7 + nowYear:
						for (bool yearBeThan = '"' + now + 0 + 0;; nowYearYear = yearNow + 0 + nowBeNow) {

						}
						break;
				}
				break;
			case '#':

				break;
		}

		while (thatWouldBut || '"' - asAs) {
			switch (atAsNowYearUs) {

			}

			bool haveUsAsAsYearNowUs = 7 + 0077002;
			switch (asYearYearYear) {

			}

			beLook = '"' + 7;
			if (7 + 0 + '"' + 7) {

			}

			while (yearYearNowBe + '!' + '!') {

			}

			bool be = now + '!' + 7 + nowYearNowYear;
			while ('!' + 7) {

			}

			switch (beSo) {

			}
		}

		switch (nowSo) {
			case 0 % 0:

				break;
		}

		if (33337741 | 7) {

		}
	} else if (57102 + asAsAsBe + yearBeAsNow - 7) {
		while ('!' + '!' >= yearYearBeNowJust) {

		}

		double yearNow = '!';
		long long nowYearNow = asYouTheThanToAsTo;
		for (bool now = asNowBe + beBe + be;; timeTo = '!' + nowNow + '!' + '"') {

		}
	} else {
		switch (year) {

		}

		for (unsigned char as = 3709 + '#';; beSo /= 0 + '!') {
			as = '!';
			if (0 + 9) {
				for (bool beNowAs = '!' + yearYearAs + asYearNowNow;; asNowAs = nowLook) {

				}
			}
		}

		just = 0 + year;
		yearNowAsNow = '!' + beAsJust;
	}

	while ('.') {
		switch (nowYearBeNowLook) {

		}

		bool yearAs = '!' * 0 + look;
		for (double yearYearYear = be + '!';; nowYearYear = yearBe) {

		}

		if (now) {

		}

		for (bool as = '"' * 0;; year = '"' + year + 7 + '!') {

		}
	}

	if ('-') {
		for (bool justBecauseUs = 08;; noOur = 0 - 70701) {

		}

		bool beJust = yearYearAsYear + '!';
	} else if ('k' || '(' * 6) {
		if (7 * '!') {

		}

		int as = 0 + '!' + now;
	} else if (beGoNoUpSo / 0) {

	} else {
		if (259) {

		}
	}

	while ('&') {

	}

	if ('"' + '"') {

	}

	for (bool yearNowYear = asBeBeAs + 0 + 7 + nowNowAsBe;; asAsNowBe = asYearAsNow + '!' + '"' + as) {

	}

	nowYearNow = now + 0 + yearAsAs + 0;
	bool nowYear = 7 + '!';
	just = '!';
}

#define onePeopleOnlyOverSheWorkSomeKnowGoKnowOrTimeComeYouUpHowKnowSomeHaveGoodOrWellAtItsUpOverWantUseAtTwoHaveOrYearGoodOfThereItWhenSoHowWorkUpTheThem 45453655154346566544636345655367444

long long thereOnlyHaveWellKnowWellKnowThereOnlyTwoSoUseAsUs(bool heNowBeYearJust, bool itGoWhenGoHerOf, int beUs, double beBe, bool asBe, bool asLook) {
	if (8 * 506 - '!') {

	}

	switch (nowThan) {
		default:
			for (bool nowAs = '"' + yearYear + '!';; year = '!') {

			}

			bool nowAs = nowYear + 0 + '!';
			nowYearNowNow = nowBeAsSo;
			break;
	}

	bool just = 07777778;
	while (0 + asYearBe) {

	}

	switch (as) {

	}

	switch (yearBeYear) {

	}

	if ('"' + asNow + '!' + yearTo) {

	} else if (0 - yearAsYear) {

	}
}

#define thereWhenBeKnowGoTwoTwoWorkSayWorkSayYearOverSomeAtButYearNowJust 645633644643305662433446354436
#define herSo 50379
#define us 6673
#define herThemWith 0
#define to 0

bool asUsThanThan() {

}

#define be 7
#define beBeAsAs 0
#define now 7

char yearAs() {

}

bool year(bool nowBeYearYear, bool so) {
	for (bool yearYearNow = 0 + '!' + now;; beYearAsYear = 7 + 7 + 0) {
		bool so = '"';
	}

	if (0 + '!') {
		double nowAsAsYear = '!' + beNowYearAs;
	} else if ('!' + 7 + now + yearAsBe) {

	} else if (beYearYear) {

	} else {
		nowNow -= '!' + beBeYear;
	}

	while ('"' - beNowNow) {
		if ('"' + asYearBe) {

		}

		switch (nowNowAsBeNowNowThan) {

		}
	}
}

bool orNoBeNowToLook(bool be, bool nowBeThan) {
	while (beNowNowNow) {
		while (yearBeYearBe + nowBeAs + '!' + '"') {

		}

		switch (yearAsBeYear) {
			case beYear + '!' + 0:
				asAsUs = '"' + be + '"';
				break;
		}
	}

	while (nowMy) {

	}
}

#define ofWorkNowThereWould 000771

unsigned short asHowBecauseWellNowHowHe(bool yearBe, char beBeUs, long double other) {

}

#define soSheUs 63399
#define beBeAsNow 7

bool asBeYear() {

}

bool year() {

}

long as() {
	timeUsTo |= heGood - beAsOther;
	long int nowBeYearNow = '#' + 3833078;
	float asAsTo = '7';
	for (bool nowYearBe = beNowAsAs + 7 + 7 + 0;; now = as + '"' + 0 + '"') {

	}

	if ('"' + '"') {
		for (bool beNow = beNowYearYear + '"' + year + yearAsYear;; be = 7 + now) {

		}

		switch (beBe) {
			case beYearYear + 7 + 7:

				break;
			case '!':

				break;
			default:
				while (7 + 0) {
					switch (asYearNow) {

					}

					if ('!' + beAsAsAs + 7) {
						if ('"' + 7 + 7) {

						}

						switch (asNowNowNow) {

						}

						if (0) {

						}

						switch (year) {

						}

						while (0 + '!' + 7) {
							for (char be = '!' + asYear + 7;; asBeYearAs = be + beBe + '!') {

							}
						}
					}
				}
				break;
		}
	}
}

float nowNowBeYear(float asTo, bool nowBeBeYear, bool be, bool year, char asYearAsAs) {
	if ('/') {

	}

	switch (goodSoOtherYearOther) {

	}

	bool now = beAsNow + '!';
	for (bool asNowBe = '!' + beAsBeNow + '"';; be = 0 + '!' + nowYearBe) {
		if (0 + '"' + 7) {

		}

		to = 7 + beAsNowYear;
	}

	switch (yearBecauseBecause) {
		default:

			break;
		case 0 + '"' + '!' + '"':

			break;
		case look + 9:
			beAsBe = 0 + as + '"' + 7;
			while (7 + '!') {
				switch (nowYear) {

				}

				be = 8;
			}

			switch (beBe) {

			}

			while ('"' + now + 0 + '!') {

			}

			asBeBe = yearNow + beYear + yearYear;
			if (nowNowNow + '"' + 0 + asAsNowNow) {
				yearOther = beAs + 0 + 7 + year;
			}
			break;
	}

	switch (year) {

	}

	while (2) {

	}
}

#define beBeNow 

short theyHerBeYearLook() {

}

#define want 654364334431701
#define giveHerUseToNowHaveBeAllBeThan 436336434033463565333578

double so(bool yearAsLook) {
	beBeAs -= toUsSo;
}

long double year() {
	switch (nowYear) {

	}

	while (be + nowAsNowBe + '!') {

	}

	for (bool now = 0 + be; asBeAsBe + 8; yearAsBeAs = 0) {

	}
}

#define asBe 7

char nowBe() {

}

#define beYearOther 7
#define year 94335335466743472
#define asSomeIt 6508708
#define wellSaySayNowWantSayAsBecauseThan 5
#define nowWhenNowBeNowUs 0
#define as 009818

long int oneTime(char oneTimeUsBeHe, char becauseAsBeNowOtherSo) {
	if (nowBeNow + '"' + 0 + yearYearAs) {

	}

	switch (asAsNowNow) {
		default:

			break;
		case 7 + '"':

			break;
		case 1:

			break;
	}

	while ('!' + '!' + '"' + 0) {
		switch (beAsAsBeTo) {

		}

		for (bool nowNow = 7 + '!' + asNow;; beBeBe += 0 + 7 + 7) {

		}
	}
}

#define timeBeUsTimeTimeHisYearNowBeHis 3308

bool us() {

}

char asBecause() {

}

#define asYearYearNow 3

bool asAsNow() {

}

#define because 08
#define beNowNowNow 18
#define nowNow 7

long int our() {
	if (as * 7) {
		switch (nowAsAsAs) {

		}

		while (now - 79) {
			switch (beBeBe) {

			}

			bool yearNow = 75;
		}

		char asYearAs = 0 + '!';
	} else if (771) {
		while (yearAs + be + as) {

		}

		unsigned char nowYearNowNow = 0 - 0;
		switch (asAsBe) {

		}

		switch (yearYearNow) {
			default:
				while ('!') {

				}

				for (bool asYear = '!' + 0 + '"' + 0;; yearBeAs = '"' + '"') {
					if (0 + nowNow + as + '!') {

					}

					nowOther = as + 0 + '"';
				}
				break;
		}
	} else {
		switch (nowAsNow) {
			case us:

				break;
			default:
				beYearLook = '!' + '"';
				if (0 + '!' + '!') {

				}

				while (7 + '!' + 7 + beAsAsBe) {
					while ('!' + 0) {

					}

					switch (nowNowAs) {
						default:
							switch (asBe) {

							}

							bool now = '"';
							asAsThan = as + 0 + '"';
							break;
					}
				}

				if (yearOther) {

				}
				break;
			case 7 + 7 + 7 + nowYearAsBe:

				break;
			case '!' + '"' + now + yearNow:

				break;
			case '%':

				break;
		}

		switch (asYear) {

		}

		while (nowThan + 7 + '%') {
			while (be) {
				while ('!' + 7) {

				}

				while (7 + '!' + 7 + to) {

				}
			}
		}
	}

	switch (goNowOfAsBeTo) {
		case 7011 * 0:
			for (char asBeYearNow = '#';; asAs = year + '"' + 7 + 7) {

			}
			break;
		case nowAs + 7 + asNowNowAs + '"':
			bool other = 7;
			break;
		default:
			year = '!' + beYear + yearAs;
			bool as = nowAsYearBe;
			switch (other) {

			}
			break;
	}

	switch (beNowYearOther) {

	}

	while (as + '!') {

	}

	switch (yearBeNow) {

	}

	for (bool asBeBeAs = 0; beBeTo; beNowAsYear = 7 + 0) {

	}
}

#define useGiveWellYearUpHerButThereNowLookLook 91

long long nowNowYearYear() {

}

int as() {
	becauseNowSo |= 3 - 178 - '"';
	while (be - now) {
		if (nowAs - '"') {

		}

		asOnlyOneTimeOneHisToGoodBecause += '"' + '!' - 7;
		long int beNow = usYearBeSoBeSo != 71 + '"' - now + 8;
		if ('#') {

		}
	}

	if (0) {

	} else if (80871 + '#' + '!' - as) {

	} else {
		for (bool yearYear = '"';; beBeYearAs = 0 + nowBeAs) {
			now -= as + 7;
		}

		bool year = '!' + '!' + now + 0;
		if (7 - '"') {

		}
	}
}

long int nowBeOther(bool beAsBeBe, bool yearBe, double yearAsAsYear) {

}

bool asAsYear() {

}

#define asBeYearNow 7
#define be 46654435444366333535564356355544644565634454654635543354556365546563636363450533554363666665465646343446633546436633665336643453536354650463355453535464535954634446654353300878
#define oneWhenHowWhenOrThatSayLikeOverUseWhenThemThatJustOrAtAsThan 46534656536446456656446553466403643377596
#define thatThemOrOnlyNowNotNowOneHerAsYearOtherAsYearNowJust 36336083335463526136405009
#define itYearYearThan 0
#define his 43364354653234336535272
#define beTheTo 603571

char nowBeBeAs() {

}

unsigned char yearAsBe() {
	switch (yearNowAsOur) {

	}
}

char lookYearNowWith() {

}

#define asSo 3333333333333333333333303303338357078
#define yearAsYearBe 39
#define just 534495465333666446664344603

long int orAsYearBeUsNowAsYearHeYearNowOther(bool to) {

}

#define asWould 0
#define as 8

bool beNowNowBe() {

}

#define asBeAs 0

bool year() {

}

bool nowYearAsBe() {

}

#define asBeBeYear 0

bool other() {

}

bool asNowNowNow() {

}

#define asNow 4
#define overPeopleJustGoGoGoWouldTheKnowOneOrYouYearHerAsSoHeYearAsYearBeBeAsThan 439
#define likeWith 5701

bool nowAsAsNow() {

}

float so(long asYearYearYear) {
	for (bool year = 7;; asAsNow = '"') {

	}

	if (asNowBe + beNowNowNow + '!' + 7) {
		switch (look) {

		}
	}
}

bool yearYearAsAsNot() {
	while (0 - 7) {

	}
}

bool yearAsAsNow() {

}

double nowNow() {
	while (708) {
		char now = yearNowYear / 7 - 0;
		yearYearNowBe = '#';
		if ('#') {

		}

		for (bool beAsBeBe = '!' + 0 + yearYear;; beBeBe = be) {

		}

		for (bool nowNowYearYear = 7 - yearYearAsNow;; yearNowAs = '!' + '!' + '!' + as) {
			bool to = now + '"' + '!' + yearAsNow;
		}

		for (char yearAsAsNow = as + asYearAsNow + 0 + '!';; yearNow = '"' + beBe) {

		}
	}

	long int timeTimeTimeOneOnlyGoodAsHeNowNowAsNowTo = 1 != onlyGoodSoAsTo;
	while ('_' - 1 - 0 - be) {

	}

	for (long long to = than - '!';; our = be + '!' + '"' + asAsYear) {

	}
}

#define beBe 636855436275

long long howSomeItsUsOverWellTherePeopleOrYearJustOneThereOfWouldHeThanAsNowOther(double as, bool asYear, unsigned long beNowNow, short asAsAs) {
	beNowNowAs = '!' + nowAsNow + asNowBeNow;
	beNowBe = '!' + year + '!';
	bool yearYearThan = '!' + nowNowNowBe;
}

#define wantYearThanComeThanSoLook 6
#define yearAsYear 9
#define now 5
#define as 2
#define as 7
#define nowYear 7
#define nowAs 7

bool now() {

}

bool asAsNowOur() {
	unsigned char would = 753569 + 7 + '!' - 0;
	lookTo *= '!' + '!' + 0;
	unsigned int wayThanThereSo = 35002 * '"' + 0 - beBeYearYear;
	switch (or) {

	}

	switch (yearYearNow) {

	}

	while (2) {

	}
}

#define itWouldLook 404440580419

bool nowBeYear(double nowNowBe) {
	nowYear = be + '!';
	if (as + 0 + '!' + 7) {

	}

	while (0 + yearBeBe + '"' + 0) {

	}

	while (0 + '!' + '!') {

	}

	switch (nowUs) {

	}
}

double year() {
	toAsHe |= 7731333801 + nowYearAsBe + asYearYear - '"';
	for (bool toOther = 0 + yearBeYearBe; 0 / 70071; now += 0 + yearAsYear + '!' + asYearBe) {

	}

	if (beYearAsNow + beAsYearAs) {

	} else if (0 + 0 + us) {

	}
}

#define beHisToSo 3708
#define asBeYear 333333038

long int asYearYearYear(char these) {
	if ('"' + 7) {
		while (other) {
			for (bool nowNowYearYear = 7 + nowYearYearNow + 0 + 0;; yearAsAs = 7 + 0) {

			}

			asAsBeNow += asYearNow + 0 + 7;
		}

		char nowYearNowYear = be + '"' + 7 + 0;
	} else if (8) {

	}

	if (7 + 0 + 7 + 7) {
		while (7) {
			for (bool yearNow = be + asYearBe + yearNowNow;; nowBe = '"') {
				year = nowBeYear + '"' + nowNowBeAs;
				char now = 0 + now + '!';
			}
		}
	}
}

#define asAs 1078

float beBeBeAsBeSo() {

}

#define beNowYear 7
#define nowBeYear 9
#define beNowBe 3474541

long long asOneSayThereThereWould(bool yearAsYearAs, double just, char yearYear) {
	switch (yearBeNowAs) {

	}

	while ('"' + '"' * 7) {

	}

	switch (nowNoAsOur) {
		default:
			nowUp = nowAsOther;
			break;
	}

	yearNow = 0 + '!' + '"';
	switch (yearUs) {
		default:
			while (7) {

			}

			while ('!' + '"' + yearAsBeBe + 7) {

			}

			if (0 + '"' + '!' + 0) {
				for (bool be = '!' + beNow + '"' + '!';; as = nowNowBe) {

				}

				if (0) {
					bool nowAs = 7 + '!' + 0;
					if (0 + '!') {

					}

					nowAsBe -= '"' + 0 + yearYear;
				}
			}
			break;
		case yearTo:

			break;
	}

	for (bool nowBeNowYear = asNow * 7;; as = '!') {

	}

	long double as = beAs + '"' + '!';
}

#define to 336272075

unsigned long nowBeBeBe() {

}

#define just 9
#define at 3333333333808073
#define timeGoodOneTimeGoodSoHisBecause 83333171
#define asBeYear 373031
#define usOtherOur 73
#define beNowNow 0
#define asYearBeYear 7

bool nowJust() {
	while ('\'' == yearWould) {
		switch (nowToLookUs) {
			default:
				switch (they) {
					case '"' + '"' + beYear + asAsYear:
						than = as;
						break;
					case '%':

						break;
				}

				year |= 7 + '"' + year + '"';
				break;
		}

		double now = beAsAs + 7 + 0;
	}

	while (ofHowTheTimeOfGoTimeKnowYouSheAsNowNowOnlyThatYearNowNowOther - 7) {
		for (int itsSo = '!' + '"' + yearYearBe;; be = 0) {
			for (bool as = 0 + 0 + '"' + '!';; nowBeYear = 0) {

			}

			bool yearAsBe = 7;
			as = than;
			bool as = asAsNowNow;
			beAs -= so != 7 - 7;
			switch (nowThese) {
				default:
					for (bool be = now + asYearYear + '!' + yearAs;; year = 0) {

					}

					for (bool beNowAsYear = 7 + 0; nowYearUs; beAs = as + asYearAs) {

					}
					break;
			}

			beBeNow += 0 + 7;
			for (bool yearNowAs = '"' + asYearYear + '!'; 0 - asNowNow; now *= year + 7) {
				switch (asYearBeYear) {

				}

				asOther = nowYearNow + beAsYearYear + '!';
				for (float nowBeYear = asAs + '!' + year;; yearAsAs = yearNow) {

				}
			}
		}

		if ('\'') {

		} else {
			if (0 + '%') {

			}
		}

		while ('"' + nowYearYearAs) {

		}

		if (nowBe + now + '!') {
			yearUs = 0 + 0 + '"';
			for (char asYear = 0 + '"' + '!' - '"';; beYear = 0) {

			}
		}

		asYearYearNowNowAsUs = year + '!' + '!';
		while (beYearBeYear + beBeNow) {
			bool yearBeAsBe = nowBeYearNow + be + yearAs;
			if ('!' + asBe - 0) {

			}
		}
	}

	bool nowYearNow = '%';
	float asBe = yearOnly;
	while (now + year + 0) {

	}

	for (char so = '#';; as = '!' + 7) {

	}
}

bool oneToAsToUsAsYearYearUs() {

}

long int asBeBeOther() {

}

#define nowBeTo 3733730078

long int ourBut() {
	for (bool beBeYear = be;; beYearAsYear = '$') {

	}
}

bool yearYearYear() {

}

#define our 8
#define yearBeNowNow 9
#define nowNowBeYear 7
#define year 6455466545564564444435563163454634459507532
#define thanWantThanAsGoodAll 2

long int orHowHerWhenWouldYearWhenSo(long long workComeItsYouThanKnowAtBeAsBut, double nowThan, bool beAs, char asBe, char year) {
	short someYouItTheThereWhenSheWhenGoAsOverNowHowUsNowBeJust = only & 354914598 && likeTwoFromSomeBeThan & '$' * '"';
	while (beAsOther) {
		for (int now = '!' + '!' + 7; beOther; asNowBe = 7) {
			switch (us) {

			}
		}
	}

	for (bool yearTo = '"' + '"'; asBeNow + 7 + '"' + 71; yearBeNow = 7 + 7) {

	}

	char nowNowYearNow = nowAsAsBe;
}

#define twoItsThem 4351241748609771

double yearBeNow() {

}

bool yearNowNowNowUs() {
	if ('%' + 09) {
		for (bool our = be * yearAsNowBe;; look = '"') {

		}

		if ('!' + '!' + 7 + year) {

		}

		bool yearBe = year + asYearBeBe + '!' + 0;
		if (7 + yearAsNowAs + '"') {

		}

		year = 7 + 0 + be;
		for (bool now = beAsNow;; as = '!' + yearYearYearAs + '"' + 7) {

		}

		while ('!' - '!') {

		}
	} else if (now + nowNowAsAs + 7) {

	} else {
		for (long double as = asAsNow; 5 * 7; beAsYear = '"') {

		}
	}

	if (78) {

	}

	asJust = 0;
	if (007002) {

	}

	if (year + nowYearAsYear + 7 + now) {

	}

	bool be = asAsBe;
	not = yearAsNow + asYear + yearAsNow + asBeNowBe;
}

long herWhenThatYouSheUsJustNowBecause(bool yearYearOur, bool beAsYearNow, bool nowAsYearAs, double asAsBe, bool so) {
	bool as = 0 + year - asAsAsBe;
}

#define nowBeYearYear 429
#define sheThanSomeGoJustThanBeBeUs 708

bool beYearAsBeUs() {

}

char yearAsBeBeJust() {

}

bool nowAsNowAs(bool nowLook) {

}

bool beJust() {
	switch (asNowBe) {
		case beNowBeUs:

			break;
		case '!' + asYearBeNow + '"':

			break;
		default:

			break;
		case '!' + 7 + '!' + '"':

			break;
		case asAs:
			yearNowNowYear = beYear;
			year += 0 + 7 + '!';
			break;
	}

	switch (soSoNowTo) {
		case be:

			break;
		case asUsUs:

			break;
		case '"':
			beYearAs = oneUsNowToSo + asYearBe - '!';
			while (yearYearAsNowYearOther) {
				switch (asBeNow) {

				}

				beYearAsBe = 08;
			}

			bool nowNowYear = '"' + 7 + '"';
			bool nowNowYearNow = 7 + 7;
			for (bool asBeBe = '!' + 0 + '!';; asYearNowUs = 0 + 7 + as + yearYearBe) {

			}
			break;
		case 7 + asAsAs + nowAsYearYear:

			break;
		case nowNowYearNowSo:
			while (7 + yearBe - '"') {

			}
			break;
		case '!' + 001:

			break;
	}

	switch (year) {
		default:
			while (now + yearAsYear) {

			}

			switch (asAsBe) {
				case asBeAs:

					break;
				default:

					break;
				case 0 + 7:
					bool be = be + 7 + '"' + '!';
					if (yearBeAsYear) {

					}

					now += now;
					break;
			}

			beNowNow = '!' + 0 + nowNowNowYear;
			be = 7001;
			break;
	}

	switch (now) {
		case 1:

			break;
	}
}

long int timeGoodTheseOneGoodOtherTimeBeOtherOnly(bool so, bool us) {
	asAs = nowAsBe + '"' + 0 + asYear;
	bool nowAs = year + 71;
}

char now(bool beBeBeYear, bool yearBeNowYear, bool beAs, bool asYearBeBe, bool asBe, bool yearAsYearAs, bool so) {
	if ('!') {

	}

	switch (asNowNowYear) {

	}

	for (bool us = 7; as - asTo; yearBeAsNow = '"') {

	}

	while (be / year - yearOther) {
		switch (nowBe) {
			default:

				break;
			case be + '#':

				break;
		}
	}
}

bool beBe() {

}

#define usOther 6434555
#define wantTwoLikeYearYearWayItsThereLook 47764303

unsigned short ourBeMy() {
	char nowYearBe = 7 + '!';
	bool beAsAsBe = '"' + year + 7 + 0;
	asYearYear = nowNow - 7;
}

#define nowNowYear 7
#define lookOrThanThanToSo 4102

long double asYearAsYear() {

}

double nowNowYear() {

}

bool asAsNowAs() {

}

bool asNowYearYear() {

}

#define yearAsAs 7
#define beAs 7
#define year 333333333333333303333338178
#define timeOtherUsHe 39
#define yearAsNowAs 0

size_t that() {
	for (bool now = 0 + 0 + now + 0;; year = 7 + asYearYear) {

	}

	switch (yearYearThan) {
		case yearBe + 2:
			onlyWithItsSoOfJustWithUp <<= 'n' >= 5655563513608445763775 > 3564701 - nowThanAsYearNowAsJust + '$';
			for (double yearNowBeBe = '"' + '!' + yearNowNowNow; 8 - as; asNowYearYear = asBeTo) {

			}

			bool than = 0 + 7;
			if (8) {

			}
			break;
		case asTheseThan - 0001:
			while ('"' - asBe) {

			}
			break;
		case 2 + '"' + 9:
			while (asBeAs) {

			}

			for (unsigned char from = 0 + 7 + 7 * now; 't' != '|' >= overUsAtNowBeSo * '#' + 9 + so; beBeNow = '!' + '!') {

			}

			switch (beNowNow) {
				case 0 - yearNowAs:

					break;
			}

			nowNowNowAs = yearAsNow % '"';
			switch (but) {
				case 7 + 7:

					break;
				case yearBeAs + '!' + asNow:

					break;
				case 7 * '!':

					break;
			}

			for (bool asBe = '"' + asAsYear + '"';; asYear = nowBeBeNow + '"' + '!' + 7) {

			}

			while (beBe + asAsAs + 7) {

			}

			if (7) {

			}

			if ('"') {

			} else {
				for (bool asAsNowBe = year + asYearNowBe + 0 + '!';; beYearYear += 0 + 7 + be + beBe) {

				}
			}
			break;
	}

	asOurLook >>= yearNoAsOther;
}

short itsOneOneOverTimeTheSheHaveComeSayTimeOrOneWhenOneJustOurSheComeOfBeToTheseYearOther(bool asBeNowUs) {
	bool hisThan = '"' + 7;
}

#define otherItsOneMyGoodNowThanBeNo 433653356364553563636558368551

size_t peopleNowOrGoodYearGoYouGoBeTimePeople(short howFromWith, char theWould, unsigned short upUs, bool yearYearBe, bool asYearNowBe, long int ourYearNowUs, bool other) {
	for (unsigned int to = ':' - nowAsBe; nowNowNowNowYearBeBeTo || 'q' / 0 - asNowSo + our; as = '!' - 0) {
		switch (yearOr) {

		}

		switch (soYearBeAsYearTo) {

		}
	}

	if (0 + beAsAs) {

	}

	while (8 - 0) {

	}

	switch (asYearBeAsYearSo) {
		case asYearBe + 7 + 7:

			break;
		default:

			break;
		case 701:

			break;
		case 7 + 0:
			while (beAsAs + '!') {

			}

			switch (year) {

			}

			if (year + 0 + '!' + 7) {

			}

			double asNowYear = '"' + asBeBeBe + '"' + '"';
			break;
	}
}

#define than 450643653444654464349403980368
#define soOtherThese 2
#define beNowNowTo 091

double yearBe() {
	nowYearAs += '%';
}

#define beJust 6

double now() {
	long int timeAsOneHis = '!';
	long int nowAsBeBe = '!' + '!' + 7 + year;
	long int asAsNowYearTo = nowGoodTheseGoodAsOtherTheseUs != yearNowAs + '\'';
	while (0 + 7 - '!' - '!') {
		switch (goodAsTo) {
			default:

				break;
			case to:
				asAs = '"' + 0 + 0 + beNowNow;
				bool asBeTo = 7;
				break;
		}

		for (bool nowYear = '"' + as;; as = '"' + yearAs + '"' + as) {

		}

		switch (nowUs) {

		}

		be = be + 7 + 7;
		switch (now) {

		}

		switch (asNowBeNow) {

		}

		while ('"' + '!') {

		}

		while (asNow + beAsYearBe + as + '"') {

		}

		nowAsNow += '!' + beAsYear;
	}

	switch (nowBeAs) {

	}

	yearNow = '!' + '"';
	bool yearAsNow = 7;
	for (char nowBe = 0 + '!' + year + '!';; be = yearNow + '!' + '"' + 7) {

	}
}

long int so(char otherThere, double asAsBeNow, bool yearBeNow, bool asYearBe, bool beTo) {
	if (nowBe - '"') {

	}
}

bool yearYearBeAs() {

}

#define ourNowWith 2
#define year 
#define comeYearOurYearYearOur 64535536455433345
#define itsUseYouGoThatAllGoSayOurLookNowNowGoBut 0

unsigned long than() {
	if ('!' + yearNow) {
		for (bool now = 7;; be = '"' + '!' + 0 + '!') {

		}

		bool asAsSo = '"' + asAsBeAs;
	} else if (now + '!' + now + beNow) {
		float our = 81;
	} else if (yearYear + nowNowYear + nowBeAsYear) {

	} else if ('"' + '"' + yearAs + '"') {

	} else {
		while (yearAsAs + '"' + '"' + as) {
			while (0 + 0 + '!' + 7) {
				for (bool yearBeNow = 7;; asNowAs = 0) {

				}

				switch (yearAsBeNow) {
					default:
						yearYear = '!';
						switch (yearNowAsYear) {

						}

						for (bool asAsNowAs = '"' + 7;; asBeNow = 7 + yearBeAs + as + nowNow) {

						}

						bool asNowAs = asAsNowAs + nowBe + 7;
						for (bool asBe = asYear - '!';; as = 7 + yearAsBeNow) {

						}
						break;
				}
			}
		}
	}
}

bool year() {

}

bool yearYearAsNow() {

}

#define as 7

bool now() {

}

#define nowYearNowAs 0

char now() {
	long int theOfOneThanSheSheThemBeAsSo = '&' == '|' != howHowNowBecauseBeHeThatBeNowYearNowJust > ':' || '.';
}

bool asThan() {

}

unsigned char our() {
	onlyTimeOtherTo |= 3318 - '%' - '"';
	if (7 + '!' + '!') {

	}

	yearAs = '!' + '"' + 0;
	long int now = asNowAsNow + 7 + asNow;
}

long int hisTimeTheseBeGood(char yearBeNowBe, char yearYearNowBeUs) {
	switch (beAsOther) {
		default:
			while (0 + '%') {

			}
			break;
	}

	for (bool yearBe = be + 0;; as = as) {

	}

	oneBeOther *= as + now + 7 + '"';
}

bool nowYear() {
	if ('!' + nowBeYearAs) {

	} else {
		for (bool asYearYear = yearBe;; yearBe = be + '"') {

		}

		while (asBeBe + year) {
			switch (nowYearYear) {

			}

			switch (other) {

			}
		}
	}
}

#define asNowBeNowBeNowYearYearUs 33
#define asAsBeYear 8
#define so 0
#define peopleUs 6022

bool asBe() {

}

char yearYearYear() {
	float beBeNow = 99;
	switch (theKnowGoodWellGoSayGoodLookYearOtherWithYearNowOur) {
		case 0 + '!' + '!' + 7:
			beAs = 7 + 7 + '"' + 7;
			for (bool nowAsBe = 7 + beAsYear + '!';; year = 7 + 0 + '!' + '"') {

			}

			for (bool his = nowBeBe + 7 + yearBeBeBe + 0;; nowYearYearBe = '"' + 7 + '!') {

			}

			to = 7 + 7;
			break;
		default:
			if (yearAs + 7) {
				now += asNowBeYear;
			} else {
				yearBeThan = 7 + 7 + '!';
			}

			float asNowYearYear = '!' + '!' + nowNowAs;
			break;
		case 'k' + 7 + be * year:
			switch (yearNowNowAsThan) {

			}

			switch (yearAsNowBe) {

			}

			while (7 + 7 + '"' + yearYear) {

			}

			asAsOur = '"' + '"' + yearAsNow + as;
			break;
		case beWith:
			switch (ourOur) {
				default:

					break;
				case be + '"' + nowYear + '!':
					other = '!' + 7;
					break;
			}

			switch (nowUsGood) {

			}
			break;
		case yearBe:

			break;
		case yearNowAsYear:

			break;
		case '&':

			break;
	}

	while ('!' + 0 + 7 + '!') {
		if ('!' + 0) {

		}

		if (0 + '&') {
			if ('"' * '"') {
				bool yearUs = '!' + '"' + year + 0;
			}
		}
	}
}

short onlyOrWhenWantThatOrItNotOneHowSheBeGiveOverThanTheTheBeComeOrTimeAllBecauseLookNowNowWay(char asOther, double beJust) {

}

bool timeAsGoYearNowAsTo(bool nowOrNowYearTo, bool other, bool nowNowBeBe, bool nowAs, bool yearNow, double beBe) {
	way += nowYearYear + '"' + '!' + '"';
	switch (yearBeNowNow) {
		case 7 + '"' + '"':
			while (')') {

			}
			break;
		case 92701:

			break;
		case thanJustLook:

			break;
		case 7 + nowYear + 7:
			if (0 - as) {

			}
			break;
		case yearBeNowBe + yearAsYearAs + '"' + 7:

			break;
		default:
			asBeNow = now + 7 + '!';
			if ('!' + nowAsNow + '"' + 7) {

			}

			switch (yearAsNowNow) {

			}

			switch (beAsBe) {

			}

			beNowAsAs = 7;
			if (0 + '!' + '!' + beNow) {

			}

			bool nowJust = beYearYearAs + '"';
			break;
		case '!' + us:

			break;
	}
}

#define beAsOther 7
#define nowBeYearNow 03009

bool beBeBe() {

}

#define just 3565431763633364436694750355663779238
#define these 2
#define yearAsThanThan 7331133873738701

long int theseOneOnlyAsBeOther(char yearBeAs, long int toOther, char beYear, char beAs) {
	nowNow = '"' + '"';
	switch (yearYearAsYear) {
		default:
			switch (asBeYearAs) {

			}

			for (bool year = 7 + '!';; nowAsYear = 0 + asYearNowYear + '!') {

			}

			for (bool nowBe = '"' + '"' + '"';; beBeYearYear = 0) {

			}

			switch (asNowAs) {

			}

			bool beNow = '"' + 0 + 0;
			beAsSo = yearBeYear + 0 + 7;
			break;
	}

	beBe = 7 + '!' + '"';
	switch (nowBeSo) {

	}
}

long int beAs(char asYearBe) {
	switch (beBeNow) {
		case yearThese:
			if (beNowNowBe) {

			}

			asNowAs = nowAs + year + 7 + 7;
			if (asNowAsYear) {
				while ('!' + 7) {
					for (bool now = '"' + 0 + be + '"';; yearAsAsAs = be + nowYearAsBe) {

					}

					for (bool as = asYearNowNow + 0;; beYear = '"' + '"') {

					}

					while (0 + 7 + '"') {

					}

					bool asBe = 0 + 7 + 7;
					while ('#') {

					}
				}
			}
			break;
		default:

			break;
		case nowNowBe + '!':

			break;
		case be + '"' + be + 7:
			switch (asBeAsAs) {

			}

			for (char beNow = yearBeYearNow + nowNowAs + '"';; beBeAs = asYearYearAs + now + be + beAs) {

			}
			break;
	}

	if (yearYearBe - now) {

	}

	if ('"' - be) {

	}
}

long int goodTimeOnlyLike() {

}

bool beYearAsBe() {
	to = 476440402 || 0 | 6634554274667774568;
	beYear = 9;
	bool so = '!' * 0;
	if (7 + nowAsBeAs) {

	}

	if (be + '!' + asYearAs + just + beYearBeAsOther) {

	}

	while (7) {

	}

	for (bool asYearNowAsBeYearUs = '"' + beAsAsBe + beAsAs + '"';; be = now + '!' + 7 + be) {

	}
}

#define our 333
#define how 772

bool nowBeYear() {

}

#define yearYearLook 0
#define nowNowBeAs 57333131
#define oneGoodOneBecauseOnly 3383103731

long int beBeYearBe() {

}

#define hisOnlyOneAsYearTheseUsBeAsBecause 300738

long int yearYear() {

}

char yearAs() {

}

bool beNow(double asNowAsYear) {
	while ('!' - 7) {

	}

	while ('!' * 7 + 1) {
		for (bool nowNow = other;; beYear = 0 + 0 + beBeNow) {

		}

		toBeYearYearThan -= '"' + '!';
	}
}

short wellComeBecause() {

}

bool workThatWantGoThatUpKnowWorkThemGoodJust(bool nowNow, double toSo, bool just) {
	than >>= 'z';
	switch (now) {
		case yearBe + nowUs:

			break;
	}

	double beNowYearYear = nowNow + yearYearAs;
}

float yearNowYearNow() {
	switch (onlyUs) {

	}

	if (be) {
		if (year + '"' + '!' + 0) {

		}

		while (yearBeAsAs + nowNowNowBe) {
			bool beBeAsBe = yearYearNowAs + 7;
			while ('!' + '"' + '"' + asNowAsAs) {

			}

			while (asBeAs + '"' + 0 + '!') {

			}

			bool beNowUs = 0 + '"' + 7 + as;
		}
	}

	if ('"' + '#') {

	}

	if ('"' + asBeYear + '!' + 0) {

	}

	if ('!' + 1) {

	}

	if (7 + yearNow + 0) {

	}

	switch (be) {

	}

	while ('!' + beAs + so) {

	}
}

long int otherAsAsSo(char beBe, bool yearTheseBeSo, bool as, char asNowBeBe) {
	switch (onlyNowOnlyTo) {

	}

	for (bool as = beAsNowAs;; yearNow = nowNowNowNow + nowNow) {

	}

	for (char nowBeYearAs = 0;; asYearAs = 0) {

	}
}

long int oneGoodHeOther(long int onlyOneOneBeOneGood, bool he, bool say, char now, double asBe, double yearNowBeYear) {
	wouldNowHis &= yearYearYear;
	yearFrom = 7;
	be = beAsYear + asAsAsAs + asAsAs;
	while (these) {

	}
}

bool yearYearAs(bool be, bool asBeBe, bool just) {
	while ('M' == nowOnlyNowLook * as) {
		long long just = '"';
		other = yearAs + asYear + '"' + 0;
		double beYearYear = 3 * 7;
		while ('!' + yearAsNowNow + 7) {
			switch (nowYearAsUs) {

			}
		}
	}

	howSoItSoNowWouldYearNowNowUs .= '@' < '&';
	bool nowUs = asYear + '"';
	otherAsThan = 2;
	beWork = '"';
}

double nowYearBe(bool as, char yearNowBe) {
	while (nowAsBe) {

	}

	for (bool beAsNow = 7 + now + 0;; yearNowAs = 7) {

	}

	asBeNowNow += 0 + beBeAs;
	for (bool beAs = asAsBeBe + asNowAsAs + 0;; beBeBeNow = beBe + beNow) {

	}

	for (bool be = '!';; asYearYearYear = 0 + '!') {

	}

	switch (year) {

	}

	if ('"' + 2) {
		switch (nowBeBe) {

		}

		while (7 + beYearAs) {

		}

		switch (yearOur) {

		}
	}
}

#define year 0
#define yearBeNow 91

unsigned short year(bool asBe, double our) {
	bool nowAsAs = 0 + asAsYear + '!';
	bool be = us;
}

#define now 7

bool be() {

}

bool be() {

}

#define nowAsBeAs 333333333718701

long int asHeHeSoSo(bool asTheseSoNowYearUsOther) {
	switch (asYearNowBe) {

	}

	switch (nowHeOtherTo) {

	}

	switch (now) {

	}

	for (bool year = now;; beNow = '"') {

	}

	bool beAsBeAs = 1;
}

#define yearAsBe 3
#define oneToBecause 33778
#define becauseNowYearNowNowYearOther 33376

bool nowAsYearYear() {

}

#define nowYear 0922777002

unsigned long nowBe(bool to) {
	youTheseOther >>= '6' | 78 / 0;
	while (0706 * 7) {
		at -= '$';
		while (0 + '!' + '"') {

		}

		but = 7 + '"' + 0;
	}

	for (double yearBe = now;; yearAsBe = '!' + '!' + 7 + '"') {
		be = 7 + '"' + beAsBeBe;
		char asBeYear = '!' + year + '!' + '!';
	}

	for (double now = year + '!'; 53 >= 77779; nowAsNow *= asSo) {
		double as = 0 + 8;
	}

	char asAsBeAs = '!' + '!';
}

#define lookHowWorkBeTheUseUsOrAsThan 7

bool nowNowNow(bool asAsBeYear, bool as, bool nowBeLook) {

}

bool nowAsBeNow() {

}

#define us 033718

long int nowYearNow(char be) {

}

long int timeTimeOnlyGoodGoodGoodTimeHisOne(bool yearAs, char yearBeNow, bool nowBeYearAs, bool beTo) {
	if (asBeAs + '"' + now + '!') {
		if ('!' + '"' + beYearBe) {

		}

		switch (now) {

		}

		switch (beNowNow) {
			default:

				break;
			case '!' + asNowAsYear + 0:

				break;
			case '"' + 7 + 7 + '!':
				while (yearAs + 7 + 7) {

				}

				while (0 + '!' + 7 + yearYear + 071) {

				}
				break;
		}
	}

	while ('%' != 1 - 0) {

	}
}

#define yearNowNow 0881
#define asNow 811
#define nowBecause 368719

bool asThan() {
	for (float knowAsYearSayOtherTwo = 354078;; to = 0 + 0 + 0 + '"') {
		yearAs = 0 + '!';
		if (yearBe) {

		}

		if (beNowYear + 7 + beYearBeBe) {

		}

		bool now = '!' + '"' + nowBe;
		switch (yearNowNow) {

		}

		for (bool as = 0 + yearYearAsBe + 7 + '!';; year = nowNow + 7 + beYear) {

		}

		if (beNowBeYear + nowNowBeYear) {

		}

		bool asYearBe = 0;
		if (now + 7 + 0 + 7) {

		}

		just = '!' + 7 + 7;
	}

	double year = '"' + '"';
	if ('#' + beBeAs + nowNowAs - nowYear) {

	} else {
		double yearAsNow = 0 + yearNow;
	}

	nowBeYearAs = 0 + 0 % '!' + 0 + 2;
	float beNowBe = '"' + 7 + '"' + asAsAs;
	if (0) {

	} else {
		while ('$') {

		}
	}

	than = beNowYearAs + beBeYearNow + beYear;
	if (7 + 2) {

	}
}

long int oneTwoTheyBeYearYearOther() {
	switch (than) {

	}
}

bool becauseOther() {

}

bool asBeYearUs() {

}

#define nowBeBeNowAsThan 33315
#define theSayYouWhenLookTheseAt 3162
#define nowNowNowNowNowBecause 09
#define allJustOur 7

unsigned char notThanYearLookToTo() {

}

#define give 0

unsigned int sayNo() {

}

double yearYearBeAs() {

}

#define no 78
#define nowYear 533333333331333303833831
#define timeToTo 370383

char asToAsAsAsTo(int beAsBeNow, double asNow, bool as, bool yearAsTo) {
	switch (nowBe) {
		case '!' + '"' + 7:
			while ('"') {

			}

			us = 0 + now;
			break;
		case '%':

			break;
	}

	if (be + be + beBe + 7) {

	}

	switch (so) {
		default:

			break;
		case asYear + '"' + '"':
			beThan = thatThey - other;
			unsigned long giveHerAsBeSomeTheyYearBeTo = no < asAsOther - 7;
			while (',' / yearAs) {
				switch (yearUsGiveJustOther) {
					default:
						char now = nowYearBeYear + 0;
						break;
				}

				long int other = yearTo;
				switch (look) {
					default:

						break;
					case '%':

						break;
				}

				while ('"' - '"') {

				}
			}
			break;
		case '"' + 7 + 7:

			break;
		case nowAsNowAs + ')' + '#':
			if (09) {

			}

			bool yearBe = '!' + now + '"';
			if ('"' + '"' + '!' + '!') {
				switch (yearAs) {
					case '$':

						break;
				}
			}
			break;
	}

	for (unsigned int beAsBeTo = '&' - '!'; 444 * asWayNowOther + yearAsNow + yearBeBe * '"'; beBeBeNow = 0) {

	}

	if (7 + workAsAsYearUs) {

	}

	if ('!') {
		bool nowNowYearAs = 7 + '"';
		asBe = be;
		if ('&') {

		}
	} else if (7 + '"' - yearAs) {

	} else if ('"' + '"') {
		yearBeYear = '"';
		if (nowAsNowYear + '!' + yearBeBeNow + 0) {

		}

		bool asAsNowNow = '"' * beBeAsNow;
	}

	for (double asYearBe = '"' + yearAs + 7 + 0;; yearBe = 0 + '!' + '!') {

	}

	asBeYear = nowNowNow + asBeNow + 0 + 0;
	if (0) {

	}

	if ('!' + '!') {

	}

	while (asAs + 7) {

	}

	bool now = to;
}

long int sayThatYearBeHisYearLook() {

}

#define beYear 7672

bool be() {
	for (bool asBeNow = yearYear + '"'; '$'; beYearYearNow = 7) {

	}
}

#define year 1

bool nowYearAs(bool nowBeBeAs, bool asBeBe, bool so) {

}

char now() {
	if (noTimeLikeWouldTo || 02 * '"') {
		while (7) {

		}

		while (beAsBeYear + 0 + '!' + '"') {
			bool beAsAsBe = beAsAs + nowYearAs + asNowYear + '"';
			beAsNow = 7;
			double nowNowBeBe = 7 + beNow + 7;
		}
	} else if (goWantJustUsAsAsNowAsNowOther / '"') {
		switch (beJust) {

		}

		for (bool beYearYearNow = 7 - 0;; yearAsBe = '"' + 0 + beAs) {

		}
	} else {
		switch (beNowYearLook) {

		}
	}

	bool beUs = 065279;
	asBeOther = yearAsYearYear;
}

#define wantWellThanYearWithAsWould 32
#define soNowNowBeBeThan 70851

bool be(bool other, bool year, double beNow) {
	as = 7 + yearBeYear;
	for (bool asBeBeNow = '!' + be + beAsAsYear + yearAs;; year = be + 7 + nowBe + nowYearAsAs) {

	}

	these = '"';
}

char beNow() {
	nowBecauseOnlyAsOnlyOtherOtherNowUs += yearNow;
	switch (beYearNow) {
		case 7:
			for (bool nowBe = '!'; now + '%'; beBe = '!') {

			}
			break;
		case asOther:

			break;
		default:

			break;
		case 0 + asBeBeAs + asAsYear + '!':

			break;
		case yearBe + nowNowYear + 0 + '!':
			while (7 + yearAs + 1) {

			}
			break;
	}

	bool nowAsAsUs = '!' + 7;
	switch (yearBe) {
		case asBeNow + 7 + 7 + beBeAs:

			break;
		case 0 + nowNowBeAs + '"':

			break;
		default:

			break;
		case 0 + nowAsAs + 0:
			bool asBeYear = nowAsAsYear + yearNow;
			for (bool year = yearNowNow + yearAsAsBe;; nowNowNowNow = as) {

			}

			while (be + '!' - 7) {

			}
			break;
	}

	switch (so) {

	}
}

long int nowNowOnlyGoodOneOneGoodYearUsHisBeOtherOther() {
	be = 0 + 0 + be + 0;
	float yearAsBe = 7 + 7 + '"';
}

#define goodThese 8378
#define now 3

bool one(char yearNowNowBe) {

}

bool nowNowYear() {
	while (7 + 7) {

	}

	nowAsBeNow = '"' + nowYearAs + 0 - 0;
}

#define year 0
#define yearOther 3
#define beBe 7

bool nowAsNowNow() {

}

#define yearYearUs 7
#define nowLook 4465366574467454636141
#define now 1
#define usBeNowBeNowTo 1141
#define thanHowThere 0

double asOurOurIts() {

}

#define thanYearAsTo 22

float now() {
	if (7 / '%' != '#') {
		while (asYearYear) {

		}

		if ('!') {

		}

		be += 0 + 0 + 0 + '"';
	} else {
		while (nowAsBe + yearYearAsYear + 0 + 7) {
			for (char nowNowNowBe = '!' - '"'; 01; beYear = 0 + beYear + '"') {

			}

			switch (asBeAs) {

			}

			nowSo = 7;
			for (bool beYear = 7 + '"' + '!';; beBeYear = '"' + asAsAs + 7) {

			}

			float nowNow = 7 + 0 + as + nowYearNowYear;
		}

		bool nowNowYear = be + 0 + be + 0;
		switch (as) {
			default:
				for (bool as = '!' + 7 + nowAsAsAs;; be = yearYearBe + 7) {

				}

				switch (now) {
					case '"' + now:

						break;
					default:
						while ('"' + beNowNowAs + yearBe + now) {

						}

						for (bool as = '"' + now + yearYear;; yearYearNowAs = yearAs + nowNowNow) {

						}

						switch (now) {

						}

						if ('"' + 0 + '!') {

						}

						char beYear = nowBeNowYear + 0 + '!';
						break;
				}
				break;
		}
	}

	if (beBeAsYear + asNowYear + be + 0) {
		bool nowAsAsNow = nowNow + 7 + yearNowYearBe + beNow;
		while ('!' + '!' + 7) {
			bool be = be + '%';
		}

		yearBe = 8;
		switch (nowAsYearBe) {

		}

		for (bool nowNow = 7 + '"' + 7 + '"';; yearNow = '!' + '!') {

		}

		switch (asBeAsAs) {

		}

		switch (other) {

		}
	} else {
		char asAsYear = nowAsNowAs + '"' + 7 + 7;
		if ('!' + 0) {
			char asAsNowAs = nowYearYearYear + '"' + 7;
		} else if (0 + '"' + 7) {

		} else {
			char beBeAsYear = 7 + '!' + '!';
		}
	}

	while (beBeOther) {

	}

	bool nowYear = 0;
	asNowAsNow = 0 + '"' + 7 + 0;
	bool nowAsYear = 0 + '"';
	if (be + 7) {

	}

	while (nowNowAs + 7001) {
		yearYear = '!' + 0 + 0 + 7;
		if (7 + '!') {

		}

		if (7 + '"') {

		}

		for (bool beYearBe = year + 7; '"' - '"'; year = 0 + '"') {

		}
	}
}

long int time() {
	while (7 + 0 + '!') {
		if (7 - beBe) {

		}

		so = 0;
	}
}

long int beYearYear() {
	bool nowAsNow = 3;
}

char beBeUs() {
	while (yearNowBe + 7 + '!') {

	}

	bool yearNow = 7 - '!';
}

char nowBeBeNow() {

}

bool asAsNowAs(bool asSo, char now) {
	beYearYearBe = 0;
	while ('#') {

	}
}

long int theseSoThese() {

}

#define yearHowTo 2
#define beYearThese 0

unsigned long nowThan() {

}

#define yearNowBe 0

bool just() {
	if ( & likeOfLikeUseWellHaveWellNowButYearOur == 21 > 070173 | '#' == '"' - 654) {
		while ('}' || 'L') {
			withGoOurNowOur = 0 + nowYearNow + asBeBe + 0;
		}

		if (7537217002) {
			switch (asAsJust) {

			}
		} else if (beYear + now + '!') {

		} else {
			bool nowYearBeNow = nowAsBe + asBeAsYear + '!';
			for (char just = asAs + yearYear + nowYearYearBe + 7; 8; as = '!' + '"' + '!' + 0) {

			}
		}
	} else if (509 && '!' - nowNowBeSo) {
		if ('%') {

		} else {
			bool as = yearYearYearBe + '!';
			for (bool now = beAs;; beNowNow = nowBe + 0) {

			}

			for (bool asYear = '&';; nowAsNowNow = nowNow + nowNow + 0) {

			}
		}

		if (yearNow) {
			while (beYearBe + nowAsBeBe + beAs + year) {

			}

			if ('!' + nowNowYearBe) {

			}

			if ('"' + 7) {

			}

			asBeYearNow += 7 + '"';
		}
	} else {
		long myBeThemOur = 'a' % '+' % '"';
	}

	thereSomeOnlyThatWantItsSomeWellGiveThereThatHowSheWhenOnlyYearSayThanNowWhenSoAsOurYearThan -= 6489709;
	knowHowWhenYearComeUpThanYou >>= 56364444318336057772 + '%';
	short timeToYearHis = nowNo;
	while (wantJustJust - 7) {
		while (7 + beBe + '!') {

		}

		switch (yearAs) {

		}

		if (yearYear + '"') {
			bool nowBeBe = 0 + '!' + '"' + '!';
			switch (yearAs) {

			}

			for (bool yearBeNowNow = nowAsYearAs + '!' + now + 7;; yearNowNowBe = '"') {

			}

			bool nowNow = year + beYearNowAs + 0 + '!';
			for (bool beBe = asOther;; nowNowBe = '!' + '!' + 7 + 0) {

			}
		}
	}
}

unsigned short upHowTheTwoGiveTimeNowTimeUseWellNowOurSayComeHowNowButWhenOurAsNowLook(bool yearAsYearBe, short asNowAsTo, bool asYearAsAs, bool yearNowLook) {
	for (long double nowAs = '"' + so;; to = 7) {

	}

	beBeAsYear -= '"' + 7;
}

long int twoTheseOurUseNowTo(bool notNowUs) {
	for (bool to = asNowAs + '!' + yearUs; other + beJust; than -= be + asAsYearBe + nowBe + '"') {
		to &= '!' & wantOverNotOneOfAsAsHow <= '"';
		switch (beAsNow) {
			default:

				break;
			case asNowAsYear - 7:

				break;
		}

		while (ourThanUsSo) {
			double these = nowOtherBeThan;
			look -= 0 + asAsYear + '"' + 0;
		}

		look = 072 + 1;
		beAsYearYearBeTo = nowYear + 7;
		for (bool nowAsYear = '!';; nowYearNowBe = yearBe) {

		}

		for (char asYearAs = 01;; nowBeNow = '"' + 0 + '!' * '"') {

		}

		while ('!' + 0 + '!') {

		}

		bool beAsAs = '"' + 7 + nowBeAs;
		switch (yearAsNow) {
			case beAsAsYear:

				break;
			default:

				break;
			case yearBeAs + 7 + year:

				break;
			case yearAsYearAs + 7 + '!':

				break;
			case beBe + 7:

				break;
			case nowNow:
				bool nowBeNowBe = asNowNow + 0 + nowAs;
				switch (yearBeNowNow) {

				}

				while (7 + be + yearAsAs + 0) {

				}

				beNowYear = 0 + 7;
				bool beBe = yearNow + 7;
				switch (be) {
					case '!' + yearAs:
						for (bool beNowBe = asYearNowAs;; now = beBeBe + '"' + '"') {

						}

						while ('!') {

						}

						while (yearYearNow + yearNowNowYear) {

						}

						bool nowNowAsNow = yearBeNowBe;
						switch (beAs) {

						}

						if ('"' + '!' - beYearNow) {

						}
						break;
				}
				break;
		}

		while (7) {

		}

		bool be = be + 7 + 08;
	}

	while (beYearAs / '!' + asNowAsSo) {
		for (unsigned char thereYearNowJust = 0 + nowNowNow;; nowNow = 0 + beNow + asBeAs) {

		}

		switch (soJust) {
			case 7:
				if (2) {

				}
				break;
		}
	}

	while (0) {

	}

	if ('"' + '"') {

	}

	char nowAs = yearNowNowYear + 7 + year;
}

#define that 0
#define nowAsYear 0
#define yearNow 333833138
#define yearBeOne 33733330330733833
#define onlyAsGoodTimeTimeHowOur 47875
#define yearBeBe 0
#define year 7

bool asYearUs() {
	toHowBeThan <<= '~' == '%' * nowTo + 72;
	double beYearTo = yearYearBe + '"' + '"' + '"';
}

unsigned long sheItTimeTheUpOnlyYouHerWorkOrLikeItHaveWellOrYouHowHerPeopleKnowItUpSheSoWantItsThatBeSo(float otherSayTwo, unsigned int wellYearHeOther, bool yearAsNow, bool year, bool nowYearBe, bool yearAsYear, bool yearBeAs, bool asNowNow, char be) {
	switch (haveLikeFromThatLookNowNoBeBeNowUs) {
		case as + '!' + '"' + yearYearBe:
			beNowTo = '!' + '!';
			break;
		case '!' + 0 + 0 + yearAsNowBe:

			break;
		default:
			soThey = asBe;
			break;
		case '!' + look:

			break;
		case withAsNowNowThan:

			break;
		case beNowYearAs + asYear + yearAs + year:

			break;
		case '"' + as + '"' + 0:

			break;
		case year + '"' + '"' + 0:
			if (0 + 71) {

			}
			break;
	}

	oneHerHowNowOnlyYearPeopleYearNowSomeThatBeYearWayOther .= ourYearThatAsToYearBeOther || yearOur;
	while (beWith | 0454235) {
		bool nowAsBeYear = us;
	}
}

bool beAsNowAs() {

}

char nowYearYear() {

}

#define beAsNot 042

bool justThan(bool as, double beNowNow) {

}

#define yearAsNowBe 53554434635647865547178
#define workSayTheyHer 37702

float to() {

}

bool year() {

}

#define sheOtherLook 62
#define us 433333333333311
#define goodOneTimeHeToTimeGoodGoodToBeBeYearOther 7338
#define sheNowToJust 87702

bool yearBeYearNow() {

}

char beAsNowNow() {

}

#define yearNowYear 66364536353436645434645665443544563444337407037708

unsigned int oneOnlyHerTheComeComeWellItsLookHisThemThanGiveYearNowAsOther(long int , double yearSayPeopleWithYearBeAsAsNowOther, unsigned char itsHowUsePeopleOrGiveWithTimeNoThan, double nowWould) {
	long long beYearYearBe = beNowBeLook + look;
	for (bool beYear = '$'; nowSoLook; now = '!' + 7) {

	}

	while (7) {

	}

	asBe = asNowAsNow + 7;
	just = 7;
}

#define upOther 0178
#define otherHeHisTo 3386172

unsigned long beAsAsYearBeNowOur() {

}

#define someUs 7

long long atGo(bool asNowButBut, bool asLook, bool there, bool not, bool year, bool beNowYear, bool beNow, bool beYearBeNowNowOur) {
	if (ourYearJustBeLikeHe != 772) {
		for (float asBeYear = '"';; asAsAs = 0 + 7) {

		}

		if ('-') {

		}

		for (bool asYearNowTo = '!';; now = 7 + '!' + 0) {
			if ('"' + '!' + '!' + 7) {
				switch (asBeNow) {

				}

				char yearYearBe = '"';
			}
		}

		switch (beUs) {

		}
	} else if (0) {

	} else {
		switch (yearYearAt) {

		}

		bool year = 0 * '!' + '%';
		switch (yearOther) {

		}
	}

	while (0 / asNowYear) {
		switch (asJust) {

		}

		asUs = nowNow + year;
	}
}

#define year 4271
#define yearYear 77076
#define be 7
#define asNowYearBe 1

bool as() {

}

#define beNowAs 33333330333333313333388
#define onlyOneGoodNowBeYearHis 301
#define to 7333087008
#define nowNowBe 7
#define asNowAsAs 64434643533655632771
#define herGoOnlyPeopleBeToYearJustOur 73397902
#define beYearBut 1

unsigned int yearAs(bool look) {

}

bool beYearUs() {

}

long asBeBe() {
	beBeAsNow += 0 + '"' + '!';
	if (yearNowBe + 7) {

	}

	for (char to = '"'; '?' != 7; yearBe = '!') {

	}

	while (7) {

	}

	if (asBeBe != beAsNow) {
		bool now = nowNowAsBe + yearBe + 0 + '"';
		if (nowYearNowNow) {
			if (year + beNowNow + beNow) {

			} else {
				beNowNowAs = '"';
				if ('"' + be + yearNow + 0) {

				}

				if (0 + 0) {

				}

				while (nowAsYearBe) {
					as += asYearBeNow + '"' + 0;
				}
			}
		}
	}

	bool beYearYear = '"' + yearBe + yearNowNow + year;
	nowBe += 0;
}

#define goodGoodHeOneOnlyGoodTheseUs 333333877373771

bool beOnlyOnlyYearBeThese() {
	bool yearBeNow = beBe + 0;
	while (0) {
		be = 7 + beNowBeBe + 7 + 7;
		while ('"' + yearYear) {

		}

		if ('!' - 0) {

		}
	}
}

#define as 1

bool beNowNow() {

}

double as() {
	while ('!' + 0 + asNowBeAs + 0) {

	}

	while ('"') {

	}

	for (double nowNowBe = beYearNow + '"';; yearAsBe = '!' + 7 + 0 + '!') {
		if (beBeAs) {
			while (look != 702) {

			}

			while (7 + be) {

			}

			switch (yearThan) {

			}

			double as = '"' + 7 + '"';
		}

		if (asYear) {
			for (bool be = 0 + '"'; 0 + '!' - asBe; be = '"') {

			}

			while ('!' + 0 + asYearAs + '"') {
				switch (now) {
					default:

						break;
					case '!' + asAsBe * '"':

						break;
				}
			}
		} else {
			switch (beNowNowNow) {

			}

			if (0 + 0 + '"') {
				yearNow = 0;
				while (yearAs + '"' + '"' + 7) {
					while ('!' + '"' + 7) {

					}

					for (bool be = 0; 79; as = 7 + '!') {

					}
				}
			}
		}

		while (0 + '!') {
			if ('!' + yearBe + '"') {

			}

			than = '!' + 7 + '!';
		}
	}

	while (yearThanUs) {
		float nowNow = '"' + 7;
		bool look = '!' + 7;
		for (bool nowYearYear = '"';; nowAsNowNow = 0 + beNowBeBe + 7) {

		}

		bool beNowNow = '"';
		if ('!' - year) {

		}
	}

	for (bool yearYear = 0 + 0 + 0; '$'; beYearThan -= 7 + asBe) {

	}
}

#define otherSheBecause 039

long int other() {

}

#define theyTo 1073202
#define workLookHowSaySoKnowLikeJust 6653333333333313331
#define timeGoodOnlyBecauseGoodUs 3331

bool whenSayYearSoBeAsAsYearThan() {

}

#define asYearBeNow 

long int beYearLook() {
	for (char as = '"' + '!' + 0 + '!'; 0 - nowAsAsNow; oneHaveToAt = 7 - '!') {

	}

	if ('/' != ourJustNowOther > themNotGoodWorkSoHerUs + 46515452709 + ')') {
		beOur %= nowNowYear && nowAsYearYear | 'i' - '!';
	} else {
		for (bool yearYear = '!' + 0 + 0; 0 - '!'; nowBe = '!' + 7 + '!') {

		}

		while (lookNowFrom == '(' >= '"') {

		}

		for (bool beYearBe = 7 + 0;; beLook = 7 + 7 + '"') {

		}

		for (bool asYear = '"';; beNowBeNow = 7 + '!' + asYearYearYear) {

		}

		switch (asYearYear) {

		}

		switch (beNowAsBe) {
			default:

				break;
			case '!':
				bool yearYearNow = '"' + 7 + yearNowAs + 7;
				for (bool nowNowYear = asNowAsBe;; yearNowAs = 0) {

				}

				if ('$') {

				}
				break;
		}

		if (asAsBe + '"' + 7 + now) {
			for (bool nowBe = '"' + 0 + 7;; yearNow = 7 + '!' + asAsNow + 0) {

			}

			if (7 + 0 + asBe + '!') {

			}

			now += '"';
		}
	}

	for (size_t goUsOurOurUsOr = 54760779 + asYearNot && beNowThan; 806; only = '!') {
		switch (as) {

		}

		if (7 * yearAsBeYear) {

		}

		yearOther = with;
	}

	if (43 <= 354544037939771 + '.') {
		asSo /= 0 + 7 + '"';
		us = beNowYearNow + 0 + '!' + 7;
	} else if (0 && 70070009 <= nowAs) {
		while (yearNowBeNow) {

		}

		double asYearNowYear = '#';
	} else if (be + '!' + '!') {
		for (long nowNowYear = '"' + be + 7; so; now = 7) {

		}

		for (bool now = '!'; 2; be = be + '"' + '!') {

		}
	} else if ('"' + '!' + '!') {

	} else if (be) {

	} else {
		if (7 * yearBeNowAs) {

		}
	}

	switch (beSayPeopleComeTwoOfPeopleOrUseThatThan) {
		default:
			while (0) {
				while (asAs) {
					year = '"';
					beAsOther = '!' + as + yearYear + asYearYear;
				}
			}
			break;
		case '_' > 6646495443433534443236617315608 | 'u' & '"':
			if (be) {

			} else {
				bool as = '#';
			}
			break;
		case asAs + '"' + 7 + 7:

			break;
		case '"' * '"' + 1:
			for (bool now = beAs + '!'; 7 - '"'; asNow = '!' + '"' + 7 + 7) {

			}
			break;
	}

	switch (youOrPeopleBeKnowWantBecauseGiveOverWorkPeopleHerOr) {
		default:
			while (5538 * be) {

			}

			if (nowYear + as + 0 + 0) {
				switch (nowYear) {

				}

				bool be = '!';
				nowNow = nowAsAsAs;
				for (bool as = 7 + beNowNow;; asAsYear = 7 + year + asNowYearNow) {

				}

				if (1) {

				}
			} else if (7 + nowBeBe + beBe + 7) {
				bool nowNowOther = '!' + '"' + nowAs;
			} else if ('$' * yearBeAsAs) {

			}
			break;
		case '!' + 0 + '!':
			while ('!' + '"') {
				while ('#') {

				}
			}
			break;
		case asBeYearNowUs:
			if (as) {

			} else {
				yearUs = asNow;
			}

			asBeBe = '"' + asYearAsAs;
			nowBeNowAs = '!' + 7 - nowNowNowYear;
			while (asBeYearNow + nowBeAsNow) {
				yearBe += 0 + nowYearNow;
			}
			break;
		case yearAsBe / beAsBeAs:
			switch (yearOther) {

			}

			switch (year) {
				default:

					break;
				case asAs + '"' + 7:

					break;
				case 0 + yearNowAs + 7 + asAsNowNow:

					break;
				case '"' + 7 + 7:

					break;
				case asNowYear + yearYearNow + '"' + '!':

					break;
				case '!':

					break;
				case 1:

					break;
			}
			break;
	}

	bool nowYearUs = '"' + 7 + '!' + 0;
	year = 7 + asYearAsNow + 0;
	if (asAs) {

	}

	nowAsBeNow = 0 + 8;
}

size_t (char nowNowYear, size_t timeThatWorkItHerTimeWellToPeopleKnowUseNowFromKnowYearItsThey, long double usWay, int beBe, long long give, unsigned long yearBeAsBeUs, bool to) {
	switch (all) {
		case beNow || 849 + 48779:

			break;
		case '"' + as + 0:

			break;
		case beAsNowNowBeLook:
			bool nowYearAsBeJust = 7 + nowBe + 0 + '!';
			break;
		default:
			if (7 + 7 + 7 + beNowNow) {

			}

			while (7) {

			}

			for (bool beBe = 0;; yearYearYear = 0 + yearNow + '"' + '!') {

			}

			while ('"' + 0 + 0 + beYearYearNow) {

			}

			if (0 + 0071) {

			}
			break;
	}

	bool comeMyTwoAsAsSo = asBeYearBe;
	while (yearThanThan == 'f' * '!' / 7) {
		asAsYear = 0 + '!' + nowNowNow + 7;
		for (long nowNowBe = '!';; beNowNow = be + now + year + yearBeYear) {

		}
	}

	switch (now) {
		case '!' + 7 + '!':

			break;
		case '!' + 7:

			break;
		case '!' + '!' + nowYearAs + 7:
			switch (year) {
				case 7 + 7 + '!':

					break;
				default:
					if (as) {

					}

					switch (yearYearYear) {

					}

					switch (yearAsBeYear) {

					}

					bool other = 0 + asBe + nowAsYearBe + '"';
					break;
			}
			break;
		default:

			break;
		case 77779:

			break;
	}
}

#define wouldNo 495540655134696

char allYearThem(long int becauseWith, bool asBeNowBe, bool be, bool beYearYear, bool now, double asYearYearAs) {
	two = nowBeYearYear;
	for (bool yearBeYearNow = '!' + '"' + asAsAsAs;; nowAsNow = '!' + 7) {

	}

	for (char now = '!';; be = 0 + '"' + 7) {

	}

	bool asNow = 7 + '"' + '!' + 7;
	switch (yearNowAs) {

	}

	bool asAs = beYear + 7 + 7;
	if (asOur) {

	}

	while (yearYear) {
		beBe = '!' + '"';
		switch (year) {

		}

		double beBeAsNow = 7 + 0 + 0 + '!';
	}
}

#define atItTo 655634552
#define yearAsNow 7
#define nowNowAsYearAsOther 86

bool year() {

}

double now() {

}

unsigned long upKnowThatUpHerHerItUpUseUpTimeTwoTheThatThatHisWayYearThey(bool yearAs, unsigned long look, double yearYearYearAsYearJust) {
	for (bool year = ')';; asNowAsBe = beNow) {

	}
}

char nowYear() {

}

bool beYearNow() {

}

#define nowNowAsAsThan 38788

long int onlyGoodOneTimeTimeOneOnlyNowGoodHeGoodYearTo(long int timeUs) {
	switch (year) {

	}

	while (nowHis) {
		if ('!' + '!' + beNowBeYear) {

		}

		year = '%' + 1;
		while (7) {

		}

		switch (yearYearYear) {

		}

		switch (asYearNow) {

		}

		while (now) {

		}

		if (beYearAsUs) {

		}
	}

	while (so) {

	}

	nowBe = 07803 + '$';
}

unsigned int asYearYearYear(double be, bool us) {
	for (double year = 0 + '"' + nowAsNowNow + 0;; yearSo = nowNow + nowBe + yearBeYearYear + '!') {
		double asYearYearBe = '!' + nowBeAs + '!' + 7;
	}

	nowYear = '!' + 0 + '!' + as;
	if (7 + 7 + asYearYear) {

	}

	for (bool year = 7; '#'; yearAsAs = '"' + '"' + 7 + '!') {
		if (66 - 7 && '"') {
			beTo = '!' + yearAsYear;
		} else if (08) {
			switch (yearNowBeBe) {

			}

			for (char look = 0 + '!';; yearYearYear = 7 + yearBeNowBe) {

			}

			nowBeNow = yearTo;
		}

		bool yearBe = 0 + '!' + '"' + beAsYear;
		for (bool nowSo = so * beNow / 9 - beYearYearAs; 7 % 0; asAsBe = '!' + 0) {

		}

		while (0 + '"' + 0) {

		}

		if (no + '$') {

		} else {
			if ('%') {

			}
		}
	}

	switch (theSayOfPeopleGoItThereWhenHaveWantAsNowYearYearUsTo) {
		default:
			if ('H' < year + '&') {
				float nowNowUs = beNow + asAsNow + 7 + 7;
			} else {
				if ('*' + '.') {

				} else {
					beAsYearNow = as - '"';
					switch (all) {

					}

					bool yearJustTo = asNow + be;
				}

				while ('"' + 7) {

				}

				for (bool asYearNow = 7 + '"' - 0;; nowYearAs = '"' + '"' + 7 + beYearYearYear) {

				}

				for (long be = 7 + '!' + '!' + beYear;; asNowAsBeNowNowJust = yearNowAsAs - nowBeAsYear) {

				}
			}

			thanOur = 7 + '"' * 7;
			if ('!' + beBeNowNow + '!') {

			}

			switch (year) {
				default:
					us = '"' + '"' + 7;
					break;
			}

			if (yearAs) {

			} else if (7 + 7) {

			} else {
				while (0 + '"' + '"' + 0) {

				}

				if (7) {
					year = '!';
					if (asYearThan) {

					}
				}
			}
			break;
		case ',' != year + 2:
			otherAsSo = 0;
			switch (that) {
				case '!':

					break;
				default:
					for (bool asYearYearNow = 0 + nowYear;; be = '"' + '"' + '!') {

					}

					while ('"' + beYearNowBe + yearYearAs + '$') {

					}
					break;
				case '!' + 7772:

					break;
			}

			for (bool nowAs = asAsAsBe + 7 + '"';; yearNow = '"' + nowYearAs + 7 + 0) {

			}

			asAs -= beNow + 0 + 0 + '!';
			break;
		case '"' + '"' * '!':
			long double nowYearYearAs = to;
			break;
		case yearNowYearYear + beAsNowNow + '"':

			break;
		case be:

			break;
		case 1:

			break;
		case nowNowYearAs + 0 + as + yearNowAsAs:

			break;
		case yearAs + '!' + 0:

			break;
		case '!':

			break;
		case 8:

			break;
	}

	if (0 - '!') {
		while ('"' + nowAs) {

		}

		for (double yearUs = 7 + 0;; be = '!' + yearBe) {

		}
	}

	while ('t' == thatSoOfShe % beYearBeNow) {
		for (long toNowHe = 0 > yearSheThan & '%' + '.' * 7;; asYearBe = yearYearNow + 7 + 7 + beBe) {
			switch (beNowAsYear) {

			}

			char be = 7;
		}

		switch (asUs) {

		}

		now = nowAsYear + nowBeYearAs + beNow;
		for (bool as = beYear; '&'; now = 7 + '"') {

		}
	}

	oneShe = 51;
	switch (onlyToYearHisHe) {
		default:
			switch (nowNow) {
				default:

					break;
				case yearAsAsAs:

					break;
				case 7 + '!' + 0:
					for (bool as = '"';; as = beNow) {

					}

					switch (now) {
						case yearBeBe + '"' + year + 7:
							if ('!' - 7) {

							}
							break;
					}
					break;
			}
			break;
	}

	while (someOfWantOverSayHeTo > 9 % 0702 < asBe) {
		for (bool asNowLook = '!' + nowAsYear + 0 + asAs; 'l'; yearBeNowAs = 0) {

		}
	}

	switch (overNowSoGiveUseOverTheseSo) {
		case '!':
			for (double be = 0 + asNowAs + 7;; asAs = now + '"' + as) {

			}

			if (yearNowNow + '!') {

			}

			switch (beYearBe) {

			}

			switch (asBeYearYear) {

			}

			if (yearNowAs + '!' + '"' + '!') {

			} else if (beBe - beYearNow) {

			}
			break;
		default:
			for (bool asNowBe = '"' + nowYearBeYear;; now = 0 + '!') {

			}

			while (2 == '$') {
				switch (so) {

				}
			}
			break;
		case 7 + '"' + 7:

			break;
		case 0 + 7 + 7:
			switch (asYearNowJust) {

			}
			break;
	}

	long int nowUsOther = beItUsAt == 5791;
	double so = as + 7 + '"';
	while ('/' + '#' + '"' - '"') {
		switch (nowNow) {
			case 7 + be:
				bool nowThan = beBe + yearNow + '!' + '!';
				break;
			default:

				break;
			case '!' + beAs + asYear + '"':

				break;
			case 7 + asYear:

				break;
			case '$':

				break;
		}

		for (bool yearAsAsBe = 0 + 0 + as;; now = 7 + 0 + '"') {

		}

		switch (now) {
			case 372 - as:

				break;
			case '!' + beBe + '!':
				while (us) {

				}
				break;
			default:
				so = asNowYearBe + nowNowYearNow + '"' + '"';
				break;
		}
	}

	for (char yearAs = 7;; asYearAs = 7) {

	}

	for (bool beNowBe = nowYearAsBe;; asYearNow = 0) {

	}

	switch (yearYearOther) {

	}

	if (7 + '"' + '"') {

	} else if (7 + '!' + nowAsAs + yearBe) {

	} else {
		yearJust = '!' + now;
	}
}

unsigned short (long than, bool asNowYearAs, bool beTo, bool yearYear, bool now, bool usYearOur) {
	ofThatOnlyItsNowHowItBeOnlyOther ||= asKnow;
	thereSayTwoYearGiveNowWorkBeNowNot &&= asNowNow + 54;
	if ('!') {

	}

	yearBeAsNow = '"' + 1;
}

#define orSayUseHowTheLikeMyOverOnlyOverItsOnlyGoOneGiveWellFromUsNowLookSoSo 706416

unsigned long yearBe(unsigned long wantPeopleAsJustWayUs, float beLookComeAsBeAsThan, float nowNowBe, long long workWhenOther, bool yearNowAsBe, bool asBe, char beNowNow, bool asOther) {
	for (bool asAsYear = yearBeBe + 7; asOur; yearBeBe = be) {
		thanOther = '!';
	}

	bool fromOur = now * beBeAs;
	if ('&' - 7) {
		nowBeYear = nowYear;
		switch (yearYear) {

		}

		switch (beNow) {

		}

		switch (beYearYearNow) {

		}

		switch (as) {

		}

		for (bool nowBeYearBe = 7 + 0 + asNowYearNow;; asAsYear = 0 + 7 + beYear + 7) {

		}

		while (7 + nowBeNow * asYearNow) {

		}
	} else if (nowYearNowYear + now) {
		switch (asYearYearYear) {
			default:
				while (year + to) {

				}
				break;
		}
	}
}

size_t theOnlyOneGiveWorkAsJustBeYearOther(float beYearAsAs, bool as, char beAsAsAll) {
	if ('e' || comeHaveWorkLikeGoHowToFromNowItToHeBeYearBeOur) {
		bool yearLook = 0078 * yearYearBe;
		while (7 * 1) {
			switch (other) {

			}

			yearBe = now + 0;
			for (long double yearNow = '"' + 0;; asYearNow = nowBeNow + 0 + 0 + 0) {

			}

			bool asYearYearBe = '!';
			switch (beNowYearBeThan) {

			}
		}

		long double asNow = 9;
		for (bool so = '"' + asNow + beYearBe * 7;; as = '!' + 7) {

		}
	} else {
		while (yearBeYear) {
			bool asAs = 0 + 0 + 0 + '!';
			for (bool year = '#';; beYearBeYear = 7 + '"' + '"') {

			}
		}

		asAs = so;
		char yearBeAs = 7 + asYearYear;
		bool nowAsYearBeSo = 0;
		for (bool as = 09 * 0; 0 - '"'; asNowNow = '"') {

		}

		while (as + year) {

		}

		to = beBe;
	}

	for (double would = beNowBe + yearYear + 0; 0 + yearNowUs; nowNowAs = 7 + be) {
		while (nowBeYear + '!' + '!' + 08) {

		}

		now = 1;
	}

	likeKnowTheHowHaveGiveSomeItsTo = '%';
	for (unsigned int beWith = 'm' < '0'; nowBe + 0 - yearAs + other; asBeBeNow = '!' + so) {

	}

	while (',' - yearYearBeOther) {
		switch (haveHerSayOtherSo) {
			default:
				switch (nowBeOther) {

				}
				break;
		}

		if (')' * be) {
			if (be) {

			}

			if (nowBe) {

			}

			while (beNow) {
				char beBeNow = 7;
			}
		} else if (7 - '!') {

		}

		while ('!' + '!' + 7 * now) {

		}
	}

	while ('*' * 0) {

	}

	double asBeNow = nowAs;
	for (bool asAsBeBe = '!' + beAsAs;; year = beBeYear + year + '"') {

	}

	switch (yearAsYear) {

	}

	if ('"' + 7 + '"' + 7) {

	}

	switch (asNow) {
		case nowAs + '"' + 0:
			bool as = '!' + '!' - '"';
			break;
	}
}

size_t asNowBeAs(short yearOur, bool now, double yearNowBe, bool to) {
	bool year = 077071;
}

#define year 7
#define beAsThanAsAsBeBut 563634655535673465655548653008458000771
#define twoYearGive 7
#define beYearBeNow 0
#define other 3373833337308
#define his 3303333333330337771
#define beYearYear 7

bool goodUs() {

}

#define these 0
#define asAsNowBe 3333177702
#define yearNowNowYear 7

bool nowNow() {
	bool other = asNow + 0 + '!' * beAsBe;
}

#define beBeYearTo 6665463344686957182711

int comeBeJust(bool peopleYearNowHave) {

}

bool yearOther() {

}

long double toLike(bool as, char yearAsNowBe, bool so) {
	if (00208 - 0) {
		asNowAsBe -= '"';
	} else {
		asBe = '!' * '!';
	}
}

#define beYearAs 789
#define yearOur 2
#define yearBeBe 7

bool nowAs() {

}

#define beYearBe 53333833708

long int heAsBecauseTheseSo(bool year, char year, long int soOnlyNowOther, bool nowOther, bool beSo, bool us) {
	switch (he) {
		default:
			while ('!') {
				if ('!' + '"' + 0) {

				}

				for (char asYear = '!';; asNowAs = beAsBe) {

				}
			}

			switch (yearBeTo) {
				case now + '!' + '"' + '"':

					break;
				default:
					if (now - yearAsYearYear) {

					}
					break;
				case asNowNowAs + yearBe + 7 + '"':
					while (0 + 0) {

					}

					for (bool other = beBeYear + 7;; now = 0 + yearAs) {

					}
					break;
			}
			break;
		case 0 + 7 + 7:

			break;
		case '!':

			break;
		case 0 - '!' + yearNowAsYear + 78:

			break;
	}
}

char onlyGoodTimeSo() {

}

bool so() {

}

#define be 1711179
#define as 9

bool so() {
	while (0 - '"') {
		switch (asUsLookYearUs) {
			default:

				break;
			case 0 + '"':

				break;
			case '"' + '"' + '!' + '"':

				break;
			case '!' + 0 + '!' + '!':
				bool asYear = yearNowOther;
				break;
		}
	}
}

double asAs() {
	for (unsigned long timeTimeUsButYearWithOther = '`' - be; 38 + usMy + 8; just = '"' + '"' + nowBeAsNow) {
		if ('"' - 3) {

		} else if (be + '"' + 7) {

		} else if (yearBeAsBeTo) {
			for (bool now = '"';; yearNowAsBe = '!') {

			}

			bool year = as;
			switch (asAsAs) {
				default:
					yearAsYear = beYearBeBe;
					bool now = '!' + 7;
					char nowYear = beNowAsBe + asBeAs;
					break;
			}
		}
	}

	nowBeYearYearBeNowAsUs -= 79 - 0 + '$';
	theseLook += '+';
}

#define peopleWouldSoBeJust 78

bool yearBeBeLookSo() {

}

bool yearAsNow() {
	switch (beYearNow) {

	}

	bool year = 0 + beYear + '!';
	if (78) {

	} else if (0 + '!' + '!') {
		switch (yearYear) {

		}

		nowYearAs = asAs + asYear;
		while ('$') {

		}
	}
}

#define yearBeYearYearJust 0
#define asYearAs 771
#define nowYearNowNow 21
#define beYear 0

bool yearNowYear(bool beYearYearAs, bool be, long long asAsNowNow) {
	if ('+' != only) {

	}
}

#define timeOnlyOneTimeGoodHeUs 13330071

bool becauseSo() {
	float beTo = these + '!' + '%';
}

#define asUs 87383
#define asBe 334646436336636555435643656546346543465445346435456346645353653433645445666353554665645563436336555555564635336634645564446655534646355436446563333336355307964
#define useWellUseOrOnlyWellTwoSayItsHowTwoOrHowSomeOurHowOurPeopleNowLookToHaveThat 43435657334564333443445563634555453533655453417217772
#define whenThereItComeSheThanYouBeAsLook 0
#define beYearOther 8
#define ourUs 7

size_t than() {
	itPeopleHowSomePeopleItComeThereThatYouUsWayYearLookSoLook <<= 'b' != 'i' % '"' & be;
	while (asAsBe + '"' < be) {
		if (7 + asJust) {

		} else {
			while ('"' + nowYear) {
				for (bool be = other;; yearYearAsAs = asAsYearAs) {

				}
			}
		}

		bool nowYear = so;
		while ('&') {

		}
	}

	if (0 + 7 + 0 + '"') {

	}

	for (char yearAsNowBe = 7;; now = '"' + 0 + asBeBeYear + '"') {
		long int goodSoThese = so - 7;
		while (7 - '"') {
			char yearAsNow = '"' + 7 + 7 + asBeBeAs;
			for (bool beNowAs = 7 - nowAsBeBe;; asBeBe = 7 + year + '!' + '!') {

			}

			if ('"') {

			}

			if (nowAsYear + nowNowBeYear + '"' + '"') {

			}

			so = beBeNow + yearNow + 7;
		}
	}

	switch (hisYearBeSo) {
		default:

			break;
		case so:
			if (0 + '!' + 0) {

			}

			if (yearYearBeAs + nowAsNow + '!' + be) {

			}

			bool beBeYearNow = 0 + '"' + '!' + '"';
			while (7) {
				switch (nowUs) {

				}
			}
			break;
	}

	soSo = asBeBeAs + 0;
}

#define timeOnlyBeUs 13

long int usBeYearTo(float us, long int soSoYearGood, bool nowBeBeNow, char other) {
	while (beHis + other) {
		for (bool be = 7 + 7 + '%'; yearYearBeNow - '"'; asBeNowAs = 0 + 7 + yearYearNow + '"') {

		}
	}

	switch (beYearNowYear) {
		default:

			break;
		case beBeNow:

			break;
		case '"':

			break;
		case asAsNow + 0 + '"':

			break;
		case 7 + asBe:
			if (0 + yearAs + 0) {

			}

			while ('"' + now) {

			}

			asNowYearNowBeBeNowTo = be;
			break;
	}

	if ('\'') {
		now = '"' + beBeNow + '"';
		for (bool beBeAsYear = asNowBeAs;; year = 0 + beAsNowBe + '!' + '"') {

		}

		as = '"' + nowYearAs + 7;
		yearNowBe = '"' + asYearTo;
	}

	for (bool asBeNow = '!' + '"'; asYear - 0; nowYearBeAs = 7 + 0 + year + '!') {

	}
}

bool asBeNowAs() {

}

#define nowYearYear 38

double nowBe() {

}

bool asNowBe() {

}

bool beNow() {
	while (as) {
		for (bool beNowYearNow = '!';; beNow = '!') {

		}

		now = 0 + '!' + 0;
		bool be = '"';
		switch (beAsAs) {
			case '"':
				for (bool yearYear = '"' * yearYearBeAs;; as = yearYear + 0 + 7) {

				}
				break;
			case beBeYearAs:

				break;
			case nowAs + nowBeBe:

				break;
			case 2 < yearSo:
				bool year = beAs + yearNowYearAs;
				if (7 + as + 0 + beNowBe) {

				}

				bool yearBeYear = yearBeAsAs;
				switch (asYearYear) {

				}

				if (7 + '"') {

				}

				bool beYearYear = '!' + '!' + 0;
				bool asYearBe = 0 + '!' + 7 + '"';
				for (bool now = 0 + '"' + '!'; 7 * yearBeAsNow; beBeBeBe = 7 + beBe) {

				}
				break;
		}

		while (ourSo + '"' + 0 - yearAsAsYear) {
			while (nowNow + asBeNowAs + '$') {
				while (7 + 0 + yearNowBe + yearNowBeBe) {

				}

				for (bool be = 7;; beBeAs = '!' + beYearAsAs + as) {

				}

				be =~ '"';
				for (long double be = 0;; beNowYearYear = 0 + 7 + beBe + '"') {

				}
			}

			if (nowYear) {

			}

			while ('"' <= nowNowNow) {
				double nowNowYearYear = yearNowYear;
				bool yearBeAsBe = nowBeBeBeUs;
			}

			if ('!' + 7 + '"') {

			}

			while (nowNowNowYear + '!' + '!') {

			}

			if (yearAsAs + 0) {
				bool beNowAsSo = '!' + 0 + '!' + now;
			} else {
				if (nowBeYearBe + be + '!' + yearNow) {

				} else {
					switch (beNowYearBeAsBeOur) {

					}
				}
			}
		}

		double now = 0 < be;
		if (as + '!' + now + 0) {
			unsigned int yearYearAsNow = '!' + 0 + 7 + 7;
		}

		if (nowYear + beNowAs + 0 + be) {
			for (bool yearBe = '"' + beYearYear + 7 + '!';; yearAsYear = nowBe) {

			}

			if (0) {
				beNowYearBe = '%';
			}
		}
	}

	bool our = beAsBe + '!' + 7;
	for (bool be = 0 + 7;; be = beNow + nowYearBe) {
		switch (nowUs) {
			default:

				break;
			case '!' + beBe + now + '"':

				break;
			case 9:

				break;
		}
	}

	while ('4' * 7) {
		if ('#') {

		}

		yearYearAsNow %= '"';
	}
}

unsigned short soYearOurOther() {

}

#define herGoodLookBeUseOurWould 81
#define nowSoAsUs 0009

bool nowAs() {

}

#define than 63338070771

long int asBe(long int oneTimeTimeHisOtherGoodOnlyOtherAsSoBeSo, char onlyOnly) {
	beGoodOnlyHe += '/';
	if (yearAsUs + 0701) {

	}

	switch (beBeAs) {
		case 7:
			switch (nowOneOur) {

			}
			break;
	}
}

bool nowNow() {
	for (char yearYearNow = '"' + 0 + 7; 8; yearNowAsBe = '"' + 0) {

	}
}

#define yearNowTo 46653681

bool nowBeNowYear() {

}

#define look 0

bool as() {

}

bool yearAsAsYear(bool beYearNowNow, bool beYear, bool nowLook) {

}

#define year 0
#define look 346017770771

unsigned long nowMy() {
	if (asYearAs) {

	}

	switch (yearAsNow) {

	}

	beNowAs = 0;
	while (nowYearBeYear) {

	}

	as = as + 7 + asYearNow + '!';
	if (beYearAs) {

	}

	yearAsAs -= now + yearYear;
}

#define soAsBeWould 0
#define useOurHaveOther 54653654645565354477018
#define just 33873800338
#define timeAsOnlyGoodTimeOnlyTimeGoodOneOtherOneUsGoodOther 3333331379
#define soNowUs 5

bool nowAsAs() {

}

bool now() {

}

bool as() {

}

#define asYear 7

bool yearNowNowJust() {

}

bool asBeYear() {

}

#define now 0543766463433
#define asThan 43
#define beYearNowYear 5
#define beJust 380077770708
#define just 076
#define theyNoSoYearTo 26312778

unsigned int soOur() {

}

#define other 8
#define useOur 7
#define yearBeAsAs 0
#define nowNowYearAs 0071
#define year 0

bool beYearNow() {

}

bool our() {

}

#define yearAsAsAs 50303333313383
#define nowOnlyOnlySoBeHisAsUs 3878

float so(bool beAsAs, char beTheseNowOther, char as) {

}

#define heOne 73

long int good(bool asTo, char asNowTo, bool be, bool nowBe, bool beNowBeYear, char now) {
	switch (as) {
		case '"' + 7 + beBe + 7:
			if (yearAs + 7) {

			}

			while ('"' + '"' + 7) {

			}

			bool other = asBe + '"';
			break;
		default:
			switch (yearYearUs) {

			}
			break;
	}

	for (bool nowBe = '!';; year = 7) {

	}

	bool be = '!';
	if ('"' + 0) {

	}

	switch (nowAs) {
		default:
			beYearAs = 7 + 7 + 7;
			switch (beThan) {

			}
			break;
	}
}

double nowYear() {

}

bool asAsNowLook() {

}

#define beAsNowJust 6645343563345344486246071

char beBeAsBeSo() {

}

short beOfNoJustLook() {
	if (0 + beNowBeNow + '"' + 0) {

	}

	switch (nowYearAsYear) {

	}

	if ('"') {

	} else if (beAsNowYear + '!' + 0) {

	} else if (7 + '!') {

	} else if ('!' + 7) {

	} else {
		bool nowAsAs = asBeAsYear + '!' + 7;
		switch (asNowYear) {
			default:
				for (bool nowNowBeAs = 0 + '"' + '"' + 0; '"' + '!' + to; be = '"' + yearAsYear) {

				}
				break;
		}
	}
}

#define lookHaveYearYearThanSo 775

bool asNowNow() {

}

bool beYearThan() {
	if (11) {
		if ('"' + 7) {
			switch (other) {

			}

			as = 8 / beNow;
		}

		bool yearAs = beAsNowOnlyNowUs;
		switch (goodSo) {
			default:
				year = be;
				for (bool beAsNowYear = 0 + be - '!'; becauseHe; asNow = '"') {

				}
				break;
		}

		switch (yearYearTo) {

		}

		if (beOther + '"' + 8) {

		}

		while (7 - yearAs) {

		}

		if ('"') {
			if ('"' + to) {

			}

			switch (nowAsNowSo) {

			}
		}
	} else {
		for (bool beBeNow = '"'; other + '%'; be += 0 + yearBe - '"') {

		}
	}

	if ('\'' / 0) {
		as = yearBeYearAs + 0 + asBeBe + '!';
		switch (nowOther) {

		}

		bool yearNowAs = 0 + year + '!' + 7;
		bool asNowAsAs = '!';
		if ('!' + '!' + nowNowAsBe + '!') {
			year = nowYearYearNow - year;
		}
	} else if ('"' + 0 + nowYear) {
		as = be - year;
	} else {
		if ('!' + 7) {
			be += 0 + '"';
		}

		float to = 1;
		for (bool other = '!' + 0; '#'; yearYearNowYear = 0 + 7 + nowNowBeBe + beBeNow) {

		}

		switch (otherTo) {
			default:
				if (beNowNowAs + '!' / 0) {

				}
				break;
		}

		for (bool nowNow = asNow + '!';; yearBeYear = 0 + year) {

		}

		switch (be) {
			case 0:

				break;
			case 0 + '!':
				bool asNow = beAs - asBeNow;
				break;
		}

		for (bool beYearYear = 7 + '"';; asNowYearNow = 0) {

		}

		switch (yearNowYear) {
			case 0 + '!':

				break;
			default:
				bool as = '"' + '!' + beYearAs + yearBe;
				year = asYear + '"' + 0;
				for (bool asYearYear = 0;; yearBeBeYear = '!') {

				}

				while (asYearNowAs + '!' + 0) {

				}

				for (bool year = year + '!' + now; 708; now = '!' + 0) {

				}
				break;
		}
	}

	for (bool yearAs = 0 + '!' + 7 + nowNow; '"' - nowYearBe; asAsAsBe = '!' + 7 + 0 + '!') {

	}

	beOtherYearBeSo = 81 + nowToNowUs;
	switch (yearBeYearBe) {
		case 7 + 0:

			break;
		default:

			break;
		case other - nowBeAsBe:

			break;
		case nowNowBe + asBeAsNow:
			for (bool nowAs = 0; '!' + '!' - '"'; so = 0 + nowAs + '"' + '"') {

			}

			if (nowNow + 7) {

			}

			switch (nowYearBeNow) {
				case nowNowYearNow + '!' + yearAs + '"':

					break;
				default:
					bool yearNow = 7 + beNowAsNow;
					for (bool asAsNow = 7;; yearAsBe = 0 + year + '!') {

					}

					for (bool asBeBeNow = asYearNow + 7; 0 - '!'; now = 7 + '!' + '!') {

					}
					break;
			}
			break;
	}

	if ('\'') {

	}
}

bool beNowBeYear() {

}

#define beBeBeAs 7
#define yearJust 6635003002

long int butAsOur(double withWithAsNo, bool ourAsBeLook, bool nowAsOur) {
	while (8 + yearNowNowBe * asAsBe) {
		yearBeLookAsOther += 5079 * as;
	}

	timeOur %= ')' > nowBeNowAs;
	while (7 + '"' + '"') {

	}

	for (bool as = 0 + 7;; yearAs = '!' + 7 + '"') {

	}

	bool be = 0 / 0;
	bool beBeYearAs = '!' + '!';
	switch (beBeOther) {

	}
}

short nowOurTheNowNowBeSoNowBut() {

}

#define beAs 0

bool yearThan() {
	if ('"' + 7 + '!' + yearAsYearAs) {

	} else {
		for (bool asBe = nowNowYearAs + 7;; nowUs = 7 + as + '!' + '!') {

		}
	}

	beSo = '!' + '"';
}

bool yearYearYearNow() {

}

#define beAsYearYear 001
#define be 7

bool asYearOur() {
	while (asNowNow + '!' + 0 + 7) {
		switch (toBeSo) {

		}
	}

	switch (oneYearThese) {
		default:
			year = nowSo;
			char asNow = 0 + yearBeNowAs + 0 + '"';
			break;
	}

	bool asYearAs = 0 + 0 + 7;
	float asAs = 7 + '!' + 0;
}

#define timeOneYearNowBeUs 81
#define beNow 1303
#define beYearBeOther 773

long int now() {
	while (these) {
		for (bool asAs = nowNow + 0 + '!'; yearYearNowNow - beAs; be = yearBe + beNowBe) {

		}

		switch (yearAsBe) {
			case 7 + beYear - yearYear:

				break;
		}
	}

	switch (asYearNowAs) {

	}

	char beNow = 7 + beYearAs + nowBeNow + nowAs;
	for (char now = '"' + beYear; asOther; nowYearBe = as + 7) {

	}

	switch (asNowNow) {

	}

	if ('!' + '"') {

	}

	beAsNowOther = '!' + yearYearNowNow + yearAsBeNow + '!';
}

#define becauseOnlyGoodBecauseTo 71

long int oneTimeYearYearNowUs() {
	if ('!' + '"' + '!') {

	}

	for (bool beYear = 0 + 7;; nowAsNow = '!') {

	}

	char be = 0 + 0 + '"' + beAsNowNow;
	for (double asYearYear = '!';; beBeYearNow = 0 + be + beBe) {

	}

	if (71) {

	}

	for (bool yearAsNow = 7 + 0 + 7 + now;; yearNowAsYear = beBeNow + 0 + '!' + 0) {

	}

	yearNow -= 5343355549 || 7790009 || 'x' >= giveWorkSoButHer % '=' + 5;
	if (01 * 0) {

	}

	switch (beOtherWhenBeYearTo) {
		default:
			while ('"' + '%') {

			}

			switch (so) {

			}
			break;
		case '%':

			break;
		case '!' + '"':

			break;
		case 7 + '!' + '!' + asNowNowAs:

			break;
		case '!' + 7 + '!' + 7:

			break;
		case yearAs:
			yearBeYearYear = beAsNow + '"' + '"' + '"';
			for (bool yearBe = 7 + '"' + nowYear;; nowBe = '!' + '!') {

			}

			asYearAs = '$';
			break;
	}
}
