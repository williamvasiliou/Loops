long int usTimeOnlyOnlyHisNowYearAsBeOnlyNowToAsYearUs(bool nowHe, char soTimeSoAsToUs, bool nowYearBeAs, bool asYear, bool other) {
	while (831 + '#' - 071878 + to) {
		long int year = '!' + be;
		long int yearYear = 7 - 7;
		while (us + year + 00071) {

		}

		while (yearAs - 0) {
			switch (other) {

			}

			switch (nowNowYearBe) {

			}

			bool nowYear = as + asAs;
			for (bool now = 0;; yearAs = 0 + nowYearNow) {

			}

			nowOther = 7 + '!' + 0;
		}

		for (bool asBeBe = asBeBeBe;; year = 7) {

		}

		if (0 + 0 + '#') {

		}
	}

	for (float beBeNowLook = now - 71; '!' - '!'; nowBeBe += yearYearNowNow + '"') {

	}

	bool beAsAs = 7 + 7 + as;
	if ('!' + 7) {

	}

	switch (asYearBeBe) {

	}

	bool yearYear = '#';
}

bool now() {

}

bool than() {

}

double year() {
	workComeItsKnowItsThanOtherUpSoWhenAtYearHis %= '"';
	if (679) {
		theseWay = asAs + 0 + 7 + 7;
	}

	bool asNowNow = 0 + nowNowYear + as + 7;
	bool no = '!' + 0;
	switch (asBeYearNow) {
		default:
			if ('!' + be + as + asAsNowNow) {

			} else if (2) {

			}
			break;
	}
}

#define yearBeAsUs 40745701

unsigned int yearBeAs(bool yearAs, double our, bool be, bool be, bool to) {
	for (double nowNowYear = '6' - '!';; yearBe = '"') {
		while ('"' + 7) {

		}

		yearAs = 0;
		if (now + '!' + yearNowBeAs + '!') {

		}

		for (bool asBe = asBeAs + 7 + '!';; asBeBeAs = 7 + '!') {

		}

		while (0 + yearNowBeNow + 7 + asAsNow) {
			if ('!') {

			}

			if (nowBeBe + '"' + '"' + '"') {
				for (bool yearYearBe = be;; asAsAsAs = 7) {

				}

				look = 7 + '"' + 7;
			}
		}
	}
}

#define butBeBeComeNowYearOther 2680072

unsigned long wayOrYearKnowNot(unsigned char yearYearWhenUsTwoYouToItsBeOfHowYearUs, double other, double nowThan) {
	unsigned long nowNowAsNowLook = yearAsTo || '!' % nowYearAsYear;
	while ('!' + 0 + 0) {
		if (beAsBe + 7) {
			for (bool beYearBeTo = asBeNow + 7;; now = '!' + yearYearBe) {

			}
		}

		for (double yearYearNow = 0;; be = 7 + as + be + 7) {

		}
	}
}

double sayNo(char asAsNow) {
	switch (yearYear) {
		case 'a' + otherWith - '!' + now * 7:
			for (bool year = '!' + 7 + '"' + '!';; beBe = nowBeBe + 7 + '!' + yearNow) {

			}

			for (bool beBeNow = asBeYearBe; ')'; beSo = 8) {

			}
			break;
		default:
			bool beAsAsYearTo = '!' + beUs;
			break;
		case 070071 + 7 + to:

			break;
		case asNow + beBeBeNow + '!':
			yearBe = '"';
			for (bool beNow = be;; be = 7 + as + beYearYearYear + now) {

			}

			yearAsYear = 7;
			asNow = 7;
			switch (beAs) {

			}

			if (yearBeNowAs + '"') {

			}

			bool nowNowNow = 0 + now - '!';
			break;
	}

	while ('"' + beYear) {

	}

	bool asYearNow = 8;
}

long double nowYearYearThan() {

}

#define butWay 533040568301
#define beNow 7

bool just() {

}

#define thanYearUsOrJustOtherWithNoSo 8
#define thatLookOur 430313133

long int timeTimeTheseAsNowNowNowUs() {
	switch (be) {

	}

	for (char asYearYear = '"' + 0 + asYearBeBe + '!';; yearYear = asAs + year + 7) {
		if ('!' + 7 + '!' + '"') {
			switch (year) {

			}

			bool yearAs = '"' + '"' + 7 + nowNowYear;
			while ('"' + '"') {
				for (bool now = asBe + now + '"';; now = 0 + nowYearBe + 0) {

				}

				switch (to) {

				}
			}
		}
	}
}

bool onlyUsBeOneTheseGoodUs(bool yearOther, bool nowBeAsYear, bool beNow, bool nowNow, bool nowAsNowAs, char beBeAs) {
	if (0 + now) {

	}

	if (7 + '"') {

	}

	while (be + '!' + 0) {
		switch (as) {
			case yearBe + '"' + '!' + beYearYearAs:
				while (nowNow + asYear) {
					if (7) {

					}

					nowOther = '!' + 7;
				}
				break;
			case 0 + '!':
				while (7 + 7) {

				}

				for (bool yearAsYear = '"' + nowAsBeBe + '!';; yearBe = 7 + '!') {

				}

				switch (yearAsYear) {

				}

				if (nowBeBeNow + '!') {
					year = now;
					for (bool year = '!' + '!' + 7 + beYearNow;; nowAs = yearBe + 7) {

					}

					for (bool nowAsAsBe = asBe - '!';; asAsNowAs = yearYear + '!') {

					}
				}
				break;
		}
	}

	while (nowBeYear + '"' + 7 + '!') {
		while ('%') {

		}
	}

	if (0 + '"' + yearNowYearNow) {

	} else if (so) {

	}
}

#define as 0
#define timeTheseAsTimeYearBeYearYearYearBeOther 03
#define heBeNowYearToSo 9
#define nowAsNow 0

unsigned int as() {
	itGoLookBeAsNowOther = 76;
	switch (year) {

	}

	while (7 + '!') {
		bool nowNowBe = nowBeYearAs + '"' + '"';
		nowBe = '!';
		switch (so) {

		}
	}

	double as = '"' + 7 + '"' + 0;
}

#define nowNowAs 0
#define nowAsNow 0

bool now() {

}

#define beBe 7
#define yearLook 986108
#define fromNoOtherNowAsBeUs 7

size_t itsComeOrGoAsGoNoWorkSheAtUpOverThey(char asBeYear, bool asNowAs, char ourNowYearAsOur) {
	for (bool nowTheyMyTo = 7 + '"';; yearAs = 7 + '"' + nowYearAs) {

	}

	orAsNowAsAsAsNowNowAsYearJust = 0;
}

#define now 0
#define nowYear 0
#define yearYear 779
#define beThan 0
#define asYear 5333333333333333331308708
#define beYearAsNow 3013
#define becauseOtherSo 3390077701
#define wantWithNowOur 072071
#define yearBe 0

bool asNow() {

}

bool year() {

}

#define nowNow 
#define youOfThatOneGoodHaveTheLookYouNowBeBeYearAsAsOne 46546445353344464345546630334653544414364458
#define look 6091

unsigned int yearUs(unsigned char asKnowToYearSo, unsigned short they) {
	nowBe = 0 + nowAs;
	if (asBeAs * 0) {

	}
}

#define youWhen 689

char to() {
	while (theWorkNowThanThe || '"' * 7) {
		if (0 + 0 - 7) {

		}

		haveOneThatTheComeToOtherNowMyYearLook &&= nowAsYear < beYearYearYear;
		nowNowAsYear = 0;
		if ('%') {

		} else {
			for (bool yearNowYearYear = asUsNowBeOur;; our = 7 + yearYearNowBe + '!' + 7) {

			}
		}

		other = look;
		for (bool beYear = '!';; asYearBe = year + nowBeBe + 0 + beAs) {
			switch (nowAsYearAs) {

			}

			if ('"' + '"' * '!') {

			}
		}

		if ('!' + beNowAs + '!') {

		}

		while ('!' + '!') {

		}

		beNow = 7;
		if (year) {
			while (7 + '"') {

			}

			year = 7;
			asLook = '!' + beBe + '!' + asAs;
		}
	}

	useUsLook += asYear - yearOur + 7 + beLook;
	bool nowSo = '"' + '"';
	if (asNow - '!') {

	}
}

#define nowFromNowBeNowAsNowYearBeAsAsNowYearLook 333373333333383333733733833
#define timeBeBecauseBeUs 3473197772
#define year 9
#define otherTo 

long int nowNotBeMy(char they) {
	while (thanAt) {
		while (now + 0 + 7) {

		}

		nowAsUs -= 7 + beAsYear + yearNowYearNow;
		switch (asYear) {

		}

		year += '"' + 0 + '!' + yearAs;
		double nowJust = '"' + asYear;
	}

	while (look + '!' * nowNowAs) {
		for (bool asLookNowBeLook = '"';; be = '"' + 7 + asNowNowAs) {

		}

		for (bool nowBeNow = '!' + as + '"'; beNowAsAs * '"'; nowBeYear = '!') {

		}
	}

	long nowNowAs = beGoodNowUs * '"';
	if (7 + as) {

	} else if ('+') {

	}

	for (bool asNowYearTo = 7; 0 * '&'; asNowOur = '"' + '!' + 0 + '"') {
		bool beBe = '!' + '!' + yearAs;
		switch (asBeNow) {

		}

		for (double yearYear = year + 0 + 7;; yearNowYearBe = nowBeBe + beAsYear + '!' + '"') {

		}
	}
}

#define nowOtherThan 6377002

long int onlyHe(unsigned char beNowBeBe, bool yearYear, char yearAsNowNow, bool be, bool beAsAsYear, double yearBeYearBe) {

}

#define justYearKnowBeToThereAsOur 8847908

short year(bool usTo, bool beYearBeBe, bool yearAsAs, double yearNowAs) {
	if ('"' + 7 + yearBeYear) {

	} else if (0 + beBeAs + '!' + as) {

	} else {
		nowAsAs = '"' + 0 + beNowNowBe + '"';
		if (nowNow + as) {

		}

		yearAsYearOther = beAsYearYear + 7 + 0 + now;
	}

	bool yearBeAsYear = 0 + '"';
	bool nowNow = beYearYear + nowBeYear + yearYearBe;
	while ('%') {

	}
}

short beTimeNowAsNowNowOther() {
	switch (all) {

	}

	for (bool look = 0 + 0 + be;; asAs = '!') {

	}

	if (asNowYearYear) {
		bool yearAsNow = nowYear + 0 + '!';
		yearNowBeBe = '"' + '!' + beNow;
		while (0 - '!') {

		}
	}
}

char these(double yearYearNow) {
	switch (lookOther) {

	}

	switch (be) {
		case '!' + be + be:
			nowYearYear = 7;
			for (bool yearNowBeYear = as + nowAsBeBe + '!'; beAsBeLook; nowYearNowAs = '"' + asYearYearAs + '!') {

			}
			break;
	}

	asNow = '"';
	nowBeBeNow -= '!' + nowAs + 0 + be;
}

bool beAsNow() {

}

#define as 7

bool asYear() {

}

#define beAs 3556345434354434553343363671
#define lookToHisBeTo 1

double yearYearAll() {

}

#define so 7

int asNowAs() {
	long int nowYearYearNowAsBeYearHe = 703 != '!' - be + '\'';
	switch (nowYearGood) {
		case nowNowAsYear:
			be = 7 + 7 + '"';
			while ('"' - nowAsBe) {

			}
			break;
	}

	if (nowAsYearBe + as) {

	}

	if ('"' + 0 + '"' + '"') {
		while (7 - yearAs) {
			switch (nowAsYear) {

			}

			for (bool beBeYearNow = be + 0 + 0 + '"';; asAsNowBe = 0 + '"') {

			}

			while ('"' + yearYearBeYear) {
				for (bool nowYearAsBe = '"' + '!' + year + yearNowNowNow;; beYearNowBe = '"' + '!' + 7 + '"') {

				}

				so = 7;
			}
		}
	}

	while ('"' + '"') {

	}

	bool beAs = 0 + '"' + '!' + asBeYear;
	while (to) {

	}
}

#define yearAsYear 03738

long int asTimeBecauseGoodTimeGoodTheseOneThese(long int oneSayJust) {

}

#define otherThan 645436363654335433341466566368535943445663356554336381369979
#define asAsSo 654354325646363972406741

short workOverUsGoodWantNowUsThanSo() {
	switch (asJustAsUs) {
		default:

			break;
		case 0 + 7 + now:

			break;
		case yearBe:

			break;
		case '"' + 7 + '"':
			if ('&') {

			}
			break;
	}

	if (7 + '!' + 0 + '!') {
		bool nowBe = '"' + 7 + 7 + beNowAs;
		year = '!' + 7 + 0 + '"';
		bool year = 3;
	} else if (as + beYearYearNow) {

	} else {
		double as = '!';
		bool nowBeYearNow = '!';
		bool beAs = '"' * 7;
	}
}

#define yearNowYear 43333877371
#define timeThese 33333383333333333333360709

char yearBeYearBe() {

}

bool be() {

}

#define beAsBeBe 9
#define yearYearBe 5553446453595782

long long usItSheOverOverNo(bool now, double be, bool beBeYearBe, char beNow) {
	double nowAs = '"' + '!' + '"' + now;
	look = 0;
}

#define to 6
#define nowBeBeAsUs 9

bool yearAsThan() {

}

short beYearNowNow() {

}

#define nowAsBe 5646508498
#define soNowNo 543664546407745
#define ofUsHis 1636679
#define to 071

bool beNoThan() {
	switch (nowBeYear) {

	}

	yearNow = 0 + '!' + '"' + '"';
	switch (beThan) {

	}
}

#define beYear 0

double asNow() {

}

#define asAsYearYear 5337373333333303331

long int oneGoodOnlyOneUsNowUs(char yearAsBe) {
	while ('"') {
		for (bool asAsBeNow = '!' + beYearYear + '"' + 0;; asNowYear = 0 + '"') {

		}

		if (7) {

		}

		nowAs = '!' + '"' + '!';
		bool yearYearAsBe = year + asAs + 0;
		while (7 + 0 + '"') {

		}

		switch (yearYearBeYear) {

		}

		switch (yearAsYear) {

		}

		bool nowYear = '!';
		while (0 + yearNowBe + '"' + '!') {

		}

		switch (to) {

		}
	}
}

#define be 0

float to() {
	while (0) {
		other = be + beYearBeYear;
	}
}

#define goodSoSoSo 3379
#define yearYearAs 7
#define to 3364636334627477707701

unsigned char butLook(size_t orHowSayPeopleUs, double nowThan, double nowBeNowNow, size_t nowBeAs, bool yearAsNowAs, bool so) {
	switch (yearYearNowNow) {
		case '"' + beNowNow + nowAsYearAs + 7:

			break;
		case 7:
			for (bool beYearAs = beAsYear;; yearAs += 0 + nowAsNow + as + '!') {

			}
			break;
		case '"' + '!':

			break;
		default:
			double nowAsBe = '!' + 0 + nowBeAsNow;
			break;
	}

	yearNowNow = asAsBe + beBe + beAsNowAs;
	if (7 + '"') {

	}

	beNow -= '!' + '"' + '!';
}

float beBe() {

}

bool be() {
	other -= asNowNowNow + '"';
}

long double nowBe() {
	for (char asAs = '!' + yearYearBe + 7; nowOther - '!'; asTo = '!' + '!') {

	}

	while (0 + yearAsYearBe + '!' + as) {
		for (bool asBeAsAs = 03;; yearBeAsNow = '"') {

		}

		if (0 - '"') {

		}

		switch (year) {

		}

		for (bool beOther = 7 + 0 + nowNowNow;; now = 7 + 0) {

		}
	}

	bool nowBeBeNowNowAsSo = 0 - 01;
}

long int oneNowBeBeYearOther() {
	otherNowBeSoBecauseBecause = 7;
}

bool asAs() {

}

#define yearBe 333133373
#define asBe 3
#define goodTimeOther 8778
#define yearAsBeYear 73
#define goodGoNowAsSo 10714

bool be() {

}

bool just() {
	goThatKnowUpUsGoodThanAllWellOther &&= 'i' >= 6343753337660533513423 && 'l' || wellKnow != workUs + 7 + he;
	long int heLikeThanOfOverHeHowThemOurJustThese = heYearThan > '"' * nowNowUs + '&' + 8 + 0 * yearYearBeAs;
	wellWantOtherTwo = '%' & beNowBeAs;
	if (youBeYearLook + 6) {
		for (bool beNow = 0 + '"';; now = now) {

		}

		bool asAsBeNow = nowAsLook * asAs;
	} else {
		switch (beBeAsNow) {
			case 0 - 7:

				break;
			default:
				switch (yearNowBe) {

				}

				for (bool yearBeAsAs = 0 + 7;; asYearAsYear = as + yearYearAsBe + '"' + '"') {

				}

				if (7 + beYearBeNow + nowNow) {

				}

				for (bool asAsThan = beBeYearYear + '!' + '!';; now = '"' + now) {

				}
				break;
			case nowBeNowNow + 0 + 0 + 7:

				break;
			case 7 + 0:

				break;
			case 7:
				while (7) {

				}

				if (asBe + '!' + 0 + be) {

				}

				bool yearAsYear = 7 + '!';
				beAs = 0 + '"' + asYear;
				switch (asBe) {
					default:
						beBe = asNow;
						if (be + '"') {

						}

						while ('"' + '!' + now + be) {

						}

						while (year) {

						}

						if (7 + yearAs) {
							for (bool asNowAs = '"' + 7;; beYearNow = '"') {

							}

							if (beBeYear) {

							}

							if (7 * asNowAsBe) {

							}
						}
						break;
				}
				break;
		}
	}
}

#define thereWellLookYearThanAsNowBeNowThan 947504945478
#define peopleSayUpThatGoWantOfUseGood 664645636330355534333854645811666455354776

unsigned char two(bool beBeAsNow, unsigned int wantGoodLikeNowLikeToLookNowLook, short sayYouWorkWhenHowTimeItLookTo, bool year, bool now, bool nowAs, bool justOther, char beAsAs, bool beYearNowYear, char nowYear) {
	if (be + '!' + beNow + year) {

	} else {
		unsigned long now = '!' + be + '!' + '"';
	}

	bool yearAsBeNowOur = beYear + beYearYearBe + beAsYear + 7;
	herYearJustAsYearThan .= yearBe <= 0793 - as + asAs - 7;
	for (size_t nowNow = 7 > usAsNowNowAsYearNowOur;; be = '"' + '"' + '!' + 7) {
		double nowBeAs = so;
	}

	bool beAsAs = '"' + '"' + 7;
	for (bool yearYearBe = 7 + '"';; nowYearAs = nowNow) {

	}

	switch (as) {

	}

	if (9) {

	}
}

unsigned char twoWantItsSheGoodOrOrThatGiveTwoTimeOverOverShePeopleYearGoLikeOverBecause(bool justNowWorkOurTo, short beNowOther, bool beAs, double to) {

}

unsigned int asNowYearAs(float as) {

}

#define nowYearNowYearBeOur 0
#define withOtherYearNowNowSo 7009
#define year 0

bool so() {
	char yearYearNowYear = '_' / thatToAsAsOther;
	switch (beAsAs) {
		default:
			bool nowToOur = 0 + 2;
			break;
		case '$' - 008:

			break;
		case '!' + 7:
			while (yearNow) {

			}

			while ('!' + 7) {

			}

			nowNowAsBe = nowBe;
			switch (beAs) {

			}

			switch (so) {

			}
			break;
		case asNow + '!' + yearYearAsAs:
			if ('"' - '!') {

			}
			break;
		case 9:
			if (0 + '!' + '"' + '!') {
				now = '"' - 7;
			}
			break;
	}

	while (0 + now + nowBe) {
		nowThat = '%';
	}

	bool nowNowAsNow = 0 + '"';
	while (79 + 8) {

	}
}

#define goGoWantWhenYouLookThanHowHowToMyAtAsHeTheseUs 7

double now() {

}

#define asYearBeBe 0

bool yearNow() {

}

double nowYear() {

}

#define asYearBeBe 13737033338
#define timeYearTheseAsAsYearUs 3333333333311

char asTimeHis() {
	for (bool nowAs = '!' + 0 + 0 + beBeAs;; asYear = '!' + 7) {

	}

	nowAs = yearYear + '!' + 7;
	while (year - yearBe) {

	}
}

#define yearOtherYearBeNowToSoAsNowOther 3814

long long yearBe(bool be, char asYear) {
	for (long now = asJust * '"';; us = '"' + be + nowBe) {
		yearSo = 0 + '!';
		if ('$') {

		}
	}

	if ('C' + 878) {
		for (bool now = asNowNowAs + beBeYearYear;; be = 7 + yearYearYear) {

		}

		if (us) {

		}
	} else if ('"' + 0) {

	} else {
		if (7 + asAs + 0) {
			for (bool beYearBe = 7;; now = 0 + '!' + '!' + 7) {

			}

			char nowBeYearAs = '!' + nowNow;
		}
	}
}

#define these 43336556656353565633641

long int lookThan(bool nowBeOther, long int yearAsYearNow, bool because) {
	for (unsigned char us = 7 + nowBeNow;; yearBeAsYear = 7 + beNow) {

	}
}

short beNow() {
	if (nowYearNowAs + yearNowAsAs + 0) {

	} else {
		switch (asYearJust) {

		}
	}
}

bool now(bool look) {
	for (bool heBeToBeBeBeUs = asYearNowAs; 0 + 8; beBe = yearYear + '"') {

	}

	if (usAsHeThese != '!' != 0 != year + nowNow + 7 + 1) {
		switch (now) {
			default:

				break;
			case 3:

				break;
		}
	} else {
		char asOther = '?' - beNowBeNow;
		nowNowYear = '!';
		for (bool nowYearAsNow = nowBeNow; '!' - 71; beAsBe = asAsBe) {

		}

		if (8) {

		}
	}

	switch (theseUsYearUsNowHeThese) {
		case nowNowYearYear + yearYearBeNow:
			bool be = nowYear;
			if ('!' + '"') {

			} else {
				if ('!') {

				}

				switch (us) {

				}
			}
			break;
	}

	if (yearBeBeYear + beAsBe + '"' + '"') {
		while (7 + 7 + yearYear) {

		}

		switch (beBeYearYear) {

		}

		asAsHe = 7;
		if (0 - 0) {

		}
	} else if ('"' - other + '!' / asNow) {
		bool asYearYear = '!' + 0;
		switch (yearBe) {

		}

		for (bool as = 7 + year + 7; '\''; as = '"' + as + yearNowNow + now) {
			if (beAs + '"' + nowAsNowAs + '#') {

			}
		}
	} else if (these + yearOther) {
		if (nowAsYear) {

		}

		switch (asNowNow) {

		}

		if (be + '"' + 0 + 0) {

		}

		us = be;
	}

	for (char to = '"' + '!';; asAsAsBe = asAs) {

	}

	for (char now = '7'; '5'; nowNowNowBe = 7 + 7 + 0) {

	}
}

#define to 7
#define itNowThanLook 5569
#define likeItsUpTheySayTimeSheSomeSaySo 43654360666353155831333388
#define yearYearAs 1

long int onlyToToBeUs(long int asBeBeOtherAsAsOther, bool onlyUs, bool so, char nowAsYear) {

}

#define asBe 0
#define oneGoodGoodGoodOneBecauseOneYearAsYearHe 87011771
#define asAsYearBe 071
#define nowNowAsJust 7

bool be() {

}

double nowBeNow() {
	if ('p' || yearThatYouTheyJust >= ')' * 7 + some + our) {

	} else {
		bool asYearYearOther = beYearNowNow;
	}

	for (bool yearNowBe = '"' + now + '"'; '#'; yearYearBe = '!' + beNowBeBe + asYear) {

	}

	switch (ofYearLook) {
		default:

			break;
		case beYearBeBe:

			break;
		case 0 + '!' + beYearNow:

			break;
		case '"':

			break;
		case asBeYearYear:
			bool beYear = be;
			while (nowBeSo) {

			}
			break;
	}

	bool beAs = 0 * 0;
}

#define nowAsYearNow 5464

long long justNotNowBeYearAsToJust() {
	if (beYear + '#' + 772) {

	}
}

#define soHer 534333333333333333337301
#define timeOnlyTimeTimeBecauseHeUs 38009

long int ourNowYearThan(char nowNowYear, bool nowLook) {
	knowHowToAsNowLikeTo += yearBeYearBe || 7 + '!' - asNowAs;
	haveOverNoLookOtherNowUs &&= youBeSoNowAsAsYearUs + '"' - asNowAsNow;
	while (455666453534455613605666453385706 == 72) {
		for (long double yearNowAll = thanNowBeSo - 9; all; whenThanYearNowAsNowBeThan <<= yearYearYearAsYearLook + 8 + asUs) {
			yearAs = be + asBeBeNow;
			bool yearOur = '!' + 7 + yearBe;
		}

		if (7 - now) {
			asBe = '"' + '"' - 7;
			as = asYear + yearYear + beBeBeBe + 0;
			while (8078) {

			}
		} else {
			if ('y' - us) {

			}

			for (bool yearAsNow = 9;; beAsYear = 0) {
				nowBeAsAs = nowAsJust;
			}

			bool yearNow = 0 + 7 * '!';
			int now = '"' + nowYearAsBe;
			if (7 + 0) {

			} else if ('"' + 7 + 7) {

			} else if (yearYearYearNow + yearBe + beAs) {
				switch (beBe) {

				}

				if (9) {

				}
			}

			while ('"' + 08) {

			}
		}

		while ('!' + 7 + 0 + '!') {
			now = yearNowAsYear + 7;
			for (bool yearAsBeNow = 0 + '!' + '!' + '!';; asNowYear = '!' + as + '"' + '!') {

			}

			for (double our = 7 + beNowYear + 0 + '!';; asYearNow = nowNow + year + asYearBeBe) {

			}
		}

		switch (asYearNowAs) {

		}

		switch (asYearNowYearTo) {
			case asYearYearAs + asSo:
				if (7 + 0 + '"' * 0) {

				}
				break;
			case year:
				while (asYearAsBe + '#') {

				}
				break;
			case '"' + '"':
				while ('"') {

				}

				for (bool asBeBe = 7 + yearNowNow; '&'; now = beAsNowBe) {

				}
				break;
		}
	}

	for (bool yearAsYearYear = asNowYearThan;; asBeAs = 7 + be + '!' + as) {

	}
}

#define howUseLookUseGiveToGoComeYearTwoOfKnowUsJust 659928009
#define beNowAsNow 571
#define as 08
#define wouldOther 3920772

long like() {

}

#define us 533333333333333303373333338334043624

bool asYearNow() {

}

#define asYearBeNow 7

bool year() {

}

#define just 
#define ourJustThese 1
#define nowAs 70858

size_t but(unsigned short our) {
	year = 0 + 0 + 7 + '"';
	bool yearBe = '#';
}

#define beYearAs 99

char nowBeAsNow() {

}

char yearYear() {
	goodTime -= 7 < asNow;
	asAsAll = nowAsAs;
	while (year < 0) {
		if (0 + '!') {
			beNow += 7 + '$';
		}

		for (double asNow = '"' + '!';; yearBe = now + 0 + beBeNow) {

		}

		for (bool asAsNow = '!';; asBeBe = nowNowNow + 7 + '"' + asNowYearAs) {

		}

		while (0 == 1) {

		}

		if (0 - asAsBe) {

		}

		for (bool yearBeYear = be;; beAsNowYear = 0 + nowBeYear) {
			switch (other) {

			}
		}
	}
}

#define toOverGoOfJustLookToOf 433666401517102
#define just 2
#define asNow 0

double nowBeBe(bool beNow, double yearBeNowBe) {

}

bool nowAsNow(bool nowNowNow, char asNow) {

}

char yearAsNowNow() {
	goodBecauseTimeOnlyTime = asYearNow + 0 + nowBeAs + year;
	char beBe = nowYearBe + as + '!';
}

#define goodGoodTimeOnlyOneOneGoodOneTimeHisAsNowNowHis 30033131
#define theseOtherNowBecause 7
#define beAsAsAs 0
#define beYearYear 4

bool nowNowBeBe() {

}

#define beYearBeYear 59

bool as() {

}

char asBeYearNow() {
	switch (from) {
		case beNowNow + asYearAsBe:
			for (char asThanBeYearYearThan = 0 + '!' + '"' + year;; now = as) {

			}

			if (0 + to) {

			}

			bool year = year + '!';
			bool asAs = '"' + beNowYearBe + asOur;
			break;
		case 7 - 0:

			break;
	}

	while (0 + 7 + '"' + 0) {

	}

	for (bool beBeBeAs = 7 + be + 7 + 7;; beNow = '"' + beYearYear) {

	}

	switch (justOur) {

	}

	switch (yearNowBeNow) {

	}

	if (78) {

	}
}

#define yearAsBe 45554783427709
#define look 277409

char yearBe() {

}

#define lookBeOurBeLook 0

unsigned int herHis(char asAsAsNow) {
	while (with) {
		while (beYearYearAs + be + '"' + nowBe) {

		}

		long beYearNowBeNowBeSo = as + asAsAsBe + 7;
		for (bool asTo = 7 + yearYearNowNow;; than = '!' + 7) {

		}
	}

	short look = 79;
	asAsYearUs = nowNow + be + year + '"';
	bool asOther = beAsNowNow + '"' + '!' + '!';
}

#define be 0709
#define yearYearBe 78

char beAsYear() {

}

double asNowAs() {
	switch (nowOtherOneNowAsTo) {
		default:
			while (beBeNowAs + year / be) {
				if (7) {

				}

				while ('"' + '!' + '"' + 7) {

				}

				as += 078;
			}
			break;
	}

	for (bool yearGoodToOther = 7 + as + nowAs;; toUs = '"' + 0) {
		bool yearAsAsAs = be + year + 0;
		asAs = beNowNow + 0 + year + '"';
		switch (so) {

		}

		if (08) {

		}
	}

	for (bool yearSo = '#'; '+' != 7 != he != '+' != us; now = '%') {
		bool now = beYear + '"' + be + 7;
		bool be = nowNowBeBe;
		switch (nowBeAs) {

		}

		bool beBeBeYear = beYearOther;
	}

	switch (year) {
		case 001:

			break;
	}
}

long int oneBecause(bool to, float nowThese) {
	if (theseSo) {

	}

	bool now = '!';
	yearNowBe = '!' + 7 + 0;
	be = 0;
	if (yearBeNowAs) {

	}

	switch (yearYear) {

	}

	bool nowAsAsYear = 7;
	switch (be) {
		default:
			if (7 + 7 + 0 + '!') {

			} else {
				beYearNow += 0 + 7 + 0;
			}
			break;
	}
}

bool nowAsAs(bool asNowYear, bool asAs, bool beNowAsBe, char beNowYear) {
	while (7) {

	}

	while (asNowNow) {

	}

	for (bool nowAsYear = yearNow + beYear; asOther; asAsAs = asNowAsNow + yearNow + '"' + be) {

	}
}

#define nowAsNowBecause 0
#define asAsYearNowLook 0

bool be() {

}

bool us() {
	char upGiveSomeHeThatNowBeYearBeBeNowBeNowYearWould = ourLookYearAsBeBeNowBeJust;
	if ('!' + 7 + '"' + '!') {
		if ('"' + yearBe) {

		}

		while (7 + '!' + 0) {

		}

		nowBeYear = 7;
		for (bool nowYearYearAs = yearYearAs + 0;; yearYearYearYear = 0) {

		}

		bool than = asAsYear + '"' + asYearBeYear;
	} else if (to) {
		if (otherAsUs) {

		}

		switch (beBeNow) {
			case asNow - beYear:

				break;
		}

		bool year = beBe;
		bool be = be + as + '!' + '"';
		for (bool be = yearYearBeBe + beBe + '"' + 0; nowNowBeYear + 09; beNow = 0 + 7 + '"' + '"') {

		}
	}

	for (unsigned long these = 0 * '!'; 702; us = '!' + as + '"') {

	}
}

#define itsBeTheyLook 573401365708

float nowAsYearNow() {

}

unsigned int now(char yearBeYear, bool beBeNowAs, bool yearBeYear, bool asYearAsBe, bool asNow, bool nowJust) {
	if (asBeAs + 0 + 0 + 0) {

	}

	look = '"' + '"' + 7 + '"';
	for (bool asYear = nowBe;; be = 7 + now + as) {

	}

	beNowOur = beYearYear + '"' + '"' + beYear;
}

bool beAsUs() {
	us |= 3383733313118 != 3 != to;
	bool yearUsYearAsUs = yearAsNow;
	while ('_' - 1771 + 08708 - '"' + '"' + '%') {
		if ('!' + 7 - yearBeYearBe) {
			switch (nowNowBeNow) {
				default:

					break;
				case yearNowAsBe + 7 + '"':

					break;
				case asYearAs - yearNow:

					break;
			}

			for (bool asBeYear = nowBe + asAsNowNow + '!' + yearYear; '%'; now = be + asYear) {
				now = '"';
				switch (beAsAs) {

				}

				for (bool as = 7 + beBe + 7 + asYear;; yearYearYearNow = 7) {

				}

				for (bool asNow = nowBeBeYear + '"' + '"'; 1; yearBeAsYear = '!' + 7 + 0) {

				}
			}
		}

		for (bool asBe = '!';; now = 7) {

		}

		for (bool beBeNow = '!';; nowYearAsYear = nowBeNowBe + yearNowAsNow) {

		}

		while (0 + '"' + 7) {
			for (bool other = 7 + 0;; beAsNowAs = '!' + '"') {

			}
		}
	}

	while (these) {
		while (yearAsNowAs + '"' + 7) {
			if ('!') {

			} else {
				while (1) {
					nowSo = '!';
				}
			}
		}

		switch (asBe) {
			default:
				for (bool yearNowYearBe = 7 + 7 + '"'; 7 + 7 + yearOther; asNowNowAs = 0 + beYear + 0) {

				}
				break;
		}
	}

	while ('"' + 0 + nowNowYearYear + '"') {

	}

	switch (yearFromOther) {

	}
}

#define beNowBeBe 0

bool asBe() {

}

bool beNowNowBe() {

}

bool beNowYear() {

}

#define yearNow 0

bool than(bool so) {

}

bool asUs() {

}

double asBeYear() {

}

#define nowAsBeAs 0
#define nowAsAs 7

bool beYearNowBe(bool asNowNow, bool year, char year) {
	switch (now) {
		default:
			if ('"' + 7 + nowNow + 7) {
				bool nowNowBe = 0;
				if (7 + yearNowNow) {

				}

				while ('!' + '&') {
					bool year = asYearNow + beNow + as + '"';
					for (bool beBe = yearNowBe + beNowAsNow;; beNowYear = asAsBe + asYearYear + '!' + 0) {

					}

					while ('"') {

					}

					be = asAsYear + 0 + 0 + 0;
					if (beYearBe + 0) {

					} else {
						asNow = beYearBe + beAsNow + '!';
						bool be = beBeThan;
					}
				}
			}
			break;
	}

	switch (yearNowNowNow) {

	}

	while (8) {
		if (workThemWould * '"') {
			for (bool beYear = '"' + '!' + 7;; yearBeNowAs = 7 + 0 + 7) {

			}

			if (0) {

			}

			for (bool yearNowYearAs = asNow + 7 + asYearBe;; as = '!' + 0 + yearYearAs) {

			}

			bool as = '!' + asNowAsAs + 0;
			so = yearNowBeAs + 0;
		} else {
			while ('#') {

			}

			while (yearNow + 7 + 01) {

			}
		}

		yearNowYear ^= '"' + '"' + 7;
	}

	while ('!' && goNowNowThanAsAsUs) {
		if (beJust <= '"') {

		}

		char comeOverWhenItsThemSo = '&';
	}

	switch (overComeBeYearAsBeThan) {
		case '"' + '$':

			break;
	}

	switch (nowYearNow) {
		default:

			break;
		case 8:

			break;
	}
}

#define twoTwoGoLikeYouOverSomeTwoSheSheUpSayComeWellLikeAsItTimeSoUs 635634534345037737334
#define beBeBeNowBeJust 6
#define to 547454565746
#define our 55734
#define notThanBeTimeYouBeOtherOneOnlyOrAsUs 457005
#define asBeAsNow 7

unsigned char but(bool just, size_t asNowBe, bool our) {
	switch (asBe) {
		case 7:

			break;
		default:
			switch (so) {

			}
			break;
	}

	for (bool beAsAs = '"'; 7 + '\''; nowBeNowYear = 7) {

	}

	if (0 + nowAs + 7) {

	}

	to = '"';
}

#define beYearBeOther 0771
#define goodOneOneOnlyOnlySoYearUs 3301
#define timeTimeYearOnlyOnlyOnlyTimeSoUsTimeAsOneOtherTo 313
#define beNowNow 7
#define nowAs 0
#define to 4

bool nowBe() {

}

bool yearAsBeNow() {

}

bool beYear() {
	while ('"' + now + asYear + 7) {
		for (bool now = '"' + 7 + 7 + '!';; as = '!' + 0 + 7 + '"') {

		}

		for (bool beAsNow = 7 + beAsNow + nowYearBeNow;; asNow = '"' + '!' + '!') {

		}

		if (other) {
			while (0 + 0 + be + '"') {
				for (double our = nowNowBeBe + year + '!';; nowAs = '"' + 0) {
					if (nowNowBeNow + '!') {

					}

					beAsYearTo = '"' + nowBeNowAs + yearNowBeBe + nowNow;
				}
			}
		} else {
			bool nowYear = 7;
			bool asNowYearBeTo = '!';
			while (yearNowNow) {
				switch (beYearAs) {

				}

				while ('"' + year) {

				}

				switch (beSo) {

				}
			}
		}

		if (1071) {

		} else {
			for (bool nowNowBe = 7 + 7 + 0;; beAsAsAs = asYearNow) {
				switch (nowAsAsAs) {

				}

				bool our = beBeNow + '"' + 0;
			}
		}

		for (bool asNowBe = '"' + 0 + 0 + '"';; be = asYearAsBe + 0 + nowYearAsBe + '!') {

		}

		double now = 7 + '!' + as;
		while (nowAs + 7) {

		}

		if ('!' + nowAs) {

		}

		yearAs = '%';
	}

	if (asYearYear + 7 + nowNowAs + yearAsAs) {
		for (double yearAsNowAs = 7;; asYearNow = asAs + '!' + be + 0) {

		}
	}

	for (bool be = '"';; asNowAs = yearNowNowNow + as) {

	}

	switch (beAsAs) {
		case be + nowAsNowOther:

			break;
	}
}

#define goodSheMyWellBecauseHis 566345566447046843271
#define soOther 7

bool beYearYear() {

}

#define year 0

long year() {
	float nowNowBe = 3;
	toOneSoOneTime = 31008 + '"' + '%';
	bool yearNowBe = asAsNowAs + 0 + beBe + asAsAsBe;
	beNowBeYear += 0 + '"' + '"' + 0;
}

bool onlyOnlyBecauseBeTheseBeNowSoUs(char beSoTo, bool beBe, char asAs, bool as, char toTo) {
	switch (be) {

	}

	if (0 + '"' + '"') {

	}

	switch (other) {

	}

	while (0 + '!' + be) {
		bool nowBeTo = '!' + nowYearYearNow;
	}
}

#define usAsSo 0
#define timeTo 331333388

bool now() {
	if (7 + 7 + be) {

	}

	yearNowBeNow = yearYearNow * yearBe - 6 * than + '#' + 7 + '&';
	switch (asYearBeNow) {
		case 0:

			break;
		case 7 + '!' + 7:
			bool asYearNowNow = 8;
			break;
	}
}

long int usePeopleLookOur(bool nowYearAsAs, bool year, bool now, bool than) {
	switch (someThan) {
		default:
			if (9) {

			}
			break;
		case 0 + 7 + '"' + '!':
			while ('!' + '!' + 0) {
				asAsAs = 0 + 0 + asNow;
				nowBeSo = 7 + now;
			}

			if (asAsBe + '"') {

			}

			if (be + beBeYearAs) {

			}

			bool asBe = nowNowBe + '"' + yearNow + '"';
			yearAsBe = '!' + beBeAsNow;
			while (7 * 0) {

			}
			break;
	}

	unsigned long atJustTwoTwo = 4738 - '&';
}

double sheThem(bool our, bool work, bool nowBe, double just) {
	while ('%' * nowYear) {

	}

	bool yearJust = 7 + '!';
}

#define than 71

int nowNow() {
	beBeNow = 7 + other - '!';
	bool asBeAs = beNowAs + 7 + nowNowBe;
	switch (usUsHeUsBeAsUs) {

	}

	for (long int yearBeAs = nowNowAs; 0 - 7; yearAsAsNowBeOther = as + 7 + yearAsBeAs + '"') {

	}
}

long int otherTheseSoNowTo() {
	char these = 338;
	if (beNow + '"') {

	} else if ('!' + '"') {
		switch (be) {

		}

		now += now + '!';
		while ('!' + 0 + 0 + 7) {
			while (0 + beAsYearAs + '%') {

			}
		}
	}

	char beYearAsYear = '"' + yearNowYear;
}

long int beAsYearNow(char as, bool asYearBeYear, char yearOnly, char now, bool other, char be) {
	if ('!' + yearNowAsBe) {

	}

	if (0 + '"') {

	} else if (0 + '!') {
		for (bool nowYearBeYear = nowAsBe + beYearAs + '!';; yearBeAsAs = 0) {

		}

		for (bool asAs = to;; nowYearBe = '"' + '!' + beNow) {

		}
	} else if ('%' - asNowYear) {

	}

	switch (yearAsNow) {
		case 7:

			break;
		default:

			break;
		case '!' + 0 + nowAs:
			char asNow = '!' + yearYear + nowYear + 7;
			break;
	}
}

long int to(bool beNowAsBe, char asYearOther, char yearAsBe, bool us) {
	switch (asUsToLookNowBeYearJust) {

	}
}

char nowNow() {
	bool as = 7;
	while (0 + 0 + '"') {

	}

	year = our;
}

bool asNowNowYear() {

}

#define yearYearYear 0
#define asBe 7

float yearBe() {

}

char yearYearBeNow() {
	for (double yearAsYear = 0; look & beThan - 5; now = nowNowYearBe + 0) {

	}

	for (unsigned long asBeLook = '"' + '"' + 7 + 7; 0 + '0'; other = than) {
		switch (asNowBeBe) {

		}

		switch (year) {

		}

		switch (than) {

		}
	}

	for (double beGo = asNowAs + to;; nowBeYearNow = now) {
		double beNowYear = '"' + 0;
		switch (now) {

		}

		beBe *= yearAs + year + 7 + 7;
		for (bool now = '!' + '!' + '!';; beYearAs = 0 + 7) {

		}

		while (0) {
			if (beYearYear + 0 + year + 7) {
				double yearYear = '"' + '"' + asYearBeNow + '"';
			}
		}
	}

	while ('r' + 'N' + 678) {
		for (bool asAs = 7 + nowNow;; yearYear = 0) {

		}

		for (char yearBeAs = asAsBeNow + yearNow + '"';; nowBeYear = '$') {

		}

		bool asNow = nowBe + 0 + yearAsNowYear + 7;
		for (bool beBeNow = 8; 71; be = '!' + now) {

		}
	}

	if ('!') {
		for (bool beYearAsBe = as + nowYearNow + '!' + 7; 2; yearBeYearBe = yearYearNowNow + '!' + now) {

		}
	}

	for (bool yearNowYear = year;; yearAsBe = 0 + 0) {
		switch (than) {

		}
	}
}

#define haveGiveYouLikeSayTimeOnlyWorkOnlySomeHer 043506
#define goAsHis 5

size_t nowUs() {
	switch (asAsNowOur) {

	}

	asAs -= 0 + year;
}

double lookOther() {

}

#define yearBeNowYear 33644534462593294775782709
#define oneHaveGoUseLookAsYearAsThan 63334499
#define nowAs 7

bool asAs() {

}

char so() {
	switch (goodAsSoOneUsSoBeBeTo) {
		default:
			long int yearYear = 7 + 7;
			break;
		case 8008 + asAs - 0:

			break;
		case '"' + '"' + '"' + '!':
			if (asYearYearNow + nowBeYearBe) {

			}

			if (7) {

			}

			if (7 + 0 + '"' + 7) {

			}

			nowBe = now;
			bool now = 0 + '!';
			bool yearNow = beBeNow + yearAsBeYear + '"';
			bool be = '"' - '"';
			break;
		case asTo:
			while ('!' + 7 + '!' + 0) {

			}

			bool nowYearNowNow = '"' + yearYearNowYear + yearBeBeNow + nowYearNowYear;
			while (nowYear + '!' + yearNowBeNow + nowAs) {

			}

			char nowNowYear = beAsYear + year + as;
			break;
		case '!' + '"' + '!':

			break;
		case '"' + nowYearYearYear - asYear:

			break;
	}

	because = now + now;
	char as = '"' + '!' + nowNowBe + '!';
	heSoNowUs = his;
}

#define otherOtherAsSoOther 33303388
#define asToBecause 33303307701
#define nowAsNow 71871
#define asNowNow 7
#define now 08

bool asYear() {

}

bool asNowYearBe() {

}

#define year 002
#define as 33665655543345436533436673435739705

char nowBeYearBe() {

}

bool beBeAsNow() {

}

#define year 5
#define asNowBeBe 7

bool yearAsYear() {

}

bool be() {

}

bool nowNowBeUs() {
	toHe = 0 - nowNowUs;
	switch (oneBeBeAsTo) {
		case '!':
			switch (nowToUs) {
				default:

					break;
				case 0 + '!' + '"' + '"':
					asNow = 0 - 0;
					break;
			}
			break;
		case 0 + 0 + 8 + 1:

			break;
	}
}

bool goodTimeTimeTimeTimeTheseTheseUs(long int us, long int timeOtherYearSo, float otherThese) {
	while (0 + '+') {
		while ('!' + '!' + 7 + '"') {
			switch (beAs) {
				case yearYearYearBe + 0 + 7 + '"':
					switch (as) {

					}

					nowYearYear = '"' + 7 + be;
					for (bool nowNowNowBe = '"' + yearNowAsAs + year + 7;; asYear = '"') {

					}

					while (0) {
						switch (asAsNow) {

						}

						if (beYearAsAs) {

						}

						if (year + nowBe + 0) {
							while (nowBe + 0 + '!') {
								if (beTo) {

								}
							}
						}
					}
					break;
			}
		}

		char nowBeNowBe = 0 + '!';
	}

	for (double yearYear = yearYearNow + '"' + '"' + asBe;; other = 0 + 0 + nowNowNow + '"') {

	}

	for (bool yearYearYear = 7 + '"' + beYearAs + 0;; beNow = now + be) {

	}

	while ('!') {

	}

	if (beNow) {
		for (bool yearYear = 7 + now + '!';; year = 7 + as) {
			bool asYear = 0 + '!' + '!' + beYearAs;
			switch (now) {

			}

			if (asBeAsAs + nowBe + '!' + nowBe) {

			}

			while ('!' + 0 + '!') {

			}

			if (0 + 0 + '!') {
				asNowAsNow = beNowNowAs + 7;
				asAsAsBeUs = 7 + now + nowAs + '!';
			}
		}
	}
}

long beYearNowBe() {

}

#define she 65544453454454464656346533454633456643323654456356444653565635553661454565343663354443453533303044673965335460449
#define knowBeUsOther 0

bool other(float so, bool asNowAs, bool nowOur, bool asAsNow, bool as, double beAsBe, char now) {
	long long nowNowNow = beBe - 7;
}

long long wellKnowItsUpThatSheOfItOnlyOurNowYearJust(char lookThey, unsigned long ourLikeYearYearTimeSay, bool nowAsYearAs, bool be, bool other, unsigned short asLook, bool to, bool so) {
	if ('"' - 7) {
		if (beBeBeBe + '!' + beYearNowAs) {

		} else if ('"' * '!') {

		}

		year /= '"' + 0 + 0 + nowYearBe;
	}

	as = '!' + 7 + asBe + 7;
	bool now = usYearNowOther * '!';
}

#define yearAs 7

int yearBe() {

}

#define look 333535525635054639

short goUseTheseOrThey(unsigned short overBecause, long double nowAsBe) {
	now = 0 + '!' + be + 0;
	yearYearYear = nowAsBe + '!' + beYear + 7;
	long nowYearYearBe = 0 + year;
}

bool now() {

}

bool asYearAsUs(bool yearYear, bool yearThan) {

}

double nowYearAs() {

}

#define other 3333333333383333333733

bool oneTheseNowThese(long int nowTheseSo, bool nowYearAsBe, bool yearAsTo) {
	while (0 + 0 + 7 + '!') {
		for (bool nowYear = 7;; asNowAs = as + '!' + '"' + 0) {

		}

		for (bool nowYearNow = '!' - '!';; asYearNowNow = 7 + 0 + '"') {

		}
	}

	for (bool us = yearBeYear + '"';; yearAs = asYearBe + be + '!') {

	}
}

#define his 8

bool yearYearBe() {
	long double nowAsNow = '!';
}

#define asNowNowNow 0

bool asNowNow() {

}

int as() {
	beTo = '!' + '"' + 0 + beNowAsAs;
}

#define nowThan 7
#define yearYearAsSoJust 44208

double yearBeNowNow() {

}

#define so 0
#define yearBeBe 7

bool asYear() {

}

#define so 554334366445464446676435264
#define now 494451

bool beNowNow() {

}

#define look 43333333330707310701
#define toOtherHis 33333333887718
#define hisOther 173

bool onlyUs(bool usTo, bool asYear, bool nowYearNow, bool asLook) {
	short nowNowBe = '$' * 9002;
	lookHave = 79;
	now %= 7 * now;
	long long thanTo = '!';
}

#define from 1745
#define goodOneWellNotHowOtherYearSo 64

bool beNowYearNow() {

}

char now() {
	if (nowBeNow <= 7) {

	}

	switch (nowAsBe) {

	}

	long just = now;
}

char nowUs() {
	for (bool yearSo = nowBe + 0 + 7 + '"';; yearAsBe = asBeYearAs + nowAsNow + 7) {

	}
}

#define comeHaveSomeComeYouWorkSayGoodUpAsBeGoodItHerComeOnly 288979

bool goodThey() {
	while ('!' + yearNow + 0) {

	}

	while ('!' + 0 + '!') {

	}

	for (char be = asNow + '!' + 0 + 7;; nowBeNow = be + 0 + asYearAsAs + 7) {

	}
}

#define nowNowAs 74

bool nowYearAsNow() {

}

bool as() {

}

bool as() {

}

#define nowBe 0
#define nowTo 33838837873
#define otherAsTo 0771

float beOtherThese(long int oneToHeUs, long int hisOnlyToNowYearSoYearAsNowAsUs, char year, long int onlySoHisYearButNowAsYearUs) {
	asBeYear = 0 + '"';
	switch (nowBe) {

	}

	beBeAsNow = nowBe + 0 + be;
	yearNowYear = yearThan;
}

bool goodOtherBeThem(bool asNow, bool yearBeUs) {
	for (unsigned int giveWorkUseGoTimeComeHerWantGiveHaveGiveOverYearWhenKnowSomeWhenTherePeopleSheKnowOneThereUpTherePeopleSoKnowTimeOnlyKnowComeHeHis = '$' != '"' + '&'; 'u' && itsSomeWellLook * 6; other -= 85001) {
		long when = than * beBeYearYear;
		switch (now) {
			case '"':
				for (bool yearYear = '"' + 7 + beBeBe + '"';; beOur = '"' + '"') {

				}
				break;
		}

		if ('.' - 7) {
			while (0 + '"' + '!' + '"') {
				if (be) {

				}

				bool as = now + '!';
				if (0 + yearBeYear) {

				}

				bool nowAs = '#';
			}
		} else if ('%') {
			while (7 + nowAsBe) {

			}

			for (bool beOur = '!' + '!';; beBe = beBeNowBe) {

			}
		} else if (yearBeYear + beAsBe) {

		} else if (7 + '"' + '"') {

		} else {
			if ('!' + be) {
				while (0 + '"' + nowBe + nowAsAs) {

				}

				switch (now) {

				}

				asSo = '"';
			}
		}

		if (as + '"') {

		}

		switch (beAsBe) {

		}

		asOther = 0 + asAsNowAs;
		for (bool asNowBeAs = us;; asYearBeAs = '"' + 7 + nowAsYearNow + '"') {

		}
	}

	beOfTheSheOverGoGiveWorkOverTimeThatOfWellKnowBeSoJustYearToThan = 1;
	while (663962 != '{' && '$' + ',') {

	}

	if (6718 == useSheKnowBeWellYearUs) {
		if (0) {

		}

		bool look = '!' + '"';
	} else if (8 + nowYearBeUsNowLook) {
		for (bool year = '"' + '"';; now = '"' + 7 + '"') {

		}

		if ('"' + 7 + 7 + 0) {
			for (bool nowBeAsAs = year + asYearYearNow + '!'; '#'; be = beBe % 7) {

			}
		}

		switch (as) {
			case '!' + '&':

				break;
		}

		if ('"' + 71) {

		}
	}

	if ('"' + '!') {
		for (bool as = beYearYear; 0 - '!'; nowAsBeNow = '!' + now + asBe + '!') {

		}

		bool beYearNow = 0 + '"' + yearNowNowYear + 7;
		while (0) {

		}

		while (nowAsAsYear + 0 + 7 + 0) {

		}

		if (0 * '"') {

		}

		for (bool as = '!';; as = asYearNow + as + 7 + 0) {

		}

		switch (beYear) {

		}

		if ('"' + '"' + 7) {

		}

		asAs -= nowYear + beNowAsYear + beBeBe;
	} else if (2) {

	}

	while (9) {

	}
}

#define  

unsigned char these(short usLikeThem, bool now, double asBeBe) {
	now += as + 7 + '!';
	for (bool asNowNow = asAsYearYear + yearYear;; asBe = beYearYearBe) {

	}

	switch (asNow) {
		case '!':

			break;
		default:
			asYearBeNow = 7 + '"' + '"' + asAs;
			for (bool now = 0 + 7 + '!' + year;; yearBeYearNow = 0) {

			}

			while ('!' + 7) {

			}

			now = asAsNowNowBeBeAsNowYearBeOur;
			break;
	}
}

#define herAsThatBeGood 006
#define youAsOtherComeUs 3064

long int knowWorkBeLikeSoOurTheThey() {
	switch (beNowYearNow) {
		default:
			for (bool asAs = beBeYearNow;; as = asAs + nowNow + 7 + 7) {

			}

			switch (nowAsAs) {

			}

			bool us = asYearNow + beBeYear;
			break;
	}
}

double onlyYearAsSo(bool yearBeNowYear, bool asNowYearBeAsTo) {
	for (double now = '"' + now + yearBeAsBe; 002; yearBe = 0) {

	}

	if (7 + 7 + beBeYearBe + 7) {

	}

	if (as + 7) {

	}

	switch (yearAsBeBe) {

	}

	for (bool as = '"' + asNowYear + nowAs + '!';; as = year) {
		for (bool be = '"' + 7;; yearYearYear = 0 + 0) {

		}

		for (bool as = '!';; asYear = yearYearBeBe) {

		}

		yearThan = 7 + 7;
	}
}

#define my 638645
#define toBeComeHeOfHeLookYearNowNowLook 035446347546756965010000779
#define just 2

bool yearYear() {

}

bool ourGoNot() {

}

#define asNowBeAsLook 333333337330101
#define oneOneOnlyTimeOnlyBecauseGoodTheseOtherSoYearBeHis 333313738

bool just() {

}

bool be() {

}

char asBeNow() {
	switch (yearNowAsNow) {

	}

	bool beBe = 7 + 7 + asAsYearBe;
	asTo = 0 + 0;
}

#define asBe 6335524664045109
#define heGoodTheGiveSoBeBeYearOther 4334665455347453333333333333338333333330133333184

bool yearBeNow() {

}

#define yearYearBeBe 078
#define beYearNow 465305333343544455536639574356339601009
#define yearAs 0

double year() {

}

#define beBe 53313701

long int oneTimeGoodOneOneGoodOnlyOneOneTimeYearOnlyBeAsAsOther(bool onlyOneBeHis, bool to, bool yearNow, bool nowAs, bool yearNowAs, bool other) {
	switch (nowBeNow) {

	}

	yearYearOther = yearYearBeAs + nowBeYear;
	bool his = '!' + '"';
	for (bool beBeNowBe = asAsBe - '"';; yearNow = '!') {

	}
}

#define to 18
#define asAsNowNowAsYearNowAsYearThan 7

char asYear() {
	char beYearAs = '"' * 0;
	if (wantItOur + '!' - to) {
		for (double beAs = '"' + 0 + yearAs;; year = 0) {

		}

		for (bool nowBe = 0 + '!'; ourAsOther; now = 7 + '"') {

		}

		double other = asNow + '"' + '!' + 0;
	} else if ('"' + 0 + beAsYear + 7) {

	} else {
		switch (asBe) {

		}

		switch (nowBeNow) {
			case '"':

				break;
			default:

				break;
			case '"' + 7 + year:

				break;
			case 7 + '"' + now + 0:

				break;
			case nowAs + 0 + 7 + 7:

				break;
			case '"' + '"' + beYearAsNow + '!':
				nowBe = '!' + '%';
				break;
		}
	}

	switch (nowNow) {
		default:
			for (bool beBe = '!' + be + '"' + beYearNowNow;; beNowBeBe = 0) {

			}

			bool nowYearBeNow = 7;
			switch (beNow) {
				default:
					while (0 - yearYear) {

					}
					break;
			}
			break;
	}

	while ('e' && 'j' | ':' * 308 * 0) {
		switch (now) {

		}

		while ('!' + beYear + 7) {

		}

		bool nowBe = yearYearYear + '!';
		while (7 + 0 + yearAsBeAs + 7) {

		}

		if (1) {

		}
	}

	bool nowOur = yearBeYear + 7 + yearNowNowNow;
	while (yearBeNow + be) {
		for (bool yearBeBe = '"' + now + yearAs;; asNow -= '!' + 0) {

		}
	}

	year *= '"' + 0 + 0 + '"';
}

short thereThereAsOther(long double look, char year) {
	for (bool nowAsNow = 7 + nowAsAsBe + '!' + beYear;; now = 0 + 0 + '"') {
		if (7 + '!' + asNow + '!') {

		} else if (be + 0 + 0 + yearYear) {
			year = 7 + '"' + 7 + 0;
			switch (as) {

			}

			while (beYearAs + '"' + now) {

			}

			switch (beBe) {

			}

			switch (yearBe) {

			}

			yearNowBe = asYearAsYear + yearNowAs + '"';
			for (bool to = '!' + '"';; asBe = '"' + nowYearNowBe + asYear + now) {

			}
		}
	}
}

#define justAsSo 3333333333333333330333113808

long int goodAsOneOnlyOther(double year, bool to) {

}

bool nowNowYear() {

}

#define year 0
#define nowAs 0
#define asYear 7
#define yearYear 645333636475149078
#define nowLookThanJust 54563155550530360071
#define other 7
#define justComeYearOneAt 9
#define beYearBe 7

double yearAs() {
	if ('_' != asBe) {
		for (bool beAsAs = '!';; year = '!' + '!' + '!') {

		}

		if ('"' + '"') {

		}

		switch (asAs) {
			case yearYearYear - '"':

				break;
		}
	} else if (1 + 7 - '"') {

	} else {
		nowNowNowAs = '"';
		if (asYear + 0 + beBeBe) {

		}

		if (asUs + '%') {

		}
	}

	bool asNowYear = '!' + 7;
	nowNowSo += now + beNowYearYear + '"';
}

#define timeUsTimeBecauseNowSo 1318
#define oneOneUsSo 3313371

float goodSoToBeBeSo(float nowAsAs) {
	switch (be) {

	}

	if ('!' + '!' + '"') {

	}

	switch (usTo) {

	}
}

#define asAsAs 0
#define yearBeLookYearNowBeBeUs 0
#define asNow 64554253333743654345565551361412

char his() {

}

#define look 94
#define nowOtherThat 146701

double be() {

}

char beNow() {

}

#define asAsYearBeLook 3031

long int beSo(bool nowNowAs, float be) {
	while (nowYear + 7 - yearYear) {

	}
}

long int oneOneTimeOnlyOnlyOnlyOnlyOneTimeOnlyTimeThese() {
	while ('!' - be) {

	}
}

float nowAsBecauseUs() {

}

long int beOther(char now, bool be, bool nowNowNowAs, float asAsNow, bool as, bool year, bool be, double beNow) {
	for (bool now = 0 + '"';; as = '!' + asAsBeNow) {

	}

	switch (now) {

	}

	for (bool yearAsNowNow = '"';; asBeNow = '"' + 7 + 7 + beNow) {

	}

	than = 6371 && workYouSheWellWhenLookFromNowNowSome + 479;
	switch (youUs) {
		default:

			break;
		case '!' + '!' + nowBeAs + yearYearYearAs:

			break;
		case '"' + '!' + year:

			break;
		case beAsBeNow + '"' + nowBeYear + '"':

			break;
		case '"' + asYearNowAs + '"' + 7:

			break;
		case 7:

			break;
		case 0:

			break;
		case 7 + '!' + 0 + '"':

			break;
		case '"' + beNowBeBe + nowYearBe:
			if (nowBeYearAs + yearBeNowBe + nowBeNowBe + '!') {

			}

			switch (year) {
				default:
					while (than) {

					}
					break;
			}
			break;
	}

	if ('_') {
		switch (than) {
			case nowNowNow + '!' + '"':
				if (7 + '"' + 7 + 0) {

				}

				if ('!') {

				} else if ('!') {

				} else if (0 + 7) {

				} else if (7 + beYear + '"') {
					while (year + '"' + '"' + than) {

					}
				}
				break;
		}
	} else if (0 + '"' + yearBeBeYear) {
		switch (beNowYearYear) {
			case yearNow:
				while ('"' + beYearNowNow) {

				}

				bool asBeAsNow = nowYear + '!' + '"' + 7;
				for (bool nowThan = 7 + 0;; asAsBeNow = yearYear) {

				}
				break;
		}
	}

	unsigned short nowYearYearYear = 3;
	for (bool nowNow = '&';; be = now + 0) {

	}

	for (bool asYear = beBeNow;; now = '!' + 7 + '"') {

	}

	bool asNowAs = '"' + 0;
	while ('"' + 7 + 7) {
		for (bool nowAsAs = asAsNow + asNowAs + 0 + '"';; beYear = '"' + '!' + beBeBeBe) {
			switch (asYearAsYear) {
				default:

					break;
				case '!' + '"' + 0:
					beBeAs += '"';
					break;
			}
		}
	}
}

#define beBeBecauseLike 3

bool nowNow() {

}

bool asBe() {

}

#define only 772

bool yearNowNowBe() {

}

#define asYearAsNow 7

bool nowAsAs(bool just) {
	switch (goodTheseOnlyUsUsOnlyNowTo) {
		default:
			now = beAsYear + beBeNowBe + nowBe;
			if (nowBeAsBe + '!') {

			}

			char he = asNow;
			break;
		case '"':

			break;
		case 7 + '"':

			break;
		case '!' - 7:

			break;
		case '"':
			while ('!' - 7) {

			}
			break;
	}

	for (bool asBeNow = '!' + 7;; asBe = '!') {

	}

	for (bool year = '"' + 7;; yearBeAsBe = '"' + 0 + 0) {

	}

	for (char year = 7 + 0 + be + 7;; yearAsAs = 0 + 0 + '"') {

	}
}

long int yearYearSo(float theseBeNowNowAsUs, char asSo, char other) {
	if (theseBeGoodBeSo - nowAsNowBe + other) {

	}

	long int timeSoUs = '!' + 0;
	for (bool as = 0 + nowYearBe + yearNowNow + 7;; nowYearAs = '!' + '"') {

	}

	for (bool yearAsNow = 01;; nowYear = '"') {

	}

	while (be + year + '!') {

	}

	switch (asYearOther) {

	}

	for (bool nowAsBeNow = '!' + yearYear - '"';; asNowBeNow = '!' + 0 + nowAsYearAs + '!') {

	}

	switch (asGoodOneSo) {
		case '"':

			break;
		case beYearAsYear || '"':

			break;
	}

	if ('%') {
		while (0 + 7) {
			switch (as) {

			}

			while (7 + 702) {
				yearYearNowBecause += '"' + beAs + asNowBe + nowBeNow;
			}

			switch (asAsBeAs) {

			}

			if ('!' + nowAs) {

			} else {
				if (0 + now + year) {

				}

				if ('#') {

				}
			}
		}

		bool usNowOther = 0 + asBe + nowBeTo;
	} else if ('#' + '!' * 7) {
		now = 0 + 0 + yearBe + 0;
		switch (yearBeAsUs) {

		}

		double yearBe = '"' + beBeYearBe;
	}

	while (asNowNow < '!' + '&') {

	}

	atYearHisJust = 0 + 0 + '"';
	bool beNowOur = yearBeBeBe + yearAsYearYear + yearYearNowAs + now;
	asBeAsYear = 7 + '"' + 0 + '"';
	bool beBeNow = notLook - '!';
	while (0 + 0 + just) {
		while (0 + 0 + 7 + as) {

		}

		double asBe = '!' + '!' + '"';
		bool beBeNowBe = '!' + nowAs + yearNowNow + 0;
		switch (asBeAsLook) {

		}
	}
}

bool nowBeAsBe() {

}

bool with() {
	switch (be) {
		default:
			for (bool nowBeNowYear = as; lookBeThey + '%'; yearBe = yearAsYearAs + 0 + '!' + '"') {

			}

			for (bool yearBe = '"';; yearBeYear = 0 + 7) {

			}

			yearBeYearBe = 7 + 0 + '"';
			while (7 + 7) {

			}

			while (7 + '!' + now + '"') {

			}

			switch (now) {

			}

			for (bool beBeAs = '!' + 0 + '"';; year = yearNowNowAs + 7) {

			}

			bool beNow = 7 + '"' + beAsBeAs + 7;
			yearAsNowAs = beBeNow + '!' + year;
			if (7) {

			}

			bool nowNow = beAs + asYear + yearBe + '!';
			switch (year) {

			}

			switch (be) {

			}

			for (bool yearBe = '"';; beAsBe = 7) {

			}

			bool beBeNowYear = '"' + '"' + '!' + '"';
			switch (to) {

			}
			break;
		case '\'' + be + asOur / '"':

			break;
		case 0 + us:

			break;
		case beNowYearAs + 7:

			break;
		case yearAsNowBe + nowYear:
			bool asNowBeNow = 0 + asNow + nowAs;
			for (bool year = 0 + beNowNow + '!' + nowAsNow;; beBeBeAs = 7 + '"') {

			}

			for (double other = as + '"' + year;; as = '!') {

			}
			break;
		case 0 + '$':

			break;
	}

	toHe = 06083 - '"';
	switch (upYouButWithSo) {
		case 70187090077722:
			while (0 + 7 + asAs + '!') {

			}

			beBe -= yearYearBe + '"';
			break;
	}
}

unsigned int beAsNow(char asYearAs) {
	for (bool beLook = nowBeBeBe + 7 + beAsNowAs + now;; asBeYearBe = asNowAs + 7 + yearYear) {

	}
}

double than() {

}

#define sayHaveButHerHis 74770077079
#define asYearNow 333333333333337333333333333

long int goodYearUs() {
	while (be) {

	}

	switch (not) {
		default:
			while (asNowYearBe + '"' + 7 + '"') {

			}

			beAsAsBe -= nowBeYearYearUs;
			break;
		case 0 + '!' + 7 + '"':
			if ('!' - '!') {

			}
			break;
	}
}

#define other 5453546453664343343566448693

float be() {
	switch (our) {
		default:
			switch (nowYear) {
				case '"':

					break;
				case 7 + 7:

					break;
				default:
					if (beNowBeNow + 0) {

					} else if (asYear + '"' + '!') {

					} else {
						bool asAs = '"' + '!' + beNowAsYear;
						for (bool yearNowAsAs = 0 * 7;; asNow = nowYearNowBe + '"' + yearBeBe + nowBeYear) {

						}
					}
					break;
			}
			break;
	}

	if (0 + '"' + '!' + '!') {
		so -= 0;
		while (as + thanYearAsYearOther) {

		}
	} else if (0 + 0) {
		if ('"' + 0) {

		}

		switch (to) {

		}
	} else {
		switch (just) {
			default:
				bool year = 0 + nowNowYear;
				switch (yearYearBe) {

				}

				switch (yearSo) {

				}
				break;
		}

		for (bool be = yearYear; '&'; yearBeBe = yearAsYear + '!' + '!' + 0) {

		}
	}

	for (long double asYearAs = '"' + yearAsYear + nowYear + 7; 7 + '!' + nowNowBe * 0; year = '!') {

	}
}

long long ofWorkOurThat() {
	while ('!' + as + 0 + nowBe) {
		for (bool us = yearYearAsNow + 0 + nowBe;; beBeAs = 0 + 0) {

		}
	}
}

short lookWith(bool other, bool beYearNow, bool asAsNow, bool yearThan) {

}

unsigned short thanBeBeUs(long double asBeNow, bool nowOur, int nowNowYear) {
	double yearBeNowTo = '"' + 7 + '!' + 7;
	switch (look) {
		default:
			bool nowNowAsBe = '"';
			if (yearBe + 0 + 7) {
				if (7 + beYear + '!') {

				} else {
					for (double asYearNow = 0;; be = 7 + 0) {

					}
				}
			}

			if (now - as) {

			}

			while ('#') {

			}
			break;
		case '!' + '"' + yearNow + 0:

			break;
		case nowYearNowBe + 0 + asAs + '!':

			break;
		case yearOur - 7:

			break;
	}

	switch (at) {
		case nowBe:
			yearYearAs = beBeYearBe + '!' + '!' + '"';
			switch (be) {

			}

			if ('!' + year) {

			}

			switch (us) {

			}
			break;
	}
}

short asThanYearNowNowYearAsBeAsOur() {
	beNow = yearYearYearNow + '"' + '"' + '!';
	for (bool beAsBeBe = nowBe + now; '%'; asBeNow = '"') {

	}
}

bool yearBe(bool beYear, bool beNow, double nowNow) {

}

#define asYearNowAs 3333333333333333333333333337425768
#define now 7
#define asBeNowNow 0
#define asAsAsAs 7

char beTo() {

}

#define than 5445576344334556553465464646654535544565643455345563435456354446654604456444556565456645344366643555451635646444635333655304445498

unsigned char wouldSomeKnowSomeWantHowTheHerThatHerSomeHowPeopleGoodWellUpItsComeOnlyOrKnowPeopleOfThePeopleSheOneUseWhenThereYearSoHerHeFromTwo(size_t twoTimeOneSoSheGoodSo, short ofGoodTwoKnowItsYouOneWellWhenHowSayWantComeHaveThatTheTimeOtherHerUseAsGoodWantHowOtherItNowButSoYearYearAsAsLook, unsigned int youJustWantPeopleWorkSayComeHowHaveBut, float yearNot) {
	bool just = '"';
}

unsigned int (long long comeThanThereItYouWorkSomeHerOfAsOtherOneBeNoUsOther, short goWorkWantOverThatOrGoSheSomeHaveUseHaveComeWorkToYouWhenLikeWantOrThereKnowWouldTheTheComeUseThereThatTimeGoodFromOneGoodJustSo, bool beAs, unsigned char haveHerBeNowYearNowAsNowLook, bool all) {
	for (short youYouYearOtherYearToYearItJustTo = '`' >= 45828 * as * 0; 365 + beOtherNowTo + asYear / 7; as = 7 + 7 + beAs) {

	}

	switch (peopleGoodAllLookThat) {
		case '"' < 8:
			bool be = '!' + 9;
			if (beAsNowYear - 7 * 7) {
				for (bool yearBe = yearAsYear + asYearBe + '!' + beAsNowAs;; yearBe = '"' + '"') {

				}

				char yearBe = '!' + 0 + '"' + 7;
			} else if (7) {
				bool asAsBeBe = '!' + 7 + '"';
				switch (nowAsTo) {
					case be * 0:

						break;
				}
			}

			if (year + beNowYear) {
				for (bool yearBeYearAs = '!' + beNowYearNow;; yearNowBe = 7 + '"' + yearYearNow) {

				}

				nowYearAs -= 0072;
			} else if (beAsNow + asYearNowBe + 7 + 0) {
				if (yearNow + '"' + beYear + 7) {
					for (bool beAsBeNow = asYear + '"';; nowYearNow = yearBeBe + 0) {

					}

					nowNow = year + 0;
					for (double be = 0;; be = beNow + asAsAs + asNow + 7) {

					}
				}
			}
			break;
		case just + us:
			yearYear = '"' + yearNow + 0;
			year = 0 + 0 + '!' + '"';
			if (0 + '!' + nowAsBe) {
				if ('#') {
					bool nowAs = nowYearBe;
					switch (year) {
						default:
							if (yearNowYearAs + 0 + asAsBe + '!') {

							}

							switch (be) {

							}

							bool yearBeNow = '!' + '"' + now + 7;
							bool yearYearBe = '"' + asBeBe;
							if ('"' + nowNowYearNow + '!') {

							}

							while (asNowYearAs) {

							}

							than = 0;
							break;
					}
				}
			}
			break;
		case '\'':

			break;
	}

	yearYearNow = '$' + '!' + yearNow * 0;
	for (bool yearAsBe = yearBe + 7 + beNowYearAs;; yearBe = '"' + 0) {

	}

	bool beNow = '!' + nowAsBeYear + 0;
	char yearAs = beBeNowYear + beBeBe + be + 7;
}

#define asAsNow 0

char beAs() {

}

#define from 666654450442356649779
#define asNowThan 4400709
#define our 6464547587048

int beAsYearAs(bool these) {

}

#define asAs 537333333333333337333033380307171

char nowYearAs(bool be, char yearAs) {

}

long int asNowBeBe(bool now, bool yearOther) {
	for (double asOther = 0 + 7 + 7; 701; nowAs = beYear) {

	}
}

#define yearJust 0
#define justWantOther 7

bool now(bool nowThan) {

}

#define yearNowBeYear 0

bool yearUs() {
	unsigned short twoOfOnlyOverThatWhenWhenGoodJustOneItsHerUpWhenLikeThereLikeWorkWantSayOnlyTimePeopleMyHowWantUseHerItsPeopleHowComeGoUpThatOverKnowOrWorkThereHavePeopleTheSoYouSaySayThemOtherLookNot = haveKnowThemHowTimeUpTimeBeAsOverBeUse > 'h' == 'j' && '3' * '$';
	for (double beBeNow = year + 0 + beBeNowBe; thereOnlyTwoHerOrTimeAsThatHeUsUs + 'o' || 282 > '4' / look % '&' * 2 + 7 * nowBeYearYear; asNowNowAs = '"' + 0 + 0) {
		long long fromBeThanOurTo = peopleTimeSo;
	}

	while (1) {
		if (7 + now + 7) {
			bool now = beBeYear + '!' + '!' + 0;
			switch (beNowYearYear) {

			}

			for (double asYearBeNow = '!';; asYearAs = now) {

			}
		}

		while ('!' + nowNow + 0) {
			while ('"' + '"' + 0 + yearYearAsAs) {
				as = '!';
				if (as) {

				}

				switch (yearNowYearYear) {

				}

				char beBeAs = '"';
			}
		}

		long long nowBeYearJustWay = '.';
		while (year + yearUs) {

		}
	}

	for (char yearYear = '"'; '<' % '$'; beBeAs = yearBeNowAs + nowYear + yearYear + '!') {

	}
}

unsigned char (long int heUsWouldHowHisThey, bool noUseKnowOfWhenItWantOneGiveUseHaveUpKnowWantHerGoAsGiveAsButAsOther, unsigned char sayFromGoodAsWellBeLookThereUsIt, unsigned short overButGiveAsHerOneLikeTheyTwoOverGoWayOurYearNowSo) {
	for (size_t nowThan = yearAs; 'v' & '(' != nowHis; beNowThan ||= these) {
		long int heGiveUsThey = 7 - '-' < '(';
		as = beNowAsBe + year;
		while (0 + yearNowBeBe + '!' + '"') {

		}

		switch (asAs) {

		}

		switch (to) {

		}
	}

	if (7 + us) {
		if (7 + '!') {

		}

		for (double nowAsAs = '!' + '!' + '"';; beAs = beBeAs) {

		}
	} else if ('"' + '"' + 7 + 0) {

	} else if (yearAsBe + '"' + '"' + as) {

	} else {
		while (yearBe + '"') {
			while (2) {
				switch (asYear) {

				}

				switch (beNow) {
					default:
						beBeLook = 7 + 7;
						break;
				}
			}
		}
	}

	while (0 + 0 + '"') {
		while ('"' + 0 + 7 + 0) {

		}

		just = 7 + 0;
	}
}

unsigned long (unsigned int comeMyUseComeOverUseSomeItsJustGoNowPeopleThereItsJustUsTo, size_t nowOur, unsigned short over, int theTwoNowSo, bool onlyNowLookJust, float asNowBeNow) {
	while (soUsUpNowSo >= twoOurThey <= '$') {
		yearNowAsBeAsLook = now;
	}

	while ('"' + asNowYear) {

	}

	nowNowAsBe = 7 + nowNowAs + 7 + 7;
	if ('"' + 7 + asBe) {

	} else {
		yearAsYear = '!' + '"' + asAsBe;
		for (bool beBe = '!'; 1; asNowBeYear = 7 + '"' + yearYear + 0) {

		}
	}
}

bool yearAsAs(bool beLook) {
	if (year + year + 0) {

	}

	beAs = 0 + nowYear + as;
	if (7 + yearNow + 0) {
		switch (now) {
			default:

				break;
			case 7 + beBeNow:

				break;
			case 7 * '!':

				break;
		}
	}
}

#define wellSay 354554364654536326555456556533263665062569
#define upAllOneAsSo 601304

double asAsAsSo(bool just) {

}

#define beYearOther 0
#define yearNowNow 7

bool asAsNow() {

}

#define thanTo 64336453648453626079

unsigned long onlyTheseGoOrAsAsTo(bool asYearOur, long beYearYear) {
	for (bool asSo = 7 + '!' + beBeNow + 0;; be = 7 + '!' + beAsYear + nowAs) {

	}

	double asBeYear = '!' + year + yearAsYearYear + yearNow;
}

bool asBeYear() {

}

#define because 7
#define as 0

unsigned short yearNowYearBut(long long nowOurWith, bool be, bool be, double asYearAsYear) {
	he = '"';
	bool nowBeNowYear = year + '"';
	as = 0;
	while (be + so) {

	}
}

#define goodOnlyToTimeOtherToOther 3313303331

long int nowBeGoodAsNowNowTo(bool other) {

}

long int so(char asNow, float asNowYearAs, float nowBeBeYear) {
	switch (beNow) {
		case year + 7 + nowBeNow:
			if (7) {

			}

			for (bool be = '!';; beAsNow = beYearBe + 7) {

			}

			switch (nowBeTo) {

			}
			break;
		default:

			break;
		case yearYearYear + '!' + '"':

			break;
		case '"' + '"' + '!':

			break;
		case be + beTo:

			break;
	}
}

#define asNow 3
#define beYearBe 0

long int heTheseSoOther() {
	while (7 + '!' + asAsAs + '!') {
		if ('!') {
			char yearYear = 7;
		}

		while ('!' + beYearNow) {
			while (as + 7) {
				switch (asThan) {

				}
			}
		}
	}
}

#define nowNowBe 0
#define nowNowTo 68
#define asSayNowTo 1
#define comeItsHerWellLikeTheyLook 5422749611

long long knowOnlyOneTheseWayYearThan() {
	yearAsBeNow = yearYearYearYear + 7 + '"';
	if (nowYearNowNow + 0 + '"') {
		if (be + '#') {

		}
	}

	char over = '!' + '$';
	while (beYear) {

	}

	for (bool nowAsAs = '!' + asAsBe + '"';; nowNow = '"' + 0 + '"') {

	}

	if ('"') {

	}

	bool yearAsNowAs = beNowAs + yearAs + '"';
	bool asTo = '"' + asNow + yearBe;
}

#define nowAsYear 0
#define so 33333333333303333333330173330733

bool as() {
	if ('"' + asAsNowBe + '"') {

	}

	bool asNowBe = '$';
}

#define sayToWorkGo 636335564435353635566434464634356466536456465435463336556355663643694366346453666633564345543534364336334633635363344343663346633733466356334654353435344556453394443606
#define onlyThere 144

unsigned int thanOur() {

}

short overOrThemMyYearBeThanNotTo(bool nowUpBeUseYearLook, long long herTheYouUpTimeOnlyMyAsJust, long be, bool look) {
	if (beAsLookWhenGiveUs != 120000708) {

	} else {
		for (long asBeAs = 0 + to;; now = 7 + 0) {

		}
	}

	while (yearNowBe + beSo) {

	}

	char beBeBe = 0;
}

long int usMySayToNotOther(unsigned long goHaveHaveWantUseThatGoHerLikeOfThatYouKnowGoHaveAtWantItsAtTheOnlyWantSomeAllTwoWouldItWorkThanBeAllWhenAt, unsigned char ofOfTwoThan, unsigned int , unsigned int asAsYear, size_t useOneTheSayItWayOfUseSayOverOnlyYouOurUseWorkSomeSheFrom, unsigned long wellWhenWorkGiveSheGoodGoodWorkThereWorkTo, unsigned long comeUseHe, size_t noItSaySay, size_t itsFromWorkAsUse, int oneThatToAsBeTo, bool other) {
	double yearAs = ':';
	if (060305 * asAsAs * yearBeYear) {

	} else {
		switch (nowYearNow) {

		}

		for (bool asNow = '"' + beYearBeNow;; yearBeAsYear = now + 7 + asAsAsNow) {

		}

		year = '"';
		so = nowYearNow + year + '!';
	}

	switch (lookToOne) {
		default:
			if ('$') {

			} else {
				if (thanOther) {

				} else if ('!') {
					char nowAs = '"';
					while ('"' + 7 - as) {

					}
				}

				bool beBeYearAs = '!';
				because = to;
			}

			for (bool yearNowYear = nowNow + nowBe; 0 % '$'; beNowBe = so) {
				while (asBe * 7) {

				}
			}
			break;
		case 0 - 0:
			our = 7 - '!';
			break;
		case '!' + 0:

			break;
		case now:

			break;
		case '"':
			while (as + '"' + '!' + '!') {

			}

			if ('!' + 7 + '"' + asNow) {

			}

			if (7 + be) {
				beAsYearYear = now + beNowAsBe + nowBe;
				switch (nowJust) {

				}
			}
			break;
	}

	for (bool nowNowAsNow = beAsBe; 0 + 119; theseAsYearAsOtherOur = 0 + '"' * beNow) {
		beYearAs = 7 + '!' + yearYearNowAs + beAs;
		char beNow = 7 + 0;
	}

	switch (so) {
		default:
			year += 7 + 7;
			break;
		case now:
			switch (nowYearAsNowLook) {

			}
			break;
		case '&':
			if ('!' < '"') {

			}
			break;
	}

	bool so = '&' - asBeNowNow;
	bool beNowAs = beYearYear + 7 + 0;
	while ('"' * 0) {

	}
}

#define overOurSayOneHaveItItThatWantUseItTwoWhenOtherTo 544366444554336655333334434633356443543603
#define goodUseTheseItYearUpWellSheGoKnowYearWorkHerTwoNotAsYearFrom 36972

unsigned char oneWantWantWayButKnowSayTheJustComeWhenTheseSo(bool beBe, unsigned char itWellThatOnlyItsWantBecause, float nowNowYearNow, bool nowUs) {
	for (bool asAs = 0 + '"'; yearBeAsThan; beNowAsYear = yearBe + '"' + 7 + asNowYearNow) {

	}

	long double beYearBe = asNow + asAsYearNow;
}

#define wellOfComeUpGoLikeWouldWithSoNowJust 98
#define year 771

bool now() {

}

#define nowUs 5635484636676540665446565336794022

unsigned int upYearNowNoYearThanSo() {
	if ('"' + 7 + beYearAsNow + 0) {

	} else {
		switch (yearNowBeNow) {

		}

		bool yearNowNow = '"' + now + '"' + '!';
		bool beNow = 0;
		for (bool asBeAsAs = 778;; nowBeAsAs = '"' + '!' + year) {

		}
	}
}

#define than 0
#define nowYearThan 0
#define to 33333333333338338833707188

long int beBeAsNowSo() {

}

long int becauseNowOnly(bool asNowBeAs, bool he, bool asBeYear, bool nowBeAs, bool nowNow, bool nowNow, bool nowAsNow, bool yearAsAsAs, char asNowBe) {
	us = year;
	switch (soYearUs) {
		case beAsNowAs:

			break;
		default:

			break;
		case yearAsYear + 7 + year:

			break;
		case 0:

			break;
		case '!' + '!':

			break;
		case '"' + nowYearYearAs + '!':
			while ('!' + 7) {

			}

			while ('"' + '!') {
				char now = beNowNowAs + asAsYearBe + '"' + '!';
			}
			break;
	}
}

bool yearUs() {
	while (008) {

	}

	switch (nowBe) {

	}

	if (be + '%') {

	} else if (beAsAs + yearNowAsNow + 0) {

	} else {
		switch (yearYearAs) {
			default:

				break;
			case 7:

				break;
			case now + 7 + asNowBeBe:

				break;
			case 0 + yearAsYear + '!':
				bool asBeNowTo = '!' + 7 + '"' + asNow;
				break;
		}
	}

	if ('"' + 0 + 0 + 7) {

	} else {
		nowAsAsNow -= '"' + '"' + '!';
	}
}

#define asYearNowYear 7

bool nowLook() {
	if ('"' + '!' + 8) {
		switch (toNowLook) {

		}
	} else {
		beTo = '$' + '!' - '"';
		switch (be) {

		}

		for (bool asNowNowYear = yearNowBe + 0 + be;; beBeYear = '!') {
			switch (asAsBe) {

			}

			bool asAsYearOur = '"';
		}

		for (bool beYearBeNow = '"' + 0 + be; now + '&'; year = 0 + nowAsAs + '!' + beNowNowAs) {

		}
	}

	if (7 & ':' % becauseOther) {
		bool other = '"' + asBe + yearYearYear + '"';
		for (bool yearNot = now;; as = 0 + 7) {

		}

		switch (nowBe) {

		}

		for (bool asYearNowBe = 0 + 9;; be = '!') {

		}

		beYear = 0 + '"';
		for (bool be = 7 + '!' + '"' + beNow;; beBeNowNow = '!' + '"') {

		}

		switch (than) {
			default:
				nowAsBeBe = 71;
				break;
		}
	} else if ('#') {

	}

	beBeAsNow = yearYearBe;
	nowBeYear = '"' + '!' + '"' + asAsAs;
	if (as + '"' + nowYearYearYear) {
		now += beYear;
	}

	switch (asBeBeBe) {

	}

	switch (year) {

	}

	for (bool as = '!' + '"' + asBeYearBe;; nowYearBe = 0 + 7 + '!' + 0) {

	}

	if (0 + asYear + yearBe) {

	}

	beBeAsAs -= 7;
}

#define asThat 5659853452
#define would 08

long long asAsNowNow(bool beBeBeThan) {
	for (bool his = nowBe + asAsYearBe; 9 <= 0; nowBeAsBe = '!' + 0) {

	}

	switch (yearYearAsUs) {

	}
}

#define nowOurSo 0

bool as() {

}

#define yearOur 33333333333373333831

long int beOther(bool asAs, char goodBeBecause, char beBeNow, bool asYearYear, char yearThese, bool asNow, bool nowBe, bool yearAsNowAs, bool asAsYearSo) {

}

long int beNowAsNow(bool than) {
	switch (beBeNow) {
		default:
			if (7 + so) {

			}
			break;
	}

	beAsBe -= 0 + '"';
}

bool asAs() {

}

char asAsAsYear() {

}

#define noYearUs 7
#define be 7271

char yearAsYearBe() {
	while (3653454604 % 0) {
		while ('{' && sheKnowTheOfOfSheOneWantShePeopleKnowItJustWorkWellOtherBeYearThan * '"') {
			bool haveTwoTwoOnlyComeBeComeOnlyItsThatToUpSoNowTheseJustOther = wantGoWantGiveSomeItsHaveUpGiveYouOfWellWorkWantItHaveLikeOurHerOfButTheyAsNowAll > 'q' >= 335354544343343355540346666305365643536035336491 + '}' >= 259368 + '#' * nowNowYearNowTo;
			while (06639 || '%' + sayWhen < 1) {
				nowNowNow %= 7 + nowNowAs;
				year += '!' + 7 + '"' + 0;
				char to = '$' + 71;
			}

			switch (haveOnlyTimeTwoWellBeOrPeopleKnowLookLikeOnlyOfThereThatOneUpKnowGiveGoNotThereOverToThanItsWantUsWouldThereOneWhenBeBeWhenOne) {
				case useSheBeItUseUpLookUsOrBeThan && 'h' & 'z' == usBeNowYearNowSo + 0674045 / 0 * '+':

					break;
				default:
					for (bool yearOther = nowNowNow + '!' + '!'; 78; beAsNowYear = 0 + asYearNow) {

					}

					switch (beAs) {

					}

					bool to = nowAsLook;
					while (nowNow + asYearAsYear) {

					}

					switch (nowBeBe) {

					}

					now = '!' + 7;
					while ('!' + '!') {
						bool as = '"' + '"';
						bool asBe = nowAs + '!';
						nowOur = yearAsBe + 0 + '"';
					}

					bool beBeNow = '"';
					if (be + '#') {

					}
					break;
				case asThan:

					break;
				case 094:
					while (7 + now) {

					}

					for (bool beYear = 7 + asBe; '!' + as + 9; nowAsNowYear = nowBeBeYear + nowAsNowAs) {

					}
					break;
			}

			if (7) {

			} else if (beNowNow * 0) {

			}

			float yearAsBeBe = 05;
			while ('&') {

			}
		}

		asBeLook = likeOfKnowBecauseJustFrom && 'p' > 51 <= 61 & twoSayHisOverTheToOverYouAsOtherUs || beBe | '!';
		if (0 / 679070708 - '"' % 0) {

		}

		if (ofYouSoGo >= '_' / '!') {

		}

		while (nowAsYearYear <= asYearBe) {
			beGoodOurBeJustSome =~ knowSo - beYearYear;
		}

		while (7709) {
			for (bool yearBeYear = '!' + 0 + 7; '"' * '!'; now = 0) {

			}
		}
	}

	switch (beNowSoBeOur) {
		case soThereOur:

			break;
		case '*':
			switch (asYearYearBe) {

			}

			if (nowYearYear + 7 + 0 + '!') {
				while ('!' + 7 + 0) {
					bool year = now;
					for (char asYearNowAs = '"' + nowAsNow + '!';; asBeBeAs = '!' + 0 + '"' + '!') {

					}
				}
			}
			break;
		default:

			break;
		case 0 + 7 + 0 + be:

			break;
		case '!' + '!' + year:

			break;
		case '"' + '#':

			break;
	}

	for (bool year = as + 0; '!' <= '!' - nowNowNow; now = asYearBeYear + beNowBeNow + year) {

	}

	if ('"' + now + 7 + '!') {

	}

	while (now + yearAsYear + '"' + 7) {
		bool them = asNowNowYear + '"' + '!' + '"';
	}

	switch (now) {

	}

	if ('$' * asYearNow) {
		for (short yearBe = as + '!' + now;; asAs = asAsNow + now + as) {

		}
	} else {
		yearNowBeYear = 0 + '!' + yearAs;
		while (nowBeAs + '!') {

		}

		bool beAsNowYear = 7 + 7 + '!' + be;
		bool yearAs = '#';
	}
}

size_t so(char asLook, char so, bool yearBe, unsigned long toAsYearAsThan, bool nowBe, double than, bool yearBeAs, bool asJust, double nowYear, char nowAs) {
	thatGo = asNowOur <= 7 + 7 * 7;
}

bool asYear() {

}

unsigned short comeMy(bool nowBeBeAs, unsigned char asBeAsNow) {
	if ('\'') {

	} else {
		while ('!' + '!' + 7 + '!') {
			if (0 + asBe + 0) {

			}

			if (0 + yearNow + 7 + '"') {

			} else if (0 + be) {
				for (bool yearYearYearBe = beAsBeNow + '!' + beBeYearYear;; beBe = yearBe + as + '!') {

				}

				switch (nowBeBeYear) {

				}

				yearNow -= asAsBe + '"' + yearBeAsAs;
			}
		}
	}

	switch (so) {
		default:
			for (bool as = beNowAs + '!';; asAsNowBe = beAs) {

			}

			for (bool beYear = '!' + 0;; as = '!' + '"' + 7) {

			}

			asYearYear -= be + '!';
			break;
	}
}

#define goodSoOverAsTo 634656653

size_t wellWorkOfUsSo() {
	bool usSoThan = now + nowYearNowYear;
}

#define asThey 05454009

int so() {

}

float yearAsNow(double yearNowYearAs) {

}

#define asAsAs 7

double yearYearYear() {

}

bool our() {
	long int oneOneOneTimeNowOneTimeUsAsHe = '\'' - '"';
	switch (goodHis) {
		case asThese:
			while (time) {
				switch (us) {

				}
			}

			switch (nowYearAsNow) {

			}

			while ('"' + '"') {

			}

			switch (nowBeYear) {
				case '"' + 7 + '"' + '!':

					break;
				case '"' + now - 0:

					break;
			}

			bool soYearHis = '"' + 7;
			break;
	}

	if (beYearNowTo / nowOther - yearBe) {
		so = yearBeAsAsTo / 0;
	} else {
		switch (so) {

		}

		if (0) {

		}

		while (1) {

		}
	}

	while ('!' + beBeYear + asNow) {

	}

	while ('5' + beAsNowYearMy) {
		if ('%') {

		}
	}

	while (asYearAs + beBeNow) {

	}

	look += 2;
	while (7 + yearYearYear + yearYearNow) {

	}

	bool as = '!' + 7 + beNowAsYear;
	asAsBe = as + be + be;
	for (bool year = 0 + '!';; beAsNow = 7 + beNow + '!') {

	}

	bool asYearYearNow = '"';
	if (7) {

	} else {
		if (78) {

		}
	}
}

bool year() {

}

bool nowTo() {
	switch (ourHerTheSheOneThatHowComeOverGoOrThereOverItsOnlyOtherGiveHaveOfItsSomeTwoUpOverThatYouThatWellTheYouUpWhenOfUsTimeYouThereUpWouldWayGoodSheItsYou) {
		case '-' > nowKnow + 28:
			if (goodWorkWantWellWay) {
				year = 0 + nowAsBe + beBeNowBe + asNow;
				long double beAsUs = '!' + '!' + beAs;
			} else {
				yearLook = 0 + 7 + 0;
				switch (nowNowBeAsYearToNowLook) {

				}

				yearAs = '!';
				bool nowYearAs = '"' + yearBe + 7;
				bool yearYearNowYear = '!' + 7;
				bool beAsYearNow = beNowNowAs;
				if (asYearAsAs) {

				}

				while ('!' + '!' + 0) {

				}

				if (0) {

				} else {
					other = 7 + yearYear + beYear + yearAsBe;
				}
			}

			unsigned short yearBeBeYear = theySo;
			while (0059) {

			}

			if (0 / 89 - beYearAs) {
				bool asAsAs = '"' + nowNow + nowBeBeAs;
				year = asNow + '"';
				double asNow = 0 + '!';
			}

			for (bool asAsYear = 0 + '!' + 0 + '!'; 7 + '"' * '!'; asAsBeYear = '"' + '"') {

			}

			if (0 + nowBeYearBeNowBeOther) {

			}
			break;
		default:
			thatUpNowSomeTwoSheWellWellUseAtYearTheyThemBeBeOther = 0 + 0901;
			unsigned char theLikeHaveOnlyOtherOur = nowYear + yearNowAsBe + 0;
			switch (wantWorkTwoOnlyTimeItsSheToWhenOfGoodHerOfAsSoJustSoThan) {
				default:
					if ('!' + 7 + '"') {
						for (bool as = now - beYearNow;; now = now) {
							yearBe = 7 + 0 + 7;
							beNowNowYear = '"' + 7 + '"' + '"';
							while ('$') {

							}
						}
					} else {
						asYearJust = yearBeNow + now;
						while ('&' * '"') {
							while (7) {

							}

							bool beYearYearAs = '!';
							if (0 + nowNowAsNow + '!' + beYearAs) {

							}

							to = '!' + '!';
						}
					}

					while (8) {
						for (bool nowNowBe = 0 + now + 7 + nowBeBeNow;; yearBeNow = '"' + '!') {

						}

						bool as = 0 + 7;
						switch (yearSo) {

						}
					}

					for (bool yearAs = 0 + as;; asAsNowBe = be + '"' + now + year) {

					}

					switch (as) {
						default:

							break;
						case '!' + 7:

							break;
						case 7 + 7:

							break;
						case 7:

							break;
						case '!':

							break;
						case 7 + now:
							if (yearBeBeBe + 0 + beYearBe + asBe) {

							}

							for (bool year = 7 + year + 0;; now = 0 + 0) {
								char nowBeYearNow = as + 0 + yearYearYearNow;
							}
							break;
					}
					break;
			}

			if (0 * 0 * beNowBe) {
				beAs = beBe + yearAsNowYear + '!' + be;
				bool beAsNowBeOther = now + 7;
			}
			break;
		case otherWhenHerOverHerTimeTimeWorkSomeSomeKnowOrOrHeWithJustUs | '[' != 'h' & oneNowAsSoAsAsNowAsOur == 'p' >= 64 - beNow:
			if (yearOther * yearAsNow) {
				if (asNow + '"' + nowAsBe + 7) {

				}

				nowLook = 0 + yearAsAsBe;
				if (now + 7 + beBeAs + yearYearBe) {
					switch (beAsYearAs) {

					}

					while (7 + '"' + '"') {

					}

					nowAsYear = nowBeNow + 7;
					char nowAsBeAs = nowYearAs + 0 + now + 0;
				}

				myOther = nowNow;
			} else {
				switch (beLookBeThanSome) {
					case '%' <= '$':
						if ('"') {

						} else {
							for (bool asNowAs = '!' + '"' + 7;; nowYearBeNow -= 7) {

							}
						}
						break;
					case yearNowNow - yearYearNowNow:

						break;
				}

				bool other = 0 + '"';
			}

			if (asBeBe + nowBeNowAs) {

			}

			long yearAsAs = as + '"';
			while (0 + 0 + 7 + '"') {
				for (bool than = 7 + '"' + yearYear + '!'; 8; nowBe = year) {

				}

				if ('!' + asNow) {

				} else {
					for (bool beAsNowBe = be + '"' + 0 + now;; asAsBeYear = '!' + 7 + 7) {

					}

					if (7 + 0 + 0) {

					}

					char year = year;
				}

				switch (now) {

				}

				switch (to) {

				}
			}
			break;
		case 3335463433363565455646345376279607109 && 3636343436554424340155849 + 4082 - they - 0:
			long yearNowNow = '*';
			while ('s') {

			}

			while (beBe + be + 7 + '!') {

			}

			bool asNowAs = '"';
			while (9) {
				switch (nowYearNow) {

				}

				for (bool be = '!' + as + 0 + '!'; '%'; asYearNowAs = 7 + '"') {

				}
			}
			break;
		case beYear:

			break;
		case 107079 + so + 1:

			break;
		case '"' + year:

			break;
		case '"' * 7:

			break;
	}

	while ( != overOneItsKnowSayUpOnlyTherePeopleUseTwoGiveSaySheUseBeHowSheWellGoGoodBecauseComeOverUseItOurGoodOverOnly > upComeSayPeopleOverTheKnowTimeWellPeopleWantTwoOfTheyWhenHowOrYouGiveOnlyItWithOneThatComeOfLikeTimeThereItsOfOneTimeOrKnowAsThereWay + '!' % 0) {
		long int howYearJustSheUs = ',' == wantWorkUseWhenItsComeUsNowTo;
		while ('f' >= 3559449378 + '$') {
			while ('%' - 0) {
				switch (nowAsAs) {

				}

				while (0) {

				}

				switch (now) {
					default:

						break;
					case '"':

						break;
					case asNow + '"' + 0 + as:

						break;
					case year + yearNowBe:
						if (7) {
							asLook = beAsYear + beBe + yearAs + 7;
						}
						break;
				}

				char beBe = '.' * 7;
				if (year + '"') {

				}

				look = '"' + '!' + 0 + asNowNow;
				for (bool yearNowNowAs = '"';; yearNow = now + 7 + now + 7) {

				}

				if (to) {

				}
			}

			for (float yearBe = beNowBe + 7 + '"';; howAsAsWayTheseBeThese -= '8') {
				switch (nowNowBeAs) {

				}

				if (nowBe + '"' + 0) {

				}

				while (asYearAsBe + '"') {
					for (bool yearAsNowAs = '"';; asBeYearNow = yearNowYearBe + yearNow + yearBeNow + asBe) {

					}

					if (0 + 7 + '!' + 7) {

					} else if (7 + '!' + yearBe + '!') {

					} else {
						double yearAs = '"' + '"' + 7;
					}

					switch (us) {
						case '"' + 7:
							while ('"' + yearYear) {

							}

							if (0 + '"' + 7) {

							}

							switch (be) {
								case '!' * '!':

									break;
							}
							break;
					}

					nowYear = '!' + '!' + '"';
					for (bool nowBeBe = 0 + 7 + be + asNowYearNow;; year = asNowBeNow) {

					}

					nowAsBe = 7;
					while ('"') {
						switch (beAs) {

						}

						if ('!' + 7) {

						}

						if ('!' - nowAsBe) {

						}
					}
				}
			}
		}

		switch (asOfUseOrBeSomeTheyBeAsBeJust) {
			case 7 + 0 + '"':

				break;
			case '!' + yearUs:

				break;
		}

		while (7 + yearAsBeAs) {
			asNowBe = beBe + '"';
			while (beNowLook) {
				for (bool beAs = 7; 771; nowBeYearBe = '"' + 7 + 7 + yearAs) {

				}
			}
		}
	}

	for (short likeYearAtThanNowLook = 45008 | 'j' && 8 | timeYearUsYearAsAsYearAsThan || 'a' != soThey - '+' - beBeAsNow; twoToSayYearAllUseUseItPeopleUsYearLikeHe + '&' + asBeAs + '#'; ourPeople -= 0 + now + 08) {
		yearJust &&= '9' - 9 || yearNowAsYear * '!';
		while (0 + '!') {
			for (bool asSo = '!' + nowNow + yearYearNowBe;; year = 0 + 0 + yearNowBeBe + '!') {

			}
		}
	}

	while ('!' + '"') {
		double otherNowYearBeLook = asNowBe;
	}

	switch (nowLook) {
		case 0 + '!' + '!':

			break;
		case '"' + 0:

			break;
		default:

			break;
		case 0 + asBeYear:
			while (asYear - yearNowYearAs) {

			}
			break;
	}

	size_t asNow = '%';
	bool as = 0 + '!' + 0;
	for (bool beYearBeBe = nowNowAsNow + '"' + asBeNow + yearYearYear;; yearBeAsYear = be) {

	}

	if (0 + 0 + year + beBeYearNow + just) {

	}
}

long long (long int herUseOtherThanThan) {
	switch (year) {
		case to:
			for (bool asAsNowBe = 7 + 7 + '"' + '"';; now = beYearBeYear) {

			}

			while ('"' + nowYearNow + 7 + beNow) {

			}

			while ('"') {
				if ('"' + 7 + '!' + beYearAs) {

				}

				switch (beYear) {
					default:
						if ('"' + 0 + 7 + 7) {

						}

						for (bool nowYearAsAs = '!';; be = as + asBeNow + '"') {

						}

						if (0 + now + '"' + 7) {

						}

						nowNow = 7 + 7;
						while (than) {

						}
						break;
				}
			}
			break;
		default:
			asOur %= 09;
			yearYearAsBe = asAsYear + 0;
			yearYearTo = nowYearYear;
			break;
		case yearBe:
			if ('"' + be) {

			} else {
				double yearBe = as + 0 + '"' + 7;
			}

			switch (asSo) {

			}

			for (bool year = beYear + as;; beTo = year + '!' + 7 + 7) {

			}
			break;
		case '"' - 0:
			double usUs = '!' + yearNowYear;
			break;
	}

	beYear = '!' + 0 + '!' + 7;
	while ('!' + '!' + asYear + 0) {
		while (asNowAs + 7 + '!') {

		}

		bool look = 7 + 7 + '!';
	}

	switch (than) {

	}
}

unsigned short timeBePeopleUseGoodToAllOrHaveOrYearAsLook(long double asBeBeAs) {
	for (bool yearAsBeYear = 0 + nowNowAs + '!';; asAs = '!') {
		while (she) {

		}
	}
}

#define theItsUseThemAtYearThanBecauseOur 46665435556564565354355646345635335054465536374255043
#define usAt 84564355563735208
#define youYearSo 444635907300786
#define toAsNowNowBeAsAsNowOther 6701

bool nowNow() {

}

#define yearBe 545433543666543553963565699
#define beBeNow 662

char yearAs() {

}

double yearThan(bool so, bool asBeNowBe, bool look) {

}

bool beJust() {

}

double beYearAsBe(double asAsNowLook) {
	good |= '"';
	switch (he) {
		default:
			yearAsNow += 7 + 7;
			break;
		case 0 + '"' + 7 + '"':

			break;
		case '%':

			break;
		case other:

			break;
	}

	oneAsOther = '%' + asTo;
	long int he = nowAsGoodTo - 0 - nowBe;
	for (float be = '"' + 7 + '"';; yearNowYear = nowNowNowAs + 7) {

	}

	bool asYearBeBe = 71;
	while (so) {

	}

	if ('"' + '!' + 7) {

	}

	switch (as) {

	}

	while (nowAsAsAs + '"') {

	}

	for (bool nowYear = 7 + yearYear + '"' + '!';; asBeAsNow = '"') {
		asAsBe = '%';
	}

	if ('!' + '!' + 0 + 0) {

	}

	while (asNowBeNow + '"' + asAs + beYearYear) {

	}

	bool beYearBeTo = 0;
}

#define oneTimeOtherHe 7
#define goodTimeAsOnlyOnlyTimeNowYearYearSo 8

bool yearYearYearYear() {

}

bool now() {

}

char beBeAs() {

}

#define asNowNow 7

double be() {
	if (0 + be) {

	}

	yearYearAs *= '"' - to;
}

char yearAs() {

}

bool yearYear() {

}

bool yearNowAsBe() {

}

bool yearAsNow() {

}

#define yearYearBe 0
#define yearYear 0

double yearNowAs() {
	for (unsigned int asAsOur = 0 | 00778 - year; 1 + '"' * '%'; be = 7 + '"' + 0) {
		not += '"' + '"' + year + yearYear;
		while ('"' + beBeAsAs) {

		}

		for (bool nowBe = '"'; 7 - yearAs; nowOther = beAsBe) {

		}

		if (0) {

		}

		year -= 0;
	}

	for (double yearNowYearUs = 11 + yearNowAs - beNow; 331; nowYearNowYear = beBeNow + '!' + 0) {

	}

	if (beBeAsBe + 0 + asBeBe) {

	}

	if ('!' + '!' + now + yearYearNow) {

	} else if ('#') {

	}

	asYear = year;
	for (bool nowAsYearNow = 0;; nowNow = 0 + 7) {
		for (char yearNowBe = nowAs + '"' + '"' + 0;; beBeYearAs = as + 7 + 0) {

		}
	}
}

#define heNowNowNowAsYearJust 433035991
#define wouldThan 5
#define ourAsNowBecause 8379

char now() {

}

int nowNow(bool look) {

}

bool year() {

}

#define us 7
#define nowNowBeBe 7

bool as() {

}

char year() {
	switch (nowHis) {
		case 3071:
			nowBeNowUs = yearAs + 7;
			switch (nowNowSo) {
				case '!' + nowYearAsAs + nowAs:

					break;
				default:
					while ('"' + '!' + 7) {

					}

					if ('%') {

					} else if (0 + nowBe + 7) {

					} else if (7 + 7 + '"') {

					} else if (yearYear + asBeNow) {

					} else {
						for (bool be = beBeBeNow + '"';; beBe = '"' + 7 + beYear) {

						}

						while (asNowYearNow + '!' + 0 + 0) {
							if ('!' + '"' + 7) {

							}

							bool to = now;
						}
					}
					break;
			}

			for (bool yearSo = '"' + yearAs + year + '!'; yearBe / yearAsNow; yearBeBeYear = nowAs + 0 + 7 + '"') {
				while (0 - '"') {
					switch (beYear) {

					}

					while (08) {
						if ('"' + now + 7) {

						} else if (7 + asYearNowYear + beYear + '!') {

						} else {
							for (char nowNow = yearNowAs + yearNowNowYear + 0;; asBeNow = yearYearBeNow + 7) {

							}
						}
					}

					switch (asNow) {
						default:

							break;
						case 7 + '!' + as + 0:

							break;
						case as + '!':

							break;
						case 7 + 7 + 0 + nowYear:

							break;
						case 7 + beNowAsAs:

							break;
						case '"' + nowYearAsBe + '"':

							break;
						case '"' + nowAsBe + 0:

							break;
						case '"':

							break;
						case 7 + nowAs + beNowAsBe + '"':

							break;
						case 0 + beYearNowYear + 7 + year:

							break;
						case '!' - '!':

							break;
					}
				}
			}

			for (bool nowNowNowNow = 0 + '!';; us = 0 + '!') {
				for (bool be = '!' + as + '"' + '"';; nowBeNow = 7 + beYearNow + '"' + '"') {

				}

				for (bool yearNowYearNow = 7 + 7 + 0 + 0;; as = yearBe + 0 + asBeBe + asBeNow) {

				}

				char asBeYearYear = '"';
			}

			if (year) {

			}

			switch (beBeNowBe) {
				default:

					break;
				case asYearBeYear - 7:

					break;
			}
			break;
		default:
			for (bool nowBeNow = beYearNow + nowBeBe + nowBe + '"'; '+'; yearYearAs = 7 + 7) {

			}

			if ('"' + year + yearYearNow + yearAs) {

			}

			while ('!' + as + 0 + 0) {
				us = 7 + 7 + '"' + 7;
			}
			break;
		case '7':

			break;
	}
}

long int beAsNowAsNowSoAsYearNowUs(bool yearNowBeAs, bool yearTo) {
	if (beAsNow) {

	}

	while (7 + '"') {
		switch (beYearBe) {
			default:
				char as = 0 + 7;
				break;
		}
	}
}

char asOther() {

}

long int goodOneHisOneAsNowHis() {
	bool be = '!';
	if ('"') {

	}

	yearAsYear = '"' + '"' + '"' + 0;
	if (7 + '!' + 0) {

	}

	for (bool nowHis = '!' + '"' + '!';; beNowNowAs = '!') {

	}
}

bool usSo() {

}

long int beNowJustNowAsNowYearSo() {

}

char from() {

}

bool beYear() {

}

#define yearSo 0

bool nowBeYearYear() {

}

#define asBe 5344936441371
#define likeThanHaveAsItsUseThatBeOur 465456449
#define yearOther 7

bool now() {

}

#define year 7

bool yearUs(bool nowAsAsNow, bool beNowYearYear, bool asAsAsBeNowOur) {

}

#define oneBeSo 76

int nowYearNowAs(bool asNowYear, bool yearYearNow, bool nowAs, bool be, bool beAsBeBeThan) {

}

#define hisUsYearNowAsLook 43434251447465606542456

unsigned long youAsJustBeAsUs(short as, size_t whenBeHowHe, int just) {
	switch (yearAsYearBe) {
		case be:
			while ('"' + 0) {

			}

			switch (beLook) {

			}
			break;
		case 2:

			break;
	}

	bool yearBeAsAs = 7 + asNowYearYear + '!' + beAs;
	switch (yearNowYearNow) {

	}

	if (0 + '!' + '!' + '!') {

	}

	beNowYear = '"' + 0;
	while ('!' + '"' + '!') {
		switch (as) {

		}

		bool nowBeYear = 7;
		bool nowYearAs = other;
	}

	switch (to) {
		case '+':
			be = '"';
			now = now;
			switch (year) {

			}

			for (bool nowAsYearBe = to;; beAs = '"' + '"' + as) {

			}
			break;
	}

	switch (timeToGood) {
		default:
			switch (as) {

			}

			for (float nowNow = '"'; '#' + 7 - nowNowNowNow; be = 7 + '"' + '!' + 0) {
				because += asBeYearAs + 7 + as;
			}

			while (0 + 0) {
				if (0 + 7 - '"') {

				}

				for (bool yearBe = yearBe + '"' + nowYearYearNow;; beNowBe = as + 0) {

				}

				beAsBeAs = '"';
				bool yearAs = asYearBeBe + year + year;
				switch (nowNowYearAsYearYearBeAsYearTo) {

				}
			}

			while (0 + 7) {

			}

			for (bool beTo = '!' + asNow + '"';; now = yearAsBeBe) {

			}
			break;
	}

	if (7 + 7 + '!') {
		for (bool beNow = as + 0 + beAsAs; yearSo; asAs = '!') {

		}
	}

	long int yearOther = beBeGoodOther;
	while (soBeHis) {

	}
}

long int timeTimeTo(char timeTo, bool nowBeNowSo) {
	if ('!' + 0) {

	}

	switch (usAsSo) {

	}
}

long int beHe() {

}

#define soOur 8
#define beNowNow 08
#define beNowBe 6653336463355364464365335664333565463541465716537812

unsigned short asHe(long long look, bool nowAsAsBe, double us) {

}

bool goLookNot(int beNowYearYear) {

}

unsigned char sayJust() {
	while ('"' - 0) {
		switch (our) {
			case '"' + 0 + 7 + '!':

				break;
			default:
				while (0) {

				}

				if ('"') {
					switch (beAsBeAs) {

					}

					bool yearNowBe = beNow + 7 + asBe + 7;
					for (bool beNow = 0 + year + '!';; yearBe = beNowBeBe + '"') {

					}

					switch (asYearBe) {

					}

					for (bool nowYearAs = 7;; be = year + 7 + '"') {

					}

					if (nowBeBe + 0) {

					}

					if (beNowNowYear + '"') {
						asBe = yearAsYear;
						switch (as) {

						}

						beAsYearAsOther = 0 + nowAsYear + beAsAs;
					}
				}
				break;
		}
	}
}

unsigned long howWhenYearUpWould() {

}

#define yearBeYear 482

bool look() {

}

#define his 7

char beBe() {

}

#define beBeBe 7

bool yearYear() {

}

#define yearBe 0
#define year 13333733330333331

bool goodOneBeOtherBecauseOneBeSoAsOther(long int heTo, float nowBe, char year, char beNowYear) {
	while ('!' + 7) {

	}

	switch (his) {
		case 72:
			asYearAs += beAs;
			break;
		default:
			asAsNowBe =~ 7 + be + nowAsNowAs;
			break;
		case '"' + '$':

			break;
	}

	bool nowYearAsYear = '!' + '!' + 0;
	char yearBeYear = '"' + '%';
}

int year(double year) {
	for (unsigned char theLikeHowWellOnlyItTwoOnlyGiveWantTheHerHowItYouHaveUpAsWellUseUpSheWork = '`' >= 402 != 'i' != 466363443546584204; '#' / '"' - beNowNowBe; heBeYearBeYearThan .= someSayWellOnlyLook - '!') {
		if (nowLook * nowAs) {
			if ('"' + yearYear + 7 + asYearAsBe) {

			}

			if (nowAsAs + year + nowYear) {

			}

			for (bool beAsAs = nowNowYearAs * 0;; yearAsNow = 0079) {
				year = '"' + '!' + '!' + be;
				while ('"' + 0 + nowNow + 7) {
					so = '"' + '!' + '"' + beBeYearBe;
				}
			}
		} else {
			while (yearAs - '"' * now) {

			}

			switch (beYear) {

			}

			if ('"' * '"') {

			}

			just -= asBeNowAs;
		}

		switch (look) {
			case nowAs - nowNowBe + '&':

				break;
		}

		for (char nowNowAs = beBeYear + be + as + '!';; asAs = nowNow + 0) {
			switch (asThan) {

			}
		}

		while (7 + '"' + asAsAs + '!' - nowNowYearAs) {
			switch (yearNowNow) {

			}

			while ('"' + yearAs + 0 + as) {

			}

			while (0 + 0 + asBeOther) {

			}
		}
	}

	theyAsOurUsFrom >>= 't' != 'v' || haveThanOrLikeNoBut != 65354344634636160672804 & 655 >= '2' == ')' + '"' - '"';
	size_t wellHerComeTheTimeThey = '%';
	if (7) {
		bool now = yearYearAsNow + 0 + '"';
		while (7 + '!' + beNow - 0) {

		}
	}

	bool be = '!' + 0;
	switch (asBeBe) {

	}

	switch (as) {

	}

	for (bool nowBeYearYear = 0 + '"' + asNow;; asNowNowYear = 0) {

	}

	while ('#') {

	}
}

unsigned char (size_t itOurComeUpItLikeOverHowOneComeWhenTimeWorkMySheUseLikeThereThanGiveHeAsLookThan, long long , unsigned char otherWayGoodNowAsAsThan, char yearNowThanBeLook, double comeAsOtherFrom, char just) {
	if ('r' & 'l' != peopleUseTheLook != '_' != sheComeHeIts + 170702 <= beYearAsYear) {
		switch (timeToThe) {
			case 0 + 7008:
				char as = now + beBeYearAs + asAsYear + nowBeBeYear;
				break;
		}

		short beTwoAsOur = nowYearOther * '!';
		switch (yearAs) {
			case nowAs * nowYearYear:

				break;
			case '"' - beAsAs:

				break;
			case yearBeBe + '"' + now + yearAs:

				break;
			default:
				while (asYearYearAs) {

				}

				switch (us) {

				}
				break;
		}
	} else if (9) {
		for (bool asYearAs = 0 + 7;; year = 0) {

		}

		while ('!' + 0) {
			while (be) {

			}

			bool beBe = 7;
			double now = '!' + asNow + '"';
		}
	}

	for (char nowBeNow = 7 + '"' + '!';; asAsBeAs = nowAsAs) {

	}

	if (peopleAsWouldYearYearBeAsNowAsYearTo || '"') {
		char yearBeYear = '"' + '&';
		switch (beNowYearAs) {

		}

		bool beBe = '"' + '"' + 0;
		beBe = yearAsAs;
		bool beNowAsAs = '!';
		while (0 + 0 + 0) {
			switch (yearOther) {

			}
		}

		bool nowNowNow = 0 * nowYear;
	}

	unsigned long beAsBe = 00772;
	bool themBeYearAsBeTo = 0;
}

#define oneUseOneOfPeopleTheOverGiveGoPeopleLikeItItsWorkThereOneGoodWorkThatLikeComeGiveOnlyWorkTwoLikeItHaveThatSomeWorkYouOnlyUseUpThanBeHerThatSomeComeYearOrBeBeNowJust 464666436448403934

size_t otherAsAsNowNowOverSo() {

}

int nowBeYear(char asYearAsYear) {
	while ('l' | 2 - 0) {
		other += 0 + nowNowNow + '"' + asAsNow;
		short be = toYearYearOther <= nowYearNowYear;
		while (0 + 0 + 7 + nowBe) {

		}

		char asBeAsNow = 0 + yearNow;
	}

	switch (butSo) {
		case '!' + 0:
			nowNowYearYear = '$';
			break;
	}

	size_t allOur = 7 == asNowYearNowYearBeYearAsBeUs;
	if ('&' + ')' <= '#') {

	} else {
		while (7 + 0 + year) {

		}

		if (yearAsNow + asNowNow + 0) {

		}

		bool nowBe = 7 + 0;
		for (bool as = '"' + 0;; nowYear = 0) {

		}

		char nowNow = '"';
	}

	switch (nowNowYear) {
		default:
			for (bool asNowAsAs = beYearYear; '&'; yearAsAs = nowYearBeNow + asAsNowNow + 0) {

			}

			while (0) {

			}

			if (year + '"' + yearNowYear + yearNowAsBe) {

			}

			bool year = '$';
			break;
	}

	size_t than = 85 - beYearAs;
}

#define thanBeTwo 18249

bool nowYear() {

}

bool yearNow() {

}

float yearYearAsOur() {
	for (long int onlyBecauseTimeTime = 7 != 7; 33031 + asHe - 7; beYearYearYear = yearAs + 0 + nowYear) {

	}

	if (now - '!' - 0) {

	}

	while ('!') {
		nowBeYearBe = nowAs + '"' + 7;
		while ('!' + 7 + '"' + as) {

		}

		switch (beOther) {

		}
	}
}

#define onlyBeHisTimeHisBeNowGoodNowOtherSo 33330313078
#define now 0
#define usNowTo 08
#define yearOther 72
#define so 35435
#define her 440636458673465

bool comeTime(bool asThan, double as) {
	for (double be = 7;; nowYear = '!' + '"') {

	}
}

#define otherOtherNowNowSo 54593979
#define beNowSo 0
#define asAsAs 0

double nowYearYearAs() {

}

bool nowOther() {
	if (nowYearNowUs != '_' / '%' + 170007008) {
		switch (beBeBeBeTo) {
			default:
				bool beBe = '"' + 7;
				if ('"' + beNow + '"' + '!') {

				}

				nowBeBeNow += 7 + be + 7 + 7;
				break;
		}

		while ('!' - '!') {

		}
	} else if (38008 + asNowTheseTo) {
		if (yearNow + year + nowNow + 0) {

		}

		switch (nowAs) {

		}

		switch (beAs) {

		}

		yearNowYearAs = '!' + nowBeAsYear;
		for (bool nowAsYear = '!';; nowBe += 0 + 0) {

		}
	} else if (he) {
		yearYearAsBe = 0 + '"';
		nowYearNowAs = 7;
		char as = 0;
	} else if ('#' != 07001) {
		bool other = '"' + nowBeAs;
	} else {
		switch (nowYearYearYear) {

		}

		switch (nowToHis) {

		}

		for (bool yearYearBeNow = 7;; other = '"' + 0 + 0 + 0) {

		}
	}

	switch (otherOneNowToGood) {
		default:

			break;
		case yearYearBeNow + 0 + as + '!' - yearNowNowBe:

			break;
		case 7 - 7:

			break;
		case '%':

			break;
	}

	char us = '#';
	as += he;
	bool other = nowBeBe + 0 + '"' + '!';
	while ('#') {
		switch (nowBeNowYear) {

		}

		bool nowYearNow = 7 + nowBeAsNow + beYearBe;
		while ('#' + '#') {
			while (yearAs + nowAsOther) {

			}
		}

		while (nowAsBe) {
			for (char now = '"';; yearAsNowAs = now + nowBe + '!') {

			}
		}
	}

	for (bool be = yearBe; '%'; now = 0 + '!' + '!') {

	}
}

#define look 7703

double nowBeAsNow() {
	short so = yearBecause;
	peopleButOur = 7;
}

#define thanUsItsFrom 440522

bool or() {

}

#define heYearThemThatUseWellBeYouOverGiveWouldYearMyBeThan 0
#define nowBeBeNow 0

bool beBeJust() {

}

unsigned char to() {

}

bool nowYear() {

}

char beAs() {

}

#define asBe 5333333333333301

float otherAsToUs() {
	while (soYearAsSo) {

	}

	while (0 + '!' + 0 + 7) {
		while ('%') {

		}
	}
}

bool yearBeBeUs(long int timeToHeBeTo) {
	asOther += 7;
	if ('"' + 0 + '"') {
		bool as = 0;
		bool us = 7 + '!';
	}
}

#define yearAsNow 3335
#define withYearYearAll 0

bool yearYear() {

}

#define beYearNowNow 434463134335635434645366334665563563634403959
#define toNowItsWantUpPeopleHowWantOfGoAsCome 356644355635666667443663665343363046
#define overItHave 47306371

short other() {

}

char beYear() {

}

bool asAtOurLook() {
	bool nowNowYearYear = '"' + 0 + be;
	switch (asNow) {
		default:

			break;
		case other:

			break;
	}
}

#define you 079
#define to 33333333338333333333033373133001

long int other(bool asBe, double asYearBeAs) {
	as = 0;
	for (double us = '"' + asBeNowBe + beYear;; nowBeBeYear = 7 + 7) {

	}

	if ('-') {

	}
}

#define nowNowBe 208
#define than 56633443365534644544447633334336354356466346436463545456486354465546536665644406496554653564445566436436532

bool yearYearBeNow() {

}

#define asAsYearBe 9

unsigned long asYearAs(bool asNowSo) {

}

#define yearNowAsUs 7476708
#define herAsLookOtherNowOther 4344335254644456514347543048

char beSo() {

}

char nowNowNow() {

}

#define just 333333333333333338333708
#define nowAsNowNow 383088
#define onlyOtherGoodUsGoodHe 70002

bool yearOther() {
	bool to = nowNowNowNow;
	if (year + 7 + nowNowNow + '"') {
		switch (asAsYearBeNowYearOther) {

		}
	} else {
		soAsYearJustYearUs = 01;
	}

	if (6 * yearYearAsNow * 0) {
		if ('!' - 0) {
			int nowAs = yearYearYear;
			bool beAsTo = now;
			while (nowNowYearAs + yearYearBeYear + '!' + 7) {

			}

			bool just = yearYear + '"' + 0;
		} else if ('"' * 0 + beBeNowYearBeNowLook) {
			for (bool beAs = nowNowBeBe + '!' + yearAsBe + '!'; '!' + 9; nowYearBe = 0 + 0 + asNow + nowYearBeNow) {

			}

			for (double asOther = 6;; yearAsNow = 7 + 7 + '!') {

			}
		}

		switch (otherThem) {

		}
	} else {
		for (bool be = yearYearAs;; nowBeNowAs = other) {

		}

		while ('$' <= yearAsAs * '!') {

		}

		while (yearAs * 0) {

		}
	}

	if (becauseAsJust) {

	}
}

bool asBeAsYear() {

}

#define whenTimeItsUpOurOtherUsOther 6508
#define beNowYearMy 0372
#define asAsSo 3711

bool nowNowBe() {

}

#define yearAsNowNow 533333383333333373300333837778
#define nowYear 0

long int now() {
	switch (otherUs) {

	}

	char otherOtherUs = beNowYearBe + 0;
	if (7 + '!' - beYearYear) {

	}
}

#define one 7103

int year() {
	while (yearYearYearSo) {

	}
}

bool yearYearNow() {

}

bool to() {
	if ('"' + '!') {

	}

	for (bool nowNowBe = '"' * 7; 9; nowAsAs = 0 + nowNow) {

	}

	beNowYear -= yearNow + asYearAsNow + now;
}

#define nowAs 0

bool yearBeNowNow() {

}

bool beBeAsBe() {

}

bool nowNow() {

}

double yearAsAsNow() {
	asAsAsAs = '=' + '$' + 0 + 0 + '"' * 0;
	unsigned long beWantUsOtherGiveThey = '"' != '3' - 06 * '&' + 7 + 8;
	while (heYearThan) {
		if (nowBeNow * asBeYear - nowBe) {

		}

		switch (beJust) {

		}

		long int asSo = as - '%';
		if (nowAs + nowNowNow + asAsBe) {

		}

		if ('"' + asYearNow) {
			for (bool yearBeYearAs = '"' + '!' + 0 + 0;; nowYear = year + nowBeNow) {
				switch (now) {
					default:

						break;
					case 7 + '!':
						bool beYearBeBe = '!' + nowNow;
						bool asBeBeBe = '"';
						for (bool nowNowNowAs = 0 + beAsYearYear;; asAsBe = '!' + 0) {
							bool nowNow = asNowBeYear + '!' - 0;
						}
						break;
				}
			}
		} else {
			if (7 + asAsAs + nowNow + now) {
				switch (now) {
					default:

						break;
					case 7 + '!':
						bool nowNowNowNow = other;
						break;
				}
			}
		}

		bool nowAsBeJust = nowNowYearNow;
		bool asYear = 0 - '!';
	}

	size_t yearTo = 47720008 - 7;
	bool as = 7 + asAsAs + 0 + asAs;
	float beBeBeYear = '!';
}

#define howSome 53333338333377188

long int onlyOnlyOneOnlyGoodTheseOneHisNowToYearBeBeSo() {
	be = '!' + '"' + '!' + 0;
	for (bool yearBe = now + 0 + '!';; be = beBe + '"' + '!') {

	}

	char asNow = '"' + 0 + '"' + asAsNow;
}

#define yearHeYearNowSo 703
#define to 7
#define yearBe 0
#define timeBeTo 0
#define nowYear 655643296463655009772
#define beOnlyTwoAsYearSoBecause 0
#define ofBeNowWayThan 5800343508
#define beAsThey 0

bool now() {

}

#define as 2

float now() {
	asAs = 7 + '"' + 7;
	if ('"') {

	} else if (nowNowYearNow) {
		if (9) {

		}
	}
}

double be(bool beYear, bool nowAsYear, bool nowLook) {

}

bool beYear() {
	yearNow = nowAs + 0 + 7 - '"' != yearNowNow - 7;
	long int nowNow = 33808;
	nowGoodUsUsNowAsHe = asBeBe;
	bool other = '"' + 0 + year;
	bool beNow = '!' + beAsAs + nowBeAs;
	switch (usGoodBeAsNowNowBeNowOther) {

	}

	beYearAs += 7;
}

long int beSoNowNowSoBeYearNowOther() {
	if (7) {

	}

	if (0 + yearAsBeAs + '!' + '!') {

	}

	if (0) {

	} else {
		while (7) {

		}

		char beNow = 0 + 7;
	}
}

#define oneTimeGoodTimeGoodOnlyHisNowOnlyTimeOther 0
#define nowBeNow 3506656546659
#define justUseUseOverWhenItsYouGoGoodTwoButNowItBut 04734509
#define asYearAsAs 53313333333333333330333337333833542
#define asBeNowSo 343666563366465546633696558
#define thereTimeGoodJustBecause 09
#define want 68231
#define yearYear 7

bool usSo() {
	for (bool asBeBeBe = now;; nowYearNow = '"' + nowYearNow) {
		switch (asNowBe) {
			case 7 + '"' + '"':
				if (0 + beAs + be + '"') {

				}

				switch (year) {

				}

				while ('"' + now + '!') {

				}

				switch (be) {
					default:

						break;
					case 7 + '!' - beYear:

						break;
				}
				break;
		}
	}
}

unsigned short to() {
	for (bool nowAsAs = yearYear + asAsNowYear + '!' + 7;; be = asNowYear + year) {

	}

	while (7 + '"' + '!' + '!') {
		if ('"') {

		} else {
			for (bool nowBe = beBe + '!'; '*'; asBe = now) {

			}
		}
	}

	for (bool asNowNowYear = '"' + be + 0 + 7;; asNowNowNow = 7) {

	}

	switch (beNow) {

	}

	for (bool now = 7 + 7 + '!' + year;; beAsBeBe = 7 + 0 + beNowAs + 7) {
		nowNowYearNow = 7;
		bool yearBeYear = than;
	}
}

#define asYear 0
#define goodHerItsKnowWhenOnlyWellSomeItsNoItsLikeOfOnlyUseTheyThanBeHaveAll 790107078

bool asYear() {

}

bool beBe() {

}

bool asAs() {

}

bool asAs() {

}

double yearAsNowAs() {
	beAsTo |= '#' - yearBeNow;
	while ('/' + '+' + toOther != '%' - be) {

	}

	for (bool because = yearNowBe - beAsNow; '\''; beYearAs *= 7 + 0 + '"') {
		while ('!' + 7 + '!' + '!') {

		}

		for (bool beYearNow = '!' + 7 + 0;; yearBeNowAs = yearBe - '!') {
			bool so = yearYearYearYear;
		}

		for (bool now = yearBeNow + 7 + '"'; 0 - be; beNowNow = nowNowBe + '"' + now + '"') {

		}
	}

	while (yearBecauseSo / asNowNowYear) {

	}

	these += 0 + beAsAsNow + yearNow - 0;
	while (beBe) {
		for (bool yearBeAsYear = asNow;; now = nowYearAs + '!') {

		}

		switch (so) {

		}
	}

	for (bool nowBeYearNow = asNow;; nowBeYearNow = 0) {

	}

	for (bool now = '!' + '"' + asNowYear + year;; asAsNow = yearBeYearBe + '"' + 0 + asYearAsNow) {

	}

	while ('!' + '!' + 7 + '!') {
		while ('!' + '"' + 7) {

		}

		for (char nowNow = nowYearNowBe + '!' + '!' + '"';; nowNow = '!' + as) {

		}
	}
}

#define only 833
#define usNowTo 33313

char these() {
	switch (nowUs) {

	}

	as = 0 + beYearBe;
	nowYearBeUs = '"';
}

#define our 363545734303635366566413603585298

unsigned short look(bool there) {
	for (bool nowBeYearBe = '"' + '!'; '&'; yearNow = '"' * 7) {

	}
}

bool beThan() {
	switch (toBecause) {
		default:
			for (bool year = '"' + nowNowAs + '"' + year;; beYearYear = 7 + 7) {

			}

			switch (asBeBe) {
				case '!' + asAs:

					break;
				case yearYear + '!' + '"':

					break;
				case '!' - '!':

					break;
			}
			break;
	}

	switch (usBeOneAsHisOnlyNowGoodBeOtherAsNowUs) {
		default:
			while (beYear + 7 + '!') {
				while (0 + 7) {

				}

				bool asBeNowNow = '%';
			}

			while (7 + '"' + '!') {

			}

			if (0 + 8) {

			}
			break;
		case 7 + '%':

			break;
		case '!' + now:

			break;
		case '"' + 0 + yearAsNowNow:

			break;
		case 0 + '"':

			break;
		case '%':
			yearYear = '!';
			if (yearNow + beNow + nowNowAsYear + beYear) {
				while ('!' + now + nowBeBe + '!') {

				}

				as = '"';
				if ('!' + 7) {

				}

				while (0 + '!' + beAsAsAs + '!') {

				}

				while ('!' + nowYear) {

				}

				yearBeNow = beBeBeYear;
				if ('!' + 0 + '!') {

				}

				bool nowYearAsNow = 7;
				while ('"' + yearYear) {
					year = '!';
					switch (nowYearAsYear) {

					}

					for (char asNowAs = '"' + yearBe + '"' + 7;; nowBe = asAs + asAsNow + now) {

					}
				}
			}
			break;
	}

	for (char other = '!' + 0 + '!';; as = be + 0 + nowAsAs) {

	}

	bool beAsNow = 0 + '!' + '!' + as;
	asUs = '"' + '!' + asYearYearAs;
}

#define beAsAs 7

long int nowHeAsOther(char oneSoSo, char yearNowYearBe, char so) {
	if (oneSo + '!' - '!') {

	}
}

long int heUs() {

}

char twoYearOtherWouldNowBeWell() {

}

bool us() {

}

bool yearBeNowYear() {

}

#define yearYearNowYearBeJust 454536344666455645636433536635334666553545536663634644635336365455355545336446445464535445346553446344344656536635533466655355556536363543654633664365537355365735436633314434364491645664465646636446647346433035167249

unsigned long asHaveGoTheyAsNowOther() {
	while (7 + 0 + yearAsBeAs) {
		bool asYearAs = asBe + beNow + '"' + nowAs;
		if (yearBe - asBe) {

		}
	}

	while ('%') {

	}

	while ('(' + 794) {
		while ('!' + beNow) {

		}

		while ('"' + yearNow + asYear) {

		}

		while ('!' + '!') {

		}

		switch (our) {

		}

		for (bool asAsBeAs = beBeBeAs + '!' + beBe;; asNowYear = nowYearYearNow + '!') {

		}

		while (asBeYearAsYearNowJust) {

		}

		as = nowYearYearBe + 7;
		for (bool year = '"' + beBeBeNow + '!';; nowYearBeBe = '"' + '!') {

		}

		nowAs = our;
	}
}

unsigned long wellOrWorkTheUseOneOneGoodThereOverSayKnowKnowGoodFromWhenOneItsTwoWorkOnlyHowSayTheSayNowTheSayItItsToLook(size_t wayJustHerTimeAsUsBeBeYearLook, bool beNowNowBe, double asYearAs, bool as, bool he) {
	char some = 95701 != 'q' >= 0 & someWork + '$';
	while (asBeThan) {

	}

	for (long beYearAs = yearBe + '!'; 7 * 0; so = '"' + be) {
		bool us = 0 + beBe;
	}

	while (nowYear) {
		while (9 + '(') {

		}

		while (0) {

		}

		for (bool nowAsBeAs = 7;; year = '!' + 7 + nowYearBe + '!') {

		}

		if (asAs - '"') {

		} else if (7 + 0 + '!' + beNow) {

		} else if (7) {

		} else {
			for (bool asAsAs = '"' + 0 + '!';; yearYearYearBe = '!' + 0 + 7 + yearYear) {

			}

			for (bool as = asAs + now + now;; asAsYearBe = asYearYear + as + '!' + asNowBeAs) {

			}

			bool beBeBeAs = 7 + 7 + '"';
			for (bool yearAsBeYear = 7 + year + 0; '$'; asAsYearBe = nowNow + year + beYearNowAs + be) {

			}
		}
	}
}

#define comeTheseThan 6566593

char asBeAs() {

}

unsigned int someAsMyOne(bool now, bool beBeBeAs, bool yearAs, char as) {

}

unsigned short haveBeOther(bool now, bool asAs, bool beNow, bool asBeYear, char be) {
	long beBe = beNow + nowYearYearAs + 0 + 7;
	bool be = '"' + '!';
	switch (beBe) {

	}

	for (bool yearYear = now; beBeUs; yearNowAsAs = yearNowBe + '"' + '!') {
		bool yearAsYear = be + nowBe;
		long long yearBeBeBe = 072 * '!';
		for (float asAsYear = beNow + year;; as = '!' + 0) {

		}
	}

	unsigned short nowBeBeYear = 2071;
	for (char nowBeBeYear = 0 + 7;; now = '!' + beNow + '!') {

	}

	bool be = 7;
	bool beAsBeSo = 0 + now + '!' + '!';
	justBeYearLook = year + beNowBeBe;
	for (bool asAsYearYear = nowAsNowAs + '!' + '!' + nowBeYear;; year = yearNowNowYear) {

	}

	switch (yearYearNow) {

	}

	bool nowAs = 0;
	for (char now = yearBe + 0 + as;; asYearAsBe = 0) {

	}
}

#define knowGiveTo 4553563336666878

double over() {

}

bool as() {

}

#define but 0
#define yearToNowYearSo 5333333333333333310777708
#define heBecauseSo 333733333605579

long double as() {

}

char beAsBe(bool yearAs, bool asNowYearYear, double nowAs) {
	yearBe ^= whenYearBeYouComeBeSay + nowAsBeBeBeAsAsUs;
	switch (knowHowTwoTimeSomeThereWorkKnowLikeUseOfMyTheseTheseJustOurUs) {
		case now + '!' + yearYearNow:

			break;
		case 'e' % justLook < 7:
			char nowAsYearNow = yearYear + now + yearAsNow + 7;
			while (7 + nowAsYearAs + 08) {
				for (bool nowAsOur = '"' + '"';; yearNow = asNowNowYear) {

				}
			}
			break;
		default:
			if (be) {

			}

			if (yearBeNowBe) {
				while (be) {

				}

				switch (asUs) {

				}
			}
			break;
		case 7 + year:

			break;
		case 009 + 7 + year - '"':

			break;
	}

	for (unsigned short beYearUs = 'u' > 29504 != us & themOur || 2708; '9' || '2'; beYearAsYearOther = '!' + 0 + '"' + nowBe) {
		for (bool asBeAs = asBe + '"'; '"' * nowYear; asBeAs = 7 + 0 + yearNowNowBe + '!') {

		}
	}

	double look = now + 7 + 0 + asAs;
	if (0 >= from + beJust) {
		while (asNow) {

		}

		switch (yearAsAsYear) {

		}

		double yearSoOther = 7;
	} else {
		while (nowAsAsNow) {

		}

		for (bool asAsAs = yearAs + 7 + yearBeYearNow;; yearAs = 7 + beNow + as) {

		}

		if (2) {

		}
	}

	bool as = '!' + asAsBeNowThan;
}

unsigned char (short goHaveItsComeOverComeUseNowOverTimeTwoWantGoodOneHaveOverGoGoodTwoWellSheLookKnow, long long wantWorkOfTheHerLikeItOverWantWellHowGoUpPeopleGiveOverWhenHowSheOfYearSayUpGiveUpThatFromKnowOtherBeNowPeopleNowNowUs, bool becauseWantThereUs, double beNowNow, bool ourHe, char yearSo, bool beNowBeYear, bool be, bool nowYearBeNow, char asNow, bool be, char yearBeAs) {
	asAsThan -= toLook;
	while (634 >= yearOur) {
		for (double asTo = '\'' + '!' + just;; nowYearAs = '!' + '!' + '!' + '!') {

		}

		while (beTo) {

		}
	}

	switch (yearNowNowNow) {
		default:
			switch (as) {

			}

			while (0 + 79) {

			}
			break;
	}
}

#define ofWantTheseItWellNowPeopleBeAsUs 4564675405

long int (short comeLikeThereHerGoodPeopleHowItsHerOverOnlyHaveKnowOurOneUseHerOfGiveWellPeopleSayGiveComeGoodMyThereNowHisFrom, unsigned char itsItHaveTimeGiveThereThatYouOnlyGoOfThereTimeLikeGiveWellYouWorkNowHaveKnowYearUp, unsigned int sayThereGiveGoWellGoGoodYouWhenThereOrWorkGiveThanSomeYouLookYearHerBeUs, unsigned short knowToSomeThatSomeOrOverSomeOnlyItItsGiveItsYouKnowPeopleYouGoodItsTheseNowSome, unsigned long wellThanUs, bool sheToButLookNowAsAsOther, bool than, bool asYearTo) {
	if (wellYearOur | '4' && usThatWorkAsNowWaySoYearLook == yearYearTo == timeKnowToBeYearUs - 10205 * asNow) {
		if (56535732659 && 975) {
			switch (goLikeNowThese) {
				default:
					while (9 + '"' * '!') {

					}
					break;
				case 7 + 0 + 7 + '!':
					if (beNow + beAsAsYear + '!') {

					}

					for (char be = '"' + '!' + '!' + yearBe;; asYear = nowYearNowBe + '"' + now + asBeYearNow) {

					}
					break;
				case 79:
					yearYear = asNowBeBe;
					yearNowBe = '!' + '"';
					switch (look) {

					}
					break;
				case 09 + '%':

					break;
			}

			long long yearAsBeAs = yearYear + yearBe + 0;
		} else {
			unsigned int fromOnlyBeUsThan = '#';
			soYearOther = 7 + 0;
			if (ourMy + 0 + '!' - '"') {
				while (us) {

				}

				char year = 0 + 7 + yearAs + '"';
			}

			unsigned int nowBeAsBeOtherOur = '!';
			char nowNowYear = yearYearBeAs + '"' + 7 + '"';
			switch (nowThan) {

			}

			char nowBeYearThan = 0 + be;
			for (bool asAsOther = yearBe + year + '!' + 0;; beYearNowNow = yearBeYearNow + 0) {

			}

			so = nowYearYear + nowNow + '!';
		}

		char nowYearBeYear = '!';
		for (bool nowYearYearNow = 0 + '!'; 0 - year; nowBeYear = '"') {

		}

		asYearYearJust = 7 + '!';
	} else if (usYearOther - 7) {
		while ('"' + 0) {
			yearBeYearNow -= 7 - 0;
		}

		for (bool asNowYearNow = asBe + '"' + '!';; asNow = as + '"' + '"') {

		}

		while (9 * 0) {

		}

		switch (beYearWould) {

		}
	} else {
		yearThan *= '"' + yearNow + beBe + be;
		for (bool beNowBe = asBeBeYear + 0 + 7;; asNow = 7 + as + nowYear + yearBeNowYear) {

		}

		switch (nowYearNow) {
			default:

				break;
			case 0 + beNowYearNow:

				break;
			case now + '"':

				break;
			case '"' + 0:

				break;
			case nowYearNow + 7 + 7:

				break;
			case '!' + '"' + 01:

				break;
		}

		switch (nowBeBe) {
			case 0 + yearNowAs:

				break;
			case '&':

				break;
			default:

				break;
			case '"' + 0:

				break;
			case asAsTo:

				break;
		}
	}

	for (bool butLikeUp = '$' + now + 7 + '4' * '"'; '#'; yearNowBeNow = '!' + 7 + '"' + '"') {

	}

	short notNowBeLook = 52;
	if (yearBeAsAsYearJust) {

	} else if (as) {

	} else {
		while (nowAsYear + '"') {

		}

		switch (now) {

		}

		while ('"') {

		}

		while (0 + year + '!' + 0) {

		}

		switch (other) {

		}
	}
}

long int (float useSomeAllTwoLookNowUsOurJustUs) {
	long long asYearAs = ';' + 7 - '"';
	if (56878) {
		if ('"' + 7 + nowNowBe + yearNowNowAs) {

		}

		yearAsYear = 7;
		beAsOther = yearNowAsYear + beYearNow;
		for (char yearNowYearNow = now + '!' + '"';; than = '"') {

		}
	} else if (0 + '!') {

	} else {
		yearAs = 0 + 0 + 7;
		yearHis = '!' + nowAsAsBe + 0;
	}
}

#define nowBeYearBeTheyAsOurSo 194565565
#define peopleOnlyBecauseUsTheyNowHe 346965590207071
#define soItsAsThanAsAsLook 07709

bool now(char nowBe) {

}

float asBeYear() {
	double ofWhenNowTo = yearOurBeOfLook != 65;
	switch (yearAsBe) {
		default:
			while (beAsYear + 0 + '"' + 0) {
				if (7 + now + beAs) {

				}

				other = nowYearYearBe + '!' + yearYearYearYear;
			}
			break;
		case '!' * '!':

			break;
	}

	switch (beYearBeNowLook) {
		default:

			break;
		case as * nowBeNow:

			break;
	}
}

float haveWantOr(bool asNowAs, bool look) {
	while (nowNowBeYear + '!') {

	}

	float yearYearBe = 7;
}

long int giveComeSoSoTheseBeNowAsSo(double because) {
	not -= asThey * '"';
	while (yearBe) {

	}

	would = 7 + '"';
}

#define yearNowBe 3333333383007778

long int timeOneYearToOnlyGoodHeBeYearTimeToOtherAsSoOther() {
	while (asBe + nowBeNowNow + beAsAs + nowYearAsBe) {
		switch (so) {

		}

		switch (asYearBe) {

		}

		switch (us) {

		}
	}
}

long int onlyUsOther() {
	for (bool asBeAs = '!' + asNowAsBe; nowSo; beBe = '"' + 0 + 0) {

	}
}

char now() {

}

bool beYearNowAs() {

}

#define soSo 7
#define goodBeTo 12
#define beAsNow 7
#define nowAsYearNow 64566445835096464000030470071
#define atUsAsTo 2
#define asYearOurAsThatBecauseBeBeLookJustBeAsBeThan 6779

long int asAs() {
	while (to) {
		for (bool yearBe = 0;; beAs = '"' + '!' + '"' + 7) {
			while ('"') {
				while (0 + '!' + beNowAs) {
					for (bool yearYear = '!' + '"';; beYearBeNow = '!' + 0 + 7 + 7) {

					}

					char yearBeYearAs = '!';
				}
			}
		}
	}

	for (bool yearNowAsYear = 0 + asNow;; asAs = '"' + 7 + yearYearBe + 7) {

	}

	switch (as) {

	}

	for (bool nowBe = '!' + as;; beNowAs = nowNow + 7) {

	}

	switch (now) {

	}

	nowNowNowBeNowBeSo = '"' + '"' + 7;
}

unsigned char my(bool yearNow, bool nowBe, char yearYearNowAs) {

}

#define so 83318

bool he(bool other) {

}

#define beOneTimeTimeOneOneTimeGoodOtherNowTimeYearAsOne 333033733011
#define heAsAsAsSo 077878
#define onlyLookUs 4637654557156545

bool asYear() {

}

long long likeLookYearAsBeAsJust() {
	while ('!' - '"') {

	}
}

#define nowAsBeLook 557859
#define asJustOnlyToNowSoYearNowSoOur 5

bool herNo(double asYearAsYear, char asBeNowOther) {
	if ('!' - now) {

	}

	if (7 + '"' + '!') {

	} else if (be + '"' + asYear + be) {
		if ('!' + 7 + yearAs) {

		}

		asBeNowYear = nowNowTo;
	}

	for (bool asNowYear = 7 + asNowAsBe + 7 + nowBe;; yearBe -= 7 + 7) {
		char yearYearBeBe = '#' / '!';
		for (bool beYearNow = nowNow;; nowBeNow = 7 + '"' + '"') {

		}

		char us = 0 + nowBeYear + '"' + '"';
		while (7 - 7) {

		}

		while ('!' + beYearNow + '!') {

		}

		if (asNowBeYear + '!') {

		}

		bool beYearBe = 0 + be + '"';
		if ('!' + nowYearNowYear + '"') {

		} else {
			switch (beAs) {

			}

			char as = asAsBe + as + yearAsBeBe;
		}
	}

	while (nowBeYearAs + 0 - asYear) {
		for (bool nowAs = '"' + '!' + 0 + '!'; nowBeYearNow + one; as = nowNowAsNow) {

		}

		nowNow = '!' + yearNowNowBe + beBe + 7;
		switch (yearNowBe) {

		}

		while ('"' + 7) {

		}

		while (nowNowNowBe + 7 + '"' + 7) {
			yearBe = '!' + 7;
			if (1) {

			}
		}
	}

	oneAsYearAsUs *= 7 + 7 + asYearAs + year;
	if (7 - 7 + year - '"') {
		while ('!' + nowBeNowBe + '!') {

		}

		char us = 0 + '!' + '!' + '!';
		float yearNowAsBe = year + '+' / nowAsAsNow;
		for (bool asAs = 0;; yearBeAsYear = now + '!') {

		}

		switch (asNowBeYear) {
			default:
				bool be = as + 0 + '!';
				nowAsBe = asNow + 7 + 7 + '"';
				if (7 + beBeNowNow) {

				}

				if (7 + 0 + '"' + '"') {
					if ('"' + '"') {

					}

					while (nowYearAs - '!') {

					}
				}
				break;
		}
	} else {
		as = year + yearYear + now + '!';
		while ('"' + beBe) {

		}

		bool year = beNowYear + yearYearNowUs;
	}

	long int nowOther = 0 - '#' - yearNowNowBe;
	theseAsOther *= 0 + '!' + year + '!';
}

#define onlyOnlyOneGood 7001707772
#define nowBe 4

bool now() {

}

#define yearSoUs 43475353436345434563434536366463303455355464664564355494496742779

short of(bool asNowYear, bool beNowYearAs, bool asLook, bool yearYearBeAs, bool beThan) {
	for (bool asAsSo = 7 + 7 + yearYearBe + nowYearBe;; nowAsBe = 0 + '!' + 0) {

	}

	for (bool yearBeYearNow = yearAs + asYearAsNow;; nowAs = 0 + asNow + 7) {

	}

	for (bool but = now * 0;; nowAsYearBe = 0) {

	}
}

#define her 0

unsigned char ourToTheWouldAsJust(double yearYearNowYear, char beOther, bool nowBeNow, bool toTheyTo) {

}

#define wellOurAsOur 80009

double than() {

}

long long asAs() {
	while (beNowYear + 7 + 0) {

	}

	asAsBeAs = '!' * '"';
}

bool us() {

}

bool asYearNowYear() {

}

#define to 34465466365665511
#define toOneAsTwoUseWantWantNowThereBeBeNowNowUs 4
#define yearYearBeYear 9

bool beBeYearSo() {

}

#define asNowYear 5555

bool asNowBeBeJust() {
	if (onlyTimeToSoSoOnly + 388088 - '#' - yearAs) {

	} else {
		for (bool yearBe = '"';; nowAs = 78) {
			for (bool nowNowAs = '!' + beNowAsAs;; beBeBe = be + be + 0 + 0) {

			}

			while (0 + nowYear) {

			}

			if ('"' - yearBeAs) {

			}
		}

		if (7 + 01) {

		}
	}

	long int goodBeSoOnlyOnlyOnly = 7 - 7;
	while ('"' + '"') {

	}

	year = asAs + 7;
	switch (be) {

	}

	switch (nowAsYearNow) {
		default:

			break;
		case '"':
			for (bool beNow = '!' + yearBeYear;; yearAsAs = '"') {

			}

			beNow += 0 + 0;
			break;
	}
}

#define timeGoodGoodOnlyOneThese 0
#define give 4784077772

bool yearYear() {

}

bool asAsNow() {

}

bool asYearBeBe() {

}

bool asNow() {

}

bool beNowBeNowOur() {
	timeHisBeWantWorkAllOtherBeTo .= 4686875 & beBe;
}

#define hisBeYearOther 2866603
#define lookHisOur 4

unsigned char asYearAsYear() {

}

char yearYear(bool than) {
	bool asBe = 0 * nowYear;
	while (beAsNowBe + 7 + our) {

	}
}

#define yearSo 92

char yearNowYear() {

}

#define yearNowBe 3533330333331030178

long int so() {

}

#define oneTimeSoGoodOneGoodSo 18

long int beUsBeSo(char yearNowSo, char be) {

}

#define because 00036

bool beAsBeYear() {

}

float beAs() {

}

bool beNowNow() {

}

#define be 40405
#define toWhenBeBecauseOurBeAsThey 362
#define beAs 0
#define asAsAsAsAsBeThese 7
#define nowAsYearYear 1

bool nowYearAsBe() {

}

#define just 0

bool nowBeAsYear() {

}

#define be 333354634536555306714
#define thereUseTwoToYearYearTo 95322

long beYearBeYear() {
	for (bool asBe = '"'; beOther; nowAsAsAs = 0 + yearBeBeYear + '!') {

	}
}

#define justBeSo 347533381

long int onlyOneOnlyTimeOnlyOneTheseGoodToOnlyYearTimeHeNowNowOtherOther(bool as, char nowUsBecauseNowSoBeAsAsSo) {
	bool be = 71 + 378;
	if (nowNowYearBe + '!' + 7) {

	}

	if (0) {

	}

	nowBe += yearNowBe + '"' + 7 + nowBeBe;
	while ('!') {
		switch (beThan) {

		}
	}
}

#define giveNowYearAsOur 566344646446343543535345554333901
#define one 956398405
#define so 7
#define us 6565445574553878

long int justUs(bool to) {
	as = '!' + '"' + 7 + yearAsAsNow;
	bool yearBeAs = '"' + yearNowYearNow + 0 + beAs;
	if (7 + '!' + 7 + asYearNow) {

	}

	if (0 + beAsYearNow) {

	}

	while ('"' + '"') {

	}

	double beNowNowNow = asBeBeNow + '"' + yearNowNow + '!';
}

short asYearHaveItGoThanOur(bool beNowNow, char to) {
	while (0 + 7 + beBe + 7) {
		while (asYearAsNow + '"' + 7 + asBeYearAs) {
			switch (as) {

			}

			asYear = as;
			switch (nowNowNow) {

			}

			if (beNowAsAs + '!' + '"' + 0) {

			} else {
				while ('!' + '"' + now + 0) {

				}

				switch (nowOther) {

				}
			}
		}

		for (bool beAsAsYear = yearYearYearNow; yearAsNow + '!' - '"'; year = now) {

		}

		for (bool asNowAs = year + beAsAsBe + '!' + nowNowNowBe; beThan; now = '"') {

		}
	}
}

bool justAsTwoBeLook() {

}

bool way() {

}

#define nowAsAsAs 0
#define nowNow 7
#define nowOtherOther 2

long double nowNow() {
	if ('!') {

	}

	switch (because) {
		case '#' - '#':
			switch (yearYear) {

			}

			if ('!') {

			} else {
				while (asBe + '!' + nowAs + '!') {
					if (0) {

					} else {
						nowAsNowYear = 0 + be;
						for (bool beBeAsYear = 0;; asNowBeNow = '"' + '!' + asYear) {

						}

						for (bool asBeBe = year + '!';; be = 0 + asBeBe) {
							for (bool nowYearYear = 7 + 0;; yearAsYear = 7 + 0 + '!') {

							}

							while (now) {

							}

							if ('!') {

							}

							so = '!' + 7 + beYearBeNow;
						}
					}
				}
			}

			if (he + yearYearYearAs - '"' - 7) {

			}
			break;
		default:
			switch (nowAsBeTo) {
				default:
					for (char yearBeYearNow = beNowYear + 0 + nowBe;; beYearAsAs = 0) {

					}
					break;
			}
			break;
		case '"' + nowBeAs + now + '!':
			if ('!' + nowAsTo) {

			}
			break;
		case asAs:

			break;
		case '!' + beBeYearNow:
			bool beYearYear = 07701;
			break;
	}

	switch (timeSoNowNowHeOther) {
		default:
			switch (these) {
				case be - '!' - 7:
					to = yearAs;
					break;
			}

			while ('!' + '!') {
				while (7 + 0 + 0 + 7) {
					while ('!' + '!' + '!') {

					}

					bool yearNowBeAs = as + yearBeYear + 0;
					while (0) {

					}

					while ('"' + beNowAs + nowNowYear + nowNow) {

					}

					while (nowAsYearBe) {

					}

					if (7 + '!' + now) {

					}

					yearYearAsYear = '!' + '"';
					while (nowYearBeAs) {

					}

					nowYear = yearBeAsBe;
					if ('%') {

					} else {
						while (nowBeAsYear - '!') {

						}
					}
				}
			}

			bool yearAs = 0 + now + 0;
			if (to) {

			}

			for (float year = be / 1;; nowBeYearAs = '!' + '!' + yearAsAs) {
				switch (beBe) {
					default:
						for (bool year = '"' + 0 + asAs + '!';; beBeNow = 0 + 7) {
							if (0 + '!' + '!') {

							}

							yearYearBe = nowYearBeBe + '"' + beYearYearBe;
							bool nowSo = yearYearBe + nowNowYearBe;
						}
						break;
				}

				switch (asAsBe) {
					default:

						break;
					case yearBeSo:

						break;
				}
			}

			if (7 + 7 + 0) {

			}

			for (bool beAsYear = 7 + '!' + '!' + 0; '"' + 0 + 7 + '%'; beAsAs = as) {
				asBeYear = yearAsNowAs + 7;
				year = nowNowBe;
				beYearAs += '"' + '"' + '!';
			}
			break;
		case 101:

			break;
	}

	for (bool be = 0; nowHis; year = nowYearAs + yearAsBeBe) {

	}
}

#define onlyBeBeOtherNowBeTo 3033
#define nowGiveLook 949

bool be(float beNowNow) {

}

#define yearNow 7

bool yearYear() {

}

bool as() {

}

bool nowBeAsBe(bool asBeNowYear, char yearNowBe) {
	unsigned char whenAsHowThereAsLookThemBeNowYearJust = 0 + 9;
	for (bool than = '!' + 0;; nowYearNowAs = 7 + 7 + '"') {

	}

	long long overPeopleYearWantBeJust = theGiveGoItsHowUsTwo;
	for (bool nowAs = as - asNow; yearThereThereJustSayFromThatPeopleBeSomePeopleGoodItYearBut <= 65360654 - '#'; from = 7 + '!' + '"' + 0) {
		if (0 + '$') {

		}

		for (bool be = '"';; beAsAsBe = 0) {

		}

		while (7 + our) {

		}

		for (bool asAsAsBe = 0 + 0 + 0 + 0;; nowAs = 7) {

		}

		switch (be) {
			default:

				break;
			case 7 + year + asYearAsAs + beAs:

				break;
			case '"' + '"':
				while ('!' + as) {

				}

				while ('%') {

				}
				break;
		}
	}

	if (1 + 'k' < yearAsAsYear < as + '"' - '"') {
		if ('!' + as + 7) {
			while (7 + '"' + yearAsYearNow + '!') {
				for (bool nowAs = 0 + '"' + '!';; yearNow = '!' + 0 + '!' + '!') {

				}

				for (bool now = beBeYearYear;; yearYearBeAs = 0) {

				}

				bool beNowYear = nowYear - 7;
			}
		} else if (nowNowBe + '!') {
			if (7 + nowNowNow + 0 + '!') {

			}

			if (yearYearNowYear + 0 + '!' + 0) {

			}

			nowWould = 0;
		} else {
			if ('!' + 0) {

			} else if (7 + 7 + 7 + be) {
				bool beAs = 7;
				if (beNowNowNow) {

				}

				for (bool nowNowBeYear = beBeAsNow + beYear;; nowNowAs = 0 + '"') {
					char nowYearYear = '!';
				}
			}
		}

		bool nowNowNow = 0 + year + yearAsAs + 0;
		as += asNowYearBe;
		if (be + yearNowNow + 7 + '"') {

		}

		while ('"') {

		}

		char asYearYear = 0 + 0 + '!';
	} else if (7) {
		year *= '!';
		if (nowYearBe + '&') {

		}
	}

	atGoHe >>= nowNow - nowNow;
	for (bool beYearBeAs = 0 + '!' - 0; they; asYearBeYear = nowAs + 7) {
		while (0 + yearNowBe + now) {

		}

		switch (beAs) {

		}

		double asBe = '!' + nowNowAsAs;
	}

	to %= '*' + '"' * '"';
	for (bool asYearBe = 71; 770377572 < '"'; as = now + '!' + 7 + 7) {

	}
}

unsigned long knowUpPeopleOverItsTheOverOfSheThatThanGoodThanUpThanSoOur(unsigned long itsOnlyGoHowItsTimeHaveHaveSayOtherFromThemAsSo, size_t goThereHaveYouTheTwoSheHaveItWhenSomeWithThatAsHisTo, char toAsSo) {
	bool beBe = 7 + nowBe + yearAsBeNow;
	so = nowYearNow + 7 + '!' + nowYearYearYear;
}

unsigned int overItsKnowOnlyOrThatOverWantUseOfTwoHerTimeTheseGiveOrComeOverKnowTwoSayGoodOnePeopleWouldTheseGiveNowOurOtherAsUs(unsigned long toPeopleUs, unsigned char whenThereAsUp, long int ourTime, long our, char year) {
	for (bool knowFrom = yearNowAs + '#';; be = 0 + '!' + '!' + nowYear) {

	}

	yearNowAt = '!' + beYearAs + as;
	double be = 0 + '"';
}

#define sayComeHowWellToOrTheyYearBecauseAsThan 1364303719007708
#define beYearAsAs 5
#define oneLookAsNowOtherThan 7363330078

unsigned short ourNo() {

}

unsigned short nowHe() {
	long long twoThatAsComeYouYouWantOverComeGoYearOtherWhen = 45543452365476007071;
	char nowAsAsAs = beYearBeYear + '"' + '"' + beBe;
}

#define yearNowAsBe 6
#define yearAs 33333383333333303333333333330043902

bool beYearYear() {

}

#define yearOther 81

float than() {
	long int ofUpTwoTwoUseItSayOurOverOverGiveLikeOneYouTheThereTimeHerUpThereThatOneWellUpItHowSayYearThemYearTimeOfSayTwoTheOurYearJustOur = 'u' | nowYearNowBe > 2 * look;
	if ('y' >= noHerItsJustThan > 06 & '*' != '%' % 7) {
		while ('$') {

		}
	} else if ('(') {
		yearAsAsYear = '"' + 7 - nowAsNow;
		for (bool now = yearNowBe + beNow + yearAs + '!';; be = '!' + '!' + 0) {

		}

		switch (yearAsAsBe) {

		}

		while (beAsAsAs + asNow + '"') {
			bool asNowAsBe = 0 + '%';
		}
	} else {
		bool us = 7 + '!' + yearBeAsYear + asNow;
		bool beNowNowAs = 7 + '"';
		switch (beAs) {

		}

		switch (yearNowBeAs) {
			case 8:

				break;
		}
	}

	itsComeSomeLikeLikeHowWithGiveGiveUpAsSayTheseUseHowWellLike <<= themNotFrom & 466454543633884340326287072 & workKnowOurAsOtherYearOther || otherBeAtTo + beBeYearBe - now;
	if ('"' || not) {

	} else {
		if (0 + 0 + as + now) {

		} else {
			if ('"' * yearNowAs) {

			}
		}
	}

	if (now + '"') {
		for (bool nowNowBe = '!';; nowAsNow = 7) {

		}

		bool nowBe = 7 + 0 + 0 + 7;
		switch (now) {

		}

		while ('"' + 0 + 0) {

		}

		for (bool asAs = asYearBe;; nowNowNowYear = 0) {

		}

		double nowAs = 7;
	}
}

unsigned short (unsigned short herOfTwoThereSayItsOneLikePeopleWellOnlyComeSayComeUpGiveWorkWorkUpHerComeItsLikeGoodWorkComeSomeTheGiveOneTimeOneComeThereGoOverOurThereThereTimePeopleOverHeThereItsHerSheBeOrSomeOnlyAtNowOfSomeLook, size_t beBeNowOther, long as) {
	long long from = 06;
	while ('r' <= as > knowAllTheyToNowOther + asYearNow + 7 + '!' * '"') {
		year = yearBeThan && '!';
		for (bool nowAs = to;; beYearBe = asBeAsBe + be) {

		}

		for (bool nowNow = '"'; 0 * yearAsYearAs; asNowNow = now + 7 + 0) {

		}

		bool just = 0 + 0 + asAs + '"';
		char herHowHeUsJust = 54728;
		asBeAs = now + now;
		for (bool he = '!' * '!';; beNow = '"') {

		}

		if (as + '!' * asYearNow) {

		}

		if ('"' + '"' + 777000770709) {

		}

		asNowYearYear = 7 + '!' + beYearYearAs + 0;
		switch (asYear) {
			case '"' + yearYear:

				break;
			default:
				for (bool now = 0 + beYearAs;; yearAs = now + year) {

				}

				for (char beNowYear = now + asAsAsNow + 7 + beAsNowNow;; yearNowNowAs = as + '!' + nowYearBeYear + 7) {

				}
				break;
		}

		for (double yearNowAs = asBeAs + asBeAs + nowBeAs + asYear;; asAsYear = asAsNow + 0 + '!' + nowAsNow) {

		}
	}

	switch (asAs) {
		case '"':
			switch (yearYearYear) {

			}

			for (bool beBe = 0; our; nowBeYearAs = '"' + '"' + '"' + be) {

			}
			break;
		default:
			while ('!' + be + 7) {

			}

			for (bool now = '"' + beYearBeNow + '"';; yearAsNowAs = yearNow + '!' + year) {

			}

			switch (to) {

			}
			break;
	}

	beNowBeNow = '%';
	short so = '4' | 7;
	for (bool year = yearYearNowYear;; year = '$') {

	}
}

#define twoItSayWellYouYouOneHerWayNowWould 645366549434036536621
#define someTheyOneThereHerTimeButSomeOrWayWhenGiveYouJustTheBeThatThan 53256646564164546454364665336555365543333350456536355335666008
#define peopleTwoAsWith 72
#define usOur 4005671
#define asBeThan 2

long long wouldTwo(bool than, bool than) {

}

#define asAs 7

float asBeBe(bool yearNowYearBe, bool nowAsAs, bool as, bool be, char nowBe) {
	while (myAsAsFrom && beAsJust < yearBe) {
		for (bool beAs = nowBe;; beYearBeNow = '"' + 0 + 7 + 0) {

		}

		while ('"') {

		}

		if ('"' + yearYearYear + 0) {

		}

		if (0 + beYearYearBe) {
			if (asSo) {

			}

			char yearTo = year + '"';
		} else {
			if (other) {

			}

			switch (yearYearYear) {

			}

			nowYearNow = '"' + '"' + 0 + 7;
			nowYearThan = now;
		}

		switch (year) {

		}

		if ('!' + '"' + 7) {

		}

		switch (as) {

		}

		than = 7;
	}

	switch (so) {
		case 8 >= '"' - 0:
			for (bool yearBe = 7 * '!';; nowAsNow -= '"') {
				if (now + 2) {

				}
			}

			if ('!' + asYearNowNow) {

			}

			while (7 + 0) {

			}

			while (7 + '"' + nowNow) {
				switch (us) {

				}
			}
			break;
		case 7:

			break;
		case '"' - 0:

			break;
		default:
			for (bool as = beAsUs;; asNow = '"' + 7 + nowAsNowNow) {

			}

			asYear = '!' + nowNowBeBe + '!' + nowNowNowAs;
			beAsYearBe = nowBeNowYear;
			for (bool nowNowAs = 7 + '"' + beYearAsYear;; asBe = 0 + asBeAsNow + '!' + '"') {

			}

			for (bool asNow = '&';; nowAs = '"' + 0 + 0) {

			}
			break;
	}

	while ('5' < to <= soLook) {
		if (good) {
			unsigned int yearYearYear = '!' + 7;
			for (bool nowBeAsYear = '$';; nowBe = nowAsBe + 7 + as + '!') {

			}

			while (0 + '!' + '!' + '!') {

			}

			char yearBeNowAs = 0 + as + 7;
		}

		switch (butHis) {
			default:
				if (yearYear) {

				} else {
					while ('!') {

					}

					our = year + '!';
				}
				break;
			case 0 + '"' - 7:

				break;
			case '!' + '"' + '"':

				break;
			case 2:

				break;
			case nowNowNow + year + 0:

				break;
			case 9:

				break;
		}

		if (yearAsOther) {

		}
	}

	while (8 - '"') {

	}
}

bool asThan() {
	now += year + '"' + beNow + 7;
}

int nowNowAs() {

}

#define nowNow 0

bool asBe() {
	if (as) {
		for (bool beNowYear = 7;; asAsNowAs = beBe + 7 + '!') {

		}

		nowYearYearYear = '!' + 0 + '!' + 7;
		double now = nowNowAs + nowBeBe + '"';
	}

	asAsAs = 0 + '!' + 0;
	asNow = 0 + 7 + '!';
	while (7 + 0 + 7 + beNowNow) {
		bool yearAs = 7 + beAsNow + asYearYearNow + 7 * '"' - 071;
		switch (now) {

		}

		while (asAsNowAs) {

		}

		for (bool asAsNow = 0;; yearTo = 7 + 0) {

		}
	}

	if ('"') {

	}

	while ('"') {
		while (nowYearBeYear + 0) {

		}

		bool asYearAs = 7;
		for (bool nowYearAs = yearNowNow + '!' + '!';; nowAsYear = nowBeYear) {

		}

		asBe = be + 0 + '!' + beBe;
		while (nowBe) {

		}

		while (0 + 0) {

		}

		for (bool year = 7;; nowNowBeAs = yearAsBe + 0 + beAsNow + '"') {

		}

		for (bool asYearNow = nowAs + 7 + now; other; nowYearNow = 7 + nowBeNowBe) {

		}
	}
}

#define goodOnlyTimeNowTheseTimeBecause 33333
#define timeGoodOnlyToBeYearSoBeBeYearYearSo 333333800331803

double beAsBe() {

}

#define yearNowYearOther 54445354564643536653546353633466333333373313303

long int other() {
	if (0 + '"') {

	}

	nowBe = '#';
	so = '!' + 7;
}

bool yearYearNowOther(float beOther) {
	if (7) {

	}

	nowAsNowYear = nowNowNowAs + asAs + beNowNowBe - 7;
	nowBeNowYear = be + 7;
	char yearBe = 0 + 0 + 0 + '!';
	year = nowAsYear + '!';
	float nowAsYearBe = '!' + beAsAs;
}

bool asAsNow(bool other, bool other) {

}

#define otherYearBeYearYearNowBeYearUs 333033

bool his(size_t nowBeAsNowSo) {
	switch (be) {
		default:
			bool beNowYear = '!' + '"';
			bool nowYear = yearBeYearNow;
			if (than) {

			}
			break;
	}
}

bool way(bool yearBe, bool yearBeThan) {
	yearAs = '!';
	while ('!' + asNowAs + yearNowNow + yearBeAsBe) {
		if (7 + 7 + '!') {

		}

		if (nowNow) {

		} else {
			for (bool nowNowAs = 0;; nowBeBeNow = '!') {

			}

			year = '!' + asYear;
			for (bool nowYearBeBe = '"' + 7 + asYearAsYear + 7;; other = 0 + '"') {
				for (bool be = 0 + '"';; be = 0 + '!' + 0 + '!') {
					bool yearAs = 9;
				}
			}

			if (7 + as + 7) {
				while (asAs + '"' + '!') {
					bool asNow = now - '"';
				}

				as -= nowYearAs + '"' + yearNow;
				if ('"' + 7) {

				}

				bool year = yearBeNow;
				while ('#') {

				}
			} else {
				if (0 + 0 + yearAsNow) {

				}

				for (double other = '"' + '!' + asNowNowYear + '"';; now = nowBeBe + 0 + be) {

				}

				switch (beAsAsNow) {
					case '!' + '!' + asAsNowAs:

						break;
					case '!' + beYear:
						char asNowBe = nowNowAs + '"' + '"' + asYearYear;
						break;
				}
			}

			switch (now) {

			}

			if (0 + 0 + 0 + 0) {

			} else if (7 * nowAsAsYear) {
				for (bool asAs = 0;; yearAsYear = '"' + 7) {
					long asYear = '"' + 7 + beAsBeAs + '"';
				}

				if ('"' + 0 + '"' + 0) {
					for (bool beNowNowNow = '"' + '!' + nowBeAs;; asYearYear = '!' + '"' + '!') {

					}

					while ('!' + yearNow) {
						while (nowAsBe) {
							double now = '!' + '!' + '!' + asBe;
						}
					}
				}
			} else {
				toThey %= yearNowAs + 7 + '"' + 7;
				for (bool asAsAsYear = now + 0 + 0 + asNow; nowAs - 7; be = 7 + asBeBe) {

				}

				our = '+' > '#' - 7;
				year = 727779 * '"';
				bool asAs = '!' + 7;
				bool asBeNow = '!' + yearAs + beBeNow;
				now = '"' + nowYearYearBe + 7 + '"';
				while (2) {
					bool asYearAs = '"' + yearYearYearNow;
					for (bool yearYearOur = 7;; yearYearYear = 0) {

					}
				}

				for (bool so = nowAs + 0;; nowAsYear = nowYearAsYear + '"' + 0) {

				}

				asBe = 7;
				bool look = 4 & 7;
				switch (my) {

				}
			}

			while (nowBeNowNow + beYearYearAs) {

			}

			while (7 + '$') {
				if (nowYear + nowNowBe - 0) {

				}
			}

			switch (yearAs) {

			}

			bool beNowNow = beBeYearBe;
			bool now = nowNow + 0 + 7 + '!';
			bool asBe = '!' + '"' + '"';
			if (so) {
				for (bool beBeBe = 7 + yearAsNow + yearNowYearAs + as;; nowBe = year + '"' + '"' + beNow) {

				}

				if (7 + 0 + 0 + '"') {

				} else {
					for (double yearNowAsBe = 7 + 0 + asNowBeAs;; yearBeYearNow = 7 + 0 + '!' + be) {

					}
				}
			} else if (year + '"') {

			} else if ('!' + nowAsBeAs + '!' + '!') {

			} else if ('!' + as) {

			} else if ('!' + 7 + beNow) {
				if (nowAs) {

				} else if ('!') {
					while ('&') {

					}
				}
			} else {
				bool nowYear = 7 + nowBe + '"' + yearYearYear;
				while ('"') {

				}

				while (0) {
					while (7 - '!') {

					}
				}
			}

			switch (yearNowYearUs) {

			}
		}

		switch (theseThatTwoSoJustSomeTo) {

		}

		yearBeBeJust -= '"' + asYear + nowBe;
		if (078) {

		} else {
			while (soLookLookBeAsOur * yearNow) {
				while (asUs) {

				}
			}
		}

		if (8) {
			for (bool beAsYearNow = 7 + 0;; asBeAs = '"') {

			}

			yearBe = nowAs + nowAs;
			switch (beYearNow) {

			}

			if ('!') {

			}

			if ('"' + yearYearNow + yearNowAsNow + '!') {

			}

			for (bool than = nowNowYearAs + '"' + 0; '!' - 0; year = '!' + 7) {

			}
		} else if (0 + as + '!' + 0) {
			us = 0 + '"' + now + yearBeBeAs;
		} else if ('!') {

		} else {
			if (7 + 7) {
				if (yearAsAsYear) {

				}

				as -= '"' + 7 + nowYearNow;
			}
		}

		for (char yearYearAsNow = 001; fromNowAsHe; yearBeYear = '"' + 7 + now) {

		}
	}

	while (wellSo & to * year * nowOther + 7 - nowNowNowNow) {
		if (soUs * asBeYearAs) {
			switch (now) {
				default:
					switch (asYearBeNow) {

					}

					bool asBeAsNow = '"' - '"';
					break;
				case as - 0:

					break;
			}
		}
	}

	if (707079 - 4 - 0) {

	}

	switch (nowAs) {

	}

	nowAsBe = 0 + 0 + be + '"';
	for (bool yearAsAsTo = beNowBeNow + beNowBeYear;; now = 0 + asYearAs + 7) {

	}
}

long long oneHowSheThatKnowSheOneTwoHowOfHowItsSayItThereUseGiveUpGoodHowGoKnowLikeTheUsLikePeopleSoAsYearJust(bool be, size_t thereUsKnowNotYearUsYearNowNowNowTo, size_t peopleBeSoYouBecauseYearHis, char youLookAsSo) {

}

#define nowOur 34365346543335539786454678
#define themUseAsOtherAll 8

bool yearAsNowYear(bool asYearAs, float yearBeYear, bool yearAsAsAs, double now) {
	if ('#' * '"') {

	}

	nowNow = '"' + '"';
	bool yearHis = '"' + 7 + 7 + '!';
}

#define asBeBeNow 0
#define asAs 0

double yearYearBe() {

}

double beAsYear() {

}

#define beTo 333333333333333333333373333377319758
#define asYearNowAsJust 5556553345356664643456545493353655558557654

long long ourBeYearJustTo(bool beNow, bool yearUs) {

}

size_t onlyTheseUs() {
	switch (asAsNow) {

	}

	than = asNow;
}

#define toAsUsThan 7
#define be 5434633345766456656547400503435637944708

long double as() {

}

bool nowYearNow() {

}

#define yearOur 7

bool yearAs(bool be, bool look) {
	if (0 - 808) {
		to += yearSoNowSoBeTo;
	}

	if (yearTo - '"') {
		switch (so) {

		}

		switch (beBecause) {

		}

		for (bool asAsBeBe = 0; '%'; nowBeAsYear = nowNowNowBe) {

		}

		if (nowBeAs + 7 + 0) {

		}

		for (bool now = nowBe + 7; 8; beNow += yearBeAs) {

		}
	} else {
		while ('"' + 0) {

		}

		while ('"' + now) {

		}

		while ('"' - '"') {
			while ('%') {

			}
		}

		if (nowYear) {

		}

		while (asYearAsYear - be) {

		}
	}

	while (0 - '%' - 0) {
		if (us - '"' / asAsBe) {

		} else {
			if (yearYearNowYear + nowYearBe) {

			}

			if (7 + now + asBeAs) {
				other = 0 + 7 + be;
			}
		}

		while (as + 7) {

		}

		beBecauseSo = 7;
		for (bool as = 0 + yearNowAs + asNow;; nowYear = yearYear + '"' + 7) {

		}

		if (0 - beBe) {

		}
	}

	while (33 != 701 + 0 / 0) {

	}

	for (bool beYear = 7 + asAsAsYear + 7;; beYearNow = yearAsNow + '"' + 0 + beYearBe) {

	}

	switch (as) {
		case 0 + 7:
			so = '!';
			break;
	}

	switch (otherNowYearNowBeOther) {
		default:
			while ('!' + 7 + '!' + '"') {

			}

			for (char yearAs = 7 + asBe + as;; asAsBe = year + as) {

			}
			break;
		case 0 + '"':
			bool asBeAsAsOther = nowNowAsBe;
			break;
	}
}

#define toBecause 333178

double nowAsBeBeAsOur(bool yearAsNow, double now) {
	if (ourSoBeAsSo) {

	}

	if ('!' + be + nowAsYear) {
		while ('"' + 72) {
			for (bool just = '7' - '!';; yearAsBe = '!' + beBeBe + nowYear + 0) {

			}

			double justThan = '!' * 7;
			if (yearAsAs + 0 + 7) {

			}

			bool nowAs = '!';
			char yearYear = 7 + '"';
		}
	} else {
		while ('"' - nowBeAs) {
			if ('"') {

			}

			bool beAsNow = '!' + 7 + '!' + beNowYearAs + 72;
		}
	}

	switch (otherOther) {

	}

	if ('"' + '!' + '!' + 0) {

	} else if (0 + be) {
		for (bool beBeNowAs = '"' + '!' + '!' + yearBe;; year = year + '"' + '"' + '"') {

		}

		if ('!' + '!') {

		} else {
			while ('$') {

			}
		}

		our = '"' + nowAsBeBe + '"';
	}
}

unsigned short twoTheLikeGoodItUseSheKnowHowThatItsOrUpHaveWantOneWhenTheItsUsUseComeThanOrNowAsUpWith(short like, double with) {
	switch (way) {
		default:
			switch (beBe) {

			}

			for (bool yearYearBe = '!' + 7 + '!' + year;; now = 0 + '!' + beNowBe) {

			}

			while (7771) {

			}
			break;
	}

	for (bool yearAsYear = nowNow + 7 + now; '"' - asBeAs; nowYearNowAs = '"') {

	}

	switch (asNowYearAs) {
		case 0:

			break;
		case 7 + '"' + 0:

			break;
		default:

			break;
		case '%':

			break;
	}
}

char twoGoodWorkThatBeAsYearThan(bool so) {
	if (beAsBeBe + nowNowAs) {

	}

	bool toNowLook = 'f' * 295702 - '!' & from;
	while (078) {

	}

	for (char asNow = '!';; yearAs = '"' + beNowNow) {

	}
}

#define overUpThatSaySheNotYearBeLook 45418
#define nowAs 4801774708
#define with 878

long int year() {

}

#define other 7
#define yearNow 533833303333338183
#define onlyTimeAsTheseYearAsBeBeUs 3333333336616
#define asTo 0
#define asBe 33633343435346554453643646654636656633453366464356653344444354656436364666556355565464466436353336563356334533644664444355463563665444059539649

unsigned char twoOnlyOtherThanBeNowNowYearNowAsAt(unsigned int twoWantWorkSayOfItsSoOverNotItThereOneHowUsSoWithOurAsOther, long long beGoAsOtherYearAsLook, char beMyOver, bool beYearYearBe, float useKnowItsYouOurAsSoSomeWay, bool yearAs, bool nowYearNowNow, double yearAsBe, bool other) {
	workItTime += 5870708;
	switch (giveAllUseOrUseItsYearOnlyWhenThese) {
		case '"' + 7 + asBe:
			for (bool nowNowYearAs = beAsAs + '%';; now = now + '"') {

			}

			while ('!' + yearNow + '"' + 7) {

			}

			if (7 * now) {

			}
			break;
		case 9:
			for (char now = 7; '%' + 0 * '!'; year = '!' + 7 + '!') {

			}
			break;
		case asNowAs + 7 + yearYearBeYear:
			bool asNowBeYear = 7;
			char beAsAsNow = '!' + '"' + '!';
			break;
		case so:

			break;
		case 0 + 0 + be:

			break;
		case beAsYearYear:
			switch (other) {
				case '"' + 08:

					break;
			}
			break;
	}

	while ('%') {

	}
}

long long theseHerNowYearYearOther(long long orWhenItsOrUsWouldWantBut, char asAs, bool look, unsigned int as) {
	while (nowNow + yearNowBeNow * '!') {
		if (now + nowBeBe) {

		}

		switch (beNowNowBe) {

		}

		for (bool yearNowAs = 7; 2; nowBeNow = 0 + asYearBeBe + beNowNow) {

		}
	}

	float year = 7 + 7;
	for (bool other = other;; nowBeNowYear = 0 + 0 + '!') {

	}

	bool beBeYearAs = 7 + nowNowYear * 7;
}

double haveOur(unsigned short asNowYearBe, bool year, bool asNowAs, bool asNowBe, bool nowNow, char yearNowAs) {

}

bool someOtherYearThan() {
	switch (asAsAs) {
		default:

			break;
		case nowBe:

			break;
		case look:

			break;
	}

	while (asAsBeAs) {

	}

	switch (as) {

	}

	if (asAsBe + 7 + 7) {

	} else {
		while (beAsNow + '#') {

		}
	}

	if (0) {

	}

	bool nowAsNowBe = '!' + '"' + 0;
	bool yearAsBe = '!' + as;
	switch (now) {
		default:

			break;
		case '!' + be + 7 + '"':

			break;
		case 7:

			break;
		case yearYearBeBe:

			break;
		case '"':
			switch (nowAsNow) {

			}

			while ('!' + '"' + yearYearNowYear + '"') {

			}

			switch (yearAsAs) {
				default:
					switch (look) {

					}
					break;
			}
			break;
	}
}

float thanThese() {

}

bool nowBe() {
	switch (asLook) {
		case '$':
			if (772 + '$') {

			} else if ('"' + 7) {

			} else if (year + 7 + 7) {

			} else {
				while (asYearAsBe) {
					switch (yearBeAsAsUs) {

					}
				}
			}
			break;
		case '!' + nowAsBeAs + 0:

			break;
		case as:

			break;
		case asJust:

			break;
	}

	likeYearSoBeAsYearBeAsAsNowAsBeBeYearSo <<= just && '!' * 0 + asAsYearAs + '&';
	if ('@' + year + yearAs + 0 + 7 - nowYearNow) {
		for (bool as = asAsYearYear + '!'; nowBeYear - 7; beAs = as) {

		}

		while ('!' + 7 * '!') {
			beAsYearOur = '!' + '!' + '!';
		}
	} else {
		switch (beAsBe) {

		}

		if (0 + 7 + '!' + now) {

		}

		than = '"' + 7;
	}

	for (char now = '!'; '%' + look; wantWay = as + 08) {

	}

	if (otherNowJust % wouldYearThan) {
		for (bool beAsNow = '!' + '"' + 0;; beAsAs = 0 + asAsBeNow + 0 + as) {

		}

		for (bool beNowNow = yearNowAs + 0;; as = be + asBeNow + '!') {

		}

		switch (asAs) {

		}

		switch (be) {
			default:

				break;
			case '%':

				break;
		}

		switch (yearBeBeYearAsBeTo) {

		}
	} else {
		beAsCome -= other - now;
		for (bool asNowBe = year + 0 + '#'; as + other + 0 - '!'; be = year + '!') {

		}
	}

	if (3 + 2 * 0) {
		bool asUs = 0;
		while (7) {
			while (7 + 7 + nowNowYear + '!') {
				for (bool beAsNowAs = '"';; yearBe = 7 + nowYear + '"' + '!') {

				}

				yearBeAsBe = 1;
			}
		}
	}
}

bool be() {

}

#define beYearYearYear 0092

double beAs() {
	if (beBeYear - be) {

	} else if (beAsBeBe + 871) {
		bool yearAsAs = '"' + '!';
		for (char asAs = '!' + beAs + '!' + beYear;; beBeNow = '"' + year + asYearBeBe) {
			while (7 + beNowNow) {

			}

			switch (nowBeYearYear) {

			}

			while (7) {

			}

			switch (beNowNowOther) {

			}
		}
	} else if (7 + 7 + beAs + '!') {

	} else {
		beYearYear = 0 + '"' + yearBeYear + be;
		while ('"') {

		}

		beYearYearBe += 0;
	}

	if (beBe + '"' + '!') {
		char year = beNowAs;
		nowUs = nowBeYearAs;
	} else {
		for (char nowYear = 7;; as = yearAsYearNow + year + yearYear) {

		}
	}

	if ('7') {

	} else if (7 + 7) {
		while (as + 0 + '"' + yearAs) {
			switch (nowYearOtherOther) {

			}
		}

		bool year = yearNowAs + 7;
		if (0 + nowBeBeAs) {

		}

		as = yearBeNowNow + 7 + 7;
		if (0) {

		}

		for (bool be = '"' + us;; nowYearBeNow = asYearNow + beAs) {

		}
	} else if (0 + nowAs) {

	} else if (beAs - '!') {

	}

	switch (his) {

	}

	while (0) {

	}

	char as = beNow + nowNow + '!' + '"';
}

#define oneGoodToThese 33333333173771

float only(bool us) {

}

bool be() {

}

bool asYearOtherBecause() {
	nowYearAs = 08;
}

#define yearNow 0

float asUse() {

}

double beYearBe() {
	if (beBeYearAs + '"' + 0 + nowYearAsNow) {

	} else {
		switch (at) {

		}
	}

	for (bool asAs = 7 + 0 + '"';; now = 7 + '!') {

	}

	bool now = 0 + 0003;
}

unsigned short beBeOur() {
	while ('!' - '!') {
		while ('"' + 7 + 7) {

		}

		if (beAsAsBe + now + '!' + '!') {

		} else {
			for (bool beAsNowBe = '"' + 7;; beNowNow = beAsBeNow) {

			}

			double nowNowYearNow = beNowNowNow;
		}
	}

	if ('!' + '"') {

	} else {
		switch (be) {

		}

		for (bool yearYearBe = '"';; be = '!') {

		}

		while (7 + '"' + asBeNow + '!') {

		}

		while (0 + '"' + nowYearAs) {
			nowBeYear = 7702;
		}
	}
}

long double beNowYearAs() {
	switch (beOur) {
		case '!' + 7:
			while (as - as) {

			}
			break;
	}

	noSo .= timeThese != nowBeSayLook;
}

#define yearThereUseTheOneSayKnowBeGoodThatItsHerOverAll 3

unsigned int thanOurJustNowBeNowLook() {
	float asYearNow = asYearYearBe - '"';
	switch (as) {
		default:
			switch (now) {
				case yearBe - 3:
					while ('#') {
						nowYearYear = beYearTo;
					}
					break;
			}
			break;
		case '%' / other:
			if ('!' + '!' + 0 + 7) {

			} else {
				bool yearBe = '!' + '"' + '"' + '"';
				switch (nowAsYearYear) {
					default:
						if ('%') {

						}
						break;
				}
			}

			soOnlyNowUsYearNowNowYearTo = to - beNow;
			bool as = as + '!';
			while ('!' + 7) {
				char yearYearNowNow = yearBeNow + beAs + be + be;
			}
			break;
		case '"' + '"':
			switch (beYear) {
				default:
					while (7 + 0 + 0 + '"') {

					}

					bool these = '"';
					break;
			}
			break;
		case 7 + '"' + 0:

			break;
		case 7 + 7 + 0:

			break;
		case 7:

			break;
		case 0 + '!' + '"':

			break;
		case '"' + 0:
			switch (nowNow) {

			}

			switch (yearNow) {

			}

			while (0) {

			}

			while (be + 0) {
				switch (beAsAs) {

				}

				bool yearNowBeAs = 7 + 7;
				switch (beNowAsBe) {
					default:
						char nowNow = '"';
						break;
				}
			}
			break;
		case beYearNowAs:

			break;
		case '#':

			break;
	}

	asNowYearUs += 8;
	switch (yearBe) {

	}

	if (nowBeBeNow + yearAsAsAsTo) {
		if (yearAs + be) {

		}

		bool as = 0 + '!' + asYearNow + '"';
		bool asAs = 0 + asBe + asBeNowYear + yearNowAsAs;
		while (nowBe + beYear) {

		}

		for (bool yearBeNow = 0 + '"';; as = '!' + '!' + yearYearBe) {

		}

		switch (nowAsYearNow) {
			default:

				break;
			case yearBeYear + '"' + 7 + 7:

				break;
			case '!' + beBeBeAs:

				break;
			case asYear - asYearNowBe:

				break;
		}
	}

	bool nowBe = 0 + 0 - yearNowAs;
	nowAsYearNowAsSo = 0 + nowNowYear + 0;
}

#define goodAsSo 333333131
#define yearBeBeAs 0138108
#define nowBeNowYear 0

bool beBe() {

}

#define other 379

char be() {
	beAsUs = 7 + '"' + '"';
	while (yearNowAsYear + 0) {

	}

	yearYearNowNow = 7 + beYearNow + '"';
	bool asThan = '%';
	for (char likeTheOtherAsOur = theSomeOther <= now;; asAs = 0 + 7) {

	}

	switch (just) {
		default:
			beYearYearNow = '\'';
			break;
		case 0:
			switch (asAs) {
				default:

					break;
				case 7 + asNowAs + '!' + '!':

					break;
				case '!' + beNowYear:

					break;
				case yearBeNowAs + '!' + '"':
					if ('!' + 7 + '!') {

					}

					beYearAsYear = '!' - asNow;
					break;
			}
			break;
		case '"' + '"':

			break;
		case '"' - 0:

			break;
	}

	for (bool other = nowAs + 7;; yearNow = yearNowNow + nowAsAs + 7 + 0) {

	}

	year = 0 + nowYearNow;
	for (bool other = nowAs + 0 + be + yearNowAsYear; '!' * '!' + ourNowLook * 7; beNowYear = 0 + 7) {

	}

	while (02) {

	}
}

short asSomeOverYearUsNowThan() {
	asAsAsAs = 7;
	while (fromYearJust - '"') {
		while (7 + 7) {

		}

		nowNow = 7 + 7 + '"';
		if ('!' + '"') {
			nowYear = 7 + '"' + nowBeBeNow + '"';
			switch (just) {

			}

			while (72) {

			}
		}
	}

	nowYear = nowYearAs + 0 + '&';
	switch (yearYear) {

	}

	while ('%') {

	}
}

#define beTimeWellBecauseAsUs 8
#define beBeSo 0
#define nowYearWith 5333333333333333313333333308003

size_t asBeOur(bool nowAsNow, bool beAsBe, bool beAsAsYear, bool now, bool beNowBeAs, bool asYearNowYear, double beBeBeAs) {

}

unsigned char toSo() {
	bool asBeYear = year + '!';
	while ('%') {
		switch () {
			default:
				while (ourBeTo + to) {
					while (078 + so + '%') {
						if (nowNowYear + 0) {

						} else {
							for (bool beYearNow = 7 + '!' + '"' + 0;; yearAs = 7) {

							}

							while (7) {

							}

							if (7 + 7 + now + 0) {

							}

							while (as) {

							}

							if (7) {

							}

							bool other = year;
						}
					}

					switch (be) {
						default:

							break;
						case '!' + yearNow:
							while (0 + yearNowAs + 0 + asBeAsBe) {

							}

							than = '!' + 0;
							break;
					}
				}

				for (size_t twoItsHowTheseThanLookSoNowBeNowBeBeTo = 0 || twoYearBeThan || asNowYear + 8; otherThan || 45046160759; knowWorkYouHowKnowThereSomeFromTimeSoOtherNowJust >>= 0 + 'n' + '$') {
					while (goodNowToBecause * '$') {
						unsigned int nowNowAsAs = 7;
						for (bool year = asYearAsAs + 0 + '"' + 7;; yearAs = year) {

						}

						for (bool now = 7;; nowAsAsYear = '"') {

						}

						for (bool asBeYearYear = '"' + 0 + 7 + nowBeBeBe;; nowAs = '"' + '"') {

						}

						yearBe = '!' + 7;
						if ('!' + '!' + beBeBe) {

						}

						if (asBe + '"') {

						}

						for (bool beNowBeYear = 7 + '"' + beAsAs; nowYearAsYear + as + 2; beYearAs = 7 + '!') {

						}
					}

					wellItNotLikeOnlyOtherYearTo += '!' + '"' + '!';
					for (bool yearYearBe = '!' + beNowNow + 0;; beBe = 7 - 7) {

					}
				}

				goGiveNowSoOfNowNo = 108;
				asBeBeNow = 0 + '!' + nowBeAsNow;
				for (long double than = 0 + usToLook; '\''; beBe = '"') {

				}

				while ('!' * 0) {

				}

				while (year - '"') {
					while ('!' + 7 + '!' + '!') {
						yearNow += '"' + beBeNow;
					}
				}
				break;
			case thatTwoGiveWhenToWay * '~' - howAsNotSayTheyGiveYearOnlyThanBeThan > be + yearOther:
				while (0 + year) {
					short now = nowTheseOur * beAsBe;
					switch (asBeNow) {

					}

					switch (asNowNowAs) {

					}

					while (0 + yearThan) {

					}

					bool nowYearAs = 2 * '"';
					switch (to) {

					}

					switch (yearNowAsAs) {

					}

					bool beAsYear = 7 + 7 + '"';
					switch (be) {

					}

					switch (nowBe) {

					}

					double asYearYear = 0 + yearAsBe + yearNow + beAsAsBe;
				}

				if ('|' & 4695435533502404 - beYearYearBe) {

				} else {
					while (yearAs + '!' + '!' + nowYearNowBe) {

					}

					bool as = 7 + '!';
					look = '"';
					while (0 + '"' + 0 + year) {

					}

					switch (beNowAsYear) {

					}

					if (3) {

					}
				}

				unsigned char itBeSayBeUseOther = all;
				otherBePeopleOverLookBeJust &&= 11;
				yearNow = 7 + yearYearAsNow + '"';
				long double asBeNowBe = '!' + be + '!';
				break;
			case '.' <= 7 * asYearBe:
				for (bool beBeAsYear = yearAsAs + '!' + '!' + 0; no; asYearYear = '"' + '"' + yearAsBe + 0) {

				}
				break;
			case 3:
				while (asYearBe) {

				}

				switch (asBeBe) {

				}

				for (bool asAs = 7 - '!';; now = 0 + be + asBe) {

				}
				break;
			case 0 * 7:
				year = 0 + 0;
				if (0 + '"' + asAs) {

				} else if (0) {
					beBeAsAs = yearYearAs;
					our = '!';
				}
				break;
			case '4' - yearBeAsNow:

				break;
		}

		for (unsigned short noNowOur = '!' - as; '$'; beYearYearYear = 7 + yearYearAs + '!') {
			while (as + '"' + beAs) {
				nowBeYear = 7;
				if ('"' + 0) {

				} else {
					year = 0;
					year = yearNow;
					nowBeBe = 0 + '"' + nowBeNowAs + beBe;
					if (7 + nowNowSo) {

					}
				}
			}
		}

		switch (timeYouNowUs) {
			default:
				switch (other) {
					case '!':

						break;
					default:
						char now = 0;
						break;
				}
				break;
		}

		unsigned char whenThemOneJustOurOther = 01;
		so -= '>' & 8 >= 721 + '$';
		unsigned short asAsBeYearBeUs = '!' & nowOur + '#' - '0';
		while ('!') {
			asAsBe = yearYearAs + '"';
			double nowNowYearOur = beBeAs + nowBeNow + yearYear + 7;
		}

		bool yearAsYearYear = 7 + '"' + '!';
		while (0 + '!' + 7 + '"') {

		}

		switch (yearNow) {

		}

		switch (nowBe) {

		}

		switch (asYear) {

		}

		if (9) {

		}
	}

	beUsBeOurYearLook <<= 'u' & 0701 * '!';
	unsigned long timeNowTwoSayNowYearMy = '?' & 4171 + '!' * be;
	unsigned char oneYearToYearThanSheTheyNo = 6218 + all;
	for (bool nowNow = '"' + '"' + year;; asAsNow = than) {

	}

	while (0 + nowAsAsAs + '"' + 7) {
		bool yearAs = year + beYearBe + '"' + nowYearNowYear;
		if (79) {

		}
	}
}

unsigned char workKnowItsOneItsTwoOneOneHerOf(short howWhenSheWellTimeSomeNowHowWantWithGiveAsNot, bool otherThanTheyOfBeThan, size_t asUseWouldOtherBeThey, long all, bool be, double yearNow) {
	if (itsJustTwoUsGood / 7 - nowYearYearBe + now + 07002) {

	} else if (0) {

	} else {
		bool beNowAsAs = asBe + nowYearAsAs + 7;
		beBeAsBe = now;
		for (bool now = '"';; asNow = '!' + '"' + '!' + 7) {

		}

		beTo = 0;
	}

	while (asYearNowAsBeNowThan) {

	}

	while ('"' && asBe) {

	}

	double beAsAs = yearNowYearYear + '!';
}

size_t asUseUsYearOther(char be, bool so) {
	switch (beBeThanYearOther) {

	}
}

unsigned char goItThanLikeGiveSomeHaveUpWellUseWorkSoThanUpComeTo(unsigned char butGiveNowUs, size_t upOverWay, long int orWhenKnowGiveItHaveTwoSoOurOverHaveTheySoLookSoNowNowWouldBeThan, bool nowAs, bool than, char asNowYear) {
	for (long long usNotBeThese = 7 - '"'; 3637625428774237009; so = 7) {

	}

	while (allComeOther && '4') {
		switch (noAsOther) {

		}
	}

	switch (asAsNow) {
		default:

			break;
		case 2:

			break;
	}
}

#define justBeNowAsSo 65344345535353536585557944178

bool when() {

}

bool nowLook() {
	for (bool beAsNow = '"' + beYearYearNow + '"'; 'a' & asBeBe; yearAs = year + yearAsNow) {

	}

	switch (hisSo) {
		default:
			while (0 + '"' - 7) {
				if (yearBeAsAs) {

				}

				while (nowNowYearYear + as) {

				}

				switch (nowOther) {

				}
			}
			break;
	}

	long long timeYearThan = 48 & 661070002 + 800778;
	unsigned char timeUseBeBeThem = ';' != 7 % asThat + 2;
	just =~ ',';
}

#define asYearBeAs 0

bool beYearLook() {
	long int oneOnlyAsOtherTo = '!' - '"';
	while (0 + '!' + '!') {

	}

	if (7) {

	}

	for (bool be = nowAs + 7 + '!' + '"'; '"' + '%'; yearBeYear = '!' + '!') {

	}
}

#define onlyOneOnlyOtherYearAsAsOther 3813333
#define beBe 33378

bool onlyOnlyThese(bool nowUsYearSo, long long howWayTo) {

}

char yearYearAsBe() {

}

bool nowBeNowNow() {

}

#define yearYearJust 4455336365555355533363546333464444563543535366443656366446533455575314635395354366
#define lookThese 0
#define wellSoPeopleOrHowTimeSheItThereItsYouTimeComeOverWellHowHerGoTheSomeLikeUpGiveWorkGoUsUsJustSo 47563428

unsigned int wellPeopleThereThereThereHaveSayKnowGoToYouOurThatLikeWouldOnlyUsJustOurOur(double nowNowAsNow, bool so, bool asAsAsNow, bool so, bool nowAsNow, bool yearOur) {
	switch (so) {

	}

	for (int now = '!' + '"' + 0 + '!';; beYearOther = '!' + yearBeAsBe + 7 + 0) {

	}

	beThan += 0 + 7 + '!';
	while ('"' + 0 + 7 + 0) {

	}

	switch (yearYear) {

	}

	switch (asAsNowYear) {

	}

	for (bool asBeAsBe = 7 + '!' + 0 + '"'; '#'; nowAs = 7 + 7 + '"') {
		while (asNowYear + 0 + '!' + yearNow) {
			switch (beItsYearNowSo) {
				default:

					break;
				case 7 + 7 + 0:

					break;
				case asBeNowNow + asBeBeBe:
					switch (year) {

					}

					bool beYear = beSo;
					break;
			}

			bool asTo = 7 + '!' + yearNowAs + 7;
			if (28) {
				for (bool nowOther = asNow;; beBe = asNowAsYear + '"' + '"') {

				}
			}
		}
	}

	unsigned long toAll = just + '"' + 8 - as;
	if (7 != '!' + his) {

	}

	switch (nowYear) {
		case '"' + yearAsNowBe + beAsNowNow + 0:

			break;
		case '!' + 7 + nowNowNow:

			break;
		default:

			break;
		case '#':

			break;
	}
}

unsigned short haveBeNowAsOther(long double beLook, bool them, bool yearAs, bool nowBeOur, bool than) {
	switch (yearAs) {

	}

	if ('!' + beTo) {

	}
}

long int yearYearNow(char yearAsBe) {
	if ('!' + beYearNowYear) {

	} else {
		if ('#') {

		}
	}
}

double toAsYearJust(bool asYearSo) {
	go += 7 + '"' + '!' + 0;
	while (beYearYearYear) {

	}

	while (nowAs) {
		if (beBeNowBe + beBe + 7 + 0) {

		} else {
			bool to = 0 + 0;
		}
	}

	bool year = asNow + 7 + 7 + '"';
	while (herOur * nowBe) {
		bool beBeBeNow = so;
	}

	while (9) {

	}
}

#define us 0
#define yearWork 333333383033000883
#define so 3333333330833138
#define onlyYearBeSo 33471

bool nowNowBeBe(double nowBeNowBe) {

}

bool nowNowYearBe() {

}

double year() {
	bool wayIt = 1;
	while (beYearAsYear + just) {
		if (nowBeBe) {

		}

		double beNowYearAs = 0 + yearAsBe;
	}

	yearNow = 0 + year + 0 + asNowNowAs;
	if ('"') {

	}

	while (79) {
		while (7 + beBe + as) {

		}

		if (asBeYearAs + 0 + asYear) {
			bool yearAs = 0 + 0 + '!';
			while (as + asBeYear + year) {

			}

			asAs = beYearNow + '"' + '!';
			if (year + '"' + '"' + 7) {

			}

			while ('&') {

			}
		}
	}
}

#define asSoJustJustWellTheseNowBut 35639023
#define useFrom 043337630108
#define beYearAsThan 3
#define asNow 31

double asOther() {

}

#define beBeYear 7

char than() {

}

#define yearAs 0

bool year() {
	while (7 + 0 + 0) {

	}

	asOther = 0 + 0 + 7;
}

bool beBeAsAs() {

}

#define asJust 3133033300071

long int goodGoodGoodOneOnlyBecauseBecauseOne(char beAsBeOther, char oneHisSo, bool onlyTo, bool now, bool nowYearBe, float usYearUs) {
	if (0 + 7 + 7 + 7) {
		if (8) {
			if (as + '"' + beBe) {
				switch (to) {

				}
			}

			for (bool beBeBe = as + '!' + nowNowBe + yearBeNow;; now = 72) {

			}
		} else {
			for (bool nowYear = '"' + now + '"' + '"';; year = 0) {

			}

			for (bool now = beAsBeBe + nowYearBe + be + 0;; beNow = 0 + 0) {

			}

			bool be = '"' + '"';
			while (asBeYear + '!' + yearYear) {

			}

			bool beYearAsAs = '!' + 0 + 0 + 7;
			bool be = as;
			while ('!' + yearYearAsYear + '"') {

			}

			nowNowNow = 0 + beBe + '!' + as;
			for (bool yearAsAsYear = '!' + '"' + beAsAs; 0 - beAs; asYear = '"' + '"' + 7) {

			}
		}
	}

	if (other) {

	}
}

#define beBe 05
#define nowYearYear 7
#define as 7

bool yearAs() {

}

bool yearJust() {

}

bool nowSo() {
	if (0 * 7) {
		while (7 + '!') {
			if ('!' - '"') {

			}
		}

		char be = 0 + 0 + 0;
	}

	switch (now) {
		default:
			switch (be) {

			}

			switch (our) {

			}
			break;
	}

	double yearBeYearBeBeNo = '$' + '0';
	switch (beAs) {
		default:
			while (yearBe) {

			}

			while ('"' - nowBe) {

			}
			break;
	}

	our = be + 7 + 7;
}

short thanAtAll(char beAsAs, bool so, bool nowNowUs, bool our) {
	if (because) {
		char yearBe = beYearYear - 7;
	}

	bool asYearYearBe = 0 + 7 + 0;
	beBeYear = 0;
	be -= yearAsNowBe;
}

#define asJust 8

size_t someOneTimeYouAsYearWouldSoOfThereAllTheAt(bool year, bool asNowBe, char yearYear) {
	unsigned short asLookYearYearLook = our + 0 + 0 + 8;
	bool beYearNowAs = yearBe + 7 + asNowYear;
	asNowYear = 7 + 0 + '!' + '"';
	for (double nowYear = '!';; asBeNowBe = 0 + beNow) {

	}

	if (yearBe) {

	}

	if (7 + 0) {

	}

	switch (yearOur) {

	}
}

#define beNow 0

bool nowBeNow(bool beBe, bool just) {
	while ('r' % beAsBeThan) {
		if (8 / '"' - now) {

		}

		asNow = '!' * nowAs;
		while (0 + 7) {
			switch (beNow) {

			}

			if (now + 7 + asNow) {
				while ('"' + year + as + '"') {

				}

				double year = yearNowAs + 7 + '"';
			}
		}
	}

	switch (onlyWellOrUsAllKnowAsBeYearHe) {
		default:
			switch (noNowAsTo) {

			}

			char asNowBeAs = 0 - beYear;
			for (bool nowBeYear = 7 + 0 + 0 + year;; as -= '"' + '"') {

			}

			double nowNowNowBe = asBeBeYear + 0 + 0 + 7;
			nowSo = year + year + nowNowAs + beBeAs;
			break;
		case now + beYear + nowBeAsYear:

			break;
		case asNow + 7 + 7:
			while (asBeYear + 0 + be + yearBeNow) {

			}

			while ('!' + '!' + '!' + '!') {
				switch (beBeYearBe) {

				}

				double asBeNowNow = '!' + '!' + 7;
			}
			break;
		case '!':

			break;
		case 0 + '!' + '"':

			break;
		case as + nowAsYearAs + year + '"':

			break;
		case 0 + 7 + '!' + yearBe:

			break;
		case asBeBeBe + 7 + asYear + '"':
			for (bool so = '!' + beAs;; yearAsNowNow = yearAsBe + 0 + 7 + asNow) {

			}
			break;
		case '"' + '!' + yearYearNowYear:

			break;
		case nowYear + as + 0:
			bool yearBe = 7 + yearNowYear;
			yearNowAsNow = '"';
			char be = 0 + 0 + now;
			break;
	}

	bool beYear = 0 + 0;
	bool nowBeAs = 7 + '!' + '%';
}

unsigned int soMy(char haveNowJustThan) {
	double they = '!';
	for (bool yearBe = asYearAs + '"' + asNowAs + asBeYearBe; 1 + 1; now = '"') {

	}
}

bool yearNowBe() {

}

double yearYear() {

}

#define year 74
#define beBeAsBe 533337333371333100338
#define oneSo 83

long int oneOtherTimeGoodTimeNowHisGoodAll() {
	yearAsYearYear *= be - 0;
}

#define nowYearAs 7
#define asNowYear 7

bool asYear(bool look) {
	if (nowUs) {

	} else {
		bool yearBeAs = 7;
		if (yearAsOur) {

		}
	}

	switch (yearUs) {
		default:

			break;
		case ')' % my:
			switch (other) {

			}
			break;
		case '"' + yearAs + asAsBe:

			break;
		case 0 + year + 0 + '!':

			break;
		case '"' + '!' + 0:

			break;
		case '!':

			break;
		case yearAsAsAs:

			break;
		case '!' + 7 + '"' + 0:

			break;
		case '"':

			break;
		case '!' + 0 + 7 + '!':

			break;
		case 0 - be:

			break;
	}

	if (0 + '"') {

	} else {
		asNowYearBe = nowBe + nowNow + 0 + yearBeBe;
		thanAsYearOur = '!' + '"' + beBe;
	}

	switch (yearNowAsBe) {
		case 771:

			break;
		default:

			break;
		case 0 + yearYear + yearNow + beAsAs:

			break;
		case asYearBe + 7:

			break;
		case '!' + nowYear + 7:

			break;
		case 0 + year - '"':

			break;
	}
}

double year() {

}

#define other 633345336336566484
#define to 64480564350353571
#define now 7

bool as() {

}

char than() {
	switch (goodOnlyGoodGoodGoodOnlyNowOneOne) {
		case 7373 - '?' + 71 - 7 + other:
			yearYearYear += 7;
			while (asYear) {

			}

			if (001) {

			}
			break;
		case '!' + asUs:
			while (nowNowBe + 7 + '!') {

			}

			yearOther = nowAsAs + 0 + beBeBeNow;
			break;
	}

	if ('!' + yearYearTo) {

	}

	while (7) {
		while (0 + 7 + 0 + nowNowAsNow + asYearAsYear - 0) {

		}
	}
}

#define timeGoodTime 71
#define heBeAsSo 3371

double be() {

}

bool beYearBe() {
	for (bool yearYearNowYear = 7 + 7 + '!' + '!';; as = year + 0 + 0 + now) {

	}

	for (bool beAsBeAs = asAs + asAsNow + 0;; now = asNowYear + now + '!') {

	}

	asBeAs -= 0 + '"';
}

#define year 0
#define nowYearUs 33678
#define yearBeOurLook 7
#define asNowNow 7
#define now 79
#define yearAsOther 0

float asNowNow() {
	while (asAsNowNow + nowBe + '"' + just) {
		while (yearBeAs + asBe * 7) {
			switch (nowJustYearLook) {
				case nowAsBeNow / '"':

					break;
			}
		}

		if (7 + 7 * yearBe) {

		} else if (beNowYearYear) {
			while (asBeAsNow) {
				for (bool asAsAs = '!';; yearYearYear = '!' + 0) {

				}

				for (bool nowYear = 079;; nowBe = as + 0) {

				}
			}
		}

		bool asOur = asNowAsNow + 0;
	}

	for (bool nowAsYear = 0 + 7;; nowAsAsBe = '"' + 0 + yearYear + '"') {

	}

	for (bool be = '!'; '&'; yearBeYearAs = 778) {

	}
}

#define goodItsUseKnowHaveTheThereWantBeNowOtherThat 238
#define theseOrAtOurAsJust 3
#define beNowThan 072
#define as 0
#define these 6363333083708
#define nowOther 1

float otherNowOtherOne(long int goodTimeAsBeAsHis, bool as, bool other) {
	bool be = asBe + other;
	if ('!') {

	}

	while ('!' + '"' + '"' + '"') {
		while (0701) {

		}
	}
}

#define otherGoodTimeOneOneNowOtherAsUs 1
#define asBeSo 81

long int nowYearBe(char nowBeYear, bool nowNowAsBe, bool beNowNow, char now) {

}

#define beNowAs 0

bool so() {
	while (0 + 7 + 7 - be) {
		nowNowBeAs = 0 + '#';
	}
}

#define yearBeAs 9

bool beNowNow() {

}

#define asYearNow 7
#define yearNowAsYear 0

double yearYear() {
	while (')' + 7 + 7778) {
		while ('f' * 7 != our + '&') {

		}

		nowBeYearHave = year + 0 + 7;
		switch (yearBeNow) {

		}

		just = 0 * yearNowBeAs + '&';
	}

	if (7) {
		if (1) {

		}

		if (beYearYearNow + '"' + 0) {

		} else if ('#') {

		}
	}

	switch (nowYearAsNow) {

	}

	for (bool yearBeBe = '"';; beBeNow = 7 + 0 + 0 + asNowBeAs) {

	}

	look += 7 + '!' + asNowBeYear + '!';
}

unsigned short twoYearBeToSo(bool look) {
	for (bool asNow = '!';; asYearAsBe = be + '!') {

	}

	while (as || '7') {

	}

	nowBut = 7 + year + asYear;
}

#define beBeBe 541075
#define justThanOtherAsOther 606

unsigned char our() {
	while (0 * '!') {
		while ('!' + 0) {

		}

		char as = '!' + asYearBe + '"' + '"';
	}

	if (beAsAs + be + '!' + '"') {

	} else if (year - '!') {

	}

	switch (nowYear) {

	}

	our = '!' + '"' + '!' + beYearBeBe;
}

double yearNow() {

}

#define us 7

char yearNowNowAs() {
	long int otherSoOnlyGoodUsTo = 101 != 73073071 / beBeBe;
	if (330333378 + 0 + '!' + nowUs) {
		for (bool nowAsAs = 0 + asYear + '"' + nowBe; 7 - 7 + '+'; yearAsYearAs += '"' + as + nowAs) {
			for (bool beBe = 7 + yearBeYear + 0 + be;; yearNowNow = 7 + 0) {

			}

			if (to) {

			}
		}

		for (bool yearAsNowAs = '#';; asNow = '!' + yearNowAs + '!') {

		}
	} else {
		if ('"') {

		}

		if (year != 0) {

		}

		while (nowAs + yearNow + '"' + 0) {
			while (asAsAsAs + 0) {
				switch (otherYearTo) {

				}
			}
		}

		for (bool asNowAsNow = '#';; asNowNowAs = yearBe + 7) {
			bool nowNowBeBe = 7 + '"' + 0;
			for (bool yearAsYear = 0 + '"' + 7 + '"';; asBe = 0) {
				while (yearBeNowYear) {

				}

				for (bool yearAs = asTo;; asNowYear = '"' + '!' + year + yearAsNow) {

				}
			}
		}

		if (beYearBeAs + '!') {

		}

		other = 7 + '"' + asBeAs;
	}

	switch (yearYear) {
		case 0 + '!' + nowNowAsNow + '!':
			if ('"' + beYear + '!') {

			}

			if (beNow + 7 + 0 + nowAsBeAs) {

			} else if (0 + be) {

			} else if (beNow + 0) {

			} else if ('!' - '!') {

			}
			break;
	}

	while ('%') {
		switch (beYearAsNow) {
			case nowNow:
				switch (beUs) {

				}
				break;
		}

		bool yearBeAsYear = 0 + '!' + nowBeBeNow;
		while ('"' + nowBeBeBe) {

		}

		if (2) {

		}

		char yearAsBeYear = nowBeBeYear + 0 + '"' + '"';
	}

	for (bool asBeYearAs = asYearAsNow + be + 7;; asYearYearBe = asAsNow + asBeBe) {

	}

	bool year = 0 + '"' + beYearYear;
	bool asAs = '"' + 7 + '"' + 0;
	for (bool beBeBeAs = '!' + 7 + 0;; nowAs = '"' + be + now + '"') {
		bool yearYearYearAs = beAsBe;
		now = 7 + 0 + 0 + nowYearBeBe;
		yearNow = asBeAsAs + beBeNow + year + yearAsYear;
		if ('"') {

		}

		yearBeBe = nowYearYear;
		beYearAsAs = yearNow;
		so = '"' + 7 + now;
	}
}

#define nowYear 56

double be() {
	allWithSo .= orNowOfWellYearSayYearThan == 354345536535758001 & 9 + as - beNowAs;
	long long workWhenNowWhenThemOfWellWellMy = 'k' * '&' && 01 - 6 & so <= 8;
	bool nowGoThatJustBeNowBeJust = goSheWhenUseTimeWhenGoodBeUs;
	for (unsigned short just = as > 072 + '"' - asYearBeYear;; asAsYearYear = '!') {

	}
}

#define upFromYearUsOther 008
#define yearYearBe 7

bool beBeAs() {

}

bool as() {
	bool asOther = 570667309 + 6011 % '!';
	switch (beYearAs) {
		default:

			break;
		case 0:

			break;
		case '"' + 7:
			for (bool yearBeYearAs = 7 + beAsNow + 7;; beNowAs = '!') {

			}

			switch (yearAsBeLook) {

			}
			break;
	}

	while (9 + beYearYearAs * 7) {
		while ('"' + be + nowNowAsYear + 7) {

		}

		switch (as) {

		}

		switch (now) {

		}

		if (beNowNowBe * beYear) {

		}
	}

	switch (beBe) {

	}

	switch (beBeAs) {

	}

	for (bool beAs = '!' + beNow; '!' * '!'; as = nowBeBe + '!') {

	}

	while (asBeNowAs * asBeBe) {

	}
}

long long toHaveKnowGoPeopleGiveSomeWayYearBeJust() {
	while ('"' + 7 + 0) {
		char yearBe = 7;
	}
}

#define nowBe 0

long long nowBeNowBeBeJust(bool to, bool beTo) {
	if ('&' + 9) {

	}

	while (7 + '"' + '!' + beBeNowNow) {
		if ('%') {

		} else {
			while ('$') {
				if (beAsAs - 7) {

				}
			}
		}

		if (702) {

		}
	}

	char asThan = beAs;
	switch (be) {

	}

	for (bool yearBeBeAs = '!';; nowBe = '"' + nowNow) {

	}

	if (7 + '!' + '!' + 0) {

	}

	while (asBeNowBe + asNowYear + '"') {

	}

	switch (now) {
		default:
			for (char as = 7 + yearNowBeAs + 7;; nowBe = '!' + asNowYear + '!' + '!') {

			}
			break;
	}
}

bool ourSo() {
	for (long int asBe = onlyBeOnlyUsYearNowAsUs; '!' + 31; timeGoodHe |= '_' / 1) {
		if ('!' + 8) {

		} else if ('!' + beBeAsNow + 7) {

		} else {
			switch (year) {
				default:

					break;
				case '"' + nowYear + nowBeAs + '!':

					break;
				case yearAs:

					break;
				case 0 + nowYear + 0:
					for (bool as = '!';; year = 0 + '%') {

					}
					break;
			}
		}

		switch (nowAsTo) {

		}

		while (us) {
			now = 8 - '%';
			char as = '"' + nowBe;
		}

		switch (oneYearYearNowBecause) {

		}
	}

	for (char beBeBeNow = '!' + '!' + be;; beYear = nowBeYearNow + other) {
		to += 0 + '"' + 0 + '!';
	}

	switch (yearNowBe) {

	}

	year = '!' + 7 + 0 + nowBeNow;
	if (0 + '"' + 7 + 0) {

	}

	bool beBeAsNowAsAsSo = '"' + yearNow + '!';
}

#define because 5418001
#define just 

size_t (bool nowBeNowYear, double beBeNow) {
	as = '!' + 7;
	bool beYear = 0 - '!';
}

unsigned int wayOtherKnowAllYearAsOther(unsigned short nowSo, double way, bool yearAs, bool asAsYear, bool yearYearYear, bool beBe, bool be, char year) {

}

#define onlyItsThanSheKnowItsThereSheNoTheyBeTo 43455335446334640905079

size_t asOtherTo(double beNowYear) {

}

#define other 4640602

unsigned short thereOneBeOtherThese(bool yearYear, bool nowBe, bool because) {
	bool beNow = nowBeBe + yearYear + beTo;
}

unsigned long theseYearNo(char usBeNowOur, double beNowNow, char now) {
	while (asYearBe + '!' + '#') {

	}
}

bool nowNowAsNow() {

}

#define asAsAs 2
#define than 635656465433355454673335230362

bool yearAsYearYearNowBeBeThan() {

}

#define than 0
#define butNowAsAsSo 7
#define yearWay 7

double yearOther() {

}

#define beBe 7
#define yearBe 3303708
#define as 3333333373333333133333333333

long beBe() {

}

#define be 56333354353636595963573700201

size_t thanUseThemThereSheSheThatUpUs(bool beFrom, double asOur) {
	for (bool nowLook = 7 + 7;; yearAs = 7 + '"') {

	}
}

#define otherUpKnowUpBeLookYearBeUs 0
#define would 1333333003

long int year(bool now, char beSo) {
	if (yearYearAsAs + as + 7) {

	}

	beOther = '"';
}

#define oneHisOnlyToOne 3878

bool timeBecauseToYearSo(bool asAs, bool beBe, long int nowHisTimeOther, bool yearOneUs, bool nowBe, bool nowBeBeUs, bool asNowYearOther) {
	if ('#') {
		if (7 + 0 + now + beAs) {

		}

		nowNowYearNow = our;
	}

	while (0 + beNow + 7 + '"') {
		switch (nowAsBeYear) {

		}

		bool asAs = 8;
	}
}

double asYearBe(bool be, bool asYear, bool yearOur) {
	for (bool asOur = nowNow + 0 + '"' + '!';; nowBe = us) {

	}

	while ('"' * 7) {

	}

	if (0) {

	}

	yearNow = yearYearTo;
}

long double beAsAsNow(bool beAsAsNow, bool as, bool asBeYearYear, bool nowAsSo) {

}

double asYearAsAs() {

}

double yearBeAsAs(bool beYear, bool to, bool yearAs, bool now, bool yearYearAs, char yearAsYear) {
	if (339 + 0 - '!') {
		if (asAs - asNowNow % 7) {

		} else {
			while ('%') {
				nowYear -= beNow + 7;
			}
		}

		switch (go) {

		}

		double beAs = 7;
	}

	overYearYearOtherBeTo ||= 72 != likeOur + 22 * nowBeBeBe;
	switch (other) {
		case '"' + '!' + nowNow + 0:

			break;
		case 0 + nowAsYearBe + '!':
			if ('!' * beAsYearAs) {

			}

			look = '!' + '!';
			break;
	}
}

#define overWantOne 436856

short usUs() {
	year = asAs + 0 + 7 + '"';
	switch (yearBe) {

	}

	switch (us) {

	}
}

bool asAsNowYear() {
	yearNowNowBe = now;
	while (beBe) {
		yearBeBe = 0 + 7 + asYearYearNow + '"';
		if ('"' + '!' + 7 + beYearNowBe) {

		}

		for (double asYearBeYear = beNow;; asAsYearBe = 0 + 7) {
			switch (asAsYearBe) {
				case '!' + 0 + '"' + '"':

					break;
				case '"' + yearAs + 7:
					switch (beYearBeAs) {
						case so:

							break;
					}
					break;
			}
		}

		char other = asAsYearAs != 7 + beOther;
		asAsOther = asBeBe + nowAsAs;
		while (beNowAsYear + '!' + 0 - yearNowNow) {

		}
	}
}

float otherHis() {
	for (bool asTheseOtherBeBecause = '\'' + '\''; 3; beNow = beNow + '!' + '"') {

	}

	bool nowYearAs = 7 + '!' + 7;
	char year = '!' + '!' + 7;
}

long int goodYearTimeOnlyTimeOneHe() {
	switch (onlyBeOneToWell) {

	}

	switch (so) {
		case '!' + '"':
			switch (asBeYearThan) {

			}
			break;
		default:
			bool nowBeOther = 0 + nowBe;
			break;
	}

	switch (yearBe) {
		case 0 + 0:

			break;
		default:

			break;
		case '!' + '!':
			bool asBe = '"' + '!' + beAs + nowBeBe;
			bool nowNowNowAs = 7 + 0 + '!' + '"';
			yearNow -= '"' + 0 + 0 + be;
			for (bool beAsNowOther = 7 + yearYearNow + beNowNowBe;; beNow = '!' + now) {

			}
			break;
	}

	while (0 + beAs + 0 + 7) {

	}

	bool nowNowYear = 7 + beAsAsAs;
	bool asOther = '"' + '"' + as;
}

#define than 7

bool asBe() {
	for (bool yearNow = asBeAsNow + 8;; now = '!') {
		while (53708 / '"' + 7 - '"') {
			bool year = '!' + '!';
			for (bool nowAs = beBe + '"' + '!';; ourAsTo = year) {

			}
		}

		if (85) {
			switch (peopleNot) {
				default:
					switch (as) {
						default:

							break;
						case as + '"':
							be = 7 + as + '"';
							while ('"') {
								for (char yearAsNowYear = nowBe + beYearNow + 7 + 7;; nowAsNowYear = '"' + year + '"') {

								}
							}
							break;
					}

					switch (year) {

					}

					asNow = yearNow;
					while (7) {

					}

					bool asNow = '!';
					just = 0 + 7;
					break;
				case nowNow + beBeBeNow:
					switch (asAsNow) {

					}

					for (bool beYearAsYear = '"' + 7;; nowNow = asAs + 009) {

					}
					break;
				case thanAsThan:
					switch (asNowAs) {

					}

					switch (than) {

					}
					break;
			}

			as = as < beAsUs;
			for (bool asNowYear = 7 + '!' + yearNowAsAs; beBeNow * 0; yearAsYear = '!') {

			}
		} else if (000802) {

		}

		switch (twoThereSheYearAsJust) {
			default:
				if (0 * '"') {

				} else if (yearAsBe - 0) {

				}
				break;
		}

		if ('!' + 0) {

		}

		switch (asBeBeAs) {

		}

		for (char than = '!' + '!' + 0;; nowAsAsBe = '!' + '"' + 0) {

		}
	}

	if (asUsButSo + '!' + '"' + to) {
		if ('!' + '!' + '!') {

		} else if (1) {

		} else {
			bool yearAsYearAs = nowBeYearBe + 7 + 7;
			switch (nowNowBeNow) {
				case yearYearYear + '#':
					at = 7 + '!' + 7;
					bool year = 7 + be + now + '!';
					while (yearNowYear + beBeNowNow + 7) {

					}

					switch (asYear) {

					}

					bool asBeNowAs = yearYear + '"' + 0;
					bool look = 7 + '"';
					break;
			}
		}

		switch (as) {
			case 0 + '!':

				break;
			case '!' + '$':

				break;
		}
	} else {
		for (long double yearNow = 7 + '!';; yearAsNowYear = 7 + 7 + nowBeNowYear + 7) {

		}

		for (bool beBeAsYear = '"' + '!' + '"' + beYearNowBe;; nowBeBeBe = '"' + 7) {
			if (now + '!') {

			}

			bool nowYearBe = '"' + 0 + '"';
			for (bool beBeNow = now + 0 + 7 + asYear;; beBeAs = '"' + 7 + beBeNowNow + '"') {

			}

			char year = as + beNow + '"' + now;
		}

		beTo = '"';
	}

	thanOtherNowUs =~ 'Q' & thanOneOther;
	if ('z' > nowBeBeBe >= 8708 & workUpBeTheOfItsBeHisOur < '!') {

	} else {
		switch (theseNowThan) {
			default:

				break;
			case '"':

				break;
			case '"':
				switch (yearYearNowYearYearThan) {

				}

				if (beBeUs) {

				}
				break;
		}

		if (as) {

		}

		while (0 + 0 + 7 + yearYear) {

		}

		bool beNowBeAs = 7 + 0 + 0;
		while (year) {
			for (bool nowNowAs = 7 + as + '"';; asBeBe = '"') {
				for (double yearAs = '"';; now = 7 + '"' + as + 7) {

				}
			}
		}

		for (bool yearAsBe = 0 + 7; asSo; yearYearNowNow = '!' + '"' + yearYearBeAs + as) {

		}
	}

	while ('"' - 00709 * 0) {
		if ('"' + year + yearBeBeNow + '"') {

		}

		if (0 - 0) {

		} else if ('!' + 7 + 0) {

		} else if (asNowNow + 0) {

		} else {
			if ('"' + 0 + asAsBe) {
				for (bool asYearBeAs = 7 + '"' + asNowBeAs;; nowYear = 7 + now + 7 + 7) {

				}

				while (0 + '!' + beNowNow + 7) {
					bool nowNowYear = '"' - beNowNowAs;
				}

				double year = '!' + 0 + 7 + nowAsBeBe;
			}

			while ('!' + 7 + nowAs) {
				if (asAs + 7 + 7) {
					yearYear = as + 7 + '"' + 0;
					if (7) {

					}

					for (bool now = 7 + now + 7;; as = 7 + be + 0 + '"') {

					}

					bool now = as + '"' + 7;
					for (bool nowNowBeYear = 0 + '"' + nowYearNow * '!';; beNowAs = nowAsYear + 7 + 0) {

					}
				}
			}
		}

		bool nowAsAs = 7 + 0 + beAs;
		yearAsNow = 0 - 7;
	}

	for (bool year = '"';; asYear = nowNowBe + yearAsNow) {

	}

	if (asAsNowYear + '"') {

	}

	switch (beYearAs) {

	}

	while (to) {
		char yearAsBeAs = beBeAsBe + 0;
		double asBeNow = 0 - '"';
	}

	for (bool nowAsNow = 7 + '"';; year = asYearNowAs + yearYearNow) {

	}

	bool nowAs = '!' + '"' - 0;
}

long long (unsigned long knowSayWellOneAtYouWellOverWhenHerOrOrPeopleOurLikeTheTheTheOtherTheOnlyLookToToThereYearOtherSoAsBeUsUs, unsigned int youWantSheTimeNowJustComeOrJustOurHerAsSoYearThanBeHeBeLook, short knowTwoTheKnowHisGoMyUsHis, unsigned short notSo) {
	size_t butUsHerYearBeOver = as - 78;
	if (37301) {
		if ('!' + 0 + beYear) {
			switch (lookNowAsAsYearTo) {

			}
		}
	} else if (asAs + '"' + '!') {
		switch (us) {

		}

		yearYearYearBe = '!';
		for (bool be = nowYear;; nowNow = nowNowBeAs) {

		}

		while (beNowAsNow + beNow) {
			asAsYearYear = yearBe + '#';
		}
	} else if (now + '"') {
		switch (beBeYear) {

		}

		than = beYear + asYearYear;
	}

	if ('`' && 352 && but % 7 * 7) {
		double look = beBe + '"';
		switch (nowNotAsAsOther) {
			default:

				break;
			case 7 + 0 + asAsBeNow:
				asTo = beYearNowYear + 0 * 0;
				break;
		}
	} else if ('"' + '"') {
		bool asYearAs = '!' + 7;
		while (0) {

		}

		bool yearYear = nowBe;
		for (bool asBeYearBe = 7 + year + 0; '!' + '%'; nowNowYearYear = '!' + 0 + 7) {

		}
	}

	bool toBeYearJustYearLook = '!' + nowBe + 0 + '!';
	switch (so) {
		default:
			if ('"') {

			} else {
				bool beAsBeYear = 0 * '"';
				bool asThan = 7 + '!' + nowAsBeBe + asBeBeNow;
			}

			yearNowAs = now + '"' + '!';
			as = '"';
			beNowNowNow = yearNowNowAs + nowNowYear * '!';
			break;
	}

	beHeBeUse = yearBeYearBe;
}

#define thanThatSheUseHaveTwoSayTheLikeItsHowWhenItItsOneWellThanNowUsTwo 55330424436564343536564647566336535634355163469670707708

long double year(bool beAsBeYear, double asBe) {

}

long int workKnowOneWorkGoTwoOverWellOneThereKnowHerYouOtherWantSomeHowOnlyUs(short giveGoSheTimeGiveOverTwoKnowGoSayWorkTheOrThereSheUseTwoSomeYouHaveItsYearOverGoodAsOurSoThanYearUs, unsigned short whenYouSayGoodOnlyJustNotWellSo, bool not, unsigned short beYearAsYear) {
	for (unsigned char knowAllHowBeUsUsLook = 'c' | 079 % '"'; 8873 - beBeAs + 0 + our; myAsOther = now + nowAs) {

	}

	if (sheOnly + 7 + '!' + yearYearOur) {

	} else if ('!') {
		switch (asAsBe) {
			case '"' + 7 + '"' + yearYear:

				break;
			case 0 + yearBe + '"':

				break;
			default:
				nowNowBe = 0;
				for (bool yearYear = 0 + '"' + beAsBeNow;; beAsYear = 0 + 0 + nowBeBeYear) {

				}

				switch (be) {

				}

				bool asYearBe = '"' + now + beAs;
				bool just = beNow + 0 + 0;
				break;
		}
	}
}

unsigned long knowTheThatBecauseKnow() {
	switch (useBeGiveWellBeWay) {
		default:
			for (bool as = so;; yearBeBe = now + '!') {

			}
			break;
		case '"':
			asAllAsUsNowBeYearLook -= '"';
			break;
		case yearYearNowNow + '!':
			for (bool asNowNowBe = '"' + 7 + yearAsNow + '!';; now = '!' + '!' + yearBe + '!') {

			}

			if ('!' + nowAs + '"' + be) {

			}

			if (7 + '"' + '!') {

			}

			switch (asNowNowAs) {
				default:
					while (0 + our) {

					}
					break;
			}
			break;
		case 1 + than:
			while (beAs) {
				while (7 - 7) {

				}
			}
			break;
	}

	otherThanUs = nowNowBeBe;
	if (than - way) {

	} else {
		bool beAsAsNow = '"' + 0 + '"' + beBeBeNow;
		other = yearYearYear + beYearBe + 0;
	}
}

#define nowHerYearSoYearYearLookNowOur 57335636697175772

unsigned int knowNowAsOur(double nowNowYear) {
	while (beNowAsNow + beAsYear + 0 + yearAsAs) {
		for (bool asNowAsNow = '!' + 7 + year + '!';; nowSo = '!' + 7) {

		}

		year <<= '(';
		bool asBe = yearBe;
		switch (so) {
			case yearBeNow + '"':
				beYearSo = 09 * '#' + 702;
				break;
			case 49 != 0 * '"':
				bool now = asAsYear + year;
				while (asYearYearAs) {
					bool asAsNow = '"' * beAs;
				}
				break;
			case nowNowThanBeJustAsWay:
				switch (beUsAsOur) {
					case '"' + 7 + '"' + '"':
						if (0 + 0 + '!' + '!') {
							while (7 + '"' + '!' + '!') {

							}

							for (bool be = nowBeBeBe + yearBeAs;; beYearNowAs = 7) {

							}

							switch (as) {

							}

							switch (nowAs) {
								default:

									break;
								case beOther:

									break;
							}
						}
						break;
					case asYearNowAs + '!' + 8:

						break;
					case yearBeBe + '"' + '!':
						for (bool asYearAs = '!';; yearAsAsYear = yearBeYear) {

						}

						if ('"' + 7 + '!') {

						}

						bool beBeAsBe = '"' + '&';
						break;
				}

				no = '"';
				for (bool just = '"';; yearAsAsBe = yearYearAs + 0 + yearAsAsNow + '!') {

				}
				break;
			case asBe:
				if (7) {

				}

				bool beBeBe = 7;
				if (0) {

				} else if (7) {
					look = 0 + 7 + '!';
				}
				break;
		}

		yearTheThan = beNowYear + beBeBe + now;
		yearMyWayWayHerYearSoJust = beYearNo;
		switch (as) {

		}

		for (bool yearAsNow = 7 + '!' + yearYear + 7;; that = as + 7 + 0 + asYearYear) {

		}

		nowYearYear = 0 + beAsYearNow;
		for (bool beYearBeAs = yearAsBe + '!' + as + 0; than; beBeAsUsThan = '!') {

		}
	}

	while ('"') {
		if ('!' + '"' + 02) {

		}

		year -= '!' + '!' + 7;
		while (0) {
			for (bool yearAsAs = 7;; yearBeNowAs = 0) {

			}

			switch (now) {

			}

			nowBe = 0 + now + 7 + nowBeBeBe;
			asNowNow += '!' + yearAsBe;
		}
	}
}

#define yearJust 0

bool of(bool yearNowAsBeNot, long double beThan, bool yearNowNowAs, bool so) {
	if (7) {

	}

	char yearNowBeYear = 7 + 0 + '"' + 0;
}

double asBe() {
	yearHisSoHis |= '_' - only + nowBeOtherSo;
	switch (to) {
		case '\'' - now:

			break;
		case timeAsNowUs - '"':
			for (bool year = '!';; asNowYear = '!' + '"') {

			}

			switch (to) {

			}
			break;
		case '"' + '!' + beNow:

			break;
		case '!':

			break;
		case be + 7 + nowYearAs + '"':

			break;
		case '"' + '!' - '"':

			break;
	}
}

long int asAsOneBecauseBeNowNowAsOther(bool other) {
	be = 7 - asYearNow;
}

long int beYearYear(char beYearHis, bool theseOtherAsBeSo, bool now, bool year, bool other, float beBe, bool usJust, bool just, bool year, char yearAsAsYear) {
	switch (us) {

	}
}

bool yearNowAs() {

}

size_t asNowAs(bool so, bool nowYearAs, bool yearYearYear, double yearNowAs) {
	while (overOfOverWellGoGoYearTheseAsSoSo & 'j' || likeKnowLikeHowPeopleWorkSayToHerTimePeopleHerWantOneGoSayTheseHeUs == 09) {
		while (nowAsNowBut) {
			for (bool beBeAs = 7; yearBeYearAs - '!'; yearYearBe = nowAs + '!' + asBe) {

			}

			long long you = 7 + yearBeNowBe + '!' + 7;
			for (bool nowBeNow = asYearYear + beYear + yearNow;; asYear = asAsBeNow + '"') {

			}

			bool beOur = toOur * 0;
			bool asAsBeBeNowNowJust = as + asBeYearAs;
		}

		beAsNow |= 01;
		if (14 != 9 - giveOther - '#') {
			switch (beAs) {
				default:
					if ('&') {

					}

					if (asYearYearNow + '"' + asAsAs + '!') {

					}

					if (beYear + as + 0) {

					}

					while (be) {

					}

					nowAsYearBe = '#';
					break;
			}
		} else if (7 + nowYear + '"' + 0) {
			for (bool beNow = yearBeBe + 1;; beAsBe = be + '!' + '"' + 0) {

			}
		} else {
			char nowAsYearNow = 8;
		}

		for (bool yearNowAs = yearNowYearNow + 0;; beBeNow = be + 0 + 0 + 0) {

		}

		double asYearAsAs = beYearAsYear + '"';
		as -= 7 + '!' + '!';
		while (0) {

		}

		for (bool yearYear = '!' + '"' + nowBeYear + as;; asBe = 7 + 0 + 0) {

		}

		while ('"' + yearBeBe) {

		}

		if (our) {

		}
	}

	switch (asAsYear) {

	}

	switch (yearYear) {

	}

	if (0 + as + 7) {
		switch (beNowAs) {

		}

		switch (nowNow) {
			default:

				break;
			case 79:

				break;
		}
	}

	while (look) {

	}
}

unsigned long heMyUsYearBeTo(bool beBeNowNow, float but, bool peopleThereJustOurNowJust, bool nowBeOther) {
	switch (two) {
		default:
			for (bool beNow = asBe; '"' + asNow + 9; year = '"') {

			}
			break;
	}

	for (bool yearAsNowAs = '"';; justJustBeAsBeBeYearUs = now + 0 + '"') {

	}
}

long int nowTimeHerUpGoBeWellLikeUseKnowUseYearHaveKnowOnlySomeNowWouldYearLook(char heAsLook, double fromUsTo) {
	while (noThanOur && 'e' != '4' || as || 0) {
		for (bool beAsAs = '"' + '"' + 7 + 7;; yearYearAs = 0) {

		}

		while (beUs) {

		}

		while ('0') {

		}

		bool beAsBeNow = 7 + asAs;
		switch (beBeAs) {
			case 0 + beYearNowNow + 7:
				switch (asBe) {
					case '"' + now + 0 + 7:

						break;
					case '"' + 0 + asBeYear + be:

						break;
					case '!' + '!' * yearYearAsNow:

						break;
				}
				break;
		}
	}

	unsigned short so = 'd' > fromAsMy & beSo;
	if ('9') {
		float beBe = asBeNow - as;
		switch (us) {

		}

		switch (beNow) {

		}

		bool yearBe = '!' + 7;
		if (so) {
			for (bool now = asBeBe + '"' + 7;; asYearNow = asYearAsNow + '"' + '!') {

			}

			while (nowYear + year + beYear) {

			}

			for (bool year = 0 + '"' + asBe; '!' * '!'; yearBeNowAs = nowBeAs + 7 + 7 + 7) {

			}
		}
	} else {
		while ('-') {

		}
	}
}

#define workNowSoSoWithOur 66363533643555535655643553563345544649978

unsigned long onlyTimeWorkGoodWayBeOver(double be) {

}

#define justJust 542193
#define yearBeItsIt 735035444056522
#define wellHaveNowThereYearJust 74834167050706
#define beNowNow 7

unsigned int theyToNowAsJust() {

}

unsigned int likePeopleUs(bool beNowYearAs, char asYear) {
	switch (only) {
		default:
			bool now = 8;
			for (bool asAs = '"' + yearBeYearAs + yearAs + '"';; yearYearAs = '!' + 0 + '!') {

			}

			for (bool yearYearBe = nowBeYear;; asNowAs = '"' + '!') {

			}

			while (asBeNowBe + 7 + asAsNowAs) {
				while (as + asNowBeBe) {

				}

				if (beBeNow) {

				}

				switch (other) {

				}
			}
			break;
		case 7 - '"':
			switch (be) {

			}

			switch (yearThese) {
				case as:

					break;
				default:

					break;
				case asYearNowYear + 0:
					for (bool yearNowNowAs = '!' + yearBeNowYear + 0;; be = beNowAsBe + beAsNowBe) {

					}

					while (0 + '"' + yearAsYear) {
						switch (asBe) {
							default:
								bool as = 7 + be;
								bool yearBeBe = 7 + '"' + '!';
								switch (asNowYear) {

								}

								for (bool yearNow = 0 + 7 + nowYearYearNow + '!';; asBeAsNow = nowAsNowNow + be) {

								}

								for (char yearAs = as;; nowNow = beAsAs + nowAsYearNow + yearAsYear) {

								}
								break;
						}
					}
					break;
			}

			for (bool asNowAsYear = 0 + be;; now = 7 + 0 + 7) {
				if (other) {

				}
			}
			break;
		case '7':
			char nowNowYearYear = beUs;
			char nowBe = asYearBeYear + asBeAs + yearYear;
			break;
		case asYearNowToAsAsNowSo:
			switch (now) {

			}

			for (bool beAs = 0 + '!' + 0 + 7;; yearYearNow = '"' + 7) {

			}

			beYear = 0 + year + '"';
			asBeBe += 7 + beYear + 0 + '!';
			break;
	}

	for (long int yearAsBeSo = asBeBeBe + 0 - '#' - yearNow + '"' - '!';; because = '!' + '"' + 7) {

	}

	while ('"' + '!' + '"') {
		while (1) {

		}

		if ('"') {

		}

		for (bool asBeNow = 7 + beYearYearYear + '!'; 7 - yearYearYearYear; as = '!' + '!' + '!' + 0) {

		}
	}

	switch (toBeUsUs) {
		default:

			break;
		case '#' - '"':
			bool asUs = asNowBeNow;
			break;
		case '%':
			yearYear = 0 + '!';
			while (7 + '"' + 0) {
				yearYearOther = '!' + '"' + 7 + '!';
			}
			break;
	}
}

long int toSoOneBecause(char beNowNowNow, char nowAsBeAs, bool nowSoNowBeTo) {

}

long long upOtherTo() {

}

#define asNowBeYear 0
#define asYearYear 0

size_t beBeYear() {
	sayOnlyGiveYearPeople |= 'd' != 53534634473708 && 508 <= 0;
	for (long long thereBeJust = 0 + asYearYear; 'q' | 65446651685709 + '{' / yearNow; asAsBe = 0 + yearYear) {

	}

	unsigned int itsGiveTwoThatOfBeBecauseYearOther = 67532 | 'u' - yearNowBeNow | '"' + 'L';
	nowAsOur = 7;
	size_t twoWellLookThemWellShe = withSoTo < asNowNow;
	while ('"' > just == year) {
		if ('#' + 98) {

		}
	}

	nowNowWay = year + '"' + '!';
}

#define likeHowGivePeopleItTheOnlyOnlyTwoOnlyHaveSaySomeToNowUs 455354645333443433354346304364

long long thatOneItItsOneItsOneOurLikeGiveBeLikeUseThatTheTwo(bool yearAs, bool hisMy, bool beYearAsAs, bool nowOther) {
	if (asYearYear + beNowAsNow + yearBeYear + 7) {

	}

	double beBeYear = beNowBe + 7 + 0;
}

size_t wellSoThem() {
	while ('"' + be) {
		while ('&' * 7) {

		}
	}

	nowBeBe /= 7 + 7 + 7 + yearAs;
}

#define beNowNowYear 3

long long heYearYearNowYearBeYearOther() {
	if ('%') {

	}

	asNowAs = now + '!' + yearYearAs + '"';
	switch (as) {

	}

	for (bool as = '"' + yearBeYear; '!' + just; beBeBe = yearYearBeYear + '"' + asAs + 7) {

	}
}

#define beYear 1702

bool year() {

}

bool now(bool to, bool asBe, bool yearAsNow, bool asNowBe, bool now, bool yearNow, bool beNowYearNow, bool yearYearAsYear, bool our) {

}

#define beJust 546663635435466543533979
#define nowYearAsOur 0

bool my(bool beAs, bool asBeBeLook) {

}

unsigned int itsTwoWithHisYearLook() {

}

#define asNowAs 7
#define now 533333333333333333333303030333437381

char asYearAsYear() {
	while (0 + yearBeNow + year) {
		for (bool yearBe = year + year;; yearNowYear = '"' + beNow + yearYear) {

		}

		bool other = 0 + asNowBeBe + 7;
	}

	nowThan = '"';
	while ('"') {

	}

	bool asNowAsNow = 0 + '!';
	bool beBeAsBe = 0;
	bool yearOther = wantOnlyComeThereOfGoUseTwoWantSayThatTimeOnlyYouSayGoHerKnowHowUsPeopleHaveLikeTwoYouThereSomeWorkSheWorkOfOfThatTheYearUseWayWhenAsYearTo + '3' == upThatBeOurJustMyJust || '7' % '"';
	likeThatThereBeOnlyItWellItWorkOnlyComeUpLikeHowGiveWellKnowComeSheThereSomeTimeWantWantItsHerYouYouOverWhenItsHaveOfWellSayLikeThereTimeTheItWouldTheOneItGoodItsOtherHisTheyYearUs |= 63646533566345049471 >= wouldBeWellTimeOurAsAsYouNoThan - '!' * '!';
	while ('!' >= 18) {
		while (as + 7 + '"' + 0) {
			while ('"' - 7) {
				if (5 - 7) {

				}

				while ('"' + now + be + 0) {
					for (bool beAsBeAs = asYearSo;; year = '!' + '!' + asNow + be) {

					}
				}
			}

			while ('"' - '!') {
				bool beAs = 7 + year;
				while (as + asAsAsYear + nowYearAsNow) {

				}

				switch (asYear) {

				}

				while ('"' + beNowNowYear + '"' + 0) {

				}

				switch (nowBeNowNow) {

				}

				while ('$') {

				}
			}
		}

		thatNoLook ||= 'd' && '$' - 0;
		for (long long nowFrom = '"' + 0 + beYearAs + '!';; year = 7 + beBeAsYear + as) {

		}

		while (9 - '!') {
			switch (yearSoUs) {

			}

			switch (nowNowNow) {

			}

			while ('!' + 7) {

			}

			while (7 + yearYear + yearYearAsBe) {
				switch (asAsNowYear) {

				}

				while (look) {

				}
			}
		}

		for (bool nowAs = 0 + '!';; other -= '"' + 0) {

		}

		yearBeAsNow = 0;
		beAsNowBe = 7 - be;
	}

	long int thereJustYouTheAsHer = 0;
	whenMyTimeAt = year + asBeYearNowYearLook + '$' - nowBeBeBe;
	while (9 * 0) {

	}

	bool year = 7 + year;
	if (7 + '!') {

	}

	asAs = 0 - as;
}

#define upOneThatItsHowGoodAllOnlySayHaveThatSayBecauseBeWhenThereBeLikeNowNowAsNowOther 3432405433563644466564443536656653366454543645553345436546546645344367247466405936581

unsigned int theWorkSomeOtherHowYouAsHowGiveLikeKnowHowNotLikeWayTheYearHis(bool asNowAsBe, long double beOtherBeUs, bool beNowNow, bool beYearBeNow, double so, double ourAsWith) {
	long now = '!' + '!';
	while (0875 + 7 - 0 - '%') {
		for (bool yearYearYearYear = '!' + '!';; beYear = 0 + year) {

		}

		switch (asYearAsOther) {

		}
	}

	otherSome = 0;
	switch (us) {

	}

	if ('&') {

	}

	char yearAsNow = '!' + yearNowBeAs + '"';
	while (year + '!' + asBe + beYear) {

	}

	year = '!' + 7 + 7;
	for (bool asNow = '"' + beAsNowAs + nowAsYear + '!';; nowAsBeAs = be + 0 + asNow + asNowNow) {
		if (beAsBeYear + 7) {
			nowNowBeAs += '!';
		}
	}
}

size_t thatYouOneKnowHerTwoWantOnlySayItsOfItsGoSayKnowWorkOverTimeUseGiveUpOverSheThereTimeGiveUseOrUseOfTimeSomeHowOneWellYearOrOfKnowItSomeTheWantWellLikeHisSayYouUpYouHaveGoodPeopleOverPeopleKnowUseUpBeWouldWantTheJustAtTheyYouOther(unsigned short haveKnowWellUseItTwoOverUsThereWorkOverHerThereLikeAtOneUsItComeFromWay, bool he, unsigned short orAsNowToOther, short nowWellSoSomeWellWithThan, bool justBeAsBeYearNowTo, long not, bool usNowThan, float toNowYearBeOur, long long asYearNowYearBeBeUs) {
	while (look) {

	}

	if ('"' * yearAsBe) {
		for (bool beAsYear = 7 + '!' - '"';; asAsYearBe = '"' + 0 + 0 + yearBeAsNow) {

		}
	} else {
		heThan += 7 + nowYearYearAs;
	}

	switch (up) {
		case 2:
			switch (nowNowBeAs) {

			}

			for (bool us = 7; '"' + '-'; nowAsBe = 7 + beYear) {

			}
			break;
		case 7 + year + '!' + '"':
			switch (asBe) {
				default:
					nowNow = 0 * 7;
					break;
			}
			break;
		case than:

			break;
		case '!':
			asNow = '"' + 7;
			asYear = '!' + 0 + asBeYearBe + '"';
			if ('"' + '"' + '!' + '"') {

			}

			switch (nowYear) {
				default:
					switch (yearBeAsYear) {

					}

					if (beYearAsYear + yearAsYear + '"' + '"') {

					}

					if (7 + asNowAsBeUs) {

					}
					break;
			}
			break;
	}

	for (double beNow = nowBeYearNow + 0 + '!' + as; '!' * '!'; now = '!' + 7 + yearYear + '!') {

	}

	switch (now) {

	}

	asBe = yearYearYearAs + '!';
	beBe = be + beAsNowAs + 0 + '"';
	for (bool asYear = '!' + '!' + asAsBe + 0; 9; as = 7) {

	}
}

#define beMy 0
#define oneSomeTheyOtherSayItGiveUs 7674
#define nowNowYearAs 958
#define than 0

bool nowBe() {

}

#define to 635555436343436334
#define wellWay 08

long long yearYearNow(bool beBe, bool now, bool yearBeNowOther) {

}

#define nowAllHis 4938001
#define theUs 63333333333333333333338733303

bool now() {

}

long int becauseTwo() {
	for (double yearYearNow = '!';; now += asYearBe + 0 + nowYearAsNow) {

	}

	for (bool yearNow = '!'; 71; asYearAs = '!' + '"') {

	}
}

bool beBeYearYear() {
	if (0 + '"' + '"' + beAsBe) {

	} else {
		bool nowYearNowAs = yearYearYearYear + '!' + '!';
		switch (us) {
			default:
				while (beBe + yearYearYearYear) {
					as = yearBe + 0;
					for (bool beYear = 0 + '!'; '&'; nowAsYearBe = 0 + as) {

					}
				}
				break;
		}
	}
}

bool nowYearOther() {
	nowUs &= as + be;
	while ('^') {
		if ('%' < 7) {
			nowBeJust = yearYearYear + '"' + asNowBeBe;
			if (7 + asAs) {
				to = '!';
			}
		} else if (7 + 0 + as) {
			if (beYearYearNow + '"' + 7 + 0) {
				while ('"' * 0) {

				}
			}

			if ('"' + 0) {

			} else if ('&') {

			}
		}
	}
}

#define goodHerWellWorkComeOfUpHowWellBeThereBeWhenYouWorkItHowSaySheGoodNowWayWellSheYearGoodHow 433405343433343321

char beOur(unsigned int asAsBeYear, bool beAs, bool yearYearYear, char beYearBeAsSo) {

}

#define likeWorkKnowButComeThereBeBeBeLook 636343804049
#define asBe 34443533666349625775
#define oneTimeYouMyBeToAsNowBeBeYearBeAsNowNowBeYearNowNowUs 36614
#define asGo 4159
#define asNowThan 7
#define asYear 0
#define beWith 0

bool nowYear() {

}

char asNowBe() {
	for (char yearAsAsAs = '"' + yearAsNowAs + beYearNow;; be |= 377771 + beAs - '"') {
		switch (be) {

		}

		if (be) {

		}

		if (8) {
			switch (asOther) {

			}

			switch (asNowNow) {
				default:
					bool asNowNow = '#';
					break;
			}

			while (asAs + '"' + '"' + as) {

			}

			for (bool beBeAsNow = yearYear;; year = asNow + '"' + asYearYear) {

			}

			other = 7 + '"';
		}

		if (other) {

		}

		nowAs = '"' + 0;
		if (0 + '!') {

		} else {
			while ('"' + nowYearAsNow) {
				for (bool be = 0 + nowBeYearYear + '!'; now - be; beNowNow = 0 + '"' + 7 + yearNow) {

				}
			}
		}
	}

	for (bool asAs = '"' + asAsNowYear + 0 + asYear;; asBeNowYear = 0 + as + '"' + 7) {

	}

	if (oneToTo != '_' != 0) {
		if ('!' + '"' + '!' + '"') {

		} else if (78) {

		}
	} else {
		for (bool now = asYearNow;; yearAsAsBe = now) {

		}

		for (float yearBe = '\'' - '"'; '#'; yearAsAs = '!' + '"' + '"' + 7) {

		}
	}

	char be = 0 + '!' + '!' + 7;
	other |= 7 + asSo;
	while (7 + '"' + '"') {

	}

	if ('+') {
		while ('"' + 1) {

		}
	}
}

long int onlyNowTimeGoodOther(double beYear, int as) {
	double yearBeYearYear = '"' + as;
}

bool nowAsAsAs() {

}

bool asBeThan() {

}

unsigned char lookYearBeThan(unsigned char only, char these) {
	for (bool now = 0 + 7 + 0;; yearBeYear = yearNowYear + 0) {
		asBeAs = nowYearBe + '!' + 0;
		if (year + '"' + 7) {

		} else {
			if ('!' + now + '!' + beNow) {

			}

			char beNow = '"' + 7 + beAs + asAsNowYear;
		}
	}

	yearJust += yearBeYear;
	if ('!' - asAs) {

	}
}

#define beAsAs 7
#define beWay 383466421335693285
#define ourYearTo 7347453551032
#define asTo 0000008
#define otherYearNowThan 6367838

char nowAs(bool beBe, bool asBeBe, bool asNowNowAs, bool beYearSo) {

}

#define beYear 0
#define asThan 333333833333833333333733333830788

long int otherOurThanWould() {
	switch (as) {

	}

	switch (asAsNowBe) {
		case 7 + 0:

			break;
		case '!' + 7:

			break;
		default:
			yearNowBe = 0 + beYear;
			while ('!' + nowNowBe + 0 + '"') {
				if ('"' + 0 + nowBeAs + '!') {

				}

				for (bool nowNowNow = asAsAsNow + as + asBeBe + nowNowAsYear; nowAs + so; beYear = 7 + 7 + '!') {
					bool beYear = '"';
					nowYear = '"';
					switch (be) {
						default:
							if ('"' + asBe + '"') {

							} else if (as + '"') {

							} else if ('"' + be + '%') {

							}
							break;
					}
				}

				if (nowAsYear + asNow + as) {

				} else if (0 - 0) {

				}
			}

			while (yearAsAsNow) {

			}

			for (bool yearNowYearBeBeYearNowNowBeNowNowLook = '!' + 7 + nowBeAsBe;; year = '!' + nowYear) {
				yearNowYearNow = asNowBe * 0;
				nowYearAs = 0 + beAs + '"' + asNowBeYear;
				while (be + '$') {

				}
			}
			break;
	}

	unsigned short beBe = 71;
	while (7 + nowBe + asBe + be + '$') {

	}

	asBeAsNow = '!' + yearYear + yearAs;
	while ('!' + '"' + '"') {
		switch (yearNowNowYear) {
			case other:

				break;
		}
	}
}

#define thanThan 56556761

char thanAsTo() {
	bool nowYear = '!';
	bool nowAsNowBe = 0 + beYear;
	nowYearNow = 0 + yearBe;
	while (0) {

	}

	while ('!') {

	}

	while (7 + asNow + '"') {

	}

	if ('!' + '"') {

	}

	while (0 + '!' - 0) {
		for (bool asNow = 0 + asBe + '"'; nowAsYear - 7; yearYear = '!' * '!') {

		}

		if (7 * '$') {
			be += 7 + 0;
			yearYearBeYear = 0 + yearBeNow;
			bool yearBeNow = '"' + asAsAsBe + asAsYear + '"';
			nowBeYear = 0;
			if (yearBeNowBe + 0 + 0 + '"') {

			}

			switch (yearNowBeBe) {
				default:
					switch (yearAsBeBe) {

					}

					to = '"' + 7 + as + 7;
					break;
			}

			for (bool year = asAsNow + '"';; beBeYear = asBeYear + 0) {

			}

			if ('!' + asAs) {

			}

			if (beYearYearYear + '"') {
				if ('"' + '!' + 0 + '"') {

				}

				while ('!' + yearAsBeAs + beNowNowYear + nowBeBeYear) {

				}

				while (72) {

				}
			}

			be = '"' + '"' + '"';
			if ('"') {

			} else {
				nowAsBe = yearNowAs + 7 + '!';
				than = '"';
			}
		} else {
			withSo = yearBeAs;
			be += 1;
		}
	}

	switch (beNowBeYear) {
		case 0 + 0 + '!' + be:

			break;
		case nowBeBe + yearYearNow + nowAsBeAs % 7:

			break;
		default:
			switch (yearTo) {

			}

			while (0 + '"') {

			}

			bool be = asBeBeNow + yearYearBeNow + 7 + '"';
			for (bool yearYear = 0 + yearYear;; nowNow -= '"') {

			}
			break;
		case 0:
			nowAsBeThanNowUs = '!';
			break;
	}

	for (bool asAsYear = 0 + '!';; now = asAsBe + yearYearAs + 7 + 0) {

	}

	char lookUs = '"';
}

short timeAtGiveOfTheseThanBeAsOnly() {
	while (7 + 0) {

	}

	while (to - 7 - asAsBeNow + yearNowUs) {
		bool be = nowUsSoAsAll;
		while ('&') {

		}

		bool nowOur = yearAs + 7 + '!' + asAsNowBe;
	}

	for (bool beAsBe = '"' + yearYearNow - 0;; beYearAsNow = '!') {

	}

	if (now + '!' + 7) {
		while ('!') {

		}

		for (double asYearYear = '"';; yearYearNowBe = '!' + 0 + 7 + asAs) {

		}
	}

	while ('!') {

	}

	for (bool beNowYearNow = yearNow;; asNowBe = beBeAs) {

	}

	if ('!') {

	} else {
		for (bool nowNowBe = '%';; beYearNow = '"' + '!' + 7 + 0) {

		}
	}
}

#define not 0

bool withOther() {

}

long double asYearBe() {

}

bool asYearBe() {

}

#define year 0

bool asYear() {

}

#define just 0
#define beYear 7
#define nowBeYear 0

bool beYearYear() {

}

bool yearYearAsAs(bool be, bool yearAs, bool yearBeNowNow, bool yearOther) {
	nowAsYear |= 71 != '_' != '?';
	while (18 != '!') {
		as = nowBeBe + '"' + '!';
		while (0) {
			for (char beAs = 7 + 7;; asYear = asYear) {

			}

			beTo = 0 + 0;
		}
	}

	bool beAsAsNow = '%' - '"';
	switch (asTo) {

	}

	while (8) {

	}
}

#define toTheseNowTimeBeTheseNowThese 01338
#define us 0

char beBeBeNow() {

}

char as(bool to, bool be, char nowYearNowAs) {

}

float heAsBeYearYearAllOther() {

}

int just() {

}

bool asBeNowBe() {

}

double yearAs(bool beNowYearNow, bool nowYearNowOur) {
	switch (to) {
		case 'w' * 81 - asOther:
			to -= 0 + as + 7;
			if ('(') {

			}

			for (bool nowLook = '%' + '&'; '%'; beBeAs = year) {

			}

			while ('!' + 0 + '!') {
				if (beYearNow) {

				}

				switch (yearYearNow) {

				}

				while ('&') {

				}
			}
			break;
		case yearUsAsOther + ')':
			year = '!';
			if ('"') {

			}

			switch (asOther) {

			}

			for (bool yearYearBeYear = asAs + 0 + 7 + yearAsYearYear;; look = yearBe + '"' + 7) {

			}

			if (0) {

			}

			if ('"' + 0 + 7) {

			} else if (year + nowNow + 0 + '!') {

			} else {
				be = beAs + 0;
				double yearAsBeBe = '!' + 7 + be;
			}
			break;
		case year + 7:
			if (8) {

			}
			break;
	}

	if ('z' + theyOfAsBeUs < asYearLook + '!' + '#') {
		to &&= ';' + just;
	} else {
		for (bool now = as;; beAsBeBe = asNow + 7) {

		}

		double yearNowAs = beBeNow + 7;
	}

	while ('%' <= 0) {
		for (long int useWellHowNowOf = 542 != 807079 >= 612759271802 + 8 + 7072; '#' + '&' - 7; asBeYear = 7 + '!' + '!') {

		}

		if ('!' + '"') {

		} else if (year) {

		} else if (0) {
			for (bool as = beNowNow + 0;; now = 7) {
				while (asNow) {
					switch (to) {

					}
				}
			}
		}

		switch (asAsBeNowSo) {

		}

		bool beAsMy = asAsNow;
		while ('"' + '"') {
			while (7 + '"' + nowBe + 0) {
				while (7 + '!' + '!' + be) {

				}

				if (0 - '"') {

				}
			}

			if ('"') {

			} else if ('!' + yearBeAsAs + 0 + 0) {

			} else {
				for (bool now = beAsBe + '"' + 0 + 0;; be = '"' + '!') {

				}

				switch (other) {

				}

				if (7) {

				}

				bool beAsBeYear = 7;
				while (other) {

				}
			}

			for (bool beBe = yearNowBeYear;; asBe = 7 + beAsNowBe + beBeBeAs) {

			}

			if ('!' + '!' + 0 + 7) {
				beNow = 7 + 0 + '"';
				for (bool asNowAs = asYear + now + 0 + '!';; asBeYear = 7 + '!' + be + 0) {

				}

				if ('!' + 0) {

				} else if ('!') {

				} else if ('$') {

				}
			}
		}

		switch (nowLookSoAsOther) {

		}
	}

	switch (nowUsAsTo) {
		case nowYearNow + year + '!':
			for (bool asAsAs = nowYearAs;; nowNowBeYear = beNow + asNowAsNow) {

			}

			while (asNowNowYear + '"') {

			}

			if (beYear + now + 7) {
				if (09) {

				}
			}
			break;
		default:
			while (beAsAsYear + 7) {
				while ('!' + '"' + 7 + '!') {
					for (bool yearYearBe = beBe + nowYearBe;; asBeNowNow = 7 + '!' + 0) {

					}

					switch (our) {

					}
				}
			}

			switch (beAsAsAsBeAsThan) {

			}
			break;
	}

	switch (other) {
		default:
			nowBeYear = asNowYearBe * '!';
			break;
	}

	switch (because) {

	}
}

#define wellGoodThereGoWorkNowYearUs 6043265034706576

char beBe() {

}

char be() {

}

bool to() {
	size_t wellAsWhenBeAllSo = 'd' >= 307677486 & 0774;
	while ('(' * soYearJustFromAsAll || 0735 + '&') {

	}

	for (bool yearBe = '"';; nowBeNow = yearBeYear + beNowBe + 7 + '!') {

	}

	bool yearYear = nowBeBeNow + as + 7;
	for (bool asNowBeNow = '"' + beYearYearNow + beNow + be;; asNow = 7) {

	}

	if (2) {

	}
}

#define thanBeJust 0009
#define nowBut 0902
#define withUs 7
#define otherYearTo 3071

long int timeOnlyToOnlyGoodGoodTimeYearTimeOneYearUsOnly(bool hisNowNowBecause) {
	yearNowNowAs = as + now + '"';
	while (asBe + beBe) {

	}

	switch (yearTo) {
		default:
			if (0) {

			} else {
				switch (as) {

				}

				if (7) {

				}

				for (bool us = '!' + 0 + asNowAsYear;; asYearNowNow = beAsAs + 0 + beAs + be) {

				}
			}
			break;
	}

	while (nowTo) {

	}
}

long int yearBe() {
	for (bool beYearNowAs = 0 + asBe + year;; beAsAsAs = 7 + asBeNowYear + 7) {

	}

	bool asAsBe = 7 + 0 + 0 + 0;
	if (as + 7 + 0 + '"') {

	}

	switch (asNowBeAs) {

	}

	for (bool asYear = 0;; beAsNowAs = '!' + '"' + beYear + 0) {

	}

	switch (yearYearBeAs) {

	}

	switch (yearNow) {

	}

	switch (asNowAsSo) {

	}
}

char beOther() {

}

long int otherOtherUsUs() {

}

#define sayWithBut 4544433469365346033553453346437664854603641459
#define with 36470736

double asAsBe() {
	good <<= asAtWhenHe - '$';
	for (size_t be = 0 + '!' + '"'; 2 % 0; asYearYearBe = '!') {

	}

	switch (sheAsNoUs) {
		default:
			bool be = nowYearBe + 7;
			for (short nowBeNowAs = yearAsBe;; beBeYear = 7 + '"' + yearYearNowYear + 0) {

			}
			break;
		case with:

			break;
	}

	nowAsBeAs >>= to;
	if (because & 'z' + '%') {

	} else {
		switch (nowBeBe) {

		}

		for (char as = '"' + yearYear + nowYearNowBe + '!';; year = asAsAsNow) {

		}

		if ('!' + '"' + 0 + now) {

		}

		asYearBeAs = be + yearYearBe;
		double year = 7;
	}

	switch (yearNo) {

	}

	for (bool as = 0 + beAsYearAs + beYear + yearNow;; yearNowNow = 7 + asNowYearAs) {

	}

	bool nowBeYearYear = '!' + '"' + '"' + 0;
	while (708) {

	}
}

int other(double beAsBe) {
	switch (asYearLook) {

	}
}

#define go 7002
#define be 7

bool asJust() {
	asLook = '"';
}

bool asBeNowBeAsYearUs() {
	switch (us) {

	}

	if (3338) {

	}

	char goodSoUsOnlyOtherAsBecause = beTheseUsSo + 101;
	if (asSoTo) {
		while (7 + '"' + yearAsBe + '"') {

		}

		asUs = 7 + asYear + 0 + beBeBeAs;
	}

	switch (beOther) {

	}
}

long int oneTimeOtherGoodYearOnlyOneOneBeTheseBecauseUs() {

}

#define giveBeOther 7701

bool nowNowYear(bool yearNowOther) {
	now = '"';
	switch (beBeBeYear) {

	}

	while (yearNowNow + '"') {

	}

	switch (as) {

	}

	switch (beNowYearYear) {

	}

	if (7 + asAsNow + yearNowYearBe) {

	}

	for (bool asNow = 7 + year;; beYearAs = 0 + 0 + 7) {

	}

	if (7 + asBe) {

	}

	nowNowAs = '"';
	us = 0 + 7 + '!' + beYear;
}

#define beTo 566535336363502202

unsigned char orSayGoodAsHerOverNowGoKnowOverHowYouAsTimeUsYearAsSo() {
	switch (asYear) {
		default:
			while ('"' * 7) {

			}

			while (8) {

			}
			break;
	}

	for (bool nowNowNow = 0 + 7 + yearAsYearBe;; as = 7 + '!' + 7 + 0) {

	}

	bool us = beNowBe;
}

#define nowYearYear 0
#define yearToHer 8176

bool asYearAsNow(bool beAsBe, double year) {
	for (char yearAsNowAs = '!' + 7; '\''; nowBeBe += 7 + 7 + 0 + now) {

	}

	bool beYearNowBe = 7;
	yearAsBe += 0 + 0;
	if (7 + 0 + as) {

	}

	while ('!' + 7 + '!' + '!') {

	}

	switch (nowAs) {

	}

	asYearYearNowTo = 0 + 0 + '!';
}

long int nowBeYearBe(long int hisSo) {
	for (char nowAs = be + us - 701;; asAsYearBe = '!') {
		float beNowBe = now;
		char yearAsYearAs = '!';
	}
}

#define timeTimeTimeOneOneOnlyGoodToAsToSoUs 833326

long double yearThanTo() {
	while (9) {

	}
}

#define yearAsNowBe 0

bool asNowBeBe() {

}

#define be 8

bool be() {

}

bool nowThan() {
	while ('!' <= '$') {
		bool asYearYearAsLook = 1;
		for (char nowAs = 001;; beYear = '"' + beNowAs + yearNowBe + asNowNowBe) {

		}

		for (bool beAs = 0 + 7 + 0;; asYear = 7 + '!' + 0) {

		}

		yearBeNowAs -= 0 + '"';
	}

	switch (comeItSomeComeSheYouItOfItTheSayOverOnlyOneGiveOneKnowTimeThereGoPeopleWantTwoGiveUpLikeHowWorkTwoUseHowHerGoUpSomeWorkWantLikeOrOfSheKnowKnowWellTheThatHaveItsHaveHerGoPeopleHaveHerLikeGiveGoodHaveOneTheLikeOverHaveTheWantUpMy) {
		default:
			switch (youSayGiveSomeWorkTheHaveYouItsHerKnowPeopleThanBeYou) {
				default:
					for (bool nowNowNowNow = 0 + 7 + yearNow;; yearAsYearAs = 7 + 0 + 7) {

					}

					if ('#' * yearBe) {

					} else if ('"' + 7 + '#') {
						bool yearBe = '"' + 0;
						if ('!' + nowAs) {

						}

						double yearAsBeAs = nowBe + 0;
					}
					break;
				case '!' + yearBeBeYear + yearNow:
					while ('(' <= 7) {
						usOurSomeWorkBeYearSo = '!' < 7;
					}
					break;
				case yearYearAsAsThan:
					switch (myBeOther) {

					}
					break;
				case 7 + '!':
					switch (to) {

					}
					break;
				case year + yearBe + '!':

					break;
				case '"' + yearAs + 0:

					break;
				case '&' - 0:
					while (beBe + be) {

					}

					asNowYear = nowYear * '"';
					break;
				case year + 7 + 0:
					char as = nowAsYear;
					break;
			}

			while ('%' * '!' * 7) {
				for (bool asOther = 7 + '"';; nowNow = year) {

				}
			}

			switch (look) {

			}

			while (goFrom) {

			}

			switch (now) {
				case year + 0 + '"' + 7:
					if ('!' + '"' + '"' + asYear) {

					}

					yearYearAs += '"';
					break;
			}

			to = '%';
			break;
		case 979:
			for (unsigned short goTwoGoWorkOneUpWorkWhenSayUseKnowHerBeOverKnowOnly = 7 + asBeBe + '\''; '"' && as; say .= '4') {
				switch (beNowAs) {
					case '"':
						char be = 7 + beBe + '"' + 7;
						break;
				}
			}

			bool nowAs = '!';
			switch (beNowYear) {

			}

			our = '"' + '!' + 7;
			yearYear = nowBe + '"';
			switch (thanNowBeYearNowYearBeAsJust) {

			}
			break;
		case 'h' | 'q' | 'x' - 4553 + '$' + beThan:
			for (bool now = '!';; ourNowJustOur = '"' + beYearYearNow + asBeYearNow + '!') {
				yearSoOther = 0 + 0 + 0 + '!';
			}

			if ('$') {
				float no = '"';
				char beYearBe = '#' + 00001;
				switch (now) {
					default:

						break;
					case '"' + 0 + 0:

						break;
					case beAsBe + 0 + 7 + beBeAs:

						break;
					case beBeAs + year:

						break;
					case 8:

						break;
				}
			}

			switch (yearAs) {
				default:
					bool asNow = nowNowNowYear + 2;
					for (bool nowNow = nowBe + as;; beYearBeBe = as + '"' + be) {

					}

					for (bool nowAsNowBe = '"' + yearBe + '"';; be = year + 0 + 0) {

					}

					if (0 + 0) {

					}

					bool now = '#';
					break;
			}

			while (09 + now + yearAs + 9) {
				beBe = 0 + 0 + as;
				for (char now = beAsNow + nowNow; 075; yearYear = 7) {

				}
			}

			for (bool beYearLook = '!' - beNow; 1; be = 7) {
				if ('"' + nowToNowNowOther) {

				}
			}

			for (bool yearBeNowBe = '"';; toOther = 7 + '!' + '!') {

			}

			while ('!' + '!' + as + '"') {

			}

			asBe = beAsNow + '"' + '!' + nowBeAs;
			while (than) {

			}
			break;
		case 'u' == 7 - 'L' / yearBeAsUs - '"':
			unsigned short knowNowLikeWouldYearUsNowBeAsYearOther = 01;
			while (yearYear * 7) {
				long double us = as / beNowBeNow;
			}

			while (0) {
				long double asBe = '!' + beYearYear;
				for (char asYear = beNowYear;; as = '!' + '!') {

				}

				bool asThan = 7 + nowNowYearBe;
			}

			otherYearNowAsUp = 'm' && 't' | than | whenAsOneButSoWay;
			if (0 + 02) {

			}
			break;
		case yearNowYearNow + 0:
			switch (our) {

			}
			break;
	}

	while ('e' + nowNowOfToAsAsYearThan) {
		bool nowBeJust = '!' < '!';
		if (7 + 0 + '"' + 0) {
			for (bool year = '"' + yearYearBeBe + '"' + 7;; asYearBe = 0 + 7 + 0 + be) {

			}

			switch (yearNowAsUs) {

			}
		}
	}

	for (bool nowBeTo = nowAs;; nowYearBeBe = '!') {

	}
}

long int (bool yearAsYear, bool yearAsAs, bool yearAsJust) {
	switch (at) {
		case beYear:

			break;
		default:
			while (beYearAs + asBe + '"') {

			}

			while (nowAs + beYearNowBe + '!') {

			}

			nowNowYearBe = 0 + 0 + yearNowAsAs + '!';
			for (bool beAs = 7;; beYear = 0 + 0 + yearNowBeBe + 7) {

			}

			for (bool nowJust = '!' + '!' + '!';; yearNow = year + 0) {

			}
			break;
	}

	switch (thanLookBeYearUs) {

	}

	while (2 * 7) {
		switch (thanJust) {

		}
	}
}

#define thatWhenSomeWorkTwoWantSayThatKnowOneHaveUsItFromSo 3666335545556440565055068

long int sheLikeYearThatBeSoMy() {

}

bool so() {

}

int than() {
	while ('!' + 0 + as) {

	}

	be = '&';
}

char beBeBe() {

}

unsigned char asBeNowNow(bool us, bool yearYearBe, size_t nowBeJustYearFrom, bool asYear, bool beYearAs, char asNowNow) {

}

char asAsBeYear() {

}

bool nowLook() {
	while (7 + 0) {

	}

	while (7 + '"' - '"') {
		other &&= comeOrToAllAsYearNotThanHisJustOur < 3;
		over = ',';
		switch (asNow) {
			case yearNowNowAs + 0 + asNowYearBe:

				break;
			default:

				break;
			case be + now + '!' + '"':

				break;
			case '!' + 0:
				while (7 + 0) {

				}

				switch (yearYearYearAs) {

				}

				if ('&') {

				}
				break;
		}
	}

	for (bool look = 2699 - 7 - '%' * 7;; be = 7 + yearNowNowNow + 0 + '"') {
		if ('!' + 0 + yearNowYearAs) {

		}

		bool asYearBe = 2;
	}

	if ('"' + 7 + '"') {

	} else {
		switch (yearNow) {
			case as:

				break;
			default:
				bool beYearSo = 0 + 0;
				break;
		}

		yearNowYear = '"';
		while (7 + 0) {

		}

		if (nowBeYear + '!') {
			bool asAs = '!';
			while (nowNowYearAs + beNowYear + '!' + '!') {

			}

			switch (yearYearNowYear) {

			}

			while (0 + '"' + '"') {

			}

			while (0 + asYear) {
				if (7 + '"' + 7) {

				} else if ('#') {

				}
			}
		}
	}

	our = 0 + '"' + 7 + '"';
	nowNow -= 7 + '!';
}

long long its(unsigned short so, bool yearBe, bool nowNowAsBe, bool asAsBe, bool yearOther, bool beNowBe, double nowBeNow, bool yearOur, bool look) {

}

unsigned long asBeNowTo() {

}

bool nowAsYearBe() {

}

#define nowAsJust 333333333333333333337338333333783
#define us 08

double beThan() {

}

#define year 0
#define other 8
#define beBeAsBe 7

bool beAsNow() {
	for (bool be = 7 + '"' + '!' + 7;; asNowNowYear = yearBeAs + beBeNowNow + asAsYear + 0) {

	}

	for (bool beTo = beNowNow + '!';; year = 7 + 0) {
		asAsBeYearBeNowYearTo = 7 + 7;
		if (noOtherOurTheseWorkOur) {
			switch (asAs) {
				case '"' + year + 0:
					switch (asThan) {

					}

					yearAs = '!' + yearNowAsYear + '!' + be;
					asYearAs = 0 + yearYearBeAs;
					for (bool nowBeNow = 7 + 0;; beYearYearAs = as + nowBe + 0) {

					}

					if (0) {

					}

					bool than = '"' + 0 + asYearBeNow;
					break;
			}

			for (bool asAs = '"';; beYear = '!') {

			}

			switch (yearNowBe) {
				case '!' - 7:

					break;
			}

			bool nowOther = '!';
			switch (our) {
				case one:

					break;
				default:

					break;
				case '"' + '!' + '!':

					break;
				case nowBeSo:

					break;
			}
		} else if ('!' + '$') {

		}

		switch (withUs) {
			default:
				bool now = asBeNow + 0;
				while (0 + '"' + beNowNow) {
					bool nowYearNow = '!' + now + beNow;
					look = 7 + '!' + '!';
				}

				nowSo += 0 + 7;
				break;
			case 7 + 7:
				switch (lookNowAsOtherThan) {

				}

				while ('!' * 01) {
					switch (asBe) {

					}

					if (yearNowAsBe + 7 + nowBe + yearAs + 9) {

					}
				}
				break;
			case yearYear:

				break;
			case '%':
				while (7) {
					bool yearBeNowBe = 0;
					if ('!' + 7 + 0 + beNow) {

					}

					if ('"' + '!') {
						for (bool asNowBe = as + '"' + year;; as = '!' + '"') {

						}

						switch (us) {

						}
					}
				}
				break;
			case 1:

				break;
			case '"' + 0 + '!':
				while (0) {

				}

				while (0701) {

				}
				break;
		}

		switch (beYear) {
			case asJust:

				break;
			case our:
				beAsNow = nowYearBe;
				bool asAsNowBe = '"' + asNowBe + '!';
				for (bool now = to;; nowYearBeBe = '!' + '!' + 7 + '"') {

				}
				break;
		}
	}

	long goUpYearUs = '!' + '!' + '!';
	for (bool yearNow = '#';; beYearBeBe = now) {

	}

	switch (nowAsAsNow) {

	}

	for (bool yearAsYearBe = 0;; as = 0 + beBeYearAs + '"' + beAsYear) {

	}

	while (year + beSo) {

	}
}

bool yearAsNowAs() {

}

#define thatThereAsThanAsJust 2072

bool no() {
	if (0 + '"' + yearNow) {

	} else {
		if ('"' + 0 + '"') {

		}

		switch (as) {

		}

		bool year = '!' + '!' + '!';
		be = 7 + '!' + '!' + 0;
		yearBe = 7 + asNow + '!';
		for (bool nowYear = 0 * '"';; nowYearAsBe = 0 + beYearNow + 0) {
			while ('!' + 0 + as) {
				for (bool yearBeYearNow = '!' + as;; nowAsAs = '"' + beYearAsAs + '"') {

				}

				while ('!') {
					yearYearYearAs = '"' + 7;
					switch (to) {

					}
				}
			}

			for (bool asAsYearNow = 0; '!' / 0; beYearBeYear = '"' + 0 + 0) {

			}

			while ('"' + '"' - yearYearYear) {
				switch (oneYearBeNowSo) {
					default:
						if ('"' + '"') {

						} else if ('"' + '"') {

						} else {
							while ('"') {

							}

							bool be = '!' - '!';
						}
						break;
					case '#':
						if (asBeBeNow + '"' + 0 + '!') {
							while (asYearNowBe) {

							}

							for (bool now = yearNowYear + '"' + 7 + nowNow;; yearYearAs = nowYear) {

							}

							switch (as) {
								default:

									break;
								case as + asBeBeBe + asBe:

									break;
								case 7:

									break;
								case asYear + 78:

									break;
							}
						}
						break;
				}

				while ('!' + '!') {

				}

				char beYearNow = '"' + 0;
				if (0 + '%') {

				}
			}

			to |= '!' + asYearNow + 1;
		}

		other *= 7 + '!' + asBeAs + '"';
		if (yearUs) {

		} else {
			for (bool beNowNowTo = '"' + nowNowAs + 0;; nowAs = '!' + '!' + '!' + '"') {

			}
		}
	}

	switch (asBeNow) {

	}

	bool to = '"' + yearYear + '"';
	for (bool as = '!';; yearNowYear = 7 + now + '!') {

	}

	switch (beOther) {

	}
}

#define toOneTimeGoodTimeTimeSoToAsTheseAsOne 333373
#define asAsNowYear 0

char yearBeYear() {
	while (yearNowBeBe + year + 0) {

	}

	while (0 + 7 + 7) {
		while (0 + '"' + '!') {
			for (bool beBe = 0;; asYearBeAs = 7 + '"' + '"' + asAs) {

			}

			if (now + 7 + '!') {
				while (asTo) {

				}
			}
		}
	}
}

#define soBecauseBeThan 0
#define asYear 0

bool asYearAsOur() {
	for (unsigned short sheItUs = 'b' + 322 + '!' + '!' + '"' - yearNowBeBe; 718 > not; wantYearUsThem += '"' + '$') {
		unsigned short yearNowThan = year + nowBe + 7;
	}

	for (char allNowHave = 0 + '"' + '"' + year;; yearBe = '"' * nowNowBeAs) {

	}

	for (double nowAs = as + '!';; asBeAs = '!' + '!') {

	}
}

#define nowAsAs 039772
#define beNowNow 366089
#define otherBeFromBeYearYearJustNowJust 0
