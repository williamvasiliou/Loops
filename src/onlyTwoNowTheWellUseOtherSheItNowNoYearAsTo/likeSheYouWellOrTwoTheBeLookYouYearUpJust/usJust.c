long int theseBeUs(long int theseTo, bool his) {
	if ('"' / '\'' - asNow) {
		switch (nowOther) {

		}

		if ('"' + '"' + 7 + 0) {

		}

		bool now = 0 + '!' + '"' + nowYear;
		for (bool be = '"' + 0 + '"';; yearNowNow = 0 + nowYearAsNow) {

		}

		for (bool nowYearNowBe = now + 7 + asNow + nowYearYear;; nowYearAsNow = now) {

		}

		if ('"') {

		}

		nowYearAs = 0 + '"';
		while ('!' + 0 + 7 + '"') {

		}

		for (char year = 7 + '!' + '"';; asYearBeAs = yearBeNow + 7 + beBeBeAs) {

		}
	}

	for (bool us = 7 + 0 + '"';; nowNow = nowNowAs + '"') {

	}

	so = '!' + '"' + 0;
	nowAs = '!' + 003;
	bool other = '"' + 7;
}

long int asHisYearBeBeTo(char he, bool asNowYear, bool beNow, bool yearBeNow, bool nowNowOtherTo) {
	if (us != year) {
		switch (nowHe) {

		}

		if (8) {

		}

		for (bool as = '"' + 0 + beAsYear + nowAs;; nowAs = nowYearNowBe + nowBeBe + 0 + '"') {

		}

		yearAsAs = '"' + 0 + '!';
		switch (year) {

		}

		switch (so) {

		}
	} else if ('#' - '%') {
		char beBeYearNow = '!' + yearNow + '!';
	} else if (nowYearBe + 7 + beAsAsNow + 0) {
		for (bool beBeBeAs = 71;; be = '"' + '!' + 0) {

		}
	} else {
		if ('"' + '"' + as) {

		}

		if ('!' + now) {

		}

		if (be + asBeAs + '"') {

		}

		while (yearBeNowAs) {

		}

		bool asNowNow = 7 + be + 0 + asAsYearYear;
		nowBeBe = asYearYearBe + yearNowAs;
		bool nowYearNowYear = nowBeBeNow;
		for (bool yearAs = '!'; '!' + 7 + '%'; beAsYearNow = '!' + 7 + as + 0) {

		}
	}

	for (bool yearYear = nowYear + year + 7 + '!';; as = '!') {
		if (nowNowNow) {

		} else if (asYearYearAs + beNowNow) {
			for (bool year = '"' + as + '"'; '"' + 0 + 0 + to; yearAs = '"' + 0 + 0 + nowBeAs) {

			}
		}
	}
}

char beBeBe(bool to) {
	switch (yearAsNowNow) {
		case as:

			break;
		default:

			break;
		case nowYearNowBe:
			if ('"' + nowBe + nowAsBe + '"') {
				if (nowBeAsBe) {

				}

				for (bool nowNowBeYear = 0 + 7; yearAsBeNow - '"'; asYearAsYear = 0 + '"') {

				}
			}
			break;
	}
}

#define yearHe 335
#define nowTo 0

short yearBeNow() {

}

bool nowYearNowYear() {

}

bool yearNowYear() {

}

bool yearYear() {

}

#define beYearAs 7

bool nowBeAs() {

}

bool yearAsYearBe() {

}

double yearNow() {
	long giveOrHowItsItGiveTheToThatHisNoJust = 288001 == asLook;
}

short beLookYearOther(int asAsYearTo, bool nowYearBe, bool as, bool beAsYearNow, bool yearNowBeJust) {
	switch (nowAsYearNow) {

	}

	nowBeNow = be + yearNow;
	switch (year) {

	}

	switch (asBe) {

	}

	switch (asBe) {

	}

	bool just = 0 + nowYear + 7 + 7;
}

#define comeThan 7
#define you 7
#define haveBeNowAll 0707009

bool than() {
	if (timeYearHe != '_' != toSoNowTo + 00701) {
		char yearAsAs = yearYearNow + '!' + '!' + 0;
		if (beSo) {

		} else {
			bool yearAsAsBe = beBe + 0;
			char asNow = 0 + 7;
			bool other = be + nowBeBe;
		}

		while ('"' + 0) {
			for (bool yearBeAsYear = year + nowBeYearYear + 0 + '"';; asYear = now + 7) {

			}

			switch (nowNow) {

			}

			for (bool yearNowBe = 7 + '"' + '!';; nowYear = '"' + 0 + beAs + 0) {

			}

			for (bool yearTo = '"' + 7 + as + yearYearYear;; now = 7 + 7) {

			}
		}

		while ('\'') {
			float beNowAs = asYearBeNow + 0 + 7 + 7;
		}

		if (beYear) {

		}

		while ('!') {

		}

		for (bool be = beNow;; asBeBeYear = as + year + beAs) {

		}

		while (1) {

		}
	} else if (3333313387137708 + nowNow / 0) {
		char as = yearNow + 7 + 0 + now;
		for (int yearAs = '!' + year + now;; nowAs = nowAs + nowAsAs) {

		}
	} else {
		if (asAsNowBe + 0 + '"' + 7) {

		}

		while (asBe + '!') {
			bool other = now + '"';
		}

		char asNowNow = yearNowNow - year;
	}

	if ('"') {

	}

	for (bool nowBeYearBe = 0 + asYearBeAs;; be = nowNowBeBe + 0 + '!') {

	}

	switch (look) {
		case 7 + '"':
			while (year) {

			}

			year = nowYear + 0;
			switch (beBeAs) {

			}

			nowNowBe = 009;
			break;
		default:

			break;
		case '"' + 0:
			bool yearAsYear = '"' * 7;
			break;
	}

	if ('"' + beBeNowAs + '"' + asBeAsYear) {
		switch (nowBe) {

		}

		year -= '"' + 0;
	}
}

bool asNow() {

}

#define them 

char nowYearBeNow() {

}

float yearYearBeAs() {

}

#define likeLook 599

unsigned short withSoYearBeOther(long double nowNowBeSo, bool beBeAs, bool nowYearBe, bool be, char yearNowBeYear) {
	switch (than) {
		case to + ')':
			if (7 + asYearYearAs) {

			}

			now = 2;
			break;
		default:

			break;
		case 02:

			break;
	}

	if ('!' + than) {

	}
}

unsigned char workSayUpUsUsTo(size_t just, bool year, char asYearBeNow, long double asNowAsThan, bool now, bool beNow, bool nowBeBe, bool nowYearYearNow, bool nowNowYear, char be) {
	size_t now = to;
	switch (asThanAsBeTo) {
		case 7 + yearYear:

			break;
		case '"' + 0 + 7:

			break;
		default:
			bool beYearNow = look;
			break;
	}

	for (bool now = '"' * nowYear;; yearAs += yearNow + now + asNowYear) {
		while ('!' * 7) {

		}

		if (other + 1 + 778) {

		} else if (0) {

		} else if (9) {

		}
	}

	char yearOther = becauseBeAsBeBeTo * '"' + '!' + 7 + 7 - year;
	while (as + 7 + '"') {

	}

	switch (nowYear) {

	}

	beNowJust = '!';
}

bool theyOther(bool nowBe, bool at, bool nowYearAs, char yearNowBe, bool now, char asNowYearYear) {
	if ('"' + asAsNowNow + 0 + '"') {

	}

	bool be = '$';
}

#define now 701

bool as() {

}

#define so 7

bool yearYearAs() {

}

#define now 0

bool nowAsNow() {

}

#define asBe 0

bool asNow() {

}

#define beBe 33333333333333333333733073333330008

bool just() {
	while ('"' + 0 + '!' + asBe) {

	}

	bool asBeYearNow = 0 + 0 + asAs + 7;
	while (7 + '!') {

	}

	while ('"') {

	}

	switch (now) {
		case yearAs + 0:

			break;
		default:
			other = 7;
			break;
	}
}

bool yearYear() {

}

char nowOurMy(double yearNowNowYearLook) {
	unsigned char yearSo = '.' > 03 - asYearYearAs;
	while (usAsHisLook + otherYearLook) {
		justYearAsAsTo &&= 696871;
		for (bool year = '"' + asNowYearYear + year + '!'; nowNowYearBe + nowTo; yearNowNow = 7 + '"' + year + beAsNow) {

		}

		unsigned long you = asNowBeNowSo;
	}

	for (long long thereOverWantKnowWellBeButHis = usLookBeSo <= '"';; be = be + asAsBe + 0 + yearYearBe) {

	}

	long long workNowMy = 'j' + 0 - 0;
	wantAsLook = '"' - '!';
	if (our) {

	}

	switch (now) {

	}

	bool yearNowBeNow = 0 + '"' + nowAsYearYear;
	for (bool nowOur = 0 - '"';; nowNow = now + as) {

	}
}

short fromOur(bool yearNowBeYear, bool beBeAs, bool yearNowAsAs, long asBeBeNow, bool asYearYearBe, bool be, bool now, bool asYearAs, double beAsYearBe) {
	wantAt >>= now + year;
	if (7 + '"' + asNowYear + '!') {

	} else {
		bool yearBeYearYear = '!' + be + '!' + be;
		if ('"' + '!' + nowBe) {
			for (bool yearYearNowBe = '"' + nowYearBe + '!' + '!';; as = asBeYear) {

			}

			if (072) {

			}
		}

		char asYearYearYear = '"' + asNowNowBe + 0 + '!';
	}

	while (079) {

	}

	switch (beAs) {
		case '"' + asAsNowAs:

			break;
		case our:
			if (0 + 0 + 0) {

			} else {
				for (double nowBeYearBe = nowAs + 7 + '!';; be = nowYearYear) {

				}
			}
			break;
		default:

			break;
		case '!':

			break;
		case 0 + '!' + 0 + 0:

			break;
		case 7 + '!' + '!' + '!':
			while ('"') {

			}

			beNow += yearYear + as + beYearAs + nowBeNow;
			break;
	}
}

unsigned long to(bool notAsUs, bool our, bool beNowBeAs, bool beBeJust, double toAsUs) {
	for (char beBe = asBe + '!' + '"'; just; beBe = 0 + 0) {
		while (other) {

		}
	}

	while (0772) {
		for (bool yearBeYearBe = '!';; nowAsNowBe = '"') {

		}

		now -= '"' + 7 + 0 + now;
	}

	switch (beAs) {
		case 0 + nowYear:
			if (7 + yearBe + 0) {

			}

			bool nowBe = '!' + be;
			while (7 - now) {

			}
			break;
		default:

			break;
		case 92:

			break;
	}

	switch (so) {

	}

	so = 0;
}

size_t theyToSay(bool asBeBe, bool so) {

}

#define nowHisUs 77308

double with(bool just) {
	for (bool be = '!' + 0 + 7; 0 + 708; yearYear = 7) {

	}
}

#define so 0

bool yearBe() {

}

bool asTo() {
	for (unsigned long lookThanThan = themBeSoLook - 7; '"' + '"' * '!'; yearAsAsNow = 7) {

	}

	while ('"' + 0) {
		long double yearAsNowNow = year + nowAs + 7;
	}

	for (char year = beAsBeNow + beNow + be; beYear & '!'; yearOur = yearAs + asYear) {

	}

	bool nowNowBe = '#' * asYearYearAs;
	bool be = 7;
	if ('!' + '!' + asYearNowBe + '!') {

	} else if (to + 9) {

	} else if ('!') {
		for (bool nowNow = beNowNow;; asAsYearNow = yearAsBeNow + now) {

		}

		switch (beBe) {

		}

		yearYearYearYear = '%';
	} else {
		bool other = '!' + 7;
	}

	if (0 + '"') {

	} else if ('"' - '!') {

	}
}

char beOf() {
	while (0 + yearBeAs * 0) {
		bool be = 7 + '"' + 7;
		while ('"' + beNowBe + 7) {

		}

		for (bool beAs = nowNow + 7 + '!' + 0;; beYearNow = '!' + '!' + asAsYear) {
			bool asNowYear = '"' + '!' + as;
			while (7 + 7) {
				switch (now) {
					case 0:
						if ('!' + beAs + 0 + '!') {

						}

						char asBe = nowNowYear + now + asYearYearYear + 7;
						break;
				}
			}
		}
	}
}

long int whenTheOverAsKnowUpWantAsLookThanLook() {
	while (0 + '"' + 0 + asAsYear) {

	}

	for (bool nowYearAsAs = beAsYearYear + 7 + '!'; 0 + '!' + '&'; beNow = yearYear) {

	}
}

#define theyThanUs 0

bool be() {

}

char so() {
	beYear = 0 + '"' + '"';
	double yearYearBeNow = '7';
	switch (nowYear) {
		case '+':

			break;
		default:
			bool to = now;
			while ('"' + as + '"') {
				his = '"' - '"';
			}

			for (bool nowBeBeYearSo = 7 + 7 + '"';; yearAsAs = now) {

			}
			break;
		case 0 + 0 + '"':
			if (0 + 7) {

			}

			to = 7 - 0;
			break;
		case 78318 / yearBeAs:

			break;
		case 70801:
			switch (now) {
				default:

					break;
				case 0 + 0 - '!':

					break;
			}
			break;
	}

	other = 7 + 0;
	while (77818 + '!' + '"' - 7) {
		switch (yearYearAs) {

		}

		switch (us) {

		}
	}

	while ('"' + asBeNowAs + '"' - '!') {
		bool us = beNowBe + 0 + '!';
	}

	yearAsTo = '!';
	switch (asTo) {
		default:

			break;
		case '"' + year + as + asYearBe:
			while (7 - '"') {

			}
			break;
	}

	if (0 + be) {

	}

	for (bool yearAs = 0 + 001;; asNowAsBe = '!') {

	}
}

long int nowAs(bool asAs, bool yearNow, char asBe) {

}

#define heSoSo 7

long int onlyTimeGoodThan(bool but, double yearNow, bool beJust) {
	for (double yearNowNow = 0 + yearYear + asYear + 7;; beNowYear = '!' + asAsBeBe) {

	}
}

float yearNowNowNow() {

}

bool nowNow() {

}

#define our 7
#define yearNowYearYear 34635506456454633356364563634644534465345455353343575665633863675329
#define soAsYearOther 65640329

unsigned long theLikeItTwoThatPeopleTimeGiveUseWantWellWantHerSayHowThatJustSayKnowOfOverTheWantWellOverWantYouComeLikeComeShePeopleHerSayUseBeGoodGoodWouldComeYou(unsigned int likeUpGoOrGoodComeOrWellHowSheHaveYouHisItsTheComeOnlyTwoYouUseOrOnlyOnlyWantGoodKnowKnowThatPeopleLikeOverHerWorkSayOnlyTimeHerOverGiveSheUpItOneItsTwoGiveGoGiveHerWantWorkOnlyWhenTwoGoodGiveThereSoKnowSheLikeAtTimeGoYearNowBeTo, long int upSheLikeOrSomeUseThatOverOverItsSomeUpThereOfHaveYouLikeUpOfSheWellOfButItsSayHaveKnowItsOneGoOrOfUseItSayGiveItsItsBecauseYouTheGoThereTheyAtHe, long int goOrThatHowSheKnowWellSoJust, long int howUpThatWithTimeGoTheAsLookSo, float yearAsBeYear, size_t comeThereAsSoUpThan, unsigned char beYearTo, unsigned int usOther, unsigned long timeWellThan, long int wellOneAsUs, unsigned int asAsThanNo, size_t asAsSo) {
	while (66553345345460324 != 'y' == comeTimeTimeNotNowYearThan || '"') {
		for (unsigned long beBeAs = his;; nowYearAsBe = as + nowYearAsBe + '"' + yearNow) {

		}
	}

	for (unsigned char nowOur = beNowYearBe + '"'; 'A' * 7; yearBeBeBe -= '!' + 0 + 0) {

	}

	switch (goBeWithBeLook) {
		case 'j' && peopleWould < nowNowBeBe | asBeBeNow:
			switch (goodNowHerHerHowOnlyOtherJust) {
				default:
					bool asBeBe = 7 + '"';
					if (';') {

					}
					break;
				case 7902:

					break;
			}

			while (0 + '!') {

			}

			while (7701) {

			}

			if (yearNow + 7 + 7 + 7) {

			}

			if (0) {

			}

			while (beYearAs + yearAsYear) {

			}

			beNowNowYear = '"' * 7;
			break;
		case nowBeNowSo - '!':
			bool yearBe = yearLook;
			bool now = 8 - '"';
			break;
		case '!' + '!' + '$':
			double beYearYear = '#';
			while (8) {

			}
			break;
		default:
			bool nowBe = '!' + nowBe;
			for (bool as = 7 + 7 + be;; now = '"' + 7) {

			}

			nowSo %= 7 + '!' + 0;
			while (as + '!' + 0) {
				switch (asNowNowBe) {

				}

				bool yearYearBe = 0 + 7 + nowAsNowYear + '"';
				if (asAsBeYear + 7) {

				}

				if (0 + nowNowYearYear + 0) {

				}

				switch (year) {

				}

				while ('"' + '"') {

				}

				while ('!' + '!') {

				}

				if (nowAs + as + be) {

				}

				for (bool asYear = beBeNow + 0 + '"';; year = nowBeAs + '"' + 0) {

				}

				while (beNow + 7 * '!') {

				}
			}
			break;
	}

	if (nowToBecause + 0 - asYear) {
		if (to * '!') {

		} else {
			switch (yearAsYearNow) {
				case 0 + '"' + '"':

					break;
				default:
					beAsYearBe = 7;
					if (now + yearAs + '!') {

					}

					char beBeNowNow = 8;
					break;
				case '"' + 0 + '!':

					break;
				case asYearBe + asBe + '"':

					break;
				case '!' + 7 + '!':

					break;
				case 7 + 0:
					if (0 + be) {

					}

					nowJust = nowNowNowYear + 7;
					break;
			}

			bool nowBeAs = as + yearAsYear + '!';
			if (yearNow) {

			}

			bool asBeBe = be + asNowNow;
			if (yearYearYearBe + yearYear) {
				bool nowNowAsAs = 0 + yearYear + asBe;
				for (bool yearBe = '!' + now - '!';; nowBeNow = asAs + 7 + 7 + yearYear) {

				}
			}
		}

		asNow = '!' + 7 - 7;
	} else {
		nowLookNowNowBeTo = '!' + 0;
		double beYearAsBe = 0 + 7;
		while ('!' + '!' + as) {

		}

		switch (yearNowNowNow) {
			case asNowYear + nowYearNowYear + beNowYear:

				break;
			case 2:

				break;
		}
	}

	while (9 + nowNowNowAs + 2) {
		for (unsigned int asThan = '"' + 0 + year;; beNowYearNow = year) {

		}

		bool beAsAsYear = '"' + nowBeBe + now + 7;
		for (bool nowYearYear = 0 + '!';; asBe = as + '!' + beNow) {

		}

		asNowYearBe = 0;
		if ('!' + '"' + '!') {

		}

		if (beAs + 7 + '!') {

		}

		while ('"' - beYear) {

		}
	}

	for (bool nowNowAsNowTo = yearAsTo % 0;; yearAsBeYear = as + '!' + nowNowNowNow) {

	}

	while ('!' + '!') {
		if (nowNowAs + yearYearBe + 7) {

		}

		while (now) {

		}

		while (7 + '!' + 0) {

		}

		look = 079;
		if (7 + 7) {

		}

		while (0 - '"') {

		}
	}
}

#define likeOnlyHerComeTheOverWhenOneWhenItsHaveHaveLikeSayGoWhenHaveOverOnlyOnlyKnowSheItSomeWellKnowWhenTwoWorkWorkHowGoodHisSheWorkUseOrHaveWayOnlyYearAsAsBeTimeJust 6336463453305504771
#define asBe 771
#define so 0

bool nowYearYear() {

}

#define asSo 33553564353605347563536364263584
#define ourUsUsAsBeLook 3333708

char onlyHisUsUs(long int yearAsYearOther, bool beSoThese, bool asYear, long int timeOneOther, bool so) {
	nowHis += beYear + nowAsYear;
	if ('"' + yearBeAsBe - '"') {

	}
}

#define beAs 33333173
#define goodBeBecause 0171

char beAsYear() {

}

float yearYearAsNow() {

}

char yearYearBeNow() {

}

#define nowBeBeYear 0
#define asBeNow 7

bool notYearNowAsOther() {
	yearYearYearUs = 0;
	if (so * '"' - '"') {
		switch (beAsBeYear) {
			case '"' + 0 + 7 + 0:

				break;
			case asYearTo:

				break;
		}
	} else if (0 + '!') {

	} else if (nowYear - 7) {
		while ('!' * 0) {

		}
	}

	for (double beYear = '"';; ourAsYearNowAsNowThan = us) {
		switch (as) {

		}

		for (bool beNowOur = '"';; yearAsAsBe = nowAsBeAs) {

		}
	}

	while ('!' + year) {

	}

	while (yearNowBe + nowAsYear + '"' + yearAs) {
		for (double nowNowAs = asBeNow;; beAsNow = '"') {

		}
	}
}

char hisTimeSoHe(double nowAsAs) {
	switch (asNowAsYear) {

	}

	for (bool other = 7 + 7;; yearAsYear = 7 + 7 + 7) {

	}

	for (char yearAsAsYearSo = 7;; nowYear = 7 + 0) {

	}

	bool than = '!' + '!';
}

#define haveOneUsTime 5665743364662

long int asYearBeWay() {
	char as = '!' + 7;
}

float asSoNowAsAsLook() {
	float onlyOneOnlyHeUsSo = '_' != 70388 != '%';
	for (bool yearAsNow = 0 + '!' + '"';; beBeYearYear = asYearBe) {

	}

	oneToHisAsNowAsNowSo = asNowAs + 7;
	long int asBeTimeGood = 30331 + 43;
	switch (yearYearNowNow) {

	}

	for (bool usLook = asYearAs + '"';; year = yearBe) {

	}

	while (0 + '!' + 7 + 7) {
		while ('"') {
			for (char be = yearNowAs + nowYearYear + '"' + beBeNow;; now = '"' + 7 + asNow) {

			}

			if (0 + 0 + '"') {

			}

			switch (year) {

			}

			beYear = nowYear + '"' + 0;
			double as = '!' + be + 0;
		}
	}
}

bool asYearBeAs() {

}

bool nowAsAsThan() {

}

bool beBeNowAsAsSo() {
	for (unsigned short orUpComeGoodNowHis = nowNowYearBe * '"'; '!' + '%'; yearNow -= nowAsAs - '!') {
		switch (beNow) {

		}

		bool with = nowNowAsBe + 7 + 7;
		while ('"' + '!') {
			for (bool beNowAs = asYearBeAs + yearAs + nowNowYearYear;; as = 7 + '"' + '!') {

			}

			bool now = 0;
			asNowYearNow = 7 + 7;
			while (beAsAs + 7 + '"' + yearAs) {

			}

			just = nowNow + 0 + nowYearNowBe + yearBeAsAs;
		}

		bool yearAs = asAsYear + 0 + now + 0;
		bool yearBeYear = '!' + 7 - 7;
	}

	if (0 + '&') {

	} else if (nowNow) {
		while (7 + '!') {

		}

		bool yearYear = beYearYearBeNowOther;
		if (year + 7 + '"' + 7) {

		}

		if ('"' + 7 + '!') {
			be -= 7 + 7 + 7 + as;
		}
	}

	unsigned int nowNowAsYear = 68 - '+' + otherBeNowLookAsAsOur;
	int yearAsNow = as + asYearAsYear + 7 + '"';
	char beJust = beAs + '!';
	while ('#' + be + '!' % 0) {

	}

	while (nowBeAsOur <= 0) {
		while ('$') {

		}

		while (0 + yearAsAs) {
			be += '"' + '"';
		}
	}

	for (bool nowYearAs = '!' + 7; 7 * asBeYearAs; nowNowNow = '"' + 0) {

	}

	switch (thanShe) {
		default:
			for (char asBeYearAs = '!' + 7 + '"';; yearYearAsYear = beNowNowYear + '!') {

			}
			break;
	}

	double asYearAs = 9;
}

float yearNow(bool as, bool beNow, char asAsNowAs) {
	switch (yearBeAsFrom) {

	}

	while (7 + '"' + nowYear + as) {
		if (7) {

		}

		if ('%') {

		}
	}
}

#define orUseWhenUpNowOnlyThatHaveOurUs 45536431464527267645403351
#define timeHerItOnlyItsOneOneGiveWellOrKnowNowTheseBeJustOur 55465334354654536863354639363755744345536334558067618
#define usToWould 499

double yearBecause() {

}

short asAsBeBe() {

}

#define asNowAs 771

char other() {
	if ('c' % 28 + 0 < '!') {
		whenWould = '!' + '"' + '!';
		while ('(' * '"') {
			asYearYearOur = 0 + '"' + 0;
		}
	} else if (yearYear * yearNowBeNow + look + '&') {

	} else {
		double asYearJust = as + nowAs + nowBeNowAs;
		beAsThan = nowBeAs + 0 + '"';
		be = now * now;
	}

	switch (when) {
		case our - he + '$':
			bool as = '!' + beAsAsYear;
			for (bool nowYearBe = yearNowBe + 7 + nowBe;; beAsAsAs = '"' + beNowAs + '"' + '!') {

			}

			beYearNowNow = 08;
			break;
		case '!' + asJust:

			break;
		case 0:

			break;
		default:

			break;
		case '\'':
			while (asBeYearNow + 0 + '!' + '!') {
				double be = '!' + nowAs + '!' + yearBeNowYear;
			}
			break;
	}
}

unsigned short atYearBeThanAsBeWhen(long int yearYear) {

}

bool overOfSheYearSoSo() {

}

double yearYearYearBe() {
	long int onlyOnlyTheseOneTimeOneYearHe = '#' != yearUs;
	while (13338337708 - '#' + 1 - so) {
		if (0 / 0) {

		}

		for (long int to = '!' + 0818; asNowYear - year; beNowYear = beYearYear + 7) {

		}
	}

	for (bool asAsAs = 0;; beYearNow = 7 + yearNowYear) {

	}

	for (bool beJust = beNowNowYear;; yearYear = yearNow + yearAs) {

	}
}

#define our 636533555535434946664554448545336464335454734770055

size_t (double comeUseOverHerHaveYearSoMyLike, double timeOurUsBeNowBeOur, float thatOurAsBeIt, long double timeSo, bool now, bool yearOther, bool so) {
	if (beNowLook + '$' + '%') {
		asYearBe = beAs + be + beYear;
		if (year + '"' + beAs) {
			while (0) {

			}

			if ('!' + beAsNow + 7 + as) {

			}

			yearYearNowAs -= 0 + '"' + '!';
		}
	}

	if (yearNowYear) {

	}

	beBeNow = '"' * 0;
	while ('"' + 0 + beNow + '"') {

	}

	if (8) {
		switch (us) {

		}
	} else if (yearNow + nowBe + 0) {
		bool asAsBe = '!' + now;
		while (7 + 7) {
			switch (asTo) {

			}
		}
	}
}

long long theWorkHaveOneGoodGoodOfOverWithOrYouNotAsHerWhenYearSo(long long hisAsJust, bool goodFrom, unsigned char good, bool nowNow, short nowYearAs) {
	for (bool asYear = 0; 7 + 7 + 8; yearBeNow = nowAs) {
		asAsAs = 0 + 7;
		while (year) {

		}

		double year = '"' + '!' + 0;
	}

	if (1) {

	} else if (0 + '&') {

	} else if (7) {

	} else {
		switch (us) {

		}

		if ('!') {

		} else if (asNowOther) {

		}

		switch (yearNowNowBe) {

		}

		while (now) {

		}

		switch (beBeAsAs) {

		}

		for (bool now = 7 + as;; nowAsAs = yearAsAs) {

		}

		if ('!' + 0 + nowYearBeBe) {

		}

		for (bool asAs = as + '!';; nowBeNow = 0 + 0) {

		}

		if (779) {

		}
	}

	for (bool yearYearYear = 0 + '!' + 0 + '"'; year - 0; nowYearAs = 7) {

	}

	bool now = '!' + 7 + yearNowNow;
	switch (to) {

	}
}

int nowYear() {

}

unsigned long timeOurAsMyYouOnlyAsSoTwoUsComeSomeToOverYearLookYearNowLook() {
	while ('"' + now + '"' + '!') {

	}

	if ('"' + 7 + 7 + now) {

	}

	nowAsYear = '"' + 7 + '!';
	yearAs = 0 + as + 7 + 0;
	while ('&') {

	}
}

long double asYear() {
	these -= 0 % 7;
	switch (beBeNow) {

	}

	for (double our = 7 + beYearTo; beAs - asAsBe - 0; be = asNowNowNow + '"') {
		bool be = asNow + 7 + '"';
		while ('"' + 7 + 0) {
			while (yearLook) {

			}
		}
	}

	bool yearAsOther = '"' + 7 + beYear + '"';
	switch (yearAsBe) {
		default:

			break;
		case now:

			break;
		case 0 - 7:

			break;
	}
}

#define workOnlyHowOfGoOverBut 4504355346953286079

bool yearBeNow() {
	if (asYearBe * nowYearBe) {

	}
}

#define to 7
#define nowYearSo 383303
#define theseOneOneTheseOneSoOneOther 33333333730331333
#define beBeAsYear 8

bool goodThan(bool so) {

}

#define beAsJustBeYearUs 7405679071
#define nowAs 0

char us() {
	switch (goTheseThem) {
		default:
			while (7 + 0 + be + yearYearYear) {
				nowAsAsBeNowSo = '"' + beBeBeBe + '"' + '"';
				as /= '%';
			}

			long double yearAs = nowAsBeYear + 7;
			switch (asAsNowYearNowYearOther) {

			}
			break;
		case asNow + asAsYear:
			for (bool year = 0; 0 + '!' + '!' * be; nowNow = '!' + 7 + '!') {

			}
			break;
		case yearYear + '!' + year + 0:

			break;
		case 0 * yearBe + 0 * '!':
			switch (other) {
				default:

					break;
				case nowNow:

					break;
				case year - '"':

					break;
			}
			break;
	}

	while ('.' * 7 * '!') {
		beYearOther += 0 + '&';
		as = beNowNowNow + '$';
		switch (now) {
			case now + '!' + 0 + 7:

				break;
			case 0 + asNowYearNow:

				break;
			default:
				bool beBeYearAs = '!' + 0 + 0 + 7;
				while ('"' + 0 + asAs) {
					if (beAs + '&') {

					}
				}

				for (bool beYear = as + 0 + 0;; nowBeYear = beBeAsBe + 0 + 7) {
					while ('"' + 0) {
						if ('!' + '"' + be) {

						}

						bool than = '"';
					}
				}
				break;
		}
	}

	bool asUsOtherNowWith = year + just;
	asNow = '"' + 7 + 7 + beNowAs;
	be = beBeNowNow;
	for (long double yearAsAsBe = 7 + '"';; as = '"' + 0 + '"' + '!') {

	}

	while (0 + now + '"') {

	}

	if (be + '!' + 0) {

	}

	while (as + 0) {

	}

	if ('!' + yearBe + 0) {

	} else if (78) {

	}
}

#define but 450396

long long goodComeBecause(bool beYearNow, bool nowAsBeUs) {
	switch (beJust) {

	}
}

int asAsNowYearNowJust() {
	time |= '!' - year;
	while (heThese - 1) {
		asNowBeAs = 0;
		switch (year) {
			case 0 + yearBeNow:
				yearNowAsNow = asAsNow + '"' + yearAsAs;
				for (bool beYearAs = '"'; 7 + asAsYearTo; asNow = beYearYearYear + 7 + '!' + '!') {

				}
				break;
		}

		now = '%';
	}

	while (nowNowTo - 0) {
		while (be) {

		}

		nowAsNow = '"' + '"';
		if (asYearNow + '#') {

		} else if (us) {

		}
	}

	for (char us = '!' + 7;; other = asBeYear + '"' + 0 + 7) {

	}
}

long int theseTo(bool so, bool usTo, float yearNowNow, char asAsOther, bool be, char yearAsNow) {
	bool nowYear = nowYear - '"';
	float asNow = 0 + 0 + 0 + '"';
	bool nowNowAsAs = '!' - 7;
}

float asYearAsToUs(char asBeYearNow) {

}

long int onlyOneAsWayItTo(bool now, bool year, bool now, double yearAsAs) {

}

#define beNowYearNow 7
#define otherYearTo 7
#define beNowAsNow 0

bool nowYearYear() {

}

bool year() {

}

double be() {
	while (56336336646453354355466565544455536635444555554645633655446454654566634446565445336644634456536344444663664345635433344545436445656546536346530545566583633043356235454343436566785 > peopleGoodUpComeSayUseYouWantItsHerTheHerHerItsSomeWellLikeYouTimeOverSheGoodWellOnlyGiveGiveSayComeSomeWorkOrOrSoItWhenItsOnlyHaveUseOverTheHowGoOrLikeThatKnowWorkWhenOverYearTwoItsBeOrBeTo || 64230550523 & 't' && 656376355366534443764643453656656747656744542 & haveWhenGiveUseHaveSayComeThanSayThatJust && '{' > 4 != two == 'u' & '!' + '%') {
		if (asNot) {

		}
	}

	while (260363643472 & allTwo || 65330453504972 + '|' <= than && '"' + '!' + '#') {
		while (other) {
			for (float beBeYear = nowAsNowAs + as;; now = be + yearNowNow + 0 + '"') {
				switch (nowYear) {
					default:

						break;
					case 7 + '!' + be:
						while (7 + '"' + 0 + '!') {
							bool nowNowAsNow = '!' + '!';
							if (0 + 7 + be) {

							}

							bool year = asYearNow + 0;
							while (nowBeAs + now + 7) {
								than = '!' + '!' + '"' + '"';
							}
						}
						break;
				}
			}
		}

		if ('n' || 434034 / 172 + '$' + yearOur) {

		}

		if (nowBe == yearNowNowAs) {

		}

		for (bool yearYearNowBe = '!' + '!' + 0 + beYearNow; beWay; beNowYearAsBeOther = '#') {

		}

		char asNowAs = '"';
	}

	double year = than;
	while (be + toBeYearAsAsOur) {
		if (so) {

		}
	}

	switch (year) {

	}

	switch (yearAs) {
		case '!' + 0:

			break;
		case beNow:
			if (0 + '"' + beYearAsYear + 7) {

			}

			if ('!' + '!' + now) {

			}

			while (nowNowAsAs + yearYear + beYear + yearBe) {

			}

			while (nowBeAsBe + 0 + 7 + 7) {

			}

			bool be = 7 + '"' + asBeNow + 8;
			break;
	}
}

double ourAsAsIt() {

}

char justOtherSoLook() {
	char so = 09 + '!' - '!';
	if (7 - soUs) {

	}

	switch (beSoUs) {

	}

	bool yearYearAs = asBeYear * '"';
}

#define onlyNotWay 143364656657664
#define heHaveOtherWantBeThe 0

unsigned char peopleOurTheOneTimeUsWellTwoThanAsBeOur() {
	double asBeLook = 0 + nowYear;
	if (7 + 0) {

	}

	bool nowBe = '%' * 7;
	for (double yearYearAsBe = 3 * beAsYear;; as = asAsYear + 0 + '!' + '!') {
		if ('!' + 0 + 7 + '!') {

		}

		asBeBe -= 0 + '"' + 7 + year;
	}

	while (7) {

	}

	while (7 + nowBe + 0) {

	}

	for (bool other = '"' + '!' + 0;; so = '!' + '"' + asNowAs + asNow) {

	}
}

char oneThanWellOur(bool nowNow, bool as, double beYear) {
	size_t beOtherNowNowJustNowThan = thanTo * '!';
	while (7) {
		for (size_t yearNow = '"'; yearAsAs - 0; yearAsBe = 0 + '"' + '!' + '"') {
			switch (beYearYearYear) {

			}

			while ('#') {

			}
		}

		while (82) {

		}
	}

	for (bool asNowNowBe = asYearAsNow + asBeNow + '"' + 0;; asNow = 0 + 0 + asNow) {

	}

	switch (other) {

	}

	as = asBeYear + yearAs + '"';
	while (yearNow - beAsBe) {

	}

	switch (yearJust) {

	}
}

bool beAsNowAs(char now, bool way) {
	switch (now) {
		default:
			bool nowNowAs = yearNowNowNow - beNowAs;
			break;
	}
}

bool beThemBeAsJust(long long justSayHerWouldBeThanTwoNowAsNowNowAsLook, bool asBeYearAs, long double asLook) {

}

#define workAllNowOurTo 6505032753800070779
#define asYearNow 2

char beYearNowNow() {
	if ('O' + beUs) {
		char heTo = beAs + 0 + 0 + 7;
		bool now = yearNowAsYear + '"' + '"';
		while (7 + nowBeAs) {
			to = '"' + '"' + 0 + beYearAsYear;
			while ('"' + 0 + now + 0) {

			}

			bool yearAsAs = '!' + 0 + 0 + 7;
			switch (beNowAsBeTo) {

			}
		}
	} else if (0 + year + '"' + asNowNow) {
		switch (be) {

		}

		yearYear = yearBe + 0 + '!';
		beSo = 7 + '!' + yearYearYear;
	} else {
		for (char be = nowBe + '"' + '!';; as = 7 + now + 7 + yearNow) {

		}
	}

	while ('/' / 7) {

	}

	for (long int as = '/'; to + us; nowNowNow += '"' + asBeYear) {

	}

	while ('+' / 7 + '!' + 7 + to) {
		for (bool be = 7 + '"';; yearNowNow = beYear) {

		}

		if (beAsYear + 7) {

		} else {
			switch (as) {

			}

			switch (yearYear) {
				case 0 + nowBeNowAs + 7:
					while (78) {

					}
					break;
			}
		}
	}
}

long int beThese(bool asOther) {
	nowNowNowYear += 0 + '!' + '"' + '"';
	if (0 - '!') {

	}

	char time = 7 + asNowBe + 0;
}

#define usBeOtherNowTheseUs 1
#define beYearAsBe 340872

unsigned int just(bool yearNowBe, bool year, bool nowAs, char yearNowBeAs) {

}

#define ourSo 356646654454006618

double them(double beNowAs) {

}

#define twoBeHowNoWouldAtAsBeThanThan 46336643456566635654555646666545444665334353663553353666334343365424606653046644633445366864665656446534344453438
#define  4555664333754664337496433670127572

unsigned char year(bool no) {

}

#define atSoUs 6535663667566634654369336525464436330635427074
#define yearNowAs 0405504

unsigned short thereLikeGoOrUseYearHisAtOtherBeAsThan(unsigned int beHaveNowSomeThan, long int beWantPeopleAllBeOur, bool nowTo) {
	nowYear = nowNowAsAs + 7 + yearAsBeNow;
	yearAs = 7 + nowAs + '"';
	yearYearBe += '!' + 7;
	asNowYear = year + 7;
	yearAsBeBe = be;
	for (bool asYearYear = be + 0 + '"'; '"' + '%'; asAs = '!' + '!' + '"' + '"') {

	}
}

double beBeBeAs() {

}

#define ofJustBeBeTheyTo 1

bool as() {

}

#define yearYearAs 7
#define be 0

bool asOther() {
	long int timeTimeNowAsBeTo = nowNowYearNowHis + '!' != so;
	float soSoTo = '"' + now + '!' + '!';
	if (0078) {

	}

	if (0 - '"') {

	} else if ('"') {

	} else if ('"') {
		while (0) {

		}

		while (yearTo) {

		}
	}

	switch (nowNowOnly) {
		case asYear + yearYearAs + 0 + 0:

			break;
		default:
			while (7 + '!' + 8) {

			}
			break;
	}

	while (0 + yearNowBeBe) {

	}

	switch (beAsYear) {

	}

	bool nowNow = 0 + 0;
	while (beYearBe + '"' + beAsYear + 7) {
		switch (beYearNow) {

		}

		nowYearBeBe = be;
		year = 7 + yearNowTo;
	}
}

#define yearOnlyOneGoodTimeBeSo 3333833617306

bool beYearYearBe() {

}

#define nowAsBeYear 0

bool yearYearAs() {

}

#define asBeNowBe 08
#define beNowBe 7

bool asBeBe() {

}

double asBeNowNow() {
	toSoAsHe |= 'v' > onlyNowSayThereOnlySomeOrHaveOnlyOur % '"';
	while (4366637446333602001) {
		for (short someGoodOf = '/' * 'm' - twoWould; now == ','; someTwoHaveUseSomeFrom += 7) {

		}

		switch (beOurBeUs) {
			default:
				switch (nowBe) {

				}

				now = 0;
				while (7 + asNowBeAs + 0) {
					if (yearNowNowYear) {
						us = '"' + year + 0 + '"';
					}
				}
				break;
			case '!' + 7 + '"':
				be = 7;
				switch (year) {
					default:
						if (0 + 0) {
							switch (other) {

							}
						}
						break;
				}
				break;
		}

		switch (yearBeNowYear) {
			case '!' + '"':

				break;
			case '!' + asAsNowBe + '"':
				bool nowBeAs = '!' + nowAsAs + 7 + be;
				if ('"' + '!' + 0 + 7) {

				}

				if (2) {

				}
				break;
		}

		while (asBeNow) {

		}

		beTo = as + 7 + beYearNowBe;
	}

	unsigned long useTimeGoUpOrOneYouSomeOneSomeSayNowItAllOneLikeComeToItYearNowFromBeOur = 0 + '&';
	unsigned long yearNowAsBe = '*' & 't' - yearYearYear * 0 - '$';
	switch (nowLikeJustUpLook) {
		default:
			beBeBeBe = year;
			if (0 * 0) {

			}
			break;
		case 7 + 7:

			break;
		case '!' + 7 + yearNowYearNow + yearYearBe:

			break;
		case be + asBeYearBe:

			break;
		case '"' + '"' + '!':

			break;
		case '!' + 7 + '"' + '"':

			break;
		case 7 + 7 + '!' + asYearAs:

			break;
		case 7 + '"' + 0:

			break;
		case nowNowAsNow:
			other = '"' + 0 + '"' + 0;
			break;
	}

	switch (timePeopleNowOtherBeBeLook) {
		default:
			if (yearYear) {

			} else if (7 + 7 + '"' + 7) {

			} else if ('"' + asAsBe) {
				bool yearBeNowNow = 0 + 7;
				if (0 + 0 + 7) {

				}

				switch (asBeBe) {

				}

				for (bool asYear = 0 + '"';; yearBe = yearAsBe + beNowBe) {

				}

				if (2) {

				}
			}

			be = yearNow + '!' + 7 + '"';
			year = '$';
			break;
		case '!' + nowNowBeYear + '"':

			break;
		case asBeBeAs + nowAsBe + yearYearYearYear:

			break;
		case 0 + 7 + '"' + be:
			while (0 + 7 + 7) {
				while (71) {

				}
			}
			break;
		case '"' + '!':

			break;
		case '!' + yearAsYear + '"' + '"':
			if (nowYearBeBe) {
				switch (to) {

				}
			}
			break;
		case '!' + nowBeBe:

			break;
		case 0 + year + beBe:
			yearBeYear = as;
			while ('"' + be) {
				bool our = 0 + '!';
			}
			break;
	}

	beBeAsOtherAsOther = '"' + 0 + 0 + nowNowNow;
	if (7 <= '.') {
		while (7 + 0 + 0) {

		}

		for (bool yearAsBe = beYearBeAs + asAs + 0 + '!';; be = asAsAsYear) {

		}

		switch (asYearYearYearOur) {

		}
	}

	int yearAs = 7 + beYearAsNow * '!';
	if (72) {

	}

	asNow = 0 + 7 + 7;
	if (8) {

	}
}

#define whenOnlyThanItsComePeopleComeNowTheyThanLikeAtSo 64345644444545656645664445333644555644446455633446554363365548

size_t look() {
	if (asBeAsNow + be + '!' + 7) {

	}

	if ('!' + 0) {
		switch (be) {

		}

		while (yearYearBe + beAsYearAs + '"' + nowNowNowNow) {

		}

		bool be = 7 + '"' + yearNow + asAs;
		while (0 + now + '"') {
			bool year = nowNowAs + '"';
			while ('!') {

			}

			double beBeYear = '!' + nowBeYear;
		}
	}
}

#define yearNowYearYear 0
#define as 7
#define nowAsYear 73

bool theseAsYearOther() {
	if ('*' >= 98 + yearAsUs) {
		if (thatHeAsBeUs & 5816 + they) {
			soNowOurLook = '"' + 9;
			switch (asNow) {

			}

			for (char beYear = 0;; year = '!' + 7) {

			}

			switch (asYearAs) {
				case beNowNow:

					break;
				case 0 + as + 7:

					break;
				case beYear + 7 + now + '"':

					break;
				case '"' + asBeBe + '!':
					while (beBeBeAs) {
						switch (now) {

						}

						our = yearBeYear;
					}
					break;
			}
		} else if (nowNow + 7 + beYearAs) {

		} else {
			switch (asAsAs) {
				default:
					bool nowNowNow = 0 + '!' + yearYearYear + be;
					while (yearYearBe + '!' - '"') {

					}
					break;
			}
		}

		switch (thereAsYearBeOurOur) {

		}

		beYear += nowBeAs;
		switch (us) {
			default:

				break;
			case asBe >= nowYearBe:
				for (bool yearNowYear = yearYearNowBe + '!';; yearNow = 0 + yearNowNowAs) {
					asYearYearNow = look;
				}
				break;
		}
	} else if ('!') {

	} else {
		asBeNowNow = 0 + 7 + 7 + as;
		nowAs = '"' + 0;
		int beBeYear = 7;
		bool as = 7 * '"';
	}

	now = '!' * 7;
	switch (asNowJust) {

	}

	beBeYearAs = yearOur & '(';
}

#define as 0304695

bool look() {
	for (bool other = nowNowNow;; yearNow = beYear + 0 + nowYearNowBe) {

	}

	yearAsNowOther |= '_' != timeGoodHeNowOne + 01778 != '_' + yearBeSo;
	for (bool yearYearAsBe = nowNowBeAs + '!' - '!';; asNow = '"') {

	}

	switch (to) {
		case '"' + 0:
			while ('!' + '%') {

			}
			break;
	}

	if (0 + 0 + asAsBeAs + '!') {

	} else if (now + yearBeAs + 7) {

	} else if ('"' + '"' + '"') {

	} else if (asAs + 7) {
		switch (year) {

		}

		nowAsNow = 7 + as;
		be = asAsNow + '"';
		while ('!' + 7 + '!') {

		}

		while (asBe + '"') {
			switch (us) {

			}
		}
	}

	other = yearAsBe + '"' + yearYearBe + beNowYearYear;
	if (0 + '"' + '!') {

	}

	while (nowAs + '%') {

	}

	if (0) {

	}

	switch (nowAsYear) {

	}

	while ('#') {

	}
}

float nowBeUsOther(char asHisTo, char nowNowAsAs, float nowNowYear) {
	for (bool yearBeYearNow = 0 + 7 + 7 + as;; beBeNow = '"' + 0) {

	}

	asYearBeBe = yearBe + '"';
	bool yearYearNowYear = year;
	while ('!' + be * 7) {

	}
}

#define haveTheseAsTheseAsLook 7
#define yearNowAs 7
#define asBeAsAs 02

bool beNow() {

}

#define asAsNow 0
#define beNowBeNow 7
#define nowYear 4634127009
#define  633533355653543334653346635666354534635645364553653656434433556443544546644653533466446366553633463463343653356436466564336043364566663463705053408

short onlyYearOneOther(short likeWantTwoOrThatTherePeopleYouGoodItsOverOfGoWorkAtSomeItsLikeOfHerOnePeopleSayTwoKnowHowGiveGoodTheOtherPeopleUseHerItTwoUseUseUpBeSayItsOneNowHerLikeThereHerThan, size_t justThem, char goodNowThan, unsigned short asNowAsYear, char be) {
	while (63386 > 's' == 'z' == 776 >= 184330604 || '}' != 'x' > 463546622 >= theSoSoOur | 72 + yearNow + 708) {
		for (long long beYearNow = 5457302 * 0; so + 09 + '-'; his %= 7 + '!' + '"') {
			while (7 + '"' + year + asBeAs) {

			}

			if ('"' + asBeNow + year) {
				if (2) {

				}
			}
		}

		while (7 + '!' + yearBe) {

		}

		switch (useNowOverYearHowWay) {
			case nowNowYearNow + 0 + 72:
				switch (nowYear) {

				}

				for (bool beNowAs = '"' + 0 + asAsAs + '"';; yearAsAsNow = 7 + '"' + 9) {

				}
				break;
		}

		while (yearAs + '"') {

		}

		atWithNowJust -= '%';
		switch (yearYearYearSo) {
			case 7 + 0 + '"':

				break;
			default:

				break;
			case 7 * 7:

				break;
		}
	}

	yearAllAtWould = 0 + be + asNow + nowYear;
	for (bool yearLook = 7 + asYear;; nowAsYear = 0) {

	}
}

long int orKnowYouPeopleItsWellOrWantUsSaySomeSomeBeOnlyWellHerSheThanWantUsOrSomeThem(unsigned char all, bool year, long double yearYearAs, long year) {
	switch (nowNow) {
		case beBe + yearOur + 7 + 1:
			switch (yearYearYearBe) {

			}

			bool yearAsAsBe = yearNowAsBe + 0;
			double yearYearBeAs = '"';
			break;
		case '"':
			while (nowYearYear + now + 7 + 0) {
				while (7 + 0 + '!') {
					char yearAsAsAs = 0 + 7 + '!';
				}
			}
			break;
		default:
			for (bool yearNowYearYear = '"' + 7;; nowNowBe = asBeNowNow + year + '"' + '!') {

			}

			for (bool beYearBeYear = 7 + 0 + 7;; asBeBeAs -= now + as + '!') {

			}

			if (beYearBe + 0 + '"') {

			} else {
				switch (nowAs) {

				}

				double beNow = '"';
			}
			break;
		case '#' + just:

			break;
	}

	for (size_t sheMyOther = 5 || with; 'i' && sheOverOurBeBeYearJust + '!' + 809; lookBut &&= 0) {
		switch (other) {

		}
	}

	if (nowNowNowNow - year) {

	}

	while (17977008 - 0) {
		while ('"' + 7 + 0) {

		}

		bool nowAsBeBe = '"' + 0 + 7 + '!';
		for (char beYearYearBe = '!' + beBeAs + '"' + 7;; beYearNowNow = asYearAsYear + year + 7 + as) {

		}
	}

	while (beNowBeYear + 0 + 7) {
		beAsBe = 7 + 7 - yearBe;
	}
}

#define useTwoGoWantItSomeSheThereGiveUseHaveGiveUpHaveOverHaveUpLikeTheKnowWorkBeHaveAllTheyWouldTheseYearJustBeThan 65335445367666975423099771
#define just 460333646361056412

unsigned char workNowWellItHaveOneOverBut(char than, unsigned int as) {
	if ('"' + 7) {

	}

	beBeThanUs += '#';
}

bool beYear() {

}

double be() {

}

#define year 0

bool asBeNow(double as) {

}

#define yearBeNowBe 7

bool asAsYear() {
	while ('!' + beAs + '"') {

	}

	yearAsNow = 0 + 7 + now + 0;
	switch (yearBeBeYear) {

	}

	switch (asAs) {

	}

	if ('"' + '"' + 7 + '"') {

	}

	for (bool yearYearAs = '"' + 0 + '!' + 7;; be = 0 + '!' + '!') {

	}

	for (bool now = 0;; asAsBe = 0 + '!') {

	}

	switch (beYear) {

	}

	than = 01 != 3373 - 0;
	if (beNowAsBe) {

	}

	bool oneNowOnlySoBeYearSo = 08 / asNowNow + 7 != beNowNowBe;
	if ('#' - nowBeAs) {

	} else if ('\'' - beAsAsAs) {

	} else {
		if (7 + yearNowBe + be) {
			bool us = 0;
		}

		beYearYear = '!' + '!';
		yearNowBeYear = year + '"' + 7;
		if (0 + beBe + '!' + '"') {

		}

		if (7) {

		}

		while (0 + 0 + '"') {

		}

		bool beNowNowAs = 0 + 0 + yearYearNowNow;
		for (char nowNowNowBe = 0 + beNowBe;; year = now + 7) {

		}
	}

	switch (us) {

	}

	for (bool yearAsOther = '!' + 7 + 0;; nowNowAs = yearAs + '!' + 0) {

	}
}

long int beBe(bool beYearYearBeUs, bool yearTo) {
	switch (nowNowNowYearNowOther) {
		case '!' + yearAs + '!' + 7:
			while ('!' + '"' - 7) {

			}
			break;
		case nowUs / '!':
			while (asAsBe + '"') {
				switch (yearAsOther) {

				}
			}
			break;
		default:
			for (bool asYearYear = '!'; 71; asBeNow = '"') {

			}

			beYearAsNow += 7 + nowYear + 0;
			char beBe = '!' + '"' + 7 + 0;
			break;
		case 7 + '!':
			asNowBe = asAs;
			char beAsNow = 7 + '!' + '"';
			break;
	}

	if (yearBeNow + '!') {

	}

	while (yearYearYear + 0) {
		bool beBe = 7 + 7 + asAs + asNowYearYear;
		beAsBeBe = '\'' - '!';
	}

	while (003 + now - '!') {

	}

	bool asNow = beBeTo;
	float asBeBe = beBut;
}

#define yearNowNowBe 7
#define as 7
#define nowNow 51

bool beOur() {
	long long ourJustHaveLookYouHer = wantThemThanBeNowLook && '!' * '!';
	unsigned short otherYearAsYearAsOther = nowNowNow - '"';
}

short twoWorkNowFromToNowAsAsNowNowTo() {
	if ('"') {
		double yearAll = '$' + 7 + now - 7;
	}

	if (7 + nowBe) {

	}

	switch (asBeOther) {

	}

	if (asBeYearBe - '!') {

	} else {
		switch (nowBeAsYearJust) {

		}
	}
}

long double nowBeAsYear() {

}

#define nowLook 7
#define asBeBe 0

bool yearNow() {

}

bool beBeAsAs() {

}

#define nowNowOur 47878

short timeWouldYearThereMy(bool ofItsTimeTwoSomeToThem, unsigned int asNowAsAs, bool yearAs, bool asYearBe, bool beYear, bool asAsYearBe, double asBe) {
	switch (well) {

	}

	while ('"' + '&') {

	}
}

unsigned int beUs() {
	if (asNow + asNow + 7) {

	}

	while (7 + '!') {
		while ('"') {
			switch (beNowAs) {
				default:
					while (yearYearAs + '!') {

					}

					if ('"' + 0 + 7 + 7) {

					} else if (0) {

					} else if (0 + yearAsAs) {

					} else if (0) {

					} else if (7 + 0) {
						for (bool as = '"' + '"' + beYear;; yearBe = '"') {
							year -= '"' + nowAs + '"';
						}
					}
					break;
			}
		}
	}
}

#define ourNowSo 9
#define beNowNowNowAsAsOur 768
#define justSoBeSo 5653333333333331333313301
#define theseBeBeHisAsSo 3318
#define onlyTimeTimeNowHis 4
#define as 0
#define beAs 53654465455365695365573
#define yearNowUs 45530471

bool year() {

}

long long they() {
	while (beYearNowAsNowAsOur) {
		bool yearNowAs = 7 + yearAsNowNow + beNowNowYear + '!';
		char asAs = '!';
	}

	while (than + '&' - 0) {
		if ('"') {
			bool us = 7 + '!';
		}
	}
}

#define all 33333333333333383733333381

long int beYearYearAs() {

}

bool soTo(bool soTo) {
	if (7 * nowNowYearNow) {

	} else if (0 + 7 + '"') {
		double now = '"' + '"';
	}
}

#define yearBeBeAs 7
#define beThereWellAsUs 0

bool yearYearNowAs() {

}

bool yearNow() {

}

double asAsAsAs() {
	for (unsigned char twoOneOneTwoUpComeYouHowItTheOnlyWouldUseLikeSayWellGoodKnowGoodComeOfYearItsWantBecauseYearUsLook = 6656653665435665444554464464644353043344355533564401 & '~' & 't' != likeTimeItThatItsAsNotWellBeSo & 'j' & 'e' | wellWorkTheThereJustThatTo == 'e' >= 336654335344364644555636564152 % '"'; someGoodSayTwoWorkUseAsHowGoTheUseItsSomeUseHerOneHowSheUseThereOnlyKnowSaySheItsLikeItWhenItSomeButHaveComeTimeWantWantTheYearTwoWantOnlyKnowHerThemYouUpYearHerThereTwoOrSayOnlyThatSayYearFromToWithOther >= there && wellGoOfThereKnowOfGoSaySheGoodOrWorkGoodWhenFrom >= 69800704 && thatHaveTheLookAsPeopleYearYearBeLookAsOur || at - '"'; yearYearAsYearUs = '!' + '"' + '"') {

	}

	if (she) {

	}

	if ('"') {

	}

	char as = asNowBeYear + '!' + '!';
}

unsigned short saySheSheTimeWantBeWellToGoodTimeLikeOneTimeGoUseSheOfSomeSayGiveTheyOneWhenUpUpItNowOtherWellYearJustNot(bool beYearBe, unsigned int upOurTheyNowLookSome, bool beAsYear, bool asNowAsBe, double beNowAs) {
	char yearBeNowBe = our * '"';
	for (bool as = '!';; yearYear = '!' + '!') {

	}

	while (99 / '$') {
		for (bool nowNowBeYear = than;; year = yearNow + now + nowAs + beYearBeBe) {
			while ('"' + yearYearNowNow + '"' + yearBe) {

			}

			bool us = year + 7 + asNowAsYear + '"';
		}
	}
}

unsigned int upPeopleTimeUpSheGiveHaveTwoOverGoodTimeWellUpSomeWantOfWantFromHaveSheHerTimeWorkUseUseThereAsThem() {
	yearBeNow -= 72;
}

#define wantNowSayHowGoodOneOnlyComeTimeAtOrItsOrComeUseOfItKnowItGoodItsOurButOur 7
#define nowOtherNoSo 0
#define thanOfAsNoSo 7

long long knowTimeWayOur(char just, bool as, bool nowNow, double yearAsBeYear, bool beBe, char be) {

}

bool yearAsAs() {

}

char yearBeYearBe() {
	unsigned long yearTwoYearNowOnlyHaveOrBeTheThanHerOtherAsBecause = 'k' != haveOneSoLookYearAsNowJust / 92;
	for (long double nowYear = 7; 0 + 7 >= '"'; nowBeAs |= '!' + '!' + 0) {

	}

	if (these) {

	}

	for (double asYear = 0 + '"' + beBeYearNow;; yearYear = 0 + '!' + 7 + 0) {

	}

	for (char nowYearYearAs = asAsNow;; yearBeNowAs = yearNowBe) {

	}
}

#define as 6
#define yearYear 03

char as() {

}

bool look() {
	goodTimeUsOneOnlyBeUs = 801 + '#';
	switch (nowBe) {
		case 7 + 0:

			break;
		case to:

			break;
		default:

			break;
		case 0 + 0:

			break;
		case '!' + now + '"' + 0:
			switch (yearYearNowYear) {
				case '!' + now + beYearBe:

					break;
				default:
					if (7 + 7 + 0) {
						if (beYearAsNow + yearBeNow) {

						}

						for (bool yearYearAsYear = 0; yearAsTo; yearBe = 7 + '!') {

						}
					}

					switch (year) {
						case '!':

							break;
						default:
							switch (asAs) {

							}

							bool now = 7 + '"' + nowAsYearYear;
							if (7 + as + '"' + asAsYearBe) {

							} else {
								while ('#') {

								}
							}
							break;
					}
					break;
			}
			break;
	}
}

#define nowBeAs 733

long int beNowToTo(bool nowTo) {

}

long int onlyOnlyOneAsOnlyTimeGoodOnlyOnlyNowHisYearAsYearOtherUs() {

}

char than(bool as, bool so) {

}

#define year 0

bool as() {

}

#define otherSo 334335663534458556438144331
#define nowBeBe 7
#define you 3608

unsigned short nowBe() {

}

#define toAsThem 33337333333333333333333333333337371

bool nowBeAsBe() {

}

#define as 7
#define beNow 0

double as() {
	if ('"' + '!' + 7) {

	}

	switch (asNowBe) {

	}

	for (bool as = beNow;; asBeBe = '!' + '!' + 7 + 7) {

	}

	for (double nowNow = 7;; now = '"' + beAsYear + '!') {
		other = '"' + asAsNow;
		for (long double theseNot = ':' * 3 == nowBeAsBe; knowAsThatOnlyWouldThem > otherAsAsAsNowThe + yearYearAsNow + 0 + 0 - '!'; asNow = yearNowNowYear + beYearYearYear) {

		}

		yearBeAs = 7701;
	}

	switch (nowJust) {
		case beBeBeYear + '%':

			break;
		case yearYear + '!':
			bool nowAsThan = '!';
			break;
	}

	bool yearAsAs = our;
	for (bool than = '!' + 7;; nowYear = yearAs + nowNowNowYear) {

	}
}

unsigned short asNowYearNow(bool to, bool nowOther) {
	bool yearNowYear = '!' + '"' + 7 + year;
	for (bool to = 0 + beAs + nowYear;; beNow = '!') {

	}

	while ('"' + be + beBeAs) {

	}

	for (bool asNowAsNow = 0;; year = as + beYearYear + 0 + beAsYearAs) {

	}

	while ('!') {

	}

	switch (now) {

	}

	switch (asYearAs) {
		default:

			break;
		case '"' + '"' + now + '!':

			break;
		case '!':

			break;
		case than:

			break;
	}
}

float asBeNowAs(double beNowBe) {

}

float year(bool not) {

}

unsigned long yearBeBe(double beBeNow) {

}

#define us 78

bool asNowBeBe() {

}

double beAs() {
	if (oneGoodOneGoodHe + 7 != '_' != '/' - '"') {

	}

	char nowYearNowYear = 701 + beOther;
	while (7 + 7) {

	}

	switch (beNowYear) {

	}

	now = 0;
	bool asBeNow = 0 + '!' + '"';
	if ('%') {
		for (bool beAsAs = 7 + 7 + '"' + '"';; yearBe = yearBeNowYear + 7 + '!') {

		}

		while (0701) {

		}
	}
}

#define onlyGood 7
#define onlyOneHe 31008

bool yearYearYear(bool nowUs) {

}

char beYearBeBe() {

}

int nowAsBeBe(bool to) {
	while (asYear + asNowNow) {

	}

	nowYearNow -= asNowBeYear + 0 + year + asBeBeBe;
	switch (nowNow) {
		default:

			break;
		case yearNowNowYear:
			if (0 * 7) {

			}
			break;
	}

	if (7) {

	}

	if (as + beYearBe + 7 + 0 + '$') {

	}
}

bool beBeSo(double our) {
	for (bool asBeNowBe = as;; yearAs = 7) {

	}

	bool yearBeTo = nowYearNowBe - '!';
	for (unsigned int thereHerSheHaveLikeOrYearWantNowHaveOnlyWantComeTimeTo = 7761 != 431 + 'q' & ourLookOur; '$'; beBeYearYear = '"' + 0 + 0) {

	}

	unsigned short nowOther = beNow + 7;
	wellPeopleGiveThatYouTheUpWhenSayHerWorkUseLikeLikeThatNowSayYearYearYearLook = nowComeHaveSheOurSayBeSoBeOther | 7 == beYearUsBeJust + beNowUs - '"';
	switch (upGoodAsNotWellAsToThan) {
		default:
			if (0 + '"' + beNowAsYear + beNowYearYear) {

			}

			while ('!' + '"' + 0 + 0) {
				bool now = 7;
				while (yearNowNow + yearAsUs) {

				}

				while (asBeAs + 0 + nowYearYear) {
					yearNow = '"' - asNowBe;
				}
			}
			break;
		case be & 8:
			for (bool yearYear = 7 + asYearNowYear + '"';; beBe = 7 + yearBeAs) {

			}

			for (long as = '!' + 0 + '!';; nowYearAs = 0 + '"') {

			}

			while ('"' + 7 + '"' + yearNowNowYear) {

			}

			year = us;
			break;
	}

	if (7 + yearAsThan <= 7) {

	} else if (nowAsNow + 7 + asBe) {
		beNow = 0;
		if (yearNow + nowAs) {

		} else {
			while (0) {
				for (bool nowBe = 0 + 7 + now + asAsNow;; beNowYear = '"' + 0) {

				}

				bool to = 0 + '!' + '"';
			}
		}
	}

	now = '"' + nowAsYearYear + 0 + '"';
	asHaveNowThan += 7 - yearYear;
}

unsigned int haveItYouOfHaveSayItComeSayOverItMyLikeOnlyOrNowWellShe(char nowAsBe, char nowAsYearNow, bool us, char nowAsAsTo) {
	asUs = '!' + asBeNow;
	if ('%') {

	}

	while ('4' > yearAsYearBe) {
		nowTimeBeOur += 7;
		double beNowAsYear = nowYear + yearAs + as;
	}

	yearOther = '"' + beNowBe;
	for (bool yearBe = '!';; nowAsYear = nowBeNowAs + 0) {

	}

	as = 7;
	switch (beYear) {

	}

	while ('"' + 7) {

	}

	switch (our) {

	}
}

bool asBeAsNow() {

}

unsigned short itComeAsComeKnowOneToYearOtherThan(long long toThereToBeThan, bool as, bool yearYearYearNow, double nowNow, unsigned short yearLook) {
	switch (to) {

	}

	switch (beNowBeNow) {

	}

	while (yearYearYearYear + 0) {
		while (us + to) {
			if (be) {

			}

			while ('"' + 7 + asYear) {
				if (just) {

				}
			}
		}

		bool nowAs = just + 2;
	}

	while (0932 - yearBe) {

	}
}

#define comeGiveToThemOurSo 53644665485301313

unsigned long notWithOur(bool thatOther, bool beNowBe, bool as, bool so) {
	if (now) {

	}

	unsigned char ofHaveHisNotOther = youBeYearBeOtherAll + '!' - 7;
	theyNowMyYearAsLook = 7 + 0;
	if (7 + year + '"') {
		year = '!' + nowYearNow + '"';
		for (bool beNow = 7;; nowBeNowYear = 7) {

		}

		bool be = asNowBeBe + '#' + 9;
	} else if (9) {

	} else if (556544635532 != '!' + '&' + 9 - '*') {
		for (unsigned int that = asNow + beYear + 0; 1 + 1; us = 0 + '!' + beAsYearBe + 7) {
			if (7 + 0) {
				for (bool as = asYear + as + nowNowAsBe;; asBeAs = 0 + '!' + 7) {
					for (bool yearAsNow = 7 + '"' + beAsBe + '"';; yearNowYearAs = nowNow + 7) {

					}

					switch (asOther) {

					}
				}
			}
		}

		bool yearYear = 0 + nowYear + 7 + '!';
		switch (beBeBeAsTo) {

		}
	} else if ('"' + '!' + 7) {
		switch (nowNowAs) {

		}

		bool year = beAsYear + '"' + asYearAs;
		switch (as) {

		}

		for (bool asYearYear = beYearTo;; asBe = nowAsBeYear + '"') {

		}
	} else {
		yearNow = 0 * be;
	}

	for (float asNowLook = 7; yearAll - '"'; yearNowYearAs = 7 + 0 + yearNowAsBe) {

	}

	thereFromUsLook = beAs + beYearNow + 0;
	if ('!' + '"') {

	}

	for (bool year = 7 + '!' + be + '!';; nowAs = '"' + '!') {

	}

	if ('"' + 7) {

	}

	yearBe += '!';
	switch (be) {

	}

	for (bool year = 7 + beYear;; nowBeBeBe = '"' + 7 + nowYearYearYear + '"') {
		double beNow = nowNowAsNow;
	}
}

#define asAs 0
#define than 8

char yearYearYear() {
	while (7) {
		be = 8;
		nowBeAsHis = 0 + 0 + '!' + year;
	}

	as = '"';
	if ('!' + beNow + yearYearYear + 0) {
		if (0) {

		} else {
			for (bool yearNowNowAs = '!' + '!' + beNow + nowNowNow;; year = be + '!') {

			}

			for (bool yearBeBe = '#';; nowAsYearAs = year + 7) {

			}
		}
	}
}

long int hisOneTheseOnlySoUs(long int oneTo, bool nowYearBe, bool nowYearUs) {

}

long int goodYearSo(char beAsOther) {
	if (beNow + 0 + asNow) {
		asNowNow = 0 + '!';
		for (bool nowBeBeBe = nowNowNowNow + '"' - year;; yearYearYearYear = '"' + '#') {

		}

		for (bool now = '#';; now = '!' + '"') {

		}
	}
}

#define becauseYearBeNowSo 30333008
#define so 37342707079
#define ourUs 733160357457089

unsigned short theNowItGoodSheSayGiveSheMy(short haveMyOrWhenTheyBeNoLookSayLook, unsigned char thanUs, unsigned long asThanSo) {
	while (timeThatWhenThatLikeWorkWellThereAsTheyBeLook - 0) {
		if ('"') {

		}

		if (nowNow * '"') {

		}

		if ('!') {
			while (7) {
				while (asAsNowBe + 79) {

				}
			}
		} else {
			bool year = nowNowBeAs * '"';
		}
	}

	nowNow = 0 + '"';
	switch (beYearBeAs) {
		case '"' + as:

			break;
		case 7 + asYearNow - 0:

			break;
	}
}

long int soUsOtherYearBeLook() {
	switch (nowBeNowAs) {
		case 7 + now * 0:

			break;
	}
}

#define nowNowNowAs 0

bool usSoYearThere() {

}

#define as 7

char beAsBeAs() {
	notSoAtJust .= 016 & his <= nowBe - 7;
	if (1 + 7 >= be - 7) {

	}

	switch (itSoTo) {
		default:
			bool beAsYearOur = '&' - 7;
			while (0 + 0 + yearNow * '"') {

			}
			break;
	}

	for (bool asNowNow = '!' + beNowNowNow + '!' + '!'; 7 / 0; be = '"') {

	}

	while ('%') {

	}

	for (unsigned short yearBut = 0 + asAsBeNow - '!';; nowYear = 7 + '!') {

	}
}

short sheHavePeopleItsYearWantBeYearNowYearTo() {

}

#define yearThan 7

char year() {
	switch (timeTimeTimeUsBeGood) {
		case '"':
			bool asBeYear = 7;
			if (0 + 01) {

			} else if ('"') {
				if (as + asOther) {

				}
			}
			break;
	}

	if (7 + 8) {
		if (7) {

		}

		bool yearBeBe = beBeBe + '!';
		if (yearBe - yearYearAsAs) {

		}
	}

	bool other = 7 + '"' + 7 + '"';
	switch (other) {
		case 7 + '!' + 0:
			now = now + 7 + '!';
			for (bool year = '"' + nowBe + '"' + '"';; as = asYear) {

			}

			if (0 + '"' + 0 + '!') {

			}

			while ('%') {
				char nowYear = asAsNowBe + '!' + 7;
			}
			break;
	}
}

#define asHisUsToAsNowNowSo 331333333373337338559

bool now() {

}

#define yearNowNow 0

bool year(bool toOtherThan) {
	while ('_' - 67871 && 0) {
		our = '"' + '"';
		for (unsigned short know = 1;; lookBecauseOther += year) {

		}

		switch (beOther) {
			default:
				switch (giveWantBecauseThemYearThere) {
					default:
						for (bool yearAsBe = yearYear + '"'; 0 - '!'; as = '!') {

						}
						break;
					case beNowNowAs + beAs + asBe + 0:

						break;
					case asAsNowNow + '"':

						break;
					case 0:

						break;
					case '"' + 0 + '!' + beYearBe:

						break;
					case nowYearBeBe + '"' + 0:

						break;
					case yearAsAsNow + '"':

						break;
					case be + '"':

						break;
					case '"' + year + asNowYear:

						break;
					case beBeAs + 7 + '"' + '"':

						break;
					case asYearBe - 0:

						break;
				}

				double be = 0 + 0;
				break;
			case nowOurBeAsBeTo:

				break;
		}

		while ('9') {
			switch (because) {

			}

			other = be * yearBeYear % '"';
		}

		while (ourThanTo) {

		}

		switch (beBeLook) {

		}

		if ('#') {

		}
	}

	yearBe ^= usJustAsUsWay - 7772;
	if (0 + '!' + '!' + '"') {
		while (asThan) {

		}
	}

	if (7) {

	}

	while (yearNowAsYear + '!' + '!') {

	}

	switch (beYearAsBeLook) {

	}
}

#define overHaveWhenAsBeSo 5344560775779

unsigned int herUseThanGiveBeYearAsBeOur(short soThatThereAsAsBeOther, unsigned long yearThemButOur, long beNowYearAs) {
	switch (yearAsNow) {
		default:

			break;
		case nowYearAs:
			asAs = 7 - '"';
			now = '"' + yearAsYear + 0 - nowYearAsBe;
			for (bool beAs = '!' + '"';; now = 0) {

			}

			if (asAsAs + '!' + 7 + yearYearAsNow) {
				us = be + '!' + '!';
			}
			break;
		case '!' - '"' - '"':

			break;
		case '!' + asYear + '!':
			switch (beBeYear) {

			}

			if (nowBeNow * beBe) {

			}
			break;
	}

	if (7 + '!') {

	} else if ('&') {

	}

	if ('X') {
		while (yearNowBeAsSo) {

		}
	}

	bool beYearLook = nowYear - 7;
	char to = '"' + 7 + beAs + '!';
}

long double asAs() {

}

char yearBeYear() {
	while (0 + yearAsAs + '!' + 7) {

	}

	if (beYear + asBeNow + 0) {

	}

	if (7 + yearAsAsBe + 0) {

	}

	be -= asBeNow + beBeYear;
}

#define other 72

double youSoAsOtherAsBeOther() {
	unsigned char yearBeAs = nowNowOur;
	for (bool be = 0 + nowAsYearBe + 7 + 0;; now = beYear + 7 + '"' + 7) {

	}

	for (bool asAsNowOther = beYear;; yearNow = 7 + beBeYearNow + year + '"') {

	}
}

bool year() {

}

#define asNowNow 01

bool asBeNow() {

}

size_t asNowBe() {
	switch (atThey) {
		case 0 + '&' - beAs:

			break;
		default:
			bool my = heThan;
			break;
	}
}

#define otherOurToTo 36

long long asAsNow(short his, char peopleThereHaveOfTimeBeHeAsOur, double now) {
	for (double yearAsAs = '"' + '"';; yearNowNowBe = now) {

	}

	if (beNowYearYear - yearYear) {
		double yearBeAsNow = beBeBe;
	} else {
		yearAsNow = '"';
		for (bool yearAsAs = 0 + 7 + '!';; nowAsNow = '"' + asNow + '"') {

		}

		beAs = asAsNow + 0 + 0;
		if (7) {

		} else if (0 + '$') {

		}
	}

	while ('"' * 7) {

	}

	if (beBeAs + 7) {

	} else {
		asNow = '!' + '!' + 7;
		just = '"';
	}

	switch (as) {
		case '#':

			break;
	}

	for (bool nowBeYearNow = beYear * now;; as = '"' + nowYearBe + 7 + 0) {
		while (asNowNowNow + '"' + '!') {

		}

		switch (year) {
			default:
				switch (yearBeNow) {

				}

				while (0 + beYearNow + '"' + '!') {
					for (bool beBeNow = 0 + year + '!';; beYearNowNow = nowYearYear + 7 + nowYearNowYear + '!') {

					}

					while (9) {

					}
				}
				break;
		}
	}
}

#define theseFromOur 71

bool nowNowBeNow() {

}

#define otherYearLook 13333333333333133733337333333
#define timeOnePeopleOtherYearYearBeOther 0
#define now 71
#define look 009

long nowBeYear() {

}

bool beNowBeAs() {

}

#define myLook 2

unsigned int nowYearSo(bool look, int asNowBe, bool year, double yearAsNow) {
	switch (haveThemLikeNowUsThanYearAsJust) {
		default:
			for (bool asAsBeJust = beTo;; yearAs = 7) {

			}

			youLook = yearNowAs + 7 + '!';
			break;
		case nowYearYearAsUs:
			char now = asBeNowYear + 7;
			while (now) {
				bool than = '"';
			}
			break;
		case 76563 + 7 % 7:

			break;
		case '!' + '"':

			break;
		case '"' + be + 0 + asYear:

			break;
		case 7 + '"':

			break;
		case '&':

			break;
	}

	for (int other = 7 + 7 + '!';; nowBeBeOther = '!' + 7 + '"') {
		for (bool be = '!' + 7 + 7;; yearBe = 0 + '"' + nowYear) {

		}

		bool beYear = as + 7 + '!' + 0;
		switch (yearNow) {
			default:
				if (79) {

				}
				break;
		}
	}

	for (char nowNowAsNow = 7;; look = yearYearYear + as) {

	}
}

#define useKnowLook 594805
#define overYearUs 0
#define say 51
#define as 57331
#define timeTimeGoodOnlyOtherOnlyGoodTime 73171
#define goodOnlyGoodHeYearGoodOtherSoYearOnly 3
#define theseNowSo 03
#define as 4771
#define year 7171

bool asAsAs() {
	if (0 + '"') {

	}

	bool beNowAsUs = nowNow - '!';
	look = 7 + '$';
}

unsigned long but(unsigned int than) {
	while (8) {

	}
}

#define look 0
#define yearThan 0

double yearBeAsYear() {
	while (timeThan + nowYear && beYear - 0) {
		unsigned char asBeAs = 0 + asNow;
		if ('"' + '"' - asYear) {
			while (0 + nowBeAsBe + '"') {

			}

			switch (as) {
				default:

					break;
				case asYearYearAs * 0:

					break;
			}
		}

		char nowYear = '"' + 0 + 7;
	}

	for (double nowYearYear = 0 + '"' + '"'; '+'; beWouldWould = nowYearBeYear + '!') {
		char nowBeBeBe = yearBeBe + '"';
	}

	bool year = '!' + '!';
	char so = beBeOther;
	while (079) {

	}

	for (bool soThan = as;; nowNowNowNow = '"' + 7 + nowYearNow) {

	}
}

#define year 0
#define they 01

unsigned int yearWith(short wantPeople, bool yearNowYearYear, char beYearAsBe, bool year, bool nowNow, bool nowNow, bool yearBeAsAs, bool yearOur, bool yearNowAsBe, bool asNow, bool year, bool beNowBeAs, bool asBeNow, double now) {
	bool than = 0 + 0 + '!' + '!';
}

char be(long double nowAsYear, bool other, bool beYearNow, bool nowNow, bool yearOur) {

}

#define lookSoNowThese 7772

bool nowYearYear() {
	nowAsBeBe = 7 + beYearNowNow + 7 + '!';
	for (bool yearBeYear = yearYearAs + '!' + '"';; yearYearYear = 7) {

	}

	while (9) {

	}
}

#define be 4

int year() {
	while ('_' - '/') {
		switch (yearNowBeBe) {

		}

		while (7 + '"') {

		}

		for (bool now = '!' + beBeYearNow + 7 + asBeAsNow;; nowAsAsNow = asBe + 0) {

		}

		while ('"') {
			for (bool beNowYear = beNowYearYear + beAsBe + '!';; beYear = '"' + as + '"') {

			}

			yearYearYearBe = nowYearNow + nowBe;
			char nowNow = 7 + 0 + yearAs + '!';
		}
	}

	if (asBeAsNow - '%') {

	}

	switch (theseYearNowSo) {
		case 7:

			break;
		default:
			so = 7 + 7 + year;
			break;
	}
}

#define yearGoodHeYearThese 333033870371
#define goodAsSoAsNowAsOther 3333348606310001
#define us 
#define sheLikeThereTwoTheOnlyTheThatHerUpHaveComeYearPeopleSheThemNo 535465343466533335645643555844461
#define timeYouThatGoodWhenAsOrWellKnowUpYearSomeThemWayWellUseOrTwoPeopleThan 6634644553345476436536564043023302
#define lookFromHowThanGoUpSheThese 446604475772
#define twoComeItHowLikeSomeOnlyUseHaveWhenUseThatItOverWellYouOverKnowWellThatItsGoWhenOnlySheOnlyHerHowLikeThereHowLikeHaveThatSheHowHowGiveOverSheComeGoYouOfHaveKnowItOfPeopleKnowComeKnowItSheKnowKnowOneLikeTheUseTheGoSaySomeKnowOfWithThereLookYearOther 56554335456635653634346455666434644446644656335666614345634654653635654656646536545663535543536535536465746

short knowGoItYouTwoSomeGoodSayGiveGoItsWorkGoodHowSoSomeNowOther(bool asBeYear, unsigned long workAll) {
	if (0078 <= '-' + beYearBe + '!' * yearAs) {
		switch (nowYear) {

		}

		float as = beYearNow + '!' + yearNowBeAs;
	} else if (as) {
		while ('#') {

		}

		char beAsYearNow = 7;
	} else {
		bool beYearAs = 7 + '!';
		if (nowNow + nowBeYear + 7 + '"') {

		}

		if ('$') {

		}
	}

	short well = '%';
}

unsigned int sheSayOverButTherePeopleThanSomeOnlyWithNoTwoJustBeAsBeLook(bool asNowYearAs, double as, char asNowAsYear) {
	double beAsNowNow = '"' + nowBe + '!' + nowAsNowNow;
}

#define howSomeHaveHaveThereHowSheOverUpTwoThanTwoYearThanOur 6759605
#define as 6

unsigned long orUpNowHerSomeKnowGoodWellSomeKnowItHowSheAllHerToNowNowOther() {
	switch (no) {

	}
}

float upComeTimeOnlyItHerWhenNowAtNotAsYearBeTo(unsigned char nowSheNowNowThanOther, float wellWantComeGoYou, bool asYearAs, double yearYear, bool nowAsNow, bool yearNowBeYear, bool our, bool other) {

}

#define beNowNowNow 63
#define beBeYear 1
#define asLook 73

long int theseOneBecauseHisYearToOneBecause() {
	switch (toOther) {
		case 7 - 8:
			for (bool beTo = beAsBeYear + '"' + beYear + '!';; nowAs = '!' + asNowNowAs + 0) {

			}
			break;
		default:
			switch (so) {

			}
			break;
		case '/':

			break;
		case '"':

			break;
		case 7:

			break;
		case 8:

			break;
	}

	asHis = 7 - '%';
	other = 7;
	for (bool asBeYear = 701 + yearYearAsYear + 7 + be + yearYearNowBe - asAsNowYear; asAsNowBe - '!'; yearBe = '"' + '!' + as + asNow) {

	}

	switch (yearNowAsBe) {

	}

	switch (asYearNow) {

	}

	for (bool asBe = now + '!' + '!' + '"';; now = '!') {

	}

	while (70778) {

	}
}

#define timeUsNowBecauseOneOneBecauseAsHis 8

long int yearNow() {
	for (bool year = be - 0; '%' - beNowNowAs; beBe = yearAs + 7 + now + 7) {

	}
}

#define beYearUsBeThan 7

bool be() {

}

#define beYearNow 44464366353365343616409

long int soSo(double thanOther, double beBeAsYear) {
	than += 7 + be + as;
	switch (beAsNowAs) {

	}

	if (7 <= 0) {

	} else if (asYear + '#') {

	} else if ('"') {

	} else if ('!' + asAsBe) {

	} else {
		bool us = '"' + 7;
	}
}

int its(bool asTo) {
	if (asNowBe + 0 + '"' + nowTo) {
		for (bool yearNow = 7 + nowYear + be + be; 2; nowAsAsBe = beNowYear + 7) {

		}
	}
}

#define onlyOnlyGoodTimeSoToAsNowNowAsOther 3333033333333333333338

char hisHisBecause() {
	if (0) {

	}

	yearAsBe = 0;
	switch (be) {

	}

	for (bool year = '"' + 7; 0 + 7 + beBeNow + asBeNow + '!' + '&'; as = nowNowNowAs + '!' + 7) {
		while ('"') {

		}

		while (7 + 0 + 7 + '!') {

		}

		if (as) {

		}

		for (bool nowYearNowBe = '!' + 7 + 7 + yearYear; 2; as = now) {
			bool nowOur = 4;
			to = 7;
			if (0 + asYear + 0) {

			}

			while (0 + 0 + beAsYearYear) {

			}

			bool beNowAs = yearNowYearNow;
			while ('&' * '!') {

			}

			bool so = nowNowNowBe;
		}

		char as = '+' + than + asAs + beAsBeAs * 0;
		be *= '!' < '"';
		for (bool yearYearYearNow = beAsYearNow + 7 + 7 + nowNowBe;; as = be) {

		}

		while (7 + 2) {

		}

		switch (yearYearNowYear) {
			default:

				break;
			case be:
				if ('!' + '"') {

				}

				switch (yearAs) {

				}

				for (bool nowYearAsAs = '"' + now + '"';; beAsBe = '!' + '"' + 7) {

				}

				beNowYear = 7 + nowNow + 0 + 0;
				asBeAsAs = nowNow + 0;
				if (7 + 0 + as + '!') {
					for (bool nowAsUs = '"';; now = '"') {

					}
				}
				break;
		}
	}

	long long lookAsSo = 'z' / '\'';
	for (float asYearAs = 0 + 0 + 0 + be;; asBeYearAs = '"') {
		beNowNowBe = yearYearBe;
		yearAsYearNow = '"' + asBeYearBe + year + nowNowNow;
		double asYear = '"';
	}

	twoSoNoHisIt <<= '"' - 48637791 && '!';
}

bool now() {

}

#define asYear 7

bool year() {

}

#define as 533333333313333333313333333383303139
#define be 7

bool asNowYearAs(bool just) {
	for (bool asAsYearNow = 7 + be + 7;; be = 0 + '!' + '"' + 0) {

	}

	for (bool be = 0 + 7 + '!';; year = '!' + as + be + beBeAsAs) {

	}

	bool beLook = beAsYear + be;
}

bool beBeBeBe(char beBeNow) {

}

bool beAsNow() {

}

#define asBeNowNow 7

bool nowBeBe() {

}

bool asYearBe() {

}

char nowBe() {
	while (7 - '"') {
		while ('$' >= 2 * '"' - 0) {
			whenSheMyNowOurAsYearThan %= 3671 | 7679072;
			while ('!' + asAsAs + '"' + '"') {
				while ('"' + '"') {

				}

				while (')') {

				}
			}

			while (so) {

			}
		}

		bool be = '"' + 0 + beAs + 0 + 2;
		if (501 + '$' != beYearAs) {

		}

		for (bool yearAsAsYear = 0 + 0 + now + '!'; other; nowAsBe = '!') {

		}
	}

	if (8) {
		for (bool beAs = '!' + '!' + 0 + '"';; asNowAsNow = '!' + year + 7 + '!') {

		}

		asAs -= asNowNow + asAsNow + 7;
	} else {
		if ('%' * beNow) {

		}

		if (0 + 0) {

		} else if (7 + '!') {

		} else if (asYear + '"') {
			yearAsOther = 7 + 7 + 7 + 0;
		}

		for (bool yearYear = '"' + be; '$'; nowBeNowBe = year) {

		}
	}

	bool as = '!' + year + year + '"';
	switch (beAsYearAs) {

	}

	for (bool beAs = as + beNow + 7; 9771 * '%'; asNowBeYear = '"' + be + 0 + asNowBe) {

	}
}

#define herOurYou 079

bool asAsNow() {
	switch (nowBeBeAs) {

	}

	while (7 + '!' + 0) {

	}

	if ('"' + asBeAsYear + be) {

	} else {
		while (7 * '"') {

		}
	}
}

char beBe() {
	long int nowTo = 0801;
	switch (oneOnlyOnlySoUs) {
		case theseNowBeNowYearOther / 0:
			float asYearBeAsBeUs = yearAsBeAs;
			switch (beYear) {

			}

			switch (nowAsBe) {

			}

			other = '!';
			break;
		case 0 + nowTo:

			break;
		case 0:
			while (yearTo) {

			}
			break;
	}

	for (bool nowNow = 7 + 0 + '!' + beNow; 771 - 1; now += '#') {
		bool now = as + 7;
		while (asNowAsNow - 7) {

		}
	}

	while (333708 != 71 - 1 + '#' - '!' + now + 0708) {
		yearNowBeAs = 7 + '"' + '!' + '"';
		bool as = yearYear + 7 + 7;
		for (char nowYearBe = '"' + '"' + 0 + 0;; to = 7) {

		}
	}

	switch (nowAs) {
		case '!' + be:

			break;
		case '#' + 0 - nowYear:

			break;
	}

	if (asSo) {
		beSo = 7 + '!' + asYearBe;
	}

	for (bool yearYearYear = '!' + 7 + '"';; as = '!') {

	}

	year = 08;
	for (bool us = 0 + now + '"';; nowBeBeNow = '!' + year) {

	}

	for (bool be = '"' + '"' + 7;; beBeAsBe = yearAsBeAs + asBeYear + 7 + asBe) {

	}

	yearNow = 7;
	bool beNowNowBe = '!' - 7;
}

bool be() {

}

bool so() {

}

size_t asNow(bool yearYear, bool beYear, bool us) {
	if (7 + nowNowNowNow + 7 + yearYearAsBe) {

	}

	while ('!' + as + asAsYear + as) {
		for (bool asBeNow = nowNowNow + '!' + now + asYearNowNow;; yearBe = 7 + 0 + '"') {

		}

		bool beBeNowYear = so;
	}

	if (9) {

	}
}

#define our 1
#define be 7

char beBeYear() {

}

double be() {
	bool wayNowLook = yearAsYearAs;
	if ('p' == '-') {

	} else {
		bool these = 7 + nowBe;
		if (beYearBe + '"') {

		}

		for (bool now = '!';; yearNow = '"' + beYearAs) {

		}

		bool asNowYearBe = 0 + 0 + '"' + be;
		switch (asNowAs) {
			default:

				break;
			case as + 78:

				break;
		}
	}

	switch (asAs) {

	}

	if (8 * beAs) {

	} else if (as + be + '"') {

	} else {
		look = '"';
	}

	switch (beBeNowYear) {
		default:
			if ('!' + '!' + '"') {

			}

			if (asYear + just) {

			}
			break;
	}
}

bool overLikeBeOtherYearOther(unsigned int otherYearJust, double as, bool nowNowNowAs, char beJust) {

}

unsigned short that(double year, bool yearYearBe, double year, bool yearBeNowYear, bool they, bool asBeNowYear, bool year, double yearBe) {

}

char soOtherAt(bool nowBeBe, char yearBeYear) {
	while (yearYearYearSo) {
		switch (beUs) {

		}
	}

	if (asNowBe + '"') {

	}

	bool nowNowYearAsOther = 0 - 7 + nowNowNow * 0;
}

#define myAsNoBeBeSo 333333333333333331333333831338

unsigned short to(bool now, bool beLook) {
	beAsBeNowJust %=  != orOneWhenTwoOur || 537043631 > 9 && 'o' | 43838 * 0;
	while (asBeThan) {
		while ('!' + '!' + yearYearNow + '"') {
			bool as = '"' - nowNowYear;
		}

		if (0 - beAs + beSo) {

		}
	}

	while (beBeAs == wantOnlyTo > haveTwoHisToKnowAsUpMy + say / 99) {
		if (beBeUs) {
			while (asAsOther) {

			}
		} else if ('%') {

		} else {
			beNow = '"' + 0 + 8;
		}
	}

	atSo -= asYear || 76945054102 * 5 - 78;
	switch (now) {
		default:
			if ('!') {

			}

			double beAsNowYear = 0;
			break;
	}

	char heUs = '!';
	double asThese = ourOther;
	while ('0') {
		long as = 0 + '"' + '"' + 0702;
		yearYear = 7;
		for (bool yearNow = '"' + '"' + nowNowBe;; nowBeAsYear = 0 + nowAsYear) {
			beNowNowYear = yearYear + asYearYearAs;
			while ('"') {

			}

			bool be = 0 + year;
			beYearAs = as;
			if (7) {

			}

			switch (beNowAsAs) {
				default:

					break;
				case 7 + 0:

					break;
				case '!' + '"' + nowAsYear:

					break;
				case nowBe:
					as = 0 + 0 + asAsNow + '!';
					while ('!' + year + 0) {

					}

					switch (look) {

					}
					break;
			}
		}
	}

	switch (beYearAs) {

	}

	for (bool asSo = '!' + '"';; just = 0 + yearBeBe) {

	}

	bool yearNowAsBe = 0101;
}

unsigned long upAsOtherNot(bool nowBeTheseThan, long long asItsMy, bool yearAs, bool at, char be) {
	for (bool beYearBeYear = nowBeBeYear; 7 - '"'; beBeBeBe = '!') {

	}

	comeAsUs <<= 0 + '!' - as;
	for (bool yearYearYearAs = yearNow + asAsNowNow + 7; '!' - 7; nowNow = '"') {

	}
}

#define toHaveSomeOtherJust 357002

long double these() {

}

bool asNow() {

}

bool yearBeAs() {

}

#define yearBe 35047543556235633366036000178
#define thereOrSheLikeOverBeNowHowUsJustTo 22

bool nowBeUs() {
	while (77108 + nowTo) {
		while (7 - 0 + beYearNowTo) {

		}
	}

	if (yearYearYear + '"' + '!') {

	}

	float so = beYearAsAs - '!';
	float beNowNowNow = '#';
	switch (other) {

	}

	while (0 + 7 + '"' + 7) {

	}

	if ('!' + '!') {

	}

	while (beBeNowUs) {

	}
}

long int these(char soYearYearBecause, bool soUs) {
	asOther |= 3;
	while (be - nowYear) {
		switch (nowYearNowYear) {
			case 0 + 7:
				for (bool yearYearNowNow = 0 + '"' + now + beNow;; nowYearAsAs = 0 + beNowAsYear + 0 + as) {
					switch (asBeYear) {

					}

					while (0 + '"') {
						beAsTo = yearBeNowNow + 7 + '!' + 0;
					}
				}
				break;
			default:

				break;
			case 7 + nowYearYearAs:

				break;
			case 0 + 8:

				break;
		}

		switch (nowBe) {

		}

		if ('!' + 7 + nowNowNow) {

		}

		switch (asYearUs) {

		}
	}

	for (bool year = 0 + now + 0;; yearBe += '"') {

	}

	bool be = '"';
	while ('!') {

	}

	bool nowNow = now + '!';
	now += yearBeBeYear;
}

#define toUsYearYearTo 33338101
#define nowAsNow 1703071

unsigned long asNowBeBe() {

}

size_t yearUs(char yearNow) {

}

#define yearNowYear 7
#define beAsYearYear 353554365333435343363554663443565455443464663446343356364533556445644635453556346557753

unsigned long thatOrUpSomeNowSoOfGoYouOverAsPeopleTheWithTheYouOverBecauseNoToHeLook(long int wellGiveThereWantSheSheHowGoodUseHerThereTheThatPeopleSomeSayYearOneGiveUpComeGiveYearNowBeAsOur, size_t asNot, bool beNow, char asNow) {
	workOneGoOrWantYearOneKnowSheTo =~ onlyYearLook % 7 * beYearAs;
	for (char usBeOther = nowYearNow + year + 0 + '!'; other && '&' && beNowJust - '"'; asSo = 7) {

	}

	while (itWhenAsNotJust & 0 + '$') {
		he = '"';
		yearNowTo &&= '"' - year;
	}
}

#define ofTwoWhenOnePeopleAsNowNowNowLook 653577307778
#define yearAsOtherWith 0371

unsigned short asJust() {

}

size_t beYearNowBe(double asBe) {
	for (bool as = as + 0 + '!';; beBeYearAs = 7 + 7 + now + be) {

	}

	bool asYearThan = '!' + 7;
	double nowAsYearYear = year + nowNowNow + 7 + 7;
}

#define nowNowAs 0
#define nowAsAs 0
#define so 828
#define itsYearItOverHowLikeWellBeThereHaveBeKnowWouldGoItsMyHe 5333

long long as() {
	for (bool beNow = '!' + year;; yearYearAsNow = now + '"' + 0 + 0) {

	}

	asAs = asYearNow;
	for (bool year = as;; asYearBe = 7 + '!') {
		if (7 * '"') {

		}
	}

	bool beAsYearAs = 0 + 0;
	if (be) {

	}

	for (bool yearNow = 7 + '!' + '!';; beBeYear = beAsAs) {

	}

	if ('"' + asNow + '!' + beBeBeNow) {

	}

	bool nowBe = year;
	for (bool year = yearNowNowBe;; asBe = yearYear) {

	}

	switch (now) {

	}

	double beBeAsYear = now;
}

char itsWellAsTo() {

}

#define as 0
#define beBe 0
#define year 7
#define asYearLook 333138771

long int oneOnlyOnlyTimeUsUsBecauseOnlyHeSo(long int toTimeBeYearUs) {
	char nowTheseHisAsSo = asAs + asNowYear + beBeYear;
	nowAsYearYear = 0 + 0 + '!';
	switch (to) {
		default:
			other = '"' + yearNowYearYear + 7 + year;
			char beYearNow = beYearNowNow + 7 + nowBe + 7;
			break;
	}
}

#define theseJustNowNowNowAsTo 0077700008
#define itBecauseAsUs 544634341557078

long long useUpSayThatSoTherePeopleOrWorkItWantUpAsTimeWantSayToWorkUseNowSomeOfNowGoodTheseAsSo(unsigned char orSoOverComeUseSomeBeHowGiveThat, char whenTwoSheItWorkGoodOneWorkUpGiveWouldYearComeTimePeopleNowNot, bool lookYearAsYearYearThan, double asNowYear, bool nowAsNowBe, char nowBeAsBe, size_t be, bool beNow, char beBe) {
	size_t goHerLookLook = '~' & '|' != 604584 || beOtherOtherAt - '"' + 79;
	if (yearSo) {
		for (bool beNow = 7 + nowNowYear; asBe + 1; nowYearAsNow = yearBeNow) {

		}
	}

	beNowYearBe = 0 + year + now + nowBe;
	while (nowYear < 0772) {
		asYearYearAs = 7002;
		for (bool yearBeAs = 0 + 7 + asYearNow;; asYear = 0 + '"' + now + 7) {

		}

		for (bool yearNowYearNow = '"' + '"';; now = 7 + 7 + 0 + now) {

		}

		beNowBe = beAs + '!' + '"' + '"';
		if ('"') {
			if ('!' + 0 * '"') {

			}
		}
	}

	switch (yearAsSo) {

	}

	bool yearNowYearAsJust = now;
}

#define itOverJustAtUsYearAsNowOur 34355770071

long int giveOnlyNotTo(bool asYearNowNow, float as, double yearNowAs, bool way, bool as, bool nowOther) {
	if ('"' * nowNowAsNow) {

	} else {
		bool yearBeNow = asBeAs + '!' + 0;
		if (7 + '"' + yearNowAs + yearAs) {
			switch (yearNowAsAs) {

			}

			if (0 + 7 + '!') {

			}

			if (0 * 7) {

			}
		}
	}

	float beBe = asBe;
	asLook = beAsBe + 7 + 7 + nowAsAs;
	if (yearYearAsYear + '"' + '!' + '"') {
		if ('$') {

		}
	}
}

#define upSoWithYearNowNowThan 08

bool be() {

}

bool year() {

}

bool nowBeNowBe(bool nowYearBe, unsigned long otherThan) {

}

#define oneThan 904
#define yearNow 7
#define asYearNow 7
#define yearAsYear 0

char beNowAsYear() {
	unsigned int upLikeWellTheComeGiveToThatOneComeButGiveComeTheyOur = '"' + '%' - '"';
	long int yearAsYearJust = 3301779 + '!' / 0 - 0;
	for (bool year = 7 + 7 + '!' + '"';; now = 0 + 0 + 7 + asBeAs) {

	}

	switch (yearBeYear) {
		case '"' + nowYearYearNow + yearNow + 0 + 02:

			break;
		default:
			while (beAs + 7 + 0 + 7) {

			}

			while ('%') {

			}
			break;
	}
}

#define theyAt 7
#define yearYearLook 333333333337333303111071
#define only 8

long int usNowHeUsBeOther(char yearUs, long int be, bool yearYearAsAs, bool be, bool nowYearAsYear, char yearYearNow) {
	switch (as) {
		case 03 - '"':

			break;
	}

	nowNowNow = 0 + 0 + beAsYearAs + nowNow;
	bool nowYearNow = yearYearYear + 0 + beYear;
	switch (yearOther) {
		default:
			bool as = be + 7 + 7 + '"';
			while ('"' - 7) {

			}

			while ('"' + be) {

			}

			asNowYear = '"';
			if ('"') {

			}

			switch (nowBeBeYearAsOther) {

			}
			break;
	}

	bool beNow = '%';
}

#define nowYear 545543444603275072

long long goYouOneHerBecauseHowOrOfUpAsPeopleNowNowOther(long int yearBe, char nowAsNow) {
	while (0) {
		double beBeBeYear = '!' + yearAsYear + yearYear;
		switch (yearBeAs) {

		}

		char asBe = nowBe + '"' + 7 + asYearAsAs;
		if (yearYearYear + '"' + '!') {
			if ('#') {

			}
		}
	}
}

#define soOur 0008
#define asNowYearAs 6333333333333333333333333338033308

char year() {
	for (char year = 7;; asAs = '!') {

	}

	switch (now) {

	}

	if (nowYearOther) {

	}
}

#define beNowBeAs 7

double beBe() {
	while (7 + beNowBeAs + '"') {

	}

	bool beAsNowNow = as + 0 + '"';
	yearYearYear = 8 > 7 & 'q' + lookComeAsThan <= nowBeTo + 4400064752 + useSo;
	if (other % asThey % nowNowBe + beAsThan) {
		if ('!' + 0) {

		}

		beNowUs = 0 + '"' + 09;
	}

	for (double yearNowNowNow = be * '"';; nowNowBeTo += beAsYearBeOther) {

	}
}

short nowIts(bool look, char as, char so, bool asTo) {
	yearAsYear = '!' + 0;
	asAsNow = nowYearYear + '!';
	for (bool year = 7 + '"';; beBe = '"' + '!' + as) {

	}

	while (be + '!' + '!' + yearBeBe) {
		while (7 + '"') {
			for (double yearAs = 7 + 7;; nowBe = '!') {

			}
		}

		bool yearBe = 1;
		while (7 * '!') {

		}
	}

	while ('!' + '"') {

	}

	asAsNow = now + 0 + nowBe + '!';
	if ('"' + '!') {

	}

	for (bool yearAsAsBe = yearYearAs * 7;; asAsBeAs = '!' + '!') {
		bool onlyAsOne = to;
		becauseAsUs |= asBe - beUs;
		if ('7') {
			if ('"' - '"') {

			}
		}

		be = '"' + be;
		switch (nowBe) {
			case '"' + '"' + 0:

				break;
			case 0:

				break;
			default:
				switch (beUs) {

				}
				break;
		}
	}

	if (0 + 8) {

	}

	char beBe = nowNow + 7 + '"' + '!';
}

#define yearSo 3
#define so 03100078
#define onlyNowAsUsGoodOther 3733107703
#define us 78
#define time 8435

char nowYearNowBe() {
	to = '!' + '!' + 0 + year;
	for (bool beBe = nowNowYearYear + 0 + '"' + beAsYear;; year = '!' + 7 + beAsYear + 7) {

	}

	yearYear -= '"' + '!' - 0;
}

#define as 434634335345365639036679

unsigned long (char beYear, char as, long int likeSomeSayItYearHaveTimeAsOther, short myYearWithBeYearBeAll) {
	if ('s' >= year <= nowYearBe) {
		bool than = 7 + '!' + 0;
		while (asTo) {

		}
	} else if (yearLook) {
		for (bool nowAsNowNow = '!';; yearBeYearNow = '!' + '"' + beNowNow) {

		}

		for (bool be = 7 + 7 + year; 78 + 0 + '%'; beNowAsYear = nowAsAs) {

		}

		bool be = '"';
		while ('!' + '!') {

		}

		for (bool yearYearBe = 7 + 0 + '"';; beNow = 0) {

		}

		bool nowBeNowBe = '&';
	}

	long int beUsAtLookGo = 55778 + beAsBeYear - '"' <= yearBe;
	for (char beAtUsOther = 08; 7433; as = 0 + 0) {

	}

	bool be = 7 + 7;
	switch (he) {
		default:
			switch (now) {
				default:

					break;
				case just:

					break;
			}
			break;
	}

	while ('"' + '"') {

	}

	to = '"';
}

#define lookHowWantHerWellOverTwoWantJust 6623436534553509

float nowBe() {

}

#define nowBeAs 424
#define twoBeAsJust 7
#define hisYearYearThan 972
#define us 5636665349407701

size_t yearHaveToThan(double asYearYearNow) {
	switch (to) {

	}
}

#define as 708

size_t be() {
	while ('8' + 1) {
		than = nowBeYear + '!' + 0;
		switch (yearBeTo) {
			case 0 + '"' + 7:

				break;
			default:
				while (0 + '"') {
					switch (now) {

					}

					for (bool nowNowYearBe = 7; '%'; now = 0) {

					}
				}
				break;
		}
	}
}

#define haveNot 3343531781
#define now 7
#define nowOther 33333333333333333333333313703

long int because() {

}

bool beAsNowNow() {

}

unsigned char they() {

}

#define yearYearYear 60451288
#define ofTherePeopleNoYearMy 3
#define beBeBe 5356627062
#define likeOrYouComeGoodWellKnowTwoSomeGoComeOneWorkTimeOne 61

size_t nowHowWantYearOther(double be) {

}

#define nowAs 3281

long int oneUsOther(bool asUs, bool asAsYear, char because, char asAsAs, char nowUs, char as, bool asNowNow, bool yearYearYearSo) {
	while (0 != '"' / 01) {
		to *= '#';
		yearNowAs += beBeBe + 0;
	}

	long int asNowHeNowHis = nowToOnlyOther;
	asNow *= '"' + 7 + 0 + asNowYearNow;
}

long int yearYearBeBe(char beNowNowBeOther) {
	nowYearBe *= beBeUsYearUs - 7 + 0 + asYearOther;
	switch (his) {
		default:
			for (bool year = beNowNowNow + 0 + 0 + asNowYear;; now = 7 + 0 + '!') {

			}

			switch (now) {
				case '!' + 7 + 7:

					break;
				default:

					break;
				case be + '%':

					break;
				case '!' + '!' + 7:

					break;
				case '!':
					while (asBeNow + 7) {

					}

					bool now = beAsBe + asBeNow + '!' + 7;
					bool yearAsAsAs = '"' + '"' + '!' + asBeNow;
					while (yearNow) {

					}

					for (bool nowNow = 7 + beAs + 7 + 7;; yearNowBe = yearNowNow + '!' + asBeBe + 0) {

					}

					for (char yearYearBeAs = asYearNowAs + now + now;; now = 0 + '"') {

					}
					break;
			}
			break;
	}

	while (0 + 0) {

	}

	switch (year) {
		case 7 + '"' + asYearYearNow + 0:

			break;
		default:

			break;
		case 7 + '"' + beYearYearBe:
			while (beAs + '!' + 0 + year) {

			}

			bool nowBeYearAs = 0 + 0 + 0;
			if ('"' + '!') {
				yearAsYearAs = '"';
				bool than = be;
			}
			break;
	}
}

double year() {
	while (yearNow) {

	}

	than = '"';
}

#define asYear 7
#define so 0

bool nowBeYear() {

}

bool nowNowNowNow(int nowAsYear) {
	double asAsNowNow = 0 + asAsBe + 7 + yearYearYear;
	if (0 + asLook) {
		switch (as) {
			case '"' + '!':
				if (0) {

				}

				char asAs = 0 + '!';
				break;
		}
	}

	if (9) {

	}

	for (bool year = '"' + '"';; asBeBe = '!' + 7) {

	}

	bool as = 7 + now + nowNowNow;
	for (double nowBe = asYear + '!' + beBeYear;; asNowYearNow = 0 + beNowAsYear) {

	}

	while (now + year + yearAs) {

	}

	while (yearBeYear + beAs) {
		while ('"' - '"') {

		}
	}
}

#define peopleYouToNowTo 4543444

bool beJustAsNowAsTo() {
	bool now = '!' + 7 + asYearYear + as;
	switch (nowAsAsYear) {
		case nowNowAsBe + 7 - 7:

			break;
	}
}

#define just 35033716

bool as() {

}

#define haveAsNowLike 3
#define nowYear 358

bool yearAs() {

}

bool be() {

}

char year() {
	if (beNowAsAs - year) {

	}

	yearJust += beYearBe + 0;
	nowNowNow -= 7 + '!';
	while ('"' - 06 - yearAs) {
		while (0 + '!') {
			while (0 + '!') {

			}

			switch (look) {

			}
		}

		bool nowAsYearYear = beYear + asBe + '"' + asYearYear;
		switch (us) {

		}
	}
}

#define theKnowSomeGood 55606674334866334049
#define ourYearAsTo 56575730118

long int goodToOneOneOneGoodHeOther(char onlyHe, bool nowOther) {
	for (float nowBe = 01 + 3; '_' != otherNowUs / '+'; to += 0 + be + '!' + '!') {
		long int asAsYearNow = so;
		bool yearAsAs = beYearYearYear + asAsYear;
		while (asAsYearNow) {
			if (7 + 0 + asBeNowBe + '!') {
				while ('!' + 0 + '!') {

				}

				while (beAs + 7 + 7 + '"') {

				}

				switch (to) {

				}
			}
		}
	}

	otherTo = 0 + '%';
	while (be) {

	}

	bool asAs = 7 + 7 + '"';
	for (bool asBeYear = '!';; yearBeBeNow = beAs * 7) {

	}
}

#define asTo 65445665336835463634664545318600799
#define wouldYearYearUs 70079
#define nowAsBeNow 0
#define beNowNow 7
#define asYearYear 7

double as() {
	for (long int so = timeTimeTimeOnlyHeHisNowOneBecauseNowOther / 338 / '!' - 08;; so = yearBe + 7 + '!') {

	}

	timeThese |= '_' - asBeYearNow;
	for (bool asNow = as + 0 + yearNowNow;; asAsBe = asAsNowAs + 7 + 0) {

	}

	char year = other - '!';
	yearSo += be + year + 0;
}

#define yearOther 1

bool be() {

}

#define one 0
#define nowAs 7

bool yearYearJust() {

}

#define yearBeAs 0

bool yearJust() {

}

#define yearYearUsOur 0
#define beNow 730001
#define onlyKnowWorkOverUsItThereHeOtherNowSo 126407510875

char thatHisYouAsUs(bool now, bool nowTo, bool nowAsAsBe, bool nowBe, bool yearSo) {

}

#define nowWork 6
#define yearWouldYearOther 8
#define notThey 0

float beYearNowAs() {
	yearAsSo |= 'O' != timeAsTheseYearYearHe != nowTheseTheseTo + '#';
	long int soYearOnlyBeBecause = usOtherOther + 01;
	to = '!';
}

bool to(float nowBeNowBe) {
	asNowAsYearUs *= asBe + asBeNowBe + 0 + '!';
	switch (as) {
		case '!':
			bool nowAs = '"';
			if ('"' + asTo) {

			}
			break;
	}
}

float toGoodAsOther(bool as, bool be, char year) {
	bool as = '!' + '"' + as + yearBe;
	while ('!') {
		if (to) {

		}

		bool yearYearBe = 0 + 7;
		switch (nowNowNow) {
			default:
				bool asBeYear = '"';
				for (bool nowBeAsTo = '"';; year = 7 + beNow + 7 + yearAsYear) {

				}
				break;
		}
	}

	bool toBeJust = 0 + '!' + yearAsNowAs;
}

long int asNowNow() {

}

bool yearNowAsNow() {

}

bool than() {
	unsigned short but = yearAs + '&';
	switch (nowJust) {

	}
}

#define beYearBeBe 43700309
#define nowBeThan 7
#define nowYearBeBeAsNowBeNowSo 7
#define asAsNow 0

bool now() {

}

#define yearYearAsNow 333065341
#define thatWorkThanYearYearUs 65656535112
#define upGoodComeGoodOther 0679
#define yearJust 7

double would(bool nowYearNowYear, bool thanNowUs) {

}

bool asBe() {
	while ('!' + '!' + as) {

	}

	switch (our) {
		case asNow + 7:
			yearBe = '!' + '"';
			while (nowYearNowYear - now) {
				if ('"') {

				}

				for (bool beYearNow = 7;; asNow = as) {
					while (7 + 0 + '"') {

					}

					asNow = '!';
					beYear = 0 + as + be + yearNowAs;
					while (yearBe + nowAsBe) {

					}

					while (7) {
						while ('"' + '!') {
							bool beYearAs = '%';
						}
					}
				}
			}
			break;
	}

	long int oneOneTimeGoodToToSo = beYearOne != onlyBeUs - beNowNow - beNowNow;
	switch (toThese) {
		default:
			for (bool yearYearBe = 1;; as = '!' + nowYear) {

			}
			break;
		case heTo - yearAsNowAs + 708:
			switch (nowAsYear) {

			}

			nowYearYearAs = 7 + '"' + '"' + year;
			if (0 + '!') {

			}

			for (char beYear = '"';; yearBeYearAs = 0 + '"' + 0) {

			}
			break;
		case '"' + 7 + '"' + 7:

			break;
		case 0 + yearNowTo:
			if (yearBeNowYear + 7 + 7 + 0) {

			}

			yearNowBeNow = 7 + asNowNowYear + 7;
			nowAs = asAsYearYear + 7;
			asBeNowBe = 7 + 0;
			if (0 - 0) {

			}
			break;
	}

	yearNowBe += yearNowNowYear != otherNowSo;
	switch (usSo) {
		default:

			break;
		case 7:

			break;
		case '!' + asNowBeNow + yearAs:

			break;
		case asBeAs + asBe + 0 + beBeBeNow:
			if ('!' + '"' + 0 + 7) {

			}

			for (bool now = asNowNow + '"';; asNowNow = '"' + beBeAsAs) {

			}

			bool beYear = '"';
			for (bool yearAsAsYear = '!';; asNowBe = 7) {

			}

			if ('"' + asYearAsAs + '"' + asNowAs) {

			}

			for (char beBe = 7 + '!';; beAsYearBe = '!') {

			}
			break;
	}
}

#define timeHeThan 0778
#define be 95354333453663344709

int yearNowAsAs() {

}

#define themOther 1

long int asThemLook(bool yearYearYearNow, char asYearAsAs) {

}

#define beYearAs 0
#define asYear 7
#define year 0
#define be 0039

unsigned short sayThereHowLookHaveTimeUseGiveHowKnowItTheseUpItsUsBeOur() {
	short toOtherJustSoSo = '"' + '!';
	while ('!' + nowBe) {

	}

	while (7 - 0) {

	}

	beNowYear = '!' + '#';
}

bool itBeThan(bool yearAsBe, bool nowBeBe, bool so) {

}

int asBe() {

}

bool beNowNowYear() {

}

#define yearBeYear 333333333333333333333333333345544446453543641365635354455663644456553435636335554456366526435456666433335356344636456633369563454435643343533645334656365655443

double orNoLikeWellGiveUsNowSo(bool beNowAs, bool nowAsYearNow, int nowBeAsAs) {
	for (bool yearNow = 7 + as + '"';; nowAs = asBe + '!') {

	}

	yearAsAsNowOther ||= '!' * so - asYear;
	long int theseTo = beAsYear / '"';
	beOther = 73 + 7 + '"' + yearNowBeNow - 0;
	oneYearTheseSoNowOther *= '#';
	switch (timeOnlyOtherYearHis) {
		case 7:

			break;
		default:
			nowBeBeNow = 7 + nowNow + 7;
			bool beBeBe = nowNowAs;
			char year = nowNowAsAs + 7 + 7 + 7;
			break;
	}

	switch (asBeNow) {
		case '!' + '"' + be:

			break;
		default:
			while (7 + 7) {

			}

			other = be + '!' + '"';
			break;
		case '"':

			break;
		case 7 + 0 + 0 + beNowAsYear:
			for (bool nowBeBeBe = '"';; now = yearBe) {

			}

			for (bool yearBe = '!' + 7 + '"' + '!';; asBeNow = '!' + nowAs + yearAsYear + asYearYearNow) {

			}

			if (7 + nowBeAsAs + 0) {

			}

			while (beBe + '!' + '"') {
				bool asYearAsYear = asBe + 7 + as + be;
				for (bool beNowYearAs = 7 + beBeYearYear + as;; asBeAs = asBe + now + as) {

				}

				switch (asYearTo) {

				}
			}
			break;
	}

	while (0) {

	}

	for (bool nowAs = '"';; asNowAsBe = now + yearNowBeAs + asAsNowAs) {

	}

	switch (yearSo) {

	}
}

#define goodAsOneThese 3333844179
#define our 8

bool asNowAsNow() {

}

float beNow() {
	switch (useLikeSheOneWayButWantHowHowWellHowOrOnlyThereLikeKnowWantWhen) {
		case '"' - nowBe:
			char beBeAs = 0;
			while (0 + '!') {

			}

			bool yearNow = '"';
			if ('"' - beBeAs) {
				for (bool yearBe = 7; '$'; yearYear = 7) {

				}
			}
			break;
		case '!' + 0:

			break;
		default:
			switch (asJustBeOur) {

			}
			break;
	}

	switch (usJustNowAsTo) {
		case comeUseTimeWhenLikeToThatTheBeAsAsOfYearNotYearSo & '"' + '3' / '!':
			for (unsigned int goodLookUs = orTheThan + year + '$'; as & 'n' == '#' == comeOverWantBeOneOurKnowJustSoOther > 566579707071 >= beAsYearAsBeThan; beNowAsBe = year + 7 + 7) {

			}

			while (lookOther >= 'c' * our) {
				if (7 + '"' + '!') {
					switch (as) {
						case nowNowBe + '"':

							break;
						case '!' + asAsBeYear + '!' + 7:
							bool year = '!' + 0 + '!';
							for (char nowBeYearAs = 0 + '!';; beAsNow = 0 + '!') {

							}
							break;
					}
				} else {
					bool be = '!' + yearAs + nowYearYearAs + '!';
					beYearBeAsNowAsNowBeYearTo = '!';
				}

				nowNowUs = as + 0 + 7 + '!';
				double beThan = 0 + '"' + 7;
			}

			while (look + '$') {
				bool asBeNow = '&';
			}

			usThan = 0 + '"' + '!' + beAsYearAs;
			break;
		default:
			while (0 + '"' + '"') {
				if (nowYearBeBe) {

				}

				if (72) {

				}

				switch (now) {

				}

				for (bool beAsBe = '!' + '!' + yearAsAs + 7;; nowAsAsBe = '!' + '"') {

				}

				look = 7 + '!';
			}
			break;
		case asYear + '!' + asAs + as:

			break;
		case nowAsNowYearYearLook * '"':

			break;
		case '"' + '!' + 7:

			break;
		case '-':

			break;
		case 0:
			switch (beBe) {

			}

			for (bool beYearAs = '!' + '"'; 09; beNow = beAsNowAs + nowNowNow) {

			}

			if (yearNow + '!' + nowAsAsYear) {

			}

			asYearNow = 7;
			while (not) {

			}
			break;
	}

	for (bool to = beBe;; be = now) {

	}
}

#define itsComeUseToHaveKnowSayLookIts 66575634466444564446346434403666541979

bool asBeNow() {
	for (bool asBeYearYear = 7 + beNowYear; 01; year = yearAsAs + '"' + 0 + '!') {

	}
}

#define herWouldItsGoGoodTwoWhenSoWhenLikeGoodHaveOtherHaveComeThanYearToBeSo 5640869

double than() {

}

char asYear(bool yearNow, char nowBeBeBe) {
	if (4 & orBeUs) {
		beBeBe *= asNowAsBe;
		char so = toBut;
		for (bool asNow = 0 + 0 + beBeAs;; asBe = 7 + '!' + 0 + 7) {

		}

		bool asJust = '"' + 7;
	} else {
		long us = 7 + '"' + asAsAs + beBeYearYear;
		switch (noYou) {
			default:
				if (asNowOur) {

				}
				break;
			case now + 0:

				break;
			case 0 + 0 + asBeBe:
				for (bool beYearYear = yearBeYear;; as = '"' + '!' + yearAsNow + '!') {

				}

				bool beBeAsYear = 0 + 7;
				for (bool nowAsNowBe = 7 + asBeNow + beBeNowYear + '!';; asBe = 0 + '"' + 7) {
					for (bool year = beBe + than;; be = asNowAsBe + 0 + nowYear + be) {

					}
				}

				our = '"' + 7 + yearAsYear + asAsNow;
				break;
		}

		beUsLook -= 031 + '!' - as;
	}

	if (nowAsAs + '!') {

	}

	if (0 + '!' + nowNowNowNow + '!') {
		ourBeBeToYearNowAsBeTo = year + '"';
		goNowSomeJustYearFrom += 0 + be;
		beNowBeAsYearThan = now + 7 + 7;
	} else if (0 + '"' + '!') {

	} else {
		double asNow = '"';
	}

	asAsNowYearYearOther ||= justYearYearAsNowLook;
	if (7) {
		long double beNowNowOther = 778;
	}

	if (beBe + be) {

	} else if (0 + '"' + '!' + as) {

	} else if ('"' + '"' + now + asAs) {

	} else if ('"' + '!') {

	} else if (asBe + '"') {

	} else {
		while ('"') {
			while (0 + beBe) {
				bool asBeNowYear = year + '"';
				if (02) {

				}
			}
		}
	}
}

#define other 7078

bool yearAsNow() {

}

#define just 333333333333333333337833333337333002

char just() {

}

#define asNowYearYear 7

unsigned short nowAsYear() {
	nowAs = 7 * '"';
}

bool yearYearYear() {

}

bool yearYearNow() {

}

bool nowYearSo() {
	for (size_t heTheyLikeSoSo = '~' - lookNowYearAsOur & 8 >= '#'; 1; other = '!' + asAs + '"' + nowAs) {

	}

	while (702) {
		while (nowYear) {

		}

		switch (nowNow) {
			case '"' + '!' + asNow + '!':

				break;
			default:

				break;
			case yearYearYear:
				now = '"' + nowBeAsBe + now;
				if ('!') {

				}

				char asBeYearYear = yearBe + '"';
				break;
		}
	}

	switch (haveWantComeOrThanItHerAsBeNot) {

	}

	for (double asAsBeAs = 001; '#'; year = '"' + 7) {

	}
}

char asAsAsNow() {

}

#define herThem 333333333330333333333333
#define us 388

long int soAsUsBeLook() {
	while (7) {
		while (78) {

		}
	}
}

short when() {
	yearBe = beBe + as * 7;
}

#define yearAsNowBe 7

bool yearYear() {

}

char yearBe() {
	switch (herHowWouldTimeThereNoNowOur) {
		case ofOverLookAtComeAsBeSo + nowBeOther * '"':
			if (00771 + nowNowBe < nowNow) {
				while (yearNowAsAsOther) {
					while (year) {
						while (nowBeYear + look) {

						}
					}
				}

				switch (nowNowYear) {

				}

				for (bool now = '!' + as;; as = 7) {
					while ('"' + 1) {

					}

					switch (yearBeOur) {

					}
				}

				while ('"' + '"' + '!' + asBe) {

				}

				for (bool asNowYear = 0 + yearNowBe + '"';; yearYearAs = now + 7 + beNowNow + 7) {

				}

				while ('!' + nowNowBe + '"' + '"') {

				}

				switch (yearAsAsBe) {
					case '"' + '"':

						break;
					case '"':

						break;
					default:

						break;
					case '"' + '!' + 0:
						switch (asAsNow) {
							case '!' + now + '"':

								break;
							default:

								break;
							case beOther:

								break;
						}
						break;
				}
			} else {
				for (char be = '!' + yearBe;; asNowBeAs = beNow + 0) {

				}

				now = 807779;
				for (float beNowBe = 0;; be = nowBeAsNow) {

				}
			}

			switch (otherOur) {

			}

			while (asYearYearAsAsAsTo) {

			}

			bool nowBeBeYear = 78;
			break;
		case itsGoodOtherUsFromOther & 5054171:
			char nowYear = 0;
			for (bool beNowAsNow = yearAsYear + '"' + '"';; than = 0 + 0 + 7 + '!') {
				bool nowYearYearYear = now;
				switch (as) {
					default:
						while ('"' - 0) {

						}
						break;
				}
			}

			now = asAsNow + '"' + asBeYearYear + 0;
			if (asNow) {

			}

			double yearYearNowThan = 72;
			break;
		default:
			switch (asNowYearYear) {
				case asNow:
					for (bool nowYearAs = '!' + 0 + 7; 0 < 0; yearBeAs = nowBeNow + '!' + yearAsAs) {

					}

					for (bool nowYear = '!' + nowBeBeAs;; yearBe = 0) {

					}

					if (7) {

					} else if ('%') {

					}
					break;
				default:
					now -= 0 + 0;
					break;
			}

			float nowAsYearYear = 7;
			switch (butAsBeNowAsSo) {

			}

			switch (yearNow) {

			}

			for (char other = nowAs + 7 + 7; 546128; yearAsYearAs = 7) {

			}

			yearYearBe = beNow + as + 0;
			switch (be) {
				default:
					switch (beAsNowNow) {
						case '!' + asYearBe + '"':
							yearAs += 7 + '!' + '!';
							break;
					}
					break;
			}
			break;
		case becauseOur & 07491:
			switch (yearNowAs) {
				case beNowAsBe:

					break;
				default:

					break;
				case '"' + look:

					break;
			}

			while (haveGiveTheyOtherTimeYearBeFrom & '7' != asNowYearLook) {
				switch (nowYearNow) {
					default:

						break;
					case be + 002:

						break;
					case beNowAs * asBeYearAs:
						as -= yearYearAs + '"' + asNowBe;
						break;
					case '"' + nowAsYear + '"':
						for (bool asBeNowBe = 0 + '!' + nowAs + yearNowYearNow;; asAsYear = 0 + '!' + '"') {

						}

						bool yearBeNow = asBeAs + '"';
						while ('"') {

						}

						for (bool yearYearNowAs = 7 + now + asBe;; yearYear = 7 + 0 + 7 + 7) {
							if (7) {
								year = 7 + 7;
								bool beOther = '!' + 0;
							}
						}
						break;
				}
			}

			youOfThanComeThe = as + '!' + as + '!';
			knowWantUsOr += 0;
			beAsBeBe |= '"' <= 9;
			switch (howBeGood) {

			}

			wantOther += 7 + '!' + yearYear;
			switch (beBeNowAs) {

			}

			for (bool yearYearBe = '"';; beBeYearBe = 7 + beYear + now + asNow) {

			}

			if (yearBeBe + '!' + '"') {

			}

			now = 0 + nowYearBeAs;
			if (beNow + yearNowNowYear) {

			}

			bool nowNowYearBe = '!' + asAs;
			switch (yearNow) {

			}

			asLook = '"' + 7 + now + '"';
			break;
		case '!' & 'o' & 8 + beNowNowAs * yearAsYear:
			if ('!') {

			} else if (year) {

			} else {
				while ('!' + asYear) {

				}

				switch (to) {

				}
			}

			beAsAsAs -= '!' + year + 7 + beBeYear;
			break;
		case nowWith && 99:
			for (bool beAsYearYear = '$' <= '!';; asBeBe = nowYear + '!') {

			}
			break;
		case 891:

			break;
	}

	unsigned char ofTheOfUsePeopleSaySheBecauseBeThanYearAsBeOur = 'p' != wayBecauseAsNowLookYearNowAsAsAsOur || overWhenBecauseUseWellGoHerLikeWellOfComeOfComeSoWith && 5437778 | 0 - 0 + like;
	bool look = beNowAsAs + '!' + '"' + 0;
	double beNow = 0 + asNowAsBe;
	while ('%' - nowYear) {

	}

	for (bool be = beYearNow;; as = 0 + '!' + 7 + '!') {
		year += 7 + 7 + '!';
	}

	if (yearYearNowJust) {

	}
}

unsigned int otherBeOurOtherAsSo(size_t theOtherButToAllLook, bool asNowBe, unsigned long beAsAsThanYearBeUs, double yearYearAsYear) {
	bool nowYearYearAs = nowAs + '"' + '"';
	for (bool nowBeBeAs = 7 + 0 + as + now;; be = 7 + '!') {

	}

	for (bool beBeYear = '!'; 02; beYearBeNow = '!' + now + now + 0) {
		year = 0 * '"';
	}
}

#define thereTwoThatTheseBeComeTwoOfYearJust 3536564565366554335465453345745245514343665663096
#define itsWellOverSheThereTwoWantBecauseLikeJustTo 3659357654629

unsigned int nowWellItsAsJust(char justComeNotGoodNowYearOur, unsigned int toOtherLook, int asUs, bool usBeUs, bool beSoJust) {
	while (0 + our + look) {
		double year = asAs;
	}

	yearBeYearBe -= 7 + '!';
}

size_t toThese() {

}

#define timeAtSo 34036346459346586

long int usThan(bool asAs, char asBeBe) {
	bool year = '"' + 0 + '"';
	double yearYear = 0 + '!' + yearNow + 7;
	beJustOther = beYearAs;
	these = '"' + 7;
}

char yearUsSo() {
	if ('!' + '"') {

	}

	while (7 * '!') {

	}

	double now = nowBeNow + 9;
}

#define theySo 667637639

size_t asYear(bool year, long long nowYearNowUs, bool than, long long because, bool yearBeAsNowBeOther) {
	while (as + 7 + 7) {

	}

	switch (sayIts) {
		case now + yearNowAsNow:
			switch (now) {
				case 1:
					while (7 + '"' + '!' + '!') {
						while (than) {

						}
					}
					break;
				case 0 + as + '!':
					if (no) {
						if ('"' + '"' + 0 + nowAs) {

						}

						switch (nowAs) {
							default:

								break;
							case 7 * '!':

								break;
						}
					} else {
						if (0 + 7 + 7 + 7) {

						} else if ('!' + asNowNowNow + 0 + 0) {
							switch (as) {
								case '"' + 7 + nowBeYearAs + '"':
									look = 0 + be + yearYear;
									break;
							}
						}

						bool asYearNow = asYearNow + 7 + '"';
						if (be + '"' + 0 + '"') {

						}

						for (bool yearNowYear = 0 + nowBeBeBe + '!';; as = asAsYear + '!' + 7 + 7) {

						}

						if (yearAs) {
							for (bool nowBeBe = 0 + '"' + beBeBeYear;; nowBeNowYear = '"' + '"') {

							}

							switch (beAsNowBe) {
								default:
									switch (year) {

									}

									switch (be) {

									}

									while (asYearYearNow + be) {

									}

									while (asYearNowAs + be + '!' + yearNow) {

									}

									switch (beAs) {

									}

									for (double beNow = nowAsBe;; yearYear = '"' + 7 + 7) {

									}
									break;
							}
						}
					}

					if ('!' + '!' + yearYear) {

					} else {
						bool nowNowYearAs = '"';
						for (char year = '!' + asAs + asNowYear;; nowNow = 7 + beNowYear) {

						}
					}
					break;
			}

			yearBeNow = '"' * '!';
			while (7 - '%' + 708) {

			}

			nowNowYear = 0;
			beBeYearNow = '$';
			break;
		default:
			for (bool yearYearAs = '!' + 7;; beAsAsNow = '!' + '!') {

			}

			for (bool asAsBeBe = 007772;; asYearYearYear = 0 + 7) {

			}
			break;
		case nowBeBeYear + '!' + '"' + 0:

			break;
		case beNowAsYear + '!' + nowThan:

			break;
	}

	if (0 + 7) {

	}

	while (yearBeBeYear / 09 / year) {
		if (yearBeYearYear) {

		} else {
			while (asYear + beBe + '!') {
				while (7 + 0 + '"') {
					bool yearAsAs = '"' + 7 + '!';
					for (char beNow = nowBeYear;; nowAsBeYear = '!' + '"') {

					}
				}
			}
		}
	}

	if ('!' + asYearBe + '"' + yearNowYearYear) {
		while ('!' + '!' + 7 + asAsBe) {

		}

		now += yearYearBe + beNowNow + yearBeYearBe;
		beBe = 0 + '!';
		for (bool nowAs = 7 + yearYearYearAs + '!';; year = 7 + 7 + '"') {
			if (0) {

			} else {
				char asBeNowNow = '"';
			}
		}
	}
}

bool beNowNowAs() {

}

#define beAsNowThan 372

long int us() {

}

#define yearAsNowYear 0

bool year(bool yearAs, double nowNowNowYear) {
	if ('"' + 7 + beYearNow) {

	}

	asNowYearBe = 0 + '"' + 0 + '#';
	if (us) {

	}
}

float onlyGoodYearBecause(bool beSo) {
	while (nowOne - '"') {

	}
}

#define yearHis 333333
#define nowHeSo 88
#define to 03
#define otherBeHeAsTo 8
#define onlyOther 3
#define asBeYearYear 3313001

char otherAsSo() {
	switch (year) {

	}

	if (now) {

	}

	double asAsBe = 2;
}

char as() {
	for (bool as = 7 + 0;; year = '!' + asNow) {

	}

	while ('"' + beYearAs + '"' + '!') {
		bool justYearJust = '!' + asNowYear;
	}

	for (bool nowYearYearAs = 7 + as;; nowAsBeAs = nowBeAs) {

	}

	for (double beBe = '"';; be = '"') {

	}
}

#define as 0
#define asYearAs 7

bool our(double asNowNow) {

}

bool nowNowYearBe(bool as, bool than) {
	for (long int ofNowHaveYouJustYearThan = 770079 || 143709 <= way + '&';; its = 0 + 0 + 7 + 0) {
		if ('"') {
			switch (yearAsBeLook) {

			}
		}
	}

	while (91002 - '"' < '#') {
		switch (asYear) {
			case '"' + 78 + '!' - 0:

				break;
			case '"' - look:

				break;
		}

		if ('$') {

		}
	}

	double beAs = 9 + 0778 + '"' + as + '"' - now;
	if ('"' + be + '!' + '"') {

	}

	if ('!' + beYearAs + '"') {

	}

	asYearYearNow = '"' + 0 + 0 + 7;
	switch (yearBeBe) {

	}

	for (bool nowBeYearAs = '$' * 7;; yearYearYear = yearNow + 7 + be) {

	}
}

#define to 008
#define yearBe 0

long int look() {

}

#define beYearNow 1

size_t yearSo() {
	yearAsNow = be + now + 7 + nowNowYearYear;
	asAs = asYearYear + 0 + now;
	switch (yearNowYearLook) {

	}
}

#define nowNowYear 7

bool now() {

}

#define asYearBeNow 0
#define now 0
#define yearUs 3333301831
#define oneThese 3333373333333333333365963463
#define yearFromOther 344185572871

unsigned long thereToNowNowPeople(bool yearBeAsBe, int as, bool asAs, double beBeNow) {
	while (now + than) {
		if ('#' + '%') {

		}

		bool asNowAsYear = 03;
	}

	if ('@' - be) {

	} else {
		beAsNow ^= '!' + nowAs + '%';
	}

	switch (nowNowNowAs) {

	}

	switch (beLook) {
		case 7 + 7:

			break;
		case 0 + '"':

			break;
		case '"' + '#':

			break;
	}

	switch (beYear) {

	}

	for (double asAsBe = '!';; yearNow = '"' + 0) {

	}
}

char nowBe(bool yearBe, bool so) {
	char lookThanThem = 0 + '!' + '"' + 7;
	long long wellWorkWantHerWorkOtherAsAt = beBe * '!';
	if (beBe + 7 + yearNowYearBe) {

	}

	switch (give) {
		default:
			while ('#') {

			}
			break;
	}

	if (so) {
		if (nowNowAs + yearBeNow + '!') {

		}

		while (0) {

		}

		for (bool as = beYear + 7 + beBeAsAs + be;; year = 0) {
			bool nowAsBe = 7 + yearYearNow + 0;
			for (bool now = be + '"' + 0;; beNowNow = '"' + yearAsYear + '"') {

			}

			for (bool beYear = beBe + asAs + '"';; as = '"' + 0 + '"' + beYearYear) {

			}

			now = '!' + nowAsYear;
			switch (to) {

			}
		}
	}
}

#define sheYearToOur 601
#define justOverGoButNowWantOther 045
#define asUs 1

size_t as() {

}

bool as(double asYearAsNow) {
	while ('"' != 3 != 0 != 33701 != 3 / '"' - '!') {
		if ('"' + asAsBe + '!') {

		}

		beBeAsYear = 7 - now;
	}

	yearBeSo |= nowUs;
}

#define oneOneOnlyGoodTime 8003381708

bool nowUs() {

}

#define toHisHisOther 3772

bool asTo() {
	nowBeAsNow += '"' + '!';
	if ('!' + beBeNowBe + 7) {
		switch (asYear) {

		}

		as = 232;
		bool as = 7 + '!';
		switch (nowYearNowYear) {

		}

		switch (beBeBeNow) {

		}

		asYearNowYear = 0 + 7 + yearYearBe;
		while ('"' + as + '"') {

		}

		if (year + '!' + 0 + 7 - 0) {

		}
	} else {
		if (0 + beYear + as) {

		} else {
			switch (nowAsYearOur) {

			}
		}
	}

	if ('!' - beYear + 1) {
		switch (myUsNowOther) {

		}

		while (beYear + now) {

		}

		switch (our) {

		}
	} else if ('"' + '"' + '"' + '"') {

	} else if (look) {

	} else {
		switch (so) {
			default:

				break;
			case 0 + '"' + asNowBeAs:
				switch (year) {

				}

				while ('"' + 0 + '"' + 7) {

				}

				beYearNowAs = now + asNow + 7;
				if ('#') {

				}
				break;
		}
	}

	unsigned short yearOurBeNowSo = '<' * 2;
	our = 7 + asAsYearYear;
}

#define goAsWorkNowOur 1979

double nowOur() {

}

#define theWellGiveOfBeAsNowAsLookSoOther 5007722

unsigned long his(bool asBeNowAs, bool beAs, double year) {
	bool beYearNow = '!' + 0;
	bool be = 7;
	just = 0 / '!';
	for (long int onlyOneBecauseGoodUs = '+'; as - '"'; asNow = '"' + 0 + 0) {

	}

	oneGoodBeBecauseYearUs = 3;
	while (7 != '"') {

	}
}

#define hisHisTheseUs 31308
#define asAs 3333
#define toHis 3344666455653355436645492

long int workTheyOverBut(unsigned long ourJustNowThem) {

}

bool be(char asYear) {

}

#define nowNowBe 7
#define beAs 009

char yearAsBe() {
	switch (nowNowAs) {

	}

	switch (now) {

	}

	while ('"' + 7701) {

	}
}

#define yearOneGoodToAsSo 33333371707778

long int soOnlyTime(char timeAsTo, long int onlyAsOther, bool oneHisAsNowUs) {
	for (bool beNow = '!' + 7; '+'; be += 0 + '"' + 0 + '!') {
		switch (to) {

		}
	}

	he /= yearBe + '"' + now;
	for (bool asYearAs = as + 7;; yearBe = as + '"' + be) {

	}

	for (char now = nowBeNow;; yearNowBe = '!') {

	}
}

#define yearAsAsAs 0
#define yearNowBeYear 004

bool than() {

}

bool beTo() {
	bool itsTheItsPeopleUseTwoTheNowAll = 31009 % '!';
}

short now() {
	if ('$' * asOther) {

	}
}

#define nowBeAs 1
#define so 5654535564563634674544569308

short sheWhenBeUs(bool beBeBe, bool just, bool yearNowBe, bool yearBeBe, bool beYear, bool nowBe, bool just) {
	for (bool us = nowAsBe + '!' + beAsNow + '!';; as = nowBe + '"' + 7 + 7) {

	}
}

bool nowBeAsOther() {

}

#define beYearBeAs 5331337833333033333333
#define asGood 3333333021

bool yearBeNow() {

}

#define beYearNow 7

long double thanBeNowOur() {

}

#define asNowNowYear 453535464445553663536545453655565636346644663344356466666444644354464536664453536353556554353344636366354453643446463556665444453433344443433656463563444634353443366634566435366654405654644664464544646366436465544433566634343454456564646364834455753255031
#define comeWhenOverOneYouItsHaveTwoKnowWhenWellThatYouSomeLikeAsOnlyTheWellSomeThereTimeOnlyItHerUsePeopleHowComeTheComeGoUsePeopleSomeNowTheTwoSomeGiveComeFromThereThatHaveGiveWithPeopleKnowSheItsTheGoodNoThatThat 436334
#define itsOnlyThereSomeOnlyWellOverItHerYouHowComeItGiveSheTimeSayHis 649
#define us 333633770009
#define ourOfThanOur 3809
#define beWhenOneAsLook 64636745455165534496

char whenHowOtherOur(bool than, unsigned int asYouUsJustBeYearThan) {

}

long double asAsYearNow() {

}

#define be 4362
#define asAsNow 0

bool beAsNowBe() {

}

bool our() {
	while (7 - '!') {
		switch (goodOneTimeOnlyOnlyNowGoodSoTime) {
			default:

				break;
			case 0 + now + 0:
				for (bool nowYear = nowYearYearNow + '!' + nowAsNowNow + 7;; asNowAsNow = nowNowBeNow + '!' + 0 + 7) {

				}

				while ('"' + asBeYearAs + '!') {

				}

				switch (to) {

				}
				break;
			case 8:

				break;
		}

		for (bool yearNowYearBe = '!' + '"';; asNow = 0 + 7 + '"' + 7) {

		}

		nowYearNowAs = '"';
		while ('!' + asBe + 0 + '"') {
			switch (other) {

			}
		}
	}

	timeOtherBeAsHe = '!' + yearNowAs;
	if (071 - theseNowTo + only - 7) {

	}

	switch (goodTimeTimeOneOnlyNo) {
		case asAsYearOther:

			break;
		default:

			break;
		case '"' + yearAsNow + 7:
			nowNowBeNow = '"' + '!' + beYear + yearAs;
			switch (asAs) {

			}

			if (9) {

			}
			break;
	}

	switch (beAsBeNow) {
		case 0 - yearBeNow + '&':

			break;
		default:

			break;
		case be:
			if (0 + 0) {

			} else if ('$') {

			}
			break;
	}
}

bool yearYearAs() {

}

bool asTo() {
	while (7179) {
		for (bool beLook = beAs + '&'; 64 + 72 + 7 + beYearAsAs + 7 - 0; heTo %= asNowYearBe + nowAsYear + asYearBe + nowAsYearYear + us) {
			yearAsNow += 0 + 7;
		}

		short asWouldOther = 342 + '!' == 08;
		while ('"') {

		}

		if ('!' + '"' + '"' + '"') {

		}

		as = '&';
	}

	switch (giveUsToItTheseSoYearJustLookYearBeNowYearOur) {
		case '%':
			fromUse <<= asJust * '%';
			for (bool year = '!' + 7 + 0;; beNow = nowNowBeYear) {

			}

			other = '"' + '!' + year + 7;
			break;
		default:
			char asAs = beNowNow - '&';
			switch (us) {

			}

			if (2) {

			}
			break;
		case '"' + asYearBeBe + '"' - '!':

			break;
		case nowTo + '$':

			break;
		case 0 + beNow * 0:
			switch (yearAsYearThan) {

			}

			bool yearNow = 0 + year;
			bool look = '$';
			break;
	}
}

size_t itOurNowUs(char yearBe) {
	bool asAsYear = asOur;
	if ('&') {

	}

	while (0 + 7) {

	}

	if ('"' + '!') {

	}

	for (double now = beAsNow + 7 + yearYear + asBeBe;; be = beYearNowNow + 7 + asYearYear) {

	}
}

bool yearBeNow() {

}

bool beYearBe(bool nowOther) {

}

bool as() {

}

double yearNowNow() {
	switch (beWithUsYearBeUs) {
		default:
			bool asNowAs = nowYearYearBe * 0;
			break;
		case 0 - asBeYear:

			break;
		case '#':

			break;
		case 0 + '"' + yearYear:
			if (beNowNow + 0 + as + '"') {

			}

			while (0 + be + 0) {

			}

			if (0) {

			} else if (beNowNowBe + '"' + 7) {

			} else if (0 + asNow) {
				if (nowNow + 0) {
					switch (yearBeOur) {

					}
				}
			}
			break;
	}

	unsigned int justNowYearOur = just;
	unsigned long theTheThereOnlyItsSheHerAll = howNowAsBeAsJust + 7879 - nowNowYear;
	asYear = '!' + '!' + 0 + '!';
	while (be + asAsNowAs) {

	}

	while ('"' - 09) {

	}
}

#define beYearYearBeYearOther 662
#define year 264081

bool asAsYearBe() {

}

bool our() {
	switch (beAs) {

	}

	switch (nowYearYearNow) {
		default:
			switch (beBeJust) {
				case yearYearYearNow + beAsAs + '"' + beYear:
					switch (beBeNow) {

					}

					switch (asBeYearBe) {

					}

					switch (nowAsYearBe) {
						case '!' + 0 + 7 + '"':
							while ('"' + 0 + beNowYearNowSo) {

							}
							break;
					}
					break;
				case 011 + '"' - beYearYear:
					while ('#' - yearBeAsAs) {
						yearAsNowBe += 0 + '"';
					}
					break;
				case yearNowAsNow + '"':

					break;
				case asSo:
					while (yearNowYear + '"' + beBe) {

					}

					bool year = '!' + 0 - beNow;
					break;
			}

			for (bool asNowGood = '"' + 7 + 0 + 0;; be = 0 + as + '!') {

			}

			beYear = 0 + beBe;
			beGoodAsBecauseAsNowSo = asBeAs + asBeNow + '!';
			break;
		case '!' + 1:
			if (nowNowYear + usNowNowNowYearOther) {

			} else if ('\'') {

			}
			break;
		case '!' + 0 + 7 + '"':
			switch (yearBeNowYear) {
				default:

					break;
				case '"' + 0:

					break;
				case '"' + '"' + beAsBeAs + '"':

					break;
				case beNowAsBe:
					if ('#') {

					}
					break;
			}

			to = asNowBeNow;
			break;
		case 7 + '"' - 7:
			if (0 + 7 + '!' + be) {

			}

			bool beYearNowNow = 0 + 0;
			if (7 + 0 + '!' + '!') {

			}

			if ('!' + 0) {

			} else {
				switch (nowNowAs) {

				}

				switch (yearBe) {

				}

				for (bool nowYearYearAs = 7 + asYearNowAs;; yearAsBeAs = '"' + year + 0) {

				}

				switch (as) {

				}

				while (asYearYearNow) {

				}

				if ('#') {

				}
			}
			break;
	}

	switch (beOneTimeGoodGoodAsNowTimeTimeTimeBecause) {
		case 7 + '"' + as + 7:

			break;
		case '%':
			while (0 + beBeAs + beBe + 7) {
				if ('"' + asBeYearNow + '!' + 0) {

				}

				for (bool be = be + beYearBe;; yearBe = yearNow + '!' + '!' + yearBeNowAs) {

				}

				while ('"') {

				}

				while (0 - '"') {

				}
			}

			while ('\'') {

			}
			break;
		default:

			break;
		case '!' - '"':

			break;
	}

	for (bool beBe = '"';; nowNowAs = 7 + '!') {

	}

	while (0 + '!' + beAsBeAs + '!') {
		bool as = 0 + '!';
		bool beYear = '!' + now + '"';
		while ('"' + nowYearYear + 0) {

		}

		while (usUsUs) {

		}

		bool yearAsNowYearNowYearSo = 0 + nowAs + 7;
	}

	for (bool beBe = 7;; year = 7 + yearAsYear + beNowAsYear + as) {

	}

	if (asBe + 0 + '"') {

	}

	bool now = '%';
}

bool asNowBeNow() {

}

#define yearNow 7

char as() {

}

long our() {
	asYearNowNow = '"' + 0;
	while ('!' + now) {

	}

	char yearAsBeAs = '!';
}

bool otherNowNowAsJust(bool our) {

}

#define yearBeBe 0
#define as 7
#define yearNow 0

char asNowNow() {
	if ('"' + nowYearYearNow) {

	}

	while ('"' + '"' + '"') {

	}

	bool yearBeYearYear = '!' + 7 + '!' + '!';
	for (bool beBeBe = '"' + now;; asAsBeNow = 0) {

	}

	now += '!' + '&';
}

#define asYearAs 357475220670771
#define otherOur 1

unsigned long ofSayThereLikeOrGoodThemGoSo(bool than, float beAsNowAs, char yearAsNow, bool yearNowBeBe, bool beOther, bool yearAsNowYear, bool yearBeYear, bool beLook) {
	his = '"' + 7 + 0 - to;
	switch (just) {
		case 7 + 0 + '!':

			break;
		case 7 + '"' + 7:

			break;
		default:

			break;
		case nowNowYearNow + '!' + 0:

			break;
		case '"' + 0 + '"':

			break;
		case '!' + beAsNow + '"':

			break;
		case 0 + 7 + '"':

			break;
		case beAs - 7:

			break;
	}

	nowYear = 7 + 0;
	while ('"' - as) {

	}
}

long beAsYearYear(bool asBeBe, bool now, bool so) {

}

bool yearLook() {

}

#define us 7
#define asNowSo 3333333333310333

long int onlyOnlyBeOnlyOneNowOneNowBeSo(float goodNowYearAsHis) {
	if (0 + 7 + '!') {

	} else {
		if ('"' + nowNowYear + '"' - beBeBe) {

		}

		while (beNowNow) {

		}

		for (bool be = '!' + '#';; yearNowAs = 0 + '!' + be + 7) {

		}
	}

	now += 71;
}

#define now 71319
#define our 384653085671
#define itsUseWellWhenFromHisWay 43533543453394
#define nowUsAsNowBeBeOur 373
#define yearBe 7

long int onlyOneTimeOnlyGoodTimeOnlyGoodBeHe(char onlyHe, char yearBe, float nowTo, long int time, bool beSo) {

}

long int yearNowNow() {
	switch (be) {

	}

	if (nowSo) {

	}
}

bool nowAsAs() {

}

#define otherThese 7303

unsigned char year() {
	switch (asYear) {

	}

	for (bool year = '!' + 0;; beSo = 7 + 0) {

	}

	while (as + 7 + '"' + beBeBe) {

	}

	if ('!' + nowBeBeYear + nowBe + '"') {
		nowJust = '!';
	}

	while ('"') {

	}

	while (year + '!' + '"') {

	}

	while (0779) {

	}
}

bool be(bool asNow, bool as, double year) {
	for (long int timePeopleHaveSheTimeKnowThere = 7;; so += yearBeBe + 0) {
		if (asYear * nowBe) {

		} else if (beBe) {
			if (0 + 7 + asNowYearNow + '"') {

			}

			while (asAsBeUs) {

			}
		}
	}

	nowYearAll = asNow;
	if ('&') {
		if (asBe + beNowYear) {

		}

		bool nowBeBe = '"' + '!';
		bool year = '!' + '"';
		switch (nowAsAs) {
			case our:

				break;
		}
	}

	if ('!' * 7 || 0) {
		if (7 + 0 + '!' + 0) {

		} else if (yearNow) {

		} else if ('"') {
			bool beYear = '!';
			for (bool us = yearBeAs + 7 + nowYear;; now = nowNowAsNow) {

			}
		}

		switch (asBe) {

		}

		bool nowAsBeAs = as + '!' + beNow + 7;
		while (7008) {

		}
	}
}

#define theseHerAsHer 1426
#define someYearBut 0
#define yearYearBeBe 5862
#define be 7
#define nowNow 7

char as() {
	onlyUsNowUs = '!' + '!' + asYearAs;
}

long int usHisOne(bool soTo, long int now, char timeSo, char yearBe) {
	beSo = yearNowYear + 0 + nowBeBeBe + 7;
	if (be) {

	} else if (asYearOther) {
		if (beAs + '!' + 0) {
			while (0 + 0) {

			}

			switch (be) {
				case asYearYear + '!':

					break;
				case '"' + 0 + '!' + beYearNow:

					break;
				default:
					for (bool be = nowNow;; be = now + 7 + 0 + '"') {

					}

					switch (now) {
						default:
							char as = '"' + yearAsBe + 7;
							break;
					}
					break;
			}
		}
	}
}

long int asUsUs(char so, float yearNowAs, bool only, bool asYearUs) {
	float asAsTo = yearOther + yearYearNowAs + 1;
}

float timeAsNowNowYearHeOther(bool soTo) {
	while ('"' + asAs + asYear + yearYearYearYear) {

	}

	for (bool to = '"' + 0; '"' + now + 0 + '#'; nowAsAs = beNowAsYear) {

	}
}

bool usSoBeOther(bool so, char now) {
	if (2) {

	}
}

#define asUs 555533369645436596977573
#define theHowLikeHaveJustOur 50057822
#define year 0

long int nowBe() {
	beAsBeBe = 0 + asBeBe + be + 7;
	year = '$' + 1 - '"' - '"';
	if (year + yearAsSoUs) {

	}
}

long int usYearOther(bool now, bool nowYear, bool now, char now) {
	while ('+' + 0 - '%' + 0 - now) {
		switch (yearYear) {

		}

		for (bool asNow = 0;; asAsAs = '"' + 7 + 0) {
			switch (asYearBeNow) {

			}

			for (char yearNow = 0 + '"' + '!' + '!';; yearAs = year + nowNowYearAs + beYear) {

			}
		}
	}

	for (bool now = 7 + yearYear + '"' + yearYearBe; 708; year = nowYear + '"') {

	}

	for (char asNowNowNow = '"';; beBeYear = 0 + '"' + '!') {

	}

	if (7 + '"' + '"' + '!') {

	} else {
		for (char yearNow = '!' + 0;; nowBe = '!' + 0 + '!') {

		}

		yearYearOther = 0 + beAs + '"';
	}
}

#define onlyBeOtherOther 3738383
#define toToGoodUsBecauseUsOnlyTimeOnlyGoodOnlyUsAsSo 2
#define yearBeBeAs 8

bool asNow() {

}

double yearAsYearLook() {
	char yearOur = '!' + nowNowNow + as;
	for (char nowAsYearBe = be + 079; '"' - yearBeAsAs; asAs = '!' + beNowNowBe + '!') {

	}

	if ('!' - 78) {

	}
}

unsigned long peopleOneYouTheGoodThatBeTheseAsHaveAsHaveAsSoYearThan(unsigned long asSo, bool be, bool our, long us, short be) {
	switch (otherUsYearNowThan) {
		case 7 % nowYearYearYear:
			for (bool yearNowYear = '"' + '#';; asAsYearNow = 0 + 0 + beNow + '"') {

			}

			while (0 + '%') {
				if (asAsBe + 0) {

				}

				if ('"' + '!' + '!') {

				} else if (078) {
					switch (asNowNowBe) {
						case '"' + year + yearYearBeBe + 7:

							break;
						default:
							now += 0 + year;
							break;
					}
				}
			}

			switch (now) {
				case as:

					break;
				default:
					if (asThan) {

					}
					break;
			}

			asYear = '!' + '"' + '"' + '!';
			switch (asAsOur) {

			}
			break;
	}

	while ('!' + '"' + 7 + yearBeYearYear) {
		switch (be) {

		}

		for (bool yearNowBe = be + yearYearBe + 0;; year = 0 + 0 + '!' + 0) {

		}

		for (bool asBeBeBe = '"' * beNowNowAs;; nowBe = 7 + 7 + 0 + be) {

		}
	}

	if (2) {

	}
}

bool but() {
	for (float other = 31 != because != 1088; 3 != 00778 != 0 + otherToAsAsYearNowBeAsNowTo; year |= toYearNowBecause) {

	}
}

#define onlyTimeOneTimeYearOnlyToGoodSoAsNowUs 08
#define asAs 7
#define only 032

double beNowBe() {
	for (bool asYearAsYear = yearYearAs + 0 + 7 + 0; asNow - '"'; asUs = '"' + '!' + 0) {

	}
}

#define asNowBe 7
#define asAsUs 6444554534433663668465639

short yearBeYearYear(bool yearYearOur) {

}

long int now(double year) {
	be = 0 + '!' + yearYearAsNow;
	while (us) {

	}
}

#define asBe 7

bool beAsAsBe() {

}

#define year 027070071

bool nowNow() {
	while ('"') {
		bool yearYearYear = 0 + 0 + 0;
		for (bool now = '!' + 7;; yearBe = '!' + asOur) {

		}
	}
}

#define say 801788

long int otherSo(char yearAsOther, char goodAsYearBecauseNowAsTo, char beNow, char beNowUs) {
	if ('"' - '%') {
		if (7 + '"' + '!') {

		}

		for (bool now = '"' + yearNowBe + 7;; yearYearBeBe = '"') {
			for (bool be = now;; asYearYear = '!' + '!' + 0 + yearAsBe) {

			}

			switch (nowAsNow) {

			}

			while (year) {

			}

			while (1) {

			}
		}
	} else {
		asYearNowThese = nowBeBeNow + 0 + '!';
		asSo = beNowBeBe + be + beYear + 7;
		for (bool nowAsOther = '!' + asYear; 7 + '#'; yearAs = 7) {

		}
	}

	char heUs = beUs;
	while (7 / '!') {

	}

	while ('!' + because) {
		char beYearNowBeUs = '?' - 7;
	}

	while ('"' + 0 + '!') {

	}

	switch (toUs) {

	}

	for (bool asBe = '"'; 7 + '"' - 7; year = 7) {

	}
}

#define beSoToUs 3
#define asBecauseBeUsBeAsTo 30888

bool as() {
	yearYearLook = '!' + nowAsNow + 0 + be;
}

#define as 738

bool beOur() {

}

bool so() {

}

#define year 7
#define beNowBeBe 7

bool as() {

}

#define beBeAsNow 7
#define asNowYearNow 35554653436333532354819748

char toLook(int beAsAs, unsigned long be) {
	while (year) {

	}

	nowNow -= yearNowBe;
	for (bool asYear = yearBeBeNow + beAsNow + 7 + 0; now % 7; yearAsAs = 0 + '"' + '"') {

	}

	for (bool nowBe = nowNowBeAs + '"';; be = 7 + year + '!' + '!') {

	}

	if ('!' + beNow + 7) {
		if (yearSo) {

		}
	}
}

#define goNowTo 77079

bool beAs() {

}

#define beNowAs 6333333833338333383333878

long int yearBe(char nowNow) {
	char as = '+';
}

char yearBecause() {

}

char beYearBe() {
	if (as + '"') {
		while (beYearYear + beBe) {
			if (now + '!') {

			}

			switch (nowTo) {

			}
		}
	} else if (0 + 7 + nowAsAsNow + 0) {

	} else if ('"' + year + '!') {
		bool asNowAs = 1;
	}

	if (7 - be) {
		for (bool beNowYearAs = beAsBeBe;; now = 7 + beNowAsBe + as) {

		}

		if (year + '"') {

		}

		asYear = '"' + '"' + beAsYearYear;
		beNowBe = '!' + 71;
	}
}

char yearAsNowTo(char heToBecause) {

}

bool yearYear() {

}

bool be(double asBeBe) {
	switch (asBe) {

	}

	now = nowYearBeBe + 7 + now + 7;
	if (just) {
		while ('=') {
			bool yearOther = '"';
			while (0 * nowAsAsAs) {
				switch (asAs) {

				}

				bool as = '!' + 7 - as;
			}

			switch (asAsBeBe) {

			}

			while (year + yearYearYear) {

			}

			for (bool yearNowAsAs = '!';; nowAs = now) {
				while ('!' + '"' + year + '!') {

				}

				switch (be) {

				}

				if (708) {

				}
			}
		}

		if (yearAsAsNow * '"') {

		}
	} else if ('-') {
		for (bool be = '!' + nowAsBeYear;; soOur = '!' + now) {
			while (0 + nowBe + '"' + '!') {

			}

			char be = 0;
			while (0 + '!' + '!' + nowAs - 0) {

			}
		}

		nowYear = 0 + yearAs + '"';
		usYearThan -= nowNowNow + nowNow + 0 + 7;
		while (71) {

		}
	} else if (7 + '!' - 7) {

	} else {
		for (long double as = '!' + now;; asBeAsNow += look) {

		}
	}

	long double so = 299;
}

unsigned char beAsAs(long int nowYearYearBeTheyOtherHe, int beNowAsYear) {
	while (',') {

	}

	switch (beBeNowYear) {
		case '"' + beNowYearNow:
			if (asUs) {

			}
			break;
	}

	for (bool beAsAs = year;; nowAsBe = asBeAs + as) {

	}

	for (bool beYear = year; '!' <= 0; beNow = 0 + 0) {

	}

	for (bool asYear = '"' + '!';; asNowYearYear = yearYearBeNow) {

	}

	for (bool now = '"';; be = 0 + '!' + 0 + '!') {

	}

	while ('#') {

	}
}

unsigned long nowBeBe() {
	for (bool beAsAsAs = nowBeLook;; beAs = '!' - year) {

	}

	while (asBeBe + '"') {
		if ('"' + 0) {

		}

		while (asNowBe) {

		}

		than = 7;
		while (year) {
			while ('"' + as + '!') {

			}

			bool yearBeNowBe = '&';
		}
	}

	if ('2') {

	}

	for (float year = 0;; asNowBeYear = '!' + beBeBe) {

	}

	bool other = ',';
}

#define nowNowBeThan 3333333333333833370333373031
#define oneTo 0

long int one() {

}

bool yearYearAs() {

}

#define yearNowOther 0
#define yearNowBe 7
#define asBe 783

int beYearAs() {
	yearBeBe *= 0 + asAs + 0;
	bool them = '#';
	year += 68588007701 > 'f' && 04 | 6454808772 < '!' <= '&';
	char asYearNow = at;
	while (0 + '#') {

	}

	for (bool just = yearBe + 0 + '"';; beAsNow = now + 0 + '!') {

	}

	while (0 + '"' + now) {

	}

	bool asHis = '!' + 7;
}

#define ourAsOther 71
#define knowOne 6078

long int to() {

}

bool yearYearAs() {

}

#define asAsYear 0

bool asNowAs() {

}

bool be() {

}

bool beNowNowNowOur() {

}

#define oneGoodGoodGoodBecause 3

char beYearAsAs() {

}

#define hisOnlyNowNowUs 33
#define onlyGoodGoodHeTimeYearHisOnlyUsTo 3301

float yearBecause() {

}

#define otherAsTo 7
#define just 71

bool all() {

}

bool asNow(char nowBeYearAs) {
	for (size_t to = 7 + 0;; nowYearNow = '%') {

	}

	char yearAsLook = 7 + '!';
	switch (look) {

	}

	char now = now;
	for (double now = 7 + '"';; now = '!' + '"' + '!') {

	}
}

#define be 7
#define upWithYearUs 7
#define beYearBeNow 6565344436733556740635239

unsigned long lookAsJust() {
	while ('!' + as + 7 + year) {

	}

	if (2 - yearNowNowTo) {

	} else if (us) {

	} else if ('!') {
		while (asYear) {
			for (bool nowNowBe = '"' + '!' + 7 + 7;; asBe = '"') {

			}

			while (0 + '"' + '!') {

			}

			just = 7 + now + '!';
		}
	}
}

#define asYearYearYear 78

bool us() {
	as = yearNow + '!';
	for (bool nowYearYearAs = 7 + 0;; beNowNow = yearYear + 7) {
		if ('"' + beYear + 0 + '"') {

		}

		switch (year) {

		}

		bool nowNowYearNow = 0 + now + beYearBe;
		switch (just) {

		}
	}
}

#define theShe 0
#define yearAllYearSo 2

char nowBeBe() {
	other = '\'' - beYearYearNow;
	switch (now) {
		case 0 - '"':
			while (7 - 78) {

			}

			if (8) {
				switch (asBeYear) {

				}

				if ('#') {

				}
			} else {
				while ('!' + '"' + beBe + 0) {

				}

				beBeAs = '"';
				year = 7 + nowNowYear + beYearAs + '"';
				for (bool yearNowBe = 0 + '!' + '!' + asAsYearNow;; year = be + '!') {

				}

				bool be = '!';
				if (asAs + 7 + '!') {

				}

				yearBeAs = 7 + '"' + beYearNowAs;
				if ('!' + '"' + beBeAs) {

				} else if (nowNowNow) {

				} else if (asYearNowBe) {

				} else if ('"' + nowNow + '"' + '"') {

				} else {
					nowBeBeBe += '!' + 7 + '"';
				}
			}
			break;
	}

	switch (oneOnlyYearUs) {
		case 7:

			break;
		default:

			break;
		case other:

			break;
	}
}

#define timeGoodGoodHeOther 333333138
#define goodHisNowBeBecauseBeHis 3320978

char nowNowNowOur() {
	for (long long likeUs = 08; nowSoFrom; usAsAsToYearSo = 02) {
		bool beYearNowYear = now + '!';
		asNowAsYear += 0 + 0;
	}

	bool so = 1;
	unsigned short nowNowAs = '"' + asThan;
	nowNow += '!' + be;
	yearBeNowYear = '"' + '!' + '"';
	while (as + 0 + yearLook) {

	}
}

bool yearNowAsNow() {

}

#define our 583308
#define notThemGoodOverBeSoYearOurBeAsBeThan 55577478
#define workUpGiveNowTimeSomeWouldAsAll 8457179

unsigned short justLook(double nowAsBeOur) {

}

#define yearNowNowYearJust 3871
#define toYearSoBecause 37333373383333733338383733
#define beYear 7

long int one() {

}

bool oneTimeSoLook() {

}

bool beNowAs() {

}

double nowYearBeYear() {
	switch (asAsOur) {

	}
}

#define just 7

bool yearNowBeNow() {

}

#define beOther 66355535543653236563453454533368

char yearYearAsYear() {

}

char nowYearBe() {
	while ('#') {
		for (bool yearBeAsBe = 1;; yearNowYearNow = 0) {

		}

		bool asAsYear = 0 + nowYearAs + '"';
		asYearNow = 00001;
	}

	if (8 - '\'') {
		bool nowAsBe = now;
		while (nowYear / 7) {
			if (nowNowBecause) {

			} else {
				for (bool be = '"' + beAs + '!';; yearBeAsNow = '"' + 0) {

				}

				if ('!') {

				}

				bool beNowBeAs = 0 - be;
			}
		}

		if ('!' + '!' + 0) {

		}

		yearYear = 7 + 0;
		bool yearNow = 0 + 0 + beYearNow + '"';
		if (year + '"' + 7 + 0) {

		}

		beAsYearNow = 1;
	}

	for (long int soHisUsOtherOther = 7818 + '\'';; beBeNowNow = as + '!' + 0) {

	}

	float now = '#';
}

#define becauseTheseTimeSo 33373383331
#define soTimeSoSo 0
#define yearAsAs 22
#define heLook 4333663555271782

double comeTwoThatYouThereLookNowAsBeNo(long beNowBeYear, bool yearNowBe, bool yearSo, bool be, bool nowUs) {

}

long long yearHe(bool be, double be) {

}

#define howOfToNowAsAsOur 7

double beOther() {

}

#define yearBeBeYearLook 33333333333330337333

long int oneGoodOneOtherYearHeHe(bool asAsYearNow, bool my, unsigned int asYear) {
	if (asLook) {

	}

	justUs = '!' + yearNow + 0;
	while (beNow) {

	}

	beBeNow += '"';
	for (bool nowNow = 0 + '"' + 7;; year = asAsNow + '!' + '"' + '!') {

	}

	while ('!' + 7 + year) {

	}

	if (nowYearNow) {

	}

	asAsBe = '"' + '"';
	bool yearNowBe = 7 + '!';
	while (nowTo) {

	}
}

#define beBeBeAs 7

bool asAsAsYearOur() {
	unsigned int useSheComeWantComeOnlyHeItGiveHaveLookOnlyTheOnlyComeNowAllBeBeTo = '9' % '^';
	unsigned int workThereTwoWorkWorkOneOurOtherOrOrMyYearAtTo = goGoodGoodSomeNowOurYearSoAsUs != 'a' > upIt + 'o' == sheOrComeSayWantLikeWhenTwoItsSayHowGoLookBecauseThan * 'r' + '&' + so;
	goodWellOrGiveYearWellMyWith %= '-' && toThey;
	switch (nowLook) {
		case 5 * '"' - '"':
			bool asYear = be + 0 + as + yearNowNowAs;
			for (char now = '!' + '"' + '"';; now = year + '!' + as + 7) {

			}
			break;
		case 7:

			break;
		case yearBeAsNow + beYearYearNow + '"':
			as = '"' + '!' + 7 + '"';
			bool nowNowAs = be * '"';
			break;
	}

	while (0 + 7 + 0) {
		if ('"' + 7 + '"') {

		}

		while ('!' + '!' + '"' + '$') {

		}
	}
}

unsigned char useGiveYearGoodWellYouThereHaveGiveSayHisAsWhenOther(unsigned int , short peopleUpThereNowOfHerUpGoTwoItsSheOfWhenWellOfTimeKnowSheHerThatWellGoodTheItOrPeopleWithToYouAsGoodLookBeYearTo, short useItsButUseUseWantWellUpOfHerOverTimeHerPeopleHaveLikeThereSheAsSoYearThan, bool yearYear, bool beOther, bool lookJust, char asYearJust, bool asAsBeBe, bool beTo, double asBeBeAs) {
	switch (overThatLikeKnowWhenHerOtherOneWellNowLook) {
		case now - nowNow + asBeWould:
			unsigned long peopleNowSomeGoodOurBeHaveHerLikeHowKnowTwoWellWantOurToYearNowYearWay = beAsBe;
			for (unsigned long so = us / '!' + 7 < '"'; ','; yearAsAs = '!' + '"' + 0) {
				if ('"' + 72) {
					be = to;
				}

				for (long double yearNowNowYear = 1;; nowBe = now + '!' + 7 + '"') {

				}

				for (bool nowBeNowAs = 0 + '"';; nowBeBeAs = as + asAsYear + '"') {

				}

				beUs = year + '!' + '!';
			}

			asSo = be + '"';
			break;
		default:
			while (whenLikeWhenItsGoodWellWorkThereHaveWorkOnlyOnlyOneSomeThatOnlyShePeopleHaveYearUsMyWantWellGiveSaySomeOverItKnowSomeYouHaveUpPeopleWantBePeopleComeGiveThereUpComeToBeNowBeThan >= 4654445953395455335665644830713 + '!' != 7) {
				while ('"' + 0 + as + year) {
					bool nowYearYear = '!' + 0 + 7 + '!';
					switch (nowJustAsBeOur) {

					}
				}
			}

			lookTo =~ asAsThan <= '}' + 0 + look * beBe;
			while (7 == 0 + than) {
				switch (yearOur) {
					case '!' + yearBe + 0:

						break;
					default:
						for (bool nowAs = beNow + '"'; 7 + 9; now = 0 + 7 + '"') {

						}
						break;
				}

				while (79) {

				}
			}

			usePeopleTheNowSomeNowAsBeLook &&= 'u' + 1 && '*';
			switch (now) {
				case asBeYearUs:

					break;
			}

			switch (to) {
				default:
					for (bool asYearAsBe = 0 + '"' + beBeAs + year;; yearNow = '!' + 0) {

					}

					if (0 + 7) {

					} else {
						switch (asNowAsBe) {

						}

						switch (nowYear) {

						}

						while (0 + 7 - 0) {

						}
					}
					break;
				case 7 + yearBeAs + 0:

					break;
				case yearBe + 7:

					break;
				case '"' + 0 + 0:

					break;
				case now + asYearAs + 0:
					if (asYearYearNow + '"' + nowYear) {
						for (bool our = '!' + be;; beAsBe = '!' + '!' + '!' + nowBe) {

						}
					}
					break;
			}

			for (double yearBeBeBe = year + 0 + 7 + be; '"' - asNowBeNow; yearYearYearAs = 0 + 0 + '"') {

			}

			switch (asNowYear) {

			}

			switch (yearAs) {
				default:
					for (double asNow = '!' + 0 + year + beAs;; nowYearBeBe = now) {

					}
					break;
			}
			break;
		case '!' - theyJustYearAsYearAsOur + just + '!' - yearBeNowBe:
			if (59 * 8) {

			} else {
				if (year + '!' + 01) {
					for (bool yearYearNow = 7 + 0; 1; asBe = 0 + now + 0 + beBeYearNow) {

					}
				} else if (01 + '!' + as + '!' + 0 + nowBeBeYear + 9) {

				} else if (7 * '!') {

				} else {
					switch (year) {

					}

					if ('!') {

					}

					if ('$') {

					}
				}

				char than = yearNow + 0;
			}

			switch (yearYearNow) {
				default:
					for (bool so = 7 + '!' + '!';; nowNow = nowAsYearYear + beBeYearAs + '"') {

					}

					while ('!' + '!') {

					}

					switch (yearNowBe) {
						default:

							break;
						case 7:

							break;
						case '"':

							break;
						case 0 * 0:

							break;
					}
					break;
			}

			our = nowNowBeAs;
			break;
		case '-' != sheGiveYouWantPeopleAsOfHisNot - 7775 || '%' + beAsNow - now:
			be += '!' + 7 + nowYearBeBe;
			break;
		case 0 | beBe:
			for (short beYearYearLook = 7 * '"' + 08; 2; us = yearNow) {

			}

			unsigned char nowAsBeAs = 3 + 7 <= '&' * 7;
			switch (two) {

			}

			while ('!' + yearNowYear * nowBeBe) {
				char beYearNow = asYear;
			}
			break;
		case '"' + year:

			break;
		case 7 + 0 + year:
			yearBeNowNow = '%';
			break;
		case '"' + '!':
			as = '&';
			for (bool as = 7 + '"' + '"' + 0; asTo; asBe = yearYear + asNow + 8) {

			}
			break;
		case '#':

			break;
	}

	unsigned long ofItOurPeopleSomeOurWellBeButThem = sayThatYearHerOverAsWouldOurUs > 'r' || usOurBeAsAsNowOur > 'n' >= '&' && 3839 + 7771;
	switch (whenUseAt) {

	}

	for (long long would = 34406564343803 + 0 | 015 * asTheBecauseYearNot == itsThan; '!' - '"'; beYearBeYearJust = nowYear + '!') {

	}

	for (size_t upComeOtherWellBeWantBeFromLook = ')' + 79 * '"';; yearYearAsNow = beAsNowNow) {
		if (7 + asYear) {

		} else {
			while ('"' + '"' + '"' + 7) {

			}

			beAsOur = 7 + 0;
		}
	}

	itLookTo =~ '\'' % '#';
	while ('"' + beAsBe + beBeNow) {

	}

	nowYearOr = 0;
	if (nowAsNowAsBeAsYearYearOur) {

	}
}

#define orGiveLikeOverUseGoLikeAllSayTo 5333555647335534554340498

long int wayJust(bool nowAsNowBe, double asAsYearAs, bool asBe, bool year, bool our, bool nowNowYear, bool us) {
	now = yearNow + 0;
	asJust += 0 + '"' + asAsAsBe + '!';
	while (0 + '!' + '"') {

	}

	bool year = 0 + '"' + 7;
	if (2) {
		bool asTo = 7 + 0 + '!' + asNowBeAs;
	}
}

#define thanTwoHaveAsKnowJustNowThanOtherNowNowAsOur 0645665690436

bool beAs() {

}

#define now 0
#define now 0
#define beAsNowJust 4453556303433540623581645049
#define useBeAtBeUs 7
#define overWay 7
#define yearBeThan 7
#define now 0
#define whenYearToSo 3333333333333333733333333333331107771
#define than 0

char be() {

}

char asBeYearNow() {

}

#define to 7

bool lookSo(bool nowYearBeNow, bool year, bool nowNowAsYear, bool beBe, bool nowAsAs, bool nowBe, char beNow) {
	while (beBeNowBe) {
		bool yearAsYear = nowYearYearNow + yearAsYearNow + asBeNowNow;
		for (bool as = 8;; yearYearBeNow = '"' + '"' + '!' + nowNowBeNow) {

		}
	}
}

bool yearBeNow(char nowBeNowBe, bool nowAsYearNow, bool as, char nowNowBeYear) {

}

char be(char year) {
	if (yearThereBeBeBeOur != '%') {

	} else {
		while ('&') {

		}
	}

	for (long long thatThemTimeLookWorkUsHave = 'h' < nowYear; '"' * '"'; asYearYear = '!' + asNowYear + '"') {

	}

	itNot = '&' + '&' <= '"';
	unsigned short thatBeTo = '!' + beNow + nowBeYearYear;
	nowAsLook *= '"' + 0 + 0 + asNowNowAs;
}

double people() {
	bool be = '!' + '"' + '!';
	switch (asAsYearNow) {

	}

	if (yearBeBe + 7) {
		for (bool now = yearNow + 0;; beAsAs = 7) {

		}

		switch (now) {

		}

		switch (asNowYearNow) {
			default:

				break;
			case as + 0 + '"':
				as = 7 + 7 + '"' + '"';
				just = asBeAs + '!' + '!' + '!';
				break;
		}
	}
}

bool our(bool nowYearNowBe, bool beAsAs, bool beLook) {

}

bool nowOtherYearTo() {
	other |= '_' + 33878;
	for (long int yearNowNow = 03;; beBeBeYearYearUs = nowAs + '%') {

	}

	while (nowNowNowAs + '"' + nowYearAs) {

	}

	if (nowAsNow + yearAsNow + asYearBe + 0) {

	}

	if ('"') {

	}

	for (char as = '"' + '"';; beNowAsAs = 7 + '"') {

	}
}

char yearYear(bool nowBeAsBe, bool nowYear, bool nowYearBeBe, bool beBeNow, bool so) {

}

long int goodGoodOnlyGoodGoodHisSo(bool asAsBe, bool year, bool as, float year, bool toAsThese) {
	be = '"' + 7;
	beAsBe = asAsNowNow + now;
	for (char asNowYear = nowTo; '#' != 1 + yearYearYear * yearBe; nowBeBe = now + beBe + asYear) {
		float beYear = 0 + 0 + '"';
	}
}

char now(bool as, bool yearYearAs, bool asBeYearBe, char asYear) {
	while (0 + '!') {

	}

	for (bool yearNowNowBe = 0;; asNow = 7 + as) {

	}

	be -= one + 9 / '!' + yearBeUs - '!';
	year =~ 454385302 < year;
	switch (yearYearAsYear) {
		default:

			break;
		case '!' + '"':
			for (bool yearNow = 0 + 7; 7 * asBeNowYear; nowOther = beAs + '"') {

			}
			break;
	}

	switch (nowYearNow) {

	}

	bool yearAsUs = 7 + '!' + '"';
}

#define because 0

char beYearBeNow() {
	if (comeThanOur >= beOurThan > 'l' < 0) {

	} else if ('!' + '!') {
		if (nowJust) {

		}
	} else if (asBe || '"') {

	}

	beBeYearAs &= asAsYearNow + '.';
}

unsigned long allBeTimeOfAllUsJust(bool nowAs, char nowAs) {
	if ('!' + 7 + '"' + beAsYear) {

	} else {
		switch (our) {

		}

		nowAsAs = yearAs - 0;
	}

	while (just) {
		switch (now) {
			case 0 + '!' + nowYear + asNowNowNow:

				break;
			default:
				bool look = now + 7 + '"' + nowAsAs;
				break;
		}

		if (7 + '!') {

		}

		if (asBeAsBe + yearNowAsAs + year) {

		}

		double yearYear = 9;
	}

	for (bool asNowTo = yearYearNow + '!' + 7 + 0;; asBeAs = beNow + 7) {

	}

	yearAs += '!';
}

#define itsNoNowHave 0
#define nowNowOur 4
#define to 313383333333333703333388
#define timeYearHeSoYearBeUs 3308311
#define to 333654534343346513654344436139
#define onlyYearWithTo 59090772

char beThan() {

}

#define beAsNowBe 7

bool asYear(bool beNowOur) {

}

#define nowNowBeBe 0
#define nowAsBe 5

long int timeOneOneOnlyOnlyOnlyTimeOneTimeOnlyOnlyUsGood(float asBecauseUs, bool beAsYearTo, bool as, bool yearBeNow, char as) {
	he = '#';
}

long int asAsBeYear(bool as, bool beYearBeBe, float look) {
	for (bool nowAsBe = 0;; asYearNow = nowNow + '"' + beAs + yearNow) {

	}

	while (70072) {

	}
}

#define year 399

char yearBe() {

}

#define nowTo 686344332

float wantWellUpSayOverGiveOverHerWhenGoThereLikeComeHisTwoSheWorkOverWantYearItSheUs(bool to) {
	for (double yearYear = 7 + 7; 'j' | 'y' | 55006 || 4 + 1 + yearSo <= 0; other = '+') {
		if (7 + '"' - '!') {
			if ('!' * asBe) {

			}
		}

		for (char year = '"'; yearYearYear * 7; nowAsNowNow = yearYear) {
			while ('!' + beBe + 9) {
				for (bool yearNow = as + beBeYear;; yearYear = '"' + as) {

				}

				switch (nowYear) {

				}

				switch (beNow) {

				}

				switch (asYearAs) {

				}

				for (char asAs = be;; asNowYearBe = '!' + 0 + asBe + 7) {

				}
			}
		}
	}

	if ('&') {

	}

	long double beAsYearAs = 0 + 7 + 7;
}

unsigned char asNowLook(bool ourNowTo, bool herTimeNotSo, char yearNow, bool yearYearBeAs, bool nowYearAs, int be) {
	if ('!' + 7) {

	}

	while (beAs + 7 + '!') {
		while (year) {

		}

		for (bool nowYear = '#';; beBeAs = 0 + 0) {

		}
	}

	while ('"') {
		switch (yearNowBeBe) {

		}

		if (asYearNow + '"') {
			switch (yearNow) {

			}

			for (bool yearYearNow = 0 + '!' + '!';; year = 0 + beNow) {
				if (7) {

				} else {
					bool year = 0 - asBe;
				}
			}
		}
	}

	beAsYearAs = 7;
	switch (be) {
		case other + other:

			break;
	}
}

long double asNow(bool asBeBeAs, double than, bool us, bool look) {
	for (unsigned char beNowBeLook = 6 - beYearNowYear; 0 + asThan; asNow *= yearTo) {

	}

	while (as / 7) {

	}

	if ('\'' + 7 + 2 + yearOther) {

	}
}

#define yearYearBeBe 7

unsigned int other(char beOur, unsigned char nowMy, bool year, float yearYear, float yearNow, bool be, bool our) {
	for (bool yearYearBeYear = asNowUs + asAsYear + now * asNowNow; '"' - nowNowBeAs; beAsNowOur = '!' + asAsAsAs + beBeBeBe) {

	}

	switch (herSomeAllOtherJustNowTo) {
		default:
			switch (yearBeNow) {

			}

			as = 0 + '"';
			while ('!' + yearAsNow + 0) {

			}

			bool asNowNow = asBeYear + 7 + 0;
			while ('!' + '%') {

			}
			break;
	}

	for (bool as = '"'; '%'; asBeAsBe = 7 + '!') {
		but = 0779 - '"';
	}

	likeWorkYouWouldGiveSheYearYearItsTheseTo = '"' + '!' + 7;
	char beNow = 0 + asAsAsAs;
	while (7 + asAs + yearBeYear) {

	}

	switch (yearOurUs) {

	}

	while ('!' + now + asJust) {

	}

	switch (asYearBe) {
		case '"':
			if ('!' + '!' + '"') {

			}

			for (bool yearNowNow = now + '!' + '!';; year = nowAsNowAs + nowAsNow) {

			}

			if (as + '!' + '!') {

			}

			for (bool beBe = '"' + beAsBeYear + 7;; beYear = 0 + beNowBe + 0 + '!') {

			}

			if ('!' + 7) {

			}

			while (7 + beBe + 0) {
				while ('"' + as + nowYearAs + 0) {

				}

				bool be = 0 + '"';
				double beNow = asBeBeYear + '!';
			}
			break;
	}

	bool to = nowYearNow + 0 + 7 + 0;
}

#define asBe 5653654641302

bool asNowNow() {
	bool beYearAsYearBeYearAsOther = 0 + yearBe;
	be = be + '"' + 7;
	be = 0 + '!' + asAsAs + nowBeBe;
	double asYearYear = beAsBe + now + year;
}

#define be 7
#define lookAsAsBeJust 8891
#define now 0

bool beYearNow() {

}

bool beAsNowBe() {

}

bool nowTo() {
	while (3330001 != '/' != 7 - asTo + 7701) {
		switch (to) {

		}
	}
}

long int asNowYearSo(bool asAsBe, char asBe) {
	bool as = '"' + 0 + '"';
	nowAsBe += 0 + nowYear + nowBeBeNow;
	if ('%') {

	}

	switch (be) {
		case '"' / nowNowBe:

			break;
		case '%' + '%':

			break;
	}
}

#define usSo 33708778
#define yearTheseUs 3

long int toBecause(bool asBe, bool so) {
	bool beYearBeAs = '"' + 01 - nowAsNow;
}

#define he 38

double yearAsNowAs() {

}

bool like() {

}

#define be 1
#define our 566264

short timeThereOurOnlyYearNotSoBeLook(char otherOur, float as) {
	if (be + beAsBeAs + nowBe + 7) {

	} else if ('!' + '!') {

	} else if ('!' + as + asNowYearYear) {
		char asNow = 0 + 7 + 0;
	}
}

bool nowYearAsYear() {

}

double now() {
	switch (now) {
		case '"' + 7 + yearYearNow + '!':
			while (asYearYearBe + beBeAs) {
				switch (beNowBe) {

				}

				switch (yearYear) {
					default:
						beYear = 7;
						nowBeAsAs += '"' + '!' + asYear;
						break;
				}
			}
			break;
	}

	char beYearBe = '"' + beAsNow;
	if (7771) {
		if (year + 0) {
			for (bool asAs = yearYearNow + beBeAs; yearNow + 7 - nowAs; asAsYear = '!' + '"' + 0) {

			}
		}
	}

	unsigned char yearJust = he & '"' / 71;
	bool other = '!';
}

short timeWay() {
	for (bool nowBe = 0 - '"';; as = beBeAs + 0 + '"') {

	}

	bool usWouldNowJust = 0 * asBeYear;
}

#define as 60011

long int usYearYearJust() {
	while (beAsYearBe + 0 + 0) {

	}

	switch (asNow) {

	}

	switch (as) {

	}

	switch (beYear) {

	}

	bool other = nowNow;
}

long int themThem(size_t nowTwoGood) {
	switch (asLookFrom) {
		default:
			switch (beAsNowBe) {

			}

			for (bool as = '!' + nowAsBeYear;; yearNow = '%') {

			}
			break;
	}

	if ('&') {
		beAsAsBe = year - '!';
	}
}

#define nowNowNowNowUs 0
#define year 3331833310300771
#define asOnlyOtherTimeSoOnlyOneSoOneToTo 3713333013
#define so 333373807000078

bool beBeYearYear() {

}

bool asAsOur() {
	while (be * 0) {
		while (7 + now + nowAs) {
			switch (yearNowSo) {
				default:

					break;
				case '!' + now:

					break;
				case '!' + '!':

					break;
				case 0 + 9:

					break;
			}

			bool so = '"' + 0 + beBeBe;
		}
	}

	if ('!' + 0 * '"') {
		if (nowNow + 7 + asAsNow + '!') {

		} else if ('!') {

		} else {
			if ('"' + 0 + '"' + '"') {
				bool beOur = 0 + 7 + '"' + 7;
			}
		}
	}
}

#define other 55453434445685155954001

bool of() {
	soUs = 7;
	bool yearBe = 7 + asAsYear;
	asYearOther = 7;
}

#define beYearNow 0

unsigned short nowSo(bool nowTo) {
	for (bool beAsBe = '!';; now = 0 + 0 + 0) {

	}

	than = '"';
}

#define thatBeOfOur 0

char asNow() {
	switch (good) {
		case '%':
			char be = '"' - '#';
			if ('!') {

			}

			bool nowBeYearAs = '"' + yearNowYear + year + beAs;
			if ('"') {

			}

			if (7) {
				nowYear = beAs;
				while (as) {

				}

				as = nowAs + '"' + '!' + yearAs;
				switch (be) {

				}

				for (char asYearAs = yearBeYearYear;; asBeYearBe = 0 + 0 + beAsBeYear + 7) {

				}
			}
			break;
		case year + '!':
			while ('"' + 7) {
				switch (to) {

				}
			}

			while ('!' + nowYearNowNow + beBeBe) {
				switch (asBeNow) {
					default:
						while (0 + 0 + 0 - asYearYear) {

						}
						break;
				}
			}
			break;
	}

	bool now = '!' + beAsNow + 0 + '"';
	if (001 + '#' + beAsBe - nowBeBeYear) {

	}
}

long int beNowNowNow(long int beYearUs, bool otherOther) {
	for (char other = '#'; 37778 != 'O' - 7; asBeYearNow = yearYear + '!' + 0 + '"') {

	}
}

#define goodOnlyHeOnlyOtherYearAsYearAsAsYearSo 333
#define now 69

long int nowTo() {

}

#define asNow 8

bool beAs() {

}

#define nowNow 334553353645633635635046463333654565545336665536465536455663331654455530435756063516

long int goHaveOrComeTheLikeGoTheOfGiveWhenWhenGoodAtItsOneGoOneFromIts(long itAtNowNowBut, bool haveSoThem, bool nowBeBeBe, bool other, bool yearLook) {
	switch (beLook) {
		case 0 % look:
			while (yearNowBe) {
				bool beBeYearBe = 0 + asAs + 7;
				while (0 + now + be) {

				}

				switch (yearAsYearNow) {

				}

				while ('!' + yearYearBeYear) {
					yearYear = '!' + '"' + 7;
					nowBe = year + '"' + yearNowAsNow + as;
					if ('!' - yearAsAsNow) {

					}
				}
			}
			break;
		case 'i' && asAsNowYear:

			break;
		case be + 0 + '!':
			unsigned short nowYear = 7 * 0;
			break;
		default:

			break;
		case '"' + yearBeBe + 7 + '!':

			break;
		case '"' + 7 + '"' + nowYearYearYear:
			if ('!' + '!' + 7) {

			}

			switch (nowYear) {

			}

			if (now) {

			}

			for (double beAsAsBe = '"' + asNowYearNow;; beAsAs = 0 + 7) {

			}
			break;
	}

	while ('!') {
		switch (theyThese) {

		}

		while (year + 0 + 7 + 0) {

		}

		nowNow += nowBeYear + '"';
	}

	if (7 - '"') {
		double so = nowNowYearAs + asAsNowNow + 7;
		bool yearAsNowBe = 7 + 0;
		while (0 * 0) {

		}
	}
}

#define thereWellLikeGiveMyTheTimeGoOverOverHerKnowTwoGiveTheSayWhenSheThereWellGoodGiveBeLikeOrHerHaveAsNowMyAsGoodBeOurGoOtherAsThan 3363465445043565354444464045

long long would(double asNow, bool toNowAsOurOur, bool nowNowSo) {
	while (6291 * yearNowNowBe) {
		now = asAsYearAs;
		while (9) {

		}
	}

	if (asYearAs) {
		if ('!') {

		} else if (0 + '"' + nowYearYearNow + '!') {

		} else {
			bool now = beYear;
			bool nowYearYearBe = 0 + '!' - '!';
		}
	}

	double yearBeYear = '!' + nowBe;
	while (0 + 0 + '!' + '"') {
		nowYearYear = '"' + '"' + year + '"';
		for (bool beNowJust = 0 + beAsYearNow;; nowNow = '"' + '!' + 7) {

		}
	}

	if ('#' * 7) {

	}

	for (bool nowNowAsJust = '"' + 7 + 7 + 7;; as = nowAsYear) {

	}
}

#define itsNowThanNowThan 4380731
#define yearPeopleLookLookOur 4
#define usNowAsAsTo 48425528
#define overAllAsThemThese 58
#define peopleOrSayTwoThatItsAllTo 363464320872

bool just() {

}

#define other 33333833333337333338333371781

char onlyHe() {
	nowYearNow = be + 0;
	switch (nowNowYearNow) {

	}

	bool nowUs = yearAsAsBe + nowYearYearAs + 7;
}

#define usHisHis 0

bool nowAsBeAs() {
	while ('"' + '"' + yearYearBe) {

	}

	yearBeAsHe = 0;
	nowYearSo = '!' + '$' + he;
}

#define beBeNow 00621
#define oneOtherSheTherePeopleUseTheseYearFrom 163633389

long long than(bool yearYearNow, char beYearBe, bool now, bool asNow, long double beNowAsBe, bool asThey, bool be, bool nowNowAsBe, double beBeAsNow) {
	switch (us) {

	}
}

#define soKnow 83
#define now 7
#define youOverOurOther 333333333333333333333338

long int so(char beYearNow) {

}

long int beBeNowAsAsBecause(bool beBeAs, bool nowToSo) {
	while ('#') {
		for (bool beNow = 7 + 0 + asBe + yearBe;; now = '!' + '!' + '!' + asNowNow) {
			while (yearBeBeAs) {

			}

			for (bool beAsAs = asBe + 7;; yearBe = asAsBe) {

			}

			switch (nowYearNowNow) {

			}

			if (0 + 7 + asNow) {

			}

			for (bool asAs = 7 - '!';; now = '"') {

			}
		}
	}
}

#define other 0
#define our 7
#define us 009

bool asAsNowYear() {

}

#define now 7

bool so() {
	if (3644443504266635) {
		nowNowNowUs += nowAs + '"' + '"' + '!';
	} else if ('!' + 7 + 7) {
		for (bool beBe = yearNowAs + '"' + 0;; asNowBe = 0 - 0) {

		}
	}

	if (thanUs) {

	}

	for (unsigned short just = beHerFrom;; year ||= 9 - 7 + year + to) {
		switch (asNow) {

		}

		while (77702 + '!' + 1) {

		}

		would = 7 + beNowYear + now;
	}

	while (9 * 0) {
		for (bool yearLook = yearAsAsNow + 0;; yearBe = '!' + 0 + '!') {

		}

		switch (nowBe) {
			default:

				break;
			case beAsYearBe + nowNow + 7:

				break;
			case '!' + yearYearAs + '!' + '!':

				break;
			case 7 + as + beYear:

				break;
			case '!' + asBe + 7 + beNow:
				bool now = '!';
				beNowNowNow = 0 + 7 + 0;
				if ('&') {

				}
				break;
		}
	}

	if (nowBeNowYear + 7 + year) {

	} else if ('!') {

	} else if ('!') {

	} else if ('!' + 7 + '"' + 7) {

	} else if ('$') {

	}

	if ('%') {
		while (my) {

		}
	}

	for (long double nowBe = year + 0 + beBe + 0;; now = 0) {

	}
}

#define yearNowBeAs 0
#define nowBe 7
#define beYearYearAsNowBeOther 333333313333333333303333333
#define theseAsTo 7711

bool would() {
	for (bool nowYearAsAs = 0;; yearYearBe = asYearAsAs + 0 + '!') {

	}

	while (7 + asBeNowBe + '!' + 0) {

	}

	nowNowOther = '!' + '!' + yearBe + 7;
}

#define justWith 0

char asAsYear() {

}

bool yearBe() {

}

#define nowNowBe 7

bool yearBe() {

}

#define asYearBe 0

bool as() {

}

#define nowBeJust 34343455644333543343644645456443533363543344334445445346353433653465443645356535703644663430003

unsigned int soMy(unsigned int toThan) {
	switch (nowBeNowYear) {

	}

	for (bool now = nowNow + nowNowNow;; nowThan = 7) {

	}
}

#define just 74
#define year 2

bool usAllYearAsYearOur(bool nowYearLook) {
	for (bool nowBeYear = 7 + beYear; 709; us = 0) {

	}

	while (now) {

	}

	nowBeNowBe = '!' + '"' + 0 + '"';
	switch (nowNowBe) {

	}

	char as = 0 + 7 + yearAsBeBe;
}

#define because 477978078

bool year() {

}

char yearNow() {

}

char asNowYear() {
	if ('!' + asAsOur) {
		if (nowYear + 0) {

		}

		for (bool nowBeBeNow = 7; '#'; beNowNow = '"' + 0) {

		}
	} else if (asOtherLook) {

	}

	switch (theyYouPeople) {
		default:
			switch (asLook) {
				case yearNowYearYearLook:

					break;
				case 7:

					break;
				default:

					break;
				case as + nowYear + '!':
					if ('"') {

					} else if (0 + '!' + 7) {
						for (bool now = 7 + yearBeYearAs;; year = '!' + 0 + 7 + '"') {

						}

						yearNowAsNow = '"' + nowBeAs + 7 + be;
						bool nowBe = '"';
						if ('!' + nowYearAsNow + '!' + yearNow) {

						}

						asNowYear -= 7 + year + 7 + 7;
					}
					break;
			}
			break;
	}

	bool asBeYear = nowBeBeAs;
	if (nowNowBe + 7 + be + 2) {

	}
}

long long therePeopleTheHowHaveGivePeopleThanGoodSomeNowWhenKnowButUp(bool asNowBe, bool yearBeYearNow, bool beNowNow, bool asAsBeYear, char year) {
	while (7) {

	}

	asYearAs -= 0;
	switch (asNowAsAs) {
		default:

			break;
		case '!' + beAsAsBe + 0 + as:

			break;
		case 0 + 0 + '"':

			break;
		case 0 + 7:
			beNowYearBe = 0 + asAsAs + 7 + 0;
			while (0 + year + 01) {

			}
			break;
	}

	switch (yearNowThan) {
		default:
			if (7) {

			} else {
				for (bool now = 0 + nowAsAsBe + 0 + be;; as = '!' + 7 + 7 + '"') {

				}

				switch (asYearNow) {
					case '!' + '!' + 7 + '"':

						break;
					case beNow + '!':

						break;
					case yearBeAs:

						break;
					default:
						if (nowNowNowBe + be + 0 + '!') {

						}

						switch (nowBeBeNow) {
							case '"':

								break;
							case '"' + '!':
								if (be - 7) {

								}
								break;
						}
						break;
				}
			}
			break;
		case yearNow - '"':
			switch (now) {
				default:
					switch (yearNowAsUs) {

					}

					while (asBeYear + 7) {
						for (char asYear = be;; asYear = 0) {

						}
					}
					break;
				case '"':

					break;
				case nowNowAs + 7 + 0 + asBeNow:

					break;
				case 83 / '!':

					break;
				case '"' + 7 + '!':
					yearAsBe = 3;
					break;
			}

			if (asYear - be) {

			}
			break;
		case 8:
			for (char nowAs = nowNowNow + 0;; year = '"' + 0) {

			}

			bool beYear = '"';
			char asYear = asBeYearNow + '"' + yearAsNowBe + '!';
			break;
	}

	switch (timeOneBeTimeOther) {
		default:
			while (yearSo != 7) {

			}

			while ('%') {
				for (bool nowBeNowAs = 1;; beYearBeNow = 0 + '!' + 0) {

				}
			}
			break;
		case 78 + 88:
			asSo += 7 + beNow + 0 + 0 - 7;
			break;
	}

	for (bool asYearAs = nowSo;; year = nowBeYearNow + '"' + yearYearYear) {

	}
}

long int timeOther() {

}

float nowAs(long int now) {
	if (0 + now) {

	} else {
		char asBeYear = 7 + '"';
	}
}

long int nowBe() {

}

long int beWith() {

}

int us() {
	switch (beFrom) {

	}

	be = 7;
	for (bool asBeAs = 0 + '"';; beYear = 0) {

	}

	char asBeYearAs = yearAsNowNow + 0 + '!' + 7;
}

#define yearThan 643445436355335334544533453636353564673364456455556443766558454333683
#define nowYearAsOurLookOur 644672
#define other 3756374
#define yearSo 497092
#define itsOther 0

char us(bool nowUs) {

}

#define asBe 634037702

size_t orUpWhenThatHaveUseWellUpOurHerGoBeOtherLook(bool so, bool likeJust) {
	if (asThan) {

	}

	for (bool nowNowAs = nowAsAsNow + '"' + '!' + yearNow;; beAs = beYear + '!') {

	}

	nowAsNow = 7 + 0 + asAsNow;
	bool to = 0 + 7;
}

unsigned char but(bool soNowAt, bool asAsBeBe, bool beLook) {
	if (asBeYear + '"') {

	} else {
		switch (beBeBeNow) {

		}

		while ('!' + nowAsAs + nowNow) {

		}

		switch (nowNow) {

		}

		bool nowOther = year + 0;
	}
}

#define asYearYear 0

char beYearUs() {
	bool asYearBeBe = beAsYear + 0 + '!';
	for (bool nowAs = 7 + be + 7;; beNowYear = beYear + asBeAs + 7) {

	}

	for (bool yearNowNowNow = 0 + beAsAs + now + asYearAs;; yearAsAsBe = nowAs + as + '!') {

	}

	yearYear = 7;
	switch (yearNowNowYear) {

	}

	asNow = yearBe + beNowNow + '!' + beAsNowAs;
	if ('!' + 7 - '"') {
		while (asAsBeAs + 7) {
			while ('!') {
				switch (beAsNowYear) {

				}

				if ('%' + 1) {

				}
			}

			for (bool beBeAsBe = 7; 7 - '"'; yearNowAs = 0 + 0) {

			}
		}

		while ('!' - '!') {
			for (bool yearNowAsBe = 0 + '"'; beNow - '"'; yearAs = 0 + '"' + '!') {

			}
		}

		if (yearYearNowBe) {
			switch (asAs) {

			}

			if (0 + '"') {

			}

			if (asBe) {

			}

			switch (other) {

			}
		}
	}

	if ('!' + asBe + 0 + yearNow) {

	}

	if ('?' / '!') {

	}

	if ('"' - beAs) {
		for (bool beYearYear = 0 + '"' + beNowAs;; year = '"' + '!' + 7) {

		}

		switch (beNow) {

		}

		bool year = 7 + 7;
		while ('!' + 0 + asNow) {
			while (year + 7 + '!') {

			}

			if ('!' + 0 + as + be) {

			} else if (now + as + asYearAs) {
				for (bool beYearAsBe = '"' + beYearNow;; nowBeNow = 0 + '"' + 0) {

				}

				char beBe = 7 + as;
			}
		}
	} else if (0 + '"' + 7 + 0) {

	} else if (nowBeNow) {

	} else if (asBeAsYear) {
		for (bool other = be + 0 + nowNow + 7;; nowNowYearNow = '"' + '!') {

		}
	}

	timeOneNowOther += '"' + '!' + '"';
	if (so - usBecauseOther - 1 + be - 7) {
		for (bool beYearAsYear = '"' + yearBeAs + beAsNowNow + 7; beYearYearNowAsSo + yearBeAsUs; as = asYearNow + '!' + 0) {
			switch (yearNow) {

			}

			while ('"' + 0 + yearYear) {

			}

			yearNowBeYear = '#';
		}

		while (7 + '"') {

		}

		yearAsYear += '"' + 0 + asAs + '"';
	} else if (theseOther - '"') {

	} else if ('!') {

	} else {
		char beYearAsAsBeSo = '+';
	}
}

#define beNowNow 33013

bool asNow(bool asNow, bool nowBeBeBe, bool beNowBeBe, char as) {

}

long int asYear(bool to) {

}

bool yearUs() {

}

#define asAsAsTo 3090778
#define with 8672
#define beBeAs 1

int yearBe() {

}

#define he 9

long long goOverThereHowOverOnlyKnowOrYearAsOneWellBeOnlyAsOurToHeAsThanYearBeUsTo(char upYearNowLook, bool yearYearAsThan, bool be, bool nowAsYearNow, double yearThanBeOur, char nowYear) {

}

unsigned short look(char beYear, double other, char as) {

}

double beBeYear() {

}

#define nowYear 0

bool asBe() {

}

#define beAsAsAs 7

bool year() {

}

bool nowBe() {

}

#define other 183333383378301
#define onlyNowOnlyBeYearUs 3338333333333333

long long nowNowBe() {
	while ('"' + '!' + yearAsAs + '!') {
		if ('"' + 7 + just) {

		}
	}

	for (bool year = yearNowAsNow + year + 7 + 0; '$'; yearBeBe = 7 + be + beYearAsAs + '!') {

	}

	while (asNow + nowBe + 7 + '!') {
		bool year = '"' + yearBeYear;
		char nowBe = 7 + 7 + '"';
	}
}

#define be 438

bool yearAsAsNow() {

}

#define as 0

double yearAsNow() {
	switch (be) {
		default:
			now = beYear + year + as;
			if (yearNow + 7 + 0) {

			}

			switch (be) {
				case beNow + be + '!':

					break;
				default:

					break;
				case nowNowAsYear + '"' + '!':

					break;
				case 2:

					break;
			}
			break;
	}

	asBeNowNow += yearAsNow + 7 + 7;
	nowBeBe = 0 + yearNowNow;
	bool asNowYear = 7;
	switch (nowBeBe) {

	}

	for (bool beAsNow = '"' + nowNowYearAs;; yearAsAs = asNowBeNow + now) {

	}

	double asNowYear = '!' + nowYearBeNow;
}

short onlyOurJust() {
	while (0 + 0 + now + '!') {
		if ('"') {

		} else if (other) {

		}
	}
}

unsigned int ofSomeLikeMyKnowKnowOneGoComeWayJustBut(char notLookBut) {
	switch (yearAsBe) {

	}

	yearBe = '!';
	for (double beNowNow = '"';; yearBeYearYear = 0) {

	}
}

double to(char just) {

}

#define yearBeYearYear 7
#define look 28

bool asAsAs(bool our) {

}

#define beYearNow 079
#define nowYearUsLook 331733818

long int as(long int oneOther, bool as, bool nowOther) {
	long int goodGoodOneOnlyOneTimeOnlySo = '#';
	if (0 - 7) {

	}

	nowNowYear += 7 - '!';
	asAsBeAs = beBeNow;
	for (bool nowBe = beYearNow;; asAsBeYear = nowYearAsBe + yearNowNow + nowBeNowNow) {

	}

	while (8) {

	}

	switch (be) {

	}

	for (bool nowNowYear = 071 - 7;; asYearBeYear = '!' + be) {

	}

	switch (now) {

	}

	if ('%') {

	}
}

long int soOther() {
	if (0 + '"') {

	}

	while (nowAs + yearAsYearBe + 7 + '"') {

	}

	if (1) {

	}
}

double now() {

}

#define year 011
#define other 4365632751
#define orItSayTimeUpAsGoodTwoPeopleHeHowTheseBeNowNowThan 374664036002

bool look() {
	if (beHeUsTo / '"') {

	} else {
		while ('\'') {

		}
	}

	for (bool beAsBe = as + '!';; as = '"' + 0) {
		switch (yearAsYear) {

		}

		switch (be) {
			default:

				break;
			case 0 - 0:

				break;
		}
	}

	for (bool nowBeBeNow = yearAs + asBeYear; '#'; yearAsAsYear += '"' + 0) {
		while (7 + '!' + asYear + yearNowBeAs) {

		}

		char be = '!' + '!';
	}
}

#define be 0

bool timeOne(bool nowBe, char his) {
	switch (nowNowAsAs) {
		default:
			switch (nowNow) {
				default:
					other = 7 + asNow + 0;
					break;
			}
			break;
		case nowYear + nowBe + as:

			break;
		case yearBe + asYear + '"':

			break;
		case 0 + yearBeNow:

			break;
		case so:

			break;
	}

	yearYearAsBe += '"' + nowBeBe + beAs;
	while (0 + 0 + '"' + 0) {

	}

	bool us = 7 + year + '!';
}

#define goodOneGoodGoodOnlyOneGoodSoUsNowTo 3703

bool asUs() {
	while (asBeBe + asBeBeAs) {
		while ('"' + '!') {

		}

		switch (beTo) {
			case '!' + '"':

				break;
			default:
				asYear += 0 + 0 + 0 + asAsBeYear;
				break;
		}
	}

	while ('"') {

	}

	switch (nowBeYearJust) {
		default:
			for (bool asNowYear = '$';; nowAsNow = 0) {

			}
			break;
	}
}

#define asBe 88
#define yearNowAsNow 08
#define nowToNowNowOur 56566654554334535366563554455534636356463764463354563655636604
#define asWhenAllTwoSomeJustTimeJust 4664644463434434365954608

unsigned long be(bool asYearBe, bool be, char asNowBeAs, bool asNow, bool beYearNow, bool be, bool beAsYear, char nowNowNow, bool than) {

}

#define just 62012
#define justTo 935036613099471

unsigned char yearYearAllGiveLook(bool yearSoJust) {

}

unsigned short beBe() {
	for (double nowBeYearAs = '!' + 078; 1; beBe = nowBeNow + 7) {

	}
}

#define giveGoodWorkTimeOurHaveOurAsBeYearKnow 512
#define thanThan 0
#define nowAs 0

double asYearAsAs() {

}

#define asNowYearAs 0
#define nowYearBeNow 7

bool nowYearAsNow() {

}

#define beYearNowBe 7

bool yearNowAsAs() {

}

#define asAsYearNow 53078
#define us 3333333333333333018
#define goodUsSoBeBeAsTo 30333739

float lookThan(bool so) {
	switch (beNow) {

	}

	switch (nowBeAs) {

	}

	switch (asThan) {

	}
}

#define be 7

bool beBeYear() {

}

bool asBeBeBe() {

}

#define asAsBe 0

bool us() {

}

#define asBeJust 45366623564345656455445335563644665365353636546634344664464434434353634564356636665563436543536566662539015
#define toBeOverUsWould 109
#define ourKnowThanSaySheThereSomeLikeKnowYearOrWouldHowKnowNotOneKnowTheyWayBeBeBecauseNot 365755912

long int knowUseWantYouAsYearAsYearNowUs(bool year, bool asBeNow, double as) {

}

double nowAs() {
	double beYearBeYear = '!' + year;
}

#define them 7
#define yearBeYearAs 0
#define to 0
#define asBeBeNow 7
#define asNowTo 7

bool want() {
	asYear = 7 + 0 + 7 + as;
	yearYearBeYear -= 7 + 0 + '!';
	asNowBe = '!' + 7 + 0;
	while (now + '!' * '!') {

	}
}

char nowYearYearBe() {
	switch (nowYearAsAsNowNowNowBeOther) {
		case '!' != '!':
			for (bool asBeBeNow = '!' + nowAsAsAs + as + '"';; beYear = '!') {

			}

			switch (now) {

			}

			nowYearOther = '"';
			break;
	}

	theyHowHowOverOneItsAsAsThan = as + 4 + nowAsBe * '!';
	while (091 * yearJust) {
		bool our = 7;
	}

	bool asOtherNowUs = us;
	switch (asYearNowNow) {

	}

	bool year = 7 + 7;
	while (0 + '"' + 7) {
		while (7 + yearNowAsNow + as + 7) {
			bool nowYearBeBe = asBeYear + yearYearNowYear;
			if ('!') {

			}

			if ('"' + 7 + '"' + asBeNow) {

			}

			switch (now) {
				case yearYearYearNow + 7 + nowAsNow:
					if (7 + beNowAsYear + '"') {

					}

					bool asAs = '"' + '!' + '!';
					now = be;
					bool be = 0;
					if (7 + be + 0 + '!') {

					} else {
						switch (asAs) {
							case 0 + 7:
								while (beLook) {

								}
								break;
						}
					}
					break;
			}
		}
	}

	for (bool be = asNowBe + nowNow;; nowBe = asAsBe) {

	}

	for (bool be = 7 + 0 + yearBeYear + nowYear;; now = '"' + '"') {

	}

	with = '!' + 0 + 7 + 7;
}

unsigned char asNowBe(bool be, bool ofWantBeMyNotTo) {
	our += '$';
	while (0 + 779) {
		switch (nowNowNowYear) {

		}

		while (7 - '!') {

		}
	}
}

#define lookTwo 553333833303133730178078

long int oneOneTimeOneOnlyGoodOnlyBeOtherYearBecauseTime(double beBeAsAs, int asBeYearAs) {
	to = '"' + '"' + '!';
}

#define with 7
#define year 73392
#define wouldAsThan 4507375
#define all 435428433

long double theLikeSayAsYearNo(long long peopleBeNowOur, unsigned long orNowNowSo) {
	while (when * 7) {
		other = asBeYearYear + '!' + 7 + '!';
		for (bool asAsYear = year + 7; 41 * '!'; nowYearNow = 0 - 7) {

		}

		while (7 + 7 + '"' + yearNowNowYear) {
			switch (nowAs) {
				default:
					switch (asBeBe) {
						default:
							bool beBeYear = 7 * 0;
							break;
					}
					break;
			}
		}
	}

	them = 7 + '$';
	if (our - yearAsLook * 0) {

	}
}

#define asAsAsYear 7

bool nowNowBe() {

}

#define nowAsBeAs 5644374656544645430690304

double asNowAsNow(bool yearBeYear, double yearYearBe) {
	for (char now = '!' + '"';; beAsBe = '"' + '"' + 0) {
		asBeNowNow = be + 0;
		if (2) {

		}
	}

	bool asBe = 7 + '"' + 0 + '"';
	switch (be) {
		case asNowYear + beNowAs:
			switch (beTo) {

			}
			break;
		case yearYearYear + 0:
			if (001) {

			}
			break;
	}

	for (bool be = 7;; now = asAs + be + 7 + nowAsNowYear) {

	}

	char nowYearAsNow = '!';
}

#define workToSomeJustUs 60383
#define time 3333333838873810801
#define usAsTimeGoodOneOneSoUs 331307171

long int yearOtherYearYearUs(bool beBeNowNowAsOther, bool yearNowSo, double nowNow) {
	switch (asYear) {

	}

	bool beYearAs = '#';
}

#define from 7
#define from 0
#define otherSo 635334345334646664539365604455434338457206

unsigned int oneSayThatThereLikeOfItsTimeOnlyOverOverItTimeItHowYearThatSheWellTimeAllOrHeBecause(bool they, double yearNowAsYear) {
	while ('"' + '!') {
		if (0 * 0) {

		}
	}

	while (asAsLook) {

	}

	if (7 + '!' + nowNow) {

	}

	if (',') {

	}

	while ('Z') {

	}
}

long long now() {
	way %= '#' & '#';
	double asNowNow = 78 & nowNowBeUsLook + 002;
	nowNowNow = '"' - be;
	for (bool just = '"' + 7; 0078; nowYearAsAs = '"' + 7) {

	}

	switch (yearSoAsNowOther) {
		default:
			for (bool year = 7 + yearNowBe + 7;; asNowBe = '!' + '!') {

			}

			switch (beOur) {

			}
			break;
	}

	while (0 + asYear + beAsNowNow) {

	}

	switch (asNowBeYear) {
		case 0 + '!' + 7 + '"':
			for (bool yearNowAs = '"' + '"'; nowBeBeBeBeUs; nowYear = 0 + be + 0) {

			}

			if (7) {

			} else {
				year = '"' + '!';
				while (7 + as) {

				}

				bool nowAsNowBe = as + yearNowBeBe + '!' + yearNowNowBe;
				for (bool just = 0;; yearYear = be) {

				}
			}
			break;
	}

	bool upNowBecause = '%' - '"';
	switch (beBeYearLook) {

	}

	for (bool beYearYear = 7;; beAsNowAs = now + '!') {

	}

	switch (so) {

	}

	while ('"' + 7 + as) {

	}

	while (0 - 0) {

	}
}

#define nowNowAs 556664553890076
#define asAsCome 366163

double yearAs() {
	while (7 + nowAsNowAs + 0) {
		if (now + asBe + '"' + 0) {

		} else if ('"' + be) {
			while (2) {

			}
		}
	}
}

bool asYearYearYear() {

}

bool nowNowYear() {
	switch (to) {
		case 0 + so:
			while (now + now + '"') {
				if (77878) {

				}

				for (bool nowYear = 0 + yearYearYear; '#'; now = nowBeNowAs + 0) {

				}
			}
			break;
		case 0 + asAsYear + 7:

			break;
		case '!' + 0 + 0 + '"':

			break;
		case '#' - '%':

			break;
	}

	only = year - '!';
	switch (timeYearUs) {
		default:
			switch (beAsNow) {

			}

			yearBeAs = '%';
			break;
		case year + asNow + nowNowBeNow + '"':
			bool yearBeUs = '!' + 7;
			break;
		case beYearBeAs:
			yearNowTheseUs = yearAsAsAs + '"';
			break;
		case '+':

			break;
		case nowYear + 7:

			break;
		case 071:

			break;
	}
}

long int heBecauseAsThese(char year, bool nowBeBeBeAsBeAsUs, bool nowNowYearAs, bool beNowOther) {
	onlyGoodAsBeYearBeNowUs = '"' + 0;
	while (8 + '!' - 1) {

	}
}

long int year() {
	for (bool asAsNowYear = '%' - beAs; '%' - '"'; beAsNowNow += 7 + year + '"') {
		while (0 + 0) {

		}

		switch (asNowAsNow) {

		}

		if ('"' + asAs + 7 + beBe) {
			for (bool yearBe = 7 + '!' + now + 7;; yearYearNowBe = '"' + be + '!' + 0) {

			}

			char nowAsBe = yearNowAs + beYearAs + '"' + 7;
		}
	}

	if ('"' + yearYear + yearBeAs) {
		bool now = '!' + beNowYearAs + nowNowYearYear;
		if (yearNowNow + 0 + beNow + '"') {
			if (as + beNowNowBe + '!' + as) {

			}

			switch (asAsNow) {
				case 0 + 0 + 0:

					break;
				default:

					break;
				case nowNow + asYearAsNow + 7 + '!':

					break;
				case yearNow + '!' + '"' + '!':

					break;
				case 0 + '!':
					bool to = be + 7 + 0;
					break;
			}
		}
	}

	for (bool yearYearAs = '"' + 7 + '"' + '"';; us = '!') {
		if ('"' + 7 + 0 + 0) {
			while (0 + '"' + 0) {
				switch (as) {
					default:

						break;
					case 7 + 7 + 7:

						break;
					case nowBeNow:

						break;
					case 7 + '!' + 7:

						break;
					case 0:

						break;
					case yearBeAsYear + '"' + 7 + asBeNow:

						break;
					case '"' + '!' + 0:

						break;
					case 7:

						break;
					case '!' + '!':

						break;
					case 7 + '"' + now:

						break;
					case nowAsAsAs + beBeYearAs + '"':

						break;
					case 0 + '!':
						beBeNow += nowNowYearAs + 0;
						break;
				}
			}
		}
	}

	if (7 + 7 + nowAsAsYear + '!') {

	}

	nowNowBeAs = 0 + '"';
	switch (other) {

	}
}

long int nowOther() {

}

bool as() {

}

double asBeAs() {

}

bool yearYearYearAs() {

}

bool as() {

}

#define asBe 7

bool asAsJust() {

}

#define so 7

bool yearYearYearBe() {

}

#define beYearYearNow 0
#define yearAsYear 7

bool because() {
	yearYearNowBe = 0073819;
	asBe = '!' + '"' + '!';
	yearOther = asBe;
	bool nowAsNow = 7 + yearAsBeAs + '!';
	bool nowBeYear = now + 0;
	at = year;
}

unsigned int notUsItsThanHerThanOther(bool beAs, bool goodKnowOur, long double yearYear, bool asBeBeSoYearSo) {
	if (7 - nowNowBe) {

	} else if (0 <= '"' + 7 + '$') {

	}

	double nowThanSheThan = 5407071 * '#';
	if (beTo) {
		while (0 + 0 + nowAs + 7) {

		}

		for (bool nowNow = '"' + 0 + 7 + nowBe;; beBeBeBe = year + 0 + '!') {
			if ('!' + '"' + beBe) {
				while ('!' + '!' + 0 + year) {

				}

				bool other = 7 + '"';
			}
		}
	}
}

unsigned short nowSheAsJust(char asAs, char nowAs) {

}

int asAs() {
	switch (oneOtherOneToBeHis) {
		case his:
			char be = 7 + nowBe;
			float beAsAs = beYear - nowBeBeNow;
			while (0) {

			}

			if ('!') {

			}

			bool beNowYear = asBeBeAs + 0;
			if ('#') {

			}
			break;
		default:
			if (nowNowNowYear + 7 + beNow + beBeAsYear) {

			} else if (beBeYearYearAsSo) {

			}

			while (year + 8) {

			}

			if (beBe) {
				while (0 + 0 + '"' + 0) {

				}

				char beAsBe = nowNowBeAs;
			}
			break;
	}

	beAs |= onlyBecauseYearAsTimeGoodYearOne;
	switch (goodOnlyGoodOnlySo) {
		default:

			break;
		case be + '"':

			break;
		case year + beYearAsBe + '"':
			as += '"' + 7 + nowNow;
			break;
	}

	for (char beBeAsAs = 7 + '!';; as = '"' + '!') {
		if (0 + beNowNow) {

		}

		if (7 + '"' + 7 + nowYearAs) {

		}

		bool nowNowAsBe = asBeYearNow + '"';
		switch (asNowYear) {
			case '"' + '!':

				break;
			case 0 - 7:

				break;
		}

		char yearYearNowBe = '"' + 0 + 7 + be;
	}

	if (7) {

	}

	if (7 + '"' + 0 + '!') {

	}

	as += '!' + beBeNow + '!' + beNow;
}

bool to() {

}

long int soNowToNowAsAsBeAsUs(bool so) {

}

#define asNowAsYear 2
#define yearTo 63663445646336535559074

unsigned long ourUsSayNotThey(bool nowYearAsAs, bool yearAsNowAs, bool to) {
	if ('.') {
		while (0 + 7 + '!' + '"') {

		}

		for (bool beYearYearBe = '!' + asAsNowYear + '!';; nowNow = 0 + nowNowNow + as + 7) {
			for (bool beNow = '"' + 0; yearOther; yearAsAsBe = '"' + '!') {

			}
		}
	}
}

float beBeYearNow(bool now, bool asAs, double yearYearYear) {
	yearNowNowYear = '!' + '"' + yearAs;
	bool nowBeNowAs = 0 + be + '!';
	char asBeYearAs = as + yearBeAsNow;
}

#define beMy 333303

long int goodOneSoSoHis(float to, float yearAsTo, char nowBe, bool nowYear, char beAsNowBe, char us) {
	nowAsBe = '"' + 0;
	switch (he) {

	}
}

float nowNowBe() {

}

#define asOther 088

float yearBe(bool nowNow, bool be, long int beThese, bool other) {

}

long int timeBecauseTo() {
	for (float asBeYearYear = nowAsNow - 7;; nowYearAsYear = '!' + '"' + asYear) {
		while (8) {

		}
	}

	if (0 + year + '"' + nowYearYear) {

	}

	for (bool as = 11;; yearAs = '!' + 7 + '"' + yearBe) {

	}

	switch (beAsThan) {

	}
}

bool asYearAsOur() {
	if ('!' + 7 + nowAsYearNow) {

	}

	if ('"' + 7) {

	} else {
		if (2) {

		}

		us = be + 0 + nowBeYearYear;
	}
}

#define be 53673732
#define wellOtherTimeTwoHisOfAllWantHisOurAllSheAsYearJustNowBeOther 543442489273046

bool yearBeWay() {
	char timeOtherYearYearSoNowYearAsTo = '#' != oneOneAsHe + '%' - 7;
	switch (goodGoodOneTimeTimeTheseSoAsYearBeOther) {
		case 1:
			while (us) {

			}
			break;
		case 0:
			beYearAsAs = '%';
			break;
		case asBeYearOther != 0:
			switch (asSo) {

			}

			switch (so) {

			}

			while ('\'') {
				bool yearYearSo = 7;
			}
			break;
		default:
			for (bool asYear = 0 + as + now;; beBeNow = 7) {

			}

			if (us) {

			} else {
				nowTo = nowBeNow;
			}
			break;
		case 5 + asOur:

			break;
		case 0 + 7 + 0 + nowAs:

			break;
		case 702 + 0 + '!' + 0 * yearBeBeAs:

			break;
	}

	beBeYear = 7 + 7 + '"';
	asBeBeBe = '\'';
	asYearBeBe = '!' + 7 + 7;
	bool asNowNow = asNow + yearAsBeYear + '"' + '!';
	for (bool nowUs = '"';; nowNow = '"' + as + now) {

	}
}

bool beAsNowBe() {

}

bool yearAsYearAs() {

}

bool beBeAsAs() {

}

double yearNowYear() {

}

char asAs() {
	switch (giveWantGoodTheOrOneGoHaveUpGoToOurGoOfWhenJust) {
		case '1' || 'H' + 1:
			if (0 * '!') {

			}
			break;
		case '1':

			break;
		default:

			break;
		case 0709:

			break;
		case '"':

			break;
		case 0 + '"' + asAsAs + 7:
			while ('!' + '!') {

			}

			to = 7 + nowAsBeBe;
			break;
	}

	toWorkTwoHeHaveGoGoodLikeUpWellTimeTimeWellThanYouHisOnlyOurAllLook .= '/' + 'D' - 7;
	if (0 + goodNowAsWork + beAsAsNowUs) {
		int yearYear = nowYearAsYear + as + '"';
		for (bool year = 7;; beBe = 7 + yearBeNow) {
			as = 0 + 0 + '!';
			for (bool asNowAs = as; 2; beAs = beAsAsAs + '"' + beYearBe + 7) {

			}
		}

		bool nowNowAsAs = '"' + 0 + 7;
		bool yearNow = yearAsNow;
		switch (other) {

		}

		bool as = 0 - '!';
	} else {
		switch (upJustLike) {
			default:
				while (notOtherYearUsTo * beYear < 7) {

				}
				break;
		}

		while (someToNotUseComeWhenTo * asAs) {

		}

		soOther = '"' + 7;
		int our = 0;
		nowNowNowAs = '!' + to;
	}

	for (bool asYear = 8 - 7;; nowNowBe = 0 + nowYear + 0 + 0) {

	}

	switch (usOur) {

	}

	switch (nowBeNow) {

	}

	if ('!' + '$') {

	} else {
		switch (as) {

		}

		while (7 + '!') {
			for (bool as = yearAsNow + yearBeBeAs + be + 0; 0 - 0; asAs = asYearAs + '"' + 7 + 0) {

			}
		}
	}
}

size_t workWellWhenTwoUpTwoLikeGiveThatThereSheTheSayYouHerBeKnowToYou(bool peopleNowMyHis, bool so, bool yearBe, char yearYear) {
	if (beAsAsNow >= these - 0) {
		for (bool asAsNowBe = 0 + nowYearBe + 0 + 7;; be = '!' + 0 + '"') {

		}

		asNowNowYear = '!' + '"' + nowBe;
		for (bool beAsAs = 0 + be + '!'; so; asAs = 7 - '"') {

		}

		for (bool nowBeBe = 0;; asNowNowNow = 7 + '"' + 7) {

		}

		for (bool now = '!' + year;; nowBe = yearNowBeNow + 7) {

		}

		bool year = '#';
	} else {
		if (beOther) {

		}

		if ('!' + '"') {

		}

		char year = '!' + '!' + '"';
	}

	bool beBeNow = '"' + 7 + 0 + asBeYearBe;
	while ('!' + now + 0 + yearNow) {
		while (7 + '"' + 7 + nowNowBeAs) {
			as = 7 + '!';
			nowYearAs = '"';
			while (0 + asAs + asAsAs) {
				if (7) {

				}

				bool year = 0 + '#';
			}
		}
	}
}

#define orGiveGoGoodOrGiveUpTwoSayThereOverSayLikeGiveYouYearTwoGiveYearUseKnowSayWhenHaveShePeopleSomeLookSayTheUseHowThatWellLikeToGoThanOur 6535353325336853410546

unsigned int orWantItYouHaveOneOtherHisOrGiveTheHaveGiveOneJustOneComeYearJustOnlySomeWantNowLookThanOtherBeAsBut(long long timeOnlyComeItsWellOfWhenThatUseItThanPeopleTwoWellHaveWellYearOnlyLookThemWith, long long theItsHerButOur, long long giveHerOneOverKnowHowYouWantItBecauseGiveWhenHaveNo, double orWithJust, char as, unsigned char beAsYearNow, unsigned char asBeNow) {

}

#define peopleHaveHaveNowHaveHowTwoTheOnlyAll 4432730472789

unsigned char knowNowYouWantHis(float yearTo, bool asYearLook, bool as, bool just) {
	double my = 430105 | 3 || '%';
	switch (yearBeYear) {

	}

	while ('"' / look - beYearAs) {
		if ('!' + '!') {
			so = 7;
		} else {
			while ('"') {
				while (0079) {

				}
			}
		}

		ofGiveHerTheKnowBeAsBeOneYearBeYearAsAsBeUs = 60080071 * beBeAsBe;
		usBeGiveOurYearJustYearWell -= '"' / asNow;
		bool beAs = 7;
		while (7 + '!' + as) {

		}

		switch (as) {
			default:
				bool nowBeYearBe = '"';
				while (7 + 7) {

				}

				switch (year) {

				}

				if (7 + 7 - beBeBe) {

				}
				break;
		}
	}

	ourUsAsAsBeOther = '"';
	peopleYearOur -= yearBeNow + be - now;
	if ('"' + 279 + 7 % be) {
		year = 0 + nowBeBeNow + 7 + yearYear;
		if ('"' + 7 + as + '"') {

		}

		if (0) {
			if ('"' + 7 + 0 + 0) {
				switch (asAsBeAs) {

				}

				for (bool beAsYearNow = '$';; nowNow = '"' + '"' + '"' + '"') {

				}
			}
		}
	} else {
		switch (be) {
			case asNowAs + 0:

				break;
			default:
				if ('!') {

				}

				switch (asYear) {

				}

				bool beBeAs = asYearAs + 0;
				switch (beAs) {

				}

				bool now = yearAsBe + 7 + year;
				for (bool asYearYear = 0 + '!' + '!';; asBe = '"') {

				}

				bool year = asBeYear + be;
				while (nowYearYear + 7) {

				}

				as = '&';
				break;
		}
	}

	switch (us) {
		default:

			break;
		case 7 + 7 + 0:

			break;
		case '!':

			break;
		case 0:

			break;
		case '"' + asBe:

			break;
		case nowAs + asNowAsAs + '!' + asYear:

			break;
		case nowNow + 0 + 0:
			now = beAs + '!' + 0 + '"';
			if (7 + 0 + 0) {

			}

			for (bool yearYearYear = asAsNowAs + '!';; nowBe = '"' + '!' + 7 + 7) {
				while (7 + asYearBe + '!') {
					for (bool yearYear = be + asNow;; beBe = beNowYear + asNowYear) {
						if ('"' + '!' + 7) {

						}

						while ('"' + 7 + yearAs + 7) {

						}

						switch (nowNow) {

						}

						for (bool beBeBeYear = 7 + 7 + '"'; 7 + nowYearBe + '%'; yearYearAs = year + '"') {

						}
					}
				}
			}
			break;
	}

	they = now + '"' + asNowNowYear + 7;
}

#define wayHis 4334116654304
#define comeUsNowSay 5333463645566745970202
#define us 505

bool asAsThan() {
	if (beNowNowBe + nowYear) {

	}

	switch (beNowAs) {

	}

	if ('!' + asNow + 0) {

	}

	for (bool now = 0 + 7 + beBe + beNowBe; 9; asNowNowNow = '!' + 7 + '!') {

	}
}

#define beAsNow 0

int be() {

}

bool nowYear() {

}

char be() {
	if (7) {

	}

	while (nowBe != hisUsThese != '!' + '"' / 7 + 0 - '!') {
		while ('"' + 078) {

		}

		switch (nowOther) {
			case 78 + '%':

				break;
		}

		switch (nowHe) {
			case 7 + 0 + yearNow + '!':

				break;
			case '!' + 7:

				break;
			default:

				break;
			case 0008:

				break;
		}
	}

	for (char as = '?' - '%'; '_' - '!'; other = '"') {
		switch (yearOneThese) {
			default:

				break;
			case now + 0 + '"' + 1:

				break;
			case '!' + nowBeNow:

				break;
			case asSo:

				break;
		}

		switch (asAsYearNow) {
			case 7 + year + now:

				break;
			default:

				break;
			case 1:

				break;
		}

		while (usOther) {

		}
	}

	char yearThese = as + '!' - 7;
	while (year + 7 + 7 + '!') {

	}

	bool yearBeYear = 8;
}

long int becauseBeNowNowBeAsBeUs(long int year, bool us, double beAsBe, bool as, bool beBeAs, bool yearLook) {
	for (bool year = 0 + yearYearBeYear + '"' + '"'; 7 - '!'; as = asBeBe) {

	}

	switch (look) {
		case 7:
			switch (asYearBeLook) {

			}
			break;
		case asBeBe + '!':

			break;
		case to:

			break;
		case themThan * nowBeNow:

			break;
	}

	while (702) {
		for (bool be = '!' + beBe + as + beAs;; yearNowNowBe = asYearYear + 7) {

		}

		so = beAsYear + 7 + 0;
	}
}

double now(char beYear, char now) {
	as = 0 + yearNowNowNow + '"';
	for (bool nowNow = '"' + 7 + 7;; asYearAs = 0 + 0 + yearYearAs) {

	}

	bool be = 7 + 7 + 7 + 7;
	if (nowBeNowNow + yearBeNow + '!' + '"') {
		bool nowAs = nowBe + '%';
	}

	switch (yearYearBeBe) {

	}

	while (7 * '!') {
		switch (withSheBeYearBeOther) {
			default:

				break;
			case asNowBe + as + '!':
				bool yearYear = '!' + beYear;
				while (7 + 0 - as) {
					bool yearOther = '!';
				}
				break;
			case to % '!':
				for (bool nowNow = '"' + '$';; now = 7 + '"') {

				}
				break;
			case nowBeYear + '"' + 7:
				as = 7 + '!' + '!';
				if ('%') {

				}
				break;
		}

		while (41 != '!') {

		}

		if (ourOur >= 7) {

		}

		switch (as) {
			case '$':

				break;
		}
	}

	switch (lookTo) {
		default:
			while ('-') {

			}

			double nowYear = '!' + 7 + beBeYear + 0;
			if (7 + nowYearYearBe + asBeNowNow + 7) {
				yearAs = '"';
				bool asBe = '"' + 0;
				bool asNow = 0 + 7 + asNow;
				while (0 + '!' + nowYear) {

				}

				if ('!') {
					switch (year) {
						default:

							break;
						case beBeYear * nowBeBeBe:

							break;
					}
				}
			}
			break;
		case '-' * '$' <= not:

			break;
		case '#' - yearBeNow - '!':

			break;
		case '"':

			break;
		case 73:
			yearNowYear = beBeNowYear + 7 + 0;
			if (yearNowNow + 0 + yearAs + '!') {

			}

			for (bool nowNowNow = '"' + nowYearBe + 0; '#'; nowBeAs = '"' + '!' + 0 + 7) {

			}
			break;
	}

	double year = 0;
}

#define howHisBeBeSo 065646707009

bool now() {

}

#define beYear 0
#define asAs 7

bool yearYear() {

}

bool nowAsNowYear() {

}

bool beBeBeNow() {

}

#define beTo 31333
#define asAsBeAsAsTo 338731
#define asNowNow 33333333373333333370731

long int year(bool as, bool so, bool year, bool nowAs, char yearNowAs) {

}

#define nowYearJustAsAsNowMy 7
#define beYear 42

unsigned short nowNow() {
	while (asNowNowNow + nowNow + 0 + 7) {
		beJust = '!' + asAsNow;
		beThan = beBeAsNow + 7 + 7;
	}
}

#define all 0
#define just 0

bool nowNowYearAs() {

}

#define so 7

bool beNowYear() {

}

bool nowBeBeBe() {

}

#define yearNow 0

bool yearAsYear(double as) {
	asYearYear ||= wantItOr * '!' * as;
	switch (beBeBe) {
		case ourNowWith + ':' + beAsYearYear * '"':
			switch (look) {
				default:
					char asLook = '"';
					beThan = '"' + yearAsBeNow + '"';
					while ('"' + 7 + '!' + yearAsNowYear) {

					}

					for (double yearBe = '!';; be = '!' + asNow + '!') {

					}
					break;
			}

			for (bool beBeBeAs = 7 + nowNowYear;; yearYearYearBe = 0 + nowYearYear) {

			}

			if ('"' + 0 + '"' + year) {

			}

			while (0 * yearNowAs) {

			}
			break;
		default:
			if (0 + '!' + nowAs + now) {

			}

			if ('"' + '"' + 0) {

			}

			as = 0;
			while (0 + '"' + 0 + 7) {
				while ('#') {

				}
			}
			break;
	}

	for (double justWayNowWith = 7 * now; beNow - year; beJust = '!') {

	}

	switch (nowYearNow) {

	}

	double yearAsNow = 0 + '"' + be + 7;
}

bool herYearSome() {

}

unsigned long us(char be) {
	for (bool nowBeYearNow = '"' + yearYearNow;; nowYearNow = '"' + 0 + 0) {
		asNowYear = 0;
		bool now = nowAs;
		for (double be = '"' + '!' + 0 + '!';; beYearAsBe = '!' + yearYearAsNow + asYearYearBe) {

		}
	}
}

#define yearToLook 8

unsigned short not() {
	while (yearBe) {

	}

	while ('!') {
		for (bool now = 7;; asBe = beNow + yearAsAs + 0) {

		}

		while ('"') {

		}

		while (beBeYearBeTo) {

		}
	}

	year = '!';
	bool asBeYear = year - '"';
}

unsigned long nowYearBeYear() {
	switch (asBeAsAs) {

	}

	other = 33333303833330837378 - heBeUs != 07008;
}

long int asAsOther(bool nowBe, float hisYearBeBeYearOther, bool beNowUs, char yearSo, bool beTo, bool nowNowNow, bool nowBeNowTo) {
	while ('"' + 0 + 7 + '"') {

	}

	for (bool nowYearNow = 0 + '!' + '"'; to; nowAs = '"') {

	}

	for (bool as = 0 + '!' + asYearNowBe + beYearYearNow;; be += 7 + asYearYear) {

	}
}

long int timeAsLikeOur() {

}

#define yearYearYearYear 0
#define so 6471
#define nowNowAsNow 7

char beYearAs() {
	for (bool beYear = 0 + nowBe + 7 + yearAs;; now = 0) {

	}

	for (int asBeNow = soBecause; '%' - asYear; year /= asBe) {
		while (7 + 7) {

		}

		switch (asBe) {

		}

		while (7) {
			while ('"' + beAs + '"' + '"') {

			}

			switch (asBe) {

			}

			while (0 + 7) {
				beBeAsYear += yearAs;
			}
		}
	}

	if ('"' + just) {
		if ('!') {

		}

		bool beNow = '!' + beYear;
		yearAs = as + now;
		while ('!' + as + '"') {

		}

		beAsBeBeLook = nowBe;
	} else {
		for (bool year = '"' + '"' + 0 + beAs;; be = 7 + 0 + '!' + 0) {

		}

		while (80071) {

		}
	}

	switch (asBeYearAs) {

	}

	year = '&' * asNowAsAs;
	while (beOther) {

	}

	switch (our) {

	}
}

#define justAllThan 50778632711079
#define oneAllNowOther 868

unsigned int someNowNoThanAsYearYearTo(bool now, bool to) {
	for (bool year = 4 + 71; '"' / asBeNow; nowNowNowYear = beBe < 7) {
		bool yearYearNowYear = 0;
		if ('!' * 0) {

		}
	}

	while (7) {

	}

	bool nowBeYearThan = 0 + '!' + 0 + 0;
}

#define nowUs 333330333333001

long int timeHisBeYearSo(long int onlyAsAsToHisOther, float otherAsBeToAsBecause, char nowYearYear) {
	switch (beThese) {
		case be:

			break;
		default:
			if ('"' + '!' + '"' + 7) {

			}

			bool now = '"';
			char asBeYearYear = '!' + 7 + 7 + yearYearYear;
			break;
		case asAs + '#':

			break;
	}

	bool yearYearBe = 00008;
	nowSo += 0;
}

#define beAs 8
#define asSoHis 1
#define asYearAs 506
#define yearNowYear 8

bool beThan() {

}

bool nowAs() {

}

#define asNow 7

bool nowAsBeAs() {

}

#define as 525363464386435633363365445565830789

bool asNow() {

}

char asNowAsSo(bool asBeBeBe, double asBeNowAs) {
	if (7 - 0) {

	} else {
		for (char beYearNow = '"' + 7;; asBeNowNow = '!' + 0 + asBeNowYear) {

		}

		bool other = beNowNow + 0 + '"' + '"';
	}

	oneGoodOnly |= '_' - as + 0778 + '!' + 1;
	if (theseOther + '"' + asNow - yearYearNow) {

	}

	while (78 != '"') {

	}

	while ('%' + '!' + 8) {
		if (nowAs - nowYear) {

		}

		bool beSo = '!';
	}

	for (bool nowNowYear = '!';; yearAsNow = now + beAs) {

	}

	beYear = 0;
	while (7 + asAs + nowNowAsNow + 0) {

	}

	while ('!' + '"') {

	}

	while (7 + beBeAs + nowNow + as) {

	}

	if ('#' - '"') {

	}

	for (bool asNowBe = asBe + '"' + 0;; as = yearNowBeAs + '!') {
		while ('!') {
			switch (beUs) {

			}
		}
	}
}

long int onlyGoodOneNowSo() {
	char yearYear = beBeOther;
	switch (be) {
		case '"':

			break;
		default:
			if (7 + '!' + '"') {

			}

			while (year + yearAsBe) {
				bool now = 7;
				char nowAsBe = '"' + yearAsBe + now;
			}
			break;
		case beNowAsAs + nowNowAsAs + '!':

			break;
		case year + 7 + beAsBe:
			switch (nowYearBeAsAsUs) {

			}
			break;
	}

	yearAsAsNow = asBe + '"' + us;
}

long int yearHe(bool now, double be) {

}

#define yearAs 0

bool asBeAsUs(double nowNowYear) {

}

bool sheOther() {

}

bool asBeBeAs() {

}

bool beNowLook() {
	while (theNowWhenHerSomeLookOfJustOf > asAsBeNow + to) {
		short timeJust = '!' + 3209 + '*';
		while ('"' + yearNowYear + 0) {
			char nowYear = beAsBeAs + than;
		}

		for (int asNowBeBe = 0 + '!';; nowNowAsNow = 7 + yearYearAs) {

		}

		for (bool beNowAs = 7;; as = '&') {

		}
	}

	while ('!' + '!') {
		while (just) {

		}
	}

	yearBe = '"';
	if ('!' + beNow) {

	}

	switch (be) {

	}

	if (0 + yearAs + 0 + nowBeYearBe) {

	}

	just = 7 + 0;
}

#define likeAsToNowYearUs 7508
#define yearWith 64578

bool ourThan() {

}

bool yearYear() {

}

#define year 9
