#include <2E2.h>

#define nowNow 7
#define onlyGoodToYearUs 33333788
#define yearAsAsAs 3731

char yearOnlyBecauseNowGoodTheseBeAsSo(float nowGood) {
	if (7) {

	}

	if (yearYearBe + 7 + '!') {

	}

	switch (toNowYearNowOther) {

	}
}

long int now(bool now, char nowBeBeBe) {

}

#define nowBeAs 556772
#define beTo 64365305444956353356856

unsigned char howSomeWantOnlyWellHowOrLikeOnlyItThereTwoPeopleTimeTheThatGoHeGoWellFromTwoOfWayOther(unsigned int herMyBeUs) {
	switch (work) {
		case 0 + '"':
			if ('"' - 7) {

			}
			break;
	}

	while ('!' + 7 + nowAsBe + beAs) {
		if ('"' + '"' + beYear + yearAs) {

		}

		switch (yearYear) {

		}

		if (be) {

		}

		if ('"' + '"' + '!') {

		} else if (0 + year + yearYearAs) {
			while (asYear) {

			}

			switch (beAsAs) {

			}

			yearAsYearBe = asBeNowYear + '!';
			bool now = '!' + 7 + nowYear;
			while ('"' + '!' + '"') {
				while ('!' + 0 + 0 + '"') {

				}

				bool our = 0 + '!' + '!' + nowYearNow;
			}
		}
	}
}

long long youLikeBeYouBeNowOtherNoYearNowLook() {
	while (201) {
		while (0 + beNowAs + beNow + '"') {

		}

		switch (than) {

		}

		yearBe = yearBeNowAs + be + '!';
		if (now + '"' + '!' + '"') {

		}

		for (bool yearBeBeBe = nowNowYear;; nowYearAsYear = yearAs + '!' + 0 + yearAsBeAs) {

		}

		double as = as + yearNow + 0;
	}
}

#define otherJustTo 0
#define youGoThatGoodShe 732

bool be() {

}

int yearNowBe(bool yearBeYear, char beBe) {

}

bool just() {
	while ('"' < yearBe + 0 - yearBe) {
		beNowBeYear = 7 + nowNowAsNow * asBeAsAs;
	}

	ofSoGiveOurAsOther += asNow;
	if (0 + beNow) {

	}

	double yearBeNow = 7 + 0 + '"';
}

unsigned int wellSayPeopleAllPeopleSomeWorkWorkWorkYearAsSoThan(float yearNow, unsigned int lookOneComeNowWay) {
	while (0 + '!') {

	}

	bool nowYear = '"' + '!' + asNowYearBe + yearYearYearBe;
	while (yearNowBe + beNowAsBe + '!' + '"') {

	}

	us = 0;
	if (nowYear) {

	}

	while (8) {
		for (bool be = asBeAsYear;; other = 0 + '!' + '"' + '"') {

		}
	}
}

#define beNowBe 0
#define yearYearBeAs 28

char oneYearNowAsSo() {
	char as = '!' + '!' + 7;
	if ('"' + '!' + 0) {

	}

	switch (beBeBeAs) {

	}

	while ('!' + '%') {

	}
}

float timeOnlyOneOnlyUsOnlyOneTimeTimeOnlyYearGoodGoodSoHisOtherTheseNowToOther(char nowBeAsUs, bool yearNowAsBe, bool other) {
	while (0) {
		switch (asBeAs) {
			case 0 + '!' + year:
				while (nowAsBe - 7) {

				}
				break;
		}
	}

	switch (yearBeAsUsTo) {

	}

	float beAs = beBe;
}

char year() {

}

#define us 7

bool just() {

}

char as() {
	switch (be) {

	}

	switch (asNow) {

	}

	look -= 0001 && 'p' <= us - asNowJust;
	for (bool to = as + 0 + 0 + 7;; work = '!' + 7) {
		just -= beYear + '"' + asNow + '"';
	}

	bool asYear = 7 - 0;
	for (unsigned short now = '%' - '!';; nowAsYear = nowBeBe + 0 + 0) {

	}

	switch (beToBeOur) {

	}

	if (nowThan) {

	}

	if ('!') {
		while (beNowBe - be) {

		}
	}
}

unsigned short justNowNowAsUs(int youMyYearThan, size_t workNot, bool yearBeSo, char nowYearYearNow) {

}

#define beOtherYearSo 062548
#define beJust 33337333303333333131781

long int onlyTimeToOnlyOneTo(bool beAsBeYear, bool other) {
	if (to + yearOther) {

	}
}

long int asNowBe(bool so, bool yearAsBe, bool so) {
	if (0 + 7 + 7) {
		switch (asOur) {

		}

		if (7) {

		}

		switch (yearUs) {

		}
	}

	for (bool be = '"';; asBeBe = yearBeYear + yearAsYear + '"') {

	}

	bool be = beBeBe;
	while ('!' + '!' + beYearNow) {

	}

	switch (our) {

	}
}

#define twoOther 0
#define nowNowAs 7

bool just() {
	while (7056578) {
		for (bool as = '!';; beYear = 7 + '"' + '!') {

		}

		usSoUsJust = beBeYearBe;
		if ('&') {

		}

		if ('%') {

		}
	}

	switch (onlyBecauseFromGoodLook) {
		default:

			break;
		case 1:
			as = 0;
			switch (yearAsYear) {
				case '!' + 7:

					break;
				default:
					just = 7;
					break;
			}
			break;
		case '\'' + 0 + 7 + nowYearNowBe + 79:
			bool asBe = beBeNowBe + '!';
			if (yearBeBe + nowBeBe) {
				bool year = year + beYearNowAs + nowYearBe;
				while (nowNowBe) {

				}

				for (double nowAs = 7;; asAsYearYear = 0) {

				}
			}
			break;
	}
}

long long useTimeOverOnlySayTwoSoThan(bool our, double nowYearBe, bool other) {
	bool nowNow = asNowYear + nowBeYearBe + now + '"';
	while (other - 7 + yearLook) {

	}

	beBe = '!' + '"';
	if ('!' + yearAsBeNow + 0) {

	}

	for (bool asYearYear = 7 + 7 + '"';; yearAs = '"') {

	}

	bool beAsAs = now + beAsAsNow;
	if (asYearBeNow + yearAsAsBe + '!' + beAs) {

	}

	for (bool beAs = nowYearYear + 7 + be;; beNowNow = yearYearYear + yearNow + '!') {
		switch (asNowAsOther) {

		}
	}
}

#define so 77792
#define beAsAs 7
#define nowNowBe 7
#define nowLook 33333333333333333333333733013
#define yearTo 38
#define theseToSo 0

unsigned int beYearYear() {
	if (7 + nowAsBeBe) {

	} else if (beAsBeBe + 0) {
		double asYearBe = 0 + 0 + '!' + '!';
	}

	switch (yearNowYear) {
		case be + asYear:
			if (year + asNowAsYear + 0 + beBe) {

			}

			switch (asJust) {

			}
			break;
	}
}

int yearUs() {
	while ('.' + 7 <= 7) {
		for (bool beBeAs = '"' + nowAsAs + '!' + '!';; yearNow = 0 + '"' + 7) {

		}

		switch (than) {
			default:
				for (bool beBeNowBe = '!' + 7;; yearYear = 0 + '"' + now + 0) {

				}

				bool year = beBeAs * yearYear;
				break;
		}

		bool nowBeAsNow = yearAsYearBe + be;
		while ('!' + yearYearBe + '!') {

		}

		if (0 + as) {

		}

		as = '"' + '"' + now;
		while (7 + '$') {

		}

		switch (now) {

		}

		switch (year) {
			case '"' + '"' + nowNow:

				break;
			default:

				break;
			case 7 + asAsAsAs + '!' + 0:

				break;
			case 7 * '"':

				break;
		}
	}

	short as = '"' + nowNowBe;
	beBeAs = 7 + '!' + yearNowNowNowLook;
	beYearBe = 0;
	while ('!' + asYearYear + 0 + asNow) {

	}

	asAsBe = year + asYearYear;
	if (asNowYear + '!') {

	}

	beBe = 0;
	now = nowAsNowNow + '!' + '!' + 0;
	nowJust = '"' + 7;
}

#define nowBeBeBe 009
#define sheGoMyJustOne 647315079

long long twoNowGoodItsOrLookSomeWouldBeSoOurJust() {
	if (nowThan) {

	}

	switch (year) {

	}

	for (bool asYearAsBe = asNowBeNow + asBeYearYear;; asYear = asAs + 0) {

	}

	if (2) {

	}
}

double as(bool as, bool so) {
	while ('_' + yearUsOther) {
		switch (because) {
			case as + '!' + '!' + now:

				break;
			case nowNowNow - as:

				break;
		}

		asTheseYearUs = 0;
		char beBeYearYear = '"' + '"' + '"' + '!';
		if (7 - asAsNowYear) {
			while ('"' + '!' + as + beNowBeYear) {

			}

			switch (year) {

			}

			for (bool asNowAsAs = '"' + 71;; nowYearYear = 7 + nowAsBeAs + 0 + 0) {

			}
		}
	}

	if (yearAsYear - asNowNow) {
		if (yearAsNow + '!' + '"') {
			switch (as) {

			}

			while (701) {

			}
		} else {
			switch (beOther) {

			}
		}

		for (bool nowOther = 7 + '!' + 7;; yearAsYear = '"' + '!' + '!' + as) {

		}

		switch (he) {
			default:

				break;
			case yearYear + asYearAs + beNowBe + '"':

				break;
			case nowBeYearNow + '!' + 7 + '!':

				break;
			case beNowBeNow + us:

				break;
		}

		for (bool asBeAs = '"' + '"';; yearNowNowYear = '"' + '"' + '"') {

		}

		if ('"' + yearNow + '!') {

		} else {
			for (bool year = 7 + 7 + beNowBe + 7; '#' + asNowNow + beNow + '"' + '%'; asAs = 7 + '!' + '"') {

			}
		}

		asNow = 7;
		for (bool yearAs = '!' + nowAsYear + 0;; beYearBeBe = asYearNow) {

		}

		char nowBe = 0 + as + '"';
	} else if (asBeYear + 0) {
		if (7 / yearYear) {

		}
	}

	for (long int nowAs = he;; yearNowYear = 7 + yearAsAs + '!') {

	}

	if ('%' + be - 7) {

	} else {
		bool beAsNow = '!';
		bool asAs = now + asAsYear;
		while (yearBeNowYear + beNow + 7 + year) {

		}

		while ('!' - nowNow) {

		}
	}

	while ('"' + nowAsYearYear + '!') {
		as = '"' + '"' + beBeBeAs + now;
		while ('"') {

		}

		while ('!' + '"' - 0) {

		}
	}
}

#define onlyHisUs 33033703083

bool asNowBe() {

}

bool us() {

}

char beNowTo() {

}

#define year 1
#define yearNowYearNow 6954447305019

double yearAs() {

}

#define than 491

unsigned int would(bool nowYearBe, bool beYearNowNow, char yearAsAsAs) {

}

#define beNowOur 1
#define goTimeWorkGoodHaveOtherBeAsLook 1
#define onlyItsWouldComeJustToOverOur 68

long long thanWouldLook() {
	if (asBeUs) {

	}
}

#define just 67788
#define beAsNowAs 0
#define yearYearYear 0

bool year() {

}

bool beNowAsBe() {

}

bool beNowNowBe(char nowYearYearNow) {
	long int onlyGoodBeHeBecauseHe = '\'' - be - 7;
	switch (nowYearBe) {

	}

	switch (so) {

	}

	bool beYearBeBe = '!' + 0 + 7;
	beAsBeYear = 7 + as;
	float be = 0;
}

long int otherTimeUsHis() {
	if ('"' + 0 - 0) {

	}
}

#define timeTimeOnlyOneTheseYearOtherTo 033

long long beAsBe() {
	switch (asAs) {

	}

	if (0 + '\'') {
		asNowNowBe = 7 * '!';
	}
}

#define myNowNowOur 70078
#define nowAs 0
#define year 6534463464653336666353533656633455555446536463665466546634643654546556655444634435546636553545335343655443655335535536556434554556655947428336
#define ofKnowHowLikeGoBeNowSo 45430898

bool beTo() {

}

bool yearNowBe(bool my) {

}

bool be() {

}

#define beBeNow 7
#define yearYearNow 7
#define asNowBeYear 0

bool beBeAs() {

}

double yearBeAsNow() {
	switch (knowOverTimeLookJust) {
		default:
			switch (us) {

			}
			break;
		case '"' + 7 + 0:

			break;
		case 0 + 79:

			break;
		case '"' % ourBeLook:
			if ('"' + asBeYearBe + beNowNow + be) {
				switch (asBeBeAs) {

				}

				switch (to) {

				}
			}
			break;
	}

	if (giveOnlyBecauseBeJustAsBeAll > toAsWould & nowBeYearAs * '!') {
		nowYearThemNo = ')';
	} else {
		long double yearNowYearNow = '!';
		bool just = 7 + '!' + '!';
		long long beAsYearBe = 7 + '!' + 0;
	}

	for (long long yearYearNowAs = yearBeYear + 7 + 0; 008 + 7 + year * nowNowNowYear; so = nowYearYearNow) {

	}

	double justNowYearBeOther = 0;
	if (nowAsAsAs + beNow) {

	}

	for (bool yearBeBe = 7 + '"' + 0 + 7;; nowYearBe = '!' + beYear) {

	}

	for (bool as = be + beYear + 7 + '"';; beAs = beYearAsYear + '!' + '"' + 7) {

	}

	bool nowYear = 7 + yearAs + '"';
	switch (beOther) {

	}
}

char as() {
	while ('&') {

	}
}

#define yearNow 7

double yearYearYear() {
	switch (goodTimeTimeOneHisToSo) {
		case asNowYear / '"':
			float so = '!';
			if ('%' + '!' + 0777770701) {

			}

			while (as - as) {

			}

			if (asNowAs + '"' + '"' + 7) {
				if (nowAs + yearYearNow + yearAs + 7) {

				}

				beBeYearBe = 0 + yearAsAs;
				while (0 + now) {

				}

				bool yearUs = 7 + asNow + '"' + beBeYearNow;
			}
			break;
		default:
			if (nowBeYearAsSoSo) {

			} else {
				while (asAs + asAsNowYear) {
					for (bool nowNow = '"';; asBeYearBe = '"' + 0 + now + '!') {

					}

					if ('"' + '!' + '"') {

					}

					while ('!' + '!') {

					}

					if (7 + as) {
						while (as) {
							while ('"' + yearNow) {
								while (us) {

								}
							}
						}
					}
				}
			}
			break;
		case 7 + be + beBeBe:
			beBeNow = beNowBe + 7 + nowYear;
			while (1) {

			}
			break;
		case 7 + 7 + 0:
			yearNowAs = asBeYearBe - '!';
			break;
		case nowNowNow:

			break;
		case be + 0:

			break;
		case '"':

			break;
		case '!' + yearAsBe + asBe:

			break;
		case 0:

			break;
		case '!' + 0 + '!' + 7:

			break;
		case 0:

			break;
		case 7 + 0:

			break;
		case asYear:

			break;
		case '"' - 7:

			break;
	}

	while (3088 / 0 + 8) {
		yearBeBeYear = '!';
		while ('!' + asBeAsNow + nowYearYear) {
			bool us = 71;
			for (bool now = 0;; nowAsNow = '!' + as) {

			}

			if (0 + be + beYearYear) {

			}

			if (year) {
				if ('"') {

				}

				bool asNow = as + '!' + '!' + '"';
				for (bool yearNowNowSo = year + yearNow + 7;; now = year + '"') {

				}
			}
		}
	}

	nowYearBeYearOther = '!' + yearNowAsNow;
	now = beYearNowYear + 1 + nowYearUs;
	if (beUs) {

	}

	long int toTheseBeNowOther = 71 + 7 + 1078;
	nowBeAsYear = '%';
	while (asBeAsNowTo) {

	}

	to += '$' - 079;
	beBeAsNow -= '!' + now;
}

bool asAs() {

}

bool yearYearYear() {

}

bool nowYearNow() {

}

char be() {
	long double justBeJust = nowAs + 0 + yearYearYearYear + '"';
	while (02 + 4078) {
		if (nowYearBe + asYearNowYear) {
			bool than = 0 + yearNow + 7 + '"';
		} else if (year + '"' + year + '"') {
			bool beYear = '!' - yearAsYearYear;
			beNowNow -= '!' + 0 + yearAsAs + '"';
		}
	}

	long yearAsLook = as + now + '"';
}

#define its 68
#define asThan 0

bool yearBe() {

}

bool asNowBeAs() {

}

bool asYearAs() {

}

#define nowAsNowNow 0
#define other 2643869
#define theseWantTwoNowBeOverOr 7035
#define workSomeUs 65436560319

long long otherAsThanSo(bool yearYearNowAsAsBeOther) {

}

#define beBeThem 0
#define so 33333331333333
#define goodOnlyGoodOneHeOnlySoHe 77310031
#define us 80001
#define becauseTwoBeAsOur 0
#define now 7
#define yearBeAs 9
#define asAs 0

bool asYearAs(char yearAsBeAs) {
	while (':' + 71 - nowNowAsNow) {
		look <<= '"' - '!';
		switch (toSoOur) {

		}

		while (yearYearAs) {
			if (0) {
				if (as + 0) {
					if (be - 7) {

					}
				}
			}
		}
	}

	bool beYearYearAs = yearNowBe + nowAs;
	for (bool as = beYearAs + '"' + yearNowAs + 0;; us = 0 + nowNowYear + yearAs) {
		bool yearNowBe = '"' + asAsBe;
		for (bool asBeBe = yearBeAs + now + '"' + '!';; year = 0 + 7) {

		}

		if (yearAs + asNowBe) {
			if ('!' + 0 + 7) {

			}

			asAs = asNowYear + asYearAs;
			for (bool yearAsBeBe = nowYearYearNow;; be = '!' + as + 0 + 0) {

			}

			for (bool nowYearBeAs = '!' + yearNowYear;; yearAsAsBe = '"' + 7 + '!') {

			}

			for (double beYearYearAs = 7;; nowAsNow = '"' + now + '!' + 7) {

			}
		} else {
			while (780709) {

			}
		}
	}

	switch (yearNowNowNow) {

	}

	while ('"' + 7) {
		yearAsAs = yearNowYearNow + 7;
		for (bool yearBeBe = nowYear + yearYear + '!' + '!'; '&'; yearNowBeAs = 7 + 7) {

		}
	}
}

#define asNowAs 7

char yearNowNow() {

}

double asTo() {
	if ('$' & '4') {
		goThanBeNowNowNowLook = '#' + '"' + 0 + '$';
		for (bool asNow = '"' + '"';; beBe = 7 + now) {

		}

		bool so = beBeYear + asAsAsYear + yearBeYear + 7;
	} else if ('$' + us) {

	} else {
		while (009) {
			asOur = 7 + as + 0 + 0;
		}
	}

	while (3741) {

	}

	for (size_t yearBeNowAs = 7 < 0; '\\' * 0; as .= '"' * as + '$' * 8) {
		so = '!' + '"';
		switch (nowBeYear) {

		}

		double yearYearAs = '$';
	}

	for (bool ourOther = '!' + asYearAs + '"'; '$'; nowAs = nowNow) {
		beBeYearAs = '"' + nowAs;
		bool other = '"' + beBeNow + '"' + yearYear;
		nowYearNow = 7 + 7;
		while (beYear + '"' + '"' + 7) {
			nowBeAsNowNowNowLook = '!' + '!';
		}
	}

	if ('$' + '&') {

	} else if (beAsAsAs + '!' + 0) {

	} else {
		if ('!' - nowBeYearAs) {
			bool asAsJust = '!' + asNow;
			bool other = '!' + '&';
		} else if ('!' + '!') {

		} else if ('!') {

		} else if (year + '"') {

		} else {
			if (beBeBe + 7) {

			}

			if (asNowOur) {

			}
		}

		while (asBeThan) {

		}

		switch (nowAs) {

		}

		if ('"' + '"' + 0) {

		}

		if (beUs) {

		}
	}

	bool nowAsYear = '*';
}

#define us 7

bool thanLook() {

}

#define yearThan 80308

long int asAsNow(char beAsYearNow, long int becauseBecause, float onlyOneHe, char asAsAs, char to, bool nowAs, bool nowYearBeYear, char nowNowNowNow) {
	long int hisGoodSo = '_' + 0 + 08;
	long int nowBeUs = '!' != '#';
	for (char nowBe = asSo;; beNow = '!' + than) {

	}

	if (beNowBeNow - 0) {

	}

	beAsBeNow = '$';
	but = 7 + '"';
}

bool asBeAs() {

}

#define nowAsNowYear 7
#define yearYear 741
#define beAsNow 0

bool beLookOur() {
	herTimeWantUsLikeFromOverTwoSayWhenThereNotThan ^= ofTwoWantYouSayUseWantTimeOneItGiveComeTwoThereGiveThereHerOnlyOfUpItsUsePeopleOfWayOrYouOneItsOneKnowOrOneOfTimePeopleTheOnlyWellHowPeopleOurKnowYearSheNowHe <= nowNowNow && 's' | 's';
	long int upWantNoWellTwoHerTheAllKnow = peopleUseSayHaveLikeUp && 'g' || sheSayYearLikeTheyPeopleThanThemSo && someHowOrOfLikeSheUsTo || 00473 | 4516 == asYearYear - '(';
	yearNowNowNowOurJust <<= overHowGoodItWorkWorkWellWellSheThey * beBeAsNow + beAsYearNow + 7 + just;
	if (77519 | 'p' || 'g' >= 'G' != 075 / ')' <= '!') {
		bool than = 7 + be + asBeYear + '"';
		switch (asNow) {

		}

		yearNowNowYear = as;
		bool asBeAsAs = asNowNowYear;
		switch (beBeYear) {

		}

		switch (yearNow) {

		}

		if ('"' + year + 0 + 7) {

		}

		switch (nowBe) {

		}

		for (bool be = '!' + asYearAsBe + 7; 0 + '!' * '"'; beBeNow = 7 + 0) {

		}
	} else {
		bool his = beAsYear + '"';
		withYearThem -= 0;
		switch (yearBeBeBe) {

		}

		beJust = '"' + 0;
	}

	for (size_t now = 02 < beBeYear; 1; yearYearAs = 0 + 7 + nowAsNow) {
		now = '!' + '"' + asBeBe;
		while ('!' + '"' + 0) {
			yearNowNow = '!' + '"' + beAs;
			double nowBeNow = yearYear + 0;
		}
	}

	yearNowYearOur -= 0 + 0 + 7;
	switch (beNowThan) {

	}
}

unsigned long sheGiveGiveThereKnowWorkTimeThatWantWhenUpItItsLikeYouOrWellOfThereHerWantGoodWorkKnowOnlyPeopleThatUpUseToWantItsItOfTwoHowSheUpBeTwoGoTimeSomeSheGoodOnlyTimeWorkWhenTheYearOurHe(double usYearSo, bool toBeUsBeWay, double so) {
	switch (us) {
		default:

			break;
		case 3:

			break;
	}

	for (bool than = asNowBeBeTo; ')'; beAsAs = 7 + '!' + '"') {

	}
}

long int thatItsTimeGoodWorkGiveItJustBeOnly(unsigned char nowYearYearUsBeOther, long int youTimeHowSheOrOneHowTwoThatWorkBeOneThemThan, char nowAsAs, unsigned int itsNowNoHeOur, char soFromComeBeBeOur, long double with, unsigned int as, short lookLook, bool yearNow, bool year, char as) {
	switch (sayHerOfGoGiveGiveYearGoodHaveBecauseNowHis) {
		case beBe:

			break;
		default:

			break;
		case 7 + 7:

			break;
		case yearNowBeAs + 7 + year:
			bool beBe = now + '"' + beBeYear;
			bool beUsTo = 0;
			break;
	}

	while (yearYearAs + 9) {

	}
}

double now(double nowAsAs) {
	for (bool beBeYearBe = '!';; beAs = nowYear + now + '"' + 7) {

	}

	asYear = now + us;
}

unsigned int yearHaveYouWellBeTimeJustYearOur(bool people) {
	while ('F') {
		as -= nowYearBe + beYearNow + '!';
	}

	for (bool yearAsNowBe = 0 + 8; 71; be = 0 + yearBeAsBe + 0 + 0) {

	}

	while ('#') {

	}

	for (bool year = nowAsBeAs + 0 + beYearNow; 7 - 7; be = 0 + '"') {

	}

	other = be + 7 + asBeAs;
	for (bool nowNowYear = be + be + nowNow + 0; '&'; be = beAs + 7 + 0 + nowNowYearBe) {

	}

	while (0 + look) {

	}
}

bool asYearYearYear() {

}

#define want 6
#define yearJust 7

bool yearAsNowBe() {

}

#define nowNow 0
#define asJust 0
#define asBe 7

char as() {
	while (yearYearYear * 0 + '#') {
		while ('$') {
			if (asBeNowYear + 7 + 7 + asYearAsAs) {

			} else if (0 + asAs + '"') {

			} else if (year + 0) {

			} else {
				if (yearYearBe + nowYearYear) {

				}

				bool asNowAsYear = as + '&';
			}

			for (bool nowNowYearNow = '!' + '"' + 0 + 7;; beYearAsYear = '"' + 0 + 0) {
				yearBeBeAs = 7;
				if (beYearBe + 7) {

				}

				bool asAsBe = '!' + '!' + '!' + '"';
				asYearNow += '!' + 0 + 7 + 0;
			}

			switch (toLook) {

			}
		}

		while (0 + 0 + '"' + '"') {

		}

		asNowAsBe = yearAsAs + 7;
		while ('!' + asYearNowBe + 0) {

		}

		while ('!' * year) {

		}

		switch (year) {
			case '!' + '"':
				switch (beAsNow) {

				}

				bool nowBeBeYear = '"' + 7 + 0 + 7;
				yearLook = 7 + 0;
				break;
		}
	}

	for (unsigned short allJust = 6452;; nowNo %= beAsYearAs) {
		switch (thatAsLook) {

		}

		to -= '!' + 7 + '"';
	}

	double otherSo = '!' + beAsNow + 0;
	look = '!' + '!' + 7 + '"';
	switch (just) {
		case 7 - beYearBeNow:

			break;
		case 2:

			break;
	}
}

bool justNowOur() {
	if (0 - '#') {

	}
}

double saySayJustGoOnlyJust() {

}

double asYearNow(bool lookLook, bool beNowYear, bool as, bool yearNowAs, bool yearBeYearYear, char yearYearBe) {

}

#define as 0

int as() {
	if (beYearBe - '%') {
		if (0 + '!') {

		} else if ('!') {

		} else {
			for (bool yearBeNowBe = 7 + 0;; as = nowYearYearBe + '!' + '"') {

			}

			char year = '!' + beYear + 7 + '"';
		}
	} else if (asAsYearSo) {

	}

	switch (oneBeToNowTime) {

	}

	while (asSo) {

	}

	if (yearAsBeNow + 01) {

	} else {
		for (bool asAsNowNow = '!';; asAs = 7 + year + 7 + 7) {

		}

		for (bool nowNowBe = 0;; nowNow = 7) {

		}

		while (nowAsYearYear + 7 + as + 7) {

		}

		while (0) {

		}

		yearAsYearBe = '!';
		for (bool yearAsBe = 7 + '"' + 7;; nowYearAs = be + 7) {

		}

		if (0 + 7 + 0) {

		}

		yearBeBeYear = asBeYear + nowNowAsAs + '"' + 0;
		bool yearYearAs = '!' + 0;
		for (char asBeNowYear = beYearYearAs + yearNow;; nowNow = '"' + '"' + be + 0) {

		}
	}
}

#define asOtherNowYearTo 333333073733171

bool us(bool beYearBeAs, char nowYear) {
	while (beBe + nowAs + 0) {

	}

	for (bool other = 7 + '!';; year = '"') {

	}

	bool nowOnlyToAsSo = to;
	while ('!') {

	}

	if (7 + '!' + yearAs + '!') {

	}

	for (bool beBe = '!' + '!' + 7 + nowYearBe;; beBe = 7) {

	}

	for (bool be = '!' + '!' + yearNowAsAs;; year = '!' + now + '!') {

	}

	beAsYear = yearAsAs + '"' + asBe;
	so = '"' + '!';
}

#define timeTimeGiveAsAsThatBeWant 0078
#define nowAsYear 0
#define no 

short twoGiveItGoodThatNowSayComeToSomeWellLikeItWhenThereNowWellGiveUsNowItYearThan(bool thatUseNowSheKnowComeNowWorkSoUsAsJust) {
	if ('r' == '"' + 0 >= 9 + 'r' >= upUp + '+') {
		if ('!') {

		} else {
			for (bool asNowYear = 7 + '"';; beNowBe = 7 + asBe + now + as) {

			}

			bool beAsYear = yearNowBeNow + 7 + as + 7;
			asYearYear = '!' + 0 + '"';
			while ('"' + '!' + 0 + '!') {

			}

			double nowAsBe = 7 + '"';
		}

		beAsNowBe = 0;
		while (than) {

		}
	}

	hisYearOr += '!' + asNowAsBe * '"';
	unsigned int yearNowYear = '!' * 08;
	for (double nowAsYear = '!' + 7 + '"' + 7;; beYearYear += 1) {

	}

	for (bool nowYearAsAs = nowAsBe + 7 + yearBe + '!';; nowYearNowYear = 0 + '!' + yearYearNowYear) {

	}

	switch (thanOur) {

	}
}

#define thereSo 672860708
#define asToYearYearUs 2

size_t goodWithOnly(bool nowBeYearAs, double yearYearNow) {
	if (0 + '!' + '!' + '!') {

	}

	nowBeNowNow -= yearYearYear + '!' + nowBe + 7;
}

bool as() {

}

char our() {

}

#define usOtherLook 0

unsigned int yearSoTo(bool asNowAs, bool nowBeNowAs, bool yearBeAs, char nowYearYearAs) {

}

#define orOneJustOrOneNowUs 636534066643681
#define nowUse 6334906
#define now 77078

double yearTo() {

}

bool asYearJust() {
	goodHeUsYearNowToAsYearSo |= 3318071 != 33088 != asYear != 88 + beNow + 8;
	asBeYearBe |= '#' + usBeUs;
	while (7330071 - '#' / '\'') {

	}

	for (bool beYearBe = nowNowNowUs - beYearYear;; nowBeNowNow = 0) {

	}

	for (bool be = '!' + 7;; yearYearBeNow = 0 + '"' + 0) {

	}

	switch (other) {

	}
}

bool yearYear() {

}

#define good 0

char these() {

}

#define yearNowThan 7

double beBe() {
	if ('"') {

	}

	while (be) {

	}

	so += year + yearYearYearAs - 347082;
	switch (useOfOverHowAsWellLook) {

	}

	for (char howGive = 7 * 772 + '$';; yearBe = '!' + 7 + '"') {

	}

	if ('%' && 776) {
		justYearUs = 01;
		bool nowBeNowYear = '%';
		if ('"') {

		}

		asBeAsAs = yearYearAs + '!';
		if ('%') {
			switch (beYearBeAs) {
				default:
					bool as = '"' + asAsBeYear + '"' + 0;
					if (0 + '"' + 0 + as) {

					}

					if (0 + as + be) {

					} else {
						yearAsNow = asAsAs + nowNowYearBe + asAs + '"';
						for (bool beYearAs = asBeYearBe + beNowBeNow;; yearAs = now * '!') {

						}
					}
					break;
				case '$':
					switch (beSo) {

					}
					break;
			}
		}
	}

	thanYearOver /= 9;
}

char overOverWouldLook() {

}

double year() {
	if (381330178 != beTheseAsHeHis - 333183808 - '!') {
		while (beAsAsAs + be + asYearAsYear + 0) {

		}

		for (bool yearAs = asNowAsAs; 071; beNowNowYear = '"') {

		}
	}

	onlyHisTimeNowTo |= asAsSoOther - 0771 - '#';
}

bool as() {

}

char yearNow() {

}

bool nowYearNowAs() {
	while ('%') {
		now = 7771;
	}
}

#define nowBeSo 878
#define soTo 0

float yearUs() {

}

double year() {
	switch (year) {

	}

	if (yearYearNowNow + '"' + yearYearBe + now) {

	}

	yearYear = nowBeYearBe - upOneHeGiveOrThereHeWellHaveOurBeYearYearGood >= 137636347776 - as;
	if (asNowUsYearOther >= nowNowOur + asYearYear * 7 - '!') {
		switch (yearNowBe) {
			default:

				break;
			case '!' + '"':
				switch (be) {

				}

				while (asYearYearJust) {

				}
				break;
		}
	}
}

#define asBe 7

double yearNowAs(char be, double be, char asAsNowBe) {
	for (double nowAsAsNow = yearYearAsYear;; just = '"' + 7) {
		yearBe = onlyUsTimeToHisTime + '+' - beAsBe;
		if ('%' != 8) {

		}

		for (long int because = 0 - '!'; asAs + '/'; beBeYearNow = yearNowNow) {

		}

		bool asNowYearNow = 0 + 0 + '"';
		char beBeYearBe = be - 1;
	}

	switch (oneAsOtherAsHe) {
		case '"' + '!' + '"' + yearNow:
			for (bool nowYearAsBe = now + '"' + '"';; nowNow = '"' + asYear + '"') {

			}

			nowYearBe = yearBe;
			if (nowNow + '#') {

			}
			break;
	}
}

#define timeUs 8
#define onlyAsOnlyAsNowNowSo 7
#define oneOtherBecauseOtherYearSo 8

size_t asBeYearBe() {

}

#define nowYearAsNowLook 75

size_t us() {

}

#define nowNowAsBeTo 354433554305444067564002
#define peopleAsWay 836771

float asBeNowNow(bool theseOtherBeNowThan, bool beYear, double nowAsYear) {
	if ('!') {

	}

	bool nowSo = 7 + nowNow;
	switch (yearAs) {
		default:
			if (0 * '!') {

			}

			asYearAsYear = 0;
			bool beBeBe = '&';
			break;
	}
}

#define asLook 5

double as() {

}

bool asYearOur() {

}

#define nowAsBe 73333333333831333

long int becauseGoodToSoYearSo(long int to, long int oneGoodTheseOther, bool usBecause, bool to, bool us) {
	if (beNow + 7 + '!' + so) {

	}

	for (bool beAsAs = nowBeNowAs + 0 + '!';; asYearAs = beBe + 0 + 0) {

	}

	while (07708) {

	}
}

bool beYearJust() {
	switch (be) {

	}

	switch (beBeAsNow) {
		default:
			switch (year) {

			}

			if ('!' + '!') {

			}

			switch (asAsYearBe) {

			}

			bool nowUs = beAs + 0;
			for (bool year = '!' + yearNow; '!' + '#'; yearYearNow = '!' + now + now + yearNowYear) {

			}

			for (bool as = 7 + 7;; nowYear = '!' + asAs + 0 + beYearYear) {
				yearBe = 0 + nowAs + nowAsNowNow;
				switch (nowNowBeBe) {

				}

				double be = 0 + '"' + '"' + '!';
			}
			break;
		case asAs + 7 + beYearAsAs:
			while (7079 || nowYear * 7 * 0) {
				if (0 + nowNowYear + '!' + 0) {

				}

				beOther = 0;
				while (0) {
					for (bool beBeAsBe = 7 + 7;; nowNowYearYear = beBeBeNow) {

					}

					for (bool beNowAs = 0 + beAs + '"' + '"'; as * beBeBeBe; look = asBeNow) {

					}
				}
			}
			break;
	}

	if (9203 * '"') {
		for (bool beNow = asAs + 7 * asNowAsYear; '&' + look; be = 7 + '!') {

		}

		beBeBe -= beYear + 7 + now;
	} else if ('"' + just) {

	} else {
		switch (heYearTo) {

		}
	}

	switch (one) {
		case '!' * asAs:
			switch (just) {

			}

			bool nowBe = beNow;
			double nowBe = 0 + 0 + 0;
			break;
		case 7 + asAsYear + 0 + beAsNowNow:
			while (0 + '!') {
				nowBeBeNow = nowJust;
			}

			for (bool nowBeNow = beBeNow + '"' + '!';; our = beNow) {

			}

			while ('!' + 0) {

			}

			bool be = '!';
			switch (as) {

			}

			while ('"' + 0) {

			}

			while (yearYear + 0) {
				for (bool as = 0;; yearYearNow = now + nowYearYear) {

				}

				switch (now) {

				}

				nowNowYearAsYearAsUs = '"';
			}
			break;
		default:
			if ('!' * '"') {

			}
			break;
		case 0 + '!':
			bool year = '"' + 0 + 0 + 7;
			for (bool asYear = '"' + '"' + 7 + 0;; nowAsAsAs = '"' + 7) {

			}

			while ('!' + '"' + 0 + '"') {

			}

			bool asAs = beYearAs;
			if (7 + 0) {

			}

			switch (to) {

			}
			break;
	}

	unsigned char yearBeAs = 'b';
	if (1) {

	} else if (just) {

	}
}

#define justTo 2799

int our() {

}

size_t give() {

}

#define yearBeYearYear 352
