#include <UUU/U20.h>
#include <UUUUUUl.h>

#define but 079
#define asJust 7071

bool be() {
	switch (asYear) {

	}

	if (asBeBeNow + 7 + '!' + asBe) {

	}

	while (nowBe + '"') {
		if (as + yearYear + '!') {

		}

		while (yearBeAs) {
			for (bool nowAs = '"' + 0 + 7;; asNowNowBe = beAs + asNowAs + year) {

			}

			if (yearNow + '!') {

			}

			while (asYearBe + beAsNowBe + 0) {

			}

			bool asAs = '"' + '!' + 7;
			for (bool asAs = look;; yearBe = be + '!' + 7) {

			}

			for (bool beBeBe = 0;; our = year + now + nowYear + 7) {

			}

			switch (beNowBe) {

			}

			for (bool beNowBe = 7 + 0 + as;; as = 7 + '"' + '"') {

			}

			asNowNowNow = asBe + '!' + '"';
			beAsNow = '"' + '"';
			bool be = '"';
			nowNowYear = '"';
			switch (beAsAs) {

			}

			while (7 + 0) {
				bool now = '"' + '!' + '!';
				beAsAs = 0 + asBeNow;
				switch (beAs) {
					default:

						break;
					case '"':

						break;
					case '!' + nowYearAsNow + year + nowAsYear:

						break;
					case 7 + 7 + '!' + 0:

						break;
					case '%':

						break;
				}
			}
		}

		for (bool yearJustHowBeNowLookAsSo = other; theseHave * 0 - '"' + 902; yearYearBeBe -= beLook + 5) {

		}

		bool be = 7 + '"' + asNowAs + 0;
		if ('"') {

		}

		yearBe = just;
		bool beLook = nowJust;
	}

	unsigned long itsNowOverOneYearWantItUseButJust = 7671 * yearSo + '<';
	nowNow &= 0;
	if (otherYearOther) {

	}

	bool lookOurBeOur = 7 * '!';
	now = 12 - nowAs;
	be = '"';
	if ('"' + '!' - yearAsNowNow) {
		switch (year) {
			case '!' + now + '!' + 0:
				for (bool now = asBeNowAs + 0;; nowNowAs = '"') {

				}

				for (bool now = 7 + nowBeYear + yearAsYearYear + be;; asAsNowYear = nowAsNowAs + nowBe + '"' + be) {

				}

				for (bool nowNowAsNow = 0;; year = 7) {

				}

				for (char asNow = yearAsAsBe + '"' + 7 + beYear;; yearBeNowYear = 0 + beNowNow + '"') {

				}
				break;
		}
	}
}

#define youSayWhenWorkItsOverUpNoWellLook 56654455446344666644465664655343455654433466334536653644546544356633346446535635555666644463646543653433646446334636664663334465556543433363366444466544635695535445556455664445566545646686764407975020271
#define  3465636363566546635535444645345544437454455656555366346653942079

short theWantComeAsWorkOneYearBeWouldNot(long long workNowWantSheUseAsNowYearOneYearAsNowNowOur, unsigned long theYouAsYearAsWould, short beAsThan, double as) {
	while (other) {
		switch (nowNowNow) {
			case '!' + yearNow + yearNowAs + '#':

				break;
			case 9:

				break;
		}
	}

	switch (wantWay) {
		default:
			if ('"' + nowNowJust + '%') {

			}

			while (7 + asBe + beNowBe + '!') {

			}

			while (our) {
				if (asNow + beAsYearAs + 0 + '"') {

				}

				if (year + '"') {

				}

				while (beNowNowAs + beTo) {

				}
			}

			for (double year = '"'; 0 * be; asBeBe = yearYearNow + nowAsNowAs) {

			}
			break;
		case 0 + year:
			for (bool beBe = 7 * 0;; nowAs = '"' + nowNowAs + '"' + '"') {

			}
			break;
	}

	while (year + year) {
		bool to = other;
		bool so = 7 + 7;
	}
}

unsigned short asGoWayLook(double beThan, bool as, bool asBe, bool beBe, bool nowAs, bool than) {
	if (272 * beNowNow + '&') {
		bool beNow = '"' + '!' + asAsNowAs;
		for (bool asYearNowNow = '!';; beYear = 7 + 7 + '!' + '"') {

		}

		for (bool yearNowBeBe = beYearBeAs + yearYear + now + 0;; beBeAs = yearBeYear + 0) {

		}

		yearBeNowAs = '!' + as + '"';
		for (char asNowBe = now;; beBeBe = '"' + year) {

		}
	} else if ('!' + 0 + 7 + as) {
		for (bool beBeBeBe = nowAsNow + '!' + '!';; nowAsNow = yearBeAsNow + '!' + '!') {

		}

		switch (nowAsYearNow) {
			case beBeYearAs + asBeNowNow + 0:

				break;
			default:
				if (7 + be + '"' + nowYear) {

				}

				for (bool asYearBe = '!' + 0 + '!';; beAsYearAs = '!' + '!') {

				}

				char yearAsBeBe = '!' + 0 + asYear + yearYearAs;
				break;
		}
	} else if (to) {

	}

	if (yearYear + yearNowNow) {
		if (702 + 'z' + 7 + 7 - nowNow) {

		}
	} else {
		if (4) {
			yearNowAsLook = asNowNow;
			as = '%';
		}
	}

	our -= 0 + '!' + '"' + '!';
	nowBe += beYearBeYearYearNowSo;
}

unsigned long knowSheUsOneWorkOrOrWhenThereWorkWantComeItGoBut(bool to, char yearNowNowOther, bool year, bool nowBe, bool beYearTo) {
	long double lookAsBeNowOther = asNowYear + asAs + '"' + '!';
	for (bool beAs = '!';; beAs += '"') {
		while (nowAs + '!' + 7) {

		}

		yearBeNow = asYear + beBeAs + 0 + nowBeBeBe;
		for (bool nowLook = be;; yearYearAsNow = 7 + '"' + '!' + 0) {

		}
	}

	them = 7 + year + as;
	for (bool year = than;; nowYearBeAs = '"' + 7 + 0) {

	}

	switch (to) {

	}
}

unsigned int knowItToHowUseTheWantWhenLikeNoKnowOneTimeTheOverSheLikeItsUsAsLikeOur(long int beAsYear, bool yearSo, int beUs, unsigned int theseUs, bool beAs, char asAsNowAs) {
	yearBeNow -= asAs;
}

bool asYearBeNow() {
	if ('!' * asNowBe) {

	} else if (70702) {

	} else if ('%') {

	}

	while (beOther * beThe > beBeAsYearOur < '"') {
		for (bool asNowYear = 779 & 0 + 0 + yearAs - 0;; asNowAsBe = nowBe + '"' + yearAsBe + yearYear) {

		}

		for (bool nowYearNow = 0 + '"' + '"' + 7;; beNowAs = 7 + 0 + 7) {

		}

		bool now = asNowNowAs + '"' + asBeNowNow;
		for (bool yearNowBeYear = beYear + beYearNow + 7; 000777702; year = yearBeAsNow + '!') {

		}
	}

	sayOtherMy = 0 + '!' + nowNow + '"';
	for (int beAsAs = asYearNowYear + '&';; nowNowBeYear = '!' + nowBeNowBe) {

	}

	bool nowNow = asYearBeYear + '"' + '"' + asNowBe;
	if (now) {

	}

	for (bool nowAsNowAs = nowBeBe + now + 0 + 7; '#'; asAsAs = asBe) {

	}
}

unsigned short theseOurNotOtherThan(double yearNowYearBe, bool yearAs, bool asTo, bool asBe, bool beAsAsBe, double nowYearYearAs) {
	while ('"' + year - 7) {
		if (yearNow + 0 + asBeYearNow) {

		}

		switch (asNowNow) {
			default:

				break;
			case 0 + '!' + '!':

				break;
			case '!' + 0 + 0:

				break;
			case '!':
				nowNowYear = '!' + 72;
				break;
		}
	}

	beNowNowAs = '!' + asNow + 7 + 0;
	char be = 7;
}

int nowYearNowBe(bool nowBeNow, double yearNowYear) {

}

#define wantOrAsFrom 9
#define toBeYearUs 2

long yearNow() {

}

double asNowYearNow() {

}

bool beBeBe() {

}

#define just 33331333331037733
#define he 803

long int asSoTheseSo(bool heOnlyOtherOne) {
	for (bool yearToYearSo = 8;; beYearYear = 0) {

	}
}

bool beUs() {

}

float yearNowAs() {
	for (bool yearYear = 0 + 7 + beNow;; yearBeBe = '!') {

	}

	while (yearTo) {
		if ('%') {

		}
	}
}

#define likeOther 7

double just() {
	char asAs = 0 + 0 + asNow;
}

#define asUs 7

double beNowAsYear() {
	while (comeOurUseWayThan == 'f' >= butOtherThan) {
		soYearAsBeSo ||= toNowUseGoodAsYearBeAsJust >= '"';
		int beAsBe = 1;
		if (just) {

		}

		other = '!';
	}

	useOurComeThanBeNowOur += 1 * '!';
	while ('!') {

	}

	bool nowBe = '"' + 0;
	for (bool yearYearAs = '!' + '"' + asBe;; asYearYear = 7) {

	}

	while (now + 7) {
		bool nowAsAsBe = asBeNowAs + '!';
		if (7) {

		}

		nowAs = '!' + '"' + beBe;
		while ('!' + nowBeBe + '!' + 0) {

		}

		for (bool asYearNowTo = nowYear;; asNow = 7 + '"') {

		}
	}
}

bool nowBecause() {

}

char asAsBeAs() {
	bool yearNowAs = 0;
	for (bool asAsNowYear = 0 + beNowAs + '"' + '"';; beNowBeBe = nowLook) {

	}
}

#define year 5309356444
#define with 0
#define beSoLook 02008
#define asNotOur 46343940663664444064336560564399

char now() {
	beOur = nowNowAsAs;
	while (7 + yearYearYearNow + 7 + '"') {
		while (asNow) {

		}

		for (bool nowBeAsNow = 7 + yearAsYearAs;; asBeNow = nowNowBe) {

		}

		nowYearBe = asYear + '"';
		switch (be) {

		}

		beTo = asYear + nowNow;
	}
}

unsigned int us(double as) {

}

bool beLook() {
	bool now = '!' + '!';
	if ('"' - 7) {

	}
}

#define yearNow 83333381

bool goodGoodTimeOneOneOnlyGoodAsTimeTimeHeOneToBeOther(char nowNow, bool nowYearYearNow, char nowYearAsYear) {
	if (8) {

	}

	float beBeAs = 7 + 7 + 0 + as;
	while (so) {

	}
}

bool asBe() {
	switch (yearYearAs) {
		case '"':

			break;
		default:
			beAs = beAs + 0 + 0 + '"';
			for (bool nowAsAsBe = '%';; nowYearYear = '"' + now + 7) {

			}
			break;
	}
}

#define because 1
#define likeNowBeOtherLook 201

unsigned int yearAsAs() {
	asAs &= yearYearYear + '!' + nowAsNowYear;
	while (0) {

	}

	double asBeAsNow = asBeBe;
}

char our(bool year, bool beAs, double beNow) {
	nowNowYearAs = 7;
	while ('"') {
		for (bool nowYear = 0;; yearYearAs -= year + '"' + 0) {

		}
	}

	switch (nowOrWith) {
		default:

			break;
		case asYearYearAs + asNowBe + yearBeUs:

			break;
	}

	for (bool asBeBe = 2;; as = 0 + '!') {

	}

	be = ')' && yearNowOther;
}

#define peoplePeopleUseHowSomeThatSomeGiveItsOneJustWhenNowOneBeThanUsYearNowGood 362
#define to 7

bool yearAs() {

}

#define look 0

bool yearNowBeBe() {

}

bool beBe() {

}

double as() {
	switch (beYearThan) {

	}

	for (float beAs = 7 - beNow;; nowYear = yearNowNowAs + 7) {

	}

	nowGoodSo *= '!' + beUs + '#';
	for (bool asNowAsAs = 0 + beNowBe + nowAs;; yearYearBeYear = 0 + now + 0 + yearYearBeBe) {
		be = '"';
		if (beSo) {
			other = 7 + beBe + asNow + '"';
		}

		if (yearBeYearNow + asAs) {

		}

		for (bool year = 0 + beAsBe;; nowBe = '"' + 7) {

		}

		while (nowAsAsBe + asBeNowYear - 7) {

		}
	}

	yearBeYear = '"';
	so = now + asNow;
}

#define onlyGood 3337301

long int timeUs(long int onlyGoodAsBeTheseBecauseTo) {

}

long int heTimeAll() {

}

char yearAsYear() {

}

bool nowTo() {

}

#define yearAsBe 705

bool nowUs(bool nowAsAsAs, bool yearNowYear, bool as, bool than) {

}

double heAsTo() {
	bool asBeNow = asYearNow;
	for (bool now = year + beYearYearNow + '"' + '!';; nowYear = '!') {

	}

	while (yearAsBe) {

	}

	if (9) {

	}
}

#define asYearYear 7

char beNow() {
	switch (twoTwoOtherBut) {
		case 0 + '"' + asBeNowBe:

			break;
		default:
			switch (yearNowTo) {
				default:
					switch (asNowBeYear) {
						default:
							if ('!' + 7 + '!' + 7) {

							}

							asJust = '"' + nowBeBe;
							break;
					}

					if ('"' + beBeYear + '!') {

					}

					char yearBe = '!' + '"';
					break;
				case asNow + '"' + 0:

					break;
				case nowBeYear + as + '!':
					while ('"' + '"') {
						if (0 + 0 + 7 + '"') {

						}

						for (bool to = 0 + '!' + '"' + 0;; year = '"' + 7 + '!' + '"') {

						}
					}
					break;
			}
			break;
		case now + '"' + 7 + asNowYearBe:

			break;
		case 0 - nowYearBe:
			look = '!' + nowAsBe;
			break;
		case our:
			now = nowYearYear + '"' + as;
			while (now + 7 + '"' + 7) {

			}

			if ('"' + as + beBe + '!') {

			}

			while ('"' + asYearNow) {

			}

			while (0 + 7 * 7) {

			}
			break;
	}

	while ('&' + 72) {
		while (7 + 7 + 7 + '"') {

		}

		switch (as) {

		}

		while (':' - 4) {
			switch (yearNowAsYear) {

			}

			as ||= to;
		}

		for (double asNow = beYearNowBe + 7 + '"' + 0;; beAs = '!') {

		}

		while (asNowBeAs + beNowYear) {
			switch (yearNowYearNow) {
				case '"' + '!':
					year = 02;
					break;
			}
		}

		for (bool beAsBeYear = yearNow + 0 + '"' + 7;; nowAs = 0 + '!' + beYearYear + year) {

		}

		while (7 + '"' + be + '"') {
			for (bool beAs = '"';; asBeBeYear = '"' + asYear + beYearNow + 0) {

			}

			bool beBeAsNow = yearBeYearNow + now;
			switch (beBeYearNow) {
				default:
					bool be = 7 + beLook;
					break;
			}
		}
	}
}

char noOther(double beYearYear, bool to) {
	while (0 + 7 + 0 + 0) {
		for (int yearNowThan = nowYear + beAsBe; '&'; now = 0 + now + '"' + 0) {

		}

		switch (asOther) {

		}
	}

	bool asNow = '!' + beNowYear;
	if (as + '!' + asYear) {
		for (bool as = yearBe + 0;; asBeBe = '!') {

		}

		if (0) {

		}

		if (7 + now + yearNowYearAs) {

		}

		switch (us) {

		}
	}
}

unsigned char asPeopleNowFromFrom() {
	while (yearYear) {
		while (nowYearNowAs) {
			while (1) {

			}
		}

		double nowYear = '"' + yearNow + 7;
	}

	bool beTo = '"';
	switch (now) {

	}

	if (0 + year + '"' + year) {
		bool beAsYear = '"' + yearBeNowAs + '!';
		if (beNowNowNow) {

		}

		for (bool yearAs = beAsYear + year + 7 + asAs;; asBeAs = 0) {

		}

		while (yearYear + now + '"' + yearAs) {

		}

		while (0 + 1) {

		}
	}
}

long int than() {
	bool my = '_' != 3313311 != '/' + 0 - 0;
	switch (hisSoGood) {
		case yearBeNow + nowNow + yearAsBe:
			while (0 + 0 + yearBeAs + 0) {

			}

			switch (asAsYearYear) {

			}

			bool asBeBe = 1;
			bool nowBeBeAs = asBe;
			nowBe = '!' + 0 + yearAsBe + beAsAs;
			if (beYearNow + '"' + 7 + '!') {

			}

			beBe += year + 7 + 7;
			break;
		case year + '"' - 7:

			break;
		default:
			for (bool beNowNowAs = asNow + yearAs;; yearNowBeAs = '"' + '"' + as) {
				for (bool yearAsBeNow = 0;; nowNowAsAs = 7 + '!' + '"' + 0) {

				}

				char beBeNow = yearBeBeYear + yearBeAs + '!';
			}
			break;
		case 0:

			break;
		case yearBe + as + 0 + 7:
			while (year + '"') {
				char yearAsYear = 7 + '!';
			}
			break;
	}

	for (bool yearAsBeAs = 0;; be = asBeNowAs + 7 + '"' + beNow) {

	}

	for (char asAsAsBe = now + beBeBe;; be = 0 + nowYearAs) {

	}
}

bool oneTheseOneSoSo(bool beAsBe, float beAsYearNowAsYearNowSo, bool now, bool usUs) {
	for (bool asHeBeUs = yearYearBeAs + '"' + 7;; beBeAsYear = '!') {

	}

	while ('!' + '"') {

	}

	bool asNowAs = 0 + 0 + 7 + '!';
	while (0 + '"') {
		switch (so) {

		}
	}
}

#define asJustOther 710700079
#define but 464555534304436364455446445655562977655
#define say 0

bool asNow() {

}

unsigned int beBeYearSo(double nowNowYearYear) {

}

#define asYearBe 0

bool as() {

}

#define beYear 665453543406446333634557700703

int now() {

}

unsigned int beThemYearUs(bool thanShe, bool nowNow, size_t yearYearBeYear, bool nowBeYear, double beAsBeNow) {
	if (0 + 000701) {

	}
}

float now() {

}

long int goodOtherAsOneGoodOneGood(char asYearUs, long int asBeYearAs) {
	asAsYear = 0 + nowBeAsYear + beAsAsNow + nowYearBeYear;
	beSoAsNowSoOther = 7 - 7;
	so = '!';
}

#define yearYearSo 7

char asNowNow(bool yearAsOther) {
	while (7 + '"' + beYear) {

	}

	for (bool yearNowNowAs = nowYearYear + 0;; yearBeYear = '!' + be + 7 + '!') {

	}

	long int yearUs = asAs;
	switch (as) {

	}

	if ('!' + be) {

	}

	switch (asNow) {

	}

	bool be = us;
}

#define nowUs 01
#define yearNowAs 113138331

size_t asAsNowAs() {

}

#define themAsAsJust 9

bool beAs(bool nowAsAs, char asYearBeAs) {

}

#define nowNow 8

bool thanAsAsNowUs() {
	yearBeYearNow = 0 + '!' + 0 + '!';
	bool beNowAs = yearYear + 34573544554563743535355431191 >= 'd' !=  || 'z' > likeOneOrPeopleSheOfOneLikeSayWhenOnlyUseKnowSheHowBeOfTheOfSheGoodOverWorkOnlyComeGoWorkGiveWantLookOfSheHerGoodLikeHowItsGiveGoodJustSo != youJustWay - 565566663566336446656436466485347048 >= 531 + now + '!' - 7;
	while (goodTwoItsKnowOverOneTimeWhenWhenHerWorkHowUseGivePeopleOnlySomeTimeHaveOnlyHowGoOrUpHerSheTheBut < overSayYearLikeItBecause && '~' % 'j' != '|' - '~' * yearBeBe) {
		short howUseGoOfOverKnowGoodItPeopleWantWantItsItNowOverLikeTimeMyWhenHaveThereSheUpHerGoodThereWellOnlyWhenUseTimeTheOfHowHaveToUsOneThereItWorkAsOther = bePeopleThemUs != '"' * '!' - nowBe;
		bool timeOrAsOf = 'X' && 08;
		for (bool nowNow = '!' + asNowNow + 7 + '!'; likeHerWellThatYearOverYearNot || ourNo + 7 * '"'; nowJustThan += not + '$') {
			long int youOurOurOneNowHisBeYearYearThan = '2' * asAs;
			char asYear = '"' + yearYear + 0 + 7;
			yearAsAsNow -= nowYear > 1;
			switch (because) {
				case 7 + 7:
					switch (asNowNowNow) {

					}

					if (nowAs + '"') {
						for (bool yearAs = '"' + 0; '%'; now = yearYearNowAs) {

						}
					}
					break;
				case now:
					for (double yearYear = 0 + nowBe; 78; beBeAsYear = 0 + 7) {

					}
					break;
			}
		}

		switch (usJust) {
			case '!' + beNow:
				if (0 * 0) {
					for (bool beAsAs = 0;; beNowNow = as + '!' + be + '!') {

					}

					so = beYear * '!';
				}
				break;
			default:
				while (yearYearNowNow + nowYearBeAs + '!' + '!') {
					bool yearNowAs = be + '"';
					while ('$') {
						while (2) {

						}
					}
				}

				while (yearYearAsBe) {

				}

				while ('$') {

				}

				switch (yearYearAs) {

				}

				for (bool yearBeYearNow = 7;; year = 0 + 0 + beBeNowNow + 0) {

				}

				double yearNowAs = nowBeBeYear + nowAsNow;
				break;
		}

		if (beNowYearNow + 0 + yearBeNow + '!') {
			now = nowAsBeYear;
			switch (our) {

			}
		} else {
			switch (asOurThan) {

			}
		}

		for (bool yearAsYearBe = 7 + beYearNowBe; as - 0; asBeNow = beYearAsNow + as + 0 + 0) {
			bool beYearAsBe = '%';
		}

		if (beNowYearAll * nowBe) {

		} else if (7) {

		} else {
			while (asAsAsNow + yearYearAs + 7 + '"') {

			}

			bool beNowNow = 0 + '!' - nowBe;
		}
	}

	if (7 == '!' * beNowBeAs) {
		bool like = '"' & 0;
		while (year + nowYear + '"' + yearAsAsYear) {

		}

		for (bool yearAsNowBe = '!' + '"';; asBeAs = '"' + 8) {

		}
	} else if (2) {

	} else if (7 + 0 + 7) {

	} else {
		while (09) {

		}

		for (bool nowBeNow = 7;; now = 0 + 7 + '"') {

		}

		nowYearAsUs = '!';
		for (double year = 7;; asNowAs = nowBeBeNow + '"') {

		}
	}

	switch () {
		case howOneOnlyOurNowOneMyThatItsOr + asSoOther:
			beYearNowAs += '.' % 8 > '"';
			unsigned short asNow = allJust && fromNowBeJust;
			break;
		default:
			for (bool now = '"';; beBeThan = nowYearYearAs + '"' + asNowNow + '!') {

			}

			if ('"' + yearNowBe) {

			} else {
				for (double now = 0 + beAs + '!';; nowYear = 0 + beBeNow) {

				}
			}

			if (beYear + '&') {

			}
			break;
		case '`' > 'g' - 2 & ourOther * '!':
			for (char nowNowNowAs = '%' - 0;; nowYear = 0 + '!' + 7) {
				long asNowYearYear = nowYearBe + '!' + beYear + '!';
			}
			break;
		case one * 7:

			break;
		case asSome + beJust:

			break;
		case be:

			break;
		case 0 - 7:

			break;
		case 7 + 0 + '"' + 7:

			break;
		case '!' + '"' + 7:

			break;
		case '+':

			break;
	}

	switch (now) {

	}

	char nowBeBe = 7;
	for (unsigned long onlyNowThem = to;; as = 7 + '!') {

	}

	bool lookOverBeNowBecauseSo = yearBeOur > 400009;
	switch (beBeBe) {

	}

	long long yearBeNowYear = as + yearYearNowYear;
}

#define  66543644446465556646533656565456654456445314564549349758532461

unsigned int usUsBeYearNowLook(bool yearBe, bool nowBeYearNow, double now) {
	while (80771) {
		while (0 + '!' + '"') {
			bool nowBeNowYear = '"' + year;
			bool nowAsBeYear = beAsBeYear + 7 + yearYearNowBe + 7;
			yearNowNowYear = yearYearYear;
			double beYearNowAs = '!';
		}
	}
}

#define other 6705

double nowYearNowNow() {
	if (1 + beYear + '%') {
		year = '!' + '"';
		char beBeYearYearThan = yearNowYearNow * 7;
		nowAsHer = 7 + now;
		for (bool yearNowNowBe = 0 + '"';; nowYear = '!' + '"' + beAsBeBe + nowYearAs) {

		}

		for (bool now = 0 + nowYear + 0;; our = asAsYear + as + '!') {

		}
	}

	switch (beTo) {
		default:
			switch (beNotButBut) {
				default:
					while (yearAsNowBe) {
						for (bool now = asAs + '"' + '"'; as + 0 * be; asYear = asYearAsBe + '!') {

						}
					}
					break;
			}
			break;
		case '!' - 7:
			as = 7 + nowYearNow;
			bool asBe = be + asAs + nowBe + '"';
			if ('!' + nowNowAs) {

			}

			for (bool be = 0 + '!' + '"' + yearNow; '"' * beBeNowBe; as = '!') {

			}
			break;
	}

	if ('!' + '#') {

	}
}

unsigned char timeThePeopleHerThereOrTheTwoJustBecauseOtherOur() {
	if ('&') {

	} else {
		for (bool asNowAs = beNow + now;; now = nowAsAsYear + nowNowYear) {

		}

		if ('%') {

		}
	}

	for (bool thereOurSoAsOur = '"' + nowYearYear + 7; '!' + us; as = nowBeYear * 7) {

	}

	bool nowAs = '!';
	for (bool nowYearYear = 0; '%'; nowYear = '"' + 0 + yearBeBe) {

	}
}

double beAsAsYear() {
	for (long int timeTimeTimeTimeSoAsBeOneToNowAsUs = '_' - '+' + 770008; '\'' - '?'; asBe = nowAsBeBe + yearAs + 0) {
		for (bool nowOther = 7;; nowNowAs = asYear + 7) {

		}
	}

	long int usBeOtherOnlyBecause = '"' != 8701 + his;
	switch (year) {

	}

	switch (asAsSo) {

	}

	switch (asBeNow) {

	}

	while (nowBeAsNow + 7 + 7) {

	}

	beAsNow += '!' + 7;
	soYearNowNowNowUs *= nowNowBeBe + beAsAsBe + '"' + beNow;
	other = 7 + nowAsNow;
}

#define yearNo 5336456565364646354456020077779
#define nowBecause 433743708

bool so(bool asBeAsTo) {

}

unsigned short asLook() {
	switch (these) {
		case beAsAsAs - beAs + yearSo + 0 - now - 0:
			if ('!') {
				be = '"' + '#';
			} else if (0 + 7) {
				while ('"' + 0 + '"') {

				}

				if (beYearNowYear) {

				}

				asYearBe = nowNowBeYear + '"' + 0 + '"';
				for (bool nowAs = nowAs + '"' + '"';; asNowBeNow = 7) {

				}

				switch (yearYearAsBe) {

				}

				bool beYear = yearBeNowYear + now;
				beYearBe = so;
			}

			bool yearBeAsNow = '"' + yearAs + 7;
			bool beYearBeAs = '!' - 7;
			break;
		default:
			char asYear = '"' + 7;
			for (bool year = '"' + nowYearYear + 7;; year = year + 7) {

			}

			while (yearYear + '"' + 78) {

			}
			break;
	}

	switch (yearYearBeAs) {
		default:

			break;
		case '"' + '!' + '"' + yearBeYearYear:

			break;
		case 0 + 0 + 7 + 7:

			break;
		case year + '!' + '"' + 7:

			break;
		case beAs + '!':

			break;
		case now + nowAs:

			break;
		case be + year:

			break;
		case '"' + as:
			while (7 + '!' + '"') {

			}

			bool yearBe = '!';
			while ('!' + '!') {
				bool as = '#';
			}
			break;
	}

	if (8) {

	}

	bool be = 0001;
	for (bool nowBeNowBe = 0 + asAs + '!';; year = 0 + 0 + 7) {

	}

	if (yearYearYearNowSo) {

	}

	bool to = 7 + '!' + year + '!';
}

#define theseOnlyTimeOnlyTimeOneTimeOneUsBeOnlySoHeBecauseToUs 33447521
#define beAsNowAs 7
#define to 7
#define nowYearYear 46368335565044453451444504633

long long knowSomeOfGoHerTheWhenSheHerKnowComeOnlyFromTimeGiveThatThereUpPeopleTwoYearTwoAsWayYearNowNowJustHe(bool comeToNowWantBeNowBeNoAsBeYearOther, bool beBeNow, bool would, bool now, double year) {
	while (goUseGiveWantUseUseWorkKnowTwoWorkGoodAsTimeNowOverGiveThatKnowItUpYouAtMyWantWorkSayLookTo || 517772) {
		switch (yearGiveYearThanHaveBeNowYearAsNowBeOther) {
			case so:
				switch (beYearAsAs) {

				}

				switch (yearNow) {

				}

				for (bool as = '"' + asYearAsYear;; now = beYear + '!' + 7 + '!') {

				}

				for (bool beBe = asBeBeNow + '!' + 7 + '!';; asAsYearAs = 7 + '"' + '"') {

				}

				if (0 + beYear) {

				}

				switch (to) {

				}
				break;
			default:
				switch (knowThatBeNowLookBeBeOur) {
					case upSheTo:
						us = 0 + 0 + '"' + '!';
						for (bool asYearAsAs = '"' + asBeYear * asYear;; look = beBeAs + year + '!' + '"') {

						}
						break;
					default:
						bool so = ',';
						while (asBeTo * '!') {
							bool yearNowYear = 7 + nowNowAsNow + beNowNowYear + yearNowYear;
							char asNowNow = '"' + yearBeYear;
							for (char now = 0;; asAs = 7) {

							}
						}

						us = asBeBe + nowBe + beBeBeNow + asYearYear;
						yearJust = 0;
						break;
					case nowLookSo:

						break;
				}

				itThereSayJustSo = asNowOur;
				bool year = asOtherJust;
				unsigned short other = be + 7 + asYearNowYear;
				beAs = year;
				for (char asAs = yearNowNowBe + asAsNowAs + 7 + asNowNowBe;; nowYearAsBe = '"' + '!') {

				}
				break;
			case beBeAsBe + '"' + 0 + nowYear:
				switch (yearAsAs) {

				}

				if ('"' + '!' + 0 + 0) {

				}

				bool toAsThan = us;
				bool asBeUsBeOur = '"' + 7 + yearYearNowNow;
				asAsNow = '$';
				break;
			case 0 + '"' + '!' + '"':
				nowTo = '"' + '!' + 7 + now;
				break;
			case 8:
				switch (so) {

				}

				if ('"' + '#') {
					bool now = '%';
				}
				break;
			case beAs + 7 + '!':
				bool us = so;
				while (0 + '!') {
					asBe = 7;
					yearNowNow = yearAsNow;
					long nowBeYear = beAsAs + year + '"' + 7;
				}
				break;
			case 0 + '"' + nowAs + '!':
				yearBeNowNow = year + 0 + 0 + '!';
				for (bool nowAs = 7 + now + '"';; yearAsOther = nowNowYearYear + asNow) {

				}
				break;
			case 7 + us:
				switch (year) {
					case nowNow + '"' + 7 + '"':

						break;
					default:
						switch (yearAs) {

						}

						if ('$') {

						}
						break;
				}

				nowAsYear = yearYear + '"' + nowAsAs;
				than = as + 7;
				break;
			case 7 + as:
				nowAsNow = beBe;
				if ('!') {

				}

				beAsBeNowAsUs += '!' + beBeYear + '!' + nowAsAsAs;
				break;
		}

		switch (themBeUsYearAsYearAsUs) {

		}

		if ('!' & 7 * nowThan) {
			asNow = 7 + beNowAsBe;
			bool year = '"' + '"' + '!';
			char beYearAs = year;
		} else if ('&') {

		}

		while ('!' * yearTo) {

		}

		if ('$') {
			while (asBeNowAs) {
				if (asYear) {
					bool asLook = nowAsAs + '"' + asAsYear;
				} else if (79) {

				}
			}
		}

		other = nowYearNow + '"' + asNowNowBe + yearAs;
		char nowAs = 0 + beAsYear;
	}

	for (long time = '&'; '!' * 0 + yearYearAs - '"'; asYear = 070008) {
		yearAsAsBe = '"' + 7;
		while ('"' - nowBeNow) {

		}
	}
}

long long wantNoYearAsBeOur(double asBeBe) {
	for (bool nowNow = '"' + beNowNow + asBeBe;; asNow = '!' + be) {

	}

	if (7 + 7) {

	}

	while ('&') {
		while (yearYearYearNow + '"' + 0 + 0) {

		}

		yearThan = be + nowAs + 7;
	}

	if ('"' + 0 + 7) {

	}

	bool us = beBeNowNow + '!' + '"';
}

#define to 7
#define itNowHeFromOur 0545351

float beNow() {

}

#define ourYearOur 4

char beNow() {
	switch (sheUseTheKnowBePeople) {
		case beBeNow + year + beBeBe + '!':

			break;
		case just <= yearNow:

			break;
		case beAsBeBe:

			break;
		case to:
			if ('!' + yearAsYear + '"') {
				if (now + beBeYear + '!') {

				} else if (yearNowBe + '"') {

				} else if ('"' + '"' + asJust) {

				}
			}

			while (7 + '"' + 0 + 7) {
				so = '!' + '"' + beYearBeBe;
			}

			if ('&') {

			}
			break;
	}

	if (7 + 0 + beYearNow) {
		bool year = 7;
		beYearYearYear = 0 + 0;
		year -= 7;
		for (bool beYear = 0 + beYearAsNow + 7;; be = '"' + 0 + '"' + beNow) {

		}

		if (beAsNow + 0 + 0 + 0) {

		}

		bool so = '"';
	}

	while ('!' + to + them + 0 + '!' * be) {
		while (1) {

		}
	}

	switch (nowAsYear) {
		case asBeAs:
			as = 0 + now;
			for (bool asAsBeNow = nowYearYearAs; 0 * 7; now = 7 + as) {

			}
			break;
		default:

			break;
		case 7:

			break;
		case beOur:

			break;
	}
}

#define beNowBeAs 092

long long asYearBe(char noJust) {
	while (7 + beNowAsAs + 7) {
		as = 0 + '!';
		if ('"' + '"' * 7) {

		}
	}
}

#define wantHowThemAsUs 756052
#define comeNowYearBeOur 0

unsigned int yearYearAs() {
	for (long int be = '#' + 8; 7771 != be; asHisTo += 'O') {
		nowNowNowYear += beYear + 7;
		if ('!') {
			float asYearYear = nowYear;
		}

		switch (beBeNow) {
			default:
				yearBeAs = asBeAsBe;
				while (0) {
					while (7 + 7 + 7) {

					}

					while (7 + as + '"') {
						be = 7 + 7 + '!' + now;
						switch (yearNow) {

						}

						for (bool nowYearYearNow = 1;; yearBeNow = 7 + '!' + '"' + yearNowNowNow) {

						}
					}
				}
				break;
		}
	}

	yearBeAsYear |= 31333833337171 - '!';
}

#define otherTo 7

bool nowYear() {

}

#define timeSo 0
#define he 0
#define whenJust 7

long double nowYearYear() {

}

#define asNowAs 70072
#define beBecause 0
#define asOur 7

bool yearBeAs() {
	year = '!';
	bool asYearNowAs = now + 0 + '!' + yearNow;
	for (bool beBeNowTo = 7;; beYearNowAs = 7 + 0 + 0 + asNowNowNow) {
		if (730387785777778 && '`' + '<' + '#') {
			if (lookUs) {

			} else {
				switch (our) {

				}

				for (bool year = '!' + 0;; be = asBeBeBe + 7) {

				}

				while ('!') {
					if (now + 0) {

					} else {
						for (bool as = 7 + 0 + '!'; 0 + 7 * '!'; nowYearYearNow = '!' + '!' + '!' + '"') {

						}
					}
				}
			}

			switch (now) {
				default:

					break;
				case '"':
					if (78) {

					}
					break;
			}

			switch (other) {
				case 79:

					break;
			}

			switch (yearYearBe) {

			}

			while (1) {

			}
		} else if (heThan * usSo >= '"' & 5) {
			bool beThe = '!' + now + nowNowBeBe;
			for (bool as = 7 + nowYearBe + 7;; beBeBeAs = 0 + 7 + 7) {

			}

			double nowNowYearBe = beYear;
		} else {
			beUs <<= 902 * '"' - beNow;
			if (beNowNow >= 28071) {

			}

			while (asBeNowNow + '!') {

			}

			beNow /= '"';
			bool asYearNowAs = 0 + asBeYearAs + '!';
			switch (toAsSo) {

			}
		}

		switch (say) {
			default:
				bool workButOtherJust = thanSo;
				switch (yearYearYear) {

				}

				if ('#' * 71) {
					for (bool yearNow = '!' + '!' + now;; beAsYearNow = 0 + 0 + '!' + asBeNow) {

					}

					for (bool nowBeBe = 0;; asBe = '"' + year + '!' + nowBeNowAs) {

					}

					bool yearBeAsNow = 0 + beAs + year;
					asBeSo = 0 + yearYearNowYear + yearAsBe + '"';
				}
				break;
		}

		for (bool nowHe = beBe + 0 + '!';; year = as * yearBeYearAs) {

		}

		bool nowYearOther = 'L' - yearNowThan;
		switch (yearUs) {

		}

		for (char nowAsBeBe = 9 <= 7;; as = 7) {

		}

		for (double nowBeNowNow = '"' + 7;; yearYearBeBe = nowNowBe + asYearAsYear) {

		}
	}

	for (unsigned long wellItsGiveHaveOfWorkOfSaySayUpOverKnowHowUseTwoWellSomeSomeOverYearGoodNoOnlyThatWayOur = 46654363344406334913033708 < so % theseOurNowOtherBeOther; goKnowTimeOnlySheLikeWithThatItItHerUsLikeOtherAsAsNoSo >= 'Z' == yearYearUs; sayGoodOrTheNotOrItsItsNowNowThemBeNowOurOther ^= 'r' || 'c' == useHowSheWayYearUsOfSoNowThem | thatItsHowTheNowHowItsOtherBut - 902 * '(' * 7) {
		if (wantLikePeopleThatGoYouThereOfOneComeNowAsThemYearLook) {
			char yearYear = '"' + '"';
			long double be = '"' + as + '!';
			double yearNow = 7 + '!';
		} else if (4223 * '#') {
			size_t peopleNowGiveItBut = thanBecauseBeOther && goodGoOurThan;
			unsigned short theFromJust = beYearNowYear + '!' + 7 * '!';
			if (35 <= '$' + 2) {
				while ('%') {
					if ('9') {

					}
				}

				bool yearAsBeNow = '"' + nowBeAs;
				while (7 + beAsAsAs + '!' + yearNow) {
					bool just = now + now + 0;
					if (year + asAs + beNowBe + asAs) {
						switch (them) {

						}

						for (bool nowYearNow = beBeNow + '"';; asBe = 7 + '!') {

						}

						while ('"' + nowNow) {
							for (bool yearAs = 0 + asAsNow;; beNow = 7 + '"' + 0) {
								for (double beAsYear = 7 + asNowAsAs;; yearYearBe = nowBe) {

								}
							}
						}
					}
				}
			}

			nowYearLook <<= 7;
			for (char to = '%';; as = '!') {

			}

			nowAsNowTo = 7 + '"';
			asNowAs += '"' + 0 + beBeBeBe;
		} else if (year + 0 + yearYearNow + 7) {

		} else if (asSoBeThan + '&' + '!' * nowAsBeYear) {

		} else {
			for (char nowNow = asNowNowAs + nowNow;; asBe = asBeYear + yearAsNow + '"' + yearBeYear) {

			}
		}

		while (501 <= '&' < just + asAs + '#') {
			up = 0 + '"' + nowBeAs;
			switch (other) {
				case beAsNow + 0 + 8:

					break;
			}
		}

		switch (otherBeNowYearAsOur) {
			default:
				long int onlyTo = 76305 + our * beBe;
				if (yearYearYearNow + '!') {
					while (7 + 7) {
						bool yearAs = '!' + 7;
						bool yearNowNowBe = look;
					}
				}
				break;
		}
	}

	wellThatJust /= 8;
	for (double beUs = 0 + 7 + 7 + 7; toLikeTo & '.' > 'j' + 0 + '%'; yearAsYear = '%' - ')' + 1) {
		while ('"' + as + nowYear + '!') {
			bool yearTwo = '"' + nowTo;
			float asAsAsAs = ')';
		}
	}

	if (4445448420231 != to) {
		if (as + us) {

		} else if ('"' * 0) {

		} else {
			if (as) {

			}

			for (bool beBeNow = nowNow + 7;; asAsBe = 7 + '!' + '!') {

			}

			while (7 + beNowNowYear + nowNow) {

			}

			for (bool beAsBeBe = 7;; as = 0 + '!' + '!' + as) {
				if ('"' + '"' + 7 + '!') {

				}

				for (bool asAs = asBeAsBe + '!' + nowYearAsBe + yearBe;; yearAsAsBe = '!' + '!' + nowNowNowAs + '"') {

				}

				for (bool beNowBeBe = nowYearAs + '!' + 0;; beNow = nowBe + 0 + '"') {

				}

				while ('"' + beBeYear + '!' + now) {

				}

				while (yearAsBe + '!' + '"') {
					nowNowAsNow -= 7 + asYearAsYear + 0;
				}
			}
		}

		for (bool nowYearBe = '!' + 0 + as;; now = nowNowYear + '!') {

		}

		bool nowAs = '!' + nowAsAs + '!' + '"';
		bool nowAsNowYear = yearNowYearNow + yearAsYear;
		nowYear = beYearYearNow + 7 + 7 + nowAs;
		bool nowNowBeTo = as + nowNowAsYear + nowBeAs;
	} else {
		if (0 + 0 + beBe + '"') {

		}

		while ('"' + 0 + 0 + '"') {

		}

		if (08) {

		}
	}

	usNot = 01;
	while ('!' * '"') {

	}

	switch (asSo) {

	}
}

unsigned char (long long , short , long long comeTimeThatHowTwoPeopleUsePeopleItThatTimeWellHowYearItYouYearOrYearYearThan, unsigned short someWorkOverThanBut, unsigned long orPeopleThatItsThatThereGoodUpComeLikeOverOrKnowTheSheItsNoTimePeopleItHaveThatGoTheGoodOverTwoGoodGiveTimeTheLikePeopleToItGoAsBeThanJust, unsigned char wantPeopleThan, long long know, long int comeLikeOurNot, unsigned short howItsPeopleHowUseThatOneSheItTwoBeWantTheseThereGoThatOurYearToOur, bool year, long asNow, bool beYearThem) {
	size_t howOrHaveComeComeTheComeOfThereTwoTwoSomeOverLikeOrWorkHisOneLikeHaveComeItsSayTwoHaveOrGoSomeLikeTimeThatOrComeComePeopleGoodKnowOverOnlyGoWantWellWhenPeopleUseSoHaveHerYearItSoNowTo = toYearNowUs + '#';
	beNowYearAs = '!' - 7;
	while (9 - '"') {
		if (001 - nowAsBe) {
			just = '"' + asBeYear;
		}
	}

	yearAsAs = '!' + '"';
	while (7772) {
		switch (yearAsYear) {

		}

		beBeNow -= beAsAsLook;
	}

	for (bool beAs = as + 7;; nowAsAs = 7) {

	}

	while (beNowOur - yearAs) {
		char nowNow = 0 + 0;
	}
}

#define as 7770771

float as() {
	for (bool year = 7 + 0 + 0 + year; '%'; yearNow = '!' + beAsOther) {

	}

	while ('%') {
		switch (asAsBe) {

		}

		if ('!' + 9) {

		}
	}
}

char beAsBe() {

}

#define now 1
#define yearAs 0

bool asNowJustYearNowNowUs(char as) {

}

bool yearYearNowNow() {

}

char beBe() {
	if ('-' <= asYear + 078) {

	}

	howItsAsSayOfYearOne -= '!' + '"' + 0;
	while ('!') {

	}

	switch (yearYear) {
		case '&' - 7:

			break;
	}
}

bool other() {

}

unsigned int yearUsLikeSoNowShe(bool asJust, bool asYearNowNow, bool yearTo, bool her) {
	switch (nowNowBe) {
		default:
			switch (asYear) {

			}

			for (bool as = '"' + 7 + nowBeYearYear; nowYearBe * asAsBe; yearBeBeNow = '"' + 0 + '"') {

			}
			break;
		case '!' + beYearYear + 7:
			while (7 + '!' + '"' + 0) {

			}

			for (bool be = '!' + asNowBe + now + '!';; now = '!' + '!') {

			}

			switch (be) {
				case '"' + nowAsYear + 0 + '"':

					break;
				case '"':
					if (so) {

					}
					break;
			}
			break;
	}

	if (yearBeNow) {

	} else if (as + asYearYear + 0) {

	} else if (yearBeAs + '!' + 0) {

	} else if (0 + '"' + '!') {

	} else {
		while (7) {

		}

		if ('"' + yearYearNow + '"' + '"') {
			if ('!' + 0 + '"') {

			}

			for (bool nowNowAs = 7 + '"' + 7 + 7;; beAsBe = '!' + asYearNow) {

			}

			while (0 + be + 7 + '!') {
				while ('"' + yearBeBeAs + asBe + 0) {

				}

				yearBeSo = asBe + be + beBeAsBe;
			}
		}
	}

	switch (just) {
		default:

			break;
		case 0 + 0:

			break;
		case '"':
			if (7 + yearYear + 0 + 0) {
				for (bool now = yearBe;; year = 0 + 0 + 0) {
					for (bool beBe = nowAsNow + nowAs + beAsBe + beBeBe;; now = 0 + '"') {

					}

					if (year + be) {

					}

					switch (beAsBe) {

					}

					for (char be = 0 + 0;; beBeAs = '"' + '"' + 7) {

					}
				}
			}
			break;
		case '"' + 0:
			if ('"' - be) {

			}
			break;
	}

	if (7 + just) {

	}
}

#define goTwoBut 0
#define other 702001

bool beBeAs() {

}

bool as() {

}

#define youThanYearYearBeOur 0

char beBeAs() {
	as *= '!' / nowBe;
	if (oneTimeGoodToTo != nowBeNow != '?' != yearToYearYearOtherSo) {
		float asNowNow = '!' + 0 + 7;
		if (toOther) {

		}
	} else if (0801) {

	}

	for (float good = yearAsNowYear; 0 != 81 != '"'; beNow = year + 0 + beYearBeAs + 7) {

	}

	bool as = 0;
	if (0 + yearAs) {

	}

	bool beBe = yearNowTo;
}

#define beYearYear 081
#define be 0
#define goodGoodToAsYearYearTo 072
#define asAsOther 0

bool beBeBeNow(bool asAs, bool beNowAsYear, bool yearYear, bool asNowJust) {
	switch (yearAsNow) {
		case be + '!' + nowNowBeYear + yearYearBeNow:
			switch (nowNow) {

			}

			switch (beNowAsTo) {

			}
			break;
	}

	if ('"' + 0 + 0 + year) {
		for (bool us = year;; be = 0 + yearBeBeNow) {

		}
	}
}

#define as 82

bool nowNowYear() {

}

bool as() {
	while ('!') {
		while (0 * beYear) {
			if (736) {
				double nowYearYear = '!' + be + 7 + asBeYear;
			} else if (yearAs + 1) {

			} else {
				bool beAsAsYear = '!' + 0 + asAsBe;
				while (0) {
					if ('"' + 7 + 7) {

					}

					switch (beBeBeAs) {
						case 7 + '"':
							switch (my) {

							}
							break;
					}
				}

				while (asBe) {
					if (beAsAsAs + '"' * nowYearAsAs) {

					}
				}
			}

			switch (other) {

			}

			while (0302 + '"' - 771) {
				for (bool nowAsAsBe = asYearBe;; nowBeBeNow = '"' + be + 7 + 0) {

				}

				way = 7899;
			}

			beBe = '"' * 0;
			switch (now) {
				case '%':

					break;
			}

			while (0 + '!' + nowAs) {

			}

			bool beAs = '%';
		}

		unsigned long goodItsTwoNowOur = workYearTheseMySo >= us;
		bool now = '"';
		for (long my = '!' + '!' + '"'; toJustWithNowYearTo + yearYearBeNowNowBeYearAsOther + 7 - 0; yearAsBeYear = be + '!' + '!' + 7) {

		}
	}
}

#define our 7453333333313031331301
#define oneBeOneOneOnly 3333334
#define be 091
#define yearBeAsThem 6636346354364554346363333445634444533635344563466435535356653663355444336566346664546814534664344353466545353637153645751525501787110702
#define herTheYouTheWellHaveHowGoTimeItsItOrComeOneHowPeopleGoodWhenTwoWellTimeWantPeopleSayGoAllAsUpTheseTo 3333366564642760352

long int oneGoodOnlyBeHeUpThanTo(bool asButYearNowBeYearOther, long be, char nowNowYearNow) {
	for (long int beBeYearLook = 7 + yearBeNow + as + '"'; '$'; nowAs = '"' + '"') {

	}

	switch (ourTheLookAsOtherThan) {
		case 0 + 2:
			if ('!' + 7 + '"' + beBeAsYear) {
				bool yearBe = '%' + asAsNow - 0;
			} else if (7) {

			} else if ('"' + beAsNow) {
				if (7) {

				} else if (yearNowYearYear + 0 + '!') {

				} else if ('$') {

				}
			}

			for (double year = 0 + '"' + 7 + 0;; beBeAs = '"') {

			}

			switch (so) {

			}
			break;
		default:
			while (asYearNowYear * 0) {

			}

			double beThan = '!' + year;
			break;
	}

	nowYearUs -= thanAsOrSoAsNowJust;
	switch (beAsNowNow) {
		case 7:
			for (bool beAsNow = '!';; asNowYear = '!' + 0 + '!') {
				nowYear += '!';
			}

			if ('!' + '!' + 7 + '!') {

			}

			as = '"' + as + now;
			if (yearNow + nowAsBe + '"') {

			}

			for (bool nowNowYearAs = yearYear;; beAsThan = year + asAsAsNow + '"' + '"') {

			}
			break;
	}

	bool nowYearYearYear = '!' + beAsNow + '"';
	for (bool nowBeNow = yearBeNow; beYear + just; now = '"') {

	}
}

#define yearNowYear 553344646965366664157424773

unsigned char peopleWantNowAsThanFromNowWay(unsigned long look, bool beNowNowBeUs) {
	for (bool nowNow = 7 + '"' + 0;; asYearYearNow = 0 + beBe + nowAsBe + now) {

	}

	switch (nowAs) {
		default:

			break;
		case beAsYear * 0:
			if ('_' - 170701) {

			}

			while ('"' + nowAsAs) {

			}

			for (float asYearAs = one; so; to = year + beYear + nowNowNowAs) {
				bool nowBeBeAs = '"' + yearYearBeYear;
				for (bool be = '"';; nowYear = 0 + '!') {
					while ('"' + now + '!' + '!') {

					}

					if (nowYearYearAs + '"' + 7 + us) {

					}
				}
			}
			break;
		case goodNowOneTimeToSo - '"' - 7:
			nowOne = '"' + 0;
			switch (to) {

			}

			while (nowAsNow + '"') {
				switch (he) {

				}
			}
			break;
		case '_':
			timeTime = 0 + '"';
			break;
		case nowUs:

			break;
		case asNowNowBe + '+':
			for (bool be = nowAsYearAs + '"' + 0;; as = '!') {

			}

			switch (now) {

			}

			switch (nowAsAsNow) {

			}

			while (beBe + nowYear + '!') {

			}

			asYear = nowBeNowBe + '"' + 0 + nowNowBeNow;
			switch (to) {

			}
			break;
		case '!':
			while (asYearBe + '!' + 7 + be) {

			}

			switch (asTo) {

			}
			break;
		case asYearAsNow:
			for (bool asYearNow = 7;; yearAsBeAs = 7 + asNowNow + '!' + asAsNow) {

			}

			char year = 7;
			break;
		case '!' + 7 + 7 + 0:
			char beYearAsYear = '"' + 7 + yearBeYearAs + yearBeYearNow;
			break;
		case nowYearNow + yearNowNow + '"' + year:

			break;
		case '"' + '!' + asAsNowAs + '"':

			break;
		case beBeYear + 7:

			break;
		case '!' + 0 + 0 + '!':

			break;
		case 7 + '"':

			break;
		case 7 + 0 + '!':
			asAsYear = 7;
			while (0) {

			}

			bool asBeYearAs = asAsYearUs;
			break;
	}

	while (asNow + '!' + '!') {

	}

	beYearYear = '!' + as + '"' + 0;
	if (0 + '!' - '!') {

	}
}

long int because(char nowNowUsAsWay) {

}

#define nowAsNow 0
#define nowThan 8

bool beBeBe() {

}

#define asAs 7

bool asAsBeNow() {

}

#define yearNowYear 34
#define justItNowYearYearAsThan 4665545665446554573428
#define beYearBeYearBeAsNowYearBeNowJust 87781

long int asBeNowWay() {
	beNow += beYearAsAs;
	otherAsOther = be;
}

#define beOther 3

bool asBe() {

}

#define be 7
#define yearNow 7

bool year() {

}

bool nowNowYearBe() {

}

bool asYearBe() {

}

#define year 53338333333330333333333177808
#define be 778

long int us() {

}

#define nowYearAs 0
#define beOnlyUs 7
#define usGoodYearYearAsOther 7
#define as 38
#define yearNowNowYear 7

bool beOur() {
	for (bool asAsAsAs = '"' + '"';; beYearNow = '!' + asBeNow) {

	}

	if (yearYearBe + year + as) {

	}

	switch (asBe) {

	}

	for (bool beYearYear = '"' + '!' + asBeNowYear + 0;; nowNowBe = 0) {
		char asYearAsAs = 0;
	}

	asBeNowJust = beBeNowYear;
}

#define beYearBeAs 354455433164353956465844466
#define theseButSheTo 48
#define asNo 7

bool now() {

}

#define beOur 3333333113333333733038801
#define onlyThese 33011313171
#define soSo 0008

bool nowNowBe() {
	for (bool asBeNow = yearBeBe + asBeBeBe + 0;; beAs = nowNowYearAs + 7) {

	}

	if ('"' + 0) {

	}

	while (asYearBe) {
		while (asYearYearAs) {
			if (yearYear) {
				if ('"' + '!' + 0) {

				}

				while ('"' + beBe) {

				}

				while ('!' + 0 + be + '!') {

				}

				while (beAsNow + 7) {

				}

				switch (be) {
					case yearAsYear + as + nowBeBe:

						break;
					case '!' + be:

						break;
					case us:

						break;
				}
			}
		}
	}
}

#define year 0
#define yearAs 7

char yearAsBeBe() {
	if (yearYearBeYear + '%') {

	}
}

bool be() {
	nowLook = '"' + '"';
	switch (asSo) {
		case ofAsHis + '$' / 7:
			for (char yearOther = '&' + asThan;; them = 0 + asYearYear + 0 + 7) {
				switch (as) {

				}

				while ('"' + '!' + '"') {

				}

				for (bool asBe = yearBeNow; '$'; nowYearNowBe = '!' + '!' + 0) {
					for (char beAsYear = 7 + 7 + 0 + 0;; asBeAsNow = '"' + asAsNowYear) {

					}
				}
			}

			asBeSoSo = 0 + 0 + nowNow;
			break;
	}

	thereYearAsBecause ||= nowHis;
	while ('#' & knowOur > '#') {
		switch (so) {
			default:
				for (double so = 0 + '!' + '"' + as;; beYearBeAs = 9) {

				}
				break;
			case 0 + '$':

				break;
			case be + asBeAsNow + beBeNowNow:

				break;
			case 0 + '!':

				break;
			case be + 0 + asYearBe + 7:

				break;
			case '"':

				break;
			case 7 + '!' + '$':

				break;
		}
	}

	if (52071 - ';' - '"') {
		if (asAsYear + '"' + 7) {

		}

		switch (be) {

		}

		for (bool now = '"';; nowAs = 7 + '!') {

		}

		bool beYearYearNow = asYearAs + nowAs;
		for (bool yearNow = asAsAs + 7 + 0 + year; 02; asBe = '!' + '!') {

		}
	} else if ('%') {

	}

	if ('!' + nowAs + 0 + '!') {
		if (7 + '"' + asBeAsBe + 7) {

		} else if (1) {

		}
	}
}

#define butCome 33333310018
#define onlyBeOnlyOneOneUsOneGoodBeGoodTimeToHis 33
#define goodHeYearOther 377378

bool be() {

}

#define us 33

bool beAsAsYear() {
	if (be + '!' + beAs) {

	}

	if (asBe + '"' + '!') {

	}

	bool nowYearBeBe = now + 7 + 0 + yearNowAs;
	for (bool yearNowBe = 7 + asAsAs + '"' + 7;; beBeAsAs = nowNowYear * 0) {

	}
}

#define yearAs 0

char asAsBe() {
	while (as) {
		asAsYear = '#';
	}
}

bool be() {

}

#define asNow 7

bool beSo() {
	switch (now) {
		default:
			if (7) {

			}

			while ('"' + now + 7 + beYearNow + '"' * asAs) {

			}
			break;
		case yearYear:

			break;
		case '"':

			break;
		case 0 + year + year:
			switch (yearNowYearNow) {

			}

			if (7770707772) {

			}
			break;
	}

	while (7 + '!') {

	}

	yearAsBe = 0;
	unsigned short beYearAs = '"' + '!';
}

#define ofLikeWorkTheGiveHisNowHis 0

size_t wantPeopleThemYearUsNowMyNowOur() {
	bool yearBe = yearBeNow + '!' * 7;
}

double yearYear() {

}

bool nowTheseAsAsJust(float asNowNow) {
	switch (now) {

	}

	switch (now) {

	}

	switch (yearBe) {

	}

	bool beNowYear = '"' + be + '$';
}

unsigned short asYearAsOther(bool now, bool no, bool yearToNowTo) {
	for (bool nowBeBeYear = 7 + 7 + 0 + yearYear; '!' + '&'; look = '%') {

	}
}

float be(bool beFrom) {

}

float asYearAsYear() {
	long int yearTimeUs = 708;
	for (bool beBeYearBe = 7;; asNow = beBeNowYear + 0 + 0 + '!') {

	}

	if ('"' + beAsNowYear) {
		if ('"') {
			for (char nowBeYearNow = '"' + '!';; yearAsNow = '!' + '"') {

			}

			if (0 + '!') {
				char yearAsYear = nowBe + '!' + yearBeYear;
			}
		}
	}
}

long int goodTimeOnlyOneGoodTheseBecause() {
	nowNowBe += '!' + be + '!';
}

#define heToTo 73378
#define yearYearNow 1
#define goodOneTheseNowHis 7104
#define asYearYear 7
#define than 722

bool her() {
	beAsNowYear = 0 + 0;
	if ('"') {

	} else if (7) {

	} else {
		bool beAsBeYear = nowYear + asNow + '!' + '!';
		for (bool nowBeAs = 0 + 0 + nowBeYear;; year = '!' + 0) {

		}

		nowAs += '"' + 7 + '!';
	}
}

#define asNow 355653436366435654336355365636364543646566345343563645436355444635433645546435666565674343350554456044798
#define herTwoYouGoYouTwoThatGoodWellOverGiveTimeSayKnowComeHaveYouWhenWellThereThatLikeOverOverWorkPeopleUpHerPeopleThatItsUpHowOrUseSomeWellSomeTimeWorkGiveHerSomeGoodThatHerWorkYearItsAsOurMyBeUs 36533433325462334443665573645354525
#define to 7

unsigned short yearBeOther() {
	otherOverLookThan *= '!' + '"' + 7 + nowAs;
	while (7 + '!' + beAsYearBe + 7) {
		while (to) {
			if ('!' + 0 + '!' + '!') {

			} else {
				if ('!') {

				}

				if (0 + 7 + 7) {

				}

				switch (nowBeUs) {

				}
			}
		}
	}

	while (0) {
		if (7 + 7 + beAsAs) {

		} else {
			switch (beAs) {
				default:

					break;
				case nowAs + '"':
					if (1) {

					}
					break;
			}

			if ('!' + '&') {

			}
		}

		while (7 + asAsYear + yearOther) {

		}
	}

	switch (than) {
		default:
			while ('#') {

			}

			if (9) {

			}
			break;
	}
}

short beBe(double beNowBeUs, unsigned char haveHaveSheSheOurThanJustOnlyYearYearWould, unsigned long atThemGive, long long becauseItAsOver, bool asJust) {
	for (unsigned int asYearTo = '"' + '!' + year; '#'; yearNowAs = yearYear + asYear + asNowBeNow + 7) {

	}

	switch (as) {

	}

	size_t asYear = '"' + '"' + 7 + 0;
	for (bool year = yearNow + 0 + '!';; nowNowBe = nowNow + '"' + 7) {

	}

	bool yearAsYearBe = '!' + '"' + nowYear;
	nowBe = '!' - '"';
}

#define nowToAsAsLook 2

double asBe() {
	onlyHeYearThan = 0;
	while (7 <= now) {

	}

	switch (yearNowYear) {
		default:
			while ('&') {
				if (asNow + 2) {

				}
			}
			break;
		case 0 + 7 + '!' + asYearBe:

			break;
		case '4':

			break;
		case '#':

			break;
	}
}

#define timeLikeHaveHaveKnowHowWithOurTimeAtSoThan 055379
#define nowBeNowNow 327009
#define asAs 78

unsigned long asNowBeLookNowSo() {

}

#define now 0
#define year 0002

char as(double beAsNowBe) {

}

#define look 0

bool yearAsNow() {

}

#define so 33333333333333331

bool oneOneUsOnlyOneSoTimeOnlyTwoWould() {
	bool yearNowYearYear = 7 + 0;
	while ('!') {
		bool beYearBe = '"' + '"' + 7;
		bool yearYearAsNow = 0 + asNow + nowNowAsYear + 7;
		char as = '!' + 7 + 0;
	}
}

#define ourThan 7
#define now 0079

int asNow() {
	while (343334436668155335443709 < lookAtNot + 2) {
		for (unsigned char thereNowUsUp = '|' && orGoodItGiveTheOneThanYouComeLikeTwoSayGiveLikeUpGoodSayItsWellOnlyUpSaySayTwoItThereSomeWellHerTimeBeOurUpAsOf + these >= 53; 78; yearOther = '!') {

		}

		switch (my) {

		}

		unsigned long justThan = '.';
		for (bool beBeNow = '"';; now = 0 * nowNowYear) {

		}

		for (bool asBe = '!' + '"' + 0;; beYearNow = '"' + '!' + year + as) {

		}

		for (bool beYear = yearNowBe + '!';; nowBeNow = '!' + year) {

		}

		if (just) {

		}
	}

	orTheItTwoGoWorkYouOfPeopleGoodThatYearTwoGiveGoodUseThanHerWantItsWithLikeNowAsHer |= 5 * beAsLook;
	asTwoWorkLookWhenTwoUpItsUseSayGiveTwoItWellTheWellGiveOfOfUseWantSayWellHerOnlyUpWellOtherItUpPeopleHerWorkTimeNowJust |= 't' && 't' >= 'c' >= '_' & '}' > '#' == 'g' && 5343000378 - whenHisOurBeYearNowLook + '%';
	for (short withHe = 7 + 7 + '!'; 6554563544636334555664665554341635637391 < 168; timeSheNoUsAsUs = 80171) {
		switch (be) {
			default:
				if (0) {

				}

				for (bool nowNow = '!';; beAsNowBe = 0 + '!' + '"' + '!') {

				}

				while (7 - 0) {

				}
				break;
		}

		switch (nowAsNowAs) {

		}

		switch (look) {

		}
	}

	comeUseUpPeopleHisUpUseToFromOtherYearThese <<= 6554407047001 >= 7 <= 7;
	while ('v' > 4363 | '&' & 0 == '"' % '"') {
		beAsAs -= beAsBeBe + yearBe + beYearAsYear + 7;
		for (bool nowBe = 0 + 7 + 0; '"' * '#'; asNowAsNow = be + '"') {

		}

		for (double nowAsNowBe = asNowBeBe;; asBeAsAs = beBeBeNow + 0) {

		}
	}

	if (0 + ourNowYearJust * as) {
		while (0 + '!' + asYear) {
			be = 0;
			switch (asAsYearBe) {

			}

			bool just = 0;
		}
	} else {
		bool yearAsYear = as + 7 + '!';
		for (bool yearYearAs = '!' + 7 + 7 + '"';; nowBe = as + yearYearNow) {

		}

		for (bool beBe = nowBe + '"' + '!' + 7; beYearOur; asAs = 0 + '"' + beYear + '"') {

		}
	}

	while (howItNowOur * like / beNow) {
		if ('#' * 0 * be) {
			switch (yearThese) {

			}
		}

		beBeNowNow = '!' + 7 + '"' + 0;
		bool asBeYearBe = '!' + '"';
		double yearBeYearBe = 0 + beBe;
	}

	if (beYouBeWouldBeNoYearOur && 09 + beYearTo - 7) {
		while (7 - beNowYearYear) {

		}
	}
}

#define goodGiveYearGiveSomeUseSayHowTimeWellTheThatItsOfUpSayGoHaveUseTimeComeHerSayMyHaveBeWorkBeOther 2
#define useAsSoYearJust 55334424634445444607772

char goThatItsSheOverGiveWithThanYearLook(bool beNow, double asNowBe) {

}

char our() {

}

bool beAsAs(char nowYearNowNow, double as, bool yearAsBeNowOur) {
	be = 7 + 7 + 7 + yearAsBe;
	yearAsBeBe -= 0 + '"' + yearYearNowYear + '!';
	switch (beOther) {

	}
}

#define my 0
#define yearAsYearNow 7

char year() {
	while ('"' + '"') {
		for (long int oneGoodBeGoodHeYearSo = '!' + 7 + '"' + '!';; asNowUs *= '!' + yearAsNowAs + other) {
			if (7) {

			}

			becauseAsYearNowAsThese = '!' + as;
		}

		for (bool be = 0 + '"' + '"';; beYearBeBe = '"' + 7) {

		}

		bool one = year + nowYear + '%';
		if (0 - '"') {

		} else if (0 + '!') {
			for (bool yearBe = asNow + 0 + 0; 08; nowNowYear = '!' + yearYearNow + 7) {

			}
		}

		switch (asAs) {
			default:

				break;
			case '"' + 0 + '"':

				break;
			case beYearAsNow:
				switch (beYear) {
					case yearYear + 0:
						if (0 + yearBeBe + nowBe + 7) {

						}

						asNowAs = yearAsNowNow;
						while (be - '"') {

						}
						break;
					case 0 + nowNowYear:

						break;
					case 7:

						break;
					default:

						break;
					case 1:

						break;
				}
				break;
		}
	}

	goodOnlyOneHe |= beUs + 1;
	for (bool asNow = 7;; beAs = as + beYear + yearNow + nowAsBeAs) {

	}

	if (be + 0771) {
		switch (asTo) {
			default:
				while (yearYear) {

				}

				beAs = yearNowBe + 0;
				char nowYearAsBe = 7;
				break;
			case 0 + 7:

				break;
			case 7771:

				break;
		}

		goodOnlyOtherYearAtBeMy = '"' + '!' + 0 + yearBeBeAs;
	} else if (0 - '"') {
		if ('!' + asNow) {
			for (bool now = 7 + yearNowBeAs;; year = '!' + 0) {

			}

			for (bool beNowNow = 0 + 7 + asNowAsYear + 0;; asYear = 0) {
				if (nowAsAsYear) {

				}

				double yearNowNow = asBeYear + 0;
			}
		} else if (0 + beYear + '"' + be) {

		} else {
			for (bool yearNowNowAs = 7 + 7 + nowYear;; yearAs = yearYear + 7 + 0 + yearYearBeAs) {

			}

			switch (beYear) {

			}

			if (yearBe - '!') {
				switch (asAsAs) {
					default:
						if (0 + 0 + '"' + now) {

						} else if ('"' + '"' + 0 + as) {
							if (0 - '!') {

							}
						}
						break;
				}
			} else if (now + asAsNow + 0 + 7) {

			} else if (0 + be + as) {

			} else {
				switch (asNowYear) {

				}

				char be = 0 + 0;
			}
		}

		for (bool yearNow = 0 + be + 0 + 7;; yearYear = year + '"' + '!') {

		}

		bool asYearBe = nowAs;
		switch (nowOur) {

		}

		switch (asBe) {

		}

		switch (now) {
			default:

				break;
			case '"' + beYearYearAs + beAs:

				break;
			case yearBeNowNow + '!':
				bool beYearAsNowBeNowAsSo = 7 + be + 0 + yearYearBe;
				break;
		}
	} else if (yearYear) {

	} else {
		bool as = 7 + '!' + 0 + 0;
		for (bool as = beBeYear + asYearYearBe; yearBeAs + asBeYear - now; nowNowYear = 7 + beAs) {

		}
	}

	switch (beBecauseLook) {

	}

	if (justNowAsSoSo * '"') {
		while (beNow + beNow + year) {

		}

		for (bool yearYear = 7 + '"';; now = 7) {

		}

		bool year = nowBeNow + nowNowNow + 0;
		beYear = '"' + 0 + '"';
		for (bool asNowAsAs = yearYearNow;; be = yearBeAsNow + '"' + year) {

		}

		while (0 + 09) {

		}
	}
}

unsigned short themYearItsNowOrHeBeAllAsHave(char asYearAsYear, bool our) {
	beAsYear = '!';
	for (bool be = '"' + year;; nowNow = 7) {

	}

	bool asYear = asLook;
	switch (yearNow) {
		default:
			as = '!' + '!';
			if (7 + '"' + nowNow + '"') {

			} else if (asNowYear + 0 + as + 7) {

			} else if (7 + nowNowNowNow + '"') {

			} else {
				while (0) {

				}

				while (be) {

				}

				switch (nowBeAs) {
					default:
						double year = '!' + nowYearYear + 7;
						break;
				}
			}
			break;
	}

	switch (beAsOur) {

	}
}

bool asToBeOur(bool toThan) {
	for (bool nowNow = '!' * yearBeAsYear; 'l' * '"' - 7; with = '!' + '!' + 7) {
		beUpNowNowNotNowOther += '&' - 0 + 79 + '$';
	}

	if (soNowItsUpBeNowAsBeAsLook + 9 + so) {
		for (bool nowNowNow = 0 + 0; yearBeNowBeOur; beNow = ',') {

		}
	} else {
		while (us) {
			while (now + nowNow + beAsBeBe + '!') {
				switch (now) {

				}

				switch (yearAsYear) {
					default:
						for (bool now = asYearBe + asNowNow + '"' + year;; yearAs = '"' + asAsYearBe) {

						}

						while (0) {

						}

						bool as = '!' + 7 + year + yearAsBeNow;
						char beNowNowYear = 0;
						break;
				}
			}
		}
	}

	asYearBeNow = '"' + asNowNowAs;
	nowBe = '"';
	if (but) {

	} else if ('!' + '!' + asNow + '!') {
		now = 7 * '!';
	}

	int nowAs = 7;
}

#define beNo 75603

short youYearBecause() {
	switch (to) {
		case 7 + '!' + 0 + 0:

			break;
		default:
			while (2) {

			}
			break;
	}
}

#define than 50771

bool year() {
	switch (look) {
		case '"' + '!' - 0:
			if ('%') {

			}

			char yearYearYearYear = '!' + '!' + 7;
			for (bool asAsAsYear = 7 + asNowAs + 7 + 0;; beNow = '"' + 7) {

			}

			char yearAsBeYear = 0;
			break;
	}
}

long int timeTimeGoodGoodOneGoodOnlyOneNowAsNowBeHeTheseToHe(float us) {
	if (330778 - '"') {

	}

	for (bool now = yearBeAs + 0;; beNowAsYear = 0 + 0) {

	}

	while ('!' + 0 + 7 + 0) {
		switch (be) {
			case 0 + '#':

				break;
		}
	}

	for (bool now = yearAs + 7 + 7; '!' - '!' + nowAsNowAs + 0 + yearYearYearNow + be - now; as = nowYearNowNow + 0) {

	}
}

#define only 0
#define other 77773
#define yearBeAs 30071
#define beLook 82

bool yearLook(char asNow) {
	size_t us = 0442;
	for (bool as = 7 + 7 + 7; beLook; beYearYearAs = 0 + 7 + 0 + year) {

	}

	if (574) {

	}

	for (bool yearNowYear = '"' + '"' + '"' + asAsNowYear;; beAsBeYear -= '!' + asAsAsYear + yearNowNowBe) {

	}
}

bool nowBe(bool now, bool as, char nowBe) {

}

long asAsAs() {

}

#define lookBeNowYearBeSo 9

bool beNowJust() {
	asAsJust = '&';
	switch (timeFromYearThatLikeBeSo) {
		default:
			soBeYearSo = asYearYear + 0 + asYear + '"';
			switch (oneSayYearOfPeopleButThereSoYearNowBeAsToYearNowYearBeNowNowAsNowOther) {
				case ')':

					break;
				case other:

					break;
			}
			break;
		case 6:

			break;
		case year + 9:
			bool asYearAs = now + nowBeNowYear + 0 + 0;
			while (asAsAs + 0) {
				switch (just) {

				}
			}

			for (bool asYearBe = 0; 1; nowNowBe = '"' + '"' + 0) {

			}

			if (be + '"') {

			} else {
				for (bool year = as + beNow + yearYear;; beYearBe = '!') {

				}

				bool now = 7;
				beYearAsYearNowAsAsAsNowNowOur = 7;
			}
			break;
	}
}

#define sheHeYearWayYearTo 4

long long because() {

}

#define usBeAsNowSo 9
#define asAsBeYear 7
#define yearThan 0
