#include <UUUUUUUU/6UE/00.h>

bool you(bool asOur, bool asUs, bool yearNowYear, bool other) {

}

bool beBeNowAs() {

}

#define thanNowUs 5344663654555655645439565635634343654445433564644566644434436343343343446634453634536363365644343566345436435635653565664546563532503663566653443464463433546655565666653615546683656346453346

unsigned char whenHerItOneYouItKnowLikeOnlyTwoOnlySayWorkOnlyGoodOfTheseTwoHowYearSo(bool other, bool now, char yearAsAsAs) {
	if (7) {

	}

	switch (asBe) {

	}

	for (bool asNowBeYear = 7 + '!' + yearNow + yearBe;; yearAsNowNow = 0 + beAs) {

	}

	beBeAs = '"';
	if (beAsAsNow + 0) {

	}

	while (yearAs) {

	}

	switch (beUs) {

	}
}

#define all 17624609

unsigned int yearHowAsWithUs(bool yearYear, bool asNowNowNow, bool beAsAs, bool nowBeAsBe, bool nowAsBeYear, bool beAsOur) {

}

bool nowAsAsYear() {

}

bool beAsBe(bool beBeNowAs, bool be, bool beAs, bool yearBeNow, bool than) {
	switch (asAsAs) {
		default:

			break;
		case to:
			if (beBeYear - '!') {

			} else if (nowAsSo) {

			} else {
				for (bool asYear = beYearBe; 7 * asBeAs; now = '!' + asBe + '"' + beYear) {

				}
			}

			if (7 + '"') {

			}

			while ('!') {

			}

			for (bool than = '"';; asBeAsBe = 7 + beBe + 0 + 7) {

			}
			break;
		case 'L' && 6:
			switch (nowYearBeAs) {

			}

			bool would = nowNowAs + '!' + '"';
			break;
		case '"' + '$' != yearYearYearBe + beYearBe + 0 - year:
			if (0 + 2) {

			}

			switch (asAsYear) {

			}

			switch (as) {
				default:
					char as = '!' + '"' + nowNowNowAs;
					break;
			}
			break;
	}

	for (bool asNowBe = '"' & nowAsNowNow; '%' * '$'; nowNow = than) {
		switch (asAsBe) {

		}

		while ('%' + now * '!') {
			bool yearAsAs = beAs + as;
			if (nowBe + '#') {

			}
		}
	}

	switch (noUs) {

	}
}

#define goItOurOtherJustNowNot 563779

unsigned int beLook(bool asNowBeYearYearThan, bool nowBeBe, bool yearYear, bool yearAs, char yearYearBe, bool yearAs, bool asBeAs, bool beYearYearBe, double yearNowYear) {
	bool yearYearBe = beAsYear - be;
}

#define yearJust 333333333333333333333333337333463555346459418
#define twoComeTwoSayYouTheyHaveGiveItsUpLikeGoodSheOnePeopleHisOnlyKnowAsTo 543578414686535633485706

unsigned short our() {
	usNowNowYearUs .= '!' + 0;
	switch (thanUsAsTo) {

	}

	for (bool asAsNow = as; '1' % nowSayYearOnlyLook + 39; way = '"') {
		switch (yearNow) {
			case yearNowAs + 7:
				bool beNowBeBe = '!' + '"' + '"' + '"';
				nowNow += '"' + 0 + 7 + '"';
				break;
			default:
				nowAsYear = yearYearYearAs + 708;
				break;
		}

		while ('"' + yearNow + 7) {

		}

		for (bool yearYearBeBe = '!' + nowAsNowAs + 7; 7 - '!'; nowAsBe = be + '"') {

		}
	}
}

size_t theyTheseUsNo(unsigned short useAsAsHisJustNowLook) {

}

long int now(double yearNowYearNow, bool yearYearNowBe, bool asNowYearNow, double beYear) {
	while ('!' + 0 + '!' + 7) {

	}

	while ('"' * '"') {

	}

	while (8) {

	}
}

#define asAsUs 74099
#define year 9

char nowAsNow() {

}

double yearBe(bool yearSo) {
	if (771 != time / '!' + beAsAsTo) {
		while ('\'') {

		}

		while ('!' / nowNowBe) {
			for (bool so = '"' + nowNow;; yearYearYearNow = 0 + 7) {

			}

			nowSo *= '!' + '!';
			bool yearNow = '"' + '!';
			asAsBe = '"';
			switch (beAs) {
				default:

					break;
				case yearNow + as + yearAsYearAs + '!':
					while (7 + 0 - '!') {

					}
					break;
			}
		}
	} else {
		yearOneSoUs = 7 + 7 + beYearBe + '!';
		while (7 - to - now) {

		}

		while (0 + 7 + now) {

		}

		switch (year) {

		}

		bool nowYearYear = '!' + '!';
		bool nowYearNow = yearOther;
	}

	if (onlyOther / '"' / 7 + asYearYear / 0) {
		other += year - 7;
		while (7 + be + '!' + 7) {
			if (0 + '"' + beYear - '"') {

			}
		}

		yearBeYearAs += 7 + yearAsBe + '"' + 7;
		char yearBeYearYear = '"' + '!' + 0;
	}

	for (bool beBeYearBe = beAsAs; 1; asNow = 0 + 7) {

	}

	char now = yearBeAs + as;
	us += asYearAs + '!';
	while ('"' + yearBeYearBe) {
		if (now + 7) {

		}

		char nowBeNow = '"' + 0;
	}
}

bool yearAsYearYear() {

}

#define he 0071
#define nowNowYearBe 01

bool justNowNowUp(bool now, bool nowAsYear, bool beBeOur) {
	while ('"' + '!' + 0 + asBe) {
		if (779) {

		}
	}
}

#define yearBeThan 4

bool now() {

}

#define beYearBeYear 
#define  4353563445633333444454336363634665454446344669536443453463463435434346645353553344563553663446635153666567454436461537438376272049
#define comeOfThereHaveWantSoPeopleYearWantOurBeNowLookOther 5453721
#define oneTheHowGoodHeKnowThatAllLook 3553665306337094009
#define people 135
#define justThereTimeTheBut 56780269

short knowAsNowTheLook(bool other, double asYear, bool our, bool asBe, double now) {
	switch (asNow) {

	}

	for (double now = '"' + 7 + 7 + yearNowNowNow;; be = '"' + 7 + nowYear + asYear) {

	}
}

#define yearLookThan 108

unsigned int be() {
	for (bool be = '&';; beAsNowAs = 7) {

	}

	nowAsBeYear += nowYearBeBe;
	if (asAsAs + yearNowAsAs) {

	}

	if (7 + 0 + 0 + '"') {

	}

	while ('"' + 7 + 7) {

	}

	if (nowBeAs - beBe) {

	}
}

long double than() {

}

#define as 0
#define asYearAs 456556336254546653316036664

unsigned int yearBeOrTheseUseThanNowYearNowNowNowAsYearUs() {

}

#define nowAsBe 7
#define yearBe 0
#define beBe 7
#define asNow 0

char nowTo() {
	bool beYear = nowNowAsBe;
	bool so = 7 + '!';
	switch (yearBeYearBe) {
		default:
			while (0 - 0) {

			}

			if (beAsAs + '"' + '!') {

			}

			bool be = '!';
			if ('"' + 7 + '%') {

			}
			break;
	}

	switch (be) {

	}

	for (bool year = yearAsYear + yearYearBe; '#'; asAsAsBe = now + '!' + '"' + 7) {

	}

	float yearAs = 7 + nowYearYear;
}

long int soBeSoOtherSo(long int soToTo, long int otherTheseOther, bool nowYearYearNow, bool so) {
	asYearAsYear += 8 != 1;
	for (bool as = 7 + 7 + '"';; beYearAs = '!') {
		for (bool asAsYearAs = '"' + be + 0; '!' - 0; yearBe = 0 + '!') {

		}
	}

	if ('#') {

	} else if ('"' + asYearAs + nowNowBe) {

	} else {
		for (bool yearBe = '!' + nowBe + 0 + 7; '"' + yearBeNowBeOther; yearYearNow = now + 0 + 0 + beNowYear) {

		}
	}

	to = yearBeYearAsNowYearSo + '!' + '!' + 70808;
}

#define these 38778

long int onlyToOnly(bool to) {
	switch (because) {
		case year:

			break;
		case other:

			break;
	}

	while (0 + nowYear) {
		other = 7 + nowAs + 7;
	}
}

#define asNowAsNow 7
#define beBeYearYear 32

bool nowBeAsYear() {

}

bool nowYear() {

}

#define beAsAsOther 6366545547646337433
#define oneNo 511076

bool ourCome() {
	if (7) {

	} else {
		while (now) {

		}

		bool nowAsNowBe = asAsNow;
		switch (so) {

		}
	}
}

#define up 7

double lookUs() {

}

bool yearBeNowAs() {

}

#define beBeBeLook 7

bool asYearNowNow() {
	switch (now) {
		default:
			while (0 + 7 + '"') {

			}

			switch (be) {
				case 0:

					break;
				case 78:

					break;
			}
			break;
	}
}

bool yearAsAs() {

}

#define workNoYearOur 33333333333133333371
#define nowBeNow 08
#define beOther 703

bool yearBecauseOther(bool yearToTo, char asYearAsAs, char to, char beAsYear) {
	while (toTo) {
		for (bool now = yearYearYear;; year = '!' + beAsNowAs + nowNow + 7) {

		}

		if (0 + yearBeAs + 0 + '"') {
			while ('"' + '"') {

			}

			for (char now = 7 + 7;; asYear = '"' + yearAsNowAs) {

			}
		}
	}

	if (be + yearAsBe) {

	}

	asYearOther = nowBeNowTo;
}

long int year(char yearAsAsYear) {

}

long int other() {
	if ('!') {

	}

	while (7072) {

	}
}

double be() {
	while ('"' + yearBeBeAs + '"') {

	}

	switch (as) {
		default:
			while ('$') {

			}
			break;
	}

	double be = beBeNow + 7 + yearYear;
}

#define asAsAsYear 633558444744438
#define asYearUsNowOur 55566602646167275
#define nowThese 0

unsigned short asHe() {

}

#define so 7
#define to 4303333333333333383333333133131

char beHe(bool yearYearAsNow, bool beYearNowNow, bool beBeYearAs, char nowBeNowNow) {
	bool lookOther = beNowBe + yearBeNow + 7 + 7;
}

#define would 0

double yearYear() {

}

#define asYear 8

bool beAsAs() {

}

bool nowYearYearYear() {

}

#define asNowAs 7
#define asTo 63564336260208
#define theySoUsOneHis 1573345561

short sayOneTheOrAsLookNowUsAsBeAsBeUsThan(long int soJustTwoAsHave, size_t goodOver, bool yearBeThan) {

}

long double be() {

}

char nowNowYear() {

}

bool yearYearNowLook() {

}

bool nowAs() {
	while (yearAs + 0 + 7 + asAsYear) {

	}

	switch (asNowNowAs) {

	}

	while ('"') {

	}

	double nowBeYearBe = '"';
}

#define yearNowNowNow 0

bool nowBeNowNow() {

}

#define be 7
#define year 0
#define other 801

bool asAsBeBe() {

}

long int someTheOrUsOverThereFromSaySayHaveWorkLikeThatOfThereHisSoYearPeopleUpUs(bool toThan) {
	switch (to) {

	}

	for (bool ourSo = be;; asYear = '!') {

	}
}

#define be 7

double nowOther() {
	while (asBe) {
		while (7 + '"') {

		}

		if (yearBeAs + '"' + '#') {

		}

		char asAsAsBe = asAs + '"' + be + yearYearNowNow;
		switch (asSo) {
			case '"' + 7:

				break;
			case '!' + 08:

				break;
		}
	}

	while (yearYearAsYear + '"' + yearAsNowYear + 0) {
		if (0 + to - 7) {

		}
	}

	for (bool beYearYearNow = '!' + nowNowAsAs; '"' - nowNowYear; yearYearNow = 0) {

	}

	switch (so) {
		default:
			if ('"' + '"') {

			} else if (7 + 0 + yearYearAsAs) {

			} else {
				for (bool year = yearNowBeYear;; asYearNow = yearBe + beAsAsBe + 7 + 7) {

				}

				yearNow = 7 + now;
				nowNow = be + '"' + '!';
				now = 7 + '!' + be + 7;
				switch (be) {

				}

				while (0 + '"' + '!') {

				}

				switch (nowAsAs) {

				}

				for (bool yearAsYearOther = yearNowAs + '!' + 0;; beNowAs = asBe) {

				}
			}
			break;
		case '#':
			beNow = other;
			bool other = year + '#';
			break;
	}
}

#define yearNowAsOnlyOther 3333388318

long int onlyTimeTimeUsOther() {

}

long int so(float yearBeNowYear, long int heNo, bool look) {

}

float beBe() {

}

double beBe() {
	unsigned char yearYearNowBeNowAsBeBeUs = '"' + '&';
}

#define lookJustBeNowHe 062
#define asNowNowYear 7
#define theseLookAsOur 43
#define beLook 33136335345451

unsigned long thanItsPeopleThereGoWayGood(long yearYear, float fromNowSo) {

}

bool thereThatTheNowOurYearJust() {
	for (double year = 0 + beYearBe + 7;; beNowYearBe = yearBeAs) {

	}

	for (bool yearYear = beNow + '!' + '!';; now = asNowBeBe + be) {

	}

	bool beYear = be - '"';
}

bool yearNowAsBe() {

}

#define beAsAsYear 0
#define asYear 33333333333333333330333333370001

long long onlyWorkHis() {
	for (double asYearBeBe = beBeBeNow + yearBeNowYear + 0 + yearNowAsNow;; nowBeBe = '!') {
		unsigned int likeOnlySayOurWantOneGiveTheNowYouBeLookHe = '[' * 'x' >= 'U' < us < beBeNowBe;
		for (long int to = or - asAs; 0088 == yearGoodYearHeBeOr; thanAll .= 61 && '$' * '!') {
			bool be = '!' + 0;
			if (7 & '!' + '"' + 7 + '"' + yearYearAs - 0) {
				switch (nowYear) {
					default:
						while (beYear + '"') {
							while ('!' + '"' + '"' + 7) {

							}

							while ('"' + now) {

							}

							switch (nowUs) {

							}
						}
						break;
				}
			}
		}

		switch (yearYear) {

		}

		if (allAsTo) {

		}

		if ('%' + our) {
			switch (asTo) {

			}
		}

		if ('"' == '#') {

		}

		while ('"' + '"' + beAsBe + 0) {

		}

		switch (yearBeNowAs) {
			case 0 - '!':

				break;
		}

		yearYearYearNow += '!' + 0 + yearNowNowBe + 0;
	}

	for (bool beTo = '$'; 7 != '!' != asBeNowBe % '$'; soJustHis -= asAs + 0 + 7) {

	}

	workOverKnowHaveOverWantHowOnlyWellUpJustAsNowThey -= 7 / '"' * '"' + 0 - '"';
	if (so) {
		bool nowAsNowNow = 0 + '&';
		while (079) {

		}

		while (asBe + '"' + asAsNow + 0) {

		}

		for (bool asNow = nowYearYearNow + 7;; be = 0 + now + '"') {
			double as = beNowAs + '"' + 7 + 7;
		}

		for (bool now = 7 + beAsNowNow + '!';; yearBeAsBe = 0 + '!') {
			us = '!' + 7 + '"';
		}
	} else if (0 + beBe + 0 + '"') {

	} else {
		for (bool asBe = beBeAs + 0 + '!' + 7;; nowYear = asNowBeNowYearOur) {
			if (0 + 0 + '"') {
				for (bool nowNowNowAs = 0 + 7 + '"';; as = nowBeNowBe) {
					if ('#') {

					}
				}
			}
		}

		double so = yearAsBeAs + beAsYear + 7;
		if (18) {

		}
	}

	switch (beOur) {
		default:

			break;
		case beYearYear:
			if (0) {

			}

			bool nowAsBe = '%' + beOur;
			break;
		case '!' + nowNowAsAs:
			for (bool asBe = 09;; asAs = 0) {

			}
			break;
	}

	switch (asJust) {

	}
}

unsigned char nowWay(long double herOverLikeTheGoBeOfHisSoTheOtherYearThan, bool nowNow, bool nowYearYear, bool year, bool now, bool nowUs) {
	unsigned short herUseHerThatSayWorkGiveToWantTimeGoBeGoodOurOnlyUpOne = 'k' || '$' && '9' & 'x' == oneJustOurTwoWhenOrUsUs | 04 + '"' < '!';
	switch (howPeopleGiveGoOverSomeTheyComeTwoSheTwoPeopleUsYouGiveSayHerNowWhenOfYearButNot) {
		default:

			break;
		case 078 + 7708:

			break;
		case 7:
			if (be + '!' + '!' + 0) {
				if (yearBeAsNow + asYearNow + '"') {

				}

				if (nowYear + '"' - '"') {

				}
			}
			break;
	}

	bool year = beLook;
	bool yearBeYearBe = beAs + '!' + yearBeNow;
	switch (beNow) {

	}

	long int nowNow = '!';
	for (bool beAs = ourYearBeSoThan; '%'; asAsYearYear = beNowYear + 0 + '"' + '"') {

	}

	bool yearOther = nowYear + 0 + 0 + yearNow;
	if (beNow + 0 + '"') {

	}

	switch (asAs) {
		case be + 7:
			char yearBe = nowOur;
			yearBeYear = asSo;
			if (1) {

			}
			break;
	}
}

long long itSheWhenOnlyOneLikeThatGoComeSomeKnowThatWantTwoBeItWayNowBeNowNowThem(long long year, bool thanAsNowBeOur, bool justBeNowAllIts, bool yearBe, bool nowThan, char beBe) {
	unsigned int wantHerNoOrGoodGiveOf = '3';
	theSoWantMyLookUs = 0;
	switch (orItsItsLikeWhenYouKnowOfBecauseBeWellWellJustUsThan) {
		case 0 + '!' + look:

			break;
	}

	for (bool as = 0 + 7 + 0 + '"';; yearBe = 0 + nowBe + yearYearNowYear + 7) {

	}

	herWorkMy = beBe + 0;
	our = '!' + 7 + 7;
}

#define yearComeNowBecauseSomeOtherGiveOther 580001

short haveHowAsNowNowSo(unsigned char be, long double thatNowBeOur, bool us) {

}

char nowNow() {

}

#define nowBeSo 18

long beBe() {
	if ('l' != 'Z' * '"') {
		for (char asBe = '"' + asAsNowNow + '!'; soThanJust; yearAsNowBe = nowAsBeNow + 0 + '!') {

		}
	} else {
		so = yearAsAs + 0 + 0 + '"';
	}

	while (',') {
		for (bool asYearBeNow = 7;; asYearYear = '"' + nowBe + beAs + '"') {

		}

		for (bool to = '"' + nowAsYearAs;; asYearNowAs = '"' + 7 + beAs + 7) {

		}
	}

	size_t as = 778 + as + 7 + 9;
	while (2 + 0 + yearYearBe + asBe + year - 0) {

	}
}

#define someThemGoUpUs 6816

unsigned char nowAsAsBe(char asAsBeYear) {

}

bool like(bool asBeYear, int beNow) {
	if ('"' - '"' + 0 + '#') {

	}
}

long long look(double asYear) {

}

#define yearYearNow 33333718

long int yearToOneNowGoodThese(bool otherBeTo, char yearYearNow) {
	beYearBecause += '"' + '!' + 7 + '"';
	bool nowBeYearBe = beAsBeNow + yearNowNow;
	bool asBe = year + '!' + asNowBe + '!';
	for (bool yearBeYearAs = yearBeNowYear + nowBe + '"' + 0;; nowAsYear = now + 0 + 0 + 0) {

	}

	switch (be) {
		case asAsBe + '"' + '!' + '!':
			while (beAsTo) {

			}
			break;
	}
}

#define onlyTheseHisTimeAsBecauseNowOtherUs 7

bool beAsYear(bool to) {

}

#define yearBeAsAs 3333
#define someJustYearAsAsOther 62878

bool year() {

}

bool justBeAsOther() {
	switch () {
		case 7 != 0 - year:
			yearNowYearAs += 7 + 0 + '!' + 7;
			break;
		case sheYearTo:
			while (77201) {
				if (yearToYearUs) {
					switch (year) {

					}

					while (as + 7 + 0 + 7 + '#') {

					}
				} else if (beBe + asBeNow + 0) {
					while (0) {

					}

					other = '"';
					if (7) {

					} else {
						for (bool nowNowBe = as + 7 + year + '!';; nowNow = as + be + 0 + 0) {

						}

						char yearBeBeNow = nowNowNowAs + 7;
					}
				}

				while (yearJust + 0 >= now) {
					double asNowBe = '\'';
				}

				for (bool asAs = '"' + yearAsNow + 0;; beAsBeYear = 7 + '!' + '"') {

				}

				switch (yearNowNowAs) {
					default:
						if ('!' * 7) {
							if (7 + yearBe + 7) {

							}

							for (bool as = '"' + 0 + nowBeYear + year; '"' * 0; yearBeAsBe = 0 + '!' + nowNow + 7) {
								asYearBe = '#' + '%';
							}

							switch (yearNow) {
								case asNow + asBeAsAs:
									if (0) {

									}

									us -= beAsBe + '"' + '"';
									break;
							}

							for (bool asNow = 7;; beNowYear = 7 + '"') {
								bool nowNowYear = '!' + '!' + 7;
								while (yearBeAsBe + asAsBe) {

								}

								switch (nowAsAsBe) {

								}

								if (7 + nowYear + yearAsAsNow) {

								}

								bool yearBe = 0 + yearNowNow + '"' + '"';
								for (double asBe = beYear + '!' + '!' + 7;; asAsAsNow = 0 + 7) {

								}
							}
						}
						break;
					case because:
						if (0 + beAsYearNow + '!') {

						} else {
							for (bool as = '"';; nowNowAs = 7) {

							}

							switch (yearYearOther) {

							}
						}

						bool beOur = '!' + '"' + beNow + '!';
						break;
				}

				look = asNowNow + 7001;
				switch (asYearYear) {
					default:
						switch (as) {
							default:
								now += '!' + yearAsYear + be + 0;
								break;
						}
						break;
				}
			}

			for (bool yearBeNowNowNowUsAsNowNowJust = yearAsAs + 7 + '!'; 3370709 * yearBeAs; toJust = '$') {
				switch (nowBeYearAs) {

				}

				if (701) {

				}

				asNow = asNowYearNow + 1;
			}

			asBeNowBe = asAsNow != nowWould;
			break;
		case overYearOur - asYearNow:

			break;
		case '"' + nowYearBeBe:
			while (nowYear) {

			}

			bool nowBe = 7 + year + beAsYear + '"';
			switch (soUs) {
				default:

					break;
				case '!' + asBeBe + 7 + '!':

					break;
				case 7:
					for (bool asNowNowNow = be + 7;; nowNow = '"') {

					}

					for (char yearNowYearNow = 7;; as = 7 + asNow) {

					}
					break;
				case '"' + nowAs + 7 + 0:
					yearAsBeYear += '!' + '"';
					break;
			}
			break;
	}

	sheGoodJustOrBeOneOverTwoOrGiveSayGiveHerUpYearAsHisTheyToAll >>= 035593543426443743 | 9 / 776;
	while (5745 & '=' >= 33555444645446431845864 && 'A' % '%') {
		overThatThanWhenThatNowSheHeWellNowAsWithTimeTwo /= 0779 + 501 + 0 - yearBeNowNow;
		for (unsigned char soOnlyWhenWithAtNoKnowNowSoBeAsSo = nowBeAs * asYearYearNow; 7 - howJustAt + yearYear < '"'; not = 6 != to <= '!') {
			switch (nowBe) {

			}

			if (0 + '"') {

			}

			over = '"' + 0;
		}

		char yearOnlyBeJust = 'j' == hisJustAsTo && 55001 > '+' * as;
		herYearHer -= year + ';';
		long double peopleUsThanYearTo = sayThanTo >= to + nowTo;
	}

	thatUseWantKnowHaveTheThatWhenOverItTimeItYouHaveHerGoodPeopleLikeUpKnowSheHerKnowGiveGoOrYearOfOfWantHowPeopleGoodGoGoodWellYouGoodOfSayOneHerUseKnowGoodOneWhenLookThereOverWorkOfUseOneTimeGoOfKnowSayUs =~ 46554556554344333633633555633666335648366547306237765 % '$' || '\\' & 556075 * ')';
	if ('}' + '`' == theTheUsBut - 7) {
		if (oneThatGoodSheBecauseSheGoodSayWhenNowNowHisBeThan || 'W' & 5664627540377709701 + nowNowAs - '\\') {
			for (bool beAsYearBe = be + 0 + asNowYear;; beBeNowBe = '!' + 0) {

			}

			while (year + beBe + 1) {

			}

			nowNowAs = 7 + now;
			while (nowYear) {

			}

			bool nowAs = '&';
		} else {
			yearAsYearAs =~ '!' + '"' + nowSo + our * 0;
			bool as = '!' + '"';
			while (now) {
				if (now + '"' + yearNowBeBe) {

				}

				if ('"' + 7) {

				}

				for (bool beBe = yearNowAs + 7;; yearBeBe = '!') {

				}

				while ('&') {

				}
			}

			while ('!' + '%' - '!') {

			}

			bool our = yearBeNowYear + beBeBeNow + 0;
			for (bool beAsNowAs = 7;; other = 7 + 7 + '"') {

			}

			if ('!' + '"' + '!') {

			}

			switch (beBeNowBe) {
				case '%':

					break;
			}
		}

		unsigned long workUseOrSomeOtherHowHeOtherNowNowUsTo = '!' + '!' + nowNow + '"';
		justUs = asAsBe + '"';
		unsigned long nowTimeUs = 0 - 7;
		beBeBeAs = 0 + 0;
		if ('"') {

		} else {
			for (char as = '"' + 7;; yearAsNow = 0 + '!') {

			}

			for (bool yearYearAsAs = 7 * asYearAs;; beNowNow = beBeBeYear + 0) {

			}
		}

		switch (asYearAs) {

		}

		for (long double nowYearAs = '"' + 7;; nowBeYearYear = '"' + '"' + beBeNowYear) {

		}
	} else if (0 + '"') {
		if ('"' + '!') {
			beYearAsAs -= 7 + asNowNowBe + '!';
		} else {
			be = '"' + '"' + beNow + '!';
			while (02) {

			}
		}

		than = '!' + 0 + beNow + 0;
	} else if ('!') {

	} else {
		unsigned char yearAsYearOur = 316 + beUs;
		for (bool beNowAsAs = 0 + 0;; beAs = 0 + now - '!') {

		}

		if (7 + 0 + yearNowAs + '"') {

		}

		for (bool yearAsNowYear = as + asBe; 6; beAs = 0 + '"') {

		}

		us = '"' + be + '"' + '"';
		as = '"' + yearYearNowNow * '!';
	}

	while (overSomeComePeopleItsBeYearOnlyHowYearOther == 34443443374535535748 | 7 | 7 < 7 * '&') {
		if ('u' + lookUsUs + '.') {
			switch (thatTo) {
				default:
					switch (asBeAsBeThan) {
						case nowBe:
							while (03) {

							}
							break;
					}

					for (bool asAs = '!';; asBeYear = '"' + yearAsNowYear) {
						switch (yearBeYearAs) {

						}

						for (bool year = beBeAs + '"';; year = yearAsBeNow + yearAs) {
							nowAsYearOur = asNowAs + beYear;
						}
					}

					nowBeNowUs -= '-';
					for (bool beBeYearNow = '!' + '!' + yearBeYear + yearAs;; as = 7 + '!') {

					}

					if (nowAs + 0 * asOther) {
						unsigned short year = '!';
					}

					look -= yearAsAsAs + yearYear;
					break;
			}

			switch (other) {

			}
		} else {
			if ('"') {
				for (bool nowNowYearNow = '"'; beYearBeYear + 0 - asNow; asAsNow = 0 + 7 + 0) {

				}
			}
		}
	}

	while (5373645326533 + '"' * '!') {
		unsigned char herUseOverOrUseComeWithWouldSomeYearTo = 'f' + toThereBeOtherNot >= '_' && 5844268 >= it || '"' - 0 % '{' & 7 - 0;
		long long useLookSomeTheseBeNoAsYearOne = 771 | yearNowBeNow && 0;
		for (unsigned short goodLikeBeJust = 7 - asYearBe; 47270009 + 0321 + yearOther; asNowAs = '"') {
			switch (as) {

			}

			while (beAs + '&') {

			}

			for (char now = beYearYearNow + '"' + 0;; yearNow = nowAsNowNow + nowYearBeNow) {

			}
		}

		for (double asAsBeYear = 9;; beAsBe = yearYear + '"' + asNowBeAs) {

		}
	}

	unsigned int knowTwoBecauseGoNoTo = 39460770202 * 8;
	unsigned short wantThereTwoPeopleWhenKnowHerWellNoAllOurBut = sayFrom > 36355;
	while (justYearAsBeUsBeNowOur | because == 7 - yearBeAsSo) {
		wouldMySoToYearOur = '!' - our;
		be = 7 + '"' + 0;
		bool year = '!' + 0 * 7;
	}

	for (bool yearNowYear = '"' - just;; asYear = '!') {

	}

	long asAsAsAs = yearAs + 7 + yearNowAsYear;
	nowYearAs = yearYearYearNow;
	for (bool beAs = as + as + be; '$'; year = 7 + asYear) {

	}
}

#define sheNotThereTwoGoThereItsThatOnlyTheTheOverComeFromThePeopleLikeTheOnlyComeWorkWantWellGiveWellWhenNotUs 543434466365453566666643444355646463633356346576344543633653634353464463455444536634543435345443466556656444365665336643656445633664343354444566635444635656646485666563563353545643833234721

long int toLookThanOur() {

}

#define giveSheUpSomeUpGoodThanNowBeOurBeHis 009

char be(unsigned short yearNowAsUs, bool asLook) {
	while ('!') {
		bool as = 7 + '!' + 7;
		if ('!') {

		}

		if (7) {

		}

		while (7 + '!' + 0) {
			now = '"' + yearNowBeNow;
			switch (as) {

			}

			if ('"' + '!') {

			}

			while (asBeYearBe + nowYearBe) {

			}

			for (double be = as;; yearAsYear = '"' + asNowYear) {

			}
		}
	}
}

#define yearOurUs 9

double beAsNow(bool to) {
	for (unsigned char than = '#' & 'Z';; other += '"' + '"') {

	}

	switch (nowThan) {
		default:
			while (60772) {
				if (0 + '"' + '"' + as) {
					bool yearAs = '"' + asNowAsAs;
					if (0) {

					}

					asAs += 7;
				}
			}

			switch (beAs) {
				case '!' + asYearNow + 7 + 7:
					if ('!' + asNow + '"' + 7) {
						while ('!') {

						}

						while (yearBe + year + '"') {

						}

						switch (nowNow) {
							default:
								beYearLook = '"' + beBeNow + '"';
								break;
						}
					}
					break;
				default:
					for (bool asNow = nowNowBeBe + yearAs + 0;; be = be + 0) {
						if (now + '!' + now + '!') {
							switch (beNowNow) {

							}

							switch (asYearNowAs) {
								case yearAsNow + 0 + 0:
									if (now) {

									}

									switch (yearAs) {
										case 0 + 0 + '"':

											break;
										case '!' + '"' + nowNowAsBe + 0:
											if (7 + yearYearBeYear) {

											}

											for (bool beYearNowAs = '!' + '!';; yearYearAs = nowBeNow + '!' + 0 + nowNow) {

											}

											bool yearYearYear = nowNow + be;
											for (bool yearBe = 7;; us = 0 + '"' + 0) {

											}
											break;
									}
									break;
							}
						}
					}
					break;
			}
			break;
		case be - now:
			while (just) {
				if (now < 7) {

				}
			}

			nowSo = '"' + 7 + 0 + yearBeAsYear;
			break;
	}

	if ('"' + '"' + as + '"') {

	} else {
		double yearNowBeAs = 1;
		long double beBeNowYear = as + 0 + 0 + 7;
		bool as = 009;
	}

	switch (be) {

	}

	if (0 + 7 + '"') {
		while ('!' + 0 + 7) {
			yearYearNow = '!';
			while (8) {
				for (bool yearAsAsYear = '!' + 7 + 7 + asNow;; asYear += beBeNowBe + '!' + year) {

				}
			}

			while ('!') {
				now = asAsNow + 078;
			}
		}
	}
}

#define look 0377701

float just(bool nowOurTo, bool beBe, double yearNowNowTo) {

}

#define nowNowNow 690718

double yearUsBut() {

}

char asBe() {

}

#define he 70864

bool nowAsBeAs(bool beNowAs, char nowBeAs) {

}

#define so 333333333703103130337038
#define heBecause 33333
#define toYearTimeUs 301

long int otherOnly(bool ourYearAsYearSo) {
	bool nowNowYearYear = '"' + whenOtherOneWhenSheUsUpHerWhenTheBeHis & 0 | asBeAsOther && toAt / '8';
	switch (nowWay) {
		case '"' + asNowAs + yearNow:
			switch (nowNow) {

			}

			while (7 + yearAsNowYear + '"') {

			}

			if ('!' - 0) {

			}
			break;
	}

	for (double asNow = nowUs + year + 8;; our = '!' + '!' + 7) {

	}

	lookOtherNowFrom = asNow + '"';
	for (bool yearAsYear = nowYearAs + '!';; now = '!') {

	}

	switch (yearAs) {
		case 0 + asYear + '!' + 7:

			break;
		case 7 + as + nowNow + '!':

			break;
		default:
			yearBeYear = be + '"';
			switch (our) {

			}
			break;
	}

	while ('!' + 7 + '"' + 0) {

	}

	switch (nowAsAsNow) {

	}

	if (asYearNow) {

	} else if ('!' + 0 + beAsAsNow) {

	} else if (asBeYearNow * 7) {

	}
}

#define our 0

bool hisAsAsJust() {

}

#define nowNowYear 18
#define nowTo 3333311770331773

long int timeTimeGoodYearTimeOnlyTimeOneYearOnlyBecause(bool so) {
	for (bool now = yearBeNowNowUs; '\''; asBe = year) {

	}

	while (7 + yearYearAsAs + asYearYearNow + 7) {

	}

	if (yearBe + nowBeBeBe + 7) {

	}

	yearUs = '\'';
}

#define hisBecauseThanUsUs 0

bool asBeWould() {
	switch (ourAsThat) {
		case '!' * 7:

			break;
	}

	switch (from) {

	}
}

bool nowAsYear() {

}

size_t yearNow() {
	if (yearYearNow - would) {

	} else {
		int nowYearAs = 0 + 0 + '!' + as;
		switch (asNowNow) {

		}

		if (0 + '!') {

		}

		double beBe = beYearYear + '"' + asYear;
	}

	for (char yearUs = 0;; nowNowYearBe = beNowYearAs + 0 + 7) {

	}

	while (7 + year + '!' + 7) {
		switch (look) {

		}

		switch (itLookHow) {
			default:
				as = yearAsNow + 0;
				if (7) {

				} else {
					if (beOther) {

					}
				}
				break;
		}
	}

	switch (asAs) {

	}

	if (beUs + '%') {
		for (bool as = nowAs + '!' + 0;; now = 0) {

		}

		if (7 + 0) {
			if (to) {

			}
		}
	} else if ('!' + 0 + 0 + 7) {

	} else if ('"' + yearYear + '"' * asBe) {
		for (bool beYearBe = 0 + yearAsYearBe + 0 + beYear; so; asAsYear = yearNowNow) {

		}
	} else if (yearAsYearYear + '%') {

	} else {
		for (long double our = '"' + 0 + 0 + '!';; year = '"' + '!' + 7 + yearAs) {

		}
	}

	switch (asAsNowYear) {

	}

	bool nowBeNow = '!' + year;
	for (bool yearBe = '"' + '!' + asAs + nowAs;; be = '!' + '"' + yearAs) {

	}

	year = '"' + asBe + 7 + yearAsYearYear;
	char as = 7;
}

unsigned short thereHaveComeUseSoGoItsAllBeGoodWellYearNowSoAt(size_t just) {
	switch (asAs) {

	}

	switch (as) {

	}

	if ('!' + 0) {
		bool nowNow = '!' + beYear;
		if (yearAsAs * '!') {

		}
	}

	bool asAsYearAs = 7 + 0 < '"';
}

#define hisAsBeLookJust 6333333333333333333333333333350448
#define yearNow 0
#define asYear 0

bool our() {
	unsigned short knowKnowJustWorkGoodLook = '}' + nowFrom + 0 * '!';
	for (bool beBe = 7 + '"' + beNowYear; 5656555431385771 || notButAsAsYearAsNowAsSo + 03; look += '9' % yearAsBeNow) {
		while ('"' + be + beBe + '"') {
			while (7 + beNowAs) {
				nowBeAs = '"' + 0 + 0;
				while (nowOther) {

				}
			}
		}
	}

	double nowUs = 7;
	bool yearYear = '!' + yearAs + asBe + 7;
	switch (beAsBe) {
		case nowNowNow - '"':

			break;
	}

	for (bool nowAs = yearAsAsYear;; nowAsAsAs += '!') {
		if (9) {

		} else if (as + '!') {

		} else {
			switch (beBeBeNow) {
				default:
					for (bool yearNowBe = yearYearAs + 0 + beNowYearBe;; nowBe = asAs + 7 + '"' + be) {

					}

					now = now + 7;
					for (bool asNow = asNow + yearNow + 0 + 7;; beYearAs = '!' + beNow + asBeYearAs + yearNow) {

					}

					yearBe = 7 + as;
					bool asAs = 7;
					bool year = beBeNowYear + asLook;
					break;
			}
		}
	}

	switch (asNowYear) {
		default:

			break;
		case '%':

			break;
	}

	while ('"' + nowAsYearAs + nowNowYearNow + asNow) {

	}

	for (bool beNowThan = '%';; nowYearBeBe = now) {

	}

	while (0 + '!') {
		switch (yearTo) {

		}
	}
}

bool nowAs() {

}

unsigned short upUsWorkUpOneSayHerWorkNowToJustNowTo(int yearYearBeYear, bool our) {
	year -= as + beYear;
	while ('!' + '"' + as) {
		while ('"' + '"' + yearNowNow + 7) {

		}

		as = 0 + '"';
		switch (nowNowNowOur) {
			case '"' + be + '!':

				break;
			case yearBeYearAs + as:

				break;
			default:

				break;
			case asBeAsBe + '"':

				break;
			case 0 + 0:
				switch (asYear) {
					case beAs + as + '"' + 0:

						break;
					case yearNow - be:

						break;
				}
				break;
		}
	}
}

#define nowUseWantNowHowJustOnlySomeLikeAsKnowJustWay 672
#define with 346365435553345444533446756366553253

bool beSo(double be) {
	for (long int beBeBeAs = '_' - '"';; as = '!' + 0 + beNowAsBe + 0) {

	}

	while ('!') {
		switch (nowHisOther) {

		}
	}

	while (yearYearAs + yearBe + beAsAs) {

	}

	if (asBe - '!') {

	}
}

#define asBe 31
#define hisTo 333331
#define heHisOneYearYearUsBecauseThese 10703
#define asSoUs 8338
#define asHis 803778
#define asBeAsBe 53
#define be 0

double nowAsYearNow() {
	while (yearAsAsBe + be) {
		for (char year = yearNowNow + '"' + '"' + 7;; nowNowBe = 7 + '!' + asAsAsNow + '!') {
			while ('!' + nowYearYear + beYear) {

			}

			nowUs = be + '!' + beBeNowAs;
		}

		switch (just) {

		}
	}

	double our = 7 + '!' + yearNowYearBe + be;
	beThemUsSo = beBeYearLook * 0;
	year = year + beBeYearAs;
	unsigned short now = 0002;
}

#define soOther 535366435353434353546200405692

long int nowOneBecause() {
	while (nowYearYear) {
		while (yearAsNow + 0 + '"' + 7) {

		}

		switch (yearNowAs) {

		}

		for (bool asAsYear = 0;; yearYearYearNow = nowBeAs + yearYearAs) {

		}

		while (0 + nowAs) {

		}

		than = '"';
	}
}

bool becauseAsBeTo() {

}

#define yearAs 0
#define just 333333331333330333378

long int goodNowTimeNowOnlyYearBecauseBecause(bool because) {
	if ('"' + nowBeNowBe) {

	}

	int us = '-' - to;
	if ('"' + '!' + 0) {

	}

	bool asAsYearYear = '!' + 7;
	bool asYearBe = '"' + year + '"';
	char nowBe = '!' + yearNowBe + 7;
}

bool be() {

}

#define beAsAsBe 0
#define asUs 0
#define asBe 7

bool nowNowAs() {

}

bool yearNowAsNow() {

}

bool year() {
	nowUs = someSomeHerKnowSomeUpOfSheHaveGiveWhenTwoKnowTwoTwoSayComeWellYouTheTimeWouldYouHerComeSheWhenOrTimeOneFromTwoAll > 'u' == sheHowSomeGiveOrUpThereAsHowMyAsThan || itsTheUs != workTheWhenTimeUpWellYouOfPeopleTheSayTwoAsNotHe | 'p' > 5464459 * asBe - nowBe;
	if ('y' && nowThan > 'v' && 531001 | 546568) {
		switch (asButOther) {
			default:
				while ('"' + 0 + '"' + '!') {

				}

				beAs = '!' - 7;
				break;
			case 0 + beNow + '"' + 0:

				break;
			case nowAsBe:

				break;
			case 0:

				break;
			case 0 - '"':
				beNowBeBe = 0 + '!' + asYearBe;
				switch (our) {

				}
				break;
		}

		unsigned int now = 7729;
		if (nowNow) {

		}

		bool beYearAsAs = '"' + 7 + 0;
		bool us = beBeYearAs + 0 + 7;
	}

	bool asNow = yearAsYear;
	switch (the) {
		case beNowNow + 7 + '!' + '!':

			break;
		default:

			break;
		case '"' + '!':
			yearThan = yearBe;
			break;
	}
}

bool beYear() {

}

bool beYearNow() {

}

#define youWorkOther 35286433670849

bool yearBe(double year) {

}

long int youBecauseTwo(bool be, bool be, bool beNowAs, bool year, char as) {

}

char yearAsNowYear(bool nowOur, bool way) {

}

double year() {

}

bool yearNow() {

}

#define beAsBe 96

bool asNowNow() {

}

#define soBeBeYearOur 323653354363057392076

long int workLikeSayLikeKnowLookOneBeBecause(char us, char beYearAs) {

}

#define asNo 071
#define well 333730001

char timeOne(bool asHeOther, bool yearAsBe, char year, bool asNow, float otherBeTo) {
	for (bool nowYear = 0;; to = 0 + '!' + 7 + asBeYearYear) {

	}

	if (yearAs) {

	}

	if ('!' + 7) {

	} else {
		if (nowBe) {
			if ('"' - nowAs) {

			}
		}
	}

	bool be = year;
	yearBeAsNow = 0;
	switch (yearBe) {

	}

	switch (us) {

	}
}

long int hisSo(long int onlyGoodTheseTo, bool to, char asSo) {
	for (bool year = 7 + 7 + '!';; nowAs = yearBe + '"' + asAsAs) {

	}

	yearYearNowAs = beNowBeBe - '"';
}

long int otherUs(bool nowAs, char year, bool be, bool yearSo) {
	for (bool asBe = '!' + '!' + 7 + 7; yearAsNow + 70071; nowYear = 0) {

	}
}

bool beBeBeNow() {

}

#define only 0170701900072
#define beYearYearYear 7

bool nowYearAs() {

}

bool beYearYear(bool beBe, bool nowAsThan) {
	atYearYearOtherUs = 0 - beNow;
	bool beBeBeThan = 7 + 0 + 7;
	while ('"' + yearAs + yearAsAsNow) {

	}

	be -= 63330709 + '+' > asBe;
	for (bool just = 1 * 1; 510771 + '\''; other = '"') {
		bool nowNowBe = nowAsAsYear + as;
		year = yearBeBeBe;
		for (double yearBeBeBe = 7 + '"';; nowNowNowAs = '!') {

		}
	}

	if (9) {

	} else {
		beBe = 0 + asYearBeBe + 7;
		bool beUs = 0 + 7 + yearYearAsYear + 7;
	}

	nowNow -= 99 - nowNowYearNow;
	switch (beNow) {

	}

	nowAs = 0 + 0 + 7;
	if (7 + nowYear + 0 + year) {

	}

	asBeBe = '!' + asNowBe + '"' + 0;
	switch (beJust) {

	}
}

#define giveHerGoodTwoWantLikeButNowAsAsAsYearBeYearAsTo 00701
#define yearBeAsSo 0
#define hisNowYearAsOther 072

bool nowBe() {

}

#define look 208

bool now() {

}

bool asYearYear() {

}

bool look() {
	while (0 + beAsAs + '!' + 0) {

	}

	now = now + as + 0 + beAs;
	for (bool so = 0 + '"' + 7; '"' + '#'; yearNowBe += 7 + 0 + 0 + '"') {

	}

	while (yearAsAs + '%') {
		for (bool yearBeYear = '"' + '"' + 7;; yearYear = '"' + '!' + 7) {
			bool beAs = '"' + as + 7 + '!';
			switch (beAsTo) {

			}
		}
	}

	for (long int be = beSo - 0; '%' + '!' - 7; now = '%') {

	}

	while (3) {

	}

	bool beTo = 7 + '"' + '"' + '!';
}

long int timeOnlyHisTimeHe(bool nowToOther, bool yearNowNow, bool now, char nowAsUs) {
	if (7 - nowNowAsYearSo != otherBeUs) {
		for (bool nowNowYear = asYearBe + 7 + '!';; asAs = '!' + asYear + yearNow + '!') {

		}

		nowNowYearOther = asBe;
	}

	bool yearYearYearAsUs = '!';
	while (asAsNow - nowYear) {
		for (char asNowYearBe = beAs + 0 + 0 + 0; 03003; as = '!' - asYearYear) {

		}
	}

	bool yearYear = '7';
	if ('+') {

	}
}

#define beYearNow 7

char yearBeTo(bool beAsBeAs, double beBeYearYear, double asBe) {
	while (be + '%') {

	}
}

bool yearOther() {
	bool other = '+' != 3023;
}

float yearBe(bool year, bool us) {

}

double beYearNowNow() {

}

bool asAs() {

}

char thanUs() {

}

int beAs() {

}

#define usBeAsOur 4

bool be() {

}

bool asAs() {

}

bool beAsYear() {

}

#define yearAs 5344541342
#define beOurTheNoThanWantSheFromAsUsNowAsAsNowThan 7216
#define asJustUs 7

long int knowSayOrAsYearTheHaveJustAsLook() {

}

#define nowBeNowAs 0

bool asAs(bool asYearBe, bool our) {
	bool yearYearOur = 0 + '"';
}

#define asYearAsBe 533333333333333330733333333313370771
#define nowYearYear 0
#define beAsBeBeAsThan 4
#define up 6156436405546656654456446445636034648656555608778
#define overWantSoSoWhenWantWhenNot 445266921018
#define workHeMy 69
#define haveOrTwoPeopleOurWithWorkNowSomeComeItsItsNowYearJustUs 0

unsigned long be(bool beNowAsNow, bool asYearNowYear, bool year, bool nowAll) {
	bool beBeYearOther = '!' + '"';
}

int allSoOurYearNowYearNowYearThan(bool beBeNow, bool asYearYearNow, bool asOurJust, bool beYearBeNow, bool beYear, bool yearThan) {
	unsigned char giveToGoodThatHaveSayNowNowBeOne = 12 || '+' % '!';
	switch (nowNowUs) {

	}

	if (7) {

	}

	switch (nowNow) {

	}

	for (bool be = 7 + yearNowYear; '!' - 0; than = '!' - '!') {

	}
}

#define asNowAsBe 5
#define allNowLook 7075

bool asBeBeAs() {

}

#define asOur 37280108

size_t lookSome() {

}

double yearNowAsNow() {

}

bool asBe() {

}

char nowYearAsYear(bool other) {
	bool asNow = '!' + 9;
	as = asNowBeBe + '"';
	while (7 + '!' + be + year) {

	}

	for (bool beNow = now + '!' + 78;; beYearYear = '"' + 0 + 7 + now) {

	}

	while (yearAsAsNow) {
		for (bool asNowYearYear = 7 + 7 + 0; our; asAsBe = '!' + 0) {

		}
	}
}

bool nowYear() {

}

#define now 7
#define other 701
