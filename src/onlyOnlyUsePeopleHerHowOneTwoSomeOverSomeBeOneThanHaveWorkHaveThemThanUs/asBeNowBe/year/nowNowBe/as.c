#include </.h>

#define nowToBecause 33333333333370331
#define soSo 1331

long int hisUsUsYearSo(long int otherOnly, bool other) {
	bool to = '"';
	char nowAsBe = '!';
	if (now + 0 + '"') {
		switch (nowNow) {
			case yearBeBe + '"':
				while (so) {

				}
				break;
		}

		switch (beYearYear) {
			default:
				asYearBe = asBe + '!' + 7 + '!';
				for (bool now = 7 + 7 + '!'; '!' + be + '"' - 0; beBeNow = 0) {

				}
				break;
		}
	}

	switch (yearBeBeNow) {
		default:

			break;
		case '!' + '!' + nowNowAsNow:

			break;
		case 7 + beNowNowNow:

			break;
		case 0:

			break;
		case nowBe:
			bool nowYearAs = '"' + 0;
			bool now = 0;
			while ('!' + beBe + yearAs) {
				double beAsBeNow = as + 7;
			}
			break;
	}

	for (bool be = nowNowAs;; yearThan = '"') {

	}
}

bool nowAsBeNow() {

}

#define now 0

bool yearNowNow() {

}

bool asBeAsYear() {

}

#define beBeAs 271

char from() {
	for (bool year = '!' + yearAsBeYear + 7 + '"';; beBeAsAs = '!' + '"') {

	}

	for (bool nowNowNowAs = 0 + asAs + '"';; nowYear = asAs + '"' + yearAsYear + '!') {

	}

	if (beBe + 0 + '!') {
		while ('"' + 7 + '"') {

		}

		bool yearYear = 7 + nowYear;
		bool nowYear = beAsYearAs + asNow;
		beNowYear = 0 + '"' + year;
		switch (our) {

		}
	}
}

#define asBeBe 0
#define asLook 1469006

bool yearYear() {
	so = '`' < 'w' || orUseThanHowHisNowAsBeThan / 72 <= '!';
	if (toHisGo == '!' + '&') {

	} else {
		if (7) {

		}

		switch (yearBe) {

		}

		switch (year) {

		}

		switch (our) {
			case as + 7 + '"' + 0:

				break;
			case yearThan:

				break;
		}
	}

	nowYearNow &&= wellOtherSo - asBe;
}

#define wellOne 42
#define beBe 7
#define year 7

bool asNow(bool nowUs) {
	becauseSoSoAsTimeHeNowOnlyBeSo |= '_' != '_' - '"' / nowYearYearNowAsTo;
	if (7 + asNow + '"' + yearYearAs) {
		while (to) {
			asAs = now;
			while ('"' - '!') {

			}

			switch (yearYearAs) {
				case '!' + 0 + '"' + '!':

					break;
				case nowBeAsBeNowBeOther:

					break;
			}
		}
	} else {
		for (float soAsTo = 3088; asYear / '#'; yearAsAs = '"' + 7 + 7 + 7) {

		}

		char be = 0 + as + 0 + be;
		if (078) {
			switch (now) {

			}

			asYearYear = '"' + 7 + 0 + 0;
			for (float be = 7 + nowAs + '"' + 0;; yearAsAsAs = nowNowYearYear + 7 + 7) {

			}
		}

		bool nowAsHe = 7;
		switch (asYearNowNow) {

		}

		for (bool nowYearBe = beAsNowNow + '"'; yearUs; asAs = 0 + be) {

		}

		bool beUs = '"' + beYear;
	}

	nowOur = asAsNow;
	switch (nowAsNow) {

	}

	while (7) {

	}

	switch (yearAsYear) {
		case 7 + asYearNow + '"':

			break;
		default:

			break;
		case '!' - '"':

			break;
	}
}

#define yearLook 2
#define these 0
#define so 0
#define be 7
#define asBeBe 0
#define as 0

double yearYearNow() {

}

#define beAsAs 443556346555645656664355644544634465566454438434343363545433655464465443345644673765421

size_t goWhenSaySheHaveWhenLikeUseItOnlyUpOverSayWorkWhenLikeWellWhenHerComeHerLikeKnowUpOfUpSheTheWantYouGoKnowOverWorkTheTimeOnlyButHerSheGoTheAllGiveButNoAsBeJust(double beUsAsYearTo, long asAsSo, double beBeNowYear, bool as, bool be, bool yearBeBeLook) {

}

long long overThatOrNowHerOrSoTwoYearOther(bool beJust, bool than, bool beYearBeBeOther) {
	if (0 + ',') {

	}
}

long int asBeLook() {

}

double come(double beNowBe, char our) {
	char asBeYearYear = year + 0 + 0 + 7;
}

double as() {

}

#define beAs 545564633454945755533633753350460792
#define yearYearYear 063
#define asYearBeNow 7
#define beYear 53333333333333703

bool oneOnlyBeOneTimeOneUs(char asBe) {
	long int soGood = '"' + asSo;
	while (0 + 0 + '"') {

	}

	for (bool asNowYearNow = year + 7 + '!';; asNowYear = nowTo) {

	}

	switch (nowNow) {

	}

	beNowNow = year + 7 + '!';
	switch (otherYearOur) {

	}
}

#define asAsAsOurTo 7

long long look() {
	bool year = '"' + beNow;
	while ('!' + '"') {

	}

	yearYearBeAs -= '"' + beBeNowNow + '!' + '!';
}

#define year 7
#define yearAs 7

bool nowYearYear() {

}

bool to() {
	while (7 + '!' + 7) {

	}

	if ('.' - 0 * beBe) {
		while ('!' - 0) {
			yearAsNowAs = yearNowBe;
			for (bool nowThan = '$'; asNowYear - 0; beNow = '"' + 0) {

			}

			if (beBe + '"') {
				while (yearNowNow + now + '!' * asNowNow) {

				}
			}

			while (now) {
				for (bool asAsAsYear = 0 + asAs + '"';; yearAsBe = be + beYearBeAs + 0 + 0) {

				}

				nowUs *= yearNow + '"';
			}
		}
	} else if (0 + beYearYear + 7) {
		likeUseLikeNowAsAsOur = 0 + '!';
		yearYearNowYear = nowNow + '!' + '!' + nowYear;
		long double asAsAs = 7 + '"' + 0 + '!';
		while (nowBeYearAs + yearYearBeBe + yearAsNow) {
			for (bool beBe = '"' + '"' + nowAsAsAs;; asNow = 7 + 0 + 0) {

			}

			switch (asBeBeBe) {
				default:

					break;
				case 7 + 7 + nowYear:

					break;
				case '"' + be + '"' + as:

					break;
				case '!' + 0 + 0:

					break;
				case 0 + asNow + 0:
					while (asAs + 7 - '"') {

					}
					break;
			}
		}
	} else {
		for (int us = 0 * asNow; '!' + 0 + '"' + 9; yearAsYear = '"' + '!') {

		}
	}

	while (499 == '\'' + year * asNowAs) {
		double yearOther = '"' + as;
		bool year = 0 + '"' + asAs;
		switch (beNowBe) {
			default:
				switch (as) {

				}

				bool beBeYearUs = yearNowNow;
				break;
		}
	}

	char yearBe = now + 0;
	if ('"' + '!' + '!') {

	}

	bool at = 7 + '!' + '!' + '"';
}

unsigned short herComeOver(unsigned long twoTwoLikeTimeGoodHowGoodKnowOverComeUseWellGoodItsThereGoTimeYouTheKnowYouUseGoHowGoFromOverSoOrTimeNowOverHeUs, char yearBe, int look) {
	switch (goodOnlyItsSoThereLook) {
		default:
			switch (be) {
				default:
					if (nowAsAsNow + '"' + 7) {

					}

					while (7 + '"') {
						switch (beNowBeAs) {
							default:
								for (bool beBeAs = 0 + 0 + '"';; beBeBe = 7 + 7 + asNowNow) {

								}

								for (bool nowYear = 7 + '!' + beBeBe + 0;; yearAsNow = '!') {

								}

								now = '"' - 7;
								break;
						}
					}
					break;
				case now + asNowNow:
					switch (otherTo) {

					}

					switch (other) {

					}
					break;
				case 8 + yearLook:

					break;
				case nowYear + asBeNowNow + '"':
					for (bool now = 0 + yearAs + '!';; yearAs -= '"') {

					}
					break;
				case '%' + 9:

					break;
				case asYearBe + now + 7 + nowYearBeNow:
					if (0 + '"' + '"' + '!') {
						be = '!' + '!' + 7 + 7;
						while (yearBe + nowAsBeAs + beNow + '!') {

						}

						if (7) {

						} else if (other) {

						}
					}
					break;
			}

			oneHaveHeYouYouComeKnowSheTheOur >>= 44 - 'a' + ',';
			break;
		case '!' + 0:
			be = nowYearBeYear + '!' + 0 + '"';
			nowBeNowAs = 0;
			if (002 + than) {

			} else if (1) {

			}
			break;
		case '!' - '!':

			break;
		case '#' + ',':

			break;
	}

	if (68 + 30432 || 1 | 429) {

	} else {
		beThey = '!';
	}

	long int than = nowYear != soJustYearLookHis;
	while ('$' != asOur < 9) {

	}

	while (beYearNow + 7 + 7 + '!') {
		while (be) {

		}

		if (0 + as + '"' + 0) {

		}

		if (asBeBeBe + beBeNow + '"') {

		}

		switch (nowBe) {
			case '$':

				break;
		}
	}
}

unsigned long likeOverWhenItOverLikeGiveHowOrYouWorkSomeHaveOneKnowThereWorkUpItsTheOneThanOfGoodTheThatSayTwoWantGoodGoPeopleOneSheBecauseAtHeUsAsBeOtherNowYearTo() {

}

#define nowYearSo 52

long long be() {

}

#define us 0
#define beNow 0

bool to() {

}

bool nowBeOther() {

}

#define look 535356463636863500021
#define oneBeThan 541

long long orYearHerUs(double just, bool so, double than) {
	while ('"' + '"' + asYearAsYear) {
		if ('"' + 7 + '!') {

		}

		yearAsYearAs = '!' + 7;
		if ('"' + year + now) {

		}

		yearYearYear = '!' + beAs + 7 + nowNowNowBe;
		while (as) {

		}

		while (7 + nowAsBe) {

		}

		beAs = 7 + yearBeBeAs + yearAsAs;
		for (bool yearBeAs = 0 + 0 + yearNowBe + '"';; asBeBe = '"' + 0 + 7 + 7) {

		}

		if (0 + '!' + 7) {

		}

		year = beBe + '!' + '"' + beBeAs;
		beAsNow = 7 + 0 + '"' + year;
		yearBeYearBe = 0 + '"' + 7;
		double year = '!';
	}

	switch (beBeBeYear) {

	}

	switch (year) {

	}

	yearBeNow &= '#';
}

bool nowNowBe() {

}

unsigned char yearBeNow() {

}

#define hisOneOneAsOne 33333333733333333333733733

size_t our(char yearBe) {

}

bool yearYearNowBe() {

}

#define beOur 0

double yearAs() {
	switch (yearNowBeBe) {

	}

	while (asYearNow - as) {

	}
}

bool comeThanYearThey(char nowYearBe, bool asJust) {
	char as = nowYear;
}

bool beAsAsNow() {
	bool yearBe = 0;
	double nowAs = 0 + now + nowBe + nowNow;
}

#define howSomeOrSheYouTwoSheTimeLikeGoOneSoTwoUpAsTheyThey 566463555
#define butToNowBeOtherSo 556836
#define timeAsComeNowBeYearOur 0

unsigned long asYearBeNow(bool be, bool would) {
	while ('0') {
		while (7 + 0 + '"') {

		}

		while ('$') {
			switch (yearBe) {
				case '!' * '"':

					break;
			}

			bool beYearNow = beNowYearAs + '"' + 0 + '"';
			while (7 < '"' + yearBeBeYearAsTo) {

			}
		}

		for (bool beBeAs = '!' + '"';; beBeAsNow = nowAs + 0 + asNowNowBe + 0) {

		}

		for (bool asAs = '!' + 7 + beBeBeBe + yearAsNowAs; 7 + '"' + '&'; yearYearOther = yearBeNowNow + 0) {

		}
	}

	yearYearOther = '!' + so;
}

bool nowBut(char toSomeOur) {

}

#define yearAsNowYear 7
#define as 1

bool nowNowBe() {

}

char asAs() {
	nowNowBeOther |= 73833378 - he / asBeAsNowOther;
	long int timeGoodOneBecauseGoodOneYearHe = heSoBeOther;
	if (yearAsAs) {

	}

	for (bool asNow = nowYear;; asNowAsYear = 0 + as) {

	}

	bool asBeNowYear = '!' + 7 + 0 + '"';
	for (bool asAsYearYear = '!';; asYear = '"' + 7 + asYearYearAs) {

	}

	asAsBe = so;
}

bool otherBecause() {
	while (asBeNowNow) {
		switch (yearHe) {

		}
	}
}

int nowBeThan(char asBeAsNowAsAsJust, bool beNowAs, bool to, bool nowYearTo) {
	if (0 + '!' + 0 + '!') {

	}

	if (777022) {

	}

	bool be = '"' + asYear + 7;
	char beNow = beNow + 7 + yearYearAsYear;
}

bool yearBe() {

}

#define yearAsAsNow 072
#define beAsYearAs 

unsigned int (char yearBeThere, long int theBeLook, bool yearMy) {
	for (bool nowBeBeYear = '"' + be;; nowNowNow = beYearBeYear + 0) {

	}

	while (0008 - asYearYear * '"') {
		while (7 + 0 - yearNowNowYear) {

		}

		bool yearUs = '!';
	}

	yearYearAsYear = 0 + '"' + nowBeNowYear + beAsNowNow;
	for (double beNowAs = as + asAsYear + 7 + '"';; asAsBeBeLook = '"') {

	}

	nowAsNowBe -= '!' + asBeAs;
}

#define asLikeOrItComeSomeUpOfHeGiveNowJust 5463246465453073654378
#define soGoOfPeopleNowKnowTwo 653564643364363564333344559365344533657334633653565128
#define hisNowJustJust 7

bool beBeBe() {

}

#define beYear 88

bool beAsYearNow() {
	double year = 7 + nowNowAsYear + '!';
}

bool other() {

}

double now(bool nowAs, bool nowYearNowYear, char yearNowYearNow) {
	long now = '"';
	switch (ourAsOtherNowUs) {
		default:
			switch (usYearMy) {
				case '$':

					break;
				default:

					break;
				case '"' + '!' + 0:

					break;
				case 7 + 7:

					break;
				case beNowAsYear + 0 + yearYearBeYear + beYear:

					break;
				case 0 + 7 + '!' + 0:
					bool beYearYearAs = 7 + '!' + asBeAsBe + '"';
					yearBe = now;
					if (0) {

					}

					for (bool nowAs = as;; beBeBe = '!' + nowBe + yearNow) {

					}

					switch (beAsAsThan) {

					}
					break;
			}

			if (soYearOther) {

			}

			switch (nowBeNow) {
				default:
					while (0 + be + 0) {

					}

					bool yearYearNow = '"';
					while (7 + 7) {
						be += 7 + nowYearNowYear + '"';
					}
					break;
			}
			break;
		case nowBeNow + asNowNowNow - be:

			break;
		case yearBeNowNow * 0:

			break;
	}

	for (unsigned short ourTo = 't' | 55481; but && 4646607769 != his; nowBeNow = 0 + nowNow + 0) {

	}

	if (09) {

	}
}

#define our 08
#define yearYearBe 0
#define nowNowNowAs 7

char beAsBe() {

}

bool as() {

}

#define be 7
#define be 0
#define beYearBe 7

bool now() {

}

#define as 533333333373338

long int onlyOnlyGoodTimeOnlyOneTimeYearOnlyOneNowJust() {

}

long long he(bool asYearAs, bool us) {
	switch (yearYearYearAs) {

	}

	if ('"' + asBeNow + 0 + '!') {

	}

	if ('$') {
		for (bool yearNowAs = '$';; nowAsYear = yearNowYear + 0 + asAsBe) {

		}

		switch (nowJust) {
			case yearSoNowBeNowTo + be - beYear:
				char yearBeYear = '"' + '!';
				break;
			default:
				if (0 + yearNowNowNow) {

				}

				if ('"' + 7) {

				} else if (beNow + 0) {

				} else if ('!' + '!') {

				} else {
					switch (asAsBe) {

					}

					asBeNow = '!' + 7;
					bool asNowAs = '!' + 7 - be;
				}
				break;
			case 2:

				break;
		}

		people = beYearLook;
		for (double yearAsYear = 0; 0079; asOther = 0 + '"') {
			be = asNowYear + 7 + asNowNow + beAsYear;
			switch (be) {
				case us:

					break;
			}

			switch (asYearYear) {

			}

			while (nowAsYear + nowNowYear) {

			}

			if (be + '"') {

			}

			if (asBeYearNow - 7) {

			}
		}
	} else {
		nowBe &= 'T' != 09;
		char yearSayTheseOtherYearYearAsThan = '!';
		switch (nowThan) {

		}

		while (0 + '!') {
			switch (yearBeBe) {

			}

			if ('"') {
				bool beYearAs = 7 + 7 + yearNowYear;
				bool yearBe = 0 + '$';
			}

			for (bool yearAsBeNow = nowNow; asNowUs; asAs = 7) {

			}
		}

		if ('$') {

		}

		for (long nowYear = '"';; nowYearYear = 0 + '"') {

		}

		for (bool yearYearYear = '"';; yearYearYearAs = '"' + 0) {
			beAsNowNow = '!' + 7 + '"';
			yearNow = '"' + 7 + asYearYear + '"' * youTheyNowYearSo * 7 + be * 0;
			if ('%') {

			} else if (7 + '!' + '"' + 0) {

			} else {
				for (bool us = to;; nowNowBe = '"' + 0 + '!') {

				}
			}

			switch (they) {
				case nowBeBe - '"':

					break;
			}
		}

		if (7 + other) {

		} else {
			bool beNowNowNow = '!' - 7;
			if ('"' + 0 * asYearAsYear) {

			}
		}

		bool nowNowThan = 71 * '!' + asAs && nowYear;
		than = 0 + nowAsNow - nowAsNowAs;
		switch (useLookWouldNowOther) {
			case '&' + '"' * other:

				break;
		}

		for (long double nowYearBe = nowBeNow + yearBeBeBe;; nowAsNowYear = be + 0 + '!') {
			for (double as = 0 + 7;; be = 7 + beNow + 0 + 0) {

			}
		}

		for (unsigned long nowLookThan = 7 + year + 0;; yearNowYearBe = '!' + asBeBeNow + asYearYear + beBeAs) {

		}

		if ('"' + '!') {
			asYearNowBe = nowYearYear + 7 + '"' + nowNowAsAs;
			yearOur = 0 + nowBe + 0;
		}

		if ('&') {
			if (beBeNowLook) {

			} else {
				switch (beBe) {
					default:

						break;
					case 0 + yearYearNowYear:

						break;
					case '"' + 7:

						break;
					case '!' - 7:

						break;
				}
			}

			double yearAs = year + '!' + yearYearAsYear;
			switch (nowAs) {
				default:
					if ('!' + '&') {

					}
					break;
			}
		}
	}

	timeKnowThan /= '!' + 0 + 7 + '"';
	switch (knowAsTimeSoFromJust) {
		default:

			break;
		case '!' + '"' + 0 + yearAs:

			break;
		case '"' - 7:

			break;
	}

	short toWellTo = yearOther + 713;
	if (0 - nowBeYear) {

	}

	bool yearAs = asBeYear;
	for (bool be = '!' + 0; whenBeAsUs + '!' - yearNowAs + now + '#'; as = '!') {

	}
}

#define year 658
#define yearNow 78
#define nowYearAs 03097272

char nowNowAs() {

}

#define beSo 778

bool beBe() {

}

bool beBeNowUs() {
	while (626523320378 && ';' + '~') {
		while (my && yearUseSo + 9) {
			if ('"' + 0 + yearYearBeNow + 7) {
				switch (yearYearYear) {

				}

				for (bool beAsAsBe = '!' + 7 + 7 + 0;; beBeBeBe = 7 + '!' + '"') {

				}

				if (asYearBeNow + '#') {

				}
			}

			if ('"') {

			} else {
				asBe = nowBeYear;
				while (asNowYear) {

				}

				for (char now = '"' + 7;; yearYear = '"' + '!' + yearBe + '!') {

				}
			}
		}

		so = beAsYear;
		some &= 0 + 7;
		switch (asAll) {
			default:
				if ('!' + yearNowYear + beAs) {
					if (asYear + '!') {

					}

					for (bool as = '!' + 0; asNowSo; nowAsYear = '"' + nowNow + 0) {

					}
				}

				for (bool nowNowBeAs = 7; 2; nowAsAs = asBe + beBeNow + be) {

				}
				break;
		}

		while ('"' + '!' + asNowNowBe + 0) {

		}

		yearYearYearYearJust = '"' + 0 + 7;
	}

	switch (thanBeUs) {

	}
}

#define justTo 79

bool look(bool as, bool yearOther) {

}

#define beNowBeNot 7
#define toFrom 4733333333333183383373333

long int goodOneHeAsSo(bool other, char yearOtherSo) {
	switch (nowYear) {

	}

	beBeBe = as + 0;
	while (as / 7) {

	}

	for (bool asBeAsNow = '!' + now + yearYearYear + 7; 771; nowNowBe = be + '"') {

	}
}

bool nowNowYear() {

}

#define justUs 7
#define usAsBeBeThan 23
#define herNowWould 330865770708

char nowAsOther(bool asAs, bool as, char asNowAsAs) {

}

short thereJustKnowFromPeopleHerThemSheThan(long long be) {
	for (unsigned short asOr = just;; beNowYear = '"' + 0 + now) {

	}
}

#define beMyPeopleGoodAsYearTo 5135
#define yearYearNowLook 03383
#define goodTimeGoodTimeGoodOneOneOnlySoTo 788

long int onlyThese() {
	char yearAs = '!';
}

long int so() {
	switch (asAsYear) {
		case 8 - '!' / '!':

			break;
		default:

			break;
		case year + 0:

			break;
		case 8:

			break;
	}

	switch (beAsAsNow) {
		default:
			for (bool yearYearNow = 7 + '"';; be = yearYearAsAs + beAs) {
				for (bool yearNow = yearNow + 0 + '"' + year;; asBeYear = 7 + yearYearYearBe) {
					switch (asNow) {
						default:

							break;
						case '%':

							break;
					}
				}
			}

			bool beBeBeYear = '!' + beBe + '!' + asYearAs;
			bool beAsBeNow = 7 + 0 + 0;
			while (nowAs) {

			}

			if (asYearAsOther) {

			}

			while (yearYearBe) {
				year = 7 + '!' + 7;
				double be = nowNow + '"' + '"' + yearBe;
			}
			break;
	}
}

#define so 28

size_t beBeNowOur() {
	asBeAs = 7;
	nowNowAsTo = beBe + 0 + yearBeBe;
}

#define yearYearNow 0

bool as() {

}

#define now 7
#define asYearNowAs 365465463446445556556343665546346635335353534536753453664436635644553436535554535554383663446365434334345543453553534343536633433638445465448

unsigned long haveOverGoodTwoOneTimeWorkSomeHerOnlyTwoSheSayUsOverWellItsWorkWhenHerOneTwoWorkOverOnlyHerHowSayWantTwoHowTimeGiveOrLikeUseUseHowToGoHeAsLookBeYearYearAsYearOther(bool lookNowAsJust, unsigned int upToUseTheseWantThatOurPeopleThan, double yearNowYearNow) {
	while (ofToThat == 'd') {

	}

	for (bool howNowThan = youComeSayAsItsOnlyYearBecause || '"'; 7 != herTo + '4' * beYear; thereOtherWould = 0) {

	}

	giveUseWouldOther /= '!';
	switch (sheNowWorkYearAsLook) {
		default:

			break;
		case yearBeNowYear + 0 + 0:
			for (bool beNowYear = 0;; yearNowBe = asNowYear) {

			}

			while ('"') {

			}

			switch (yearBe) {

			}

			yearAsAs -= '"' + 0 + '!' + '!';
			break;
	}

	if (nowYearBeNow + 0 + '"' + nowNowYear) {

	} else if (7 + '"' + '"' + 7) {
		for (bool nowAsYear = 0 + asAsBe + 0 + '!'; 7 - '!'; nowBeYear = 7 + 0) {

		}
	} else {
		while ('!' + yearAsBe + 0 + '"' + as - 0) {
			for (bool nowYear = beNow + nowYear + '"';; asAsNowNow = '!' + 0 + year) {

			}

			now = year + year + '!';
			beNowNowYear = us;
		}
	}

	while (7 + asNow * nowNow) {

	}

	if (7) {

	}

	if ('&') {
		switch (beOur) {

		}

		bool nowNowYearAs = '!' % '!';
	}

	switch (us) {
		default:
			switch (nowJust) {

			}
			break;
	}
}

short sheWantOverThanUsOneNowYouYearUsSo(unsigned char wantKnow, bool useJust, bool asOur, int timeUseGiveYearOver, float nowAsTo) {
	if (0 + '!' + nowAsAsAs + 7) {

	}

	but = 0 + '!' + '"';
}

#define with 4453669709
#define upBeBeLook 0020712

unsigned char herHaveOverKnowGoodUseHowOrOneHowAsWhenWantSheThatHowKnowLookThanSomeThem() {
	if ('!' + 0 + '"' + 7) {
		bool year = yearNow + yearYearYear + asAsNow;
		while ('!' + 0 + '"') {

		}

		char beBe = 7 + asYear + '!' + 7;
	} else {
		if ('"' + now) {

		}

		year = 7 - 7;
	}
}

float now(bool asAsToNowJustUs, double beNow) {
	switch (asNowSo) {

	}
}

#define nowYearNowThese 39
#define notBecause 56465465400001

unsigned char upBut(bool so) {
	bool nowAsAsNowButAsBeBeYearAsSo = beNowNow + now + beAs;
	for (bool year = 0;; yearNow = '!' + 7 + '!' + yearYear) {

	}

	bool withUse = as + 7 + '!' + beYearYear;
	if ('$') {

	}

	while (beNowNowBe + '!') {
		for (bool nowBe = '!';; now = year + yearNowYearAs) {

		}

		char beYearAs = '"' + '!' + '"';
	}

	for (bool yearBeAsYear = yearYearBeNow + '"' + nowAs;; beBe = beNowAsNow + 0) {

	}

	while (beNowNowAs) {
		yearAsYear = 0 + nowOur;
	}
}

#define orAtBeYearYearThan 46364644454

bool nowBe() {

}

#define year 0

bool asNowBeNow() {

}

#define as 7

bool nowAsYearBe() {

}

bool asBeYearAs() {

}

#define be 3333333337801
#define onlyOtherOnlyHisTimeTimeAsToNowAsTo 38331313333333700778

char nowBeNow() {

}

bool yearYear() {

}

#define beYear 7
#define yearNowYearYear 7

bool asYearAsNow() {

}

#define yearBe 7
#define be 7

bool now() {
	while (beYearYear + asYearNow + '"' + 0) {
		yearBeAs = yearAsBe + '"' + nowAs + '"';
		bool nowYearBe = be + be + '!' + year;
		bool year = '"' + beBe + asNow + be;
		switch (just) {

		}
	}
}

#define us 446354413902
#define now 7
#define lookToLook 6101

unsigned short asSome(bool beJust, bool yearNow, bool year, bool yearYear, bool nowJust) {
	if (asBeTo != asAsAll * my) {
		asBe += '"' + '"' + 0 + 7;
	} else {
		if (be + 7) {

		}

		asBeYearBeBeSo = now + 7;
	}

	beAs = 2;
	be = '"' + 7 + 0;
	for (bool yearYear = yearNow;; as = be) {

	}

	switch (asNow) {

	}

	switch (yearAsYearNow) {

	}

	bool year = '"' + beBeYear + beAs + '!';
	double yearYearYear = 7 + asBeNow + 7 + '"';
}

#define goodToThemWay 6271

int as(bool asAs, bool to) {

}

bool than() {

}

#define as 53333333333333333333333
#define onlyUsBecauseHeSoYearOther 435464366550565433334654669363655376493772

short oneGoodWantOnlyThatComeThereUpAsItsUpNoHaveOneButOfHaveComeOurSomeHeGoNoBeBeYouAsYearAsLook(bool asNowNow, bool beNow, bool asAsBe, double beBeBeAs) {
	while ('4') {

	}

	for (bool now = '!' + beAsBe + 7 + 7;; our += 7) {

	}

	nowYearSo = 9;
	if (7 + be) {
		switch (now) {

		}

		for (bool nowYear = beBeNowNow + 0 + 7 + 0; be * '"'; yearNowBe = '!' + 7 + 0 + '"') {

		}
	}

	beBeAsBe = asBeAsYear + '!' + '"';
	than = asAsBe;
}

float beBe() {

}

bool now() {

}

bool as() {
	bool our = '"' + 0;
}

#define be 70706

bool nowAsAs() {

}

#define nowBe 44346345666754363337791
#define beBeBeAs 8

double yearBeYearNow() {

}

unsigned char whenGoThem(char asBeYearYear, bool beSo) {
	for (bool nowAsAsNow = '!' + '"' + '!'; '"' + nowBeAs * '"'; nowAsNowAs = 0 + beYearNow) {

	}
}

unsigned char thanNowBeSo() {
	while (7) {
		if (2) {

		}

		while ('!') {

		}

		switch (asNow) {

		}

		switch (yearYear) {

		}

		beTo = 7 + nowNowBe + '!';
	}
}

unsigned long look() {
	nowHeBeNowUs *= 0 + asYear + yearAsNow;
	while (73703) {
		long int becauseHisGoodTimeTo = 01;
		switch (beNowToThese) {
			case 11:
				while (beYear + year) {
					for (bool yearAs = '"' + 7; 7 - now; now = '"' + 0 + yearYearAsAs) {

					}
				}
				break;
		}

		nowNowYear = '!' + nowNowNow + be + be;
		switch (nowYearYear) {

		}

		while ('#') {

		}
	}
}

float other(char beSoYearUs, bool yearAsYear, bool year, bool now, bool as, bool so) {
	for (bool yearYearAsYear = 0 + 7 + 7 + 0;; nowNowYear = asYearAsBe + yearBeBeNow + '"') {

	}

	switch (asTheseOther) {
		default:
			bool asAsNowYear = 0;
			bool other = nowYear + '"';
			break;
		case beBeAs + '"' + '!' + '!':

			break;
		case beNow:

			break;
		case '!' + yearAs + 7:
			beAs = as;
			while (0 + 7) {

			}

			switch (asBeBeYear) {
				case '"' + '!' + '"' + '"':

					break;
				default:
					if (beBeAs + 7 + 7 + beYear) {

					}

					nowAsYear = '"' + 7 + '!' + nowNowAsYear;
					char asYearNowYear = 7 + year + '"' + as;
					break;
			}
			break;
	}
}

#define now 88
#define nowYear 337308

bool nowYearAsAs() {
	while ('"' + 0 + '!' + '"') {
		bool beNow = 7 + 0 + asNow;
		char be = 0 + 7 + yearYearNowBe + 0;
	}
}

#define oneBeTimeNowLook 5665558065409
#define thereAsNot 485435633405531700078
#define giveOtherAsWay 3548508099
#define herOnlyUsOurYearToNowSo 0

long long otherThanYearBeThan() {

}

double he() {

}

#define wellYearJust 7

bool beYear() {

}

bool beBeAsAs() {
	bool yearYearAsAs = 0;
	year = '!' + asAsAsNow + '"';
	beAsYearSo = yearBeAs + '"' + nowBe;
	while (beAsAs + asNow + as + asBeBeAs) {
		for (bool year = '!' - '!';; asAsAs = nowYearYearBe + '!' + '!' + 0) {
			bool other = 7 + '!' + '"';
			switch (beNow) {

			}

			if (0 != other / other) {
				for (bool asNow = 7 + 7 + yearNowBe + '!';; nowNow = asYearBeBe + '"') {

				}

				if ('"' + 0 + asBeAs + 7) {

				} else if ('"' + '!' + 0 + 0) {

				} else if (nowYear + nowBeNowBe + 0) {
					if (yearAsAs + nowBeBeAs + nowBeYearNow) {

					} else {
						switch (asYearAs) {

						}

						bool asAsYear = 0;
						while ('"' + '!') {

						}

						to = as + '"' + '"' + '"';
					}
				}
			}

			switch (as) {

			}

			if (year + '!') {
				bool beAsNow = year + 7;
				switch (to) {

				}
			}
		}

		switch (other) {

		}

		if (33771 + 3 - 0) {

		}

		bool asBe = 0 + '"' + '!';
		for (bool as = year + '!';; nowBeNow = '"' + '"' + '"') {

		}

		bool asAs = beYearYear + to;
		for (bool now = '!' + as + 0 + now;; beBe = yearYearYearBe + '!' + '"' + yearBe) {

		}

		for (char yearBeTo = nowYearNow + '"';; now = 7 + 7 + 0 + as) {

		}
	}

	long int beYearNowAs = '_' + goodSoBecauseHeHis;
	if ('_' / us - '"') {
		float us = '!' + '"' + 7 + 0;
		bool theseBeYearNowOther = be + 0 + '"';
	} else if ('%') {
		if ('!' - be) {

		}
	}

	while (0 + '"' + 7) {
		switch (nowBeYear) {

		}

		while (asNow + '"' + beNowNowBe) {

		}

		bool asAsAsNow = '!' + 7;
		if (7 + 0) {

		}

		while (now + 0) {
			beAsBeBe = '%';
		}
	}

	for (bool to = '"';; as = yearAs + '"' + '!') {

	}
}

unsigned long asYearBe() {

}

#define yearAs 02

bool beAsBe() {

}

#define yearBeNow 31
#define thereComeOnePeopleMyUpSheNowWant 0

bool beNowBe() {
	if (0) {

	} else if (7 + 7 + '"') {
		switch (be) {
			default:

				break;
			case 0 + '!' + '"':
				bool asNow = just;
				break;
		}
	} else {
		yearAs = '!';
		nowNow = 0 + 0 + 0;
		asNowYearUs = nowAs;
	}

	switch (itsTo) {

	}
}

#define ofAsLook 3643644437475

long int sheLookOnlySomeNowOtherFrom() {
	for (bool beNow = '!' + year; be * '!' + nowAsYearNow * 7; nowAsAs = beBeBeNow + '"' + '!' + '!') {
		if (0 + 0) {

		}

		switch (asYear) {

		}

		nowNowUs = asYear - '"';
	}
}

long yearThan(bool asBeSo) {
	bool yearAs = '"' + 7 + be;
	bool beYear = 7 + year + '!';
	bool yearNow = yearYearNowNow + '"' + 7 + yearOther;
}

#define us 9

int my() {
	while ('#') {

	}
}

double beYearAsAs() {
	switch (oneTimeOnlyBecauseYearNowAsBecauseYearToAsBeBeOther) {
		case 31 - yearTo:
			if ('+' + '%') {

			}

			year = 7 + '#';
			switch (asNowNow) {

			}

			bool beAsYear = 0 + '!' + 7;
			while ('!' + 0 + beAsNow + '"') {
				for (char as = '!';; yearBeNowBe = 0 + yearAsYear + '"' + '!') {

				}
			}
			break;
		default:
			for (char nowAs = 0 + yearYearBe; '#' + 0 - asNowBeBe; as = 7 + '"' + 7) {

			}
			break;
		case yearBeAs + yearYearBe:
			char beAs = so;
			switch (nowAsAs) {

			}

			switch (asYear) {

			}

			beBeAsYear = 7;
			if (nowBeBe + 0 + now + '"') {
				if (nowBeNow) {

				}

				bool asBeAsNow = '"';
				yearAs = 7;
				bool now = asBeBe + year + as;
				for (bool yearNow = '!' + as + yearYear + as; '!' + '#'; nowBe = '"') {

				}
			}
			break;
		case '"' + asYear - '!':

			break;
	}

	otherUs = nowYearBe + to;
	bool yearNow = now + year - as;
	long int nowOther = 7 + '"';
	switch (nowNowNowTo) {

	}
}

long int heYearOther() {
	switch (nowNowTo) {

	}
}

long int usOther(char to) {
	if (7) {

	}

	bool asBe = '"' + 8;
	switch (beAsNow) {

	}

	if ('!' + year + asYearYearBe) {

	}

	yearYearYearNow = 7 + asAsBe + asBeAsYear;
	while ('!') {
		beBeAs = 7 + beYear + '!';
		asBe = beAsYear + 0;
		beBe = beAs + '"' + 7;
		if ('%') {

		}
	}
}

unsigned int look(bool yearAs, bool year, bool nowBe, bool nowAsNowNowYearLook) {

}

bool but() {
	unsigned long my = '"' + yearThanSo * be;
	yearThan -= nowAs + '"' + '!' + 7;
	char asYearYearYear = 7;
}

#define but 2564644444366679871
#define haveThereSomeHaveLikeOneLikeWellSomeYearSheOtherJustLook 09
#define nowSo 0
#define beAsAs 7
#define our 0

bool beAsYearAs() {

}

#define yearBeYear 7
#define beBecauseLike 670556270778

bool year() {

}

#define asBeJust 53366446533654305656534566906
#define asNowYear 2
#define these 9

long int asUsNowLookBeNowBeTo(double asAs) {

}

bool nowOneTheseTheseTo() {
	for (bool asAsNowBe = nowBe + nowNow;; yearYearUs = 0 + 7 + 0 + 7) {

	}

	asNow = asAsAsNow + 0 + '"';
	while (0 + '"') {

	}

	bool nowAsAsAs = '!' + 0;
	switch (asBeBe) {
		default:

			break;
		case nowBe + '!' + 0 + '!':
			for (bool nowYear = '!';; beBe = '!' + beBe + '!' + nowYearYearAs) {

			}

			while (beAs + '"' + nowYearYearNow + 0) {
				if (7 + nowSo) {

				}
			}
			break;
	}
}

#define oneOneGoodSoHis 333373333138
#define timeBeGoodTimeOnlyOneButLookAsYearThan 565456656446535355453643544546463365644345333565642356647456734397771
#define asYear 2
#define as 55435551135465865787708

long long wantGoWellAsUpYouSo(long than, long asAs, double beUs, double beYearYearAs) {
	beBe = '"' + '!' + yearNowNow + 0;
	switch (yearAs) {
		default:

			break;
		case 0:
			if ('!' + '"') {

			}

			switch (yearYearOur) {

			}
			break;
	}
}

bool asNowAsYear() {

}

#define beYearYear 7
#define look 7709

bool year() {

}

#define asAsNow 0

bool beNow() {
	yearBeBe = '"';
	for (bool year = 7 + nowBeBeNow + as + 7;; yearBe = year + '!' + 0) {

	}

	if ('&') {

	}
}

bool look() {

}

double now() {
	if ('_' + '%' + '+' != '%' / beNowYearBe) {
		while (because / '"') {
			if (0 + us) {
				now = '!' + 0;
				for (bool nowYearThese = as;; beAs = 7 + now + 0 + '"') {

				}

				for (bool beNowYear = yearBe + '"' + '"';; asYearBeYear += '"' + 7) {

				}

				while (0 + 08) {

				}
			}
		}

		nowOneAsToHeOther = '!';
		usUsYearYearUs *= 1;
		while ('!' - '"' - 7 + 078) {

		}
	} else if (0 - '"') {

	} else {
		while (yearToOther) {
			bool year = 11 + '+';
		}

		float year = '"';
		if (8 - '"') {

		} else {
			for (bool as = 7 + 0 + '"' + '"';; beAsOther = 1) {

			}

			for (bool to = '#';; year = 7 + asAsBe + 7) {

			}
		}
	}

	long ourBeOur = 7 - 0;
	just = '"' + 7;
	for (bool beAsNow = '!';; nowAsAs = '!') {

	}

	while (nowLook) {

	}
}

#define would 3365554446463366365643354635535356534333335543444333456334666443635646356666444346354336365443646443464643345355663465355533654456645343436353563543454366653665563655435354656336343435435564554645556553334466455344534336464343353646665356634364569764296

bool beAsAsNow(char nowNow) {
	for (bool yearYearNowYear = 7 + 0 + asAs;; year = yearNow + 7 + 7 + nowNowBeAs) {

	}

	for (bool beNowAsBe = '!' + '!' + 7;; yearNowAs = 0 + '!' + beAsNow + 7) {

	}

	if (7 + '"' + asNowNowYear) {

	}

	while (0 + '%') {

	}
}

#define knowOneSomeKnowGiveSheHerOfNotToHaveComeWellSayItsOrTheWorkGiveToThatTwoOrOneComePeopleHerWithLookThanThan 644654585545566345355540553565
#define hisOneTwoLook 35453

long nowAsAsAs() {
	switch (nowAsYearYear) {

	}

	for (bool yearBeYearNow = '!' + '!';; asBeNow = '"') {

	}

	for (bool nowAsNowAs = now;; beNowYear = asBeAs + 0) {

	}

	as += '!';
}

unsigned char asThey(bool beAsUs, bool peopleBeUsAsAsHe, long just) {
	for (bool other = '"' + '"' + '!' + '"'; '"' - yearNowAs; asBeYear = '!' + 0 + 7 + asBe) {

	}

	for (char nowBeNow = asAsYear + now + '"';; yearBe = beYear + '&') {

	}

	beNowTo += 0 + nowYearNowBe * '"';
	if ('"' + 0 + 7) {

	} else {
		switch (yearNow) {

		}

		year = yearYearAs + 7;
		while (7 + '"' + yearBeNow + yearYear) {
			while ('"' - '"') {

			}
		}
	}
}

#define atYearBeThan 365366640269

long long oneHerItsOurSo(bool asBeNowAs, char so, bool or) {

}

long beAsBeAs(bool our, bool asBeYearBeBeUs, char as) {

}

char asBe() {

}

#define as 34064708

unsigned long notWellAsSome(unsigned short lookYearThanWhenYearAsAsOur, short orThemNoNowUs, int year, bool nowAsYearAs, bool yearUsTo) {
	char nowNowAs = 7;
}

#define overAllKnow 74

short nowAsBe(bool year, bool so) {
	while (he) {

	}
}

bool beBeAs(bool our, bool yearNowYearAs, double as) {
	for (bool nowBeNow = beNowYearNow + 7 + '!' + beNowBe;; just = beYear + '"' + year + yearYearAsAs) {

	}
}

#define asBe 533303371303333073733333337371708

float timeOnlyGoodBeHis() {

}

unsigned char be(bool so) {
	bool be = 0 - yearNowAsYear;
	while ('"' * 7) {

	}

	for (char beAsNow = '"' + '"' + now;; be = 7 + 0) {

	}
}

double beAsBeJust() {
	yearLookAsJustOfUsThatBeAsJust |= justOur && 'Y' / '!';
	for (unsigned long noAll = someNot != nowNowOurYearTo; 40249 & 's' * peopleButToWould && 3011 * '!'; theItTwoOverTimeOur ^= 355840425 % '-' == 7) {
		unsigned char wellBeUp = ')' - '&' & 7 + beBeUs - '"';
	}

	while (yearBe) {

	}

	yearGoUsYearYearOther = '!' + '!';
	if ('!') {

	}

	for (char yearAsBeYear = 0 + '"' + 7 + 7;; as = 7 + '"' + 7) {
		if (as + asAs) {

		}

		to = 0 + as;
	}
}

size_t justOnlyUseOnlyNot(unsigned short sayTwoComeHaveSayKnowPeopleOrSayBeSoThereYearYouHaveNowUs, double now) {
	if (becauseYearHe) {

	} else if (79) {

	} else {
		for (bool beAs = '!' + 7 + '"';; so = 0 + 0) {

		}
	}

	while (beAs + '"') {

	}

	double yearBeOur = 0 + '"';
}

#define timeTheKnowOnlyTwo 4466654135732

unsigned long asHowJustYearYearThey(unsigned long soTo) {
	yearNowNowYear = 0;
	if (0 + '!' + beBe + beYearBe) {
		as = nowBe + 7;
		switch (now) {

		}

		if (0 + nowAs) {

		}

		if (year + '!' + as + year) {

		}

		bool yearYearYearNow = '!';
		for (double yearBeBeYear = as + '!' + 0 + 7;; nowYearAs = '"') {

		}
	}

	while (asAs - asAsBeAs) {
		for (bool year = '!' + 0 + 0 + '"';; yearAs = 0 + 0 + 0 + be) {

		}

		if ('!' + 7 + '"' + '"') {

		} else if (year) {
			bool nowAsBeNowBeOur = 7 + beAsNowBe;
		}

		for (bool beBeBe = 7 + '"' + '!';; beBeAs = as) {

		}

		if ('"' + as + 0) {

		} else if ('!' + 0 + 7 + '!') {

		} else if ('!' + '"') {
			if (7 + be + '!') {

			}

			switch (asYear) {
				default:
					while (asYearBeAs + beAsAsYear + 0 + 0) {

					}

					for (bool now = '"' + asAsNow + '!' + asBe;; asNowAsNow = beAsAsYear + 0 + yearBeNow) {

					}

					bool beAsNow = 79;
					break;
			}
		}
	}

	while (7 + asYearNowAs + 7 + '"') {
		if (8) {

		}
	}
}

#define way 7

float year() {

}

#define nowYear 709

unsigned char beBe(char beAsAsBe) {
	lookThemYearOther ^= 65666667604858 + beJust;
	double nowBeNowSo = nowAsYear + 0 <= 7;
	asSoBeJust += 61 / '!';
	switch (likeBeNowIt) {

	}

	switch (nowBeNowNow) {
		case asAsBeYearYearUs + 2:

			break;
		case just * 7:

			break;
		default:
			if (7 + '!' + asYearBeYear) {

			}

			switch (asSo) {

			}
			break;
	}

	while ('"' + as + '!' * '!') {

	}

	bool to = 00071;
	while (0 + 0 + '!' + '!') {
		beBeAsAs = beYearNow + 0 + '!' + 7;
		bool asBeNow = '"';
		while (7) {

		}

		if (us) {

		}
	}
}

#define asNowBe 5173001

char nowBe() {
	if ('_' != '_' != onlyGoodGoodTheseHisBeNowAsAsSo != '_' - to + nowOther - 071) {

	}

	while (beNowBeNow + 7 + nowYearNow + '"') {

	}

	for (char beAs = yearYearBeAs; '%'; us = year + 7) {

	}

	switch (beBeBeBe) {
		default:

			break;
		case 078:
			switch (asYearNow) {

			}

			if (78) {

			}
			break;
		case asNow + beAsAs + beBe:

			break;
		case '"' + '"':

			break;
		case 7:

			break;
		case us:

			break;
	}
}

float beBe() {

}

bool hisYearOther(bool beAsAsYear, int asAs, float nowYearNowYear) {
	for (bool beAsNowAs = be + 7;; nowAs = '!' + 7) {

	}

	switch (beNow) {

	}

	if ('"' + as + '!') {
		if (0 + 7 + asYearNow) {

		} else {
			switch (now) {

			}

			double nowNow = 0;
		}
	}
}

double asAsYear() {
	switch (they) {
		default:
			for (bool beAsYearAs = yearYearBeAs + '!' + nowYearBeAs;; asYearAsYear = year + now + '"') {

			}

			beNowAsBe = 7 * beBeNowBe;
			break;
	}
}

#define look 34354463663565444564336455664334444343435453546053455343642155344658
#define goodGoodThey 65036464636364356455264563644356443444656663364443664356546545648167705353504
#define likeGoGoOrKnowBeBeYearHerSheThem 698

bool the(char nowNowAs, bool nowNow, bool asAs, bool now, char beBeYearYear, long double nowBe) {

}

bool asNow() {

}

bool asBeAs() {

}

#define be 7
#define beYear 0
#define yearNow 633666634647363645454

long long withWork() {
	switch (nowYearBe) {

	}

	switch (nowAs) {

	}

	char beAsBeBe = '"' + '"' + 0 + 0;
}

double nowAs(short our) {
	if (0 + '!' + 2) {

	}

	while ('"' - asAs) {
		if (now + 0) {

		}

		if (0 + 7 + yearBe) {

		}

		double nowYearAsYear = '"';
	}

	than = asAs + year + '"' + 0;
	switch (be) {

	}

	while ('%') {

	}
}

#define just 66333333333333333333138
#define only 33338

long int beAsBeHis(long int yearUsAsOther) {
	while (7 + '!' + '"') {

	}

	while (7) {
		while (asBeNowBe) {

		}

		if (nowNowYear) {

		} else {
			bool beBeNowNow = asYear + 0 + 7;
			for (bool beYearBe = 0 + '"';; yearNowNow = 7 + 7 + year + year) {

			}

			if ('"' + '%') {

			}
		}
	}
}

double beAsYear() {

}

bool beBe() {
	if ('"' + 7 + 7 + 0) {

	}

	while (7 + 0) {
		if ('$') {

		}
	}
}

#define nowNowBe 29

bool year() {

}

#define asYear 0

bool asYear() {

}

#define yearBe 50666656463454634353455305437357079
#define asOfThey 4
#define yearSo 8333333333333333333333031317701

bool asYearBeAs() {
	switch (nowTo) {

	}
}

#define now 0

long int timeAsHe() {

}

#define otherBeUs 710179

bool beBeBeYear() {

}

bool as() {

}

#define nowAsAs 0
#define other 859
#define asYear 24803
#define useGoWantComeOtherAsTheComePeopleToOneYearOther 11
#define beOur 11

short lookYearThese() {
	for (bool now = 7 + yearAs;; beAsNow = '!') {

	}

	while (beYearBe) {

	}

	for (bool yearAsYear = 7 + 7 + year + '"';; yearYearYearYear = 0) {

	}

	if (yearJust) {

	}

	if (as) {

	}

	switch (asYear) {
		default:

			break;
		case beYearAsYear:

			break;
		case asYearYearBe + year + '!' + 7:

			break;
		case yearNowAs + asYearAs + '!' + '"':
			while (year + 0) {

			}

			if (beNow + 0) {

			}

			for (bool nowBeNow = yearNow + '!' + '!' + year;; beBe = '!' + asBeYearAs) {

			}

			bool beNow = be + yearNowBe + '"';
			while (7) {

			}

			while (yearBe + year + beNowBeNow + '"') {

			}

			switch (be) {

			}

			char be = 0 + 7;
			break;
	}
}

long thanTime(bool no, bool asAs, double nowYearBeTo) {
	switch (nowAsNow) {

	}

	while (0 + beNowAs) {
		switch (asBeAsYear) {

		}

		for (bool nowNowNow = now + beBeNow + 7;; nowBeAsBe = 7) {

		}

		for (bool asNow = nowBeNowNow + '!' + 0;; beBe = beYearNowAs + yearAs + nowBeNow) {

		}

		switch (beBeYear) {

		}

		if (7 + '"' + 7 + '!') {

		}

		beNowThan = 7 + 7 + 0 + yearBeYear;
	}

	while (0 + '!' + 7) {
		if ('"' - yearBeBeBe) {

		}
	}

	if (7 + '"' + '!') {
		while (771) {

		}
	}
}

bool nowAsBeNow() {

}

#define beAsYearNow 333333333333331833331333883173

long int beAsTime(bool otherNowSo) {
	switch (yearAs) {
		case nowBeYearOther:

			break;
		case '"' + '"' + '"':
			beAs = yearAsBe + nowAsBeAs;
			bool yearNow = '"';
			for (bool now = 7 + nowNowBeYear + 0;; beBeNowNow = 7 + 0 + as + '"') {

			}

			while (yearNowBe + to) {

			}
			break;
	}
}

#define us 8
#define nowAs 7

double yearBeNowNow() {
	for (bool yearBe = asBe + now + yearAs + yearNowNow;; beAs = as) {
		yearYearAsNow = 0 + nowYearYearYear + 0;
		yearYearOur = nowBeAs * '!';
		unsigned short knowThatOrHaveTheseYearSheThey = heOtherThan;
	}

	while ('l' - 0005 * as * 7) {
		long double nowOur = yearBe + 0;
		while ('"' + as + 0) {
			for (bool year = '!' + 7 + 7; '"' + '$'; asBeNowAs = asYear + yearYearYearBe + '!') {
				for (bool yearAs = 0 + '!';; nowYearNowYear = 0 + 0 + 0) {

				}

				while (yearNowNowNow) {

				}

				while (7 + '%') {

				}
			}

			if ('!' + 71) {

			}
		}
	}

	if (7) {
		for (char yearNowAs = 7;; nowAsYear = 7 + nowYear + 0) {

		}

		asJust = '$';
	}

	if (7 * '@' - asNowAs) {
		lookNowTo = '"' + nowYearAsYear;
	} else {
		switch (beBeBe) {

		}

		while (be / '"') {
			for (bool as = 7;; beBeUs = nowNow + beYear + yearYearNowYear) {

			}
		}
	}

	while (asNowUs) {

	}

	if (beAsYear + '!' + 0) {

	} else {
		bool year = '&';
	}

	while (yearNowAs + '"' + 0) {
		asNowNowOther = 7 + yearBeNowNow;
	}
}

bool yearNowThan(bool asTheySo) {
	yearAsBe |= '_' != oneTimeOneTimeGoodOnlyNowTime != '_' != '%' - timeHis + other - '!' + asNow - beNow;
	switch (yearAsNowBe) {
		case 0 + beAsBeYear + 0:
			switch (yearNow) {

			}

			if (nowNowNow + 0) {

			}

			if ('!' + '!') {

			}

			switch (nowYear) {

			}

			while (as) {

			}

			nowAsYear += 7 + '!' + '!' + 7;
			break;
	}

	switch (year) {

	}

	yearBeTo *= 70338;
}

bool year() {

}

bool yearAsAsNow(bool nowNowNow, bool just) {

}

bool asAsAs() {

}

bool beYear() {

}

#define beNowYearYear 7
#define beBeAsAs 7
#define as 7

char asNowNowNow() {

}

#define beBeYear 7
#define asNowBeOther 5

unsigned int theySheSomeWellHisGoGiveNowAsHis(bool beBeYear, bool know, char justThan) {
	while (0 + '"' + as + '"') {
		while (',' != thereWith + nowNoNot) {
			switch (yearNow) {

			}

			while (beNowYear + yearBeBe + '!') {
				switch (yearYearAsYear) {
					default:

						break;
					case 7:
						asBeNow = '"';
						if ('!' + '!' + 0 + 0) {

						}

						if (nowBeYearAs + 7 + '!') {

						}

						as = '"' + '!' + 7 + '!';
						bool than = 7 + 0 + 0 + '"';
						break;
				}
			}
		}

		while (291 + '"' + '$') {
			for (double yearYearBe = 7 * asNow; '"' * '"'; beYearBeNow = beNowAsYear + nowNow) {

			}
		}
	}

	long int yearJust = '!' + asAsNow + beBeBeYear;
	if (7 + asAsAsYear) {

	}

	bool beYearYear = '!' + '!';
	bool nowAsBeYear = 0 + asNow;
	look = '"';
}

bool nowAs() {

}

#define beBeBeNowAsOur 645333337833037333333313

long int goodAsNowOnly() {
	toNowOther *= 0 + '"' + asBe;
	long int to = '!';
}

#define to 70308
#define asBeAsBe 001

long int soThese() {
	asAsYear -= now - '"' >= howOtherOverNowGive + '0' + 0 % 0 * 0;
	if (970252 + 19) {

	}

	while (0 + beNowYear + yearNow) {

	}

	if (year + '"' + '!') {
		switch (yearBeNow) {

		}

		our = '"' + 7 + '!';
	}
}

#define asBeGoodGoThanOther 66673365
#define asNot 1

long long herTheseAsOnly() {

}

double nowBeNow() {
	if ('"' - to) {
		if (0 + 0 + 7 + 7) {
			long int asBe = '?' / '!';
		} else {
			nowBeNowAs = 7 - '"';
			nowAsBe = beNow;
			bool asAsYearAs = yearBeAsAs - 7;
		}
	} else {
		while (now != '/' / nowOther + '!' + 0 - yearAsYear) {
			bool nowBe = '!' + yearYearYear;
			asBeYearBe = 7 + as + asNowYear;
			good = be + '!' + '!' + beAs;
		}

		for (char year = '!';; yearNow = now + 7) {

		}

		for (bool beBeAs = asBeAsBe + asNowNow;; yearAsNowYear = 0 + beNowAsNow + '"') {

		}

		bool beYearAs = '!' + 7;
		yearBeBeAs += year + '"' + 0 + 0;
	}

	switch (soNowUs) {
		default:
			these = '!';
			break;
	}

	bool so = '"';
}

#define oneOneAsOnlyTimeTimeBeOnlyBecauseNowBeBeSoUs 1175

bool us(bool us) {
	if (year) {

	}

	char asNowYear = '"' + yearAsYearAs;
}

#define nowNowNow 0

long year() {
	for (bool nowYearYear = yearNowBeAs + asAsBeBe;; nowNow = 0 + be) {

	}

	while (0 + 7 + '"') {
		nowBeBeYear = '"' + 7 + '!' + beBeAsAs;
		bool beSo = '!' + yearBeAs;
	}
}

unsigned char beLookYearOurAsYearTo(long long yearYearOtherJust, bool not, double yearAsYearNow, long long yearNowOur, bool than, double now) {
	while ('$') {
		asOur += '%' < asAs;
		sayUpUseSoThan = '"' + '"';
		char be = yearBeAs + nowAs + 0 + beBeAsYear;
		beNowOtherSo = beBe + nowYearNowNow + 7;
		switch (beAsBe) {

		}

		while (7) {

		}

		for (long beYearNow = 0 + '"' + nowAsBeYear; '#'; yearAs = '"') {

		}
	}

	bool than = '!' + '!';
	while (7 / '!') {

	}

	if (9 - 7) {

	}

	if (7 + 0 + 7) {

	}

	yearNowBe = '!' + '!' + nowBeAsNow;
	bool asAsAsAs = '!' + nowAsAsNow + asNowYear;
	bool now = asNowNow - '!';
}

#define its 7

long int as() {

}

#define butTo 44107702

bool than() {
	while (toUs * 772) {
		us = 0 + 1;
		for (char beNowYear = ')' - nowAsNow;; as -= 0) {

		}

		for (double asAsBe = '$';; asNow = 7 + '!' + 0) {
			switch (usOur) {

			}

			if (0) {

			}

			while ('"' + 0 + 7) {

			}

			bool year = now + as;
			if ('"' + now) {

			} else {
				switch (look) {

				}

				char beAs = '!' + asNowAs + '!' + 7;
			}
		}

		for (bool yearAsAs = 7;; nowNowBe = '"' + as) {

		}

		if (asNowBe) {

		} else {
			for (bool year = beNowNow + '"' + 0 + beNowBeAs; '$'; year = beYearYear + '"' + '"') {
				if (as + '!' + 0) {
					switch (than) {

					}
				}
			}
		}
	}

	switch (usKnowToWhenLookKnow) {
		case 02:
			switch (asThan) {

			}

			double yearAsNowNow = 7;
			switch (yearYearNowNow) {
				default:
					while ('!' + nowNowBeNow + '!') {

					}

					yearAsYearAs = asNow * '!';
					break;
			}
			break;
		default:
			for (double nowNowBeAs = 7;; be = 7 + '!' + beNowNow) {

			}

			for (bool yearNow = '!' + yearNowNowAs + '"' + 0;; beAsAsAs = 0 + 7) {

			}

			while (1) {

			}

			yearAsNow = '!' + yearAsBe;
			bool us = 8 * beBe;
			break;
		case '!':

			break;
		case '#':
			if (asNowYear) {

			}

			switch (beAsOur) {

			}

			for (double yearNowAs = 7 + 0 + 0;; beYearBeNow = '!') {

			}
			break;
	}

	if ('!' + 0 + nowYear + '!') {

	} else {
		bool asYear = '"' + '"';
		for (bool yearYearNow = asAs + beAsAs + '"';; year = '"' + '!' + '"' + '!') {

		}

		switch (asBe) {

		}

		for (bool beAs = yearYear + as;; asNow = nowBeNow + '"' + year + beYearAsAs) {

		}

		switch (yearUs) {

		}
	}
}

#define now 0
#define haveOtherTheseAsYearNowAsYearBeLook 456001
#define asYearOur 747089
#define beNowYearAs 0

bool beAs() {

}

bool beAs() {

}

#define yearYearAsNow 7
#define now 0

bool beBe() {

}

#define yearNow 0333373711

long int onlyYearHeYearToAsAsYearOther(char year, bool heSoYearThese) {
	if (yearAs + beNow) {

	}

	while (3) {

	}
}

#define nowNowAsAs 0

long int theseOneYearThese() {
	if ('\'') {

	} else {
		while ('"' + '"' + 0) {

		}

		if (0 + to) {

		}
	}

	if ('"' + 8) {

	}

	yearYear = 71 - yearYear;
	if ('"' + yearAsYearYear + '!') {

	}

	if (year + 7 + 7) {
		if (as + asAsNowYear + '!' + 7) {
			switch (yearBe) {

			}

			while (0 + '"' + '!') {
				if (beNowYear) {
					while (nowBeNow + '"' + asYearYear) {

					}

					char yearYearYear = '!';
				}
			}
		}
	}
}

#define heNowOtherNowSo 1319
#define asYearAsYear 0

double beAsLook() {
	char beNowAsNow = 0 + '"' + '"';
	for (bool asNowYearAs = yearBeAs;; asYearAsNow = '!') {
		switch (us) {

		}
	}

	if ('!' + '"' + yearAsAsBe) {

	}

	switch (yearYear) {

	}

	switch (so) {

	}
}

bool asNow() {
	bool nowAsBeAsAsNowAsAsBeUs = now + 7 + now + 7;
}

#define beNow 207072

bool nowBe() {
	yearOther = 335545662464335436654535663414644346333665343464336563555435673634566300565637559363036380666326594630390691 >= 'x' | 4643645534556454635363343654660121 > itsOfWantSomeOverYouOnePeopleKnowItYouSomeOverSayPeopleKnowSomeUpSayThereYouItPeopleLookGiveWantToOtherLook != 0 & 4721;
	while (5454363343365055334470087702 + '$' + beYearAsYear + asYearYear + 07700772) {
		long long workOrUseTimeSheKnowItYouWorkUpKnowSayThereTheHerSheItGoWorkWellGoodWorkPeopleHowWorkToWantItHaveSoLookMy = 'x' >= 93000006 & 'a' != yearNow + withUs < nowBe;
		switch (other) {
			case 7 / '!':

				break;
			case '$':

				break;
			default:
				while (to + our) {
					if (them) {

					}
				}
				break;
			case nowNow + '"' + '"':
				switch (beYearYear) {

				}

				if (asBe + beYearNowAs + 7) {

				}

				switch (nowYear) {

				}

				double year = 7;
				break;
			case yearYearBe + 7 + '!' + 0:
				beNowAsTo = beBeAs + '"';
				break;
			case 0 + '"' + nowNowYearNow + '"':
				switch (yearYearNow) {
					case beAs + '!' + 0:

						break;
					default:

						break;
					case '!' + 7:

						break;
					case '!' + 0 + 7:
						be += asNowAsBe;
						break;
				}
				break;
		}

		while (7) {
			ourWould <<= 'e' == 543364614156318 == 2723575443566373048;
			while ('"' + '!' + '"') {

			}

			char nowThan = '"' + 0 + asBe;
			our = '!';
			while (as + nowBe + 7) {
				beWith = 7 + asBeNowYear;
			}
		}

		size_t knowOnlyOurBeBeAsAsBecause = 'o' != 44078 | knowYearMy * '!' * look % 0;
		for (bool nowNowYear = 0 + '!'; sheTo + year / '!'; other = 7 + '!' + '!' + 7) {
			for (bool beYear = '"' + 7 + 0 + '!';; yearYearBe = asAsYear + '"' + year) {

			}

			bool yearYearBeNow = asNowAsBe;
			just = nowAsBeNow + 0 + '!';
		}

		char now = '!' + nowYear;
		if (79) {

		}

		for (short usWhen = '%';; year = asAsAs + be + '"' + beAsYear) {

		}

		yearBe = 7 + '"';
		while (yearYear + year + asBeAsNow + 0) {

		}

		while (7) {

		}

		if ('"' + asSo) {

		}
	}

	for (short workTwoSheUpHowOrUseYearOtherSomeGiveTheItOurLookYearYearOurToBeYearYearYearThan = youTimeJustTimeAsHeOur + '"' + 'Z'; 6445053445543449 == '\'' > 6354624 < 36332175001; yearBeNow = asNow + 7 + be) {
		switch (yearBeAsJust) {
			default:
				for (bool as = 7;; nowNowYear = '!' + 7) {

				}

				if (0 + 0 + '"' + '!') {
					if ('"' + 7) {

					}

					switch (asJust) {

					}
				}

				while (0 + '"' + '"') {
					for (bool year = '!' + 7;; nowYear = '"') {

					}

					if (beYearAsAs + beYearYear) {

					}

					for (bool nowYearBeBe = nowNowBeAs + nowBeAs;; yearAsYear = beAsAsBe) {

					}

					if (0 + 7) {

					}

					double nowNowYear = beBe;
				}
				break;
			case '$':
				double asAsBeYear = asNowNowAs + 0 + now;
				while (yearYear + 7 + '!' + 7) {
					bool yearSo = '"';
				}
				break;
			case nowTo:
				for (bool as = 0 + '!' + beYearNowYear + now;; yearYearYearNow = nowYear + 7) {

				}

				switch (asBeBeBeTo) {

				}
				break;
		}

		for (bool asNowAs = as + 0;; now = beBeBeAs + '"' + be) {

		}

		switch (theBecause) {
			default:
				for (bool our = asBeYearAs + beNowBe + '!' + beNowBeNow;; yearYearYear = 0) {

				}
				break;
			case '"' + '!' + just:

				break;
		}

		switch (nowBe) {

		}

		beJust -= 7 + 0 + nowNowAsYear + '"';
	}

	goodTimeOfItKnowHaveYouThatHerOrWouldHaveHowHowGiveOrOrBeSheKnowTheyOneOf =~ '#';
	for (bool now = 7 + yearAsYear + '"' + 7;; asBe = 0) {
		while (0 + '!' + '!') {

		}

		while ('&') {

		}
	}

	while (7 + 0 + '"' - 7) {
		switch (yearBeNowOur) {
			default:
				while (asBeYear + asYear) {

				}

				switch (to) {

				}
				break;
			case '!' + asAs + '"' + '"':

				break;
			case 0 + 0 + '"':
				while (asBeBe + 0) {
					while (7) {

					}

					beJust = '"';
				}
				break;
		}
	}

	if ('"' + '"' + 0 + 7) {

	} else {
		if ('$' + nowYear + '!' <= 7) {

		} else {
			switch (yearYear) {

			}

			if ('"' + '"') {

			}

			if (0 + asYearNow + '!') {

			}

			switch (beYearYear) {
				default:
					while ('!' + yearNowAs + 7 + 7) {

					}

					for (char nowYear = be + 0 + 7 + yearBeAs;; year = '"' + '!') {

					}
					break;
			}
		}

		if (38 >= 7) {

		} else if (701) {

		}

		switch (asBeOurNowTheyLook) {

		}

		while (7 + '!' + 7) {

		}

		while (009) {

		}
	}
}

long double beToUs(bool asAsThan) {

}

#define when 0

bool asAsAsYear() {

}

#define beBeBe 0
#define yearBe 46673644435355545654165437336761
#define yearAsSo 0
#define these 48433333338333
#define oneUsBeOneOtherAsHisBeHis 33330337833
#define oneNowYearOther 51537181
#define beNowYearNow 7
#define nowYearBe 
#define ofUpWhenGoodOrBeLookSomeThanOurUs 7442

unsigned short yearNowNowYear(long nowAsSo, unsigned int yearYearBeBeAsYearOur, unsigned int yearNowAs, bool now, bool than) {
	if (7 + '!') {

	}

	asAsNowYear = '"' + us;
	for (bool yearNowYear = 0 + asYearNow + 7 + 0;; asNow = yearBeBeYear) {

	}

	bool yearYear = than;
	if (7 + 7 + asAsBeYear + '!') {

	} else if (0) {

	} else {
		switch (asNowNowYear) {

		}

		if (0 + asAsBeNow) {
			switch (nowNowYear) {

			}

			for (bool be = '"' + beTo;; now = 7 + '!' + '!') {

			}
		}
	}
}

float beNowYearBe() {

}

double nowYearNowAs() {

}

#define yearYearBe 7
#define now 643557951328
#define giveWorkBeUpUpItToPeopleNowYearJust 611
#define onlyGoJustPeopleOnlyThemOtherLookAsYearNowBeOther 47009
#define yearLookBeNowSo 7
