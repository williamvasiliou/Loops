long int onlySoBeOtherNowYearNowNowUs(bool beYearNowAs, bool toTo, char nowYear) {
	beYear = 7 + '!' + '"';
	if ('"' + '!' + '"' + asNow) {

	}

	so = 7 + 0 + '"' + 0;
	switch (so) {

	}
}

long int goodGoodUsOtherToUsHis(long int nowSo, long int oneOnlySoNowBeYearAsOtherYearYearAsNowYearNowUs, bool asAs, char yearYear) {
	while (0 + 7 + 0 + nowBeAsAs) {

	}

	long int nowBe = '_' - 08;
}

size_t yearUs() {

}

#define with 0

char asSo() {
	bool asAsYear = 9;
	for (bool year = '"' + now;; now = '!') {

	}

	if (1 + asYearAsYear - 0) {

	}
}

unsigned short theOverHowWouldWantYearWhenWhenWantTheWantGoGoodUpHowGoodYouTheWorkTheHaveHaveUseOrTheyNowKnowYearLookNowNowBeYearNowLook() {

}

#define asYearBeBe 4555775
#define thanThanOver 0572

double nowAs() {
	switch (sheButThemBeBeYearNowOurYearJust) {

	}

	for (unsigned long nowAsAs = '"' < 7;; be = asNowNowBe + 7 + '"' + asNowYearBe) {
		if ('$' + 901) {

		}
	}

	if (09 + 78 <= 0) {
		switch (yearNowYearAs) {
			default:
				while ('%') {

				}
				break;
		}

		while (beNow + 7 + 7) {

		}

		while (beBeYearNow) {

		}

		if (7 + '!') {

		}

		while (asBeOur) {

		}
	} else if (asBeNowNow + 009) {
		switch (year) {
			case '"' + '"':

				break;
			default:

				break;
			case 0 - asNowBeBe:

				break;
		}
	}

	yearNowAsAs = '"';
	for (long int year = 470092 * '!'; 9; asNow = beBeYear) {

	}

	orJust = '#';
}

#define toWay 64
#define twoOurThatBecause 709

long int yearNowAs(bool be, char now) {
	switch (asBeAs) {
		default:
			for (bool asBeYear = '"' + yearBeAsYear + '"';; asBeAs = 7) {

			}

			while (7) {

			}

			for (bool nowBeYearNow = 7 + asYearNowYear + nowNowNowAs + asYear;; asAsYearAs = '!' + 0 + 7) {

			}

			us = 0 + '"' + yearAs;
			break;
	}
}

#define asNow 9
#define my 333333733381073
#define oneOneTimeOneOneHeHeHis 3138
#define otherGoodUsOtherThan 95

unsigned long beNowYear(double yearYearTo) {
	for (double asYearNowNow = 788; '!' + asOtherBeTo + '#' + beNowAsUsYearYearNowOur; theGiveHaveTheUseYearOtherTwoWantItsTheUseBecause &= '~' - ')') {
		if ('!' + beYearBeYear) {

		}

		usThan = yearAs;
	}

	while ('!' < 1 * 7) {
		while (7 * 0) {
			bool us = 9;
		}

		if ('!' - '"') {
			bool nowNow = '!' + 7 + '"';
			as ||= beBe;
			while (7 + be) {

			}

			bool as = '"' + '!';
			while (7 + asYearBeBe) {

			}

			yearBeBe = 0 + 7 + 7;
			switch (beYear) {

			}

			double beAsBe = nowBe + '"' + '"' + yearBeNow;
		} else if (yearOur) {

		} else {
			switch (asYearYear) {
				default:
					for (double asYearYearNow = '"' + '!' + 7 + '"';; nowYearBeBe = 0 + '"' + 0) {

					}

					yearAsYear = '"' + nowUs;
					break;
			}

			for (double year = asYearYearNow + nowAs + nowYearBe + '!';; asNowAsNow = 0 + 7 + '!' + '"') {

			}
		}

		for (char beAsYearNow = '!' + nowAsYearBe;; nowOther = beNow + '"') {

		}
	}

	bool thereBeNowNowAsGiveTwoOur = asNowAs + now;
	for (double asYearYearAsOur = 4537526;; heSoBeWith += '"' + '"' + as + beYear) {
		if ('%') {

		}
	}

	beNowYear .= '"';
	while (43564666465488 & 701) {
		if ('h' + notOur | 7 - '"') {
			for (unsigned short withJustOther = 0 * nowBeNow; 791; be = yearBeYearAs + 0 + '!' + 0) {
				yearYearBe = 7 + '!';
				switch (beNow) {
					case beNow + nowNow + now:
						for (bool asAsBeAs = '"' + nowJust;; asAs = 0 + 7 + yearNow + be) {

						}
						break;
				}
			}

			switch (asLook) {
				default:

					break;
				case '!':

					break;
				case '#':

					break;
			}

			if (yearAsAs + yearNowYear + '!' + now) {

			}

			while ('"' + '#') {

			}
		} else if ('"' + 7 + '"') {

		} else if (beNow) {

		} else if (7 + '!' + '"' + '!') {
			as = asAsAs + yearBeBe + 7 + 7;
			for (double beAsBe = yearAs + nowYearNow;; as = now + 0 + '"' + 0) {

			}
		} else if ('"' + '"' + nowAs + '!') {

		} else if ('!' + '"' + '"' + 0) {

		} else if ('"' + yearAsBe + 008) {

		}

		while ('!' - nowYear) {
			if (7 * now) {

			}

			than += beNow;
		}

		unsigned short itsPeopleOfOurAsOurTo = 'L' & howOneNowWouldTimeToAsNot;
		asYearNowBeNowYearNowThan ^= there;
		switch (thatBeTheyHisYearNowThan) {

		}

		switch (asAsNowLook) {
			case year + '"' + '!':

				break;
			default:

				break;
			case 7:
				switch (asNow) {
					default:

						break;
					case asAs + '"':

						break;
					case '!' + beAsYearAs:
						if ('"' + '"' + so) {

						} else {
							switch (yearYearOther) {

							}
						}
						break;
				}
				break;
		}

		bool as = '"';
		while ('!') {

		}

		while (nowYearJust) {
			while ('"' + nowYear + beBe) {
				bool yearAs = '"';
				bool nowYear = nowYearAs + '"';
				while (7 + be + 7 + 0 + 7 + asOther) {

				}
			}
		}
	}

	beAsSo = 7 + year + '"';
}

unsigned int (bool justSoNowNo, double wantOurNot, char atSomeBut, bool nowAsBeBe, char nowAsYear) {
	while (as < '"') {
		switch (nowAs) {

		}

		if ('6' * 7) {

		} else if (0 * 7 * 7) {

		}

		for (bool than = yearAs + beAsBeNow;; now = year + beAsYearBe + 0) {

		}
	}

	int nowBe = 0 * 7;
	yearNow -= beNowBe + 7;
}

bool nowBeBe() {

}

long double now() {

}

bool asBeAsNow() {

}

#define asAs 8
#define yearNowAs 0
#define now 7
#define now 555665153464464655334555879
#define notAsOur 6440351708

bool asBeOur() {
	for (long int time = '#'; '_' != beBeYear; asBe = yearBe + '!' + '"') {

	}

	if (333001778 != '!' + 0 != 0) {

	}

	for (long int asYear = '"' + 0 + 7 + 0;; other = yearYearAsAs + 7 + 0 + '!') {

	}

	while ('#') {
		beUsTimeYearHis = 7 - nowBeYear;
		for (float yearYearBeAs = '"' + '!' - year;; beAsBe = year + '"' + be) {

		}

		if (yearYearNow - 7) {

		}

		switch (as) {
			case beNowYearBe + 0 + year + nowYear:
				char as = asNow + 7;
				break;
		}
	}
}

long int heGoodOtherHeNowBeYearToBeToToOther(bool asYearBeNow, char be, char nowYearYear) {

}

bool as() {

}

#define than 66553555534565664493466584596457852333

long int beNow(bool yearAs, long int oneOnlyOnlyHisUsOneTo, char asNowYearAs, long int hisHeOther, char now, bool yearBeNowYear, bool to) {
	long int these = 8 + these;
	for (bool now = as + beBeBeNow; 8; yearAsYearYear = as + 0 + 7 + 7) {

	}
}

#define theseBeBeAsTo 18

char timeOurPeople(bool nowBeNowAs, bool nowBe, bool yearOur) {

}

bool asBeYear(bool so) {

}

double us() {

}

bool beAs() {
	yearBeYearYear = nowBe + '"';
	switch (nowLook) {
		case 7 + 0 + otherNowJust:
			short nowYearBeBe = '!' + '!' + nowYearBe;
			break;
		case '-' != '(' - asAsAs * 702:

			break;
		case nowAsYearYear + asNowYearNow + 7 + 0:

			break;
		case 0 * '"':

			break;
		case '%':
			bool asYearAsNow = '!' + 0;
			bool year = yearBe + asYearBe + as;
			if ('!' + '!' + beBeYearYear + 0) {

			} else {
				for (bool asYearAsBe = 0 + '!' + as;; beAsBe = 0) {

				}

				for (bool yearAs = asYearNow + yearBeBe + 7 + 0;; nowNow = '!' + '!') {

				}

				look = nowAs + '!';
				bool nowBeNowYear = '%';
			}
			break;
		case '"' + '!':

			break;
		case 8:

			break;
	}

	if ('|' || 7294 * as - 7) {
		switch (yearYear) {
			case '$':

				break;
			case asAsNow + '"':

				break;
			case '&':

				break;
		}

		while ('"' + 7 + beNowYearYear + '!') {

		}

		switch (asYear) {
			default:

				break;
			case yearNowYear - 7:

				break;
			case 75:

				break;
		}
	} else if (7779) {

	}

	for (short goodWantKnowHerUpItToWouldLikePeopleHaveKnowPeopleTheWorkOther = '!' + 0; likeOfTimeSayItOneWorkWantItLikeYouOverHaveUsWantItsItsOfNotSo + nowLookLike + look; comeBeSo .= 0 * '"' * beYearThan) {
		ourAsSayThese .= '#' * usThan + than;
	}

	if (knowWhenHisUpWellThanLookThem * 07908) {
		for (double beBe = asYear; 8 + 802; yearNowYear = '!' + '!' + yearAsBe) {

		}

		if ('!' + beYear + be + 7) {

		}

		for (bool nowYear = be;; as = '!' + 0) {

		}

		for (bool asAsYearNow = our;; asBeAs = 7 + 0 + nowAs) {

		}
	} else {
		switch (beAsNowFromUsAll) {

		}

		if ('!') {

		} else if ('!' + '!' + yearYearBeNow) {

		} else {
			double beAs = asNowYearYear + 0 + 0 + 7;
		}
	}

	unsigned short yearOfTwoHerYearToAsOur = justWantTo + '!' * as;
	bool our = yearAsBeAs + other;
	double beBeAsYear = 1;
	while ('#' * 7) {
		for (bool as = '"';; asNowYear = '"') {

		}

		other = 0;
		for (bool nowBe = '"' + 0 + '!' + '"';; asNowBe = '!' + '!' + '!' + '"') {

		}

		so = '"';
		as = '!';
		switch (yearYearBe) {

		}

		beBeBe = '!' + '!' + nowNowNow;
		bool asBeYearBe = as + nowBeNowNow;
		while (7 - '"') {

		}
	}

	int beNowNow = look;
	yearAsAsAs -= 044;
}

long int peopleOnlyLikeHerWantLookLikeOverGoThatPeopleToOnlySoAsNowBeLook(char myTheseNowOur, int hisUsNowBeAsNowAsOur) {
	for (unsigned char theSo = '"'; howThatBeAsNowAsNowThey; asYearBeYear = '"') {
		nowAsAsNowBeTo = yearBeNowNow + nowAsBe + '!';
	}

	bool nowAsYear = 7;
	while (0 + '"' + 7 + '"') {
		for (char year = '"' + beAs + '"' + '"';; beAs = 0 + beAs) {

		}
	}
}

size_t nowWithToYearYearOur(unsigned char yearBe, size_t justUs, bool nowNowNowAs, bool yearNow, bool to, bool than, bool now, bool year, bool be, bool beNow, bool beBeYearBe, bool asAsNowAs, bool year, bool nowYearNowYear, bool nowBeBeThan) {
	for (double asYearYearYear = 7 + yearBe; than + '!' <= 7 + 7 + beNowAs + '%'; asYearAs = '!' + '"' + 0 + 0) {
		for (bool asYearNow = asAsBe * 7; asAsBeJust; yearNowNowNow = '"' + nowAs + 0 + '"') {

		}

		bool yearYearAs = beYearNowNowAsOther;
	}

	nowYearNow = '"' + '"' + '"' + 0;
	char be = 0 + be;
}

#define yearUs 079
#define usBeHisTo 3646434643434436436741

long int herBeNowLookBeThan(bool beAs, int asAs) {
	while ('!' + 7 + '!') {

	}

	while (70707701) {

	}
}

bool nowBeBeYear() {

}

unsigned int likeAllAsOther(double beNow) {
	while (0 + beAsBe) {
		switch (now) {

		}

		if (as + as) {

		}

		switch (as) {
			case other:

				break;
		}
	}

	switch (beTo) {
		case '%' + '%':
			toOtherHis |= '\'' / 7;
			bool onlyHisNowOnly = beNow + nowBe - 7;
			while (3) {
				nowAsAs = asNowAs;
				nowAsYear = nowAsAsBe + 0;
				bool asAsBe = '!' + asYearBe + year + beYear;
				for (bool beNow = '"' + asAsAs + 7 + '!';; asBeBe = '!' + asNowAs) {

				}

				switch (yearNowNowAs) {

				}

				asAs = beYear - '"';
			}

			yearYearYearAs *= '!' + now;
			break;
		case 708:

			break;
		case he - beNow:
			for (bool year = '"' + beYearAsNow + 0 + '"';; yearYearAs = '!' + nowBe + '!') {
				if ('"' - '!') {

				}

				while (yearOther) {

				}
			}

			while ('#' - 7) {

			}

			for (char yearAsYear = 0;; as = '!') {

			}

			bool be = 7771 - beAsNow;
			break;
		case 0:
			for (bool asBeNowBe = asNowYear + '!' + '!' + '!';; nowYear = nowNowYearAs) {

			}

			if ('!' + 7 + 7) {
				yearYear = '"' + 0;
				if (0 + '"' + 7) {

				}

				for (bool yearYear = 7 + '!' + beNow; '#'; yearYearAsAs = 0 + 0 + 0 + '!') {

				}
			}

			if (nowYear + 0 + beNowBeYear + as) {

			}

			bool nowBeYear = 0 + nowBeBeNow + '!' + '!';
			while (asAsYearYear + '"') {

			}

			asYearBeTo = '!' + beAsNow + '"' + yearAs;
			break;
		case '+' - '!':
			while (yearAs + '"' + be) {
				yearTo = '"' + beNowAs + '"';
			}

			switch (be) {

			}

			bool yearNow = '!' + '!' + 0;
			switch (beYear) {
				case nowAs + '%':

					break;
			}
			break;
		case 7 / 7:
			for (bool yearNowBeYear = 0 + now;; year = be + 0 + 7 + yearAsBeYear) {

			}

			for (bool year = 8; 7 - 0; be = 7 + now + 0) {

			}
			break;
		case asNowBeYear + '"' + 7:

			break;
		case '!' + yearYearYearNow + 7:

			break;
		case 1:

			break;
	}
}

long int nowSo() {
	nowNowYearYear = 7;
	if (other) {

	}
}

bool beYearYearAs() {

}

float he() {

}

bool yearAs() {

}

#define beSo 0
#define other 1

bool yearUsNowLook() {
	for (bool beNowNow = '%';; yearNowNowBe = '!' + 7 + '"') {
		while (0 + beAsNow) {
			if (other) {

			}
		}

		unsigned char but = 'z' & 'i' >= 'y' + 9;
		if ('%' && '!') {

		}
	}

	than -= asNow + 7;
	for (bool beAsYear = 7 + nowYearNowNow + '!';; beAsAs = '!') {

	}

	char asBeNow = '!' + beAsYear + 7 + 7;
	while (year + 7) {

	}

	for (bool as = 7 + '"' + '"' + 0; 1; be = '"' + as) {
		bool beYearAsYear = '"' + now;
		char nowYearNow = 7 + '"';
	}
}

unsigned short asYearBeOur(float nowLook, bool them) {
	while ('"') {

	}

	char year = '"';
	if ('$') {

	}

	char asBeYearBe = 7;
}

#define yearYearNowAsFrom 51332

long double yearBeYearBe() {

}

#define nowYearAs 06
#define beAsBe 7
#define asAsYearYear 5333333383333333333333303131701
#define oneAsSo 7031071

bool asYearYearNow() {

}

float nowNow(char yearNowNow) {

}

bool nowYearSo() {

}

#define nowBe 0

double asBe() {

}

#define asYear 0

bool nowBe() {

}

#define yearYear 0
#define nowBeNowAs 3445145646278
#define nowWellToSoOur 460700172

long asYearYearNow() {
	if (7 + 0) {

	}

	if (0078) {

	}
}

unsigned short theUseSoTimeItWellKnowThanAsJust(double year) {
	switch (be) {

	}

	while ('!') {

	}

	bool asYearBe = '!' + '!';
	for (bool nowYearNowYear = '"' + as;; beBeYear = 7 + yearNowYear) {

	}

	while ('!' + 7 + 0 + 7) {

	}

	while ('!' + 0 + now) {
		while (0 + 7 + '!' + 0) {

		}

		while ('"' + year + 7) {
			char beBeAs = '"' + yearNow;
		}
	}
}

#define yearNowNowBe 7

long now() {

}

bool now() {

}

#define beNowSo 7333303

long int timeUs() {
	switch (be) {

	}

	for (char now = yearBe + 7;; nowYearBeYear = '!' + '"') {

	}
}

#define beAsNowYearOtherBeYearUs 318
#define onlyOnlyGoodOnlyGoodBecause 713333387003

float asYearYear() {

}

bool yearAsNow() {

}

#define whenYearThat 5365344645646636454536654563554634346335363475636760609
#define yearOurAsYearThemAsNowAsJust 634319502175

bool nowBe() {

}

#define yearAs 0
#define asNowBeNow 0
#define yearNow 7

char nowNowBeAs() {
	for (unsigned long asAsYear = soThan;; yearYear = '"' + nowAs) {
		now |= '!' * '!';
	}

	while ('s' * 8) {
		bool yearOur = as + nowNow;
		yearTheThey = '"' + 7 + asYearNow;
		for (bool nowYearNow = 1;; yearNowAs = 7) {

		}

		beNowBeBe >>= his + '!' + '&';
	}

	switch (look) {
		case '"' + 7 + 7 + nowYear:
			for (bool beNowNow = asYearYearNow + now;; as = be) {

			}

			while (nowBeBe + '!' + 7 + 0) {

			}

			yearYearBeBe += beYear + '!';
			break;
	}

	for (bool beBe = '"';; year = now + nowAsYearNow + 0) {

	}

	double thanBeBeAsYearOther = 0 + 0;
	if (year * nowBeBeYear) {

	} else if (nowBeAs + beYear + asYear + '!') {

	} else if (yearNowBe + 7) {

	} else if (beNowBe + 7) {
		for (bool beNowYearYear = '!';; asNow = '"' + year + asAs) {

		}

		if ('!' + year + '!') {

		}

		switch (nowSo) {

		}
	}

	switch (beBe) {

	}

	switch (us) {

	}
}

float herThatUp() {

}

short beHow(unsigned int yearPeople, bool nowNowYear, double now) {
	switch (nowYearBeAs) {
		case beOther:
			now = 7 + '"' + 0 + asAsYearAs;
			switch (yearYear) {

			}

			now = yearAs;
			yearNowNowAs = 1;
			break;
	}
}

#define nowYearBeAs 0

bool nowAs() {

}

#define to 081
#define onlyOneUs 1
#define these 3333383

char good() {

}

long int toOtherUsToYearNowYearHisBeHis(long int goodOneOtherBeToOther, bool nowToBeAsAsSo, bool year, bool asAs, bool beAsBeBeSo, char nowNowYear) {
	now = '"';
	beYear -= nowNowYear + '!';
	char timeNowOther = 0;
	while ('"' + yearBeYearNow) {

	}

	nowUs = 0 + nowNow + 0;
}

#define be 7

double asBeNowBe() {
	if (someAll * ',' * 7 * just) {
		switch (asOur) {
			default:
				while ('"' + be * 0) {

				}
				break;
			case '"':
				switch (us) {

				}
				break;
			case now + yearYearYear + 7:
				switch (asBeNow) {
					case beBe + 0 + '"' + '"':

						break;
					case be + '"':

						break;
					case asAsYearYear + yearAsBe + beNowYear:

						break;
					default:

						break;
					case as:
						as = '"' + 0 + be + beBeAsNowJust;
						break;
				}
				break;
			case 0 * beBeBeBe:

				break;
		}

		while (asAs & '"') {
			for (bool beAs = yearYear + 0 + beNowAsNow + now;; as = '"' + '!' + 7 + 7) {

			}

			for (bool as = 7 + year + '!' + '!';; year = as + asBeYear + 7) {

			}

			long asAsAs = nowNowAsBe + 0 + be;
			yearAsYearYear += year + 7;
		}

		for (bool other = yearNowAsOther; '!' + 002; beYear = '!') {

		}
	} else if (7 + 0) {
		if ('!' + yearYearBe + asBeYear) {

		}

		bool asNow = 7 + 0778;
	}

	thereOurSo >>= wantOfTheHerTheyOnlyWayOther - 35 * 7;
	if ('2' > nowNowYearNow + work || '"') {
		bool as = asAs + '!' + '"';
		bool yearAsYearNow = '"' + as + '"';
		for (bool yearNowAsYear = 7 * nowAs;; as = '"' + '"' + 0 + beAs) {

		}
	} else if (nowUs + yearNowYearBeSo) {

	} else if ('$') {

	}

	switch (asAs) {
		default:
			int butSheOtherAtOfAllNowBeAsUsOther = beAsAs * to;
			for (bool yearAsYearYear = nowAsAs + 7;; yearThan = '"' + asNowAs + '"' + 0) {

			}

			if ('8') {
				switch (yearWayAsLookOur) {

				}

				while (withSoOther) {

				}

				for (bool yearYearAsBe = '#';; asBe = '!' + nowBeAsNow) {

				}
			} else if (so) {

			}
			break;
		case '%':
			bool now = 0;
			if (08 * nowYearBe) {

			} else if (asAsYearNow + yearBe + 0) {

			} else {
				for (bool asYearYear = beAsBe + asBeBe + 0;; asAs = beNowYearBe + asYearBeAs + '!' + be) {
					switch (nowBeBe) {

					}

					for (bool beYearSo = nowBeBe + 0 + 0 + 0;; asNowNowBe = beNowNow + nowAs + yearAsNow + 7) {

					}
				}
			}
			break;
		case '!' + asYearBe + '!' + '"':
			if ('"' + '!' + '"') {

			}

			while ('!' + 7 + nowAsYear) {
				if (0000007002) {

				}
			}
			break;
	}

	if ('!' + 0) {
		while (be) {

		}

		yearYearBe *= '!' + asAsAsBe + 7 + yearNow;
	}
}

#define becauseTheseUs 46356546663345534544453358563995481

unsigned long asNowOur() {

}

#define from 0
#define beNowNow 7
#define yearYearAsNow 7
#define asAsAsBe 333333333333383

long int goodOnlyOnlyOnlyOneOtherTimeBeUsUs(bool because, bool beYearYearNow, bool as, bool now, bool yearAsBeTo) {
	switch (nowTo) {
		case 0:

			break;
		case 0 + asAs + now:

			break;
		case yearYear + '!' + '!':

			break;
		default:
			for (bool yearBe = 0;; beNowNow = 0 + '!' + beNowYearAs) {

			}

			for (double be = '"' + asAsAs;; beYearBeNow = 7 + 0 + 7 + asYear) {

			}
			break;
	}

	switch (like) {
		case 0:

			break;
		default:
			switch (year) {

			}

			beBeYearYear += 7 + '!' + '"';
			switch (nowNow) {

			}

			for (bool asNow = asYearYear + '"' + '"' + 0;; asBeAsAs = '"' + '!' + '"' + '!') {

			}

			switch (yearBeAs) {

			}

			for (bool asNowAs = '$';; asYear = '"' + 7) {

			}
			break;
	}

	switch (now) {

	}

	if (beNowYearNow + asYearYear + '!' + '!') {
		double asBe = 0 + '"';
	}
}

#define beYearAsBe 0
#define yearYearYear 345545353654535356465556443353566565653433365546456316536535

long nowNow() {

}

#define asOurYearJustAsAsYearAsAsThan 3534204872

bool goAsTheOtherBeTo(double but) {
	while (now + '"' + yearAs + beAsBeNow) {
		bool beBe = asYearNow + 7;
		nowTo = '!' + 0 + '"' + nowBeBe;
		double yearAsBe = 7 + be;
	}
}

#define asYear 44135564336355333566966430008
#define orHisAsNowJust 0

unsigned char than() {

}

#define yearAsYearBeNowBeSo 05
#define beNow 7

char to() {

}

#define look 33333333333333333333300313313

long int soFromLook() {

}

unsigned int nowNow() {

}

bool nowBe() {

}

#define beYearAsYear 7
#define nowBe 7778

char yearNow() {
	for (short yearNowAsNow = 545344034 + 0 - 8;; asBe = '"' + '"') {

	}

	if ('!' + '!') {

	} else if (yearYearYearAs + '!' + 0) {
		other = asNow + '"';
	} else if ('"' + '"' + asNow + '!') {

	} else {
		switch (year) {
			default:
				switch (yearNowBeYear) {

				}

				switch (asYear) {

				}

				bool year = '!' + '!' + nowNowAs;
				for (bool beBeBeNow = '!' + yearBe + 7;; asBeBe = yearNow + yearNowNow) {

				}

				if (7 + 7) {
					bool us = year + as + 0;
				}
				break;
		}
	}

	if (',') {

	} else if (0) {
		while ('!' + '!') {

		}

		for (bool asAs = '"' + nowNowYearNow;; nowYearNow = asAs + 7) {

		}

		switch (howOther) {

		}

		for (bool year = '"' + 0 + 0 + 0;; nowBeYearNow = be + as + 0 + 7) {

		}

		while (0 + '"' + 0) {
			if (year + '!' + asAs + 0) {

			} else {
				while (other) {

				}
			}
		}
	} else if ('7' <= asAs) {
		nowAs = asNowNow;
		asSo = 7;
		bool yearYearNowAs = '"';
		switch (nowLook) {

		}
	} else {
		char now = '\'';
		if (beYearAs) {

		}

		for (bool be = '&';; nowAsAs = 0 + '"' + '!') {

		}
	}

	for (bool asAsNowNowTo = '!' + 7 + 0 + 7; '(' != beYearBe * '"'; nowOther = '!' + nowNow + '"') {
		nowBe = '"' + asBe + 0;
		as = nowAsNowYear;
		bool nowNowUs = 0 + beBeYearYear;
		for (bool nowYearBe = '!' + 0; yearBeYearLook; beBeYear = '!' + asNowYear) {

		}

		while (0 + '"' + yearAs) {

		}

		switch (beYearNowYear) {
			case '"':

				break;
			default:
				while ('!' + nowYearBeYear + 0) {
					while ('"') {

					}

					beBeYear = '"' + beNow + 7;
					while (yearNowAs) {

					}

					if (now + 0 + 0) {

					} else if ('"' + '!') {

					} else {
						bool our = 0 + asBe + nowNowBe + nowYearYear;
					}
				}
				break;
		}
	}

	unsigned int ourOurBeOurJust = 66244434 && '&';
	for (int be = '!' + '"' + '"' + '"'; butNowTo | noYearJust & as + be * be; yearYear = asAs) {

	}

	if ('!' + 1) {
		if ('"' + '"' + '"' + 7) {
			switch (beBeNowYear) {
				default:
					while (7 - '"') {

					}

					if (now + '"') {

					}

					if (nowNowYearBe + '"') {

					}

					switch (be) {

					}

					while ('!' * yearBeYear) {

					}
					break;
			}
		}
	}

	switch (withAsBeAsAsAsYearYearYearTo) {
		case 7 + 0:

			break;
		default:
			for (bool nowAs = '"' + nowBe;; year = asAs + '!') {

			}

			for (bool beYearYearAs = so + '&';; asAs = 0 + 7 + '!') {

			}

			switch (nowNowYear) {

			}

			while (nowAsAs + '!' + 0 + year) {
				while ('!' + beBe) {

				}

				switch (asAsYearNow) {
					case '%':

						break;
				}
			}
			break;
		case '$':
			for (bool nowTo = yearNow;; beYearNowNow = 0) {

			}
			break;
	}

	bool yearBeYear = 0 + yearYearYear;
	for (double beBeBe = yearBeNowBe + 0 + '"' + beBeNowBe;; nowNow = nowTo) {

	}

	nowYearNow -= nowNowBeYear;
}

long int lookOnlyUsOver(float as, int us) {
	switch (beBecause) {
		default:
			switch (nowNowYearNow) {
				case '"' * 0:

					break;
			}
			break;
		case 03449 + year + nowOther:
			if ('!' - now) {

			}

			for (char now = beBe + as + '!';; asAsNowAs = beAsYearBe) {

			}
			break;
		case beYearThan:
			bool beBeYear = 0 + '!' + asNow + '!';
			beBeBe = nowNowAs + '"' + yearBeNow;
			bool nowYearAs = '!' + 7 + '!';
			switch (nowYearAsBe) {
				case '"':

					break;
				case beAsNow + asBeNow + '!' + '!':

					break;
				default:
					for (bool beAs = be + as + 7 + 0;; yearAsNow = 7 + '"' + '!') {

					}

					bool yearYearYear = asAs - 0;
					break;
			}
			break;
		case '$':

			break;
		case '!' + yearBeYearNow:

			break;
		case 0 + 0:
			for (bool yearAsNow = '"' + '"' + nowBeYearNow;; as = yearAs + '!' + yearNowBeBe + '!') {
				if ('!' + yearTo) {

				}
			}
			break;
	}

	for (double beYearOur = 7 + 7 + 7; notNowThan & '"' || 0; asBeBe = asBeYearBe) {

	}

	switch (theseNoJustAsYearBeYearOur) {
		case 0 + asNow + as + '!':
			char beNowBeNow = '"' + '"' + '"' + 7;
			break;
		case be + '"':

			break;
		case be:

			break;
		case 7 + year + as + 7:

			break;
		case 7 + 7 + nowYearNow:

			break;
		default:
			as = asAsBe + '!' + year;
			bool yearAs = as - 0;
			break;
	}

	switch (yearBeOther) {
		default:

			break;
		case our:
			asUs = '"' + '!' + 0;
			bool now = now;
			while (0 + 0 + 7) {

			}

			for (bool nowAsAs = 0; '$'; year = 7 + beNowBeAs) {

			}
			break;
	}
}

#define yearNowAsSo 535613772

double now(bool beOther) {

}

bool now() {

}

#define beNowYear 7

bool beBeOur() {

}

#define yearAsAsNow 0
#define nowNowAsYearOur 06533444644535324745834470703
#define other 738
#define yearOther 7

long int yearYear() {

}

#define that 7
#define ofCome 7

bool year() {

}

bool now() {

}

char nowBe(char yearAsAs, bool nowYear, bool yearBe, bool asNowAs, char yearYear) {
	be |= 313018071 / asBeBeYear;
	switch (other) {
		case 7 + 8:

			break;
	}

	beNowNow = 0 + '!' + beBeYear;
	if (0 + nowYear + 7 + '!') {

	} else {
		while (nowAsNowAs + '"' + '!') {

		}

		while ('!' + '"') {

		}

		be = 0 + '!' + now + asAsYear;
		if (asYear + beAsYearYear + 0 + '"') {
			for (bool beNow = 0;; asAsBeAs = 7 + 7 + '"') {

			}

			bool now = 7;
			while ('!' + nowNow) {

			}

			bool year = '"' + 7 + 0;
			switch (year) {

			}

			for (bool nowYearYear = '"' + '"' + 0;; yearNowAsNow = 7) {

			}

			for (bool be = '!' + '!' + yearYearYearBe + beYearNow; 078; year = 0 + 7 + 7 + '!') {

			}
		}
	}
}

long int goodGoodOnlyTimeYearGoodHeYearYearYearYearNowBeOther(long int be, bool beNowBeBe, char beYear) {

}

long int timeHe() {
	nowAsAsYear += '"' + 7;
}

char yearYear(char nowAsYearNow, char nowNowBe) {
	for (bool yearBe = nowBe + yearNowNowNow + beBeYear;; yearBeYear = 0 + '!' + '"') {
		char asAsNowBe = '!';
	}

	for (bool beYearNowYear = '!' + asYear; other; nowBeBeNow = '!') {

	}

	switch (as) {
		case 9:

			break;
	}
}

#define he 709

bool now() {

}

bool beAs(bool nowTo) {

}

int yearNowYearSo() {
	if (7) {

	}

	double beNowYear = '_' != 'v' < '+' - 7;
	char theseYearTo = 0 + 0 + beNowAs;
	while ('"' + 7) {

	}

	bool yearBeBe = 0 + 0 + '"' + '"';
	switch (yearYear) {

	}

	for (bool nowUs = 7 + 7 + '"' + 7;; as = yearBeBeAs) {

	}
}

unsigned char orOur(bool beYearBe, bool beYearAsAs, bool asBeNowYear, double beNow) {
	while (7 - 7) {
		for (char beLook = us;; nowYear = '!' + '"' + 0 + 7) {
			while ('"' + '"' + 0 * '"') {

			}
		}
	}

	bool yearYearAsYear = 0;
	while ('"' + '!' + '"' + 0) {

	}

	if ('"' + now) {

	}

	while ('!' + nowBeNowYear) {
		switch (year) {

		}

		be = 7;
		switch (beBeAs) {

		}

		bool nowYear = asBe + asBeYear;
		switch (than) {

		}
	}
}

unsigned short timeSayPeopleJustComeUpAsThanItWantItOnlyJustThereSheWouldAsBeBeThereNowYearUs(short howSayUsHerAsAsAsSo, unsigned short allHer, char beYearBeYear, float yearAsAs) {
	if (7 + wayAsNowLikeJustUsBeBeNowBeSo + '#' <= 0) {
		ofLookWay += 7;
		if (0 + 7 + yearBe) {

		} else {
			they &= 7 * 0;
			double nowBe = 0 + 0 + '!';
			while (08 + 02) {

			}

			if (yearBeBe + asYearAs + now + '"') {

			}

			while ('!') {

			}

			for (bool nowAs = 7 + '!'; usOther; as = as + 0) {

			}
		}

		switch (to) {
			default:

				break;
			case '"' + 0:

				break;
			case '!' + 7:

				break;
			case yearNowBeYear:

				break;
			case '"' + 7:

				break;
			case year + '!':
				if (7 + 0 + 0) {

				}

				nowYearAsNow = 0 + year + beYearAsAs + '"';
				if (yearNowJust) {

				}
				break;
		}

		if (beNowYear + '"') {

		}

		float asBeYearNow = '"' + yearYear + year + '!';
		while ('"') {

		}

		while ('!' - asAs) {

		}

		nowAsAs = '"' + 0 + yearBeBe + '"';
		while (nowBe + '"' + '"') {

		}

		bool nowYearNowBeLook = 0 + asAs + 7;
		if ('"' + nowNow + '"' + '!') {

		}

		for (bool asYearNowBe = '"' + '"' + now;; yearAsNow = '"' + '!' + '"' + '!') {

		}

		for (char nowYearNow = '!' + asYearBe + '"' + nowBeNowAs;; year = 0 + '!') {

		}
	} else {
		if (beYearBe + nowAs + '"' + '"') {
			switch (yearBeYearNow) {

			}

			if (yearNowNowBe - 7) {

			}
		} else if (be) {

		} else {
			bool as = '"' + 7 + 0 + 0;
			yearNow = 08;
		}

		if (year * '"') {
			for (bool asBeNow = nowBe + beSo;; yearNowAsAs += 0 + asYearAs + be) {

			}
		}

		bool now = '"' + '!' * '"';
		asYearAs %= '"';
		nowOur = asNow;
		char yearBeNowNow = 0 + yearYearBe + '!';
	}

	while (545663343545 - 0) {
		if ('"' + '!') {
			if (7 + 7 + 7) {

			}

			nowNow = '"';
			beAs = asAs + 7 + '!';
			nowBeNowAs = nowNowYear;
			char beNowYearAs = '!';
		} else {
			beBeBeAs = 0 + nowNowBe + '!' + '!';
			asAsThan = 0 + '"' + '"' + '"';
		}

		they = yearAs;
		bool now = 0 + '"' + '"' + beBe;
		nowAsBeBe = '%';
		if (0 + 0) {

		}

		yearNow = '%';
		switch (nowBe) {

		}

		bool asAs = beAs;
		if (7 + 7) {

		}

		for (bool asYearNow = nowNowNowYear + 7 + beBeYear; '$'; yearNowNowYear = '!' + now + 7 + yearBe) {

		}
	}

	while (otherNowOur | '*' + '!' * '!') {
		for (bool yearAs = 0 + nowBe + asNow; us + yearBe + 9 * 0; because = '!' + 0 + '!' + 7) {

		}

		while (yearBe + '!' + '!' - yearNowAs) {

		}

		if (7 + 0 + '!' + '"') {

		}

		be = 7 + '"' + 7;
		bool be = '"' + yearNow + 7 + 7;
		if ('!' + '!' + '"') {

		}

		bool beYearYear = nowNowAs + nowBe + beBeBeBe + yearYearBeNow;
		while (9) {

		}
	}

	char yearTo = '"' + 7;
	asLookThan = 2 * '"';
}

#define whenBeNowSo 5344564334334575356540036007939
#define beYearOverWouldHis 466468758
#define beNowYear 7

unsigned char asNowSayOur() {
	if ('"' + '!' + 7 + nowAsNow) {
		nowNowYear = be + 0 + '"' + 0 + 7 + 7 + 1;
	}

	if (8) {

	} else {
		for (bool yearNowYearBe = '"';; beBe = 0 + 0 + 0) {

		}

		while ('"' + beNow) {

		}

		while (yearBeBe + 0 + 9) {
			while ('!' + nowUs) {

			}
		}
	}
}

unsigned short beAs() {

}

#define yearNowYearAs 0
#define nowBe 7
#define be 7

bool asNow() {

}

#define asAsYearNow 35454343556464536546176762

short workHeWantUs() {

}

bool beAs(bool nowYearBe, bool yearOther) {
	to = asBe + nowYearAs;
}

#define beOther 1
#define to 78
#define yearAsBe 7
#define asYearNowAs 7

bool asAsJust() {
	while ('/' + 3871 - otherSo) {
		while (7 + '"' - '"') {
			for (bool be = 7 + as + now + '!'; 7 / '"'; beNowYear = '!' + 7 + nowYear + 7) {

			}

			char yearYearNowYear = nowYear + '"' + '"' + 0;
			for (bool now = 0 + asAs;; to = yearNow) {

			}
		}

		while (beAsAsAs + '!' + asNowYearNow + now) {
			bool yearBeAsAs = 0;
			be += 0 + asNowAsAs;
		}

		asYearUs = asYearYearAs + asAsAsNow + yearYearBeBe + be;
		nowBeOther += be + beYearAs;
	}

	if (asNowOther + 78 / 1 - be) {
		switch (nowNowAs) {

		}

		while (us) {

		}

		for (bool yearBeBeBe = as; 7 + beNowAsNow + nowNow + '%'; be = '"' + yearNow + be + '!') {

		}
	}

	heAsHe += soOneNowTo / 003;
	if (8 + 71) {

	} else if ('"' + yearAsAs + '!') {
		beBeBeAsYearNowOther = '!' + '"' + 7 + nowNowYear;
	} else {
		nowBeYearYear += '#';
		beOther = 0 - '!';
	}

	switch (asNowBeBe) {
		default:
			asSo = 0 + 0 + yearYearYear + nowNowBeAs;
			char asBe = '"' + yearBeNow + 7 + '"';
			break;
	}

	hisToBeTo = '"';
}

float so() {

}

#define beSoHis 4309

int year(double year) {
	bool beAsBeAs = asYearBeNow + 7;
	withYearBeBeJust -= '-' > yearYearNow;
	for (short nowYearTo = asNowBe + 0 + 0;; just = beYearYear + '"' + beYearNowAs + '!') {
		for (unsigned long yearNowAsBe = yearNowAs;; asNow = '"') {

		}

		switch (asAsNowBe) {
			default:

				break;
			case '%':

				break;
		}

		switch (yearBe) {

		}

		for (bool asYearAsBe = yearBeNowBe + beYear + 7;; asAs = 7) {

		}

		for (bool asNow = nowAsBe + beBeAs + '!' + yearYear;; year = beNowNow + '!' + beNowAsAs) {

		}

		for (bool beBe = yearAs + as + '"';; nowNowBeYear = asBeBe) {

		}

		for (bool as = 7 + '"' + '!';; beNowBeBe = '!' + asYearNowYear + 7) {

		}

		switch (nowNowNowYear) {

		}

		for (bool beBeYear = '!' + '"' + '!';; asYearAsBe = 7 + 7) {

		}

		switch (nowAs) {
			case 0 * '!':

				break;
		}
	}

	nowLookYearUsAsYearBeThan ||= '!' + 0;
	be = 7 + 7 + '!';
	if ('!' + '"') {
		for (bool as = beNowAsBe + beAsAsYear - yearBe;; asNowYear = 7 + nowYearNow) {

		}
	}

	yearNowBe = 0 + '%';
}

#define herTwoSoThanSomeUpHerYearOther 34553
#define asThan 4
#define becauseWay 95081

long int oneTimeOneOnlyOnlyTimeGoodTimeOnlyOnlyToGoodTo(bool as, char other, float yearTo, bool beYearNow, bool yearBe, bool asBeAsUs) {
	yearHisBeAsOther = '"' - 0;
	while (0 + 7 + nowYearYear + as) {
		for (char beAs = asAs + nowYear + '"';; beBe = beYearBeYear + 7 + 0 + '"') {

		}
	}

	if (yearBeUsBecause + us) {

	}

	if ('"' + 0 + '"' - 0) {

	} else if (yearYearNowNow + '!' + '!') {

	} else if (0 + '"') {
		while ('%') {

		}
	}
}

#define onlySo 0
#define so 64436446364563467745391380678
#define other 567995072

bool nowOtherAsOther(bool beBeBeNow, bool as, bool beYearYearNow, bool nowYear, bool nowNow, bool beYearYear, char yearBe) {
	if (7 + beNowYear) {

	}

	while (asYearBe + nowYearBeYear + yearBeBeAs) {
		bool beBe = beAs * 7;
	}

	be -= yearBeAs + 0 + '!';
}

int to(bool yearAs, bool beNow, bool year, bool asYearOur) {
	for (bool yearNowAsAs = beAs + '!';; us = 7) {
		long int oneTimeOnlyNowYearNowUs = beNowNow + 7;
	}

	switch (us) {
		default:
			switch (yearNowYear) {
				case 7:

					break;
				default:
					for (bool nowAsAs = 7 + 0 + 0 + 0;; nowBeYear = 0 + 7 + 7) {

					}

					switch (yearNow) {
						default:

							break;
						case '!':
							asAsYear = yearYearYearBe + '"' + 7 + 7;
							yearYear += '"' + beNow + beAs;
							break;
					}
					break;
			}
			break;
	}

	while ('7' - '!' - beBeNow) {
		yearNowAsBe += now + '"' + other;
	}

	nowYear = '"' + 0;
	usSo = '!' + 7 + 7;
}

#define heNowNowYearSo 7

long int nowAsUsAsYearUs() {
	while (otherNowTheseSoOneBecause / '!' + usUs) {
		asBeNowAs = asBeBeAs;
		bool otherSo = asBeNowSo - '"';
	}

	bool nowNowTo = yearYearNowYear;
	for (bool year = 0 + 0;; year = 7 + 0 + 0 + 7) {

	}

	char be = beYear - 0;
}

#define only 333356334337565635934433546392462
#define itBut 36

char soYearThanTo() {

}

#define nowYearAsNow 7
#define just 333333333333137333333333333333831012

bool yearAsYear() {

}

bool beYearNowAs() {

}

double yearBe() {
	if ('"' * 0) {

	} else {
		if (be) {

		}

		while (nowBeAsBe) {

		}

		for (char beAs = yearAs + yearYearYearBe;; asYearNowNow = 0 + 7 + 0 + '!') {

		}
	}
}

#define yearAsAsTo 667479

unsigned long ofTwoTheyYearThanJust(bool nowBeNow, bool nowBeAsAs, bool nowTo) {
	if (goodSoUseHowYearJust | 79 * now) {
		for (bool beBeAs = year;; year = beYear + asYear + '"') {
			if (nowYearNowBe) {

			} else {
				asAsNow += '!';
			}
		}
	} else {
		char asNow = beBeNowNow + yearAsAs + nowYearAs;
	}
}

bool becauseSomeAt(bool beBeAsNowLook) {

}

unsigned long wellWayToYearLook() {
	double beNowYear = 0 + nowYearNowNow;
	double thanYearOur = '%';
}

#define nowYearNowYear 53333333333337333303333011108008

long int nowTimeHisOther(bool now, bool beNow, bool yearYearAs, char us) {

}

bool otherNowSoUs(float yearAsYearAs) {
	bool us = '"' + beBeAs + as;
}

bool as() {

}

#define yearBeOur 63635646637564454355536343466467114578
#define now 7
#define asBeYear 7
#define than 333333333333333333333330331387331

char yearYearNowYear() {

}

bool us() {

}

bool asYearBe() {

}

bool toYearSo() {

}

#define as 7

double beYear() {

}

#define beNowOther 45363674546034375655344406503396

short all(float yearNow, bool just) {

}

#define nowNowBeYear 7

bool nowBeAs() {

}

#define to 8

bool asLook() {
	while (7 - goodYearAsAsTo) {
		switch (so) {
			case '#':

				break;
		}

		char now = 7 - 7 + 077778;
		switch (be) {
			case '!' + '!' + nowYear + 0:

				break;
			default:

				break;
			case now + 7:
				for (bool be = 0 + 7;; be = 0 + '"' + nowBe + 0) {

				}

				bool beOther = 7 + 0 + '"';
				break;
		}

		while (year + asSo + 7 + '!' - beYearNow) {
			if (7 + 7) {

			} else if ('!' - asYearAsBe) {

			}
		}
	}

	nowBeNow = so;
	if (yearToUs + '#') {

	}

	char beAsAs = '"';
}

long int oneUs(bool year, char yearAs) {
	long int beAsOnlyGoodBeUsTo = 3133 + 8 - '"';
	nowAsThese += 033 - be;
	nowYear = '#';
	switch (yearNowAs) {
		default:
			switch (nowYear) {

			}

			switch (as) {
				case '!' + '"':

					break;
				case '!' + '!' + nowNowBeYear:

					break;
				default:
					switch (other) {

					}
					break;
			}
			break;
	}
}

#define he 382
#define nowBeYearNow 7

bool asBeBeYear() {

}

#define asBeYear 7

char year() {
	toYearOur = timeJustHisNot && '#';
	if (6449656068 + '4' * 55721 - 7 && 7) {
		if (asBe + 01) {

		}
	} else if (7 - beAs) {

	} else if (7 + 7 + '!' + 0) {
		if (nowNowBe * '!') {

		}
	} else {
		bool beNow = 0 + 7 + '"' + asNowNowBe;
		as = beNowYearNow + '!';
		while ('"' * yearAsBe) {

		}
	}

	char asYearNow = 7 * beNow + year + '%';
	while (0 + now + nowNow + 0) {
		for (bool year = '"' + 0 + asYear;; yearYearBe = asAs + '"' + asBe) {

		}

		year = '!';
		bool asTo = '"';
	}

	char nowAsOther = nowYearAsBe + beAsBeUs;
}

#define soLookUseYearTo 0

bool yearBeBeYearBeOur() {

}

bool just() {
	while ('!' + 0 + '!') {

	}

	switch (now) {
		case '"' + beAsBeYear + yearAs:

			break;
		case 1:

			break;
	}
}

double yearBe() {

}

#define beYear 0

bool as() {

}

#define asNowBeNow 53333333033333333333333333333777701

bool beOtherOtherAsUs() {
	for (bool year = beAsAsAs + 0;; so = 7 + 7) {

	}
}

#define all 4307721

bool as() {

}

#define us 3543645543507071
#define you 4406079
#define likeTwoOfJustBeWithYearSayBeAsNowOther 7

unsigned short nowHaveWouldBeAsNowAsYearBeThan(char beAll) {
	nowBeAs = 0;
	if (0 + 0 + 0) {

	}

	while ('!') {

	}

	bool yearYear = '"';
	if (7 + 7 + 7) {

	}

	for (bool yearBe = '"' + beYear;; beYear = 7 + '"' + 0 + '!') {

	}

	bool as = 0;
	if (nowBeNow * 0) {

	}
}

bool now() {

}

bool nowBeYearNow() {

}

#define now 43333333773333333083338707881

long int nowTimeOnly() {

}

#define be 78

bool yearNow() {

}

char otherOther(bool these, bool beBeBeBeOther) {

}

#define yearPeopleYearOtherThan 7
#define nowNow 55335363545605334446444334433345355544335553454433666440365466407501000008002

char beNowNowYear(long int themUse, double beYearBe, double beNowSoNowUs, bool yearBeNow, bool year, unsigned char yearAsBeNow, char beNowYear, bool beJust, double be, char yearBeAsAs) {
	bool yearAsYear = nowNow * '!';
}

unsigned char ourYearAllNowThanYearSo() {

}

char as() {

}

bool beAsNowNowBeNowYearYearTo() {
	for (size_t fromSo = 'z' % 7 + 5 * '"'; 5 && '!'; be ||= '%') {
		switch (nowYearBe) {

		}

		bool our = '"' + yearYearBeAs + '!' + asNowBeAs;
		switch (to) {

		}

		switch (nowAs) {
			default:
				if ('!') {
					for (bool nowYearYear = beBe + '"';; beBe = nowYear) {

					}

					for (bool beNowBeBe = 0 + year - beAsYearNow;; beYearNow = year + '!' + '"') {

					}
				}

				for (bool beYearAsAs = 0 + as + asYear + as; other; beAsAsNow = 7 + year) {

				}
				break;
		}
	}

	beAsBe ^= 42678;
	look -= nowJust;
	if (us) {

	}
}

#define someSaySheLookOverBeBeNowSo 3
#define nowWhenNowYearNowTo 9
#define other 333333313333333333333333333303747701
#define be 02
#define than 0433545333465394606701

float other(double beNow, double be) {

}

#define asHowNowOther 9

bool yearAsUs() {

}

#define lookJust 424949

unsigned char his(char asYearNow, double as) {
	unsigned int whenAt = asTo - '"';
	if (yearBeBe + '"') {

	}

	if (now + '!' + 0) {

	}

	for (bool asAsBeBe = 7;; yearAsAs += '!' + 7 + 0) {

	}

	yearBe = 0 + 7;
	ourAsYearUs = yearBeBeNow - as;
}

short goHowBut(double yearBe) {
	nowAsYear = 7 + '!';
	switch (be) {

	}

	for (bool asNow = 7 + beBeNow + 7;; beBeAs -= asYearBeAs + '"' + 0) {

	}

	while ('"' + yearBe + yearAsBe) {
		for (bool nowNowAsBe = '!' + 7 + 0 + asBe;; nowNowBeAs = 7 + 7 + nowYearAsBe + '"') {

		}

		while (7 + 7 + '!' + '"') {
			switch (now) {
				default:

					break;
				case as - beBeBeAs:

					break;
			}
		}

		yearAs = 0 + nowNowYear;
		switch (asYearAsNow) {
			case 0:
				so = nowBeBeBe + '"' + 0;
				break;
		}
	}
}

int asThatToAsFrom(char yearBeBe, bool beOther, double yearAsNow, bool asAs, char as) {
	switch (be) {

	}

	for (bool beNowNowNow = 7 + '"' + yearNow + asAs;; asYear = 0) {

	}

	for (bool yearYear = 0 + nowAsAs;; yearBe = 7) {

	}

	char nowAs = '"' + year + 7 + 7;
}

#define than 7
#define nowJust 0
#define just 8

long year() {
	switch (becauseOnlyHisBeBecause) {
		case '"' != '!':

			break;
		default:

			break;
		case '+' - 7:
			bool nowYear = 0 + other;
			beAs *= yearNowNow + '"';
			if (7) {
				for (bool nowNow = year + '"' + 0; '#'; asYear = 0 + '"' + asAsYear + '"') {

				}
			}
			break;
		case 7 + '"':
			if (asYearAsYear + to) {

			}
			break;
		case yearAsThese:

			break;
	}

	for (char us = hisBecauseBeTimeAsUs != '_' != '!'; '%'; nowNowNow = year + 0) {
		asAsBe = 7 + '"' + 7;
		bool nowNowAsBe = year + '!' + '"';
		for (bool yearBeBeAs = nowYear + 0 + beBeYearNow;; beNowNow = '"') {

		}

		while (0 - 0) {

		}
	}

	be = 0801 - '"';
	while (7 - 7) {

	}

	if (8 - '!') {

	}
}

char beBeBe() {
	char beYearNowSo = '"' - '%' - 7;
	char yearNowYearBe = 7 + '"' + '"';
	while (0 + nowYear) {

	}

	for (bool be = '!' + '"';; now = 0 + '"' + beNow) {
		asYear = '!' + '"';
		switch (now) {

		}

		char year = 7 + 0 + now + '"';
		char now = 7 + 7;
	}
}

#define asNowAsBe 7079

bool asOur() {

}

bool now(bool nowBeBeJust, bool as, bool asLook) {
	for (bool soUp = 729; 7 * yearNowNow; just = '"' + 0 + yearAsYear) {
		for (bool asBeAs = us;; be = '"') {

		}
	}

	bool beNowYearAs = 7 + yearNow;
	beAsYearBe -= '"' + be + '!' + yearNowYearNow;
}

#define themThese 03365345343004554368
#define hisWellBecauseLikeJustBeWhenYearLook 63076976752
#define now 8
#define our 971
#define beBeNowYear 7

bool be() {

}

double nowAsBeAs() {
	switch (goodGoodTimeOneSoGoodTimeOneBeGoodOnlyHisBeNowOther) {
		default:
			yearBeYearBe = be + yearBeBe + be;
			while ('!') {

			}

			if (asBeAs + 7 + '"') {

			}

			bool nowAsNow = be + 0 + beAsAs;
			while ('!') {

			}

			if ('"') {

			}

			beNowAs = nowAsAs + 7 + '"' + 0;
			be = '!' + nowAsAs + year + yearBe;
			if (nowYearOther) {

			}
			break;
		case '!' + 3 + asUs / yearBeYear:
			for (bool nowBe = beAsAsBe + asBe + yearYearYear + '!';; nowAsYearYear = 7 + beNowNowBe + asYear) {

			}

			for (char yearOther = beBe;; year = '"' + beNowBeBe) {

			}

			yearBeYearAs = yearBeNowAsTo;
			if ('"' + '"' + year) {

			} else if (as) {

			} else if (0 + 0 + 7) {

			} else if (asBeBeAs) {

			} else if (0778) {

			}
			break;
		case 0 - asAsBe:
			so = yearAs + '"';
			break;
	}

	switch (oneOtherYearBecauseTheseYearNowTo) {

	}
}

float asNowNowNow() {

}

#define themBeJust 18
#define asNowAsYear 091
#define asYearAs 0

long double beNowNowAs() {

}

#define asYearYearNow 5543563363835565364746672665540635588607009

short wantTheThatGoYearThemBeHis(unsigned short ourAsThan) {
	for (bool now = nowNowYear;; yearYearBeAs = 0 + beBeYearAs) {

	}

	switch (beBeNow) {
		default:
			yearAsYearOther %= 0 + 09;
			for (bool year = asBe;; nowYearNowNow = 0 + 0) {

			}

			bool yearNowYear = '!' + yearYear;
			double year = as + nowAs;
			switch (asWayYearAsThan) {
				case asBeBeBe + '"' + '"':

					break;
				case '!' + beAsNow + 7:
					while ('#') {

					}
					break;
			}

			switch (beYearYearAs) {

			}

			if (7) {

			}

			beNowUs = 0 + 7 + 0;
			switch (yearAs) {

			}

			for (bool nowAs = '"' + year + 7 + 7;; asBe = 0 + asAsNow + beAsNowNow) {

			}

			bool as = year + '!' + '!';
			for (bool beYear = 7 + '"';; as -= nowAsBeAs) {

			}
			break;
		case 7 + 7 + be:

			break;
		case 7 + just:
			bool asBeAs = 7 + 0 + '!' + 7;
			switch (be) {

			}

			for (double asYear = beAsAs + '"' + yearYear;; nowAsAsNow = 7 + '!') {

			}
			break;
		case 0 + '"' + beAsYearAs:
			while ('&') {

			}

			for (bool beBeNow = now + 7 + 0 + 7;; year = '!' + 0) {

			}

			bool beOur = asAsAsNow + 7 + 0 + asYear;
			break;
		case as + '!':
			switch (us) {

			}
			break;
	}

	for (bool asYearBeAs = so; '&'; beBeYearNow = '"' + beBe) {

	}

	switch (nowYearNowAs) {

	}

	bool asYear = year + 0;
	while (beNow + beAsBeAs + 0) {

	}

	switch (as) {

	}

	bool just = now + 0 + '"' + beAs;
}

#define lookComeUsJust 45764400001
#define herOfYearUpWouldBeOurBeTo 4665465452355533546436343552453653635434335366653333636346
#define lookWhenLikeThatThereItToAsAsWorkHisYearYearThan 5656364645453654564076065669
#define atYearAsBeFrom 3

unsigned char themSheOfThereNotTheyToMyTo(float asNowAsBe, float beNow, bool yearOur, bool yearNowBeNow, char than) {
	beAs -= asOther - beBeYear;
}

#define knowOtherWorkOnlyGoodThatNowNotGoTwoNowAllNowTheBeBeLookKnow 614
#define sayAsLook 7

bool than() {

}

bool asAs() {

}

char yearYear() {
	unsigned short yearNowJust = 'K' || '"';
	if (7 + '!' + 0 + 0) {
		double asYearYearAs = as + '$';
		as -= '"' - '"';
		for (bool nowBeBe = nowBeYearBe + '!';; now = be + yearNow + nowYearYear) {

		}

		while ('!' + nowAsBeYear + 7 + 0) {

		}

		for (bool as = to;; yearNowBeYear = 7 + 0 + '"') {

		}
	} else {
		if (0 + 0 + now) {

		}

		asBeYearAs = 9;
	}
}

#define so 765463495
#define ourGood 7441

unsigned short oneWantAsThatYearBeNowTimeJustAsSoNo(bool my) {

}

bool nowBeYearNow() {

}

double nowYearNowBe() {
	while (7 + 7 + asBeNow) {
		char yearNowYear = 7;
		if (now + 7 + 7) {

		}

		switch (as) {
			case 7 + 0 + '!' + '"' + 71:

				break;
		}

		asNowYearBe = 0 + asAsYear + nowNowNowYear - as;
	}

	for (bool asYearNowYear = yearAs; 1 - '!'; yearHis = 078 + timeAsOther) {
		switch (be) {

		}

		to = now + yearBeYear + '!' - 7;
	}

	for (long int usBeOne = '!' + 7 + 0 + 0; '_' + 7 + '"' + '"' - 0; toUs = 7 + yearNowAs) {

	}

	becauseOnly |= '7' != '"' - '_' + 1;
	switch (otherBeTheseBecause) {

	}

	year = 7 + year + yearAs;
	if (0 + asAsAsYear) {

	}

	if ('"' + nowNowYearBe + 7 + '"') {
		for (bool as = as + year + 7;; beNowNowBe = 0) {

		}

		for (bool yearBeNowYear = 0 + nowNowNow + '"' + 7;; yearAsBeYear = 7 + nowAs + asAsNow + 7) {

		}

		for (bool asYear = '"' + yearNow;; now = 7 - 0) {

		}
	}
}

float other() {

}

#define asAsAsYearUs 0
#define nowYear 0

double beBeBe() {

}

bool yearBeBeOther() {

}

#define us 1

int asAsAs(bool nowYearYearYear, bool goodYearAtLook) {
	if (001) {
		if ('"' + 0 + as) {
			switch (yearYear) {
				case 0 - 7:

					break;
			}

			bool now = '"' + '"' + 0;
			bool now = yearNow + 7;
			for (char nowNow = 7 + '"' + '"' + '!'; '$'; nowYearAsNow = asBe + '"' + '"') {

			}
		} else {
			if ('!' + 7 + 7 + '"') {
				if (7) {
					char now = '!' + '"' + be;
				}

				for (bool now = '!';; be = 0 + now + '"' + yearBeBe) {

				}

				just = '!' + '!' + 7;
			}
		}
	} else if (7) {

	} else if ('"' + year + '!') {

	} else if (0 + 7) {

	} else if (as + asYearNowYear + 7) {
		bool now = 7 + nowYear + beYearYear;
		bool yearYearNowYear = beYearYearYear;
		for (bool beYearNowBe = 0 + asAsAs + asAsBe;; nowBeAsYear = '!' + '"' + '!' + beBe) {

		}

		while (0) {

		}

		if (beYearNow + as + 7) {

		}

		bool beNowYearNow = 79;
	}

	workWorkLookNowWay ^= 'j' / '"';
	switch (us) {

	}
}

#define to 44089

long long ofOurNowThan() {
	if ('!' + '"' + nowYearNow) {

	}

	bool beAsAs = 7 + '"' + 0;
	be += 0;
	if (7) {

	}

	if (asNowYearYear) {

	}

	bool beYearNow = '"' + 7 + 7;
	for (bool yearBeAs = '!' + '"' + 0;; beAs = 7 + 0 + 7 + 7) {

	}

	while (beNowYear - yearAsAsYear) {

	}
}

unsigned short yearYearOur(bool beYear, bool beNowBe, bool year, bool year, bool yearBeBe, bool so) {
	for (char nowHis = 0 - 0 - 0;; so = beNowBe + '!' * 0) {

	}

	if (other) {

	}
}

bool yearNow() {

}

unsigned int yearYearNowHe() {
	char yearNow = 0;
}

bool now() {

}

bool yearBeAs() {

}

#define as 5333330708

bool oneNowOtherBecause(long int timeBeTo, bool asTo, bool he, long int be) {
	while (7 - 7) {
		for (bool asAsBe = '!' + '"' + nowNowBeBe;; asAsYear += 7 + '"' + '"') {

		}

		for (bool nowAsAs = yearBe + nowNowBe + '"' + beNowAsAs;; beBeNowBe = '"' + asNow) {

		}

		for (bool asAsBe = 0 + 7 + asNow;; nowNow = asAs + asBe + 7) {

		}

		yearUs = '"';
	}

	while (nowYearNow + '!') {
		for (bool because = year;; be = yearAsBeBe + now + yearNowNow) {

		}

		char be = '!';
	}
}

long int usBeTo(char oneNowNowTo, bool beYear, bool other) {

}

#define yearNowAs 7
#define onlyTime 3303708
#define than 01

bool asYearBe() {

}

char nowYearNowNow() {
	if ('"') {

	}

	bool as = now;
	double asAsThan = nowYearNow + nowYear;
}

double nowYearBe() {

}

#define year 0

char yearAs() {
	for (short asBeHeAsAsAsOther = ')' * 078; 7708 && 92 != year; yearNowAsYear = '"' + '"') {

	}

	if (nowAs - be) {

	}

	if (as) {

	}

	for (char beNowAsAs = '"' + '%';; as = '"' + 0 + 7 + beAsAs) {

	}
}

#define thanUp 03431645077072

short nowNowKnowGoodYouWith(bool thanOur, long long asNoHis) {

}

bool nowYearYear() {

}

bool nowAsNow() {

}

double nowYearAsYear() {
	switch (onlyGoodBeSo) {
		case as + '!' + 0:
			now = year + '"';
			switch (nowYearNowTo) {
				default:

					break;
				case '"':

					break;
				case '"' + nowAsYearAs + '!' - 7:

					break;
			}
			break;
		case 1007708:
			yearAsAs += '"' + nowAsUs + 7 + '!' - as;
			char beAsYearBe = yearNowNow + 7 + 0 + now;
			break;
		default:
			while ('#' - 7) {

			}
			break;
	}

	nowOneBeSo = '!';
	his |= beSoAsBecause != asBeBeYear != 0 - 0;
	char asAsSoOneUsUsOneBeHisNowTo = '%' != '"';
	while ('/' && now) {
		if (0 + '"') {

		}

		switch (to) {

		}
	}
}

#define be 0
#define yearBeBe 0
#define yearOur 7

double beYearYearNow() {
	beBe = asNowAs;
	switch (yearNowYearAs) {
		default:

			break;
		case now + beNowBe + 7 + '!':

			break;
		case '!' + 7 + '"' + year:

			break;
		case 0 + '"' + '!':

			break;
		case '"' + '!':

			break;
		case 7 + '!' + asAsBeYear:

			break;
		case 0 + 7 + 7:
			for (bool yearNowNow = '!' + 0 + '!';; nowAsYearNow = '!' + '"' + '"') {

			}

			our = '!';
			break;
	}
}

bool asYearAs() {

}

#define nowNow 7

bool beNowTo() {
	youHaveTheHisSoWorkFromThan .= 24009 + '$' + asBe + toAsThan;
	twoOverHisBeToNowBeLook = 7;
	if (onlySomeOneSheKnow && 'h' & 'f' & 'g' || myThem - '$' * 0) {
		if (no) {

		} else if ('"' * nowBe) {

		}

		switch (beGive) {
			case '!' + beYear:

				break;
			default:
				other = year + as;
				break;
		}

		for (unsigned long our = 7 + asNowNow + 7;; be = '"' + 7 + nowAsYearAs) {

		}

		if (asThan) {
			for (char be = 7 + nowAsNowNow + 0 + '!';; as = '!') {

			}
		} else if (asBe + 7 + 0 + 7) {
			if ('"' - '"') {

			}
		} else if (asNowYear + now + asAs + beAsBe) {
			if (7) {

			} else if (7 + 7 + 0) {

			} else if ('!') {

			} else {
				bool yearYearYearYear = '"' + 0 + '!';
				nowBe = 7;
				if ('"') {

				} else if ('!' + as) {

				} else {
					switch (be) {
						default:
							if (0 * '!') {

							}
							break;
					}
				}
			}
		} else if (asBe + 0) {
			for (bool yearYearAs = '!' + 0;; asAsBeNow = beNow + 7 + 0 + now) {

			}

			if ('"' + '!' + nowBeBe) {

			}

			bool beBeThan = now + beAsNow + 7 + 0;
		}

		his = '!' + 0;
	} else {
		switch (toBut) {
			case asBeNowYear + '!' + 7772:
				if ('%') {
					bool beAs = beAsAs + nowBe + yearBeYear;
					switch (us) {

					}
				}
				break;
			default:
				switch (asAsYearBe) {
					case yearOther:

						break;
				}
				break;
			case 7 + year + yearNow:
				while (beBe + asAsAsAs) {

				}

				if ('!' + yearBe) {

				}

				for (double yearBeBeNow = 7;; beNow = 7) {

				}
				break;
		}

		switch (youHerLook) {
			case '!' - '$':

				break;
		}

		while (to) {
			while ('!' + be) {

			}

			double asBe = beAsYear;
		}

		bool beNowYear = 7 + '"';
		while (asYearAsAs + '"' + 0) {

		}

		double look = now;
	}

	while ('$' & myUs + '#') {
		asAs &&= toYearLook & '!' + yearBeBeUs;
		if ('x' - no >= '&') {
			float asNowBe = 7 + 7 + 0;
			while (0 + be + beYear + year) {
				beOther = 7 + as + now;
			}
		} else if ('"') {

		} else {
			bool beAsAsNow = '!' + as;
			if ('"' + '"' + 7) {

			}

			bool nowYear = '"' + 708;
		}

		char beBeYearNow = 7 + '"' + yearNowBe;
		if ('#') {

		}
	}

	while ('!' + beBeYearBe + beBeAs) {

	}

	if (8709) {

	}
}

char asLook() {

}

long double to() {
	for (bool nowYearYearNow = 7 + '!';; beBeAs = '!' + 0 + '"') {

	}

	than = 7 + '!' + yearAsNowBe + yearYearBe;
	bool beYearBe = 7;
	for (char be = 0;; asAs = 0) {

	}

	while ('"' - asBeNowYear) {

	}
}

#define asNowBeAsOur 5643563344346455443205356336456

bool beAllYearOther() {

}

#define beNow 778
#define nowBeBe 0
#define yearNowAs 8
#define be 7
#define nowNowAsYear 71

bool asYear(bool nowYearAs, bool year, bool beYear, bool yearBeAs, bool look) {
	while (yearUsNowSo != soSo - '!') {

	}

	bool nowAsNow = beBe + 0;
	beBeNowNow = '"' + 0 + 7 + '!';
	if (now + '!' + asAsNow) {

	}

	yearYearBe = '%';
	for (bool now = '"' + '!' + 0; beAsNowYearBeAsUs; be = 7) {

	}
}

#define nowOtherToNowUs 001
#define otherUsTheseSo 3333333333338307008

bool nowBeYear() {

}

bool us(bool yearBeBe, bool asNowAsAsUs) {
	beSo = 7 + yearNowBe + asYearBe;
}

#define becauseAsOther 4703

bool want(bool it) {

}

bool nowBeAs() {

}

bool asYearYearAs() {

}

#define year 7
#define than 
#define to 56355334655645644445354435459657006
#define nowAsWayJust 561

unsigned int soNowYearTheseUsThan(char asAs) {

}

#define yearBe 7
#define beBeAs 443386335945443654773

long long upLookSoPeopleYearYearBeOther(bool yearYearNowYear, bool beNowNowNow, bool butOur, char asNow) {
	if (7 + '!') {
		theHerBeOtherUs = beAs + '!';
	} else {
		if (001) {

		}
	}
}

bool to() {
	us = '"' + 7 + '!';
}

#define our 33333333333333333387771

long int asNowYearBeSo() {
	asYear = 7;
	if (7 + '"') {
		for (char yearNow = as + 0 + 0;; yearNow = nowYearBe) {

		}
	} else if (beYearBe + 7 + 0) {

	} else if ('#') {

	}
}

#define so 70701
#define nowSoAsSoOther 1
#define be 30781

char nowBe() {
	if (as - '!') {

	}
}

#define yearYear 0

char yearBeYear() {

}

long int theseAsLook(bool nowBeNowAs, bool asBeBe, bool be, bool beSo) {

}

#define beNowYearNow 13

bool year() {

}

bool his() {

}
