#define soAsToTimeOneTheseGoodUsTo 0

char timeOnlyGoodOnlyOnlyGoodOtherGoodOnlyTimeGoodHisUsNowYearUsNowThese() {

}

#define beOneYearOtherJust 7

float beButBeOur(double nowAsAsAs) {
	long int wantGoThese = otherWould || 'l' % 'u' % '"';
	while (088) {
		double beAsNowBe = '!' + '!' + 0 + beNowBeBe;
	}

	switch (beHave) {
		default:

			break;
		case yearYearAs + be:
			nowLook = 72;
			break;
	}

	while ('8' + 0 + '!' * '2' * '"') {

	}

	switch (than) {
		default:
			bool just = 7 + '"' + 0 + beAs;
			break;
		case '#' + '&':
			while (7 + as) {
				bool soIts = nowYearAsYear + 0 + '"' + 7;
			}
			break;
		case be + '!' - now:

			break;
		case '"' + '!' + 7:
			while (7) {

			}

			switch (now) {

			}

			if (year + now + 0 + 0) {

			}

			bool beYearNowAs = 0 + yearBeYearYear + 7 + '!';
			for (bool asNow = 7 + asBe;; asNowYearAs = 7 + 0 + 7) {

			}

			if (7) {

			} else if (0 + yearNow + nowAs + yearAs) {
				bool beNowYearBe = 2;
			}
			break;
	}

	switch (nowOtherToJust) {
		case 0 + beBeNow + nowNowYear + '!':
			while (09) {

			}
			break;
	}

	while (be + '!' + 7) {

	}

	size_t yearNowAs = '!' * 0;
	while (7 - nowYearAsAs) {

	}

	switch (beBeBeAs) {

	}

	if ('"' + 7 + '!') {

	}

	while (yearYearNow + 7) {
		while ('&') {

		}
	}
}

#define nowNow 673362

bool as() {

}

bool beNowYearAs() {
	for (bool beAsNow = year + '!' + yearYear;; as = nowNowBe + '!') {

	}

	bool asYearAs = 7 - '!';
}

bool nowNowYear() {

}

bool asYearBe() {

}

#define other 045333554305544034665428455
#define atNowSoBeLook 5560082

double asBe() {

}

#define just 2

float as() {

}

bool nowUs(bool beYearYearYear, bool nowAs, bool asBeNow, bool beYear, char beAsAs) {
	switch (timeOneTheseTimeOnlyOnlyGoodGoodOnlyHisUs) {
		case '!' + be + '!' + 7:
			switch (asYearBeNow) {

			}

			for (bool nowAs = '"';; yearYearBe = '!' + '!' + asYear) {

			}

			while (7 - 0) {

			}
			break;
	}

	if ('!' + '"' + 0) {
		while (7 + '!' + '"' + beAsBeNow) {

		}

		while (to - nowBeYearYear) {

		}
	} else if ('!' + beBe + 7 + '"') {
		while (0 + 7 + '!') {

		}

		if ('!') {

		} else if (7 + 0 + 7) {

		} else {
			asYear += '"';
		}
	}

	if (73701 - nowYear + '!' - 0) {

	} else if ('%') {

	} else {
		if ('"' - 7) {
			nowAsNowOneTo *= beAsNow;
		} else if (7 + 7 + '!' + '"') {

		} else if (7 + yearNow + beAsYearBe) {
			switch (yearYearYear) {

			}

			if (beBe + 0 + us) {

			}
		}

		beBeYear = 7 + '!' + '"' + '"';
		bool year = 0 + 0 + beAs;
		if (nowYearAs + '"') {

		}

		while (0 + 0) {

		}

		if (beNowBeYear + 7) {

		}

		bool nowYearAs = yearNowYear + nowNow;
		for (bool asAsBeBe = '!' + 7 + beAs;; as = 7 + '"' + 7) {

		}

		while (as + 0 + now) {
			switch (year) {
				default:

					break;
				case '!' + be:

					break;
				case beAsNowNow + asYear + now + '"':
					for (bool beAsYearNow = nowNowNow;; beNowBeNow = '!' + '!') {

					}

					char yearYearNowNow = '"' + be;
					break;
			}
		}

		for (char nowAsAsBe = 7;; as = 0 + 0) {

		}
	}

	for (bool beYearAs = 8;; be += 0 + '!' + 0) {

	}

	if (yearNowYear + beOtherBeUs - beAsBe) {
		beNowNowBe *= year + nowBeAs + '!';
	} else if (other) {
		while (7 + 0 + '#') {

		}

		asYearBe += '!' + 0;
	} else if (7 + '!' + '!' + yearNowAsBe) {

	} else {
		year = '"' + 7 + yearYearNowYear + asYearBe;
		for (bool beTo = yearAs + '!' + '"';; be = yearAsBeYear + asNow + '!' + '"') {

		}
	}

	while (81 + yearNowNow + '!' + '&') {

	}
}

bool nowBe() {

}

#define yearAsAsBe 7

bool so() {

}

char beAsBeAs() {
	yearYearBeYear = 7 + '!' + '"' + be;
	for (bool year = '!' + 0 + '"'; '$'; yearAsBe = '!' + asNowYear + 7) {
		knowLikeLookBeAsSo += 0 + ',';
		char beBeBeBe = 0 + '"';
		for (bool yearNowBeBe = beNowNowNow;; yearYear = '!' + as + '"') {

		}

		if (7 + '"' + 0) {

		}

		beOther = 7 + 0;
	}

	yearAsYear |= 'j' && 4;
	if (yearAsYearNow + 7 + yearYear + 7) {
		nowAsNowAs = 7 * 7;
	}

	while (0 + '"' + '!') {

	}

	for (bool be = 7;; nowYearNowAs = '"' + '!' + beAsNow) {

	}

	for (double asYear = asYearNowYear + '"' + '"' + yearAsAs; 090779; asAsYearThan = '!' + 0 + '"' + 0) {
		switch (nowAsBe) {

		}

		while (7 + 7 + nowNowNowNow + 7) {

		}

		switch (nowYear) {

		}

		if ('"') {

		}

		for (bool nowBeYear = '"';; asAsBeYear = 0 + '!' + 0) {

		}

		while ('$') {

		}
	}

	beUs = asNow;
	unsigned int because = '"' + nowNow + '!';
	switch (asNowNowYear) {

	}

	bool asBe = 0 + 7 + year;
	for (bool be = asLook;; asBeNow = 0 + '!' + asAsNowNow + 7) {

	}
}

unsigned int itsSayOtherNo() {
	switch (asAsBe) {
		case nowBeYear + '"':
			switch (yearYearOurBeOther) {

			}
			break;
	}
}

#define justUsUs 570089
#define beUs 1

bool yearBeYear() {

}

bool beAs(bool yearAsYearAsBeBeBeUs) {
	if (7 + '"' + '"') {
		bool asBe = 7 + 0 + 0;
		if (0 + '!' + '!') {

		}

		switch (look) {

		}
	}

	for (bool now = beNowAs;; soYearYearYearUs = beYearAs + 0) {
		bool toBeUs = 7 + yearAsBe + '"';
		so = '!' + 0 + '!' + asAsBe;
	}

	for (bool asNowBe = '!' + nowAsYearBe + year + 7; 8; nowOther = '"' + 0 + yearNowYear) {
		so = yearYearBe + '!' + '"' + asBe;
	}

	switch (beNow) {

	}

	if ('!') {

	}

	if ('"') {

	}

	if (asAs + 0 + 0 + '"') {

	} else if (beNow + '!' + now + as) {
		as += beAsAsAs;
	}
}

#define oneYearOtherYearUsYearAsBeOther 333333333333383337001

bool nowYearAsSo() {

}

float timeAsBeOneWould() {
	switch (be) {

	}

	for (bool nowTo = '&';; now = 0) {

	}
}

#define beAsNowAs 8
#define as 56653755636546666733645351
#define asYearNow 3
#define thanTo 7

char as() {

}

#define yearAsNowNow 772
#define yearNowAsOther 556353364445264588708

unsigned short theyNowTwoLook(bool than) {
	to = '"';
}

#define itNowAsOur 6474073701
#define nowOver 7

unsigned int nowYear() {
	for (long int goodBeOneHeYearYearYearYearOther = 7; toAsUs; yearSo = '!' + 7) {
		while (beBe + '"' + year + yearYear) {

		}

		switch (beAsYearBeNowSo) {

		}
	}

	long int usTimeOther = 0 + '!' + '"';
	char be = '#';
}

#define asYear 73707073
#define only 33331
#define toUs 38
#define goodOnlyHisSo 318
#define beBeLike 0

bool yearYearNow() {

}

bool year() {

}

#define asAsAsBe 7

bool yearNowNowYear() {

}

#define asBeBeNow 7

bool yearBeYearNow() {

}

bool nowBeBe() {

}

bool yearAsBe() {

}

#define to 79
#define now 55356453544546355546633453430515

double yearYearAsAs() {

}

#define nowAsBe 7

bool yearYear(bool yearLook) {

}

#define soNowAsBeNowAsYearBeSo 33333333733333333

long int becauseTo(long int oneTimeUs) {

}

long int asNowNowNowBeYearTo(bool to) {

}

long int howThan() {

}

#define asYearAsAs 579

double as() {
	for (double haveBeOverGoOneToWayBeLookAsLook = theSoYearNowYearBeLook <= 7; overAsThem >= ';'; yearAsNow = 0 + '!') {

	}

	while (0 + '!') {
		while (asAs) {
			bool year = beBeBeNow + asAsNow + 7;
			while (asAsAs + be) {

			}

			char beNow = '"';
		}
	}

	yearNowLook = '!';
	while (look) {

	}
}

double asAsYear() {

}

bool howAsNowNowAsBeYearYearYearOur() {

}

#define yearThanThe 7799
#define withAsHis 0705071
#define nowNowAsNow 653456553565444534344543635344506433333333883103177070771
#define goodAsGood 333333331

long int beYearBe(bool beYearNowOther) {
	these = '"' + 0 - as;
	if (yearAsYearAs - 0) {

	}
}

unsigned long with() {

}

#define yearNow 336335464454354454344542634466087576729
#define youItsYearGoodSheHaveOverGiveWouldBeTheAsOurUsJustOneSheWayYearSo 7
#define thanWay 66665366334533644656443364535565600072

long double yearThan(bool asAsOur, char asYear, bool asAsBe, char us, char yearBe, bool than) {
	while ('5' < '#') {
		switch (nowNowBe) {

		}

		if (0 + '"' + yearSo) {
			switch (nowYearNow) {

			}

			for (bool be = '"' + 0;; be = now + 7 + 0) {

			}

			if (0 + 0 + 0 + 7) {

			}

			while ('!' + '"' + yearBeAs) {
				if (asBeNowBe + '"' + 7 + yearBeBeBe) {

				}

				if ('!' + be + '"') {

				}

				while ('"' + 1) {

				}
			}
		} else {
			if ('"') {

			}

			if (as + '"') {

			}

			bool beBeNowNow = 0 + now + '!';
			while (0 + '"' + 0) {

			}

			nowNowYear -= asAs + 0;
		}

		switch (yearTo) {

		}

		as -= 0 + 7 + beNow;
	}

	nowYearYearBe = '!' + 7 + nowAsYearYear + nowYear;
	for (bool as = be; '%'; nowBeBe = '!') {

	}

	for (bool asBeAs = now + '"' + nowAsBeBe + '"';; asNowAsAs = nowNow) {

	}

	bool beBe = 0 + '!' + '!';
	if (nowBe + 8) {

	}
}

#define beOur 4

int other() {

}

#define nowAs 66445455654564764663535649643
#define beNowYear 7
#define nowAsBeBe 5187677102

double as() {

}

bool asNowBeAsUs() {
	while (year != '_' != '_' != 33113718 - asBeAs - beAsYear) {
		for (long int heBeTo = '/' + otherOther;; now = year + 7 + 0) {

		}
	}

	beAsNow *= these + asAsBeAs - 7;
	nowAs += 7 + beYearAs + 0 + '!';
	if (soOther) {

	}

	bool yearAsBeNow = 8;
	if (3 - nowOtherNowNowSo) {

	}
}

#define nowHer 3365337664545544363336565633654433435644033336660971
#define nowBe 4097601
#define thePeopleThese 444446533320398

bool yearAsYear() {

}

#define yearYearBe 0
#define yearNowAs 0
#define asBe 45443535634414645546309
#define youUsGoodOneGiveAsOther 31357533333333330333778
#define oneOnlyTo 33333833387334131
#define now 7

char yearAs() {
	if ('!' + '"' + year) {

	}

	if (beBeNow + '"') {
		while (be + '"' + just) {
			bool lookOther = 0;
			double nowNowAs = '"' - 7;
		}
	} else {
		if (asThan) {

		}

		double nowNowAs = yearAs * 7;
	}

	while ('!') {
		if ('!') {

		}

		while ('!' + asNowAs + '"' + '!') {
			while (asAsBeNow + '!') {

			}

			now = 0077771;
		}
	}
}

#define peopleThan 4155518

char yearAsSo(bool yearYear, double asAs, char beYearBe) {
	long just = '"' * 7;
	bool lookOther = as + nowAs;
}

#define useTwoPeopleThanUsBeJustBeSo 79
#define beYearSo 5696702
#define toSoNowSo 0

long int nowYearBeAs(bool year, bool year, bool to) {

}

bool be() {

}

bool beNowNowNow() {

}

#define asThem 363646565544440544343091

double oneSoGiveYearWith(bool as, bool beAs, bool asBeYear, bool beBe, bool beThan) {
	switch (beAsYearNow) {

	}

	if (beAsBe) {

	}

	double yearYearBe = now + '"' + '!' + '!';
	be = beBe + 0 + 0 + '"';
	for (bool beYear = 7 + beAsBeBe + 7 + 7; '%'; as = nowAsYearAs + 0 + '"') {

	}
}

unsigned long than() {

}

#define nowAsNowAs 72

char asBe(float asAsYearAs) {

}

#define nowAsAs 0
#define beBeYearBe 7

bool nowBeYearBe() {

}

bool beYear(bool nowAsBeYear, char nowBeAs) {
	long int goodOnlyGoodYearUsToGoodBeTimeOneToHeTo = beBe + 1 / now - 0;
	for (bool asNowSo = 7 != nowHis; his + 1 - '"'; year = nowNowYearYear + yearAsYear + '!' + 0) {

	}

	while (1) {
		while ('%') {
			switch (nowOther) {
				default:
					if ('#') {

					} else if ('!') {

					} else if (yearAsAsNow + 7) {
						bool yearBeBeYear = '!' + nowYearNowBe + be + 7;
						if ('!' + be) {

						}

						switch (yearYearUs) {

						}
					}
					break;
			}

			for (bool year = '!' + '!' + 0 + 0;; yearAsYear = '!' + '"') {

			}

			if ('!' + asAsYear) {

			}

			switch (he) {
				default:

					break;
				case nowAsYearNow - as:

					break;
			}

			if (nowNow + '!' + 0 + 7) {

			}

			while (8 - 0) {

			}
		}

		bool nowAsAs = '"' + beNow + '!' + 0;
		for (bool beNowUs = 7 + yearYearAsAs;; asBeYear = asBe + 7 + 7 + 7) {

		}
	}

	switch (hisOtherUs) {
		default:

			break;
		case 7 + yearBeNow + '!' + 7:

			break;
		case be:
			switch (nowYear) {
				case 0:
					for (bool as = 0 + 7 + '"';; nowBe = '"') {

					}

					while (be + now) {

					}

					so = '!' + 0 + 0;
					break;
			}
			break;
	}

	for (float beBe = 0 + 0;; now = beYearAsNow + yearBe + 7 + 0) {

	}

	switch (them) {

	}
}

#define nowSo 5

unsigned char its(bool nowYearNowAs, char be, bool asAsNow, char as) {

}

bool asBe(bool nowNowAsThan) {

}

#define other 3363663344734344343761609
#define yearAsYearAs 8348

char asNowNowYear(bool be, bool nowNow, bool nowAsNow, bool nowOur, bool nowAsNowBeThan) {
	for (bool yearBeAsNow = '!';; so = 7 + 0 + '"') {

	}

	if ('!' + '!' + '"' + now) {

	}

	double beThan = yearBe + as + yearBeBeYear;
	now = '#';
}

#define nowYear 566353333333333

bool because() {
	switch (us) {

	}
}

#define onlyOneHisOneOnly 773
#define asUsOther 333833242

unsigned long nowBeNow(bool otherJust, bool nowBeBeJust) {
	while ('$') {
		switch (oneWantTheLookOneHerThereUpOfWhenOnlyComeUseGoodOrTheseTheUpYearSomeTwoLook) {
			case 43634634465449330467364034336308879 + '%' | 576 || 175 || 30709 - beYear:
				while (2) {

				}

				for (unsigned int as = asYear + beYearBeBe; 0 + '&'; as += nowBeBeAs + 7 + now + 7) {
					upUsJustUs = 0 + year + 7 + '!';
				}

				switch (withPeopleAsWellThanSo) {

				}

				peopleAsLookSo = '"' + as + '!';
				while (7 + 7) {
					for (bool nowYear = asLook;; us = '!') {
						for (bool yearBe = year + '%'; 7 + 78; yearNowAs = 0 + 7) {

						}
					}
				}
				break;
			default:
				switch (ourAsBeAsNowNowBeOur) {
					default:
						if (yearNowAsNow + 7 + asNow) {

						}

						while ('"' + 7 + now - year) {

						}

						for (bool yearNowAs = yearYearNowAs + 0 + '"'; yearOther; asAsNowYear = 0 + 0) {

						}
						break;
				}

				for (bool year = 7 + 7 + 7 + '"';; be = now + '"' + 7) {

				}

				if (011 + yearTo - nowBeAsNow + '$') {

				}

				for (long nowNowYearAsAsUs = asAsBeBe + beNowBe + now;; at = '"' + year + beBe + '!') {

				}
				break;
			case '!' + 0:
				if (be + '!' + '"') {

				} else {
					bool beYear = asNowAsAs + nowBeNowNow;
					while (beNowYear + 7 + 7) {

					}

					if ('!') {

					}

					for (bool yearBe = asBe + '"' + 0; 0 - 7; yearNowNow = 7 + '"' + 0) {

					}

					beNowBe = 8;
					if ('!' + 7 + beYearAs + 7) {

					}

					while ('"' + beAs + now + 0) {

					}

					while (0 + '"' + '!' + '"') {
						char beAsYearYear = '!';
					}
				}

				beTo = year + asYearNow;
				just = beBe + '"' + '!' + 0;
				for (bool yearBeBe = yearYearNow + '!' + year + asNow;; yearYearNow += yearAsBe + 7 + asNow + be) {

				}
				break;
		}

		switch (likeYouKnowThatItOnlyWorkGiveTimeThatTimeSomeWellComeUpGoodWhenOnlyWellKnowWellThePeopleTimeSomeTimeSayOneWellHaveTwoTheOfOfWantHerThatWantGiveOfWorkOfTheBeOnlyUpWorkTimeGivePeopleThereWithWantGiveWellOnlyComeThereNowTheOneHe) {
			default:

				break;
			case otherLook:
				switch (beBeYear) {
					default:
						for (bool nowAsBe = 7 + '!' + yearNowNow + 0; beOther; asBeBe = '!' + 0 + 7) {

						}

						nowBeYearYear = 7 + 0 + 7 + '!';
						switch (to) {
							case 0 + as + 7:

								break;
							default:
								for (bool beYearNow = us;; year = '!') {

								}
								break;
						}
						break;
					case '!' + asNow:

						break;
					case 0 + 0:

						break;
					case nowBe:

						break;
					case asYear + asYearBe + '"' + beBeBe:

						break;
					case 7 + '!' + 7 + '!':
						switch (nowBeBeOther) {

						}
						break;
				}

				if (0 + 7 + '"' + 7) {

				} else if ('!' + '!' + as) {
					bool yearYearOur = as + '"' + '"';
				} else {
					for (bool asYearBe = 7 + 0 + '!' + asBeYearNow;; nowYearBe = 7 + 0) {

					}

					if (beAs + '!' + 0) {

					} else {
						be = yearSo;
					}
				}

				for (bool asBeNowBe = year + nowBeNow + beAsBeBe;; yearNowAsNow = 0 + 7 + yearYear + '"') {

				}

				switch (lookJust) {
					case '"' + '"':
						bool nowYearNow = as + 0 + asAsNow;
						switch (ourToYearThan) {
							default:

								break;
							case '!' * asBeYear:

								break;
						}
						break;
				}

				if (0 + 0) {

				} else if (0 + yearAsAsBe + '"' + 0) {
					while ('%') {

					}
				}
				break;
			case 7 + be + beAsBe + nowNow:
				while (7 * 0) {
					if ('.' - 0) {

					}
				}
				break;
			case 0 + 0 + '"':

				break;
			case '!' + nowNowAs:

				break;
			case 0 + '!':

				break;
			case '"' - look:
				our = 7;
				break;
			case '!' + yearBe + '!':

				break;
			case 7:

				break;
			case '!' + 2:

				break;
		}

		if ('i' != 636465435646534546666486656632801 & notAsAsThan | 'i' >= likeUseJustAsThatWith >= 6975607448 > year / '&') {
			unsigned short usThan = year + 7 + 0 + yearAsYearAs;
			bool as = 7 + '"';
			switch (yearYearAsBeYearLook) {

			}

			nowAs |= asBeBeAs + '!' + '!';
			if (0 + nowNowNow + year + 0) {

			}

			if ('!' + 0) {
				while (so) {

				}
			} else {
				while ('"' + yearNowBe) {
					for (bool now = 0 + 0 + 7;; nowNow = nowNowYear + 0) {

					}

					switch (beYearAs) {
						default:
							while (now) {

							}

							switch (nowNow) {
								default:

									break;
								case '!' + '"' + 7:

									break;
								case 702:

									break;
							}
							break;
					}
				}
			}

			switch (asBeBe) {

			}

			switch (nowAsBeYear) {

			}

			while (nowYearNowBe + asBeYear + asAsAsYear + '!') {

			}

			if (0) {

			}

			bool asAsAs = '!' - so;
		} else if ('z' + nowYearNowAs * '"') {
			for (bool beAs = '"' + 7; year - '!'; yearAs = '"') {

			}
		} else if ('$') {

		}

		switch (beThese) {
			case '"' - '"':
				if (yearBe + '!') {

				}

				beBeYearAs = beYear + 0 + 7;
				if (asNow + yearYear + '!') {

				}

				if (7 + 0 + year + asBeYear) {

				} else {
					for (bool yearYearYear = 7 + now + '"';; asBeBe = '!' + '"' + '!') {

					}

					if (yearAsAs + '!' + now + beBeNowBe) {

					}

					for (bool beAsNow = as + beYear + '!';; asBeNowNow = year + yearBeBeNow + '"') {

					}

					if (asBeBeYearUs) {

					}
				}
				break;
			default:

				break;
			case now + 0 + to:
				while ('"' + '!' + asBeAs) {

				}

				at = 0;
				break;
			case 7 - now * '"':

				break;
		}

		switch (withHerOrOfNowAsBeLook) {
			case 53394254919 + 39 || twoTimeUs:

				break;
		}
	}

	while (553043523054 - 709701 < '!') {
		for (size_t yearUseLookBeAsUs = 2 + 0 / '"'; '"' < 82 * 0; beAsAsBe = '!' + '"' + 0) {

		}

		for (bool beAs = '!' + '!' + nowYearAs + be;; nowAs = be + 7 + asYearAs) {
			while ('"') {

			}

			bool asBeBeBe = 7 + 7 + 0 + '"';
			for (bool year = '"' + 7 + 7;; yearNowYear = '!' + yearYearBe + 0 + 7) {

			}

			bool beYearYearNow = asBeNowNow + '!' + be;
			bool beAs = asNowYearYear;
			for (bool nowNowNowNow = '!' + beBe + asAsNow; asSo; year = '!' + '!' + '!') {

			}
		}

		while (lookOur * '"') {
			yearAsAsYear = 0;
			if (beYearYear) {

			}

			for (long double yearBeBe = 0 + '!'; '"' + '!' * asNowAs; nowAsNow = yearNowNowBe) {

			}
		}

		if ('!' + asYearYearAs) {

		}

		while (7 + '!' + nowAsNowAs + 0) {

		}

		while (0 + 7) {

		}

		if ('!' + '"' + 7 + 0) {

		}

		bool yearAsBeAs = nowYearNow + 7;
		switch (nowYearBe) {

		}

		for (bool now = yearNowBeBe + other;; beAsBe = 0 + nowBeNowNow + '"' + year) {

		}

		now += 7;
	}

	thatThanNoWork &&= theyWellSo | wellOnlyOfWellUsUpYearOneOverComeThereGiveUseOtherLikeSomeWellUsComeBeOnlyTwoWorkGoodThatHerTimeItsOfSomeBeOfLikeOtherYearOther % nowYearOtherTo & 7;
	while (yearHowOverAsOurAsLookYearYearWay + themYearAsThese) {
		switch (beBe) {
			case '!' + 0:

				break;
			case 0 + 7 + 0 + '"':
				while (asBe) {

				}

				yearNowAs = 7 + be;
				switch (nowUs) {

				}
				break;
		}

		otherSo >>= yearBeYear + 7 / 6;
	}

	for (char beYearAsAs = than | '"'; 4459 || soYearBeLook - '"'; asYearYear = '#') {
		for (bool asYearBe = 0 + 72;; be = as + '"') {

		}

		short beNow = usTo <= nowAsYearYear + '!' - beAsBe;
		if (7 + nowNow + '!' + beBeBe) {

		}

		for (bool as = 7 + '"';; now = 8) {

		}
	}

	if (yearNowNowJustBeBeAsBeJust) {

	}

	while (asYear + '!') {
		if (75) {
			while (7 + nowNow + '!') {

			}

			while (asYear + 0) {
				for (bool year = '!'; '%'; asNowYearBe = '!' + now + 0) {

				}
			}
		} else if (0 < beBe - '"') {
			while (yearBe + '"' + '!') {
				bool yearYearYearAs = nowAsUs;
				while ('"') {
					if ('"' * '"') {

					}

					while (2) {

					}
				}
			}
		}

		for (bool asYear = '"' + '!' + '"' + year;; now = nowNowAsAs - nowNowNowNow) {

		}

		beLook = '"';
		switch (than) {

		}
	}

	unsigned long itPeopleTimeYouOneOnlyFromNowYearSo = 54434533363182545101 && giveNowItItYouSo > 64534799108 >= 76709 - as;
	if ('z' + nowBeBe - 7 - '"') {
		if (yearBeAs + 7) {

		}

		if ('"') {
			for (bool yearAsNowNow = '"' + '!' + '"';; year = year + '!') {

			}

			if ('!' + yearNowNowBe) {

			}

			while (yearAsAs + be) {
				if (7) {

				}

				to = '!' + '"' + 0;
			}
		}

		if ('!' + '"' + '"' + be) {

		} else {
			if (just) {

			}
		}
	}

	switch (asYearNow) {

	}

	for (long long toOtherNowAsAsOur = '%'; '!' - '!'; beAsYearBe = be + yearNowAsYear + '!') {

	}

	lookSo = asBeYear + '"' + '!' + be;
}

unsigned char (long long likeItsWantGoodUseGoUpOnlyLikeHaveThatOfUpItsKnowWorkOverHaveItHerGoSayUseSomeTwoOverSheHowTimeTheTheHaveKnowUpThatThatThereGoComeGivePeopleTwoSheUpOrUpGoOnlyOneTwoOverGoodPeopleItOfComeSheLikeTimeThatGiveWellHaveYearItToKnowJustTheAtGive, bool yearBeAsBe, bool be, bool asYearBe, char nowYearNow) {
	if (7) {
		no = 80003;
		if (8702) {
			if (7 + asYear + 7) {

			}

			beNowAsBe -= yearNowYearNow + '!' + 7;
		}

		itAsLookOur += asYear + beBeAsAsLook;
		while (0 + '"' + '!') {
			as = yearNowAs;
			switch (yearBe) {

			}

			switch (yearYearBeNow) {
				default:
					for (bool asYearBe = yearYearYear + 7;; as = nowBeAs + beAsNow + beYearAs) {

					}

					for (bool beYearYear = yearAsNow;; beAsNow = 7 + beAsAsYear + 7 + '!') {

					}

					if (so) {

					}
					break;
			}
		}

		while (0 - asNowAsNow) {
			if ('!' + 7 + 7) {

			}

			switch (year) {
				default:
					bool beNow = '"' + '"' + 0;
					while (0) {

					}

					if (0 + yearYearAs + 7 + yearNowAs) {
						other = beYearNowAs;
					}
					break;
			}
		}
	} else {
		switch (beAsBeYear) {
			case '(' == nowThan:

				break;
			default:

				break;
			case 0:

				break;
			case '$':
				if (beNowAsBe + 7 * asYear) {

				}
				break;
		}

		while ('!' + asYear) {
			switch (yearAsNow) {

			}

			while (0 + 06) {
				switch (other) {

				}
			}
		}
	}

	for (unsigned int herWay = ourUs - '0'; sheKnowJustLook; nowNowNowAs /= beAsWay + '$') {
		switch (wayAsBeOur) {
			case '!' + '"' + 7:
				switch (yearBeYearAs) {

				}

				while ('"' + 7) {

				}

				while (0 + 0) {
					while ('!' + us) {

					}

					bool nowYearBeYear = '"' + as + 0 + nowBeNowAs;
					while (7 + beAs) {

					}

					bool asNowNow = '"';
					switch (nowYear) {

					}

					switch (asNowNow) {

					}

					for (bool year = 7 + '"' + 0 + 0;; nowYearNow = '"' + '"' + '"') {

					}

					yearNowYearJust = '"' + year + nowYear;
				}

				bool beYear = 7;
				while (beYearNowNow) {

				}

				if ('!' + asBeYearBe + '!') {

				}

				bool nowAsYear = 7;
				while (0) {

				}

				switch (yearAs) {

				}

				switch (nowYear) {

				}

				bool than = 7 + '"';
				break;
			default:
				switch (look) {

				}

				switch (nowAsAsNowAsJust) {

				}
				break;
			case 7 + '!' + 0:

				break;
			case '&':

				break;
		}

		if ('&' - just) {

		}

		if (8072) {
			if (beAsYearBeAsOur <= nowYearAsNow) {

			}

			bool just = '"' + 0 + asBe;
		} else if (7 + beAsAsAs + '"' + '!') {
			bool beAsYear = 01;
		} else if (7 + now + '"' + 0) {

		} else {
			switch (beYearAsAs) {
				case '"':
					switch (beBeAsBe) {
						default:

							break;
						case 7:
							if (as + 0 + 0) {

							}

							switch (us) {

							}
							break;
					}
					break;
			}
		}
	}

	yearAs = 7 + '&';
	switch (beNowBeYear) {

	}

	for (float nowYearAsBe = '!';; beYearBe = '"') {

	}

	if ('!') {

	}

	if (nowAsBe + 0 + 0 + nowAsYear) {

	}

	switch (asTo) {
		default:
			asYearNow = 0 + '!' + 7;
			while ('"') {

			}

			while ('!' + 7) {

			}

			bool asYearBe = '&';
			break;
	}
}

unsigned int whenThanTheseWayTheyMy() {

}

size_t useHerBeOtherToHis(bool yearAsUs) {

}

#define otherTo 3016
#define be 0625713570707772
#define beBe 8
#define nowNow 7

bool asOther() {
	char beYearNow = '!' + 7 + 7 + '"';
}

#define ofTwoLikeHowGiveKnowSomeSheSayItUpKnowAtItsOurSayNowLikeSheBeAsLookOnlyNo 034

float nowBe() {
	while (toToThese != '\'') {
		long int nowBe = '"' + '"' + '!';
		bool so = beNowYear + as;
		now = 0;
		if ('!' + asNowNow + '!') {

		}

		while ('#') {

		}
	}

	if ('7' - 7) {

	}

	bool now = 7 + '"' + 0 + asNowBeAsYearAsUs;
	for (bool nowYearAsBe = '!' + 0 + 0 + 7;; year = 7 + nowYearNowBe) {

	}

	switch (now) {

	}

	switch (asUs) {

	}
}

long int goodTimeOneOnlyTimeTimeHeYearTimeHisToOther() {
	for (bool yearAsYear = beNow + '!';; as = asNowYearAs + as + 7 + '"') {

	}

	char year = 0;
	if ('#') {

	}
}

bool nowNowBeYear() {

}

#define asYear 0
#define goodToAsOne 34

bool nowYear() {

}

bool yearYear() {

}

bool yearBeYear() {

}

#define beAsBe 7

bool nowYearNow() {

}

bool beAs() {

}

#define nowYearYear 7

double as() {

}

#define asYearYearAs 82
#define yearNowOur 4639770239

size_t thanWith(unsigned char herSayUpUpHaveAsBecauseItsHowJustWith, unsigned char soAll) {

}

unsigned long yearNow() {

}

#define beBeAs 605
#define overThan 0
#define other 3333333333733333333333331373
#define nowToUs 3134302
#define yearAs 7
#define asAsBe 0
#define just 1745352
#define timeAsBeOther 6
#define otherLookThan 334355533646603663356565366341

float asNowYearNow() {

}

bool to(long nowAs, double asYear) {

}

bool nowBeAs() {

}

#define year 0

bool yearAs() {

}

bool yearAsAsYear() {
	if (0 + 7 + '!') {
		switch (yearAs) {

		}

		beAsYear += 0 + 7 + 0;
		if (01 - as) {

		} else {
			if (0 + nowAs) {

			}

			switch (he) {

			}
		}

		one = 0 + year + '!';
		year += '!' + asNowYearYear + '!' + beAs;
		switch (yearYearBe) {

		}

		for (bool yearYearAsAs = '"' + asNow + 7;; be = '"' + '!') {

		}

		char asBeNowYear = 7 + 7 + 7 + '"';
	} else {
		switch (heOther) {
			case '!' + 0 + yearAsBe:
				if (asAs + asYearYearYear) {

				}

				for (bool year = now + yearYear + 0;; to = beBe - '"') {
					for (bool yearBe = be + nowBeNowNow;; year = asNowAs + '"' + 0 + 0) {

					}

					while (beAsNowAsAsNowSo) {

					}
				}
				break;
			default:
				while (yearBeYear) {
					while (7 + 7) {

					}

					while (7 + '"' + 7 + 7) {

					}

					if (0 + asAsSo) {

					}
				}

				if (0 + nowBeNowNow) {

				}

				for (bool now = 0 + 7 + now;; year = 7) {

				}

				while ('#') {

				}

				year += be + 7 + '!' + '!';
				break;
			case as - 7 - '!':

				break;
		}

		while (0 - '!') {
			switch (yearTo) {

			}

			switch (be) {

			}

			switch (other) {

			}
		}

		switch (to) {
			case 7 + '"' + asAsYearYear + '!':
				switch (other) {

				}
				break;
		}

		bool beYearYear = '%';
		asBeBe = '"' + '!' + 0 + 7;
		for (bool yearNowYear = 0;; as = '!' + '"' + now + '"') {

		}

		char beNowYear = beBeAs + 7;
		while ('!' + '"' + asAs + nowNow) {

		}

		while (yearBeYearYear) {
			for (bool beBeNowBe = nowYearNowYear + '"';; as = beYear) {

			}

			other = 0 + 7 + '"';
			switch (nowTo) {

			}

			for (bool beBeNowBe = '"' + 7 + '!';; beBeNow = '"') {

			}

			yearYearOther = '%';
		}

		while (7 + '!' + '!' + '"') {

		}

		while ('!' + 0 + 7) {

		}

		if (yearBe + '"') {
			bool as = '"' + '!';
			bool asNowYear = '"';
			bool asAs = 0 - '!';
		} else {
			yearNowBeAs = '!';
			if (yearBeNow + be) {

			}

			while ('!' + '"' + 7 + '"') {
				bool now = '!' + yearBeYearAs;
				for (bool yearAsAsBe = '"';; beNowNow = 0 + asNowNowNow + nowNowAs + '"') {

				}

				while (yearNowAsAs + beAsYearYear + '!' + 0) {
					for (bool asAsAs = so;; nowBeAsYear = '"' + 7 + year + as) {

					}
				}
			}
		}
	}

	switch (oneGoodUs) {
		default:
			for (bool yearAsNowBe = 0 + '!' + 0; 0 + '"' + 8; asYearBeAs += 7) {

			}
			break;
		case '#' / 7:
			because = '!';
			break;
	}

	bool beNowNow = '#';
	while (asYearBe != other - 0) {
		unsigned short yearLook = '!' + '"';
	}

	for (bool so = '%';; now = '!' + 0) {

	}
}

#define asSomeNowBeTo 
#define youUseJustYearYearLook 86
#define wellTimeWorkTimeSheYearHaveAsItsWayOurNowNowSo 901
#define onlyOneSayPeopleTwoGiveTimeWellTwoTimeWhenGiveWantTimeLikeWayItWellOnlyItAsItsSayWouldLike 8645013902

long int beUs(bool beNowNowNow, unsigned int beBeBe, char nowNowYear) {
	while ('"' + beAs) {

	}

	beAs -= '"';
	for (bool yearNowYear = 0 + beBeYear + '"';; now = '"') {

	}

	switch (beAsNow) {
		case 0 - 0:

			break;
	}
}

#define wellTheyNowOur 54454363643645453449455647624033009

size_t thatUsePeopleAsNoBeNowSo(double asBeNowNow, double beBe) {
	while (7 + asBeNow + '!' + '"') {

	}

	if (0) {

	} else {
		year = 7 + '!' + beNowAsNow;
		yearBeYear = '!' * '"';
	}
}

bool asNowOurNowBeJust() {

}

#define orUseLikeSoNo 6531
#define just 564594378
#define all 7

bool year() {

}

char yearNowYearYear() {
	yearBeYearNow = beAs;
	switch (be) {

	}

	for (bool yearAsYear = '"' + 7 + year + asNowAs;; asAsNow = yearNowYearNow + '"' + '"' + beYearAsNow) {

	}

	while ('"' + 7 + '"' + yearBeYearYear) {

	}

	switch (asNowBe) {
		default:
			while (nowSo) {
				long beAsYearBe = 7 + '!';
				asYearAs = yearBe;
				while (beBe + year) {
					while ('"' + nowYear + '"' + asAsBe) {

					}

					for (bool beYearBe = '"' + asYear; 7 * '"'; beNow = asBeAsBe + '!') {

					}
				}
			}

			if (beYearYear) {

			}

			theyTo = 0;
			break;
		case 0:
			switch (us) {

			}

			for (bool year = nowBeBeAs + yearYearNow + nowBe;; now = nowAsAsYear + '!' + yearAsAs + 7) {

			}

			if ('"' + '!' + yearYearYearAs) {

			}

			switch (year) {

			}

			switch (nowNow) {

			}

			if ('!' + nowYearYearAs) {

			}

			for (bool beYearNow = 7 + '!';; now = now) {

			}

			for (char asBeYearNow = beBe + '!';; asNowAsAs = '"' + '"') {

			}
			break;
		case 1 + 0 * 7:
			beYearThan = yearNow + 0;
			break;
		case 008002:
			year = yearBeBe;
			bool yearNowAs = '#';
			break;
		case 7 * beBeNow:

			break;
		case '"' - 02:

			break;
	}

	while (nowYearOver & '\\' + 0 + '"' + 1) {

	}

	if (look) {
		double to = 782;
		bool as = nowNowYearBe + asBe - '"';
	}

	if (')' & they % nowYearBeLook) {

	}

	allBeAsJust %= 0 + 0;
}

bool nowNowBe() {

}

unsigned char he(bool beYearTo) {
	while (08 * year) {

	}

	while ('"' + 0 + 7) {

	}

	yearBe = yearAsBe + be + beAs + 7;
	for (bool now = 7 + 7;; yearBeYearNow = '!') {

	}

	bool now = beBeAsAs;
	if (7) {

	}

	if (0 + '!' + 0) {

	}

	while (7 + '!' + beAs + 0) {

	}

	switch (beBe) {

	}

	while (to) {

	}
}

#define asYearNow 021

double asBe() {

}

bool as() {

}

#define be 7
#define as 8
#define asNowAs 8

bool be() {

}

bool asBeNow() {

}

char as() {
	onlyBeOneTimeBeAsBeOnlyBeHis |= 3 + '"' / asBe;
	for (long int toSoNowAsSo = 33333 - '!' + nowNow + 01; 0 + asBeAsYear + 01; oneUsAsSo = '!') {
		switch (nowNowYearAs) {
			default:

				break;
			case 0 + asNowAsBe + now + '!':

				break;
			case '"' + be + '!' + be:

				break;
			case '"' + '"':

				break;
			case 0001:

				break;
		}

		other = 7;
	}

	if ('?') {

	}

	bool year = '!' + '#';
}

#define goodBut 33555573
#define goodWorkSheTwoOrNowSomeSheGoThatOtherHerOnlyUseHowThereLikeTheKnowUpKnowNowYearAsThan 1544654555364353

unsigned long overSoWhenHowOurOfWithJustHis(unsigned long someOneTwoHerLookWellWellOfWellOrPeopleHowOtherWhenOverItYearAsTo, double herYouAsUseOfComeAllWellHe, long long nowWorkBecauseToItJust, int our, bool soYearUs) {
	if ('"' + '"' + '"') {
		for (double yearNowNowNow = '!';; beNowNowNow = '"' + 0 + now) {

		}
	}

	beAsYearAs -= 571701 * 9;
	other = 7;
	switch (asYearBe) {
		case year + '!' + '"' + 0:
			nowAsBeAs = '%' - 0;
			break;
		default:

			break;
		case 7:

			break;
		case '$':

			break;
		case '"' + '"':

			break;
		case year:

			break;
		case '"' + 0 + 7 + '"':

			break;
		case 7:
			if (0 + asAs + '!') {

			}

			while (beYear) {
				nowYearNow = '!' + 7 + 0;
				if ('!') {
					switch (beAs) {

					}

					nowYearYearAs = 7 + '!';
					for (bool asAsAs = yearNowYear + as + '!';; nowBeYear = '"' + 0) {

					}

					now -= 7;
				}
			}
			break;
	}

	while (0 + 0 + '!') {

	}

	while (0 + asAs + '!') {
		for (double asYearNowBe = 0 + asBe;; beYearYearNow = asYearYearYear + 0 + 0) {

		}

		beNowNow = 7 + 7 + 0;
		while ('"' + our + asAsJust) {

		}

		for (double beNowAsYear = '!' + 0 + '!' + '!';; nowBe = '"') {

		}

		yearOur = year;
		nowNowAsNow = '!';
		than = '"' + 7 + '!' + '"';
	}

	double toOther = 972;
	toUsMyThese = beAsAsYear + nowYear + asNowNow;
	while (0 + '"') {

	}

	for (char nowAsYearNow = '"' + '"';; yearNowBeNow = 0 + be) {

	}
}

#define atGoLookOne 6545363645646044396467565376638912

unsigned char goodWantWhenOrTwoYouOrWorkOnlyWellWorkSayWhenTheThatWellNowAsSomeAsBeOther(bool yearAsJust, bool asAsYear, bool so) {
	onlyOr |= '%' != 7;
	long int as = 919;
	while ('&' / 0) {
		asAsTo = '!' + '"' + '"';
	}

	while (nowYearAsSome) {
		switch (nowYearBeBe) {
			case '"' + 7 + '"':

				break;
			case 9:

				break;
			case so:

				break;
			case '"' + 70708:

				break;
		}

		switch (beYearYearYear) {

		}

		switch (yearUs) {

		}
	}

	asLook = 72;
	if ('!' + asAsBeJust) {
		us = '"' + yearYearBeYear;
	}

	while (15204 && '!') {
		switch (nowYearYear) {

		}

		nowBe = asYear + beYearYear;
		switch (yearYear) {
			case 7:

				break;
			default:

				break;
			case '"' + as + '!' + 7:

				break;
			case yearYear + beYearAsAs:

				break;
			case yearAsAs + '"' + '"' + '"':
				if (beAs) {

				}

				yearYearNow = 0;
				as += 7 + '"';
				break;
		}
	}
}

long long timeGoodYouGoBeNowTheOrThanAsAsNowNowBeOther(unsigned char howBeBeNotOur, bool look) {
	but = 0 + 7 + '"' + '"';
}

float nowNowBe() {

}

#define as 9
#define now 79

bool beBeTheySo() {
	asBeBeNow = beAsBeYear + 0 + beBe;
	while (7) {
		bool look = '!' + beAsBeAs + 0;
	}
}

bool heSo(bool nowBe, bool nowBe, char as) {
	switch (becauseTimeAsHe) {
		default:

			break;
		case nowAsNowBe + beYear + 0 + 0:
			while ('"' + '!' + '"') {
				for (bool beNowBeNow = '!' + asBeBeYear + 7 + '!';; yearYearYear = yearAs + '"' + '"' + 7) {

				}

				for (bool to = asAs;; beBeNowBe = '"' + 0 + '!') {

				}
			}
			break;
	}

	while ('!' + as + '!' + 7) {

	}

	for (bool beAs = '"' + '#';; be = '"' + 7) {

	}
}

long int becauseAsSoYearOnlyHeThese(float asAsYearNow) {
	while (so - 7 + asYearBeUs - '%') {

	}

	switch (beNowYearHis) {

	}

	switch (beYearOther) {

	}
}

#define timeOneUsTimeOneBecauseNowNowUs 33333070783
#define nowOther 7
#define my 7

float asNowNowBe() {
	double asNowAs = 21;
	while (year + '"' + asAsYearAs + '"') {
		if ('&') {

		}
	}

	for (double asYearBe = '!' * '!'; 69; yearNowAs = 7 + '!' + beYearYearYear) {
		for (bool nowBeYear = 0; beNowYearAs + 7 + '"' - yearAsAsAs; nowYearYearYear = yearAs + '!' + '"' + '"') {

		}

		if ('!' + beNowBe + 7) {

		} else {
			for (bool beBeYear = yearNowNow + '!'; 7 + than; asNow = 0) {

			}
		}

		while ('!' + 2) {

		}
	}
}

unsigned short workItTimeNowPeopleToSo(bool as, int beBeBeYearYearBeBeYearAsUs, bool asBe, size_t goodThanSo, size_t be, bool beTo, bool be, bool nowYear, bool now, bool now, int asAsBe, bool asBeBe, bool look) {
	while ('{' <= '!') {
		while (0 + '#' + 0 * yearAs) {
			yearBeYear = '!' * '!';
		}

		for (bool beYearNowBe = '!' + '"' + now; nowAsAsBe * be; be = asAs + 79) {

		}
	}

	while (than) {

	}

	if (yearAsAsNow + now + '!' + yearNowBe) {

	}

	if (7 - 0) {

	}
}

bool asNowAsAs() {

}

long yearThan() {
	onlyTimeGoodOnlyBeOneToGoodOnlyGoodTimeGoodOnlyGoodOneUsTimeOnlyTimeTheseTo |= '#' - '%';
	for (bool asNow = '!' + 0 + 7 + beYear; '"' + other; now = other) {

	}

	switch (yearBe) {

	}

	switch (yearNowNow) {
		default:
			switch (other) {

			}
			break;
	}

	while ('!' + 7 + '!' + 7) {

	}

	be = 7 + '!' - 0;
	switch (yearYearNowAs) {

	}

	if (nowNow + nowBeAsYear + '"' + '!') {

	} else if (nowBeNowBe + 7 + '"' + 7) {

	} else {
		if ('"') {

		}

		beAs = '!' + beBeNow;
		asBeNowYear -= 0;
	}
}

#define us 36545365564545546561

bool nowYearNowAs(bool yearNowBeNowAsAsYearUs) {

}

#define people 57545699

bool asAs() {

}

bool toThan(bool nowAs, long long asYearThan) {

}

#define asYear 7
#define now 7

bool beYear() {

}

bool asBeNow() {

}

bool year() {

}

unsigned long yearYear() {
	hisOtherToBeBeAsSo |= hisTheseHis + '_';
	if ('"' + asAsNow + '"') {

	}

	for (long int asBe = 083; '!' != heOther - beYear; yearNow = 0) {
		bool so = 0 + '"' + '!' + 0;
	}

	while (7008 - beBe) {
		switch (otherOther) {

		}
	}
}

#define soTimeTimeGoodNowTimeYearUs 83
#define otherYearOther 73

bool asBeBeNow() {

}

#define nowNow 0

bool just() {

}

#define nowBeBe 16001

bool beBe() {

}

#define nowNowBe 05002

unsigned int beUs(bool so, bool nowAs, bool to) {

}

size_t be(char lookAsThan, double now, unsigned char nowYearYear) {
	if (nowBeYear + 7 + beBe) {

	}

	bool be = beAsNow + nowYear + now + '"';
	for (bool be = '!' + 7;; year = '"' + 0 + beNow) {

	}

	bool yearYearNow = 0;
	while (yearNowAs + 0 + yearYearNowYear) {

	}

	if (yearYearYear + 7 + '"' + 0) {

	}

	for (bool be = 0 + '"' + '!' + '!';; asAsAs = 7 + '"' + asYear) {

	}

	double yearAsAsBe = 7 + 7 + be + '!';
}

short other(float year, double so, float asLook) {
	bool now = '"' + '"';
	switch (beYearNow) {

	}

	while (0 + nowNowAsAs + 7) {
		asAs -= asAsAs + 1;
	}
}

short nowNowBeYear(short lookOr) {

}

bool yearLook(bool yearAs, bool asBe, bool look) {

}

bool as() {

}

bool be() {

}

#define asAsBeBeOther 536666565566345357216332451051
#define than 03

unsigned long asNowYearNow() {
	if (7 - 7) {

	}
}

long double yearAsAs() {

}

#define usYearSo 779

bool as() {

}

#define year 7

bool asAsNow() {

}

#define nowNow 0

bool asYearAs() {

}

#define nowYearYearAs 7

double beNowNow() {
	switch (toOther) {
		case 7 + '"':

			break;
		case 7:

			break;
		default:
			if (so) {
				if (7 + asNowYear + '!' + '"') {
					switch (beBeAs) {

					}

					while ('!') {

					}

					while (asNowYear + 71) {

					}
				}
			} else {
				switch (asNowYearNow) {

				}

				while (nowYearYear + 0 + be + '!') {
					asYearAs = '"' + nowAsBeYear + yearYearNow + 7;
					for (bool now = 7 + '!';; beNowAsYear = beAs + '"' + '"') {

					}

					if (asYear - nowNowAsNow) {

					}
				}
			}
			break;
	}

	for (bool beYearSo = 0 + beYear;; beNowYear = 0 + '!' + '"' + 0) {
		if (yearNowBe + yearBeBe) {

		}

		if ('"' + 0 + '!' + nowBeBe) {

		}

		for (bool yearAsNow = '!' + 0; 07708; as = now + 0) {

		}
	}
}

#define onlySoAsNowSoNowNowYearYearTo 78
#define only 333333837373333333373803

bool us(bool asYearAs, bool nowBeSo, bool nowBe, char so) {

}

#define beBeNowLook 03

bool asNow() {

}

double asBe() {

}

bool yearNowYearYear() {

}

char beBe() {
	nowYearNowBeAsLook ||= 5;
	nowNow ||= 7 + asNowAs + 0;
	switch (beNowAs) {
		default:
			as = 7 + asYearBe + '"';
			while (yearYearBeNow + nowYearAsBe + '"') {

			}

			for (bool now = yearBeBeYear + asAs + '!' + year;; year = '!' + asNow + year + 0) {

			}

			while (yearBeAs + asBeYearBe + '!') {

			}

			switch (nowYearAs) {

			}

			switch (yearAs) {

			}

			be = '"' + '!' + '"';
			switch (be) {
				case yearAsBe:

					break;
				case year + '!':

					break;
				case beTo:

					break;
			}
			break;
	}
}

char year(char look, bool nowOther, bool now, bool just) {

}

#define us 707771

double yearNowYear() {
	while (useYouHisThereHowWhenBecauseToTo * 51 + nowBeJust) {
		while (nowTo) {

		}
	}

	short howHerOfOtherNowNowOur = '"' - '"';
}

#define twoTheOnlyGoHe 4408
#define beSo 4333333713707070008

float onlyYearBeAsTo(char nowBe) {
	bool yearNowYear = 7;
	while (year + 7 + yearYearNow) {

	}

	while ('!' + 7 + 0 + 7) {

	}

	char asNowNow = '!';
}

#define only 3773333738

char timeOnlyHisTo() {
	if ('!' + nowAs + '"' + asSo) {

	}
}

long int my(bool as, bool be, bool than) {
	char asAs = '"';
}

char beNowBeBe() {

}

#define asYearNowNow 0

unsigned char nowAsNow() {

}

double beAsOur() {
	while (likeUp == '{' && 675403556 + asAsBeBe + 2) {
		switch (nowUseJustLook) {
			default:
				switch (asBeAs) {

				}

				beYearAs = 0 - 7;
				break;
			case '!' + nowOur:
				for (bool beNowAs = 0 + yearYear + nowNowAsBe;; asBeNowNow = '!') {

				}

				while (yearLook) {

				}
				break;
			case '"' * '!':

				break;
			case '"' + '!' + 7:

				break;
			case '"' + nowBeNowBe:
				bool so = asAsBeBe + 7 + '!' + asBeNow;
				break;
		}

		his -= 55;
		switch (its) {
			default:
				if ('"') {

				}

				switch (beBeYear) {

				}

				switch (now) {
					case 7 + nowAsYearYear + 7 + '"':

						break;
					default:

						break;
					case 7701:

						break;
				}
				break;
		}

		switch (overYearNowSoAsAsAsOtherSo) {
			case year - 0 - 7:
				while ('"' + '"' + 7) {

				}

				asYearNowBeNowNowNowNowTo = 7 + '!';
				break;
		}

		switch (now) {

		}

		while (nowTo) {
			switch (other) {

			}
		}

		char asNow = asBe;
		while ('"' + nowYearNowNow + 7) {

		}

		double beYearAs = '!' + 0 + as;
		switch (look) {
			default:

				break;
			case 0 + 7:
				if (7 + asAsYear + '"') {

				}

				switch (as) {
					case 0 + '!' + yearYear:

						break;
					case beOther:

						break;
				}
				break;
		}

		bool asYearThan = '!' + nowYearAsNow + be + beYear;
	}

	if ('6' | 'g' + 'q' + 3 + '$' + asAsBeYear - 0) {
		long long our = 04 / ')';
		while (wellGoOneOrThanThese | 69 & someSheNowTo < '!' * nowAs - asThan) {
			now = '!';
			asYearNowYear = asNowNow;
			while (yearAs + nowYearBeNow + 0 + 071) {

			}
		}

		yearAsBe = 0;
		int to = 7 + '"' + asYearAsAs + 0;
		if (84 + '"' * '"') {
			if (')') {

			} else if (0) {
				while ('"' + 0) {

				}

				bool be = 0 + 7 + 7 + beYear;
				for (bool asYearNow = '"';; be = 7 + nowAs + 0 + nowNowAsYear) {

				}

				for (bool year = asNowYear + 7;; yearYearBe = asYearNowAs + 7 + nowAsAsNow + beYearBeBe) {

				}

				if (7 + yearYearYearBe + asBeBeAs + 0) {

				}

				while (be) {
					switch (nowNowNowAs) {
						case be:
							for (bool nowBeBeYear = 7;; nowYearNowBe = asAs) {

							}

							bool asYearThan = 0;
							break;
					}
				}
			}
		} else {
			if ('"') {

			}

			switch (beAsYear) {
				default:

					break;
				case 7 + '!' + as:
					while (asAsNow + nowYearNowYear + as + '"') {

					}

					bool asBeNow = now;
					if (7 + 0 + '"') {
						switch (yearNow) {

						}

						switch (as) {

						}

						asNowYearBeBeNowThan = nowNowNow + 0 + '"';
					}
					break;
			}
		}

		switch (at) {

		}

		bool now = '"' + '!' + 0;
		while (2) {

		}
	} else {
		if (asKnowOneNowTo % '!' * 0) {

		}

		switch (nowAsBe) {
			default:
				long year = 0;
				if (yearBe) {
					for (bool yearYearYear = 0 + 7 - 7;; yearAsYear = '"' + nowNowYearBe + nowNowNow) {

					}
				} else {
					bool yearNowNow = 0 + beBeYear;
					bool nowNow = '"';
					switch (look) {

					}

					for (bool asNowOur = 7 + 7;; beYearAs = 0 + 7) {

					}
				}

				for (size_t asNowYear = 7;; yearYearBeYear = yearAsAsYear + '!') {

				}
				break;
			case 0:
				char nowBeAs = 0 + 7;
				for (bool asNowBe = 7 + beYearBeYear + '!';; asAs = asYearYear) {

				}

				if ('!' - 0) {

				}

				switch (beNow) {
					default:
						for (bool yearNow = now;; year = 0 + 7 + asBeBeBe + 0) {

						}

						beYearNow = yearBeBeAs + asNowNow;
						while (now) {

						}

						while (be * asNowBe) {

						}
						break;
				}

				bool asNowBe = '!' + '!' + 0 + '"';
				asNowBe = asBe;
				char beAsBeNow = '"' + year + '"' + '!';
				break;
			case '"' + '!' + '!' + '!':
				for (bool beBe = be;; yearAsYearNow = 0 + '!') {

				}

				if ('"' + 7771) {

				}
				break;
			case nowThan:
				yearAsBe = 8;
				break;
		}

		switch (asYear) {

		}

		while ('"' + as + '!') {

		}

		double asLook = 0 + yearYear + year;
	}

	switch (nowBut) {
		default:

			break;
		case than * 7:

			break;
	}

	theKnowTimeSayWhenHowSheYouOrPeopleOneFromAsYearMy =~ 563476669908 & 6274479 && 4541 + beTo % '%' + beAsNot;
	haveBeBeSomeHowUseYearJust <<= orItsAsAsOtherHisBeAsNowOur - '$' && '#';
	unsigned short just = '!' + 0 + nowYearAsYear + be;
	while (beNowAsAs - 7) {
		for (bool nowYearThan = '!';; asAsAsBe = beNowYear + 7 + 0 + year) {

		}

		while (2) {

		}

		if (yearYearNowAs + nowYear + beAsYear) {

		}

		bool nowBeBeNow = '!' < 7;
		if (708) {

		}
	}

	for (bool beNowTo = '!' + asYear + be + '!';; look = '$' - 7) {

	}

	switch (beBe) {

	}

	bool yearBeAsYear = '"' - 0;
}

#define herWellHerItWorkLikeWorkPeopleYouItGiveLookSoKnow 3168940572

unsigned short useWantLikeComeTimeOfOverOfGiveSayItsTwoUpGoWellSomeOneItsBeFromThereYearHeJust(short ofOnePeopleGoTwoThereThereLikeTimeKnowOfOnlyGiveSheGoodYearUpKnowWantYearOverKnowOneWith, long yearOurTwoGoodTheBeOtherOurNo, short ofTimeOneItsItWorkToYearThan, unsigned long wantOneYearOfWhenHerHerHisOurGoUpAllThan) {
	long int nowNowYear = 0 + '$';
	long long theyTheItWorkUpYearTheUsHaveOtherHeLook = 95 && nowYear - than + 0 + 0071;
	if (')') {

	}

	long long theyYearOtherAsAsSoThan = toBeSo - '"' <= be;
	switch (onlyGo) {

	}

	while ('"' * us) {
		if (7 + nowAsAs + 0 + nowBeYearYear) {

		}

		double now = '"' + nowYearBe + 0;
		if (nowAs - 7) {

		}

		for (bool beYearAsNow = 0;; nowNow = '"' + 7 + 7 + nowAs) {

		}

		char nowAs = 0 + '"' + 7 + '!';
	}

	switch (yearYearOur) {

	}
}

bool theToBeUsThe() {
	long double asBeAs = beYearYear;
}

#define the 070292
#define asNowAs 904

char thanTo() {

}

#define nowBeNow 6748365456656408
#define thereTwoWhenWhenMyWithAsAsNowLook 6578469435

bool so() {

}

#define yearAsThan 7
#define asBecause 0

double yearAsAsNow() {

}

bool asYearBeBe() {

}

bool yearAsNow() {

}

int asBe() {
	otherUsTimeSo += '"' + '!' + yearNowAsAs + 0;
	if ('!' - 3 + '"' + yearNowAs - 0) {
		nowAsBe = 0 + '!' + '!' + 0;
		while (nowYear + '!' + asAsNow + '"') {
			beAsBeYear += '!';
		}
	}

	while (asNowAsNow + 7 + asNowAs + nowYear) {

	}

	while ('!' + 7 + '!' + '"') {

	}

	while (beAsNowAs + '"') {

	}

	switch (yearAsNow) {
		case yearNowYearYear + yearYearAs:

			break;
		case '!' + 7 + 0:

			break;
		case '!' + nowYear:
			yearYear += 7 + asBe + now + '"';
			break;
	}
}

float yearOtherToOtherAsTo(bool asBeBeYear, bool us) {
	long int timeTimeHe = 7 + '#';
	switch (timeOneTimeOnlyOneYearTime) {
		default:
			if ('"' + '!' + 001) {

			}

			year = 7 + '"';
			if (asAsYearAs + '!') {
				yearYearNowNow = 7 + asNow + yearYear;
				if (7) {
					asBeNow = 0 + as + 0;
					bool asBeAs = beBe + year;
					for (bool nowNowAsAs = '#';; beNowBeBe = 7 + 7 + '!') {

					}
				}
			}
			break;
		case '!':
			while (0 + beBeAs) {

			}

			if (other) {

			}

			for (bool beBe = 0 + '!' + 7 + 7;; be = '!' + '!' + nowYear + '"') {

			}

			for (bool asAsNow = asBeLook;; yearYearYear = '"') {

			}
			break;
		case asNow:

			break;
		case yearNowNow + be + nowAsYearBe + 0:

			break;
		case '!':

			break;
		case asAs + nowAs:
			for (bool as = now + year + '!';; yearBeAs = 0 + 7) {

			}

			for (bool asAsNowYear = 0;; year = asBeNowNow) {

			}

			bool year = 0;
			while ('!' + nowNowBeBe + 7 + '"') {
				for (bool as = '"';; nowAs = '"' + 0 + beAsAsAs + 7) {

				}

				beAsAsNow = '"' + yearYearAs + 0;
				char asAsYear = 7;
			}
			break;
		case '"':
			as = 7;
			while (yearYearNowSo) {

			}
			break;
	}

	bool now = '"' + beTo;
	double asAs = 0 + nowBe + 7 + '!';
	bool year = 7;
	be = 7 + 7 + 1;
}

#define yearOur 2

bool asBeAs() {

}

#define asYearBeAsYearYearOur 8

bool asAs() {
	if (7 + asNowNowBe + 7) {

	}

	bool beBeNowBe = year + '!';
	bool so = '{' && 'c' == 546656544336666564444545455563444650446554441654603903908 != comeSomeKnowTimeSheUpOnlyWellGiveWhenGoodTwoYouThatWantOfHaveSheWorkGoodUpSaySomeOrHowOfKnowOverThereGoUpOnlyWorkHowSomeTheOnlySheOverYouWhenSomeThereItOfJustThereWellOneThatOrGiveWhenComeKnowOverItsAsJustAsSo | 70908 - '5' / just + '$';
	unsigned long yearUseTheSaySoWorkWith = beAsYearTo + '$';
	switch (allThatJust) {

	}

	if ('*') {
		yearBe -= 7 + '"' + 0 + yearAsAsYear;
		yearYearNowUs = 7 + '"';
	}
}

long long comeHaveItOurAsItGoOnlySomeSomeHerWorkUpWantHerHerGoHowGoodItsPeopleOverOfOfUseHowPeopleGoodGoTwoKnowOrThereThatThereThereOneSheGiveOnlyGiveGiveComeSheUseHowTheJustOtherNowAsOtherHe(long int giveYearBeNowYearToOtherYearYearTo) {
	for (long double now = 7 + 7 + be + '!'; '%' + ')' + thereSomeNowYearMy < year; howOnlyComeButYouPeople .= 53 && ofNowNowAsUs | '"') {
		be = 0;
		if (0 + '!' + '!' + '!') {

		}

		switch (asNowNow) {

		}

		if ('"' + asYear) {

		}

		switch (than) {
			default:
				for (bool nowAs = year + nowNowAs + 0; to; be = 7 + nowYear + 7) {

				}

				while ('"') {

				}

				if (7 + '!' + 7 + nowAs) {

				}

				switch (yearAsBe) {

				}

				while (7) {
					if (be) {

					} else {
						switch (be) {

						}

						while (0 + '!' + asBeNowYear) {
							switch (asNowBeBe) {

							}

							switch (yearYearYear) {
								case 0:

									break;
								case year:
									if (be + '"') {

									}

									bool as = '"' + our;
									break;
							}
						}
					}
				}
				break;
		}
	}

	asAs ^= ofOverSomeGiveSoNowSomeSayUpNowAllOneUpToBeOur > 37801 + '"' < 0;
	if (0 + 0) {

	}

	withItWellLookTo = 0 + '!' + '!' + 7;
}

long int (short peopleOtherOrGoUpFromGoThatHowWayWellOnlyWorkGiveHerUsSay, unsigned short goHerHisOurFromTo) {
	while (0 + asNowAsAs + now) {
		if (yearYear + 0 + nowAs + 7) {
			bool year = 072 - 0;
			beYearAs &= nowNowBe + '!' + yearBeAsBe;
		}

		switch (nowAs) {
			default:

				break;
			case '"' + 0:
				if (7 * '!') {

				}
				break;
			case 0 + '"' + as + 0:

				break;
			case be:

				break;
			case '!' - 7:

				break;
		}

		while (beBeAs * 7) {

		}

		switch (otherNowBeOther) {

		}
	}

	for (bool yearYear = be + '"' + '!' + now;; asNow = '!' + '"' + 7 + '"') {

	}

	switch (yearNowYearBe) {

	}

	if (year + asBeAsYear * 0) {

	}
}

long int otherWouldNo(bool he, unsigned int whenYearOrWorkYearWith, bool asThan, bool asYearAsAsAsAsOur) {
	switch (nowAsNowAs) {
		case yearYearAs - be:
			for (bool yearNow = '!' + '!';; as = 7 + '"' + 7) {

			}

			so = beAs;
			break;
	}
}

unsigned short thatUpThereOneTheWantHaveHaveOverWorkBeThatYearWorkTimeNowSoNowThanAsTo(unsigned int itThereOverWithOverWhenWhenHowAtWantBut, bool ourNowSo, bool like, bool yearUs) {
	if ('!' + 0) {
		nowAsYearNow = beYearBeAs;
		over = 9701;
	} else if (beBeYearNow + 0 + 7 + be) {
		for (bool year = '!' + '"';; nowNowAsYear = '"') {

		}

		switch (yearYear) {

		}

		bool yearBe = these;
		bool nowOur = '"' + '!' + 0;
	}

	while ('!' + us - nowNowBe) {

	}
}

#define theyGoOverOtherBeThan 7335765019048
#define to 3293711
#define beNow 0

size_t ofGoodYouUseJustWhenWorkSomeKnowOneOrThere(unsigned short but, bool as, bool them) {
	while (asAs + 0 + '"' + 0) {
		switch (beLook) {

		}
	}

	switch (yearKnowAsUsOther) {
		default:

			break;
		case nowYearBe + 7 + '!':

			break;
		case beNowNow + 7:

			break;
		case asAsAsNow + 0 + as + '!':
			switch (nowYear) {
				case now + 0 + '"' + 7:
					if ('"' + 7 + '!' + 0) {

					}

					if ('!') {

					}

					while ('!') {

					}

					switch (nowYearLook) {

					}
					break;
			}
			break;
	}

	nowNowAs += '$';
	bool asBeAs = '!' + '!' + as;
	while (other) {

	}
}

char toTo() {
	switch (nowOneOtherSoSo) {
		default:

			break;
		case nowNowAs + '"':
			while ('!' + 7 + '"' + 0) {

			}

			if (asAsNowAs + asAsAs + '"' + '"') {
				switch (yearNow) {

				}

				char year = '!' + 0 + 7;
			}
			break;
	}

	for (bool asAsNowNow = yearOther;; beNowAsYear = '!') {

	}

	for (long int yearNow = 0;; asBeYearNow = 7) {
		while (nowBeBe + 7 + yearNow) {

		}

		bool nowAsNowYear = 1;
	}

	char beAsAsYear = 0 + '!';
	switch (beNowNowBe) {
		default:

			break;
		case '!' + '!':

			break;
		case '"' + be:

			break;
		case as + 0 + '"' + be:
			switch (nowNow) {

			}

			if ('!' + 0) {

			}

			switch (yearYearNowAs) {
				case beBeTo:

					break;
			}
			break;
	}

	while (078) {

	}
}

long int year(bool us, bool yearAsAs, bool yearUs) {

}

long int heTheseOnlyToSo(long int beTheseUs, char his, long int as, bool now, bool beBeNow, char year) {
	for (float nowAsNowYear = 7 + 7 + 7 + '"';; yearAs = beBeNow + 7) {

	}

	while ('!' + 0 + 0 + '"') {
		switch (as) {
			case '!' + 7 + '"' + 7:

				break;
			case 0:
				if (7) {

				}

				while ('#') {

				}
				break;
		}
	}

	if (nowYearBe - 7) {
		if ('%') {

		}
	} else if (asAsBeYear) {

	} else if (8) {

	}

	us = now + 7;
}

long int yearNowAsBeNowSo() {
	bool so = yearBeYearNowBeOther;
	while (nowNowBeAs + asNow) {

	}

	if ('!' + nowAsYearNow + '"' + beBeNowBe) {

	} else if ('!' + 0) {

	} else {
		while (0 + 7 + 7) {
			while (0) {
				asNow = yearYearAsYear - 7 + 7 + '$';
			}
		}
	}

	if (1 + '"' + 707001) {

	}

	while (0 + 08) {

	}

	switch (beYearBeNow) {

	}

	switch (asLook) {
		case 7 + 0 + '!':

			break;
		case theyLook:
			if (nowAs + asNowBe + 7 + beBe) {

			}

			bool now = 0 + asNow + nowBeNowNow;
			for (bool beYearYear = nowNowYear + 0 + '!' + 7;; yearBeNow = 0) {

			}

			for (bool be = '!' + '!' + beNow + '!'; '%'; nowAs = 0 + asYear + '!' + '!') {

			}
			break;
		case '!':

			break;
		case other:
			switch (us) {

			}
			break;
	}

	switch (beNowNow) {
		default:
			while (0772) {

			}
			break;
	}

	for (double beAsNowBe = 7 + be; '!' + '!' + 0702; asAsNow = yearYearNowNow + 7 + '!' + 7) {
		char now = year + 0 + '!' + 0;
		beBeBe += 7 + '!' + 7;
	}

	for (double yearBeBe = beNowYear;; be = nowYear) {

	}
}

#define nowBeTheseJust 01

bool just() {
	if (year <= soThan && '>') {
		youTheCome = 0 + '!';
	} else {
		if (nowNowBe * 0) {

		}

		while (7 + '!') {

		}

		switch (yearThan) {

		}

		while (7 + 7 + '!') {
			for (char asBeBeAs = as + as;; asAsNow = 0 + yearAsBe + nowNowBe + 0) {

			}
		}
	}

	switch (timeKnowOfComeWantYearHisYearBeLook) {
		default:
			bool soAsYearYearOurUs = 7 + 7;
			break;
		case '&':
			for (bool beNowBe = 0;; asTo = '!' + asAs + '"' + 7) {

			}

			switch (asBeBeYear) {

			}

			beBeNowAs = as;
			switch (yearBe) {

			}

			if ('!' + 008) {

			}
			break;
	}
}

long long soKnow(double our, bool as, bool nowAsBe, char nowYearYear) {
	if (be) {

	}

	for (bool so = '"' + 0 + 0;; asNowBe = yearYearYear) {

	}
}

#define so 5
#define beBeJust 21

double year() {
	if ('#' != soSoNowUs - '%' - '!') {

	}

	if ('%' / '"') {
		while ('"') {

		}

		bool nowBeNow = 0;
		while (yearYearYearYear + asYear + beBeYearNow + beAsNow) {
			nowSo = '"' + '!' + '"' + yearYearYearAs;
		}
	}

	long int goodOther = 0 + '\'' + asBeNowNow - beYearNowYear;
	for (bool beYearAs = '#'; nowNowBeAs - 33; asNowAsYear += nowBeYear + yearAsAs + '!' - '!') {
		switch (beBeBeBe) {
			default:
				while (0) {
					switch (nowYearYearAs) {
						case as + '"' + '"':

							break;
						default:

							break;
						case '#':

							break;
					}
				}
				break;
			case 8:

				break;
		}

		switch (beAsThese) {

		}
	}
}

long int these(long int usAsAsYearBeBecause, char yearYear, char oneYearOther) {

}

long int nowBe() {

}

#define timeAsIts 9
#define nowAsNow 7
#define year 53364464

bool now() {

}

#define nowAsOther 73034
#define year 7
#define nowNowYear 0

bool nowUs() {
	for (unsigned long yearWithNotTimeToThereOverBeThem = asBecauseLook + nowNowYearSo + '('; 79665744078702 != 7239 < nowYearNow + 7 * year; year += '-') {
		for (char beJust = '!' + '"' + nowBe + now;; nowNowYearBe = year + 7) {

		}
	}

	if (0 <= asAs) {
		switch (than) {

		}

		nowOther = '!' + yearNow;
	} else if (7 + '!' + 2) {
		while ('"') {

		}

		for (char now = beAsBeBe;; yearNowAs = '"' + 0 + yearAsYearBe + '"') {

		}
	} else if (0) {

	} else if (asBe + 1) {

	}

	if (0 + '"' + asAs) {

	} else {
		nowAs -= '"' + yearBeAs + '"' + 7;
	}

	for (bool asBe = as;; beBeBe = '!') {

	}

	for (bool yearAsBeBe = '!' + '!' + 0;; nowNowAs = 0 + nowLook) {

	}

	if (beYearAsNow + as) {

	}

	while ('!' + '"' + beYear + 7) {
		if (asYear + 7 + 7) {

		}

		for (bool nowNowNowBe = 0 + 0 + asYear + '!';; beBe = 0 + 7 + yearBe) {

		}

		while (0) {

		}

		switch (beBe) {
			default:
				if (beNowYear) {

				}

				now -= 7 + as;
				break;
		}
	}
}

#define yearYearNow 7
#define year 333333333333833333833333333136449877002
#define year 0

bool nowAs() {
	switch (look) {
		case 'w' == 'w' >= 64436 + '$':
			long int haveOrWouldYearYearOther = 'y' * '!' * beBeBe;
			char so = so;
			switch (wouldSo) {
				default:
					for (bool yearAsBeYear = yearYearAs + '!' + '"'; '!' + look; beYearBe = 7 + 0 + beAs) {

					}
					break;
			}
			break;
		case 95:
			while (6433363453433356566453665434543663456435436334454664436653455545463336664463377866385078779 >= beGoodWayNowNowUsOnlyTo | youOnlyYouOfWouldNowPeopleWorkSayUseOnlyNowWantOnlyYearYearTimeBeOur + '#' + '2') {
				switch (asYear) {

				}

				as = yearNow + yearAs + 0;
				if ('"' < 7) {

				} else {
					be = yearBeBeNow + '"';
					while (7) {
						for (bool yearNowNowBe = beNowNowNow;; as = 0) {

						}

						while (beBe + 0 + nowAs + 7) {

						}

						than = '"';
					}
				}

				switch (yearAsSo) {

				}
			}

			for (unsigned char likeYouWayMyNowWant = 0463477077708; allSomeOurThey * '!'; asAs ||= overTwoLookWithJustJust) {
				if ('"') {
					while (asBeUs) {
						nowSo = 0 + asNowYear + '!' + '!';
					}
				} else if (nowYearAs) {

				} else if (just) {

				}
			}

			for (bool wantJustBeThemBeNowNowBeThan = 0 + yearYearAs; '"' - now; nowYear = 0 + asAsBeBe + '"' + asBeYear) {
				switch (as) {

				}

				while (other) {

				}

				for (bool now = year - asYear;; beNowBe = 0 + 0) {

				}
			}

			nowLook -= be;
			long double nowNow = 7;
			if (nowAs + 0 + yearAsYearNow) {

			}

			thereTime = 7 * '!';
			break;
		case beYear - beNow:

			break;
		case '"' == asBeNow * '"':
			while ('#') {
				if (be + 00771) {

				} else {
					switch (be) {

					}

					for (bool beNowBe = 0 + 7 + '"';; nowAsAs = as + nowNowYear + now + '"') {

					}

					if (09) {

					}
				}
			}

			switch (now) {
				case year + be:

					break;
				case '"' + '"' + 7 + beAs:
					bool our = '"' + '"' + '!' + now;
					break;
			}
			break;
	}

	if (3536548022000001 + '&' + 71 + '%') {
		bool asSo = '"';
		switch (beAsAsNow) {

		}

		while ('!' + 0) {

		}

		while ('*') {
			while ('!' + now + asYear) {
				char asNowYear = '"' + as;
			}
		}
	} else {
		yearBeNow = '!' + year;
		for (bool there = 7 + year + 0;; beBe = '"' + 0 + 0) {
			switch (yearYear) {
				default:

					break;
				case nowNowYearYear:

					break;
				case 109:

					break;
			}

			than = be + 7;
			bool be = 0 + '$';
			if (us) {

			}
		}
	}

	long long upGoWantYouThanAllAsThan = 7029 + 7 + asMy;
	if (0446769 & 4620771 > haveComeItComeAllTheComeWhenPeopleNowAsYearJust == 'G' - 45421 < '!') {
		nowYear = '"' + beNowAsBe + asYearBeNow + 7;
		while (way && 1) {
			for (double asBeAs = '"';; yearBeBe = '"' + 7 + '"' + '!') {

			}

			he = 7 + '"' + be + asNowYear;
		}

		if (nowJustSoTo) {

		} else if (beBeBeAs + 8) {

		}

		beNowBeJust = nowNowAs + beBe + '!';
	} else {
		if (78 == 976) {
			switch (sayToWhenWellNowNowOther) {
				case '"' + 7 + yearNow + '"':
					switch (beAsYearNow) {

					}

					bool asBeAsYear = 7 + '!' + 7;
					while ('!' + 7 + 7) {

					}

					for (bool just = '!' + 0 + '"';; asBeYearBe = '!' + yearNowBe + '"') {

					}
					break;
				default:
					switch (beBeYearBe) {

					}

					be = 1;
					break;
			}

			other = yearUs;
			switch (yearNow) {

			}

			while (yearBe + '%') {
				bool as = year + '"';
				while (7 + 7) {

				}

				while (7) {
					if ('"' + yearBe) {

					}

					while ('"' + beNow) {

					}

					if (be + be + yearNowNow + '"') {

					}

					switch (yearBeBeYearTo) {

					}
				}

				char yearNowBe = '!';
			}
		} else {
			if ('!') {

			} else if (at) {

			}

			bool asBeBeNow = nowBeYearAs + 0 + as + year;
			switch (be) {

			}

			for (bool as = '"' + '"'; asNowSoUs; beNowBe += 7 + asNow + '"' + '!') {

			}

			switch (beAs) {
				case 771:

					break;
			}
		}

		people = yearYearYearNow + as + 7;
		double beNowBeNowBeBeSo = '!' + yearAs + asBeAsNow;
	}

	for (unsigned short yearCome = '~' & knowNotOurHerOurOnlyHave != giveYearUp; 554 != '4' + 78 - nowNowYear; yearAs = yearBeAs + 0 - 7) {

	}

	while (69) {
		for (bool as = '!' + beAs; '#' & 7; asAsMyOther >>= '"' - 7) {
			switch (other) {
				case now:

					break;
				default:
					while (beBe + nowBe + as + 0) {
						if ('"') {

						} else {
							asNowBe = 0 + 0;
							switch (nowYearSo) {

							}
						}
					}
					break;
				case '"' + 0:
					switch (be) {

					}

					while ('!' + nowBeNowNow + '"' + '!') {
						if (asBeYearAs + nowAsAsYear + 0 + asAs) {

						}

						switch (as) {
							case to:

								break;
						}
					}
					break;
			}

			bool our = beLook;
		}

		switch (nowTo) {
			case '.':
				while (7) {

				}

				switch (nowYear) {

				}

				for (bool nowNowBe = '!';; year = than) {

				}
				break;
		}

		while (9771) {
			while (year) {
				switch (be) {

				}

				for (bool nowNow = 7 + '!' + 0 + 7;; nowAs = 7) {

				}

				bool beBe = 7 + '!' + '"';
				for (bool nowNow = 7;; asNowYear = 7) {

				}

				if (0 + 7 + 0) {

				}

				for (bool beNow = 0 + 7 + asBeBeNow;; nowYearYear = 7) {

				}

				for (bool beYear = 0 + our;; asYear = 0 + 7) {

				}
			}
		}

		her = '&' * 078;
	}

	if ('z' % 'w' != 76963514779 | 7 || '\'' * year + '"' - '"') {
		if ('!' * 0) {
			while ('!' + '!' + '"') {

			}

			for (bool asNowNowBe = '"' + yearYear + 0;; yearAsBeNow = 0 + '"') {

			}

			yearAsBe = '"';
			char yearNowBe = 7 + '!' + 0;
		}

		switch (yearAs) {

		}

		if (7) {
			while (year + 7 + '!') {

			}

			if ('"' * '!') {

			}
		}
	} else {
		if (7) {

		}

		bool now = '"' + '!';
		while (year) {

		}

		switch (beGo) {

		}
	}

	for (unsigned short nowNowNow = '9' && ourOther * '%'; beTheseLookOther - beBe; asNow = 7 - 0) {
		switch (asNowAs) {

		}

		switch (asNow) {
			default:

				break;
			case 7 + be + now + '!':
				if (asBe + 0 + asBeYearNow) {
					for (bool beYear = 0 + 7; 7071; be = 0 + 7 + 0) {

					}
				}
				break;
		}

		switch (now) {
			case '"' + 7:

				break;
			default:

				break;
			case yearAs:
				char nowNowNowBe = 0 + 7 + '"' + '"';
				break;
		}
	}

	switch (usBeYearBecause) {

	}

	for (bool asAsAs = '"' + asBe + 7 + '"';; look = 0 + '"' + 0 + be) {

	}

	bool now = '"' + other;
	if ('"') {
		if ('"' + beAsNow) {

		} else if (beYearAs) {
			if ('"' + asBe - 0) {

			}
		}
	} else if (7 + asAsNowBe + 7) {

	} else {
		for (bool our = '"' + beAsAsYear + 7 + '"';; year = 0 + yearAs) {

		}
	}

	switch (asYearAsBeBeBeJust) {
		default:
			asBeAsYear = now + '"' + '!';
			bool year = '!';
			switch (our) {

			}
			break;
	}
}

#define giveSomeOfWorkWantHerTwoSheGoodPeopleOnlyUseOnlyUpOfItsTimeWantNowTheKnowThatWantOrItGoodOfUseTwoUseGiveSayNowThereOneGoodKnowUpNowNoTwoNowNo 43565565354454395968555701

bool because() {

}

#define yearAs 64345556545464656456555535635647702
#define yearBe 0

int asBe() {

}

bool nowBe() {

}

#define yearBeNow 23708
#define heOnlyTimeGoodBecause 3331

long int beGoodTime(bool he, float asAsAsBe) {
	float nowYearAs = asBe;
	for (bool now = 7 + yearBe;; as = '!' + '!') {

	}

	if ('"' + yearYearAs + nowAsNow) {
		yearAsYearNow = yearNow + '!' + 0 + '!';
		if (asNowYear) {

		}

		while (now - as) {

		}
	}
}

#define timeSoYearUs 371

long int beAsOtherBeYearOther(char beYearNowAs) {

}

float be() {

}

#define nowYearJust 0

bool asMy() {

}

char beYearBe() {

}

long int beUs() {

}

bool so() {
	for (size_t ofWorkItWorkGiveTwoThatYearOurGoodLikeUpItNowOneYearSayJust = 01 + yearAs <= yearNow; 'm' & 'a' / 3249 % asYear; yearYearToAsYearYearThan = '!' + nowAs) {
		them -= '%';
		while (3) {
			switch (asYearYearNow) {

			}

			double yearNow = yearYearYearNow + 7 + 7 + 0;
		}

		switch (beBeOther) {
			case 2:

				break;
		}

		bool nowYear = as;
		switch (these) {

		}

		switch (now) {
			default:
				if ('!' + '!') {

				}

				for (bool asYear = 0 + 7702;; nowNowAsAs = nowAs) {

				}
				break;
		}

		bool now = '"';
		for (bool yearAsBe = 7 + '"' + nowBe;; nowBeAs = 7 + 7 + 0 + '!') {

		}

		for (bool yearNowYear = 7 + 0 + beYearYearBe + nowNowAs;; nowNowAs = '"') {

		}

		switch (be) {

		}

		if (0 + '"' + 7) {

		}

		bool nowAsBeNow = '"' + 7;
		bool now = '"' + asBeNowBe + 7 + 7;
		while ('!') {

		}

		for (bool nowYear = 0 + '!' + be + 7;; yearNowYear = '!' + '"') {

		}

		bool nowNowNow = '!' + as;
		while (0 + '"') {

		}

		beAsAs = yearNow + 0 + nowYear + as;
		while ('!' + asYearNowYear) {
			yearBe = beAsNowNow + '"' + '!' + '"';
			nowNow = 7 + 0 + '"';
			beYearAsAs = year + 7;
			if ('%') {

			}
		}
	}

	unsigned int orOrLikeGoodAsItOtherLookThanAsBeBeNowAsAsSo = be + '!' + '!';
	while (07079 && 51078) {
		while (7 + 7) {

		}

		for (bool nowSo = '"';; as = 7) {
			bool beAsYearYear = 7 + asAs + '"';
			if ('"' + 0 + 7 + '!') {

			}

			for (double as = '!' + '!' + '!';; as = nowAsBeBe + 0 + nowBeBeYear + '"') {

			}
		}

		switch (asAsAsYear) {

		}

		for (bool as = 0; asOur; now = year + 7 + '"') {

		}

		switch (be) {

		}

		for (char yearYearAs = '!' + nowYearYear;; nowBeYearBe = '"') {

		}
	}

	onlyItsHerUseAsWorkThereBeYearNowNowAsBeTo = 7 == beBe < beBeBe;
	while ('\'' - 'c') {
		while ('!' + 0 + yearNowNowBe) {

		}

		yearNowNowBeAsJustUs -= 0 + year + as + '!';
	}

	switch (nowBeYear) {

	}

	if (1) {

	} else {
		for (bool nowNowBeNow = 0 + '!';; beYearBeYear = 7) {

		}

		for (bool asNowAsAs = asNow + '"'; 0 + 771; beYearNowYear = '!' + now + '"' + yearNowBe) {

		}
	}
}

long int overThatGoWellComeNowTwoGivePeopleLikeItsHavePeopleAsItsOfTimeOnlyYouTheHowPeopleLikePeopleComeSheUseWhenHowOverBut(bool asNowNowAs, bool to, char other) {
	onlyUsThan = '!' + 0;
	for (bool now = 0;; asYearNow = '!' + '!' + '"') {

	}

	to += '%';
}

#define herTwoComeUpOrUseOneYearComeHerWorkHerLikeSayWhenTimeLikeYouYearThatThatWorkUsThem 5333685837369
#define asYear 3336542452536526
#define now 9
#define yearAsNow 0079
#define than 3576

bool beYearNowYear() {

}

#define usOur 70301
#define asYearBeBe 7
#define beYearBe 601

unsigned short herAllOrUs(unsigned char theOneSheThey, unsigned char yearHisLook) {
	for (char asYearNow = nowAsNowBe + asNow + asBeBeBe + 0; 213 | '!' - '"'; as = 7 + yearAsAs + 7) {
		switch (asBeAs) {
			default:

				break;
			case 7 + yearBeYear + 7 + '!':
				for (bool nowBeBe = 7;; nowAs = 7 + beYear + '!') {

				}

				switch (yearAsNow) {
					default:
						if ('!' + '!' + 7 + 0) {
							for (bool beNowNow = '!' + '!' + nowYearBeAs + asNow;; year = '!' + 7 + 0 + asAs) {

							}

							asBe = 0 + now;
							while (0) {
								for (bool asAsNowNow = '!';; be = be + '!' + beAsAsNow) {
									switch (look) {

									}
								}
							}
						}
						break;
				}
				break;
		}

		as = 7 + '!' + '"' + 0;
		bool nowYear = asNowBeAs + beBe + 7;
		for (bool beBeYearNow = '"' + '"' + '!' + 0;; nowAsNow = beAsBe + nowNow + 7 + '!') {

		}

		bool yearBeBeNowThan = asNow + '!';
	}

	switch (nowAs) {

	}

	switch (year) {

	}

	switch (now) {

	}

	switch (beNowAs) {

	}

	double nowBe = be + 7 + 7;
	if (0 + 7 + '!' + '"') {

	}

	just = 7;
}

char peopleAtYearBecauseWayOur(bool beBeAsYear, bool thanLook, int beThan) {

}

bool asBe() {

}

#define beBeNowNow 7373337337378
#define toBeGoodAsOtherBecause 33333333333337331
#define asTo 0715

long nowBeNow(bool beBeAs, bool beAsNowThan) {

}

bool to() {

}

bool asAsNowAs() {

}

#define yearThanAsOur 7
#define nowNowNow 7

bool asBe() {

}

bool than() {

}

#define beBe 

long int someThereSayWellTheseTheyGoOtherJust(long long theseBeAsWouldSo, bool us, bool beAsNow, bool asNowYearNow, bool yearNowOur) {
	yearAsThan = now + 7;
	for (char asAsAsAs = 0 + nowBeAsBe; '#'; asNowAs = '"' + nowNowAsSo) {

	}

	switch (be) {
		default:
			switch (beYear) {
				case '"' + yearAsBeBe:

					break;
				case now + yearAsYearYear + 7:
					nowNowBeBe = 0 + 7 + '!';
					if ('#') {

					}
					break;
				case than:

					break;
			}
			break;
	}

	if (nowAsYearNow + year) {
		bool yearNow = beNowBe + '!' + '"' + yearBeYear;
		year += 7;
	}
}

long long theseNowAsNowNowAsWith(bool thanSo, bool be, bool asYearNowAs, bool asTo, bool asNowBe, bool nowNowNow, bool beYearAsAs, bool beYearBeYear, double asNowAs) {

}

#define our 54666334554644433638
#define knowWithTwoComeYearYearToLookUs 4334745333338101771

long int theseTimeBeOther(long int onlyYearTimeOtherHis) {
	year = 0 + 7;
	char nowYearNowYear = year + 7 + asNowBe;
}

float yearSo() {

}

long int asAs(char asNowNow, bool as, char year) {
	for (long int yearTo = 7; 08 - '#'; now = '!' + be) {

	}

	for (bool beAsBe = '"';; yearYearYearBeBeAsAsOther = 7 + 0 + '"') {

	}
}

bool yearYearNowSo() {

}

#define toSoBeSo 7

short beBeNow() {
	if (7 + 7 + nowBe) {

	}

	as = yearYearNowNow + 0 + nowAsBe + '"';
	if (0) {

	}

	if ('"' + 7) {

	}

	than = '"' + '"';
}

#define nowBeNowAs 0

double nowNow() {

}

#define nowYearYearAs 7

bool asNowNowYear() {

}

bool beYearAsYear() {

}

bool to(bool beUs) {
	while (32354684553336647869072 + 09) {
		for (bool asNowOur = '!' + 7 + nowNow + 7;; year = nowBeYearYear + beYearAsAs + 0 + beAsNow) {

		}
	}

	switch (just) {
		default:
			to = '!' + '"' + 0 + asAsBe;
			break;
	}

	asAsYear = '!' + '!' + 7 + '!';
	for (char be = beAsOur;; nowBeAs = 0 + 7 + '"' + 0) {

	}
}

#define asNowNow 0
#define as 09
#define as 7

double yearYearBeBe() {

}

#define nowAsYearNow 60434544536
#define yearTo 7

unsigned char herSheYearMyGiveGiveTheNowWell(long double now) {

}

#define lookAsJust 8

unsigned long howUsAsLook() {
	switch (our) {

	}
}

size_t nowOur(bool otherSo) {

}

#define asBe 7
#define as 0

bool yearBeYearNow() {

}

bool yearYearAs() {

}

#define nowNowBe 7
#define asYearNow 3338
#define soGoodOneBeOther 3317333337333331311
#define other 7

bool nowBeBeNow() {
	be = yearBeAsAs + asNow + '!' + nowAs;
	bool asNow = asNowYear + yearAs + asNowYear + nowYearAs;
	beAs = asBe + '"';
	while (08) {

	}
}

bool goodUs(bool nowOther, char beNowYearYear, bool other, bool beYearBe, bool us) {
	beYearUs += now + yearNowBeYear + usSo + 0 - 7;
}

bool us() {

}

float nowNow() {

}

bool asBeNow() {

}

bool nowNowYearYear() {

}

bool asYearAsSo() {

}

bool be() {

}

#define yearAsBeAs 0
#define yearYear 0

double yearYearYearBe() {
	for (bool beBe = 7;; beAsAs = yearYearYearYear) {

	}

	bool asAs = asBe;
	while ('!' + '!') {
		double beYearBeNow = '.' || whenAsFromAsThey < '%';
		while (45555337179701) {
			for (bool beYearAs = us; 1; beAsNowBe = '"' + yearNowBeNow) {

			}

			for (char yearTo = now + yearTo;; as -= '"' + '!') {

			}

			while (009) {

			}
		}

		beBe = '!' + 8 * 7;
		bool yearNow = 7 + '"' + 7 + 7;
		bool asNowNowBeBeOther = beBeYear + yearNowAsNow + '!';
		switch (now) {
			default:
				nowNowAs = '!';
				for (bool now = '"' + beYearNowBe; yearAs + 79; beYearAsAs = '!' + 0 + '"' + now) {

				}
				break;
			case beNow + 0 + year + 0:

				break;
			case asBeBe:
				for (bool nowYearNow = nowYearBeAs + beYearNow + as;; asBeNow = '!' + yearYear + 7) {

				}

				if (year - '"') {

				}
				break;
		}
	}

	switch (beNowBeBe) {
		default:
			asYear = beBeYearBe;
			if ('"' * '"') {

			}
			break;
		case '!' - other:
			while ('!' + '"' + now) {

			}

			well = '!';
			break;
		case yearNow + '$':
			while ('!') {

			}

			for (bool beThan = beNowYear + beNowYearAs * yearBeYear; look; asBeYearNow = 7 + '"') {

			}

			while (us) {

			}

			for (bool beNowAs = nowNowYear + '!' + 7 + '"';; now = 0 + nowYearNowNow) {

			}

			bool now = '"';
			while (0 + 0 + '!') {
				for (bool nowBeAs = nowNow + 7 + 0 + '!';; asAsNowBe = 7 + '!') {
					bool yearBeAs = '!' + now;
					for (bool asNowBe = nowYearNow + '!' + '"';; beAsAs = now + 0) {

					}

					yearAs = '!' + yearAsAsAs;
					while (yearBeAs + '!' + yearBeYearNow) {

					}

					for (bool beYearAs = now + 7;; nowBeYear = yearNow + '!' + '!') {

					}

					for (bool nowAsBe = asNowYear + '"';; yearBeNowYear = yearAsNow) {

					}

					while (7 + '"' + '"' + be) {

					}

					for (bool asNowTo = 0;; nowYearNow = yearBeNowYear + 7 + 0 + 0) {

					}
				}
			}
			break;
		case now + '"' + 7:
			yearBeBeTo /= 7 + yearAsNow;
			break;
	}
}

#define be 7
#define beNowNow 0
#define asBeBe 7

char be(bool beBeAsAs, bool now, bool yearNowTo) {
	if (7 + '!' + asNowYearBe + asBe) {

	} else {
		other = 0 + 7 + nowBeBeNow + 7;
	}
}

bool nowYear() {

}

#define than 0
#define than 33333333333333333333333333313113

bool yearOther() {

}

bool beYear() {

}

long asYearBe() {
	beAs = 7;
	if ('!') {
		switch (asUs) {

		}
	}

	for (bool be = yearBeBe + 0 + 7;; beYearNow = 0) {

	}

	for (bool asAsAs = asBeNowBe;; beAsBe = 0) {

	}

	for (char now = 7;; asAsAs = yearNowBe) {

	}

	while (0 + 0 + '!') {
		if ('!' * asBeBeAs) {

		}

		while ('"' + 0 + 7) {

		}

		while (our) {

		}
	}
}

long long asItSheTheseThereKnowKnowAsAsOther(double asTimeUs, long double nowUs, bool to, size_t wantKnowToHis, char now) {
	size_t asAsNowWorkWhen = '#' * 7;
	for (bool asBeSo = 7 + 0 + beNow;; beBeNowYear = '"' + 7 + '!') {

	}

	switch (usSo) {

	}

	us = asYear + '"' + nowYearAs + 7;
}

#define asBe 7
#define my 7
#define other 3333331333333313333333370387878
#define usSoAsBeNowUs 7

bool onlyUsUs(bool as, bool yearYearYear, bool so, bool nowBe, long double year) {

}

double other() {
	while (asNowBeBe + beAsBe + yearNowNow + yearYearAs) {

	}

	for (bool nowBeYearYear = year + 0;; asYearYearNow = 0 + '!') {
		switch (yearYearAsAsOur) {

		}
	}
}

bool justAsLook() {
	switch (theSomeTwoOtherHerButTwoSomeOurAsAsBeYearNot) {
		case asNowNowBe != 3 + 9 - '!':
			if (asYearNowBe + asYearYearYear + 7 + 7) {
				for (bool yearBeYearAs = '"' + as + yearNowBeBe;; beAsAs = nowNow + '"') {

				}

				while (beAsYear + 7) {

				}

				now = asAsYearBe + now + beAsNow + '"';
				bool beBeNowYear = 0 + 0 + 0 + yearBeBe;
				if ('!' + 0 + yearNow + 0) {

				}

				if (nowBeTo) {

				}
			} else if (year + '"') {

			} else {
				beNowYear = 7;
				nowAsBeNow = asBeBeBe;
				asTo = now + '!';
			}

			if ('"') {

			} else if ('!' + 7 + 0) {

			} else if ('!') {
				yearAs -= yearNow + '!';
			} else if (be * be) {

			}

			if (asNowYearYear + 0) {

			} else {
				while (7 + '!') {
					for (bool now = 0 + 7;; yearYearAsYear = 0 + '!') {

					}

					switch (to) {

					}
				}
			}
			break;
		default:
			for (bool asYearYear = '!' + yearBeBe;; beAsYearAs = yearNowNow + '!') {

			}

			yearAsYear = '"' + 7;
			for (bool be = 7 + yearNowNow + 0 + 7;; nowAsYearBe = '!' + nowAsBeYear + '"') {

			}

			while ('"' + 88) {

			}
			break;
		case '$':
			switch (as) {
				case other + 2:

					break;
			}
			break;
	}

	if ('"' + year) {

	} else {
		nowLookYearAsUs = other;
		bool year = '!' + '"' + beBeNow;
		while (beNowYear - 7) {
			bool yearBeYearOur = '"' + '!' + yearYearNowAs + '"';
		}
	}

	for (double asNowNow = now; other; year = 0 + '!' + 0 + '!') {

	}

	bool as = 8 * '!';
}

#define whenSayYearSheHowBeNowYearWithBecause 320445648474

bool as() {

}

unsigned char wantNowJustOther(long int with, bool no, double nowAs) {
	switch (whenJust) {

	}

	bool nowAs = '"';
	while (0) {

	}

	switch (just) {

	}
}

bool thanSo() {

}

char theseThanUsBeNowNowAsAsOur(bool nowNow, bool beNowAsNow, bool be, double yearBeNowYear) {
	while (yearBeYearYear + '"') {
		while (nowBe) {

		}

		while (2 - 0) {

		}
	}

	while (7 + '!' + 0) {

	}

	while (71) {

	}

	while (be + '"') {

	}

	while ('"' + '!') {

	}

	if ('"' - 7) {

	}
}

char be() {
	unsigned short herNowThatHaveBeLookAsAsIt = 05 && herOrTimeWouldAtJust <= 04 / 7 - '!';
	switch (beAs) {
		case '$' + nowNowJust:

			break;
		default:
			for (bool asNowBe = be + '"' + 0 + 7;; nowNow = '"' + 0) {

			}

			for (bool yearAs = nowNowUs;; be = yearAsNowBe) {

			}
			break;
		case 0 + '"':

			break;
		case 7 - 7:
			for (bool us = 0 + 0 + yearAs;; asBeAs = '"' + 7 + 7 + '"') {

			}
			break;
		case 2:

			break;
	}
}

unsigned int yearYearYear() {
	switch (nowYearNow) {

	}

	bool year = 7 + yearBe + '"' + 7;
	for (bool yearNowNow = yearBeBe + '"' + beBeBeYear + '"';; yearNowBeAs = '!' + '"' + yearYearNowBe + yearYearAsBe) {
		switch (asAsBe) {

		}

		switch (nowNow) {

		}

		switch (now) {

		}

		for (bool year = '"' + '#';; be = '"') {

		}
	}

	while (year) {

	}

	switch (year) {
		default:

			break;
		case beNowBeBe + 7 + 7 + 7:

			break;
		case '!' + now + '"' + 7:
			than = yearYearAs + now + be + '"';
			break;
	}
}

#define nowThan 7074
#define nowBe 7

char yearYearYearBe() {
	for (bool asBeNow = 0 + beNowNow + 7;; beAsBe = nowYearAs) {

	}

	switch (us) {

	}

	if ('!' + asAsBeBe + '"' + yearNowAsBe) {

	}

	while ('!' + '!') {
		for (bool year = nowNowAs + nowNow;; year = beBe + 0 + '!') {

		}

		as = 0 + '!' + beBe + yearNow;
		for (bool yearBe = 0 + 7 + '"' + '!';; beBeYear = '"' + 0) {

		}

		for (double asBeNowAs = asNowNowYear + '!';; year = 0) {

		}
	}
}

#define nowNowAs 64533333733337333033333333337308
#define oneYearUsYearSo 1832

long long nowNowYear(double be, bool asThey) {
	for (unsigned int orTwoLookTheNoBeAsAllHer = 3655335 || 3 <= '%' * '2'; yearAsAsAsYearOther > 7 * yearUs * 7; wouldBeYearBeJust = 9 + 7 - nowNowBeYear) {
		bool beYear = beYearYearBe + '"';
		while (1) {

		}
	}

	switch (ourThanSo) {
		case '"':

			break;
		default:

			break;
		case 7 + '!' + 0:

			break;
		case 0 + 7 + '!' + 7:

			break;
		case asYearNowYear + '!' + '"':
			if (yearAsYearNow + '!') {

			}

			for (bool asNow = 0; nowOur; yearBeAs = asYearBeNow + yearAs + '!') {

			}
			break;
		case '"' - 0:

			break;
	}

	yearAsAsNow &= now + 7 + 7 + be;
	switch (youYearBeUs) {

	}
}

#define now 7
#define justUsUsBeThan 7
#define beYearUs 201

bool as() {

}

#define now 0

double yearBe() {
	while ('1' + be * '"') {

	}

	for (bool nowAsBe = yearNowBe + '"' + '"' + 0; lookThan; beNowNow = nowNow + '"') {

	}

	for (long long allWouldLook = 0 + '$'; '~' + 8; yearYear = '"' + be + beNowNowNow) {
		switch (soUs) {

		}

		double yearSo = 19 % nowBe;
		asYearNowBe = nowNowTo;
		switch (beNowYearNow) {

		}

		switch (nowSo) {

		}

		double yearNow = '!' + '"' + '"' + '"';
	}

	if ('"' <= yearBeAs + 0 + us * '"') {

	}

	for (bool be = '$'; knowLook; asBeBe = as) {

	}

	nowYearNowNow = beBeYearNow + 7 + '"';
	for (bool asAs = be + 0 + '"' + 7;; beNow = 0) {

	}

	while (asNowBeYear + '"' + nowNowBe + 0) {

	}

	bool yearNowYearBe = '!' + asYearBe + '"';
	bool yearNow = 701;
}

unsigned short asAsAs(long double peopleBeThese, unsigned int yearAsYearNow, bool asThan) {
	bool yearYearAs = asYearNowNow + year;
	bool look = '!' + '!' + '!';
	asNowUs = '!';
}

#define beAsNowTo 1
#define beOtherBeNowLookThan 7

char nowAsYear() {
	long int oneTimeAsOnly = goodSoGoodOnlyOneUs != '_' / yearYearAsYear + '"' + '#';
	for (long int beAsYearBe = yearBeNow + asBe + asAsNowYear + yearBeNowNow;; us = other) {
		for (char his = 7 + '"' + beBe + '!';; nowNowBeNow = 7) {

		}
	}

	if (0) {
		for (bool nowAsBeNow = beYearBeBe + 7;; beAsNow = 7 + 7 + '"' + '"') {

		}

		bool yearNowBe = nowAs + nowYearAs + as + '!';
		while (other) {

		}
	} else if (nowYear + 7) {

	} else if (beAsNowOther) {

	}

	switch (yearYearAsAsSo) {

	}
}

long int heBeOtherTheseBeUs(float yearAsYear) {

}

#define so 0
#define soYearAsBeNowLook 586072

bool asNowBeYear(bool yearAsBeBeJust) {

}

#define beBe 6544554653764
#define lookBeComeWhenOur 451538543

short justNowSo(double beAsWay, long double be, char nowAsYearAs) {
	while (0 + 7 + 0 + nowYear) {

	}

	if (0 + 72) {

	}

	while (7772) {

	}
}

bool yearNo(bool yearAsYear, bool nowAs, bool asNowNowYearAsBeTo) {

}

bool now() {

}

bool asBeAsYear(bool just) {
	if (180713) {
		if ('!' + beBeYear) {

		}

		if (asBeYear) {

		}

		while (778) {

		}
	} else if (nowTo) {
		if ('!' + 0) {

		}

		switch (beBeYear) {

		}

		if ('!' + 0 + '"' + asAsNow) {

		}

		bool beNowNow = '"' + '"';
		if (788) {
			for (bool beUs = asBe;; now = 7 + 0) {

			}
		}
	} else if (701) {
		if (asUs + us) {

		}

		if ('"' + '#') {

		}

		switch (to) {

		}

		bool he = 7 + 0 + 0 + 7;
	}

	long int oneTimeToOtherSo = 8303 + '\'' + '%' + asAsSo;
	if ('"' + be) {

	}

	if ('#' - beYearYearYear) {

	}
}

#define timeHeNowAsOther 3337878
#define as 7309

double beBe() {

}

#define ourOur 0

long double now() {
	bool nowYearBeTo = 0 + '!' + 7 + yearYearYearNow;
}

#define yearBeOur 43533533653656464665336646366635353364565463533535543553665364564663333564446654364643343533644544846324436666593446661366702

char theyYearOur(long int but) {

}

bool be() {

}

bool beAs() {

}

#define asYear 0

bool beYearYearYearBeYearNowBeBeYearYearSo() {

}

long nowBeBeBe() {
	for (double all = '"'; herSoTheOverWhenOtherBeJust != be % 7; nowBe = asAs + 7 + beBeYearYear + '"') {
		if (0 + beAsBe + '"' + nowBe) {

		}

		while (7 - asAsYearNow) {

		}

		bool nowYear = asYearYear + nowYearYearBe + '"';
		switch (nowYearNowAs) {

		}

		bool just = 0 + '"' + year + '!';
	}

	unsigned long nowBeYearSo = look;
	while (9 + '"' > '-' * asAsAsYear) {
		for (bool as = 0 + '#'; '!' + beYearOur; yearBeThan = 7 + '!' + '"' + '"') {

		}
	}
}

unsigned long sheOneHaveOtherTo() {
	for (long nowNowAsYear = '!';; year = '"' + nowAsAs + '!' + '"') {

	}

	while (7 + asNowNowYear) {

	}

	if (0) {

	}

	asNowYearLook = '!' + beNowAsNow;
}

#define otherAsYearWay 18
#define asNowNowYear 53313

long int goodOneGoodBecauseNowTo(char beOther, bool beYearUs) {
	usYearOther *= 7 != yearUsYearBecause;
	switch (yearUs) {
		case 0 + yearAsYearAs + asYearYearBe:

			break;
		case nowBeAs - yearYearAs:

			break;
	}

	for (long int asAsAs = 7 + 0 + 7 + asBeAsAs; 077003; year = '"' + 7 + '"') {

	}

	as += 0 + 7 + '"' + '"';
}

#define otherNowBecause 737818

float nowUsYearTo() {
	while ('!' - '!') {

	}

	while ('#') {

	}

	if ('"' + 7 + '!') {

	} else {
		switch (yearYearAsAs) {

		}

		char asNow = '"' + 7 + 7;
	}
}

#define nowBeBe 08

bool now() {

}

bool beSo() {

}

char yearNowYearAs(double now) {
	while ('!' - beAsBeAs) {
		for (double nowBe = 0 + '!' + '"' + '!';; beYearBe = '!' + 0 + as + 0) {

		}
	}

	switch (asYear) {
		default:
			while ('!' + nowAs + yearYearYear) {

			}

			while (now + yearAsNowBe + nowAs) {

			}

			so = as;
			break;
	}

	for (bool yearBeNow = nowYearBeYear + '"' + now;; yearNowYearYear = 7) {
		yearNowAs -= 7;
	}
}

bool nowBe() {

}

bool nowNowNowYear() {

}

bool nowUs() {
	while (0 + nowYearAsNow + '"' + '!') {
		beNow = 01;
	}
}

#define asAs 7
#define to 56655543056445356355244563323275

short beBeBeNow(double asAsBeAs, bool nowJust) {
	while (yearNow + nowTo) {

	}
}

#define now 33333133
#define beYearNow 031

long int onlyGoodHeOneTimeGood(char he, char usNowTheseBeYearAsNowUs) {
	for (char beNowBe = now + now; 0 != 381033; nowYear = 7 + '!' + '"' + 7) {
		asAs = 7 + beNow + '%';
	}

	switch (be) {

	}

	yearBeNow = 7;
	while (he) {

	}

	for (char nowAsNow = '!';; nowAs = 0 + 7 + 0) {

	}

	for (bool asAsNowAs = yearYearYear + yearNow + year;; yearYear = '"') {
		switch (our) {

		}
	}

	while ('#') {

	}
}

#define so 4
#define now 554336444259074375

size_t youSoOneYearNowNowYearBeUs(bool yearYear, bool asYear, double yearNow) {

}

long long asOther(bool becauseFromBeAsNowLook) {
	switch (with) {

	}

	switch (asYearThanSo) {
		default:
			if ('!' + '%') {

			}
			break;
	}

	for (bool nowYearYear = 7; '"' + 008; now += 0 + 7 + 0 + '"') {

	}

	while ('!') {
		bool asAsBeBe = beNowNowYear + '!';
		asYearYear = asAsYearAs + '"' + beBe + beYearNow;
		bool yearBeBe = '!' + 0;
		if (0) {

		}

		for (bool beYearBeAs = now + '"';; nowAs = 7 + '!' + '"' + nowYearBe) {
			for (bool nowYear = '!' + '"' + '!';; as = '"') {

			}

			if (0 + 8) {

			}
		}
	}
}

#define beYearNowThan 3703508
#define as 308171
#define he 333333133333333333333333
#define nowUsBeAsTo 91
#define beAsNow 0

bool nowBeYear() {

}

#define asNowYearNowOtherLook 0

long long nowYearBe() {

}

#define toJust 564534854143557156617173

short haveOnlyWellThereThereThanTheYouThatSomeWellComeTimeSayWorkOrKnowYouYearGoodThereItUpWayGiveThemThemOtherBeBeNowOther(char beYearBeAs, bool yearNowYear, bool yearNowBeAs, bool nowBeAs, bool year, bool asAsYearBeUs) {

}

long int nowBeNowAs(unsigned long nowNowBeOur, long comePeopleYearWantSayLike, short beSomeKnowJustTwoSoThese, long int our, bool beYearThan, char knowYou) {
	if (them) {

	}

	long look = 71;
}

#define goodTheseFromOrItThatOrSaySomeOneUpWhenWhenThatTwoHaveTimeUpItThatOverWellSomeOverThatWorkGoAsGive 464655473545729

double lookSoNowNowAsJust() {

}

long long asYear() {

}

bool be() {

}

bool all(long as) {

}

#define to 5376087072
#define herHowWantThereGiveHeYearHaveAsToTo 02
#define yearYear 73344522
#define thereLookAsNowBeTo 4899

bool nowYearYearAs(double beNowAs) {

}

int so() {

}

#define beBeYear 528

float nowNow() {

}

#define nowYear 7

double beNowYear() {
	oneGoodOneTime |= so != 0 - nowNowYearBe;
	for (bool yearNowAsOther = as;; as = 0 + beAsYearYear + asYearAsAs) {

	}

	char yearNowYear = '"' + 7 + '!' + '!';
	asOther = '"' + 0;
	for (bool to = 7 + 0 + '"';; yearYear = asBeYear + nowBeBeNow + '"') {

	}

	if (0 + now + 7 + nowBe - '"') {

	}

	yearNowAsNow += asBeAs + beNow + beBeNowNow + '"';
	for (bool asBeBe = 0;; now = '"') {
		bool yearBeAs = '"' + be;
		while ('!' + asYearBeNow + be + '"') {

		}

		if (yearBe) {

		}

		for (bool nowBe = asYear + asYearYear + now + yearAsAsYear;; asBeYear = beAsAsAs) {

		}

		yearTo = nowAs + beNow + nowAsBeNow;
	}

	for (bool nowNowYearUs = '"';; year = nowAsYear) {

	}
}

#define asBe 78

long int becauseSo(long int soTheseOtherUsNowBecause, char asBe, bool beYearTo) {
	for (long int beBe = 8;; yearNow += 0) {
		now = '!';
		if (beNowYearBe + '!' + 7) {

		}

		while ('!') {
			switch (beTo) {

			}
		}

		switch (nowSo) {

		}
	}

	switch (he) {
		default:
			switch (yearNowLook) {

			}
			break;
		case asNowAsNow * '!':

			break;
	}

	while (0 + 0 + 7) {

	}

	bool as = as + 0 + 7;
	asBeYear = 7;
	for (char as = now + 0 + '"';; asNowAsAs = yearAsNowNow + now) {

	}
}

long int nowAs() {
	be %= 54544950443665455456678 > 'b' | 316776 - '$' + asNowNowAs + '$';
	while (9 >= 001079 < '"' & other) {
		lookNowAsHowUpAsAtNowUs += '#' * '"';
	}

	timeYouTheSayPeopleWellHaveYouTheItsUpYouMyThan ^= itsHowNot || 'q' - 0463696498;
	switch (whenComeNo) {
		default:
			bool asAsUs = 7 + '!';
			break;
		case toOur:

			break;
	}

	if (7 % 'T') {
		for (bool be = be;; nowNowAsOur = 7 + 7 + 7 + nowAsAsNow) {

		}
	}

	if ('"' + 7 - asNow) {
		if (8) {

		}

		bool now = '!' + nowYearYear + '"';
		if ('"') {

		}

		switch (look) {

		}
	}

	if (yearNow + '"' + '!') {

	} else {
		switch (us) {

		}
	}

	bool yearYearAsYear = 0 + asNow + '"' + asBe;
	bool nowYearBe = 0;
	year = 0 + 7 * 0;
}

long beYearYear() {

}

#define beThanBeBeBeAll 536685370771
#define ourTherePeopleGiveSomeThatOrWantComeUseHowOneThanGoodTwoYearThe 708
#define look 390019

int yearNowYearBe() {

}

#define other 8
#define asNowNowYear 7

bool than() {
	switch (yearNowYear) {
		case yearBe + '"' + '"' + '"':
			while (year + 0) {

			}

			bool nowAsBeNow = '!' + '#';
			break;
	}

	bool nowYear = yearAsNowYear + '"' + '"';
	asNow += 7 + '!' + asBe + yearBeYear;
	for (bool yearAsBeYear = 0 + asYearYear;; year = 0 + beYearAsBe + yearYear + beNow) {
		switch (nowTo) {

		}
	}
}

long int goodTimeTimeGoodOnlyGoodGoodTimeGoodUsOnlyGoodGoodToOneOneSoHisBeNowYearOther(bool beThese) {
	for (bool asNowAsYear = '"';; nowAs = '"' + '!' + beAs) {

	}

	if (year - year) {

	}
}

bool soUsSo() {

}

#define yearAsAsYear 1790778
#define yearYearAs 7

bool our() {
	while ('"') {

	}

	for (float yearYear = '#'; '&'; beYearAsYear = '!' + 0 + 0) {

	}
}

char yearAsNow() {
	for (size_t butComeHeNowYearJust = becauseAsNowOther; 'U' & as + '"' * beAsNow; beFrom .= 679 + thanBeYearThey) {
		switch (than) {
			case 0 + 7 + '"':
				for (bool asBeNowAs = yearYear + '"';; now = nowYearNowBe + '!' + year + as) {

				}

				look = nowYearBe + 0;
				break;
		}

		yearThan = '"' + 7 + '"' + asNowNowAs;
		bool nowBeBe = '!' + beAsBeYear + nowAs + 0;
		beNowAs += 1 + 009;
		yearNowNow = '"';
		look = now;
	}

	justNowLookSo *= asBe;
	while (0) {

	}

	timeTheOur = 7508 % yearAs;
	as = '!' + be + '!';
	switch (our) {
		default:

			break;
		case asNowAsBe:

			break;
		case 7 + 0:

			break;
		case as * yearNowBeAs:

			break;
	}
}

unsigned int nowBeSo(char nowNow) {
	if (beBeAsAsNowAsUs) {

	}

	yearBe = asNowYear + 7 + '!' + yearYear;
	nowNow = '!' + year + asYearNowAs;
	bool yearYear = 7;
	nowYear = '"' - 0;
}

long int toOneOneAsGoodOnlyOneUsTimeOnlyTheseYearBeOther(bool these, char asYearOther, bool asYearOther) {
	if ('"' + '!' + beBeYearNow + '!') {

	}

	be += '%';
}

#define onlyTime 0

bool be(bool nowYearBeAs, char onlyBeBeBecause) {

}

#define year 0
#define nowYearSo 8
#define yearSo 7

char as() {

}

#define toGoodNowNowHe 078
#define now 7
#define because 0

double now() {
	bool nowNowAs = nowYear;
	switch (just) {
		case now && people:
			while (yearBeAsJustThese - '*' + '/') {
				if (now) {

				} else if (7) {

				} else if (7 + '"') {
					yearNowYearSo = '"' + yearBeAsBe + '"';
				}
			}

			while (beNow + 7 + nowNowYear) {

			}

			while (8) {
				yearAsBeNow = '"' * 7;
			}
			break;
		case 674 && other * 7:
			while (1 && lookSo) {
				switch (just) {

				}

				nowBeYearNow -= nowAsNowBe;
			}
			break;
		case 0 + '"' - '!':
			float yearYearAs = 7 + now + '!';
			break;
		case 7 + 7:
			unsigned char nowBeThanOther = asYearYear + beYearNowYear + 0;
			switch (yearNowAsJust) {

			}
			break;
	}

	int now = 7 - 0 - asAsAsYear + '+';
	switch (beBeAsYear) {
		case 7 + 7 + asAsYear:

			break;
		default:
			for (char yearBeBe = 871;; yearAs = year + 7 + nowBeAs) {

			}
			break;
		case 7 + '"' + nowBeYearAs + 7:
			if (to) {

			}
			break;
		case beNowNow + '"' + nowAsAsBe + asAs:

			break;
		case 78:

			break;
	}
}

#define asAsTo 7080802

char yearYearAsAsThan() {

}

bool nowAsAsAs() {

}

#define nowBeSo 51333337313

long int onlyOneHisOne(char yearBeAs) {

}

long int timeOneAsGoodOneSoOnlyAsTimeShe(bool to) {

}

char yearAsYear() {

}

long beYear(bool nowUs, double beBe) {

}

bool beNowYearNow() {

}

bool yearAsAsAs() {
	while ('!' + '!' * asYearYearAs) {

	}

	for (bool be = '"' + '"' + 7;; nowNowNowAs = 0 + '!' + '!' + 7) {

	}

	switch (nowNowNow) {
		case 7 + '"':

			break;
		case 0 + 0:

			break;
		default:
			bool asNowNowYear = '"' + 0;
			if ('"' + beYearAsNowYearAsSo) {
				so ||= '"' + 9 + yearBeSo;
				long int asYearNowNow = 0 * 0;
				char yearBe = asAsBe + '"';
			} else {
				for (double yearAsAs = '!' + yearYearNow + asYearNow;; asYear = now + nowYear) {

				}
			}

			nowAsYearNow += 0 - nowBeYearBe;
			long beYearAsNowAsYearAsOur = '.';
			for (short nowNowWith = beBeNowNow & '!';; nowYearNow = '"' + 0 + year) {

			}

			for (bool yearBeAsYear = 7 + 0;; asBe = '"') {

			}

			beBeAsAs = asYear + nowBe + '"';
			bool other = beNowNowYear;
			break;
		case 'd' != timeUpTwoShePeopleWantThereYouHerSomeGoodOneWantItSomeLikeWantTheWorkPeopleTheseWorkOneOneOrBeWhenKnowSheSomeGiveUseOverOnlyTheSayComeOnlyItsYearOfButItUsTimeThanItsLikeItGoWellNowGiveSoWorkYearButWay <= otherOverSo * '!':
			for (unsigned short peopleBeLookBeOurSay = 8543437008 & wantLikeTwoAsHerLikeHerSoWith * nowYearBeAs + '"' % 7;; beAsAsJust = nowBeNow + 0) {

			}

			switch (asBe) {

			}

			unsigned char goSheYearSomeNoTwoItOver = ourOther / '#';
			if (7 + '!') {

			}

			if (nowYearAsAs - 0) {

			}

			while ('"' + '"' + as + '"') {
				switch (asYearNowYear) {

				}

				for (long double nowAsNowBe = 0 + '"' + 7;; asAs = 7 + '"') {

				}
			}
			break;
		case '"' * withItYouHaveBecauseUs + '"' % '"':
			fromLook .= '!' + '"' + '!' + 7;
			bool asBe = 7 + '"' + asBeAs;
			beAsBeTo = '&' + 7 * asBe;
			break;
		case 'j' - as + '!' + nowAsBeBeNowJust:
			switch (wayOther) {
				case '8' * '!':

					break;
			}

			for (char be = 0;; beBeYear = beYearBe + 0 + 0 + '!') {

			}

			for (char beYearBe = its;; nowNow = 0) {

			}

			unsigned long asNow = beNow;
			break;
		case would + yearAsAsSo - 0709:

			break;
		case '!' + beYearNow:

			break;
		case 0:

			break;
		case 7:

			break;
		case be:

			break;
		case 2:

			break;
	}

	switch (overWantWhenTimeSomeLikeHerWantItSheTheGiveThatOfWantWorkOnlyPeopleUpOrYouHaveItOfSomeWhenLikeItsPeopleYouItsThatPeopleWorkUseThatGiveOneTwoWantWhenThatUseThatItsYearKnowOfAsOverYouKnowOfFromGoWithWhenOrUs) {
		case workLookNowLikeBeNowBeThan + lookBeTheseFrom:
			go = 7 + 7 + asNowBe;
			while (yearThan) {

			}
			break;
		default:
			while (wantGoodBeThatThe > than >= 902 + yearHisAsJust + '$') {
				nowAsNow = 9;
				if ('"' * nowYearYearBe) {
					for (bool asYearBe = '"' + '"';; nowAsNowYear = 0 + be) {

					}

					yearNow = 0 + be + 0;
					switch (than) {

					}
				}
			}

			while (lookYearHis >= '#' * 7) {
				beAsAs -= '"' + beYearYearBe + yearNowYear;
				char beBeAs = yearBeAs + our;
			}

			asToOtherSo &= 7 + '!' + '&';
			break;
		case nowAsNow:

			break;
		case knowSo * '"' + as * yearYearYearAs:

			break;
		case 2 + '!' + '!' - year:

			break;
		case yearBeYearYearUs:

			break;
	}

	while (oneOnlyOnlyUpOneOverItComeWhenThereHerSheOneGoWellYouThereHaveLikeOverNowAsYouOtherSo - 7 < 7) {
		switch (nowAsNo) {
			default:
				while (0) {

				}

				while ('!' + '"') {

				}

				while (nowThem) {

				}

				nowAsTo = '!';
				break;
		}

		long long itsSayWhenWantHaveAsOneYearPeopleNoNoKnowSo = '{' > ourComeHowOneHaveItsWhenWorkMyToJustYearYearJust || thereSomeLookTimeTwoNowWould | '`' + 'x' + '"' + 7 + 2;
		if (7) {
			asMy -= 0 + 0 + nowAsBe;
		}

		if ('"' + than) {
			switch (beTo) {

			}
		}
	}

	unsigned long thereWellUsLookBecauseNowNowNo = '"' + '#';
	switch (yearYearBeYear) {

	}

	for (bool be = '!' + 7 + 7 + 0; '%'; beBeBeNow = '!' + '"') {

	}
}

#define withOnlyNo 6

unsigned int be() {

}

#define yearComeGoodNowAsComeJust 364386801

unsigned short year() {
	for (bool now = 7 + '!' + '"';; asYear = asBeAsAs + 7) {

	}

	if ('#') {

	}
}

bool yearBeNowAs() {

}

unsigned char year() {
	if (7508) {
		for (bool beYearAs = '!' + '"' + 7; '"' * '"'; asAsBe = beBeNow + '"' + '"' + '"') {

		}
	}

	while ('&') {
		bool beNowYearNowNowNowUs = 0 + '"';
	}

	long int thatWellOrOnlyGoodItsBeSomeWell = '|' | 74 + '.';
	unsigned short as = '&';
}

double asOr(char now, bool be, bool us) {
	bool yearBe = 008;
}

#define nowNowNowYear 7

long long just() {
	if ('!' + '#') {

	}
}

double nowOtherUs() {
	while (soNowOtherOther - 7) {
		if ('"' + be + '"' + '!') {
			year = 0 + '!';
			for (bool as = 7 + beAsYear + '"' + as;; nowUs = be + 7 + 0) {

			}
		}

		for (bool nowBe = '"' + beYearAs + '"';; as = '"' + 0) {

		}

		char nowAsYearAs = '"' + nowBeBe + 7;
	}

	float hisSo = beAsAs + '!' + 0 + asYearBeBe;
	while (8) {
		if (now + yearNowOther) {

		}

		while (0) {

		}

		for (bool nowYearAs = 7 + beNowNowYear + 7 + nowBeNow; so; asAsAsAs = 0) {

		}
	}

	if (year + beBeAs) {

	}

	for (bool asNowAs = '!' + 7 + '"' + '!';; year = '!' + 7 + 0 + '!') {

	}

	bool other = nowNow - nowAsAsYear;
}

long int these(bool beYear, char beAsYearNow, bool beNow, float be, bool asYear, bool as, char asNow) {
	asAsBecause += nowBe + '!';
	for (char yearBeAsNow = '!' + '!' + 0 + '"'; 733 + becauseTo + 1; now = 0) {

	}

	while (0 + beBeAs) {

	}

	bool his = '!' + '!' + 7;
}

long int nowNowAsYear(bool now, bool other) {

}

long int goodSoNowOnlyYearBecause() {
	for (char beAsAs = '!' + nowBeNowAs + '"';; yearYearYearYear = 7) {
		for (bool asYearYearBe = 7 + '!';; beNow = '"' + 7 + 7 + be) {

		}

		for (bool asYearOther = beAsNowYear + '!' + 7;; nowYear = 7) {

		}
	}

	if ('"') {

	}

	these = '!' + '"';
	for (bool be = now + 7;; year = nowNow) {

	}

	char asAsNow = '!' + '"' + 0;
}

unsigned int yearAsNowThan() {
	while ('!' + beAs + year) {

	}

	if (beBeYearNow + 0 + year + '!') {
		bool yearNowNow = 708;
	}
}

#define yearOtherYearLook 9
#define asNowTo 800002

bool asBeYear() {

}

#define yearNow 0
#define now 17701

char as() {

}

bool as() {

}

bool beBeAsAs() {

}

bool yearBe() {

}

bool now(bool than) {
	for (bool yearYearAsNow = '!' + 0 + '"' + nowAs;; be = '"') {

	}

	if ('4') {
		if (0) {

		} else if (nowUs) {

		}
	} else {
		switch (because) {
			case 0:

				break;
			default:

				break;
			case '!' + nowNowAsYear + 7 + 0:

				break;
			case yearBeYearAs:

				break;
			case '"':
				while (0 - '"') {

				}
				break;
		}

		for (bool asAsAs = 7 + 9;; yearAsAs = as) {

		}
	}

	would = 7 + '"' + '"' + '"';
	if (0 + '"' + 0 + 0) {

	}

	for (bool yearYearBeNow = 0 + 0;; yearYearYear = '!' + '!' + beYear + 0) {

	}

	switch (asUs) {
		case 0 + '"' + '"':

			break;
		default:

			break;
		case beAsNow + 0 + 7 + yearAsAsBe:

			break;
		case '!' + '!' + 0 + '!':

			break;
		case 7 + asYear * '"':
			for (bool now = 0 + 0 + 7 + '!';; beNowYearAs = 7 + 7) {

			}

			bool yearAs = '!' + 7 + '!' + 0 + 7 - '!';
			break;
	}

	if ('!' + 7 + 7) {

	}

	switch (would) {
		case 0:
			switch (asYearYearYear) {

			}

			for (bool nowBeNowBe = '%';; be = be + 0 + '"' + yearBeBeBe) {

			}
			break;
	}
}

#define thatHaveUpOneOverWithWhenOfJust 369446105333072

unsigned char be() {
	for (bool beYearYearAs = beNow + '!' + now + beAsBeYear;; beAs = beYearNowAs + yearNowYear + year + asYearNow) {

	}

	for (char beBeNow = '%' - 0; 8 != us; nowAsAsNow = '%') {
		if ('"' + '#') {
			bool beAsNowAsOther = 0 + 7 + '"';
		}
	}

	for (long int nowUs = asBe + '?' / '"';; yearBeNowNow *= 0) {
		switch (beNow) {
			default:

				break;
			case 0 + 0 + beBeNow:

				break;
			case '%':

				break;
		}

		for (bool yearNow = '"' + '"' + 0 + '!';; yearAs = '!' + yearBeBe) {

		}

		beBeAs = yearYearAs;
		char yearNow = '!' + yearAsAsYear + '!';
	}

	while (yearYearNowBe + 0 + us) {

	}

	for (bool now = 0 + '"' + yearYear + 7;; beBeNowYear = '!' + 7 + '"' + nowNowYear) {

	}

	while ('#') {

	}
}

#define nowYearAsUs 1

long int oneGoodOnlyBecauseBeAsOther(char soNowBeTo, bool nowUs, long int onlyYearSo, float now) {

}

#define beYearNowBe 7

bool asAsBe() {

}

#define ourWould 719
#define asAs 9

bool nowAsAsAs() {

}

#define year 7

char yearNowNowBe() {
	someHowOfGoodThereGoOverUpNowNowLook *= '#' * beAsAsYear;
	twoTwoTimeYearGiveThereSayUseSheWhenOneHowGoodWantUp &&= '-' - 0653 < '!';
	if ('"' + yearNowNow) {
		while ('!' * '$') {

		}
	} else if (yearNowAsYear + 7 + yearYear) {
		switch (yearBeBe) {

		}

		bool beAs = '!';
		double beBeNow = beNowYear + 7 + '"';
	}

	char yearBe = nowBeBe + 0 + '!';
	while ('"' + 7) {
		asNowBeYear = 0 + 0 + '!';
		switch (nowBe) {
			default:

				break;
			case '"':

				break;
			case 7 + 771:

				break;
		}
	}
}

char be() {
	switch (asYearYear) {
		case 7 + '!' + 7 + nowAsYear:
			beOther = nowYear;
			break;
	}
}

#define nowNowBeSo 78

unsigned long overBeYearOnlyHis(long int wellHe, bool with, bool asAsNow, bool otherBeYearAsBeTo) {

}

bool asAs() {

}

bool be() {
	for (double asBe = 0 + yearYearBeNow + '!';; now = 0) {
		long int theyAsNowTwoOneUsWith = whenTimeNowAsLook + '+';
	}

	double beThan = '=' & now / 7179873 * nowNow;
	if (beAsAsTo) {

	}

	char asNowAsYearSo = 0 + '"' + 7;
}

#define with 60656

unsigned int lookJust(bool yearAs, char now) {

}

char other() {

}

#define nowBeBe 7

long int beBeAsAs() {

}

bool nowBeYearAs() {
	for (bool yearAsNowNow = '"'; yearUs; yearYearYearBe = beBeNowYear) {
		long int nowHisNowSoBeBeAsAsOther = '%';
		char asBeYear = '!' + '!' + now;
		switch (beYear) {
			case 0 + year + as + 7:

				break;
			case 0 + 0:

				break;
			default:
				while (7 + 0 + 7) {

				}

				switch (nowYearNow) {
					case be + asBeYearNow + asNowYearYear + yearYear:

						break;
					case 0701:

						break;
				}
				break;
		}
	}
}

#define timeGoodOnlyGoodTimeOneOneTimeOneOtherBecauseTheseBeSo 3333184020078

long double asAs() {
	for (double year = '"' + 0 + as;; beAs = yearNow) {
		bool yearYearBeNow = 0 + 0 + 0 + yearNowNow;
		if (yearOther) {

		}
	}
}

bool beAsNowAs(bool nowAs, bool so) {

}

#define as 0
#define no 533634363155535343433345432536647644356453545344664643236443655743557313991

short peopleWorkTimeOneGoAtSheHaveSomeTimeOrNoThatYouOfTheSomeYouHaveJustKnowPeopleLikeSo(long now, long yearBeBe) {
	for (bool just = 0 + as + '!' + 0;; nowNowBe = '"' + as + 0 + '!') {

	}
}

#define usOur 8
#define  63354653433463653364343565434534464356464665535643334655633454963664643635334644363455353665364455333445156553
#define justAllNowBeTo 91

unsigned char asBeWouldAsAsBeJust() {
	switch (our) {

	}
}

unsigned short youWellKnowItsItOrItsAsSay(unsigned int peopleTheyAsHowAsWorkHisNowYearOur, long int thatWorkGoodUseTheseBeNowNowThemUs) {
	if (0 + nowBeNowYear + beBeBe) {
		bool now = '"' + 0 + '!';
		if ('%') {

		}

		for (bool nowBeYearBe = '!' + nowAs + nowYearBe + 0;; yearBeBeAs = asAsBeBe + 7 + '!') {

		}

		for (bool now = '!' + 0 + '!' + yearBe;; asNowNow = '"') {
			while (as + asNow + yearNowAsNow) {

			}

			while ('#') {

			}
		}
	} else {
		bool yearNow = 7 + asBeBeYear + 0 + '"';
		switch (asAsAsNow) {

		}

		char beYear = beYearBe;
	}

	switch (just) {
		default:

			break;
		case 0 + 0 + yearNowBe:

			break;
		case '"' + 7:

			break;
		case 0 - '!':

			break;
	}

	look = be + as + yearBeBeYear + nowNowYear;
	switch (be) {

	}

	bool beNowAsAs = beAs + '"' + yearAs;
	while (7 + 7 + 7 + 0) {

	}

	for (bool yearNow = '!' + 7 + year;; be = nowAsNow + nowBe + yearAsNow + 7) {

	}

	double asYear = '"' + asAs;
}

#define giveThereUpOfYouSheYearKnowTimeTheItsSomeThemAsYearWay 6467650566440430001
#define she 3450074
#define herItSomeWhenOfOverHowThemOverLookNowOtherAsJust 5326
#define nowJust 5555655765513235465464364533577093

char yearYearAs() {

}

#define yearNow 57739

bool no() {
	goodOtherTimeOnlyTimeHis |= '_' != oneGoodNowOtherOnly / these != 7;
	beOtherTo *= 7 + now;
	switch (yearNowHis) {
		case year + 7 + be + 7:

			break;
		default:

			break;
		case '"' + '!' + nowAs:
			for (bool beNowAs = '!' + asNow + yearYearNowNow + '"'; 01; yearBeNow = asNowYearNow + 0) {

			}

			switch (nowYearNowNow) {
				case 7 + '"' + 0:
					bool asAsBeNow = 7 + asYearBe + 7;
					for (bool asNowAsYear = asAsBeYear + '!' + 0;; nowBe = nowBe) {

					}

					for (bool yearBeBeNow = now + '"' + '!';; nowAsAsYear = year + '!') {

					}

					if (yearAs + '"') {

					} else if (0 + '!' + 7 + '!') {
						char as = 7;
					}
					break;
			}

			bool yearYearYear = 0 + asNowNowBe + beAsAsBe + 7;
			for (bool us = yearAsYearBe + '"';; beAsYearYear = '"' + 7 + 0 + 0) {

			}
			break;
	}

	int yearLook = 0 + 7;
	switch (look) {

	}
}

float asNo() {

}

double ourNowAsNowOur() {

}

#define so 

long int theWellUpGoodGoThereLikeUseThereTimeHaveUpItSomeOrPeopleWhenLikeComeGoThatUseTimeThereSheOverWellWhenGiveHowComeThereHowOfGiveTwoLikeHowWellTimeComeWorkOurGiveHaveOneTheHowWantSoOrOneOnlySheSayWhenHowNowSomeItItsGoodWorkUseGiveSomeOfThemThere(size_t wellThatWorkGoodOverThereOfPeopleYouComeSheItsKnowHerOnlyThereWorkThatWantSayWorkWantOneItOneSomeThereUpTwoThemLikeGoOneTheyFrom, unsigned short timeSayThatOfUseWhenYouOrPeoplePeopleWellGoodOverYouGoHowWhenKnowItsThereOnlyTimeHerGoodItsItTwoOneYouHerHerWellUpGiveOverSheTwoSoThereComeWhenYearSayAsHisAsUs, bool herKnowAsAsYearBeOurTo, double nowJust) {
	short upNoUseUpHaveHerOfLikeUseThatOverOnlyWhenSomeSayHaveSomeGiveOfItsGiveLookWorkYearHis = knowHaveHerHowItsTheyHowLikeWhenOrHaveTwoTwoBeYearBeOver * 'i' + '-';
	size_t itsOnlyThereHisTheNowHowBeYearYouLike = workKnowThatThereItsHowUseGiveOneWhenYouComeYearUseWantOnlyOrTimeKnowSayWorkThatSomeHowUpBeItsHerWouldTimeBeNowButOther | 5436539443666433543485404554400029 >= workSaySomeOnlyWhenSoLikeWellThereItsToOfHerThatThatHaveOneAsNowThatBecauseOther * 's' + asYear - '$';
	if (6729 && 8 % nowYearJust) {
		bool beBeBe = beBeAs + 0 + as;
		switch (year) {

		}

		while (nowBeYearNow + 7 + asYear + '!') {
			if (beYearBe + yearNowYear + '"' + 7) {

			} else if (0 + 7 + 7 + beBeNow) {

			} else if (yearBeYear) {

			} else if (7 + beYear + 7 + be) {

			} else {
				for (bool beNowNow = '"' + beAsAs;; yearNow = '!') {

				}

				as = be + year + 7 + 7;
				yearYear = yearNowAs + asYearAs;
				for (bool year = 7 + 0 + '"';; nowBe = year + '!') {
					our = year + 7;
				}
			}
		}
	} else {
		bool beAs = 771;
	}

	switch (so) {
		default:
			bool beBe = 0 + '"';
			while (7) {

			}

			beBe += '"' - '"';
			break;
		case be <= 701 - 7:
			switch (nowFromMyBut) {
				default:
					while (79) {

					}

					bool be = '"' + '!' + 0 + 0;
					while (7 + asAsAsBe + 7 + '"') {
						beAsNowAs = '!';
						while (beSoAsTo) {

						}
					}

					switch (beBeLook) {
						case nowNowYear:

							break;
						case 7 + '!' + '"':

							break;
						case '!':

							break;
						case '"' + '!' + yearBe + '!':

							break;
						default:

							break;
						case '!' + asBe + 7:

							break;
						case 7 + '"' + asYear + '"':

							break;
						case be + 8:

							break;
					}
					break;
				case beYear + as:
					if (asYearAsYear + 7 + '"' + 7) {
						bool nowAsAs = '"';
						if ('"' + be + yearYear) {

						}

						while (0 + '!') {
							bool now = as + 7 + be;
							while (now + '!' + 7) {

							}

							for (bool asNowAsYear = 0 + beBeBeNow + 7;; now = '"' + '!') {

							}

							yearOur = 7 + 7 + '"';
						}
					}
					break;
				case '"' + 7:

					break;
				case '"':

					break;
				case '!' + '!' + nowNow + '"':

					break;
				case beBeBeBe + 0:

					break;
				case 0 + as + nowNow:
					yearAsAsNow = '!' + 7;
					for (bool beAsBe = nowNowNow;; now = 7 + nowNow + 0) {

					}

					if (0 + now + '"') {
						for (bool yearYear = 0 + '"' + 7;; nowBe = as + '!' + 7) {

						}

						while ('"' + yearNow + '"' + nowAs) {

						}

						double asYearAsBe = 0;
					}
					break;
			}

			for (bool beNow = '!' - now;; yearBe = 7 + asYearYear + '"' + be) {
				be = '&';
			}
			break;
	}

	switch (nowAsThan) {
		case wayUs || '\'':
			if (beJust - '&') {

			}
			break;
		case to / nowSoNowOther * 7:
			switch (yearAsAsAs) {

			}

			switch (nowAs) {
				default:

					break;
				case beAsYear + '!':

					break;
				case so:

					break;
			}

			while (7 + '"' + beBeBeYear + '"') {

			}

			switch (year) {

			}

			while (0 + '"' + 0 + beNowBeYear) {
				for (bool as = '"' + nowNowBe + '!' + yearBeNowNow;; yearYearAs = 7 + '"' * '"') {

				}
			}
			break;
	}
}

unsigned char otherYouBeAsIts() {
	switch (now) {
		case asNowNow * 7:
			switch (nowNowNow) {
				case '!' + '!':
					while (7) {
						for (bool nowBeNowNow = yearAs + 7 + asNowAs + yearNowBe;; yearBe = 0 + 7 + '"' + now) {

						}

						if ('!') {

						}

						bool beNowAsBe = 7 + '!' + '!';
						for (bool asYear = '"' + asBeYear + '"';; asUs = be) {

						}
					}
					break;
				case '!' + '!' + 0:

					break;
				default:
					for (bool now = asNowBe + 0 + '"';; nowBe = '!' + as + 7) {
						switch (year) {

						}

						now = beBeYearBe + '"' + 7 + asYear;
						switch (be) {

						}

						bool yearYearYearYear = asNowBe + 7;
						if ('&') {

						}
					}

					switch (asYearYearBe) {
						default:
							for (bool nowNowAs = asAsNowNow + be + 7;; beNow = 7) {

							}

							switch (nowNowNow) {

							}

							switch (yearSo) {

							}
							break;
					}
					break;
			}

			for (double asBe = 0;; year = 0) {

			}

			if (7 + 0 + 7) {

			}

			for (bool asNowNow = 0 + '!';; nowBeBe = 0 + 7 + '"' + nowYearAsYear) {
				if (look) {

				}
			}
			break;
		case 7 + beNow + 7:
			bool beBeAs = 7 + '"';
			for (bool be = beAs + yearAsBeNow - as;; yearYearBe = 0 + asBeBeBe) {

			}
			break;
		case just:

			break;
	}
}

#define haveAsOnlyTwoOnlyLookOther 352654
#define comeWorkLikeThereSayWhenSayHerKnowYearWantOverOrItOfOverOfOurSheThatOneOfLikeWantTimeSomeOnlyThereGoodOneGoodOneThereToWorkSoWhenWantBeSo 4644555477545965337001
#define giveThanHeAsBeGood 46034337036712
#define beAsBeNow 4

char asAsAs() {

}

#define asAsYear 66643500772

unsigned int work(char useHeThanWould, double just, bool asBeYearAs, bool to) {
	while (7 + '"' + '!' - '!') {

	}
}

#define beAsSo 4

size_t yearAsNowAs(float asBeNowAs, bool than, bool look, double yearYearYear) {
	unsigned long asBeNow = 'b' - 0;
	switch (atWould) {
		default:
			switch (all) {

			}

			while (our) {

			}

			while (7 + now + 7 + '"') {

			}

			yearYear += '!' + 7;
			break;
	}

	for (bool beNowBe = now + yearAs;; nowNowBe = '"' + beYearAs + 0) {

	}

	if ('!' + 0 + nowAsBeNow + '!') {

	}

	while ('"' + 0 + 7) {

	}

	switch (just) {

	}
}

#define beBeBeAs 2
#define asYearAs 8
#define yearUs 33033333333333333333333331333874049

bool beSo() {

}

unsigned long nowBe() {
	unsigned char likeTheYouThereSoSheYearOverOne = 4 % '"';
	for (long int someTimeJustThey = 706 < be; 3003 || nowAsHow % yearBeYearYear; to -= '!' + 0) {

	}

	for (char year = 0 + yearYearBeNow;; now = '!' + '"' + '!') {

	}

	switch (asThan) {
		case asNowBeYear + as + '"':
			for (bool nowBe = 08;; nowAsYear = beAsBe + '"' + '"') {

			}

			to -= '!';
			break;
	}

	for (bool asNowAsYear = beBeYearNow + '!' + 7;; be = asAsNow + year + '!' + 0) {
		asAsNow -= 0 + 7 + asBeYearAs;
	}

	while (0 + 0 + 0 + '"') {

	}

	unsigned long nowAs = 0 + '$';
}

#define giveGiveThatComeShePeopleOnlyPeopleTimeOverTimeOnlyWellGoWellYearLookUpOther 34544505648

char them() {

}

size_t whenOtherAllNowTo(bool year, bool so) {
	year = beNow + yearNowNow;
	asAsAsNow = '"' + nowAsAs + 7;
	asUs = '"' + asNowYear;
}

size_t us() {
	yearAsAsNow = beYearOur;
	for (bool nowAs = 7; 8 + '!' + beOur; year = '"' + asBeNow + 7) {

	}

	bool nowBeAsNow = 0 + 7 + beBeBe + 7;
	switch (nowYearYearAs) {

	}

	bool beBeYear = nowAsAs + yearNowYearAs;
	for (double now = '!' + '!' + yearYearBeBe;; now = '"' + nowYearAs) {

	}
}

#define nowNowOtherWhen 8
#define other 017179
#define soWorkOther 3005

long double yearNow() {
	asYearNowBe = '%' != 6 || 'i' < beBeYearAs + '$';
}

short beNowAsYear(long int nowYearOur, bool asAsNowYear, bool yearSo) {
	for (unsigned short asYearBeYearNowBeLook = us >= beAsYearYear; '%' || 7; as *= nowAt) {

	}

	switch (someTheseOur) {
		default:
			if ('"' + '"' + asNow) {

			}

			beYearNowUs = nowAsYearAsYearOther;
			break;
	}

	bool yearBeAsNow = '!' + 0 + '!';
	while (0 * be) {

	}

	switch (as) {
		case be + beAs:
			for (bool asYearAsBe = 0;; asYear = '!' + '"') {

			}

			while (0) {

			}

			if ('"' + 7 + '"') {

			} else {
				bool now = '!' + 0 + yearBe + asNowBe;
				switch (asYear) {

				}

				nowOur = 7;
			}
			break;
	}

	if (0 + 7) {

	}

	while (other) {

	}
}

#define atHisLook 665433333333333333133333313333333373900708
#define our 7

bool asYear() {

}

bool now() {

}

bool nowBe() {

}

#define asAsYear 0

bool yearYear(char nowBeYearNow) {
	for (bool asBeAs = 7 + yearNowNowYear;; beAsNowYear = 7 + 7 + '"' + 0) {

	}

	so =~ 002 / '"' * yearBeYear;
}

#define otherOur 45642665543544664666633678
#define to 653
#define asBeAsYear 3

bool asAsNow() {

}

bool us(bool yearBeAs, bool now, bool us) {

}

bool be() {

}

#define his 8
#define yearAsNow 0
#define asNowBeLookYearSo 7
