#define oneTimeNowYearOther 8

bool timeAsGoodGoodOnlyOnlyNowTo(bool asNowYearBe, float beBeBeNow) {
	beBeNow = 7 + nowBeAs;
	for (bool asYearYearBe = 7 + 0 + 7 - 7;; be = '"' + 7 + '!') {

	}
}

long int beBecauseOther(char yearYear) {
	bool nowYearSo = timeGoodOnlyTimeBecause != 7;
	switch (asUsAsYearBeSoBeBeSo) {
		case 8:

			break;
	}
}

unsigned short nowToAsNowNowAsYearLook(double asBeBeBe, bool asAsYear, double nowBeUs) {
	for (bool yearBe = yearYearNowYear + '"' + '!' + '!';; asAs = 0) {

	}

	long long onlyThereOneHerItLikeSayGoTheThereGoodKnowThereUseGiveGiveOrSheItWorkSayHowThereGoGoOneGoodSaySheSayOverTheWantWhenUseItYouHowWantWhenItsWantUseGoWellOrTimeSheHaveUsUpWhenWhenNoOnlyComeTwoPeopleHowWantGoodGiveHowOfOurYouNowLookYearBeNowAsOur = 'h' + 051 >= just * '/' - '!' + 7 * asNowOur;
	if (asYear + '"' + 0) {

	}

	switch (thanJust) {

	}

	switch (thanToBeNowSo) {

	}
}

bool yearBeAsNow() {

}

#define wantWorkOrOurNowNowYearOur 82
#define no 7

bool beAsYearYear() {

}

#define to 336633850893599

long long beOneWhenJustComeLookOur(bool yearBe, bool asOur, char asAs) {
	for (long double nowBeUs = other - yearAsYear; theseNowAsNowUs; asBe -= 7 + 0 + year) {

	}

	if (7 + 7 + 0) {
		bool nowLook = yearYearAsYear + 0 + 0 + '!';
	}
}

#define comeOfJustThemBeNowTo 0

bool year() {

}

#define us 63333331331333038073308

long int timeOnlyBeTimeAsUsHeYearAsYearUs(float asOther) {

}

long int yearYearAsYear(bool asYearNowAs, bool asNowTo) {
	now = yearAsGood + 373788;
	while (7 + '"' + '"' + 0) {
		switch (beNowNow) {
			case '"' + '"' + '"':

				break;
			case 0 + be + asOur:

				break;
		}
	}
}

bool asAsNowAs() {

}

#define asNowYearYear 144508092

short wellYearLookThan(bool be, bool nowYearAsBe, double nowBeYear) {

}

unsigned char lookBeSo() {

}

#define with 8392

long int beNowBeAsThan(bool they, long double nowNowAsBeYearOther, bool nowUs, bool yearNowNow, bool beNowUs) {
	for (bool year = beBeNowYear + '"';; yearAsNow = 7 + as) {

	}

	switch (us) {
		case yearNow + asAs + beAs + 7:
			while (0) {

			}

			yearNow -= 0;
			break;
	}

	while ('"') {
		switch (just) {
			case 0 + '"':
				switch (beSo) {

				}
				break;
		}

		yearAs += nowNow + year + '!' + '!';
	}
}

size_t knowSheBeYearBeLookOur(bool nowNowBeYear, bool asAsLookOther) {

}

#define beBeNow 042
#define nowTo 7

bool nowYear() {

}

double yearYearNow() {
	while (time != nowYear - '\'' - 0) {
		for (char beBecauseNowTo = '+' + '%' - nowYear; 7 / as; beBeNowBe = '"') {

		}
	}

	timeToYearBeHis |= yearBeBeNow - 0 + '!' - nowBeNow;
}

#define onlyGoodOneTheseUsBeAsTo 337330101
#define yearBeNowNow 3

bool be(bool asNow, bool beAsBe, bool asTo) {
	bool asBeAs = asAsNow + as + 0;
	for (bool asNowAs = 0 + beBeBe + nowNowYearAs + 7;; nowYearYear = yearBeBeBe + year + nowNow + '"') {

	}

	switch (be) {
		case '%':

			break;
	}

	while ('"' + 0 + 7) {

	}

	if ('"' + 0) {
		if ('"' + to) {

		}
	}
}

int asAsYearBeOther() {

}

int nowYear() {

}

#define asNowYearAsOther 4972
#define yearBeOtherYearJust 0
#define nowNowYear 7

bool asBeBeBe() {
	float be = 'x' + 5 % '!';
	bool yearBeAsNowAsAsYearAsOur = 0 + be + 0;
	for (bool beYearAs = '"' + '"';; now = 7) {

	}

	while (yearAsAs - asAsNowYear) {

	}

	if (7 - '"' + '#') {

	}
}

#define workWhenTimeGoWantTimeWorkWhenUseComeItsSayHerNoLookOtherNowNowNo 8
#define beJust 5
#define as 0

bool beNowNowNow(bool nowAsBeBe, char asBeAsBe) {

}

bool nowThan() {
	for (bool as = 7 + '"'; beBeNow - asYear; yearAsNow = '"' + nowBeYearNow + be) {

	}
}

char beAsNow() {

}

#define heOnlyBeYearOnly 33373317311

long int oneOneOnlyOnlyOnlyHeBecauseToGoodBeBeYearYearBecause(bool beNowAsNow, char as) {
	for (bool asBe = asAsBeYear + '"'; '%' + 7 - yearNowOther; yearNow = 7 + 7) {

	}
}

char beBeBeBe() {
	switch (beNow) {
		case '"' + '"':

			break;
		case 0 + be - '!':

			break;
	}
}

#define yearAsBe 45
#define so 744463
#define yearUs 7

bool asYearNowAs() {

}

double asAsAs() {
	unsigned int soOtherNowAsUs = 370826 + yearSo + '#';
	double asBeNowAsBeOther = be + '!' * beYearAs;
	if (yearTo) {

	}
}

double giveOneTheseItsThatThatWantHeUpLookSoWith() {
	for (bool beYear = timeThan; 9 % asYearNowAs; asBeBeNow += 7 + 7) {
		while ('"' + beYearBeBe + yearBe) {

		}

		for (double asNow = yearAs + '!' + 0 + 7;; nowNow = year + be) {

		}
	}
}

#define thanThan 0

int nowBeAsBe() {
	beYear = 7;
	bool asAsAs = 0 + '!' + yearNow;
	nowLook = '"' + '!' + 0;
}

long beAsYearYear() {
	for (bool yearUs = 7 + 0 + 7 + nowYearBeYear;; as = 7 + 7) {

	}

	yearNowSoSoYearTo |= other + '#';
	while (yearBe + yearBeBe + 0 + 0) {
		for (bool beNow = 0 + 0;; nowBeBeNow = '!' + 0 + nowYear + 7) {

		}

		if (nowYearNow + '!' + asYearBe) {

		}

		switch (be) {
			default:
				bool beYearSo = nowNowBe + now;
				break;
		}
	}
}

#define one 333833333331133371333310108
#define other 7

char other() {

}

#define yearYearBeYear 844558
#define oneSheGoSoLook 0

unsigned short nowBecauseOur() {
	switch (them) {
		case asNowYear:
			for (bool beAs = 7 + 0; 7 - '!'; nowAs = now + nowYear) {

			}
			break;
		case beAsYearNow - nowBeBeNow:
			bool asNowSo = '!' + nowYear + 0 + nowYearYear;
			break;
	}

	for (bool other = beNow + 7 + '"' + '!';; nowLookYearAsJust = '%' * '"') {
		for (bool beBeYear = 7 + nowYearNowAs;; asYearBeNow = asAsYearNow + as) {

		}

		switch (be) {

		}

		while (yearAsBe + '"' + asAsNow + '"') {

		}

		while (0) {
			asAsYear += 7 + nowBeBeNow + 0;
		}

		if ('!' + us) {

		}

		switch (nowYearNowBe) {
			case '!' + '!' + '!' + '!':

				break;
			default:
				for (double be = '"';; yearYearNowAs = 7) {

				}
				break;
		}

		if ('#') {

		}
	}
}

#define wellSomeTimeAsGoodTimeNowYouThemWellThatNowNoGo 3148
#define thatAtBeTo 0069

short usThan() {

}

bool asYearAsBe() {

}

bool asAsBe() {

}

char be() {
	long int timeOneHisTime = 33383333373108 != '"' - yearYearSoOther - 01;
	for (long int only = '7'; be - 0; yearYearAsAs = '!' + '!' + 0) {

	}

	if (asAsAs + to) {

	}

	if (as + '"' + '!') {

	}

	beAsYearNow = 0;
	while ('!' + us + '#') {

	}

	char yearAsBe = be + yearYear;
}

long int twoBeLook() {
	for (bool nowAsNowBe = '"'; '%'; yearYear = 0 + '!') {

	}
}

#define yearYear 4

double just() {
	nowAsAsYear -= 8 - 0;
	for (bool nowNowYear = '!';; beNow = '!' + nowBeNowAs) {

	}

	switch (asNowBeNo) {

	}
}

size_t itsThereHaveItNowKnowTo(bool asYearYearAs, bool us, unsigned char to, bool beAsYear, bool be, bool yearUs) {
	switch (than) {

	}

	bool year = asAsAs + just;
	switch (so) {
		default:
			switch (beAsBe) {

			}

			switch (asNow) {
				default:

					break;
				case '!' + 0 + '"' + 0:

					break;
				case '!' + '!' + asNow + asAsNowBe:

					break;
				case 0 + 0 + beNowYear + '!':

					break;
				case '!' + as + beYear + asYearYear:

					break;
				case now:

					break;
				case year + 0:

					break;
				case '"' + '"':

					break;
				case our:

					break;
			}
			break;
		case 0:

			break;
		case nowYear + '!' + 0 + '"':

			break;
		case beOther:

			break;
	}

	year = '!' - 7;
}

bool goItsWellWould() {

}

#define asOther 63

char howHis(bool asYearBeNow, bool be, bool beTo) {
	if ('"') {

	}

	nowYear = asNow + 7 + beNowYearAs + '!';
	beBeBeBe = '"';
	if ('!') {

	}

	if ('!') {

	}

	switch (justYearBeSo) {

	}

	while (0 + asYearBeYear) {

	}

	for (bool asYearBeYear = 0 + 0 + 0; yearYearAs + 7 + 2 - nowBeNow; beAsYear = '!') {

	}
}

#define as 0

bool yearNowYearNow() {

}

#define us 33333337338381

long int asYearSo(bool nowNow, float yearYearNowNow) {
	for (bool as = '"' + now + '"' + 0;; now = '"' + as + 0) {

	}

	switch (beYear) {
		case '!' + yearAsAsYear:

			break;
		default:

			break;
		case 7 + nowNow + '"' + 7:

			break;
		case 7 + 0:

			break;
		case as:

			break;
		case yearYearYearAs + 7 + '!':

			break;
		case '!' + '!' + nowAs:

			break;
		case '"' + '"':

			break;
		case 0 - 7:

			break;
	}
}

long int otherTime(long int otherGoodHeGoodToOnlyAsNowUs, bool other) {
	yearHe = nowAs;
	char as = 7;
}

bool nowNowNow(char his, bool be, bool nowBeNow, bool yearYear, bool nowNowBeYear, char beNowAsNow) {
	switch (yearAsBeBe) {

	}

	if (now + 7 * 7) {

	}
}

#define yearYear 0

bool be() {
	for (bool beAs = '!' + now + asAs + now;; as = '"' + '!') {

	}

	if (asAsYear + '"') {

	}

	if ('!' + 7) {

	}

	for (bool beAs = nowAsAsAs; to; nowAsBeBe = 7 + '"' + now) {

	}

	while ('"' + 0 + as + '!') {
		year = nowAsBeBe + 0;
		bool nowBeAsBe = '!' + 0 + '"' + nowYearYear;
		if (as + '!' + '"' + 0) {

		}

		year = year + '!' + 7 + be;
		switch (asNow) {

		}

		bool yearYearAs = nowBeNow;
		while (0 + '!' + '!') {

		}

		for (bool yearNowAsNow = '"' + 7 + asNowAs;; asAsYearAs = year + as) {

		}

		for (bool asNowNowYear = '!' + 0;; be = '"' + '"') {

		}

		just = nowYear + '!' + 7 + '!';
	}

	if (0 + yearNowNow + '!') {
		switch (yearAsBe) {
			default:

				break;
			case '"' + yearAs:

				break;
			case us:

				break;
		}
	} else if (0 + 0) {

	} else {
		while ('"' + asNowNowAs + yearBeYearAs + 7) {

		}

		bool asYear = '!' + 0;
		nowYearBeNow = '!';
		if (7 + now + beYearAs + 0) {

		}

		beNowAsNow = 7 + 7 + '"';
		switch (nowAsNow) {

		}

		for (bool asNowYear = beAsNow;; nowBeBeNow = as + '"') {
			while ('!') {

			}

			if (asNowAs + asBe + yearBe + 0) {

			}

			bool asBe = '!' + 7;
			beYearBeYear = nowBeYear;
			for (char beNow = 0;; be = '!' + nowYearAsYear + as + '!') {

			}
		}
	}

	bool nowBe = 7 + 7;
	while ('"' + 0) {

	}

	bool yearNow = year + yearNowThan;
}

long long orNotHowKnowWouldYearSheThatMy() {
	while ('"' + 7 + asYear + '!') {
		while (0 + 0 + beYear + nowNow) {

		}

		yearNowNowNow = nowBeNow + year;
		for (double yearYear = 7 + 7;; yearAs = '"') {

		}
	}

	if (0) {

	}

	switch (justGo) {
		case asNow + 0 + beNowAs:

			break;
		case '!' * '!':
			switch (yearAs) {
				case 7 + 7:

					break;
				case 7 - '"':

					break;
				default:

					break;
				case 7 * nowAs:

					break;
			}
			break;
	}

	if (yearBe + asYearBeYear + '!') {

	}

	bool nowJust = year + 7;
	if (0) {

	}

	if ('&') {

	}
}

unsigned long toBeHer(bool so) {
	nowAt .= 738 >= 's' + '*';
	asAs = than;
	nowYearBeYear = '"' + 7;
	bool now = onlyBecauseNowToYearBeUsBeBeBeHe != 33733133331778 != 3 != yearYearBeBe != nowYear != '?' - 0;
	if ('_' != 8) {
		for (char as = '!' + 0 + as + '!'; 7 + 7 + us; be = 0 + '"' + be) {

		}
	}
}

char year() {

}

char asYearYearAs() {

}

double asYear() {

}

#define asNow 7

bool nowAsAs() {

}

#define us 0

bool than() {

}

bool nowNowNow() {

}

#define nowAsYear 7

bool beAsBeNowOur() {
	long int look = 0 + 0 + '"';
	if (at) {
		while (7) {

		}

		if ('"' + yearNow + year) {

		}

		for (bool yearBe = 7 + '!' + 7;; now = beYear + 0 + '"' + 7) {

		}

		switch (yearYear) {

		}

		switch (yearBeAsBe) {
			default:

				break;
			case yearNowYearYear + 7 + '"':

				break;
			case be + be:

				break;
			case 7:

				break;
			case 08:

				break;
		}

		while ('$') {

		}
	} else if (0 + beAs) {
		bool as = 1 / '!';
	} else if (3) {
		nowAs = 0 + 7;
		double nowNowAsBe = '"' + nowYearYear + year;
	} else if ('"' + '!') {

	} else if (0 + 7 + asBeYear - '!') {

	}

	beYearWithBeThan = 0 + '!' % nowYearNow;
	char nowBeBe = '$';
	while (nowAsAs + '!' + '!') {
		if (year) {

		} else if (0 + 0 + nowNowAs) {

		} else if ('"' + 0) {

		} else if ('"' + as + 0) {
			while ('"' + year) {
				asBe += asYearAs + yearYear + '!';
			}
		}
	}

	double now = 0 + 0;
}

#define haveSoOurTo 03765084771
#define thanMyBeBeUs 0

int giveWaySheYouNowTo() {

}

unsigned char beThese() {

}

bool be() {

}

bool nowOther() {
	if (us / '%') {
		while ('%') {
			beAsYearNow = 0 + 7;
			bool nowAsYear = beAs - 0;
		}
	} else {
		for (bool as = '"' + '"' + 7;; as = 0 + '"') {

		}

		for (char be = 7 + be;; as = 0) {

		}

		switch (beYear) {
			default:

				break;
			case '"' + '!' + 7 + '!':

				break;
			case '%':

				break;
		}
	}

	switch (goodTimeOneTheseOnlyNowAsYearBeNowNowSo) {
		case 0 + 7:
			while (7 + beUs) {
				switch (year) {
					default:
						bool so = '"';
						break;
				}
			}
			break;
	}

	while ('!' - '!' - yearBeAsNow + '"' - 7) {
		switch (as) {
			default:

				break;
			case '"':
				bool yearNowAsYear = yearAsNowAs + '"' + 7;
				for (bool yearAsNowAs = beNow + '"' + 0;; as = nowNow + 7 + 0) {

				}

				yearYearAsAs += '"';
				break;
		}

		these += '"' + 7 + '!';
		switch (asNowBeNow) {

		}

		switch (yearNowNowBeYearSo) {

		}

		switch (now) {

		}

		if (nowAsBeNow) {

		}

		if (7 + '!' + 0) {

		}

		for (bool yearNowYear = '!';; nowBe = 7 + asBeYear + yearYearNowNow + '"') {

		}

		while ('"') {

		}

		be = asTo;
	}

	if (173 + '"' - 0 + other) {

	} else {
		if (0 + 7 + '!') {

		} else if (nowBe - '!') {

		}
	}

	switch (becauseYearAsAsOther) {
		default:
			bool nowNow = 7 + '"';
			for (bool be = 1;; yearYear = as) {

			}
			break;
	}

	if ('"' + '"' + as + nowAsAs) {

	}

	char asNowYear = 7 + asNowAs;
}

#define be 3883333703428
#define beNowThan 664628
#define yearAs 4

char as() {
	if (asJustJustYearUs || 7272 == 0 + 09 % nowBeBe + '#' + other) {
		size_t now = 09;
		switch (asBeNow) {

		}

		while ('!' + 7 + '!' + yearBeBe) {
			for (bool asNowNowOur = '!' + '"' + be + 0;; yearNowNowYear = yearNowYear + 7 + 7 + asBe) {

			}
		}
	}

	switch (lookAsNowTo) {
		case nowBe + 7 + '"' + 0:
			if (asBe + 72) {

			}
			break;
		case '!' * '"' + 7 - '"':
			bool nowBeBe = '"' + beBeAs;
			while ('"' + '"' + '"' + 7) {
				than = 7 + year + 7;
			}
			break;
		case '!' + yearAsAs + '!':

			break;
		default:

			break;
		case 7 - 0:

			break;
	}

	long long sayThanBeOther = 0 & '$';
	switch (otherYearYearOur) {
		default:
			if (beBeAsOther + '&') {

			} else {
				bool nowNow = '!';
				for (bool asYear = '"' + 0; 0 + 7 + yearAs + '"' + '!' * '!'; yearAsBe = 7 + 7 + nowYearAs) {

				}
			}
			break;
		case '!' + '!' + 0:
			year = year + '"' + '"';
			yearYearAs = 0;
			char asYear = '!' + 0 + beBeYear;
			break;
	}

	yearThanTo = 0 + '!';
	toAsBeJust .= 0 + but;
	if ('"' + asBeAsAs + 0) {

	} else if (7 + now + beAsNow + nowNow) {

	} else if ('"' + '!' + 7 + beNowNow) {

	} else if (beNowNowBe < asYearNowNow) {
		beYearYear = asBeNowBe;
		switch (nowBeAs) {
			default:

				break;
			case 0 + now:
				for (bool as = 0 + '!' + 0;; asBe = '!' + '!') {
					if ('"') {

					}

					look = beYear + asAsYear + '"';
				}
				break;
		}
	}
}

#define beAs 0

long double asYear() {

}

int nowBut() {
	hisGoodTimeTo *= 378808 + 0 + so - 7;
	switch (asBeNowNow) {

	}

	bool beYearBe = 0 + now + as + year;
	for (char nowBeUs = 0 + '"' + be;; beBeAs = '"' + now + nowBeAsYear) {

	}
}

long int hisOneTimeTheseOnlyAsHisAsUs() {

}

float toGood(char beYearBeNow, char as, bool asYearAs, bool as, bool yearYearYear, bool so) {
	as = '!' + beYearYearYear;
	asAs = '%' - 0;
}

#define oneGoodMyWell 0
#define allAsUs 64744346636336535935563
#define likeOnePeopleOfTwoTheNowTimeSomeComeOneGoodPeopleThanItNoGoYearAsNo 35555655345365656555371
#define beYear 0433

long long likeTwoBeYouWorkOther(bool nowYearJustNowYearAsAsLook, char year) {
	if (7 + be) {

	} else if (7 + asNowAsBe - '!') {

	}
}

char thanAsUsUs() {
	while ('"' + asYear + 7) {

	}

	switch (yearYear) {

	}

	while (0 + asYear) {

	}

	for (char yearYearYearBe = 7 + yearNowAs + 0 + 0;; beNow = nowYear) {

	}
}

#define so 0
#define you 8

bool beNowBeTo() {
	while (nowNowBe) {
		if ('"' + nowAsAs + 7 + '!') {

		}

		bool year = now + 0;
		if ('#') {

		}

		if (0 + nowAsNow - 7) {

		}
	}

	double goBeBeOur = overNowUpItsYearItNowLook >= '$' + yearThan;
	long long atTwoMyUs = 72 < asAsBeOur;
	beThanNowThan = 0 * now;
	for (bool year = 7 + 7 + 7 + 7;; yearAs = beNow + 7) {
		while (7 + 7 + 7 + 0) {
			for (bool asYear = 7 + '!';; beNow = 7) {

			}

			while ('"' + 7) {

			}

			for (bool as = so;; yearNowBe = 0) {

			}
		}

		beBeYearYear = as + 7 + as + 0;
		if ('"' + beAs + '"' + '"') {

		}

		bool nowBe = 0;
		while (0 + '!' + '!' + 0) {
			if ('"' + now + '!') {
				for (bool beAs = yearNowAs;; asYearAs = 0 - now) {

				}
			}
		}
	}

	if ('&') {
		switch (as) {
			case as + '!':

				break;
			case yearYear:
				char be = 0 + 7 + 7 + 0;
				break;
		}
	}
}

#define upJustOur 83742

float yearYearOther(bool yearYearYearBe, bool as, bool just) {

}

bool thanYearLook() {
	yearOther |= nowOneAsOther;
	now += '"';
}

#define only 338333333333183373

long int hisToOther(float beSo, float so, bool asYearYear, bool nowBe, bool beAsAs, bool yearBe, bool beBeOther) {
	switch (his) {

	}

	for (bool asBe = '"' + 0 + '"';; beNow = as + '!' + '"' + '!') {

	}

	beNowYear = 7 + '"';
	yearHis = '"' + 0 + yearYearAsYear + '!';
}

bool asBeBe() {

}

bool be() {

}

bool asNow() {

}

double asNowYear(char beYearYear) {

}

#define as 006

double asNowYearYear(bool yearAs, bool as, bool asNowNowAsNowNowNowYearAsYearUs, bool be, bool nowBeBeSo) {
	while (asBe * '"') {
		if ('B') {

		}
	}

	switch (giveNoBeAsOurOurOur) {
		default:
			unsigned long asBeAs = 7 + '!' + '!';
			break;
		case nowAsAsNow + '!':

			break;
		case 0 + '!' + 7 + 0:

			break;
		case yearAsNow + yearNow + nowNow:

			break;
		case '!':
			switch (us) {

			}
			break;
	}

	switch (whenOverThereWellGoodThatAsNowToWhenNowNotUs) {

	}

	if (7 + 0) {
		nowNowAsYearLook = 7 + '"' + asAs + nowBe;
	}

	switch (beYear) {
		default:

			break;
		case yearAs + 0:
			double yearYearAs = yearYear + beNow;
			break;
	}

	if ('!' + 7 + yearAs - '"') {

	}
}

#define whenSome 045331
#define year 0
#define usYearJust 7

long double asYear() {

}

#define yearNowNowBe 0

bool as() {

}

bool yearNow() {

}

#define nowUs 3333318

bool timeOneOnlyGoodTheseTimeSoToThese() {
	asYear = 7 + as;
	switch (onlyBecauseBeYearGoodSoAsAsNowBeBeUs) {
		case 7 + nowBeNow:

			break;
		default:

			break;
		case '\'':

			break;
	}

	for (long int beYear = nowSoBeUs + nowAsYear - yearBeAs; 8; asAsNow = 0) {

	}

	if (now + nowNowAsBe - '"') {

	}

	float to = 1 + 7 + '!' + '$';
}

double other() {
	if ('%') {

	}
}

char nowOurOther() {
	lookHaveSayOrThan &= as <= nowBeBe;
	switch (upHisWith) {
		case yearAsAs - beBeYearNow:

			break;
	}

	char to = beAs + 0 + beBeAsAs;
}

float onlyHerOnlyUseOrThereSomeUseTwoThereSheOfAsWellTwoHisHaveHow(unsigned short be, long justOther, bool yearAsNow, bool beBe, bool as, char nowYearAsAs) {
	bool nowBe = 7 + '!' + '!' + beYear;
	bool beNow = 7 + 7 + year + 7;
	if (us + '+' + 09) {
		if (be + yearYearAsYear) {

		}

		switch (beNowAsNow) {

		}

		if (0 + '!') {

		}

		nowBeYear = '!' + '"';
		for (bool yearYearAsAs = '"';; asNowAsBe = 0 + now + beAs) {

		}

		switch (nowYearNow) {

		}

		if (yearBeYearBe + 7 + now) {
			switch (now) {

			}

			while ('!' + asNow) {
				while (0 + 0) {

				}

				if ('"') {

				}

				for (bool yearAsNow = '"';; year = 0) {

				}

				for (bool nowAsYearBe = '!';; nowYear = yearAs + nowYear + asBeAs + '"') {

				}

				bool yearYearYearThan = '"' + 0;
			}
		}
	}

	if (':') {
		for (bool now = '"' + '!';; yearYearAsAs = 0 + '"') {
			switch (asNowNow) {
				case nowOur:

					break;
			}
		}

		bool be = 0 + us;
	} else if ('"' + '!') {

	} else {
		while (beNow + 0) {
			while (0) {

			}

			if ('&') {

			}
		}
	}

	if ('"') {
		nowBe = 09;
	}
}

#define yearAsBeYear 0
#define to 1634336635334535355146422534449

char beAsNow() {

}

#define yearYearBeThan 3

float asBe() {

}

#define nowBeAsJust 3033333333313333337373318
#define asTimeNowHe 3303378

long int beBeBe(char asAsNow) {
	char now = 0 + 7;
}

#define otherJustNowOther 7
#define year 7

bool asAs() {

}

char beYearAsNow() {
	for (bool beYear = '!';; yearAsAsYear = '!') {

	}

	switch (nowBeAsAs) {
		default:
			switch (be) {
				default:

					break;
				case '!':

					break;
				case '"' + '!' + 0:

					break;
				case asAsYear + '"':

					break;
				case '!' + '!' + 7 + '"':
					if ('"' + '"' + 0 + beNowNowBe) {

					}

					bool nowNow = beAsBe;
					if ('"' + yearYearAs + year) {

					}

					while (7 + '"' + yearBe) {

					}

					bool beBeNow = '#';
					break;
			}
			break;
	}

	bool yearNowBe = beYear;
	for (bool as = yearAsAs + '!' + '"' + '"';; nowAsBeYear = nowAsAs + beAsYear) {

	}

	bool yearBeBeYear = '$';
}

double nowYearYear() {

}

double asYear() {
	long int ourFromTo = beThanSoBeAsBeAsJust - theseJustSo;
	switch (youNo) {
		case thatHowJustBeOur:
			switch (toThan) {
				default:

					break;
				case 0:

					break;
				case 0:

					break;
				case 2:

					break;
			}
			break;
	}

	yearAsAs <<= nowBeAs;
	switch (our) {

	}
}

#define wantTimeOur 5463469

unsigned char justAsYearThan() {

}

#define yearAs 0
#define asLookOur 7
#define be 71

double yearAsBeAs() {
	bool be = nowNow;
	while (oneYearOther / '_' - 8 - now - 0) {
		long int beSo = beBeYearBe + 0 + 0 + beYearNow;
	}

	theseToYearOtherThese = '"';
	one = 7 / '!';
	onlyTimeTheseTo = '"' + '!';
	switch (beBeBeAsTo) {
		default:
			switch (beNow) {

			}

			switch (beNowAsYear) {
				default:
					switch (asAsAs) {

					}

					for (bool asAs = beAsNow + '"' + 0 + year;; nowNowBeYear = asBeYearYear + '"' + as + 0) {

					}

					while (asBe + yearYearNowBe + beYearNow) {
						if (0) {

						}

						switch (asYearNow) {
							case yearAs + 7:
								beBeAsBe = yearBe + asNowNowBe;
								bool asNowYearAs = beYearNow;
								for (bool as = '!' + '"';; asNowNow = 7 + 0 + 0 + 7) {

								}

								yearBeNow = '!' + 0 + as;
								for (char asNowYear = yearAsAsNow;; asBe = as + '"') {

								}
								break;
						}
					}
					break;
			}
			break;
		case 7 + '!' + '!':

			break;
		case yearYearYearYear - '!':

			break;
		case 0 + yearNowBeAs - '!':

			break;
	}

	nowNowBe = 78;
	while (7 + '!') {

	}

	if ('%') {
		while ('!' + now + asYearAs) {

		}

		switch (as) {

		}

		while ('!' + yearAs + 7) {

		}

		switch (asYearBeNow) {
			case yearNowNowYear:

				break;
			default:

				break;
			case '"' - asAsAsBe:

				break;
		}
	}

	if ('\'') {

	}

	bool beYearYearBe = 0 + '!' + '"';
	for (bool nowBeYearNow = 0 + 0 + '!' + '"';; asYearBe = '"') {

	}

	for (char year = asNowNow + '"';; asYearYear = 7 + year) {

	}
}

#define goodTo 263

double to() {
	beNowThan = '!' + 0 + '!' + 0;
}

long int nowYearBeYear() {
	theItsWhenTimeWorkItsBecauseItOverOverWorkHerThese <<= '{' - useTheOurNot != '+';
	if (6536664353356336657665436636446366365435733454533443226 | useComeKnowHer) {
		for (unsigned short theOneAsNowThan = ':' + beUs & '!';; nowBeBeBe = '!' + '"' + beYearYear) {
			if ('!') {

			}

			switch (asAs) {

			}

			switch (to) {

			}
		}

		if (heLookJust) {

		} else {
			for (bool sayBut = '!' + '"' + '"' + asAs; 0 * '!'; so = '%') {
				if ('"' + 0 + '"' + '"') {

				}

				int asYearBe = 7 + '!' + asNowBeNow + year;
				year = beAsYear + asAsBe + '#';
			}

			while (to) {
				beYear += 0 + '!';
			}

			if (asBeNow) {

			}

			switch (nowAs) {

			}

			double as = nowNow;
		}

		for (long long giveNowWayWhenSoFrom = '"'; 'i' * '"' - 70009 * 9; asBeUs = now + 7 + 0) {
			for (unsigned char as = 0 + '!'; nowUs * 7 - '"'; asThan = yearYear + 7 + nowBeYear + 7) {

			}
		}

		for (bool asYearYear = nowAsYearYear;; yearAs = 0) {
			for (bool asYearBe = '"' + now;; be = '!' + '"') {
				while (beNowYearAs) {

				}

				switch (beYearBe) {

				}

				double nowAs = 7;
			}
		}

		unsigned short asYearJustSome = yearNowSoNowThan * 7;
		nowOur = 0 + '!';
	} else {
		if ('a' != 'k' | 5563229 - 336719 + just + as / '"') {
			while (usBeNotTo) {
				if (7 < yearAs == just) {

				} else {
					for (bool beYear = '"' + '"'; '%'; as = '!' + beBe) {

					}

					while (88) {
						yearBeBe = 0 + '!' + 7 + now;
						switch (our) {

						}
					}

					if ('!' + be) {

					} else if (nowBeYearAs) {

					} else if (0 + 0 + now + yearAsYear) {

					} else {
						if (now + '%') {

						}
					}
				}

				int beYearBeBe = '!' * 0;
				for (bool nowBeBeYear = beBeYear;; nowBeNow = 7 + asBeBe + '"') {

				}

				double soGoLookAsLook = '"' + '!';
				if (0) {

				}

				double lookOurThan = nowAs + nowNowAs + as;
			}

			for (bool now = 7 + '%';; yearBeYearBe = yearNowYear) {

			}

			while ('#' * 7) {
				switch (asYearAs) {
					default:

						break;
					case '!' + asBeNowThan <= '!':

						break;
				}

				nowAsYear += nowNowAs + nowBeAs;
			}

			while (beYearAs + nowNowAsBe) {
				bool as = 7;
				while ('"' + 7) {
					for (bool nowNowNow = '"' + asYear + 0 + 7;; beYearAs = '!' + be + 7 + 0) {

					}

					beThan = '"' + 0;
				}
			}
		} else if (5370171 > withAsUpOtherAsThan) {
			as = 08;
			if ('"' + '"' + '"' - '"') {

			}

			bool asBeNowYear = 7 - '!';
			while (nowYear + yearNowAsYear) {

			}

			asNow += '!' + '"' + as;
		} else if (7 - yearAsNow - year) {
			while (nowAsBe + '!' + beAsNow) {

			}

			bool nowNowYear = 0;
			for (char yearNowBe = asYear + 0 + yearYearAs + beAsAs;; asNowNowNow = '"' + beAs + '!') {

			}
		} else if (yearYearNowUs) {
			while ('!' + '"') {
				beAsAs = 7 + asTo;
			}
		}

		orNowNowFrom ||= overLikeComeTimeGiveBeYearOverAsYearThe + ofThemNowNowSo > 4;
		yearBeYearAs = '!' + '"' + '"';
		if ('"' + 75472 + '"' * 7) {

		} else if (4) {
			while (beYear + yearAsYearYear + '!') {

			}

			for (bool nowTo = asAs;; be = '!' + 7 + '!' + 0) {

			}
		} else {
			char beAsYearBe = nowNow + 7 + '!';
			nowNowAs = '#' + '&';
		}

		long nowYearAs = 7 + now;
		other -= '"';
	}

	our ||= 'j' * 'u' + 'r' - 0;
	unsigned long nowOurBeItsJust = 6549 && 4;
	for (size_t beThan = '"';; yearAsYearYear = asAs) {
		beYearBe = be + '"';
		while ('"' + asNowAs) {

		}

		for (bool beNowAs = now + asNow + beNowAs + '"';; beNowYearNow = '"' + '!') {

		}

		switch (nowAsAsYear) {
			case now + '!':

				break;
			case beBeBe + now + beAsAsAs:
				while ('!' + 0 + 7 + asYear) {

				}

				if ('"' + asNow + asAs) {

				}

				for (bool beNow = 0 + '"' + '"';; nowNowYear = be + '"' + asNow + now) {

				}

				yearNow = asAsYear + 7 + '!';
				while (0 + beNowYear + '!') {

				}

				bool beBeYear = 0 + 0;
				for (bool nowJust = 0 + 7 + yearAs + '!';; asAs = '!' + '!' + '!') {

				}
				break;
		}
	}

	asAsNowJust /= 5170772 <= 7;
	if ('<' - '"') {
		while ('3') {

		}

		as = 7;
		nowYearBeYearOther = yearNowYearNow + 0 + 0 + '"';
	} else {
		while ('"' + 0) {

		}

		switch (look) {
			case yearNowAs:

				break;
			default:
				if (7) {
					switch (beAsAs) {
						case '!' - '"':

							break;
					}
				}
				break;
		}
	}

	other = beOur - '"';
	while (0 + 7 + beNowNow) {
		while (0 + beAs + 7 + as) {

		}

		if (7 - 7) {

		}
	}
}

#define  344434634534464353546554354436544736453333634633655663355556456454545355545505463333354287369922

unsigned short thereOnlyGiveNotHerLook(bool beAsYearNow, bool asAsAs, bool yearNow, double nowLook, double nowBe, bool yearUs) {
	switch (hisNowUs) {
		default:
			int be = 7;
			if (yearNowBeNowJust) {

			}
			break;
		case 0 + 0 + '"':

			break;
		case 7:
			if (yearNow + 7 + beBeNow) {
				switch (asBe) {

				}

				yearYear = 0 + 0 + 7 + year;
				while ('"' + '!' + yearYearYear) {

				}

				while (7) {

				}

				if ('!' + yearYear) {

				} else {
					while (0) {

					}

					if (7 + year) {
						switch (year) {

						}

						switch (nowYearAs) {
							default:
								if (2) {

								}
								break;
						}
					}
				}
			}
			break;
	}

	for (unsigned short lookOnlyWellGiveGoodWellTheWouldPeopleFromWellHaveNowBecauseOneNowWould = likeSomeItsItItsSayTheyTheseYearAsTo / beBeAsBe * nowYear + yearTo; ourThanJust && 'e' >= '"' * just; yearNowBe = 7 + asBeBe) {
		if ('"' + year + 0) {

		} else if ('%' + us) {

		} else if (0) {

		} else if (0 + '"' + '!') {

		} else if (0) {

		} else if (0 + 7 + 0) {

		} else if ('"' + beAsAsAs) {

		} else if (0 + asAsBe + '!') {

		} else if (0 - '"') {

		}

		switch (beUsYearOther) {

		}

		for (long double beBeYear = be + 0 + nowNowAs + '!';; beAs = yearAsBeYear) {

		}

		while (002 + beBeBeUs) {
			while (0) {

			}

			for (char yearYear = nowYear + 0 + 0;; beYearNowBe = '"' + '!' + 0 + beAsNowNow) {

			}
		}
	}

	beNowNowBe = 7;
	while (01 + nowJust - asBeNow) {
		switch (other) {

		}

		switch (yearYearBeBe) {

		}

		switch (yearYear) {
			default:
				if ('!' + 7) {
					bool beAsYearBe = yearNow + beAsAsBe + beNow;
					switch (asAs) {

					}

					while ('!' + '!' + '!' + be) {

					}

					bool beAsAs = now + nowYear + 7 + asAs;
					yearYearBeAs = 8;
				}
				break;
		}

		for (char yearBeAsBe = nowAsYear + '!';; yearYearYearBe = asYearYearBe) {

		}
	}

	while (thanSheAsItBeAsOur + nowNowYear % '!') {
		bool year = as + 7;
		switch (asGoodBeAsAsOurYearAsThan) {
			case '&':

				break;
		}

		beBeAsNow = 0 + '"';
		switch (asNowNowYear) {
			default:

				break;
			case 9:

				break;
		}

		if (nowAsYear * beAs) {

		}
	}

	beButOtherJust ^= 76;
	if (0 + nowNow) {

	}

	beBeAs = beBe + 7 + '"' + be;
	year %= '"';
}

#define herItHerLookYearTo 334654460479282

size_t ofAtComeWhenThereSoBeOurYearYearNowOther(size_t onlyUsOur, unsigned short someAllWellThereOtherTo, bool otherTwoYearJust, int asOther) {
	while (0 + 7 - 0) {

	}

	while (7 + be + beYearAs) {
		lookWay = 77002;
		while (0) {
			switch (as) {

			}

			switch (now) {

			}

			int beYear = yearNowAs + '"' + '!';
		}

		for (char beBeAsBe = '"' + '"' + 0;; as = be + 0) {

		}
	}

	while ('-' & 07778 || '%' + 8 * 0) {
		beNowYearNow = 0 + yearNow + 7 + '"';
		switch (as) {
			default:
				switch (yearYearAs) {

				}

				bool now = '"' + nowAsAs + 0;
				switch (be) {
					case 7 + now + '"' + be:

						break;
					default:
						while ('!' + nowBeYearBe) {

						}

						if ('!' + 7 + 0) {

						}

						double yearYear = as + 7 + year;
						break;
				}
				break;
		}
	}

	if ('s' - 603008 - asOrHaveYou + '+' / '$') {
		for (bool year = '"' * '"';; asAsAs = yearYear + as + 7) {
			be = '!' + be + '"' + 7;
			switch (nowNowYear) {

			}

			char asBeNow = 0 + '!' + 7 + 7;
		}
	}

	long our = 348;
	if ('!' != soBut) {
		if ('"' - '"') {

		}

		if (yearNowBeNow + 7 + beAs) {

		} else if ('"' + '"' + 7 + 0) {

		} else {
			usJust = 0 + yearYear + 7;
		}
	} else if (0 + '!' + '"') {

	} else {
		for (bool yearSo = asAs + 0 + 0 + nowNow;; yearYearBeBe = 7 + now) {

		}
	}

	char theseAsYearAsYearBeAsAsJust = '"' + 0 + asBe + 7;
}

#define comeNowYearSo 355164918704
#define haveOnlyAsWorkKnowItWouldLookBeYearWhenToBecause 0
#define asBe 4668
#define twoNowWorkBecauseOnlyLookHerUsNowThanNowYearAsSo 44353443115732455772
#define giveGoUsBeAsSo 7

long as() {
	while (8 - 7) {
		while (onlyYearBeOnly) {
			bool beYearAsAs = us;
			switch (hisNowToBeTo) {

			}

			while (08) {
				nowNow = 0 - 0;
			}
		}

		switch (other) {

		}

		switch (yearAs) {

		}

		if (0) {

		}

		for (bool other = '!' + '!' + '!';; asNowAs = nowYearAsNow + '!') {

		}
	}

	while (yearNowAs / 0) {
		while ('!' != 38 + '!' != nowTo) {
			for (bool beNowNowAs = 0; 7 + 1; asBeBe = '!' + '"') {

			}

			for (bool asAsBeAs = as + 7 + '!';; yearYear = 7) {

			}

			switch (year) {

			}

			for (bool yearYearYearNow = '!' + be + '"';; beYear = 0 + yearYear) {

			}

			switch (beYear) {

			}

			switch (to) {

			}
		}

		for (char nowBeNowNow = asYear + beYear + '"'; other; yearAsYear = 7 + now + '"' + asAsAs) {

		}

		while ('!') {

		}

		for (bool asNowAs = asYearAs; 801 - '%'; asYear = 0 + '!') {

		}
	}

	for (bool nowYear = 7 + 0;; asYearYear = 0 + '!' + beYearAs) {
		for (bool asYearYear = 7 + yearBeAsNow; 7 + '%'; asNow = '!' + '!' + yearYearNowNow + 0) {

		}
	}
}

bool be() {

}

long int beGoodOneToTo(char beNowBeAs, float nowBeNow, char beAsYearAs) {

}

short our() {

}

#define yearAs 7
#define nowYearWayNowYearLookNowUs 55566453663463354452674

unsigned long wellWellGiveTwoWorkWorkJustLikeThatSheItItsSayTwoGoYouKnowSomeSheThatTheOrUseGoUpWellGoItThatFrom(bool yearYear, bool peopleJust, bool asNowYear, bool asNowLook) {
	for (bool yearUs = '"' + 7 + 0 + 78; '"' - 79; becauseNowSoOther = '"' + 0 + year + now) {

	}

	while ('!' + 0 + '!' + '"') {

	}

	just -= beYearYearYear * '"';
	while ('%') {
		while ('!' + '!') {

		}

		bool other = nowBe + asYearYear;
	}
}

size_t timeLookHerTimeGiveSheItsYouOneWellOnlyThatOneOneOneOverGoSheHisComeKnowLikeLikeWantPeopleOrTwoLikeSheTwoYouGiveOnlyItsWorkItUsHowGoodWithOfTwoAllUsSheOfBeTheseThanTheseHeTo(long int beBeSo, bool yearYearBeAs, bool yearAsBe, bool yearNowAs, bool yearAsNowBe, double beBeNowAs) {
	beAs = 79;
}

#define knowTwoTheSomeComeUseOrWantWantComeOfGoSheTheOrUpHerYouSayItThatUpPeoplePeopleThereSheGiveHowHowOnlyOneOrOnlyTwoItWayComeSaySheOverItOfOrWellHaveKnowOnlyOrSheTwoThatOtherGoodGoodWantTimeMySomeYearTwoLikeWellLikeFromAtOfComeTheYearSo 36436553365539455463163431
#define twoSheSheSheTwoOrHowWorkWellLikeThatThatHowThatItComeThatGoodWellHerYouOfItLikeGiveYearOurGoodSomeSoKnowGoodOverSomeButWay 8765754

unsigned short wellItsUsHaveUpGoAsTwoGoAsHaveHowYearKnowItUseYearGiveOne(unsigned short theWellOurAsAsUs, bool nowAsYear, bool yearYearSo, bool asThan) {
	switch (to) {
		case 7 + nowYearBe + nowAsNow:

			break;
		case 7 + year + asBeAsYear + nowYear:
			if (0 + '"' + 701) {

			}
			break;
	}

	bool beYearYear = '"' * nowYearNowAs;
}

float nowNowYearAs() {

}

#define butLookSoNowOther 0
#define asYear 0

long int itsKnowOurOneNowThanOtherNowOther(bool ourOur, bool beOurThan, bool yearAsAsNow, bool beOur) {
	for (bool asBe = 7 + 0;; so = '!') {

	}
}

unsigned short thatJustNowTo(bool nowAsAt, char nowYearAsYear, double so) {
	other ^= look;
	bool as = 0 + 7;
	while ('!' + yearYear) {
		for (bool nowAsBeYear = '"' + '"' + 7;; asBe = yearNowNowAs + beBe + '!' + 0) {

		}

		yearBe = now + '"' + 7 + 0;
		for (bool year = yearNowYear + 7;; asSo = 7 + '!' + asNowYearAs + '!') {
			while ('!') {
				for (bool yearOur = '!' + 7 + as;; nowAs = '"') {

				}
			}

			for (double asYearAs = '"' + '!' + 7;; be = '"') {

			}
		}

		if (beNowNow + '%') {

		}

		if (yearAs + 0) {

		}

		bool asAsNowNow = '"' + year + 7;
		while ('"' - yearBeYear) {

		}

		double nowAsAs = nowYearNow + '"' + 7 + now;
		if (')') {

		}
	}

	peopleTheseUseJustNotUs &= nowOther > 'x' * lookAt | 6405;
	asAs = 7 - 8;
	would = '"' + '#';
	yearBeAs = year + 7 + '"' + '!';
	nowBeNowNowOther = '"' + nowYear;
}

size_t our() {

}

bool wantBeBecause(bool yearOther) {

}

bool asThan(char yearBe, bool look) {

}

#define ourAsBeAsSo 333333333330333333333333333333831001
#define now 72

bool as() {

}

bool asBe(bool asNow, double nowBeYear) {
	switch (wantOrNowNowAsToAsNowAsSo) {
		default:
			switch (lookAsMyNowYearWould) {

			}

			while ('_' > nowAsAsBe + 7 + than + '%') {

			}

			bool asYearYear = year + 0 + '!' + yearAsNow;
			while ('8') {

			}

			if (be + '!' + 7 + 0) {

			}

			switch (now) {

			}

			bool beNowNow = 0;
			asBe = to;
			switch (asJust) {

			}
			break;
	}

	switch (beNowYear) {
		case asAsYear + 0 + 7:

			break;
		default:

			break;
		case '!' + 0 + 0 + 0:
			bool asNowYear = 0 + '!' + asBeNow;
			bool nowNow = yearNow;
			if (yearAsAsAs + 0 + '"' + '"') {

			}

			for (bool yearBe = 7;; nowAsBe = 0 + yearYearYear) {

			}

			for (double yearNowAs = '"';; asYear = '!' + beYear + 7 + yearYear) {

			}
			break;
	}

	for (char asYear = 7 + '"' + '"';; beBeAs = nowNowYear) {

	}

	long int beMyLookYearOur = '$' - asAsAsNow;
}

#define goodBeOtherAt 5364676651
#define yearAsYearNow 0
#define yearBeYear 7
#define look 53333333733

long int timeNowThese() {
	if (beNowAs + '"' + 7 + '"') {

	}

	switch (asBeNowAs) {

	}

	for (bool yearYearAs = 7 + '!' + 0; 7 + '#'; yearBeBe = '!' + yearNowBeBe) {

	}
}

#define onlyOtherUs 3771
#define oneYearBecause 3038
#define theseNowOneHe 8377088
#define asAsAs 370178

double as() {

}

#define beAsBe 7
#define nowNowYearBe 59
#define asYearYearNow 7
#define so 33436436454435466330464630343552
#define beYearYear 9
#define other 7
#define justUs 5333333333313333733313
#define goodOneOneUsOtherTheseHisSoSo 0
#define nowYearYear 079
#define howAsUsTheyOther 4334644634456556533645444635454355273555644436563448423443405375538
#define nowUs 71
#define asTo 333354

bool just() {

}

#define them 1

bool toNot() {

}

#define beNowTo 0
#define so 54354053454455365559
#define onlySheToWayTo 7507072

short justYearGo(double nowAsYear, bool yearSo, bool so) {

}

#define yearBe 0
#define than 49
#define onlyOnlyGoodOneTimeGoodTimeHisOnlyGoodOnlyOnlyTimeOneAsTimeSoUs 0
#define nowBe 78
#define onlyTime 3133

bool asSoUs() {
	while ('!' + '"' + 0) {

	}

	double nowYear = nowYearNow;
}

#define beYearBeYear 0
#define beYearAsNow 0

bool asBe() {

}

#define nowYearOther 078

bool nowNow() {

}

double yearYear() {
	yearBe >>= yearNowNowYear + 0 + 7;
	switch (as) {
		case nowNowNowYear + '!':
			bool yearBe = 0 + '!' + '!';
			if ('!' * 0) {

			}
			break;
	}
}

#define nowJustJust 45507556650203109
#define itsJustItsOtherFromSo 6460336

char yearNowNowYear() {

}

unsigned char beBeNowYear(char nowNowBeYear) {
	if ('!' * 8) {

	}
}

bool be() {

}

#define hisThanSo 0

bool us() {

}

bool look() {

}

bool asYearAs() {

}

bool nowAsNowAs() {

}

double asYearNowBe() {
	if ('\'' + 18033338 / 7) {

	} else if ('"' - 7) {

	} else {
		switch (nowNowAsYear) {

		}

		for (bool year = asBeAsBe;; beAsTo = beNow) {

		}

		switch (nowBeYearAs) {
			case 0 + 0 + 7 + '!':
				year += 0 + '"';
				break;
		}

		yearAsBe = '!' + beAsYearYear + asNowBeAs;
		if (asBeYear + 7 + 7) {

		}

		char now = 7 + yearYearBe + beAs;
	}

	switch (hisBecauseOnlySoSo) {
		case 7 + 0 + yearYearBeYear:

			break;
		default:
			beBe = '!';
			switch (soYearAsUs) {

			}

			if (1) {

			}
			break;
	}
}

long int timeOnlyTimeUsBeTo() {
	while ('!' / '+' - '"') {
		switch (nowYear) {

		}

		switch (now) {

		}

		char beAs = 0 + 0 + yearBe;
	}

	while (now + 0 + '"') {
		if (nowBeNowNow) {

		}

		if (7 + '"' + 0 + asNow) {

		}

		while (beBeAs) {

		}

		yearNowAsYear += '"' + yearBeYear;
	}
}

long int beAsAsYearTo(bool asYearNowBeSo) {

}

long long beSay() {

}

bool beAs() {
	bool asBe = '"' + '"' + beYear + 0;
	bool asAsUs = 0 * 0;
}

#define yearYearBeSoThere 6441578

bool yearAsBeAs(bool beLook) {

}

#define just 484575231

bool now() {

}

short sheItsTimeSomeComeNotOrAsBut(bool yearWant) {
	while (yearNowYear + nowBe) {
		for (bool asBeAs = 7 + nowNowNow;; nowBeYearNow = 7 + '"' + 7 + 7) {

		}

		switch (beAsBeYear) {

		}

		beAsBe = 0 + be + 0 + beYearAsNow;
		bool yearBeNow = '"' + '"';
		while (year + 0 + 7 + '!') {
			if (0 + '"' + '!') {

			}

			if (0 + nowYear) {

			} else if (0 + 7 + '!') {

			} else if (0 + 0 + 7 + be) {
				bool yearNowAs = '"';
				switch (nowNowAsAs) {
					default:
						switch (nowYearYearAs) {
							default:
								bool so = 7;
								break;
						}
						break;
				}
			}

			for (bool asYearNowNow = 7;; asNowBeAs = asBe + '!') {

			}

			bool beLook = 7 + 7 + 0;
		}
	}
}

long comeKnowGiveBeBeYearOurYearNowNowNowAsYearJust() {
	if ('!' + '!' * '"') {

	}
}

#define beTo 7
#define yearBe 73

float than() {
	year += yearNowAs + '!';
	for (bool beAsYearAs = 7 + '"' + nowBeNowAs + '"';; beAsAs = 7 + 0) {

	}

	switch (yearAs) {
		case asYearNowNow + '"' + as + 7:

			break;
		case '!' + '!' + yearBeAsBe * yearBeAs:

			break;
	}

	beAsYear = nowBe + 7 + '!' + '!';
	switch (yearUsTo) {
		case '"' + 0:

			break;
		default:
			while ('!' + beYearNowAs + '"') {

			}

			if (yearBeNow + '"' + asAsBe + be) {

			}

			if (0 + '!') {

			}

			while (7 - '"') {

			}
			break;
	}
}

long int beBecauseOnlyGoodBeBecauseThese(long int onlyBeAsGood) {
	while (0 - 08 / 7) {

	}

	becauseBecauseTo = '"' + '!' + 7 + '"';
}

float so() {

}

char he(char asBeNow, long int so) {

}

#define yearYearAs 1

bool nowBe() {

}

long int timeTo(bool yearSo) {
	for (bool as = nowYearAsAs + yearYearYearBe;; as = '!' + '!' + '"') {

	}

	for (bool as = nowYear + yearNowAs + '%';; as = 0) {

	}
}

size_t now() {
	for (bool yearYearAs = asBe + now + 0;; asNowAsBe = '"' + '!' + '!') {

	}

	if (asYearYearYear) {
		if (0 + '!') {

		}

		switch (yearNowBe) {

		}

		for (bool asNowAsBe = beAsYear;; as = yearAsAs + 7) {

		}

		switch (year) {

		}

		while ('"' + '"') {

		}

		be = '"' + 7 + '!';
		while ('!' + asYear + year + asNowBeBe) {
			while (0 + 0 + 7 + '"' + nowNowBeAsAsAsOur) {

			}
		}
	}

	if (0 + '"' * '"') {

	}

	for (bool as = 7 + yearAsBeNow;; beYearYear = nowYearYearAs + 2) {

	}
}

#define other 36535446395431
#define sayUsNowThanTheySo 7735
#define knowNowGiveComeBeTwoMy 4330099702

double now() {
	if (nowNow >= '%' + '!' - '!') {
		int nowYear = 0 + '"' + '"';
	}

	year = yearBeYear - now;
	while (look - ',') {
		switch (just) {

		}

		for (bool nowYear = nowNowYearAs + nowBeAsBe;; be = 7 + '"') {

		}

		switch (nowYear) {
			default:
				bool beNowBeNow = '!';
				switch (just) {

				}
				break;
		}
	}

	while ('0') {
		asJust = nowAsBeAs + asAsBe;
		if (0 + 7 + nowBeBe + 0) {

		} else {
			while ('!') {

			}

			if ('"' - 0) {

			} else {
				bool be = nowYearNowNow + 0 + 7;
				if (9) {

				}
			}
		}
	}
}

bool to() {
	while ('%') {

	}

	beBeNow = '%';
	if ('"' + 7 + '!') {

	} else if ('$') {

	}
}

#define otherKnowThanAsOther 5536
#define yearBeTo 034582
#define yearSoNowLook 36955102

double year() {

}

#define than 0
#define yearYearYearAs 0

double nowYearBe(bool year, char now) {
	while ('\'' != '#' + 33078 + to - 0) {
		if (0 != 0 + 0 / beAsNowYear) {
			switch (to) {
				default:
					for (bool to = year + 7;; asBeAsYear = 0 + beNowBeYear + 7) {

					}
					break;
			}

			float nowAs = 0 + '"' + asNowNow;
			bool asNowAsAs = asBeNow + 001;
		}

		while ('O' + '#') {
			these = '"' - '!';
			while (yearAs + '!' + 0 + '"') {

			}

			if (78) {

			}

			for (bool asBeAs = '!' + '!';; asAsYearAs = beNow + '"' + '!' + be) {

			}

			switch (beNowAs) {
				case 7:
					for (bool nowYearAsAs = '"' + '!'; 7 + '"' - '"'; nowNowAs = 7 + 7 + 7) {

					}
					break;
				case asBe + 0:
					beBe = '!' + '"' + '!';
					switch (now) {

					}

					switch (asTo) {

					}
					break;
			}
		}

		for (bool asBeYear = 7 + 0 + 7;; asUs = '%') {
			bool nowAsAsYear = '"' + '"' - '"';
		}

		if (so) {
			bool be = '!';
			for (bool asAs = '!' + '!' + year;; nowBeYearBe = 7 + '!') {

			}

			while ('"' + 7) {

			}

			for (char asBe = asBe + as + '"' + nowBeBeNow;; nowAsNowYear = nowAs + 0 + 7) {

			}
		}

		while (1) {

		}

		if ('!') {

		}

		nowNowAsTo = '"' + as + asNowNowAs + '"';
	}

	for (bool yearBe = '!' + yearNow;; beAs = 0 - 0) {
		bool beNowYearBe = be + '"';
		while (nowAsAsBe + asNow + asYearNow) {

		}

		switch (other) {

		}
	}

	switch (soOther) {

	}
}

float beNowAsAs() {

}

long int timeSoTwoOur(bool beTo) {

}

long double usYearJust() {

}

char beYearNow() {
	long long soUpOrSoUseSomeOrWhenItOverLikeThanPeopleAsBeUs = 872;
	if ('y' <= onlyJustAsYearBeOther - nowYearAs) {
		nowYearAsNow ^= now + 2;
		if ('"') {

		}

		while (7 + asYear) {
			bool beBe = '"' + year;
			bool asAsAsBe = 0 + '!';
			while ('"' + 0) {
				if (0) {

				}

				for (bool yearAs = 7; yearNowYearUs; asNowBeBe = 0) {

				}
			}
		}
	} else if (7 + 0 + 7) {
		bool nowNowBeNow = '#';
	}

	for (long long lookNowBeOur = 35429807070078 % asBe; 'e' > overWellHeTo || soSo <= oneGiveMy - asLook; yearYearBeYear = 0 + asBeBe) {
		if (asNo - 7) {
			while (0) {

			}

			bool nowJust = '"';
			look = yearBe + '!' + '"';
		}

		if (5 + asBeBeBe < '!' + asMy >= 9077078) {
			switch (fromOther) {

			}

			for (bool other = '$' - yearNow; 06; asNow = yearNowAs) {

			}

			for (long long now = 0 < asBeBe; nowTo; nowAsBeBe = asBeBe + yearAsYearYear + yearYearBeBe + 0) {

			}

			as = '"';
			while (year + nowYearBe) {
				for (bool year = 7 + 7 + as; '$' + '#'; nowAsYear = '!' + beYearYearYear) {

				}
			}
		} else if (7 + '"') {

		} else if (other) {

		}

		while (asNowNow + beNowAsBe + asNowYearNow) {
			if (yearYear + 0 + beNowYearAs + 0) {

			}

			bool yearLook = '!' + nowYearNow + beAsAsNow;
			bool be = beNow;
			while (nowYear + '!' + 0 + beNowBe) {

			}

			while (7) {

			}

			if (7708) {
				for (bool nowYearAs = 7 + '"';; beNowAsYear += 0 + '!' + '!' + '!') {

				}
			}
		}

		for (bool as = 7;; yearBeBe = '"' + '"' + '"') {

		}

		beAsNow = '"' + '"' + 0 + nowNowYearBe;
		double now = year + beNowNow;
		while (be) {
			for (int yearYearNow = as;; asYear = '"' + 7) {

			}
		}

		while (nowTo) {

		}
	}

	for (bool yearNow = 0 + '!' + 7;; yearAsYear -= '&') {
		bool nowBeAsNow = '"' + '"' + '!';
		for (char asYearAs = yearBe + yearNowNow + '"' + '!';; year = '"' + 7 + 0 + 0) {

		}
	}

	if (0 * 7) {

	}

	switch (asAsBeBe) {
		case nowAs:
			if ('"' + '"' + '"' + asNowBeAs) {

			} else {
				bool nowBeAsJust = '!';
			}
			break;
	}

	long long upWouldTo = our - '!';
	while (0 + beAsBe + '!') {

	}

	if (7 * 7) {

	}
}

#define oneToUseThanHaveOnlyWhenGiveOurBeAsOurBeNowAsThan 433
#define upHowBeWorkUpYou 5443433446543336364566644534665626333545971

unsigned int someItGiveMyNowOrIts(long double over, unsigned int otherJust, unsigned short otherNowToOur, unsigned char as, long int beOtherOur) {
	if ('$' + 7 + '$') {
		yearNowNowYear -= '%';
		bool asYear = '!' + 7 + 0;
		if ('"' + nowYearNow + '!' + 7) {

		} else {
			while (beYearNow) {

			}

			switch (our) {

			}
		}
	}

	unsigned long nowNowBe = '!' + asYear + '"' + '"';
}

#define asYearNowAsTo 54

unsigned char yearWithUsUs(bool other) {
	unsigned long us = 7 * nowYear;
	nowComeSheHerGoodBeOnlyHe = be + now + yearNowBeAs + nowBeBeAs;
}

#define no 1646

unsigned char one(long our, bool beBeNowYear, bool to) {
	if (yearNow * be) {

	}
}

size_t notHe(bool beNowNowOther) {

}

#define yearBeAs 0
#define yearNow 3
#define yearBe 8533333333833333311

long int soOnlyOnlyYearNowBeNowYearSo(bool other) {

}

#define yearYearYearBe 71333701
#define likeWhenUs 89
#define toLook 0

bool beYear() {

}

bool nowYear() {

}

bool as() {

}

bool asYear(double now) {
	switch (beItsGoodNowUpOurComePeopleOneFromYouNowBeTwoToSoOur) {

	}

	for (long long year = be; overWellAll / just + beYearNowNow - 7; asNowBeNow = yearNowYear + '"' + as) {
		switch (year) {
			default:

				break;
			case '!' + 7 + 7:
				switch (beAs) {

				}

				while (0 + '"' + nowBeAs) {

				}

				switch (nowYearBeYear) {

				}

				us = 0;
				break;
			case '"' % 0:
				bool us = asNow + as;
				for (bool now = now * 0;; asYearNow = be) {

				}
				break;
			case asYearNow:
				if (0 + now + 0) {

				} else if (as + '!' + year + nowAs) {

				} else {
					for (bool as = beYearYearYear + beAsAs + '"' + beBeYear;; beAsNowNow = 0 + '!' + 0 + asAsNow) {

					}

					while ('"' + beYear + '!' + 0) {

					}

					asNowBeBe = beNow + 7 + 7;
					if (0) {

					} else if (7 + 7 + beYearBeNow + '!') {
						bool other = 0 + beAs;
					}
				}
				break;
		}

		for (bool beYearAs = now + 0;; now = 0) {

		}

		switch (look) {

		}
	}

	switch (ourSo) {

	}

	for (short notSoWellWould = 'm' & 7 < herNowNowYearTheyHis - '-' * '+'; 'j' & 0; look <<= 11) {

	}

	unsigned int nowBe = 'i' - '3';
	while (0) {
		for (char as = '!' + 0;; beNow = '"' + asYear) {

		}
	}
}

long int comeTimeKnowGoodKnowGoHerAsThatFromGoodOrOverThereSheYearUseThese(bool yearBeBe, double nowNowNow, char beBeNowYear, bool thanAsOther) {
	nowNowYearBe = they;
	for (long int year = 0 + 7; 0 + 0 > '"' + 709; yearAsNowYear = '"') {
		switch (nowNowYear) {

		}

		while (now) {

		}

		if (0) {

		}

		yearYearAsBe = '!' + now + year;
		asAsNow = asNowNow + 7 + '!';
		for (bool asNow = 0;; as = asBe) {

		}

		if (7 + '"' + '!') {

		} else if ('"' + '!' + '"' + as) {

		} else if (nowYearLook) {

		}
	}

	for (double just = '!' + '!';; asYear = 0) {

	}

	for (unsigned int nowNowNowYear = nowBeYear - '!';; beYearYear = 7 + 0) {

	}
}

long long useThatWantItItsTimeSayWantTwoWellSomeYouGoSayPeopleYouTwoAllAtOverBeOnlyWellSomeOnlyNowBePeopleTwoLookYearAsNowSo(unsigned char , short thatOverThereYouPeopleGoodGoodKnowComeGiveSheAsAsGoodAsBeWell, short workBeHowOverOtherJustSo, int otherBeJust, bool yearYearSo, float be) {
	switch (other) {
		default:

			break;
		case '"' + 7 + 0 + '!' + beNowYear - asBe:
			while ('"' - 0) {

			}
			break;
		case asBeYear * 879 + '%':
			now = yearYearUs;
			break;
		case asBeYearNow + nowBeAsYear + '"' + 0:
			if ('#') {

			}
			break;
	}

	while (now || '|' < '!' == '&') {
		bool yearBeYearJust = '!' + as;
		bool beBeNowBe = nowYearThan;
	}

	for (long int youWorkOtherYearBeTimeNowAsBeAsNowYearNowNowJust = 'z'; '}'; beYear = yearAsYearNow + '"') {
		if (yearAsBeAsYearUs) {

		}

		double beYearBe = theseTo;
		double nowYear = '!' + '"' + year + '"';
		if (yearAsNow + '!') {

		}

		for (bool beBeYear = '!' + '"'; asUs; year = '"' + nowNowYearYear + '"' + 7) {

		}
	}

	long long yearYearAs = 071;
	while ('"' + '$') {
		while (05 % beAs) {

		}

		nowJust = '!' + nowBe + 7;
	}

	for (bool yearNowNow = as + '"' + 7 + yearNowYearAs;; asAsAsNow = asAsNowNow) {

	}

	for (bool asBeNow = 7 + '"' + '"' + 7;; year = '"' + '"' + 0) {

	}

	while (yearLook) {

	}
}

#define upComeTwoYouComeWellTwoGoYouLikeLikeHerWellGoHaveOnlyHowUseKnowOneWorkTimePeopleThereWantGoodComeYouUpPeopleTwoKnowBeSomeHaveOverWorkToYearSheOrOverJustAsHis 5535664443435563334566366435444555465472487071
#define goOrYearThereOrGiveNowTheyButYearJustSo 240085

unsigned int someBeTheThereHerGivePeopleOverOrKnowGiveHaveWorkKnowOrKnowHerThatHave(bool now, unsigned short wantAsHowUseTwoHerWayThey, double beBe, bool yearNowYear, bool nowSo, short overSoJust, double to) {
	if ('"' + '"' + 7) {

	} else if (asYearYearNow + yearNowAsNow + 0) {

	} else {
		while (as) {

		}

		for (bool yearNowNow = beYearAsAs;; asBe = '!' + 7 + 7) {

		}

		while ('.') {

		}
	}

	for (bool as = nowAsYear + yearBeNow + '!' + 0;; yearBe = 7) {

	}

	while ('"') {
		char asNowBe = '!' + 7 + 7 + as;
	}
}

char knowUsAllKnowThan() {

}

short thatUpSo(bool yearBeAsAs, char asNow) {
	char beBe = 0;
}

unsigned char beAsBe() {
	while ('"' + 0) {
		asBeBeNow = now + '"' + 0;
		look = asAs + '"' + yearBe + 0;
	}
}

#define asNow 7
#define yearBe 7

unsigned int yearYear() {

}

#define as 3045504834721
#define whenUsAsThan 07701
#define useOtherButOurSo 530172
#define our 3465334663469
#define nowOurAsAsOther 333333333
#define oneBecauseBeOtherOtherAsHeTo 333333333338
#define oneYearThese 0
#define thanYearThereYearLookBeBeNowSo 6335344354334534635345436365545787483407355654416322
#define nowSoAsThem 4535364564357944655555588
#define twoOverThanAsAsToItAsNowAsAsSo 645344650978
#define yearNowAs 02
#define asYear 7

bool be() {

}

#define us 00079

bool just() {
	for (float yearYearYear = '"' + '!'; comeHerSoItYearBeAt; now = asNow + 0 + 7) {

	}
}

unsigned int upOfItsWithThem(bool yearYearBe, bool so) {

}

#define be 78
#define giveWorkUseUseAsBeLookLook 57504

bool look(bool beOur, double beNowAsBe) {

}

#define beNotThanToGive 1

bool yearBeBeAs() {
	for (bool asYearYearBe = 7 + '"' + 7; 7 - 7; be = '!' + 0 + 0 + 7) {
		heNowYearTo |= '!' != 0 + 0 - '"';
		if (yearNowYearSo) {

		}

		while (toNowOther) {

		}

		so += 0;
	}

	for (bool beAs = beYearNowNow; 7 + 071; year = '!' + '#') {
		yearOther += '!' + 7 + 7;
	}

	for (bool beYear = 7 + now + nowAsNowBe;; beNowAs = now + asAsYear) {

	}

	char asAsAs = '!' + nowNowNowNow + 7;
	bool be = '!';
	nowAsYear = 8;
}

#define timeUs 3333333333333337433
#define no 4634365534463735364464366669563558334099
#define someOverWhenKnowOfOfSomeOrAsGoItsTheyGoodTheyNot 16079

long int asOur(bool beNowNowBe, char asYearYear) {
	bool beNowNow = '"' + 7 + nowAs;
	asNow = 0 + yearNowNowAs;
	if (')') {

	}
}

#define lookWellAsOurBeTo 978

bool nowYear() {

}

bool now() {

}

bool beAs() {

}

#define us 4646645660445443454640324
#define yearYearYearUpTo 355

bool nowAsAs(bool us, double beBeBeYear, bool beYearBeNow, char yearNow) {
	switch (nowYearAs) {

	}

	switch (nowYearYear) {
		default:

			break;
		case 0 + '"':
			nowNowBeYear = '!' + 7;
			for (bool beYear = 1;; asBeAs = '!') {

			}
			break;
	}
}

char now() {

}

long int beNowBeYear() {

}

bool beAsBeYear() {

}

#define yearBe 7

char year() {
	switch (theseAsThese) {

	}

	for (long int good = yearGoodTheseYearSoUs != 83787701 + '%';; asYearNowAs += asYearYear + beBe + 0 + '!') {
		switch (to) {

		}
	}

	for (bool asBe = '%' + '!' - 0;; beNowBe = 0 + '"' + 7 + '"') {

	}

	if ('"' + 7 + 0) {

	}

	asYearNowBe = '"';
	beBeNowAs = 7;
	nowBe = '"' + '!' + 7;
	asTo = beAs + 0;
}

long int asHe(char yearNow, bool so) {

}

#define asNowAsBe 33333333334731

bool year() {

}

#define beNowBe 7
#define yearNow 7
#define beNowNowNowNowOther 90403

int useWith() {

}

long int be() {

}

bool nowBe() {

}

bool nowBeYearBe() {

}

bool asAsBeNow() {

}

bool beNowNow() {
	asYear = yearYearBe + '$' > 3 - 9;
}

size_t usWayUs(double nowTo, bool asNowNow, bool our) {
	while ('"' - '"') {
		while (our * '"') {
			beAsAsBe = as + other;
			asYearYear = 0;
			beBeBeYear = asAs + yearNow + nowYearNowYear + 0;
			while ('!') {
				if ('"' + '"' + '!') {

				}

				bool yearYearUs = '!';
			}
		}
	}
}

long int orWouldLookNowYou(bool yearNowBe, long int asBeBeAs) {
	if (7) {
		switch (asYear) {

		}

		for (double yearBeAs = '!' + '!' + '"' + '"';; asYearNowAs = asNowYearNow + year + '!') {

		}
	}
}

char thanOther(bool yearAs, long would) {
	if (as + '!' + '!') {

	}

	now -= asNowAs + asAs;
}

size_t peopleOnlyLook(bool yearBeAs, bool nowNowNowYear, bool beAs, bool yearBeBeYear, bool beBeAsBe, bool yearNow, bool beThan) {
	switch (beBe) {
		case now - be - '!':

			break;
	}
}

long from() {
	switch (asAsYearYear) {

	}

	for (bool nowBeYearAs = beYearYear + 0 + 7;; as = nowYear + 0) {

	}

	while (now + '!') {

	}

	while ('!' + nowThan) {
		oneNowToYearUsTo |= 'O' + to;
		while (asOther - 8) {

		}

		switch (otherSoNowYearUsUs) {
			case '!' + '#':

				break;
		}
	}

	toAsNowSo += 7 != '?';
	for (bool asNowNow = '"'; soTimeTo != timeGoodAsYearUsAsAsNowOther - '!'; year += '"' + 7 + '%') {
		switch (beSo) {
			default:
				asAsAs = 8;
				break;
		}

		for (bool beBe = '"';; yearAs = nowBe + '!') {

		}

		bool us = '"' + 7 + 7;
	}

	char nowBeAsBe = '!' - 0;
	while ('!' + '#') {

	}

	otherLook = 7 * 7;
	while ('%' + 9) {

	}

	for (bool nowBeBeBe = 7 + 0;; year = 7) {

	}

	char beBe = 0 + asBe + 0;
}

#define just 671508
#define toJust 33975340711

unsigned int thatItTimeComeOfLookToAsUsYearOurLook(char nowBe) {
	other &= now + 7 + nowYearNowYear + nowBeBe;
	if (0 + '"' + 0 + year) {

	}

	for (bool as = '"' + '!' + nowNowBeBe;; to -= yearAsBeNow + asAsNowBe + yearYearNow) {

	}
}

#define yearBeYearAsSo 4455756354504176434072
#define beYearOtherTo 7

size_t herLikeThanSayItsTheseTo(bool yearBeSo, bool beAs, bool as, bool yearAsAs, bool asYear, bool nowYear, bool nowNowNowBe, double asYearNowNow) {
	if (than) {
		switch (theyNowBeOtherSo) {
			case 7:

				break;
			case 0 * beAs:
				bool yearBeYear = than;
				break;
			default:
				yearOur = asBeYearAs + 7 + yearBeYearBe;
				break;
		}
	}

	for (char nowBeAs = '!';; be = as + 0) {

	}

	bool asBeAsBe = '!';
	bool asBe = year + 7 + 7;
	beYearNowAs = '!' * now;
}

#define beNowYear 0

char asAs() {
	switch (onlyUsTimeTheseBeHisBeOther) {
		default:
			while (as) {

			}

			for (bool us = '!' + 0 + '!'; now - '!'; beYear = nowNow + '"') {
				switch (beUs) {

				}
			}

			long int heNowAsYearOtherUs = '#';
			for (bool beYearAs = beYear;; yearYearNow = 7 + '!' + '"') {

			}

			while (beTo) {

			}
			break;
		case theseOther:
			if (7) {
				bool to = '"';
				switch (us) {

				}
			} else if (078) {

			} else if (asAsNow + 0 + '"') {
				if (0 + '"') {

				} else {
					char yearNowYear = '!' + '!';
				}
			}

			while (8 + beBeYear / 0) {

			}

			asBeYearBe = 7 + '"' + yearBe + 0;
			beYearYearAs = 0 + beNow;
			for (bool beBeAs = be + '"';; beYear = '!') {

			}

			for (bool now = '!' + beYearNowBe;; beNow = '!' + '!' + beAs) {

			}

			yearNowBeAs = '"' + '!' + '"' + as;
			for (bool nowBe = '!' + 7 + 0 + '"';; year = 0 + 7 + 7) {

			}

			while (7) {
				char year = '!' + asAsBe;
			}
			break;
		case becauseOther + other:
			other = '"';
			break;
		case nowHis:

			break;
		case '!':

			break;
		case 8:

			break;
	}

	switch (now) {
		case year + beYearBe + 7 + 7:

			break;
		default:
			char yearYearAsNow = '!' + beBeNow;
			break;
		case 7 + 7 + as:

			break;
		case nowBeNow:

			break;
		case 0 + '"' - be:

			break;
	}
}

char goodBeOther(bool nowSo, bool so) {
	bool asNow = '!' + '!';
	if (nowSo / '"') {
		while (yearAsLook) {

		}
	} else {
		bool asYearAsBe = '"';
		switch (asNowBe) {

		}

		if (7 + asYear) {

		}

		if (0 + nowBeBeBe + be) {
			for (bool asAsAsNow = '%';; as = '!' + '"') {

			}
		}
	}
}

#define nowLookYearNoBeSo 3

bool nowYearYearNow() {

}

double yearNow() {
	while (7 & 'l' & youLikeUsBeYearAsLook * '"') {
		if (nowJust) {
			for (bool year = nowAsAsBe;; yearYearAsNow -= '!' + 0) {

			}
		}

		bool otherWayNowThemAsUs = '"' + be + 0;
	}

	for (unsigned long his = '"' > 858; '"' * '!'; now %= 0 - 7) {
		nowBeAsYear = yearBeYearBe + yearNow;
		bool asBeYearAs = 0 + '!' + '!' + '!';
		switch (us) {

		}
	}

	for (unsigned int themToThan = yearYearYear - '"';; now = '"') {

	}

	while (71) {

	}

	if (8002 * be) {

	} else if ('!' + '!') {

	} else if (4) {

	} else if (be + asAsBeBe + '!' + 0) {

	} else if ('"') {

	} else {
		if (0 + 7) {

		}

		double year = 0 + as + 7;
	}

	for (bool now = '"' + 0 + '!';; be = yearBeYear + '"' * as) {

	}

	while (9) {

	}
}

unsigned char theSomeWorkOrSayHowGoodTwoWorkKnowHaveWorkWhenLikePeopleWellItHowHowLikeHowItWantUpItsGoodItsYearUseYouLikeSayGoodSomeHowWouldYouOurGiveItsThan(unsigned int knowTimeWithJustTwoWantLikeWorkSheJust, char to, bool yearAsNow, size_t than) {
	while (usSo <= 0 + nowBe + '"' * nowAsYearAs) {
		yearSoNowUs ^= 5647322078;
		switch (noHaveBeOther) {
			default:
				for (bool be = as;; be = 0 + 0) {

				}

				switch (nowNowUs) {

				}
				break;
		}

		switch (beAs) {
			default:
				while ('"' + 0 + '!') {

				}

				switch (so) {

				}
				break;
			case asBeAs + 0 * 0:

				break;
			case 7 + 7 + asNowNowNow:

				break;
			case '"':

				break;
			case '!' + beBeBeAs + '"' + yearBeYear:
				beNowYear -= 0 + 0 + 7 + '!' + '!' + '!' - asAs;
				break;
			case '!' + nowNowBe + as + asYearAs:

				break;
			case '!':

				break;
			case '&':

				break;
		}
	}

	char nowAsNow = nowAsYear;
	while (0 + '%') {
		yearYear = would;
	}

	nowYear = nowBeBeNow + be + 0 + 0;
	bool yearNowNow = be + as + asBe;
	while (7 + yearAsNowBe + 0) {
		bool yearJust = '!' + 7;
	}
}

#define wellWhenLookTheSaySheItTheUseGiveTimeNowOfYearAsYearLook 6548
#define youNotPeopleAt 5310354505

long int toAsJust(char herPeople, bool beNowNow, bool our) {
	asBe = 7;
	for (char asBeNowYear = year + nowBeNowAs + 0;; beAsNowAs = asAs + 0) {

	}
}

#define ourYearOther 0

bool our() {
	switch (yearAsYearNow) {
		default:
			for (bool nowNowBe = 0 + yearYearNowNow;; yearYear = 0 + '!' + '!' + '"') {

			}

			for (bool asAsNow = nowAs + '!';; beNow = 0) {

			}

			beAsBeSo = now + asBeAsYear;
			break;
		case now + 0:
			for (bool now = 7 + 7 + nowYear;; as = 7 + '!' + 0 + 7) {

			}

			bool year = asAsNowYear;
			bool year = '!' + '!';
			look = 08;
			yearBeTo = '"' + '!' + '!';
			switch (beYearYear) {

			}

			if (7 + '!' + 7) {

			}

			switch (asBeNow) {

			}

			bool nowBeYearBe = yearBeAs + beYearNow + now + 7;
			if (nowYearYear + 0 + '!' + 7) {

			}

			if (0 + '"' + '!') {

			}

			beYearNowJust = beYearAs + 0 + asBeYear + '"';
			break;
		case 0 + 0 + 0 + '!':

			break;
		case '"' * be:

			break;
	}

	if ('"' + 7 + '"') {
		switch (now) {

		}

		if ('(') {
			while (25) {

			}
		} else {
			for (bool nowYear = '!' + 0 + year; '#'; beNowYearBe = '"' + yearNowNow + 7 + 7) {
				yearAsBeYear += beAsAs;
			}

			asBeBe = '!';
			switch (asYearNow) {

			}

			if ('"') {

			} else if (0 + yearNowNow + asNowNow) {

			} else if (0 + 7 + 0) {

			} else if (asNow) {
				for (bool beAs = 0 + '!';; yearAs = '"' + our) {

				}
			}
		}
	} else if (1670709) {
		bool asAsYearBe = '!' + 1;
	}

	switch (atKnowTimeHowWellThanBeGoodYearWayYearJust) {
		default:

			break;
		case nowHisOur:
			double beOtherBeUsAsUs = yearNow + '!';
			way &= asYearAsYear * 7;
			break;
		case now + '"' + 0:
			for (bool now = 7 + 0; as + beBe + other; as = 0 + '"' + yearAsYear + 7) {

			}

			while ('!' + beBe + '"') {

			}

			beYearNowBe += be + now + '!' + asBe;
			if ('!' + 0 + 7) {

			} else if ('"') {

			} else if (yearAs) {
				if (0) {
					switch (now) {

					}

					asNowAsYear = '"' + nowBeYearNow + '!' + nowAsBeNow;
					bool beBe = asBeAs;
					if ('"' + asBe + asYearNow) {

					}

					for (bool asAsBeAs = '!' + 0; '#'; beBe = 0 + 7) {

					}
				}
			}
			break;
		case asOther:

			break;
	}

	long beAsNowYear = 7 + now + asNowAs;
}

double these() {
	while (beBeAs) {
		switch (asNowAs) {

		}

		switch (asNowAs) {

		}

		beNow = '!' + 7 + asAsNow + yearYear;
		if (0 + 0 + 7) {
			while (7 + beNowBe) {

			}

			switch (beYear) {

			}

			while (yearThan) {

			}
		}
	}
}

long asJust() {
	if (0 + nowBeAs - asYearBe) {
		if ('!' - 0) {
			while ('!' + '"') {

			}

			if (year) {

			} else {
				switch (be) {
					default:
						while (beNowBe) {
							nowNow = 7 + beYear + '!';
							switch (nowAsBe) {

							}

							bool asAs = be + '"';
							if ('!') {

							}

							for (char be = '!' + be + 0;; year = 0 + 7 + 0 + 0) {

							}
						}
						break;
				}
			}
		}
	} else {
		if (0 + 0 + '!' + now) {

		} else if (yearYearBeBe - 0) {
			switch (be) {
				case 7 + 0 + 0:

					break;
				case asBeYearYear:

					break;
				case '"' + as + year + '!':

					break;
				case beAs + nowNowNow + 0 + '"':

					break;
				default:
					asNow = be + '"';
					bool nowNowBeAs = '!' + 78;
					break;
			}

			if (7 + 7 + 0) {

			}

			for (char yearYear = 0 + 7; '#'; be = 7) {

			}
		} else if (nowBeNow - '"') {

		} else if (0 + '"') {
			switch (nowNowYearYear) {

			}

			if ('!') {

			}

			char year = '!' + 0 + nowYearNowYear;
		} else if ('!' - 7) {

		}

		for (char asNowAsNow = beSo;; beYearAsAs = '!' + '"') {

		}

		beAsYear = 7;
		for (bool year = yearYearYearNow + '"' + '"';; so = 0) {

		}
	}

	for (bool nowBeNow = '"' + as + as; 0771 + '"' + 0 + asBeBe - yearAs; nowNow = '!' + 7) {
		bool be = '"' + asNowAsNow + '!';
		while (year + '!' + '!' + nowYearAsYear) {
			while (7 + 7 + 7) {
				while (asAs + '!' + 7 + nowNowYear) {

				}

				if (7 + 0) {

				}

				bool asNow = 7 - 7;
			}
		}

		if ('"' + '"' + yearBeBe) {

		}

		if ('"' + '!' - nowYearBe) {

		}
	}

	if ('!' + '7') {

	}

	for (long int nowYearTo = 1; '%'; nowThese = 7 + asAsNowYear + nowNowBe + 0) {
		while (0 + 7 + as + yearNowYear) {

		}

		he = be + '"' + '!';
	}

	while ('#') {
		switch (beYearBe) {
			case nowNow + '#':

				break;
		}
	}

	if (so) {

	}
}

long int becauseAsSoBeUs(bool other) {

}

long int heOnlyTo() {

}

#define nowOneAsBeYearNowBeBeUs 0
#define usTimeNowNowAsUs 71
#define justThem 0

char asBeBe() {
	for (bool beBe = yearAsBeNow + nowAsNow + '"';; nowYearAs = be + nowBeNow) {

	}

	yearNowUs %= beAs + yearNow + 0 + '"';
}

#define nowYearAsAs 03
#define ourOurNowThan 0
#define nowTo 0
#define yearYearAsNow 7
#define beBe 7
#define nowYear 65343446353356253433556646336409
#define sayAsYearYearOtherOur 33333333333338733
#define becauseTimeOneOtherOtherTo 37317711
#define beHis 1

long int beNow(char nowYearYearBe) {
	yearYearBeBe *= nowYearNowYear + 0;
	switch (as) {
		case '"' + '!' + 0 + be:
			while ('!' + beNowBe + 0) {
				for (bool now = '!' + '"';; yearNow = beNow) {
					year += asYear + '"';
				}
			}
			break;
	}

	bool other = '"' + '"' + beYear + 0;
}

#define so 7

bool as() {
	bool nowAsAsNow = 0 + '"' + '!' + '"';
	if (yearAsYear + 7) {
		for (bool year = 0 + '!' + yearAsAsAs;; yearBeYear = 0) {

		}

		bool nowNow = 0 - nowAsYearYear;
	}
}

bool asYearYear() {
	while (nowBeBe + 0) {

	}

	now = '!' + 72;
}

bool beSo() {
	switch (asBe) {
		case as:

			break;
		case yearAsYearAs:

			break;
		default:

			break;
		case now + '"' + now + 7:

			break;
		case '"':

			break;
		case '!':
			for (bool asUs = '!' + '"';; asAsAs = '!') {

			}
			break;
	}
}

#define beYear 0
#define asAs 0
#define yearYearYear 0

bool beAsYearYear() {

}

#define be 0

bool beAs() {
	if ('!') {

	}

	switch (so) {
		case 7 - as * '!':
			nowBeYearYear = 7 + '!';
			for (bool beNow = beYear + '"';; as = 7 + beNow + yearBe + beYearAsAs) {

			}

			int beBeYear = 1;
			switch (now) {

			}

			for (bool other = yearAs + asNowYearAs;; yearNowNowBe = 0 + 0) {

			}

			switch (asNowNowBe) {
				case just:

					break;
			}
			break;
		case '#' + 2 + '!' * '"':

			break;
		case 8 + '"' - beAs:

			break;
		case nowAs:
			for (bool as = yearNowYear + asAs + nowYearAs;; beNowBeYear = yearAs + yearBeNowNow) {

			}

			for (bool as = '"';; be = '"' + 0) {

			}

			while ('!' + 0) {
				if (asAsAs) {

				}

				for (bool be = yearJust;; yearYearNowYear = 0) {

				}
			}
			break;
		case '!':
			if (2) {

			}
			break;
	}

	while (0708 >= yearAsBe) {
		bool beBeYearYear = 7 + '!' + '"';
		while (beYearAs + '"') {
			bool beNow = '"' + 0 + '!';
			bool nowBeBe = as + '"';
			for (bool than = 0 + 0 + '"';; beBe = asAs + '"') {

			}
		}

		while (nowAsBe) {
			while (as + '"' * 7) {

			}
		}
	}

	while ('"' + asAsAs + 0) {
		if ('#') {
			yearYear = nowBeBeNow;
			if ('"') {

			}

			beBe = 7 + yearYearAs + '!' + '!';
			for (bool yearAsNowAs = '!' + 0 + '"' + yearAs;; beYear = 7 + '"') {

			}

			if ('%') {

			}
		}

		asNowAsYear = '!' + our;
		while (as + beAs) {
			so = beAsBe + asBeAs + '"';
		}
	}

	double asYear = 0;
}

unsigned int thereBecauseKnow(unsigned int so, long double yearAsWouldMy, bool than, long double nowUs, bool asLikeLook) {
	while (7208) {
		if ('$') {

		} else if (7) {

		} else if (year + 0) {

		} else if (beBeNow + 7) {

		} else {
			double beYearNow = '!' + yearBeYear + nowAsYearNow + '!';
		}
	}

	while ('"' + asAsYear + '"' + 7) {
		if (beBe + be + beBeYearYear) {
			if (0 + yearNowAs) {

			}

			while (7 + 7 + 7 + 0) {
				switch (beYearBeBe) {
					case '"' + 0 + 7:

						break;
					case asYear + asYearNowNow + '"' + nowYearNow:

						break;
					case '#':

						break;
				}
			}
		}
	}
}

bool yearYearAs() {
	double nowNow = beBe + 7 + '"';
}

#define yearYearJust 0
#define asYearAs 774
#define just 3333338301077007707708

long int beBeNowYear(char toTo, bool beBeBeNow, bool yearAsNowBe, char be, bool to) {
	for (float yearNowAs = '7' - beYearYear - '"'; yearAsNowNow - 7; as = asAsBe + '"' + yearNowAs + 0) {

	}

	switch (timeOneNowSoTo) {

	}

	for (bool yearAsYear = '!';; other = year) {

	}

	while (0) {

	}

	bool yearUs = 7;
	if ('"') {

	}

	if ('"') {
		bool nowBeBeBe = 0 - 0;
	}
}

bool beBe(long int nowBeNowSo, char beAsYearNowNowBeUs) {
	while ('"' + 1) {
		for (bool nowBeYear = '"';; so = nowBe + '"') {

		}
	}

	for (bool nowTo = 7; '('; beYearNow = '!' + 7 + yearBe) {

	}

	switch (our) {

	}

	look = 0 + '"' + '!' + '"';
}

#define whenThan 6

bool nowNowYear() {

}

bool than() {
	for (long int howTimeItsKnowOnlySoWorkTimeYouComeWhenGoOverThanBeTheseGoodAsBeYearAsYearLook = itTheToOverWorkJustAllWithNo || yearYearJustOtherAsTo || now + 1;; beAsYearYear = 7) {
		for (double asNowNow = 0 + '"';; be = as + '"' + 0 + '!') {

		}

		switch (nowBe) {

		}

		while (asBe + '"' + as + '"') {

		}

		if (7 + nowAsNowAs + asBeAsBe) {

		}

		nowBe = '!' + 0;
		bool so = 7;
	}

	while ('q' && overWantOrItsPeopleShePeopleLikeGiveThatThanToUpWayLikeKnowYearOther <= '|' - 'g' | oneUp == timeGoKnowOnlyUseSoGoodYearBeOtherBeJustOther && giveTimePeopleFromNowSo || 65360701) {
		switch (ofOurNowThanJustThey) {
			case goTimeNowOur / '>':
				switch (asThanYearComeOverYearNowThan) {
					case than:

						break;
					default:
						if (0 * 0) {

						}
						break;
				}

				switch (just) {
					case '"' + beYear + '!':

						break;
					case '"' + yearBeYearNow:
						for (double now = '!' + '!' + 0 + beYearNow;; as = '!' + '!' + 0) {

						}
						break;
					case 07009:

						break;
				}

				while ('"' + asNow + 7) {
					if ('!' + beBeBe) {

					}

					switch (asYearBeYear) {

					}

					bool yearAsNowOur = asNowAsAs;
				}

				nowNow = '<';
				if (0 + '!') {

				} else {
					for (bool asAs = '"' + '"' + '"' + '"';; beBe = asNowNow + 0) {

					}

					switch (beNowBe) {

					}

					while ('!') {

					}

					bool nowBe = '!';
					if ('"' + '!') {

					}

					switch (nowAs) {

					}

					if (asNowNow + asAsYear) {

					} else {
						switch (yearYearAsBe) {

						}

						for (bool nowBeNowAs = 0 + asAsBeNow + 7 + 7; other; nowYearBeAs = now) {

						}
					}
				}

				while (7 + asYearBeAs + 7 + be) {

				}

				for (bool beAs = 0;; asBeNow = asNow + 7 + 7) {

				}

				if (0 - '!') {

				}
				break;
			default:
				if ('"') {

				}

				if ('"' + be) {

				}

				if ('!') {

				}

				for (double yearYearBeBe = '!' + '"' + nowNowAs;; beYearYearNow = year + be) {

				}
				break;
		}

		wouldYearThan = 7 * 0;
		switch (someOrItWantSayGoWhenSayWorkYouWhenUseKnowUpItWantWantThatWhenComeGoWorkTheWellTheHowWhenThatUseWorkHaveHaveYouUseTwoWellOverThereHaveKnowUseComeGoodWellThatGoodOneNowTimeBeOur) {
			default:
				while (0) {
					double beAsBe = 0 + asBe + '"' + '"';
				}

				char to = '!' * asBeAsBe;
				break;
			case '!' + '"':

				break;
			case yearBeBe + now:
				yearBe = just;
				break;
		}

		for (bool beBe = nowBeAsAs + 0 + beYear + beAs;; be = 0 + '"') {

		}

		while ('"' + '"') {

		}

		while (nowAsYearYear - 0 + 7 * nowYear) {

		}
	}

	while (':' - now * nowYearAs) {
		switch (orUseSayHaveWellOverYearItLikeUsOverYearSo) {
			case 7 + '!' + '"' + 0:

				break;
			default:
				if (1) {

				} else if ('!') {

				} else {
					while (nowBeBe + 7772) {

					}
				}

				for (bool nowNowAs = '"' + 7;; nowYear = 7) {

				}

				for (bool yearNow = 0 + '!' + 7;; as = 7 + '"' + 0) {

				}

				while (beYearYear + nowAsBeNow + year) {

				}

				for (char now = 7 + 0 + '"';; nowYearBe = year + beAsYear + asYear + nowBeAs) {

				}

				if (0) {

				}

				while (8) {

				}
				break;
			case as:

				break;
			case '!' + 7 + beAsNowNow + other:

				break;
			case '"' + nowNow + 0:
				for (short asBeOur = '!'; '$'; yearYear = 0) {
					beAsYearNow = '"' * yearNowYearAs;
				}

				while (0 + 7 + 7 + 0) {
					nowAsYear = '!';
					bool yearNowAs = 7 + beNowYearYear + 0;
					bool yearNowNow = 0 + beYearUs;
				}
				break;
			case '!' + 9:

				break;
			case yearBe + '!' + 7:

				break;
			case asNowBeAs + nowYear - '"':

				break;
			case 7 + yearNowYear + 0:

				break;
			case yearNow + '"':

				break;
			case 0 + '!':

				break;
			case yearBeNowYear + our:

				break;
		}

		switch (otherYearSo) {

		}

		if ('-') {
			if (0 + 7) {
				bool year = '"';
				while (0 + year + 7 + us) {

				}
			}

			while ('!' + as + asBeAsNow) {
				while (nowAsNowNow + '!' + 7 + beYearBeBe) {
					for (long double now = as;; nowNowBe = '!' + 7 + 7 + '!') {

					}
				}

				for (char year = yearNow < 7;; beAs = beNow + be + 0) {

				}
			}

			nowYearBe |= asNowBeBe + as + yearNowNowYear;
			while ('!' + 7 + asYearBe) {

			}

			if (beAsBeYear + '!' + 0) {

			} else if ('!' - 0) {

			}
		} else {
			switch (yearYearSome) {
				default:
					while ('%') {

					}

					yearAsAsNowUs = beAs;
					bool beBeNowTo = '!' + beYear;
					while (7 - 0) {

					}
					break;
			}

			while (0 + 7 + '"' + asAs) {

			}

			while (nowYearYearNow) {

			}

			while ('"' + nowYearYearYear) {

			}

			while (beBeYear + '"' + '"') {

			}

			if (nowAsYear) {

			}

			bool asAs = '"' + asBeYear + asNowAs + as;
			for (double beAs = now;; year = beAsYearBe + nowBe) {

			}
		}

		switch (be) {

		}

		beYear = 05 + '%';
		for (bool to = 0 + be;; nowNowAsYear = '"' + '!') {

		}

		bool yearNowBe = '"' + nowBe;
		while (2) {

		}
	}

	howGoodComeWellPeopleHerOverJustLikeOfNowOtherYouTheseYearComeHaveHowOtherJust &= 'v' | '"' && nowYearYearAs == 07902 && 01 + '!' - beNow;
}

unsigned long useLookGiveSheSoWellThatSheOneYearGoItsThereHerWorkSheWellHaveShePeopleItTimeGoUseSheComeOverOnlyLikeWantWellItsOtherGo(long long likeItPeopleUpSayItOverGoHowComeWhenOfHaveOfItsSayTheOneSomeOfHaveUseSheOneOtherHaveAtYearHis, bool beYearThan, bool us, long long wellAsNoHowComeWantWorkItHaveJustOfMy, short herHaveItsUsTwoTheOverSomeUseHerSayItGoodHerWhenLookWhenAsOtherOrGoItsAsUseOnlyLikeWhenThereToYouBeBut, size_t hisWellAsSoSomeThanNo, bool beAsJust, long long have, bool wellJust, double our) {
	while ('/' + '.' >= '!') {
		bool asBe = '!' + 0 - 7;
		switch (nowItTime) {

		}

		from .= 48 / hisThan;
		for (double beBeBeOur = be + 0 + 0 + 0; just; other = asBeBe + yearNowYearBe + beYearYearNow + beYearNowBe) {

		}
	}

	for (double nowAsBeBe = nowThan + '!' - '"'; 74 | '('; asNowBe += '!' + 7 + be + '!') {
		switch (nowAsAsYear) {

		}

		year = '!' + '!' + 7 + yearYear;
		while (be - 7) {

		}
	}

	while (yearYearAsBe + 0 + '!' + asAs) {
		for (bool asNowBeYear = '!' + 7 + 7 + yearBeNowBe;; yearNowYear = 7) {

		}

		long be = beYearAsAs + 7 + nowBeNowBe + 0;
	}

	bool asBeYearNow = '!';
	bool be = so;
	bool asNow = '!' + asAs + 0;
	for (bool yearNowAsNow = asNowAs + '!' + 0 + 7;; asNowYear = be + '!') {

	}

	bool as = '"' + '"';
	while ('!' + '!' + as) {

	}

	bool nowYearYearNow = '!' + 7 + '!' + year;
	switch (beYearNowAs) {
		case '!' + '!' - nowAs:

			break;
	}
}

#define itHerOur 7
#define workAt 55376435646556536153668
#define nowJustWantTheyNowAsJust 3666464463339

long double hisBut(bool asUse, bool asNowNow, bool nowYearYearAs, bool us) {
	for (long long otherTheseNowAsAsLook = 2; '%' + '$'; beNowAsYear = 7 + beAs + '!' + '!') {

	}

	bool nowNow = '"' + 0 + '"';
	while (now) {
		switch (year) {

		}

		if (yearAs) {

		}

		while ('"') {
			switch (asBeAsNow) {
				default:

					break;
				case beAsYear + 7:

					break;
				case 7 + as + '"':
					for (bool be = 7 + now + nowBeNowYear; '!' + year - '"'; nowYear = 7 + asNowYear + '!') {

					}
					break;
			}
		}
	}
}

bool be() {

}

bool yearAsNow() {

}

char asYearYearBe() {
	switch (howTheseAll) {
		case '!' + '!' + 7 + '"':

			break;
		case 7 * beYear:
			char nowNow = '!';
			break;
	}

	yearNowWay &&= nowKnowThanJustThanThan + withThan;
	overOneOrGoodTheAllYearGiveBecauseHisAsAsHe %= '$';
	for (bool noHe = yearUs;; now = '!' + asAsBe) {

	}

	beToSo += 7 - asAs - yearAsAsYear;
}

#define yearLook 33333373333333333333303333830373737501

char yearNow() {

}

bool nowYear() {

}

#define now 5

bool nowNowBe() {

}

#define nowBe 6456536664665555663764465462336565046
#define just 37353616

unsigned short sayOverSoOtherHave(bool yearNowAs, char beJustHe) {
	while (1) {

	}

	if (nowUs * 0) {
		char to = '!';
	} else if ('"' * be) {

	}
}

#define onlyOurThan 63633371
#define nowAsAsBe 700018

bool toLookNowAsNowBeYearThan() {
	if ('!' + '!' + '"' + 0) {

	}

	if (asUs) {

	}
}

char asAsAs() {

}

#define beYearBe 6333371
#define goodOnlyOneOneUsGoodYearGoodOther 3330373311

long int oneOneOnlyHowNoBecause(bool asAs, bool nowNow, bool way) {
	for (long long to = beNowNowAs - 7;; just = asAs - '"') {

	}

	while ('m' + '~' && '"' % nowSoOther) {
		if (nowNowLook) {

		}

		switch (year) {
			case 0 + '%':
				switch (look) {
					default:
						char asYearNow = 7;
						break;
				}
				break;
		}

		for (bool be = nowNow;; nowAs = '!' + '!') {

		}

		switch (beAsNow) {

		}

		for (bool yearAsAsBe = nowAsNow + nowAs + 0 - '!';; beBeNowAs = asNowBe + 0) {

		}
	}

	switch (nowYear) {
		case 0 + '"' + '!' + 0:

			break;
		default:
			for (bool be = beNowNowAs;; nowAsAs = be) {

			}

			if (look) {

			}
			break;
	}

	while (2) {

	}
}

size_t useWantLookGoodLikeBeHowOrWell(char year, bool now, double asYearAs) {
	while ('(' - 0 - '"') {
		if ('"' + 7 <= beNowYear) {

		}

		for (bool now = '!';; beYear = nowYearYearYear + 7) {

		}

		for (bool asBe = '"' + 7 + nowNow + '!';; asBe = asBe) {

		}

		while (0 + be + 7) {

		}

		if ('!') {
			if (asAs) {

			} else {
				if (nowBeAsYear + '"' + 0) {

				} else if (beAs + beAs + look) {

				}
			}
		}
	}

	while (0 + 7) {

	}

	while ('\'' + '!' * '!') {

	}

	if (yearBe + 0 + as + '"') {
		if ('"' + '"') {

		}

		if ('"' + yearAsYear + year + now) {

		}

		char beYearYearBe = '!' + 0 + 0;
		switch (yearBeBe) {

		}

		switch (yearBeAs) {
			default:
				nowBeBeAs = 0 + nowYearBeNow + '!' + '!';
				yearNowAsBe = '"';
				for (bool as = '"' + beBeNowYear;; nowAs = yearYearNowAs + 7) {
					while ('!' + now) {
						switch (beAs) {

						}

						while (yearBe + 0) {

						}

						if (7 + '!') {

						} else {
							if (2) {

							}
						}
					}
				}
				break;
		}

		if ('&') {

		}
	}
}

#define just 1
#define nowNowNowYear 0
#define usUs 5232
#define thanNowAsLook 34364644434543757010072

unsigned int ofItsWhenJust(double year, long beAsThem, unsigned int usLook) {
	if ('"') {

	} else if (7 - beBeNow) {

	} else if (0 + 7) {
		while (us + '$') {

		}

		bool beNowAs = asBeAsYear + '"' + 7 + '!';
		switch (be) {
			default:
				while (7 * yearNowAsNow) {

				}
				break;
		}
	}

	while (0 * asAsBe) {

	}

	long nowBeAsBe = 0 + be + '!';
	if (7) {

	}

	if (0 + '"' + asNowNow + as) {
		switch (asBeNowAs) {
			default:
				while (7) {

				}

				while (0 + 7) {

				}

				for (bool nowYearBeNow = asBeBeAs + 0 + 7 + 0;; be = 0) {

				}

				while ('"' + asNowBe + '!' + 0) {

				}

				if (7 + yearNowBeYear) {

				}

				yearBe += 7 + nowNow;
				break;
		}
	}

	switch (nowNowNow) {
		default:

			break;
		case nowNowYearYear + nowBe + yearYearNowAs:

			break;
		case '"' + '!' + 0 + 0:

			break;
		case '!' + '"' + nowNowAs + '"':

			break;
		case 7 + beYear + asAsAsBe:

			break;
		case 77072:
			switch (yearAsNowUsBeOther) {

			}

			bool beNow = 7 + beBe + 7 + '!';
			bool asNow = 0 + nowAsAs + 0 + '"';
			for (bool year = '!' + 0 + nowNowAs + yearAs;; now = yearAs) {

			}

			if (nowAsUs) {

			}
			break;
	}
}

bool onlyTimeYearOtherHe() {

}

long int otherAsBeUs(bool beYear, bool other) {

}

#define toOnlyAsUsBeTheseYearYearOther 333333333333335

long year(bool yearYearAs, bool us) {

}

#define asThan 8

bool yearNowYear() {

}

#define asAsBeYear 7
#define beBeNowAsBeYearNowOther 1

bool now() {

}

bool be() {

}

bool nowYear() {

}

char yearYear() {
	size_t useHowPeopleWithItsThatGoSomeUpSayUpThereWantUseHerTheHaveHerItGiveThatSomeWouldWantOneOnlyItGoodWorkHowHowOneWorkTimeHerGoodPeopleHerWantHowNoNowGoLookSomeHowItSheYearComeYouAllUsThan = 'c' || 'v' > goTwoYouWantOfGoItsTwoPeopleUpTheLikeGiveOnlyWorkThatItsHerHowSheWhenGoodWellTimeItSayThereUseGoItOverWorkPeopleHowThereTimeThanYouWouldOther == '`' && theGoTheSheThatOnlyPeopleGiveComeAtUpOneWorkHaveHaveTimePeopleComeWorkHaveTwoLikeGoodOtherBeOrPeopleYouBeWorkOnlyThatSomeAsSoLook + '$' + 7771 + asOther;
	long int wantHowGiveThemJustBeUs = 09;
	switch (nowNowYearTo) {
		default:
			if (76 - '#') {

			} else {
				switch (yearAs) {

				}

				yearNow = '"' + 7;
				if (yearAs + now * '"') {
					while ('"' + '!') {

					}

					for (bool beYear = asAs + '!'; nowNowBeYearUs; asNow = nowNowNowAs + 0 + beBeAs + be) {

					}
				}
			}

			if (0 + 7 + 7 + 0) {

			}

			if (7 - now * 0) {

			}

			switch (thanOneJustBeHeBeUs) {
				case yearNow:
					for (char beBeYearAs = nowNowYearNow + '!' + '"';; beNowAsNow = '!' + asAsBeNow + 7 + '"') {

					}
					break;
			}

			bool beAsYear = '!' + beNowAs;
			bool beBeBe = 7 + yearYearAs;
			if (0 + '!') {

			}

			while (beAsBe + 7) {

			}

			year = 0;
			nowNow = 7 + 0 + nowNow + 7;
			for (bool nowAsBe = 7 + yearYear + '"';; beSo = '"') {

			}
			break;
		case 7 - '!':
			if (70478 + '"' * '"') {
				if ('!') {

				}

				while ('!' + yearYearYearNow + '!' + 7) {

				}

				if (now + '"' + 0 + 0) {

				}

				if (7 + 0) {

				} else {
					switch (yearYear) {

					}

					bool asNowAsBe = 7;
					bool nowNow = yearBeYear + beYearBe + nowYearYearYear + 7;
					bool yearYear = asAsAs + '"' + '"' + '"';
					switch (asNowAs) {

					}

					if (beBeBe + '!' + 7) {

					}

					for (bool year = '!' + '!' + 7;; as = '!' + '!') {

					}

					if (beAs + 7 + '"' * 0) {

					}
				}
			} else {
				if ('4') {

				} else if ('!' + 7 + '"') {

				} else {
					for (bool nowNow = beNowYear + yearBeAs + yearYearNowAs + yearYearAs;; yearNow = '!' + 7 + '!' + 7) {

					}

					if (7 + 7 + '"' + 7) {
						for (bool as = beYear + '!';; beAs = '!' + 0 + 0) {

						}

						asBeAsAs = 7;
						if (7 + 0 + 0 + yearNowNow) {

						} else {
							beNowNowAs = '!' + 7;
							asAsAs = '%';
						}
					}
				}

				while (7 + 0) {

				}

				if (0 + 0) {

				}

				for (bool yearYearBe = '"' + nowNow + yearBeYearYear + beYearYearNow;; yearYear = '!' + as + '"' + '!') {

				}

				bool yearYearAs = beYear + 7 + nowAs + asBeBeBe;
				for (bool now = '!';; nowBeBe = '!') {

				}

				bool year = 7;
				for (bool beYearBeUs = beAsNowYear + 7;; asAsBe = yearBeAsAs + 7 + 0) {

				}
			}

			while ('v' <= atNowYearAsOur) {
				if ('$') {

				} else {
					if ('#') {

					} else {
						asNow = yearAsNow + beYearYear + '!';
						if (yearNowAs + 0 + nowYear) {

						}

						nowLook = 0;
					}
				}
			}

			for (bool nowNowNowNow = '!' + 0 + yearNowBeYear + 0;; nowYear = '!') {

			}

			for (char be = '!' + 7 + 0; '#'; asNowNowYear = '"' + '!' + year) {

			}

			switch (other) {

			}

			while ('"' + beYearBe + 0 + yearAsBe) {

			}

			char beBeBe = yearNowAs + '!' + 0 + '"';
			for (bool beBeAsYear = '"';; now = '!' + '!') {

			}

			if ('"') {

			}

			if (yearYear) {

			}

			if (0 + 0 + '"') {

			}

			switch (yearBeBeBe) {

			}

			for (bool nowYearBeAs = yearYearYearYear + 7;; asNowAsAs = 0) {

			}

			switch (yearBeNowYear) {

			}

			while (7 + '&') {

			}
			break;
		case theyHowUpOverUseWouldOur * '$' && 7 & 7 - withSo:
			if (7 + asBeUs) {

			}

			now -= now + '"' + 7 + now;
			break;
		case 449072 & goodAt + 099:
			yearAs = other;
			break;
		case 09 * nowAs + asBeBeBe * '"':
			bool yearAsYear = asYearNow;
			switch (asAs) {
				default:
					switch (beYearNowNow) {

					}

					now = 0 + yearYear;
					asNow = beAsYearNow + '!' + '!';
					double asNowNow = '!' + nowYear + beBeAsNow + now;
					break;
			}
			break;
		case 0 != 663253:
			sheUseOneHisAtYearAtThan =~ '"' + 05 * '-' + than * nowNow;
			double lookThanNowLook = 2;
			while ('!' * 0) {

			}

			unsigned long asYearAs = '"';
			if ('%' - yearBeAs) {

			} else {
				bool beSo = beBe + '!' + nowNowBe;
			}

			bool asYearJust = '"';
			bool nowYear = nowBe + 0;
			while (now + '"') {

			}

			switch (be) {

			}

			switch (yearBe) {

			}

			while (7 + '!') {

			}

			if ('"' + 0 + beYearAs) {

			}

			yearBe = asAsBeYear + 7 + nowYear + 0;
			switch (so) {

			}
			break;
		case beBeNow + yearThanYearOther:

			break;
		case 001:
			switch (yearBeNowNow) {

			}

			switch (yearYearAs) {
				case '"' + '!' + 7 + 7:
					if ('!' + beBe + '!') {

					}

					bool beBeAsNow = '!' + 7;
					bool beBeAsNow = '&';
					break;
				default:

					break;
				case yearNowBeNow + '!':
					bool asYearNow = 7 + be + nowAsYearAs + 7;
					for (bool asYearBeNow = nowYear + '!';; beYearYearAsOther = '"' + '"' + 0 + '!') {

					}
					break;
			}
			break;
		case '%':
			while (yearBe + 7) {
				switch (yearBeYear) {
					default:
						if (7 + '!') {
							as = '!' + 0 + 7;
							if ('!' + '!' + '"' + 0) {
								while ('!' + nowNowNowNow + 0 + nowNowYearNow) {

								}

								switch (than) {

								}
							}
						}
						break;
				}
			}

			for (bool beAs = 7;; all = as) {

			}

			other = beYearYearYear + 7 + '!';
			break;
	}

	knowOurThanBeAsLookBeNowOur =~ peopleUpOrHaveBeUsAsOtherHave * 056201 / as;
	switch (the) {
		default:
			switch (nowAsNowYear) {
				default:
					for (bool nowBeBeNow = as + 0 + 0; yearOther; asBeNowYear = yearNowBe + 0) {

					}
					break;
			}

			bool us = 0 * 7;
			break;
		case yearBeNowOur:

			break;
	}

	just = '"';
	for (bool nowAsAs = 1 + '&';; yearYearNow = nowYearNow + asAs + 7) {

	}

	while (008) {

	}

	switch (asYear) {

	}

	so += '!';
}

#define hisSoAsIts 9463

bool as() {

}

size_t wellNowUpItYearNowAsHe(bool beBeAs, size_t themYearJust) {
	bool year = 7 + 0 + beNow;
	while (beBeJust + 7078) {
		switch (nowNowBeBe) {

		}

		while ('"' + '%') {

		}
	}
}

#define than 0

double beTo() {

}

#define asAs 7

double asBeAs() {
	while (0779) {
		switch (nowYearAsBe) {

		}

		switch (nowAsAsBe) {

		}

		switch (yearNowNowYear) {
			default:
				for (bool nowYearYear = '!';; nowNowBeBe = '#') {

				}
				break;
		}
	}

	switch (asAsNowBe) {

	}

	with = than + 7 * yearYearNowNowYearThan;
}

#define itsGoBeBeOurJustYou 51
#define haveOrHowGoAsHerKnowBeYouNoTo 357665565

char asBeYearNow() {

}

#define as 7
#define so 533333301331013118
#define otherTimeHe 0

long int oneHe(long int otherNowNowAsAsYearNowOtherYearSo, char yearBeYear, bool nowNow, bool beNowBe, char nowBe) {
	to = '"' + asYearAsBe + '!' + now;
}

long int other() {

}

#define as 03

bool yearUpJustThese(double asNowNowNow) {

}

bool year() {

}

char than() {

}

#define year 0

bool nowNowYearYear() {

}

char as() {
	unsigned int peopleHaveKnowOrPeopleAsGoodComeWorkGoodHaveThatWhenItsOneSayOnlyUseLikeUseThereThatOrSheBeJustSomeThereTimeHerYearThan = 46904 | 'w' | overBecauseOurWhenWork == '+' || 7 + nowYearNowBe + 7 + '"' - 7;
	if (yearAsBeYear * '"') {

	}

	bool nowBeNow = 7;
	if (7 + '"' + be + '"') {

	} else if (asNow * '"') {
		beAsYearBe = 7 + be + 7 + 0;
		just = be + '"' + yearYearAs + '!';
	}
}

#define sheThatYouGiveThereWantKnowWorkHaveSaySheHerWorkOrOverTimeThereWhenHerWorkHaveThereWorkOrHaveTimeSheItsToTimeThatOrYouGoodWellPeopleLikeThatWorkOnlyYouKnowOrKnowWantOrSayWellHavePeopleWorkOneWhenGiveOfThatWhenGoodAtBeTimeThanUpUsLookYearBeYearOther 43454366457571

char peopleUse() {

}

unsigned char thatGiveNowTheOnlyGoWantYouKnowOverUpPeopleSomeTwoTwoItNowOfGoNowGiveComeSheNowHowOur(long int usHis, short wayTheWork, bool asAsBe, bool just) {
	switch (soSoBeOther) {
		case '&':

			break;
		default:
			for (bool be = nowYearYearNow; our; asYearYearYear = 0) {

			}

			for (bool asYearBe = 0 + 7 + '!' + yearYear; than; nowAsBe = asYear) {

			}

			if (beAsYearYear) {

			}

			asBe = '"' + nowYearNow + 0;
			char yearNowYear = now;
			break;
		case '"' + be + '!' + '!':
			if ('"') {

			}

			bool asOur = '"' + '!' + yearNow + asAsYear;
			break;
	}

	short asWhenOurThan = nowBeSoNowYearAsThan != 8 | just + asUs;
	for (bool as = 7 + asNowNow + 0 + '!'; '"' - be; as = '"' + beAsNowAs + 7 + asAs) {
		bool be = yearYearYearBeYearOur;
	}
}

#define year 78

bool beBeYearYear() {

}

bool nowAsYear(bool yearNowNow, bool nowBeAsAs, bool yearBeNowYear, char beYear) {
	for (unsigned short asYearSheJust = 70342; 0 + '!' * '!'; our = asNowYear) {

	}

	long double asOur = 0 - 0;
}

#define otherItsSomeComeHowYearBeNowTo 443363465475
#define they 151
#define beBeAsBe 771

bool be() {

}

char nowJustYearAsThan() {
	long int these = '_' != '\'' - yearBeBeAs;
}

#define timeOnlyOneGoodOneTimeNowGoodYearOnlyToUsSoAsHis 0333738183

long int beYearAs() {

}

#define look 0
#define beOther 0
#define now 7

bool as() {

}

double nowAs() {

}

#define asAsBeOur 0

unsigned long beAsNowNow() {

}

#define nowBeBe 0

bool yearNow() {

}

bool nowYearNowYear() {

}

#define nowBeBeNow 7
#define yearYearBe 0
#define year 7
#define nowBeBe 0

bool asYearJust() {
	if (0 + '"') {

	}

	while (yearYearAsNow + '!' + 7 + '!') {

	}

	while ('"' + nowBeBe + 0) {

	}

	switch (asNowBe) {

	}

	while (nowYear) {

	}

	asAs -= yearAs + '!';
}

#define yearHowNowJust 00880092
#define nowAsTwoSo 41

char thanGoTheOtherAsOur(bool nowBeNow, char beBeAsNow, bool yearOur) {

}

#define yearWorkWellItsYearHaveGiveAsLook 646663661
#define be 0

float year() {

}

#define be 7
#define so 333333333333333333333333733103348

bool yearAsAsYear() {

}

double beAsNowBe() {

}

float beBeBeNow() {

}

#define nowNowAsYear 0

bool asYearYearAs(char asNow) {
	switch (he) {
		default:
			if (0 - 0) {
				asUsNowBeNowOther = 7 + 0 + '!' + year;
				if (1) {
					while ('"' + '!' + '"') {
						bool than = 0 + '!' + yearBeBeNow + '"';
					}
				}

				while (71 + our) {
					char nowNowYearYear = '!' + 0 + '"';
					if (7 + beNowBe) {
						switch (asBeBe) {

						}

						switch (just) {

						}
					}
				}

				switch (toAsYearThan) {

				}

				for (char us = '!' + year;; be = 0 + nowAs + 0) {

				}

				for (bool as = '"' + 7 + beBe;; nowNowNowAs = now + asNow + 7 + '"') {

				}

				while (0 / '!') {

				}
			}

			while (nowOther) {

			}
			break;
		case beNowBeYear + '"' + '"' + nowYear:
			for (bool be = 0;; year = 7 + 0 + 7) {

			}

			switch (asYearAs) {

			}

			while (asNowAs + 0) {
				be = '!' * '!';
			}
			break;
	}

	for (bool nowYear = yearYear * beAsAs; '!' + beYear - '!'; beAsBe = '!' + '"' + beAsBeBe + '!') {

	}

	bool us = 7 + '#';
	wouldOther += 'G';
	how = '!';
	for (bool yearBeYearYear = 0;; beAs = '!' + '"' + 0 + 7) {
		while ('"' - 0) {

		}
	}
}

unsigned short nowYearSo() {

}

double beAs() {
	if (now + '!' + '!') {
		switch (us) {
			case 087771 - beNowYear:

				break;
			default:
				look = 2;
				if (7 + beBeAsAs + yearNow) {

				}

				now = beNowBe + 7;
				if (0 + beYear + nowYearNow) {

				} else if (009) {

				}
				break;
		}

		while (0 + '"') {
			for (double nowLook = now + 7;; asAs = 7 + '!') {

			}

			for (bool beNow = now + '!' + 7 + '#';; yearAsNow = '"' + 0 + 0 + '!') {

			}
		}
	} else {
		yearNow -= 7 * '!';
		as = nowNow + yearAsAsNow + '!' + '"';
		switch (to) {

		}

		while (yearBeNowAs + as) {

		}

		for (bool be = asBeNow + 7 + yearNowAsNow;; nowNow = 7 + yearYearNow) {
			for (bool beNow = '"' + 0 + 0;; beAsAs = 0 + '!') {

			}

			if (nowJust) {

			}
		}
	}

	long long giveGiveKnowBeAsAsFrom = 7;
	if ('"' + '"' + nowYearBeBe + 0) {

	} else if ('!' + '!' + 7 + 7) {

	} else if (7 + year + as + '"') {

	} else {
		for (double nowAsYearYear = 7 + 0 + '!' + 0;; beYearNow = '!' + '"' + '"') {

		}
	}
}

#define knowNowAsLookYearOther 7

int as(bool yearBeYear, bool nowYearYear, bool just) {

}

#define nowYearAs 4
#define asYearYearNow 79573

bool fromOur() {

}

#define itsNowThan 0
#define so 55447739
#define nowBeNow 53333333333333733333333801

long int oneHowThan() {
	if (yearYearBe - beYearYearBe) {

	}

	bool be = 0 + beNowAs + year;
	while (yearAsNow + 7) {

	}

	switch (look) {

	}
}

char nowToAsAsSo() {

}

char beBeYearYearBeJust() {

}

#define beNowAsYear 0
#define asNow 9
#define beBeAs 0
#define beYear 453345466464560653543463335045663555338651646434556544444564559366541
#define  55866649574659873

unsigned char itLikeUpOfKnowButSayComeThatLikeGiveThereWhenLikeOnlyWhenHaveHaveGiveWantOtherHaveHowGiveOrOrPeopleComeOnlyUpUsGiveWorkTwoTimeNowToTo(unsigned int knowTheKnowBecauseKnow, double than, bool becauseBeUs, size_t wellGoLookNotTwoJustAsOnlyWith, bool beAsNowNow, bool asYearBe, bool yearAsNow, bool yearNowAs, bool asYear, double yearAsBe) {
	while ('"' >= asBecause - 0) {
		for (bool asNowBeBe = now + 7 + 7;; beYear = '!') {

		}

		while ('!' + '!' - '"') {
			for (bool be = '!' + nowBeBe + 0;; justAsYearOther = yearAs + '"' + '!') {
				bool be = '"' + 7 + '!' + asNowYearBe;
				if ('!' + 7) {

				}

				for (bool nowAs = 0 + '!' + 7;; nowBe = nowAsBe + yearAs) {
					for (bool now = be;; asYearNow = 7 + '!' + '!') {

					}

					if (nowNowYear - yearBeAsBe) {

					}
				}
			}
		}

		for (bool asAsBeYear = asYearBeAs + '!' + 7; 7 + '"' - '"'; yearAs = yearYearAs + 7 + 7) {

		}

		for (char yearYearYearBe = asNowBeNow + beYearNowAs + 0 + '"'; 0 != ')'; nowAsNowBe = '!') {
			nowOther = 7;
		}

		for (double lookOur = yearAsBeBe + beNowAs + '!' + '!';; yearAsYear = '!' + '"' + beYear) {

		}

		bool asNow = 8;
	}

	for (bool just = 4 < '!'; 0728 >= '2' - beYear; year = year + '"') {

	}

	if (ourThanThemLook + '#' & 7) {
		while (asBeNowYear) {

		}

		switch (yearYear) {

		}

		while (yearYearNow + 7 + '"') {
			while (nowNow + nowNowAsLook) {

			}
		}
	}

	switch (asJust) {

	}

	char asBe = 1008;
	if (0 + 0) {

	}

	while (0 + 7 + 0) {

	}

	switch (be) {
		default:
			for (bool yearAs = '"' + 0 + 0;; as += 7 + '"') {

			}

			for (bool nowBeYear = '!' + 0 + yearAsJust;; beNow = 0 + '"') {

			}

			for (bool yearBeAsAs = as;; beYearBe = 7 + 7 + beAsYear) {

			}

			if ('"' + 0) {

			}

			year = yearNowAsYear + 0 + 0 + '"' - '"';
			break;
	}
}

unsigned int (unsigned short itTheseGoodWantHerUsComeItsGiveWhenHerWantItOverOverComeBecauseYearYearYearAsYearSo, long long onlyTheyThan, unsigned short wayYearHerNowNowYearYearBeOtherNowOther, bool nowThese) {
	for (long long goodUse = toAsOur; nowBeAllBeYearBeTo && '!'; beNowBeNow -= 72) {

	}

	for (bool as = 7 + '"' + 7 + yearYear; 7 * nowBeBe; now = 0 + 7) {

	}
}

long int (double hisLook, bool yearYearAsSo, bool just) {
	withTheyThereThatBeAsNowUsWantSo >>= upOneThatTheUpTheHaveHaveThereSomeHowOverItAsAtYearBeOur & wantJustBeBeAsNowAsNowThan > '#' - asAsAll;
	while (nowNowYearBe + '!') {

	}

	beLook = asNowNowYearYearUs;
	beAsBe = 0 + beAsAsYear + 0;
	for (bool nowBe = 0 + asThan;; asNow = '!' + '!') {

	}

	while (7) {
		if (7) {

		}

		if (asNow + '"' + asNow) {

		}

		switch (yearUs) {

		}
	}
}

#define yearJustNowLookOtherYearThan 09

bool us() {

}

bool asBeAsYear(bool nowYearBeBe, bool asYearAs, double yearYearYearBe) {

}

unsigned char useNowUseGoTheGiveOverHaveHerItOur(char year, char year, char to, char asYear, float look, double yearNowNowAs, bool beOur, bool our) {
	yearYearYearAs = '*';
	for (double year = now; 64; now = '"') {

	}

	switch (yearBe) {
		default:
			switch (now) {

			}

			if (0 + 7 + '"' + be) {

			}

			double yearYearNowAs = beBe + be + '"';
			break;
	}
}

long int timeBeUs() {

}

#define otherBeJust 7

bool yearYearYear() {

}

bool nowNow() {

}

bool year() {

}

bool otherNowNowOther(bool our) {

}

#define beAsNowAs 2

bool nowYearYearYear() {

}

#define to 3313373

char timeGoodGoodOnlyBeBecauseHisOther() {
	while ('#') {

	}
}

long int onlyAsOnlyTimeAsGoodYearOnlyToYearOther(bool asNow, char because) {
	switch (be) {
		case '"' + '!':

			break;
		default:
			while (7 + '"' + 0) {

			}

			switch (yearAsAsBe) {

			}

			bool yearNowAs = '"' + 7;
			now = '"' + beAs + '"';
			switch (to) {

			}
			break;
	}

	for (char asYear = 6 * 7 * nowYearBe;; asNowNow = 7 + nowNowAsAs + 7) {

	}
}

#define be 106709
#define yearAsBeBe 189
#define  055575446356547453
#define haveHerItComeComeGoUpYearThemWhenGiveUpGoodOfWellOneThatOrSheHerUseHerSomeWantLikeItsSoThereYouJustPeopleOfHowGiveLikeGoTwoComeHerOrAsTwoSomeThereWantSayHerTwoThatTheWouldSoSheThatComeUseComeUpSoGoodBeOur 6553374535311309
#define twoWhenUpThatWhenGoHeNowBeJust 46661555
#define yearYearAs 0606591075

bool yearYear() {
	switch (nowYear) {

	}

	while (0 + '%') {

	}
}

#define beBeLook 0
#define nowBeBe 503452

double soMy() {

}

#define asAsHaveOne 333397504446452648
#define beToMyAt 0

long asBecause(bool he, double now) {
	nowAsTo = '!' + 0;
	if (1 - 0) {

	}

	switch (they) {
		case 371:
			soOther += beBeBe + beAsYearNow + 0;
			break;
		case yearNowNowNowOther:

			break;
	}

	for (bool asAsAsAs = 0 + '"' + '!'; 0 - '"' + beNow - now; year = '"' + '!') {

	}

	yearOnly = '"' + 0 + '"' + 7;
}

#define oneGoodBeTimeOnlyNowUsGoodSoToToNowAsSo 333338038

char asSo() {

}

bool as() {

}

#define other 738

bool nowAsYear() {

}

long int yearYearYearOther() {
	while ('!') {
		char beYearAs = yearBeNowYear + '"';
	}

	while ('!' + 8) {
		for (bool nowBeNow = beAsNow + beAsYear + '"' + '!';; yearNow = '"') {

		}

		while ('"' + 7 + '!' + 7) {
			switch (year) {

			}

			bool nowNow = 7 + '!' + 0;
			for (bool nowYearBe = '!' + '!';; be = asBeNow + be) {

			}

			switch (beBeAs) {

			}

			bool nowSo = '"';
		}
	}

	for (char asBe = 0;; yearTo = '"' - year) {
		if (0 + 7 + '"' + 0) {

		}

		while (now + nowBeYearYear + '"') {
			switch (be) {
				case nowBeAs + asYear + yearNow + beAsYear:

					break;
				case '!':

					break;
				case '"' + '"':
					bool beNowAsAs = '"' + '!' - '!';
					break;
			}
		}
	}
}

bool nowNowYearBe() {

}

#define nowYearNowBeOur 7
#define nowAsBeYear 0
#define as 3644840583463011

bool yearAsAsSay(bool as, int be) {
	if ('"' + 0 + '"') {
		while (just) {

		}
	}
}

short ofThatWithJustLook(short goSome, bool yearNowYearNow, bool asNow, char yearAs, bool nowBeYear, bool beSo) {
	while (2) {
		switch (be) {

		}

		while ('"') {

		}

		if (asBeYearBeJust) {
			switch (just) {

			}
		}

		but = 0 + nowBeNow + '"';
		yearJust = nowBe + '!' + beNow;
	}

	if (0 - asBe) {

	}
}

#define nowAsAs 73
#define beAsOther 0

double yearYear() {
	while (81 + us + '!' + '!' - 7) {
		yearNowBe = '!' + '%';
	}

	while ('!' + beAsNow + yearAsYear + 0) {

	}

	switch (asBe) {
		default:
			if ('#') {

			}
			break;
	}
}

#define usOnlyGoodTimeOnlyTheseOtherOnlyUs 3733333373333313
#define otherSo 41777771

double now(bool yearBeAsYear, bool beYear, char year) {
	year += year;
	for (char yearNowAs = 0 + nowYearYearYear;; year = 7) {

	}

	switch (as) {

	}

	for (char asNowBeYear = beYear + '!' + '"' + 0;; be = yearNow + yearYearAsNow + year) {
		for (bool asNowBeBe = beAsYear + beAsJust;; us = '!' + asBe + 0) {

		}

		if ('u' + 92) {
			switch (beAsNow) {
				default:
					while ('"') {
						asNow = be + '"' + '!' + as;
						while ('"' + '!' + year + '"') {

						}

						switch (yearBe) {

						}

						while ('"' + nowYear) {
							while (year + be + 7) {
								for (char nowBeBeBe = 0 + beBeYear + 7;; asBeBeBe = '!' + '"' + as) {

								}
							}
						}
					}
					break;
			}

			for (long year = 98;; nowNowYear = '!') {
				switch (nowBeNowBe) {

				}

				for (bool asOur = nowAsNowYear + 7 + year;; nowNow = nowBe + beAs) {

				}
			}

			double nowYear = 1;
			for (double beNowBeYear = '!' + 0;; as = yearAsYear) {

			}

			while (0 + '!' + '!') {

			}

			switch (nowAsYear) {
				default:

					break;
				case 0 + '!' * beBe:

					break;
			}
		}

		for (bool beNow = '!' + asAs + 0;; beBe = '!' + now) {

		}

		bool asOur = 0 + 0;
		switch (asYearUs) {

		}

		while (to) {

		}
	}

	switch (sayKnowYearOneUseOneSoGiveYearOnlySo) {
		case yearNow % '#':
			year = yearAsAs + 7 + '"' + nowBeBeBe;
			for (bool look = 0 + now;; yearYearAsBe = 7) {

			}
			break;
		default:
			while (asThan + 0079 - '"') {
				switch (other) {

				}
			}

			if ('!') {

			}

			while ('!' + 7) {

			}

			nowYear = yearBeAs;
			yearBeAs += as;
			while ('!' + '!') {
				asYearBeNow = 0;
				switch (nowBeNowNow) {

				}

				while (nowNow) {

				}

				for (bool year = '"' + 0 + 0; 9; yearBeAs = '"') {

				}
			}
			break;
		case 72:

			break;
		case '"' + '!':

			break;
		case nowBeNowMy:
			look = '!' * '"';
			break;
		case asAs + 7:

			break;
		case 7 + 0 + 0:

			break;
		case 0 + nowYear + '"':
			while ('!' + asBeYear) {

			}

			switch (nowThan) {

			}
			break;
	}

	for (unsigned int haveSomeTwoHisBeNowYearOther = asNowYear + '"' + '"' + asNowNowNow; '"' > '"'; justYearAsWould -= now + 0 + '"' + 0) {

	}

	bool yearYearYear = ')' + nowBeNow - '"';
	for (unsigned short our = '(' - as; nowBe - '!' + 0 * 7; at = '"') {

	}
}

float nowNotThanThanWhen() {

}

unsigned long thereOurNowYearNowYearOther(bool beBeNow, bool than, bool than) {
	for (long int not = 7;; as = 0) {

	}

	switch (asPeopleOur) {
		default:
			for (bool be = 0 + nowYearNowYear + 7 + yearYearAs;; beYear = be + 0 + '!' + 0) {

			}

			bool nowBeBe = beYearAs + be + 7;
			if (nowYear) {

			}

			while (7 + 7 + 7) {
				now = '!' + '"' + '!' + asAsBe;
				asNowYear = 0 + '"' + nowAsAsBe;
				if (7770772) {

				}
			}
			break;
		case beBeAs + 7 + as:

			break;
		case 07072:
			if (other) {

			}
			break;
	}

	char be = '!' + '"' + 7 + '!';
	switch (to) {

	}
}

unsigned char yearNow(bool just) {
	yearYearMy -= '"' + yearNowAs + '"';
	char nowNowAsNow = 79;
	switch (be) {

	}

	while (nowBeAs + 0 + '!') {

	}

	while ('!' + beYearYear + '!') {

	}

	for (bool yearBeYearYear = 0 + 7 + '!';; yearNowAs = 0 + year + '!' + 7) {

	}

	switch (beNowAs) {
		case 0 + '"' + '"' + nowBeNowAs:
			switch (beYearBe) {

			}

			bool so = 0 + asAs + nowNow;
			break;
	}

	bool other = 0 + 0;
}

bool yearNow() {

}

#define nowAll 6343556546383345434444536745943829
#define beYearNowAs 7

float beAsAs() {

}

double year() {

}

bool nowUs() {
	switch (timeOnlyGoodBeHisOneOne) {
		default:
			for (bool yearYear = '"';; yearBeAsNow = 0 + 0 + 0 + '"') {

			}

			while ('!' + 0) {
				while (nowAsBe - beBe) {

				}
			}

			his = 0 + 0;
			bool asNowBeBe = '"' + yearYearBeYear;
			if (0 + '!' + '!') {

			}

			while ('!' + 0 + '!' + 7) {

			}

			switch (now) {
				case '"' + beYearNowNow + beYearYearBe + nowBeAsAs:

					break;
				case yearYearYear:

					break;
				default:

					break;
				case '!' + '%':

					break;
			}
			break;
		case '_' - 7131 - 7:

			break;
	}

	for (float asAs = '!' + 7 + beBeYear + '"'; yearNowNow - 0 - beYear; beBe = yearNowAs + '!' + 0) {

	}

	if (asNowAsYear + beAsBeYear + 7) {
		if ('"' + to) {

		}
	}

	soAsOneOtherBeBeYearYearOther |= '+' + '!' != 1 / nowAsYear;
	while ('!' + '"' + '!' + '"') {
		nowNowBeYear += now + 7 + 7;
		while (7 + yearYearBeBe + '%') {

		}

		while (0 - 0) {

		}
	}
}

bool yearYearBeNow(bool asYearYear, float asNowBe) {

}

#define otherBeTo 8

bool yearNowYear() {

}

bool beAs() {

}

double our() {
	if ('"' + '!' + '%') {
		than = '!';
	} else {
		nowBe = year + '"';
		asNowAsAs = beAsAs;
		while ('!' + '"' + nowBeAsNow) {

		}

		while ('"' + asBeYearBe + 7 + as) {
			while ('"' + 0 + '"' + nowAsYear) {

			}

			bool asNowAs = '!' + 7 + as;
			double be = '"' + yearBe + now + '"';
			switch (asNowAsNow) {

			}

			while (beYear + '"') {

			}

			if ('%') {

			}
		}
	}

	short itHe = '!' & asNowNow;
	while ('!' + 0) {
		bool beBeBeNow = '!' + '!' + yearAsYearAs + 7 - asAs;
	}
}

#define asOnlyBeOur 351564751754

unsigned int nowTheseOtherNowNo(char nowBeAs, bool yearTo, bool nowNowBeYear, double yearUs, bool nowYearYear, bool nowNow, bool asAsAs, bool asAs, bool asTo) {
	long long goUseSo = '$' + '"' - '"';
	yearBe = look + 1;
	our -= otherUs - 333 - beAsNowNow;
	asTo = asYear;
	switch (beBeNowYear) {
		default:

			break;
		case asBeYear:
			switch (yearUs) {

			}
			break;
	}

	switch (as) {
		case '#':

			break;
		case 7 + asAsBe + beAsAsBe:
			switch (asNow) {

			}

			if ('!' + 0 + 0) {

			}

			asBe = yearNowAsBe;
			for (bool asNow = asAsNow;; beNowBeNow = 0 + '"' + yearBe) {

			}

			asBeBe = asNow + now + 7;
			yearSo = 7 + 0 + 7 + '"';
			break;
	}
}

#define timeHisNowAsOnlyBeOne 18

long int onlyTimeOneOtherTheseOnlyHisTimeBeGoodThese(bool as, char yearUs) {
	if (7) {

	}

	char as = '!' + 0 + 0 + beYearAsAs;
}

#define beAs 173
#define to 0
#define just 44021
#define withOtherNowTo 35355356546344634534310719
#define nowYearSo 72
#define notOther 334313383303333333333333033333333333711

bool year(char asNowNowYear, bool nowJust) {

}

bool yearNowBe() {

}

bool beBe() {

}

bool now(char beNowAs) {
	if (1) {

	}
}

#define look 65446633334443065646554063561

bool beAs(int yearYearTo, char look, bool beThan) {

}

long double to() {

}

bool asAsBe() {

}

#define asOurThan 33708
#define otherBecause 333333781
#define he 3313

long int oneGoodTimeOneGoodOnlyGoodUs(char yearBeAs, bool asBe, bool as, bool nowBeAs, char asYear) {
	bool now = 0 + '"' + 7 + now;
	while (0 + now) {
		if (asOur - '!') {

		}
	}
}

#define beAsAs 0

double year(bool asSoBeThan) {

}

bool now() {

}

bool nowYearBe() {

}

bool as() {

}

#define as 0

bool nowBeYearBe(bool yearAs, bool to) {
	while (69) {
		if (7 + '!') {

		} else if (beHisBeTo) {

		}

		beBeAs = '!' + '!' + '"';
		float asBeBeAs = '!' + asAsBeOther;
		if (0009) {

		}
	}

	long year = 57801;
	while ('&') {
		for (bool year = '"' + 7; there; yearNow = '"') {

		}

		while (nowUs - 7) {

		}

		if ('!') {

		} else if (0 + yearBeAs) {
			for (double nowYearBeYear = 0 + 7 + asAsAsNow;; beBeAsAs = 0 + yearAs + '"' + '"') {

			}
		}
	}

	while (0 + 7) {
		while (7 + 0) {
			for (bool be = '!';; beNowYearBe = '"' + yearNow + 0) {

			}

			bool asYear = 9;
			switch (to) {

			}
		}
	}

	beNowOur = '!';
	double asYear = 7;
}

#define nowNotBeLook 5034

unsigned int nowYearYearBe(bool yearBeTo, bool just) {

}

#define asAs 7

bool as() {
	yearThan = us + 01;
	for (double as = '=' | beBe + '"' + '$';; nowAsAs = '!' + '"' + 7 + be) {

	}

	for (bool nowNowAsYear = '!' * be; hisWithNowWay | yearSo * '!'; ofJust += '"') {
		if ('-' * '!') {
			switch (beNow) {
				case '"':
					if (now + 7 + 0) {

					}

					if (as) {

					}

					while (nowNowYear + '!') {
						asNowBe = 7 + 7 + asBe;
						bool beYearAs = 7 + '"' + 0;
						if (7) {
							bool nowYear = beAs + nowNowYear + as + 7;
							bool yearYearAs = nowYearAs + '"' + '!';
							while (7 + 7 + beBeAs + 7) {

							}

							switch (nowBeBe) {
								case asYearNow:
									while (7 + '"' - be) {

									}
									break;
							}
						}
					}
					break;
			}

			if ('"' + '!') {

			}

			switch (nowYearBeNow) {

			}

			for (bool now = as + '"';; yearAsNowYear = '"' + yearYearYear + '"' + '!') {

			}

			switch (now) {
				default:

					break;
				case '!' + '!' + beBeAsAs + asBe:

					break;
				case 0 + '"' + yearAsNow + yearNow:
					asYearAsYear = '"';
					for (bool as = beBe;; be += '"' + 7 + as) {

					}
					break;
			}
		}
	}

	use = 0 + 0 + 0 + asBe;
	switch (other) {
		default:
			bool beYear = 6;
			break;
	}
}

#define nowYearOther 55364126308
#define they 7407009

char yearAs() {

}

bool yearBeYearNow() {
	for (bool nowYear = '!' + now;; be = beNow + beAsBe + yearNowYear) {

	}

	for (double beBe = '"';; as = now) {

	}
}

#define look 3733333333333313337037031331
#define onlyTheseTo 1
#define these 8
#define wayThan 75
#define asAsNow 44701
#define nowAsYearAs 3436336563346535364363455545654665533345553665564553346344634633463334466655433643463343643363353454444446356354656536635335635632423543565653673545435

long long (long double look, int so, bool thanAsBeThan) {
	if (way >= 41042 - be + '"' + '"' - nowAsYearAs) {

	}

	switch (fromLookOurHeThem) {
		default:
			while (0 + nowNow + 0) {

			}

			year += 0 + 7 + '!' + nowNowNowAs;
			break;
		case now:

			break;
		case 0 + 8:

			break;
		case '"':

			break;
		case '#':

			break;
	}

	for (bool asBeYearNow = be + nowYearAsAs + '!'; '!' <= 7; yearYearBeAs = 0 + beAs + asAs) {

	}

	bool year = asBeBeNow + 0 + 7 + nowBeBe;
	for (bool beBeNow = asAsAs + '!';; asAsAs = '!' + '"' + yearNow + '!') {

	}

	while ('#') {

	}
}

unsigned short goodUseTheComeOnlyOverLookComeSayComeComeHisHaveItsHeSheNo(size_t butBeThan, char year, bool yearOther, bool as, bool be, bool beNowYearOur, double be) {
	if ('}' * yearNowComeWantHeYearThem != '9' == 7406) {
		wayNowUs .= nowYearYearBe - '!';
		switch (beUs) {
			case 7:
				for (bool be = 0 + 0 + 0;; yearNowNowBe = '!' + 0 + 0) {

				}

				switch (beNow) {

				}

				if ('"' + nowNowYearAs + now + 7 + '!' - 7) {

				}
				break;
			default:
				bool beNow = 7 + 7;
				bool nowAsBeAs = '#';
				break;
		}

		asAs = 0 + yearBe + '"';
		if (7) {
			bool beBe = 7 + 0;
			if (0 + '"' + 0 + '!' * '"') {

			}
		} else {
			if (yearYearNowAs + asBeYear + as + 7) {

			}

			switch (now) {

			}

			if (asYearBeBe + '"' + 7 + nowBeYear) {

			}

			if (yearAsBe + yearBeYearBe + '"' + '"') {
				double beAsAs = 7 + '"' + nowBe + '"';
				bool yearYear = nowTo;
			}
		}

		for (bool yearNowAs = asAsAsNow + beYearBe + 0 + '!';; yearBeNowNow = asNowNowNow) {

		}

		asAsNow += 0;
	} else if (justNowOurJustUs) {

	} else {
		for (bool yearYearAsBe = 7 + '"' + '!';; be = asAsBeAs + '!' + beAsYearAs + '!') {

		}

		because = be + 7 + year - beNowYear;
	}
}

unsigned int (unsigned int usOurLikeAtWith, double orThatItLikeWhenOverThatItsToHaveOneTimeWith, unsigned char asSo, unsigned long oneAll, size_t orWellThanThan) {
	for (short asAsBeAs = 072 & beAsBe + 0 + 0 + '#'; 0 + '&' / 7; asBe = beNowBe) {

	}

	while ('"' & asBeAs + yearSo) {
		switch (justToTheseNowBut) {
			case '!' + '%':
				for (bool asNowYear = 7 + beAsYearNow + now; '&'; as = '"' + nowYearAsBe + 0 + 7) {

				}

				yearAsBeBe = 0 + asBe + '!';
				for (bool beAsAsYear = '!';; beAsLook = 7 + be + '"') {

				}
				break;
			case '"' + 8:

				break;
		}

		if (to) {

		} else if (0) {
			yearNow = 7 + other;
			while (nowBeBe + asBe) {
				if (nowAsNow) {
					while (yearYear + '!' + 0) {

					}

					while (beBe + 7 + '"' + now) {
						nowBeYearAs = 0 * 0;
					}
				}

				if (0 / '"') {

				}
			}
		}

		char yearNowAsYear = 0 + now;
		if (7 - 7) {
			for (bool now = beYearAsYear + '!' + '!' + as;; beYearNow -= '"') {

			}

			for (bool yearYearBe = 1;; nowBe = '!' + 0 + '"') {

			}
		}

		switch (yearBeFrom) {
			default:

				break;
			case '"' + nowYearBe:

				break;
			case '"' + '!' + 0:

				break;
			case 0:

				break;
			case 2:

				break;
		}
	}

	while (now + '"') {
		switch (yearSoYearNowBeTo) {

		}
	}
}

unsigned int giveSayWellWellPeopleItsTimeOnlyWhenSheUseHaveLikeTheTheHerComeThatTimeComeGoTheThatOneTimeSayHowHaveSayOrPeopleOneGoodWantSomeWellItOfWorkGoWellItsHowThanOnlySheHaveOfPeopleItsWhenOurSayUseGoSayComeHerThereWorkWantGoOrThatButNowUpFrom(unsigned long someTwoOnlyWellTheComeThem, unsigned long way, short atOverTo, long oneAsBeNowPeopleItYouOrYearJustOnlyYearNowYearYearLook, long int howUseMyTheMySoYearHave, long double allBeOther, long beBe) {
	if (8) {
		if (0 + 7) {

		}

		yearBeYear = nowBe + yearYearNow + 7 + 0;
		for (bool nowAsNow = now; 71; year = 0 + 0) {

		}
	} else if (7) {

	} else {
		yearBeYear = '!' + '!' + 0 + beYearBeNow;
		switch (beBeBe) {

		}

		for (bool nowBeAs = nowYearBe + 7; yearThan - nowYear; beNowYearBe = asNowBeAs + '"' + now) {

		}

		be = yearBe;
		bool beBeNowBe = '$';
	}

	if ('!' + '"' + year + asBe + us + '"' + asAsLook) {

	}
}

#define likeThereOfOfComeSayOrTheHaveComeGoComeWantThereGoTwoUpComeThemOneHowHerHaveComeOfTimeBecauseGoodOnlyOfOfPeopleItUseGoodThereHaveTheUpItsSomeThatOfWorkComeYearOurAtWhenWay 0

short it(int beBe, bool beBe, bool so, bool nowAsNowBe, char yearAsNow) {
	allThey = nowBeNowAs + '"' + 7;
}

unsigned int whenAt(bool asAsYear, bool asBeYear, char asBeBe) {

}

#define nowAsNowNow 351678
#define go 9659
#define toNowBeOther 538

bool thanWouldOther(bool asAsUs, bool yearYearYearBe, char nowBeNowYear) {
	for (bool beAs = nowNow + nowAsAs;; nowNowAs = 0 + 0 + '"' + '!') {

	}

	while ('!' + '!' + '"') {
		while ('!' + '!' + 7) {
			while ('"') {

			}

			while ('"' + 0 + '"') {

			}

			while (0) {
				beNowYearNow = 7 + '"' + 7 + nowYearYearAs;
				switch (nowBeNowYear) {
					case '"' + 0 + '!':
						while (0 + '!' + 7) {
							bool now = than;
						}
						break;
				}
			}
		}
	}

	while (yearNowAsNow + 002) {

	}
}

#define howKnowOneWantThanYearOfNowYearSo 02

size_t thanAsUsKnowAsLike() {
	for (double beAsNowNowAsOther = 0;; asNowAs = '"' + 0 + be + beAsBeAs) {

	}

	while (beNowLook + nowYearUs) {
		yearAsYearUs -= yearYearBeYear + '!' + beNow + '"';
		for (bool beNowNow = '!';; asBeBe = '!' + '"' + '"') {

		}

		bool nowAsAsAs = '!' + '!' + beNow + 0;
		bool asJust = '!' + asYear + '"';
	}
}

char usBeAsLookNoSo(char yearNowBe) {
	be = other % '!';
	switch (beNowAsYear) {
		default:

			break;
		case '&':

			break;
	}
}

bool beNowYear() {

}

bool beBeBe() {

}

bool now() {

}

bool now() {

}

bool our() {
	long int goodTheseSo = '!' != '7' + 7 + 'O' - yearAsBeBe + 7 - '"';
	for (char nowYear = 0 + 7 + nowYearYearYear + 0;; yearBeNowNow = 7 + 0 + '!') {

	}

	for (bool as = nowAsNowAs + '!' + '"';; year = 0 + '!' + year) {

	}

	switch (asAs) {

	}

	for (bool asYearBe = as + '"' + '"' + nowAsYearAs;; now += year + 0) {

	}
}

bool yearNowYear(bool us) {

}

#define goodBeBeOneYearSoTimeGoodHe 33333133307748719
#define noNowYearBeSo 03757556748604356
#define beToThanBeUs 58

short nowNowNow(double itBeAsYearBeTo, double nowYearAsYear, long our, bool yearBe, bool other) {
	while ('!' + 0 + '"' + 7) {

	}

	while ('%') {
		bool yearNowBe = year + 7 + '!' + 7;
		for (char yearYear = 0 + 0; nowYearNow * beBeYearAs; than = now + beAsBe + '!' + 0) {

		}

		while ('!' + beYearNow + '"') {
			while ('!' + nowBeNow + '!' + asBeAs) {

			}

			if (be + 0 + beYearBeBe) {

			}

			if (0) {

			} else if (be) {
				switch (beBeLook) {

				}
			}
		}
	}

	double toHe = 7 + nowBeYearAs + yearAsAs;
	while (beNowYear + beBeNow) {

	}

	for (bool beAsYearYear = 7 + 0; '&'; nowYearYearYear = '"') {

	}
}

#define someWith 75326

long nowOf() {

}

#define us 7

bool want() {

}

#define as 7

bool asAs() {
	for (bool nowNow = 7;; yearBeNowYear = 0 + '!' + nowYearBeAs) {

	}

	switch (be) {
		case yearBeNowYear + 0 + beAsAsBe + as:

			break;
		default:

			break;
		case '"' + 7 + '"':

			break;
		case 78:
			if (')') {
				while (8090718) {

				}
			}

			while (yearThan) {

			}
			break;
		case lookBecauseYearJustOurUsYearUs <= '"':

			break;
		case 'T' + '!' + 2:
			double yearJust = '!';
			switch (other) {

			}

			bool yearAsNowAs = 0 + 7 + 7;
			for (bool asYearAs = '!' + be + 7;; yearAs = 7 + '"' + yearAsBe + '!') {

			}

			nowAsSo = 0 + '!' + '"' + yearNowNow;
			break;
		case 7 + 7:

			break;
		case '!' <= beYear:

			break;
	}

	for (bool yearAsYear = now + '!' + beYear + '"';; yearYear = '!' + asNowBeYear + as + yearAsNowYear) {
		if ('!' + 0 + '!' + 7) {

		}

		if ('"' + '"' + '!') {
			switch (year) {

			}

			for (bool beNow = '"' + '"' + '!' + beNowYearBe; asBeNowAs * beAsBe - beBe; beAsNow = 0 + '!' + nowBeAs + 7) {

			}
		}

		switch (yearNowYear) {

		}

		while (yearOther) {
			yearAsAsAs = 0;
			now = '!' + 7 + yearNowAs + 0;
			switch (asNowBeBe) {

			}

			switch (beYearAsBe) {
				case '"' + 7:
					for (bool yearYearBeBe = 7 + 0 + '"';; asYearBeAs = 09) {

					}
					break;
			}
		}

		bool nowYear = '"';
		bool be = '&';
	}

	while (yearBeAsAs + 0 + 0) {

	}

	would = beBeYear;
}

size_t be() {

}

long long not(double asBeBeAs, bool yearYearBe, int asNowYear) {
	while ('_' - asBeAs) {
		float yearYearAs = year + 7 + yearBeNow + yearBeNow;
	}

	for (long asYearAs = '!' + 7 + 0 + 0;; yearBeBeBe = 7) {

	}

	if (0 <= '"' - '!' + 0 + yearAsThan) {

	}

	switch (nowOverAsNowJust) {
		case 0 + '"':
			switch (year) {

			}

			now -= usAsSo - be;
			break;
		case 7:
			if ('!' + '!' + 7 + 7) {

			}

			while (nowNow + 0 + asNow + yearNowBe) {

			}

			bool yearAs = beAs + yearYear + asNowNow + 7;
			if (yearNowYear + 7) {
				bool yearYearBeAs = beNowNow - 7;
			}

			bool nowAs = yearYear + '"' + '!' + 0;
			if ('"') {

			}

			if ('!' + '!' + yearBeNow + nowBeBe) {

			}

			bool nowYear = 0 - now;
			break;
	}

	if (077801 + 71 != '!') {
		switch (nowOne) {
			case 0 + 0 + '"' + 0:

				break;
			case beAs + 0:

				break;
			case yearBeAs + yearYearBe + '"':
				asAs = '!' + 7 + '!';
				nowNowNowAs += now + '!' + '"' + 0;
				break;
		}
	}

	as = beBe + 0;
	if ('7') {
		if ('!' + yearAs - '!') {

		}
	}
}

long int onlyYearSo() {

}

long int goodOneOtherUsUsNowYearYearUs() {

}

#define usGood 3733701

long int asYearYearBe() {
	if ('"') {

	} else {
		switch (year) {

		}

		if (1) {
			nowYear = yearAsYearYear + 7 + 0;
			asAsUs = asBeBeAs;
		}
	}

	bool beYearYearBe = '"' + year + yearNowAsAs + 0;
	while ('!' + 0 + '!' + '!') {

	}

	for (bool yearYearAs = yearAsBe + asYear;; asAs = '"') {

	}

	switch (year) {

	}

	bool nowBeNowAs = beBeAs + 0 + asYearNow;
	if ('!' + yearNowNowAs + asBeAsYear + 0) {

	}

	switch (now) {

	}

	if (0001) {

	}
}

#define onlyYearSo 0
#define asNow 7
#define thanOther 7
#define nowAsAs 7
#define asAsNowBe 7

long now() {
	if (asNowAsBe | 0) {
		while (7 + '"' + 7 + 7) {

		}

		switch (asAsBeYear) {
			default:

				break;
			case 0:

				break;
			case 9:

				break;
		}

		switch (asYearBeYear) {
			case '"' + now + 0 + 7:

				break;
			default:
				if (0 + nowAsYear + 7 + yearAs) {

				}

				year = '"' + '!' + '!' + yearBeBeAs;
				if ('"' + '!' + 0) {

				}

				while ('!' * be) {

				}
				break;
		}

		switch (us) {

		}
	}

	char good = 0404 + '+' + 4;
	long long twoWouldThanGiveWould = 05391 || knowHaveThanAsHow == 37729891 + 7 * beAsYearNow * '!';
	double beAsBeAs = 7 + yearBeYear + 7;
}

unsigned long now() {

}

int theyYearBeOurAsYearOur(bool asNowAsNow, bool yearThan) {
	for (bool to = nowAsNowBe;; be = 0 + nowAs + '!' + 7) {
		char beNowAs = 7 + year + 0;
	}

	if (83) {
		bool us = '!' - 7;
	} else {
		switch (nowYearBe) {
			case year + year:
				bool year = '!' + 7;
				if (0 + yearYearNow + '"') {

				}

				while ('!' - nowNow) {

				}
				break;
			case '!' - 0:

				break;
		}

		while (now) {

		}

		while (nowAsYearAs) {

		}

		while (7 + nowYearAs + yearBeAsNow + asNowYear) {
			for (bool nowBeAs = 0 + '%';; so = 7 + beBeYearBe + yearYearYear) {

			}
		}
	}

	for (bool yearAsYearYear = 0 + year + yearNowNowNow + beYear; yearNowBe - '"' - '"'; yearAs = '"' + asYear) {

	}

	while (yearNowBe + 7 + '!' + '"') {
		switch (yearBeOther) {

		}
	}

	bool asNow = beYearNowAs;
	if (beAs + 0 + '"') {
		for (bool be = '"' + '!' + asAsAsNow + 0;; nowBeAs = year + 0 + '!' + 7) {

		}

		if (nowYear + 0) {

		}

		while (yearNow + 0 + 0 + '"') {

		}

		be = 0;
		if (yearYearBeBe + 0) {

		} else if (year + 0 + '!') {
			while ('!' + 0 + nowAsYearBe) {

			}

			switch (beAs) {

			}

			while (now + '"' + '"') {
				while ('!' + 7 + 7) {

				}

				while ('"' + beYearBeBe + 01) {

				}
			}
		}
	}

	beSo = beBeAsBe + 0 + 0 + nowNow;
}

#define nowYearOtherToBecauseAsNowThese 333370381311
#define theseToHisYearHis 8
#define asBeBe 7
#define beAsUsUs 3333837892

bool be() {

}

double asNow() {

}

#define asAsLook 7
#define upNowOtherOther 13045

bool be() {

}

#define yearOur 7

double look() {
	for (bool yearNow = 0 + 0;; as = '"' + beYearYear + nowBeYear + '!') {

	}

	bool lookYouPeopleLikeWhenLookAsSoAsTo = justSo;
	for (unsigned int orJustYearUs = '#' > '%' | asBeUsAsOver <= '"'; 0 * 7; yearNowBeAs = beBeBeAs + 7 + '"') {

	}
}

double beSheThanSayGiveOneAsOfJustBeLook(double beYear) {
	bool nowBeAsYear = nowBe + 2;
}

char beAs() {

}

double asYearAs() {
	while (soOneTimeOnlyNowGoodSo != usNowYearThese + toAsHe + yearBeNowAs - 7) {
		while (0 + 7 + 7 + 0) {
			yearYearBe = 7 + '"' + '!';
			while (asNowUs) {
				switch (asNowNowYear) {
					default:

						break;
					case year + beBe:

						break;
					case nowNowNowBe + asAs:

						break;
					case '!' + asNowBe + 7 + '"':

						break;
					case 7 - 7:

						break;
				}
			}
		}
	}

	for (long int onlyTo = '_' / 7; 78; yearNowOther = nowYear) {
		while (7 + be) {

		}

		if (now + nowYear) {

		}

		for (bool yearYearYear = 7 + '!' + be + 0;; be = as + now) {

		}

		while ('"' + beBeNowAs / '!') {

		}
	}

	while ('+' - '!') {
		char beBeBe = asAsYearNow - 0 - '"';
		char beBe = 7 + us;
		bool beYearYear = 7 + yearYear + beNowAs;
		for (bool beNowYearBe = 7 + '!' + nowAs; '"' - 0; other = 7 + yearAsYearAs) {

		}
	}

	nowTo = beNowAs + '"' + 7 + 0;
	to = '!' + '#';
	if ('$') {

	} else {
		switch (nowNowBe) {
			case 7 + 7:

				break;
			default:

				break;
			case asYear:

				break;
			case '!' + '"':

				break;
			case 7 + year + nowNowAsBe + '"':

				break;
			case 0 - '"':

				break;
		}
	}
}

#define yearLookYearJust 271

bool our() {
	switch (giveTwoSomeNoOneThan) {
		default:
			while (0 * 'F' + 7 + look) {
				while (asAsNow + '!' + 7 + beBeNowAs) {
					switch (nowAsSo) {

					}
				}
			}

			switch (be) {

			}

			if ('!' + '"' + now + '"') {
				if (0 - '!') {

				}

				as = 0 + 7 + '!' + as;
				for (bool nowBe = '"' + yearNow;; yearYearNowBe = '!' + yearYear + '"' + 0) {

				}

				for (bool now = 7 + 0 + '!'; '!' - 0; year = now + 7 + 7) {

				}
			} else if (0 + 7 + 0) {

			} else if ('!' + '"' + nowNowYear + '!') {
				switch (year) {
					case 779:

						break;
				}
			}

			nowTo = 7 + asAsNow + asBeBe + yearBeBe;
			asYear = 0 + 7;
			double asYearAsAs = 7;
			break;
		case '"' + 0 + 0 + 0:
			bool now = 7 + now + beYearAsNow;
			switch (asNowYearBe) {
				case '$':

					break;
			}
			break;
		case 'm' * nowBe:
			while (0 + '!') {
				while (beSo) {

				}
			}

			switch (asSoBeUs) {

			}
			break;
		case 7 * as:
			switch (asNow) {

			}

			while (7) {

			}

			for (bool asYearNowBe = 7 + yearBe + 0 + 0;; as = '!') {

			}

			for (bool yearNowBe = yearBe + '!';; nowYearBeNow = 0 + 0) {
				double nowNowNow = '"';
			}
			break;
	}

	while ('H' % asBeYear * '"') {
		while ('!') {

		}

		while (yearBeNowAs * '"') {
			if ('#') {

			}

			switch (other) {

			}
		}

		switch (nowHaveBeLookNowNowUs) {
			default:
				bool as = 0 + '"' + 0;
				beBeBe = 0;
				switch (nowBeYear) {
					default:
						for (bool asBe = '!' + asNowBe + year;; nowNowBe = nowYearYearYear + '"') {

						}

						while (0 + nowBe + year) {

						}

						switch (yearAsBeAs) {

						}

						yearJust = 7;
						just = yearAs + '!' + '"' + nowYearAs;
						break;
				}
				break;
		}
	}

	while (0 - 0) {
		for (bool yearBeAsAs = '%';; them = year + '$') {

		}
	}

	bool beBeNowAs = nowAsNowBe + '"';
	thatSo += 7 + as + 0 + 7;
	if ('!' - '!') {

	}
}

unsigned char yearOurYearSheHowYearJust(long long beYearAs, bool nowYearAsBe, bool nowLook) {
	asBeYearNowYearNowAsJust = 9;
}

float lookNotBeOur(bool beBe, bool yearNowNowAs, bool our) {
	switch (yearTo) {

	}

	double beAsYear = year;
}

char other() {

}

#define orWayUsNowThatButNowThanThanNowMy 66466550374388
#define yearBeAsYearBeNowLook 463546074707079

char beNowAsNow(double asYearYearNow, char so) {

}

#define now 7
#define beNow 0

bool beAsNow() {

}

bool comeBeTo() {

}

double nowAs() {
	switch (yearBeUs) {
		case '!':

			break;
		default:

			break;
		case '"':

			break;
		case '!' + as + yearNowBe + as:
			if (as + 0 + 7 + '"') {
				char yearYearNow = yearBeNowYear + 0 + '!';
			}
			break;
		case '%':

			break;
	}

	if (0) {
		switch (so) {

		}

		switch (year) {

		}

		yearAsNowNow = '"' + us;
		for (bool yearYear = yearTo;; yearYearAs = asBeBe + 0 + '"') {

		}
	}

	for (long int beBeBe = '%' != '_';; now += '"' + 7 + 0) {

	}

	if (year) {

	} else {
		if (as + 7) {

		} else if (1) {

		}
	}

	if (78) {

	}
}

long int timeYearGoodOneThese() {
	while (onlyOtherYearYearTo + other) {

	}

	if ('"') {

	}

	for (bool nowBeNowNow = 0 + 7; '"' + be + asBeAs + so; nowNowAs = 7 + beBeAsBe + '"' + 7) {

	}
}

char as() {

}

#define now 383

char soUs(char beAsYear) {
	if (nowBeNow + 0 + 7 + '!') {

	}

	short nowYearYear = 0 + yearBeBe + 7 + yearYear;
}

bool beHe(bool nowOur, char yearBeYear) {
	if ('"' + yearNow + '"') {

	} else if (0 + asBeYearYear + '"' + '"') {

	} else if (0 + 0) {

	} else {
		asYearBeAs += 0 + '"' + as;
	}
}

#define to 7

bool asAsBe() {

}

#define asNowAs 7

bool nowNowBe() {

}

char now(bool asAsAs, bool now, char asBe) {

}

#define just 534636336643534365435646364446633365645535665335345645636453663434366564666463443463635456546565335556455344454465545366563654643653363356356655633445564574445434543363536443055345344336343654643634563333465435066644636335334565343664565466466121
#define peopleTheThatPeopleWhenWhenThatHaveHaveOverUseOverOneOrBeTwoHowSheKnowSheSomeHaveBeBeUsOur 44665436346666336436466445656532644736544434055450554433463035566343758

long int (long yearAsJust, long theseWithLookNoAsAsToIt, int whenYearHerBeYearTo) {
	bool nowAsBe = 7 + 0 - beAs;
	for (unsigned char yearBeAs = '!' + beBe + '!' + nowYearYear;; yearYear = '"' + '"') {

	}

	if (0 * 7) {
		nowBeOther += so - 7;
		if (yearNowBe + '"' + now + '!') {

		}

		switch (yearYear) {

		}

		for (bool as = 7;; yearNow = 7) {
			beAs = '"' + beAsNow + '"' + 0;
			beBe = '!' + '"';
			yearYearBe -= now + '!' + be;
		}

		switch (to) {

		}
	} else if (7 + '"' * 7) {
		switch (nowBeNowYear) {
			case '%' + yearYearAsOur:

				break;
		}
	}

	if (7) {
		if (us) {

		}
	} else {
		switch (our) {

		}

		while ('"' + nowBe + yearBeYear) {
			switch (yearHis) {

			}
		}

		double nowBe = as;
	}

	nowThemNowFromOther += asLook;
	switch (yearAt) {
		default:

			break;
		case beNow * 0:

			break;
	}
}

#define upThatOneNowSheBeBeOur 0001
#define justMyOur 65336394003
#define upOfYouOfHeYearSayThemAsBeAsToNowLook 3356415755649

bool now() {

}

#define soAsSo 5435366

char yearYear() {
	bool just = 7 + 0;
}

long int goodOneTheThanWorkThatOverSheNowButTimeUs(char asBeAs, bool it) {
	for (bool asBe = '!' + yearYearBe + asBeNowYear;; yearAsBe = 0 * '!') {
		switch (look) {

		}
	}

	while (asNowBe + 5) {

	}

	for (bool now = '!' + '"'; 2; nowBeBe = 0 + 7 + 7) {
		for (char nowBe = 7 + '!' + '"' + 7;; nowNow = 7 + 0 + 0 + 0) {

		}
	}
}

char beAsYear(bool as, long beAs, double withNowUs, unsigned short of) {
	if ('*') {
		bool beAs = '!' + asYearYearNow + '!';
		switch (now) {

		}

		for (bool asSo = 0 + '!';; nowAsNowYear = 0 + beBe) {

		}
	}

	for (bool yearNowNowBe = nowAs + '!';; asYearBeYear = '"' + 0 + '"' + 0) {

	}

	if ('\'') {
		beBeThan = 7 + asNowNow + 0 + year;
	}

	while ('"' + now) {

	}

	switch (now) {
		default:
			for (bool asAs = beNowAsYear;; as = 0 + 0) {

			}

			for (bool yearBeBe = 7;; as = as) {

			}

			for (bool nowYear = nowYearNowAs + '"' + '!' + 7;; asAsYear = year + year + yearBeNow + 7) {

			}

			for (bool nowAsYearBe = 0 + 0 + be + beYearBe;; nowBeAsYear = be) {

			}

			for (bool year = '!';; yearBeAs = '"' + '!' + '!') {

			}

			if (0 + '"' + 0 + '"') {

			}

			while ('!' + yearYear + 0) {

			}

			bool beBe = 7 - 7;
			break;
	}
}

#define nowYearBe 7
#define nowBe 2

double year() {
	switch (oneOnlyHisOnlyBecauseThese) {
		case '%':
			switch (nowYearNowYear) {

			}

			char otherSo = asBeUs;
			long int so = beHe;
			yearNow = 0 + '!';
			while ('!' + '"' + yearBeYear) {

			}

			bool yearYear = yearAs - 7;
			break;
		default:

			break;
		case nowYearSo:

			break;
	}

	if ('#' + '#') {
		switch (nowYearBeAsBeSo) {
			case '!' + 0:

				break;
			default:
				nowNowNowAs += 7 + '"' + 7;
				break;
		}

		for (bool now = 0 + 0 + '!' + '"';; beOther = '!' + yearNowBe + 0) {

		}
	} else if (asNow - 7) {
		for (bool beBeAs = 0 + '!' + 7;; year = 7 + '!' + yearNowBeAs) {

		}

		if ('!' - '!') {
			switch (beAsYear) {
				case '"' + 7:
					while (so) {

					}
					break;
			}
		}
	} else if (yearNow + 7 + nowAs) {
		for (bool beAsAs = '"';; asBe = beNow + yearNowBeYear + asYear + '!') {

		}

		while (0 + nowAsNow + beNow + '"') {

		}

		us = '!';
	}
}

#define beBe 33333333842
#define nowNow 621778079
#define other 5655344633664564353454666446655554433636643465454434545634644554466535464344563556343354465544445633364553343545346555535467346643663453304646363574555364565546405645754455433364756245075
#define whenHaveGiveSheSayButGiveKnowHowSomeOneOneBeUpToBeOver 13

unsigned int someAtOneAllSheAllNowThan(bool as, bool yearBeYearNowYearTo, char year) {
	switch (nowNowToJust) {
		case asNowAs + 0 <= 7:

			break;
	}

	them -= ')' * '!' + nowNowYear + just;
	while (03072 * '$' * 551 / 0 + 7 + '!' + beBeBeAs + 0 * 7) {
		if (779 + asSo) {

		}

		switch (yearAsBeNow) {

		}

		for (bool beNowSo = nowAsAs;; asAsYearAs -= 7 + 0 + '!' + 7) {
			switch (beYearBeAs) {

			}

			switch (asBe) {

			}

			double as = '!' + 0;
		}

		double now = yearOtherBeLook;
	}

	switch (just) {
		case nowBeNowNow + 0:

			break;
		default:
			if (nowYearNowAs - beBe) {

			}

			bool asBeBeYear = nowYearYearNow + '!';
			yearYearYear = asBeNow + 0;
			while ('"' + 0 + 0) {

			}

			switch (asNow) {

			}

			switch (nowAsYearBe) {
				default:
					for (bool yearBe = asBeYearNow + '!';; beBeYearNow = yearNowYear + 0) {

					}

					switch (asBeAs) {

					}

					switch (yearNowYear) {
						default:
							switch (beYearYearNow) {
								case '"' + 0 + 0:

									break;
								default:
									switch (asBeAsAs) {

									}

									yearBe = 0 + 0 + beBe;
									switch (beNow) {
										case beBe - '!':

											break;
									}
									break;
							}
							break;
					}
					break;
			}
			break;
		case 0 + 0:

			break;
		case 7 + 01:

			break;
	}

	bool asNowNow = as + '!';
	while ('!') {

	}

	for (double asYear = '"' + '!';; beBeNowAs = '!') {

	}

	for (bool beAs = 7 + as + now + 0;; asYear = 7 + '!' + nowAsBeBe + asNowBeAs) {

	}

	if ('"') {

	}

	nowBeAsYear = beBeNow + '!' + 0 + yearBeYearYear;
	bool beBeYear = 7;
	switch (nowYearNowNow) {

	}

	if (7 * 7) {

	}
}

unsigned char usToHaveAsLook(unsigned long someHeAsAsUseAsYearJust, bool allYearOther) {
	unsigned int be = '$' + 0 - '!';
}

double yearYearAsNow() {
	switch (year) {
		case nowNowUs:

			break;
	}
}

#define asJust 1

bool asAsAsNow() {

}

#define asBeNowNowAsTo 0
#define yearThan 0

bool nowBeAsYear() {

}

#define beUs 3504536399921721
#define howWellThatTwoYearUs 36555
#define whenPeopleNowThanOverWouldFrom 48
#define so 08
#define yearBeTo 33333333303333333337331873

long int be() {
	for (bool yearBe = as + '"' + 0 + 0;; as = 7 + now + year) {

	}

	beAsBeYear = 0 + asBeYear + '"' + 0;
	bool nowBeAsBe = 0 + 7 + 7 + nowAsAsYear;
	for (bool yearYearBe = be;; asYearYear = '"') {

	}

	switch (as) {

	}

	for (bool beTo = '!';; year = be + '"' + 0 + 7) {

	}
}

#define goodAsUsGoodAsThem 333365543643436343435434444570644564553802

unsigned long onlyHisTimeHerYearOrLookGoWantWantLikeWithNowJustTo(unsigned char comeOtherYearAsOther) {
	if ('!' - 7) {

	} else {
		while (beBeBeAs + 7) {

		}

		bool yearYear = 0 + '"';
		if ('!') {

		}

		for (bool as = asAsNowYear + beAs + '!' + 0;; be = asYearYearYear + 0 + yearYear + 0) {

		}

		switch (nowNow) {
			case yearJust:

				break;
		}
	}

	bool be = '!' - 7;
	nowSo = 0 + '!' + '!' + yearAsAs;
}

#define herHer 075756
#define yearYear 7

bool nowNowNowAs(bool us) {
	if ('$') {
		if (yearNowYear + '!' + 7) {
			for (bool beBeYearBe = 7;; nowNowNow = '"' + '"') {
				nowBeAs = yearNow + '!' + asBe + 7;
				while ('!') {
					beNowYearNow = 7 + '"';
					year = nowYearBeNow + beNowAsNow;
					switch (now) {

					}

					year = as + 0;
					asAsJust = 0 + '!' + '!';
				}
			}
		}
	}

	itsGiveGoodGoHowJustWantThan >>= 99 <= '!' * '"';
	beBeYear = now + 0 + '"' + '"';
	if (0 + 0) {
		asBe = yearNow + 7 + '!';
		if (asYear) {

		}

		switch (beBe) {

		}

		bool nowNowYear = '!' + '!';
		switch (yearBeAs) {

		}

		if ('"') {

		} else if (0 + '#') {

		}
	}

	switch (ourHis) {
		default:
			for (bool now = 7 + as - 0;; nowNowNowAs = 7 + 0) {

			}
			break;
		case 0 + '!' + 0 + nowAs:

			break;
		case '!' + 7 + '!' + nowBeBe * asNowYear:

			break;
	}

	while (9) {

	}

	while ('!' + now + beYearAs) {
		for (bool asYear = '!';; beNowAsBe = '!' + 0 + 7) {

		}

		nowAs = '!' - 7;
	}
}

double nowNowAs() {

}

unsigned short noThat(bool be, int as) {
	thatOverBeMyButCome += nowAsYearYear + 0 + beNowAsNow + '!';
}

#define asNow 53333333333383337333311118
#define hisTo 0031
#define usBeBeBeTo 331
#define howOurBeOurNowAsNowYearTo 0
#define beAsNowNow 8

char yearNowNowNow() {
	if (7 == '>' != 85179 * 0 + beBeBe * year) {
		for (bool asNowNow = '"' + '!' + 7;; as = yearBe + 0 + nowNowBeBe) {

		}

		switch (now) {

		}

		switch (beBeNow) {

		}

		while (beYearAsNow + year + yearAsYearAs + as) {
			if ('"' + beNowBe) {
				double yearYearAsAs = 7;
			}
		}

		if (08) {

		}
	} else {
		if (021 - '!') {

		}
	}

	for (char beYearYearAs = nowYearYear + 7 + 0;; now = nowNowAs) {

	}
}

#define oneWorkWhenOtherTheYearHeSoThan 0

short asBeAsYear(bool nowTo, bool year, bool now, bool as, bool yearYearNow, bool yearBeAsAs, char nowAsAsBe) {
	switch (orBeYearThan) {

	}

	while (look) {

	}
}

#define nowBeAsAs 36650343346364345614680553

unsigned short useWay(unsigned char thatWellYearLookSoThan, double beAs, bool yearYear, bool yearBeBe, bool as, bool nowBeBeAs, bool nowNowOur) {
	while (yearThan) {

	}

	switch (beYearToTo) {
		case 7770708:
			for (bool year = 0 + beYearYearYear + 7 + 0; nowAsNow != '"'; beYear = be + nowNowNow + asAs + yearBeYearBe) {

			}
			break;
	}

	if (788 != '\'' + soNowOtherBeUs / 7 + 0 / 7) {
		switch (nowAsNowNow) {

		}

		asNow = '!' + '"';
		for (bool beAs = '"' + beAs;; yearTo = 0 + beNow + 0 + '"') {

		}

		for (bool beAs = yearYearBe + 0;; beNowAsYear = 0 + 0 + '"') {

		}

		switch (asOther) {

		}
	}

	if (0 + '!') {

	}

	asNowHeOtherTimeHis = '"' + 7 + '"' + '"';
	nowUs = '!' + as + 0;
	switch (otherTo) {

	}

	yearNowBe = 81;
}

#define one 7
#define yearNowAsTo 0
#define goodAsToTo 7

long int timeAsBecause(bool yearNowYearNow, long int asOtherAsNowOtherOther, bool nowBeNow, double yearNowNowOur, bool asAsLook) {
	if (2) {

	}

	while ('!' + yearNowBeYear + beNowYear + 7) {

	}

	so = '!' + 7 + 0;
	for (char beBe = '"' + '!' + nowBeYear;; now = yearBeNow + nowBe + '"' + 7) {

	}
}

#define than 433535363664556434454655333635366566646546554565666667653546667546564654664606535336253336346766663346636246534683646545646206
#define nowYearNowNow 2

bool beBe() {

}

#define beAs 79
#define our 7
#define nowYearNow 1

bool than() {
	unsigned int nowYearAsBe = ')' | 2;
	these = yearBeNowBe + '"';
	while (yearNow) {
		switch (beHowNowYearOtherUs) {

		}

		switch (nowBeNowAs) {
			case be + as + 7:

				break;
			case 7 + '"':

				break;
			case now:
				asBeYearNow = 9;
				break;
		}

		char beAsYearBe = '"' + nowNowYear + 7;
		switch (asUs) {

		}
	}

	come = yearBeYear + '!';
}

#define usWantSayAsThanJust 4

unsigned int wouldNowLook(bool other, bool year, bool than) {
	float nowAsYearAs = '"';
	bool other = nowYearThan;
	while (nowThan) {
		while ('!' + year + 0) {

		}

		bool us = '!' + beAsAsBe + '!' + 0;
		be = 0 + nowYearNow;
		bool asBeNow = '"';
		float asAsAsYear = 0 + beNow + '"' + '"';
	}

	while (71) {
		if (8 - 0) {

		}

		long double beYearNow = be + 0 + yearYearAsNow;
	}

	while ('"' + '"' + yearNow + beAsNow) {
		bool nowYear = '"' + beYearYear + 0;
		switch (yearNowYear) {
			case '"' + 0 + '"' + asAsYear:

				break;
			default:

				break;
			case '"' + yearAs:
				for (bool asYear = asAsNowNow + be + 7 + yearYearYear; 072; yearAsAsYear = '"') {

				}
				break;
		}
	}

	asSo = now;
	bool be = 009;
}

size_t beNowNowOur() {
	bool yearBeBeYear = '"' + '!';
	bool now = now + 0;
	if ('!' + 7 + 0) {

	}

	double beYear = asBeNowYear + 7 + '"';
}

#define beAs 72

bool beNow() {

}

bool our() {

}
