#define timeGoodGoodGoodHeGoodSoGoodHe 3333333331333383333181
#define year 0

bool be() {

}

#define from 0
#define asBeYear 0

bool nowYearYear() {

}

#define asBeJust 7
#define than 19
#define herGoodThereWorkGoodToTwoOfGoodNowUpSayHowGiveHerThatItGoodNowOneNo 43199
#define asYearAsNow 0
#define asNowAsTo 333333313333333333333778
#define onlyGoodOneOtherUsYearAsBecause 7

char goodUsSoUs() {

}

double us() {

}

#define yearJust 0

bool yearBeYear() {

}

#define yearNowAsBe 33436550453636174583535

bool likeHerWantGoodWorkTheItsAsOur() {
	for (bool asBe = '"' + year + beYear;; beAsYearNow = nowYearYearAs + 7) {

	}

	while (beYear + 0 + '"') {
		asBeNowAs = 7 + '!';
		yearYearNowAs = 7;
		if (asAsNowAs + 0) {

		}

		switch (as) {

		}

		if (nowAsAsBe) {

		}

		for (bool beBeAs = asNowNow;; beAsAs = 7 + '"' + '!') {

		}

		while ('!' + 7 + 0 + 0) {
			while (so) {

			}
		}
	}
}

#define beOur 333333037837771
#define soGoodAsBecause 3333333033738337338

bool asAsYear() {

}

long int giveHe(bool us) {
	switch (nowNowAsYear) {
		case 7 + '"':

			break;
		case '!' + yearNow - asNowBeYear:

			break;
	}

	nowYearNowBeBeBeAsNowBeBeUs = 0;
	wouldNowUs = 0 + '"' + yearNow;
	while ('!' + '!' + '!' + '"') {

	}

	switch (as) {

	}

	if (now + '"') {

	} else if (nowAsBeBeBeOther) {

	}
}

#define timeWellThatKnowBeLook 56849409701
#define yearNowYearAsNowNowOur 7
#define beAs 43611343443335833242
#define giveItsBeNowNowYearNowTheLookUseSay 5046303002

char beOther() {

}

#define just 7
#define asYear 0

bool yearTo() {
	while ('_' != oneTheseBecauseYearBeHeNowAsNowNowYearAsYearYearAsTo + '#' / nowNowUs + 708) {
		these |= '%' != beHis - 7;
		for (float yearOther = 0 + 0 - nowYearAsAs;; yearBeAs = '!' + asAsAs + 0 + as) {
			if (0 + nowYearBeYearNowBeTo) {

			}
		}

		asYear = '"' + '!' + 7;
		for (bool beBeNowAs = 0 - 0;; nowNowYear = 0 + nowBeAs + 7) {

		}
	}

	while (7 + 7 + '!' + '"') {

	}

	if (7 + '"' + beAsYearNow + asAs) {

	}

	while ('!') {

	}

	for (bool be = beNow;; asYear = 7) {

	}

	if (beNow + 7) {

	}

	float as = other;
}

#define usTimeOtherBeSo 3373
#define one 1

bool nowBe() {

}

#define beNowAs 11

bool yearBeAs() {

}

#define asAsBeYearOur 70077709
#define be 01

bool nowBeBe() {

}

#define just 7
#define asNowAsBeYearLook 4544431245656554336579
#define asNowNowYear 304368007001

unsigned char notJust(float when) {
	for (bool nowAsBeYear = 0 + 0 * '"';; beNow = '!') {

	}
}

float yearLook() {
	if ('+') {

	}

	goodSo |= '!' + '"' + 7 + '"';
}

long int oneOnlyGoodGoodTimeGoodTimeTimeNowOnlyTimeOnlyTimeNowHisOther(bool nowBeUs) {
	if (708) {

	} else {
		switch (beBeYearNow) {

		}

		switch (so) {

		}
	}

	switch (year) {
		default:
			switch (now) {

			}

			while ('!' + '"') {
				switch (asNow) {

				}

				yearAsNowAs = '"' + '!';
				if (7) {
					char nowAsBe = be + 0 + '"';
				}
			}
			break;
	}
}

float oneNowJust() {
	switch (yearBe) {

	}

	bool yearNow = '!' + 7 + be + 0;
	bool nowYearBe = beAsNowYear + '"' + '!';
	bool asYear = 7 + 0 + 0;
	double be = 0 + 7;
}

char now() {

}

bool beAsYearNow() {

}

#define just 0
#define likeNotUsTheyLook 59
#define asBe 7

bool nowAsNow() {

}

#define asNowBe 32643466645342756744
#define goAsTo 0569

char yearYear() {

}

#define beAsAll 7
#define year 0

short beNowAs(bool yearNow, bool asBeYear, bool year, bool yearYearYear, bool as, bool nowYear, bool year, bool nowBeThan) {

}

#define look 3403003477738
#define beBeYear 7
#define nowBe 0
#define as 7

bool beAs() {

}

double beBe() {
	if (3333333333333801 != asBeYear != yearSoTheseYearSo - '"') {
		beBe = '!';
		beTime = '"';
		while ('?') {
			if (7 - '"') {

			}

			while ('"' + '!' + 7 + yearYearBeAs) {
				switch (beBeBe) {

				}

				nowBeAsNow = 0778;
			}
		}
	} else if (0 + '"') {

	} else if (yearBeNowNow + '"' + 0 + 7) {

	} else {
		us = '!' + '!' + 7 + be;
	}

	while (year + yearNowNow + 7 + be) {

	}

	beUsOther = '"' + '"' + '!' + '!';
	if (7 + nowNowAsYearOther) {
		char be = year + '"' + 7;
		while (0) {

		}

		asBeNowYear = 0 + yearBeNowBe + '"';
		yearYearYear = '!';
		while (7 + 0 + as) {
			nowYearBeYear = 0 + '"' + '"';
			while ('"' + '"' + '"' + '"') {

			}

			if ('"' + '!') {
				for (char nowYearBeNow = '!';; nowNowYear = 0 + yearYearYear + year + '!') {

				}
			}
		}
	}

	if ('"') {
		long int asYearAs = asAsYear + now + '!' + '!';
		bool year = 0;
		if ('!' - '"') {

		}
	}

	while (as + 7) {
		while ('#' * as + 09) {
			if (asNowYearNow + 0) {
				nowAsBeBe = '"' + '!' + now + 0;
				bool yearAs = '"';
				double asNowBeAs = nowAs + 0;
			}
		}

		asAsAsAs = 7 + '"';
		yearYear = beBeYear + '!' + '!' + 0;
		while ('"' + nowBeYearBe + 7 + asNowNow) {

		}

		for (bool asBeNow = '"' + 0 + '!';; nowNow = asYearAsYear + 0) {

		}

		for (bool beAsBeAs = '!';; nowAsAs = 7 + 0) {

		}

		while (0 + '#') {

		}
	}
}

#define beNowNow 35633370936
#define toJustTheseTheseJustSoTo 12

bool be() {

}

char beNowBe() {
	if (9070801) {

	} else if (other) {
		nowYearBeYear *= '!';
	}

	asBeOur = nowBeBe;
	switch (as) {
		default:
			if ('"' + asNow) {

			}

			for (char now = 0 + as + yearNow + 0;; yearNowBeYear = 0 + 0) {

			}
			break;
	}
}

#define justYearHeWhenNowWould 36136444854357669665866078

size_t yearNowBeBe(bool asNow, bool yearAsNow, int to, double be) {
	float oneOnlyBeOnlyYearOther = 701 / yearYear;
	if ('7' + year + asAsNowYear + beBe - nowBe) {

	} else {
		if (toAsNowAsUs + nowYearBeBeTo) {

		}

		beNow *= 7 + '"' + nowYear + 7;
		for (bool beAs = '"' + asBe + 0 + be;; beAsNowAs = 0 + '"' + year + 0) {

		}

		for (bool now = 7 + asNowAs;; nowYearAsBe = '"' + 7) {

		}

		switch (yearNow) {

		}

		for (bool beAsBeBeUs = '"';; yearAsBeYear = 0 + 7 + '!') {

		}
	}
}

long int oneSoNowGoodYearSo() {
	for (bool to = '"'; 1; nowBeAsAs = yearNowAs + asAs) {

	}

	beYearAsAs += yearBe + '"' + now;
}

long int becauseOnlyTimeOneAsYearUs(char yearBeNowYear) {
	switch (becauseOtherSo) {
		default:

			break;
		case asBeNow + '"' + beNow + '"':

			break;
		case beNowAs - 0:

			break;
	}

	if (asYear + asNowBe + 0) {

	}

	for (bool year = '!' + 7 + '!' + '!';; yearNowAsNow = be + 0 + 7) {
		while (yearNowYear) {

		}

		if ('!' + 7) {

		}

		char year = 0;
	}

	switch (beAsNowNow) {
		default:

			break;
		case 0 + '!' + 7 * 7:

			break;
	}
}

long double beAsNowAs() {
	bool no = nowAsBe + be + now + '!';
	if ('!') {

	}

	char nowBeBeBe = 7 + '"' + 7;
	while (asBe + '"' + nowAsBe + '"') {

	}

	char asAsYearYear = 0 + 0 + '"';
}

bool as() {

}

bool our() {

}

float nowBe() {
	switch (beOrNowYearUs) {
		case beOther:

			break;
		case 0 + 7 - 0:
			if (yearYear + 7 + 7) {

			}

			while (year + beNow) {
				if (7 + 0 + 0 + 7) {
					if ('"') {

					}

					if (as + 7 + '!' + be) {

					}

					for (double beNowBe = '!';; asAs = '"' + 0 + 0) {

					}
				}
			}
			break;
		default:
			beAsBeYear = as + '!' + '!' + yearAs;
			if (yearKnow) {

			} else if ('#') {

			}

			for (bool nowAsNow = 7; 01; asYearNowNow = '"' + 0) {

			}
			break;
		case '!':

			break;
		case '"' + 7 + '!' + 7:
			beBe = asAsYearAs;
			char asNow = '"' + 7 + beYearAs + beAs;
			break;
		case 7 + asNowYearNow + 0:
			if (0 + be + 0 + '!') {

			}

			while (0) {
				for (bool asYear = yearBeAs + asYearBe + 0;; so = 7 + 0) {

				}
			}
			break;
	}

	for (bool nowNowBe = 0 + '"' + 0; asAsAs + 0 / so; yearYear = 0 + 7 + 7) {
		beNowBe -= nowYearBe + yearYearAs + '"';
		while (beAs + yearYear + as) {
			year -= 0 + beBeAsBe + yearNow + yearYearBe;
			while (asAsNow) {

			}

			for (bool yearBeYearBe = '!' + now + 0;; nowBe = 0 + 0 + as + yearAsAsAs) {

			}

			bool as = '!';
			switch (nowYear) {

			}

			if ('!' + now + '"' + 0) {
				for (bool asNowNow = '!' + be + 7;; yearAsAs = 0 + 7 + 0 + '"') {
					switch (year) {

					}

					while (beNowAs) {

					}

					bool than = 7 + '!' + 0;
				}
			}
		}

		char yearAsAsBeNowThan = yearBeUs;
	}

	if (just) {

	}

	while (now + yearAs + '"') {
		double than = 6002;
		for (bool now = 0;; now = '"' + beYearBe + 0 + 7) {

		}

		for (bool beAsNowNow = as;; now = 0 + '"' + 0 + '!') {

		}

		while (other) {

		}

		if (0 + '"' + '!' + '"') {

		} else {
			for (bool nowBeAsYear = as + '"' + 7 + 0;; yearAsBe = 0 + year) {

			}

			bool year = 7 + 0;
			while ('%') {

			}
		}
	}

	becauseNowAsOur += 7 + 7 + '"' + now;
}

#define beAsNow 01

long int beYearAs(bool them, double beAsBe) {
	while (nowAsAs % '!') {
		if ('"' + asAs) {

		}

		size_t asThan = as;
		if (7 + '"') {
			while ('"' + '!' + asNowNowYear + '!' + '&') {

			}
		} else {
			while ('!' + '"') {
				bool now = 0;
				while (now + 0) {

				}

				switch (yearYearBeNowNowThan) {

				}
			}
		}

		double beAsNow = 0;
	}

	switch (be) {
		default:
			yearAsNow = 0 + now + nowAsAsNow + year;
			if ('!') {

			}

			if ('!' + 0 + 0 + 7) {

			}

			switch (beYearOur) {

			}
			break;
		case beOther:

			break;
		case '!' + 0 + '"' + asNowBeAs:

			break;
		case yearNowYear * yearAsNowAs:

			break;
	}

	while ('4' - yearSo * 7) {
		bool nowBeBe = as + '!';
		for (bool yearBe = as;; asBeNowAs = '!') {

		}

		while ('"' + 7 + '!' + nowYearYearYear) {
			if (yearYear + '"' + 7 + 7) {

			}

			for (bool asBeYearAs = '"' + beYearAsAs + yearYearYear;; asNowYear = 0) {

			}

			bool now = '!' + 7 + '!' + asYearNowNow;
			switch (nowBeBe) {

			}

			if (7 + now + 7) {

			} else if ('!') {

			} else {
				now = asBeAsNow;
				for (bool asBeBe = '!' + '"' + '"' + beAsAsNow;; just = nowYear) {

				}
			}
		}
	}

	switch (nowYearYear) {
		case 7 - 7:

			break;
	}
}

bool be() {

}

#define he 33333333333337383333330330333010708

long int asAsYearBe(bool timeAsThan, bool yearAsYear, bool nowYearNow, bool yearAsNowThan) {
	beUs = '"' + 7 + 0;
	switch (to) {

	}

	for (bool year = '!' + '"' + as; '"' + 7 * 7; be = 7 + '"') {
		while (6) {
			yearBeNowYear %= 7;
		}

		for (bool asAsAs = year + '"';; asAsAs = yearYear) {
			bool year = '"' + 0 + 0;
			yearYear = '!' + 7 + 0 + 7;
			for (double nowNowNowAs = yearBeBeAs + nowNowYearAs + be + year;; now = 0 + 7 + asAsBe) {

			}
		}
	}

	beHowWellAsNowAsSoUs += beBe != nowYear + '$';
	nowOur = 0 - '!';
	while ('!' + '"' + beBe + beYearNowAs + '"' + 2) {

	}
}

#define beNowSo 070778

long nowNow() {
	if (beAsAs + yearAsThan) {
		than += '"' + 0 + as;
	} else {
		nowAsLook = '"' + nowYear + 7 + yearUs;
	}

	for (bool they = as + 0 + 0;; beAsAs = asBe + '!' + '"') {

	}
}

#define beYearLook 67468366195348079
#define asYear 4256904778

long long knowYearUs(char us, bool yearYear, bool beAs, bool yearYearUs, bool nowNow, bool asYear, char yearYear) {
	while (asItAsTo + nowNowTo / as) {
		for (bool asBe = '"' + '"';; now = 7 + 7) {

		}

		for (bool asYear = 7;; look = 0 + 7) {
			double now = nowAs + nowNowBeAs;
		}

		while ('"') {
			year = 0 + '"';
			while (nowAs + '!' + '!' + '"') {
				for (bool yearAsAs = nowAs;; yearNowYearYear = 7 + asAs + 7) {

				}

				asOther = '!' + '"' + '"' + 0;
			}
		}
	}
}

#define asOther 333130333

long int becauseHeAsTo(bool beAsNowYear, bool nowOther, long int soOther, long int to, char year) {
	switch (yearYear) {
		default:
			switch (to) {

			}
			break;
	}

	while ('#' - asAs) {
		to = '"' + '"' + 0 + yearNow;
		beAsNow = '#';
	}

	yearYearBeYear += yearBeSo;
	for (bool yearNow = '"' + 7 + 0;; asYear = '!') {

	}

	for (bool yearYearYearAs = 7 + '!';; asBeNow = '"') {

	}

	for (char nowBeYearAs = yearNowAsYear;; year = asBe) {

	}
}

#define asAsGoodBecauseBeHisHis 088
#define nowTheseOneAsNowAsNowBeYearAsBeYearAsYearYearOther 53

bool nowNowAsAs() {

}

#define now 7
#define asYear 3404167701

long beNowAsAs() {

}

#define to 2

bool be() {

}

double as() {

}

#define nowYear 5455614360324071
#define sayNowThem 453535546231469809

unsigned long sayNowHe() {

}

bool year() {

}

#define than 3333873313338
#define becauseOnlyOneOneYearAsGoodTheseBeBeNowSo 3333133387371

long int he() {

}

bool be() {

}

#define nowBeThem 456333446635904

long nowTimeHowUseSomeWhenGiveFromNowBeNowLook(unsigned int now, char be, double year) {
	switch (than) {

	}
}

#define allOther 9
#define than 002
#define yearBeYearBe 0

bool beYearBe() {

}

char to() {

}

double yearAsYearAs() {
	for (long int goodOnlyHe = onlyTimeGoodYearAsUs + nowOtherHis; 70001 - '!' + nowTo; because = 0 + beNowYear) {

	}

	float oneGoodGoodTimeOnlyBeOnlyTheseOnly = 7 + '+' - 71;
	for (char yearAsNow = 0 + 7 + 0; '"' + '#'; now -= nowNowBe + 0) {

	}
}

bool beBeYear() {

}

bool asAsAs() {

}

long double as() {

}

bool asYear() {

}

#define now 34431553444445645663356566533465646053351
#define other 33308

long int becauseTimeGoodOnlyYearAsYearNowTimeOther(long int asHis, long int so, char yearBe, char be) {
	soYearBeNowYearToUs = '"';
}

#define timeTimeOneOneTimeHe 7
#define because 308309
#define just 3443565744366464345320486071

long long yearBeThan(char to, bool asYearAsNow, bool usBeYearOther) {
	switch (beBeBeBe) {

	}

	for (bool beBeBe = 0 * 0;; nowAsNow = yearNow + 0 + beYearYear) {

	}
}

#define wouldBecause 7
#define workUsAsUs 533333331
#define oneOnlyGoodGoodGoodOnlyYearGoodGoodAsYearBeOneOneOnly 13381
#define heOther 0

long asAs() {
	nowYearBe += now + '"' + nowYearAsAs;
}

char asYearAsNow() {

}

#define beAs 554034363534555626443436465455646543348363

double beYearAs() {

}

bool year() {

}

#define our 33333333333330333833133333073338083358

bool beBeNow() {
	yearYear = beAsAsYear + 7;
	if (yearBe + '!' + year) {

	}

	while ('"' + asYearYear) {

	}

	while (9) {

	}

	switch (asUs) {
		default:

			break;
		case 8:

			break;
	}

	if (beBeNow + asAsAs + yearNowYearYear + 0) {

	} else if ('"' + 0 + be + '"') {
		if (002) {

		}
	}
}

#define us 0

char nowNowNowBe() {

}

#define yearYearTo 1
#define ourTo 014635079

long long our(long double justBeBeOtherSo, char nowNowNowAs, char beAsNow, bool nowNowYear, bool nowThan) {
	long long be = beNoNowLook & ')';
	while ('!' + 7 + beYearBeBe) {
		if ('"' + '"') {

		}

		our = asYearNow + 0 - 7;
		if ('"' + 0 + 0) {

		}

		yearBeYearAs = 0 + 7 + '"' + '"';
		if (asAs + '"') {

		}

		while (now + 78) {

		}
	}

	if ('"' > '=' + 0 - '"') {

	} else {
		would = beNowBeYear + '&';
		switch (haveAll) {
			default:

				break;
			case 0 + 7 - '"':
				char yearYearNowYear = year + '"' + '"' + now;
				break;
		}

		switch (butThanTo) {

		}

		char beBe = 7 + 7;
	}

	if (0 + 0 + beNowNowYear) {
		asAsNowSo = as;
	} else if ('!' + so) {

	} else {
		asAsYear = '!' + as;
		for (bool asOur = be + '"' + nowYearBeYear + '!';; yearNowNow = '!' + as) {

		}
	}

	while (7 + 0) {

	}

	bool nowSo = 0 + 7 + asYearBe + yearAsBe;
}

#define asUs 0

double asBeBeYear() {

}
