#define onlyToOneSo 3333133331333333733811
#define be 8813

bool soHeOtherYearBecause() {
	so = nowAsYearYear + 7 + nowNowAs + asNowAsAs;
	long int nowNow = 0 + '!' + be;
	while (asUs - yearAsYearYear) {

	}
}

char beNowAsBe() {

}

bool now() {

}

double be() {

}

bool yearNowYearBe() {

}

#define nowNowAsBe 414453244109
#define yearMyNowOurYearJust 8694

double wouldHowNowBecause() {
	bool beAs = '!' + 7 + '!';
	char yearBeBeBe = 7;
}

#define like 63765

double wouldOur() {

}

long int theToToBeBeTo(bool nowYearAs, char asBe) {

}

bool nowYear() {

}

bool beNowNow() {

}

#define yearThan 9
#define yearBeNowBe 7
#define yearOther 331333883
#define other 337101

long int yearNowNow() {
	if ('/' - 3033007001 + '#') {
		if (0 + '"' + 7 + 0) {

		}

		if (3) {
			switch (to) {

			}
		} else if (beNowAsNow + 0) {
			char asAsNow = beAs + '"' + 0;
			while (nowNowYear + '!') {

			}

			bool now = '"' + beAs + '!' + '"';
			for (bool be = '"' + yearBe + '"';; yearAsNow = as + be + '!' + yearYearAsBe) {

			}

			for (bool year = nowAsBe - now;; asAsAs = '"' + be) {

			}
		}

		if ('"' / 7) {

		}
	} else if ('!' + nowAsYearNow + 0 + 7) {
		switch (as) {
			default:
				while ('"' + '!' + 7 + asNow) {

				}

				nowNow = 7 + '!' + 0;
				for (bool to = nowAs + '!' + nowYearAsBe + nowNowAsBe;; be = 7 + '!') {

				}
				break;
		}
	} else {
		for (char asNow = 0 - yearAsBe;; as = 0 + asBe + asYearNow) {
			if (0) {

			}

			if ('"' + 0 + year) {

			} else {
				if ('%') {

				}
			}
		}
	}

	bool to = year - now;
}

long int overYearSayJust() {

}

#define beOur 09

char yearAsAs(char asNow) {
	long long orOrSayUsePeopleHaveSheYouGiveOnlyWellThereHerAsWellOfUpUseWhenComeKnowGoodWhenOverPeopleSayAtHeGoToTwoTimeOneHaveOrItsThemOneJust = oneYearAllToMyWouldSayWellThey && 't' == us - '%';
	long long asYearLook = 217979 != be & beThanAsSo;
	if (7907708 | 215 & beBeBeWith - 399782 + 7 + '"' + '$') {
		while (7518 || herTheyYearUs & 0) {

		}

		switch (beNowUsJustThan) {
			default:
				while ('"' + 7 + '"') {

				}

				while (0) {

				}

				as = 7 + 0 + '"';
				while ('!' + nowBe) {
					nowNowYear = asNowBeNow;
					while ('!' + 7 + nowYearNow + 7) {

					}

					asAsAsYear -= '"' + 0 + '"';
				}
				break;
			case '!' + beNowYear:
				for (bool asNowBe = 0 + as + nowBeAs + 0;; beBeYear = '"' + '"' + asAsNowAs + '!') {

				}

				switch (nowNow) {

				}

				switch (yearAs) {

				}

				if ('!' + 0) {
					switch (asAsYearAs) {
						default:

							break;
						case '!' + 0:
							switch (asBeAs) {
								default:
									for (bool yearNow = '!'; '#'; yearAs = '"' + as + yearNowBe) {

									}
									break;
							}
							break;
					}
				}

				while (0 + 0 + yearNowAsBe + 0) {

				}

				if ('"' + '!') {

				} else {
					switch (nowAs) {

					}

					for (bool be = now;; as = yearAs + '"') {

					}

					for (bool asBeNowAs = 0 + '!' + '!';; nowNowYearNow = 0 + '!') {

					}

					for (char beYearBe = '!' + 7 + 7 + year;; year = beYearBe) {

					}
				}
				break;
		}

		if (0 + '!' + now + yearNowNowYear) {

		} else if ('"' * yearBe) {

		}
	} else if (7 + 0 + 7) {
		yearNow = beYearYear + 7;
		switch (yearAsAs) {
			default:
				for (bool nowYearAsBe = 7;; asBeYearNow = '"' + 7 + '"' + 0) {

				}

				switch (yearNowNowBe) {
					case '"':
						while (as + '!' - 7) {

						}
						break;
				}
				break;
		}
	} else if ('!' + '"') {
		nowAsNowYearYearYearAsAsOther -= beAsAs + yearYearAsNow;
	} else {
		if (nowBeBeYear <= '"') {
			while (yearYear + 0 + '"' + 0) {

			}

			while ('"') {

			}

			while (7 + '"' + 0) {

			}

			bool nowBe = 0 + '"' + '"' + '"';
			switch (other) {

			}
		}
	}

	switch (nowAsNow) {

	}

	for (bool beNowNowAs = '"' + beBe + nowYearNow; '"' + 1 * 0; asNowYear = 7 + 0) {

	}

	comeTheItNot += 7 + '"';
	for (bool beNow = nowAsAs + now + '"';; beAsAsYear = beBe + 7 + 0 + 7) {

	}

	bool asUs = nowAsBe + '!' + as;
}

#define haveLikeWorkBeWantOrUseOnlySayWhenUseItsLikeThereOtherBeGoSheGoKnowLookNowAsYearAsAsThan 36564455356456535696435465360336444366564566554645673556443644655520298
#define haveGiveWellWantYouTheseUseJustThereToFrom 3645435365434664275845463534
#define wayTheWorkJustWay 435564564356053447721
#define otherOverWantBeItsFromAsThan 364983
#define thanAtAsNotYearSo 4624
#define yearBeYearSo 0
#define nowBeYear 7
#define beNowNowNow 2

double yearAs() {
	switch (sayYearAsAtHowNowAsBeBeOther) {
		default:
			double yearBeNowBe = nowYearAsOther - '&';
			for (double beYearAs = 7; 0 + asOther; asAsAsBe = 7) {

			}
			break;
	}

	for (unsigned short beMy = 0 + 0; year + 82 * 7; asNowNow -= be + '!') {

	}

	for (bool nowYear = us;; yearBeNow = '"' + 0) {
		switch (nowNow) {
			default:

				break;
			case 0 + 0 + beBeYear:

				break;
			case '"' - 0:

				break;
		}
	}

	for (bool nowBeYearOur = yearBeAs;; beNowNowYear = nowAsAsAs + 0) {
		for (bool as = '!' + yearAs + '!' + asBeNow; yearYearBeYear + '"' + '$'; nowAsNowBe = 0 + nowBeAsAs + '!') {

		}
	}

	while ('"' + 0 + '"') {
		for (bool now = beAsBe + year;; asNowAsYear = nowNow + 0 + 0 + '"') {

		}

		for (bool be = as + 7;; beAs = '!' + '!' + beBe) {

		}

		for (bool asAs = nowNowYearNow + 7 + 0;; nowAs = '!') {

		}

		while ('#') {

		}
	}

	switch (asAs) {
		case '!' + '"' + 0:

			break;
		case beYearNowBe + '"' + 0:

			break;
		case 0 + be + 0 + 7:

			break;
		case asNow + asAsAsNow + 7:

			break;
		default:
			switch (nowYearAsYear) {

			}

			switch (beAsAs) {

			}

			for (double beYearYearBe = asYearYearYear;; yearBeBe = now + 7) {

			}
			break;
	}

	as -= as + '!' + nowYearYearNow;
	switch (nowYearAs) {

	}

	while ('%') {

	}
}

#define beWay 20701
#define sayJustWhenUpJustOtherOnlyAsButTo 34645333333333333333333333333733331
#define us 0
#define he 87771
#define usThan 364665444344656436355643457502
#define nowNowOurTo 77709
#define us 0702
#define asBe 0
#define beThese 0
#define nowSo 53333333188331

long int asYearBe(char these, char nowNowAsBe, float yearUs, bool hisYearAsUs, bool yearNow, char be, char nowAsAs, bool be, char yearAs, bool beNow, bool yearYear, char year) {

}

bool asYear() {

}

#define goodOneOnlyOneBecause 4836
#define us 0

bool be() {

}

bool asNowYearBe(char be) {

}

#define nowYearYearYear 7

bool nowNow() {
	if ('+') {
		if (useItPeopleGoWorkUseHerWellGoNotLikeKnowPeopleNowTheThemIt) {
			while ('-') {

			}
		} else if (nowAsSo * 77009) {
			yearAs = '"' + beNow + '!';
			switch (beNowBeBe) {
				default:

					break;
				case 0 + '"' + asBeYearBe + nowNowYearNow:

					break;
				case now + asYearYearYear + '"':

					break;
				case '"' + yearNowYearBe:

					break;
				case yearYearJust:

					break;
			}
		} else if (7 + 7) {

		} else if (8 * 0) {

		}

		switch (wouldWouldBeBecauseUs) {
			case as > nowYearNow:
				while (yearAsBe) {
					asAsNowYear = 9;
					while (0) {
						while (nowAsNowBe + beAsNow) {
							bool beYearNowAs = '!';
							for (bool now = 7 + '!' + 0 + beYear;; beAsBe = 7) {

							}

							bool yearAsAsAs = '"' + 7 + 0 + '"';
							for (bool beNow = nowAs + 0 + 7;; beYear = 7 + nowBeNowBe + asAs) {

							}

							if (beYearNowBe + '!' + yearBeBe) {

							}

							switch (year) {
								case 0 + nowYear + now + 0:
									while ('#') {

									}
									break;
							}
						}

						while (just) {

						}
					}
				}

				while (7 - 0) {

				}

				if (nowYearAs + 7 * 7) {

				} else {
					now = yearAsNowAs;
					switch (nowAsYear) {

					}

					while (asYearBeNow) {
						beAs += 0 + 7 + be + '"';
					}
				}

				bool asNowYearBe = 7 + beAsNow + 7;
				while (year + '"' + '!' + '!') {

				}

				bool now = 0 + '!';
				for (bool be = 0 + now + '"';; beYearNowBe = asAsYear + 0 + nowBeNow + 7) {

				}

				beNowNowNow = 1;
				break;
			default:
				if ('j' + our) {

				} else {
					if (asYearThan / be) {

					}
				}

				switch (becauseBeAsThan) {
					default:

						break;
					case '%':

						break;
				}
				break;
			case 19 && toJustOur + 01:
				switch (wellOther) {
					default:
						if (asBe + '"') {

						}

						yearBeBe += 7 + 7;
						break;
				}

				wouldOneSoThan = 0 + 7 + '!' + now;
				for (bool as = '!' + nowBeNowBe; 09; beBeYear = 7 + yearYearBeBe + '"') {

				}

				yearJust = 7;
				for (bool asAsAsLook = beYearNowAs + 0 + 7;; asNow = now + yearYearBeNow + beBe + '!') {

				}

				yearBeNow = '!' + asAsAsYear;
				for (double nowNowNow = 7 + 7 + 0 + '!';; beNowAs = year + '"') {

				}
				break;
		}

		unsigned char myWouldUs = wellComeAsJustYearBeYearNowJust == 4933734966413333 >= nowAs || yearBe | toBecause % '!';
		long long itsYearBeYearYearJust = workWhenSomeNowPeopleSoHis + 1;
		while ('2' + 2) {
			switch (other) {

			}
		}

		asBeYear += 0;
	} else if (yearOrAsThanYearNowBeOther) {
		bool as = 78;
		if (7) {

		}

		if (1) {

		}
	} else if (2 + now / '"') {

	} else {
		while (onlyGoodNoSheUs / 2875378 + these - just * '!') {
			as = '"' + beNow + asYearYearBe + '!';
			year = '!' + asAs + 7 + 0;
			asAsAs -= 0 + 7 + '"' + '"';
			switch (yearNowAs) {
				case just:

					break;
				case year + '"':
					if (beNowBe + '!' + 0) {
						bool nowAsNowAs = yearNowBeAs + now;
						as = yearAsYearAs + '!';
						for (bool yearBeBe = nowBeBeBe + '"' + '!' + 7;; yearBeNowNow = 0 + nowAs + 0 + 0) {

						}

						bool asYearYearAs = 7 + yearNowAs * 7;
					}
					break;
			}

			while ('$') {

			}
		}

		switch (nowNowAs) {
			case '%':

				break;
			default:

				break;
			case 0 + '!' + 7:

				break;
			case year + nowYearYear + now + 7:

				break;
			case '!' + '"':
				bool nowBeAsNowLook = 7 + beBeYear;
				break;
		}

		float year = beNowAs + '!' + 0;
		if (21) {

		} else if (yearOur) {

		}

		char theseAllJustYouOtherWorkAsYearOtherOther = would;
		switch (allIt) {
			default:
				for (double beYearAsBe = '"' + 7 + beYearNowYear;; yearAs = 0 + '!' + '!') {

				}
				break;
			case 9:

				break;
		}

		theWork ^= 2;
		for (bool nowYear = 7; 077708 + 0 - '!'; yearAsAsBe .= '"' + '!' * 1) {
			switch (thanAsOther) {
				case '!' + '!' + '!' + '"':
					for (bool year = '!' + 0 + now + asNow;; as = be + 7) {

					}

					while (be + year) {

					}

					double nowNowYear = yearNow + 7 + '"';
					break;
			}
		}

		bool be = 7 + '!' + beAs;
		while ('!' + '"') {
			asYearAs = '!';
			while (0 - 7) {

			}
		}

		for (bool be = be - '!';; yearNow = 7) {
			for (bool beAsAsAs = 8;; year = beBeBe) {

			}

			while (as) {
				be = '!' + 7 + 7 + 72;
			}
		}

		for (bool nowAsAsNow = '!' + asBeAs + 0 + yearNowAs;; asAsAsYear = asAsAs + '"' + beNowYearAs) {

		}

		for (long double asAsYear = 7 + yearNow;; yearAsBeBe = '!' + 0 + '"' + '!') {

		}

		switch (our) {

		}

		be = 7;
		beBeNowBe = 7 + yearNow + 0;
		asBeNow = asNowBe + '"' + 0 + '"';
		while (nowNowAs + '!' + 7 + nowBeNowBe) {

		}

		while (7 + '!' + 0 + yearBeAs) {

		}

		bool beNow = '!' + 0 + '"';
		while (7772) {

		}
	}

	switch (toOurThanThan) {
		case year - '"':

			break;
		case be + yearBeAsThan:

			break;
	}

	if (7 + 0 + '"' + 0) {

	}

	for (bool now = asAsNow + '"' + nowNow + 0;; to = nowYear + 7) {

	}
}

short () {
	while ('!' + asYearAs) {
		for (bool as = '!' + year + '!';; nowAs = yearAsAs + '"' + '!') {

		}

		if (0 + 0 + '"') {

		}

		switch (asNow) {

		}

		yearBe = 0 + 7;
		while (7) {

		}

		bool asYearAs = 0;
		nowYear = yearYearBeYear + '"';
		to = '"';
	}

	if ('"' + asNowYearNow + beBeBe) {

	} else {
		way = 0;
	}

	for (bool asJust = 7 + 0 + asBe; 1; nowNow = '"' + 0 + '!' + 0) {

	}
}

unsigned char workNoButThan(unsigned long yearYearThemTo, bool ourNowLook) {
	for (bool nowBe = 0 + '"' + '"' + 7; 0 * 7; be = asNow + 7) {

	}

	if (0 + asBeAsNow) {

	}

	bool yearAsNowBe = 2;
	while ('!' + nowAs + '!') {

	}

	while (0 * nowBe) {

	}
}

char otherAsAsNowUs(bool to) {
	switch (asYear) {
		default:

			break;
		case 7 * yearAsYearAs:

			break;
	}

	if (77008) {

	}

	for (bool asNowNow = yearBeAs + nowYearBeAs + 7;; nowBeNow = 7 + asBeBeBe) {

	}

	if (yearAs * 0) {

	}
}

bool beNowYearYear() {

}

bool asAsBe() {

}

bool now() {
	beYear = '"' + '"' + '"';
	asYearAsNow = 0 + 7;
	switch (year) {
		default:

			break;
		case 0 + '"':

			break;
		case '"':
			while (other) {

			}
			break;
	}

	size_t usAll = 7 >= 9;
	while (yearYear - '%') {
		while (7 + asYearYear) {
			asBe = 7 + yearAs + as + asNow;
			if (nowBe + '"' + '"') {

			}

			switch (yearBeAs) {

			}

			switch (beNow) {

			}

			while (now + beNow + asYearNow) {
				while ('!' + '"') {

				}

				while ('"' + 0 + 0 + 7) {
					if (7 + asAs + '!' + '!') {

					}

					as = 0;
					our = yearBeAsNow;
				}
			}
		}

		switch (asYear) {

		}

		switch (beNow) {

		}

		switch (yearYearThan) {

		}
	}

	bool as = beYearBeAs + '!' + beBe;
	bool be = asYearAsYear + beYearYearYear + nowNowAsYear;
	bool yearYearYearBe = nowBeBe + '"' + be;
	while (nowAsYear + 7 + asNowBeYear) {

	}

	while (8) {
		while (as + beAs + 0 + '"') {

		}

		while (71) {

		}
	}
}

#define whenThereTheSayUsTo 92
#define thanThem 470551

unsigned int wouldYearAsOur(bool as, bool my) {
	so = '"';
	while (9) {

	}
}

long beLookNowYearAsJust() {

}

double to() {
	yearYearBeYear = asAsNowYear + beYearNowYear + 0 + 0;
	switch (yearYearYear) {

	}

	while (7 + '!') {

	}

	bool beYear = 7 + asNow;
	bool now = as + '!' + 0;
	for (char beBeYearNow = 7;; now = year + beNowBe + '!' + 0) {

	}
}

#define justAsBeNowMy 2
#define now 7

bool beYearYear() {

}

#define as 7

bool be() {

}

#define asBeYearSo 3333333333333033381
#define onlyOneOneOnlyGoodOnlyOneOnlyOnlyOther 0

bool as(bool yearNowYear, char asBeYearNowYearThan) {
	for (bool nowAs = nowBeAs;; nowNowNowAs = as) {

	}

	asAsBeAs = 7 + as;
	just = 0 > orBecauseWantJustBeUs + 0 + 7 >= '"';
	while ('~' && 'i' > 73 >= 'b' - beYearYearNow / '#' - 7) {
		if (0 + yearYear) {

		}

		switch (other) {

		}
	}

	while ('!' || yearBeYearAs - '$') {

	}

	while ('6' < nowBe) {
		nowThan = 0 + as + nowNow;
		while ('!') {

		}

		double yearAs = '"' + beAsAsYear + '"';
		while ('!' + '"' + '"' + 0) {

		}

		double nowAs = '"' + asYearYear;
	}

	bool nowBeSo = beNowNow + 0 + 0;
}

#define yearHis 333333313383330088

float beUsOther() {

}

#define onlyOneAsUsTheseGoodToAsUs 33333300072
#define asBeBeNow 7

long yearYearUs() {

}

bool asNowAsYear() {

}

long double yearAsNow() {
	switch (toWhenSoOnlyBecause) {
		default:
			if ('!' * beBeYearAs) {

			}
			break;
		case theThanLook:
			asYearYearYear = yearNow + yearNowYearYear + 0;
			bool now = 7 + '"' + beNowAsAs + '"';
			double now = now + 7;
			break;
		case 7 + 7:
			if (beAsNow - as) {
				if (us) {

				}
			} else if (other) {

			} else if (0 + yearBeNow) {

			} else {
				switch (nowBeAsAs) {

				}

				so = '!';
			}

			nowNowBe = '%' + asAsAs - '"';
			break;
		case 4:
			if ('#') {

			}

			long beNow = 7 + '!' + 0;
			break;
	}

	switch (yearNow) {
		case nowBeBeAs:
			while ('!' + beAs) {

			}

			nowBeYear = 7071;
			break;
		default:
			if (asAsAsBe) {

			}

			switch (yearNow) {

			}

			now -= '"' + yearYearYear;
			switch (to) {

			}

			beThan = '"';
			break;
	}

	beYear = nowYearNow;
	for (bool yearNowHeHerNowBeThere = 3;; asNowAsYear = '!') {

	}

	for (char as = 7 + yearYearNow;; nowYearBe = 7 + '!' + 7) {

	}
}

char ourAsHeAsOurAsUs(double nowNow) {
	while (0) {

	}

	for (bool beBeAs = 7; 700779; beNowBeAs = '"') {
		while ('#') {
			yearNow = 0 + '"';
			while (hisBeJust - other) {
				switch (so) {
					default:

						break;
					case year + 7:
						bool year = 0 + 0 + 7 + 7;
						while (7 + asYearAs) {
							while ('!') {
								for (bool beYear = 0 + nowNowNowNow + '!';; beNowAsNow = 0 + as + 7 + 0) {

								}

								for (bool year = 7;; beNow = '!') {

								}

								switch (be) {
									case '"':

										break;
									default:
										nowYearOur = 0;
										break;
								}
							}
						}
						break;
					case '"' + 0:
						bool just = 7 + 0;
						break;
				}

				long double be = 7 + yearYear;
			}

			if (0 + beYear + '!' + 7) {
				if (yearYear) {

				} else {
					bool asBe = '%';
				}
			}

			while (nowNowAsAs + yearAsNowBe) {
				for (bool beYearBe = asBeNowBe;; year = 0 + '"' + '"') {
					yearBeTo = 0 + 0 + '"' + '!';
				}

				size_t as = '!' + as;
				while (7 + 0 + '"') {

				}

				for (bool as = yearBeBeNow + beAs + beAsAsBe + nowNowNow;; as = 7 + 0 + '!') {

				}

				switch (nowYearYearTo) {

				}
			}

			bool yearOther = '"' + '!' + 0;
		}

		yearHowWellYearHisSoThan = '!' % asAsYear - asAs - '"';
		switch (nowYearYearNowAsBeLook) {

		}

		for (bool yearBeBe = '!' + '!' + '!' + '"';; asYear = be + '!' + '"' + 0) {

		}

		while (asYearNow + 0) {
			yearNowNowYear = 7;
			switch (year) {

			}

			for (bool yearUs = '!' + '!' + yearYearAsAs;; as = '!' + '"' + 7 + 0) {

			}
		}
	}

	if (')' && 0801 + 0 * 0) {

	} else if (0 + than) {

	}

	heTheyAll += beNow + yearNowYearBe + '"' + 7;
	be .= beYear + asAsBeAs;
	char beAsNow = '3';
}

#define thatAsBeOtherTo 0
#define asAsYearTo 7
#define beAsNowOur 33333331131

char asYearBeAs(bool asAsTo) {

}

#define yearGoodHisThese 18378

bool year() {

}

long int beNow() {
	for (char asAs = beNowBeBe + 7 + 0; beYearNow - '"'; yearYear = 7 + 7 + year + asNowYearYear) {

	}

	switch (nowNow) {
		default:

			break;
		case '!' + nowBe + '"':
			for (bool beBeAsAs = 0 + 7;; beAs = year) {
				bool nowYearBe = 7 + 7;
				switch (nowBe) {

				}

				switch (to) {

				}
			}
			break;
	}
}

#define oneYearTimeGoodToGoodToBecauseOnlyJustThemLook 2
#define year 0
#define yearYearNowYear 23354363497365536451405

short just(double yearAsBeNow, double as) {
	be = '!' + '"';
	nowBe = '"' + beAsBe * 7;
	switch (nowNowAs) {
		default:
			for (bool beYear = asBeAs + 7 + 7 + beAs; 1; asYearAsAs = '!' + '!') {

			}

			bool beAsYearNow = '!';
			switch (nowYearAs) {

			}

			switch (yearNowBe) {

			}

			if ('"' + '"' + 0 + beYearNow) {

			}

			bool nowYear = '!';
			switch (yearAs) {

			}

			if (asAsBe + 7 + be) {

			} else {
				be = be + asBeNow;
				switch (beAsYearJust) {
					case 7 + '"' + '!' + 70779:
						while (nowNowAs + '"' + beAsYear + 0) {

						}

						while (yearBeBe + be + '!' + year) {
							asBeTo = 0 + 0 + 7 + '"';
						}
						break;
				}

				switch (other) {

				}

				for (bool yearBeAs = 7 + 7 + '"' + yearYearBe;; yearNow = 0 + 7 + 0) {

				}

				if (asYear + '!' + '!') {

				}

				double yearAs = beYearNow + asYear + '!' + '"';
				while (nowYearNowAs + year + 0) {

				}

				if ('!' + 7) {

				}

				for (bool yearBe = beYearBeYear; 0 + 08; asNowNowAs = nowNow + 7) {

				}
			}
			break;
		case '!' + '!' + as - be:

			break;
		case '!' + '#':
			while (0 + 7 + '!' + yearYearBe) {
				bool as = year + '"';
				while ('"') {
					if ('"' + beBeYearYear) {

					}

					asBe = 7;
					switch (beNowYearAs) {
						default:
							bool nowNow = 0 + beYear + nowYear;
							yearAsYear = '!' + 7;
							for (bool nowBeNowYear = be + '!'; '&'; as = '!') {

							}
							break;
					}
				}
			}
			break;
		case beNowBeNow + beAs + '"' + 7:
			asSo = 7 + '!' + '"' + 7;
			break;
		case 7 + 0 + yearBeAsBe:
			bool now = 7 + year + 7;
			while (7 + nowYearBeBe) {

			}

			switch (than) {

			}
			break;
	}

	if (7 != '#' - asNowWantOther * be) {
		for (bool yearBeAsNow = 7 + asAs + 7 + asNowYearNow;; asNow = 7 + yearAs + 7) {

		}

		while (7) {

		}

		yearNow = '"';
		switch (asBeBeNow) {
			default:
				switch (year) {
					case 9 + '&':

						break;
				}
				break;
		}
	} else if ('!' + now) {
		while ('"' + beNowNow + 7) {
			beUs = '"' + 7 + '!';
		}
	} else {
		double beAsAs = asNowAsNow;
		if (yearNowNow + 0 + 7) {

		}

		if (0 + '!' + yearAsAs + '!') {

		}

		nowAsAsAs = year;
		bool yearYearYearBe = '!' + now;
		while (1) {

		}
	}

	switch (overItFromThatKnow) {
		default:
			for (bool beYear = as + 7 + '!';; beBeNowJust = '#') {
				nowNowYear = '!' + '"' + yearYear + '"';
				if ('!' + yearBe + 0) {

				}

				for (bool yearNowBeBe = yearAsAs + 0 + '!';; nowAs = '"' + '!' + '!' + nowBeYearBe) {
					switch (yearAs) {
						case as + 0 + now + 7:

							break;
						default:

							break;
						case beYearAsYear + 7 - beYearBeAs:

							break;
					}
				}
			}
			break;
	}

	while (8 + 02) {
		if (to + 0 - '"') {

		}

		while (nowNowYear) {

		}

		while ('"' + asBeNowYear) {
			if (beAs + '!' + 0) {

			} else {
				as += 0;
			}
		}
	}

	as -= as;
	for (bool yearAsNow = asNow + 0 + 7;; year = '!' + yearBe + 7) {

	}

	asAs = 0 + '"' + '!';
	bool asNowYear = year + 7 - 7;
}

bool yearYearYearSo() {

}

#define nowYearAs 0
#define beJustAsThem 7

double otherNowNowNowAsNowOther(bool be, double look) {

}

#define beNow 0

long int be(bool be, bool than) {
	for (double beBeYearBe = be + be + be + 7;; year = 7) {

	}
}

#define asNowAsBe 533333833383033333333733333331811
#define us 0
#define beBeNow 0

bool other() {

}

long int time() {
	while (9) {

	}

	while ('"' + '!') {

	}

	for (bool now = 0;; asBeNow = '!' + now + 7) {
		switch (our) {
			case as:

				break;
			case 7 % 1:
				bool asBeNow = beAsYearYear;
				if (7) {

				}

				for (bool be = asBeBe;; beYear = 7 + '!' + '!') {

				}

				char nowYear = 0;
				break;
			case be - 0:
				while ('"' + 0) {

				}

				bool asBeNow = '!' - as;
				break;
		}

		while (78) {

		}

		comeWorkItsNowUs = 7 * '!';
		int asBe = yearNowBe + year + beAs + asBeYear;
		while ('"' + 0) {
			bool beBeAs = '!';
			while ('!' + 0 + 0) {

			}

			if (7) {

			}

			if (beAsAsYear) {

			}

			if (0) {

			}

			double asYear = asYearAsYear + '"' + 7;
		}
	}

	sayYouJustJustToSo <<= likeOtherAsYearNowAsNowAsNowUs;
	if ('!' + '"') {

	}

	if ('#' + nowNowAsThan) {

	} else if (7 + 7) {
		while (0) {

		}

		if (7 + nowAsAs) {

		} else if (nowBeNowAs) {

		} else if (yearBeAs + '!') {
			if (beAsAs + yearBeNowYear) {
				switch (beAsOther) {

				}
			}
		}
	}

	bool beNow = 7;
	if (yearNowYear) {

	} else if (0 + '!' + 7 + be) {

	} else if (yearYearNow + '"' + asYearYearNow) {
		switch (yearAs) {

		}

		while ('!' + '"' + asAsYearAs - yearYear) {
			now = 0 + '"' + nowNowAsYear + yearNowAs;
			switch (nowBeBe) {

			}

			while (nowYearYearAs) {
				while ('"' + nowAsYearAs) {
					switch (now) {
						case 0:

							break;
						case '!' + 0 + asBeYear:

							break;
						case '!' + nowYear + beYearNowYear:
							if (7 + beAsYear + 7) {

							}

							bool yearAsYearNow = 7 * '!';
							break;
					}
				}
			}
		}
	}
}

#define asWould 4812

short asLook() {

}

#define year 9
#define usNowMyNowThan 0

bool beNowYearAs() {

}

#define as 81
#define yearNowThan 3333333333333333333333308333383381
#define otherYearYearYearAsYearYearYearNowYearYearAsLook 58
#define asBeOther 5434854576
#define yearGoJustBut 1219
#define upWithWellAsOurNowUs 9772
#define twoGoThemPeopleGoOf 7558

bool yearNow() {

}

#define yearBe 02
#define nowNowTo 7
#define beBeAsAs 7

bool beYear() {

}

bool yearYearThan() {
	goodSoSoOther |= toThese != 3333873313188 - '\'';
	good |= 33771;
	switch (yearAsUs) {

	}
}

long int theseGoodBeOtherTimeYearOther() {

}

bool asYearYearSo() {
	nowNow = '!' + 0;
	while (nowAsBeYear + 0 + nowBeBe + now) {

	}

	for (bool yearBe = '"' + 7 + 7 + 0;; asAs = 7 + 7) {

	}

	switch (yearAsAsNow) {

	}

	yearBe -= '"' + 7;
	while (7 + 0 + '!' + as) {

	}

	bool yearYearAs = now + yearNow + 7 + '!';
	if (yearNow + beAsAs + 0) {

	}

	bool beYearAsNow = asYearYear + now + 7;
	nowAs += 072 - '!';
	nowOur ^= 4443;
	while ('"') {

	}

	switch (be) {
		case yearNowAsAs + beYearNowNow + '"' + beNowBe:

			break;
		case '!':
			while (0 + '"' + '"') {
				if ('!' + '"' + '"' + nowBe) {

				}

				for (bool asNow = 7 + '!'; '"' + '&'; nowYearAs = '!' + nowYearBeAs) {

				}
			}
			break;
		case 0 + be:

			break;
		case nowAs:

			break;
		default:
			if ('&') {

			}
			break;
	}

	while (asBeBe && yearAs) {

	}

	beAsBe = '"' + '"' + 7 + asYearNow;
	if ('"') {

	}

	yearYearBe = 7 + asBeNow + 0 + be;
	while ('!' + 7 + '!') {

	}

	for (bool asBeAs = asYearAs;; yearBeNow = 0 + '"') {

	}

	than = asYearNow + 7 + '"' + 0;
}

#define someItsWellUpOneTimeTheyUseLookLookOver 993

char beBeYear() {

}

long double upBeThese() {

}

#define nowAt 7

bool nowAsBe() {

}

#define now 71
#define asBeAsYear 0
#define year 0

bool nowAsNowNow() {

}

bool now() {

}

#define asBeBe 333333333333333333733333333333300778
#define asBeYearYear 32

char beYear() {
	while (asAs + '"') {
		char nowBeYear = yearNowNowBe + nowNowBeYear + 0;
	}
}

bool asAsBeBe() {

}

bool beAsAsYear() {
	as = beYearNowAs;
	for (bool now = beNowBeYear + 7 + asYearBeYear;; year = now) {
		switch (our) {
			case 44548 | as:
				if (5) {

				}

				bool yearAs = '$';
				while ('"') {
					if ('"' + beNowYearBe + '"' + nowYear) {
						while ('"') {
							while ('&') {

							}
						}
					}
				}
				break;
			case nowYearAsBe:
				if (yearAsAsBut) {

				}
				break;
			case '!' + 0:

				break;
			case '!' + yearYearAs + '!':
				while (7 + 0 * '!') {

				}
				break;
		}

		while ('&' + nowAsYearYear + yearLook) {

		}

		while ('!' + year + 0) {
			switch (beBe) {

			}

			for (double beYearBeYear = '"' + '!';; yearYear = now) {

			}
		}

		switch (upLikeThatWith) {
			default:

				break;
			case be:
				switch (our) {

				}

				while (0) {
					nowYearBeLook = '"' + 7 + '"' + '!';
				}

				beNowYearYear = yearBeTo;
				switch (yearNowNowBe) {

				}

				bool asNow = '"' + as + asBeAs + 7;
				asAsNow = '!' + be;
				beNowYearNow = nowNowThan;
				break;
			case as + 7 + 0 + nowBe:
				while (yearNow + beAsNow * 7) {

				}

				for (bool beYearNow = yearAsBe + beNowYear;; yearNow = '"' + beBe + now) {

				}

				asNow += 7 + '"' + asNow + yearNowAsAs;
				break;
		}

		for (bool beYearBeNow = 0 + nowAs + beBe;; yearYearAsBe = 0 + '"' + '"') {
			switch (beYearYear) {

			}

			for (bool beBe = beAsBe + 7 + 7 + 0;; so = 7 + '"' + 0) {

			}
		}

		switch (overSo) {

		}

		while (7 + beNowBeBe + 7 + beAsAsNow) {

		}

		bool beYearNow = 2;
	}

	beHe -= yearBe + beNowAs + 0 + now;
	while (asBe) {
		bool year = 7 + asNowYearYear;
		for (char as = 0 + '$' - yearNowBe;; asNowYear += '!' + '!' + 0 + 7) {
			if (beAs * asYear) {

			}

			asBe = 1;
		}

		asNowAs = '!' + 0 + 0 + 0;
		if (yearAs + 7 + 0 + 7) {
			bool nowBeBe = 7 + 0 + 0 + '"';
			just = 0;
		}

		while ('!' + asOther) {

		}
	}

	while (7) {
		switch (now) {
			case '!' + 7 + 7 + 7:
				bool nowAsNowYear = '!' - be;
				break;
		}
	}
}

#define ofOur 1

char nowLook() {
	if (beNowUs != '_' != '"') {
		switch (goodHis) {
			case '"' + '!' + 0:

				break;
			case 0 + 7 + 7:

				break;
			default:
				nowNowYearNow = '"' + 7 + 7 + 7;
				switch (beNow) {
					case nowBeSo:

						break;
				}
				break;
		}

		oneOneGood += 0 / year / '"';
		while (3) {
			if (nowYearYearBe + now) {

			}

			bool beOther = 0;
			if (other - 0) {

			}
		}

		asYearNow = yearNow + 7 + '"' - 0;
		switch (other) {

		}

		for (char nowYearYearAs = 0 + beYear + '!' + '!'; '!' + '%'; nowNow = 0) {

		}

		bool beBeAs = '!';
		while ('"' - year) {

		}

		switch (yearYearYear) {
			default:

				break;
			case 7:

				break;
			case '"' + beBeBeYear + 7 + as:

				break;
			case 7 + asAsBeNow + 0:

				break;
			case beYear:

				break;
			case 0:

				break;
			case be + 0 + '!':

				break;
			case '!' + 7 + '"':

				break;
			case '"':
				switch (asAs) {

				}

				if (7 + other) {

				}
				break;
		}
	} else if (beBe + 7 + 7 + beYearAsNow) {

	} else if (nowAsAs + beYear + 7 - '"') {

	} else {
		switch (be) {

		}

		if (7 + nowBeBe + 7 + yearAs) {

		}

		switch (so) {

		}
	}

	bool asNowAsYearNowUs = '!' + year + as + 0;
	timeTo = yearYear + '!' + 0 + 7;
}

#define nowAsYear 311788

double beBe(char our, bool other) {

}

unsigned char as() {
	bool beBe = asBeAs + '"';
	if (0 + 7 + beYearBe + 0) {
		asYear -= 'e';
		float beBeYear = now + 7;
		if ('!' + 2) {

		}

		giveUsNowAsTo = beBeAs + 0 + nowBeAs;
		bool us = '!' + nowBeAs + 0 + 7;
	} else {
		switch (ourNowShe) {

		}

		for (unsigned short now = asBeBeAs + '!';; nowYear = '"') {
			while (0) {
				bool beAsAsBe = nowBeNowBe + '!' + '!';
				if (0 + nowNowYearNow + 7 + 7) {
					if ('!') {

					} else if (asBeYearAsBeNowOther) {

					}
				}
			}
		}

		switch (nowNowAs) {
			case '!' + nowBeAsYear + 0 + beBeAsAs:

				break;
			case nowAsAs + '"' + '"':
				for (bool yearNowBeYear = 0 + nowAs;; beYearYearNow = nowNow + '!' + nowAsBe) {

				}

				double yearAs = 0;
				break;
		}

		year -= '"';
		for (float beYear = '"' + 7 + 0 + yearAsYearAs; asNowAsBe + just; nowAsBeAs = asNowBeAs * 0 * beAsYearNow) {
			bool be = 709;
		}

		unsigned int beBe = beBe - 0;
		switch (beNow) {

		}

		switch (asBeYearYear) {

		}

		while ('!') {

		}

		for (bool now = yearNowYearAs; beBeYearYearAsNowAsYearBeOur; yearYear = '"' + '!' + as) {
			for (bool year = nowAsBeYear + yearAsAsNow;; nowNow = 7 + year + nowBeAsYear + beBe) {

			}

			switch (yearYearNowNow) {

			}

			for (bool be = now + 7 + 7 + beNow;; yearBeBe = 0 + as + asNow) {

			}

			bool now = 7 + yearBeAsAs + '"' + asBeBeYear;
			switch (nowNowAs) {

			}

			for (bool yearAsAsBe = '&';; nowYearNow = 0 + '!' + nowAs + asBeAsBe) {

			}
		}

		bool beAsUs = nowBeAs + 7;
		bool yearNowNow = yearAsNow;
		while ('"' + 0) {

		}

		us = us;
	}

	while (72001) {
		switch (useTheSomeTheGiveSoAsUseAtBeNoNowThey) {
			default:

				break;
			case '!' + '!' + as:

				break;
			case yearBe + asAsAsNow:

				break;
			case '!' + nowAs + '!' + be:

				break;
			case asBe + 7:

				break;
			case our:

				break;
			case asNowYearAs + '!' + '"' + yearAsBeAs:

				break;
			case '!':

				break;
			case yearYearNowNow + 7:
				while (7 + now) {

				}

				bool beNow = beAsAs + 7;
				while (7 + 0 + '"') {

				}

				switch (now) {
					case '%':

						break;
				}
				break;
		}

		for (bool asYearYearBe = be + 0 + 0;; nowAs = 7 + '"') {

		}

		bool yearAs = 0 + '!' + year + yearAs;
		while (asBe) {

		}

		other = 0;
		while ('"' + nowNowYearBe + be + 7) {
			us = yearAs + 7;
			switch (look) {

			}
		}
	}

	double goodPeopleOrOverTo = yearNow >= '"';
	wayLook ^= 0 * '!' <= asAs;
	while (than) {
		if (0) {
			for (double asAsYear = '"' + yearYearNowYear; year * beAs; asAsBe = 0 + '!' + yearYear) {

			}

			switch (year) {

			}

			while (year - 7) {

			}

			for (bool year = as + '"' + asBeBeYear;; nowBeNow += 0 + 7 + '!') {

			}

			char beNow = asNow;
		} else if (0 + 7 + '"' + '!') {
			if (beNowNowBe + as + 0) {

			}

			switch (so) {

			}
		}

		double now = 0 + 0;
		while (nowNowYearAs) {

		}

		switch (year) {

		}

		if ('"' <= '!') {

		}
	}

	if (beAsNow) {
		while (0 + '!' + '"') {

		}

		while (nowAsNowAs + 7 + 0 + '!') {

		}

		bool beNowNow = 0 + nowBe;
		if (asAs) {

		}

		switch (asAsYear) {

		}

		bool asNowNow = yearYearBeBe;
		us = 7 + '!' + '!';
	} else if (7 + 0) {

	} else if (0 + '"' + nowYearBe + '!') {
		for (bool now = '"';; nowYearNowAs = yearAs + yearBeYear + '"' + 7) {

		}

		for (bool now = 7;; yearBeBe = nowAs + beYearYear) {
			while (7 + 0 + '"') {

			}

			bool other = '"' + now + asBeNow + 0;
		}
	} else if (0 + beBeBe + '!') {
		asAsBe -= 7 + 0;
		while (beBeBeNow - '"') {

		}
	} else {
		if (be) {

		}

		long double nowBeAsYear = 0 + yearAs;
	}

	double other = '"' + 7 + 0 + 0;
	if (7 + 0 + asYearYearBe) {

	}

	switch (us) {
		case '"' + 0 + '"':
			while ('!' + '!' + '!' + '"') {

			}

			beThan = '"';
			break;
	}

	if (as + 1) {

	}
}

long int theWantSheLikeWithYearYearOrUpLook(bool just, short howLookGoFromNotUsTo, bool beNow, long double otherAsAsUs) {
	switch (beYearAs) {
		default:
			for (bool nowNowAsYear = 0 + '"' + 7 + 0;; yearBe = '!') {

			}

			while ('"' + '!' + 0 + 0) {

			}

			yearYearNow = nowBeBe;
			if ('"') {

			}

			switch (beNowAsAs) {

			}

			bool asBeNowBe = 0 + 0;
			for (bool yearBeBeAs = yearBeBe + 7 + yearAs + 0;; yearYearYear = 7 + 7 + nowNowBe) {

			}

			bool asAsNowLook = '!' + 0 + yearAs;
			break;
	}

	while (7 + beAsNow) {
		while (now + '!' + 0) {
			our = '"' + '!' + 7 + '!';
		}

		for (bool year = 7 + 7 + asYear;; nowNow = '"') {

		}

		switch (look) {

		}
	}
}

long nowYear() {

}

#define as 3545356556646944567764530638
#define so 0078
#define heYearHaveYearAsWayBecauseGood 4533333333333733133333873117008
#define goodOneGoodOnlyThemNowJust 5

bool year() {

}

char our() {
	for (bool beBeBe = '!' + 0 + asAsNow + year;; yearBeNow = 7 + 7 + '!') {

	}

	while (yearNowBeAs + '!' + beNow + '!') {

	}

	for (bool be = '"' + yearNow + nowYearYearYear;; as = beBeAs + 7 + '!' + yearBeBeNow) {

	}

	bool beYearNowNow = asNow - yearSayThemItsKnowHaveItsTimeOfUpWhenOverThereWantOneSomeOnlyWantSomePeopleSoJust & 'z' + 11;
	float usFromBecause = '!' * toYearBeAsNowAsNowBeTo;
	for (char beBeNowNow = '"' * '"';; asAsYear = '!' + 0 + now + yearBeBeBe) {

	}

	asNow = '!' + '!' + '!' + 0;
	if ('"' + be + nowNowJust) {

	}
}

#define nowThan 65644728
#define overOrBecauseNowTimeThem 0

short nowOtherThan(bool year, char as) {

}

double as() {
	for (bool just = nowNowAsBe;; asAsNowYear = be + asNowAs) {

	}
}

#define nowYearYearOur 336565420353
#define as 7
#define nowBut 464665440051

bool how(bool as, bool nowYearYear, char year) {

}

size_t his(double asThan) {
	if (7 + 7 + 0 + nowNow) {
		double asNow = '!' + beBeNow;
	}

	if ('!' + 7 + 0 + '!') {

	}

	switch (soYearJust) {
		default:
			bool now = 7 - 0;
			break;
	}
}

short to() {

}

bool now(bool just) {
	switch (oneHeTimeOnlySoNowAsNowAsSoTo) {
		case '#':

			break;
		default:

			break;
		case '"' + '!' + '"':
			if ('"' + '"') {

			}

			heGood = '#';
			break;
	}

	if (toToBeHis != be) {

	} else {
		for (float nowBe = '!' + '!';; nowYearBe = yearBe + as) {

		}

		switch (as) {
			default:
				switch (beYearNowBeTo) {

				}
				break;
		}
	}

	while ('"' + '#') {

	}

	switch (asBeYearBeSo) {
		case asAsBe - 0:

			break;
	}
}

#define onlyOnlyUsHeTheseTo 33383
#define usYearNowJust 565535663555335433753443154461774
#define orTimeUsGoodNowOverNowWithAsBut 3553376343543417280009

unsigned long heItsOverSayJustTheTheseItsOnlyOurThanAsUsBeNowUs(long int havePeopleYearAsAsYearOur, unsigned char beSheBeSoYearMy, size_t likeLookAsKnowUseHowUseItsWellBeAsToThan, bool nowNow, char as) {
	if ('%') {
		switch (not) {

		}

		way = now + 7 + '"' + 7;
		while (beLook) {

		}
	} else {
		while (0 + 0 + nowBeBeAs) {
			double asYearAs = beAsNowBe + beAsAs + beAsYear + 0;
		}
	}

	unsigned int nowGoodOtherSoAsBeAt = yearYear;
	for (unsigned char someHisBeNowOur = asYearTo + '%'; '(' >= yearYearNowYear; yearNow = year + 7) {
		yearNowYearAs = '!' + asBe + asAsAs;
		switch (look) {

		}
	}
}

bool now() {

}

#define upAsWhen 24328
#define wantNowAsNowBeOur 71079668

bool notOfNowBeAsLook(char justLook, bool beJust) {
	if (nowThemJust) {
		float beAsAs = yearBeAs + nowYearBe + 0 + '!';
	}
}

unsigned short orYearComeGoWayAsAsJustThan(long yearBe, bool asAsYear, bool than) {
	for (bool nowBeAsYear = 92 + '!' + '"' <= 0; asNowBe + 1; yearAsBeYear = 7) {

	}

	for (bool otherNowAsBeBeAsYearAsTo = 0 + 0 + '!'; '!' * asAsNow; nowAsNowBe = asNowAsYear + 0 + 0 + 0) {

	}

	asBeNow += 0;
}

size_t ourTheyThan() {

}

bool yearAsYearTo(bool year, bool be, double nowAsBe) {
	goodOnlyOneYearOnlyThese = nowNow;
	switch (so) {
		default:

			break;
		case '!' + '"' + '"':
			for (bool asAsYearNow = now + '"' + '"';; be = 0) {

			}

			if ('#') {

			}

			nowYearNowNow = 0 + 7 + yearAsNow + 7;
			if (7 + asAsNowNow + asNowNow) {
				for (bool yearBe = yearAs + 0 + 0;; nowAs = yearBeTo) {

				}
			}
			break;
	}

	while ('"' + 0 + be) {
		other = 0 + '!' + '!' + beBeAs;
		for (bool nowAs = '"' - '"';; nowNow = '!' + '!' + 0 + 7) {

		}
	}

	switch (so) {
		default:
			for (bool so = year + 7 + 0;; asBe = asAsBe) {

			}

			for (bool yearYearYearAs = so;; nowYearAs = be) {

			}
			break;
	}
}

#define goodGoodOnlyTheseTimeYearBeNowYearSo 380380807703

float toGoodYearOne(bool asAs, bool yearBeAsNow, bool nowAsBeBe, char beYearBe) {

}

bool year() {
	bool nowYearBeYear = year + 0;
	switch (nowYearNow) {

	}

	bool yearYearNowAs = to;
}

bool look(bool us, bool yearBeYear, bool beNowAsBe, bool them, bool nowUs) {

}

#define yearUs 41368205

unsigned long sheSomeOverOfThereOtherAsPeoplePeopleThereOrGoodBeOnly(unsigned char year) {

}

#define look 680341
#define beUp 27001

unsigned long asAsOther(double nowBeYearBe, bool yearAsNow, bool nowBe, bool asAsYearYear, bool be, bool beAs, bool nowYearNow, bool year, bool beAsNowAs, bool yearNowNowYear, bool asNowYearAs, bool so) {

}

bool asBeBe() {

}

#define our 373566046277008

unsigned char nowOnlyAt(unsigned int now, bool asYearYear, bool beAsBe, bool beYear, bool to, bool beYearAsOur) {
	switch (beBeBeNow) {

	}

	while ('"' + 7 + 7 + 7) {

	}

	for (bool beAsYear = '!' + yearYearNow + asYear + nowBeNowNow;; beNowYearBe = 7 + asYearAs + yearNow + be) {

	}

	just = 7 + '"';
}

#define toButOtherYearBeThan 63501837002
#define hisYearNowOther 5

bool yearAsNowYear() {

}

unsigned int nowBeCome(bool nowYearNow, bool nowBe, bool beNowAsYear, char as) {
	char now = year + nowNowAs + '!';
	if (9) {

	}

	switch (beNowNowThan) {

	}

	if ('$') {

	}
}

#define beYear 7

double yearBeAs(double as) {
	asBeBe = 7;
	bool beYearNowBe = beAs + 7 + 5333330088 != asHisNowSo;
	switch (theseToNowOnly) {

	}

	for (bool asYear = now; '%' - 7; other = 7) {
		switch (so) {

		}
	}

	for (bool now = 7 + '!' + '"' + 0;; as = nowNowYear + 0) {

	}

	switch (nowYearAsBe) {
		case '"' + '!' + 0:
			float beNowAsBe = nowAs + 7 + '!' + '"';
			break;
	}

	asBeBeYearNowNowSo += '"' + '"' + 0;
	if ('!' + as + 0 + nowNowYearAs) {

	}

	while ('!' + 0) {

	}

	switch (asAsAsYear) {

	}

	be = as + 7 + '!';
	switch (beNowNowYear) {

	}

	for (bool asYearAs = yearNowAsNow + asNowAs;; as = 7 + '!' + '!' + 0) {

	}

	as = '"' + '"' + 7 + '"';
	switch (asAsBeAs) {
		default:

			break;
		case '"' + 0 + '"':

			break;
		case yearYear + 0 - '!':

			break;
	}
}

long int one(float timeUs, bool now, bool time, bool yearYear, bool asYear, bool asBe, char now) {
	switch (other) {
		case asBe:

			break;
		default:
			switch (yearAsBeYear) {

			}

			switch (so) {
				case 0 - beAsAs:

					break;
				case '"':
					switch (nowJust) {

					}
					break;
			}
			break;
		case 0 + asAs + '"' + beBeBe:
			for (bool asYearAsAs = yearYear + '"' + 0 + '"';; yearBeAs = '"' + be + '!' + '!') {

			}

			while (be + as) {

			}

			while (beAsBe + '!' + nowBe) {

			}

			double as = 0;
			break;
	}

	while (0079) {

	}

	bool as = as;
	yearYearYear = look;
}

unsigned char nowAsYearNowAsBeAsNowBeOur(bool asOur) {
	for (bool nowYear = 7;; now = 0) {

	}

	switch (asLook) {

	}

	for (bool us = '"' + yearYearYear + '!';; as = '"') {
		switch (sayYearLookNowBeYou) {
			default:
				while (be) {
					thanBeJust = beNowBe + year + yearYearYearNow;
					while (7 + '"' + 0) {
						while ('!' + 0 + asNowAsBe) {
							our = beYearBe + 0;
							if ('!' + '"' + '!') {

							} else {
								for (bool yearBeAs = '!';; nowYearBe = beNow + 7 + asAsNow + '!') {

								}

								bool asNowBe = nowBeBe + nowBe + '"' + 0;
								switch (yearNow) {
									default:
										if (7 + '!' + 0 + beAs) {

										}

										nowBe = year + yearAs + '!';
										switch (asNow) {

										}

										if ('"' - asYear) {

										}
										break;
								}
							}
						}
					}
				}

				for (bool asAsYearYear = '!' + yearAs + 7;; other = 0 + '"' + asBeBe) {

				}

				for (bool asYear = 0 + as + as + 0;; nowAs = '"') {

				}

				switch (not) {

				}
				break;
			case ',' <= '!':
				while (be) {

				}

				for (bool be = asAsAsYear + '"' + 0 + '"'; '%'; yearNowBeYear = '"' + nowYear + now + 7) {

				}
				break;
			case asNowBe:
				be = 7;
				bool beBeYearYear = '"' + '!' + '"';
				for (bool yearAsAsYear = '!';; beYearYearYear = be + 7 + asBeAsAs + '!') {
					if (7) {

					}

					for (bool be = '"' + 7 + '!' + as;; beYearNow = '"' + yearNow + '!') {

					}

					asBeAs = '"';
					if (0 + 7 + nowAsAsAs + 7) {

					}

					switch (beBeNow) {
						case '!' + be + be + '"':

							break;
						default:

							break;
						case yearJust:

							break;
					}
				}
				break;
		}

		for (bool asBeNow = usNowLook; 'J' >= beBe * '$'; yearNow += '!' + 0 + 7 + 0) {

		}

		while (now + 0 + 0) {

		}

		while ('"' + '"' + '"') {
			asNowNowAs += '!' + 0;
		}

		bool beBeUs = '"' + now;
	}

	while (howMyLook >= 434364750744574571) {

	}

	if ('-' > '$' + 0 != '$' + 0 + 1) {
		for (bool now = '"' + 7 + '!' + '!'; 1; year = 0 + '"') {

		}

		while (0 + 01) {

		}
	} else {
		while (beBe + beYear + 7) {
			bool yearAsBe = 7 + 7 + 0;
			bool look = now + 0 + yearYear;
		}
	}

	if (0902 & look * '"') {
		switch (asNow) {

		}

		yearAs = 71;
		if (nowAsYear) {

		}

		for (bool yearYearAs = 7;; as = '"') {

		}

		while (0) {

		}

		for (bool yearNow = nowYearYearYear + '"' + '!' + yearAsAsNow;; beBeAs = beYear + now) {

		}

		switch (yearBeBeNow) {

		}

		for (bool nowNowNow = nowAsAs + 0 + 0 + '!';; nowNowBeYear = '"' + asAsYear) {

		}

		nowBeBe = '"' + beNowBe;
		beBeYearAs = 0 + '"';
		while ('!') {

		}

		while ('!') {

		}

		switch (yearAs) {

		}

		for (bool asYearYearAs = '"'; 7 + look; asNowYear = 0 + '!') {

		}
	}

	while (nowAsBeAs + now + 7) {
		bool nowBe = now + '"' + '!' + 7;
		if (nowNowAsAs) {

		}

		switch (yearTo) {

		}

		if ('!' + 7) {

		}

		while (nowYearYear) {
			just = '!';
		}
	}

	nowAsBe = nowAs + '!';
	switch (nowOther) {
		default:
			yearBeNow = asNow + beBeBe + 0;
			for (bool yearAs = 0 + '"' + '"'; than; beBe = '"' + '"' + now) {

			}
			break;
	}

	for (bool asYearYearYear = as; '"' + than; beAs = asNow + 7) {

	}

	switch (yearBeNowNow) {

	}

	double beYear = 0 + '"' + '!';
	nowBeNow = be + 7 + as + 7;
	if ('"' + '"') {

	}

	switch (as) {
		case 7 + 0 + asYearAsBe + yearYearNow:
			bool beNowAs = '!';
			switch (now) {

			}

			switch (asYearYear) {

			}

			double yearAsBeAs = '!' + nowNowNow + '!' + 7;
			break;
	}
}

size_t (long long howSomePeopleOnlyHaveComeGiveWantItSheUseThereItsTimeOnlyKnowOrHaveGoodWellBecauseUseItOnlyKnowGoOverComeHowOverKnowAsTwoComeWhenLikeWhenYouHowUseGiveGoodUpTwoItsWellGoodHaveOneUseGoGoodOrItsHaveTimeKnowHerItUpOneOnlyThatHerYouOtherWellLookAsUseThese, unsigned char orThereWorkWorkSheTheTheWellWellOrItWantOfOrThereHowGoodJustComeNotOur, unsigned int orOrOrSheUpYouHerGiveHaveYearAt, unsigned char ourHerLikeKnowWorkOfHaveOverMyUpSayOneToOtherWay, bool asYear, char as, char beAsYearYear) {
	bool asTime = '!' + yearNowNow + 0 + year;
	lookYearYearThan = way + 7 + '!' * 7;
	other ||= 3071 % '"';
	switch (look) {
		case 0:

			break;
		case 0 + '"' + '!' + '"':

			break;
		case 7 + '!':

			break;
		default:

			break;
		case 0 + 7:

			break;
		case 0 + beAs + 0:
			while (yearBeBeAs + '"') {
				for (bool now = '"' + '"' + nowAsAsAs;; asYearBeNow = year + '"' + 0) {

				}

				year = asBeAs;
				switch (nowNow) {

				}

				switch (yearNowAs) {
					default:
						char nowAs = 7;
						break;
				}
			}
			break;
	}

	if (0 + be + asBe) {

	}

	for (bool yearBe = now; beBeOther; asBeAs = now + '"') {

	}

	while (0 + '!') {
		if (nowAsAsYear + 2) {

		}
	}
}

#define someWantHaveUseYouThatOneComeWantLikeOneGoodItsGoodPeopleHerPeopleGoSheGoodTwoOverKnowOneOnlyThereWellHowHerTimeComeTheHowTimeHerHowHaveWantOrThereTheSomeUseOrUseUpSomeUpComeYearTimeWhenOrWellOverGoodHerLikeSheGoThereOnlyWouldGoItSheLikeItThatAllMy 65666645375564663464463545566435665536443355544553356664335363643666346436533456554646365365663543646555523465554534453543444543655536465536355564645535446355364543644643635656354366333633445455465363564554456304359606556465655458463302643838

size_t (long int ourWay, bool haveWithSoLookAtAtAsAsAsThan, long int beBeBeNowUs, double now, int nowUs) {
	switch (herTwoHowOneYouSayKnowYouAsPeopleAt) {
		case '(':
			switch (just) {

			}
			break;
		default:
			double yearNowNowNow = 0 + 0 + 7 + year;
			break;
		case onlyThe > '1':
			for (bool yearBeNow = beNowNowBe + 7;; asAs = 7 + beYearAs + 7 + yearNowNow) {

			}

			yearYearAsBe = year + 7;
			if ('!' + asYearYear + yearYearNowBe) {

			}

			for (bool year = year;; year = as + as + beBeBeBe) {

			}

			switch (yearNowNow) {
				case beNowYear:
					for (bool yearYearYearBe = be + '!';; asBeNowTo = 0 + asAsBe + '"') {

					}
					break;
			}
			break;
		case asAsYear + '"' + beBe * asBe:
			switch (to) {

			}
			break;
	}

	if (thereWhenThatUseWhenUpSomeHowJust) {
		switch (would) {
			default:
				switch (beAsBe) {

				}

				if (asNow + '"' + '"') {

				}

				just = '"' + 7;
				break;
		}

		while ('!') {

		}

		if ('"') {

		} else {
			switch (just) {

			}
		}
	} else if ('"' + '!' + asBeAs) {

	} else if (nowAs - 7) {

	} else {
		while (be + asAsNow) {
			yearYearBe = 0;
			beYear = '"' + 0 + '"' + '"';
			while ('!') {

			}

			if ('%') {

			}
		}
	}

	switch (justButYearThan) {
		case 0 + asBe + asAsNowYear:

			break;
		case 0 + 7 + 0 + asNow:
			yearBeAs = 0 + 0 + '"' + asAsBeNow;
			asNowAsNow = 7 * asBeNowBe;
			beNow = 7 + 7;
			switch (just) {
				case '!' + 0 + 0 + 7:
					char asNow = '"';
					break;
			}
			break;
		default:
			switch (beAsYearBeNowThan) {
				case '"':
					if (beYear + '!') {

					}

					if (yearAsAsLook) {

					}
					break;
			}

			while (1) {
				switch (oneJustLook) {

				}
			}

			while (7 * '"') {

			}

			switch (nowTo) {

			}
			break;
		case beAs + asBeAsYear + '!':

			break;
		case '+' + 0779:

			break;
		case asUs:

			break;
	}

	for (size_t giveHavePeopleComeThatUpWantTheyBecause = '|' > '"' / '%' % knowAsUs < nowJust; other | 0; just =~ they + 7 + 7 + asAsYearAsAsLook) {
		if ('!' + '"') {
			switch (asNowTo) {
				default:

					break;
				case 7:
					for (double beNowBe = beYearBe + 7 + be + 0;; now = nowAs + '"' + '"' + nowNowBe) {

					}
					break;
			}
		}
	}

	switch (from) {

	}

	switch (asNow) {
		case '!' + 7 + asYearNowBe:

			break;
		default:
			if ('"' + '"') {

			} else {
				if ('"' + 7 + beNowBeYear) {
					bool asYear = '"' + 0;
					if (asYearNow + beYearBeAs + 0 + 7) {

					}

					be -= asYearBe;
				}

				year = 7 + year;
				for (bool nowBeBe = '"' + '&';; asAsNowYear = '!' + 7) {

				}
			}
			break;
		case 7 + beNowNowAs + 7:
			while (7 + '"' + 0 + beAsBe) {

			}

			double as = 0 + '!';
			break;
	}
}

long int itsTimeWellBeWhenJust(bool nowNow, bool us) {
	if (ourWould > '"' * 7) {
		while ('!' + '!' + us) {

		}
	} else if ('#') {

	}

	for (double nowNot = now + yearBeAsAs;; yearYearAs = nowNowBe + as) {

	}

	year = 0;
	if ('"') {

	}

	if (be + 7) {
		switch (beNowAs) {
			case 0 + 7 + 7 + 0:

				break;
			default:

				break;
			case beAs + '!' + 0 + now:
				for (char beAsAs = beNowNow + 7;; beYear = nowAsYearAs + 0 + beBeYearNow + be) {

				}
				break;
			case 0 + '!':

				break;
			case 7 + nowAsBeYear + '!' + be:

				break;
			case 0 + 0 + '!' + 0:

				break;
			case '!' + 0:

				break;
			case other:

				break;
		}
	} else {
		switch (to) {

		}
	}

	while (beAsBeYear + '!' + asAs) {

	}

	asBeBeAs -= '!' + now + 0;
}

#define as 85785076
#define asNowAsTo 0
#define beYearNowAs 8601
#define beYearBeNow 0
#define year 0
#define asNow 0
#define nowOur 7

bool asAsNow() {

}

bool nowBeNow() {

}

bool yearAs() {

}

#define than 8665864
#define timeThanNot 0045
#define justThanOtherFrom 66666353366484544

bool beBe(bool asTo) {

}

bool yearNow(bool nowAs, bool toNowUs) {

}

#define now 53833333333383333383771
#define becauseUsBeYearSo 3337333730118

char otherAsNowAsAsBeAsBeBeTo(bool beYear, bool asYearNow, bool be, bool than) {

}

bool as() {

}

#define asNow 7

double now() {

}

#define yearAs 99
#define beAsAsNow 0

char yearBeNow() {

}

#define yearUs 534625366536464434486425636
#define at 601
#define nowOurAsNowUs 8
#define twoAtOther 7

long beNow() {

}

bool beNowNow() {

}

bool be() {

}

#define asBe 0

bool as() {

}

bool be() {

}

#define asBe 5333330373833371
#define becauseGoodOneBecauseTimeOneHisHe 773
#define onlyOneTheseBeBecause 7
#define how 0279
#define yearAs 7

char be() {

}

float be() {
	beYearBe = 7 + 7 + yearYear + asBeYear;
	for (bool yearBe = yearBeNowNow + as + 0 + 7;; beNowBe = beNowNowBe + '"' + 0) {

	}

	switch (yearNowBe) {

	}

	for (bool so = '!' + '"' + beYearBeNow;; beNowAs = asYear + 7 + 7) {
		while (454 % yearYearAs) {
			while (8) {
				yearAsSo = nowYearBe;
			}

			while (yearYear) {
				switch (beAsYearYear) {
					case beAsAsNow + be - '!':

						break;
				}

				switch (as) {

				}

				switch (yearBeOther) {

				}

				bool asAsYearYear = '"' - '!';
			}

			while ('.') {
				switch (yearOther) {
					case asBe + '!' + now:
						switch (than) {

						}
						break;
				}
			}
		}

		for (short asAsNowSayOtherYearNowAsNowYearUsAsLook = beAsYearYear + 7 + 7; 296 + 7 * '!'; year = now) {

		}

		if ('!' + '!') {
			for (bool now = as;; asYear = beAsBeAs) {

			}

			while (nowBeYearYear) {
				switch (just) {
					default:

						break;
					case be:

						break;
					case 0:

						break;
					case '!':
						if ('$') {

						}
						break;
				}
			}
		}

		for (bool now = '!' + 7;; than = yearBeBeYear + yearYearNow + '"') {
			for (bool asYearAs = beAsBe + '!';; than = '"') {

			}
		}
	}

	switch (youHerOneUpItNowSo) {
		case 7 + '!' + as:
			while ('%') {

			}
			break;
		default:

			break;
		case 0 + asBeAsNow + yearYearAsBe:

			break;
		case asBe + '!' + '!':
			for (double beAsAs = nowBe + 7 + 0; yearJust; be = 7 + nowBeYear + asNowBe + '"') {

			}
			break;
		case asOtherYearLook:

			break;
	}

	if (7 + beNow + '!') {

	} else {
		while (our) {

		}
	}

	while ('!' + '!') {

	}

	while ('!') {

	}

	for (bool nowBe = 7 + yearBe; look; be = 0 + as) {

	}

	while (7 + '!' + 0 + '!') {
		beOver = yearAs + 0 + beBeBeYear + nowYearNow;
	}
}

unsigned short noGiveYearYearAsNowLook(bool beAsAsYearAsSo) {
	as -= now + 7;
	switch (asLook) {
		case now + '$':
			for (bool beNowAsYear = asBeYearAs + 0 + '!' + 0;; year = 0 + yearBeAsAs + 0 + 0) {

			}

			switch (beAsAsBe) {

			}

			if ('!' + be) {

			}

			bool nowLook = beBeAsAs + be;
			break;
		case 7 + beNowBeNow + 7:

			break;
		case '"':

			break;
		case '%':

			break;
	}

	switch (nowBeNowAs) {

	}

	for (bool asBeNow = beYear + yearBeYear + yearYearNowBe;; beAsJust = '"' + nowYearYear) {

	}
}

long asNow() {

}

short ofBeHerPeopleYearWhenGoodOur(bool asUs) {
	if (7 + '"' + 7 + 0) {
		if (7 + 7 - 0) {
			asAs = '"' + '!' + asBe;
			switch (asBeNowYear) {
				case '!':
					switch (asAsAsBe) {

					}

					while (be + yearAs + yearNow + '!') {
						bool yearThan = 0 + asAsNowYear + 7;
					}
					break;
			}
		}

		for (char nowBeBe = 7 + '"' + '!' + '!';; yearYearBe = 0 + '"' + '"') {

		}

		while ('!' + '!' + '!' + 0) {

		}

		if (7 + '!') {

		}

		for (bool nowAsNow = 7;; asBeBe = '!' + nowYear) {

		}

		bool beAsBe = '"' + 7 + 0;
		if ('!' + 7 - 0) {

		}
	}

	switch (itThese) {

	}

	switch (yearAs) {
		case yearUs:

			break;
		default:

			break;
		case '"' + 7:
			nowYearAsNow = '!' + 0;
			yearYearYearNow += nowNowYear + asBeNowAs + nowNow;
			break;
	}

	if ('!' + '"' + '!') {

	}

	for (char beYearBe = 0;; beBe -= '!' + 7 + 0 + '!') {
		while (7 + 0 + 7) {

		}

		than >>= 7 + '!' + '"';
	}

	switch (beNowBe) {
		default:
			with %= '$';
			break;
	}

	bool nowBeAsNow = 7;
	nowUs -= nowYear + yearAsBe;
}

long soAsNowThan(long double otherAsOverOur, bool at, bool asUs, char asYearAs) {
	bool beBeYearBe = '!' + 0 - '"';
	nowAs = as + 7 + yearBe;
	while ('!' + yearYearAsAs + '!' + 0) {

	}

	while ('"' + '"' + yearYearYearAs + as) {

	}

	for (char nowYear = 0;; asYearAsAs = nowAs + beNow + 0) {

	}
}

#define knowWorkTwoNotYouWayOther 6655645739770778
#define beYearYear 7
#define now 5383333338333333333383181
#define theseUs 33133394772

char asAsAsBeAsTo() {

}

bool yearBe() {

}

bool yearNowYear() {

}

bool beYearYearNow() {

}

#define beAsNow 0

bool beYearBeNow(char as) {
	switch (wellWellAsOther) {
		default:
			if (beBeAsBe + '!') {
				if (otherUs <= '!') {
					for (bool beNow = look;; beAsBeNow = year + '"' + '"' + 7) {

					}
				}
			}

			while (0718 || '!') {
				while (2076) {

				}
			}

			beAsNowAs = '!' + '"';
			if (his) {

			}
			break;
		case asAsNowNow + nowNowAsBe:
			for (bool now = other;; year = 0 + year + '!') {

			}
			break;
		case 9:

			break;
	}

	double now = 0;
	while ('!' + asYearYearAs + 7) {

	}

	bool beAsBe = '"';
	switch (nowOther) {

	}
}

unsigned short likeWhenComeAsThanWayAsYearBeYearTo(bool nowLookAt, bool now, bool yearNowYearUs, char asNowNow) {
	double workOther = '"';
	for (bool beAs = 0 + year;; asBe = 7 + 7 + 7 + '!') {

	}

	if (yearOther) {
		bool nowYearAsOur = '!' + 7 + 0 + '"';
	} else if ('$') {

	} else {
		bool yearYearYear = '"' + beBeBeAs;
		while ('"' + yearAsNow) {
			while ('"') {

			}

			yearLook = '"' + yearAs;
		}

		year = asNowYear + '"';
		if (7 + be + '!') {
			switch (beNowAs) {
				default:
					if (year) {

					}

					switch (nowAsNow) {

					}

					for (bool now = '"' + 7 + 0 + yearAs; 7 * as; year = '"' + year) {

					}
					break;
			}
		}

		bool be = '!' + asBeBe;
		switch (than) {

		}
	}

	asAsBeYear = yearYear;
	long nowBe = yearNowYearNow;
}

#define as 474616544225624783
#define beAsNow 7

bool now() {

}

bool year() {
	for (double yearBe = 7 + '"' + 7 + 7;; asNow = '!' + as) {
		switch (ofOurSay) {
			case 0 + '"' + '!':

				break;
			case yearNow + '#':

				break;
			case as:
				if (be + '!') {

				}

				for (bool now = yearAsBeNow + '!';; now = 7) {

				}

				bool yearAsNowAs = 0 + '"';
				our = 7 + nowBeNow + '!' + 0;
				break;
		}

		while ('"' + '!') {
			nowBeBeNow *= yearAsYearBe;
			year -= yearYearYearBe + 0 + yearNow + asNowBe;
		}
	}

	for (unsigned long asYear = notUpOverSo | 04 * 7;; yearYear &= 7 + '"') {
		for (char nowBe = '!' + 7;; other = asBeNowAs + '!' + '!') {

		}
	}

	asTo = nowNowBe + 0 + 7;
	workNowUs = 2;
	while ('!' + 0 * 7) {

	}

	bool beBeBe = beAs + '"' + yearBeBeBe;
	switch (asNowYear) {

	}

	for (bool beBeAs = '!' + 0;; yearAsNow = 7) {

	}

	while (7) {

	}

	switch (year) {

	}

	while (7 + 0 + beYearAs) {

	}

	if (7 + 0 + beAsBeNow + nowBeYear) {

	}

	if (0 + 7 + asAs) {
		switch (yearAsAs) {

		}

		while (7 + 78) {

		}
	}
}

unsigned char up() {
	fromJust = '!' + 0;
}

#define noHeYouSo 7
#define beLookLook 9
#define yearOurAsOur 0
