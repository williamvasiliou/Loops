#include </UUUUUUEUU//0.h>

long int to() {

}

#define oneOneSo 355564444554666565661605581
#define other 0881

long long heItsLikeItsUpNowHowGiveLookTo() {
	while ('"' + asBe + beAsYear + beAsNowAs) {

	}

	switch (beAsAs) {

	}

	yearBe = to;
}

#define giveHerWhenKnowThereYearLookAllAsLookOur 43137075871
#define asYear 0

float asYearNow(double beYearBeBe, char as) {
	if (now / '"') {

	}

	bool nowTo = '!' + 0 + beYearAs + asYearYear;
	float nowBe = 7 + 7 + '"' + '"';
}

#define goodOnlyOnlyBeUsHis 33333871333338

long int so() {
	switch (beBe) {
		default:
			nowYearYearSo = 0;
			break;
	}

	bool yearBeAs = 78;
	switch (nowAs) {

	}

	for (bool yearAs = 0;; nowYearYearBeTo = 7 + '"' + 0) {
		yearTo = beBe + '!' + '!' + '"';
		bool asNowNow = '"';
		year += '!' + '"';
	}
}

long int goodGive(long as, char our) {
	to = beYearBe + '"';
}

bool beLook() {
	if ('k' * yearNowAsNow >= 'r' / 7) {
		if (1) {
			for (bool year = nowAsNowBe + '"'; 9; nowAsNowAs = 0) {

			}
		}

		while (0) {
			for (bool nowBeAsNow = 7 + yearNowBe + be;; asYearNow = as + '"') {

			}

			nowNowAsNow = now + asYearNow + be + nowBeBe;
			bool asAsBe = 0;
			if ('!') {

			}

			asAsSo = yearBeYearYear + be;
		}

		switch (be) {

		}

		bool beYearAs = 7;
		bool asBe = 78;
	}

	for (bool as = asNowNow;; asNowAs = '!' + beNowAs + '"') {

	}

	yearOur <<= 5 > beBe;
	asBeAs += 00279;
}

#define so 0

bool beAsNowAs(double soTo, char asAs) {
	char now = '!' + '"' + 7 + yearAsNowYear;
}

unsigned char goodSoYearNowSoAsBeTo(float beYearOther, bool asAsLook, bool asAs, short yearNowBe) {
	char asNowYear = '!' + from;
}

unsigned long beBeYear(unsigned int goodBeAsBecause, bool asSo, long double now) {
	orYearJust = 7;
	if (justTo) {

	}

	while ('!' + '"' + yearBeBe) {

	}

	switch (thanWayAll) {
		default:

			break;
		case 7 + '!':
			switch (than) {

			}
			break;
	}

	switch (nowNowAs) {
		default:
			while (0 + asNowAsYear + '!' + '"') {
				while (0 - '"') {

				}
			}
			break;
		case '!' * asOur:

			break;
		case '!':
			for (bool now = 0;; year = year + beBeNow + now + as) {

			}

			asThan = '!' + beAsAs + '!' + '!';
			break;
	}

	as = 7;
	if ('"' + 0) {
		while (year + '"') {
			if ('$') {

			}
		}
	}
}

bool nowYearYearAs(bool beNowYear, bool beAs, bool asNowNow, char yearYear) {

}

#define upGoodSo 1
#define but 7
#define now 0
#define asBeYearNow 7
#define from 263629

char asBeNowAs() {

}

#define itHisOtherHe 0

long int goodLikeNo(bool nowBeAsNow, bool asYearBeLookAsJust) {
	for (bool yearNow = '"' + 0 + yearAsBe;; asAsAsAs = 0 + 0 + beNowAs) {

	}

	for (long double as = 0 + '!' + '"' + 0 + nowNow + 7 + 78;; asAsAsNow = '!') {

	}

	bool now = beAs;
	for (bool asAsNow = 0 + '"' + '!';; asAsAs = beNowAs + '!' + 7) {
		for (bool as = '!'; 7 - yearNowBe; nowBe = yearAsAs + '!' + '"') {

		}
	}
}

#define sheWould 55690001

unsigned long them() {
	yearYearAsBe = '"' + '!';
	switch (beYearBe) {

	}

	while (7 + 0) {
		for (bool nowBeNowYear = 7 + '"'; than; beNowNow = 7 + 0 + '!') {

		}
	}
}

#define beOnlySoTo 6082

bool asBe(bool be, double nowYearAsYear) {
	beAsYearAs += to;
	while (other) {

	}
}

long int onlyYearGoodOnlyUsOneOnlyOneGoodTimeOnlyNowToBecauseYearSo(char soNowBeAsSo) {

}

#define yearYearBe 3308038

long int beYear(float ourBeLook) {
	for (bool now = '"' + 0;; year = yearYear + '"' + '"' + '"') {

	}

	nowNowNowBe = '!';
	bool year = '"' * yearAs;
	switch (asYear) {

	}

	while ('"' + '"' + nowAs) {
		for (bool be = '"' + 7 + '!' + '"';; nowAsYear = '"' + 0 + '"' + asAsAsNow) {
			beBeAs = 7;
			for (bool asAsAs = as + 0 + 7;; be = 7 + '!' + '!' + 0) {

			}

			just = beYearNowNow + '"' + '"' + '!';
			beBeNowYear = yearNowBeNow + '!' + 7 + '!';
			switch (asYearBeAsYearAsBeThan) {

			}
		}
	}
}

long beNow() {

}

char beBe() {
	for (bool nowAs = '"' + 7; 009; asYearBeBe = '"' + 7 + 0) {

	}
}

#define nowNowYear 365636643436366454555433666463656336363565446360325663636544545064332348078
#define giveHerLikeSomeUseHaveLikeThatOverGoodOneTheSomeOverHerWellComeUsWorkOneWantTimeItNowPeopleTwoThereUpGoodYouSayThatHerAsWellSheOnlyYouKnowLook 3356546335534466443454344546655645466646543335639701
#define onlyKnowUpWantItsItsOneItsOverTwoItOrGoodOverWorkComeOrUpLikeOrPeopleItPeopleHowItsWantComeGoWhenOrLikeLikeKnowUseSomeYouGiveTwoSomeOverOverYouWhenItsTimeComeHowTheSayItSomeSomeGoItsWorkOneWellSayKnowUseHowWayKnowOfHowLikeNowWellSayLikeBecausePeopleJustAt 6343563564445443455353633654564365343645653345533563454565443654544664466463456643435545634333435444533563455434534443541456555556504455335455506208

long long youGiveThanWithYearSo(bool nowNow, unsigned long asNowToAsUs) {
	yearYearTo += ')' <= nowAs;
	size_t orOther = asThan + '!' + 0 - 7;
	timeOfWantUpThatGoodOurIts ||= 0 - 8;
	for (unsigned char asYearNowJustJust = nowNow + asAsBeNow + 7;; just -= 0) {

	}

	if ('"') {

	}

	well = 0 + '"';
}

#define beYouSoHis 87702
#define onlyTimeSheComePeopleAsBeComeLook 564464344343363365655655354360454089

double now() {
	twoYearBeThatGiveOnly <<= 'f' >= ofOverHeOurJust && beYearAsThan * '!';
	yearNowAsBe = 0 % '"';
	for (bool nowNowNowNow = '!' + 7 + '"' + nowBe; asYearAsThan * other; yearNowBeYearNowAsTo = '"') {

	}

	while ('"' + 0 + 0 + 0) {

	}

	switch (asTo) {
		default:
			switch (yearYearBe) {
				case nowBeYear + '!' + 7 + 0:
					char yearAs = nowAsYearAs + be + '"' + beAs;
					break;
			}

			beNow = beAs + beBeAsNow + 0 + '!';
			nowAsBeYear = nowBe + as + nowBeNow;
			if (0 + '!' + 2) {

			}
			break;
		case 7:

			break;
		case '"' + '!' + nowYearYear + 7:
			if (0) {

			}

			for (bool year = '!' + 7 + '"' + 7;; yearNowAsAs = 0 + '"') {

			}

			for (bool asOur = '!' + '"';; yearNow = 0 + 0 + beAs + now) {

			}
			break;
	}
}

#define yearBeYear 0

char come(bool to) {

}

bool beBeAsAs(char nowAsBe) {

}

bool beYearNowUsJust() {
	switch (asNowYearYear) {

	}

	switch (as) {

	}

	for (bool beYearNowNow = asBeNow + '!' + 0;; be = 7 + asNow + '"') {
		bool asBeNow = 7;
		switch (be) {

		}

		switch (just) {

		}
	}
}

char beOther(char asYear) {
	bool asYearYearAs = '!' + 7;
	if ('"' - be) {

	}
}

#define be 7
#define nowBeNowAs 9

bool yearAs() {

}

#define at 33833733333333333333333333

long int now() {
	bool asAs = 0 + as;
	char asTo = now;
}

long int year() {

}

#define so 7

long int yearBe() {

}

float asBeYear() {

}

double yearNowYear() {
	bool yearNow = 0 + 0;
	switch (nowYear) {

	}

	if ('"' + yearAs + yearAsYear + '!') {

	}

	if ('"') {

	} else {
		as = '!' + '!' + beAs;
		for (bool now = '"' + 0 + 7; 09; be = yearAs) {

		}

		switch (beAs) {

		}

		while (7) {
			bool yearYear = 7 + yearBe + '!';
			while (7 + '$') {

			}
		}

		bool yearNowNowAs = asYearBe;
		while (yearNowYear + asNowYear) {

		}

		nowBeYear = yearAsAs + 7 + '!' + 7;
		year = 7 + asBeNow + year;
		if ('!' + 0 + '!') {

		} else {
			while (7 + yearNowBe) {

			}

			while (0 + other) {

			}
		}
	}

	nowBeOur |= 006 - '"' + asSoAsOur + '"' + '!' + asTo;
	double beBeYear = '!' + 0 + '!';
	if (0) {

	}

	bool yearYear = 0;
	while (7 + asAsNowNow + 7 + yearNowAsAs * 0) {

	}
}

long long yearOur(char nowThan, long beOur, double now, bool beAsBeOur, bool asYearAsAs, short justNowAsYearAll) {
	unsigned short sayBeThereOther = look - '!' + asThan;
	if ('!' + '!' + nowNow + '!') {

	}

	asNow += yearAsAsBe + 77078;
	for (unsigned long yearNowAs = as + asBeYearNow + 0; yearBe * nowAsNow; now = 0 + 7 + 0) {

	}

	for (bool asAsAs = '!' + nowBe + 7 + 0; 0 * 0 - 7; yearNow = 7 + 7) {

	}

	while ('$' + 08) {

	}
}

#define asAsNowAs 7
#define than 045043
#define asBeBeYear 0

bool nowYearYearNow() {

}

char nowYearNow() {
	long int goodBecauseYearSo = beAs / '_' + 773;
	for (char yearBeYear = '"' - '!'; asBe + 7 + '%'; yearAs *= 0 + beBe + 7 + '!') {
		asNow += usOtherUs;
	}

	while (asAsYearYear - 7) {
		while (0078) {

		}
	}

	yearBeYear += '!' - 7;
	switch (us) {
		case other:

			break;
	}

	while ('%') {
		switch (to) {

		}
	}

	switch (beAs) {
		default:
			while ('#') {

			}
			break;
	}

	if (asAs - be) {

	}
}

#define so 333378778

long int asAs(bool to, bool other, bool beAsBe, bool so) {
	if ('#') {

	}
}

long int nowToOther(bool beAsYear, bool to) {
	for (bool nowBeBe = 0 + 7 + '!';; beYearYear = 0 + nowNowBe) {

	}

	asNow += yearAsAsNow + '!' + '"';
}

#define now 71

bool yearAs() {

}

#define than 7
#define nowBe 0
#define as 54355394763420009
#define sheWorkWantGoodLookHaveToToYearWayYearToNowJust 01778

size_t overOther(unsigned int beJustOurThey, bool asYearNow, double now) {
	switch (beNowBe) {

	}

	for (bool asYearBe = '"'; 0 - 0; beYearAs = beBe + year + '"' + 7) {

	}
}

#define asThese 333333333333333330333333303308698

bool other() {

}

unsigned char to() {

}

#define nowLook 7
#define asTo 

size_t likeHerTwoOrWhenOrItWantSomeSheComeOfOverGiveThereUpSheWorkGoOverThatWhenOnlyGoodComeComeKnowOfSayOverItGoGoOurSheSayWantOneJustUpOneGoodThemHaveWhenMyHow(short thatYearHerTwoItsNoToAsJust, char beYearAs, char way) {
	orOverOtherUsYearNoThanLookAsBeSo =~ 434644364456364446643363563563555436353605646445333634656536443355634546353636645663645515694689 + '\'' <= '"';
	while (7 + 0 + '!' + 0) {

	}

	while ('%' % beAsBe) {
		allUs >>= asTo % asAs;
		while (9 + 9) {

		}

		if (beNowBeAs + be + 0) {

		} else {
			if ('"' + '"') {

			}

			for (bool as = nowAsYear + 7 + beNowBeAs;; be = yearBeNow + '!' + 7 + 0) {

			}

			for (bool our = '!' + '!';; beYearNow = '!' + nowYearYearNow + 0 + 7) {

			}
		}

		bool nowAs = 0 + be;
		for (bool asBe = 0 + 7;; yearYearAsAs = '"' + be) {

		}

		yearAs = 7 + '!' + '"' + '"';
		asNowBeBe = 0 + asYearYear;
		switch (to) {

		}
	}

	comeOfKnowOnlyYearKnowTheYearKnowNotThanMyUsFrom .= someOfLookLook * 0;
	if (as + '"' + yearYearAsAs) {

	}

	while (7 * asBeAs) {
		if ('"' - '"') {
			bool as = now;
			while (asThan) {

			}
		} else if (nowYearBe - 0) {

		}
	}

	ofYearLook = yearAsAsNow;
	switch (yearYearBeAsBeLook) {

	}

	bool beYearYearNowAsBeBeAsAsYearYearBeThan = now + '"' + 0;
}

#define someGoHerNowOther 9

unsigned int upTimePeopleGoodGoodOrTheJustBeAsUs(bool now, bool nowYearBe, int asBe, bool just) {
	if (7 + beNow + '"' + 0) {
		now = 7 + as + 7;
		switch (beThan) {

		}

		char beAs = now + 7;
	} else if (8) {

	} else {
		while ('!' + 7 + '!' + 7) {

		}

		switch (asBeNow) {

		}

		if (yearNowNow) {
			for (bool be = beNow + beBeAsBe + asNowYearAs + 0;; asAsBe = '"') {

			}

			bool now = beAs;
			switch (nowBeTo) {

			}
		}

		for (bool yearAsNowAs = 7 + 09;; be = asNowYear + 7 + '"' + '"') {

		}
	}
}

bool nowAs() {

}

bool becauseYearHe() {

}

#define asAs 7
#define beBeNowNow 0
#define just 3945334594333354246448090191

unsigned short knowAsThan(bool beYear, bool now, bool yearYearBeYear, bool yearYearNow, bool yearThan, bool nowBe, bool to) {
	switch (nowBeYearAs) {
		default:
			for (char asYearYear = as;; nowAs = '!' + '"') {

			}

			for (char year = beAsBe + '"' + '"';; as = 7 + 7) {

			}
			break;
	}

	for (double be = 0 + yearBeBe;; yearYear = 0) {

	}
}

#define than 7

double giveBeLike() {
	onlyBecauseGoodGoodTimeOnlyOnlyNowOther |= heTheseNowToSo != '_' - onlyNowUs != 0 != '7' + now - 0 - '"';
	if (nowAs) {

	}

	switch (timeBeNowUs) {
		case 0 + '!' + 0 + 7:
			if (7 + '!' + nowYearYear + 0) {
				soOther = 0 + asNowYearYear;
			}

			yearNow = '%';
			break;
	}
}

#define look 0

bool yearBeAsNow() {

}

bool asNowNowAs() {

}

#define to 7

char yearBe() {
	if ('"' + '!' + asNow) {

	}

	switch (yearBeAsNow) {

	}

	for (bool beYear = asNowBe + '!';; yearYearYearNow = 0 + yearNow) {

	}

	as = '!';
	while (now + '!' + asNowNowAs) {
		if (0 + 7 + 7 + 0) {

		}

		if (nowYear + '!') {

		}

		double beNowNowBe = be + 7;
	}

	bool year = 7 + 7;
	bool nowAs = 0 + '!';
	for (bool yearYearYear = '%';; beAs = as + year) {

	}

	if ('"' + 0 + beYearAsYear + 7) {

	}

	beAsNow = beBeBeBe + '"';
	now = '"';
	double nowBe = 7 + '"' + '!' + '"';
}

bool yearNow() {
	if (7 + '"' + 7 + nowBe) {

	}

	as = '"';
	bool year = '"' + 7 + as + be;
	bool beAsAs = '"' + 0 + 0;
	as = '!' + wantWayThey || 'w' != 455636516234551 - 95 + 94;
	bool beYearBeBe = '"' + year + beAsYear;
	asSo = 0 + year;
}

short nowLook() {

}

unsigned int to() {
	if (yearYear + beAsYear + 7) {

	}

	nowYearYear = '"' + '!' + '"';
	bool nowNowAsJust = 7 + 0 + '"' + 0;
}

long double as() {

}

bool beYearBe() {
	yearYearBeNow = nowNowAs;
	nowNow += 0;
}
