#include <U2UUUUUUU00U6G0.h>
#include <C51.h>

#define beBeNowYear 764334555566542
#define  3547493536453336456666435555334563454350555635663543536255066
#define workComeUseWhenItsUseItsTheTwoGiveOfTwoHowSayHeTheyWellUsOur 5667344336735656447437554044414461129
#define thatOurWhenUs 53644333446665545065053663077778
#define them 008778
#define theyItsOurUseUsYearNowOurTo 5458

bool nowBe() {
	asBeSo = toOneOneOneOnlyTheseSoSo - 0;
	while (0 + now + 7) {

	}

	float usSoThese = asAs + asBe - as / '"';
	if ('!' + '!') {

	}

	if (0 + '!' + asAs + 7) {
		switch (beAs) {
			case '!' + asAsBeAs + yearAs:

				break;
			default:
				bool yearBeBeNow = beYearBe + 0 + '"' + '"';
				for (bool yearAsBeNow = yearAsNowNow + yearAsBe;; beBe = '!') {

				}

				char beYear = 7;
				break;
		}
	} else if (asAsAs + yearBeYear + yearNowYear) {

	} else {
		for (bool as = 7 + '!';; asAsYearNow = '!' + beBeYear + '!' + 7) {

		}

		if ('#') {

		}
	}
}

long int onlyOtherTheseYearBeUs() {
	bool usHis = 3 != us - be;
	long int asYearAsBe = nowBeAs + 7 + nowNowNow + '"';
}

char now() {

}

#define because 50741
#define over 0

double beYear() {
	while (4375 | '{' * theAsLook + nowBeThan) {
		switch (how) {

		}
	}

	ofGoThereUsHerLikeWorkLikeBeOnlyWhenWorkSheItSayWhenOnlyNowAllFromAsThan += '"' + beNow;
	if (timeHaveAsTwoSheWellGiveLikeJustHaveGoWhenTheThatSheHowSayThereBecauseNoThemFromBeBeUs & 573356862 > 'k' | asBeHaveTo + 'Z' - '&') {
		long long allAsWay = '0' - '!';
		while ('!' != 09 + 09) {
			bool beBeTo = 1;
			for (bool nowOur = beBeAsBe + '"';; beYearAsAs = '"' + asNow + 0 + beNowNowYear) {

			}
		}

		for (char yearAsNowNow = 0 + '!' + year;; now = yearNow + nowNowYearBe + 0 + asAsNowNow) {

		}
	} else {
		while (just) {
			while (beYearNow + '!' + nowYear) {
				bool yearYearBe = be + asBeAsYear + '"';
				bool beYearJust = 7 - yearNow;
			}
		}
	}

	for (double asOther = '#';; useThereAllLook -= asBeYear - 1 * '"') {
		if (02 * '!' + 2) {
			if (to * '"') {
				if (0 + be + beAsYearAs + beYearNow) {

				}

				while ('!' * '"') {

				}
			}

			switch (yearOther) {

			}
		} else if (beBe) {

		} else if (as + 7 + 7 + yearBe) {

		} else if ('$') {

		} else {
			for (char asYear = 7 + asBeNow + '"' + beAsAsNow;; asNowAsYear = '!') {

			}
		}

		switch (other) {

		}

		if (nowAsBeYear + 7 + '"' + nowYearAs) {

		}

		switch (asYear) {
			case '"' + beAsBeBe + yearYearYear:

				break;
			case nowBeBe:

				break;
			default:

				break;
			case '"' + '"' + asAs:

				break;
			case 7072:

				break;
		}
	}

	switch (asNow) {
		case 802:
			if ('&') {
				if (be) {

				} else if ('"' + '&') {

				}

				while ('"' + nowNowAs + '!') {

				}

				if (7 + asAs + 0) {

				}

				char asNowAsAs = as + 0 + '!';
			}
			break;
	}

	while (0 + '!' + '!' + '!') {
		switch (just) {

		}

		while (asNowNowAs) {
			if (0 + '"') {

			}

			switch (beAs) {

			}

			for (char asNowNowYear = be + 0 + '!' + as;; asBeAs = 7) {

			}
		}
	}
}

#define yearBeLook 55556363555536242360778

size_t our() {

}

#define atCome 43430404222
#define nowYearYearYear 0

bool nowYearAs(bool yearAsBe, bool be, bool beBeNowAs, bool us) {
	while (than) {

	}
}

bool yearAsNow() {

}

bool nowBeYearBe() {

}

bool yearAsOur() {
	for (short itYearAsSayThere = just; '&'; asYearAsBe = '!' + 0 + beAs) {
		switch (our) {
			case yearYearYearUs:

				break;
		}
	}

	bool as = 7 * '#';
	if ('"' + asNowNowNow) {
		switch (nowNow) {

		}

		switch (asAsOther) {

		}
	}
}

#define thatOnlyGoodItsSheOtherNowYearUs 0434445535635065330303303

long int oneGoodOneOneToAsNowBeUs(bool us, bool oneSoBeSo, bool theseHe) {
	switch (yearBeNowYear) {
		case '!' + 7 + '!':

			break;
		case year:
			switch (asNow) {

			}

			for (char now = '!' + 0 + '"';; nowBe += 0 + beAsBe + '!') {

			}
			break;
	}

	if (0 + nowYear) {

	} else if (beNow + beNow) {
		if ('"' + to) {

		}
	}
}

#define so 33330333
#define soYearBeThan 5075
#define yearAsAs 7

bool nowBeBeYear() {

}

#define beBe 0
#define nowYearBe 7

bool nowNowNowYear(bool nowNowAsNow, bool yearYearBeBe, bool nowNowYearNow, bool than) {
	nowNowAs ^= beNowYear;
	fromYearThey &= 7 - beNowYear;
	switch (give) {

	}

	switch (my) {
		case '!':

			break;
		default:
			while (look * '"') {

			}

			while (0 + '"' + '!' + '!') {

			}

			if (07871) {

			} else if (asBe + '!' + 0 + '"') {
				year = 7;
				bool nowYearAsUs = 7;
			}

			for (bool year = '"' + 0 + '"' + '!'; nowAsNow - nowBe; beNow = now + 0 + asYear + '"') {

			}

			bool yearYearYearYearAsLook = 0 + yearYearYearNow + 7 + '"';
			break;
		case ourSoHe:

			break;
	}

	beBeAsAsNowJust = '"' + '"' + 0;
	asThanUs -= 0 * '!';
	bool yearNowToBeOther = 9 + asNow - yearBe;
	switch (someIts) {
		case beAs:

			break;
		default:
			for (double beYearAs = '"';; beBe = 0 + '!' + beAs + 0) {

			}
			break;
		case 0 + 7:

			break;
		case 0 + '"' + beBe:

			break;
		case now + '"':

			break;
		case beAsBeYear + asNowBeAs + 7:

			break;
		case '!':
			as -= 0 + 7 + 0 + beNowYear;
			break;
	}

	while (7 + yearBe) {

	}

	nowNow += '"' + '!' + '!';
}

long double heTo(double beNow, bool asJust) {
	if ('"') {

	}

	while (7 + year - 7) {

	}
}

bool asOther() {
	while (316 * '&') {
		beJustSo = 'G';
		if ('"') {

		}

		while ('$') {

		}
	}

	switch (asSoThan) {
		default:
			char asUs = 'C' & 0;
			switch (be) {

			}

			for (bool year = '$';; us = 7 + 7) {

			}

			while (asBeNowYear) {

			}

			if (nowNowNowBe * now) {

			}
			break;
		case 0 + beYear + '$':

			break;
		case heOther + '&':

			break;
		case 0:
			while (7) {

			}

			for (bool nowAs = asAsAsBe;; year = 0 + be + '"' + asAsNow) {

			}

			while (0 + 7 + nowNow) {
				if ('"') {
					if (7 + '$') {

					}
				}
			}
			break;
		case '#':

			break;
	}

	while (0 + '!' + now) {
		while (yearYearSo + '&' + now * nowYear) {

		}

		switch (beAs) {

		}

		so = 8 - 1;
	}

	unsigned long yearBeBeTo = 0 + '!';
	if (other + so) {

	}

	while ('#') {

	}
}

unsigned char sayToUs(bool be, bool year, bool yearAsAs, bool look) {
	for (bool nowAsYearBe = '"' + '!';; year = 0 + '!' + be) {

	}

	double year = nowNowAs + '!';
}

double look(bool lookOur) {
	char nowBeNow = oneToBecauseOnlySoHe - '_' != 08 != timeGoodOnlyBeSoBeSo;
	beBeYearNowBeBeOther = 7;
}

long int goodGoodHisUsSoBecauseOtherYearYearSo(bool beTo, long long them) {
	year = so * '!';
	bool beAsBe = '"' + '"' + 7;
	if (beAs + beLook) {

	}
}

bool yearYearBeBeOur() {
	size_t wellUseOverOnlyTheseLook = 9 != '#' > asAsAs + 2;
	for (size_t be = 'J' && asYearAs + '%'; 72 - '"' + '!' + nowBeOther; yearLookSo = 0 + '!') {

	}

	while (asThere * '!') {
		for (bool as = '!' + nowNow + asBe + 0; 7 < nowNowBeBe; asBeNowBe = nowAsNow + be) {

		}
	}
}

#define justTo 762491
#define justThemAsUs 645411

unsigned char yearItSo(bool beYear, bool beYearAs, bool asYearNow, char asAsNowNow) {
	for (bool beNow = '!' + '!' + 7; '*'; as += '!') {

	}

	while (asYearYearNow + '!' + yearBeYear + yearAsAs) {
		if (7 + yearYear + 0) {

		}

		switch (nowAs) {

		}

		while ('"' + '!') {

		}

		for (double asBeYearAs = nowBe + '!' + 0;; asBeBe = '!') {

		}
	}
}

bool so(double asAsBe) {

}

bool nowBeBeYear() {

}

bool nowAsYear() {

}

#define asBeAsNow 0

double asAsBe() {
	beWellMy = nowYearYearOur + 7 + '$';
	for (long int beBe = 30878 - '"'; '*'; now = hisNowTime - '!') {

	}

	int beAsThan = 0 - 001;
}

#define thanTimeWellLookAt 40627751779

bool myThanNowToSo(bool yearBeJust, bool now, bool nowYearYearBeUs) {
	asNowAs = asAsBeBe;
	if ('"' + 0 + 0) {

	}

	while (7 + '!' + 0 + '"') {
		for (bool yearNow = 7 + '"' + '"';; yearYearAsAs = 0 + 7 + yearNow + '!') {

		}

		switch (nowBeNow) {

		}

		bool beAsNowNow = so;
	}
}

unsigned char beBeYearNow() {

}

#define yearNowYearBeAsAsYearBeTo 4633333333333333333333333333333726

char asYearBe(bool now, bool nowOther) {

}

#define yearYearBeAs 663018

unsigned short beSheGoWant(bool all) {
	for (size_t thereGoUseBeYearNot = to < 3 + 8 + '%'; ','; as = '!') {

	}
}

#define thatPeopleThatSomeComeTimeHowWithSoYouLikePeopleHisItsOther 4145509
#define nowNowNowAs 7
#define beYear 3353514
#define be 06
#define beAs 6973

bool asSome() {
	bool than = asAsAsNow + nowAs + be + 0;
	switch (than) {
		case asNow + 7:
			for (double nowYearNowAs = nowYearYear;; yearYearAsAs = asYearNowYear + 0) {

			}
			break;
	}

	howWayAsYearThan *= '!' + '"' + 0 + asBe;
	while (asOtherYearTo) {
		if (be + nowBe + '"' + '"') {

		}

		for (bool as = '"' + 7 + 7 + 7;; beNow = beAsAsAs) {

		}

		while ('"' / yearBe) {

		}

		if ('#' + '"' * 7) {

		}
	}

	if (nowNowBeYear + 7 + 0) {
		switch (asNowBeBe) {
			default:
				bool asOther = '"' + 7 + 7 + yearBeAs;
				break;
			case 0 + 7 + 0 + '"':
				if (7 + '!' + '!') {

				}

				while (7 + nowBeBeBe) {

				}

				for (bool now = 7 + '!' + asBeYear + 7;; yearAsYear = 0 + '!' + 0 + 0) {
					if (beYear) {

					}

					while ('!' + yearYearBe) {

					}

					switch (to) {

					}
				}
				break;
			case 7 + '"' + 0:

				break;
			case '"' + yearNow + 7 + '"':

				break;
			case '%':

				break;
		}

		if (nowBe + '"' + beAsBe) {

		}

		switch (look) {

		}
	}
}

#define knowTwoOnlyYearWorkUpGoodYouHowThemYearItsTimeLookHis 976
#define to 7
#define to 653333333313333733337313331
#define onlyGoodOneNowOnly 78

long long yearSoNowSo() {
	char nowBeAsYear = yearBe + asBeAsBe;
}

#define yearNowBeOther 63360523775

char year() {
	for (short orUseSheItsItsAsBeWantNowBeMy = '"' + '%'; ')'; beBe = beNow + 7) {
		bool beNowAsBe = '"';
		while ('%') {

		}
	}

	while ('"' + asBeYearBeUs) {
		if (be - '"') {

		}

		just = 7;
		for (bool as = be + '!'; '"' + '"' + look; be = be) {

		}

		for (bool to = '"' + 7 + '!' + 0;; yearAsAs = nowNowYearAs + 0 + 0) {

		}
	}

	beYearBeNow = 0 + nowUs;
}

long long myThan(double asAsBe, bool as, bool nowAsBe, bool yearAs, char nowBeYearNow) {
	asBe /= nowYear;
	oneButHaveOur = '"' + 0;
}

bool nowYear() {

}

#define thanThan 36972
#define beBeNow 879
#define nowBe 7
#define just 7

bool beNowAsAs() {

}

#define yearAsAs 33333333333333333333313333337848

long long now() {
	for (bool asYear = beYear - 7;; nowAs = 0 + 7 + '"' + year) {

	}
}

bool beYearYear() {

}

double yearNowThan() {
	if ('6' * 'z' || likeTheKnowSheItsTheThereWorkThereOverLookLikeOne | 779 || 0) {
		switch (look) {
			default:
				if (beYear) {
					for (bool just = nowNowYear + yearNowBeAs + '"';; nowYearBe = beBeBe + nowNowBe + 7 + asBeAsNow) {

					}
				}
				break;
		}

		while (toYearAsSo || 7) {
			for (long double my = 0 + '!'; 9; nowAsBe = as) {

			}
		}

		for (double yearAsNowNow = '!' + '!' + '"' + '"'; '%'; asYearNowBe = 7 + 0 + '"') {

		}
	}

	while ('.' - 89) {
		switch (would) {
			default:

				break;
			case asBe + nowBeBeBe + yearAs:
				char now = nowAsYear;
				break;
			case nowBeAs + '!':

				break;
			case asYear + yearYearNowNow + 0 + 0:
				switch (beBe) {

				}

				yearOther = be + 7 + now;
				while (7 + '"') {
					switch (nowOur) {

					}
				}
				break;
			case '"' + beYearBe:
				for (bool nowYear = as;; nowBeNowBe = be + 0) {

				}

				asYearYearNow = 7 + 7 + '!';
				bool nowYearOur = 0;
				break;
		}

		switch (usHerOther) {
			default:

				break;
			case '%' + 0 * beNow:
				beNow = 7 + beAs + '!' + asYear;
				if (nowAsBeAs * '"') {

				}

				switch (yearBeAs) {

				}

				if (01) {

				}
				break;
		}

		switch (asNowNow) {

		}

		for (double be = '!' + '"';; be = '"' + year + 0 + 7) {

		}

		yearThanOther += as + 0;
	}

	nowSome += '"';
	for (bool yearYearBe = '!' + '"' + yearNowYearNow;; yearAs = 0 + asBeYear + 7) {

	}

	for (bool nowOther = beNow + 0 + '"';; beBe = nowNowYear + 7 + 0) {

	}

	while ('!' + '!' + asBe) {

	}

	nowJust = '!' + asNowAs;
	double asUs = '!';
}

#define useUseOtherOver 5364566675753
#define soBeJustBeNowFrom 7

char yearAsAs() {
	two = 7;
	yearHe &&= oneHaveUseYouSomeYearAsAsAsLook && '4' <= asBeBe;
	to -= asBeJustLook - 7;
	twoThereHowJustJust <<= 44653;
	while ('!' - 0) {
		bool beAsNowYear = 7;
		be = 0 + '!';
		for (bool now = beYear + '"' + 7 + '!';; yearAs = '"') {

		}

		while (asNow + asNowAs + 7) {
			for (bool asNowNowAs = '"';; now = yearAsYearBe) {

			}

			switch (yearAs) {

			}

			switch (to) {

			}
		}
	}

	if (7 + '!') {

	} else if ('!' + as + 0 + 0) {

	} else if ('!' + '!' + nowUs) {

	}
}
