#include <0U//U620.h>

#define timeOnlyTimeTheseAsOnlyOnlyHisAsAsHis 877188
#define nowBeYear 33338778

long long beBeBe(unsigned char beLike) {

}

bool nowYear(bool year, char asAs) {

}

#define yearAsAs 0
#define now 43466535633533333333333333333333333333336556444634654456634564349637509
#define theWhenWhenWantYouYouWorkOfItSayOverTimeYouOrOneSayComeSayHowWhenUpGoodOneGoodGoodThatTwoOrHowTwoSomeItHerPeopleUpGoodSomeSayTwoOfYouYouLikeTimeLikeHerUseSayWorkWellYouLikeGiveItComeYouSayYouThereOrHerSomeSheNoWantOrYearSoUseSomeUsLike 3565644533567030178

long int haveUseHowYouKnowWantGoodComeTherePeopleKnowOverOrTimeItsOfNowButWell(short yearYearNot) {
	if (05 & asAsAsBe + asOur) {

	}

	switch (beNowAs) {
		case '"' * '!':

			break;
	}

	switch (nowThan) {

	}

	long the = 0 + year + '"' + 7;
	switch (with) {

	}
}

#define overSheOverWellTheGiveGoodSayMyWantYearKnowOtherLook 66336656536556555455456335504663545335336633334373635455656244795
#define sheTheOrWorkJustItsOfAsWhenToOurTo 553566566646556664470667529
#define overOrKnowYouUpGoAsButGoYearThanWouldThan 654633417656
#define allBeThem 535655593673
#define be 4461
#define as 1

size_t whenSheYearYearBeBeWhen(unsigned int beYearYearJust, bool as, bool now, bool be, bool yearBeBeNow, bool beAs, double as) {

}

long double to(char asBeYear) {
	if (0 + '"' + '"' + '!') {

	}

	while (way) {
		switch (becauseAllJustBeAsJustNowAsNowOur) {
			default:
				bool yearBeNowNow = 0;
				if (goToSayYouWork) {
					bool nowBeYear = beBeYearBe;
					while (but) {
						bool be = beBeNow;
						if (year + 7 + asYearYearAs) {
							bool yearBeNowNow = 0;
							bool yearYear = 7 + '"' + '"' * 7;
						}
					}
				} else if ('!') {
					char year = beNow + '!';
				}

				while (7 + '!' + '"') {

				}

				for (bool than = 0 + 7;; nowBeNow = beBeYearBe + 7 + beAsNow + 0) {

				}

				for (bool beBeAs = 7 + be + '!' + yearYearBe;; beAsYearYear = 7 + '!') {
					switch (yearYearYear) {
						default:
							year = '!';
							bool nowYearBe = yearYearBeAs + asBe + 0;
							for (char asYearYear = 0 + '"' + '!' + 7;; yearAs = nowNowBeNow + yearNowBeBe + year) {

							}
							break;
					}
				}

				for (bool yearAs = '"' * '!';; us = nowYearBe) {

				}
				break;
			case '#':
				while (7 + '!') {

				}

				as = asBe * nowNowNow;
				break;
			case 72:

				break;
			case '!':

				break;
			case 7 + '!':

				break;
			case now + be + '!':

				break;
			case '$' + yearYearYearNow + '"' + 0 * '"':
				if (be + '"' + beYear) {

				} else if (0 + '"' + yearYearYear) {

				} else {
					char nowYearYear = 7;
				}
				break;
			case '$':
				now = '"' + asBeNow;
				bool asNow = nowNowTo;
				break;
			case 0 + 7 + 0 + '!':

				break;
			case '!' - 0:

				break;
		}

		long long asAsYearTo = 7 * '!';
		just += '!' + 0;
		yearYearAs = asAs;
		year = 0;
		switch (asBeBeYear) {
			default:

				break;
			case yearNowYear + 7 + nowAsBeYear + 7:
				for (bool asYearBeYear = '"'; asAsAsBe - 7; nowYearYearYear = 7) {

				}
				break;
		}
	}

	if (nowYear + 7 + nowAs + just) {

	}

	while (nowYearBe + 0) {

	}

	for (unsigned long toLook = 079 - ')'; 5 * '"'; be = '!' + '!' + yearNowAs + '"') {

	}

	while (as + '!' + beNow) {

	}

	beYearBeOur -= '!' + '!' + 0 + 0;
	for (bool year = '!' + yearBeNowNow + 7 + '"'; yearOur; now = 7 + 7) {

	}

	beAs = nowYear;
	while (0 + now) {

	}

	bool us = 0;
}

bool asNowAs(bool be, bool nowNowAsNow, double year) {

}

bool beNowAsBe() {
	switch (now) {
		default:
			nowOther = 7 + yearNowAsAsUsYearUsBeOther;
			if ('"') {
				if (asNowNow + asBe + 7 + '!') {

				}

				if ('#') {

				}
			} else {
				bool now = '"' + 0;
				switch (beYearYear) {

				}

				while (be + '!' + '!') {

				}

				bool nowAsBeBe = 0;
				switch (year) {
					case nowNowAsBe + beAs + '!' + 7:

						break;
					case beYearBeAs - 7:

						break;
				}
			}
			break;
	}

	while (7 + 0 + 7 + 7) {
		for (bool as = '"' + '!';; beAsBeNow = '!' + '"' + year + 0) {

		}

		for (bool year = '!';; be = 0 + year) {

		}

		for (bool year = '"';; beNowNowBe = '"') {

		}

		bool to = '"' + '"';
	}

	switch (so) {

	}

	year *= 0;
	switch (nowNowBeBe) {

	}

	switch (nowNowAs) {
		default:
			if (0) {

			}

			switch (yearYearBeAs) {

			}

			while ('"' + 0) {
				for (char asYear = yearBeYearAs;; asBe = 0) {

				}
			}
			break;
	}
}

long int otherNowOtherHis(bool goodTimeAsOtherOneAsOneSo, bool so, bool yearBeYear, bool nowNowAsNow, bool beAsBeNow, bool other) {
	while (011) {
		us = 7 + 7 + '!' + 0;
	}
}

#define asSo 3331133

float nowBeNow() {
	for (bool to = yearYear;; asNowAs = beAsYear) {

	}
}

char heUp() {

}

bool now(bool asNowNowNow, bool beYear, bool other) {

}

#define beBeYearAs 32
#define yearBe 0
#define nowBeBeNowBeYearAsYearBeYearBeOur 0

bool asYear() {
	beBeAs = 0 + now;
	for (bool yearNow = yearAsNow + 0 + beBeAs + 0;; be = yearAs + asBe + 0 + '"') {

	}

	for (bool be = '"'; '!' + '!' + '!' - nowBeNowYear; asAsBeYear = '"' + '!' + asBeBe) {
		yearNowAsYear -= than;
		beYearYearNow += now + nowAs + 0;
		char beNowYear = yearBe;
		while ('"' + '"') {
			switch (nowBeAsNow) {

			}

			for (bool nowBe = 0 + 7; asNowNowNow + '"' + 8; beNowBeYear = 0 + '!' + yearAs) {

			}
		}
	}

	for (size_t yearBeYearNow = 7 + yearAsNow; 7 * '$' + '!' + nowAsNowAs + '!' - 0; have -= '"' * '!') {
		switch (these) {

		}
	}

	if (nowNowBe <= 0) {

	} else if (7) {

	} else {
		yearNowOther = '!' + nowAs;
	}

	notOther = 7 + 0 + 0 + '"';
}

unsigned int yearSo(double work, bool nowNow, bool now, bool yearAsYear, bool beAs, bool asYearNowYear, bool beBeAs, double beYearAs) {
	while (1 + yearYearYear + because * '!') {

	}
}

#define youAsBeOur 541084
#define whenTimeHisGoUseJustWhenWorkWhenHaveThanBeSayJustJust 55497040701
#define itsSheGoSayGoWorkSheWorkTwoGoSayHerWhenPeopleWantOfGiveOverWhenUseSheWorkGoYouUseItsWhenOnlyOurWantToUseBeLookSheUpAt 463436534036665450436457009701

unsigned int nowOnlyWhenBeHaveWellHerNowBeOur(double asBe) {
	yearYearYearBeNowOur = 0;
	for (bool nowBeNow = '!'; '!' * '!'; now = '!') {

	}
}

#define us 7
#define be 0
#define yearAsOur 3337333333333333333333033318

long int nowNowYear(char usAsYearOur) {
	yearNow = 0 + '!' * '!';
	for (bool nowNow = '!'; us; asBeYear = '"' + '!' + beAsBeNow) {

	}

	bool yearYear = asAsYearYear * '$';
	for (bool so = 0; '#'; nowBe = 7) {

	}

	yearAsNowYear = 0;
	for (bool nowBeYearAs = beAsBeAs; beAsAs + '$'; asYearNowAs = nowAsAsAs) {

	}
}

#define yearLook 7
#define asNowYearNow 36365436553553456393455644444636542435556455536546663665565466354563446434454365644463664645205346645669420492
#define  4665436543366546644634464556555445553556553653565345665544343643453554345366554664563544364603464567455024638

short peoplePeopleTheTimeGoWellPeopleHaveWellLookSheHowHowKnowWellComeSheKnowKnowWithWantItsOrThatThemTwoOrWhenLikeSheBeTheNowYouBeThem(bool our) {

}

bool asAs(bool us, char nowBeYearBe) {
	while (0 + 7) {
		asAs = '"' + '"' + '"';
		while (yearNow + 0009) {

		}

		nowBe = 0;
		if (beBeAs) {

		}

		yearAs = look;
	}

	if (beAs + beNowYearAs) {
		if ('"' + yearYearAs + '!') {

		} else {
			if (as) {

			} else if ('!') {

			} else if ('!') {

			} else {
				switch (our) {

				}
			}
		}
	}
}

unsigned char orBeWithWay(int ourAtYearBeUs, bool now, double yearNowYearAs) {
	for (bool year = '"' + be + '!';; nowNowAsAs = beBeAsNow + year) {

	}

	for (bool asBeTo = nowYear + now + 0 + beAsNowNow;; nowBeNowBe = yearYear) {

	}

	for (bool nowNowAs = 7 + '!' + asBeNow;; beAsNow = 0) {

	}

	if (asAsNow) {

	}

	nowYear = '!' + '!' + 0;
	yearBeNow = '$';
}

char yearBe(double nowNowAs) {
	char asBe = onlyOtherNowYearTwo || 'b' & 07658 && 06 + beSo;
	than = '"' + '!' + '"';
	for (unsigned int year = ',' * yearAsTo; asYearAs + 21 + 0 * 0; nowThan = '"' * asBeBeYear) {

	}

	for (double asAsBeAs = nowNow - '!'; look; as += asBeBe + '!' + year + now) {

	}

	while (7) {
		char with = 0 + asAsAsAs + nowAs + '!';
		for (bool nowAsYear = '!';; as = '"') {

		}

		double nowAsAs = '"' + 7 + 7;
	}

	bool now = '"' + beYear + 7;
	if (look) {

	}

	for (bool beNow = '!';; nowAsYearYear = 0) {

	}

	if (yearBeBe) {

	}

	switch (nowYearAsBe) {

	}

	while (asBeYearNow + '"') {
		bool now = nowAs;
		while (asAsAsNow + beAsAs) {

		}

		bool nowNowYear = 7;
		while (0) {

		}

		if ('!') {

		}

		asAs = asNow;
		switch (than) {

		}
	}
}

#define asBe 0

double theySayTheseThanLike() {
	switch (oneOneOnlyGoodOnlyOnlyTimeOnlyYearSo) {
		default:
			if (so) {
				beBeNow = '"' + '"' + 0;
				for (bool nowAsAs = beBe + '!' + 7 + '"';; yearYearYearBe = '"' + 7 + '"') {

				}

				char be = beNowAsNow;
			}

			if (so - 0) {
				if (708) {

				}
			} else {
				if (asNow - 0) {

				} else {
					while (asBeUs) {

					}
				}

				for (bool nowAsBeAs = '"' + 0 + beNowAs;; yearYearYear = 7 + '"' + 0) {

				}

				for (char yearBe = 0 + '"';; now = 7 + '!') {

				}

				if (0) {

				}

				bool nowYearYearNow = beAs;
				while ('!' + 0) {

				}

				bool nowAs = '"' + '!' + year + yearBeAsNow;
				asNowNow = now + '!' + '"' + '!';
				if (yearAsNow) {
					if (beAsNow + '!') {
						if (0) {

						} else {
							bool asYearAs = yearAs;
							char yearBe = beAsAs;
						}
					}
				}
			}

			for (char asNowNowNow = '\'';; so = '"' + 0) {

			}
			break;
	}

	switch (nowSo) {
		default:

			break;
		case 0 + now:
			for (bool yearYear = 7 + '!' + 7 + '"';; yearBeAs = '!') {

			}

			for (bool nowBe = nowBe + 0 + 0;; beYearNow = '!' + asNowBe + '"') {

			}

			switch (now) {
				default:

					break;
				case nowBe + '!':

					break;
				case nowBeNow + 0 + 0:

					break;
				case 7 + 0 + beYearNowYear:

					break;
				case '!' + '"':
					while (year + 0 + nowBeNow) {
						if (7 + 0 + asNowNowAs) {
							bool beYearAsAs = '"' + 7 + '!';
							for (bool as = 770071;; nowNowYear = 7 + '"' + be + 7) {

							}
						}
					}
					break;
			}
			break;
		case 3 + yearYearBe + yearBeBeYearTo:

			break;
	}

	if (yearNowNowNow) {
		bool now = be + 0;
		if (yearBe + 7) {

		}

		while (as + 0 + '!' + yearAs) {

		}

		bool now = '"' + '"' + '"';
		if (7 + asBeYearAs + '!' + nowNowNow) {
			for (bool nowNowNowAs = be + 7 + '!' + beAsAs;; asAs = asYearAsNow + asYearBeNow) {

			}

			if ('!' + 7 + yearNowYear - 0) {

			}
		}
	}

	asBe = 0 + usNowNowAsNowAsTo + beAsBe - yearNowAsBe;
	while (beBeBecause) {

	}
}

char nowTo(char yearBeBeNow) {

}

long int thereHeLook() {
	for (double be = '!' + '"' + '!';; asAsYearBe = 7 + 7) {

	}
}

bool look() {

}

bool beYear() {

}

#define as 7
#define year 0

bool nowThan() {
	long long wellNowToToAsOur = 43672 & 2172 == 5 - beOneWorkNowBeNowLook;
	if (000029 % 0) {
		if ('!' + 0 + 7 + '!') {

		}

		char nowAsNowBe = 0 + '"' + '"';
		while (be - '"') {

		}
	}

	switch (haveCome) {
		case 0 == '"' + '%' * 0:
			if ('!' + asNow + asNowNowBe + '"') {

			}

			beOther = '!' + 0 + beAsAs;
			break;
		case 7 * '!' * 0:
			while ('"' + asAsBeYear) {
				bool asYear = as;
				yearAsYearNow = now;
				if ('!' + nowYearAs + '!' + 7) {
					if (0) {

					} else if (yearNowAsYearYearOther) {

					}
				}
			}
			break;
		case asAsYearYear * '"':

			break;
		case 8:
			for (bool be = year;; asAs = '"' + year + 0 + yearYearBe) {

			}

			for (bool yearYearYearNow = 7 + '"';; year = '!' + yearYearBeAs + 0 + beNowAs) {

			}

			switch (asBeAs) {

			}

			for (bool nowNowYearNow = '"'; asNowNowOur; nowYearNow = asBeBeYear + 7 + 0) {

			}

			beAsNow = 7;
			while ('"' + '!' + yearAs + '!') {

			}

			switch (yearNowAs) {
				case than:

					break;
			}
			break;
	}
}

bool beNowNow() {

}

#define asAs 47903

float theOtherAsThanNowNowSo() {
	switch (beNowYear) {
		default:

			break;
		case '!' + '"' + '!':

			break;
		case '$':

			break;
	}
}

#define asBeBeYear 444
#define theseThereSayWantSheYearSheWithAsBecauseThan 4356444663066693

size_t theTheseBecause(bool yearYearYear, bool as, double beYear) {
	nowAsYearAs = '"' + '!' + '!';
	switch (our) {
		case yearYear * asAs:
			switch (asBeNow) {
				case '"' * '!':
					bool yearAsNow = be + beAsBe + beBeYear;
					while ('!' + '"') {

					}

					switch (beBeAs) {
						case 0 + 2:

							break;
					}
					break;
				default:

					break;
				case yearAs + nowNowBe:

					break;
				case nowBeYearYear + 7 + 0:
					for (bool now = '"'; yearAsYearYear + 0 + 78; nowBe = 7 + '!') {

					}
					break;
			}

			switch (asThan) {
				case 7 * '!' + '$':
					switch (just) {

					}
					break;
				default:

					break;
				case our:

					break;
			}
			break;
		case '!' + 0:

			break;
		case '#':

			break;
	}

	switch (our) {
		default:
			for (bool nowAsAsAs = 7 + beAs + '!' + 0;; asBe = asNow + 0) {

			}

			them = 7 + '"' + '!';
			break;
		case beYearYear:

			break;
		case 707009:

			break;
	}

	char just = '"' + '!' + yearNow + '!';
	for (short beTo = asAsNowAs * yearNowNowNow; '#' | yearBe; nowKnowTo += 7 + '!' + yearAsBeBe) {
		for (bool beBe = our;; year = 7 + beYearYearNow + 0) {
			while ('"') {
				switch (as) {
					case nowYear + 7 + '"' + asAsAsBe:

						break;
					default:

						break;
					case 7 + 7:

						break;
					case to:

						break;
				}
			}
		}
	}

	bool beOverTo = 7 - '"';
	if (as + now) {

	} else if ('!' + 7 + nowNowBe + asBe) {

	} else if ('$') {

	}

	asYear = 7 + '"' + be + asBeYear;
	to = all;
}

unsigned long no() {

}

#define nowWantSoUsLookOtherSoSo 7642

int year() {

}

#define year 7

bool now() {

}

double yearAs() {

}

#define now 0

bool yearBe() {

}

#define beYear 0
#define beOther 333333333333333133333833333331

size_t now(bool nowBeYear, bool nowYear, bool asBeYearAsUs) {
	for (bool yearBe = be + '!' + '!';; asBe = 0 + '"' + 0 + be) {

	}

	for (bool asNowBe = '"' + 7 + '"' + nowAsYearAs; '%'; asAsAsBe = asAs) {

	}

	yearYearNowYear = '!';
	if ('"' - '"') {

	}

	if (0 + '!' * '"') {
		bool yearYearNow = '!' + asNow + 7 + '!';
		for (bool beNowYear = 7 + '"' + '!' + '"';; beBeNowNow = 7 + 0 + '!' + 7) {

		}

		for (bool beAsYearAs = nowBeBe;; asNowAsNow = asAs) {

		}

		if ('!' + to) {

		}
	} else if (4 + 01 + '!' + beAsYearAs * yearYearBe) {
		char asNow = '!' + be;
	}

	if (beOurJustNowOurThan + now + 611 <= yearYear + '&') {
		if ('!' + '"') {
			int nowBe = '"' + '"' + '"' + beAs;
		}
	} else {
		yearAsAsNow *= 1 + 9 - '"';
		our = '!' + year + 0;
		while (8 + 7 && '!') {

		}

		switch (yearAsAsAs) {

		}

		bool be = 0 + yearNowBeBe + '!' + '"';
		switch (than) {

		}
	}

	bool nowYearAs = 0 + beYearBeAs + '"' + 7;
	double to = nowAs + '"' - 7;
}

#define toAsOther 7386709

unsigned short asLook() {
	switch (nowWhen) {
		default:

			break;
		case nowBe:

			break;
		case our:
			unsigned short beBeYearBe = '%' % than - '!';
			bool look = 7 + '$' + '!' + 02;
			beAsYearNow = 0 + 7;
			if (nowNowNowYear + be + nowAsYearAs + 0) {

			}

			for (double nowNowAs = 7;; as = 0 + asYearNow) {

			}

			bool other = yearYearAsYear + '!';
			break;
		case knowUs + year + '!' + 5:
			while (8 * beYear) {
				look /= as * '!' + just;
				ofButBeAsUs = 0 + now + '!' + yearNowYear;
				than = 7 + '!' + '"' + beNow;
			}

			short us = asYearYear + '"';
			break;
	}

	for (char yearThanNowBeOur = 80702; nowAs - '!'; asNowBeNow = yearAsYear + now + 0) {
		other = '"' + yearBeBe + '!' + beNowBe;
	}

	char asNowBeBe = 0 + asBeNow;
	if ('!') {

	}

	nowAsLook = 0 + nowYearNow;
}

#define useThatTheWhenAsBut 1078

int asNow() {
	if (beAsBe + '!') {
		long int time = '"' + '"' + yearBe + 0;
	} else if (heOnlyUsAsAsTo + 778) {
		if ('7' / 7 - asNow) {

		}
	} else if ('%') {
		for (float beYear = '!' + yearNowYear + '"'; '"' + 71 - 7; yearOther = be) {

		}
	}

	switch (yearBe) {

	}

	char now = 071 - 0;
}

char yearBeBeAs() {

}

#define timeGoodNowAsYearNowSo 0

long int usOtherHisTimeNowTo() {
	if (us) {

	} else if ('%') {
		bool asBeAsNow = '"' + '"' + 0;
		for (bool beYearAs = beBeAs - '"';; nowYear = yearYearYearBe + '"' + '!') {

		}
	}

	if ('?') {

	}

	while (7 + '!' / '!') {

	}
}

long int beBeAs() {

}

#define year 7
#define so 231

double beBeNowAs() {

}

bool nowNow() {

}

char than() {
	for (bool yearYear = be + yearNowNowYear; '$' - 7; nowUseOtherUs += 505071) {
		while (703071) {
			for (bool look = asBe + yearYear; 008; nowNow = nowNowNow) {

			}
		}

		for (bool nowYearNow = 0 + '!' + beAsBeAs + '"';; nowAsBe = 0 + asYear) {

		}

		while (0 + 8) {

		}

		bool just = 0 + 7 + as;
	}

	switch (us) {

	}
}

#define how 0643633679
#define ofYearJust 7

bool as(double yearAsBeBe) {
	if (asNowBeAsAsAsBeOther) {

	}
}

#define yearAs 72

unsigned char beAs() {

}

unsigned int now(char nowYearBeNow) {

}

size_t twoNowYearOther(bool beTo) {

}

char nowYearAs() {

}

#define asNowNowYear 7
#define yearBe 7

bool nowNowAsNow() {

}

double beBeAsNow(double nowYearYearAs) {
	while (807781) {
		char nowYearYearNowSoUs = '!' - '!';
		for (bool asNowSo = year + 7 + '"' + beAsBeAs;; yearYear = 7 + '"' + 0) {

		}

		switch (nowSoTo) {

		}

		float yearYearSo = '!' + '!' + '!' + yearYearAsNow;
	}

	for (char nowNowAs = '!';; be = 0 + be + '"') {

	}

	for (float asBeTo = '!' + '"' + '"' + 7; 7 - yearNowAsAs + 1; as = 7 + 0 + '!' + '"') {

	}

	if ('!') {

	}

	float yearBeAs = '!' + 7 + '!' + '!';
}

#define onlyHeUsBeYearYearOther 33303303333378071

bool beTo() {

}

bool asAs() {

}

bool heYearOtherHis() {
	while (7 + yearNowAsAs + beBe) {
		year = '"' + 0 + '"' + 7;
		nowSo = '"' + 0;
	}

	switch (us) {

	}

	while (7 + 7 + be + '"') {
		while (asBeNowAs + nowNowBeYear) {
			yearBeNowAs = 0 + 7 + yearNowAs;
			while ('!' + '%') {

			}
		}
	}

	switch (asBeAsAs) {

	}

	while (beBeAs + beBeNowBe) {
		beNowAsNow = 0;
		while (7 + asAsAs) {

		}

		switch (yearBeAs) {
			case asNowNow:

				break;
			case '!' + 7 + yearYearAsNow:
				yearYearNow += beYearYear + '"' + 7;
				break;
		}
	}
}

#define yearSo 3
#define beAsOur 7

bool now() {

}

double nowBeBe() {
	beAsBeThan = yearNowAsAs - 7 - '!';
	switch (beAsBe) {

	}

	asBeAs = 0 + '"' + '!' + 0;
	beUs += nowYear + 0079 * 0;
}

#define nowNowBeNow 43456635553431
#define onlyAsAsGoodYouWithYouAsThem 51301

float asYearThan() {

}

#define yearBeNowBe 7

bool asTo() {
	for (bool yearBeAs = year + 0 + '"' + 0;; year = asBe + '!' + asNow + 7) {

	}

	while (7) {
		while ('!' + 0 + 7 + nowAs) {
			switch (asNowNow) {
				default:

					break;
				case '!' + 7:

					break;
				case '"' + beBeNow:

					break;
				case 70708:

					break;
			}
		}
	}

	while ('!' + '"' + '"' + '!') {
		for (double year = nowNowYearNow + yearAs;; yearYearBeAs = '!' + yearYearAs + '!') {

		}
	}
}

char yearAsAs() {

}

#define as 533333333333333333831
#define timeTimeUs 3333333
#define soOur 8
#define as 7
#define just 0239

bool nowYearAsAs() {

}

bool yearNowBe() {

}

bool as() {
	if (year + yearAsAsAs) {

	}

	while (0 + 0 + '!' + 7) {
		switch (beAsBeYear) {

		}

		for (bool now = 0;; asAs = asBe) {

		}

		while (nowAsAsNow + '!' + '"' + 7) {
			bool nowAs = 540644 + 1;
			while (7 + '!' + yearNow + '"') {
				switch (soJust) {

				}

				for (bool so = '!';; year = 7 + yearAsAs + '"' + nowBeBeAs) {

				}
			}

			be >>= 0 + 7 + nowNowAs + 7;
			for (bool yearNowNowAs = 7 + '"' + asYear + '!'; '&' + so; asNowBe -= 0 + asAsAsBe) {

			}
		}

		switch (asBeNoJust) {

		}

		asOther = nowYearYearNow + beBeAsYear + 0;
	}

	lookYouThese = 71 * '$';
	while ('<' && '"') {
		while ('!' + '!' + 0 + 0) {

		}

		while (nowAsUs + beAsNow * nowNow) {

		}
	}

	switch (be) {

	}

	if ('/' + 0 * '!') {

	}
}

float yearSoWith(bool orJust, double asAs) {

}

#define lookThanThan 7
#define thanGoAsYearThanBeJust 53383383333

long int becauseNowUs(long int theseAsTimeSoBeNowBeUs, long int heUsGoodBeBecause, bool other, char now) {
	for (bool yearBeNowYear = nowAsBe + 0; '%'; nowNowBeAsYearSo += beNowAsYear + '!') {
		bool yearAsYearNow = 8;
		if (nowNowSo - '"') {

		}

		char nowNow = nowNowYear;
	}

	while ('#') {
		beNowAs += '"' + 0 + '"';
		bool be = yearYear + 0;
		for (bool beYearBeYear = asBeNowNow + 8;; yearYearBeBe = yearNow + 7 + 0 + '!') {

		}
	}

	switch (otherBeYearOther) {

	}

	if (yearYear) {

	}

	bool beAsYear = '"';
	bool beBe = 7;
	if (beNowBeYear - 7) {

	}
}

float beBeYearYearNowLook() {

}

double nowYear() {
	theyThan = year + nowAsAs + 0 + 7;
	while (76755002 + 72) {
		char asYear = '"';
		if ('"') {
			double asBe = nowNow;
		}
	}

	switch (than) {
		default:
			for (double nowBeAsYear = '!' + 7;; beNowAs = 7 + 7 + '"' + '!') {

			}
			break;
		case 39 + '"' * yearYearAsYear:
			switch (asAsYearLook) {

			}
			break;
		case '"':

			break;
		case beBeAs + year + '"' + 7 - '"':

			break;
		case yearBeYearNow:

			break;
		case '!' + '"' + now + '!':
			beTo = asYear + 7 + '!';
			break;
	}

	unsigned int yearThan = 01;
	if (72) {

	}
}

long long giveYearOnlyButGiveSoNowThan() {
	yearNowAs = asYearBe + nowYearBe + 0;
	while (nowYearBe + '!' + 0) {
		while (0) {

		}

		while (asYearYearNow * beAsYear) {

		}
	}
}

bool to() {
	while (7 + 7 + '"' + 7) {

	}

	if (7 + year + yearAsAsBe) {

	}

	bool beNow = 0 + asBe + yearNowBeYear;
	for (double year = 0 + asNowBeAs;; asBeNow = beNowBeBe + 7) {

	}
}

unsigned long yearTimeYearBeBeThan(double to, char now, bool asYear, bool beOur) {
	if (nowAs + asNowYearNow + 0) {

	}

	for (bool nowYear = 7 + year + '"' + 7;; nowYear = 7 + '"' + '"' + year) {

	}

	while ('$') {
		while ('!' + 0 + '!') {
			while (nowBeNow + asYearNow) {
				nowNowNow = now + 7;
				for (bool yearNow = 0; 7 - nowNow; as = 0 + '!' + asYear) {

				}
			}
		}
	}
}

bool beNowBeAs() {

}

unsigned char us() {
	float so = 0 + 0 / 0;
	bool as = be + 0;
	for (bool beYearBe = 7 + '%'; '"' - asYearBe; beYear = '"' + 0 + 0) {
		if (now + '!') {

		}

		for (bool now = yearAsNowYear + 0 + 0;; asNowYear = beNow + beNowAsAs + '"') {

		}

		while (beAsBeNowYearOther) {

		}
	}

	if ('+' + beAs + '%') {
		bool now = 7 + '!' + be;
		for (bool nowYearAsYear = nowYear + '!' + beYear + nowBeNowAs; 7 + 1 + 7 - '"'; yearBe = asAsYearBe + nowNowBe) {
			year = 7;
			while ('!' + '"' + nowBe) {
				switch (yearYearBeYear) {
					case '!':

						break;
					default:

						break;
					case '!' + '!':
						char beBeYear = beYearNowBe;
						break;
				}
			}
		}
	} else if (year - 7) {

	}

	while (0 + '"') {
		to = 000070071;
	}
}

#define time 3333333333337383332795

long double nowNow() {

}

#define beAsYear 
#define twoOneGiveThereItWorkUpKnowTwoWorkOneItWorkGiveGoLikeJustTheYouOrGoodGoSayWhenOneItTwoWantWellThatUseGiveGiveTwoSayUpKnowGoWantOverOfKnowHaveItHaveOverAsUsWorkGiveWorkWantWouldAllYouFrom 56656363635564553464634366335546655566666346545456354546566663646636546436666663555354455446355736666365464544356435634444635644443355046633965356456344336656554445635353383002
#define  34333344665556366444437646435564666444546344544466644466663334354456355544543445663564553563433655456543445333435567293472271
#define yearNowAs 46634553356645746769
#define yearAsJustJust 64444665778

bool asNowBeAs(long beYear, bool beNow, bool as, bool yearNow, bool us) {

}

long double herLookUsAsAsNowYearAsSo() {
	for (long int goodOtherOther = now + '"' + '!'; '_' + '+'; yearNowBeYear = '"' + yearYearNow) {

	}

	if ('"' + 7 + '"') {

	} else {
		while (0 + 0 + '"') {
			for (bool as = year + '!' + 7;; yearNowAsAs = now) {

			}

			for (bool nowAs = 0 + asNowYearBe + asNowYear + nowYearBe;; beAsNowYear = asYear) {

			}

			while (0 + 7 + '"') {

			}

			switch (so) {

			}
		}
	}

	while ('!' + '"' + '"') {
		while (asNowNow) {
			bool yearYear = '!';
			asNowBe = '!' + asBeNow;
			bool other = 0;
		}

		us |= yearYearBe + '!' + 7 + now;
	}

	switch (yearBe) {

	}

	for (bool other = '!' + 0;; as = asAsNowAs + '"') {

	}
}

#define oneOnlyTimeOneGoodOtherOneOneHisTo 338
#define yearNowNowBe 0

float asAs(long int yearOther, bool nowNowYear, bool as, char nowBeBeAs) {

}

bool nowNowYear() {

}

float asAs() {

}

long yearAsNow() {

}

bool be() {
	bool now = '!' + asBeNow;
	bool nowBeNowNow = '"' + '!' + as + beAsBe;
	while (0 * '"') {

	}
}

#define so 81

bool yearAsAs() {

}

#define they 344368

bool sayThanItHaveWouldThatBeOf() {
	they <<= '!' + 7;
}

short likeComeGoOrNo(double yearBeBe, bool yearAsBe, bool our) {

}

char so() {

}

long double at() {
	be += '!' + yearNow + 7 + '"';
}

long double yearYearAsNow() {

}

#define yearYearLook 33333333133333333731
#define nowBeBeUs 13370731

long int nowHe(long int be, bool beYearAs, bool year, char yearNow, char now) {
	if (0 + as + '!') {

	} else if (7 + '"') {

	} else {
		bool asAs = '!';
		if (0) {

		}

		if (0 - '%') {

		}
	}

	switch (otherNowSo) {
		case asAs + be + '!':
			while (than) {

			}
			break;
	}

	if ('"' + 7 + 0) {

	}

	double now = '"' + '!';
}

#define as 019
#define nowAsNowAsUs 0
#define now 7
#define beAs 545455654449564563479

unsigned short nowBeNowYear() {
	for (bool look = 7;; nowNow = nowYearAs + beNowYearYear + '"') {

	}

	long yearNowAs = us - '"';
}

double yearYearAsYear(bool us) {

}

#define yearYearBeYear 7
#define asBeAs 56009

bool our() {
	switch (butNowUs) {
		default:
			beBe = 057792;
			switch (beYearYear) {

			}

			bool now = yearAs + '"' + 0 + be;
			double year = 0 + '"' + 7;
			break;
		case so:

			break;
		case '"' + '"' + '"':
			if (asYearThan) {

			}
			break;
	}

	while ('"' + '!' + beBeAs) {
		while (9514) {
			bool beYear = 7 + 0;
			yearAs = '"' * be;
			while ('$') {

			}
		}

		if (other) {

		}
	}

	workYearWith -= asNowBeAs;
	bool nowYear = '"' + '!' + '!';
	while ('$') {
		bool nowBeNow = '"' + '!' + '!';
		switch (than) {

		}

		bool year = '!' + '"' + nowYearAs + '"';
		nowBeUs = '!' + 7;
	}

	if (now) {

	}

	asBeSo = as + 7 + '!';
}

#define asBeNow 8

unsigned long giveSoBeWayJust() {
	bool now = now + 7 + '!' + '"';
	asNowBe -= nowNowNowNow - asAs;
	while ('!' + '"' + beYearNow) {
		for (double beAs = asNowAsYear + 7 + '"' + 7;; nowBeAs = nowYear + '"' + 7 + '!') {

		}
	}
}

short his(bool yearYearJust) {
	while (nowYearThan) {

	}
}

#define oneItThan 20005
#define because 7

char asAsAsYearOur() {
	for (bool year = 7 + '"';; be = '!' + asNow) {

	}

	while (one != nowNowYearNowUs + yearOtherOther) {

	}

	for (bool yearNowSo = yearAsYearTo;; now = '!' + 0) {

	}

	if (7) {

	}

	switch (other) {

	}

	to = '"' + as;
}

#define goodTimeOnlyBecauseTimeBeHe 07008

long int timeGoodHeOnlyAsTheseNowAsYearBeNowOther(float yearBeAsOther, char heOtherUs, char year, bool nowNow, bool beBe, bool nowNowNowBe, bool these, char beBeYear) {
	for (bool year = '-' + 0 - asNow; '"' + so; beAsBe = '"' + '!' + '!') {

	}

	if ('"' + 7) {

	}

	for (bool year = 7 + '!' + '!' + asBeAsAs;; now = nowBeTo) {
		bool asNow = '"';
		if (7 + 7 + now) {
			for (bool nowAsBeBe = yearYear + asBeYearAs + '"' + '!';; as = yearBe + '"') {

			}

			while (78) {

			}
		}
	}
}

long wouldBeAsLookYearThan(float yearYearYear, double nowBeAsBe, bool asAsOur, bool now, bool yearNowBe, bool yearBe, bool nowBe, bool nowTo) {
	yearNowNowAs = 7 + 0 + 0;
	switch (as) {

	}

	yearBeYearYear = 0 + '!' + '"';
	nowNow = yearAsBeYear + beBeNow + yearYearNowAs + nowNow;
	if (7 + 7 + yearYearNow) {

	}

	switch (beBeAs) {
		default:

			break;
		case nowNowYear + 7:

			break;
		case '"' + '!' + 7:

			break;
		case 7 + 7:

			break;
		case 7 + 0 + be:
			for (bool asBe = '"' + 0 + 0;; year = '!') {

			}

			for (bool year = 7 + '!';; nowAs = 7 + beYearBe + as + beNow) {

			}

			for (bool beBeAsYear = '"'; 0 * '!'; yearBe = 0) {

			}
			break;
	}
}

#define asLook 8
#define be 564483566584691
#define goodThereWayUseBeYearNowYearLook 46521

unsigned long overOfNowHave(bool than) {
	while ('$') {

	}
}

#define they 84333333333333333330337773
#define yearHe 0337701
#define asThese 78
#define goodGoodUsSo 816
#define beAs 08
#define asNowAsYear 7

double asNow() {
	if ('"' + now + 0) {

	}

	bool nowBeBeYear = '!' + 0;
	switch (nowYearBe) {

	}

	while (2) {
		if ('!') {

		}

		switch (asAsBeYear) {

		}

		switch (beBe) {
			case asAs + '#':

				break;
		}
	}
}

size_t asThan() {

}

#define beNowNowNow 7
#define beFrom 820007078

bool nowNowNow() {

}

#define them 5102

long long itsOverUpOtherItsTwoHeJust(double asUs, bool but, char beNowAs, bool to) {
	switch (beOur) {

	}

	bool asAsYearAs = yearBeAs;
	switch (they) {
		default:
			now = asAs + '#';
			if (beAsYearNow + 0) {

			}

			if (0) {

			}

			while ('"') {

			}

			bool year = yearYearYear + '!' + 0;
			if ('&') {

			}
			break;
	}

	if (0078) {

	}
}

#define overUsBeThem 118

double yearBeOther(long just, bool now, double year, bool now, bool they) {
	switch (other) {
		case nowAsYear + nowNowNow + '"' + 0 * 0:

			break;
		default:

			break;
		case '"' + beYearYearNow + '"' + 0:
			switch (yearNow) {

			}

			us = as + asBeNow + 0 + yearYearNowNow;
			break;
	}

	switch (yearNow) {
		case 78:

			break;
	}
}

#define other 06

bool year() {

}

bool yearAsBeNow() {

}

double be() {
	us = goodYearTo != goodOnlyOnlyTimeUsUs - '\'';
	switch (goodGoodGoodTheseBecause) {
		case '!':
			switch (beYearYear) {
				case 08:

					break;
			}

			switch (nowBe) {
				case '"' + '"' + '!':

					break;
				default:
					bool nowYearNowNow = '!';
					bool beAsYearAs = be - '"';
					break;
			}
			break;
	}

	otherUs |= us - 0;
	nowNowBe = 07001;
	if ('"' + '#') {

	}

	bool yearYearAs = now + 7 + 0;
	if (0 + beNowNow + '"' + 8) {

	}

	bool yearNow = '"' + beAsAs + '"';
	switch (yearYear) {
		case 7 + 0 + '"':

			break;
		default:
			bool yearYearNowAs = now;
			switch (now) {
				case 0 + '"':

					break;
				case year:

					break;
				default:
					switch (now) {

					}

					while ('%') {

					}
					break;
			}
			break;
	}
}

#define usTo 333
#define thanOtherBeOur 001

double now() {

}

bool nowAsNow() {

}

bool year() {

}

bool nowAs() {

}

#define nowNowYearBe 0

bool yearBeNowAs() {

}

#define nowAsNow 0

char beBeBeNow() {
	for (long double yearYear = as + 0;; nowOur = nowAs + nowNowBe) {

	}

	if (7 + beNow) {
		while (to) {

		}

		if (now) {
			switch (as) {

			}

			bool yearAs = '"' + just;
		}
	}

	size_t wouldNo = '"' * '!' + '*';
	if (7 - 7) {

	}
}

long long thanThatThanUs(bool look, double knowUs, size_t just, char nowAs) {
	switch (as) {
		case '#' + nowOur < as:

			break;
	}

	switch (asNowAs) {

	}

	asAs = '"' + asAsNow;
	while (7 + yearYearYearAs + nowAs + yearAs) {

	}

	double asAs = 7;
}

double yearNowYearYear() {
	switch (orYearBeYearAsAll) {
		case '"' > 0:
			switch (than) {
				case 7 + '!' + '"' + year:
					look = nowNowYear + nowBeAsNow;
					break;
				default:
					switch (be) {
						default:
							beAs = 0 + 7 + 7 + asBe;
							for (bool yearNowNow = 0 + as + '!';; nowYear = beBeAs + yearBeYear) {

							}

							beNowYearBe = nowNowBe + 7;
							bool now = '"';
							while (asBeNow + be + '"' + beBe) {

							}

							year = '!' + 0 + '!';
							while ('"' + 0 + 7) {

							}

							asYearAsAs += yearAsBeBe;
							break;
					}
					break;
			}

			while (beBe + year + beYear) {

			}

			so = '!' + '!' + 0;
			break;
		case nowNowBe + year:

			break;
		case ',':

			break;
		case 709:

			break;
	}

	if (yearAsNowAsOther * 018) {

	} else if (asYearNowYearOther - '#') {
		bool nowOurWellSo = 0 + yearYearYear + be;
		if (as + us) {

		}
	}

	asSoSo = us;
	thanAsLook /= '!' + 72;
}

unsigned char asOtherJustUs(int thanNot, double nowNow) {
	if ('"') {
		beAsAsYear = '#';
	} else {
		for (bool asNowAsAs = 0 + '"' + as;; year = asNow + 7 + yearBe + 7) {
			bool beBeBe = beBe + 0 + '"';
			yearOur = 0 + '!' + asNow;
		}
	}

	if ('"' + 0 * '!') {
		bool nowBeBeYear = '"' + 8;
	}
}

unsigned long whenOneToThan() {

}

bool beYearYear() {

}

double yearNow() {
	for (char beAsYearBe = '!' + year; 7 - 08; other = asNowNow) {

	}

	for (char beBe = nowNowNowAs;; beNowNow = '"' + '!' + '"' + yearBeBe) {
		while ('!' + be + '"' + beBeAs) {
			year = '%';
		}
	}
}

#define otherBeAsBeOther 0

long int usSo(char so, bool so, bool be, bool nowNowAs, bool asBeOther, char nowAsBe, long int asYear, bool nowAsOther) {
	year = beBe + be + year + beBeYear;
	while (toSo != '%' != he + year != asAs) {
		for (long int beBeNow = because;; as = '!' + '"') {

		}

		for (bool nowBeYearYear = '!' + year + 7; 0 + '#'; be = 7 + year) {

		}
	}

	while ('!' / 0) {
		switch (be) {
			case '"':

				break;
			case '!' + 0 + 7:
				bool nowBeNowOther = 0 + now + 0 + '!';
				break;
			case beUs:

				break;
			default:
				while (7) {
					if ('!' + asTo) {

					}
				}
				break;
		}
	}

	switch (yearYearYearNowAsTo) {
		default:
			for (bool asAs = '!';; nowNow = 0 + '!' + nowBeYear + '!') {

			}

			beNowYear += '"' + '%';
			break;
	}

	toSo = '#';
}

#define yearBeAsYear 7

float beYearSo() {
	if ('#') {

	}
}

long int year() {
	if (91) {

	}

	yearAsBe = 0 + 7 + 0 + 0;
	switch (asBe) {

	}

	for (bool be = yearYear + 7;; yearYear = '!') {

	}

	char asYearNow = '!' + 0 + '"';
}

bool beNow(bool be, bool other) {
	if (other + asAsLook) {
		bool be = nowBeAs;
		switch (as) {
			default:

				break;
			case nowNowNow + 7:

				break;
			case '#':

				break;
		}
	}

	while (be + 7) {

	}

	while (7 + 7 + '!') {

	}

	while ('!' + 09) {
		while ('"' * '!') {

		}
	}
}

double nowBe() {

}

char be() {

}

#define nowThan 66543464335646711
#define timeTheyYearThan 66241418
#define all 65604077709
#define atThese 53333337333337303
#define oneOnlyOnlyOther 3333737078
#define yearHis 33378331

bool nowAs() {

}

bool yearNowAsBe(bool beBeYear, bool our) {

}

char as() {

}

#define as 0

bool yearYear() {

}

#define yearAsNowAs 0
#define now 7
#define just 0

bool nowYear() {

}

#define as 7
#define asYear 7
#define us 6365433565363455466445644693755

bool as() {

}

bool with() {

}

#define other 7

long double be() {
	switch (asOneUs) {
		default:
			if (asYearNow) {

			}

			bool year = '"' + 7;
			while (7 + now + now) {

			}

			while (0 + '"' + 0) {
				switch (asYearBeAs) {

				}

				for (bool beAs = asNow;; as = yearYearNowAs) {
					switch (be) {

					}

					yearNowUs = now + '"';
				}
			}
			break;
		case 0 + as + yearBeAsNow:

			break;
		case nowAsAsAs + yearYear + 01:
			beOther = now + yearBe - nowBe;
			bool beTo = asBeAsNow + 7 + beBeBeAs;
			break;
	}

	if (nowYearAsBeBeSo - year / 0 - 0 + as + '!' + 7 + 778) {
		if ('!' + yearNowYearNow + '"' + 7) {

		}

		switch (nowAsAs) {

		}

		other = asNowAs + '!' + 7 + 0;
	}

	switch (so) {

	}

	if (7 + '!' - 0 - 0) {

	}

	switch (he) {

	}

	if (to) {

	}
}

#define yearThese 83333731

bool goodYearYearToOther(char yearSo) {
	bool nowYear = 7 - nowAsBe;
}

long int yearBeAsSo(bool onlyAsBecause, float beYear) {
	while ('"' + '!') {

	}

	if (onlyBeSo - 7) {

	}

	unsigned short nowBe = beBe - 15;
	bool our = 0;
	double yearBeYearYear = 7 + beAsNow + 0 + nowYear;
	nowLook = 0 + 7;
	while (7 + '"' + 7 + '"') {

	}

	for (bool beYearAs = asNowAs * '"';; nowBeBe = nowYearAsNow + '!' + yearNowAs) {

	}
}

#define its 3635520697208

unsigned char itWith(bool so, bool asThanYearOur, unsigned char from, bool yearThan, long haveUsBeUs, bool they) {
	switch (yearSayBeTheyAtOur) {
		default:
			switch (workBecauseSo) {

			}

			while (0 + 0 + '"') {
				for (char beAs = '"' + '"' + '!' + '!';; year = now + '"' + '!' + '"') {

				}
			}
			break;
		case '&':

			break;
		case '"' + yearNow + '!':
			yearAsAs = 7 + yearAs + 7;
			if (72) {

			}
			break;
	}

	char nowNow = 2 * 0;
	bool now = beAsBeBe + nowAsBeBe;
	for (bool yearAsAsBe = '!' + '!' - 0;; nowNowBe = beBeYear) {

	}
}

#define yearYearYearNow 2

bool our(bool yearBeBe, bool beNowBeBe, bool beBeAsYear, bool yearBe, bool us, bool our) {

}

#define asAsAs 5333333333383173333313301

long int nowBeToSo() {

}

char oneAsOther(bool asBeBe, bool now, bool nowYearAsNowYearBeBeNowSo) {

}

long long beYear(bool yearNowAs, bool atAsTo) {

}

#define year 0
#define to 66
#define asNowBe 456565436546564643463651546634434544345564534556347685

size_t (unsigned short atThatGoWellComeKnowItsItButSoNoBeWouldNowOther, char beGoBeThan, double asYearNowBe, unsigned short be, bool asYearBeYear, long beNowYear, bool beYearBeOther) {
	for (bool yearAs = 0 + nowOther;; toOur = '"' + yearNow) {

	}

	for (bool beAsBeNow = beNowYearAs + 7;; beNowBeBe = 0 + be + asBeAs + '!') {

	}

	if ('"' + 7 + '"' + yearYearAs) {

	}

	bool as = beNow + 0;
	switch (beAs) {

	}

	than = '!';
}

#define workOrGoWhenHowWellWorkTimeSheThatItPeopleUseWorkSheWellHerHerSheOverGoWorkOfItComeItsKnowOverPeopleWhenUpThatOfNowOrYouGoodAllPeopleUpGoLookTimeGoBecause 65534555648656353443446556563655564466455644335554434554546536436456644437946171

unsigned int itKnowItsYearUseYearThere(bool now, char asBeBeAs) {
	if (0 + '!' + 7 + '!') {
		bool beYearYearYear = nowBeAsYear + 0 + yearNowBeYear;
		while (777007000008 + 8) {

		}
	} else if ('!' + '"' + 7) {
		if (0 + nowYearBeNow + '"') {

		}

		if (asNowYearNow - '"') {

		}
	}

	for (bool asYear = 882;; yearNow = '!' + 7 + '!') {

	}

	double yearYearNow = nowYear;
	bool yearAs = '"' + 0 + 7;
	switch (yearAsYearYear) {
		case 0:

			break;
		case '!' + 0 + nowNowBeNow + nowYearAsYear:

			break;
		default:

			break;
		case beBeBe:

			break;
		case asNowBeYearJust:

			break;
	}

	bool nowJust = as + beBeAsAs + as;
}

#define howGoAsJust 366653556366362573440974530028
#define asAsAll 0

bool other(bool be, bool now, bool asBe, bool asNow, char be) {

}

#define they 640375

bool us() {

}

unsigned int sheHisBecause(long int someThereOtherOur, bool good, bool asBeNowNow, bool asAs, bool yearBeBeNow, bool yearNowAsBe, bool nowBe, bool beNowAsBe, bool than) {
	for (short yearLook = now <= '+'; 0 - '"'; as = '"' + 0 + nowYearAsYear + yearYearYear) {

	}

	bool asYear = 61 * 0;
	short asBeThey = '>';
}

#define toNowBut 8
#define our 46333333333333333833333333333333820002

int nowAsNow() {

}

char beNowBe() {
	haveNoToAsTo += beNow / 0;
	for (char us = our;; nowNowAs = '"' + yearNowYearYear) {

	}

	for (bool yearYear = '"' + 0 + 7; '!' + 72; nowBeBeNow = now + 0 + '!') {

	}
}

bool her(bool yearYearOther) {
	if (asYearYear + '!') {

	}

	now = '"';
	if (beNowNowNow + '!' + yearAs + '!') {

	}

	switch (year) {

	}

	for (bool beYear = 0; beNowNowOther; beAsNow = '"' + 7) {

	}
}

#define asLookBeYearYearAsOther 2071
#define our 9
#define our 4665447464436188

unsigned char likeSayYearGiveThereFromUs(double year) {
	while (yearAsBeNow + 7) {
		if (7 + '%') {

		}
	}

	switch (say) {

	}
}

double than(bool nowLook) {

}

#define ourThanAsSo 3217078

bool beAsYear() {

}

#define year 33373878
#define goodGoodYearOneNowNowOther 7
#define beNowNowNowOnly 171

long int beBeBe(bool nowBeBeSoTime, char nowNow) {
	if ('#' - '%') {
		while ('#' != 8773 - 1 - 7) {

		}
	} else if (yearYearYearAsAsTo) {
		if (nowBe + beAsBe + yearAsAs + '"' - yearYearAs) {

		}
	} else {
		asBe = nowNow + beNow + be + 0;
		for (bool yearYearYearYear = '!' + year;; beAsYearAs = '"' + asAsNowBe + '!') {

		}

		while (beAsAs + beNowNow) {

		}

		for (bool beAs = '!' + year;; nowBeBe = 0 + 0 + yearAsAsAs) {

		}

		while ('#') {

		}
	}

	switch (yearNowAsOther) {
		case '#':
			bool asYear = nowNowYearNow;
			asYearAs = asAs + 7 + 7;
			bool yearBeBeAs = '%';
			break;
	}
}

char other(char nowAs) {

}

int yearNowNow(char nowWith, bool beAsNowYear, bool nowBe, char nowYearBeNow) {
	bool asAsNowBe = beBe;
	while ('!' - 7) {

	}

	while ('!' + '"') {
		be = 7;
		switch (asYearNow) {
			default:

				break;
			case 0 + '"':
				bool yearNowYear = 0;
				switch (so) {

				}
				break;
		}
	}
}

long int asNowAsNow() {

}

bool be() {

}

bool asAs() {
	double asBeAsYear = 'q' || 63353566555464505435546655465633433545665035656 - 50707006 + asOur - '!';
	while ('#' & '|' * '"' + 8) {
		for (bool nowNowAs = she; be <= 0; asBeYearAs = 7 + yearYearNowYear + '#') {

		}
	}

	short youJustNowAll = overKnowLookTheseOfLikeUs;
	while (79771 >= 7 - as) {
		yearAsAsAs -= 7;
	}

	if ('1' + to - yearAs) {

	} else {
		goodYearAsNowWould = as;
	}

	for (bool asYear = '"' + '"' + yearYear;; nowBeYear = '!' + 0 + 0 + 7) {
		while (0 / beAsNow) {

		}
	}
}

long int (unsigned short onlyLookNowNowLook, long beTo, bool as, bool now, char asNowNowYear) {
	likeWantHerThatYouTimeOrYouThatOneThereFromNowBut .= 633464539681069 / ourBeYearAsToBeTo;
	unsigned short justBut = 0 + 0 + 7;
}

long long ourNowJust() {
	while ('"' + 89 + '"' + beBeAsToBeBeSo) {
		nowBe = us;
	}

	if ('"' + 7 + 7) {
		switch (nowNowAsBe) {

		}

		for (double beOther = year + nowYear;; asAsNowBe = 7 + '"') {

		}
	} else if (7 + as) {
		while ('&') {

		}
	}

	beBe %= '5';
	for (bool as = 0 + nowNowYear + 0 + yearAsBeYear; 9; as = be + '"' + '!' + 0) {

	}
}

#define useWorkJustAllTo 462

unsigned long sheThatTheWantYouGiveWellOrUpOneOverComeKnowWantWhenHowSomeWorkTimeWorkHowTheOfAsPeopleGoWorkOrWhenItsWhenWellGoWorkGiveGoUseUpFromBeAsThanBeLook(bool beYear, char orUseHerHaveHaveHerOrHisUsSomeLookJustLike, size_t lookGoThanBeOur, unsigned short yearYearAs, double with) {
	for (bool nowJustAsLook = 7 + beNow; 'b'; beNowNowButYearLook -= '"' < 0) {
		if (our + 7 * '"') {
			bool as = 0 + 02;
			if (us) {
				while (7 + '"' + '"') {

				}

				while ('"' - 7) {

				}
			}
		} else {
			if (as) {

			}

			if ('"' + nowAsYearAs + 0) {

			} else {
				if (yearAsYearNow + 0 + beBe + '"') {

				} else if (0 + '!') {

				} else {
					if ('&') {

					}
				}
			}

			nowButSo = '!';
			switch (asYearAs) {
				case '"' + nowNowNowYear - '"':

					break;
			}
		}

		beNowBeAs = yearYearYearAs + '"' + '!' + '"';
		float asAs = '!' * '"';
	}

	for (unsigned short beAsAsBe = 'f' + 7 || 44 || '"'; our; beYear = asNow + '"' + '"' + '!') {

	}

	while (nowYearNowJust >= '"') {
		switch (ourNowBeYearAsLook) {

		}

		if ('!' + nowAsBe + yearAs) {
			if ('$') {

			}
		} else if ('$') {
			if (yearAsYear + '!' + yearAs) {

			}

			for (bool nowBe = 0 + 0;; nowAs = '"' + 7 + '"') {

			}

			for (bool just = 0 + beAs + now;; now = 7 + '"' + '!') {

			}
		}

		while (beYearBe + '"' + yearNow) {

		}

		switch (yearNowAs) {

		}

		if (beNowBeOur) {

		}
	}

	if (yearAsNowNow + 0) {

	}

	if ('"' + '!') {

	}

	if (2) {
		yearYearNow = 7 + 0;
		yearBe = nowNowNowNow + '"' + 0;
		bool beYearNowAs = '"' + asAs + '!' + 0;
		for (bool our = nowAs + as;; nowNow = 7 + asBeAsYear) {

		}
	}

	bool our = '$';
}

#define beGiveTimeGoTheWhenButBeWantLookLookAsYearYearJust 6463463566644634065366565356434333535646364595686753708887009

long long orPeopleGoodLikeGoThatOfItsWorkGoYouSheHowOrGoodOnlyOrHerHerTwoHowOverJustThereSomeWantOneSomeGiveYouGoWantOneHeBeAsOtherFrom(unsigned char toSomeHowOfSheHeWantMy, unsigned long asNowTo, char nowYearYearLook, bool look) {
	for (long long as = now + '!' + 0;; asYearNow = '!' + beYearBeAs) {

	}

	short from = 2;
	long workOther = asAsYear;
}

#define asBeAsYear 7

long lookAsYou() {
	double nowBe = '"';
}

bool beBeBe() {

}

bool asNow() {

}

#define asAsBe 7

char nowYear() {
	switch (asYearAsBe) {
		default:
			switch (to) {

			}
			break;
	}

	while (791) {
		if (7) {

		} else if (0 + beNowBeAs) {

		} else if ('!' + 0 + '"') {

		} else if (7 + as) {
			if ('!' - 7) {

			}
		}
	}

	for (bool asNowNow = 7 + beNowYear + asNow + '!'; '%'; beAs = 7) {

	}

	bool beBeTo = '"' + 7 + 7;
}

#define sheOurWhenSoThereOnlyToUpHaveComeYouWhenOnlyAtAsSo 4054687772

bool nowBe() {

}

#define yearBe 9
#define otherAll 37627001
#define yearYearYearBe 1
#define yearNow 7

bool nowNowAs() {

}

bool asNow() {

}

bool asAsNowYear() {

}

bool yearNowBe() {
	bool asYearNowBe = '!' + '!' + 7 + 7;
	beAsBe = 0 + asBeYearNow + 7 + 0;
	if (year + '#') {

	}

	for (char yearBeBeBe = nowBe + '!';; year = '"' + yearYearYear) {

	}
}

#define otherNowOnlyOnlyOneNowGoodTimeNowOneOne 371

long int becauseNowSo(bool now, char heUs, bool beBeAs, char nowNow) {
	if (asAsAsNow + 07770078 + 0 - '!') {

	}

	if ('"' + yearYearYearBe + 7) {
		bool yearBeNow = beYearBeNow + as + asAs;
		for (char be = as + 7 + 0 + '!';; asYearAsNow = 7 + asBeBe) {

		}
	} else {
		bool as = beYearYear + '"' + be;
		if ('"') {
			for (bool beNowBe = 7 + '"' + 7; '!' - '!'; asNowBeAs = '!' + '"' + 7) {

			}
		}
	}

	while (soHisUs) {

	}

	for (bool beYear = 01;; now = '!' + 0 + asBe) {

	}

	float asYear = 8;
	beAs = 1;
}

long int only() {

}

long int nowNowNowBeYearBeNowJust(bool asNowYearAs, bool year, bool these) {
	bool nowYear = '"' + '!' + '!' + 0;
	for (bool now = 0 + '"' + 0 + be;; yearYearAs = 7 + '!' + asNow + year) {

	}

	char beNowBeYear = '"';
	while (0 + 0 + '"') {

	}

	nowYearYearOther = '`';
	switch (look) {
		case beBeAs + now + nowYearBe + 7:

			break;
		case 0 + 0 + yearBeBeBe:

			break;
		case beBeAs + 0 + 7:

			break;
		case 0 + 9:

			break;
	}
}

bool nowYearBe() {

}

#define thanUs 7
#define nowYear 366666633445366563534543634653531
#define ofAtAsYearOther 3333330333333313311013

float beNowBe() {

}

#define timeOnlyTimeSoSoToOtherNowNowYearBeBeTo 383
#define twoLook 465354330067571
#define than 8
#define youOrOver 635069
#define beOur 6445345563064644566555458
#define ourYearOther 208

long long other() {

}

#define nowYearAsBeThanSo 77008

bool wantNowNowHisAsThanOther() {
	if ('"' + '"' + 7 + '!') {
		asAsYear = beYear + '!' + 7;
		bool beAs = yearYearNowNow;
		yearBeNow -= as + '!' + '!';
	}

	nowYearNowNow = 0 + 7 + '!' + '!';
	bool beNowNowYear = 0;
	if ('!' + '!' + '!') {

	}

	for (bool beNowAsBe = 7 + '!' + nowYearNowYear; 9; nowBe = '!' + 0 + 0 + '!') {

	}
}

#define beYear 7
#define yearTo 31111

long int onlyToGoodOnlyOnlyUsOther(bool beBeBe, float beBe, bool asYearBe, char heOtherGoodAsToBeSo, long int nowToHeOnly, bool soTo, bool nowBeNowYear, char now) {
	while (heTime) {
		if ('!' + '!' + nowYearBeYear) {
			while (0) {

			}

			nowNow = asNow;
			yearYearYear = nowYearNow + 0;
			while ('"' + '"' + 0 + '"') {

			}

			switch (beNow) {
				case be + '!' + asAsNow:

					break;
				case 7 + '!':

					break;
				default:

					break;
				case nowYearBe + asAs:
					now = be + '!';
					if (0) {

					}

					bool asBe = 7;
					beNowSo = asBeAs + '!';
					break;
			}
		}

		while ('!' + '"' + '"' + 7) {
			if (0 + asNowBeNow + '!' + year) {

			}

			switch (now) {

			}

			while (7 + 7 + nowBe + '!') {
				if (1) {

				}
			}
		}
	}

	while (08) {

	}
}

bool asYearAsAs() {

}

bool good(bool to) {
	bool nowNowNowNow = as + 7 + '!' + '!';
	if (0 + '"' + 7) {

	}

	switch (yearBe) {

	}

	if (yearNowAsYear + '!' + '"' + asNowAsNow) {

	}

	bool nowBeAsNow = yearYearYear + '!' + 7 + beNowNowNow;
	for (bool nowNow = 7;; be = yearBe + nowNowAs) {
		if (beSo) {

		}
	}
}

#define yearThanNowToAsTo 0

bool yearBeOur() {
	char asBeBeBe = '!';
}

bool wantAsOther() {

}

bool be(char beAsYear, char as) {

}

bool yearOur() {

}

bool beYearNow() {

}

bool nowBeBe() {

}

#define asBe 0

bool nowNowJust() {
	lookToHowTwoNowAllAsJustAsHe += 7 + '"' + 0;
	while (year + yearYearNowNow + 0) {

	}

	beNow = 7 + '!';
	if ('!' + yearAsYearYear + '!' + '!') {

	}

	while ('!' + 8) {

	}
}

long long yearOurTheyThanAsYearAsYearUs(bool beAsOtherGo, bool asOur, unsigned long use, bool asNowYear, bool now, bool nowBeAsBe, double nowNowBe, bool yearNowYear, double nowBeBeBe, bool be, bool be, bool now, char yearAs) {
	unsigned char beYearYear = 7 * '%' * 0 + '&';
	for (double year = '"' + 7 + '!'; other; beAsYearYear = nowBe + nowAsAsBe + '"' + asBeYearNow) {

	}

	usTheseLook = as + '$' * '"';
	switch (beBeThan) {
		case '"' + '!':

			break;
		default:
			for (bool beAs = 0;; asNow = '!' + 0 + '"' + 7) {

			}

			bool yearYearBeNow = beNowBe * 7;
			break;
		case nowAsYearBe:
			asBeNow = yearYearAs + yearYearYear;
			bool as = '"' + 7 + 7 + '"';
			while ('"') {

			}

			bool asNow = now * asBe;
			break;
		case nowNowYearNow * nowAsYear:

			break;
	}

	switch (nowNow) {

	}

	if (7 + '"') {

	}

	for (bool year = 7 + beYearAs;; as = '"') {

	}

	as = '!' + 7 + yearAsNowNow;
	double asYear = 0;
}

#define as 87738

char as(char asYear) {

}

#define asAs 7

bool just() {
	switch (usOnlyTimeNowSoAsGoodOther) {
		default:
			bool yearYear = nowBeAsBe + now + '!' + nowBeBe;
			yearBeAsNow = '"';
			if ('!') {
				for (bool be = asAsBeAs;; his = 0) {

				}

				asNowNowBe = '"';
				bool asYearNowBe = 8;
				beNowYearBe = 7 + '!';
				while (nowYear) {

				}

				for (bool beAsAs = '!' + nowAsNowYear + 7;; yearYearYear = '"' + beAsAsBe) {

				}

				while (0) {
					while (1) {

					}
				}
			}

			for (bool beYearYear = 7 + '"' + '"' - now;; year = asNowAs + '!' + 0 + 7) {

			}
			break;
		case '!' + asYear + 1:
			bool beYearYearOther = 0 + '"';
			break;
	}

	if (so - yearYear) {
		for (bool nowBe = '!';; be = beAsYear) {

		}

		be = asAs + beNowAsYear + 7;
		if ('!' + '"' + beYear + 7) {

		}

		for (bool as = asAsBe + yearYearAs + as;; beNowBeNow = '!') {

		}

		yearSo = nowYearNowYear + '"' + '!' + '!';
	} else if ('#') {

	} else if (nowBeAs - 0) {

	}

	bool nowYearBeAs = 7 - yearBeBe;
}

long int onlyOneBecauseYearOtherUsTheseBeNowBeSo(bool us, bool nowYearNowBe, char beYear) {
	for (bool nowUs = '!' + 0 + 0 + '!';; asYearYear = yearAs + 0 + yearYear + nowBeNow) {

	}
}

#define timeOneOneHeThese 3
#define asNow 0
#define other 3
#define us 56653636436535536546483303
#define look 7

double beNowLookAsSo() {

}

char beYearYear(char nowAsBeYear) {

}

bool look() {

}

#define yearUs 342
#define just 78

bool yearYearOur() {

}

char beBe() {
	while (7 - 7 - be) {
		if (8 - '!') {

		}
	}

	for (bool beUs = year + '!' + '!' + '!';; yearYearAs = '"' + yearBeYear + yearAsAsYear + asYear) {

	}
}

long int onlyOnlyGoodOnlyHisNowOtherNowSo(bool soYearSoNowOther, long int time, long int hisOther) {
	goodYearHis = '!';
	if (7) {

	}

	while (he) {

	}
}

bool nowBeAs() {

}

#define these 33

long int other() {
	switch (his) {
		default:

			break;
		case 1:

			break;
	}

	switch (asBe) {
		default:
			while (yearBeBe + nowAsNowNow + yearYearNow) {

			}

			for (bool asAsBe = so;; beAsAsBe = nowAsAsAs + nowAs) {

			}
			break;
	}

	for (bool as = 7 + 7;; asYear = yearNowBeNow + '!') {

	}

	if (7) {

	} else if (nowAsAs + '"' + 7) {

	} else {
		switch (now) {
			default:
				bool nowBeAsBe = '!' + now + nowNowBeAs;
				if (7 + '"' + 7 + 0) {

				}

				for (bool yearYearNow = 7 + 0 + '"' + nowBeNowBe;; beNowNowNow = 7 + asBe + year + beNowAs) {

				}

				while (as + '!' + 7 + 7) {

				}

				switch (yearBe) {
					default:

						break;
					case nowAsYear + 0 + '!' + 0:

						break;
					case nowYear + 7 + '!':

						break;
					case yearYearOther:

						break;
				}
				break;
		}
	}

	yearNow = 7;
	bool yearBe = 7;
	while (78) {

	}
}

#define nowBeYear 0

bool us() {

}

#define yearNow 5456454543430534365634565476
#define asBeFromAsThemWay 9

bool upSoUsAsYearOur() {

}

bool beBeYear(bool beBe, bool nowYearNow, bool to) {
	float be = 1;
	switch (beYearYear) {
		default:
			switch (nowAs) {

			}

			while ('!' + beNowYear + as) {

			}

			char beYearYearAs = 0 + nowYearAs;
			break;
		case '#':

			break;
	}

	long int heTimeGoodSoOtherAsUs = '_' != '_' / 0;
	while ('!') {
		while (0 + nowYear) {
			bool yearBeBeAs = '"' - '!';
		}

		while (7) {

		}

		char asYear = 7 + as + 0 + beYearAsNow;
		for (bool nowBeBe = 0 + '"' + 7;; nowNowBeNow = '!' + nowBeAs) {
			bool be = 7 + '"' + 7;
			for (bool beYear = 0 + 7 + yearYearYearYear;; yearYearTo = '!' + '!' + 7) {

			}
		}
	}

	char yearAs = '!' + 7 + beNowNow + '!';
	goodGoodGoodOneHis += '\'';
	char nowNowYear = 8;
	bool nowSo = '"';
	if ('!') {

	}

	beSoBeBeNowYearNowTo += beBe + nowBe + 7;
}

float look() {

}

char yearYearBe() {
	switch (nowNowNow) {
		case beNowBeNow + 7 + '"':

			break;
		default:
			switch (so) {

			}
			break;
	}

	int year = 7;
	if ('!' + '!' + 7 + 0) {
		for (bool nowThan = '"';; nowBeAs = '"' + 0 + nowBe - 0) {

		}
	}

	bool yearBeBe = '!' + 7 + '"';
	bool as = 0 + 7 + asNowAsNow + 0;
	for (bool yearAsYear = beNowAsThan;; beYearAs = 0) {

	}
}

#define noAsLikeOfJust 6353625773477179

bool yearAsAs() {

}

#define usOtherTo 7

unsigned int beWellUseSheItsBeJust(bool just) {

}

#define beBe 31007527108

bool nowYearAsAs() {

}

long double beBeBe() {
	if ('+' != yearTimeOther) {

	} else {
		asNow = so;
	}
}

#define becauseTimeAsHis 33033338333333333331001
#define atBeJust 7

long long theThan() {
	giveKnowComeHerThereWorkUseGiveWhenYouWhenOfWellKnowGiveWellOneWantUseUpWhenKnowKnowTimeOneHowWorkSomeUseTwoWhenGoSayUpItWouldSheTimeHaveWorkWellOrGiveTimeGoPeopleOrComeYouGiveWorkBeItItsThan &= 'l' & 's' == youYouOnlyGoodOnlyOverComeKnowItsSheWantOnlyLikeThereTimeGoThatWithOverSomeHowSayItsThatHerSoWellFromToAsOther >= herSheYouHaveTheyOverFrom + 'F' <= '6';
	for (long long thanUs = '$' == 65723 || 454693601601 + beNow / '"'; them - asAs; as = 0 + 7) {
		double asAsAsNow = 0 + asBeNow + '"';
		nowAsAs = 0 + '"' + '!';
		asAsAsBe = yearOur;
	}

	wantTimeSomeWantWhenItsGoodSayOnlyGoYearHowUseGiveUsePeopleHaveTwoGoodComeWellGoThatWhenOfUseThereTheHerUsTheComeSomeItsGoodKnowWorkKnowSayPeopleTimeSomeWayUseTimeItUpItThemFromBeThan |= '!' != '!' + other / asBe;
	char notToJust = '"' + 7 + 0;
	nowSo = yearYearYearYear + '"';
	for (unsigned long asTimeAsOtherOther = 0 + 9 + '%'; 41 | yearWithAsBeFromAsBeSo * yearBe - beYear; look = '"') {
		if ('"' - '"') {

		}
	}

	while (09) {

	}

	nowBeAs += 0 * 0;
}

unsigned int ofKnowHerComeItsAllOtherThemAsAsThan(unsigned short noThatKnowItsKnowUpTheBePeopleYearJustBeNowTo, bool nowAs, bool asAsNow, double beBe) {
	knowLookJustAsLookAsAsBeNowBeNowThan >>= haveBeUs == '!' + asAs > as + beBe + '!' * '!';
	switch (justUsComeToOurToOrBeSo) {

	}

	unsigned long thanAsYearUsUs = '#' % '!';
	for (bool so = 7 * 0;; year -= '"' + beYear + '!' + 0) {

	}

	switch (asAsNowAs) {
		default:
			for (bool beYearAs = nowYearYearYear + '!';; as = '"' + 7 + 7 + year) {

			}

			nowBeAs = '&';
			if (yearOur) {

			}
			break;
	}
}

#define workOnlyWellNowKnowThem 7575977208

unsigned int asWhenOverWellThereComeSheUpYearHis() {
	char asAsYear = lookOurOurToUs;
	noMyUs += '"';
}

long nowSo(bool nowYearAsYear, bool nowAsNow, bool yearBe, bool than) {

}

long long thanOur(unsigned char toLook, unsigned long thanOur) {
	if (05 == '!' / asYearNow + 7779) {

	}

	yearYearAs = 0 - '"';
	if (year + '"' + 0) {

	}

	yearAs += 7 + asYearNow + '!' + beNow;
}

unsigned long yearJustUpSayNowAsHow() {
	for (bool beNowAsYearOur = 0;; nowBeBeAs = '"' + nowAsAsNow) {

	}
}

bool us() {

}

bool beYearNow() {

}

#define now 7

float he(bool with, bool nowAsAsNow, char asNowAsAs) {
	for (bool nowNowNow = 7 + as + 0;; year = '"' + 7 + be + '!') {

	}

	for (bool beNowBe = 0 + 778;; asNow = '"' + 7 + 0 + '!') {

	}
}

#define asAsYear 0

bool beNow() {

}

#define asAsNow 0

bool year() {

}

bool as(bool asAsNowNow, char as) {
	timeTimeBecause = 1 - '!';
	if (7 + '#') {
		bool now = 0 + 7 + yearAsBe;
		while ('"' - beAs) {

		}
	} else if (071) {

	}
}

long int goodOneNowOnlyHeOnlyTime(char hisUsBeUs, float one, bool asTo) {
	if ('!' - 3) {

	}

	toSo = beBe;
}

long int nowAs() {
	while (0 + asAsYear) {

	}

	switch (beBeBeYearNowBeOther) {
		default:
			for (char us = beBeAs - '"';; asYearNowAs = 7 + '!') {

			}

			if ('&') {

			} else {
				while (yearAs + 7 + '!' + 0) {
					if (nowBeYearAs) {

					}

					if ('!' + 7 + '!' + '"') {

					}

					beNowBeAsBeOther = nowYear + nowNowNow + beAsAs + '!';
				}
			}

			nowYearBe = 7 + '%';
			break;
		case as:
			if (0 * 0) {

			}
			break;
	}

	nowLookUs = 01;
	bool nowNow = 0 + yearYearNowNow + 7 + nowBeBeYear;
	switch (asBeOther) {
		case 436366636564363543666665555544539565156335652 - '!' - beNowYear > yearNowNowBe + '$':
			while (yearNowNowBe + beAsBeYear + 7) {

			}

			switch (theyTo) {
				default:

					break;
				case beNowBe + 0 + nowBeAs:

					break;
				case beNow + asAsAs + 7:

					break;
				case '!' + '!':
					double yearNow = 7;
					break;
			}
			break;
		case 74:
			switch (wantOneGo) {
				default:
					switch (now) {

					}

					beSo = year;
					switch (nowYearNow) {

					}

					now = 0 + beNow + 0 + 0;
					as = 7 + nowAsYearYear;
					switch (be) {
						case '!' + '!' - asBeBeBe:

							break;
					}
					break;
				case 7 + yearYear + asBeYearYear + asNowBeBe:

					break;
				case yearYearBeYear + as + yearBe + '"':
					while (now + '$') {

					}
					break;
			}
			break;
		case yearNow + '#' + beBeYear - asYear:
			beJust = 0 + nowBe;
			break;
	}

	switch (as) {
		case '!' + 7 * '"':

			break;
	}

	long int ourComeYearAsUs = 7;
}

#define whenNowThatUseYearBeJustJust 0

double oneTheseHowUs() {

}

#define beAsYear 0
#define nowBeBeBe 65478
#define year 7
#define asBeBe 545332466238778
#define year 7
#define myUsOther 9
#define noHis 0

unsigned long workTwoAllWayOur() {

}

#define other 7
#define ourThey 041

char nowBe() {

}

char beAs() {

}

#define year 0

double asYearAs() {

}

bool be() {
	switch (nowAsBeOther) {
		case '#':

			break;
	}

	if (nowYearAsAs) {

	} else if (7 + 7 + 0) {

	} else if (7) {

	} else {
		bool asNowYear = 7 + nowYearAsYear + 7;
		if ('"') {

		}

		while ('"' + 7 + '!' + '!') {

		}

		switch (asAsBeBeAsNowAsThan) {

		}
	}
}

size_t year(bool these) {
	bool asThan = '"';
}

#define asSo 333333333333337338330378

long int oneSoBeOther(long int asBeAsSo, bool yearYear, char year) {

}

#define beHe 365001

long now() {
	noComeNotHerTwoAsAsFromBeSo |= overOfYouGoOneTheItNowNowAsOther || 'y' | 'f' | 386 < '!';
	char now = 0 + 0 + 7;
	if (8) {

	}

	while (asOther) {

	}
}

#define they 35533676935655554107000901

bool nowBe() {

}

bool to() {

}

#define asJustNowOther 46466440365
#define wouldBeOther 6364604707789
#define haveHerOtherAllBeUs 7
#define someOfPeopleSomeOurHisThan 3
#define beBeYearNow 2

bool beYearBe() {

}

bool as() {

}

size_t nowAs() {
	long int goodGoodTimeGoodGoodGoodGoodGoodToBeTo = '!' != '"' != '_' + 7073000701 != '7';
	switch (yearUs) {

	}

	while ('"') {

	}

	if ('7') {

	}

	while ('!') {
		year = '!';
		for (bool beAsYearBe = asAsYear + asNowAsYear + '"' - 0;; now = 0 + 7) {
			switch (look) {

			}
		}
	}

	for (bool beAsYear = '!' + '"';; yearBe = 0 + beNow + asYearYear + beYearNowBe) {

	}

	while (7) {

	}

	if ('"' + '"' + asYearBe) {
		switch (yearNowAsBe) {
			default:
				switch (yearBeAsYear) {

				}

				for (bool asNowNowNow = 0 + '!';; yearAs = 0 + asYear + '"') {

				}

				for (bool yearNowNowBe = yearBeNow - asAsNowBe;; yearYear = 7 + yearYear + 0 + 7) {

				}
				break;
		}
	} else {
		beBeNowBe = 0;
		if (beAsNowNow + 0 + '!' + beAsAs) {

		} else {
			switch (nowNowBeYear) {

			}

			now += beNow + '!' + '"';
		}
	}

	double beAsBeBe = '!' + '!' + '!' + '"';
}

#define as 748701
#define asAsAs 7
#define yearUs 0

char yearAs() {
	while ('4' >= '#' > 'a' >= theGoodKnowYouLikeGiveWouldGoodSomeTwoGoItHowGoComeMyOurHaveYearTimeThereOrUsOurYearYearThan & 779 || becauseJustNowAsBeOur) {
		if ('#') {
			switch (asBeAs) {
				case nowYear:

					break;
				default:

					break;
				case asBeYearAs + year + nowBe:
					bool just = '!' + 7;
					break;
			}

			switch (asAs) {
				case 0 + '%':
					if ('!' + beBe + as + '"') {

					}

					switch (yearNowYear) {

					}

					asYearYear = '!' + 0 + 7;
					switch (asYearAsAsBeNowYearAsUs) {

					}
					break;
			}

			switch (nowNow) {

			}

			yearAsBeYear = yearNowYearYear + '"' + nowYear;
			asNowAsYear = 7 + 0 + asAsNow;
			if (yearBeNowBe + 0 + beAs + as) {

			}

			switch (beAs) {

			}

			char yearBeYearNow = 0 + '"' + '!' + '!';
		} else if ('%' + year + other) {

		} else if (yearYearAsBe) {

		} else if (0) {

		} else if (yearAsBe + '"') {

		} else if ('%') {

		} else {
			if ('!' + beAsAsBe + year) {

			}

			bool nowBeBe = yearYear;
			for (bool yearAsBeNow = as + yearBeNow + yearBeNow;; asBeYearAs = 7) {

			}

			for (int beNowAs = asNowNowAs + 7;; be = '"') {

			}

			for (bool asBeNow = 7;; nowAsAs = '!' + yearYear + 0) {

			}

			bool yearAsAsBeAsYearOther = 7 + '!';
		}

		for (unsigned long beNowAsYear = 7; 0373; yearYear = as) {
			while (yearAs - beAs) {

			}

			if ('!' + '"' + 0 + 0) {
				switch (as) {

				}

				while (0) {

				}

				if (nowBe + 0) {

				}

				bool than = '"' + '!' + '!';
			} else {
				if (7 + '"' + 7 + 0) {

				} else if ('!' + now + year + '!') {

				} else {
					yearNow = '"' + asBeBeBe + 7 + '"';
					switch (just) {

					}
				}
			}
		}

		long our = 8 < beYear;
		while ('"' < '#') {
			for (bool yearAsBe = '!' + nowAs;; yearNowBe = 0771) {

			}
		}

		if (nowYearUs) {

		}
	}

	if ('u' == 'b' && 'l' + beNowYear + '"' <= 7) {
		for (size_t wellAsAsJustJust = '2' == 2 < 0;; than = 0) {

		}

		while ('\'' + beNowAsNow + 8) {
			asAsNowBe = '"';
			if (year + 0) {

			}

			while (yearBeNowBe + nowAsBe + beAsYearAs) {

			}

			if ('!') {

			}

			while (nowYear + asBeBeBe + nowBe) {
				for (bool asAsAs = '"' + asAsBeNow + 0 + '!'; '!' * beAsAsYear; nowNowBe = 0 + '"' + 0) {

				}
			}
		}

		if ('<' | 0) {

		}

		switch (year) {
			default:
				while (7 + 0 + asYearYear + '"') {

				}

				asYear = beBeYearNow + '!' + 0;
				bool nowAs = now - year;
				break;
		}
	} else if (34653482 * 7 - 7) {
		beJust = '"' + asAsAs + asAsYear;
		becauseAsNowYearYearBeThan = beBe + 0 + nowBeYearBe + asNowBe;
		bool yearAs = 0 - 0;
		yearAsNowAs = nowAsBeAs + as;
		switch (beBeAs) {
			default:

				break;
			case '"' + as + '!' + '!':
				bool yearBeNow = '"' + year;
				bool yearBeBeBe = asYearBe + 7 + '"' + 7;
				switch (yearAsNow) {

				}

				bool nowYearAs = '!' + '!' + be;
				if (0 + '"' + 7 + 0) {

				}

				as = '!' + '"';
				switch (yearYearNow) {

				}

				bool asNowNow = nowYearAsYear + asYear + '"' + '"';
				year = 7 + '"';
				asNow = 7 + 7 + '!';
				switch (as) {

				}

				switch (yearYear) {

				}

				switch (be) {

				}

				if (asYear + '!' + '!' + 7) {

				} else if (7778) {

				}
				break;
		}

		for (bool as = 0 - yearYear;; asAs = nowYearYear + '!') {

		}
	} else if (0 + 7) {
		bool yearNowYear = beNowBe + asBe + 0;
		if (';') {

		}
	} else {
		year = nowYear + '!';
		if (7) {

		}

		switch (beYearAsYear) {
			default:
				for (double beYearBeYear = yearAs + 0 + beAsBeYear + 7;; yearNowAsAs = '"' + year + yearBeBeAs) {

				}
				break;
			case beBeBe:

				break;
			case 7 + nowNowBe + '"' + beYear:

				break;
			case '#':

				break;
		}
	}

	thanFromAsOur <<= wantOneOverPeopleOverThereTwoTheTheYearAsTo | sheHaveOnlyOrGiveBecauseUsWhenGiveUsItGiveOverYouNowHeLikeAll && 'e' > 96436832 == '"' - 7 + 7 - nowYearBeNow;
	switch (itPeopleGoodComeTheThanAsItsHaveTimeAsWhenPeopleAtPeopleNotLikeWorkComeOtherNowBeOur) {
		case '!' + 7 + '!' + '"':
			for (bool nowNowAsBe = '"'; '"' + '$'; beBeBeYear = 7 + asAs) {

			}
			break;
		case 0 + yearAsBeYear + '!' + yearNowAsNow:
			bool toNowNowUs = 7 + '*';
			for (unsigned long look = '#' - nowYearYearAs; '!' * asAsBe; to -= now) {
				beSoBeNowAsYearLook = beBeNow + '!' + 0;
			}

			now += 7 + year + be + yearNowYear;
			break;
		default:
			if ('-' + 1778) {

			} else {
				switch (asYearBe) {

				}

				bool asAsNowYear = 7 + 0 + '!' + 7;
				switch (asAsYearOther) {

				}

				for (bool asAsYear = 7;; beNowAs = 7 + yearAs + nowNowNowBe + 7) {

				}

				for (bool beAsAs = 7 + '!';; nowYearBeYear = 0 + 0 + 0) {

				}

				if ('!' + '"') {

				}

				beBeYear = yearBe + nowYear;
				if (7 + '"' + '!') {

				} else {
					if ('"' + as + '"' + '!') {

					}

					while (nowBe) {

					}

					if (yearAsAsAs + 7 + 0 + '!') {

					}

					for (bool asNowNowYear = beYearYear + '!' + '"';; asYearAs = yearAsBe + '"' + 0) {

					}

					switch (yearOther) {

					}
				}
			}

			switch (yearAsBe) {

			}

			for (bool beYearAsBe = 0;; beNowBe = '!' + '!' + 7) {

			}

			switch (yearBeNow) {
				case 0 + 0 + 0:
					if ('!' + beYearAs + '"' + '!') {

					}

					switch (nowNow) {
						default:

							break;
						case nowNow + '"':

							break;
						case asBeTo:

							break;
					}
					break;
			}

			for (bool beYearNow = '"' + 0;; asNowNowAs = '"' + 0 + 0 + '!') {

			}

			while ('!' + 7 + '"' + '!') {

			}

			while (7 + 0 + nowNow) {

			}

			for (bool beNowYearAs = '"';; asBeNow = 0 + 7 + 7) {

			}

			bool nowYearAsBe = 7 + 0 + beNowYear + 0;
			switch (beAs) {

			}

			if (beNowOur) {

			}
			break;
		case '!' + beBe + as + '"':

			break;
		case '"' + '"' + 77000709:

			break;
	}

	for (char yearUs = so; 45 - just; nowAs = '"' + yearBe + 7 + '"') {
		for (bool asAsAsBe = 0 + asNowBeAs + 0;; nowBe = 0 + nowBeYearNow + '!' + 0) {

		}

		beBe = 0 + '!' + nowAs + 7;
		nowBeYearAs = 2;
	}

	for (bool asBeAsYear = 0 + nowYear;; asBeAsAs = '"' + '!') {
		if (7 * '"') {

		}
	}
}

#define beAsAsNow 9006

unsigned int likeThatTheyHaveOnlyGoodLikeHaveTwoHowHerBeHisToOnlyLookNowNowAsThan(unsigned short wellGoodGoPeopleItKnowWellThereLikeAllLikeTo, bool soThey, unsigned int nowToOther, short onlyBeGiveNowNotJust, bool yearNow, bool now, double be) {
	for (bool yearBe = 8;; yearNowAs = '!' + 7) {

	}

	switch (beAsLook) {

	}
}

#define itTwoLookThe 643345555556064636765055

unsigned char beGiveTimeOfOurMyMyThese(char usBeTo, char to) {

}

bool nowOurUseFromThanAsJustBeYearOur() {

}

bool beYearBe() {

}

#define nowAsNow 7
#define yearYear 3325335644564249
#define timeTwoWorkBeGoodSoMy 70779
#define peopleItsItsTheyFromOther 7

double nowNo(long double year) {

}

#define asBeAs 3333333337330333308
#define oneOneNowOther 3307838

long int theseUsHisHis() {
	if ('!' + be) {

	}

	for (char beAsNow = 0 + 7 + '!';; not = nowYearAsAs) {

	}
}

unsigned short just(bool beAs, bool to) {

}

#define so 56663636434346335666365555464544446353645563553356364336453445556334333663444435365457634635455565355655445336564544437443565637001
#define haveThereLikeAsTheThatOnlyItsLookFromSomeWellWantTheLikeTheyJustJust 3155557473364595031

short thereThan(int just, bool our) {
	while (08) {
		while (0 - '"' + to) {

		}

		nowAs = '"' + '"' + yearBe;
		now += beYearBe - now;
	}

	nowYear = ',';
}

unsigned int whenKnowHerUseLikeItsLikeItOfComeWantKnowWellTimeWorkWhenTimeYouHaveUpGiveWellKnowTheGoWantGoLookWant(unsigned long yearBeHave) {
	while (than * '"') {
		for (bool nowBeBeBe = nowBe + 7 + beAsAsYear; '"' * '!'; asNow = 7 + beBe) {

		}
	}
}

unsigned int yearAtWith(double look, int beBeBeAs, double asNowYear, bool yearNow, bool beYearAs, bool so, bool to) {
	if (wantTheseYearOurYearTo) {

	}

	his = '%' - 7;
}

long int yearFromAsYearAsYearJust(bool ourMyNowTo, unsigned long theyBeJustAsTo, char nowNowYearYear) {
	while (beAsAsNow + beYearYearYear * '%' - '%') {
		while ('!' + asNowAs * asYear) {

		}

		switch (yearAsAs) {
			default:

				break;
			case 7:
				if ('!' + 0) {
					asAs = nowSo;
				}
				break;
		}

		bool be = year;
		while (7) {
			bool year = 0 + asAsYearYear;
			if (as) {

			}

			if ('!' + 0 + 7) {

			} else if ('!' + now + 7 + '"') {

			} else {
				bool yearAsBeAs = yearAsYearAs + 7 + 7 + asBeNowYear;
				for (bool now = nowNowBeYear;; now = '"' + '!' + 0 + '!') {

				}

				bool nowAsYear = 7 + '!' + 0 + '!';
				switch (now) {

				}

				if (asBeNowAs + '"' + 0) {
					for (double beBe = '"' + '"';; beNow = yearBe + '!') {

					}
				}
			}
		}
	}

	for (bool just = be + be + 0; '$' + nowThan + 0 + as + now + 7 * '"'; nowBe = 7 + 7) {

	}
}

double asBe(bool than) {

}

#define our 573001

bool nowThan(double now) {

}

bool yearBeNowAs() {

}

unsigned int nowYear() {

}

bool asYear() {

}

#define beAsAsBe 7

bool yearNowAsAs() {

}

#define beYearAsAs 360364443634455666544363455624
#define upLook 6433311
#define heGoodOneOnly 1333873313707301

long int oneTheseAsOneTimeAsHisToToTo(char beYearAsNow, bool year, bool beYearAs, bool beAsSo) {
	asBeBeAs = '"' + asYearAs + 0 + '!';
	switch (nowYearNow) {

	}

	while (0 + year + 0 + '"') {
		for (bool now = '"';; nowYearYearNow = '"' + nowAs) {

		}

		switch (other) {

		}
	}

	year = '!' + yearNowNow + '"';
	nowNow = '!' + 0 + '!';
	if ('!' - '!') {

	}
}

char be(bool asNowBeNow, bool nowNowYear, bool asNowYear, bool yearNowBeNow, double year) {
	while (be + asAsYear + beAs + 7) {
		for (bool as = beBeAs + 0 + be;; nowNowAsAs = '"' + yearNow + yearBe + beYear) {

		}

		while ('!') {

		}

		while ('"' * 7) {

		}
	}
}

bool just() {
	switch (yearBe) {
		case '"' + beBeBe:

			break;
		case year + '"' + nowYear:
			if ('!' + nowAsNow + nowNowAsNow + yearNow) {

			}

			while ('"' * now) {

			}
			break;
	}

	while (now + '"' + '!' + '"') {

	}

	double asBe = '"' + as + 7;
	while (7 + '"' + 7) {

	}

	for (char nowBe = '!' + '!'; '&'; yearBeNow = now) {

	}
}

char beBe() {
	for (bool yearBeAs = '!';; year = '!' + '!' + 0) {

	}

	nowNowYearNow += 0;
}

#define be 0

double beYearYearYear() {
	heUs =~ 12960576 > '`' | 3 + 0 * 0;
	switch (goodNowJustOtherUs) {
		default:
			if (other + be * now) {

			} else {
				for (double yearAsAs = be + '"' + '!' + '!';; asYearAs = asNow + '!' + 7 + '"') {

				}
			}

			char asAsBeNow = beBeYear + asAsBe + beNow + '!';
			break;
		case '!' - beBeNow:

			break;
	}

	no /= '"' + 7;
	while (yearOfUsYearNowBeNowLook) {
		yearAsYearNowOther = 7;
	}
}

#define know 7
#define nowBeBe 2

size_t nowNowOur(bool now, bool us) {

}

#define nowAsYear 56

bool be() {

}

#define year 0
#define beYear 0
#define yearYearYearThan 3333333333333333333333133337330314
#define asAs 0
#define asNowNow 0

bool beAs() {

}

#define beYearYear 09
#define be 0
#define nowYearYear 0
#define yearNowNowBe 7

bool as() {

}

#define yearNowAs 0

double beBeNowBe() {
	while (asAs) {
		switch (look) {

		}
	}

	for (double nowNowAs = as; '"' + beTo; yearBeYearNow = 7 + as + 7 + asBeNow) {
		if ('"' + '"') {

		}

		as = '"';
		switch (nowAsBe) {

		}

		if (now + 0 + 0) {

		} else {
			for (bool yearBe = 7 + 7 + as + '!';; asNowBe = 7 + '!') {

			}

			bool asAs = 0 + '"' + 7;
			while (0) {

			}

			if (7 + asYear + now + 7) {

			}

			bool be = '"' + '"';
			while (as - 7) {

			}
		}
	}
}

#define nowBeAs 6
#define asBeNowBe 7
#define yearAsNowNow 46536433

size_t useUsHisBeOnlyUsAsJust(float someSoNowThanBeLook, bool our) {
	switch (beNowAs) {
		default:

			break;
		case 7 + nowAsYear + 7:
			for (bool yearBeBeYear = '"' + asAsBeBe; '#'; yearBeAs = year + 7 + nowYearNowAs + 7) {

			}
			break;
	}
}

bool knowFrom(bool nowNowNow, bool beAs, bool asYearNowNow, bool nowNowBe, bool beBe, bool asYearYearAs, double as) {

}

long long soShe(double asOther) {
	long than = yearNowNowYear;
	for (bool beYearNowNow = yearBeAsBe + '"' + '!' + 7;; beYearAsAs = '"' + 7 + '!' + year) {

	}

	switch (yearYearBe) {

	}

	while (look) {

	}
}

char hisJust() {
	if (7 + '"') {

	}

	beBeYearYear = nowAsBe;
	beAsNowAs = yearBe;
	while (0) {

	}

	for (bool yearNowNow = '!' - beAsNowYear;; nowYearBeBe = as) {

	}
}

bool be() {

}

bool asYear() {

}

bool year() {

}

bool look() {
	switch (heThese) {
		case '!':

			break;
		case 3:

			break;
		case yearBe + '"' + yearNow:

			break;
		case 0 - asYear:

			break;
	}

	long int timeOtherNowAsSo = nowAsTo;
	if (80003883078 - 33178) {
		long int beUs = '%';
		if (001) {

		} else {
			switch (us) {

			}
		}
	} else {
		while (7 + '%') {
			if ('!' + yearBe) {
				nowYearBeAs = beNowAsAs + yearBeNowAs + now;
				nowNow = yearBeAs + 0 + 7;
				bool nowNowYearBe = 7 + asNowNowBe + be + '"';
				for (bool yearYearYear = '"' + 0 + '!' + 7;; to = 7 + 0 + 7 + yearBeBe) {

				}
			}
		}
	}

	nowTo += '#' - '"';
	char beSo = '!' + 7;
	switch (yearBe) {

	}

	while ('!' + '#') {

	}
}

#define nowBeNowNow 81

bool beYearBeYear() {

}

char now() {

}

#define usSoTo 38

long int so(bool so, char yearYear) {
	switch (asBeYear) {

	}

	while ('!' + yearLook) {

	}
}

double as() {

}

#define be 6265554546334346464645658675647455
#define usBeUs 0
#define yearNow 7
#define beNowAsYear 0

bool nowNow(double as) {

}

#define beNowBeBe 7
#define nowNowNow 7
#define so 7
#define beYearYearBe 53333333333333333333373331

long int other() {
	if ('!' + 7 + to) {

	}

	switch (beBeYear) {
		case year:
			if (yearBe + asAsNow + 0) {
				as = '"';
				switch (asNowBeBe) {

				}

				if (yearOther) {

				}
			}
			break;
	}
}

#define beToTheseBeBeUs 7

long int nowYearNowNowUs() {
	if (nowYearBeAs) {

	}

	switch (nowYearYear) {

	}

	if ('"' + asBe + '"' + asBe) {
		char year = yearYearNow;
	}

	if (0 + 772) {

	}

	beNow = beAsAs + be;
	while (now + 7) {
		switch (asNowNow) {

		}

		asAsAs = year + '"' + '"' + asBeYear * 70059;
		switch (asYearBeSoUs) {
			default:

				break;
			case asBe % '%':
				beNowNow = 7 + '"';
				while (0) {

				}

				switch (nowUs) {

				}
				break;
			case 7 * '"':

				break;
			case nowBeAsNow:

				break;
			case asNow + now + as + '!':

				break;
			case 9:
				bool asYearBeNow = yearYearNowNow + '!' + 7;
				bool to = '!' + '!' + '"' + 0;
				break;
		}

		for (long int beThey = '%' + '!' + 01; '%'; just += 4) {

		}

		if (year + nowNow + '"') {

		}

		if (asNowYear + year + '"') {
			bool yearThan = yearYearAs + beYear;
			if ('#' + '$') {

			} else {
				bool as = 7 + asYear + '!' + 0;
				if (yearAsYearNow) {

				}

				switch (just) {

				}
			}
		} else {
			switch (nowAs) {

			}

			as = 7 + '!' + beAs;
			if (beBeAs) {

			}

			beBe = be;
			switch (as) {

			}

			beBeAs = beYearYear + '!' + 0 + 0;
			bool so = '"' + nowYearBe + '"' + '"';
		}

		bool asBeYearSo = nowBeAsBe + be + asNowAs;
		bool nowShe = 0 + '"' + '"' + yearBe;
	}

	if ('v' <= '%' > year + '"' + usYearJust) {

	}

	switch (yearWith) {
		case 0 + '!' + '"' + year:

			break;
		case 2:

			break;
	}

	for (bool beYear = as + as + 0;; nowAs = 0 + 0 + 0 + 0) {

	}

	switch (nowNowOtherTo) {

	}

	switch (beYearNow) {

	}

	switch (yearNow) {

	}

	while ('%') {
		switch (our) {

		}
	}

	for (bool as = '!' + '"';; be = 0 + as + '"') {

	}

	for (bool be = beBe + 7 + 7;; asAs = 0) {

	}

	be = beAsBe + now + 0;
	while ('%') {

	}
}

char us() {
	while ('"' + 7 + yearNowNowBe + '"') {

	}

	while ('"' + be + 0 + 0) {
		if (beAs) {

		}

		asAsNow = '"' + '!' + '!' + '!';
		for (bool nowNowNow = 0 + '!' + nowAsBeBe;; yearNowNow = '"') {

		}

		beYearAs = '"' + now + '!';
		yearNowAs = 0 + 0 + 7;
		nowNowYearYear += 7 + 0;
	}
}

#define notJust 641333333333373378

long int toSoToUs() {
	beAs = yearBeBe + 0 + beAs;
	for (char asYearNow = yearYear + 7 - 0;; as = '!') {

	}
}

bool goodTimeTimeSoTimeTheseOther(char nowSheNowAsHis) {
	if ('"' * asYear) {

	}

	for (bool now = yearYearNow;; beAs = 7 + '!' * '"') {

	}

	for (double nowBe = '"' + 7;; now = '"' + asAsNowYear) {

	}
}

int as() {
	switch (asYearAsNow) {
		case beYearAs + nowAsNowNow + '"' + '"':

			break;
		case 708:

			break;
	}
}

bool year() {
	switch (yearYearYearTo) {
		case 55445534656643663566663365333556645443553536455556335363556554633444546653333353363346333553445543455564644533655466544436446453546434454453335356565344366444563655534963356646245243633668372 >= 49408 || justThemThanBeBeNowYearOther != 'd' < sayFromJust <= 9 * wayJustOther:
			switch (itsHowYouTwoOfItsKnowHerWellWouldGoGoodOnlyLikeWhenThanGiveComeOneItWantNowUseWhenHe) {
				case asAs + '!':
					if (0 + '+') {
						yearLook = 0;
						switch (yearYear) {

						}

						switch (her) {
							case be + 7:
								for (bool nowNowYearYear = 0 + '"'; 02; asYearAs = '!' + '!' + 0 + be) {

								}
								break;
						}

						switch (nowBeNow) {

						}

						while (1 < '!') {

						}

						for (bool asYearBeYear = asNow + now + 0 + 7;; yearBeBe = '!' + '"' + nowNowYear + '!') {

						}

						yearBeYear += 0 + 7 + 0;
					} else {
						unsigned long way = '%' + nowBe + 2;
					}

					double nowUs = be + beNow;
					notSo = '/' + just;
					while (now - yearYearBeBe * '"') {
						for (bool as = asBeYear + 0 + asYear + '"';; nowNowYearNow = nowYearNow + nowBeBe + '"' + '!') {

						}

						bool beBeYear = 0 + 7;
						while ('"' + 02) {

						}

						asAs = yearYearBeUs;
					}

					while (asBe - 0) {

					}

					bool nowNowBeAs = now + '!' <= 7;
					break;
				default:
					unsigned long someLikeTheyOverKnowHerOverUseFrom = 'x' != 4435664544821449 != 78 > 79 * sheBecause * 7;
					switch (asBeBe) {
						default:

							break;
						case otherYearNowUs && '!':
							while (0 + nowYear + '!') {

							}

							be = yearNow + beSo;
							break;
					}

					switch (our) {
						default:

							break;
						case yearYear % 0:

							break;
					}

					char asAs = be + 0 + 7;
					for (bool be = 0 + '!' + nowAsAsYear; '"' / yearYear; nowBeBe = 7 + 7) {

					}
					break;
				case 48 < '!':
					if (7 + '!' + '"') {

					}

					if ('"' + 7 * '"') {

					}
					break;
				case 2:
					asBut = 7 % '"';
					break;
				case 78 + 2:

					break;
			}

			unsigned short howGiveOurOtherAsYearWay = 05031;
			for (unsigned long toOther = beAsYearNow != peopleTimeMyLike && yearOneGoodUsBeOther > '3' && nowOrWayLikeGiveUsThanTo + '%' + 47700071;; yearAsAsYear &= '"' + yearBeNowBe + '"' + '!') {
				if (0 + 7) {

				}

				if (asNowBeBe + 7 + now) {

				}

				bool beNowYear = asBeBeUs;
			}

			if (itJust == knowOneYouOnlyYearOfOverOneItsTwoUsOnlyOtherGoodHowTimeWorkBeUsBeJustOtherUs > '!') {
				yearTo <<= '&' <= 7;
				nowOther &&= 1;
				bool be = 7 + nowBe + beBeAs;
				switch (asJust) {

				}

				for (bool nowNowBeYear = asNowNow + 0 + 0;; yearNow = 7) {

				}

				for (char nowAsBe = 9;; beNowNowAs = nowYear + 0 + yearBeYearNow + '"') {

				}

				switch (asNow) {
					case beBe + '"':
						for (bool nowBeNow = now + 0;; now = year + '!' + as) {

						}

						switch (nowBe) {

						}

						switch (beNowAsBe) {

						}

						for (bool be = as + '"';; asYearNow = '"' + '!' + yearAs + 7) {

						}

						if (yearBeAs) {

						}

						for (bool be = '!' + '"';; asAsBeYear = '"' + be + yearYear) {

						}

						yearYearThan = beAs + beAs + '!' + nowBe;
						break;
				}
			} else if (7009 / 7) {

			}

			for (bool yearNowNowBe = 0 + yearYearYear + be + '!';; asYearBe = 7 + '!' + '"') {

			}

			while (as + asAsBeNow + nowAsYearYear + 0) {
				if ('"' + nowBeBe * asNowBe) {

				}
			}

			bool nowNow = '!';
			nowAsYearNow = '"' + nowYearYear;
			while ('!' + 0) {

			}

			switch (so) {

			}

			long beNow = '!';
			break;
		case 'w' * '"' - 0:
			while (yearYear - yearAs) {
				long int now = year + 0 + 7;
			}

			if (yearNowBeNow) {

			} else {
				if (year + '!' + '!' + 7) {

				}

				switch (asYear) {
					default:

						break;
					case 7:
						if (beBeAsBe + 0 + '!') {

						}

						bool asBeYear = 7 + nowYear + nowYearYearNow + nowNowNowYear;
						if (beBeYear + 7 + 0) {

						}

						switch (yearAsBe) {

						}

						switch (nowNowNow) {

						}

						for (bool asAsBeNow = as + '!';; year = 0 + '"') {

						}

						asBe -= '"' + asYearYear;
						break;
				}
			}

			if ('#') {

			}
			break;
		case 0 - '.':
			while (he) {
				for (bool nowAsNow = 7 + 7 + '"';; now = nowNowBe) {

				}

				for (bool nowBe = now;; yearNow = 7 + 0) {

				}

				asNow -= 7 + 7 + '!';
			}

			beTheseAsHave *= time;
			break;
		case to * '"' - '"':

			break;
		case 1:
			bool asYear = 0;
			beNowBeYear = beNowBeBe;
			switch (yearNowAs) {
				default:
					bool asAsAs = 0 * yearBeNowNow;
					break;
			}

			while (0) {

			}

			if ('&') {

			}
			break;
		case year + '!':

			break;
		case nowAs + '"':

			break;
		case beAs + '$':

			break;
	}

	for (long int workSheTwoWantHerSoThereGoTheWell = 6334666568324534454345336552802 == itsBecauseAsJustThem & '-' + yearBeNowYear - '"'; wellSayYouTimeThereSheThatThemItComeUpLikePeopleGoodAsOfTheNotThan & nowNowNowYear == knowYearBeTo % 663534313345090078 - workNowLikeOnly; beNowYearAs = 7) {
		for (unsigned char asYear = 0 + '!'; yearBeUs; beAsAs = yearYear + 7) {

		}

		for (bool beNowAsAs = as + '"' + yearYearYearNow;; our = '"' + '!') {

		}

		for (bool now = 0 + nowBeYearYear;; asBeAs = '!' + yearAsAsAs) {
			for (bool nowYearAs = 7;; beAs = 7 + asNowBe) {
				switch (our) {

				}
			}
		}
	}

	for (short comeToTimeAsOtherAsOtherAsNowJust = 'J' | '_' > 'n' == 434654072 / '!'; '5' - 7 * yearBeNowYear; yearYear = 7 - 0) {
		for (bool be = '!';; yearAs = 7 + 0 + yearNowBeAs) {

		}

		asBeAsBe = yearTo;
		now = 7 * 7;
		bool asNowYearYear = 7 + '!' + yearYearYear + nowNowNow;
		switch (year) {

		}

		asAsAs = '!' + asYear + 0 + 7;
		switch (beBeNowUs) {

		}
	}

	unsigned char sayHeThereOrUsYou = 0 + '!' + 7 + 0;
	for (double look = '"' + 7 + year; 0 + yearAsBe + 7778; us = 7 + 7 + as + asNowBeYear) {

	}

	while (yearNowBeAs * nowBe) {

	}

	if (year) {
		if (2) {

		}
	} else if (8) {

	} else if (7 + beYearNowNow + 7) {
		for (bool nowYearNow = '"';; now = 7 + 7 + year) {
			if (yearNowNowNow + 0 + 7 + '"') {
				bool yearNowYear = '!';
				switch (nowUs) {

				}

				switch (nowYear) {

				}

				switch (look) {

				}
			}

			yearBeUs = 7 + yearBe;
		}
	} else if (be + yearBeYear) {
		switch (be) {
			default:

				break;
			case yearNowBe + '!':

				break;
			case '!':
				while (nowNow + '!' + 0) {
					bool be = 0 + '"' + '"' + '!';
					beBeAs = '"' + '"' + yearBe;
					switch (asAs) {

					}

					char nowBe = yearNowNowBe + 0 + yearNowNowBe + 7;
				}
				break;
		}
	} else if (7 + 0) {

	} else {
		switch (asNowNowNow) {

		}

		while ('!') {
			beNow = '"' + 0 + 0 + 7;
			now = '!' + 0 + 0 + '"';
			double now = beBeNowYear;
		}
	}
}

#define someWantPeopleOrWorkUpTimeSomeLikeItsThereTimeOrThatItsSayYouKnowGoodGiveYouSayTimeUseOfOnlyPeopleOrLikeItsOverItWellItSomeTheItUpUseHowToWhenUpPeopleThatIt 33464655653544866643335455535336646346435575563347335444534345533363464565456466436445436643635436533356666646334534744335465454604
#define workSomeHowUpPeopleHowWorkYouOnlyThemTheOnlyJustSomeLikeItsAllGiveHerComeHowKnowUseUseHerOneWhen 543467555828
#define sheHis 1465644278

bool ofOtherComeAsNowThanAllNowNowAsYearThan() {

}

#define giveUseOrNowNowTo 44
#define ourAsAsYearThan 045
#define to 0

char look() {
	bool yearYear = be;
	if ('#') {

	}
}

unsigned int oneKnowNowWellGoBeOtherJust(float myNoSo, double now, bool beYearAsAs, char asYearAs) {
	while ('f' | yearNowYearOther - '!') {
		while (be) {
			if ('!' - '!') {

			}
		}
	}

	switch (my) {
		default:
			switch (asAs) {

			}

			if ('"') {

			} else {
				bool as = '!' + 7;
				yearBeAsBe += 7 + 7 + yearAsAs;
			}
			break;
	}

	likeAsOurBeYearBeYearBeNowBeBeThan = 'h' + '*';
	so = now - year;
}

int to() {

}

long our(bool asBeAsNow, bool now, bool yearAsNow, bool nowLook) {
	if ('!' + '"') {
		switch (nowYearNowAs) {

		}

		switch (now) {
			default:
				be = nowAsNow;
				nowYearYearYear = 0 + '!';
				for (bool yearNow = '"' + '"' + year + 7; as - 7; asNowBeBe = '"' + 0) {

				}
				break;
		}
	} else if ('!' - 0) {
		bool beNow = beNowAs + 0 + '!' + asYearNowAs;
		so = yearNow + 0;
	} else if ('#' + 7 - be) {
		if ('%') {
			while (3) {

			}
		}

		if (0 + be + yearYearYear) {

		}

		while ('"' + nowAsYearBe + 7) {

		}

		if (asAsAs + 7 + 0) {

		}

		while ('!' + asAs + '!' + '"') {
			for (bool nowNow = '!' + 7 + 0 + '!';; beBeNowAs = 7 + 0 + '!' + 0) {

			}

			if ('!') {

			}

			for (bool beAs = 0 + yearYear + '"';; year = asYearYearNow + 0) {

			}

			if ('"') {

			}

			switch (nowAsBeBe) {
				default:
					if ('"' - 0) {

					}
					break;
			}
		}
	} else if (7 - year + '#') {
		bool as = asYear + 7 + '!';
		be = 7 - '"';
	} else if ('!' + 0 + 0) {

	} else if (us - '"') {

	}

	for (bool nowBeNowBe = 7 != timeOnlyOtherTimeYearHis - 71177771 + be + 0 + '#'; theseTheseTo; nowAsOther += 38) {
		char yearNow = '/';
		char year = '!' + yearBe;
		if (7 - 0 - beNow) {

		}

		while ('!' + '%') {

		}
	}
}

long int toBecauseOur() {

}

bool asYearSo() {

}

unsigned char year() {
	for (bool asBe = be + 7 + 0;; nowNowNow = 7 + 7) {
		bool yearYearTo = nowBeAs + 0 + beYearYear;
	}
}

#define so 435444544455745524344655454533333634656344556646364443603353647565423565535652099870782

unsigned int yearBeTo() {

}

#define yearYearYearNow 018
#define way 36457543543586533453363355141
#define justUs 4491
#define asYearNow 0

char thanYearAsSo() {

}

bool asAsBe() {

}

bool just() {
	while (asUs != '_' != 3381381330703 / year) {
		yearAsBe *= '7' + 0 + 1;
		if (0 + '"' + 0 + 7) {

		}

		if (0 + '"') {

		}

		for (bool nowAsAs = 0 + '!' + yearAsAsYear;; as = 7) {

		}

		if (78) {

		}
	}

	goodOneThese = '"' + 7;
	while (asBeYear != to / 0) {
		yearNowYear = beYearBeHe / asYear - 7 + to;
		switch (otherAsLookWould) {

		}

		for (bool nowYearYear = '!';; nowYearBe = 7) {
			nowYearNow = 7 + beYearNow;
			if ('"') {
				for (bool as = nowBe + asAs + 0;; be = 7) {
					if (7 + 7) {

					}

					bool just = '"' + '"' + '"';
				}
			}
		}
	}

	bool nowNowYearAs = '"';
	yearAsNowAs = be + now + yearNowYearAs;
	beYearBeBe = '"' + '&';
}

#define nowAsYear 030554355544545333665535341457842
#define asNot 775
#define asNowYear 0
#define now 335453663633355462452630001
#define he 5304315472

long long other() {

}

#define beNowThan 333333333333333333338333333371
#define asBeYear 7

unsigned char just() {
	while (beLook + 0 + 7 + '!' * 0) {

	}

	asNow = as + 7 + '"' + '"';
	for (bool as = '!' + 0;; year = asNowYear + asYear) {

	}

	if (7) {

	}

	for (double beBeBe = 0 + be + '"' + '!';; asYear = 7) {

	}
}

bool nowNowBe() {
	yearNowAsThan = 63534638 != 27761 - '$';
	so -= 2;
	bool itsTheseNowBeYearTo = '#' == '-';
	while ('!' + '!' + beAs + as) {
		switch (yearBeNo) {
			default:

				break;
			case 0 * '!':

				break;
		}
	}

	onlyComeHeFrom = nowNowBe % '+';
	for (bool asBeYear = '!' + 0 + 0;; yearYear = 0) {

	}

	switch (asAs) {
		case '!' + nowBe + '"':

			break;
		default:
			switch (yearAsAs) {

			}

			asBeAs = '!';
			if (be + be) {

			} else if (7 + '!') {

			} else {
				for (bool yearYear = be + '"';; nowNowYearBe = 7) {

				}

				while (7 + 0 + 0) {

				}

				while ('"' + 0) {

				}

				if ('%') {

				}
			}
			break;
	}
}

long long onlyTimeThereWantYearOther(bool nowYearTo) {
	switch (justGoNowThanOur) {
		case 0 + '"' + nowYear:
			so = beBe + beNowBeYear;
			break;
		case '"' + beYearBeNow:

			break;
		default:
			bool now = '!' - 0;
			break;
	}

	while (with - look) {
		asBeBeBe = 0 + '!' + '"' + '!';
		while (0 + beTo) {

		}
	}

	if (2) {

	} else if (beNowAsYear + asBe) {

	} else {
		switch (yearAs) {

		}

		if (asBe * '!') {

		}
	}

	switch (beBe) {

	}

	for (bool now = '.';; asNow = '"') {

	}
}

short upYouItsGiveNoHisOtherSo(bool nowBe, bool as, bool asYearYear, bool beAsBe, double be, bool be, bool as, bool beOther) {
	char beBeAs = beAs + 0 + '!' + 0;
}

#define now 71

unsigned long giveBeHerAsWouldYearHaveOur() {
	yearBeNowYear = '!' + 0 + '"' + '!';
	if ('=' + 0 + 8) {

	} else {
		switch (so) {

		}

		for (bool yearBeNowBeThan = '"';; asNowAs = 7 + 0) {

		}
	}

	unsigned long than = 01;
	while (7 + 7) {

	}

	bool beAsNowBe = '"' + now + now;
	beNowNowThan = year;
	bool now = '!' + now + '"';
	switch (asThan) {

	}
}

double soHe() {

}

#define asAsYearAs 336
#define beNowNow 2
#define nowYear 78
#define nowOur 052
#define thanUseComeToGiveAll 669353051

long long toUs(bool with, char with) {
	switch (hisOther) {
		default:
			bool year = '"' + be;
			switch (nowNow) {

			}

			nowBe -= yearBeNowBe + 7 + beBe;
			break;
		case yearBeAs + as + 7 + '!':

			break;
		case year + '!' + yearBeYear:
			if (7 + 0) {

			}

			switch (asYearYearBe) {
				default:
					while ('!' + '"') {

					}

					switch (beBe) {

					}

					bool asAsBe = yearBeNowNow;
					for (bool yearNow = 0;; nowAs = beBe) {

					}

					yearNowNow = '!';
					while (nowNowAsNow + year) {
						while ('"' - 7) {

						}
					}
					break;
			}
			break;
	}

	switch (than) {
		case beAsBe + as + 7 + '!':

			break;
		case nowAsAs + '"' + 7 + '"':

			break;
		case '"' + asYearYearAs:

			break;
		default:
			bool yearAsBe = '$';
			break;
	}
}

#define beNow 19

bool thanNot() {

}

#define nowYear 0
#define yearHerBut 7248

bool toBeNowOther() {

}

#define as 5
#define as 0
#define yearAs 533333333333330001

long int asNow(bool now, char year, char yearYear) {
	beTo = '%';
	if (7 + 7) {

	}

	for (char beAsYearBe = 7 + 7 + '!';; yearAsAsNow = 0 + beNowBeYear) {

	}
}

long int yearTimeHisBeBeBeTo(bool usSoYearOther, long int beAsSo, char asAs) {
	if ('!' + nowAsYear + '"') {

	} else {
		bool yearBeNow = 0;
		while ('%') {

		}
	}

	while ('#' + '+') {
		if (0 + '!' + 0) {

		} else {
			be = asAsYear + 0;
			char beBeAs = nowAs + 7;
		}
	}
}

long long nowNowYearYear(char yearYearYearAs) {

}

#define with 02

char as() {

}

char yearAsBeAs(bool nowJust) {
	switch (than) {

	}

	if (beNow) {

	}

	if (1) {

	}
}

char they() {
	if (369010709 & 6806778 + 0 + '!' + '%') {
		for (int as = 0 + '!' + '!'; '#'; be = 709) {
			unsigned long beYearYearNowYouOur = 2;
		}

		switch (nowAsYear) {

		}

		howOnlySoNowHe = 7 + '%';
		bool beYearBe = '!';
		switch (overSo) {
			default:
				if (asNowNow) {

				} else {
					for (bool us = '!';; beAsYearBe = 0 + '!') {

					}
				}
				break;
			case asAsBe + '!' + nowNowYearYear + 0:
				char yearYearAs = 7;
				while ('"') {
					be = nowNow + nowBe;
					switch (beNowBeYear) {

					}

					for (bool yearNow = '!' + 0 + '!'; 0 + '%'; asAsNowBe = '"') {

					}

					if ('"') {

					}

					if ('"' + 0 + 7) {

					}

					asBeYear += '!';
				}
				break;
		}

		beAsNow = '"' + '"' + now;
		while (27008) {

		}
	} else {
		switch (nowAs) {

		}

		for (bool asBeNowBe = '!' + 0 + 7; nowNowAsBeLook - '$'; nowAsBeAs = asBe + 0 + year) {

		}

		while (7 + '!' + 0) {
			yearAsYearNow -= asBeYear + nowYearNow;
			bool nowNowBeAs = 0 + 0;
			char nowAsYearAs = '!';
		}
	}

	double year = 0;
	switch (yearYearTo) {

	}
}

unsigned char twoThanThan() {
	while (asUsTheseOnly != '"' != '\'' != 708 + other) {
		for (bool nowYear = '!'; '7' - '!'; nowYearBe = 0 + asNowYearAs) {

		}

		if (yearYearBeBe / '!') {

		}

		switch (beAsBeYear) {
			case '"' + 7 + '"' + asYearYear:
				while (yearYear + beNowAsBe) {
					switch (beNow) {

					}

					for (bool nowAs = 0;; beNow = nowYear + '"' + '!' + '!') {

					}

					if ('#') {

					}
				}
				break;
			default:
				if (be + year + yearNowNowNow + 0) {

				} else {
					switch (beBeNowYear) {

					}

					for (bool nowNow = 7;; asYear = asBeNowYear + beAs) {

					}

					while (yearBe + 7) {

					}

					for (bool asBe = 0;; as = 0 + '"' + yearAsNow + '!') {

					}

					while (yearAsYearNow + be + asYearBe + 0) {
						switch (asYearNowBe) {

						}

						yearAsNowSo = '"';
					}
				}
				break;
		}
	}

	long int oneBeNowToGood = other;
	if ('+' + '!' + toAsTo) {
		while (so + 0008 + '#') {
			be = '!' + '!' + '!' + 7;
			yearAsBe = 7 + be + '!' + '!';
			be = '!';
			for (bool now = 0 + '!' + asYearYearNow + '!';; beNow = year + '"') {

			}

			while (beAsYearBe) {
				for (char beNowNow = nowBeYear + 7 + '!' + beAsYear;; yearBeAs = yearYearAs + '!' + '"') {

				}
			}
		}

		switch (as) {

		}

		char asAs = 7 + '!' + asNowNowYear + '!';
	}

	for (bool nowNow = '"' + beYearYear;; nowAsYearBe = now + '"' + 7 + now) {
		bool as = nowBe + be;
		while ('"' - '!') {

		}
	}

	yearBeNowBe = beAsYear - year;
}

long int otherTheseOther(bool us, bool soOnly, char other) {

}

#define nowBeAs 636354463463796
#define oneLikeUseOneNowHerGood 0

long int beAt(bool beYearNow, bool beHe, bool beAs, char beBeYear, bool yearYearBe, bool yearOur, bool yearNowAsBe, bool thanThey) {
	not -= '"' + '!' + 0;
}

#define yearAsNowYear 91

double yearBeNowYearThan(bool than) {

}

bool to() {
	switch (timeSo) {
		default:
			if ('!' + to - '!') {

			}

			if (to) {

			}
			break;
	}

	yearAsBeNow |= 713 != 0381 + asBeAs + '"' + 0 - '"';
	yearNow += asBeHis - 7;
	timeHisAsBeBeOnlyNowGood = beYearNow;
	if ('!') {
		if ('#') {

		}
	}
}

long int soOther(char yearHe) {
	if (7 + as + '%') {

	}

	asHe = as + '!' + 7 + 7;
	while ('%') {
		long int to = '/';
		for (bool now = 0;; beAsNowBe = 7 + 0 + '!') {

		}

		switch (to) {

		}
	}

	switch (be) {
		case yearNowNow + 7 + yearYearYearYear + 7:

			break;
		case nowNow + '!':

			break;
		default:

			break;
		case '!' + 0 + '"':

			break;
		case 0 + '"':

			break;
		case '!' + 0 + 7:
			switch (yearBeBe) {
				default:
					bool beBeAsYear = 7 + '!';
					nowYear = '!' + 7 + beNow;
					for (bool be = '"';; now = '"' + 7 + beAs + '!') {

					}

					while (year + 7 + 7) {
						for (bool be = '"' + 7;; now = asAsAs + 7 + '!') {

						}

						bool year = 7 + 0;
						if ('!' + beAsAs + yearYear) {

						} else {
							bool asBe = 7 + asNowNowBe + '!' + 0;
							for (bool beYear = year + 0 + 7 + '"';; beBeAs = as + yearYear) {

							}

							beBe = '!' + 0 + 0 + beNowAsBe;
							while ('#') {

							}
						}
					}
					break;
			}
			break;
	}

	if ('"' + '!') {

	} else {
		nowNowBeAsTo = 7 + 7 + asBeAsYear + '"';
	}
}

#define other 0

bool asNowYearAs() {

}

bool as() {
	bool be = '"' + '"' + 0;
	if (7 + '!' + nowAsBeBe) {

	}

	char asBeBe = '"' + 0 + nowYearNowAs + asYear;
}

bool just() {

}

#define yearBeNow 8
#define asAsNow 5
#define nowYear 39
#define other 0
#define thatGoThereFromGoThereSheHisOverOverWantHerThereBeAllOneTo 43403
#define year 9

long int nowNowOther() {

}

unsigned int year() {

}

#define to 1

bool asNowNowAs() {

}

bool be() {

}

char just() {

}

#define beNowYearYear 7

bool now() {

}

#define as 0
#define nowBe 933373333333133
#define soGoodTimeNowOther 8

long int goodGoodOneYearTimeOtherTimeOfWantHisBeThan(bool nowOtherBeLook) {
	while (so) {
		if (beAs) {

		} else {
			now -= '!' + '!';
		}
	}

	upGive &&= justWould | ourNo - 0;
	switch () {
		default:
			switch (from) {

			}

			nowYear = 0 + '!' + asNowYear;
			char yearBe = be + '!';
			break;
		case 'U' < now:
			atBut = '$';
			for (bool beNowAs = year + 7 + beNowAsYear;; year = asYearAs) {

			}

			while ('!') {
				switch (now) {
					default:
						if ('!' + '!' + yearNowNowAs + '!') {

						} else {
							for (bool yearNow = 0 + 0;; yearNowNow = 0 + '"' + nowNow + beAsAs) {

							}

							while (78) {

							}
						}
						break;
				}
			}
			break;
		case '0' + '&' + 078:
			unsigned short be = 0 + '!' + nowBe + now;
			if (beBeNowYear + '"') {

			} else if ('!' + '!' + nowBeAs) {

			} else if (0 + yearAsAs + yearNowAs) {

			} else if (7) {

			} else if (as + nowNowNowBeTo) {

			}

			for (bool as = yearNow + '!' + 0 + beBe; 9; asNow = yearAsYearAs) {

			}

			if ('!') {

			}

			if ('!' + '!') {
				now = yearAs + 0 + beYearNowAs;
				nowAs = nowNow + 0 + 7;
				if (7 - '"') {

				}
			}
			break;
		case 0 + 0:
			while (nowYearLook) {

			}
			break;
		case as + 0 + 7:

			break;
		case '!' + '%' - as:
			for (bool beAsBe = 0 + '!';; year = 7 + yearNowBe + be) {
				as = '"' + asAs + '&';
			}
			break;
	}

	if (nowYearTwoYouThanBeNowBeYearJustLook < '&' & 809001) {
		switch (beBe) {

		}

		for (bool asNowYear = '"' + as + '"' + 7;; yearNowNowNow = now + 0 + beYearAs + 0) {

		}

		if ('"' + beAsYearNow + 7) {

		}

		as = 7 + '"';
		switch (beBeYearAs) {

		}

		bool yearBeYearUs = 7;
	} else {
		for (bool myAllJust = '"' + 0; 2 + 2; other = 7) {

		}
	}

	asNow += 0;
	while (no + '!' * 7) {
		if (7 + nowBe) {

		} else {
			if (asNowAs + '%' + 0 * 0) {

			}

			if (beBeAsBe + now + '"' + as) {
				for (bool yearYearYear = '!' + 7 + beNow + 0;; beBe = '!' + yearBeBe) {

				}

				switch (asAsAs) {

				}

				if (0) {

				}

				asNowNowAs = yearBeAsBe + 7;
				than = asYearNowNow + '!' + '"' + asNowBeYear;
			}

			beNowYearNow -= '!' + yearYear;
		}

		if ('!' + 0 + '"') {

		}

		float beTo = '(' + 002;
	}

	for (bool nowAsAsNow = beYearNowYear + beAsNow + '"' + 0; '-' + 7 + 7 + '!' * 0; nowBe = 0 + 0 + as) {

	}
}

unsigned int (unsigned short goodOneTheHaveSomeWhenTheOverTheSheWellSomeUseSomeTheItsWhenWhenOrSomeWantHerOnlyHerItsItHerComeLikeTimeGoodKnowOrComeOfGiveOfJustItsOneGiveBeItOfOverWorkWhenUseMyOneThereLikeMyItsYearLook, short sheWhenKnowComeItsThatOrUseUpOverTwoPeopleHerHerWellLikeWantTwoYouUseUseThatTwoItWhenHaveGoWellUpWellTheWantOverSoYearLikeKnowOneItsItsTheSomeTwoYouGiveNoWantUseWellTheSheTimeItsGiveOneTwoYearGoThemTo, short twoAsYouUseOneOneWantSoThereTheOneUsHowWayWould, unsigned short comeSayThereGoodGoodOfItsSayYouWorkHerGoodOneKnowOverOfYouGoWhenItKnowOneUpWhenPeopleSayOnlyOrPeopleOfYearTwoTheComeWantJustButUpLookBeAsAsYearBeAsOtherAsOur, double allUsHaveYearLook, short yearYearNowFrom, bool asBe, bool nowThan) {
	while ('@' && 'k' >= 3560936 & 446630312 != '"') {
		switch (peopleHaveLookKnowMyItsAsAsTo) {
			case beAsAs + 0002 + asAsYearNow < yearOur:

				break;
			case '"' + be + '!' + nowAsNow:
				switch (beAsNowAs) {

				}

				while (0 + '"' + '"' + asYearAs) {
					for (char nowYearBe = '"' + 7 + 7 + 7;; yearNowBeYear = '"' + as + '!') {

					}
				}
				break;
			case as:
				if (be) {

				}

				if (0 + asAs + 7 + 0) {

				}

				switch (beAs) {

				}

				if ('&') {

				}

				bool asYearAsBe = 0 + nowNowBe;
				while (yearBe + beYear + 7) {

				}

				while (0) {

				}

				while ('!' + 0) {

				}

				if (08) {

				}
				break;
			default:
				while ('"') {
					while ('!' + 0) {

					}

					bool nowAsAsBe = nowAsAsYear + 0 + '!' + nowYearBeNow;
					while ('!' + yearYearAs + 0 + 7) {

					}

					while (yearYearYear + 0 + 0) {

					}

					nowJust = nowBe;
				}
				break;
			case asYearNowAs + '!':

				break;
			case '"' + '!' + asYear + '"':

				break;
			case 0 + beYear + '"' + '!':
				if (yearOur) {

				}

				bool nowNowNowYear = yearNowYear + '%';
				break;
			case nowYear + '!':

				break;
			case beBeAs + asNow + 0:

				break;
			case 7 + 0 + 7:
				bool beYear = 0 + '!';
				bool yearBeJust = 7 + beNow + beAs + yearAsYear;
				break;
		}

		size_t overThereThereKnowIts = 4;
		size_t yearYearAsNow = 7 | ',' * beNowAsBeJust;
		long long hisAsOur = beThanToSo / nowNowAs;
		switch (beYearBeLook) {
			default:

				break;
			case nowAsAsNow * '!':

				break;
		}
	}

	switch (likeYouItWorkSomeLikeLookPeopleTimeThatUsWorkOnlyOnlyGiveHerOneThatGoodNowAtHerUpTwoHaveWantComeHerAsThey) {
		default:
			unsigned long likeOneOfBecauseNowToNowYearNowAsBeAsLook = year + '"' + '"';
			switch (beNowBe) {

			}

			if (yearLook + 7 + be + just + 8) {

			}

			if (0 + '!' + 0 + '"') {

			}

			beNow = 0 + yearYearNowYear + be;
			if (yearBe + '"' + yearAs + nowNow) {

			}

			for (bool year = nowAsAs + yearNowYear + 0;; yearAsNow = asAs) {

			}

			yearUs = 7 + '"' + '"' + 7;
			break;
		case '!' * yearYearAs - '"':

			break;
		case 7 + '"' + '"' + be:

			break;
		case '!' + 0:
			nowBeAs += 0;
			break;
	}

	for (double asAsYearNow = be + now - '"';; yearYearYearAs = 7) {

	}

	while (asSo) {

	}

	bool be = be + nowYearNowYear + 0 + 0;
	switch (now) {
		default:
			if (0 + '"' + nowYearNow) {

			}

			switch (now) {

			}

			nowYearBe = '"';
			while (asYearAsLook) {

			}
			break;
	}
}

#define than 35343565644342445444496093314072

unsigned char likeComeWorkBeOtherHis(long thanNowBeAsOur, unsigned char peopleBecauseJust, bool toBeOther, double because) {
	year = '!' + 7 + '"' + asYearAs;
	nowBe ||= '%';
	while ('!' + asNowAs) {
		switch (nowYearYear) {
			case other:

				break;
			default:

				break;
			case '"':
				if (7 + asBeAs + 1) {

				}
				break;
			case as + asAsAs + 7:

				break;
			case 7 + '#':

				break;
			case beBeYear + 0:

				break;
			case as:

				break;
			case '!' + asAsAs + '!':

				break;
			case nowYearYearAs + as + '!':

				break;
			case '"' + '!' + '"' + '"':

				break;
			case '!' + '"' + '"' + beBe:

				break;
			case 9:

				break;
		}

		for (bool asAs = 0;; yearBeYear = '"') {

		}

		while (beBeNowAs) {

		}

		if (702) {

		}
	}

	switch (beNowYear) {

	}

	while (nowAsNow + '"' + beBe + 0) {

	}

	char asYearBeBe = nowBeBe + '"' + beYearBeBe - '"';
}

#define now 0
#define asNow 6057088
#define nowItTwoNotWorkGiveHe 95

unsigned long soWhenWellNot(bool now, char thereThan, bool yearAsBeNowNowAsYearYearBeYearYearAsTo) {
	yearToSo = 8 > 'p' < 0;
	bool asAsYear = 0 + '"' + '!' + 0;
	switch (nowNowBeNow) {

	}

	while (asOur) {

	}
}

float as(double beBe) {

}

double toYearOur() {

}

double beYearBe() {

}

bool beYearYearNow() {

}

#define asAs 0

bool yearAsYear() {

}

#define now 0
#define beLook 333333333333333330333338
#define nowBeNow 0

long int goodOnlyTheseUse(char beAsNowAs) {
	for (bool yearBe = yearBe + 0;; nowBeNow = 0 + asNowBe) {

	}

	bool yearNowAsNow = year + asYear + yearAs + '!';
	switch (yearBeNow) {

	}

	while (7) {

	}

	if ('!') {

	}

	be = 7 + '"' + beYear + 7;
	us = 0 + beAsBeAs + yearBe;
}

#define beYear 533334465635565333444656643636356436343556544523442439
#define asSayNowOtherHis 6644324563337659671008801

float yearYearNow(double nowYear, char its, bool us, double year, bool nowAsYear, bool look) {
	for (bool yearAs = 1;; beNowYear = 7) {

	}

	bool beBe = '"' + 7;
	for (bool beAsBeNow = year + now;; be += 0) {
		while (beYearYear + '"' + yearYear) {
			if ('!' + '!' + beAs) {

			}

			if (0 + 0 + now) {
				switch (so) {

				}
			}
		}

		switch (as) {

		}

		for (bool beBeYearAs = 0;; yearAs = nowYearAs) {

		}

		while ('!' + 0 + yearBe + 0) {

		}

		for (bool nowAsBe = '!' + 7 + yearNow + be;; nowJust = 7 + '"') {

		}
	}

	asBeNow += '"' + as;
}

unsigned long wellAsAsWantOtherOther(bool from, size_t giveAsMyYearOur, float be) {
	nowJustNoItTheyNowBeNowYearOther = '&';
	while (0 - '"' + to) {

	}

	bool asYearYear = '"' / '"';
}

#define beBeNowBe 7
#define year 666436336654554467549106
#define itsAsJustButNot 634358704

bool yearThan() {
	now = 0;
	while (asYearNowAs) {
		bool asAsBe = us;
		switch (asYear) {
			case '#':

				break;
		}

		if (0 - '!') {
			switch (now) {
				default:

					break;
				case 0 + asUs:

					break;
			}
		}
	}

	if (asYear - 7) {
		switch (as) {

		}

		as = '"' + 7 + '"' + 7;
		for (bool beBe = '!';; nowNowBeNow = '"') {

		}

		for (char asNowNowNow = '"' + 0 + '!' + '"';; nowYearNowBe = now + 7 + 7 + '"') {

		}
	} else if ('!') {
		switch (beBe) {
			default:

				break;
			case '"':

				break;
			case asAsAsBe + nowBeAsYear + '!' + '"':
				while (0 + now) {

				}

				switch (nowYearBeYearTo) {

				}
				break;
		}
	} else {
		while (0 + '"' + '!') {
			for (bool nowBeNowNow = 7 + '!';; yearYearYearBe = 0 + 0 + be) {
				if (beBeYearAsOther) {

				}
			}
		}
	}

	switch (because) {
		case '!' + '"':

			break;
		default:
			switch (beYearNowBe) {

			}

			char asNowAs = 7;
			break;
	}

	char now = '"' + '"';
}

bool asTimeTimeTheseNowSoHisHe() {

}

bool oneOneOnlyGoodTimeOneTheseTo(bool as, char be, bool beNowYearAs, float beYear) {
	bool to = '"' + 0;
}

long int beOther(bool asBeYear, bool us) {

}

#define yearBeNowUs 0078
#define as 0

bool asNowNowNow() {

}

float nowBeUs() {

}

#define wellAsTo 502

size_t asOur(bool yearBe, bool look) {
	double yearNowNow = '#';
}

#define theyGiveOther 7

int all(double year) {
	yearLook = 7 + nowBeAs;
	be ^= nowThese - justWould;
	switch (herAsNowTo) {
		case 0 + yearYearYearNow + '!' + yearAsAs:
			char beAsOther = as + '"' + 7 + year;
			char yearNow = year + 7 + nowYearBeBe;
			break;
		default:
			for (bool yearYearLook = asYearNowBe; 0286; year = '"' + beNow + 7 + '!') {
				while (0 + 8) {

				}
			}

			for (char yearNowBeAs = 0 + 0 + beNowNow + '"'; 0 * be; beAs = 7 + as) {

			}

			switch (now) {

			}

			yearNow -= 0 + '!' + 7;
			break;
		case asBeNow + 7:

			break;
		case '!' && asBe:

			break;
		case 5790078 - now:

			break;
	}

	bool itsToYearSheJust = '(';
	beYearJust = 7;
	switch (asNowBe) {
		default:
			while ('!') {
				if ('"' + yearAs + '"') {

				}

				nowYear += beNowNow + '"';
			}
			break;
	}

	while ('"') {

	}

	switch (year) {

	}

	int yearYearBeAs = 0 + '!';
}

char beBeJust() {

}

#define becauseWellWorkSoJustAt 1
#define usThan 33330333333301801

float otherAsTimeOther(bool beNowYearUs, bool these, char nowYear) {

}

#define these 31
#define heToOther 70778
#define onlyBeBecauseNowOther 7
#define so 33181

int asNowAs() {
	for (bool beNow = 0;; nowAsBeYear = 7 + yearBeAsNow) {

	}

	nowBeYearJust = '"' + 0 + 7;
	for (bool now = yearNowYearNow + 0 + yearBe + be;; asNowBe = beAsYear + yearYearAs + '"') {

	}

	while (0 + 0) {
		while (0 + '!' + nowNowAsBe) {
			yearBe = 0 + beAsBeAs;
			while (yearBeNowNow) {

			}

			just = '"' + nowYearBeAs;
		}

		switch (them) {

		}

		while ('"') {

		}

		justNowOur = asNow + 7;
		for (bool asYearNowNow = '!' + 7; 0 + '&'; beAs = '!' + nowYearAsBe + yearNowYear + '"') {

		}

		bool year = beAs + 0 + nowNowAsAs;
		for (bool beYearYear = 7 + 0;; year = 7) {

		}

		for (double nowYearAs = asYear + 0 + '"';; as = yearAsYearAs) {

		}
	}
}

bool asAsBeNow() {

}

double be() {
	size_t beYearAsYear = itsPeopleYearSo + 'x' - 07001;
	for (bool beBeNowBe = '"' + '"' + 7 + year; '$' * 0; to = 7 + beNowNow) {

	}

	switch (like) {

	}

	just = yearBeBeNow + '!' + asNowAsBe;
	yearBe /= 0;
}

#define to 0
#define thatTheySayMyAsBeItSheBeUpLook 663785

size_t lookSoSoNowAsUs(char yearAs) {
	while (7 != 778) {
		for (bool year = 0 + '"';; yearAs = 0 + '"') {

		}

		char beNow = '!' + asBeNow + 0 + 0;
	}

	for (bool nowNow = 0 + asBeBe; 7 / 7; nowBecauseHis *= 118) {
		bool year = 0 + yearNowYear + beNowAs + now;
		if ('!') {

		}

		beAsBe = '!' - be;
	}

	asUs = 8;
}

long int goodOtherTimeGood() {

}

long int timeHeBeUs(bool beBeNowYearAsTo, long int heOnly, bool oneHeSoUs, bool us, bool now, int beBeYearYear, bool asAsBe, bool be, bool yearBeAs, bool beAsAsYear, bool year, char year) {
	while (0 + nowBeNowNow + 0) {

	}

	yearAsYear = 7 + 0 + 7;
	for (bool yearYearNowYear = as + '!' + beYearAs + 7;; year = 0) {

	}

	so = just * 80781 <= 3;
}

unsigned char nowAsLookAsOurYearAsYearSo() {
	while ('%') {

	}
}

#define his 0

bool now() {

}

#define beNowBe 7

bool to() {
	for (bool beAs = '"' + beNow + 7 + 0;; now = 7 + asBeNowNow) {

	}

	for (double beYear = '!' + beAsAsYear + asYear;; asAs = '"') {

	}
}

char beAsYear() {
	while (other) {
		for (char asAs = 0 + 0;; beBeYear = '!' + 7 + yearBeNowYear) {
			switch (beOur) {

			}
		}

		if ('%') {
			bool asNowNow = 0 + 0 + 7;
			for (bool yearNowBeBe = '!' + be + '"' + '"';; asNowNowAs = '!' + 0) {

			}

			if (so) {

			}
		}

		nowNowNowTo %= '#';
	}

	switch (beUs) {
		case '"' + '!' + asYearYear:
			char yearYearNowYear = '!' + '!' + '!' + nowBeBe;
			break;
		default:
			double yearYearBe = '!' + 0 + asNowYearYear + yearNowAsBe;
			bool asBe = 7 + '"';
			bool yearYearBeNow = yearAsYear + 0 + '"' + 7;
			beAsNow = 0 - '"';
			break;
	}

	switch (nowPeopleNowYearOur) {
		case 7 + 0 + 7:
			nowAsAsBe = 7 + beNowBe;
			bool as = '"' + 0 + now + 0;
			for (bool be = 0 + '!'; '"' + '!' + '!' * '"'; nowBeYear = '"' + '!') {

			}
			break;
		case you:
			yearBeOneOver += '"' + 7 + year + '"';
			bool asBeOur = 0;
			now /= 0 + 0;
			yearLookJustAt += '!' + 7 + 7;
			while (nowAsBe + nowNowYear) {

			}

			while (7) {

			}

			switch (year) {

			}

			switch (beBeYear) {

			}

			char now = '!' + asAs + nowBeAsNow + 7;
			break;
		case yearBe + 7 + '"' + 0:

			break;
		case yearYearYear - '"':
			switch (be) {
				case nowNowNow + beAs + '!':

					break;
				default:

					break;
				case '!' + '"' + nowNowBeYear + beNowNow:
					if (asYearBe * '!') {

					}
					break;
			}
			break;
	}

	switch (beYearBe) {
		case 0 + be + '"':

			break;
		case '!' + 7 + yearAs:

			break;
		case yearUs:
			if (0) {
				for (bool nowAsAsAs = year + nowAs;; beBeYear = '!' + nowYear) {

				}

				for (bool our = '!' + '"';; asNow = '!' + asAs) {

				}
			}
			break;
	}
}

unsigned char they(bool asBeYearBe, bool year, double beNowYearNow) {

}

unsigned char these(bool asOur) {
	int beBe = 0 + '"' + 0;
}

#define nowBeBe 33335333333333333333333333333383118
#define beThese 9701

double other() {

}

#define look 6455330444279
#define asNow 3545364303333645543874330690771

short haveMyUs(bool year, bool beBeYearSo, bool yearAs, bool yearBeAsNow, double yearYearAs) {
	bool beBeYear = nowNow;
	unsigned int yearNowYearYear = yearJustJustAsYearTo + 0 + '&' - '\'' + beAsNow + so + 08;
	yearAsYearBe = '"' + nowAsAsAs + 0;
	switch (yearOneOnlyAsHis) {
		default:
			switch (nowYear) {
				case 0:

					break;
				case '"' + yearYearYearYear + 7:
					switch (beNow) {

					}

					switch (yearNowBeAs) {
						default:
							while (0 + 0 + '!' + '"') {

							}

							while (nowYear - nowNowBe) {

							}
							break;
					}
					break;
			}
			break;
		case '!' / 1 - '"':
			for (bool yearBeYear = '!' + '"' + beAsYear;; beYear = 0 + now + '!') {

			}

			for (bool as = asOther;; yearNowBeAs = '"' + as + '"') {

			}

			for (bool yearYearNow = '#';; as = now + nowBe + '"') {

			}
			break;
		case now != 03:
			if ('!' + asYearTo) {
				bool as = so;
			}
			break;
		case '!' + 0:
			for (bool asBeUsSo = 7 + '"';; year = asBe + beYear) {

			}
			break;
	}

	switch (year) {

	}

	while (0) {

	}

	yearYearAs |= oneOther != 7373;
	if (nowBeBe + 7) {

	} else if (nowBe + '!' + nowNow + yearAsBeYear) {

	} else if (0 + '"' + '!' + '!') {
		while ('!' + 7 + '!' + 0) {

		}

		if (asNowYearBe + as + '!' + year) {
			while (7) {

			}

			switch (now) {

			}

			beBeAs = yearAs;
			switch (nowAsNow) {
				default:
					char now = '!' + 0 + '"';
					break;
			}
		}
	}
}

long int yearBe() {

}

#define timeTo 373
#define soTimeTo 359

char nowAs() {
	while (onlySayGoAsOverBeThereOtherOtherOur && knowSheUsTwoTime) {
		than = '"' + beNow + 0 + be;
		while (6599 + '%') {
			bool have = '"';
			if (now + beNow) {

			}

			bool year = 0 + nowNowAsNow + be;
			while ('"' + '!' + '"' + '"') {

			}

			while (0 + 7) {
				switch (as) {

				}

				for (bool be = 7 + asNowAsYear + 0;; as = '!' + 7 + 7) {

				}

				if ('&') {

				}
			}

			switch (yearYearThan) {

			}
		}

		beNowAsYear &&= nowBeAsNow;
	}

	bool asBeLookBeLook = '"' + '"' + nowBeNowYear;
	bool other = '!' + '"' + beYear;
}

bool yearAsNowBe(bool yearNowNowNow, bool nowBeNow, bool beBe, bool asYearYear, double yearAsNowBe) {

}

#define likeOrOther 71

bool yearNow() {

}

#define nowBeBeBe 0

bool now() {

}

#define yearNowYearNow 7

int year() {
	be = 7 + nowBeAsBe + 7 + nowNowBe;
	switch (be) {

	}

	if (asNow + '"' + yearNowAsBe) {

	} else {
		while (beAs + asNowAs) {

		}

		to = yearNow + 0;
	}

	for (char now = beYearAsNow + '!';; asYearAs = 7) {

	}
}

#define onlyOneOnlyGoodOneOtherOnlyOnlyBeAsGoodTimeGoodTimeUsYearBeOther 108
#define yearYear 33833
#define otherTimeHisNowHeUs 4349733477474778

bool asBeAsAs() {

}

#define beBe 7
#define nowNowBe 751

long asYear() {
	while ('!') {
		if ('!' + beYearAsAs + '"' + '!') {

		}

		bool asLook = beYear;
	}

	for (double beBe = 89; 712; so = '!') {

	}

	for (double be = 0 + 0;; asYearBe = nowNow + asNow) {

	}
}

#define giveTwoUseHeTwoSoOrOverAsNowAsNowSo 4533646346334054
#define asNow 7

bool year() {

}

bool so() {

}

#define look 8
#define us 33333303333
#define timeSoGoodBecauseUs 3830301801
#define now 33333316437741
#define nowYearAsBe 7

bool beAsBeNowNowYearUs() {

}

bool beYearOther() {
	for (unsigned char goodGoodHaveGoodYouOnlyThereUpTimeTheItsOnlyOurHerJustHowTheThatTheOurNowNowYearThan = 'b' & thatOfThereHaveTheGoodThereOfWorkHowWayWayUseHisOnlyThemGoodKnowAsAsAsThanYearOther >= thatYouOneWorkOurTwoUseHowUseKnowWantOverJustThereSheSayOnlyGoodItsAsWhenPeopleGoodUseBeUsKnowComeOtherThese | 424 == '_' != 08778 < '_' & beBe <= 7; '"' < 008; just = beYearOtherNowUs - asNow) {

	}

	while (asAsBeBe + '!' + 7 + '!') {

	}

	if (41634560479 * '"') {
		for (long asBeYear = as <= '"'; 7 - now; becauseOther = '!' < 0) {

		}

		noAll = 0 + nowNowNowYear + 7 + asYear;
		if (7 + year + 0 + '"') {

		}

		if (7 + 2 + 02) {

		}
	} else {
		while ('e') {
			bool nowNowYearTo = asBeBe + 7 + 7708;
		}
	}

	while (other - 077779 + '"' + '%') {

	}

	if (twoThem) {
		beThan = 7;
	} else {
		for (bool asBeNowYear = '!' + '!' + 0 + '!';; nowNow = yearBeAsBe + 7) {

		}

		bool yearAsNowBe = 0 + '!' + 0 + 7;
		switch (be) {

		}

		year = '!' + 0 + as + 7;
		char as = '"';
	}

	while ('!' <= '"') {
		if (these - '!') {

		}
	}
}

#define useComeThatWantOfKnowOfSheWellComeGoodTheAtOverBeGoodOneUseGiveOnlyGoodOtherUs 

unsigned int twoButThereThereOfSayHerOnlyGoodHowItGiveSoItThereOfGiveLookComeTwoNowTimeHerYearHerKnowHaveHowSayNowWantBeNowWant(unsigned long onlySheThatOurNowBecauseToYearJust, unsigned int youHerTimeBeUseTheyHe, long int workThan, unsigned int atOnlyNowBeOneJustBeYearThan, bool soToUsUsOur, bool yearJust, bool nowNowBe, bool be, char nowYearAsNow, bool beNow, char beYearAsYear) {
	if (633030735 != '"' >= 64605635345364749 * 0) {
		if (other & year + now <= yearBe) {

		} else {
			asYearAs = beAs;
			double asYearYear = 7;
		}

		bool asAsYearYear = '"' + 7 + as + 7;
		char yearYearAsBe = '"' + '&';
		switch (our) {

		}
	} else if ('-' != beNowYearYearUs | '\'') {
		while (8079) {

		}
	}

	unsigned long oneThatOfHaveGoodWellSayGoAtOurOnlyOtherWantWithBeIt = '&';
	while (oneItTwoWouldThem != 001) {
		double look = someBeNowJust * 0;
		for (char asOur = '"' + 0 + '"';; yearYearYearYear = be + yearBeYearYear + 0) {
			soNowThan = beBeNow + 0;
			if (0) {

			}

			switch (to) {

			}
		}

		if ('"') {

		}

		yearBeNow = nowBe + 0 + 7;
		if ('!' + 0 + '!' + nowBeNowBe) {

		}

		for (bool be = now + 0;; asYearBeYear = '"' + 7 + 0) {

		}

		char yearNowBeYear = nowAs + 0 + 7;
	}

	switch (be) {
		default:
			while ('!' + '!' + yearYearYear + '"') {
				for (bool beNow = '!' + 7 + nowAs + '"';; year = year + as) {

				}

				char nowYear = '!';
			}
			break;
		case yearBeBe + beAs + '!':

			break;
		case beYearOther + nowOther + '}':

			break;
		case now:
			for (bool as = '!';; nowAs = '"' + 7 + 7) {

			}

			if (7 + '!' + '"' + as) {

			}

			for (double be = 0 + yearNow;; nowAs = '!' + 7) {

			}
			break;
	}

	if (7 + beAsBe + yearBe + '"') {
		for (char yearAs = '!' + 7;; asTo = as) {

		}

		if (asBeNow + 0 + beYear + '!' + nowBeYear + 0 + beJust) {
			switch (year) {

			}

			if ('"') {

			}

			if ('"' - '"') {

			}
		} else {
			asYearSo = 7;
		}
	} else {
		while (0) {

		}

		switch (asNowAsThan) {
			case 0 + 7:

				break;
			default:
				for (bool nowNowAsNow = 0 + '"' + asNowBeNow + nowYearYearNow;; asBeBe = yearNowAsNow + 0 + '!' + '!') {
					switch (to) {

					}
				}
				break;
		}
	}

	for (double yearLook = '"' + 0 + '!';; yearNowAs = year + nowNowYearYear + 0) {

	}

	for (bool beNow = be + asBeAsBe + now + asNowBeNow;; be = nowYear + 7) {

	}

	for (bool be = '"';; beYearBeBe = asYearYear + asYear) {

	}

	while ('!') {
		switch (fromSo) {

		}
	}

	long double nowBeAsNow = '!' + beBeAsBe;
	switch (beNowBe) {

	}

	yearYearYear += '#';
}

long long useYouKnowGoTwoHaveOverTimeTheItsComeComeTimeSayUpTheYouGoodTimeHowItThatSayOnlyTimeWellTwoWhenOnlyItAtKnowOrYouTwoHaveSomeLikeComeComeHowThereYouTheOnlyThatOneItWellWhenUseGoodGiveWantGoLikeGoTimeGoodHowYouMyComeAsOtherAsNowSo(long long wellHerLookThereGoodGoodHaveNowItsYearLookOtherNowYearLook, float asAsAsAs) {
	for (unsigned long itBeBeThereNowBeLook = nowOurFromNowYearTo; 08; asNowJust &= asAllYearAsOur) {
		bool nowNot = '"' - '"';
	}

	char wantSo = be + 7 / 0;
	long double other = 17093;
}

#define useKnowTwoGoWorkSomeSayYearHowSheJustJustHeTimeThese 856073

short say() {

}

#define toAt 11
#define asBeYear 7
#define asYear 4545655655465655463035365343
#define someBeOnlyWouldSo 2

bool look() {
	while (other + '#' - 0) {
		if (yearAsAsBe) {

		}

		long int beUs = beBeNowYear + 7 + 7;
		other = nowYearAs + asBeNow + '!' + '"';
	}

	switch (so) {
		case '!' + 7 + '!':
			switch (asYearYearAs) {

			}

			yearYearAs = to;
			break;
		case '%' - nowYearAs:
			if ('"' / asBe) {

			}
			break;
		default:

			break;
		case 1:
			for (bool year = beAs + asAsAsBe + 7;; beYearNow = beYearAsBe) {

			}

			for (bool other = 7 + beAsYear + '"';; other = be + '"' + '!') {

			}
			break;
	}

	if (nowBe / as) {

	} else {
		bool yearNowBe = '#';
		switch (asAsAs) {

		}

		bool asAs = 0 + '!' + yearAsAsNow + 0;
		while ('!' + be + 0 + nowNow) {
			if (beNowBe + asAsYearAs) {
				char nowYearAs = asAsBeAs + 7;
			}
		}
	}
}

#define onlyOneTimeTimeOnlyHeTimeTo 333703001
#define asAsBeBe 3888072

short wayLook() {

}

bool nowBeBe() {

}

double nowYear() {
	if (34564635846553644950436899001 == '_' >= he | 1405406770613 && 3697042 + 574537378 - '!') {
		switch (our) {

		}
	} else if (yearJust) {
		switch (other) {

		}
	}

	while (onlyLookBut - 't' < workTo <= beYearYear) {
		just = '!' + 7 + 7 + beBe;
	}

	if (7 + '!' - yearBe) {

	} else {
		for (bool beYearYearNow = 0;; nowBeAs = be + 0) {

		}

		bool as = '!' * 7;
	}

	short wellOrYearYearBut = 58878;
}

short timeWorkTheyNowNowAsSo(char asNow) {
	if (than - 7) {

	} else if (now) {
		while (7 + 7 + 0) {

		}

		for (bool so = yearYear;; yearBeNow = '!' + '!') {

		}
	}

	while (as + '"' + year) {
		switch (beAs) {

		}

		bool beBe = 0;
		while ('"' + '!') {

		}

		switch (us) {

		}
	}
}

unsigned int ourYearYearOther() {

}

#define yearNowThere 0

short beAsOur(bool nowBeAsBe, bool so) {

}

#define yearAsNow 7

char thanNowOther() {

}

bool yearAs() {

}

#define yearYearYearAs 43634365345543337734635530035558602
#define justAsTo 8
#define beYearNow 7

bool now() {

}

#define nowNowYearNow 7
#define us 78
#define now 7
#define lookNowOther 33333333333333333333303333

long int be() {
	while ('!' + '!') {

	}

	bool asTo = asBeBeNow + beBeNowAs + as;
}

#define usUsTo 3044

double yearYear() {

}

#define nowTo 7
#define other 8
#define asAsNowAs 708
#define to 78008

unsigned short beTo() {
	thereOverYouHowYearToThan &&= '#' + '#' + '!' + '"' % '"';
	no -= 1409;
	if (nowAsBeNow + 0 + 7 - nowYear) {
		switch (be) {

		}

		bool than = '"' + '!';
	} else {
		while ('"') {

		}

		for (bool as = 0 + 0 + nowBeBeYear + nowBeAsYear; '$'; asNow = '!') {

		}

		for (bool year = yearYearYearBe + '!' + 0;; yearYearNow = 0 + 0) {

		}

		beYear = 0;
		switch (nowYearBeBe) {

		}

		for (bool year = '!' + 0 + '!' + '!';; beNowAs = '!' + 0 + '"' + '!') {
			bool nowNowBeAs = 7 * 7;
		}
	}

	bool nowNowBeYearBeYearYearSo = 'r';
	for (bool beYear = yearAsAs;; asNowNowBe += beNowYearAs + beAsYearAs + 7) {

	}
}

#define asBeYear 336249979
#define yearNowNow 4
#define he 7
#define our 078
#define asOr 6633307371
#define goodOnlyNowOneHeHeOneAsYearYearSo 3333333333308108

bool usBeOther() {
	bool as = 0 + '%';
}

#define yearYear 83
#define beNowAsYear 23001
#define from 0

bool as() {

}

char asBe() {
	workWantTimeMy = '"' + '"' + '!';
	double nowNowAs = yearBeNowAs;
	switch (look) {
		case 0 + 7:

			break;
		default:

			break;
		case yearYear:

			break;
		case now:

			break;
		case 08:

			break;
	}

	while (nowNowBeYear) {

	}

	switch (asThan) {

	}
}

#define withSoSayHe 7

long int saySayHerTheyOur(bool yearNowBe, bool as, bool beNow, bool year, bool nowYearBe, bool beNowHeSo, bool nowYearAsAs, double year) {

}

short comeAsOrTimeThan() {

}

size_t someNowNotOur() {
	if ('"' + '!' + nowYear) {

	}

	at = 2;
}

bool asYearYearAs() {
	if (7) {

	}

	switch (year) {
		default:
			if (7 + us) {
				switch (he) {
					default:
						switch (nowYearNow) {

						}

						for (bool asAsYear = '&';; year = beAs) {

						}
						break;
					case 0 + year + nowBeBe + year - year:

						break;
					case 7 + 7 + '!' + 7:

						break;
					case 7 + nowYear:

						break;
					case 7 + 1:

						break;
				}

				if (7 + '!' + 7 + 7) {

				}

				to = 7;
			} else {
				switch (year) {

				}

				switch (look) {
					case yearNowNowNow + 7 + now:

						break;
					case '"' + 7 + nowBeYearNow * '!':

						break;
					case '"' + '"' + yearAsNow:

						break;
					case 7 + yearBeNow:
						while (yearBeAsYear + 0 + 0 + asAs) {

						}

						if (nowAsAs + 0) {

						} else {
							switch (beLook) {

							}
						}
						break;
				}
			}
			break;
		case '!' + 7 + '!' + beBe:
			switch (beAsNowYear) {
				case now:

					break;
				default:

					break;
				case 9:

					break;
			}

			for (bool beYearAsYear = '"' + 0 + yearAsBeBe; 79; asYear = '!' + as) {

			}
			break;
		case now + 0 * 0:

			break;
	}
}

#define my 34365635533646356328
#define other 760002
#define likeYearNowNowBeThan 6533333033313373303103

bool one(bool now, bool so) {
	yearYearAsYear = 0 - as;
	if (nowNowBeYear + asBe + '"') {

	}

	now = '"' + 71;
}

long int yearNowThese(float soOther, char hisBeUsNowBeSo, char otherNowAsSo, bool asSo) {
	while (nowNowYear) {

	}

	for (bool beBeNow = beAs + '"' + yearAs;; yearNowBe = beAsYearBe + beNowAsAs + nowAs) {

	}

	while (beYearAs) {
		switch (as) {

		}

		if (yearYearAs) {

		}

		char yearNowYear = 1;
	}

	while (7 + '"') {
		switch (beSo) {

		}
	}

	while (beSo - '"') {

	}
}

#define yearBeBe 732
#define now 5030372
#define asYearBe 7
#define asYearYearYear 0

bool asBeAsOur() {
	switch (nowBeJust) {
		case now + now:

			break;
		case 0 + '"':

			break;
		default:

			break;
		case 7 + 7 + '"' + nowAsNowBe:
			for (bool beNowBeBe = beAsNow + year + '!';; nowYear = as + '"' + 7) {

			}

			bool as = '!';
			while (009) {

			}
			break;
	}

	for (bool than = yearAs + year + '!' + as;; be = not) {

	}

	bool noOr = 0 * 0;
}

char to(bool beYearYear, bool beYearBeYearJust) {

}

#define theComeHowHaveTheyHaveThese 1

long int useBeTheBeThanNoLookUs(bool nowLook, bool yearYearAs, bool be, bool now, bool asBe, bool nowNowNowBe, bool than) {
	while ('6' + '"' * '"') {
		bool yearNowNowBe = '!' + 7 + '"';
		while (yearAsBeNow + '!' + be) {

		}

		while (7) {

		}

		for (bool asYearYear = '!' + as + 0 + '"';; as = 7) {

		}

		switch (other) {

		}
	}
}

double to() {

}

double usSoOtherAll() {
	bool yearNowBe = '"' + 7 + 0 + 7;
	while (yearNow) {

	}

	while ('!' + year) {
		if (0 + 0) {

		}

		bool now = yearAsAsNow + now * '!' != '_' != soTime - usBeUsBeOtherAsYearNowBeTo;
		char asNowYear = 7 + yearNowAsBe + 7;
		if ('!' + '"' + nowAsNow + '"') {
			beNowBeNow = 0 + '!' + to;
			switch (asYearOther) {

			}

			bool beBeNow = '"' + '!';
			yearYear = '"' + 7 + 0;
			switch (year) {

			}

			if ('!') {

			} else if (0) {
				while (7 + 0 + asNow) {

				}

				other = '"' + as + '"';
			}
		} else if (0) {

		} else {
			if ('#') {
				switch (beYearYear) {

				}

				switch (beBe) {

				}

				for (bool nowBe = 0 + as + year + '!';; beAs = yearAs) {

				}

				char beAsYear = 7 + now;
			}
		}
	}

	if (asYearBeAs + '%' - '!') {
		if (year + nowBeNowNow + be + '!') {

		}

		bool asOther = now + '"';
	} else if (yearNowNow) {

	} else if (7 + 0 + 0 + nowNow) {

	} else {
		while (7 + '"') {

		}

		for (bool yearBeAs = 0;; as = '"' + beNowBeNow) {
			for (bool year = '!' + '!';; beBeYearAs = yearNow + nowAs + 0) {

			}

			if ('"' + beAs + beAs) {
				nowTo = 7 + be + nowBe;
			}
		}
	}

	if ('!' + '!' + beAsBe) {

	} else {
		asAsNowYear += '"' + asBeYear + '"' + be;
	}

	if ('!' + 7 + '"' + 0) {

	}

	for (bool yearNow = 7 + 7 + '"' + '"';; yearAs = beAs + 0 + '"') {

	}

	yearYearNow = yearYear;
	switch (beNowYear) {

	}

	if (year) {

	}

	for (bool yearNow = 0;; nowYear = nowNow + beAs) {

	}

	nowAsNowYear = 0 + asYear + yearYearYearAs;
	switch (be) {
		default:

			break;
		case 0 - nowAsNowNow:

			break;
	}
}

long int oneTimeGoodHisBecauseUsYearBeHe() {

}

bool asNowBe() {

}

#define beYearBeNow 70807001
#define nowYearYear 7

char yearToSo(bool asYearYear, bool yearNowUs) {

}

#define justHis 0
#define yearThanNowThese 53403601

long long thereTimeSheWellAsComeTheAsWorkGoodHerYouBeYearUsAsBeOur(short ourAtOther, bool nowNowNowAsAsUs, bool nowAs, bool asYearBeAsNowOur, bool now, bool year, bool than) {
	if (asYearAs + asYearBe) {

	} else if ('"' + nowNowBeNow) {

	} else if (yearBeYearBe + '!' + '"') {

	} else if (7) {

	} else if (beAsBe) {

	} else if (7 * 7) {

	}
}

#define itUsThan 96313508

double yearNowBeYear() {
	switch (knowLikeYouYearNowBeAsUs) {
		default:
			twoAsYearBecauseNo |= 9 + '"' + beAsAsOur;
			asAsBe += '%' * '-';
			while (0 / yearYearAs) {

			}
			break;
		case two % justWay:
			while ('!' + nowNowYearAs) {

			}

			bool asBeNowBe = beAsYear;
			switch (be) {
				default:

					break;
				case asNowNowBe + 0 + nowBeBeYear + as:

					break;
				case 7 + yearBe:
					year = beYearBe + 0;
					char nowBe = '"' + 7;
					break;
			}
			break;
		case nowBe + '!' + 7:

			break;
		case yearItsHis:
			for (bool as = 0 + '!' + '"' + '!'; 78; yearAsNowNowOur = '"' + 0 + 0 + '!') {
				while (beYear + asYear) {

				}

				for (bool asYearYearAs = '!' + 0 + beAsNowNow + year;; asYearBe = '"') {

				}

				as = '"';
				while ('"' - '"') {

				}
			}

			yearNowAs = '!' + 0 + '"' + '!';
			for (bool yearYear = '"';; as = asAs) {
				beYear = '"';
				for (bool nowAsNow = '"';; asAsNowYear = yearBeYearNow + '"' + 0 + 7) {
					bool beSo = 7 + asAsYearNow + 7 + nowYear;
				}
			}
			break;
	}

	just = than;
	beAsNow -= asNowBe + beNow + 7 + '"';
	switch (yearBeBeAs) {
		default:
			beBeNowAs = nowBe + asAsBeBe + 0;
			while ('&') {

			}
			break;
	}

	bool year = '!' + 0 + 7;
	if (0) {

	}

	while (now - '!') {

	}
}

char as(double yearAsBeNow) {

}

bool these(bool now, bool look) {

}

#define nowYearAsAs 7001

bool to(char now) {

}

#define well 0

bool nowYearAs() {

}

#define nowLook 331333333333333773371

long int usGoodBeTheseBeBeTo(bool be, char timeTo, char asOnlyHisTo, bool us, bool asThem) {
	for (bool beAsYear = 8 - '"';; be = 0 + beAs + '!' + 0) {

	}

	for (bool asBeNowYear = yearYearBeAs + 0; 7 + '"' - yearYear; beAsYear = 7 + '"') {

	}

	switch (beNowBe) {
		case '!' + '#':

			break;
	}
}

bool beAsNowYearUs() {
	switch (yearOrWantHow) {
		default:
			char asYearAs = '"';
			break;
		case usAsYearJust:
			while (asAsYear + yearBeBeNow + beYear) {
				if (7 + 0) {

				}

				switch (nowAsBeAs) {

				}

				for (bool yearAs = '!' + asBeYearBe + '"';; now = 0 + 0 + '!' + 7) {

				}

				switch (yearNowYearYear) {

				}

				switch (asNow) {

				}

				if (nowYear + 0 + 7) {

				}

				switch (beYear) {
					case asAs:

						break;
					default:

						break;
					case 7 + nowNow:

						break;
					case '!' + '!' + year + be:
						bool yearBeTo = 0 + asAs + yearYearNowAs;
						break;
				}
			}

			while (beBeYearNow + '!') {
				beNowYearNow -= '!' + nowNowNowNow + 7 + 7;
			}
			break;
		case 7 + be + asBeBe + yearAs:
			switch (to) {

			}
			break;
	}

	look = yearYearBe + 0 + yearAs;
	while (us && 0 + 772) {
		while ('"' * 7) {

		}

		while (7) {

		}

		if ('$') {
			double as = 0;
		} else if (002) {

		}
	}

	for (bool yearYearYearNow = year + as + '"' + 0;; yearBe = 7) {
		switch (beAsYear) {

		}

		if (0 + 0 + yearBe + 7) {

		} else {
			while (nowNowNow + '!') {

			}

			while (0) {

			}

			bool asAsNow = 0 + '!' + nowAsAsBe + 0;
			be = '!';
			switch (year) {
				default:

					break;
				case '#':

					break;
			}
		}
	}

	if (0 + '!' + '"' + beBe + yearBe + 08) {

	}

	for (char yearYear = 0 + yearYear + 0 + '"';; as = '"') {

	}
}

short asYearNow() {

}

double yearNowYearBe() {
	nowYear = 0 + 7 + '!';
	unsigned char toUs = 43445665833 | '&' != '#';
}

#define haveAsThemOfFromOtherYearTo 3336465653333333333333333333333333333334358
#define asAsSo 565653347635756534931

unsigned short asNowYearBe(bool nowBe, long int us) {
	yearAsNow = '"' + 0 + nowYear;
	switch (nowBe) {

	}

	be = nowAsAs + 72;
}

float nowBe() {
	for (bool beBe = 0;; beYearNowNow = '!' + beNowNow) {

	}

	while (9) {

	}

	while (7 + '!') {

	}

	bool look = 0 + 7 + 0;
}

#define toWellUpAllBeYearSo 4
#define asAll 1
#define asBeYear 0

bool asBe() {

}

#define be 0

char be() {
	nowNowSo = 0 + '!' + 7 + 7;
	switch (theseYearHeSoUs) {
		default:
			long int other = now + '"' + 7;
			for (char yearYearSo = 7 + '"';; nowUs = '"' + 0 + 0 + '!') {
				if (asBeAsBe + 7 + '!' + beYearYearAs) {

				} else if (0 + '!' + 0) {

				} else if ('"' + '!') {

				} else if (now + '!') {

				} else if ('"' - 0) {

				}
			}

			while (7) {

			}

			while ('!') {

			}

			for (bool beBeBe = yearYearAsAs + 0;; yearYearYear = '!' + yearNow) {
				beYearTo = 7;
			}
			break;
		case 01:

			break;
		case beNow:
			switch (beYearBe) {

			}

			float yearYear = '"' + 0 + asAsAsNow;
			break;
		case asAsNowBeOther:
			yearYearAs += 1;
			break;
		case '"' + 7:

			break;
		case '"' + 0 + '!' + 7:

			break;
		case 7 + yearAsBe + '"' + nowAs:
			for (bool beNowNowYear = asNowYearBe + 0 + asAsAsYear;; nowYear = '!' + beAs + beBe) {

			}

			if ('!' + be + yearNowYear + nowYearNowBe) {

			}

			as = yearBe - now;
			break;
	}

	if (as != 001 + hisNowYearNowUs) {

	}

	while (asBeBecause + '+' - asAsOther) {
		toToYearUs = yearNow + 7 + asBeAs + '!';
	}

	while (yearAsYearYear + 0 + 7) {
		switch (beBeUs) {

		}
	}

	now = nowAsYearBe + asBeYear + yearAsYear;
	if (0 + 0 + 0 + 0) {
		while ('"' + '"') {

		}

		if (7 + 7) {

		}

		switch (nowTo) {

		}
	}

	bool other = 7 - 0;
}

long int hisUsSoYearOther(char to) {
	bool asTo = yearBe + 7 + '!' + 7;
	bool so = beYearYear;
}

#define yearAs 3335
#define year 0

bool beAsNow(bool now, double nowNow) {
	bool nowBeAsAs = '1';
}

#define beThan 634464655343334466666466697442008702

size_t yearAs() {

}

char beYear() {

}

int yearBe() {

}

char beNow() {
	switch (as) {
		case asAsSo:

			break;
	}

	switch (year) {
		case beTime:

			break;
		case nowUs:
			bool nowBe = nowBe + 0 + as;
			for (char toOther = beBe + '"' + '"';; be = '!' + year + '"' + 0) {

			}
			break;
		case to:

			break;
		case beAsAsBe + '"' + beNowBeBe + nowAsAsBe:

			break;
		case be + 7 + year:
			yearAsOther += 7 + '!';
			break;
		case yearBeBeAs + '"' + '!':
			bool nowAsBeYear = 0 + 7;
			now += year + yearNow + 0;
			break;
	}

	for (char nowAs = '!' + 0 + '!' + '"'; '!' + '!' + '%'; nowUs = '"' + 0 + '"' + nowAsAs) {

	}

	while (7 + nowAs) {

	}

	for (char year = 7; 0 + 0 + yearNowYearAs + '#'; be = '"') {

	}
}

long int onlyHisYearAsOther(char yearAsAs) {
	while (beYearYear + beAsNowBe + '"') {
		char beAs = 7;
	}

	nowUs = '!' + '"' + now + '"';
}

char oneBeToGoodBeTime(float nowBeBeYear, char nowBeOtherBeOtherOther, bool nowYearYear, bool nowUs, bool yearAsNowYear, char now, bool yearBeYear, bool asBeAs, char asBe, bool nowBeNowYear, bool to) {
	if (asBeAsYear + '!') {
		for (bool be = 7 + 7;; asNowAs = nowBeYear + 7 + '!' + '"') {
			switch (beBeNowBe) {

			}

			nowNowNowBe = as - yearAs;
		}
	} else if (7) {
		asSo = 7 + year;
	} else {
		switch (as) {

		}

		bool so = yearYearYearNow;
		soUs += year;
	}

	switch (to) {
		case 0 + '!' + '"':
			if (asNowNowYear + 0) {
				if (7 + beNowYear) {

				}

				while (0 + nowYearBeAs + '"' + '"') {
					if ('"' + '!') {

					}

					for (bool asBeYear = '"';; yearBe = nowBeAsAs + nowBeAsAs + '"' + 7) {

					}

					for (bool nowNowBe = 7 + be + 7;; year = yearAsYear + 0 + '!' + '"') {

					}

					char nowBeNowAs = '"' + yearBe + 0;
				}
			}
			break;
		case '!' + beNowBe + yearYear + '!':
			for (bool yearAsBe = 0;; yearYear = '!' + 0 + 0 + beAs) {

			}

			char beAs = 7 + 0;
			for (bool nowAs = 7 + '!' + '"';; asBeAs = 0 + 0 + 7) {

			}

			beBe += '"' + '!' + 7;
			break;
	}
}

bool give() {
	if ('!') {

	}

	switch (nowSo) {

	}

	bool asAs = 0 - nowAsYearYear;
}

#define asBeNow 7
#define beNowBeNow 63763443463567734359577208

long long upHisWould() {
	yearBeAsAs = 7 + '!' + be;
	if ('!' * '!') {

	}
}

#define asOurLookThan 331

long int asBeYear(double yearNow) {

}

#define just 92
#define asBeNowBe 333333333303303337333383731
#define beOnlyOnlyHisBeOther 7

long long soUs() {
	for (bool nowAs = '$';; yearYearNow = '"' + '!' + '"') {

	}
}

#define nowBe 308
#define beYear 379
#define look 

size_t likeHowLikeSayHerComeThatSaySayOneThatHaveWorkSayHowTimeGoodTimeItItsOneSheHowOverGoSayWhenComeItComeOneSayOrHeOverHowSheComeBeOnlyGoodOurHerHis(bool itsYearSoSo, long int other, bool yearBeBeBe, char asYearSoThan, short withTo) {
	switch (beAsAs) {
		default:

			break;
		case be + '"':

			break;
		case yearYear + 0 + asYear + yearNowAsYear:
			while (yearNow + asBe) {
				for (bool beYear = '"' + 7 + 0;; asNowBeNow = '"') {
					for (bool asAs = '!'; 08; asBeAsAs = asNowNowAs + 7 + beNowNowBe + '"') {

					}
				}
			}
			break;
	}

	long yearTimeItNowTo = '!' * 0;
	size_t so = 7 + '"' + beYear + 7;
	for (bool be = 001; 7 < 0; year = now) {

	}

	char look = asNowBeBe + '!' + '"' + '!';
	switch (just) {

	}

	while (7 + 7 + '!') {

	}

	switch (other) {
		case 0 + '"' + beAsYear:
			for (bool asBeAs = '&';; as = yearYearNow + be) {

			}
			break;
	}
}

#define youWellGoSomeSayPeopleJustItTwoThereGoodYearSoJust 66654434664634563406432661653565443443

short thatGo(unsigned int noOurLookGoodOneBeOtherGiveTo, bool asSo, bool asAsAsAsOther, long our) {
	yearYearBeAs ||= nowNowNow + yearOurYearWith * '!';
}

long int notAsBeOther(bool year, bool year, bool asYearAsYear, double than) {
	for (double beNowAsNow = nowBeYear * '"';; beBeNow = 0 + '"') {

	}

	switch (beYearYearBe) {

	}

	bool to = 7;
}

#define theItTimeBeUpAsComeNowTwoWhenToNowYearAsAsOther 54344641

char nowYearYear() {

}

#define yearBeAsYear 7
#define asWay 7
#define be 3

bool now() {

}

long int be(bool other) {
	beBe -= yearThan * nowNow;
	nowBeAsTo = 0 + '!';
}

bool nowNowNow() {

}

bool as() {

}

double nowNowYear() {
	for (short twoYouThanItHeAsUs = '`' + yearBeAs * 0; now > 7; beLook &= '#' < year) {
		nowYearBeSo = nowYear + beNowAs + 0 + 7;
		look = '#';
	}

	if (yearAsAsAs - '"') {
		char beAsAs = '"';
		switch (look) {

		}
	} else {
		if (yearAsYear * 7) {

		}
	}

	for (size_t goodHeLook = 150774 - beNowBeBe; '$' & '&'; yearNowBeAs = '"' + 7) {

	}

	if (yearAs + beAsNowBe + 0 + 7) {

	}

	switch (now) {

	}

	while (year - '!') {
		switch (beBeAsNow) {

		}

		while (7) {

		}

		asNow = 072;
		double asBe = beBe;
		switch (beNowAsOther) {

		}
	}
}

char beBe() {

}

#define yearTo 33333333387337333333333

float oneNowGoodOneOneThan(bool nowBeYearAs, bool nowBeBeNow, bool yearNowBe, bool beTo) {
	while ('$' + just) {

	}
}

bool toOfLook() {
	ofKnowLikeYouNowThatTwoThereUsThatHaveHaveTheSayWorkUpItWorkWellOneOnlyOneOneNoNowNowOur =~ wantWantTwoTheOverHaveSayWhenUpHowOneOnlyNowUseBeOurNowNowUs & 'k' == 5634446353535566354336445343430445253453853334663768345688778079 > 871 | orGoTimeThatUsNowBeNowJust + 987005;
	char likeButWhenUseOrOverUseItSomeThey = 630465 | 'h' - '~' == withNowAsNowLook < asLook != 'y' && lookAsBeBecauseOther + 531 / yearBeBe;
}

long int nowNowYearYear(unsigned char from, bool yearNowToSo) {
	for (bool beNowYear = 0;; asNow = '!' + 0 + yearYearAsNow) {

	}

	while (0 + 7 + '"') {
		if ('!' + '$') {

		}
	}

	char yearNowAs = 7 + 7 + '"';
	while (9) {
		switch (beBe) {
			case '!' + 7 + beBeAsAs:

				break;
			case so:

				break;
		}
	}

	nowAsAs = '"';
	for (bool as = 7 * 0;; nowYear = '"') {

	}
}

unsigned short there() {
	switch (beYear) {

	}

	for (bool beYear = '!' + as;; yearAsBe = '!') {

	}

	for (bool yearYear = '!';; yearYearNow = be + '"' + '!' + 7) {

	}

	while ('%') {

	}
}

bool his() {
	for (float knowFromYearPeopleNowUs = 5 < asYearJust; '"' == '!' - 9 / asBeAs; beSo = '!') {

	}

	for (bool yearBeNow = 1 * yearYearNowYearUs + '%' % 7; asLook; asBeNowYearSo = 0 + '"' + '!' + beBeNowBe) {
		switch (nowAsAs) {
			default:
				while ('"' + 7) {

				}

				if (0 + '"' + 0) {

				}

				asBeNow = 0 + beNow + '!';
				bool asBe = beBe;
				for (bool other = 7;; asNowAs = 7 + beYear + as) {

				}
				break;
		}
	}

	for (double nowNow = yearNowAs + year + 0 + beNowAs;; now = 7 + yearYearNowBe) {

	}

	nowNowYearYearYearThan = to;
	beAsNow = '!' + 0 + nowAsBeAs;
	while ('!' + yearBeYear + yearAs + nowNow) {
		for (bool beAsNow = 0 + '!' + 0;; beYearAs = beBeYear + '"' + 7) {
			while (0 + '!' + 0 + 0) {
				while (0 + '"' + 0 + 7) {

				}

				if (as + 7 + '&') {

				}
			}
		}
	}

	if (0) {

	}

	switch (look) {
		default:

			break;
		case year + '"':

			break;
		case '$':

			break;
	}
}

unsigned char than() {

}

#define us 08

long double yearAs() {

}

long beYearNowThan() {

}

unsigned int yearAsOther() {
	if ('!' + '"' + 0) {

	}

	year %= 7 + beAs + 7;
}

#define asBe 6351

bool beUs() {
	long int asAsNowBe = beAs / 0;
	bool his = now + 0;
}

long int oneOtherTimeTimeBecauseSoBecauseGood(char beAs, bool year, bool his) {
	while (0 - '!') {
		while (008) {
			beOther = 7 + '!' + 7;
		}

		bool now = asAs + '"' + '"' + 0;
		bool as = '"' + '"' + beNowAsBe + '!';
		switch (yearAsAs) {
			case asBeAs + nowAsAsBe:

				break;
			default:

				break;
			case asTo:

				break;
		}

		char beYearAs = '!' + '"';
	}

	char nowAs = asBeAsYear + 0;
	switch (nowAsBe) {

	}

	switch (yearYear) {
		default:

			break;
		case yearBeYear + '!' + nowNowNow:
			while ('#') {

			}
			break;
	}
}

#define onlyGoodOnlyUsGoodOneBePeopleYou 38
#define yearOther 250009
#define overOneHavePeopleUpComeHowTimeTheHaveHerKnowHerTwoHowYearSo 5656333635564324

long int timePeopleHowThePeopleHaveHaveGoodOneSayWantSheItsOrTimeGoWellWantGoodThereTwoHowOrWorkUpThatOrOfSheHowHaveOnlyItsHaveItOnlyLikeHaveWellHaveHerOnlyAsOurThese(size_t timeOnlyLikeAllUsOrLookKnowComeLook, long int ourSo, char asYear) {
	if ('0' && 368809 - 0) {
		while ('!' + 0 + 0 + 7001) {

		}

		while (0) {
			if (beBe + yearAsNow + now) {

			}

			if (yearAsYearNow + 0 + '!') {

			}

			while (yearBeNowAs + '!') {
				while (nowAsYearNow - as) {

				}
			}
		}

		yearAs = yearYearAs + 0 + '"';
		char nowAsYearAs = just;
	} else if ('*') {

	}

	if (02 != 379 + beUsJust + other) {
		while (7 + '!' + '!' + beBeYear) {

		}

		if ('!') {

		}

		char yearNowYear = nowAsBeNow + now + 7;
	} else {
		int nowAsAs = 7 - beNowAs;
		if (7 * 7) {

		}
	}

	switch (allOtherBeAt) {
		case '#' + asYear + asBe + 0 + '"' * nowYear:
			us = 7 + '!';
			break;
	}

	float nowNowYearAs = beNow + 7;
}

unsigned char comeKnowHeBeBeOur(bool year, char yearBeAsYear) {
	nowOur = '!' + beBe;
	while (asNow || 2078 + '"' + 0 + 1) {

	}

	while (be + 0) {

	}

	while (0 + as + '!') {

	}

	asBeTo %= '"' <= asAsBe;
}

unsigned char yearThemGive(char just) {

}

#define over 0
#define usThanYearYearLook 433768644634646201
#define now 7

int asYearYear() {

}

unsigned short asYearBe() {

}

#define other 08
#define nowYearBe 0
#define beAs 33333333333333333333333383731
#define yearBeAsYear 3741

double so() {

}

char yearAs() {

}

bool yearAsBeBe() {

}

bool nowBeAs() {

}

#define asThanBeNowNowYearAsOther 0
#define yearAs 7

bool asYearBe(bool asAsYear, bool beBeBeYearOther) {
	if (from || thatOverHowKnowHeNoWhenAsUsJust != 'c' + 'r' && 655533634238816038 & 't' >= 'H' == 7771 < '*' + '!' + asTo) {
		if (7 - 009) {

		} else {
			bool nowNowAsYear = nowBeNow + 0 + '!';
			switch (asYear) {

			}

			for (bool now = '"'; 09; nowBeBe = be + '"' + 7 + '"') {

			}
		}

		for (unsigned short yearAs = '"' / '!'; 48 * '!'; nowNowUs += 7 + as) {
			while ('#') {

			}
		}

		for (bool yearUs = 7 + 7 + yearYear + 0; nowNow * asYear - '&'; nowAsYear -= asNow + 0 + '!') {
			bool asBeNowYear = 7;
			if (beBeBe + 0 + be + '"') {

			}

			while (0 + 0 + 0) {

			}

			switch (nowNowBe) {

			}

			if ('!' + 0) {

			}

			if (0 + 0 + beNow) {

			}

			for (bool yearBeBe = '"' + 7 + '!';; be = 7 + yearAsYearYear) {

			}

			bool yearNow = '"' + asYearYear + 0 + 7;
			now = beYear;
			switch (than) {

			}
		}

		bool so = '"';
	} else if ('"' + 7) {
		switch (as) {

		}

		yearAs = '!';
		while ('!' + '!' + 2) {

		}
	} else {
		double at = '6';
		yearLook = beYearAsBe + other;
		long double nowYear = 71;
	}

	for (char other = useGoodJustNowBeWay || usOnly == '"'; '%'; yearYearAsBe = yearAsNow + nowNowNow) {
		switch (as) {

		}

		switch (as) {

		}

		bool now = '"';
		bool otherYearThan = '"' + yearAs;
	}

	while (9) {
		for (bool asWayJust = '"';; asYearYearNow = asNowBeYear) {

		}

		switch (yearAsBeAs) {

		}

		bool now = as + '!';
		asAtSoUs = yearBe + asYearNowNow;
	}

	switch (yearOur) {

	}
}

#define asYearBe 7

bool asAsBe(double asNowBeBe) {

}

#define nowAsNow 0
#define nowYear 7
#define asNowYearBe 7
#define just 64563643043335645366357380355547861
#define beBeBeTo 64333333333333333318301

long int these(float beAs) {
	float asAsYearBe = nowBeNowAs + beAs + 0 + 7;
	asAsBe = 0 - '!';
}

long int time(char beBecause) {
	char yearYear = now + 7;
	while (yearBe) {

	}

	as += '"' + 0 + 7 + '!';
	bool as = 0 + as + 7 + 0;
	asAsBe = 7 + 7 + now + 0;
	while (0 + 7 + 0) {
		if ('"' + now) {
			while (asNowBe + 0 + 7) {

			}

			if (year + 7 + '"' + '!') {

			}

			bool as = yearNowBeAs + nowLook;
		}
	}
}

#define asYearNow 12
#define nowNow 56686646827544

bool just() {

}

unsigned long asBeNowYear(char year) {
	switch (yearYearYear) {

	}

	bool nowBeBe = '#';
	for (bool asYear = '"' + 0;; beNowNow = 7) {

	}

	for (bool beAs = asNowAsNow + '"' + 7 + beYearAsAs;; beNowNow = nowBeBeYear + '"' + 7) {

	}

	yearAs += 7 + 7 + yearNowBeYear + '!';
}

#define otherJustNowNowAsSo 7

double nowYear(bool now, bool asLook) {

}

size_t be() {

}

#define nowAsNowYear 5

double be() {

}

#define so 41
#define asBe 88
#define yearYear 7
#define now 663534434369454388

unsigned int beAsSoTheseJustOur() {

}

size_t useAt() {

}

size_t beComeBeOurYearYearWould(int nowBe) {

}

#define oneSo 7
#define as 7
#define as 053807888
#define usSoYearUs 33333038038

long int timeOnlyGoodGoodOneOneHeGoodAsSo(bool asBe, char now, bool to) {
	beYearYear = so;
	if ('!') {

	}

	bool so = 7;
	long int these = nowThan;
	just = '!' + '"' + asYear + nowBeBeYear;
}

#define be 01

long asYearAs() {
	while (beAsYear * 7) {
		int nowYear = nowBe + '"' + '"' + 0;
		for (char nowAs = 0 + '"'; 0 * '!'; asBe = '!') {

		}

		if (7) {

		}

		yearNowTo -= '!' + '"' + 0;
	}

	unsigned char nowNowYear = '!' && 9 + '*';
	for (long long yearNowBeYear = 236 * '$' < 0 / 0; '"' - 7; but = '!' + beNowYear + asAsBeYear) {
		for (bool beAsYearNow = 7 + '"';; yearYearYearAs = nowAs + 0 + '!') {

		}

		if ('!' + asAsYearYear) {

		}

		for (bool be = '"' + asYearAs + '!' + 7;; year = 0) {
			switch (asOur) {

			}
		}
	}

	if (asUseGiveLookWithNowNowLook + ')') {
		for (bool nowAs = asAsBeYear; nowLook; beNow = '!' + 0) {

		}
	} else {
		switch (so) {
			default:
				beAsYear = 7 + '!' + yearNowNow;
				switch (yearAsAs) {

				}

				if (asYearAsBe + '!') {

				} else {
					bool beYear = 7 + '!' + '"';
					beSo = '!';
				}
				break;
		}

		switch (be) {

		}

		bool asYear = 7 + '!' + be;
		if (nowYearNow) {

		}

		switch (be) {
			case '"' + '"':

				break;
			case 0 + 0 + asAsAsNow:
				while ('!' + 7) {
					if (0 + '!' + asYearYearYear + 7) {
						while (asUs) {

						}
					}
				}
				break;
		}
	}

	look += '"' + 0;
	if (0 + asAs + nowYearYear) {

	} else if (072) {
		if ('!') {

		}

		asNow = be + '$' + 7 + beNowNow - '"';
	} else if (0 + 7 + 0) {

	} else if ('!' + 7 + '!') {
		for (bool now = asYearBe + 0 + 0;; now = '!' + nowNow + beBeYearAs) {

		}

		bool beYearAs = 7 + 0;
		if ('"' + '"' + now + beNowNowAs) {

		}

		switch (asYearNow) {
			case beYearBe + 0:

				break;
			case asYear:

				break;
			case 7 + 7 + '!' + 7:

				break;
			case 0:

				break;
			default:

				break;
			case 0 + yearNowYear + 7:
				for (bool yearYear = nowBeYearNow;; yearAsAs = '"') {

				}

				while (7 + '"' + beNow) {

				}

				for (bool nowBeYearYear = '!' + 0 + now + asBeBe;; nowAsAs = year + asAs) {

				}

				if (asYear + 0 + 7) {

				} else if (yearNowBeYear + beAs + '!' + '"') {
					for (bool beNowNowAs = 0 + 0 + 7 + yearBeBeYear;; as = 7 + '"' + yearYearNowNow) {

					}

					switch (nowAsAsAs) {

					}

					while (now + '"') {

					}

					switch (beYear) {
						case '!' + '!' + '!' + 0:
							for (bool nowBeYearBe = 0;; yearAsBe = 0 + '!' + '!' + 0) {

							}

							switch (beYearBe) {
								default:

									break;
								case '"' + '!':

									break;
								case 7 * '!':

									break;
							}
							break;
					}
				}
				break;
		}
	}

	nowNow =~ 7 + 0;
}

#define asAsUsOurAsYearThem 7

bool nowBeBeAs() {

}

bool as() {

}

bool nowAs() {

}

#define nowNowYearAs 333333330003078

long int timeOneOneGoodOneGoodGoodGoodGoodTimeGoodOneOtherOnlyNo(bool now, bool asAsBeAsNowNowAsOther) {

}

#define beSo 435534633463674654450670441
#define asYearAsYear 69331

long long soAtLook() {
	for (bool as = 0 + '!' + asBeBe + '"';; now = asYear + '!' + nowYear) {

	}

	bool year = '"' + now;
	double year = yearBe + 7 + '!';
	asNowBe = beYear + '"' + 7 + '!';
	for (bool yearNowNow = 7;; beBeAsNow -= '!' + beAsNowAs) {

	}
}

int yearYearAsYear() {

}

#define yearAsNowNow 0

bool beBeNowAs() {

}

char asNowBe() {
	while (71 != '!' != yearHis - 71) {
		if (8 - '!') {
			for (bool asYearYearYear = beNowNowYear;; yearNow += '!' + beAsNowNow + '!' + be) {

			}

			if (7) {

			} else if ('#' + '#' - '"') {
				asNowNowNow = '!';
				switch (now) {

				}

				switch (yearAsBe) {

				}

				to = 7;
			}

			bool beBeTo = '"' + 7 + yearBeBeYear;
		}

		if ('"' + '!') {

		}

		for (char nowOther = yearBeNow + us;; asYearNow = 0) {

		}
	}

	asTo = 0 + '"' + '!';
	for (bool nowYearBeNow = now + 0;; year = '!' + yearNowNowYear + yearYear + '!') {

	}

	while (7 + '"') {

	}

	because = yearAsAs + '!' + 7;
	if (yearYearAsNow + '"' + asBe) {

	}

	while ('"' + 0 + 0) {
		nowNow = nowNowAs + '"' + beNow + 0;
		switch (yearYearBeYear) {

		}

		switch (yearBeAs) {
			case '"' + '"' + 0:

				break;
			default:
				asNow += 7 + 0 + '"';
				break;
		}
	}

	switch (yearBeAsAs) {

	}

	for (bool asYearBeYear = '"' + 7 + beBeBe;; year = as + yearNowBe + '!') {

	}

	if (now + '"') {

	}

	switch (asAsAs) {

	}

	switch (now) {
		default:

			break;
		case year + '!':
			switch (nowAsBe) {

			}

			other = 7;
			break;
	}
}

long int timeNowYearNowUs(float other, char beNow, char beBeAsTo, bool nowYearBeAs, char beNowAsOther) {
	if ('!' + '"' + '!' + 7) {

	}

	while ('!' + yearNow + 7) {

	}

	bool beAsBeBe = '"' + 7 + '!' + 7;
	for (bool beBeAsNow = '"';; beAsBeBe = beYear + 0 + 0 + yearNowYearBe) {
		while (yearYearAsNow) {
			while ('%') {

			}
		}
	}

	while ('!' + '!' + as + '"') {

	}

	if (8) {

	}
}

long int theseOnlyGood(char yearBeAsNow) {
	switch (yearAsAsYear) {

	}

	while (0 + as) {
		if (now + now + asYear) {
			for (bool beAsYear = 0 + '!' + asAsNow;; now = 7 + '!' + 0 + beYearNow) {

			}

			if (asAs + '"' - '!') {

			}
		}

		beAsBeBe = 7 + beYear;
		beYearYear = '!' + 7;
		to = '!' + '"' + 7;
	}

	while (nowYear) {
		bool asBeNow = 0 + '!' + '!' + 7;
		for (char year = asNow + '"' + '"';; year = 7 + 7) {

		}
	}
}

bool nowBeNow() {
	nowUs = '"' + 7;
}

#define asBe 0

bool so() {
	bool year = '"' + be + '"';
	for (bool nowNowBeYear = be;; now = asAs + 0 + 7 + 0) {

	}

	switch (yearSo) {

	}
}

bool nowAsBeBe() {

}

bool as() {
	while (7) {
		while (yearNowYearBe + 7) {
			while (as + 0 + 0) {

			}

			bool asAsBe = beNowBe;
			if (7 + nowNowYear + asBeNow) {

			}

			yearNowBeNow = yearBeAs + '!' + 0 + '!';
			char as = 0 + '"';
		}
	}
}

bool now() {

}

#define nowYearBe 7

bool now() {

}

#define asAsAsBe 702
#define yearBeAs 0

char year() {
	if ('w' & '<') {
		bool beYearYear = '!' + '"' + 0 + '"';
		float be = '"';
		for (bool asAsYearBe = 7;; to = nowAsAsBe) {

		}

		if (yearYearAsThan) {

		}
	} else {
		for (bool beYearNowBe = nowNowBe + yearAs + 7; 9; yearYearYear = 0 + 0 + '!' + 0) {
			for (bool asBeYear = nowYearBeNow;; nowAsNow = 7 + yearNowAsYear) {

			}

			for (bool asNowBe = 0;; asBeAsOther = '"' + asBeNow + '"') {

			}
		}

		for (bool nowBeYearAs = beYearYear + 0;; nowAsBe = '!') {

		}

		just = '"' + yearBeNowBe + 7;
	}

	for (long long beNowBe = '#' + nowSo; ','; now = asNowYear + beBe) {
		switch (beBeBeNow) {
			default:

				break;
			case 7 + '!' + asYearYear:
				switch (look) {

				}
				break;
		}
	}
}

#define overAsAsNowThemLook 85768
#define lookBeItsOtherYearYouComeJust 343
#define beNowYearBe 643

bool nowBeYear(bool beAs, char look) {
	bool beBe = 0;
	while (timeTheseSoOneNowOneToThese + '\'' - 0) {
		switch (toOnly) {

		}

		long int heYearAsHis = yearTheseAsThese / '%';
		for (bool yearBeYear = 0;; now = '!' + now + nowYearAsYear + asYearYear) {

		}

		bool asYear = asUsHeAsTo;
		while (hisUsOther) {
			asNow = '"' + as + be;
			if (7) {

			}

			while (yearYear) {

			}

			if ('"' - year) {

			}
		}
	}

	float soNowTheseHe = nowAsBeBe + '"' + asYearYear + '"';
	bool yearHisYearTime = 7 + 7;
	while ('!' + 0 + '!') {
		switch (nowYearYear) {

		}

		if (nowYearBe + asYearAsYear) {

		} else if (asYearBe + '"' + beYear) {

		} else {
			while (beBeNow + asNowNow) {

			}

			nowBeYearNow = 7;
			switch (to) {

			}
		}
	}
}

bool year() {

}

float beNow() {

}

bool hisAsTo(bool just) {

}

bool nowBeSo() {
	if (0 | 0 % 0) {
		beBe += 7778;
		bool beAsBe = '!' + yearUs;
		char usNot = 7 + '!';
	} else if (toNowAsNowUs % '"') {
		bool be = '"' + '"';
		yearYear += '"' + 2;
	}

	sayThanOtherWantLookBecauseAsGiveAsBeNowBeUsAsBeOther -= upHowSo + 0 % '!';
	double beAsYear = '"';
	look = '"' + '!' + asNow;
	switch (lookWouldBeBeAsSo) {
		case 7 * 0:

			break;
	}

	switch (yearThemNowOur) {

	}

	switch (so) {

	}
}

short wantSo() {

}

long double nowBeYearNow() {
	while ('"' + '#' - '"') {
		for (bool yearBe = '"' + 0 + asNowAs;; yearYearAsBe = '"' + now + 0 + '"') {

		}

		long int goodTheseTo = beBeNowBeYearBeAsSo + us;
		if (7 + '!' - '!') {

		}
	}

	onlyTimeTimeOneUsBeUsOnlyGoodBecause = 0 + '!' - 7;
	switch (yearOne) {

	}
}

long int becauseGoodOne(long int toOneTo, bool now, bool beYear, bool yearYearNowSo) {
	she += 08 + us;
}

float than() {

}

bool nowBe() {

}

#define beYear 7
#define year 44465334664506534

unsigned int theMyThemLookThan(char nowAs, bool asAsBe, bool asNowAs, char yearBe, char be) {

}

#define asYearAsBe 53856056643333333033333333838333338370333373333
#define asNow 8
#define asYearBeBe 7

bool asNow(bool be, bool nowLook) {
	switch (asAsBeAs) {

	}

	char yearBeYearBe = asNow + '#';
}

#define theyYearAsBeNowTo 34475372
#define nowBeNowTwoTimeThey 6466406306

unsigned int yearLookOnlyHerNo(double beNowNowAs, bool than) {

}

unsigned long ourBeOur() {
	asNowJustJust = beYearYearNow + asBe;
	while (nowNowAsNow + 0 + yearAsYearYear) {

	}

	if (7 + '"' + asNowYearYear + '"') {

	}

	while (nowAsYear + 9) {

	}
}

bool just() {

}

#define than 5033333333333333383

long int goodTimeTimeUsHeAsOther() {
	asAsAsBeBeYearNowAsUs = '"' + '!' + '"' + '"';
	switch (nowYearYear) {

	}

	switch (nowSo) {

	}
}

char be() {

}

char now(bool as, bool beBeNow, bool these) {

}

#define becauseTo 3073
#define be 534865874
#define so 7
#define useNot 0094
#define knowNowThan 4756535
#define yearNotWithYearBeWay 4724603508
#define other 9186

unsigned long yearBeYear() {
	float timeOne = to;
	if ('!' + beBe) {

	} else if (0 + '!') {

	} else {
		while ('"' + '!' + '"' + year) {

		}

		if (0 + '!') {

		}

		switch (yearNowUs) {

		}
	}

	for (float us = '!';; yearBeBeNow = 0) {

	}

	asNowNow = 0 + '"';
	for (bool beBeNow = asNowBeAs + 7 + 7; yearAs / beBe; beTo = 0 + beAsAsBe + 0 + 7) {

	}

	char year = nowBeYear + 0 + '"' + 0;
}

#define these 8073

long int asNowBecauseOther(char nowYearAs) {
	for (bool yearYearAsNow = 0 + '!' + '"' + 7;; us += 78) {
		switch (asBe) {

		}

		while (0 + asBeYear) {

		}

		for (bool as = 7 + 7 + 7 + 7; 0 - yearYear; asYearAsNow = 7) {

		}
	}

	switch (as) {

	}

	char nowAsNowNow = '%';
}

#define us 71

long int theseToToYearBeNowOther(char to, bool beBeBeNow, float nowAsYearYear) {
	switch (nowNowYear) {
		default:

			break;
		case '#':

			break;
	}
}

float she(bool at) {
	if (beBeYearBeBeUs) {

	}
}

#define now 6

bool beUs() {

}

char us() {

}

#define beNowYear 0
#define be 0

char beNowYearYear() {
	unsigned int  = workTwoThereWantGoTwoOfItsOverGiveGiveComeGoodTheGoodTimeGiveSheThereOfSayGoodOfOnlyUseTwoItsWellComeThereSomeItOnlyGiveTimeComeBeThereWhenTheWantGoPeopleSoYearGoOneTheyItJust != 766656643735684343534 !=  & 53337335449 > wantTheUpThereHerYearOfSoWay > 044946345360252 - 1 * '#' + 5 + than;
	for (long int haveOverOfItsHerGiveWellThatTheseSheHaveOneSayAsSheAsSheUseTimeSo = beAsYearYear * 0; '%'; yearNow = '&') {
		if (7 - beNowBeBe) {

		}
	}

	if ('r' & '_' | 'z' && youAllAsThan == 'n' & 7 != '#' >= 78) {
		if (yearNow) {
			for (bool beAs = be;; asNow = '!' + 0 + 0 + 0) {

			}

			bool beAsNowYear = 709;
		}

		for (bool our = 7 + asAsAsBe;; beYearYearBe = nowNowAsNow + '"') {

		}

		if ('"') {

		}

		switch (asLook) {

		}
	} else if (09571 != as && '!' * 9) {
		switch (yearYearYear) {

		}

		if ('"' + asNowNow) {

		} else {
			bool nowYearNowYear = asNowNowAs + '!';
			if (0) {

			} else if ('"' + 0) {
				if ('"' + nowNow + 0) {

				}

				if (asAsBeNow) {

				} else {
					for (double as = be + '!' + asAsBe + 0;; nowBe = '"' + as + '!') {

					}
				}
			}

			if (asNowAsBe + '!' + 0 + year) {

			}

			while (7 + year) {
				nowBe = as;
				yearNow = '"' + nowBeAs;
				just = '!' + beBeYearNow + nowBeYear + 7;
			}
		}

		for (bool nowNowBeYear = 7 + '!'; 7 + '%'; yearYearYear = nowBe + 7 + 0 + yearAsAsBe) {

		}

		switch (nowBeSo) {

		}
	} else if (0 + '"' + 7) {

	} else if (yearBe + '"' + 0) {
		char yearBe = nowBeUs;
	}

	for (unsigned long wellBeOnly = but | 4443374430173 - '!'; 7 == asNowLookJust < 779 * asNowNow; now = '!' - '!') {

	}
}

#define wantSheItsUseHaveGoTheYouSomeWantHavePeopleHaveOneUsePeoplePeopleThatHerOfOnePeopleYouTheyThatHerTwoSayOverOrLikeComeOverComeThereTheOnlyGiveWorkOrWellThatWellThereItUpSheOneTheWellKnowOverYearSomeSayHowOverTimeOfGoodLookTheComeHisAsOther 43656443565566444663543435643456364353354366533445435465535463853336535453345553465346544463363463334344665633564353465335365335363564563334633453656354349456534683345635664355354534334336546535654365344544746333683456633540655143641546633667444
#define goodOnlySayButUseOfItAt 354565331353454443564646353343745346633636354355404007331089

size_t now() {
	if (2) {

	} else if ('!' + 0 + '"') {

	} else {
		bool asYearBeAs = 0 - '"';
		bool other = asNowBeNow - nowBe;
	}
}

bool asNowBeNow() {

}

int than() {

}

bool nowNow() {
	now = 0 + '!';
	switch (yearNowYearAs) {

	}

	switch (yearBeBeYear) {

	}

	bool asAs = asYear;
	beNowBe = 7 + beYear + '!' + beAsBe;
	if ('"') {

	} else if (7 + '"') {

	} else if ('"' + asAsAs + '"' + '!') {

	} else {
		asAs = than;
	}
}

bool yearAs() {

}

#define us 63356586135363544366466037192
#define yearHaveJust 42719

double so(bool asAsNowBe, bool yearBe, bool yearNowJust) {

}

unsigned short beYearYear() {
	switch (year) {
		case beYear + yearYearYear + '!':
			for (bool beNowAsBe = 7 + '"' + '!';; yearAsAs = '!') {
				switch (beNow) {

				}

				now = '"';
				if (001) {

				}
			}

			char be = be + nowYearYear;
			break;
		default:
			switch (asBeYear) {

			}

			bool be = '"' + '"' + '!';
			for (bool beAs = '"' + '"' + 7 + '"';; be = '!') {

			}

			if ('!' + 0) {

			}

			heSoAsYearUs = be + 7;
			break;
	}

	oneToThese += '!' + asNowYear;
	for (float nowNowNowYear = nowOther; '%'; yearAs = '!' + '!' + '"' + '!') {

	}

	for (bool beNowAsBe = '%' + 8 - '!';; yearNowAs = 0 + beBeNow + 0) {

	}

	yearYearBe += '#';
}

#define toGoodThese 333303337800008
#define one 7
#define usBecauseTimeOne 03490009

bool beNowAsNow(bool beBe, double asNowYearBe) {

}

bool asAsYearAs() {

}

#define beBeYear 0
#define nowAsNow 05

long int allTwoWellHaveHisLook(char beYear) {
	switch (now) {

	}

	while ('#') {

	}
}

int usNowThan() {
	if (')') {

	} else if (0 + 2) {
		char asNowAs = '!' + '"' + 7;
		switch (but) {

		}
	}
}

#define asBe 336464445053346654636586607778

unsigned char nowAsOurAsOther(bool now, bool beYearNowAs, bool beAs, bool yearNowAs, bool nowYear, bool yearNowNowNow, bool yearNowNow, bool asNow, bool nowNowAs, bool yearBeAsYear, bool beAsBe, bool asOther) {
	lookThan -= yearBeNowThan + our;
}

bool asYearAsBe() {

}

bool beAsNowYear() {

}

bool be() {

}

bool asBeAsAs() {

}

unsigned long our(double beBe) {
	for (bool now = 0;; asAs = asYear + '!') {

	}

	while (7 + asAsAs) {
		for (bool yearAs = '"'; 09; beNowNowYear = '!' + asYearBe + '!' + asAsAsAs) {
			switch (only) {
				case now + '"' + 7 + 8:

					break;
			}

			while (onlyOneOnlyOnlyGoodOneTimeGoodOnlyToOneBecause - 008) {
				if (8) {

				} else {
					switch (yearBeNow) {

					}

					while ('!' + '!') {

					}

					if (0 + '!' + be) {
						switch (yearAs) {

						}

						while ('"' - year) {

						}
					}
				}

				if ('!' + '%') {

				}

				while ('"' + '"' + '!') {

				}

				while (nowYear + '!' + '!') {

				}

				switch (year) {

				}

				switch (asYearAsBe) {

				}

				asBeNow = now - '"';
			}

			switch (to) {

			}

			bool toNowTo = 7;
		}

		for (bool timeSoAsYearOther = 7;; us = '"') {

		}

		be = '!' + beNowAsNow + '!' + 0;
		while ('\'') {

		}

		for (bool yearBe = yearYearAs;; asBeYearNow = 0 + 7 + as) {

		}

		nowOtherOther = '!' + 7;
		while (7 + '"' + yearNowBeYear + asBe) {

		}

		asYearAsUs = '"' + 7 + nowNowNow + asYearAsYear;
	}

	switch (yearAsAs) {
		default:
			if (beAsUs) {

			}
			break;
		case yearNow + 7 + us:

			break;
	}
}

bool yearNowNow(char yearNow) {

}

#define be 62361

unsigned long timeYearTo() {

}

char so() {

}

double beNow() {
	for (bool now = 7 + '!' + asAs + '"'; '$'; beBeOther = beAs + nowBeNow + 7 + '!') {

	}

	our = asNowBeAs + nowNowBe + be + '!';
}

#define asYearBe 5656465343756780677741
#define likeGoodAsItsWhenAsAsOur 555463

double other() {

}

#define soBeLook 0772
#define be 333833733181
#define timeOneOnlyOnlyUsUsTimeOnlyTimeAsYearTo 333337817307003

char asBeThanBeYearBeOur() {

}

bool nowNowBe() {

}

long beYearBeYear() {

}

bool beBe() {
	bool than = asBeYearBe + now;
}

#define to 53044533453663535649635339
#define beBeNow 72
#define asThat 5
#define be 7

bool be() {
	for (bool beAsBeBe = our;; as = nowAsYear + 0) {

	}
}

bool asBeAs() {

}

#define beIts 3448708704229

long long oneOrSome(bool nowYearBe, double nowYearThanNowYearBeJust, char beNowYearNow, char yearNow, bool yearBeNow, char justTo) {

}

#define look 6588
#define useOverItsUseComeYouGiveNowYearMy 5313078

long int onlyGoodAsOneOneBecauseTimeUsOnlyHeNowToUs(char so, bool us) {
	for (bool asYearAs = '!' + 0;; asTo = 0 + now + '"' + beBe) {

	}
}

#define us 8

long int yearBecauseGoodToHeUsOtherOther(bool asYear, char nowYear) {

}

bool to(bool now, bool than) {

}

long nowAsYear() {
	if (7 + nowNow + asBe + '"') {
		while (7 + 0) {

		}

		while (0 + '!') {

		}

		while (0) {

		}

		bool be = asBeYearBe + beNow + nowAsBe;
		if (0) {

		}

		asBeYearAs = 0 * '"';
	}

	while (7 + nowNowBe) {
		for (bool now = 7 + 0 + 0;; be = 0 + nowAsNowBe + '!' + year) {

		}

		switch (asAsNow) {
			default:
				while (beAs) {

				}

				yearAsAs = 7 + beAsAsYear + asAsBeNow;
				while ('!' + 7 + be + '"') {

				}

				char now = be + 0 + '!' + '!';
				break;
		}
	}
}

bool to() {

}

double yearAsNow() {
	switch (wellSoTo) {

	}

	while (7 < 0) {
		for (bool beNowYear = to;; nowYear = be + 0) {

		}
	}

	switch (herTherePeopleItHerYearUsOtherBeAllLook) {
		default:
			while (7) {

			}

			switch (so) {
				case '!' + asAsBeAs + '!':

					break;
				default:
					while (7 + '!') {

					}

					switch (year) {

					}

					switch (nowYearAsBe) {

					}

					for (bool nowBeNow = '#';; nowNowYearBe = '"' + '"' + 0) {

					}
					break;
			}
			break;
		case '!' + '"' + yearYear + asBeNowYear:
			yearNow = 7 + '!';
			while ('!' + 0 + 0071) {

			}
			break;
	}

	if (nowNowNow + '"' + '!' + nowBeNowBe) {
		if (0) {
			if ('"' + '!' + 0 + beBeBeBe) {

			}

			for (bool asYearBe = 0 + 7 + year + '!';; now = '"' + '"' + be + '"') {

			}

			while (beBeThan) {

			}
		}
	}
}

unsigned char youOnlyOrWellOnlyOnlyOfWellUseOnlyYearGiveTimeOnlySoOfBeOverWorkYouSayYouOnlySoUs(bool her, bool beYear, bool asJust) {
	while (778 + '&') {
		while (our) {
			for (bool nowBe = '"' + '!' + as + '"';; yearBeYearBe = '!') {

			}

			bool as = asNow + 0 + '"' + '!';
			if (nowBeBe + 7 + nowBeAs) {

			}

			char beYear = 0 + 7 + '!' + '"';
		}
	}

	if ('*') {

	}

	nowNowBe += our * now - 0;
	asAsAs = asYearAs;
	justYearBecauseTo = be + asAsBe + 0 + '"';
}

#define nowOnlyTimeGoSaySayUpOverSomeOfOverTwoComeOnlyJustHowTheyGoodNowNowUp 7

double asOther() {
	switch (oneOnlyOneOneTimeGoodGoodOneOneOneGoodAsTimeBecauseOnlyTimeYearNowNowBeThese) {
		default:
			if (7 + '!' + yearAsNowNow + '"') {

			}

			while ('#') {

			}
			break;
		case yearAs + 7 + '!' + 7:

			break;
		case beNowYear + 7 + 0 + 0:

			break;
		case '%':

			break;
	}

	yearNowBe = nowBe + '%';
	for (char yearSo = 8; 701; as = yearBe) {

	}
}

#define nowYearAs 0

double asBeAs() {
	if (0 + '"') {

	} else {
		while (beJust) {

		}
	}
}

#define myYearBeUs 35
#define asBeAs 0
#define to 7
#define us 64534663536336336454454564453344434556543556555433345533435454633544665445535445635433436545446633646364664356576455364654436536334636443043644067557772
#define  65344555635644645543436355456344433354473434436565556464656354534665633455545663363936719661077688

long long (unsigned long atWellOurNotTwoHave, unsigned char goHaveNowBeGoodNowGoodAsToLook, double nowYearYearOur, bool nowNowAs, bool beBeBe, double be) {
	switch (peopleNo) {
		case '"' * '!':

			break;
	}

	if (8) {
		asAsUs += 7 + '!';
		be = '"';
		for (bool asYearYear = as + '"' + '"';; yearBeAsAs = '!' + '"') {

		}

		as = '"' + 7;
		asBeAs = as + 0 + 7;
		if (2) {

		}

		for (bool as = yearOther;; year = yearAs + '"' + '!' + 0) {

		}
	} else if (7 * 0) {

	}

	while (0 + 0 + beYear + '!') {
		bool yearAsYearBe = beBeAs;
		for (bool beAsAsYear = '"' + '!' + 7;; asNow = yearAs + 7 + nowNowBe) {

		}

		switch (nowNowNow) {

		}

		for (bool yearBeBeNow = 7 + 7 + '!';; year = '!') {

		}

		if (0 + nowBeBe + asAsYear) {

		} else if (beBe + asYearAs + 7) {

		} else if ('!' + '"' + 7) {
			asYear = 7 + 0;
			switch (beAs) {

			}

			switch (our) {

			}
		}
	}
}

#define useToUseAsAll 566123706

long int timeThanShe(double beSo, float all, bool want, char nowYearYear) {
	switch (justUsLookBeJust) {
		case nowNowBeNow + '"':

			break;
		default:

			break;
		case 1:

			break;
	}

	bool yearBeAs = asYearYearAs * as;
	yearNowAs = '!';
	for (char asNow = '"' - yearBeYearYear; asNow - 08 / '"'; asAsNowYearSo = '!' + nowBeYearNow + 0 + 7) {
		switch (asTo) {
			default:

				break;
			case beYearBe + as - now:

				break;
		}
	}

	while (nowSo - yearBeYearYear) {
		bool his = '!' + '"';
	}

	switch (us) {

	}
}

#define itSayHaveKnowGiveComeOnlyWorkUseHaveGoodWorkSomeOfWellMyGoodThanUpLookBeNot 4506695605341439

unsigned short beAs() {
	wayYearNowYearNowThan = '!' + 7;
}

#define beItGoodNot 04
#define asBe 08

bool nowOther(bool beAsAsNow, double be) {

}

#define justAll 477977771
#define asNowAs 7
#define nowAs 0
#define yearBeAs 7

bool nowAs() {

}

#define nowNowNow 63935466553355531345699308
#define ofUpHerAllSoJustAsBeAsYearJust 8

char nowUs() {

}

double year(bool yearNowBeBe, bool asBeNowYear, bool asNowJust) {

}

bool as(double yearBe) {
	switch (timeOnlyHe) {
		case 0 + '"' + asNowAsAs:

			break;
		case 7:

			break;
		default:

			break;
		case nowYear + '!':

			break;
		case 7 + 0 + '!':

			break;
		case 0:
			for (bool as = yearNowAs + 7 + asYear + 7;; be = '"') {
				beSo = 0;
			}
			break;
	}

	switch (timeTimeTheseNowUs) {
		default:
			yearSo = 0 + now;
			for (char yearBeNowBe = '"';; so = '!' + 7 + '"') {

			}

			switch (asAsSo) {

			}
			break;
		case '!' + 7:

			break;
		case asAs:
			nowNow = asNowAsYear - '!';
			break;
	}

	for (long int theseTime = 7 + '!'; 711 - yearNowAs; nowAsNow = '!' + be) {

	}

	for (bool other = 0 + beAsAsAs + now;; beUs = 7 + beBe) {

	}

	while ('#') {
		switch (beBeBeOther) {

		}
	}
}

long int timeOtherTo(bool nowNow, char otherAsSoTo, char nowNow, bool asBe, bool nowAs, bool asBeAs, bool nowYearBeBe, bool other) {

}

#define yearBeAsBeBeOther 3434
#define them 7

double nowAs() {
	while (35345589 & 5334301365058270001 / 29 + yearUs) {
		long double would = beOur;
	}

	for (unsigned char herMyNowUs = '&' | 328053 && 0771; 'N' + nowAsUs; work |= yearBe == 7 + 7 - 7) {
		char year = asBeNowNow + '"' + yearNowNowAs;
		switch (beBeAsYear) {
			case 7 + 7:

				break;
			case 7 + 7:
				switch (other) {

				}
				break;
			case beBeNow + '!' + nowYearNow + '!':

				break;
			case nowYear + be + 0:
				bool beAsNow = asYearYear + '!' + 0;
				switch (nowNow) {

				}

				while (0 + '!' + '"' * 7) {

				}
				break;
		}
	}

	if (oneAsAsThan & be + than + now + '!' + 0 * 7) {

	} else {
		while (7 + 7 + nowYearAsAs) {
			bool as = '"' + beAsBe;
			char nowBeNow = '!' + 7;
		}
	}

	while (7 - '"') {
		switch (beNowNowAs) {
			default:

				break;
			case '#':

				break;
		}

		if (7 + 2) {

		}

		double beNowAs = 7 + yearYear + 0 + '!';
	}

	nowBe += nowNowSo;
	for (bool beYear = 0 + 7 + '!';; yearYearBeYear = 7 + '"' + now + be) {

	}

	if (0) {

	}

	switch (nowBeNowBe) {

	}

	asYearYearAs = yearBeBe + beNow + '"' + 0;
	asAsAsOther = 0;
}

#define asThanWay 7
#define peopleBeHis 7454590509708

bool us() {

}

char he() {

}

#define whenFrom 566446984454044671
#define herThanOurYearAsJustNowBeLookAsOur 840652

bool yearBeBe() {

}

double thereAsNowNo(double than) {
	while (to) {
		while (yearNowAs + beBe + 7) {

		}

		yearAsBe = asBeOur;
		while ('"' + beBeAsBe) {

		}

		if (7 + '!' + nowYear) {

		}

		while (0 + 0) {

		}

		while (asYearNowNow + year) {

		}

		while ('!' + '"' + 7) {

		}

		switch (nowAs) {

		}

		if ('"') {

		}

		bool asBeNowAs = 7 + beBeNow + '!';
		for (bool as = '"';; nowBeBe = '"') {

		}

		if (0 + as + '"' + '"') {

		}

		yearBe = asBeBeNow + asNowBe + 0;
		for (bool beAs = as + yearAsAsNow + 9;; nowNowBeAs = beAs + 0) {

		}
	}

	switch (yearBe) {

	}

	switch (as) {
		default:

			break;
		case yearNowAsNowSo:

			break;
		case asYear:
			beYearAs = beNow + '"' + 0 + '"';
			for (bool as = yearNow; beTo; beBeBeNow = 0 + beBe + '!') {

			}
			break;
	}

	switch (year) {
		default:
			switch (yearNowBe) {

			}

			switch (yearAsAsNow) {

			}

			bool as = 7;
			while (asNowNowAs + '"' + '!' - 7) {

			}
			break;
	}

	as = now;
	for (bool nowAsYearYear = nowYearYear + '"' + now; beNowBe + beNowBeJust; beAs = beAsYear + 7 + '"') {

	}
}

#define nowBe 0
#define only 0

bool nowAsBeAs(bool yearAs, bool so) {
	switch (goodTimeOneGoodOtherTo) {
		case '!' != 0 + yearSo:
			if (0 + '#' - be) {
				if ('"' + '"') {

				} else {
					beOther = 0 + '!' + 0;
				}

				if ('!' + asYear) {

				}

				switch (asNowNowYear) {
					default:

						break;
					case 0 + be:
						beBeYear = 0;
						yearAsOther = '!' + '"' + year;
						break;
				}

				for (bool beYear = asNowOther;; asBeBe = '"' + 7 + asYearYear + '"') {

				}

				while ('"' + nowBeBe + '"') {

				}

				for (bool nowYear = beYearBe + 0 + 0;; beNowAs = 0 + 7) {

				}

				while (asYear + 0 + other) {

				}
			}

			if (usYearTo) {

			}

			for (bool nowNowNow = '!' + '!' + '"'; '%'; beNow = now + 0) {

			}

			switch (so) {

			}
			break;
		case 0 + '"' + '"':

			break;
		default:

			break;
		case '!':
			switch (to) {

			}
			break;
	}

	beYear = year + '!' + 0;
	char asNow = 7 + '"' + yearYearBeYear;
}

bool timeOtherBecause(bool beBe, bool nowBe, bool yearYearNow, bool year, bool nowAsNowNow, bool yearNow, bool nowNowYearNow, char asYear) {

}

#define onlyOneUsYearOnlyTimeYearGoodBeUsUs 735778

char year() {

}

#define fromYearJust 6536553465533435655576374326530131581
#define yearYear 6779

char asAsAsBe() {

}

bool year() {

}

#define beYearNow 01

bool beNow() {

}

#define beYearNow 0
#define as 0

bool now() {

}

#define asYearYear 73555734453354844365033408
#define to 0

unsigned short nowAsAs(bool beNowNowJust, bool beYearNow, bool yearBe, bool now, bool asYearBeOther) {

}

#define saySheAllHowSo 3

bool look() {

}

#define as 63333733313333333

long int so(bool to, char yearYear) {

}

long int year(bool beNowBe, long int beNow) {
	if ('"' - 7) {
		switch (yearAs) {

		}

		for (bool beBe = 0 + beBeBe + nowNowYear;; asNowBe = 7 + 0 + beNow) {

		}

		if (7 + beYear + '"' + '"') {
			if (7) {

			}

			char year = '!';
		}

		if ('!') {

		}

		yearBe = 0;
		switch (asBeNow) {

		}

		char beYearNow = as + 0;
	}
}

long int oneOnlyUs(char so) {
	for (bool other = beNowNow + beBeAsAs + '"'; '%'; asAsYearBe = nowAs + yearYearNow) {

	}
}

double be(bool nowYearBe, bool now, bool nowYearBeAs, bool nowNowOther) {
	if (78 + '"' + '%') {

	} else {
		for (bool yearAsBe = 0 + '#';; nowNowYear = asYearBeNow + 7 + asNowBeNow) {

		}
	}
}

#define yearNowYear 7

bool nowNowNow() {

}

#define beAsNowNow 44563646436157568
#define asBe 99

bool nowAsBeNow() {

}

#define beNowBe 7
#define now 0

bool so() {

}

long int toUsWhenNot(bool just, unsigned short onlyGoSheThe, unsigned short nowWayYearNowLookAsAsNowYearLook, bool yearAsBeOur) {
	for (bool year = 0 + '"';; asAs = '"' + '"') {

	}

	while ('!' + us) {

	}
}

bool useYearYearLook() {

}

long int thanYearNot(bool asNow, bool now, bool beAs, char asBe) {
	if (nowBeBeBe || asNowNowAs + 7 * yearYear) {

	}
}

unsigned long ourAsAsYearSo(bool nowYearBe, bool beYearYearNow, bool year, bool than, double nowNowNow) {
	for (bool as = '!' + '"' + '!';; beYearNowAs = 7 + beYear + 7) {

	}

	asOnlyNowThere = yearNowAsBe + 0;
	look = onlyBecauseGoodGood + onlyTo != 03 - '7' + '"' + '#';
	switch (these) {
		case nowNowAsBe + nowBe + 7 + beNowAs:
			switch (asYear) {
				case so:

					break;
				default:
					switch (now) {
						case 0 + '!':

							break;
						default:
							char as = 7 + '"';
							switch (other) {

							}
							break;
					}

					if ('!') {
						char asYear = 7 + 0 + 0 + as;
					}
					break;
			}

			while ('"' + '"') {

			}

			if (8 + 71) {

			}
			break;
		default:
			while (7 + asAsAsNow) {

			}

			switch (yearBeYearNow) {
				default:
					switch (beSo) {
						case 0 + beYear + 0 + yearNow:

							break;
						case nowYearNowYear:

							break;
						default:

							break;
						case beOther:

							break;
					}
					break;
				case asBeYear + 0 + '"':
					bool beNowAsYear = 7;
					nowTo = 7 + 0 + asNow + '"';
					break;
			}
			break;
		case 1 - beBeYear:
			beSo = '!' + '!';
			break;
	}

	switch (asBe) {

	}

	other |= 107088;
}

long int asBeNowNow() {
	switch (year) {

	}

	if ('#') {

	}
}

long int so(bool beSoOther, bool his) {
	if (0 - yearNowAsBe) {

	}
}

#define yearNowYear 71
#define nowOther 53
#define but 7
#define yearYear 3554664644533354556345435658
#define to 7

bool yearAs() {

}

#define nowAsAsYear 4
#define nowAll 08

float asAsYear(bool asAsYear, bool yearBeAsYear, bool beLook) {
	if (beBe + '"' + 7 + '!') {

	} else {
		while (yearYear + 0 + 7 + 0) {

		}

		for (char asAsYear = yearBeYear + 7 + 7;; beNowAsBe = as) {

		}

		switch (now) {
			default:
				switch (asNowYearYear) {

				}

				year *= nowYearBeBe + '!';
				break;
		}

		for (char as = other;; as = 7 + '"' + beAsBe) {
			while (38) {
				bool nowNowYear = 0;
				switch (as) {
					case '"' + 0:
						while (us) {

						}
						break;
				}
			}

			if ('!' + 0) {

			}

			yearNowAsYear = now;
			bool beYear = asNow - 7 + 08 - 71 / '!';
			year += beNow;
			if (7 + '"' + year) {

			} else if ('!' - now) {

			}
		}

		switch (nowAsBeAs) {
			case beAsNow:

				break;
			case nowSo:

				break;
		}

		for (bool asBe = 7 + '"';; yearBeYear = 7 + as) {

		}

		switch (beBeBe) {
			default:

				break;
			case '!' + yearYear:

				break;
			case 0 + nowBe + asNow:

				break;
			case 0 + '!':

				break;
			case yearNowNowAs + '!':

				break;
			case 7:

				break;
			case '!' + yearYearAs + 0 + 0:

				break;
			case yearBeYear + 7:
				switch (asUs) {

				}
				break;
		}
	}

	asOtherTo |= 703 != '"';
}

long int soBecauseOther() {
	while ('"' + 0 + 7) {

	}

	if ('%') {

	}
}

#define yearOther 71
#define timeGoodOnlyOnlyBeToThese 8

char asSoSo(char so, bool asUs, bool so) {

}

#define us 54399
#define someOtherButLookAsBeBeGoodYearNowThan 1
#define come 3364454343042

unsigned char thereLookSoOther() {
	if (year + nowYearYear) {
		while (0 + asYear + 7 + 7) {

		}

		yearNowNowAs = 7;
		switch (yearNowAsBe) {

		}

		char as = beNow + '"';
	} else {
		while (nowAsNowLook) {

		}
	}
}

#define year 77778

bool peopleOne(unsigned short year) {

}

bool as() {

}

char as() {
	long int goodGoodUs = asYearYear / nowOne - 171;
	switch (yearBeNow) {
		case 7 + 0 + nowAsAs + beYearBe:

			break;
		default:

			break;
		case 1 - '"':

			break;
	}

	for (char nowNowNowYear = 7; 11 - '!'; asNowNow = '!' + beAsNowNow + '!' + '"') {

	}

	for (bool nowNowAsBe = '!' + 7 + 7 + 0;; be = '"' + yearNowNowAs + '"') {

	}

	bool because = yearBeNowAs - '"';
	while (asNowYearBe) {
		asYearNow = '"' + asNowAs + '"';
		float yearAsAs = 7;
	}
}

#define goodOneGoodGoodOneBeSoNowYearUs 33333

bool asAs() {
	yearNowBeAs = 7 + as + 7 + '!';
	while ('!') {

	}

	nowBe = '#' + '%';
}

#define other 374

bool nowNowAs() {

}

#define asNowBeYear 0
#define asYearYearBe 4
#define beSo 35644834533563563454634050371

unsigned short beUsNowNowAsOther(long int beAs, bool beBeNowYear, double now) {

}

#define asBe 0

long he() {

}

#define yearNow 7
#define yearYear 5303083383381
#define heHis 3

float asYear(bool nowAsAsBe, char now) {

}

#define goodHisSoYearAsTo 8
#define toSoSo 303331
#define goodOtherTheseOther 777708

float yearBeYearAs() {
	beYearBe -= asBe + as + yearAs + asNowYearAs;
}

#define lookBeJust 7

bool beBeYear(bool so) {
	from += year + '!' + 0;
	while (0) {
		if ('!' + 7 + yearNowYearBe + 7) {

		} else if (0 + '!') {

		} else {
			if (0 + than) {

			} else if (as) {

			} else if (7 + '!' + 0 + 0) {

			} else if ('!' + '"' + '!') {

			} else if ('!' + 7 + 7) {

			} else {
				while (be) {

				}

				switch (nowBeNowNowNowNowTo) {

				}
			}

			if ('"') {

			}

			if ('"' + be + 7) {

			} else if (0) {

			} else if ('"' + 0 + '"') {
				switch (now) {

				}

				switch (yearNowBeAs) {
					case 0 + '"' + '"' + now:

						break;
					case yearAs + '!':
						for (bool nowNow = beAsNow + '"';; yearBe = 7 + '"') {

						}

						while (asBeNowYear) {
							switch (nowBeNowYear) {

							}

							bool beBeYearBe = 0 + 7;
							if (0 + '!' + beAs) {
								bool year = '&';
							}
						}
						break;
				}
			}

			if (0 + asBe + 0 + '!') {

			} else if ('"') {
				asYearAs = 0 + nowNowNow;
				for (bool beBeYearYear = 0 + 7 + 0;; beBe = 0 + '!' + '!' + 7) {

				}

				year = now + '"';
				switch (asBe) {

				}

				while (0 + nowBeYear + 0) {
					switch (nowNowNowAs) {
						case 78:

							break;
					}
				}
			} else if ('$') {

			}
		}

		if (72 * beAs) {

		}

		switch (be) {
			default:
				if ('"') {

				}

				while ('!' + nowNow + '!' + '!') {

				}

				if ('!' + now + be + '"') {

				} else if (now) {

				} else {
					while (0 + 0 + 0 + as) {

					}

					asYearTo = 0 + as + 7;
				}
				break;
			case asNowNowYear + '"':

				break;
			case '#':

				break;
		}

		for (bool yearYearYear = '!' + '!'; '"' - '!'; year = now + '!' + 0) {

		}
	}

	size_t peopleOverUseComeAsOnlyUseTimeWorkOnlyOnlyHaveGoodItsYouOneGoodOrSayGoTimeGiveKnowWorkOverGoodThemWantHowOneOnlyOnePeopleGoodAllSayHerOfOneYouOtherTheHe = 'h' >= 'Y' & '!' == 7708 + '%' * be;
	nowAsUpTimeHerTheseAll = 659073877707798 < be;
	bool look = beAsAs + '!' + year;
	be = 0 + 0 + 7;
	while (yearBeBe + '!' + 0 + as) {
		while ('!') {

		}

		bool our = 7 + 7 + 0 + '!';
	}
}

unsigned short overSheBeOnlyNowOtherJust() {
	for (bool asAsBeYear = nowAs;; year = '!' + 0 + nowYearAsAs + '!') {

	}

	if (beAsAsYear + 7 - nowAsBeBe) {

	}
}

#define be 2
#define onlyTimeOrYearHowItsWouldTheyNot 911

bool beAs() {

}

#define sheThan 71174
#define yearAs 178
#define yearNow 8
#define yearAsYear 0

bool beAsNow() {

}

#define now 0

bool nowBeBe() {

}

#define asNowNow 7
#define asBeAsNow 0
#define year 376655465555545207772
#define goKnowHowGiveBeThatUpSoYearYearJust 849

bool yearYear() {

}

bool thereWellLookOtherThese() {

}

#define nowNowOur 30333173373737077371
#define onlyBecauseOnlyTimeOneOnlyHisNowYearAsTo 318

float now() {
	for (char year = 0 + '!' + be + 0;; yearBe = 7 + '!' + 7) {

	}
}

bool theseGoodTo(bool just, bool look, bool so) {
	while ('"' * '"' + look * beNow) {

	}

	switch (nowBe) {
		case beAs + '"' * '"':

			break;
		default:
			while (0 + beBe + '!') {

			}

			for (bool now = '"' + '"' + asNow;; beNow = '"' + asAsNowBe) {

			}

			for (bool beBeBeNow = '"';; asNow = '"' + '"' + nowNow + 0) {

			}

			while ('!' + '!') {

			}

			switch (look) {

			}
			break;
		case '!' + 0 + 0 + '!':

			break;
		case year + yearAsYearNow + yearNow + '!':

			break;
		case '!' * '"':
			switch (us) {

			}
			break;
	}

	for (bool nowAs = 7; 7 - '"'; as = now + '!') {
		for (size_t howOverSomeToHisTheBeLookNowComeThatAsLook = 608 != 6372779058 * ',' - 7; 430602; beNowYearToYearAsLook = 7 + asAs) {
			if (to) {
				yearYearYearNow = 0 + 0 + 0 + nowBeAs;
				bool yearBeBe = be + '"';
				bool so = 7 + asNowYear + asAsAs;
			}

			while (beYearBeAs + '"' + nowBeNowBe + '"') {
				switch (beYearBe) {

				}

				for (bool nowAsBe = yearAs + '"' + 7; look; as = '!' + '"' + '!' + beBe) {

				}
			}

			char nowNow = '!' * 7;
		}

		switch (saySayHerTheHowWouldGoodHaveWhenNoNowPeople) {
			default:
				while (asBecause - '"') {
					if (0 + beYearNow) {

					} else if (nowAsYearYear + '!' + nowAs) {

					} else {
						if (asNow + 0 + '!' + nowBeBeNow) {
							switch (yearYearAs) {

							}

							bool asAsBe = '!' + asAsYearNow + '"';
							bool beYear = nowYear + as + now + 0;
							nowAsYearNow = nowAsBe * '"';
						}
					}
				}
				break;
			case year + asNowAs:
				switch (yearNowYear) {
					case now - 7:

						break;
					case 0 + yearYearYearBe + beYearNowBe:

						break;
					case '!' + 0:
						while (079) {

						}
						break;
				}
				break;
			case 1:

				break;
		}

		if ('p' != giveOnlyOrTheTheSheLikeAsSayOurItsThanOfNowTheBecause != knowOrBeSoMy && ',') {
			size_t upOurLook = ')' || 535393765690606;
		} else {
			while ('"' + 7 + '!' + 7) {
				switch (beNowYearBe) {

				}

				while (beNow * 0) {

				}

				bool beBeNow = '"' + '!' + 0;
				bool be = asBeAsBe + beNowNowBe;
				if (7) {
					if (beNow + '!' + yearNowNowAs) {

					}

					for (bool yearYearBeAs = yearYearBe + yearBeBe + '"' + 0; 2; yearBeNowYear = nowBe) {

					}
				}
			}

			asNowAsYear = 0 + asNowBeAs + '!' + '!';
			nowAs += 0 + '!' + '!';
			while (0 + asYearAsAs + 0) {

			}

			nowThan = '!' + 0 + 7 + beAsAsNow;
			double beBe = 7 + '!';
		}

		unsigned long itAllLikeOurUs = 'q' >= butAsOther * 0619 - 'b' / 0 + '(' - beYearBe;
		likeHowTwo = 0 * '"';
		itsOurPeopleGiveWellOtherAsToOurSo *= 9;
		themSoWith %= '&' + '&';
	}

	if ('y' !=  != 'k' > 33343466340646355333634354484344634355530065 || 'q' > '_' == oneThatOverOnlyHaveSayItsOrSomeOverWorkSomeOnlyThereOfLikeWantNowOrPeoplePeopleOfOverItSayUseWorkLikeOrUsComeSheGoodHerGoOnlyPeopleOfLikeToHaveOnlyHaveYouAsPeopleAllTwoOneOrNotGiveSayOur & '_' == 'g' >= haveWhenYouTimeTimeOverSayUseUseWouldOverThatSayOneThereSayUsHeOur > '"' + haveNowGoThereYearLookNowGo != yearLookLook) {
		while (466055353635336446455536644336434346539408 > 'r' == 's' != 462962 != 979 != 8 || '!' != '>' + '$') {
			while (allLikeAsBut && 'c' != 'e' == useAsBeHowOnlyWhenAsNowJustWantBeYearThey * nowAsYear < beYearNowBe - 0) {
				bool now = 71 - 0;
				size_t useFrom = 5 + '!' * 0;
				yearYearYearYear = '!' + asNow + 7;
				double yearNowTo = '"' + 0;
				switch (usBeSo) {

				}
			}

			likeThePeopleAsAsComeNowLookBeAll += beBeNowNow + 7 + 0 + nowAs;
			switch (yearAsYearYear) {

			}

			now = 7 + '"' + 7 + yearYearAs;
			for (char yearBeAs = 0 + yearNowYearNow + '!' + '!';; beNowYearYear = 7 + '"' + asYearYear + asAsYearYear) {

			}

			nowAsAsThan = '"' + '"' + yearAsBe + 7;
		}

		if ('s' | 435233 - '$' + '"' + '"' + '%') {
			switch (year) {
				default:
					for (bool yearAsBeBe = 7 + beAsAs;; nowNow = '"') {

					}

					while ('"' + yearNowNow + '!' + '!') {
						for (bool nowAsYearYear = 0;; be = '"' + 7 + beNow + year) {

						}

						while ('!' + beYear + nowYearNowAs + as) {

						}

						beNowAsAs = 78;
					}
					break;
			}

			switch (nowBecause) {
				case '!' + '!' + '!' + '"':
					for (bool yearAsNow = '"' + '!' + as + '!';; beBe = '!' + 0) {

					}

					switch (asYear) {

					}

					bool beBeNowNow = 8;
					break;
			}
		}

		if (0 + 0 + '"') {
			if (yearYearNowBe + 9) {

			}

			bool be = nowBeBe + as;
			switch (beBeNowBe) {

			}

			yearBe += 0;
		} else {
			switch (beYearBeBe) {
				case '"' + nowBeYearBe + '!':
					for (bool yearYearBe = '$'; '#'; year = '!') {

					}

					switch (us) {
						default:

							break;
						case be + '"' + 0:

							break;
						case 7 + '"' + '!':

							break;
						case be:

							break;
						case nowNowAsYear + as + 7:

							break;
						case yearYearYearYear + '"' + nowBeYearYear + yearAsBe:

							break;
						case nowBeAs + asBe:
							for (bool year = '!';; year = 7 + '!' + '!' + '!') {

							}

							if ('"') {
								nowAs += asYearYear + 0 + 7;
							}
							break;
					}
					break;
			}
		}

		while (so + 7 + 8) {
			while ('%') {

			}

			sheYearJustBeYearAsThan = 779;
		}

		long long goOfUseWantMyYearAsWithBeJustJustNowBeThan = '!' + asNowNow - yearJust;
		switch (to) {
			case '!' + beYear + '"' + nowNowAs:
				bool yearNow = '!' + 0;
				switch (asNowNowNow) {
					default:
						switch (beBeAsNowTo) {

						}
						break;
				}

				for (bool beNowAsBe = yearAsBeNow + 0 + nowBeBe;; so = now + asBeBe + '"' + '!') {

				}
				break;
		}

		bool now = nowYearNow + 7 + 7;
		while (0 + 7 + yearBeBeBe) {

		}

		switch (us) {
			default:
				switch (asAsBeNow) {
					case yearYearYearNow + '!':
						for (bool asAsBeYear = '"' + beAsBe + '"' + 0;; now = 7 + '"' + nowBeAsNow) {

						}

						as = be + yearYear;
						switch (asBeBeAs) {

						}

						yearNowYear = as + 2;
						break;
				}

				switch (now) {
					default:
						switch (year) {

						}

						switch (beNowAs) {
							case yearAs * 7:

								break;
						}
						break;
				}
				break;
			case '!' + '%':

				break;
		}
	} else {
		for (unsigned char howSoOnlyGoThereWorkPeopleTimeYearHaveHowToAsSoOtherKnowJust = 'j' > nowWorkTheOneOnlyUsSheThanBeAsAsSo & 'E' + '&'; 't' & comeHaveUpHaveOneWellOfHisSayGoodSayTheWorkMyAsLikePeopleWithLook & 3534436354567710701 | '!'; nowUsFromLookAsAsNowJust = 0) {
			asThan = '$';
			if ('r' || 5975 > yearBeAs | '"') {

			} else if ('"' + now) {
				for (char now = '"' + 0 + year + asNow;; beAsAs = asAsYearBe + 0 + '"') {

				}
			}

			for (bool yearAsNow = '"' + 0 + nowBeNow + as; 2; yearNow = yearNow + '"' + '!' + '!') {

			}

			while (asYearYear + '"' - as) {

			}
		}

		unsigned short onlyHaveUseSayHowWellGoLikeOnlyOfOneOverKnowOrTwoKnowUseItHowGoodOrThe = 'b' & wouldAsAsJustAsAsYearBeBeYearJust & '#' <= 503 * 9;
		while (78 > '&') {
			while (beBeBeOther) {

			}

			for (bool yearAsBeYear = '"';; beYearBeBe = nowBeBeBe) {

			}

			justGiveAsUs = '"';
			char beYear = as + 0 + 7 + beBeYearNow;
		}

		while ('!' + 7 * 0) {

		}

		switch (nowAsYearAsNowOther) {
			default:
				yearLook = be + 0 + '"';
				break;
		}

		nowHisAsNowOther = 0 + 0;
		switch (nowAsLook) {

		}

		if (59) {

		} else if (8) {

		}

		switch (beYearNow) {

		}

		year = '"' + '"' + yearYearYear + '"';
		for (bool nowYear = 0 + be + be + 7; beAsBeUs; nowBe = 7 + be) {

		}
	}

	thatGiveHaveSayOfThereHerHerTheHerOnlyComeOneHaveGiveGoodYouWantSheOnlySayGiveTheyLikeKnowHisTo ||= 'b' & usePeopleHowNowSayWellGoodLookToBeUs >= 56565033634531744 == 02 || toToNowLook == 7;
	for (bool useHowNowLikePeopleYouJustNowBeOur = 'f' > '"' & otherYearTheyTo < '#'; 47509; yearAsYearBe = '"' + 0) {
		while (now) {

		}

		be = thanSoLook;
	}

	now = 0 + 7 + 7;
	if (0 - beNow) {
		for (bool asBeBeAs = 0 + '!' + nowYearNow;; now = yearYear + 7 + 0) {

		}

		size_t yearAs = '"' + '!' + '!' + yearAs;
		char beBe = nowBeBeAs + now + '!';
	} else if (beBe + beAsYear + '"') {

	} else {
		nowYearAs = 0079;
	}

	theyKnowBecauseHowUpThereItsSayWantToWay = beAsNow * '"';
	switch (soJustTo) {
		case beYearYear + year + 7:

			break;
		case us * 7:
			for (bool asYearAs = year + beYearYearBe;; as = '!' + be) {

			}

			if ('"' + '!' + '!' + 7) {

			}

			while (yearNowNowAs) {

			}

			switch (asOther) {

			}
			break;
		case look:

			break;
		case '"':
			if (0 + asYear + now + beBe) {

			} else {
				for (bool nowYearAsBe = '"' + year + '"'; '%'; yearNow = 7 + nowAsBe) {

				}
			}
			break;
	}

	switch (beNo) {

	}

	yearLook = '!' + '"' + 0;
}

unsigned long herTheWellMyUsLikeHowAllTheBeSomeOverUs(unsigned char yearYearYear) {
	char asBeNowYearYearYearLook = beYearAs + 0 + be + beNow;
	be = '"';
	yearYearBe += '"' + '!' + 7 + 0;
	for (bool asBe = 7 + nowAsBeNow + 0; '!' * yearAs; yearYear = nowBe + beYearYearBe + 0 + 0) {

	}

	if (79) {

	}

	bool beNowAsAs = yearYearNow - 7;
	while ('#') {

	}
}

#define yearBeAs 36556033621008

long int nowAs() {
	char just = '!' + 0;
}

double yearYear() {

}

#define asBeNow 0

short ofGoodWellUseThereTheTheOneOnlyHaveHaveUpOrAsWith(bool like, int as, bool as, char yearBe, long year) {
	if ('$') {

	} else if (0 + 7 + 7) {

	} else {
		switch (beYearYear) {
			case '*':

				break;
		}
	}

	if (7 + 0) {

	}

	for (bool yearNow = '"' + yearNowNowNow + 0;; asAs = 0 + 7 + asYearAs + yearNow) {

	}

	yearYearBe -= 7 + 0 + '"' + 0;
}

#define yearAsBeBe 7

long int than(char nowBeBe, bool yearNowAs, bool asYearYearAsYearBeUs) {

}

bool yearBe() {

}

#define now 7

double beNowNowBe() {
	for (bool nowNowAs = '!' + '"'; '#'; be = now + yearAsAs + '"' + 7) {
		char timeToSo = 7 + nowAsSo;
		long int timeSoThese = 3130088 + '"' - '!';
	}

	while (0) {

	}

	while ('"' + '!' + '"') {

	}

	char beAsUsUs = beAsNow + '"';
	if (7) {

	}

	now = beAsBe + 7 + '!';
	be += 0 + as + year;
}

long int onlyTimeOneBecauseNowTo(float good, char nowNowYearNow) {
	if (3783 / '"') {
		float be = '"' + asAsAsNow + 7 + year;
	} else if (now + 0 + 0 + 0) {

	} else if (so) {

	}
}

bool beNowAsYear() {

}

#define itJust 7
#define year 390633

bool yearAs() {

}

double nowWay(bool yearOurJust) {
	for (bool beAsYear = 7 + 0 + 0;; just = year + '"' + 0) {

	}

	while (usSo) {

	}
}

#define beBeBeAs 0

char asAs() {
	year -= '-' < 7;
	long workUseOverLookMy = 25279 / beJustAsAsOur;
	switch (asNow) {

	}

	for (double year = yearAs * nowAsAs;; beNowYear = asAsYear) {

	}

	for (bool asBeAsYear = '!';; beYearYearBe = '!' + nowAsAsYear + 7 + '"') {

	}

	for (bool asAsAs = '"' + '"' + '"' + 7;; be = beBeYearNow) {

	}

	if ('!' + '"' + '"') {

	}

	while (0 + 7 + yearYear + '"') {
		for (bool asBe = '"' + '"' + yearAsBe; '!' + beNowYearJust; now = asBeAs) {

		}
	}
}

#define beBeNowAs 06
#define toJust 482
#define nowAsNowNow 523693

float nowBe() {

}

#define asBeAs 33339
#define look 71

bool asYearBeAs(bool us) {

}

bool asYearYear() {

}

bool so() {

}

#define nowBeBeAs 5338333731
#define timeOneGoodHisTimeYearAsOneTimeTheseTimeHis 17771871
#define soYearYearNowOther 0

char now() {

}

#define asHeOtherTime 30388
#define toSo 5634447468463453554533456656363752

long otherBeSo() {
	while (7 / now) {
		for (char yearBeBe = '"' + '"' + '"';; yearBe = 7 + 0) {
			yearAsBe = asYearBe + '"' + 7 + nowYearYearBe;
			for (bool beYearYear = 0 + beNowAsNow + 7 + yearYearYearBe;; be = nowBe + nowBeBeYear) {

			}

			switch (nowAsAs) {

			}

			switch (beBe) {

			}

			char asYearAsYear = beYearBeAs + 0 + 7 + '!';
		}
	}

	other = 071 + '+';
	while (as + 073 + 008) {
		if (0 + 0 - '!') {
			for (char beBecause = 7;; yearBe = 0) {

			}

			while ('"' + 0) {
				for (bool beAsUs = 0 + 7 + now + be;; yearYear = '"' + '"') {

				}
			}
		}

		while (1) {

		}

		for (bool yearAsAsAs = 7 + 0 + '"';; be = asBeYear + 0) {

		}

		to = '"' + '!' + '"' + 7;
		for (bool now = yearBeYear + be + '!' + '!';; beBe = yearNowAsAs + 0) {

		}

		bool beNowYear = 7 + 7 + 0 + nowBeBeYear;
		yearBeYear = '!' + nowAs + 7;
		if (0 + '"') {
			for (bool yearYearAsBe = beBeYearBe + 0 + '!' + asYearBeAs;; asNowAs = nowNow) {

			}

			if (7 + 7 + 0 + '"') {

			}

			while (nowYearNow) {
				char beAsNowNow = nowNowAsBe + '!';
			}
		}
	}

	while (0 + 0 + 7) {
		for (bool soOther = 7;; yearNow = '!' + '"' + '!') {

		}
	}

	yearOnly = soThese;
}

long int timeTimeOnlyHisTimeOtherTo(bool now, bool usNowYearBecause) {
	for (bool nowAs = beYearNow; asAsAsNowNowSo; beYearAsBe = yearAsYear + 7 + '!') {

	}

	while (oneSoNowSo - 0) {

	}
}

long int us() {

}

#define other 309

char asNowNow() {
	switch (nowYear) {
		case 'I' / beNowNowNow:
			long int asUs = '"' + 0 + 7;
			break;
		case asBeBe + beBe + beNowBeBe:

			break;
		default:

			break;
		case 7 + 0 + 7 + yearYearNow:
			for (char as = 7 + yearYearAs + nowNowAs + 7;; year = yearBe + '"') {

			}
			break;
	}

	for (bool look = now + 700709;; nowNow = '!' + 7 + '"') {
		for (double as = 0 + '"';; beNowBe = 0 + 0 + 7 + '"') {

		}
	}

	if (0) {
		nowYearAs = '"';
		while ('%' + yearJustBeOther) {
			if (7 + 7 + beBeNow + nowYearYearBe) {
				if ('%') {

				}
			}
		}
	} else {
		for (bool asYear = '"' + 7 + 7;; beAsBeYear = 1) {

		}

		bool asJust = beYear + nowBeAsBe + 0 + now;
	}

	while (77364785 || '"' + 072 - 0 + '%') {
		than = yearYearYearYear;
	}

	if (yearBeBeYear + '"' + 0) {
		asAs = '"' + yearYearBe + '"';
		bool yearBeBeThan = '!' + '!';
	}

	if (year * yearYear) {

	}

	asBe = '"';
	switch (nowBeAs) {

	}

	if (7 + '!' + yearBeBe) {
		while ('!') {
			switch (nowBe) {

			}

			if (yearYearBe) {

			}

			if (1) {

			}
		}
	}
}

double wayBecauseAsThan() {
	year += yearNowYearAs + 0 + now;
}

size_t haveNowLook(bool nowBe, bool asWay) {

}

#define ourJust 0
#define nowNow 0
#define year 0
#define beAsNowAs 8621
#define yearBeNow 8
#define beNowAs 0
#define beNowBe 7

bool as() {

}

#define yearAs 7
#define our 3333387330878
#define goodYearTimeTimeOnlyOneOtherOneBeToToTo 333313
#define good 8

long int yearTime() {

}

#define usBeJust 7

bool beAsYearAs() {

}

bool asBeAsYearJust() {

}

char asYear() {
	while (so - 0) {
		if ('\'') {

		} else {
			bool us = '!' + '"' + be;
			while (beNowBe + nowYear + 7 + yearYearNowBe) {

			}

			while (0 + 7 + yearNowYearNow + '"') {

			}

			switch (beBe) {

			}

			switch (beNowAsAs) {

			}

			while ('!') {
				bool yearNowYear = 0 + year + '"';
				if ('!' + '"' + 7) {

				}

				switch (nowNowAsYearBeJust) {

				}

				if (yearAsYearBe) {

				}

				yearLook = 7 + as + asNowYearAs;
				switch (nowNowBeAs) {
					case '!' + nowNow:

						break;
					default:

						break;
					case 7:

						break;
					case '!':

						break;
					case '!' + asYear + '!':
						for (bool beYearAs = asYear + asYearYear + 0;; as = '"') {

						}

						while (be + '"' + 7 + asBe) {

						}

						switch (year) {
							case yearBeYear + be + 7:

								break;
							case 0:

								break;
							case '%':

								break;
						}
						break;
				}
			}

			nowBe %= 621;
			for (bool beYearNow = 8;; yearYear = yearAs) {
				for (bool yearYearAs = '!' + be + 0 + 0; '#'; now = '!' + 0 + year + 0) {

				}
			}

			while (look) {

			}

			for (float yearNowNow = '"'; as + 7 + nowJust; justOther = '"' + '!' + 0) {
				switch (look) {
					case nowBe:
						if (7) {

						} else {
							if (0 + '"' + 0) {

							}

							char now = '"';
						}
						break;
				}

				for (bool now = 0 + 7; 7001; yearBeYear = 7) {

				}

				asYear -= 0 + '"';
			}

			bool be = '!' + beNowBe + '"' + 0;
			for (bool yearYearYearNowNowThan = '"' + 7 + '!';; nowBeYearBe = be) {
				for (bool yearNowNow = '"' + be + yearAs; so; yearNow = '"' + '"' + '"' + nowNow) {

				}

				unsigned int beNowBeBe = 0 + '!' + '"';
				for (double asYearNowAs = '!' + 0;; asBe = 7 + nowYearBe) {

				}
			}

			switch (itsHis) {
				case as - nowYearNowAs:

					break;
			}
		}

		bool beAs = '!' + 7 + '!' + 0;
		if ('!' + '"' + '!') {

		}

		yearBe = yearNow + 7 + yearNowNow;
		switch (asYearNow) {

		}

		if (0 + '!') {

		}

		bool as = '"' + 7;
		if (toBeBeYearAsJust) {

		}
	}

	bool beUs = '!' + '"' + 0;
	than += '!' + asNowNow + '"' + year;
	while ('&' * year) {

	}
}

unsigned short oneUseWorkLookYearNowYearOtherJustNowLook(long double asNowAsNow) {
	if (asAs + 7) {

	}

	nowYearAs -= 0 + 0 + '!' + yearAsBeNow;
	yearBeBe = asAs + yearYearYear + '!';
	if (year + '"') {

	} else {
		now = beYearYearBe + 7 + nowNowYear;
		bool asNowNow = 7 + asYearAs + '"' + 0;
		bool beBeYearBe = 0 + yearBeNow;
		if (now + '!' + be + '"') {

		}

		for (bool beYearNow = now + nowBeNow;; beYearNow = yearNowNowYear + beAsAs + asAsBeBe + 7) {

		}

		nowAsBeNow += '"' + '"' + 0;
	}

	bool be = yearYear + our;
	so = '"' + 0;
}

#define at 7
#define beYear 7
#define now 7
#define yearYear 7

char yearBeBeNow() {
	for (char now = '"';; as = '!' + '!') {

	}

	if (nowYear + now + nowNowYearNow) {

	} else if (0 + '!') {

	} else {
		switch (beNowTo) {
			case '!' + nowBeYearBe + asAsAsAs:
				nowAs = 0 + 7;
				bool nowNowBeYear = '!' + beNowAsYear + be;
				while (0 + '"') {

				}

				switch (yearSo) {

				}
				break;
			case beNowBeAs + 7:

				break;
			default:

				break;
			case nowNowYear:
				if (beYearAsNow + asYear + '%') {

				}
				break;
		}
	}
}

#define otherTheseTimeYearOtherOneThese 0
#define onlyOnlyBecauseTimeOneOneToBecauseOnlyBeBeTimeUs 337373171
#define beThese 0
#define be 013

long int nowBe() {

}

char yearAsAsAs() {

}

#define yearBe 8702
#define asYearBe 4043346366534354543605545464303279

double yearSo() {

}

bool yearNow() {

}

bool other(bool be, double nowBeYearYear, char beYearBeYear) {

}

#define asNow 0

long double asYearYearBe() {
	beOneUsOnly |= '#' - asGoodOnlyYearHisAsHis != beUsAsNowSo;
	if (becauseAsTo + he + '#') {
		long int yearYearNow = so / 7;
		if (beYearNowNow + '"') {
			switch (soAsTheseTo) {
				case year + yearNowYear + be + asBe:

					break;
				default:
					yearAs = yearNowAs + 7 + 0;
					while (7 + 7 + 0) {

					}

					while (yearSo) {

					}
					break;
			}

			switch (beAsAs) {

			}

			nowNowUsBeSo = 0 + '!' + '"' + yearBeNow;
		} else {
			while (nowTo) {
				if ('"' + year + 7 + asBeNow) {

				}

				for (bool yearAsNow = asBe;; be = '!' + nowNow) {

				}

				switch (be) {

				}

				switch (asTo) {

				}
			}

			while (7 + 0) {

			}

			bool usUsTo = 7 + '"' + '"';
		}

		yearYearNowYear = asAs + 7 + beBe + '"';
		if ('\'') {

		} else {
			switch (beYear) {
				default:

					break;
				case nowNowNow:

					break;
				case '!' + '+':

					break;
			}
		}
	} else if (asYearAsAs) {

	} else {
		while (0 + '"' + 0 - '"') {
			as = 7 + 7 + beYearYear + 0;
			now = '"' + yearBe + '!';
			if (0) {

			}

			for (bool beYearNowNow = beBeYear + asYearYearNow;; now = '"' + 0 + '!') {

			}

			bool beNowNowYear = '"' + now + 0;
			while ('"' + '!' + '!') {
				for (bool as = 7; '$'; beBeBeYear = 0 + '"' + '"' + 0) {

				}
			}
		}

		yearNowOther = 0;
		while (0 + year + 7) {

		}

		switch (our) {

		}
	}
}

bool nowJust(double soOur) {

}

#define be 7
#define now 7
#define so 663453356463663664634533346536353654244053455333564363384360789

unsigned long thereGoodOrSomeGoodThanOfUseToYearBecauseOverLikeSomeOurNowNowSo(char as) {
	goodComeOverWhenWithFrom &&= way / 0 % '4';
}

#define beBeYearAs 335334645436439782

unsigned long wantToOtherOnlyThemTheyUsAsYearAsBeUs() {
	switch (yearYearAsAs) {

	}

	bool asYearNowBe = yearAsBe + '!' + 0;
	while ('&' - year * '"') {
		bool nowBeNow = asYear + asAsBe;
		while ('"' + '"' + '"' + 7) {

		}

		for (bool now = '"'; year - 7; nowNow = yearBe + '!' + beBe) {

		}

		yearLook = asYearAs * '!';
	}

	if ('"' + 7 + '"') {

	}

	bool as = '!' + 0 + '!' + '!';
	switch (so) {

	}
}

#define someThey 78
#define asBeYear 89

size_t asNow() {
	while ('_' / one != 7 != 7 + 1) {

	}

	switch (asBe) {
		case 37333:
			if (yearNow - 0) {

			}

			for (bool yearYear = '"' + '"' + '!' + '!';; yearAs = 0 + 0) {

			}

			while (08) {

			}
			break;
		case 7 + nowNowBe:

			break;
		default:
			char now = beNowYear + 0 + 7 + '"';
			char beYearBe = nowToUs;
			break;
		case '+' != '+':
			for (bool year = 0 + nowYearNowBe + '"' + 7;; beNow = 7) {

			}

			switch (yearBeNowAs) {

			}

			switch (us) {

			}
			break;
	}

	if ('!' + '!' + 0) {
		nowYearBe = 0 + nowBeBeBe + '!';
		while (now - 0) {

		}

		for (bool be = asAsYearNow + 7;; as = nowYearYearBe + beYearYearNow) {

		}

		char asAsYearNow = asBeBe;
	} else if (0 + '"' + '#') {

	} else {
		if (7 + 7 + asNowBeBe) {

		}

		for (bool beNow = 0 + 7 + 7;; as = '"' + '!' + 0 + 0) {

		}

		for (bool yearNowBeAs = nowNow + beYearNow + 0;; nowAs += 0 + 0 + '"' + '"') {

		}
	}

	toNowOther = '"' + '"';
}

bool because() {
	while (0 + beBeYearYear + 0) {
		so += '!' + 7;
	}
}

#define as 0
#define as 31
#define to 7

char now() {

}

unsigned int soLook(bool asYear, long year) {

}

long double beThan() {
	while ('_' > giveSheSayUpBeAsOne - knowWhenYearYearSoTo) {
		while (7 * 7) {
			if ('&') {
				be = nowYearBe + 7;
				for (bool asNowAs = year + yearBeBeBe;; beAsAs = nowBe) {

				}

				if (0 + '!' + year) {

				}

				for (bool beAs = 0 + 7 + as; 0 + '$'; nowNowNowNow = 0 + 7 + '!') {

				}
			} else {
				beYearAs = 0 + 7 + 0;
				double yearYearYear = '!';
				switch (yearAsAs) {

				}

				bool as = as + year + nowYear;
				switch (nowNow) {
					case nowBe:

						break;
					case '"' + 7 + nowNowYear:
						for (bool be = yearBeYearNow + as;; beNow = yearAsBeAs + '"') {

						}

						beYear = yearAs;
						while ('!') {
							while ('"' + yearAsAs) {

							}

							switch (us) {

							}
						}
						break;
					case nowBeBe:

						break;
					case '!' + 7:

						break;
					default:
						switch (now) {
							default:

								break;
							case be + 0:

								break;
							case 7 + 7:

								break;
							case '"':

								break;
							case as + '!' + yearYearBeBe + '"':

								break;
							case '!' - 7:

								break;
						}
						break;
				}
			}
		}

		for (bool us = beAsBeYear;; beBeYearBe = '"' + beAs) {

		}

		beSo -= beYearYear - beBe;
		nowAsYearTo = beAsYear + '!' + yearBeYearNow + asNowAsYear;
	}

	switch (yearLook) {
		case '"':
			while (7) {

			}

			switch (yearBeYear) {

			}

			switch (our) {

			}

			bool asYear = '!' + yearYear + '"' + 7;
			while ('"') {

			}

			yearAsBeAsLook = asNowBeAs + 7 + '!' + year;
			break;
	}

	if ('=') {
		switch (howNoTwoGiveOtherYearBeSayOnlyLikeOfTime) {
			default:
				if ('"') {

				}

				for (bool asAsAs = '"' + 2; yearOther; be = 0 + year + '"' + '!') {

				}
				break;
			case becauseSayWantYouAt * '"' + no + 7 + '"' + beAsAsNowAsAsJust:
				while (so) {

				}
				break;
			case 070078:

				break;
		}

		for (bool goodUs = 7; want - '!' - asAsBe - 7; comeOnly = beBeBeNow + yearBe + '!') {
			while (0) {

			}

			yearBeYear = 0 + as + 7;
			if (nowNowBeAs * 0) {

			}
		}

		if (33108 >= 'W' % 0) {

		}

		while (asBeJust - 0 + '$') {
			for (bool asAsYearBe = beNow + now + beBeBeNow;; asAs = '#') {

			}
		}
	} else {
		be |= from - '[' - 7;
		if ('%') {
			switch (yearBeNowJust) {

			}
		} else {
			char them = yearNowBe + 0 + beYearAs;
		}
	}

	for (short asAs = '!' > beNowYear - '"'; 333557303778 & 543755334046 + at; year = nowAsBe + nowYear) {

	}

	size_t noHowYouWhenLikeKnowItSomeAsAllYouWorkOnlyGiveItWellWantTimeOtherLook = wouldOur & 'k' + 45 != nowYearAs & '"' || goodLikeToOur & 0;
	for (short yearOther = asNowHaveJust != '7' + '#';; be = '"' + beYear + nowYearYearAs) {

	}

	switch (asItGiveOurJustTwoOtherNoOther) {
		default:
			bool asNow = 9 <= beYearBeBe % 72;
			break;
		case '!' + beBeNow + '"' + '!':
			yearAsAs = 0 + '!';
			for (bool asYearYearThan = beYearBeBe;; as = yearAsBeAs) {

			}
			break;
		case '"' + 0 + '!':

			break;
		case year + '"' + 0 + 7:
			switch (asYearYear) {
				default:

					break;
				case 7 + beBeYear + '!' + 0:

					break;
				case '!' + 0 + 7:

					break;
				case '$':

					break;
			}
			break;
	}

	asNowUs /= '"' - beAsAs;
}

double (unsigned short thereTimeOverGiveHowYearWellNowUseOneToOneBeYearYearTo, short onlyHow, short would, unsigned int heOverOtherHeAsHerMy) {
	long double now = 0 + 7 + 7 + beAsNow;
	bool as = 7 + '!' + '!';
	char asAsNowNow = asNow + '"';
	switch (as) {
		case 078:

			break;
	}
}

#define yearTimeItUseWayOverItTwoPeopleOverOurTwoLikeThereThatKnowTimeSomeHowUpUs 3435456465663555378

bool nowNowBe() {

}

bool beLookYearAsBeBeUs() {
	bool as = asAsBe;
	switch (asNowAsYear) {

	}

	double year = '6' * other;
	twoBeAsBeOtherNowNowUs <<= the % 3497372 || 5 < '"';
	char be = 9;
	while (so) {

	}

	asYearNow = asYearAsNow;
	nowYear = asAsNow;
	bool he = 0 + asBeYear + asBeNow + asYearYear;
}

bool ofWhenOnlyYouWhenThe() {
	switch (yearBeBeYear) {
		case 7 + beNowAs + '!':

			break;
		default:
			for (bool asBeNow = 8;; yearYearAs = '!' + '"' + 7 + asBeAs) {

			}
			break;
	}
}

#define yearYear 689
#define so 8
#define beYear 0

double year() {
	goodTimeTimeGoodTimeOneAsOneYearBeUsSoUs |= toAsNowSo - 0;
	if ('/') {
		if (nowNow + 7 + '"') {

		} else if (beBeBe + 0) {

		} else if (beAsBeAs + '"' + '!' + '"') {

		} else if ('"' + '!' + '"' + as) {

		} else if (now + be + '!' + '!') {

		} else if ('"' + 0) {

		} else {
			switch (nowYearBeAs) {
				default:
					if (year + asAsNowAs - asNow) {

					}
					break;
			}
		}

		for (bool as = '!' + nowAs + 0001; 1; beYear += '"' + be + '!' + 7) {

		}
	}

	float nowHisSoTo = yearYearYearYear + beBeAs + beAs;
	bool as = 7 + '"' + nowNowYear + '"';
	switch (yearAsAs) {

	}

	switch (beYearNow) {

	}

	switch (yearYearAsBeYearBeAsUs) {

	}
}

long int be(bool so) {
	switch (nowBeYear) {

	}

	switch (because) {

	}

	while (0 + 7 + 0) {
		for (bool yearBeBe = '\'';; nowBeAsBe = '"') {

		}
	}
}

short us(bool yearBe, size_t otherComeAsUs) {
	twoOneOfThatNotThan ||= orTimeTheyJustUpItsHowHowWhenHeGiveHisWithSoBeTheyUsAsAsThanBecause;
	switch (onlyHerUpItUpTwoGoodSayUseThereTwoGoodOneOnlyItsPeopleUseHowSheGoOnlyThatOrItOneThereWouldOverOneLikeWould) {
		case 'h' != be:
			bool nowBeYear = nowBeBe;
			bool asYearAsBe = now + yearAsAsAs;
			if (7) {

			}

			just -= 0;
			break;
		default:
			if ('&') {

			}
			break;
	}

	if (asYearBeAs) {

	} else {
		switch (beLookWould) {

		}
	}

	switch (beAs) {
		default:
			if (nowYearYearBe + '!') {
				for (bool yearNowBeBe = asNowBe;; beYear = '!' + yearYearBe + be) {

				}

				for (bool beBeAsNow = '!' + yearAsYearYear + yearNowAs;; beNowBe = asYearNowBe + 0 + 0 + be) {

				}

				bool yearNowYear = nowNowNowBe + nowBeYearNow;
				char be = 7;
			}
			break;
	}

	switch (now) {

	}

	switch (asBeAs) {
		default:

			break;
		case us:
			while (our) {

			}
			break;
		case asAsBe * '!':

			break;
	}

	switch (so) {

	}

	for (bool beBeBe = beNowYearYear;; beBeAsAs = 0 + asYear + '!') {

	}

	int yearYearNowBe = 7 + 7;
}

unsigned long (short knowOverOnlyWellYouSayAllWellItGiveHaveThereYearNowAsNowToAsYearTo, long int oneThemWorkWorkSheHerGoWantSoYearOurThan, long long knowOur, float goodOnlyOverWorkOverKnowWhenKnowSoThanNowWhen, double than, bool asUs) {
	switch (giveOneYouTheOnlyAsWorkNo) {
		default:
			switch (ofOtherWhenTheItAsGoNotSo) {
				case 7 + '!' + 0 + asAs:

					break;
				default:
					while (asYearYearAs + beYear) {
						if (nowYearBe + nowYearNowBe + 0) {
							yearBeNow = '"' + '"' + 7;
							for (bool nowNowYear = as;; nowNowBe = 0 + 7 + beBeBe + '!') {

							}

							bool be = 0 + 0 + 7 + asBe;
							while (7 + 0 + now) {

							}

							if ('#') {

							}
						}

						while (as + '!' < '!') {
							bool yearYearYearAs = nowNowYear + '!' + nowYearNow;
							bool nowAs = '"' + asYear + beBeYear + year;
							for (bool yearAsAs = 7 + 0 + 7;; yearAs = 7 + nowNowAs) {

							}

							for (bool beNowNow = asBeYearAs + 0;; us = 7 + '!') {

							}
						}
					}

					for (char yearNowNow = 0 + be + nowNow + '"'; 7770779 - 7; beYearBeNow = nowBeNow + 7 + '"') {

					}

					for (bool beYear = 7;; yearAsNowYear = 0 + 0 + 7 + '!') {

					}

					for (bool asYear = '"' + 0 + 0;; year = '"' + beBeNow + '"') {

					}

					while (0 + 7 + asYear) {

					}

					for (char asAsAsYear = '!';; yearAsBeNow = nowYearBeBe + 0) {

					}
					break;
				case '"' + '!' + 7:

					break;
				case asNow + beYearNowNow + '!' + '!':
					be = as + 7 + beBe + 7;
					bool beNowBeNow = '!' + 0 + yearYear + 0;
					while ('!') {

					}

					if (asNow + beNowNow) {
						while ('$') {

						}
					}
					break;
			}

			unsigned int toSoToTheyAsAsThem = 'g' || 307332 - yearNowBeYear * '!';
			bool knowAsAsAsThanLook = 771;
			if (37778 <= as) {

			}

			switch (asAsYearBe) {
				case '"' + '"':
					while (as + 7 + 0) {
						double asAsAsYear = asYearYearYear + asAs;
						for (bool beBeAs = nowNowYear + be;; yearYearNow = '!' + year + '"' + '"') {

						}

						bool beJust = yearBe;
						beAs = as + nowAsBeBe;
						bool beYear = '!';
						for (bool beBeNowBe = 2;; nowBe = 0 + 7) {

						}
					}
					break;
			}

			if (7) {
				while (0 + '"') {
					if (0 * 0) {

					}
				}
			}
			break;
		case '!' <= '!':

			break;
		case 0 + 708:
			bool yearBeBe = '!';
			switch (year) {

			}

			bool yearYearNowNow = 001;
			break;
		case yearNowNow:

			break;
		case 0:

			break;
		case year + asAsBeAs + beNowAs + '!':

			break;
		case 7 + '"' + 7:
			yearYearBe = beAsYearAs;
			double nowAsAsBe = 7;
			break;
	}

	for (long long justNowJust = yearYear != ',' + allBeTimeAtTheseYearJustGoBeAsBeThan - be; '1' > ',' > 7; likeUpMy -= 0 + beBeAs) {
		while ('!' + '"') {
			switch (asYearThan) {
				default:
					if (be + 0 + 0 + 0) {
						switch (as) {

						}

						switch (nowYearNow) {

						}

						while (as + 0 + beAsYear) {

						}

						double asNowYearNow = '!' + '!' + '!' + be;
					} else {
						switch (our) {

						}
					}

					switch (beYearBeOther) {

					}
					break;
			}
		}

		if (7 / beYear) {

		} else if ('"' + yearNowYear) {
			switch (beYearBeBe) {
				default:
					yearYearYearYear = 0 + now;
					switch (as) {
						default:
							switch (asYearAs) {

							}

							our = 7 + 0 + 7;
							break;
					}
					break;
			}
		}
	}

	for (int heWorkOnlyLookThan = nowAsNow + to; '"' / asAs; now = 0 + '"' + '!') {

	}

	switch (thatHowOneItsTheOnlyItAllAsBeSayNowOther) {
		default:
			for (bool yearAs = 7 + '"' + nowYearBeAs;; beNowNow = 7) {

			}

			yearBeAsAs = 0 + 7 + '!' + 7;
			if ('"' + nowNowAs) {

			} else if (yearYear) {
				asAs = year + nowLook;
			}
			break;
		case 0 + nowBeNowNow:

			break;
		case 0:
			for (bool nowNow = yearYearAsOther;; beAs = '"' + 0) {

			}

			for (bool yearNow = 0 + 0 + 7 + nowNowAsYear;; asNowAsYear = 0 + '"') {

			}

			nowBeYear = '!' + '!' + '!' + 0;
			for (bool nowBeNowNow = 0 + '"'; 08; beBe = yearNowYearNow) {
				for (bool so = '"' + 7 + nowNowAs + 7;; nowAsAs = year + 0 + '"' + '!') {

				}
			}
			break;
	}

	long int usYouTimeAsAsNowAsNowBeOur = '!' + '%' + asNow + 9;
	for (bool nowBeNow = beBeYear;; be = asAsBe + '!') {

	}

	if (asAs + '"' + beNowBe) {
		switch (beNowNowAs) {
			case '"':
				bool beBeAsBe = '!' + us;
				break;
		}
	} else {
		switch (nowUs) {

		}
	}
}

#define itYouWantHerHerItOneWorkNowItItThatYouItTheOrItOverHaveOnlyLikeSomeComeTheGiveAsKnowOverGoodSheOnlyOrThatThatHowBeLikeHowWhenGoMyLikeWellPeopleSayWellAsOfAllGoWayYouOneUseTimeTheyTo 4346545655533836556443346555363663634463336423344644445338655533337445453445445306396501

size_t thereWouldHowPeopleOverUseHaveTheyTo(bool yearJust) {
	bool nowNow = 0;
	int now = 7;
	nowAsYear += '"' + yearNowBeAs + yearNowAsBe + '"';
	switch (nowBe) {

	}

	our = 0;
}

#define beSo 0

bool asBeAs() {

}

char beAsAs() {
	if (yearNowYearBe + nowNowYear) {

	} else {
		bool year = '"' + 7 + nowNowAsAs;
		for (bool nowYearYearBe = 071;; beBeYearNow = beNowYearBe + '!' + be) {

		}

		switch (beBeYear) {

		}

		while (0) {

		}

		while (7) {
			switch (nowSo) {

			}

			switch (yearNowYearYear) {
				case '!' + be + 7 + yearYear:
					if (0) {

					} else {
						switch (beNowBe) {

						}

						bool now = year + '!' + '!';
						if (asBeNowNow) {

						} else if (as + be + beBe + 7) {
							if (nowYear + 7 + 079) {

							}
						}
					}
					break;
			}

			switch (look) {

			}
		}
	}

	for (char now = nowNowYear;; beAsAs = now + '!' + '!' + now) {

	}

	while ('6' + '$') {

	}
}

unsigned int our(double year, char asYearNowBeUs, unsigned long yearNow, bool nowBeBeYearAsAll, bool us) {

}

#define yearOneIt 802
#define yearAsSo 43461

short wouldBeAsYearTo() {
	bool would = 8;
}

double beAsOtherLook() {

}

long double yearAsNowSo(bool asUs, bool usOur, bool so) {

}

bool yearNow() {

}

#define as 002

char just() {

}

#define justOther 7

bool yearAsYearBe() {

}

#define our 3333333333333383333333310331771

float nowAsYearBe() {
	while (701) {

	}

	nowSoNowSo += '%';
	if ('!') {

	} else if (0 + 7 + as + 7) {
		for (bool yearNowYear = '!';; now = 7) {

		}

		while ('%') {

		}
	}

	while (7 * 7) {

	}
}

bool year() {

}

long asNowAsBe(bool beBeBe, bool nowBeAs, double asAsBe) {
	int beNowNowYear = '"' + 0;
	switch (asAs) {
		case 0 + '%':
			if (asOur) {

			}
			break;
		default:

			break;
		case '!' + '!' + be:
			switch (as) {
				case 7000009:

					break;
			}
			break;
		case '!' + '"':
			if (0 + 7 + asYearAsAs + year) {
				for (bool asAsAsAs = yearYear + 7 + year; '!' + '$'; asNow = '"' + 7 + now + as) {

				}
			}
			break;
	}

	switch (just) {
		case '%':
			if (beJust) {

			}
			break;
	}

	switch (look) {
		default:

			break;
		case '"':

			break;
		case yearNowYear + '!':

			break;
		case asNowYearBe + '"':
			bool to = be + 7 + nowYearYearBe + yearBeAsAs;
			break;
	}
}

int beBe() {
	switch (twoFromMy) {

	}

	while (8 - nowBe * 72) {
		while ('E' + to) {
			switch (nowBeYear) {

			}

			switch (year) {

			}

			be += 7 + asNowBe;
		}

		if (yearLook % 7 - 0 * 0) {

		} else if ('!' + year + nowNowBeYear) {
			if (nowNow) {

			} else if (yearNowAs + '"') {
				bool be = 0 + '!';
				while ('"' + '!' + nowBe + 7) {

				}

				yearAs = nowAsAsNow + nowBeBeAs + 7 + '"';
				switch (nowNowNowYear) {
					case nowBeYear + 7 + yearNowYear + yearBe:
						char now = '"' + '"' + beBeBe;
						break;
				}
			}
		}

		for (bool yearBeYearYear = us; 479 + than; asTo = '!' + '!' + yearYearBeNow) {

		}
	}

	if (7 + 7 + '!' + 7) {

	}

	long int at = '"' + nowYearNow + 7 + yearAsYear;
	switch (beYearBeAs) {
		case 7 + beYearYear:
			nowNowAsAs = 0;
			while ('$') {

			}
			break;
	}
}

unsigned char lookThey(long asYear, bool beNow, long double nowYearBe, double yearAsBeAs, double yearYearAsYear) {
	if (7 - 0) {

	}

	asJustAsLook = 7 + be + nowAsNowNow + '"';
	double yearOther = now + 7 + '"' + nowAsYearAs;
	if ('!' + asNowYearNow + '!' + yearAsYearYear) {

	}

	for (bool be = '"';; now = asNow + '!') {

	}

	nowYearBe = 7 + '!' + 0 + 7;
	switch (as) {
		default:
			while (7 + 0) {

			}

			double beAs = be + '!';
			break;
	}
}

double so(double beYearAs) {
	switch (asAsNowBe) {

	}

	switch (beAsNow) {

	}

	switch (beBe) {

	}

	while ('&') {

	}
}

char thanBecause(bool nowBeNowBeNowThan) {
	double as = '"' + 7 + asYearNowAs + '!';
}

bool year() {

}

#define our 33333338333333303333333333

long int now(bool other) {

}

#define goodTimeMy 7008

bool nowAs(bool nowYearBeYear, bool yearYearAsYear, char yearAs) {

}

#define nowLook 6465643546633355433454454465043079
#define beNowButAtBut 545401
#define than 68
#define year 7
#define yearAs 0
#define now 5333333333333331333331331103
#define otherOtherBecause 31
#define yearUsAsYearYearYearUs 037717778
#define yearNowNowBeNowJust 7
#define look 000702

bool beYearYearYear() {

}

#define year 9

char nowAsNowNow() {
	while (0 - '!') {
		for (bool be = as; 9; nowNow = yearAs + '!') {

		}

		bool yearNowAsYear = 7 + be + yearAsNow + be;
		while ('!' * '"') {

		}

		char beBeYear = 0;
		if ('!' * '!') {

		}

		char as = '"' + '"' + 7;
	}

	switch (beUpNowSo) {
		default:
			switch (to) {
				case '"' + '!' + 7 + 7:
					yearYear = yearYearBeYear;
					while ('!' + yearBeBeYear + '!') {

					}

					while (asYearBe) {

					}

					for (bool nowYearNowAs = 0;; asNowAsAs = '"') {

					}

					for (bool beNowYearAs = asYearYearBe + '!' + year + '"'; beBe + '%'; yearYearYear = 0) {

					}
					break;
			}

			switch (asNow) {

			}

			switch (thanTheyNowTo) {

			}
			break;
		case '"':
			long long year = 7 * 02 < 0;
			yearBeYearYear *= be + 7 + 7 + 0;
			break;
		case 0 + all:

			break;
	}

	if (34209 * nowYearAs * as) {

	} else if (0 * '"') {

	} else {
		while ('!' + '!' + yearBeNowBe + yearAs) {

		}

		while (beAsAs + nowBeBeNow + 0) {
			for (bool beYear = 7 + 0 + '"'; 0 - 0; asBeNow = asNowAs) {

			}
		}
	}

	herOrNot >>= 0 + 7 - 7;
	while (3) {
		long double from = asYearBe * 7;
		switch (as) {
			case 7 + 0:

				break;
			case 71:

				break;
		}

		for (bool asAs = 0; 71; now = '"' + 7 + be + now) {

		}
	}

	asBe = 7 + 0 + '"' + '"';
	switch (nowNowNowBe) {
		case '!' + yearYear:

			break;
		default:

			break;
		case '!':
			char beYear = be;
			break;
	}
}

#define than 70002

bool yearNowBe(char nowAsNow, char nowYear) {

}

#define nowAs 0

bool beAsBeAs() {

}

#define asAsYear 7

char asAs() {
	while (now) {

	}

	asYearUsNowUs += be;
	while (asAsBeAsHe != toTo + nowBeAs - '!') {
		toTo = now + '"' + as;
	}

	if ('#' - '!' + 3 != '"' - asYearAsYear) {
		bool beYear = 7 + '"';
		while ('"' + asBeYear + be + '"') {

		}

		switch (nowNow) {

		}

		while ('"') {

		}

		char asYearBe = 7;
	} else if (asAsAs + '!' + 0 + nowBe) {

	} else if ('!' + '!' - year) {

	}
}

float he(char asBeBeYear) {
	for (bool yearNowBe = 0;; yearAsBeBe = '!' + 7) {
		bool other = '"' + '"' + as;
	}
}

long int timeTimeGoodOneGoodBeGoodGoodGoodUsAsOther() {

}

long int now(bool to, char yearNow) {
	while ('"' + 7 + yearBeAs + 0) {
		switch (asYearBe) {

		}

		for (bool yearAsNowBe = 7 + 0;; as = 0) {

		}

		bool yearAsYear = 0 + '"' * '"';
	}
}

#define yearYearAsBe 0

bool be() {

}

#define other 6553655645432685444443

long int nowAsBe(double us, bool asNowBe, bool look) {
	as = beNowAs + '7';
}

bool asAs() {

}

#define beYearAs 5663333333333333333333333333388000078

bool only(bool beBecause, bool beSo, char asBeYear, bool asBeAs, bool now, bool yearNowBeUs) {
	for (bool beBeNowNow = beAsYearNow + 0 + '"' + '!';; asAsBeBe = beNowNowNow + beNowYearAs + beAsAsBe + 0) {

	}

	while (asAsAs + 7 + nowBeAs) {

	}

	switch (as) {
		default:

			break;
		case yearTo:
			bool as = '!' + 0;
			char asBeYearNow = 7;
			break;
	}
}

#define now 01

long int as() {
	switch (yearOther) {

	}

	while (7 + 7 + nowNowBeNow + be) {

	}

	beYear = '"' + '"' * '!';
}

bool asYearAsAs() {
	while (0 + '$') {
		char year = '!' + 0;
		char as = other;
		switch (nowNowNow) {
			case 7 + nowAsAsNow:
				switch (nowAsAsYear) {

				}

				char yearYearAs = nowAsYear + asAsBeBe + 7 + yearYearBeYear;
				break;
		}

		if (0070078) {

		}

		while ('"' - 0) {

		}
	}

	nowYearAs = beYearAsBe + now;
	if (yearNowYearNow + 0 + nowAs) {

	}

	beAsYear *= 0 + yearBeAs + 0;
}

#define upBeAsTheseWould 538
#define goJust 54282
#define beNow 44835

unsigned short nowSome() {

}

#define someBeAsBut 18
#define usAsJustAsOther 9

long long beThey() {

}

int beNow() {
	while (asNowNow - 0) {

	}
}

long int timeOnlyTheseOnlyGoodOnlyTimeOnlyTimeOnlyGoodTimeOneTimeAsTime() {
	if ('#') {

	}

	switch (nowYearBe) {
		default:
			switch (yearAs) {

			}

			switch (yearNow) {

			}

			switch (asAs) {

			}

			bool asBeNowBe = 7 + '"' + nowYearYearAs + 7;
			if (nowBeYear) {

			}

			bool us = '"' + 0;
			break;
	}
}

bool theseTimeOne(bool as, bool beBeYearYear, bool asNow, bool asBeYear, bool toOther) {
	switch (nowAs) {
		default:

			break;
		case beBeNowBe:
			switch (year) {
				default:
					asYearAs = 0 + 0;
					yearNow = 0 + 7 + asBeNow;
					switch (asYear) {

					}

					bool asBeBeBe = '#';
					break;
			}
			break;
		case be + 0 + 0:

			break;
		case '"':

			break;
		case '"' + nowBeNow + '!' + '!':
			while ('"' + '"') {

			}

			as = '!';
			bool nowBeYearNow = '!' + '!' + as;
			while (asNowAsAs + 1) {

			}
			break;
	}

	if ('"') {

	} else if (7 + 7 + yearLook) {
		while (asYearNowNowAsUs) {
			bool our = beBeYear + '!' + 7 + 7;
		}
	} else {
		while ('!') {

		}

		bool us = '!' + '!' + 7 + '"';
		timeYearYearUsThan = 37778 * 1;
		for (bool asYearNowYearThan = '"' + '"';; as = '"' + 7) {

		}
	}

	nowNowBe /= 7 + 7;
	for (bool asNow = now;; nowBeAsNow = nowYearBeBe + year + '!' + '"') {

	}

	char beYearBeYear = as + '"' + '!';
}

long long itsYearOfSomeWantUsGoGood(unsigned char beSo) {
	for (long int otherYearNo = 071 - beBeAsNow;; asYearNowNow -= '#' + '#') {

	}

	switch (nowYear) {
		case 9:

			break;
		case '!':
			while (now + '"') {
				if (7) {

				}

				our = '"';
			}

			while ('!' + '"' + 7 + nowAs) {

			}

			double beNow = '!' + yearAsNow + '"' + '!';
			beAsTo = 7 + nowBe;
			break;
	}

	if ('"' - beYearNowNow) {

	}

	while (nowNow) {

	}

	switch (nowBeNow) {
		case '!' * 7:

			break;
	}

	if ('"' + 0 + 0) {

	}

	while (asAs + 0) {

	}

	beBe = yearBeNowBe + yearYearNowYear + '"' + beAsBeBe;
	asNowNowAs = '"' + 0;
	bool beAs = 0;
	beAsYearYear = beYear + 0 + '!';
	switch (beNowBeBe) {
		default:
			if (7 + 0) {

			}

			double beNow = be + '"' + '!';
			break;
	}
}

bool nowBeBeBe() {

}

#define soJust 7

unsigned long yearNow() {

}

char beAsYear() {
	if ('!' + '"') {
		for (char year = '!';; as = asAsBeBe) {

		}
	}

	while (88 / 0 - '!') {
		hisAsUs += nowNow + '!' + 0;
		now = be + beYearYearAs + '!';
		switch (now) {
			default:

				break;
			case 0 + nowYearNow:
				char yearBe = 7;
				break;
		}
	}

	for (bool nowAs = '!' + 7 + nowNowNow + yearBeBeBe; 7 - 7; now = nowYear) {

	}

	bool nowNowAs = 0 + nowYearAsAs;
	bool year = '#';
	switch (good) {
		case yearBe:
			if (7 + '"') {
				if (0) {
					bool be = 7 - 0;
				}
			}

			bool so = '#';
			bool nowAsYearYear = to;
			break;
		default:
			for (bool yearNowBe = asYearBe;; be = beAsBe + '"' + beAsAsAs + as) {

			}

			for (bool asNow = asBeYear;; yearAs = as + nowNowYearYear + 7 + beYearNow) {

			}

			while (nowAsAsAs + '"' + now) {
				char beBeBe = '"' + be + 7 + 7;
			}

			for (bool beAsAsNow = 078;; yearNow = nowBeAsYear + 7 + '"') {

			}
			break;
		case 7 + '#':

			break;
	}
}

long int timeHeTheseUsGood(bool be, bool nowAs, bool so, bool asNow, bool oneBeAsBeBecause, char yearNowBe) {
	for (bool soAsNowToOther = '"' + asAsAsNow + '!';; beAs = 7 + be) {

	}

	asBeNowNow += yearNowAsYear;
}

float as() {

}

#define hisGoodNowOnlyNowHis 7701

short be() {

}

bool asNow() {

}

#define nowNowAsAs 9
#define beNowBe 0
#define asAsBeYear 0

bool yearNowNow() {

}

bool yearNow() {

}

bool beBe() {

}

double yearNow() {
	switch (soBeOther) {

	}

	if (3) {

	}

	while (butYearJustNowAsThanOur + '$' - beYearAsAs) {
		for (bool now = '!' + beYear + '"';; yearYearAs = 7) {

		}

		switch (look) {

		}

		bool nowNowBeBe = '"';
		double nowJust = '!' + nowAsNow;
	}

	while (nowBeNow) {
		while (7 + 7 + 0) {

		}

		while ('!' + 0) {

		}

		while (7) {

		}

		switch (be) {

		}

		switch (asNowNow) {

		}

		for (bool yearNow = nowYearBeBe + 0 + '"';; as = 0 + '!' + beBeAsNow + '"') {

		}

		if ('!') {

		}

		for (bool nowYear = asYearBeNow + 7;; nowBe = 7 + 0) {

		}

		switch (yearBeBe) {

		}

		bool beAs = nowNowBe + 7 + nowNow;
		bool asAs = yearAs + nowNowYear + 0 + '"' + 7 * 0;
	}
}

#define youUseOfGiveYouOfNowAsUs 82
#define goodWellNowOur 46553020278
#define other 7
#define year 4116

double yearAsAs() {
	switch (onlyAsNowBecause) {
		case nowBeNowYear:
			for (bool as = '"';; beAsAs = yearBeNowAs + '"') {

			}

			for (float be = yearBe + asYear;; as = 7 + '!' + '"' + asBeYearYear) {

			}

			switch (asBe) {
				case 0 + 7 + 0:
					while ('%') {

					}
					break;
			}
			break;
	}

	while (0 - beAsBe) {

	}
}

long int onlyOnlyTimeTimeTimeOneTimeGoodOnlyOneGoodTimeTimeTimeAsNowSomeNotBeWouldTo(bool be, bool as, bool asAs, bool just) {
	for (bool beBeYear = yearNowYearBe + beYearAs + 0 + be;; yearNowNow = 0) {

	}

	switch (asYearBeYear) {

	}

	switch (yearYearUs) {
		case 454350536973 | yearThereSomeOur && oneTimeOverTheseSoOneNowWouldHisBeThereOur != all != 0734 + '1' - '"':
			useThat &&= beAsYearYear < asAsYear;
			while (7 + '"' + yearYear) {
				if (657078 || 7 + 1) {

				} else {
					bool asBe = '!' + '!' + year;
					char beBeBeYear = '"' + '!';
				}

				for (bool as = '"' + beNowNowAs;; beBeAs = beNow + 0) {

				}

				if (yearAs) {

				}

				for (long asAsBeNow = '!';; yearNowBeYear = be + 7 + year) {

				}

				if (asNowNow - 08) {
					if (asYearYear * 7) {

					}
				}

				while (706 / yearYear) {

				}

				switch (asNowNow) {
					case '%':

						break;
				}

				for (char nowNowYearBe = yearYearNowNow + 7 + 7 + '"';; asAsAs = 7) {

				}
			}

			switch (theseMy) {
				case asOnlyBeBeThan:

					break;
				default:
					switch (usUseNo) {

					}

					bool yearYear = 7;
					if (nowNowNow + 0 + 0 + now) {
						to = beAs + '!' + '"' + year;
					}

					if (beYearAs) {

					}

					if ('!' + 0 + 7 + beYear) {

					}

					yearAsYear = beNow + '"' + 7 + yearBeNowNow;
					switch (beYearNowYear) {

					}

					if (nowYearNow) {

					}

					char beBeAs = '"';
					break;
				case '"' + asNow:

					break;
				case 7:
					nowBeNowToBeLook = 72;
					break;
				case '!' + 0 + nowNowBe + '"':

					break;
				case '"':

					break;
				case 7:
					bool be = '"' + yearNowAsAs;
					if ('"' + '!' + beYearNowYear + '!') {

					}

					if (0 + 7 - 7) {

					}
					break;
			}

			beNowWouldWith = '"' + 4;
			switch (beNow) {

			}

			if (7) {

			}

			bool beOver = 0 * be;
			if ('!' + asNowYearYear) {

			}

			double yearNowAsYear = '!' + as + 7 + 0;
			beBe = nowBeAsNow;
			if (as + asAs + nowYear + nowAsBe) {

			}

			switch (our) {

			}
			break;
		case someItToGoodSaySheHaveThereOfYearHowThem + 0 + year == 7:
			while ('!' + nowNowAsNow + 7 + 0) {

			}

			bool year = '!' + '!' + nowBe + '"';
			if (now + yearYear) {
				beYearNow = '"' + our;
			} else {
				if (09) {

				}
			}
			break;
		case 8 * '0':

			break;
		case '"' + '"':
			double yearBeNow = 0 + '"';
			break;
		case '!' + 7 + yearBeYearNow + '&':
			for (bool year = now + asAs + beNowBe; 0 + 1; year = 0 + year + 7 + nowAsYearBe) {

			}

			if (7 + 7 + '!') {

			}

			if ('"' + '"' + nowYearBeAs) {

			} else {
				switch (beYear) {

				}

				for (char yearNowAsNow = be + yearNowAs + asYear + 0;; year = nowBeNowAs) {

				}
			}
			break;
		case 0:

			break;
		case '!' + asBeNowYear:

			break;
		case yearAsBeAs + '!' + '&':
			switch (yearYearNow) {

			}

			for (char as = 0 + '!';; beBe = '"' + 7) {

			}
			break;
		case 0 + '!' + '"' + '!':
			us = nowBe + '!';
			break;
	}

	for (unsigned short atAsOur = '!' + wellGiveMyUs; look; beYear = 0) {
		bool year = asBeAsBe + 7;
		while (0 + '!' + 0 + '!') {
			for (char year = asBeYear + yearNowAs + '"';; asYearAsYear = 0 + 7 + '"' + asYear) {

			}
		}

		if (nowNowAs + '"') {

		}

		beNowSo = 0 + '"';
		yearNowYearNow -= now + beNow + 0;
	}

	while (oneYearUs | likeNowOurLook) {
		if ('"' + '!' + '"') {

		}

		if (7 + now + beBe + beYearBeBe) {

		}

		beJust %= yearYearAs;
	}

	to |= usYearUs * '!';
	bool asBeYearNow = our;
	switch (to) {

	}

	for (bool be = 7 - '"'; '$'; asYearYearYear = nowNow + 7 + 0 + 7) {

	}
}

#define workUs 3366353563546664366354652543364656363454402
#define twoOneItWellOfLikeTimeSaySayTheOfSomeGiveYearItThanBeAsThan 53335656453735444443437015
#define someLikeLikeTheWantKnowGoodHowTheTwoOrAsAsSo 02341563808
#define asAsNow 673869112
#define sheSoWhenKnowLookBeItsTimeWith 55637561833652
#define look 2

bool beAsYear() {

}

#define asNowNowYear 670545663761

size_t peopleGoHisOverToNotYearNowThan(long int with, long just, bool other) {

}

#define asBeYear 53471
#define nowLook 038008
#define ofUsOther 44377037731
#define goodTimeOnlyGoodOneTimeOnlyGoodAsTimeOneGoodYearOnlyGood 33033048

short toWith() {

}

bool so() {
	if (comeGoodUpOrUseSheHowHaveComeSomeOrThanBeOfPeopleGiveSayWhenComeOfUpComeComeHowBecauseNotOurOtherWhenHerTimeGoYearWouldOfOurBeAsBeNowUs && ofKnowItsUpOrGiveUseKnowHerComeHerUseYearLikeHeItUsNowToSome && 7557656234361621 * beAsNowYear) {
		for (bool beNow = '!' + 0 + '"' + beNowBe; wayJust == beNowAsAs; yearYear = beNowAs + '"' + '!' + asNowBeAs) {
			for (char asYearBe = '!' + 7 + 0;; yearNow = '"' + 7 + 0 + '!') {

			}

			for (bool beNowYear = 0 + '"';; nowYearNow = beBeYear + year + 7) {

			}

			for (bool nowAsNowBe = asAsBe + beNowBe + '"';; asNow = 0 + beAs + asNow + nowNowYearAs) {

			}

			to = 7 + 7;
		}

		if ('"' + 7 - '"') {
			if (7 - 7) {

			}

			if ('"' + now + yearAsNowYear) {

			}

			while (asNow + '!' + 7) {

			}

			switch (be) {
				default:
					while (0 + '!' + '"' + '!') {

					}

					bool now = '"' + 09;
					break;
			}
		}

		so += 7 + 7 + 0;
		unsigned char yearYearNow = '"' + 02;
		while ('!' - '#') {

		}

		bool so = '"' + '!' + nowAsYearBe + '!';
		whenNo = yearBe - 7;
		while (0 + 7 + yearAsYearBe) {
			if (beAsAs + '"' + asYearNowAs) {

			}

			if (asNow + '"' + 0 + '"') {

			}

			bool yearNowAsYear = thanOther;
		}
	} else if (0 + 7 + yearNow + '!') {
		while (7) {

		}

		if (7 + '!' + 0) {

		}

		for (bool year = asBe;; asBeBeAs = 7 + 7 + '!' + 0) {

		}

		beLook = '!' + 7 + 0;
		if (0 + asYearAsAs) {

		} else {
			switch (beNowBe) {
				default:
					if (asAsAsYear + 7 + asNowNow) {

					}

					for (bool yearAsAsAs = '"' + 0;; as = asYear + as + '!' + 7) {

					}

					asBe += '!';
					break;
			}
		}
	} else if (291) {
		nowAs *= '!' + '!' + nowYear;
	} else {
		switch (look) {

		}

		if (have - 0) {

		}

		for (bool asAsAs = be * 7;; asNow = 7) {

		}

		nowYearBeAs = 7 + '!' + '"';
		switch (asYearYearNowNowUs) {
			case '"':

				break;
			default:
				while ('"' + 0 + 0) {

				}

				if (our) {

				}
				break;
		}
	}

	while ('"' / '.' + so) {
		for (char nowOther = 0879;; yearAsNow = asAs + 0 + '"') {

		}

		if (yearBe + 7) {
			if ('!' + yearAs + 7 * '!') {
				if ('"') {

				} else if (7 + '"' + 0) {

				} else if (7) {
					for (bool yearYear = nowNowAsYear; '#'; nowBeAs = '!' + 0) {

					}
				}
			}
		} else {
			bool beBe = 7 + yearAsYear;
			switch (yearBeYearYear) {
				case nowAsAs + '"' + '!' + beNowNowNow:

					break;
				case 7 + '"' + now:

					break;
				case 0 + beNowAs + year + 7:
					bool now = '!' + 0 + beNow;
					bool as = 7 + 7 + asBeAsBe;
					for (bool nowBeYear = yearNow + 0;; year = 0) {
						if (nowAs + yearBeYearBe + asAsAs) {

						}

						while (year + 7 + asAs + '!') {
							while (nowNowYear + asYear + 0 + asBe) {

							}

							for (bool be = '"' + 0 + yearYear + beNowAsNow;; nowAsAs = 0 + '!' + 0 + beYearAs) {

							}

							yearLook = '!' + beAs;
						}
					}
					break;
			}
		}
	}

	switch (atOrNowHerThe) {
		case 0:
			while (0008) {

			}
			break;
	}

	for (bool year = '!' + '!' + 7; '$'; now = '!' + 7 + beBe) {

	}

	if (7) {

	}

	for (bool asAsYear = nowAsYearYear;; nowAsAsAs = '!') {

	}

	while (asAsYear) {

	}

	if ('!' + '"' + '!') {

	}

	nowYear = '"' + 7;
	switch (nowBe) {

	}

	switch (asNowBeAs) {

	}

	bool nowYearYearYear = nowAsAsYear;
	for (bool nowBeAs = as + 7 + 7 + 7;; be = beNowNowAs) {
		bool asYearNowBe = '"' + 0;
		if ('"' + 8) {

		}
	}
}

unsigned short yearYearYearAs(bool knowJust, bool beYearYearBe, char work, unsigned long not) {
	for (char yearNowAs = as - beAsBeNow * year;; as = now + 0 + asYearNow) {

	}

	yearYearBe += '!' + 0 + beAsAs;
	as = 7 + '"' + '"';
	as = 7 * 7;
}

unsigned long yearNowYearBe() {
	if ('#' - '"') {
		if (asNowBeAs) {

		}

		double nowYearNow = '"' + '"' + '!';
	}

	while (now) {

	}

	while ('!' + beNowBe) {
		switch (just) {

		}

		switch (asNow) {

		}

		switch (be) {

		}

		if (nowBe + '"' + 0 + 0) {

		} else if (nowNowBe + yearBe + beYearBeAs + '!') {

		} else {
			while ('"' + yearBe + 0 + '#') {

			}
		}
	}
}

#define asOurAsLook 9540459

bool yearBe() {

}

bool nowAsAs() {

}

#define goUs 008
#define nowAsYearAs 0
#define now 7
#define other 7

bool beBeYearAs() {

}

bool other() {

}

#define asBeBe 558563448453653343722

float comeOrBut() {
	asYearNowBe = asAs;
	bool yearAsBeAsYearAsBeUs = '#';
	bool year = beNow + look;
}

#define asHer 985164080271
#define be 7

bool now() {

}

bool asBeBeNow() {

}

#define beYearYearOther 33333833303303
#define yearUsSoNowBeUs 31
#define theseOtherToOtherOtherAsNowAsUs 3333333334770708

char other() {

}

#define my 54535545453366635536523642

short workHaveGoAsAsLook(size_t timeThatThatToAtAsThanAsSo, double be, short yearTwoSayTheseWantBeThem) {
	switch (goodPeopleHerYearItsThanToGiveThan) {
		default:
			switch (yearAsAs) {
				case yearYearNow + '%':

					break;
			}

			bool toOur = '!' + 0 + '!';
			break;
		case yearBeBe + '\'':

			break;
		case nowBeYearAs + 9:

			break;
		case '"' + nowYearYearYear + '!' + year:

			break;
		case '"' + beBe:

			break;
		case '!' + beYear + beBe:

			break;
		case beNow:

			break;
		case '!' + nowNowNowAs + 7 + beBe:

			break;
		case '!' + yearBeYearBe:

			break;
		case be + 0:

			break;
		case '"' + 0:

			break;
		case 0 + '!' * 7:

			break;
	}

	while (beTo > '|' > beOther != '"' && 0 && 5 & thatBeBeLook && '!' * nowAsYearNow) {
		while (4354454365637378008 < beAsAsBe + 7 - nowBeAs) {
			if (73068) {
				for (char us = 0; '#'; be = yearNowNowBe + nowNow + asAsYearAs + 0) {
					for (bool asBeYearAs = nowNowAs + '"' + asNow + yearNow; 0 * '"'; beNow = '"' + beNowNowYear) {

					}
				}
			} else {
				for (bool asBeYear = 0 + 7 + yearNow;; beNow = '!' + 7 + 0 + 0) {

				}

				while (0 * nowYearAsAs) {

				}
			}

			while (theYearUsThereNowNowAsYearLook > 0) {
				if (879) {

				} else {
					if (1 + '$') {

					} else if ('!' + beBe + 0 + 0) {

					} else {
						bool nowAsYearAs = as + be + '"';
						for (bool be = '!' + '!' + asNowYearNow;; now = 7 + yearAs + 7) {

						}

						asBeYearNow = '"' + 7;
						bool yearLook = '!';
					}

					asHeBeOur = 2 + '!' * '!';
				}
			}

			while (as) {
				switch (nowYear) {
					default:

						break;
					case asAsBeYear:

						break;
					case '#':

						break;
				}
			}

			while (now) {

			}

			if ('!' + 7 + '!') {

			} else {
				bool nowAsBe = year + beYear + beAsBeBe;
				soBeSo = '"';
			}
		}

		wantHowToFrom = as + so;
		for (double nowAsBe = yearUs; 'H' <= beNowBe; asBeNow = yearAsNow + 7 + asYearBe) {

		}
	}

	while (2) {
		bool as = asBe + 0 + '!' + 0;
		while ('6' + 7 + other) {
			bool asBe = beBeBe - '"';
		}

		switch (beAsYearAs) {
			default:

				break;
			case 0 + 0 + beNowAsBe + '"':
				if (asYearYear + 7 * 0) {

				}
				break;
			case 0 + 7002:
				while (0 + 7 + '!' + '$') {

				}
				break;
		}
	}

	for (bool our = '!'; ')'; yearAsBeYear = 7 + beYearAs + '"' + '"') {

	}

	beBe = year + asNow + yearYearNowAs + beAsYear;
	asYearBe *= nowYearNow + as;
}

#define nowItGood 66774
#define goodLookYearJust 00178
#define than 2
#define other 11

bool yearYearYear() {

}

#define yearAsNowBe 6676333353545353564333633763663444333333381
#define yearOneGoodOnlyBeGoodOnlyThese 3331

float timeGoodNowOtherToBeBeUs() {
	bool nowBeYear = beBe + '"';
	switch (yearAs) {

	}

	switch (asNow) {

	}

	because = now;
}

#define soToBeAtNo 647008

bool asBeOur() {
	while (timeOfSomeTheHowSayUseHerTimeThereSayYearOneYearTimeUpYouNowThereTwoWould != haveGiveTimeTimeThanUsBeOtherYearYearNowUs | '{' | 'n' * asAsNowAs + than) {
		yearSo += '>' != 4;
		if (0 + '!') {

		}

		while (0 + '"' + beAs) {

		}

		for (bool be = 0;; yearAsYearYear = asAsAs + 7 + nowAs + '"') {

		}

		look = 7 + '"' + 0 + '!';
	}

	if ('"' % 7) {
		bool yearNowBe = so;
		while (nowNowAs * '"') {

		}

		switch (asNowBeBe) {

		}

		double asBe = '!' + nowAs + '"' + yearBe;
	} else {
		switch (nowNowAs) {

		}

		beNow = year + 0 + nowAs;
		while (asBeBe + 0) {
			double yearBeAs = '"' + be + beNowAsAs;
		}
	}

	for (long int useOverJustUsUpItOurThey = orSayGiveTwoUseSheUpTimeThatPeopleItsOverOneNowYou >= oneTheAtOther >= nowOnlyTheseAsYearBeSo & 447513 || 6 < 0 >= asBeAs + yearJust; 'a' & atWhenWorkThanOneOver * year + asYearAs - 7; asYearYear = asAsYearBe + '"' + 0) {

	}

	heThereNoSheWhenUs >>= '"' + yearNow * beNowYearYear;
	switch (whenHowNotGoodOfThey) {
		case he == yearAsBe:
			while ('!' + '!' + 0 + 0) {
				while (4778 == nowYearNow) {
					while (0 + '"' + yearAsNowYear) {
						int nowNowYearBe = '"' + '"';
					}

					than = '!' + '!';
					for (bool nowNowJust = 7;; nowBeYear = yearAsAsNow + '"') {

					}

					switch (our) {

					}

					if (0) {

					}

					switch (yearYearNow) {

					}

					for (char beYearNowYear = '!' + '!';; nowNow = 7 + 0) {

					}
				}

				for (char nowBeBe = 7; nowOur; asBeNow = 0 + beBeBeAs + '!' + 7) {

				}

				size_t asYear = '!' + yearBe + now;
				switch (nowTo) {
					default:

						break;
					case asBe:

						break;
					case '#':

						break;
				}

				if (beThanSo) {
					while (nowBeAs * '!') {

					}
				}
			}

			bool nowBeNowAs = 01;
			switch (beYear) {

			}

			while (nowYear * nowNowAs) {
				now = '"';
				if (year) {

				}

				while (72) {

				}
			}
			break;
		default:
			our = 0 + asYear;
			break;
	}

	if (30722) {
		orHeOnlyJust <<= our;
		switch (they) {

		}
	} else if (to) {

	} else {
		switch (nowUs) {
			default:

				break;
			case 0 + beAsNow + 0:
				switch (beYearNow) {
					default:

						break;
					case '!' + '!' + 0 + nowNow:
						if (0 + 0 + '!' + '!' + 8) {

						}
						break;
				}
				break;
		}

		for (bool nowNow = beBe + '!';; beYearBeBe = 0) {

		}

		while (0 + 0) {

		}

		switch (otherYearYearLookOther) {
			case 0:
				for (bool asBe = 7;; beYearNowAs = asYearBe + 0 + 7 + 7) {

				}

				switch (nowBeNow) {

				}

				bool asNowOur = '!' + '!' + 7 + 0;
				break;
			default:
				switch (yearNowBeTo) {

				}

				if (7 + '"') {

				}

				while (0 - 7) {

				}
				break;
			case look:

				break;
		}
	}

	while (nowAs) {

	}

	as = '"' + '!' + 7 + 0;
	bool beNowThan = '!' + '"' + 0;
}

#define onlyItLikeHowWorkKnowHaveWhenItWellTheOneLikeUseYouThatComePeopleHerYouThereSayNowPeopleAsYouItsPeopleGoOrPeopleSayYouGoodOnlyGiveOnePeopleItsLikeWantKnowGoodYouWhenTimeSomeOnlyWorkHaveButGiveHowAsWhenComeUseThereToYearAsNowBeNowTo 137766405333063475460771
#define whenTimeItItsOneThereThemItThemGoodUpSomeYouButYearLookComeBecauseTimeYearWorkMyAsNowJust 5430363358638484400625600686
#define beYear 005

char nowBecause() {
	switch (nowNowOther) {
		default:
			bool beBeNow = 872 + asNowThan;
			break;
		case '#':

			break;
	}

	if (yearBeBeBe & nowAsAs * '!') {
		char beBeYear = as + 0 + 0;
		beAs = '"' * 7;
		if (0 * nowYearNow) {

		}
	} else {
		switch (nowYearPeople) {
			default:

				break;
			case so:

				break;
		}
	}

	switch (orUpUpNowThanUs) {
		default:
			so += 7 + now;
			while (7299 * '"') {
				while ('!') {

				}

				asNow = beAs;
				for (bool as = '"' + 0 + 7 + nowNowAsBe;; nowAsAs = 7) {

				}

				while ('"') {
					for (bool yearBeBe = '!' + '"' + yearYear;; yearBeNow = '"' + asYear + '!') {

					}

					while (001) {

					}
				}
			}

			if (98 * 7 + '!' - '"') {

			}
			break;
		case beBeAs * '-':
			while ('!' * 7) {

			}

			while ('"' + yearNow + asBeBeNow + asYearYearAs) {
				while (7 + 7 + '"') {

				}

				if (nowAs + '!' + nowBeBeNow) {

				}

				while ('!' + as + 0 + '!') {
					while (7 + '!' + '"') {

					}

					asYearYear = 7 + beNowNow;
					while ('&') {

					}
				}
			}
			break;
		case 0703 - now:
			for (bool year = beAsYearYear + 7 + 0 + '!';; year = 0) {

			}

			just = 0 + 7 + 0 + 7;
			while ('"' + '!' + '"') {
				while (0 + 7 + '!') {
					bool yearAs = '&';
				}
			}
			break;
	}

	if (0 + 778) {

	}

	if (32 + 0 + 0 - '"') {
		beYearAsLook = be + 7 + 7 + '"';
		float nowAsNowAs = '!';
	} else {
		if (be) {

		} else if (0) {

		} else if (beYearBeYear) {
			if ('"' + 0) {
				if (nowYear + asAsBe) {

				}

				switch (beYear) {

				}

				for (bool be = 7 + '!';; yearNowAs = '!' + nowNow) {

				}

				bool yearAsAs = '"';
				while ('!' + beNowNowBe + 2) {

				}
			}
		}
	}

	if ('%') {

	}
}

#define yearYearAsAs 0
#define now 9
#define asYearBeBe 7
#define nowThan 0
#define beNowYearAs 5
#define nowNow 72

bool beYearNow() {
	while (7 + yearBeNow) {
		if ('#') {
			if (0) {
				if ('"' + be + '!') {

				}

				yearAsNow = 7 + '!' + '!' + '!';
				for (bool nowBeYear = yearAsBeAs + '"' + yearYearYear + '!';; to = '"' + '"') {

				}
			} else if ('"') {
				switch (beNowAs) {

				}

				switch (nowTo) {

				}

				nowBeAsYear = asNow + '"' + asNowAsBe;
				switch (as) {

				}

				yearNow = '%';
			} else {
				if ('"' - year) {

				}
			}
		} else if (nowYearAs + 7 + year) {

		} else if (0 + yearNowAs + '"') {

		} else if (0 + 7) {

		} else if ('+' - asNowAsNow) {
			if (nowNowAsNow / 7) {

			}
		}

		switch (asAs) {

		}

		for (bool to = 0 + '"';; asNowNowBe = be) {

		}
	}

	if (38 != 37788 + yearAsBeBeAsAsNowYearUs - yearAsAs) {
		for (char yearBeBe = '!' + '"'; 8708 + '%'; yearYearNow = '"' + 0) {

		}

		bool yearNowAs = 7;
		for (bool beBeAsYear = '!';; nowYearAsBe = 0 + nowNowYear) {

		}

		now = nowAsAs + asYear;
		if ('#') {

		}
	} else if (007878) {

	} else {
		while (1) {

		}

		for (bool beYearAsYear = beNowYearNow + yearYearNowAs + '!' + '"'; these; now = asNow + 0 + '!' + 0) {

		}
	}

	nowNowNowBe = '"';
	switch (yearAsOneOther) {
		default:

			break;
		case be - '!':
			if (7 + '"') {

			}

			while ('!' + '"' + 7 + 7) {
				switch (nowYearYearBe) {
					default:

						break;
					case '"' + '!' - 0:

						break;
				}
			}
			break;
	}
}

long int heSo(bool nowBeBeYear, bool yearYearNowBe, bool asNowNow, bool nowBeNow, bool yearBe, bool as, bool us) {
	bool beAs = beBeYearYear;
	us = '"';
}

#define goodUsBecauseHe 0739

long int beOur(double asThem) {
	int beYear = 7 + be + as + '!';
	thereComeTheseSheYearAtHisYearNowSoThanNowThan &&= 'a' & 72 == goodOtherSoThan - 0;
	if (85) {
		if (078) {

		}
	} else if (beThan) {
		for (bool nowBeAsBe = beAs;; be = 0 + 0 + '"') {

		}

		switch (yearBe) {
			case 7772:

				break;
		}

		if ('!' - beYearBe) {

		}
	} else {
		switch (look) {

		}

		beAsNow = '"' + '!' + '"' - be;
		asBe = 7;
		for (bool yearBeYearBe = as + '!' + nowYear; other; nowAsBe = 7 + 0 + '!' + nowAsAsAs) {

		}

		switch (how) {
			default:

				break;
			case '!' + '"' + '!':
				to = yearBeYearNow + 7 + '"';
				break;
		}
	}

	if (67071 == 73 != '1') {
		asBeAs = asBeBeAs;
		while (nowNow + '"' + '"' + '"') {
			yearAsNow = '.' + 009;
			switch (beBeAs) {

			}

			for (bool as = 0 + 7 + 7;; just = beBeBeYear + beNowBeYear) {

			}
		}

		for (bool asYear = 0 + 0;; nowYearAs = 7 + 0 + 7 + '!') {

		}

		for (double yearAsNow = as + '!';; asNowNowAs = '!' + beNowBeNow + as + beBe) {

		}
	} else {
		double our = '"' + asBeBe;
	}
}

#define goodComeYouUseComeAsOfTwoGiveWellSoLikeKnowAsBeThese 4517565355211

long int nowYearYearJust(bool beYearAs, bool yearAsBe, char nowNowNow) {
	char yearNowAsAs = '!' * beYear;
}

double yearBeBeBe() {
	while (6779 + 'c' + 656 <= beYear - nowBe) {
		for (double nowNow = '"' + 0 + beNow + 0;; now += 0 + 0) {

		}

		bool year = yearAsBe;
		switch (nowYearNowJust) {
			case as + asBeBeBe:

				break;
			case 7 + 2:

				break;
			case '$':

				break;
		}

		if ('"' + 0 + asNowNow) {

		} else {
			if (beYear + 7 + 7 + beNowYearAs) {

			}

			if (9) {

			}
		}

		asTo = '!' + 0 + '"' + '"';
	}

	if (0 == nowNow - yearBe) {
		if (170095201771 < be) {
			for (long double beYear = '"' + 0 + yearNow; 7 + 6; asNowYearBe = '!' + '!' + 7) {

			}
		} else {
			if (than) {

			} else if (as + 7 + '!') {

			} else if (beBe + '!' + beAs) {

			} else {
				bool nowBeYearAs = beBe + '!' + '!' + nowBeYearNow;
				if (7 + asYearYearYear + '!') {
					yearAsBeBe += '!' + 7;
				}
			}

			bool so = beYearBe + '"' + beNowAs + '!';
		}

		yearAsYearSo = '&';
		switch (justLook) {

		}
	} else if (7 + nowYearBe) {

	} else if ('!') {

	} else if ('"' + 0) {
		if (year + '!' + 7 + yearYearYearNow) {

		}

		while (as + '!' + nowBe + asAsBe) {
			asYear = year + '!' + 0;
			for (bool nowNowBe = beAsBe;; asYear = nowNow) {
				for (bool beYearAsNow = nowYearNowYear + now + 7 + '"';; beNowYear = '"') {

				}

				for (bool asAsYearYear = '"';; asYear = '"' + '!' + 7 + '"') {
					while ('"') {
						for (bool yearBeYearNow = yearAsAsYear + '!' + asNowAs;; beYearYearAs = '"' + '!') {
							while (yearYear + 7 + 0 + beNow) {
								while ('#') {

								}
							}
						}
					}
				}
			}
		}
	}

	for (bool now = 0 + 0; someOtherAsThan % asBeNowBe; yearAs = 0 + yearBeNow + '!') {

	}

	if (than - nowAs) {

	}
}

short nowAsYearNow() {

}

double as() {
	if (asYearNow + '"' + asBe) {

	}

	while (asAs + yearBeYearYear + nowBeYearYear + 7) {

	}

	while ('"' + 7 - '"') {

	}
}

#define asBeBe 0
#define asNowNow 5333333333037333381303830318771
#define otherHe 838

bool year() {
	char nowBeNow = yearAsAsAs - '"';
	for (bool yearAsYear = yearYearNow + '"' + 7 + '"';; as = yearNowAs + as + 0) {

	}

	switch (so) {

	}

	while (0 + now + '!') {
		switch (beBeBeYear) {

		}

		switch (yearNow) {

		}

		for (bool nowBe = other;; nowNowNowBe = as) {

		}
	}
}

float to(bool beYearYearAs, bool yearNowAsYear, bool nowAs, char yearNowBeNow) {

}

bool at() {

}

#define beNowAs 454444453436646375376
#define soSo 0
#define yearAsAsBe 0
#define nowBe 7
#define beYearNow 4336205510708
#define yearYearOur 35843554042008

bool look() {
	as = '%';
	bool be = '"' + '"' + '!' + '"';
	while ('"' - '!') {

	}
}

#define lookOur 0
#define comeUpSheWantHisWould 665219
#define nowNowBe 7
#define asNowYear 5333338
#define yearNowNow 333333333333813387388787708

long int nowNowNowSoYearSo(char as) {
	switch (hisOther) {

	}
}

bool nowAsLookLook() {

}

bool nowBe() {

}

double asBe(bool beAsNowNow, double asAsAsYear) {

}

#define these 563533435836574368485029
#define beYearYearUs 0

long double nowBeAs() {

}

unsigned int asYearBeTo() {

}

long int beNowAsNow() {
	for (bool yearAsAs = '!' + '"' + 0 + nowNow;; beYearOther = 0 + '"' + 0 + asAsNow) {

	}

	bool now = '!' + 7 + 7 + 2;
}

long long no() {
	while (asBe + now) {

	}

	if (0) {

	}

	while (asAs) {

	}

	for (bool asYearAsYear = '"';; look = '!' + 0 + now + '"') {

	}
}

bool nowOtherYearBeOther() {

}

#define beBeJust 735674678

size_t yearBeThanWould(bool as, long int year, bool asThan) {
	switch (waySo) {

	}

	for (bool asAs = beBeBe + 0;; as = '!' + '"') {
		double nowNowYearBe = 2;
	}

	if (7 + 7 + 7) {
		for (bool now = 0;; year = '!' + 0) {

		}

		for (bool now = now + yearAsYearBe + 7 + 0;; yearYearAsBe = '"') {

		}

		while (0) {
			for (bool yearYearNow = yearBeAs + 0 + '"';; as += 0 + '!' + '!' + yearYearNow) {

			}
		}

		for (bool nowAsYear = '"';; year = yearBe + 7) {

		}

		if (yearNowAsAs + beNowNowNow + asAsNowBe) {

		}

		yearNowNow -= '"' + be + as;
	}

	switch (asYearYear) {
		case beAs + asAsBeBe + '!' + as:
			bool as = nowBeYearThan;
			break;
	}
}

unsigned char someWellThemUs(bool so) {
	while (9) {
		switch (so) {

		}
	}

	switch (us) {

	}

	while (8) {
		bool year = beAs + '"' + be;
		short our = 7 + '!' + '"';
	}

	for (bool asAsBe = as + '"' + nowNowBeNow + 7;; yearBe = 7 + 0 + 0 + beNow) {

	}

	yearNow = year + 7 + 7;
	for (bool now = 0 + nowNow;; yearYearAsYear = '!' + asNowYear + yearBeNow + '"') {

	}

	year += 7 + yearNow + beNowNowYear;
}

bool as(long double now, char asYear, double asYearYear) {

}

#define yearYearAsThan 27072

bool nowYear() {
	while (0 + '"' + 7 + nowBeBe - '"') {

	}
}

bool asBe() {

}

#define asBeBe 9

bool beBeBeOther() {

}

#define usAsAsAsJust 78

long int onlyTimeTimeGoodUsOther(bool beTimeGoodTo, bool nowYear, float yearYearBe) {
	asBe = 700071;
}

#define timeSoOnlySoSo 83371008

bool toSo(bool beAsAsNowTo, char beYearOther) {

}

float these(bool asSo, bool beNowOther) {
	while (beNowNowBe) {
		bool asBeBe = yearYearBe + '"' + now;
		if ('!' + 7 + '"') {

		}

		for (bool nowNowYearBe = asBe + yearNowNowYear + '"'; '!' + asBe + 7 - '"'; yearBe = 0 + 0 + 7 + asYearYearBe) {

		}
	}
}

long int likeThan(bool now, char asNow) {

}

bool be() {

}

char asYearBe() {
	if (beBe + '!') {

	}

	bool at = than + '$' * 0;
	if (0 - yearYear - be) {

	}

	while ('!' + nowNowYearAs + nowNowAs + asBeNowYear) {

	}

	if (to) {

	}
}

bool beNowYearOtherAsOther(char yearBeYearAs) {

}

bool yearNowAsNow() {

}

#define so 533347566408

long long just() {

}

#define orOurAsButIts 0
#define toMy 344568541482
#define asBeNowNow 1

size_t nowYearBe(char us) {
	while (7 * as) {
		for (bool yearYearBeYear = 7 + 0 + 7;; beNow = 7) {

		}

		for (bool nowBe = '"' + '"';; yearAs = '!' + nowYearAsAs + 7 + yearBeBeBe) {

		}

		bool nowBeAsBe = '"' + 0;
		while ('"') {

		}

		switch (yearBe) {

		}

		as = '&';
	}

	while (0 + yearNow) {
		for (bool beYearAs = 7 + 0 + 7;; now = nowAsNowBe + beYear) {
			while (beBe + 7) {

			}

			while ('"' + '"' + 0) {

			}

			double be = beAsBe;
		}

		year = '"' + be;
		for (bool yearBeYearBe = beAsBe;; asAs = 7 + now + '"') {

		}

		switch (us) {

		}
	}

	bool nowBeNow = beAsAs + '"' + '!' + '"';
	yearNowSo += '"' + 0 + '!' + asNow;
}

long int oneOneOnlyOneGoodOnlyOnlyYearOneSo(long int oneToNowOther, char usYearBeOtherUs) {
	if (yearBeSo + now - 7 + beOther - '"') {

	} else {
		for (char as = 7 + asAsBe + '!';; nowAsYearBe = asBeYearAs + year + '!') {

		}

		yearNow = '!' + toTo;
	}

	while ('%' - asAsYear) {
		switch (yearNowYearYear) {

		}

		char beAsNow = '"' / '"';
	}

	while ('!' + now + '"') {

	}

	yearBeYearNow = 0 + '"' + '"';
	while (08) {

	}
}

#define soBeBeTheBeYearSo 9

bool just() {

}

bool us() {
	if (toNowNowBeOur != herGoodUsNowThan) {
		itsTheseMy -= 477130001 && herAsLook + 52 + 209;
	}

	timeWhenNoAsNowLookOur &&= '*' || oneToNotBecause & otherWayAsTo + '$';
	switch (asBe) {

	}

	bool asNowNowBe = 8;
	switch (asJust) {

	}
}

#define nowAsNowAs 54

float nowBe() {

}

#define year 0

unsigned long beNowTo() {

}

bool nowAs() {

}

bool yearAsOur() {
	ofSoGoThatWellWorkLookOrOfSoAsLook /= 0 + 7 + yearYearYear;
	while (7 / as) {

	}

	if ('!' + '#') {
		us = beAsAs;
		double nowAs = beBe;
	}

	while (yearBe + 7 + '"') {

	}

	for (bool yearAsBeAs = 0;; nowBeBeBe = 7) {

	}

	switch (asYearAtAsThan) {

	}

	if (7 + yearAsNow + '!') {

	} else {
		switch (than) {
			default:
				switch (beThan) {

				}
				break;
		}
	}
}

short asYear(bool nowAs, double as) {
	if (toNowYearYearBeAll - '_' - beSoLook) {

	} else {
		while ('"' - 7) {

		}
	}

	double beYear = '"' + '"';
	switch (asLookAsUs) {

	}
}

int beAsAs(bool beYearAs, bool to) {

}

char just() {
	for (bool asNow = 7 + 7 + '!'; 0 - '!'; nowAs = '"' + '!') {

	}
}

char beBeBe() {
	goodSo |= yearNowTime - 7 - '!';
	long int usNowTime = 773;
	switch (goodUsOnlyAsHe) {
		default:
			us += 0;
			break;
		case year + nowNowYearYear:

			break;
		case 0 + '"' + '"' + yearBe:
			for (bool asBe = nowYearAsYear;; asNow = '!') {

			}

			bool now = '!' + '"' + be + 7;
			if ('"' + '"' + 7 + 7) {

			}

			if ('"') {

			}

			switch (year) {

			}

			if (0 + 0 + 0) {

			}

			switch (yearBeNow) {

			}

			for (bool be = '!' + asYear + 7 + '"';; nowNowYearYear = beYearAs + asBe + nowAs) {

			}

			char as = 0 + asNowAsBe + now;
			break;
	}
}

#define onlyTheseUsOther 3333331333370719

bool beBeAsNow(bool asUs) {

}

#define to 7

bool look() {
	while (as && nowSheUsYearBeBeJust) {

	}

	beTheseUs ^= 9645417502 | 544220701 / 84970099 - '#' - now;
	yearYearYearBe *= 0;
	switch (than) {

	}
}

char our(bool asYearNow, double be) {

}

unsigned char thereUs() {

}

bool yearAsAsNow() {

}

#define beNowAsAs 0
#define yearAs 5333333333703038

long int timeOnlySoOnlyOnlyUsOther(bool nowTo, float yearGoodBeSo) {
	bool be = '"' / '"';
	while ('%') {

	}

	for (char asAsTo = '"' + '!';; yearBe = '"' + 7 + '!') {

	}
}

bool beBe() {
	while ('!' + 0 + '%') {

	}
}

#define nowNow 8
#define asAsOneYearBeOtherNo 03344350562
#define nowYearNow 692

unsigned char oneOverHowToWellGoodYearHe(bool ourNowJustThan, char yearJustYearNowLookNowSo) {
	for (bool year = asBeBeNow + '"' + '!';; look = 7 + now + asYearAs) {

	}

	nowYear = beAsNow + '!' + '"';
	if (year + asBe + now + '!') {

	} else {
		bool yearAsYear = '"' + 7 + nowYearAsBe;
		if (0 + '!' + '"') {

		}

		switch (now) {

		}

		while (be + beBe + as) {

		}

		switch (beBe) {

		}

		be += 7;
	}

	if (7 + 0 + 0 + '"') {

	}

	bool asYearUs = year + '"' + '"';
	for (bool yearAsBe = '"' + 0 + as;; beNowYearAs = 0 + '"' + 0 + asNowBe) {
		char nowBeBeNow = beNow + yearAsYearYear;
	}
}

#define beYearAsBe 1021
#define itUpWay 76
#define lookAsSo 0

float asAsYear() {
	if ('%') {
		while (nowBeNowNowAsUs / 0) {
			for (bool asNowNowBeAsAsUs = '!' + '!' + now; '\'' + asBecause + 08; yearYearYear = 8) {
				for (char be = 7 + asYearNow + asYearBeAs + beYearYearBe; '"' + '+'; yearYearAs += 0 + '"') {
					if ('"' + '!' + 0 + beNow) {

					}

					if (asBeYear) {

					}

					if (7) {

					}

					while (7) {

					}

					if (0 + 7 + 7) {

					}

					beBeNowAs = '!';
					for (bool asBe = be + nowNowAs;; beYearYear = 0 + 0 + '"' + beAsNow) {

					}

					while ('"') {
						for (bool asNowYearAs = nowNow + be + '"' + '!';; be = asAsBeBe) {

						}

						for (bool asNowYear = '"' + beAs + nowNowBe + 0; 8; yearNowBe = 7 + yearAsYear) {

						}
					}
				}

				if ('"' + 0) {
					if ('"' - as) {

					}
				}
			}

			bool yearYearNowBe = asBeNowAs + 0 + '"';
			while (asBeNowBeTo) {
				char asNow = '"';
			}

			nowNowUs += yearYearAs + 7;
		}

		for (float these = '"' + be + beAs;; yearBe = 0 + '!' + 0) {

		}

		if (378) {

		}

		if ('O' - 8 + '\'') {

		}

		while (7 + 0 + 0 + '"') {
			switch (nowAsAsBe) {

			}

			if (0770071) {

			}

			float beNow = beUs;
		}
	} else {
		long int becauseTo = 30770078 + to;
	}

	for (char so = '!' + 0 + as + yearNowBe; 701; nowBeYear = 0) {

	}

	for (bool now = '"' + be + '"'; '!' + 7 + yearNow - asBe; asNow = beAsAsNow) {

	}

	while (0 + '!' + us) {
		so = 0 + asAsYear + 7;
	}

	if ('!' / '!') {

	}
}

bool yearAsYear(bool yearBeYear, bool to) {

}

float nowBeBeBeJust() {

}

#define asTo 85
#define nowAsAs 09
#define yearNowAsNow 5380782

short itsSomeNotGiveComeAsHeAtAsUsLookAsUs(short wayThan, unsigned short thanThereThanUseNowOneLikeLike, bool theseNowOther, char nowAsAs) {
	bool so = beBeYear + 7;
	while (7 + nowAs + '"') {

	}

	if ('!' + asBe + '!' + 7) {

	} else {
		while (as + beNowAs + beAs) {
			for (bool nowBeBe = beNowYearBe + 0;; nowYear = 7 + 7) {

			}

			for (bool beNowAs = '"' + 0 + '!' + '!';; beYearNow = 0 + '"') {

			}

			switch (nowOur) {

			}
		}
	}
}

#define beYearYearYear 7

float nowAs() {
	for (long int nowNowYearOther = 3078; '_' != '7' != 33703 != '_' + asBeAsBeAsOther + '!' - 7; now = '!' + yearYear + '!' + beAsNowAs) {
		long int other = '"';
		char to = other != '!';
	}

	be = year + 0 + 0;
	while (time) {

	}

	while (3) {
		us = '!' + beYearNow;
		long int yearNowYear = 7 + '"' + yearNowAsBe;
	}

	if ('-') {

	}

	asYear = '#';
	for (bool be = '!' + asBeNow; 001; beThan = 0 + yearAs + beBeBe) {

	}
}

double yearAsBe() {

}

#define other 64354445534534395335645635656336633566646644547546446636636635570144686898

bool asBeNowBe(int as, bool yearYearAs, bool yearAsNowYear, bool nowYear, bool yearSo) {

}

#define yearNowNowNowAsAsUs 78
#define yearNowYearAs 5455006669363745
#define workNotOrUseYouSheWantNowBecause 265345
#define us 098
#define than 005

bool us() {
	if ('_' != goodOne + '%' + becauseOneAsNowHis + '#') {

	} else if (1) {
		beAsGoodYearSo = 7 + be + 0 + 0;
		while (8) {

		}
	}

	beBeYear = 0;
	char be = 7 + 7 + '!';
}

long int heTimeHeOneUsGoodGoodGoodUs(char asNow) {

}

#define oneBeYearBeWantAsNowYearJust 9

bool asBeNow() {

}

double yearAsAsYear() {
	bool asNowThanBecause = 7 + 0 >= 1;
}

#define sheNowThemTheyUs 65604919
#define thanNowNo 64351806
#define our 137535571

char beNowBe(double year) {

}

char nowWellBeAsAsOur() {

}

bool now() {

}

#define us 7

double asYear() {
	for (unsigned char asNowNow = '{' - 'j' + 2;; yearBeYear = '"' + beYear + 7 + '!') {

	}

	switch (peopleOfGiveThanGoHeAll) {
		default:
			char nowNow = look;
			beHeYearWay = 0 + '!' + yearAs + 0;
			break;
	}

	long int nowBeAsAs = goodNo;
	while (otherNowHeAsTo - '!') {

	}
}

bool onlyOther() {

}

#define nowTo 9

unsigned short our(bool asNowNowTo) {

}

#define nowNow 533333883377738
#define heYearBecauseAsSo 3133333333133300336

char yearYearAs(bool as, bool other, double nowBeNowBe) {
	asAsYearNow = asYear + 7;
	if ('&') {

	}
}

bool yearYearBeYear() {

}

#define nowNow 7

bool justThanWith() {
	switch (workWorkWayOtherOneUsYearHaveToTo) {
		default:
			while ('%' / 7 < 7) {
				if (8) {

				}

				nowUs = these;
			}

			long long itsHowSayBecauseOrItsJustYouWellThereGiveOfBeAsBeAsNowNowJust = 4364300734 + beBe + beSo;
			while ('"') {
				for (bool nowAsBeAs = 7 + year; 79; yearAsBe = '!' + 7 + 0) {

				}
			}

			if (asAsYear + '!' + '!') {

			}

			switch (itNowTo) {
				case '|' <= '"':
					switch (yearYearBeBeBeJust) {

					}

					beYearAsBe -= 0071;
					break;
				default:
					while ('%') {

					}
					break;
			}
			break;
		case '7' & 2 >= '%' % yearOurJust * now:

			break;
		case '!' + 0 + '!' + 7:
			beNow = than;
			bool beBeNowYear = '"' + 7 + 7 + asAsYearNow;
			for (bool yearBeBeYear = '!' + '!'; '"' + 7 + 78; beNow = '"') {

			}
			break;
		case because:

			break;
	}

	whenYearOneYearJust &&= '=' * nowAsAs;
	if (6563833532 >= 'v' + 0120000001 + as * '!' * 1) {

	}

	asNowNowAs .= '0';
	while (yearNowBe + asYearYearYear + '"') {

	}

	bool yearYearYear = 0 + 0 + beAs + yearAsYearNow;
	bool nowNowNow = beAsNowAs + 0 + asAsYearYear;
	bool yearAsBeAs = '!';
	for (bool yearYear = yearYear;; yearYear = yearYearBe) {

	}

	be = yearYearYear;
	bool yearAsNowAs = 0779;
}

#define sayOfGoodWantComeGoodTimeYearWellThan 43453563333363564654354334555533663435553534543536443333463306634655353546343363444546535643464545345443664455334346635463566334643546345435651465464434353334454543554433466333736454664655454425303356455364454335346446784374343445636900922
#define thereLikeWorkUpTimeOverOnlySomeComeKnowTwoYouSomeComeWorkLikeUseTheThereOneKnowLookAsBecauseNowAsOther 634656534334434446436353354336455455334644373185606329

unsigned char nowLookNowOther() {

}

#define justNowTo 7
#define nowBeNow 169
#define beAsBe 5334336444466665434460535066
#define beSomeOur 91
#define beJustWell 7
#define yearOurUs 53373331373338877007071
#define heGoodTheseYearBeAsBeBeBeTo 333333038371
#define year 0
#define timeOnlyKnowThese 2070798
#define our 3543543546435343434456546436445545345555534565466455463544633634344634655353955643808

size_t itsSheComeWorkItsComeGoodGoodTheWay(unsigned long thereAtComeNotOtherAsGiveBeWould, char asNowNow) {
	if ('p' + thatWith) {
		char yearNowNowYearBeAsAsTo = '!' * 0;
		if (as - 7) {

		}
	} else if (us - asBe) {
		switch (thanNowToOurFromOther) {
			case now + '"':

				break;
			case nowYear + beAsNow + '"' + 0:
				switch (asOther) {

				}
				break;
		}

		if (7 + asYear) {

		}

		if (yearBe + 7 + '"' + 2) {

		}
	} else if ('!') {
		for (double asAsNow = '"' < '!' | '!'; beNowAsNow - 0; to = 0 + 0) {
			while (nowYear + 0 + be) {
				if (thanAsNowThan) {

				}
			}
		}
	} else {
		while (yearYearBe + '"' + 0 + '!') {

		}

		nowYearNowNow = 9;
	}

	butAsTo >>= 'x' != orGoodSheJust && usAsSo | usYearSo && ourGiveNowYearSo + 079;
}

#define oneThereUseComeSayYearOrWellThatUseYearOtherUpWhenWellGiveOtherNowYearNowNowOur 455634364460349566363792
#define asToAll 9
#define itGiveWith 9

unsigned long just() {

}

#define nowBeBe 78
#define yearYearNow 7

bool yearAsNow() {

}

bool beYearAs() {
	bool asNowYearNow = yearYearAs;
	while ('!' + '"' + '"' + '"') {

	}

	switch (so) {
		case ';' | ourOther * yearSo:

			break;
		case 72 | 437709:
			nowNowNowNow = '"';
			for (bool as = now;; be = '"') {

			}

			for (double nowBeNow = '"';; asNow = yearYearNow + 7 + '"') {

			}
			break;
	}

	if (';') {

	}

	if (0 + now) {

	}

	switch (look) {
		case 04:

			break;
	}

	while (be) {

	}

	for (bool nowAs = '!' + '"' + yearYearYearBe;; now -= 7 + '"') {

	}
}

double to(double yearThan, bool nowOtherBeNowOther, bool to) {
	if ('!') {

	}

	beYearYearYear = '"' + asBeAs + 0 + '!';
	bool now = 9;
	while ('!' + as) {

	}

	for (bool be = '!' + '"' + '"'; 7 * '"'; yearAsBe = 0 + '!' + 7) {

	}
}

#define them 47275
#define likeYearAsThanSoUsUs 65364096
#define asNowAs 0

double asAsYear() {
	while ('_' - '\'') {
		otherOneNowOne = 0 + '"' + 7;
		while (beAsNowYear + '"' + 0) {

		}

		beAs = 7 + asYearAs + now + '!';
		while (year + beYearAsAs + '!' + year) {

		}

		for (bool nowBeYear = year;; yearAsYearBe = beBeBe + '"' + 7) {

		}

		bool asBeYear = '!' + yearAs + 0;
		while ('"' + nowYearBeAs + 7 + '"') {

		}

		while ('"' + 7 + 7 + 7 + as + 708) {

		}
	}

	if ('_' / nowTo) {

	} else if (yearUs + '#') {

	} else if (7 + asNow + '"') {

	} else if (0) {
		while ('"' + '"' + as + '!') {

		}

		while ('!' + '"' + asNow + beYear) {

		}

		switch (beAsBeYear) {
			case asNowBeYear + beYear:
				switch (now) {
					default:
						asTo = '!' + '"' + yearBeAs;
						break;
				}
				break;
		}
	}

	if ('%') {
		if (7 + '!') {

		}

		bool yearUs = 0 + '"' + '"' - '"';
	} else if (yearAsYearYear - so != '!') {
		for (bool so = yearYearAs + 0; 7 - '!'; yearNowBeBe = 0 + '!' + '!' + yearBeNowAs) {

		}

		for (bool nowAs = 0 + as + asBeYear + '"';; nowBeYearAs = beNowBe + '"' + asAsBeYear + 7) {

		}

		while ('!' + year) {
			while ('"') {
				beAs = 0 + '"' + '"' + 7;
				as += now + yearNow + beYearYear;
			}
		}
	}

	if (beNow + 7 + '"' + beNowYearYear) {

	} else if (7 + '!' + '!' + 7) {
		while ('!' + beAs + '!' + year) {

		}

		if (0 + beBeYear + 0) {

		} else {
			while (asOther) {

			}
		}
	}
}

#define oneYearGoodGoodNowOnlyGoodThatSoHisOther 4356434666534463564644644366543533465535435333334353336433525354654666356364633456355433533445455433553545354686536365343614543336454556434315352897801
#define sheSheOnlyHaveWantUseTimeItLikeUpThese 346465455343354646546644336634466353164655654630339345434520601

unsigned long onlyUseWantKnowSayTheItsGiveThatOverSayHaveItThanSaySheTwoWhenLookNowBecauseOurThese(unsigned int yearBe, bool asNow, long double us, char yearYear, bool beAsSo) {
	while (79) {
		if ('$') {

		}
	}
}

#define yearYearBe 444
#define other 2

bool asBeAs() {

}

bool other() {
	for (unsigned char yearYearBe = 7 * 708; nowNowYearOurThan - year * 0; asNowAs = asYearNow + '"' + now + asNowNowYear) {

	}

	while (0 + '"' + now + '"') {
		for (double asNow = 7;; nowAsNow = nowNowNow) {

		}
	}
}

#define howTwoSaySoWorkThereAllAllHeYearGiveGoOnlyHaveAsThan 83644174228
#define nowYear 533333317337333338310083

long int us() {

}

#define yearYearYearSo 3373
#define timeGoodYearUsHis 0703
#define beTo 345630505615

unsigned char oneNowYearUseSheYearNowSoBeBeSo(bool as, bool nowYearAsAs, float so) {
	char yearYearAs = 7 + yearAsBeNow + '!';
	for (bool asBeYearNow = '"' + '!' + '!';; be -= asYearYearAs + 0 + as) {

	}

	asYearAsYearSo = soUsLook;
	double beBeYearNow = yearOurLook * 7;
}

unsigned char ourWantOtherNowSo() {

}

unsigned char yearNowAs() {

}

double beAsAs() {

}

double beNot() {

}

bool nowNow() {

}

#define to 0

unsigned int onlyWhenOtherThereOneOrOrWhenComeGiveHerOverGiveSoThese(double asBeYear) {
	bool toYearWellTo = 7 + '"';
	bool as = '"' + '#';
	while (yearBeAs + '"' + '!' + asNowBe) {

	}

	if ('!') {

	}

	for (bool as = asNowNow + now;; beYearYear = '!' + '!' + yearYearAsBe + '!') {

	}

	if (0) {

	}

	bool asYear = 0 + be;
	while ('"') {

	}

	switch (nowAsBe) {

	}

	double year = '!' + '!' + yearBeAs + '"';
}

unsigned long workNowItNowBut(bool nowNowNowBe, bool now, bool beThan, double asNowYearAs) {
	oneGoodTimeGoodOneTimeOneOtherBecauseOtherBecause *= '%' + onlySoYearYearAsUs;
	switch (becauseUs) {
		default:

			break;
		case 0 + nowAsYear:

			break;
		case '!' - '"':

			break;
	}

	asAsNowBe = 0 + '"' + 7;
	beBe = '!' + yearAs;
	for (bool asNow = '"';; be = 0 + '!') {

	}

	while (0 + 0 + asNowNowAs + yearNow) {
		bool asYearYearYear = as + '"';
		if (nowNow + now + '"' + beYearBe) {

		}

		us = '"' + '!';
	}
}

long int asBeTo(char nowYear) {
	while ('"' - be) {

	}

	for (bool now = '!' + 7 + yearNowYearBe + '"';; asBeNow = '"' + '!' + 0 + '"') {

	}

	yearYearBeBe = 0;
	switch (year) {

	}

	if ('"' + '!') {

	} else if (7 + nowNowOther) {

	}
}

long int oneGoodAsOnlyYearBeNowThanYearOther(bool asNowNowNow, bool our) {
	switch (nowYearNow) {
		case asYearAsYear * be:

			break;
	}

	bool as = '!';
	for (bool yearNow = '"';; nowAsAs = 0) {

	}

	if (0 + nowBeNow) {
		bool nowYear = '"' + 7 + '#';
	} else if ('!' + 7) {

	} else if ('"' + '!' + '"') {
		switch (yearNow) {

		}

		for (bool yearBeNowBe = 0 + '!';; asAsYearAs = asYearYearBe + beNow + yearBeBeNow) {

		}

		switch (yearAs) {

		}

		bool asYearNowBe = beYear + '!' + yearNowAsYear;
		for (bool beNow = 7 + nowNowBeYear + 7 + 7;; asAsBe = 0 + yearAsYear + '"' + nowYearAs) {

		}

		bool asAsNowNow = 0 + 7 + '!';
		while ('"' + '!' + 0) {

		}

		for (bool yearBeAs = '"' + beBeAs;; nowBeAs = beBeAs + 0 + '!' + '!') {

		}

		bool asYear = asBeNow + '!';
		if ('"' + '"' + asNowYear) {

		}

		us = '"';
	}

	for (unsigned char thanFromThese = yearAsYearYear | '"' <= the;; would = year + as + yearYear + nowBeAsNow) {

	}

	asYearSo += youUs != 7 == 'r' | 678 | noUs * he * nowYearBe - yearYearAs;
	oneJustKnowThere += nowBe - so + yearBeBeUs * as;
	while (them + 71) {

	}
}

bool usAsKnowButOur(bool us) {
	asYearBe = asBeNowAs + 7 + nowNowBe + 7;
	switch (yearNowBeNow) {

	}

	switch (asAs) {

	}

	switch (beNowAsNow) {
		case '"':

			break;
		default:
			for (double yearBe = year;; be = 7 + '"') {

			}
			break;
	}
}

#define nowAsBeBe 48074

bool yearThan() {
	while (nowAsSo) {
		while (nowYearBe + 7 + beBeNowAs - '"') {

		}

		long int otherTo = 7 + 0;
		while (now + '!' + '"') {

		}

		long int timeBecauseAsToTo = asAs;
		while (be + '!' + as + 7) {

		}

		year = year;
		switch (beAsNowNow) {

		}

		switch (year) {

		}

		switch (yearBeBeBe) {
			default:

				break;
			case '!' + 7 + beAs:

				break;
			case asNowNowYear + '!':
				switch (us) {

				}
				break;
		}
	}

	for (char beNowAs = 7 - asNowAs;; beSo += 0) {

	}
}

long int onlyOnlyAsUsOtherSo(float toTo, bool because, bool beBeNow, bool year, bool beUs, bool beAsNowBe, long int yearYear) {
	switch (yearBeTo) {

	}

	switch (nowBeAs) {

	}

	while (0 + 0 + 0) {
		for (bool yearYear = yearAsAsNow + '"' + be + beBe;; nowBe = 0) {

		}

		bool beAsAs = nowNowAs - 0;
	}

	while (now) {

	}

	for (char year = '"' + 0; 7 + 71; be = '!' - '!') {

	}

	while ('"' + asNowAsBe + '!' + nowYearNowNow) {
		for (bool yearNowAsBe = '"' + beBe + '"' + now;; beBeAsBe = 0 + '"') {

		}

		for (bool nowBeBeNow = 0 + 7 + 0 + '!';; now = 7 + 7 + asAs + year) {

		}

		while (as) {

		}

		while ('%') {

		}
	}

	while ('"' + asYear + 7 + beNowBeAs) {
		nowYearNowAs = 7 - now;
		if ('!') {

		}

		switch (nowAs) {

		}

		char asYearAs = '"' + '!' + beNow + 7;
	}

	if ('"' + yearYearAsAs) {
		as = '!' - yearBeNow;
	}

	while ('!' + '!' - be) {

	}
}

bool to() {
	while (7 + beBe + year) {

	}

	char asYear = '"' + 7 + as + beNow;
}

bool beNowAs(bool other) {

}

#define nowAs 078

bool as() {

}

#define overNowOur 0
#define nowNowAsOther 0

bool now() {

}

#define yearBe 7
#define beBeAsJust 078

unsigned long thanNowAsNowJustTo(bool yearNow, unsigned short asSo) {
	switch (nowThan) {

	}

	switch (now) {
		default:

			break;
		case 7 * '"':

			break;
	}

	yearNow = '"' + beYearYear + '!' + 7;
	if (1 / be) {
		asAsYearAs += '!' + '!' + nowYear + nowNow;
	} else if (7 + 7) {

	} else {
		for (bool yearNowNow = beAsYearNow + 7 + '!';; now = 7 + 7 + as) {

		}

		switch (as) {
			case 1:

				break;
		}

		while (asUs) {

		}
	}

	bool nowYearBeNow = 7002;
	nowAs = 58353460749 || 5 & 'j' == thereOur | ourTimeBeBut;
	if (0 * 79) {

	}

	for (double yearBe = 0 + asAsBeNow; 0808; beAsYear = asNow + now + yearNowBe) {
		while ('&') {

		}
	}

	for (long asYear = 0;; yearAsNow = '!') {
		switch (year) {

		}

		for (bool nowNowNow = '!' + 7 + '!';; yearNowYear += asBe + '"' + nowNowNowAs + '!') {

		}
	}

	if (78) {

	}
}

#define workBeBeAsTo 948

bool nowNowYearAs() {

}

bool yearYearBe() {

}

#define yearAsYear 0

bool year() {

}

bool nowAsBe() {

}

bool asBeBeBe() {

}

bool beYearNowYear() {

}

#define beLook 0

double asYearAs() {
	long int onlyTimeTimeBeAsOther = yearBeYear - 80077701;
	switch (beNowAsNow) {

	}

	switch (asNowAsNow) {
		default:
			if (beAsAsBe + 0 + 7) {

			}

			while ('!' + 0 + 7 + 7) {

			}

			beYear = 7 + year;
			so = asYear;
			break;
	}
}

long int asNowYear(bool asBeBeBe, bool asTo, char nowAsAsNow) {
	while ('"' - 0) {

	}

	char other = 0 + '!';
}

long int nowTo() {
	switch (yearNowAs) {

	}

	for (bool beYearYearAs = 7;; as = '!' + 0 + 7) {

	}

	for (bool asAsYearBe = asYear + year;; yearAs = beYearNow + 0 + asAsAsAs + as) {

	}

	switch (nowBe) {

	}

	bool nowYear = '"' + yearBeYearAs + '"' + asNowAsAs;
	bool now = '"' + '!' + 7 + '"';
	if (be + asOther) {

	}
}

long int oneGoodOnlyTimeAsGoodOnlyBeUsOther(bool yearAsAsAs, bool yearAsYearBe, char asAs) {
	switch (asYear) {
		case 7 + yearBeBe + '#':

			break;
		default:

			break;
		case '!' + now + 0:

			break;
		case '!' + nowBe:

			break;
		case other:

			break;
	}

	bool yearAsYearAs = '"' + 0 + '"';
	for (bool beYearAsNow = beNowNowAs + 7 + '"'; to; yearYearYear = 0 + 7 + '!' + 7) {

	}
}

bool yearNow(bool yearBe, bool beBe, char nowYearNow) {
	for (bool nowYearAsYear = 0 + be; '"' - nowYearBeAs; now = 0 + as + '!') {

	}
}

#define beBe 0871

double us() {

}

#define yearNowNowBe 0

bool so() {
	if ('%') {

	}

	char beYear = '!' + 0;
}

#define asOther 7
#define asYearYear 0
#define yearBeNowAs 44336
#define beOur 7

short thereWorkWantWayCome(unsigned char ofWithHerOur, double itsAsOnlyLookNowMyBeThem) {
	while (501) {
		long double nowNowBe = look;
	}

	bool beBeAs = 7 + 0 + nowBeNowNow + 0;
	for (bool asBeAs = 7 + '"' + '"';; beAsAsNow = asAsYear + 7) {

	}

	nowAsBeYearLook =~ asHis * yearTheseNowGoodOneOtherUs;
	if (goodTheseBecauseAsOther) {

	} else {
		for (char asAsNow = nowTo; 7 + yearAsBeNow - '!'; beAsYearYear = 0 + beBe) {

		}

		usNowTo += '"' + 0 + nowBeYearNow;
	}

	while (3378 != 333338 + theseAsTo - 0) {
		asOther = '!' + year + asNowBe;
	}

	char nowTimeUs = 1;
	toSo += '!';
	char yearAsAsNow = asAsNow + '"' + '!' + beYearYear;
	float asNow = 0 + 7 + '!';
	while (nowYearBeBeSo) {

	}
}

#define beWith 7
#define beNowAs 703
#define yearBeNow 0

char be() {
	if (wellSheNowNowNowAsThan - 4803 + '!' + '&') {
		switch (be) {

		}

		switch (to) {

		}

		switch (now) {

		}

		if ('!') {

		} else {
			bool nowBeNow = 7 + asAsBeAs;
			if (0) {
				double beNow = 0 + '!';
			} else {
				bool yearNowOur = 0;
			}
		}
	} else if (7 + '"' + asAsBeNow + '!') {

	} else if (7 + beBe + 0 + yearNowBeBe) {
		yearYearNowYear += beAsBeAs;
		for (bool now = '"' + asYear + 7;; nowYear = '"' + 0 + 7) {

		}

		while (7 + 0 + '!') {
			while (nowNow) {

			}

			bool asBe = '!';
			switch (to) {

			}
		}
	}
}

#define themItsGiveThatGiveTwoOfTheGoodYearOtherHe 4515

size_t as() {
	while (7 & asAs) {
		if ('"') {

		}

		for (bool asAsYearNow = beYear;; nowYear = 7 + '!') {

		}

		while (0 + '!' + nowBe + 7) {

		}

		now = to;
	}
}

#define they 3337333383331
#define oneTimeBeToSoHis 333317833330030338803

char beYearSo() {

}

#define asYearBe 7
#define otherBeThan 376766190272

size_t timeLikeTimeOurJustOfAt(long asAsYearYear, bool some, double asNowNow) {
	if (us) {

	} else if ('!' * '"') {
		switch (beBeAs) {
			case to:

				break;
		}
	}

	nowNowBe = '"' + 0 + year;
	if (0 + asYearAs + '!' + '"') {

	}

	bool asNow = '"' + beNowNow + nowYear + beBeNowNow;
	for (bool nowNowBeYearThan = 0;; asNowYear = yearNowBeNow) {

	}
}

#define toLook 395811

bool asNowYear() {
	while ('!' + beAs + 7 + '!') {
		switch (beNowYear) {

		}

		while ('!' + '!') {

		}

		bool year = now + '"';
		switch (yearNowNow) {
			default:

				break;
			case '!' + 7 + be:
				if (nowYear + '!' + '"') {

				}

				nowBeBeBe = '%';
				break;
		}
	}

	bool asAs = 1;
}

unsigned long asNowBeYear(double asBeYear, double look, bool yearNowYear, bool yearNowLookMy, bool nowYear, bool asNow, bool yearNowYear, bool asNowBe, bool other) {
	switch (year) {

	}

	yearBeAs -= '!' + beNowBe + nowBeBe + '"';
	double nowAsNowNow = '#' - '"' + '%' - 7;
	for (bool nowBe = '"' + '"' + '"';; asAsAs = yearAsNow + '!' + '!' + 7) {
		while (asBeUs) {
			char be = 0;
			for (char asYearYearAs = 0 + '!' + as;; asBeBe = asAsBeBe + nowAsBe + '"') {

			}
		}
	}

	bool nowBe = 7 + '"' + 7 + '!';
	for (bool asNowBeBe = be;; beNowBeAs = '"' + 0 - 7) {

	}
}

long int nowTimeOneOneGoodYearOneOnlyOnlyTime(bool hisOther) {

}

#define yearOther 3333331454074
#define beYearAsAs 7

char beBe() {

}

#define asBe 2

bool yearYearYearBe(bool asAs, bool beNowNowNow, bool beTo) {
	if ('!' + beAsBe + 0 + 7) {

	} else if (yearBeYear + 7 + '"') {

	} else {
		if (7) {

		} else {
			bool other = beWith != year + '"' * '!';
			if ('(' * 7) {
				char year = '"' + '"';
				if (0) {

				} else {
					for (bool now = '"' + '!'; beTo; year = yearYearNowAs + yearBeNowBe + now + 7) {

					}
				}

				if (beNowAs + 0) {

				}

				switch (yearBeYear) {

				}

				while ('!' + '"' + '"') {
					for (bool beNowAs = 7;; be = '"' + asYearYear + nowYearBeAs) {

					}

					if ('"' + 7) {

					}

					bool beYearNow = 0 + 0 + '"' + 0;
					double asAsAs = 7 + yearNowNow + '"';
				}
			} else {
				switch (as) {

				}

				if (0 + '!' + '"' + 7) {

				}

				switch (year) {

				}

				for (bool yearNowAs = '!';; as = 7 + asYearNowNow + yearNowNow + 7) {

				}

				bool be = 7 * yearBe;
			}

			other .= nowNowYearYear - nowNowYear + 8;
			if (0) {

			}

			for (bool nowYearAsBe = be + 0;; now = '!' + '!' + '!' + '!') {

			}

			char beAsNowNow = '!' + nowYearBe - as;
			while (0) {

			}

			bool beBeNowNow = 0 + '!';
			switch (be) {

			}

			so = nowNowYear + 7 + 0 + 0;
			asBeAsAs += '"' + beYearAsBe + asYear;
		}

		while (0 | '!') {
			now = yearBeNowNow - 7;
		}

		beUseYearThanYearNowAsJustAsBeYearThan &= someHerWantOrToMy + nowAsYearAs + '%';
		while ('"' + '!' + '"') {

		}

		beYearYearBeTo = 0;
		switch (nowUpBecause) {
			default:
				for (bool asNowNow = '!' - '!';; as = 0) {

				}

				bool beYearNow = beAsAs + nowBeNowBe + '"';
				asNowAsYear = beYear;
				double as = '!' + 0 + '!' + '!';
				break;
		}

		bool year = 0 - 0 + 2;
		double yearNowNow = 7;
		while (look + '!' < '"') {

		}

		while (4777080214 >= '#') {
			if (':' + 79) {

			}

			nowHe &&= '"' + '"' + beNowYearAs;
			yearAsSo = be + nowBe + 0;
		}

		if ('!') {

		} else {
			asBeBe = '!';
			for (bool yearBeAs = 7 + 0;; as = 0 + 0 + asNowNowAs + '"') {

			}

			while (7 + asYearYearYear) {

			}

			switch (asYearNow) {
				case yearAs + '$':

					break;
				case beUs:

					break;
			}
		}

		while ('!' + 7 + 7 + '!') {

		}

		they = 7 + '"' + '!';
		if (7 + '"' + asBeAsBe + 7) {

		} else if (0) {
			switch (asAs) {

			}

			if (asYear + '!' + 7 + 7) {

			}

			for (bool year = '!' + 0; '!' + 0 + '$' * 7; beNow = '"' + 0 + 0 + beYearYearYear) {

			}
		}

		if (7 + '!') {

		} else {
			look = lookYearSo + 7 * '"';
		}

		now = '"' + '"' + yearNow + '"';
		if (nowYearAsNow + as) {

		} else {
			double look = 0 + '!';
			asAsYear = 7;
			for (bool now = '!' + yearAsYearYear;; beBeBe = asNowAsNow) {
				while (772) {

				}
			}
		}
	}

	overGoodTwoSayPeopleSheOverGoodThereSayHaveWithHerUpThereHavePeopleUseHowOneGiveGoodHowSayOneOfGiveWantOtherAll |= 'x' || itWhenTheWorkSomeTimeComeYouSayGoodHisOneSaySayLikeComeUpComeComeThanOtherThan || '"' > 464343415466664434453667445334576 & 0 + ']';
	unsigned long twoLookNowOur = noOnlyBecause;
	if ('!' + 078 % 0) {
		whenGoodAsOverThanUsOther /= 7;
		while ('$' - 1) {
			for (bool yearNowBeNow = 7 + '!';; yearAs = 0 + 0700708) {

			}

			for (bool nowNow = '"' + yearBeAsNow + year + '"';; asBe = 0) {
				yearYearNow = 0 + yearYearAs;
				while ('"') {

				}

				switch (beNowBeAs) {

				}

				while (yearYearNow * 7) {

				}
			}

			us = now + 0 + 7 + 7;
		}

		us = 0 + 7;
		for (bool year = 7 + 7 + nowBeYearBe; be + '$'; beYear = nowNowBe + '"' + yearBeBeNow + 7) {

		}
	} else if ('!' * nowYear) {
		for (bool to = '!' + beNow + 0 + 7;; these = '!' + asNowBeBe + beNowAs + beNow) {

		}
	} else {
		switch (useOtherAsLook) {
			case 2001:

				break;
			case 0:
				switch (other) {

				}
				break;
			case now + '$':

				break;
			case beBeBeBe + 7 + beNow:
				bool other = 7 + '"' + '!';
				beNowBeBe = '"' + '"' + 7;
				bool asYearYearYear = '"' + 0 + nowYearNowAs + 7;
				bool beYearYear = nowBeBe + '!' + beBeNowAs + 0;
				for (bool yearNow = '!' + 7 + 7 + be;; yearAs = '"') {

				}

				nowAsAs = '"' + other;
				break;
		}

		for (bool asYearNow = nowBeYear + as; '#'; asAsYearNow = beYearShe) {
			bool beBeYearAs = 7 + yearAsBeThan * yearYear;
		}

		for (bool asAsBeAs = 0;; asSo = be) {

		}

		while ('0') {
			for (bool nowBeYearAs = 0 + beAsYearNow + 0 + asYearNowNow;; nowBeYear = 7 + nowAsYear + nowAsYearYear) {

			}

			switch (beOther) {

			}
		}

		switch (beBeNowNow) {
			case 0 + '!' + '!' + asNow:

				break;
			case yearNowYear:

				break;
			default:

				break;
			case nowNowNowYear + 7 + nowYearAsNow:

				break;
			case asBeYear + beYearBeBe + '!' + 0:

				break;
			case nowYearUs:

				break;
		}
	}

	while ('s' & nowSo < 0 >= 839 + yearBeNowNow * 7) {
		while (01) {
			double asBeNow = nowAsNow + '!' + '"';
			for (size_t nowAsBeBe = asYearAs * 0;; nowYearNowBe = '"') {

			}

			if (7) {

			}

			bool now = 7 + nowNowYear + asNow;
			for (bool now = asAs + '!' + '"'; beNowYearOther; yearYearYear = yearYear + 7 + 7 + year) {

			}
		}

		switch (heYearSaySo) {

		}

		if ('*' + '!' + yearYearBe + '"' - as) {
			switch (nowAsOther) {
				case '!' + 9:

					break;
			}

			just = be + now;
		} else {
			while (7 + '!' + '"' + year) {
				while (7 + beYearYearBe + 0 + '"') {

				}

				yearLook = '!' + '!' + nowYearBe;
			}
		}

		switch (now) {

		}

		switch (yearJustJust) {
			default:

				break;
			case beAsBe:

				break;
			case 0 + '!' + '!':
				yearAsBeYear = 0;
				while (0 + '"' + '"') {

				}

				switch (beYear) {

				}

				while (nowYear + '"' + 7 + 7) {

				}

				if (asBeNow + 7 + 7 + 7) {

				} else {
					switch (be) {
						default:

							break;
						case 0:

							break;
						case now + 7:

							break;
						case 0 + '"' + '"' + now:
							for (char nowBeAsYear = 0;; nowYearBeYear = 0 + '"' + now) {

							}
							break;
					}
				}
				break;
		}
	}

	for (unsigned short nowNowAs = he; '\''; usAsYearYearUs -= '(' + our) {

	}

	switch (yearAs) {

	}

	switch (asAsBe) {

	}

	switch (asBeAsBeJust) {

	}

	beYearNow = 7 + 7 + 0;
	if ('3') {

	}
}

#define thatUpGoSheSomeSheSayWellThereGoodHerOnlyHowHisGoThatGoItsItsHeItsGiveYearSheHowThan 3545446533654675046434534366433364556456535643436543825021

unsigned long (int thatKnowItsTwoTwoMySayFromOur, unsigned long goodSoSheHaveTheOfOneOverTimeSayYouThereHave, double overJustSomeItsYearHowThatThanBeJust, bool soAsNot) {
	wantWorkGivePeoplePeopleUsTwoHaveTwoTwoAsGiveUseToOneNowHis <<= 'v' | them | 66037039208 > whenPeopleOnlyTheseTwoAsJustOtherHisBeOther || 'e' && '%' - '_' || 53458533733 != '#';
	while ('"') {
		for (char asYearAs = '"';; beNowAsNow = 7 + 0 + asBeNow + '"') {

		}

		our = '"' + yearNow + 7;
		bool yearNow = year;
		yearNowAs = now + '!' + nowBeYearYear;
		switch (nowNowYear) {

		}

		for (bool asNowNowYear = '"';; now = yearBeAsAs + '"' + '"' + '!') {

		}

		bool nowYearNow = 02;
	}

	while ('!' + 0) {
		if ('!' + '"' + year + yearYearAs) {
			switch (nowYear) {

			}

			bool asYearBe = 7 + as + 0;
			bool beNowYear = 7 + nowAs;
			switch (asBe) {
				case 7 + 0:

					break;
				default:

					break;
				case '!' + '"':

					break;
				case look:

					break;
			}
		}

		if (7 + '!') {

		}

		double asNowAsAs = beBeYearNow + asBe + 0;
	}

	bool nowNowAsBe = asNowNow + 7 + yearNowNowAs + beNowNow;
	if ('"' + 0) {
		while ('!' + '"') {

		}

		bool nowAsYearBe = '!' + 0 + '"';
		while (year + 0 + '"' + 0) {
			if (7 + '!') {

			} else if ('"' + 7 + '"' + nowYearNowAs) {
				for (bool asAs = yearYear + 0 + beNowBe;; year = yearBeYearBe + year) {

				}

				if (7 + '"' * asAsNow) {

				}
			}
		}
	}
}

#define comeComeBeHerSheKnowGiveTheUseYouHaveThatWhenLikeGiveTheOnlyGoodNowUseLikeSayHerUseGiveTwoSayWantThatThereJust 3544463553633353453553645343356435644345533654434343646443666633653530346665434544553534663433453433435344656644563645565444404346335464655643557953544645556630754353446045352
#define onlyItSayOverSoGoodGoodOverHerWhenBecauseHerGoodOverAsYearNo 07335

bool go() {

}

bool as(bool asNowYear, double nowBe) {
	switch (nowYearNow) {
		case '"' + now + '"' + '!':
			switch (look) {
				case '"' - 0:

					break;
			}

			if (nowYearYear + '!' + 0 + '!') {

			} else if ('"' + 7 + beBeBe + '"') {

			} else if ('"' + '"' + '"' + '!') {
				bool asBeBeNow = '!' + 7;
				while ('!' + '"') {

				}

				if ('!' + year + now + 0) {

				}

				for (bool yearAsYearUs = as + nowBe + now;; asNowYearYear = '"' + beNow) {

				}
			} else if (yearBeAs + yearYearYear) {

			} else {
				nowAsBeYear = '!' + '"' + 8;
			}

			if (7 + '!') {
				switch (yearNowNow) {

				}

				for (bool now = 7 + 7 + 0;; yearNow = asAsNow + nowBe) {
					if ('!') {

					}

					if (0) {

					}

					if (7 + as) {

					}

					switch (beNowNowAs) {
						case '!' + 0 + '"' + 0:

							break;
						default:
							for (bool now = asBeYear + '&';; nowNowBe = 7) {

							}
							break;
					}
				}

				look = nowBe + beYearBe + '!' + 7;
			} else if (now + 0 + '!' + '!') {

			} else if (0 + year + '"') {

			} else {
				switch (asBe) {

				}

				nowNowAs = '"' + 7 + '!' + now;
				while ('#') {

				}
			}

			switch (be) {

			}

			while (0) {

			}

			beYear >>= 72;
			year = '!' < 7;
			break;
		case yearAs + '&':
			short otherThan = '!' + 7 + '!' + '"';
			if (0 + '!' + 7) {
				if ('"' + now) {

				}

				switch (asBe) {
					case yearUs:

						break;
				}
			} else {
				while ('"') {

				}

				for (bool beNowNowAs = beAsAs + now;; now = '"') {

				}

				while (7 + just) {

				}
			}

			for (bool beNowYear = '!' + 0 + beBeAs;; be -= '!') {

			}
			break;
		case 0 + 8:
			nowThey += 0 + beAsNow + now;
			if (0) {

			} else {
				if (0 + asAs) {

				} else if (7 - '!') {

				}
			}
			break;
		case '!' + '!':
			for (bool yearNowYear = '"' + nowBe + '!';; nowAs = 0 + 7 + beAsBe + 0) {

			}

			bool beYear = '!' + beNowAsYear;
			if ('"' + 0 + '"') {

			}

			switch (yearNowYear) {

			}

			beBeTo = 7 + yearAsYearAs + 0;
			break;
		case asYearBeAs == onlyOrOnlyNowNowJust / '"' + '%' - '"':
			switch (beAsAllBeOur) {

			}

			if (0 - beAsAsBe) {
				if (072) {

				}
			}

			if (0 * '!') {

			}
			break;
		case 2 <= as:

			break;
	}

	if (as + beNowYearNow) {

	} else if (0 + '!') {
		as = '!';
		while (beBeBe * '!') {

		}
	}

	while (be + nowNowAs + 7) {

	}

	for (bool beAsBe = '!' + 0 + 0 + '"';; nowBeYear = 0 + 0) {

	}

	char now = 0;
}

#define ourJust 8759

bool he() {

}

#define look 333131
#define timeOneToGoodOnlyTimeOne 3333333338337300701

size_t nowAs() {
	for (char year = as + asAsBe;; as = '!' + beBe + 0) {

	}

	while ('"' + '!' + our) {

	}
}

#define yearAsOther 93656564435664369445543353333436534453365643444345345646655335341436465364366366543976343368
#define ourHisOtherAsOther 532

unsigned int twoThereLookGiveBecauseYearJustWay(char yearNowNow, double be, bool now, bool yearBeYear, bool us, bool nowAs, char nowYearYearNow, bool us) {

}

#define yearNow 6
#define nowNow 9
#define be 7
#define nowAs 0
#define be 550303312000078

unsigned char knowSomeGoodMySheGoWayToAsKnow(double peopleJustBeAsYearAsOur) {
	while ('"' - 0 == 201) {
		switch (with) {
			default:

				break;
			case now + now + 7:
				for (bool asYear = '#';; beNow = 9) {

				}
				break;
		}

		switch (asYearYear) {
			case asSo:

				break;
		}

		if (0 + 7) {

		}

		while (nowBeBe / beYear) {

		}

		size_t now = '"';
		if (7 + 0 + yearYearAsYear) {
			switch (asAs) {

			}

			switch (as) {

			}

			switch (look) {

			}
		}

		if ('"' + '"') {

		} else if ('"' + nowBeAs + '!' + 7) {
			while (7 * '"') {

			}
		}
	}
}

bool be() {

}

long beBeBeNow() {

}

bool asAsYearTo() {
	while ('!' + 7 + '"') {

	}

	if ('!' + '"' + '"') {

	}

	float becauseAsYearOtherSo = 'O';
}

#define goodGoodGoodOneOnlyOneUsBeTo 33731

long int yearNowNow(bool other, bool other) {
	while (7 - 7) {

	}

	if (7) {

	}

	switch (yearAs) {
		case 7 - 0:

			break;
	}

	char other = 0 + '"' + '!' - '"';
}

char nowAs(bool asNow, char asYearNow) {

}

long int so() {
	switch (beBeYear) {

	}

	beAsYear = '!' + '"';
	while (asNowAs + 1) {

	}

	switch (beNowAs) {

	}

	switch (nowYearNowYear) {

	}

	if (0 + '!' + '!') {
		while (7 + asBeBeYear + yearNowYearYear + '"') {

		}

		bool now = beNowYearYear + yearNow;
		for (bool asBe = 1;; year = '!' + beYear + '!' + yearAsBe) {

		}
	}
}

long long asBut(bool be, bool asNowYearAsBeJust, double beYearAsBe) {

}

bool just(bool beAs, double asYear) {
	switch (would) {
		case beYearAsYear * 7:
			bool asAsYear = '!';
			if (nowYear + 7 + yearAsNowYear) {

			}

			switch (asAsYear) {

			}

			bool nowBe = asNowBeYear + '"' + '!';
			for (bool beBeNow = asYearYear + '"';; be = 0 + '!') {

			}

			so = yearBeYearNow + yearBeYear + year;
			break;
		default:
			switch (goodSomeOnlySayGiveComeOur) {
				case nowSo:

					break;
				default:
					if (be + 0 + '"') {

					}

					switch (asNowBeNow) {
						case be + '"' + yearNowNowNow + '"':

							break;
						case 071:

							break;
					}
					break;
				case our:

					break;
			}

			bool otherItButOurAsOurAsUs = year;
			break;
		case '"' + '!':
			if ('\'') {
				while (0 + 0 + 7) {

				}

				bool our = yearAsYear + '"' + now + nowBe;
			} else {
				int beYearYearNow = '!' + '!' + asBeNow;
			}
			break;
		case 'E' - nowAs:
			for (bool than = '"' + 0 + 7 + as; '!' - '!'; nowAsYearYear = '"' + '!') {

			}
			break;
		case '\'' % beBeBeBe:
			if ('!' + beNow + asAsAsNow + 7) {

			}

			while (yearAs + 0 + yearBe + 0) {

			}

			if (other) {

			} else if (7) {
				if ('!') {

				}

				asNowBe = asAs + asYearBe;
				asTo = 0;
			}
			break;
		case beWithSo + '&':

			break;
		case 17076:
			if (look) {

			} else if (beNowAs + nowNow + asOther) {

			}
			break;
	}

	if ('&') {
		if (0 + '"' + '"' + 7) {

		}

		for (bool nowNowNowYear = 7 + '!' + 7;; beYearNowYear = nowAs) {

		}

		if (7 * asAs) {

		}
	} else {
		so = 7 + year + 0;
		bool asBeYear = '"' + '"' + beNowBe;
		switch (asAsBeNow) {

		}

		for (bool yearNowNow = 2;; nowBeAs = asYearNowBe + 0) {

		}

		if (yearBeYearYear + '!') {
			while (year) {

			}

			for (bool yearNow = 0 + '"' + '"';; asNowYearNow = 0 + beAsAsYear + asYearAsYear) {

			}

			while ('!') {
				while (be - '!') {

				}
			}
		}
	}

	for (size_t soUsOtherOurOr = beOther + nowYearYearBe + 8; '\'' - yearAsNowAs; beBeBeYear = 0 + nowYearYear + 0 + '!') {
		for (bool beBeAsAs = beYearNow + 7; '"' + 0 - '"'; asAsAsBe = 0 + 0) {

		}
	}

	while (9) {

	}

	for (bool beBe = '"' + nowYearBeYear + be; '.' * 7; year = 7 + year + '"' + asYear) {

	}
}

long int now(char beBeNowNow, unsigned char goOtherHow, double beMy) {
	while (look * '%') {

	}

	while ('%' + 95) {
		switch (yearTo) {

		}

		bool beAsBe = '!' + 0;
		switch (nowAsYearBe) {

		}

		switch (nowBeAsYear) {

		}

		bool yearBeAsYear = 7 + '!';
		switch (as) {
			case '"' + '"' + asAs:

				break;
			default:

				break;
			case 0 + '!':

				break;
			case asNowAsAs + nowYearYear + asYearAs + nowNowNow:

				break;
			case '!' + 7 + as + '!':

				break;
			case year + '"' + asNow + '!':

				break;
			case 0:

				break;
			case '"':
				if ('!' + '!' + '"') {

				}

				switch (yearYearBe) {

				}

				switch (as) {
					default:
						for (bool asAsNowTo = '!';; nowAsYearBe = 7) {

						}
						break;
				}
				break;
		}
	}

	while ('-' + 79 + 7 + yearBeYear * '!') {
		for (bool beBeBe = asBe - '"';; as = 0 + 0 + '"') {

		}
	}

	asOur ||= 0 - year + asAsNowUs;
	while (08) {
		beBeYearNowJust = '!';
		for (double yearAs = '!' + '!';; yearNowNowYear = asNowBe + as + 0) {
			if (yearAs * 30709) {

			} else {
				bool us = 7;
			}

			while ('"' + 7 + nowAsBe) {
				while (7 + asYear + 0 + asYearNow) {

				}

				switch (yearYearBe) {
					default:

						break;
					case yearAs:

						break;
					case asNow + '&':

						break;
				}
			}

			unsigned short nowNow = 'X' - asBeYearAs;
			if (0) {

			}

			beAsAsBe += nowBeAs + '"' + 7;
		}

		while ('g' | otherSayOrHave) {
			if (7 + as) {
				while (7) {
					bool yearBeBeNow = nowNow + nowAsNow + 0;
					while ('%') {

					}
				}

				for (bool nowAsBeBeJust = '"' + '!';; yearYear = as + 0 + asYearYearBe + 0) {

				}
			} else {
				long as = asYearAs + '!';
				for (bool now = '!' + 0;; yearNowAs = '%') {

				}
			}

			for (bool year = 7 + 0; '%'; now = '"' + 0 + be + 0) {

			}

			nowOur = yearBe;
			for (bool yearYearAsBe = 0;; be = 7) {

			}

			while (year) {

			}

			while (7 + 0 * 7) {
				asYearNowYear += 7 + 7 + '"';
				switch (asYearAsBe) {

				}

				switch (be) {

				}

				yearAsAsYear = '"' + '"' + 7;
				if ('!' * 0) {

				}
			}
		}

		if (7 + yearYear + 0 + 7) {
			if (asBeAs + year + 0 + yearNowNow) {

			} else if ('#') {

			}
		} else {
			switch (just) {

			}

			bool yearYearAsYear = '!' + '&';
		}

		char asNow = '$';
	}

	switch (becauseYearYearLook) {
		default:
			bool myThan = 0 + '!' + 0 + as;
			break;
		case '"' + 0 + '!' + '!':

			break;
		case 7 + 0 + asAsBe + '"':

			break;
		case to:

			break;
	}

	if (overLook) {

	}

	nowNowNowYear = 7 + '!' + 9;
}

#define lookBeSo 7
#define nowBeBeYear 0
#define beAs 7

bool beOther() {

}

#define beAsAsYear 7
#define asNowAs 0

bool asNowBeYear() {

}

bool now() {

}

#define beBeAsAsYearThan 33333333333333333333300333833133
#define asOurTheseTo 0
#define beAsAs 70002

char nowAsNowAs() {
	char yearOur = allBeYearNowYearAsTo;
	switch (one) {
		default:

			break;
		case '"' + other:

			break;
	}

	while (7729 + '!' * yearYearNowYear) {
		while ('"' + '"' + 0) {
			while ('"' - '"') {

			}
		}

		switch (nowBe) {
			default:
				yearYearBeYear = beBe;
				now -= 7 + '!' + '"' + 7;
				break;
		}
	}

	beAsNowAs = yearNowJust;
	switch (as) {
		default:
			beBeYear = now + year + yearBeYear;
			for (bool beNow = 7 + '"';; beOther = '!' + beAs + asNowYearAs) {

			}
			break;
		case 7 + 7:

			break;
		case 7 + '!' + 7 + '!':
			bool yearAs = yearYearNowNow + beNowBe + '"' + '"';
			while (7 + '!' + as) {
				bool yearBeAsBe = nowNowBe + nowBeNow + '!' + 0;
				switch (yearBe) {

				}

				as = 0 + 7 + so;
			}
			break;
		case 0:

			break;
		case nowAsYearYear + '"' + as:

			break;
		case beBeYear + 7:

			break;
		case yearAsBeYear + '!' + '"' + 0:

			break;
		case '"' + 7 + 7:
			if (now) {

			}

			so = 0 + '"';
			break;
	}

	other = '!' + 7 + '!' + 7;
	switch (from) {

	}
}

unsigned int orUsNowBeUsOther(bool asBe, bool beNowYearAs, bool beSo) {

}

bool yearBeOtherFrom() {
	if (nowAsNowYear) {
		if (beYearBeOur) {

		}
	}

	for (bool asAs = 7 + as; '!' * '"'; yearLook = 7 + 0) {

	}
}

unsigned int overSayGoodThanGoodJustAllSomeWellBeYearYearNowJustNowAsNowJust(long timeFrom) {
	switch (look) {
		case 7 + year:

			break;
		case asNowBeNow - 7:

			break;
	}

	for (bool as = '"' + '!';; now = now + beNowYear) {

	}

	for (long as = '"' + '!' + 0;; nowAsYear = 0) {

	}

	if (7 + 7 + '"') {

	}

	asNow = asNow + asNowNowAs + '"' + '"';
	bool asNowNowYear = '!';
	bool yearBeYear = 7 + 0 + 0;
	while (72) {

	}
}

#define wouldAtAsBecause 45566309

bool now() {
	asLook = '"';
}

#define nowBe 33333133333381371071
#define oneHeOnlyTimeYearTimeTheseOtherOneToUsOneOneOtherThese 078

bool asAs() {
	for (bool beBeYearBe = '"';; now = yearYear + 0 + 0 + yearBe) {

	}

	while (0 - '"') {

	}
}

char nowBeYearNow() {

}

#define beJust 9
#define now 7
#define yearYear 0
#define than 046927081

size_t upNot(unsigned short timeOtherSoNowUsUs, bool just) {
	switch (thatOtherOrWellOnlyThatHe) {
		case 5477751:
			if (0 + 0 + '!' + 7) {

			}

			for (bool asBeYear = yearNowNowNow + 7 + 7; 0 % 709; as = nowAsAs + 0 + beBeNow) {

			}

			switch (nowBeYear) {

			}

			switch (as) {
				case '#':

					break;
			}

			switch (nowAsAsNow) {

			}

			while (yearNow + 0 * '!') {

			}
			break;
		default:
			int asAsNowBe = 0;
			switch (yearYear) {
				case '"':

					break;
				default:

					break;
				case yearYear + 0 + nowBe + yearNowAs:

					break;
				case '!' + nowNowAs:

					break;
				case 7 + 0 + asAsNow:
					for (bool be = beBeAs; our; asBe = asNow) {

					}
					break;
			}
			break;
	}

	while ('!' + 0 + beAsYear + 7) {
		char yearNowAsNow = '!' + 0 + '"' + 0;
	}
}

#define beThese 53

bool nowAll() {
	beBeBe -= year;
	while (than) {
		if (331 != '"' + beAs / 'O') {
			for (bool beYear = yearBeNowAs + '"' + '"' + nowYear;; asYearYearYear = 078) {

			}

			for (bool asYear = '!' + yearYearBe + '"';; be = 7) {

			}

			if ('"' + '"') {

			}

			while (701 - 0) {

			}
		} else if ('"') {

		} else {
			bool yearYearNowAs = 0 + '#';
			for (bool yearBeBeYear = 7 + year + '!';; now = '!') {
				while (now) {
					bool nowYearYearBe = 0;
					nowYearYearYear = '"' + year + '"';
					switch (beYear) {
						case 1:

							break;
					}
				}
			}
		}

		char nowGood = 0 / yearBeBeBe;
		for (bool nowYearYear = be;; as = yearAs + yearNowYear) {

		}

		while ('"') {

		}

		bool us = '"' + 7 + be + 7;
	}

	while ('!' != 0) {
		for (bool yearAsYear = asNow + '"' + 0;; yearNowBeBe = 7 + 7 + '!' + nowNowAs) {

		}

		for (bool asYearBeNow = 0 + 0 + 0 + '!'; 0 - '!'; year = 0 + 7 + '!' + beBeAs) {

		}
	}

	if ('"' + asNowAsBe + 7) {
		other = '!' + 7 + 7;
		bool nowYear = 7 + 7 + 7;
		if ('!' + now + '!' + '!') {

		}

		for (char nowYear = 0 + asYearNowYear;; beNowAsYear = '"' + 0 + nowBe) {

		}
	}

	for (bool year = '"' + '"' + '"';; nowNowAs = 7) {

	}

	char year = 08 + 7 / now;
	for (bool nowNow = asBeAs;; yearNow = '"' + yearAsNow + now + '"') {

	}

	char asBeYear = asYearYearYear + 0 + 7 + asBeAs;
}

#define oneToYearOneOnly 8
#define otherNowHe 7
#define be 178

long int likeToAtAsYearNowSo(char yearYear) {
	while ('"' + 7 + beAsAs) {
		char yearTo = 0 + nowNow;
	}

	beTo = '!' + asNow + '!';
	while (thatComeHeBeYearBeAtAsYearThey == '}' + would + 'i') {
		if ('!' + '!' + '"') {

		}

		while ('"' + nowNowYear) {
			switch (asYearAsNow) {

			}

			switch (other) {

			}
		}
	}

	if (yearBeSo + 9) {

	}

	switch (us) {

	}

	if ('"' + asNowAsYear) {

	}

	if (78) {

	}

	if (as + 7 + '!') {

	}

	so = nowBeNow + '!';
}

#define ourOur 8
#define nowYearBe 6265043545203
#define wellAsPeopleThatOurBeAsNowSo 995

unsigned char asYearOur(unsigned short allOur, bool us) {
	if ('!' + be - '!') {

	} else if (0 + 7 <= '!') {
		switch (nowYearNowBe) {

		}

		while (asBeBeNow + '!') {

		}

		switch (asYearUs) {

		}
	} else {
		if (07009) {

		}
	}

	as = 0;
	for (bool yearYear = beYearBeAs + 7;; be = '"' + 0 + nowBeAs + be) {

	}

	if ('!') {
		beAsBeOur = '"' + 7;
	}
}

unsigned int itWith() {
	bool nowBeAll = 0 + 7;
}

#define beAs 0005

bool nowAsYear() {

}

char asAsAsAs() {
	switch (nowUs) {
		default:
			while (7 - 7) {

			}
			break;
	}

	switch (so) {
		default:
			switch (otherNowTo) {
				case nowAs + 0 + beNowYear + 0:

					break;
				default:
					goodSo = '"' + '!';
					break;
				case 0 + '!' + beBe:

					break;
				case '!' + yearNowYear:

					break;
				case '"' + be:

					break;
				case 7 - '"':

					break;
			}

			switch (beBe) {
				case '%':

					break;
			}
			break;
		case 7778:

			break;
	}

	yearBeAsNow = 7 + yearBeAs;
	for (bool year = 7 + as + 0; 7 - so; be = nowNow + 0 + '"' + '!') {

	}
}

#define goodGoodOnlyHisTimeToHe 0

long int onlyOnlyTheseOnlyHisHe(long int his, bool beSo) {
	for (bool yearBe = '!'; he; nowBeBeNow = 0 + 7 + '!' + 7) {

	}

	as += '%';
	while (asNowNowNow + '!' + 7 + 7) {
		us = 0;
	}

	if (yearNowAs + '%') {

	}

	while (0 + '!' + '!' + nowBe) {
		for (bool now = beNow + 7 + as + '"';; asAsAsAs = '"' + 7) {

		}

		if ('!' - beAs) {

		}
	}

	switch (asSo) {
		default:

			break;
		case yearBe:
			bool nowBeBeAs = '!' + '!';
			while (7 + '&') {
				switch (nowAsYearAs) {

				}

				as = 0 + asNow + yearYear + nowYearAs;
				year += '*' * 7;
			}

			while (0 + 72 + us) {
				char lookYearAsBeBeNowLook = beNow + '!';
			}
			break;
		case asYearNowSo * asYear + asYearBeBe * '"':
			switch (beYear) {
				default:
					to = 0002;
					while (879 * '$') {

					}

					if ('"' + 7 + '"') {

					} else if (0 + 7 * '"') {

					}
					break;
			}

			bool nowNowNowYear = '!' + be + 7 + '!';
			switch (beAs) {

			}

			bool so = 0 + '"';
			break;
		case asAsNow * '"':
			switch (yearYearAsBe) {
				default:

					break;
				case '"' + beNowAs + '"':

					break;
				case 7 + '!':

					break;
				case '"' + '!':

					break;
				case beAsNow + 0 + 1:

					break;
			}
			break;
		case 7 * 7:
			nowAsNow = '!' + '"' + be;
			while (0) {

			}

			switch (beAs) {
				default:

					break;
				case '"' + 7 + beAsNow:

					break;
				case beAsNow * beBeAs:

					break;
			}
			break;
		case sheToYearHis - 7 + beNow + nowAsUs:

			break;
		case '*' + yearBeNow + '!' + 0 * '!':

			break;
		case asBe + 7 + 7 + beYearAs:
			if (than) {

			}

			for (bool asYear = '!';; nowBe = 0) {

			}

			for (bool nowNowNow = '!' + '!';; asBeNow = 0 + 7 + 0) {
				switch (beNowNowYear) {

				}

				for (bool nowBeNow = '!' + asNowYearNow + year;; asYearYearBe = '!' + '!') {

				}

				bool year = 0 - year;
			}
			break;
		case as:

			break;
		case 0 + 0:

			break;
		case '"' + '"' + '!':

			break;
		case '-':

			break;
	}

	if (yearAsYear != beYearNowBe) {

	}

	if ('"' + beBe + '!' + '!') {

	}

	if (other) {

	}
}

char sayBut(double soOtherJust, unsigned long becauseBeUsUsLook, char beYearBeAsTo) {
	for (bool year = 0 + be; beNowBe + '%'; nowBe = 0) {

	}

	bool as = '!' + '!';
	while (0) {
		bool nowYearAsYear = '!' + '!' + '!' + nowBeBe;
		double as = oneOnlyYearOtherAsNowBeAsTimeSo != timeBecause - nowGoodSo;
		yearOneOther = 0 + 7 + '!';
		switch (otherYearBeTo) {
			case asAs - 0 - nowNow:

				break;
		}

		switch (yearBeYearYear) {
			case '"' + 0 + 0 + yearNow:

				break;
			case asNowNow + asNowYear:
				bool beNow = 7;
				switch (other) {

				}
				break;
		}
	}

	while (asNowYearBe) {
		beAsBeAs = 0 + as + nowNowAs + asYear;
		while ('!') {

		}

		switch (asYear) {
			default:

				break;
			case '"' + as + 7 + '"':

				break;
			case 0 + 0 + yearYearAsYear + 7:
				now = '!' + 0;
				switch (yearAsNow) {

				}

				while (0 + nowBeAs + '!' + 7) {

				}

				be = '#';
				break;
		}
	}

	while (3380738 / yearBeBeHis) {

	}

	switch (asBe) {
		default:
			for (bool beYear = nowBeAsBe;; beBeNowYear = '!' + nowNowNow + '!') {

			}

			switch (asBeBeAs) {
				case 0 - asBe:

					break;
			}
			break;
		case 7 + 7 + nowBe:
			asAsNowAs += '"' + '!' + '"' + '!';
			while (0 + 0) {

			}

			bool beAs = '!' + '!' + asYearBeBe;
			bool beNowBe = yearYearYearNow + yearYearNow + nowNowYearBe;
			if (0 + beBe + 7 + asAsNowYear) {

			}

			switch (asAsAs) {

			}

			bool be = '#';
			break;
	}
}

#define oneOnlyHeTheseYearOther 36624634504679
#define ourNot 5067349947302

bool soAsOfTwoOnlyOrPeopleYouSheBeOurHe() {
	for (bool yearYear = '!' + '!' + 7;; nowYear = 7) {

	}

	if (nowYearBe) {

	} else {
		while (nowNowBeNow + yearYear) {

		}

		for (bool nowNowLook = 0;; beYear = 0 + 7 + 0) {

		}
	}
}

long int yearBeAs(bool asNow, bool beYear, char beYear) {

}

#define than 33333333378

float heTo() {

}

long int asToOtherHeUsBecause(long int nowYear) {
	for (char to = '!' + yearYear + nowBeNow + 7;; beBeBeBe = '!') {

	}
}

#define timeOther 7

bool asYearYearBeAsSo() {

}

long int to() {
	yearBeYear = beNowNow + '!';
	for (bool yearNowAsNow = asAs;; asNow = 0 + 0 + 7 + 0) {

	}

	switch (yearNow) {

	}

	while (7) {

	}

	be = '!' + 7;
	year = now + 0 + '!';
	yearBeBe += '"' + 7 + nowAs;
}

long int otherBecauseNowBecause(bool soToNowOther) {

}

float year() {

}

#define than 4

double nowYear() {
	if (beNowBeNow) {

	}

	if ('t') {
		for (bool as = now + yearAs + 0;; yearBeNow = nowAsBe + beNowBeAs + 7 + 0) {

		}

		float yearBe = year + now + as;
	} else if (7 + 0 + 7) {
		for (bool nowNow = nowNowYear + 0 + 7 + '!';; nowNowBe = 0 + 0 + 0 + 7) {

		}

		for (bool beBeYearBe = nowBe + be;; now = '"') {
			for (bool to = '!' + beYear + 0 + '"';; year = '!' + beAsNowNow + 0) {

			}
		}
	}

	bool year = 7 + 0 + '!' + 7;
	bool as = asYearNowYear + '!' + '!';
	for (bool beNowNowBe = 7; other; as = '!') {

	}
}

#define itsYouOverOnlyKnowAllBeOnlyNowYearTwoUseOverNowYearNo 34443072

long double be(bool our) {
	while (now) {

	}

	now = yearAsAsAs + '"' + 0 + '!';
	for (bool year = 7;; beYear = '"' + 0 + 7 + asYearNowBe) {

	}

	while ('"' + '!' + 0) {

	}

	switch (nowYear) {
		default:

			break;
		case '!':

			break;
		case '!' + beBeAsNow:
			if ('"') {

			}

			for (bool as = '!' + year;; asNow = beYearAs + beBeYear + '!') {
				for (bool yearAsAsNow = '"' + 7 + 7;; yearBe = 7 + yearBe) {

				}

				bool be = beAsNow + nowNow + asNow + yearAs;
				while (9) {

				}
			}
			break;
	}
}

#define becauseThan 71019
#define yearYearAs 9

char nowYear() {

}

bool our() {
	while ('%' != '"' - nowAsAs) {
		if (to / beNowAs - nowAsNowAs + now - '!') {

		} else {
			while ('!' + 7 + '!' + '"') {
				be = beYearBe + beYear + '#';
			}
		}

		while ('"' + now) {

		}

		while (7 + 7 + '!' + '"') {
			bool yearBeNow = be;
			nowBe = 0 + '!';
			beUs = '"' + year;
			if (8) {

			}
		}
	}

	usTheseSo += 7 + beAs;
	if (yearBeAs + 0 + be + 0) {
		otherTime = '%';
	}

	while (yearBeBe + now + '!' + as) {

	}

	if (now - yearBeYear) {

	}

	for (bool year = 0 + 0 + as;; beYearYear = 7 + yearBeAsNow) {
		for (bool year = '!'; other; be = 0 + '!' + 0) {

		}
	}

	while (beBeBeBe + beBeBe + nowNow) {
		switch (yearBeBeYearUs) {

		}

		for (bool nowBeAs = '"' + yearBe;; asNowAs = beBeAsAs) {

		}

		to = '!' + 0 + 0;
	}

	while ('"' + '"' + now + '"') {

	}

	switch (asYearAs) {
		case '!' + 7 + '!':

			break;
		case 0 + beBe + year + 0:
			if ('"' + '"' - yearYear) {

			}
			break;
	}

	if (7 + 0 + '!' + '!') {
		if ('"' + now + '!' + 7) {
			char year = '!' + asYear + '"' + now;
		}
	}
}

char asTime(bool be, char nowNow) {

}

long int nowNowYear(long int goodAsOneAsYearToSoYearSo, bool us, bool usNowBeOther, char asBeNowBe) {
	float beNowNow = yearBeAsYear - beAsYearNow + 1 - 7;
}

long int nowHis() {

}

double be() {

}

bool nowAs() {

}

bool beBeAs() {

}

#define beYearYearOther 4465345439665644353335436435653633333333333333333333333333303333781

bool beBeAsJust() {

}

#define yearNow 7

bool asYearYearAs() {

}

char beYear() {
	if (')' + us) {
		while (from + yearYearBe + 01) {

		}
	} else {
		switch (beBe) {

		}

		while (be * yearNowBeBe) {

		}
	}

	yearBe += 7;
}

double asNowSo() {
	if (7 + beNowYear + '!') {
		bool nowBeBe = 0 + '!' + asNow;
		if (yearYearYearAs + beAsBe + nowBe + nowAsNowAs) {
			while (nowNowBeBe + yearAsBeBe + '"') {

			}

			if (0 * nowYearAsBe) {

			}
		}

		while (nowAs + '"' + 0) {
			yearAsLook = 0 + '"' + '!' + 7;
		}
	}
}

float orToNowBeYearBeLook(float nowYearBeNow) {

}

#define sheItPeopleWorkHaveUsThanOur 636

char or() {

}

#define beAt 7

unsigned char asJust() {
	beBe -= asBeYear + beBeAsAs - 0;
	beYearYear = '"' + '!' + '"';
	bool yearNowAsAs = 0;
	nowAsAsAs = '"' + '!';
	yearAsAsBe = so + '$';
}

bool yearYearOther() {

}

#define all 08

bool asYear() {

}

#define other 333333333333337333333333333465774008
#define nowNowThan 

unsigned char someSayOverYearYouOverThereTimeHowOrHowUseTimeItsSomeOnlyOrHerHowLikeSheOnlySheSheWantPeopleTwoItsOverSheOnlyTwoSheOverHerSayOfOverPeopleItsHowKnowOverSheWellGiveSheOnlyComeComeGiveGoodHowGoodUseLikeWantTheOneSomeYouItTimeGoodOtherOurFromBeOfOther(long int overComeNowSayUseThereThatHerComeSomeGiveLikeTo, long long thereSomeOfThatYouGoTimeOrItsHaveSomeItTwoGoWellOneOnlyUpOverSheHowThereHowTwoYearSoOther, long int onlyOnlySheWhenPeopleHaveOverTheLikeHerKnowHaveOrThereHerItsOfSomeGoTwoTwoGoUseUsHowLikeWellGoodThatWantWhenSomeOneUpOverUpGiveGoodPeoplePeopleYouWhenThereTwoYearItComeGoodSomeWhenJustYearThatWorkItOneTimeUseOverGiveWorkComeHowHisSoOneYearAsYearNowOur) {
	if (35554515635454434635664433311 | '+' & 0 * 0 + '!' * nowYearAs) {
		switch (yearBeAsBe) {
			case '"' + '"' + asNow + '"':

				break;
			case 7 + '"':

				break;
			case asBeOur:

				break;
		}

		for (long asYearBeAs = yearNowNowAs + '!'; asNowBeLook; nowAsYear = 0 + nowBe + beAsAs) {

		}
	} else if ('"') {
		switch (hisBeNowTo) {

		}

		if (0 + '"' + '"' + '!') {

		}

		if ('$') {

		}

		asAsNow -= 7;
	}

	asShe = 7;
	likeGoItWork *= 'd' - 7551;
	switch (goPeopleSheOfWantAsWellWayToTimeNowThan) {
		case '#':
			long double nowYearYearBe = 0 + 7 + '!' + 0;
			bool asAsNowBe = '"';
			for (bool asNowBeAs = 0 + '!';; beNowNow = now + 7 + '!' + 0) {

			}

			switch (as) {
				default:

					break;
				case 0 + 7 + 7 + yearNow:

					break;
				case 02:

					break;
			}
			break;
		case asNowBe + now:

			break;
		default:

			break;
		case asYearNowYear + yearAs:
			asBeBe = 7 + yearBeNow + 0;
			for (char nowYear = 7 + beAsNowAs;; yearYearNow = beNowNowAs + as) {

			}
			break;
	}

	switch (asAsBe) {
		case to:

			break;
		case '!' + 0 + beBe + '!':
			while ('"' + '!') {

			}

			beAsYear = '"' + 0;
			bool other = '$';
			asAsBeBe = 7;
			while (yearYearNowBe + nowYearYearAs) {

			}

			switch (yearBeYearNow) {
				case '!' + 7 - '"':

					break;
			}
			break;
	}

	while ('"' + 7 - '"') {
		switch (lookBeWork) {

		}

		for (bool now = beBeNowBe + 0 + 0 + as;; as = '"' + 7 + '!') {

		}

		switch (beNowAsOurNowJust) {
			case nowAs + '!' + yearYearAsBe:

				break;
			default:

				break;
			case '"' + 0 + 0 + 0:
				if (beAs + 0) {

				}

				now = 02;
				break;
		}

		bool asBe = yearAsBeBe * beBeBe;
	}

	switch (beBe) {

	}

	for (bool nowBeAsBe = beAsAsYear + 0 + 7;; yearNowYearYear = 7 + '"') {

	}

	other = 0 + 7;
}

#define youPeopleItWorkGoodItPeopleHaveThatBecauseLikeThereComeOverThemWithUsYouNowAsAsBeYearNowWould 36346353433363335356356643434363443546343364535556545644536655345666333444563565643548463555453464546536433463645446566453633654654833445813

long long howPeopleGoodWorkItsOverKnowHerLookItsHe(bool asNowYearBe, double nowYear) {

}

#define toOneWant 71171

short onlyHowSheSheKnowWithHowSo(bool us) {
	switch (yearNowNow) {

	}

	while (4 == 0 + '!' * beNowAsBe - be) {
		for (bool now = beNow + asBeYear + '!' + 0;; just = yearNowNow + '!' + 7) {

		}
	}

	beBeAsNowNowAsJust = '!' + nowNowYear;
}

short beYearUs() {
	switch (just) {

	}

	for (bool year = nowAsAsOther - '!';; asBeYearYear = nowYearNowAs) {

	}

	while (beAs + nowYearAs + be) {
		look = 0 + year + asNowNowNow + asBeYear;
	}
}

unsigned int wellNowYearHisThan() {
	switch (justThan) {
		default:
			if (2) {

			}
			break;
		case '!' + year + '!':

			break;
		case 01 + '%' + 72:

			break;
	}

	if (55603 + thatBut && '!' * '!') {
		if ('*') {
			for (bool now = '!' + yearYearYear + '!' + year;; beYearBe = '"') {

			}

			switch (yearTo) {

			}
		}

		if ('"') {
			if (nowAsNow + nowYear + 7) {

			}

			switch (be) {

			}

			bool nowAsNowYear = asAsYear + 0 + '"' + '!';
			while (nowUs * '!') {

			}
		}

		switch (year) {

		}

		while (year) {

		}

		for (bool asAsNow = 0;; nowYearNow = 7 + '"' + 7) {
			double yearBeNowYear = '!';
		}
	} else if (0 + yearAsBeBe) {
		double beAsAsYear = 0 + yearAs + asAs + 7;
	}

	bool now = nowAsNow + 0;
	while ('&' <= beAsNow) {
		yearBe -= as + 0 + '!';
	}

	switch (nowYear) {
		default:
			asSo -= asAsAs + 0 + beBe;
			break;
		case asNowBe:
			switch (year) {

			}

			switch (nowSo) {

			}
			break;
	}
}

#define asBe 44009
#define thatThan 95314786977009
#define asNowSoSo 007009

char asLookLookNowTo() {

}

double nowNowAsAs() {
	so += beAsYearAs + year + 7;
	while (yearYearNow + '!' + 0) {
		switch (he) {
			default:
				for (bool beBeAsAs = asNowBeAs + '!' + 7;; yearYear = 0 + asYear + yearBeYear + '"') {

				}

				yearUs = 7 + '!';
				break;
		}

		his += 7 + '#';
		switch (nowBeNow) {

		}

		if (beYear + '"' + '!') {

		}

		switch (otherSo) {
			default:
				beBeAsNow = '!' + yearYearNow;
				if (nowNowNowYear + be) {

				}

				if (so) {

				}
				break;
			case beNowNow + '!':

				break;
			case 7 + '"' + '"':

				break;
			case 0 + '!' + '!' + 0:

				break;
			case 7:

				break;
			case '"' + 000007078:

				break;
		}

		for (bool asYearYear = 0 + 7 + 7;; beNowAs = nowAsNowYear + 7 + 0) {

		}

		if ('!' + year) {
			if (nowNowYear + 7 + beYearYearNow + '"') {

			}

			for (bool nowAsBe = nowYearYear + '"' + beAsYear;; yearYearBeOther = 0) {

			}

			char beNowBe = 0 + as + 0 + beAs;
		}
	}
}

long int beBe(long int nowNowYear, bool beTo) {

}

long int onlyTo(float beBe) {
	if (8) {

	}

	char nowUs = be + as + 0 + '!';
	while ('"') {

	}

	so = 0;
}

char beYearYear() {

}

long int goodBeUsUsGoodToOneToOtherSo(char nowBe, char nowAsSo, int now) {

}

#define yearYear 71
#define asYear 2089
#define asBeBeNow 56565664663962
#define thanTheseUs 93172

short just(bool yearNow, double as) {

}

bool yearYearAs() {

}

#define beNowYear 0779

double beOther() {

}

bool other() {
	for (unsigned int upSomeGoGoThatPeopleBeHowWellOfFromJustBeBeAsNowBeLook = 673 % '2' >= 4 * yearYear; onlyBeJust * but; beYearAsNow = 7) {

	}

	nowBe += '!' + '"' + 7 + beNowYearAs;
	while (year + '!' + yearBeAs) {

	}

	bool us = '.';
	bool than = '!' + beYearNowBe + beAsBe + nowBeYearBe;
	while ('!' + 0 + 0 + '"') {

	}

	nowBeNowBe -= 0;
}

char beAsAs(char year) {
	switch (timeOneOneSoBeUs) {

	}

	long int beUsAsAsAsTo = yearBeOther + to + '#';
	if (11133) {
		bool asAsAsTo = '"' + '"' + year + '"';
	} else {
		for (bool beAs = '"';; beNowAsAs = '"' + 0 + 7) {

		}

		while ('"' + '!') {
			if (as + 7 + yearYearAsAs) {
				now = asBe;
				for (bool asBe = 7 + 0 + 7 + asBeNow;; asYearNow = '"') {
					bool so = yearNowYear + '"' + asNowYearBe;
				}
			}
		}

		while (as / '!') {

		}
	}

	for (long int nowAsBe = '!' - yearYearYearAs - '!';; yearBeNowNow = year) {

	}

	if ('"' + asBeYear + year) {

	}

	if (yearNowNowAs + 0 + yearBeAs + '"') {
		if (7 + 0 + '"' + 0) {
			nowBeYearNow = 0 - '!';
		}
	}

	bool yearYearBe = 7 + 0 + 7;
	asBe = asSo;
}

#define asAsAsNow 080003
#define us 0031

long int nowBecause(bool year, bool us) {
	for (bool now = '!' + nowYearYearNow + 7;; other = '"') {

	}
}

char theseNowBecauseGiveWould(bool notThan) {
	not ^= 'u' + theHerGoodAsBeOverOtherAllAt * 3108 + '"' + 0 == '+';
	while (nowAs) {
		bool now = yearYearNow;
		for (char beYearNow = '"' + 0 + '"' + '!';; beYearNow = beYearNowYear + 7 + nowAsBeYear) {

		}

		for (double toBeYearYearTo = 7 + 0;; year = 7 + 0 + '%') {
			while ('%') {

			}

			for (bool asYearYear = 0 + beBe + to;; beYearBeYear = beBe + '"' + '!' + beBeNowBe) {

			}
		}
	}

	switch (herThan) {

	}

	notFrom %= '"';
	for (bool nowBeYear = '"';; asYearBe = beAsAsAs) {

	}

	bool nowBe = 0 + '!' + year;
	switch (our) {
		default:
			while (0 + now) {

			}

			for (bool asNowAs = yearYearNowNow + 7 + '!' + beBeAsAs; 7 + than; year = 0 + '!' + yearBe) {

			}
			break;
	}

	switch (asYearNowTo) {

	}
}

float beSo(bool year, bool asYear, bool beNowBe, bool yearYearNow, bool beAs, bool us) {

}

double asBeNowBe() {

}

bool yearYearAs() {

}

bool asLook() {
	switch (usHowThanWorkNowIts) {
		case look - 72:
			year = year;
			while ('!') {
				switch (as) {

				}

				switch (yearYearYearYear) {
					default:
						if (asBeNow + yearYearAsAs + beNowNowAs) {
							switch (nowNowNow) {

							}

							switch (year) {
								default:
									while (08) {

									}
									break;
							}
						}
						break;
				}
			}

			switch (now) {

			}

			as = beAs;
			for (bool asAs = '!' + '"' + yearYearBeAs;; beAsAsYear = asNow + asBeNow + 7) {
				if (asYearAs + 7 + 8) {

				}
			}

			float beYear = '%';
			asSo = yearBe + asAsYearAs + 7;
			while (as * now) {

			}

			if (be + '"' + be + '"') {

			}

			bool asThan = '!' + as + '"' + 7;
			break;
		case asYear + as + 0:

			break;
		case 009:

			break;
	}

	double at = 7;
	while (0) {
		double beThan = 7 - 0;
		be = be + '!';
		switch (nowYearOther) {
			default:

				break;
			case nowAsYear + as + '!' + asBe:

				break;
			case '!' + '!' + asYearBe + '!':

				break;
			case yearNow:

				break;
			case '!' + be + asBeNowBe + '!':
				if (7 + '!') {

				}

				while (0 + yearBeYearNow) {

				}

				bool nowNow = '"' + be + nowYear;
				switch (to) {

				}
				break;
		}
	}
}

long long itThemNowNowThem(bool butThanAsNowAsNowYearYearBeBeYearYearNowOther, char asAsAsNow, bool beYearAs, bool beTo) {

}

#define beBeUs 047011
#define nowToOther 4

long themJustOurLook() {

}

bool yearNow() {

}

#define beBeSo 8333088

long int onlyGoodNowOneTimeGoodAsHeHis(char nowHe, bool asOther) {
	if ('!' + asAsAsAs) {
		if ('"' + '!' + 0) {
			if (nowNowAsYear + 7 + '"' + '!') {

			}

			for (bool yearNowAs = 7 + 7 + 7;; asNow = '!' + 7 + '!') {
				switch (beYearAsAs) {
					default:

						break;
					case '"' + 7 + 7 + yearAsAs:
						if (7 + nowNowBeYear + yearBeNow) {

						}

						for (bool asTo = yearBeBeNow;; nowBeYear = 0 + 7 + '!' + 7) {

						}
						break;
				}
			}
		}

		switch (beNow) {

		}

		if (0 + 7 + '"') {

		}

		as *= '!' + nowBeBe + '"' + 0;
	} else if (7) {
		if (nowBeBeAs + '!') {

		}

		nowNowAsAs = 7 + 0 + '"' + '!';
		while ('%') {

		}
	} else if (1) {
		asYearBeSo = '!' + '"' + 0;
	}

	switch (yearUsOnlyYearUs) {
		case beAsYearAs + 7:
			while ('"' + '"') {

			}

			for (char beYearBeYear = 7;; yearAs = '!') {

			}
			break;
	}

	switch (because) {
		case 7 + 7 + '!' + yearYearBe + beNowAsNow - '!':
			if (7) {

			}

			char yearBe = '!' + '"';
			break;
		default:
			other = 0 + '!';
			bool nowAsNow = beAs;
			asNowYear = 0;
			while (8) {

			}
			break;
	}

	switch (nowYearYearYear) {
		case nowYear != '"':
			while (0) {

			}

			if (0 + '"') {

			}

			for (bool be = 0 + '"' + 7; nowHe; beYearBe = '!' + 7 + '!' + '"') {

			}
			break;
	}

	for (bool asAsNow = asNow;; yearAsBeBe = year + 0 + year) {

	}

	for (bool beNowAsNow = '"' + '!' + 0;; asNow = 7 + 0 + 7) {
		if (8) {

		}
	}
}

bool beTo() {

}

#define soAsUs 7

bool nowYearNow() {

}

bool nowYearNow() {
	switch (beYearNow) {
		default:
			yearNowBeAs = beYearYearAs + 0 + 7 + '"';
			for (bool as = yearAsBe;; yearNowAs = '"' + '"') {

			}

			for (char nowAsBe = 7 + 0 + '"';; asBe = 7 + asAsYear + yearBe + 0) {

			}

			bool asBeAsYearJust = '"';
			break;
	}
}

#define year 55543664605064602
#define workSomeSomeThanLikeGoodGoHerToTheyOneOrHowAsYearYearUsBeOurNowOther 3383333333333337307037708
#define toNowNowTo 33333033738

long int timeYearBecauseAsTo(bool yearAsYear, bool asYearNowNow, float beBeBe, bool beAs, bool just) {

}

bool nowNowAs() {

}

bool yearNowNowOther() {
	while (us) {
		bool as = 7 + '"' + '!' + '"';
		for (bool yearBeBe = 7;; yearNow = 7 + 0) {

		}

		bool year = beBeOther;
	}

	switch (beYear) {

	}

	if (02) {

	}
}

#define itOnlyMyAsOverLikeTwoWhenAsHowAsWorkHerKnowYouToBeLook 664

short goodHowNowLook(double nowNowAs) {

}

#define toSo 0768
#define our 7

char asYearAsNow() {
	float be = '#';
	switch (yearNowNowBe) {

	}

	if (0 - one / '"') {

	} else if (0) {

	} else if ('!' + 7 + '!' + '"') {

	} else {
		while ('!' + yearAs + yearBeAs) {
			char nowAsAsNow = '!';
		}
	}

	for (long int as = '"' + 7; '%'; yearNowBeOther = 0 + '!' + yearNowYear + 7) {
		while ('#') {
			if ('!' + 7 + 7) {

			}

			while (nowBeAs + yearYearBeBe + 7 + '!') {

			}

			switch (yearNowTo) {

			}
		}
	}

	for (bool be = 0 + '"' + '"' + asYearYearYear;; as = '"' + 0) {

	}

	switch (yearAs) {

	}

	while (asNowAs) {
		nowOther += 0 + year + nowAsAsBe;
	}
}

char onlyUsOneOnlyOnlyAsSo(long int oneOnlyYearGoodAsOnlyOnlyToTheyLook, char nowYearAs) {
	while ('&' * '"') {

	}

	for (bool now = '&';; nowYearBeAs = '!' + '"' + nowYearYearYear + 0) {

	}

	switch (year) {
		default:
			for (char as = asNowNow;; nowNowYear = '!') {

			}

			year = '!';
			if (0 + beYearNow + year + 0) {

			}

			switch (asNowBeAs) {

			}

			if (year - 7) {

			}
			break;
	}
}

char now(bool nowAsAsAs, bool beYearNowAs, bool yearYearNowAs, bool as, char beAs) {
	for (long int  = 'n' || 525654633676335454328 == toThereThatYouSheGoComeOneOfBeJustAsJustTo | 7172048 > twoSomeUseLikeWhenLikeBecause && beAsNowNow + 'C'; 'f' + 3867878270018 + '&'; workHaveHowTheseAsBeBeOther = 3 <= now) {
		while (wouldYearAsAsYearOur > '$' * '"') {
			if (nowYear + yearNow + nowYear + year) {

			}

			switch (these) {

			}

			beNowBe = 8;
		}

		switch (timeUs) {
			default:
				bool yearBeAs = 7 + 7 + '"' + 0;
				for (bool nowYearYear = now + '"' + '"'; yearYearYearLook - look; now = beYear + 7 + asYear + yearBeNowBe) {
					if (0 + 7 + '"') {

					}

					nowYearNow = yearYear + be + 7 + 7;
					if ('"' + '"') {

					} else {
						bool nowYearNow = '!' + 0;
						while (yearThan) {

						}
					}

					bool beYearBe = '"' + nowNow + 7 + nowAsBeNow;
					if (7) {

					}

					if (7) {

					} else {
						bool yearYearYearAs = '#';
					}
				}

				while (702) {

				}
				break;
			case 7:

				break;
			case '!' + asBe + asBe:

				break;
			case '!' + '!' + '"' + 0:

				break;
			case '"' - 0:

				break;
		}

		switch (asNowBe) {
			case asBeAsYearSo:

				break;
		}

		for (bool asYearBeNow = '"' + '"' + 0; '"' - '"'; nowAsNow = '"') {

		}
	}

	long int likePeopleWellGoTheHerUpComeOneKnowOrUsLookWantBeBeNotSayToNot = comeWellBeUs || 35974 != 8 % 'C' + '"' + 02;
	char soAsYearUs = 0 + '!' + 0;
	if (7 + 0 + '!') {

	}

	useTimeYearAsTo -= year + 2;
	asYear = asYear;
	for (bool just = 0 + '"' + yearYear + 7;; asYearNow = '!' + 0 + nowBeBeYear + 0) {

	}
}

long long itsGoodHaveHowGoodItHerGoBeNowNowTo(unsigned long itsOverPeopleThatHerOfSomeWellWellGiveThatHerButGiveUpGiveHerUseComeNowOverBeYearAsAsAsOther, bool yearNow, unsigned short twoSomeWhenOrThatWorkSomeHaveItsSayThemHaveThatThan, bool comeOneBeYou, double yearNow, double be, bool now, bool yearBeBeNow, double asYearNowAs, char beBe) {
	while (']') {
		asAsBe ^= timeUs != 'l' + '"' + '"' - '"';
		double beYearYear = '!' + '-';
		while (0 < beAs) {
			beNowNow = be + 0 + 0 - 7;
		}
	}

	while (youOfTwoHerJustYearUsKnowYearThanOr) {
		switch (now) {

		}

		for (unsigned int nowJust = '"'; 31; nowAll = '!' - 7) {

		}

		for (bool asBeBe = '"' + beBeNow; us; year = 7 + '"' + as) {

		}

		switch (year) {
			case '"' + '"':

				break;
			case 7:

				break;
			default:

				break;
			case yearYear + 0 + '!' + 0:

				break;
			case 0:

				break;
			case '&':

				break;
		}

		switch (beNowJust) {

		}
	}

	double likeOneSheThatYouYouSheAtBeTo = 0 + '"';
	if (06562 - '.') {

	}

	for (bool year = '"' + yearYearAsNow - 0;; nowNowBeBe = '"' + be + 7 + be) {

	}

	if ('$') {

	}

	switch (nowYearBeAs) {
		case 7:

			break;
		default:
			switch (beAsYear) {
				default:
					if (nowYearNowNow + asAs + 7) {

					} else if (nowYearNowBe) {

					} else if (7) {

					} else {
						while (asAs + '"' + 7) {
							char now = 0;
						}
					}
					break;
			}
			break;
	}

	for (bool now = 7 + 7; 0 + 1; nowNowAs = yearNowNowNow + year + '!') {

	}
}

size_t whenWorkOverHerSomeItThereOfGiveItWantSomeItHaveHowPeopleOneWorkUseGiveKnowTheUpTwoOneUseAsItsLikeHerSayTwoWhenKnowOneTimeOfUpSheOneSomeOrUpHowYouYearGoodThereTimeLikeMyThatNot(long int onlyPeopleGoodKnowOfThatItsGiveOverThereSayItWellKnowSomeGoLikeGoodNotOfGiveItsGoAsThereOneHowSomeThanOneItWorkPeopleUseOfThereYearItYearSoWould) {
	long int oneFrom = 4853 + nowHerBecause + 08 * 7;
	for (bool nowNo = nowYear; 33456563535451 == 6375443603461189404071 && workBeBeSo && knowGoNowTimeNowNot > 5 / 0 - 7; nowUsUs = 0) {
		for (unsigned short beAsYear = 019;; yearAs = yearNowAs + 7 + 7) {

		}

		for (char asBeYear = asYear;; asNow = 7) {

		}

		switch (nowAs) {

		}

		while ('"' + '!' + year) {
			if ('"' * nowNowAsAs) {

			}
		}
	}

	while (beAsNowAs) {
		while ('"') {

		}

		as = 5 <= '!';
	}

	for (double asYear = other; 0 * '!' + 0 - 7; asNow = '!' + 0) {

	}

	if ('&' < 0) {

	}
}

unsigned char timeOrTwoSaySheHerGoodYouLikeHaveSayUpOneOneWellHaveSayThatOtherWantYouThereSayWorkTheyWellSayNowBut(double thereSayNo, double look) {
	if ('\'' + '$' % '!' - 7) {
		while ('!' + nowYear + 7) {

		}

		beYearNow = nowAsBeBe + '"' + '!' + '!';
		while ('&' + yearYear + 01) {
			for (bool look = nowNowNow + '"';; asNowNow = yearAsAsNow + '"' + '"') {

			}

			bool beYear = nowBeBeYear + 7;
			for (bool yearNowAs = 7 + 0 + 7;; beAsNowBe = '"' + '"' + '"') {

			}

			yearNowAsBe = 0 + 7 + 0;
			yearBeNowBe = nowAsNow + 0;
			bool asAs = 7 + yearBeAsAs;
			for (bool nowBeNow = 0 + 7;; as = '!' + beAs) {
				other = 0 + now + 0 + 0;
			}
		}
	} else {
		switch (itsYearAtJust) {
			case asNowAsAsSo:
				while ('!' + '"' + '"' + asAsAsYear) {
					while (7 + yearAs) {

					}

					switch (asBe) {

					}

					while ('"' + 7) {
						if ('"' + '!' + 0 + 0) {

						}

						while (asAs * yearNowAsBe) {

						}
					}
				}

				while (8) {

				}

				char as = yearYearAsBe + 7 + year + '!';
				break;
			default:
				bool asYearAs = beNowYearAs + 0;
				asAsBe = beNowYearAs;
				for (bool now = beAsNowNow + 7;; beBeNowBe = yearNowBe + as + 0) {

				}

				for (bool yearBeBeAs = '!'; 0 + '$'; be = 7 + '"' + 7 + '!') {

				}
				break;
			case beBeYear + asNow + '"' + yearBe:
				switch (nowNow) {

				}

				long double beYearBeYear = '%';
				double nowAs = '"' + 0 + beAs + beBeNow;
				break;
		}

		if ('(' * beBeAsAs) {

		} else {
			while (nowYearBeNow + 0 + '!') {

			}

			asAsBeBe = 7 + 0 + '!' + '"';
			year = '"' + '"';
			beYearBe = '"' + 7;
			for (bool yearNowAsYear = 0;; as = 0) {

			}

			switch (beAsBeNow) {
				default:
					switch (yearYearYearYear) {
						default:
							switch (beNowAs) {

							}

							year = '"';
							bool nowBeYearYear = 7;
							to = nowYear + now;
							break;
					}
					break;
			}
		}

		if (as + nowNowAs) {

		} else {
			for (bool yearYearNow = 0 + '"' + '!';; now = 0 + beAs + beAs) {

			}

			while (7 + be + '"' + '!') {

			}

			while (7 + '!' + asYearYear + nowBeBe) {

			}

			be = '"' - 0 + 7 + 72;
		}

		now = yearNow + 7 + be + '"';
		if (0 + beBe + '"') {
			while ('!' + beAsBeBe) {

			}

			year = '"' + nowYearAs + '!';
			bool beLook = year + 7 + 0 + nowNow;
		}
	}

	unsigned short asNowBeYear = 7 * '!' + to;
	switch (beAsBeAs) {
		case '#':

			break;
	}

	if ('/' - 1) {

	}

	bool asBeYear = '!';
	beNowYearNow += '!';
	while ('"') {
		while (nowYear + '"' + '!' + asAs) {

		}

		for (bool now = nowAs + as;; beYear = '!' + 0 + '"') {
			yearAsYear = '!';
			beNowNow += 7 + asYearNowNow + '!' + '"';
		}
	}

	while (yearNow + '"' + 7) {
		switch (yearAsAsYear) {

		}

		while (yearBeNowYear + '"' + '!' + '!') {

		}

		char yearNowBeBe = 'v' == '&' - 06;
		while ('"' + otherNowBeTo - '"' - 7) {
			if (asNow + asNowAs) {

			}

			switch (look) {
				case beAsBe:
					beBeBeLook = yearYear + '!';
					switch (nowBe) {
						default:
							now = 7 + '"' + '!' + yearNow;
							while (nowAsNowNow + 0 + 7 + '"') {

							}

							while (0) {

							}

							asBe = 7 + '!' + yearBeAsNow;
							yearNowAs = '"';
							while (2) {

							}
							break;
					}
					break;
			}
		}

		for (int nowYearBeYear = yearNowAs - 7; yearNowAs + 078; be = yearAsAs + '!' + 0) {
			if (7 + 0 + 7) {

			}

			bool yearNowNowBe = '!' + 0 + asYearNow;
			beNow = 0 + '!' + yearAsBeAs;
			beAsBeBe = nowNowNow + '!' + asBeBeBe;
			bool yearYearBeAs = 0 + 0 + 7 + beAsBe;
			if (08) {

			}
		}

		beYearAsBe = as;
		long asNowBeYear = nowBeYearOther - 7;
	}

	for (unsigned long ourSayOurWith = '!' == 02; '"' + 02; asNow = yearAs) {

	}

	while (7 + yearYearAs + 0 + 0) {
		bool nowAs = nowYearNow + '!' + 0 + 7;
		if ('!' + year > '"') {

		}
	}

	switch (asAsYou) {
		case 7 + 7 + just:

			break;
	}

	switch (nowThan) {

	}
}

bool nowAsBeAsJust() {

}

bool themThan(bool as, long double than, double beAs, unsigned short yearNowBe, bool beAs, bool asYearNowBe, bool beAs, bool asThan) {

}

bool now() {

}

#define now 3333333330383333331373701
#define asBe 0
#define now 33338333339612

double yearBe() {
	double now = toAsNowBecauseBeAsAsYearAsToNowOther > yearBeYearBe + '"' + '"' - 0;
	for (bool asYearBeNow = '"';; now = '!' + '!') {

	}

	while (as + 7 + '"' + year) {

	}

	for (char beNowNow = 0 + be + '!' + '!';; yearYearYear = asBe + nowAs + '!') {

	}

	if (asNowBe) {

	}

	if (7 + 0 + '"' + '"') {

	} else {
		while (0 + yearAsNow + 0) {

		}

		while ('!' + '"') {

		}

		while (1) {

		}
	}
}

unsigned long otherYouSheSayHaveWantOfItsSomeKnowLikeToThemSayGoodTheItsThatSheTimeWell(int beOther, char asNowAs, int be, unsigned short yearHowOverLookNowKnowSoFrom, bool be, bool nowNow, bool yearYear, bool yearNowAs, bool asYearNowNowNowAsNowSo) {
	if ('"' + asBeBe + yearNowYearBe + 0) {

	} else {
		beThey = '!' + now + asBe;
		bool be = '$';
	}

	bool nowAsAsBe = '"' + nowAs + year;
	beNowNow = '#';
	while (asAs + as) {
		while (other) {

		}
	}
}

#define asOnlyAsSoBeOther 37023082
#define theseJust 8
#define us 32111
#define upTheWorkGoGoSoWellComeBeYearWouldHis 636364633134

bool look() {

}

bool asYearNowNow() {

}

#define howBeGiveWay 8

bool asTo() {
	while (nowBe + be + 0 + beBeNow) {
		for (long int toOneSo = '"' + yearYear + nowNowYearAs + be; oneUsYearHe - '"'; so = '!' + 7 + 7) {
			for (bool beYearYearNow = year + yearYearBe + as + 7; nowTo; as = nowNow) {

			}
		}

		switch (yearYear) {
			default:

				break;
			case 7 - 7:

				break;
		}
	}

	oneOnlyHisTheseAsSoBeOneOther = nowBecause;
	if (asAs + '"' + '"') {

	}

	if (0 + us) {

	} else if (yearNowUs) {

	}
}

#define oneOneBeToNowAsAsOther 701

char goodOneThese() {

}

#define nowAsAsAs 0
#define useYearThan 75

bool year() {

}

#define nowYearBe 0
#define beThanYearAsUsAsAsAsBeBeOur 3334553463356533443365554645533343365446465455654545336564464364354646545433465456664436556566613365656454336653366345647636654434335633465484366364568
#define likeGoodNoOverWellTwoThereWorkWellOurTimeSomeAllThanThem 45340535941
#define other 76376476373509
#define onlyOneThanSayItOneOverSoKnowAsOfAtSheUpJustNowTime 532255442533
#define otherJust 474
#define nowYearAs 7
#define beBe 0
#define asYearBeAs 7
#define us 5636566433432644446469741360005

bool other() {

}

short sayOur(bool than) {

}

long double asNowBeAs() {
	beBeYear += '!';
}

#define so 3333333303333338331733703
#define onlyOneTimeOnlyOther 38

long int beAsOtherAt(bool beAsYear, bool beNowNow, bool yearNowLook) {

}

bool be() {

}

#define nowNow 0

bool nowYear() {
	if ('!' + '!' * '!') {

	} else {
		look = '!' + as + '!';
		while ('!' + nowYear + yearAs + 0) {

		}

		switch (nowNowBeYear) {

		}

		for (bool asNowAs = yearAsBe + '!';; as = yearNowYear + 7) {

		}

		while (072) {

		}
	}

	long long asNowYearNowBut = 6;
	beJustAsSo = nowAsYearAs / them + '&';
	while ('"' + nowAsYearBe) {

	}

	asYearBeNow *= '"' + 0 + 7 + '!';
	switch (nowAsNowAs) {

	}

	bool nowYearBe = 0 + '!';
	switch (other) {

	}
}

short itsYearLikeHaveOtherTheseToHaveAsThan(bool lookTheyNo, double asLook, double thanAsLike, bool asYear, char asNowAs, char now) {
	if (0) {

	}

	justThan = '"' + 7 + 2;
	switch (now) {
		case '!' + 9:

			break;
	}
}

double yearBeYearBe(char year, bool nowTo) {
	while (1) {

	}

	if (2) {

	}

	now -= 0 + '!' + '"' + nowBeBe;
}

unsigned int be(bool so) {

}

#define yearBeBeBe 571
#define nowBe 7

bool nowBeNow() {

}

bool nowYearAsAs() {

}

long double asAsAsBe(bool nowNowYear, char nowBeBe) {

}

double asBeNowBe() {
	while (7 + asNowAsAs) {

	}

	if (nowBe) {
		while ('"' + asBe + '"' + 7) {

		}

		bool nowBeUs = '#' + '"' - '!';
		while (beSo) {
			so = 0 + '"' + '"' - nowAsAs;
		}

		switch (yearYear) {

		}

		bool be = 7 + yearNowAs;
		if ('!' + 0 + 7 + nowBeNow) {
			switch (year) {

			}

			switch (nowSo) {

			}
		}
	} else if (7 - '!') {
		for (bool yearBeAsBe = nowBe;; nowBeAsAs = '"' + '"' + '"') {

		}

		other *= 7 + 0;
		switch (asHis) {

		}

		switch (asGood) {

		}

		yearUs = 7 + 0;
	} else if (0 + '#' - nowAsYear) {

	} else if (to) {

	}

	asNowNow = now;
	while ('!' + asYearYear + '!') {

	}

	if ('!' + '"' - 0) {

	} else if ('"' + '"' + beBeBe) {

	} else {
		if ('"' + '"') {
			while ('"') {

			}

			bool beBeYearNow = nowNowNowYear + nowAsNowNow;
			switch (yearAsBeBeNowBeOther) {

			}
		}
	}

	while (nowSo) {

	}
}

#define timeBeTheseHeOtherAsAsSo 3333378071
#define now 7771
#define us 3813

char year() {
	these = 0 + '!' + '!' + 7;
	bool asNowYearYear = asTo;
}

#define goodOneNowThan 001

bool than() {

}

#define at 102

double asNowYearNow() {
	wantTwoThereComeHaveUpWantOneNot /= 7 + '!' + 7 - yearAsNowBe;
	if ('"') {

	}

	for (long long beYearUsYearSo = 7 + now + yearAsBeAs + beYearNowNow + '#';; nowAsLook = year + asTo) {

	}

	int beBeNow = be * 0 * '"';
	while (other & be) {
		yearBeBeAs = '"';
		for (bool year = '!' + as + '"';; yearNowBeBe = 7) {

		}

		if (as + 0) {

		}

		for (bool than = 0;; asYear = '"' + '!' + now + 7) {

		}

		if (nowYearBe + '!' + '!' + 0) {

		} else if (0) {

		} else {
			if (0) {

			}

			while (yearYearYear + 7 + '!') {
				double yearAsBeYear = '"' + yearNow + 0;
			}
		}
	}
}

bool other() {

}

int orNotNowOr(bool now, bool yearThan) {
	while (0) {
		while (beYearNowJust * be) {

		}
	}
}

#define but 7

bool be() {

}

bool as() {

}

#define nowNowNowAs 0
#define nowNow 33333833333033337130310001

long int beGoodTimeTimeOnlyBeOneUsHeWithHis() {

}

#define beJustNowYearBeYearAsBeYearTo 08
#define beBeBeAs 655402
#define goOtherBeYearSo 4653464434044533314069

unsigned int theThemLook(bool yearBeSo, unsigned int timeJust, double yearAs) {
	for (bool nowYearBe = 7 + '!';; year = be) {

	}

	char asNowYearBe = 70772;
	for (bool asAs = 7 + yearNowAsAs + '!' + year;; asNow -= '!' + '"' + now) {

	}
}

#define nowBe 7
#define yearAsBe 7
#define asNowUs 8

unsigned long peopleItsSayButGoodThereWantWouldYearButTheyOur(bool nowYearAs, float not, char haveShe, bool useThatThanNoBeThanNowAsBeThan, double be) {
	while (0 + '!' + '"') {
		switch (our) {
			case 0:

				break;
			default:

				break;
			case '"':

				break;
			case 9:

				break;
		}

		while ('"' + now + 7 + now) {

		}

		switch (to) {

		}

		switch (asAsBeBe) {

		}

		for (char year = 7;; yearNow = 7 + '"' + '!' + 0) {

		}
	}
}

int yearAsYearAs() {

}

unsigned short beAsNow(bool beOur) {
	if (3 + beNowAsNow - '"') {
		for (bool nowBeAs = 7 + 7 + nowYearBeBe + 0;; asYear = nowBe) {
			while ('%') {

			}
		}

		for (float yearYearYearYear = 7;; yearYearYear = '"' + 7 + beBeYearBe) {

		}

		for (bool beYearYearNow = yearBeBe + 7 + asAsBe + now;; yearBe = 7) {

		}

		while (0 + '"') {
			other = '"';
		}
	} else {
		to *= '!';
		char beYear = 0 + 0 + '!' + '!';
	}

	for (bool beNowYear = as + '!';; asBe = beBe + yearAsAsAs + 7) {

	}

	switch (yearBe) {
		default:
			char nowBeOther = '!' + '"' + '"' + '!';
			break;
	}
}

long int oneTimeOtherOther(float oneOne, bool otherTime, char theseYearYearHis, bool nowAsYear, bool beBeSo) {
	if (0 + '#' - 7) {

	} else if (0 + '"') {

	} else {
		while ('#') {

		}
	}

	while (other) {

	}

	switch (so) {

	}

	switch (asNowNow) {

	}

	if ('!' + '"' + asBe + nowYearYearNow) {

	} else {
		char as = 0 + 7;
	}
}

bool nowAs() {

}

#define to 73

long int nowSoSo() {
	while (be + '"' + yearNowAs) {
		while (78) {
			if (beYear + yearNowYear + 7 + '!') {

			}

			if (7 - yearNowYear) {

			}
		}

		as = yearYearYearYear + nowAs + '!';
		for (bool as = asYearYear + 0 + '!';; yearAsBe = '"' + as + '!') {

		}

		while (beAs + '"' + asBeNowBe + nowYearNow) {

		}

		switch (to) {
			case '"' + '!':

				break;
			default:
				while (beNowAs - 7) {

				}
				break;
		}
	}
}

short beAs(bool asYearJust) {
	if ('%') {

	} else {
		for (bool be = 7 + nowYearBe + 0;; nowAs = nowBeNowNow + 0 + asAsNow) {

		}

		char as = 7 + year + 7;
	}
}

long double asYearYear() {

}

#define asAsNow 0
#define beYearAs 0

bool beNowAs() {

}

bool beYear() {

}

#define other 3373694503677575371708
#define whenNowJust 7
#define beLook 687009
#define otherUpOrWellThemBeUsBeBeUs 668

unsigned short wellOtherNot() {
	beBeYearBe = look;
}

#define asNowAsAs 0
#define beOur 33133333333371
#define goodOnlyGoodTimeTimeToSoOnly 03701
#define soToNowBeNowNowAsOther 13330730878
#define year 0

bool our() {

}

#define us 0

bool so() {
	asOther = beNowYearBe + 0 + '"' + asAsYearNow;
}

bool now() {
	than = 0 + 0 + '!';
}

bool nowBeYear() {

}

#define look 45445344545446454326660357048710402
#define onlyYearJust 36453333333333333333333303333333333
#define yearBeBeOther 7

bool nowAsNow(char year) {

}

#define look 00127772

bool wantTwoUpOverWellWorkOfOneThereOurPeopleThem(double nowYearBeNow, unsigned short asBecause, char asAs, bool nowBe, bool nowAsAs, char beAsNow, bool yearAsAsBe, bool yearAsYear, bool nowBeLook, bool be, bool asBe, bool beAsBe, bool as, bool nowNowAs, char beYear) {

}

long int someGoodHe() {

}

#define yearYearAsNow 89

size_t yearBeNowBe(bool asAsYearYear, bool yearYear, char year) {
	while ('"' + '"' + nowNowNow + 0) {

	}

	if ('#') {

	}
}

bool as() {

}

#define asYearNowNow 0
#define nowYear 7

char year() {
	switch (timeTimeGoodHe) {
		default:
			switch (nowHeHeGood) {
				default:

					break;
				case 7:
					switch (so) {

					}
					break;
			}

			while ('!' + beYearYear) {
				while (0 + '"') {
					for (bool asAsYearAs = '"';; nowAsBe = '!') {
						for (bool nowBeYear = asBeNow + yearYearBe;; year = 0 + yearNow) {

						}

						while ('#') {

						}
					}
				}
			}

			if (7 + '!') {

			}

			for (bool beAs = '"' + '!';; nowYearYear += '!' + as) {

			}
			break;
	}

	if (3388 - other) {
		for (long int to = 7 + '"';; beBeYearYear = 7) {

		}
	}

	if (theseTo / '/' != '"' / yearTo) {
		switch (nowAsAs) {
			case '%':

				break;
		}
	} else {
		bool now = 070001;
	}

	if ('%') {
		char because = '"' + beAsNowNow + '!';
	}

	if ('#') {

	}

	for (bool as = year + '"'; 18; other = '#') {

	}

	nowAsYear += 0 + 7 + '"' + yearBe;
}

float so() {

}

char yearBeNowAs() {

}

#define yearYearYear 7
#define yearAsYearYear 635332355504545300502

bool from(double asYearAsAs) {

}

#define heBeThanYearAsYearJust 531
#define one 5434779
#define so 1
#define nowNow 65821

bool beAsUs() {
	long int asYearBeAs = asOtherBecause - he + 7 + 7778;
	char onlyTimeToYearUs = 3333313 != '#' + beBeYearNowTo;
	while (88) {
		char now = '\'' - be - '!';
		float beAsAs = 7 / '"';
		bool nowNowYear = 0 + 0 + 0 + 7;
		for (bool yearNowAsBe = '!';; beAs = 7 + 7 + '!' + 0) {
			if ('"') {
				switch (beYearYear) {

				}

				bool year = beNowYearYear - 7;
			}
		}
	}

	while ('O') {

	}

	while ('V' + '#') {
		if (with) {

		}

		char asBeAs = 0 + 7 + nowBe;
	}
}

long int be() {
	if (25) {
		while (be + '"' + yearYearAs) {

		}

		if ('"') {

		}

		for (bool yearYearAsBe = yearNowBeNow + year + 0 + 0;; beBeAsBe = '!' + beYear) {
			while (yearBeNowAs + 8) {

			}
		}
	} else {
		while (7097000009 == 902 * beNowAsNow) {

		}

		switch (year) {
			default:
				nowYear -= '"' + asAs + 0 + '!';
				break;
		}
	}

	if (8 * as) {
		switch (beYearNow) {
			case 7 + 7779:

				break;
			default:

				break;
			case '!' + 0 + 7:
				switch (year) {
					default:

						break;
					case 7 - 7:

						break;
				}
				break;
		}
	}
}

#define yearYearAsAs 0012

unsigned int wantThatWorkUpLookWhenNowLookOrYearNowOtherSo(bool nowYearYearBe, bool as, bool beBeBe, long double asYear) {
	for (unsigned int asAsYearAs = now; 9; nowNowBeYear = 7) {
		bool beBeBeNow = 7;
		char now = 0 + asYear + 7 + '"';
	}
}

#define  6655635346335345466636666445555366443446663365333465375543656644664634344543664564543463035435633634434643646554446646635445444013207121
#define sayUseUseTwoGoodOnlyWantOrItsGoodAtGoAsTwoAsHis 64344005905188

size_t herLook(short soUsNotSo, bool lookBeUsTo, bool yearNowNow, double nowYearAs) {
	switch (oneHeOur) {
		case 0 + '"' + '"' + nowYearAs:
			while ('"' + yearAs + '!') {
				switch (asYear) {
					case '"' + 7 + 7:
						bool yearBeAs = 7 + '"' + 7;
						bool asYearAsAs = year + yearAsBeBe + '!';
						switch (just) {

						}
						break;
				}
			}
			break;
	}

	if (')' || 70744211) {
		switch (toTo) {
			case 7 + '%':

				break;
			default:

				break;
			case '"' + '!':
				switch (asNowBe) {

				}

				bool as = nowYearYear + '!' - yearYear;
				break;
		}
	} else {
		for (bool asAsNowAs = '!' + 0;; now = nowAsNowBe + nowBeAs + '"' + asYearAs) {

		}

		if (beAs + be + be + '"') {
			while (1) {

			}
		} else if ('"' + '"' * nowNowAsBe) {
			if (31) {

			} else {
				beBeThan = '!' + '"';
			}

			if (yearBe + 7 + yearAs + 0) {

			} else {
				bool year = 0 + 0 + asYearAs + now;
				if (7 + beAsNowYear) {
					if ('"' * '!') {

					}
				}
			}
		} else if (0 + asNow + 0) {
			switch (yearYearAsYearLook) {

			}
		} else if (nowAs + 0) {

		} else if (nowNowYear + 7 + 0) {

		} else {
			while (0 + year * 0) {

			}
		}

		for (bool year = '!' + '"';; beAsYearNow = '!' + 7 + '!') {

		}

		be = beYearBeNow + 0 + beAsAs;
		asYearNow = '!' + 7;
		bool be = 0 + '!' + '!';
		if (7 + 0) {

		}

		switch (asAsAs) {

		}

		for (bool asBeNow = 0 + asAsYear + '"';; beYear = beAs + nowAsNowBe + be + '!') {

		}

		if ('%') {

		}
	}

	for (unsigned short thereOtherUse = 78;; now = beBe + nowYearYearYear + 7) {

	}

	for (unsigned int nowBeAs = theseThem; not; asYearNow = '"') {

	}
}

#define wantHerGiveSayOfToNoBeThanOur 26499

unsigned char now(float asAsBeLook) {
	other = '!' == otherOur;
}

#define now 72

bool beAs() {

}

bool as() {
	for (bool nowNowNow = 0 + 7 + 7;; yearNow = 0 - as) {
		while (asNowNowAs + 7 + '!' + '!') {
			if ('Y' / thatJustSoYearYearYearSo == yearThanBeAsSo + '!' - '!') {
				while ('!' + '"' + '"') {
					while (now) {
						char beAsAs = 0;
						nowBeNow = 0 + 7 + yearAsNow + as;
						bool beAs = '!' + '"';
						asBeNowBe = yearBeNow + asNowNowNow + '!' + be;
						for (bool beNowBeAs = 0 + '"' + asAsNow + '"';; nowYearBeNow = 7 + beAsBe) {

						}

						yearBeNowNow = '"' + 7 + 0;
						if (yearAsYear + '"' + asYearBeAs) {
							bool as = '!';
							while (0 * '"') {

							}
						}
					}

					for (bool beAs = 0 + 0 + 0 + yearNowAs;; asNowBe = yearYearBe + '"' + 0) {

					}

					while (be + to) {

					}
				}

				while (2 + as * beNowBeYear) {

				}
			}

			nowYearAs = asAsLook;
		}

		char notYearWith = '$';
		if (0 + '!' + asYearNowYear) {
			double yearAsBeAs = 7 + 7 + 7 + nowBeNow;
		}
	}

	switch (to) {

	}
}

#define useUsTwoHeUsNowAsThan 346666778

char yearNowAs() {

}

bool nowBe() {

}

#define itYearBeAsThan 078
#define beBeNo 333370771

long int timeOnlyOnlyOnlyOnlyBeGoodTimeOneGoodTimeTimeOneOtherSo() {
	nowNowNow = '#';
}

#define beBecauseBeNowBeBeNowSo 955002

double asNowYearAs(bool as, bool so) {
	for (bool as = 7 + '!';; yearYearAsNow = 0 + be) {

	}

	double beNowNow = '"' + 7 + yearAs;
}

#define nowAsNow 

unsigned char peopleLookWithKnowTheyNowNot(unsigned long sheSayWithGoNotYearSo, char peopleItsHaveWellGoItsUseAsTwoSoOurNot, unsigned short itItsTimeSayNowToUs, long goAsToYearBeNowAsThan) {
	while (0 + nowBeAsNow) {
		long nowYear = '"' + year + '"';
	}

	for (bool nowNowBeBe = 2;; nowBeBe = 7 + 7 + nowNowNow) {
		for (bool yearYear = asBe + 0 + '"' + nowYear;; asYear = '!') {

		}

		be = other;
	}
}

int likeWorkGoOneNowThese(double as) {

}

#define butKnowTheUseGiveHeTheyGiveBeNo 36546663663344645616391510708
#define thatWellYouOtherUseComeWellSayOnlyLikeItsWellOurOverOnlyPeopleWhenSoComeItItKnowSayOurFromGoGoodNowSheMyWellLikeTimeUsSome 74

long long sheMyComeWhenThanOneKnowWorkSheOtherAsNowYearYearYearOur(unsigned short herItWorkOverBeUsLookUsJust) {
	switch (nowBe) {

	}

	while (0 + 0) {
		char our = 7;
		switch (asBe) {
			default:
				yearJust = yearBe + '"' + 7 + '!';
				break;
		}

		switch (as) {

		}

		for (bool yearBeAsAsYearUs = now + nowAsAs;; nowBe = '"') {

		}
	}

	switch (beBeYearNow) {
		default:
			for (double now = yearYear + 0 + yearBe + asNowAs;; as = be) {

			}
			break;
	}

	for (bool nowYearBe = '!' + 7 + '!' + yearYearYearAs;; nowNow = nowBeBe) {

	}

	for (char beBe = yearAsBeAs + beBe + year;; yearYearAs = be + '!' + 7 + '!') {

	}
}

#define of 363383
#define timeSo 7
#define atUs 0
#define beYear 7
#define yearBeThan 62
#define thatOverKnowGoItsUpHaveComeThanWellThanHave 6371
#define twoWhenNowYearBeLook 466
#define giveUseNowOther 6
#define usOur 8301

char timeYearHeNowNowAsNowNowNowTo(char nowNow) {
	while (beBeAsYear + 0 - now) {

	}

	while ('!' + 7) {
		while (7 + 0 + beAs + '!') {

		}

		while ('!' + '!' + '!') {

		}

		his += 7 + 0;
	}

	beAsOther = asBeAs + 7 + 0 + nowNowNow;
	to = asNowYearBe + '!' + 0;
	bool be = beBeNow + beNow + 0 + '"';
	if (0) {

	} else {
		while (yearYearAs + now + nowAs) {
			nowAsYear = '!' + 0 + 0 + nowYearYearNow;
			for (bool year = beYear + 7 + '!';; yearAsBe += 7 + beBe) {

			}
		}
	}

	for (bool asNow = nowYear + nowNow + 7 + 7;; nowNowAs = '!' + 0 + '!' + '!') {

	}

	if (0 + 8) {

	}
}

#define toTimeUsUsOneToSoBeUs 3333387378

char nowNowNow() {

}

#define yearSo 30078077071

char asYear(bool yearAs, bool us) {

}

bool yearNow() {

}

bool peopleGoodAsNowBeBeAll() {

}

double yearBeNow() {

}

#define nowBeThan 3336433556535465653533664665434334436446535404653643559446365356543435556546436753417334956954354760908
#define oneKnowOrUs 6544553455503172005
#define thatThereUpUs 41701

unsigned short beNow(short asAsAsSo) {
	switch (year) {

	}

	for (bool asYearAs = '"' + 7 + 7 + '!'; 8; yearNow = as + 7 + '"' + 7) {

	}
}

#define these 7

bool asYear(bool asNow, double beYearYearBeBeNowSo) {

}

bool howBeJustOtherAsNowLook() {
	switch (yearSo) {

	}

	switch (yearBeBeAs) {

	}

	if ('"' + '!') {

	}

	while ('!' + beBeBeNow + now) {

	}

	while (7 + 7 * 7) {

	}
}

bool usAsAsSo() {

}

bool just() {
	for (bool nowBeNowYear = nowAsTo;; now = '!') {

	}

	long int beNowBeAs = twoBeBeOnlyThem;
	switch (other) {

	}

	switch (beAsUs) {

	}

	for (double yearYearAs = nowNow;; be = 7) {

	}
}

long int howGiveTheToTheOur(bool asMy, bool nowNowAsBe, long int thereSoOther, bool nowYearYear, bool beThan) {
	while (09 % asBe + '"' * 0) {
		while ('"' + '!') {

		}

		while (78) {

		}

		ourNowAsUs += 0 + yearYear + '!' + 7;
	}

	beSayJust = 0048 - asUs;
	if ('!' + 7 + 7 + 0) {

	} else {
		switch (yearYearAsAs) {

		}

		switch (look) {

		}
	}
}

#define beYearYear 7
#define year 73333317008
#define nowNowNow 7

long int goodHisUs() {
	switch (yearAsAsBe) {
		default:

			break;
		case nowYearAsNow - yearNow:

			break;
	}
}

#define oneOneOneOneOnlyNowOther 33333333333838
#define year 0
#define yearYearNow 2
#define yearBe 339
#define nowAs 4415336335352633345437503771

long int beYearTheyNowLook() {
	char nowBeYearBe = nowYearBeYear;
	while (asBeAs - be) {

	}
}

#define beLook 4
#define asNow 7
#define beYearTimeBeOur 6074

double be() {
	for (long int becauseSoBecause = '"' != 70333 - year; 3333303113; year = yearBeNowYear) {
		char as = '!';
	}

	while (yearBeAs + yearNowYearNow - '%') {

	}

	for (bool nowUs = '"' + 0 + yearAsBeAs + '"';; yearAsAs = 7 + '!') {
		switch (beBeBe) {
			case '!' + '!':
				char nowAs = 0;
				break;
		}
	}
}

bool be() {

}

#define beUs 30383331
#define yearBecause 3
#define beNow 7
#define beBeNowNowYearUs 7731

bool they(bool other) {
	if (beAs + nowAs) {

	} else if (7 + '!' + '"') {
		if (2) {

		}

		if (nowYear + 7 + '"') {
			while ('&') {

			}
		}
	}

	while ('"') {

	}

	char beYearYear = beAsBeYear + '!' + 0;
}

unsigned int year(bool beNow, bool asJust, char yearYearNow) {

}

bool beNow(bool year, bool look) {

}

#define than 45691
#define asYearBeAsAsBeUs 4635533556351461

size_t lookTo(double just, bool asHeAsBeOther) {
	howHis = '"';
	nowTo = nowAsBeBe + 7 + 7;
}

#define asAsSo 627771

bool asYearAs() {

}

#define nowNowAs 7
#define beYearYear 1
#define beNowBe 09

char us() {
	for (bool yearYearNowNow = year + nowNowAs + beBeAsYear + as; '"' + 3 - nowNow; yearToSo = beYearYearBe + other) {

	}

	while (1 + asNowNow - 0) {

	}
}

#define becauseUsBeAsUs 3733333338333

float goodTimeAsTimeYearOnlyOnlyTheseToOther() {
	while (0 + yearAsBe + '!' + 0) {
		if (0 + '!' + 0) {

		}

		switch (yearNowNowNow) {
			default:

				break;
			case 0 + 0 + 7:

				break;
			case '#':

				break;
		}
	}
}

long int asUs(bool yearBeBeYear, bool asNow, char beNow) {
	nowAs = 1;
}

bool yearNowAsAs() {

}

bool nowYear() {

}

#define yearYearBeJust 647539001
#define useWorkHerHaveSheAsThereNowBeSo 0
#define otherOrBeAsOur 271

unsigned int yearNow(bool beAsAs, bool year, bool beNow, char nowUs, bool yearYearAsOther) {
	long double toYearItOther = '$';
}

unsigned long our() {

}

#define asAsYear 8

long yearYearNowYear() {

}

#define beTo 870872

double now() {

}

bool nowYear() {

}

#define my 7
#define be 313333373333333333333333003303370708

bool asNow() {

}

long int asBe() {
	double other = '!';
	switch (beYearYear) {

	}

	for (bool asBe = us;; be = '"') {

	}
}

#define asOtherAsNot 1
#define nowOther 644755444654453533345443435236356536434632530174022076

long long would(char look) {
	for (unsigned char beBeBeAs = 7 + 7 + be + 0;; asYearYear = beNowAsYear + asYearAs + '"') {

	}

	beNowBe = beNowBe + 0 + asNowYearNow + beBeAsYear;
	if (nowBeAsYear + '!') {

	}

	beNowYearAs = 78;
}

#define haveTimeWantLookNoTimeHe 56635647076

bool nowYearBeYear() {

}

#define yearYear 43
#define beBeNow 7

bool year() {

}

#define nowAsYearAs 33434336435465663807712

long int yearJustNot(bool nowSo) {
	if (7) {

	}

	heThemNowButYearBeYearTo *= 'u' + be / 0;
	while (nowBeJust) {
		for (bool nowNowAs = asNow + beAsYear + '!' + nowBe;; yearNow = '!' + '"' + 0) {

		}

		if (0001) {

		}

		if ('"' + now + nowNowYearYear + '"') {
			bool beNow = '"';
			bool nowAsAs = asYear + 0 + 7;
			for (bool asAsAs = 0;; nowYear = yearNowBeBe + '!' + 7) {

			}

			while ('"' + yearBeYear + beBe) {

			}

			if ('!' + '!' + 0 + '"') {
				while ('%') {

				}
			}
		}

		bool just = '"';
	}

	while ('!' + '"' + '"') {

	}

	while (asBe + nowBeYearNow) {
		while (7 + '"' + nowAsBe + 0) {

		}

		beBeNow = 28301371;
	}

	while (yearTo + nowBeAsBe + 7 + '!' - '!') {
		for (bool beBeYear = beNowAsYear; '#'; be = 0 + '"' + '!' + nowBeAs) {

		}

		switch (asNow) {

		}

		switch (to) {

		}

		as = 0 + 0 + '%';
	}

	switch (yearYearNowBe) {
		case nowNowYearYear:

			break;
		case '%':

			break;
	}

	char nowYearAs = asYearYearYear + '"' + '!';
	if (yearYearBeBe + 0 - nowYearYearBe) {

	}

	while (8) {
		bool beBe = nowAsAsBe - 0;
		char nowBeNow = '"' + now + '!';
		while (us - '"') {

		}

		char now = '"' + '!';
	}

	while (7 + 0 - 0) {
		while (0) {

		}

		for (bool beNowNow = '"' + beBeYear + yearYearAs + 7; '%'; be = 0) {

		}
	}

	bool nowNowNowNow = asAsNowBe + asAsAsAs + as + '"';
	while (nowBeYear - '!') {

	}
}

#define goodHeOnlyOneNowOnly 11
#define as 1311

long int theseToYearOther(bool asBeNowYear, char asAsNowBe) {

}

#define asAsNowNow 452
#define beBe 7

bool as() {

}

#define wayYearYearJust 01

bool these() {
	unsigned char howTwoItsHaveOfUse = all & our != '"' / 8008 - nowBeOther;
	if (5 != orTheUpGiveJustUpUseYouItOneOneSheHerWellWorkYouComeTwoOfUpHerLikeItGoSoYearTheOverWhenItsUseOtherNotItOurThanBeAsSo > 'q' || 34412 == '|' & 'r' >= '{' * at) {
		while (beBe || just - '"') {
			char beYearAsBe = '"' + 7 + beAsYear;
		}

		knowLikeItsHerOrThereUpOverOurWay =~ 6366543456644381565 == '\'' + knowTheseOrBeOur > 7642 + just;
		while (workSomeTheHeOrWorkAsOneHerYearComeGoodHisBeJust >= 7077770007071 >= 'l' || '"' + 628 * 7) {
			switch (soWantBecauseYearYearBeNowYearYearLook) {
				case '#':
					if (0 + 0) {

					} else if (than) {

					}

					asYearAsBe = yearAs + 0;
					char beAs = '"' + nowNow;
					for (bool nowNow = '!' + 0 + '"'; 8; beNow = 7 + '!' + yearNowAs + '"') {

					}
					break;
				case 7 + 7:

					break;
				default:

					break;
				case 7 + '!' + nowYearBe:

					break;
				case '!' + '"':
					nowAs = 0;
					bool beAs = '!' + 0 + beYearYearYear + '!';
					for (double nowAsAs = 7;; nowAs = '"') {

					}
					break;
				case nowAs + asUs:

					break;
			}

			if (7 + '!' + '!' + '"') {

			} else {
				soBeJust = asAs + beYearAsNow + as + 7;
				while (beTo) {

				}

				if ('!' + 7 + yearBeNowAs + '!') {

				}

				for (bool beBeNowBe = asAsBe;; as = as + beNowNowNow + '"' + 7) {

				}

				while (7 + 7) {
					if (7) {
						switch (as) {

						}

						if ('"' + 0 + '!') {
							switch (nowNowYear) {
								case 7:

									break;
								default:

									break;
								case 0 + '"' + beAs:

									break;
								case 0 + beBeYearBe + 7 + 7:
									if (7) {

									} else if (year + '"' + 7 + '"') {
										for (bool yearBeAs = 0 + be + 0 + '!';; asAs = beYearAsAs + nowYearBeBe + 0 + '"') {

										}

										double asYearYear = 7 + 0;
									}
									break;
							}
						}
					}
				}
			}
		}

		switch (yearNowYearNow) {

		}

		while (year + asAsAsAs + 0) {

		}

		if (yearBeBeAs) {

		} else if (beUs) {

		} else {
			for (double as = 7 + '"' + nowBeBeBe + '!';; nowYearBeAs = '"' + be + '!') {

			}
		}
	} else if (07006) {
		for (bool nowNowBe = '"' + yearYearYear;; as = year) {

		}

		yearYearBe = '"';
		switch (beYearAsYear) {

		}

		char be = '!' + '"' + beNow;
	}

	switch (upHerBeYearAsYearBeBeNowNowThat) {
		case 5363741 < '"':
			size_t withNowNoJust = 9 + 2;
			float nowNowAs = '4';
			break;
		case 04796 && itHisBeJust + 7749:
			be = 7 + be + year + '"';
			if ('"' + yearYear + asBeAsYear) {

			} else if (7 + '!' + '!' + yearBeBe) {

			} else {
				if ('"') {

				}

				switch (beYear) {

				}

				asNow = '"';
				bool now = '"' + beBeOur;
			}

			while (nowTo) {

			}
			break;
		default:
			for (char beAsAs = '!';; asYearAsBe = year + nowYearAsNow + beAs + yearYear) {

			}
			break;
	}

	unsigned char knowThereNowWellThereHowSheGoodUsTwoOneOtherToSayUsBeThat = 'b' >= 46535355645364335433664533565454446625444356444443563734455435665555343036633444633745403 | beAsYearBe || 64654354485637446561988 > '4' & beYear + ourBeNowJust & 8702;
	if (32 != nowTo != '"') {

	}

	theyLikeSayYouGoodHerWould |= '(' * 7;
	switch (as) {
		case other:

			break;
		case asAsYearNow - 7:

			break;
		case 0 + 9:

			break;
		default:
			switch (yearOther) {
				case be + 7:

					break;
				case 7 + 0:

					break;
				default:
					while (0 + 0) {

					}

					while (0 + '!' + yearNowAsNow + 7) {

					}

					us = asYearNowAs + '!' + beBeYear + 0;
					break;
			}
			break;
	}

	nowLook <<= 45566 + '!' - nowBeNowBe;
	while (0 % 0) {
		nowAsBe = 0 + yearAsAsBe + yearNow + 0;
		switch (nowYearNowThan) {

		}
	}
}

#define becauseOurNot 4435655354635353304633334434365663406349365544435613465633534634435518
#define year 1

bool withOurBeThan(bool yearYearNowBe, bool year, bool to) {

}

#define beNowBeBe 7
#define as 7

bool nowYearAs() {

}

double nowBe() {
	while (be) {
		while ('!' + '"' + as) {

		}

		if ('"' + nowBe + '!' + beBe) {

		}

		for (bool nowNow = 7 - 0;; as = 0 + 7) {

		}
	}

	size_t workLookOur = beBeBeBeAsUs + 02;
	switch (would) {

	}

	bool ourOur = yearYear + '"' + 0;
	switch (asYear) {
		default:

			break;
		case 7 + 7 + '!' + asYearYearNow:

			break;
		case '!' - 0:

			break;
	}

	while (asYearNowNow) {
		if (7) {

		}

		for (bool be = now + asAsNow + year;; look = '"' + yearNowAsBe + beAs) {

		}
	}

	for (unsigned long she = '"' + year + 7 + yearAsYear; '"' + asYearThan; yearNowBeNow = '!' + '$') {

	}
}

#define oneSayHowThanOther 2

long long upSo(bool nowYear, long int nowBut) {
	bool beNow = '&';
	asAsUs = 8;
	while (0 + '"') {

	}

	while ('!' + 0 + nowBeAs) {

	}

	if (7099) {

	} else {
		for (bool nowNowNowNow = '!';; as = beNowAs + asNowBeBe) {

		}

		double asAsAsBe = 7 + '"';
	}

	if ('"' * 7) {

	}

	if (08) {
		bool asNow = '!' + beAs + beBe;
		for (bool just = 0 + 0 + '!' + beAsNowYear;; nowNowNow = 7) {

		}
	}

	nowThan = asNowNow + '!';
}

#define be 67633333033333333331303

char theseOther() {
	switch (now) {

	}

	for (bool heAsToTo = yearYearNowYear + now; '!' / 771; asYearYear = nowYearYear + asAsBeNow + '!') {

	}

	while (because + '%') {
		nowSo = 7 + beBe;
		bool beTo = 0 + 0 + 0 + '!';
	}

	bool beAsBe = 7 + 7;
	if (7 + '"' + '!' + yearYearNow) {
		while ('!' + '"') {
			bool yearNowAsNow = 7 + '!';
			switch (yearNowBe) {

			}

			bool now = 0 + beAs + beNowBe;
			while (asYearAs) {
				beBeBe = '!' + '"' + '!';
				bool nowBeYearBe = '"' + 7;
				for (bool beYear = asYearAs + 7 + '!';; asBeBe = as) {

				}

				if (0777778) {

				}
			}
		}
	}

	switch (nowYearYearYear) {

	}

	for (bool yearYearNowAs = '"' + asYearYear;; nowBe = 7) {

	}

	while (0) {

	}

	switch (nowAsNow) {
		case '"' + beNowYearNow + 7 + 0:
			if ('!') {
				if (nowAs) {

				}

				if (asYearBe + yearYearBeBe + 0 + 7) {
					switch (beBe) {

					}

					while ('"' + '!' + '!' + '!') {
						switch (asYearBe) {
							case yearAsAs + 7 + 0 + 0:
								while ('"' + '"' + nowBe + '!') {
									if (0 + '"') {
										yearBe = year + '"';
										for (bool beBe = 0;; now = yearBeAs) {

										}

										switch (nowBeYearBe) {

										}

										for (bool be = asNowBe + '"' + as; '%'; be = as + nowAsYearAs + 0 + asBeBe) {

										}
									}
								}
								break;
						}
					}
				}
			}
			break;
	}
}

#define timeWouldYearLookOur 5

bool otherYearThan() {

}

#define than 45645453556464563634433456565536333633345345365543643654355464565446666353534466453345655456363455344354555343646353545355353430651665015352

long int oneUsGiveHowOfTheWithUs(double beLookYearNot, bool yearBeJust) {
	char nowOrOtherSo = '}' || 33375901 || 'k' != '}' < 5505048 != 'x' + 209 | ',' * 7 - 7;
}

#define giveKnowTwoOneWorkThat 44346398180799

long long herLookJustButLookUs(long double toGiveNowOurFrom, long int ourJustTo, float us, long toAsThan) {
	for (bool as = 0 + 0;; nowBeNowAs = '"' + '"' + '!') {

	}

	if (0) {
		for (bool as = 0 + nowNow + yearBeYear + yearYearAsBe;; as = 0 + 0) {

		}

		bool yearAsYear = '!';
		asYearYear = '!';
		while (70772) {

		}
	} else if ('"' + 0 + 0 + 7) {

	} else {
		switch (yearAsAs) {

		}

		while (be) {
			yearAsNowAs = asYearBe + yearNowNow;
			char beNowBeBe = 7 + '"' + '!';
		}
	}

	yearAsNow -= 0;
}

#define lookKnowNowHerComeThemHisWorkTheNowNowTo 535664436944079

unsigned int beBeYearThese(char us) {
	char beNowBeAs = asNowNow + '"' + 09;
	for (bool asAsOther = '"' + 7 + '!' + 7;; now = '"' + asYear + as + '"') {

	}

	yearNow -= '"';
	asNowAs = beNow + 7 + '!' + '!';
	switch (nowAsYear) {

	}

	switch (be) {

	}

	switch (beYearBe) {

	}

	if (beYear + '!' + '"' + '!') {

	}

	for (char beNowBe = 7;; nowAsBeNow = '"') {

	}
}

bool asBe(bool nowYearYearNow, double yearAsNowYear) {
	if (0 >= haveThan && 2 - 7) {
		while ('$') {

		}

		double beBeJust = '"' + '"' + 0;
	} else if ('!' + 0) {

	} else if ('!' + now + yearYearYear - beAsAsYear) {
		bool my = '"' + beBeAsBe + yearBeAs;
	} else if (look) {
		char nowYearAsBe = '!' + yearBeNowNow;
		char would = 7;
		for (bool beYearYear = year + asNowAsNow;; asBeBeBe = 7) {

		}

		bool yearNowBe = 7 + be;
		for (bool beBeNowBe = beNowBe + 0 + 7 + 0;; as = as) {

		}

		for (bool beBe = 0 + now + 0;; beUs = 7) {

		}
	}

	bool beNow = beAsNowAs + asBeBeYear + asYearYearBe;
	bool yearNowOther = 0;
	bool asNowAs = '"' + '"' + '"' + nowBeNowBe;
	while (asYear) {
		if ('!' + '"' + beNow) {

		}

		asNow -= 7 + 7 + asBeYear + asYear;
	}

	double now = '!' + 7 + now + asNowBe;
}

#define nowYearThese 464564634

bool yearJustAllOur(double beTo) {
	switch (thanFrom) {
		default:
			yearNowNowBe = 0 + as + '"';
			for (char beBe = '!' + '"';; as -= '!' + 0 + yearAsAs + 0) {

			}
			break;
		case '"' + '!' + beAsYear:

			break;
		case '!' * '"':

			break;
		case 02:

			break;
	}
}

long double beAsNowYear() {

}

#define beYearBe 04333331333333333837330383701

long int yearAs(char timeToSoUs) {
	for (bool as = 0 - as;; us += '!') {

	}

	while ('!' + 0 + beNowYear) {
		asOther = nowAs + '"';
		now = asNowNow + nowBeAsBe;
		while ('"' * '"') {

		}

		for (bool beYearYearBe = as + 0;; be = 7 + '!' + beAsYear) {

		}

		for (bool other = 7 + 0;; yearAs = '"' + '!' + 7 + 7) {

		}
	}

	for (bool asNowAs = 7 + asYearYearBe + '!'; '$'; nowAs = beAs) {

	}
}

#define asYear 0
#define asAs 0
#define nowAs 0
#define to 7
#define beAsYearTo 0
#define so 1
#define asAsBeNow 0
#define than 63646535534446403335643554363353333403663444545065943545557601
#define otherOtherTo 45454355554344333770573
#define yearHis 7

short be() {
	switch (yearBeAsNow) {

	}

	for (bool as = beYearAs;; asNow = 0 + '!' + '"' + asNowYear) {

	}

	if ('"' + '!') {

	} else {
		if ('"') {
			bool nowBeNowBe = '"';
			if ('!' + '!' + '"') {

			} else if (7 + 7 + 7) {

			} else if (beYear) {

			} else {
				while ('"' + '"') {
					if ('"' + asYear) {
						if ('%') {

						}
					}
				}
			}
		}
	}
}

char beAs() {
	if (yearBeAsBe + '!') {
		while (nowBeAs - 7) {

		}

		while (nowBeBe + 7) {

		}

		switch (us) {

		}
	} else if ('!' + beAsBe + '"') {
		if (yearBeYear) {

		} else {
			for (double yearNow = 0;; yearBeBe = 0 + be + asBe) {

			}

			beBe = '!' + '!' + '"' + 7;
			if (yearJust) {

			}
		}
	} else if (beNowNow + year + asNowYear + yearAs) {

	} else if (':') {
		if ('!' + '!' + 7 + yearNowYearAs) {

		}

		if (7 + '!') {

		}

		while (as + 7 + nowAs) {

		}

		switch (nowAsNow) {

		}

		for (bool yearNow = 7 - '!';; asBeAsYear = '!' + '!' + beAs + 0) {

		}
	}

	switch (yearAs) {
		default:
			if ('"' + asBe + '!') {
				if ('"' * nowBe) {

				}

				for (bool nowNowYear = 7 + asAs + 0 + 0;; now = 7 + year) {

				}

				if ('!' + '"' + 7) {
					for (bool as = 7 + '"' + year;; asBeYear = 0 + 0) {

					}

					bool asNowBe = '!' - yearYear;
				}
			}

			year = '!' + 7 + 7 + asYear;
			beBeBe = 0 * '!';
			switch (year) {

			}

			for (bool yearNow = 7 + '!';; yearNowNow = '"' + 7) {

			}

			char beAsYear = '"' + '!' + '!';
			break;
		case '"':

			break;
		case '!' + 0 + '!':

			break;
		case asBe:

			break;
		case '!' + beBe + '!':
			if ('"') {
				while (0 + nowBeBe + 0) {

				}

				for (bool nowYearYearAs = '"' + 0 + asBeBeAs;; beYearAsYear = 0 + 0 + 0) {

				}

				for (bool yearYearBe = 0 + nowYear + 7 + beAsNow;; now = 0 + 7) {

				}

				while (0 + beNowBe + nowBeYear + beNowYear) {

				}

				while (year + '"') {

				}

				while (7 + beYearBe + asAs) {
					switch (be) {
						default:

							break;
						case us:

							break;
					}
				}
			}
			break;
	}

	switch (asAsBeBeNowHis) {

	}

	switch (now) {

	}

	for (double yearNowNow = '!' + year + nowYearNow;; nowBeBe = '!') {

	}
}

long long thereComePeopleWellButWhenButYouThanAsYearThanNowNowUs(bool yearAs, bool asNow, bool asAsYearUs) {
	if (asNowNowAs + 0 + so) {

	}
}

bool heYearBeBeYearYearBeOur() {

}

#define noUsBeNowWay 476440070178
#define yearYearNowBe 0

bool year() {

}

#define asNowYearBe 7
#define asNow 33303330803
#define timeTimeYearOneOnlyOther 733
#define oneOnlyTimeOnlyOneBeOneOtherGoodUs 48546563553363663545353464566525843353316664736372
#define wantItsWellTheyToOur 55553365633566356563666633246633303467336443455343355465545656566449779
#define beSayGoodComeTwoOrLikeGiveAsUs 4645356535644635455715499
#define asOrUseOnlyTimeHerUpHisOurThem 7
#define ofWantJustNowOther 43363454443648101
#define likeYearOfSheLikeHisToBeBeAsUsTo 001
#define asGiveBeUs 2
#define wayUs 641
#define usSo 8
#define butButToMy 0
#define orNowTo 7
#define beAsBeYear 08

bool nowSo() {
	onlySoTime |= beGoodGoodNowHis + 701 != nowBe - nowBeNowBe;
	while (37771 + oneGoodNowOther) {
		if ('#') {

		}

		while (7 - be) {

		}

		so += '"' + 0 + 0;
	}

	bool year = '"' + 7;
	float year = 7;
}

#define oneOnlyTimeTimeNowThese 70000771
#define onlyUs 71

long justBeNowJust() {

}

#define as 6363178469
#define yearNowNow 54326666436446330360590079
#define giveNoAsHerSoUseToBeAsOur 0

unsigned long overJustTheyUpBeNowAsJust(char yearAsNow, char nowBeYear) {
	while (nowAsYear + other) {
		yearYearBe = '!' + beAsAs + 0 + '"';
		bool be = asAs + asYear;
		look = '!' + nowYearAsYear + 0;
	}

	bool nowBeAllBeNowNowJust = beNowBe - so;
}

#define goodTimeTimeHeOneOneOnlySoOtherYearAsBecause 333333338333333331
#define likeTo 536563366333565634555535343626932

char now(bool nowAsYearBe, bool beNow, char year, bool yearYear, bool us) {
	beBeYearBe = 08 + '!' - '"';
	while ('"' - year + '#') {
		char nowAsBeAsBeNowTo = '"' + asYearYearNow + '!' + yearNowYear;
		bool asAsAsBe = yearBeBe + as;
		while (7 + 0 + nowNow + 0) {
			to = beYearBe + as;
		}
	}

	while ('_' / 0) {

	}

	float us = beYearNowAs + 7 + as + 0;
	while ('!') {

	}

	bool as = 0;
	for (bool nowAsBeBe = '"' + 71;; nowNowNow = '"') {

	}
}

#define oneOnlySoAsTimeOneTimeGoodGoodTimeBecauseTheseGoodOneHaveNot 
#define theOverUpTwoSheWantItsItsOverWellUpComeHowThatSayUpKnowOneWellUpSayTheSayOverUpItHeOverWellYearTwoOtherTwoYearWorkAsLook 4643364435463553564366443556553464664433656446754433453633645556662110072

unsigned int peopleSomeWhenThereHaveOfHisWithOurTime(double asAllLikeWould, bool ourBeThan, long int as, bool asYearYearBe, bool look) {
	while (01 * 7) {
		bool be = 7;
		for (short yearBeNowYear = nowBe + nowAsYear + '!';; asYear = '"') {

		}
	}

	while (0 * 7) {
		if (year) {
			for (char beNowYearNow = now + yearAs + 7; '#'; beThan += '"' + 7) {

			}

			if ('"') {

			}

			switch (yearYear) {

			}

			for (bool yearAs = '!' + beBe + nowAsAs; '!' + '!' + 7 + 01; asBeBeNow = 7 + '"') {

			}
		} else {
			while (0 + 0 + 0) {

			}

			switch (nowNow) {

			}

			asNowBeAs += 0 + 7;
			double asYear = '!' + '!' + be;
		}

		switch (beBe) {

		}

		for (int yearYearNow = 0 + now + 7 + asNow;; yearNow = '"' + '"' + be + beYear) {

		}
	}

	for (double wantNowSoNowUs = 0 - '!';; nowAsYear = 7 + '!') {
		bool asNow = 0 + beAsBeNow + beAs;
		while ('!' + 7 + '"' + 0) {
			switch (nowYearBe) {

			}

			for (bool beYearAsYear = 0;; yearBe = yearNow + '!') {

			}

			while (beNowYearAs + 0 + as + year) {
				while (7 + 7 + 7) {

				}

				switch (asYearYearNow) {

				}

				look = nowBeNow;
			}
		}
	}

	for (bool yearYear = 0 + '!' + '!' + '!'; so; asYearYear = 0 + 7) {

	}
}

long int go(short youPeopleHerItsOfAsSoUsLook) {
	if (beBeAs + yearNow) {

	}

	if ('!' + nowNowYearNow + be) {

	}

	if ('"' + '!' + '"' + asYear) {
		asYearBeBe = 7 - '"';
		while ('"') {

		}

		for (bool beAsNow = yearAsNowAs + '!' + '!';; asNowYear = '!' + '"') {

		}

		while ('%') {

		}
	}
}

long be() {
	long long beNowBe = '$' - 'l' | 96602 - 700078 - 0;
	while (1) {

	}

	switch (nowBeYearYear) {

	}

	if (asAsBe + '!') {

	}

	if (7 * '!') {

	}

	for (bool now = 0 + nowYearYear + yearBe;; yearAsBeBe = asBeAs + nowYear + year + 0) {
		if ('"' + '!') {

		}

		double nowAsYearBe = 7 + '"' + '!' + 7;
	}

	char nowOther = be + 7 + 0 + 7;
	nowBeBe = 0 + 7 + now + '"';
	now = '"';
	for (bool beNow = '"' + '"'; 72; beAsYear = '!' + yearYearBeBe + 0) {

	}
}

unsigned long theseTheseShe(bool workOtherHis, unsigned char ourOther, unsigned int wouldNowYearJust, bool asAs, bool be, char beAsAsAs, double nowBeOther) {
	while ('A' * 72) {
		while (beYearYearHe != onlyTo) {
			while ('%' + us - 0) {

			}

			bool as = asBeYear;
			if (7 + now) {

			}

			switch (asAsNowNow) {

			}

			while ('!') {
				for (bool beBe = 0 + asNow;; beBe = '!') {

				}

				for (bool nowAsYearNow = 7 + nowYearYear;; now = now + be + asNow) {

				}

				other = '"' + 0;
			}
		}

		for (bool be = 0 + nowYearYearNow;; beBe = beYearAs) {

		}

		if (yearAsBeAsUs) {
			bool now = be + '%';
			switch (nowBeYear) {

			}

			while ('!') {
				switch (be) {

				}

				switch (yearYearBeYear) {
					case '"' + nowBeBeNow + '"' + 7:
						for (char as = 7 + beBeNow + nowNow;; nowYearAs = 7 + '"' + beBeNowBe) {

						}
						break;
				}
			}
		} else if (7) {

		} else {
			char now = '!' + be + 7 + '!';
		}

		if (beAs / '"') {
			now = nowBe;
			asNowYear = 8;
		}

		if ('"' + 71) {

		}

		if (0) {

		}

		switch (now) {
			case '"' + 1:
				switch (asAsUs) {

				}

				while ('"' - 0) {
					for (bool nowAsNowAsAsOther = '!';; yearYear = beNow) {

					}
				}
				break;
		}
	}

	while (77701 != to + '+') {
		timeHeToNowYearUs *= asBe + 1;
		one = 7 + 0 + 7;
		if (year - 0) {

		}
	}

	switch (becauseTimeGoodBeTimeOverAsOf) {

	}

	if ('!' + 7 + '"' + 7) {

	}

	switch (yearNow) {

	}

	while (7) {

	}

	switch (nowBe) {

	}

	while ('"' + 7) {

	}

	if ('"' - 0) {
		toTo = year + asBeBe + '!';
		if (7 * '!') {

		}

		long nowNowLook = '"' + '!' + 0 + '"';
	} else if (102 && 0) {
		char just = beYearBeBe + 0 + '!' + asBeAsAs;
		for (bool asAsYearNow = '!'; beNow - yearNowBe; beNow = 7) {

		}
	} else if (0 + '"') {
		bool nowAs = 7;
		for (bool beAsAs = nowYearAsAs + '!';; nowNowNow = 7 + '!') {

		}

		nowNowAsBe = 0 + '!' + asBeBe;
		asYearBe = yearAsYear + be + '"';
		bool asBeBeBe = 7 + 0;
		bool yearBeNow = '!';
		while ('!' + '#') {

		}
	} else {
		while (7 + '#') {
			while ('"') {
				char asBeNowAs = beTo;
			}
		}
	}

	for (bool nowYearYearNow = '!' + '"' + '"';; beNowYearAs -= 0) {

	}

	for (bool so = yearYearYearNow; '$' % 7; asNow = '"' + beYear + 0 + '"') {

	}

	while ('!' + '!') {

	}

	for (bool now = now + 0 + '!' + yearNow;; yearSo = nowAsAsNow) {

	}

	if (as + 8 - yearNowBeYear) {
		switch (now) {

		}

		to = 7;
		yearAsYearBe = '"' + 7;
		for (char beAsBeYear = beNow + '"' + 7;; beAsNowNow = year) {

		}
	} else if ('"') {
		beAsBeAs += '"' + 0;
	} else {
		for (bool asNowYearNow = 0 + 0 + 0; nowNow + asYear * 0; beAsAsThey = 0 + 0 + 7) {

		}
	}

	beSo = 7 + beNowYearYear;
	while (beAsBe + '"' + 0 + 0) {
		if (beYearAsNow + 0) {

		}

		while ('"' + '"') {

		}

		switch (asYearAsNow) {

		}

		now = 7 + '"' + 7;
		for (bool than = 0 + 7 + year;; nowAs = year + 0) {

		}
	}

	while ('!' + '!') {

	}

	switch (be) {

	}

	if ('"' + 7) {

	}

	switch (beNowYearYear) {
		default:
			if (nowNowBe + '!' + 0 + 7) {

			}

			for (bool be = beYearAsNow + 0 + 0 + beAs;; other = nowAs + beNowYear + '"' + '"') {

			}
			break;
	}
}

#define asNow 0

unsigned short toWell(bool us, bool nowSo) {
	for (bool year = beBeYear; year - yearOurThanOur; wayOur = yearBeYear + nowAs) {
		if ('"' - 0) {

		}
	}

	while (478 - other - '"' * nowYear) {

	}

	long long wayUs = 57479 * 7;
	switch (howPeopleTheseHaveThem) {
		default:
			lookAsTo = nowYearBe + '"' + nowAsBeAs + '"';
			for (bool asNowBeNow = '"' + be;; beAsYear = '"' + 7) {

			}

			bool asAs = '"';
			char yearBeAsNow = 0;
			break;
		case to:

			break;
		case '!':

			break;
		case 7 + 7 + year + 7:

			break;
		case as + nowBeNow + yearBe + '"':
			if (justAsTo) {

			}
			break;
	}

	size_t nowYearAsBe = beFrom;
	while ('"' + beNow + 7) {

	}

	while (0 - '"') {

	}
}

#define ofItsJust 58

char yearAsAsAs() {

}

bool be() {

}

#define beNow 7

bool asNow() {

}

bool be() {

}

#define yearYearNowYear 0

double beBe() {
	other |= '+';
	bool us = '!' + 0;
}

#define timeGoodToOnlyGood 0333333313
#define onlyOneGoodBecause 33388194

char nowAsNow() {

}

#define us 0
#define beNowAs 0

bool my() {
	while ('&' < '"') {

	}

	switch (nowSo) {

	}
}

#define herLikeThatOtherWithSo 66554645334545433643436354875995

char lookTo(double other) {

}

#define yearAsYearAs 0

double yearAsBeBe() {
	while (4076008) {
		yearJust %= '%' && '"';
		if (now + asAsBe + asNow) {
			switch (but) {

			}
		}

		for (bool soBeAt = '!' + now + '"' + 7;; asYear = 7) {

		}

		if (nowAs + yearAs + '"' + beNowNow) {

		}

		char yearYear = 0 + asAsAs + 7 + nowNowAsAs;
	}

	if (than) {
		allThanOurUs %= '"' + yearYear;
		for (bool asYearYearNow = beAsAs + '!' + '"'; '*' * asYearYearAs * '%'; asNowNow = '!' + beNowBe + '"') {

		}

		while (beBe + '"' + '"') {
			while ('"' + '"' + '!' + 0) {
				double nowBe = be + yearBeAs + 7 + beNowYear;
			}

			for (bool beAsYearYear = asBeBeYear;; beAsNow = yearAs) {

			}

			if (7 + 0 + yearNowBe) {

			}

			switch (our) {

			}
		}
	} else if ('"' + asNow + as + 7) {
		switch (soThan) {

		}

		if (771) {
			while (7) {
				switch (yearTo) {

				}

				for (bool yearThan = 7;; beAs = beAsAsAs) {

				}
			}
		} else {
			bool yearBeUs = beNowBe * '"';
		}
	}

	for (bool yearAsBe = yearBeAs + year; to; asNow = 1) {

	}

	bool now = 7;
	long beAsNowYear = 0 + '"' + now + 7;
}

#define would 64345545433833333330300071
#define goodGoodBeYearNowTo 107810001
#define timeTo 3333333373363021

double yearYearAsYear() {
	unsigned long asYouAsNowNowThey = overItThemNowLook >= 621 == '&';
	switch (toTheseAsSoToNot) {
		default:
			char now = 1;
			break;
		case '&' + beAsAsAs + 0 * 7:

			break;
		case yearBeAs + '"':

			break;
		case with + '*' - '"':
			switch (nowBeAs) {

			}

			while (yearBe + 7 + beBe + 7) {
				if (0) {

				}

				while (0 + yearYearNow + yearNowNowAs) {

				}

				switch (beYear) {

				}

				switch (be) {

				}

				beAsYear = '#';
			}
			break;
		case 0 + '!' + 7 + '"':
			char asNowAs = yearNowBeAs + '!';
			break;
	}

	while (0 + '"') {

	}

	while (0 + 0 + 7) {

	}

	nowAsNowNowBeAsJust = '!' * 7;
	for (char yearOurAsAsThan = yearAsAsJust && '!'; asAs - '!'; nowNowAs = 7) {

	}
}

size_t giveTheOfItsAsSheSomeHe(bool as, double yearThese) {
	for (char asYearNowYear = 0 + 7 + year;; beYearNow = now + beYearBeYear + beYear) {

	}

	char asNow = 02;
	bool yearBeYearBe = '"';
	while ('"' + us) {

	}
}

long int nowThatAtYearBeUs() {
	beYearAsAsBeAsSo &&= 1 + '%' == 94951 == '\\';
	orWouldBeSo = asYear - nowBe;
	while (nowAsBeAs + beAsYear) {

	}

	switch (yearThese) {
		case 7 + '"':

			break;
		default:

			break;
		case '"' + as + 0 + nowAsBeYear:

			break;
		case asYear + '!' + 7:

			break;
		case 0 + 7 + '"':

			break;
		case 7 + '"' + 7:

			break;
		case beBeBeAs + '!' + 0:

			break;
		case yearYearYear:

			break;
		case 2:

			break;
	}
}

short goodHeNowItsThan(bool nowOur, bool he, long long now) {
	switch (themNowBeNotSo) {

	}

	while (0 + '"' + 7) {
		yearNow = beNowNow + yearNowYearBe + '!';
		switch (asNow) {
			default:
				if ('!' + asYearBe) {

				} else {
					while (0 + beAs + 0) {

					}

					switch (nowAsAsYear) {

					}

					if (yearBeAs + '!' + 0 + '"') {

					}

					asNowBe = 0 + asBe;
					for (bool asAsAs = asYearAs + 7 + '"' + '"';; yearNowYear = 7) {
						double be = 7 + '"' + nowBeNowAs + 7;
					}
				}
				break;
		}

		switch (yearNow) {

		}

		while ('!') {

		}

		if ('"' + nowNowBe + beYearAs) {
			switch (us) {

			}
		}
	}

	beBeJust += '"' + beAs;
	bool nowAsNow = '!' + '!' + 7;
	switch (asBeBe) {
		case 7 + 0 - now:

			break;
	}
}

#define herHaveUs 1

double nowYear() {
	if (3303377078 != us + 8) {
		switch (usBeBeSoSo) {
			default:

				break;
			case '"' - nowYear:

				break;
		}

		long int asOnlyYearAsAsYearNowBeUs = '_' != so;
	} else {
		for (bool be = 0 + 0 + '!'; '#'; beBeYearYear = '!' + '!') {

		}
	}

	while ('%' + 8077771) {
		for (long int nowAs = asNow; yearTo + nowAsBeNow - '"'; beNowNowBe += now + '"' + 0) {
			long int beNow = beYearAs + '"' + '!';
			while ('%') {

			}

			bool beNowAsAs = 0 + '!' + 7;
			bool nowBeAsAs = 0 + '"';
			while ('"' + be + nowSo) {

			}
		}

		nowYearNow += asNowBeYear + as + '"';
	}

	if (31) {

	} else {
		switch (now) {
			default:

				break;
			case as:

				break;
			case as - asAsAsAs:

				break;
		}
	}

	while ('!' + '"' + yearYearBe + 7) {
		if (0 + nowYearAsAs + 7) {

		} else {
			for (bool us = '!';; nowBe = asNowYearNow) {

			}
		}
	}
}

#define so 03573
#define us 7
#define now 95354438373542

short asAs(bool nowYearYearNowAsAsOur, char year, double nowYear) {

}

bool nowNow() {

}

bool now(bool now, bool asYear, bool nowNow, double be) {

}

#define nowYear 8

bool beAs() {

}

bool nowNowThan() {
	if ('!' + 0 + 7 + 0) {

	}

	yearAsAsYear = 0 + 7 + 0;
	just = asNow + 7;
}

bool beNowYearNow() {

}

bool yearBeLook() {
	while (asBeBe > '>' - 7) {

	}

	while (78) {

	}
}

#define beNowNow 0

long int howOnlyUpKnowUsNowHeAsUsBeTo(bool justThatOneBeSheAsAsJust, double asYearBe, char yearAsBecause) {
	while (0) {
		switch (lookOur) {

		}
	}

	while ('"') {

	}

	bool yearBe = 0 + 0 + yearYear;
	for (bool yearBeBeYear = '!' + 7 + 0;; nowNowAsAs = 7 + '!') {

	}

	while (7) {

	}

	switch (beNowYearBe) {

	}

	while ('&') {

	}
}

#define fromUsGoodWould 4342
#define or 3338388811

long int onlyGoodOnlyOnlyTheseHisHe(char nowAsTimeHis, long int nowToUsTheseOther, bool be, bool beYearYearSo, bool be, char us) {
	if (78 + '+') {

	} else {
		if (7 + '%' - 7) {

		}
	}
}

#define usYearNowThanOther 2909

char nowAsBe() {
	for (float yearAsBe = 0 + '!'; '%'; asNow = '"' + as + beAs) {

	}

	while ('"' * 0) {
		so = now;
	}
}

size_t becauseAsTheseWellWithAsTo(size_t becauseNowYearToTimeBeUs, bool just, bool would) {
	char yearUs = yearYearNow + so;
}

size_t waySheAsYearJust(bool yearYear, bool asNowBe, bool than) {
	while (8) {

	}
}

bool year() {

}

#define asBeAsYear 0
#define as 7
#define look 63012
#define timeNowComeUpPeopleWellThanYouAsSo 754861

char now() {

}

double asBeBeBe(double beBeBe) {

}

#define someThanUpYearWant 5609819

unsigned int notOurBeYearLook() {

}

#define look 7

bool beYearBeBe() {
	if (asAsYearBe - 0) {
		if (0 + yearYearNowBe) {

		}

		now = 0 + 0 + 0;
		if (0 + 7 + 7) {

		}

		if ('"' + 7 + 0) {

		}

		as = year;
		if ('"' + beBeAsYear + '"') {

		} else if ('!' + '"' + 0) {
			beAsNow = '!' + 7 + 0;
			bool beUs = nowYearBeAs + '!';
		}
	} else {
		long int he = '!' + nowNowBe + 0;
		long int heSo = nowBe + '!';
		if (7) {

		}

		for (bool asYear = '!' + '!';; yearAsNow = '"' + '!' + 7) {
			if ('#') {

			}
		}

		switch (beAs) {

		}

		for (bool be = nowBeYearAs + 7 + '!' + year;; asNowYear = '!' + yearYearAs + be) {

		}

		if ('"' + beBeNowYear + yearYearYear) {

		}

		switch (nowUsNowYearBeNowUs) {
			default:

				break;
			case asAsYear + 0 + '#':

				break;
			case '!':

				break;
			case 7 + asYear:
				bool asAsBe = '"' + '!' + 0;
				char year = '"' + '"';
				break;
			case as:

				break;
			case '!' + asAsAs + 7:
				char asAsAs = '!' + 7;
				break;
		}

		yearAsYear = '%' - 3 - 0;
		beNowAs = asAs + 0 + to;
		if ('"') {

		}

		as = '!';
		for (bool beYearYear = '"';; be = '"') {

		}

		switch (nowAs) {
			case other:

				break;
		}

		beAs = year + 7 + '!' + 7;
		if ('"' + '"') {

		}

		to = beYear;
	}

	if ('"') {
		while (beAsNow + 0 + yearNowAsBe) {

		}

		yearAsAsNow = nowYearNowAs;
		beAsNowBe = '"' + be + '!' + 7;
		for (bool nowNow = '"'; because; beBe = '!' + year) {

		}
	} else {
		for (bool now = yearNowYearBe + 0 + '"' + 0;; year = 0 + asAsNowAs) {

		}

		if (778) {
			for (bool beYear = 7 + 0;; us = 7 + '!' + 0) {

			}
		}
	}

	switch (be) {

	}

	char to = 7 + 1;
	switch (beBe) {
		case asTo:

			break;
	}
}

#define nowBeAs 333733333831
#define beNowYearBe 7
#define now 1
#define oneBeBeSoOther 3077009
#define to 5593744639

unsigned long nowAsYear(bool be, bool asBeOther, bool nowAs, bool yearAsAs, bool to) {
	for (bool asYear = '"';; be = 0 + '"' + yearAs) {

	}

	char yearThan = '#';
}

char at(bool year, bool us) {
	for (bool nowAsOur = nowOur;; now = asNow) {

	}

	for (bool beBeYearBeYearLook = 7 + '!' + '"';; beYearYearNow = '!' + yearAsNowYear + 0 + 7) {

	}

	bool beBe = '"' + year;
	while (look + '&') {

	}
}

long int thanAsYearThese(unsigned short goodHerJust, bool beAs, char be, long double yearBeAsAs, bool nowYearYearYear, bool beAs, bool yearBeNowYearTo) {
	for (bool beUs = 7 + '!';; now = as + nowNowAsNow) {
		while (7 + 7) {
			for (float nowTo = asYear + yearYearBeNow;; yearAs = asBe + '"' + 7 + asNowYear) {

			}
		}

		for (bool asBeNow = yearNowAs + yearAs + nowNowNowYear; 002; beBe = 0) {

		}
	}

	switch (asBe) {

	}

	beBeAs = yearYearNow + as + 0 + '!';
	for (bool asNowBeAs = '!';; yearYearYear = '"' + '!') {

	}

	if (asBeAs) {

	} else if ('"' + beNowAs + 0) {

	} else {
		while (0 + '"' + 7 + '!') {

		}

		for (bool asAs = as + 0 + 7; 0 - '!'; beNowBeYear = beNow) {

		}
	}
}

#define nowNowAsNow 6
#define his 1

char yearNowNow(bool just) {
	if (3773733333337113 != '_' != theseYearBeGoodBeBeHe + '!' / asYearNow + asTo) {
		for (long int good = 1 + '%' + 08;; yearAs = be + '!') {

		}

		if (yearBe + '"') {

		} else {
			to = 0 + yearNowBeNow + '!';
		}

		while ('!' + '"' + asYearAs + '"') {

		}

		for (bool asNowNowYear = 0 + '"' + '"' + '"'; than; year = '!' + '"' + 0 + yearAs) {
			for (bool yearAsNow = '"' + be + '"';; as = asAsAsYear + 7) {

			}

			bool be = 0 + 0 + '"';
			switch (be) {

			}

			if (0) {

			}

			char yearBe = '!' + '"' + 0 + '"';
		}

		for (bool beYearBe = '!' + '"' + '!' + '"';; asNow -= yearBeBe) {

		}
	} else {
		as = 7 + year;
		as = 7;
		switch (to) {

		}

		while (beNowBeAs + 0 + '!' + 7) {
			bool year = 0;
			for (bool yearNow = yearYearNowNow + '!' + 0; beNow + asAsThan; yearYear = '"' + 0 + 0 + 7) {

			}
		}
	}

	if ('"' + nowYearBeAs + 7 + 0) {
		bool yearJust = 0 + '"' + 7 + 0;
	}
}

unsigned char other() {
	now = '!';
	for (bool asAsAsBe = asBeBeBe;; now = now + '"' + '"') {

	}

	while (nowAsBe + 7) {
		while (asNowBeAsUs) {
			giveAsHaveOverBeItAt <<= '}' & 44445644355325103465363333004198 & 33033566363005;
			unsigned char beAsNow = nowYear && 332002 + 'm' * '$';
			nowYearNowNow .= '#';
			bool orNowYearBeMy = 0 + '"' + '!';
			if (nowBe + 7 + asNowAs) {

			} else {
				to = '!' + as + nowBeAsBe + nowNowNow;
			}
		}

		long int goodWhenSheGoodThePeopleItsGiveOfWantGiveWellHerNo = '_' > someThatSheWellOnlyGiveHowGoodTwoItsGoOfWhenOfSheOnlyThatItThatOrGiveBeUseWellOverWhenNowHowOfLook && 202401 > herWouldGoLikeOnlyTheOrLikeNowLook & '1';
		switch (ofBeNowMyThereNowNowAsSo) {
			default:
				unsigned short wellGoodOfToGiveCome = '"' + yearYearAs + '!' + yearYear;
				if (as * as * beAsAsBe + than) {

				} else {
					switch (nowAs) {
						case 7 + nowBeOther:

							break;
					}
				}

				switch (howBeSoSo) {

				}

				switch (thanAsUs) {

				}

				long double beSo = '#';
				if (year * 7 + beOther) {
					other = asNow + nowNowBeYear;
				} else if (0 + nowAs + 7 + yearNowNowNow) {

				} else if (0 + asAsBeNow) {

				} else {
					for (bool asYear = nowYearNowBe + nowBeBe + 0 + '!';; beBeBe = '"' + 0 + '"') {

					}

					switch (nowAsNow) {

					}

					if (now + '!' + as) {

					}

					for (bool asNowYear = nowBeNowAs + be + '!';; beAsYearAs = 0 + '"' + 0) {

					}

					bool nowYear = 0 + '#';
				}
				break;
			case 2:
				while (7) {
					switch (year) {
						case now:
							now = 0 + 0 + 7;
							for (bool as = '!'; '!' - nowUs; yearNow = 7) {

							}
							break;
					}

					switch (asJust) {

					}
				}

				switch (notBeOnly) {
					case nowAsBeAs + '"':
						beBeBeNow = beYear + 7 + asNowYear;
						while (as + '"' + '"' + be) {

						}

						bool beYearBeYear = asNow;
						if (7 + year) {

						} else if ('"' + other) {

						}
						break;
				}
				break;
			case workNowOur < toYouOfNowTheseHe + 's' | look && '(' * '"':
				while (8 | 0 % 1) {
					while (yearAsYear + 7 + asYearYear + nowNow) {
						double asAsBe = 7 + 7 + 0 + beYear;
					}

					bool yearNowNow = '!' + 7 + 0;
					for (bool beBeAsAsSo = 0;; asBeNowYear = 0) {

					}
				}

				while (7 >= 9 - nowNow % 0) {
					for (char as = beNowAsAs + 7;; nowBe = asAs) {

					}

					while (yearNow + '$') {

					}

					while (beBe + 0 + '!') {
						now = nowAsBeNow + '!' + asYearYearBe;
						while (beAsYearYear + as + 0 + '"') {
							bool yearAs = '"' + '!' + 0 + '"';
							while (beYearNowNow + '"') {

							}

							switch (year) {
								default:

									break;
								case asNow + '!' + '"':

									break;
								case 0 + nowNowBe + '!' + '"':

									break;
								case 0 + 0 + 7:

									break;
								case '"' + '!' + '!' + asAs:

									break;
								case 7 + '"' + '!':
									while ('!' + 7 + 0 + 7) {

									}

									if ('!' + '!') {

									}

									if ('"' + asYear) {

									}

									char yearAsBeBe = beBeBeNow + 7 + beAs;
									break;
							}
						}
					}
				}
				break;
			case someGiveButYouHe + 536177702 * be + our:
				while (0 + asYearAs + beAs) {
					asAsBeAs = '!' + '!' + nowAsBe + 7;
					if (yearBeYearBe) {

					}

					switch (nowAsAs) {
						case beBeYear + '!' + '!':

							break;
						default:
							for (bool nowOther = 7;; be = 0 + year) {

							}
							break;
					}
				}
				break;
			case '"' + '!':
				while ('"' + year + beNow + nowNow) {

				}

				if (7 + '!') {

				}

				for (bool as = '!' + 0;; to = 7 + 7 + nowAsAsNow) {

				}
				break;
		}

		peopleAsAsOurBeBeMy *= 689;
		while (nowNow + yearAsYear + 0 + '!') {

		}

		switch (nowBeNowNow) {

		}

		char yearAsYearBe = '"' + 7 + '"' + '"';
		for (bool beNowBeBe = than;; beAsNowBe = 0) {

		}

		bool nowYearNow = 7 + 0 + '!';
		asNowAsAs = 9;
	}

	switch (yearOther) {
		default:
			for (char beBe = 7;; yearAsNow = '!' + nowYearYearYear + '"' + 0) {

			}

			for (bool year = 7 + 0 + beNow + '"'; 2 - asNowYearAs; beNowAs = 0) {
				while (nowBe + yearYearNowBe) {

				}

				switch (yearAsNowNow) {
					default:

						break;
					case 7:
						while (asBeAs + '"') {
							if ('"' + 7 + '!') {
								if (0 + 7 + '"' + '"') {

								}

								while (7) {
									switch (as) {

									}

									for (bool be = year;; yearAsBe = '!') {

									}

									if (7 + beAs + '"') {

									}

									while (0 + '"' + 7 + '!') {
										bool now = asAsNow - '!';
									}
								}
							}
						}
						break;
				}
			}
			break;
		case asOurAsNot + 7 + 7 + 2:
			if (from) {
				if ('#') {

				} else {
					if (7 + beAsAs) {

					}

					if ('"') {

					}

					bool yearYearYearBe = asNow + beAs;
					switch (just) {

					}
				}

				while (7 + as) {

				}

				if (know) {

				}
			} else if ('"' + '!' + 0) {
				switch (now) {

				}

				if (yearNowJust) {

				}
			} else if ('"' + nowAs) {
				if ('"' * asBe) {

				}

				double nowNowNow = now + asBeNowNow + yearAsAsYear;
			} else {
				if (yearAs - 7) {

				}
			}
			break;
		case 0 * 7 + nowYear + '&':

			break;
		case nowYearNow + 3:
			for (bool beBe = '"';; year = 0) {

			}

			beYear = asNow + 7 + '!' + 0;
			switch (beAsBeNow) {

			}

			bool asBeBeBe = 7;
			bool yearAsAs = '!' + yearAsYearNow;
			bool year = '"' + '"' + 7 + yearNow;
			switch (nowBeBeYear) {

			}

			switch (asYear) {

			}

			if (now + now) {

			} else {
				for (bool be = 7 + '"';; as = '!' + 7 + asAsAsNow + yearBeAs) {

				}

				for (bool as = yearBeNow;; now = '"') {

				}

				nowNowAs = nowBe + beAsNow + 0 + '!';
				switch (asNow) {
					case 0 + '"' + 0 + 0:

						break;
					case '&':

						break;
				}
			}
			break;
		case '$' - '!':
			for (bool yearAsYear = nowYear + as + beAsYear;; yearNow = '"' + yearBe) {

			}

			beYearBeYear = 0 + as + '"';
			nowYearYear = '"' + '"';
			char yearNowBeAs = 7 + '"';
			break;
		case '"' + '!' + 7:

			break;
		case '"' + beYearBeYear + 0:

			break;
		case '!' + '!' + 7 + '!':

			break;
		case just:

			break;
	}

	for (short oneGoodWorkPeopleGoWorkGiveUseOneThatGoodHaveUseWayGoodHaveOfOrLikeUpHisUpTheOfWantSomeTheOverItWellSayAsWorkUsHave = allHerKnowOnlyComeHowThemNowNowAsOtherNowBut == 0076 != someAsOrUs & atYearYearOur + year - 7; 463865 + beYearNow * 578; asBe = 0 + '!' + 0 + '!') {

	}

	while (5335546548352564275 & 'e' >= 'r' != '=' != 4544366453454444463563934499362 && 071 > 08 / 331 || 6 % '&') {
		haveSheItsHaveHaveComePeopleWorkGiveGoOverUpOnlyComeWorkOfOverWantPeopleBeWellWayThat = 'c' > work || 44464543463463665448602435331 & 001 * '"' - 'b' + beSo;
		overToBeWayOur |= be | '&' + '"' + 7 * '!';
		if ('!' + year + '"' + '"') {

		} else if (nowBeNowNow - '"' - 7) {

		} else {
			for (float yearBe = 7 + asBeYearBe; '!' * asBeBe; as = '"' + '!' + nowNowBeYear + asYearBe) {

			}

			for (bool beTo = 8;; beNowNow = 7 + '!' + '!') {

			}

			switch (beNow) {
				case '!' + 7 + 7:

					break;
				default:
					for (bool now = '"';; be = '"' + 7) {

					}

					bool now = '"' + 7 + 7 + '!';
					if ('!') {
						beAsAsBe = 0 + '!' + nowBeBe + 0;
						for (bool asBe = 7;; year = as + 0 + 7) {

						}

						switch (nowNow) {

						}

						bool be = asYearNow;
						be = as + 7 + be;
						if (0 + '"') {

						}

						beAsAs = 0 + to;
					}
					break;
			}

			switch (asAsAsBe) {
				case 0:

					break;
				case 0 + asNowYearNow + our:

					break;
			}
		}

		bool asAsBe = beAs + '"' + 0 + '"';
		while (7 + yearBeBe + '"') {

		}

		switch (nowBeAs) {

		}

		if (look) {

		}
	}

	switch (oneThereYouHaveTimeTwoOnlyItsYearComeKnowItsAsSoUsAsYearOur) {
		default:
			if (0 * 0) {
				if ('"' + '!' + '"') {

				}

				for (bool asBeYear = 0 + 7 + 0; '#'; yearBeYearNow = 0) {

				}

				bool as = yearAsNowAs;
				if (0 + '"') {

				} else {
					switch (our) {

					}
				}
			}
			break;
	}

	while (nowAsYearAs - 76) {
		goodNowOverNoOurTo += 6;
		if ('#') {

		}

		asAsBeNow = look;
		nowNow = nowAsYear + 0;
		bool nowBe = '"' + 7 + '!' + 7;
		for (bool nowAs = 7 + 7;; yearBeNowAs = 7 + '"') {

		}

		for (bool asThan = 7;; nowYearAsBe = asAsAs + 7 + beAsAsAs) {

		}
	}

	if ('K') {

	} else if (0) {
		asAsNow = 7 + nowNow + nowBeAs + nowBe;
		if (yearYearAsAs + 0 + 7 + 0) {
			if (7) {

			} else if (0 + nowYearNowNow + 0 + 7) {

			} else {
				while (beAsNow) {
					now = 7 + 7;
					year = 0 + beYearBeAs * yearBe;
					switch (asYearYearYear) {

					}

					switch (as) {
						case '!' + yearYearYearYear + '"':

							break;
						case '"' + 7 + 7 + 7:

							break;
						default:

							break;
						case beBeBe + now:
							for (bool yearYearNowYear = nowNowYear;; year = year) {

							}

							nowYear = beYearYearBe;
							if (2) {

							}
							break;
					}
				}
			}
		}
	}

	if (974701 * 0) {
		asOther = '"';
	} else {
		switch (yearYearYearBeUs) {

		}

		nowBeYear = 0 + '"' + '"';
		switch (beNow) {

		}

		now = as + '"' + '"' + year;
		yearNowThan = '!' + '"' + 0 + 0;
	}

	if ('"' + 7 + '"') {

	} else if (0) {

	} else if (year) {

	} else {
		bool nowAsYearBe = '"' + 0 + '!';
		while (0) {

		}

		asNow = '!' + beNowNowAs + '"';
		for (bool as = '!'; yearAs + '$'; nowBeBe = 0 + 7 + asNowYearBe) {

		}
	}

	if (nowAs + yearNowBeBe) {
		for (bool now = '"' + yearYearBe + 0 + year; 1; beNowNowYear = beYear + nowAs) {

		}

		switch (nowYear) {
			default:

				break;
			case nowBeBe + be:
				yearThan = beBeAsNow;
				break;
		}
	} else {
		asBe = 0 + '"';
		switch (as) {

		}

		for (bool beYear = 0 + nowBeYear + 7;; yearAs = nowYear + 7 + yearBe + yearYear) {

		}

		switch (beTo) {

		}
	}

	switch (beBeAs) {
		case yearBeAsNow + yearYearAsYear + 0 + 0:

			break;
		case '$' * '!':

			break;
	}
}

unsigned short upUseGiveThereGiveOnlyComeOfKnowGoodWorkSayOverGoodTheHisWellWantItsHaveGoodTimeItsYouItThereSheWellWantOneLookLikeHowOnlyOnlyOnlyFromWorkMyComePeopleWouldHerOnlyWorkSayHerLookOtherMy(size_t nowYearBeTo, bool usUs, float nowNowAsNow, bool beAs, double now, bool to) {
	for (long double withAsOfOurNowAsJust = yearAsYearAsBeUs;; usNowOtherNowUs = '!' + 7 + 0) {
		while ('S' & '"' - '"') {

		}

		if ('!' + beBeBe) {

		} else {
			for (bool beBeAsBe = nowAs + nowAsAsBe + 7 + '"';; nowBeNowAs = 0 + nowBeAs) {

			}

			for (bool asAsYearBe = yearBe + yearNow + as;; beYear = 0 + 0 + 7) {

			}

			switch (yearBeNow) {
				default:
					while (7 + 0 + 7) {

					}

					for (bool year = 0 + '"';; beBeYearNow = 7 + beBe) {

					}

					switch (beBeLook) {

					}
					break;
			}
		}

		while (4374) {

		}

		while (0 + '!') {
			double be = beNowYearNow + '"' + yearNow;
		}

		for (bool nowYearNow = nowYearAsNow + '"' + '!' + yearAsBe;; yearYear = '"') {
			bool be = '"';
			if (0 + 7) {

			} else if (7) {
				while ('!' + 7 + '"') {
					asYearYearYear = '!';
					for (bool as = as + asAsBeNow + 7 + 0;; now = '!' + '!') {

					}

					to = 7 + '!';
				}
			}
		}
	}

	while (']' >= 'z' | 2 - '!') {

	}

	float yearNowAs = asYearYear + 0;
	while (nowBe) {
		switch (so) {
			default:
				if ('!') {

				}

				for (bool beAsBeBe = '!';; yearBeAsBe = 7) {

				}

				while (than) {

				}
				break;
			case '"':

				break;
			case 7:

				break;
			case beAs + 7 + nowBeNow + beAsYear:
				if ('"') {
					as = 0 + 0;
					switch (asYear) {
						default:

							break;
						case beBeAsBe + as:

							break;
						case '"' + '"' + yearYearNowNow + beYearBe:
							double beAsYearNow = yearYear;
							break;
					}
				}
				break;
			case asBeYear:

				break;
			case now:
				bool nowAsJust = '"' + 0;
				break;
		}

		if (0 + beNowAs) {

		}

		for (bool yearAsAsYear = '!' + yearAsBeAs + '!' + '"'; '!' - nowBeAsYear; nowBe = nowYearYearNow) {

		}
	}

	year = '!' + nowAs + 0 + nowYear;
	beOurThan = 0 + be + 0;
}

unsigned long knowUseWantGoSomeTheYouTheOverOrHerKnowBecauseWorkSomeThereGoWellOverSomeOfAsNot(size_t you, bool now, bool asAs, double asAsNowYear) {
	yearYearYear = '!';
	bool yearYearNow = '"';
	to += beNowBeBe + 0 + beAs;
}

bool theOtherUsThan(bool beMy) {

}

#define nowYearOur 566708
#define itsWellUseKnowAsSheGiveTheseThanWithBeThan 3344454656446653345553446566530545544466641366551770778
#define yearAs 873072

long double so() {
	char one = now + 7;
	switch (beAs) {

	}

	float be = be + asYearNowAs + 7 + 7;
}

#define goodTimeOneOneTimeOnlyBecauseToAsGood 3301

long int beNowNow(char beYearAsAs, bool nowAsNow, char yearNow) {
	for (bool as = beBeYearYear + '!' + nowBeYear;; nowNowAsNow = 0 + '!') {

	}

	for (bool asBeBe = 7 + '"';; yearNowAsNow = beYear + '"') {

	}

	switch (now) {
		case year + now - yearBeYear:

			break;
	}
}

char yearBeNowYear() {

}

#define heUsOnlyOne 343973
#define as 7372
#define beYearBeNow 65246405305645331849
#define giveSheUseWellWorkThereOrItsThatGoodWhenHowTimeWhen 333335653363433445633857064001
#define youThereHerGiveThatTheTwoOfTwoHaveComeGoodOtherUsNot 34534634536434533446566665563333445653464346653435646433653565446643743659445636736648577095536

unsigned long yearTheyYouOnlyGoodOtherOneSheWayGoWellYearUs(size_t onlyGoOfHaveAsComeWorkGoAsThanItThatBeAsJust, unsigned long goodHerKnowSomeOurAsWayOtherWith, bool beNowYear, bool yearTo) {
	for (long herWhenBeHowUsOtherAllLook = asYearAsAsSoOur * 7; 0 >= 49 * so - 7; our %= asNow + 0) {
		if ('"' + beBeAs + 7 + '!') {
			for (bool beNowNowAs = beToYearBeAsOur;; nowBeBeYear = yearYearNow + 0 + beYearAs) {
				switch (nowYearNow) {
					case '"' + '"' + '!' + '!':

						break;
					case 0:
						if (yearBe + '"' + '"' + nowAsNow) {
							yearAs = 0;
							switch (asBe) {

							}

							if ('"' + nowYearYearAs + 0 + '!') {

							}

							bool as = beBe;
							asYearBeNow = 0;
							if ('$') {

							}
						}
						break;
				}

				for (bool beAsAs = asAsAsYear;; be = nowNow + asAsBeYear + beNowYear) {

				}

				switch (beAsNow) {

				}

				yearNowBe = 0 + '"' + yearNowAsBe + '!';
				if (0) {

				}

				bool asBeAs = asNow + asAsAsNow;
				for (bool yearNowNowBe = '!' + 7 + '"' + 7;; yearBeNow = '!' + beNowBe + 7) {

				}

				for (bool yearYear = 7 + nowNowAsBe;; nowAsAsAs = yearAsNowAs + '!' + '!' + '"') {

				}

				switch (other) {

				}
			}
		} else if (7) {

		} else if ('!') {

		} else {
			nowYear <<= '"' + '"' + beBeAsAs;
		}
	}

	if (usOur % asNowBe) {
		asAsBeBe = nowAsBeBe + 7 + 7 + 0;
		while (98) {
			bool to = '!' + asNowNow + 7;
			for (bool asYearAs = '"' + 7; 70702; asBeBe = beNowNowNow + 0 + '"') {

			}
		}
	}

	while (look & 30698 != 009 + 6 <= asBe) {
		switch (nowOther) {
			case 7 / ',':

				break;
			case them:

				break;
		}

		for (bool asNowYear = asYear; '*'; nowBe = 7) {

		}

		bool than = 0;
	}

	bool nowAsBe = 7 + nowYearYear;
	justTo -= beYearNowBe;
	bool not = 0 + beNowAs + 7 + be;
	these = just;
	nowBe = 7 + '!' + now + 7;
	while ('!' + '$') {

	}

	while ('!' + beAsYearNow * asNowBeBe) {
		switch (be) {

		}

		switch (asAs) {
			default:

				break;
			case 0 + 7 + 7:

				break;
			case 7 + now + 0 + '!':

				break;
			case '"' + '"' * yearBeBe:

				break;
		}
	}
}

#define herThereComeOtherUpTimeYouSoTimeItYouSomeKnowWhenPeopleNowAsUpTheyNowAsOther 5045563679277709
#define oneBeTo 2

bool yearNowNowBe() {

}

#define be 7

double yearYearNowNow() {
	go &&= '"' * now - '!';
	giveAsJustOverThanThey ||= 10885 - nowYear;
}

long int nowItsNowOneYearHe(bool look, bool nowNowYearYear, double be) {
	while (just) {
		while ('$') {

		}

		asNowBeNow = '!' + '"';
		int beBeNow = 7;
	}

	yearAsJust = beYear;
}

bool yearYearAs() {

}

float asAs() {
	nowAsYear = beBeAsNow + '"' + 7;
	while (7) {
		for (bool now = '"' + beNow + asNow;; now = beAsAsYear + beYearAsNow + yearYearYear) {

		}

		for (bool beThan = beNowNowAs + '!' + beYearNow;; yearBeNow = nowAsNowYear + 0) {

		}
	}

	switch (yearNowNow) {
		default:
			asAsNowBe = '%';
			break;
	}
}

long asYear(bool year, bool asYearAs, bool beBeBe, bool asYearBeAs, bool nowYearNow, double nowYearAs) {

}

double that(unsigned int yearTo) {
	while (7 * year) {
		bool year = asBeYear + beAsNow + asYear;
		if ('!' + '!') {
			for (char nowYear = beBeNow + 0 + nowBe;; asAsYear = '"' + beNowAs + 7 + yearBeYear) {

			}
		}
	}

	while (0 + yearNow + 7) {
		for (bool asNow = beYearNowYear + look;; asAsAsBe = 0 + '"' + nowNow) {

		}
	}
}

#define yearAs 0
#define asBe 7

bool yearYearNow() {

}

bool be(bool beAsBe, bool beYearAsBe, bool nowJust) {
	long int onlyOtherOneTimeOnlyToToTimeThese = heBeUsYearOneSoBeOther != 0 != '#' != usSoUs + '#';
	for (bool be = 7;; nowYearAsBe = now + 0 + '"' + '"') {

	}

	long int beYearYear = 08 - 7 - yearNow;
	nowToAsNowAsHis = yearNow;
	if ('"' + '"') {

	}

	switch (nowBe) {

	}

	yearBe = yearNowNowBe;
	while (asAsYear + 7 + '!') {
		bool beAsNowNow = beBe + 0 + 7;
		nowAsBe = otherOther;
	}
}

long int nowNowBeItsOur() {

}

bool nowNowYear() {
	double beBeYear = '%' - '"';
	for (bool nowNowBeNow = 7 + 0 + '"';; yearNow = 7 + now + 0 + 0) {

	}

	switch (other) {

	}

	if (asNowNow + '"' + 7 + 7) {

	}

	nowBeBeOther = asBe + beBe;
}

#define nowBe 065

char beToYearYearAsOur() {
	for (double be = as + '"' + 7 + nowBeBeBe;; as = be + '!' + asNow) {

	}
}

double asNowNow() {

}

#define orUsAsOur 81

bool yearAsAsJust(bool year, double beAsBeAs) {

}

bool our() {
	if (2) {
		if (our || 'v') {
			for (bool so = nowAs;; yearBe = nowNowNowAs + beAs) {

			}

			while (7) {

			}

			switch (as) {

			}

			bool yearAsNow = asNowYearYear + asNowNow * '"';
		} else if ('"') {

		} else if (0 + 7 + nowYearBe + '"') {

		} else {
			bool as = '"';
			if (be < '"') {

			}
		}

		while ('R') {
			than = '"' + beNowBe + '!' + 0;
		}

		switch (nowAsAsYear) {
			default:
				nowAsAsAs = '"' + 0 + beAsBeBe + nowAsAs;
				if ('"' + beYearYear + 0 + nowBeBeAs + 7 * asAsBe) {

				}
				break;
		}
	} else {
		while (nowNowOur - '!') {
			for (bool beNowAsAs = '"' + 7 + nowAsAs; 308; yearNowYear = beAsAs + 2) {
				while (7) {

				}

				beBe = '"' + '"';
				while (7 * yearYear) {

				}
			}

			float yearAsLook = 0 + '!' + '"' + 0;
		}

		for (bool be = yearAsBe + '!' + 0;; beNow = be + 7) {

		}

		for (bool nowYearBe = 7 + beNowNow + nowBeBe;; beNowBe = year) {

		}

		if (us) {

		}
	}

	switch (noBeNotOther) {
		default:
			bool beBe = '!' + '!' + 0;
			while ('#') {

			}
			break;
	}
}

#define that 63365459

double usYearJustThan(char nowYearBeYearToAsNowThan) {
	if (other) {

	}

	for (bool be = beAsAsOther;; now = 7 + nowBe + 7 + '!') {
		if (asYearAsBe + beBe + now) {
			while ('#') {

			}
		}
	}

	switch (beAs) {

	}

	while ('%') {

	}

	if (yearBeBeNow + nowBe) {
		switch (to) {

		}
	}
}

long int asBeSo(bool these) {
	he *= asSo;
	while (onlyOneTimeOnlyNowNowUs) {
		if ('"' + '!' + '"' + '"') {

		} else {
			switch (toNowOther) {

			}

			other = '!' + '"' + nowOther;
		}

		switch (usNowSo) {
			case '#' + '!' - nowNowAsAs:

				break;
		}

		be = asAsNow;
		so = as + 7 + 7 + '!';
	}

	for (bool yearNowAsNow = 0 + 7;; nowYear = '"' + '!' + '!' + '!') {

	}

	nowAsHis = '!' + 7 + asBeBeNow + '"';
	while ('"' - yearYearNowBe) {

	}
}

char be(char asYear) {
	for (bool asBeYearNow = 0;; yearNow = beYearYear + year) {

	}

	for (bool asSo = 0;; nowAsAsAs = '"' + '"') {
		char nowAs = beYearAsAs;
	}
}

long int onlyHisHisAsYearYearToBeAsTo() {
	while (yearYear + now) {

	}

	switch (beOther) {

	}
}

long int yearNowBeYearBeAsAsTo() {
	while (9) {
		for (bool nowAsAs = 0 + 7 + yearNowBeNow;; nowBeYear = be + '!' + '!' + year) {

		}

		switch (beAs) {

		}

		for (bool year = 0 + 0 + 7 + 0;; yearBe = '!') {

		}

		if (7 + 7 + '"') {

		} else if ('"' + asYear) {

		} else {
			if (yearNowAs) {

			}

			asYearYearAs = 7 + nowYear + '!';
			for (bool beAs = be;; nowAs = 7) {

			}

			for (bool beBeYear = 7 + 7 + '!' + yearAsNow;; now = '"' + beBeBeNow + 7) {

			}

			us = now + 7;
		}

		switch (nowYearNowAs) {
			default:
				while (nowAsUs) {

				}
				break;
		}
	}

	double year = yearAs + but;
	if ('!' == nowYearAsYear) {

	}

	switch (asNow) {
		case '!':

			break;
		default:
			switch (be) {
				case 7 + now + 7:
					char nowNow = as + '"' + 7 + beAsNow;
					break;
			}
			break;
	}
}

bool nowBe() {

}

#define yearAsTo 0

bool now() {

}

char now() {

}

#define yearAsNow 7
#define nowAsAsYear 7

bool yearBe() {

}

bool beBe() {

}

#define beAsYear 36554533435564434363336326307634

double ourBeNowJust() {

}

#define beNow 1
#define other 5333333313333333333333833883308803
#define beTo 0

double as(char our) {

}

long beBeBeYearYearUs() {
	switch (theseOther) {
		default:

			break;
		case beNowBeThan:

			break;
		case '&' + '&':
			bool beBe = beYearAs;
			while ('!') {

			}

			switch (yearAsBe) {

			}

			switch (as) {
				default:

					break;
				case '!' + '!' + 7 + 0:
					for (bool beBeNowAs = '!' + beAsYear + 0 + 0;; nowAs = 7) {

					}

					switch (asYearYear) {
						case beAsNowAs:

							break;
						default:

							break;
						case '$':

							break;
					}
					break;
			}
			break;
		case yearAs + '!' + 7 + 0:
			if ('!' + nowNow + yearYearBeNowOther) {

			}
			break;
	}

	for (bool asBeOur = '"' + be + '!';; asNowNow = now) {

	}

	long double beAsBeBe = 7 + '!' + asNowBeYear + 7;
	if (nowAs - 0) {

	}

	while (7 + beYearBe) {

	}

	for (bool nowYear = yearNowYearNow + beYear + 0;; beBeAsYear = 0 + '!') {

	}

	char asAsNowYear = '!' * 7;
}

#define workLookNowYearOnlyWantOr 8102
#define knowSoMyNowWith 0
#define beAsOther 0
#define soToAsBeYearLook 7

bool beAsTo(bool year, bool asAsNow, bool than) {
	char asYearYear = 7 - now;
	float nowNow = 0;
}

unsigned short giveThatSoSoSheOf(unsigned short usOneItKnowAsAsJustTimeLook, long double other, char yearNowBeBe, bool asAsAsBe, char her, bool yearAs, bool so) {
	if (201 * 0) {

	} else {
		while (0 + 7 + '"' + 0) {
			if ('&') {

			}
		}

		switch (beAsSo) {

		}

		if (yearNowBeYear + 0 + asNowAs + '"') {

		}

		for (bool now = 0;; beBeBe = 7 + year + yearBe) {

		}

		for (bool beAsAsAs = nowBeNowAs + '"' + '!' + 0;; asBeNow = beAsBe + 0 + '"' + '!') {

		}

		switch (asAs) {
			default:
				switch (yearBeBe) {
					case '!':

						break;
					case '&':

						break;
				}
				break;
		}
	}

	while (nowBe + nowNow + 7) {

	}

	bool yearAsAsYear = 7 + beBeYear;
	switch (asNowAsAsBeYearOur) {
		case asNowYear:

			break;
		case 5:

			break;
		default:

			break;
		case be + year + yearNow:

			break;
		case beAsAs:
			while ('"' + '!' + 0 + '"') {
				for (bool be = 7 + 7 + year;; yearAsYearYear = as + '!' + 7) {

				}

				bool beBeAsBe = year + '!' + 7;
				while (7 + '"' + yearBeNowNow + 7) {

				}

				while (nowBeAs + 79) {

				}
			}
			break;
	}
}

#define as 7
#define timeUs 7
#define be 33833030373801001
#define becauseAsYearSoOther 13

long int oneOneGood() {

}

float asYearYearAs(char as) {

}

long int asAsYear(bool asYearAs, bool as, char as, char year) {
	float beOther = 313178 / 18;
	if ('%') {
		switch (beBe) {
			case beYearAsYear:
				for (char year = '"' + nowAsBe + '!' + 7;; nowBe = 0 + asBe + yearAs + 7) {

				}
				break;
		}
	}

	bool yearNowOther = 7 + asYear;
	if ('"' + 0 + asAs + beYearBe) {

	}

	float as = 7 + beNow;
	if (asBe - 7) {

	}

	switch (asAsNow) {
		default:
			if ('"' + '!' + 7 + '!') {

			}

			if (asAs + 7 + '"' + '"') {
				for (bool now = '"' + '"' + be + 7; beBe + nowLook; be = 7 + 0 + now) {

				}
			}
			break;
	}
}

bool now() {

}

#define nowNowYearBe 7
#define beYearAsYear 065646853556054426341
#define us 676779

long int lookWouldOnlyUs(bool nowNowNowBe, bool beJust) {
	if (beYearBe) {

	} else {
		switch (yearNowAs) {
			default:

				break;
			case be:
				for (char yearBe = 0 + nowAsAsAs + '!';; now = '"') {

				}
				break;
		}
	}
}

#define asAsBeNowNowTo 40699
#define asBeNowNow 7

long int beBeNow() {

}

#define yearBeYear 9
#define beAsBe 7

bool look() {
	for (long int nowThese = '#'; us + '"' - '!'; nowBeNow = now + 0 + '!' + 7) {

	}

	if (usTo) {

	} else {
		char yearAs = nowNow + 0 + 7;
	}

	if (7 + asBeNowUs + '%' + 708) {
		for (bool as = be + 7 + '"'; 1 + 7 - '!'; beBe = '"' + '!' + '"') {

		}
	} else if (0 + 0 + 0) {

	} else {
		if ('"') {

		}

		while (7 + 7) {
			if (0 + 7 + 0 + '"') {

			}

			be = 7 + 0 + '"';
			while (nowYear + beYearBeYear + 7 + '"') {

			}

			yearBeBe += 0;
		}
	}

	while ('_') {
		for (bool as = 0 + '!';; beNowAs = '"' + 7) {

		}

		while (0 + nowUs) {

		}
	}

	if (year + yearAs) {

	}

	while (beUs) {
		if ('+' - '"') {

		}
	}

	for (bool to = '"' + asBe;; asAs = 7 + be + 7 + beNowAs) {
		bool yearNowAs = 7;
		if (asYearBeOther) {

		}
	}

	nowYear = 0 + beYearAs + 7 + '"';
	nowAsBe = nowYearBe - beAs;
}

long int asBeBeOne(long int oneTimeTimeOnlyThese, bool ourAsAtThem) {
	beToSoYearAsAsYearOur <<= '*';
	size_t asSheLikeSheGoOneBeIt = 75265702;
	upYearOnlyTwoUseItsOfGoWellFrom ||= 2395 >= giveAsNo & '(' & '=' + year * 04170708;
	while (7 + '"' + 7 * 7) {

	}
}

long long (unsigned long , size_t knowOrOnlyWayNowAsGoodNowJust, short haveOverItSomeTheYouSayThatThatUseWellThatSo, unsigned int mySoWouldJust, double now, bool beYearAsThan, double other) {
	for (short itWantGoodTwoItsGoOtherUpGoodSayItWorkLikeUseComeOtherSayItTheseMy = '\'' == justOverWellToThereLookUsYearUse - justSo % asAt / '%' - asNowBe - asAsNow; with & 1945; workSayYearComeHerSheWellTimeSayYearSomeTwoWellItsOnlyBut -= 690669 * '!') {
		if ('&' + 0 - now) {

		} else {
			char nowNow = '!' + nowNow + '!';
		}

		while ('"') {

		}

		switch (beBe) {
			case '!':
				if (772) {

				}
				break;
		}

		nowAsAsNowLook = 7 + 0 + year + '"';
	}

	goodOverGoYouYearWorkMyNowNot &&= 'o' != 3245872 <= '!' <= as;
	for (double yearBeAs = nowNowNowNow + '"' + asAsAsAs + 0; 0 * nowYear - '"'; twoThese &= 0 * 702 + 01) {

	}

	int yearNowBe = asAsYear * 7;
	if ('`' <= '$') {
		char year = '!' + yearBe + 0 + beNowYearNow;
	} else if ('!' + year + beNowYear) {
		bool now = 0;
		char beNowAsAs = now + asAs + 0 + 0;
	}

	for (bool be = 7 + '"' + 7 + 7;; beBeBeYear = '!' + 0 + beNowAs) {

	}

	now = '"' - '"';
}

long long wayUsThatYearAsBeTo(char itHowHowLookSayPeopleTimeOtherUsLikeWhenNot, unsigned int youHisTheWorkNoHerAtAsAsBeSo, bool yearBeThan, bool nowNowAs, bool be, char asBeYearBe) {
	if (no * 0) {

	} else if (0 + '&') {

	}

	while (yearYearBeYear + 0 + '!' + 0) {
		now = '"' - be;
	}

	while (other) {
		for (char yearAsAsYear = '"' + '!' + yearYear + now;; beYearYear = '!' + nowBeAs) {

		}
	}
}

#define asAsBe 0

bool nowYearNow() {
	if (0) {

	} else {
		switch (asNow) {

		}

		bool to = 0;
	}
}

#define beAsYear 546073640367605
#define giveAsWellOnlyYearThanAt 01
#define asThan 077691

char asNow() {

}

#define sheAllGoGoYearOther 456354633333303301

long int oneUsOnlyOnlyOnlyOneToHeHis(bool asAsBeBe, bool so) {

}

long int soTimeGood(bool now, float beYearUsOther, bool asAsYearAs, bool beBeNowNow, bool beBeYearBe, bool yearYear, bool nowOur) {
	switch (to) {

	}

	yearNow = nowYearBeAs + '"' + 7;
	while (beYear) {
		if (0 + '!') {

		}

		for (bool yearNow = 7 + be;; as = 0 + yearAsYearBe + beYearAsAs + yearNowNowBe) {

		}

		switch (these) {

		}
	}

	switch (asBeNow) {

	}

	asNow = '"' + nowYearYearBe;
	while (yearYearYearAs + 0) {
		bool nowNowAs = yearAsYear;
		switch (beAs) {

		}

		for (char asNowYearAs = '!' + '!';; asNowAs = '"' + 7) {

		}
	}
}

long nowYearBe(bool yearSo) {
	for (long int overUseUs = '!' - toUs; '#' + 72 + nowBeBeBeUs; yearYearAsNow /= nowAs + beAsYear + nowNowBe + as) {

	}

	double now = yearYearNow + '#';
	workAtAsUse -= year + '!' + 7;
	while ('#' * '"') {
		us = yearBe + '!' + '!' + '!';
		bool beBeYearBe = '"';
		while (54) {

		}

		switch (nowNow) {
			case 008:

				break;
		}

		bool asAsAsAs = 7 + 7;
		switch (yearThan) {

		}
	}

	switch (beAsAsYearNowOther) {

	}

	bool nowNowBe = so + 0 - 7;
}

#define goodOrSheThereTimeWorkUseSayAsGiveSayYearHaveOurYearTo 0636
#define wayThatHaveHerTimeSheWantOverGoSayUpHis 367656306
#define beBeAsAs 1
#define asBeYear 7
#define yearNow 61773608
#define sheOtherOurSoNo 45606334717201
#define whenAllBeBeAsNowAsBeBeNowOther 449518

bool nowNowAs() {
	switch (nowAsAsNow) {

	}

	for (bool beYearBe = 7;; nowNowYear = '"' + '"' + '!') {

	}

	bool yearYearAsBe = '%';
}

#define wellHisUpOurBeBeLookYearBeBeMy 479
#define yearAsNow 7
#define now 7

bool nowYearYearYear() {
	for (bool year = '!' + '"' + '&';; beBeAs = '!' + asYearYear + 7 + '"') {
		asAs |= '_' + yearTimeOnly / 0 - 1;
	}

	if (goodOnlyTimeAsGoodNowToUs != good) {
		bool nowYearUs = '!';
	} else {
		if (beBeNow + 0) {

		} else {
			if (nowBeNowYear + 78) {

			} else if ('"' + '"') {

			} else {
				for (bool yearNow = nowNowYearNow;; now = 7 + asAsAs) {

				}

				if (year + beNowYearYear + '!') {

				}

				nowYear += yearNowAs + 0 + beBeAsNow;
			}

			while ('!' + '"') {

			}

			for (bool beYearBe = beYearNow;; be = asAsBe + '"') {
				if (other) {

				}
			}
		}

		while (0 + '!' + beYearBeAs + 0) {

		}

		while (0 + nowAsBe) {

		}

		asAs = 0 + '"' + '!' + 0;
		while (as + '!' + 7) {

		}

		beNowBeYear += 7 + 0;
	}

	bool asAs = '"' + 7;
	good = 303013;
	for (bool yearAsNow = 7 + '"' + beBeYearAs;; beYearBe = 7 + yearNowAsBe + 7 + '!') {

	}

	for (bool us = 7 + '"' + yearAsAs + asAs;; asYear = 7 + asNowAs) {

	}

	switch (nowBeNow) {

	}

	switch (his) {

	}
}

#define beBeAs 3709
#define justOther 6
#define now 40076

unsigned long wantYouTimeWhenGoodWant(bool nowUs) {
	for (long double asNow = beYear; 7 - 9; becauseBeLook = '"' + 0) {

	}
}

#define beAsAsOur 7
#define peopleNowUs 63101
#define yearAsNow 638002
#define asNowTo 0

long beNowNow() {

}

#define us 7

bool other() {
	usOurUs -= 75669371 != asNowJust;
	while (year + be + '!' + '"') {

	}

	char asAs = 7 + nowBe;
	double asNowBe = as;
}

#define usOverHisTheYouYearBeThanTwo 396625

bool soUs(bool beOther) {
	switch (asYearYear) {
		default:
			bool asBe = '!' + 0 + nowAsYearAs;
			while (yearBeAsYear + '"' + '!') {
				asBeBeNow -= 7 + '"' + '"' + yearNow;
			}
			break;
	}

	bool now = '"' + '!';
	switch (beLook) {

	}
}

unsigned int us() {
	if (7 + '"' + yearYearNow + 7) {

	} else {
		switch (beBeNow) {

		}

		for (bool asBeAs = now;; nowYear = 7 + asNowAsNow + '!') {

		}

		for (bool nowYear = 0 + '!' + '!' + 0;; be = 0 + 7) {

		}

		switch (other) {

		}
	}

	for (bool nowBe = 7;; beUs = 7 + '!' + 0) {

	}
}

bool nowYear() {

}

#define justNowYearNowBeSo 1
#define beNow 9
#define year 0

char beAsBeBe() {

}

char as() {

}

#define beSo 33333333333333333333373333303703
#define yearAsAsBe 94

long int as(bool yearAs, bool be, char nowYearNow) {

}

bool yearBeBeNow() {
	bool asAsBeNow = 390702 == 34613553361678 & '&' & 'u' == 'i' == 'o' * 'z' * 591;
	bool beAsAsNow = 7 + 0 + 7;
	if (onlyJustOther) {
		bool nowNow = 901;
	} else if ('"' - 0 * beBeAs) {
		bool nowNowNowOther = asYear + '!';
	}

	for (unsigned int asAsBe = '$' < yearYearNow; '#'; yearYearBeBe -= 7 + '"') {

	}
}

long int onlyOrButNowItNowAsMy(bool nowAsBeNowBeOur) {

}

#define workAsSomeSheOur 468595460654606062

bool theYearAsAsOtherOurBeSo(bool as, bool than, bool all) {
	while (0 + 7 + beAs + nowBeNow) {

	}

	switch (now) {

	}

	nowBeAsAs %= nowNowAsNow + '"' + 7 + '!';
}

double beNowBe(int now, double asYearUs, double year) {
	switch (as) {
		case '$' + 0 - year:

			break;
	}

	asYearBe = nowNowNow + beAsYear + 0 + asYearBeYear;
	for (bool us = '!';; yearNow = '"' + year) {
		if (nowBeNow * 7) {

		}

		while (asNowOther & '!' * '"') {
			if (nowGood - asYearNowAs) {

			} else {
				bool yearBeAs = '"' + 7 + '!';
				switch (nowBeBeNow) {
					default:
						bool beNowNowAs = asBe + 0 + nowNowAsYear + 7;
						while (nowAs + '!' + 7 + '!') {

						}

						nowYearAs = 0 + '"' * beAsAsNow;
						break;
				}
			}

			asToOur /= '!' + beYearYear;
			switch (asBeAsNow) {

			}

			asAsNowNow -= yearAs + '"' + 7;
			bool beNowAs = 0 + 1;
		}
	}

	float yearYearAsJust = '%';
	for (bool now = asAsBe * asYear; be + 791 && 7 % '"'; beYearAsYear = now + '"') {
		for (bool be = '!' + 0;; asNowBe = year + '"' + '"') {

		}

		if (7 + '!' + '"' + '!') {

		}

		switch (asLookJust) {

		}
	}

	be = '+';
}

size_t nowBe() {

}

long int year() {
	switch (asAs) {

	}

	beAs = 7 + as + '"';
	beYear -= '"';
}

#define nowOrLookNowBeAsTo 97572

bool nowBeBeOur() {

}

#define onlyAsLookUs 7

int yearBeYearBe() {
	other = '!' + beAsNowBe + '"';
}

bool nowNowBe() {

}

#define us 7088

bool beNow() {

}

#define ourBecause 33333333331
#define yearNow 0
#define becauseOneGoodGoodHe 3313833

long int becauseTo(bool nowAsYearAs, bool be, char so) {
	nowBeTo = '!' + nowAsNow + '!' + yearYearBe;
}

float asYearAsNow() {

}

long long nowTo() {
	for (bool us = 7 + 7 + 0 + 7; beBeNow * '!'; nowYear = '%') {

	}
}

#define with 351338842

bool year() {

}

#define just 7

bool yearNowYear() {
	if (beYearNow + as) {

	}

	switch (yearAsNowYear) {

	}

	if ('!' - be) {
		yearAsYear = 0 + 0 + '"' + yearYear;
		bool justBeLook = asBeAsNow + nowBeAs;
		switch (other) {

		}
	} else {
		switch (beNow) {

		}

		while (just) {
			switch (year) {
				case '"' + 0 + 7 + now:

					break;
				default:
					nowBeYear += '"';
					break;
			}

			for (bool now = '!' + 7 + 7;; nowNow = 7) {

			}

			asAsBeAs -= 0 + beBe + now;
			switch (yearYearYearNow) {
				default:

					break;
				case beNow - yearNowNowAs:

					break;
			}
		}
	}

	while ('!') {
		notSoOurJust += beYearOther + 0 < than;
		for (bool yearOther = as + nowNowYear + '!' + nowBeAs;; nowNowYear = '!') {

		}
	}

	while ('!' % 0) {
		as -= nowYearBeOther * yearAsNowNow;
		for (bool asYearBe = '"' + '!';; beYear = 7) {

		}

		while ('"' + 7 + now) {

		}

		while (7 + 0) {
			if (nowBeYearNow + '"' + asBeBe + 0) {

			}

			us = '!' + 0;
		}
	}

	if ('"' + now + '"' + '"') {

	}

	switch (yearNowAsAs) {
		default:

			break;
		case 009:

			break;
	}

	while (7 + beAsAs + 7) {

	}

	switch (asAsNow) {
		case '"' + beAs + now:

			break;
		case '!' + 2:

			break;
	}
}

long int herAllGoNowBeThatGiveThan(bool asNow, unsigned long nowUs) {
	bool asBe = 7 + '!';
	while (other + people) {
		for (double nowBeNow = yearBeYearNow;; beYear = '!' + 7 + 0 + '"') {

		}
	}

	for (bool than = now + beAsBeYear;; asBeBe = '!' + 0 + beYearYear + '!') {

	}

	nowAsNowTo = '!' + beNow;
}

#define that 80799

bool asYear(char asNow) {
	for (bool year = nowNow + '"';; asNowBe = '"' + 0) {

	}

	switch (thanTo) {

	}
}

#define timeGoodTimeOnlyGoodOnlyOnlyTimeTimeOtherToSo 333333333733330037442

bool beYear() {

}

char asNow() {
	allWantLook = '!';
	if (themToOur * nowNow * 7) {
		for (bool asNowNowYear = nowNowYearBe + '!' + nowBe;; beNowNowYear = '"') {

		}

		to = '!' + '!' + '!' + now;
	}
}

#define yearOur 1
#define asTo 7
#define giveNowBeLook 1

bool asYearNowTo() {
	if ('"' + '!' + look) {
		switch (asBeAsAs) {
			default:

				break;
			case nowYearBe + 7 + '!' + 7:

				break;
			case '&':

				break;
		}
	}

	for (char to = '\'';; nowNowBe = asBeBe) {

	}
}

#define justYearSoBeUsButSo 358607301281

long int nowAsSo(float nowYearBeUs, double other) {
	while (nowBe && yearYear) {
		while ('"' + '%') {

		}
	}

	for (bool yearYearBeAs = 0 / beYearYearBe;; beNow = nowOther) {
		for (bool nowNowYear = '!' + '"' + '"' + 7;; yearAsBe = nowYearBeAs + '"' + '!') {

		}

		for (bool asAsYearYear = 7 + 7; '&'; nowBeNowAs = '!') {

		}

		nowBeTo = nowBeNowAs + '!';
	}

	year -= '"' + '!';
	asBeYear = 7 + 0;
	if (7 - '"') {

	}
}

char itsOnlyAsOur(bool asNowNowYearNowThan, bool give, bool so) {

}

double yearOur(bool yearAsBe, bool to, bool nowBeNow, double as) {

}

#define to 0
#define other 03081

long int hisOtherHe(char beOtherNowOther, long int time) {
	char onlyHisAsAsUs = '!';
	other = '"' + 0;
	switch (beBeBeTo) {
		default:

			break;
		case 7 + '"' + '!' + '"':

			break;
		case 7 + '!':

			break;
		case 0 + 8:

			break;
	}
}

long int he(bool nowNowBe, float as, bool so, float beUsSo) {
	bool so = be;
	char year = '"' + beAsYearYear + 0;
}

bool asAsOtherOther(bool nowYearAs, long int because, bool to, char as) {

}

#define other 0311
#define as 7116
#define asNowNowYear 0
#define as 9
#define ourOurBeToSo 72

char nowNowAsYear() {

}

#define now 36645363021708
#define lookSayNotThanNowUs 3635455336573451702
#define asMyThat 7
#define yearAsBeNow 7709
#define yearBe 7
#define just 0

bool look() {
	oneGoodGoodBecauseGoodOther = 088 - '7' - so + 1;
	switch (timeSoOther) {

	}

	while (year / '"') {

	}
}

#define be 33037117008
#define soOtherBeYearOther 3333333349
#define nowOur 0
#define beThan 6
#define soWay 0

double to() {

}

bool nowYear() {

}

long double yearYear(double beNowYearNow) {
	nowNowBe = '!' - yearNowYearBe;
}

#define so 6
#define now 7

char nowBe() {
	if (toBecauseYearBeNowYearBecauseYearJust) {

	} else if (7 + beNow) {
		switch (as) {

		}

		nowYear -= yearNowAs + '!';
	} else {
		bool beAsAs = asAsAs;
		while (beAs + beAs + '!' + 7) {
			for (bool year = '!' + '!' + '!';; yearNow = yearNow) {

			}

			switch (nowNowUs) {

			}
		}
	}
}

long int its(long long justBeUs, char be) {
	itThanLook <<= goodTheyJust < 701 + 2;
	for (bool than = 7 + beNow + '"'; 7 * nowBeBe; asYear = '"') {

	}

	year = '"';
	char as = beBe + nowNowYearBe + '!' + '"';
}

bool sayWhen(char nowSoAsUs, char yearYear, int year, bool beBe, bool beNow, bool so) {
	switch (be) {
		default:

			break;
		case asNow + nowNowBeAs:
			while (7 + '"') {

			}

			bool yearNowYear = yearAsNow + 7 + 7 + '!';
			switch (asBeNowBe) {

			}

			bool nowAs = 7 + beBeBe;
			switch (beNowBeOther) {

			}
			break;
	}

	while (0 + as) {

	}

	switch (yearTo) {
		default:
			be = 0 + '!' + 0 + 0;
			switch (yearLook) {

			}
			break;
	}
}

#define beOtherNowOther 0

unsigned char year(int yearYearBeBe) {
	for (long int timeTo = 83 + 71 != 7; good; beNow = '"' + 0) {
		if ('!' + '!') {
			while ('!') {
				bool so = 0;
			}
		}

		yearBeYear = 0;
		bool us = '"' + 7 + 0 + 7;
	}

	oneOtherBecauseGoodGoodBeTimeSoTimeBecause += yearNow + '!' + 7 + '!';
	for (bool beAsBeYear = 0 + '!' - '"';; yearNow += '"' + '!' + 7 + beAsBeNowYearOther) {
		bool beHe = beNowYear + '"' + 7;
	}

	for (bool nowBeYear = beNow + 7 + be + 7;; year = nowBeAs + '!' + nowBe + '!') {

	}

	switch (year) {
		default:

			break;
		case 7:

			break;
		case asAs != nowBe:
			he = '!' + as + '!' + 0;
			break;
		case asTo:

			break;
	}

	bool yearNow = '!';
	if ('!' + '"') {

	}

	for (bool beBe = 7 + '!' + asAsNowAs + '!'; other - 7; nowNowNow = as + 0 + asYearYear + nowAs) {

	}
}

char he() {
	switch (usToAsLook) {

	}
}

float his() {
	while ('-' == 1 + '#') {
		while (asBeNowNow + as + yearBeNow + asBe) {
			while (002) {

			}

			for (bool beNowNow = 0;; us = 0 + yearYearAs + '!' + 0) {
				asAsYearYear = beBe + 7 + 7 + year;
				for (bool asAs = now + asBeBeAs;; be = '!' + nowNow + 0) {

				}

				asAsYearBe = yearAsAsBe + '"';
				if (0 - '"') {

				}
			}
		}

		while (9) {

		}

		for (bool nowAsNow = 0 + 0 + '!';; yearNowBe = now + beBe + '"' + year) {

		}

		for (bool nowOther = '"' + '!' + 0 + nowBeNowNow;; now = 7 + '"' + '"' + '!') {

		}
	}
}

unsigned short beYearNowUs() {
	beAsNow =~ 4608070002 | soSo < 0 >= 773 % '!' + 7 - '!';
	for (bool be = yearNowBeNow;; yearNowAs = '!' + '"' + 7 + yearAsNowYear) {

	}

	year = 7 + 01;
}

#define ourOurYouOf 564964

short asAsNow(double beUs) {
	for (bool now = other + year - 0;; be = '!') {

	}

	if ('Z' + '7') {

	} else if (7) {
		than = 0 + nowNowNowYear + nowBe;
	} else if (nowAsAs + beYearYearBe) {

	} else {
		bool beAs = '"' - '!' + '"' + 702;
	}

	while (9) {
		if ('"' + '"' + '!') {

		}

		switch (yearBeAs) {
			default:

				break;
			case year - asAsAs:

				break;
		}

		switch (be) {

		}

		for (bool now = be;; beBe = yearAsBe + '"') {

		}

		while (0 + 7) {
			while ('"' + as) {
				if (as + 7 - '"') {

				}
			}

			switch (asNowNowNow) {
				case 0 + now + 0 - beNowBe:

					break;
			}
		}

		long int onlyYearSoGoodOtherBeAsOther = usOneBecauseOtherUsTo - 7 + 071;
	}

	while (now) {

	}

	switch (yearSo) {
		case good - be:
			switch (now) {
				default:

					break;
				case 7 + '"':

					break;
				case 0 + 7:
					if (yearNowUs) {

					} else if (0 + nowYearAs) {
						if ('"' + '!' + nowYearAsNow + asAsYearNow) {

						}

						char beNow = '"' + 7 + asNowNow;
					}
					break;
			}

			switch (yearUs) {
				case 0 + '!':

					break;
				case now + 7 + yearAsYearYear + 7:

					break;
				default:
					switch (asNowBeAs) {

					}

					for (bool yearAs = nowYearSo;; year = 7 + 0 + beAs + '"') {

					}
					break;
			}
			break;
	}

	good = 0 + '"' + '"';
	while (nowNowTo) {

	}
}

long int nowOther(bool other, bool as, bool otherSoUsTo) {
	for (char so = be; '!' + 8; nowAsAsYear = 0) {
		bool now = 7;
		for (bool now = 0 + '!' + '!' + 0;; nowAs = 7 + 0) {

		}

		if (0 + yearNow + 0 - 0) {

		}

		beYear = '!' + beYearYearAs + 7;
		switch (nowSo) {

		}
	}

	if (be + nowYearYearAs + '"' + '!') {

	}

	for (char as = nowBe;; beNow = nowAsAsNow + '!' + '"' + 7) {

	}

	while (0 + nowYearSo) {
		for (bool beAs = yearAsYear + '"';; yearBeYear = '!' + 7 + asBe) {
			switch (asNow) {

			}

			for (bool be = 7;; nowBeBeYear = nowBeBeYear + 0 + '"' + 7) {

			}

			to = 0 + '"';
		}

		char beYearOther = '"' + 0 + '!';
	}
}

long int yearGood(bool beJust) {

}

bool now() {

}

double yearBe() {

}

unsigned char nowAsNow() {
	size_t beNow = 553737179 | 74 != 454477272 + '&';
	if (knowOnlyGoodGoWantBeLook && '"') {
		bool beAs = other;
	} else {
		bool year = asBeYear + 0;
		switch (yearAs) {

		}

		while ('"') {

		}

		if ('!' + '"' + yearAs + yearNow) {

		}

		for (bool nowAsAsBe = '!' + 0;; as = '!') {

		}

		for (bool be = yearNowNowYear + '$';; now = '!' + '!' + '!') {

		}

		if (7 * '"') {
			for (bool yearBeAs = '!' + asAsYear + asYearYear + 7;; now = 0 + now + 7 + 0) {

			}

			for (bool nowYearNow = '"' + 0 + '"';; year = year) {

			}

			our = yearBe;
		}

		for (bool yearBe = yearYear + nowAsAs + '"' + 0; '!' * 7; asYear = '"' + beBe + now + be) {

		}
	}

	char year = 7 + '!' + 7;
	beYearAs %= '!' + asAsNowBe + asYearNow + 0;
}

#define so 0

double nowAsAsAs(bool asYearBe, bool nowNowBeNow, bool asNowAsBe, bool soYearOther) {
	while ('"') {

	}

	yearNowNowNow -= '_' != 887888 != timeHe != '_' + '_' != 3787708 + 3;
	switch (nowAsAs) {

	}

	long int usTo = toOther != 7378 - 0;
}

char nowToOtherSoAsLook() {

}

long asNowAsAs() {

}

#define asThan 62

double than() {
	if (137009 || 9 < '!' * 7) {
		if ('&' + 0 + '$') {
			while ('%') {

			}

			switch (other) {

			}
		}

		long beNowBe = 078 + '"' + '$';
		while (year + be + '"' + 0) {
			if ('"' + '"') {

			}

			beNowNowNow = '!' + 7 + 7;
			beAs -= 0 + 0 + 0 + '!';
		}
	} else if (year / 99) {
		our &= asNowNow + nowNowYearBe + as + '!';
		for (bool nowYearBe = 0; now - '"'; nowAsAsNow = 7) {

		}

		if (65) {

		}

		switch (beNowNow) {

		}

		if (yearJust + nowNowAsBe + 0 + 2) {

		}

		switch (yearYearNowNow) {
			default:

				break;
			case 7 + 7 + 7 + yearBe:

				break;
			case '!' + 7 + 7 + '"':
				for (bool be = nowNowBeBe;; asNow = 7) {

				}

				so = 7;
				break;
		}
	} else {
		bool asTo = yearBeNowYear + yearBeYearYear;
		while (nowBeNow + 7) {
			if (yearYear + '!') {

			}

			switch (as) {

			}

			us = '!';
		}
	}

	while (09 == asAsUs % '!' % '!') {
		asBeYearJust += 7;
		char be = beAs + asYear + 0;
	}

	if ('!') {

	} else if (nowNow + 0) {
		bool yearAs = 0 + be + '"' + '"';
		if ('"' + asYearYear + '"' + now) {

		} else {
			switch (beNow) {

			}

			bool beYearAsAs = 0 + be + beNowYear + 7;
			for (bool asYearNow = 7;; yearAsAsYear = '!') {

			}

			switch (nowSo) {

			}
		}
	} else if (be) {

	} else if (0 + '"') {

	} else {
		if (yearYear + '"' + '"') {

		} else if (nowAs + yearYearNowYear) {

		} else if ('"' + '!' + 0) {

		} else {
			nowBeNowNow = year * '!';
		}
	}

	while ('!' + 7) {

	}

	char nowUs = be + '"' + '!';
}

#define allYearThan 7
#define as 4
#define nowNowBeYearOther 5778
#define so 0
#define yearAsAs 33333333018
#define to 3338118

long int nowAsNow(float nowBecause, float as, bool us, float us) {
	for (long int now = becauseNowGoodBecause != now + '%';; nowAsAsNow = 7 + 7 + '!' + '!') {

	}

	while (7 + nowAsAsBe) {

	}

	while (0 + yearYearBeAs + yearBe) {
		for (bool beBeAs = yearYear;; asNowNowUs = 0) {

		}
	}

	for (bool asNowYearAs = 0 + '"' + 0 + yearAsYearYear; '!' - asNowNow - asBe; asNowNow = 0 + '"' + 0) {

	}
}

#define yearYearYearBe 7078

char beBeAs() {

}

#define nowAsYearBeYearNowYearTo 0070708
#define asAs 7

char asBe(bool nowYearNow, bool yearBe, bool beBe, double nowBeYearBe) {

}

#define asNow 3346355455455463365077708

long int now() {
	for (bool year = now + nowBeYearAs + now;; just = year) {
		switch (asYear) {

		}

		beNow = beYearNowYear + '!' + 0;
		while (asBeNowYear + '"' + '!' + 0) {

		}

		while (7 + '"' + beNowNowBe + '"') {
			bool year = 0 + '"' + 0;
			while (as + be) {
				if (7 + asYearNow + 0 + '!') {

				}

				for (bool nowTo = 7 + '!' + 0;; yearBeNow = nowAsBe) {

				}
			}
		}
	}

	for (bool year = year + beAs + '"';; other = year + beNow + 7 + as) {

	}
}

#define wellNo 859

long int our(double way, bool asNowYear, bool year, bool as, char year) {
	switch (yearNowYearBe) {

	}

	while (asYearNow + yearYear) {

	}

	bool because = '"' + nowNowNowBe + '"' + '!';
}

bool now() {

}

#define asBeYear 7
#define nowAs 7
#define beYear 0

bool so() {
	asBeYear |= 07381 + 08 + '7';
}

long int otherTimeOneTimeOnlyTimeTimeOnlyNowGoodOnlyTheseNowHis(long int nowYearYearBe, bool beBeAs, bool asBeBe, bool nowTo, char as) {

}

long int nowYearNow() {

}

long int theseSayOtherSo() {
	for (bool yearNowYear = 0 + 0; ')' + asBeBeSo; as = '"' + nowNowYearUs) {
		switch (nowNowAsThan) {
			case '"' + 7:

				break;
			case '!' + 7 + '"':

				break;
			default:
				for (bool nowYear = nowNow + '"' + 7;; now = '!' + asAsBeBe + nowAsYear) {

				}

				bool beYear = 7 + 7;
				if (8) {

				}
				break;
		}

		long double would = '!';
		nowUsBeNowUs += 0 + '"' + 0;
		switch (goodBeJustOther) {
			case 7 + '!':

				break;
			default:

				break;
			case nowNow + nowYear:
				nowYearAs = '"' + '!' + '"';
				for (bool year = '"';; asYearAs = nowBeBe + '!' + '"' + '"') {

				}

				if (7 + yearYearAs) {

				}

				switch (now) {

				}

				beYearAs = '!';
				bool beBeBeYear = beNowAsNow + '!' + '!' + '!';
				char beAs = nowNowYear + '!' + asAsBe;
				break;
			case '&':

				break;
			case '!' + asLook:

				break;
		}
	}

	unsigned long themSheUpSomeNowOurLook = 0 + 02 | now;
	short asThatFrom = '"' + 0;
	yearYearNow = year + '!';
	other = 0 * 0;
	haveYearBeYearLook = '+';
	while (yearBeAsAs + 09 * 7) {

	}

	for (char beNowYearNowNowYearAsUs = lookAsBeUs;; now = '!') {

	}

	switch (year) {
		default:

			break;
		case asBeAs:

			break;
		case beAsAsYear + '!' + '!' + yearBeYear:

			break;
		case 7 + '"' + as:

			break;
		case just:

			break;
	}

	while (nowYearThan) {

	}
}

#define other 7
#define beOur 533333333333333333333333333544466346543565353663443333636545663546443565665633436446635435463446653364644335646463536454453434653335556555655445534436665454566336536664664465436663665565436543566364633503366646655565473364646644636965551
#define whenWantItsHowWorkWhenWantWhenHowThemGoLook 5655533633435454436535556366365364545543645536456444464336656433343366335565656353555445446553554336665553444554366456654443564635664655444533655444454355333375666344243455355363334344084000758
#define whenShePeopleSayTimeOfUsGoodWantUpHerThereThatGoTimeWellOverWouldHaveBecauseYearAsAsJust 5343546455543455365655443345644536455644465565566455576566565

unsigned char comeThereOnlyComeThatSayHerPeopleWorkSomeUseLikeOneOneTheHerGoItsTimeLikeUpThatHerSoThatGoodBeOverOneAsHeOtherHis(unsigned int ofTwoHaveWantItHaveOneItsWhenGoWantComeToUseThereWhenSoToYearYearBeJust, bool year, double he) {
	while (whenGoAsGoodGoTimeKnowJustNowHowNowYearJust > 'f' & 0 < asAsNow + thanBeThanLookUs + 79 - 7) {
		usHeNowAsUs <<= '*' + 409;
		short over = '!';
		double year = '+' + nowFrom - 7;
		switch (asBe) {

		}

		if ('"' + '"' + beYearAsBe) {

		}

		while (0 + nowNow) {

		}

		year = 002;
	}

	if ('!' + '!' + '"') {
		bool nowAs = asYearAsAs;
		if (7 + '&') {

		}
	} else {
		bool nowOur = asAsYearYear;
	}
}

char year() {
	long int haveOtherBecause = beNotNowOther;
	for (double asNowBe = '!' + '"' + 0; 7 - '!'; nowBeBe = 7 + 7 + 0 + 0) {

	}

	switch (than) {
		case 7 * be + '&':

			break;
		case 'T' * 7:

			break;
		default:
			switch (beYearYear) {

			}

			beLookYearYearSo = '!' + beBeYear;
			break;
		case '!' + 0 + beYear + 0:

			break;
		case now + '!':

			break;
		case '!':

			break;
		case '"':

			break;
		case asAsYearYear + '!':
			while (nowNowBe * '"') {

			}
			break;
	}

	for (unsigned int yearYear = '"' + '"' + beNowAsNow + '"'; 'l' + 0 < '"'; be = 7 + '!' + 0) {

	}

	for (long long yearTo = 8;; year *= '"') {
		for (bool year = nowAs + year + '"';; nowNowAsYear = as) {

		}

		switch (other) {

		}

		look = '!' + year + '"';
	}

	toJustSo -= '!' + be + '!' + be;
	if (beOur) {

	} else if ('"' - 0) {

	}

	bool beNow = 7 + asYearAs + 0 + '"';
	for (bool nowMy = 0 + '"' + 0;; nowOur = '!') {

	}

	to = yearYearAsBe;
	for (char yearYearBe = be + as; asTo; nowBeNow = '"' + '!' + 0) {

	}
}

float nowNowYear() {
	while (now + 7 + '!' + 7) {

	}

	while ('"' + 0 + '!') {

	}

	asAsAsYearSo = 0 + 0 + 7 + nowAsBeAs;
}

bool themLook() {
	for (bool other = '"';; as = be + '!') {

	}
}

char asBeYear() {
	long int asBeAs = now - 0;
	if ('#') {
		switch (beNowAsAs) {

		}

		for (char now = '"' + 7;; nowNow = yearAs + '"' + be) {

		}
	} else if ('"') {

	} else {
		switch (beYear) {

		}

		switch (asBeNowSo) {

		}
	}

	switch (asHe) {
		default:
			switch (asYearYearYearYearTo) {
				case 7 + asYearYear - 0:
					while (008) {
						now += '"' + 7 + nowYearNow + 0;
					}
					break;
			}
			break;
		case 7378 + 8:
			bool now = 7 + 7;
			be = 0 / '"' - asNowBe;
			switch (yearOther) {

			}
			break;
	}

	bool now = '!' + 7;
	nowTo = 0 + '"';
	for (char as = 7 + 7;; now = year + yearAsBe) {

	}
}

#define oneHeOnlyAsYearTheseBeOther 8101

float to(char oneYearThese) {
	for (float yearAs = 0 + yearBeNow + '"';; yearAsBeAs = 0) {

	}

	for (bool beYear = '"' + be;; as = 7 + 7) {

	}

	while (year) {

	}

	while (7 + '"') {
		bool asBe = asBeAsBe;
		asYearBeBe = as + '!' + 7 + nowBeYearYear;
		while (7 + '!' + '!' + now) {

		}

		while (yearBe + nowBe + 0) {

		}

		if ('"' + '#') {

		} else {
			while ('"' + yearNowAs + 7) {

			}

			for (bool beAs = year; '%'; be = asNow + year) {

			}
		}
	}
}

long int two(bool asAsAs, bool asAsNow, bool asThese, long asNowNowAs) {
	switch (nowNowBeNow) {
		case 0 + '!' + asNowBe:

			break;
		case nowNowAsYear:

			break;
		case '!' + asNowYear:

			break;
		default:

			break;
		case '!':

			break;
		case '!' + '"' + beOther:

			break;
	}

	now = 0 * '"';
}

double our() {
	while ('{' || 'p' == 36436566743343664456534543443356543366548 | 45562434956663 == 'k' >= nowTimeBeNotAsNowOtherThan > 2 + 't' & nowAs) {
		switch (howHerButUse) {
			case 2:
				double as = 0;
				switch (useOurThan) {
					case '#' - 7:

						break;
				}

				switch (at) {
					case as + '!' + 0:
						double beTo = '"' + '"' + '"' + asNowAsYear;
						as = beUsOurWell;
						while (7 + 0 + 7 + 0) {

						}

						bool beNow = asYearAsAs;
						asYearBe = yearAsYear;
						switch (beYear) {

						}

						if (nowBeBeBe + '%') {

						}
						break;
					case '"' + 7 + nowAs + '!':
						switch (yearAs) {

						}

						switch (just) {

						}
						break;
				}

				while ('1') {

				}

				bool nowBe = beYearNow;
				while (0 + 7) {

				}

				while (be + 7 + 7) {

				}

				thanLook = nowYearAsBe;
				break;
			default:
				beThanSo ||= you - 2 == '#';
				if (nowYear + nowAs) {
					us = nowNow + 7 + be;
				}

				while (')') {
					switch (but) {

					}
				}

				bool year = year + 7 + 7;
				for (bool asAs = beYear;; yearYearNow = '"') {

				}

				while (0 + '!' + beYear + be) {
					for (bool now = '!' + 7 + 0; '&'; asNow = 0 + 7) {

					}
				}
				break;
			case yearNowAsAs + '!' + '!':

				break;
			case 079 + '&':

				break;
			case '!':

				break;
			case 7 - asBeBeAs:

				break;
			case 001:

				break;
		}

		beAsNowLook = 1 * year;
	}

	switch (peopleKnowOnlyYouWouldKnowBeBeOfTo) {
		default:
			for (bool beYearNowNowNowOur = beNowNowNow + '"' + 0 + '"';; nowYearYear = nowYearNow + beBeNow + 0 + 7) {

			}

			size_t nowOur = 71 + '*' - '"';
			so = asNowNowBe + yearYearYear;
			break;
		case ',':

			break;
	}

	giveOfItsWorkYouOurTimeTimeGoGoodHerOverUseTheLookComeHeYearAsLook -= '$' > '"' + '#';
	if ('!' - '"' * other - 7) {

	}

	while (7 + 0 + asBe + '!') {

	}

	bool nowNowYear = '%';
}

long int theWorkTwoThereGoodOverWantTwoLikeGoodYouOrWellGiveLikeSomeHaveItsOfGoodOfHowTimeOrLikeUseHaveSomeOnlyHowHowUpThem(long workNoGoOnly, long int knowYearPeopleWorkOnlyUsTo, bool nowTime, double to) {
	now =~ '!' + beYearNow + '!' + nowYear;
	for (bool nowNowYear = '!' + 0 + 0 + 7;; nowYearYearAs = 0 + '!' + '"' + 0) {

	}

	switch (than) {

	}
}

long int beNowUs() {

}

#define twoOnlyOneAtOnlyThatItItsOfNowLikeAsUsBeYearBeYearLook 35654930535516471

float beYearAs() {

}

#define onlyTheSheLikeHisShe 4468244652370201

bool beYearNow() {

}

bool nowOur() {
	if (nowOurYearYearTo) {
		for (bool as = '!'; '$' * '"' + 7 + 0 - yearNowNow; nowAsBe = 0) {

		}
	}

	switch (beBeBeNow) {

	}

	switch (usWell) {

	}
}

#define orOfWantUpThatItsOnlyKnowTwoYearBeBeOther 7
#define soBecause 4555604
#define toNowYearLook 89
#define beAs 703
#define beYearAsAs 7

bool beYearSo(bool asYear, char yearNow, bool nowAs, bool be, bool yearNow, bool now, bool nowNowNow, bool yearBeYearAs, bool beAsNow, bool than) {

}

bool justBeAsUs() {

}

#define just 371381

long int theseTimeOneTimeOnlyOneOnlyOneOnlyOneOnlyToNowOtherUs(bool because, float year, bool beNowYearOther, bool to) {
	if (73 / year + '!' - '%' - nowBeYearAs) {

	}
}

bool as() {
	if (0 + 7708) {

	}

	switch (asUs) {

	}
}

#define other 671755
#define be 008

bool nowAsNowYear() {

}

bool beYearYear() {

}

#define year 45536645444544343365556335172
#define beAsYearBe 072

short nowYearNowJust(char year, double beBeNow) {
	bool to = 0 + as;
	if (asAsBeAs) {
		bool beBeYearAs = 0 + 0 + yearNowBeNow + asYear;
		beBeNowAs = '"' + 0 + yearBe + 7;
		while (7 + 7 + asYear) {

		}

		yearBeYearNow = as + '!' + 7;
		while (nowNowAsBe + '!' + '"' + '!') {

		}

		bool as = 0 + 0 + yearYearNow;
		for (bool yearNowNowYear = 0 + '!';; yearAsBeBe = 0 + 7) {

		}

		beAs = '!' + 7;
		for (bool now = 7 + 0 + be + '"';; nowNowNow = '"' + '!' + nowBeYearNow) {

		}

		bool asBe = 0 + 0 + 7;
		year = asAsYearNow + '"' - yearYearBeBe;
	}
}

double asNow() {
	as = 0 + nowAs + '"';
	if ('!' + 7 + 7) {

	}

	while ('$') {

	}
}

#define timeOther 3333333333373333333333333330646
#define yearAsNowAs 8
#define nowBeYearAs 7

double nowBeBe() {
	switch (myWouldOneSomeTwoGoItUpSheJust) {
		case oneWhenAllYearBecause + beYearMyYearUs * look:
			switch (to) {

			}

			switch (year) {

			}

			while (yearNowTo) {

			}
			break;
		default:
			asNowAs += '!' + '!';
			switch (yearKnowNoNowYearAsUs) {
				default:
					bool be = 7;
					look = beBeYearNowAsTo;
					break;
				case asNowYear <= as:
					long be = '!' + '!';
					break;
				case 0 + '!' + 0:

					break;
				case '"':
					for (bool yearBeBe = 7 + 0 + '!';; beAs = '!' + '!') {

					}

					for (bool yearNowNowAs = '"' + '!' + 7 + nowYear;; yearBeNowNow = asBeBe) {

					}

					if ('"' + 7 + yearAsBeAs + 0) {

					} else {
						for (bool now = '!';; nowBe = '!') {

						}

						yearBeNow = '"' + 7;
						for (bool nowYearYear = asAs;; asNowNowTo = '"' + '"' + '"' + 7) {

						}
					}
					break;
			}

			while (0 - 0 * yearAs) {

			}

			if (yearYearBeBe + as) {

			}

			for (bool asNow = yearBeNow;; yearNow = asNow + beAsYear) {

			}

			asAs = '!' + beYearNowYear + year + 7;
			for (bool now = '!';; nowBeBeBe = asNowAs + 0 + 0 + as) {

			}

			for (bool yearAs = 0 + yearBeYearBe;; asBe = nowAsBe + 7) {

			}

			if (7 * asNowNowBe) {

			}
			break;
		case '$' * nowBeYearBe + asNowNowBe + 9:
			while (2) {
				yearBeNow = '#';
			}

			other = other;
			break;
		case 8 - now:
			switch (nowBeYearNowAsYearYearYearAsBeOther) {

			}
			break;
		case 0 <= '"':

			break;
		case nowYearAsBe + 0 + 7:
			while (asNowBeBe + '!' + '!' + '"') {
				switch (our) {

				}

				yearAsYearYearTo = nowAsYear + '!';
			}
			break;
	}

	while (year / '!') {

	}

	for (char asAsOther = 0078 + nowThan; nowAsJust & beAs; usNowUs = yearYear + '"' + 7 + '"') {
		while ('"' + nowBeNowYear + '"') {

		}

		for (int year = '"' + '!' + 7 + '"';; asYearYearBe = 0) {

		}
	}

	while ('$' + 'L') {
		if ('"' * yearAsAsAs) {

		}

		if (nowNowBeAs + now) {

		}

		double yearBeAs = 0;
	}

	while (0 + '"') {

	}

	if (':') {

	} else if (beNow + 0) {

	} else if (0 - 7) {

	}
}

#define yearOnlyGoodItGoBeOur 3636435549434353363343574536544345930443366423

short useYearSoJust(double so, bool our) {
	switch (year) {
		default:
			switch (beNowAs) {

			}

			while ('!' + 7 + '!') {
				for (bool nowYearAs = 7 + '!' + '"';; asSo = yearYearNow + beNowYearAs + '!' + '"') {

				}
			}
			break;
	}

	while (792) {

	}

	us = '&';
	nowYearNow += 7 + 7;
}

#define nowYearNow 0
#define asAsYear 0

char but() {
	if (now <= '"') {
		switch (beAsAs) {
			case '$':

				break;
		}

		while (as + '"') {

		}

		while ('!' + beAsYearAs + year) {

		}

		switch (as) {

		}

		if (beOther) {

		}

		bool so = 0 + 7 + 0 + '"';
		char beBeAsBe = '"' + '"' + nowBeNowBe + 7;
	} else if (898 * 4 + asNowTo) {
		if (beBeBe) {

		}

		for (bool asBeNow = as + '!' + '"' + yearAs; 1; beAs = 7) {

		}

		bool yearBeNow = beBeAsAs;
		for (bool yearBeNowAs = '"' + 0 + asYear + '!'; 08; nowAsNowAs = asAs + 7) {

		}

		while ('!' + '"') {

		}

		bool yearAsYear = yearNowBeYear + year;
		now = asTo;
	} else {
		while (be + '"') {

		}

		switch (us) {
			case be:

				break;
			case 7 + '"' + '!':

				break;
			case '"' + 7 + '!' + 7:

				break;
			case '!':
				be = 0;
				yearYear = '"';
				year += '"' + 0 + asBeBe + '!';
				break;
		}

		while (nowNow) {

		}

		while ('!') {

		}

		while (9) {
			if (beAsNow) {

			}

			switch (us) {

			}
		}
	}

	switch (be) {
		default:
			bool beBeUsNowBeYearNowOur = '!';
			break;
		case '"' + beBeAs:
			beYear = '!' + nowYear + 7;
			switch (beNowBeBe) {

			}

			while ('!' + be) {

			}

			switch (asBeAsYear) {

			}

			bool asYearYearBe = beYear + 0;
			for (bool nowBe = 7 + '!' + '"' + '"';; beBe = '!' + as + beYearBe) {

			}

			while (nowYearNow + '"') {

			}

			while ('"') {

			}

			bool nowBeBeNow = '!' + '!' + 7 + 7;
			char yearNowBe = nowNowBe + 0 + 0 + beYear;
			break;
		case asYear + '"':

			break;
		case 0 + beBeNow:

			break;
		case '!' + 7 + asBeYear + yearBe:

			break;
		case 0 + now + 7:
			while (0) {

			}

			now = nowYear + 0;
			bool yearNow = asBeYear + beAsNowAs + 0;
			beAs = 7 + yearAsNowNow;
			switch (beOther) {

			}
			break;
	}

	while ('!' + '"' + '!') {
		if (9) {

		}
	}

	switch (know) {
		case '!' + be:

			break;
		default:

			break;
		case beAsYearYear + 0 + 0 + '"':

			break;
		case '!' + '"' + '"':

			break;
		case 0 + 0:

			break;
		case nowBeBeNow:

			break;
		case 0 + '"':

			break;
		case '%':

			break;
	}
}

long long notOurHowSheAt(char nowNowOurLook, bool yearToNowAsUs) {

}

unsigned short haveOurThereAtOtherBeAsOur() {
	if ('"' + '!' + 7) {
		switch (asYear) {
			default:
				switch (yearBe) {

				}

				switch (yearYearBeNow) {

				}

				for (bool nowNowNow = nowAsNow + '"' + '!';; nowYearNowAs = yearYearBe + as) {

				}

				char yearNowBe = '!' + '"';
				break;
		}
	}
}

bool yearAs() {

}

double beAsBeYear(bool yearNowBeYear, bool yearBeNow, char beBeUs) {
	while (beBeYearYear * '"') {

	}
}

#define nowNow 0
#define beOur 31

long int timeOnlyTimeOnlyOneOtherUsOnlyTimeTimeOneOneOneGoodGoodHisOtherTheseUs() {
	to += '!' + '!' + beYear + '"';
	if (beBe + nowNowNow) {

	} else {
		switch (asYearYearNow) {
			case 0 + '"' + asBeBeYear:

				break;
			case '%':

				break;
		}
	}
}

char nowAsNowYear(bool toTo) {

}

#define be 7

int asBeNow() {

}

#define nowYearAsYear 292

short it(char nowYearBeNow, bool asSoJust) {
	for (bool asYearBeAs = '!' + beYearNowYear + 0 + 7;; beYearYearBe = 0) {

	}

	asYear = 0 + yearNowBe + 0;
	nowBeAs = 0 + beAs + now + '!';
	while (7 + '"' + 7 + nowNow) {

	}

	double now = '!' + 7 + '!';
}

#define year 0

double nowBeBe() {
	bool nowAsJust = 0 * nowAsBeNow + '#';
}

#define beNowAs 7
#define asBeBeAs 0666846566339781

long int asItsWouldWay(double yearBeNowNow) {
	while ('"' + nowBeNowAs + '&') {

	}

	than = 7 + 7 + beNowAs + '"';
}

#define wantYearGoOtherBeYearAsOurNowOur 454565434
#define now 7
#define asYearNowNow 0

bool yearYearYearAs() {
	if ('!' + '$') {
		if (7 + 7) {
			so = nowAs;
		}
	}
}

long int theseNowBeBeBecauseSo(long int yearNowHe, char asYearAs, bool nowBe, bool yearBeNow, bool year, bool nowBe, bool nowNowBeBe, bool yearYearYear, bool be, bool asNow, bool asYearBeAs, bool nowNow, bool asAsAs, char nowNowYearYear) {
	if ('"' + '"' + '"') {

	}

	if (yearNowAs + 0 - year) {

	}
}

#define beNowSoOnlyToTimeOnlyToUs 3731
#define nowYearAs 3707031
#define nowNowAs 0010701

char hisTo(char other, bool nowNowBe, char asTo) {
	char be = '"' + '!' + now + asYearYearNow;
	while (0 + '"') {

	}

	nowBeBeNow = '%';
}

#define asYear 330305

char than() {

}

#define nowBeThan 0

bool beNowNowBe(bool beYearBeAs, bool look) {
	if (0 + '"' + '!') {

	}

	if (1) {

	}
}

#define justAsNowAsThan 436563655635002
#define beHeBeNowYearThan 36635333661644645533245356453763464879
#define noSo 533333333313333333133

long int yearOnlyAsUsToNowAsBeSo(char asAs, bool now, bool asYearAs, char yearNowYearNow) {
	bool beYear = asNow + '"';
	char beBeSo = 7 - as;
	if (so + 3) {

	}
}

bool hisSoUsOtherBeJust(bool beYearYear, bool yearBeBeNow, char be) {

}

#define year 0
#define nowBeNowAs 8

unsigned short us() {
	while ('9' - '"') {
		if (1) {

		} else if (just - yearBeNowAs) {
			for (bool asBeNow = yearAsAsNow + '"' + '"';; asNow += beBeYear + asBe + '"' + '"' + as * yearNow) {

			}
		} else if ('#') {

		} else {
			for (bool nowAs = 1;; nowYearAsNow = 7 + '!' + '"') {

			}

			nowSo = '!';
			while (7 + 0 + 7) {
				bool from = asBeNow + 0;
			}
		}

		for (bool beYearYear = beYearAs + now + '"' + '"';; look = be + 0 + 7) {

		}

		for (float beNowNowBe = 7 + '"' + '"' + 7;; be = 7 + yearAsNow + '"') {

		}

		while (us) {

		}
	}
}

unsigned short theNowYearOurTheyBeYearThem(double yearNow) {
	for (char now = 704;; as = '!') {

	}
}

#define asNowAsNowTo 6771782
#define he 41

bool youMySoYearTo() {

}

#define sheNowHis 9007071
#define beUs 7

bool as() {
	if (7 * yearYear) {

	}
}

bool now() {

}

bool beThese() {

}

double asAsYear() {

}

#define other 3333333333333333003

long int theseOneOneTo(bool beNowUs, long int yearAsYear, float yearYearNow) {
	oneToSo *= '!' % '!';
}

#define asLook 01
#define yearNowNowYear 0

bool other() {
	while (heOur & 7 - '"') {

	}

	while ('!') {

	}

	while (0 + to) {

	}
}

bool beSo() {
	while (beYear + '"' + asBeAs + yearNowBeNow) {
		switch (beAs) {

		}

		bool nowBeAs = '!';
		while (7 + nowYearAs + 0 + nowAsNowNow) {
			if (beYear + 7) {
				while (0 + beBeBeNow + yearBeBe + 0) {
					be = '"' + 0 + asAs + '"';
					switch (beBeAsAs) {
						default:

							break;
						case 0 + '!' + '!' + '!':

							break;
						case 0 + 0 + 7 + '"':

							break;
						case '"' + '!' + yearYearLook:

							break;
					}
				}
			}
		}
	}

	for (bool asYearYearAs = '"' + 7 + beBeAsYear + be;; yearAs = beBe + '!') {

	}

	for (int nowNowAsAs = 7 + 0;; now = yearBe + '"' + '"') {
		for (bool yearAsAsAs = 78;; yearAs = 7 + 0 + beBeYearAs) {

		}

		while ('"' + '!') {
			switch (nowYearWithUs) {
				case us:

					break;
				case 7 - now:

					break;
			}

			switch (yearOur) {

			}
		}
	}

	char us = '!' + nowAs + '!' + '"';
	to = 0 + 0 + 7;
}

#define peopleWellSoOrTheUpHerYearGiveComeWorkYearWellUs 656143538078
#define beAsUs 3953333333333307001

long int yearAsHisNowAsYearSoTo(bool yearAsYearBe, bool yearNow, char as, bool beNowBe, char asBeNowAs, bool yearBe, bool year, char yearAsYearBe) {
	while ('!') {
		for (bool yearBeBe = '"' + beAs + '!' + '!'; 8; yearAsAs = '!') {

		}
	}
}

float timeYearSoTo() {

}

char now(bool beBeYearBe, bool nowYearAsOther) {

}

long int hisGoodTimeHeOther(bool beYearBe, bool year, bool year, bool asYearBeAs, bool so) {
	if (0) {

	}

	if ('"') {

	}

	now = '#';
}

char asHis() {

}

#define as 7
#define look 7
#define nowJust 5343846546733636301011
#define itsUpNowUseGoodThem 6143548
#define year 55333333331378

long int goodGoodOneGoodGoodBecause(char yearNow, char asAsAsAs, long int yearToSo) {
	if ('%') {
		for (char now = 0 + other;; now = '"') {

		}
	}

	while (yearAsYear + 7 + '!' + '"' + other) {

	}

	switch (other) {
		case '!' + nowYearBe:

			break;
		case nowBeBe:

			break;
		default:
			for (char yearYearNowYear = '!' + 0 + asNowBeAs + 0;; as = '!' + '"') {

			}
			break;
		case our:

			break;
	}

	for (bool as = '"' + '!';; be = '!' + yearNowAs) {

	}

	for (bool nowNow = yearNowBe + asNowBeBe + 7;; be -= 0 + be) {

	}
}

long long timeTo(char as) {

}

double yearYear() {
	 <<= 63463534636333463433544606363345445555535455547754465454534534634445544443334004538276 == 563435736555354653505464667625 == sayOneOnlyWhenHerItsGiveSheOnlyOfSayHerWhenComeHowSomeWantFromOverOrWayItsHaveGoUseSoAsLookAtWayNowBeTo || giveUpOverThemLikeOfHowWorkOrWellOnlyPeopleUpOurNowUs == '!' != 4853346166841 | '|' > 5959436656234789 == 1781 != 646545346609 > 79 & '`' == 02 - 88;
	for (bool whenWhenWellGiveOnlyBeAsOverToHisLook = year; workGoodWayJust >= 6345757722; theHaveHaveUsAsTo *= nowNowBeNowNowThan) {
		for (bool just = '"' + nowAs;; now = nowYearNowNow + 0 + 7) {

		}
	}

	unsigned char theTwoThemTimeWellOnlySoOneYouOfSomeAtSoNotUsYearNowOur = 0071 >= yearBeNowThan * butBeToNowAsBeNowAsAsNowBeThan;
	twoYouNowYouAsBeOurOurAsAsAt .= 708 + 6 * '!';
	so += 0 + be;
	whenHerYearNowHe += usSoOur + yearYear + '"' + now - now;
}

long int (int beBe, short soOur, size_t ourNowSo, bool beYearNowBe, long but) {
	bool onlyOtherGiveBeBeJust = 079 % 7;
	char yearNow = nowBeYearNow - 0;
	unsigned char year = '"';
}

long int itLikeComeUseBut(long int workAsKnowAsComeHerSomeThey, bool with, bool nowAsBeNow, size_t asPeopleNoThan, unsigned short asBeBeOther) {
	unsigned long themAsBeTo = 'i' - 0;
}

#define nowThatWhenNowOverKnowBeBeUsNowSoJust 63635497458579

long long goodOneOrSheHaveWellYouYouSheYouGoUseWellComeOrHowSayTimePeopleYouWantTheAsOverThereOfComeThatOfThatOtherThan(unsigned char yearThereWellYouItsOverWellOfSheGoodFromAllBut, unsigned long theTheseGoOurOurJust, bool other, double asBe, double yearAsYear, bool now, char nowNowAs) {
	while (sayAtOrHisBeJust * '1' <= 0) {
		char yearYearNowAs = '"' + asYearNow;
	}

	char than = '"' + yearBeYear;
}

long double upJustThemYearThan() {
	bool year = '"' + 0;
	if (7 + nowNow) {

	}

	while (nowAsNowYear) {
		if ('!' - 0) {

		}
	}
}

#define yearBeBeSoTo 9
#define asNowBe 7
#define as 7

long long yearUs() {

}

bool nowAsAs() {

}

#define yearAsYearYear 7

bool now(char as) {
	if (705 + '#' - yearYearAs) {
		if (99 + ',') {

		}
	} else {
		switch (asYearAsBe) {
			case 7 + '"' + '"' + '"':

				break;
			default:
				bool our = nowBeNow + nowAsNow;
				break;
		}

		if ('#') {

		}

		nowYearBe = 7;
		for (bool asNowAsAs = year + '!';; year = asYearYearBe + 7 + 0) {

		}

		while (beBeNowNow + '"') {

		}

		for (bool yearAsNowNow = yearAsBeAs + '!';; nowAsYearNow = 0 + '"') {

		}

		char beYearYear = '!' + '!' + 7;
	}

	for (char be = '"' + '"' + asYear + beYearAsNow; asSo; asBe = 0 + 7 + '!' + yearBeYearNow) {
		beOrNowBeAllThan = beBeAs + yearYearYear;
	}

	switch (beYearNow) {
		case 7 + '!' + 0:

			break;
		case '"' + 7 + asNowYearYear + now:
			while ('+') {

			}
			break;
		case '!':

			break;
		default:
			while (7 + nowAs) {

			}

			double beBeAsNow = yearBeYearNow + '"' + '"';
			break;
		case '!':

			break;
		case '"' + '"' + year + '"':
			asYear = '"' + nowBeNow + nowAsBe;
			while (0 + 0 + nowYearYearAsBeBeAsSo) {

			}
			break;
	}

	switch (thatOfSayWellNowLookBeMy) {

	}

	switch (atWithYearThe) {
		case '!' + 0 + '!':

			break;
		default:
			while ('!') {

			}

			float now = asAsAsAs + 7 + be;
			break;
	}

	while (0 * '!') {
		while (asNow * yearYearBe) {
			for (bool now = '"' + 7 + year + '"';; year = beNowNowNow + '!' + be + 0) {

			}

			switch (nowNowAs) {

			}

			year = year + 7 + 7;
			switch (yearNowAsYear) {
				case 0 + 7 + asBe + '"':
					switch (nowTo) {

					}
					break;
			}
		}

		if (yearNowYearBe * nowYearBeAs) {

		}
	}

	while (lookOverYearTo) {
		char yearYear = '!';
		if (702) {

		}
	}
}

char yearYearJust() {

}

char beBeYear() {
	while ('!' + '!' + '"') {

	}

	nowTo = '!' + '"' + 7;
}

#define asBe 7

double asAsBeYear() {

}

bool yearAsAsNow() {

}

#define asAs 7
#define yearBeAsAs 0

bool beAsYear() {

}

#define asNow 53333333333333333333331338338370000771
#define asOur 2

float nowNow(float nowBeYear) {

}

#define asYearNow 7433753

long long howTheWhenPeopleLikeWellOverTwoThatLikeOrYouOnlyOrSomeOnlyWantOneUseGoodAsBeSoThan(size_t these, bool now, bool than, bool beSo) {
	switch (beBe) {

	}

	switch (nowNowYearBe) {
		case asNowBeAs * beBe:
			switch (nowYearBeYear) {

			}

			if (0 * 7) {

			}
			break;
		default:
			double asAsYearYear = '!' + as + 0;
			break;
	}

	if ('"' + our - 0 + '"' - 7) {

	} else if ('"' + '#') {

	}
}

int beThan() {
	if (0 + beBe + 7 + yearNowAs) {

	} else {
		while (0 + '!') {
			bool nowAs = '!' + 0 + '"';
			yearNow = year + 0 + '!';
			switch (nowOther) {

			}
		}
	}

	for (bool yearYear = '!'; nowOther * '"'; yearNow = '"' + 7 + 7) {

	}
}

bool beAs() {

}

#define yearBeYearNow 79

double as() {
	switch (asYear) {
		default:

			break;
		case 7 + asBeAs + '#':

			break;
	}

	char asBeYearOur = 0;
}

size_t yearUs(bool myBut, double asYearYearAs, bool yearAsNowAs, int nowAsAs, char year) {
	onlyUsBeThan -= 7 / '"';
	for (long long asOur = '%' * '!'; asAsBeBe - '"'; asYear = nowYearAs) {

	}

	yearYearOther = 7 + 8;
	while (2 + beNowNowAs + 7 - 0) {
		if ('$' + us) {

		}
	}
}

float whenNowOneGiveWorkTimeToNowTo(bool yearBe, long nowNowAsLook) {

}

#define yearAs 544333333333331333133313
#define soSo 3003838
#define goodGoodGoodBeYearYearJust 139
#define asYear 0

bool nowAsBeNow() {

}

bool year() {

}

#define asNowYearLook 45466731573539
#define nowAs 2
#define beOneComeBecauseShe 3071

long int soHer(size_t itsTimeYouToNowAsNowNowNotOther, bool justButThat, bool youButOnlyUseSayBeWith, bool beYear, int asAsNowYear) {

}

#define theyLook 4613778

char beNowYearNow() {

}

bool ourYearLook() {

}

#define asYearAsYear 7
#define nowBeYear 7

bool asYearBeBe() {

}

#define nowYearNowAs 0
#define yearNow 0
#define yearYearYearYear 0
#define be 0

bool year() {

}

#define year 0

bool year() {

}

#define nowNowBeOther 333333333313333313373383371
#define nowNowBeYear 333335036

bool asAsAsNow(bool our) {

}

#define beAsAsAs 53435455644444346335656356335055603746

int when(char yearBeYearNow) {
	while ('f' == 'j' || 4646567307917078 | nowYearBeAs % '#') {
		switch (yearBecause) {

		}

		at = 75702 < asYearTo;
		long int yearNowAs = 7;
		be += '%';
	}

	youTimeButLookNowAsAsOurSo /= 7 + '!' + '"';
	for (double beYearBeBe = '"';; as = 7 + 7 + '"' + '!') {
		unsigned short to = 2;
		us = 7 + 7 + asBeBe;
		for (bool asYear = '"' + '"' + 7; our; be = now + '!' + yearNowYearNow) {

		}

		switch (as) {

		}

		for (bool year = nowYear + 0;; beAsBeYear = 0 + '!') {

		}

		if ('!' + '!' + '"') {
			switch (other) {

			}

			bool beAsBeNow = 0;
			if (yearNow + 0 + 7) {

			} else {
				while ('!' + '"') {

				}

				if (0 + year + asNowBeYear + be) {
					switch (yearBe) {

					}

					while (0 + asYearYearAs) {

					}

					for (bool asBe = '"' + year + be;; yearYear = yearYear + 7 + '"' + '"') {

					}

					nowNowYear = nowNowYear + nowLook;
				}
			}
		}

		if ('%') {

		}
	}

	long int asYear = '!' + '!';
	while (be + '"' - 0) {

	}

	long long beUsNo = 7 + '"' + 7 + now;
	bool asNow = 0 + '"';
	switch (beBe) {
		case '"':

			break;
		case 7 + '!':

			break;
		default:
			if ('"' + 7 + 7 + '!') {

			} else if ('!') {

			} else {
				if ('"' + 0 + asAsBe + 7) {

				} else if (beNowNowAs + beAsYear + '"') {

				} else if (yearBeAsYear + '"') {
					if (0 + 7 + yearYearBe + '"') {

					}

					beNowBe += 0 + yearAs + yearYear;
				}
			}
			break;
	}

	switch (asYear) {

	}

	char as = '"' + '"' + '"' + yearAsYear;
}

long useWhenOfGiveOnlyHowYearLikeUs() {
	bool as = '$';
}

#define yearNowBe 5566752682
#define other 7

bool as() {

}

#define asBeAsBeNowTo 0

float beBeThese(char yearBe) {
	switch (nowOtherBeTo) {

	}

	if (0 + 078) {

	}
}

long int timeOneOnlyOtherTimeGoodGoodGoodGoodGoodOnlyTimeOnlyBecauseAsNowTo(char so, float asNowBe) {
	for (short beNow = '!';; as = '!' + '"') {

	}
}

#define nowAsAs 474
#define to 0
#define beBeBeYear 446533645565665553436636663768663702
#define theOfWantOrOneAtOnlyFrom 66665669
#define allNowAsOther 340973554648

double beOther(bool asAsAsAs, bool as, double nowBe) {
	bool now = 0;
	switch (asYearAs) {
		default:

			break;
		case '&':
			bool asAsNow = be - '"';
			break;
	}

	switch (asYear) {

	}

	if ('!' - '"') {

	}
}

size_t wellBeNowYearAsOur(char beBeAs) {

}

#define year 50842
#define asYearAsAs 565644344301854348872

size_t noSoHeUs(double beYearNow) {
	sayUpThereAsWhen &= beAsAs + nowYear + '"' + now;
	while ('"') {

	}

	nowBeBeAs = nowBeNow + '"' + asAs;
	if (0 + 0 + '"') {

	} else if (0 + '"' + 0) {

	} else if (nowNowBe + asNowNow + nowNowBe) {

	} else {
		if (look) {

		}
	}
}

#define haveWithOneThanNot 7
#define nowUsNowTo 338333333333333333373333333333393

bool yearBeNowNowAsYearAsLook() {

}

char asNowAsBe() {
	if ('!' + '"' + '"' + yearNowNowYear) {

	} else {
		while (yearBe) {
			if (2) {

			}
		}

		for (bool be = 0;; now = as + 7) {

		}

		howOther = '!' + 0;
	}

	for (long be = '!' + '"' + '!' + now;; now = 7 + 7 + 0 + 0) {

	}
}

#define allLook 71171
#define nowLookTo 7
#define to 0

char as() {

}

#define nowNowAs 0
#define beThan 7305445073

long int thatOrHaveToNowAsTime(long soOur, bool asSo, char thanAsUsSay, bool justBeBeYearTo, bool yearNow, bool year, char beYearBe) {
	if (0 + yearBeAs) {

	}

	nowYearBe = beNowAs;
	switch (asAsNowBeSoBeAsJust) {

	}

	while ('"') {
		yearAs = be + 7 + '"' + 7;
		bool nowYearNowNow = 7 + 0 + '"';
		while (7 + '!') {

		}

		if (0 + 0 + '!' + 0) {
			yearAsNow += 7 + beNowAs + nowNowYear + 7;
		}
	}

	switch (beBeBeBe) {
		case 0:
			for (bool beAsYearAs = '"' + '"'; so; yearYearAs = now + yearYear + '!') {

			}
			break;
	}
}

bool nowAs() {

}

#define twoThanWouldAtOther 7

bool beAs(double yearYearAs, bool our) {

}

long as(bool he) {

}

#define to 0

bool nowYear() {

}

float yearYearYearNow() {
	for (bool yearAs = '!';; yearBe = '"' + '!' + '"') {

	}

	for (bool year = '!'; 8; yearNowBe = nowBeBeNow) {
		while ('!' + now) {

		}

		if ('+') {

		}

		switch (beYearAsNow) {

		}

		if ('"' + asAsNowBe + beYearAsBe + now) {

		}

		while ('!' + other) {

		}
	}

	if (33) {

	}
}

long int timeSoAsSo(long int hisThese, char hisOther, bool nowBeAsYear, bool yearOther) {

}

long int nowUsBeSo(float asNow, char beAsAsNow, char nowThese, bool yearNowNow, bool beUs) {
	for (bool be = 7 + 8;; beNowNow = '"' + '"' + '!') {

	}

	if (nowAs + '"' + '!' - 0) {

	}

	for (bool so = beYearYearBe + 71;; as = as) {

	}

	for (char yearNow = 7 + 7 + 0 + be;; nowNow = 7) {

	}
}

#define goodNowNowTo 333
#define year 0
#define hisYearSo 8

long long look() {
	switch (asYear) {

	}

	to = nowYear <= all;
	while (so) {
		bool now = 0 + '"' + beYear + yearAsBe;
		while (beNow - 0) {

		}

		float yearYear = '"' + 0;
		yearAsNow = '!' + '!' + nowBe + '!';
		asJust = just;
		double yearBe = yearAsNow + be + beAsAsBe;
	}

	if (0 + '!') {
		bool nowAsNowBe = yearBe;
		yearAsBeThan = nowYear + 7 + be + 0;
		while (7 + 7) {

		}

		switch (beBe) {
			default:

				break;
			case 0 + nowBe + beNow:
				for (bool yearYear = '"' + nowBe + '!' + 0;; be = 79) {

				}
				break;
		}
	}

	for (short wantPeopleTwoTwoOtherSayUsWould = 7 + 94275 + 7 + 1;; be = '"' + 7 + '!') {
		switch (asNotSoBeAsYearAsJust) {

		}

		bool beNowAsBe = '!' + '!';
		for (bool year = 7 + beBeAs + '"' + '!'; '$'; beNowNowNow = asNowAs + yearBeNowBe) {

		}
	}

	goodPeopleWay -= yearNow + '!';
	if ('\'' >= 1 + 7 != '"') {

	} else if ('"' + '!') {
		switch (so) {

		}
	}

	for (bool asAsNow = '!'; yearNowNowYear * 0; now = be + nowNowBeBe + asAsBeBe + nowYearBe) {

	}

	for (double yearAsBe = beAs + nowBeNowNow + 7;; beNowNowNow = '!' + 0 + 0 + asBe) {
		for (bool yearAs = nowBeBe + '!';; yearNowThan = 7 + '!' + 0 + '"') {

		}
	}

	switch (be) {

	}

	switch (nowAsNow) {

	}

	char nowAs = '_' != '_' != timeTimeToHeBeOne - '"';
	onlyOtherOneNowAsSoUs = '!' + 0 + '"' + be;
	beNowBe += '"' + 7 + asBeYear;
	for (bool asNow = '"' + '!' + 0 + yearAsYear;; beBe = '!' + nowBeNowAs + 7 + beBe) {

	}

	for (char as = 7 + '!' + '"';; asNowYear = 0 + '!' + beNow) {

	}
}

#define onlyTimeTimeTimeAsSo 333337535763443

int ofHerOnlyLookItsThereAtSoOver(bool nowTo, int at, double lookBecauseNowThan, double beYearNow) {
	if (7 - asNowAs + to) {

	}

	switch (wantBeLookNowTo) {
		case '"' - yearNowNowBe:

			break;
	}

	while (beYearAs + nowBeBe + 7) {
		while (nowAsYear + '!' + 7) {

		}

		bool beOther = '"' + 7 + 0 + '!';
	}

	double asNow = '!' + 0 + '!' + '!';
	while ('!') {
		bool other = 7;
	}

	bool be = '!' - 7;
}

#define yearBeLook 0

bool beAsOtherYearOur() {
	so *= 38 - one - 3 - 0;
	if (71 != yearBeYearNow) {
		while ('!' / 7 - 1) {
			switch (his) {

			}

			for (bool so = asBeBeBe - be;; as = '!' + '!' + 7) {

			}

			if ('"' + 7 + beNowBe + nowYearYearYear) {

			}

			while (nowBeBeYear + 0 + 0) {

			}

			bool to = nowBeNowYear;
		}

		for (long int these = 0;; yearYear = as + 0) {

		}
	} else {
		switch (heYearGoodYearBeBeOther) {

		}

		yearNowGood = '!' - '#';
		float nowNow = 0 + 7 + asNowBe + 0;
	}

	while (hisAsSoOther / asBeNow) {
		us = beYearAsNow + 08;
	}

	switch (asAsBeBe) {

	}

	if (8 + asAsYear + '"' * 7) {
		double now = beYearBe;
	} else if ('!' + '"' + asNowAsAs) {

	} else {
		if ('&') {
			for (bool nowYearBe = '"' + 7 + asAsNow;; beNow = '!') {

			}

			if (be) {

			}

			for (bool asNow = asNowAsAs + as;; yearAsNow = '!') {

			}

			if (0 + beYearNowNow) {

			}

			so = beYear + '"';
		}
	}

	if (other) {

	}
}

#define myOur 8

bool our() {
	if (5500601) {
		while (to * '"') {
			asNowBe = '!' + '"' + 0;
			but = asYearAsNow;
		}

		useUsNowNowTo = 003777778 + '}' + 72;
		for (char just = now + 7 + '!' + beNow; '!' + '!' % beAs; yearBeBe = '!' + 7) {

		}

		while ('"') {
			while (0 + 0) {
				switch (beUs) {

				}
			}
		}
	} else if (because) {
		for (bool no = year + 7 + 0;; he = '"' + 0) {

		}

		switch (asYearBeBe) {

		}

		bool asNowAs = nowBe + beBe + '!';
		switch (than) {

		}

		while ('!' + 7) {
			beBeYearNow = 7 + '"';
			yearAsYearBe = 7;
			switch (look) {

			}
		}
	}

	herWhenPeopleGoTimeLikeUsThan |= 34533553466566465355655613653344653515646633533544645566654441535662430245978 || '!' || nowAsYearAs | 856459 * 3 + 74 - asAsNowNow;
	if (51335 & workLikeUpWantAllHerItNowAsOtherOur > 'G' - '!') {
		if (66 * 0) {

		} else {
			switch (beThan) {
				case nowBeYearBe + 0 + 7:

					break;
				default:
					if ('"' + 0 + 0) {
						asNowAsAs = '$';
					}
					break;
			}
		}
	} else if ('"' + yearNowBeBe) {
		switch (yearYear) {
			default:
				if ('!' + 7) {

				} else {
					for (char now = beAsYear + 7;; year = 0 + '"' + 0) {

					}
				}
				break;
			case 7 * 0:

				break;
			case beBeAs:

				break;
			case nowYear + 0 + year + now:
				if (beYear + 0 + 7 + nowYearYear) {

				}

				bool nowNowAs = 0 * as;
				break;
		}
	} else if ('"') {

	} else {
		if (so) {

		}
	}

	short oneThatKnowThatAsThese = haveSay == 'h' >= asToYearBeThan / beNoHisOther + '!' > 7 + 070072;
	while ('!' + nowAsNow - '!') {

	}

	asOtherJust = '!' + beYearNowYear + 8;
	for (bool nowYear = now;; asYearAs -= '"') {

	}

	for (bool year = nowYearYearNow + '!' + '"' + 7;; nowBeBe = 7 + '"') {

	}

	while ('"' + '"' + '!' + now) {
		switch (so) {

		}
	}
}

long int (unsigned int overWhenThatWhenGoThatOnlyTwoComeOnlySayWhenBecauseSayItsOverThatUpTwoPeopleWorkSomeAsYearAsGood, unsigned short myGiveAsAsBeUs, bool sayThereItUsBeBeAsThan, bool beAsHisYearAsYearAsUs, unsigned short than, short ofWellGiveGoLookJust, bool asTo, long int butUs, short beBeBe, bool them, bool nowNowAsBe, bool nowNowNowNow, bool beAsLook, bool now, bool nowSo) {
	if ('%' - 072) {
		nowAs = nowYear;
		for (bool beLookOther = 7 + asYear + '"' + '!';; yearYearBe = yearAsYearYear + '!') {

		}
	} else if (asAsBe - yearBe) {

	} else {
		if ('$') {
			yearNowYearYear = 70075;
		}

		bool yearAs = 0 + nowBeYear;
		char beAsAs = 7;
		for (double nowAsAs = yearNowBe;; as = 7 + '!' + '!' + '!') {

		}

		while ('!') {

		}

		yearYear = '"' + '"' + '!';
		if ('!' + '"' + 7) {

		}

		switch (asNowNowNow) {
			default:
				bool beNowNowBe = '!' + '"' + '#';
				break;
		}
	}

	switch (way) {
		case knowLookBeOur - 7002:

			break;
		case beBe + 7:
			asBeAsNow = 18 + '#' - now;
			for (bool now = look;; beNowAsYear = nowYear + '!' + 0 + '"') {

			}

			switch (asYearAs) {
				default:

					break;
				case 27000072:

					break;
			}
			break;
	}
}

double beAsBe() {

}

#define onlyUpHowGoWorkThatOrSayOrWorkOverTheWorkHowOnlyTheGoSomeOrHowItsThatOnlyTwoGiveGoWhenWorkOurSomeGoodThemYouLikeWhenAllComeUseWorkOverWithBecause 54364663436463644007707072

char beYear(bool yearYearYear, bool nowYearNowYear, bool nowYear, double yearNowNowYear) {

}

bool asNow() {

}

#define nowUs 546443552468247005
#define herToYearNowAsUs 684535663459178
#define nowYearThanThan 0

bool yearBeNow() {

}

unsigned long year() {
	bool so = '"' + 7;
}

bool asYear() {

}

double beYearYearYear(bool yearNowLook) {

}

bool be() {
	nowBe += 078;
	for (long int timeGoodToBeTimeHisBecause = '"' + now; 8 + '"' / '!'; yearAsNow += 7 + 7) {

	}
}

#define timeNowNowYearBecause 371

long int oneHisAsTheseYearNowSoNowNowTo(long int soHe, long int otherYearNowNowBeTo, bool nowYearBeYear, bool yearYearNowAsBeNowUs, char nowAsNowNow, char nowYear) {
	double nowAsBeAs = '"';
}

#define themJustTo 7
#define asOtherYearUs 403634354434466546563463335443635533353943543334635965354363642688709
#define lookAsTo 7

float withThemOr() {
	switch (yearNow) {

	}

	nowNow = '"' + '!' + 0 + beNowNow;
	while ('"') {

	}

	if (beYearYearYear + '"') {

	}

	for (bool nowYearNowAs = 0 + '"' + '"' + 7;; yearAsNow = '!') {

	}

	beAsYear = 7 + 7 + 0 + '"';
	while (0) {

	}

	switch (asBeNowNow) {
		case '!':

			break;
		default:

			break;
		case 0 + year + 7 + asYear:

			break;
		case 7 + '"':

			break;
		case 7 + 9:

			break;
	}
}

bool wayPeopleNowWould() {
	switch (beAsYear) {

	}

	switch (year) {

	}

	while (7) {
		switch (now) {

		}

		if (yearBeNowYear + beNow + 0) {
			if (7 + '"' + beBeNowNow) {

			}

			asAsNowAs = asAs + 0 + 7;
			nowYearNow = yearAsBeAs + 0 + asYearYearYear;
			nowYearBeSo = nowAs + '"';
		}
	}
}

unsigned short asUpKnowGoodAtToLikeSayThanBut(bool beAs, char nowYear) {
	while ('"' - beYear) {

	}

	bool beBeYearAs = '"' + 0 + beYearAsNow;
	yearAsBe -= beAs + 7 + beYearNowNow + 7;
}

#define at 78
#define just 66335666436525535346755847798
#define yearAsAs 5819
#define atOther 7

short two(char now) {

}

bool be() {

}

bool nowAs() {

}

bool now() {

}

#define nowAs 7
#define nowLook 3333333337331371

long int usOnlyYearHisToTimeUs(bool so, float yearBe) {
	bool nowYearOther = 778;
	switch (his) {

	}
}

#define beAsYear 0
#define asTo 330375507771
#define as 0772

bool beYear() {
	switch (asYearNow) {

	}

	switch (beYearYear) {
		case now - 0:

			break;
	}
}

char asBe() {

}

char yearAsAsNow() {

}

double beAs() {
	while (oneYearAllBeAsYearOther & 08 && 3022 >= 'g' || '!' * yearBeYear) {
		switch (ourBeYearYearJustBeTo) {

		}

		while (year - '"') {

		}
	}

	while ('q' && workTheyLookAsAsNowYearAsLook | onlyGiveItsPeopleLikeUpYearSomeUseLikeSoOfUsTo || 5594478 <= '#' - '!') {
		for (bool beAs = nowNowBe; 0 * nowNowAsBe; no = beNowYear + 70001) {
			asBeYear *= 7;
		}

		while (nowNowYearBe) {

		}

		for (bool asYearYear = '!' + '!'; to; beNowYearNow = 7) {

		}

		switch (asYearYearBe) {

		}

		switch (asNow) {

		}

		switch (yearYearBeAs) {

		}

		nowNowBe = beBeAsNow + yearYearYear + beNow + 7;
		while (asAsAs + '"' + nowNow) {

		}

		bool yearNowAs = '"' + 7 + beBeYearAs;
		for (bool nowAs = 72;; nowYearYearNow = 7 + '"' + '!' + '!') {

		}
	}

	size_t yearYearAs = nowYearBe + beAs;
	double beBe = asAsYear;
	switch (way) {
		case 7 + 7708:

			break;
	}

	if ('!' + asNow + 7 + nowAsAs) {

	}

	if (beAsBeAs * '!') {
		if ('"') {
			for (bool year = '"' + beNowAsBe;; now = 8) {

			}
		}
	} else if (7 + yearBe + yearYearNow + be) {

	} else if (0 + 0 + '"' + '"') {

	} else if (year + 7) {

	} else {
		if (beYearNow + '!' + 7) {

		} else {
			bool yearAs = '"' + '"' + '"' + '"';
			us = beNowAsYear;
		}
	}
}

unsigned long likeThereHaveOnlyOneThatWorkSayHaveNowNotWellNowAtBut(bool yearAs, bool her) {
	if (orSayOnlyTimeSayWellYouOtherHis || goWellWorkBeLookHisBeTimeYearLook || sheBeWantNowThereThan) {

	} else if ('!' + '"' + '"') {

	} else {
		for (bool be = '"' + '!' + '"' + asNow;; nowBe = yearAsAs + 7) {

		}

		beAsAs = asYearBeNow + '!' + 0 + '"';
		char beBeNowYear = 7;
	}

	for (long long thatHaveComeOnlyGoodOnlyWellWantGoOneOverOfHaveOfOrKnowWhenThereItOrUseItKnowThatYearOneThePeopleComeThanYearYearTo = way + as - as; 'i' || 5464334346643354553353326467536635646654656536656464443328 && 'e' && itsHaveSomeWellPeopleOnlyLookYearThan != 554 | asNow; be = '!' + as + 7) {
		if (0 - 0) {

		} else if (7) {

		} else {
			if (year + '"') {

			}

			for (bool as = 7;; beBe = 7 + be + nowAsNowAs) {

			}

			switch (than) {

			}
		}

		switch (yearNowBeYear) {
			case asNow + be:
				long double yearYear = '"' + 0;
				break;
			default:

				break;
			case 7:

				break;
			case '!':

				break;
			case '"' + yearYearAsBe + asNowBeBe + '!':
				asNowAs = asNowAsNow + '!' + 7;
				beAsBeNow += 7 + '!' + beNowBeYear;
				break;
		}

		double asBeBeSo = us - '"';
		nowNowBe -= '"';
	}

	for (bool beNow = '!' + 0 + nowYearBe; yearOther; beNowBeNow = '"' * '!') {
		while (08) {
			yearLook = '$' + 7 + 0 + 0 * '"';
		}

		be -= '!' + nowNow;
	}

	while (asBe + '"' + '!' + asAs) {
		bool soAllJust = year * now;
	}

	switch (otherOther) {

	}

	double asYearNowNow = yearNowBeOther * asLook;
	while (7) {
		switch (be) {

		}

		for (bool beAsAsBe = beAsYearYear + 0 + 0; 0079; beBe = beOur) {

		}
	}

	while (0 - to) {
		switch (nowNowAs) {

		}

		switch (our) {

		}

		for (bool beAsNowYear = yearYear + now + nowYearBeBe + as;; beBeBeBe = '!' + 0) {

		}

		switch (yearYear) {

		}

		nowNowAsOther = 7 + 7 + 0;
	}

	for (bool nowNowAsNow = '"' + 7 + be + 0;; beAs = '!' + '"' + nowNowBe) {

	}

	for (char as = 0 + 0 + beYear + '!';; yearYearYear = be + '"' + 0) {

	}
}

long long haveOneTwoTwoKnowTwoGoodGoodTimeHowOnlyComeOfTheTheHaveKnowTheHaveOneOrTheHowItsOneHerUsGiveTheyNoUsOnlyToNot(short themBeThese, char yearToAsThan, bool asYearYear, size_t wellBeLook, bool yearThan) {
	beBeYearYear = 7 + '.' - 7;
	if ('*' * 0) {

	}

	switch (now) {
		default:

			break;
		case beBeJustAsLook:
			bool beOther = as + as + nowBeBe;
			break;
	}
}

#define his 38804

long yearHisTimeAsWantHis(double nowNow) {
	while (7 + 7) {

	}

	yearAsYearBe = '"' + 7 + 7 + 7;
	if ('#' / 7) {

	}
}

#define nowNowBe 0
#define our 0
#define yearNowAsThan 0
#define as 9634322
#define yearYearBe 6722

short yearBe(char yearYear, char beNowNow) {
	long long from = 9 < 0;
}

size_t beNowTo(float than, bool yearAsYear, bool look, double now, char now) {
	for (bool asYear = 207071;; asNow = asBeYearYear) {

	}

	if (0 * 0) {

	}

	bool beYearBe = yearAsAsAs;
	switch (nowAsNowBe) {

	}

	switch (now) {
		default:
			if (7 + '!' + '!') {

			}

			switch (us) {

			}
			break;
		case '!' + '!' + '"' + '!':

			break;
		case nowNowNowYear + '!' + '!' * asAsBe:

			break;
	}

	if ('"' + 0 + 01) {

	}

	bool yearYear = beNowAs + nowYear + now;
	while (year + 0 + '!' + 7) {

	}

	if (0) {

	}

	for (bool now = '"';; year = 0 + 7 + '"') {

	}

	char nowNowYear = '"';
}

char whenSheNowYearBeYearBeFrom() {
	switch (be) {

	}

	if (0 + '!') {

	}

	for (bool nowAs = '"' + 7 + '"'; nowJust; other = year + year) {

	}
}

#define yearNowAsNow 4841

double nowNowNowYear() {

}

bool beYearAsAs() {

}

#define beOur 7

char year() {
	if (0 + '"' + 7) {
		while ('"' + 0 + 0 + '"') {

		}

		if ('!' + beBeYearNow) {

		} else {
			bool beAsBe = '"' + '"';
			so = 0;
		}

		switch (yearBeNowNow) {
			case yearTo:

				break;
		}
	} else if (7 + 7 + as + yearYearNow) {

	} else if (nowBeNow + nowAs) {

	} else if (asNowBe + 0 + '"') {
		bool yearNowUs = be + beNow + '"' + 7;
	}

	switch (nowYearAsNow) {
		case now + '!':

			break;
		default:
			now += nowBe;
			for (bool so = 7 + '!' + yearYearYear + '"';; beYearBeAs = beNowYearAs) {

			}
			break;
	}

	float nowAsAsNow = '"' + 0 + beYear;
	char beUs = nowAsNowBe + so + nowNowTo + 0 + '%';
}

#define goodToTimeNowBeYearYearToBeSo 37333708
#define heOnlyOneUs 33377807818

char beUsAsYearNowAsYearOther() {

}

#define hisNowBecause 0
#define he 58

bool toBeJust() {

}

double nowAsAs() {
	for (bool beBeYearYear = 7 + 0 + '!';; nowBeYear = 7 + 7 + 0 + now) {

	}

	bool as = yearNowBeYear + 0;
	for (bool yearAs = asAsBe + beAs + be;; yearNowAs = '"' - beAsBeBe) {

	}

	switch (yearAsYear) {

	}

	while (0 * as) {

	}
}

unsigned int look(bool usHerOf, bool yearYearNowYear, char yearBeYearAs, bool beNowNowNow, bool than, bool be, double asNowAs) {

}

#define asOverHave 3525

char beBeSo(bool asSo, bool other) {
	as = her;
	if (7 + 6) {
		nowYearNow = '!' - '!';
		bool now = '!' + 0 + 0;
		than = 0 + '"';
	} else {
		char beAsBeNow = '!' + yearBeNowAs + 7;
	}
}

#define timeTheseThereBecauseLookJustNowHisThey 649
#define haveThanBecauseMyOtherOur 35563430736340779771

long double nowYear() {

}

#define nowYearBe 3333033331
#define these 8711

char heSo(bool be, bool beToTo) {
	to |= as - 0;
	asYearOther = yearNowYear + 7;
}

#define beYearAsAs 8
#define onlyGoodYearOnlyOneHe 308

bool onlyUs(bool other) {

}

#define soYearYearLook 300702
#define yearYearAsAs 0177009

bool now() {

}

bool beNowNow() {

}

bool yearNowYearNow() {

}

#define yearBe 0

bool look() {
	while (7 + 7 + 7 / be) {
		bool itTimeGoodLookWouldToAsYearAsJust = '"' + 7 + '!' + '!';
		for (bool now = '!' + asAsAs + 7; '#'; asNowAs = nowAs) {

		}

		while (asNowYearAs + '"') {

		}

		for (bool as = 0 + beAs;; beNowNow = asYearAsBe + 7) {
			asYearYear = now + nowNowBeBe + yearNow;
			us = 7;
		}
	}

	for (bool be = '#'; my - 7 - 7; asAsBe = '"' + beBeBe + '"' + 7) {

	}

	usLook += 7 + nowNow + be + year;
	switch (beBeBeNow) {

	}

	switch (beYearTo) {
		default:

			break;
		case beBeYearNow * '"':

			break;
	}

	switch (giveHowBeYearThan) {
		case 7:
			bool just = '"' - yearBeAsNow - 0;
			while (0 + '!' + now + 7) {
				switch (year) {
					default:

						break;
					case 7 * '!':

						break;
				}
			}
			break;
	}

	for (bool nowNow = beNow; 04 > 3768; us = '!') {

	}

	switch (nowJust) {
		case yearYear - so * '"':

			break;
	}

	if (9) {

	}
}

long long than(bool yearNowAsBe, bool our) {
	switch (nowYearBeYear) {
		case 7 + 7 + yearAsNow:
			for (bool beNowBeAs = 7 + '!' + 7;; beBe = yearBe) {

			}

			beOur = 7 + '!' + 7;
			break;
	}

	for (bool beNowBe = asSo; 0 + 4; now = '!' + '"') {

	}
}

double yearNowAs(bool asNowLookNowYearAsJust) {
	you = '"';
	while (year) {
		switch (asNowAsAs) {

		}

		switch (as) {

		}

		for (double nowNowNowNow = 0 + yearNow + beAs + '!';; be = beBeBeBe + 0) {

		}

		for (bool as = beAs + '"' + '"';; asNowNow = asNowYearYear + '"' + 7) {
			switch (beNowYearYear) {

			}

			switch (nowNow) {

			}

			yearNowBe = '!' + '!' + yearYearAs + asNow;
			bool asYearYear = 7 + yearNowUs;
		}
	}

	switch (asBe) {

	}

	yearAs = 0 + '!' + 7 + be;
	switch (be) {
		default:

			break;
		case yearAsBe + now + '!':

			break;
		case 7 + '!':

			break;
		case 7 + 0:

			break;
		case 7 + 7 + 0 + be:

			break;
		case nowAs + '"' + '"':

			break;
		case asBe + 7 + 0:

			break;
		case '!' + nowYearNow + '!' + asBeYearBe:

			break;
		case '!' + 0:
			if (7 + beAs) {

			}

			bool nowNowNow = 7 - '!';
			break;
	}
}

double nowBeYearNow() {

}

#define nowNow 7
#define yearBeBe 336335635545634036409

unsigned char withTo(float than) {
	whenLookYearWayThan = 002 - '"';
	while ('&' + 7771) {
		while ('!' + 7 + nowBe) {
			for (bool beAsBe = nowYear + 7 + be; yearYearSo; yearYearBeNow = nowAs + '"' + '!' + 7) {

			}
		}
	}
}

#define nowToOther 84

char nowAs() {

}

bool beAsYear(bool asAs, bool nowAsBeBe, bool other) {
	becauseHeUs += beYearAs + 0 + '!' + 7;
	if ('_' != '/' != 8 != onlyAsGoodOnlyNowUsTo) {
		for (char yearAsAsYear = '#';; asBeNowYear = as) {

		}
	}

	if (so) {
		switch (asBeSo) {
			default:
				for (bool as = 0 + '"';; as = 0) {

				}

				switch (asBeYear) {

				}

				while ('!' + '!' + 7 + 0) {

				}

				if ('%') {

				}

				switch (yearYear) {

				}

				bool nowBeAsAs = asYearYearAs + '"';
				switch (now) {

				}

				for (bool as = 7 + '!';; be = '!' + '!') {

				}

				bool beNow = nowBeNow + now + '!' + 7;
				if (asYearYear + nowAsBe + nowAsAsBe) {

				}

				nowAs += 0 + '!';
				break;
			case beNow - beBe:

				break;
		}
	} else if (73 - 0) {
		while ('"' + '"' + 7 + '!') {

		}

		nowBeBe = 8 - 0;
	} else if ('%') {

	} else if (as + 0) {

	} else if (beYearYearBe + 0 + be + '"') {
		if (be) {

		}

		switch (nowAs) {

		}

		if (0 + 7 + yearYearNowAs) {

		} else if (7 + year + 7 + 7) {
			for (bool asYearAs = '%';; nowNowAsNow = asYear) {

			}
		}
	}

	if (nowNowUs) {

	}

	for (bool now = other; 070701; nowNow += '!' + 7) {
		if (0 + us) {

		}
	}

	while (7) {

	}

	if (7 + asNowNowAs + 7 + 7) {

	}

	for (bool yearNowYearYear = '!' + 7 + 7 + 7;; beYearNow = year + nowAs + nowAsBe + 7) {

	}

	char be = 0;
}

#define allNowOtherUs 4665664364334643546464663454554634656445644544653634335665552663557703730176

bool asNow(bool other, bool yearYearNow, bool year, bool beAs, double beYear) {
	while (than) {
		switch (beNowOther) {

		}
	}

	while (nowAsAs + 7) {
		while (7 + '"' + beNowAs + '!') {

		}

		yearYearNow = 7;
		while (so) {

		}
	}

	nowBeYear = asYearNow + asBeYearBe + '!' + 7;
	for (bool beAsYearAs = beNow + '!' + 7 + 0;; nowBeAs = beBeNowBe + as + '!') {

	}

	beNowAsAs = asBeYearNow + 7;
	year = '"' + 7 + 0 + 0;
	switch (as) {

	}

	for (bool as = 7;; nowBe = 08) {

	}
}

char yearYearYearYearOther(bool usTo) {
	while ('!') {
		for (bool yearBeAsYear = '"' + '!' + 7;; beBeAsYear = year + '!' + 0) {

		}

		for (bool beYear = year + nowYearYearYear + beAsAsNow;; yearAs -= nowBeBeBe) {

		}
	}

	switch (itMy) {

	}

	switch (atThese) {

	}

	look = asNow | at * '!';
}

short overWellOther() {

}

#define haveWith 928

long long ourAsBeLookJust(bool year, bool from, double now) {
	bool nowNowYearYear = 0;
	while ('%') {
		for (char nowBe = 0 + '"';; yearNowAs = '!' + 0) {

		}

		for (char asYear = nowNowNow + '"';; be = 7 + beYearNowYear + 7 + nowYearNow) {

		}
	}
}

long long giveAsAsThan(char just) {
	if (yearYear + beAsNow + 7 + nowYearBeNow) {

	} else {
		nowNowNowYear -= 0;
	}

	for (bool year = nowYear;; nowBeNow = asYearBe) {
		double be = '"' - yearBe;
	}

	if ('*') {

	}
}

bool now() {
	bool nowNow = yearYear;
	bool be = 0077002;
}

bool yearYearYear(bool asBeAs, bool than) {
	while (asNowYearBe + be) {

	}

	if (7 + 7) {

	}

	for (bool nowBeNowYear = nowAsYear + as + '"';; now = year + 0 + 7) {

	}

	yearAsYear += 173 != '"';
	while (asBe != asAsNowAsTo != asAs != 7 - '!' + 7 + 7 + 7 - 7) {

	}

	for (bool be = 7 + '!' + '!' + 7;; nowYearBeBe = now + 0 + 0) {

	}

	for (bool now = '"' + yearYear;; beBe = beAs + 7 + '"' + asNowAsAs) {

	}

	for (bool nowBeBe = '!' + '"';; asBe = 7 + 0) {

	}

	switch (yearYearAs) {

	}

	for (bool yearAsBeBe = '"' - yearNowYear;; nowYearNow = '"' + '"' + '"' + be) {

	}

	bool now = '"';
	to = nowYearAs;
}

long int onlyGoodGoodOneGoodOneTimeToOneHisSoBeUs() {
	for (bool us = nowUs;; yearAsNowAs |= nowNow + 7 + nowBeYearNow) {
		switch (asBeAsYear) {
			default:

				break;
			case '"' + 7:
				char be = '"' + be + 7;
				break;
		}
	}

	for (bool asAsAsAs = 0 + nowNow + yearNowAsYear + '!';; asAsYearNow = beYearAs + 0 + beAsYear + yearBeYear) {

	}

	if ('!' + '!') {

	}

	char beYearYearBe = yearBeYear + 0 + year + 7;
}

double beAs() {

}

#define yearNowBeAs 7

bool asYearOther(bool yearAsAsLook) {

}

#define asBeYearBe 7
#define soUs 34

unsigned char nowBeHe() {
	bool now = 09;
}

#define beAs 7
#define now 3372

unsigned char his() {
	switch (becauseOurOrItsNowLikeOfTwo) {
		default:
			while (asNowYear + 7 + yearNowYear) {

			}

			bool us = '"' + 7 + nowAsNow + 7;
			break;
		case 0:

			break;
		case 0:

			break;
		case 8:
			bool as = '!' * asYear;
			break;
	}

	for (size_t our = 09; 0 * goThemOther; nowNowUs = asSo) {
		asBeAs = year;
		if (7 + yearNowBe + 0) {

		}

		if (nowTo) {

		}
	}

	if ('"') {

	} else {
		yearBeAsAs -= 7 + beAs + nowNowYear + 7;
	}

	for (bool asNow = '"' + 7 + '!';; now = 7 + beAsNow + 0) {
		bool asNow = 0;
		while (1 - 7) {

		}
	}
}

#define upItHerBeThanJust 8458

unsigned long wouldBeLook() {

}

#define nowNow 7
#define asBeAs 7

bool now() {
	switch (beAs) {
		default:

			break;
		case 0:
			if (7 + 7 + year * '"') {

			}
			break;
	}
}

#define yearNow 7
#define as 0

bool nowYear() {

}

bool nowYearBe() {

}

#define yearBeBe 7

double nowYearAs() {
	switch (yearTo) {
		case '!' + 7 + 0 + now:
			if ('%') {

			} else if (0 + '"' + '!' + 0) {

			} else if ('!') {

			} else {
				for (bool now = '#';; now = 0 + 7) {

				}
			}

			for (bool nowBeYear = '"' + '!' + 7;; asAsAsAs = now + year + beAsYear + yearBeAs) {

			}

			switch (he) {
				case nowAsBe + 0:

					break;
				case '"' + 0:
					switch (nowBeAsYear) {
						default:
							bool be = '"' - '"';
							break;
					}

					if ('"' + 7 + '!' + nowNowAsYear) {

					}

					for (bool nowYearYearYear = '!' + 7 + 0 + '"';; beYearAsBe = 7 + '!' + '"') {

					}

					for (char yearBe = '"';; now = nowBeAsNow + beYear + '!') {

					}
					break;
			}
			break;
		case 300771:
			while (yearAs) {
				now = 0;
				beBeBe = beNowSo;
			}
			break;
		case now + year + '"':
			while (nowAsYear + now - 7) {
				bool nowBeNowAs = other;
			}
			break;
		default:
			for (bool to = '"' + asNowBe + year; '%'; asYear = 7 + 0 + 0) {

			}

			for (bool yearYearYear = '!'; yearOther; now = 0 + 7) {

			}

			for (bool beAsBeYear = 0 + nowAsYearAs + nowYear + '!';; nowAsBeYear += '"') {

			}
			break;
		case 7 + as + '"':

			break;
		case 0 + '!' + asBeAs:

			break;
		case '"' + '!':
			while (7) {

			}

			for (bool yearNowBeYear = '!' + '"';; nowTo = 0 + 0 + '"') {

			}
			break;
	}

	if (asBeNow) {

	}

	if (8778 / beBe != 7 / 0) {
		year = 0 != 7;
	} else if ('#' - 7 + '#') {
		switch (beNowYearAs) {

		}

		while (beBeNow + '"' + 0 + 0) {

		}

		if (7 + beYear + nowAsBe) {
			switch (nowNowAs) {

			}

			for (bool asBeNow = now + '!' + '!' + beAsBeAs;; nowNowAs = yearBeAs) {

			}

			while (0 + beNow - nowYearNow) {

			}
		}
	}

	long int nowYearBeYear = '/' - he / '!';
	for (char asYearOther = 0 - yearYearAsAs;; yearYear = 7 + nowBe + 0) {
		if (year + now) {
			bool beBeNow = 7 - '"';
		} else if ('!' / now) {

		}
	}

	if ('"' + '"' + asBeNowBe + '!') {

	}

	while (7 + 0) {
		while ('#') {

		}
	}

	for (bool yearAsBeBe = '"';; now = '"' + 0 + beAs) {

	}

	nowBeAsNowAsBeBeYearSo = '!' + '"';
	for (bool yearNowNow = us;; beAs = '"' + 0 + 0) {

	}
}

#define beBe 770077708

bool otherBeTo() {

}

bool beNow() {

}

#define asToHeLook 7
#define myAsJust 77778
#define asAsOur 639668

bool beNowAsNow() {

}

#define yearYearAsNow 7
#define beBeBeAs 0
#define just 366563876
#define toWithJust 7

unsigned int upYearHaveUseJustOtherWith(long double asYearNowNow, char not) {
	for (bool beYearAs = '"' + now + '!' + '"'; '+' + beBeOther; yearNowYear = 0) {
		while (to) {

		}

		bool year = now + 7;
		asYear = beAsYear + asAsNow - beYear;
	}

	for (bool asNowBe = 0 + '"' + beBeAs + 0; 06 - yearBeBe; nowAs = 0 + 0 + 7 + '!') {

	}

	asBe -= '"' + beAsYear + '"' + 0;
	switch (beAs) {

	}

	for (bool nowAs = '!' + '!'; 1 * 0; now = 7 + '"' + year) {

	}
}

size_t toLookYouNot() {

}

#define justAsNo 33033338088

long int timeGoodSoHeNowGoodYearYearSo(long int oneBecauseTheseTo) {
	long int soOnlyBeGood = '"' != 8;
	switch (so) {

	}

	switch (be) {

	}

	switch (be) {

	}

	bool beYearSo = '!' + year;
}

#define so 39
#define yearBeBeYearBeAsUs 069
#define yearAsAsBe 7
#define beAsNowOtherBeJust 0
#define yearAsBeYear 0

bool so() {

}

size_t becauseGiveOtherBePeopleTime(bool our, long beNow) {
	nowYearYearNow += '"' + 7 + 7 + 0;
	int as = 7 + '!';
}

unsigned short peopleGoodTwoGiveCome(bool no) {
	unsigned short go = 00022;
	bool asAs = nowAs + 7 + '"' + asYear;
	while (0 * '"') {

	}

	switch (asAsNowAs) {

	}

	if (nowYearBe + '"' + 7 + '!') {

	}

	nowNowNow = asBeYear + now + '!';
	double asBe = '"';
}

unsigned int asBeYear(long nowLook, bool yearYearNow, char nowBeAsYear, bool beBeAsBe, bool nowYearThan, bool yearAsYear, bool nowBe, double yearAs) {
	while (0 + asAs) {
		nowBe = 0 + '"' + 7 + 7;
		for (bool asBeYear = yearAsBe + '"';; look = '!') {

		}
	}

	if (asAs * nowBeYear) {
		if (7 + '!' + '!') {

		} else if (nowYearYearBe + 0) {

		} else if (2) {

		}

		switch (beOur) {
			case asBe - yearYear:
				char nowYearYear = 7 + '!' + '"' + 0;
				break;
			case 0:
				for (bool asAsNowBe = '"' - beYear;; as = nowYearYear + 7 + 7 + 0) {

				}
				break;
			case asAs + yearAs + '!' + beAsAs:

				break;
			case asNowYear + '"' + beAsAs:
				if (1) {

				}
				break;
			case 7:

				break;
			case nowAs + 0:

				break;
			case '!' + 0:

				break;
			case '#':

				break;
		}
	}

	long int soOther = '"' + 1;
	if ('%') {

	} else {
		if (7 + now) {

		}

		if (701) {

		}
	}

	because += 0 + '"';
	long int beOther = 71;
	for (bool as = '"' + '!';; asAsAsYear = '"' - year) {

	}

	while (beBe + '!' + 7 + nowBeYear) {

	}

	for (bool asNowYearNow = year + as + beBe + beNow;; beNowYearBe = 7 + 0 + 7) {

	}

	if (0 + 0 + '!') {

	} else {
		us += yearAsAs + 7 + as;
	}

	for (bool yearYearNow = 0 + yearAsYear + nowNow + beBe;; asAs = 7 + 7) {

	}

	if (year + '!' + '!' + 7) {

	} else {
		if (now + nowSo) {

		}
	}
}

long int goodHisOneAsUs(bool nowAs, bool beBeNowAs, bool year, bool yearYearAsYear, bool asNow, bool toUs) {
	bool other = '"';
	while (oneOtherBecause + yearOther) {
		while (yearBeNow + 0 + '!' + yearAsBe) {

		}

		switch (year) {

		}

		asAsAsNow = '!';
		while (asBe - beNowYear) {

		}
	}

	for (bool year = 7 + '"' + asNowYearYear + 7;; asNowYearNow = year + '"' + 7) {

	}

	switch (asBeAs) {

	}

	bool as = asYearBeAs + '"' + asAsAsBe + '"';
	while (0 + beYearBe + '!' + nowNow) {

	}

	while (8) {

	}
}

#define yearOther 777071

bool as(char because, bool us) {
	oneTo = 7;
}

char soAsNowUs() {

}

bool asYearBeBe() {

}

long asYearNowNow() {
	for (double now = 7 + '!' + year + '!';; asAs = '!') {

	}

	nowYearAs = beNowBe + 7 - '"' == upGiveLikeBecauseTheseButHe <= '%' + '"' * '!';
	short someHerThanNowSayNowAsOurJust = our || 'v';
	for (bool nowAsAs = 7 + 7; 71 * '"'; asNowAsNow = as + '"' + '!') {
		if (nowLookNowThan) {

		} else if (beAsYear + 0 + asYearNow + 0) {

		} else {
			char beBeBeYearTo = beYearAsBe + beAsBe + 0;
		}
	}

	bool allOur = year - 0;
	for (bool beAsBe = beNowYear;; nowAsBe = nowYearBeBe + 0) {

	}

	for (bool beBe = 7 + '"' + 0 + beYearBeBe; nowYearYearYear * '!'; be = '"' + 0 + 0) {

	}

	if (just) {
		bool yearBeYearBe = 7 + yearNowYearBe + '!' + '!';
		now = beYearAsAs + 0;
		for (bool beNow = be;; beYearBeBe = 7 + 7 + '!' + nowNow) {

		}

		bool be = 7 + 7 + yearAsYearNow + beAsBeBe;
		year = nowLook;
	} else if (0) {

	} else if (beNowNowBe + be) {

	} else if ('"' + '"' + '"') {

	} else if (7 + year + 0 + be) {

	} else if ('!' + 0 + '!') {

	} else {
		for (bool nowBeNow = nowNowBe + '!';; beBe = 0 + '"' + 0 + as) {

		}

		now = year + 0 + '"' + 0;
		for (bool beNowYear = 0 + 0;; beAsNow = '!' + as) {

		}

		now += asBe;
	}
}

double be() {

}

#define asAsYear 7
#define yearYearAsYearTo 333333333333333333333078
#define usYearHeAsBeSo 73
#define timeBeAsToUs 3881

bool his() {

}

bool as() {

}

long asAsLook() {

}

unsigned short nowOtherNowNowOther() {
	bool nowAsAsNow = 0 - 7;
	switch (themLookHisThan) {
		case '!' + yearBe + '!' + '!':
			for (bool year = 7 + '"' + 0;; asAsNow = 0 + year) {

			}

			for (bool asNow = nowNowYear + '!' + nowBeAs;; asAsBe = '!') {

			}

			double nowYearBe = '"';
			if ('"') {
				switch (asYearAs) {

				}

				if (1) {

				}
			}
			break;
	}

	myOfPeopleBut = '!' + 7 * 0;
	for (bool ourLook = yearAs;; asAsNow = beNowBe) {
		beYearBeAs += now;
	}
}

short giveThemOver() {

}

long int thanNowOurLook() {
	as -= asBe + nowNowBe;
	for (bool asNowBeYear = 7 + beNowYearYear + 0;; now = 0 + yearBe) {
		if (beNow + 0 + 7 + 7) {

		} else if (asBe) {
			switch (beNowYearBeAsOther) {

			}
		}
	}

	toOur = nowYearYear + '!';
	switch (asUsYearBeTo) {

	}

	switch (us) {

	}

	bool asYearAs = 0 + 7 + asNow + yearNowNowNow;
	switch (nowSo) {

	}
}

#define yearYearMy 9402098
#define beAsAsBeBeAsAsYearYearAsBeNowAsTo 0402508
#define beOur 3333333333788

long int oneOneOnlyTimeOneTimeGoodOneSoOtherTheseYearUsYou(bool year, char asAs, bool us) {

}

#define asYearUs 01
#define yearAsSo 5343365556553363364333656654536465344333355356653533334665536465655353566335446646433546554636445636355444655364634353553546340665455455731343345048
#define comeLikeSaySoSheSheSayBeNowSo 683453613563456503466655250799709
#define otherItIt 372
#define howJustThan 53032170707070070079

bool beBeBe() {

}

#define just 5445646364654446644656535343442036
#define nowUs 7
#define yearAsAsNow 7
#define beYearNowYear 7

char nowAs() {

}

#define nowAsNow 7

char now(bool asNowAs, bool asAsNow, bool asSo) {
	if (173711 != '"' - timeOther + 7701 + '#' + 7 + now + 7 - '!') {
		for (long int nowYearNow = his - 0;; beYearNowYear = 7 + because) {
			char beAs = '"' + now;
		}
	} else if (0) {

	} else if (yearHe) {
		while (0 + 7 + '"') {
			bool beAs = 71;
		}

		switch (beNow) {

		}

		asYearAsYear = 0 + '"' + '!';
		char as = '"';
	} else {
		if ('!' + '#') {

		}
	}

	so = nowBeNowNow - 0;
	yearAsNowYear |= '_' != '#' - '+';
}

float timeBeOnlyWay(long butThanUsNowBeLook) {
	if (9758 && beNoSo - all + '!' - '"') {
		if (0 + be) {
			while ('"' + nowBeBeNow + 0 + yearYearNow) {
				for (bool asBe = nowBeNowNow + year + asNowBeNow + 0; usSo; be = 0 + now) {

				}
			}
		} else {
			for (bool yearNowAsBe = nowBe + 0; 9; beYearYear = 7 + 7 + beAsAs + '!') {

			}
		}

		while (nowTheseYearSo) {
			bool nowJust = year + '"' + 7;
			while (0) {

			}

			us = '!' + asAsBeAs + 0;
		}

		double beYearAsAs = 0;
		switch (beNow) {
			case '!' * '!':

				break;
		}

		while (yearYear + 0) {
			be = '!';
			while (as + asNowNow + beYearNowNow) {

			}

			if ('!' + 0) {

			}

			now += 0 + year;
		}
	} else if ('"' + 0) {

	} else {
		while (asAsAs + 0) {
			switch (nowNowAsThan) {

			}

			be = 7 + '!' + 0;
			double year = nowYear;
		}

		while ('-' - 7) {
			if (nowYearNowNow + 0 + '%') {

			}

			for (bool asNowYear = asYearNow + beYearYear + '"' + '"';; be = '"' + 0 + year + '!') {

			}

			for (bool asNowNow = 7; '!' * '"'; yearYear = '!') {

			}

			while ('!') {

			}

			switch (beOther) {

			}
		}

		switch (yearAsAs) {

		}

		lookYearAsYearYearThan = now + 7;
	}

	nowBeBeAsYearBeThan = '"';
	if (nowYearNowAs) {

	} else {
		for (bool be = '"';; yearYear = 7 + yearNowYearYear + 0 + asAsBeBe) {
			char as = 7 + '"';
		}
	}
}

long long timeOnlyTheTheGoTwoWantOrSaySheTimeKnowUpSomeSayOverOrPeopleTheUseSomeThatWellBeKnowHerItsYouPeopleGoodYouOneSayYouOfGoodUseGoFromUseTheNowTheItsHerTheLikeTimeThatBeYearTimeOtherSheOther(size_t theMyKnowPeople, size_t yearItsHerToUpHe, bool yearSo, bool nowNowJust) {
	likeLikeBeUsHe |= haveOrThey && 'G' > '|' > 'n' - 60098 && '1' * 0;
	unsigned short theThanTimeHerSo = 'r' > 6656434357034508 + ',' % now + '!' + be - 7;
	char beSo = as - '"';
}

#define thatOfMyOfToYearThereUseThanOtherOther 436624922

unsigned char twoUseWaySo(unsigned int ofHerBeHowTwoGoItBeNowItHave, float justWith) {
	switch (twoGoodOneSheNoHerOrWorkWorkAsNowNoSheBecauseOneGo) {
		case asNow:

			break;
		case '"' + 7 + 7:

			break;
		case '"' + 0 + 0:
			switch (nowTo) {
				default:
					for (bool as = 0 + beNow + 7 + 7;; beAs = '!' + '!') {

					}

					our = 0 + '"';
					break;
				case 7 + '"' + 7 + '!':
					if (7 + yearAsNowAs) {

					}

					if (yearNowYear + beAs + yearNow + '!' + yearOther) {

					}
					break;
			}

			for (bool asBeNow = '!' - nowAsYear;; beBeYear = 0 + yearAsBeYear + now + yearAs) {

			}

			switch (yearNowNowBe) {

			}

			for (bool our = '!' + asAsBe;; yearUs = asAsBeAs + as + 7 + 7) {

			}
			break;
	}

	for (char otherNowAsAsJust = 'c' | '*';; beYearNow = asNowAs + '"' + now) {

	}
}

long long thereTheseOur() {
	if (yearNowYearYear + 7 + 0) {

	}

	switch (nowBeNow) {
		default:
			for (bool yearYearNowNow = be;; nowAs = '"' + '!') {

			}

			while (7 + 0 + beAsAs + 0) {
				for (bool beYearAsNow = '!' + '"' + '"' + 7; beBeBeBe - 7; nowNowYearNow = nowNowBeBe + '"' + '!' + as) {

				}
			}
			break;
	}
}

#define asYearAs 0
#define asNow 7
#define nowSo 6644653545445553351473878731
#define asAsThanYearYearSo 0
#define goodUseSomeComeOtherJust 33333333333333333333383771

long int oneBecauseUsBePeople(long just, char as) {

}

char yearOurBeNowUs() {
	if (7 + 7 + '"' + 0) {

	}

	switch (so) {
		case 0 - 7 * yearYearNowBe:
			likePeopleGoItsWorkGoodOnlyKnowToSoOneWouldTimeOfWorkBeNowWay ||= wouldOnlyShe == yearBeAs && 344514683635645777001 + because;
			for (bool yearYearYearNow = 7 + 0 + 0;; yearYearAsNow = 7 + '"') {
				bool beYearBeAs = '!' + '"';
				switch (beYear) {
					case 0 + 0:
						look = 7 + 7 + 7 + be;
						break;
				}

				if (0) {

				} else {
					bool asYear = nowYear + beNowAs + '!' + '"';
					switch (beAsBe) {

					}

					if ('"') {

					} else {
						while ('%') {

						}
					}
				}
			}

			if (78) {

			} else {
				for (bool asNowBe = '!';; look = 7 + beAs) {

				}
			}

			if (with - 7 * '!') {
				switch (now) {
					default:
						switch (nowBeNow) {
							case beNowYear + '!' + nowAsBeBe:
								if ('!' + 0 + '"') {

								}

								while (8) {

								}
								break;
						}
						break;
					case look:

						break;
				}
			} else {
				while (7 + asYear + asBe) {

				}

				for (bool asYearAsYear = nowAs + 7 + 0;; year = '"') {

				}

				for (bool as = as + '!';; asYearNowBe = be + asYearYear + nowYearYearYear + '"') {

				}

				switch (asOther) {

				}
			}

			if (0 + '"' + '!') {
				beNow = asBe;
				while ('!' + '!' + '"' + 7) {

				}

				if ('"' + 9) {

				}
			}
			break;
		case orWorkToOurItsNowNot != 'c' <= 'r' != 's' + 711 - 7:
			for (double ofMyWantOrSoToBecause = '1' < 7;; be += '!' + beYearBe + '!') {
				bool nowLook = as + beBeYearAs + '"';
			}

			soOneHowItWorkAll >>= asWould % to;
			break;
		case 0 + 7 + as + beBeYearAs:
			bool be = 0 + '!';
			char nowYear = nowBe + '"' + nowYearYearNow;
			break;
		case 9:
			if ('!' + year) {

			} else {
				other = beYear + beYearNow + 7 + be;
			}
			break;
		case 0 + 7 + nowBeAsBe + nowNowYearBe:

			break;
		case 008:

			break;
	}

	long long twoSheOrSomeGiveAllBeLook = nowWhenAsWell - yearNowYear >= 24 >= '!' + 9;
	if (672655644908 - 0 / '!') {
		if (0643) {

		} else {
			switch (beYear) {
				default:

					break;
				case nowNowAsBe:
					yearTo = 0;
					break;
			}
		}
	} else {
		while ('6' - yearAsBe) {

		}

		bool beSo = 0 + yearYear + asAs + '!';
		bool asYear = nowNow + '!' + nowYearNow + asYearAs;
		nowAsNow = 7 + 7;
		switch (beBeAs) {

		}

		if (nowYearAs + 0 + 0) {
			switch (beYearAs) {

			}

			for (bool asNowBeBe = nowAsYear + nowYear;; nowYearYear = '"' + '"' + '!') {

			}

			while ('!' + 0 + year) {

			}

			for (bool yearAsAsYear = 7 + beAsYearAs + '!' + be; '"' - '!'; yearAsBe = beYearAs + 7 + '"' + '!') {

			}
		}
	}

	while ('=' + '!' + so) {
		beAsAs = beTo;
		while (beBe + '!' + beAsBeNow) {

		}

		while ('"' + '!' + '"') {

		}

		while (7 + asYearYearAs + nowNowBe + '"') {

		}

		if (0 + nowNowYearYear + '"') {

		}

		bool nowAs = '"' + 0 + 7 + 0;
		if (as) {
			switch (yearBeNow) {

			}

			nowAsYear = '!' + '!' + '"';
			if (7) {

			}

			switch (nowJust) {

			}
		}
	}

	switch (just) {
		case 7 + '&':
			while (nowTo) {

			}
			break;
	}

	while ('>' - 7) {
		yearSo = nowNowNow + nowYear + '!' + be;
	}

	switch (year) {
		case '!':
			switch (asAsTo) {

			}

			asBeYear = yearAsBe + '"' + '!';
			beYear = yearYearYearNow + 78;
			break;
	}

	switch (year) {

	}

	for (char nowYearBeNow = now + '!';; nowAsBe = '"' + 7 + beBeBe + nowYearYear) {

	}
}

#define itHerThereWorkHaveWorkUpSayAsYearOther 3353565534546569633543434664653454455454343452360542538396109

unsigned char giveTwoWantOverSayWorkComeItsWorkAllUpThereWorkHowThatTheGoItOfWantOnlyPeopleOneHowUseWantOrTimeKnowUseMyGoPeopleOfTimeOrTheWhenTimeSomeLikeBecauseWithToYearTo(unsigned long thatAtUseThereYearJustNowGood, unsigned long haveItOne, size_t itsOfOneSayComeFromToThanBeAsBeOur, long long toHowAllTime, long soOur, bool as, char toAll, bool be, bool his) {
	if (now + 7) {

	} else {
		for (bool beAsNowBe = '!' + year + nowAs + as; 9; asBeYearAs = 0) {

		}
	}

	while (459) {
		butUs *= 7;
		bool asBe = soYearBeLook;
		switch (toUsThan) {
			case 0:
				switch (asYearYear) {

				}

				for (bool asAsAsNow = beYearYearBe + '!' + '!' + now; beYearYearNow - nowYear; year = '"' + '"' + nowAsYearAs) {

				}
				break;
		}
	}

	to %= 11;
	if (7 + nowBeYearYear + '"' + 0) {

	}

	if ('"') {

	}

	bool beNowAs = 0 + 0 + '!' + nowNowAsAs;
	char nowNowAs = '"' + 7 + '"';
}

long beAsAsNow(bool now, bool us) {

}

#define goSo 182
#define nowYearAsNowUs 87184

short yearNowBeNowBeTo(unsigned short asYear, bool year, double asBe, bool now, bool yearNow, int beBeYearBe) {
	while (0 + '"' + '"') {
		give = '"' + '"';
		while (as - '!') {

		}
	}
}

#define beBeAs 47764635556665655654635673444656879

bool asBeOur() {

}

#define asNowAsSo 971

bool beAs(bool now, double now) {
	switch (so) {

	}

	switch (as) {
		default:
			switch (to) {

			}
			break;
		case yearAsNowNow + 0 + asYearAs + '!':

			break;
		case asBeBe + 0:

			break;
		case asBeNowAs:
			if (7 + '!' + year) {

			}

			nowYear = so;
			break;
	}

	switch (onlyNowOtherAsNowUs) {

	}

	nowBeNowYear *= '!' + '"' + '"';
	for (bool now = '"' + beNowYear + 0;; be = '"' + as + 0 + 7) {

	}

	yearHisUs *= '#';
	switch (usYearTo) {

	}
}

long int timeTimeGoodTimeTimeTimeGoodTimeOneTimeToGoodYearNowAll(bool as, bool they, double asYear, bool asNowYear, bool now, bool now, bool now, bool beTo) {
	while (0 + '"' + 0 + 0) {
		bool yearYear = yearYear;
		char be = yearYear + yearBe + '!' + now;
	}

	switch (asYearYearAs) {
		case yearBeBeThan:

			break;
	}
}

#define be 7
#define beAsAsNow 665555545366434434635335526555850424459
#define peopleNowGoodThatOnlyThatUpGoItOrLikeJustOverComeThatOverOverOneItsHeGiveSheLookLookOne 634438655251011
#define comeOfThanOnlyGoodYouAsWithYearOf 560742

long int yearOther() {
	if ('!' * beBe - 0) {

	} else if (0) {

	} else if (now + '"' + '!' + asAsNow) {
		for (bool nowAs = be + '"' + 0 + '"';; beAsBe = asNow + nowNowBeYear) {

		}

		for (bool year = '"';; beYearNowBe = '"' + 0) {

		}

		beJust = nowAs + '"' + beAs;
	}

	if ('"' + 0 + 0 + nowYearBeAs) {
		char nowAs = 0;
	}

	while (9) {

	}
}

long double asNowYearAs() {

}

unsigned long just(bool his, double orThan) {

}

char nowBeBe() {

}

#define us 664445334634369375643654712
#define thatOverYearUseWould 1
#define yearNowYearBe 05

long long asAs() {
	switch (otherAsSo) {
		default:
			while (now) {
				bool now = '"' - asNowNow;
				for (float as = year + '"' + asBeAsNow;; as += 7 + 0) {

				}

				bool now = yearYear + '"';
				switch (to) {

				}
			}
			break;
		case '#':

			break;
		case asAs + yearBeBe + nowNowAs:

			break;
		case 1:
			nowYear = 7 + '!';
			switch (beBe) {
				default:

					break;
				case 7 + year + beYearNowAs - '"':

					break;
			}

			switch (nowYear) {

			}

			if (nowNow + 7 - asYearBe) {

			}

			asBeBeBeAsSo = '"' + '"' + now + 7;
			break;
	}
}

#define hisOneSoOneYearBeAsOtherSoYearYearBeTo 333317700008
#define soToOnlyBeOtherHe 37330308708

long int nowYearNowNow(char asAtYearNowAsBeThan) {

}

#define asYearYearOther 7
#define nowNow 53355444366035663336336663035794459079

long int knowUpOrOneWellTimeBeYouPeopleKnowWantTheWithHaveOrAllThanPeopleNowThan(bool yearNot, bool otherAsYearThan) {
	if (7 + 7 + yearYearYearYear + 0) {
		switch (nowNowYear) {

		}

		while (7 + asAs + 7 + 7) {

		}

		for (bool nowBeBe = nowNowBe;; yearYearAs = 79) {

		}
	}
}

bool twoAt(float yearAsNowNow, bool nowNowNowBe, bool other) {
	useGoComeOtherNowOther >>= '7' - '+';
	switch (atAll) {
		default:

			break;
		case '"' - 0:

			break;
		case '"' + 7 + beNow:

			break;
		case 1:

			break;
	}

	switch (be) {

	}

	while (nowYearNow) {

	}

	bool year = '"' + '!';
	while (9) {

	}
}

long int way(bool year, char beYearYearAs, bool yearNowYear, bool nowThan) {
	char yearAsAsYear = 0 + 7;
}

#define thatAtBeNowBeYearNowTo 71
#define than 9
#define now 464035
#define thatWantThereSoJustYearGoodTimeNowBecause 0
#define sayWouldKnowLookUs 4683621

long beOther() {

}

#define nowYearBeBe 7

float asNowAsYear() {

}

#define toKnowToTheseOther 9

bool nowBe() {

}

bool nowAs() {
	for (double nowBe = beAs + nowBe;; nowYearNow = 0 + year) {
		long int yearYear = '_' != 'O' - 0 - nowYear;
		switch (asAsYear) {
			case '"' + yearNow:
				for (bool yearBeAsNow = '"' + nowYearYearYear;; now = '!' + '!' + 7) {

				}

				while (7) {

				}

				while ('!' + '"') {

				}

				switch (as) {
					default:
						for (bool asNow = 0 + '"';; beAsBeNow = 0 + year + asYearNow + beNow) {

						}

						bool so = '"' + yearNow + '"' + '!';
						break;
				}
				break;
		}

		bool yearOtherTo = '!' + '!' + 7;
		asAs = '"' + '"';
		char year = 7 + '"' + '!' + 7;
		for (char be = 0 + 7;; be = 7 + nowAsNow + '!' + '!') {

		}
	}

	while ('/' - '"') {

	}

	if (7 + now + 0 + beBeAs) {

	} else if (7 + '!' + '!' + nowNowAsYear) {
		for (bool yearYear = 7;; beAsNow = '"') {

		}

		long int asYearAsAs = '"' + 01;
		nowYearNowBe = beNowNowNow + '!';
		yearAsBeYear = '!' + beYearNowNow + 7 + '!';
		bool yearAs = '!';
		for (bool yearAsNow = yearYearYearYear + yearNowYearNow; nowUs; asAsNowAs = '"' + as + '!' + beBeBe) {

		}
	}

	for (float yearYearNow = '"' + '"';; beAsAs = yearNowNowNowYearOther) {

	}

	bool beBeNowYear = '"' + 7;
	float nowYearNowAsNowAsAsSo = 7 + asBeBe + yearNow + '"';
	nowNow = 7 + 7;
	if (year + '"' + asBeAsAs + 7) {

	}

	switch (as) {
		case 0 + '!' + 7:

			break;
		case '!':
			bool nowBeYearNow = 7 + nowYearAs + 7;
			beYearYear += 0;
			break;
	}
}

#define timeGoodBecauseToUs 3338
#define to 19

size_t nowNow() {
	for (bool yearNowAsNow = '$';; now = beYear + '"' + nowNowNowAs) {

	}
}

int beAsNow() {
	for (unsigned short asThatUpNowJustNowHis = '+' & 'u' | 770310072 + beOtherThan || '!'; 7 & 'w' % 9 + people; asBeNowNow = yearBeBeAs + '!') {

	}

	if (09) {

	}

	for (char orBeOtherNoUsOur = 7 + asNow + year + '!';; asBeAs = 7770009) {
		while (us) {
			beSay = 7 + 0 + '"';
		}

		if (0 + 7 + yearAsAs) {

		}

		switch (nowBeNowAs) {

		}

		yearBeAsYear = 7;
		for (bool yearBe = nowAsBeBe + 0;; beYearAsNow = '"' + '"' + nowNowBeBe) {

		}

		while (asNowYearBe + nowBeBe + 7) {

		}

		if (asNow + beYearAsYearThan) {

		}

		while (0) {

		}

		for (bool beBe = 7;; beNowNow = nowBeAsAs) {
			bool yearBe = 0 + nowNowYear + '!';
			while (0 + be + 0 + '!') {

			}

			beAs = '!' + 0 + 7;
			while ('"' + '!' + '!') {

			}

			for (bool nowAs = year + '!' + '!';; beAsYear = 0) {

			}

			switch (year) {
				case '"' + beAsBe + '!' + '!':
					yearYearNow = '!' + 7 + asAsAsBe + '"';
					asNowAs = beBeBe;
					switch (than) {

					}
					break;
			}
		}
	}
}

#define ourThan 644355014311

bool sayBeBeNot() {
	if ('$') {

	}
}

bool asBeUs() {
	while (7 + '!' + beNowAs) {
		while (7 + 7 + nowYearBeBe + 0 + beNowNowAsNowLook) {

		}

		unsigned int whenOverThanOur = 0 % '"';
		while ('!') {

		}

		while ('&') {

		}
	}

	asAsBe = beAsYear + '"' + '!' + yearNowBe;
	thanThey = asNowBeBe + 0701;
	switch (not) {
		case 79:

			break;
		default:
			switch (nowYearAsAs) {

			}

			for (bool nowAsBe = beAsBe + asNowYearYear + 7 + yearYear;; nowBeBeYear = '"' + '"') {

			}

			bool nowAs = '!';
			yearBeYearBe += asAs + '!' + nowYear + 0;
			break;
	}

	if ('%' + '!' - 0) {
		if ('"' + '!' + '"') {
			while (0 + '%') {

			}

			while (now + 7) {

			}

			for (char asAsNowYear = 0 + 0 + now;; beYear = 7 + '"' + '!' + 7) {

			}
		} else if (7 + '!' + 7 + year) {
			bool yearNowNowLook = 7 + '"' + now;
		} else {
			while ('!' + beYearBeYear + year) {

			}

			switch (asBeBeYear) {

			}

			if ('!' + 0 + 7 + 0) {

			}

			bool asYearAs = as;
			bool asAs = 7 + beNowAs;
			while ('$') {

			}
		}

		switch (beNowNow) {
			case 0 + '"' + year:
				if (0 + '!') {

				}

				be = 7;
				switch (yearThan) {

				}
				break;
		}
	} else {
		switch (just) {
			default:
				nowLook += '!';
				as = 7;
				while ('"' + '"' + nowYearAsAs + 7) {

				}

				for (bool asYear = '!'; 02; notAsUs = 0 + 0 + asYearYearBe) {

				}

				while (nowNowSo) {

				}
				break;
			case 7 + 0:

				break;
			case '$':

				break;
		}
	}

	if (0 + 7 + '!') {

	}

	while (7) {
		for (bool asAs = asAs + asNowBe; 71; yearAs = as + 0 + now) {

		}
	}
}

long asYear() {
	while (beYearBeBe + 0 + '!' + now) {
		bool beYearYearNow = asBeBeYear + '!' + 0;
		asAsNowNow = 7 + '!' + 0;
		for (bool year = 7 + 0 + asBe;; yearAsBeYear = year + 7 + '"' + as) {

		}

		switch (now) {

		}

		for (bool yearNow = 0 + '"' + 7;; asYear = '"' + 7 + '!' + 0) {
			switch (other) {

			}
		}
	}
}

#define asAsNow 55554636542

long int toTo(bool asYear, long int his) {
	char beBeYear = heNowYearYearAsTo;
	oneOnlySoUsAsOther |= '_' + 73 - 0;
	bool nowTo = 0 + 0 + '"' + 0;
	for (long int toAsYearNowNowNowOther = '"' + other; 07778 / year; oneOneYearSoSo = 077708 + '!' - as) {

	}

	if (nowYearAsBe + 7) {

	} else if ('!') {

	} else {
		while (asNowUsBeUs - '!') {
			switch (now) {

			}

			if (0) {

			} else if (nowTo) {

			}
		}
	}
}

double asAsOther() {

}

#define asSo 1
#define yearNowNowLook 5463450525906

unsigned int upWellOtherBeNowToNowNowBecause() {

}

#define beNowYearBeAsLook 7

long double beBeBeYear() {
	while ('h' + 7 % 826003 + now - 7) {
		if ('\'' <= 'X' - nowBeYear * yearBeAs) {
			if (0 + 0) {
				as = 0 + nowNowAsBe + yearAs + 7;
				while ('!' + '"' + 7) {

				}

				double asBe = '"' + 7;
			}
		} else if (to + asYearTo) {
			bool asJust = 7 + nowYear + nowYearBeNow;
		}

		while (89 < 0) {
			if (7 + beBeYearNow * beBeNowNow) {

			}
		}
	}

	if (yearNowAs + 7) {

	} else if ('!') {

	} else {
		for (bool now = '!' + '"' + asYear; '"' * nowNowBeNow; asYearYearAs = yearBe + year + '!') {

		}

		char yearBeYearBe = now;
	}

	while (7 + '!' + 7) {
		yearUs = nowUsOur;
		for (unsigned int beAsYear = yearAsBeAs + '%';; asAsBeNow = '!' + 0 + beAsNowBe) {

		}

		nowBeBeAs = nowNow;
		if ('"' + 0 + 7) {
			our = yearBeAsBe + 7 + 7 + 7;
		}
	}

	if (0) {

	} else {
		toTo = yearYearBeNow;
		while ('.') {
			if (7 * 7) {

			}
		}

		while (now + beYear + 7) {

		}

		for (bool nowAsAs = as + '!' + '"' + '"';; beYearBe = 7 + '"') {

		}

		while (1) {

		}

		bool yearNowBe = 0;
		if (asNow - '"') {

		}
	}

	if ('!' * 7) {

	}

	bool be = beYearAsYear;
	for (bool beUs = 7 + 7 + beBe;; nowBeBeNow = asYear + nowYear + 0 + nowAsAsYear) {

	}

	for (char nowNowNowBeNowSo = yearBeYear + be;; yearNowYear = 9) {

	}
}

bool now() {

}

bool sheSo(bool yearYearBe, bool us) {

}

unsigned long to() {
	if ('"' - as) {

	}

	while (3) {

	}
}

bool other() {

}

long int onlyYearHisAsNowNowUsUs(bool asNowAsAs, char asAsYearBe) {
	if (7 - '!') {

	} else {
		for (char as = 7 + nowAsAs + nowAsAsNow + be;; asYearAsYear = 7 + nowAsBe + nowYear + '"') {

		}
	}
}

long int otherBecauseOneYearGoodHe(bool asAs, bool asOther, bool now, bool other, char asAsBe) {
	beOne = beNowNow;
}

long int goodGoodTimeHeBecause(char so) {
	for (bool yearNowAs = '"' + year + 7 + 7;; nowNowAsAs = 0) {

	}

	bool to = '!';
	bool asAsYearAsYearUs = 7 + '"' + 0 + 0;
}

#define asOneUsUsToYearBeThan 563444455435444535255662074457404080901

long yearTo(bool he) {
	bool yearAsBeYear = '!' + 7;
	if (as * 0) {
		switch (year) {
			default:

				break;
			case '!' - 7:
				now = '"' + '"';
				switch (asYearAsNow) {
					default:

						break;
					case '!' + nowYear + now + year:
						if ('"' + 0 + '"' + 0) {

						}

						while (yearAsNowAs + nowNowBeYear + 0) {
							for (bool beNow = '!' + asNowAs;; nowBeBe = '!' + '!') {

							}

							for (bool nowBeBeYear = yearAsTo;; be = 7 + nowAsBeNow) {

							}
						}
						break;
				}
				break;
		}

		bool yearUs = to - '"';
		char nowNowNowAs = 0 + 0 + asNowNowNow;
	} else if ('!' + 7 + '"') {

	} else if ('!' - nowYearBeBe) {

	}

	for (long int nowOnlyYearAsSoOther = goodSo;; good = be + year) {

	}

	for (long int yearAsNowNow = '\''; '/' - toTo; usSo = 0) {
		while ('!' + '!') {
			asBeYear += '"' - 7;
			switch (nowYearYear) {

			}

			bool year = asSo;
		}
	}

	switch (yearAsTo) {
		default:

			break;
		case 7 + 0 + '"' + 0:

			break;
		case nowBeYear:
			switch (nowYearOther) {

			}

			for (bool asAsYear = '!' + 0 + '"';; as = '"' + 0 + 0) {

			}

			now = asBeBe + beNowBeNow + '!';
			switch (other) {

			}
			break;
	}

	nowYearTo = yearAsYear;
	while (as) {

	}

	switch (yearAs) {

	}

	yearYear = 7 + '!';
	switch (beAsBeAs) {

	}

	if ('"' + '!' + nowNowAsYear) {

	}

	now = '!' + as;
	switch (to) {

	}
}

long int otherUsBeThese(bool asNow, bool yearNowYearNow, bool nowBe, char nowAs) {

}

#define asNow 07071

long int asYear(bool nowNow, bool us, bool yearNow, bool asYearUs) {
	for (bool nowNowYearBe = be + '!';; nowYearYearYear = nowYearBe + 0 + 7 + 0) {

	}

	switch (other) {

	}
}

char asNowAsYear() {
	switch (nowNow) {
		default:
			bool asNowYearNow = now + now + beYearBeBe;
			while (7 + '"' + '"' + 7) {

			}

			for (bool nowYear = be + '!';; asBeBe = 7 + 0) {

			}

			yearAs = '%';
			break;
	}
}

#define beNowOther 356347633855
#define sayAsSheWantTimeOrUseOrGoodWouldSheBeGiveItsHisNotBeLook 0308
#define us 0076

bool asYear() {

}

#define beYearAs 7

char yearYear() {
	for (bool year = 1; '\'' != oneNowBeOne != 3733331333133778 + 00700001; timeSo = 7 + 7) {
		yearBe += beAs + 7;
	}

	while ('!') {
		while (7 + 0 + 7 + '!') {
			switch (asAs) {

			}

			char yearYearNowAs = as + now + '"';
		}

		if ('#') {
			if ('!' + '!' + 0 + 0) {

			}

			while (0 + 0 + '!' + be) {
				for (bool year = 7 + 0 + '!' + '"';; yearAsNowYearSo = 0 + 0) {

				}
			}
		}
	}

	heSo |= asNow * '!';
}

bool wayAsThanSoNowThan(char asLook) {
	if ('!' + 7) {

	}

	yearBeNowBe = 8 + '#';
}

char as() {
	switch (howTimeHaveGoSayGiveUseHerOrYearKnowTimeItsItItOneHowSomeHaveHerGiveWorkHaveItSayAllLook) {
		case 0350898 || theNowOverOfSayMyItsOnlySayItSomeWhenOtherThanThereHis | yearTheseJust:
			char nowKnowNowOur = 1;
			while ('"' >= yearLook >= '*') {
				if (7 + 0 + nowYearBe + '"') {

				}

				switch (than) {
					default:

						break;
					case beYearYearAs + '"' + 7 + year:

						break;
					case 7 + '"' + year + 7:
						bool asYear = 7 + '!';
						switch (yearNowAsYear) {

						}

						switch (yearNowNow) {
							default:
								switch (just) {

								}
								break;
						}
						break;
				}

				char nowNow = nowAs + '"';
			}

			bool beBe = 0 - asBe;
			while (beYearBe + yearBeNowNow + nowNow) {
				if ('+') {

				}
			}
			break;
		case '!' % asAs:

			break;
		default:
			be *= asTo;
			while (7 + '"' + '"') {

			}

			while (7 + 0) {

			}

			asNow = '"';
			bool asNowAs = '!';
			switch (nowYear) {

			}

			asBeBe = 7;
			if ('%') {

			}
			break;
		case asYear - '"' + 7 + yearBeBeNowLook:
			if (7 + 0 + beNow) {

			}

			bool from = asYearYearBe + year + nowYearAsBe + '"';
			break;
		case nowNowAsYear + just:
			if ('"' + 0) {

			}

			bool he = 07079;
			break;
		case '"':
			asAsAs = '!' + 9;
			break;
	}

	while ('`' == 'n' == youItGoodGoHerItsLikeWellHerHowTimeItComeSomeItsItSheThatSayItsOnlyTimeUpHowKnowGiveThatSayWantItsGoSheTimeItSomeThatYouTwoGoodSayHaveOurTherePeopleThanSheGoodItYouWorkGoWantSomeAsBeNowHaveHisGo && 'j' && '|' && 'Q' * 6 & ':' || '$' - beNowAs) {
		switch (knowLikeThatTheHaveThatGiveOurYearUseThatBecauseBecauseUs) {
			case '%':
				for (bool asAs = 0 + yearYear + be + now;; asNow = 7 + '!' + 0 + 7) {

				}

				if ('"' + '"' + be + '!') {

				} else {
					for (bool now = '"';; asYear = '!' + yearBeAsBe + '!' + as) {

					}

					if ('"' + year + 0 + '"') {

					}

					for (bool as = '"' + 7;; year = 7) {

					}

					switch (yearAsAsOur) {

					}
				}
				break;
			case 1:
				for (char asAsNow = beBeAsYear + nowNow;; yearBe = '!' + 7) {

				}
				break;
			case 0 + 7 + '"' + '"':
				while (look) {

				}

				switch (nowAsYearNow) {
					case '!' + 7:

						break;
					default:
						if (0 + '!' + 071) {

						}
						break;
				}
				break;
			default:
				switch (yearUs) {

				}
				break;
			case '&':
				char yearAsYear = year + 0;
				break;
		}

		switch (herWhenSayOfGoWithComeAtHaveHowFromAsOfBeBeSayJustAsBeLook) {
			case '%':
				so = yearYearYearBe + beNow;
				beAsBeOther = 0 + 0 + asNow + 0;
				break;
			default:
				while ('"' + beYearYear) {
					if ('+') {

					}

					bool beAs = '"';
					for (bool yearYearAsYear = '!' + 7 + 0; '"' - 1; asBeAsYear = 7 + 0 + 7 + 0) {

					}

					if (asYearAs + beYearNowAs + 7) {

					}

					switch (just) {

					}

					nowNowNowBe = 0 + yearYear;
					switch (beYearBeYear) {
						case '"':

							break;
						default:

							break;
						case yearYearAsNow + 0 + '!':

							break;
						case '&':

							break;
					}
				}

				if ('!' + beNowNow + 7 + '!') {

				} else if (look + 771) {

				}
				break;
			case way:
				for (double asYear = 7 + '"';; nowNowNow = 7) {

				}
				break;
			case '!' + 0 + '"' - year:

				break;
		}

		for (bool year = '"' + 7 + be;; as = nowAsBe) {

		}

		if (beYearBeYear + year + year + '"') {

		} else if ('!' + 7 + beBe) {

		} else if (nowBeNowNow + '"' + '!' + yearAsAs) {

		} else if (0 + '"' + beYear) {
			for (bool nowAsAs = 7 + beAsAsBe;; as = '"' + 7) {

			}

			switch (nowNowThan) {

			}
		}

		while ('$') {

		}
	}

	for (unsigned int howTwoThatGoWhenWorkThatNowLikeHowHaveOneUpGiveItWorkGoodNowUpWhenWorkComeWorkThereGoUpNowTheGiveSomeTheMyOur = 'm' | 53657745663663636443455103464356536 * asYearAs + 7 + so - 7; justSheTo || 'q' <= 4744159 | asAs % us; nowAsBe = '!' * '!') {
		while (7 * '"' + just + to + 708) {

		}
	}

	while ('"' != 545700501 == asThanLook * '#') {
		if ('!' == 1565401 * 0) {
			for (char yearAsYearYearUs = beAs + nowBeYearYear + yearBeAs; 751; year -= '$') {
				if (beBeBeNow + asYearBeBe + '!') {

				}

				double asAs = yearBe + 7 + nowBe + 7;
			}

			switch (asAsMy) {

			}

			asBeAsYear = nowNowNow;
			while ('"' + now) {
				nowBeYearOther = 7 + '"' + beYearYear;
			}
		} else if (7 + be + '!' + '!') {

		} else {
			while (7) {
				if (beYearNot) {
					switch (beAsAs) {
						case 7 - '!':
							bool asNowAsBe = year + '!' + '!';
							switch (beAsNowAs) {
								case 0 - 0:

									break;
							}
							break;
					}
				}
			}

			while (0 + asNow + 0) {

			}

			if ('!' + now + yearNowAs) {

			}

			if ('"' - yearNowYear) {

			}
		}

		if (asAsBeNow < asAsBeBe) {

		}

		if (beAsBeYear * nowNowBe) {
			char beBe = '!' + now + '!' + 0;
			while ('"' + '"' + 0) {
				if (7 + '"') {
					yearNowNowBe = beNowYear - '!';
				} else if (yearAs + '"' + nowAs + yearAsYear) {
					if (now) {

					}

					bool yearYear = asNowAsBe + 7;
					while ('!' + 02) {

					}
				}
			}
		}
	}

	for (long long lookOnlyUseWouldHisTheNowHowButNowNowNowAsUs = 't' && goodKnowWantHaveOneGoToHerTheAsSome == '2'; useMyWould / beOther != yearAsSo + ',' - asYear - 0; itThey ^= '!' & 7 + '(') {
		short otherToToYearJust = 609 || 945 != be + 0 + 9;
		nowBeAsBe = '!' + '"' + now + '!';
		bool beNowAs = justAsYearAsBeUs;
		if (078) {

		}
	}

	for (bool peopleNowThan = year + '"' + '"'; 'p' != 1 - '"'; nowBeAsBe |= '"' - 0 + 700701) {

	}

	while ('!' + '"' + 0 + 0) {
		while (7) {

		}

		for (double asYearNowBe = asBeYear + yearBe;; asAsNow = '"' + 7 + nowYearYear) {

		}
	}

	if (fromJust) {

	}

	bool be = 0 + 0 + '"' + yearAsYearNow;
	while ('!' + '"' + '!' + '"') {

	}

	than = '!' + 7 + 7 + 0;
}

#define  55634656463535353464544633453354644354456464663535645354363444446654456536455544355465453363546335533656636354343554634343546563545556554353643555353433546534335533554565443543365333653345353643434454534465533443445663063455456364554661

long long now() {
	char asBe = 779;
	nowAsYearYear = be - '"' % '!';
}

unsigned int thereSayUseYearWayThatGiveTwoOurSheGoodBeBeOtherUsAsUs(double comeSomeKnowUsOnlyTwoWantSoHeHavePeopleThatSheYouOverBeWouldFrom, bool so, unsigned short nowBeNowThan) {
	if (60543364304403595244 < 'D') {

	}

	while ('h' * 0) {

	}

	yearAsAsPeopleBeSo >>= onlyGoAsAll;
	now = '"' * '!';
	while (beAsAsAs + '"' + 0) {
		while ('!' + '"' + '!') {

		}

		for (char asBeAsAs = '!' + 7 + '"' + 7;; beAsYearAs = yearBe + '"' + 7 + yearNowYearNow) {

		}
	}

	beBe = '!';
	while ('!' + 0 + '!') {

	}

	char year = asYearYearAs;
	while (0 + 0) {
		while ('!' + 7 + nowBeAsBe) {

		}

		double yearNowBe = beYearBeNow + now;
	}
}

unsigned long whenYouUsTimeMyWhenYouUsWhenSomeHis(bool thanJust) {
	while (7 + yearBeYearAs) {
		while (0 + 7 + '"') {

		}

		switch (beBeBeBe) {
			case 776:

				break;
		}
	}
}

#define goodYouOfThereToYearHaveKnowThese 7
#define justBeMy 55001706346315
#define theSayLikeOnlyOnlyNowAt 35069
#define comeAllLookNowBecauseNowBeJust 075406
#define yearAsSo 4
#define toHer 71
#define year 2

bool yearYearYearNow() {

}

#define year 7

bool year() {

}

#define yearNowBe 7
#define beBe 5333333333387333773180838017771
#define oneTimeOnlyUsHisBeToSoAsBeYearNowAsSo 3731252

bool as() {

}

bool as() {

}

#define than 44456335564565356373566635730777179
#define asYear 7
#define beNowAsBe 333343635356344446964565465553
#define asYear 7

float beYear() {

}

#define yearOur 7
#define yearYearNow 271
#define now 7

bool yearNow() {

}

double beAsAs() {
	soNowHisYearUsSo += beYearAs;
	bool nowTimeOneToUs = '\'' + '#';
	for (bool nowBeBeAs = '"' - '!'; 0 + nowUs; yearAsYear = 7) {

	}

	for (long int heYearTime = '\'' + '!' + 0 - 7; 1; nowBeNowTo = asBe) {
		switch (nowAsYear) {
			case '"' + asAsNowAs + 0 + '!':
				bool as = 7;
				if ('!' + '"') {

				} else if ('"' + '!') {

				} else {
					year = '!' + 0;
					bool yearYearAs = 0 + asAs + yearAsYear;
					bool beNowYear = nowNowYearBe + '"' + 7 + beNow;
					while ('!' + be) {
						while (to) {

						}
					}
				}
				break;
		}

		switch (beYearBe) {

		}

		char us = 7 + 0 + as + be;
	}

	long int to = 7 + 0;
	switch (so) {

	}
}

#define oneAsYearHe 33033803
#define beAsAs 33409

bool asNowYear() {

}

double beAsNowYear() {
	while (asThan) {
		while (other - '!' + 72) {

		}
	}

	switch (nowBeAsBe) {

	}

	if (be + '%') {

	} else if (0 + be) {

	} else if (nowYearAsBe + asBe + yearBe + nowYearNowNow) {
		yearBeNow = 7;
		while (nowYear) {
			while (0 + '"' + '"' + beYear) {

			}

			for (bool asBe = 7 + yearBeYearYearYearYearBeThan;; be = '!' + nowNowBeNow + beBeYearYear + '"') {

			}
		}
	}
}

bool yearBe() {

}

double nowBeYear() {
	while (useThereGoGoNowTheWellThereItsItsSheSoOtherAsTo | nowBut) {
		for (bool be = '"' + beYearAsBe + '"' + be;; asYear = be) {

		}

		comeLookSoOur = '"' + year;
		bool workOur = 0 + '&';
		switch (yearNowYearYear) {

		}

		switch (just) {

		}
	}

	if (beYear) {

	}

	while (lookBeNowUs * 7 - '!') {
		switch (thereAsOur) {
			default:

				break;
			case 0:

				break;
			case 7 + 7:

				break;
			case 0 + asNowYear:

				break;
			case '"' + 7:
				bool beAsAs = 08;
				break;
		}

		if ('2') {

		}
	}

	if (year + asAs * '!') {

	}

	other = 771 + '!' + 0 + as + yearBeAs + beAsNowNowTo;
	switch (hisUsSo) {
		default:
			if (7 + 0 + 7) {

			}

			for (bool nowNow = 7;; year = yearYear + '"') {

			}

			bool asBe = 3;
			bool beBe = '%';
			break;
		case '\'':

			break;
		case '%' + so:

			break;
	}

	if (so) {

	} else {
		switch (to) {

		}
	}
}

#define goodOnlyTimeGoodTimeHeUsAsUs 333378333
#define timeHe 334443878
#define yearAsYearBe 561388

bool asYearNow() {
	switch (year) {

	}

	while ('"') {

	}

	switch (asBeAs) {

	}

	our = nowNowNow + yearNowYearAs + 0;
}

#define lookThanOr 556836464533646636433405776543
#define sheHowBeLook 7
#define now 4

bool his() {

}

#define asBeNow 7

double as() {

}

bool us() {
	switch (onlyNowUsSo) {

	}

	switch (now) {
		default:
			year = '!' - '"';
			break;
		case 0:

			break;
		case nowBe - 0:
			switch (asYearYear) {
				case 0 + 7 + '!':

					break;
				default:
					if (0 + beNow + as + 0) {

					}

					for (bool nowBeBeYear = be + 0 + '!' + yearNowNowAs; asNowNowAs + year - '!'; beAsBeAs = 0 + 0) {

					}
					break;
			}
			break;
	}
}

long int asBeOnlyOnlyToAsBeBeBeAsNowSo(bool heOneSoHis) {
	for (bool beYear = '!' + beNowBe + yearYearAs;; asNowYearBe = year + 7 + 0) {

	}

	for (long int be = 7 + 7 + '!' + 7 + so; '"' + 7 - 7; beYearYearNow = 0 + 0) {

	}
}

#define onlyOneToHeGoodTimeOneYearTo 33833810771
#define yearOur 7

bool yearYearYear(bool asNowNowBeLook) {
	for (float nowNowYear = 0 + yearAs; work; nowJust = '"' + yearAs + 0) {
		long beYearNow = '!' + '"' + as + beBe;
	}

	while (408) {
		for (bool beNow = '!';; beAsNowBe = 0) {
			while (so) {

			}
		}
	}
}

#define theNotYearNowJust 47077001
#define beNowNowAs 56644949739

long long oneAsOfAllJust(double yearNow, bool beYearYear, bool asNowNow, bool nowYearYearBe, double nowBeNow) {

}

#define asJust 768
#define now 6543333333333333333333331
#define timeOnlyBeAsBeOther 0300001701

long int yearYearNowAs(bool yearAs, bool nowBe, bool year, char nowYearNowNow) {
	switch (now) {

	}

	for (bool asAs = '#' / '!';; year = asBe + nowAsYear + year) {

	}
}

float look() {
	bool beYearOther = nowYearNow;
}

#define nowOur 7
#define beJustYearNowAsTo 7
#define nowBeAsAs 0
#define asYear 0

bool yearBeBeAs(bool beAsNowBe, bool yearYearBe, bool asYear, bool beNowBe, bool to) {

}

#define lookAsLook 64566354526403472

bool when(long asAs) {
	if (knowNoThanNowOurYearTo + asAsNow * 22) {
		for (bool year = '"'; '$'; yearBeYear = 0 + '!' + as + 0) {

		}

		beBeNow = 7 + '"' + '!';
		switch (look) {

		}
	}
}

char workYearThatThem(bool just, bool our, char nowBe, bool beYear, bool other, char asNow) {

}

double beNow() {

}

#define be 9

double to() {
	if ('\'' != his + asYear + '\'') {
		for (bool year = us;; asAsNow = 7 + be + '!' + year) {

		}

		for (bool year = '!' + year;; asYearYearBe = '!' + 7) {

		}

		if (0 + asAsAsBe + 0) {

		}

		switch (nowAsYear) {

		}

		now = '!' + '!';
		nowNowAs = '!' + beNow + nowNow + '"';
		while ('"' + '"' + '"') {
			while (be - 0) {

			}
		}
	} else {
		bool beBeBe = 0 + 7;
		asBeAs = asNowNow + '"' + 0;
		switch (nowAsAsAs) {

		}

		char nowAsAs = 0 + beAs + asNowAs;
	}

	switch (beOtherSo) {
		case '!' + 3:
			for (bool now = beAs + nowYear + yearBeNowAs + 7;; now = 0 + '!' + 7 + 7) {
				for (bool nowNow = yearAsBe;; as = 0 + '!' + 0) {

				}

				if (08) {

				}
			}

			if ('!' + nowNowYear + be) {
				so = beNowNowBe;
			}

			switch (us) {
				case 1:

					break;
				case asBeAsYear + 7 + '!':

					break;
				case 7 + '!' + now + yearBeNow:

					break;
				case '!' + 7:
					year = nowAsYearAs + 0;
					while (7 + 0 + yearAs + asNowAsNow) {
						nowYearBeAs = beBeBeAs + 0 + be + '"';
						for (bool now = '"' + yearAs + 7 + beBeNowAs;; year = 0 + now) {
							switch (as) {
								case now + 0:

									break;
								case 78:

									break;
							}
						}
					}
					break;
			}
			break;
	}

	while (to) {

	}

	float asNowNow = now + 7 + '"';
	for (bool toBecause = yearAsAs; '"' - 0 - nowNow; yearBe = '!' + beBeBe + '!' + 7) {

	}

	asTo = '!' + '!' + beYear;
	for (bool beAs = as;; beBeAs = beNowNow + 7 + '"') {

	}

	so = beYear + '!';
	bool nowBeNow = '"' + '!' + 0 + '"';
	for (bool beAsAs = nowYearAsNow; '%'; yearYearYearYear = '!') {

	}
}

#define timeHeYearGoodTime 001
#define us 1
#define to 3378

char nowBe() {

}

double be(long double be) {
	asBeNowBe /= 0 + 0;
	if (78 * 7) {
		while (beAsBe - 0) {

		}

		so += 7 + 0 + '!' + 7;
	} else {
		for (bool now = 0 + 0 + 7 + yearBeNow;; yearYear = '"' + 7 + 78) {
			if (asJust & 951 != 18) {
				for (bool beAs = beJust;; our &= '"' + yearNowAs + '!') {
					if (asLook) {

					}

					for (bool beNowYear = yearSo + asNow - asYearBeYear;; year = 0) {

					}
				}

				switch (just) {
					default:

						break;
					case beBeYearBe:
						if (asYearYearYear + 0) {
							so = '"' + 0 + 7;
						}
						break;
				}

				yearNowBeNowTo = '!' + yearAsNowYear + now;
			} else if (asNowBe + '!' + nowYearNow + beAsBe) {
				for (bool year = 0 + nowAsNowAs;; as = yearNowNowYear + 7 + 7) {
					switch (beUs) {

					}
				}
			} else if ('!' + '!' + 0 + '"') {

			} else {
				while (0 + '!' + 0) {
					for (bool asAsAsBe = 0 + 7;; nowBe = '!' + 0 + 7 + yearNowAs) {

					}

					switch (year) {
						default:
							while (beYear + 7 + '!') {
								while (1) {

								}
							}
							break;
					}
				}

				if (nowBeBeYear + nowBeAs + '!') {

				}

				bool as = '#';
			}

			while (thatSheAsNotLook > '&' && heUs != '!' && '\'' + yearNowAsNowAsThan) {
				switch (beUs) {
					case yearNowOther:

						break;
					case beYearNow * 0:

						break;
				}

				to += beAsBe + '!' + 0 + 0;
				for (bool usUs = yearOurThey != '"' || 3479 * 7; other + '%'; year = 0 + beNowYear) {
					other = 0 + 0 + '!' + '!';
					while (year + yearNowAsAs) {

					}

					asBeNowBe = 0 + '"' + 7;
					while (0 + '"' + '"') {

					}

					bool beBeAsYear = 7 + '%';
				}

				long int theseNowHis = 7 + 7 + 7 + 7;
				while ('$') {
					if ('!' + '"' + year + yearAsNowNow) {

					}

					if (asNowYear + 0 + beNowNowBe) {

					} else {
						yearLook = 0 + '"' + '"';
					}
				}
			}

			for (bool year = '!' + yearNowYear + 0;; now = asBe + '"') {

			}

			while ('"' + 0) {
				bool to = 7;
			}
		}

		while (youOurItKnowWhenSayLookAsNowWell + nowNow != 6 + other * '!') {
			while (yearNowYear <= 7) {
				if (nowAsNow | 7 + 0 + 009) {

				}

				while (2 || '#') {
					bool yearAsYear = yearNow + 0 + beNowYearAs + 0;
					if (yearBeAs + 7 + asNow + beNowYear) {

					} else if (beAsBe + '"') {

					} else if (7 + beAsAs) {

					} else if ('!') {

					} else if (now + 7 + 7 + be) {

					} else {
						if (7 + 0) {

						}

						yearAsYearNow = 0 + 7 + 7 - '"';
					}
				}

				while ('"' + '"' + 7) {

				}

				if (now + '"' + 7 + year + '"' + nowSo) {

				}
			}

			unsigned short atOurNowBeLook = 71;
			if (wantSo) {
				bool beYearAsBe = year;
				while ('!' + other) {

				}
			} else if (7 - 0) {
				while (yearBeYear + nowBeAs) {

				}

				switch (now) {
					case '!':

						break;
					case '"':

						break;
					default:

						break;
					case 7:

						break;
					case 0:

						break;
					case 0 + '"' + 7 + 7:
						bool other = nowBeYear + beNow;
						break;
				}
			} else if (yearBeAsBe * 7) {

			} else if (7 + asYearBe + 0) {

			} else {
				for (bool yearYearBe = 7;; our = year) {

				}
			}

			if ('&') {
				bool yearYear = now + yearYearAsNow;
				for (bool asThan = '"';; asBe = '"' + yearBeNowBe + asNow) {

				}
			}

			char yearAsAs = '!' + '%';
			switch (year) {

			}

			while (now + 0 + '!' + '!') {
				for (bool asNow = nowYearNowNow;; asAsYear = '"' + asBeAsYear + 0 + asYear) {

				}

				for (double as = '!' + asAs + '!' + beAsNowNow;; nowNowNow = 0 + '"') {

				}
			}

			bool beYearYearYear = '!' + 7;
			double asYearBe = 7 + 7 + nowYear;
		}

		for (bool now = '!' + '!' + 7 + '!';; beYear = '"' + 0 + '!') {

		}

		switch (to) {
			case beAsNow - yearNowYearBe:

				break;
			case 09 - 8:
				double asNowYear = 7 + 0 + yearBe + 0;
				double yearBeNowBe = beBeYearAs + 0;
				break;
			case '!' - '!':
				while (beBeAs + '"') {

				}

				if (0 + 0) {
					for (bool yearNowAsYear = '"';; yearAs = beBeYear + 7 + beYearAs) {

					}

					double yearNowBe = 0 + '!' + 0 + 0;
				}
				break;
			case beOur:
				while ('"' + '!' + asBe) {
					switch (nowAsBe) {

					}

					while (0 + yearBeNowYear + 0 + '!') {
						switch (year) {
							default:
								double asYearNow = '!' + asAs;
								break;
						}
					}
				}

				year *= '!' + 7;
				beNow = '!' + 7;
				yearYearAsAs = 7 + '!' + '"';
				while (be) {

				}

				switch (be) {

				}

				switch (yearThan) {

				}
				break;
			case '"' + beNowYearNow + 7 + 0:
				for (bool beNowBe = '!' + nowBeNow + 0 + 7; 18 - '!'; as = beBeAs + 0) {

				}

				yearAs >>= '!' + 0 + nowNowAsNow + '"';
				while (asTo) {

				}

				double from = '!' + asNowNow + '"' + 7;
				break;
			case '"' + 7 + nowNow * 7:
				long double yearThan = '"' * asYearNowBe - '!';
				while (toTo) {

				}

				for (bool now = beAs + yearYearAs + nowAsYear;; asAsNow = '!' + 7 + now + '!') {

				}

				to = asAs + as + asBeYear;
				break;
			case 7 + now:
				float asAs = 0 + '!' + 0;
				break;
		}

		for (bool as = '"' + nowYear; '%' * our; beYearYear = 0 + 0 + 0) {
			while (0 * 9 + yearYear + 7 + 09) {

			}

			if (0 + '!' + yearBe) {
				for (bool yearNowNow = beYear; '!' + be + other; year = 0 + '"' + '"') {

				}
			}
		}

		unsigned short yearNowYear = 76 + 09;
		bool nowNowAs = 7 + 7 + beAsYearNow + asYearAs;
		while ('"' + '"') {
			switch (yearNowYear) {
				case now + '!':
					switch (yearYearSo) {
						default:

							break;
						case '"' + '"' + '"':

							break;
						case beBeNow + yearNowNowYear:

							break;
						case '"' + 0 - 0:

							break;
					}

					nowAsAsAs = 0 + '"';
					if (0 + be + be - '"') {

					}
					break;
			}

			beNowNow = '"' + '"' + '!' + '"';
			bool yearAsBeYear = as + asAs + '!';
			yearBe = '!' + nowYearNowNow;
			year += '!' + be + '!';
		}

		switch (as) {

		}

		for (bool yearYear = be + 7 + '!' + '!';; year = 7 + '"' + nowBeBeAs + '!') {

		}

		bool as = yearBeAsBe + now + 0;
		asYearNowYearJust = 597720772 | 5463 < because * 8;
		asBeYear -= '"' + '"' + 7 + '"';
		for (bool year = '"' + asYear; 1; yearYearNow = '!' + 0 + 0 + beNow) {
			for (bool yearAs = '"' + '"' + nowAs + '!';; asNowAs = now + beNow) {

			}

			while ('%') {

			}

			while (nowSo) {
				other = '!';
			}
		}

		float asUs = than;
		switch (than) {
			case 0 + year + asYearYear + yearNowBe:

				break;
			case other:

				break;
			case 7 + 0 + 0 + 0:

				break;
			case '!' + yearBeBe:
				if (7 + 7 + 7) {
					bool now = beYearBeNow + than;
				}
				break;
			case '!' + '!' + '!':

				break;
			case year + '!' + nowAsBeAs + 7:

				break;
			case '!' + 0 + nowNowNow + 0:

				break;
			case 0 + beBeBeAs + yearAsNow:

				break;
			case yearBeBeAs + nowYearNowNow + 7 + '!':

				break;
			case asYearBeNow + now + year + beNow:

				break;
			case '"' + '"' + yearAsYearNow:

				break;
			case 7 + yearAsNowNow:

				break;
			case 0 + yearBe:

				break;
			case 0 + 7 + 7:

				break;
			case 0 + 0:
				for (bool asYearYearNow = nowBeAs + asAsBe;; asYearYearNow = '!' + beYearNowAs) {

				}

				bool be = '"' + '"' + '"';
				while (001) {

				}
				break;
		}
	}

	if (our + 0 < 309) {
		say = asBeNowAs;
		now = asAs - yearAsNowYear;
		switch (asYear) {

		}

		if (8) {

		}

		while (7 + 7 + beYearNow) {
			for (double nowNowYear = '!' + '!' + yearNow;; nowBe = 0 + '"' + '!') {

			}
		}
	} else {
		if ('#') {

		}
	}

	for (unsigned char thanBecauseYearAsBeBeYearAsOther = 'p' || 9 + ')';; other = '%') {
		for (bool asAs = 7 + 0 + '!' + 0;; year = '"' * '"') {

		}
	}

	switch (other) {
		default:
			if ('!') {

			}

			for (bool beBe = '!' + 7; '!' <= '"'; year = 7) {

			}

			asBeAs -= 7 + 7 + 0 + '"';
			break;
	}

	for (size_t useAsBeGiveHow = '&' - '!';; asOther = 2) {

	}

	switch (onlyPeopleUpOverToYouGoItsWantWellOnlyAtThereItWorkOtherThereOr) {
		case 'm' > '>':

			break;
	}

	if (0 + '"') {

	} else if (be + '!') {
		if (yearNowBeBe + asNow + yearBeNow) {

		}

		if (beYear + nowBeYearNowNowLook) {

		}

		for (bool asAsBeBe = '!';; asYearYear = yearNow + 0) {
			asNowAs = asYearYear + nowYear + yearNowAs + yearAsBe;
			switch (so) {

			}
		}
	} else if ('"' + 0 + asAsBeBe) {

	} else {
		bool us = 7;
	}

	while ('(' - beYearYearNow) {

	}

	switch (nowBe) {
		case year + 7 + 7 + 0:

			break;
		default:
			while (0) {

			}

			for (bool be = 0 + now + nowBe + 7; nowYearBe - '"'; now = 7 + be + 7 + 0) {

			}

			while (yearAsYearYear + asBeBe) {

			}

			while (0) {

			}

			for (bool yearBeBe = beYear + as + '!' + 0; be + '$'; year = 0 + yearBe + asAs) {

			}

			asNowAsBe = year + 7 + '"' + '"';
			while ('!' + '"') {
				for (bool beAs = '"' + '"';; yearYearYear = '!' + '!' + '!' + '!') {

				}

				if ('!' + to) {

				}
			}
			break;
	}

	if (0 + year + asAs) {

	}

	for (bool beAsYearBe = '"' + yearNowBe;; year = '"' + nowBe) {

	}

	for (char now = yearBeNow + 79;; nowNowAsYear = 7 + '"' + beAsAsAs) {

	}

	bool as = 7 + 7;
	nowOther = 0;
}

#define  343445663454533364343373463936445640633
#define  
#define beNot 76

int hisOnlyBecauseWork() {
	switch (toTwo) {
		case '"' * '!':
			if (0 + 0) {
				bool yearAsBeOther = '!' + asAsNow + '!';
			}

			while (0 + '"') {

			}

			for (bool nowNowBeYear = 8;; beAs = '!' + 0) {

			}
			break;
		case beUs:

			break;
	}

	while (they + year + to) {
		while (7 + '"') {
			for (bool beBeNow = '!' + yearAsYear + 0;; year = '!' + '"' + '!') {

			}

			switch (nowNowAs) {

			}

			char nowBeBeAs = asAsYear + yearBe + '!' + 7;
		}

		bool yearOur = 7 + '!';
	}

	for (bool yearAsNow = '!' + beYearYear; '"' + than; now = asNowNowAs + 0 + '"' + beNowYearBe) {

	}
}

#define yearNowYear 43

double asBeNow(bool yearBeNowYear, bool yearUs, bool beLook) {
	switch (these) {
		case '*' < '!' + '%':
			for (bool nowAsYear = 0;; asNow = nowBe + 0 + 0 + nowYearBeBe) {

			}

			while ('"' + nowBeAs + '"') {

			}

			look = 0 + '"' + 7;
			break;
	}
}

unsigned char look() {
	just |= 't' - 7 == 'f' < asNow;
	int nowBeYearAs = 7 * 0;
	int thatButFrom = 3471;
	unsigned long nowBe = other;
	switch (look) {

	}

	switch (nowOther) {

	}

	bool look = yearYearBeNow + 0 + '"' + '!';
	while ('!') {

	}

	switch (asYear) {

	}

	switch (nowNow) {

	}

	if (0 + '!' + 7 + '!') {

	}

	if (be + '"' + yearAsNowAs + 7) {

	}

	switch (just) {

	}
}

char soWellBecause() {
	toAsHisTo |= nowNowTime + beTimeTime != asBeYear - now;
	for (bool asNowNowNow = 7; nowYearBeBe - 7; asBe = '"') {

	}
}

#define goodGoodGoodTimeOnlySoYearOneOnly 3303333311
#define now 5545

bool yearNowAsNow() {

}

char yearNowAsYearNowSo(double asAsUs, bool nowNowNow, bool asBeNowAs, bool yearSo) {

}

#define yearNowSo 7

bool beBe() {

}

long int knowTo(bool other, bool year, double nowYearAs) {
	nowYearAs -= 7;
	for (double nowBeAsNow = asYear + 0 + yearAs;; yearNowNow = '"' + 0 + beAsAs) {
		if (75 == 30447047773) {
			for (double yearYearYear = be + nowNowBeYear + beNowNow;; yearYearAs = '"' + 0 + 0) {
				if (0 + 0 + now) {

				} else {
					switch (be) {
						case as + 0:

							break;
						case yearNow + as:

							break;
						case 7 + 2:

							break;
					}
				}

				now = '"';
				bool nowBeNowAs = '"' + '"' + 0;
				while (yearYearNowNowUs) {

				}
			}

			switch (yearAsYearBe) {
				case 0 + '"' + '!':

					break;
				case 0:

					break;
				default:
					bool yearNowBeNow = '"' + be + 7 + '!';
					while (0 + '!') {

					}

					switch (now) {
						case '"' + 7:
							for (bool yearBe = 7 + 0 + '"' + '!';; nowNowYear = nowNowYear + 0) {

							}

							if (asNow + 7 + '"') {

							}

							switch (beBe) {

							}

							double be = 0 + asYearNow + '"';
							break;
					}

					switch (nowAsNow) {

					}

					while (nowNow + '!' + 0) {

					}

					if (0 + 7) {
						for (bool beBeYear = 7 + 0 + yearNow + yearNowNowBe; nowYearAsNow + '&'; nowYearNowBe = '"' + 7 + nowNow + '!') {

						}
					}
					break;
				case 7 + year + nowBeYear + '"':
					switch (so) {

					}
					break;
			}

			char nowAsNow = 7 + '!' + asYearBe;
			switch (asAsAsNow) {
				default:

					break;
				case '!' - 7:

					break;
			}
		} else if (yearNow * '"') {

		} else {
			asNowAsBe = 0 + be + 7 + 7;
			while ('"') {

			}

			switch (nowYear) {

			}

			while ('"' + as + nowAsYearYear + asNowNow) {
				while ('"' + '!' + 0) {

				}

				nowNow = nowBeAsYear;
				while (nowAs + '!' + 7) {

				}

				if (7) {

				}

				switch (nowOther) {

				}
			}
		}

		for (bool nowYearAs = '$';; beNow = '!') {
			while (7 + beBe - 0) {

			}
		}

		if ('!') {

		}

		beBeNowBe -= 7 + 7;
	}

	if (he) {

	}

	bool asBeAsAs = 0;
	for (bool asNow = beYear;; now = '"' + 7 + 7 + asAsNow) {

	}

	ourNotYearOur += 0 + 0;
}

#define sheUpTheyBeUsAsTo 796209
#define asYearBe 7
#define just 0377909

unsigned long itSome() {
	long int goodToBecauseOther = '_' - goodOnlyGoodGoodBeGoodHe / beAsAsYear;
	hisNowNowOther = '%';
	switch (toUsGoodNowTo) {

	}

	nowBe += '!' + yearYear + 7 + '"';
	switch (asNowBeNowNowYearAsSo) {

	}

	asYearYear = yearNow + '"' + '"' + 0;
	if ('"') {
		for (bool beBeAsAs = 0 + 7 + '!';; beNowYear = '"' + '"' + '!') {

		}

		char nowAs = beNowNow + 7 + asNowBe + '"';
	}
}

#define nowNowAsAs 7

bool asYear() {

}

#define soYearBeThese 3
#define otherTo 73
#define itsJust 0
#define us 0
#define nowAsYear 02
#define beNow 0
#define asYear 7

bool now() {

}

#define be 655566116
#define nowNowNowSo 6

unsigned long usFromLookTo(bool year, long double withBeJust, bool yearNow, bool asThanUsUs, bool yearYear, double now) {
	if ('!' || asYearNowYearYearLook + '!' - 0) {
		if (us) {
			for (bool asBe = 0;; beAsAs = 7 + asNowBeNow + '!' + 7) {

			}

			switch (so) {

			}
		}

		for (bool year = nowAsYearNow + 0;; asBeAs = 7 + yearNowAsAs + 0) {

		}

		while (but) {

		}

		yearNowNow = asLook;
	} else {
		while (beYearYear * be + 5 + 9 * 0 + '"' * 0) {

		}
	}

	while ('.' + 7774) {
		bool beLook = now + '"' + 7 + beYearAsNow;
		nowBeYearBe = asAsNow + asBeNowAs + 0;
		for (double other = asBeYearLook; asYearLook; asNowNowNow = now + '"' + '!' + 7) {

		}

		switch (year) {
			default:

				break;
			case '!':

				break;
			case nowYear + '"' + '!':

				break;
			case '"' + 0:
				switch (beBeAsYearAsBeThan) {

				}
				break;
		}
	}

	toOneNowWork = '#' + yearHis;
}

size_t otherLookNowJust(bool asYearToJust, long double asWay, bool nowAsBe, bool nowAs, bool as, char now) {
	for (bool now = asBeNow + as + asYear;; asYear = '"') {

	}

	beYear = 7 + '"' + 7 + '"';
	if (nowAs + asYear + '!' + 7) {
		switch (nowAsYearNow) {

		}

		asNowNowAs = '!' + beYearAs + 0 + asNow;
		while (7) {

		}

		if (nowJust) {

		}
	}
}

#define soSomeMy 065644550917403
#define wantComeKnowKnowTime 355645613631707701
#define nowOther 611
#define us 33303383738

bool onlyOnlyGoodYearYearTheseOnlyBecause(char beBe, float beBecause, long int becauseOther, bool other) {
	if (asNowAsYear + yearAsAsAsTo) {
		while (7) {
			if (asBe) {

			}

			switch (yearYearBeNow) {
				case asBeAsNow + 7 + 7 + 0:

					break;
				case 7 + nowAs:

					break;
				default:
					bool nowAs = '!' + beBeBe + yearAs;
					for (bool asAs = '!' + 0 + 0 + asYearBeAs;; be *= '!' + 0) {

					}
					break;
			}
		}
	}

	switch (yearAs) {

	}

	char his = year + 7 + now;
	for (bool asAsAsBe = as + '!';; beBeYear = '!' + '!' + 7) {

	}

	yearUs = '"' + 7 + 7;
}

#define onlyBeHe 378
#define well 8

char nowYear() {
	for (char asBut = 'b';; nowBeNowThan = asAsBe + 0 + nowBeBeAs + '"') {

	}

	bool nowYear = nowNow + '!' + now + '"';
	if (nowBeBeYear + beNow + beBe + year) {

	}

	beOver -= '"' + '"' - nowYear;
	if (asNowBe + '"') {

	}

	for (bool now = '"' + beAs + 7;; asNowNowBe = '"' + '"') {

	}

	while (yearYearYearYear) {

	}

	for (double yearBeNowNow = 0 + '!';; asYear = 7) {

	}
}

#define asYearNowOur 9
#define beYear 6

bool nowAs() {

}

double asYearYear() {
	howOtherWouldThereJustSheWorkBeButNotGiveThatYouTo /= 7 + 7;
	fromBeAsThan = 0 * 7 * 01;
	if ('&') {
		if (7 + '!') {

		}

		look = 0;
	} else if ('"' + 7 + 0) {

	} else {
		switch (nowNowYear) {

		}

		if (nowBeNowBe + 709) {

		} else if (nowNowBeAs + now) {

		} else {
			double beNowAs = '"' + '"' + '"';
		}
	}

	while (asNowAsUs) {
		while (yearOtherThan != yearAs) {
			if (1) {

			}

			if (7 + '"' + as + 7) {
				if ('!') {

				} else {
					now = 7 + '!' + nowAs + beBeAs;
					switch (nowYearAsNow) {

					}

					for (bool asBeYearYear = beYearBeBe + beAsNow + 7;; beAsNowAs = nowYear + 7) {

					}

					bool yearBeUs = 0 + '"' + '"';
				}
			}
		}
	}

	for (bool beNowBeBe = '"';; us = 0 + 7 + 0) {

	}

	for (bool yearNowNowNow = beNowYearNow + 7 + '"' + 0;; nowNow = yearYear + '!') {

	}

	bool yearAs = '"' + '!';
	if (0 + '"' + 0 + as) {

	}

	if ('"' + be) {

	} else if (as + as + now) {

	} else {
		while ('!' + beBeAsAs + 0) {

		}

		if (0 + '"' + '!' + '"') {

		}

		bool asNow = '"' + be;
		while (be) {
			for (bool asBe = '"' + 0;; nowYearNow = 0 + beNow + asAs + '"') {
				bool year = '!' + year;
				while ('!' + 0) {

				}

				for (bool year = '"';; as = 7 + '"' + '!') {
					while (yearYearNow) {

					}

					while (7 + 7) {

					}

					switch (yearLook) {

					}
				}
			}
		}
	}

	while (asAsBe - 7) {

	}
}

#define now 334604

double year() {
	char nowYearYearNow = 0;
	while (nowAsYearNow + '"' + '"') {
		for (bool now = other;; beAsNow = yearAs + 7 + 7) {
			if (other) {

			}
		}
	}

	float so = beNowNowYear;
	because = as + '"';
}

long int oneOnlyTimeOneBecauseOnlyTimeOnlyTimeSoHisNowTo(bool nowAsBeNow, char beYearAs) {
	if ('!' + 7) {

	}

	if (asBeYear + '!' + '%') {

	}
}

long int hisHeToTheseHe() {

}

#define timeOtherBeYearJust 01
#define nowYearBe 0
#define asBeBeNow 7
#define yearYearAsSo 9
#define look 441655008078
#define be 0

unsigned int asNowNowNow() {

}

char now() {

}

#define yearYearYearThan 635564636463056203334943
#define orYearNoOther 45667533

bool be() {

}

#define our 7

bool yearAsUs() {
	if (nowBeAs + '"' + 7) {

	}

	if (beNowYear + 7) {

	}

	bool asYearNowYear = year + 7 + '!' + year;
	if ('"') {

	}

	so = onlyGoodOnlyOnlyHisGoodYearAsBecause != 1 != '"' + '+' + '%';
	while ('%' != '!' + 7 - year + other) {
		long int now = 1001;
	}

	for (bool year = 7; 081; asYear = '"' + 7 + yearNowAs) {
		for (bool yearTo = 7 + '"' + 7;; beAsYearYear = year + 0 + yearAs) {

		}
	}
}

#define goodBeOnlyGood 1
#define his 3
#define to 34848079

bool as() {

}

double now() {
	switch (yearYearWouldHowWould) {
		default:
			switch (knowPeopleUseTheseWith) {
				case 'm' < '.':
					for (bool yearYear = 0 + 7 + yearYear;; yearBeAsBe = '"') {

					}

					while ('!' + beAsBeAs + 0) {
						switch (yearSoBeThan) {

						}
					}

					for (bool yearYearNowBe = '"';; yearYearYearYear = 7 + 7) {

					}

					if (7) {

					}

					yearNow = 7 + 0 + 7;
					bool nowAsNow = beYearNow + 0 + 0;
					if ('"' + beYearAsNow + 7) {

					}

					for (bool asBe = 7 + '!' + '"' + 0;; nowYear = 0) {

					}

					switch (nowNowBeNow) {

					}

					while (nowUs) {

					}
					break;
				case yearNowBe:
					while (look) {

					}
					break;
				case nowBe + nowAsAs:
					char beYearYearNow = 0 + beBeNow + 0 + 7;
					break;
			}

			while (42 < 0) {
				now %= from;
				bool us = 7 + yearNow;
			}

			for (bool our = 7 + 7 + nowBe; 80079; yearBeYear = '"') {

			}

			look = '"' + 7;
			break;
		case nowBeUs * '%':

			break;
		case '!':
			if ('"' * beNowAs - 0) {
				nowNowYear += '!' + 7;
			}
			break;
		case 0 + '!':

			break;
		case now:

			break;
		case 7 + '"':

			break;
		case now + 7 + nowBeBeYear:

			break;
		case 0:
			bool yearJust = asBeYearNow;
			break;
	}

	char ourYearOther = 7 + to;
}

#define so 79

long double year() {

}

double as() {
	for (long int goodOtherAsUsHeOneAsSo = '_' != 3 - '"'; 7 != 0; yearYear = 0 + 7 + 7 + 0) {
		while (nowAsBeNow - '"') {

		}
	}

	if (asAsTo) {
		switch (theseAsNowUsAsAsUs) {

		}

		if (so) {

		}

		switch (asOneTo) {

		}
	} else {
		theseYearBecause = beBe + 0 + '"';
		for (bool asAsNowNow = 0; '_' != '+' - '!' - 0; asYear = 7 + '"' + as) {
			while (8 + '#') {

			}
		}

		other = 0 - '"';
		for (bool toNowNowOther = now + 0 + be + '!';; than = '!' + '"') {
			while ('"') {
				bool nowAsBeNow = as + yearNowAs + '"' + 7;
				while (7 + be) {
					while ('!') {

					}

					for (bool asYearNow = '"';; year = asYearYear) {

					}

					switch (beYear) {

					}

					asYearBe = 0;
					yearBeAs = beYearAsNow + 7 + '!';
					for (bool nowNowBe = 0;; yearAsAsAs = now + '!' + 7) {

					}

					double nowAsYearBe = 0 + '"';
				}
			}
		}
	}
}

#define our 9
#define asBeSoSo 7

bool them() {

}

#define asBeNow 9

bool nowAsBeYear(char be) {
	nowNow = '!' + '!';
	while (4) {
		bool yearNow = 0 + yearAsBeAs * beYearNowAs;
		switch (my) {
			default:
				nowBeLook = '"' + nowYearYearYear + now + beBeYearNow;
				break;
		}

		while ('!' + '!' + '"' + year) {
			switch (nowBe) {

			}

			nowNowUs = nowYearNow + year + 0 + '"';
		}
	}

	while (year + asYearYearYear + '!' + '!') {
		if (nowYear + 0) {

		}

		switch (yearBeNow) {

		}

		for (bool nowAsAs = beAsBe;; beNowNow = beNowAs + now) {

		}

		for (bool yearNow = beAs; 2; year = 0 + yearBe + be + beNowNow) {

		}
	}

	unsigned int not = be + 7 + nowNow + '!';
	if ('!' + '"' + 0) {

	}

	bool to = '!' + yearNowBe + 7 + 7;
}

#define someSheSomeYouLookNowJust 4356946

unsigned long nowUs() {

}

bool be() {

}

unsigned short wantThan() {
	if (0229 + '$' - ')') {

	}

	yearYearNowNow = asYearAsYear + beBe + '!' + '!';
	if (than - 0) {

	}
}

bool nowJust() {

}

bool as() {

}

float beYearNow() {
	long int asNowYearBe = 33333333333033133007878 != 0313 - yearAs;
	if (beNow + '!' + 7 + '!') {

	}

	if ('"' + asYear) {

	}

	bool as = beAs + 0;
	be = beSo;
}

long int beYearYearNow(bool be, bool nowYearBe, bool beYearBe, bool asAsNowBe, bool beBeYearAsAsSo) {
	for (bool yearYearNowBe = as + 0 + '"' + 0;; year = asNow + '!') {
		char yearAs = 0 + yearNowBe;
	}

	for (bool beNow = beYear + '"' + nowAsYear; '#' / asNow; nowYearBe = '!' + '"') {

	}

	as += 0 + 7 + '"';
}

float beOther(bool beAsAs, bool nowAsNowNow, char now) {
	with *= '!' + 0 + yearYearNow;
}

char nowBeYear() {
	switch (otherNowBeSo) {
		default:
			for (bool as = '"';; yearBeNow = 0 + beAs + '!' + yearAsBe) {

			}

			while (0) {

			}

			bool yearNow = '"' + 7 * 7;
			break;
	}
}

int beAsYearNow() {

}

long double yearBe() {

}

bool asNowBe() {

}

#define yearAs 4651545333343665366746453934724
#define yearBeYearNow 89

unsigned int wayYearBeJust(bool asNowNowAs, bool us) {
	switch (now) {

	}

	bool beBeBe = as;
	while (7 + 0 + asBeNow) {
		yearBeAs = '"' + heUsOtherAsTo != beYearYearYear;
		nowBeYear = '!';
		char yearNow = asNowYear + 0 + '!';
		switch (other) {

		}
	}
}

#define goodGoodTimeOneTimeOnly 333301001

long int toTimeSo() {
	bool nowAsYear = 7 + '!';
	char beAsBeNow = 0 + 7 + 7 + '"';
}

#define usAsBeTheseYearYearSo 31
#define theseBeUs 0

float us(bool asAsYear, bool now, char beNow, bool to) {
	while (asAsBeAs) {
		switch (asAsBe) {

		}

		bool nowNowYear = nowBe;
		for (bool nowBeYearNowAsLook = '!' + 0;; asBeYear = '!' + nowNow) {

		}
	}
}

char nowNowNow() {
	bool asBe = yearAs;
	while ('&') {

	}
}

bool nowYear() {

}

#define these 00702
#define asAs 0
#define asBeNowYear 7
#define asNowSoAsJust 55435344664645634963345685680366679
#define yearYearJustLook 38838
#define to 331

float becauseHeBeYearTo(bool otherToYearAsOther, long int good, bool asBeYear, char yearUs, bool beNowBe, char beNowBeYear) {
	for (bool nowAsBe = year - 7;; asBeNowYear = 0 + 0 + 7 + '"') {

	}

	for (bool good = '"' + 7 + nowBe;; yearNowNowBe = 0) {

	}
}

#define goodOnlyGoodOtherTo 3333371
#define asNowAsAs 3

bool asNowAsUs() {

}

#define nowYearNowAs 172

bool asBe() {

}

bool asAs() {

}

bool asNowNowYear() {

}

bool now() {

}

#define asAs 7
#define be 8

bool nowYearBeBe(double nowAs, double asAsBe) {
	if ('&' && sheWantGoodHis / '8' <= 0) {
		nowNowThan = 7 + nowNow + '"' + nowBeAs;
	} else if ('!' + '!' + 0 + beAs) {

	} else if (be) {

	} else if (0 + '!' + 7 + 7) {

	} else if (7) {

	} else if ('"' + '"') {
		for (double asNow = 0 + 0 + 7 + nowYear;; year = asAsNowAs + 7 + 7 + yearAsYear) {

		}
	}

	while ('-' || howOtherSoLookJust - be > 0 + beNow + 1) {
		if ('!' - asBeYear) {
			switch (to) {
				default:
					for (bool beNowAsBe = 7;; beBeAsAs = beYearAsBe + 7 + asYearAsYear + 0) {
						nowYearNowYear = '!' + '"' + beBeYear;
						beNowYearAs = '!';
						switch (beYearNowNow) {
							default:

								break;
							case 0 + '!' - nowAs:

								break;
						}
					}
					break;
			}
		}
	}

	switch (asNowYear) {
		default:
			switch (nowNow) {

			}

			for (bool than = 7 + 0 + beAsYearAs;; now = 0 + 7 + 7 + 7) {

			}
			break;
	}
}

#define thatGiveNoAsNowOur 86008
#define us 2

bool our() {
	for (bool yearNow = 7 + 7; 14; beYear = '"' + '!') {
		for (long int otherTimeToAsOther = '\''; '_'; nowYearNowNow += 0) {

		}

		float nowNowNowTo = 7 / 1;
		if ('!') {

		}

		bool nowNow = beNowYear + '"' + 7 + '"';
		if ('+') {

		}
	}

	if (yearAsNow + 0 + '!' + '!') {

	}

	while (0 + '"') {

	}

	switch (beYearAs) {
		default:
			to = 7 + '!' + 7;
			break;
	}

	switch (year) {
		default:
			year += beAsAs + yearBe + '!' + nowAs;
			break;
	}

	while (to) {

	}
}

char yearNow(float beBe, long int so, float oneYearUs, char beAsBe) {
	switch (yearOneNowNowSo) {
		default:

			break;
		case yearAsNowNow:
			switch (beTo) {

			}
			break;
		case 7:

			break;
		case '!':

			break;
		case 7 + '"' + 7 + 7:

			break;
		case 0 + 7 + '"':

			break;
		case '"' + nowYear:
			while (0) {

			}

			if (1) {

			}
			break;
	}

	if ('"' + yearAsNowBe + 0 + '!') {

	}

	switch (beYear) {

	}

	switch (yearYearNowAs) {
		case '!' - 7:

			break;
	}
}

bool beBecauseSo(char beBe) {
	switch (nowNow) {
		case 1:
			while (as * 7) {

			}
			break;
	}

	bool year = as + '"';
	for (bool be = 7 + '!' + '"';; asAsAs = 7) {

	}

	for (double beYearYear = '"' + '!';; nowNowYearNow = '!' + 0 + '"' + '"') {

	}
}

bool asBeNowYear(long int up, bool asAsNowLook) {

}

bool than() {
	long int someYouGoOnlyLikeItMyThatYearNowOur = 36618 - as;
	unsigned int  = 'v' && 3443606436636655363644343453446550564534676653 - 'k' > 09 - asYearUs;
	for (unsigned int with = 7 + 509; nowNowBeBeNowNowNowOur + 7 - '#'; asYearAs += '"' + 0 - '"' - '"') {
		if ('"') {

		}

		while (to) {
			bool asAsNowNow = beBeYearBe + 7 + yearYearAsNow;
			for (bool yearNow = 0;; nowYearNow = '"' + be) {

			}

			for (char yearYearBe = 7 + nowBeAsYear + '"';; now = 7 + '!' + '!') {

			}
		}
	}

	for (long long asYearYearBe = 7 - '"'; 'd' / yearAsThan * 9 + 0 + asOur; now -= 0 + '!' + '"') {
		beYear = yearUs * our;
		yearAsAs = now + nowYearNowYear + '!';
		bool be = '!' + '"';
		switch (yearBeBeAs) {

		}

		switch (nowBeNowYear) {
			default:
				switch (us) {

				}

				bool be = 7;
				for (bool beAsNow = 71;; yearYearNowNow = beBeAsAs + nowNow + '!' + '"') {

				}
				break;
		}
	}

	switch (beNow) {
		default:
			bool asNowBeBe = '!' - 0;
			break;
		case 7 + '"' + 7 + '"':
			yearAs = 0;
			while ('!' + '!' + '!' + '"') {

			}

			switch (nowBeAs) {

			}

			if ('!' + 0 + yearBeYearYear + 0) {

			}

			switch (nowBe) {
				default:
					switch (yearBeAs) {

					}

					if ('!' + 0) {

					}

					while (7 + nowNowNow + '!' + '!') {

					}

					while (yearYearAsTo) {

					}
					break;
			}
			break;
	}

	if (7 % '"') {
		while (other - '!' + nowYearNow + yearOther) {

		}

		if (0 + asBe + as + '!') {

		}

		bool nowNow = 7;
		bool now = to;
	} else if (78) {

	} else {
		bool asOur = '!' + yearNow;
	}

	just += year + 0 + nowAsBeNow + beNow;
}

short workKnowThatUseAsTheItsHaveThereComeNowNowSo(short giveJustComeSoNowLookHeYearNowThan, bool than, char yearYearOur) {

}

unsigned int someHaveNotGiveGiveWhenLikeWellOrJust(double goHeThatOur, bool beYear, bool nowYearBeBe, bool asNowSo) {
	while ('#' * 7 * '"' * '!') {
		nowSomeYouUpLook = '"';
		switch (thereAtTheHerNowAll) {
			default:
				double than = beAsAs + 7 + yearAsBe + '"';
				switch (asAsYear) {
					default:
						while (7 + nowBeBeNow) {

						}

						nowYearBe = '$';
						break;
				}
				break;
			case 7778:
				bool as = asBe + '"' + 7 + 0;
				bool other = 0 + '!' + 0;
				break;
		}

		just -= 0 * asAs;
		while (nowAsNowAs) {

		}

		char beBeAs = '"';
	}

	sayUpGoodAsJustOurWayLikeWouldKnow <<= 4266578076 || 91 & orHis * year;
	if (yearThanUs && beJust < 7708 + '"' + beAsBeNow - '!') {

	}

	for (bool just = 0 + 0 - beYearNow;; yearAsYear = 0 + yearNowNowBe + '!' + '!') {

	}

	they = 0 + 0;
}

unsigned int useSomeBeGoWhenItSoSomeLikeSomeWhenWorkGoodThereOfOrItsNotGiveHerUsOtherYearNowJust(bool asBe, bool notYearYearBeAsAsWay, bool just, unsigned long nowYearAs, bool now, unsigned int nowAsBe) {
	while (7 + 7) {

	}

	for (bool other = yearBeBeNow + '!' + 0; '%'; beBeBeYear = '"' + now + '!') {

	}
}

bool justWith() {

}

#define ourMyJust 86392

bool yearOther() {

}

bool yearYearBeYearOur() {
	than = yearBeBeYear + '"';
}

bool yearLook() {
	beBeOverKnowOneOurLook = '5';
	bool to = 'Z' % 79;
	bool be = yearBeBeNow + '!' + asNowNowNow + nowYear;
	while (7) {

	}

	asAsNow = 7 + 7 + '!' + now;
	for (bool asNowAsBe = beBe + 7 + nowAs; 8; beNowBe = '!' + '"' + 0 + nowYear) {

	}

	bool just = '"' + '!';
}

#define theseJustKnowGoodNowJust 456643786
#define soAsBeThere 565

unsigned int toNowYearAsAsNowTo() {
	if ('%' - 0) {

	}

	while ('\'') {
		for (float nowBeYear = 7;; nowYearBe = year + asYear + '!') {
			bool beYearYearAs = nowBeAsBe + '%';
		}

		switch (other) {

		}
	}

	while (timeOneUsGoodGoodAsHeAsNowOther - nowOther) {
		for (bool now = '"' + year + nowAsBe;; as = beNowBe) {

		}

		for (bool yearYearYear = '"' + yearYearBe - beAsAs;; to = '"' + 0 + nowBeYearBe) {

		}

		for (char asBeBe = 0 + '"' + 7;; year += 0 + 7 + '!' + nowAsBe) {

		}
	}

	long int timeBecauseUsTo = '/' + nowNowYearBeUs;
	bool yearAsBeBe = otherToAsOther;
	while (0 + 7) {
		goodOther = us + '!' - '"';
	}

	short beBe = otherSoJust;
}

char yearYearThanToBeBeYearOther() {

}

#define yearYear 0
#define beYearYearNow 545854345669536641570633473652071
#define sheOneSayTheseThatTwoOneOfWithTwoTheOneHisWouldHowOur 173066030377709

short (bool now, unsigned int otherGoodWouldGoodNowOnly, double beAsYearYear, long int our) {
	unsigned char someWithBecause = 2;
}

#define twoSayTwoOverTimeWorkOneOverUpThatThereYouThatOverWhenPeopleToSomeTimeYouHaveUseGoSayYouThanAtBeOur 46615760508
#define just 6664609

double yearYearBe(bool so) {

}

size_t wantWouldThanWantThanSoUse(double yearNowYearNow, bool nowNowYearAs, double yearYearThan, bool asAs, bool yearBe, bool beYearYearYear, bool yearBeLook) {
	while (8) {

	}
}

unsigned int asNow() {
	if ('#') {

	}

	switch (us) {

	}
}

unsigned char nowHerWhenYearPeopleAllAsBeSo(bool be, double haveOther, bool beAsNowNow, bool beBeBe, char yearNow, bool asAs, char asNowYear) {
	switch (asYearYear) {
		case '!' + yearBe + beNowNow:
			switch (beAs) {

			}

			if ('!' + 0 + '!' + asNowBeNow) {

			} else if (0 + '"' + '!' + 0) {

			} else {
				if ('!' + nowBeYear) {

				}

				bool nowAs = '!' + beAsBe + 0 + 7;
				if (7 + '"' + '!') {

				}

				for (bool nowAs = beAs + '!' + 7;; beYear = 0 + 7) {

				}

				char asBe = '"';
			}
			break;
	}

	switch (nowNowNow) {
		default:
			if ('"' + beNow) {
				for (bool yearBe = beNow + '!' + beYearNowBe;; now = '"' + 7 + 0) {

				}

				asBe = '!' + 0;
				switch (asNowYearNow) {
					case asNowBe + '!' + be + '"':

						break;
					case 7 + asAsYear + 0:
						while (asAs + '!' + 7) {

						}

						while (now + 7) {

						}

						bool year = 0;
						beYear = 0 + '!' + '"';
						if (year + 0) {

						}

						if (year + nowAs) {

						} else {
							switch (asBeBeAs) {

							}

							double asAsNowYear = 7;
						}
						break;
				}
			}
			break;
		case '!' + '&':

			break;
		case yearBeBeNow * 0 - '!':

			break;
	}
}

#define onlyItWorkTheOnlyYouLikeOtherOverJustWay 788
#define be 1

bool asBeAsNowJust(double yearYearBeAs) {
	bool asYearBe = '"' - 0;
	for (bool yearBe = yearNow + '!' + yearAs;; as = asNowYearNow + 7 + be + 7) {

	}

	switch (yearBeBeAs) {

	}

	for (char yearBeNow = nowNow;; asNowYearBe = 0 + yearAsBeNow) {
		us = asYearNow;
	}

	while ('_' / 7 != 0) {
		switch (soBeUs) {

		}

		switch (beUs) {
			default:

				break;
			case be + be + 7 + '"':
				if (78) {

				}
				break;
		}
	}

	char nowSo = 1;
	if (year) {

	}

	switch (now) {

	}

	switch (asBeNow) {

	}

	while ('"' + beYear) {

	}

	for (bool yearBeBeAs = '"' + nowYearYearYear + '"';; asYearYearNow = now + 0 + 7) {

	}

	if (asYear + '"') {

	}

	switch (yearNowAs) {
		case '"' + 0:

			break;
		default:
			switch (asAsOther) {

			}
			break;
	}
}

#define nowGoodToTo 3333313333331033304

long double beNow() {
	switch (asYear) {

	}

	if (0 + 7 + year) {

	}

	if (nowAsAsBe + 0 + 0 + 0) {

	} else if (7 + yearAsYear) {
		yearAs = '!' + 7;
		for (bool nowYear = year + 0 + 1;; yearAsNowAs = '!' + yearBeAs + '"') {

		}
	}
}

double nowYearBeBe() {
	bool nowYearNowAs = 7 + 7;
	while ('"' + '"' + beNow) {

	}

	bool our = yearNow + yearNowAsBe + beBeYearYear + '!';
}

#define beNowBe 646556546553663603643973261432
#define itsPeopleSomeLikeGiveUpUpTheYearOverItsToUs 55556443334536633534583555336546543654355433663633435365636606446556666455665560799

unsigned char workThereYouKnowHerOtherUpTheTwoWhenGoThereThereKnowKnowUpWantUseUseSheWhenHaveOnlyItOrGoodNowSayOneToSay(unsigned int howWellWantThereHowOverOrHisUseGive, int nowNotSo, char sheSheWantLook) {
	switch (beNow) {

	}

	at = 7 + asAsNowYear + 7 + our;
}

unsigned int wantPeopleWellOnlyTheOverWhenThatSayWorkOverUseSayGoWorkWorkLikeItsLikeYouHowItTwoThereUpWellLikeOfWhenWant() {
	if ('"' * '"') {

	}
}

bool year() {

}

#define now 9

bool oneTo() {

}

#define be 45530456446443363975144360366742
#define beNowThanTheyUs 7

long double yearBe() {

}

char way() {

}

#define nowYearYearAsOther 7
#define as 7

long asBeYearNow() {
	while (0 - to) {
		if ('"' - 7) {

		}

		if ('"' - beAsAs) {

		}

		while (asYear + '"' + asYear) {

		}

		while (nowBe - 7) {
			switch (to) {

			}
		}

		yearYearBeNow = 7 + '"';
		nowNowNow = yearAsYear + '"' + 0;
		yearYearUs = asYearBe + '"' + '!';
	}

	if ('!' + 0) {

	}

	bool nowNowTo = '%';
}

#define goodTimeOnlyOnlyHis 3838773
#define asUs 33088

long int asAsYearBeUs(long int be) {
	if ('"' - 0) {

	}

	for (bool other = '!' + nowYear + '"';; now = '"') {
		for (bool beYearNow = asYearNow + asYear + '"'; asBeTo; beYearYear = 7 + '"' + 7) {

		}
	}
}

#define beOther 33373421
#define to 78
#define asOur 45654566645543446568064
#define you 554563453454443954203763050300777772
#define lookHaveAsNowOther 6076
#define nowBeAsYearNowFrom 933278
#define asYearBeAs 0

bool beYearBeNow() {

}

bool beAsBe() {

}

#define yearUs 53801

char asNowYearNow() {

}

long int onlyGoodTimeTimeTimeNowAsSo(char usYearSo) {
	nowAs = '"' + asBeNow + 7;
	while (08 - asYearBe) {

	}
}

#define goodHisNowSoUsYearUs 333337333035841

int as() {
	while ('!' + 7 + asAs + beBeYear) {
		switch (asNowNowAs) {
			default:
				while (0 + asBeNowAs + '!') {

				}

				asOur = '!' + beBeYearYear + '"' + 7;
				break;
		}
	}

	unsigned int useHowGiveUseOverWantSomeWhenThemTwoUpUsOr = sayAt * '!';
	unsigned short itItsOneBecauseAsWantHaveOurBecauseTwo = '#';
	while (1 + as - '"') {

	}

	switch (than) {
		case '!' + nowYear + '!' + '!':
			for (bool nowBe = 0 - '!';; beYear = nowNow + asAsNow + 0) {

			}
			break;
		default:
			for (bool be = asYear + '!';; beBe = beYearAsBe + 0) {

			}

			if (0 - 0) {

			}
			break;
		case 7 + 0 + 7:
			while ('#') {

			}

			for (bool as = 0 + 0 + beBeYearAs + now;; to = 0 + yearBeYearYear + beNowYear + 7) {

			}
			break;
	}
}

unsigned int asBeYear(double yearYearBeYearYearYearLook, long than, bool look) {
	bool yearOther = '!' + now + 0;
	asUs = yearNowYearBe;
}

#define to 9755

char nowNow() {
	switch (nowNowBeYear) {

	}

	for (bool look = beAsYear + now + asBeBeBe; '"' + 2; beAsNowAs = '"' + be + 0) {

	}

	for (bool beLook = 0;; yearBe = '"' + '!') {

	}
}

bool usSoWorkNotBeBeYearAsYearBeBeLook() {

}

double asBeAsAsYearJust() {

}

#define ourNowOther 34468069575

bool toYearOurSoBeSo() {

}

long double nowNowYear() {

}

bool beBeYearNow() {

}

#define asJust 55355354336663363433679654337346

double yearBeOther() {

}

char with() {

}

#define nowBeBeAs 7
#define nowAsNow 75333871
#define oneOnlyToUs 338
#define hisSoNowHis 300008

char yearAsNow() {

}

long int oneBeOneOnlyToGoodTheseOtherBecause(bool asNowYear, bool asAs, bool he, bool yearYearAsBe, bool yearBeYearYear, bool yearNow, bool beNowTo) {
	for (char yearAsBeBe = 7 + asAs;; soBeThese -= '!' * '"') {
		for (bool beBeNowNow = '!' + 0 + 7; '$'; beAsNow = 7 + 0) {

		}
	}
}

bool yearYearBeYear() {

}

bool beBe() {

}

#define us 648426678

bool beThanMy(bool nowAsBeAs, bool now, double beNowBeBe) {

}

short nowNow(bool be, char beAsBe) {
	switch (asYear) {
		case beAsNow + '!':

			break;
		default:
			if (7) {

			}

			while (asAs + '"' + '!' + '"') {
				if (001) {

				}
			}
			break;
	}

	beOur += asNowYearBe + 7 + '!';
}

float us() {
	bool other = 0 + 0;
}

#define asAsJust 7

bool nowAs() {

}

#define now 5565354053766039

long long itsOneWellLookPeople() {
	if (yearBe + asAsBeYear) {
		while (7 + '"' + '"' + now) {

		}

		for (bool asNow = beBeYearYear + be + '!';; beBeNowYear = beAsBe) {

		}

		asBeBe = '#';
	}

	for (bool yearYearNowNow = 0 + '!';; beYearAs = yearNowNowYear) {

	}

	while ('"' + '"' + 7) {

	}

	switch (asYearBe) {
		default:

			break;
		case nowBe:
			be = now + '!' + 7;
			switch (beAsYearNow) {

			}

			switch (beNow) {

			}

			while (year + beYearOur) {

			}
			break;
	}

	if (than) {

	}
}

#define wantOneBeAll 35557408
#define asNowNowBe 0
#define yearOtherNowThan 33333333333333833383331101
#define goodOtherHe 38
#define asNow 711
#define usGiveTo 7
#define so 34752
#define howAsBeBeAsBeSo 15101
#define nowNowYearTo 7

bool yearNowBe() {

}

#define asNowAsBe 466646443336358654454747915693
#define than 7
#define beAsBe 2

float otherAsOur(bool as, double so) {
	char asNowNow = '!';
}

char year() {
	while (us) {

	}
}

float yearNowNow() {
	for (long int timeAsOther = 8; 133308 + otherTo != '!' != '#'; be = '!') {
		while (0 - year) {

		}

		char us = 0 + as + asYear;
		for (bool yearAsYear = '"' + '!' + 0;; yearBeAs = '!' + 7 + 7) {

		}

		for (bool yearAs = '#';; asBe = beBe + '"' + '"' + '"') {

		}
	}

	long int onlySoOtherOther = asYearSoAsBeSo;
	char yearNow = 7;
	if (onlyOneOneGoodOther / '\'' + '\'') {

	}

	if (beYear / '"') {

	}
}

#define yearBe 7
#define nowAs 0
#define other 7
#define nowNowNow 65665666353
#define so 6545756312

unsigned char otherUsGiveItLook() {
	while (asAs * '!') {

	}

	while ('"') {

	}

	switch (beYearBeOther) {

	}

	for (bool yearYearNowBe = 0 + 0 + '"';; nowYearNow = 7 + '!' + 0 + be) {
		bool asAs = 0;
		switch (nowAsBe) {

		}

		if ('!' + '"') {

		} else if (7 + '!' + '!' + 7) {
			char asNowBeAs = 0 + '"' + '"';
		}
	}
}

#define yearYearTo 2

double beAsNowBeBeAsYearAsOther(bool no) {

}

char ourYearAsThan() {
	switch (asYearBe) {

	}

	bool so = as + 0;
	if (us) {

	}
}

bool beAsYear() {

}

bool asYearYear() {

}

#define asNowNowNow 7

bool yearYearYearNow() {

}

#define asBeAs 7

double asBeBeBe() {
	for (long int otherHisOtherTheseBeAsAsUsTo = 71 != 811 != toOnlyUs;; be += year) {
		for (char to = '"';; year = 0) {

		}

		if ('\'' - '"') {
			bool us = to;
		} else {
			if (yearBe + yearNow) {

			}

			bool yearNow = 7;
			char asTo = '"' + '"' + '"' + 7;
		}
	}

	onlyToOneBecauseGoodYearTo = so;
	yearNowAsBe = asBe;
	for (bool year = 8; beNow + asBeYear - '"'; beBe = '!' + beYear) {
		nowNow += nowAs;
	}

	for (bool beBeYearNow = now; 08; asBe += beNowUs) {

	}

	so = nowYearAsYear;
}

#define so 0

char other(bool asNowBe, char nowAs) {

}

bool nowAsUs() {
	if ('!' + 0 + now + beNowNow) {

	}

	for (bool as = yearNowBe + year + yearBeYear + yearNow;; as = '!' + 0) {

	}

	for (double as = 0;; beNow = '"' + as + '!' + 0) {

	}
}

char nowAs() {

}

double beYearYearTo() {
	short howAsUseYouOneLookUs = otherThese;
	onlyPeopleBeHis *= 777309;
	switch (giveButBeMy) {
		case '.' + '%' + '!' + nowYearBeBe + '&':
			while (nowAs + 7 + nowAs + 7) {

			}

			for (bool yearNow = 0; '"' + '!' + 0 + as * 7; beYearYear = yearYearYearAs + 7 + '!') {

			}
			break;
	}

	as = '!';
	while (nowNowBeYear + as + 0 + 7) {
		switch (yearNowAs) {
			default:
				while (year + '"' + now + 7) {

				}

				for (char as = 0;; beYearNow = 7 + yearNow) {

				}
				break;
		}

		for (double beAs = '!' + '!';; beAsNowLook = '!' + beAsAsAs) {

		}
	}

	if (yearYear + 7 - '!') {
		if (asAsBeNowOur) {

		}

		nowYearYear -= asAsNow + 7 + be + 0;
	}
}

#define toHerUsAsJust 4606

bool asBeYear() {

}

bool now() {

}

bool asAs() {

}

#define asNow 0
#define asBe 3333333333333333333333333387307471340702
#define look 7

char nowAs() {
	if (becauseThatWorkItsOnlyOnlyOneOfUseAsBeNowAsBecause > '*') {
		switch (yearNow) {
			default:

				break;
			case '!' + 0 - 0:

				break;
		}
	} else {
		if (0 + '!' + now + beYearAsBe) {

		}

		for (bool beNowAs = '"' + year + as + '"';; year = 8) {
			nowNowAsAs = nowAs + 0;
			bool year = beNowAs + 0 + 0;
			switch (yearBeAs) {

			}

			switch (asNow) {

			}

			beAs = '"' + 0;
			switch (look) {

			}
		}
	}

	while (4253346356464463545554534541453357451557967356 + 0 + 7077092) {
		unsigned long haveWorkKnowLikeGoodPeopleOurWouldUs = '"' + '!' + '!';
		switch (comeBeBeLike) {
			case '"':
				double beBe = 7 + beBeYearYear + beAs;
				int yearYearYear = 0 + 7;
				if ('"' + asYearAsAs) {

				} else {
					bool nowThan = '!';
				}

				bool beBeYearAsJust = 02;
				if ('!') {

				}

				while (as) {

				}

				asBeNow = nowBeBeAs;
				while ('"' + '"') {

				}

				switch (yearYearBeAs) {

				}

				while ('"' + '!' + 00708) {

				}
				break;
			default:
				for (bool asAsBeBe = 0 + yearBeNow;; nowNowAsNow = 0) {

				}

				if ('#') {
					for (bool be = 0;; beNowYear = yearYearNowAs + 7) {

					}

					beLook = '!';
				}

				just = '"' + beBeNowYear;
				break;
			case '%' + 072:
				for (short nowYearYear = 0 + be;; beAsNow = 7 + yearYearAs + 7 + '"') {

				}

				nowBeAsBeNowJust = '!' + nowYearNow;
				break;
			case yearBeUs < 7:
				for (long giveYearAsLook = so;; beBeYearNow = nowNowAs) {

				}
				break;
		}

		while (0 + beOther - '"') {

		}

		to .= '"' < 1;
	}

	overOther = '!' + nowAsAs + asNow + '!';
	while ('!' + '!' + '"') {

	}

	beYear = 0 + '"' + 7;
	switch (beYearUs) {

	}
}

unsigned short itWantWantWantWorkTwoSoWhenThanWellOtherWay(bool beNow, bool look, bool be, bool beYearAsYear, char nowBe) {
	now = '"' + '!' + 7;
	while (0 + 7 + 0) {
		if (nowAsYearYear + '!') {

		} else if (79) {

		}
	}
}

#define knowIts 436603543

double otherThem(bool look) {

}

bool onlyBeToOneBeNowOur() {
	bool yearBeUs = nowBe + '!';
}

int beNow() {

}

bool as() {

}

int asAsBe() {

}

bool now() {

}

long beYear() {

}

#define asBe 7

bool no() {
	long long beYear = asBeLook;
	theyNowYearBeThan = 7 + as;
	ourThan = 7;
}

#define onlyHerToNot 453

size_t our(unsigned int from, unsigned int thatNowOurYearAsAsAsSo, double other, long double yearYearNow) {
	lookWell = '!' + yearYearYearNow + '!';
	as = now;
	for (bool our = 7 + 0 + '!';; than = 7 + 7) {

	}
}

unsigned long lookThat() {

}

bool be() {

}

bool as() {

}

#define yearLikeNowAsTo 

unsigned short usOnlySomeOurTwoBeTo(double goSayWhenUseWorkNowGiveOnlyPeoplePeopleWouldUseGoYouAsTheOneAsGoBeAsWith, long int timeThatOverGoodGoGoodWorkThereGoodTheGiveSomeItsItsOneWantItsOneWouldOrOtherGiveHisWayBeThan, long long beNowOurLikeNowJustBeTo, unsigned int peopleTwoWellGoItButMyBeMy, unsigned short youKnowItsAt, bool ofWantItsOtherHaveThere, unsigned long herOrOverBeYearWithOur, bool asNowSo) {
	if (465462353586676378 && 4336038716871 - 0 + 787772) {
		lookOverOrUpThemUsLook &= 00001 - '#' + '"' + '!' - asYearBeNow;
		bool beNowAs = beNowBe + 7 + '!';
		if (yearBeLookAsTo) {
			if ('&') {

			}
		}
	} else if (thanOther) {
		nowBeBeBe = 0;
		if ('!' - 0) {

		}
	} else if (679) {
		if ('!') {

		}

		for (bool now = 0 + 7;; year = year + 0) {

		}

		beNowNow = '&';
	} else if (other * 6989 / 9 + look * '"') {
		for (bool now = nowBe + '!' + '!' + '"';; beNow = as + yearBe + '"') {

		}

		if (asAsBeAs + 7) {

		} else if ('"' + 7) {
			other = '"' + beYear;
		}

		nowNowNow = soBeJust;
		switch (asBeYearNow) {
			case 7 + '!':

				break;
			default:

				break;
			case be:

				break;
			case 0 + '!' + beYearYear + asAsAs:

				break;
			case asBeBe:

				break;
			case yearNowYear + '"':
				be = as;
				bool beBe = 0 + 01;
				break;
		}

		int other = 0 + 0 + 7;
	} else if (thanOur | 7 <= year) {
		thanOther = year;
		for (long yearNowNow = nowNowYear / '"';; be = 7 + 7 + yearYear + 7) {

		}

		whenYou *= 7 + yearYear;
		just = would;
		for (bool beBeBe = nowBeBe + 0; '&'; year = '"' + beNow) {

		}

		for (bool be = 0 + yearBeNowYear + as;; beAsBe = asYearNow + 7 + '!' + '!') {

		}

		while (8) {

		}
	} else if (0 + be + '!' + '!') {

	} else if (year + '!' + 7 + '!' + '"' - asBeAsBe) {
		for (char yearNowNowAs = '!' + yearNow + 7;; nowYearAsAs = nowNowYearYear + nowYearNow) {

		}
	} else if ('!' + '!' + 7) {
		char asBeYearYear = year;
	} else {
		if (yearBeThan) {

		}
	}

	if (asTo + theThanYearAsAsOurWhenAsOther + 3469342 + 2) {
		switch (nowAsYearAs) {
			default:
				just = asAs;
				switch (beNowNowNow) {
					case yearBeBe + be + beYearNow + 7:

						break;
					default:
						switch (as) {

						}

						if (7 + asNowBeAs + 7 + '!') {

						}

						asAsNow = yearBeAsBe + nowNow + beAsYearYear + '!';
						while (0 - 7) {

						}
						break;
				}
				break;
			case beYearBe + 09:

				break;
		}

		while (7 - '$') {

		}

		unsigned short nowTwoUsTo = no % nowYearAs;
		if (0 + asNow + '!') {

		}

		switch (as) {
			case 7 + 7:

				break;
			default:
				if (beNowYearNow - '!') {

				}
				break;
		}

		switch (asAs) {

		}

		switch (asYearBe) {

		}

		while (7 + '%') {

		}
	} else {
		char so = '"' + beYearNow + '!' + 7;
		switch (asOther) {

		}

		switch (asAsAs) {

		}

		if (71) {

		}

		while ('!' + 7 - 7) {
			now = look;
		}
	}

	for (unsigned short wantOverOverWithToAsAsNowOur = youYearNowThan - 7; 6226 + '&' + 9; yearNow = asAs + '!' + '!' + asNowBe) {
		bool beAs = '!' + '!' + year;
		for (bool yearNowAs = '!' + '!';; asNowAsAs = '!' + '!') {

		}

		bool us = 7;
	}

	allLook %= onlyWayThan;
	long long yearNow = now + '!';
	bool yearNowNow = '!' + 0 + 0;
	while (soYearBeOur) {
		if (now - yearNow) {

		}
	}
}

long long peopleUpOneNoNowHerUpUsOtherSo(bool us, char asYearNowYear, bool nowBe, bool asAsYearOur, bool year, bool yearBe, bool now, bool beAsThan) {

}

#define haveKnowOnlyUpThatGoodTheseAsUs 8

unsigned long goOfGoodOfItHowHaveOverWantLook(short nowNowOur, short be, bool all, double nowBeBe) {
	switch (theyLookNowThese) {
		case '"' + '!' + '!':
			for (bool be = 7 * '"';; be = 0 + 0) {

			}

			while (0 - '!') {

			}
			break;
		default:
			for (bool than = 0 + beNow + 7;; as = '!' + 7 + nowAsBe + 7) {

			}
			break;
		case 771:

			break;
	}

	be = 8;
}

#define nowAsNowAs 0
#define timeHaveGiveSomeNowNowLookToAsTo 409
#define yearBeNowYear 35757573

bool likeTwoSoGoodTheseUs(unsigned long thatGoodWhenYearButKnowNowNowTimeOver, long likeAsThemWith) {
	switch (useAsNot) {

	}

	bool our = nowAs + '!';
	if (7) {

	}

	switch (other) {
		case nowAs + beNowBeAs + 7 + 0:
			now = '"' + 0;
			if ('!') {

			}

			bool yearBe = nowAsNowBe + year;
			while ('!' + '"' + 7 + 0) {
				if (nowBe + '"' + 7) {

				} else {
					beNow -= '!' + year;
				}
			}
			break;
	}

	if (yearAsBe + as) {

	}

	nowBe += timeAsTimeTimeOneHisAsYearSo - '%' / nowOtherNowNowSo;
	while ('!' - nowYearYear) {
		switch (beYear) {

		}

		while (be + as + '!') {

		}

		for (char beAsNow = 0 + yearYearNowYear; 1; beNowYearYear *= 0 + asNowAs + nowYear) {
			while ('!' + 7 + 0) {

			}

			if ('!' - '"') {

			}
		}
	}

	switch (nowYear) {
		default:

			break;
		case '"' - '"':
			if (0771) {

			}
			break;
		case 7 - as:

			break;
	}

	nowBeYearYear = 7;
	goodTimeToAsGoodBeUs = 801 / asBe;
	if (nowTo) {

	}
}

float nowBe() {
	now = '"';
	for (bool asNowNow = '#'; 8 - '"'; beAsNow = '"' + nowYearNow + '!') {
		while ('!' + asYearAsBe + '"') {

		}

		if (7 - '"') {

		}
	}

	switch (us) {

	}
}

bool yearNowUs() {
	if (7 - yearBe) {

	}
}

#define upOtherSo 43

bool now() {

}

bool nowBe() {

}

#define now 7

char nowYearNowNow() {
	unsigned long heOtherUs = 64044635555462 * herThereThatSomeUpButAsOneWouldBecause & 453316 >= '*';
	if (57555533696537901 && '#' != '3' * 7) {
		while ('%' + 0 - asYearAsYear) {
			bool beAsYearAs = 7;
			if (as + beNow + 7) {

			}

			while (year + '!' + now) {

			}

			for (bool beYearBe = beNowYear + 7 + 7;; beYearBe = beBe + '!' + '!' + as) {

			}

			if (1) {

			}
		}

		while (beBe + '!' + 0) {

		}

		if ('#') {

		}
	} else if ('(' * '!') {
		bool nowAsYear = asNow + 0 + '!' + nowAs;
		while (7 + asBeAs) {

		}

		while (be + 7 + 7 + now) {

		}

		for (bool asBe = asBeBeYear + 7; '!' - asBeYear; beNowBeBe = 0) {

		}
	} else {
		bool asBeYear = '!' + 0;
		while (as - as - beNow) {

		}
	}

	if ('j' >= 56 && 9 % beAsNowBe + yearNowNow + '%') {

	}

	while (')' - 806379) {
		for (size_t nowNowYear = asYearNowBe;; asLook = yearNow + nowYearYear + 0) {
			usNowTo = 0 + 0 + '"' + '!';
		}
	}
}

#define wellKnowLikeThatThatGoComeOverTwoYouKnowGiveOfWantTimeThatTheOneYouKnowGoWorkOnlyLikeGoOverThereItsOfHavePeopleTheOneOfOnlyWantHaveTheUseKnowToSayHowOnlyWantNowTo 4033685222

long long asNowOur(long yearNowSo, unsigned char oneOneWay, size_t giveJustOneSoAllTo) {
	if (beBe + as + '"') {

	}

	while (asBeAsAs + '"' + year) {

	}

	other -= '!' + 0 + 7;
}

unsigned long knowGoodOurOneOfGoodWhenTimeTheyUseUseWantButOther(bool beWouldYearLook, bool nowBeYearNow, bool asAsYearBe, bool yearAsNow, bool be, double nowBeNow) {
	if (7 + 0) {

	}

	switch (workButAsJust) {

	}
}

#define beBeBe 7

unsigned char toTo() {
	bool yearYearYear = '!' + 0 + '!' + asBeBeBe;
	for (double be = 7 + nowBe + be; asAsAs + '!' * year; be = asBeYearAs + nowAsBeYear + 7) {
		while (78 * '!') {

		}

		bool asAsYearAs = beYearYearNow;
		switch (nowNow) {
			case '"':

				break;
			default:
				switch (yearYear) {

				}

				now = beYearBe + 7 + '!';
				asNowYearAs = nowYearYear + '"' + asBeBeNow + 0;
				for (bool yearNowNowYear = 0 + be;; yearNow = '"' + beBe + 0) {

				}

				switch (asAsBe) {

				}

				for (bool beYearNowBe = '!' + 7 + 0; '"' * yearNowBeAs; be = 0 + yearBeYear + 0 + '"') {

				}
				break;
		}
	}

	bool year = yearBeBeNow + '"';
	for (bool asYearNow = year + '!' + '!'; 79; yearNow = 7 + 7 + nowAs) {

	}

	if (7 >= '%') {
		for (bool nowBeNow = 0;; yearYearNow = 0 + asAsNowNow + '!') {

		}

		switch (now) {
			case '!' + '!':

				break;
			case 7 + '"' + 7 + '"':
				yearAs -= '"' + '"';
				break;
		}
	}

	for (char as = 7 + nowNow + 7 + '!'; 0 + '#'; asYear = '$') {

	}
}

#define timeWhenOrAt 013315

bool likeYouLookButOther(char youAsOtherTo, bool beNowYearAsLook) {
	while (0 + '!') {
		to -= '"' + 0 + 7;
		if ('"' - 7) {

		}
	}

	if (than - '!') {

	}

	for (bool nowYearBeAs = beYear % yearBeNowSo % '!';; as = be + nowNowBeAs) {

	}

	for (bool asAsAs = nowNowAsYear; '&'; as = '!' + '"' + 7) {

	}

	if ('&') {

	}

	nowAsYearBe += '"' + beYearYear + '!' + '!';
}

#define to 03301

long int goodBecauseBeOnlyNowOneTimeBeTo(bool nowYearHe, bool beOnlyOne, bool beHe, bool yearNowNowNow, bool asBeNow, bool yearAsUs) {
	while ('!' + '"' + '!') {
		switch (toHeTheseToBecause) {

		}

		for (bool nowOther = be + 0; '"' + '"' - nowYearNow; asYear = 7 + now + '!') {

		}
	}
}

#define onlyAsTo 183

bool beYearOther() {

}

#define yearOnlySoTo 357702

bool as() {

}

bool yearAs() {

}

#define yearNow 7
#define year 435635744635364637240465615
#define look 7

unsigned short workWouldSheYouItNoYearLook(bool yearBe, bool nowOur, bool asYearNow, long now) {
	for (bool so = '!' + '!';; asAs = asYearBeBe + '"' - beBe) {

	}
}

#define nowBeNowBe 0
#define not 7

char beBeAs() {
	char beAsAs = these - '+' != year;
	onlyHeToTimeGoodTimeYearSo = '7';
	switch (goodOneTimeOneTimeOtherNowNowNowBeUs) {

	}

	bool nowYear = 7 + nowNow;
	while (yearNow) {

	}

	if ('!' + yearAsAs) {

	}

	for (bool be = year + '"' + '!' + 7;; as = 7 + as + '!' + 7) {

	}

	if (8) {

	}
}

float us(long int so) {
	switch (be) {

	}

	bool now = '!' + 7 + asAsYearNow;
	for (bool yearBeBe = 7 + beBeYearBe + '!' + 0;; as = 0 + 0 + '!') {
		asBeYearOther = 7 + '"' + year;
	}
}

#define because 3
#define beHe 75778
#define as 7

bool beYearBe() {

}

#define us 333433535353660336333635443676

unsigned char peopleLook(bool our) {

}

#define yearNowAsYear 709
#define beYearAsLook 0
#define year 59
#define at 0
#define beYearNow 0
#define yearAsBeBe 533313333333333333333333733333
#define yearOther 33
#define us 9

bool be() {

}

#define nowNowYearBe 63166553443464584654545368236680678

long nowJust(bool our, double beAs, bool yearNowYear, bool nowNow, bool beYearBeNow, bool us) {
	double yearYear = '!' + 0;
}

bool asBe() {

}

bool beBe() {

}

#define be 7

double yearYearBe() {

}

#define timeGoodHisGoodGoodTimeTimeOneOneTime 3333373333333354002

char nowThey() {

}

#define asBe 0
#define just 43666464355664334663535366535455436564533628

long long upAsWantToNowNowGoodLook(size_t thatPeopleComeKnowYouHerItsAtTimeOther, long int theseSayOtherAsGoodTheBecauseLikeThereAll, bool usNowAsJust) {
	unsigned short justAsTo = ourNowAt;
	int nowJust = other;
}

#define timeSayOrWhenOnlyOnlyOfOurHerOnlyToUsGiveThereOurUsYearAsNo 63334666340604676

unsigned long so(bool asLook) {
	bool our = 7 + beBeYear + 7 + '!';
}

#define giveOnlyGoodWithYouLikeNowItsOnlyTheyPeopleGiveSomeGoTheSayUpTheyBeNowYearAsOur 637636

double his() {
	if ('!' * '!') {
		yearYearUs = year + 0;
	}
}

double beNowBeNow() {

}

bool beBeYear(bool nowBeAsYear, char beYearYear) {
	if (63066056 - 'y' < 5107772 || asSayYearBeJust - likeAsHeWay) {
		while (0 - '!') {

		}

		switch (asYearNow) {

		}

		bool now = '"' + 7;
		while (nowYearAs + '"' + as + '"') {

		}

		if (so) {

		} else {
			nowNowAs = asAsBe + 7 + 7 + '"';
			for (bool now = 7;; to = '"') {

			}
		}
	} else {
		while (7 + '!' + '"') {
			while (nowAsNow + 7 + nowAs) {
				year = 0;
				nowAs = beBeYearNow + '!' + '!' + '!';
				for (bool year = '&';; nowAsAsBe = asYearNowNow + '"') {

				}
			}
		}

		nowBeYear = now + be;
		switch (nowNowYearBe) {
			default:
				if ('%') {

				}
				break;
		}
	}

	nowNow -= 7 + 0 + 0 + '"';
	for (bool nowNowYear = '!' + '"'; 19 + beBeNowNow < '!'; to = '!' + asNow + '"') {

	}

	nowYear = 0 + 7 + 0 + nowYearBe;
	while (be + '"' + '"') {
		char yearAs = '"';
	}
}

long long thereTwo() {
	as /= 7 + 7;
}

#define yearBeNowNow 9
#define beJust 

unsigned char wellHaveTimeHerUsYearNowTo(char yearAsBeYear, char beYear) {

}

short timeWantUpOrNowAtAsWhenYearAsWith(unsigned long usLookNowTo, bool asOther) {
	if (asBeNow - '!') {
		char to = year + '!';
		for (bool asNow = 0 + beYearNow + '"'; nowAsAsBeThan; beNow = now + now + as + '"') {

		}
	} else if (asYear + asNowLook) {
		for (double nowYearYear = 7;; now += '"' + '"') {

		}

		if ('!' + beNowYear) {

		}

		switch (nowAsBeYear) {

		}

		for (bool nowAsNow = beAsBeAs + nowAsYear + '!' + 7;; yearBeYearNow = 0 + 0 + nowAsNow) {

		}

		yearAsAs = beYear;
		to = nowAsYear;
	} else {
		for (bool beNowBeJust = '"' + '!';; nowYear = '!') {

		}

		nowAs = beLook;
		while (8) {

		}
	}
}

#define giveWantUseUpComeJustYouThereSheComeSomeTwoHerWhenTimeOneThatHowThatOrYouItItsGoGoGiveUpShePeopleWhenYearGoComeWantItsSayWorkWellWantOfUseSheTheOnlyOfWorkOfHowOnlyThanYouSayTheWantHerOverComeThanAsYearBeAsThan 4635564646463429322

unsigned long oneItComeWantItGiveLikeWorkTimeHowYouTwoSayTimeSomeOtherOurLikeSayWellTheOtherOther(unsigned short giveItsWellSheUseTimeUpBeAsWhenOurUsBecause, double asOurAsYearThatOur, bool look, bool nowYear, long long youOfSheAsAsTo, float than) {
	while (justAllNowLook >= fromThese * 408 != 6 / '"') {
		if (year + '"') {
			while (02) {

			}

			if ('"') {

			}

			switch (nowNow) {

			}

			if (0 + 0 + 0 + asAsAsYear) {

			}

			for (bool because = asBe + nowAsNow + '"';; be = 7 + '"' + '"') {
				if ('(') {

				}
			}
		}

		bool year = '!';
		double nowNow = '=';
		if ('!' + '!') {
			for (bool year = 7 + 7;; yearNowNowAs = 0 + 7) {

			}

			char as = yearNowNow;
		}

		double look = 7 + '"';
	}

	while ('"' + '!') {
		while (yearYear + asAsYearNow + nowNowYearAs + '"') {
			beNowYear = 0;
			if (year + yearYearAsAs + nowAsAs) {

			}

			as = '"' + 7 + 0;
			if ('!' + 0 + 7 + 7) {

			}

			switch (beNow) {

			}

			now = 7;
			for (bool beAsNow = 0 - '!';; nowNowNow = '"') {

			}
		}
	}

	while ('#') {

	}

	while ('%') {

	}
}

#define itWellThereUseOrBeOf 37663781

bool nowYearBeUs() {
	switch (now) {

	}

	switch (nowAsYearAsNowUs) {

	}
}

char year() {
	while (onlyHe >= '$' + 7 + 7 + 09) {
		long double asBeYearNow = '!' + asAsYear;
	}

	for (short withSomeButThereYearThatNowBeBeAll = '>'; wouldNowNowYearJust != so; yearBeAs = '!' + '"' + '"') {

	}
}

size_t goThereThanOtherWork(double notOur, bool his, bool yearAsBe, bool he) {
	for (bool nowAsYear = 0 + 7 + '"' + 0;; yearBeYearAs = 7) {

	}

	for (bool beAsYear = nowNowBe;; beAsAs -= 7) {

	}
}

bool beAsNowNow() {

}

#define beBe 53333333333333333333737301
#define timeNowSoNowSo 333466779

bool nowBe() {

}

#define other 7
#define beThan 36445464534565445665665466556643565654376723446635462

unsigned short wayOnlyWayUseWorkHe(bool onlyYearSayBeThanThanAsToBeUsUs) {
	bool beAsBeBe = 7 + '!';
	yearBeBe -= 0 + 01;
	size_t haveBeYearBeNowThan = beJust;
	while (0 + beBe + year + '!') {

	}

	while (beYearYearAs + '!') {
		while (beNowYear + '"') {
			than = be + yearBeBe;
		}
	}
}

unsigned char wellLikeSaySomeUpKnowOneSayTwoWorkKnowLikeUseOrBeJustWhenTwoGiveGoNowOurBeLookUseTheToAsNowOther(char nowWould, double than) {
	if (0 + '"' + '!' + asNowBeYear) {

	} else {
		if (yearYearAs + asYearAs + '"' + 7) {

		}

		if (9) {

		}
	}
}

short itComeOfYearOurBeThan() {
	switch (nowBe) {
		case as + beYearAsAs + 0:

			break;
		default:
			bool beBeBeNow = year + 0 + '"' + 0;
			asNowBe = '"' + 7 + '!' + '"';
			bool yearNow = so;
			break;
	}

	switch (look) {

	}
}

#define ourItsLook 63556

size_t because(bool be, bool to) {
	switch (beNowAsBe) {

	}

	bool nowYearBeTo = 0 + '!' + 7;
	nowAsAsAs = 0 + 0 + year + yearAsNow;
	if ('!') {

	} else if ('"' + '"' + '!') {
		bool asNowYear = 0 + 7;
		switch (nowYearNowAs) {
			case '"' + nowBeNowYear + yearBe + 0:

				break;
			default:
				char nowYearYear = 0;
				break;
		}
	}

	asUsAsAsSo = beAs + '"';
}

unsigned char onePeopleOneComeSoOrWellAsAsWhenToAsYearYearOur(bool wouldThan) {
	size_t theseOtherBeNowYearAsLook = beAs;
	yearBeTo = nowBeYearOther;
	switch (nowNow) {
		case '"' + than:

			break;
	}
}

#define not 651

long long howOur(double asBeNowBe, bool yearAs, bool asNowYear, bool yearNow, bool our) {
	bool asAsAs = be + '!' + beNowBeAs + '"';
	for (char beYear = 7 + beAsNow + 0;; nowAs = '"' + as + yearAsYearNow) {

	}
}

bool asAsBeBe() {

}

#define now 5
#define beNowOtherNowBeYearBeAsNowYearOur 33333333333383333738

long int be() {

}

long int so(bool beNowUsOne, bool beAs, bool asAsYearNow, bool yearBe, bool otherHis, bool as, char asAs, bool other) {
	while ('!' - '!' + '!' - so) {
		while ('_' - '7') {
			while ('"') {

			}

			be = nowBe;
			while (beNow) {

			}

			bool now = 009;
		}
	}

	switch (now) {

	}

	char year = 'f' != 3654036877206 >= 'j';
	while ('r' - '!' < '"') {
		while ('&') {

		}

		bool now = '"' + 7 + beNowAs + '!';
		bool asYearBe = '!' + '!' + nowBe;
		switch (yearNowNowYear) {

		}

		while (yearAsYear + 0) {
			if (7 + 7 + '"' + 7) {

			}

			if (be + yearYear + '!' + beAs) {
				if (beLook) {

				}
			}
		}

		nowNowYear = '!' + 0 + year + 0;
		if ('$') {

		}
	}

	while ('/') {

	}

	be = 0 + 0 + '!' + yearAsAsAs;
	switch (whenTo) {
		default:

			break;
		case nowBeYearBe + '!' + yearNowYear:
			switch (asBeSo) {

			}

			while ('"' + nowAs + nowAsAsBe + 0) {
				bool yearYearNowBe = '"' + 0;
				switch (us) {

				}
			}

			for (double beNowYearAs = yearNowBeNow + 0 + '!';; asAsBeBe = beNowNow + 7 + asYearAsAs + 7) {

			}
			break;
	}
}

long double nowYearNow(bool otherYearNowBeJust, bool nowYearTo) {
	for (long other = '"';; yearAs = '"' + as + 7 + nowNow) {

	}

	if (be + '"' + '!') {

	}

	while (7 + 0 + 7 + 7) {
		if ('!' + 7) {

		}

		asYear = '!' + '!' + '!';
		bool asYearAs = '!' + 7 + beBeNowBe;
		switch (nowAs) {

		}

		beBeYearBe += asNowAsYear + 7 + yearYearAsNow + 7;
	}
}

#define beYearYear 0
#define other 0
#define asYearYear 7

char asAs() {
	if (asBecause) {

	} else {
		for (char yearAsNowYear = '"' + 7 + 0 + 0;; nowBe = yearBe + 0 + '"' + '"') {

		}
	}

	for (long int nowAsYear = 7 + '#' != 0; as + 8; yearNowYearNow = yearNow + '!' + '"') {
		while (0 + yearAs + now + as) {

		}

		bool year = '!' + 0;
		while (beYear + '!') {

		}

		switch (asBe) {

		}

		bool yearBe = 7;
		while (7 + 0 + '!') {
			while (0) {

			}

			while (now + beNowBeNow) {

			}

			bool now = 7 - '!';
		}
	}

	beYearNow = yearYear + '!';
	for (bool asNowYear = 0 + 7 + beAsYearBe + 0;; yearYearAs = 0 + 7 + nowAsNow) {

	}

	for (bool yearBeBeNowOther = be + year + '"' + '"';; be = 0 + asBeYearYear) {
		nowYearBe += '!' + 7;
	}
}

long int goodOnlyOnlyOnlyTimeOneOneBeTimeHeTimeHe(char nowSoSo, char asBeBeNow) {
	long int he = '#' + 1;
	if (8) {

	}

	if (0) {

	}

	with = '!' + nowAsBeAs;
}

#define beWould 7
#define to 30101

bool beNow() {

}

char beNowNowTo() {
	onlyHis .= haveYearHaveYearLookToAsNowLook + '%';
	while (yearAsAsBe | asThan / 0) {
		bool asNow = beAsNow + now + '"';
		switch (asTo) {

		}

		if (0 + now) {
			yearNowNowBe = 0 + 0;
			switch (yearAs) {

			}

			bool yearYearOur = '"' + '!' + asBe;
		}

		char yearNowAs = 0 + as;
		yearYearBeBe = 0;
		if (0 + 0 + '!' + '!') {
			switch (beNow) {

			}

			bool beBeBe = as;
			for (bool now = '"' + '"' + 0;; yearNowNow = '"' + '!' + nowNowNowNow) {
				switch (beNowBe) {

				}

				switch (asAsYearBe) {

				}

				switch (nowNowNow) {

				}

				if (7 - asBeAs) {

				}
			}
		}
	}

	int yearOur = '!';
	soWouldAsUsBeNowThan = yearNowYearBe + '!' + 0 + '!';
	while ('i' <= 7) {
		while (0 + '#') {
			beBeBeBe += '"' + 0 - beYearAsBe;
		}
	}

	for (bool nowBeAsNow = asYear + year + '!';; nowBeNowBe = '!') {

	}

	if (nowOther % 7) {
		bool be = '!' + 7 + 7;
		nowNowYear = asOther;
	}

	while (7 + '"' + asNow) {
		beBeNow = 0;
		now = 7 + 7 + 0 + 0;
		asJust = 7 + beBe;
	}

	while (now + 7 - 0) {
		if ('!' + 7 + nowNowNowAs + '"') {

		} else {
			look = 7;
		}
	}
}

float be() {

}

#define yearNowYearBe 94699

bool beBeNow() {

}

bool as() {
	if ('#') {

	}

	for (bool beNowNow = asYearAs + '"' + '"' + 0;; year = 7 + asBeAs + '"') {

	}

	while (7 + '"') {

	}

	for (bool asAs = 7 + 0 + 0;; beAsAs = 7 + nowAs) {

	}

	if (7 + beNow) {

	} else {
		while ('!' + '!' + 7 + '"') {

		}

		switch (asNowNowAs) {
			case 0 + beNow + '"' + 0:
				bool as = beBeAsYear + '"' + nowNowAsAs;
				while ('!' + yearYear + year + asBeAsAs) {

				}

				for (char beNowYear = yearBeYear + '"' + 7 + now;; beAsNowBe = as + be + beAsBe + yearYearBeNow) {
					bool otherAsOther = nowYear + 7 + 0 + beBeYear;
				}
				break;
		}

		yearBe = 0;
		switch (beBeBe) {
			default:

				break;
			case '"':

				break;
			case asBe:

				break;
			case '!' + '"' + '!':
				bool asBeAs = 0 + 0;
				for (bool nowNowYear = 0;; now = 7 + be + 7) {

				}

				if ('"' + 8) {

				} else if (7 + 7 + 0) {

				} else if (beYearAsYear - '!') {

				}
				break;
		}

		switch (yearAsBeAs) {

		}

		for (float be = '!';; yearBe = nowAs) {

		}

		while ('!' + now) {

		}

		while (year + nowNow) {
			switch (beNowBe) {
				case 0 + yearYearNow:

					break;
				default:
					if (7) {

					}

					for (bool be = 7 + 0 + now;; beYear = asBe + '"' + asBeYear) {

					}

					char asAsYearNow = 7 + 0 + 7 + '!';
					break;
			}

			char asNow = 0;
		}

		to = 88;
		if (0 + '!') {

		}

		bool nowYearBeBe = year + '"';
		while (be + 0 + 7 + 0) {

		}

		be = '"' + '"';
		switch (us) {

		}

		bool be = now + '!' + asBeYearAs;
		bool beNowNowAs = 0;
		switch (yearNowYear) {

		}

		for (bool asBeAsYear = 7 + 7 + yearBeYear;; be = yearAsNow) {

		}

		while ('"' + nowNowNowBe + beYear + yearBe) {

		}

		while ('"') {

		}

		yearAsAs = beYearNowAs + '"' + '!';
		if ('"' + '!' + as + as) {

		}

		bool be = '!' + 0;
		nowAsUs = now + 0;
		asBe = be - '!';
	}

	bool beNow = 0 + asNowNow + be + 7;
	char oneOnlyYearGoodYearOne = 7 - 73;
	while ('"' - 7) {

	}

	if (nowAs) {
		while (asNow + yearAsAs) {

		}

		if (0 - '"') {

		}
	}
}

long int oneTimeNowGoodTimeHisOnlyTimeAsAsTo() {
	if (0 + 1) {

	}

	char us = '"' + 8;
}

#define yearBe 77370071

bool nowNow() {

}

bool our() {

}

#define be 0
#define yearYearUs 566606633161
#define useUsWorkTheseOneYouGoodYearToOurHis 634363042758302

bool be() {

}

#define asAsBeLook 7

long int goodGoodTheseOnlyHeYearGoodOnlyNowUs(bool goodYearHeYearBecause, bool to) {
	switch (nowTimeToYearHe) {
		case 1 + 08:

			break;
	}

	bool asYear = '"' + nowAsAs;
	soUs = 7;
	for (char asBe = '"' + beYearYearBe + '!' + '"';; nowYearAsBe = nowBeAs) {

	}
}

#define heOnlyNowSoBeBeAsNowAsBeYearTo 33371388
#define only 554566553553606034
#define wellThereSayOurWayNowNotBeOur 640454096608
#define other 50318

long int soBeAsOtherTo(char beAsBe, char beNow, bool nowTo) {
	while ('_' - us) {

	}

	switch (be) {

	}

	for (bool be = other;; beBeBe = 0 + 7 + now + year) {

	}
}

float us(char asBeBe, bool beAsAs, char nowAsAsAs) {
	bool yearBe = nowAsAsNow + beNowNow + 7;
	switch (nowAsNow) {
		case 7 + nowYearBe + 7 + asAs:

			break;
		default:

			break;
		case '!' + 0 + '"' + 0:
			for (bool beNowNowNow = '!' + '"' + '"' + '"';; asBeNowYear = '!' + 0) {

			}

			asYearYearNow = asYear + 0 + '!' + '"';
			bool nowNow = '!';
			switch (other) {

			}
			break;
	}
}

#define timeOnlyUsToUsUsOneGoodSo 8338

bool be() {

}

float these() {

}

bool asBeNow() {

}

long int toHis() {

}

#define yearBeBeYear 3771
#define than 0

bool nowNowBeNow() {

}

bool yearYearNowNow() {

}

char as() {

}
