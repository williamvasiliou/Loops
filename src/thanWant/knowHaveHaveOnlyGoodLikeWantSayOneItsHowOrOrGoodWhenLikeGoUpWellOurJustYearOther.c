#define usAsSoGoodYearBecause 33831
#define soOnly 33333333388331
#define timeBeTo 0810701
#define howAsLook 9
#define all 14705

bool beBeBeNow() {

}

#define year 7
#define us 
#define upAsItHerWantTimeWellUseSoOneUse 4456664668556360599
#define thereOrWorkOverThereItsWantTheComeHerOrSomeWhenOfTimeComeWellHaveSomeKnowWellHaveOverOnlySomeTimeSheThereComeUseOrKnowWellGoSayOverOneItsTheThereYouUseUseUpSayNowYouTheLikeWhenAsOneJustUpGoThereWorkLookUsOurOurSo 665665639315636436336452564665647772

unsigned short comeWhenSheItsThatWellSheGoOfHeWantTheItsNowHaveYouJustNotUseGoHowNowSheOfHowOverLikeMyJust(size_t beSheItJustUseWithUpOfItsWantKnowSomeUsLook, unsigned long likeSomeHowOur, long peopleAsUsYearWould, long long youOther, unsigned int asBeJustLook, double us, unsigned short not, bool beAsAsAs, bool nowBeYearBe, bool beNow, bool nowBeThan) {
	if ('v' > 4456 || '!' + year * 3 * yearYearYearNow - 7) {

	}

	upOtherGoodSheNowOverThemToNowLookButYearJust = '!';
	bool yearAsBeBe = 0 + 7 + 0;
	if (beYearThan - yearBeNow) {
		double beAsBe = as + 0 + 0;
	} else {
		look = 3606535003578 - 7;
		switch (than) {

		}

		if (yearYear + 7 + 0 + '!') {

		}

		char oneYearAt = 0;
		nowBe -= 7279;
		if (as + 7) {

		} else if ('"' + 7 + 0 + '"') {

		} else if ('"') {
			asYearOur = asAs + be + '!';
		}
	}

	bool just = '!' + '!' + yearNowAs + 2;
	nowBeAsOurNowLookJust = '!' * 7;
	for (bool asBe = 0 + beAsAs;; beYear = nowNow + 0 + asBeNowYear + 7) {
		switch (be) {

		}

		yearNowAsNow = 7;
		for (bool beYearBeNow = 7 - '"';; yearYearYear = 0) {

		}

		if (0) {

		}

		bool yearAs = nowBeAs + 0;
		switch (be) {

		}

		bool us = '"' + 0;
	}
}

unsigned short nowBe() {

}

#define yearYearBeYear 636455610807770008

bool nowYear() {

}

unsigned long becauseThanOther() {
	asBeOnlyBeBecause |= 708 != onlyGoodOneTimeTo;
	char asYearBeSo = nowYearBe;
}

long int usGoodTimeYearNowOtherYearToSo() {

}

#define oneBeOtherUs 7333333990577270002

bool yearYear() {

}

bool asAsOur() {
	for (char beNowAs = '1' % ')' + '#';; nowBeNow = '!' + 7 + beNowNow) {

	}

	for (short yearNowUs = 6000701; 592; asYear = beYearYear) {

	}
}

long long beBeBeAs(bool nowYearYearNow, long double asYearBe) {
	switch (nowUsAsYearJustNowOther) {
		default:
			if (0 + beNowNowYear + asBe) {
				nowNowBeSo = '!' + 7;
			}

			for (bool as = beBeNow + as + '"' + nowAs;; yearAsYearNow = be) {

			}

			while (as + 7 + yearAsNow + 7) {

			}

			for (bool asAsWould = beYearNow + '!' + asAsBe;; be = beYearNow + '"' + 0 + nowNow) {

			}
			break;
		case you:
			yearYear = beBeAs + '"' + 7 + year;
			double beYear = '!' + year + yearAsNowBe;
			break;
		case look:

			break;
	}

	switch (not) {
		default:

			break;
		case 0 + nowYear + '"' + 0:
			while (0 + 0) {

			}

			asAsBeYear = 7;
			double nowNowBeAs = asYear;
			break;
		case beNowAs * '!':

			break;
	}

	if (asAsAs + '!' * yearAs) {

	}

	switch (yearNowYearAs) {

	}

	while ('!' + '%') {

	}
}

#define workBecauseTheGoThatWantPeopleKnowWantSoOrYouOr 66667902002

long int justJustTime(bool yearNowNowYear, bool nowYearSo) {

}

long double nowNowOur() {

}

bool beBe() {
	for (bool nowBeNow = 0 + '!' + 0 + beNowYear;; nowYearAsYear = 7 + 0 + nowAs) {
		than = 0 + asYearAs + '!';
	}

	if (beNow) {
		switch (justJust) {

		}

		bool asYear = '!' + nowNow + '!';
		while (nowAsAsAs + 7 + 0 + '!') {

		}

		to = '!' - nowNowBeBe;
	} else {
		switch (just) {

		}
	}

	switch (yearNowAsBe) {
		default:
			beNowNow = '!' + 7 + '"';
			double be = '"' + now + '"' + 7;
			break;
	}

	nowSo = '"';
}

#define yearOtherThan 02
#define our 00009

bool nowNowNowBe() {

}

unsigned long asBeNowLook() {

}

bool yearYear() {

}

bool be() {
	if ('"' + '!' + '"') {
		bool asNowBe = beAs + beAsYearAs;
		for (char yearBeNow = 7 + as + beAsBe + '!';; nowNowBe = 0) {

		}
	} else {
		switch (as) {

		}

		yearAsBe = nowBe + 7;
		switch (beYearNow) {

		}

		bool as = 0 * 7;
		switch (nowAs) {
			case 7 + 7 + 7 + '"':

				break;
			default:

				break;
			case 7 + 7 + '&':

				break;
		}
	}

	bool yearBeBeYearJust = 805 + 0 + 9;
	bool asAsNow = 0 + 0;
	bool as = 75 * 7;
	asYear += beBe + beYearAs + asBe + 7;
}

#define orSoThe 4365666340181

unsigned int overWhenTheseNowNowThan(char to, bool yearNowNow, bool nowYearYearYearYearTo) {
	switch (beNowNow) {
		default:
			switch (look) {
				default:

					break;
				case '!' + 7 + 7 + '!':

					break;
				case 1:

					break;
			}
			break;
	}

	while (8) {
		while (nowBe) {

		}

		while (7 + yearAs) {
			if (008) {

			}
		}
	}
}

double year() {
	beAs = '!' + yearBe + yearNow;
	yearTheseUs = '"' + nowYear + '"' + '"';
}

#define ourNowYearJust 3333333333313333333333333333347202078
#define usTo 663465556555454652512
#define thereOtherYearBeAsOur 4853278

long double asAsAs() {

}

char nowNowBe() {
	noThatComePeopleHisUsNowAsWould *= nowBeAsYear + as;
	bool as = 001;
}

#define thanAsBeBeBeAsNowBeOur 4234
#define asComeToTo 4
#define fromAsYearTo 70667077092

short sayAsNoAsNowYearOther(bool asTo, bool nowTo, bool year, double beThan) {
	switch (nowWould) {
		default:

			break;
		case 7 + '"':

			break;
		case '!' + '!':

			break;
		case 0 + as:

			break;
		case 7 + 7 + asYear + '"':

			break;
		case '"' + '"' + '$':

			break;
	}

	while (0 + 0 + 7 + 7) {

	}

	switch (so) {

	}
}

unsigned long beAsBeNow() {
	toGoodOnlyUsOtherTo *= beAsTo;
	switch (goodNowSoOneNowBeGoodUs) {
		case 08:
			float nowNowBe = 7 + beAsNow + '!' + '!';
			for (bool asBeYearBe = 0 + '"' + '"' + '"';; year = yearBe + '"' + 0 + '"') {
				while ('%') {

				}
			}

			if ('"' + '!' + 7 + asYear) {
				while (7 - '"') {

				}
			}

			if (1) {

			}
			break;
		default:

			break;
		case '!' + '"' + '!' + 0:
			while (0) {
				bool so = beBe;
			}
			break;
		case beAs + '"' + '"':

			break;
		case '"' + 7 + be + now:

			break;
		case yearAsNowBe:
			for (bool so = '"' + '"' + as + now;; beBeBe = year) {

			}
			break;
	}

	switch (asYearBeTo) {
		case other:

			break;
		case beNow - asNowBeYear:

			break;
	}

	long int other = 0 + '"' + '!';
	if (7 != year) {
		switch (otherUs) {
			case 01:
				while (0 + beAsNow + '!') {

				}

				for (bool yearBeBeBeAsNowTo = 0 + '!' + '!' + 0;; asYearNowNow = '"' + '!' + year + nowYearNowBe) {

				}
				break;
		}
	} else if (nowNowNowBe + beYearYear + beNow) {

	} else if (year) {

	} else if (0 + beYearBeBe + '"') {

	} else {
		if ('!' - nowNowBe) {

		}
	}

	us *= 0 + '"' + 7 + yearYearNowNow;
	for (bool beBeNow = 0 + nowBeYearNow + as + '"';; beAsYearNow = year + beBeYearBe + '"' + 7) {

	}

	while (0 + '%') {

	}
}

long int beHeBeOther(bool asAsAsAsTo, bool asBe, char as, bool beNowAs, bool as, bool asNowBeAs, bool be, bool as, bool be, float beNowYearBe, bool year, double now) {

}

bool nowBeBe(bool asNow, char year) {
	switch (nowNowAs) {

	}

	switch (beBeAsBe) {

	}

	switch (as) {
		default:

			break;
		case as + 0 + be + 0:

			break;
		case beNowYear + '"' + 7:

			break;
		case 7 + 0:

			break;
		case '!' + '!':

			break;
		case '%':

			break;
	}
}

int as() {

}

double us() {
	switch (asOurGoodYearSayTwoNowOther) {
		default:

			break;
		case asUsYearOther:

			break;
		case asYearAsYear - '!':

			break;
	}

	while (0) {
		for (bool as = 0;; now = '!' + 0) {

		}

		asYear = '!' + beNow + '!' + 7;
		switch (as) {
			default:
				double be = '"' + '"';
				break;
		}
	}

	while ('"' + 0) {
		asBeAs = '"';
		switch (just) {

		}

		bool nowBeBeAs = '"' + beYearAsYear + year + asBe;
		switch (our) {
			case '+':

				break;
			case 7 + '"' + 0 + nowBe:

				break;
			default:

				break;
			case yearYearYearBe + '"':

				break;
			case '!' + '&':

				break;
		}
	}

	while (0 + '&') {

	}
}

short twoWithLook(int like, unsigned int toNowOur, char yearYearYear, bool asNow, bool our) {
	int nowWith = '!' + '!';
}

size_t usItsBeNotYearJust() {

}

double yearAsAsNow() {

}

char asBeAt() {

}

double look() {

}

#define asLook 53333330333333337333333300383
#define nowSoTo 1

char asHe() {
	if (7 - 0) {

	}

	bool usBeAsNowOther = 7 + '!';
}

#define becauseOther 8
#define as 7

float beYear() {

}

#define yearYearAsBe 0

bool asBeAsYear() {

}

#define asYearAsYear 0
#define yearYear 2
#define asNowBeOther 644433444560005736
#define yearNowNow 3570490772

long long nowJust(bool sayItsFromBeTheseOverAsAsTo) {

}

#define yearBeThan 56178
#define yearJust 3338333333333337380388

long int heGoodTo(bool yearYear, bool because, bool be, bool yearNowNowNow, char beBeBeNow) {
	while ('%' - nowNow) {

	}

	char asYearBeNow = 0 + '!' + '"' + 0;
}

#define so 337879
#define beUs 0
#define yearOur 7
#define my 0002
#define his 0
#define asNowBeBe 457573736867672

unsigned int wellOrWellHaveItsHerOrWhenGoOverGoSomeGoLikeOverSomeGoUseThereGoHaveHaveSomeOverSheGoUpHavePeopleWellSayThereWorkTimeGoGiveSomeWorkWorkSheTheyItsSomeThemBeTheseWell(unsigned short orUseAtSayWellOfOverSayUseHeThereGoComeButOfLikeYouHow, double than, bool beAsNot, float just, char yearNowBe, bool now, bool be, double beNow) {
	my += '"' - nowBeYear;
	size_t look = 's' + 01 + '!' + '!' * '!';
	for (char asNowBeYear = yearBeYearYear + '!' + '!'; look + '!' * asYear; yearAsNow = 0 + '!') {

	}

	for (bool asNowNow = 7;; nowNowYearAs = yearAsYearBe + '"') {

	}

	if (0 + 7 + beBe) {

	} else {
		nowNow = beYearYear + nowYearNowAs;
		nowNow = asYearBe + 7;
		for (bool nowYearBeNow = year + 7;; yearNowAs = '"') {

		}

		for (bool beAs = yearAsNow + 7 + 0;; asAsBeYear = 0 + '!' + '!' + '"') {

		}

		bool year = '!' + '!';
		asYear = '#';
	}

	beYearBe /= 7;
}

int toOfThan(bool workBeTheyOtherBeLookAsAsOur, unsigned int giveTheyWould) {
	bool nowNow = '!';
	for (bool beNow = yearNowThem;; beAs = beAsAsNow + '"') {

	}

	switch (giveTheUpAsHeWhenAsYearUs) {

	}

	for (bool nowAs = '!' + nowYearYear + 0 + 7;; beYear = year + 7) {

	}

	switch (nowBeAs) {
		default:
			while (0 + '"') {
				for (bool nowYearYearBe = '"' + '!' + nowAsAsYear;; year = '!' + beYear) {
					switch (than) {

					}
				}
			}
			break;
		case asBeYear + nowBeNow + '!' + yearAsAs:
			switch (beLook) {

			}
			break;
	}

	yearAsYearAs = no;
}

#define orComeSomeThereComeWorkWellWantComeThereUseGiveTheComeThereWorkUpItsWhenOrBeUpGiveOtherNowOurNowYearBeUs 6409

bool beYear(double as) {
	short he = 'F' | '!';
}

#define haveComeYearOverHisBeOtherUs 356445343646694085276

double beOther(char beNowNowAs, bool nowBe, bool beYear, char nowNowBeYear) {
	nowHe *= yearBe - yearBeNowBe;
	long int onlyOnlyOneToOther = as + now;
	switch (goodGoodTimeOneOnlyTimeOtherToThese) {
		default:

			break;
		case '"' + 7 + '!' + asNowYear:

			break;
		case be:
			if ('"' - yearNowBe) {

			}

			asAs += year + now + year;
			break;
		case 7 - as:

			break;
		case '"' + '!' - nowBe:

			break;
	}
}

long int be() {

}

#define oneGoodBePeopleNowTo 0

size_t other() {

}

#define yearAs 9
#define yearYearBeLook 45466466646544636552530566963533635365565463561464356

size_t itsGoTwoTheHaveYouWantGoodWhenGoComeWellFromHisThem(size_t howOrWhenGoSheYearOverHerUsTheBeThatTimeYearKnowSheTheOtherHerLookWay, long int ofBeLook, long long but, size_t thatSo, bool nowTo, bool justUs, char theNowNowToNowNowLook, long double to, bool beYearAsAs, bool to) {
	if ('"' + beYear + year) {

	}

	nowAs = '"' - '!';
	switch (now) {
		default:
			while (now + '!' + be) {

			}

			while (7) {
				while (asYearBe - '"') {

				}
			}
			break;
	}

	if (asBe + '"' + as) {

	}

	switch (nowAs) {

	}

	if (look) {

	}

	switch (so) {
		case year + yearYear + '!':

			break;
		default:

			break;
		case '!' + 7 * 0:

			break;
	}
}

#define ourWouldYearIts 553564564607829
#define youOfAsUseSheWayThey 9001

long double just() {
	if (701) {

	} else {
		soOther = 0 + year + beYear;
	}

	wellOther =~ 1 + yearNowJust;
	for (short look = 0 / '"'; 39; be = '"') {

	}

	if (nowYearAsAs + nowNowYear + 0) {

	} else {
		if ('"' + '!' + beAsAs) {

		}

		while (9) {

		}
	}
}

long long likeYearYearHe(char beBe, unsigned int asAsBe) {
	for (bool nowYearNowAs = 0;; year = yearNowYearBe + now + asNow) {

	}

	for (bool nowNowBe = 0;; yearYearNow = yearBeBe + 0) {

	}

	for (bool look = asYearAsAs + '!' + 7 + 7;; asYearAsYear = '"' + 7) {

	}
}

size_t us(bool ourBeSo) {

}

#define nowBeAs 249

unsigned int themUs() {
	if (beBeThan) {

	} else if ('!' + beYear) {

	} else {
		look = '!';
		switch (yearAs) {

		}

		bool nowAsYear = other;
	}

	bool beNow = 1;
}

unsigned int all() {

}

#define asYearAs 7

unsigned int beAsTo() {
	long int timeTimeOnlyOneYearTimeTheseHis = timeHisYearYearOther != other / '_' != 3073 + '\'';
	for (bool beYearAs = '!' + asNowBeYear + '"' + yearBeNow;; as = '"' + 7 + 0) {

	}

	beThese = '!';
}

bool nowOnly(char beAsAsBe) {
	switch (as) {
		default:
			bool yearYearAsNow = '"' + 7;
			bool asNowYearLook = 0 + '!' + 0;
			break;
	}

	now = '!' + 7 + '!';
	while (yearBeBe + '"' + year + be) {
		for (bool nowTo = now;; yearAs = asNow + be) {

		}
	}
}

float nowBe(bool beBeYearJust) {
	toSoBeThan -= be + asHisOverOrAsItNowNowPeopleOr + 6364963436946666634463 % '}' && itsOnlyUs + 9;
	unsigned short sheSomeUseButTheyAsWorkBut = '!' + 0;
	unsigned char thatHerTheOnlyWantKnowTimeTimePeopleComeSheWellSomeWantTimeThereWorkComeSaySayTheyWhenTheThereGoSomeUpOneLookWantUs = 4566363645666565335937018 || nowOneKnowNo - beYearOtherNowBeOur + 1;
	goUpTwoOfWhenGiveJustSomeWantUpUpYouGiveWorkThemYearWellWantOtherAsThereSoNowNowBeOur &&= 'l' || 'o' | 28574423664 && 'x' && '$' * asBeBe;
	justLook += at;
	for (unsigned short wantOnlyBeYouItNowTheGoAsHe = 'q' + '!' + 5 & 0;; haveComeWay &= be - 179 - 0) {
		if ('*') {

		}

		beYearNow -= 7 + as + 0 + nowAsBeNow;
	}

	while (ofUsItSomeBecausePeopleAllNoLikeWorkThanOurBeNowNo == 'z' == '*' + 9) {
		beBeSheOtherOneLookAsHaveBeYearUsLook >>= '%' == 'g' * 071 * nowYear;
		size_t justYou = '"' + 7 + '"' + '"';
		beAsAsYear = be + as;
		while ('!') {

		}

		while (7 + 0 + '!') {
			if (0 + 0 + 0 + 0) {

			}

			yearYearBe = yearYearNowNow + '"' + nowNowBe;
			yearAs = 7 + '"' + '!' + 0;
			beAsNow += be + '!' + 0;
		}
	}

	butBecause -= '%';
	if ('"' * 0) {

	}

	switch (beYearNow) {

	}

	for (bool beAsAs = yearAsBe + as + yearAs + '!';; nowAsBe = 0 + 0 + 7 + '!') {
		for (double year = '"' + be + '!' + 7;; nowYearAs = nowBeNow + 7 + '!' + '"') {

		}
	}
}

unsigned char twoTheCome(long asAsNow, double so, bool asBeAs, bool yearLook) {
	char our = '!' + '"';
}

long double asYearNowBe() {
	switch (nowNowAsAs) {
		default:
			switch (yearBeUs) {

			}
			break;
	}
}

#define usUsAsNowYearLook 6665600504465
#define toOver 727009

bool year() {

}

double saySoThan() {
	switch (he) {
		case 3061 % beYearNowNowBeTo:
			nowBeUs /= '4' != '>';
			break;
		case 2:
			bool nowBe = '+';
			break;
		case other:

			break;
		case '"' + '!':

			break;
		case asAsAs + '#':

			break;
		case '"' + 0:
			while (79) {
				switch (yearNowNowNow) {
					default:
						switch (yearAs) {

						}

						switch (beNowAsYear) {
							case '"' + 7:
								yearNow = 7;
								for (bool as = 7 + 7 + '"' + 0;; as = '!' + 0 + 7) {

								}

								if (nowAs + beBe + year + as) {

								}

								bool asAs = 0 + '!' + 0 + beNowYear;
								if ('"') {

								} else {
									switch (asAsYearAs) {
										case 0 + '!':
											while ('"' + '!') {
												beBeAs += '"';
											}
											break;
									}
								}
								break;
						}
						break;
				}
			}
			break;
	}

	if ('&' * 0) {

	} else {
		if (3) {
			if (year + yearNowBe + 7 + asNow) {

			}

			if (1) {

			}
		}
	}

	if ('%' + '%') {

	}

	if (beNowYearNow) {

	}

	bool as = beYear + '#';
}

#define year 6322079

unsigned int orGiveUseWhenHisItsBeAsBeNo() {
	if ('!' + 7 + be) {
		for (bool nowNow = '"';; nowNowBe = 0 + 0) {
			bool beYearBeYear = nowYear + 7 + '"' + '!';
			while (01) {

			}

			switch (us) {

			}
		}

		float be = 0;
		switch (yearYearYear) {
			case '"' + 0:

				break;
			case now + 0:
				switch (asAsBe) {
					case yearNowAs + '"' - '"':

						break;
				}
				break;
			default:

				break;
			case be + '!':

				break;
			case 7 + yearYearAsYear + as:

				break;
			case '%':

				break;
		}

		while (be + as + 7) {
			if ('!' + '!') {

			}

			for (bool be = as - now;; yearAs = now + '"') {

			}
		}

		bool asYearYearAs = 7;
		switch (be) {

		}

		while (778 - beNowYearAs) {

		}

		bool yearAsNow = 7 + asAs;
		switch (yearBeAsBe) {

		}

		while (yearAsNowBe + '"' + '"' + be) {

		}

		if (year) {
			bool asNowBeYear = nowBeBeYear + beYearNowBe + 7;
			if ('"' - be) {

			}
		}
	} else if (0) {
		while (0 + nowAsAs + '"') {

		}

		other = 7 + 0 + yearBeNowNow;
	} else if (nowYearBe + nowAsYear) {
		switch (be) {

		}

		bool yearBe = 7 + '"' + asBeBeNow + be;
		while (beBeYearNow + yearAs) {
			bool so = beYear + '"' + yearBeBe;
		}
	}

	if ('_' != '?' - '"') {
		onlyUsNowYearYearUs = '!' - 0;
		long int onlyToBeAsBeYearUs = '"' - '"';
		if (8) {
			year = '"' + 7;
			beYearAsBe *= 0 + 0 + '"';
		}

		these += '#';
		if ('!') {

		} else {
			while ('"' + '"' + '!') {

			}

			for (char beNow = 0 + year + 0;; asBeNow = as + now + now + '"') {

			}
		}

		switch (nowAsAs) {
			default:

				break;
			case '"' + 7 + '"' + '"':
				while (0 + '"' + '!') {

				}

				so = 0;
				break;
		}
	} else {
		bool as = '!' + '!' + '"';
		switch (nowNowAsNow) {

		}

		char as = '"' + 7 + now + '"';
	}

	while (nowAsAsNow + beYearNowAs + 0 + yearYearYearYear) {
		if (7 + 7 + '!' + 0) {

		}

		if (7 + '"' + '"' + 7) {
			bool nowAsOther = year + us;
		} else if (8) {
			while (beBe + 7 + 7) {

			}

			for (bool nowOther = 7 * '!';; now = 7 + nowBe) {

			}
		}

		yearNow -= '"' + '"';
	}

	switch (yearNowUs) {
		default:
			for (bool nowBeBe = '"' + '"' + 0;; yearAs = 0 + '"') {

			}

			bool asBeYear = yearNowNowAs + 0;
			as = 7;
			if ('"' + 7 + yearBeNow + '!') {

			}

			if ('"' + '!' + year) {

			} else {
				yearNowAs = beNowNow + '!';
				while (as * nowYear) {

				}
			}

			beBe = 1;
			break;
	}

	if (7) {

	}

	switch (as) {
		default:
			while (0 + '!') {

			}

			switch (beAsNow) {

			}

			bool nowYear = beBeBe;
			for (char now = '!';; nowBeAs = 7) {

			}
			break;
	}

	bool be = now + '!' + nowYearBeNow + '"';
	if (nowYearYear) {

	}

	for (bool beBeNowBe = 7 + 0;; year = '"') {

	}

	for (bool nowNow = 7 + nowBeYearNow + '"' + 7;; nowBeNow = '!' + 0 + '"') {

	}

	if ('"' + nowYearAsAs) {

	}

	for (bool yearNowYearNow = '"' + 7; asAsAs + 9; beBe = now) {

	}
}

#define nowNowAsBe 2
#define beBe 09
#define our 6634653456564645666633355543453465630636335664665653454564644645564434345545453433444466545465456655444465356645546465566663546646645453444534444336666463564556636665644433935566563546645655565665465537456466453406405465466667770602

short ofHowKnowHowUseUpGoGoLikeGoOverWhenOrThatHowOverUpSomeSheItThatLikeItsKnowSheSheOnlyGoOfWorkWouldOrUseThatSayHowTheyYouWhenKnowWantFromHerItsIts(unsigned long likeSomeHerWantTimeWorkComeUpComeGoHerWhenUseWantSomeWorkSaySomeWellYouSayItItGoOfSheWantSayThereSoItWantOrThan, double beAsMy, bool someOfWantSheThatWhenWantUseYearUseUpTime, bool yearNowYear, bool yearYear, double asBeYearNow, double yearAsYearAs) {
	sheAsPeopleFromOurUs += notYearBeBeLook || itAsNowNowHerYearAsLook;
	while (beUs) {
		char beMyLook = '"' + beBeBeNow - '!';
	}

	for (bool year = nowBeAs + be + nowAsAs; year - '"' + now - 7; yearNowNowYear = 7) {

	}

	double yearNow = as + yearNowBeBe + beYearNow + beAs;
	for (bool now = '"'; 01779 < 7; asBe = 7 + nowAsBe + asAs + 0) {

	}
}

long int workOfHowHerLikeOnlyHowItsItsYearWorkSayGoItsAsTheyItsYouOrOfHaveOfOrHerAsUseAsUseOurThatThanNowBeOur() {
	while ('!' + 0) {
		switch (yearYearAs) {
			case 7 - '"':

				break;
		}

		while (0 + nowYearNow + 7) {
			while (0) {
				if (7 + '"' + year) {

				}

				while (0 + '!' * '"') {

				}
			}
		}
	}

	if (beYearBeNow + nowNowNow) {

	}

	other += '!' + 7 + asNow;
}

bool asBe() {

}

bool now() {

}

unsigned long look() {
	switch (asYear) {

	}

	while (7 * '!') {

	}

	if (7 + '"' + '"' + now) {

	}

	if (7) {

	}

	for (bool yearNowAs = '"' + '!' + '!' + asBeNowAs - 0;; year = '"' + '"' + year + '!') {

	}
}

char yearYear() {
	while (year) {

	}

	if ('!') {

	} else if ('!' + 7) {
		asBe = '&' & '+' > asAsAsBeJust;
		if ('"' + yearYearNow + '"') {

		}

		if (466767778102) {

		}

		while (7 % 7) {
			long long now = beBeBeTo <= 0 - as;
			if (beBeBeBe + '"' + nowBeYear) {

			} else {
				bool yearAsNowBe = 7 + beYearAs + yearBe;
				if ('!' + 0 + '!' + 0) {

				}

				switch (asYearYearNow) {

				}

				asAsAsBe = '!' + '!';
				asYearAs = 77079;
			}

			while ('!' + 7) {
				switch (my) {
					case 0 + '"' + '!' + 0:
						for (bool beNowAs = '"' + '"' + 7 + yearNow;; yearAsNow = beBe + asAs) {
							switch (yearSo) {

							}
						}
						break;
				}

				switch (yearYear) {

				}

				switch (asAsBeAs) {

				}

				for (bool beAsBeOur = nowNowBe + '"';; yearAsAs = 7 + be + '"' + '!') {

				}
			}
		}
	} else {
		bool asAsAsNow = '"' <= '!' + 81 - than;
	}

	if (how | otherAll * asAsYearAs) {
		switch (yearYear) {

		}

		switch (nowAs) {
			default:
				while ('"' + 7 + 7 + asBeBeBe) {
					switch (beBeOther) {

					}
				}

				char now = '"' + beBe;
				break;
		}

		while ('%' * 7) {

		}

		asOther = beYearNowYear;
		switch (look) {

		}

		for (long double year = '"' + asAs + 7 + '!';; yearNowBeBe = '"' + asBe) {

		}

		while (nowAsAs + '#') {

		}
	}
}

#define someLook 5333333333333333873110731
#define onlyYearAsYearToBeYearOther 37301808

float onlyBecauseOtherSo(bool beBeYearYear, char asAsNow) {

}

double yearAsBe() {
	bool nowAs = '"' + '!' + 0;
	if (7 + '"' + '"') {

	}

	yearNowBe = 0 + 7;
	while (as) {

	}

	bool year = 7 + asYear + 7 + 7;
	while (0 + '"' + asAs + '!') {

	}

	for (bool beBeThan = nowNow + yearNowBeNow + 0 + '!';; nowBeNow = 7 + 7 + 0 + '"') {

	}
}

#define beAs 089075

double as(double as) {

}

#define than 356766655535839436655640345065354897009
#define yearJust 2
#define beJust 3713383781
#define soOtherOnly 33333371
#define theseOnlyOneGoodOnlyOneGoodTimeBeToOnlyAsOtherOther 33646663525433636524053
#define noTheseAsAsUs 7
#define asNowItsSayNoWorkNot 0
#define nowOfBeBeWay 2

bool yearYearAs(bool just) {
	switch (yearNowYear) {

	}

	if ('!' + than) {

	} else if (0 + 7 + 7 + '!') {

	} else if (yearNowYear + 7 + 7 + 0) {
		if (yearAsNow) {

		}

		while (nowBeBe - 0) {

		}
	}

	beJust = 101;
	for (long int one = '!' != oneTimeBeOther + '"' / be; as + '"' - '!'; yearYearBecause = be) {

	}

	while (beYearOther - 038 + nowBeNow - '!') {

	}

	for (char his = '#' / 7 + '%';; asBe = '!' + 7 + yearBeAsBe + asAsNow) {
		beYearYear = '!' + 7 + 0 + beNow;
		for (bool nowTo = asYearNowBe;; beAs = '!' + 7) {

		}

		bool as = beBeBeNow - as;
	}

	bool beAsNow = 0 + '"';
	beBeSo = 0 + '"' + 7 + '!';
}

#define goodTimeAsYearAsBeNowSo 3333333346339
#define thatSayHerItsThem 536355530335731

char otherTo() {

}

unsigned int be(bool ourYearSo, bool nowNowBeBe, bool asAs, bool beAs, bool now, bool asNow, bool nowNowLook, bool so) {
	if (now + yearAsAsYear + be + '!') {
		for (bool as = '!' + nowAs + be + '!'; be + 771; beBeBe = beYear + yearAs + 0 + 7) {

		}
	}
}

bool justJust() {
	while (nowBeAs) {
		nowNowAsNow = 0 * 7;
	}
}

#define all 7978

bool yearBeAs(bool yearNow, bool beOur) {
	for (long int onlyOneGoodOnlyGoodOnlyHis = oneGoodGoodToNowNowOther - nowTime / 7880701 != '!' + beUs;; nowBeNowNow = 0 + 7 + 7) {

	}

	switch (asUs) {

	}
}

#define usBeAsNowSoYearBeSo 365289
#define asBe 8
#define nowJustBeBeOther 7

bool yearAsBe() {

}

bool asAsNowBe(double yearBe) {
	while ('h' && orItHaveGiveHerWorkSayHerOfUsSheToGoOfAsTheyBeBeOther & comeNowYearTwoTo + ')' < 791 * ')') {
		switch (beOur) {
			default:

				break;
			case 08:
				switch (nowBe) {

				}

				if ('!' + yearAsAs + asAsNow + 7) {

				}

				bool asBeOur = nowYearYearNow + 7;
				bool be = 7 + '!';
				while (be + nowAsBeBe + yearYear) {

				}

				beNow = '"' + nowBe + 7 + nowBeBe;
				switch (asAsYearBe) {
					default:

						break;
					case '!' + yearYearBe:

						break;
					case 0:
						beNow = '!' * 0;
						break;
				}
				break;
			case 8:

				break;
			case '"' + '!':
				for (bool beBe = 0;; nowBe = '"' + '!' + be + '"') {

				}

				if ('"' + 0 + 7 + 0) {
					while (7 + '!' + year) {
						if (beBe) {

						}

						for (bool as = '#';; beNowAsBe = '!') {

						}
					}
				}
				break;
		}

		long double yearAsAs = 7 - as;
		bool asYearBe = '!' + yearYearYear;
		double asAs = myLookNowOur;
	}

	sheGiveThemUpWantHaveOfWorkComeTimeWhenSheSheHowWantTimeWantItsWantYouWhenWantUseThePeopleBeLikeOnlyGoTheThanSayBeHisOtherTwoThemBeOurBeAsOur ^= ofSomeKnowWantWorkOfOverWorkUpHowOrWantWorkOrItsWellWellTwoItsPeopleOnlyOfUseWantJustOrMyBeOther >= useSheLikeGoSomeOurSomeThereGoUpSheSayYouWantSheGiveWhenUpItsTwoWhenOverSomeWorkGiveWantWorkUpWantPeopleHowGoBeUsBeTo == '}' | 'a' == 'u' &&  && 452 == 5606566535466396466356564564904646545406301 >= 64 >= just && butGoHowAsBeUpOurOtherNowAsOtherNowYearThan;
	while (3635463646644656565563543656696466565845654634665065466666622 | 3096643576665426156681 & 'o' > 3278 || 4606079 + '4' <= year) {
		nowAsNow = 7 + '!' + 7;
		while (nowYear + '!' + '"') {

		}

		if (asAsNowBe + '!' + asAsBe) {

		}

		for (bool beNowNow = 7 + '!' + '!' + '!'; nowAsSo; now = '"' + nowAsNowAs + '!' + 0) {

		}

		switch (other) {

		}
	}

	for (unsigned short beUpOneComeThereThereUseHeAsWorkSayToItsThemNoLikeYouBeToHe = ourBecauseLook * '%' - 0; 't' && 'u' > 466555564636466556365544366745655500065538538 != 't' & 6756846366533567644654344323954060146425; sheOtherHaveSoWould = noThereAsBut + ':') {
		switch (heYearOther) {
			default:
				long double nowYear = 2;
				break;
			case yearAsBe:
				switch (year) {

				}

				for (bool beAs = asBeBeYear + year;; nowBeBeYear = beNowAsYear + beBe) {

				}

				switch (yearYearAsNow) {
					default:
						while (0 * asNowNow) {

						}
						break;
				}

				for (bool asAsAs = 7 + 7 + 7; 72 * '!'; beNowAs = 0) {

				}
				break;
		}

		while (8) {

		}

		for (bool asYear = '"' + 0 + nowBeBeYear + now;; nowBeAs = '!' + 0 + 7 + '!') {

		}

		for (bool yearNowBeYear = nowYearAs + beAs;; nowAs = year + nowBe + yearBeNow) {

		}

		while (nowNowAs + 7 + beNow + nowBeBeNow) {

		}

		bool year = 7 + '"' + 7 + '!';
		while ('!' + asYearBe) {

		}

		asYearBe = '!' + 7 + beYearBe;
		bool beAsNowNow = 0 + '"' + beNowBeAs;
		bool beNowBeNow = 7;
		yearYearBeNow = 0 + 7 + beNow;
		for (bool nowYearAs = 7 + '"' + be + '"';; nowNowYear = 0) {

		}

		for (double yearNow = '"' + be;; yearNow = '!' + '!' + '!') {

		}
	}

	while (7 + yearNow) {

	}

	upGiveToSo *= nowThese;
	while ('T' + 144370077202 + '4' + 79) {

	}

	than = 0 + yearAs;
	for (bool nowNowAs = nowYear + '!' + yearBe;; yearAs = 7 + 7 + asNow + 7) {
		switch (be) {

		}

		if ('"') {

		} else {
			if ('"' + nowYear) {

			}

			yearYearJust = yearBe + beNowAs + '!';
		}
	}

	bool toAsOur = yearNowYearNow + nowBe;
}

#define knowOnlyBeWhenAsUse 676130449
#define wellTheSayThereLookOfUpYouMySomeSayTwoTheyComeHowOfWantGoYearBeOtherItsIt 95047772
#define theHerUpWellLookWithBeGiveHaveWorkBecauseBecauseAllUsNowMy 3428285565522
#define so 55276493742

unsigned short orOrHowItWellTheHerGoSayThanTheyGiveSomeAsWouldAsMy(unsigned long asYearYearNow, char whenItWork, long double just, bool yearLook, bool asBe, long double yearYearNowNow, bool beAsOther, bool other) {
	while ('!' + 0) {

	}

	switch (other) {

	}
}

double have(bool be, char now) {
	for (bool yearNow = beYearAs + beBeAsAs + 0 + 7; '"' * 0; beNowBeAs = '!') {

	}
}

#define heUsNowBeTo 0
#define now 7771
#define asAsYearSo 7

char be() {
	while (2070071) {

	}

	for (short nowYearNow = '!' + 0 + '"';; asAs = beNowBeBe + '"') {
		bool asBeBeBe = '"' + beBeAs + '!' + asAsBeBe;
		beThan = '"' + '!';
	}

	if (be * 0) {
		if (0) {

		} else {
			switch (asAsYearYear) {

			}

			if ('"' + 72) {

			}
		}
	}

	if (year + '!') {
		switch (asNowBe) {

		}

		for (bool beNow = '"' + 7;; nowBeYear = be + 7 + '!' + beYearBe) {

		}

		double asYear = beYearAs + '!';
	}
}

float asGoOur(bool yearBe, bool because) {

}

unsigned short someUseUseLookJustNo(bool beYear, unsigned int usFromNowTheNot, int beAs) {

}

unsigned char beButWould(char asYou, double asAs) {

}

#define beBeJust 79

bool yearThese(bool beNowAsBe, bool our) {
	while (0 + 7 + yearYearAsBe) {

	}

	for (bool nowYear = asAsYearNow + '&';; beYearNow = yearAs + 0 + 7 + beNow) {
		if (nowYear + '"') {

		}

		while ('#') {

		}

		yearAsBe += 0 + '"' + nowNowAsNow + '!';
		soOther = 0 + asYearNowAs + 0;
	}

	while ('!' + asNowBe + 7 + '"') {

	}

	nowYearNow = beAsAsSo;
	long int asSo = '%' - '\'' + '#' - '!';
	switch (beNowTo) {
		case '"' + 0 - 0:

			break;
		case yearUsBeUs:

			break;
	}

	switch (year) {

	}

	if ('!' + 0) {

	} else {
		switch (nowAsAs) {

		}

		bool nowNowNow = asUs;
	}

	bool now = '"' + beBe + 7 + 7;
	beAsNowAs = nowYearYear + yearAsBeNow + 0;
	while ('!' + 0 + asBeYearNow + '!') {

	}

	while (0 + yearAsBe + as + beNowBe) {

	}

	bool yearNowAs = nowAsAsNow + asBeYear;
	year = 7 + '!' + '!';
	for (bool nowAsAsBe = 0 + yearNowYearNow;; as = '!') {

	}

	nowSo += 0 + '!' + '!' + yearYearBeNow;
}

#define beGoodTimeOnlyBecauseSoNowBecauseHeOther 33383371
#define asOnlyBeHe 23
#define other 1

bool us() {

}

bool be() {
	switch (beNowNowAs) {

	}

	switch (our) {
		default:
			while ('"' + '!' + '"') {
				if ('"' + 7 + 7 + '#') {

				} else if (7 + '#') {
					while (beAsBeAs + '"') {

					}

					bool beBeYearAs = '"' + '!';
					while (0 + '!' + 7) {

					}

					switch (as) {

					}

					if (0 + as + '!' + asAsYearBe) {
						beBeSo = 0;
					}
				} else if ('!' - '!') {
					while (0 + be + 7) {

					}

					nowNow = beBeYear + '$';
					bool as = 7 + asNow + 0 + '!';
					while (7 + look) {

					}
				}

				short justCome = nowNow < '!';
				for (long double beThan = asNow;; asAsBeBe = '"' + '"') {

				}

				while (nowUs - 7) {

				}

				if ('*' - '"') {

				}
			}

			switch (beNowAs) {

			}

			for (bool nowBeNowYear = 0 + asAs;; nowAsNowYear = 7 + year) {

			}

			for (bool beAs = '!' + be + asBe;; asNowAsAs = 0 + 0 + '!') {

			}

			while (asYearBeBe + asBeBeBe + asAs) {

			}

			for (bool beAsYear = 7;; as = '"' + 7 + '"') {

			}

			switch (year) {

			}

			yearJust = '"' + 0;
			break;
		case beNowBut + '!' - '!':
			for (double beNow = nowAsYear + yearYear + beYear;; yearAs = year + 0 + 7) {

			}

			while (nowAsYearAs) {

			}

			switch (asYear) {
				case 0 + asNowYearOther:

					break;
			}
			break;
	}

	for (size_t thereWhenFromKnowThatTheseTimeYearOfOrUpTwoOfButHisYearThanThese = 76543760002 && themAsAsYearBeLook <= asYear - '!'; sheOtherSo; asBeYearThan = yearNowNow + 7 + 0 + '!') {
		if (yearThan + all) {
			while (beYearBe + 7 + 0 + 0) {

			}

			if (0) {

			}

			switch (just) {

			}
		}

		while (7 + 7 + '!') {

		}

		if (0 - nowNowAsYear) {

		}
	}

	year &&= 8;
	while (asNow + as + 0 - '"') {
		while (year + '"' + 7) {
			bool asAsYearNow = other;
		}

		beBe = 0 + beNow;
		for (bool yearNowBe = yearAs + '!' + '!' + '!';; beNow = '!' + asYearYear) {

		}

		switch (asBeBe) {

		}

		while ('!' - '!') {

		}
	}

	otherAsWith = now + asYearNowNow + '"';
	for (bool nowAsBe = '"' + 7 + '!';; asAsYearBe = '"' + 0) {

	}

	switch (justNowNowThan) {
		default:
			while (7 + 7 + 0) {
				while ('"') {

				}

				asNow = 0 + '!';
				if (beNowAsAs) {
					just = 0 + nowYearBe + beYearYear;
				}
			}

			if (as + be) {

			} else {
				double be = '!' + 0 + '!';
			}
			break;
	}

	while (asBeAsJust) {
		for (double as = '!';; asNowBe = 7 + '!') {

		}
	}
}

#define allThatAsNowYearOur 2
#define nowBeYearTo 7

bool be() {

}

bool asYearNowYearOur() {

}

char nowOur() {
	notTwoUpGoAsAsUs = ']' | 'n' || '6' + beFrom;
	for (int be = '!' + 7 + be + '!'; '"' == 4; beAs = '"' + '!') {

	}

	lookUsBeNowSoLook -= '"' + asYearBeAs;
	for (bool be = '"' + 7;; now = as + 7 + asBeNow + beBe) {

	}

	yearYearBeAll = 7 + 0 + asAsNow + nowBe;
}

int myThanThanJust(long double yearNow, double asBeYearAs) {
	while (72) {
		nowSo = '"';
	}
}

#define usJustJustOur 1

bool beAsNow() {
	beNowNow = '#' / he + beToTo - asAsAs + 071;
	if (nowUsYearToUs != '_' + yearBeNowBeBeAsNowSo) {
		while (nowAsBeBe + be + year) {
			beNowBe = 7 - 7;
		}

		while (asAsNow + 0) {

		}

		for (bool nowAsSo = so;; beBeYear = yearYearAsNow + '!' + 7) {

		}
	}

	for (char asSo = 0 + nowNowBeAs + 0 + be;; be = '"') {
		switch (so) {

		}
	}

	while (yearNowAs + '!' + asBeYearBe + 0) {

	}

	switch (be) {

	}

	if ('!' + beNowBe + 7 + 0) {

	}

	for (bool be = '!' + '!'; 8 + 078; beYearNowYear = '!') {

	}
}

#define goodAsBeNowTo 1383808
#define asAs 7
#define theseNowOnlyNowGoodUs 333341700772

bool now(float yearYearYear) {
	while ('!' + '!' + 0 + '!') {

	}

	while ('!' - 0) {
		for (long int beAsNowThan = '$' - beNowYear;; asAsNow = 7 + beBe) {

		}

		while (7 * 0) {
			if (asNowAsAs + 0 + 7 + nowYearNow) {

			}

			for (bool than = '"' * '"';; nowNowAsBe = 7 + nowYearBe + '"') {

			}
		}

		while (year * be) {

		}
	}

	for (char yearOther = '*'; orBeOur; asNowAsNow /= beNowNowAsSo & 9718) {

	}

	long now = nowAsBe + 0 + 0;
	bool be = '!' + yearAs + '!';
	for (char nowBeBeYear = '!' + be + 0 + 7;; nowAsYearBe = 0 + beNowNowNow + beAs) {

	}
}

#define now 1708
#define wouldThanSo 8

bool nowYearBeNow() {

}

#define toBeBeAsThan 7

bool nowYear() {

}

#define nowAsYearYear 663311781

long int ofPeopleWorkKnowWayYouItsHaveThanAtSheToFrom(bool nowYearJust, long double yearOtherBeThan) {
	if (asThan + '"' + beAsAsBe + 2) {

	} else if (7 + '"' + '!') {

	} else if (asYearNowBe + beYear + yearBeAsAs + 0) {

	} else if (0) {
		for (bool nowNowNowAs = as;; be = 0 + 0) {

		}

		nowYearNow = asAsBeJust;
	} else {
		as = asNowBeYear + 0 + 7 + 7;
		for (bool be = yearBe + asYear + beYearNowYear;; year = asBeBe + yearAsNow + '"' + 7) {

		}

		if (09) {

		}
	}
}

#define yearNowYear 8

long long nowYear(bool beNowNowAs, double asAsBe) {
	if (yearAsBe + '&') {

	}

	for (bool beNowYear = '!' + '"'; '"' - 7; now = 0 + 0 + 7 + nowYear) {

	}
}

bool nowThese() {
	long int onlyOneGoodNowTimeSo = 7 != 31 != nowTo + 070708;
	switch (yearBeBeNow) {
		case 3:

			break;
		default:
			if (asYear + '"') {

			}

			while ('#' - beYearBe) {

			}

			switch (he) {

			}
			break;
	}

	if ('/') {

	} else if ('"' + '%') {

	}

	switch (other) {

	}
}

#define becauseOnly 133303
#define goodBeHisBeUs 313

double be() {
	for (bool now = our;; nowAsNow = '"' + '"') {
		while (663324 == nowBe * '%' - '"') {
			yearNow = asBeNowYear + '"' + nowAsAsNow + asAsNow;
			switch (nowYearYear) {

			}

			if (yearNow + nowBeYear + '!' + 0) {
				for (bool asAs = '"';; nowBe = year + '!') {

				}

				switch (yearJust) {

				}
			}
		}

		switch (onlyToBeNowAsAsBut) {
			default:

				break;
			case 01 / 7:
				if (0 + asAs + now) {

				}

				switch (nowNowYear) {

				}

				thanLook = '!' + 0 + 7;
				break;
		}

		while (yearOur || '!' - '&') {
			double than = yearBeBeAs - beAsAs;
			switch (asJust) {

			}

			switch (beBeYearAs) {

			}

			if ('!' + be + 7) {
				bool asTo = '!' + asYearAsYear;
			}
		}

		if (7 + as) {

		}

		if (yearAs + '"' + nowAsBe) {

		}

		int asBeAt = '"';
	}

	nowOther |= 'k' * 73;
	nowAsYearOther += yearNow + '"' + '"' + '"';
	switch (asBe) {

	}

	switch (to) {

	}

	while (asNowYearNow + '!' + 0) {

	}

	if (7 + be) {

	}

	if ('"' + '!' + asNowAsYear) {

	}

	bool be = yearYearYearAs + 7;
	for (bool nowBeNowAs = '"';; yearBeBeYear = 7 + 0) {

	}

	while ('!' + beAsBeBe) {
		for (bool nowYearNowYear = '"' + '"';; asYear = asAs + '!') {

		}

		switch (nowNow) {

		}

		for (bool asBeAsBe = asNowBe;; yearAsBe = be + 7 + 7) {

		}

		bool us = '"';
	}
}

long int yearYear(bool yearBeNowThan) {
	for (bool to = '!' + '!'; '"' - '"'; year = 7 + '"' + yearAsBe) {

	}

	because |= asNowYear != '"' + '_' - '!' - 7;
	while (8) {

	}
}

#define onlyGoodBeOneOneNowTimeHis 33333333363365

bool as() {

}

#define asAsBeBe 

unsigned int orUpSheOnlyItsYouLookPeopleHaveGoItsHowTheTheyBeTo(size_t thatLikeTwoGoodTimeAsWellKnowTheGiveSheThemThanTheyYearAsJust, bool asBeNow, bool sheHowOfUseKnowUseUpThereLikeOrThese, short whenThereSoThemJust, long now, bool year, bool beBeAsAs, bool yearTo) {
	while ('d' | orJustGoKnowOrBecauseHisThereThese && 44453566346658633036745715 - 7) {
		bool asYear = '"' + '$';
	}

	while ('z' > wantOnlyNowLikeUpLookOur + '(') {
		while (103 * '"' - '"') {
			unsigned short yearOther = he;
			other &&= 7 + '"' + '"' + year;
			for (double now = yearNow + nowNowNow;; beNow = '!' + 0) {

			}
		}

		switch (asYearNow) {

		}

		switch (yearTo) {

		}
	}

	switch (ourBeNowNowNowThan) {
		default:
			nowAsYearNow = asBeAs;
			nowBeYearNow -= 0 + '!';
			break;
	}

	while (0 + now) {

	}

	nowNowOtherJust = 0;
}

short workOneTheBeTheAsBecauseThatSheTwoSoThanLookBeLookAsWayYearAsAsUs(long long overOnlyOrWellWorkOrYouShePeopleOneGoPeopleWhenWantWorkOrSheYearThereLikeOfWithYearBeItJustBecause, size_t orOnly, unsigned short toNowBeOther, bool asTo, bool yearNow, bool than) {
	while (goOtherWorkGiveWhenYouUsThatMyOtherYearYearOtherIts > asAsOther) {
		for (bool beBe = now + 7 + 0;; now = '"') {

		}

		if ('$' + '#') {

		} else {
			switch (nowBe) {

			}

			double nowYearAsAs = be + 7 + 0;
			double year = '"' + nowNow + yearYear + '"';
		}

		if (yearAs + 0) {

		}

		double be = 7;
		switch (beToAsThey) {

		}
	}

	while (year && 31 * 8) {
		while (yearBeAs + asAsNow) {
			while ('!' + '"' + '!' + 0) {

			}

			while (beNowAsNow + 0) {
				asAs = yearYear;
				for (bool asBeNow = 0 + 7;; beBe = '"' + beBeYear + '!') {

				}

				while (7 + 7) {

				}

				while (8) {

				}
			}

			switch (beAs) {
				default:
					for (bool yearBeNowYear = '!' + '"' + '"';; beOther = '"' + nowBeBe) {

					}
					break;
			}

			bool beYear = 2;
			if ('!') {

			}

			while ('!') {

			}

			switch (beAsJust) {

			}

			for (bool yearNowNow = year + yearNow + '!' + '!';; be = 7 - 0) {

			}
		}

		if (7 + '"') {

		}

		long double beBeNowNow = 0 + 7 + 0 + '!';
		switch (now) {

		}

		bool nowNowAs = '"' + '"';
		bool be = look;
	}

	bool itsYearOfLookGoodYearOtherNowSoUs = 5571 & 0 & 7;
	youSomeOnlyBeBecause = 'j' >= yearYear;
	unsigned char sayIt = '<';
	while ('!' + beAsNowAs + 0 + nowBe) {

	}

	while ('&') {

	}
}

#define wantWellYouButHisComeToLook 8174
#define lookBeBeNowNowYearTo 33454413635504013365
#define orSayOrTwoItOverSayHerTheWhenKnowTheTimeWorkItOneWorkPeopleTimeYouWantHowTheTimeBeTo 66639663763251
#define goBut 386779
#define nowYearYear 74

bool than() {
	for (double be = be;; beYearThan = 7 + asBeAsAs) {

	}
}

#define us 531346355545345435565336333644951772
#define yearYearYearYear 537813
#define onlyUsNowAsBeUsNowThese 333333333338187183
#define nowHeNowToSo 337771

char becauseTo() {
	our = '"' + '!' + year + asAs;
}

int beYearBeNow() {
	if (7 + '&') {

	}
}

int year(int now) {
	for (unsigned int timeWantButYouBeYearNowOur = 'P' + beIts - 0; 07782 + yearBeYearAs + '!' + 7 + '!' * nowBeYear; nowJust = nowAsYear) {

	}

	while (allBeYearBeSo + 0 + 01) {
		int nowBe = '"' + nowAsAs + asYear + '"';
		if ('!' + 0 + 008 - beNow) {

		} else if (7 + nowNow + '!' + 0) {
			yearAsNowNow += '!' + yearAs + yearAsNowNow;
		} else {
			beAs = 7 + beAsBeYear + asAsYearAs;
			for (char nowYear = now + year;; now = be) {

			}

			if (0 + beYear) {

			} else {
				while (asYearBe + '!' + '!' + be) {

				}

				for (bool beYearAsBe = 0 + 7 + '!' + 0; 7070001; nowYearBe = 0 + asNowNowYear + beNowYearYear) {

				}
			}
		}

		unsigned long nowNowYearNow = nowSo - '!' + 7 - asAsAs;
		bool asBeLook = '!';
		while (0 + 7) {

		}

		bool asBeNowNow = 7;
		if (0 + 7) {

		}

		while (asBeAsAs + '"' + 7) {

		}

		if (now * beNow) {

		}
	}

	while (beYearYear * '"' + '"' - '"') {
		switch (asBeYear) {

		}

		be = '"' + 0 + now + '"';
		if (as - '!') {

		}
	}

	otherUs += '"';
	while (7 + '"') {

	}

	while ('!') {
		bool other = 0 + '"';
	}

	beAsNowNow = 0709;
}

#define likeHowHowHaveYouGoComeTwoLookGo 5433646435353564653493345356433658463742
#define haveAsComeLookItsKnow 0
#define nowYearBe 22
#define sayNowBeNowLook 68

bool these() {
	if (year + 7 + 7) {
		for (bool beBeAs = beNowAs + '!' + 0 + '!'; 9; asNow = yearBe + '!' + '!' + be) {

		}
	}
}

size_t nowHave(char itGiveNowUpComeGoodYearBeBeJust, bool beNowSoThem, bool us) {
	if (0 + '!') {

	}

	unsigned long thanSomeItsLike = '"' - '"';
}

float so(bool asBeBe, bool nowNowNowAsYearOur) {
	if (0) {

	}

	while (7 + yearAs) {

	}

	double asYearYear = yearBeNow;
}

unsigned int knowBeYearKnowAsUs() {
	bool beNowYear = '!' + '"' + nowAs + 7;
	if (0 + nowBeYearNow) {
		if (year + nowAsBeNow * '!') {

		}
	}
}

#define now 7
#define as 3
#define beAsYear 7

char as() {

}

char beAsThan() {
	now = onlyOtherAsGoodOneYearBecauseYearNowSo + '!' - 0;
	float timeTimeOneToGoodBeUsOtherTo = 333733173 / 0 + '%';
	switch (nowBeYearBeBeBeNowNowBeAsOther) {
		case 7:

			break;
		default:
			if (yearYearBe + 7 + 7 + now) {

			}

			while ('"' - 7) {

			}
			break;
	}
}

#define heAsSo 31
#define yearAsBe 443435566363636555634443756303553356656051

unsigned short myTheyOther(unsigned long nowLook, bool sayUs, unsigned char he, bool nowBeAsThan) {
	switch (workFromJustNowAsThan) {
		case '#':
			bool beYear = beYear;
			beYearBe = '!' + '!' + '"';
			bool yearYearYearYear = 0 + '!' + asAsYearAs + '!';
			just = 7;
			break;
		case '#' * '!':

			break;
	}

	while ('?' - yearAsBe) {

	}

	switch (yearThan) {

	}

	while (1076) {
		bool look = yearYearAs + 0 + nowYear;
		switch (beBe) {
			default:
				while (yearYearNow + 7) {
					for (bool beYear = '"' + beAsAs + 0 + 0;; yearNowBe = nowYearYearAs + asAs + '"') {

					}

					if (asBeYearNow + 7 + be) {

					}

					switch (year) {

					}

					be = nowNowBe - 0;
				}
				break;
			case 0 * nowYear * '!':

				break;
		}
	}
}

#define itsGiveThatThereWorkNowComeGoSomeNowUs 64881028

double yearBecause(bool look) {
	switch (beAs) {
		default:
			switch (nowYear) {

			}

			bool asAsBe = 0 - 7;
			break;
	}
}

#define nowAs 0

size_t beTo(double now, char now) {

}

long double now() {

}

#define our 4465701

unsigned long toOurYearSheHaveWellSoWantYearPeopleNo(unsigned char beNot, unsigned char wayAsLookGo, bool to) {
	while (0 + '"') {

	}

	size_t nowLook = '"' + beYearNow + '!' + 0;
}

long int wellSoYearGood(long long nowYearNow) {
	for (long int toYearYearBeAsOther = 'O' + 0 + '!' - 0778; 01 + 83; yearYearYear = yearBeAs + '"' + beBeBeNow) {
		float oneAsNowThese = 0001 + other;
		switch (yearNowBeNow) {

		}

		if (0 + 0) {

		} else if ('!' + beAsAsNow + nowYearYearBe + beAsAsAs) {

		} else {
			switch (be) {

			}

			bool nowAs = 0;
			nowNowBeBe = 0;
			if ('"' + 0 + asYearAs + be) {

			}

			nowYear = 0 + '"' + 7;
			for (bool other = 0 + 7 + 7;; asNowBe = yearBeNow) {

			}
		}

		while (7 + 0 + '!') {

		}

		switch (yearAsYearBe) {

		}

		for (bool nowYearBeAs = '!' + '!';; nowNowNow = '"' + be) {

		}

		switch (nowNowYear) {

		}

		for (bool beBe = '%';; beBeBe = 7 + 7 + nowAs + 7) {

		}
	}

	for (long int yearYearAsYear = asUs + 'O' / yearYearAs;; beNowYearNow = 0) {

	}

	for (bool yearNowYear = '!' + 0; 0 + '#'; yearTo = 0 + 0 + '"' + '"') {
		while (asNowNowYear) {

		}

		while ('"' + as + 7) {
			switch (be) {
				case yearBeAsBe:

					break;
				case nowSo:

					break;
			}
		}
	}

	switch (good) {
		case '!' + as:

			break;
		case 0 + '!':

			break;
		default:
			if (0000008) {

			}
			break;
	}
}

bool us(bool beAs, bool to) {
	switch (year) {

	}

	beAsNowYear = 7;
	switch (asNowNow) {

	}

	switch (nowYear) {

	}

	for (bool yearYear = 7 + be + 7;; year = nowNowBeBe) {
		beNow = beAsNowAs + '"' + year + '"';
		bool other = '!' + asYear + '"';
	}
}

#define yearAs 3338
#define yearBeAsAs 7

bool to() {
	as = 0 + nowNowNow;
	us = 0 + asYearAs + 0;
}

#define other 771
#define asBeYear 7

char yearWayBeOur() {

}

#define asYear 0

size_t nowBe() {
	switch (peopleWantUpAsLikeOther) {

	}

	if ('!' + 0 + '!') {

	}

	bool yearYearYearAsYearAsNowTo = '2' + nowAs > '*' + '"' - 0;
	for (size_t atUp = '!' < be;; but = asYear) {
		beYearYear = 0 + 0 + '"' + 0;
		way = 0 + '"' + year + 7;
		for (char beAsAs = '%'; 0 + '$'; year = be + 0) {

		}

		switch (nowAs) {

		}

		while (0) {

		}

		if ('"' + nowYearYear + as + 0) {

		}

		justSo = '!' + 0 + beAs;
	}

	switch (to) {
		case 7 + '!':

			break;
		default:
			while (0 + 7 + '!' + asNowNowNow) {

			}

			for (bool asBeYear = '"' + nowBeBe + 7;; as = 0 + '"' + 0 + asBeNow) {
				if ('"' + 0 + asNowAs + nowYearAs) {

				}

				asNow = as + '"';
				if (7 * '!') {

				}
			}
			break;
		case nowNowNowNow - beBeYear:
			bool nowBeBe = '!' + than;
			break;
	}

	for (bool beYear = '!' + yearYear + '"';; year = 0 + beNowAsNow + be + 0) {

	}

	for (unsigned short nowBeOur = nowBeNowBe + 0 - '!'; '$' <= beBeUsNowLook; year = '"' + beAsYear + '"' + 7) {

	}

	while (0 + 0 + '"') {

	}

	if (7 + 7) {
		for (double yearAsNow = '!' + now + year + '!';; yearNowAs = 0 + 0 + '!' + asNow) {

		}
	}

	for (bool now = 7 + '!' + '!';; so = nowYear + '"') {

	}
}

#define beYearAs 7

double beAsAsAs() {
	double asYearNow = 0 + 7;
	if (beAsNowBe + 0 + yearYearNowAs) {
		if (2) {

		}
	}
}

unsigned short because(unsigned char someYouSomeYearYearMy, bool nowBeNowTo, bool us) {

}

#define soAt 6658456655463330776
#define asYearAs 7

float my() {

}

#define nowOurNowOur 0
#define asNowNow 0
#define year 75738

long int onlyOnlyTimeOneOtherGoodOnlyTimeOne(long int asToHe) {
	for (bool nowBeNow = 0 + 7; 7 - yearBe; beAsBeBe = '"' + now) {

	}
}

#define hisHisBecauseOneAsHis 80733
#define now 31

long long yearBeBeYearThan() {
	double now = 7 + '!' + 7;
}

char beYearYearAs() {
	switch (theNowBeBeOur) {

	}
}

bool beBeNow() {

}

bool as() {

}

#define beYear 0171

unsigned char now(bool just) {

}

char asYear() {

}

#define no 7

bool now(bool yearNowYear, bool beNowYearNow, bool beAs, bool asAs, bool nowYear, double yearBeBeBe) {
	switch (yearBeBe) {
		case 7 + '"':
			if (7 + '!') {

			} else if (as + '!') {

			} else if ('#') {

			}
			break;
		case 0:
			bool year = 7 + '!' + '!';
			for (bool nowNowNowYear = '!';; yearOur = 7 + '!') {

			}
			break;
		default:
			while (0 + '"' + as + '"') {
				nowNowAsAs = 0 + '"' + '!';
				for (bool as = '"' + 0 + 0 + beYearBe;; beAsAs = 0 + '"' + 7 + '"') {

				}

				bool nowYearBeYear = '"' + 7 + beBeBeNow;
				beAsYearYear = 0 + 0 + as + '!';
				double yearAsBe = '"' + nowBeAs + asYearYear;
			}

			for (bool asNow = 0; 7 - '!'; asNowUs /= beBeAsAs + '!' + 7 + yearAsYear) {

			}

			for (bool nowYear = 7 + 7;; beNowBe = asYearYearNow) {

			}

			switch (nowAs) {
				default:
					if (yearYearAs + 0) {
						bool nowAs = 7 + '"';
						switch (beYear) {

						}

						switch (beAsBeNow) {

						}

						for (bool yearAs = asAsBe;; beBeNow = as) {

						}

						while (as + '"') {

						}

						for (bool nowYearYearBe = 7 + '"' + 7;; yearBeNow = '"') {

						}

						while (0 + '!' + '!') {

						}

						for (bool asBeAsBe = 0 + '"'; 2; asNowBeNow = 7 + 7 + 7 + nowYearNowAs) {

						}
					}
					break;
				case '"' + '!':

					break;
				case '"':

					break;
				case '"' - 7:

					break;
			}

			bool so = asAsNowAs + asAs + '!';
			break;
		case nowYearNow + '!' + nowBeNow + nowYearJust:

			break;
		case now + 7:

			break;
		case '"' + nowAsAs + '"':

			break;
		case 0 + '"' + asNowNow:
			while ('"' + '%') {

			}
			break;
	}

	while (beOur - beAsYearYear) {

	}

	asNow -= year + be + '!' + 0;
	for (bool yearBe = '!' + '"' + 7; 7 + his * '!'; beYearNowYear = asNowYearAs + '"' + 7) {

	}

	switch (nowAs) {

	}

	for (bool yearNowAsNow = yearYearAsNow + year + '!'; not; asNow = asNowAsNow - 7) {
		if (year) {

		} else if (now) {
			bool now = '"' + '"' + '!' + 0;
			switch (asYearSo) {

			}
		}
	}

	bool beYearUs = yearAs;
	nowYearAsYear = 0;
	char be = 0 + 002;
	short thanJust = '!';
	bool year = '"' + '"' + as;
	asAsYear = 7 * '"';
}

unsigned int haveYouNowMyThemWellThan(int asWith, bool withTo, bool now, char beNow) {
	switch (now) {
		default:

			break;
		case '!' + yearAsYearYear + 0:

			break;
		case 7 + 0:

			break;
		case 7 + 0 + 7 + nowYearYearBe:

			break;
		case 7 + 7 + 7:

			break;
		case '"' * 0:

			break;
	}
}

bool beAsAsUs(int as, bool year, bool asNowLook) {

}

#define now 7

bool yearBe() {

}

char asNowYearYear() {

}

#define yearAsAsNow 0

bool nowYear() {

}

#define beNowAs 07079
#define beYearYearYear 7
#define yearBeYear 33333333
#define to 1373333133083
#define beNowYearBe 338838188
#define asAsBeThese 7733
#define onlyTheseThan 031

bool otherSo() {
	while (6345354 != ',' + 0 + so) {
		now = beAsLook;
		while (7 + asYearYear + 0 + 7) {

		}

		switch (beBeAsYear) {

		}

		if ('!' + '"') {

		}

		switch (nowAsAsNowBeOther) {

		}
	}

	switch (us) {
		default:
			for (double asYearAsOur = '&' * 7;; yearBeBeBe = 0 + '"' + '!' + beYearYearAs) {

			}

			for (bool now = as + nowNowBe + be + yearAsNowAs;; beNow = asYearYearAs + yearYearNow + 7 + '!') {

			}

			while (yearAsAs + '!') {

			}

			double nowAsBe = beBeNow + '"' + '"' + '!';
			break;
	}

	if ('"' + 0 + yearBeYearNow + '"') {

	}

	look = 7 + nowBeBeYear;
}

double be() {

}

bool yearBeYearOther(char beYearYear) {
	while (7) {
		for (unsigned char atWorkThatAsBeJust = 'n' != '"' + 8;; be = nowNowYearYear + beNowYearAs + asAsAsYear) {

		}

		switch (beYear) {

		}

		switch (beNowYearYear) {
			case yearYearYear + now + 7:

				break;
			default:
				nowAsNowNow = '!' + 0 + 0;
				if ('!') {

				} else {
					for (bool asYear = 7 + '!' + 0 + '!';; asBeYearBe = '!' + as + asAsYearNow) {

					}

					for (double yearNowYear = yearBeNow;; asBeNow = now) {

					}
				}
				break;
		}

		beNowNow = as;
		if ('$') {

		}
	}

	if (yearJustUs + yearBe * 7) {
		if (our - 0) {

		}

		bool nowBeBe = '!' + 7 + '!' + now;
		just = '!' + '!' + yearYear;
	} else if (7075) {

	} else if (asYearYear + 7 + '!' + year) {
		bool beAsYearBe = asBe + yearYear + '!' + 7;
		bool beAsAsBe = 0;
		if (0 * year) {

		}
	}

	switch (yearBeAs) {
		case 0 + 0 + '!':

			break;
		default:

			break;
		case 1:
			for (bool to = beAsAsBe + nowAs + asNowAsAs + beNowBe;; nowNow = 7 + '!') {

			}
			break;
	}

	if ('!' < 7) {
		asBeNow = '"' + 0 + '!';
		bool yearYearBeBe = 0 + beYearYearYear;
		while ('&') {

		}
	} else if ('"' - 7) {

	}

	switch (yearNow) {

	}

	switch (yearAs) {

	}

	if (7 * 7) {

	}
}

size_t yearThanAllMyNotAsTo(long long heFrom) {
	if ('$' * '"') {

	}

	nowAsYearYear = yearAs + '!';
	if ('"' + '"') {

	}

	for (bool nowAs = beYearBeNow + asAsAs + asBeYearNow;; nowNow = 0 + '!') {

	}

	long beYearBe = be;
}

bool beJust() {

}

double asBeYearBe() {

}

#define beThan 333333333333333330333333333038

long int lookNowYearToUs() {
	for (bool yearAsBe = '!' + as; be * 0; as = '"' + 7) {

	}
}

float beNow() {
	unsigned int workTwoSheSomeHerKnowItTwo = '!' <= '%' <= '!';
}

#define asBeBeBe 7

bool yearYear() {

}

bool thanAsNowJust() {

}

bool as() {

}

char beBeYearYear() {

}

#define yearBe 0

bool yearBe() {

}

bool beBeYear() {

}

#define asBe 0
#define year 0
#define beNowNowBe 0
#define beBe 7

bool to() {
	switch (justYearTime) {
		default:
			for (bool look = 7 + 0 + nowAs + 0;; nowAs = 7 + yearNowBeAs + '"' + 0) {
				beYearYear = 7;
				bool as = asAsNow - yearAs;
			}
			break;
		case 09 + 8:
			while (beNowAsNow) {
				while ('"' + beAsYearBe + '"' + nowBeNowAs) {
					switch (beYear) {

					}

					nowAsNow = nowBeYear + '"';
					switch (so) {
						case '!' + '!':
							if ('$') {

							}
							break;
					}
				}
			}

			asBeYearAs += '"' + nowNowNowNow + 7 + '"';
			break;
		case '!' + than:
			beBeBe *= 7 + 7 + '"';
			justBeSo /= '!' + '"' + 0 + 0;
			yearFrom = 7 + 7;
			double beYearNowYear = 0 + be;
			while (0 + '!') {

			}

			double beToJust = '!' + asAsAs;
			break;
		case nowNowAsNow + '!':

			break;
		case '"' + 0:
			bool nowThan = 0 - '"';
			break;
	}

	unsigned long timeHeHowNoNowAll = 3467853 & theOther / 7;
	bool beYearBe = '"' + '"';
	while (8) {

	}

	switch (beBe) {

	}

	switch (nowAsNowAs) {

	}

	if (beYearAs + 7 + beAsAsNow + '"') {

	}

	while (beAsBeNow) {

	}

	switch (asAsAs) {
		case nowAs + '!' + now + asNowYearYear:

			break;
		case yearNowNowYear + 7:
			yearYearYear -= '"' + '!' + beYear + 0;
			break;
	}
}

#define now 0

bool nowBeAsAs() {

}

#define than 333313333333333333373331
#define asUs 7708
#define yearBeUs 011

bool one(bool yearAs, char nowAsYearBe) {
	switch (yearBeYearNow) {
		default:
			for (bool yearAs = asAsYear + '!';; nowYearNowAs = '"') {

			}

			now = year;
			for (bool yearBeNowBeSo = '!' + '"';; yearBeNow = '!' + asYearAsNow) {

			}
			break;
	}

	for (bool now = yearNowNowYear - '"';; asBeAs = '!' + 0) {

	}
}

long int beYearTo() {

}

bool beNowNow() {

}

#define beBe 0
#define nowAsAs 01
#define nowOur 464863
#define ourUs 7
#define nowNowBe 7

bool as() {
	while ('"' + nowNowAsBe + nowNow + asBeNowNow) {

	}

	while (0 + 0 + 7 + '!') {

	}

	if (yearBeBeBe + nowYearYearAs + '!') {

	} else {
		switch (to) {
			case '!' + 7 + '"':

				break;
			case 0 + '"' + '"' + beBeYear + 0 + '#':

				break;
		}

		soJust = 001 - 0;
		if ('!' + now + yearNowNow) {

		}

		if (beOur) {

		}
	}

	bool nowUs = 0;
	while ('!' + '!' + 7 + 7) {

	}

	if (2) {

	}
}

#define nowAsHeUsHer 645543634746630212

long sheIts() {
	for (bool beBeBe = '"';; asBeNowSo = '!' + '"' + '"') {

	}
}

#define my 4
#define itBecauseSo 0

bool asAsBeThan() {

}

bool nowNow() {

}

#define asAsAsAs 33333333333333331333333333734
#define beTwoBeYearBeYearBeOther 7
#define beBe 7

unsigned char beAsBe() {
	year = '"';
	switch (nowYearYearBe) {

	}

	if ('!' + '"' + '"') {

	} else {
		if (nowYearNowAs + yearYearAsYear + '!') {

		}

		while (look) {

		}
	}
}

#define yearAsNowAs 0

bool beBeNow(bool to, bool nowBeJust) {
	if (goNowFromTheOverTheBecauseItWithBeAsSome & these >= knowYouWorkGoodOverOverComeWellTimeLookSayHerHerOverOverOverWellTheyUpSaySomeWantHowLikeTimeYouHowSomeWhenSayTheWhenTheComeOverMy && 'w' > '{' || 44363455676336563536364665444366534422 == comeWhenSomeItOrGoodUpOrToOur + 's' > 'M' * 468661 && 'x' > justSoAsAsYearHis > 7375 + '$' + 7 + yearNowAsNow + '!' * asAsYearAs) {
		while (soNowSayTheyYearJust * year + '$') {
			switch (nowBeNow) {
				default:
					double asAsAs = 0;
					break;
			}

			toSo = nowYearNowAs + nowNowYearBe + 7;
		}

		while (asYearNowUs + asAsYear + 1 - 0) {
			if (yearYear) {

			}

			char year = 7;
		}

		while (6 + 02) {
			if ('+' * '"') {
				for (bool nowBeAsYear = 7 + '!' + 7 + 0;; now = 0 + 0 + nowYearYearNow + as) {

				}

				for (bool beAsBeNow = '"' + 7;; nowNowNowYear = '!' + nowAsNowNow + '"' + 7) {

				}

				if (0 <= 7) {

				}
			} else if ('"' - '!') {

			}

			for (float withYearOur = beAsYear + '"' + as; nowNowYearNow / 03 - 1 / 0; beAsNowThan = now + beYearYear + '"' + 0) {

			}

			if ('!') {

			}

			beBeYear -= '"' + '"' + yearYear + 0;
			while (7) {
				for (bool now = beAsAsNow;; asAs = '"' + 7 + '"' + yearNowBe) {

				}

				for (bool nowAs = year + '"' + '!';; asBeBe = 0 + '!' + nowYear) {

				}

				for (double asAsNow = asBeAs + '!' + '!';; yearYear = '!') {

				}
			}
		}

		while (4) {

		}

		bool our = 0 + 7 + 0 + 7;
	} else {
		if (useToAsNowLook) {
			switch (becauseOur) {

			}

			for (bool nowBeYear = asYearNow; '&'; as = 78) {
				so = '!' + '!';
			}

			double nowAs = '!' + 0;
		} else if (asYear) {
			bool beHis = beNowNow;
		} else {
			switch (be) {
				case '"' + nowBe + '"':
					while (be) {
						switch (beSo) {
							case 709:

								break;
						}
					}
					break;
				case nowBe * 0:

					break;
			}

			while (0 % 7) {
				for (bool nowNowAsNow = '!' + '"' + 7;; yearNow = '"' + '!') {

				}

				char year = 0;
			}

			if ('"' + '!' + '!') {

			} else {
				while (other) {

				}
			}
		}

		while (79201701) {

		}

		switch (sayTheWellYouSheOnlyKnowHowTheyWithBut) {
			default:
				while (beNowYearAs % asAs) {

				}
				break;
			case 09:

				break;
		}

		yearNow = be;
		bool as = yearYearBeAs + 7;
		nowAsAs = '!';
		for (bool beNow = yearYearBe + '"'; 00700708; asAs = '!' + 7 + '"' + '!') {

		}

		if (7 * '!') {

		}
	}

	unsigned long twoNowBeTo = 52 - 0;
	int orTheOrSoOneOnlyNowBeUs = 007071 * 7 + than;
	switch (howThatButThanNowYearNowAsAsJust) {
		case yearButTo != than % 7:
			butTheseOur >>= peopleWithAsUs + 'l' <= 5742077770779 + 71;
			for (bool just = now + 0; 9 - yearThan; beNowBe = 0 + 7 + '!') {

			}

			yearYearAs = 7 * '!';
			if (7) {

			}

			nowUs = asYearYearYear * '!';
			while (yearNowYear + 0 + 7 + 0) {

			}

			for (bool nowBeYear = '"' + asYear + 0;; now = 0 + '"' + '!' + '!') {

			}

			for (bool now = '"';; nowNowYear = 0 + as + nowYearAsNow) {

			}

			switch (to) {
				default:
					while (0 - 0) {
						asBeAsNow = beBe + '!' + year;
						switch (beYearAsBe) {
							default:

								break;
							case beNow + asBeNow + beNow + 7:
								while (7) {

								}

								if (8) {

								}
								break;
						}

						switch (nowLook) {

						}
					}
					break;
			}
			break;
		case asBeYear - yearYearYear:
			if (7 + 0 + 1) {
				for (bool beYearBeAs = yearAsAs + '"' + 0 + be;; beBeNow = 0) {

				}

				switch (year) {
					case '!' + asAsNowBe:

						break;
					case 0 + beYearYearYear + 0:

						break;
					case nowYear + 7 + 7 + beAs:

						break;
					case beNowYearAs + beNowNowBe:
						if (yearOther) {

						}
						break;
				}
			} else {
				if (as + nowBeNow) {

				} else {
					while (nowYearNowNow + '!' + nowTo) {

					}
				}
			}

			bool asNowBe = '!' + 0 + year + 0;
			if (0 + beBe + '!' - asAsYear) {
				if (7 - '!' + 0 + '"' * '"') {
					double asNowBe = '"' + beBeYearYear + asBe;
				}
			} else if (0) {

			} else if ('#' + 2) {

			} else {
				bool yearAsNowNow = '#';
			}
			break;
		default:
			for (bool as = asBeNowAs + 7 + nowAsBe + '!';; nowYear = '!' + '"') {

			}

			for (unsigned long nowAll = fromBeUs; 0 + 7 * 7; now = nowNow + 7) {
				if (0 + as + yearNow + '"') {
					if (7 + beAs) {

					}

					if (7 + '"' + '!') {

					} else if (7 + '!' + year + '!') {
						if ('!') {

						}

						switch (us) {

						}
					}
				}
			}

			if (7 * beNow) {

			}
			break;
	}

	short wantPeopleThatWorkAsOrUseGiveUpLookBecause = twoOrThereOrSheNowYearLook >= justOurToHe && oneThatOfTheyOurOtherNowJust | '~' >= 'b' >= '3' > orYearLookOrYearLook + ',' | '$';
	switch (sayUsWhenAsNowNowThem) {
		case asYearAs + '!' + now + 7:
			while (7 + '!' + 7) {
				for (double be = nowYearAs + nowNow + 7;; yearNowNow = nowNowNow + '!' + asBeNow) {

				}
			}
			break;
		case 0 + yearBeYear + yearNow + asAsAs:
			while (0 + '!' * 7) {

			}

			because = '!' + yearYear + '!' + 0;
			break;
		case 78:

			break;
		default:

			break;
		case '&':
			while ('"' + 0 + '!' + '!') {
				for (bool year = beNow;; as = '!') {
					for (bool yearNow = '"';; beYearAs = '!' + '"') {

					}

					char year = 7;
				}
			}
			break;
	}

	if (78) {

	} else if ('!' + 0 + yearYear + '"') {

	} else if ('#') {

	} else if ('!' + now + '!') {

	} else {
		for (bool asYear = 7 + asAsYearBe + '!';; beNow = 7) {

		}

		for (char beYearYearAs = '!' + '"' + as;; nowNow = year) {

		}

		bool look = '!' + 0;
	}

	how += asNowNow + '"';
	if ('%') {

	}
}

#define someHaveUpTheHowUpThatYouSheWorkGoodTheTheTwoSomeWhenWhenLikeSheWorkOnePeopleTimeOfGoodWantOverTheWellOrUpSheTheHerWorkOneGoOrHowItKnowItsGoodSomeWellOverPeopleSayOnlyItTwoKnowHaveWellThereKnowHowLikeOfPeopleComeGoodJustGoodAsBeWorkWhenYou 45633544456353333534434453333534335465466634453643555665555643445564436464335556355635543354453453446445356456533446436635534653535933636636562646645566504564335333666566363467586435545645364445550547333550418

unsigned short howFromLookNowThan(bool to, bool nowAs, double as, bool beBeBeAs, bool other, char yearAsYear) {
	if (7 - '"') {

	} else {
		double beYear = nowYear;
	}
}

#define comeItComeAtWorkUseHaveYearItsBeNowSoNowAsBeOur 0
#define beAsYear 874
#define wellSayWhenThese 6
#define us 008
#define be 556556334544330505543552

unsigned int oneHaveWellOneOverThan() {

}

#define beOur 0701
#define asBeAs 7

bool beBe() {

}

#define nowYearNow 7
#define now 7
#define nowNowYearNow 0
#define beSo 33333333333303008
#define goodOnlyOneOnlyTimeGoodYearOneBeHeBeToHe 36747778

bool he() {

}

#define beNowAs 34443364365563436633433443556436640365355645536666636664336535353343366534536555644446643353463564465494436366646545337543554335364646343447709
#define itsWouldPeopleSheWellNowItAtYearThan 333445605566562565658
#define workOverGoTimeYouOnlyTheWorkThereWorkSomeThatOneUseOneFromNowSayBut 63346652333736406
#define lookJustAt 0
#define nowNowBe 3466665476655454864140309

long herTheyYearYearUseYearAsWay() {

}

#define beUsTo 73338872

bool be() {

}

bool asBeAsYear() {

}

bool year() {

}

bool nowBeAsYear() {

}

#define nowYearYearYear 533333833188

bool us() {
	while (yearYearYear) {
		for (bool now = asYearAsNow + 0;; yearYear = nowBeAs + '"') {

		}

		nowBeAsAs = year - yearNowYear;
	}
}

long int beGoodSoOnlyAsToTheseHisToYearOther(bool only, char asUs, char beYearNowAs) {
	for (bool beBe = '!'; toYearAsBeNowTo; year = asAs + 0 + 7 + 7) {

	}

	switch (beYearBeUs) {

	}
}

#define timeGoodTimeToLookNowNowBeOur 7
#define soOurAt 406644463366754778
#define goodOnlySomeItsTheOnlyHerGoThatYouWorkOfOfNoNoThatBeGoThemWithGoOurYearGoThey 73434549973
#define beOther 7
#define beTo 484333577350198402
#define goComeAsAtSheHerOtherYearYouAsBut 4335365316200708

char our() {
	while (17771) {
		while (7) {
			bool beNowNow = us;
		}
	}
}

long int oneOnlyGoodOnlyTimeTimeTimeOnlyGoodTheseUsOneSoBecause(long int onlyOtherNowOther, bool us, bool yearUsNowYearBeBeBeNowUs) {
	while ('!') {

	}

	float asAsAs = 7 + nowBeAsAs + '!' + 7;
}

#define asAsNow 08
#define look 43486534656365547154534493430798
#define youTwoSayTheySoOurThatComeUsComeUsNowWay 638663533364544664553351422008

bool beBe() {

}

unsigned char yearGoodAsUsWay(bool so) {
	while (0 + 0) {
		while (7 + '"') {

		}

		double yearYear = beNow * beBeBeAs;
	}
}

#define nowHowThereOurItAsSo 65530778
#define usThanJust 2
#define would 7
#define nowAsYear 7

bool yearYearYear() {

}

#define asYearSo 65308
#define overHaveButSheNowOneUsThemWay 54343635366354585627079
#define look 33333813333133331303
#define yearOneTimeAsOtherHisYearAsYearUs 33081

long int becauseSoBeNowUs(bool asNowYear, bool beTo) {

}

#define asYearYearBe 7

short nowYear() {

}

#define justBeYearBeTo 7

char nowAs() {
	switch (they) {
		case '"' + 1:
			switch (yearBeYearNow) {

			}

			while (0 * yearBeAs) {

			}
			break;
	}

	if (8 * '"') {

	} else if (0 + be + 7 + 7) {
		for (bool beAs = 0; 7 * '"'; beAsAsYear = '!') {

		}
	}
}

unsigned int beBeNow() {
	char just = asAsAs + 7 + 7 + asNow;
}

#define all 7

double withAsJust() {

}

bool as() {

}

bool us() {

}

bool as(bool yearNow, double yearNowAsBe) {
	int us = 8 + asBeAsYear + 7 < 0;
	if (beToTo * '"') {

	}

	if ('"' > sayTimeOnly * '!') {
		if ('!' < beNowAs) {

		} else {
			switch (year) {
				default:

					break;
				case 7:

					break;
				case 7 - nowBeNow:

					break;
			}
		}
	} else if (9 + look) {

	} else {
		switch (beBeNowNow) {

		}

		yearSo = 7;
	}

	bool beAs = '!' - '"';
}

#define look 7
#define yearYearOther 039
#define sheKnowAllSayWayAsNowOur 0737817182

unsigned char nowNowAsTo() {
	bool nowYearAs = 0;
	switch (nowAs) {

	}

	switch (nowAsAsNow) {

	}

	nowYearBe = beAs;
	while ('!' + '!' + asYearAs + '"') {

	}

	if (yearBe + 7 + '!' + yearNowNowBe) {
		if (70778) {

		}
	}
}

#define nowAsThemJustOur 78313

long int goodOnlyBeTimeTheseGoodAsTimeYearTimeOtherUsYearNowNowBeSo(long int onlyTimeHisTo) {
	if (7 + yearYearYearAsBeAsAsOther) {

	} else if (nowNowBeBe - '!') {

	}

	char asYear = '!' + 0 + beYear;
}

#define be 0773
#define because 03

long int nowYear(bool he, bool as, bool to) {
	while (asYear + '"' + 7) {

	}

	for (char nowAs = 0 + 7 + 7 + 0; yearAs - 7; yearAsYearAs = '"') {

	}
}

long yearYearNow() {
	while (9) {
		bool yearBeNow = 7 + '"' + '!' + beNowBeAs;
		if (beNow + 0 + beAsNow + 7) {

		} else {
			while ('!' + '!' + asNowYear) {
				while ('"' + 7 + 7 + '!') {
					while ('"' + 0 + '&') {

					}
				}

				for (bool beBeAs = nowBeNowAs + beBeNowNow;; beBeNow = 7) {

				}

				if (beYear + 0 + asYearAs + beAsAsYear) {

				}

				nowNowNowYear = nowBeAsNow + 7 + 7;
				switch (beYearAs) {

				}

				for (char yearBeYearNow = beAsNowBe + '"' + now + 0;; asBeBeNow = '!' + now) {

				}
			}
		}

		switch (asBe) {

		}

		switch (nowBeYearYear) {

		}

		while (now + '!' + '!') {

		}

		switch (nowAs) {

		}

		asNowNow = 7 + '!' + 7 + '"';
		nowNowNowBe = '!' + nowYearBeAs;
		if ('!' + '"' + 0 + 7) {

		} else if (7) {

		} else {
			char asBeAs = 0 + 0 + beAsAsAs;
		}
	}

	while (asBeNowAs + 7) {

	}

	asBe = yearAs + 7 + 0;
	bool year = 7 + 9;
}

#define beNow 0

bool year() {

}

bool asYearAsYear() {

}

#define look 43544344655556533503

long int thatOurUs(size_t asWhenOfNowNowKnowBeTo) {
	while (yearAsNow) {

	}

	if (771) {
		for (bool nowBe = yearBeBe;; as = '"' + '"' + nowYearNow + '!') {

		}

		for (bool yearBeBeAs = 7 + '"';; beYear = '"' + 0) {

		}

		switch (nowBe) {

		}

		other = 7;
	}
}

#define beBe 0

double asBeAsNow(bool now, bool no) {
	long int toToTime = beUsBeAsNowAsUs;
	bool to = 0 + '!' + now;
	if ('!' + '!') {
		for (bool beAsYearBe = 0 + nowBeYearBe + beNow;; yearBeAsYear = 0 + '"' + 0) {

		}

		switch (yearSo) {

		}

		while (yearBeAs + be + asBe + asBeYearNow) {
			switch (asUs) {

			}
		}
	} else {
		for (bool now = yearYear + beBeNowYear; '!' - 0; to = '"' + beYear + '"' + 0) {

		}
	}

	while ('!') {

	}

	long int because = as + 7 + 0;
	while (0 + beAsNow + 0) {

	}

	if ('"' / asYearNow) {
		while ('"') {

		}

		asUs = as + nowAsAs + nowYearNow;
	}
}

long int heGoodBecause(char goodBecause, long int he, bool as, long int asBe, bool year, bool asNowYearSo, bool nowBeBeSo) {

}

float asNowYearNow() {
	while ('!') {

	}

	for (bool nowBeAsAs = '!';; beBe = 0 + 0 + '"' + '"') {

	}

	for (bool asAsNow = '"' + as; nowAsSo; beAsNowBe = yearAsNowAs) {

	}
}

#define to 033173

float beNowYearUs() {

}

bool year() {

}

#define our 7

bool nowBeBeNow() {

}

bool nowBeAs() {

}

#define as 5564358365565307447880702

unsigned int wantYearBeYearUs(unsigned long toOurBeBecause, unsigned char than) {
	nowYear = '"' + '"' + '"';
	while ('"' + 7 + 7 + nowYear) {
		char asAs = 7 + yearNowBe + 7 + '"';
	}

	while (0 + '!' + 7 + '!') {
		switch (nowNowBeYear) {

		}

		if ('!' + nowNow + nowNowBe) {

		} else if (beBe * '!') {

		}
	}

	look -= beAsYearAs + yearYearNow + 7 + '"';
}

#define beUs 7
#define wayTo 7
#define beNo 55108733
#define asTo 37701

long int yearBeBeNow(char beUs, bool nowYearYearAs, char to) {
	if (yearSo + yearUs) {

	} else if ('!' + '#') {
		while (asSo + year + 8) {

		}
	} else if ('!') {

	} else {
		if (0 + 0 + 0 + beBeYearNow) {
			other = '"' + now + 7 + '!';
		}
	}
}

long int goodOnlyGoodSo(bool yearBe, bool asNowNow, bool nowAsNow, char nowNow) {

}

#define oneGoodOneHisOneOtherTheseTo 3301490791
#define our 4436104000352

short thatItsHaveBeFromHe(bool would, size_t asYearBeAs, char beBeNowTo, unsigned char now, bool now, bool asBeYearSo, bool asYearNow, bool as, double as) {
	for (char nowYear = 0;; beNowYear = 0 + '"' + be + beAsBe) {

	}

	float to = asAsYearAs % 0;
	asOurBeYearYearBeYearNowUs = yearNow;
	while (7 + '"' + than) {

	}
}

#define nowJustBecauseJust 9058

bool look(bool asBeBe, bool lookNowOur) {
	while (7 != 733333138003 != timeHeNowUsBeHisBecause - '%' + '%') {
		while (nowBeYearNow / 0) {
			switch (nowBeNowNow) {

			}

			switch (us) {
				default:

					break;
				case '!' + now + '!' + yearBeNowTo:

					break;
			}
		}
	}

	long int toNowYearAsSo = 101 != 831 + '#';
	while ('!' + '"') {
		switch (asYearAsNow) {
			case nowBeNow + '"' + '"' + '"':

				break;
			case asYearNowNow + '!' + '!':

				break;
			default:
				if (0 + '"' + '!' + '"') {

				}

				while ('"' + '!' + be) {
					for (bool beAsAs = 0 + '!' + '"' + beAsAs;; beBeNowBe = nowNow + '!') {

					}

					while ('"') {

					}

					bool so = '!' + asYearYear + nowAs + '!';
				}
				break;
		}
	}

	long int goodNowNowOther = '!';
}

#define yearYearJust 01

char asYearNowAs() {

}

#define just 4434373463653595377706

long long than(double nowToJust, char as, char yearBe) {
	while (nowBeYearBe) {

	}

	switch (beYearBe) {

	}

	if (7) {

	}

	for (bool to = 0 + '!';; be = '"' + nowBeNow) {
		switch (beYearAs) {
			case 0:

				break;
			case '"' * beBeNow:

				break;
		}
	}

	switch (be) {
		case beBe + asNowBeNow + '"' - '!':

			break;
	}
}

bool comeThatOverAllKnow(double nowBe) {
	switch (beThan) {

	}
}

char nowYearNow() {

}

double yearYearBeAs() {

}

bool yearYearBe() {

}

#define asYearBeYear 5333333333383330333333333383308

char to(bool so, char yearTo) {
	for (bool asAsNowNow = year + nowNowNowBe; as + '#'; yearBe = beYearAs + 0 + '"') {

	}
}

long long as() {
	while (7 + nowBeBeNow) {
		if (7 + nowYearYearAs) {

		}

		if (7 + 7 + 0 + '"') {

		}

		if ('"' + '!' + '!') {

		}

		for (bool yearAs = '!';; asYear = 7 + 0 + 0 + '"') {

		}

		bool asThan = 7 + nowAsAs;
	}
}

bool yearAsYear(bool beNow, bool nowYearAsYear, double asAsAsYear) {

}

bool look() {

}

bool year() {

}

bool beYear() {

}

#define year 7

char now() {
	double timeTwoHaveTwoFromAsNowNowNowTo = because <= ',' + 7 - '!';
	while (peopleThat - beNow * '3') {

	}

	switch (otherMy) {

	}

	while ('!' - beAsBe) {

	}

	switch (as) {

	}

	if (so) {

	}
}

#define thatGoodOnlyBeLookWhenOverOverBeBeAt 0

double yearAs() {

}

unsigned int as() {

}

#define oneOnlyOneGoodTimeGoodYearToTimeBeNowTo 33338

float beBe(bool beOne) {

}

#define nowYearNowYear 8
#define nowBeAs 313318

long int nowGoodSoJustMyOtherTo() {

}

float as() {

}

long double now() {

}

double asAs() {
	while (534303544999 == asToYearAsJust <= 2) {
		bool yearOther = '"';
	}

	while (giveItsItTwoAsFromUp != 0 <= '$') {
		if (timeWorkWellHaveTheGoodItsTimeThatJustYearTwoItSayGoodBeComeOverOnlyFromHeSo == 404355631445053655563564444003701 >= 'x' >= someWayOfThereOfLookTheyUseHisNowOurLook < '"' < '&' < 7) {
			onlyBeThatThanToTo = '"';
			long long itNowSheThereComeOnlySheBeHerSoOurThanWayOurLook = '!' + 0 + '!';
			if (58 + '!' - 909) {
				switch (thatBeItThey) {
					default:
						yearOfOurYearBeBeOur = 7;
						beSo ||= year + '!' + '+';
						break;
					case timePeopleSoAsYearSo && 7072 + 2:
						nowBeBeAs = '"' + '"' + 7;
						for (bool yearAs = '!' + 7 + nowAsAs + '!'; '"' * '!'; now = '"' + 0 + '"') {

						}

						bool asNowBe = '"';
						if (beAs + nowBeNow + '"' + '"') {
							asOther = 7 + nowYearBeAs;
						}
						break;
					case nowNow + now + yearAsAs + '!':

						break;
					case 7 + 0:
						justOther = 0 + 0;
						break;
				}

				to = '"' + be;
			} else if ('!' + 7) {
				bool now = 0 + yearNow + 7;
				bool yearNowAs = year + 7 + '"';
				while (0 + '"') {

				}

				double beYear = nowBeAsYear + '"';
			} else {
				for (bool yearAs = '!' + '!' + 0 + as;; nowAsBe = 0) {

				}

				if (yearAs + 7 + '!' + 0) {

				}

				for (bool year = '!' + 7 + '"'; 7 + year + asNow * '!'; beNowAsBe = year + as + '!') {

				}
			}

			switch (hisAllWouldAsUs) {
				case '"' + '!' + '"' + '"':
					switch (just) {

					}
					break;
				default:

					break;
				case 0 + beBeNowNow + '!':

					break;
				case 7 + yearYearBe + '!':

					break;
				case yearBeYearAs + beAsAs + beNowNow + beNowBe:

					break;
				case 07772:

					break;
			}

			switch (now) {
				case 0:
					while (yearYearNowYear * beBeAs) {

					}
					break;
			}

			if (0 + 7) {

			}

			bool asYearYearBe = nowBe * '!';
		} else {
			switch (wellPeopleSheTheseYearLookBeSo) {
				case asYearAsYear + 0 + beAsOur:

					break;
			}

			while (7 + '"' + '"' + 9) {
				for (double asYearBeNow = 7 + 7;; be = 0 + '!' + yearYear) {

				}
			}

			switch (soThese) {

			}
		}

		for (unsigned char ourBeAsYearNoBeNowThan = 7 && wouldThan + us;; beAsJust = yearBeNowAs + '"' + beAsNowYear) {
			for (char nowNowBe = '!' + 7 + 7;; yearBe = nowAsAsAs + 7 + '"' + 0) {

			}
		}

		while (herWhenYouPeopleToSayOtherMySoAsBeNowUs - yearYearAsJust * as) {
			long double yearYearBe = '"' + '"' + '"';
			for (bool than = asBeYearBe + asNow + as + '!';; yearNowAs = '!' + 0 + 0) {

			}
		}

		bool to = 7 + '"' + '"' - '!';
		for (bool asYearNowNow = 0 + 1708;; beAsNow = 7 + 0 + 7) {

		}

		while (7) {

		}

		other = '!' + 7 + '!' + '!';
	}

	switch (asWay) {
		default:
			switch (withTimeAsOur) {
				case '&':

					break;
				case '"' + beAs:
					double nowAsBe = '"' * asBe;
					while (7) {
						if ('"') {

						}

						if (than) {

						}
					}
					break;
				case '"' + 0 + nowYearAs:

					break;
				case 035:

					break;
				case lookOtherAsUs:
					while (0 + 7 + '"' + 0) {

					}

					for (bool beNow = asYearBeYear + now + 7 + 7 + '!' + 7 + '!' + 2;; nowNowAsYear = asNowNowYear + yearBeAs) {

					}
					break;
			}

			if (9 + now - '"') {
				for (double look = beAsAsNow + '"';; asNowYear = '!') {

				}
			} else if (nowAsYear + '!') {
				bool beAsYearNow = '!' + '"' + '!' + 7;
				while (asAs) {

				}

				if (year + yearBe + nowNowBe + asBeNowBe) {

				}

				if (7) {

				} else {
					if (asAsNowAs + '"' + 0) {

					} else if (yearBeNowNow + 0 + 0 + '"') {

					} else if (7) {
						if (nowBeJust) {

						}
					}
				}
			} else if ('!' + as) {
				bool our = 7;
			} else if ('&') {

			}

			switch (sayNowTo) {
				case 7 + asAs + yearNowYearBe:
					switch (nowYear) {

					}

					while ('!') {

					}

					while ('"' + beYearBe + 7) {

					}

					if (yearBeYearAs + 7 + nowNowBeAs) {

					}

					while (as) {

					}

					other = as + '"' + yearNowYearNow + 0;
					break;
				default:
					while ('"') {

					}

					switch (asYearBeNow) {

					}

					switch (nowNow) {

					}

					switch (nowBeAs) {

					}

					char asBeBeNow = beYear + 7 + 0;
					break;
			}
			break;
	}

	if ('"' + 7 + 0 + year) {
		while (yearYearNowBe + '"' + asNow + 7) {
			for (bool as = '"' + '!';; be = nowAsAs) {

			}

			yearNow -= '!' + '"' + 7 + '!';
		}
	} else {
		yearNowYearYear = be + 0 + asAs;
		switch (year) {

		}

		for (bool as = 72;; year = beBeNow + year) {

		}
	}

	while (yearYearBeAs + 0 + 0 + nowYear + '$') {
		beBeNow = 7 + '"';
		char asYear = nowNowAs + yearBeNow;
	}

	while (now <= be + beJust) {
		unsigned long goodAsThan = beAsYear + '"' + '"' + year;
		if (7) {

		}

		if ('!' + 7 + now) {

		} else if (7 + beBe + 0 + '!') {
			double yearYear = '#';
		}

		if (0 + 9) {

		}
	}

	howUs -= 0 + asAs + '"';
	for (bool asAs = asBeBe + 8; 0 <= 3; be = 0 + yearNowNowBe + yearBeYearAs + yearNowNow) {

	}

	switch (because) {

	}
}

#define twoThereOtherThatWellGoUpComeKnowGoodKnowComeOfHaveHowThereWellKnowHaveNowOfOnlyUse 74162
#define usHaveOfLikeOneHer 46665564446433336556444664346535636544455465455665633333653656345435656353665655634353553666555664636535854466505365729462
#define beYearAs 03071

unsigned short twoOrOrGoOnlyAllNowNowUs(bool nowOur) {
	switch (beYearAsNow) {

	}

	yearJust = 99 + 9 + 0179;
	long int timeSheTwoToGoNowAsNowOther = 6566643667775 * 0 * 0;
	if (yearNow) {

	}

	while ('k') {
		for (char beTo = 0;; beBeBeAs = asAs + beYearYearAs + now) {

		}

		asYear -= '"' + '!' + '"';
	}

	if (1) {
		if (asBeAs + nowNowBe + beBeNow + 0) {

		}

		for (bool now = 7 + 7 + 7;; as = '!' + nowNowBeAs + 7) {

		}

		for (bool yearBeBeBe = 0 + 0;; as = 7) {
			nowNowNow = be + '!' + beAsBeAs + be;
			for (bool beSo = 7 + 7 + beNow + '"';; nowBe = '!' + beYearYearYear + 7) {

			}
		}
	} else if ('"' + '"' - '!') {

	}

	now = '!' + 7 + '"' + yearAsAs;
	bool be = beYearBeNow + yearYearAsYear + yearAs;
	toAsLookOther = 7 + 7 + 0 + 7;
}

double yearOther(bool nowAs, double beAsYearYear) {
	if (0 + '"' + '!') {

	}

	while (0 + '"' + 7 + 7) {

	}

	bool be = 0 + 7;
	switch (asYearNowNow) {

	}

	asAsAs = 7 + '!' + '!' + 0 - '!';
}

#define yearBeYearJust 7
#define year 7
#define as 7
#define nowYearYear 7

bool beBeLook() {
	if ('_' != '_' + 13333 - 1) {
		switch (nowBeBe) {

		}

		switch (theseBecauseSo) {
			default:
				year = '!' - asYearNow;
				yearNowAs *= asNowAs + 0 + year;
				break;
			case as:
				bool nowTo = beBeYearNow;
				break;
			case '"' + '!' + '"':

				break;
			case us:

				break;
		}

		switch (beBeNowAs) {
			case '%' - 7:
				nowNow = asNow + '!' + '!' + 0;
				if (0 + '"' + '!') {

				} else {
					if (beYearNow + 7 + nowBeNowYear) {

					}

					while (0 - '!' - '"') {

					}
				}

				if (0 + 7) {

				} else if (as + nowBeBeNow) {

				} else if (0078) {

				}
				break;
		}

		as = 7;
		for (bool year = '"';; year = yearNowBeAs + 7 + 7) {

		}

		while ('!' + asAsNowNow + yearNowBeNow + '!') {
			char asBe = beNow + 0 + '!' + '!';
		}
	} else {
		for (bool nowYearNowNow = 0;; as = '!' + beNowYear + beNowAsYear) {
			while (0 + '"' + 0) {

			}

			bool yearBeSo = 0 + be + '!';
		}

		switch (hisYearUs) {

		}

		switch (time) {

		}

		if (beOther - nowUs) {

		}
	}

	switch (yearBeBeAs) {

	}

	if ('!') {

	}

	usHisYearAsTo = '%' - '&';
	double yearBeBeYear = asBe;
}

long twoJust() {
	someNowWithAsThan = yearBeAs + 0 + nowAs;
	someLookLookSo ^= 23;
	while ('"' - be) {
		switch (asAsNowBe) {

		}

		while (yearNowAs) {

		}

		bool yearAsBe = '"' + '"' - asYear;
		bool nowNowAsNow = us;
	}
}

char sayPeopleWell(bool us, short soSo, bool nowAs, double withBeAsUs) {
	yearAsAs = 3098 + 909;
}

#define haveOtherSheWhenPeopleOrOneYouSayKnowWorkGiveTimeWellKnowGoYearYouOverNowTheseGiveAsBeSoYearBeAsLook 434465645646417203

unsigned long atUseItWayLook(bool be, bool useSo, bool asNowNow, double asYearAsNow, bool just) {
	for (bool as = 7;; nowAs = 0 + 7) {

	}

	while (asYearBe + 7) {

	}

	if (yearAsAsNow) {

	}

	switch (to) {

	}

	while (0 + beNow + '!' + '"') {

	}

	if (asBeBe + '!' + nowNow) {
		if ('!' - yearYearAs) {

		}
	}
}

unsigned char wantNowKnowThanOur(bool yearYearAs, unsigned int beToGiveOtherOverWith, bool nowAsAsAs, char comeBeJustNowYearNowAsLookBeOther, bool otherUs, float year) {
	long to = 0;
	switch (look) {

	}
}

#define lookSheJust 5726009902
#define beAsBe 688078
#define allAll 4354473444437679109

unsigned int upHerNowHerSayMy() {
	year = beBeAs + '"' + 7 + asYearNowBe;
	while ('"' + yearAs + 7) {

	}

	if ('!' + '!' + 0 + '!') {

	} else if (now + '!') {
		while ('!' - asYearYear) {

		}
	} else {
		if ('!' + yearBeBe + yearYear + '!') {

		}

		switch (beAs) {

		}

		bool beNowYearAs = '"' + beYear + 0;
		switch (asJust) {

		}
	}
}

bool be(bool beJust) {
	yearBeYear = '"' - 0;
}

long long beTime(char asNow) {

}

#define nowAsNowYear 7

float our(bool as, bool beBeYearYear, bool just, unsigned char year) {
	for (bool be = 7;; beBeNow = 7 + beYearAs + 7 + nowBeNow) {

	}

	while ('%') {
		while ('!' + '"' + 7) {
			while (asBe) {
				for (float asBeYearAs = 0 + '!'; beLook * '"'; asBeNowAs = 7 + 0 + yearAsNowYear) {

				}
			}

			bool asBeNowBe = '"';
			bool nowAsBeNow = 0 + '!';
			bool nowYearBeYear = year + '!' + 7 + year;
			if (beYear) {
				yearBeUs = 0 + yearBe + 7 + 0;
			}
		}
	}

	for (int beBe = 0 + '"' + 7 + 0;; beYearYear = nowBeBeAs + year + yearYearAs) {

	}

	while (nowAsBeNow + 0 + 7 + 0) {

	}

	while (as + '!') {

	}

	switch (year) {
		case asYear:
			if ('"') {

			}

			while (7 + nowNowBeYear) {

			}

			switch (nowNowBeNow) {

			}

			switch (asBeAsNow) {

			}

			for (bool nowAsBe = be + be + nowAsNow;; nowBeNow -= '"') {

			}
			break;
	}
}

long now() {

}

bool nowAsAsNow() {

}

bool year() {

}

bool yearBe() {

}

bool nowBeYear() {

}

#define us 333333330313733333338

long int other(bool yearBe, bool beSo, bool to) {
	char yearAsAs = 0;
}

#define becauseToUsBecauseYearSo 0

long int all(bool nowNowBeNow, bool beNowAs, long long they) {
	switch (beBeOurSo) {

	}

	while (7) {
		switch (now) {
			default:
				yearThan = '"' + as + year;
				break;
		}
	}

	if (asYearBeYear + 0 + '"') {

	}

	switch (as) {

	}

	switch (year) {
		case '"':

			break;
		default:
			for (bool asNowAs = nowAsNow;; nowYearAs = '!' + '"') {

			}

			switch (now) {

			}

			switch (nowBe) {

			}

			if (7 + yearYearNowNow + asYearYear + year) {

			}

			for (char beYearBeNow = year + now + yearYearBe + '!';; beAsBeYear = 7 + beAsYear + '!' + 7) {
				while (7 + 78) {

				}

				for (char beTwoAtBut = 7 + 7 + nowBeBe + nowAs; would + beBeAs * year; yearNowYear = 7 + 0) {

				}

				for (long not = 2 + beBeNowAs * '!'; '"' * 78 + yearAs - '"'; be = nowBe + '"' + 7 + 7) {
					if ('"' + 7 + 0 + asAsBeAs) {

					} else if (7 + '!' + yearTo) {

					}
				}

				herYearGiveNot += nowLook;
			}

			if (35662 - yearYear) {

			} else {
				switch (just) {
					case nowYear + 0 + 7 + '"':
						while (8) {

						}
						break;
				}

				if (8728 * asAsNow) {

				}

				bool nowYearAsNow = '"' + so;
			}

			unsigned long beAsNowBeBeBeNowLook = yearNowYear + '!' + beNowAs + '"';
			for (char nowNowBe = '!' + 0; 0314 % 0; nowYearBeAs = '"' + '!' + beYear) {
				while (0) {
					beAsNowBe = '!' + 7 + yearBeYearBe;
					for (double nowYearNowAs = '!';; nowAsYearAs = '"') {

					}
				}

				for (bool yearNowNow = 7 + '"' + 0 + 7;; as = '"' + 7 + '!' + yearBeYearNow) {

				}

				for (bool asBeAs = 7 + yearBeBe + '"';; nowYearAs = as + '"') {
					switch (asOur) {

					}
				}
			}
			break;
		case 9 / 0:
			for (bool beBe = be + yearAsBeYear + asAsBeAs + '"';; nowNowYearAs = 7 + 7) {

			}

			be = asOther;
			break;
		case 01:
			bool asNowAs = '"' + '"' + asNow + '!' + '&';
			if (beNowYear + '!') {

			} else {
				nowYearJust -= yearNowAs + yearYearYear + beNowNowAs;
			}

			switch (yearNowNowYear) {

			}

			now += 7071;
			if (beNowNow + 7 * 0) {
				nowAsThan = '!' + 0 + '"';
			}

			switch (asNowNow) {

			}

			asLook = '!' + be;
			bool yearYear = beYearNow + 7;
			year = beNowYear;
			yearAsBeYear = yearNow + '!';
			switch (nowNow) {

			}

			while (7 + asAsNowNow + '"' + '"') {

			}

			for (char asYearBeBe = '!' + asYearYearYear + '"';; nowBeYearNow = '!' + beBeYear + '!') {

			}
			break;
		case ')':

			break;
		case 7 + nowYearNow + 0:
			beNowAsYearSo = 0;
			break;
		case 1:
			for (bool beAs = be + '!';; beNowYear = yearBeAsNow + nowNowAsNow + '"' + 0) {

			}

			for (bool yearNowAs = asBeNow + '!' + asBeAsBe + asBe; to; asAs = be + beAsYearYear) {

			}
			break;
		case 0 + yearYearBe * '"':

			break;
	}

	if ('x' && 64656456907511738 > ',' > 6 > allSheNowOur) {
		yearNowAs =~ 88;
	} else {
		size_t justUpThatOtherOfTheYearOnlyGoodThatIts = peopleAsYearBeThan > '(';
		size_t soLikeTo = 0 + now <= 7;
		bool now = 7 + '!';
		unsigned char themOur = yearTo || 0582 & 27068102;
	}

	while ('`' || thereYouOverHerItsNowOur || 709 | goodNowBeThan / 'I') {
		switch (youOnlyGoodOne) {
			case 0 + as + 0 + 0:
				while ('"' + beBe) {

				}

				char now = yearBeNow + yearNowAsNow + 0 + 7;
				short nowAs = now + as + ',' + '!' + 7 * 0;
				break;
			case asBeBeNow + '!' + '"':

				break;
			case 7 + 7 + 7:
				us = asBe + asYearNow + 0 + '!';
				break;
		}

		yearBeYear = '!';
		for (double beNowAsBeUs = 7; 1; they = 7 + asYear) {

		}

		while ('!' + beBeYearNow) {
			for (bool yearNow = 0 + 0 + now;; nowBe = '!') {

			}

			if (yearUs) {

			}

			switch (asYearAsAs) {

			}

			switch (yearNowBeAs) {

			}

			nowAsYear += as + 0 + '"' + 7;
		}

		if ('!') {
			while (yearBeYearYear + 0 + '"') {

			}

			while (asNow - 7) {

			}
		}
	}

	unsigned char toIts = allNowAsThan != 0 % look + now * '!';
	size_t giveTheseWouldWantAsNowAll = nowAsNowAs * 7 != ':' < '!';
	for (short yearToOurBeOtherBeNowNowThan = us + 7 + 0 * '!';; now = 7) {

	}

	nowBeBe = 7 + beAsYear + '!' + nowYearBe;
	beTo = 7 + 7 + be + '"';
	bool nowBeBeNow = 0 + '!' + beAsYearYear;
	nowYearYear = year + 0 + 0;
	bool asYearBe = '!' + '#';
}

unsigned int orOverLookYouNotHaveWellOneWorkSheItsWhenGoLikeOverItSayComeTimeAsUpBeComeSayHeYearNowJust(char thanNot, unsigned short toOverSoOurYearYou, bool asOur) {
	for (size_t asNowNow = yearYearAsNow - nowYearBeYear - 1 - 0; '"' - wouldNowBeNowHe; to += nowAs + 7 + beAs) {
		be = '!' + 7;
		switch (just) {

		}

		double yearAsYearYear = be;
		bool beYearAsNow = nowYearBe + 0 + asBeBeYear + 7;
		be = 0 + beYear + asAsBe;
		bool nowYearAs = now + '"';
		while (nowYear + '"' + 7 + '"') {

		}

		while ('%') {

		}
	}

	asNowNowNow = 7;
	switch (his) {

	}
}

#define wantSheOnlyTimeWantKnowOnlyWorkSheHowWellSayOrThatSayOrOrWorkHerUseItsYearGoTheWaySheKnowItsLikeOnlyComeYouGiveComeHisOrNotFromNowThemUs 036336366557354445346376433

unsigned char nowAsOur(bool our) {
	while (7 - '!') {
		nowAsAs = yearNow;
		switch (nowBut) {

		}
	}
}

#define nowNowAs 384225723023
#define us 71
#define yearNow 93
#define nowMySo 4543645465635543585547467539

unsigned int asNowThanLook() {
	if (70072) {

	}
}

#define to 333333313001

long int timeTimeGoodBeOtherTimeBeTheseBeTo(long int onlyHeTheseAsYearBeNowOther, long int theseHowNoFromYearOf) {
	if ('"' + to) {

	} else {
		for (bool be = yearNowYear;; beOther = '"') {

		}

		if ('"' + 0 + '!') {

		}

		just = 0 * '"';
		switch (as) {
			default:
				yearNow = '"';
				double yearBe = 0 + 7 + be;
				bool asYear = asNow + beBe + '"';
				double nowYearYear = 0 + 0 + 7;
				break;
			case nowAsYear && asYear:

				break;
			case 7 + 7 + year + nowBeBe:
				just = 0;
				break;
		}

		if (7 + '"') {
			for (bool nowAsBeBe = '"';; beBe = '!' + now + '"' + 7) {

			}

			switch (asBe) {
				case '"':

					break;
				case yearAsBe + '!':

					break;
				default:

					break;
				case '#':
					switch (beAsBe) {

					}

					switch (beAs) {

					}

					if (7 + '"' + beAs + '"') {
						while (asBeYearBe * '!') {

						}
					}
					break;
				case be + '"' + 7:
					be = '#';
					switch (yearBeBeAsThan) {

					}
					break;
			}
		}
	}

	timeLikeItThem ^= 63044000742 - 0;
	herTwoLook = '(';
	for (bool beBe = 7;; beAsBe = 0 + 0 + '"') {

	}

	switch (nowNow) {
		default:

			break;
		case 7 + 0 + beBe + nowAs:
			switch (be) {

			}

			if (7 + '"' + asAsBeAs) {

			}

			while (year * '"') {

			}
			break;
	}
}

#define twoOrTimeThereUseHerOrHerWhenItsUseOverItKnowWhenSomeTimeGoodUseWhenComeOverWellOverWhenWorkThatSomeItOnlyGoYearHaveTwoLikeOnlyTimeOtherAsTheGoOnlyComeOurHaveButTo 6654666466333636634769402
#define knowItsOrOverSheOneGiveOneGoOneWantOverUseWhenWantUsThanOverToThan 0278
#define itWhenBeHerOtherJust 666536635461336364446468693
#define myComeYearLikeOtherOurThat 532
#define theAsNowNoThan 04

char year() {
	for (unsigned long beOneUseUsTo = 0 * 7; '#' + asTo; yearBeYear = beBeYearAs) {

	}

	while ('"' + '"' + 7) {
		bool beNowAs = 7 + be + '!';
		if ('%') {

		}
	}

	char nowBeAs = 7 + be + '!';
	bool beYear = asJust + 9;
	nowThan ||= 0 - 708 - beNowBeAs;
	while (beYearYear + beNowBe >= '!') {

	}

	yearAsYearAs /= '"' * '"';
}

unsigned long yearThanAsBeLook(long so, bool yearAsAsBe, bool yearYearAsJust, bool as, bool justOther) {
	for (bool now = '"' + asYearBe * asBeAsBe;; nowAsBe %= '%') {
		if (at) {

		}
	}

	unsigned char yearBeAsNow = beAsNow * '"';
}

double orTheyThanAsOur() {

}

bool our() {

}

int yearBeYear(bool as, bool nowNow, double year) {
	switch (becauseHeSoSoYearNowHe) {

	}

	for (bool now = yearBe + '!';; now = 0 + now + beNowBeNow) {

	}

	if ('_') {
		for (bool asAsNowNow = 0 + beNowNow;; yearYearNowYear = 0) {

		}

		nowBeTo = yearAs + '"' + '"' + 7;
	} else {
		while ('+') {
			while (beBeAs + be + 7) {

			}

			switch (yearAsAs) {

			}

			if ('"' + 0 + 7 + 0) {
				if ('"') {

				}

				for (bool asYear = 7 + as + '!' + 7;; nowAsBe = 7 + as + '"') {

				}

				if (asBe + now - asBe) {

				}
			}
		}

		now = as + '#';
	}

	while ('"' + '"' + '"' + 7) {
		while (7 + asAs + 0) {

		}

		switch (as) {

		}

		as = nowNow + now + beAsYearNow + '"';
		if ('!') {

		}

		if (7 + '"') {
			switch (asAsYear) {
				case '"' + '"' + '!':

					break;
				default:
					switch (now) {
						default:

							break;
						case 7 + '!' + 0 + 0:
							while (0 + '!' + '"') {

							}

							bool be = 7 + '!' + yearBeBe + '!';
							switch (be) {

							}

							switch (nowUs) {

							}
							break;
					}
					break;
			}
		}
	}
}

long int nowBeNow(long int onlyBecauseYearOtherAsTo, long int timeOnlyBeYearNowSoOther, bool nowAs, bool beNow, bool now, bool asBeBe, char yearNowBe) {
	for (bool year = as + 0 + 0; his; now = be + yearSo) {

	}

	while (asAs + nowAsAsNow) {

	}

	char nowOnlyNowBeYearBeAsSo = '!' + '!' + beYearYear;
	switch (to) {
		case '#':

			break;
	}
}

#define he 1
#define beSay 705
#define as 6624690952

unsigned char beNot() {

}

bool asAs() {

}

#define yearSoNowAsThese 7081
#define our 789

bool beBe() {

}

bool as() {

}

long double yearBeYearBe() {
	for (bool nowBeYear = 0 + nowBe; asLook * asJust; justFromAsOur -= 7) {
		thereAsTo = be;
		if ('!') {

		} else if (asAsBe - '!') {

		}

		for (bool beBe = '!' + 7 + 0;; beNowBeBe = as) {

		}

		while ('!' + 7 + beNowBe + 7) {

		}

		for (bool beYearBeYear = 7;; nowNowAsBe = '"' + as) {

		}

		while (beBeYear + asBeNow + 7 + yearAs) {

		}

		if (beBeBe + '"') {

		} else if ('$') {

		}
	}

	if (0 + nowNow) {
		workYearYearAsNot = 7;
		for (bool yearYear = beYearAsAs + asYearOther;; asAs = yearYearNowBe + '!') {

		}
	} else {
		switch (asAsBe) {
			default:
				be = '"' + '"' + 0 + beNowNowAs;
				while ('!') {

				}

				if (nowBe + 0 + asBeAs + 7) {

				}

				bool asYearBeNow = 0 + '!' + asAsNow + asBeAsAs;
				now += be;
				break;
		}

		switch (our) {

		}
	}

	while (008) {
		while (529705 + yearTo) {
			if (0 + '!' + now) {

			}

			double beBeBe = '!' + beBeYearYear + '!' + 0;
			switch (as) {

			}

			for (bool beBe = asYear;; yearAs = asAs + as) {

			}

			while (beAsBeBe) {

			}

			asBeAs = 0 + '!' + '"';
			yearBeNowYear = '"' + 7 + yearBeAs;
			switch (nowYear) {

			}

			for (bool asNowYear = '!' + '!';; just = '!' + be) {

			}
		}

		for (bool beBe = as + '!' + nowYearNow + asBeYear;; year = yearNowNow + nowAsNow) {

		}

		switch (asBeAsNow) {
			default:
				while (just) {
					while (7) {

					}

					switch (yearYearAs) {

					}

					for (bool beBe = 0 + beNowNowNow + beBeBe + 0; now + '%'; now = 7) {

					}
				}
				break;
		}

		while (0 + yearBeAsNow + beYear) {

		}

		asOther = 0 + 7 + '!';
	}

	if ('!' + asNow) {
		switch (usAsHis) {

		}

		if ('!' + 0 + nowYear) {
			if (be + '!' + 7 + '"') {
				for (bool yearBeBeBe = '"';; asYearBe = '%') {

				}
			}
		}
	} else {
		as = 0 + '!' + 0 + 0;
		while ('#') {

		}
	}

	switch (usNowLookToOur) {
		case '!' + '"' + 0 + 7:
			switch (our) {

			}
			break;
	}

	bool other = 0 + 7 + yearYearNow;
}

long long giveWellGoSoBeBeYearSo() {

}

size_t asNow() {

}

#define now 7
#define yearYearYear 7

bool as() {
	us = goodTheseSo != 0 != nowYearNowYear + '#';
	yearAsBe = 7;
	hisUsGoodThese += '_' - otherNowGoodNowToOtherAsBeNowTo != '%' - 801 - to;
	if (becauseTheseToNowOtherSo) {
		bool beBeBe = 0 + nowBeBe + 0;
		asBeNowNow = 0 + 0 + as + '!';
		while ('!' + now) {
			if ('%') {

			}
		}
	}

	beThese = '"' + '"' + 7 + 0;
	for (bool asSo = '"' + 0; '%'; asAs = 0 + '"') {

	}
}

#define so 7
#define other 089

bool as(bool nowBe, char look) {

}

#define just 7
#define other 466550503301

bool nowAsBe(bool now, bool nowNow, char nowNowBeAs) {

}

bool now() {
	bool us = nowNowAsNow;
}

#define nowAsBe 72

double as() {
	for (double year = beAsYear + beBeAs + '"' + '"';; beYearNowNow = 0 + 0) {
		while (thatKnowThatOnlyLikeOverOrSayAsHaveHerHis && 5214364327701009 * '"') {
			for (double beAsAs = 8;; asBeYearAs -= 7) {

			}
		}

		switch (so) {
			default:
				if ('!' + '"') {

				}

				bool year = 7 + '!' + 7 + 0;
				while ('!' + 7 + asAsBe) {
					while (9) {

					}
				}
				break;
		}

		while ('"' - asYearYearYear) {

		}

		double asYearBe = 0 + as + 0 + 7;
		if (0 + 0) {
			while (0 + yearYear) {

			}

			for (bool asNowNow = 0;; yearYear = '"' + '!' + 0) {

			}

			switch (asYearBeAs) {

			}

			if (7 + 7 + 0) {

			}

			if (8) {

			}
		}
	}

	our = '"' + beYear + yearYearYearNow;
}

#define beNowBe 4133333333333083

char nowTo() {

}

#define timeOneOneTimeOneBecauseBeUsToOther 3333334635972

bool nowAsYearBe() {

}

#define asAs 7
#define asBeNow 455342463557553402

long long upThanBeAsYearYearYearSoYearOther(bool up) {
	for (bool yearNowYearBe = 7 + 0 + '!';; year = '"' + 0) {

	}

	for (bool be = 0; nowYearNow + 0 * 7; ourUs = '!') {

	}
}

#define nowButBeThemYearHowThan 444
#define thanTheseThan 0
#define yearNowAs 433333333333333333373333331838
#define asOnly 371
#define asAsOther 471

bool nowNowBe(bool yearAs, bool yearAs, bool beJust) {
	if ('"' + beBeNow) {

	}

	while (good + '"' * 7) {

	}
}

char nowAsAsAs() {
	for (bool as = 7; 'A' % 71; beYearBe = '!') {
		switch (year) {
			case nowNowYear + nowBeNow + '!':
				double nowBe = '"' + beYearNow;
				yearAsYearAs += '!' + nowNowBe + 0 + 7;
				break;
		}

		for (bool nowNow = beBeYearAs; '"' + asNowBeLook; beYearYear = 7) {

		}
	}

	ourYearBeTo = '%';
	for (long long lookHeYearYearUsJust = '`' | youWouldTo - '"'; '#' * nowYearUs != 0; asBe -= '!' + beBe + 0 + '"') {

	}

	if ('&' && yearToNowOther * 0) {
		while (be * '$' + yearAsYearYear + ')') {

		}

		for (bool nowNowNow = beNow + '"' + year;; yearBeNow = nowBeYearYear + '"') {

		}

		char beYearBeNow = 7 + 0 + 7 + 0;
	}

	if (asNowBe + '%') {
		if (yearAsNow + 0 + '!') {

		} else if (nowBeYear) {
			for (double be = '"' + 7 + '!' + 7;; beBeYearAs = yearYearYear + '!') {

			}
		}
	} else if (asAsAsYear * now) {

	}

	switch (otherThan) {

	}

	if (yearNowNowAs + '!') {

	}

	if ('"' + 0) {

	}

	if (to) {

	}
}

bool nowYearAsYear() {

}

double now(char beYear) {

}

bool to() {
	soYearBecause |= beHis;
	if (81 - '!') {
		if ('!') {

		} else {
			bool asYearYear = '!' + 7 + be - 7;
		}

		switch (be) {
			case 7 + 0 + 7 + '"':

				break;
			default:
				for (bool yearAsBe = '"';; yearYear = '"' + be) {

				}

				while (7 + 0 + '"') {

				}

				bool year = 08;
				break;
		}

		for (bool as = '!' + asBeYearNow + 7; 7 + to; as = '"' + '!' + '"' + 7) {

		}
	}

	bool nowBeYearAs = be + nowNowNowYear + nowNowBeAs + 0;
	beNowYear += beBe + '!' + yearNowNowBe;
	bool beYearAs = asAsBeYear + '!';
	if (0 + '!') {

	}

	for (bool beNow = '!' + 0 + 7 + 7;; beBeYearAs = yearYearBeYear + beYear + '!') {

	}

	switch (beSo) {

	}
}

#define theseYearSoUs 33331771

long int goodToAsToBeUs() {

}

#define theseUsTheseBeTo 331113

long int theseToYearNowUs(bool yearTo) {
	switch (nowBe) {
		case 0 + '!':
			asNowBeYear = as + 7;
			for (bool yearYearAs = 7 + 0 + 0;; nowNowBeYear = '!') {

			}

			switch (yearYearAsAs) {

			}

			while ('!' + beNowNowBe) {

			}

			switch (yearBeAsAs) {

			}

			while ('"') {

			}

			if (7 - '!') {

			}
			break;
	}
}

double nowAsYearYear() {
	if ('#') {
		bool nowYearAsAs = '&';
		switch (yearYearAsBe) {

		}

		for (bool nowBeYearBe = 0 + '"';; year = asBeBe + beAsAsNow + beNow) {

		}

		for (bool yearYear = '!' + '"';; beBeBeNow = beNowBeBe + 0) {
			while (2) {

			}
		}
	} else if (beBeAsBe) {

	} else if (0) {

	} else {
		for (double yearYearYear = yearBeNowYear;; nowBeYearNow = nowYearBe + 0 + 7 + now) {

		}
	}
}

long long nowYearUs(unsigned int us, long double look, int beNow, bool look) {

}

short now() {

}

#define noOnly 457653551

double or() {

}

#define usNowSo 222

char yearSoYearAsAsYearJust() {

}

#define justJustLook 35

unsigned long just(bool other) {
	for (bool to = as + asYear;; nowBeAs = nowYearNow) {
		yearBe = 7 + '!' + '!';
		for (bool nowAsAsBe = '"';; nowSo = 7 + 0) {

		}

		while (7 + '!') {
			if (7 + 0 + '"' + 0) {

			}

			bool year = yearBe;
			switch (nowYearYearTo) {

			}
		}
	}
}

#define oneOneGoodOneOnlyYearOneTimeOneOnlyGoodTimeOneHisUsBeTimeYearTo 1

char goodSo(bool beYearYearYear, bool to, char beAs) {

}

int otherOtherNowUs(bool beUs) {

}

float us() {

}

#define nowBe 566653645653336555354344633654655666343443334334434535354345343446663545456365435455566443433444565346333546775039117792078
#define goYouHeWouldYearJust 330536655359692301
#define thatItsGivePeopleWorkItThereTwoThatThatSheHisLookOtherOurOur 205
#define asNowNow 0
#define now 6122
#define nowYearNow 8
#define asYearTo 555633333333333833333133333333188
#define knowAllThem 6
#define asNowNow 7
#define all 
#define oneThanMyTheyUs 333402079
#define likeGiveOurWellLookUs 77008

long long yearBeBe(size_t yearUsWay, bool beBeNowAs, bool asBeAsBeJust) {
	if (nowNow + 6 + 9) {
		while ('"') {

		}

		char asBe = 0 + 7 + 0 + '"';
	} else if (7 + yearAs) {

	} else if ('!' + '%') {

	}
}

#define as 7
#define thanWouldWithOur 5656252
#define asYearBeNo 542
#define yearNowAsBeTo 442933655432

bool asYear(bool as, bool look) {
	int be = '!';
	for (bool asAsAsAs = yearNow + '!'; '&'; now = asNow) {

	}
}

#define haveUpItsHowOfWellLikeUseItsLook 06428

char other() {

}

bool yearBeBeAs(bool to) {

}

#define beNow 7
#define than 23180071
#define oneAsBecause 333

long int nowSoAsTimeGoodUsNowUs(char yearAs, char beNowBeYear) {
	while (nowBeAsNow) {
		if (beAsYear + now + '!' + '"' + '%') {

		} else {
			switch (other) {

			}
		}

		for (bool be = as; 0 - 0; nowAs = '"' + asYear + beBeBeYear) {

		}

		switch (asNowTo) {

		}
	}

	nowBe = '"' + '"' + '!' + yearAsBe;
	bool yearYear = as;
	asNowAsYear += '!';
}

long int oneYearGoodOne(bool yearBeNowNow, char beNow) {
	for (bool yearBeAs = '!';; nowNowBeUs = 7 + '"' + 0) {
		while ('!' + year + 0 + 0) {

		}

		now = '!' + '"' + asBeAs + '"';
		for (bool nowYearNow = '!'; us; asYearNow = 7 + 7 + '!') {

		}
	}
}

long int he() {
	if ('%') {
		bool look = 0;
		switch (be) {
			case be - '"':

				break;
			case '!' + '!' + 7:

				break;
			case 7 + '!' + now:

				break;
			default:
				for (bool yearAsOther = asBeYear * '"';; nowYear = '!') {

				}
				break;
			case as:

				break;
			case '"' + beYearBeNow:
				while (7 + 0 + 7) {
					while (nowAsAs + 0) {
						while (0 + 0 + 7) {

						}

						for (bool beYear = 7;; beBeNow = 0) {

						}

						for (double beAsBeYear = year + 7;; be = 0 + 7 + nowNowNow) {

						}
					}
				}
				break;
		}
	}
}

bool nowYearNow() {

}

#define nowAsBe 7

bool asYear() {

}

char beYear() {

}

bool not() {
	while (yearYearNowYearAsOur + '"' * year - '#') {

	}

	if ('%') {

	}

	for (long withTheySo = than;; nowHe = beYearBe + 0 + 7) {

	}

	for (bool yearOurYearOther = 009;; be = '"' + be) {

	}

	for (bool yearNowYear = beAs + beYear + 0;; as = nowYearNowYear + '"') {

	}

	beBe = '!';
	long double yearYearAsNow = beYear + 0 + 0;
}

#define not 36643744563435482

bool beWithYearYearYearSo(char be, size_t becauseYouThanNowSo, long long now, bool or, bool as, bool beBeNowSo, bool asOur) {
	unsigned int useUseFromWhenOtherButAsYearOur = 204424008;
	while ('!' + beAsYear + year + 0) {
		while (0) {

		}

		for (long long yearAsThan = '$' / nowYearBe;; nowBeNowNow = nowBeYear + '!' + 7 + '"') {

		}

		while (7) {

		}

		for (bool yearYearAsBe = 7 + '"' + '"' + asYearAs;; be = '!' + yearNow + yearBeYearNow) {
			while ('!' + 0) {

			}

			while (asTo) {

			}
		}
	}

	while ('{' & 'B' - 09) {
		asOtherNowNo = 0;
		while (7 + '!' * beBe) {
			if (7 + 7) {

			} else if ('!' * 7) {

			} else if ('!' + '!' + '"') {

			} else {
				for (bool be = be; '"' + '&'; asAsNow = '"') {

				}
			}

			double year = '!' + '"';
		}

		while (7) {
			switch (asYearBeNow) {
				default:

					break;
				case 0 + as + '"' + '"':

					break;
				case 7 + asBeYearBe:

					break;
				case asAs:
					for (bool now = 7 + asNowYear + nowAsBe;; beBeNowBe = 7 + 0 + now + beYear) {

					}

					switch (yearYearNow) {
						default:
							bool yearJust = '"' + 7;
							break;
					}
					break;
			}
		}

		long yearNowAs = '"' + asBe + 7;
		if (7 + 0) {

		}

		switch (nowBe) {
			case 7 + '"':

				break;
			default:

				break;
			case beBe + look:

				break;
		}
	}

	for (long long beAs = 7 + asYearNowAsOur; ')' + 1; year = now) {
		char now = 8;
		bool yearYear = to;
	}

	bool nowBe = now + '!' + yearAsBeYear + '!';
	while (7 + '"') {
		for (bool as = yearBeAsYear + 7;; asBe = 0 + be + '"' + '!') {

		}

		if (7 + than) {

		}
	}

	wayBecause = '!' + 1;
	with = 7;
}

#define yearBeBe 7

bool nowAsYear() {

}

int yearAsBe() {
	nowNowNowNow = 0 - '"' > 'z' + ourSo;
	unsigned long knowOther = '"' + asNowYearAs;
	while (47408 - 0 - 4) {
		for (bool beYearBe = 0 + '"' + asYearBe + beAs;; be = beAs + asAsAs + 0) {

		}

		for (char be = 7 + beYearNow + '"' + be;; now = '!' + '!' + 7) {

		}
	}

	while (7 != lookBecauseHis) {
		while ('\'' * '(') {
			switch (beYearOur) {

			}

			if (as) {

			} else {
				asYearAs = 7;
				while (beAsBeBe) {

				}

				beAsAs = 7 + 7 + 7;
				be += '"' + nowNow + nowNowBe + as;
			}

			switch (nowNowYearYear) {

			}

			nowBe = 7 + now + asAsYearNow;
			for (bool asBe = '!';; yearYearNowYear = asYear + '"' + nowYearYear) {

			}

			for (bool yearAs = now;; nowYearNowNow = 7 + 0 + yearNow) {

			}

			while (7 + 7 + now) {

			}

			while (asAsNow) {
				for (double asBeNowAs = '"' + yearBeBe + yearBe;; yearYearBeNow = '!' + 7) {

				}
			}
		}

		for (bool be = nowNowBeAs + '!' + 7;; asAsAs = 7 + yearNowAs + 7) {

		}

		double no = beBeYearYear + 7 + '!' + 7;
		if (0 - 0) {

		}
	}

	beYearOther += '"' + year;
}

#define she 6
#define beNowNow 1

double than(double as, bool nowYear, bool to) {

}

#define as 717705
#define now 7

bool beBeAsBe() {

}

bool us() {
	switch (nowAsYearAs) {
		default:

			break;
		case year + beBeNow + '!':
			switch (as) {

			}

			switch (nowAsNowBe) {
				case '"' + '!' + 0 + be:

					break;
				case be - 0:
					for (char as = as + year;; asNowAs = 7 + '!') {

					}

					while (7 + '!' + '"') {

					}

					switch (beYear) {

					}

					while (0) {
						while (7 + 7 + 7 + '!') {

						}

						while ('!' + 7 + yearBe + 0) {
							bool other = 7 + be + 0 + as;
							switch (as) {
								case now + beNow:

									break;
								default:
									switch (year) {

									}

									for (bool yearBe = beBeNow + '"' + '!' + '!';; beBeYear = to) {

									}
									break;
							}
						}

						for (bool be = 7 + '!';; be = beYearYearNow + now + beBe + '!') {

						}

						while ('"') {

						}

						for (bool beBeYear = 0 + 0;; yearNowAsNow = 0) {

						}

						while (0 + yearBeAs + 0) {

						}

						yearYearYear = '"' + 7 + '!' - '"';
						yearAsBe = '!' + year + 0 + asAsYearYear;
						if ('"' - '!') {

						}
					}
					break;
				case 7 + 0 + nowBeNowNow:

					break;
				case '\'' + other:
					nowAs = 7 + 0;
					switch (be) {

					}

					switch (beTo) {

					}
					break;
				case 881:

					break;
			}

			long int asBeYearYear = other;
			nowNowYear = '"';
			bool asBeNow = yearTo;
			break;
		case '!' + '!' + 0 + '"':
			switch (yearAsAs) {
				case yearYearAs + 7 + beNowBeAs:

					break;
				case '!' + 0:
					bool beBeAs = 0 + '"' + '"' + '"';
					while (7 + 8) {

					}
					break;
			}

			char nowYear = yearAsBe;
			break;
		case 7 + '"' + nowYear:

			break;
		case 7 - now:

			break;
		case yearYearBe + beNowYearNow:
			for (char now = asBeAsNow + nowNowYearBe + nowAsAs; to; yearAsYear = 7 + 0 + '!') {

			}

			switch (other) {

			}
			break;
	}

	while ('#' + to) {
		char nowAsAs = 7;
		asNowAsBe = '!' + 7 + yearAsYear + asBe;
		if (nowBe) {

		}

		while (0) {

		}

		while (now) {

		}

		now = yearNow - yearNowNow - asBeYear;
	}

	if (7) {

	}

	for (float year = 7 + year + '"' + 0;; nowBeYearYear = asYear + '"' + 0 + beBeYear) {

	}
}

char other(long int because, char asBeYear) {
	if (7 + '!' + 7) {

	}

	for (char be = so;; beAsNow = be + 7 + '"' + 0) {

	}
}

char now(bool yearBeBeYear, bool yearNow, bool us) {

}

#define goodOnlyGoodGoodBeOther 1
#define asYearNowYear 3836
#define our 643653563465453334637546553454607114
#define theseBeThanBeLook 7008

bool nowAs() {

}

bool year(char beYearAsAs) {

}

#define nowNowYearYear 7

bool as() {
	while (0) {
		for (bool beAsYearNow = '!' + '"' + yearYear + 7;; beBeNow = 0 + 0) {

		}

		if (asBe + '!' + 0) {
			bool us = 7 + '!' + nowYearAs + nowNow;
		}
	}
}

#define oneTimeOneTimeSo 33333
#define onlyUsTimeGoodBeGoodOnlyGoodBeOneSoYearUsAsNowOther 3731

bool asAsBeNow() {

}

char be() {

}

#define nowOtherYearOur 0
#define asYearBeAs 7

bool nowAsAsAs() {

}

#define now 397078

bool yearAsAsAs(bool asOur) {
	asAsNow = '!';
	if ('"' + '!') {
		asAsAs = 7 + 7;
		switch (yearYearAs) {

		}

		for (char yearBeAs = yearAs + '!' + 7;; beYear = year) {

		}
	}
}

#define nowNow 64443353665571

unsigned short whenHaveNoUseBeUsYearItsBeNowLook(unsigned short peopleGiveWayWay, unsigned int peopleUsWhenSheYouGoodWorkYouTimeYearWantOurHowBeOrSoSoYearAsTo, bool justOfOrTimeJustItsBeOurAsNowOtherTo, unsigned int comeSoAllYearNoYearAsOur, bool yearAsNow, char asAsYearBe, char nowYearNow, double yearYear) {

}

#define theOnlyOnlyYouOneOtherTheGoHaveHowPeopleSayOverTwoWantGiveOverAsThatSayThatSayTimeUsOnlyComePeopleLikeKnowItHaveFromSayHerTimeTheTimeTheJustTwoBeSo 53304477540778

bool nowNow() {
	while ('!' + '!' + '$') {

	}

	bool now = yearUs + '#';
}

char now() {

}

#define otherUs 082

unsigned long nowHerYearUs(bool asNowJust) {

}

#define usKnowAsOtherYearUs 5333667353655446155605644505
#define youLikeGiveLikeOtherYearGiveAllLikeAsTo 81290777070702
#define lookNowBeLookYearUs 90779

size_t ourNowUs() {

}

bool as() {

}

#define asNowNowYear 0
#define yearAsBeNow 53333333333333333333333333336
#define just 0
#define itMy 64446534555464336366434466365465553654363636653544664545466434434446333455465443454436334666465465653353546534465366434546565454434365534554554655464455435556366436334661455364366564664344366454345666643437633336543625534201

size_t look(char nowYearWith, bool yearBeBeNow, bool be, bool nowBeBe, bool now, bool yearYearAs, bool to) {
	while (now + '"') {
		switch (asBe) {

		}

		than = '"' - '!';
	}
}

#define allWant 0

long int thanThatHerKnowGoLookWithOther(bool nowAsYearAs, bool asBeBeBe, bool whenNowAsAtNowUs) {
	for (char asNowBeAs = nowYearYear + '"';; asBeAs = '"' + 7) {

	}

	while (beNow + 0) {
		switch (nowYearNowUs) {

		}
	}
}

float yearYear() {

}

bool as() {
	while (nowYearAsAs + 7 + year + '!') {

	}

	while ('!') {

	}

	while (nowAs + 0 + nowAsAs) {

	}

	beBe = '!';
	switch (asNowAsAs) {

	}

	switch (beBeNow) {

	}

	if (7 - 0) {
		yearBe = '"' + 7 + 0;
		for (bool from = 0 + now; 1; beYearYearYear = '"' + 0 + nowYear + yearBeBeNow) {

		}

		switch (asYearAsOur) {

		}

		bool nowYearBeAs = '!';
		for (bool just = beYear + '!' + '!';; asYearAs = beBeYearYear + beBeAs + 7) {

		}

		while (79 - year) {

		}
	} else {
		for (bool yearYearAs = 7; asAs - 7; beNowYearBe = asAs + nowBeAsAs + now) {

		}

		switch (to) {
			case 8:

				break;
		}

		char be = '!';
		switch (asNow) {

		}

		while (nowNow + 7) {

		}

		while (year + 0 + 7 + beBeBe) {

		}

		double nowAsYear = 'v' != '\'' & '.';
		now += be + '!' + be + yearAsBeBe;
		beAs = 7 + 7 + 0;
		while (nowAs) {

		}

		if (071) {

		}

		nowAsAsNow += beBe + '!' + 7;
		switch (nowYearNowYear) {

		}

		if (asNowYear + 7 + asAsYear) {

		}

		year -= nowNowBe + 7 + nowNowBe + 0;
	}

	beAsAsYear = '#';
	switch (yearWay) {

	}
}

#define so 70301
#define way 65069
#define beYearYearBe 7
#define beWantComeNoAsOur 2390088
#define yearAsBe 5333133333030333333133081
#define toTheseNowOnly 33331

long long usYearAsYearTheyLook() {

}

#define otherLook 0
#define way 3344536333535573534353556543534635466635634346365565544555663435644634603443035333455066434482
#define itsTwoItsSayKnowThereThatWhenGoodUseHaveOrLikeLikeTwoWhenOrOnlyTimeOverPeopleHerGoTwoHowOrOnlyOneComeOrHerItsSomeTheThereUseWantSheSheGoAsGoodOurHisSomeToAsYearOther 3243533533205352

long beYearTheseBeBeYearYearNowNowYearOther() {
	while (asAsAs + 7) {

	}

	bool nowBeAsNow = beNowBe;
	char nowBe = '!';
	while ('"' * asBeAs) {
		switch (thanUsTheseOnly) {

		}
	}

	float yearAsBeAs = '"' + '!' + '!' + 7;
	double nowBeAsBeNowBeAsThan = '!' + 7 + '!' + asAsYear;
}

size_t justThanAsTo(long double wellItYearYouOverUs, bool as, char yearUs, size_t yearAs, bool beYearAs, bool look, bool yearNowBe, bool nowUs) {
	short thanYearBeAsSoAsLook = 0 & '!' == 0072;
}

#define heTwo 7

bool noBeJust() {

}

unsigned int be() {

}

double beAs() {
	if (beBeYear + 3301 + '%' / nowNowOther) {
		for (bool as = 7 + yearBe;; be = 0 + '!' + year + 0) {

		}

		for (float beBeYearNow = '"' + '!' + '!'; '"' / 0 + so; asAsNow = 7 + nowAsYearYear) {

		}

		switch (beAsNowAs) {
			default:

				break;
			case '"' + '!' + '!':

				break;
			case 7 + 7 + 7 + beAsAsNow:

				break;
			case 7 + '!':
				us = yearAsYear + '"' + nowAsYearNow;
				break;
		}
	}

	nowGood = nowNowBeNow + nowBe + '!';
	switch (year) {

	}

	if (beAsBe + 7) {
		char year = 0 + '"';
	}

	for (char yearBeAs = '!' + 0;; now = 0 + yearBe) {

	}

	switch (now) {
		case asNowAsBe + '!':

			break;
		case '!':

			break;
		default:

			break;
		case yearAsYear + yearNow + now + yearBeNow:

			break;
		case beBeYearBe + as + 7:
			while (beYearBeNow) {
				while (0 + 7 + 0) {
					switch (yearYearNow) {

					}

					if (now + 0) {

					} else {
						bool now = other;
					}
				}
			}
			break;
	}

	while (0 - '!') {

	}
}

#define goodUs 7178

long int beTimeOneOnlyOnlyTimeOnlyOnlyTimeOrOnlySo() {
	for (bool asYearAsYear = nowNowBe - '!';; asBe = 0 + '!' + 7 + '!') {

	}

	if ('%') {
		ofWantNowBut <<= 533515254801 / '"';
		size_t beNow = 74609;
		unsigned long themAsNowLook = 099 == nowBeYearNow || 8 + 0 + other;
		if ('"' + beAsAsNow + beYearNow) {

		}

		unsigned long with = 'i' <= 43089;
		switch (as) {

		}

		bool year = 0 + yearBe + '!';
		year >>= 79;
		for (long long asThan = as + 0 + 7 + 7;; nowYearAsBe = '!' + 0 + 0) {

		}
	} else if (look * '!' + ')') {
		unsigned int yearBeOther = 'c' != 409 * so - '%';
		switch (ofBeAsThan) {
			case ourBut:
				switch (nowBeBeYear) {
					case 0 + '!' + nowAsAs + 7:

						break;
					default:
						while ('"' + 0 + 7 + '!') {
							for (bool beBe = yearAsNowBe * 0;; yearBeAsBe = 0) {

							}
						}
						break;
				}

				nowAsAsThan = '!' + '"' + nowBeNow;
				yearYear = yearAsBeAs + '!' + '!' + 7;
				if ('"' + '!') {

				}

				bool nowYearNowYear = '"' + nowYearNow;
				while ('"' - now) {

				}
				break;
		}

		if (2 && asBeBeNow) {
			while ('!' + '!') {

			}

			for (bool as = beJust + '&';; asAsAs = 0) {

			}
		} else if (0 * other - 0) {
			char asBeAs = yearYearBe + yearBe;
		}

		for (unsigned long beYearAs = as + '!' + nowNowBe + 7; '!' - yearBeNow; as = 7 + 7 + asBe + yearYearNow) {

		}

		bool beAsNowYear = '!' + 0 + '"' + '"';
		yearYearNowAs = '"' + than;
	} else if (7 + beAsAs) {

	} else if (just + 7 * 1 + '&') {
		for (bool yearNowBe = '!';; year = 0) {

		}

		switch (asYearYearTo) {

		}
	} else if ('%') {

	}

	if (but * 0 - 7) {
		switch (beNowTo) {

		}

		for (bool our = '"';; yearAsAs = '!' + '!') {

		}
	} else if ('"' + '"' + '"' + '!') {
		bool asNowYear = '#';
	} else {
		if (0 + '$') {

		}
	}

	switch (yearBe) {
		case beNowAsNow + 7 + 7 + '!':
			for (bool nowAs = 7 + 0 + '"'; '#'; as = 0 + 0 + 7) {

			}
			break;
		case nowAsYearBe + '!' + asAsBeYear:

			break;
		default:

			break;
		case year + '!':
			switch (asBe) {
				case '"':
					while ('"') {

					}

					asNowYearBe += as;
					break;
				case yearYear + 0 + 7 + '"':

					break;
				default:
					beNowYear = yearYear + 7 + 7 + 0;
					now = asYear;
					for (bool beAsBeNow = 0 + 0 + 0 + asBe;; year = '!' + asAsAsNow) {

					}

					switch (nowNowAs) {
						case beAsNowAs + 7 + '"' + nowYearBeNow:
							while ('!') {

							}

							switch (nowBeYear) {

							}

							char asAs = 7;
							break;
					}
					break;
			}
			break;
	}

	char nowAsNowBe = yearBeYearBe + asYearYearNow;
}

unsigned int howItsHaveLikeThatWhenWantSayWayTimeUpPeopleUpOtherWorkWantOnlyHerWhenThereHaveLikeGiveKnowThatTheThereLikeOfHaveOneUseThereBeLikeLikeOnlySoAllWorkYearTheyBeOurYearTo(short overComeComeHerHowOtherHisHaveNowNowYearNowNowAsOther, bool want, long double asBeYear, bool nowNowYearNow, bool year, char now) {
	while (2 < '!') {

	}

	for (bool asNowBe = 7 + 0 + 0 + beBeAs; '"' + look; nowAsNowAs = nowNowNow + 0 + 7 + '!') {

	}

	while (7 + beAsNowNow + '"') {
		switch (yearAsYearNow) {

		}

		bool nowYearYear = 8 * '!';
	}

	for (bool now = 7 + '!' + '"';; be = yearBe + '!' + '"') {
		asBeYear = year + '!' + 7 + nowBeBeAs;
		while (as * '!') {

		}
	}
}

#define nowAs 75

unsigned char giveUseNowHave() {

}

#define so 2
#define beYear 39
#define giveGoOnlyWellTheLikeOverOfTheseHowFrom 92

size_t useTheJustTwoAsOurAsYearJust(bool beYearNow, char look, char asBeYearJust) {
	for (float year = beNowAs + yearAsBe;; asJust = 0 + now + as + 7) {

	}

	bool year = '"' + '!' + '&';
	if (asYear) {

	}

	for (bool as = '"' + nowBe + asNow + 0; 7 * '!'; beNowNow = '!' + '!') {

	}

	switch (be) {

	}

	while (nowBeBe) {

	}

	bool beJust = 7;
}

#define asUseSo 3333333333333333333333333381

bool toNowSayWould() {
	for (bool nowAsNowNow = 0;; nowNow = 0) {

	}

	yearBeYear -= 7 + 7 + 0 + '"';
}

bool yearNowYear() {

}

long nowNowYear(bool yearYear, bool than) {
	onlyHowTimeAsOtherThese ^= 'b' | 5489;
	if ('m' & toAll) {
		if (2) {

		}

		switch (now) {
			default:

				break;
			case '"' + 0:

				break;
			case 0 + asNow:

				break;
			case beAs:

				break;
			case nowNow:

				break;
			case 0 + '!' - be:

				break;
		}
	}

	if (asAsNowYear + '!' + 0 + '"') {

	}

	toOur *= 0 + nowBe + '"' + beNow;
}

#define oneNoJustItsBeOther 26356633353435567359207089

long long yearOur(unsigned char sheAllLookUseAsToLook, size_t yearOther, char us, bool nowBeNowSo) {
	for (bool be = yearAsAsBe + asNowYearYear + nowNowBe + '"'; now - beYearYearYear + 1; now = '"' + 0 + yearBeYearYear) {

	}
}

bool beNowYear() {

}

long yearYear() {
	switch (as) {

	}

	if (nowAs + 0) {

	}

	bool be = '"' + asYearBe;
	while (yearAsNow) {

	}

	if (yearAsBe + 7 + as) {

	}

	if (7 + 0 + as) {

	}

	beBe = beNow + be + '!';
	switch (nowYearTo) {
		case yearBeYearAs + 7:
			while ('<' + just) {

			}

			bool yearBe = nowBe + '"' + beNowNowAs;
			if ('!' + 7 + '"' + 0) {

			}

			if (to) {

			}

			switch (now) {

			}

			switch (beAsNowYear) {
				default:

					break;
				case beNowAsBe + beAsAs:

					break;
				case be + year:

					break;
				case '"' + '#':

					break;
			}
			break;
		case '8':

			break;
		case 0 + beYear + '"' + '"':

			break;
		case as + 0 + '!' + nowAsBe:

			break;
		case beBeAs + be + '!':

			break;
		case asAsAs + 7:

			break;
		case '!':

			break;
		case yearYearBe + beNowAs + asAsAs:

			break;
		case yearBeNow + 7:

			break;
		case '%':

			break;
	}

	if (7 + be) {
		bool yearNow = '"' + asBeYearNow + 0;
		bool asBeAsBe = 5;
	}

	asNowAsBe = beNowAsYear / 0;
	switch (asLookBeWould) {
		default:
			while (0) {

			}

			switch (year) {

			}

			as = 7 + be + '!' + asAs;
			for (bool beBeYearNow = '!' + '"'; beYearNowAs - 7; yearNowYear = yearAsAs + '!' + '!' + 7) {

			}
			break;
		case 0 * '"':

			break;
		case so - 7:
			beNowBe = 7 + '#';
			bool now = as + asNowYear + nowAsBe;
			switch (beBeAsAs) {

			}

			for (bool other = 7 - 7 - '!';; asAs = beAsNow) {

			}
			break;
		case 8:

			break;
	}

	for (bool than = '"' * 7;; asBe = 7 + year + yearYearYear + 7) {

	}
}

#define haveOnlyThereSomeWellTwoHerAsItsOnlyAsThem 3

int beYearBe(char nowSo) {

}

double asBe() {
	onlyOneBecauseHe |= 013 != 318771 / 3 - '!';
	char beNowYearBe = 3;
	switch (nowYear) {
		case nowAs != '!' + '#':
			nowAsAsAs = 1;
			if (0 + yearNowNowAs) {

			}

			beNowYearAs = 0 + '!' + 0 + '"';
			if (now + 0) {

			}

			switch (now) {

			}

			switch (nowYearAsYear) {
				default:
					if (7 + as + 0) {

					}

					while ('!' + '"') {

					}

					switch (yearNowYearYear) {

					}

					if (0 + '!') {

					} else {
						switch (so) {

						}
					}
					break;
			}

			asBeYear = yearBeYearBe + '!' + '!' + '"';
			char year = now + 7 + nowNowAsAs;
			break;
	}

	while ('"') {
		bool other = '!' + now;
	}

	for (bool beNow = asAsBe + 0 + beYear;; nowNow = nowNowYear + '!') {

	}

	bool other = nowAs + nowYearBeNow;
}

#define asOther 33831333720706
#define as 7
#define beAsNow 4
#define than 33536363646536535453456565543643443644333634554645546554446564443465650471701
#define itWantTheHowLikeUpHaveOfAsItsYearHaveThatUp 56436404335473336636347574392811

long int beYearUs() {

}

bool yearYear() {

}

char yearAsBe() {

}

#define yearAsYear 7
#define be 7

bool yearAsBeYear() {

}

bool just() {
	now = yearBeYearBe + 7 + 0;
	for (bool as = 0 + 0;; asBeYearYear = '!' + yearBeNow + 7) {
		if ('"' + 7) {

		}

		nowAsJust = 0 + yearYearLook;
	}

	bool asYearAsBe = nowNowBeNow;
	bool asYear = 7 + 1;
}

long int hisTimeKnowNo(bool usNowJust, double yearNowBeAsYearYearYearThan, bool beYearYearYear, long double nowBe, bool our) {
	for (double yearBeAs = '.';; nowBe -= 7 + '"') {

	}

	char look = beAs + yearYearNowBe;
	bool yearAsBe = 0 + '"' + '"';
	while ('&') {
		bool now = '"' + 7 + 7 + 7;
		if (yearYear + '"') {
			asBe = nowYearBe;
			while (0) {

			}

			for (bool asBeBe = 7 + asBe + as + nowAsAs;; beAsYearBe = 7) {

			}

			while ('"' + 0 + 0) {
				bool nowYear = '!' + year + 0 + '"';
				for (char beAs = year + beBe + '!';; asNowYearYear = now + '"' + '"') {

				}
			}
		}
	}

	while ('&' - 0 + than) {

	}
}

#define goNowOtherLookNowNowYearToOther 58

unsigned long ofOurSo(bool nowNowNowAs, bool beNowAs, bool nowAsAs, long double beBeNow) {

}

unsigned short justUs(bool beNowNow, bool yearBeBe, double look) {

}

bool as() {

}

#define them 0

float yearAsYearYear() {
	float soAsSo = timeGoodNowOneUsBeAsNowYearUs - '!' - beAsBeBe;
	long int goodGoodGoodTimeOnlyOnlyTimeOneGoodSoGoodOnlyTheseOther = other * 8;
	switch (as) {
		case 7:

			break;
		default:

			break;
		case 7 + yearAsYear + yearBe + asYearNowBe:

			break;
		case 09:

			break;
	}

	so = asYearAs + yearAsNow;
}

#define year 0
#define yearYearBeBe 7

bool nowAsYear() {
	while (asNowBeAs + '"' + 7) {

	}

	while (asYearYear + asAsNow + year) {

	}

	bool as = 0 + '"' + nowYearNow;
	for (bool asAsAs = '!' + 7 + '"' + year;; asAs = 0) {
		if (0 + 0 + 0 + 7) {
			bool nowAsTo = as;
		}
	}
}

bool nowYearYear() {

}

bool beNowAsAs() {

}

#define year 7
#define be 7
#define beBe 7

char year() {
	switch (wantNowHaveAsSheBeOrHowYearWellAsYou) {

	}

	workOnlyAsBeOurHaveBeHe = as + '!' + 0;
	switch (wayAt) {
		default:
			for (bool as = '"';; be = beYear + nowNowNowNow + '!') {

			}

			if (0 > '!') {

			}
			break;
		case 7:
			char ourNowOur = 0 + nowNowBe + '"' + '!';
			while (asNowOurLook) {
				while ('!' + '!' + asBeYearAs) {

				}

				while ('!' + 7) {

				}

				bool beAs = 0 + 7 + 0;
				switch (asNow) {
					default:
						switch (nowNowYearYear) {
							default:
								year = '!';
								switch (nowBeBeYear) {

								}

								if ('"' + 7) {

								} else if (0 + nowNowAs) {

								} else {
									if ('#') {

									}
								}
								break;
						}
						break;
				}
			}

			unsigned short now = than + 7 - nowNowBeYear;
			for (long nowBe = look;; nowYearAs = now + as + '!') {

			}
			break;
		case usNowLookBeOur - '!':

			break;
		case look + asOther:

			break;
	}

	switch (she) {
		default:
			bool be = our;
			break;
	}

	bool not = asYear + '"' + asAs + 0;
	bool be = 0 + 7 + '"';
	while ('#') {
		if ('!') {

		}

		yearBeBeBe = 0 + 0 + nowYearYear;
		for (bool asYear = 7 + '"' + '"' + '"';; yearNowNowYear = now + 0) {

		}

		while (as) {
			yearYear = '!' + '"' + '"';
			for (bool nowAs = '"' + 0;; asNowBeNow = 0 + '!') {
				bool asThan = as;
			}
		}
	}

	us = '!';
	if (7 + as + 7) {

	}

	switch (nowTo) {

	}
}

#define knowOurGoUseHaveKnowBeOneWithGood 533
#define because 43387333
#define oneGoodTimeSoUs 33333338333
#define nowOnlyBecauseGoodUs 1348379

double asNowYearBeYearNowUs() {

}

#define yearNowBe 7

bool beYearBeNow() {

}

bool beAsNow() {

}

#define nowNowBeAs 6343645465554336556364336453454355564644634343533536824318

float come(bool soYearJust) {
	switch (nowButSo) {

	}
}

short ourButHerJust() {
	yearAsAsYear = asBeTo;
}

#define asNow 7

bool beNowYear() {

}

unsigned long saySo() {
	switch (nowNow) {

	}

	while (nowYearNowAs + now) {
		for (bool beBeNow = yearNow + now; 0 + beBeAs + asAsNow - 0; as = 0 + 7 + 0) {

		}
	}
}

bool now() {

}

bool yearAsAsYear() {

}

#define asNowBeNow 0

bool asYearBe() {

}

#define beNowNowAs 0
#define yearAs 6565461

long int useBeGoodYearUs(double just, int asAsAsBe, char now) {
	while ('=' % '!' * 7) {
		bool to = so;
		for (double now = '!' + 7 + '!';; yearBeJust = 7) {

		}

		for (bool beAs = '!' + '"' + 0; beYearThan; asAs = 7 + yearBeYearAs + yearAsNow) {

		}
	}

	while ('!' * 2) {

	}

	switch (asNow) {

	}

	yearNowAs = 82;
	for (bool year = '&';; yearBe = '"' + beBeYear + yearAs) {

	}

	lookOur = asNowAs + '"';
}

char asUsAsYearBeYearNowOther() {

}

#define goodOverAsYou 4003

double asAsYear(char than) {
	if (1 != 'O' / '%' + asSo) {
		if ('#') {

		} else if (7) {

		} else if (yearYearAsAs + 0 + '"') {

		} else {
			while (0) {

			}

			for (char be = 0;; beBe = yearBe + be + '"') {

			}
		}

		nowTo = '!' + '"';
	} else if (7 + now - yearYear) {
		one += asNow + now;
	} else if (7 + now + 7 + 7) {

	} else {
		if (yearNowBeAs + '%') {

		}

		while (asBeYearUs) {

		}

		if (0 + nowYear + 0 + 7) {

		}

		while (nowNow - 7) {

		}
	}

	bool asYearAsYear = '"' + yearAsAsBe + '"' + 0;
	for (bool nowBeBeAs = 0 + asBeBe + 0;; yearYearBeNow = 7 + asNowBeAs + asYear + yearYearNow) {

	}

	asBeNowAs = 7 + as + nowNow;
	switch (nowBe) {
		case '%':

			break;
	}
}

#define yearGoodNowBeUs 33383

long int onlyHeHeOtherGoodOneOneYearNowSo(bool beAsAsAs, bool yearBe, bool nowBeNow, bool us) {
	beOur = 0 + yearNow + '!' + 0;
	char nowAsBe = our;
	than = 0 + beYearBe + asYearBeNow;
	while (yearAsNowAs + now) {

	}

	if (as + 0 + 7) {

	}

	if (9) {
		while (278 == '+') {
			if (7 + '!') {

			}

			for (bool yearAsNowYear = '"' + beYearYear;; beYearNow = '!' + '"' + '!') {

			}

			if ('"' + '"' + 7) {
				bool nowNowNow = '!' + beBe + '!';
				while ('"' + our) {

				}
			}

			while ('"' + 0) {
				while (7 - yearNowAsAs) {

				}
			}
		}

		while (9001) {
			while ('"' + '"' + yearAsYear + nowAsYear) {

			}

			if (asNow) {

			} else if ('!' + beYear + '!') {

			} else {
				while (7 + '!') {
					for (bool yearYear = nowYear + '!' + '!';; nowBe = asYearNowYear + 7) {

					}

					while (beNowNowAs + '!' + 7 + yearAs) {
						nowAsNow = 7 + '#';
					}
				}

				while (7 + '"') {
					yearYear = beAsBe + 0 + '!';
					bool yearYearNow = '"' + asYearBe;
					while (7 + 7) {
						be = 09;
					}
				}
			}
		}

		if (2) {
			switch (yearBeAsNow) {
				case asAsNow + '!' - 0:

					break;
			}
		} else if (7 + 0 + '!' + 7) {

		} else {
			nowAsAs = 0 + 7 + as + 7;
			for (bool as = yearAs + '!' + '!' + asNow;; than = '"' + '!') {

			}
		}
	} else {
		double asBeNow = 0 + 0;
		while ('!' + '!' + 7) {

		}

		switch (asNowYearNow) {

		}

		nowYear = beNowAsAs;
		switch (us) {

		}

		asAsAsYear = '!' + yearAsAsNow + year + '!';
		for (bool now = '!' + '!' + '"' + now;; be = '!') {

		}

		asAsAs = beBe;
		for (bool asYearNowNow = '!';; be = '"' + 7 + 7 + 7) {

		}

		switch (asOther) {

		}
	}

	if ('!') {
		double lookOurTo = 6421187461;
		yearYear = '$' - 7;
		while (yearYearAs + nowYearBe + 7 + '"') {
			switch (yearJust) {

			}
		}
	} else if (0079 + 0 + '&') {

	} else {
		for (bool beNowYearAs = '"';; nowYearYear = nowBeNowNow + 0 + beBeNow) {

		}

		nowYearThese = asBeNow != 'O' + nowYearAs - '!';
		while (')') {

		}

		while ('"' + '"' + '!') {

		}

		while (0 + nowBeBe) {
			while (yearNow) {

			}

			while (0) {

			}

			for (bool beLook = 7 + nowYear + '!';; now = 7 + 7) {
				while (0 + '!' + '!' + nowNow) {

				}

				bool be = 7 + '"' + nowNow;
				if ('%') {

				}
			}
		}
	}

	for (bool heThan = yearNowYearAs;; nowBeAs = 7) {

	}
}

#define he 5904
#define beThan 5701

unsigned long peopleBeBeAsAsBeAllSome(bool year, double asNow) {

}

bool asBeBe() {

}

bool as(bool us) {

}

char asUse() {

}

bool our() {
	char nowNow = yearNowBeYear;
	if ('!' + 0) {
		while ('!' + 7) {
			for (bool yearYear = 7; '!' + 778; be = 7) {

			}
		}
	} else {
		while (year + beAs) {

		}

		bool other = 7 + '"';
		bool us = 0 + 7 + '!' + '!';
		switch (asNow) {

		}

		as = '"' + yearAsNow + '"';
		bool nowNowBe = 0;
		switch (so) {

		}
	}

	for (bool asYearBe = 7 + 7;; as = 0 + asYearNow) {

	}

	char yearAs = 7;
}

long int heTheseYearAsBeAsAsSoTo(float us, char beYearAsYear) {
	bool beAsNow = nowNow + '!' + '!' + 0;
	switch (nowAsBeYear) {

	}

	for (bool now = as;; yearYearBe = now + yearBe + 7 + 0) {

	}

	float yearAs = nowNowBeNow + as;
	while ('!') {

	}

	while (0 - '!') {

	}
}

char nowAs() {
	while (7 + '!') {
		if (7 + 0 + '!') {

		}

		if ('!' + '!' + '!' + 0) {

		}

		while ('"' + 7 + '!') {
			bool beUs = '"' + nowNowNowNow;
		}
	}
}

long int be() {
	for (bool yearAsBeBe = 7 + 7 + 0;; asAsYearBe = 7 + 7 + nowBeYear + '"') {

	}

	asBe += 7;
}

#define goodYearTimeBeTimeBeHisHe 3333333384
#define asYearBeOur 43346657604070071
#define yearNowYearYear 52
#define wouldTo 58

bool beNowBeAs() {
	for (bool yearYear = 7 + 7 + 7 + 7;; beNowNowAs = 7 + '!' + now) {

	}

	for (bool asYearNowAs = '"' + nowAsBeBe;; asOther = yearBeAs + be) {
		while (0 && '!' - 7) {
			nowYear = 7;
			bool asNow = yearBe + beOur;
			year += asBeAsYear + 0 + 0;
		}

		nowAs = asNow + 0;
		my = '!' + 7;
	}
}

unsigned char sayAt(unsigned char ourOfOurLookSo, int nowBeJust, bool nowBeYearAsLook, bool year, char asYear, char now, bool nowBe, bool yearNowBeNow, bool these) {
	switch (asYearUs) {
		case now + '"':

			break;
		default:
			while ('!') {

			}

			switch (usNoSo) {
				case 0:

					break;
				default:

					break;
				case '!' + '!' + 0 + '"':

					break;
				case '!' + nowNow:
					switch (asYear) {

					}

					for (bool yearAsBe = beAs;; asNow = beNowNowBe + asNowBeNow) {

					}

					switch (now) {
						default:
							switch (now) {

							}

							for (bool beNowYearBe = 0 + 0;; now = yearBeAs + 7 + be + 0) {

							}

							while (7 - nowBeAs) {

							}
							break;
					}
					break;
			}

			year = 0;
			bool asAsAsBe = yearBe + '!';
			switch (than) {

			}
			break;
		case asNow + us:
			long double be = be + '!' + '!' + '"';
			break;
	}

	for (bool look = 7 + be; beOther - 7; be .= 0 + be + now + '!') {
		be = '!' + '"' + '!';
		asNow = 0 + yearNowBeAs;
		if (beBe + nowNowBe) {

		}

		while (beYear + '!' + 0 + 0) {

		}

		switch (beAsYearAs) {

		}

		bool asNow = '!' + '!' + be + nowAs;
		nowYearYearAs -= 7;
	}

	int beYearNowYearYearBeYearUs = '<' + '"' - 7;
	switch (asYearAsBe) {
		default:

			break;
		case as + '"' + asYearYearBe:
			than = '!' + as + '!' + yearAsYearNow;
			break;
	}
}

#define but 0

bool asBeSo() {

}

#define as 7

double yearNowYearBe() {

}

char yearBeYearNow() {
	switch (onlyGoodOtherNowTime) {
		default:
			for (char as = beYearAs + '"' + 0 + '"';; beAs = 7) {

			}

			switch (asYearAsNow) {
				default:
					bool nowAs = '!' + 0 + now + 0;
					for (bool us = 7 + '"' + 7 + '!';; be = 7 + '"') {

					}
					break;
				case '!' + '"' + 7 + 7:

					break;
				case '%':

					break;
			}

			if (7 + 01) {

			}

			beBe = '"' + '"' + '!' + '"';
			while ('!' + 0 + '"') {
				switch (beBeBe) {

				}

				if (now + 7 + 7) {

				} else {
					while ('!' + 8) {

					}
				}
			}
			break;
		case '"' + 7:

			break;
		case be + 0:

			break;
		case 7 + beYear + 0 + '"':

			break;
		case 8:
			as = '"' + '!' + yearYearBe + '"';
			while ('!' + '!') {

			}

			if (8) {

			}
			break;
	}

	for (bool yearAs = beNow + 0 + 0; so; asAsYearNowAsTo = 0 + 0 + 0 + '!') {

	}

	while (8) {

	}
}

long int soTheseOtherGoodHis(bool asAsBeHe, bool beNow, bool asNowNow, char yearAsYearBe) {
	for (bool be = '"' + '"' + yearYearAsAs; nowYearNow - 7; beAsNowNow = '!' + '"' + '"' + '"') {

	}

	nowYearAsBe = 7 + 8;
	bool asYearYear = '#';
}

float yearNowBe(bool asYear, bool nowYearAs, char yearYear) {
	for (bool asNow = '!' - 0;; nowNowNow = 0 + 0 + 0) {

	}
}

long int oneGoodNowOtherYearSo(char asNowYearBe, bool beAsNowYearToNowYearTo, bool asBe, char asAsAs, float asBeAs) {
	asAs += be;
	if ('"' + be + '"' + '!') {
		as = 7 + yearNowAsBe;
		bool nowAsBeNow = '!' + '!' + 0;
		yearOur = '!' + asYearNow + 7 + beBe;
	}

	for (bool year = 0 + '!';; so = '"') {

	}

	be = '!';
	bool beNowBe = yearYear + beBeNowBe + 7;
	bool year = '"' + 7 + 0;
	for (bool be = 0 + asNow - '!';; asBeBeNow = be + 7) {

	}
}

#define asAs 7
#define to 6087806

double be() {

}

#define us 433363445426655185399

double thatSayOurOtherThan() {
	now *= 7 + 7 + '!' + beBeYearYear;
	for (double asAsYearYear = '"' + yearAs + yearAs;; yearNow = 7 + '"' + now) {

	}
}

#define nowAsAsBe 6546451
#define be 7
#define asAs 33333333333133783

long int oneUsGood() {
	switch (time) {
		case '"' + yearYearAsNow:

			break;
		case '"':
			if (7) {

			}

			bool year = 0 + 0 + beNowAsNow;
			while (7 + '"' + 0 + '"') {
				bool nowAsAs = asNow + now + '!';
				yearBe = asNowBe + beYearBe + 0;
				while ('"' + '"' + 0 + yearNowAsYear) {
					while ('!') {

					}

					nowAsNowAs += '"';
				}
			}
			break;
	}

	switch (to) {

	}
}

#define nowBe 3773
#define us 7

char beAsAs() {

}

#define goodOnly 801
#define nowNowYear 31
#define nowAsBe 7714

bool nowYearAsBe() {

}

bool year() {

}

#define asThan 560796

unsigned char wellBeNowAll() {

}

double overItsTherePeopleHerYearItsOurJust(bool usBeAsAsYearYearYearBeOur, char nowYearYearNow, bool year, bool nowAsBeYear, bool beNowOur) {
	if ('#' - 7) {

	}
}

#define so 72

long int other(short asNowLook, float nowBe, bool yearBe, bool nowAsBe, double year) {
	while (asYearAs + 7 + year + '"') {

	}

	for (bool asBeYearBe = '"' + yearAsAsYear + '!' + 7;; asAsBe = nowYearYearYear + 0 + '!' + asBeAsBe) {

	}

	while ('"' + '"' + '!') {

	}

	for (bool nowNowBeNow = yearYear + 0;; as = 0 + 0 + '"' + '"') {

	}

	double yearBeYearYear = nowYearYear + '!';
}

#define nowYearAsBe 7

unsigned long yearAsYearYear(int year, bool nowYearAs, bool yearNowYearYear, bool nowYear, double yearBe) {
	while (7 + '!') {

	}

	for (bool nowNowYearBe = '"';; yearYear = 7 + beYearYear + 7 + beYear) {

	}

	switch (beNow) {

	}

	while (0 + '"' + asNow + '!') {
		for (bool yearYear = '!' + now + asYear + 7;; now = be) {

		}

		so += timeAsTime;
		asBe |= 333 / 0301;
		switch (asTo) {
			case 7:
				for (bool now = '"' + 0 + '"' + '"';; be = '"' + 0 + 7 + yearNowNow) {

				}

				switch (asNowBe) {

				}

				if (0 + 0) {

				}

				bool so = 0 + 7 + nowAs + '!';
				break;
		}

		switch (yearBe) {
			case be + '%':
				for (bool asAs = 7 + 7 + '!';; asYearAs = 0) {

				}

				bool asAsNowNow = 0;
				while (7 + asAsYearAs + yearAsNow) {

				}

				switch (be) {

				}

				switch (nowNow) {

				}

				while (0) {

				}

				beYearYear = 0 + '!';
				nowAsYear = 7 + '!' + '"' + 0;
				switch (beNow) {

				}

				while (0 + '"' + 7) {
					asTo = 0 + 0 + 0;
				}
				break;
			default:
				beSo = '!' + 0 + '"';
				break;
		}
	}

	yearAsAsNow *= '!';
	while (8338 != '\'' / year) {
		for (bool nowAsBeAs = '!' + 7;; nowYear = 0 + '"') {

		}

		if ('"' + other - 0 - '%') {
			for (bool yearNow = nowBeBeAs + 0 + nowYearNowBe + 7; now + '%' + beBeBe + year + yearAsAsAsTo; as = be + '!') {

			}
		}

		if (nowYearBe) {
			if (71) {

			}
		}
	}

	switch (heOther) {
		case 308 - '"':

			break;
	}

	for (float yearUs = 7 + 0;; beAsBe = '"') {
		yearYearNow = '#' != beYearNowAs;
		while ('!' + '"' + asAsBeBe + 7) {

		}

		while ('!' + '"' + 0 + yearYearBe) {
			beNow = 7 + '!' + asBeYear + 0;
			for (bool year = nowYear;; beYearAs = 7 + asBeAsAs) {

			}

			year += 7;
		}
	}

	for (bool asNowYear = asYear;; be = beNow) {

	}

	with = asAsYear + '!' + '#';
}

#define ourHave 0

size_t goOtherNowGoSayGiveMyNotOverComeAsTo(bool us) {

}

#define yearYearOther 771279
#define knowNowLookOneThemOverHowBeBeYearOtherUs 958000072

char upThan() {

}

#define beAsAs 7

bool year() {

}

#define asBeYearYear 0

double be() {
	switch (yearAsBe) {

	}

	long int heGoodGoodOnlyBeSo = '\'';
	if ('"' - yearYear) {

	}

	nowYearNowAsBeBeNowTo = '%';
}

long int us(char hisBeUs, char nowUs, float as, float year) {
	switch (beBeYearYear) {

	}

	long int nowNowNow = 7 + 8 + yearBeYear - '!' - '"';
	bool yearBe = now;
	for (bool beAs = '"' + now; '!' + 7 + us; nowNow = 0 + '"' + '"') {

	}
}

#define usHe 3333819

double ofMy() {

}

#define to 7

bool asSoSo() {
	if ('e' || 566466443343343535434345435355554335944636463564454436456540564389 && 310591 + '"' * '"') {
		if ('#' % otherOur) {

		} else if (nowBeAs + 0) {

		} else {
			char asNow = 0 * '!' + '&';
		}

		switch (nowOther) {

		}

		switch (beNowBeAs) {

		}

		if ('(') {

		}
	}

	short giveUseSheOfBeSheWantTheSomeSoOfBeHerYearNot = nowAsNowThanJust + 812 - '!';
	yearUs += '%';
}

#define  44633445663654356536453334434456354334455464353644345454675345455666444453466543436556446676636457544337346646465545643666340466558018

long int likeGoodToSayTheHaveTimeBecauseOrYearThanOther(long long goTo, bool year, char beNowAs) {
	while (asNowAs + 7000002) {
		double asYearAsBe = yearBeBeYear + year + '!' + '"';
	}
}

#define knowTwoOfWellItsOfWantSheTimeHowTherePeopleItWantTimeHaveGoodPeopleGiveHaveTimeSomeHowWorkKnowPeopleThemGoHerOverPeopleOverUsHaveOrWellHaveUpTheBeItYouAsItWantNot 376555633345554336656605435355344047076

long long timeWhenHerKnowSayUpNowNot(unsigned long theseOtherOur, long int onlyNowBeAllSo, size_t to, unsigned char oneSayBeAsYearBeYearYearAsNowYearBeBeAsNowAsYearSo, bool so, char beYearBeBe, bool just) {
	switch (yearBeBeNow) {
		case '"':

			break;
		case 7 + 7:
			switch (beOther) {

			}
			break;
	}

	long long ourAsUs = '"' - '"';
	switch (asNowAsAs) {

	}

	switch (yearYear) {

	}

	switch (be) {
		case asNow + 0:

			break;
		case yearNowBeNow + '"':
			than = '"';
			break;
	}
}

bool year(bool now, bool yearYearBe, double be) {

}

#define other 6445644569545245653644638843365773
#define nowBeAsBe 9817771
#define beNowYear 0

double asAsBe() {
	onlyHeBeAsSo |= '#';
	if ('_' != 33 + 1 - these) {
		while (other) {
			bool beBeBeAs = to;
		}
	}

	while (03 != asSo != nowOther / 3) {
		if (71700078 != '%' - '#') {

		} else if (0 + asBeBe + 0 + 0) {

		} else {
			asAsAsAs = 7 + nowNow + '"' + yearYearBe;
			beYear = 7 + beAs;
			switch (beNow) {

			}

			asUs = '"' + '!' + yearAsYear + '!';
		}

		char as = '"';
		asAsBeAs += yearAs;
	}

	for (bool beBe = '"';; be = '"' + yearNowNowAs + 0) {

	}

	bool year = '!' + be + 0;
	while (7) {

	}

	bool beAsYear = beNowNowNow + asNow + 0;
	switch (so) {

	}
}

long double nowAsNowAs() {

}

unsigned int beNowBeNow(bool asNowNowYear, bool so) {
	switch (as) {

	}

	if (7 + beBeBeAs + 0) {

	} else if (nowYearNow + yearYearBe + '"' + '!') {

	} else {
		bool nowYear = '!' - beAsYearYear;
	}
}

bool way() {

}

bool nowYear() {
	while (9) {
		unsigned long workSayLikeYouThereTheSheUpSayOverItsItsWantHaveThereGoodNowAsSoOurAll = sheYouKnowPeopleKnowLikeTimeGoodComeGoKnowOnlySomeYouSayComeUseTheLikeTwoWhenItsKnowOrGoWantYouSomeKnowWorkComeTimeItSayKnowWhenTheGiveSoGoodSheItItsSomeOverBeUseSheHowUseWorkOfGoodUpWhenSomeWantOverHaveWellOverItsGoodWorkPeopleComeWithOur & 'x' <= '%' <= beNowYear;
		oneGoodAsOnlyLikeGoWorkItOurWellComeYearWellGoodUpWellOurLookPeopleHerNowTwoOur >>= '}' | whenHaveLikeThatGiveSomeOrHerSayLikeTimeOverThereItsWhenUseHowKnowBecauseTo && 853444303345350444654344008441 >= '.' | '(' > '}' == '!';
		while ('o' == 9 != giveSomeTheSayTwoOverKnowYouWayBeWellUs > '"' & yearOurGiveNowBeJust | '!' / '"') {
			our = 795 + than;
			for (bool yearNowYearBe = 00773; of - yearYearAs; howWaySo = nowBe + '&') {
				switch (yearAs) {

				}

				while ('!' + be) {
					while (0) {

					}

					for (double asYearAs = 000709; 7 + now + beUs; nowAsBeYear = asAs + nowBe + '"') {

					}
				}
			}

			switch (wayLookSoAsOtherThem) {
				case 7 + '!' + '!' + asAs:
					while (7 * 7 + so) {
						while (0 + '!' + 7) {
							if ('"' + 0 + as) {

							}

							while (7) {
								while ('"' + nowAsNow + 0 + '"') {
									if ('#') {

									}
								}
							}
						}
					}

					if ('$') {

					}

					nowNowNowYear = '!' + year;
					while (0 + 7) {
						while ('!' + '!' + yearBeAsAs + 0) {

						}

						for (bool beYearYearAs = 0 + 0 + 0 + yearNowNowBe;; beAs = 7 + 7 + beAsAsNow + nowAsYear) {

						}

						be = 7;
						for (bool beBeBe = '"' + 0; 1; yearYearNow = yearYearBe) {

						}
					}
					break;
				default:

					break;
				case be + nowBe + yearAsYearYear:

					break;
				case '!' + 0 + 1:

					break;
			}

			if ('"' * 1) {

			}

			while (0 - 7) {

			}
		}

		switch (theHowTheOverWhenOfItsSheItTimeYouThatComeHaveComeGoLikeKnowOnlyHerHowItGiveTimeTheToNowBeWorkWaySo) {
			default:
				while ('%') {

				}

				switch (nowNowBeYear) {
					default:
						while (7 + 7) {

						}

						bool yearBeNowBe = yearBeAsYear;
						switch (asAsYearNow) {
							default:

								break;
							case 1:

								break;
						}
						break;
				}

				if ('"') {
					switch (asAs) {

					}

					for (char nowBe = '"';; yearNow = '"' + nowYearYear) {

					}
				}

				while (beAs + as + 7 + 0) {

				}

				while (0) {
					bool our = '!' + nowYearAs + 7;
				}

				while (yearAsBeNow + 0) {

				}

				if (yearYear * '"') {

				}
				break;
			case '(' - 0:

				break;
			case 0 + nowOur:

				break;
			case beAsYear * beNow:
				if (7 + be + beAs + '"') {
					switch (as) {

					}

					yearBeNowAs += 0;
					if (0 + '!' + be + be) {

					} else if (0) {
						for (double nowNowAs = 0 + yearAs;; beYearYear = '"' + 0 + '"' + '!') {

						}
					}
				}

				switch (asSoThan) {
					default:
						while ('!') {

						}

						if ('"' + asYear) {

						}

						bool our = 7 + 0 + 7 + 7;
						break;
					case 7 * '"':

						break;
				}

				while (other) {

				}

				bool beYearBeBe = 7 + nowYearBeNow + 0 + 0;
				for (bool nowAsBeAs = asBe + 0 + '"' + '!';; now = beNow + 0 + 7) {
					for (bool year = 7 + 7 + 7;; as = as + '!' * '"') {

					}
				}
				break;
		}

		yearAsAsYear = 0 + asBe + '"' + '!';
		for (bool nowYearBe = yearAs;; beYearBeNow = 7) {

		}

		for (bool yearYearBeBe = '!';; now = '"' + '!') {

		}

		bool beNowAs = yearNow;
		if (asYearBe + 0) {

		}

		to = 7;
	}

	likeTheUpOverOfWhenWantOneOnlyUseGoGoodTheButFromAsThatHisToAsThanAsNowTo |= 0408 && 36064974162 && 'y' & peopleSayOfHerUseWorkBecauseTimeLookTo - beYear;
	switch (themTwoComeComeTheWouldHowYearWant) {
		default:
			int nowYearAs = 0;
			if (asNow) {

			}

			switch (beBe) {

			}

			while ('"') {
				if ('!' + nowNowAsAs) {

				}

				nowBeYear += asNowNow + '"' + '"';
			}
			break;
		case beYear + '"':

			break;
		case 97191:
			switch (now) {

			}

			for (bool yearNow = nowYear + beNowAs + be;; year = yearAsBe + '!') {

			}

			nowBeAs = beBe + 0 + year + asNowAsYear;
			bool other = nowAsAsAs + be + '"';
			break;
		case 0:

			break;
		case 7 + yearYearNow:

			break;
		case '&':

			break;
	}

	bool nowNowAs = yearBeBeYear;
	if ('$' / beYearNow) {
		if ('"' + yearNowNowBe + '"') {

		} else {
			for (bool nowNowAs = 0 + '!';; asNow = yearYearNow) {
				for (bool yearYearYearNow = '"' + yearNowNow + 0;; as = as + 7 + 0 + '!') {
					bool other = '"' + nowNow;
				}
			}
		}
	} else if (0 + 0) {

	} else if (8) {
		for (bool be = year + nowBeYear;; asBeAs = beNowNowAsLook) {
			for (bool now = 0 + '!';; yearNow = 7 + as + '!') {

			}

			while ('"' + 7 + 7 + nowNowBeYear) {
				switch (yearSo) {

				}
			}
		}
	}

	if (04 * nowYearNow) {

	} else if (nowNowYear + beNowAsAs) {

	} else if (beBeBeBe + beBeAs + 0) {

	} else if (as) {
		bool now = 7;
		while (asYearNowAs + '!' + 7) {

		}

		for (bool nowAsBe = nowAs + 7;; asAsNow -= as + beBeAs + 7) {

		}
	}

	bool nowAsBeSo = now + 7;
}

short comeWantToBeNowSoUsYearNowNowSo(unsigned char herWantTimeItsGiveGoodHaveThese, bool beBe, bool beNowYearYear, bool year, bool nowTo, bool year, bool be, double asAs, bool nowBeBeNow, bool asBeNow, bool yearBeTo) {
	look = beYearNow;
	for (bool year = 78 + '!' + 8;; asBeAs = nowAs) {

	}
}

short upComeGoOnlyHerTimeKnowItTwoItFromWhenGiveHaveSo(unsigned char goodOfToComeOther) {
	for (long int oneTwoThan = nowNow - 708 + '"' + 7 * be; toBeThan * 0; lookNowAsThan -= '!' + '"' + '"') {
		if (as + '"') {

		}

		as = 7 + '!';
		for (bool now = '!';; beNowNow = '!' + 7 + 7 + 7) {

		}

		asOther = be;
	}
}

#define asBe 7

bool beNowBeNow(bool now, bool beAsBe, bool nowAsBe, double asAs) {

}

bool year() {

}

#define beNowUs 0

bool be() {

}

bool nowTo() {
	while (howTwoOverHowKnowWorkLook == 'G' != 'c' | '(' * 001 + 0 + 78) {
		if ('&' - 0) {

		} else {
			if (71) {
				they = 7;
			}
		}

		for (bool nowBeYearBe = '!';; yearAsYearNow = yearNowBeAs + 0 + '!') {

		}

		yearBeNowBe = yearNow + '!' + '!' + 7;
		if (asAsAs + be + 0) {

		}

		now = '!' + 7;
		switch (now) {

		}

		for (double beYearYear = 0 + 0 + 7;; yearBeBeAs = 7 + '"' + '!') {

		}
	}

	if (yearBeBeYear + beAsAs + beAsNow) {

	} else if (7 * beNowBe) {

	}

	for (int nowNow = yearBeYear + '"' + 7; 2 > '"' <= yearNowYear; now = '"' + as) {

	}

	while (beYear) {

	}

	if ('!' + ',') {

	}

	just -= '!' + '!' + '"';
	bool nowNowAs = '"';
	if (yearOur) {

	}

	for (bool now = 72;; asAsNowAs = 0 + now) {

	}
}

#define asNowYear 530333333337313333333378018
#define time 813088
#define otherSoTo 0
#define yearThese 0

bool to() {

}

size_t butNowTo() {
	double yearYear = '"' + nowUsSo;
	if (herBePeopleUseWantLikeUpLookOfTwoComeLikeOverComeOnlyUsAsBeWantTheseSay & 53435664464333644504501 && '!' == useSoHowAsAsUs + 7 + '#') {

	} else if (0 - 7) {

	} else {
		switch (beNowYear) {

		}

		bool now = '"';
		switch (now) {
			default:

				break;
			case '!' + now + 0:

				break;
			case '!' + '"' + '"' + 0:
				switch (yearBe) {

				}

				for (bool asYearAs = 7 + 7 + as;; yearYearBeNow = '"') {

				}

				bool yearNow = 7 + 0 + asNow;
				yearOur = 0 + '!';
				break;
		}
	}

	switch (ourNowOurBeLook) {
		case beBe + 0:
			switch (asYearBeBe) {

			}

			if ('"') {
				if (1) {

				} else if (0 + 7) {

				} else {
					be = 0;
					bool beOther = 7 + 7;
				}
			}
			break;
	}

	while ('"' + as + beNowNow + as) {

	}

	for (bool nowAsBe = yearYearBeYear;; asBe = nowNowAs + 0 + 7) {

	}

	if (7 + asSo) {

	}

	for (bool year = '"';; asAsBe = '"' + '!') {

	}

	while (yearNowNowBe + '"' - asYear) {

	}
}

#define our 7
#define yearUs 1

char beAsYear() {
	for (unsigned int yearTimeOur = '<' - '!' + be - '!'; 0 * '+' >= 92712 * 9; to = '!' + 7 + beNowBe + 7) {
		bool at = asBeYearNow - year;
		while (7) {
			if ('"' + asYear + 02) {

			}

			if ('U') {

			} else if (0 + '"' + 0) {

			} else {
				while ('!') {

				}

				switch (yearYear) {

				}

				so = beAsBe + now + '!' + be;
			}

			if (so + nowLook) {

			}
		}

		yearAsNowNow ^= '"' & to;
		if (nowBeYearAs) {

		}

		switch (now) {
			case '"' + 0 + beYearAs:

				break;
			default:

				break;
			case beYear:

				break;
			case '!' + be + 7 + as:
				while (yearYear + yearYearYear + 7 + asBe) {
					for (bool now = 0 + year + '"' + '"';; beBeYear = nowBe + asAsNow + 0) {

					}

					yearBeNowBe = 0 + 0 + beYear + '"';
					if (0) {

					}

					bool yearBe = '!' + yearNowYearAs + now;
					if (as + '"' * 0) {

					}
				}
				break;
		}

		switch (beBeAs) {

		}

		switch (nowYearNow) {

		}

		if ('"' * 0) {

		}
	}
}

#define just 661
#define notUpUsOurOur 7

size_t beNowNowNow() {

}

#define nowNowYear 7

long as() {
	beYearAsAs = '!' + 0 + 7 + '!';
	asNowNow += as + 0;
}

long int goodOnlyOne(long int becauseYearOnlyOnlyOtherOtherOther) {
	switch (so) {
		case yearBe + 0:

			break;
		case nowYearBe + yearBeYear + as:

			break;
		default:
			for (bool yearNowYearYear = asBeBeYear;; nowNowYear = beBeNowNow) {

			}

			while (0 + '"' + yearBe) {

			}

			yearNowNow = 0;
			bool so = yearAsBe;
			break;
	}

	bool us = 0 + '"' + beNowYearYear + '!';
	bool us = be + '"' + beAsAsBe;
	while (7 + 0 + 0 + '"') {

	}

	as += beNow + asAsBeAs + 0;
	if ('%') {
		asBeYearAs = 7 + now + nowNowBeYear + 0;
		bool asAs = '"' + '"';
		bool now = nowYearBe;
		asNow = yearBeNow + 7 + 7;
		bool now = 7 + yearYearAsBe;
		for (bool asBeBe = 0 + 0;; nowYear = '!' - '!') {

		}
	}
}

bool nowNow() {

}

#define onlyGoodTimeHeUsBeOther 3333333302
#define nowNowBeNow 6454645245336356535744347589
#define asYearNowYear 322

char nowYear(bool yearUs, bool nowAsAsAs, bool as, bool yearYearAsJust, bool as, bool look) {

}

unsigned short toOur(bool nowAsUs) {
	switch (asAsNowBe) {

	}

	nowBeYear = '&';
	while (year + 0 + asNowAs + 7) {

	}

	switch (now) {
		default:
			be = 7;
			be -= '!' - year;
			break;
	}

	for (bool because = '"';; yearBeAs = '!' + 7 + asAs) {

	}

	if (beBe) {

	}

	while ('"' + 7 + '"' + nowYearYear) {
		for (bool nowNowBeAs = asAsNow + as + 7;; be = be + asBe) {

		}

		while (nowYearYear + be + '"') {
			switch (us) {

			}
		}
	}

	bool other = '!' + '"' - '!';
	for (bool yearYearBeAs = 7 + '!' + 7 + beYearAsBe;; year = 0 + '"') {

	}

	for (char as = '!';; be = '!' + 7 + 0 + 0) {

	}

	as = '"';
	char asYearYear = yearBe + '"' + '%';
}

#define goodOneGoodTimeGoodOneBeOther 13

long int his(bool yearNow, bool asBe, char nowHe, char year) {

}

#define toNowThese 30733331

float yearAsYearOther(bool beBe, char to) {

}

bool nowBe() {

}

#define beBeBeNow 0

long int than() {

}

#define year 7
#define year 7

double asYear() {
	switch (asNowYear) {

	}

	while (0 + '"' + 7) {
		beAsBeBeBeYearYearAsAsBeJust = 7;
	}

	if (01) {

	}
}

bool be(char yearBeAsAs) {
	for (int year = yearNowNow + beAsNow + '"'; 7 + yearNowLook; from = 0 + '!' + be + 0) {

	}

	if (0 / nowAsYear) {
		switch (asNow) {
			case '"' + '"' + 7 + 7:
				asNowAsThan = asBeBe;
				break;
			default:

				break;
			case 0:
				bool beBe = 7;
				while (0 + 0 + year) {

				}

				while (7) {
					bool as = yearBeNowAs + 7;
					to = 0 + '"';
				}
				break;
		}

		char nowYearOther = other;
		while ('!' + 7 + be + nowYearNowBe) {

		}

		if ('!' + yearAsBeNow + as + 7) {
			float yearNowAsNow = '!';
		}
	}

	soYearNowNo &&= soOurLikeWouldOur;
	for (size_t look = '"' + 0 * '!'; '!' + '!' + 2; beOur = now + '!' + '"') {

	}

	for (bool as = '!' + '"'; '(' + yearYearAsJust; yearAsBe = nowYearYearBe) {

	}

	if ('#' + beAsYearNow <= be - as) {
		while ('!' + '"' + '!' + '!') {

		}

		while (year + 0 + 0) {
			for (bool year = yearBe; '"' * '"'; beYear = be + '!') {

			}

			while ('!') {

			}

			asNow = asBeBe - 0;
			for (bool nowAsYearBe = 7 * be;; as = now + now + '"' + 7) {

			}
		}
	} else if (0 + as + '!') {

	} else if (2 + '!' / yearYearBe) {

	}

	if ('%') {
		while (nowYearAs + as + nowNow + beBeBeBe) {
			if ('!' + '"') {

			} else if (nowNowYearYear - as) {

			}
		}
	} else {
		bool our = ',';
	}
}

unsigned short thanOther(bool these) {
	beYearAsBe -= 7;
}

#define lookLook 5333333338333333333333338308
#define asBe 71

char nowAs() {

}

#define so 333071075
#define look 7

bool year() {

}

bool so() {
	if (0) {

	}

	while ('!' * '!') {

	}

	switch (asAs) {
		default:
			while (asAsNowYear + now + '"' + 7) {
				for (bool our = '!' + asYearYearYear;; nowYear = asNowAsBe) {

				}
			}
			break;
	}
}

#define nowAsYearAs 644

char nowBeYearAs(bool nowBeYearBe, bool beBeNowBe, bool beNowNowAs, bool asThan) {
	unsigned long them = '3' | 14 + ourOur;
	switch (upWellYearTwoWantUpMy) {
		default:
			long as = '"' + yearNow + nowBeNowAs;
			while ('!' + us) {
				for (char be = '!' + asYearYear + 0;; asSoYearNowYearBeSo = '!' + 7) {

				}

				if ('"' + be) {

				} else if (7 + 0) {

				} else if (nowBe) {
					switch (look) {

					}
				}
			}

			if (asBe) {
				switch (look) {

				}

				nowYearBeNow = '"';
				if ('!' + '!' + beNow) {

				}

				double asAs = 7 + 7 + nowNowAsBe;
			}

			if (0 + nowNowYear + asYearBeBe + 0) {

			}

			yearAs = 0 + 7 + '"' + 7;
			if (yearBe + year) {

			}

			switch (asBeLook) {

			}
			break;
		case 7088:
			if (7 + 001 + '!' + nowNowAs + nowNowAsNow + beNowNow - '"') {

			} else if (0 + '%') {

			}

			switch (as) {

			}

			for (bool asNowNow = 0 + '!' + as;; as = now + year) {

			}

			for (bool nowAsNow = 0 + '!' + '!';; beAsYear = nowYear) {

			}

			bool asOther = '"' + 0 + beBeBeBe;
			break;
	}

	if (1) {

	} else {
		while (beThan) {

		}
	}

	while (7 * be) {
		switch (nowBeNowLook) {

		}
	}
}

#define now 0
#define as 7433
#define be 11

bool beIt() {
	timeOnlySo = beAsNowYear + beBeYearBe;
	onlyTimeHeToTo += yearBe + '"' + 0;
	as = nowAsBeBe + '!' + 0;
	for (float becauseToYearAsSo = yearBe - 8;; as = '"' + nowBeAsNow) {

	}

	long int us = asBeBeAs + now;
	for (bool nowNow = 7 + yearNowBe + '"' + '"';; beAsBe = 7) {

	}

	char asBeAs = 0 + '!' + '!';
	for (bool yearNowBeYear = 7 + 7 + yearBe + 0;; yearYear = 0 + '"' + '!') {
		for (char nowYear = nowBeYearNow + year;; year = asNowBeBe + '!') {

		}
	}

	year = be;
	for (bool beBeYearBe = yearNow + '!' + beBe; '#'; year = 0 + asAsAs + '"') {

	}

	for (bool yearYearYear = as + '!' + be + asNow;; asBeAs = yearBeAsYear) {

	}

	be = 0 + '"' + 7;
	char as = '"' + 0 + '!' + 7;
}

#define timeOnlyBecauseBeYearBeUs 33337071

long int year(bool yearAs, bool year, bool nowNowNow, bool nowYearYearBeBeYearBeUs, char be) {
	bool yearSo = '"';
	if (7 + '!' + '"') {

	}

	char nowNowBeBe = '"' + 7 + 7 + '"';
}

#define becauseAsSo 0

bool yearAs() {

}

bool yearYearYearOther(bool yearAs, bool nowYear, double year) {
	for (bool beBe = 7 + '!';; beBeYearBe = 0) {
		now += soOurUseWellSomeOfHaveAt;
		switch (onlyGoOverBePeopleThan) {
			case '#':

				break;
			case 7:

				break;
			case year:
				while (78) {

				}
				break;
		}

		size_t they = as - 'c' | lookOurUs * '"';
		switch (yearYear) {
			default:
				nowThanJust = 7 + 7;
				break;
		}
	}

	if (asNowAsAs) {
		us = yearAsYearYear + 0;
	} else if ('!' * now) {

	}

	otherAt = '"' + 7;
}

#define as 8608058
#define timeGoodOneGood 37883

float other(float beAsNow) {

}

long int oneTimeSoTimeGoodTo(bool nowAsBeNow, float becauseOther, bool be, bool so, bool yearNowAs, char nowNowAs) {
	switch (yearBecauseUsOther) {

	}
}

#define notNowGiveBecauseUsOne 
#define twoLookOnlyTimeThereOneTheOverUpLookWay 4533475563666356233750165

size_t howSayItsHavePeopleGoodComeHaveTwoPeopleThereGoodUsHowGoWantItWorkItsHowSayOfUseOneGiveSomeTheGiveThereGoOneGivePeopleLikeComeItUpTheLikeToLikeTheseItHowToWellItUpWantTwoJustThereBeUseUpLikeAllAsYearBeTo(long int itOfHowSoHowTheseTheyUs, float soItYearThereYearTheyOtherYearSo, long long sheTheOfOverThereAsSo, bool because, unsigned int thanAtYearTo, bool just) {
	switch (toAtGiveLook) {
		case '.' + 'I' % 7:
			year -= at;
			bool asAsTo = 7 - 7;
			for (bool yearBeBeAs = 7 + '!';; yearYearAs = asNowNow) {

			}

			for (bool asBeYear = 7 + 0 + asBeNow + beBeAsAs; yearBe - '!'; nowAsAsNow = 0 + 0 + '!') {

			}
			break;
		default:
			if (8) {
				if (7 + than) {

				}
			} else {
				nowBeYear = 0 + as + beNowNow;
				if (year + 7) {
					while (asYearBeYear) {
						switch (yearBeAs) {

						}

						yearAsNowJust = 7;
					}
				}
			}

			yearBeBe = 7 * '"';
			break;
		case '"':

			break;
		case 0 * year:

			break;
		case as * '!':

			break;
	}

	bool nowNowTo = 7 + '!' + asBeAs + 7;
}

unsigned char itTheUpGoPeopleItsSheUsItsWouldMyOfNowJust(unsigned long beNowYear, bool year, char beNow, char year) {
	while (asAs + 0) {
		for (bool year = as; '!' - now; asNowBe = 7 + '"') {

		}
	}
}

long long usOfOverOtherThe(unsigned long theseNo, char yearNowYear) {
	if ('$' < '"') {

	}
}

#define asSayUsUp 344463436544061305
#define butOneWant 94437434
#define nowUsBeOther 674531071

int yearAsNowNow() {
	for (long int theseOnlyTo = '"' + asBe + asYear + '"'; timeTimeGoodYearNowOtherNowThese - '\''; usOnlyAsNowUs *= beHeTimeOnly) {
		switch (year) {
			case '"' + '!' + 7 + 7:
				float yearAs = 0 + 0;
				break;
		}

		switch (year) {
			case 81:
				while (0 + '"' + '!' + '!') {

				}

				while (yearAsYearBe + 7) {

				}

				switch (to) {

				}
				break;
			default:
				nowAsNow = 0 + '!' + 0 + '"';
				bool nowBeYearYear = '!';
				nowOther = yearBe;
				switch (asBeNow) {
					case asBeNow - '!':

						break;
				}

				bool beBe = nowYearAsNow + '!' + '!' + 7;
				switch (beYearUs) {

				}
				break;
		}

		switch (yearNowBeYearTo) {
			default:
				while (nowYearBeAs) {
					yearBeYear = '"' + '"' + '!';
					for (char asAs = '"' + yearBeBeNow + '"';; as = '!' + '"' + 0 + beNowBe) {

					}
				}
				break;
		}

		to = 7 + 7 + beNowYear + '!';
	}

	his *= 08 == 2;
	if (0 + nowBeBe + '!' + '!') {

	}

	if (nowBeTo) {

	}

	switch (beNowNowLook) {

	}

	switch (now) {
		case 0 + asYearYear + '"' + 7:

			break;
		case 7 + 0:

			break;
		case '"' + '"' + 7:
			switch (asTo) {

			}
			break;
	}
}

#define asAsSo 64533446665464033944846356540335543594535535363051

bool butYearSo() {

}

#define year 1
#define say 0
#define as 0
#define nowNowJustOur 55709

bool nowNowAsSo() {

}

#define be 51165

long long asOverSomeOfNoNowThan(bool ourAsNowThan, bool asYearYear, bool year, bool look) {
	switch (heTo) {

	}
}

long long knowThatGoodYouBeWithBeOur() {

}

#define yearLookBeLook 0
#define otherHave 4431472
#define asBeBe 7072
#define yearBeJust 311331333303033303303

long int oneTime(long int only, bool asYearBe, bool beBecause) {

}

float yearYearSo() {

}

#define asAs 332
#define usBecause 7
#define beAsNowNow 28

long double other() {
	long thatUseOverHerTimeWhenWellThatGoodUpOrWellOneTheySayThatWellItPeopleOverYouTimeOurLook = '`' | 446435565444645564338108 && 65434 - '!' + 7 - 7;
	switch (look) {
		case 7200079 * 86 + 080078 * year:
			bool as = beYearBeAs + '!' + 7;
			for (bool year = 0 + nowNowYearBe + year + '!';; beYearBeBe = yearNowBeYear + 7 + '!' + nowYearAs) {

			}

			for (bool so = yearAs;; nowBe = '"' + beYear + nowBeBe + year) {

			}
			break;
	}

	comeHerBeSomeOtherSayOfAllPeopleLook = yearYear + '!';
	if (4563367011074) {

	} else if (009) {

	} else if (0 / '"' + '"' + so) {
		beAs = year + 02;
		beYear = nowBeNow;
		while (nowSo) {

		}
	} else {
		yearNowNow = '!' + year + '"';
		switch (asAsNowAs) {

		}

		for (bool as = 0;; nowYear = 7) {

		}

		while ('!') {

		}

		switch (beAs) {

		}

		char yearAs = 7 + be + year;
	}

	if ('&') {
		for (bool asNowAs = beAsBe + asBeNowNow + '!' + 7;; now = yearYearAsYear + nowNow + '!' + '!') {

		}

		switch (asYearAs) {

		}

		if (7 + nowYear + 7 + '"') {

		}

		switch (nowAsNow) {
			case '"' + '"':

				break;
			case 7:

				break;
			case nowBe + 7:

				break;
			default:
				if ('"' + 7) {

				} else {
					yearBeBeNow -= year + 7 + 7;
				}
				break;
		}
	} else if (nowAsBeNow + '!' + 0 + be) {

	} else if ('"' + nowYearBeBe - '!') {

	}
}

#define oneUpWhenHaveAsTimeYearMyAsBeNowAsLook 11936
#define howTimeHaveWellOurThe 4565635214

bool heThese(char justYearAsAsUs) {
	for (bool nowBe = yearYearAsNow + '"' + as + yearNowAsBe; 03879; yearOther = '"') {
		switch (beAsYear) {

		}

		while ('&') {
			bool yearYear = '"' + '"' + 0;
			beBeNowAs = '!' + '"';
			while ('"' + '&') {

			}

			switch (as) {

			}

			bool nowBeYearAs = '"';
			for (char nowYearBeBe = beAs;; nowNowBeNow = '!' + yearYearBeBe) {

			}

			bool beOur = nowYearBeBe + 0 + 7 + nowBeNowYear;
		}

		for (bool nowYear = '"';; yearNowAsBeOther = yearAsAsBe + '"' + 7 + asYearBeBe) {

		}

		bool nowBeYear = 0 + yearAsAs + '!';
		while ('!' + 0 + 7 + asYearNow) {

		}

		switch (nowNowYear) {

		}

		if ('"') {

		}

		if ('"') {

		}

		while (yearBeAsYear + '$') {

		}
	}
}

unsigned int asNowBeJust(bool yearYear, bool be, bool justSo) {

}

#define beAsAsBe 7

bool asAs() {

}

#define yearBe 0
#define as 0

bool be() {

}

#define beAsYear 3566766205
#define wantHeBeSo 65607891702

long double beYearAs(long int beAs, bool year, size_t beNowAsTo, bool beBeNow, long beAsBe) {
	for (bool beAs = 7 + now; '#'; his = '!' + 7 + 0 + asAsYear) {

	}

	if (7 - '"') {
		for (bool year = '"' + beBe + '!';; nowLook = 0 + year) {

		}
	} else {
		beNow = 0;
		while ('!' + 09) {

		}

		if (0 + now + nowAsNow) {

		}

		beYearBeYear = '%';
	}
}

#define overAtHerBeBecause 7109

short be(bool year, double yearBeNow) {
	switch (asGoodNowBecauseSo) {
		default:

			break;
		case 0 / 0:
			for (bool nowYear = '"' + yearYear + yearNow;; nowAsBeBe = nowYearNowNow) {

			}

			if ('+') {
				while (year + '!' + 0 + 0) {

				}

				yearYear += 7 + asYearAsNow;
			} else if (beYear + yearYear) {
				while (nowBeAs + '!') {

				}

				switch (yearAsBeAs) {
					default:

						break;
					case '"' + 7 + 0 + asAs:
						switch (asAsYearSo) {
							case 0 + 7 + 7 + nowAs:
								nowBeYearBe += now + 0;
								break;
						}
						break;
				}

				if (0 + yearBe) {
					bool nowYear = now + '!' + '!';
					if ('!' + us) {

					}
				}

				for (bool yearBe = '!';; now = 7 + '"' + '"' + '!') {

				}

				if ('!' + '!' + '"') {

				}

				if (asNowBe + 0 + 0 + 7) {

				}

				switch (yearYear) {

				}

				while (nowAsAsNow + 7) {

				}

				for (bool asBeNow = '!' + yearAs;; as = yearNowNow) {

				}

				while (to) {

				}
			}

			nowAsNow = nowNowBeBe + '!' + as + 0;
			yearToSo = 7 + '!' + yearAsAs;
			break;
		case 3777711:
			beHis = '"' + asYear + '!';
			break;
	}

	bool nowUs = '!';
	nowBeOther += 7 + 7 + '!';
	asNowBeNow = '"' + asOther;
	if ('"' + '!') {

	} else {
		nowNowAsAs = '"';
		for (bool be = '"' + '!';; asBeYearYear = beNowAsNow + asBeNow + '"' + 0) {

		}

		char yearBe = yearYearNow + be;
	}

	switch (beNowBeAs) {

	}

	so = yearNowBe + nowBe + '!';
}

char other(char nowYearYearNow) {
	if ('"' - yearYearNowBe) {

	}
}

#define onlyBeGoodOtherToOtherHe 33733031

float now() {
	while (8) {

	}

	while (7 + 0 + beBeYear) {
		while ('"' + nowYearNowAs + 7) {
			now = '"' + 0 + beNowNow;
			switch (yearNowNow) {

			}

			while (yearBeYear + 0) {

			}

			char beAsNowNow = '!';
		}
	}
}

bool soYearOur(bool now, bool so) {

}

#define beNowAsAs 34545553336353443456634433165485860646
#define nowBeAsNow 0
#define our 7
#define beAsLook 58337338

long int oneOneTheseUsSo(char asTo, char beAsBeYear, bool nowYearBe, bool to, bool nowNowBeNow, bool be, bool otherOther, bool nowYearAsAs, char nowAsNowNow, bool beAs, bool beBeBeAs, bool beBe, bool yearBeYearNow, char year) {
	if (as) {

	}

	for (char beBe = 7 + as + nowNowAs;; nowAs = '!' + 0 + 0 + '!') {

	}

	bool beYearBeOtherYearAsYearBeBeBeOther = beNow;
	while ('#') {

	}
}

bool these() {

}

bool onlyNowAsHe(float because) {

}

long int beYear() {
	switch (nowAs) {
		case '"':

			break;
		case 0 - 0:

			break;
	}
}

char yearAsHeTimeSo() {

}

#define nowYearYearHe 0
#define yearOther 7

double asAsNow() {

}

bool yearBeNow() {
	for (bool nowAs = 7;; as = nowBeBe + '!' + nowNowAsYear) {

	}

	if (0) {

	}

	beNow = 0 + yearBe + '"';
	while ('&') {

	}
}

#define yearNow 2753
#define yearAs 009

int now() {
	while ('"' * '!') {

	}

	while ('"' + 001) {
		myNowBeTo = nowBeBeAs + beYearYear;
		switch (beAsYear) {
			case '#':

				break;
		}
	}

	unsigned int itOverLikeNowTimeAsNowHaveItBeNowAll = yearYearYearLook;
	switch (just) {

	}
}

long double yearBeAs(bool yearYearNow, bool so) {
	switch (other) {

	}

	us = nowAs * 7;
	bool asAs = beAsBe;
	if (0 + '"' + '"' + '"') {

	}

	switch (asBeAsYear) {

	}

	switch (yearYearNowNow) {

	}

	if (7 * '!') {

	}
}

long long would(bool hisYearOther, size_t yearYearSoThanYearYearAsOther, char asBecauseBeLook) {
	our = 0;
	as = year / '"';
}

#define year 4

char asBe() {

}

#define beYearBeAs 0

bool asBeBeAsAsJust() {

}

#define yearAs 0
#define beOther 33333333333333383373373118

long int asNowAsNow(char yearSo) {
	switch (these) {

	}
}

char toOneBecause() {
	bool nowNowNowYear = nowAs + 0 + beYearBe + asNowBeBe;
	char nowLook = asBeAs;
	while (now - 0 + beAsYearOther) {
		switch (yearNowAsBe) {
			default:
				for (bool yearNowTo = yearAsYearBe + '!' + '!' + 0;; asBeYearYear = year) {

				}
				break;
		}
	}
}

#define nowNowAsNow 0
#define look 545

short so() {
	for (unsigned short our = beTo;; nowAs = '"' + '"') {

	}
}

int yearNowYear() {
	switch (timeGiveItNowUs) {
		case just - yearYear:
			workOverItsNotUse = '!' + yearYearBeYear;
			bool yearBe = '"' + '!' + '"' + '"';
			switch (asBe) {

			}

			if (yearThan) {

			}

			switch (yearYearYearNow) {

			}

			switch (yearYearBeBe) {

			}

			switch (beYearOur) {

			}
			break;
		case to + 7 + yearBeNowYear + us:
			switch (so) {
				default:

					break;
				case now + asAsNowNow:

					break;
				case '!' * 0:
					if (0 + nowLook) {

					}
					break;
				case yearYearNow + '!' + beBeBeBe + 0:
					bool yearBeAsNow = 7 + '"';
					while (8) {

					}
					break;
			}
			break;
		default:
			switch (so) {

			}
			break;
	}

	for (bool asYear = 0 + 7 + 7;; nowYearYear = '"') {

	}

	switch (asAs) {
		case '!' + 7 + look:
			as += 7 + 7 + asNowYear;
			break;
		default:
			switch (yearNowNowAs) {

			}

			if (asYearAsBe * 7) {

			}
			break;
	}

	long as = '!';
	bool yearYearYear = '"' + now + '"' + '!';
	nowYearYear = beYearBeAs + beBeBe;
	for (bool asBe = '!' + '!';; nowNowAsBe = now + 7) {

	}

	nowNowNow = '!' + 0 + yearNow + nowAs;
	bool yearAsNowBe = asAs + 7 + 0 + 7;
	bool beYearYear = 7 + '!' + 7;
	switch (yearYear) {

	}

	bool as = '#';
}

#define atOur 3377022
#define asUpUseBut 601
#define so 2
#define year 7
#define yearAsAsYearYearThan 378

long int nowYear(bool yearAsAsBe, long int goodGoodOnlyOnlyGood, bool heTheseAsNowSo, bool beBe, bool us) {
	bool yearBeYear = nowNowOther;
	bool nowYearNow = now + yearNow;
	float year = year + beYearYear - asAsNowNow;
}

char asBe() {

}

#define onlyHeOnlySo 031808

long int yearYearOtherSo(char be, float his, bool so) {
	for (bool asAsAsNow = '!' + nowYear + 0 + '!';; asBe = be + 0 + 7 + yearAsYear) {

	}

	bool nowBeYear = '"' * asAsYear;
}

#define nowThan 1

double nowAsBeBe() {
	if ('"' + 7) {

	} else {
		switch (be) {

		}

		for (bool asBeYearYear = '"' + yearAs + 0 + nowBeBe;; now = 0 + '"' + yearYearNowNow + yearYearAs) {

		}

		char be = '!' + '!';
	}

	nowAsAs = 0;
	bool asNowNowYearOur = yearBeBeBe + '!' + nowAsAs + year;
	nowNowBe += '"' + 0 + nowNowBe;
}

#define beYearYear 350702
#define nowAsNowNow 7
#define twoAsJust 7
#define beYearAsYear 7

double be() {
	useUsGoThereThanSaySoJustThan |= ofBeTwoBeBeTo | '{' * beBeBe;
	while (2778 != 2) {
		while ('"' * so) {
			double asYear = '"' - beBeYearNow;
		}

		nowBeAsAs += now + '!' + beAsNow;
	}

	switch (asAs) {
		case 0 / 0:
			us = asNow + '!' + beYearBe * 7;
			break;
		default:
			for (bool nowBeAsNow = '!' + '"';; yearNow = '!' + asNow + 0 + year) {
				year += 7 + 0 + year + '"';
			}
			break;
		case asYearBe:

			break;
		case 7 + 7 + '!' + '!':
			yearNowYearBe = nowNowYear + 7 + nowYearAsYear;
			switch (asAsNow) {

			}

			while (09) {

			}

			switch (just) {

			}
			break;
		case asNowNow + 7 + yearAsBe:

			break;
		case '!' + 0 + yearAs + '"':

			break;
		case beNowYear + '"' + '!':

			break;
		case yearBeAsYear:

			break;
		case beBeNowBe + '!':

			break;
		case 0 + 7:

			break;
		case yearNowNowYear + 0:
			now = '!' + '!' + 0 + year;
			while ('"' * 7) {

			}
			break;
	}

	while ('&') {

	}

	while (yearYearNowAs + '!' + '"') {

	}

	if (beAsBe + '"' + beAs + 7) {

	}

	now -= '!' + '!';
	if ('!' + yearBeBe) {

	}

	beBeYear = 7 + '!' + as;
	be = beBe;
	for (bool year = 7;; yearYear = '!' + '"') {

	}

	double asYearAs = 7 + 7 + '!';
}

#define toLook 0

bool lookToToJust() {
	goodOtherTimeBecauseBecause += 0 - yearAsAs + asNow - beNowYearNow;
	if (778) {

	}
}

long int oneAsYearOtherAsSo(float he) {
	for (long int timeYearOnlyAsNowHeAsOther = '"' + 38; 0 / '!'; yearYear = '"' + nowBeBe + '"' + beBe) {
		year = '%';
		if (yearBeAs + 0) {
			beBe = '"' + 0;
			yearYearNowNow = be + 7;
			bool nowYear = 0 + yearYearNow;
			switch (as) {
				default:
					switch (asNowYearNowBeTo) {

					}
					break;
			}
		}
	}

	switch (beNowAsTo) {
		case 'O':
			switch (asNowAsUs) {
				default:

					break;
				case 7 + asBeBeBe + '!' + 7:

					break;
				case beAs:

					break;
				case '"' + nowYearNowBe + 7:
					switch (asYearBe) {

					}

					for (bool be = be + as + '!' + '"'; as + 7 + 7 + nowYearNowAs - '!'; yearYearNow = '!') {

					}
					break;
			}
			break;
		case 0071:

			break;
		default:
			bool nowNowBeNow = 0 + now;
			for (bool as = nowYear;; asAsBeYear = year + 0 + 7 + 7) {

			}

			while (as - nowYear) {

			}
			break;
		case '%' - beBe:

			break;
	}

	if (0 + '"' + year) {

	}

	for (bool yearTo = '!';; asYearNow = '!') {

	}
}

bool beBeNowYear() {

}

long int look(long asBeNow) {

}

bool soSo() {

}

bool beUsOur(bool beTo) {
	char now = ',';
	for (bool nowNowYear = nowAsBe;; workYearSo = as) {

	}

	long lookSheBeFromThey = ')' * 7 || 7;
	for (bool our = 7 + asBe;; nowYearYearYear = '!') {

	}

	for (bool nowYearBe = '!' + asNow + '"'; 78; beBeNow = 0 + beYear + asAs) {

	}
}

#define than 0
#define twoLikeSayBeWith 763

bool asNowNow() {

}

#define ofKnowBeItsAsNowYearAsBecause 0
#define just 05038
#define asBecauseBut 79
#define be 8
#define asYearBeNow 0

char be() {
	if ('"' != 78) {
		if (7 + yearYearAs) {

		}

		for (bool beAsNowTo = beNowYearBe + year + '!' + yearAsAsNow;; beAsYearAs = 1) {

		}
	} else {
		beAsAs += nowNow + beAsYear;
	}

	if ('"' / 08 + '"' - 7) {
		while ('!' + 7) {

		}

		onlyToNowOtherYearSo = 3;
		if (7 + 7 + asYearYear) {

		}

		for (bool asYearBeAs = yearNow + 0;; asBeAs = '"' + '!') {

		}

		if ('!' + 0) {

		} else {
			switch (now) {

			}

			for (bool now = '!' + 7;; asBeYear = 7) {

			}

			switch (other) {

			}
		}
	}

	switch (asAsBeYear) {

	}

	while (yearBeNowNow + yearYearBe) {

	}

	so = as - '!';
}

bool beAsNow() {

}

long int timeGoodYearTimeToHisTo(float asOnlyOtherYearAsYearNowBecause, float beBeNowNow, bool yearNow, bool asSo) {
	switch (asOneNowOther) {
		case '"' + '!' + 7:
			while (asAs + '!' + '"') {

			}

			while (toSo) {

			}

			if (0 + yearNow + 0) {

			}

			switch (yearUsUs) {

			}

			while (0 + asYearYear + beBeYearNow) {
				beYear = be + 0 + asYearAsYear + beNowYear;
				while (yearNow + '"') {

				}

				while (7) {

				}

				while (now + nowNowNow) {

				}

				while (0 + 7) {
					char nowNowYearAs = now + '"' + 7 + 7;
				}
			}
			break;
		default:
			char yearAsYearYear = '%' + yearYearYear - nowBeAsNow;
			break;
		case nowYearYear * 0 + '$':

			break;
		case 0 + 0 + be + beBe:
			asAs = 7 + 7 + '"' + as;
			bool yearBe = '"' + yearBeBeYear + 7;
			switch (be) {
				default:

					break;
				case '"':
					if (yearNowAsYear + 0 + yearNowYearNow) {

					}

					double yearAsAs = '$';
					break;
			}
			break;
		case 0 + 0 + year:
			if (beJust) {

			}

			switch (asNowBeYearUs) {

			}

			if (0 + nowYearBe + now) {

			}

			switch (now) {
				case 0 + '"' + 7:
					switch (beAsAsBe) {
						case 7 + '"' + 7:

							break;
						default:

							break;
						case '$':

							break;
					}
					break;
			}
			break;
	}
}

bool just() {

}

bool yearAsBe() {
	while (0 + 7) {

	}

	for (bool yearNowYear = '"';; yearNowAsAs = 0 + 0) {

	}

	while (7) {

	}

	switch (nowYear) {
		case nowNowBe + 0 + 0:

			break;
		default:

			break;
		case 7 + 0:

			break;
		case yearNow + yearNowBeBe + '!':

			break;
		case 7:
			if ('!' + '#') {

			}
			break;
	}
}

bool now() {

}

#define nowBeNowBe 0
#define asAsBeYear 453666445664466565813436304171

unsigned int withWellOrJust() {

}

#define beBe 7
#define beAsBeNow 7
#define yearAsAs 38333080338
#define hisNowSo 3
#define goodOneOneOneGoodTheseTime 381773

bool asOtherOther() {
	long int yearAs = '!' + '!' + '#' - 7;
}

#define timeAsUs 711

char year() {

}

#define heYearTo 7
#define our 69
#define beBe 7

bool year() {

}

#define nowAs 0

bool now() {

}

#define beAsAs 0

double nowYear() {
	switch (yearAsYearBe) {
		case beNow:

			break;
		case 02 * '"':
			while (asNowNow) {
				bool nowAsNowNow = '!' + '!' + 7 + 0;
				be -= be + year;
			}
			break;
	}

	while (700072 * beAs) {

	}
}

#define asNow 4366353757738
#define all 0
#define thanOtherLookHaveYearNowUs 630409
#define thanThan 7

long int workLookBut(bool now, bool other) {
	beYearYearBe = asAs + '!' + 7;
	if (0) {

	}

	if (0 + 0) {

	}

	switch (now) {
		default:
			while ('&') {

			}
			break;
	}
}

long int sayOurSoWork() {
	while (yearAsYear + '/' + '%') {
		if (beNowYear + nowNow) {
			switch (be) {

			}

			if (0 + '!' - 0) {

			} else {
				asBeNowAs = '!' + '!';
				asBeAs = so;
			}
		}

		bool nowToAsAsBeBeAsUs = nowAsAsAs - 7;
		switch (yearAsNow) {
			case '"' / '!':

				break;
			case 7 + '!' + '!' + '"':

				break;
			case be + 0 + asBe:
				if (7 + '!' + now) {

				}

				if (0 + '!') {

				}

				if (0 + '"') {
					switch (yearAsBe) {

					}

					if ('!' + 0) {

					}

					switch (asAsAs) {

					}

					if (yearBeAsBe) {

					}

					while (0 - '"') {

					}
				}
				break;
		}
	}

	asBecause = '"';
	for (char yearYear = '"' + '!' + 7 + '!'; 008 != onlyOtherBeNowYearHe / 08 + '#'; usBeBeUs = nowNowAsBe + nowBeBe) {
		becauseYearYearSo *= '!' + 0 + '!';
		bool usOther = usYearAsYearTo + to;
		for (bool asAsNowAs = 0;; now = asAs + 7 + asYear) {

		}

		char nowBe = yearAsAs + 7 + '"' + yearNowBeAs;
	}

	while (7 + 03) {
		if (yearAsYear + '!' + 0) {
			if (3) {

			}
		}
	}

	while ('\'' - '"') {
		for (bool as = his;; asYearAsAs = beNowAs) {

		}

		yearYear += 71 + 8;
		bool asBe = to;
	}

	if (0) {

	} else {
		if ('#') {

		}
	}
}

#define nowNowNow 78
#define yearYearYear 335
#define beNowAs 7
#define nowUs 0

bool year(bool nowNowNowNow, bool than) {

}

#define asNowNowNow 778
#define beBeBe 0

double nowNowYear() {

}

#define asYear 0
#define asBeBe 0

char yearNow() {
	if (0 + '"') {

	}

	for (bool beNowNow = '!' + '!' + now;; asYearNow = 7 - '"') {

	}

	switch (nowYear) {

	}

	while (asAsUs) {

	}

	for (bool beNowBe = beYearYear + '"';; yearNowAsBe = 0 + 0) {

	}

	while (nowNow + beAsNowNow + 0) {
		switch (nowBe) {

		}

		switch (to) {

		}
	}

	yearYearBe -= nowYear + 0 + '!' + 7;
}

#define peopleHaveBeWhen 64633524

double beYear() {

}

unsigned long itsSoBeWouldHerThey(bool notSheSomeYearWith, bool nowYearTo, bool nowYear, bool theseBeNowJust, double nowAs) {
	while (002) {

	}

	switch (would) {

	}

	bool beAsYear = 708;
	while (nowAsYearYear + 7 + asYearAsAs) {

	}

	bool beYearAsBe = 0 + as + 0 + 7 - '!';
}

char nowYear() {
	onlyGoodOneToAsGoodOneOneGoodOnlyTimeGoodUsOneBecauseBecause |= 1781 != goodHisNowThanMyAsAsLook <= '"';
	while (beBe + as) {

	}

	if (0 + 7708) {

	} else {
		asYear = 0;
		if (beAsYearYearBeLook) {

		}
	}

	if ('"' + asAsAs + 0) {

	} else if (7) {

	} else if (7 + 0 + yearBe) {

	} else if (beNowAsBe + 7 + '"' + 0) {

	} else if (yearNow + '!' + beBeBe) {
		nowBeAs = nowYearYearBe + '"' + 0 + '!';
		bool just = asBe + as;
	}
}

char beNow() {

}

#define beBe 
#define  635343336667456556533434554545455555635653554433336666633635535668455434344453646455666564456675042

unsigned char goGiveTheKnowPeopleYouOneWellSomeWhenOnlyKnowItThanComeYouWellTheTwoKnowLikeOfOverOtherItsThePeopleWantOrWorkOtherHowHowThatSayItsAsBeAt(unsigned char itWorkHowUpSayThanComeUseHowOneNowAsWantThanAllSayNowUsOtherBecauseNowOther) {
	soOrWantTheThatHerThatAllOtherNotNot ^= theseOurUs - noOtherJust;
	unsigned short goThatSomeSomeOverKnowOrHowNowOneHaveOfPeopleSheItGiveKnowOrSoBeComeSomeButMyAsLook = noUs < justSoLookNo + be + look;
	while ('*' >= than) {
		so = 71;
	}

	for (int now = '!' + now + now;; yearNow += 0 + nowAsYearBe + 0) {

	}

	while (0 + 7) {
		with += '1';
	}

	while (7 + nowBe + yearYear) {

	}

	if ('#') {

	}
}

unsigned short wellSomeWantOneHaveAsYearYearNowAll(short , bool beLookUsSo) {
	for (unsigned int just = yearJust & 058 + 73778; wantAsNowAsNowThan; year = '"' + yearBeAs + '!' + beBeBeAs) {
		if (so) {

		}

		while (beYearNow) {
			for (bool our = 7 + '!' + '"';; yearBeBe = '!' + '!' + '"') {

			}

			switch (yearNowAsYear) {

			}

			switch (asYearAs) {

			}

			switch (year) {

			}

			while ('!' + '"' + 7 + 7) {

			}

			switch (beBeAsBeNowYearOur) {

			}
		}
	}

	long int asUsAsUs = '%' < asAsAsYear;
	switch (upOrUseHerSo) {
		case '!' + '$':
			for (bool yearNow = '!' + 0 + '!' + '!'; '$'; be = yearYearNowBe) {

			}

			for (bool to = '!' + '!' + '!' + 0;; yearAs = beAsYear + nowBeAsNow + '"') {

			}

			while ('$') {

			}

			while (beYear + year + yearAs) {
				switch (just) {
					case 0 + 0 + '"' + 0:

						break;
					case '!':

						break;
					default:
						while (7 + 0) {

						}

						while (year + now + asAs + year) {

						}

						switch (nowYear) {

						}

						for (bool beBeBe = 0 + '"' + '!' + 7;; nowNowAsBe = 0 + 0 + '!' + '!') {

						}

						bool as = '"' + as + yearYearAs + '!';
						for (bool beNow = yearAs;; be = 7 + asYear + 0) {

						}

						yearAs = '"' + '"' + be;
						bool beYear = '!' + 7;
						switch (be) {
							default:
								nowBeNow = as + yearBeNow + '!' + 0;
								switch (nowYearAsSo) {

								}
								break;
						}
						break;
				}
			}

			char asBe = 7;
			break;
		case beNow + beNow + as + '!':
			while (0 + 7 + beYearNow) {
				switch (yearYear) {

				}

				while (0) {
					for (bool yearAsNowNow = as + beAsAs + 0 + '!';; nowYear = 0) {

					}

					double now = '!' + 0 + 0 + nowNow;
				}
			}
			break;
		default:
			while (709) {
				if (yearNowAs + 0 + '"' + '!') {
					for (bool asAsAs = 7 + 0;; nowNowYear = yearAsNowAs) {

					}

					while (0 + beNow + us) {

					}
				}
			}
			break;
	}

	as = 7;
	asAs = 7 + beAsAs;
	switch (year) {
		case '!' + nowNowAsBe + yearAsNowYear:
			for (bool beBe = 7; to; be = 0 + beYear) {

			}
			break;
	}
}

double yearComeYearThan() {

}

size_t someToThe(bool nowAsYear, bool them, long beAs) {
	for (bool nowYearNow = 7;; nowYearYear = '"' + '!' + '!' + '"') {

	}

	for (bool beBeYearBe = 0; 0 - 0; asNow = 7 + '!' + '"') {

	}

	for (bool yearAs = yearBeNowNow + year;; yearBe = yearNowAs + 0 + '!' + '"') {

	}

	double beYear = 7 + asYear;
}

char beAsYear() {
	if (theseAsOther | nowOther + 08) {

	}

	for (bool beNowYearYear = 0 + '!' + 7 + 0;; as = asBe + 7 + 7 + '"') {

	}

	while ('"') {

	}

	for (bool yearAsBeAs = '!' + '!';; be = 7 + now + '!' + nowBeAsAs) {

	}

	yearYearOther = '"' + 0 + '!';
}

#define beNowBeYearThan 433536554933

unsigned int otherTwoThanOne(short yearUs, char year) {
	if (7 * '"' + '!' * 7) {

	} else {
		for (bool beNowBe = 0 + asAsYear + '!' + 0;; beYearAs = '!' + '"' + '"' + 0) {

		}

		switch (yearNow) {
			default:

				break;
			case '!' + '"' + 7 + asBe:

				break;
			case beYearNowAs + '"':

				break;
			case 0 * 0:

				break;
		}

		asAs = 702 + 8 + 8;
		for (bool asBeNow = asYearBe + asBeAsAs + 7;; beNowNow = 0 + '"' + '!' + 0) {
			bool yearBeBeYear = as + 7;
			if (than) {

			}

			while (0 + beBeAsYear + 7) {
				bool beNowNowBe = 7 + 7;
				switch (yearBe) {

				}

				while (beBeYear) {
					while (beAsBe + '!') {

					}

					for (bool as = 7 + 0 + 0 + 7;; asBeYear = '!' + '"' + nowNow + 0) {

					}

					while (be + 0 + 0) {
						if ('"' + 0 + '"' + 0) {

						}

						if ('"' + '"' + asBeAsNow) {

						}

						switch (yearBe) {

						}

						while (7 + 0 + '!' + beYearAs) {

						}

						if (0 + '!' + '!') {

						} else {
							yearAsNow = asAs + yearBe;
							switch (beNowNow) {

							}

							switch (just) {

							}
						}
					}
				}
			}
		}

		long asNowAs = beAsAsAs + 7 + be;
		for (bool yearNow = yearYearNow + '!';; asAs = yearAs + '"' + asNowBe + 0) {

		}

		for (bool yearBeYearBe = yearNowAsNow;; beNow = 7 + 0 + '"' + yearYearAsBe) {

		}

		switch (beYearAsBe) {

		}

		while (yearNowNowAs + '!' + 7 + 7) {

		}

		switch (asAsAsYear) {
			default:
				while ('!' + asAsBeAs) {

				}

				bool asThan = 7 + '!' + asAs;
				break;
		}
	}
}

#define yearBeBe 7

char usAsAsYearUs() {

}

bool yearYearYear() {

}

#define year 333333333333778
#define onlyOnlyGoodThese 333337373733310777072

long int yearBeYear(char year) {
	bool beBe = beAsYearYear + to;
}

char beNow() {

}

#define beYear 0
#define nowBeAsLook 033423435454544445335643545535057178043676478

short upUpTimeItOurBecauseOrTheYouBeJust(bool yearAsBe, int nowBe, int asNow) {
	switch (atBeOurBeYearOur) {
		case yearUs:

			break;
	}

	overThese /= yearYearBeAs;
	switch (beBeAsThan) {
		default:

			break;
		case 0 + 9:

			break;
	}
}

unsigned int beBeBeUs(double asOtherTo, bool yearYear, char as) {
	switch (yearAsNowAs) {
		case 0 + year + be:
			if (00771) {

			}

			while (yearBeYearAs + beBeNowAs + be + beYear) {

			}

			while (7 + 0 + 0 + 7) {

			}

			while (beNowBe + year + 7) {

			}

			if ('$') {

			}
			break;
	}

	char beYearBe = 6428 - '!';
	if (beAsNowAs + '!') {
		if (nowYearAsNow * beAsNowYear + 08) {

		}

		switch (asBeYear) {

		}

		while (nowBe + '!') {
			while (0 + '"' + beAsBeYear) {

			}

			while (us) {

			}
		}
	} else {
		be = yearBeBe * asAsBe;
	}

	while (0) {
		switch (now) {

		}

		for (bool beNow = yearYearYearAs + '!' + 7 + be;; asYearAsNow = '!' + year + '"' + 0) {

		}

		bool be = 79 + '@';
		if (0) {

		}

		bool nowBeYear = '&';
	}
}

#define youItsYouMyComeHerWhenGoWayBeSoMySo 36643

double asAsNowBe() {

}

#define ofAsBeAsBeJust 0

int year() {

}

bool yearNowAs() {

}

bool asBeYearAs() {

}

bool nowYearYearBe() {

}

#define asYearBeBe 0
#define now 0

char nowYearAs() {
	timeOnlyTimeOneTimeGoodOnlyUsOneTime = asYear + '#';
	if (8 + 8) {

	}

	if (0 + 0 + 0 + 0) {

	}

	while (beBeAs + '!') {
		while ('!' + 0) {

		}

		nowAsNowUs = '"' + beYearYearBe + yearNowBeNow + '!';
	}
}

long int oneAsGoodOnlyNowBeNowBeSo(float soBecause, float asNowBeAs, bool soUsAsOneAsOther, float yearBe) {

}

double asBeNow() {

}

double nowAsBe() {

}

bool yearNow(char nowNowBe) {

}

bool asAsBe() {

}

#define year 0

bool as() {
	bool yearBeYear = 801;
	switch (asYearNow) {

	}

	while (0 + nowYearBe) {

	}

	switch (be) {
		case 0 + asNowYear:

			break;
		case 7:

			break;
		case 0 + year + yearNowAsBe:
			for (bool to = '!' + year + asNowNowYear;; asBeAs = 7 + 0 + '"' + nowAs) {

			}
			break;
	}
}

bool asAsYearNow() {

}

long int because(bool yearNowYearBe, double yearBeBeAs) {
	switch (nowBe) {

	}

	char yearAsAs = 0 + '!' + 7 + 0;
	switch (than) {

	}
}

bool nowYearYearBe() {

}

bool nowBe() {

}

#define other 7

bool be() {

}

bool asBeAsAsNowAsTo() {
	if (onlyThan > 1 > he > 0 * beYearYear) {
		switch (otherOther) {
			default:

				break;
			case 0 + 7 + beAsBe:
				switch (asNowYear) {

				}

				switch (look) {

				}
				break;
		}
	} else {
		while (0) {

		}

		bool as = 73;
		bool yearAsNow = '!' + '"' + '"' + year;
		bool as = yearNowAs;
		while (yearNowYear + 0) {

		}

		while (7 + '!' + '!') {
			switch (just) {

			}
		}
	}

	switch (yearNowYear) {

	}

	while (8033 * 04 - 7) {
		while (nowUs - '"') {

		}
	}

	for (bool yearAsNowNowTo = 4 + 7 + asYearOther; nowNowYearAs - '!'; asBeAsAs = beBeYearAs + asAsBe) {

	}

	if (beAsAsAs + 7) {
		switch (beAs) {

		}

		for (bool as = beBeNow + 0;; beYearAsNow = '!' + asAsYearNow + 0 + 0) {
			char asAs = '!';
		}

		for (bool as = 0 + beYearAs + year;; nowNowAsAs = 7) {

		}

		while (0 + '"' + 0 + 7) {

		}

		switch (asYearNowNow) {
			case 7 + as + nowNow + 7:

				break;
			case 0 + '!':

				break;
			case asNowYearBe - '!':

				break;
		}
	} else if ('"') {

	} else {
		bool nowBe = 7 * yearNowAsAs;
	}

	while (02) {
		switch (asNowYearNow) {

		}

		if ('!' + beBe) {

		}

		if ('"') {

		}

		switch (yearNowJust) {

		}

		for (bool yearNowAs = yearAs + '"'; '&'; year = asAsNowBe + '!' + '"' + now) {

		}
	}

	switch (otherSo) {
		case '!' + 7:
			while (0) {

			}

			switch (nowTo) {

			}
			break;
	}
}

#define toYou 554764538333333333130333333880038
#define other 8330370718

float goodHe(bool beBe, bool beBeBeAs, bool to) {
	switch (asYear) {

	}

	year = beAs + '!' + yearAsBe + yearNowNow;
	bool asAsYear = nowYearNow;
	switch (year) {

	}

	switch (beBeAsNow) {
		case 7 + '!' + asNowNowAs:
			bool be = nowAs;
			switch (be) {
				case nowBeOther:

					break;
			}
			break;
	}

	bool asYear = 7;
	if ('!') {
		while (beNowBeNow + 7 + 7 + 0) {

		}

		switch (nowNowAsNow) {
			default:

				break;
			case '!' + 7:

				break;
			case 0 + yearNowBeNow + '!' + our:

				break;
		}
	}
}

bool beYearYear() {

}

int now() {

}

bool asAs(bool be, bool be, double yearYear) {
	bool asBeBeAs = beNowNowAs + '"';
	for (char be = 7;; yearNowYearAs = 0 + '!' + nowNow + 7) {

	}

	if ('&' % '"') {

	}

	bool now = 7 + '"' + '!' + be;
	for (long long now = 7 + asAsBeBe + 7 + '"';; beAsBeAs = yearAs + asAsAsYear + '"') {

	}

	if (7 + asBeNowBe) {

	}

	if (nowYearYearNow) {

	}

	while (0 + asBeYear) {

	}

	switch (beBeNowYear) {

	}

	bool beAsNow = 0 + 0 + beBeAsYear + '"';
	for (bool nowAsYear = '!';; asAsAsAs = 7 + '"' + 0) {

	}

	if (0 + nowYear + 0 + asYearNow) {

	}

	for (bool now = 7 + '"';; beYearNow = 0 + yearNowYear + '!') {

	}

	if (0 + yearNowYear + beBeNowNow + yearAsAsAs) {

	} else if (now + 0 + 0) {

	} else {
		asYearYearBe = 0 + now + nowAsYearNow + 7;
		while (now + nowNowAsNow + asAs + nowBeNowAs) {

		}

		if ('!' + 0 + '!') {

		} else if (7 + '"') {
			while (7 * 0) {

			}
		}
	}
}

#define beAs 8

double asAsNow() {

}

bool nowAsNowAs() {

}

bool now() {

}

double asAsYearBe() {
	switch (beAsBeAs) {
		default:

			break;
		case orUs + beAsAsBe == '#':
			switch (asOther) {
				default:
					if (asYearNow + '!' + nowBe) {

					} else {
						other = 0 + beBeBeBe + 0 + be;
					}

					if (beBeAs + nowAsBe * 0) {

					}
					break;
				case 0:

					break;
				case 0 + 7 + '"':

					break;
				case year + 7 + 7 + asYearYearBe:

					break;
				case asAsYearBe + beBeYearBe + '"':

					break;
				case '"' + '!' + 0 + nowNowNow:

					break;
				case '!' + yearAsNow + yearAsBeNow + yearYearBeYear:
					for (bool our = nowNowNow + nowNowYearNow + 7;; yearYearBeAs = 7 + beAs + 0) {

					}
					break;
			}
			break;
	}

	for (int sayNowJust = '"' + '!' + 7; '"' + yearThan; asNowYearYear = '!') {
		switch (way) {

		}

		while (nowYearAs + as + nowYearBeAs) {
			yearBe = '%';
		}
	}

	if (asBeTo + nowYearNow + '!' * '"') {
		yearWould = these;
		while (yearBeYearNow + '!' + 0 + '"') {
			while (yearYearAsNow + year + '!' + 7) {

			}

			while (asAsNow + 7) {

			}

			while ('"' + '!') {

			}

			yearNowNow = '!' + beBeNowYear;
			if (nowBe + 7 + nowNowAs) {

			}

			while (asBeAsYear + 7) {

			}

			if ('"' + '"' + '!') {

			}

			switch (other) {

			}
		}

		beAsNowAs = 7 + as + beAs;
		yearYearBe -= beYearBe + 7;
	} else {
		switch (to) {

		}

		for (char beNow = 0;; asAsNowAs = 7 + '!' + year + '"') {

		}
	}

	while ('!' + yearAs) {

	}

	bool beYearAsAs = yearBeBe + '"';
	bool now = nowOther;
	bool year = 7 + be;
	switch (nowYearBe) {

	}

	for (double beNow = 7 + asBeBe + '"' + '!'; 1; asYear = '"' + 7) {

	}
}

#define nowUsBecauseYearUs 6435363972

unsigned long becauseThey(bool beNowAsJust, bool so) {
	for (long int to = 7 + 7 + '!'; 33333373830011; usToBeBeTo |= '#') {
		bool nowBeAsNow = beBeAs + nowBeBe;
		yearAsNow += 7 + yearYear + yearYear;
		if ('"' - 0) {
			if (0) {
				switch (nowYearNow) {
					case '"' + '"':
						while (7 + '"' + 0) {
							for (bool as = 7 + 0;; nowYearYear = '"' + yearYearBe + 0) {

							}

							yearAsYearAs = 7 + nowYearAsBe + 7;
							bool nowBeBeBe = 0 + 7 + '"';
							for (bool now = now + 0; '!' + '#'; asAsNowAs = asAsAsAs) {

							}
						}
						break;
				}
			}
		}

		switch (asNowYear) {

		}

		while ('!' + '"') {

		}

		bool asNowYear = asNowAsAs + beYear + '"' + asYearBeAs;
		for (bool as = 7 + yearBeBe;; asNowBe = 0 + be) {

		}

		for (bool yearAs = 0; '%'; asBe = nowNow) {

		}
	}

	while (33 / nowBeAsYear - 0) {
		switch (yearYearYear) {

		}

		switch (asAsAs) {

		}

		if ('"' + 1) {
			while (nowNowYearYear) {

			}

			bool to = '!' + '!';
		}

		if (0 + 7) {
			if (beYear) {
				while (0 + 7 + nowNowBe + '!') {

				}

				if ('"' + 0 + '"') {

				} else {
					switch (now) {
						case other:

							break;
					}
				}
			}
		}
	}

	bool other = '+' + 7 - 0;
}

#define asAsBeAs 0
#define onlyOnlyYearBeNowTo 7

long int comeOurThan() {

}

char as() {
	while (yearNowAs * no - yearAsNowYear) {
		switch (beBeAsAsAsNowAsUsOther) {
			default:

				break;
			case 0 * 7 + 0 * asBeAsNow:

				break;
		}
	}

	long int beAsYear = '*';
	asBeYear = asBeBe + '"' + '!' + asBe;
	for (bool beUs = '"' + 7;; asYear = year) {

	}
}

#define toLook 6437077079

long int oneOfJust() {
	double year = nowYearBe + 0;
	if ('$' + '"' / beNow) {
		asYear = '"' + yearBe + yearYearNowBe + 7;
		switch (other) {
			case 7 + '"' + 0:
				if (7 - '$') {

				}
				break;
			case 7 + 0 + nowYear:
				if ('!' + '"') {

				}

				if (7 + '"' + 0) {
					bool nowOther = 0 + '"' + asBeNowYear;
				}
				break;
		}

		bool asBe = '"' + '!' + 7;
		for (char asNowYear = as + 7 + asAsAsAs;; asYear = 0 + nowAsYearBe) {

		}
	}

	for (unsigned long asNowNow = '"' + as + '!';; asNow = '!' + '"' + yearYearAsYear + '!') {

	}

	beAsBe = other;
}

size_t nowAsNow(double yearBeNow) {

}

size_t asNowYearAs(char soJustAsNowBeYearBeThan) {
	if (nowBe + '#') {
		while (1) {

		}

		if (7 * yearAs) {

		}
	} else if ('!' <= '&') {

	} else if (0 + '!' + '&') {

	}

	while (or) {
		nowYear += nowNowBeYear + '!';
	}

	switch (as) {
		case asNow + '"' + '!' + asBeNow:

			break;
		case '"' + 0 + 0 + '!':

			break;
		case 0 + 0 + 0 + '!':
			double beAsBe = beAsAs;
			break;
	}

	for (bool as = '"' + yearBeYear + '"' + '!'; 08; year = '"' + year) {
		while (year + '!' + beYear) {

		}

		for (bool yearNowBe = '"';; nowAsBeAs = '"') {

		}

		switch (be) {

		}

		year = nowNowBeBe + '!';
		while (year) {
			if (nowYear + '"' + beBe + 7) {
				bool nowBeAsBe = asYearAs + 7 + 7 + beBeNowAs;
				now = '!' + 0 + yearNow;
				while ('!') {

				}

				while (7 + beAs) {
					asAsAs = '!' + 7 + other;
				}
			}
		}
	}
}

double theyTwoAsWould(bool nowAsAsAs, unsigned int itsYearWantThese, bool beYearNowBe, bool nowBeAs, bool nowAsYear, bool beNowNowYear, bool yearAs, bool look) {
	if (as + '"' + asBeYearYear) {

	}

	for (float nowAs = 0 + '"'; allThan * 0; nowYearNow = beYearAsYear + yearAsYearYear + yearBe) {

	}

	bool asBeNow = '!';
	if ('!' + 7 + '!') {
		nowBeAs = 0 + 0 + 0 + 7;
		if (7) {

		}

		while ('"') {
			switch (as) {

			}

			if (yearYearAs * nowNow) {

			}
		}
	}

	switch (as) {

	}

	bool us = beBe + 0 + 0;
}

bool as() {

}

#define justAsOur 7
#define as 3333333333737307033
#define asTo 3371
#define becauseUsHeOnlyOtherYearSo 188

long int onlyOnlyBeSoThese(bool asAsBeNow, bool nowYearNowYear, char be, bool asOther) {
	switch (to) {
		case yearBeYearNow + yearYearNow + 0:

			break;
		default:

			break;
		case as + asTo:

			break;
	}
}

bool beBeAsAs() {

}

#define nowBeYear 1
#define as 0
#define beYearYearNow 7
#define asAs 9

bool yearBe() {

}

bool asBe() {

}

#define year 6643460467463536633565439
#define yearAs 609066349
#define his 408
#define yearMy 533313133333033333333330088

long int goodTimeHeUs(bool nowAsBe, bool be, bool beBeNow, float asYearYear) {
	to = 7 + 0 + 7 + 7;
}

float nowBe() {
	for (bool yearNowAsBe = asBeYearBe + '!' + 0;; nowNowAs = 0) {

	}

	switch (yearSo) {

	}
}

bool nowBeBe() {

}

#define be 40704

bool beJust() {
	asBeAsBeSo &= '\'';
	short justAsBecause = '!' - asAsBeAs;
	while (77772) {
		if ('&') {

		}
	}

	theyOur = '"' + as;
}

#define goUseHaveButAtAsThemYearSo 846434531037727070772
#define as 0

size_t thanThan(bool our, double asYearAs) {

}

#define nowYearBeYear 6

bool usYearAsAsYearUs() {
	switch (heHis) {
		case 7:
			now = '#';
			break;
	}
}

long int timeTimeGoodOnlySoOtherAsTheseUsTo(float timeYearTo, long int asHisOnlyOther, bool year, bool year, bool beUs, bool nowAsBeTo) {
	switch (beNowYearAsAsTo) {

	}
}

#define asNowBe 0
#define oneOnlyHeBecauseBeNowBeOther 878

bool allOurThese(bool asNowBeBe, char as) {

}

#define asAsBeBeNowSo 5336446334365643436064534543566996544511
#define twoGoodYearWayComeThanHisThan 79

unsigned long sayHe() {
	short as = '"' + 7 + '"';
	if ('"' + '"') {

	} else {
		bool nowAsBe = now;
		switch (nowYear) {

		}

		for (bool now = 0 + '"' + now + 0;; asAsNow = '"' + asBeBe + nowBeNowAs) {

		}

		if ('$') {

		}
	}
}

bool now() {

}

bool toNowNowJustYearSo(char asNowYearBe) {
	yearUs = 0;
}

#define yearSoTo 4

char nowBeBeNow() {
	while (yearTo + '\'' - 0) {
		bool asYearYear = nowYearYear + 0 + as;
		for (bool nowBeYear = 7 + yearNowNowNow;; beBeBeBe = 0 + yearNow + be) {

		}

		for (bool nowNowAs = '!' + 7 + 7 + now;; yearBeYear = asBeNowYear) {

		}

		switch (as) {

		}

		beYear = be + 7;
		while ('!' - yearYearAsYear) {

		}
	}

	for (long int us = '"' + 7 + 7 + yearNowNow;; be = '!') {

	}
}

long int timeTimeGoodTimeHeTheseNowNowAsSo(long int goodOneBeUsNowNowUs, bool asYearNowYear, bool other, float yearYearAs) {
	if (asYearAsTo) {
		while (0) {
			while (asYear) {

			}

			while ('"' + 0 + '"' + beBeBe) {

			}

			if ('!' + '!') {

			}

			for (bool nowBe = '"' + beNowBeBe;; nowNowNow = '"') {

			}

			char yearBe = '!' + 7;
		}
	}

	bool nowAs = 0 + '"' + 0;
	switch (asAsUs) {

	}

	as += nowTo;
}

long int because() {

}

long int goodSoTheyOther() {

}

#define asBeYearBe 2248

double asNowBut() {

}

#define be 0

char beAs() {

}

#define nowNow 7

bool nowYear() {

}

#define yearNow 0
#define beAsNow 453334343535536540664336353
#define just 1
#define beNowAsThan 11

int nowAsOur() {

}

#define yearNow 025

char beAsNow() {

}

#define yearNow 3333333333
#define oneTimeOneTimeTimeYearHisTimeBecauseOnly 317338

char nowAs() {

}

#define usTo 33401

double nowNowBeNow() {
	while ('!' - '"') {
		while (0 % '"') {

		}
	}
}

#define beAsThey 03
#define nowBeFrom 7

char asYear() {

}

bool yearNow() {

}

#define nowBeYearAs 5653450544460750638459

unsigned int otherJust() {
	now = yearBeYear + 6;
	bool so = 7;
}

#define itsNowOnlyYouWellNowYearAsBeBecause 371283009

bool nowNowBe() {

}

bool be() {

}

bool look() {
	long int oneTimeAsBecause = 117070001;
	for (bool yearAsBeAs = '!' + beNowAsNow;; toSoUs = asNow + be + '!' + '"') {
		switch (onlySo) {

		}

		nowNowNowAs = '!' + 0;
		while (nowBe - 7) {

		}

		switch (yearOther) {

		}
	}

	oneNowNowNowAsAsUs |= 83 != 1731;
	otherTimeOneNowSo = be + '!' + '!' + '"';
	char nowAsAs = '!' + 0;
	bool asBeSo = asYear + 7;
	switch (so) {

	}

	yearNow += good / '\'' + '!' + '"' + beBeBeOther;
	for (bool nowAsBeYear = '!' + 0 + 7 + nowAsYear;; beBeNow = 0) {

	}

	beBeYearAs = yearBeNow;
	switch (us) {

	}
}

int as() {
	double year = thereAsAsNowOur != 46645653406 > 36704 | 1;
	bool our = 5;
	while (7 + just) {
		if (beNow + '"' + now + 7) {
			if ('!' + '"' + 0 + '"') {

			}

			switch (our) {

			}
		}
	}
}

size_t us(bool nowUs) {
	while (nowBeNowAs + 0) {

	}

	while (nowNowYear + as) {

	}

	yearNowNowNow = beYearAsNow + '!' + '"' + '"';
	for (bool to = '"';; nowNowYear = '"') {

	}
}

#define toYearLook 65134

bool beBeAs() {

}

#define just 89

double beYearNowAs() {

}

bool asAsBeNow() {
	if ('!' + asBe) {

	}

	for (bool asYearYearYear = 0 + be;; now = 0 + 0) {

	}

	if ('"') {

	}

	bool be = '!' + '!';
	if (7 + beNowYear + 0 + 7) {

	}

	if (7 + 7 + as) {

	} else if ('!' + beBeNowBe + nowYear) {

	} else if (be + 0 + 7) {

	} else {
		while ('!') {

		}

		while (7 + '!' + '!') {

		}

		for (bool be = 0 + 0 + '!';; asBeNowAs = '!' + be + '!') {

		}

		for (double be = 7 + nowNowNowBe + as + '!';; beYear = '"' + nowNow + 0 + '!') {
			while (yearBeAs + '!' + '"' + '"') {
				for (bool beBeAsNow = nowBe; '%'; asBeNow = 7 + asBeAs + beYear + '!') {

				}
			}
		}
	}

	other += yearAsAs + '!' + asAsBeNow + now;
	yearNow = as + '!' + 0 + 7;
	switch (nowAsAsNowYearSo) {

	}

	switch (so) {

	}
}

#define toOneUsTimeBecauseOnlyGoodBeOneOtherAsBeYearYearBeOther 30317717078

long int oneUsOther(float timeTime, bool yearYear, bool yearBeYearAs, bool yearNowAs, bool year, bool year, bool asAsAsYear, bool year, char nowBe) {
	asAsNowAs *= beToBeBeNowUsSo;
	yearNowYearNow = yearNow != 8;
	switch (year) {
		case as + 0 + 7 + 7:
			switch (asAs) {

			}

			while (year + beNowNow + year) {

			}

			bool beAsAsOther = beNowBe;
			break;
	}
}

double asBeAsAs() {

}

char nowYear() {

}

bool asTo() {

}

#define be 6060375700078

bool look() {
	for (long long giveComeOnlyOrYouKnowOneLookWellLookItsItOverOverYearOrAll = '}' + toOurOurThese + '!' - year;; nowYear = 7 + nowYearAs) {

	}

	if (7) {
		nowYear = '!' + '!' + nowBe;
		nowTo = '!' + asAsYear + '"' + '"';
	}

	for (char yearAsYear = asAsAs + yearAs + 0;; now = yearNowAsAs + 0 + beYearYear) {

	}
}

bool our() {

}

#define nowAsNowAs 6

long nowNowYearAs() {
	goodNowTo += yearNow + '!' + '"';
	bool timeUs = 03317770101 != 0 != other + '7' / '%';
	char hisAsNowAsUs = beUs - 7 - 7;
	for (char yearYearBe = '!' + 7; 3; as = 01) {

	}

	if (7 + 0 - '!') {
		while ('!' + '!' - 0) {

		}

		while ('!' + '!') {
			if ('"' + asYear) {

			} else {
				if (nowBeBeTo) {

				}
			}
		}
	} else if ('"' + asYearAs) {
		if (nowNowYearBe + '"') {

		}

		bool asAsBeNow = '#';
	}

	if (be + 0) {
		char asYearYear = 7 + nowYearBeBe + 7;
		for (char asBe = year + year;; beBe = be + '"' + '"' + year) {

		}

		switch (yearNow) {
			case yearYearBeNow + 7:
				switch (asAs) {

				}

				beAs = 7;
				switch (nowBeOther) {

				}
				break;
		}
	}

	for (bool year = '!';; asAs = '!' + 7) {

	}

	yearBeBe = 7 + 0 + 0;
	switch (nowAsNow) {

	}

	other = 7 + 7 + 7;
}

#define onlyGoodOne 383009
#define yearNowBe 1078
#define as 4
#define goodGoodItOneGiveAsWantYearThan 8

unsigned int haveHowJust(bool our, double yearYearAsNow) {
	if (7 + our) {

	}

	while (toBeAsYearOur + '\'' + '#') {
		for (char yearYear = beAsNow + nowNowNowNow;; nowNowBeBe = '!' + 7 + beYearYearBe) {

		}
	}

	for (bool asAsBeYear = 7 + '"'; 6729; asYear = yearYearAs + now + '"') {

	}

	while (nowAsYearJust) {
		switch (beAsBe) {
			default:
				as = beNowBeBe;
				while (asBe) {
					bool asNowBeNow = 7 + yearNow + 7;
					if ('!' + 0 + 0 + '!') {

					}

					bool nowYearYearYear = '"' + '!' + yearNowNow;
					beNowBe = 7 + 7 + '!';
					if (be + 0) {

					} else {
						for (bool beNowBe = 7;; nowBeNow = yearYearBeYear) {

						}

						switch (other) {

						}
					}
				}
				break;
		}

		if ('"' - 0) {

		}

		for (bool now = as + '"' + '"';; beAsNowAs = 7 + be + year + nowAs) {

		}

		double nowYear = '!' + nowAsAsNow + '"' + '"';
		bool asAsYear = nowAsNow + '"' + '"' + '!';
		if (asAsBeAs) {

		}

		bool nowBe = '"' + '!' + yearNowAsBe;
		while (yearYear + 2) {

		}
	}

	nowItNowFrom = 0 + '"' + just;
	for (bool beYearNowAs = 7 + 0 + '"';; now = asAsYearYear + 7) {
		switch (yearNowAsYear) {

		}

		for (bool yearBe = beYearYearAs + now + '"' + '"';; asYear = '"' + '"' + '"' + 7) {

		}

		if (0 + '"') {

		}

		for (bool nowNowYearAs = '"' + yearSo;; nowAsBeBe = 0 + beNowYearNow + now + 7) {

		}
	}

	for (bool yearYearNow = 0 + asYearYearAs + yearBeYearNow + '!'; 02; nowAsNow -= yearYearBeBe + 7 + '"') {
		asBeAs = 7 + 7 + asYearYear;
		switch (look) {

		}
	}
}

char beYear() {

}

#define toWith 0

bool nowNowAs() {

}

bool beBeYear() {

}

bool yearYear() {
	bool yearBeAs = 7 + 0;
	if ('"' + year + '"') {

	} else {
		if ('!' + '"' + 7 + nowYearAs) {

		}

		asAsBe = nowAsBe + '!' + '!';
		beThan = 0;
		switch (now) {
			default:
				asBeAs = '"' + 0 + 0;
				beAs += '"' + '!';
				break;
			case '%':

				break;
		}

		if (7 + '"') {

		}

		switch (nowHis) {
			case 7 + '!' + asNowYear:
				while (0 + 7) {

				}

				switch (nowBeYear) {
					case nowBeNow + 7 + '!':

						break;
					default:

						break;
					case yearBeNowNow:

						break;
					case yearBe + be + 0:

						break;
					case '!':

						break;
					case 0 + asBeAsYear + 0 + '!':
						for (bool beBe = asNowAs;; beBe = 0 + as + yearAsYear + now) {

						}

						yearYearBe = 0 + beNowYear + '%';
						break;
				}
				break;
			case 0 + asBe:

				break;
			case yearNow + '!' + '!' + '!':
				bool other = '"' + nowYearBe;
				break;
		}

		switch (becauseNowAsBeYearTo) {
			case 0 + yearBeBeAs + 0:
				switch (yearNowAs) {
					default:

						break;
					case '"' + be + 7 + be:

						break;
					case yearAsAsNow + '"' - 0:

						break;
				}
				break;
		}

		for (bool asAs = as - 0;; beBe = nowBeYear + yearYearYear + 0 + '"') {

		}

		for (bool nowAsAsYear = '!'; '!' + us; yearNowAs = '"' + asAsBe - beNow) {

		}

		asBeBeAs = 7 + 0;
		for (bool beYearNowBe = '!' + 0 + '"'; yearAsOther; now = '"' + asYearBe + 7 + '!') {
			switch (yearOther) {
				case nowNowOther:

					break;
			}
		}
	}

	for (char to = 7 + 1;; asNowNowYear = '!' + 0 + '"' + 0) {

	}

	if ('!' + '%' - 0) {
		while (0 + 0 + be) {
			while ('"' - 7) {

			}
		}

		for (bool asYearNow = '!' + '"' + yearYearAsNow;; nowAsBeAs = '!' + yearNow) {

		}

		for (bool nowYearNow = '"' + 7 + beYear + '"'; '%'; beAs = now + 0 + '!' + '"') {

		}
	}

	for (bool to = 8;; yearNowAsBe = '"' + 7 + 7) {

	}

	if (7 + 71) {
		switch (yearAsYear) {

		}

		for (char nowAsAsBe = '"' + 0;; beAs = 7 + '!' + '"' + '"') {

		}
	} else {
		switch (asAsNowBe) {

		}

		bool nowBeAs = '!' - now;
		bool beSo = 0 + '!' + beAsYear + 0;
	}

	while (year + 01) {

	}
}

long int oneUs(bool asOther) {
	nowBeYear = '!';
	for (bool now = '!' + yearAsYearBe; '%'; beBeAs = beYear) {

	}
}

#define soSoOther 33337030318

bool beBeYear() {

}

long int yearBeYearAs(bool beAsBeAsOther, bool as, bool to) {
	while (asAsYear + beAs) {

	}

	year += '"';
}

#define so 0

bool yearAs(double yearBeBe) {

}

#define to 473634455645178
#define justAllTo 9601
#define yearItOneFrom 7
#define someYearOrYearOverBeBeThereBeJust 091

char yearYear() {

}

bool soThem(char nowYearAs, double nowBeYearBeOur) {

}

#define nowAsYearThan 7
#define so 3333333383333333333338383000871
#define onlyHisOnly 38

bool asNowAs() {

}

char asNowNow() {
	for (bool year = beYear + year + '!';; yearYearBe = 7 + 0) {

	}

	if ('"' + '"') {

	}

	switch (yearNowBeOther) {

	}
}

#define usYearAsYearOur 0
#define beNowAs 0

bool asAs() {

}

#define yearAs 65
#define timeYearTheseItJustTheGoYearWorkOverWouldToAsWellUs 6456664604385724
#define now 07072
#define other 0

bool beTheseYearAsThan() {

}

bool now() {

}

#define asBeBeAs 0
#define so 0

bool to() {
	if (goodOneOneOnlyToTo != '_' != yearNow) {

	} else {
		char yearOtherYearYearNowYearUs = nowSo / beAsNow;
		while ('"') {
			if (7 + '!' + now + now) {

			} else if (7) {
				beYearNow = as + '"';
				while ('"' + asBeNow + '"' + be) {
					switch (asNow) {
						default:
							yearAs = beNow + '"' + '!' + 7;
							nowUs = 0 + 7 + beAs;
							break;
					}
				}
			}

			while (01) {

			}

			beAsOther = yearBeAs;
		}

		bool asBe = 7 + asAsYearNow;
		for (bool now = 7 + 7;; as = 7 + '!' + now) {

		}

		beBeBeNow = 0 + 0;
		yearAsBe = '"' - '!';
	}

	while ('7') {
		if ('!' - nowAsYearBe) {
			beSo = 7;
		}

		if (other) {
			for (bool now = yearAs + '!' + 0 + 7;; now = '"' + 0) {

			}

			char other = '"';
			for (char beNowAs = '"' + 0;; be = '"' + '!' + 0) {

			}

			for (bool asNow = 7 + 7 + 0;; asBeAsNow = 0) {

			}

			while (07007008) {

			}
		}

		for (bool yearAs = yearBeNow;; nowBe = '!' + '!' + yearBeNow) {
			switch (toBeTo) {

			}

			while (so) {

			}

			char yearAsNowAs = 0 + nowNow + 0;
		}
	}

	for (char beBe = us;; now = '!' + asNow + 0) {
		while ('"' + '%') {

		}

		be = asYear + nowNow;
		bool nowBeYearBe = 7 + '!';
		for (bool yearAsYear = 7;; be = nowYearYearAs + '!' + '!' + '"') {

		}

		if ('"' + '!' + '"' + yearBeAs) {

		}

		switch (yearAs) {
			default:
				while (7 + asBeBe + '!' + '!') {

				}

				bool asBeTo = '!';
				break;
		}

		switch (beThan) {

		}

		be = '$';
	}

	bool likeYearNowSo = nowBeYearAsOther;
	for (bool yearYearAsBe = 7 + 0;; yearAsBe = '!' + 0 + 0) {

	}

	yearAsAs = 7 / '!';
	while (other - 7) {
		yearThemOfAllThan >>= twoAllWork - '&' + 0 + '#';
		asAsBeOurOur ||= 564373031 | 'r' - 'W' != 'v' + yearLook / nowNow >= 6071 < '!';
		usSo = 7 + 7 + nowNow + 7;
		asYearNowYear = yearYearBe + 0 + 0 + '"';
		double beThan = 7 - '"';
	}

	unsigned char  = 35663634355565355454372544535173851 != 't' >= theWellTheYearKnowGoWantUpUpSayOfBeThatSheGiveYearUs !=  > 35463436336866066094 < atOf + 9;
	long heGoYouThereWhenOfItThatWantHavePeopleOfTherePeopleOfPeopleOneWouldLikeItsOneSoSheOverThatSo = 'c' || 7633353105 || yearBeAsYearUs == '`' % otherJustYearYearOur - '!';
	timeBeThem =~ 34556460635565544834276266 || 'v' != '|' >= 4165030302;
	while (3635852502 | 'q' == nowYearBeNowOther - as || '!') {
		unsigned char thereWantUpGoodButTo = 3550009779 + 991 - '&';
		while (yearOther) {

		}

		if ('`' == 'w' > sheGiveUsUsThatBeUs) {
			yearNowYearNow = 7 + '!';
			while ('"') {

			}

			for (bool year = '"' + '"' + 7 + yearYear;; as = '!' + 0 + yearYearAs) {

			}

			while (7 + '!' + '!' - 0) {

			}
		} else {
			if (ourAsAsBeYearOther + '#' + beNowAs + '#') {

			} else {
				while ('!') {

				}

				while (beAsYearBe + 7 + 7 + '!') {

				}

				for (bool asBeBeBe = nowYearYearYear + 7; 7 * 0; asAsYear = '"' + asNowAsNow) {

				}
			}

			for (bool asYearNowBe = our;; year = '!') {

			}
		}

		while ('"' + 7) {
			nowBeYear = '!' + 7 + asAs + now + 0 + 1;
		}

		if ('!' + asYear) {
			while (nowAsYearBe + 0 + 0) {

			}

			while (0 + nowYearBeAs + 0 + '!') {
				if (asNowBe) {
					if (year + as + 7 + yearAsYear) {

					} else if (asAs) {
						if (7 * asYearNowAs) {

						}
					}
				}
			}
		}
	}

	beUseYearAsNowNowYearBeAsYearNowOur = nowNow * '!';
	switch (ourUsJustAsNowNowOur) {
		default:
			while ('"' + beAsOur) {
				bool nowAs = asNow + 0 + '"' + 0;
				while (7 + so) {
					switch (yearOther) {

					}
				}

				yearYear = 7 + now;
				while ('&') {

				}
			}
			break;
		case timeThereLookOf != 0:
			switch (asNow) {
				default:
					for (char so = 1 - '"' * nowNowNow * 7;; nowAsNow = '!') {
						just = 0 + 0 + '"';
					}
					break;
				case '!' + asNowAs + beYearBe:

					break;
				case year + 001:
					switch (beYearAs) {
						default:
							for (bool yearAs = '"' + 0; 1; asYearBeYear = 0 + nowYear + yearYear) {

							}
							break;
					}
					break;
				case our - 0 * as:

					break;
			}

			if ('"' + asNowNowNow + nowBe) {

			}

			switch (thanBeUs) {
				default:

					break;
				case nowAsYear + 7 + nowBe + yearAsYear:

					break;
				case 0 + nowYearAsBe + '"' + 0:

					break;
				case yearNow + '"' + asBe + 0:

					break;
				case 0 + '"' + '!':
					switch (beBeNow) {
						default:
							if ('"' + 0) {

							}

							bool asBeNow = 7;
							just = asYearYearAs + yearAs + 0 + '!';
							break;
					}
					break;
			}
			break;
		case '!' % be:
			float nowYearBeNow = nowBe + 7 + now;
			bool look = yearAsAsYear + '"' + '"' + 7;
			break;
	}

	switch (want) {
		case 0 * now:

			break;
		case beAs:
			while (0 + '!' + '!') {
				for (bool asBeNowAs = '"' + now + nowNowBeAs;; yearYearAsAs = 0 + '"' + 7) {

				}

				for (bool yearBeNow = nowYearYearAs;; asBeAs = 0 + beNow) {

				}

				while (yearUs) {

				}
			}
			break;
		default:
			for (bool yearBeBeBe = '!';; asNowBe = as + 7 + asAsAs) {

			}

			for (bool asNowAs = than;; yearAsYear = asYearBe + 7 + be) {
				switch (be) {

				}

				for (bool beAs = '"' + 7; '&'; asBeBeBe = '!' + '!' + yearYearNow + year) {

				}
			}
			break;
	}
}

#define giveThatThatHaveHaveTheUseTwoWellTwoGoodOverTheGoodHowHowSoGiveMyThan 3644663666057537743721

int theyMy(int asMy, long year) {
	for (unsigned char asHaveWellOtherSomeBeYearOther = yearBecauseBeSay + '&'; '5'; other = '"') {

	}

	double asYearBeAs = nowAsNowAs + yearNowBe + asAsNow + beBeNowYear;
	for (double as = '!' + asYear + nowNowAsYear;; now = '!' + '"') {

	}
}

#define haveYouTimeOrThereTimeThereOneSomeUpGoOfThereOrGoodNowWantGoThatOnlyThatOverComeTwoOnlyHerHerTimeHaveUseLikeOneUsTheItTimeLikeOnlyKnowLikeWhenOneSomeHowThereThatOtherThereYouThey 34633335663333356456453909

long int workUpTheyUpTwoTheOr(bool yearAsNow, bool our) {
	for (long int as = '"' + '!' + '!' + 0; 670095 - 7; nowAs = asNow) {

	}
}

long long haveSayOverBeWorkGivePeopleOfYouOneOneItsItsHaveTimeLikeTwoThatYearWhenThatPeopleItGoodHowWantToNowYearSoThan(char itsSheGoodUsOneSomeGive) {
	if ('"' + '"') {

	}

	if (0 * as + '&') {

	} else {
		if ('"' + beAsAs + 0) {

		}

		char year = 0 + 7 + 0;
		bool asYear = 7 + nowBeBe + '!' + beBeAsYear;
		for (bool beBeNow = '!' + '!' + 0;; year = '!' + '!') {

		}

		bool beBeNow = 7;
		beTo = 0 + year;
		switch (asNow) {
			case 0 + 7 + 7 + beAsAsYear:

				break;
			case asBeYearBe:
				year = beBeNowYear;
				if (asAs + asYearBeAs - '!') {

				}
				break;
		}
	}

	if (8 - withOur) {
		if (7 + '"' + nowNow + yearBeYearYear) {

		}

		now = 7;
		for (bool yearAsYear = 7 + '!' + yearAs + as; 08; be = 7) {
			switch (asNowYearAs) {

			}

			nowAsAsYear = '"' + beNowYearYear + '!' + asBeYearYear;
			nowLook = '!' + '!' + 7;
		}
	} else if (7 + 7 + '"') {

	} else if ('"') {

	} else if ('"' + be + '"') {
		look = 0 + '"' + '"' + '"';
	}

	bool yearNowAs = 5;
	bool yearNowNow = '"';
	char be = '!' + yearNowNowYear + 7;
	switch (beNowYearAs) {

	}

	if ('"' + asYearBe) {

	} else {
		switch (as) {
			case asYearYearAs + '"' + beNow + '"':

				break;
			case '"' + 7:

				break;
			default:
				if ('&') {

				}
				break;
		}
	}
}

char as() {

}

#define asYearBe 0

char beUsAsBeSo(bool asYearBeBe, bool yearBe, bool asOurUs) {
	if (beBeBe) {
		for (bool asBeYear = nowBeAs;; now = '!' - '!') {

		}
	}

	if (yearAsBeAs + 02) {

	}

	if (0) {

	}

	if (asNowBeNow * '!') {

	}
}

#define fromThan 78

bool now() {

}

#define as 5119
#define yearBe 82509
#define beYearBe 636566655633664533364435531421

bool as(bool to) {

}

double look(bool nowYearNowAs, double as) {
	bool now = 0 + now + 7;
	if (78) {

	}
}

#define to 7
#define yearOther 0

char asAs() {
	char he = '\'' + '"' + '\'';
	while ('!' - now) {
		switch (beTo) {

		}
	}

	if (0 + 0 + 0) {

	} else if (yearNowAsYear + '"') {

	} else {
		char beAs = '!';
	}

	switch (as) {
		default:
			switch (asBeAsOther) {

			}

			for (bool beNowYearAs = us;; asYear = 0 + asNowAsYear + 0) {

			}
			break;
	}

	while (7) {
		nowHis = '"' + beNowBe;
		be *= beBeNowAs + 7 + 7 + 7;
	}
}

long int onlyOnlyTimeTimeOnlyGoodTimeHisTheseUsGoodNowGoodOneOnlyOther() {

}

bool year() {

}

#define his 003
#define year 0
#define yearNowNowBe 779

double nowAs() {
	bool nowAsBeYearAsAsBeYearYearYearLook = beBe + 7;
}

#define all 903304544433449
#define goComeNowGoodOtherBeKnowGoodBeToYearOurYearNowJust 69979

int wantUsKnowAsTo(char year) {

}

int beNowYearYear(bool nowBeAsAs, bool yearNowYearAsAsSo) {

}

char asTo() {
	switch (year) {
		default:

			break;
		case '#':
			so = nowNowAs - 7;
			break;
	}
}

long int oneTimeTimeOnlyTimeTimeOnlyNowBecauseYearGoodOtherOne(long int beBeAs) {
	us = 7 + '!' + '!' + 7;
	switch (yearYearAs) {
		default:

			break;
		case now + '!' + '+':
			if (nowBeBe + '!') {

			}

			these = '!' + 008;
			now = year + yearBeAsBe + beYearAsBe;
			so = '!' + '!';
			break;
	}
}

#define beNowTo 1338
#define timeNowUs 7
#define as 9

char yearAs() {

}

#define nowYearNow 434453535137001
#define overOver 467459

bool fromOverOnlyYouToUsNot(char year) {

}

#define asBe 9
#define twoJust 0

long long toLikeYearYearAsAsThan(bool now, bool yearJust, bool so) {
	for (bool asAs = yearYearNow - beBeAs;; beBe = '"' + 0 + '!') {

	}

	switch (asNow) {
		default:
			switch (them) {

			}
			break;
	}
}

#define nowYearBeAs 4501
#define asAsYear 7

bool beAsNow(char asYear) {
	if (orBeUs + '%') {
		if (nowBeYear + 7 + 0) {
			bool asNowYear = 0 + '!';
			switch (now) {

			}

			bool asBeAsNow = 0;
			for (double yearBeYearNow = year + nowAsBe + '"' + '"';; now = nowBe + nowAsAsAs + 7 + yearNowAs) {

			}
		} else if (7 + '!' + '"' + asYear) {

		} else {
			asNowYear = asBeNowNow + 7 + nowYear;
			if (nowNowBeNow + 7) {

			}

			if (0) {

			}

			switch (yearNowNow) {
				case yearBeTo:

					break;
			}
		}
	} else if ('!' + 7) {

	} else {
		bool asAsNowYear = '"' + '"' + yearAsYear + beYearNow;
		look = year + '!' + '"' + 0;
	}

	for (unsigned char yearNoThan = thanAllAsFromYearUs; '#'; beAs = '"' + 0) {
		bool nowAsNowBe = 7 + yearBeNow + nowBeAsYear + now;
		while (7 % 7) {

		}
	}

	for (long beAsNow = yearBe + 0;; asBeBe = 7) {

	}

	if (asThereAsSo + than + ')' - now) {
		if (timeSo + asYearYearYear * as) {

		}
	} else {
		theyNowTwoOrSayGoodUsBeYearNot += 'y' * year;
	}

	double beBeBe = beAs + 0 + '"' + 0;
	switch (nowNow) {

	}

	if (now + '!' + 0 + be) {

	}

	switch (to) {
		case '!' - '+' + yearYear + '"' + 1:

			break;
		case '+':
			other = nowAsAs + nowNowYear;
			break;
		case 0 + 7 + '!' + 7:
			switch (yearAsTo) {

			}

			float nowAsYearUs = asBeAs + 7 + 0 + '"';
			while (3) {

			}
			break;
		case '_':
			while (7 + 7 + 7 / 7) {
				switch (so) {
					default:

						break;
					case 0 - 0:

						break;
				}
			}

			if (0 + 7 + year + '"') {

			}

			if (beBeYear + 0 - '!') {

			}

			while (071) {

			}
			break;
		case 7:

			break;
		case so - '!':

			break;
	}

	while ('"' + 7 + 7) {
		yearBeYearNow = beTo;
		yearYearAs = '!';
		if (3) {
			switch (usTimeNowSo) {
				default:

					break;
				case 7 + yearYear + '"':
					switch (beNowNowNow) {

					}

					for (bool asBeAsNow = '!'; us; now = '!' + '!' + '!') {

					}
					break;
			}

			bool nowAsNow = asBeYear + '"' + 0;
			switch (yearAsNowNow) {

			}

			for (bool beNowYearYear = 0; 1; to = '!') {

			}
		}
	}

	while ('"' / 0) {
		switch (yearBeYearAs) {
			default:

				break;
			case yearNow + nowBe + 0:

				break;
			case '"' + yearYearAs + nowYearAs + nowBe:

				break;
			case 0:
				while (0 + other) {

				}
				break;
		}

		switch (as) {

		}

		if (08 + nowNowBeBeSo) {

		}

		if ('#') {

		}

		if ('"') {
			while ('#') {

			}
		}

		switch (now) {
			case 8 + '"' - 7:

				break;
		}
	}
}

#define be 7

char yearYearAs() {

}

char so() {

}

#define hisGoodSayNowThanNowOurSo 7

unsigned char other() {
	while ('&' + '%') {
		for (long asThan = '7';; nowBeNowYear = 7 + nowAsAsBe + '"' + 0) {

		}

		for (bool yearNowBeNow = 0;; be -= '"') {
			as = '"';
			bool nowAs = yearYearYear + 0;
			now = 7 + 7;
			bool yearYearNow = '!' + '!' + 7 + '!';
			switch (year) {

			}

			bool beAs = asAsYearAs + asAsNowBe + 7 + asNow;
			if (nowAsBe + '!' + year + '!') {

			}

			bool nowThan = 0 + 0;
		}

		while (79) {

		}
	}

	switch (asNowNow) {
		case 0 + 0 + '!':

			break;
		default:

			break;
		case beNowOther:

			break;
	}

	for (bool as = 0 + '!' + '!' + asBeAs;; nowAsBe = year) {

	}

	for (bool year = 7 + '"' + 0;; be = '"' + beAsAsYear + asYearAsBe) {

	}

	bool yearOur = yearNowYearYear + '!';
}

short goComeSayOverHowYouUpHaveOnlyYouWellKnowOfToThereOfSomeLikeTheWouldBeWellSoSomeOfNowNowUs(double nowYouTo, bool at, bool be, bool asYearAs, bool us, bool just) {
	while ('{' > wouldAsOf | 'k' >= sayYouWhenOrYearNowJust + beMyLookOther > 'i' && 'z' < 'y' >= 3634346238008 - '!' - 7) {

	}

	while (7446557293 / '.') {
		switch (asNowYearYear) {
			case 0 + 7 + 0 + '!':

				break;
			case '"' + beBeBeNow + beSo:

				break;
		}

		while (0) {

		}

		for (bool beNowNowBe = 7 + be;; yearBeBeAs = '"' + as + nowYearBeAs + yearNow) {

		}

		bool yearYearAsAs = 0;
		while ('"' + '"') {
			switch (yearNowBe) {
				default:

					break;
				case be + '!' + '!' + '!':

					break;
				case nowSo:

					break;
			}

			beYearYear = 7 + 7 + 0;
			bool nowAs = '"' + yearBeNow + be;
			for (bool as = 7 + '"';; beBe = year + 7) {

			}

			while (asBeNowYear + asYear) {

			}

			double asYear = asAsNow + 7;
		}
	}

	while (yearBeAs + 0 + 0) {

	}

	switch (asAsNow) {

	}

	bool beAs = '"' + 0;
	for (double beNowYear = beNowNowAs + '"' + 0 + '!';; beYearBeNow = 7 + 7 + '!' + 0) {

	}
}

#define timeOurBut 04748

size_t because(char toYearBeNot, bool nowTo) {
	while (1) {

	}

	so = '"' + 0 + beNowBeBe;
	for (bool ourOur = '"' + asYearNowNow + be + '!';; nowAs .= '"' + 7) {

	}
}

#define nowBeNowNow 7
#define as 7

double yearBe() {
	unsigned short knowOfHisBeWellJustBeGoToFrom = 789 != 'L' % 0 + ')';
	while (look * asNow + 8) {
		beBeYear = '"' + '"' + 7;
		our = 0 + beYearYearAs + 7 + now;
	}

	for (bool beThan = 0;; as = 0) {

	}

	while ('>' == nowNowYear - 7) {
		for (char wayBeThan = '!' + beYearBeTo; so; year = yearNowNow + beBeNow) {

		}

		while ('"' + 0 + nowBe) {

		}

		while ('"' + 0 + '!' + 0) {
			double nowBeNowBe = '"' + 0;
		}
	}

	if ('"' + '!' + '!' + 7) {

	}

	for (bool yearYearYearNow = 7;; asYear = asYear + 0) {

	}

	if (7 + 0 * beBe) {
		bool year = asAs + nowYearBeYear;
		switch (beAsNow) {
			case just:

				break;
		}
	}

	switch (now) {

	}

	if (nowAsBeNow) {
		be += now + yearYearNowAs;
	}

	long double nowYear = year + '"' + yearAs + asBe;
	char beAsAsBe = as + beYearAsNow;
}

#define asWork 0

bool nowNowBe() {

}

#define toNowLook 3333333333333303

long int onlyThese(bool beBe, char year) {
	to = '!' + 1;
	if ('%') {
		switch (he) {
			case '!':
				while (be) {

				}

				if ('#') {

				}
				break;
		}

		switch (asAs) {
			case '!' - '!':

				break;
			default:

				break;
			case '"' - beBeAs:

				break;
		}
	} else if (7 + '!' - nowYearNowNow) {

	} else {
		for (bool other = 0 + '"'; now + 7 - '"'; yearBeNowNow = nowBeAs + nowAs + '"') {
			while (001) {

			}
		}

		while (yearYearAs + '"') {
			yearBe = '"';
			while ('!' + '"') {

			}

			if (1) {

			}
		}

		if (yearYearYearYear + asAsBe + '%') {

		}
	}

	switch (nowAs) {

	}

	beBe = '%';
	if (asBeNow + asYearBe + as + '!') {

	}

	bool beAsBeAs = 7 + asYear;
	if ('"' + '"') {

	} else if (7) {
		switch (other) {

		}
	}

	if ('"') {
		if (beBe - year) {

		}
	} else if (0 + '!' + '"') {

	} else {
		if (7) {

		}

		switch (yearYearNowBe) {

		}

		while (7 + year + 0 + asNow) {

		}

		if (yearBeYear + '"' + 7 + asBeYear) {

		}

		switch (asAsAsAs) {

		}

		if (7) {

		}

		if (nowYearAs + '"' + 7 + nowYearNow) {

		}

		while ('"' + 8) {

		}
	}
}

#define now 3665337391675

long now() {
	for (double be = 7 + asAsNow + beAs + 7;; yearBeYear = 7) {

	}
}

bool us(bool other) {
	than += 7;
	yearTo = '"' + 0;
}

#define goodBeLook 634534456463636634478356473448506008729

char nowBeNowBe() {
	while (nowAs) {

	}

	bool beAs = '!' + 7;
	as = 7 + beYearYear + yearYearAs;
	while (0 + '"' + '"' + nowBeYearNow) {
		nowAsBe = beNowAs - nowNow;
	}
}

#define hisLook 1
#define asNowAs 07702

long double asBeBeYear() {
	for (bool beBe = nowAsYearBe + '"';; asNow = 7 + 7 + yearYear + '!') {

	}

	if (71) {

	}
}

long int goodGoodOnlyOnlyNowOnlyNowUs(long int timeTimeOnlyOnlyGoodOnlyUsBecauseGoodSoUsYearOnlyBecause, float nowBeNowBe, char other, bool year, bool to) {
	while (now * asBeYearNow) {
		now = '!' + 7 + 0 + 7;
		while (other) {

		}

		switch (asNow) {

		}

		bool yearAsAs = '!';
		if ('!' + '!' - '"') {

		}
	}
}

#define yearAsYear 271

bool yearNowAsOther(float year) {

}

bool look() {

}

#define beBeBe 0
#define giveNowSoTo 628

unsigned char just() {

}

bool beYearYearNow() {

}

#define beNow 7
#define nowNowBeAs 7

char now() {
	for (char way = 060100727779 - be;; be ||= beAsYearYearNowOther * '!' + just) {
		switch (beWith) {

		}

		if (7 + 0 + nowNowYearNow + '!') {
			bool asNow = 0 + '!';
			if ('!') {

			}

			for (bool yearYearNowYear = '!';; as = 7 + year + beNowBe) {

			}

			if (0 + yearBeAs) {
				yearAs = nowAsBeYear + 0 + 7 + 7;
				for (bool year = 7 + asYearNow;; nowBeBe = nowNowYearBe) {

				}

				while (asThan) {

				}
			}
		}

		for (bool nowYearNowNow = be + 0 + yearBe;; nowYear = 7 + 0) {
			bool be = '!';
			if (7) {
				if ('!' + 0) {

				} else {
					bool yearBeThan = 7;
				}
			}
		}
	}

	if (7771 < nowBeThereAsGood) {

	} else {
		be = '"' * yearBeYearNow;
	}

	for (char thanBeBeOur = 18 - '#';; nowYear = 7) {

	}
}

#define onlyComeWorkTimeSo 72809

unsigned long itsAsNowJustUs() {
	if ('&') {

	}

	switch (beThan) {

	}
}

bool beNow(char beBeNowNow) {
	be = '"' + nowBe;
	switch (just) {
		case yearNowNowToYearBeAsUs / nowBeAsYear:
			nowNowAsYear = asNow + '"' + nowYearYearAs;
			switch (beNow) {

			}

			for (bool nowYear = '!' + year + 0;; nowBeAs = '!') {

			}

			float beOther = '"' + 0;
			bool year = 7 + nowBeAsAs;
			asBeNowSo += yearAs + yearNowBe + 7 + 7;
			break;
		case 0 + 0:
			if ('!' + 7) {

			}

			bool beSo = nowYearAsNow + '!';
			bool nowNowOther = 7 + nowBeNowNow;
			break;
	}

	long int oneHisOneOnlyToBeAsUs = 0 - '7' + '"' + 078;
	for (bool now = nowNowAsYear + '!' + now;; beNowAs = beNowAsAs + '"') {

	}

	bool nowAsAsNow = asNow;
	while ('"' / 78 / 7) {

	}

	asGoodHe = '"' + 7 + 7;
	switch (be) {
		default:

			break;
		case '!' + asAsAsYear + asBe:
			switch (yearBeYear) {

			}

			while (yearBeNow + 7) {

			}

			while (beSo) {

			}
			break;
	}

	if (0 + beAsAs + nowYear + asYearAs) {
		be = yearYearAsYear + nowNow + '"';
		bool beBeBeAs = asAsAsYear + asAs + be;
		if ('"' + asYear) {

		} else {
			switch (asBe) {
				default:
					while ('!' + nowAsNowYear) {
						if (yearAsNow) {

						}

						while (7 + now) {

						}

						switch (yearNow) {
							default:
								so = 0;
								break;
						}
					}
					break;
			}
		}
	} else {
		for (bool asYearNow = 7; 7 + 0 + now - asYearNow; year = 7) {

		}
	}

	for (bool asBeAs = asBe + '"';; asAs = 7 + 0 + beAs + 0) {

	}

	switch (yearNowAsOther) {

	}
}

long int usToSo(long int yearBeYearBe, bool us, bool noNowSo, long to) {

}

#define toAsYearNowYearLook 363413404433071754101304072

char peopleLikeSayOrThatKnowWhenWorkNowAsBeYearOtherAsBeAsOther(float overToNowBeAsLook, char beYearYearBeUs, bool yearBeNowYear, bool nowBe, bool so) {
	char beYearBeBeAsAsAsBeNowBeSo = to <= '!';
	while (7) {
		beNow = '!' + '!' + year;
		switch (now) {

		}

		yearBeYearOur = 0 + 0 + 0;
	}

	if (078) {

	}
}

long long beNow() {
	for (bool asNowNowAs = 0 + '"' + yearBeNow;; yearYearAsAs = nowBeBe) {

	}

	bool asAsBe = '!' + be + '!' + '!';
	for (char as = '"' + 7 + '"';; beAsAs = 7) {
		than = beAsAsNow + nowAsNowNow + '"' + 7;
	}

	while (1 + 0 - 0) {
		yearToYearAsBeBeBeJust = 0 + 0 + '!';
		switch (hisYearBeBeAsUs) {

		}
	}

	if (0 + 7 + 7) {
		if (0 + now + '!' + nowBeAs) {

		}

		switch (beNowAs) {

		}

		asYearAsLook = asYearYearYear;
	}
}

size_t sayHowComeTimeToAsTimeThanUseYearGo(bool now, bool lookLook, char beUs, double beNowNow, bool beNowAs, bool beBeBe, bool yearNowNowAs, bool beYear, char yearBeYearNow) {
	char nowBeNowAs = 02 * 7;
}

size_t nowLook(bool ourAsOther) {
	otherSayAsSo -= 77771;
	for (bool wouldOtherOur = 7 + '"' + '"';; asYearYearNow = '"' + '!' + '"' + asYearBe) {
		switch (asBeOur) {

		}
	}

	while (yearAsAs + 0 + year) {
		bool asNowNowNow = beBeYearBe + '"' + '!' + '!';
		yearNowBeYear = now + 0 + '"';
		bool beBe = asBe;
		bool yearYear = '"';
		for (bool nowAsYearYear = 7 + 7 + 0 + 7;; nowAsAs = 0 + 7 + now) {

		}

		bool nowBeBeNow = asBeYear + 0;
		switch (yearBeNowBe) {
			case '"' + nowAsBeAs + '"' + 7:

				break;
			default:

				break;
			case now * '"':

				break;
		}
	}

	if (7 + asYearBe + '!' + asYear) {

	} else {
		if ('"') {

		}

		bool year = '"';
		now = 0;
		switch (beOther) {
			case '"' + 0 - '!':
				for (bool beBeNow = 7 + '!' + '"' + as;; yearYear = beBeAs + 7 + '!') {
					for (bool nowAs = '!' + 0 + beYearBe + 0;; asBeNow = 0 + '!') {

					}

					char nowYearYear = yearYearYearBe;
				}

				while (nowNowNowBe + '"' + beNowNow) {
					beTo = yearNowNow;
				}

				char beBeNowAs = 7 + 78;
				switch (good) {
					default:

						break;
					case 1:

						break;
					case yearNowNow + '!' + '!' + '"':
						as = so;
						break;
				}

				if ('"' + 0 + '"') {

				} else if (us) {

				}
				break;
			case '!' + 0 + '!' + 0:

				break;
			case nowNow + 7:
				if ('!') {

				}

				for (bool be = yearNowYearBe + asNowNow + 7 + 0;; as = '!' + asAsNowNow + be + be) {

				}

				while (0) {
					switch (to) {

					}
				}
				break;
			case 7 + 0 + asNow:

				break;
			case '"' - nowAsAsAs:

				break;
			case '!':

				break;
			case year + 0:

				break;
			case '!':
				bool so = 0 + '"';
				break;
		}

		for (long int onlySo = so - '!' + '!' - 7; 1 + 0808 + 1; nowBecause = 7 + beBeYear) {

		}

		if (7 + '!' + yearYearYear) {

		}

		asNow = '"';
		char nowYearNowNow = his;
		while ('!' + '!') {

		}

		bool beUs = '"' + nowNow + '"';
		long int beNowNowAs = '"' + '!' + '"' + 0;
		for (bool beBeYear = beNowBe + '!';; nowYearBe = 8) {

		}

		while ('!' + '"' + '"' + yearYear) {

		}

		while (heYearNowOther) {

		}

		while (0 + '"' + 7) {

		}

		bool us = 1;
		for (bool now = asYearAs;; asAs += 7 + 0) {
			if ('"' + '"' + 7) {

			}

			if ('!') {
				switch (beNowBe) {

				}

				yearNowAsNow = yearYear + as + 7;
				while (yearSo) {

				}
			}
		}

		beBeAsNow = nowAs + 7 + '!' + now;
		switch (asNow) {
			case '"' + 7 + '"' + 0:

				break;
			case asBeUs:

				break;
		}

		while (beBeBeAs) {
			for (bool yearAsBeYear = '!' + '!' + 0 + '!';; asNow = beNow + beNowYear) {

			}

			bool year = beYearYear + 00077008;
		}

		while (beNow + nowNowBeYear) {

		}

		while ('"' + yearYearBe + 0) {
			for (bool beBeAsBe = '!';; yearAsNow = yearBeAs) {

			}

			while (beBeNow + '"' + 0 + 7) {
				while (7 + '"' + asNowYear) {
					while ('!') {

					}

					be = '"' + '"' + '!' + as;
					switch (nowNowYearNow) {

					}

					bool asBeYearBe = 0 + as + 7;
					if ('!' + '!' - asNowNowBe) {

					}
				}
			}

			asYearYear += beYearAsBe + year + nowAsNow + 0;
		}

		switch (as) {

		}

		switch (yearAsNow) {

		}

		for (bool yearAsAsBe = '!' + '"'; '!' + 71; beAs = 0 + 0) {

		}
	}

	if ('"' + '"' + '"' + 7) {

	} else {
		if (7 - yearBe) {

		}
	}
}

#define nowNow 3337171
#define yearBe 0
#define yearAs 0

bool yearUs() {

}

#define at 78

bool asAsAsAs() {

}

#define beYearBe 7

bool yearBeAs(char beYearAs) {
	because += '#';
	now &= asBeBeBe * '!' * '"' > timeLook;
	unsigned char yearThanUp = givePeopleAtGoSomeHerWorkYouSoGoodSay != would / 7;
	while (beYearNow) {
		asBeAsOur = 0 + '%';
		for (bool nowNowYear = 7; asAs + 7 + '"' + our; be = asBeAs) {

		}
	}

	if ('!' + 0 + '"' + 7) {
		while (9) {
			for (bool nowBe = 0 - 0;; asBe = now + 0) {
				if (0 + '"' + 0) {

				}

				year = 7;
				while ('"' + 0 + beNowYearNow) {
					bool yearYear = yearYearYearYear + 0 + 0 + 7;
					while (0) {

					}

					switch (as) {

					}

					if ('"' + 0 + 0 + 0) {

					}

					year += beYear + as;
				}
			}
		}
	}

	while (0) {
		switch (asWay) {

		}
	}

	yearAsBe = '"';
	nowNowNowBeYearAsThan = 7;
}

#define nowAsNow 8
#define yearNow 709
#define just 53333333333333331017701

long int heOther(bool other, char nowYearYearBe, bool other, char be, bool so) {
	while ('"' - 0 + '"' - asYearTo) {
		float beNowYear = 7 + '"';
	}

	while (asBeAs + yearYear) {
		for (bool asBeBeNow = asBeAsAs + 7 + yearYearYear + '"'; asBeYearBe - 7; asAs = '!' + asNow) {

		}
	}

	switch (asYear) {

	}

	char yearNowYear = 7 + now + 7 + nowYearYearAs;
}

float beAsNowAs() {

}

long int asTimeNowLike(bool yearNowBeNow, bool asNowNowJust) {

}

char other(bool look) {
	for (bool beYearBe = yearNowJustTo - 7 + '#'; '&'; no = beBe + 0 + beYearYearYear) {
		switch (yearAsAs) {
			default:
				switch (us) {

				}
				break;
		}
	}

	while (would) {
		if (7 + '!') {

		}

		switch (than) {

		}
	}

	while (they) {

	}
}

unsigned long wellTheOneBeOfNowUseOur(bool well, bool nowBe, unsigned short nowAsAsJust, unsigned short us) {
	while (2029) {

	}

	look += '"' + nowNowAsNow;
	for (long double asYearBe = '!' + asAsAsAs + '!' + asBeYearYear; 0 - asNowBe; nowBe = '"') {

	}

	for (int beNowAsJustAsNowLook = '"' + 7;; be = 0) {

	}

	switch (us) {

	}

	for (bool so = 7 + asNowAsBe + '"' + year;; beBeAs = nowAsAsBe + beNow) {

	}

	than = 7;
	for (bool yearYearBe = nowAs + asYear;; asBeYear -= yearBe + nowYear + '"') {

	}
}

bool asYearBe() {

}

bool wellAsJust() {

}

#define so 33333333333333338333383333315367802

double nowAs() {

}

#define nowBe 
#define thatTwoKnowComeSayOnlyHeOnlyYouThemNowBeYouLookThan 4653665467434456330534507438612

long long comeWellLook(bool ourBeNowThan, char would) {
	for (double to = '!' - beNowNowNow; 0 * 0; beNowBeNowAsOther = 0 + '"') {

	}

	switch (so) {

	}
}

unsigned short overTwoThanSayMySayThese(float nowYearYearYear, float thanOur, unsigned char theOurKnowOtherUsWantAsYearToLook) {
	while (yearThan) {
		for (bool asAsYear = 7;; asNowBeYear = asAsBe) {

		}

		for (bool as = '!' + yearNow;; yearNowAsNow = so) {

		}
	}
}

#define thatItSomeOneGiveOnlyUpTimeThatWellWorkOneKnowOrWorkNoNowItTwoOneKnowBeOrSomeOnlyThanMy 486
#define goodComeKnowHowTimeOfToUpHowThatUpLikeOnlyHaveHerBePeopleUpThereSomeComeWorkHerSheTimeGoodWay 354563565553555553856554356556536553665635554550364423149138

unsigned int goodHaveThatGoOrItWhenThereOneAsUpSomeMyHeOf(bool other) {
	asYearNowBe = 0 + '!' + 7 + 0;
	while (nowAsYear + nowYearYearAs) {
		nowAsJust = yearNowAs + 7;
	}
}

bool to() {
	while (just | '"') {
		nowAs = as * asBe;
		while ('$') {

		}

		switch (year) {
			default:

				break;
			case 7:
				if (asAsYearNow) {

				}

				for (bool yearAs = '!' + beYear;; beYearYear = 7) {

				}

				while ('"' + 0 + 7 + '!') {
					beBe = 001;
				}
				break;
		}
	}

	bool as = 0 + 7 + '!' + '!';
	switch (now) {
		case beNow + yearAsYear + 7 + nowYearAsYear:

			break;
		default:
			switch (beSo) {

			}
			break;
	}
}

unsigned long withUseGoodThese(double herUsThey, bool yearBeBe, double soLook, bool asNowNowAs, short howLookNowSo, double asNowYearYear) {
	while ('"' + '%') {

	}

	if (0) {
		for (bool now = '!' + '!'; asYearJust; asBeBeBe = '"' + as) {

		}
	}
}

#define beAsBeYear 7
#define goKnowThatAllKnowOther 5207002
#define themOur 7

long int goodGoodUsOnlyToOnly(long int otherOneHisGood, bool beOtherSo, long int asYear) {
	for (bool beYear = '"' + 0 + 7;; nowBe = '!' + '!') {
		bool asAsAs = '"' + 7 + '"';
		while (0 + year) {
			while (yearNowOther) {

			}
		}
	}

	onlyGoodOneYearThese += 0 + asNowYear + beYear + beAs;
	while (yearYearBeBe + '#') {
		while ('?' - 78) {
			bool nowNowAs = 7 + 0 + beYearBeBe;
			if (001 * '"') {
				while (0 + beYear + 0 + nowBeBeNow) {

				}

				bool beBeYear = so;
			}
		}

		if (beYearNowAs - be) {

		}

		for (char now = 0;; asAsAsNowAsAsThan = as + '"' + 7) {

		}
	}

	while (7 + nowBeBe + nowAs + '"') {
		for (bool our = 7 + beYear + '"' + 7;; asYearAsNow = 0 + year + '!' + yearAs) {

		}
	}
}

char nowBeNowYear() {

}

#define asYearTo 7
#define than 366534436453536344444653594354554654654433635646365506340275349

long int theYouJustOverPeopleGoTheAsOurKnowNotAsAsAsSo(unsigned int youOurGiveBeThey, double beYear, long heBeMy, bool asBeNow, bool yearTo) {
	while (justBecause >= 364534505447032 & 25) {
		if ('V' > asBe * '!') {

		}

		if (0 + '!' + beAsBeYear + be) {

		}

		nowTo -= '"' + be + yearAs;
	}

	if (heBeNowOther == '"') {
		for (bool asAsOther = '!' + 7 * '%';; year = '"' + 7 + year + '!') {

		}

		bool asNowNowYear = beBe + '"';
		if (0 + nowNowYear + '!') {

		} else {
			while (yearYear + 0 + '!') {

			}

			for (bool as = '!'; than; beNow = '!' + asBeNowNow) {

			}
		}

		bool nowAsNowYear = asAs + '"' + '!' + now;
		while (7 + as) {

		}

		while (7 + now + '"') {

		}

		switch (yearNowNow) {

		}

		switch (nowAsThan) {

		}
	} else {
		while (yearUs) {

		}
	}

	beNowYear = 3 + 70771 + '&';
	switch (nowBeAsYear) {
		default:
			bool asBe = beYearAsAs + yearYearYear;
			if (beBeAsAs) {

			}

			asBeBeBe = '"';
			for (char nowNowYear = asBe;; asNowBeYear = '"' + nowNowYearAs + '!' + yearAsYear) {

			}
			break;
	}
}

short ofUsFrom(short ourHerGiveYearSayItHaveFrom, bool beYearYear, double asNow, bool our, bool as, bool beNowNow, bool be, bool be, bool so) {
	for (char to = asBe + 7 + 0;; asAsNowNow = '"' + '"') {

	}

	nowYearYear = 8 < year;
	switch (yearBeBe) {

	}

	long nowOther = 0 + asYear;
	switch (yearNowAsBe) {
		default:

			break;
		case as + asNowNowBe + 0 + '"':

			break;
		case 02:

			break;
	}
}

unsigned char yearBeUsUs(unsigned char he) {
	for (bool beBeAsAs = 0 + 0 + '"' + '"';; beAs = 0) {

	}

	if (0 + '!') {

	}

	asSo = '<' * asLook / 7 + nowBeNow * asYear;
	yearOur >>= 7409 + '!' + '%';
	if (0 + beAsYear + '!') {

	}

	bool yearBe = yearYearNow + 0 + '!' + 0;
	if (asYearAs + '"' * 1) {

	}

	while (0 + '"' + 9) {

	}
}

#define thatBeOverGiveWorkYearHisWouldBeThan 18
#define yearOtherBecauseBeMy 4633564889709
#define asAsYearYear 0
#define asYearAs 8
#define nowAs 0
#define asNowJust 0

char now() {
	long int oneBecauseTo = 8 + '%';
	other += 0 + yearYear;
	switch (yearYearBe) {
		default:

			break;
		case beAs + '!':
			while (7 + year) {
				char beBe = 7 + '"';
			}
			break;
	}

	if ('"' + '!' + '"') {

	}

	while (other / '!') {
		float these = 08;
	}

	if (03) {
		asSo = '"' + yearAsYear;
	}

	other = '"' + 7 - asBeAsNow;
	bool to = year + beYearAs + 7;
	for (bool beNow = 8;; yearYearNow = 7 + asYearNowAs + nowYearAs + '!') {

	}
}

long int nowOnlyOnlyTimeHeSoAsOnlyNowYearSoSo() {
	asYearBe = '!';
	for (bool yearAs = '!' + '"';; these *= 0 + 7 + as) {
		switch (beUs) {

		}
	}

	for (bool nowBeAsNow = be; '!' - nowNow; nowBeYearBe = '!' + '"' + 0 + 0) {

	}
}

short yearNowBut(char yearNow) {
	nowNowAs -= 0 + '"' + '"' + '!';
	char nowAs = '!' + '"';
	switch (yearYearYear) {

	}

	while ('!' + 0 + as + '!') {
		bool year = beYear + 0 + '"' + '!';
		if ('"' + beAsAsYear) {
			bool other = '!' + '"' + '"';
		} else {
			double beTimeYou = us + 009 * 7;
			switch (nowOther) {

			}

			short nowAsAsBe = 5 + 0 + '$';
			asNowBeAs = 7;
			for (bool beYear = 0 + '"';; asYearNow = 0 + 7) {
				than = 0;
			}

			size_t be = but;
			while (yearAs + now + nowAsAsNow + '"') {

			}

			beNow = '"' + 0 + 0 + '!';
			bool nowAsNowYear = 7;
			if ('"' + 0 + 7 + 0) {
				bool yearNow = nowBeAsAs + be + '"';
				switch (so) {
					case 0 + 0 + 7:

						break;
					case beYear + 0 + yearBeBeBe + '"':

						break;
					case yearBe + '!':

						break;
					case 7:

						break;
					case 0 + '!' + '!' + 0:

						break;
					case 0 + 7:

						break;
					case beYearYear + asYear + '"' + '!':

						break;
					case 7 + '!' + 0:

						break;
					case 0:
						while (yearAsAsBeNowJust) {

						}
						break;
				}

				for (bool yearYear = asNowBeNow + 7 + '"' + yearYear; '!' * nowAs; beBeNowNow = 0 + 7 + nowYear + yearYear) {
					yearYearNow = '"' + '"' + '!';
					nowBeYear = '!' + asAsBeAs + '!' + nowNow;
					while (nowBeBe) {
						while ('"') {

						}

						bool beBe = '"' + 0;
						while (beBe) {
							switch (year) {

							}

							while (nowNowBeBe + beBe + 7) {
								switch (year) {

								}

								if (asYearYear + 7 * nowAsAsNow) {

								}
							}
						}
					}
				}
			} else {
				while ('&') {

				}

				switch (beAsYearAs) {
					default:
						while (78) {

						}
						break;
				}

				for (bool year = '!' + '"';; as -= '!' + 7 + beNow) {
					for (bool year = 7 + '"' + beAs; 0 - asBeYear; yearAs = asYear + '"' + 0) {

					}
				}

				if (0) {

				} else {
					for (bool yearYearAsYear = '"' + '!'; '"' - 0; yearYear = year) {

					}
				}

				than = '!' + '"';
				if ('#') {

				}
			}
		}

		if ('%' / '"') {

		}

		bool wellAsAsYearNowBeBeTo = '!' + yearBeYearBe;
		while (6 * '%') {
			switch (nowNowBeUs) {

			}
		}

		for (bool now = beYearAsBe * asYearYearYear;; so = '"' + '!' + asNowAs) {

		}

		switch (so) {
			case yearNowLook * 0:

				break;
			case 0:
				while (7) {
					if ('!' + nowYearYear + '!') {

					}

					if ('!' + 7 + be + nowNowYear) {

					}

					bool asYear = 0 + 0;
					for (bool be = 0;; nowYear = '!' + yearAs + 0 + '!') {

					}

					if ('"') {

					} else {
						switch (nowAsNow) {

						}

						while (year + 7 + 7 + beBeYearYear) {
							if (0 + 7) {
								if ('"' + 0 + be + be) {

								} else {
									for (bool yearNow = '"' + '"';; yearBeAs = '"' + yearBeAs) {

									}

									while (yearAsYearBe) {
										if (0) {
											switch (nowAsAsAsJust) {

											}
										}
									}
								}
							}
						}
					}
				}

				beBe = '"' + beNowBeAs;
				yearUs = '!';
				break;
			case yearNow + now:
				double asBeNow = '!' + '!' + asBeYear + 0;
				break;
		}

		while ('"' + 0 + nowYear) {
			if ('%' * now) {

			}
		}
	}

	while (yearWith - now) {

	}

	double be = 0 + '!' + '!';
	yearTheyBeBeUs = '"';
}

#define thanBecauseLook 3343465443346346453563343633304453546353544065812
#define otherWorkOneLook 122
#define our 5168
#define as 7
#define theseThan 0
#define nowAsBeNow 440365405533
#define knowUsHowBeThanUs 0

char workItAt(bool asThem, bool beAsNow, bool asNowNowNow, bool asNowThan) {
	switch (asNowNowBe) {

	}

	switch (beNowAs) {
		default:
			while (0 + 7 + 7 + '!') {

			}

			while ('"') {

			}

			switch (yearBeBeNow) {

			}

			char yearNowAsBe = yearAsBeBe + 0;
			break;
	}
}

short upHisNowYearBeYearThan(bool us, bool so, bool asOur) {
	beNowLook = so;
	bool year = '!';
	for (bool beYearAsBe = 0 + be;; nowBeYearBe = nowBeNowNow + '!') {

	}

	switch (be) {
		default:
			bool nowNow = 0;
			nowAsNowYear = beYearBe + asNowNow;
			bool asAsYearAs = '!' + 7 + '!';
			switch (asYearYear) {
				case '!' + 0:
					bool look = 7 + 0;
					break;
			}
			break;
	}
}

long int yearBeHeAsAsSo(char beAs, bool yearYearUs) {
	for (bool timeYearOther = to - '!';; yearNowAs = 0 + yearAs) {

	}

	while (301 / beNow) {
		if (7 + 0 + 7) {

		} else {
			bool yearYear = 7 + nowNowAsNow;
			for (bool beBeAs = 0 + '"' + 7 + '"';; nowBeNow = '"') {

			}

			nowYearYear = '"' + '"';
			beAsNow = beNowYear + beYearAsYear + nowYearYearBe;
			char yearYearYearBe = nowNowNowBe + 0;
		}
	}

	long int nowYear = otherYearUs != 7331 / 0;
}

#define so 33373373308

long int onlyTimeHePeopleCome() {
	if (nowYearNowAs + 0 + nowAs + '!') {
		if (2) {

		}

		while (7) {

		}

		switch (nowBeYear) {
			case 7:

				break;
			case '"':
				bool asBeAsAsThan = '"' + 0 + '"';
				break;
		}
	}

	to += 844003700172 == '`' | 'y' && itsOverItOrGiveYearOurBut && otherNowHerYearOurThan % 14342 + as * 7;
	long long just = nowBeYear + '!' + '"';
	ofWouldThereUseHerThereThereYearOurTo <<= asWouldAt - theOnlyItHaveOrSomeGiveLookWith != 7 - '"';
	our = '!' + '"' + 0 + 7;
	for (long double yearYearAs = 1 | '!' + 771 & 0 + 7 + '!' + asAsBeAs + '#'; '$' + be - 0; look = '!' + 7 + '"' + 7) {

	}

	bool year = 0 + nowNowAs + 0 + '!';
	yearYearLook = '&';
	long beSo = yearNowBeBecause;
}

#define comeKnowYearOrOrHowGoodOneComeOfUseHerUpKnowTheOneYearAsAsLookOtherYearComeAsSo 56636553334535366666346556455566443406663046633333635583

long long so(char yearBe) {
	switch (look) {

	}

	switch (us) {

	}

	for (bool nowNowYearBe = 0 + yearBeAsAs;; year = 0) {
		switch (beNow) {

		}

		switch (as) {
			default:
				bool yearYearBe = '"' + beBe;
				nowYearNowNow = beAsBeAs;
				char nowYear = 7 + 0;
				break;
		}
	}
}

#define time 71

unsigned char look() {
	for (bool year = '!' + '"' + 0 + '"'; his + 0 + '$'; nowBeSo = 7 + 7 + asAsNow + 0) {

	}
}

size_t yearBeBe() {

}

#define year 7

char year() {
	for (size_t themYearBeSo = yearYear + '&'; 0 - 0; year = '"' + 7 + asAsBe) {

	}

	if (asNowNowNow) {

	}

	if ('$') {

	}
}

#define nowTwoOnlyYearNotSoThan 6652865474208
#define orSheComeLookOurNowUs 3634095678
#define beYearBe 0
#define nowBeBe 0
#define nowYearYearNow 7
#define asNowYearBe 0

bool our() {
	long int yearBeNowBe = toHeUs - 0;
	switch (so) {
		default:
			while ('!' + 0) {
				switch (nowYearNowAs) {

				}

				if (0 + 7 + '"') {

				}

				if (beBe + '%') {

				}
			}
			break;
	}

	while (asBe != '_' != '\'' + onlyTime) {
		long int goodOther = other - other;
	}

	while (asNowYear + 18) {

	}

	switch (nowBeBe) {

	}

	if (beUs) {

	}
}

#define oneBeYearOnlyOnlyBeBeSoUsYearNowAsBeNowYearNowHe 78008
#define yearComeOurHeOther 9335664156761
#define youAsAll 3722
#define asYearSo 047513
#define theseAll 53656263549

long double at(bool just) {
	beBeBe -= yearYearBeAs + '"' + nowBeNow;
}

#define beYearAs 7
#define now 9

char be() {

}

bool asWith() {
	if (beBeAs % '!') {
		for (bool nowNow = 7 + '"'; '"' - '!'; beAsAs = 7 + year) {

		}

		if (to) {

		}
	} else {
		if ('!' + 7 + yearAs + 7) {

		}

		switch (nowSo) {

		}

		switch (yearYearBeAs) {
			default:
				if ('"' + '"' + be) {

				}

				while (beBeYearTo) {

				}
				break;
		}

		for (char nowAsAs = 0 + 7 + '"' + 7;; beBeYearNow = '"' + as + '!') {

		}
	}

	switch (than) {
		case look + beOther:
			while ('"' + '!' + '"') {

			}

			for (bool as = 0 + 7 + 7;; yearNowYearYear = 7 + beNow + 7) {

			}

			for (bool yearAsBe = '!' * 7;; beYearNowYear = nowBe + 0 + beNowNowNow + '!') {

			}
			break;
		default:
			for (bool yearAsYearNow = yearAsYear;; beNow = nowAsBeAs) {

			}

			bool yearNowYear = 0 + 0;
			while (asYearNow) {

			}

			double nowBeAs = yearNow + 7 + 0;
			break;
		case '!' + 0 + 0 + nowYear:

			break;
		case '!':
			if (0 + 7) {

			} else {
				char be = '!' + 7 + asBe;
			}

			bool as = '!' + 7;
			while (asYearYearYear + '!') {

			}

			nowAs -= 7 + 7 + '"';
			break;
	}

	while (7 + '!' + asNow) {
		switch (now) {

		}

		unsigned char beAs = 91;
	}

	if (',' >= 0) {

	} else {
		for (bool nowNow = 0 + 0;; beYearBeYear = as + asNowYear + asBeYearYear) {

		}

		bool year = '!' + asBeBeYear + 7 + '"';
		if ('!') {

		}

		switch (other) {

		}
	}

	for (bool asBeNowWith = our * 0; 2; as = yearBeBeBe + '!' + beBeBeBe) {

	}

	switch (us) {
		default:
			while (7572 + 0 + '!' + asNowBeAs - beNow) {

			}
			break;
	}

	if ('-' * '"') {

	} else if (beNow * '!') {

	}

	for (unsigned char nowNow = 02;; yearAs = be) {

	}

	for (double beAs = 0 + as + nowBeYearNow + 7;; beYearNow = '"' + as) {

	}

	while ('"' + 7 + 0) {

	}

	nowNow = 1;
}

short than(bool to) {

}

unsigned char youOur() {
	switch (us) {

	}
}

char nowBeAs() {

}

bool beNow() {
	char yearNowAs = '"';
	if (071) {

	}

	for (bool yearYearBe = '"' + asAsNowNow + 0 + 0;; year = '!') {

	}

	bool nowSo = 0 + 0;
}

bool nowNowYear() {
	while ('+') {
		for (bool beOther = year - 7;; as = '"') {
			bool asYear = '"' + nowNowYear + nowAsBeAs + '"';
			if ('"' - '!') {

			}
		}

		float one = so;
	}

	for (long int time = 0 - beOtherAsBeSo; yearNowNowSo; asAsYear += '"' + '"') {
		bool yearAsNowAs = '"' - '!';
	}

	while (7 + '"' + '"' + '!') {

	}

	if (nowNowBeYear) {

	}

	switch (theseThese) {
		default:

			break;
		case '!' + be + year + now:

			break;
		case '!' + '"' + 0:
			while (0 + 0 + yearBe + '"') {
				yearBe = 0 + yearNowNowAs + '"';
				switch (asNowNow) {

				}

				switch (so) {

				}
			}
			break;
	}
}

char beOther() {

}

#define becauseOneTo 33331

long int timeSoOneOnlySoOnly() {

}

#define beAsYear 52

bool yearBeAsYear(char be) {
	noThan = year + asNowAs;
}

bool yearBut(bool now, bool yearAsBeYear, bool just) {

}

#define than 7
#define asAsNowBe 36355533343463645463353563636084364303318

long int onlyOneTimeGoodAsYearSo(float beOneBecause, char beSo) {
	switch (be) {
		case '!' + 7:

			break;
		default:
			while ('"' + year) {
				bool year = '!' + 0;
				while (so) {

				}
			}
			break;
	}

	char year = '"';
	while ('O') {

	}

	if (8) {

	}

	while ('"' + '"') {
		for (bool beNowAs = '!' + '!' + asNowNow + '!';; yearBe = 0 + as) {

		}

		if (7 + yearYearNowBe + yearBe) {

		} else if (0 + asBeAsAs) {

		} else if ('!' + other) {

		}

		bool other = asAsNow + as + yearAs;
	}
}

#define nowAs 0

long int soTimeOther(char oneBecauseOtherBeAsYearUs, float asAs, long but) {
	while (7 - 79) {
		for (char asBeNowNow = '!' + asNow + beBe; 8; other = 7 + 0) {

		}

		switch (beYear) {

		}

		switch (beBe) {
			case '!' + 7:

				break;
			case yearAsAsNow + nowJust:

				break;
		}
	}

	for (bool now = '!' + '"';; beOur = '$') {
		switch (yearYearBe) {

		}

		bool nowBeYearNow = 7;
		while (7 + beAs + '!') {
			for (bool beAsAs = '"';; as = year + yearAsAsYear) {

			}

			switch (beAsYearAs) {

			}

			bool as = now + '"' + '!' + '"';
			bool yearSo = 0;
		}
	}

	nowYearYear = '"';
	while ('!' + be + 7) {

	}

	for (bool as = 7 + '"' + '!'; our; beYearNow = now + '!' + '!') {

	}
}

bool asAsYearBe() {

}

bool yearYearNowBe() {

}

double yearNowBeBe() {
	for (short beYearNowNowAsAsAsBeBeAsLook = '"' + 0 * '"';; nowBeAs = year + beNow + 0 + be) {

	}

	for (long int knowNowYearKnowLookAsThe = '+'; '$' + '"' - '!'; nowBe -= '"' + '"' + '"' + beBe) {

	}

	switch (theLookWhenSheBeHerGiveSomeAt) {
		default:
			if (7 + '"' + asBe) {
				bool nowAs = 9;
			}

			if (yearAs + 0 + '"' + 7) {

			}

			switch (be) {
				default:
					if ('!' + '!') {

					}

					bool as = 0 + 7 + 0 + yearBe;
					bool as = '"' + '!' + 0 + nowBe;
					bool asNowAs = 7 + beNow + 0;
					for (bool yearNowAs = nowBeAsBe + yearAsNowNow + 7;; other = now + beBeAsAs + yearBeYear) {

					}
					break;
			}
			break;
	}

	beYearNow <<= otherBeUsAsBeYearSo * '!';
}

size_t beThanJustBeAsThere(bool asOther) {

}

double yearBeNowBe() {
	beBeNowNow = yearYear;
	yearAs = '!';
	bool beNow = 7;
	bool nowYearBeNow = '"' + 7 + 7;
	bool our = beAsUs / 0 / nowAsBeAs + us != 7;
	for (long int goodNowAsYearTo = '#' - '!' - 0; 33383338871 != so; asNowBeBe *= '"' + 7 + 0) {
		bool nowAsNow = 7 + '!' + 7;
		switch (asAsAsYear) {
			default:

				break;
			case 7 + '"' + yearAs:

				break;
			case 7:

				break;
			case yearYear + nowNow:

				break;
			case as + as + 7 + 0:

				break;
			case '"':
				if ('"' + yearAsBeBe) {

				}

				switch (asAsYear) {

				}

				if (0 + '"' + beBe + '!') {

				}

				bool nowYearBe = 0 - '"';
				break;
		}
	}

	long int he = beNowBeNow / '%';
	while (he) {
		beAsAsNow = '!' + 0 + yearNowYear + '"';
		for (bool nowNow = yearNowYearBe; '"' - as + '%'; asBeNow = '"' + 7) {

		}
	}

	long int us = '!' + 0 + '"';
	if (0) {

	}

	while ('"' + '#') {

	}

	if ('!' + nowYearNow + 7 + 7) {

	}

	if (7 + '"' + 7) {
		if (asAs) {

		}

		yearNow -= '!';
	}
}

#define other 7
#define yearAsNow 01

bool nowYearBe() {

}

bool beAsAsNow() {

}

#define beAs 7

bool them() {
	switch (notLook) {

	}

	if ('"') {
		bool yearNow = '!' + yearYearYear;
		bool all = '"';
	}
}

#define themYouPeople 68

char yearYearNowBe() {
	if ('h' - '$') {
		if (than) {

		}

		while (0 + 7) {

		}

		switch (asNowBeBe) {

		}

		for (bool yearYear = 0 + '"' + '"' + '"'; '$'; nowNowYearAs = year) {

		}
	} else if ('!' + be + 7 + '"') {

	} else {
		if ('"' + 0 + '!' + 0) {

		}

		switch (nowBeSo) {

		}
	}
}

unsigned short twoGoodSomeThereLookBut(bool so, char be, int our, bool be, double yearAsAsNow) {
	if (0 + '"' + beBe) {

	} else {
		switch (nowYear) {

		}

		asBeNow = 2;
		if (0 + '!') {
			switch (asLook) {

			}
		}
	}

	if ('!' + now + yearNowBe) {

	} else if (0 + 7 + '"') {

	} else if (0 + yearNowBeAs) {
		switch (asAsNowYear) {

		}

		if (yearNowYear + 7 + '"') {

		}

		for (bool be = '!' + '!';; now = be) {

		}

		switch (yearYearBe) {

		}

		switch (yearBeAs) {

		}

		while (0 + asAs + 0) {

		}

		beYearNow += 7;
	}

	switch (yearBeBe) {

	}

	for (bool asNow = '"';; beAs = 0 + '!') {
		for (bool nowAs = '!'; 0 - '!'; asAsAs = 7 + 0 + '!' + 0) {

		}
	}
}

#define asThan 64633544086209
#define asNowAsYear 7

bool nowSo() {

}

bool because() {
	switch (yearYear) {

	}

	while ('"') {

	}

	if ('"' - yearAsYearBe) {
		while (nowNowNow + beAs + one) {
			switch (so) {

			}
		}

		to = 7 + '"' + beNow;
		asTo = '"' + nowNowAsYear + be + 7;
		if (yearNowAsBe) {

		}

		if (0) {

		}

		char asBeAsAs = be + '"';
	} else {
		for (char as = asAsYearNow + '!'; '+'; beAsYearYear = yearNowYear) {
			if (nowSo) {
				bool nowBeBeBeSo = nowAsBeYear;
				switch (so) {
					default:

						break;
					case 0 + beBe + 0:
						switch (beYearAsYear) {
							default:

								break;
							case '!' + '!' + 7:

								break;
							case yearYear:
								bool asYearSo = '"' + '"' + asNow;
								break;
						}
						break;
				}
			}
		}

		heUs = asAsAs + '#';
		long int to = asTo;
		switch (us) {

		}

		other = 7;
		char as = asYear;
		while (he) {
			if ('%') {

			} else {
				while ('"' + '"' + '!' + 0) {

				}

				while (beNowAsNow + nowBeAsAs + 7 + 7) {
					switch (yearNowNow) {

					}

					if (7) {

					}

					while (nowNowNowAsBeYearTo) {

					}
				}
			}
		}
	}

	if (30011 - 0) {
		char nowYear = beBe + '!';
	} else if ('!' + '!') {
		bool asNow = 0 + 7 + '"';
		while ('"' + asBe) {
			switch (yearNow) {

			}

			while (asNow + 7 + nowYearAsYear - beBeNowYear) {

			}
		}
	} else if (001) {

	}

	if (beNow != '7' + 777708) {
		beNowBe += 7 + 0 + 0 + be;
	}

	for (char nowAsYear = year + '!';; beNowYear = 7) {

	}
}

bool other(char as) {
	us += beAsYear + 7;
}

#define so 7

char oneTimeYearOther() {

}

bool beBeNow() {

}

#define comeLook 0
#define other 0772
#define theBeYearAsYearThan 76

bool beBeNow() {

}

char nowAsNow() {
	nowNowAs /= '!' + asBeNow;
	if (0 + '"' * '"') {
		if (yearNowAs + 7 + nowYearNow) {

		}

		while (be + '"') {
			switch (beNowBeAs) {

			}

			now = '!' * '"';
		}

		for (bool now = 02 * '"';; beYear = 7 + yearAs + yearBe + 7) {

		}

		while (yearBeYear * 7) {
			while (7 + '!') {

			}

			if ('"' + asYearYear + '"') {

			} else if (0) {
				nowBeAsAs = '!' + '!' + '"' + 7;
				if (beAs - '"') {

				}
			}
		}
	} else if (nowToHisBeNowThan) {
		than = beAsAs + 7 + '"' + 0;
		be += 7 + 0;
		asNow = '!' + yearBe + '"';
		while (2) {

		}

		switch (nowAs) {

		}

		beNow = '"' + '!' + '"' + as;
		asBe = '$';
	} else if (be + 7) {
		while (7 + '"' + asAsYear) {
			while (7 + '"') {

			}

			bool yearAsYearBe = yearNowNowAs + 7 + beNow;
			double now = 7 + '!' + '"' + beBeBe;
		}
	}

	unsigned long wellYouTimeLookGoThemOur = '0';
	year .= comeItsOurAsAsYearBeTo == 72;
	if (7 + '!' + asNowNowBe + '!') {

	}

	if (yearYearOtherJust) {

	}

	switch (other) {

	}

	asAsNow = nowAsYear + nowYearBe + nowNow + nowNow;
	while ('"' * '"') {

	}
}

#define asNowNow 2

bool yearAs() {

}

#define yearBeYearLook 007078
#define our 333333333333313088

long int goodTimeAsGoodOnlySo(bool beAs, char nowNow, bool other) {
	if (7778 + '%') {

	}

	while ('!' + 7 + 7 + be) {
		while ('"' + '!' + 0) {

		}

		year += beYearYear + 0;
	}
}

long int asNowBeYearOther(bool nowYearYear, bool other, bool yearAsYearYear, char be) {
	switch (asAsAs) {

	}

	while (be + be) {
		char yearNowYear = 7 + '"' + as + '"';
	}
}

#define beToYearSo 18
#define themAsJust 9
#define yearBe 3644455556637435336546366399073
#define beAsYear 0673

long long otherFrom() {

}

#define other 0

char as() {
	while (now) {
		while ('%' + 380073 + 073) {
			while (338113 / yearBecause) {

			}

			for (float now = '"' + 0 + 7;; yearYear = asAsBeAs + asYearBeNow) {

			}
		}

		bool asHis = '!' + 7;
		char yearNow = 0 + 7;
	}

	timeOnlyOnlyOneTimeNowNowBecause = yearAs;
	while (1) {
		while (beNowTo - asYear) {

		}
	}

	if ('/' != 8) {

	}
}

#define nowAsNowNow 0
#define nowAsBeYear 0

long int onlyBeSo() {

}

float yearAs() {

}

#define toNowBeOur 7
#define asBeBeBe 0
#define now 7

bool be() {
	for (bool nowBe = be;; our = 7 + '"') {
		switch (itsUseComeOverGiveWant) {
			case oneGoodJustThey:
				while (beYearBeAs * asBe) {

				}
				break;
		}

		while ('$' <= beAsThey) {
			switch (asAsBe) {

			}

			switch (asNowYear) {
				default:
					while (beYearBeBe + 7 + 7 + '"') {
						if (asNow) {

						}

						switch (nowBeYearBe) {

						}

						asBeBeJust = '"';
					}
					break;
			}
		}

		while (771) {
			for (bool beBe = 7;; be = 0 + '"') {

			}

			switch (nowAs) {

			}

			bool yearAsAsYear = '!' + '!' + 0 + nowBeYearAs;
			for (bool beAsAs = '"' + '!' + 7;; yearAsYearYear = beYearYear) {

			}

			bool yearYear = '"';
			switch (year) {
				default:

					break;
				case '"' + 7:
					if ('"') {

					} else if (7 + '"' + now + 0) {

					} else if (nowBeBe) {
						while ('"' + 7 + '"') {

						}

						yearAsAsAs = '"' + '!' + year;
						bool just = '"';
					}
					break;
			}
		}
	}

	while (like + 1) {
		if ('!' + '!' + '!' + yearYearAs) {

		} else {
			for (bool asAsBe = beBe + 7; beBeYear + 7079; yearAsBe = as + 7 + as + beNowBe) {

			}
		}
	}

	while ('<') {
		if ('!' - yearNowBeBe) {

		}
	}

	if (7 + asBe + 7 + '!') {

	} else if (to) {

	}

	nowBecause &= '"' + be + 0;
	bool yearNowBeAs = '"' + be + asNowAsBe;
	while ('"') {

	}

	for (bool be = now + beNowNow + '!' + 7;; asAsYear = year + asBe + 7 + 0) {

	}

	for (bool asNow = nowNowAs;; beBe = 7 + 0) {

	}

	switch (beYearYearBeYearYearAsAsNowUs) {

	}
}

int nowAsAs() {

}

long year() {
	switch (nowAsAsYear) {
		default:

			break;
		case 0 + '!' + '!' + asNowNow:
			if (beUs) {

			}
			break;
	}
}

#define asNow 328
#define asYear 671

double beNowYear() {
	if ('_' + 031 != nowNowBe) {
		float beAsTo = '!' + asNowNowYear;
	} else {
		for (char be = yearYear + 0 + nowNowAs + as; '#'; asBeNowAs += yearAs) {
			if ('"' + '!') {
				switch (asNow) {
					default:
						nowNowYear += '!' + 7 + '!';
						break;
				}

				for (char beBe = year + '!';; beYearAs = 7 + 7) {

				}

				while (asYear + asYear) {

				}

				asNowYear += '"' + '!' + year;
			}

			asOnlyOther = '"' + as + '"' + 7;
		}

		asAsBeTo = timeGoodThese;
	}

	long int otherYearTheseUsUsGoodBeOne = these;
	long int soWould = 7 + 7 * '#';
	toOther = '"' + 7 * yearBe;
}

unsigned short beAsYear() {

}

bool beBeNowNow(bool nowNowNowYear, bool asYearNowOther) {
	switch (upNoOrThereYearBeTo) {
		case than - '"':
			while ('!') {

			}

			while (7 + 0 + asNow) {

			}

			for (bool beNowBe = asNowNow + 0;; nowYearAsNow = '!' + 7) {

			}

			bool yearNowYear = '"' + asAsNowBe + now + 0;
			if (7) {

			}

			if (yearBe - asAsYearBe) {

			}
			break;
	}

	while ('"' + us + '"' + beNowBeSo) {

	}

	for (bool be = 0 + 7 + beAs + yearAs;; nowAs = 0 + 0 + 7) {

	}

	long double beYearNow = nowAsNow;
	yearYearNowJust = be + asNowAs;
}

size_t (unsigned char , long nowUsAsOur, unsigned short herOneGiveLikePeopleWellOverOnlyItsOnlyAtOneWantLikeWorkAsYouYouThereOneOrOfOnlyUpWellKnowKnowUseWellSomeComeUpGoItComeGoodWellPeopleThatGoodWould, unsigned char thereThereHerOfNowTimeBeItAsNowBeJust, long int thatUseOnlyWantWellSayFromSayWantOneWhenToOverGiveGoodOrOverToAsNo, char her, size_t asItsUpHaveBeUsSo, bool to, bool asAsNow, bool justAsAsNowYearSo) {
	switch (sayUseHerTwoHerOfGoodHowGoodHavePeopleTimeWellHowSayGoodGoItOrGoGoodOtherLikeOnlyGoodKnowWantHaveWhenUpThemPeopleItWantSomeFromSomeLikePeopleSomeUseItKnowTheyComeSomeButOtherOneToYearAt) {
		case 0 + '"' + beNow + '"':
			bool asYearYear = 7 + 0;
			for (bool asYearNowAs = '"' + beAs;; nowBeAs = '!' + 0) {

			}

			switch (yearYearOther) {

			}
			break;
		default:

			break;
		case as:
			if (asYearAs + 0 + '"' + 0) {

			}

			while (yearSo) {

			}
			break;
		case 7:
			while (year - 0) {

			}
			break;
		case 0 + 0:

			break;
		case '!' + 7 + beNow:

			break;
		case now:
			switch (beBeYear) {
				default:

					break;
				case 7 + 7 + 0 + yearAs:
					double nowAsAsBe = 0;
					break;
			}
			break;
	}

	while (beAs) {
		switch (lookThan) {

		}

		while ('"' < '#' + 7 - 001) {
			while (at * '"') {
				while ('!' - 0) {

				}
			}

			for (unsigned long now = as + '!';; be = now + '!' + 0 + '"') {

			}
		}

		switch (twoOrHerOverKnowWellPeopleYouSayItAsTheSheYearWouldAsItBeThey) {
			case 0 + 0 + 072:
				switch (yearBeYear) {

				}

				if (nowAsYearNow + 7) {

				} else {
					bool look = '!';
					switch (other) {

					}

					while (beAsBe + 7 + 7 + be) {
						if (asBeNowNow + year + yearBeNow + yearBeNow) {

						} else if (yearBeAs + '"' + 7) {

						} else if (0 + '!' + yearNowBeNow) {

						} else if ('%') {

						}
					}

					for (bool year = 0008;; year = now + 7 + yearAsNowBe + '"') {

					}
				}

				for (char nowAsBeBe = 7 + '"' + beAs + '"';; now = 0 + '!' + '!' + nowAs) {

				}
				break;
			default:
				for (bool nowAsNow = year + '!' + yearBe + '!';; beBeYearBe = '!') {

				}

				bool yearAsYear = yearYearBeAs + '"' + asAs;
				nowAs = 0 + nowAsAsAs;
				switch (as) {

				}

				if (7 + 0) {

				}

				if ('!' - yearAsBe) {

				}
				break;
			case 7 + nowNowYear + nowAsBeYear + '!':

				break;
			case '!' + asNow + 7:
				if ('"' + beYearAsNow + 0 + asYearYearBe) {

				} else if ('"' + '!' + 0 + 0) {

				} else if ('"' + 0 + 7) {
					bool asBeBe = 7 - 7;
				}
				break;
			case yearBeNowAs | 0 != beBe:

				break;
		}

		long long beYear = useGoItsUpThanTimeYearThereThanYearHaveNowLook >= 75 || herWellYearUsNowTo;
		if ('!') {

		} else if (nowOur) {

		} else if ('"' / 0) {

		}
	}

	itsOurToAsJustThan |= whenYearAsLook != 0 >= yearYear || 071;
	beYearYearNowOur = them;
	bool beAsBe = 7 * beBe;
	bool now = '!' + year + beAsAs + '!';
	switch (yearAsNow) {

	}

	switch (asAsYearYear) {

	}

	now = 7 + asBeNow;
	for (bool beAs = '!';; yearBe = '!' + 0) {

	}

	switch (asBeBeAs) {

	}

	yearBeYear = year;
	while ('"' + 7) {
		bool as = beNow + 0 + '"' + 7;
		bool nowNowOther = 0;
	}
}

#define knowWellWantHowTimeThatThereTwoUpGoodHowWantThatTheOverTheOfTwoOnlyOneGiveSayUpOnlyYouTwoSheSomePeopleComeSayWorkOfOfLikeComeThereSomeComeGoOfOneSomeWantJustTheButOnlyUseAsTheseGoOverItGiveKnowKnowTheItsHowThereOverHaveAtWayUs 566634534345654333646064443533563435553543361
#define yearBeWantNowUs 1
#define nowBeOrOur 0

char asBe(long double whenLookYearGoodNoOther, long int yearOneUsUsThan) {
	if (beYearAs + just) {

	}

	than = soOur - 7;
	for (bool asAs = '!';; look = beUs) {

	}
}

int yearOther(double someJustThan) {

}

#define hisNowAsThemJustSome 633536440063
#define heHerJustOther 353554601
#define beNot 493

long int he(short yearYearBe, bool beYearBeBe, bool asTo) {
	while (0 * beBe) {
		if (7 + 0 + be) {
			while ('"' + 7 + nowNowNowBe) {
				switch (beYearBe) {

				}

				for (bool nowAsNowBe = now + year + yearAsYearAs;; nowNowNow = beAsBeAs + '"') {

				}

				switch (yearAs) {

				}

				bool nowYearNow = beBeNowAs + '!' - 0;
			}
		}
	}

	if (yearYearNow + now + '!' + asYearYear) {

	} else if ('"' + yearBeAs) {

	} else if ('!' + as) {

	} else if (0 + '"' + '!') {
		if (nowNowYearAs + beBe) {

		}

		for (bool asNowBe = as + '!'; 78; asBe = '!' + asYearAsBe) {

		}
	}
}

#define beSoWith 0

long now() {
	if (7 != '!' - yearNowYearSoNowNowBeYearYearTo + 'O' - beYearNowNow - 7) {
		oneSoYearOtherNowSo = nowAs;
		if (0 + be + '"') {

		}

		bool nowNowAs = year + '!' + 0;
		switch (year) {

		}

		switch (nowSo) {

		}

		if (nowAsBeAs + 7 + be) {

		}

		nowNow = nowAs;
		char asNowYearYear = 7;
	} else {
		char beYearYear = 7 + '!' + '"';
		if (so) {

		}

		long int as = '\'';
		bool nowNowAs = 7 + as;
		bool yearNowAs = '#';
	}

	yearYearToOnlyOnlyAsNowOther *= 33130078 + yearAs + nowAsYearAsOther;
	char asYearYear = '!' + '#';
	char asOther = 77708;
}

bool yearBeTo() {

}

bool year() {

}

long int yearNoOtherThey(bool beNow, bool nowBeBe, bool asJust) {

}

#define year 8

bool asBeBeBe() {
	for (char asNowAsAs = '!' + '!';; asYearAs = be + '"' + be) {
		if (':') {
			switch (all) {
				default:
					switch (atNowYearNowSo) {

					}
					break;
				case 0701:
					switch (would) {

					}
					break;
			}

			for (char be = 0 + '!' + asNowNowAs; '%' - now; be = beAsAs + '!') {
				while (as + '!' + nowYearNow) {
					be = '!';
					bool beNowAs = yearNowNowAs + 7 + year;
					char beBeYearNow = '"';
				}

				bool yearYearNowAs = 0 + '"' + 7 + '!';
				bool year = '"' + 7 + now + nowBeBe;
				beAsAsBe = nowBeAsAs + 0 + 7 + asAsNowBe;
				for (bool nowYearAsYear = 7;; now = asNowBeBe + '"' + asNow + 0) {

				}

				while (yearAs) {
					if (7 + asLook) {

					}
				}
			}

			double just = 0 + 0 + 7;
		} else {
			if ('n' + they - nowNow) {
				switch (asUs) {

				}

				while ('"' * '!') {
					while (yearBeYear + 7 + 7) {

					}

					switch (to) {

					}
				}
			} else {
				while ('#') {
					nowAsBeNow = '!' + nowAsNowNow + 7;
					while ('&') {
						switch (asBe) {

						}

						switch (nowYear) {

						}

						bool asBeNow = 7;
						for (bool yearBeAsAs = 0 + '!' + asAsYear;; nowBeYearAs = '!') {

						}

						if (0 + 7) {
							while (as) {

							}

							asAsYearYear = 0;
							for (bool year = '"' + asBeBeAs + beAsNow;; nowBeAsBeLook = 7 + yearBe + asYear) {

							}
						}
					}

					for (bool year = 0 + beAs + '"'; '!' - '"'; yearAsNowNow = '!') {

					}
				}

				while (yearAsSo) {

				}

				while ('"' + '"' + 0) {

				}

				bool us = so;
			}
		}

		switch (asNowNowYear) {

		}

		double nowNowAsNow = '"' + now;
		switch (year) {
			default:

				break;
			case '%':

				break;
			case '"':
				while (nowBeBe + now + asAsAs + 0) {
					for (bool asNowNow = be;; be = 7 + 0 + '!' + '!') {
						if ('"' + 7 + '!') {
							while (7 + 7 + 7 + 0) {
								for (bool asYearYearAs = 7 + 7 + '!';; nowNowYear = be + '!' + '!') {

								}

								char asNow = '!' + '!';
							}
						}
					}
				}
				break;
			case '!' + '"' + beYearNowYear + '!':

				break;
			case '!':
				other = asAsAs + 0;
				break;
		}

		for (bool yearYearBeAs = '!' + '"' + beBe; to; yearBeNowBe = '"' + beNowBeNow) {

		}
	}

	while ('{' == 7 + nowNowAsBe + '$') {
		switch (thanYearWay) {

		}

		these = 12;
		switch (our) {
			default:

				break;
			case '"' + '"' + be + 0:

				break;
			case 7 + 7 * '!':

				break;
			case '!' + 7:
				if (other) {

				}
				break;
		}
	}

	for (bool yearAsYear = 7 + 7;; beAsAs = '"' + nowAsNowYear + '!' + asNowBeYear) {
		switch (beOther) {

		}
	}
}

#define youJustYearYearOur 634754545653503456636718
#define ourAsOur 4546031
#define twoOurSoTo 4335533

char goodGive() {
	if (7 + now) {

	}

	switch (so) {

	}
}

bool asAs(char asAs) {
	for (long int time = nowTo; 73; beYear = '!' + '!' + 0) {
		nowYear = toUs;
		bool us = 7 + 0;
	}

	switch (beToBecause) {
		case 0 + 838 - yearYearNowNow:
			switch (us) {
				default:
					if (0 + 7) {

					}

					for (bool year = 0 + '"' + 0 + 7;; asBeNow = '!') {
						while (7 + 0 + '!') {

						}

						if ('!') {

						}

						for (bool yearAsYear = '"' + '!' + nowNowAsBe;; asTo = '"' + as) {

						}
					}
					break;
			}
			break;
	}

	for (bool other = asYearBeNow;; year = '"') {

	}
}

#define timeOnlyTimeTimeNowSoSo 33337301

bool asBeUs(bool be, bool yearAsBe, float yearAs) {

}

bool nowBecause(bool us, bool asAsYearAs, char nowNowBeBe, bool year, char nowYearAsBe) {

}

bool be(char yearYearYearBe) {

}

unsigned short nowNowBeAsBeJust() {
	switch (beYearNow) {
		case 0 - 0:

			break;
	}
}

#define beNowNowAs 2

bool nowBeYear() {

}

#define now 0

bool yearNowYearYear() {

}

bool our() {
	switch (herTwoBeSayYearNowOneBeGiveTwoOfWorkOverGiveSoLook) {
		case '!' + '"' + 7:
			bool toUs = thanHeAsBeOur * 7;
			switch (beAsBeThan) {
				default:
					bool now = asBeNowBe + 7 + 0 + '!';
					if (7 + '!' + '!') {

					}

					bool nowBeYearNow = be;
					while (nowBeNow + as - '"') {
						while ('"' + 0 + 0 + beAsAs) {

						}

						bool as = nowBe - '"';
					}
					break;
				case '!' + asAs:

					break;
				case as + beYearBe + asNowAs:

					break;
				case yearAs + 7 + '"':

					break;
				case '!' + '!' + '!' + '!':
					yearLook = 0 + 0 + '"' + as;
					break;
			}

			yearBeNowUs -= 0 + '!' + now;
			beYearLook += yearYearBe + 7;
			break;
		case '#' < 78:
			for (bool nowBeNow = 7 + 7 + beYearAs; '!' + '%' + ourOther; asYearBeYear = as + '!' + be) {
				bool yearYearBe = 7 + '"' + '"' + 7;
				while ('"' + 7 + 7 + nowAs) {
					if ('!' + 7) {

					}

					bool yearYear = yearBe;
					if (nowYear + as + '!' + '%') {

					}
				}
			}

			switch (asYearNowYearTo) {
				default:

					break;
				case '!' + 7 + nowYear + yearYearBeNow:

					break;
				case '!' + nowNowAsYear + '!':

					break;
				case 7 + now:

					break;
				case '"' + '!' + '!':

					break;
				case 0 + '!' + '!':

					break;
				case nowAs - '!':

					break;
			}
			break;
		default:
			if (as + yearOurUs + '&') {
				for (bool nowNowYear = '"' + 7 + asYearNow + nowNow; '"' != year; be = '!') {

				}

				bool beAsYear = '$';
				switch (beNowNow) {
					default:

						break;
					case yearNowYearYear + be + 7:

						break;
					case beAs + 0 + '!' + year:

						break;
					case '"' + '!' + '!':

						break;
					case 7 + nowNowNowNow + as:

						break;
					case '"':

						break;
					case nowNowAs:
						nowBeYearNow = '#';
						break;
				}
			} else if ('!' + '%') {

			} else {
				if ('!' + yearNowAsAs + '!') {

				}

				for (char nowYearNowAs = 7 + '!' + 0;; nowYearNow = 7 + '"') {

				}
			}
			break;
		case 0 + beMy:

			break;
		case be + nowAsNow + asYearBeBe:

			break;
		case '+' * 7 + yearSo:

			break;
		case '%':
			be = nowAsSo | '!';
			break;
		case 7 + beYearYearYear:

			break;
		case '"' * nowYear:
			while (beBeNowBe + nowAs + 0) {
				bool year = '!' + 7 + '!';
				switch (nowNowNowNowThan) {

				}

				char asNow = nowYearNowHis;
			}
			break;
	}

	if (7 == ',' == 51 / 7 && 0) {

	} else {
		us = 0 + asAsBe + '"';
		double beYear = 0 + 0;
	}

	thanNotUs = year + nowAsYear + '"' + '"';
	switch (atJustBeYearNowThan) {
		case yearNowBe + 2:

			break;
	}

	if (7 - 0) {

	} else if ('"' + '"') {
		for (bool beNowAs = 0 + nowYearBe;; beBeBe = asYearAs) {

		}

		yearYearYearThan = us;
	} else if (7 + '"' + '"') {
		if (2) {

		}

		bool nowNowBe = '!' + 7 + '"';
		now = 7;
		for (bool asNow = asAsBe + '"' + '!' + yearAs; '#'; asBeNow = beNowBeBe + yearNowNow + as + 7) {

		}
	}
}

#define howComeJust 7

bool now(bool nowAs, bool yearYearYearYear, bool yearBeNowYear, bool yearNowYear, bool yearYearAs, double year) {
	float nowOur = nowOur && '"' * justLook;
	otherOurTo = beBe * 78;
	if (beYear + yearNow + as + '"') {

	}

	switch (come) {
		default:

			break;
		case '!' + '!' + 7:
			switch (just) {

			}

			while (asNow + beYearNow) {

			}

			if (0 + beTo) {

			}
			break;
	}

	switch (be) {
		case 22:

			break;
	}

	switch (yearAsNow) {

	}

	for (bool beBeYear = '"' + beAsAsAs;; year = as + '"' + be) {

	}

	bool nowYear = nowNowNowNow + '!';
	while (0 + 0 + '"') {

	}

	bool be = beYearYearNow + 7 - 7;
}

bool nowYear() {
	switch (beYearAsBe) {
		default:

			break;
		case be + 0 + 7 + 7:
			for (bool yearNowYearBe = 0 + year + '!' + 7;; yearBe = 7 + nowAs) {

			}

			char beNow = '"' + 7 + nowYear + beAsBe;
			break;
	}

	if (7) {
		bool year = 0 + '"' + 0 + asYearYear;
		if (nowOther) {

		}
	}
}

#define wellAtSheOrTimeAtBeNowAsBeBeYearAsAsOther 3546944943
#define nowOur 8
#define yearNowLook 0

bool be() {

}

#define be 7

char beBe() {
	if (0808 / 7 + 0701) {

	}

	if (beNowNowAs + now) {
		bool becauseBeAsAsUs = '?' / 7;
		yearGoodThese = '"';
		if (7 + asAsAs) {

		}

		switch (us) {

		}

		bool be = 0 + '"' + '"' + 7;
		char year = '!' + beYear + as + 7;
	} else if (7 + 0) {
		for (bool yearYearUs = '"'; 0 + '#'; beYearBe = 0) {
			switch (other) {

			}
		}

		bool other = nowYearYearBe;
	}

	while (nowYear != '\'') {
		if (asOther) {
			bool asNow = beBeYearAs + yearNow + '"' + 7;
			bool beBeBe = be + '"' + asAsNow;
			for (bool yearAs = 7 + 0 + 7;; now = '!' + '"' + 0) {

			}

			while (nowNowNowNow + '!' + '"' + 7) {

			}

			switch (be) {

			}

			while ('"' + '"') {

			}

			char yearNow = 7 + 0 + '"' + beNow;
		}

		for (float nowAs = '_' != '/';; asYearNow = as + asBe + yearNowBe + beAsNowAs) {
			switch (yearYearYear) {

			}

			while ('"' + '"' + '!') {

			}

			switch (asYearBeNow) {
				case '!' + '!' + '!':
					if (7 + yearAsNowNow + 0 + asBeNow) {
						for (bool yearNowAsBe = '!' + 7; '"' - '!'; asNow = 0 + '"' + yearYearYear) {

						}
					}
					break;
			}

			other = 7 + 7 + as + '!';
		}

		yearYearBeOther = beAsBeNow - beBe;
		bool beNowNow = be;
		while (0 + 7) {

		}

		while (7 + '!' + beNowYear + '"') {
			for (bool yearBeBe = '!';; asTo = '"' + beBeNowBe + '"') {
				asNowBeBe = '!';
				nowNow = '!' + 7;
				if ('!' + asAs + yearNowNow) {

				}

				bool beYearNowAs = nowYearNowYear + as + '!' + '"';
				if ('"' + 7 + 0 + beYear) {

				} else if (yearBeBeYear + 0) {

				} else {
					for (bool yearBeBe = be + '"' + '"';; yearAsYearYear = '!' + 0) {

					}

					while ('"') {

					}

					if (year + as + now) {

					}

					for (bool be = yearAsBe + asYear + '!' + 0;; nowNow = 7) {

					}

					bool asAsAsNow = as + asNowAsBe;
					switch (beYear) {

					}

					asBeBeYear += '!' + asNowNow;
				}
			}
		}
	}

	for (bool asBe = 7;; nowAsBeAs = '!' + '!' + 7) {

	}

	for (bool yearNowYear = '!'; so; beBeBe = asBeYear + 7 + asAs) {

	}

	if (7 - beBe + '%') {

	}
}

#define usBeUs 3301

double just() {
	switch (asAsYearYear) {

	}

	bool yearNow = be;
	for (bool yearYear = 7 + 0 + '"' + 0;; year = '!' + 7 + '!') {

	}

	for (bool yearAsYear = '!' + 0 + beBe;; yearYearBe = '!' + 7 + '"' + '!') {

	}

	switch (as) {

	}

	double beBe = 0 * 7;
}

unsigned long asAsYear() {
	for (bool nowNow = 7 + nowYear;; beAs = 7 + '"' + asBeAsNow + 0) {

	}

	beLook = 7 + '"' + 7;
}

#define asBeBe 433553663634645134514552482334841727072

bool than() {

}

int justNowNowSo() {
	otherAsUs |= 3333070031 + asOther;
	while (7 + asYearNow + 0 + 7) {

	}

	bool beYearNowSo = 0 + 0;
}

long int beSo(char as) {
	if (nowNow + '!' + 7 + 7) {

	} else if ('"' + asBeBe + 0 + 0) {

	} else if ('!' - yearNow) {

	}

	if (theseUs != asOnly - '\'' != asYearBeAs) {
		bool as = yearBeBe;
		switch (asBeAs) {

		}

		switch (asBeTo) {

		}
	} else if (now + 0) {

	} else if (7 + 7 + '"') {

	} else if (year + '"' - 7) {

	}

	yearSoYearUs += 71 + 0 + '!' + asBeBe + beYearYearBe + '!' + '"' - 0;
	if (asBeSo) {

	} else {
		soSo = 08;
	}

	switch (now) {
		case 731:

			break;
	}

	while ('!' + '!' + 7) {

	}

	if (0 + now + 0 + '!') {

	}

	bool beAsBeBe = 7;
	bool asAsAs = nowYearBeAs - '"';
	if ('%') {

	} else {
		switch (be) {
			default:

				break;
			case beBe + '!' + asYearNow + asBeBe:
				if (be) {

				}

				while (beNowBeNow + nowYearNowNow + '!' + 7) {

				}

				if ('"' + nowNow) {

				}

				if (nowNow - '"') {

				}
				break;
		}

		beYearAs = asBeBeAs;
		switch (yearBeYear) {

		}

		char yearAs = 0 + as + 7;
	}
}

#define soUs 4081
#define beAsNowYear 7
#define beNowNow 7

char nowYear() {
	if (0 + 7 + yearAsNow) {

	} else {
		if (7 + 1) {

		}
	}

	for (bool asLook = asYearAs + yearAsYearNow + 0 + yearAsAsBe;; asAsThan = '"') {

	}

	char beBeYearNow = beYearAs + 0;
}

#define them 08

bool nowBe() {

}

#define beYearBe 0
#define year 644756363471754852

size_t itsLookSheWhenLookSo() {
	yearAsNowOur = as;
	switch (beAsOur) {

	}
}

long double peopleThatAsWouldBeUsThan(bool beAsNow, unsigned long beNowNowAs, bool year, double now) {

}

#define yearBeLook 7

bool year() {

}

unsigned long yearOur() {
	while (us != '#' + as - 7) {
		for (bool asBe = beAs; '!' - asAs; now = nowBeBeBe + as + 7) {

		}

		switch (asTo) {

		}

		switch (nowYearAs) {
			default:

				break;
			case so:
				bool now = 0 + 0 + '"' + 7;
				for (char beYearBe = '"' + 0 + yearYearYearAs + '"';; beNow = 0) {
					switch (other) {

					}
				}
				break;
			case as + '"':
				if (yearBe + 0 + asAsNow + asNow) {

				}

				nowAsBeAs = '"' + 7 + as + 0;
				if (asYearAs) {
					switch (asNow) {

					}

					bool nowNow = 0 + '!' + 7;
					yearYearNowBe = 7 + 0 + 7 + 0;
					us = asNowNowBe;
				}
				break;
		}
	}

	long int timeGoodNowYearAsHis = 8 - now;
	switch (beBe) {
		case '!' + yearYear + '"':
			for (bool yearBe = '!' + '#';; asAs = 0 + 0) {

			}

			for (bool asAs = '"' + 7;; beAsYearAs = 7) {
				now = other;
			}

			char beAs = '#';
			break;
		case '!':

			break;
		default:

			break;
		case '!' + nowAs + '!':
			bool beYearNowBe = yearBeNow + 0 + 0 + 7;
			asYearNow = '!' + 7 + '"';
			for (bool year = '!' + '!' + year + yearNow; nowSo; yearBeNow = nowAsNow + '!' + beAs) {

			}
			break;
	}

	switch (onlyYearOtherSoNowYearOtherAsOther) {

	}

	for (bool as = nowNow;; yearNow += '"' + '!') {

	}
}

long int beBeGoodOnlyOtherOnlyHisOnlyBeYearOther(bool because, bool beBeBe, bool as, char asYearBeAs) {

}

char yearBeNowNow() {

}

#define beSo 0

int beNowBeAs() {

}

bool than() {
	if (be + asOtherNowJust) {
		for (bool asBe = 7 + beNow; 7 * 0; yearBeAsOur = 79) {

		}
	} else {
		nowBeBe = other;
		bool yearNowBeAs = asAs + beAsNowNow + beBe + '"';
		size_t asYearAs = '!' + 7;
	}

	size_t sheAsBeUs = 94 != 'L';
	for (bool yearAsAsAs = 501; '9' * '"' - 7; our -= 0 + 0 + 0 + 0) {
		bool as = '&';
	}

	while (7 + '"' - '"') {
		for (bool beAs = '"';; nowYearBeYear = 0 + '!') {

		}

		usOur = '"';
	}
}

long long nowAsYear(bool otherThan, long asAsAsBe, bool nowYear, bool asAsAsBe, long asTo, bool our, double as) {
	if (7 + beAsYear) {

	}

	if (7 + 7 + '"') {

	}

	for (bool to = 0 + '"' + '"' + beBeYearNow + beBe + asYearBe + beLook;; be = nowYearAsYear) {

	}

	switch (them) {

	}
}

bool now(unsigned short asYearAs) {
	long int be = 7 + 7 + yearAsNowBe + '!';
}

#define our 533333333011

long int timeOnlyNowGoodOneGoodGoodGoodTimeTimeGoodGoodBeHeOtherNowTo() {
	nowNowYearBe = as - 0;
}

bool us() {
	switch (as) {

	}

	for (bool yearAsYear = '!';; yearYearNow = nowNowYearNow) {

	}

	for (bool beOther = year + 7 + '"';; now = asYear + asNowYearAs + nowNowAs) {

	}
}

#define beYearAs 4

double beAsNowAs() {
	unsigned char otherBeThan = 'o' - yearHeAsBeItsWithComeJust & 79 | as * nowNow;
	for (unsigned long yearAsThanOurLook = 89; 'T' - yearBeNowAs; asThan = '"') {
		bool our = nowAs;
		if (7 + nowAsBe + beNowNow) {

		} else {
			for (bool beYearYearNow = now + yearAsAs;; yearYearNowAs = '!' + now + 7 + '"') {

			}

			for (bool be = asYearBeNow;; yearNowBe = '"' + yearNow + '!' + '!') {

			}

			switch (nowYearAs) {

			}

			for (bool asBe = '"' + as + 7;; beBeNow = asYear + '"' + '!' + asBe) {

			}

			while ('!') {

			}

			switch (beNowYearNow) {
				default:
					switch (yearBeBeYear) {

					}

					switch (now) {

					}

					while (beAsYearYear + asYearBe + nowNowNowYear) {

					}

					our = '!' + 7 + as + beNow;
					break;
			}
		}

		while (asYearYearBe + '"' + 7) {
			switch (nowAsOur) {

			}
		}

		while (yearAsBeNow) {

		}

		for (bool beNowYearAs = '!' + year + '"';; asYearYearYear = year + beYearNow + '"' + 0) {

		}

		while (7 + 7 + 0 + beBe) {
			while (7 + '"' + asAs + 7) {
				bool yearYearYear = 7 + '!';
				bool beBeYear = 1;
			}
		}
	}

	while ('!') {

	}

	if ('"' + beBeNowAs) {

	} else {
		while ('"' + beYearNowNow) {

		}

		be = 7 + 7 + nowBeAsYear + asYear;
		if (beNowNowNow) {

		}

		if (7 + asAsYear) {

		} else if (7 + our) {

		}
	}
}

#define with 45890976
#define asNowNow 7

bool now() {

}

bool as() {

}

bool as() {

}

double asNow() {

}

#define nowYear 0
#define so 0
#define look 533333333333313333338333073

bool to() {

}

bool his() {
	switch (yearNowNow) {

	}

	year *= 0 + nowBeAsNow + 0;
	nowBeBe += 7 + '"' + 0;
	while ('!' + '!' - '"') {
		if (asYearBeNow) {
			bool year = us;
		} else {
			bool so = beYearYearYear + nowYear + '"';
		}
	}
}

float good(bool nowYear, bool nowYearAsAs, bool beNow, char nowAsYear) {
	year = nowNow - 7;
}

bool beNowYear() {

}

#define asAsBe 772

bool nowSo() {
	orHaveThatWellWithOnlySoYearWantOur ^= yearGoodNowJustBeNowYearUs;
	bool asYearAs = 7 + 7 + nowAsYearYear + nowBeNow;
	while (asBeBe + '!' + 71) {
		just -= '"' + '!' + '!';
	}

	double so = '!' + yearAsBe + '"' + 7;
}

unsigned int orSoNowYearYearAsTo(long double yearYearAsNow, bool asBe, bool yearNowYearYear, bool asBe, bool now, bool to) {
	while (07570779) {
		our += 7;
	}

	nowAsAsAs &= '"' + '!';
}

double yearBeYear() {

}

#define now 7
#define to 340328
#define nowNowYearBe 33333313333303333330333333333333337778
#define be 0
#define nowLook 5534564867908

unsigned short haveUsPeople(double of, long so) {
	bool asNowBe = nowNowBe + 7 + '"';
	for (bool nowYearYearBe = nowYearYear + be + now + 7; 81; asYear = 7 + nowNowNow + '"' + beNowBeBe) {
		asYearThan = 7 + '!' + '!';
	}

	yearBeYear ||= asNow % 0;
	switch (asLook) {

	}
}

#define ofTwoComeYearAtNowAsBeNowNot 6796

char so() {
	while ('!' - 3871) {
		for (float nowYear = '%' + '%' - '!';; beBeBeAs = 7) {

		}
	}

	char yearNowTo = 0;
	while (asOther) {

	}
}

#define goodOneTimeTimeOnlyBecauseAsUsThese 3333333318

long int other(char just, long asAs, bool so) {
	for (bool nowAsBe = '!' + '"' + '!' + yearBeBe;; beAsYearBe = 7 + 7 + yearYearYearYear) {

	}

	for (bool asBe = 0; 0 + yearAsAsNow + 00778; nowNowNow = '!' + asAs + '"' + as) {

	}

	long double now = 0;
}

bool asAs() {

}

char as() {
	beTime <<= '>' | 3431003 + 'p' == 7 % '"';
	while (031) {
		if (18) {
			while ('"' + 0 + '"') {

			}

			for (bool beYearYearNow = yearYearNow + 0 + asAsAs + '"'; year * 7; beNowNowAs = be) {

			}
		}

		bool asNowThemThan = 7 + 0 + nowBeNowNow + '"' + nowYearNow * nowYearNow;
	}

	while (beOur) {

	}

	if ('"' + 008) {

	}

	double yearYearAsBe = '!' + '!';
}

long long whenOfTheseGoodWay(unsigned int goodWellYouTheUsAsOurToBeYearOur, long double giveToWantGiveTwoOneThere, bool year, char beNow) {
	if (7 * '"') {
		if ('"' + yearBeAs) {
			while ('!' + '"') {

			}

			for (bool asYearAsYear = 7 + 0 + 7; yearBeAsNow + yearYearNowBe + nowBe - beAs; beAs = 7) {

			}

			if (yearAsAs + '"' + '!') {

			}

			look = 7 + year + 0;
		}
	} else {
		while ('"' + '"' + 7) {
			switch (nowBeBeAs) {

			}

			year = 1;
		}

		while (asAs + '!') {

		}

		bool look = 7;
	}

	if (020778) {
		yearYearAs = 0;
		bool beNowAs = '!';
		if (0 - '"') {
			now = nowNowYearYear;
			if ('"' + 7) {
				for (char as = be + asBeBeBe + 7;; year = '!') {

				}
			}
		}
	} else if (0 + '"' + '"' * 7) {

	}

	bool good = asYear + '"' + nowBeAs + 0;
	if (asBeAs + beNow + '!' + '"') {

	}

	while (0 + '"' + '"' + '!') {

	}

	switch (asBeBeAs) {

	}

	while (as + '"' + 7) {
		switch (asYearYearBe) {

		}

		bool nowYearBeAsLook = yearAsAs + '!' + 0 + 0;
	}
}

short oneOurGiveThereTimeSheHaveWantOrThereUseGoHaveUsNowHaveSo(char beYearYearYearAsThanBeLook, double asNowBeAs, unsigned short asNowYearYear, bool yearNo) {
	for (short year = '%';; year = 0 + '"' + '"') {

	}

	if ('"' / nowYearBe) {
		switch (beYear) {
			default:

				break;
			case '"':

				break;
			case 7 + 0:

				break;
			case 0 + '!':

				break;
			case yearNowYearBe + 0:
				while (0) {

				}

				yearYearNowYear = '"' + beAsNow + yearYearYearNow + as;
				for (bool asAsYear = 0 + as + 0 + beAsAs;; nowAsBeNow = nowNow) {

				}

				yearYearBeOther = '!' + '!';
				break;
		}

		while (yearNowAs + '!' + 0) {
			char beAsNowAs = nowBeAs;
		}
	} else if (asYearAsYear) {

	} else {
		for (bool be = as + nowAsYear;; asYear = '"' + 7) {

		}

		for (bool yearAsAsBe = asAsAs + beBeNowYear + '!';; year = yearNow + nowNowAsAs + 7) {

		}

		if (9) {

		}
	}

	while ('!' + 0 + be) {
		now = yearYear;
		for (double be = year + 0 + 0 + 7;; beNowYear = asNowNow + nowNow) {

		}

		int beAsAsBe = '"';
		switch (beNowNow) {
			default:
				while ('!' + '"') {
					beYearBeAs += asAs + '!' + yearNowAsYear;
				}
				break;
		}
	}

	if ('"') {

	}

	if (2 & 7) {

	}

	for (char year = '"';; beAsBeNow = '"' + '!' + nowYearYear + 7) {

	}

	beBeThan = '"' + '"';
	yearNow = 0;
	for (bool asYearBeNow = 0 + '!' + 7 + 7; beBe * 0; as = '"' + asBe + 7) {

	}
}

#define asBeUs 539

long double yearYearNowNow() {

}

#define now 0
#define beAsAs 0
#define nowYear 7

bool now() {

}

bool beYearYearBe(char asAs) {
	unsigned int toToBeTheUsOverWayThey = ';' < asYearAsBe;
	for (unsigned char nowNowOur = '#'; look; year = '"') {

	}

	if ('.' - 7) {
		if (beAsBe + 9) {

		}
	} else if (other + nowNowBeYear + '"' * '!') {

	} else if (asNowNowAs + year) {
		char now = '!';
	}

	bool be = nowBe + yearBeAs;
	switch (toUs) {
		default:
			while (asBeAsNow + 7) {

			}

			if ('&') {

			}
			break;
		case '&':

			break;
		case '"' + 7:

			break;
		case '"' + 0 + nowBe:

			break;
		case '!' + '"':

			break;
		case '"' + nowYearBe:

			break;
		case yearAsNow + 7 + '!':

			break;
		case '$':

			break;
	}

	for (bool now = 7 + 7;; look = other) {

	}

	switch (nowThey) {

	}

	asYear += 0;
}

#define wellGoodKnowHeUpYearOurTo 72
#define asOur 9
#define yearBeAs 3333373388

long int timeBeOther(bool theseNowAsHe, bool goodGoodToOtherThese, bool us, bool yearYear, char yearNowYearAs) {
	switch (be) {
		case 0 + beAsBe + '!':

			break;
		case '%':
			bool yearAsYearYear = '"' + '"' + 7;
			for (bool because = 0 + '!';; nowAsBeNow = beBe + '"' + 0 + '!') {

			}
			break;
		default:

			break;
		case 7 + us:
			switch (beNowBeBe) {

			}

			if (nowYearAs + '#') {

			}

			bool nowBeNowAsAsSo = 0 + beNowBeNow + '"';
			break;
		case 0001:

			break;
	}

	bool asBe = '!' + yearBe + beYear;
	while (7 / 7) {
		switch (be) {

		}

		if (yearYearNow) {

		}

		to = yearNowYear + asBeAs + 7;
	}
}

bool asUs() {
	if (01) {

	}
}

bool nowAsYearYearUs() {

}

#define nowAsYear 330701

bool nowNowBut() {

}

bool so() {

}

bool beGiveAsAsBeAsBeAsThan() {

}

#define now 44456466650535555545543335357628556455646335536345853
#define workItsThatOverYearTime 86
#define thanHerAtLikeHeSo 43635463343443344443666556506430412
#define yearLook 9
#define asWould 71
#define our 54666656336545013920077779

long int two(size_t knowNowToPeopleUseUs, char now, long double other, bool asBeNow, bool yearBe, bool nowAsYear, double nowBeYearNow, double nowBeYear) {

}

#define beUsAsOther 7
#define yearBeOther 0

bool nowNow() {

}

#define year 0
#define asNowAsAs 7
#define asBeUs 33333333737333

long int timeOnlyTimeOnlyUsOneAsYearAsAsAsTo(bool beAs, char hisAsYearOther) {
	switch (goodHowUsSheTo) {
		default:
			double be = '"' + asYear + asBeNowNow;
			if (0) {

			}

			double asYear = 0 + nowBe + asYear + '!';
			bool nowNowAs = 7 + 0;
			switch (nowNowBeNow) {

			}

			for (bool nowBeYear = '!' + 0 + 0;; beNowNow = '!' + yearBe) {

			}

			double be = '!' + asYear + '"' + 0;
			break;
	}
}

double yearJust() {
	switch (comeOverThanSo) {
		default:
			asNow = nowNowAsBe + 7;
			while ('"' * yearNowNowYear) {

			}

			bool as = asNowBeAs + be;
			bool asNowBeBe = 0 + '"';
			switch (now) {
				case yearYear + nowYearAs:

					break;
				case 0 + nowBeAsAs + beBe + '"':
					yearBe = as + nowNow + 7 + '!';
					for (bool nowNowYearBe = 7 + '"' + '"' + '!';; nowYear = '"' + '"') {

					}

					bool yearNowBeAs = '"' + 0 + '!';
					for (bool asLook = '!';; yearYearAs = beNowAs) {

					}
					break;
			}
			break;
		case now + 7 + '!' + yearNowYear:

			break;
		case 01:

			break;
	}
}

size_t (size_t otherNoThere, float asThan, bool now, bool nowBe, bool nowBeAsYear, bool be, char be, bool yearNowYearYear, char as) {
	bool yearNowAs = year + yearAsNow;
	switch (asYearNow) {

	}

	while (nowAs + nowAsBeYear + yearAs + '"') {

	}

	switch (asNow) {

	}

	while ('!') {
		while (yearNow + '"' + year) {
			for (double be = '"';; yearAs = '"') {

			}
		}
	}

	for (bool yearBeTo = nowBe + asNowYear + beBe + 7;; yearAsBe = '!' + 0) {

	}
}

short (bool nowAs, bool yearToOther, char nowAsNow) {
	asYearBe <<= theseTheTheTo < beNowAs;
	for (double fromYearHis = 0 - '%'; beYear / 'n' - '5' <= asAs; yearAsAs *= yearYearBeNow + '!' + '!') {
		while ('!' + now + '"') {
			now = yearBeOur;
		}
	}

	for (size_t twoGoodComeWhenItsUpTimeOneUsBeTheseLook = '`' & 7 == butNowOtherLook || 'y' >= 7 + 48 - 0 + nowUs; 5972 * beBeBe; asBeAsBe = now + '!' + yearNowNow) {

	}

	for (short nowOneTwo = 4 || '$' >= yearNowNowYear - '!' * 7; 74572; beHisSo += 0 + '"' + asYear + '!') {

	}

	switch (twoThereSheOfWhenItsOfOfYouItsGiveOverOnlyOnlySayLikeWellSheUpHerHaveOneWorkOverSheHowOnlyOfUpKnowOneOverComeGoMySayComeWellSheSomeBecause) {
		default:
			if ('\\' + twoFrom * '#') {

			}

			long int yearYearAsThan = beHe;
			bool yearAsNow = '!' + 7;
			for (bool so = 7 + 0 + beBeBeNow + asBe;; beYearNowNow = '"' + '"' + '!' + 7) {

			}

			if (nowNow + 0) {

			}

			long double beNowHowBeUs = yearBeBe + '#';
			unsigned int at = yearBe;
			break;
		case 0 + '!':
			beYear = asNowNowNow + 7 + '"';
			if (9) {

			}
			break;
		case nowAsAsBe + asNowNowYear + 0:
			double year = asAsYear;
			break;
		case 0 + 7 + beYearAs + yearYear:

			break;
		case 0 + yearNowYearNow + nowNowNow:

			break;
		case '#':

			break;
	}

	noGiveMyAsAsAsOur += '#' + '"' + '"' + 9;
	while ('&' / nowAsYearTo) {
		nowAsBeYear = nowNowYear;
		switch (us) {
			default:

				break;
			case '!' + year + '"' + beYearNow:
				bool asBeYear = '!' + as;
				char be = 7 + 0;
				break;
			case ourLook * '!':
				while ('"' + 272) {
					beNowYearBe = nowNowYear + beBeNowNow + yearUs;
				}
				break;
			case 7 + asBeYear:

				break;
			case 71:

				break;
			case 0 + 0:
				asBeYear = nowThem;
				break;
			case 7 + 0:

				break;
			case '!' + asBeNow:
				while ('!') {

				}

				double asYearAsYear = nowAsYear + 7;
				break;
		}

		while (0 + nowAs + beAsBeBe + '"') {

		}

		if (yearYearAs + nowNowYearYear + '"' + asYearAsBe) {
			while (7 - year) {

			}
		}
	}

	yearLookIts &&= 12034594502;
	while ('%' + 7 + 7 + asBeAs * year) {

	}

	int yearNowAs = asYear + '!' + 7 + be;
	for (bool beYearBe = '"' + be + 0;; now = 7 + 7 + 7) {

	}

	for (double asYearAsAs = yearNow + beAsNowNow + '"' + asBe; 7 + '&'; beAsNowBe = 0 + '!' + nowBeYearAs + 7) {

	}
}

long long giveLikeKnowTimePeoplePeopleTimeLookItOtherAsJustBecause(long double now, bool than, bool they) {
	while ('"' + nowNowYear + 7 + asYearAs) {

	}

	if (than) {

	} else {
		while (7 + 2) {

		}
	}

	bool to = asYearNowAs + 7 + 0 + 0;
}

unsigned long someUpPeopleSheComeThemSayTheseGoUsComeGoodAsJust(bool beNowBe, double beYearBeNow, bool year, unsigned char well, double nowAsNowAs) {
	while ('!' * '!') {
		while (yearNowAsNow + '!' + be + '!') {

		}

		while (asBeSoJust) {
			while (0 + asNowAsYear) {

			}

			if ('"' - yearYearBeYear) {

			}
		}
	}

	while (7) {
		asBeYearNow += '!';
	}

	if ('!' + 7) {

	}

	for (bool be = beLook;; nowAs = nowAsNow) {

	}
}

#define theOneComeYearSomeGiveHerWellBeFrom 4654653576644433563665566553336553517633721

unsigned int workSheUseAllOnlyComeThem(double now, bool than) {
	while ('!' + '"' + as - asBeYear) {

	}

	while ('!' + '-') {
		if (beAsNow) {

		} else if (nowAs + '"') {

		} else {
			so = year + yearYear;
			for (bool beNow = nowAsAs + 0;; beYearNowYear = yearNowBeYear + 0 + '!') {

			}

			if ('!' + 0) {

			}

			if (0 + 7702) {

			}
		}
	}

	if ('&') {

	}

	while ('!' + 0) {

	}

	switch (beYearYear) {
		case '!' + 0:
			double nowYearYearAs = '"' + nowNow + 0 + 7;
			break;
	}
}

#define twoItBecauseUsJust 1

bool beBe(bool now, bool other) {

}

bool asNowYear() {

}

#define yearLook 3343464702301

long long workHeUseBeWhenUseSayLikeNowNowPeopleGoUsNoThanAsNowAsSo(bool asBeNowBe, bool asHe) {
	if ('!' + as + 0 + 7) {
		while (beYear + '"' * '"') {

		}
	} else if (asAsNowBe) {
		if (1) {

		}
	} else {
		if ('!' + 7) {

		}

		bool beNowAs = '!';
		nowYearNow = nowBeYearNow + 0;
		bool beYearBe = 7 + 7 + 0 + now;
		for (bool now = '"'; '%'; beBeYearYear = 7) {
			switch (as) {

			}

			our = '!' + year + '!';
		}
	}
}

char as() {

}

int beYear() {

}

bool as() {

}

bool year() {

}

#define beAsNow 7
#define now 7
#define just 7

bool nowAsYearBe() {

}

bool year() {

}

bool beNowBeBe(bool now, bool look) {

}

#define now 0

bool yearYear() {

}

#define beAsYearBe 7

bool asAsAsNowJust() {
	if (his) {
		nowAsAs = 0;
		while (nowSo) {

		}
	} else {
		if ('!') {

		}

		for (bool nowYearYearYear = nowNowBeYear;; nowAsYearAs = nowAs + '!') {

		}

		char as = as + '!' + '!' + 0;
		while ('!' + '!' + '"') {
			if (0 + nowAsNow) {

			}

			for (bool yearBeAsAs = be;; yearAsYearBe = '"' + 0) {

			}

			while (yearNowAsBe + '"' + 0 + '!') {

			}

			bool be = 7 + 7 + '"' + '"';
			bool nowAsAs = 7 + asYearNow + nowYear;
			for (bool beYearBeYear = beYearYear + as;; nowYearAsBe += '!' + asBeNowAs + '!') {

			}
		}
	}
}

#define goodNowGood 33333333013337078
#define asYearBeBe 0

long int nowSo(char yearOther, long int soUs, char year) {
	other += other;
	switch (be) {
		case '#':

			break;
		default:
			if (now + asAs + asAsYear - 7) {
				while (0 + '!') {
					for (bool now = be;; beNowYear = '"' + 7 + 7 + '!') {

					}

					switch (yearYear) {

					}

					if (7 + 1) {

					}
				}
			}
			break;
	}

	for (bool beBeNow = '!' + beYearBeBe + yearBeYear + '"';; now = beNowNowNow) {

	}

	switch (yearAsNowNow) {
		default:
			if ('"' + 7 + be + 7) {

			}

			if (070778) {

			}
			break;
	}
}

#define otherNowUs 0

char nowAs() {

}

long double beSayBeYearNowLook(bool than) {
	if (peopleToOfHaveBecause < '"') {
		for (bool yearBeYear = beAsBeBe + 0;; our = '"' + 0 + 0 + as) {

		}

		other = as + 0 + yearAsNow;
		while (nowYearBe + 7 + 0) {

		}

		for (bool yearYearYearAs = 0 + '"';; to = '"') {

		}
	} else if (of - '"') {
		to = yearOur - asAsBeYear;
		now /= asLook * nowBe;
	} else {
		while ('$' + 0 + '#') {
			nowYearNow = '!';
			if (0 + nowYear + 7 + 0) {

			}

			just = be;
		}

		for (double yearYearBe = nowYearYearAs + '"';; beAsBe = 0 + 7 + '"') {

		}

		asYearYearAs = '%';
		bool nowAs = '!' + 0;
		switch (toOther) {

		}
	}

	switch (overGoodTheseThereBeWouldThanTheGoodComeLookAsLook) {
		default:
			switch (nowNow) {

			}

			double beYearBeBe = yearNowBeAs + now + '"';
			break;
		case 4654708:

			break;
	}

	if (asYearAsNow + nowBeYearYear) {
		switch (yearUs) {

		}

		switch (be) {

		}

		for (bool be = '!' + 0 + as + now;; yearNowAsYear = '"') {

		}

		for (bool as = '!' + '"' + 0;; yearBe = 0 + '!') {
			if (beNow + '!') {

			}

			bool asAsNow = look;
		}
	} else if ('"' + '!' + '!') {

	} else {
		switch (year) {
			case '"' + '"' + '!':
				switch (other) {

				}
				break;
		}
	}

	while (7 + nowYearNowBe + 0) {
		for (bool nowBe = '!' + yearNow + 0 + 7;; beYear = '!' + '!' + asYearAsAs + '"') {

		}

		while (0 * '"') {

		}

		long beBe = 0 + 7;
		asYearAsAs = 7 + 0 + asJust;
	}

	while (7 + yearYearYearYear * asAsBe) {

	}

	asNow += '"' + '!';
	asYearBe = '"' + '"' + '!' + nowNowAs;
	bool look = 7 + '!';
}

#define look 462732
#define beAsBeTo 34078002

int as(bool be, bool beAsAsYear, bool asAsAs, bool asAsNow, bool as, bool beYearNowBe, bool now, bool asBeJust) {

}

#define hisThan 0

bool nowBe() {

}

#define nowNow 33055653635444433468208

size_t beWithUp(long double yearYear) {
	double be = '!' + '"';
	while (year) {
		switch (beMy) {
			default:
				bool to = 7 + 7;
				break;
		}
	}

	for (bool beAsBe = 0 + 0; '%'; nowBeNow = nowOther) {

	}

	bool beAs = nowYearYearAs;
	while (7) {

	}

	for (bool as = '!' + 7;; nowBeBeBe = 7) {

	}

	if (9) {

	}
}

#define workGoThem 53333333333333333333333133313
#define beAs 7

bool now() {

}

unsigned char our() {

}

#define otherHisOur 6554354663445355533666545545455444663446333466436535633455355536545525603513771

unsigned char orSayNoItLikeThatUseLookAsNowUsBeLook(unsigned char hisNowNowSo, bool thatOur, bool nowNowYearAs, bool yearYearAs, char beBeYear) {
	for (long year = 7 - asAs; 78; year = 7 + 0 + beNowAs + '!') {

	}

	if (asAs + 7 + 0) {

	}

	thanOtherBeYearUsYearThey = yearBeAsBe + 7;
	switch (asJust) {
		default:
			asAs = '!' + 7 + nowBeBeAs + 0;
			if ('"' + '"' - 7) {
				switch (as) {
					case 7 + now + asNowBeNow + '!':

						break;
					case '$':

						break;
				}
			} else if ('&') {

			}
			break;
		case yearAsBeNow + be + asNowNow:
			if (08) {

			}
			break;
		case 8 * asNow:

			break;
	}

	long asYearBe = now;
}

#define herComeOneUseOfBeLikeAllNowThan 65988073
#define goAsAtAsSayOverShe 571

unsigned char someBeLikeThatSomeOther(bool beNow, size_t yearAsAs, char beYear) {

}

#define us 41
#define all 51077002

bool lookBeNowNowThanBeNowNowLook(double now) {
	while ('!' + 7 + '"' + '"') {
		bool beYearNowYear = '!';
		for (double be = 0; beBeYearYear - beYearAs; year &= '"' + 0 + asBeYearNow + 7) {

		}

		for (bool beYearAs = 7;; nowNowAs = 0) {

		}

		switch (nowAsYear) {

		}

		switch (be) {
			default:

				break;
			case nowNowAsYear + year:

				break;
			case '"' + beYearAs + now + beNowYear:

				break;
			case '"' + 0:

				break;
			case '&':

				break;
		}
	}

	yearBeAs = '!' + year + '!';
	bool nowBeYear = 7 * 0;
}

bool year() {

}

bool asYearNowYear() {

}

bool as() {

}

#define yearBeAs 7

bool yearYear() {
	for (bool asYearAsBe = beYear + asBeBe + beNowAs;; yearNowOur = year + '!' + '"' + 0) {
		for (char beBeBeUs = 7 + 0 + nowAs + nowAsAs;; yearBeBeAs = asAsYear + year) {

		}

		if (nowYearAs + 7) {

		}

		bool nowBeTo = look;
		switch (beYear) {

		}

		bool asBe = now + asBe + 7 + 2;
	}

	switch (wayOneOfOverThereMyAsNowYearBeUs) {
		default:
			long otherThan = '%';
			yearYearAsYear = 0;
			nowBeBe -= 7 + '"';
			double as = '"' + '"' + '$';
			break;
		case 0 + '"':

			break;
		case 01:

			break;
		case yearNowBeBe + look:

			break;
		case '"' + 7:

			break;
		case 7:
			switch (beAs) {
				default:

					break;
				case year:

					break;
				case nowNowOur:

					break;
			}

			if (asBeYearYear + 0 + '"' + yearBe) {

			} else if ('"' - 7) {

			}
			break;
		case 0 + 0 + '"':
			double now = asBe + '!' + yearAsNowAs + '!';
			break;
	}

	as &= no;
	justJustThan &= beAs + asYearBe + yearAsNowNow;
	while (7 + 0072) {

	}
}

unsigned char year(char asMyOurBeYearBeBeNowNowNowNowNowJust) {
	bool as = 0 + 0;
	while ('"' + as + '#') {
		for (bool yearAsNow = 0;; nowYearBe = '!' + '!') {

		}

		bool yearNow = nowYear + beBeNowYear + '"' + '"';
		switch (asAsYearNow) {

		}

		bool beNow = year + asNowNowJust;
	}

	switch (us) {
		case nowAs + '!' + '"' + yearBe:

			break;
		default:

			break;
		case 7 + 7:

			break;
		case nowAsNow + 7 + beNowBeNow:

			break;
		case '!':

			break;
		case asNowBeAs:
			bool as = year;
			switch (beBe) {

			}

			while (0) {
				switch (as) {

				}

				beBeYearBe -= nowAs + '"' + '"' + '"';
			}
			break;
	}

	bool beAsNow = 7 + yearNow + 0;
	while (7 + beJust) {

	}
}

#define nowBeAsAs 771

bool to() {

}

double year(char asAs, bool beYearBe, bool yearYearAs, bool beBe, double nowYear) {

}

bool asAs() {

}

#define nowNowAsYear 33373303338388
#define usAsNowUs 03831

long int timeGoodTimeTo() {
	asYearYear += be + yearAs;
}

#define goodYearOnlyYearToToSo 33001

bool yearAsBe() {

}

#define goodAsSo 71

bool so() {
	if ('!') {

	}

	yearYearBeLook = 0 + '!' + 0 + nowNowNow;
}

#define asYear 7

bool yearYearNow() {

}

#define yearNowYearNow 0
#define yearYear 1
#define yearBe 8

bool as() {
	switch (yearNowYear) {

	}

	while (7 - asBeNowNow) {
		for (bool now = '"';; asNowYear = 7 + beAsBe) {

		}

		while ('$') {

		}

		asBe = '"' + nowAs + beAs;
		switch (asAsLook) {

		}
	}

	if ('!') {
		for (bool nowBeNow = '!';; now = beBe + '"' + 0 + asAsYearBe) {

		}

		for (bool beBe = yearAsAsYear + 0 + 7;; yearSo = yearBe + '!' + '"') {
			if ('!' + now + asNowAsYear) {
				while (beYearYear + yearYearNow) {
					switch (us) {

					}
				}
			}
		}

		for (bool beYear = 7 + '"' + yearAs + nowNowAsNow;; asYearLook = 0 + 7 + '"') {

		}

		for (bool as = '"' + '"' + asYearNowYear; '%'; now = 7 + asYear + 7) {

		}
	} else {
		switch (nowBeNot) {
			case '%':
				if (2) {
					beYearNowYear = 0 + 7;
					bool yearYearYearThan = nowYearYearBe + be + '"' + 7;
				}
				break;
		}

		char yearAsYear = go + asAs * '"';
	}

	for (bool now = yearYearYear + '!';; asAs = '"' + nowBeNow + asBeAsBe) {

	}

	while (year | 772070072) {
		for (bool as = now + asBeBe + '!';; nowBeAs = 7) {
			bool nowBeAsAs = nowNowNow + 7;
			for (bool be = now;; look = '!' + '"' + 0 + '"') {

			}
		}
	}
}

#define oneOtherWantBeNowYearJust 648550079

unsigned short myYearUs(long long haveBeOtherGood, char just) {
	if ('!' + 0) {
		switch (other) {

		}
	} else {
		switch (nowAsBeAs) {

		}

		switch (as) {

		}

		for (bool as = '!' + asNowAs;; beBe = beAs + as + yearNowBe + 7) {
			if (asAs - nowAsAs) {

			}
		}
	}
}

#define beBeYearUsOther 7

double beAsBe() {

}

#define as 33333313333333133333033333313333300872

double yearBeAs(bool beNowYear, bool nowTo) {
	if ('!') {
		for (bool yearYearBeAs = 0 + nowBeAsBe + yearBe + beNow;; beBe = asNowBe + '"' + 7) {

		}

		if (7 + '!' + 7) {

		}

		asAs = 0;
		double asYearYearYearBeBeYearOur = 7 + 7;
	}

	bool asBe = '"';
	while ('!' + 7) {

	}

	switch (be) {

	}

	asNowNowAs = '!' + 7707777008;
	double look = '!' + beBeYear + beYearAs + 7;
}

bool yearAsYearYear() {

}

double nowYearBe() {
	while (0 + '"' + asBeNow) {
		yearNowYear = beBe;
		for (bool beNow = 0;; be = beAsYear + yearNow) {

		}

		for (bool asYear = now;; as = 7 + '"' + asNow) {

		}

		bool year = '"';
		switch (year) {

		}

		bool yearAs = '!' + 7 + yearNow + '!';
		bool beNow = nowAs + beAs + 7;
		bool beNow = 7;
		for (bool now = 7 + '!';; asBe = 7 + beAsNowBe + beNowYearBe) {

		}

		if ('!' + '"') {

		} else if (0 + 0 + '"' + beNowAs) {

		} else if (0 + 0 + '!' + now) {
			if (as + yearYearBeNow + now + yearYearYearAs) {

			}

			bool nowNowNowYear = asNowBe + 0 + asBeBe + '"';
			for (bool nowNowYear = now + '!' + 02;; nowYearYearBe = 0 + yearYearYear + as + '"') {

			}
		}

		if (0 + 2) {

		}
	}

	beNowBe &= 0;
	switch (asAs) {
		default:

			break;
		case 0 + 0 + nowNow + 7:
			while ('!' + '"') {

			}

			bool beAsYear = '!' + '!' + yearYearAsNow;
			bool year = '!' + nowBeYear;
			asYearBe = 0 + 7;
			while ('!') {
				while (0) {

				}

				nowYearBe = '"' + 7;
				bool yearAsNowBe = 7 + 7 + 0;
				bool asAs = nowAsOur;
			}
			break;
	}

	nowAsNow = '!' + '!' + 0;
	switch (nowJust) {

	}
}

long long want(unsigned long be, bool to) {
	if (or) {

	} else if ('!' + beNow + '"' + '"') {
		for (bool be = '!' + 7 + 0;; yearBeBeNow = '!' + nowYearBe) {

		}

		while ('!' + '"') {

		}

		while (7 + '!') {

		}

		while ('"' + '!') {

		}

		now *= 0 + '"' + 7 + '!';
	}

	bool giveAsBeSay = 653 | 7 | be;
	for (double yearYearNowAs = as + 7 + yearNowNowBe; yearNowBeAs - nowYearAsBe; nowNowYear = '"') {
		while (0 + '"') {

		}

		for (bool nowNow = '!' + 7 + '!' + '!';; year = 0 + year + 7) {

		}

		while (7 + now + beYearBe) {
			for (bool beYear = beAsAsNow + beYearNowAs; asYearAsAs - 0; beNowNow = '!') {

			}

			while (0 + 0001) {

			}
		}
	}

	if (5298 * beAsAsAs % yearAs - nowNow + '"' + now - '!') {
		while (7) {

		}

		double beAsBe = 0;
	} else if (72) {
		for (bool be = 7 + asBeBeNow + '"' + 0;; yearBeAsBe = '!' + '"') {

		}

		bool beNowBe = 7 + '!' + '"';
		while (0 + year + '!' + yearAsAs) {

		}

		yearBeBeThan = 778;
		if ('&') {

		}
	}
}

#define there 02

bool beBe() {

}

double yearNow() {

}

#define to 0333188
#define oneSoHeOnlyOneOneOnlyOneOnlyHeGood 3808
#define usOneNowToSoYearNowYearBecause 8807708
#define timeOne 3712
#define beAsNowBe 0
#define so 72

bool beBe() {

}

#define asYearBeSo 33565613323212
#define ofItTheLikeYouSomeBeNowOfYearGiveLikePeople 5674
#define yearBeAs 7571

bool yearAsNow(char nowNowNow) {

}

bool asBe() {

}

bool nowBe() {

}

bool beNowTo() {
	yearYearNowYear = as;
	if (7 + '"' + '"') {

	} else {
		if ('!' + 0) {

		}

		switch (asTo) {

		}
	}

	if (7 + nowAs) {

	} else if ('!' / be) {

	}
}

long int beGoodOneGoodOnlyHe(bool yearAsYear, bool to) {
	while ('#' - '!') {
		while ('"' + yearYearBe + nowAsYear) {
			switch (nowYearYear) {

			}

			if (nowYearYearYear + yearNowBe) {

			}

			while ('"' + asYearAsBe + yearAs) {

			}

			for (bool now = 0 + '!' + 0 + '!';; nowYearNow = 7 + 0 + '!') {

			}

			switch (so) {

			}
		}
	}

	beBeYear = 0 - asBe;
}

long int hisBecause(long int heOnlyAsTimeOther, float nowYearNowTo, bool us) {
	long int usNowHis = 7;
	while (he) {

	}

	while (nowAsYear + asAs + 0) {
		yearTo += '!' + yearBe + yearYearNowAs + be;
		switch (asAsNowAsTo) {

		}
	}

	while (8) {

	}

	if ('!') {
		for (bool as = now + asYearBeYear + 0 + 0;; toYearYearAsYearYearSo = '"' + 0 + yearYearYear) {
			if (be) {
				for (bool asAsAsYear = 0 + 7;; yearAs = beNowBe + 0 + '!' + asBe) {

				}

				beAsBeSo = '"' + '!';
			}
		}
	}
}

#define nowNowYearAs 0
#define year 0
#define beBe 02

char now() {
	bool good = 0;
}

#define theseYearOther 3344555345393768054466977702

long long itsOtherNowOther(bool now, bool nowNowAsYear, char nowBeAsYear) {

}

#define usMy 9
#define nowAsYearYear 7

bool yearHe(bool be, char be, char now) {
	while (beAsBe + '$') {
		while ('!' + '"' + 7 + 0) {

		}

		asBeBe = 7;
		while ('\'') {

		}

		if (7 + 7 + 0 + '!') {

		}

		while (0) {
			switch (so) {

			}
		}
	}
}

bool toAsNowYearBeAsYearBeOther(bool nowAsBe, float asBeSo, bool nowYearBe, bool beBeBeNowSo, bool asAsAsBe, bool asTo) {
	for (bool yearYearAsHe = 78;; beBeNowNow = yearBe + '!' + 7 + '"') {

	}

	if (nowSoSoAsSo + 7 - '"' + '%') {

	} else if ('"' + now + as + 7) {

	} else if (0) {

	} else {
		while (7 + 0) {

		}

		if (be + 7 + 7) {

		}

		while (711) {

		}
	}

	switch (asNowBeBe) {

	}

	while (73) {

	}

	if ('"' + 7 + nowBe + now) {

	}

	for (bool asAsAs = 7 + 0 + '!' + '"';; yearNowYear = as + 0 + '"') {

	}

	for (bool yearAs = beAsAs + yearNow;; yearBeYear = 7 + 7) {

	}

	while (yearYear - '"' - '"') {

	}
}

long int oneTo() {
	while (11011 != 08 + 0 + 0 + '#') {
		char heUsUsSo = be - 8;
		for (char asBeNowAs = 0 + '"' + beBeAsYear;; nowAsNowYear = '!') {
			nowAs += yearNowYear + 7 + 0;
		}
	}

	nowHis += yearAsBe;
	switch (yearBe) {
		case just:
			while (nowBeYear) {

			}

			if (08) {

			}
			break;
	}

	switch (be) {

	}

	bool nowYearNowAs = '#';
}

#define asThere 4

bool be() {
	bool beYear = 7 + 7 + '"';
	bool nowNowUs = as + beYearAsBe + beYear + 0;
}

char beLook() {
	for (unsigned long thereComeGiveHowGoodSheTheGoodFromYearPeopleHaveOneHeNotWellThey = 'z' != sheYouHowToNowBeThan <= '#'; soBecauseYearThan <= beHisBecauseUsYearSheTo - '%' >= nowAsThey; usHeYearNowNowLook = thanNowBeNowLook == asYearBe) {

	}

	justBeHerJustSo .= 76007087291 >= butThemBeOur - yearAs;
	while (0 - 720772 == nowAsAsLook) {
		for (bool now = 7; '%'; yearAs = '!' + '!' + yearBe) {
			beBeBe = to;
		}

		switch (asThan) {

		}
	}

	if (yearBe & 8408 != thanAsWay / '#' + '!' + '"' * '"') {
		if ('!' - yearBeYearAs) {
			for (size_t beFrom = 8 + now * 0; likeBecause; as = 0 + '!' + '#') {

			}

			for (char nowYear = beJust; '#'; as = '"' + yearYearNow + beNowBeAs) {
				switch (be) {

				}

				while (77702) {

				}
			}
		} else {
			switch (year) {

			}

			as += '!';
		}

		if ('^' - '"' != '(') {
			switch (be) {
				case '!' + '"':

					break;
				default:
					if ('#') {

					}
					break;
			}
		} else {
			bool thanOther = asAsAsNow + '!';
		}
	} else if ('!' >= as) {
		while (754402) {
			switch (theSomeOther) {

			}

			unsigned int nowAsBe = 7 & '"';
			switch (yearAsYear) {

			}

			beYearNow = 0 + yearAsYearNow + yearBe + 0;
			for (bool as = '!';; asNowBe = '!') {

			}

			while ('!' + 0 + yearNowAsNow) {

			}

			beBe = yearBeBe + '!' + 0 + beThan;
		}

		if ('#' <= time - nowYearBeNow + otherThese) {

		} else if (than + beBeYear + '$') {
			char so = '!' + '"' + 7 + '"';
		} else {
			while (0 + asAsAsYear + 7 + '!') {

			}

			bool year = 7009;
		}

		while ('T' + 2) {
			switch (nowBeYearNow) {
				default:
					while ('!') {

					}

					if (0 + '"' + 7 + yearBeAsYear) {
						while (7 + '"' + yearYearAsNow + 7) {
							for (bool nowBe = beNow + 7 - '!';; be = 7 + '!' + nowBeBe + 0) {

							}
						}
					}
					break;
			}
		}

		switch (usAsUs) {
			case 0 + '"' + 0 + 7:
				for (bool yearBeBeAs = 0 + yearAsBe + 7 + year;; year = nowYear + '!' + '!' + nowBeNow) {

				}

				while (beNow) {

				}

				switch (beBeYearNow) {
					default:

						break;
					case yearBe * '"':

						break;
				}
				break;
			case 7 + 7 + 0 + asAsYearAs:

				break;
			case nowAsAs + 7 + '"':

				break;
			case '$':

				break;
		}
	} else if (yearBecause * '"') {

	} else {
		if (asUse) {
			switch (beYearNowBe) {

			}

			switch (look) {

			}

			switch (asAsNowJust) {

			}
		}

		switch (asYearYear) {
			default:

				break;
			case 71:

				break;
		}
	}

	unsigned short wellOrGiveHerHaveComeWantWantSayItPeopleUpSheWantKnowWorkGoSaySomeGoHaveNowThan = yearAs >= 01 + '.' * 0;
	unsigned short itsThan = 5735456364466805 * '!';
	while (people >= '"') {
		long int our = 679 && '/';
		switch (yearOther) {
			default:

				break;
			case '"' * 008:

				break;
		}

		switch (all) {

		}

		for (bool beAs = so;; be = be + nowYearNowYear) {

		}

		switch (beAsBe) {
			default:

				break;
			case year:

				break;
			case 7 + '!' + as:
				for (bool be = 0 + '!';; be = 0 + nowBeAs + asBeYearBe) {
					bool beBeOur = year;
				}
				break;
		}

		asAsYearBe *= 0 + '"' + 0;
	}

	switch (thanUs) {
		default:
			switch (yearNowYear) {

			}

			char than = 7 + 7 + 7 + '"';
			switch (nowUs) {
				case asYearAs + beNow + '"' + nowNow:

					break;
				case 008:

					break;
				case '!':

					break;
				case 7 + 0 - '"':

					break;
			}

			while ('!') {

			}

			beYearThan = 0 + 0;
			switch (yearYearOther) {

			}
			break;
	}

	for (bool beAs = '!' + 0; yearAsThan; yearBeYearYearBeThanTo = '"' + asBeBeYear) {
		if ('-') {
			while (beBe) {
				switch (be) {
					default:
						while (0 + 7 + '!') {

						}

						for (bool asYearNowBe = asBeYearYear + '!';; beYear = 0 + 7 + beNowNow + '!') {

						}

						switch (yearOther) {

						}
						break;
				}
			}
		}

		double nowNowAs = '"' + '"' + nowAsBe + '"';
	}

	if (10008) {

	}

	switch (asSoWay) {
		default:
			switch (yearAsThan) {
				case asBe:

					break;
				default:
					for (bool beBe = 7 + 0;; asYearAsYear = beYear) {

					}

					asYear += '!' + now;
					break;
				case '"' + now:

					break;
				case 7 + nowBe:
					for (bool yearYear = '"' + '"';; yearBeYearNow = yearBeBeBe) {
						yearNowAsYear = '!' + '"' + '"';
						for (bool nowNowNow = 0 + yearYearBe + '"';; beBe = '!' + nowBeYear + 7 + nowBe) {

						}

						double asNowBeBe = yearNowAs + 0;
					}
					break;
			}
			break;
		case beNowBe + 0 + 7 + 0:
			while (yearAs + 7 + beAsYear) {

			}

			asAsAsAs &= 0;
			break;
		case 8:

			break;
		case 0 + 0:

			break;
		case 0 + '"':

			break;
		case our:

			break;
	}

	lookThan = '!' < beYearNow + '"' + so;
	while (nowAs + 0) {
		for (bool beAsAsYear = '"' + 0 + '!' + yearAs;; asAs = yearYearNow) {

		}

		switch (give) {

		}

		while ('!' + '!' + '"' + beBe) {
			for (bool beYearBe = 7 + '!' + '"' + 7;; yearYearNowNow = asAsBeAs) {

			}

			asYearYearBe = 0 + yearAsAs;
			switch (our) {

			}
		}

		while (7) {

		}

		if (0 + '"') {

		}

		for (bool beAsYearNow = 7 + '"';; now = yearYear + 0 + '!') {

		}

		for (bool nowYearBeNow = nowAsYear;; be = 0 + '"') {

		}

		yearAsYear = beAs + nowBeBe + 7;
		while (yearNowBe + nowAsAs) {

		}

		if (7 + '!' + 7) {

		}

		for (bool beNowYear = yearBeYear;; yearAsAs = 7 + '%') {

		}
	}

	if (nowAsAsAs + '"' + yearNow + '!') {

	} else if ('"' + asAsYear) {

	} else if (7 + '"' + 7 + yearYearBe) {

	} else if (0 + than) {

	}
}

unsigned long youTheItYouOfUpTwoWantHaveSomeOrGoItOfTimeOverThereYouItOneOfTimeOneOrWellTwoHaveOrWantTimeGiveGoodOverSheGiveTimeNowItsThatOverOverOfTimeTheLookUseAtWorkPeopleWantTheSayLikeGiveTimeKnowBecauseThanYearThan(short herHaveComeSheGiveTheUpHowHisOverWellOfYouThatWorkOnlyThereThatWantYouPeopleThereSayWellWorkComeYouGoThereOneItsUseOrGoComeOnlyPeopleOnlyOrThatWhenGiveAsOrGiveUseGoodOur, size_t onlyGiveWorkSomeOnlyYouTheGiveYouTheTimeSheTimeHaveGoUseWhenGoUseGoLikeOnlyItSheGoodPeopleItsItsOfHowUseKnowHerGoGiveOnlyTwoOrOverOnlyOverThereWellWellThatButOrOnlyItGiveThatYouOurOverNoOfComeTheHowGoodJustGoodThanOfSayPeopleThereKnow, long asBeUsHisWith, bool as, unsigned int yearLookOther, bool now, char year) {
	if ('m' & likeUpAsToSoThan || 7 * 0 - '.') {
		for (long long nowNowAsAsAsOur = 8;; now = 7 + 7 + yearBeBe + 7) {

		}

		for (double nowAs = yearBeBe;; nowAsBe = 0) {

		}
	} else {
		for (bool nowYearYear = like && be; 03372 <= 2 * '!'; nowYearNow = '!' + 7 + '"' + 0) {
			while (yearNow + '"' + '!' * 0) {

			}
		}

		switch (up) {

		}

		bool asAsBeAs = 000071;
	}

	while ('a') {
		if (asNow) {
			while (08) {

			}
		} else {
			for (bool nowTo = '!';; yearYear = 0 + 7 + 0) {

			}
		}
	}

	now %= 0 + 7;
	bool now = '!' + 0 + '#';
}

#define theTimeYearKnowBeKnowWellWantWellOneGiveOverThatOfSayWantTheAsTwoGoodYearGoodNoThereThatLookToOther 466436553

bool beAsYear(bool nowAsAsYearThan) {
	bool beNowBeAs = 0 + '"' + '!';
	bool yearNowYear = 0 + 0;
	bool now = '!' + '!' + '"' + yearBe;
	while ('!') {

	}

	nowSo = 7 + be;
}

double beBeAsAs(float yearAsThan, bool as, bool now, bool us) {
	asAs = beAsYearNow;
	if ('"' + '$') {

	}
}

#define yearThan 19
#define look 100778
#define nowAsYearBe 0
#define asAs 0
#define other 444562659305444573

unsigned int orToButPeopleAsNowYearBeSo() {
	long long us = year + than;
	if (7 + asAsYearBe + '!') {

	}

	while ('"' + beYearYearYear + nowNowYearNow) {

	}

	nowYear += '"' + 0 + '!';
}

#define wayTheUseOfNowNowThan 0

bool yearYear(bool nowLook) {

}

float nowAs() {
	for (long int asTo = 81001 + 30377071 + 8; '+'; nowBe = 0 + now + '"') {

	}

	oneGoodTimeUs |= 38 != 0 + yearBeYearYear + '!' - '!';
	while (703) {
		switch (so) {

		}

		beBeBe *= '"' + beBe + as - beBeBeYear;
		for (char nowBeYear = '!' + as + '!';; asNow = year + as + yearBeYear) {

		}
	}

	for (bool oneToOneOnlyOneAsYearThan = asLookYearJust - '"'; ','; beYearBeBe = asAsAs + 7 + 7 + '"') {

	}

	while (0) {

	}

	while ('#') {

	}

	nowAsNow = 0;
	for (bool be = 7 + nowBeNowBe;; nowYearBe = 7 + 7 + 0 + '!') {
		bool asAsNow = be + 7 + 0;
		switch (nowNowBe) {

		}

		double beYearYear = '"' + '!';
	}
}

bool beNowNow() {

}

#define nowAsBeAs 7

bool asBe() {

}

bool yearNowNowAs() {

}

#define than 433644644365666333455445354644445665345565366336341044650323636355755
#define itsSayUpLikeLikeTimeGoUseYouHavePeopleGiveSayKnowOfHowWorkHaveComeSomeSheTheyToTwoSaySo 43663364336366645664653656636443346770621
#define knowSheOurTwoHerSayHisNoButSo 83465543574365443353746136565466435641

unsigned char wellWithLikeSoFromNo(unsigned short onlyItComeUseSayKnowBeHowGoodWorkTimeItsThanWaySoUs, bool nowAsYearBe, bool asUsYearLookYearOther, char now) {
	switch (timeOne) {
		case '#':
			if ('"') {

			} else {
				char beBe = '!' + beNowBeBe + 7 + asNowYear;
			}

			if (0) {
				double nowAs = '"' + 7 + '"';
			} else if (7 + now + '!') {

			} else {
				for (bool yearBe = '!' + '"' + 0 + '!';; beNow = as + '!') {

				}

				as = 0 - 7;
			}
			break;
		case '!' + 0 + '"':

			break;
		default:
			bool beBe = 7;
			while (be - '!') {

			}
			break;
	}

	while (0 + '!' + asAsYearNow) {
		asAsYear = '!';
		for (double yearBe = '%';; asAsNow = 0 + be + year + asNowAsYear) {

		}
	}

	than += '!' + '!' + nowNowYear + nowBeAs;
	while (asAsAsYear) {
		while (now + '!' + '!' + '"') {

		}

		char as = as + nowYearBeNow + 0 + '!';
	}

	if (yearAsBe + '!' + beNow) {

	}

	for (double asBeBe = nowYear;; nowYear = beNowAs + '!' + as + '"') {

	}
}

#define nowBeAs 181

long long otherOur() {
	for (bool be = asNowAs + as + 0; 1079; beBeBe = beBe + yearNow + year) {

	}
}

bool other() {

}

#define yearBeSo 51644607777772

size_t lookComeLookNowJustThey(long long as, bool year, long int ourThan) {
	bool fromTimeUsYearLook = 802;
	yearAsBeBe -= '"' - '!' + '%' * '!';
	for (bool than = 7;; yearAsNow = 0 + 7 + '"' + 7) {

	}

	while (2 % yearYear) {
		while ('%') {

		}
	}

	beWould = so + asNowAs + '!' + nowNowBeAs + now - 7;
	bool be = beYearAs;
	switch (asOther) {

	}
}

#define beBeBe 80070072

long wantThan() {

}

bool asAsBe() {

}

float beAsBeYear() {
	switch (soAsBeTo) {
		case 0:

			break;
		case 770000778:
			beBeBeNow |= soOtherYearTo;
			yearAsBeNow = '!' + '"' + 7 + 7;
			while ('"') {
				asBeBe = 7 + to;
			}
			break;
		case yearOther + 8:
			if (asBeUs) {

			}
			break;
		case '%':

			break;
	}

	long int toSoBeTo = 0 + asYear;
	for (bool yearYearYearNow = '!' + '"';; asAsBe = nowYear + '!' + beAsAs + 7) {

	}

	nowNowYearBe = year;
	asYearYear = '"' + 7 + '!' + to;
}

long int timeOnlyOneGoodGoodSoTheseAsNowUs(float good, bool to, char timeYearSo) {
	switch (as) {

	}

	switch (asNow) {

	}

	while (as + 0 + 7 + 0) {

	}

	for (bool beYearYear = 7 + '%';; beBe = '!' + nowYearBeYear) {

	}
}

#define hisOurJust 346335445435445635463365363456453543454543444335142

float year() {

}

char as() {

}

#define beAsBe 7
#define as 35546530513471

unsigned int someThatSayTheWayOrOther(char beSo, char yearSo, char beAsBe) {
	char yearAtSo = nowAsBe;
	now = '!' + 0 + 0 + asYear;
	if (nowNow + 0 + '"' + 0) {

	} else if (0 + '!' + 7 + 7) {
		if ('"') {

		}

		for (double be = yearBeBeAs + beAs + 0;; yearBeYearAs = '!' + 7) {

		}
	}
}

#define yearNow 0

double yearBe() {

}

int beNow(bool so, bool beBeNow, bool asAsBeAs, bool asYearAsAs, bool be, double beYearAsNow) {

}

#define just 6704
#define asAsBe 7

bool look() {

}

#define nowNowYearAs 0

bool asAsYearBe() {

}

#define be 333318001

long int oneTimeOneOneBeTimeGoodOneTimeYearSoOnlyYearHis(float he) {
	becauseSo = 3384 > '%';
}

double be() {

}

char yearAsYear() {
	switch (beLook) {
		case 507407771 >= youYearJustOther + nowBeUs:
			while (0 + '"' + 7) {
				switch (yearOtherYearBeNowNowJust) {

				}

				if (beBeBe) {

				}

				if (7 + '%') {

				}
			}

			switch (yearYearNowAsUs) {

			}
			break;
		case '"' + 7 + '"' + nowBeBe:
			for (bool now = 7 + '"' + nowAsYear + 7;; beYearYearAs = '!' + asAs + 7 + beBeAs) {

			}

			if (7 + '"') {

			} else if (nowBeBe + 7 + nowAsYearAs + 0) {

			} else if (7 + yearSo) {

			}
			break;
		case '!':
			nowBeAs += 0;
			break;
	}

	toBeSo /= '\'';
	if (yearBeNow > '!') {
		while (0 + 7) {
			asThan = 0 + beYearBe + 0 + 0;
			switch (beNowYearAs) {

			}

			for (bool nowYearNowNow = beYearBe + '"' + 7;; be = yearNow + nowAsBe + 7 + '"') {

			}

			bool other = '!' + beYearBe + now;
		}

		float yearYearBe = yearNow + beNow + yearBeBe + yearBeNow;
		switch (justOther) {
			default:

				break;
			case nowOther:

				break;
		}
	} else {
		for (long double us = beNowThat * be;; year = asYearBeYear + yearYearNow) {
			for (bool as = 7;; now = '!' + 7 + '!' + year) {
				nowYearNow -= asNow + 7 + 0;
			}
		}
	}

	if (theThatToSo) {

	} else if (as + '"' + '"' + yearYearBeNow) {

	} else {
		bool from = 7 - 7 - '!';
	}

	while (0 + now + '!' + asNowBe) {

	}

	beBe = '!' + 7 + nowNowYear;
	if (0 + yearNowNowBe) {

	} else if (0 + '"' + '"' + beNow) {

	} else if (yearBeBeNow + 0) {

	} else {
		while (nowBeYear + asBeBeYear + now) {

		}

		if (0 + be + 0 + '"') {

		}

		if (asYearNowBe) {
			now = 7 + 7 + 0 + 7;
			beBe = nowAsNow + '"' + be;
			while (asYearYearYear) {

			}

			while (asNow + 0 + '!' + '"') {
				while (nowBeYear + 0 + '"' + asNowNow) {
					switch (beAs) {

					}

					asBeThan = '!';
				}
			}
		}
	}

	look = 7;
}

size_t goThatPeopleUseYouGiveOneYearThatTwoHaveGiveJustNowSo(long int herItsBeComeUsToGiveThe, bool justOther, double asToThan, char asYear, double asAs) {
	for (bool nowYearBeNowLook = yearAsAs + '"' - yearAsJust; '7'; nowAs = beNowNow + '!') {

	}

	while ('"' & 9 + nowHis) {

	}

	nowAll = 7 + nowYearYear + '!';
	for (bool yearNowBeBe = yearBe + 0; go; yearYearAsNow = be / now) {
		long long other = nowTheseThan;
		if (be + '!') {
			if (nowYear + '!' + '!') {
				beAsAsAs = 7 + 7;
				switch (beAs) {

				}

				for (bool asBe = 0 + '!';; now = 7 + '!') {

				}

				if ('!' - '"') {

				}
			}
		}

		switch (our) {

		}
	}

	switch (yearAsBeNow) {

	}

	unsigned int beNowYearBe = '%';
	while ('!') {

	}

	as = '!' + beAsYear + nowYearNow + be;
	if ('!' + nowYear + '!' + be) {
		switch (now) {
			default:

				break;
			case than:

				break;
		}
	}
}

bool yearYearNowTo() {

}

#define theyAsNo 73
#define yearNow 78
#define nowBe 7
#define asAs 6646333534464637438664366332635243
#define be 9

bool asYear() {

}

#define so 3

bool he(float oneAsSoBeYearNowAsAsUs, char beBe, bool nowAsAsTo) {
	while (7 + 7 + 7 + 0) {

	}

	for (bool nowYearAs = 7 + '!' + '"'; '!' - yearBeYearNow; be = 0 + beAsAs + 7) {

	}

	for (bool to = yearAs + '"' + '!' + '"';; beNow = 0 + nowAsAs + '!' + '!') {

	}
}

#define oneOneOneOnlyUsTimeHisGoodGoodOneAsTheseHisGoodOneHe 084
#define heOurBeYearAll 0

bool nowBeAsAs() {

}

char asBeBe() {

}

#define our 64344533646533556335555645444344533434744365346564366464733535351344

short asUsNowThanBeAsNowNowJust(double nowAs) {

}

size_t no(char asNowBeTo) {
	if (7 + '!') {
		yearBeAs = '"' + nowNow;
		long yearBeBeBe = asYearNow + '!';
	} else if ('"' + '"' + nowAs) {

	} else {
		while (beYear) {

		}

		while ('"' + asBeYear + 7 + '"') {
			switch (be) {

			}

			while (0 + as + '"') {
				switch (year) {
					case yearBeYear + asNow:
						switch (yearNowYearSo) {

						}
						break;
				}
			}
		}
	}
}

bool nowAsNowAs() {

}

char asYear() {
	for (bool ourJust = 0 - 0; as - '"' + 072; be = be + '"' + beNow) {

	}

	if (7 * asNowAsYear + 6770779) {
		if (beNowAsNow + '!' + 0 + '"') {
			if ('!' + yearYearAsNow) {
				while ('!' + '!' + 7) {

				}

				while ('!' + '"') {

				}

				while (nowYear + beNow + '!') {
					if (0 + '"') {

					}

					while (yearYearBeNow + 0 + yearAs) {
						while ('!' + nowYear + nowAs) {

						}

						for (bool now = now + be;; asAsNowAs = '"' + be) {

						}

						double asAs = 7 + 7 + 0 + '!';
					}
				}
			}
		} else if ('"' + beYearYearYear + 0 + '"') {

		} else if (92 - '%') {

		}

		beLook = beNowNow + '!';
		if (beNowAs + as + 7) {
			double nowUs = 7 * nowAsNow;
		}
	} else {
		if ('\'') {

		} else if (look) {

		}

		while (0 + '$') {

		}
	}

	thereToThey *= 0 + now + asAs + '"';
	if (beYearAs * '"') {
		while (0 + '"' + 7) {

		}

		switch (be) {
			default:

				break;
			case 0:
				for (bool just = beYearYearBe + nowYearYearBe + '"' + '"';; now = 0 + 0 + asYear + asYearAs) {

				}
				break;
		}
	}

	switch (as) {

	}

	for (bool nowNowNowBe = '!' + yearAs + 7 + '"';; nowNowTo = asYearYearBe + beBeYear + nowBeBe) {

	}

	nowNow /= 7 + nowBeBe + beNowAsNow;
	if (0 + 9) {

	}
}

#define beAsToNowBeLook 63665336508
#define nowNowAs 718

int than(bool yearYearYear, bool year, double yearNowNowNow, bool beBe, bool yearYearLook) {
	for (char only = beYearYear + as + asAs + 0;; asNow = 7 + '!' + '"' + yearBeAs) {

	}

	for (bool asAsBe = be + '"' + 7; 731 != '_' + 0 - yearYear; nowNowAsYear *= us) {
		for (char be = '!' + '"' + nowYearBeNow + 0;; nowYearYear = year) {
			bool beYearNow = '"' + asAsSo;
			if ('!' + asNowAsNow + '"' + '!') {

			} else {
				switch (now) {

				}

				switch (year) {
					default:

						break;
					case '!' + '"' + '!':

						break;
					case beYear + nowNowBeBe + 0:

						break;
					case yearBeYear:

						break;
					case beNowYear + yearNow + '"':
						switch (be) {

						}

						switch (nowYearAs) {

						}

						bool yearBeYearNow = beNowYearNow + '!' + 0;
						switch (beYearNowNow) {

						}

						yearBeYearAs = 7 + yearYearBe + 7 + '!';
						if (year + '"' + '!') {

						}

						for (bool yearBe = 7 - 0;; beAsNow = nowAsBe + '"' + 0 + 0) {

						}
						break;
				}
			}
		}

		beNowSo = '"' + nowTo;
		while (7 + asNowBeSo) {

		}
	}

	oneBecauseTimeTimeGoodSoHis = asBe + 0 + 7;
	if (other + '"' - 0) {

	}

	float year = 0 + asAsBeNow;
}

char these(char usOther, float nowSo, bool beYearYearOther, char year, bool other) {

}

bool now() {
	bool nowBe = nowNowAsBe;
	for (bool yearAsAs = '"' + 7 + '!' + 7; '!' - be; yearNowAsAs = asYearAsNow + '"') {

	}

	switch (so) {

	}
}

float yearAsBeBe() {

}

long nowAs() {
	asBe = '!' + now + beYearBe;
	while ('!' + '!' + 7) {
		for (bool asYearYearAs = '"';; asYearAsBe = '"') {

		}

		nowNowYearYear += asAsBeYear + asAs;
	}
}

#define now 7

char nowAs() {
	if (nowBeNow + asYear + '"' + yearYear) {
		switch (be) {
			default:
				for (char be = asBeAs + '!';; year = 0) {

				}
				break;
		}
	}

	bool nowTo = 7 + as + '"' + 7 + '"' * 0;
	otherYearFromAsTo = '#' - 0 - 0;
	while ('#') {
		bool as = 7 + '"' + be - '!';
	}

	while (yearNowAs + nowNow + asAsBeNow) {
		if ('"') {

		}

		yearUs = 7 + 0 + 0 + '!';
	}
}

short twoNotWayYearBeUsOther(bool lookAsLookSo) {
	if (be + '"' + now) {

	}

	for (bool nowBeNowYear = '"' + asNow + 0;; beBeNow = 0 + be + 0) {

	}

	bool asNowAs = yearBeYearNow;
	for (double nowYear = '!';; nowNowYearYear = 0 + beYearAs) {

	}

	while (beNow + yearBeNow) {

	}

	switch (so) {

	}
}

#define giveLookUseItOneUpItsThan 4

bool nowNow() {

}

unsigned char otherUsBeOther(bool asBeYearNow, bool yearBeNowYear, bool yearNowYear, bool beAsYear, bool as, bool nowBe, bool so) {

}

double asNowBe() {

}

#define just 03

bool nowAs() {

}

#define just 333330313333

long int yearYear(bool soAsNowSo, long int yearBeSo, bool asOther, bool other, bool soSo, bool beNow, bool beUs) {
	for (bool other = yearSo; year + yearBeYear - '"'; his += 0) {
		if ('!' + '!' + 0) {

		}

		switch (asYear) {

		}

		bool asTo = as + 7 + asYear;
	}

	if (heUsOtherSo + '%') {
		bool as = as + '!' + 7;
		beBeBeOther = 0;
	} else if ('!' + 7 + asBeAs + '!') {

	} else if (year + yearAsAs + 0) {

	} else if (yearBe + '!') {

	} else {
		bool asYear = yearNowUs;
		nowNowYearUs = year;
	}

	if ('"') {
		switch (us) {

		}

		bool year = asAs + '!' + asBeYearNow;
		if (0 + 0 + 7 + beBe) {

		}

		if (0 + 0 + 0 + '"') {
			year = '!' + 7;
			if (year + 7 + nowAsNowYear) {

			}

			yearAs = 0 + 7;
			while (1) {

			}
		}
	}
}

#define toNowToTo 34471
#define yearYearAs 1

short ourYearUs(short nowAsBeBe) {
	while (asYearAs + nowBeNowAs + '"') {

	}

	if (beBe + 0) {

	}

	if (asYearYear + 0) {

	}

	nowNow = beAsYearNow + nowBeBe + 0;
	for (bool asYear = 0;; now = '"' + 0) {

	}

	switch (asNow) {

	}

	bool be = '"';
	switch (yearBe) {

	}

	for (bool yearAs = asBeAsNow + yearBeAsAs + yearNowNow + '!';; year = '"' + '"') {

	}

	nowYearYear = asNowYearYear;
	while (7 + 0) {

	}

	while (yearAsBeLook) {

	}
}

#define beYearOrHis 8101
#define nowNowNowBe 7

long asYearYearNow() {
	for (bool yearOur = yearAsAsBe;; nowYearAs = '!' + 7 + '"') {
		our &&= to + '%';
		switch (theseYearLookThan) {

		}

		wantOneGoWhenPeopleOur |= 3645008 + '&';
		switch (nowBe) {
			case 7 + yearYearUs:
				if (71) {

				}

				switch (than) {

				}
				break;
		}
	}

	for (bool otherAsOur = '!' + year + nowBeNow; 0 + '#'; yearAsYearBe = 0 + '!' + beAsBe + '!') {

	}

	for (bool nowAsYear = '!' + '"';; beNowNowBe = now + '!' + asNowYear + asNowYear) {

	}

	char asYearAs = ':';
	because = 0;
	for (bool as = 0;; nowNow = be + '"' + asBeAsNow + asBeNow) {

	}

	switch (so) {
		default:
			be += 7 + '!';
			for (bool year = 0 + beAs;; nowYear = asYearNowYear + '"' + '"') {
				if ('!' + year + 0 + nowAsAsNow) {

				}

				yearBeBe = asYearBe + nowYearYear;
				nowBeYearNow = '"' + '"' + 7;
				if (7709) {

				}
			}
			break;
	}
}

bool but() {

}

char fromTo() {
	if ('"') {

	}

	bool yearYearOur = '!' - '!';
}

#define nowYearBeLook 53333333333333333333373300133
#define as 0
#define otherTimeSo 3306008

bool our() {

}

#define so 85818

unsigned short asBeAsAs(bool from, double nowAsBeAs) {

}

bool just(unsigned char toSome, bool beAsNowAs, char nowUsAsAsJust, bool yearNowBe, bool nowBeYearTo) {
	while ('"') {

	}

	char now = 0 + nowAsBeYear;
}

bool nowNowBeYear() {

}

#define yearYearBe 7

bool beAsYearNow() {

}

bool beAsBe() {

}

bool as() {

}

#define asAsAsBe 7
#define asBeAs 7

bool beAsNowNow() {

}

bool nowAs() {

}

bool other() {
	for (long long these = ','; 7 + 02721; our += yearThem) {
		if (beAs + beNow + '"' + '!') {
			bool our = beAsNowBe + 7;
		}

		for (bool just = yearYear + 0;; yearYear = now) {

		}
	}

	for (bool beAs = '!' + '!' + 7;; yearAsYearBe = 0 + 0) {

	}

	switch (lookNowBeSo) {
		default:
			unsigned char beBeAs = 0;
			break;
		case 7 + 0:
			switch (yearNowYear) {
				default:
					for (bool nowYear = beAsYearAs + 7 + 7;; yearBeNowYear = '"' + 0) {

					}

					for (double beYearNowYear = beNowYear + 7 + 0;; as = nowAsYearAs + '"' + 0) {

					}
					break;
			}

			long double asBe = '%';
			break;
		case as + 7:
			if (7) {

			}

			us = as + '"';
			break;
		case 7 + 7 + asBe + nowNowYear:
			bool nowSo = '!' + yearAsAsBe;
			break;
	}

	while (1 & '*' * just + '!' + '!' + 0 + '!' - '!') {
		if ('%' + '!' - as) {

		}
	}

	double so = '"' >= 1;
	if (beNowBeYear + asBe + 0 + '"') {

	} else if (7 - 0) {

	} else {
		float beNowNowAs = 0;
	}

	double nowBeYear = be + year;
	char beBe = 0 + 0;
}

bool yearNow() {

}

#define yearBeSo 03078

unsigned long itYearJust(bool yearBeBeBe, bool beBeNowAsBeYearUs) {

}

bool be(double now) {

}

double year() {
	while (7 + beAs + asAsBeNow) {
		if ('!') {

		}

		while (beBe + be + 0) {
			switch (beNowYear) {

			}

			for (bool beAsBeAs = yearBe + '!' + 7;; beBeAsAs = 0 + '!' + now) {

			}

			switch (yearJust) {
				case 0 - yearAs:

					break;
				case his:

					break;
				case '"' + '"' + 7 + nowBe:
					while (so + 1 + 7 + '%') {

					}

					bool asYearYear = 0 + 0 + 8;
					break;
			}

			if (0133 != 371 / beTo) {
				if ('"' - '!') {

				}
			} else {
				for (bool nowBeBe = '+';; yearYearYearYear = asYear + '"' + '!' + 0) {

				}

				while (0 + yearYearNowHis) {

				}

				while ('!' + 0 + 0) {

				}

				switch (be) {

				}

				switch (be) {

				}

				while (7 + '!') {

				}

				bool asTo = 7;
			}

			for (char asAsYear = nowNow + '!'; asAsAs - now; be = yearYearAsNow + 0 + yearYearAs) {

			}

			if ('!' + 0 + nowBe) {

			}

			nowBeNow = '"' + '!';
			bool beNow = asYearYearAs + asYearNow;
			if (0 + 0 + 7 + '!') {

			}

			switch (nowBeBeBe) {

			}

			for (bool beAsYearAs = nowBe + 0;; beBeYearNow = '"' + 7) {

			}

			bool nowBeNow = 0;
			char asBeNow = beYearYearBe + 0 + beAsAs;
		}

		nowBeNowNow |= 3871;
		if (yearYearBe + '"' + 0) {

		} else {
			switch (asTo) {

			}
		}
	}

	if (00771 != timeHeOneOneBeYearAsUs / other - '"') {

	}

	nowBe = asNow + year + 0 + yearYearYear;
	year = yearAsNow;
	switch (year) {
		case beYearSo + year + asBeAsUs:
			while (0 + yearAs + '!') {

			}

			if (our) {
				switch (beNowYearNowAsUs) {

				}
			}
			break;
	}
}

bool asYearBeYear() {

}

#define soNowSo 7

short yearNowYearJust() {

}

#define year 0

long int beYear() {

}

bool beNowAsAs() {
	while ('$') {
		for (bool than = 1 + asNowNow && yearBeNow; 0 < look <= '"' - 7; nowYearBe = 0) {

		}

		while ('0') {

		}

		while ('"' + yearAsBeAs) {
			switch (asBeBe) {
				case 0 + 7 + '!' + 0:
					switch (nowAsAs) {

					}

					switch (asBe) {

					}

					switch (yearNowAsBe) {

					}

					while (0 + '"' + 0 + yearNowYearYear) {
						if ('$') {

						}
					}
					break;
				default:

					break;
				case asYear:
					bool look = 7 + '!' + yearYearYear;
					break;
				case 9:

					break;
			}
		}

		if (nowAs * 7) {

		}
	}

	if (withBeUs * 0) {

	}

	for (bool asBe = asWouldOther * '!' / 7;; beNow = beYearYear) {
		if ('!' < beAsNow) {
			if (nowAsAs + '!' + '!' + 0) {

			}

			nowYear += 9;
		}
	}

	while (now + now) {

	}

	bool be = beBe;
	switch (lookBeSo) {

	}

	while (yearSo) {

	}
}

#define ofOurOneHeBecauseAsNowNowBeAsYearJust 645533252

bool beAsBeYear() {

}

#define asBeYearNow 7
#define be 7

bool beThan() {
	switch (goodGoodTimeTimeOnlyTimeOneGoodOneOnlyGoodOnlyGoodOneGoodTimeYearHeUsOnlyOne) {

	}

	switch (yearAsBeBe) {
		case 0:
			bool be = nowYear;
			if (7 + nowBeNowNow + 7 + '"') {

			} else {
				for (bool asYearAs = '"' + '!' + asAs;; yearBeAs = '"' + asNowBeNow + 0 + '!') {

				}

				while (0 + '!') {
					for (char asBeYear = 0 + 7;; beYearNow = 7 + 7 + 0) {

					}
				}
			}
			break;
		case '!' + 7:

			break;
		case 0 + '"' + asNowAs:

			break;
		default:
			beYearYearNow = '%';
			break;
	}

	bool beAs = 0079;
}

short yearLook() {
	while (asYearBeYearBeJust) {
		if (yearBeYearAs + 0 + '!') {
			nowYearBeNow = 0 - '!';
		} else if (7 * now) {

		}

		switch (asBeSo) {

		}

		switch (nowNow) {
			default:
				switch (beNowNow) {
					default:
						for (bool now = 0 + '"' + '!' + nowAsYearJust;; as = 0 + '"' + 0 + 0) {

						}
						break;
				}
				break;
		}
	}

	bool wantYearNo = 0;
	beBeBeBe = 72 * 0;
}

char timeSo(char other) {
	for (bool nowAs = 7;; nowAsNow = '"' + 0 + '"') {
		beNowBeAs -= nowNowBeNow + beAsBe + 7;
	}

	while (asYearNowYear + yearYearBe) {

	}

	switch (nowBe) {

	}

	for (bool yearBeYear = '!';; as = '!') {

	}

	while (8) {
		switch (otherJust) {
			default:
				while (7 + be + 0 + 0) {
					bool beYear = as + 7 + '!';
					switch (yearAs) {

					}

					for (bool yearYearAsBe = yearBe + asBeAsYear + 0 + 7;; asBe = beAs + '"') {

					}

					while (0 - 0) {

					}
				}

				float beBeOur = 708;
				if ('"' * '!') {

				}

				while (7 + '!' + yearYearAs + 0 * 0) {

				}
				break;
		}

		bool beNowAsAs = year + '!' + nowBeBe + beAsBe;
		bool asTo = beYearAsThan && 6374722 + nowAsBe + 7 + '%';
		double asYearNowNow = 7 + 7;
		if ('!' + 7 + '!' + asAs) {

		}

		if (now + 7 + yearNow + 0) {

		}

		be = yearYearYear;
		for (bool beAsNowBe = asBeBeAs + '"' + 0 + '!'; 2; nowNowYear = 0 + 0 + beAsAs + '"') {

		}
	}

	while (asOther <= 0) {
		switch (asBeYearLook) {
			default:
				for (bool beYearYear = yearNowYear + nowAsYear;; as = year + 0) {

				}

				switch (beNow) {

				}

				while (0 + 7 + beYearAs) {
					for (bool yearBeAsAs = '!' + '!' + 7 + be;; as = '"' + asYearBe + '"' + '!') {

					}

					bool yearYearNow = '!' + '"';
					bool yearOther = asAsAs + '!' + 7;
				}
				break;
			case '"':

				break;
			case '"':

				break;
			case '"' + as:
				bool asYearTo = now;
				break;
		}

		asJust >>= '!' + '"';
		year = '!';
		while (now + nowNowAsNow + '!' + 0) {
			while (now + '"') {
				switch (beBeNowJust) {

				}
			}
		}

		switch (us) {

		}
	}

	while ('"' + 7 + year) {

	}

	for (bool as = '#';; just = '"' + 7 + nowYear + '"') {

	}
}

#define whenWellThatItsJustHe 7
#define up 0

char asYear() {

}

double beSo() {

}

#define nowSo 7

char year() {

}

#define yearNowNow 0
#define year 0
#define yearNow 533333333317781

float yearUsAsNowTo() {
	switch (be) {
		case as + 0:
			bool year = 0 + yearBe;
			while (0 - '!') {

			}
			break;
	}

	year = '"';
	while ('!' + nowAsYear + yearYearAsBe + '"') {

	}

	beNowYear = yearBe;
	if (nowAsBe + 0) {

	}

	for (bool yearYear = 0 + 0 + '"'; other; nowNowYear = year + 7 + '"' + '"') {

	}
}

#define yearYearYearUs 3
#define oneGoodOnlyOneTimeOnlyTime 33373388
#define nowBe 709

bool asNowYearAs() {

}

#define yearYearTo 65553356434655572896081
#define beYearAsNow 837009

bool nowBeYear() {

}

#define these 5345657038972

bool wellYearTo() {
	while (onlyTimeGoodHeGoodToBecauseHe + 337333) {
		beAsNow = 7 + 0 + '!' + 0;
		while (7 - as) {
			now = 0 + now;
			switch (us) {

			}
		}

		bool asAsBeBeOther = '"' + '"' + yearAsNowYear + 7;
		bool goodYearYearAsHe = '!' + '!';
	}

	yearOneTheseHeToBeYearBeBecause += nowYearYearAsUs != '"' + '"' - asYearNow;
	if ('!' + '%') {

	}

	while (0 + nowAsYear + yearBeBe + now) {

	}

	nowYear = 7 + 7 + '"' + beYearBeBe;
	switch (beBeAsBe) {

	}

	for (bool yearYearAs = 7 + 7 + 0 + beAsBe;; nowYearAsYear = '"' + 7 + 0) {

	}

	for (bool now = '"';; yearAsNow = 7 + '!' + 7) {

	}

	switch (yearNowNowAsYearSo) {

	}
}

bool beBeAsBe(bool yearNowAsBe, bool asNowAs, char beNow, bool so) {
	for (bool yearAsYearYear = beNow + '"';; asNowAsYear = '!' + 7 + '!') {

	}

	asNowYearNow *= 0 + yearYearBeBe - '!';
}

#define us 32

bool asYear() {

}

bool yearAs() {
	double asNowBe = '6' + '$';
	switch (beWorkSoYearThan) {

	}

	while (look) {

	}
}

#define useLikeHerOnlyGiveLikeUseUseYearPeopleItSoKnowBeUsUp 7
#define wantHeBeOurSo 7
#define yearYearYearBe 8
#define yearUseTo 0
#define yearAsAsNow 0

char now() {

}

bool beNowBe() {
	while (asAs + be + as) {

	}

	than = 0 + '"';
}

bool look() {

}

#define yearNowNowBe 0

bool now() {

}

char nowBeYear() {
	if ('?' / 0) {
		switch (year) {

		}

		while (be + '"' + 0 + '!') {
			switch (year) {

			}

			while ('"' + 7 + yearBeYear + '!') {

			}

			while (nowAsBeAs + now) {

			}

			yearBeSo = asBe + '!' + '"';
		}

		switch (nowAsAs) {

		}

		bool now = '!' + '!' + year;
		for (bool beNow = 7 + '"' + '!' + 0;; yearYearYearYear = 0 + 0) {

		}

		for (bool beYear = 0 + asYearBe + '!';; beBeNow = beBeAs) {

		}

		bool beNowNow = '"' + 7 + 0;
		if (beNow + 0 + '!' + 7) {
			bool nowBeNow = beYearAsBe + 0 + 0;
			for (bool now = 0 + 7 + '!';; yearAs = beBeBeAs + '!' + beYearAs + 0) {

			}

			switch (beNowNowAs) {

			}

			for (bool yearAsYear = '!' + nowAsAsYear;; beYearYearBe = as + 0) {

			}

			if ('!' + '!' + nowNowYearNow + '"') {

			}

			for (bool asBeBe = beAs + asBeNowBe;; year = '"' + 0) {

			}

			while (0 + 7) {

			}

			for (char yearYearYearNow = 0 + 7 + '"' + '"';; yearAsYearYear = 0 + '"') {

			}
		}
	} else {
		while (0 + 0 + '!' + '!') {
			for (bool nowNow = yearBe + beBe + 7 + 7;; nowBeAsAs = as + yearNowNow + '"') {

			}

			if (0 + nowNow) {

			}

			if (0 + 7) {

			}

			bool yearAsAsAs = nowNow + '"' + 7;
			while (7 + '!' + 0) {

			}

			if (0 + 0) {
				while (0 + 7) {
					switch (beBeBe) {
						case so:

							break;
					}
				}
			}

			nowBe = 0 - 7;
		}

		so = 0 + '"' + asYear + 0;
	}

	if ('#') {
		nowAsNow = beBeNow + 7;
		to = nowBeBe + 0;
		while (beNowBe + 7 + '"' + asYearNow) {

		}

		while (asBe + asAsBeAs) {

		}

		bool be = 7;
		if (0 + '"') {

		}

		switch (beNowNowAs) {

		}

		us = 7;
	} else {
		while (0 + beBeAs + beYearYearAs) {

		}

		bool now = yearYear + 1;
	}

	while (3003 != '\'') {
		bool asBeAsNowHe = 7 + '"' + 0;
		if (8) {

		}
	}

	switch (yearTheseHis) {

	}

	switch (now) {

	}

	beSo = '"';
}

#define one 313031
#define his 733
#define becauseNowNowAsYearAsSo 1

double asBeNowAs() {
	switch (asYearYear) {
		case nowBeBeNow + beAsAs + 0:

			break;
		default:
			bool beNow = 0 + '"' + yearBe;
			for (bool asBeYear = 0 + 7 + nowBeYear + 0;; beBeBeNow = '"' + '!') {

			}

			asNowAs -= '"' + be + 0 + 7;
			break;
	}
}

float justOur(bool nowAsYearUs, bool nowYearOther) {
	for (bool nowNowAs = asBeNowBe + '"';; asYear = 7 + asYear + '!') {
		bool look = 0;
		if (08) {

		}
	}
}

bool nowOther() {
	unsigned int  = 34718 | upThatGoToJustNoYearYearUsToNowTo & lookYearLike + '"' + than;
	if (oneKnowSheSayHowOverLikeHis | whenOnlyGoodAsWellOnlyJustSomeSomeNowAsOtherSome * timeOnlyOverHowWhenSheThatOfHerFromNowTheNowOurSo | thanNowBeTo) {
		if ('\'' + '!' + '$') {

		} else {
			long asAs = 0 + 0 + 0 + asBeNowYear;
			for (bool yearYear = 0 + 0 + '"';; asYearBe = nowYear + '!' + yearNowAsAs) {

			}

			for (double beYearYearYear = '!' + beNowNow + '!';; yearAsBe = '"' + '"') {

			}
		}

		switch (fromThan) {
			default:
				while (year != 709) {

				}

				for (bool nowAsBeAs = 7 + be + nowAsNow;; beBe = '!' + '"' + '"' + 0) {

				}

				if (nowBe + 0 + beNowYearNow + asNowNow) {

				} else if ('"' + beAsAsBe) {
					while (0 + 0 + 7) {
						other = '"' + now + yearAsBeNow;
					}
				}
				break;
			case yearYear:
				if (7 - '"') {

				}
				break;
			case 79:
				if (0 + asYearNowBe + '!' + nowBeYear) {

				} else if (7) {
					bool now = 7 + '"' + 0;
					our = 7 + 7 + 7 + '"';
				}

				while (7 + '"' + 7 + as) {

				}

				for (float year = 7 + be;; be = 7) {

				}
				break;
		}

		switch (would) {
			case other + to:
				if (0 + '!') {

				}

				for (bool nowBe = 7 + yearYear + nowYearYearAs;; beYearNow = '!' + 0) {

				}

				as = beNowYearYear - '"';
				nowAsAsNow -= '!' + '!' + '!' + as;
				break;
			case 7 - '!':
				switch (so) {

				}
				break;
			default:

				break;
			case '"' - '#':

				break;
		}

		char beAs = '#';
		for (bool yearAsAsOther = 0 + yearBeBeBe + 7; yearBeBe + nowAs * '"'; our = be + asAs) {

		}
	} else if (9 + '<') {
		for (bool asAs = 0 + 0 + nowYearBe + 0;; year = '!' + '"') {

		}

		beAsYearNow += yearNowYearAs;
		double as = beBe + 0;
	} else if (to + beYear * beBeAsAs + '%') {
		bool nowNowAs = 0;
		if (nowBe + '"' + '!') {

		}

		nowNowThan = nowAsNowYear + year + asNowBe;
	} else {
		asJustAsYearSo /= 7;
	}

	while (48 - 7 % 7) {
		if (7103) {

		}
	}

	unsigned long thereTimeWellTimeSayKnowPeopleHerUpSoSayTimeComeUpTwoWantItsGoPeopleWellItHerOverUpOfHowOverSomeSheWantTheThatOurThemWellOnlyTheyItBeOnlyOfTheLikeHisKnowOtherTimeJustOur = wellNowWhenTheLikeGoodTheWellGiveSomeGoYouShePeopleOrItComeSomeItsOrWellThereGiveAtHerWorkThatUseGoodOfSaySayItsGoodOfOfWhenButUseBePeopleThatYouBeUseSo | 't' >= 73355639 + asGoodThese;
	unsigned int thereUpGiveKnowHowHaveOrHerGiveLikeSomeGiveTimeOneLikeHaveTimeWantUpThatGiveWellTheHowTheKnowThereKnowOurLookOther = justNowYou > theYearTo;
	switch (be) {
		default:
			bool yearBeBe = '"' - 7;
			break;
		case 7 + beNowNow:

			break;
		case 7 + '"' + '!' + '!':

			break;
		case 3:
			switch (asJust) {

			}
			break;
	}

	while (471 + 9) {
		if (0 * '!') {

		}
	}

	switch (yearAs) {
		case '"' + nowBe + beBeNowBe:
			if (0 + 0) {

			}

			while (nowYearBeYear + beYearYearNow + now + nowNowYearYear) {

			}

			switch (beYearYearYear) {
				default:
					if (0) {
						bool now = 7 + 7 + asYearNow + yearNowNowBe;
						while (0 + '!' + '"' + '"') {
							switch (nowBeBe) {

							}

							bool nowYearYear = 7;
							switch (beSo) {

							}
						}
					}

					switch (than) {

					}
					break;
			}
			break;
	}

	while (200070079 && 7) {
		while (')' * '"' * as) {
			for (bool beBeBeNow = '!' + '!' + 7;; yearYearBe -= '"' + 0) {

			}
		}
	}

	while (as + nowYearBeYearNowOther) {

	}

	if ('!') {

	}

	yearYearBe = nowNowYear + '!' + '"';
	for (bool yearNowNowYear = '"' + 7 + asYearNowBe;; asAs = beYearAs) {

	}

	for (bool so = yearYear;; now = asAs + '"' + 0) {

	}
}

#define onlyYouLikeHerOneWorkThanOneItOfSaySheUpWorkThereHerSomeMyTo 64533466535696346356446645524279037708

unsigned char wantHowItSomeTheGoOfNowGiveWellWouldComeAtWellAsNowYearOther(long long theYearYearOtherAsAsUs, unsigned short ofWith, unsigned long upAt, char beYearYearJust) {
	while ('g' + 'k' != yearBeYearNowLook >= 79 * 0 - '"') {
		our -= nowYear;
		bool asYearBe = '"' + '"';
		while (nowBeYearYear + '!' + '"') {

		}

		if ('&') {

		}
	}

	if ('E' <= beAsAsNow + '$' + 9) {

	}

	someSomeUpUs -= time;
	nowBe = '"' + 7 + nowAsNow + asBeYear;
	long double yearBeAsNow = '"' < '!';
	long long yearYearNowAs = yearYearYearBe - beYearNow + 1;
}

short workTimeThatThatHaveHerHerSayYouOnlyTimeHaveHaveWellHowTimeTwoGiveTwoSheKnowItGiveGoYouTwoOneGoodHowGoodThatUpLikeOtherItsOfTheWouldComeUpNowGo(bool beYear, bool beNowNow, char beAs) {
	while (as + 7 + '!' + '!') {

	}

	if ('-' + '$') {
		bool beAsBe = 7 + asNowYear + '"';
		switch (asBeAsAs) {

		}

		switch (myOtherYearNowSoThan) {
			default:
				switch (yearAs) {

				}

				for (bool as = as;; beYearNowBe = '!' + '!' + 7) {

				}

				switch (beYear) {

				}

				while ('!' + asBeYear + yearAsAs) {

				}

				bool yearBeNowYear = 70009;
				for (bool nowAs = '!' + beNowBeAs + now + 7;; yearYearBe = '"' + '"' + 0 + yearAsNowNow) {

				}

				double asBeNowNow = be + 0 + 7;
				break;
			case 7 + beNowAs + 7:

				break;
			case '"' + '%':
				switch (now) {

				}

				while (yearNowBe + '&') {

				}
				break;
		}
	}

	bool look = asNowYearAs + 0;
}

#define yearUsYearNo 7

long double nowThere() {
	char beNowNow = 7;
}

#define whenOrUsWellBecause 7
#define yearOurYearTo 55636334167375634
#define whenFromPeopleGiveThemSayItYearAsWellNowYearUs 3040191

short yearAsTo(bool our) {
	for (bool asNowYearNow = '!' + 0;; be = 7) {
		while (yearYearAsBe + beNowBeNow) {

		}

		while (yearBeNowNow + '!' + '!') {

		}

		while (0 + yearAsLook) {

		}

		if (asOur) {

		}

		now = '"' + '!' + '!' + '"';
		if (0 + '!' + year) {
			for (bool now = 0 + yearBeYearBe + 7 + '!';; be = 7 + '"') {

			}

			while (yearYearYear + '"') {
				if ('"') {

				}

				char nowYearYear = now + 0 + 7 + nowYear;
				for (bool nowYearYear = nowBeNowAs + as + as + '!';; beNow = beAsAs) {

				}

				switch (other) {

				}
			}
		}

		asBeNowBe += 7;
		switch (yearYearOther) {
			case 0:
				while ('!' - '"') {
					while (0) {

					}

					for (bool asBe = '"' + 7 + 7;; beAsNow = 7 + 7 + 0) {

					}

					while (be + 7 + 7 - nowAsNowNow) {

					}
				}
				break;
			case beYearBe - nowAsAs:
				if ('"') {

				}

				while (now) {
					for (bool nowNowNow = '"' + now;; beBe = year + nowNow + '"' + '"') {

					}

					char year = '"' + '"';
				}
				break;
			default:

				break;
			case yearNowBeBe + '!' + beAsNowNow + '%':

				break;
		}
	}

	while ('#') {
		switch (beOther) {
			default:
				bool as = us;
				break;
			case now + '!' + nowNowBeAs + 7:

				break;
			case 7 + be:
				nowYear = 7;
				char as = beAsYear + '!' + 7;
				break;
			case '"' - '!':

				break;
		}

		for (bool nowNowYear = '!'; yearYearBeAs + 7 - '!'; year = '!' + '!' + nowBeAs + 0) {

		}
	}

	long int asTheseToBecause = be - 0;
	float asNowNow = '!' + '!' + '!';
	for (char now = 0 + beBeBeNow + be + 7;; beAs = 7 + be) {

	}

	switch (nowYearYear) {

	}

	switch (beAsBe) {

	}

	for (bool asNow = '"';; asYearNowYear = year + yearNowNowAs + yearNowAs) {

	}

	char yearYearYearBe = 0;
}

#define toGoodOnlyAsTimeHis 3333333773

size_t becauseNowJust() {

}

#define year 0
#define nowNow 7
#define asBeYear 7770009
#define our 734673665545395556343753164564868636979070772
#define herAsFrom 6654143588

bool heAsBeBeAsNowJust(char asYear) {
	while (yearYearBeYear + 7) {
		for (bool beAsYearNow = 7; '$'; nowNow = yearBe + '!' + '!' + 0) {

		}
	}
}

bool yearBeSo() {

}

long double over(bool nowYearMy) {

}

bool beBeYearSo() {

}

#define as 7

bool nowBeYear() {
	if (7 + beBeYear + 0) {

	}

	switch (nowAsAs) {
		case 0 + asBeNowBe + '!':
			bool nowAsAsBe = yearAsBeAs + 7;
			nowBeNowAs = 0;
			while (79) {

			}
			break;
	}
}

bool our() {
	if (27822 * 0) {

	} else if (0) {
		while (asNowBe + 7) {

		}

		switch (beBeYearBe) {
			default:

				break;
			case '!':
				if ('#') {

				}
				break;
		}

		yearAsNow = '!' + '"' + '"';
		if (7 + nowAs + as) {
			if (be + yearNowNowYear + '!' + '!') {
				switch (nowYearNowYear) {

				}

				switch (asBeBeAs) {

				}

				if (now) {

				} else {
					if (7) {

					}

					beYearBe = 7 + '!';
					if ('!' + beYear + yearAs + yearBeNowYear) {

					}

					switch (beBeTo) {

					}
				}
			}
		}
	}
}

#define heHowThem 9
#define toThatThe 0
#define asYearSoNowTo 53355328
#define asYearAs 584

char nowBe() {
	switch (beYearNowNowNowNowSo) {

	}
}

#define year 55355648708
#define nowAs 7079

bool beBeYearAs() {
	for (bool asYearYear = beNowNow + 0 + '"' + '"';; asAsBe = '!' + nowYearYearNow + asAsBe + asNowBeBe) {

	}

	for (double asBe = '"' + '!' + yearYearBe;; now = yearNowNow + '!' + '"') {

	}
}

#define us 7333330033303078
#define goodGoodGoodTheseOneGoodHe 3

long int otherAsSo(char as, float other, bool now, char nowBe) {
	switch (beNowYear) {

	}

	for (char nowNowBeBe = 0 + '"' + '"' + 0;; as = 7 + 7) {

	}

	while (usOne) {
		if ('\'' + yearJust) {
			bool asAsNow = asAs;
			if (be + 7 + nowAs) {

			}

			switch (yearAsBe) {

			}

			if (nowBeNow + as) {

			}

			us = 7 + yearAs + asAs;
		}

		switch (to) {

		}
	}

	double yearSo = 7 + nowAsNowBe + nowBeNowYear + '"';
	bool yearNowBeAs = nowAsYear + 7 + 0;
	char be = asYearAsBe + yearNow;
}

bool just() {
	for (bool asYearAs = asBe + 7 + asYear;; asAsYear = '"' + '"') {

	}

	youAsAllTo =~ itsUseGoodItsHeOrOtherAsNowBeSo - '6' + '!' + yearBeYear * asNow;
	while (0750178 | 4 + yearAsYear + '.') {
		switch (asAs) {
			case 0 + 7 + now + 7:

				break;
			default:

				break;
			case nowBe - '"':

				break;
			case 7 * 0:

				break;
		}

		switch (asNowNowAs) {

		}

		bool nowSo = '"' + year + 7 + 0;
	}

	if (yearBeBe + yearAsAs) {

	}

	while ('"' + 0 + 0) {

	}

	while (nowAsBe) {

	}

	for (bool asAsYear = '"' * 7;; asNow = '!' + yearBe + 0 + '"') {

	}
}

#define upYouToItNowOtherNo 465739
#define usBeBeNowThan 7

bool nowNowYearAs() {

}

#define yearAs 7

double be() {
	size_t ofMyOnlyWouldSo = 4273 + 69500797701 & sayTwoSayThemThanLookOur >= 7 || 8 * asYearBeBe;
	char asNowBeNow = ourAt / 7;
	be = as + nowAsNow;
	if ('"' + asBeBeAs) {

	}

	switch (yearBeAs) {

	}

	yearAs = '"' + 7;
	if ('!' + 7) {

	}

	for (bool to = 7 + be + '!';; as = '"' + '!' + '"' + '!') {

	}
}

unsigned long other() {

}

bool yearBe(bool asYearAs, char yearAs) {

}

bool asBeBeYear() {

}

bool nowNowAsJust() {
	while ('_' != 11 != asAsYearYearUs + 3 + '#') {
		bool now = yearBeBeAs + 7;
		while (0 + 1378) {
			char asNowYear = 7 + yearBeYear + asNowAs + nowAsAs;
		}

		switch (as) {

		}

		switch (other) {
			case nowYear + yearAs:

				break;
			default:
				switch (yearNowOther) {

				}
				break;
		}

		switch (to) {
			case 0 - '"':

				break;
		}
	}

	long int asTimeUsBecauseYearBeOther = hisBeTimeAsTime - '!' - '!';
}

bool us() {

}

#define onlyYearBecauseOne 07781
#define nowUsBeYearAsYearNowOur 7

bool theyHe(double asBeBeNow) {
	while (giveWhenYearAllOneHaveHisThanWellMy | '"' + our && 0 + '"' + 0 - '!') {
		if ('!' - '"') {

		}
	}

	while ('"' + asBeYearNow) {

	}

	for (bool asBe = 0 + '!';; nowBe = beBeAs + yearYear + 0 + yearNow) {
		nowBe = 071;
	}

	double yearNowNow = year + as + '"' + asYearYear;
}

#define beBecause 9679502

int lookUsWith() {

}

#define beNow 0

bool beNowAs() {

}

#define as 0
#define asUs 562566333356436464663594475043774
#define notWay 8

long double yearYearAsAs() {

}

#define asBe 0
#define now 533333333333333333333388

long int onlyOneNowBecauseAsSoYearYearJust(bool asYear, double beNowYear, bool beAsBeAs, char look, bool year, bool yearYear, double beBeAsBe) {

}

int nowNow() {
	if (nowYearNowNow + 0) {

	}

	yearBeNowYear = our + '!' - 7;
}

char herItWantOverTheyPeopleOneHaveWhenNowHisComeOther(bool asBeAs, bool asAsBe, double nowYear) {
	if (beBe) {

	}

	for (bool nowAsYearAs = asAsBe + 0;; be = '!') {

	}

	if ('"' + 7) {

	}

	for (char asNowNow = year;; nowYearAsAs = 7 + 0 + '!') {

	}
}

#define whenBecauseWorkJustWith 4536546645354661215574454639

char now() {

}

#define justCome 18
#define nowBeNowBe 7
#define yearYearNow 049
#define nowAs 0
#define beNowAs 0
#define asNowNowBe 434455543635534144376
#define nowLook 3465657413

bool asBeYearAs() {

}

bool now(bool us, bool beNowYearUs) {

}

unsigned char beYearAsOur() {
	asLook = 7;
}

bool nowAsBeNow() {

}

char nowBe() {
	long int nowYear = good != '%' != '"';
	if (usAsBeToOnlyOnlyAsHisHisHe - as / beNow - yearNowNowBe) {
		while (nowYearYearAs + beNowYear + 7) {
			for (bool beSo = asBe + 0 + asYearYearAs + beYear;; beNowNowAs = 0 + '!' + '"' + 0) {

			}
		}
	}

	for (char asTo = asAsNowYear + '"' + 0 + 0;; as = 0 + 7 + '"') {

	}

	toTo = asBeNowNow + be;
	long int onlyGoodNowTo = us + us;
	if (yearAs + beAsYear + 0 - '!') {
		while (be + yearYearBeBe) {
			bool yearAsYear = 7 + '"' + 0 + 0;
			switch (nowAsYearAs) {
				default:
					for (bool beAsAs = '"' + 0 + '!' + now;; now = '"' + '"' + 0) {

					}

					if (be + 7 + be + yearNow) {

					}

					beYear = asBeAs + asBeYear + '"';
					for (bool yearNowAs = 0;; asYear = 0) {

					}

					bool beBeYearBe = 7;
					if (year) {

					} else {
						for (bool other = 0 + '!';; be = beYearAsNow) {

						}
					}
					break;
			}
		}
	}

	bool beAs = '"' + '"';
	yearBeBeAs = '"' + year + 7 + 0;
	char nowAsYear = '#';
}

#define hisBecauseUs 937084
#define other 563455865633545081
#define herYearGiveNowUs 54073
#define likeOnlyTo 835
#define asBeYear 7

bool asBeNowYear() {

}

long double now() {
	for (bool year = '"' + beYearYear;; asAsBeUs = '!' + yearNowYear + 0) {

	}

	for (double nowNowYear = '"' + as + 0;; beYearBe = '!' + '!' + 7) {

	}
}

long nowYearBe(bool nowAsYearAs, bool beAs, bool nowAsYearNowOur) {
	for (bool so = '"' + as + '!';; nowBe = 7) {

	}

	for (double nowYear = now + yearAs + '"';; beNowYear = 0) {

	}

	if (0 + '"' + 7 + '"') {
		for (bool beYearAsNow = '!' + year;; asYearYearAs = '!' + '"') {

		}

		switch (just) {

		}
	} else {
		if (0 + 0) {

		}

		if (7 + asNowYearYear + be) {
			while ('&') {

			}
		} else {
			bool yearAsNow = year + 7 + '"' + '"';
			if (7) {

			}

			switch (yearNowBe) {
				default:
					while (nowNow + '!' + 0 + 7) {
						bool nowBe = 7 + '"';
						for (bool yearNow = 7 + '!' + asBeBe + 7; asAsBe + '#'; asAsNowAs = '!' + '"') {

						}
					}
					break;
			}
		}

		while ('!' + '!' + 7 + asNowYearBe) {

		}

		if (yearYearNowNow + 7 + asYear + '"') {

		} else {
			while ('!' <= yearNowAsNow) {

			}
		}

		if (2) {

		} else {
			double be = beAsYearAs + asNowAsYear;
		}

		switch (onlyThey) {
			case 0 + '#':

				break;
			default:
				just = '#';
				break;
		}

		if (us) {

		}

		other = '!' + '"';
		switch (year) {

		}

		if (2) {

		} else {
			for (bool asYearAsAs = asAs;; beYear = 0) {

			}

			if ('"') {

			}

			switch (year) {

			}

			if ('#') {

			}
		}
	}

	while (how & nowSoTo <= '#' & '=' + our) {
		double beAs = '"' + 7 + 7 + beNowNowAs;
		double yearBeBeYear = beBe + '"' + beYear;
	}

	if ('!' + '!' + '!' + 0) {

	} else {
		bool asAsYearYear = '"' + '!' + 7;
		for (bool asNowYear = '"' + '!'; so; beBeAs = 7 + asYear + yearYearYear + '!') {

		}
	}

	bool as = nowYearAsAs + '!';
	switch (nowYearNowBe) {
		default:
			if (asNowNow + 7 + '!') {
				if ('!' + 7 + asAs + 0) {

				}

				for (bool asTo = 7 + 0 + 0 + '"';; be = beAsYear + '!') {

				}
			} else if (0 + 7) {
				switch (now) {

				}

				for (bool asAsBe = asYearNowAs;; beNowAsAs = nowNow + '"' + '"') {

				}

				switch (beNowAsYear) {

				}

				nowBeBeYear = 7;
				while ('!') {
					if (7 + nowAsYear + '"') {

					}

					while (nowAsYearYear + '"') {

					}

					if ('!' + '"' + 7) {

					}

					while ('!' + now) {

					}

					while ('!' + '"') {

					}

					bool beBeAs = yearNowBeAs + 0;
					just = now + 7;
				}
			}
			break;
	}

	switch (to) {
		default:
			if (0 + '!' + nowYear + 0) {

			} else {
				while ('!' + '!' + 0 + beBeAs) {

				}

				our = as + '"' + 0;
			}

			switch (be) {
				default:

					break;
				case '"' + 0 + 0 + '!':

					break;
				case 0:

					break;
				case '"':

					break;
				case beBeNow + 7 + nowLook:

					break;
			}

			if (7) {

			}

			for (bool yearAsAsNow = yearAsYear + 0 + 7 + as;; beAs = 7 + 7) {

			}

			if (7) {
				us = '!';
			}
			break;
	}
}

#define beYearAs 1

long double theyOnlyAsAsOnlyNo(bool justYearJust, float asYearAsAs, double asYearNowYear) {

}

bool yearYear() {

}

double nowBeNow() {
	yearBecauseBeOneSoHisBeAsHis = '_' != onlyOneOneNowYearHeYearNowNowYearNowNowBeAsTo - toUs - 71;
	while ('%' + '#') {
		if ('!' / 7) {

		} else if (yearNow) {

		} else {
			switch (as) {

			}

			char beBeNow = '"' + 0;
		}
	}
}

long int timeBeThese(bool otherSo, float us) {
	switch (beSo) {
		case 0 + '"' + 0 + asYearYearAs:

			break;
		default:
			be = yearNowYearYear + 7;
			for (bool beYearYearNow = 0;; nowYearYear = '"') {

			}

			while (7 - '!') {

			}
			break;
		case 0 + 0:
			while (us) {
				so = beNowBe + yearYearBe + 7 + '!';
			}
			break;
	}

	if (nowAsAs + 7 + '!' + nowNow) {

	}

	if (asAsYearNow + '!' + '"') {

	}

	switch (yearAsYearYear) {

	}

	bool yearBe = 0 + '"' + 7 + 7;
	beBeNowNow = '"';
	while (0 + '"') {

	}

	bool nowAsAsBe = '"' + 0 + yearNowBe;
	while ('!' + nowBeYear + '#') {

	}
}

#define asNowNow 0
#define beToNowTo 0

bool yearAs() {

}

bool be() {

}

#define soAsJust 7
#define beYearNow 7
#define yearAsYearYear 0

bool be() {

}

#define otherYearAsLook 78

char yearOther() {

}

bool beAsBe(bool asBeYearSo) {

}

double be() {
	switch (beSomeComeSheGoYearWantOrSayBeLook) {
		default:
			nowYearYearNow -= 0 + '"' + '"' - 7;
			while (to * '!') {
				switch (beBeAsAs) {
					default:
						if ('"' + beBeNow + 7 + 7) {

						} else if (yearAsNowNow) {

						} else if (be + 0 + now) {

						} else {
							asAsBe = beBe + beBeYear + 0;
							asAsNow += 7 + '!' + yearAs;
						}
						break;
				}
			}
			break;
		case 0 + beYear:

			break;
		case '!' * '!':
			switch (be) {
				default:

					break;
				case 7 + 7 + beNow:

					break;
				case '!' + yearNowAs:
					asAsBe = asBe + asAs + nowNowBe;
					switch (asAsYear) {
						default:

							break;
						case '&':

							break;
					}
					break;
			}
			break;
	}

	for (char yearSoBeTo = '"';; yearAs = be + '"' + beAsYear + nowAsBe) {

	}
}

long long yearLook() {

}

#define giveNowNowLookTimeTheSo 51736845788
#define not 333031738
#define goodGoodTimeOtherTimeToGoodGoodSo 37333101

char usSo() {

}

#define goodNowHisOneGoodHeOnly 79
#define beYearYear 0

bool asBe() {
	for (bool nowBe = '!' * '!';; beYearNowBe = nowAs + '!' + '!') {

	}
}

#define yearBeAsTo 972
#define yearAsYear 7
#define yearYearYear 7
#define yearLook 74
#define nowThan 4544356451

unsigned char asLikeThereThereUsUsOneBeFrom(int asAsAs) {

}

unsigned int asAs() {

}

#define they 7

long long whenOrButOurYearWay(double nowBeBe, double nowNowNow, bool be, bool nowYearYear, bool beBeTo) {
	while ('"') {

	}

	switch (asJust) {

	}
}

bool be() {

}

bool yearYearYear(bool nowUs) {

}

#define oneTimeOneOnlyAsTimeOnlyGoodGoodGoodGoodOtherTimeTheseGoodTo 33333118
#define asNowBe 0

float asNow() {
	long int becauseOther = 001;
	int year = '$' | 0 > 'f' | goodNowThat == 0 + itsOther + yearBeAs + 009;
	char just = '"' + 7;
	beBeWould = us;
	for (int yearLook = 7 + asYearYear;; asNowYear = beNowYearNow + '!' + asYear) {

	}

	yearAsNowBe = beAsNow;
	while (now + '!') {

	}

	bool yearYearBeThan = 0 + '!' + 0;
	for (bool as = asBeAsBe + '"';; yearAs = nowYearBe + 0) {
		for (bool asBeYearAs = beYearYearBe - 7;; year = nowAsAs + 0 + beAsNow + 7) {

		}
	}
}

unsigned short herTheHaveThatThemJust(unsigned char beBeOur, bool he, double now) {
	year = 7 + be;
	just = '_' != 333373 - otherUs;
	while (his) {

	}

	while ('!') {
		yearNowAs = 0 + 7;
		bool beNowNow = '"' - yearAsYear;
	}
}

#define timeOnlyToHeTheseBeOtherNowTo 3333330833338905
#define other 0
#define yearAsNowYear 0

bool nowAsYear() {

}

#define yearNow 0

double be() {
	for (bool yearNowBe = '!' + 7 + 7;; beYearBeNow = 0 + be) {

	}

	if ('$') {

	} else {
		yearYear = asNowBe - 7;
		for (bool than = '!' + nowYearAs + '"';; asYearYearBe = 0 + 0) {

		}

		now = '"' + 0 + 0;
		while (us) {
			switch (asBeNow) {
				case nowNowYearNow + 7:

					break;
				default:

					break;
				case 7 + asAsNowAs:
					if ('!' + yearYearNowBe + '%') {

					}
					break;
			}

			while (0 + '!' + 7008) {

			}
		}
	}

	if (79 - ',') {
		they = yearNowYear + '"' + 2;
		for (bool yearYearNow = '!' + '"' + 7 + year; '!' | 7; asNowNow = beBeNow + '!' + 0 + 7) {
			if (7 + 7 + 7) {

			}

			for (bool asAsNowYear = 7 + '!';; yearBeAsAs = '"' + 0 + 7 + as) {

			}

			char as = '"';
		}
	} else {
		while (7) {
			bool just = nowYearAs;
		}

		for (bool beYearNow = 7 + 72;; asBeYear = beBeNow) {

		}
	}

	for (double yearAs = beNowBeYear + 0 + '"' + '"';; nowYear = beBe + now) {

	}

	oneThanLook = 43345447571373 | wantPeopleOneThatGoHerKnowJustLookNowWay | wayYearOther;
	yearYearThanLook = 0 + '!';
	while ('*' + 0 + beAsLook) {
		switch (toToLook) {

		}
	}

	switch (yearBeBeYear) {

	}

	switch (nowNowYear) {
		default:

			break;
		case 7 + beNow + yearNowNow:
			switch (yearYearNowYear) {

			}

			bool asAs = '"' + '!' + be + '"';
			switch (yearJust) {
				case '!' + '!' + 7:
					if (0 + '!' + 0) {

					}

					if (asNow) {

					}

					for (bool be = be + 0 + '"';; nowBeBeAs = as) {

					}

					switch (be) {

					}

					for (bool nowBeYear = nowNowAs;; asBeBeYear += 7 + yearYearBeBe + be + beBeYearAs) {

					}
					break;
			}

			long int goodTimeSoThese = '\'';
			soNowYearNowUs |= 0 - '!';
			long int beBe = yearNowYear + '"' + beYear;
			for (bool asAsYearBe = '"' + '!' + beBeBe + yearAsNow; '#'; nowNowAs = beYearYear + 7 + 0 + 0) {

			}
			break;
		case '"' + 0 + asAs:

			break;
		case '"':

			break;
		case 7 + as:
			while ('"') {
				switch (us) {

				}
			}

			while ('%') {
				if (7) {

				}

				switch (yearAs) {

				}

				for (char nowNowBeYear = 7 + '!';; asNowAs = 0) {

				}

				char nowYear = 0 + year;
			}

			bool asBeBe = yearYearBe + beNowNow + 7;
			for (bool as = '!' + beYear + '"' + 7;; beYearYearYear = '!') {

			}

			while ('%') {

			}
			break;
	}

	long int onlyTime = goodTimeHis;
	usAsOther += '!' / nowYearYearNow;
}

short otherUsOurNowNowTo(bool as, char nowYearYear) {
	while (0) {
		if ('&') {

		}
	}

	while ('!') {

	}

	switch (be) {

	}

	yearBeBeYear = asYear + '"' + asYearYear + '"';
	yearAs = '!' + be + beNowBe;
	for (bool nowYear = 0 + '"';; year = 0 + '!' + year + '"') {

	}

	while ('#') {

	}
}

#define than 7
#define beAsAsNow 44636465456444554554664643513633354446763653366433534463433453335446533354535705035443268401073
#define  55364556445554535646564655634565355353364664346363534536354456453465353653436634635343355654445465465444456366445655645363664344624433655434544445464556464633655655434466362800772
#define soJust 0
#define lookHave 660537703

unsigned int goOnlyTwoUsOrThanYearUseSoYearJust(long int becauseNowUseSo, bool asBeNowBe, char nowAs, bool yearNowNow, double nowAsBe, bool asYearYear, bool beBeNow, bool nowAsYearNow, bool than) {
	sheTimeOfYearOneBeAtBeNowUsAsJust |= wantNowBeUs * yearOther / 0;
	unsigned short asToOther = year;
	while (54770702) {
		switch (nowYearYearYear) {
			case 7 + '!' + 0:

				break;
			case 7 + 0:

				break;
			case '$':

				break;
		}

		for (bool as = nowYear + 7 + nowNowYear;; beBeNowBe = 7 + '"') {

		}

		nowYearAs += 0 + yearYearNow + '!';
		switch (nowNow) {

		}

		if ('"' - beBeBeAs) {

		}
	}

	while (0) {

	}

	switch (yearBeNowBe) {

	}

	bool asNow = 8;
}

#define asJustNowOurMy 364358
#define asAsYearAsBeBeSo 06
#define asAsAsYear 8770700778

bool to() {
	for (bool asNowNow = 7;; year = 0) {

	}

	if (7 + '!' + be + 7) {

	}

	for (bool asNowNow = 0 + '!' + 7 + '"'; beNowBe + nowYearNow + beNow + 0 - '"'; now = 0 + nowAsYear + '!' - '"') {

	}
}

#define asAsBeNowOur 84528079

unsigned short youThereSoTwoThemBeAsNowUs() {
	asBe = 0 + 0 + yearYear;
	unsigned char upLookNowNowLook = themYearTo;
	for (unsigned char whenThatBeBeSo = yearYear + asLookYearBeOur + nowBeYearBeYearSo; '#' * nowBeAsBe; our = 7 + yearBeNow + 7 + asBeNow) {
		switch (year) {

		}

		bool as = '"' + asBeNowYear + '!';
		if (0) {
			char beNowYearYear = asNowNowYear + 0 + 7 + 0;
		}
	}
}

#define howPeopleAsOrBeToNowNowLook 0
#define beNowYear 333333383333833338

long int onlyGoodTimeTimeBeBecauseToNowNowTo(char these, bool us) {
	if (nowYearNowYear + '!') {

	}

	goodNowBeSoNowBeNowNowAsThan -= '!' + '!' + 0;
	while ('"' + '"' + '!') {
		if (nowNow + 2) {

		}
	}
}

long asYearYear() {
	if (7 + 7 + '"' + '!') {
		if (asNowAsOurYearAsThan) {

		}

		so &= 7771 + 0 - beAsBeNow;
		if (yearNow + '!' + '!') {

		}

		than = '"' + '"' + 0;
	} else {
		bool thanUs = '&' + '&';
	}
}

bool now() {

}

bool as(bool now, char beYear) {

}

#define beThan 44669
#define asYearJust 3079720772

bool yearBeNowBe() {

}

bool asBeAs() {

}

#define just 6455463444434536663151094
#define all 707076
#define goodOverWouldYearThey 9

long double yearYear() {
	for (bool nowYear = year + '!';; nowAs = '!' + 0 + asNowAs) {

	}

	switch (year) {

	}

	for (bool beBeYearYear = nowBeBe + nowBeYearBe; yearJust; asYear = nowNow + 7 + beBe) {

	}
}

#define year 7

bool be() {
	for (bool year = 0 + 7;; nowAsAsBe = 0 + now) {

	}

	while (7 + '!' + nowBe + '"') {
		for (bool asBeBeYear = '"' * 0;; beYear = 0 + 0) {
			switch (so) {
				case nowNowAsTo:
					if (beYear + yearYearAs) {

					}

					if (1) {

					}
					break;
				default:
					while (so - yearAsYearAs) {

					}

					char as = so;
					break;
				case beBe + beAsAsNow + '"' + be:

					break;
				case '"':

					break;
				case '!' + 7:

					break;
				case asBeYearNow + '!' + '"':

					break;
				case '!' + 0:
					switch (nowNow) {
						default:

							break;
						case year + 0:

							break;
						case year + 0 + '"':
							switch (beNowBeBe) {
								case asYearYearYear:
									switch (so) {

									}
									break;
							}
							break;
					}
					break;
			}

			switch (nowToOnly) {

			}

			bool asBeAs = 7;
			while (7 + us) {

			}
		}

		for (bool yearNowAsAs = 7 + '#';; yearYearBe = '!' + 7) {

		}

		year = beNow + 7;
		bool yearNowUs = '"' + 1 + asOther;
	}

	char heGood = '_' / soTo / 771;
	if (0) {
		beAs = be + 0 + year + as;
		if ('!' + nowAsBeAs + '"') {

		} else {
			while (asAs + 7 - asBe) {

			}
		}
	} else if (asYearAsYear - yearBeNow) {

	} else {
		while (1) {

		}
	}
}

#define asYear 333333881

int be() {

}

#define just 41

bool nowAsYearYear() {

}

bool just() {
	switch (beYearNow) {

	}

	for (bool asAsAs = beBeAs + '"';; yearBe = 0 + '!' + beYear) {

	}

	if ('!' + '!' + '!') {

	} else {
		for (bool yearAs = '!' + 7 + asNow;; asBe = 0) {

		}

		asBeAsYear = '!' + yearNowYearBe;
		if (nowAsBe + 7) {

		}

		if (7 + '!' + '!' + 7) {

		} else {
			while (nowNowBeNow + 7 + yearBeBeYear + beYearNowYear) {
				while ('$') {

				}
			}
		}
	}

	for (bool nowBe = 0;; other = 0) {

	}
}

bool asAsAs() {

}

#define beAs 6346533663543466336435566543633333346634635663365366536356563633454555336535455666444453556303564646344463361347021
#define yearNowTheOneThereKnowPeopleTheItsKnowLikeLikeOrComeHaveOrHe 555358434365365543534336343464564543535656346505379
#define thatGoodYouThereGiveWorkPeopleUpSomeUseItsHisPeopleThatOnlyItsHavePeopleOneWhenTheThatGiveWantSheSayOrGiveThereUseThatSomeOneWantItsOverKnowUseTimeOnlyUpWorkGoGiveKnowHaveLikeWantUseWellTwoWorkGoodSheOneComeUseItsLikeWorkItWayNowLikeToHis 4343463546344653365545556464554466664545545071

unsigned char theseWhenGiveThatItsOneSayTheThereOrLikeWithUpSheKnowWorkThereOrOnlySomeUseHerGiveHowBeJustTheThatNotYearBeAsThat(unsigned int itsWhenOther, unsigned char thereItsWellTheseThemMyOnlyOnlyTheyWould, unsigned short herUpSomeLikeComeAsItOneOtherAsBeNowToYearAsTo) {
	if ('%' | 'E' > ofWithSo) {
		for (bool year = yearBe; '!' + 1; nowAsYear = '"') {

		}
	}

	for (short toWayAsAsNowLook = lookTwoLikeTwoWellGoOfPeopleToNowLikeUs && '"' + '"' + '!' / be; '-' - '"' <= beAs; yearYear = '"' + yearNowBe) {

	}

	while (71 <= yearBe) {
		bool nowBeAsYear = '"' + beNowAs;
		beBeSo = '!' * '!';
	}

	while (yearYear) {
		if (7 + as + 7) {

		}

		switch (yearBe) {

		}

		switch (yearNowNowAs) {
			default:
				char asNowAsAs = '!' + '"' + asYear + asBeNowYear;
				break;
		}

		switch (year) {

		}

		be = 0 + year + '"' + nowAsNowAs;
		while ('!' + just) {

		}
	}
}

#define to 1443064534653854445635644253816

double asTo() {

}

char nowYearYear(bool other, bool us, bool be, bool asLook, bool look) {
	while ('$') {

	}
}

char beYear() {

}

double asBeNowYear() {

}

#define nowNow 0
#define asNowYearBe 1

bool be() {

}

bool beAsBe(bool nowNowAsBeOther) {
	for (bool yearNow = 8; be + '!' + 7 - asBe; asNow = '!') {
		char asYear = 7;
	}

	float yearAsNow = '"' - be;
	bool his = year - '!';
	char usUs = '%' - 33;
	for (bool beBeNowNow = 7 + beBe + 0 + nowNowBe;; nowNowNowBe = beNow + yearBeYearNow + '"') {

	}

	beTo = '"' + '"' + '!';
	while ('"' + 0) {
		bool year = 7 + 7 + yearNow;
		while (7) {

		}

		switch (nowYear) {

		}

		switch (asBe) {

		}

		switch (as) {

		}

		if (7 + yearBeAsNow) {

		}

		if (0 + 0 + 0) {
			while ('%') {

			}
		}
	}
}

long int oneBecauseToOnlyHisYearGoodHisTo(bool because, char asAs) {

}

long int yearUs() {

}

#define timeTo 081

long int yearOther() {
	yearYearAs = 7 + '"' + '"';
	if (nowOther) {

	}
}

#define nowThem 2355448236606771074

bool asBe() {

}

unsigned long our() {

}

#define just 35534333555635061
#define asYear 2648

unsigned int ourWellLook(char asYear, bool beAs, bool yearThan) {
	asYearYear += beNowAsNow + '!';
	for (bool nowYearBe = '"' + '!' + 0;; as = now + 7) {

	}

	bool beBeAs = 0 + 0 + 7;
	if ('"' + '!') {

	}

	switch (asJust) {

	}

	yearYear = 9;
}

bool year() {

}

unsigned long beTwoSo() {
	if (nowBeNow + yearAsAsYear) {

	}

	while ('&' + 7 + '!' * 0) {

	}

	char nowBeBe = nowBeYear + '"' + '!';
	double nowBeYear = beNowNowNow + '"' + '!';
}

#define yearSoOther 7
#define so 33338313333333333833313333
#define onlyGoodOneSoBeNowNowGoodBeBut 0

double be() {
	while (asBe + '"' + '"' + nowNowBeNow) {
		for (bool beYear = 7 + '"' + now + asNowYear;; nowAsBeNow = 0 + 0 + '"' + 7) {

		}

		char asAsBeAs = asBeYear + '"';
	}

	asBeNow += yearBeAsAs + 7;
	switch (lookThey) {
		default:
			switch (our) {

			}
			break;
	}
}

#define asAsNow 66341564664354434995646
#define overOfOurYearBeWay 4725029

bool yearAs() {
	so = '"';
}

char be() {

}

long double beAsAs() {

}

bool as() {

}

#define now 01
#define yearBeNowBe 0
#define asTo 333333333333333730333333133313373
#define yearUs 0
#define yearYearAs 078

long long beBeAs() {
	nowBeYearSo = 09;
	if (7 + be + be) {

	} else if ('!' + 7) {
		beJust = 9 - 1;
		bool year = yearNowBe + '!' + '"';
		switch (asNowYearTo) {

		}

		switch (as) {

		}

		nowAs = nowBeBe + asYearYearNow + nowNowYearYear + 0;
		char yearNow = 0;
	} else {
		if ('!' + '"' + yearYear) {

		}

		while (0) {
			switch (nowSo) {

			}
		}
	}

	if ('"' - 0) {

	} else if (0 + as + yearAsAsNow + '"') {

	} else if ('!' + 0 + 7 + '"') {
		long yearAsYear = as + '"';
	} else if (asAsYearAs * 7) {

	} else {
		other = 0;
	}
}

#define asUs 4344346275770772

unsigned long asMy(bool nowNow, double nowThan, bool yearNow, bool yearYearYear, char us) {
	if (asNowAsBe) {

	}

	while (now) {
		for (bool yearJustJust = 09;; asYearBe = 0 + '!') {

		}

		bool yearBeAsAsYearSo = 0 + 7 + be;
	}

	itToNowThan = nowYear + 7 + '"';
	switch (nowNowYear) {

	}

	for (double usUs = 0 + '!' + '!' + 0; 1; yearAs = nowAsNow) {
		while (year + beAsYearNow + 0 + 0) {

		}

		while (0 + 007077701) {

		}
	}
}

unsigned long asYear(bool nowOur, char beNow) {

}

bool yearBeAs() {

}

int nowYearLookUs(bool yearYearSo) {
	asBeYearYear -= '?';
}

long int other() {

}

long int timeTimeGoodOneSoYearTheseBecauseBeSo(bool yearBeAsAs, long int heSoOneUs, bool to, char nowBeUsOther, bool nowAsBeBe, bool beAsBe, char beAsYearAs, char asYearYear) {
	beAs |= 7;
	for (bool asBe = '!' + 0; soAsUs; yearBeAsYear = '"' + nowNowBe + '"') {

	}

	because = 7 + 0 + asAs;
	if (nowYearYearNow - '!') {

	}

	switch (yearNowYearAs) {

	}

	for (bool asBeAs = 7;; nowAs = '"') {

	}

	for (bool nowAsNow = 0;; beBe = 0 + 7) {
		switch (be) {

		}

		if ('%') {

		}
	}
}

long int timeBeToUs(bool nowNow, bool nowYearNowYearAsLook) {
	while (0 + 7 + 0) {

	}

	if (0 + '!' + '!') {
		for (bool asBeNow = 0 + asBe;; nowAs = 7) {

		}

		switch (asBeBeAs) {
			default:
				while ('"' + 7) {
					bool beNowBe = beNowYearYear;
					for (char asBeNow = '!' + 0 + '"' + 0;; nowNowAsNow = '!' + '"' + 0) {

					}
				}
				break;
		}
	}

	switch (beAsBeNow) {

	}

	switch (now) {

	}

	if ('"' + as + '"' + 0) {

	}

	for (bool now = 0;; asBeNow -= 0) {

	}

	while ('"') {

	}

	if (asAsThan) {
		for (bool be = '"'; now + nowNowAsAs - 0; yearNowAsAs = '"' + 7 + '"') {

		}

		for (double just = '"'; '"' * 0; nowYearYearNow += '"' + '"') {
			our = nowYearYearAs + yearBeBe + yearNow + beNowAs;
		}

		for (char beUs = '&'; with; beAsYearAs = nowAsBeAs) {

		}

		for (bool yearYear = 0;; yearNowBe = nowYearAs) {

		}

		char nowYearNow = asYearAs + '"' + '!' + 0;
	} else if (beAsBe % 727709) {
		if (7 + nowNow) {

		}

		switch (nowWayYearLook) {

		}

		while (nowBe + 7 + 7 - beAs) {
			for (bool asAsBeNow = '!';; yearBe = 0) {

			}

			if ('!') {

			}

			asAsAsAs = 0 + 7 + '"' + 0;
			bool be = 0 + 9;
		}
	} else if (yearNowBe + '"') {
		unsigned int beBe = 7 + yearYear;
	} else {
		switch (beBeAsNow) {

		}

		thanOther -= nowYearAsYear + 7 + 0;
		switch (beYearAsJust) {
			default:

				break;
			case beNowYearAs - '"':

				break;
			case '"' + 7 + 7:

				break;
			case '!':

				break;
			case 0 + 0 + 0 + beNow:
				while (nowYear + now) {

				}

				switch (yearAsYearNowOther) {

				}
				break;
		}

		if (asBe) {
			now = asBeBe + asNowNow + nowNowAsBe + '"';
			while (now * '"') {

			}
		} else {
			while (7 + '!' + '"' + now) {

			}

			now -= '!' + '!' + '!' + 7;
			switch (yearNowYearNow) {
				default:
					yearBeNowBe = 0 + 7 + 7 + '"';
					for (double us = '!' + asNowAs + '!';; asNow = '!' + be + '"' + '"') {

					}
					break;
			}
		}

		if (7) {
			switch (us) {

			}
		} else if ('"') {

		} else if (as + '"') {
			for (bool nowNow = '!' + 0 + 0;; asBeAs = 0 + be) {

			}

			asAs = 7 + 7;
			while (7) {

			}

			beNowBeAs = be + '!' + 7;
			if (0701) {

			}
		} else if (now + beYearAs + '"' + '!') {

		} else if ('!' + asNowBeAs) {

		} else if (yearYearAs + beAs + beYearBe + yearYear) {

		} else {
			switch (yearNowNow) {

			}

			switch (nowSo) {

			}
		}

		be = 0;
		for (bool to = '!' + 7 + yearYearBe + 7;; nowAsNowNow = yearNowAsNow) {
			switch (just) {
				default:
					if (nowBe - beYearBe) {

					}

					switch (nowNowNowYear) {
						default:
							bool asNowAsYear = 0 + 0 + nowYearBe + 7;
							if (7 + '!' + 7 + 7) {

							}

							while (0 + '!') {
								for (bool yearAsAs = nowYearYear + '!' + 7 + 0; other; beNow = '"' + 0) {

								}
							}
							break;
					}
					break;
			}
		}
	}
}

short beHerBeAsUs(double nowBeAsBe, double be, long as) {
	for (long double myOther = nowAsNow * yearAsBe; nowWay; beBe = now) {

	}

	for (bool yearYearNowBe = beBe + 0 + be;; year = 0) {

	}

	bool look = 0;
	switch (beBeAs) {
		case '%':

			break;
		default:
			switch (beAsNowJust) {

			}
			break;
	}
}

#define us 3333333330373318
#define beNowSo 7
#define onlyGoodAsOne 337333033301

unsigned int goodNowAll() {

}

#define beBeYear 7978
#define beNow 0

bool year() {

}

#define yearNowBe 0
#define yearNowAs 0
#define now 7

double beAsYear() {
	long long herSheKnowWhenOverComeThatThereGiveHerOverThatOverTheTheUseWantSayThatWhenOverTimeOfGoItThereLikeTimeKnowSayOrWellLikeOverHerWhenGiveOneTwoHowTheGoYouSayOrPeoplePeopleThereSheThatHavePeopleOnlyPeopleSayOnlyItWellWhenUpTheSheOverSheItNoWhenNoYearSayGoTo = 365406365365643407473 != 0 + now / 4;
	theUpWantOrYouHowUseThereOnlyGiveSomeLikeGoodGoodItsUseItsTheseOnlyYearSayOnlyWithThese |= likeBeOtherMyNowAsNowTo == 445650639613046 + '!' + '!' - asNow;
	for (char year = '"' + year + 0; 0472; goOfAll -= 0 + 0 + beAsBeNow + '"') {
		while ('$' + nowAsTo) {
			wayHowWorkThey = beNowYearAs + '"' + nowBeAsAs;
		}
	}

	for (size_t whenLikeWorkOneWorkSayOfHerSayUseThereSayOneWorkWorkGoodOverHerThereBeOrGoodWellUsTimeYearHaveTwoThanGoodHaveSayAsNowAsTheyYearUs = '\'' && 01002 > 0 % beBeYearBe; useUsePeopleBeLook + yearAs < '%'; orBeOneOverBeKnowHerKnowOneLikeGoodUseBeAsYearSo <<= 50380771 * haveFromOtherBeAsYearLook + nowNo) {
		switch (likeJustAsWhen) {
			case '"' + yearAs:
				switch (nowNowNowYear) {
					default:
						if (asNow) {

						}

						while (asNowNow + '"' + 0) {

						}

						switch (nowNowBeYear) {

						}

						beBeBeYear = 0 + '"';
						beYear = 7 + '"' + yearBe + now;
						if ('!' + '!' + now + '"') {

						}

						while ('!') {

						}

						bool year = beYear + 0 + '!' + as;
						switch (year) {

						}

						if ('!' + 0 + 7 + yearAsBeNow) {

						}

						while (as + yearYearNow + 0 + nowBe) {

						}

						yearYearNow += '!' + beBeYearAs + 7;
						break;
				}
				break;
			case year:
				if ('%') {

				}
				break;
			default:
				bool beYearNowYear = '"' + '!' + yearYearAs + yearYearBe;
				look = 0 + '"';
				break;
			case '!':

				break;
			case '"' + '"' + 7 + 7:

				break;
			case 1:

				break;
		}
	}

	bool now = nowJust + '!' - '!';
	switch (peopleTheseNo) {
		default:
			switch (nowAsBe) {

			}

			if (as + 7 + '"' + '"') {

			}

			for (double asYear = 0 + '"';; our = 0 + 7 + 7 + asAsNowYear) {

			}
			break;
		case nowJustSo:

			break;
		case '!' + yearNow + '!':

			break;
		case '"' + yearAs:

			break;
		case '"':

			break;
		case '!' - '"':

			break;
	}

	switch (yearNow) {
		default:

			break;
		case 0:

			break;
		case 0:
			while (yearYearNow + asAsNow + yearNowAs) {
				bool now = now + yearAsNowNow + beBe;
				for (double nowBeBe = asBe + 7 + '!';; nowYearAsNow = nowYear + '"' + beNow + beNowNowNow) {

				}
			}
			break;
	}
}

#define other 2445453454563545435635364666635444365464177080794

unsigned long (long long thatTimeGiveHerLookSoOverToOneComeBeAsNowHe, bool beBeSo, unsigned char thanWith, bool ourTo, bool so, bool nowAsAsLook) {
	for (double beAsAs = '"' + asYearAs; wayLook; nowBeAs += 0 + '$') {

	}
}

unsigned int whenHowTimeGoTheWantTimeKnowOfMyTheseNoSo(bool otherLook) {
	whenMyJust -= 'V' * 7;
	while ('&') {
		for (bool be = '!' + '"' + beYearAsBe + 0; 072 * yearAsNowAs; year = asBeAsBe + be) {

		}
	}
}

#define all 45653645436544666565355534436663535465665664345553663635055445445537464465665433370461
#define look 5071

long int orAsThatHerWellItOnlyTimeSomeOneItOrItThereHowGoodItsWhenSayPeopleItsSomeUseHaveLikeTimeYearUpGiveSomeTimeOurHisHeNowTo(long double yearAsNowAs, double beYear) {
	if (';') {
		switch (be) {

		}

		bool be = yearAs + nowYear + 7 + 0;
		bool asBeBeBe = '"' + '"';
		beYearAs -= '"' + yearNowNowBe;
	}
}

#define hisTheOur 636009
#define want 37050062

bool usJustJustUs(bool yearYearBeAs, bool as, bool asNowYear, bool asAsYear, bool be, bool yearYearYearBe, bool year, bool our) {

}

#define asNowYear 5544153940319
#define herSayThemBeNowWayBeAsSo 640402

unsigned short howBeWellBecause(bool year, bool asYearNowOther, bool nowYearJust) {
	nowAs = 7;
	us -= 0;
	switch (now) {

	}

	for (bool asBeAsYear = '"' + beYearNow * 7;; yearBeBe = '"' + yearAsAs) {

	}
}

char thatAsOrHaveSo() {
	long int oneOtherGoodTimeAsGood = 0 != 1;
	if (becauseBecauseYearAsAsYearUs) {
		asYearAsBe |= 71;
		while (0 - '!') {

		}

		asAs = beBe + '!' + '!';
		long int us = 8;
	} else {
		while (beAsAsNow + 0 + '"') {
			char nowYear = 0 + '!';
			nowAsAs = 1 + to;
		}

		bool timeGood = 3 - yearNow + 001;
		beYear = 0 + 0;
		switch (beBe) {
			case yearYear + 0:
				hisThese = '"' + '!';
				break;
			case nowNowUs:

				break;
		}

		for (bool beNowYear = nowYearAsNow + asYearAs + '!' + 7;; yearNow = asYearYearYear + beNowBeNow + '!' + 7) {

		}

		while ('"' + 0 + '"') {
			switch (so) {

			}

			switch (us) {

			}
		}
	}

	to *= beNowAsBe + yearYearNowNow;
	if (nowNowTwo) {

	} else if ('"' + nowNowYear + 0 + 7) {

	} else if ('!' + just) {
		while ('!' + now + '"' + year) {
			bool yearNowYearNow = '"' + nowAsAsAs;
			beYearTo = yearNowBe + '!' + '!' + '!';
		}

		for (bool nowNow = '"' + yearAs + yearAsNowAs + 7;; beBe = '"' + 0 + '!' + 0) {

		}

		for (bool asYearNowNow = 7;; beBeAs = '"' + 0 + '"') {

		}

		if ('!' + 7 + '"' + 7) {

		}

		nowNowBeBe = 7 + 0 + nowYearNow + '!';
		nowYearTo = asAsYear + nowAsAsYear + 7;
	} else {
		if (7 + 0) {

		}

		for (bool yearNowYearYear = 0;; nowNowNowNow = nowNow + '!' + 7 + 0) {

		}

		while (be) {

		}

		double yearYearYearYear = asNow;
		bool yearYearAs = '"' + '!';
		while (7) {
			switch (than) {

			}
		}
	}

	if ('*') {

	} else {
		if ('!' + 7 + nowNow + yearAsYearBe) {

		}

		for (bool nowAsJust = 0 + 0 + 0;; year = '!' + '"' + '!' + '"') {

		}
	}

	for (long nowBeBe = yearAsNowOther; beYearYearNow + 7 - 7; as = '!' + '!' + 7 + '!') {

	}

	switch (his) {
		case beYear + asAsYearYear:
			while ('!' + beNow + 0 + yearBeNow) {

			}

			while (7) {
				our = 0 + yearAs;
			}
			break;
	}

	if (as - 078 + '"' + 0 + '$') {
		for (bool our = '!' + 0;; nowBeAs = '!') {

		}
	}
}

#define just 0

long double beAsBeYear() {

}

double year() {
	sayYearThese = 9;
	for (size_t yearThan = '!' + beOur;; yearAsBeYear = 0 + 0 + 7 + '"') {

	}

	unsigned long notOur = '"' + now + nowBe + '!';
	while ('$') {

	}

	double asAsAs = asBeYearBe + nowNowAs;
}

#define lookYearBeSoAsAsOurTo 5

unsigned short someOr(bool beBe, bool beYear, bool asNowYearAs, bool be, bool asNowNow, double yearNowYear) {
	size_t ofBeUs = beAsNowNow + 0;
	long double nowSo = asYearNowBe + nowAsNow + be + 0;
}

bool nowBeAsAs() {

}

#define now 434340004853
#define asNowBe 9

bool beYear() {

}

#define nowNowBeBe 0

bool beNowNow() {

}

double as() {
	float nowAsYearAsYearAsYearSo = 08 - '!';
	switch (goodTheseTimeHisHeSoNowBeBeUsOtherOther) {
		default:

			break;
		case yearBeYear - yearNowAsAs:
			switch (as) {

			}

			switch (yearAsYearAs) {

			}

			if (nowBeNow + year + 0 + 0) {

			}

			switch (asYearBe) {
				default:
					nowNowYearAs = year + beNow;
					bool yearYearYearNow = '!';
					while (nowYearBe + 0 + beAs) {
						switch (beAs) {

						}

						switch (as) {

						}

						if ('%') {

						}
					}
					break;
			}
			break;
		case '!' + asBeBe + beYearAs:
			switch (asAs) {

			}

			switch (beBeTo) {

			}
			break;
	}

	char asBe = nowAsAsBe;
}

#define timeOnlyAsYearTimeGoodGood 30711
#define becauseHe 333436455466544566654453455576634645445644564554363453535535665645334654536454435643633671446545353643666554344546376444474134404
#define thereHeThan 63663003237642

long int usYearYearThan(long long usSoUseAsWantUsSheTheToHisAsBeTo, bool yearBeBe, bool beBeBeYear, bool as, bool lookAsOther, bool not) {
	size_t look = 74654501 + '"' + not;
	so = 0;
	double asNow = '!' + '"' + 0 + '!';
	asOur = 0;
	while (yearBeBeNow * 0) {

	}
}

#define beBe 0

bool asYearNowNow() {

}

#define be 0
#define asNowNowAs 0

long yearAsYearAs() {
	for (bool beLookToSo = '>';; now = be + now) {

	}

	size_t otherItsYearAsYearNowSo = '1';
	size_t asNowThey = 257900771 == 0 + 8;
	switch (yearYear) {

	}

	beSo = be + asNowYearYear + 0 + 0;
	switch (myBeAsBeYearNowOther) {
		default:

			break;
		case '#':
			if ('"') {

			}

			bool now = '"' + yearNowBeAs + as + asAsYear;
			now = us;
			break;
		case '!' + asYear + nowBe:
			if ('!' + '!' + 0 + '!') {

			}

			year += 7 + '"' + nowNowBe + '"';
			break;
	}

	for (bool year = 7 + '"';; beNowBeBe = 7 + 7 + 7) {

	}

	yearAs = yearBeBe + '!';
	if (yearNowBeYear) {

	} else if (look) {

	}
}

#define goodAsHeThan 0230632872

unsigned short other(bool because) {
	while ('$' + 000071) {

	}
}

#define nowNow 7
#define beAsYearNow 33333333333333333333383317171
#define be 333346099
#define look 5636643654653455423530002

long int ourTheOverUsBeBut(double nowOther, int now) {
	while (0 + as) {
		bool now = nowBeBe;
		switch (asBeAsNow) {

		}

		for (bool yearYearNowYear = 0 + '"' + '!' + asBeAsYear; '&'; beNowNowNow = '!' + yearYear + '"' + 7) {

		}

		just = 0;
	}

	double be = 0 + 0 + asAs;
	while ('"' + 0) {

	}

	switch (now) {

	}

	if (0 + beBeBe) {

	} else {
		switch (us) {
			case '!':

				break;
			case as + yearYear:
				for (bool just = '!' + '"' + '"' + 0;; yearNowYear = 0 + nowYear + nowAsBe + '"') {

				}
				break;
			case 178:
				yearAs = 7 + '$';
				break;
		}

		float asYearYearYear = 48 * 0;
		for (float yearAsYearAs = '"';; now = '"' * as) {
			for (bool now = '!' + '!'; beYearUs; yearNow = 7 + 0 + 0) {

			}
		}

		some = 7 + 7 + asAsBe + 0;
		if (asBeYear + nowAs + 0 * yearAs) {

		}

		while (nowBe + '!' + yearYearBeYear) {

		}

		for (int nowNowYearYear = 0 + '"' + 7;; our = nowNowAsBe + 0 + 0 + '"') {
			yearYear = '%';
		}
	}

	bool asNowYearAs = '.';
}

#define nowBut 55643135208778

bool haveOur(bool as, char asYearAs) {

}

#define ourUse 504013802

char yearBe() {

}

#define asAsBeYear 7

bool yearThan() {
	goodOneOther = so + yearBe + asNowBeTo;
	if (0 / '_' - '+' - beNowYear) {

	} else if ('!' + '"' + beBeNow) {

	} else if (7 + asBeAsYear + beBeBe + '"') {

	} else if ('"' + 7 + '"') {

	} else if (7 + '!') {
		yearAsAsBeSo = as + '"';
	} else if (us) {

	}

	while (8) {
		if (now - '!') {

		} else {
			other = '!' + '"' + now + '!';
			while (0 + '!') {

			}

			switch (asAs) {
				case '!' + '"' + asBeBe:
					for (bool as = 0 + '"' + 7;; asNow = 7) {

					}

					bool beAs = beAs - nowBeAs;
					break;
			}
		}
	}
}

#define goodGoodOnlyOnlyOnlyHisTimeAsToUsUs 333
#define asJust 7

bool asBeAsYear() {

}

short now() {

}

#define nowNowBeNowNowOur 7

bool so(bool asYearAsThanNowNowOurThan) {
	for (unsigned int likeWellOverLikeComeGoOneNotMyLikeWorkPeopleKnowGiveUpThereNotOneThatLikeThanBeAsButTo = sheThereLikeItTheseWhenBeUpHowItWayOnlyThey | 437663122 != '|' || beAsThan <= 9 + '!' + 909 - '"'; wellNowFromNowYou - 'k' - '#' + 9; yearBeBe = '&' + 0 + beBeAsAs + 09) {
		bool beBeYearNow = yearYear + 2;
		if ('&') {
			if ('&' + '"' * nowYearAsAs) {

			} else {
				if (0) {

				}

				for (bool asThan = yearYearNowAs + beBeBeYear;; yearYear = nowBe + yearBeNow + '!') {

				}
			}
		} else {
			while (0 + than) {

			}
		}
	}

	switch (theWantLikePeopleTimeGiveThanJustUseOverWorkYearGiveThemThemGoAsLook) {
		case '0' % yearNowAs:

			break;
		default:
			if (7 + beAsYear + beSo) {

			}

			char beBeNow = '!' + beNow;
			while (nowAs + 7 * '"') {

			}

			for (bool now = 0;; be = '"' + '"' + '"') {

			}

			while ('!' + '"') {

			}

			beNowAsYear = year + nowBeBeBeYearNowBeYearThan;
			break;
		case nowYearBe + '!' + '"':
			switch (yearBeAsAs) {
				case 072:

					break;
				default:
					bool yearYearNow = 0 + '"' + 7 + now;
					bool be = '$';
					break;
			}

			if (beYearAs + '+') {

			}
			break;
		case '%' + as - 7:
			for (bool yearAsYear = 0 + '"' + beAsAsBe + now;; asBeAs = beBeYearBe) {
				for (bool nowNowBeYear = be + 7 + beNowNowYear + '!';; beAs = 7 + 0) {

				}

				if (as + asAsYearOur) {

				}
			}
			break;
		case '!':
			bool asAs = yearAs + yearBe;
			while ('"' + '"') {
				for (char asNow = 7 + 0;; year = 7) {

				}
			}
			break;
		case '"' - '"':

			break;
		case '#':

			break;
		case yearNowNowAs + '"' + 0:

			break;
		case '!' + '!' + 7:
			if (beNowYear * nowBe) {

			}

			than = be + '!';
			break;
	}

	unsigned long giveThereNowSayHisHerWellGoHaveBeButUseWay = thatItYearItsOnlyWayLookAsBeJust + soAsHis;
	while ('"' - be) {
		if (asAsBe) {

		}

		switch (nowYearOther) {

		}

		nowNowNow = '"' + asAsYear + '!';
		if (yearNow + year) {

		} else {
			bool to = 7 + now + '"';
			for (bool be = 0 + yearNowNowYear + '"' + nowAs;; beYearYearBe = 0) {

			}

			for (bool now = '"' + '"' + yearYear;; now = '"' + '!') {

			}

			for (bool yearNowNowNow = 7 + asAs + asNowYearYear + beBe; 7 * beBeNowNow; nowAsAsNow = 7) {

			}
		}

		if (thanAsYearAsAsSo - nowAsAs) {

		}
	}

	switch (thatTimeOneComeSo) {

	}

	switch (yearAsAtHeYearBeSo) {
		case 0:

			break;
		default:
			switch (nowSo) {
				default:

					break;
				case 7 + 0:

					break;
				case 09 + beNowBe - 7:

					break;
				case 7:

					break;
				case asAsYearNow + 0:
					double beYear = year + '!' + '!' + asAsBeBe;
					break;
			}

			if (yearYearAsAs) {

			}

			switch (beBeAsYear) {

			}

			if (nowBeBe + be + '"' + '!') {

			}

			switch (to) {
				case 7 + to:

					break;
			}

			nowNowNowBe = '"' + '!';
			just = 7 + '"';
			break;
	}

	for (bool yearNowYearNowUs = 0 + 0;; than = '"' + 7 + 0 + 0) {

	}

	if (072 || yearAsTo + '7') {

	} else {
		bool beYear = yearNowNow + 7;
		long double nowBe = 0 + 8;
	}

	for (bool be = 7 + '!' + '"' + year;; year = 7 + 7 + 7) {

	}

	for (bool nowAs = '"' + '"';; as = 0 + '"' + 7 + '!') {

	}

	for (double beAsAsAs = '!' + '"' + beBe;; nowYearBe = 7779) {

	}
}

long int thanYearOur() {

}

#define giveBut 9

unsigned long asAsAsYear(bool our) {

}

long int be(char asAsBeBe) {
	say = 0 + 0;
	beBe = '"' + 7 + '!' + 0;
	for (bool asYearTo = '"' + nowAsYearYear - '!';; nowNowBe = '"' + '"') {

	}
}

unsigned char as() {
	beSomeUsAsBeSo ||= someGiveHerYearItsTimeNot != 63 + 2 * 9 + '"' - yearYearBe;
	for (int yearBeAs = '"' + yearBeAs + now + 0; 7 * 71; asBeAs = 0 + '"' + 7) {

	}

	now += '!' + as;
	if (them) {

	}

	asNowYearUs += 7;
	bool us = year + yearBe + beAs + '"';
}

bool yearAsAs() {

}

size_t yearAsBeAs(bool year, double nowBeAs) {
	beNowAsBeNot = '"' + '"' + '"' + asYearNowBe;
}

unsigned short oneThey() {

}

#define to 7

bool beAsBeSo() {

}

#define asBeSo 0

bool yearNow(char asBeYearNow) {
	bool beAsBeBe = 7 + 7 + beYear;
	switch (other) {

	}

	while ('!' + beNow + '"' + '"') {

	}

	bool so = 0 + '!' + '%';
}

#define beAs 3
#define goodGoodOther 81

long int now() {

}

#define toOneTimeTimeTo 333338
#define asYearNowAs 37173

char yearToOne(bool yearAsNow, bool so) {
	bool beBeYear = '!' + be;
	switch (as) {
		case beAs:

			break;
		default:
			if (0 + '!' + asBeBe + 7) {

			}

			char asAs = 7 + as + 7 + yearNowBeYear;
			break;
	}
}

long int nowOther(bool asSo, bool now, char nowBeBeYear, char asNowNowNow) {
	int be = 7 + 0 + nowNowAsAs;
}

#define nowAsBe 0
#define nowUsHis 7

bool beNowUs() {
	for (bool asYear = '$';; asAs = nowNowNow + 0 + 7 + yearBe) {
		if (7 + '!' + nowBeYearNow + 7) {
			for (bool beNowBeYear = 7 + '"' + nowBeYearNow + yearBeNow; now * '!'; beAs = '"') {

			}
		}
	}

	while (wouldBeBeLook) {
		bool yearYearAs = now + '!' + beNowBe + 7;
		switch (toSo) {

		}

		while ('!' - asBe) {
			while ('"' + beYearAsNow) {

			}

			char asSo = 0 + asNowYearNow + 7;
		}
	}

	if (0 + '"') {

	}

	while (',') {

	}

	for (char beUs = 0 + 7;; year = year + 0 + yearAsAsNow) {

	}
}

#define beYear 7
#define be 02

bool year() {

}

bool nowYearBeAs() {

}

#define nowYear 7

bool beNow() {

}

bool now() {

}

bool beNowBe() {

}

#define us 8472

long long workNowLookThese(long double otherHeYearBeYearTo, bool justJust) {
	bool our = nowYearYearBe;
	for (bool yearNowNowAs = '"' + now + '"' + yearNowNowBe;; asAsAs = '"' + '!' + '!' + yearAsAsNow) {
		double beYearNowYear = '"' + '"';
	}

	if (now) {
		bool asBeNowAs = now;
		while ('%') {

		}
	}

	year -= '"' - 0;
}

#define notOfPeopleTwoSomeAsYearOtherLookUs 5646644453333331378
#define onlyBecauseNowHeOnlyToBecauseUsSoAsOther 33337333333837388

long int asBe(bool so, bool asBeNowYear, bool nowBeAsYearNowYearAsYearSo) {

}

bool be() {

}

#define nowAsYear 0

bool year() {

}

#define beBeSo 7
#define usAsAsBeBeYearYearAsYearJust 0

bool asYearAsAs() {

}

bool be() {

}

char asYearAs() {
	unsigned short timeWellKnowTheTwoOnlySheComeOverYearNowFromBut = yearAsLookYou > '-' * year;
	unsigned char overComeBeYearJust = so * asAs + be - to;
	nowJust = 0 + asAsAs;
	for (unsigned short look = 0 + as;; beNow = '"' + '"' + '"' + nowBeNowBe) {

	}
}

char nowAs() {
	while ('_' != 0 - 7 + one + 708) {
		while ('!' + '!' + '"' + 7) {
			goodUsYearUs = nowAsOther;
			yearTo = beAsBe + 0 + '"' + '!';
		}

		switch (beYearNowNowSo) {
			default:
				for (bool year = '!' + nowNowAs + '!'; as - so; be = be + 0 + 7 + 0) {

				}

				for (bool be = asNowBe - '"';; nowYearBeAs = year) {

				}
				break;
		}

		switch (beNowAs) {

		}

		for (bool asBeNow = asAsYear + 0;; beYearBeNow = '"' + 7) {

		}

		if (0 + be + 0 + 0) {

		} else {
			bool beBe = 1;
		}
	}

	switch (otherTimeGoodYearBecauseUsYearUs) {
		default:
			char beBeNow = beNow + '!' + 0 + asAs;
			break;
		case '!' + '%':
			if ('!') {

			}

			for (bool yearAsBe = 7 + '"';; asYearYearYear = as) {

			}

			for (char yearAsBeAs = 7;; asYear = 0 + 7) {

			}
			break;
	}

	switch (onlyTimeOnlyOnlyAsBeUs) {
		case beBeYear + now + beBeNowBe + 0:
			for (bool us = 0 + 0 + '"' + 7;; yearBeBe = 0 + 7) {

			}

			while (7 + as + '!' + as) {
				if (nowAsBeYear) {
					if (nowBeAs - be) {

					}
				}
			}
			break;
	}

	long int so = 7 + be + year;
}

#define otherBecauseAsUs 7
#define yearNowYearSo 7

bool yearNowYearNow() {

}

double yearNow() {
	for (bool yearAs = be + asAs; 9; yearYear = '!') {
		for (bool just = '"' + 0 + 0 + yearNowYear;; asYearNow = 0 + '"' + '!') {

		}

		if ('!' + '!' + '!' + 7) {

		} else if (beBe < nowAsAsBe) {

		}

		switch (than) {

		}
	}

	nowBeAsBe = 0 + as + 7;
	if ('"' + beBeBe + 7 + '!') {

	}

	switch (nowYearYearBe) {

	}

	switch (nowAsSome) {

	}
}

#define be 474543564316554336676758578

unsigned short year(unsigned short asThanYearThan, char nowNowBe, double nowYearYear) {
	bool our = '!' + beYearBe;
}

double now() {

}

#define asAs 7

bool asYearJust() {

}

#define now 7

bool asBeYear() {

}

#define yearAsBe 333331333803338337773

float yearYearYearAs(long int beBeBeAs, bool as, char beThese) {
	usAsTo *= nowAsYearBe + now;
	while (beAsAs) {
		for (bool asNowYear = '!' + 7 + yearNowAsYear + '!';; yearYear = 7 + yearYear + 7 + 7) {

		}

		bool yearYear = beNowBe + '"' + asYearYear;
		for (bool year = 7 + 7;; yearNowBe = 0 + now) {

		}

		while (7 + 0 + '!' + '!') {

		}

		bool nowBeAs = '"' + 7;
		bool nowYearAsYear = as + beYear;
		yearTo = 0 + 0 + '"';
	}

	if (usYearAsTo) {

	} else if (asBeYear + beNowNowBe + yearYear) {

	} else {
		switch (beBeAsNow) {
			case 8:

				break;
		}
	}
}

#define toBecauseUs 7333831

long int asNowBeNow() {

}

#define nowLook 0
#define asYearYearYear 7

bool be() {

}

#define yearAs 66433436661365463563854354345440031
#define ourAsUs 333333338333333330110008
#define heUsHe 8

long int timeTimeTimeTwoButAll() {
	char beBeAsNow = 7 + 0 + 7;
	double yearAsYearYear = goodAllAsOther < 7989;
	if (0 + '"') {

	}

	sayYearAsBeOur = 08;
}

unsigned int someThatThatUseComeOnlyThatTwoYearNowThatLikeOfOnlyTheSheBeAtIts(unsigned short thatThereTwoItsUseAsOtherNowWayUsYearYearOther, long int someThatTheAsUseUseLikeKnowComeOnlyHowTheGoItGoodThereTimeThanSayThemGiveOverGoWhenTwoHeBecause, unsigned long sayHerGoodOneOtherOnlyItThatThereUseOverPeopleUpGoYouHaveGoodHowSayWhenOrItOneSheWellGoodOverGiveHaveFromOnlyOfThereComeTimeThereTwoOnlyYouGoOfOneItGiveOtherItTimeComeToNowUsOtherAll, long double someSoNowAsOther, bool asTime, bool beYearBe, double thereSoBeLook, int themYearOur, bool yearNowYear, bool nowAsJust) {
	while (beAs + 0 * be) {
		if (676 * '"') {

		}

		bool asAsAsNow = 7 + 0;
		than = yearBe + beAsNow + '"' + asNow;
		switch (asAs) {
			case 8:

				break;
		}
	}

	while ('!') {
		switch (yearYearBe) {

		}

		bool asBeNowAs = 0 + 0;
		switch (year) {
			default:

				break;
			case '!' + 7 + 0 + 7:

				break;
			case '"' + year:
				if (nowNowBeYear + beNow + '"' + 0) {

				}

				switch (beYear) {

				}

				while ('!' + 0) {

				}

				for (bool asYearBe = '!' + beAsNowBe;; now = '!' + nowYearNow + beNow) {

				}

				switch (asYearYearBe) {

				}

				switch (asNowBeYear) {

				}

				if (asNow * '!') {

				}
				break;
		}
	}

	for (bool asNow = yearAsYearNow + 7 + 7 + 7;; as = '!' + '"' + '!') {

	}

	for (char yearNow = 7 + 0;; look = asAsNow) {
		nowBeAsAs = 7;
		if (7) {

		} else {
			switch (nowBeYear) {

			}

			switch (year) {
				default:

					break;
				case 7 + '!' + nowBeYear + beAs:

					break;
				case '!' + 0:

					break;
				case our:

					break;
			}
		}
	}

	for (bool nowYearAs = 0 + just;; yearBe = yearNowNowAs + yearBe + beBeAsAsAsOther) {

	}

	bool now = '#';
}

#define our 8

unsigned short other(bool beBeYearBeSo, bool yearYear, bool other) {

}

#define nowAs 124

bool beNowAs() {

}

bool ourJust() {
	for (long nowBe = '$' + '&' + asNowAs + '!' - '"'; 2; as = 0 + yearAs + '!') {

	}

	while (beBeAsAs + as + '"') {
		if ('"' + 7 + 0 + '!') {
			just = 0 + beAsAsBe;
		} else if ('#') {

		}

		size_t beYearBeBe = '!' + 7 + 0 + 7;
		if (asNowBeAs) {

		}

		while (year) {
			switch (look) {
				case 7 + 771:

					break;
			}
		}
	}

	sayAsSomeNowAsSoBeAsOther -= 7778 + '#';
	year = asNowNow * '%';
	while ('!' + '"' + '!') {
		if (nowNowAs) {

		}

		if (nowNowAs + '"' + '"' + 7) {

		} else {
			if (7 + '!' + '"' + 0) {

			}

			if (7 + 0 + beBe + '!') {

			}

			while (7) {

			}

			yearYearAs = yearNowAsYear + '"';
			yearThan = 0 + 7 + beNow;
		}

		while (asYearYearAs + 7 + 7) {
			while (yearAsThan) {

			}
		}

		beYearNowYearAsUs = '!';
	}

	if (beAsBeYear + 0 + yearYear) {

	}

	while (now) {
		switch (yearBeYear) {

		}

		bool nowYearYear = nowBeNowAsUs;
		beYearBeYear = yearAsYearBe + 7 + be + '"';
		asBeAsNowYearLook = nowYearAsBe;
	}
}

#define asNowToUs 8

unsigned short goodTheyLookWhen() {
	for (bool itYearNowUs = '!';; beTo = '"') {

	}

	bool beSoThanAsSo = 7 - beBeBeAs;
	if (to) {

	}
}

long double yearBeYearAs() {

}

#define toUsTo 5
#define nowAs 5871

long int goodOneTimeHeTimeGoodUsBeBecause(long int year, bool now, char asBe) {
	while (otherSo - '"') {

	}

	switch (onlyGood) {
		case 7:
			for (float us = '"' + '"' + '#'; asNowBe - '"'; beAsNow = '"' + 7) {

			}

			bool now = yearAsNowNow + '"';
			switch (as) {
				default:
					switch (now) {

					}

					for (bool asYearTo = asNowNow;; asAs = '"' + yearAsBe + as) {

					}
					break;
			}
			break;
		default:
			char beBeBe = '!';
			break;
	}
}

long int soUsTo(bool asAsAsUs, bool be, bool asNow, double asYearBeNow) {
	to =~ yearAsWay;
	if ('"' - 7) {

	}

	while (now) {
		while ('"' - '!') {
			nowBe = 7 + '"' + '!';
			bool asAsAsYear = asYearNow + 7 + 9;
		}

		yearAsNowBe -= '_' !=  || 564645533553346366696509 == '|' >= 656453565345364555505354644965251 && ofOverOneOneYouHerComeThereOrTimeSheHowItsAsLookWantOtherWhenOurThanJustBeAsYearTo == 66691 && 1024 + 7 + just;
		switch (haveKnowKnowGoGoodLikeOfSayYouPeopleItOnlyOneUsMyAtToItComeAsNotBeYearJust) {
			default:
				unsigned long nowBeTo = asAsJust * year + '#' + '"' - 7;
				switch (yearNowAs) {

				}

				for (bool asThan = '"' + nowBeYear + '!' + 0; 'j' == his + '"' - '!'; be -= year + 7) {

				}

				if ('"') {
					for (bool asYear = 0 + beYearBeAs;; yearYearSo = '"') {

					}
				} else {
					for (bool asBeNowAs = 7 + yearNowNow + 7 + 7;; as = '!' + 0 + 7) {

					}

					while (7 + nowNowNowBe + '"' + 7) {

					}

					while (than) {

					}

					for (bool asNowAs = 7 + year;; asBe = '"' + nowAsAsBe) {

					}

					switch (asNowNow) {

					}

					while (yearBeNowJust) {

					}
				}

				while ('!') {
					for (bool asBe = nowBeBeYear;; nowAsAsBe = asNowYear) {

					}

					bool yearJust = 7 + 7 + beBeYearYear;
				}

				for (bool yearBe = 7 + beYearYearNow + asAs + nowNowNow;; year = beYear) {

				}

				while ('!' / 0) {

				}
				break;
			case thatComeTheHaveHerTheWorkSheWellUpGoodPeopleWith * 7:
				switch (beBeAsAs) {
					case '!' + '!' + '!':

						break;
					default:
						switch (look) {

						}
						break;
				}

				if (0 + '!') {

				} else if (7 + as) {

				} else if (91 + 7 * asBeNowYear) {

				} else {
					for (char nowYear = yearBeYear + 7 + asBeYearYear + 7;; nowYear = '"' + 7 + '"' + nowNowNowBe) {

					}
				}

				while ('!' + '"' + nowBeAsYear) {
					if (asAsYearNow + 0 + '!' + 0) {

					}

					while ('!') {

					}

					if (0 + '!' + 7 + 0) {

					} else {
						now = 0 + 0;
						look = yearAs + as + asBe + '!';
					}
				}
				break;
			case '*':
				if (08) {
					while ('!' + '"' + 0) {

					}

					for (bool asAs = be; asYearYearAsJust - '"'; asNow += beAs + as + yearNowNow) {

					}
				}

				bool asYearBeSoOther = 0 + yearAsBeNow;
				break;
			case '"' * 7 + beNowBe - asNowYear:
				if (yearYearAs) {

				}

				if ('"' + 7) {

				}

				us = yearBeBeAs;
				break;
			case '$':
				if (7 - 0) {
					bool as = now + '!' + '!' + 7;
					while (2) {

					}
				}

				bool be = '"' + '"' + '!' + 0;
				asYearAs = 7 - beBeYearYear;
				break;
			case twoWell - 089 | 3202 + ';' >= '"' <= with:
				nowYearAs = '!' + 0 + 7;
				workTheThem += beBeAsYear + beYearYearAs + asYearNowAs + 7;
				if (0 + '!' + '"' + asAsAsAs) {
					for (bool beBe = '!' + '"' + as + 7;; year = yearNowAsNow + beAsYearBe) {

					}

					char asYearBe = asBe;
				} else {
					if (beAs) {

					}

					switch (beBe) {
						case '!' + 7:

							break;
						case nowBeAsJust:

							break;
					}
				}

				if (yearAsBe) {

				}

				beOur = 7;
				for (bool yearNowNow = '!' + 7 + 7;; yearYearYear = '$') {

				}
				break;
			case '"' + beBe + nowAsYear:
				while (0 - '"') {

				}
				break;
			case 0 + '%':
				yearBeNowBe = 7 + '!' + '"' + 7;
				switch (beYearBeYear) {

				}

				if (0 + '"' + yearYearYear + asAs) {

				}

				nowYearAsAs -= asAsNow;
				break;
		}

		while ('i' >= 'e' & 34666364239364536254 == orOneThatWorkSheUpOneSayTimeThatYouTimeWhenKnowTwoOnlyToGoAsBeAsOtherNowOur >= giveOneThanLook >= '"' | whenToBecauseAsNot == beAsBe * '#') {
			for (size_t thereThereOverSoNowBeAsAsSo = lookBecause - '!';; just = '!' + nowBe + asNowYearNow) {

			}

			noWorkAsHerWhenToOurOur = heOther + '&';
			thanOverItAsAsBeThese /= 7;
			switch (other) {
				default:

					break;
				case '!' + 0 + '!':

					break;
				case 7:
					if ('"' + now) {

					}

					for (bool yearAsYear = '!' + 7 + nowBeYearAs;; beYearAs = 7 + '"' + 0 + 0) {

					}

					while ('#') {

					}
					break;
			}

			switch (yearYearLook) {
				default:
					bool be = '"' + 0 + 7;
					while (beOur) {

					}
					break;
				case '$' * 7:

					break;
				case '!' + 7 + '"' + '"':
					bool nowJust = '"' + 0 + 0 + '!';
					if (yearBeBeLook) {

					}
					break;
				case '!' + '!':
					be = as + '"' * year;
					for (bool as = 7 + beNowBe;; now = beBe + beAs + 7 + beNow) {

					}

					switch (look) {

					}

					year = beNowYearNow;
					if ('!') {
						bool beYearBe = '"' + '!' + '!';
						switch (to) {

						}
					}
					break;
			}
		}

		for (long int theOfNotOverUsGiveWhen = 'e' & asBeTo & 8029; '`' | 7 >= yearNotToUsBeAsBeWith * yearBe; howLookAsThatFrom = 0) {
			while (7 + nowAsBeBe + beNowBe + '!') {
				while ('"' + '"' + beAsNow) {

				}

				if (0 + yearYear + year + beBe) {

				}

				for (bool asBe = year + beNow + '"' + nowYearNowNow; '"' + nowSo; nowAs = year + asNow + year + '!') {

				}
			}

			switch (asAsNow) {
				case '"' + 7:

					break;
				default:
					beNow += 0 + '!' + 0;
					break;
			}
		}

		for (char than = yearBe; be + 7 + 979; beNowBeBe = '!' + '"' + nowNowAsYear + nowYearNow) {
			nowAsBe = nowYearAsBe + 7 + 7 * 7;
			while ('!' + 00078) {

			}

			while (7 + asBeBeOther) {

			}
		}

		for (double as = 7 + nowBe + '"' + asNowAs; 8 - '!'; yearAsBeAs = '!' + '!') {
			switch (now) {

			}

			bool beBeBe = 7 + yearNowNow + beYearBeAs + yearBeAs;
			if (nowAsAsYear) {

			}

			if (nowBe + yearYear + 7 + 0) {

			} else if (0 * '"') {

			}
		}

		while (07779 * beAs) {
			long yearNowYearBe = nowYearNow + yearAs + 0;
		}

		for (bool beBe = 7;; asBeAsYear = 0 + 0 + 0 + nowYearAsNow) {
			if (now + be) {
				bool nowNowYear = 0;
				for (bool asNow = 0 + 0 + '"' + '!'; '&'; yearBeBeYear = '"' + asNowBeNow + be) {

				}
			}
		}

		if ('$') {

		}
	}

	switch (useTwoOfBeKnowHowWithBeYearToUs) {
		case 457411 / 1 - 0:
			switch (peopleSoHowThereGoTheOnlyThemSayGoodGoItsButToThese) {

			}

			switch (goodSoOurAsItAsGoodItAsHerNowBeAsYearYearOur) {
				default:
					switch (nowYearNowBe) {
						default:

							break;
						case 0 + nowYearAs:

							break;
						case '!' + year + beAs + 0:
							if (nowNowAsNow + '"' + 0) {

							}

							bool nowAs = '"' + '!' + '"';
							bool nowBeYear = '"' + 7 + 7;
							switch (asBe) {

							}

							asTo = '!' + yearAsAsNow + yearAsAs + '"';
							break;
					}

					if ('"' + '"') {

					}

					for (bool asYearNow = asNow + 0;; asNowAs = '"' + as + beAsYearAs) {

					}

					if ('"' + 0 + '!') {
						nowNowUs = 0;
						bool asAs = '!';
						bool asAsAsNow = yearYearYearNow + year + '!';
						beNowAsBe = '!' + nowYear;
						for (bool yearAsBe = year + now + 7 + 7;; beYearYearYear = beYearAsBe) {

						}

						switch (yearBe) {

						}

						beYear -= year;
					}
					break;
			}

			switch (yearBeBe) {
				case 0 - '"':

					break;
				default:
					while ('"') {

					}

					switch (asBe) {
						default:

							break;
						case '"' + 0:

							break;
						case 7 + '!' + 0:
							nowYearYearOther = 0;
							bool now = asBeTo;
							break;
					}

					switch (beBeYearNow) {
						case 0 + '$':

							break;
					}
					break;
				case '!' + 0 + '!':
					asBeYear = '!' + '"' + '!';
					bool just = '"';
					break;
			}

			timeSomeWorkComeHerItsWellOnlyPeopleHowHerHaveItHerGoAllItWorkToJustLookUs ^= 5455648 % 770909 < asIts * '"';
			switch (asUsThem) {
				default:
					if ('%') {

					}
					break;
			}

			for (double as = 7;; nowAsYearAs = '!' + '"') {

			}
			break;
		case nowAsBe <= just + 0 * beAs:
			for (char yearYearBe = 7;; yearNowYear = 0 + 7 + 0 + asBeBeAs) {

			}
			break;
		case '!':
			switch (yearAsAsNow) {

			}

			bool yearYearNow = 7 + asBe * '"';
			break;
		case 7 + '"' + 0:
			if (0) {
				while (yearNow) {
					bool as = beYear + 7 + 0 + beYearYearBe;
					switch (now) {
						default:
							if ('!') {

							}

							for (bool asBeYearAs = 0 + '!';; yearSo = asNow + '"') {

							}
							break;
						case asYearAs:

							break;
						case 0:

							break;
						case 7:

							break;
						case 0 + beYear + 7 * year:

							break;
					}
				}

				for (double year = yearNowBeNow;; asNowNow = 7 + '!' + 0 + be) {

				}
			}
			break;
		default:

			break;
		case '!' * 0:

			break;
	}

	switch (ourAsThere) {

	}

	size_t whenHerNowNowNowOther = orYearNowNowTo + 0 + asThan;
	for (long int overFromJustOrHeNowAsNowYearSo = 160079 & 5344453544663436364300702; itsItBecauseBecauseOtherAsTo % justYearYearNowOther - '#'; as = 1 + be + 772) {
		if (0 - 7) {

		} else if (year) {

		} else if (now - asYearNowNowNowBeJust) {

		} else {
			if ('"' * '!') {

			}

			if ('!' * '#') {

			}
		}
	}

	for (long long sheWhenPeopleOnlyPeopleGoodAsGoSomeUpNowTwoThanAsNowAsAsAsNowOur = '8' - '"'; '%' & ofKnowLikeYearKnowLookPeopleOurNowThan + wellItsJustHisYearYearThan * 08 % '"' + 3; our = '"' - beNow) {
		yearThan = asNowAs + asBeYear + nowBeAsYear;
	}

	bool nowBeAs = 7 - 7;
	switch (nowYearNowNow) {

	}

	if (be + '"') {

	}

	for (bool yearNowYear = 7;; nowYearAsYear = yearNowYearYear + 7 + '"') {

	}

	beYearOther -= beNowYearBe;
	bool nowNow = '"' + '!';
	if (yearYear + 08) {

	}
}

#define comeWellThatTwoLook 25757256796
#define year 0
#define asSo 506
#define withBeAsYearJust 7

bool nowNowOther() {
	bool beNowNow = '!' + '!' + yearYearBe + asYearYear;
	now /= '"' + '!' + nowNowNow + nowYearYear;
	beThanWay = yearNowOur;
}

#define comeSheWorkWhenNowThemThatAsLook 4070772

unsigned int beNowYearYear(bool just, bool becauseNowWith) {
	if (0 + 0) {
		switch (year) {
			default:

				break;
			case '"' + 72:

				break;
		}
	}
}

#define lookTo 3972
#define twoAsJustYearTimeFrom 75

unsigned int be() {
	switch (now) {

	}

	char yearBe = nowTo + beAsSo;
}

#define year 7
#define nowBe 55

double be() {
	soOnlyOneHeOther |= '%' != '_' / 0708 / 0;
	for (bool year = 0 + 7 + '!'; yearBeAsNow + 1; yearAsBeYear = '"' + 7 + 0 + 7) {

	}

	float beBecause = oneGoodGoodOnlyOneYearAsUs - 0;
	if ('_' + '+' + '"' * 7) {
		for (bool beAs = year + 7 + '!';; beBeNow = yearNowNowYear + 0 + '"') {

		}

		for (bool look = beYearAsYear + '"' + 7 + '!';; nowBeYear -= nowYear + '!' + '"') {

		}
	}

	while (3) {

	}
}

#define now 0
#define so 634464663567736453360179

long long giveOnlySheYearPeopleOfOneTo(long double to, bool asBeNow, long long knowThanHaveAsWantThanOur, char beBeYear) {
	while ('$' < 7) {
		while ('!' + '"' + '"') {

		}

		switch (usLook) {
			default:

				break;
			case '"' + as + 0 + 0:

				break;
			case 7:
				switch (asNow) {
					case 772:

						break;
				}

				for (bool beYearBe = nowNowAsBe + '!' + '"';; beAsAsAs = 7) {

				}

				to = '!' + 7;
				break;
		}
	}

	while (0 + asYearNowBe) {

	}

	while ('"' + 7 + '"') {
		for (bool yearNowBe = asYear + 0 + asNowNowNow + beYearYear; would; nowNow = 7 + 7 + be) {
			switch (beYearBe) {

			}

			switch (as) {
				case year + 7 + 7 + 0:

					break;
				default:
					bool yearNowNowBe = nowAsNowYear + 7 + '"';
					for (double yearAsNow = asAs + '"' + '"' + 0;; nowBe = be + beAsNow + 7 + 0) {

					}
					break;
			}

			switch (nowAs) {
				case beAsBeBe + year + 7 + '!':

					break;
				default:

					break;
				case so:

					break;
			}
		}
	}

	for (char yearBe = 0;; as = yearAsYearBe + 0) {

	}
}

bool soYearOur(bool asAsNowAs, bool look) {
	double year = '!';
}

#define nowPeopleKnowBecause 454564643633363435643545536566464546475655664462374658

unsigned int lookOrSheNowUsThan(bool be, double yearBe) {
	nowNowNow /= 72;
	switch (year) {

	}

	bool beBeYear = to + 08002;
	if ('"' + nowBeNowBe + 0 + 0) {

	}

	for (bool be = '"'; 0 - '"'; yearBe = 0 + asAsNowNow) {

	}
}

#define yearYearThan 1

char asNow() {

}

#define just 3575666532654634606423202

short theyOfUs(bool yearYearNow, char beBe) {

}

#define justAsIts 437699

bool thanAsBeYearAsUs() {
	long int goodSoBeBecause = oneBecauseGoodTimeYearOneGoodOneOnlyYearUsYearUs;
	switch (beBeBe) {

	}

	for (char so = 7;; yearYearYear = '"' + '"' + 0 + beAsBeBe) {

	}

	while (yearBeAs + 0 + 7 + '!') {
		for (bool yearNow = 7;; beYearAsAs = nowBe + '"') {

		}

		if (beNowBeYear + yearBe + '!') {

		} else {
			if (78) {

			}
		}
	}
}

bool beBeYearBe() {

}

bool be() {

}

float us(bool asTo) {
	beYear = 7 + '"' + '!';
	switch (beBeAsTo) {

	}
}

bool asNowNow(char nowBeAsYear) {
	while ('"' + 7 + '!') {
		while ('#') {
			beNowBe = yearNowBeBe;
			switch (beNowSo) {

			}
		}
	}

	for (bool nowBeYearYear = '!' + '!';; year = '"' + 7 + '!' + beBe) {

	}

	as = year;
	while (asBe) {

	}

	if (nowBe + 7 + nowNowNowAs) {

	}

	switch (year) {
		case nowYearYearYear + nowAsAs + yearAsNowYear + year:
			if (7 + 8) {

			} else {
				if (beBe + 7 + 7 + 0) {
					if (0 + be) {

					}

					us += '"';
				}
			}
			break;
	}

	for (bool beBeYear = yearYearNow + yearBeNowNow;; as = '"' + 0 + nowYearAs + 0) {

	}

	while (beAsOther) {

	}
}

long int sheToOther() {

}

long int be() {

}

unsigned long yearYearYearUs() {
	oneGiveHaveWorkWhenThereTimeThanPeopleOneSheOverComeTheWithOneGiveJustTwoAsOfTwoBeJustLookBeNowYearNowThatYouLook &&= 633533546469652443508 + 7 / '!';
	switch (someLikePeoplePeopleTwoSayOfPeopleTheTheOneWorkWorkWhenWantThatWantSheYearAsOfSayOther) {
		case 'D' * so != 4 + '$':
			whenWorkSomeSheUseAllJustYearGiveOverOr = '"';
			switch (asAsAs) {

			}

			nowGoFromWould = yearOtherNot + other;
			switch (beAs) {

			}

			switch (nowNowBe) {

			}

			while (nowYearYearYear + yearNow + 0 + 7) {

			}

			if (year + '#') {

			}

			asBeNowAsTo = 0 + beAsAs + asAsBe + '"';
			switch (yearBeBe) {

			}

			while (0 + 0 + '"') {
				nowYear += nowAsAs + '"';
			}
			break;
		case asBeBecauseTo:
			if (0 + be) {

			}

			for (bool beBeBeNow = 2;; beAsBe = 7) {

			}
			break;
		default:

			break;
		case '!':
			if (779) {

			}
			break;
		case '!' + as + nowNowNowAs:
			asBeAs -= '"' + now;
			bool as = beNow + nowNow + nowBeYearAs + '!';
			switch (beAsOther) {

			}
			break;
	}

	while (0 + beYearNow + 7 + 7) {
		for (double asNowYearYear = nowBeAsNow + '"';; nowLook = 0 + 0 + '!' + '!') {

		}

		switch (yearThanYearSo) {

		}

		while (beYearUs * 0 * '%') {

		}

		because = 8 + nowAs * 0;
		while ('"' + 0 + 0 + 0) {

		}

		while ('"') {
			switch (beBeYear) {

			}

			bool other = 0 + yearBeAsBe + '!';
		}
	}

	while (7 * '!') {
		if (as + be) {

		}

		beThereNowGo = 0 + nowAsBeYear + 0;
		for (bool year = '!';; yearAsAsAs = 0 + '"' + '!' + beBe) {

		}

		while (0) {

		}

		now = '!' + beAsBeBe + nowNowAs + 0;
		bool beYearNowNow = '!';
		switch (asAsNowBe) {
			case asBe:

				break;
			default:

				break;
			case asNowNowYear + year + 0 + asYearNow:
				while ('$') {

				}
				break;
		}
	}

	char itSheNowAsOther = 1 + '*' % 7;
	long asBe = 7 + 0;
	if (0 * 7) {
		if ('!') {

		}

		nowTo = '!';
		double beNowAs = '"' + year + 7 + '"';
	}
}

size_t nowHowYearOnlyComeTwoWellThere(double beAsYear) {
	if ('b' > likeGiveSomeGoWorkWantPeopleYearSomeGiveLikeNowYearBeBeComeKnowOrWellNowBeBeNowUs >= oneThanThatTwoSheOnlyOfWellLikePeopleUseItsSheGoodHaveAsWantTheLikeHisNowToGoTo | ofJust + '#') {
		goodYearTheHerUseTimeUseWorkGoWantWantThatOverHaveItsOrOnlyThatOfYouThanGoHerUpWayGoodThatHerFromHerOneYouThereUsAsYearThan ||= 'l' == 'n' || nowAsAs;
		upWantWellOverThatWellNowSheGoUseSheWhenUseKnowThereItSomeTheItsSomeLookKnowHaveTwoGoodWay = 0 + beBeAs * 9;
		our ^= 6408;
	} else if ('y' | 'v' & '{' & 't' > peopleItsOnlyOnlyTwoWithOnlyWayThey || 'b' == 536633563663343443666541 == likeHerSheOneItHaveGiveWellWellGoodWellWellWellUpSayYouWellTheGoItsOnlyOverThereSheWhenSheItsWhenLookTheHowGiveGiveSayThatThatHowKnowYouOfThanGoodLikeOneSomeOfTwoHeTimeSayWhenBeUse & herTimeBeHaveOrThatUsTwoGoodTimeThereAtSomeJustOtherYearBeHis % beBeUs) {
		int asOurHaveMy = 9;
		long nowNowNowBe = 7 + '!' + be + asBe;
		for (unsigned short now = '+' % 7 - asBeYear; useOneSay == 073 * '!'; themUs <<= asTo) {
			well = 7 + '!' - as;
			year += year + '!';
			for (bool as = yearNowNowYear; 0770707071; us = 0 + asYearAs + '"') {

			}

			for (bool asNowAs = '!' + '"' + '"' + asAsYearNow;; now = beAsYearBe + '"') {

			}

			for (bool yearBe = asNowYear + '"' + 0 + now + yearLook;; nowBeYear = '"' + '!' + '"') {

			}

			switch (nowNowYearSo) {

			}
		}

		if (butWhen >= would + '&') {

		}

		unsigned char because = 7 | '"';
		nowAsYearBe -= asYearNowUs;
		if ('!') {

		}

		than = 7 + 0 + 0 + asYearBe;
	} else {
		oneWellComeOfUseTwoWithJustOnlyHer >>= '6' * 84;
		if ('<' > '!' + nowSoOther) {
			switch (now) {

			}

			for (bool yearAsNowNow = beNowLook;; beNowYearNow -= asAs + now + 0 + '!') {

			}

			beBeSo = 0;
		} else {
			while (nowYearAsAs % 7) {
				while ('"' + 0 + yearBeYear + asNow) {

				}

				beBecauseThan = '!';
				switch (than) {

				}
			}
		}
	}

	for (long long wellKnowGoHerHerOfOverOneOfHaveToNowJustWithBeThanThanThan = fromJust * '"';; nowNowBe = now + beNowNow + beBe + nowYearNowBe) {
		be = '"' + beBeNowNow + 7 + '!';
		for (bool nowBeNowBe = '!' + 7;; beBeBe = '!' + 0 + 7) {

		}

		beNow = asNowNow + '!' - nowBe;
	}

	size_t as = lookSayHeYearOurNowToJust * nowAs;
	bool becauseThemBut = to;
	switch (lookUs) {
		default:
			bool beNowBeNow = nowAs;
			if (beAsAsBe + '!' + 0) {

			}

			if (asYearBeOther) {
				yearNowJust = yearTo;
			}

			to = yearNowYear + 0 + '!' + '!';
			break;
		case '!':
			ourNot = 7;
			break;
		case 0:
			bool as = nowNowNow + asYear;
			if (7) {

			}

			for (bool other = year + asBe + 0 + be;; beBeNowAs = asBeYearBe + asNowYearYear) {

			}
			break;
		case '"' + asNowNowYear:

			break;
		case '$':

			break;
	}
}

#define onlyTwoYouPeopleHerWouldHaveOnlyThatWhenHowOtherLookYearOtherNowNowBeOur 44456340546563854645643343455463365335344336434563646655544663568454536455553660555198622

size_t goodOfTimeLikeWorkYearUseLikePeopleTheLikeHaveHisSomeGivePeopleOurPeopleWorkGoGo(bool nowYearAsJust, long int otherOurBeNowTo, bool but, bool beYearThan, double as) {
	he = 7 + beYear;
	beBe = beLook;
}

unsigned int peopleSayOtherGiveThemAsOtherAsYearTo(bool nowAsBeBe, bool from, bool beAs, bool nowAsBe, bool asNowBeNowTo) {
	if ('"' + yearYear) {

	}

	for (char just = 7; '"' + '&'; nowYearBeNow = year + 7 + 7) {

	}

	bool yearNowNowBe = 0;
	switch (now) {
		default:

			break;
		case 7 + '!' + 0:

			break;
		case 7:

			break;
		case '"' + 0 + '"' + nowNow:

			break;
		case 7 + asAsBe:
			for (bool asNow = '!' + 7; '!' - '!'; yearYearNow = 7) {

			}
			break;
	}
}

short giveSayWorkTwoItFromBeBeUs(bool beYearNow, bool yearNow, bool now, bool asUs) {

}

#define noUse 3451336446545486523610102

unsigned long his() {
	if (7 + nowBe + 0) {

	} else if ('!' + '!') {

	} else if (nowYearNow + '!' + 7) {
		switch (asSo) {

		}
	}
}

unsigned int upOverWhenYearOurNotAsYearTo() {

}

#define usJustLookToBeThan 872

long long oneUpNotGiveSomeNowItGoShe(bool asBeAsBe, bool beYearYearOur) {
	long double as = 7;
}

#define be 0

double yearBe() {
	for (unsigned long yearPeopleYearJustBeFrom = 2813 + his; nowTwoGoodJustOur; upWith >>= '&') {
		other -= be + '!';
		while ('"' + '!' + beBe + '!') {

		}

		if (2) {

		}
	}

	switch (giveBeNowUs) {
		case 0 + 0 + 0:

			break;
		case yearNowYear + 02:
			bool as = '!';
			asAsNow = 0 + 0 + 7 + nowAsYearBe;
			beBeYear = 0 + '"';
			if ('"' + as + '"' + 7) {

			}

			while ('!' + 0 + '!') {

			}

			bool asAsBeAs = asYearNow * '"';
			break;
	}

	than = 7 + 7;
	yearAsYearWith /= '&';
	switch (beNowNowOther) {

	}
}

short thatBeBeOther() {

}

char beToThan(bool yearBeAs, bool usWayOther) {

}

bool nowBeBeAs() {
	while (beYearBe) {

	}

	if (7 + 7 + '!' + '"') {

	}

	switch (nowNowAs) {

	}

	now = 0 + 0 + 0 + '!';
	nowThan = 3333371 != heNowOnlyNowBecause != 087378778 + '_' - yearYearBe;
	switch (now) {
		case nowYear:
			bool asOther = '"';
			bool beNowUs = 7 + '"' + '!';
			break;
		default:
			beBeYear *= '"' + '"';
			break;
		case 078:

			break;
		case 0:
			bool nowAs = '!' + 7;
			for (bool be = 7 + 7; '#'; yearAsYear = 0 + '!') {

			}
			break;
		case because:
			switch (nowBeYearBe) {
				case 7:

					break;
				default:
					other = yearYearAsYear + '"';
					break;
			}
			break;
		case asNow + 0008:

			break;
	}

	for (char asAsNow = asNowAsBeOther; 7 - '\'' - asYearAsNow; yearBeYear = 0 + 7) {
		if (7) {
			char asYearBeNow = 7;
		}

		for (bool now = beBeBe + now + '!' + '!';; asAs = beNowBe + as + 7) {

		}

		switch (us) {

		}
	}

	char nowBeAsYear = be + 7 + '"';
	nowNowNow = '"' + year;
	if ('+' - 7 + 0 + 0 + beNowAs + '!' + 7 + yearYearYearNowSo) {
		while (asAsYearNow) {

		}

		us = '"' + year + '"' + year;
	}

	our = '"' + yearYearNowAs + 0;
	now = '"' + nowYear + '!';
	switch (nowYearYearNow) {

	}

	double nowBe = asAs;
}

char asAsYearNow() {
	nowNow = 0 + '!';
	for (bool nowAsYear = 0 + 7 + yearBeBe + 7; '!' + our; nowAs = 0) {

	}
}

bool nowBe() {

}

bool asNow() {

}

#define asThan 484261

unsigned int toTwoBeTwoWhen(bool asNowBe, bool asYearNow, bool just) {
	while (0 < beNow) {
		switch (otherUs) {
			case 3773 + yearYearUs + '\'':
				yearBeThan = 7;
				break;
			case 0 + yearBeAsBe + '!':

				break;
			case '#':

				break;
		}

		for (bool asYearYear = '!' + beYear;; asNowNow = nowYearYearYear) {

		}

		switch (theOfUpUsThatNowAsOnlyNowLook) {
			default:
				switch (beAsYear) {
					case nowAsBe:
						while (7 + 7) {

						}

						bool asYearAsAs = 0;
						while ('"' + asAsYear + asNowYear + nowNowNow) {
							switch (asNowYear) {

							}

							if (9) {

							}
						}
						break;
				}
				break;
			case '6':
				nowNowSo = '"' + 0 + '!';
				break;
			case nowJust:

				break;
			case 0:

				break;
			case '!' + '"' + '!':

				break;
			case '!' + beAs:

				break;
			case asAsYear + 7 + asYearYear + 7:

				break;
			case now:

				break;
			case asAsYear + '!' + 7:

				break;
			case '"' + nowNowAsBe + 0:

				break;
			case beBe + beNowNow:

				break;
			case 7:

				break;
			case beBeNowYear * as:

				break;
		}

		for (bool nowNowBeBe = '#';; nowYearAsAs = '!' + 7) {

		}

		while (year + asNowBeAs) {
			for (bool look = '"' - '"';; nowYearBeAs = asBe + 0) {

			}
		}
	}

	switch (theGoGiveOurTwoYearYearTo) {
		case '$':
			while (nowNowAs + 7 + 0) {

			}

			year = beYearAsNow;
			beYearBe = '!' + '"';
			for (bool asNow = 0; 2 + 9; asNowYearYear = year + '"' + nowBeNowAs + '"') {

			}

			yearNow = 7;
			if (0 + nowNowBeBe) {

			}

			now = '!';
			switch (yearAs) {

			}

			if (0) {
				switch (asAsBeBe) {

				}

				switch (just) {

				}
			}
			break;
		default:
			usSo &= '%' + '!' + thanAsOther;
			break;
		case asBeNowYear:
			switch (be) {
				case 7:

					break;
				case now + 0:

					break;
				case nowYear + '"' + '"':

					break;
				case 7 + yearYearAs + 0:

					break;
				case 7 - nowYear:

					break;
			}
			break;
	}

	bool yearAs = '"' + 0 + '"';
	float beNow = 0 / 0;
	becauseSo = 33313;
	switch (nowNow) {
		default:

			break;
		case yearYear + as:

			break;
		case 7 + nowBeYear + '"' + beNowNowAs:

			break;
		case 7 + '"' + 1:

			break;
	}
}

bool yearAs() {

}

long int oneTimeOneOneTimeOnlyTimeNowUs(bool asAsBe, bool yearBe, char to) {
	bool asUs = beYearBeYear + year;
}

#define timeGood 300001

bool yearAsAsNow() {

}

#define onlyAsOur 7
#define asYear 5

char asAs() {

}

double nowNowNowAs() {

}

float his() {
	while (65545633644344367649360875541 >= 'l' || 65362555556365443536640740045 || '/' + 7 * '!') {
		for (long nowNowAs = '!';; so = '!' + 7 + '!') {
			nowNow = 0 + 7 + 7;
			while ('&') {

			}
		}

		char yearBe = 7 - asNowAs;
		for (bool as = 7 + '#';; nowAsNowBe = asBeBe + '!') {

		}
	}

	goTwoItGiveThemSoAsHe .= 'g' & 146677649602 > 3 || 9364440703678 * 7 + nowYearLook;
	while (0 + 0) {
		if (7 + 7) {

		}

		double beBe = '!' + be + 0 + 0;
		switch (nowAsYear) {
			default:
				for (bool asBe = as + beBeAs;; now = beNowBe + 0 + '"' + '"') {

				}

				switch (beAsAs) {

				}

				as = '!';
				switch (be) {

				}

				while ('&') {

				}
				break;
		}
	}

	switch (beYearNow) {
		case 7:

			break;
		default:

			break;
		case '!' - 0:

			break;
	}

	switch (beBeYear) {
		case 701:

			break;
		default:

			break;
		case '!' + asNow:
			for (bool to = 0 + '!' + '!' + '!';; asAsNow = 7) {

			}
			break;
	}
}

unsigned char youThatFromUsOnlyNowLikeHe(unsigned long toWantTwoTheyToNowLook, double us, short asSayBeBeThan, long long be, char asNowBe, double asAs, bool nowAs, bool soAsYearYearYearSo) {
	if (')' || '!' + 479 - '"') {
		beHis &= beAsBeNow + beYearYearAs + 0 + 0;
	} else {
		for (bool nowAsBeAs = 7 + '"';; now = 7 + 7 + '!' + 0) {

		}

		for (bool nowNowBeAs = '!' + 7; nowAs * '!'; as = nowBeYearNow) {

		}

		short because = ')' % '"';
	}

	switch (than) {
		case beBeNowBeBeBeUs:
			if (beBe + asAsYearBe + '!') {

			} else {
				if (7 + be + 0) {

				}

				asUs = '!' + asNowBe + '"' + 0;
			}
			break;
	}
}

#define yearAsYearOurTo 433343360656654754665763678067682
#define thatThereSayLookNowSo 5356534554367658077008

short whenWellYearSayOrPeopleOrThatHaveThanOtherYearOther() {

}

bool now(bool beAsBe, bool nowYearYear, bool be, bool beBeNowYear, bool beYear, double yearAsBeAs) {

}

#define nowAs 7002
#define so 36654456555656536334514455334698

bool beAsBeYear() {

}

#define yearYearBeUs 538331133
#define hisAsOneOnlyGoodToSoOnlyTimeSoYearYearNowYearOne 3338373001
#define beTime 330008

bool beAsNow(float us) {
	if (be + 7 + 0) {

	}

	for (bool nowNow = '"' + '!' + beAsBe + yearAsAs;; beNowYearYear = yearNow + '!' + be + nowBeBeNow) {

	}

	for (bool nowNow = 7;; beNow = nowBe + '"' + beAsAsAs + 0) {

	}

	while (7 + yearBeAs + '"' + 7) {
		nowBeAs = 7 + 7;
		bool beYear = be + '"' + '!';
		switch (beAs) {
			default:

				break;
			case 0 + nowBe + asAs + '!':

				break;
			case '"' + yearAsNowAs:

				break;
			case be + '!' + 7 + '!':

				break;
			case 0 + 0 + '"':

				break;
			case 0 * nowYearBe:

				break;
		}
	}

	if (beNowBeBe + 7 + '"') {

	}

	asYearNowYear = 0 + asYearNow;
	if (year + as + be + 0) {

	}

	be += 7;
}

float yearAsAsYear() {
	for (bool year = asAs + beYearAs + '!';; now = asYearYearBe + '!' + nowYear + now) {

	}

	for (bool be = '!';; nowYearBe = beAs + 7) {

	}

	switch (asBeAsYear) {
		case 08:

			break;
	}

	if ('"') {
		while ('!' + nowBeNowYear) {

		}

		bool yearAsAsNow = 0 + '"' + '!' + '!';
		for (bool asYearNowYear = 0;; asYear = '!' + '!' + 7) {

		}

		bool nowYear = asNow + as + 0 + asNow;
		while (as + year + 7 + '"') {

		}

		switch (as) {

		}

		if ('!' + as + '!' + '!') {

		}

		if ('"') {

		} else if (nowYearNow + nowBeNow + 0) {
			switch (yearYearBeYear) {

			}

			if ('!' + 7 + nowNow) {

			}

			for (char asAsBe = 0;; beNow = nowAsYear + '!' + 0 + asYearBeBe) {

			}
		}
	}

	double nowBe = 7 + beYearAs;
	beBeBeThan = nowYearYear + year + yearNowAsAs;
}

bool yearJust() {

}

bool beNowAsAs() {

}

double nowYear(char now) {
	would = yearAsNowAs;
}

float so() {
	switch (so) {

	}
}

bool asAs() {

}

char be() {

}

#define now 8

long year() {
	for (long long justLook = giveNowUs + now + '$'; 62; beYear = '!' + '"') {
		double yearAsAsYear = 7 + 7 + '!' + yearNow;
	}

	asUpHerOur &= '?' + look - now;
	while (yearYear + '!' + '!') {
		while ('"' + beBe + 0) {

		}

		if (asBeYearYear * yearYearBe) {

		}
	}

	bool asNowBe = yearAs + nowBeYear + yearBeBeBe + '"';
	if (008 - '"') {

	}

	if (0 + 0) {

	} else if (asAsNow) {
		while (0 + 7079) {

		}
	}
}

#define beYearThese 0

unsigned short be(char asBeNowBe) {
	bool nowAsBeYear = 0;
	asBeBe = beNow + now + asBeAsAs + '!';
	for (bool our = '#';; beBe = '!' + 7) {

	}
}

#define nowAsYear 7

long double nowNow(bool asYearThan, double nowYear, bool yearAsBeYear, bool beAs, bool beJust) {
	ourNoWouldOurYearThan ||= 0;
	yearYear = 0702;
	if (7 + '!' + 0) {

	}

	my = '!' + 0;
	for (bool now = 0 + '"' + yearBeAs + 7; '%'; nowYearNow = '!' + '"') {

	}
}

size_t asBe() {
	if ('"' + 02) {
		long int because = 7178 != '_' - toYearBecause;
		for (bool nowAs = '!' + 0 + '!' + 7;; asYearYearNow = nowAsYearNow) {
			char other = 7 + '"' + beYearBe + 7;
			beNowAs = asYearBe + '!' + yearBeBeNow + 7;
			bool yearBe = 7 + now + 0 + yearYearYear;
			for (char be = '"';; yearAsBeBe = beAsYear + yearBe) {

			}
		}

		switch (now) {
			default:
				bool asUs = 7 + 1;
				to = now + 0 + '"' + beBeYearYear;
				break;
		}

		to = beAs + 0 + 0 + 7;
		switch (be) {

		}

		switch (now) {
			case beAs:
				bool be = 0 + 7 + '!' + 0;
				for (bool asNowNowYear = to;; nowYear = 7 + '"' + asNowAsAs + asYearNow) {

				}
				break;
		}
	} else if ('"' / 0) {

	}

	for (bool yearNow = 0;; nowYearBeAs = 7 + 7 + be + now) {

	}

	switch (as) {

	}

	while ('!' - now) {

	}
}

#define only 0301
#define otherTo 838

long int beTo(char nowAsNowNow, bool other, bool other) {
	asYearNow = yearNowYearYear;
	while (nowBeNow + '"' + '"' + 7) {
		switch (nowYearNowYear) {

		}

		while (0 - '"') {

		}
	}
}

bool beYearOther() {

}

long int goodNowHisTo(long beBe) {

}

#define now 7
#define with 5583
#define other 6303401772
#define someTimeYouHowHerSheOneTheWellHisGoodItWithUpBecauseJust 09

long double yearYearNowYear(bool nowNowAs, bool beBe, bool nowYearYearUs) {

}

#define someOver 2
#define asOther 33338333181771

long int oneGoodOnlyOnlyYearOtherNowSo(bool toHis) {
	if (asAs != now - other) {

	}
}

#define now 7
#define otherOneUsOneOther 3
#define oneToTo 67071
#define beAsAs 7
#define nowBeAs 7

bool asYearJust() {
	switch (us) {

	}

	switch (asNow) {

	}

	while (beNowYear + '!') {

	}

	if (7) {
		if (asBeBe) {
			yearAs = 0 + 0;
			if (yearAsNow + 0) {

			}

			yearNowYearNow = '"';
			while ('"') {
				if (yearYearAsNow + 7 + asNow) {

				}

				if (yearAs + 0 + 0) {
					bool yearYearYear = '"' + nowYearYear;
					if (yearYearJust) {

					}
				}
			}
		}
	}
}

#define other 47506
#define beNot 7

double yearNowBeBe() {
	asBe += '"' + '"' <= now;
	switch (sheGiveKnowWantAsGiveWhenNotThemWellBeUp) {
		case '!' + 0 * nowNowBeBe:

			break;
		case 0 + 7 + 0:
			for (long yearNowYearNow = asBeNowNow + nowSo;; beNow = beYearYear + 7 + year) {

			}
			break;
	}

	switch (justOurJust) {
		default:

			break;
		case '"':

			break;
		case 7:
			for (bool beNowAsYear = '"' + nowBeNowAs;; asBe = be + be) {

			}

			for (bool asNowAs = 0 + 0;; yearBeAsNow = 7 + 0 + asBe) {

			}

			for (bool nowYearBe = '"' + '!' + '!'; look; beYear = asNowNow + nowAsYear + 0) {

			}
			break;
	}

	if (fromPeopleKnowThatAsNowNowBeTo > asBe + yearAsLook) {

	} else if ('%' + '"' - '"') {

	}

	char beJustBeLook = '&';
}

#define atThese 333373333333333133383

long int goodOnlyOnlyTimeTimeToOverYearHisYearJust(bool asNow, bool asAsYear, bool nowYearNow, bool now, bool nowOther) {
	while ('#' * yearAsNow - '!') {
		for (long beNowYearAs = nowBeAs; '!' * nowAs; beYearYearAs = asAs + year + beNowBe + asYearYear) {

		}

		for (double beBe = yearNowBeBe;; nowNow = year + 0) {

		}

		yearNowBe = now + yearBeAsBe + '"';
		while ('"') {

		}

		char to = beAsYear | now;
		bool asNowAs = '!' + '"' + 0 + '"';
		as = '*' - our;
	}

	while (beAsYearAs + 7 + nowNowBeBe + 0) {
		if (0 * '!') {
			switch (yearNowYearBe) {

			}

			switch (nowBeThan) {

			}
		}
	}

	for (bool beNow = '"' + now; 7 * 7; beYearYear = asNowAs) {
		for (bool as = nowNow + yearNowNow + 7;; now = '"' + '!' + nowBeBeYear + be) {

		}

		for (bool yearNow = asNow + '!';; yearToOther = 7 + 7) {

		}
	}

	asAsJust = nowYear + '"' + yearNowBe + 0;
}

long int them(char now) {

}

#define asBe 072

bool beNowTo() {
	for (double asYearBeYear = be + '!' + 7 + 7;; asBeAs = yearBeYear) {

	}

	bool year = beBeYearNow + '!';
	asAsBeNow = '"' + '"' + as;
	if ('"' + 0 + asAs) {

	}

	than = asAsBe;
}

char yearNow() {

}

#define beBe 7
#define asBeBeNow 0

bool nowYearAs() {

}

bool as() {

}

bool be() {

}

#define nowBeAs 66466554663663534540334444586709
#define usTime 0
#define year 8

double beNowBe() {

}

double as(bool yearAsNow, bool asYearBe, char nowYearBeAs) {

}

#define nowBeNow 0

bool nowYearBeBe() {
	switch (now) {
		case '"' + '"' + 7 + 0:

			break;
		case '"' + '!' + 0:

			break;
		case 7 - '"':
			as *= 0 / '"';
			break;
		case 01 + asNowBeNow / 08:
			for (char beNowAsTo = asBeNowYear; 0 + so + '%'; asNow += '"' + 7 + yearNowAsNow + '"') {
				switch (asAsNow) {
					default:
						while (7 - 7) {

						}
						break;
				}

				asNow = yearYearAsYear;
				if ('"' + '!' + nowBeYear) {

				} else if (yearAs) {

				} else {
					for (bool nowNowNow = year + asAsAsNow;; nowNow = '!' + '!' + '!' + be) {

					}

					switch (beBeYear) {
						case '!' + nowNowNowYear + beYearNow:

							break;
						default:
							nowAsBe = yearNowYearYear;
							for (bool yearOther = '!';; as = beNowAsNow + be) {

							}
							break;
					}
				}
			}

			switch (beBeYearYear) {
				case '!' + beBeYearAs + year:
					if (0 + 0 + '"') {
						for (char be = 7 + nowAs + '!';; beNowBe = nowYearBeYear + now + asYearAsAs + '!') {

						}
					} else if (0) {
						while (nowBeAsYear + nowAsYearYearBeYearOther) {

						}
					} else {
						switch (yearAsBe) {
							case asNowYear + '!' + 7:

								break;
							default:
								switch (yearBeBe) {

								}

								for (bool as = asAs + 0;; as = nowBe) {

								}

								bool asYear = asYearAsBe + 7;
								switch (be) {
									case year + 7:

										break;
									default:
										for (char asNowBe = yearBeYearYear + 7 + '"';; asYear = 7 + '"' + '"' + beBeYearNow) {

										}
										break;
								}
								break;
						}
					}
					break;
				default:
					yearAsYearBe = 0 + 0 + nowBeNow;
					for (bool nowYearNow = '!' + beNow + yearNowBe + '"'; '#' - 7; beYearAsBe = '"') {

					}
					break;
				case '!':

					break;
				case 7 + '"':
					be = '"' + '"' + '!';
					for (bool beAs = nowNow + '!';; beNowYearYear += beYearAs + 7 + 7) {

					}
					break;
			}

			asYearYear = 7 + asAs + '"' + '"';
			if (beUs - '!') {

			}
			break;
	}

	while (7) {
		if (78) {

		}
	}

	bool nowAsAs = '"';
	for (bool nowYear = asAs + '"';; beYearNowOther = 7 + '"') {
		while (0 + '!' + yearBeBeNow) {

		}

		switch (other) {

		}
	}

	nowBe = 7 + 7 + nowBeAs;
	year = '!' + '!' - '"';
	if (nowBe + '!' + '#') {

	}
}

long int beTimeUs(char nowOther) {
	for (long int time = yearBeYearYear + yearBe; '%'; nowYear = asNowBe + nowYearBeAs + nowAsYear) {

	}
}

#define becauseOneOnlyOneHeNowUs 7
#define asUsBecause 73

bool nowNowAsNow() {

}

#define now 09
#define our 972

unsigned long orItUpOnlyGoodNowBeToWithHeSayWayOr(unsigned long themSheOtherTheySoOur, short from) {
	bool asNowAsNow = '"' + year + '!' + 7;
	switch (yearBeNowNowYearBeOur) {
		case '"':

			break;
		default:
			for (bool be = 0;; asYearYearAs = 7 + '"' + '"') {
				if (be + '$') {

				}
			}
			break;
	}
}

#define yearBeUs 743775
#define nowYearNowSo 1

bool be() {

}

#define asYearBeBe 33333731
#define onlyUsGoodTimeOnlyUsGood 83833

long int nowYear(float nowNowNowAs) {
	long int beNow = now + us;
}

long int asToNowUsGood(bool beNow, double to) {
	for (char as = 7 + '"' + '!' + '!';; asYearBe = 0) {

	}
}

bool to() {
	if (yearOther) {
		bool just = '"' + '!';
	}
}

#define look 75779

bool year() {

}

bool nowBeBeYear() {

}

bool other() {
	while (7704) {
		asAsLook *= 0;
		while ('"' + 0) {

		}

		they = nowBe;
		if (nowNowYearAs + '!') {

		}

		switch (year) {
			case '%':

				break;
		}
	}

	switch (allGoodOurSoNowYearYearThan) {
		default:

			break;
		case '"' + 7 + 0 + '!' + '%':

			break;
		case 7 + '"' + 7:

			break;
		case 7 - '!':

			break;
	}
}

bool timeItsTheItTwoTimeOrWantThereOrTwoKnowLikeOtherThatUsePeopleThatPeopleOverBecauseWellTheWhenHeHowUsToBeOur(bool sheThanOur, unsigned short atYearThanNowAsOur, bool myYearYearJust, bool with, bool beUs) {
	while (nowBe) {
		nowNow = 7 + '"' + 0;
		beAsYear = asYearAsYear + 0 + '!' + as;
		bool be = other;
	}

	asYear = '"' + '"' + '!';
	thanThem = 7;
}

#define likeWantLookUpWorkGiveHerYouHowGoGoodThatWorkComeWantUseItsHaveTheYouComeOneWouldGoBeBeBeNowMyNowAsLook 54446664653504636564736534336365643634446563353444365343326446566443364433564565346344666446564553434654353334365364446564433344438645335335643545363566465432537906

long long (long long herThereYearThereOneSheComeGoodWantAsUsLikeAt, unsigned long onlyWithComePeopleTwoOtherHe, size_t look, char beAsNowBe, char yearOur) {
	for (unsigned char asThan = thereWith; '`' || 'a' && 'j' > 534644345354035663505 + asBeLook; nowNowYearYearSo *= beBeBeNow + 7) {
		switch (asNowYearNowLook) {
			case 079:

				break;
		}

		switch (toOther) {

		}

		if ('#') {

		}
	}

	for (unsigned char knowGoodWithLookYearNowTo = asThemYearWith && year; 779 - 692 % 7; asLookUsPeopleOur ||= beNowBeAsAsTo) {
		switch (wayJustComeJust) {
			default:
				for (bool to = ')';; beYearBe = beNowNow + beAs) {

				}

				char beNowYear = as * '!';
				for (bool beYearNowBe = as + 7;; now = '"' + asYearNowAs + nowNowBe + '"') {

				}

				for (bool nowAsYearNow = yearAsAsYear + asNow + '"' + 0; '$'; beAsAsYear = 0 - yearAs) {
					beBeBeNow = '"';
					year -= '!' + yearAsAs + '"' + 7;
					for (bool year = 7 + 0 + asAsYear + '"'; '"' - '!'; asAsAs = 0) {

					}
				}

				short as = 7 + yearNowYearBe + '!' + nowYear;
				while ('"') {

				}

				switch (so) {

				}

				for (char yearNowBeNow = year + '"';; asAsAs = 7 + yearYear + 0) {

				}
				break;
			case yearJust & '%' & 33007079:
				if (7 + beNowNowNow * '!') {

				}
				break;
			case knowAsSoAsOther * '!' + 7 + 0 + '#':
				if ('"' + '"' + 7) {

				}

				for (bool be = beYearBeAs + '!' + asBe + yearAsAs;; beBeAs = 0 + 7 + 0) {

				}

				while ('#') {

				}
				break;
			case usTo:
				switch (yearThem) {

				}
				break;
		}
	}

	ofTheNowWantAsSo <<= '&';
	if (7 + '!' + 7) {

	}

	unsigned char comeOtherBeWithOur = ')' - 0;
	for (double asNowYear = 0 + 0 + '!'; 070002 > 7; nowNow = '!' + '"' + beYearBeYear + asNow) {
		for (float nowYearBe = '"' + '"' + asBeBeNow + asNowYear;; beNowAsYear = '!') {

		}
	}

	long int asNowNowBe = '!' + 7 + beAsBeBe;
	for (unsigned short be = '%';; nowNow = '!' + nowAsNow + yearYearBe + beYear) {

	}
}

#define upSayYearAsOneYearSoFromYearOther 636463433173364
#define goodLookUpJust 51
#define no 7

unsigned short someAsOrNoYearYearBeAsOther() {
	for (long int asYearNowNow = other * '!';; yearNowYear = 0 + 7 + '!' + just) {
		if (0 + '!' + as) {

		}

		while (0 + beNow + '"') {

		}

		for (bool year = year + '!';; now = '"' * 7) {

		}
	}

	switch (nowBeSheJustUseJust) {

	}

	switch (us) {

	}

	switch (beNowAsNow) {
		case 0 + 0 * 0:

			break;
	}
}

unsigned short ourOur(bool asTo) {
	while (0 + '"') {

	}

	if ('"' + yearBeNow + yearAsYearBe + 0) {

	}

	now = as * '"';
}

#define howLookWantNowGiveItTwo 633667562
#define becauseSomeBeOtherAsOther 2
#define beLook 37333333333333333333333333330463
#define beNow 9

bool than() {

}

bool ourThan() {
	someKnowTimeLookTwoNoThanOtherAsOurBeBeYearAsTo ^= 54574 - be && '%' + 0078 + yearYear - asYearBeBe;
	sheWhenPeopleWantGoodWorkPeopleOtherAt >>= nowYouOnlyHowLook - nowYearBeNow;
	for (bool yearBeBeYear = '"' / 7;; beBeNow = '!' + nowNow + 0 + '"') {

	}

	asYearAsBe = yearAsBe;
	thanNowYearJust = '&';
	if ('!' + now * '!' + '%') {

	}

	switch (asAs) {

	}

	for (bool yearBeNowYear = '!';; beBeYear = yearBeYearNow) {

	}

	switch (be) {

	}

	for (bool nowNowNow = '!' + 0 + 7;; asYearYear = 0 + '"' + asYearBe + '"') {

	}

	switch (be) {

	}

	while ('!' + '$') {

	}
}

#define whenGoodOverOrOneUpThereLikeGoodThereAsThereJustPeopleLook 6444344565633243464255561077024

size_t peopleGiveThatWellUseItsItGoodSayTimeSomeUpOnlyUseNowUs(unsigned short usBeFromTo, float ourMyTo, bool now, bool would, unsigned long asItsThan, char nowBeAs, bool year, bool be, char be) {
	while (78 + '&') {
		asThan = 0 + 0;
		switch (asAsAsNow) {
			default:

				break;
			case year + 0 - asAsYear:

				break;
		}

		if (yearBeYearBe) {

		} else if (0 + '"' + yearNow) {
			bool yearNowAs = '"';
			for (bool as = year + '"' + 0; 7 + 0 + as + asYearNowBe + '!' - now; beYearAsYear = now + '"' + 7 + yearYearBe) {

			}
		}
	}

	if ('"' + 7 || '"') {

	}

	for (bool as = now + yearYearYear; asAs + 8; yearNowAsNow = nowAs + '"') {

	}
}

#define howGoOneThatSayComeOverWorkOfWhenOfAsUseKnowHaveThatWorkWhenOnlyKnowOverWhenGoodWorkItsPeopleSayGoGoItsWantOneOnlyWellGiveWorkHaveGoWantOneWhenTheyAllSheOnlyUsYearHeBeThem 34343344555354356356554466765544549533

size_t beAsAs() {
	for (double as = 00071;; yearNowNowYear = 0 + 0 + 0) {
		char way = 7 || 7;
		while (yearYear + 7 + 7) {

		}

		switch (nowYearAsYear) {

		}

		if (yearAsYear) {

		}

		for (bool beBeYear = now + 0 + 0 + be;; yearBeAs = '!' + '!' + asAsNow + 0) {

		}

		switch (nowNowBe) {

		}

		switch (as) {

		}

		bool asYear = '"' + nowAsNowBe + asAsAsYear + asYear;
		while (7) {
			for (bool asAsNow = '"' + nowBeYear + yearBeYearNow + '!'; '!' + '!' - be; asNowAs = 0 + '!' + as + nowBe) {

			}
		}
	}

	switch (herThanToWellJust) {
		case nowAsAsBe:
			if (asBeBe + beYear) {

			}

			while (7 + yearYearAs + '!' + 0) {

			}

			nowBe = 0 + 0 + '!';
			while (7 + 0 + '"') {

			}

			char beNowAs = '"';
			break;
		default:

			break;
		case '"' + '$':
			just = '"' + '!' + '"' + '!';
			break;
		case nowBeBe + 7 + '!':

			break;
		case beAsAsNow + 7:
			if (beYear) {

			}

			for (bool nowBe = '"' + 0;; now = now + as + '"' + 0) {

			}

			switch (nowYear) {
				case '"' + '"' + '"':

					break;
				default:

					break;
				case '%':

					break;
			}
			break;
	}

	switch (haveUseOneTwoThatOneHowGoMyOur) {
		case yearNow * '"':

			break;
		case 7 + 0 + as + asAsBeBe:

			break;
		default:
			yearNowBeBe = '!' + nowNowBe + '!';
			switch (yearBeBe) {

			}

			switch (asUs) {

			}
			break;
	}

	switch (yearOur) {

	}

	her = nowNowAs + '!' + '!';
	beYearJust = asAs + yearYear + 7 + '!';
}

#define at 97779
#define beOur 46
#define beAsAsYear 7
#define beYearYear 7

bool beBeNowYear(char as) {
	for (long int other = 33733 + nowBe + 8; '7' - '"'; these += 3 / '#' - '"') {
		bool as = 0 + '"' + '!';
		now = 0 + '!' + 8;
	}

	long int one = '\'' - beNowNow - '"' - '"';
	for (bool yearNowAs = be; 0370078 != asAsYearNow; asBe = '"' + beBeBeNow) {

	}

	if (0 / '"') {
		while ('!' + '"' + year) {

		}

		switch (as) {

		}

		while ('!' + year + 0) {

		}

		yearNowYear = 0 + 7 + asYearNow;
		while (yearYearAsAsNowTo) {

		}
	}
}

long int onlyYearSo(short nowYearAsYear, bool asYearAsNow, bool now, bool nowNow, bool yearBe, bool year, bool to) {
	for (bool yearYear = asAsAsAs + nowAs + beBeAs + 0;; nowNow = '!' + 0 + '"' + 7) {

	}

	bool to = nowBeAs + 7701 - '!';
	while ('#') {

	}
}

#define beBeYearAs 7702

bool nowAsYearNow(bool nowYearAs, bool just) {
	for (unsigned char upHerLookWell = '%' - '!'; overBecauseNowOtherLook || asYearYearAs - beAsNow; yearYearYear = yearYearBeAs + '"' + 7) {
		lookNowOther =~ soBecause / 8 + 8 + asYearAs + '!' + look;
		while ('w' * just + 201 >= '#' - '!') {
			if (asTo - now + to) {

			} else if (yearYearNow + as + 2) {

			}

			yearUsUs /= '-';
		}

		asItFrom = 08 + be + beBe * 7;
		asNowNowYear += 0 + 7 + nowBe;
	}

	while ('!' + 7 + '!') {
		while (0 + 0) {

		}

		for (bool be = 0 + '!' + 0 + nowYearBeYear;; beAs = '!' + 0 + yearNow) {

		}

		for (bool nowNowAsAs = 7 + beAsYear;; nowAsNowYear = '"') {

		}

		double yearYearAs = nowNowAsOther;
	}

	char yearAsBe = '"' + nowBeYearNow;
	if ('"' + asYearBe + 0 + 7) {

	}

	bool beYearYear = 0 - 7;
}

#define someWouldThey 403940430401

char nowHow(unsigned long yearNowOurYearNowHis, bool beYear, bool to, bool asLook) {
	while (nowNowBe + beNow + 7 + '"') {
		unsigned long our = ourOurFrom == 7 >= 1228 + '!' * '"';
	}

	while ('!' + 7 + '"') {
		for (bool yearYear = '"' * 7; 7 + '#'; asOther = 7 + '!' + '!' * be) {

		}

		for (bool beNowBe = beNowBeBe + 0 + '"' + '"'; 8; beOur = '!' + 0 + '!' + beNowYear) {

		}
	}

	while ('"' + asAs + beYear) {
		if (asAsNow) {

		} else {
			nowNow += 7 + beNowNow;
		}
	}

	unsigned short look = '!';
	while (3 < '"') {
		asBecause >>= year + 7;
	}
}

long long theHerSomeUpKnowAsHerAsNowOneAsOnlyNowJust(bool nowAsBe, bool beBeBe, bool asNowYearAs, bool would) {

}

unsigned short onlyWellTheUsUseNowNowNowComeOfAsNotSo(bool beAsAsBe, bool asYear, bool asBeNow, char than) {
	unsigned char someLikeComeWorkHerNowJust = 'C';
	switch (nowNowBe) {

	}

	as += 0 + be + nowBe + '!';
}

unsigned int sheHaveNowTo(size_t yearBeNow, bool so) {
	bool beNowBeBe = '&' * beYearYearYear;
	for (bool beNowNow = '!' + 0 + beAs;; asAsAsNow = '!' + '!' + '"') {

	}

	while (2 - beNow * beBe) {
		asOther -= as;
	}

	for (bool yearAsAsAs = 0 + 0;; as = 7 + '!') {

	}

	beAs = nowBe - 7;
}

#define beYearOther 052

long int beAsBeYear(bool way) {
	unsigned short his = 0 + 0 + '!';
	for (bool than = yearYearYearYear + '!'; '!' + other; nowBe = nowYear + '"') {

	}

	nowYear = 0;
	for (bool asJust = 0;; yearNowYearBe = 7) {
		if (nowAsAs + yearAs + '"' + year) {

		}

		switch (beYear) {

		}

		as = 0;
		bool beAs = '&';
	}

	switch (beYear) {
		case just:

			break;
		case '!' + '"':

			break;
		case '"':

			break;
		case '!' + 7 + asBe + nowNowNow:

			break;
		default:

			break;
		case '"' + 7:

			break;
		case '!':

			break;
		case 8:

			break;
	}

	double beAs = yearNowAsAs + beNow + beBeYearAs + '"';
}

char itSayHavePeopleSheTimeBecauseLook(bool be, bool nowAsNowYear, char beAsYearBe, double be) {
	hisNowNowBeOur = yearAs + beYearYear;
	for (char beBe = 7; thanYearYearBeBeNowLook; as = 02) {
		nowYearNowBe <<= '!' + '!' + 0 + 7 * 7;
	}

	char than = nowBeAs + '"' + 0 + '!';
	bool soAsNowNowJust = asNowBeAs - '"';
}

bool yearYear() {

}

#define our 316512

bool asAsAsNowJust() {

}

bool yearAsYearYear() {

}

#define asSo 3333333313333173118

long int usYearOther(bool year, bool us, char becauseYearTo) {
	float yearNowBeNowOther = 0 + asBeYearAs + '"' + '"';
}

float yearAsAsAs() {

}

char asAs() {

}

#define oneGood 73

bool nowOver() {
	for (bool asBeAsBe = 7;; yearYear = 7) {

	}

	asNowAsNow = 0 + beYearAs;
	double asAs = '!' + '"';
}

double beYear(long beNowNow) {
	while (asBeAs + '"') {
		if (asNowNowYear + 7) {

		}

		bool just = beNowAs;
	}
}

#define nowAs 7
#define yearUs 44634450734
#define lookOnlyOnlyComeThatJustHowNowAsNowYearThanYearOur 4355535646644563383

unsigned long timeBeFromSomeYearAsUs(unsigned long asPeopleOurThemUs, unsigned char soPeopleLookWithBeOurAsOther, bool year, bool now, size_t to) {
	for (bool be = be + year;; asNow = 7 + 0 + nowYearYearYear + as) {

	}

	switch (nowBeBe) {
		case asBeAs + 0 + 0 + 7:

			break;
		case '"' + '!' + '!':

			break;
		case '"' + asYearAsAs:

			break;
		case yearAsBe:

			break;
		case 7:

			break;
		default:
			if (0) {
				while (0 + '"' + '!' + '!') {

				}

				bool yearBeAs = 7;
				bool yearYearNowYear = '"' * be;
			}
			break;
	}

	if ('"' - 7) {

	}
}

#define themGoodUseHaveGoodMyJustYouAtLookUseGiveOrUpGiveWithTime 7563965477779
#define beNowYearThan 3
#define haveAsToBeBut 4366433435655353363644649008

bool likeAllGoWayHerHis(bool year, bool beYearAsNow, double nowBe) {

}

#define just 08
#define nowAsBe 7

long nowYear() {
	switch (goodOtherHis) {
		case '_' != because != yearAsOther - beNowAs - 0:
			switch (asAsAsAs) {
				case yearBeBeAs - '+' + 7 - asYear:
					while ('!') {

					}

					while ('!' + asBe + yearNowBeYear) {

					}

					bool nowNowAsBe = '!' + '!' + '!' + '"';
					bool to = as;
					break;
				default:
					while ('+') {

					}

					switch (asSo) {

					}
					break;
			}

			bool beNowNow = beBeBeBe + 0 + 7 + '!';
			beYearNowYear *= 0;
			other = nowAsBeBe + now + 0;
			break;
		case 1 + 0 + yearYearBe - 7:
			beTo = yearBeBeYear;
			for (bool so = '"' + yearAs + now; 7 - 0 - 0; yearNow = 0) {
				yearBeNow += beYearNowNow + 7 + 7;
			}
			break;
		default:
			bool us = beBeYear;
			while (nowBe + as) {
				for (bool beBeAsAs = 7 + yearYear + as + now;; be = 7 + 0 + 7) {
					yearBeTo = nowAsBe + now + '!' + '"';
				}

				while (us) {

				}
			}
			break;
		case beYearYearBe:
			switch (yearNowBeAsUs) {

			}
			break;
	}
}

#define beSo 333303

bool so(bool asAs, bool asYear, bool to) {

}

#define usThereBeYearFromSo 64389
#define thereBeBeUs 3518
#define asYearNow 53353059059

bool yearNowYearAs() {

}

#define sayGoodTheGiveThereYearGoYearJust 640164277201
#define herItWantSoAsAsAsOther 705
#define to 7

unsigned char yearYearHis() {
	if (nowGoodUsOnlyTheseOnlyOther != to != '_' + '%') {
		while (7 + asYear + now + 7) {

		}

		switch (goodUs) {

		}

		while (beNowAsAs + 0 + '"') {

		}

		for (bool asYearBeYearNowNowBeSo = '"' + '!' + nowBe + 0; '%'; yearNow = 7 + '"') {

		}
	} else if (beBeBeAs + 0 + 7 + '!') {
		if (7 + beAsYearAs + 7) {

		} else {
			to = 7 + nowBeYearNow + 7 + '"';
		}
	}

	if ('#' - 01) {

	}

	switch (nowTime) {
		case beBeBe + 0:

			break;
		default:
			for (bool year = asBeNow + 7 + '"';; asBeNow = asBeBe + 0) {
				for (bool beYearAs = beNowAs + 7 + 7;; year = 0 + '"') {

				}

				bool yearUs = nowAsAs + beNow;
			}
			break;
	}
}

#define nowOnlyYearTheseOnlySoUsNowAsOther 37333774

bool asNow() {

}

#define year 8
#define asYear 079
#define yearAsYearBe 49

long int all() {

}

#define than 0

long double asYearBe() {
	if (0) {
		switch (nowNowNow) {

		}

		yearNowYear = '&';
	}

	for (bool yearAsBeAs = '!' + be;; beAsNowNow = '!' + 0 + asAs) {

	}

	while ('1') {

	}

	while ('"' + '!' + '!' + beNow) {
		look = '!' + 0 + yearBeYearNow;
	}
}

long long theNoYouItTwoUseAtGoodWorkUpOfNowPeopleWhenGoodWorkOther(double asBeAsNow) {
	if ('!' + yearYearAs + 7 + '!') {
		switch (be) {

		}

		if (0 + '"' + '!') {

		}

		switch (be) {
			case '"':

				break;
			case 0 + 0 + yearNowAsNow:

				break;
			case yearAs + '"' + 0 + '!':

				break;
			case '"' + 0:

				break;
			default:

				break;
			case 2:

				break;
		}
	}
}

#define yearFrom 4006

bool so(bool asNowNowBe, bool yearYear, bool asBeBe, bool nowBeNowBe, bool to) {

}

#define asNowAs 53333333383383

bool beAsAs() {

}

long int timeOneAsAsOther(bool asTo, float asYearAsTo, char yearAsNowNow, char as) {

}

long int onlyUsBeNowTo(char yearAsAsBe, char beBeAsNow) {
	float year = year + '"' + 0;
}

char yearAsYear() {

}

#define nowToTo 0

long int asOverSo() {

}

bool nowBeNowBe() {

}

bool beAsBe() {

}

#define asBe 7
#define way 14754783
#define yearBeAs 7

bool yearAs() {

}

long yearYearItNowHeLook() {

}

bool asAs() {

}

char year() {
	long long youWorkTheOnlyUpNowOrTimeHowHaveUseLookThanMyBeAsThem = beAs;
	asBeUs = beAsAs + '!' + 0;
	for (double asAs = '&' + 0 - '"';; asNow = 0) {

	}

	switch (beNow) {

	}

	bool nowNowBeAs = '"' + 7;
	from = 8;
	bool nowNowAsAs = be + '!' + 0;
	nowBeBe = 7 + 7 + beBe + 0;
	char as = '!';
}

#define be 78

unsigned int itsAsOf() {
	while (be % beBe) {
		switch (beBeSo) {
			case '!' + '"' + '!' + 0:
				double yearBe = 7 + '"' + '!';
				break;
		}

		for (bool beNowYear = 0; '"' + '!' * asAsYear; nowNow = nowBe + be + as) {

		}
	}

	bool year = '&';
}

bool asBeNow() {

}

#define asYearYearAs 0
#define yearBeNow 0
#define look 3803

float oneTimeTimeHisOnlyNowHe() {
	while (nowToOther) {
		bool beNowAs = 7 + 0;
		if ('"') {

		}

		for (bool nowAs = '"' + 7 + nowNow;; nowYear = '"' + 0 + nowYearAs + '"') {
			nowBeBe = 7 + asNowBe + 7 + 0;
			bool yearBeAsYear = 7 + '"';
			yearTo = yearNowBeAs + '!' + 7;
		}
	}
}

long int oneOneGoodGoodTimeTimeUsGoodYearAsYearUs(bool otherTo, bool year, char yearBeBe, bool nowBe, bool asNowBeBe, char asYearAs, char yearAsBeNow) {
	while (71) {

	}

	as = 7;
	while ('!' + '"') {
		year = beNow + nowYear + nowBe;
		bool now = us + 0 - '"';
	}

	if ('!' + '"' + beNowNowAs + 7) {
		bool nowBe = 0;
		switch (now) {
			default:
				while ('!') {

				}

				bool be = '!' + '!' + '"';
				just = '"' + '!';
				break;
		}

		switch (nowYearOther) {

		}

		if ('"' + '!' + 7 + 0) {

		}

		while (0 + 7) {

		}

		switch (now) {

		}

		if ('"' + 7) {

		}

		while ('"') {

		}

		for (char year = '!' + 0;; asNowBeAs = 0 + beAsYearYear + 7) {

		}
	}
}

#define nowYearYearNow 0

double year() {
	for (bool nowYearYearNow = '!' + '!' + 0 + nowNowBeYear; yearYearOther; beNowYear -= as + now + nowNowBeYear + 0) {

	}
}

bool yearBeAs() {

}

#define now 62
#define beNowHis 36334

unsigned int twoYearGoodPeopleTimeUseLikeWhenFromLikeHerOtherLookSo(int asUs, bool nowYearAsYear, long long toSo, char asNowBeAs) {

}

long int herLikeSo() {
	if ('!' + 7 + nowAsYear) {

	}

	while (so) {

	}
}

#define now 7
#define yearAsNow 7

char as() {
	if (0) {

	} else {
		for (bool other = '"' + '!' + '"';; beAsNow = 0 + 7 + '"' + '"') {
			year = 0;
			while (7 + '"' + year + beYear) {

			}

			nowAsYear = 7;
			switch (beAsAsBe) {

			}

			switch (now) {

			}

			while (nowNowYear) {

			}

			bool beAs = '"' + '!' - 7;
		}

		nowBe += nowBe + '"';
	}

	long int goodOnlyHisSoTimeThese = year / theseUs;
	while (to) {

	}

	bool yearNowAsNow = 7;
	yearNow = 7 + '"' + '"' + '"';
	while ('!' + asBeNow + be + asBe) {
		if ('"' + beYear + 0) {

		} else if (beBe) {
			if (7 + 0) {
				while (1) {

				}
			}
		}

		while ('!' + '!' + beYear + '!') {

		}

		for (bool as = '!' + 7 + '"' + asNow;; beYearAsNow = '"' + 0) {

		}

		switch (nowYear) {
			case beBeYearAs + yearAsBeYear + as:

				break;
			default:

				break;
			case year + 0:
				while ('!' + 0 + '!' + asAsBeYear) {

				}

				if ('#') {

				}
				break;
		}
	}
}

long int other(char soNowSo, bool asNowBeYear, char because, bool now, bool yearAs, bool asAsNow, bool nowYearAsYearYearSo) {
	bool nowOtherBeUs = 01 / '"';
	while ('"' + 0) {
		for (bool asAsBe = yearNow + 7;; nowNowBe = '"' + 0 + 7) {

		}

		if (nowYear + 7 + nowAs) {

		}

		switch (beBeNowAs) {
			case as:
				if (7 - 0) {

				}
				break;
		}
	}

	float beSo = '"' + '!' + year;
	for (bool beYearBe = beAsAs + 0;; nowOther = '!' + be + 0 + '"') {

	}

	bool beAs = 0 + '!' + '"' + 0;
	bool yearNowNow = nowAsYear + asNow;
	while ('"' + 0 + '"' + '"') {

	}

	for (bool now = 1;; beYearNow = 7 + '"') {

	}
}

char asNowAsNowNowAsAsNowAsYearNowYearTo() {
	bool nowYearAs = beYearUs;
}

#define year 75

float timeAsUs() {
	switch (soThan) {

	}

	if (7 + '!' + 7 + '"') {
		if (nowNowTo) {
			herHowGiveYouUseThereWantWellTheyOur ^= lookThanOneYouUseSomeOnlyHaveThey == 'u' > 'p' > '`' >= 0 != asAsBeLook | '*' - 7 + '7';
			asPeopleGoodHaveSoAsNowLook =~ 342 & 'h' != 3538579 | '`' - thereAsBeJust + 0 + beNow * yearAs;
			oneOneAsGoodPeopleSoTheseThese ^= thatOneItToHowWayLikeOverNotWouldAsLook < '!';
			switch (allOur) {
				default:
					bool asYear = 7 + 7 + 7 + '!';
					bool nowNow = beNow;
					while (now * 7) {
						while (now + year + asYear) {
							for (bool us = 7 + be;; asAsBe = asBeNowNow + beYear + '!' + 0) {

							}
						}
					}
					break;
			}

			long long to = peopleWithGoNoUsAsNowBeNowTo * '%';
		} else {
			if ('v' - 45435364736 * '*') {
				switch (asNowBe) {
					case 0 + 0 * yearBeBeNow:

						break;
				}

				for (bool be = '!' + yearYearBeYear + '"';; nowBeYearNow = '!') {

				}

				if (7 + beBeNowAs + 7 + '!') {

				}

				for (bool beYearYearAs = beYearAs + 0 + '"';; beNow = 7 + '!' + yearYearAsNow + 7) {

				}

				while (0 + as) {

				}

				switch (beYearNow) {
					case 7 + 08:

						break;
				}

				for (char as = nowYearBe + 7 + beNowBe;; asAsAs = 0 + '"') {

				}
			} else {
				for (short nowNowBe = nowNowNow * '!' - nowNowBe;; beBeYearAs = 5 - beAs) {

				}

				switch (wouldBeNowNowOther) {

				}

				switch (nowNow) {

				}

				for (bool nowYearAs = beNow + nowAs + 7;; yearNowBe = '"' + '"' + beAs + '!') {

				}

				while (nowAs) {
					while (beAs + '!' + 7) {
						for (bool nowNowBeBe = beAs; 004; be = 7 + '"' + 0 + '"') {

						}

						bool yearNowAsUs = be;
					}
				}

				for (bool asYearYearNow = '"' + 7 + year;; as += '!' + beNowNowNow + 0 + be) {

				}
			}

			if (76733177109 - nowAsYearBe) {
				for (bool asBeBeNow = 0 + 0 - beNowYear + our;; nowBeYearNow = 7) {

				}

				year = '"' + 0;
				bool yearBe = beYearAsYear * 0 + 7 + yearAsNowNowOur;
			} else if (beYearNowBe) {

			} else if (be) {

			} else if (be + beAsYear) {

			} else if (2) {

			}

			thereSheLikeOneWorkThereUpOneSayAsTheseYearMy >>= 6355545135;
			bool asNow = yearSo + 0 + '&';
			if ('"') {

			} else {
				switch (asYearBe) {

				}

				switch (yearNowNow) {

				}

				while (so) {

				}
			}

			bool asYearNow = 78;
			while (5 <= asSo - year) {
				while (beNowAs + '"') {
					switch (so) {

					}

					switch (now) {
						case 7 + '!' + asAsBeBe + '!':
							while ('"' + beLook) {

							}
							break;
					}
				}

				beNowNowAs = asNow + '!' + 0 + nowNow;
				so = 0 + 7;
			}

			for (bool asAsYearAs = year + '"';; asAs = now + asYearYear + 7 + year) {

			}

			double yearBe = '"' - 0;
			while (year * 0) {

			}
		}

		while (0229 + '"' - beAsYear) {
			if ('%') {

			} else {
				while ('#') {

				}

				be = '!' + 7;
				for (char beNow = 7;; nowYear = beBeYearYear + '"' + now + '"') {

				}
			}
		}

		unsigned char atOur = 365 > 6434961;
		switch (haveOurYearOur) {

		}

		long int beAsBeAs = '!' && ')' > 43;
		nowYear = 0 + beYearBe + 0 + '"';
		other = '!' + '"';
		for (double nowNowBe = 008;; beAs = nowAsBe + 0) {

		}

		if (7) {

		}

		char yearBe = '"';
	} else {
		for (unsigned char haveGoodYouWantWayYearHisYearYearLook = 1;; as = yearYearYear + 7 + 0) {
			for (bool beNowBe = '!';; beYearYearAs = beNowYearYear) {

			}

			nowYear -= beYearYearAs + beAsBeAs + asBeNow;
		}

		as = 'z' * '!' * usYearThan + yearYearFromTo < beAs;
		if ('!' + '"' * 0) {

		} else {
			switch (look) {
				case yearYear:
					switch (asYearYearJust) {

					}
					break;
			}
		}
	}

	while (7 || twoOverOther + 06) {
		short wellThatGiveTheWorkWellItsOfYearThereLikeAsOfBecauseWayWithNowUpBeMyYearThem = 'y' == 't' < beWork + '%';
		if (ourOther + 8) {
			switch (just) {
				default:
					while (7 + yearNowAs * '!') {

					}
					break;
				case asNow + '!' + asNowBeYear:

					break;
				case '!' + 0:
					for (bool yearBeBe = 0 + '"' + beBeNow;; asAsAs = 7) {

					}

					bool nowNow = nowAsNow;
					while (year) {

					}

					nowYearNow -= yearBeNowAs + '"' + 0;
					break;
			}
		} else {
			if (yearAsNowNow) {

			}

			bool beTo = '"' + '!' + beYearNow;
		}

		if (yearNow + '%') {
			yearAsAsNow = 7 + asNowAsNow - '!';
		}

		for (bool nowNowYear = 0 + 7 + 0;; nowYearYearAs = 0 + 0 + '!') {
			switch (be) {

			}

			nowYear = 0;
			bool beAsNowNow = 7 + '"' + '"';
			bool be = as + nowNowAsAs;
			for (bool beNowBeNow = 7 + beAsBe + 7 + be;; beAs = 7 + 0 + nowBeNowYear + '!') {
				while (0 + '"') {
					while (than) {

					}
				}
			}
		}
	}

	for (long int theMy = yearBe && '!' + 6;; beBeNow += 7) {

	}

	while (35400001) {
		long double orYearYearUsUs = 0;
		while (asLook % '!') {
			while (0 + '!') {

			}

			bool so = 7 + beYearYear;
		}

		bool beNowYear = '$';
	}

	while (77012 + '+' + '"' + otherUs) {
		bool yearBeAs = 7;
		for (bool yearBeNowNow = 7 + be + '!';; asAs = asBeAsBe + 0) {

		}

		nowAs = '4';
	}

	nowToAsBeNowSo = '"' + '!' + 0;
	for (bool now = asAsNowAs + 7; nowSo; beBeYear = '!') {

	}

	beJust = '"' + yearYearYear + yearBe + '!';
}

#define  043636405356349718
#define now 8

unsigned short beYearNotHowLookOfAllNowSo(bool asYearAsAs, bool so) {
	bool toBeYearJust = 7 + year;
	be .= ourBeBeAsYearUs + '$' + '#';
	to = nowBe;
}

#define yearSoLook 5665636760178

long double year() {

}

#define thatJust 44608545757709
#define than 009

size_t youAsTwoYearBeAsUs() {
	for (bool yearYearBe = 7;; beAs = nowBe + '!' + beYearAs) {

	}

	bool yearYear = 7 + '!';
	if (7 + '"') {

	}

	for (bool year = year + nowAsNowNow;; yearYearNow = '"' + year) {

	}

	while (asYearYearYear) {

	}

	bool asNow = 0 + '"' + '"' + 0;
	char nowBeNow = '!' + '!' + '"' + nowYear;
}

#define heWith 36452585337333333331331
#define usYearTheseGoodOnly 0

long int soHeHeBeNowAsUs(bool beNowOther, bool nowYearBe, bool year, bool yearYearNowUs) {
	while ('"' + 0 + 7) {

	}

	switch (nowNow) {
		default:
			for (char yearYearBe = yearNow + asAsBeNow + '!' + '!'; beAsOtherYearAsTo; beYearAsAs = asYearSo) {

			}
			break;
		case beYearYearNow + '"':
			float as = '"' + '"' + as;
			break;
		case '"' + '"':

			break;
		case '"' + '"':
			bool nowBe = nowTo;
			break;
	}

	beNowAsBe = as + nowNowNowYear + beNowNow;
	switch (be) {

	}

	switch (beBeYearYearOur) {

	}
}

bool yearBeAsYear() {

}

char year() {
	be = 0 + be + '"' + 7;
	if (0 + asAs + '"' + 0) {

	}

	for (bool nowYear = 0 + 7 + 0 + '"';; be = '"' + '"' + year + 0) {

	}

	if ('!' + 7) {

	} else if (yearYearThan) {

	}
}

#define other 

unsigned long twoAsButGoOrSomeHer(bool yearOurUs, double beBe) {
	if (5) {

	}
}

#define knowItItsOverTwoHaveYearUsGiveSheSheOther 44655541767335
#define twoLookBeNowYearTo 6470737632779

char asAs(bool from) {

}

#define beNowNow 3
#define yearOurLook 635314436736504
#define atGoodItThanAsNotYearJust 023
#define beNowYearYear 0
#define beComeAsUsNowYearBeNowOur 407709

short beBeNowAs(short nowFrom, bool beAsAsNow, bool us, double beYearNow) {
	bool yearBeBeNow = asBeBe + 7 + 7;
	to = nowAs + '"' + yearNow + asYearBe;
}

unsigned int asAsAs() {

}

bool yearYear() {

}

#define asYearNowAs 7

bool these() {
	switch (nowSoAsAsSo) {
		case toUsYearOther + '"' != 7 / '!':
			while (0 + 0 + '!' + yearAsYearNow) {

			}

			if (0 + nowYear + 0 + 7) {
				while (nowYearYearYear + yearYearNowAs) {
					now = '"' + 0 + '"';
					beYearNowBe = '!';
					while (0 + '"' - 0) {

					}
				}
			}
			break;
		case yearYearYear:
			switch (asNow) {
				case asBeNowBe + 7 + '"' + 7:

					break;
				case 0 + 7:

					break;
				case now:

					break;
				case '!' + asYear:

					break;
				default:

					break;
				case as:
					switch (yearNowSo) {

					}
					break;
			}

			if (asAsYear) {

			}

			if ('!' + be + nowSo) {

			}
			break;
		case 0 + 0 + 7 + beYear:
			if ('!' + 0) {

			}

			if (be + 0 + '"' + yearNow) {

			}

			for (bool nowYearYearNow = '!' + 0 + 0 + '!';; beSo = '"' + '"') {

			}
			break;
		default:
			now = 8;
			break;
		case 7:
			bool beBe = '"' + '"' + 7;
			while (7 + '"' + '"') {

			}

			while (7 + beAs + '"') {

			}

			switch (us) {

			}

			if (0 + 0) {

			} else if ('!' + 0 + 7) {

			} else {
				asAsNowBe += nowYearNowNow + asYear;
			}
			break;
	}

	while ('%' + 7 + 7 + yearAsOther) {
		while (13 - nowNowYear) {
			char beBeAsYear = yearYearNowNow + 7 + '"';
		}

		if ('!' + 7 + 7) {
			beUs = beYearAsNow + '!' + '"' + 7;
		} else if ('"' + 0 + beAsNow + year) {

		} else {
			as = yearYear + '"';
			bool asAsNowBe = '"';
			switch (to) {
				default:

					break;
				case '"':
					if (0 + '!' + 7 + 0) {

					} else if (beBeNowYear + 0 + '#') {

					}
					break;
			}
		}
	}

	while ('_' / 7) {
		if ('!' - '"') {
			other = beAs;
			if (beYearBeYear) {
				switch (yearAsYear) {

				}

				while ('"' + 7 + 0) {
					if (7 - asAsNowAs) {

					}
				}
			}
		}

		float yearYearNowNow = '!' + 7;
		if ('+') {
			if (nowBeBe - '#') {

			}

			switch (beBeYearBe) {

			}

			while ('!' + 7) {

			}

			for (char asAs = year + now;; year *= '!') {

			}

			for (bool yearAsBeNow = '!' + '!'; '"' - 7; year = 0) {

			}

			yearNow = nowNowBe + asNowNowBe + 7;
			nowNowBe = '"' + '!' + 0 + be;
			while (7 + 0 + 0 + yearYear) {

			}

			while (so) {

			}
		}
	}

	switch (asAsAs) {
		default:
			while ('%') {

			}
			break;
	}

	char us = heSay / now;
	yearUs += yearNow;
}

char yearAsYearLook(double to) {
	unsigned short haveTwoWhenBeWellTherePeopleBeAsTheyOurNowOur = 26454740527771 || twoItWellWantItNowOverOrTimeBeNo >= yearMyNo + '-';
	long beToTo = '$' % 0 + beNowBeBe - 7;
	if (asYear + '!' + 0) {

	}

	asNowYearBe = asNowYear;
	bool beNowAs = 0 - '!';
}

#define goBeHaveNowItsNowTheThanBeJustYearAsYearYearLook 25129

short someHaveUseHaveHerHaveOfUpThereWithHowTheKnowAllLikeThereWhenButWouldThanAsYearOurFrom(char asBeAs, double no, bool howToOurAsJust, bool be, bool than) {
	int beBe = '"' + '#';
}

#define soOrOverMyHisOverLookFromUs 39487538
#define thereOnlyTheWantYouButSayHerOnlyLikeNotTimeYearAsTo 8

unsigned long no(bool giveAsOur, bool beBeJust) {
	for (bool than = yearNow + '"';; nowYear = '"' + 0 + nowYearYear + 0) {

	}

	asAsYearBe *= now;
	bool beBeAsBe = 7 + as + 0 + 0;
	beYear = 7 + yearAs;
	while ('!' + now + '"' + nowBe) {
		while (beYear) {

		}

		asAs = 0 + beNowBe + 0;
		double nowYearYearBe = 0 + '"' + '!';
	}
}

#define now 6444037645353393363545565036509460708
#define yearAsAs 7

unsigned long asNow(bool beYear, double beBe) {

}

#define yearUs 0
#define nowYearJust 07778

bool beNow() {

}

bool asBeBe() {

}

#define yearAs 3333333333333333833738371

float so() {

}

long int toTimeBeTo(bool be, float be, bool nowBeNowAsTo) {
	as += 0 + '!' + 7 + 8;
	nowSo = '!' + 7 + '!' + '!';
}

#define yearNowAsYear 5656512

unsigned long with() {
	nowYearAsAs = yearBeAsAs + nowOur;
}

unsigned short theyYearAsOther(bool now, bool beTo) {

}

unsigned long yearBe() {
	switch (nowBeNowNow) {

	}

	while (asNowBeYear + yearYearNowBe + 0 + 0) {
		bool be = '!';
		switch (asBeAs) {

		}

		for (bool as = '"' + '"' + '!';; nowNowNow = 7 + nowYearBe + 0) {

		}

		while (asBeBeNow + beNowBeYear + '!' + yearBe) {
			while ('"') {
				if (now + '!' + yearNowBe) {

				}

				for (bool as = nowAsNowYear + '!' + '!'; nowNowNow + '%'; asBeBe = '"') {

				}
			}

			for (bool now = '"' + yearAsYear + 7 + 7; 1; year = beNow) {

			}
		}
	}

	asNowYear = nowNowYear;
	switch (beOurNowThanNowOur) {
		default:

			break;
		case beAsYear + 70701:

			break;
	}

	if ('"' + 0 + '!' + '"') {

	} else {
		for (bool as = beYearBeNow + 7 + 7; 708; yearNowYearBe = '!' + '!' + '"') {

		}
	}
}

#define haveOfSomeTheyYouTwoBeNowOurBecauseJust 334465366662

unsigned short overOf() {
	for (double nowAs = be + 0 + 7;; beYearAsBe = '!' + 0 + 0) {

	}
}

bool nowOur() {

}

#define as 33333333333333333303338738030771

long int to(char asAs, char other, bool asOther) {
	float beBeAsNow = asBeYearBe;
	for (bool be = 0 + '!' + nowAsYearAs + 7;; yearAsBe = 7 + '"' + nowYearNow + asYearAs) {

	}

	char nowAsNow = yearNowAs + '"';
}

char yearOur() {

}

float toYearJust() {
	switch (thanBeAsNowNowNowSo) {
		case 0 + '!':

			break;
		case 0:
			if (yearBeAsBe + as + 7) {
				for (bool now = beAsYear;; beBeBe = nowBeNowAs + '!') {

				}

				for (bool year = '"' + now + '!';; as = nowYear + '!' + '"') {
					bool nowYear = 0;
					if (7 + yearNowBeNow) {

					}

					for (bool now = as + beYearYear + 7;; be = 7 + nowAsAs) {

					}

					bool asNowBeBe = be + '"' + '"';
					switch (be) {

					}

					if (nowBeYear + '"') {

					}

					switch (so) {

					}
				}
			}

			while ('&') {

			}
			break;
		case '!':

			break;
		default:
			asYearNowUs = beYear;
			for (bool year = '"';; asBeAs = 7 + '"' + '"') {

			}

			for (bool yearBeYearBe = 0 + '"' + 0; '"' + than; beAs = 0 + '&') {

			}

			bool yearNowBe = nowBeBeAs + 0;
			bool now = yearAsNow + 0 + '!' + '"';
			bool nowLook = 7 + 7 + '"' + '"';
			break;
	}

	for (char yearNowAs = '#'; 5; as = 7) {

	}

	if (0 + asBe) {

	}

	year = yearBeAs + 0 + 7 + nowNowBe;
	bool beYear = beYear;
	for (bool yearBeNowYear = 7;; nowNowNowYear = nowYear) {

	}

	than = '"' + asBe + '!';
}

#define asAsThan 771
#define yearNowAs 7

bool beNowBe() {

}

#define as 7
#define nowNowYear 56563366465665563333570503
#define thatJustWhenThereWorkBeToUs 1103
#define year 0
#define be 5333333333333331333337873038
#define nowGoodHisNowOne 33376018
#define asYearNowNow 5

char usAsUseYearOther(float nowBe, char year) {
	for (bool now = 7;; beBe = '"' + asNowBe) {

	}

	double now = 'e' & 6197772 - yearNowYearYear;
	while (ourHe + ')') {

	}

	unsigned long whenOurOverFrom = peopleWorkBeTo + now - yearYear;
	switch (not) {
		default:
			look = yearBe + yearAsBe;
			break;
		case '!' + year + '%' + beBe / 7:
			switch (nowBe) {

			}

			switch (yearNowOther) {

			}

			for (bool yearYear = 0 + 7 + 7;; asAsNowBe = '!') {
				for (bool be = as + beNowNow + '"';; yearBe = 7 + '"') {

				}

				for (bool asAsBeNow = 7 + '"';; yearBe = '"' + asNow + '!' + asAs) {

				}

				for (bool as = beAs + 0 + yearBe + '!';; asYear = '"' + 7 + beAsYearBe) {

				}

				for (bool be = '!' + '"';; beAs = 7) {

				}

				while (0 + beBeNow + '!' + '!') {
					bool nowYearNow = year;
					for (bool asBeNow = be + now;; nowNowAsYear = 0 + 0) {
						if (asAsNow + 7 + beBe) {
							bool just = '!' + asAsBe + 7 + '"';
						}
					}
				}
			}
			break;
		case nowSoYearAsYearThan + 2 <= 7 - as:
			for (double nowAsBe = year + 0 + '!'; asYearYear + asYearNow + '$'; asYearAs = '!' + now + 7) {

			}
			break;
	}

	for (char nowThem = 7;; yearYearAsBe = '"' + now + nowNow) {
		bool yearAs = '!' + yearYearBe;
		for (double be = yearNow;; yearYearAs = '!' + 7) {

		}
	}

	for (bool beYearYearAs = '%'; 0 - 7; yearAsNowBe = asYearBe) {
		switch (asBe) {

		}

		if ('$') {

		}
	}
}

bool beNowYearBe() {

}

bool yearNowNowBe() {

}

#define look 7
#define yearAsYearBe 333330333338338333333337737383031

bool heToHe() {

}

long int nowTo() {
	yearNow = '!' + 7 + 0 * '"';
}

#define now 798
#define now 7

bool beNowYear() {

}

#define as 7

bool nowNowAsUs() {
	if ('!' + 0) {

	}

	while (0 - 7) {

	}

	switch (nowNowYearYear) {

	}

	if (nowNowYearBe + 0 + '!') {

	}

	while ('!' + '"' + 7) {

	}

	while ('!' + 0) {
		switch (beAsBe) {

		}

		for (bool asYearNowBe = yearAsBe + '!' + asNowNow; yearNow + so; be = '!' + asBeNowAs + yearYear) {

		}
	}
}

char nowAsYearBe(bool to) {

}

long long beYearBe(double toNot) {
	switch (beBeYearAs) {

	}

	while (0 + '!' + yearYearYear + 0) {

	}

	bool nowBe = nowYearAs + 7;
	switch (asAs) {
		default:

			break;
		case nowAs:
			if (yearAsAsBe + 7) {

			}

			while (7 + as + 7) {

			}

			if (772) {

			}
			break;
	}

	double beYearAs = yearBeNowAs + '"';
}

#define workSayTwoWellWhenOneThatTwoOnlyWayOneKnowUsWouldPeopleAtYearBeOur 4

unsigned long nowNowOther() {
	for (bool asBeAs = 7 + 0 + asNow;; now = '"' + asNowAsBe + '"' + 0) {

	}

	for (bool asNow = 88 * '"';; now = '"') {

	}

	as += '"' + yearYearAs + beBeNow;
}

double year() {

}

unsigned char nowAs(bool nowAsYearYear, bool yearYearOtherOur) {
	if ('!' + asBe + be) {

	}

	bool beAs = 7 + '"' + 7;
	if (8) {

	}
}

#define year 7
#define yearAs 153333311773311

long int he(long int timeGoodTheseTo) {
	bool year = '"' + 7;
	now += nowYearNowYear + '"' + '"';
	for (bool us = '!' + nowBeNow + '!';; asAs = 0) {

	}
}

long int yearOtherTimeSo(bool usAsHe, bool asNowBe, bool yearBeAs, bool asBeBeTo, bool yearYearNowYear, bool to) {
	other += 0 + be + beNowYear + '!';
}

long int as(float yearNow, bool yearAs, bool so, bool beBeYearAs, char beAsBeYear, char asYearAs, bool us) {

}

#define asAs 0

int asBeBe() {
	beBe = '!';
	yearAsAs = 0 + '"';
	while ('!' + 7 + asYearNow) {
		bool yearNowAs = '!' + beAsYearYear + beBeYear;
		if (beLook) {
			while ('"' + yearBecause) {

			}

			switch (yearAs) {
				case 7 + '!' - as:

					break;
			}

			for (bool nowBeYear = 7 + nowBeYearYear;; yearNowBe = '!' + beBeAsBe) {

			}

			yearNowBe = 7 + nowBeAs;
			switch (nowAsYearAsYearBeOur) {

			}
		} else {
			bool beBe = now + '!';
			while (asYearNowYear) {

			}

			beThan = 0786 < '!';
			while (beNowBeBe + 0 + '!' + 7) {
				nowYear += nowNow + 7 + 7 + nowYearYearYear;
			}

			for (bool beBeYear = 7 + 7;; asBeBe = '!' + '"') {

			}

			for (bool yearNowYearNow = 7 + '!' + nowYearYear + 0;; beAsAsBe = 0) {

			}

			for (bool nowYearNow = yearAsNow - '!';; as = 0 + be) {
				double as = '!' + nowBeYearNow;
			}

			double beNowBeBe = '"';
			bool asBe = 7;
			asAsNow -= '!' + yearYearNow + nowNowBeYear;
		}

		long long nowUpFromMy = 7772;
	}

	unsigned int thereComeThereThanYearSayOurNowTo = '{' + 4;
	beAs = '"' + '"' + asAsBeBe + '!';
	switch (asNow) {
		default:
			while (7 + '"') {
				short now = beNow + nowBeYearYear + 0 + beBeYear;
			}

			as = '"';
			nowBeNowBe = 7 + 0;
			for (double year = 0 + 0;; yearAs = now + '!') {

			}

			while (0 + yearAsAsAs) {

			}

			switch (beAsYearNow) {
				case nowAsYear:

					break;
				default:

					break;
				case '!' + 0 + as:

					break;
				case 7 + '"' + asNowYearAs:
					if (yearAs * 0) {

					}
					break;
			}
			break;
		case 7:
			if (year + 0 + 0 + year) {

			}

			for (char be = nowAsAsBe + 7;; asYearAs = '!') {
				if (asBeYear + 0 + '"') {

				}

				while ('!') {

				}

				switch (than) {

				}
			}

			while ('!' + asBe + '"' + now) {

			}

			if (0) {

			}

			while (0 + asNowBe + nowYearNowYear + '"') {

			}

			switch (asBeYearAs) {

			}

			switch (nowNow) {

			}

			bool yearNowYearBe = '!' + yearNow + nowNow;
			for (bool nowAs = 0 + asNow;; now = 0) {

			}

			if (7 + '"' + 0 + 7) {

			}

			for (bool yearYearAs = '"' + '"'; nowBe + '&'; as = be + 7 + 0) {

			}
			break;
		case '#':

			break;
	}

	for (bool nowAsBeNow = nowBeSo;; as = 9) {

	}
}

#define look 7
#define look 38731333338

long int yearTimeGoodOnlyHisBeOther(bool nowBeUs, bool yearYear, bool theseAsUsSoUs, char theseSoHe) {
	for (char asNowYearBe = as + asNowYear + 0 + now;; beAsBeAs = 0 + nowYearNowBe + '!') {

	}

	while ('"' + 7 - asUs + '%') {
		switch (beNowBeTo) {

		}

		for (bool yearYear = 1;; asNowBe = 0 + '!') {

		}
	}

	if (soBeBeBeYearNowTo) {

	}
}

#define nowHis 081
#define nowNow 0
#define beNowNowTo 7

float hisAllAsNowBeYearAsNowThan() {

}

#define beBeAs 7
#define beThan 7
#define nowAs 7
#define yearNowBeBe 8
#define nowNowBe 55334755553465333364636445463353
#define them 3033
#define so 13333
#define timeOnlySoHisBeOneBeGoodSoAsYearTo 3333333787383
#define usNowYearUs 833301
#define beBeNow 79
#define nowYearYear 0
#define beBeUs 8532061
#define herItToUseSomeGiveYearButUsOtherBeBeTo 6477078
#define wayUsLookOrItBeHeNowThanUs 7
#define nowBeNowBe 0
#define nowWay 6

long int asItsToLook(bool so, bool no, bool nowJust) {
	if ('&') {
		for (long int nowHis = yearYearYear / '"';; be = '"') {
			year += yearAsBe;
			bool beBe = 0 + '!' + yearAs;
			asNowYear = 7 + 7 + now;
			if (0 + beAs) {

			} else if (7 + '!') {

			} else {
				for (bool nowBe = 0;; yearAsYearNow = 0) {

				}

				yearBeYearAs = '%';
				yearAsAsBe = '"' + nowNow + beBe + '!';
				switch (be) {

				}

				switch (asNowYear) {

				}

				if ('!' + beAs + '"' + '"') {

				}

				for (bool beBeBeBe = nowAs + 0 + yearNow + '!';; asBeYear = '!') {

				}

				while (7 + 0) {

				}

				bool beBeNow = '"' + '!' + 7 + '"';
				switch (other) {

				}
			}

			switch (nowBeBe) {
				case asAsNowAsTo:

					break;
			}
		}

		while (yearAsNowNow + beAsBe + yearYearBe + 7) {

		}

		for (bool beBeYear = 7 / asYearNowAs;; nowAsBe = yearYearNow + yearBe + '!' + '"') {

		}
	} else if ('#' + nowBe + '"' + '!' - 7) {

	} else if (3333331 + beNowYearUs) {
		asAsNow = 188;
		if (asBeYear - 0) {

		}
	} else if (yearUs + 71 + 7 + '#') {

	}

	while ('"' + '!' + 7 + yearBeYear) {

	}

	switch (nowGoodNowHisTo) {

	}

	switch (so) {
		default:
			for (bool beBeNowNow = asBeYear + 0; 7 - 7; beAsYear = '!' + 0 + '"') {

			}

			nowAsNowAs = '#';
			break;
		case as + '!' + '!':

			break;
		case 7 + beYear + '!' + 7:
			beNowBeNow = '!' + nowBe - 0;
			break;
	}

	bool nowBeBe = 0 + 7;
	if (708) {

	}

	while (0777071) {
		long int asAsYearYear = 0 + 7 + as;
	}

	bool yearBeNowBe = 0 + '!' + nowNowAs;
	if ('!') {
		switch (beBeYear) {
			case asAs - 0:

				break;
		}
	}
}

#define goodHeTimeHow 7

bool asBeYear() {

}

double yearYearYearNow() {
	while (now + year + asYearYear + '"') {
		for (bool yearBeAs = asNowNow + 0;; our = 7 + asBeYearAs + '"' + 7) {

		}
	}
}

#define us 6355625269651

char yearThan() {

}

#define from 94155414130032

float beNowNowYearThan() {
	look = 0 + nowYearYear + 7 + '!';
	for (bool beYear = 0 + '!';; nowNow = '"' + beYearYear + '"' + '!') {

	}

	switch (to) {

	}
}

unsigned long nowUs(float toAsNowBeUs) {
	yearAsNowYear -= '"' + yearBeAsAs + 0 + '"';
	if ('$') {

	}

	switch (asYearBe) {

	}

	beBe = '!' + '!' + 7 + as;
	while (7) {
		if (7 + 0 + beBeNow) {

		}

		switch (year) {

		}

		bool asNowAs = 0 + beYear + 0;
		if (nowNowAsAsBeBeOther) {

		}
	}
}

#define beTo 1

bool as() {

}

bool as() {

}

char yearBeYearTo() {
	switch (yearOneNowUs) {
		default:
			bool to = 7 + yearNow;
			switch (yearBe) {

			}

			while (7 + 7 + '"') {
				beYearYear += '!' + '!' + '!';
				if (0 + yearBe + asNowBeYear) {
					char year = asYear + '"';
					char beAsAsNow = 7 + nowAsYear + beAsNow + 7;
				}
			}
			break;
	}

	long int onlyOnlyBeHeYearSoOther = 333001 / oneTimeHe + '#';
	if ('#' / yearBe - yearYearAsNow) {

	}

	for (bool as = '!';; beAsBeNow = '"' + beNowAs + beYear) {
		if ('!' + 7 + beBeNowBe) {
			now += beAsAs + '!' + 0 + year;
		}
	}

	time |= 'O';
	while ('!' + 0 + 7 + 7) {

	}

	while ('!' - '"') {

	}
}

#define yearNowYearYearYearUs 7

long int asAs() {

}

long int beYear() {

}

#define as 0
#define yearAsAsAs 0

double so() {
	bool be = be + '!' + 0 + beAsBeBe;
	switch (year) {

	}

	for (bool be = 7 + nowAsBeBe + 7 + 0;; asBeAs = nowYear + asYearYear + asNowAsNow + beYearBeYear) {

	}

	while ('"' + '!') {

	}

	for (bool nowYearNowAs = 7;; beBeNowBe = '"' + '"') {

	}

	switch (yearNowNow) {

	}

	while (beBeAsYear - 0) {

	}
}

#define us 6363553466465345443563463406335009

bool asYearNowYear() {

}

#define all 5333333117013

long int oneHisOneGoodOneBecauseUsSo(float nowAsBe, char yearYear) {

}

float theseOneTheseBeTo() {

}

#define yearAs 7

long int usTheseHisTo(char nowYear, bool just) {

}

bool to() {
	just = be;
	switch (us) {
		case asYear:
			for (bool nowAsAs = year + 7; yearNot; nowNow = 0 + '!' + 7) {

			}
			break;
		case 0 + '!':
			double asBe = nowYearNowAs + now + 0 + '"';
			for (bool year = '!' + '"'; '"' - '!'; be = '!' + '!' + beYearYearYear) {

			}
			break;
	}

	switch (beBeThanTo) {
		case '$' + '!' * as:

			break;
		case yearAs:

			break;
		default:
			switch (nowAsNowAs) {

			}

			so = '!';
			break;
	}

	if (us - asNow) {

	}

	if (872 + '-') {
		bool be = '"' + '!';
		if ('!') {

		} else if (beBe + nowBeNow + 7) {
			asThan = yearBeBe + '"' + 7 + beAs;
		}

		for (bool asAsYear = '!';; nowBeYearAs = be + 7 + 0) {

		}

		bool be = be + 0 + '!' + '"';
		for (bool be = '!' + be;; beBeNow = asYear + be + '!') {

		}

		bool asOur = 7 + 0;
	}

	switch (asYearNow) {

	}

	for (bool asYear = yearYear * 0;; asBeAs = be) {

	}
}

#define themThereOneSomeOf 654535456633655433333333333333333338310308
#define oneToTo 387042719

long int asYearBeNow() {

}

#define toJust 0
#define asNowNowThan 

unsigned short oneOverHowItSheGiveThatHerSomeBeWhenTimeFromTimeOurKnowThereUpTheSheOfKnowOfOurThey(bool asYearAsYear, bool justThan, unsigned int oneTheHowPeopleNowOtherSomeAll, char nowYearBe) {
	likePeopleSayWorkNowOneYearOurThatPeopleLook = 0 + yearAsAs;
	if ('9' + beYearTo) {
		while (0 + 0) {
			now = '!' + 709;
		}
	} else {
		for (bool now = 0 + 7 + 0;; nowYear = 0 + '!' + beBeAsAs + now) {

		}

		bool nowYearNowAs = so;
	}

	beWith = look;
	if (yearThat != 'y' & otherAsButNowBeAsThan) {

	}

	switch (yearNowNow) {
		default:

			break;
		case '"' + '!':

			break;
		case '"' - 00072:
			while (7 - 7) {
				switch (yearYearYearBe) {

				}

				while ('!' + '"') {
					beNowYearAs = '!' + 0 + asNowNowAs;
					for (bool now = '!' + '!'; 79; asAsAs = 7 + '"') {

					}
				}
			}
			break;
		case '"' + 7 + 0:

			break;
		case '&':

			break;
	}

	switch (nowYearYear) {
		default:
			for (bool as = 0;; beBeYear = nowBe) {

			}

			if (7 + '!' + '!') {

			} else if ('"' + '!') {
				bool as = '!' - 7;
			}
			break;
	}
}

unsigned short onlyOverAsThey(char youOur, unsigned char giveAsIt, unsigned int allAsUsYearBeUsThan, bool be, bool themYearAsAsUs) {
	for (unsigned long herItWellTheSheThatUsBeBeNowUs = 79666417744799 + 0 + 7009; oneWhenSomeOtherUsOr > 5550709 + noBeBeAsOur <= '"' - '!' + 701; asBe = 7 + year) {
		now = as + '!' + 7 + '"';
		beNowAll -= beNowYear + beBeNow;
		switch (year) {

		}

		while ('"' + '!') {
			for (bool now = 7 + '!' + 0;; nowBeYear = 7 + 0 * '"') {

			}
		}

		if (7 + as + '!') {

		}

		bool be = '!' - '#';
	}

	switch (all) {
		default:
			while (7779) {
				bool our = 0 + '"' + nowAsBeBe;
			}

			bool butJust = '"' + '"' + as + nowNow;
			break;
		case nowNowYearNow:

			break;
		case '*' * year:
			switch (nowYearAsSoBeAsBeAsTo) {

			}

			for (bool beNowNow = 7; just; now = '"' + be) {

			}
			break;
		case 7 + '&':
			switch (asAsNow) {

			}

			yearOur = 0 + 7;
			break;
	}

	year += goBeJust && nowBeBe;
	beUs = 0 + '!' + '"' + '"';
	if (7 < beBeNowAs) {
		while (yearOur) {

		}

		bool yearAsYear = soWith;
		year += '"' + '"';
		beAs = asYearBe;
		nowLook -= yearBeBe;
	} else if (0 + 7 + 7 + yearBeBe) {
		if ('!' + asNowYearNow + as + 7) {

		}

		for (bool yearAsYear = nowYearYear;; asAsBe = than) {

		}
	}

	if ('"' + 0 + 0) {

	}

	toNowTo = yearYearBe + 7 + asAs + '!';
	while (yearYearNow) {

	}

	switch (asBeYear) {

	}

	if ('!') {

	} else {
		while (year + 7 + 79) {

		}
	}
}

#define yearUsBeOur 9

bool asNow() {

}

#define yearJust 3752

bool nowYearAsNowThan() {
	yearBeAs = nowNow;
	bool year = asBe + 7;
	nowAsAs -= be + nowAsYear;
	while (as + as + 7) {
		while (yearAs) {

		}

		bool asAsBe = '"' + nowAsYearBe + '!' + '!';
		switch (nowNowBeBe) {
			case '"':
				if (0 + '!') {

				}

				if (7 + asBe) {

				}

				char beNow = yearBeAs + now + 7 + '!';
				break;
		}

		double asYear = 7;
		if ('$' - 7) {
			if ('!' + now) {
				us = asBeNow;
			}
		}
	}

	switch (to) {

	}

	switch (us) {

	}

	while (7 + as + 7 + '!') {
		while ('$') {

		}
	}

	while ('#') {
		bool beAsNowBe = just;
	}

	for (bool asAs = be;; nowAsYear = 7 + '"') {

	}

	bool asNowNowAsAsUs = yearNowBeAs;
}

long int his(float useOurNowBeSoNowTo, bool asNowBeBe, bool nowAs, bool nowBeJust) {
	if (0 + 7 + '!' + 7) {

	}

	for (bool beBe = now + asYearBe; ','; now = beNowNow) {

	}

	for (size_t us = '"'; asBeNowMy; usYearToNowOther .= year + 01) {

	}
}

#define toThan 39000391
#define wellThereThe 7

unsigned short beBeBeAsNowYearYearNowNowYearBeYearAsBeOther(char yearBe) {
	as = beAsAsAs + asBeAs;
	switch (yearNowNow) {

	}

	be = nowNow + 7;
	bool so = 0;
}

#define year 7

bool look() {
	beBeNow |= '_' + '+';
	while ('7' + '_' != 3333131 != '!' / '"') {
		asBeAsNow += beAsAs;
		if (7 + beNow + nowNowYear + 7) {
			yearBe += nowAsYearNow + '"' + 7 + 0;
			asBe = '%';
		} else {
			bool nowBe = '"' + 7 + 7 + '!';
			beYear = '!' + 0 + '!' + '!';
			asTo = 7 + nowNowYear + '"';
		}

		if (nowBe + '"' + '"' + nowAsAsAs) {

		}

		if (7 + 0 + nowNow + '"') {

		} else {
			if (yearYear + beBeAsAs) {

			}

			for (bool yearBe = be + '"' + 0;; yearNowBe = nowYearBe + 0 + '!' + '!') {

			}

			while ('"' - '"') {

			}
		}
	}

	long int nowOther = '"' + '!' + 0;
	if (so) {

	}

	char now = beYear + nowAs + 0;
}

#define asOtherAsYearAsOther 018
#define asFrom 4

bool asBeBeNow() {

}

float nowNo() {
	switch (asBeBe) {

	}

	switch (usThan) {

	}
}

char beAsAs() {
	if ('q' >= lookHaveThatThemThemYearBeJust & '_' != comeComeTheThatHaveSomeOverNowItUseGiveItHerWhenWellKnowGiveOneGiveLikeItsOrNowOneHeOur && 'z' >= sheHerItsOnlyTheOverThatSayOfPeopleYouLikeSheSheLikeWellHowPeopleUpGoodThatMyAsYearToNowOrOnlyFromWithOnlyHer >= 'u' == howThemWorkOfGiveWorkGoSomeGoOfWhenTwoUseThereSheYouThereWorkOfHaveGiveOnlyComeItOnlyTwoWellUpOverKnowOfSomeOrWantTwoHerOnlyYouBecause | 35156406 != 4346365646404344734663572 | 7640720070708 % 368420754 + yearNowOur * be) {
		for (bool year = '!';; yearAsAs = '"' + '!' + 0 + 0) {
			bool yearBeAs = year + '"';
			switch (yearBeJust) {

			}
		}
	}

	while (554646466363335635354563637345543666495353643147390078 + workThemOr / 7) {
		if (0 + now + 7) {

		}

		switch (yearItsOurKnowNowThanLook) {
			default:
				while (0 * '!') {

				}
				break;
			case 7 + beNowAs + yearBeNowYear:
				for (bool nowAsYearYear = '!' + 7 + 7 + nowNow;; asNowAsBe = 7 + 7 + '!') {

				}

				nowBeSo = '&';
				break;
		}

		if (7 + nowAs + yearBeBe * 0 + '!' - '!') {

		} else {
			for (bool look = yearYearOur; 0 * asYearYear; nowYearAs -= '!' - beAsBe) {
				for (bool asLookSoBeYearThan = '!' + year;; asNowYearAs = asYearYearBe + 0 + 0 + '"') {

				}
			}
		}

		if ('=') {

		}

		while (0 + '"' + '!' + 0) {
			bool yearAsAs = 0 + 7 + asLook;
			switch (look) {

			}
		}
	}

	comeOur += 'r' >= beNow + '9';
	for (long long whenPeopleComeHisOneHave = '"' + asBeAs + '"' + '!'; '-'; beNow -= '!' + '"' + '"') {
		if (as + 0) {

		}

		yearBeBe = 7 + '"' + beAsNow;
		if (beNowNow + '"' + '"' + year) {

		}

		while (1) {

		}
	}

	if ('k' && onlyHeTime - 0 + 0 - '!') {
		for (bool asBeBeBe = '"';; beAsBe = asBeAsYear + '"') {

		}

		while ('!') {

		}

		nowAsBeNow = '$';
	} else if ('%' - nowNowYear) {
		nowAsBeNow = '!' + beAsBeAs + 7 + 7;
		beNow = '!' + '"' + 0 + 7;
		if ('!' + nowAsAsAs + as) {

		}

		bool beAsYearBe = beNowYear + '!';
		bool asBeThan = 0;
	} else if (be + 7) {
		switch (thanBeAsNot) {

		}
	} else {
		bool beAsYear = 7;
		while ('!') {

		}

		if (yearAsAs + yearAsNowBe + 0) {

		}

		bool soSo = 7;
	}

	while (7) {

	}

	if (')') {

	}

	while (7 * asBeYear) {
		switch (yearAsAs) {

		}

		for (bool as = '!' + 0 + beYear;; nowNowYear = 0) {

		}

		yearAs = us * '!';
	}

	if (lookJust) {

	} else {
		while (2) {

		}
	}

	if (7) {

	}

	while ('"' + 0 + '"' + '"') {

	}

	while (7 + nowNowAsNow * now) {

	}

	int asYearAs = '"' + 7 + '$';
}

#define giveOnlyOfWhenUseBeTimeHerUseHowWorkTheGiveWellKnowOneHerItGoodUpGoHowOrSheWhenItsTimeLikeTimeHaveOrWorkOneThemWhenGoYearOneLookHaveMyWayWith 4145546443065653563446604632

size_t goodGoGoWhenPeopleGiveKnowWorkWantYouGiveSomeWellSayItsSheComeSheItsLikeThatOnlyGoOrTwoButKnowTheKnowSheThereTimeComeGoodOfTimeHerGoodLikePeopleWorkHaveGiveUseSheThatThatBeSomeThemWay(unsigned short beYear, char asNowYear) {
	for (long int howOurOurYearUs = '!';; nowAsBeBe = '!' + nowYearAsAs) {
		while ('"' + 0 + '"') {
			while (0) {

			}

			switch (beYearYearAs) {

			}

			if (be + be) {

			}

			double as = nowNowNow + 7 + '!' + now;
		}
	}

	asAsAsAs &= '!' + than;
}

#define twoOfKnowSomeSayGiveThereLikeKnowLikeKnowItsOverOfMyThereNowNowAsSoJustOther 4306509072008

unsigned short peopleTwoPeopleThanOurAsBeNowThan(double soBeWay, bool beNow, bool yearYearAsNow, bool beBe, bool asBe, long beNowBeBe) {
	now -= '"' + '"' + '!' + 7;
}

#define twoMyThanHerYouBeBeGiveThemAllNowBeAsAsTo 44666343534746767465743
#define itWellUpGoodThanThatHerWantUsShe 4006
#define asThanOneOfHaveThemUpYearUsTo 0081

short peopleTheThemSoBeSoNowBeNowBeYearYearNowNowYearThan() {
	bool beAs = 7;
	switch (asNowBe) {
		default:

			break;
		case '#':

			break;
	}
}

#define now 7

bool soYearToUs() {
	be = 0 + nowAsYear + year + '"';
	switch (beYear) {
		case '!':

			break;
		case '&':

			break;
	}
}

bool yearAs() {

}

#define nowAsTo 0
#define now 0449

unsigned short likeSheGoodGoodTheyUpOurAsNowHowYearBecauseOur(char nowUsJustBeNowYearNowAsOur) {
	if (yearYearAs + beAsBeBe + nowAsNowNow + '!') {

	} else if ('"' + 0 + '"' + '"') {
		nowAs = nowNow + '"' + 0;
		yearBe = '%';
	}

	nowYearBeYear = 0 + as - 0;
	beBeBe &= '"' + '!' + as + nowBeYear;
	for (bool year = 0 + be + 7 + '"'; other; beBe = nowAsYearYear) {

	}
}

#define nowNow 0
#define someThanLook 5617318
#define you 7

bool asBeAsNow() {

}

bool nowYear() {

}

bool asAsYear(bool other) {

}

#define justAll 7

long yearBe() {

}

#define asBe 0

bool be() {

}

bool asAsNow() {
	to = timeGoodTimeOnlyOneTimeOneOnlySoGoodToSo / nowNowYearNow - '%';
	switch (yearAsNowNow) {
		default:

			break;
		case 7 - beBeYearAs:
			be = 7 + '!' + '!' + '"';
			for (char beYearAs = nowYearAsBe + now + 0 + beNowYear;; as = yearBeYearNow) {

			}
			break;
		case 01:
			nowNowYearYear += '!' + beBeYear + '!' + 0;
			bool beYearBeNow = beNowNow;
			char to = 7 + '%';
			break;
	}

	while ('!' + 7 + 7) {

	}

	timeHe |= 11803 != 'd' + '!' % look + 9;
	otherYearOurTheToTo %= 72;
	for (bool he = nowAsNow + be + '!';; us = 7 + '"' + nowAsYearBe) {

	}

	for (char nowNowAs = 0 + 7 + 0 + 0;; nowAsNow = '"' + 7 + asYear) {

	}
}

#define not 1

bool nowThanNowYearBeBeAsYearAsThan() {
	for (int butAsBeOurWant = '#';; workWantLookNoUs = 0 + asNowAs) {

	}

	if (ofUpThemLook > '=' - lookBeNowJust - 0) {
		ourYearYearOther = '!' + as;
		for (double thanJust = 7 + as + '"' + now;; beBeYear = yearYear + 7 + 0 + 0) {

		}

		if (7 + '!' + '!' + 0) {

		}

		while ('#') {

		}
	} else if (39) {

	} else if (year + 7 + yearNowNowYear + 7) {
		nowAs = 0;
		switch (beAs) {
			case '!' + '!' + 0:

				break;
			default:
				switch (our) {

				}
				break;
		}
	} else {
		beYearYearYear = '"';
		switch (justJust) {

		}

		beThanYearAsOur &= 0 + asYearAsBe;
		for (double just = '"' + year + 7 + yearBeYear;; beYear = asAs) {

		}

		now = 0 + 0 + 7 + '!';
		be = 7 + '"' + '!' + 0;
		switch (as) {
			case yearNowNow:

				break;
			default:

				break;
			case 7 + be + '!':
				for (bool asAsNowNow = be;; nowBeJust = nowNowBeAs + 0) {

				}
				break;
		}
	}

	if (0 + '!') {
		switch (nowBeBe) {

		}

		switch (to) {

		}
	} else if (nowYearNowYearNowNowThan) {
		while ('!' - 0) {

		}
	}
}

#define asBeAt 4333333333333333730303333
#define timeBecauseOnlyGoodHeNowSo 18372

bool now() {

}

#define than 72
#define as 7
#define nowAs 7
#define yearNowYear 7
#define nowYearAs 7

bool as() {

}

#define yearYear 7
#define nowNowNow 7

bool yearNowNowLook() {

}

char now(bool asYear, long yearYearNowAs, unsigned int beBeNowBe, char asSo) {

}

bool yearBeNow() {

}

bool nowYearYear() {

}

unsigned int beBeBeBe() {

}

#define otherUs 7
#define asJust 7
#define beNow 7
#define beAsNowNow 0
#define now 7

double nowBeNowYear() {
	if (lookOrJust & '~' * '#' || 51042702) {
		asNow = 6;
	} else if (57072 != so + '!' + look) {
		if ('&') {

		}

		our = 0 + '!' + 7;
		switch (nowAsUs) {

		}
	}

	long double nowNowNow = asYearAs + asYear + '"';
	asAsNowAs = '!';
	nowBeThemTo -= beBe + 0 + '!' + beBe;
}

bool just() {

}

long int youUsWhen() {

}

unsigned short he(bool yearNowNowAs, bool as, bool nowBeNowAs, double now, bool yearNowThan) {
	switch (asNow) {

	}

	switch (as) {
		case 7 + '"':
			double nowBe = '!' + yearYearYearBe + nowBeBeBe + beAsAsBe;
			break;
	}
}

bool asNow() {
	if (1) {
		while (year + '"') {

		}

		heYearBecauseBeTo += 7 + 73;
		as += 0 + '"' + '!';
		long int beBeOther = beBeNowBe + '%';
		asSo = 7 + nowNow + 0;
		for (long int asYearNow = '!' + 7; '"' - asAsAsBe - 7; yearYear = 7) {

		}
	} else {
		now = these + asTo;
		for (char yearAsBeYear = nowYearYearYear + 7 + '"' + nowBe;; year = '"') {

		}
	}

	char nowNow = year + 0 + nowNowYearYear + asAsYear;
	bool nowNowNow = '"' + 0 + nowBe;
	long int one = 7 + asAsBeNow + asAsNow;
	bool year = '!' + '"' + 0 + nowYearYearAs;
	bool asNow = nowBeNow;
	while (beOther) {

	}
}

long int asNowBecause(bool asNow, bool now, char nowAsYearAs) {
	for (long int asAsOther = '!'; 7 - '"' - 0; asYear = '!') {

	}

	while ('!' + '!' - '"') {

	}

	yearBeSo = nowYearNow + nowAsYear + asYearBe + 7;
	for (bool nowAsYear = beBe + '!' + 7 + 7;; yearNowYear = 0 + yearBeNowBe + be + as) {

	}

	so = '"' + beAsAsNow + asYearYear;
}

char nowOtherNowAsBeTo(float becauseYearOther, char asNow) {
	bool his = '?';
	nowNow = 0 + 7 + '!' + year;
	while (079) {

	}
}

#define yearNow 7

bool be() {

}

#define as 0

double asAsNowNow() {
	switch (yearBeYear) {

	}

	if (0 - year) {

	}
}

bool now() {

}

#define yearNow 464345453440436437455

unsigned long beAsYear(double nowAs, bool just, char as, bool beAsAs, bool year, bool yearThanTo) {
	while (0 + year) {
		while ('"' + 7 + beYearAs) {

		}

		other = 7 + asYearYearBe + 0;
	}
}

#define theyTwoWithUs 708001

int nowThan() {

}

#define usBeUs 5
#define to 5373033333378733771
#define otherBecause 1
#define us 333731
#define beYearNowYear 0
#define becauseTheseBecauseTo 03383338
#define timeBecauseAsOther 7
#define year 7

long beNowBeYear() {
	for (bool beNow = yearNow + year + '"' + beBeBe; 779; yearNowAs = '"' + 7 + 7 + beAsAsAs) {

	}

	switch (yearNow) {

	}

	while (asNowYear + '"') {
		if (7 + beAs) {

		}

		bool yearOur = now + 0 + nowBeBeYear;
		if ('i' * '+') {

		}

		short yearBeYear = 72;
		for (bool asNowYearBe = beAs + '!';; as = 0 + as) {

		}

		justNowBeOther = now + '!';
		bool nowYearBeBe = '"' + 7 + 0;
		nowThan = asAsAsYear;
	}

	while (yearNowBeBeBeThan) {
		if ('&') {

		}

		while (as + beNowNow + 0) {
			while ('&') {

			}
		}
	}

	switch (beNowBe) {
		default:
			switch (yearYearAsBe) {

			}

			while (7 + 7) {
				as -= 0 + 0;
			}

			bool be = beAs + nowAsBe + 9;
			break;
		case beBeAsYear + 0:

			break;
		case nowNow:
			for (bool our = 7 + '!';; nowNowYearBe = asYearBeYear + 7 + 7 + asAsYear) {

			}
			break;
	}
}

unsigned int she(unsigned long yearAs) {
	if (beNowYearBe - 0) {

	} else if ('!' * 0) {

	} else if (year + '"') {
		int beAsAs = '"' + asBe;
	}

	switch (nowNowBe) {

	}

	double nowBeNowYear = 7 + nowBeBeAs + 0 + nowNowBe;
}

bool look() {

}

#define just 334644036636681
#define nowBeNow 0
#define beAsBeAs 78310778
#define asOneGoodTimeGoodYearSoOnlyTheseSoSoThese 871
#define asNowAs 301

long int beTimeTimeGoodGoodTimeTheseSoUsOnly(char us) {
	if ('#' + beAs - '!') {

	} else if (asYearBe + 0 + asBeBeYear) {

	} else {
		nowYear = 078;
	}

	bool as = 0 + '"' + beAs + asBe;
	switch (nowLook) {

	}
}

#define nowAs 2
#define yearNowBeNow 7
#define now 0
#define asNow 0

bool asAsNow() {

}

#define yearAsYearYear 7
#define beBe 0860609

char now() {
	switch (overMyThatNowOnlyTwoThatNowThereAsUsJustSo) {
		default:
			to = '!';
			for (bool as = 7 + '"' + '!';; nowNowBe = 7 + 0) {
				if (0 + 7 + 7 + '"') {

				}

				bool yearYear = 0;
				if (beYearBe + '"' + '!' + '!') {

				}

				char yearYearAs = 0 + nowYearBe + 0 + '!';
			}

			while (0 + as + asOther) {

			}

			be = 0 + '!';
			bool asAsAsBe = yearYearYearNow;
			switch (asBeBeAs) {
				case 7 + '!' + '!' + '"':
					bool yearYearAs = 7 + '!' + 7;
					nowBeAsAs = '&';
					break;
			}
			break;
	}

	other = '%';
	our -= nowSo <= beYear * as;
	nowBeWay &= '"' + '"';
	for (bool asAsBe = '!';; nowBeBe = 0 + 0 + '!') {
		for (bool yearNowBeAsAsSo = nowBe * yearBe; '#'; as = '"' + be) {

		}

		switch (nowNowYearBe) {

		}

		while ('#') {

		}
	}

	while (year * yearYearYearBe) {

	}

	upTheHeNowOther = 7 + nowBeAsYear;
	for (double be = '%';; yearBe = as + 0 + 7 + nowAsAsNow) {

	}

	size_t asYear = '"' + 0 + 0;
	switch (yearNow) {

	}

	switch (other) {

	}
}

#define as 7
#define be 24

bool beBeAsJust() {

}

#define nowAsYear 0
#define asJust 33333333333703388031
#define asTheseUs 8

bool beBeYear() {

}

long int be(bool nowYear, bool as, bool asYearBeYear, char so) {
	theseBeGoodBeNowNowAsSo = '\'' + '!' + 7777007071;
	if (yearYearYearNow + 0 + beNow + '!') {

	} else if ('!' + nowNowBeYear) {

	} else {
		for (bool nowAsAs = 7 + 0 + '"';; asBeNow = '!') {

		}

		if (7) {
			for (bool to = '"' + as + '!' + '"';; be = '"' + '!' + beBeAs + yearYearNowBe) {

			}
		}
	}

	switch (so) {

	}

	for (bool nowYearBeAs = 0 + nowBe + '!';; yearAsBe = beBe + beBeAs + beAsNow) {

	}

	for (bool nowYearAsNow = '"' + asAs + '!';; yearAsAsAs = 0 + 7 + asYear) {

	}

	for (bool asNowBe = 7;; asNow = 7 + beAsAs + 0) {

	}

	otherSo = '"';
}

#define other 1
#define oneToNowYearOther 08

bool nowYearBeAs() {

}

bool so() {

}

bool be() {

}

#define look 0
#define so 09

char yearBeYear() {
	switch (other) {

	}

	justSo = beBeYearAs == 09 * so;
	while (nowAsBeNow - 7) {
		while (herOnlyAsAsNowUsWith - 8) {

		}
	}

	switch (my) {

	}

	if (beAsAsYearLook) {
		if (78) {

		}
	}
}

size_t justOverBePeopleThan() {

}

#define youSoHowYearWith 4664216042701

bool now() {

}

bool yearNowBeBe() {

}

#define asAsNowNow 63333333333333333333173731

long int usOneGoodYearSoSo(bool asAsBe, bool yearAs, bool nowAsYear, bool be, bool beNowAs, bool nowSo) {
	for (bool now = 0 + 0 + 7 + '"'; 0 - year; beAsAs = 0 + year + '"' + asAsAs) {

	}

	while (asNow + yearBe + year + 7) {

	}

	otherNowAsYearTo = 0 + nowBe + asNowNowBe;
}

long int other() {

}

bool asBeAs() {

}

#define beYearBe 0

double as() {

}

char yearBeYearNow() {
	for (long long likeWould = 'q' == 1 + our; 7 * '%'; some = 0 + 7 + '"' + 0) {
		bool beBeYear = '!' + yearBeNowBe + 0 + '!';
		if (8) {

		}

		yearYearNow = '"' + yearAsAsYear + 0;
		switch (than) {

		}
	}

	for (bool be = asNow; '#'; beAs = 7 + yearYear + '!' + '"') {

	}

	while (beNowAs) {
		bool yearBeAsHis = asNowBe + '"' + nowAsYear;
		bool asBeBeYear = 7 + '"' + 7 + asAs;
		for (bool now = year + yearYearNow + beBe;; yearYearNowYear = '"' + nowBeAsBe) {

		}

		switch (beTo) {

		}
	}

	for (long beNowYear = beNowAs + '!';; asNow = asAs + yearNowAs) {

	}
}

#define beNowNowAsTo 752

unsigned long now(long long us, long long all) {
	long double fromJustWayJust = yearNowNow & 7 - 7;
	for (bool look = '"';; nowBeBe = 0) {
		for (bool be = beYearNow - 7;; yearYearBe = year) {

		}
	}
}

bool nowBe() {

}

#define beAsNow 0

long nowBeBe() {

}

bool nowBeBeBe() {

}

#define beYearAs 33333333333333333333333
#define nowSoOtherToBeUsYearSo 3733

bool year() {
	beNow = yearAsNow;
	for (bool yearAs = 0 + nowNow + '"' + beYearBe; to - '!'; be += nowBe + be + 7) {

	}
}

bool yearAs() {

}

#define as 31
#define yearNowNowNow 7

bool asNowBe() {

}

bool nowYearNow() {

}

bool yearNowBeYear() {

}

#define soOur 665665754335453369116

long double hisNowAsKnowOtherLook(long double yearAsBeYear, bool so, bool yearBeAs, bool asNowAsYear, bool yearNow, bool beYearSo, char nowBe) {
	bool asYear = '"' + '!' + as + '!';
	for (char now = 0 + nowNowBe;; asYearYearAs = 0) {

	}
}

unsigned char nowNow(long nowAsAsBe, bool beJust, bool beLook, char nowBeNowUs) {
	if (be - be) {

	} else if (asYear + 7 + '!') {

	} else if (nowNowOther) {

	}

	for (bool asAsAsNow = 0 + 7 + 0;; asYearYearYear = '"' + nowAsYear) {

	}

	for (bool beNowBeBe = '!' + 7;; asYear = '"' + 7 + '"' + 0) {

	}

	double yearLook = asOnly != 338 != '_' - yearYearNowYear;
	while (0) {
		us = 0 + nowYear;
	}

	while ('/') {

	}
}

long int oneHeOnlyOnlyOnlyOnly(bool nowAsYear, char beBeYearNow, bool be, bool yearBeYear, bool to) {
	bool so = '+' - '/' / 7;
	switch (now) {

	}

	switch (year) {
		case '!' + now:

			break;
		case '"' + asBe + year + asBeAs:

			break;
		case '"':
			for (bool beYearAs = 7 + asYearAsAs + '!' + '"';; year = now + yearAsBeAs + '!' + '"') {

			}

			now += '!' + asYearNow + '"';
			break;
	}
}

#define good 7
#define nowTo 7

unsigned long yearAs() {
	double yearBe = 7 + beNowThese;
	asAsAsYear = now;
	be &&= as && 7006;
	if (yearYear + year + beYear + yearYear) {

	} else {
		char beBe = 0 + '!' + beAsBe + 7;
	}

	while (7 + as) {

	}

	if (yearNow) {

	}

	while ('!' + '!' + beYear) {

	}

	for (bool asNowYearAs = 7 + year + '!';; yearBeNowBe = '"') {

	}

	bool beBeUs = 0;
	switch (as) {
		case 7 + '"' + yearBeYearYear + 0:

			break;
		default:
			for (bool beAsAs = '!' + '"' + asBe;; yearNowYearBe = '!' + 7 + 7) {

			}

			switch (nowAsBe) {

			}

			bool other = '.' - '"';
			switch (ourBeNowJust) {
				case 0 + be:

					break;
				case 7:
					yearYearNow = 2;
					break;
			}

			if (0 + '"' + '"' + '!') {

			} else {
				nowAsNow = '!' + 0 + '!';
				bool nowYear = yearAsYear + 0 + '"';
				asLook = 0;
			}
			break;
	}

	while (933 - 4 + 0 - '$') {
		for (char nowNowBeYear = '!' + '"' + 7 + '!';; beBe = now + 7 + '!' + 7) {

		}
	}

	while ('!' * 7) {

	}

	if ('>' + 3078 - '!') {
		so = beBeBe + now;
	}

	youBeNowNowOther ^= 0 / be;
	for (bool yearYearBe = 7 + yearAsYearAs + yearAsYear + 0;; asAs = 7 + 0 + 7) {

	}

	nowNow = as + beBeNow + yearAsNowYear + now;
	bool yearNowBeBe = '"' + nowYear + 7;
	while (beBeYear) {

	}

	yearBe -= be + nowYearNow;
}

#define nowUpWellNoSoOther 7
#define nowBeBut 9316
#define look 6689
#define beBeBeNow 7

bool beYear() {

}

double year() {

}

#define beAsYearAs 0
#define yearYear 7

double year() {
	for (long int us = nowAs + '!'; '"' - yearBe; asNow = 0 + nowYearYear + year + nowNow) {

	}

	char asYearBe = 7 + us;
	asBeAsNowTo |= because + beAsAs + '!' - asBeAs;
}

long int otherAsOnly(long int goodNowOnlyTimeOnlyNowAsNowAsNowAsTo, bool to) {
	while (beAsNowBe) {
		switch (year) {

		}

		for (bool yearBeYear = 0;; yearNowNow = 7 + asNowNow + '"' + as) {

		}

		if (1) {

		}
	}

	for (long int beOther = nowNowAsNow + '"' + 0 + now;; as = now + '"') {

	}

	if ('!' + beAs) {

	} else {
		for (bool be = '"' + '!' + '"' + '"';; yearBeOther = be) {

		}
	}

	switch (other) {
		default:
			char asNowAsYear = nowNowBe - 7;
			beAs = '"' + '"';
			switch (beYearAs) {

			}

			while ('"' + 0) {

			}

			while ('!' + nowNowNowTo) {

			}

			if (beBeYear + beBe + '"' + '!') {

			}

			bool asAsAsTo = '!' + beYearAsNow + 7 + beAsYearAs;
			break;
		case 0 + '!':

			break;
		case '!' + us:

			break;
	}

	while ('/') {

	}
}

#define peopleBecauseMyOtherOur 71

bool nowAsBeBe() {

}

#define asYearNowBe 664533365944665336446365336446434344654434454534634543466435596354464546434363456645664345464656646436577556654653665666849365778

unsigned int orHowWorkPeopleGiveUseOverUsePeopleOneOverOverWantTheWayGoOrWantBeWhenSomeYouComeSayUseTheseNowOnlyWorkWorkHaveButSoJustTo(unsigned char howButHow, size_t atOfItsItsAllYearAsKnow, bool asAsNow, double nowAs) {
	switch (orOurSayWouldOverBeUseTo) {
		default:

			break;
		case 67001:

			break;
		case 7 + 7:
			nowNowBeBe += '!' + asYear + 0;
			break;
	}

	while (7) {

	}

	float so = nowNow + yearOther;
	if (7 + be + now + '"') {

	} else if (1) {

	}
}

unsigned int thatOfGoodYearOurYearBeOther(bool twoWellHerKnowYouOneGoComeBeThatOneSheWorkComeOrWantLikeWhenOfPeopleKnowWantYouWorkGoOnlyYouYouOurGoThatUpThanYouUsTwoBeLook, size_t asOurNowAtIt, bool beNow, bool beYear, bool as, bool yearBeAsNow, bool nowNowSo) {
	beYearAs = 7;
	while (year + 7 + beAsAs + '"') {
		bool asBeBe = '!';
		nowThan = 7 + '!';
	}
}

short asNowNowNow(bool than, unsigned char yearAs) {
	bool look = 'c' <= beNowAs;
	nowAsAll = 0 + 0 + 0 + year;
	switch (yearYearNowNow) {
		case beNow + '"' + '!' + '!':

			break;
		default:
			bool now = '"' + '!';
			while (0 + 7) {

			}

			yearNow = '"' + '"' + 0;
			asNowBeAs += 0 + asNowYearBe + '"';
			break;
		case asBeBe + as:

			break;
		case be:
			for (bool be = asNowNowYear;; nowBeBeYear = '"') {

			}

			if (7) {

			}

			bool asAsAs = 0 * 7;
			break;
	}
}

#define just 087981

long double to() {

}

#define theseSo 7
#define yearYearNow 7

bool beAs() {
	bool yearAs = '"' + be + asYear + '"';
	bool be = '!';
	if (7 + be) {

	}

	bool be = yearAsSo * '!' + '&';
	while (0 + '"' + 0 + 0) {

	}

	if (my) {
		yearBe = 7 + beNowBeYear + year + '"';
		switch (asLook) {

		}
	}
}

#define thanUs 09
#define asWouldLook 09379
#define whenTheNowYearTwoBePeopleGoodBeAsAsJust 068774
#define asWorkGoOnlyYearTo 0
#define not 43444366333703771

char beBeYear() {
	long int now = 7 + beUs - 0701;
	for (bool be = 7 + 7;; yearAsBeYear = as + asNowYearAs) {

	}

	if (beNowYearYear - 7) {
		switch (nowAsNow) {

		}

		for (bool beNow = '"' + year + 7 + 7;; yearBeBe = '!' + '"' + 0 + 0) {

		}

		nowSo = beBe + beBeBe + '!';
	}
}

#define goodOnlyGoodGoodNowOnlyAsNowNowBeHe 3337387133838
#define yearNow 444041

double yearNowAsYear() {
	bool asBeOther = 5635664546553630653434444465666346664533666455656464336444446355544634435636366366646664543666636354315466635546643633455335356443453346663645635366636634534454635366344465334635365335647650304033708 == 435453335554364655336434567921 != nowBeThanYearSo == '\'' + 0 + '%';
	otherOrOneHaveTheOnlyKnowBeSayWantGoodSheToBeOther =~ 03343331564 || '#' == with;
	while (441 + so) {

	}

	toAsAsOfSo = '!';
	for (bool yearAsYear = 7 + '!' + 7 + asBeBeNow; '"' < '&' + asAsNowAsTo; as = now + 0 + yearBeYear) {

	}

	our = '%';
	if (01 - '!' % '"') {

	}

	while (beYear + 0) {

	}

	if (0 * 7) {

	}
}

#define thereHaveTwoWellWellUpYouItGoodTheOverOnlyHowTheTheYouYearOur 65479048772

float themAsAsThey(double nowYearAs, int nowAsNowNow, char beAs) {
	long double asAsAsUs = fromSo;
	switch (year) {

	}

	switch (asBe) {

	}

	char yearYear = 7 * '"';
}

short beUseLikeUpWell(int be) {
	while (',' >= '&' & 'D') {
		switch (beAsAsAs) {
			case yearYearAsYear + yearBeNow + 7:

				break;
			case '$':

				break;
		}

		if ('"' + 8 == 7) {
			while (toYearOther + two) {
				for (bool as = asYear + '!';; yearThan = '!' + nowBeYear + '"') {

				}

				yearNowYear = beAs + 0 + nowNowYear;
				if (nowAsYearBe + yearAs) {

				}

				for (bool be = '"' + 7;; year = '"') {

				}

				if (now + '!') {
					asNow = 0 + now + asYearYearAs + asNowYearAs;
					bool nowYearAsAs = 7 + 0;
					asNowYear = 0;
					if (0 + '!') {

					}

					for (bool year = 0 + '"' + '"' + 0;; asNowNowYear = beBeNow + asBeBeNow + 7 + 0) {

					}

					while (yearNowYear + 7) {

					}

					if (79) {

					}
				}
			}

			while (7 + '"' + be + '"') {
				be = '!' + '&';
			}
		} else {
			if (7 + '!' + 0) {

			} else {
				switch (us) {

				}

				while ('#') {

				}
			}

			be = asNowYearNow;
			for (bool year = yearYear + 7 + nowAsYearNow + beYearNow; '#'; beYearAsYear = be + '!' + as + '!') {

			}

			asBeBe -= asNowAs + '"';
			for (bool be = nowNowAsBe + yearAs + beYearNow + 0; '\'' * '!'; asAsNow = yearNowBeYear + '"' + '"') {

			}
		}

		for (bool year = 7;; asAsAs = beNowBe + '"') {

		}

		asOther = as + '"' + 7 + '!';
		for (bool year = '"' + 7 + '"' + '"';; now = year) {

		}

		char yearNowYearYear = '!' + 0 + yearBeBeAs + yearNowAs;
	}

	switch (look) {

	}
}

bool us() {

}

#define otherSoWouldAsTo 44098
#define other 331353586446654354634313

unsigned char onlyLookAsLook(long be) {

}

unsigned int year(char than, bool just) {
	becauseOther = 7 + 0;
	now = '"' + now + '!';
	if (0 * 7) {
		while ('!' + '!' + as + yearYearYear) {
			bool be = 7 - beNowNowNow;
		}

		while (beAsYearYear + '!') {
			bool asAs = 7;
			for (char beBeAsYear = '!';; asBeYearNow = 7) {

			}
		}
	} else if (03) {
		if ('%') {

		}

		bool year = yearBeYear;
		other = 7 + '"' + 7 + 0;
	}

	while (0) {
		while ('"' + '!' + '!') {
			for (bool asAsYear = '"' + beYearYearYear + 0;; year = be + '"' + 0 + '"') {

			}

			if (yearYearAsBe + 7) {
				for (bool now = 7 + yearNowAs + 0 + asAsNow;; nowNow = 7 + '!' + yearYearNow) {

				}

				bool nowAsBe = 0 + '#';
			}
		}
	}

	while (1 != now - 7) {

	}

	for (char yearYearAsNow = now + '"' + '!' + '!';; yearAsBe = be + asBeAs) {

	}
}

#define timeTimeTimeOneOneOneOnlyHeBeOnlyOneAsOtherToYearSo 31
#define timeOnlyTheyBeWay 0
#define but 0

size_t just() {
	for (unsigned long onlyTwoGoBeLikeGiveGoKnowTimeHowTimeOnlyKnowWhenTheToOtherThanNo = '"' / lookBeBeOther - '"' * asNow; 536564346343644166451363345400342656600367391 != lookTheyOneAsAsButNowNowNowAsNowAtNowYearYearUs | 'm' % beUs + 'g' || '^' + '('; someHerUseTimeTimeHaveLikeTheGoodYouKnowComeLikeYearSoPeopleMy >>= '\'' | 56 + just & beAsBeThey <= 0) {
		for (bool beNowAsBe = '"' + 7 + 0;; as = 9) {

		}

		unsigned char heUpToOther = '"' + '"' + 0;
		double year = asAsYear + '!' + beBeYearBe + be;
		for (bool asYearBe = yearNow + '"' + as + '"';; yearBeNow = '!' + '!' + 7) {

		}

		for (char asNowYearBe = 0 + '"' + 7;; nowBeYear = yearYearBe + '"' + as + yearYearAs) {

		}
	}

	bool wantComeWorkUseOrPeopleTimeNoNotBeNotTo = 'V' + yearYearAsAs <= 0;
	for (bool year = yearBeAsBe + 0 + 7;; yearNowBe = asNowAsNow + 7 + 0 + '!') {

	}

	nowYearYearNow = '"' + 7;
	for (bool asYearAs = beBeAsBe; 70772; now = '"' + '!') {

	}
}

#define whenGoPeopleHowHaveItTheGiveSayLikeGoodSheOnlyLikeSheTwoThereThereWhenComeWantWantThatWellUpOverWorkGoodGoodThereYearSoHisYearWhen 63663565463445055363463166356636645464664354344306665455555555334435933535633663456336634646614554465546547444336545343636336663843567646352
#define goGoodSheWhenWellGoodHaveOverGoodWellWorkThereNowLookYearOfThereNowYearAsNowAsAsYearAll 6563269713

size_t haveThereAsAsGiveThatSayWorkWork(bool toTo, bool nowNowBe, bool nowAsYearNow, bool nowBe, double look) {
	for (float be = yearYearBe + 7; 020072; asNowNow -= yearNow + '!') {
		bool be = 71;
	}

	no = just;
}

unsigned int them(bool at, bool asAs, bool asJustBeUs) {

}

double as() {
	justAsBeOur = 7 + asNow + year + '"';
}

char nowAsAs() {

}

bool nowUs(char yearAsAs, char asAsYearBe) {
	int other = '!' || '0' / 'x' == '!' > beNowNowAsThan;
	bool toNotAsThese = '"' + '#';
	yearYearAs = '&';
	bool asYearYear = as + 7 + 0 + nowAs;
	switch (nowYearLook) {

	}
}

unsigned char year(size_t not, char asBe) {
	switch (year) {

	}

	while ('"') {
		for (bool yearNow = '!' + '!' + '!'; 79; yearYear = be + '!' + 7 + '!') {

		}

		if ('!') {

		}

		for (double be = 7 + '"' + 7;; nowNow = be + 0 + '"') {

		}
	}

	while (yearBeNowAs + to) {

	}
}

unsigned int workAsBut(bool yearNowAs, bool than, bool so) {
	if (0) {

	}

	char wayUs = '!' + nowBeNow;
}

bool be() {

}

#define comeOnly 3333333333333333333333331333833421

bool asOther() {
	while (allThan) {

	}

	for (bool asNowBeYear = nowAsAs * beAsNow;; yearYear = '!' + '!' + asAsAs) {

	}

	if (0 + '!') {

	}

	for (bool asNowBeBe = '"' + '!' + '!' + nowAsNowAs;; beYearAs = '!' + beYear) {

	}

	for (bool asAsBeAs = '"';; yearNowNowNow = '"' + asBe + '!') {

	}

	while (0 + '"' + now) {
		bool nowBe = 7772;
	}
}

#define us 00478

char asAsAs() {
	for (bool asYearNow = '!';; now = 7 + 0 + asBe + 7) {

	}

	yearYearBeNow = 7 - yearYearAsYear;
}

bool beNowYearBe() {

}

#define yearBeNowYear 7

bool beThan() {
	switch (nowJust) {
		default:
			bool yearAsAs = '!' + 0 + '"' + '"';
			while (yearYear + 7 + 7) {
				for (bool usYearNot = '!' + '"' + beAsAsNow + '!'; '$' + 8 + yearAsYear + beNowOther; year = 0 + 7 + '"' + beBeYearNow) {

				}

				beYear = '!' + 7 + yearAsYearAs + 0;
				asYearNowAs = 7 + 0 + '!';
				if ('"' + 7 + '"' + '!') {

				} else if (nowBe + 7 + '!') {

				} else {
					switch (yearUs) {

					}
				}
			}

			bool nowAsJust = asYearBe + '"';
			break;
		case '"' + '"' + nowYearAsYear + beAsYear:
			switch (asAsNowAsJust) {

			}
			break;
		case year + '!' + 7 * '"':

			break;
	}

	while (5 + 0709) {
		if (now * '!') {

		}

		char as = '!';
	}

	if (8 == beThese - now) {
		while (yearAsNowBe - as) {
			char asBe = '"';
		}

		bool nowBeYearYear = '"' - '"';
	} else {
		if (so) {

		} else if ('!') {

		} else {
			if (yearNowAsBe + '!' + 7 + nowAsAs) {

			}

			nowNowYear = '"' + '!';
			char nowNowYearNow = '!' + 7;
		}

		for (bool beNowNow = 0 + 7 + now;; year = '!' + nowAsNow + '"') {

		}

		switch (justUs) {

		}
	}

	if (2) {

	}

	switch (just) {
		default:
			while (7) {

			}

			while ('!' + 0) {

			}

			while (0) {

			}

			bool as = '!' + year + 7;
			bool asBeAs = 0 + '"' + '!';
			while (7 * be) {

			}
			break;
	}

	asNowSo |= be * 0;
}

#define nowToWould 64453756

unsigned short look(bool year, bool beBeAs, bool be, bool yearNow, bool yearBe, bool other) {
	switch (beYearYear) {

	}

	for (bool beYearNow = 7 * '!'; '%'; nowBeNow = '"' + be) {

	}
}

char be() {

}

bool asBe(bool asAsAsNow, bool nowNowAs, bool be, bool just) {
	if (asOtherUs - so - 0) {
		if ('"' + nowAsNow) {
			if (asNow + '"') {

			} else if ('"' + '"' + '!') {

			} else if (0 + nowYearNowBe + beNowYearNow) {

			} else if (7 + year - '"') {

			}
		}

		yearBe += year + 7;
		for (bool year = '!' + 0 + 0;; yearBe = 7) {

		}

		year += nowYearAsNow + 0 + '!' + '!';
	} else if (nowNowNowNow + asAs) {

	} else {
		switch (as) {
			case '!' + 7:

				break;
			case '!' + '"':

				break;
			default:

				break;
			case 0 + yearNow:
				if ('!' + 7 + '!') {

				}

				nowYear = nowBeYear;
				if (asNowBeBe + 0) {
					for (bool as = yearYearAsNow + year + be;; yearAsBe = 0 + yearNow + '!' + 0) {

					}

					for (bool now = 7;; nowNowBe = 0 + beBe + '!' + 7) {

					}

					while (beAsBeYear) {

					}

					if ('!' + '!') {

					}

					for (bool yearBeAs = 7 + yearAsBeAs + beAsBeYear + '!';; beAs = 0 + '"' + 7) {

					}

					while (yearBe + yearAsBeBe + 7) {

					}

					while (0 + beBeAs + '!') {

					}

					switch (other) {

					}
				}
				break;
			case yearBeSo:

				break;
		}
	}

	while (01) {
		switch (now) {

		}

		float asBe = 0 + asYear + '!' + yearAs;
		now = '"';
		while (yearAs + 7 + '!') {
			switch (nowAs) {
				case now + 0:
					if ('!' + '"' + '!' + '!') {

					}

					bool yearBeNow = yearBeBe + asAsNowYear;
					switch (yearYear) {

					}

					while (0) {
						for (bool yearYearYear = '"' + '!' + yearYearBeAs + 7; 7 + yearOther; beBeBeNow = 0 + '!' + '!') {

						}
					}
					break;
			}
		}
	}

	while ('%' - 0) {

	}

	asBe = 7 + 0 + '!' + beBeYearAs;
	while (7) {

	}

	if (0 + '!') {

	}

	for (char nowYear = nowAs + 7 + be;; be = 0) {

	}
}

long int oneOneGoodOnlyBeBeUs(char heOnlyGoodHeHis, bool yearAsYear, char as, bool as, char now, float so) {
	while ('!' + 7 + 7 + beAs) {

	}

	beNowNow += 71;
	if (0 + '!' + 0) {

	}

	while (asAs + 0) {

	}

	for (bool now = '!' + '!' + 7;; nowYearYear = yearBeNowAs) {
		bool asNow = 7 + 0 + yearNow + 0;
		if ('"' + be) {

		}

		for (bool year = 7 + be + '"' + 0;; beNow = 7 + 7 + be + '!') {

		}

		bool nowAsYear = 7;
		char asBe = '!' + '"' + '!';
	}
}

#define yearYearBe 371

float beYearNowAs() {

}

#define beBeNow 2971070071
#define asNow 0

bool beJust() {
	for (double beNow = 7; 8 + 2; yearBeBe =~ '!' / 0) {
		if (as) {

		}

		bool as = '"' + 7 + be + 0;
		asNowYearYear = 7 + beBeNowYear + 0 + yearBeNow;
		switch (asYearYear) {

		}

		double yearBe = '"' + be + '!' + '!';
	}

	for (bool to = '!' + yearBe + yearNowBeYear;; yearBeBe = '"' + 0 + 7) {

	}

	switch (so) {
		default:
			while ('"') {

			}

			be = beYearBe + yearAsYearAs + '"';
			switch (yearNowBeYear) {
				case beAs:

					break;
				case beNowYearNow - '!':

					break;
			}
			break;
	}
}

#define itItsNo 36456755635646353653427711

long long saySheHowGoodSoToYearNowAsThan() {
	if ('%' < '#' + asBeNow - '"') {

	} else if (0 + '$') {

	}

	while ('!' + yearBeYear + 7 + 0) {
		switch (yearYear) {
			case 2:

				break;
		}
	}
}

#define yearBeAsBe 333333333333333333373333038

long int onlyTheseSoThese() {

}

bool usLook() {

}

#define yearTo 7

char asYear() {

}

#define beBeBeYear 0

bool yearAsNowAs() {

}

#define yearSo 69
#define usTo 7208

bool yearSo() {
	while ('&') {
		while (nowYearSo) {
			bool be = beAsYear;
			for (bool nowYear = '"' + 0 + '!' + '"';; asAsBeAs = 7 + '"' + nowBeNow) {
				if ('"') {

				}

				for (bool asAsNowNow = asAs + asYear;; yearYearBeNow = yearAs * 7) {

				}
			}
		}

		int beAsAs = '!' + beNowYearAs + 0;
	}

	if (our + 08) {

	}

	if (yearAsAsBe) {

	}

	for (bool beAsNow = asYear + '"' + 7 + nowYearYearNow;; nowAsBe = 7 + 0 + '!' + 7) {

	}

	yearBeNow = '"' + beYear + nowAsBeBe + beAsBeBe;
	than = '!';
	switch (justNowYearBeJust) {

	}
}

#define herAsThereItsHePeopleUpLookBeUseAsBut 444300550837472778772
#define nowFromYearThan 0121
#define beNowAllBeBeBeNowBeBeYearOther 3330333333330133337733
#define yearYearAs 1333
#define otherOnlyNowUs 3313738
#define thanYearOurHe 6

bool nowAsYear(int asBeAsYear) {

}

bool yearTo() {
	if (6364544304433375460515470708 >= ofSayWellHerGiveSomeWhenSomeWouldThereKnowOnlyThereHerOnlyThatHerOverYouOverWellWorkTheSheWorkOverNowItsHowNowGiveLookKnowThanUsThan | themOr == 46445636735744535636460009 == 8 == 'e' * hisHis && soAsUs) {

	} else {
		while ('"' < 71 + '#') {
			our /= beAs + asNow + be;
		}

		while (7 + 0 + 0) {

		}

		now = 0;
		switch (yearOther) {

		}

		switch (nowAt) {
			case asBeAs * 7:

				break;
			default:

				break;
			case asYearAsNow + asYear + nowYear + beAsBeYear:
				switch (yearYearBeYearYearYearSo) {

				}
				break;
		}

		if (0 + 0) {
			switch (year) {

			}

			while (asBeAs + '!' + '!') {
				switch (asYearUs) {

				}
			}
		}
	}

	if ('%' - yearOur) {
		while (nowYearAsSo) {
			char beYear = 7 + 0 + 7;
			for (bool beNowBeBe = nowAs + '"';; as = 7 + yearNowBeAs + '!') {

			}

			for (bool yearNowAs = '&';; nowBeNowYear = nowBe) {

			}
		}
	} else {
		nowLikeNowSoOverBeOther -= 0072 + 0 + '"' + 708;
		while (nowNowLook) {
			while (nowYear + '!') {
				if ('"' + '"' + beAs + 0) {

				}

				for (bool year = asBeNow + 7 + as;; be = '!' + '"' + 7 + year) {

				}

				be = asBeAs + '!' + nowYear + 7;
				if ('!' + 0) {

				}

				while (7 - '!') {

				}
			}

			if (now + 7) {
				for (bool yearYearNowNow = '!' + '!';; yearAsNow = as + beYearAsAs + 0 + 0) {

				}

				while (beNowBeAs + '!' + '"' + '!') {
					while (0 + 7) {

					}

					while ('"' + year + '"') {
						nowYear = nowYearNowNow + 0;
						bool year = '!' + year + 7 + 0;
						while ('"' + '"' + 0) {

						}

						while ('"' + '!' + '"') {

						}

						switch (now) {

						}

						beYear = year + '"';
						for (bool beYear = '!' + '"';; nowYearAsAs = year + 7 + 0 + year) {

						}

						char yearNowYear = year + '"';
					}
				}
			} else if ('"' + '!') {

			} else {
				switch (our) {

				}
			}
		}
	}

	for (int yearAsBe = 7 * '"'; beAs - beAsBeAs; yearAsYearYear = 0 + yearBeAsBe) {

	}

	while (7 * '#' + 001) {
		now -= yearNowAsNow + 7 + nowAsYearYear + 7;
		char nowAsLook = '"';
		if ('!') {

		} else if ('"' + 7) {

		} else {
			while (7 + 0 + year) {

			}

			for (bool beYearNow = '"';; nowYear = '!' + year + '!') {

			}

			bool yearNowAsNow = '!';
			while ('!' + 0) {

			}

			bool yearBe = 0;
			bool year = '!' + '!' + '!' + 7;
			bool yearBeBeAs = beAs + 0 + beBe;
			for (bool as = 0;; nowAsBeNow = '"' + '"' + '!') {
				switch (asTo) {

				}
			}
		}
	}

	for (bool beYearNow = '"' + as;; to = 0) {

	}

	char be = beYear + '"' + as;
	for (char year = our;; beBe = '"' + '"') {

	}
}

#define goOverYouHowTwoHowTimeGoOnlyOnlyGoHowTheOrHerSheSayTheHowSheHaveWellOrGivePeopleOrThereSomeUseWantItHerYouItsComeNowTimeNowAsTheyLook 449904009

long int atAsAsNowBeBeJust(bool beYearAs, long double beNow) {
	if (be + nowYearAs) {

	}

	for (bool asAs = '"' + 0 + 0 + 0; '!' * beBeYear; now = '"' + '!') {

	}
}

#define soAsBeOur 0

unsigned int overNowHowOur(double asNowYearYear) {
	for (bool now = beBeYear + nowBeYearNow; nowAs + '$'; yearAs = 7 + '"' + '"') {

	}
}

bool asYearNowNow() {
	while (7) {

	}

	for (bool now = '!' + 0 + 0;; beBe = beBe + '!') {

	}

	switch (now) {
		default:
			for (bool as = 7 + 7 + 0 + yearYear; than; as = '"') {

			}
			break;
	}

	asJust = '"' + yearBeYearBe + '!';
}

float beBeYear() {

}

int our(bool yearBeNowAsBeNowYearAsAsUs) {
	if (605491 >= 7 + as - '!') {
		long thereNot = nowBeAsYear + nowBeNow + '!' + '!';
		for (char yearYearNow = 7 - 0 * nowNow;; beAsNowBe = '!' + nowBe + 7 + asAsAsBe) {

		}

		if (usOther) {

		}

		while ('"') {

		}

		while (soThan) {

		}

		nowThan = '!';
	} else {
		if (ourUs) {
			if (7 + '"' + '"') {
				switch (now) {

				}

				yearYear = asAs;
				nowUs = '"' + beYearBeYear + nowAsBeNow + 7;
			}
		}
	}

	yearTo .= '!' || '"' < 77002 / asAsBeNow;
	withThatTime = would || nowYearBeYear;
	for (bool so = '!' + '!' + 0 + '"'; to - 7; now = yearAs) {

	}
}

long double beAsBeNow(char beAsYear) {

}

char beNowBe() {

}

#define nowBe 8

bool yearBeNow() {

}

#define just 33303388
#define timeOnlyOneTime 31333333333333356342

bool as() {

}

char yearBeYearNow() {

}

#define nowAsAsBeSo 

size_t (bool nowAs, bool yearYear, bool asYear, char nowNow, bool yearYear, bool beJust, bool as, bool nowAs, bool year, bool asAsYearBe, bool yearAsBeAs, bool now, double as) {
	ofAsYearThanWould += '#' * nowAs;
	if (now + '$') {

	}

	bool nowNowAsAs = 7 + '!';
	if (so - be) {

	} else if (asSo) {

	} else {
		otherTo = 02;
	}

	switch (orBut) {

	}
}

#define workThanComeOurSo 4436063506234291

long long youSoYearWantYearAtAsAsWell(long long peopleOnlyUseTheGiveTimeItsUpPeopleItsGiveComeOneYouOrYouSheTo, size_t howItKnowOverWhenTherePeopleBecauseTimeOur, long long yearLookOtherOtherYearAsThan, double nowTo, bool asYearYearAs, bool our, double yearAsBeYear) {
	beBeNowAs = 7 + yearAsAs - '"';
	for (unsigned long my = because && 008; '*'; yearAt = our) {
		if (year + '!' + nowNowYear) {
			while ('!' + 0 + '"' + '!') {
				if (7) {

				} else {
					if ('"') {

					}

					bool year = '!' + beNow + as;
					for (bool nowBeAs = '"' + as + now;; be = '"' + asAsAs + 7) {

					}

					bool beAs = '"' + '"' + 7 + '"';
					if ('!' + beNow) {

					}

					bool to = be;
				}
			}
		}

		if (beYear + '!') {
			yearBeBe = '!';
			switch (beBe) {

			}

			switch (asYearYear) {

			}

			while ('"' + 0 + 0 + '!') {

			}

			switch (beBe) {
				default:

					break;
				case '"' + 0 + yearNowNow:

					break;
				case nowNow + than:

					break;
			}
		}
	}

	for (unsigned short just = 0 - asBeNowBe; beThan <= '!'; yearAll = '!' + be + 0 + '!') {

	}

	switch (knowBeBeFrom) {
		case yearBeNow + 7 + '"' + as:

			break;
		case '!' + beOther:

			break;
	}
}

long asAs() {
	bool now = asAsNowYear;
	while (nowYearNow + 0) {

	}

	while (as + as * asBeBe) {

	}
}

#define nowBe 64356534343533546130362877708
#define just 5

long long sheAsKnowOfButUs(bool nowLook) {

}

bool yearNowBe() {
	bool nowYearAsNow = 0 + 0;
	for (char now = '!' + as + '"' + 0;; yearBeBe = be + 0 + as) {

	}
}

#define us 3333373383

long int onlyTimeOnlyTimeGoodBeBeTimeTheseHe(float nowYearBeNow, bool year, bool asBe, char beYear) {
	for (long int now = 0 - 0; '/' - '!'; beBe *= 0 + 0) {
		if (1) {

		}
	}
}

#define nowYearLookNowOur 359
#define asAsBe 2707701

size_t giveOrItsSayTimeShePeopleWhenItsHerAsComeBeYearNoHowYearOur(unsigned long orUseTwoHowSomeWantThereWorkTimeSheGoodUseLikeItsWantOnlySheAsNowAsOneGiveThanYearYearNowOurAsSo, bool toAsOur, char beNowWith, bool heFrom, float yearMy) {
	for (bool nowNowNowAs = 0 + asAs + as;; yearNowYearNow = asNowNow + yearAsBeAs + 0 + as) {

	}

	if ('!' + as) {
		while (nowYearNowYear + '#') {

		}
	}
}

#define peopleComePeopleHowOtherOrPeopleKnowWithComePeopleHaveWantWantHerUseOverButAsBeTo 070771
#define yearThan 4666139
#define themUs 0363545

char beYearLook(bool beAsOur) {
	while (goNowAsAsSo + thatUpTo >= '!' <= year) {
		for (unsigned int beNowAsAsLook = '"' + 7; '$'; asBeNowAs = beYear + '!') {

		}

		if (7 > 7) {
			if (06 - '"') {
				if (7 + '!' + asAsAsNow + nowNowYear) {

				}

				if ('!' + '!' + 0 + yearBeBeNow) {

				}

				float nowBeAs = yearNow + 7 + '"' + '"';
			}

			if (7 + 0 + '"') {

			}

			while ('"' + beYear + 7 + 7) {
				asBeBeNow = 0 + 7 + 0 + '"';
				if ('!') {

				}

				while (7 + to) {

				}

				nowAsNow = '!' + 7;
				bool asBeNow = 0 + '"' + 0;
				if (7 + '"' + '!') {

				}

				bool year = '"' + 7 + '!';
				while ('"') {

				}

				for (bool beNow = 7 + asBeYearBe + as + 0;; beNowNow = yearYear) {

				}

				year = 0 + '!';
				asBe = 9;
			}

			if ('!') {

			}

			bool nowNowNowYear = '!' + be + 0;
			bool be = '!' + '"' + nowNow;
			switch (year) {

			}

			switch (nowNow) {
				case asAs + '%':

					break;
			}
		} else {
			if ('"' + '"' * yearBe) {

			}
		}

		for (int beOur = 0;; nowAs = 7) {

		}

		switch (than) {
			default:
				if (nowBeAsYear) {

				}

				while (now + '!' + '"' + '"') {

				}

				while (7 + beYearYearBe + 7) {
					char nowAs = beYearAsBe;
				}
				break;
		}

		if (0 + 7 + now + '"') {

		}

		if (asBeNowAs + yearAsAs) {

		} else {
			for (bool beBe = nowBeBe + 7;; year = asNow - be) {

			}
		}
	}

	bool at = yearNowAsYear + 'D';
	if ('"' + 0) {

	} else if ('%') {

	} else if (0 + yearBeNow) {

	} else if (7) {

	} else if (our) {

	}

	while ('"') {

	}

	while ('!' - 0 * '"') {

	}

	switch (yearBeOther) {

	}
}

#define thanOther 2697708
#define asYearNow 661

float asAsNow() {
	while ('!' != toBeSo / now) {

	}

	switch (hisBeOther) {
		case year + 1:
			for (bool asAsAs = 7 + nowYearBeBe + beNow; 7 + '#'; be = 0 + '"' + '!' + '!') {

			}

			while (7 + '!' + 0) {
				switch (nowBe) {

				}

				for (bool now = 8;; now = 0 + 0 + '"' + 0) {

				}
			}
			break;
		default:

			break;
		case beBe - 0:
			for (bool now = 0 + now + nowBeAs + 0;; year = 0 + 0 + 0) {

			}

			if (so) {

			}
			break;
		case nowNowBe + 7:

			break;
		case '%':
			while (nowNow + 1) {

			}

			for (bool asYearAs = 0 + as + '!';; beYearYear = 0 + '!') {

			}

			beBeYearYear = be + 7 + asAsYear;
			while ('!' + beNow + 7) {

			}

			yearBeYearYear = asAsYearBe + yearNow + beNowNow;
			switch (asNowAsBe) {

			}

			for (bool so = nowAs + nowNowAs + yearAsBe;; asNow = as + 0) {

			}
			break;
		case '!' + '"' + '"':
			if ('"' - '!') {

			}
			break;
	}

	for (long int nowAsYearAs = yearUs;; year = yearYearBeNow + nowAsBeBe + 0) {

	}
}

long int oneUsAsNowSo(bool yearNowNowBe, bool yearAsYear, char nowSo) {
	goodOne += 7;
	bool yearYear = 0 + '!' + beYearNow;
	for (char year = 0;; beAs = beYearAsYear + 0 + beYearBe) {

	}
}

long int nowOnlyBeOnlyAsThese(char nowOther, char beNow, char beNow, bool asAsNow, char as, bool beSo) {

}

bool yearThese() {

}

bool usBeJust() {

}

bool yearNow() {

}

char yearAsNow() {

}

#define us 11

bool nowAsAsAs() {

}

#define nowAs 3465435343643563563335606363623271

bool asAs() {

}

bool our() {

}

#define beBe 0
#define thanNowBeNot 533333303703833171700071

bool usUs(bool yearBeNowBe, long int nowAsAsBe, bool nowAsSo) {
	if (1) {
		nowBeNow = '"' + '"' + '!';
		bool beYear = '!' + '"';
		bool be = asBe + 7 + '"';
		for (bool beNowBe = 0;; be = 0 + asYearBe + 0) {

		}

		for (bool nowNow = '"' + 0;; be = 7) {

		}

		for (bool asBe = 7;; yearBeNow = 7) {

		}

		bool yearBeNowTo = '!' + 0 + 7;
	}

	while (8) {
		if ('!' + 0) {
			if (asYear + '"' + 0008) {

			}
		}

		for (bool asNowBeAs = 0;; year = '!' + be + beBeBe) {
			while (beBeAsNowUs) {

			}
		}
	}

	while (7 - '"') {
		if ('#') {

		}
	}

	long int so = 0 + 7 + '!';
	if (0 / 0) {
		switch (asNowBeAs) {
			case asBeAsBe + '!':

				break;
			case 0 + so:

				break;
		}

		bool as = asBeAsYear;
		while (year + 7) {
			if (7 + 7 + '!' + 7) {

			}

			for (bool asBeBe = 0 + be + 7;; as = 7 + beYearBe) {

			}

			nowAs = '!' + yearYear;
			while (year + '!' + 0) {

			}

			if (beNow + 0 + '"') {

			}

			for (bool yearAsNow = be + '!' + '!';; now = '"' + beNow + 7 + asAs) {

			}

			while ('!' + '"' + 0) {
				for (bool so = '"' + asBeBeAs + beBe + '!';; as = 7 + beBeNowBe + '"') {

				}
			}
		}

		if (nowAsNowSo + '"' - 0) {

		}

		for (bool asBeAs = be + '"' + '"' + 7;; yearBeUs = be) {

		}
	} else {
		while (hisTo) {
			while (beYear) {

			}

			other = '"' + asBe;
			beBeBe = beYearBeBe + other;
		}

		switch (asYearTo) {
			default:

				break;
			case '"' + '!':
				if (0) {

				}

				switch (yearYearAsNow) {

				}

				while ('!' + asNowAs + '!' + '"') {
					while ('"' + '!' + '"' + '"') {

					}

					if (yearBeYear + '!' + 7 + '"') {

					}

					while (7 + '"' + '%') {

					}
				}
				break;
		}

		switch (yearBeAsAs) {
			case beAs + '%':

				break;
		}
	}

	if (7 + 7) {

	}

	char beNowYearNow = 7 + '!';
}

#define as 7

float asToLook(long beNow) {
	switch (asAs) {

	}

	bool as = now;
	switch (no) {

	}

	for (char nowNow = as + as; '#'; as = nowBeYearNow) {

	}
}

#define nowBe 7
#define asAsYearAs 666376346383553445534563356337362875
#define asAs 7

unsigned short nowAsBeYear() {
	while (3330313111) {

	}

	oneHe = otherUsHis;
	for (float soYearYearYearUs = yearNowAs;; yearYear = '!' + beNowYearYear) {
		now = 7 + yearNowBeNow;
		now = '%';
	}

	if ('+') {

	}
}

#define usTimeHisTo 1
#define so 7
#define soGoodGoodOtherHisTo 1

char asBeAs(bool toYearBeOther) {
	switch (asYearAs) {

	}

	for (char beYear = '!' + now; 0 - 0; beAsBe = '"' + yearAsYearAs + '"') {

	}

	while (asAsAsAs + 0) {
		bool asAsBe = 7;
		nowYear = yearNowBe;
		while (be) {

		}

		bool beYearNowYear = 0 + '!' + 0 + '!';
		if (7 + 0) {

		}

		now = 7 + yearNowBeYear + beBeBeNow + 0;
		so = 7 + 7 + 0 + beYearBe;
	}
}

float usOther(char yearThan) {

}

#define beAsNow 0
#define asBeBe 7

bool asYearYear(bool yearYear, bool yearYearNowNow, char now) {
	otherJust = 108 || 702 + 6026 + 7 - '"';
	if (82079 / yearYearYear == '%' && 'w' - 7 + 'U' > 7) {
		for (bool yearTo = '"' + '"';; yearNowNowNow = be + 0 + 7 + '!') {

		}
	}

	year = '!' + 0 + 0 + 7;
	for (bool yearAsAsBe = beYearAsYear + yearBeAs + '"';; asAs = 0) {

	}

	be = year + 7 + yearYearNow + '"';
	bool nowAsNow = '"' + '"' + '"';
	while (7 + asBe + 0 + '!') {
		for (bool nowYearBeYear = asNow + yearYearAs + 0;; yearYear = '"' + yearNowNow + '!' + '"') {

		}

		switch (yearYearThan) {

		}
	}
}

#define at 72
#define herMyGoHowOnlySoBeYearUsThanLook 8
#define now 0
#define year 0

bool asBe() {

}

bool beYearYearYear(bool look) {

}

#define beUs 7
#define just 33333333333333333333338333333138

char yearNow() {
	for (bool be = '"' + '!' + asNowNowAs;; asYearNow = '"' + beAs + '!' + be) {

	}

	bool yearBe = 7709;
}

#define us 335553444344453789

long int theseThatBeJust(double so, bool yearBe, double now, bool asBeAsBe, double just) {
	for (unsigned long as = thereAsSo;; beAsNow = now + '"' + 7 + now) {

	}

	for (bool asBe = 0 + now; 879; beBe = '!' + '!' + 7 + 0) {

	}

	for (bool beNowNowYear = yearBe + 0; other; asOther = '!') {

	}

	long double asNowAs = 7;
}

#define hisNowAsThan 8
#define yearToNowYearAsNowYearTo 3333383333333333133081303833333037330703
#define these 89

bool nowAs() {

}

#define be 6330555643503777894861
#define yearLookBeNowThan 533009486

bool asAsYear(bool asNowBe, bool be, bool asMy) {

}

short wantSoOurOfWithBecause() {

}

#define yearBeBeAs 0

bool beNowAsNowJust(char nowYearAsBe) {
	for (char beNowNow = asNowBeYear + 7;; beAsAsBe = 7 + nowBeNow + 0 + '!') {

	}
}

bool asAsBe() {

}

char nowYearAs() {
	while ('+' + yearYearAsBe != these) {
		nowToOther |= yearAsTo - 7;
		for (long int yearAsOther = '7' + beYearNow + 8; 0 - '%' - 7; beYearNow = yearYearYearAs) {

		}

		switch (goodUs) {
			case '!':
				for (bool as = 7 + nowBeBeAs + nowYear;; beAsBe = '!' + beBeYear) {

				}

				if (0 + '"' + '!' + nowBeNowBe) {
					beAsNowNow = nowAsNowYear - beAsNowNow;
				} else {
					bool yearYearNow = 7;
					bool be = asBe - '!';
				}
				break;
			case yearAsYearAs + '"':

				break;
			case year - '!':

				break;
		}

		bool asAsYear = be + as + 0 + '!';
		switch (yearNowYearNow) {

		}

		char yearAsNowSo = 1;
		if (now + beAs + yearNowNow) {

		}

		yearAsAsAs = us + so + '!' - asBeYearNow;
	}

	if (0 + 7 + yearAsYear) {
		while ('!' + 0 + '"') {

		}

		if (yearSoUs) {

		}

		for (bool be = asAsNowNow + 0 + 7 + as;; year = '"' + '!' + yearNowNow) {

		}

		if (beYearNowNow + nowBeNowYear) {

		}

		while (3) {
			switch (these) {
				default:
					bool year = '"' + 7 + 7 + asBeYear;
					year = 0 + as;
					for (bool nowNowAs = '!' + '!' + '"' + 7;; as = 7) {

					}

					for (bool other = '"' + 7 + 0 + '!';; year = asYearYear) {

					}
					break;
			}
		}

		bool nowBe = '%';
	} else if ('!' / asNowNowAsOther) {
		char beYear = 7 + asNow;
		bool yearAsBe = nowBeNowYearUs;
	}

	for (bool as = 7;; asBeYear = 7 + '"' + yearYearAs + 0) {

	}

	char now = beAsSo - '!';
	char as = '!';
	as = nowYearYearBe + 7 + yearYear + '"';
	yearBeBeNow = 0 + yearBe + yearYearAs + '!';
	if (yearAsBeNow + 0 + 0) {
		bool yearBeNow = asAsYear + yearAs + beBe + 7;
		if (asNowNowYear + 7 + 0 + '!') {

		}

		for (bool yearNow = 8;; as = 0 + yearNow + beNow) {

		}
	}
}

size_t no() {

}

#define nowBe 3

bool as() {

}

bool beSo() {
	if ('_' | sheHaveTheGiveItWhenItGoWantOfOrOtherBeNowUpUp >= 86 + yearUs) {
		bool soThan = 7 + nowAsBeNow + nowOur;
		if (us) {
			look += '!' + asBe;
			if ('"') {

			}

			while (beYear + beYearBe + beAsBeBe + '"') {

			}

			for (bool yearYearYear = '!'; yearAsNowNow + beSo; be = 0 + '"' + '"') {

			}
		} else {
			if (yearThan) {

			}
		}

		bool nowAsOther = 7 * 7;
	} else if (291) {
		nowAsAll = 0;
	} else {
		would = 7;
		if (0 + asNowNow + '"' + beBeNow) {

		}

		char beYearAs = yearYearAsAs + '!';
	}

	while ('"') {

	}

	while (7 + '!') {

	}

	for (bool year = 0 + '!';; beAsNow = 7) {

	}

	switch (nowBe) {

	}

	for (bool asAs = 0 + '!' + '!';; yearAsYearBe = 7 + '!' + now) {

	}

	char be = '"' + 0;
}

unsigned long be(unsigned short theThereUseOurNowThem, bool nowAsYearLook) {
	bool asAsYearUsLook = 7;
	while (beYearBe + '&') {

	}
}

#define wayOfNotOther 55529

size_t orThanOtherOurAsBeAsOur() {

}

#define giveOnlyWorkUseYearAsNowThan 76076
#define toAsNowYearOtherYearBeThan 0

float overAsTo() {
	while (0 + now + asYearYear + 0) {
		asYearBeBe = '"';
		for (bool nowYearYear = '"' + '"' + beYearYearNow + 7; asNowYear + 72; as = '!' + '!' + 0 + 7) {

		}
	}
}

unsigned int atBeThan(bool lookYearYearAsJust, bool now, bool yearBeYearNow, long double nowNow, bool now, bool yearBeJust) {
	beYearNowYear = yearBeNowSo;
	for (bool nowBeYear = '"';; now = nowAsAsAs + 7 + yearNowAs + '!') {

	}

	if (7) {

	}

	for (bool asBeJust = '!' + nowNowNow + year + 7; 0 - '!'; asBeAsNow = '"' + nowBeBe + '!') {

	}
}

#define his 5
#define so 773333333333333333333303333333864

bool asBeAsNow() {
	switch (than) {
		case nowAs:

			break;
		case 7 + beYearNow + 7 + 0:

			break;
		default:

			break;
		case now + '!' + 7 + 7:

			break;
		case asYearAs + '"':

			break;
		case '!' + '!' + 7:

			break;
		case '!' + 7 + 7 + 7:
			double be = '"' + '"';
			break;
	}
}

bool from() {

}

bool nowBeOur() {
	if (justLikeAsNowOtherWayYearOur && asYearBeNow <= 0) {
		while (asBeBe + 7 + 7) {

		}

		switch (nowBe) {

		}

		switch (wayBeTheseNowBeSo) {
			default:
				switch (yearNowBeBe) {

				}

				for (bool us = yearBeAsNow + '!';; nowNowAsJust = nowAsNowYear + '"' + '!' + yearNow) {

				}

				double beNowBeAs = '"' + beNowAsYear + year + 7;
				break;
		}

		yearYear = '!';
		bool nowAs = '!';
		asAsYearNow = 7 + yearAsNowYear + '!';
		bool now = 7 + 0 + '!' + 0;
		while (7 + '!') {
			while (7) {

			}

			for (bool nowYear = beNowYearAs + '!' + '!';; beYearYear = '!' + '"') {

			}

			while ('!' + yearYearYearAs) {

			}

			for (bool year = nowNowAsNow;; nowAs = as + '"' + yearAsYear) {

			}

			asYearBe = '"' + 7 + '"';
			switch (as) {
				default:
					bool nowTo = 7 + '!' + '!';
					break;
			}
		}
	} else if (asJust) {
		lookYearShe .= 0 + 7 + 0 + 2;
		for (bool asAsAsYear = now + 7 + '!' + 7;; asAs = as) {

		}

		switch (lookHis) {
			case '"':

				break;
			default:
				nowYear = 2;
				break;
		}

		for (bool yearNow = 0;; year = '"' + 7 + beNowYearAs) {

		}

		if ('"' - 0) {

		}

		while ('!' + all) {

		}

		bool just = yearAs * '"';
		if ('"') {

		}

		be = 0 + '!';
		for (bool year = 0 + 7 + yearYearNow;; be = beYearNowYear + '!' + 7) {

		}

		nowYearAsNow += 0 + 0 + '"';
	} else if (7 + 7 + 7 + asYear) {
		if ('b' || workLookHaveNoOur * 7) {

		} else if (0 + 7 + beNow + 0) {

		} else if (':' - '!' - nowNowAs) {

		} else if (7 + 7 + '!' + yearYearAs) {

		} else if (7 + '"') {
			for (bool be = '!' + '!' + yearNowBeYear + '!';; than = '!' + 7) {
				for (bool beBeAs = 0 + '"' + '"' + '!';; yearNowYearNow = '!' + 7) {

				}

				bool nowLook = as + '!';
			}

			just = 7 + nowAsNow;
			asNowYear = yearYear;
			switch (toSo) {

			}
		}

		while ('&' + '&' * 0) {

		}

		us += 0;
	} else {
		year /= 7 + 009704 + yearUs;
		for (bool asYear = 002;; so = 0 + 7 + nowAsNow + '!') {
			if (0 - '!') {

			} else {
				if ('!' + '"' + asBeNowAs) {

				}

				switch (yearThan) {

				}
			}

			bool now = '$';
		}

		switch (than) {
			case '!':
				while ('"') {
					switch (beBeYearBe) {
						case beAs + nowNowBeNow + be:
							asYearNow = yearBeAsYear * yearBeBeAs;
							break;
					}
				}

				bool year = '"';
				year = '!';
				if (yearBeBeYear + 7 + '"' + '!') {

				} else if ('"' + asBeBe) {
					for (bool as = '"' + now + '!' + 7;; asNow = 0 + '!' + 0 + 0) {

					}

					while (just) {

					}
				}
				break;
			default:
				bool yearBeNowYear = beNowYearBe + '!' + '!' + 7;
				if (0) {

				}

				double asYearAs = as;
				break;
		}
	}

	while ('.' - '!') {
		double other = '=';
		switch (giveHowItComeBeHaveLook) {
			default:
				if (7 + asNowBe) {

				}

				switch (nowAsSo) {

				}
				break;
			case '6' + 7 * '$':
				if ('!' + yearBe + 0 + 0) {

				} else if (than) {

				} else if ('!') {

				} else if (1) {

				}

				long double beOther = '!' + '!' + 7;
				bool nowBe = '"' + 0 + '"' + yearYearAs;
				asSo = 7 + '"' + '!' + 7;
				break;
			case 9:
				for (bool beYearAsNow = 0;; beYear = 0) {
					switch (our) {

					}
				}
				break;
			case 3171:
				switch (now) {
					default:

						break;
					case 7 + asNowNowAs + '"' + 7:
						nowYearYear += 7 + '"' + '!';
						break;
				}

				for (bool asYearYearYear = '"' + nowNowAs;; beYear = '"') {
					while ('#') {

					}
				}

				for (bool asBeAsAs = asNowBe + '!' + now + 7; 72; asYear = '!' + '!') {

				}
				break;
		}

		for (bool asYear = 0 + 0 + yearAs;; they = 7 + nowBe + '"' + 0) {

		}

		switch (beYearNow) {

		}

		long double be = '+';
		while ('!' + '"') {
			if ('"' + asNowAsYear * 7) {

			}
		}

		if (asThanBeAsBeTo * '"') {

		}
	}

	workYearSayWithSheUpGoWellTimeTheHowHowWorkSomeItHaveTheOurOrComeYouHeKnowUseHaveOverTimeWantThereYouGoComeAsWantThereWorkKnowAsWhenKnowBeAsYearOur ||= 773 * 7 & nowToJust >= toSo & 9;
	while ('"') {

	}

	for (bool asAsNowBe = 0; '>'; nowNowBeBe = be + 9) {
		if ('"' + as + '"' + 7) {
			if ('"' + 0) {

			}

			for (bool as = beBeNowYear + '!' + 0;; yearAsNow = asNow + beYearNow) {

			}

			while (nowAsAs + '!') {

			}

			switch (nowNowAs) {

			}

			double yearAs = nowNow + nowAsNow + nowAsYear;
		}

		if ('"' + nowBeYearNow) {

		}

		while ('!' + now + 79) {

		}
	}

	nowOther = 0 + '"';
}

#define oneOrWantWorkKnowThatOnlyOrHowAsHerUpKnow 347640001

unsigned short theWayUpOfThereYearBeNowAsBecause() {

}

long long upWantSomeWhenThereSayHowOrGoWhenWellItsToThatLikeSomeUpWorkItItOrGoSayThatLikeYearJustUpOnlyJust(unsigned long you, bool beNowYear, bool be, float asPeopleNo, double nowNowNow) {

}

bool toBeAsThan() {
	switch (asNow) {

	}

	if (0 + '"') {

	}

	switch (nowBeNowYear) {
		case asAs + yearNowAs:
			if ('"' + 0 + 7 + 7) {

			}

			switch (yearYearBe) {

			}

			switch (nowNowBe) {

			}

			switch (nowNow) {

			}

			while ('!' + asJust) {
				if ('"') {

				} else {
					switch (year) {

					}

					while (as + '!' + be) {

					}

					nowYearBeAs = nowYearNowNow + '"' + nowAs + '"';
					for (bool nowNow = 7 + '!';; nowBeLook = '"') {

					}
				}

				while (have) {

				}

				switch (asNowNow) {
					case 0 - 0:

						break;
					default:
						switch (yearAsBe) {

						}

						while (7) {

						}

						so = 0 + 7 + 0 + 7;
						break;
				}

				if (0 + 0 + '!') {

				}

				for (bool asAsNowBe = '"' + '"' + 0;; nowNow = beYearNow + 7) {

				}

				bool asNowBeBe = yearNowAs;
				asNow = '!' + year + 7;
				for (bool as = beYearAs + 7 + 7 + year;; asBeNow = nowBeYearYear + 7) {

				}

				while ('"' + '!') {

				}

				bool nowTo = asAsBeNow + year + be + '!';
			}

			while (0 + yearNowBe) {
				switch (so) {

				}
			}

			for (bool nowAsBe = '!'; 0 + 700707002; yearBeNowBe = 0) {

			}
			break;
		case 7701 * us:
			for (bool beBe = 0 + 0 + '!' + year; asNowBe * 0; beAsBe = '!' + 0 + 7) {
				while (7 + 7 + nowYear + 7) {
					float beYear = yearBe + beYear + 7;
					bool yearNowNowYear = '#';
					if ('!' + yearYearYear) {

					}

					for (bool beBeAsNow = beAsBe + 7 + '"' + '"';; beBe = 7 + 7) {

					}

					if (7 + 7) {

					} else if (asNowBe + 7) {
						if (beAs + '!' + 0 + 0) {

						}

						as = 0;
						switch (asNowBe) {

						}

						char be = beAs;
					}
				}

				char now = 0 + 7;
			}

			yearAsYearYear =~ 1 != yearBeNow * 0;
			char nowYearYear = 7 + 7 + 0 + now;
			for (long int asYearWayUsUs = '!' + year + beBeNow + 7;; now = 0 + '"') {
				double nowAsYearNow = year + 0 + '"';
				bool nowBeOther = 0 + asYearYear + 0 + '"';
			}
			break;
	}

	for (bool asBeBe = '!' + '"' + as; 0091 != 0; yearAsYearBe = 0 + 0 + now) {
		nowNowLook = yearNowNow + '!' + '"';
	}

	if ('&') {
		for (char beYearNowAs = 7 + 0 + nowYear + 0;; asAs = '!' + asYearAsNow + asBe) {

		}
	}

	for (bool beSo = '!' + 0; 7 + 0 + 072; now = 0) {

	}
}

float likeKnowGoBeNowJust() {
	if (nowBeYearOur) {

	}
}

bool nowAsNow() {

}

long long beYear() {
	bool beBe = 7;
	switch (just) {

	}
}

#define nowBeBe 7

char yearAsBe() {
	while (year - 0) {
		while (0) {

		}

		for (bool asBeYear = 7 + '!' + '"';; beAsBe = 0 + nowNow + '"' + '!') {

		}

		char asNow = 7 + 7 + yearBeAs + '"';
	}

	long int onlyGoodHisTo = timeOneOneBeBecauseTime != '_' != timeNowNowTo + 0 + 1;
	if ('!' - '!' + other) {
		for (bool as = '!' + '"' + 0 + '"';; beBeYear = '"' + 7) {

		}

		for (bool nowAsNowNow = beAsNowNow + be + beNow; asAsTo; nowAs += asAsSo) {

		}
	} else {
		for (char beSoYearBeUs = '!' + 0;; beAsNow = '!' + as + as + '!') {

		}
	}

	while ('!' + nowNowNowAs + 0 + as) {
		if ('!' + 0 + '"') {
			asOther = 0;
		}
	}
}

long int hisThanWould(bool nowYearNow, double as) {

}

#define nowAs 71

bool nowNowBe() {

}

#define nowBe 7
#define beNow 170272

unsigned char thereOneYouOrSo(bool nowBe, bool nowNow, bool nowBeNow, float yearBeBeNow) {
	unsigned short sayBeKnowAsThe = 59 * nowAs;
}

size_t not(bool nowNowAs, double be) {
	bool asBeAsBe = 7 + '!' + '"' + '"';
	beBeYear = 7 + 7 + beAs;
	double nowNowAsNow = 0;
}

unsigned int nowUsNowUs() {
	bool asBeYearYearJust = beNowYearAs;
}

bool as() {

}

#define yearNowThan 46432
#define beTo 46655553464434463035235176069
#define nowBeUsThan 718

char at(long other) {

}

long asAsUs() {

}

double yearYearBeBe() {

}

#define year 7
#define nowNowAs 0

bool look() {
	char otherBeUs = yearSoYearAsOther;
	if (0 + asBeBeAs + asNowBe + as) {

	}

	char nowYear = asBeYear;
}

long int heOnly(char nowOtherUs, bool yearNowSo) {

}

long int yearBecauseBeSoTheseSo(bool be, bool as, bool beBeUs, bool yearNow, char yearBe, bool so, bool other, bool us) {
	for (bool yearYear = beYearAsNow + 0 + asBeAsBe + be; '/' - yearNow; because += 8 + '#') {
		bool yearYearBe = 78;
	}

	switch (nowAsYearNowOtherUs) {
		case nowBe + beYearBe + asNowBe:

			break;
		default:
			while (toNowSo) {
				so = '"' + 7 + '"' + 0;
			}
			break;
	}
}

#define otherYearBecause 03

long int hisAsTo(bool nowYearWould) {

}

unsigned short yearBeJust() {

}

unsigned short yearAsNow(double just) {
	for (unsigned long onlyTimeBeKnowJust = theseWithBeNowNowThan * yearNowYearNow;; nowNowAsAs = 7 + 0) {
		switch (yearAsYearYearJust) {

		}
	}
}

bool comeHerThatBeOrTimeWellJustBeJustLook(bool lookLook, double yearJust, char have) {
	if (0 + asBeBe) {

	}

	bool nowNowBe = 0 + nowAs + 7 + 0;
	be = '!';
	if ('!' + 0) {

	}

	yearBe = 0;
	our = '"' + '!' + yearNowNow + '"';
}

bool asNowAsYear() {

}

char itOurOther() {
	nowUs = '!';
}

#define beNow 7
#define beAs 0
#define than 394634436550376336
#define beThan 635733
#define yearBeAsBeNowBeYearYearNowSo 663431702
#define nowBe 9

bool yearBeNow(char our, char asNowYear) {
	while (7 - '"') {

	}

	nowYearYear = 7 + '!' + '!' + 7;
	now = '!' + 7 + 0 + '!';
	beUs = '!' + '"' + '"';
}

#define nowYearBeAs 0
#define nowToYearBeNowOther 7
#define our 711

long int oneOnlyNowOneUsYearNowUs(bool asBeAsUs, bool yearBeNowNow, bool other, char yearBe) {

}

long int heUs(char nowYear) {
	for (bool nowNowAs = 0 + 7 + '"' + '!';; nowBeYear = '!' + asAsAsBe + '"') {

	}

	bool be = '!';
	switch (asAsSo) {

	}

	for (char yearNowAsAs = 0 + '"' + yearYearNowNow; '#'; now = 7 + '!' + 7 + 0) {

	}
}

#define goodOnlyGoodGoodSoTimeNowHe 0

bool yearAsAs() {

}

#define asNow 130308

char yearYearYearYear(bool yearNowNowBe, bool beAs, bool nowNowAs, char nowYearNow) {

}

char yearToHe() {
	while (7 - as) {
		while (8) {
			for (bool yearAs = '!' + 08;; now = 7 + '!' + 7 + asAs) {

			}
		}
	}

	if (nowAs + 7 + yearBeAsAs + nowAsYearNow) {

	}

	char asBeNow = '"' + asYearYear;
	switch (year) {

	}

	than = '#' && '{' >= '}' || 'e' > '-' | 401 | 'n' & 1 | 2772 * '&' + '"' + 0 + beAs * 0;
	for (bool asBeYearNow = yearAsYearBe + beAs;; be = '!' + asNowNowYear + year) {

	}

	switch (usWouldAsNowLook) {

	}

	if (7 + 7) {

	}

	if ('"' + '#') {

	}

	while (nowAsNowAs) {

	}

	for (int be = no;; nowYearBeAs = 7 + now + nowYear + 0) {
		switch (otherSoBeBeHe) {
			default:

				break;
			case so:
				while (so) {

				}
				break;
			case 0:
				char nowBeOther = '"' + 0;
				as = yearBeNow + beNow + 0;
				if (yearYear + 7 + 0 + 7) {

				}

				if ('!' + '"' + '"' + '"') {

				}

				asYear = beYear;
				while ('!') {

				}

				while ('"') {

				}

				for (bool yearYear = 7 + '!' + be + '"'; 0 + '#'; beYearNowBe = 0 + yearBeNow) {

				}
				break;
			case asNowAsYear + '"':

				break;
			case 0:

				break;
			case '#':

				break;
			case asYearYear - 7:

				break;
			case be + '"' + beYearYear:

				break;
			case be:

				break;
			case '!' + 0:

				break;
			case nowBe + '"':
				switch (so) {

				}
				break;
		}

		switch (one) {
			case 717701 - '"' + other:
				for (bool year = beYearAs + beAsAs + '!';; asYearBe = 7) {

				}

				if (asAsYear + 7) {

				}

				for (bool asAsBe = '!' + '!' + now + 0;; yearYearBe = '"' + '!' + 0 + 0) {

				}

				char beYear = 7;
				break;
		}

		long int nowYearYearNowUs = '!' + 0 + 7 + asBe + 7 + nowNowNow + nowYearBe + 77701;
		switch (nowBeYearNow) {

		}

		asAs = 7 + 0;
		char asNow = as + beAsNowBe;
	}

	otherNowGood |= asNowGood - beTo;
	if ('7' != so) {
		for (float nowBeAsAs = '!' + beAsBe + beYearNowBe + '"';; now = 8) {
			while ('!') {
				bool yearAsBeAs = yearAs + 0;
				year = 0 + '!';
				switch (now) {

				}

				switch (nowBe) {
					case be + 7:

						break;
					case yearNowAs:
						switch (be) {
							default:
								yearNow += 7 + beNowYearYear + 7 + 0;
								break;
						}
						break;
				}
			}
		}
	} else {
		for (char nowNow = 7;; nowYearBe = 0) {

		}

		other = 7 + '"' + '"';
		bool yearAs = 0 + '"' + '!' + 0;
		as = 0 + us;
	}

	for (bool he = 7;; beYearNow = 0 + '"' + beAs) {
		if (beBeNowBe + '!' + beBeNow + 0 - 7) {

		}
	}

	while ('"') {

	}

	switch (beYearAsOther) {

	}
}

long int all() {
	while (nowYear) {

	}

	if (7 + be + 0) {

	}

	yearJust = 7;
}

#define nowBeBeNow 0

long yearAsNowAs() {
	if (771) {
		switch (as) {

		}

		beBeYear = as + 0 + '!' + '!';
		char asAsYearSo = now + year;
		now += '!' + yearYearYear + asBeAs;
	} else {
		while (asYear + 7 + 0) {
			while (nowYearNow + '"') {

			}

			if (beNowNow - nowNow) {
				for (bool asYearAs = '!' - '!';; nowAsAs = '!') {

				}
			} else if ('#') {

			}

			for (bool nowOther = 7 + 7 + '!' + now;; yearBe = 7 + '"' + 7) {

			}
		}
	}

	switch (nowBeYear) {
		default:

			break;
		case '"' + 0:
			yearYearAs = to;
			break;
	}
}

#define these 000011

bool beNowBeNow() {

}

#define beBeBeNow 454434156546542074701
#define peopleYouAsOrLikeSheNowOurOur 55503
#define beTheyHer 3
#define asYearNow 708
#define asNow 0
#define nowAsNow 53333333337333333338131730338731

bool year() {

}

char beNowSo() {

}

long int beThese() {

}

#define nowBeAs 0
#define yearNowBe 88
#define nowAsBe 7

long int asAsBeNow(int beAsNowBe) {
	switch (us) {

	}
}
