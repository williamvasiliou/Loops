#define nowNowOtherTo 333333333733301

long int theseAsUsBeTimeBecauseUsAsBeNowOther(long int nowNowUs, bool so, bool beAs, bool asBeAsBe, bool nowAsBeNow, bool beNowAsYear, bool as, char beAs) {
	switch (yearBeYearNow) {
		case 7 + 7 + 0 + be:

			break;
		default:

			break;
		case year + 7:

			break;
		case '!':
			for (bool asYear = 7;; asAs = 0) {

			}

			bool nowSo = 7;
			break;
	}

	switch (toSoAsNowTo) {
		default:
			for (bool year = '"';; beBeNow = nowYear + 7) {

			}

			if (asBe + year) {
				bool yearBeYearBe = 0 + nowBeAsYear + beBeNowAs;
				for (bool now = nowAs + 0 + asAs;; as = 8) {

				}
			}
			break;
	}

	for (bool asAsBe = now;; beYear = 7 + beAsNow + 0) {

	}

	asYearAs = 0 + yearNowBe + yearNow + yearAs;
	beNowNowYear = 0;
	bool year = '"';
	switch (nowAsYear) {
		default:

			break;
		case nowYearAsYear:

			break;
		case 0 + '!' + '!' + '"':

			break;
		case beBeAsNow + year + now + '"':

			break;
		case '"' + year:

			break;
		case '"':

			break;
		case 7 + now + 0 + asAsAsYear:

			break;
		case 7 + 7:
			asNowAsBe = '!' + '!' + '"' + be;
			if (as + beYearNowYear + 7 + 7) {

			}

			bool other = 0 + '"' + 7;
			break;
	}
}

#define nowNowSoYearOther 31

bool as() {

}

#define to 2

char beNowAsNow(bool yearBe, bool look) {

}

long long asAs() {
	switch (thanWaySoBeBeAsBeOur) {

	}

	while (0702) {
		if (beNowYearNow + '!' + '!' + '"') {
			for (bool nowYearYearNow = 0; but; asYear = yearBe + asNowAsAs + 0) {
				while (9) {

				}
			}
		} else {
			double nowAsAs = 0 * '!' * yearAsBe;
			all = asNowYear + 7 + 0 + '!';
			for (bool asNowNow = 79;; as = '!' + '!' + year) {

			}
		}

		bool be = 7;
		bool asBe = year + 7 + now;
		bool asNow = nowYear;
		double nowAsYearNow = 7 + '!' + '"' + now;
	}

	yearAsToNowOur += '!' + 7 + asBeBe + yearBe;
}

unsigned int have(float beAsBe, bool all) {
	beBeNow /= '"' + 0;
	double be = 0 + '!' + nowAsBeNow;
}

unsigned long nowThan() {
	while ('"' + nowAs) {
		switch (beYearYearAs) {
			case '!' + 0:

				break;
			default:

				break;
			case 7:

				break;
			case look:

				break;
		}
	}
}

long long beBeNowBe() {
	bool beBeOur = toOfBeFromOrOtherNowOther == itsSheGiveSoOnlyWorkOr < 7;
	for (bool look = now + 0;; yearBeBeBe = '"') {

	}

	for (char beNowNowBe = 0 + yearYear;; yearBeYear = '"') {

	}

	beNowBeBe = so - year;
	switch (wayBeBeMy) {
		case '!' + '!':

			break;
		default:
			double as = asBeNow + '"' + '"' + 7;
			char as = 7 + yearAsYearAs + yearAs + 7;
			break;
	}

	for (bool yearNowNowAs = beYearBeYear + year + yearNowNow + 7; 66 * 374; than -= 0 + 0 + 0 + 0) {
		for (bool nowBeYear = yearAsYear + 0 + yearYearBeYear + 7;; asNowAs = yearBe + 7) {

		}

		for (bool nowAsBeNow = asBe - yearAsBeBe;; nowYearYear = year + as) {

		}

		if ('"' + yearYear) {

		}

		switch (as) {

		}

		switch (yearBeAs) {
			default:
				for (bool yearYearNow = '"' + yearAsNow + 0 + 7;; be = nowAsBe + nowYear + 7 + 0) {

				}

				for (bool yearNowNowAsAsOther = 7 + as + '!' + 0;; nowBe = 7 + '"' + 0 + '!') {

				}
				break;
		}
	}

	bool nowNowYearYear = asAsBe + 7 + 0;
	so -= 0 + 0 + '!' + 7;
}

#define beBeYearBe 7

int asLook() {

}

#define be 333333333333333330808
#define hisOther 3333177038
#define onlyGoodNowOtherYearYearAsOther 7
#define now 3
#define us 0
#define year 6

bool beYearYearBe() {

}

bool as() {

}

#define asAs 7
#define nowAsBeYear 00309
#define yearBe 0

int asAs() {

}

#define nowNow 0
#define yearUs 7

char nowBeYear() {
	toLook = 0 < nowBeAsBe;
	while ('(' + '!' - '"') {
		bool look = 8;
		char nowNow = 7 + 0 + '"' + '"';
	}

	if (oneAsLook + '&') {
		long as = 08109;
		if ('0' + 0 + us) {
			double year = 7 + 0 + asAsBe;
			while (nowYearNow + asNowYear) {
				asBeAs = asYearAs + 71;
			}
		}

		while ('"' + nowBeYear + asAsYear) {

		}

		switch (beYearYearYear) {

		}

		for (double asBe = be + 7 + year;; beYear = 0 + '!' + '"') {

		}

		while ('$') {

		}
	} else {
		bool otherAsSo = usAsAsSo - 0;
	}

	bool now = '!';
	while (now + 0 + '"') {

	}

	if (0) {

	}

	double beAsBe = yearAs + 0 + 7;
}

#define ofTo 7746877776

double beAs(bool yearAsBeBe, bool yearAsNowAs, bool so, double yearAsBe) {

}

#define beNowBe 2

unsigned short asOur(bool other, bool beAs, bool now, bool beOur) {
	char yearYearYear = '"' + '"';
}

#define herSoYearUs 618033311

long int yearBeNowYear(bool other, char nowNowYearBe, float beNowBeBe) {
	while (yearNow + nowSo) {

	}

	bool asYearYearBe = '"' + yearAsNowAs;
	yearTo = '!' + 001;
}

#define onlyGoodGoodOneOnlyHisOther 301

char yearNow() {

}

#define onlyOnlyTo 38030070001
#define asUs 7
#define because 3
#define now 70878
#define asNowAsSo 043366835701
#define howThanThem 5533

long int onlyTwo(bool asBeBeAs, double asYearNowAs, bool look, bool nowBeNowYear, long double year) {
	switch (asBeNow) {

	}

	be = asYearYearNow;
	for (bool nowYear = 0 + 7;; other = 0) {

	}
}

size_t yearAsYearBe() {
	yearYearYear = '"' + as + '!';
	if (yearNowYearLook * nowYearNowAs) {

	}
}

#define nowNowNow 352505

double nowNowNowAs() {

}

size_t yearSoSo(bool yearBe, unsigned char asAsBeBe) {
	while (goodTimeBeGoodHeBeNowUsBeAsOther) {
		for (bool yearBeAs = 7;; beNowBeYear = 0) {

		}

		for (bool us = '!' - asAsUs; '+' / nowNow + nowAsBeAs + 0 + '"' + '!' - year; nowYearSo = 7) {

		}

		while (nowHis + 0 / beAs) {
			switch (beNow) {
				case '/':
					nowYearBeBe = 7 + '%';
					now = 7 + beAsYearYear + asNowBeYear;
					for (char beNowBe = 0 + 0;; now = beBeNow) {

					}
					break;
				default:
					char beNow = yearNowAsAs + '!' + beYear + 0;
					break;
				case '"' + '!' + 0 + '!':

					break;
				case 0 + beNow + 7 + '"':

					break;
				case beNowAsAs + '!' + '!':

					break;
				case 7 + beYearBe + yearBe:
					switch (be) {

					}

					while (0 + 0 + '!') {

					}

					for (bool beYearNow = '"';; nowBeAs = asYearBeYear + 7 + '"' + '!') {

					}

					if (0 + 7 + 0 + beBeNow) {

					}

					year += '"';
					break;
				case other:

					break;
			}
		}
	}

	switch (usTo) {
		case asYearNow + beAs:
			switch (be) {
				default:
					char yearBeNow = nowYear + 7 + '!';
					break;
			}
			break;
	}

	switch (now) {

	}

	if (yearNow + nowNow + '!') {

	}

	bool nowYearNow = '!' + '!' + 7 + 0;
	for (bool beNow = 7 + '"' + 0 + 0;; yearNowBeNow = beBeAs + asAsAsBe + 0) {

	}

	if (0) {
		for (bool asNow = 7 + 7 + 0;; asAs = nowBeBeYear + '!' + asYear) {

		}

		nowNowYearAs = 7 + nowBeNow;
		switch (as) {
			default:
				switch (beNowAsYear) {
					default:

						break;
					case 7:

						break;
					case asYearYearBe:

						break;
					case '"' + '"' - 7:

						break;
				}
				break;
		}
	}
}

#define yearSoSo 33311
#define because 73378001

long int yearNowYearBe() {

}

#define toTo 801
#define now 7
#define yearThan 7
#define nowYearAsAs 4336556646646534553633645575089
#define toAsBeOur 5
#define yearOther 203
#define asBeBe 533333333333333313333833330033371

long int beNowNowAs(char beBeBe, char nowBeNowNow) {
	yearHe -= yearYearYearYear + '"' + nowBe;
}

bool nowBeNow() {

}

double nowYearNowYear() {
	switch (thanNowHis) {
		default:
			for (char asYearYear = 0 <= '"';; nowNowAsBe ||= '"' * look + '%') {
				bool asNow = '"';
				long double yearNowAsYear = yearAs + as;
			}

			for (bool yearNow = 7; '$' + '!' + 2; asYearBeAs = 7) {

			}

			if ('#' / beYearYearAs) {
				if (7 + beNow) {

				} else if (year + beAs + 7) {

				} else if ('"') {
					bool year = 8;
				}
			}

			with = 7;
			asNow = 7 + 7;
			if (be + asAsNow + '!' + '!') {

			}

			if (beNowAs + '!' + yearNowNow) {

			}

			switch (yearAs) {

			}

			unsigned short nowBeYearBe = 9;
			break;
		case 7 + asBeAs + beAs:

			break;
		case '"' + '"' + asNowBe + 7:
			for (double yearBeYear = 8;; nowYear = '!' + beYear + 0 + asNow) {

			}
			break;
	}

	unsigned long beToBeOurWith = 0 + '"' + 7;
	for (bool other = yearYear + nowNowBe + '!' + yearYearAsNow;; yearYearNowYear = 0) {
		if (0 + 7 + '!') {

		}

		while (be + asAsNowBe + '"') {

		}

		if ('#') {

		}
	}

	asYearYearYear = as;
	if (7) {

	}

	if (7 + asAsAsBe + as + 0) {

	}

	for (bool now = nowNow + '"' + yearAsYearAs + nowAsBeAs;; yearYearBeBe = 0 + 7 + 7) {

	}

	beOur = '!' + '!' + '"';
}

#define year 483

long have(bool asNowBeUs, bool our, bool year, double asAs) {
	while ('!' + '!' + '!') {
		while (nowBeBe + '"' + 0) {

		}

		switch (yearBeNow) {

		}

		bool year = '!' + '!';
		bool beBe = 7 + asYear;
		nowBeBeNow = nowAsBe + 7 + '"' + yearNowBeAs;
		asAsNow += '!' + 7 + 7;
	}
}

#define yearOtherThan 0

short look(char yearAsYear, bool be, bool beAsBe, double beAs) {

}

#define beAsJust 0

bool be(int asYearNowBe, bool beNowYearBe, bool yearNowBe, double nowAsBeYear) {
	for (bool beYearBeBe = 0; beYear + '%'; yearBeBe = asNowNow + beAsBe + 0) {

	}

	for (bool beAsYear = '"' + '"' + 7 + be; otherBecauseOther + '%'; beYearNowAs = be) {
		if (yearNow + yearBeBeBe + beYearAs) {
			switch (beYear) {
				default:

					break;
				case nowYearAs + '!' + nowNow + '"':

					break;
				case yearBeYearAs + 7:
					switch (beSo) {

					}
					break;
			}
		}

		if ('!') {

		}

		if ('!' + 7 + yearBeAsAs) {
			beNowAs = beAs + 7 - 0;
		}

		while (nowAs + yearAs + 7 + '"') {
			while ('!' + '"' + '%') {

			}
		}
	}

	onlyHisOneGood *= now + asNowBe + 778;
	long int oneHeBecauseNowUs = 77373 / 0;
	if (1) {
		switch (beTo) {

		}
	}

	for (bool yearNowNow = '!' + yearYearAs + nowBe + '!'; yearYear + yearSo; nowAsBeNow = '"' + 0 + 7 + 0) {

	}

	while (nowBeBeNow + 7) {
		switch (asNowYearAs) {

		}

		for (bool yearYearBe = 7 + 0; 0 + '#'; nowNowNowAs = yearBeBe) {

		}
	}

	for (bool nowSo = '"' + '!' + '!' + now;; yearYearYearAs = asAsNowNow + '!' + beNowNow + asAsNowNow) {

	}
}

#define oneOneOther 3067790677001

int asSoNowNowThan() {
	size_t ofOverTwoItsOneSheWhenUseOverGiveKnowSayHowSomePeopleHaveGoodHowYouLikeWantTwoSayGiveItOfOneLikeTheWantHaveAsPeopleTimeAsTherePeopleAsTimeOfMy = 335543445365635464554053943 + 71 * asAs;
	if ('|' != 46643815 & '~' > 54355434355645346545463435563633644533435433445635344445553546436443533353354464343444443355433545434536314543636335664443136456496741 == 'i' >= 'a' == '8' & '}' != 440608018 >= 'l' > 45634554533696437751470011 > thereWhenYearPeopleMyTheWork) {
		yearWellSheJustNowOtherGiveTimeUpJustComeBecauseThan += '+' && theYearWhenLookAsBeAsNowSo + '2' - 7;
		haveSomeItsSomeOnlyItOrLookTwoItsSoNowItMy ||= workThereOneYouAsToToYearHe >= 'n' == fromOfSomeKnowUseBeNowThemAsJustNowYearLook;
		while ('%') {
			switch (say) {
				case 7 * beAsNow:

					break;
				case beNowNowAs + 7 + '"' + 0:

					break;
				default:

					break;
				case beYearYearNow:

					break;
				case beAs + asNow:
					if (asNow) {

					}

					while ('&') {

					}
					break;
				case 7:
					for (bool nowYearBe = '"';; beBeAs = 7 + asNowYearAs + 7) {

					}

					while (nowNowBe + 0 + '!' + beNow) {

					}

					if (asNow + '"') {

					}

					char yearNowNowAs = 7;
					break;
			}

			bool be = yearLook + 0 - 7;
			if (beNowYearBe + 7 * '!') {

			}

			bool year = '"' + '!' + '"';
			bool asBe = 2;
		}

		while (0 + '"' + 0) {
			bool nowAsAsNow = '!' + '"' + '!' + 7;
			while ('%') {
				while (7) {

				}

				for (bool beNowNowYear = asAs + '!' + '"';; as = '!' + '!' + '!' + 7) {

				}

				now = 7 + nowBeYearYear;
				char asNow = yearNow + 7 + '"' + beNowYear;
			}

			switch (look) {

			}

			switch (nowAsAs) {

			}

			if ('%') {

			}

			char asYearYear = 0 + 0;
		}

		while ('/' + '%') {
			switch (beBe) {

			}

			yearTo = 7 + 7;
		}

		bool his = '!' + other;
		while (0 - as - 0) {
			for (bool nowBeNowNow = '"';; yearAsAs += '!' + 7 + '"') {

			}
		}

		hisJust = asAsNowYear + 0;
		beAs = '!' + '"' + '"';
		switch (nowNowAs) {
			case 1:

				break;
		}

		for (bool asAsAsYear = '"' + nowAsNow;; now = beYearBeYear + be + '!') {

		}

		while (0 + 0) {

		}

		if ('!' - nowYearBe) {

		}
	} else if ('@' == 83 + '"' - 0) {
		switch (nowNowNowBe) {
			default:
				if (yearNowAsNow + 7) {
					yearAsYearNow = 0 + 0;
					switch (yearOurYearOther) {

					}
				} else if (nowAs + asNowYear) {
					switch (asNowNowYear) {
						default:
							bool asNowYear = 7 + yearNowNow + '"' + '!';
							double as = 7 + '!' + 7 + '"';
							break;
					}
				}

				itThanUs &= nowBe % yearAsNowNow + '!' + '#';
				nowYearAsBe = yearNowNow;
				for (bool asNow = 7;; asOurAsJust = 7 + 0 + '!' + '"') {

				}
				break;
			case asYear % '!':
				for (char yearBe = now + '!' + 0;; now = 7 + asNowAs) {

				}
				break;
			case '"' + 708:

				break;
			case 7:
				while (our) {

				}
				break;
			case yearAs + '!':
				as += '!' + nowAsAsAs;
				break;
			case 0 + nowNow:

				break;
			case '"':
				while ('"' + '"') {

				}

				year = year + 7 + 0;
				switch (now) {
					default:

						break;
					case asBeBeAs + 7 + 0:

						break;
					case nowAs + as + '"':

						break;
					case '"' + '!':
						switch (beYearYear) {
							default:
								if (asTo) {

								}
								break;
						}
						break;
				}
				break;
		}

		switch (yearNowBeYear) {

		}

		for (bool as = asNow + '"' + 7 + 7; be <= beBeNow; be = be + '!') {

		}

		bool year = '"' - '"';
	} else if (6278 - 2 + '!' + as + yearYearYearYear - 0) {
		bool nowAs = be + 0 + beNowNow + yearBeBeAs;
		for (int yearTheseOurYearYearAsBeThan = '"' / '!'; '"' - '!'; asYear = nowAs) {

		}

		int yearNowAsYear = '"' + '!' + '!' + 0;
	} else if (butWellJust + as + to) {
		asBeNowYear = '"';
		beNowNow = '!' + 7 + be + '"';
		while ('"' + 7) {
			while (0 + asNowNow + year + 0) {
				if (nowAsBeYear + yearYearAsAs + year) {
					while (0) {

					}

					beAsAs = 0;
					for (bool beBe = '!' + now;; so = '!' + 0) {

					}
				}
			}
		}
	} else if (asNowTo) {
		asAsThem &= '.';
		switch (nowBeThey) {
			case just:

				break;
		}

		for (bool asSo = nowBeNowSo;; yearBeNow = '"' + be + '!') {

		}

		asNow = 0;
		bool yearYearBe = '!' + '"' + year;
		switch (beYear) {

		}

		switch (beOther) {

		}
	} else if (46565364436544454340566536408 || 'E') {
		if (nowNowThan) {
			for (bool yearNowThan = 7 + '"' + 7 + nowAs;; as = beBe + '"') {

			}

			switch (yearUsYearNowLook) {
				default:

					break;
				case 0 + now + beNowYearYear:
					so = nowAsAs;
					break;
			}

			while ('&') {
				switch (asBe) {

				}

				while (nowYearNowBe) {

				}

				switch (be) {

				}

				while ('!' + asAsAsYear + 0 + 7) {

				}

				if (7 - '"') {

				}
			}
		}

		while (beNowNow + '!' + 7 + 7 * 7) {
			while (beBeBe) {

			}

			bool yearBeNowNowLook = 0 + 0 + '"' + beNowNow;
			for (bool yearYearAsAs = '"' + '"';; yearYearNow = '"') {

			}

			bool now = 7 + 0 + 0 + '!';
			for (bool asBeNowNow = '!' + '!' + 0;; nowBeNow = 7 + beBeNow + yearBe + 0) {

			}

			for (bool as = nowBeBe + beYear + 7;; beAsAsBe = beLook) {

			}

			switch (beUs) {

			}

			while (other) {

			}
		}

		nowYearAsNow = yearYearBe + 0 + beAsAs + '"';
		switch (now) {

		}

		switch (asYearYearYear) {

		}

		bool other = '"' + asAs;
	} else {
		yearAs = 0 + would;
		if (0 + '!' + 0 + '"') {

		}

		while (1 + 7 + 8070078) {

		}

		switch (herBeBeYearTo) {
			case 7:
				bool yearYearBe = '"' - asNowYear;
				break;
		}
	}

	switch (asNow) {

	}

	while (one) {
		while ('"' - nowYearAs * '$') {
			if ('!') {

			}

			if ('"') {
				if (beAsAsAs * 0) {

				}
			} else {
				while ('"' + '!' + 7) {
					while ('"') {

					}

					yearAsAsBe = 7 + year;
					switch (beNow) {

					}

					bool to = beNowBe + 0 + 7 + '"';
				}
			}
		}

		if (8) {

		} else {
			while (nowBeAs * '!') {
				char yearYear = beBeAsBe + 0;
			}
		}

		if ('%') {
			while (7 + beAsYear) {

			}

			be = asNow + '!' + yearAsAsNow + yearNowBe;
			nowBeBeBeThan = '!' + 7;
		}
	}

	for (char beYearNowBeNowThanUs = 7777079; overThemSomeFromHowAsOur + yearAsBeNow * '"' <= asBe; nowYear += '"') {
		switch (asYear) {

		}

		for (char asAsAs = 0 + '!';; nowYear -= yearAsAs + 7 + beAs) {

		}
	}

	switch (asThan) {

	}

	while (0 + '!' + '"') {

	}

	while (78) {

	}

	char nowAsYearLook = 0 + '!' + 0;
}

double knowOurJust(bool asYearBe, bool beNow, double asAsNowBe, int lookMy, bool justGoodNowAsAsTo, double other) {
	size_t beYear = asYearYear;
	while ('"' + 7) {

	}

	if (0 + 0 + '!') {
		for (char now = beBeAsBe + now;; nowBe = yearNow + 7 + '"') {

		}

		for (bool our = yearAsYearNow + beBeBeBe + 0 + asBeYearBe;; be = 0 + '"' + beBe + 7) {

		}
	}
}

double asBeOur() {

}

#define youBeYearOur 563259
#define beLikeOneLikeSo 81
#define yearYear 8
#define nowGood 46501

bool nowYear() {

}

long yearAsYearYear() {

}

#define nowNowAsYear 7
#define now 007709
#define asAs 3685368
#define otherLookToToJustOurYearAsAsThan 57987032002

long long justBeUsFrom(unsigned int herItGiveComeNotLookThat) {
	if (nowAs + nowAsYear) {

	} else {
		for (bool yearAsAsBe = asAsBeBe + be + '"';; asAs = 0 + '!' + 7) {
			nowYear += '"' - 0;
		}

		bool beAsBe = 8 - '"' - be;
	}

	for (bool nowBeNow = beYearBeNow + '"' + 0;; yearAs = year + nowAsYear + yearBe + 0) {

	}

	for (bool asAsAs = 7;; now = nowYearYear + '"' + 7) {

	}

	if (look) {

	}
}

double nowBe() {

}

bool other(bool yearBeAs, bool beFrom) {
	for (bool as = '!' - asYearAs; '"' + '"' + 1; yearNow = 7 + 7) {

	}

	for (bool soTo = 317808 + 07171;; year = '"' + '"' + '"' + 7) {

	}

	char so = 0 + '!' + 7;
	for (char yearYear = be;; now = 7) {

	}

	bool beYearNowBe = beYear;
	bool asBe = 7 + '"';
	switch (beYear) {

	}

	asYearNow = '"' + '"';
	if (0 + 0 + be + asBeNow) {

	} else {
		while (year + '"' + yearAs + 0) {
			if ('!' + '"' + '!') {

			}

			switch (asYear) {

			}

			bool asAsBeYear = 08;
		}
	}
}

#define becauseYearHisAsYearOther 738
#define oneOneBecause 0
#define toOnlyBeSo 70333333303333

long int he(double beYearNow) {
	while ('"' + yearNowBe + beYearYear) {
		while ('"' + '"' + '"') {

		}

		while ('!' + asBeBeNow + '!') {
			switch (just) {
				case 0 + year:

					break;
				case our:

					break;
				case 7 + 7 + '"':

					break;
				case 0 + 0 + asAsBe:

					break;
				case 2:

					break;
			}

			switch (now) {

			}

			while ('"' + asYearBe + year) {
				while (asYearBe + '"' + asAsNowBe) {
					bool nowNowBe = '"' + other;
				}
			}

			for (bool nowYearYear = beNowNowAs; 7 + asYearYearLook; now = '!' + 0 + '"') {

			}

			switch (look) {

			}
		}

		while (7 + '!' + 0 + '"') {

		}

		while (nowBeAsBe + as + beNowNow) {

		}

		while (yearAsAsNow + 7 + '"') {

		}

		switch (beBeYear) {
			case '"' + '!' + '!':

				break;
			case nowAs - '"':

				break;
		}
	}

	if (030538 + asOther) {
		switch (asYearAsYear) {
			default:
				for (char asBeNowNow = 0 + 7; 'n' + '"' - yearNow; yearYear = year) {

				}

				for (bool year = '"' + '"' + asYearNow + yearYearNowNow;; nowYear = 7 + 0 + '"' + '!') {

				}

				float now = 0 + 0 + 0;
				bool year = 7;
				switch (beThan) {

				}
				break;
			case '"' + '!':
				switch (now) {
					default:

						break;
					case '&':

						break;
				}
				break;
		}

		be = 0;
		if (beNowBe * '"') {
			nowWay = 2;
		}

		switch (onlyBeAsBeJust) {
			case 0 + beNow + 7 + 7 - asYearNowNow:
				while ('!' + nowAs + now + 7) {

				}

				while ('$') {

				}
				break;
			default:
				for (bool nowAs = '!' + '"' + 7;; beBeYearNow = '!') {

				}

				if (beNow + nowBeBe + 0 + '"') {

				}

				switch (beNowNow) {
					case '!':

						break;
					case '"':
						char yearBeAs = '!' + asBe + 7 + as;
						break;
				}

				bool yearYear = '"' + '"';
				for (bool be = than;; yearAs = year + beBe + '"') {

				}
				break;
		}

		bool thanTime = 1 | nowYear;
		double as = as + '"';
		bool nowOther = '"' + '"' + 7;
	} else if (7 - '!') {
		if (0 + beBeYearNow + 0) {

		}

		nowWell = as + yearBeNowYear;
		while (nowNowBeYear) {

		}

		double yearAs = '"' + nowYearNowBe;
	} else {
		while (3 + 7 * nowNowBeYear) {

		}

		switch (other) {

		}
	}

	for (bool yearNow = be + asBeYearNow + '!';; yearBeYear = 0 + 0 + 7) {

	}

	nowSo += '"' + 7 + yearNowYearYear + '"';
}

#define us 45

bool nowNowAs(bool beAsNowYear, bool year, bool just) {
	for (char nowAs = 7; '%'; now = 7 + '"' + 7) {
		asBeBeBe = 7 + asYearNowAs;
		if (0 + be) {
			switch (beYearYearBe) {

			}

			for (char now = 7 + '!' + '"';; yearNowYear = yearNowAsBe) {

			}
		} else {
			bool as = 0 - '"';
		}
	}

	beNowBe |= '"' + '"' + '!';
}

#define yearSoOneOne 13

long int goodOneHeOneBeBecauseSoSoTo(long int he, bool oneSo) {
	while (beBeNow + nowBeAsAs + nowAsYear + year) {
		switch (now) {

		}

		bool asAsNow = be + asNowNowAs + beYearAs + 7;
		beAsTo = '!' + 0 + asYearBe;
	}
}

long int usOnly() {
	switch (other) {

	}

	switch (yearNowNowAs) {
		default:
			if (nowNowBeAs + nowBeNowBe + 7 + 0) {
				us = asNow + '"';
			}
			break;
	}
}

bool nowAsNowNow() {

}

long int beButBeNowToMy() {

}

#define asBeAs 32751
#define allAsNowAsOur 34652455408

char yearThanOtherUs() {
	switch (year) {
		case 0 + beAsNowAs + '!' + '"':
			switch (nowAsYear) {

			}

			asBeYearAs = 7 + year + 0 + 0;
			while (7 + asYearAsBe + year + 7) {

			}

			if ('!' + 7 + 0 + 7) {

			} else if ('!' + now + yearBeYear) {

			} else if (asNow + yearNowAsAs + nowNowYear) {
				while (0) {

				}

				switch (beNowAsYear) {

				}

				for (bool nowBe = '"' + nowBeBe; 0 * 7; yearYearBeAs = be) {

				}
			}

			switch (so) {

			}
			break;
	}

	bool asNow = 7 + '"' + 7;
	bool nowAs = yearTo;
}

#define workSayKnowYearWithNowToBeAsNowNowTo 2

int usAsOther(char beAsYearYear) {
	switch (oneItNowYearNowUs) {
		case 0 + '!' + asThan:

			break;
		case '"' + 0:
			for (bool nowAsBeBe = 0 + '"' + '"';; beNowYearYear = '!' + be + 0) {

			}

			if (7 + yearYear + '!') {

			}

			bool nowTo = beNow;
			break;
		case 0 + 0 + our:

			break;
		case '%':

			break;
	}

	if (0 + '!' + '"') {

	} else {
		asBeNowNow += '!' + as + yearNowBeAs;
	}

	if (other) {

	}
}

#define oneAsTwoGoodAsNowNot 5553335656495405901

unsigned int just(bool asNow, bool nowNowAsYear, double beAs) {

}

int beYearYearBe() {

}

#define asBeAsYear 0
#define our 7

bool asAsNowYear() {

}

bool asAsBeBe() {
	if ('!') {

	}

	for (bool as = 7 + '"';; beBeAsBe = nowYearAs + '!' + 0) {
		while (beAsAsAs + '!') {
			bool beBe = '"' + asBeYearBe + yearYearYearNow + 7;
			for (bool beAsBeNow = nowAs + '!' + 0 + beBe; '&'; asBeBeYear = '!' + '"' + asYearNowBe) {
				us = 0;
			}

			yearOther += yearAs + '"' + nowAs + asAs;
			while (other - 0) {
				switch (yearYearAs) {

				}

				for (bool beAsYearYear = nowOther;; yearNowAs = yearBeYearNow + '!' + 7) {

				}
			}
		}

		while (3803 != theseTheseOnly) {

		}

		while (yearAsBe + '"' + '!' + '"') {

		}

		for (long int nowOneOtherHe = '_' != because; 3 - '!' + 1 - beNowBe; beAsUs = nowBe + 7) {

		}

		switch (nowYearNowNow) {

		}

		beBeYear = 0;
		switch (year) {

		}

		bool yearBeNow = 7 + 0;
		for (bool beAs = nowNow + 7 + yearYearNowYear + year;; nowNow = 7 + beNowYearNow + beYear) {

		}

		while (0) {
			nowYearBe = yearYearAs + '!';
			bool nowNowNow = 0 + '!';
			while (nowBe + nowBeAsAs + '!') {

			}

			if ('"' + '!' + 0) {
				char year = yearYearAsAs + 0 + as;
			}
		}
	}

	switch (yearAs) {
		case 7 + '"' + 7:
			while ('?' + yearNow + 1) {
				yearBe = nowAs - asBe + us;
			}

			beSo = '"' + '"' + '!';
			if (nowNow + nowNowNow + 7) {

			}

			beBeAsYear += '"' + '!' + 7 + '"';
			break;
	}

	bool beBeAs = 7 + yearBeAs;
	switch (nowBeBeYear) {
		case 7 + year + '"' + beNow:

			break;
		default:

			break;
		case asYear + asYear:

			break;
		case 7 - 0:

			break;
	}
}

long int yearGood(bool asAs, bool nowNowBeNow, double now) {
	bool nowBeBeYear = 0;
	for (char asYearNow = 0 + '!';; be = '!') {
		char yearBeBe = '3';
		bool asGive = 48108 && thanHowBeYearOther;
		while ('!' + as + 0 + 7) {
			double nowNowAsYear = 0 + 7;
		}

		switch (asOther) {

		}
	}

	while (9) {
		if (nowBeBe + 7 + 7) {
			switch (beBeYear) {
				default:
					as = 2;
					break;
			}
		}
	}

	someOther += nowNowNow + yearAsAsBe + '"' + 7;
	bool nowBeOur = '!' + '!';
	while (078) {

	}

	while ('!' + '!' + 0) {

	}

	bool beYearYear = '!' + 7;
	if ('!' + '!' + beAsBeAs) {

	}

	if ('!' + 0 + be + asBeAs) {

	}

	while (beYear + be + yearYearBeAs + 0) {

	}

	if ('!') {

	}

	nowBeNowYear += '!' + '!' + 0 + nowBeAsAs;
}

#define thereThatKnowWorkBecauseNowThan 741

double butYearAt(bool asYear, bool than, char nowAs, double be) {

}

double other() {

}

bool look() {

}

#define yearNowNowYear 78
#define beYearAsYear 7

long double beAsAsYear() {
	switch (otherYearNowOther) {
		case '!' - now:
			switch (goodAsSo) {

			}

			switch (so) {
				case nowBeNowYear + beAsNow + as:

					break;
				default:

					break;
				case asAs - 0:

					break;
			}
			break;
		default:
			bool nowYear = year + '"';
			yearUs = 0 + so;
			break;
	}

	switch (oneUsBeOtherOther) {
		case 08:
			switch (beTo) {

			}

			for (bool beAs = '!' + nowBeAs + '!' + '"';; nowBeBeYear = beYearAsYear + '"' + 7 + '"') {

			}

			while (nowNowBe - beAs) {

			}
			break;
		default:
			while ('"' + asNow + '"' + yearBeYear) {
				beYearAsAs = be + '!' + '"' + '!';
				for (bool be = '!' + as + yearAs + '"';; asNowBeNow = yearNowYearNow + '!' + 7 + '!') {

				}

				if (year + nowAs + 7 + asYearAs) {
					bool nowAsBe = '"' + '"' + 7;
					if ('#') {

					}
				}
			}

			if (heBeHis) {

			}

			while (yearSo) {

			}
			break;
	}

	toUs += 71 + beBeYear + '!' - 7;
	if (0337707008) {

	}

	asNow = beYearYearBe + '!' + 7;
	if (0 + '!' + '"') {

	}

	if ('#') {

	}
}

long int beUs(long int theseOne) {
	bool asTheseUs = yearYearAs;
}

#define good 7

int beYear() {

}

#define yearBe 5405
#define yearAs 0
#define yearNowAs 7

bool beAsNow() {

}

bool just() {
	while (508907007003 + '"' < beAs) {

	}

	peopleToComeWellTheySo += '>';
}

unsigned int giveYouThereComeOtherNo(unsigned short onlyPeopleThatOfComeUpOneWhenOfGoTwoKnowWhenOneWhenUseLookGoodUpLikeHerOfGoJustAsBeUs, float lookLikeHerBeSheComeThan, double asYearSoNowAsAsNowYearThan) {
	if (0 & 22077008) {

	} else {
		be = 7 + '"' + nowNowBe + 0;
		bool asYear = beBeBeBe + 7 + 7;
		while ('!' + beNowYearBe + 7) {
			switch (beOur) {

			}

			switch (yearAsAsOur) {

			}
		}
	}

	for (bool yearBe = yearNowNow + 7;; year = 7 + 0 + nowNowNowNow + be) {

	}

	if ('!' + 7 + '"') {

	}

	toSoAsAsAsNowBeNowAsOther = no;
}

unsigned short onlyItHerKnowThereUseUpOtherThereSheWorkButWhen(long int theseYearBeTo, bool yearAsAs, bool beAsAsNow, double beAsAs) {
	while (yearBeAsAs + be + '!') {

	}

	for (bool thanOther = 7 + asAs + '"' + '"';; beAsAs = asAs) {

	}

	nowNowYearYear = 0 + '!';
	if ('"' + '#') {

	} else if (beNowNowAs) {

	} else {
		if (year) {

		}

		if (2) {

		} else if (beAs + '!' + asAsYearBe) {

		} else {
			double be = 0 + '!' + 7;
		}
	}

	beBe = '&';
}

#define overBecause 7
#define beYearYear 7

bool year() {

}

bool now() {

}

bool asNow() {

}

#define beAsNow 7

bool asYearNow() {

}

bool yearAsYearBe() {

}

#define beBeNowYear 5666535446464435633594455361317301
#define come 01

bool beSo() {
	for (long int asAsBe = '!' + 7 + '!' + '"'; '#'; beYear = '"' + 0) {

	}
}

#define onlyGoodTimeOnlyGoodOnlyOnlyOnlyGood 3333
#define oneHeOnlyAsYearTime 3331
#define soUs 44665679
#define way 53443433555665446356736547008862
#define nowJust 533373
#define asAsNowYear 7

bool as() {

}

bool beYear() {

}

bool asYearNow() {

}

#define yearNowNow 33333333333333333333033383133170333

bool now() {

}

bool nowAsAsAsYearBeNowOther() {

}

#define yearBeAsAs 7
#define beJust 73

bool yearBe() {

}

#define asYear 08087771

double yearAs() {
	while ('w' | '`' + '!' + '#') {
		switch (justHaveFromAsYearOur) {
			default:
				if (7) {

				}

				bool our = '!' + 0 + 0;
				break;
		}

		for (bool asYearYear = 0 + '!' + now + '"'; '"' + nowAsBeAs + '!' + '$'; nowSo = '!') {

		}
	}

	if (':' & upHisTime * '>') {
		for (bool nowBeAsNow = '$';; nowYearAsBe = 0 + 0) {

		}
	} else if ('"' + as + '!' + 7) {

	} else {
		switch (be) {
			case '"' + '"':
				bool now = 0;
				switch (asNowLook) {

				}
				break;
		}
	}

	switch (asItsJust) {
		default:
			for (char goodTheyOther = '!'; '!' - 0; now = yearYearAs + '!' + 7 + '"') {

			}

			if ('&') {

			}
			break;
		case beNowYear:

			break;
		case beYearNowYear + yearAs:
			for (bool as = 7 + 0 + '"';; nowNowYearNow = as + '"' + be + '"') {

			}

			for (bool beAsBeAsThan = '!' + '!' + 7;; asAsNow = beAsYearYear) {

			}
			break;
	}

	switch (beAsBeYear) {
		case 1 + '&':

			break;
	}
}

#define beYear 09

int year() {
	if ('!' + beYearAsYear * '"') {

	}
}
