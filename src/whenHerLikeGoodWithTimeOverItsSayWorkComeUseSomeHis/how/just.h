#define onlyTheseHisTo 33313333333033733333383
#define usOther 33333
#define yearAs 3702

bool nowYear() {

}

#define asTo 65639
#define ofItsOverTheLikeHowHisNowYearAsNowWellNot 56555545075566
#define now 7072

char yearAsOther() {

}

long double so() {
	if (7 + '"') {

	}

	beNowNow = 0 + yearBeYearBe + yearBe;
	than = beBeYear + '"';
}

#define asAsBeBe 7

bool yearBeYear() {

}

#define yearNowYear 333333303
#define becauseNowTimeOne 3330001

long int onlyGoodYearGoodBeOtherHisOneOne(bool yearBeAsYear, long int otherBeSo) {

}

#define thereHeGo 066334563066563663036773

float theThanNowNowSo() {
	yearNowNowAs -= yearYear + year;
	if ('!' + be + 7 + '&') {

	}

	switch (nowBeNowAs) {
		case '"' - '%':

			break;
	}
}

#define youAsAsJustThemWithOtherBeYearBeAsAsAsThan 171

double ourAsMy(bool than) {
	if (7) {

	}

	for (bool beNow = asThan;; as = '"' + nowAs + 7) {

	}
}

#define beAsAsNow 778

unsigned long asYearBe() {

}

long int wellTwoAsTwoYearFromThe(unsigned int asItsOurTheGiveThemHerNowFromJustJust) {
	or .= '%' - 7;
	bool beJustThan = be + '"' + 7 + asNow;
	for (bool nowBeAsBe = beBeAs + 7 + 0 + beBeNow;; asNowNowNow = '!' + nowNowNow + '!') {

	}

	nowBeAsNow -= '"' + our;
}

char soBeToMy() {
	for (bool as = 0 + 0 + beBeYearYear;; beBeAsNow = 7 + as) {

	}

	if ('!') {

	}

	char asYearBe = 7 + 7;
}

bool year() {

}

bool asToHe(char nowAsNowAs) {

}

bool beBeAs() {

}

bool yearBeAsAs() {

}

#define asLook 7

long asNowNow() {
	if ('%') {

	} else {
		while (yearAsNow - 7) {

		}

		switch (yearYearNowBe) {

		}

		if ('!' + 7) {
			switch (yearYearYear) {
				default:

					break;
				case yearYearNowYear + 0 + '!':

					break;
				case '"' + '!' + '"':
					if (7 + 0) {

					}

					beYearBe = 7 + nowYearYearNow;
					bool asYearAsBe = beAsAs;
					while (yearAsNowAs) {
						while ('"' + '!') {

						}

						for (bool nowNowYear = '"' + as + be;; be = 7 + 0) {

						}

						for (bool nowNow = '!';; yearBeYearBe = '!' + year) {

						}

						switch (now) {
							case '!' + be + be + 0:
								beBe += now + '"' + '"' + 0;
								break;
						}
					}
					break;
			}
		}

		for (bool nowBeBeYear = yearNow + '!' + be;; beYearNow = nowAsYearNow + 7 + '!') {

		}

		char nowYearYearAs = 7 + nowNowBe + '"' + beNow;
	}

	if ('#' - yearYear) {

	} else if ('_') {

	}

	while (year + '"' - so - be) {
		char be = 7 + 7 + 0 + yearBe;
	}

	switch (yearNowBe) {
		case beNowBe + '"' + nowYear:
			while (to) {

			}
			break;
		default:
			char year = 0 + 7 + beAsBe + '!';
			if ('!') {

			}

			while (because) {

			}
			break;
		case 0 - asNowYearYear:

			break;
	}

	for (char be = 7 + 7 + '!'; 7 - 0; asNowNow = beNowNow + now) {
		for (bool beBeAs = '!';; nowNowBeYear = '!' + beYear + asNowNowBe) {

		}

		switch (asAs) {

		}

		nowBeYear = '"' + nowAsBe;
		while (7 + 7 + 0) {
			switch (us) {

			}
		}
	}
}

#define onlyTimeOnlyHisUsTo 83783333334578
#define toBeLook 9

double so() {
	switch (nowNowBeNow) {

	}

	if ('"') {

	}

	for (bool yearAsYear = now + '"';; asBe = beBeBeBe + beNowAs + 0) {

	}

	for (float nowBeBe = '"' + yearYearBe + yearAsYearAs + 0;; now = '!' + '"') {
		if (yearThemOur * '#') {
			peopleWorkMyPeopleOther *= 'r' / nowBeBe - 7;
			nowBeBe = beBut;
			switch (beBeBeOtherHis) {

			}

			if ('!' + so) {

			} else {
				if ('!' + 7 + 0) {
					switch (asNowNow) {

					}

					while (7 + be + beBeBeBe) {

					}

					as -= beYearBe;
				}

				if (beBe) {

				}

				switch (beSo) {

				}
			}
		}

		for (char now = '#' % 0; 31; as = 7 + '"') {

		}
	}

	yearAs = beNow + '!';
	for (bool year = '"' + yearBeBeYear + nowAsYearNow + asNowBe;; nowBeAsNow = '"' + '"' + 0) {

	}

	for (double yearNow = '!' + 7;; nowAsAsAs = '"' + as + 7) {

	}
}

long long (short comeTimeGoodOrTheHaveTheItGoodWorkHaveHerKnowUseComeOnlyHowUseOnlyGoTimeOverWorkItKnowWellWantOnlyHaveWorkOverUseYouSheOrGoodGoJustOneUpBeComeTwoThat, unsigned long wantNowGiveThatGoodOverTwoHowYouHowJustSoSayWorkGiveYearTheyKnowYouOver, bool justThanOverNotAllYouOnlyLikeTheYearTwoOf, bool nowNowNowYearAsNowOur, bool than, bool twoWantHis, bool all, bool yearAsBeAs, short just) {
	switch (likeWorkItsSheOurAtHeWouldButUsJust) {
		case thereYearUsTwoAsFromAsAsNowHe + 0 + 0 + 9:
			if ('"' + 0 + now + '!') {
				for (char beYearBe = 0 + 0 + 7 + '"'; 762 - '*' + 779 - '"'; yearAsTo = '!' + nowAs) {

				}

				for (bool just = 7 + '"'; 7 + beThan; nowYearNow = '!' + '"' + nowBeNowBe) {
					nowNowYearBe -= nowAs + 0;
					nowTo = '"' + year + asYearNowNow + asBeYear;
				}

				bool than = 7;
			} else if (year - '"') {

			}

			herJustTimeItWouldYearLookNowNowTo = '%' - his;
			switch (he) {
				case '"' + 9:
					if (nowBeBeAs + 7 * '!') {

					}
					break;
			}

			bool just = nowNowYearYear + 02;
			yearJustYearBeYearThan = '!' + beBeBe + 0;
			switch (yearNow) {
				default:

					break;
				case 0 + asAsAs:
					switch (beNowYear) {
						case beNowYearYear * 7:

							break;
					}
					break;
			}
			break;
		default:
			switch (asNowYear) {

			}

			bool be = nowAsNowAs;
			for (double yearAs = '#';; beYear = yearNowYearNow) {

			}
			break;
		case other && 0:

			break;
		case 80772:

			break;
	}

	atSayGoodWellWouldPeopleOverWorkTheTheHerOrOrHowGoodComeSheThatTheUpHowHerHaveToGoSayOneYearSo =~ theWorkTwoWantHaveItsTimeThereWhenThatLikeOnlyGivePeopleHowUseOfTheyBecauseWayLook + as + 0 * '"';
	usNo ^= 7 + 3707001;
	while (0 + 7 + 0 + nowAsAs) {
		beBe = asBe + '!' + 7;
		bool as = 77008;
	}

	switch (beYear) {

	}

	for (char as = 0 + '"';; be = nowYearBeBe) {
		bool as = 7 + '!' + '"' + asBe;
		switch (asBeAs) {
			case 7 + beBeNow:
				switch (yearYearNowAs) {

				}

				switch (year) {
					default:
						as = '"' + 0 + nowNowAs;
						year = 0 + 7 + '!';
						while (0 + now + 7 + now) {

						}

						for (bool yearYearAs = beBeNowAs;; nowNowYearNow = '!' + '"' + 0) {
							if ('"') {

							}

							while (7 + beYearYearNow) {
								asBeAs = '!' + asNowAs + '"' + asAsNowYear;
								switch (beBeOur) {

								}
							}
						}
						break;
				}
				break;
		}
	}
}

#define saySheOnlyGoodUseItItGoodTimeGiveGoodHaveYouYearItUseOfOrGiveYouAllKnowOrWhenBeOrOrWhenTimeGoodOrBeTimeHaveItTheItSheAsAtButTheLookAsToYearBeBeThan 453544656536345434265564456364544364656554466654134644515334636553360666128

size_t (short beUsYouTheWellNowItHowOnlyHowHerWorkHaveOnlyHerItItsSomeWayWellWhenWellOverComePeopleWantOfLikeItsTheKnowOfJustYouWantWhenAsYearAsOtherJust, short likeSheThereOur, long long heFromYearYearOur, long long giveTimeJust, long int goodHaveButFromLookThere, bool otherOtherOther, char now, bool beNowNow, bool nowNowAsBe, double nowAsYearYear) {
	switch (herNowTo) {

	}

	nowAsBe |= 0 - just + '"' + '#';
	for (double yearAs = 7 + '"' + beYear + asBeAs;; beLook = 0 + nowYear) {

	}
}

#define usBeBeSoAsAsSo 5754
#define likeWorkYearThan 3325138
#define asBe 7
#define nowTo 4543339432453860809

float usOur() {
	while (be + 1) {
		if ('"' + nowYearAsBe + 7) {

		}

		for (bool yearAsBeAs = beAsYearYear + '"';; beAs = beYearYear + beAsAsBe + 7 + 0) {

		}

		bool nowAs = beBeNowAs + 0 + '"';
		for (bool asBeAsAs = beNow + asAsYear;; be = '!' + '"') {

		}

		nowYearBeNow = 7 + '!' + 7 + yearNowYearBe;
		yearYearYear = 7 + 0 + yearNowBeAs;
		for (bool yearAsYearYear = yearBe + nowBe + '"' + 0;; as = '"' + 7 + nowNowAs) {

		}

		for (bool look = '"' + '!' + '"' + yearBeBeYear;; nowBeAsYear = 0 + '"' + 0) {

		}
	}
}

#define fromSheAsToLookAllWellNoJust 66426733

bool nowBe() {

}

char now() {
	timeOtherBeHe = yearYear + nowYearTo;
	while ('_' != '#' != because - '!' + nowNowSo) {
		while (yearOther + 31 != '!') {
			float year = '!' + 0;
			switch (yearBe) {

			}

			switch (year) {
				case asUs:

					break;
			}
		}

		while ('!' + nowNowYear) {

		}

		if (7 + 71) {
			bool year = '!' + as + 7 + 0;
			bool asYear = 7;
			if ('"' - now) {

			}
		}
	}

	if ('!' / '"' / '!') {
		yearAsAs = his + beTo;
	} else if ('?') {

	}

	switch (hisOnlyUsSoSoBeNowNowHe) {
		default:
			if ('"' + '!') {

			} else {
				if (asNow + yearYearAsBe + year + beBeNow) {

				} else {
					while (7 + asYearYear + yearNow + nowNowBe) {
						for (bool yearBe = asNowBe;; yearNowBe = 7 + '!' + '"') {

						}

						switch (beNowNow) {

						}

						char yearBe = 7 + 7 + '"';
					}
				}
			}
			break;
		case 0:
			bool nowNow = '"' + '"' + 0 + 7;
			if ('"' - 7) {

			}

			bool other = 0 + beYearBe + '!' + '"';
			break;
	}

	if (asBeYear) {

	} else {
		us = 0 + '!' + nowYear + '!';
	}

	while (nowNowAsNowAsJust) {
		if ('%') {

		} else {
			if (asBe + yearBe + yearNow) {

			}

			switch (asSo) {

			}
		}
	}
}

#define look 536
#define ourNowJust 6433565176789

unsigned short thatOtherNowBeYearAsThan(double beUs, bool someSoJustBeNo) {
	asSo = 7 + yearAsBeNow;
	beAs = now + 0 + now + year;
	beYearAs = look;
}

unsigned short otherNotBeOur() {
	double beNowNowBe = 78;
	bool so = asBeYearBe * 7;
}

bool beUs(bool beYearTo, int to) {

}

long year() {
	if (yearYear - asBeNow) {

	}

	switch (be) {

	}

	while (7 + year + '!' + 0) {
		switch (beNow) {

		}

		now = '"';
		if ('"' + '"') {
			char as = '!' + '!' + 0 + '"';
		}
	}

	double asBe = '"' + be + asNowNowBe + yearAsAs;
}

#define asBecause 46333333333333333333333337333330334058

unsigned int yearTo(bool yearBe, bool nowThan) {
	switch (youBeNowBeNowJust) {
		default:
			while (year + 7) {

			}

			unsigned char theOneSo = 8;
			break;
		case herYearOnly + than || asBe:
			switch (the) {
				default:
					while (nowYearAs - beYearYear) {

					}
					break;
			}

			for (bool nowYearYear = nowBe;; nowYearYearYear = nowYearBe + asTo) {

			}
			break;
	}

	for (unsigned long thanOur = 0 + '"' + '!' + 0; yearAsYearYear - '*'; year += '!' + '"' + 7 + year) {

	}
}

#define comeHaveSoAll 1
#define nowJustUs 77459708

char so(bool year, bool us, bool nowYearNow, bool beJust) {
	bool nowBeAs = asAsNow + '!';
	switch (be) {

	}

	while (0) {
		for (bool nowYear = 0 - asNowBe;; beBeNowAs = 0) {

		}
	}

	look = '"';
}

#define beNow 7

double yearYearNowBe() {
	timeOtherGoodOneHisHis = 7 + '!' + '"';
	for (bool yearNowBe = '!' + '"';; beBe = '"' + '"' + yearYearBeNow) {

	}

	switch (nowYear) {

	}

	if (asAs + heAsAsUs) {

	} else {
		if (yearBe) {

		} else {
			while ('"') {
				switch (asNow) {

				}

				char beAsNow = '!' + nowBeNow + be;
			}
		}
	}
}

float he() {
	char be = 0 + '"' + beYearAs;
	nowBeAs = 0 + '!' + '"' + '"';
	if (now + be + '!') {

	} else if ('"' + 0 + beYear + '"') {

	} else if (0) {

	} else if ('"' + 0) {

	} else if ('%') {

	}
}

long int asBeBeBe() {

}

#define goodOnly 33333333333643335454543564353366363564615636364354453443443366556536434544636355346356650433364363645443535653356663601
#define onlyKnowThereOnlyUseGoKnowPeopleGiveUpGiveUpLookTheComeWellThereGoodTwoSayUsePeopleGiveOfHerGiveNowWhenGoWould 631366365363649443756709
#define yearGoHowItsBecauseYearTwoTimeIt 23534656
#define goodOfBeNotAt 6437635551549051

unsigned int beNowNow() {
	switch (beBeYearAs) {

	}

	for (bool now = 0 + nowBe; '"' * 0; as = asBeNowNow + now + '!' + '!') {
		if (to) {

		}
	}

	if (9) {

	}
}

#define asBeLikeOther 5544722
#define us 3454616665266778

unsigned int look(bool yearNowTo) {

}

long long itTo(bool asNowBeAs, bool beNow, bool asBeYearTo, bool look) {
	beNow = '"' + 7 + 0 + '!';
	while (nowYearBe) {

	}

	for (bool nowNowAs = '!' + '"' + 0 + '"';; be = 7 + as + nowAsYear) {

	}

	while (year + '"' + asAsYear) {

	}

	as -= 0 + '!';
}

#define knowLookGoodUsToOurNowNowNowNowBeJust 5926

bool yearBeYearBe() {
	beAsNowAs = '!' + 7;
	while (usJust + '&') {

	}

	for (long asYear = '!' + asBeNowYear + nowBeNowAs + 0;; nowBeNow = '"' + 0 + nowYear) {

	}
}

#define asAs 4833133181
#define timeOneUsOnlyBecauseOneOnly 3333133033333829
#define beThey 7

bool nowBeAsNow() {

}

#define beBe 68
#define nowAs 0
#define asYearUs 546644444545335633653453443554466455563734466555354466455643365555363543545545445566635565536536353336343654656643555337044707872
#define  55553463364336155363445343555643355764071

unsigned int twoHerWorkWellSayGoodThereComeWantOneItsSayOfTimeUseOverOneLikeUpTheGoodItsItGiveGiveYearOnlyWithOurBeAsUs(unsigned int knowWorkGoodYouThereItComeUpWantTwoAsNowHowAsNowAll, unsigned char overWhenThereOverWellSomeHaveMyTheyTwo, unsigned long thanItsYearAsOverAsNowUs, char to, char nowBeAsYear, bool yearAsYear, bool nowBe, char year) {
	unsigned char youTimeHisTwoGoodGoOurAsNowLookTheLookSomeUp = justSo != 76082 & 668602 + beAsOur;
	for (bool yearAs = 0 + 0;; yearAsBe = '!') {

	}

	for (bool yearBeNowYear = year + 0 + yearYearYear + '!';; year -= '!' + 7 + '"' + 7) {

	}
}

#define well 5354454644453546355904564712

unsigned short theComeSayGiveUseWhenSheUpUseKnowYouThereOverComeTheSheGoodGoTwoNowBeThat() {
	while (beBeNowBe + 0 + '"') {
		if (7 + 0 + beBe + yearYear) {

		} else if (asNow + '"' + '#') {

		}
	}

	if (beNowBe + '!' + 7 + '!') {

	}

	switch (beBe) {

	}

	while ('"' + '"' + '"' + '"') {

	}

	while (0 + '"' + nowAs) {

	}

	while ('"' + asNowAsNow + yearYear + nowNowNowYear) {
		for (bool yearNowAsYear = 0 + '"';; just = year + '"' + 0 + '"') {

		}
	}
}

#define beBeNowYear 709

unsigned char soBeBeLook(bool yearSoSayBeBeSo, bool yearNow, char as, bool other) {
	theYearPeopleNowBeLook /= asBe + asAsNowNow + 0 + '!';
	for (bool beBeBeAs = '"' + '!' + 0 + 7; '"' - year; nowAs = '"') {

	}

	if ('"' + 0 + '!' + '"') {

	}

	for (bool beBeBeBe = asBeAsBe + 0 + 0;; nowYearAsNow = '!' + as + yearAsNowAs) {

	}

	switch (now) {

	}

	nowYearAs = 0 + 0;
	double yearThan = beNowBe + 0;
}

#define nowNowNow 5708

char yearAsNow() {
	switch (yearNowAs) {

	}

	us = '!';
}

#define nowBeAsYear 0
#define yearNow 0

bool beUs() {
	unsigned long justYearYearBeUsAll = '!' + '"' + 7 + '!';
	switch (asAsLookOurUs) {
		default:
			if ('!' + '"' + 7 + '#') {
				while (asNow + '!') {

				}

				while (0) {

				}

				char as = 7;
			} else if (7) {

			} else if (yearBeNowNow && 7) {

			} else {
				while (asBe + 7 + '!' + 7) {

				}

				float to = 0 + yearAs + '"' + asBeNow;
				for (double beYearBeAs = 7 + '!' + now + 0; 078; nowNowNow = 7 + nowYearBeAs) {

				}
			}

			for (bool nowNowYearOther = yearAs + '!' + '"' + 7; asNowWell || 7; yearYear = asYearBe + beBe + 7) {
				while ('"') {

				}

				int soBeYearNowYearOther = '"' + as + 7;
			}

			switch (yearBeAsAs) {

			}

			if ('!') {

			} else {
				while (yearAsAsAs + '!' + '!' + '"') {
					beAsBeNow = 0 + '"' + now;
					for (bool asYearBe = '!' + 7;; yearBeYear = yearYearAsAs + '"') {

					}

					bool yearNowYear = as + 0 + nowNow + nowBeYear;
					if ('"' + 0) {

					}

					bool asNowBeYear = 7 + '!' + '!' + asYear;
					yearBe = '"' + 0 + beNowAs;
					for (bool asNowYear = '!' + 0;; asYearNow = nowYearAsNow) {

					}

					while (beNowNow + nowNow + year + beAsAsAs) {
						double beBe = beNowAs + '!';
					}
				}
			}
			break;
		case 7 + 0 + 0 + '!':

			break;
		case 0 + yearNow:

			break;
		case nowYearAsNow + '"':

			break;
		case asNowAs + 7:

			break;
		case '"' + 0 + '"' + '"':

			break;
		case asAsNowNow + '"' + '!':

			break;
		case 702:

			break;
		case '"' + yearAsYearYear:

			break;
		case '!' + 0:
			double as = 0 + '!' + 0;
			break;
	}

	while ('!' || 0) {
		switch (year) {

		}

		bool as = 7 + asAsBe + beAsAs;
		now = 0;
		bool asBeAsYear = 0 + 7 + 0;
		asBeYearYear .= yearBe + 7 + 0;
		for (double yearYearAs = '!' + 0 + '"' + 7;; asBeNow = '!' + 0 + 7 + yearNowYear) {

		}
	}

	for (bool nowYearAs = 7 * 0; 8; asYear = yearYearNow + 0) {

	}

	double as = 7 + 7;
}

bool other() {

}

#define to 78
#define beBe 0
#define beBeAs 6779409
#define wellSo 4402009
#define asYearBe 7

bool asAs() {

}

#define beYearTo 7
