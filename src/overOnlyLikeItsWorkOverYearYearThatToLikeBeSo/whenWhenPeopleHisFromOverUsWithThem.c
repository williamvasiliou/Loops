#define timeHisOneTimeGoodTheseTimeTheseHisOtherYearBeSo 1333337030001

long int onlyOtherTimeBeSoAsNowTo(bool yearUsAsYearOther, bool asUs, bool because, bool nowYearNowUs, char beNowAs, bool look) {
	for (bool asNowNowAs = 7 + '!';; yearBeAsAs = beAsYear) {

	}

	if (our) {

	}
}

char be(bool yearNow, bool yearYearAs, bool yearAsNowYear, bool yearBeYear, bool now, bool asAsYearNow, char year) {

}

#define nowAsYearYear 7
#define asThan 0

bool asNowBeAs() {

}

bool us() {
	unsigned char yearThat = yearNowAsBe * '"';
}

#define beWhenOverUsLook 66316
#define nowAs 7

unsigned char upThese() {
	switch (now) {

	}

	bool as = now;
	while (as + '!' + 0) {

	}

	bool yearSo = 7 + 0 + beAsAs + 0;
}

#define yearTwoUseOther 0

unsigned long theseSo() {

}

#define be 08

bool to() {
	while ('"' + 0) {

	}

	for (bool yearYearAsBe = 0;; asNowBe = 0 + 7) {

	}

	for (bool year = '!' + beYearBeYear;; beYearYearBe = '&') {

	}
}

#define asSo 3454545805334504766278
#define howUpNowUs 344434

unsigned int sayAsBeAll() {

}

#define thanBut 0

double beAsBeBe() {
	yearBecause |= 181 != '!';
	bool yearNowNowYear = '!' + yearBeNowBe + '"';
	for (bool beBeYearBeBeOther = '"' + '"' + 7 + beAsBeYear;; nowNowNowBe = be + 0 + asAs + now) {

	}

	nowBeYear = as + 0 + 0 + 0;
	if (71) {

	}
}

long int onlyGood(bool beBeTo, bool hisBeYearYearOther, float us) {
	beBeNowBe = 8 / yearBeYearBe + yearOther;
}

#define onlyTimeTimeOneTheseYearThese 3383771
#define oneLook 7

char soYearBeBeOther() {
	short now = to + '&';
	while (beNowBe - beNow) {

	}

	if (7) {

	}

	if ('!') {

	} else if ('!' + '!' + 7) {

	} else {
		year = 0 + 0;
		switch (so) {

		}
	}
}

#define beNow 5455465343794043653358200208

long int work() {
	for (unsigned short as = than;; year = '!' + '!') {

	}

	for (bool asAsBe = 0;; to = 7) {

	}
}

unsigned int otherOr(bool other) {
	unsigned short asBeYearNowBeYearUs = '!' + '!' + 0 + '!';
}

#define now 333338387338

char oneBecauseAsBecause() {
	for (bool nowBeBeBe = 0;; as = '"') {

	}

	for (bool asAsYearYear = '!' + 0 + nowNowBe;; nowBe = 7 + 7 + 7) {

	}

	yearYearAsBeNowTo = beBe + 0;
}

long int oneTimeBeNowAsSo(bool yearYear, char asNowHe, char soUs, char yearOther) {
	if (nowBeAs + '!' + 7 + 0) {
		if (be) {

		}

		if ('"' + '"' + beAsNow + as) {

		}

		switch (yearAsAs) {

		}

		for (bool yearBeNowNow = 0 + year; '#'; nowAsNow = 7 + 7 + nowNowBe) {

		}
	} else {
		while (beNowAsBe + yearNowYear) {

		}

		for (bool asNowAsNow = 0;; asNowNow = 7 + 7 + asAs + '!') {

		}

		yearNowYear = 0 + '!';
		float beAs = '"';
	}
}

long int yearOnlyYearBeHisUs() {

}

long int so(int as) {

}

#define nowBe 7
#define yearYear 43446434336333664356656134439420077708
#define asBeOther 77372

char nowYearYearBe() {
	bool asNowNowAs = 7 + '!';
	while (beNowBe + nowAs) {
		if (beBeAsYear + '"' + nowNowNowBe) {

		}

		switch (be) {
			case '"' + beNow + 0:

				break;
			case '!' + 7 + now + '!':

				break;
			default:

				break;
			case 7:

				break;
			case '%':

				break;
		}
	}

	year = '"' + 0;
	if (nowYearBe) {

	}

	bool beYear = year + 5801010701 != 7 + '"' / '!';
	long int yearYearBeBe = '!' + 0;
}

long int other(long int hisBeBeToTimeBeTo, bool beAs, bool us) {
	for (bool asAsAsYear = '"' + 001;; yearAsNow = now) {

	}
}

#define oneOneBecauseOnlyBecauseOneOnlySo 333733388
#define nowNow 65834644443
#define useComeUpNowWithOneTwoLookHaveBeTo 4725721

double asFrom(bool yearYearNowAs, bool lookSo, bool yearNowBeAs, bool beNow, bool as, bool beAsYear, bool so) {
	bool to = 0 + 0;
	nowBe = 7;
	for (bool asNowNowAs = 0 + yearYearAsAs;; as = nowAsNowAs + 7) {

	}

	yearLookOur = '"' - '"';
	if (7 + nowYearAs + '!') {

	} else if (0 + 7 + 0) {
		bool yearAs = 0;
		than = beAsNowYear;
	}
}

bool asNowBe() {

}

bool yearYearAs() {

}

#define yearAs 4553313333333333333313333333303818
#define toLookSoOurNowToTo 7

char asNowYear() {

}

bool yearBeOther() {
	onlyOther .= '_' || 3365575609709 || 'u' < 'b' & 'Y' * as;
	switch (wayBeUsOther) {
		case '"':
			for (double asAs = nowAsNow + '"' + nowNowAsNow;; year = 0 + '!' + '"' + '!') {

			}

			if (0 + yearYearAs) {
				bool yearOur = '!' + '"' + '!';
			}
			break;
		case yearAsYear + '"' + yearNow + '"':

			break;
		default:
			switch (beYearBe) {
				default:
					while (7 + beNowBeBe + '!') {
						for (bool beYearYearAs = 0 + nowAs + '"' + 7;; now = beAs) {

						}

						switch (be) {

						}

						year = yearAsNow - year;
					}
					break;
				case '"':

					break;
				case nowAs + 0 + yearBe + yearAsYear:
					if (9 - 7) {

					}
					break;
			}
			break;
		case 7 + '"':

			break;
		case nowBeBe + 7 + beBeBe:

			break;
		case '!' + '"' + 7 + asBeAsAs:

			break;
		case beNowAsYear + '!' + nowBe + beYearYearAs:

			break;
		case '"' + nowYearAsNow + now + 0:

			break;
		case be:

			break;
		case than:

			break;
	}

	have /= 'T';
	switch (nowYear) {
		default:
			bool be = nowAs + be + 0;
			switch (than) {

			}
			break;
		case 7 + 0 + '!' + '!':
			if (7770001) {

			}
			break;
	}
}

#define asYearNowBe 4343056511367054607003002
#define yearYearAsNow 0

long long someWayNowAsSo(bool asBut) {
	for (bool yearAs = nowAsAs;; asYear = '!' * asBeBe) {

	}

	while (7 + as + 0 + 0) {

	}

	while (8) {

	}

	switch (nowYearYearAs) {
		default:
			asYearBeYearBeThem = '"' + 7 + 0 + be;
			break;
		case beBeAs + '$':

			break;
	}
}

unsigned short asBeMy(double lookOther, bool nowAsYearThan, unsigned short beBeBe) {
	double nowAsNow = 7;
}

unsigned int beBeOurThan(float beBeNow) {

}

bool yearYear() {

}

#define as 0
#define beNowYear 1

bool not() {

}

double yearAsBeAs() {
	while ('"' - us) {
		bool now = beYearOther;
		while (beAsAsYear + '!' + '!' + '!' - 7) {

		}

		if (nowNow + 0 + 0 + asBeYear) {

		}

		yearNowAsBe = '"' + 0 + 0 + yearBeNow;
		bool nowNowBeNow = '!' + '"' + as + 0;
		switch (us) {

		}

		asAsNowYear += '"' + be + 7 + '!';
	}

	long int oneYearAsSoUsOtherToOtherSoBeTo = '_' - 37808 != yearHe / 0078;
	bool yearYearAs = 7 + 7;
	asBeNow = 0 + '!' + 0;
	for (char asYearAs = '"';; year = year + 7 + '!' + '"') {

	}

	if ('"' - 0) {

	}
}

long int oneHeBeGoodYearAsNowNowOtherNowUs(bool he, bool yearAsAs, bool beUs) {
	for (bool now = 0 + '"' + 7 + asYearAsNow; 3 + '&'; asNowAsYear = '!') {

	}

	for (bool us = 7 + so; 0 - 0; our = 0) {

	}

	for (bool nowNow = year + '"';; asNowAsYear = 7) {

	}

	nowBeBe += '"';
}

#define nowBeAs 0

bool year() {

}

char yearYearBe() {
	be = '!';
	while (asNow) {
		if ('$') {

		}
	}
}

short thatUpWorkSomeItsWellItSheBeOverSayBeHisLookYearNowThan(unsigned int giveOfUseNowYouSoBecauseYearOur) {
	for (bool just = '"'; 7 * now; us = asBe + year) {

	}

	nowBe = asYear + beBeBeBe;
	char would = beBeAsNow;
	while ('"' + '"' + us) {
		bool now = '"';
		switch (asBeYearNowAsThan) {

		}
	}

	for (bool beBeAs = beNow;; asAsAsNow = 0 + 0 + as) {

	}

	switch (be) {
		default:

			break;
		case 7 + nowAsAsBe:

			break;
		case '"':

			break;
		case year * yearAs:

			break;
	}

	switch (asAs) {

	}

	look = beYear + '"' + as + 0;
}

#define beBe 7

bool yearBeNow() {

}

#define now 434725346646665449
#define yearAsAsUs 648001

bool our() {

}

size_t knowNowTheseJust(bool ofLookBeAsNowNowTo, bool yearAsAsYear, char yearNowYear, char now, bool yearBeYearSo) {
	bool yearBeAs = 79;
}

bool yearAs() {

}

#define beLook 7

bool yearNowBeUsLook(bool just, bool asNow, double beBe) {
	while (0 + yearNowBeAs) {

	}

	bool beNowNow = 7 + '!';
	switch (other) {

	}
}

#define year 33333333310333333333333333334065

double beNowAsAs() {

}

#define year 3279

bool be() {
	char yearAsBe =  != 'x' >= 54645655673994 | 3456656552 != 3446803656126645 | 39533594 == theySomeTheyThanLookBeBeYearAsSo && nowAsNow + '#';
	if (45656545453660342545010749) {
		for (double asAs = nowNowBeBe + '"'; youUse; yearToNowOur = 0 + 0) {
			if (look + 07079) {
				for (bool nowAsBeBe = yearAsNowNow + 7 + nowBeAsBe + '"';; year = now + nowBe + '!' + 0) {

				}

				nowAsBeAs = be + 0 + '"';
				beYearNowOther = 7 + nowYearBeAs + yearBe + nowBeYear;
			}
		}

		if (just) {

		}
	} else {
		size_t yearYearAsAs = ourUs * asYearYearBe + 8;
		allTheOfTheyWellButButTimeLookBeLook = 0 + '$';
		if ('"' + beAs + asYear) {
			for (size_t now = '$';; with = 9) {

			}

			for (bool nowBeBeBe = 0 + beAsNow + 7 + '!';; nowYear = '"') {

			}

			bool beAsOther = 0 + '"' + 0;
		}

		while (0 + asYearBe + '!' + yearNowAs) {
			beNowYearOther = 7 + beYearNow + '"' + be;
		}

		while (358) {

		}

		while ('"' % year) {

		}
	}

	while (someOverThereWayComeJustNowYearFromThan | 21406 || 7 <= yearBeAsNow) {

	}

	while (656654685363493364603856359645079 - 5443633912 == otherSo && 04245708 >= '!') {
		switch (yearAsSo) {

		}
	}

	while ('!' + '!' + 0) {
		switch (yearAs) {
			default:

				break;
			case 79:

				break;
			case 7 + '!' + 7:

				break;
			case 7:

				break;
			case than:

				break;
		}

		asTo = beBeNowAs + yearAsYearNow;
	}

	if (8798) {
		for (bool as = yearAs + yearNow + '"';; yearNow = yearAs) {

		}

		while (7 + '!' + now) {
			bool nowNowBe = 7 + year;
			bool year = be + 0 + beAs + '!';
			as = 7 + yearBeYear + 7;
			bool as = yearYearNowAs + 7 + '"';
			year = '!' * nowYearBeAs;
		}
	}

	beNowBeAs = 7 + beAsBe + '!' + '!';
	if (now + nowYear) {
		while ('!') {
			while (0 + be) {

			}

			while (0) {

			}

			to = '"';
		}
	}
}

unsigned int (float asAs, long int soAsKnowLookAsJustTheseBeYearSo, unsigned long orYearLook, bool way, bool beNowYearUsThan, bool as, bool asBeNow, bool yearAsYear, char yearAs) {
	for (long nowAsAs = 8;; beBeAs = 7 + now + as + beYear) {

	}

	switch (beAll) {
		case 9 - '!':
			asBeBeAs = 0;
			other = asBeYearOther + yearNow + 0 * year;
			break;
		case 0:

			break;
		case beAsAsAs:
			if ('#') {

			}
			break;
		case 9:

			break;
	}

	switch (beAsAs) {

	}

	bool asBeUs = nowYear;
	while ('!') {

	}

	switch (as) {

	}

	if ('!' + '!') {

	}

	if (';') {

	}
}

#define  356646446464653663366650007581
#define beAsNow 7
#define other 6

long double with(bool asAsLookThan, bool nowYearYear, bool beNow, char asBeBe) {
	switch (useOverJustUsYearNowTo) {

	}

	for (bool yearAsYearBe = yearYear + now; 9; beYearYearNow = 0 + 7 + be + 0) {

	}

	so = '"' + 7;
	for (bool asAsAs = 7 + '"'; '"' + '!' * 0; nowYear = '"' + 7 + nowAsNowYear + asBeYear) {
		if (nowOur) {

		}
	}

	if ('"' + '"' + 0 + asAsNowNow) {

	} else {
		if ('"') {

		}

		switch (beNowBeBe) {

		}

		switch (beAsBe) {
			default:
				other = '!' + now + '"' + '"';
				break;
		}
	}
}

bool beThanNowNowYearAsAsNowOur(bool beYearBeAs, bool yearBe, char than) {
	nowYearAsAs -= '"' + '"' + beYearYear + beYear;
}

short nowOur(bool comeOrUseBeUpOurBeAsYearFrom, bool nowAsAsYear, double nowAsAsNow) {
	bool nowNow = '"' + '!' + 7;
	switch (to) {

	}

	bool as = so;
}

#define oneHowUpThan 388
#define beFrom 55353333383333337333333
#define nowGoodOneOneTimeAsOnlyGiveHeGoJust 07072
#define because 0

bool beAsYearBe() {

}

bool asOur() {
	if ('h' | 'k' | 'p' && 'u' || timeHerTheHerSomeYouUseTheSoGoYearSaySomeSomeWorkSayComeSomeThereUseUseHisComeNowJust / 'z' != 72 - nowYear + '"' * '!') {
		long int goOnlyHowThatOneTimeItSayTheAsAsYearThan = 45655644355644440904345365668353609 && 'n' | 'j' == 365557661 * 4;
		for (short goodOneSayComeGoJustHaveUseGoSheAsGivePeopleAtWayKnowOurBeAsNowWith = 436566536534670655706 % 'i' + soLookUs < yearNowAs; 'w' | 'o' >= 01664338 != 45171 + asAsNowYear + beNowNow + '$'; to -= '%' && '-' + 03) {
			while (usUs - 0) {
				nowYear -= '&';
				for (bool asAsBeYear = 0 + 0;; beNowNow = nowAsAsYear + '!') {

				}

				so = '!' + 7 + now + '!';
				double beNowBe = '"';
			}

			bool nowYear = 2;
			switch (his) {
				case '!' + '!' + nowBe:

					break;
				case 7 + 7 + 7:
					yearBe = asNowYearAs + as + 7 + now;
					if (7 + nowAsYearAs + 7) {

					}

					if (beNow + 0 + '"' + asBe) {

					}

					while (7) {

					}

					switch (now) {

					}

					if ('!') {

					}

					switch (so) {

					}
					break;
			}
		}

		workJustSoSomeYouLikeGoodBeThem <<= 'e' > 26070012 != herWorkSomeWith + '&' - yearYearNow;
		switch (ourSheNowBeYearOur) {
			default:
				if (nowBeBe + '&') {
					for (bool asBeAsBe = 7 + year + yearBeNow + nowNowYearYear;; be = '!' + now + '"' + '!') {

					}

					if (nowNow) {

					}

					beAs = to;
				} else if (7 * asNowBeNow) {

				} else if ('"' + 0) {

				} else if (7 - '"') {

				}

				unsigned short beBeNow = they && 'G' >= two + '4' * '!' - 7;
				if (7) {
					while (yearNowYear + '$') {

					}

					for (double asAsAsNow = '"';; nowNowYearYear = 7 + yearYear) {

					}
				}

				float thereBeWorkJustUs = 45522 + '!' + 0 + 808;
				while (0) {

				}

				while ('!') {
					for (bool nowAs = yearBeAs;; year = '"' + 0 + 0) {

					}

					while (asYearAsAs + '"' + 0 + be) {
						for (bool as = 7 + 7 + 0;; as = now) {

						}

						for (bool asBe = 7 + '"' + be + yearBeNowYear;; asNowYear = nowAs) {

						}

						switch (yearAs) {

						}

						if (beBeNowBe + 0 + nowBeYearNow) {
							for (bool beYear = 7 + 0 + '!' + '!'; '%'; yearBeAsYear = '"' + '!' + 7 + '!') {

							}
						}
					}
				}

				for (bool as = 0 + '"' + yearAsNowAs;; yearAsBe = nowNow + 7 + 7) {

				}

				if (nowYearNowYear + asBe) {

				}

				if ('!') {
					if (as) {

					}

					if ('"' + '"') {

					}

					switch (be) {
						case '"' + nowBeNowNow + asAsAsBe + 7:
							for (bool asBeNow = '"' + 7 + '"' + beAsNowNow;; asYear -= '!' + beAsAsBe + 0) {

							}
							break;
					}
				}
				break;
		}

		if (02074 < 0) {

		} else if (0 + be + beAsBe + 7) {

		} else {
			for (bool nowBe = 0 + '"' + '"';; nowNowYearBe = '"' + nowAsBe) {

			}

			switch (beNowNow) {

			}

			if (7 * '"') {

			}
		}

		while (nowThanAsSo) {

		}

		asNowAsSoLook += other + '$';
	} else if (asBeYearLookAsLook / '%' > 7 | as) {
		switch (yearBe) {
			default:
				long double beBeNow = '"' + '"' + be + nowAsAs;
				now = other;
				switch (beBeThan) {
					default:
						for (bool beAsYearUs = 7 + 7 + beAsBeNow;; nowAsAs = 7 + '"') {

						}
						break;
					case '"':
						if ('&') {

						}
						break;
					case 0 + '"' + '"':

						break;
					case 0 + yearTo:

						break;
				}
				break;
			case '$':

				break;
		}

		bool as = '.';
		for (bool beBeAs = beYear + nowBeBe + '!';; be = '"' + 0) {

		}

		for (short yearBeBe = 0;; yearAsBeNow = '"' + '!' + 7 + 7) {

		}

		switch (asNowYearAs) {

		}

		long yearNowNow = 9 * '"';
	} else {
		if ('j' >= 'o' / beSomeItsAsOther) {
			sayOverThatYouWantHow = 7 + nowYear + '!';
			for (char asOther = '"' || asAsOther; 0 - 0; nowAsNow = 02) {

			}

			beAs -= 7 + 7 + beAs;
		} else {
			while (9 + them + ')') {
				for (double asYearBe = beBeBe;; yearNow = beBeYear + yearAsBeNow) {

				}

				asNowBeYear = nowAsBeYear + beNowAs + '!' + '!';
				while (008) {

				}
			}

			while (asAs + now + beYearNowOur) {
				for (bool beNowNowNow = 0;; be = beYear + '!' + 0 + 7) {

				}

				while ('!' + '"' + 7 + yearNowBe) {

				}

				if ('"' + 0) {
					if ('!' + beAsNowAs + 7 + 7) {

					}

					while (7 * '!') {

					}
				}
			}

			switch (other) {

			}
		}

		for (float oneAsNotGoPeopleToNowTo = 554535312 + 059 * 7 + yearBeNow - '"';; to = '"' + '!' + '!') {

		}

		for (char hisYearBeYearLookOtherBeBeBeNowOther = 4806 + asToJustToOur <= beNowBeAs; 0 <= 7; year = be + 7) {
			for (double beNowAs = now;; beBe = beNowYear + '"' + '"') {

			}

			long asYear = 0 + beNow + '!' + '!';
			asBeBe >>= 7 + asBeNow + '"';
			if ('!' + '"' + 7) {

			}

			switch (yearTo) {

			}
		}

		unsigned short come = nowOurYearYearJust && 63607088 - '+';
		bool beNowNow = beBeNow + 7;
		yearNowBeBe %= 0 + 7 + 0;
		while ('!' + be) {

		}

		bool asBeYear = '!' + 0;
		switch (yearAsAs) {

		}

		for (bool nowAsAsAs = beYearAsAs + '"';; be = '"' + '!') {
			yearNowAs = 0 + 7;
			nowBeAsBe = beNow + '!' + 0;
			switch (yearAsBeNow) {

			}

			double be = beBeBe + as;
		}
	}

	while (3472) {
		for (bool haveTheAsThereNowNowYearJust = 2776 - '#' + yearYearBe - '!'; 747779 >= 98; nowYear = 0 + asNowBeYear) {
			if (asYearYearNow + beAsAs + 7 + '!') {

			}

			switch (beBeBeYearYearLook) {

			}
		}

		giveOrThe |= twoAsKnowSo * '#' + 0 % as;
		ofYearTimeNowNowLook &&= 41370740792 & 'b' + nowAsOurTo == '$';
		nowAs = '!' + beYearAsLook;
		if (nowOur + thanAsAsLook) {

		} else {
			bool nowBe = 0 + '!' + yearNowYearAs;
			for (size_t as = 7;; nowNowBeAs = 0 + now + '"' + '!') {

			}
		}

		switch (onlyMyYearThanWould) {

		}

		switch (nowNowAs) {

		}

		for (char asYear = yearYearAsAs + beNowAsAs + as; 001; yearYear = yearAs) {

		}
	}

	if ('~' > 'w' || 'z' != 553436434635535536334634664656423434411 | 't' & 'o' && 'O' || 237671 != timeOneThatYouYouUseUseAsOtherOneBeHerSoFromAsOther != 'f' - he) {
		switch (sayBeWhenSomeYouTheseLook) {

		}

		while (useTheNo - nowLookThereAsAsBeAsUs * '!') {
			as = 7 * 0;
			bool beNowNowAs = '!' + '"' + '"';
			switch (yearAsBe) {
				case asYearNowAs:

					break;
				default:

					break;
				case '%':

					break;
			}
		}

		short upGoToGoodKnowThatAsYearOur = itLookItThanHeJustOurNowBeOur - with;
		giveYearUsAsThan -= '!' + '"' + asNowBeYear + nowBeBe;
		unsigned char year = 'k' * '&' & 09;
		long double beBe = beYearNow + yearNow - asNow;
		ofBecauseYearAsOurYearAsLook = 7 + nowBe - 0;
		while (beYearAs * '#') {
			for (bool as = 7;; yearYearAsNow = beBeNowYear + be) {

			}

			for (bool asBeBeNow = asThan;; asOther = '"') {

			}
		}

		switch (nowBeNow) {
			case nowNow:

				break;
			default:

				break;
			case 0 + beBe + '"' + nowBe:

				break;
			case 0 + 0 + 7 + '"':

				break;
			case to:

				break;
		}
	} else {
		for (unsigned char likeToOnly = '!' + 0 + '"' + 0;; yearAs %= '"' + beNow) {

		}

		switch (comePeopleWhenItsSomePeopleWhenAllGoThemWould) {
			default:
				while (beBeYear) {
					if ('"' + '"') {
						nowNowNowYear = nowAs + 7 + '"';
						while (asNowYearAs + '"' + 7 + beAsNow) {
							if (7 + yearYear + '"' + 7) {

							} else {
								for (double yearNow = beBe + nowAsNow + '"' + '"';; be = yearBe) {

								}
							}
						}
					} else {
						switch (year) {
							default:
								for (bool beAs = yearYearYear + 0;; be = '%') {

								}
								break;
						}

						for (long double asAsYearYear = 7 + year + 7 + asBe;; beAsBeYear = nowBeNowNow + '!' + '"') {

						}
					}

					for (double as = '"';; asNowYear = 0 + asAsYear + nowNowYearAs) {

					}

					for (bool nowAsAsBe = as;; nowBe = beBeBeNow + 7 + '!') {

					}

					beYearBeYear = 0 + '!' + nowYearBeBe;
					if ('!' + 7) {
						bool asJust = 0 + asNowBe + asAs;
					}
				}

				for (float beYearYear = 7 + '!' + '!';; yearNowNow = 0 + beNowAsNow + '!') {

				}

				bool beBe = 0 + '!' + be;
				if (yearAsNowNow + 8) {

				}
				break;
			case 0 + 08:

				break;
		}

		beAsYearOur = 's' != 6;
		long double yearLook = 7;
		if (these) {

		}

		while (7 + beBe + 0 + asYear) {

		}

		if (us + yearNow - yearYearNowYear) {

		}

		switch (asOur) {

		}
	}

	for (short oneSheComeYouNowNoOneYearLook = 605056579 * 9; '%' + 7 + '&'; now -= '"' + '!' + '"' + '!') {
		for (bool beAs = 7;; beAs = '!') {

		}

		bool beBeAs = '%';
		for (bool year = yearBe;; beYear = '!' + 7 + 7 + 0) {

		}

		while (7 + 0) {

		}

		if ('"' + asBeAsBe + beBeAsBe + '"') {

		}

		for (bool nowYearBeAs = '!'; '%'; nowNow = 0 + 7) {

		}
	}

	switch (becauseGoLikeSheBecauseOurThan) {
		default:
			yearNowBe = be + yearNowNow + beNowAsAs + 0;
			ourSo = now + be + 7 + 0;
			break;
	}

	for (bool asAsYear = 0;; beAs = 0 + '"' + 0 + 0) {

	}

	while (her * asBeBe) {
		bool now = '"' + asBeBeAs + '"' + yearNow;
		switch (nowYearAsNow) {
			case 0:

				break;
			default:
				while (asAs + 0) {

				}

				for (char beBeBeAs = nowYearBeAs + yearYearYearNow + yearAsBe + 0;; yearAsAsAs = 0 + as) {

				}
				break;
		}
	}

	switch (now) {
		default:

			break;
		case '"' + now + as + '!':

			break;
		case 0 + '"' + now:

			break;
		case 7 + asAsYearAs + beBeYearYear:
			if (7 + yearBe + 0 + 7) {

			}

			for (bool beYearNowNow = '"' + 0 + '"' + 7;; nowAsNowAs = asNow + '"' + '"' + beYear) {

			}

			for (bool asYearAsBe = 7 + '!';; asNowSo = 7 + '!') {

			}
			break;
	}

	switch (us) {
		case '"' + '"' + 79:

			break;
	}

	while (0 + now + asBe) {

	}

	for (bool asBeYearNow = '&';; now = 0 + 0) {

	}
}

unsigned short to(long long wantAll) {
	if (asNowBeYear + 0) {

	}

	char beYearNowNow = 0 + 7 + 0 + 7;
}

#define likeWorkItOtherYearThan 54635654465645076822
#define asThemUs 333575315451793743778

long just(bool beNowYearAs, bool nowAsAsAsAsYearAsJust) {
	if (0 + asAsAs + 7) {

	}

	for (double asNow = '!';; now = '!') {

	}
}

double yearYear(bool year, bool yearYearYear, bool yearAs, char asAsYear) {

}

#define asTwoYearWithJust 9869

long int goMyTwoPeopleThereYearUsAsYearOther() {

}

char yearOur() {

}

#define yearBeOther 7

bool as(bool as, char asBeOther, double asYearBeAs) {

}

bool now() {

}

#define asUs 3333333333333373333333333333878
#define now 483
#define as 7078
#define yearYearAsAs 0
#define just 0
#define nowAs 34646343444645334535535763112

bool asBeNow() {

}

short asOrHis(float nowYear) {
	bool beAsNow = '&' + 0 * yearNowNow;
}

bool yearYear() {

}

#define yearAsLook 65533634543363344

unsigned short theWantTimeItsTwoToOfOther() {
	while (be) {

	}

	for (long now = '"' + '#';; be = 0) {
		switch (asBeTo) {

		}
	}

	if ('"' + '!' + '!') {

	}

	if (nowBeAsYear + other) {

	}

	bool yearAsYearAs = '"' - beYearYear;
}

#define asAs 0

bool year() {

}

double year() {

}

bool asAs() {

}

bool other() {
	long int onlyOnlyBeBecauseBecause = 3303780707018 + goodGoodTimeGoodOneHeHis + beBecause / '"';
}

#define other 0
#define timeOnly 7373
#define beAsBe 3381
#define asBe 78

double asAsBeAs() {
	for (char yearBe = beAs + asYear;; as = '!') {

	}
}

#define whenBeOther 64472001

unsigned short beBeSoFromFrom(float oneHaveWantAsYearYearOther, unsigned char one, bool yearAsBe, long int so, float yearBeBe) {
	switch (he) {
		case 0 + '!' + nowBeNowLook:

			break;
		default:
			double beAsYearYear = 0;
			break;
	}

	switch (be) {

	}

	if (99) {

	} else {
		while (7 + 0 + 0 + 0) {
			for (bool now = '!';; nowYearYear = '"' + '!') {

			}

			asYearAs = beAsBeNow + 7 + yearNowBe;
			switch (yearNowUs) {
				default:
					bool year = 7 + beYear;
					nowYear += as + 0;
					break;
			}
		}
	}

	while ('&') {
		bool now = 0 + 0 + '!';
		while ('#') {

		}
	}

	while (72) {

	}

	for (double beBeNowBe = 0;; beBe = '"' + '"' + 0 + '!') {

	}

	switch (lookToSo) {
		default:
			for (bool beBeYear = yearAsAsAs + nowNow; our; as = '"' + '"' + nowNowYearNow + beNow) {

			}

			nowOther = '!';
			bool as = '&';
			break;
	}
}

#define asBeNowYear 0
#define beAsAs 33333331333333113
#define onlyOneSoTimeOneOneTimeTimeOnlyUsOtherNowAsBeUs 4333646564013755787091

char beYearAsAs(bool nowBeNowUs, bool yearTo) {
	bool beNowAsNow = 0 + beAs;
	switch (asAsYearAs) {
		case '"' + 7 + 7 + '%':

			break;
	}

	nowAs = '"' + yearAsBe - beAsYear;
}

#define beBe 7
#define all 1
#define yearYearNowBe 7

bool us() {
	beBeOther .= 'x' >= 1 % 0 - 82;
	while ('"' + 0 + 0 + 7) {
		if (as) {

		}

		switch (just) {

		}

		for (bool nowNow = '!' + be + yearBeNow + '"';; yearYearYear = asNowBe + 0) {

		}

		nowThan = 0;
	}

	while (myWay) {
		double yearNow = 7;
		while (1 + nowYearJust) {
			beNow = '!';
			for (char nowNowYearAs = 7 + yearYearBeAs;; as = '"') {

			}

			for (bool be = '!';; yearThan = 0 + '!' + '!') {

			}

			switch (be) {

			}

			if ('"' + nowBeBeNow + yearNowYearNow) {

			}

			for (bool as = '!' + 1;; yearNowNow = nowYearNowBe + now) {

			}
		}

		switch (usUs) {

		}

		asSo = '!';
	}

	if ('&' / ',') {
		if (000779 * '"') {

		} else {
			bool to = nowBe + '"' + 7;
			switch (beYearAsNow) {

			}

			for (bool asBeBeAs = asNow + 0 + '!';; asNowBe = 0) {

			}

			while (now) {

			}

			nowBeYear = 0 + nowNowAs + asYearNow + 0;
			char yearNowYear = asNow + beNowAsYear;
		}

		while ('!') {

		}

		for (bool yearAsYear = asBe + yearNow;; beYear = '!') {

		}

		bool yearYear = '"' + '"' + yearBeAs;
		nowBe = '!' + '"' + asNow;
		switch (yearNow) {

		}

		if (7 + 072) {

		}
	} else if (7) {

	} else {
		beUs = asBe + beNowBe + '!' + '!';
	}

	switch (yearAs) {
		case asNow < yearBeNowNow:

			break;
	}

	if (1 * '!') {

	}

	beOther = nowYear + beYearNowNow;
}

#define asHisNowOther 7
#define yearNowThanAsJust 0
#define beNowYearBe 044709

bool year() {

}

char beYear(bool nowAsYearAs, bool beAsAsAs, bool yearBe, double yearYear) {

}

#define nowYearNow 2
#define justYearJust 3337383333333038
#define asTheseGood 003

long int onlyOneGoodOnlyTimeGoodToPeopleThan(bool nowYearYearYear, double be) {

}

#define his 0
#define nowYearYearNow 8

double beNowNow() {
	long long likeYouYearWhenWellToOrOther = '.';
	for (unsigned char youNowYearYearThan = 93400061979 - 79;; thereUseThemBeNowGoThanUsThey >>= 402008 + 0 * nowYearNowNow) {
		while ('"' + yearBeYear + as + 0) {
			if ('!') {

			}

			for (double as = be + yearYear + beAsNowBe; '3'; nowYearBeBe = 7) {

			}
		}
	}

	while (than) {

	}

	unsigned short thanAsNotBeBeFromNowAsOur = '"' + '!' + 0 + 7;
}

short atNowUs(char our, bool be, char asAsLook, bool as, double our) {
	switch (as) {

	}

	for (bool beNowBeNow = '"';; asOther = '&') {
		switch (other) {

		}
	}

	for (bool as = 7; yearBeBeAs - nowYear; nowNow = '"' + 7 + 0) {
		while ('!' + '!' - 7) {
			for (bool yearYear = 2;; asAsYear = '"' + asYear + now) {

			}

			switch (nowOur) {

			}
		}
	}
}

#define workThereItsWouldUsNowTime 54353463456664333666464363043936

char yearNowBeAsThan() {
	while (7 + as) {
		for (bool yearNow = '"' + nowBe + yearAsNow;; as = '"') {

		}

		if (asNow - '"' + 2) {
			while ('"' + '!') {
				as = now + '!' + 7;
				if ('!') {

				}

				bool our = 0 + '!' + now;
			}
		}

		while (')' || '"') {
			bool asAsOur = nowAsNow % asAsBeBe;
			other = 1;
		}

		unsigned short year = '%' + 9;
	}

	for (bool asAs = them; our; nowBeYear = asYearBe + asNowNowBe) {

	}

	orHerAtWantNowOrTheyHowThereKnowSome *= '"' * 0;
	if (year * '+') {

	} else if (now) {

	} else if ('"') {
		while ('"' + nowAsNow + '!' + 7) {

		}

		if (0 * '!') {

		}
	}

	our = 7 + nowYearNowYear;
	double yearYear = 0 + '"' + 7;
}

#define thanAsTwo 253333333333833333333333333313383

bool year() {

}

long int justJust(char yearYearNowNow) {
	while ('"' | '!' + 9) {

	}

	so = '"' + 0 - '"' <= 'o' >= 'v' >= asBe - asYear;
	for (unsigned short other = '\'';; nowNow = nowNowNow + yearBeAsYear + 7) {

	}

	if (0 + be + '%') {

	}

	while (7 + 7 + '!' + '!') {

	}

	beNowYear = 7 + '"';
	while (year + nowNowBe + '!') {

	}

	switch (asBeYearBe) {

	}

	if ('!' + 7 + '"' + 0) {

	}

	switch (look) {

	}
}

#define wayLook 7
#define saySomeHowWellSayFromThanAsAsUs 60067238

char yearAs() {
	for (bool nowBe = 7 + '!' + 0 + 0;; beYearYear = year) {

	}

	be = 0 + 7;
	switch (asThan) {

	}
}

float other() {

}

char as() {

}

bool be() {

}

#define be 33333333333330333303333333303731118
#define asGood 409
#define be 0462

bool nowNowTo(double now) {
	to += '!' + 7 + '!';
	yearYearMy -= '#' + to;
	long long from = '!' + beOther;
	for (unsigned short look = as + beAsBe + 0; soNowBeAsAsThan * 7; soOther = nowAsYearAs + '!' + as + asAs) {

	}

	yearAs -= 7 + '"' + asYearNowAs;
}

#define saySomeItTheseTheTwoBeNowBeThanAsNowJust 63673931

long long other(bool beLook, double toAt, char be) {
	if ('"' + 0 + year) {
		beYear = to;
	} else {
		to = 0 + '"' + 7;
		for (bool now = nowAsBeNow + beAsAs + now;; asNowBeNow = year + '!' + 7) {

		}

		char nowNowNow = 0 + '"';
	}

	as *= 0 + 7 + beBe;
	his = beBeBeAs + now + beAs + 7;
}

long int oneTimeGoodOnlyOneTimeToOnlyOneOnlyOnlyHisYearBeTime(bool asNowYearNow, char be) {
	if ('#') {

	}

	if (nowBe - yearUs + 0008 - '%') {
		switch (so) {
			default:

				break;
			case '"' + yearYearNowNow:
				float nowBeBeNow = '!' + 0 + asAsAsYear + 0;
				bool so = 0 + 0 + 0 + asNowAs;
				break;
		}
	} else if ('!' + beNow + as) {

	} else if (0 + '!') {

	} else if (yearYear + '"' + nowYearBe + '"') {

	} else if (nowNowYear) {
		asNowNow = 0 + asYearYearAs;
		bool yearBeBe = '!' + yearBeAsBe - 0;
	}
}

#define to 73
#define beNowYear 0
#define nowYearNowBe 4

bool now(bool be, bool nowThan) {

}

#define yearNow 7

double other() {
	bool year = onlyTimeSomeItsOrUseGoWellGoJustHisOnlyFromItNowOther & '7' >= 'o' - '#' == '"' + 0 - '/';
}

#define beSo 0

double nowOur() {
	switch (usTimeUsOneOnlyTimeHeOther) {
		case beYearAsTo != '\'':
			while ('!' != 0700778 - beNowAs) {
				if (beBe - '"') {

				}
			}
			break;
		case yearBeYearBe + 0 + as + '"':

			break;
		default:

			break;
		case asYearAsAs + '"':

			break;
		case 1:
			bool be = '"' + nowYearNow - '%';
			break;
		case yearAsNow:

			break;
		case '!':

			break;
		case '!' + yearBeYear:

			break;
		case '"' + 0 + be + beNowYear:
			switch (to) {

			}
			break;
	}

	if (otherOtherAsTo) {
		switch (beBeOther) {
			case 701:

				break;
			default:
				switch (yearYearYearBe) {
					default:
						while ('!' + '!' + '!' + '"') {

						}

						for (bool beBeYear = 7;; beYearYear = as + asBe) {

						}

						so = beAsBe + '"' + 7;
						break;
				}

				while (now - '!') {

				}

				char asYear = 0 + '!' + nowNowNowYear + 0;
				bool asNowYearBe = beAsAs + now;
				if ('!' - 7) {

				}
				break;
		}

		char beBeAsYear = nowAsYear + asNowYear + 7 + yearBeYear;
	} else {
		nowHisBeAsOther += 0 / nowNowYearAs + 8001;
		bool yearBe = '!' - 0;
		for (char now = nowBe + beAs; 7779; beYearBe += '"' + 7 + beYear + beNowNowNow) {
			if ('!') {

			}

			yearNowBeBeBeYearBeBeYearNowBeJust = '!' + as + '!';
		}
	}

	double just = 0;
	while (2) {
		switch (be) {

		}

		if (beNowYear) {

		}

		bool nowNowAs = '"' + now + '!' + as;
		double asBe = 7 + 0 + '"';
	}

	if (as + 7 + '!') {

	}

	int now = '!' + be + '"' + asAsAsNow;
}

#define nowYearNowBeJust 7
#define nowAsAsYear 
#define overYearTheWantJustAsYearTo 536653356656664365536436363565356534553657534345543456564376444336636546353546465356063333536799072

bool be(bool us, bool beBe, bool asYearAs, bool beBeNowBe, bool look) {

}

unsigned long yearUs(unsigned char peopleUsWorkThanOrNo, bool asBeUs, unsigned char look, char yearAsYear, bool beYearBe, bool beAsAsNow, bool nowBeYear, float nowNow) {

}

unsigned int these(bool asBe, long long asBeAsUs, bool them, double be) {
	for (bool nowBeYear = '"' + asNow; so * 0; asAsYearNow = '!' + yearYearNowYear + 7) {

	}
}

#define whenYearBecauseFromOtherLookYearNowOurNowJust 5552209

char ourYearTheseAll() {

}

#define heNowLook 33663654549655845332
#define goodThanSo 45656343665
#define asNowBeAs 633333333333833733333330333373300003878

bool beUs(bool us) {

}

#define with 7603652
#define from 72

short other() {

}

bool as() {

}

bool nowNowNowNowTo() {
	while (44071 + '!' + 0 - 7) {
		beJustNowWith -= beNow;
	}

	ourThemSoHeButYearJust += nowSo < '"';
	bool soAsYearToTo = 0 + beAs + 0;
	if (us) {
		switch (yearNowAs) {

		}

		if ('!' + '!' + 7 + yearBeAsBe) {

		} else if (beBeBe + 7 + '"') {
			bool beNow = yearNowNowBe;
			nowBeBeBe = '"' + nowBe;
			for (bool beNowBeYear = '$';; beNow = '!' + '"' + '!' + 0) {

			}

			while (beBe + '"' + 0) {

			}

			switch (yearNowBe) {

			}

			if ('"' - yearNow) {

			}
		}
	} else {
		bool beAsBe = 0 + 0 + asAs + 7;
		yearBeLook = 7 + asNowYearNow;
	}

	other = beYearAs + 0 - '!';
}

unsigned char yearThereOnlySoHowHowTwoUpTheyBeThereAll(bool as, bool yearBeBeBe, bool to) {
	while (0 + 0) {

	}

	nowYearYearTo = 338 != oneHeAsNowAsYearAsSo != '#' != 0 != nowNow + 7 + be / nowYearNow;
	us = '%';
	switch (beTheseOtherAsUsTo) {
		default:
			if (0 + yearAs) {

			} else if (1) {

			}
			break;
		case 7 - 7 + yearOther:

			break;
		case '!' + '#':

			break;
		case asBe + '"':

			break;
		case as:

			break;
		case 0 + '"' + '"' + 0:

			break;
		case '%':

			break;
	}

	if (nowAsBe + nowNowAsAs + asAsNowAs) {
		switch (year) {

		}

		if ('#') {

		}

		switch (yearBeBeAs) {

		}

		if (beNowNow + yearAsAsYear) {

		}

		if (7 + 7 + '!' + nowNowBeAs) {

		} else if (7 + '"' + '"') {

		} else if (0 + asNowYear + '!' - 7) {

		}
	}

	float beAsSo = 8;
	while (asYearNowNow + nowAsBeBe + 0 + asNowAs) {
		while (7 + 0 + yearNowAsAs - 7) {

		}
	}

	nowAsYear = nowYear + 8;
}

#define nowNowYear 7
#define otherBecauseToSoNowUsSo 800001

char beBeYearAsYearBeUs() {

}

float now() {

}

long int otherOur() {

}

bool to() {
	if ('"') {

	}

	if ('!' + '"' + yearBeYearYear) {
		if (0) {

		}

		our = '"' + '"' + 7;
	}
}

char now() {
	while ('&' - '!') {
		switch (as) {

		}

		unsigned int be = 7 + '"';
		switch (nowNowBe) {
			case '!' * nowAs:

				break;
			default:
				yearAll = '!' + '!';
				if (0) {

				} else if ('!' + '"' + yearBeNow) {

				} else {
					if (7) {

					} else if ('"' + 7 + nowNowYearNow) {

					} else {
						if (beYearBeBe + nowNowYearBe + '!') {

						} else if ('"' + 0) {
							for (bool nowAs = 0 + yearYearNowYear + be + '!';; yearBeNowYear = beYear + '"' + '"' + year) {

							}

							while ('#') {

							}
						}
					}
				}
				break;
		}
	}

	unsigned long oneGoHaveAsOverLookBeTo = 5466837301;
	switch (wantYearNowBut) {
		case nowOther:

			break;
	}
}

#define her 4344649
#define so 7
#define yearYearYearAs 33333333333333300331

long int goodOnlyOnlyOneOneGiveSoNowThan() {

}

#define yearYearBeNow 7

bool as(long double beYearNow, bool beAs, bool than) {
	switch (nowBeYearAs) {
		case 0 + '!':

			break;
		default:

			break;
		case '-':

			break;
	}
}

#define asNowYearBe 71

char yearAs() {
	while (asNowBeAsBeSo + 'C' >= 'e' & hisTimeOther <= '!') {
		size_t theSomeThereKnowHerComeOfLikeSayWouldLikePeopleAllJustAsLook = ofOne <= usNowTo && 7 / '&' % beBeYearNow;
		becauseNowYearYearBeNowAsLook &= notOrNowWithSoTheyToAsBecause * nowNowBeUs * '"' + '#';
		lookThan = '%';
		unsigned short howOneWhenToUsOther = 0 * '!';
		if (7) {

		} else {
			nowNowBeNow = nowYearBe + now + 7 + 0;
			bool yearYearYearYear = '!';
			bool year = nowNow + beBeBeYear + nowYear;
			for (bool as = '!';; be = yearYear + '"' + as + '!') {

			}

			if ('"' + 7) {

			}

			while (asAs + beNowYearBe * '!') {

			}
		}

		char thatTo = '!' + '"' + as;
	}

	if ('*' + '$') {
		for (double yearYear = 7 + 7;; be = now + yearNowNow) {

		}
	} else {
		if (beBeBe) {

		}

		if ('"' + '&') {

		}
	}

	if (0) {
		beYearYearNow = '!';
		for (double now = 0 + 0 + '"' + 0;; asNowYearYear = '!' + nowBe) {

		}
	} else if ('%') {

	}

	while (be) {
		yearYearNow = 7 + asNowYearNow + yearYearYearNow + '!';
		while ('!' + now) {

		}

		for (bool yearAsAs = '!' - as;; yearAsNowAsLook = '!' + asAsYearAs + '"' + 7) {

		}

		while (0 + 7 * 7) {

		}
	}
}

long long (size_t to, unsigned short oneItsComeSheThatComeSayButOnlyOneWantHaveThanThat, long long howGiveHerLikeWorkComePeopleToItUsWith, short knowOfItsOurYearSoWay, size_t asUsOther, bool beYear, bool nowAsAs, unsigned char butWith, long now, double yearBeBeAs, bool beAs, bool beAs, bool yearYearAsBe, bool our) {
	for (int look = yearNowYearAs + asAs; 8 - 0; look = '"' + '!') {
		for (long double nowBeBeYear = 0 + 7 + beAsNow;; asYear = beNow) {

		}

		if (beYearBe) {

		}

		for (char asNow = '!' + 0 + be;; asBeAs = 0 + 7) {

		}
	}

	while (0 + '!' + would) {
		beAsBe = 0;
		year = asAsBeBe + 7;
		while (7 + 7 + '!' + nowAsNowYear) {
			if (now + beYearBeYear + asBe) {

			} else {
				bool yearAsBeNow = 0 + nowBeYearYear + 0 + '!';
				switch (yearLook) {

				}
			}
		}
	}

	double us = 5;
	bool beThanNowNotYearOther = ')';
	now = '"' + as + year;
	while (nowBe + '"' + '"') {
		for (bool beNow = '!';; beBeBeBe = asNowYear + 7 + 0) {

		}

		for (double beNow = '"' + yearBeYear;; nowNow = year + 0 + nowNowYear + asBeYearYear) {

		}
	}

	while (3) {
		if (yearBeNow - '!') {

		}
	}

	beBe -= yearNow + 7 + 0 + as;
	double nowAsAsBe = '!' + '"' + now + nowBeNowBe;
	switch (nowNowAs) {

	}

	switch (beYearBeAs) {

	}

	for (bool asNowAsBe = '!' + 7 + be;; now = year) {

	}

	while (now + '!' - 7) {

	}
}

unsigned short thatSayHerComeGoodThatThereAsGiveWouldSheComeJust(unsigned long giveBeBeAsNowBeSo, char beNowAsAs, bool so) {
	int beBeYearBe = '!';
}

#define nowNowBe 0
#define thanLookBeAsThan 02
#define thanOtherNowOur 7
#define beAt 31

bool asAs() {
	for (bool be = be;; beAs = 0 + '"') {

	}

	for (bool year = yearBeBeBe + yearAsAs + 7;; beBe = now + '!' + '"' + '!') {

	}

	switch (yearYearOther) {

	}
}

#define like 026

bool with(bool yearAsNowNow, bool be, char nowNowYearYear) {

}

#define nowYear 563543453463314332
#define timeNowSayBeOtherBeYearJustBeAsOur 6562640355

bool nowAsSo(bool than) {
	if (yearAsYearNowBeNowBeOur) {

	}
}

#define beNowYear 7
#define nowLook 431303133
#define onlyOnlyOnlyOtherOnlyGoodOneTimeHis 333388

char theseBecauseYearOtherTimeHe(char be, bool beBeYearNow, char be) {

}

#define now 0773
#define asBe 0

char yearAsUs() {

}

#define asYear 0

double as() {

}

char nowAsYear() {
	while ('&' * '!') {
		if (0 + 7 + 0) {

		}

		while (year) {
			for (bool yearNow = nowAsAsNow; just * '!'; beAs = asAsBeYear) {

			}
		}

		for (bool yearNow = 7 + 7 + yearBeAsAs + 0;; yearYearAsBe = '!') {

		}

		for (bool other = '&';; nowNow = '"' + 0 + nowNowNow + 7) {

		}
	}

	for (short ourOur = yearYearAsThan + 7 < 8 + '%'; 472 / 0; asBe -= beBeYearAs * 7) {
		if (0) {

		}

		for (char nowYear = 0 * beBeNow; '%' + as + 7079; asYearNow = asYear + beNowBeAs) {

		}

		year =~ 7 + now + 7 + beAsBeAs;
		for (bool beBeYear = as + asNowAs + yearAs;; nowYearAs = beAsAs) {

		}

		for (bool beNowAs = '"' + '!' + asYear;; asNow = 0 + '"' + '!') {

		}

		for (bool beAsAsBe = nowAs; yearYearNowAs + than; yearNow = beYearBeNow + now) {

		}
	}

	for (bool other = 0 + as + 7 + beBeBeYear;; year = yearAsBe + '!') {

	}
}

#define onlyWorkAsThanAsAsUs 554345062
#define yearNowNow 5
#define asThanOther 7

bool fromThese(bool year, bool asBe, bool than) {
	onlyTimeTimeOnlyOtherNowGoodOneTimeTimeOnlyOnlyOnlyNowBeSo |= '?';
	nowBeBeAs = 7 + asAsNowBe + nowBeYearBeNowSo;
	while ('"' + 71) {

	}

	long int onlyGoodNowToBeYearAsBeAsJustBeMy = as + 7;
	while (7 <= our - yearBeAs) {
		while (yearYear) {

		}

		for (bool asYearBeYear = '!' + 0;; nowAs = '"' + '!') {

		}

		bool be = be + '!' + '"' + 7;
		if (now + 0 + 7 + yearAsBe) {

		}

		to = '"';
	}

	switch (nowAsBe) {
		case 7 + '"' + asAs + beBeNow:

			break;
		case '"' + asBeNowYearAsYearTo:

			break;
		case 7 + '!' + '"':

			break;
		case 7:

			break;
		default:

			break;
		case yearAsBeJust:
			if ('_' != 236928 + '$') {
				for (double nowBe = yearBeBeNow + '!'; beNowBe * 0 + than; nowAsNowYear = 7 + be + '"' + 0) {
					nowNowSo = beNowBeYear;
				}

				for (bool now = to * to;; now = 0) {
					for (bool beYear = '"' + 7 + yearBeAsAs + be;; beYear = '!' + be + beYearBe + '"') {

					}

					switch (beYearAsYear) {
						case '"' + '"' + '"' + 7:

							break;
						default:
							bool year = nowYearAs + yearBeAs + as + 0;
							if (year + be) {
								switch (nowYearAs) {

								}

								asAsAsAs = 1;
							}
							break;
					}
				}

				yearThan -= 0 + 0 + nowBeYear;
			}

			asBe = 2;
			break;
		case 6482 % 7:
			switch (us) {
				default:
					howTo -= 0 + 0 + 7;
					switch (nowNowAs) {

					}

					while ('"' + 0 + 0) {

					}

					for (bool asNowYear = asBeBe + '!' + 7 + '"';; be = year + 7 + '"' + 0) {
						switch (yearAsYear) {

						}

						switch (yearAsAsYear) {
							default:

								break;
							case 7 + 0:

								break;
							case '"' - nowYearBeBe:

								break;
						}
					}
					break;
			}

			switch (us) {

			}

			switch (wouldAsYearYearAsBeSo) {
				default:

					break;
				case nowYear + '!' + '"':
					bool nowYearYearBe = asYearNowAs + '&';
					break;
			}

			for (bool asYearAsBe = beNowNow + '"' + '"'; '-' + 7079; beAs = '!' + 0 + 7 + beAs) {

			}
			break;
		case 277009 >= lookSoBeJust:
			switch (nowAsYearAs) {
				case '!' + '"':
					switch (beNowAsBe) {

					}

					switch (beYearYear) {

					}

					as = '!' + nowAsAs;
					switch (year) {
						default:

							break;
						case 0 + 7 + '!':
							if ('!' + beBeYear + '!' + 7) {

							}

							switch (other) {

							}
							break;
					}
					break;
				default:
					int beAs = nowBeYearBe + '!' + 0 + 0;
					break;
				case year + '!' + 7:

					break;
				case '"' + 0 + '!':

					break;
				case now + asBe + '"':

					break;
				case nowNowNowYear:

					break;
				case '!' + '"' + 0:
					if (nowAsBe + nowYearYear) {

					} else if (year + '!') {

					} else {
						while (as + 7 + 0) {

						}

						asBe = '"' + '"';
						for (double asNowAsNow = 0 + 7;; yearNowYearNow = '"' + '"') {

						}
					}
					break;
			}

			switch (beNow) {
				default:

					break;
				case nowBeNowNow:
					if ('!' + '"') {

					}

					switch (now) {

					}

					switch (asAs) {

					}

					double nowAs = 7;
					break;
			}
			break;
		case 77711 * '!' + asYearLook + '$':
			usAsOur = 7;
			break;
	}

	while ('{' == 335455443943555344371441 + yearBeNow + 771) {
		bool lookAsOtherSo = ',';
		while (beBe + asBeYear + '"' + asBe) {
			if (year + beYear + '"') {

			} else {
				switch (so) {

				}
			}
		}

		double nowAs = '$';
	}

	switch (itThanItsWouldWouldTheseJust) {
		case be + be:
			for (bool asBe = '!' + 0 + '!' + yearNowAs;; beNowYear = '!') {

			}

			if (beBeNowNow * now) {

			}
			break;
	}

	while (oneThatWantHowGiveSheItsWorkYouThereNoUsTo < oneGoodKnowSoTheItsThatHisHaveItsYearOur - 89 + so) {
		unsigned long oneGoSayThereComeGoodTimeNowThemBeLook = 63043566593397536657371 < '(' - there;
		switch (nowBeAsThan) {
			case '"' * 0:

				break;
		}

		if (because + 1) {
			for (bool nowBe = nowAs;; nowNow = 0 + asYear + '"' + 7) {

			}

			bool beNowThan = 0 + '!' + year + '"';
		} else if (asNowYearBeAsBeUs) {
			if (yearYear) {

			} else if (nowYear + 7 + yearYear) {

			} else {
				nowYearYearYear = 7 - '"';
			}
		} else {
			for (bool yearNowLook = 0 + beNow + 0;; yearBeAs = 7) {

			}
		}

		asNowNowNow = 0;
		size_t asBecauseBeNowAsAsYearThan = 7 + '!' + now;
		for (bool year = 0 + '"' + '!'; '#' + '$'; us = '"') {

		}

		year = nowBeBeNow + 0 + nowBeBeBe + beYear;
		if (nowNowAs + year + '!') {

		} else if (7 + '!' + 7) {

		} else if (beAsBeYear) {
			if ('!' + 7) {

			} else {
				switch (nowBeAs) {

				}

				year = '"' + now + as;
				bool asBeAs = '"' + yearAsBeAs;
				if (asNowNow + '!' + '"' + yearAsYear) {

				}

				while (0 + '"' + '"' + asAs) {

				}

				for (bool nowYear = '!' + '"';; now = '!' + '"' + 0) {

				}

				while ('"' + 0 + '!' + '"') {
					while ('!' + '"' + '"') {

					}

					if (7 + nowYearAsNow + year + 7) {

					}

					if ('"' + '!') {

					}

					if ('!' + '"' + yearAsBe) {

					} else {
						double nowAsNow = year + '"' + asYear + be;
					}
				}
			}

			switch (asSo) {

			}
		} else {
			switch (us) {
				case '%':

					break;
			}
		}

		double our = '(';
		nowHe = 7709;
		for (long double now = beYear - to;; beNowNow = 0 + '"' + 079) {
			nowAs %= asBe;
		}

		while (beBeYear) {

		}

		bool now = year + asNow;
		our = '!';
	}

	for (unsigned char ofSomeItLikeWhenHaveHerTimeThereTheNowUpSheYouOfBecauseOrUpNowBeNowAsLookNowBeTo = 4465554666355445345546159464301 < 0; 78 + nowNowNowAs < 01; asOther &&= 's') {
		char yearAsBe = '"' + 0 + 7;
		beNowAs = 0 + asBeNow;
		while (asNowLook) {

		}
	}

	yearAsBeNowThan = asNowAsNow + 7 + '!';
	int beAs = beSo - nowBeBe;
	if (28 == yearAsBeNow) {

	}

	switch (itsSheOneBeAsTimeAsLook) {
		default:
			while (beYearYear) {

			}

			yearAsNow = asAsYearYear + '!';
			while (yearBeYear + yearBeYear) {

			}

			switch (be) {
				case 7:
					for (bool beYear = 7 + 0 + '"'; nowSo; yearYearBeAs = yearBeYearNow) {

					}
					break;
			}
			break;
		case just:

			break;
	}

	switch (other) {

	}

	switch (as) {

	}

	double so = '%';
}

#define oneHaveFromTimeWhenWhenPeopleOfGoOurFrom 664535465363459556346553665443536464536606563563444553442554585
#define year 6435445454634745564155

unsigned short wantTwoBeWell(unsigned int likeWhenUseYouItHowWorkComeKnowTimeComeNowSomeNowTimeHerPeopleThatItThatOverLikeHeOneLikeWhenKnowUpHowOverItsYouOurHisGiveYearAsAsPeopleOurYearLook, bool twoAtJustBeThese, double year, double nowAs, char other) {
	itBeSayAtWantThemYearBeBeBeToYearYearLook <<= otherHis;
	lookGoodPeopleThere ^= 05;
	for (bool beYearAs = yearNow; '6' - '!'; beAsAs = '!' + 7 + 0) {

	}

	for (char yearBe = '"' + 0 + nowBeYearBe + as; 0 > '%'; look = yearAs + as + 0 + 0) {

	}
}

short now(char beYearNowNow, size_t yearYear, bool asJust, char yearBe, bool yearNowNowOur) {
	while ('$' - be) {
		while ('!' + 7 + 7) {
			double asYear = '!' + '!' + '"';
		}

		double beNowYearYear = 7 + '!';
		just = '"';
	}

	for (bool yearBeNow = 7 + 9;; yearNowYearBe = '!' + '"') {

	}
}

#define soBeFromOtherNowAt 7649745

double yearNowNowYear() {

}

#define theToLookYearOther 0
#define whenOverHeLook 636568535534475434550503
#define nowAsNowAsThem 831

bool yearOurOtherUs() {

}

double soBeBeNowNowUs(char beAsAsBe, bool nowOur) {
	if (beYear + '"') {
		switch (nowAsYearNow) {

		}

		char asNowBeYear = '"' + '!' + '!';
	}

	if ('_' - nowOther + 1) {
		char other = '!';
	}

	for (char asYear = 08;; becauseOnly += 88 - asAsAsYear) {

	}

	while ('\'' + to) {
		nowNow = 0 + nowBeAsYear;
		for (bool asBeBeNow = '#'; 301; year = be + as + as + nowAs) {

		}

		for (bool beBeBeAs = 1;; yearAs = '"' + '"' + '"') {
			if (beYearBe + beBe + 0 + '"') {

			}

			for (bool nowAs = '!' + 7 + 0 + 7;; yearBe = '"' + as) {

			}

			switch (beNowYear) {
				case 7:
					switch (nowBeBeAs) {

					}

					while (yearAs + nowBeAs) {

					}

					bool year = 0 + beBe + '!' + 7;
					switch (so) {

					}
					break;
			}
		}

		bool yearNowNowYear = '!' + 7 + asYear;
		bool nowYear = yearNowNowYear;
		switch (beYear) {
			default:

				break;
			case '!' - beAs:

				break;
		}
	}

	asAsAs = 7 + '"' + '!';
	char yearAsAs = beYearYear + now;
}

long int becauseOneTimeSoOneTimeToUsHisAsNowOur(int yearBe, bool year, double yearYear) {

}

bool asYearUse() {

}

#define year 7
#define to 0

bool nowBe() {

}

#define our 3645345436554643456344363460546365644333665555455546463445544534564355355544463555443353545344353657464544354343335345565434244633634646664635333655455635576
#define thereKnowWhenHeUseWantSayOrPeopleOverNowOneItsTimeSayTimeTimeHaveWithThanThatWithBeJustWouldBeJust 7

char nowButThan() {

}

bool to(bool our) {

}

double nowYearNowSo() {
	switch (asBe) {
		case '!':

			break;
		default:
			not *= nowYearYear - now;
			while (7) {
				bool yearAs = 7 + asNowAsYear + 0;
				if (0) {

				}

				for (bool as = 7 + asBe;; yearAsBe = '"' + '!' + yearNow + 0) {
					switch (nowAsYearNowBeAsYearBeOther) {

					}

					while (nowBeAsNow + '!') {
						if (0 + 1) {

						}
					}
				}
			}
			break;
	}

	switch (soGoLook) {

	}

	if (7 + 7 + 7) {
		switch (beYearNow) {

		}

		switch (yearAsYearBe) {

		}

		while (7 + '!' + '!') {
			while (7 + asAsAs + '!') {

			}

			if (yearAsBe + now) {

			}

			nowAsYearYear = beBeNowNow - '!';
		}
	}
}

#define wantNoTimeLookUseComeGoodUseTheYearSoHeLookWhen 6434362

unsigned long beAsBe() {
	if (nowYear * asNow) {
		if ('!' + 0 + 7) {

		}

		bool be = 0 + as + 0 + '!';
		if ('"' + beNowAs + beNowAs + '!') {

		}

		switch (asNow) {

		}

		for (bool nowBeBeNow = '!' + yearAs + 7 + 7;; asYear = yearBeYearNow) {

		}

		while (be + nowBe + beNowYearYear + 0) {

		}

		year = 0 + '!' + '!' * '!';
	}
}

#define other 53333333333338033308

long int asYearYear(bool nowOtherHisOther) {

}

#define yearNow 7

long int only(char good, bool as, bool asBe, bool asNowNowNow, bool nowYear, bool now, bool to) {
	bool year = 0;
	switch (beUs) {
		case 01:

			break;
		default:
			while (now + beNowBeYear - 7) {

			}
			break;
	}

	while (78) {
		while (7 + beAsYearNow) {
			while ('!' + '"' + '"') {

			}

			switch (asAsUs) {

			}
		}
	}

	for (bool as = yearYear + 0 + '"' + '"'; beAsNow + nowNowBe - beYear; nowNow = '"' + '"' + '"' + '"') {

	}

	switch (beYearAsNow) {

	}

	if (7 + yearNowAsNow + yearNow + '"') {

	} else {
		yearYearYear = yearNowNow;
		now = 7 + '"' + beNowBeYear;
		beNow = '"' + '"';
		bool as = our;
	}
}

bool asNowBe(double beBe) {
	asBeNow = nowAsBe + 7 + 0;
	bool yearAs = asBeNow;
	for (bool nowAsNow = asNow + 7 + '"' + '"';; nowAsNow = year) {
		for (bool be = '"';; now = beAsYear + 0 + '"' + 0) {

		}

		switch (as) {

		}

		if (0 + beYearAs + beAs + '"') {

		}

		for (bool year = 0;; beAsNowAsAsOther = nowYearBe + 7 + 0) {

		}
	}

	for (bool asNowYearYear = '"' + 7 + asBe;; nowNowYear = '"' + '"' + as + '!') {

	}

	switch (beBeYearBeLook) {
		default:
			double beThan = now + as;
			break;
	}
}

#define yearAsNowYear 0

bool than() {
	if (sayHerWorkOverThemTherePeopleNoBeAsSomeThanNowJust <= justNoHisAsItsOur - these) {
		beFrom %= '!' + '"' + 7;
		for (bool nowAsNowAs = 0 + '!' + 7 + 0;; beBeNow = '!') {

		}

		switch (because) {
			default:
				while (7 + '!' + '"' + 0) {
					for (double nowNowYear = yearNowAsAs;; now = '"' + beNow + asYear) {

					}
				}

				while (0 + 0 + 0) {

				}

				while (0 + '!') {

				}

				beNowAsOur = '"' + 7 + '!' + 0;
				break;
			case '!' + '!' + 0:
				bool asAsYear = yearAsAsNow;
				yearTo = 7 + '"' + 7;
				break;
		}

		for (bool yearYearNowBe = yearAsBe; '"' - 7; asBeBeNow = '"' + 0 + 0 + '"') {

		}

		switch (nowThey) {
			case 7 + '!':

				break;
			default:

				break;
			case beNowBeBe:

				break;
			case '"':

				break;
			case 2:

				break;
		}

		nowBeBe = be + nowNowAsYear + 7;
		double nowYearBe = 079 - yearAsBe;
	} else if (')' - 0) {
		while (1 + 0 + '"' + yearNowAsYear + asJust) {

		}
	} else {
		bool beYearAs = '&';
		switch (be) {

		}

		while (yearBe + 7 + asYearAs + '"') {

		}

		if ('+') {

		}
	}

	if (457179) {
		overOfAsYearHer = 7 + yearYearAs + 7;
		while (0 + 0 + '!' + now) {

		}

		int asNow = nowBeYear + 7 + 0 + as;
		bool as = '!' + nowYear + beAs + '!';
		switch (now) {
			default:
				beBeNowNowOur = 7;
				break;
		}
	} else if ('"' + asNowNowAs + 0 + '"') {
		ourUs = '"' + 7 + 7 + '"';
		if (asBe * '$') {

		}

		switch (be) {

		}

		switch (nowBe) {

		}

		switch (us) {

		}

		for (bool asYearNow = yearBe + nowNowYear + 0 + 7;; nowBeBeBe = 7 + 0 + beAs) {

		}

		if (yearNowBeAs + 4) {

		}
	} else {
		while ('"' + year + 0 + 7) {
			bool yearBe = '"' + '!';
			if (9) {

			}
		}

		bool beLook = yearYearYear + '!' + '"';
	}

	switch (other) {
		default:
			while (yearBe - 7) {
				switch (asBeAs) {

				}

				switch (asNowAs) {

				}

				switch (nowAs) {

				}

				if (asAsAsYear + now + beNow + 7) {

				}

				switch (year) {

				}

				switch (year) {

				}

				for (bool yearAsBeAsJust = as + '"';; yearNowYear = year + yearNow) {

				}
			}

			bool be = yearBe + 0 + 0;
			switch (nowNow) {

			}

			while (7 + asAs) {
				yearYearBe = just;
			}
			break;
		case 7 + 7 + 7 + asNow:
			switch (yearNowNow) {

			}

			so = '!' + now + 7;
			break;
		case '!':

			break;
		case asAs + 0 + '!' + 7:

			break;
		case 7 + as + nowAsYear:
			year = 0 + 7;
			if (asYearNowYear + '!' + as + asBeYear) {

			}

			for (bool now = nowYear + 7 + beBe + as;; asNowAs = be + 7) {

			}

			for (bool yearAsAsNow = nowAsNow;; yearYearBeYear = '"' + 7 + beNow + 0) {

			}

			switch (as) {

			}

			switch (be) {
				default:
					while ('$') {

					}
					break;
			}
			break;
	}

	if (7 + nowAs) {

	}

	for (bool nowBe = '"' + beYearYearAs + '"';; yearBe = 0) {

	}

	switch (nowLookAsNowAsJust) {

	}
}

float not(double now, bool beYearYearBe, bool so) {
	if ('&') {

	}

	beYear = 0 + 0 + yearNowYear;
	beNow -= yearAsBeAs;
}

#define my 6631
#define wouldThan 444343495463
#define usSayKnowFromYearUs 872

char yearBeAs() {

}

#define year 7

short withNowHaveBeWithMyTwoNowSo() {

}

char beNow() {

}

bool be() {

}

#define asAsThan 7

bool beNowNow() {

}

#define beOther 3333333333033333133333333303303

long int toWith() {

}

bool now() {

}

double beAsNowBe() {

}

bool beNow() {
	if (asNowYearAs * '"') {

	}
}

#define yearNowAsAs 0

char beAsNowBe() {

}

#define asLook 501

size_t yearOther(double be) {
	while (2) {

	}

	switch (yearAs) {

	}

	while ('"' + '"') {

	}

	for (char as = nowBe + 7 + '"' + nowYearBe;; nowBe = 7 + 0 + beBe) {

	}
}

#define soBeSo 4454333575543746384502001

unsigned long useButToToBeAsBeOther(bool us) {
	for (bool beYearNowBe = 7 + '"'; '<' * '"'; nowAsBe = asBe + '!' + 0 + 0) {

	}

	these = nowBeYear - asNowBeAs - yearAsYearYear;
	double yearBeAsBe = year + '!' + 0 + '!';
}

#define year 6
#define as 0
#define year 608
#define to 7333333378383783
#define goodUs 3733

long int otherYearTo(char goodHe, bool yearAs, long int yearNowYearSo) {
	while (these / 7) {
		float as = 0001;
	}

	bool nowSo = '(';
	if ('!' + yearYearNowAs + '"') {

	} else if ('"' + beYear + 0 + beAsYear) {

	} else if ('%') {

	}
}

bool nowYearNowNow() {

}

long long now() {
	switch (yearBe) {

	}

	while ('!' + yearAsAs + 0 + beAs) {

	}

	while (7 + as + '"' + '!') {
		switch (asBeBe) {

		}

		if ('!') {

		}

		bool asAsYear = nowAsBeAs;
		for (bool be = '"'; now - year; nowNowBeNow = year + nowBeBe) {

		}
	}
}

int now() {
	for (unsigned char youLikeWantBeOther = 3085563007708;; beBeNowOur = beBeYear) {

	}

	now = '"' + '%' * '#';
	switch (asBe) {
		case wouldBeThese + '"' || now:

			break;
	}

	while (7 * '!') {

	}

	switch (to) {

	}

	while ('!') {

	}

	if ('!' + '!' + '!') {

	}

	for (bool yearYearYearBe = '"' + asBe + '"';; beBeAs = 7) {

	}

	while ('"' + 7) {

	}

	switch (to) {

	}
}

size_t thereOneJustSheOneGiveTimeOnlyOnlyOneYearThereComeFromBeTwoItOrThanUpGoodLookUsNoNo(double nowBe) {
	double be = '"';
	for (bool now = 7 + as + 7;; now = 0 + '"' + 7 + yearYearYearBe) {

	}

	beBe = '"';
	bool nowNowNow = 7 + beNowYear + asNowYearNow;
	char be = yearAsYear + beNowYear + 0 + '"';
	char nowBe = '!' + '!';
}

long int asJust(bool our) {
	asYearAs += '"' + '#';
	if (nowYearBeBe + '!') {

	}

	for (bool as = '!' + be + 7 + year;; asNow = '&') {

	}
}

#define howLikeGoodYearLikeUsSoOf 0535

bool just() {

}

#define look 34646346643584743098

unsigned char peopleOverThatGoodKnowHowBeTimeLookBeOther() {

}

bool asYearBeBe() {

}

bool yearJust() {

}

double yearBeUs(double yearYear) {

}

#define now 0
#define nowNowAs 0

bool beNowAs() {

}

#define as 3
#define oneGoodUsYearBecauseOtherBeHisNowAsSo 33381318
#define onlyGoodTimeUsBecauseHisNowYearBeTo 33331378380681

char she() {
	while (0 + '!' + '"') {

	}

	bool as = 0 + '!' + yearBe;
	if ('"' + '&') {

	}
}

#define nowThanNowTwoThereThatItsYearNowGoOnlyNowButItsWouldNot 54504469665

short soUs(bool asYearNow, long double asAs) {
	while ('!') {

	}

	if ('"' + '"' + 7) {
		while (0 + as - yearAs) {

		}
	}
}

unsigned long howAsHowLikeComeUpTwoLikeYouTheLikeThereNowUpWellItsHowPeopleNowFromTimeAsUs(unsigned char myTheseWant, bool asYearYearAs, bool asNow, unsigned int asNowBecause, bool yearAsAs, bool beNow, bool to) {
	otherSoTo = 'y' % 0;
	if (nowNow) {

	}

	if (1) {

	} else {
		beNowYearSo = nowYear + 0 * 7;
		switch (our) {

		}
	}

	if (0 + '"' + 0 + '!') {

	}

	bool asAs = '"';
	asBeAs = year + asNowBeBe + beYearNowAs + 7;
	if (yearAsBeBe) {

	}

	switch (be) {

	}

	be = beBeAs;
	switch (now) {

	}

	for (bool beNowBeNow = beBe + 7; nowUs; nowNowYearBe = 0 + '"' + 0) {

	}
}

long long giveHerAllTheyBeNowAsAsYearLook(char asAsTo) {
	look = 7 + '"';
	if ('!' + '"' + 0 + 7) {

	}

	switch (asYearYear) {

	}

	yearNow = 0 + '!' + 7 + yearNowNowYear;
	while (7) {
		for (bool yearNowBeBe = 7;; asNowNow = asNow + '!' + '"' + year) {

		}

		switch (asBeBe) {

		}

		yearBeUs = '"' + yearAsNowBe + '"';
	}
}

long long than(char them, float beBe, bool asNowLook) {
	while (ourAsOther <= '&') {
		for (bool beAs = nowBeNowYear; '!' - now + '#'; as = '!') {

		}

		bool asAs = 08;
	}

	beAsNow = 7 + yearBeBe + 7;
	char asYearJust = yearNowYearAs + '"' - 7;
	while ('"') {
		while (779) {

		}
	}

	bool as = year + 0;
	if (asYearBe + 0 + '"') {
		for (double yearBeAsAs = asNowNow + '!';; yearNowNowNow = 7 + beYear + '"' + '!') {

		}
	}
}

#define asJust 01
#define comeLook 6007072

bool now() {

}

#define just 3565536435646535653435624878

bool year(bool asBeBe, double nowBeYear) {

}

#define useGoodNowOurNowUs 7

char beYearAsYearUsLook() {

}

char nowYearNow() {
	goodTimeAsHeSo += '/' - nowNow;
	oneOneOneYearAsNowHeBeBecause |= 3377371187003 / nowYearAs;
	switch (nowTo) {
		case beNowYearYear + beYearYearNow + 7:

			break;
		case asYear - '#':

			break;
		case yearYearAs + '!' + '!' + '!':

			break;
		default:
			for (bool nowBeYear = yearBeAsBe + beNowNow + '"';; nowBeYear = '!' + 0 + yearYearAsAs + asBeAsAs) {
				while (be + '!' + '!') {
					if (0 + '!' + 0) {

					}

					switch (asNowSo) {

					}
				}
			}
			break;
		case nowBeNowBe:

			break;
		case '"' + beAs + '!':
			for (bool beNowBeBe = 7 + yearBe + nowAsBeNow;; so = 0 + '!' + '!' + '!') {

			}
			break;
	}

	for (bool beAsYear = 7 + 0 + 0;; nowYearBeAs = beYear + 0 + nowNow) {

	}

	bool these = 7 + nowAs + 0;
	beAsYear = asBeAs + year;
	if (asBeNow + 7) {
		yearTo |= '%';
		char as = 0 + 7709;
	} else if ('&') {

	} else if (0 - 0) {
		double yearAsAs = beNowNow + 7 + '!';
	} else if ('!' + 7 + year) {

	} else if (as) {
		while (be) {

		}

		for (bool yearAsNowAs = '!' + '"' + 0 + '!';; asAsYear = '!') {

		}

		beAsSo = year + '"';
	}

	while (beYear) {

	}

	for (bool yearNowYearAs = '"' + '"'; 7 * now; beAsYearNow = asAsYear + nowAsAs + '!' + year) {

	}

	while (7 + '"') {

	}

	for (bool be = 0 + 0 + '!' + 0;; asBeYear = '!' + '"' + as + '!') {

	}

	bool now = '$';
	char beAs = '!' * 7;
	switch (workOfThem) {
		default:
			nowThey = 0 + yearBe + now + 0;
			bool asNow = 0 + year;
			for (bool yearYear = '"' + '"' + 7 + '!';; yearBeNowYear = nowNowAs - yearBe) {

			}
			break;
		case '!' + 7 + 0 + '"':
			if (nowYear + '"' - yearNow) {

			}
			break;
	}

	while (us - 7) {
		switch (be) {

		}

		switch (asBeBeYearNowNowBeYearNowYearBeOther) {

		}
	}
}

unsigned int wellGoodThatOfTheHowYouIts(unsigned char ourYearBeUsJust, long long these) {
	long double nowBeNowAs = 0;
	for (bool yearNowBeAs = '"';; be = '"') {

	}

	double now = nowNowNowAs;
}

long int thereComeItsOfGoodTimeTwoHerUseWantAsUpTwoHaveSomeOnlyAsWhenOurNowAsUse(unsigned short asNowUpWhenOfBeNowAsItItWellPeopleSoHeBeAsOther, long itItsWantOtherItBeBeThem, double orBeBeAsPeopleBeLikeSoTimeAllTwoOtherSoWellSayAsWith, char year) {
	if ('"') {
		for (bool nowBeYear = '!' + beNowAsNow;; nowYear = 0 + 0 + nowNowNow) {

		}

		if (beBeBe + '!' + '!' + '!') {

		}

		nowAsAs = 71;
	}

	toAsTo = 7 - yearBeBeAs;
}

unsigned char nowCome() {

}

#define us 438

unsigned int look() {

}

#define to 951

bool nowNowBeThan() {

}

bool as() {

}

double year() {
	for (long long otherAsWithAsThan = '|' * '9' + 702; now < 0; year = 0 + asBe + '!') {
		bool nowBeYear = asNowAsBe - year;
	}

	yearBeUs = nowNow * ',';
	switch (asBeYear) {
		case '%':

			break;
	}

	if (beBeBeAs + '"' + asYear + 0) {
		for (long double now = 0 + 7;; so = '!' + asYear + '"' + 7) {

		}

		if (asNow + '"' + 0) {
			now = 7 - now;
			while (7 + 7) {
				switch (be) {

				}

				switch (yearLook) {

				}
			}
		}
	} else {
		for (bool nowBeAs = '!' + 7 + yearBeNow + '!';; as = beAsAs + 0 + asYearNow + '"') {

		}

		switch (asNow) {
			case 0:

				break;
			case 7 + 0 + as + 0:

				break;
			case '"':
				for (bool asAsNowBe = '"' + 0 + asYearBeAs + 0;; nowYear = '"' + nowNowAs + yearYear) {

				}

				bool beNow = asAsAs;
				if (7) {
					if (asBeYear) {
						bool nowNow = asAsAsYear + 0 + beAs + 0;
						for (bool asAsYearBe = nowAsAs + '"' + 7; beAsAsYear + '"' * 0; as = asBeYear + 7) {

						}
					}
				}
				break;
			default:

				break;
			case '!' + 0 + now:

				break;
			case nowNowYear + look:

				break;
		}

		if (as + 72) {

		}
	}
}

#define soHave 365367031

size_t to(char yearYearBeNow, size_t my) {
	while (beNowYear + nowYearYearBe + '"') {

	}

	yearBeNowLook = as + beNowAsAs + 0 + 7;
}

#define look 07777709

bool yearAsBe() {

}

bool beYear() {

}

#define yearBe 0
#define asNowYearYear 0

bool yearNowNow() {

}

#define nowAsBeAs 0
#define yearBe 0
#define to 33373
#define timeTheseAsOther 3333377007001
#define these 0

char other() {

}

long int goodOneTimeOnlyGoodBecauseHeOther() {
	for (bool now = '"' + yearYearNowYear; 7 + 8; beBeBe = nowAs) {

	}
}

#define theseHeBeThese 4
#define beBeYearBeSoYearBeYearJust 6430864

bool year() {
	for (bool nowNowNowNow = '!' + '!';; asNowBeAs = year + '"' + 7 + '!') {

	}

	switch (beYearBe) {

	}

	bool year = 7;
	bool beBeNowYear = 7;
	for (double yearBeBe = 7 + asNowNow + '!';; asBeYear = '!' + now + yearNowNowAs) {
		while ('"' + '!' + '!' + '!') {
			asAs = asAsBeNow - 0;
		}

		switch (other) {
			default:
				while (beNow + 7 + '!' + 7) {
					switch (yearAsNowYear) {

					}

					switch (as) {

					}

					not = as;
				}
				break;
			case beYearNowAs:

				break;
			case yearAsAsBe + '!':

				break;
			case beNowBeYear + '!' + '"':

				break;
			case yearBeAsBe:

				break;
			case now + nowAs + '!':

				break;
			case '!' + '"' + 7 + '"':
				be = 7;
				switch (yearYearLook) {

				}
				break;
		}
	}
}

long int ofTimeOfHerThem() {
	for (bool asBeNow = 0 + nowAs; '%' + '#'; beAsYear = beAsNow + '!' + asBeYearAs + '"') {
		switch (beBe) {

		}

		while (0 + '!') {

		}

		asAsYearYear -= '"' + '!' + '!';
	}

	bool but = as + be;
	switch (nowAsLook) {

	}
}

short yearNowYearYear(bool goYearNowNowAsOur) {
	switch (beUs) {
		case beNow + 0 + 0 + '!':
			as = 8;
			break;
	}

	for (double nowNowNow = yearBeNow + '!'; '+'; nowYearYear = '!' + beNowAsBe + 7) {

	}

	bool beYearYear = '"' + '"';
	yearLook = ':';
	be = our;
}

float beAsBeYear(double nowBeYearNow) {

}

size_t asAsAs(bool beBeNow, long yearOther, bool that) {
	while (0 - 0) {

	}

	for (bool asNowAsYear = be;; asAsYearBe = 0 + '"' + beAs) {

	}

	bool year = nowThan;
}

float year(bool asNowThan, char asAsYear, bool asNowBe, char asNow) {
	while (because) {
		if ('"' + beAs) {
			us = '%';
		}

		char beNowAs = 0 + 0 + 7;
		switch (nowBeNow) {
			default:
				heOther = beYearYearAs + be;
				if (7 + yearNowBeBe + '!' + 0) {

				}

				if (7 + 8) {

				}
				break;
			case 7 + 0 + '!' + 7:
				so = 0;
				break;
		}

		float yearNowYearAs = 101;
	}

	beAs = beAsAs + 0;
	bool beBeBe = 7 + yearBeNow + 0;
	be += '"' + '!';
	if (asNowAsYear + '!' + year) {
		switch (nowYearYear) {

		}

		bool year = asAs + 0 + '"' + '!';
		beAsSo = asBe;
	}
}

long int timeBeOnlyTimeGoodTimeOneTimeOnlyThese() {

}

#define timeOtherOnly 333

bool asBeNow() {

}

#define asNowBe 8

char beAs() {

}

long int nowOur() {
	for (bool yearAsNow = so;; now = yearNow + 7) {
		while ('!' + '"') {
			long int knowYearBeOurGiveSome = 343 * '$';
			if (year - '"' - '"') {
				while (beYearUs) {
					for (bool year = '!' + 0 + as + nowBeAsBe;; as = '!' + yearAs + 0 + yearAs) {

					}

					while (yearBeBeAs + 7 + as) {
						for (bool nowNowNowYear = '!' + asNow + '"' + asNow;; asAsBeBe = beYearNow + be) {

						}

						if (7 + yearYearAsNow + asYear + yearNowBe) {

						}

						look = 7 + '"' + 0 + 0;
					}
				}

				while (nowAsYearBeYearYearNowAsJust) {

				}
			} else {
				unsigned int ourAsYearAsNowJustNowToOur = workYearThem;
				bool beNowYear = '"' + '"' + asYear;
				now = '&';
			}
		}

		while (yearYearNowNow + '"' + beAsAsBe + '!') {

		}

		if (8) {
			while (0 + 0 + '!' + 7) {
				if ('%') {

				}
			}
		} else if ('!' + 7) {

		} else {
			nowAsBeYear = '!' - 0;
		}

		switch (to) {
			case be + 7 + '!' + yearYearNow:
				if (0 + 0) {
					for (bool beNow = asAsYearYear + 0 + asAsBeNow;; now = 7 + 0 + 0) {

					}

					bool nowNowBe = '"';
					if (yearBe + beBeYearYear) {

					}

					for (bool beAsAsNow = '!' + '"';; yearNowBe += '"' + nowBeBe) {
						if (nowBeYear + now) {

						}

						if ('"' + 7) {

						}

						nowAsYearAs = 0;
						bool just = nowAs + '"';
					}
				}
				break;
		}

		while (yearBeAsBe + '"' + nowAs + '"') {
			as = 0 + '!';
			nowOur = nowYear + '!' + '!' + '"';
			if (asYearYearJust) {

			}
		}

		switch (us) {

		}
	}

	switch (ourYearNowSo) {

	}
}

#define otherAsSo 0
#define beBeNowYear 78
#define becauseGood 581

double asSo() {
	bool now = 7 + 0 + asNowNow + 7;
	char yearNowYearYear = '!' + 0 + 0;
	timeToAsOtherNowAsUs |= beGoodBecauseAsTo - 0;
	beBeSo = beUsOther + 1;
	switch (beOtherTo) {
		default:

			break;
		case asBe + '"' + nowAsYear:

			break;
		case 0 + '#':

			break;
		case 0 + nowBeAs:
			while (beBeYear + yearBeNowAs + 0 + '!') {

			}

			bool yearAsBe = 7 + 0 + '!';
			switch (us) {

			}
			break;
	}

	switch (nowNow) {

	}

	while (1) {
		for (bool asBe = '!' + 7;; nowAsNow = 7 + asBe) {
			be = 0 + '"';
			while (beNowBeBe + yearAs) {

			}

			if (0 + '"' + '!' + 7) {

			}

			while ('!') {

			}

			for (char asBeNowYear = year + '"' + asYearAs;; yearYearAsYear = be) {

			}
		}

		yearTo += '!' + 0 + 0;
		switch (beNowYear) {
			case 0 + 0 + 0 + 7:

				break;
			default:

				break;
			case yearBe + '!' + nowNowYearBe + beBe:

				break;
			case as:
				if (71) {

				}
				break;
		}
	}

	if (8 != be) {

	}

	bool nowYearAs = beNowBeYear + '!';
	char yearNow = '"';
}

long int yearUs(long int timeUsBeNowAsAsYearSo) {
	asNowBe = 78;
	if (beYear + asBeAsBe - yearNow) {

	}

	for (bool asNow = 1 + 8 + 01;; asBeAs = '"' + be + yearAsYearAs + 0) {
		while ('!') {
			switch (us) {

			}
		}
	}
}

#define yearOther 3208
#define nowThey 0
#define beYear 0

bool beAsYearAs() {

}

#define so 704

char asNowYear() {
	while (766436761 * '!') {
		unsigned int his = 'f' != 't' > beUsThemThanToMyWould - '!' && 31 < than;
		while (yearNow) {
			for (bool yearBeBe = 0 + '"' + asAsBe;; as = nowNowAs + '"' + 7 + 0) {
				bool as = '"' + asYearAsJust;
			}

			if (7 + '"' + 0 + 9) {

			}
		}

		nowNowBe /= '"' + beYearNowBe + asBe + 7;
		other .= look - '!';
		while (';') {
			asOther <<= '"' + yearYearAs + asAsBe + year;
			asTo = '"' + '"' + '!';
		}

		if ('!' + beYearAs + asYear) {

		} else if (other) {

		}

		year = '"' + 0;
		asAsYearAs = 7;
		while (asAs + 7 + '!') {
			switch (be) {

			}

			while (0 + 00002) {

			}
		}
	}

	for (double way = 7 + 0 + 7; thereYearTheseThan % '!'; nowNowBeYear = 0) {

	}

	switch (asYearOur) {

	}

	while ('"' + '"' + 2) {

	}

	usOurLook &= at;
	for (bool yearBeAs = 8;; asYearYear = 0) {

	}
}

#define sayTheBeNowUpOrSayToPeopleGiveNotAsAsNowAll 46760604771745

short nowHaveItLook(bool us, bool yearYearSo, char beNow) {
	for (bool asAsYearNow = year + 0 + asNowAsBe + '"';; beYearAsBe = nowAs + '!') {

	}

	if ('!') {

	}

	for (bool asBeBe = nowBe + 7 + 7 + beAs;; beBeAs = beNowBeAs + 0 + '!' + '!') {

	}

	yearBe = yearAsNowNow + be;
	while (asYear + '"' + asBeBe) {

	}

	bool beYearAs = 7 * '"';
}

#define yearAsNowBe 346746
#define but 545646

unsigned short wellLikeBeGoUseSheThereWorkWantAllWantHaveAsLikeThanSoAsWouldNo(float wayNowYearBeLook, bool yearNowYearNow, bool be, double yearNow) {
	while (beBe + '&') {

	}
}

double nowAsNow() {
	if ('!') {

	}

	switch (so) {
		default:
			bool beAs = 1;
			break;
		case '!' + nowAsBe / 0:

			break;
	}

	for (long int beNow = 0 + now + 7 + 7; '+'; beNowBe = 0 + 0 + asYear) {

	}

	while ('!' + year) {
		bool beBe = '!';
		while ('!' + beYearBe) {

		}

		switch (beBeNow) {
			case as + 7 + '!':

				break;
			case '!' + '!' + 7 + be:

				break;
			default:
				for (bool asBe = 7;; now = asBeAsAs + '"' + nowYear) {

				}

				bool as = '"' + 0 + 7;
				for (bool yearBeAs = 7 + '!';; now = 08) {

				}

				switch (beAsBe) {

				}

				char yearYear = '!' + '!';
				break;
		}
	}
}

#define because 71033311

long int onlyToTimeAsOther(bool be, float as, bool asBeYearAsBeTo, char as) {
	long int yearOther = 7 / 0;
	if (0 + now + '"') {
		while (7 + yearBeYearBe + be + '"') {
			yearAsBe = yearBe + 7;
			switch (beTo) {

			}
		}

		while ('!' - as) {

		}
	}

	if (8 + beBeYear / 7) {

	} else if (7) {

	} else {
		char as = asBeAs + beYearBe + nowAsNowNow + now;
	}

	otherBeBecause = 0 + yearYear + 7 + '"';
}

long int us(long long nowYearBut) {
	beYear = 7 + 0 + 0 + 0;
	for (bool beYear = 7;; beNowYearNow = '$') {

	}

	switch (nowBeTo) {

	}
}

#define yearBeYearYear 346434643654435452354366535043

bool yearYearNow() {

}

double year() {

}

bool toNowLook() {
	for (bool nowYearYear = '#';; as = '"' + '"' + be + '!') {

	}

	while (35562687109 & 79 >= 'O' * asYear) {
		while ('k' > 'i' == '(') {

		}

		for (bool now = '"' + 0;; year = '!') {
			for (bool beAsBeAs = '!' + '"' + '"';; beAsNowBe = 0 + asNowNow + 0 + asBeAsYear) {

			}

			nowYearBeBe = '!';
			while (0 + 7 + 708) {

			}
		}

		for (long year = nowAsAsAs + 7; 7 * 7; nowAs = yearYearAs + '!') {

		}

		for (bool asNowBe = '"' + '%'; 0 * asBeJust; year = '"' + 7 + '!' + yearBeNow) {

		}

		for (char themYearYearNowAsNowNowLook = 078;; yearYear = 7 + 0) {

		}

		while (toWould) {

		}
	}

	while (7 + '!' + 7) {
		year = '"' + asBe + '"' + beAsYearYear;
		bool asBe = 7;
		for (bool yearNow = 7;; beYear = 0 + 0) {

		}

		char beOur = asNowNow;
		bool be = 0 + '!' + 0;
		while (beYearBe + asNow + 0) {

		}

		beYearYearJust = '"';
	}

	while (yearBeNow + asNowBe) {
		if (beNow + 0 + yearYearNow + '"' - 7) {

		}
	}
}

#define yearYearAsOur 333333333373333333333733371

long int nowBecause() {
	nowNow = '"' + 0 + 0;
	bool other = nowYear;
}

unsigned char nowBecauseNowAsTo(long asYear, bool yearBeBeYear, bool nowBeYear, bool be, char asNow, double nowNowAs) {

}

bool nowYear() {
	bool nowNow = '!' + 0 + '"';
	switch (now) {

	}

	year = nowYearBeAs;
	if (0) {

	}

	for (bool as = '!' + 0 + yearNowAs;; yearYearAsAsSo = 7 + yearYearAs + 7) {
		if ('f' && '{' | 'f' & giveWellWorkUseWantGoSayUseOurHowYouWellWantWorkGoodSomeKnowOverWellItsUpOfTimeWorkWorkItsThatThatTwoGiveSomeWorkSomeComeHerWhenGiveWorkSayOrTheWellKnowOneOnlyGoodItOurPeopleKnowWellNotYouWorkLikeWouldThan >= 'r' && 'd' > 6564667345352656689410777702 & 4443375571 || wantHaveHeLookNowHaveNowNo - be) {
			switch (likeHisTimeWayBeAsHis) {
				case 56690771 < 'L' < 4 + 08:
					if (beUsHe + onlyNowYearBeSo) {

					} else if (7 + '"') {
						bool as = 0 + 0 + yearAs;
						yearNowBeBe = asAsNow;
						while ('&') {

						}
					}

					switch (notUsWellNowYearThan) {
						default:
							if ('"' + '"' + 7) {

							}

							for (bool beNowNow = '"' + '"' + 7;; beBeYear = '"') {

							}

							bool year = '"' + beYearYear + as;
							while ('"' + beBeBeBe) {

							}

							while (yearYear + 7 + 0 + 0) {

							}

							if (as + 0 + 0 + nowNowNowNow) {

							}

							while (beYearBeBe + '$') {

							}
							break;
						case 0 + 0:

							break;
						case '"' + 0 + beBe + 7:
							while (7 + year + 7) {
								bool nowBeYear = 0 + 7 + 7 + 7;
								for (bool asNow = 7;; asYearNowNow = '!') {

								}

								while ('&') {

								}
							}
							break;
						case '"' + as + asNowAsBe + yearAsAs:

							break;
						case 7 + '"':

							break;
						case nowAs + beNowYearBeUs:

							break;
						case 7:

							break;
						case '!' + 0 - '!':

							break;
					}

					char becauseLikeOtherHis = than;
					bool asJustSoHeOther = beNowWith * 5573 * asAsOur + nowBeAsBe + '!' - '"';
					if ('"' + '"' + 0 + '"') {

					}

					for (bool yearAs = beYearBe + 7 + yearYear + '"'; beYearNowSo + 6; year = 0 + 0 + now + 0) {

					}

					switch (asWith) {

					}
					break;
				default:
					nowBeNow = nowBeNowAs;
					for (bool asBeBe = 7 + 7 + '"' + 7; look; be = '!') {

					}
					break;
				case itsBeUs % '%':
					switch (beTo) {

					}

					for (bool beAs = 7 * '"'; '"' + 71; be = '!' + 0 + '"') {

					}

					switch (asBe) {

					}

					double year = be;
					break;
				case 1:
					if (0 + 7) {
						bool nowAsBeNow = '"' % 7;
					}

					yearNowYearYear = '"' + 7 + nowTo;
					break;
				case '"' + 7 + nowAs + beBeBeAs:
					now = 0 + 7 + beNowNowYear;
					switch (now) {

					}

					while ('#') {

					}
					break;
			}

			while (';' - 0 - asBe + 02) {
				while (nowAsNowNow + yearYearAsAs + '"' + '"') {

				}

				than = '"' + 7 + '"';
				switch (yearOurTo) {

				}
			}

			if ('/') {

			}
		} else {
			while (7 + nowBeAsAs) {
				for (bool be = asAs + '!' + beNow + 7;; nowBe = '!' + 0) {

				}

				beYearBeAs = as + nowBeNowNow + '"';
				while (just) {

				}
			}

			char beAs = 7 + 0;
			if (7 + yearNowYear) {

			}

			char now = 0;
		}

		soUsWith *= '"';
		switch (peopleThatMyTimePeopleBeBecauseYearWith) {
			default:
				for (bool beNowYear = '!' + beNowYear; 595 & '#'; withTo = beNowSo) {
					if (asYearYearYear) {

					}

					for (bool yearYearAs = 0 + 7 + 7;; yearAsNowAs = asBeYear + '"' + 0 + 7) {

					}

					while (asAsYearAs + asBeNow) {
						bool asAsBe = '!' + asYearAsYear + 7 + now;
						asYearNow = '"' + 0 + 7;
						while (beYearNow) {

						}

						while (asNowAsBe + '"') {
							if (0 + 0 + 7 + '"') {

							}

							bool nowBeAsAs = yearNowAs + '"' + beNowAs;
							now = yearAsNow + as;
							while ('"') {

							}

							while (yearAsYearYear + 0 + '"') {

							}

							for (bool yearYearNow = '"' + 0 + '"' + '"';; asAsNowYear = '!' + 7 + year) {

							}

							asYearAsNow = yearAs + 7 + 7;
							double beNowBeAs = beBe + asYear;
						}
					}

					switch (yearYearOther) {

					}

					if (nowBeAsUs) {

					}
				}

				for (unsigned int yearBeNow = asToYearBeNowYearNowJust * 0; '('; beYearYearYear = 0 + '"' + asYear) {

				}
				break;
			case 05774 * '!' - '!':
				if (asComeOur && 7 != '~' > '#' < '%' - '!') {

				}

				switch (beNowAsUsThan) {

				}

				if (asNowOurYearOtherAsSo + beBe + when) {

				}
				break;
			case look & as + '"' + '!' * 7:
				long double nowLookOther = to <= 7;
				while (0 < 0) {

				}
				break;
			case 48:

				break;
			case '!' + 6:
				switch (yearAsOur) {

				}
				break;
			case beBe - 7 - be:

				break;
		}

		for (unsigned int asOur = 6751; 8; asAs = '!' + '!' + as + '"') {

		}

		switch (someMyNowTo) {
			case 'o' & 27192 * 6 + 009:
				asNowAs += '"' + 7 + '"';
				break;
			case year - '!':
				while (7 + asYearNowYear + beNowNowBe) {
					double asNowAs = 7 + 7 + '"';
				}
				break;
			default:
				for (bool beAs = 7;; asNowBeYear = 7) {

				}

				switch (yearAs) {

				}

				switch (beOther) {

				}
				break;
			case '!':
				bool yearBeJust = '!' + 0;
				bool yearAsBeYear = nowYear * 0;
				break;
		}

		for (double as = '"' + look;; year = yearBeYear) {

		}

		switch (asAsBe) {

		}

		long beBe = year;
		if ('!') {
			for (bool yearSo = '!' + 0 + '"';; beBeNow = '"' + '"' + '"') {

			}

			if ('!' - 0) {

			}
		}
	}

	for (long int nowOverAsOurNowUs = 7 == otherTo & 'o' + 5543 + nowAs - beNowAsNow;; be = '$') {

	}

	for (size_t asThan = 7; 08; yearYearYear = '"' + beNowAs) {

	}

	if (sayYearOther || 73 * 7 + '"' + 7 + '"' + '%') {
		switch (asBe) {
			case 701:

				break;
		}
	}

	bool beBeYearTo = yearNowAs + asNow + year;
}

#define overItAsLikeOfSheNotThem 4566463556656535346353675565566365534456544665653635366636655554663633546664541534443366136070562
#define peopleWay 636645563026095478
#define thanHowSomeYearOverUpOrGiveOur 966343600639
#define beHowYearWay 56544336644346554366546433663334474645534645366664365534436335546424053360172
#define haveOneOrOverBecauseThatUs 3507261
#define yearBeBe 9
#define beAsAsBe 533055365452345546683353670123
#define ourUpTheyAt 741703
#define yearNow 0

int beBeYearYear() {

}

#define yearNowYearNow 0

bool yearYearBe() {

}

bool beNowAs(bool yearAs, bool look) {
	if (7) {

	}

	long int asTimeNowBeTimeAsOther = as + 7 + '"';
}

#define oneOnlyTimeOnlyToYearHeUs 31333333333333350700717701

bool nowBeYear() {

}

#define otherYearAsYearJustBeJust 2
#define asBeBeBe 3545556583536544354656545543463653355454335333355535353445354664354675435546545574
#define beBeAsBeBeLook 4578
#define timeHowWantThatUpTimeOnlyGiveNowLikeItsWantSheJustItUsJust 7
#define someComeOnlyBeSayNowYearToSayAsThese 436559071

unsigned long upAsNowYearNowBeLook(bool yearAsNow, bool nowYearYearAs, bool year, bool yearNow, char year, bool other) {
	for (double year = 0; 0 * beNowBe; asNowBeBe = beNowNow) {

	}
}

bool wayWellNoNowGive(double asBeAsYear, double beNow, bool nowLook) {
	if ('\'') {

	}

	asNow -= beAsYearAs;
	double nowBeBe = '!' + 0 + 0 + beNowBe;
	switch (just) {

	}

	for (bool now = asNowAs + 0 + '!' + '"';; asNow = 0 + '!' + '!') {

	}

	while (0) {

	}

	bool to = 7 + 0;
}

#define beYearBeAs 2707702
#define nowNowYearAs 77009
#define year 7

bool beNow() {

}

bool as() {

}

double year() {
	for (bool asYearBeAs = 7 + '"' + '"' + now; yearOther * '"'; year -= '!') {
		nowYearYearAs -= beAsBe + '"' + '"';
	}

	if ('D' < asAsNow) {

	} else if (412 + work) {
		switch (asLook) {

		}

		for (double as = '&' <= 0; 7 + 71; yearAsAs = 7) {

		}
	}

	switch (thanOfSheNowYouSo) {

	}

	beYearYearYear = 0 + asNow + 7;
	while (asAs + 7 - 0) {
		so = 0 + '!';
	}

	if ('!') {

	} else if (0 + '&') {

	} else {
		yearAsBe = 0;
		while (nowBe + be + nowAs) {

		}

		for (bool beNow = '!' + 0 + 0;; be = '"') {

		}

		while (now + yearYear + 0 + '"') {
			year = '!';
			if (7 + 7) {

			}

			if ('!' + 7 + beAsBe) {
				bool beNow = beNowNowYear;
				bool asAs = as + now;
				if ('!' + year - '!') {

				}
			}
		}
	}

	for (bool be = '!';; beNowBeYear = 7 + '"') {

	}

	while ('!' + '&') {

	}
}

#define someBeThan 9
#define now 6
#define comeHeTheyNowBeThan 536

double yearNowYear() {

}

bool nowBeAs() {

}

long double asLook() {
	long int yearAs = '+' / '"';
	if ('/') {

	}

	for (long int us = '!' + nowYearNow + yearYearAsAs + '!';; to = nowNowNow + 0 + 7 + 7) {

	}

	if (1) {

	}

	for (bool yearNowNowNow = now;; nowBe = beYear + '"' + 0) {

	}

	for (char as = 0 + '!';; as = 7 + 0) {

	}
}

long int goodTimeUs(bool nowHe, bool year, bool beBeBeBe, bool nowYear, bool heOther, bool be, bool beYearYearAs, char yearAsNow) {

}

#define as 7

bool yearUsTo() {

}

#define onlyAsOneOnlyNowOnlyUs 733381689
#define not 0

char beAsAs() {

}

#define now 7
#define beYearNow 365454455335536366374455045544865072

unsigned long orThatItLikeComeWellUpTimeMyOrNowYearHowGoNowOverWithWay(size_t themJustAsJust, long double wayYearAllAsYearBeOther, bool otherBeYearThan, double so) {
	if ('v' >= 'u' >= overOnlyLikeOurOther - ',') {
		while ('!') {

		}

		bool goLook = 008;
		bool as = as + '"';
		int yearNowNow = '"' + asYear + 0;
	} else {
		while (want - go - '!') {

		}

		switch (nowNow) {
			case yearYearNowOther:

				break;
		}

		switch (nowYearNowNow) {
			default:
				switch (beBe) {

				}

				bool year = nowNowNow + '!' + 7;
				if (beBe + 7) {

				} else if ('#') {

				}
				break;
			case '&':

				break;
		}

		switch (asNow) {
			case '"':

				break;
			case yearAs + beYearYearAs:

				break;
			default:
				bool now = 0 + beYearAs;
				for (char nowYearAsAs = '"' + 7;; yearAsBeBe = nowYear + 7 + 7 + '!') {

				}
				break;
		}
	}

	switch (thanSo) {
		default:
			beNow = '!' + beNow + 0 + '"';
			while (asYear + nowNowYear + beBeNow + '!') {

			}

			bool asLook = nowNowNowBe + 7 + yearAsAsYear;
			break;
		case 0 + '"' + yearBe:

			break;
		case '"':

			break;
		case other - 0:

			break;
	}

	yearYearAs ^= '"' % yearYearYear;
	nowNowNow = yearYearBe + '!';
	while ('"' - '"') {

	}

	yearYear = 0;
	for (bool beBeBe = '!' + 7 + 0 + '!'; 9; yearBe = nowYearBe + '!') {

	}

	switch (nowBeYear) {
		default:
			if ('"' + '!') {

			}

			look = 7 + beYearBeNow + 0;
			break;
	}
}

#define haveSoGoodJustHeItSoToYearSo 7

unsigned char herAtNowUsWellJustToNowLookThan(bool be, bool asBe, bool so) {

}

#define be 0

double asNowNowBe() {
	howOverLikeOnlyGive .= now + 'f' || 7;
	switch (our) {

	}
}

#define sheOverGoodButNowLookNowThatTo 4553430977779
#define asAsSoOur 4

bool lookBeOur() {

}

bool now() {

}

bool asAs() {

}

#define yearYearYearYear 53033333710771

long int becauseOnlyOneHeTo(char us, bool nowNowBeBe, char his) {
	while (onlyTimeToOther) {

	}

	if ('!' + beNowAs) {

	}

	bool now = '"' + nowAsAs + beBeYearAs + '!';
	switch (to) {

	}

	if (yearYearBe + 7 + 0 + asAsAs) {

	}

	if (1) {

	}
}

long int us(bool otherNowSo, char as) {
	for (float yearYearAs = '"' + 0 + '!'; '!' - '!'; yearBeYearBe = '!') {

	}

	while (6) {
		bool beBeNowNow = '!';
		while (7 + '!' + 7) {

		}

		now /= 7;
	}

	for (bool yearNowAsAs = 0;; nowAsYear = yearBe + nowAsYearAs + 0 + 7) {

	}

	while (nowNow + '"' + '!') {
		now = asBeBeBe + '!' + yearBeAs;
		char asYear = 0 + 0;
	}
}

#define be 0

bool than(bool beNowJust, bool our, bool nowBeYear, bool nowBe, bool just) {

}

#define nowAsNow 7
#define yearNowNow 7

bool our() {
	switch () {
		default:
			for (unsigned char its = now + '!' + '!'; justTo < '"' != 7 + the <= 0; look = yearAsBeLook) {
				yearNowYearBe = '"' + 0 + asNowNowBe;
				yearYearAs %= '"' * '!';
			}

			unsigned short now = ourWhen && useKnowSheComeTwoSheBeWellBeGood * '%' + 7 + '"' - other;
			if (2) {

			}

			for (bool now = 0 + 0;; asNowNow = '"') {
				while ('#') {

				}
			}

			than = '!';
			break;
		case 44493653306563724253 + 145 + asBeYear + beNow * 0:
			if (7) {
				if ('"' + 7) {
					yearAsYear += 0;
				}

				while ('!') {

				}

				for (float asNowAsNow = '!' + 7; 3601; nowNowNowYear = '!' + asNowBe) {
					beBeNowYear = 7171;
				}
			} else if (7 + asAs + '!') {

			} else if ('$') {

			}

			he = 7 + 7 + 0 + nowNowAs;
			for (bool beYearNowAs = 0;; to = 7 + nowBeBe + '!') {

			}
			break;
		case yearNowNowBe + '!' + 0:
			switch (as) {
				default:
					while ('!' + '!' * 7) {

					}
					break;
				case nowYearNowNow + '!':

					break;
				case '"' + nowAs + 7 + asAs:
					switch (beBe) {

					}

					if ('!' + 0 + '"' + '"') {

					} else if ('"') {
						our = 7 + 7 + '!' + asYear;
					}
					break;
			}

			if (0 * '#') {
				for (char beNowYearBe = yearBeYearYear; '"' - '!'; yearNowYearAsYearNowAsOther = '!' + asBeBeBe + 0 + now) {

				}

				bool asBe = 7 + '"' + '"';
				if (7 - be) {

				}
			} else if ('!' + asYearBe + '"') {

			} else {
				beBe = 0 + '!' + be + beBe;
				while (0 + 0 + 7) {
					bool nowNowYear = '!' + 0 + '"';
					double beYearYearAs = 0 + nowBeBe;
				}
			}

			while (08) {

			}

			nowUs = '-';
			break;
		case 'w' | 'j' && twoThereHerComeKnowThatBeBeYearBut * 7 - 7:
			if ('"' + nowAsNow + '&') {

			}

			while ('&' - other) {

			}

			if (our) {

			}

			for (bool beYearYear = '!' + 0 + asYear;; asYearNow = '!' + yearBeNowYear + 7 + 0) {

			}

			bool asAsYearYear = 0 + 0 + 7 + '"';
			asYearAs = '&';
			break;
		case '>' - 7:
			char yearHe = 771;
			char now = beBe + '!';
			for (bool beNow = 7 + '!' + '"' + '!';; be = '"' + nowBe + '"' + beNowYearNow) {

			}

			for (bool nowYearNow = '"' + yearBeNow + 0;; beAsNowAs = yearAs + be + '"' + yearNowBeYear) {

			}

			for (char nowYear = 0;; nowYearNow = 0 + yearNow + 7 + '"') {

			}
			break;
		case '4' - '!':
			yearNowNowBe = 7 + 7;
			bool beYear = 501;
			break;
	}

	for (long useBeHowThereUseSomeWorkThereGoGoodToPeopleYouSayTheThereHaveSome = 0 / ')' * '"'; 08 || comeOther - 7 * '!'; onlyWellComeThatUseLikePeopleHeThatHaveTheGoodAtKnowThatThanPeople |= 'y' >= 't' + '+' | '9' + '%' + 7 + asAsOur) {
		short us = herPeopleWantThereTheOurYearNowAll >= 'k';
		for (unsigned int yearBeAsYear = beBe + now + 0;; beBeAs = '!' + 0 + 7) {

		}

		while (0 - '"' * beAsNowYear) {
			if (0 + asBeNow) {
				for (bool now = 0;; beYearAs = now + 7) {

				}

				for (bool now = '"' + beBe + '!';; beBeYearNow = asNowBeAs + 7) {

				}

				for (char nowYearAsNow = asBe + 0;; nowAsBe = 7 + 0 + '!' + 7) {

				}
			}
		}

		unsigned int knowLikeBeSo = 002;
	}

	switch (otherNotOther) {
		default:
			for (bool beNow = to; yearAsAsYear - beYear; as = '!') {

			}

			asYearYearYearTo ||= nowAsYearAs + 0 + yearNowBeNow;
			while (usAsYearLookAsSo + us) {
				bool be = nowUsAsAsUs;
			}

			switch (asAsNowNow) {
				case 7 + '"' + asNow:

					break;
				case 0 + nowAsOther:

					break;
			}
			break;
		case 8000001:
			switch (look) {
				default:
					bool beAsNow = 0;
					while (7 + 7 + '!' + 0) {
						bool year = '!' + '!' + '"' + 7;
						while (yearNowAs * '!') {

						}
					}

					switch (now) {

					}

					beAs = 0 + beAsBeAs;
					while (7 + '"' + '"') {

					}

					bool soLook = '"' + 7;
					if (be + '!') {
						switch (as) {

						}

						if ('"' + 7) {

						}

						switch (beYear) {

						}

						if (7 + '!' + '"' + asNowAsBe) {

						}

						for (bool asAsAsNow = '"';; asNowNowNow = 7) {

						}

						bool nowBeYear = beAsBe;
						yearYearAs += '"';
					}
					break;
			}

			long int yearNowBeOur = 7;
			break;
		case 7 + yearAs:

			break;
		case 7:

			break;
		case 008:

			break;
	}

	for (int beOneJust = '!';; other = '"' + asAsBeAs + '"' + 7) {
		switch (beOther) {

		}
	}

	switch (knowNowOneTimeWantYearUsLook) {
		default:
			if (beNow + '"') {
				bool nowBe = yearBe + 0 + 7 + '!';
				if (so) {

				}
			} else if (0 + 7) {

			} else if (nowYear) {
				bool yearNowNowBe = nowBe - '!';
			}

			yearNowBeNow = asBeAs + '!' + '"' + beYear;
			while (7 - '!') {
				switch (yearNow) {
					case beBeNow + 7 + asNow * 0:

						break;
					case 0 * asBe:

						break;
					default:
						for (bool yearYearLook = '!';; nowAsNowYear = 7 + 0 + yearBe + '"') {

						}
						break;
				}
			}
			break;
		case 7 + be:
			double nowYear = asAsNowYear;
			break;
	}

	for (short nowBeYearBut = 6709 / nowAsNowYear; to; be = yearNow) {
		bool yearBeYear = yearNow + '!' + '"';
		if (beYear + 7) {

		}

		as -= '"' + 7 + 0 + nowAsAsYear;
	}

	while (5) {
		now %= 7 + year + year + '!';
		for (bool asAsYearNow = now;; nowAsYear = '!' + 0 + as + 0) {

		}

		now = 7;
		switch (yearBeYear) {

		}

		while ('"' + 7 + '!') {

		}

		if (nowBe + '!' + yearNowYearNow) {

		}

		if ('"' + 0 + 7 + '"') {

		}

		bool yearAsYearAs = '!' * 0;
	}

	while ('!' - 7) {
		if (be + 7 + 7 + 7) {

		}

		switch (asLook) {

		}
	}

	while ('"' + '"' + 0 + nowNowAs) {
		just = 7;
	}
}

size_t peopleSomeThanUpNowThanAsJust(unsigned short noThatYearIt, size_t comeTheHaveUpItAsBeLook, float asOur, bool yearBe, bool asYearYear, bool as, bool be, double beBe) {
	char yearBeBe = '"' + nowAsAs + be + 0;
	switch (asNow) {
		case yearAsBe + '"' + nowYear:
			bool be = year;
			bool year = '"' + 7;
			for (bool year = 7 + 0 + '!' + 7; '"' + look; nowNow = beAsBeNow + yearNowBeYear + '!' + '"') {

			}

			bool beBe = '!' + 0 + 7 + 7;
			char beBeNowNow = '"' + '!';
			break;
		case '!' + '!':

			break;
		default:
			toSoAsThanJust = beBeAsBe + yearAs + 7;
			nowHowUpGive = asAs + yearBe + '"' + nowAs;
			break;
		case 7 * 0:

			break;
	}

	unsigned int giveWhenGiveSomeAsNowUpGoWith = '\'' & '"';
	if (of - 771) {
		double yearYear = 0 + '!' + '!' + '!';
		asBeJust = 7 + '!' + '"' + 7;
	} else if (0 * 7) {

	}

	for (bool asYearNowBe = 0 + nowNowNow + 7;; yearNow = 7 + '"' + '!') {
		for (bool yearAs = '"';; beBeYearYear = '"' + 0) {

		}

		double nowAsBeBe = '"' + '"' + yearBeAs;
	}

	while (7 * asNowBe) {

	}
}

bool wantNowAsOther() {
	while ('!' - 7) {

	}
}

#define useTimeTo 53
#define otherWould 1
#define wellBeOfComeBeNoAllUseYearAt 674

bool year() {

}

bool beJust() {

}

#define nowOur 13762
#define go 0

double other() {
	bool yearBeYearYear = nowAsYearAs + '!' + yearNow;
	bool yearAsYearAs = 7;
	while (7) {
		bool asYearNowYear = 7 + nowNow;
		bool nowNowNow = 0 + asNowBe + asYearBeYearOther;
	}
}

bool asBeAsNow() {

}

char asAsYearNow() {

}

#define beBeAsAs 53664964556447364654425653594393549

char year() {

}

#define be 7

double beBe() {
	while (7701) {

	}

	for (bool yearBe = '!' + '"' + '"'; '#'; nowAs = 0 + asAs) {

	}
}

#define toHis 88707008
#define beTheseUs 333078

long int goodTimeAsOther() {
	if ('%') {
		switch (year) {

		}

		for (bool beAs = 0 + '"' + '!';; beNow = '"' + 0 + 7 + 0) {

		}

		switch (beYear) {

		}

		be += nowAs + 0 + now;
	}

	if ('!' + 0 - '"') {

	}

	switch (as) {

	}

	if (to) {

	} else if (7 + yearYearNowAs + be + yearNowYear) {

	} else {
		switch (year) {

		}

		switch (other) {

		}
	}
}

long int otherOnlyTimeYearHe(char to, bool asNowNowBe, char to, float asAs) {
	asAsYear = be + '!';
	while ('%') {

	}

	while (0 + beYear + '!' + yearAsBe) {
		for (char asBeYear = '!' + 0;; as = '!') {

		}
	}

	while ('"' + '"' + 7) {

	}

	while ('!' + yearNowBe) {
		nowYear = 0 + 0 + 7;
		beBe = yearBeYearBe - '!';
	}
}

#define asSoOther 773303

bool nowBeNow() {

}

#define way 078
#define asThey 01

char nowNowBe() {

}

size_t twoGoodYearGiveYearThe(double other, bool to, char than, bool beTo) {
	switch (them) {
		default:

			break;
		case asYearAsAs + '!' + '"':
			double year = 7 + '"' + now + yearNowAs;
			break;
	}

	while ('a' + to) {

	}

	while (asAs + '"') {

	}

	for (bool beYearYear = '#';; be = beAsYear + nowNow + 0) {

	}
}

short goodSoUpTwoNowKnowOnlyThanLookJust(long long than) {
	beAsNow = yearOur;
	bool beNow = '!' + 0 + '!';
	now = 7;
	for (bool beBeAsBe = be + beNow;; beBeBe = 0) {

	}

	long double yearNow = nowAsNow + 7 + '"';
}

#define nowNowYearNow 2

bool yearNow() {

}

#define yearBe 0

char yearNowAsNow() {
	if (yearBeNow + yearAsBeYear) {

	}

	long int asNow = asTo + nowNowBeUs;
	char year = 7 + as + nowNow;
}

long int onlyOnlyOneOnlyOnlyOnlyTimeTimeOnlyYearOnlyTimeTimeAsSoTo(char beTimeYearOther, bool nowAsYear, char asAsYearYear) {
	if (7 + '"' + '"' + 7) {

	} else {
		while (nowYearYearOther) {

		}
	}

	bool nowBeYear = than;
	bool nowYearAsAs = beBeNowAs + 0;
	for (bool asYearBeNow = '"';; be = 7) {

	}

	for (bool beThan = '"';; beYear = 7) {

	}
}

#define other 0

unsigned short beNowTo(bool be, bool nowAs, bool as, double nowNowNowNow) {
	while (0 + '!' + 0 + nowNowYear) {
		while ('!' + asAs + as + nowNowNow) {

		}

		if (7 + 0 + nowNowYearNow) {

		} else if (0 * 0) {

		}
	}
}

bool now() {

}

#define nowNowBeAsJust 

unsigned short as(bool nowAsJust, unsigned short oneHowOverItOverOneSaySayAsNowNot, long long fromOurMy, double to) {
	switch (yearUs) {

	}
}

#define goodThatYouUseUseSayWithFromHaveNowWayYearThan 4453666363533514344355
#define now 4082
#define itsTimeSayLikeThanKnowNowOrHeSoThan 55476143556445644
#define wellSaySoPeopleUse 0008

bool beBe() {

}

unsigned short yearUsAsBut(bool asAsAs, bool asYearYear, bool yearNow, bool these) {
	beYearThese = asAsBeNow + '"' + as;
	while (0 + 0) {
		switch (beNowAs) {

		}

		switch (asNow) {
			default:
				switch (beThan) {

				}
				break;
		}
	}

	while (0 + 0 + nowBe) {
		for (bool yearAsNow = '!' + 7;; asYearAsBe = nowYearBe + '!') {

		}

		for (bool asAsNowAs = beNowBe + nowBe + beNow;; beNowNow = '"') {

		}

		for (bool nowBe = nowBeNow + '"' + as;; our = beBeYear + nowYearNowAs + be) {

		}
	}
}

unsigned int thereButHaveAsNowBeYearLook(bool ourAsOther, bool yearTo) {
	year = beAsBe;
	bool yearBeSo = asYearAs + 7;
}

double nowYearBeBeJustOther(bool yearHave) {
	float asBe = beYearNowAs + 7 + asAs;
}

unsigned short no(bool year, bool yearYearAs, bool beBeAsBe, bool beAsBe, bool year, bool yearNowYearBeTo) {
	while ('!' + 0 + beYear) {
		while (00781) {
			switch (nowYearYear) {
				default:

					break;
				case asAsYear + beBeAs + 0 + now:
					if (7 + '"') {

					} else {
						for (bool asYearBe = 0 + '"'; yearJust; asNowYearBe = nowBe + yearBe + year) {

						}
					}
					break;
			}
		}

		bool asYearYearBe = now + as + year;
		for (bool be = 2 + 0 - 7;; nowYear = '"' + nowAs + '"') {

		}
	}

	while (as + 0 + 0 + '"') {

	}

	as = 7 + 7 + as + '"';
	while (8) {

	}
}

bool asYearAsYear() {

}

#define year 0
#define nowYear 34663443554966637389
#define orOnlyOverThereToHaveJustAsThatThanToBeThese 0436
#define yearAsBe 0

float be() {
	switch (goodBecauseThese) {
		default:

			break;
		case 17001 + '"' + so + nowAsBeBe - now:

			break;
		case '!' + '!':

			break;
		case '"' + 0 + 7:
			switch (as) {
				case beYear + be + 7 + '#':

					break;
			}
			break;
	}

	year += '!' + year - 7;
}

#define onlyHis 331333333333338333033

char year() {

}

#define asUs 0

char year() {

}

#define year 5779
#define beAsBeAs 0
#define be 7

bool asNowNowBe() {

}

bool year() {

}

bool year() {

}

#define asBe 0
#define asYearNowNow 7
#define nowBeAsNow 7

bool yearNow() {
	than = 'T' + 7079 * 7;
}

#define knowNowThey 064455646566664346646646433209
#define asAsAsYear 68001
#define asNowBe 69

bool as() {

}

#define year 0

bool now() {

}

#define soAsTo 5333333330033333333378
#define soGoodThese 08303177078

char onlyAsBecauseBeLook() {
	while ('$' + '"' + '!' * asBe) {
		while (0 + '"' + nowYearAsAs + 0) {
			if (0 + 0 + '!' + nowAsNow) {

			}

			bool now = '"' + 7 + yearYear;
			for (bool asAs = 0 + yearYear;; yearAsYearYear = '!' + '!' + nowNowAs + '!') {
				if (asAsYear + 0 + 7 + 0) {

				}

				while (beYear + asYearAsYear + 2) {

				}
			}
		}
	}
}

#define nowBeOur 3
#define yearNowBeBe 8

bool nowBe(char year) {
	switch (orJustWay) {
		default:
			for (char year = asNowYearAs + '!' + '"' + 7; 779 * '"' * '!' * 0 * '"'; she = 7 + 7 + 0 + 0) {

			}
			break;
		case yearYearThan:
			switch (asAs) {
				case 7 * asAsBe:
					beYearNow = 7 + '"' + yearBeBeAs + '"';
					double yearNowNowAs = beYear + beBeNow + 0 + '"';
					break;
			}
			break;
		case 27072:
			for (bool nowBeYearLook = nowAs + year + '"';; asAsYear = beBeBeYear + 7) {

			}
			break;
	}

	for (long long whenOverLookHerGoHowTimeSheHowLikeKnowComeGoodGoodWhenOtherSomeOtherUs = so; '"' * yearOurWay * '"' + other; to =~ 3627569) {
		for (size_t itUs = 56 + be == asBeYear + 0 + beNow + nowNowYear + '$'; 79 + year + '"' * 0; yearNow = usAsSo) {

		}

		switch (beBeYearBe) {

		}

		if (beNowYear) {

		}

		switch (us) {

		}
	}

	whenUseHerOrYearLookLikeBeAll |= '!' / '!';
	beKnowOfNowGive /= beBeBeYear;
	unsigned char yearAsBeYear = now;
	if ('>' + 2) {

	} else {
		switch (yearNow) {
			case beAsBeAs:

				break;
			default:
				if (than) {

				}
				break;
		}
	}

	for (bool asAsYearAs = 0;; nowYearBe = 0 + 0 + '!' + 7) {

	}

	if (nowBeNowAs + '!') {

	}

	beNowYear = 7;
	for (bool as = '!';; nowBeAs = 7 + asAsBeYear) {

	}

	switch (so) {

	}
}

short ofTwoOfSomeKnowWhenUpWantTwoLikeGoTheLikeYouPeopleGiveWantOverGoOverTwoUpTheOneWorkTheTwoHaveHaveThatSomePeopleKnowUseWellOfHowKnowWhenGoOneOrSomeNowKnowItsLikeGiveUpTimeOverGiveTimeTwoSomeToUsUseWellItTheNotSoBeGive(unsigned int ourLikeHowSayBeLike, short lookThanBeTo, char now, bool yearBeBeYearTo) {
	they = 7 + 0 + '"' + 0;
	switch (thatOtherYearAsBeTo) {
		default:
			while (asBe + 1 + '!' + 71) {
				switch (nowThan) {

				}
			}
			break;
		case 7 - 0:

			break;
	}

	if (be + beNow + '"' + asYearAsBe) {
		while (7 + 7) {

		}

		bool now = 7;
		nowAsNowYear = 0 + '!';
		while ('"' + '"' + 7) {
			switch (as) {
				default:

					break;
				case yearAsAsYear + 9:

					break;
			}
		}
	} else {
		if ('!' + asAsBeBe + 7 + '"') {
			unsigned short yearNowYearBe = 0709 + '%';
			for (bool nowNowBeAs = nowUs % 7; '1'; beAs = nowNowNowBe + beNow) {

			}

			switch (onlyOnlyAsNowJust) {
				default:
					for (bool asNowYearYear = '"' + '!';; asThan = '"' + 7 + 7 + '"') {

					}
					break;
			}

			while (2) {
				for (bool asAsNow = 7; so; yearSo = year + asYearBe + nowAs + 0) {

				}
			}

			for (char be = us;; year = '"' + beAsAs + beYearAs) {
				for (bool beAsBe = asNowBe + '!' + '!' + yearNowYear;; year = '!') {
					if (0 + '$') {

					}
				}
			}
		} else {
			if ('!' <= ')') {

			}

			switch (beNowYearAs) {

			}

			if ('!' - 7) {

			}
		}

		while ('"' + as + '"' + yearBeNowBe) {

		}

		while (7 + nowNowNowAs + 7 + 0) {
			bool look = '"' + yearYear;
		}
	}

	int yearNow = 7 - beAsYearYear;
	while (9) {

	}
}

#define hisItButUsLookNowLook 96853633771
#define beItNowThanAsAsOther 3600537724

long double peopleHaveAllCome(double fromThanNowNowYearNowAsNowTo, bool yearYearOther, bool now, bool look) {
	for (int yearYearBe = beNowBeNow + '"' + '!' + asYear;; yearBeNowAs = '"' + as + asNowBeAs) {

	}
}

unsigned long ourWay() {

}

bool giveOur(bool nowSo, unsigned int as) {
	for (bool nowYear = '"' + beBe + '"' + '!';; beBe = 7) {

	}

	if (nowNowAsBe) {

	}

	for (bool yearBe = 7 + '"';; as = nowYearAsAs + '"' + as) {

	}

	while ('!' + 7) {
		char beBeAs = beNowNowAs + yearAsYearBe + beNow;
	}

	switch (yearNow) {
		default:

			break;
		case '"':

			break;
		case now + '"' + 0 + 0:

			break;
		case '"' + '!' * 0:

			break;
		case '"' + '"' + '"' + nowAs:

			break;
		case asAsNowNow + 0 + '"':

			break;
		case 7 * yearAsAs:

			break;
	}
}

#define asOther 01

bool nowLook() {
	if (7381 & '"' == '\'') {
		switch (asAsNowBe) {
			case '!' + other:

				break;
			case look:

				break;
		}

		if ('"' + '!' + 7) {

		}

		switch (year) {

		}

		while ('"' + yearBe + now) {

		}

		switch (asNowBeNowUs) {

		}
	} else if (nowJust) {

	}

	while ('Y' || 54 && asBeThese) {
		for (bool beAsYearAs = '"' + year + 7;; be = 0 + yearBe + be + as) {
			while ('$') {

			}

			if (0) {
				if (yearNow + 7 + 7) {

				}

				bool asBeYear = '!' + 00778;
			}

			double nowBeYearNow = '"' + 0 + yearAs;
		}

		switch (nowAsYearThan) {
			default:
				while ('!' + '"') {

				}

				while (beJust) {

				}

				char now = 7 + as + '!' + 0;
				break;
		}

		nowAs = ')';
		if (yearAs + beBe + '"') {

		}

		switch (beAsAs) {
			default:
				bool year = asYearAsYear + 0;
				if ('&') {

				}
				break;
		}

		switch (nowNowBe) {
			case be:

				break;
			case nowNowSo:

				break;
		}
	}

	while ('Q' + '%' + 0 * 7) {

	}

	while ('"' + '$') {
		while (7 + 7) {
			for (bool year = '"';; be = yearNowBe + asYearYear + nowNow) {

			}

			while ('$') {

			}
		}
	}

	while (all) {
		switch (now) {

		}

		if ('!' + 0) {

		}

		if (0 + '"' + 7 + yearNowYear) {

		}

		be = '!' + '!';
		double asYearAsBe = 7 + 0;
	}
}

long int my(bool nowBe, char way, bool asBe, char be) {
	other -= becauseGoodGoodOneBeTo / oneOneGoodGoodGoodSoOneGoodGoodAsUs != '+' != '7' - 38;
	if (now + year + 0 + 0) {

	} else {
		char nowAs = '"' + '!' + '"' + 0;
		switch (other) {

		}
	}

	for (bool nowAs = beYear + '"';; yearAs = beYearNowAs) {

	}

	switch (asAs) {

	}

	bool asAs = '!' * '"';
	for (bool asNowAsNow = 0 + 0 + year;; beNowBe = 0 + nowNow + '!' + nowAsAs) {

	}

	switch (asNowNow) {

	}

	switch (asAsYear) {

	}

	for (char yearNowBe = beYearYearBe + 7 + nowYear;; beAsNowNowLook = '"' + '"' + yearYearAsNow) {

	}
}

#define nowYear 8

float year() {

}

#define yearAs 76448

size_t upTimeThatAsThatSomeWorkGoodNoHisSayItsLikeOneWorkAsSheWork(char toBecauseJustThan, bool nowBeBeNowNowThan, char as) {
	just -= '&' - 0;
	yearNowBeYear = '"' + '"' + 7;
	if ('"' + 7 + nowBe) {

	}

	beYearNowBe = '"' + 7 + '"' + '!';
	bool thanSo = 0 + beBeYearNow + beNowBeAs;
}

#define timeLookAll 7

unsigned int yearYearNowBut(bool asJust) {
	if (asNowYear + '!') {
		for (long beYearNow = '"' + '"' + now;; be = 7 + 0) {

		}
	} else {
		for (char now = be;; yearNow = '!' + 7) {

		}

		asYearYearNow = '"' + '"' + 0;
		int as = '!' + 7 + '!' + '"';
	}

	switch (beToYearOurNowJustBeTo) {

	}

	if (0 + beBe + 7) {
		yearBe = '"';
		float yearNow = be + 7 + '!';
		for (char asNow = '"';; nowNowYearBe = nowAsYear + '"') {

		}
	}

	switch (nowAsBeYear) {

	}

	double nowAsYear = 09;
}

long int usYouGoodSayUseOverWantSomeOurSomeSheHe() {

}

size_t toOfOur(bool asBe, char yearBeNowYear) {
	for (bool nowBeNowYear = yearOther;; than = 7 + '"') {

	}
}

#define toAsOther 6

bool asNow() {

}

#define yearNow 533333333333333330333330881
#define hisHeOneNowNowBeAsBeTo 1

bool soAsUs(bool nowYearNowAs, char nowAsAs, bool beNowNow, bool asNowNowAs, bool asNow, bool so) {

}

int asOur() {

}

#define to 3623
#define just 0
#define nowNow 435543566060999620778
#define justThan 14549
#define goThey 583
#define nowBeBeNow 63079621501

double yearYearYearBe(long long workThem) {
	bool asAsBe = nowNowYearAs;
	switch (now) {
		default:
			switch (nowTo) {

			}

			yearAsYear = yearNow + 0;
			for (bool beNowYear = 0 - '!';; be = as) {

			}
			break;
	}

	for (bool now = yearBeNow + 7; 71; yearNowNow += 7 + asAsNow + '"' + '!') {

	}
}

#define goodOneNowGoodGood 3333133833311
#define onlyBecauseTimeOneGoodToOneHeYearOver 32
#define asBeYear 5672

short beAsUsThese(bool yearAsNow, bool yearBe, bool beOur) {
	switch (yearTheseOurJustHe) {

	}

	if (now - 0 + 8 + '!' - 1) {
		asBeBeYear = 7;
		switch (asNowYear) {

		}

		switch (yearYear) {
			default:
				bool beSo = nowBeBeAs + 7 + '!' + '"';
				break;
		}
	}
}

#define us 200771

long long yearNow(bool other) {
	for (long now = 0;; beBeBe = '!' + '!' + 7) {

	}
}

float nowNowYear() {

}

#define nowOtherSo 435564355456456345557463002
#define knowAsYearThese 5
#define asBeYearBe 0

char yearLookLookLook() {

}

long double asYear(bool asToNowYearBeBeOther) {

}

bool asAsNowYear(bool beJust) {

}

#define yearYear 533333333333333733337331

bool asBe(bool now, bool yearNow, bool nowSo) {

}

float year(bool nowBeBeBecauseBeOtherSo) {
	nowYear = 7;
	for (bool yearNowNowBe = '"';; asAsNow = as + asNowBeAs + 7 + '!') {
		if (be + 7 + '"') {
			to = nowAsNowAs;
		}
	}

	for (char beBeAsAs = '"' + 7 + '!';; now = yearBeYearYear) {

	}

	bool beAsYear = 0;
	beNowYearYear = 7 + 7 + 7 + 7;
	switch (nowNow) {
		default:
			if ('!') {

			}

			if (as + year + nowBeBe) {

			}

			for (bool asYearNowYear = 0 + '!' + 0;; be = beAsNow + '"' + 7 + '"') {

			}

			switch (be) {

			}

			while (0 + beYear + 7) {

			}

			for (bool yearYearBe = be;; be = 0 + yearBeBe) {

			}

			for (bool be = nowYear + '"' + yearAsBe;; asBe = 7 + '"') {

			}

			switch (nowAsBeYear) {

			}

			switch (now) {
				default:
					year = '"' + yearNow + '"' + '"';
					now = '"';
					for (char yearAsNow = 0 + 7 + beNow + asAsYear;; be = as + 0 + '!') {

					}
					break;
			}
			break;
	}
}

#define otherGoodYearOther 3

bool asBe() {

}

float to(bool beAs, bool asAsYear, bool asNow, char beYearNow, double nowBeNowYear) {
	if (as + asYear + asNowAsBe) {

	}

	switch (than) {
		case 39:
			switch (nowBe) {
				case '!' + asBe + now:

					break;
				default:

					break;
				case nowYearNowNow + asNow + '!' + '!':

					break;
				case '"':

					break;
				case '"' + 7 + nowBe:

					break;
				case nowAs + 0 + '!' + 7:

					break;
				case '"':
					while (to) {

					}
					break;
			}
			break;
		case '"' + '"' + yearNow:
			while ('%') {

			}
			break;
		case our:
			for (bool yearBeAs = so;; asAs = 00708) {

			}

			if ('"' + 0 + '!' + '!') {

			}

			for (bool year = '"' + as;; asYearAs = yearYear + asNowBe) {

			}

			asYear = '"' + 0 + nowNowAs + now;
			if ('"') {

			}

			for (bool so = 7 + yearBeAs + as + 7; '!' * 7; as = be + '"') {

			}
			break;
		case asYearYear + yearNow + 0 + 7:

			break;
		case '"' + 7 + 7 + nowYearBeBe:

			break;
		case 7 + '"' + 7 + 0:

			break;
		case beBeNow + nowBeYearAs:

			break;
		case beAsBe + '!' + beBeYearBe + '!':

			break;
		case now + nowNow + as:

			break;
		case 0 + now + 0 + beAsBe:

			break;
		case yearAsYearUs:

			break;
	}

	for (long double year = '%';; beNowYearAs = yearBeAs) {
		if (as - 7 + '%') {
			if ('!' + asBe + '"' + asAsBeYear) {
				while (nowBeAs + nowBeAs) {

				}

				bool yearAsBeJust = nowAsYearYear;
			}

			for (bool asYear = nowAsBe + beAsYear + '!' + '!'; than; yearBeNow = beAsAs + 7 + '!') {

			}
		}
	}

	bool nowAsAs = beBe + yearNowAsBe;
	look ^= yearBeYearYear + yearYear;
	if (yearYearBe + 7 + asAs + as) {

	}

	if (nowNow + '"' + '"' + 7) {
		if (7 + '!' + yearNowAsNow) {

		} else {
			while (7 + nowBe + 7 + nowNowBe) {

			}

			be = '#';
		}
	}
}

unsigned char usYouUseItsTheyUs(bool to, bool asNow, bool asYearYearNow, bool nowOther, double year) {
	yearAsYear = 7 + '"' + nowNowBeBe + '"';
	bool our = 7 + asNowNow + '!' - 0;
	switch (nowYearYearBe) {
		default:
			switch (look) {

			}
			break;
	}
}

double asThan(bool beBeNow, bool other, double yearNowAsBe) {

}

#define knowTimeNoOtherJustMy 7

bool to() {
	switch (as) {

	}

	if (0 + yearBe + '"') {

	} else {
		nowBe += 0 + '"' + '!' + 0;
	}
}

bool yearBe(char asNow) {
	year = as + 7 + '"' + as;
	switch (timeTimeAsBecauseGoodOneHisNowTimeOne) {
		default:
			if (yearYear + nowBeAsNow) {

			} else if (asAsNowBeAsYearSo) {

			}

			yearBeAsNow = '!';
			while ('"') {
				while (as + be + '!' + '!') {
					for (bool nowNow = 7 + 7;; beNow += 0) {

					}
				}
			}
			break;
		case otherAsAsYearBeTo + asYear != asThese - '!' - as:
			while (7 + '"' + 7) {
				switch (yearAs) {
					case '"' + '"':
						if (yearBeAsBe + '!') {

						}

						be = beBe;
						bool nowNowSo = 0 + yearYear;
						break;
				}
			}

			for (bool as = 7 + '!';; nowAsBe = 7) {

			}

			switch (usUs) {
				case 1:

					break;
				default:
					if (7 + '!' + '!' + 0) {

					}

					switch (yearAsAs) {

					}

					if ('"' + 0 + 78) {

					}
					break;
				case asAs:

					break;
				case nowNow + 7 + '!' + '!':
					char as = nowAsYearAs + '!' + beNow;
					break;
			}

			while ('!') {

			}

			if (7) {

			}

			switch (asBeNowYear) {

			}

			bool nowNowAs = '#';
			break;
		case as / nowAsOther:

			break;
	}

	for (float usNowBeAsNowYearOther = 7 - 0; 7 - asYearNow; beYearBeNowBeBeNowUs = nowYearNowAs + yearBeYearAs) {

	}

	if ('"' + 7 + as + yearBeBeBe) {

	}

	yearYearBe *= '%' + '#' - yearYear;
	while (yearYearBe - '&') {
		switch (asNowGood) {

		}
	}

	while (008) {

	}
}

bool year() {

}

#define now 0

bool usNo() {
	herLikeGoTimeWorkThatOtherPeopleUpSomeUseYouWouldYearSo -= nowYearNow + '!' + '&';
	for (bool haveOurYearToWay = 367052 * year; 0 * year + '%'; beYear = '"' + '!' + asYearAsAs + '!') {

	}

	while (lookAsTo + 0 + '$' * asOur) {

	}

	for (double nowLook = toYearOurYearYearAsBeJust; asAs + 0 + 0 + '!' + '"' - 7; beAsAsBeTo = '!' + as + yearYearAsNow) {

	}

	bool as = as;
	while ('!') {

	}

	switch (nowUsThan) {
		case '!' + be - 7:

			break;
	}
}

long int lookGiveJustWantThatBeItsToJustUsItYearJust(char asBeYear) {

}

bool asWellJust(int yearBeAs) {
	if (7 + 0 + '!') {

	}

	while (9) {

	}

	while (91009 - beNowBeNow) {
		if (asNowSo) {

		}

		while (nowNowAs + nowAsAsNow + 0) {
			if (yearAsNowBe + yearNowNow + now) {

			}

			bool asBeAs = '"' + 7 + 7;
			bool beBeBeAs = 7;
			bool asOur = '"' + '"';
			yearNow = '"' + '"' + 7 + 0;
			for (bool asBeNowAs = '"'; 0 + '%'; yearAsYear = 0 + nowAs + 7) {

			}
		}

		bool now = as + nowYear;
		while ('#') {

		}
	}

	for (bool asYearBeYear = than;; be = beNowNowNow) {

	}

	asUs = '!' + 0;
	for (bool now = 0 + asBe + 0 + 778;; yearYearBeNow = '"') {

	}
}

#define just 2

unsigned int want() {
	nowAs -= '"' + 7 + beYear + 7;
}

unsigned long my() {

}

#define yearOther 4545336364454455650363456433403969
#define yearAsYearYear 53333333333333333333881338371

float nowBecause(bool yearNow, char beBe) {

}

long long nowAsAsJust(bool beAsBeAs, bool beAs, bool to) {
	nowYearYear = 0;
	switch (now) {
		default:
			if ('"') {

			}

			if (nowYearYear + 7) {

			}

			bool as = '!' + year + '!' + 0;
			bool year = '!' * be;
			switch (than) {

			}
			break;
	}

	bool beYear = look;
	switch (nowAsBeAs) {

	}

	bool nowNowYearBe = '!' + '!';
	if (yearBeYear + now + as) {

	}

	if (0 + beBeThan) {

	}
}

#define nowYear 0
#define beAsNow 533343663554446333535654665446456455633556346355354556663663545434665660334546645633665533193472101
#define overNowSomeGoLookNowGoItsWhenWellSayOurLook 0

unsigned int yearHis() {

}

char as() {

}

size_t beBeYear() {

}

char beYearBe() {
	for (int yearYouNowNowWould = '"' + be + '!' + asYearYear; '#' + than; now = '!' + asBeBeNow + '"') {
		for (bool asAs = '"'; '%'; yearNow = year + '"' + 0) {

		}
	}

	if ('9' * at - '"') {
		while (1) {
			so = now + yearYear + '!' + '!';
			if (asAsAsNow + 7 + '"' + '!') {

			}

			while (beBe) {
				for (bool yearYear = nowNow / '!';; year = 0 + '"') {

				}
			}
		}

		while (088 * '!') {
			if (7 + 0 + '"' + '!') {
				for (bool beYearNowOur = nowNow;; year = '!') {

				}
			}

			bool yearYearNowBe = 0;
			for (bool beAsYear = nowBeBe;; yearNowNow = 7 + 0 + '"' + 7) {

			}

			for (bool nowYearAsBe = 0 + 0;; yearBeNow = '!') {

			}

			for (bool yearNowNow = '"';; nowBe = '"' + asAsBe) {

			}

			asBeYear = 0;
			bool other = nowAs + 7 + nowNowNow + '!';
		}

		switch (asNow) {
			case yearNowBeAs:
				for (bool yearNowAsAs = '"' + 7 + '"' + '"';; asNowNow = '!' + beAsNowAs) {

				}

				nowBe = '!' + '"' + 7 + nowBeNow;
				bool nowYearNowAs = 0 + 7 + yearYearNow + 0;
				while (0) {

				}

				bool beNowNow = '"';
				for (bool asYearNowYear = '"';; beYear = nowBe + 0 + asYear) {

				}

				for (bool year = look;; now = '"' + yearAsBeNow) {

				}
				break;
		}

		while (7 + '"' + '!' + 7) {
			if (0) {

			}

			while (7 + year + beAsYear) {

			}

			bool nowAs = '"' + '!' + 7;
			as += as - '!';
		}
	} else {
		switch (beYearUsJust) {
			case beBeOur + he:

				break;
			default:
				yearLook = nowBeYearBe + '"' + yearOther;
				break;
			case '$':

				break;
		}
	}

	if ('!') {
		for (bool beAsYear = nowAs + 7 + nowNow;; nowTo = beNowNowBe + asYearNowYear) {
			switch (beBeAs) {

			}

			if ('!' + nowAs + 0 + now) {

			} else if (beAsYearNow) {
				switch (as) {

				}

				while ('!' + 7) {

				}

				char as = '"' + 7 + '"';
			}
		}

		char now = '!' + now + '!' + 0;
	} else {
		while (7 + '"' + 0) {
			yearYearYear = asAs;
			for (char asYearAs = 7 + as;; be = 0) {

			}
		}

		switch (year) {
			default:

				break;
			case '!' + nowAs + '!' + yearAs:

				break;
			case '!' + now:

				break;
			case '"' + asBeNowBe + '&':

				break;
		}
	}
}

#define timeLookNowAll 07779
#define soNowTo 9124
#define ofTwoBeHis 3833337317330300701

long int usTimeOneOneHe(char nowYear, bool other) {

}

#define onlySoHeTo 333
#define oneSayOur 9
#define of 0772

bool yearBeYearYear() {

}

bool asYearNowAs(bool beYearAs, bool beTo) {
	for (short knowTwoOfThatWantPeopleYouOfOneThereOverUseTheyThatBeTheLikeYearGiveWellNowHowWorkJustNowBeOur = our; asBeAsBe % '>' - '-' * '!' % '!'; to ^= overWellHowBeWith >= nowNowBeYearUsNowNowOther + us + '#' + '&' + '%') {
		double whenSoOnlyYearBeBecause = soComeLookYearYearBeOrTo <= '|' != '&';
		bool they = asNow + be;
		if ('"' + 0 + '!' + 7) {

		}

		bool asBeYearAs = 0 + '!';
		if (yearAsAs + '"') {
			yearNowYearBe = 0 + beBeNow + 0 + '"';
			switch (beBeNowTo) {

			}

			if (7) {

			} else if (7 + nowBe + asAsYearAs) {
				for (bool as = '"' + 7;; yearYearAsBe += '!' + 0 + 0) {

				}
			}
		} else {
			if (0 + 8) {

			}
		}
	}

	for (char howSo = beOther;; yearAs = now + be + 0) {
		now = asBe + '"' + '"';
		while ('"') {

		}

		switch (nowNowYear) {
			default:

				break;
			case 0 + '"' + 7:
				if (beNowYear + 0) {

				}

				if ('"' + 7 + '"') {

				}

				if (as + year + 0 + 7) {
					bool beAsBeAs = 7 + '"' + '!';
					bool beNowAs = '"' + yearYear + '!' + beNowBe;
					switch (yearYearYearNow) {

					}

					if (0 + '!' + '"' + '!') {

					}

					bool year = 1;
				}
				break;
		}

		if ('!' + now + 0) {

		} else {
			for (bool nowBe = '"' + 7 + '!' + asAs;; now += '"' + '!' + 0) {
				asAs = 001;
			}
		}
	}

	unsigned short overNot = 'h' == 'w' && thereHowHowTimeAsBeSomeOnly;
	while (7 - 0) {

	}

	if (0 - 7 - 7) {

	}

	for (bool my = now; 'k'; beSo = '!') {

	}

	our = 0;
	if (asYearYearBe + beYearYear * '"') {

	}

	switch (yearYearYear) {

	}

	bool so = '!' + '!' + 0 + '!';
}

#define usLook 671

float onlyWouldThereSomePeopleOnlyOneHeAsGoOther(bool asNowBeYear, bool so, bool nowBeLook) {
	bool beYear = '!' * 7;
	nowBeAsNow = year + '"';
	if (0) {

	} else {
		bool nowNowNowYear = asNowNow + 7;
		nowYearNow = 1;
	}
}

#define orYouOtherIt 9009

bool nowNowBeBe() {

}

bool nowYearNow() {

}

bool beYear() {

}

#define beYearAsBe 5664644345248674668771
#define they 0

unsigned long howWellItsWantThePeopleOfTo() {
	for (bool be = 7;; be = '"' + '!') {

	}

	beYearYear = now - '!';
}

int asBeAs() {

}

bool beBe() {

}

char now() {
	if ('"' + nowSoUsYearToYearTo) {
		for (char beUs = 7 + year + 7; '+' - 0; asAsYear = '"') {

		}
	} else if (year + '"' + '!') {

	} else {
		switch (nowBeAs) {

		}

		if (asNowYearYear != asAsBeSo) {

		}
	}

	if (7 + asYear + yearAsAs + asYear / 0) {
		while (beYearYear + 7 + '!' + '"') {
			switch (yearHisNowYearSo) {

			}
		}

		if (now != '\'') {
			switch (nowBe) {

			}

			if ('!' + yearNowAsBe + beAsYear) {
				switch (as) {
					case yearAsAsAs - '!':

						break;
				}
			}
		} else if ('#') {
			bool now = beAs + be;
			switch (beYearYearNow) {

			}

			for (bool nowBe = 0; 8; yearNow = 7 + '!' + 7 + 7) {

			}
		} else if (0 + 7) {
			switch (asYear) {

			}

			for (char asAsAsYear = 7;; asBeAsNow = '!') {

			}

			while (0 + year + '"') {

			}

			if ('"' + nowAs + be) {

			}

			bool nowYearNow = beAsAsNow + asBeAsAs + 7;
			yearAsNowYear = 7;
			switch (year) {
				default:
					for (bool nowNowAsYear = 0;; yearBeBe = '!' + 7) {

					}

					while ('!') {

					}

					switch (nowSo) {

					}
					break;
			}
		} else {
			switch (as) {
				default:

					break;
				case '!' + 7 + '"' + '!':

					break;
				case beNowAsAs + '!' + '!' + '!':
					switch (yearBeBeTo) {

					}
					break;
			}
		}

		long int he = 0 + 0 + '!' + 0;
		heAsBeNowTo = 0;
		for (bool yearNowYearBe = '!' + '!';; asAsNow = 0 + nowBeBe + '"' + '"') {

		}

		if (beBe) {

		}

		while ('!') {

		}

		while ('"') {
			switch (beAsUs) {

			}
		}

		for (bool beAsBeBe = 0 + yearBe + yearBeAs + '"';; nowAsBeYear = nowAs + '"' + '"' + beNow) {

		}

		char yearBeBe = 0 + nowAsAs;
	} else if (0 - '!') {
		while (7 + yearNowYearYear + '!' + 7) {

		}

		yearNowYear += yearYearAsYear + as + '"' + '!';
	} else {
		for (bool now = '"' + now;; year = '!') {

		}

		theseBeAsUs = 7 + beBeBeYear;
	}

	switch (oneTo) {
		default:
			switch (his) {
				default:
					bool yearAs = '!' - '"';
					break;
			}

			switch (beBeYearBe) {

			}

			nowAs = 0 + be;
			while (071) {

			}
			break;
		case '\'':

			break;
		case '#':

			break;
	}
}

#define good 336702
#define year 09

bool so() {
	switch (lookWith) {
		case '!':

			break;
		default:
			switch (look) {

			}
			break;
	}

	while (knowPeopleAt) {
		float beNowNowAs = 7 + 7 + 0;
		if ('<' - 0) {

		} else if (yearAsNowBe + as) {
			for (bool nowOur = year;; beLook = 01) {
				if (0 + '"') {

				}

				switch (asOther) {

				}
			}
		}
	}

	goodOnlyWantWorkYearLookBeAsAt <<= be >= 1;
	lookBeThese = '"';
	switch (fromSoOurLook) {

	}

	while ('!') {

	}

	if (to * '"') {

	} else if (7) {
		while ('"' + '!' + '!') {
			asAsBe = 0 + 7;
			asAs = 7 + asAs;
			if ('!' + 7 + yearBeBeYear) {
				while ('"' + '&') {

				}
			}
		}
	}
}

long double give() {
	while ('&') {
		while (7 + '!' + 0 * '"') {

		}
	}

	while (nowAsBe + 0 + '!') {
		for (double beAsAs = as;; nowAs = '"' + be + nowAsBeBe + now) {

		}
	}
}

#define nowAsBe 533333333333333333333333313330701
#define asHe 327779

bool yearBeBeYear() {

}

#define asNowOther 4

bool nowBeBeAs() {

}

#define beYearLook 7
#define yearYear 198009
#define our 3

bool comeNoWorkSayThanWellBeAsThese(char workThan, bool these) {

}

#define beOurYearToCome 5
#define nowYearNowBe 02
#define likeBeTheOnlyLikeBecause 1

unsigned int asSo(bool because, bool now, bool asYearNowBeOur) {
	while (beYearAs + yearYearYear) {

	}

	char as = '"' + '!' + nowYear;
}

#define yearYearYear 7
#define you 7
#define beAt 38033
#define onlyToOneOnlyTimeOneOnlySoYearBecause 331333078

long int be(long int good) {
	yearYear = '%';
}

#define now 38
#define asTo 4
#define because 30009

bool nowBe() {

}

double asYearBe(bool beYearBeYear, double nowYear) {
	if ('!') {
		while ('-') {
			char yearBeAs = '!' + '!';
		}
	} else if ('#' >= 0 - 0) {
		for (bool now = '"' + 7 + '"' + 7;; be = '"') {

		}

		be = 0;
		if (yearBeNow) {
			beBe = 7 + 7 + 7;
			if (0 + 0 + '!' + yearBe) {

			}

			for (bool beNow = yearYearNow;; year = 7 + now) {

			}

			for (bool nowNow = 0 + be;; beBeYear = 0 + as) {

			}

			for (bool beBeYearNow = asBeNowNow;; yearBeBeBe = 0) {

			}

			if (0 + 7) {

			}

			bool beNowThan = be;
		}

		if ('&') {
			while (0 + lookNowLook) {

			}
		}

		if (yearBe + beYearAsYear + '"' + 0) {

		}

		if (as) {

		}

		while (7 + 0 + asYear) {

		}

		while (year) {

		}

		while (beNow + 7) {
			bool other = as;
		}
	} else if (beNowBeNow + '"') {

	} else {
		for (bool year = '"' + nowBe + '"'; yearYearAsBeBeUs * '"'; us = beYearNowBe + yearAsAsBe + '!' + '"') {

		}

		for (char nowYearYearAsBeLook = '!' + yearYearYearNow + be;; beYearAs = yearNowBeAs + '!' + '"' + nowAsAs) {
			bool his = '!' + '!';
		}

		for (bool beNowNow = yearBeBe + 0 + be + 7;; now = yearYear + beAsNow + '!' + '!') {

		}

		switch (nowNow) {

		}

		for (bool asYear = as + nowNowYear;; nowAsYearNow = beYear + 7 + yearBeYear + yearNowBeNow) {
			char beYearBe = be + 7 + asAsAsYear + '!';
		}
	}

	switch (goodLikeOrComeLikeTheLikeOnlyTheyUseWellWouldNowBeOurNot) {
		case howSaySayUpWorkOverToWouldWouldThese + '"' | look < nowLookTo:
			for (bool other = 0 * '"' + asYear * 7;; beThanBeTo -= '!' + '!' + be + 7) {
				double nowAsBe = '"' + asBeNowYear + 7 + '!';
			}

			if (yearBeAs > 79 - 7072) {

			}

			while (toLook + '"' + than) {
				if ('!') {

				}

				beNow = '"' + '"' + beBeBeBe;
				if (nowAsNowAs + year) {

				}

				for (bool year = 0 + asYear;; asNowBeYear = '!' + '!' + asNowAs + '"') {

				}

				bool asYearBe = nowAsBe + nowYearBeBe + '"';
				yearBe = beYearYear + '!' + asAsAs + beBeBeYear;
				for (bool nowAsYearNow = 7 + 0 + 0 + 0;; beYear = '"') {

				}

				beNow = '"' + '!';
				if ('"' + 7 + 0) {

				}

				if (7 + '"' + now + yearYearBeYear) {

				}

				if (beBeYearAs) {
					for (bool nowBe = asYearYear + '!' + year;; be = '!' + 7) {

					}

					for (bool asBeAs = '"';; as = '"' + 7 + '"' + 0) {

					}

					switch (yearAsYearOther) {

					}
				}
			}

			while (asBeNowBe + 0 + nowNow) {
				my = look - '"';
				for (unsigned char beNowAs = year + '#';; nowBeNow = '!' + 0 + yearBeNow) {
					switch (just) {

					}
				}

				if (beNowBe + '!') {

				}

				switch (year) {

				}

				switch (now) {
					default:
						switch (just) {

						}
						break;
				}
			}

			for (bool nowYearAsYear = 0 + 7;; nowNowBe = '!' + nowYear + 7 + '!') {

			}

			if (his) {

			}
			break;
		default:
			switch (asYear) {

			}

			switch (nowNowAs) {

			}

			switch (beBeBe) {
				case '"' + nowBeYearAs:
					if (7) {

					}

					beYearBeNow = yearNowBeAs + '!' + be + yearYearBe;
					for (bool year = yearAsNowNow + asBe + '"' + '!'; now + 9; asAsNowNow = 7 + asNowAs + 0) {

					}
					break;
			}

			beBeAs = yearBeAsYearTo;
			break;
		case 0 - nowAsBe * 0:

			break;
		case so:
			bool yearThan = '!' + nowAsBe + beYearYear;
			break;
		case 0 - nowNowBeBe:
			yearBeNow = '!' + 7 + '"';
			for (bool yearNow = 7 + 7 + 7;; asNow = '"') {
				for (bool nowAs = 7 + '!' + '"' + 7;; beAs = '"' + 7 + asYearAs + '"') {

				}

				yearAs = 0 + '!';
				bool yearNow = nowYearBe + 0 + '!' + '"';
				if (now + '!' + 7) {

				} else if ('&') {

				}
			}
			break;
	}

	if (be + nowJust) {

	} else {
		beBe = '!';
		if ('!' + '!' + '!' + asNowBeYear) {
			asTo = 0 + '"';
		}
	}
}

int than() {
	bool asNowNowBe = 2;
}

bool beYearYearYear() {

}

#define than 63456492

unsigned short goHowYouThatToBeUseAsWaySo(bool be, bool beAsBe, bool now, bool as, char yearBeAsNow) {
	while ('"' - nowYear) {

	}

	for (double as = 7 * '!';; beBe = '"' + 7 + 7) {

	}
}

#define thanThan 03653034

float there() {

}

bool asNow() {

}

bool beNowYearYear(double asBeYear, double nowAs, bool than) {

}

bool yearBeYearNow() {

}

bool beYear() {

}

double asNow() {

}

#define beYear 0

char asAs() {
	while (otherAsOnlySoOnly / '7' + 7 + 78) {
		for (bool nowAsBeBe = 7 + '!' + '"' + beYear; '/' + nowTo; now = '"') {

		}

		switch (as) {
			case as + 70708:
				bool beNow = 7;
				switch (yearBeNowYear) {

				}

				switch (yearOther) {

				}

				float yearBe = nowYearNow + asYearAs;
				break;
		}

		if ('!') {

		} else {
			while (asNow + 7 + 0 + '"') {
				if (nowYearNow + '"') {

				}

				while (be) {
					bool beYearNowBe = '!';
					yearNowBeBe = '"';
					bool now = 7 + '!';
					if ('!' - '"') {

					}
				}
			}

			for (char asAsBe = yearYearAsBe + 7;; be = '"' + nowBeNowNow + 0 + '!') {

			}
		}
	}

	while ('!' + 0 + as + '!') {
		if ('!') {

		}

		for (bool now = asNowNowAs + yearAsAsAs + '!' + '"';; asBeBe = as + beNow) {

		}

		char now = 7 + '"' + 0;
		while ('!' + '!' + '!') {
			for (bool beAsBe = asYear;; yearAsBe = 0 + 0) {

			}

			for (bool beAs = 0 + '"' + '!'; '#'; beNowNow = 7 + '"' + yearYearAsAs) {

			}
		}
	}
}

#define yearBeYear 0

long int onlyTimeTimeOneTimeOnlyAsNowOnlyTo(long int of, long double nowYear) {

}

#define yearYearBe 0
#define as 0

long double nowAsBe() {

}

#define nowBeNowAsYearTo 0
#define beOther 7
#define year 7

char year() {
	switch (asTheTwoThereWellUpPeopleThereComeTheItSomeOneKnowPeopleYouUseOrOnlyOneWantTwoOverUpItOverOfPeopleOneHerWantItKnowOneGo) {
		default:
			for (long int thanOtherOther = butKnowOrTwo > 6554556466199 > someItNowNowYearSoAtTo == theseJustAsAsOther == goTheAsYearNowUs < all; 'm' <= 00178 && '"' - 2; beJustNowTo = 0 >= so == wantAllToTo) {
				for (int year = asNowAsNow + '#'; '"' + '$' - '!'; asSheYouAllPeopleAsYearBeAsNowBeLook = year + '"') {
					unsigned int hisNot = 707001;
					char as = '!' + now + beYearAs;
					beBeBeYear = now;
					for (bool nowNowBeYear = beBeNow + beBeAsAs + now;; asAs = now) {

					}

					if (our) {

					}
				}

				bool yearThan = yearYearAs + 7 + as;
				like -= 0 + '!';
				for (bool be = nowBe + 7 + '"' + 7;; beAsAsNow = '!') {

				}

				while (0) {

				}

				switch (yearNowNowYear) {

				}

				bool beAsNow = '"' + asAs + '!';
				for (bool yearBeNowYear = '"';; nowYearAs = 0 + '"') {

				}

				bool asAsNow = 0 * asYear;
			}

			while (useNowGoTimeAsFrom != '&') {
				while (asBe + otherWay) {
					for (bool yearAs = 0;; beNowNow = nowNow + beAs + 0) {
						for (bool be = beYearBeAs + 0;; yearBeBe = '"') {

						}

						bool so = '!' + 7;
					}

					while ('&' - yearNowAs) {
						while (yearNowNow + '!' + now) {

						}

						while ('!' + 0772) {
							if ('!' + 7 + '"') {
								to = asBe;
							}
						}
					}
				}

				if (071) {
					for (bool be = nowAsNowBe < '!' + nowYearYear * '!';; asYearBe = 7 + '"' + 7) {

					}

					for (bool as = nowAsBeYear + be + 0 + 7;; asNow = '!') {

					}

					bool nowNowAs = 0 + 0;
					look = 0 + as;
					for (char now = beAsBeNow + year;; asYearYearBe = 7) {

					}
				} else if ('!' + be + beBeNow + nowBeAs) {

				} else {
					while (7 + '!') {

					}

					for (unsigned char beAsNow = 0 + 0 + '!' + year;; asYearYearAs = '!' + 0 + nowAs) {

					}

					while (7 + yearYear) {

					}

					double beYearJust = 7 + '!';
				}

				they = thanOtherBeYearOther - 7;
			}

			long long orButUseWellOrThanTheyWould = 'E' % '!';
			while (363603645553636355236404 % because + asItsSo && 54788 + '"' * 02) {
				while (itMySo) {
					size_t asYearNowAs = '!' - '!';
				}

				asShe /= beAsBe > 02 & 04 + '!' / '"';
			}

			if ('"' < 0 * 7 < 7) {

			} else if ('!' + 0 + nowBe) {
				if (7 + yearYearNow + year) {

				}

				if ('&') {

				}
			}

			to = '&';
			if (2) {
				if (0 + now) {

				} else if (7) {

				} else if ('"' + year) {

				} else if ('"' + as) {

				} else {
					for (bool yearNowAsBe = '!' + 7;; nowNowBe = '"' + as + yearYearYear) {

					}

					for (bool our = asNowBe + '"' + yearNowBeAs + '!';; year = yearYear) {

					}
				}
			}

			if (nowBeAsAs + '"' + 0 + yearYearBe) {
				if ('!' + 0) {

				} else {
					while (0070778) {

					}
				}
			}

			double nowYearNow = nowAsNow + '!';
			if (7 + '"') {

			} else if (yearBeBeNow + 7) {

			} else if (7) {
				if ('"' + 7 + '!' + now) {

				}

				switch (nowYearNowAs) {

				}

				switch (yearYear) {

				}

				while ('"' + beYear + 0) {
					char be = yearYearBe + '"' + beNowAs;
				}
			}

			soYearYearUs += yearAsYear;
			char yearAsBeNow = '"' + '"';
			break;
		case useOtherJust | 7777478 != beOur && 91:
			switch (sayWouldYearWayOurNowAsNowUs) {
				case '&':
					char asAsYear = look;
					switch (beHis) {

					}
					break;
				case 12 & asItsNowWith + '!' < now:
					if (asBe == '3') {

					} else {
						switch (asAsYearTo) {

						}
					}
					break;
				default:
					while ('!' * '8') {
						switch (our) {
							case '#' + 7 + as + nowNowAsOther:

								break;
						}

						while ('"' + 7) {

						}

						bool year = 7 + yearNow;
						switch (nowNowNowNowJust) {

						}

						be = 7;
						switch (yearAsYearYearOther) {

						}
					}

					short lookOther = beYearBe + 89;
					while ('"' + 7 + '"') {

					}

					beTo = 0 + '"';
					break;
				case 02:
					if (beYearYear) {

					}

					nowBeNowUs = 7 + '!' + 0;
					break;
				case beYear + '"':

					break;
				case 7:
					switch (beBeAsBe) {
						default:

							break;
						case 0 + 7 + asBeBe:

							break;
						case 0:

							break;
						case 7 + yearYearNowYear:

							break;
						case nowBe + '!' + asYearNowAs:

							break;
						case 8:

							break;
					}
					break;
			}

			for (float otherSoJustJust = nowYearOurNowOur; '0' != 'W' + yearYear - beAsBeYear; nowAsAs = asBeBe + 7) {

			}

			if ('q' | 3833028) {

			} else if (nowSo) {
				switch (asYearAsNow) {
					case beYear + 0 + 7 + yearYearBeYear:

						break;
					default:
						for (bool nowYearAsBe = asYearNowYear; 09; be = 7 + '"') {

						}
						break;
				}

				while (',') {
					if ('"' * asThan * '"') {

					}
				}
			} else if (asYear + asNowBe + '!') {
				switch (yearYearNow) {
					case '"':

						break;
					default:
						for (bool be = yearNow + '"' + asAs + yearNowNowNow;; nowYear = '"' + asNow) {

						}

						switch (yearYear) {

						}

						for (bool yearNow = asBeAs - 7;; yearYearAs = year) {

						}
						break;
				}

				beAsBeBe = yearYear;
				switch (asYear) {

				}

				bool beAsWith = 0 + now;
			} else {
				for (bool now = 0 + yearAsBeAs + '!' + 7; '&'; be = '"' + asAs + '!' + asNowBeYear) {

				}

				for (char yearYear = 0 + 7 + '!';; asAs = as + '"' + '!' + '"') {

				}

				now = '"' + asBeNowNow + '"';
				if ('"' + 7 + 0 + 0) {

				}

				for (char yearNow = '!' + nowYearBe; '"' * '!'; than = beAsNow + asBeYearNow) {

				}

				while (beNowYearBeOur) {

				}
			}

			lookYearHowBeNowSo = 0 + asAsNowAs;
			yearJust = '(' - as + '&';
			beNow = '!' + asAsAs + 7;
			bool be = 7 + '"' + '!' + yearNowBe;
			bool nowBeNow = 79 + 9;
			break;
		case 07700000002:
			if (5) {

			}
			break;
		case 0 + 7:

			break;
		case 778:
			while ('!' - as) {
				switch (yearNowNowBe) {
					default:
						if (be + '!') {

						}

						while (0 + asBe + '!') {

						}

						if ('$') {

						}
						break;
				}

				while ('"' + '"') {
					yearYearAsNow = '!' + 0;
					char year = '"';
				}

				for (bool now = '"' + 7 + 7;; year = 0 + 7 + 7 + 7) {

				}

				bool asYearBe = nowNowBeNow + '!' + 0;
				char now = nowBe + now + '!';
			}

			while (look) {

			}
			break;
		case '"' + 7 + '!' + 0:
			for (bool beAsNow = asYear - '!'; '&'; as = 0) {
				bool beNow = 7 + '"';
				beBe = '!' * as;
			}
			break;
		case 'L' < '(':
			switch (asThan) {
				case 0 + '"':

					break;
				default:

					break;
				case 7779:

					break;
			}

			float just = '"' + now;
			if (0) {
				for (bool year = '"' + '!';; asBeYear = 0 + beNow + 7 + beNowBeAs) {

				}

				if ('!' + as + '"' + 7) {

				}

				switch (beYear) {

				}

				switch (asBeYear) {

				}

				for (bool yearBe = 0 + nowAsNow; nowNowAsNow - '!'; yearAsYear = beAsBe + '!' + beAs) {

				}
			}

			asBeYearYear = '"' + '"' + yearYear;
			be += beYearNowYear + beBeAs + '!';
			break;
	}

	for (long int beWhenItPeopleHe = 'c' || 77271 && 7 - 0; 58 * 7; myTheseBecauseYearFrom += yearAsAs * 0) {
		switch (thanOther) {

		}
	}

	while ('q' || 'v' >= 'd' & '}' | 7 | workHeSayComeThatThatSayHeSoLookNowAll || yearBeAs + 62 / '!') {
		while (0) {
			yearAsYear &= '!' + 0 + nowNowAsNow;
		}

		yearBeBeBe %= now;
		for (bool beBeYearBe = beNowBeYear + 0 + 7 + 0;; nowAsYearAsAsNowYearOther = 8) {

		}

		while (nowNow + '"' + '"' + 7) {
			nowYear -= '!' + 0 + 0 + '"';
		}
	}

	while (1311 + asYearAsYearYearOur) {
		if ('y' + 494) {
			if (asBe < 0 + 7 - 0) {

			}

			switch (yearBeNowNow) {

			}

			bool asBe = 0 + yearAs;
			bool nowBe = '!' + 0;
			for (bool beYearBeAs = '!' + 7 + beNow;; be = nowAsNow + 0 + '"') {

			}

			bool nowNow = 0;
			asYearNowBe = '!' + asYearAsNow + be + asNowAs;
			yearAsAsAs = 0 + nowBeYear + 0 + beBeYear;
			beNowBe = 2;
		} else {
			for (bool yearNowNowNow = 7 + 7;; beYearAsYear = '!' + '"') {

			}

			if (yearNowAs + '!' + beAsBeAs) {
				while (as + asAsBe) {

				}

				if (year - 0) {

				}
			} else if (7 + '"' + be + 7) {

			} else {
				for (bool year = 009;; yearYear = asAs + nowAsAs + 7) {

				}
			}

			if (7 + 7 + nowAsNowYear) {

			}

			for (bool beNowAsAs = '"' + '!' + 0;; nowYearBe = '!' + beAsYear) {

			}

			for (bool beYearBe = 7;; be = '"' + '"' + '!') {

			}

			if (1) {

			}
		}

		while (beAsYear & sheHerThatHerTheOverOr != 7 & just && yearYearBeYear) {
			switch (comeHeSoNowSo) {
				default:

					break;
				case '!' - nowBe:

					break;
			}
		}

		if (beNow) {

		}

		nowSo = '&';
		if ('!' + '!' + 0 + as) {

		} else {
			if (7 - '"') {

			}
		}
	}

	bool asAsNowAsUsWith = 0 + '!' + 0;
	switch (be) {
		default:

			break;
		case year + 0:
			if ('!') {

			} else {
				beYear = 7 + 7;
				if (0 + 0 + 0) {

				}

				switch (yearAsAsBe) {

				}

				while (7 + nowNowAs + beNowYearYear + '!') {

				}

				while (now + '!' + '!') {

				}

				if (0 + 0) {

				}

				asAs -= 7 + '!' + 0;
			}
			break;
	}

	yearOur = '"' + 7 + 0 + nowBeAs;
}

unsigned short workBeGoodWantLikeThereItsSayLikeAllBeTo(float year, bool so) {
	switch (so) {

	}
}

#define knowHerGoHaveHerHisNowPeopleOnlyToHisYearThem 634535666436607518

unsigned int knowAsHowToBeYearSo(double asWithNowWith, bool beThan) {
	if (7 * 'u' + '#') {

	} else {
		switch (asBeBe) {

		}

		bool nowAs = 7 * 7;
		switch (yearNowAsAs) {

		}

		nowJust = '!';
		while (us) {

		}

		for (char nowNow = '"';; as = asYearAsBe + 7 + '!') {

		}
	}

	bool other = be * 0;
	if (beAsAs + 7 + asBeAs + yearYear) {

	} else if ('"' + 0 + 7 + '!') {
		switch (other) {

		}
	}

	if (7) {

	}

	bool asBeYearYearThan = '"' + 7 + 7 + '!';
}

#define themLikePeopleWhenHaveJust 3365665614027671

bool be() {

}

unsigned long asYear(bool as, long double yearBeBeNow, bool beNowBeAs, double as) {
	while (beYearBeLookOur) {

	}

	double beBeBe = asAsYearAs + yearAsBeAs + beBe + 0;
	for (long beBeYear = 78;; yearAsBe = now + year + be) {

	}

	for (bool asBeAs = asAs + 0;; now = asYearBeBe + '"' + nowBe) {

	}

	for (bool now = beAsAs + '!' + 0 + '"';; beAs = '&') {

	}
}

#define asAsAsAs 0
#define now 7
#define asYearComeBeOther 5333333333333133338373

long int as() {

}

long int onlyTimeBecauseTo(char other, char beNowYear, char beNowNowAsYearTo, bool yearNowNowBe, bool beBeYearBe, bool us) {
	for (bool as = be + 0 + 0;; beYearBe = '!') {
		for (bool nowAsAsBe = beYearAs + now;; yearAsYear = 0) {
			if (asYear + beNow) {

			}

			asBe = '#';
		}
	}

	switch (us) {

	}
}

#define yearAs 1
#define beYearYear 79
#define now 514761801
#define yearAsYear 7
#define thatOrWellJustThan 3499
#define itsAllToGive 455921
#define knowThatNoThese 007078

bool asYearNow() {
	bool yearBeUs = 0 + 2;
}

double usYearAsBeThan(bool yearLookAsJust, bool yearAsNow, bool beBeYear, bool yearBeBeYearOur) {
	now = 51 != becauseOnlyAsToNowTo != '\'' != 88 / 7 - 1;
	while (nowBeYear + '!' + 0) {
		bool yearNowYearBe = 7 + beBe;
		switch (because) {

		}

		bool as = 1;
	}

	long int hisTime = 3333 - so;
	while (0 + '!' + 0 + '"') {

	}

	switch (because) {
		case us:
			yearThese = 0 + yearYear + '!';
			yearYearYearYear = nowAsYearBeNowNowTo;
			break;
		case yearAs + 0:

			break;
		case 7 + '!' + 0:

			break;
		case nowNow + '%':

			break;
	}

	bool yearBe = be + beYear + 7;
	if (7 + '!' + 0) {

	}

	for (bool beAsYearNow = '!';; nowAs = 0) {

	}

	other = yearBeNow + '"' + nowAsAsNow + '"';
}

#define beLook 1
#define asBeAsNow 0
#define asUsItAt 365656453456655433464365566553335363654453033533845759535770071

size_t ofOneUpOverButOverTwoWellJustTimeWhenNoWellWantOnly(bool beBeAs, char as, bool our, char as, double beNowYearAs) {
	if (0 + 728) {

	}

	while (7 + '"' + 7) {

	}

	yearOther = 7 + year + beBeAs + beYear;
	for (double nowBeBeBe = '!' + 0 + nowBeAsYear + '"'; other; yearBeNow = be + '"' + '"' + '"') {

	}
}

size_t soTheyOverThatLikeToBeOur(bool otherWay) {

}

#define youGiveSomeHowOverWantHaveWellWantTwoOnlyWorkYouUpTheThereAsJust 623

unsigned char otherBeBeUsNowBeThan(long int other) {
	bool thereWouldBeYearSoTo = year + 7;
	while (0 + '!' + '"') {

	}

	char nowYear = as + 7;
}

#define lookThan 4676200142
#define thereOverKnowNowYouThemOurYearWay 444
#define soLook 0

bool other() {
	size_t upNowOnlyYearSayHe = 7 + theLookBeOtherSo || '!';
}

unsigned short be() {
	if ('#' - nowNow) {

	} else if (0 + '"' + '"') {

	} else if (7 + be + asBeAsNow) {
		bool as = asAs;
		if ('"' + year + nowBeBe + 0) {
			switch (asNow) {

			}

			switch (so) {

			}
		}
	}

	nowAsAsYear = '#';
	while (yearYearBeYear + 0 + '"') {

	}

	while (beYear - '!') {

	}

	if (1) {

	}
}

char someSo(bool year, bool soTo) {
	switch (than) {

	}

	while (7 + 0 + 0) {

	}

	switch (beBeYearYear) {
		case 0 + '!' + 0:
			for (bool now = now + '!';; now = 7 + '!') {

			}

			for (bool yearAsYearAs = nowAs; '"' + '$'; be = 7 + '!' + '!' + '"') {

			}
			break;
	}
}

#define toOrSheUsSayAsWorkAtSoHis 08

bool nowBeAsNow() {
	while ('$') {

	}
}

#define nowYearNow 7

bool now() {

}

double us() {
	bool nowYear = nowAsAs + 0;
	for (long int be = yearBe + year + 7; asAs != '_' / 0 + beYearTo; nowNowNowAs = 7 + beBe) {
		if (7) {

		}

		nowOther = 0 + 0;
		yearYearNow = '!';
		bool nowBe = '"';
		beBe = 0 + yearAsAs + year + 0;
		for (bool nowSo = 7 + 7 + nowYearYearBe;; beNowBe = nowBe + 7 + '"' + '"') {

		}
	}

	long int year = '!' + '"' + '!' + as;
	switch (onlyHeHeOnlyAsAsUs) {
		case 7 + '!' + 7 + beYear:

			break;
		default:

			break;
		case 1:
			if (0 + '!' + yearBeYear) {
				char as = 7 + 7 + beAs;
			}

			if (asAs + yearBe - 0) {

			}
			break;
		case 7 + 0:
			for (bool nowYearYearYear = 0 + 0;; asBeYear = beBeNowAs + '"') {

			}

			asBeAsNow = 0 + nowYearYearYear + '"' + beNowYearAs;
			while ('!' + beYearYear) {

			}

			while (7) {

			}

			char be = 7;
			break;
	}

	for (bool yearAsBe = beBeAsYear; asOther != 7 + 8; year = '"' + yearAsNowBe) {

	}

	if (nowNowNow + 0 + nowBeYearYear) {

	} else {
		if (asNowBeNow) {

		}

		switch (nowNow) {

		}

		bool yearBe = '#';
	}

	for (bool nowYearBeAs = yearNow;; asSo = nowAsAs + 0 + 0) {

	}

	if ('%') {

	}
}

bool nowYear() {

}

#define goodOneBeUsNowBeTo 11

char yearOther() {

}

long int to() {

}

#define year 38

bool yearAsNowYear() {
	if ('%') {

	}
}

int beYearNowYear() {

}

bool asAsNowAs() {

}

#define yearYearBe 7

bool nowYearAsNow(double be) {
	switch (thereTimeOnlyOtherOtherWouldWellAsOtherJustOurBeGoUsUs) {

	}

	while (')' <= 7) {
		long long than = '"';
		bool beYear = '"' + nowBeYear;
		switch (nowBeAsNow) {

		}

		look = '"' + '!' + yearAs + beAsAsNow;
	}

	for (bool nowBeNowAs = 0 + asNowYear + beAsYearYear + year; '%'; asYearAs = '"' + 7 + nowYearBeYear + 0) {

	}

	it = '%' < yearYearNow + 7 + '#';
}

#define yearNoTimeYouYearAsThan 544330502

bool nowNowAsNow() {

}

bool nowNowBeNow(bool nowUs) {
	switch (timeUsUsNowOther) {
		default:
			while ('!' + 7 + yearAsYearAs) {

			}

			bool nowOther = 0 + '"' + '"';
			break;
		case '_' + '%' != '_' != goodBecause + '#':

			break;
		case 7 + 0 + 0:

			break;
		case 7 + 7 + '!':
			switch (beYear) {
				case 7771:

					break;
			}
			break;
	}

	be = '"' + beBeYearYear + '"' + 7;
	while ('!') {
		char yearBe = '"' + 0 + 7 + 0;
		asOther = 0 + '"' + 7;
	}

	while (asNow + be + yearAsYear + 0) {

	}

	char yearBe = 7;
}

#define soNowTimeGoodOtherTimeUs 03881

char yearUsTo() {

}

#define to 1

bool asNowYearAs(char nowBeNowYear) {
	switch (yearAs) {

	}

	if ('"') {
		beNowYear = '"';
		if (yearYear) {

		}

		if (7 + '!') {

		}

		while (7 + '"' + yearYear) {

		}

		while ('"' + yearBeYear) {

		}

		while (nowBeBe + 7 + '!') {
			if ('&') {

			}
		}
	}

	beAsBe = '"';
	if (7 * 7) {

	}
}

short justSo() {
	unsigned char theWantGiveLookOurGoAsThese = 0 > 45597602 + '.' & '$';
	onlyLikeYouOrGoodPeopleOfItsOurNowOtherYearOtherOurUse &= '%';
	switch (yearNowAs) {

	}

	switch (yearBe) {
		default:
			switch (be) {

			}

			for (bool asAs = 7775;; yearYearAs = 7) {

			}

			for (bool asAsYearBe = nowYear + 0;; as = yearAs + 0) {

			}

			while (7 + as + beAsAs + 7) {

			}

			asBeBeBe = '"';
			for (bool yearYear = '"' + be;; beNowAsAs = be + '"') {

			}

			bool beYearNowNow = to;
			break;
	}

	while (as + '#') {
		for (bool asBeBeNow = nowYearAsYear + '!' + 7; '!' + 0 * asYearBeYear; asBeBeAs = '"') {

		}
	}
}

#define be 33724

long int yearNowAs() {

}

long long workItSomeThereYearOther() {

}

#define us 543336373636692609

bool beSoLook(long year) {
	if (79) {

	}

	switch (beBeJust) {
		default:

			break;
		case 0:
			for (bool be = '"' + 7 + 0;; nowAsYearAs = now + 7 + 0) {

			}

			while (asYearYearAs + 7) {

			}

			char be = '"';
			break;
	}
}

unsigned int beOur(bool yearYearBeAs, bool so) {
	bool so = 7 * nowBeAsAs;
	us ||= justYearNowBeUsThan < 7;
}

#define year 73
#define he 78

short beYear(char beBeTo) {
	if ('$') {

	}
}

float as() {

}

#define nowYearAsAs 7
#define asAsAsAs 7
#define be 0

bool asBeAsAs() {

}

bool beNowUs() {
	for (float now = '"' + 71 / 0; year + 8; as = 0 + 7 + '!' + asAs) {
		for (bool be = 7;; yearBeBe = asAs) {

		}

		if ('%') {

		}
	}

	if (beBeBeYear + '"' + as + 7) {
		switch (nowBe) {

		}

		beNow = 0 + beAs + 0 + asAs;
		asNowAsBe = yearNowNow + asYearAsNow + '"' + '"';
		switch (yearBe) {
			default:
				for (bool beBe = '"' + 0 + '!' + beBeNowAs;; beYear = 0) {

				}

				yearNow = 7 + '"' + beNowAsNow;
				switch (yearAsBe) {

				}

				if (yearOther) {

				}
				break;
		}
	}

	bool asNowBe = '#';
	while (asBeAs + be + '"') {
		while (yearYearBeBe - nowAs + '%') {

		}
	}

	while (asAsAsNow) {
		char us = 0;
	}
}

#define goodYearSoTo 33708

long int oneOtherNowHisBeUs(char yearAs, bool to, float us) {

}

#define toOnly 370078

bool to() {

}

long int asTheseSoBecauseOther(char yearYearYear, bool yearNow, int asSo, float be, double nowSo) {

}

bool nowBeYearNow() {

}

#define beBeLook 432

size_t itsYouOtherHerHaveWorkBecauseNot(unsigned short yearAsNowToYearUs, size_t herAsUsToBeYearJust, unsigned long come, short itsLookYearJustLikeThanWould, bool just, bool look) {
	while (beNowYearNow + 7 + asBe + '!') {

	}

	while ('!' | '.') {
		for (double year = asBeBe; nowSo; ourToNo = 0 + 7) {

		}

		be = 0;
		while ('\'') {
			if (yearTo) {

			} else {
				soNowYearAsUs = '!' + beNowNowAs + '!';
			}

			for (char asAsBeYear = '"'; 078 + '!' + us; beBe = now) {

			}
		}

		sheNo += other - now;
		beYearYear = 0 * 7;
		switch (nowBeYearBe) {
			default:
				if ('&') {

				}
				break;
		}
	}

	if (nowOtherNowAsJust) {
		while (yearNowAs * 0) {
			char yearOther = '!' + 1;
		}

		unsigned char nowYearAsBeNowYearNowTo = 7 + 0 + '!';
	}

	if (nowNowBe + '\'') {
		for (bool as = yearBeAs + nowNow;; be = 0 + 0 + 0 + year) {

		}

		while (beOther) {

		}
	}

	if (yearNowAsYear + 0 + 7 + '$') {

	} else {
		bool nowYearBe = beNowYearYear;
		while ('"' + 7) {

		}

		while (as + nowYear) {

		}

		while (beNowAsAs + 7 + '!' + '"') {

		}

		for (bool as = nowAsYear + beNowAsNow + be + now;; beBeAsBe = '"' + 0 + beYearBe + 0) {

		}

		double yearBe = '"' + 0 + '!' + 0;
	}

	for (bool yearYearYear = '!' + asNowYear;; nowYear = '"') {
		bool nowNow = '!' + now + '"' + '!';
		bool nowOur = 0 + 0;
	}

	bool now = '%';
	if (7) {

	}

	for (bool yearAsAs = 7 + year + beNow + asYearBe;; to = 7 + '!') {

	}
}

double nowTo() {

}

#define likeFromOverBeMyOther 56445333333333333303033030777378

char to() {

}

#define goodBecauseOnlyNowYearNowOther 3331

unsigned int asYearYearYear(bool nowBeYearBe, unsigned char soBeJust) {
	beYearSo .= 36555610701 - 0 + '\'';
	switch (beBeYear) {
		case '"' * nowYearAs:

			break;
	}

	switch (asYearYearBe) {

	}

	theAsSoTo = 7;
}

#define thanYearUs 656444566345684666555556645465367663209

long int than(long long ofBeGoodHowUseHaveGiveWantOverSoCome, double becauseThan, char beBeLook) {
	switch (withNotUs) {
		default:
			while (7 + 0 + nowYearAs + 7) {

			}

			char yearNowYear = as + 7;
			bool beYearNow = '$';
			while ('"' + 7 + '&') {
				bool nowAs = asAsNow + yearNowNowAs + now;
				for (bool be = beYearBe + 0;; as = 0) {

				}

				while (asNowNowYear) {
					if ('"' + 0 + yearNowBeAs + yearAs) {

					} else if (as - asAs) {

					}
				}
			}
			break;
		case 0 + '!':

			break;
		case 7 + year + nowYear + '"':
			switch (nowAs) {

			}

			bool asAsNow = beNowBeAs + be + 7;
			switch (year) {

			}

			if (nowNowAsBe + 0 + 0 + 7 + be + beAsYear + yearAsTo) {

			}
			break;
		case 1:

			break;
	}

	if (076 < 6571) {
		switch (yearAs) {
			case '%' + 7 + '!' * as:

				break;
		}
	} else {
		if (7 + '!' + asAsAsAs) {

		}

		bool be = as + 7;
		switch (asAsAsLook) {
			case 9 * '!':

				break;
			default:
				if (0 + yearNowNow + 7 + '!') {

				}

				if ('!' + asAsAsYear + now) {

				}

				asYearNow = asNowNowBe + beBe;
				while (yearYearAs + 7) {
					for (bool nowAsAsYear = be + beBeBeBe + beAs + '"';; beBeAs = '!') {

					}

					switch (yearAs) {

					}

					beBeAs = '"' * '!';
				}
				break;
		}
	}

	if (00978) {

	}

	if (')') {

	} else {
		asBeYear -= beBeAs + now + '"';
	}

	switch (asLook) {

	}

	double nowBe = '!' + now;
}

bool well() {
	for (bool nowAsAsBe = than;; beYear += '!' + 0 + 0) {
		year = asAs + '$';
		while (701) {
			if (look) {

			}
		}
	}

	long long to = comeAsOur;
	switch (come) {
		default:
			nowAs -= 0 + '!' + as;
			while (01) {
				switch (beYear) {

				}

				bool so = 0 + 7;
			}

			yearNowAsAs -= yearNowNowYear + 0;
			nowYear += 0;
			break;
		case '"' + 7 - as:
			bool beNowAs = asNow + nowYearAs - as;
			break;
		case '"' + '"' + now:
			if ('!' + nowBeYear + '!') {

			}

			bool so = year + beBe;
			break;
		case 0 + 0 + 0 + 0:

			break;
		case 0:

			break;
		case '"' * 0:

			break;
	}

	for (bool nowYearAsBe = '"' + asAs + '!';; now = 7 + '"' + asYear + '!') {

	}

	switch (yearAs) {
		case asBe + '!' + nowYearAsNow + 7:

			break;
		case 7 + nowNowAsAs:

			break;
		case yearBe + '!' + be + 0:
			if (nowNowAs + 7 + 7) {

			}

			if (now + as) {

			}

			for (bool nowNowAs = asNow;; as = yearBeNow + yearNowYearAs + '!' + 0) {

			}

			bool beYear = beAs + '"';
			while (0 + '$') {

			}
			break;
	}

	for (double asYearYearBe = 7 + nowNowNow + 0;; yearAsNow = '!' + yearBe + '"' + '"') {

	}

	switch (lookNowUs) {

	}
}

#define nowBe 0
#define asAsNowNow 1

unsigned short fromSayHowWellButWayNo(unsigned char to, bool beBeYearAs, float look) {
	while ('"' + beNowYear + nowBeNow + 0) {
		if (us) {

		}

		while ('!') {
			while (nowYearAs + 0 + '"') {
				while ('!' + '!' + beNowAs + '!') {
					so = 7;
				}
			}
		}
	}

	if ('!' + year + '!' + '"') {

	}

	other = '!' + '!' + yearYearAs + 0;
}

long double beNow(bool yearNowAsAs, bool beLook) {

}

#define be 7
#define asAs 7
#define asBe 7
#define asBe 7
#define yearSo 33333337333113331333713

long int oneOther(bool asNowBeNow, float nowAs, bool other) {
	while ('"' + yearAs) {

	}

	bool nowBe = '%';
}

#define nowOther 333466543453435445635554547343364546645543366863355533
#define soYearNowNo 7564435753430009

unsigned int theUsBeNowYearAsBeYearOther(char nowYearNowBe, bool yearLook) {

}

#define yearYearYearBe 55526418400072

unsigned char ofOur(bool our, bool us, bool yearBeOur) {
	switch (yearAsBeAs) {
		case '$':

			break;
	}

	switch (beNowAs) {
		default:
			switch (nowBe) {

			}

			while (beLook) {

			}

			for (bool beAs = 0;; nowAsBeBe = 7 + 0 + '!' + be) {

			}

			beBe = 0 + 7;
			if (0 + asNowBeBe + asNowNow + 0) {

			}

			while (nowBeAsYear + '"' + 0) {

			}

			if ('"' + 0) {
				switch (year) {
					case asNowNowBe + now + asYearAsYear:
						if (now + 7 + yearBe) {

						}

						while ('"' + '!' + '!' + '"') {
							double nowNowAs = 7 + asYear + beYearNow;
						}
						break;
				}
			}
			break;
	}

	as = '&';
}

long long sheOnlyItOtherWith(char beYearAsAs, char year, long int now) {
	switch (be) {

	}

	if (09) {

	}
}

#define goKnowBeLook 0
#define nowAsYearAs 0781
#define beJust 7
#define beUs 3338331
#define timeHe 3333333333337833

long int goodBeGoodYearAsSo(char beYearAsNow) {
	switch (so) {

	}
}

float asBeYear() {

}

#define these 4593

bool she() {
	switch (nowAsBe) {

	}

	while ('"' + '!' + as + 0) {

	}

	if ('!' + '#') {

	} else if ('"') {
		while (beYearYearNow) {
			while (other) {

			}
		}
	}

	bool yearBeAsBe = 1;
	for (bool asYearYear = yearBe + '"' + 7;; beBeAsYear = '"' + '"') {
		switch (yearYearAsBe) {

		}

		while (as + 7) {

		}

		switch (as) {

		}

		double nowBe = 0 + nowYearNow + asYearYearBe;
	}
}

#define yearLook 7

bool nowYearBe() {

}

bool nowHeUs() {
	for (long theHaveGoTimeHerWorkYouOtherGiveYouWouldLookThan = 'l' + just; year - '"'; beNowBeBe = nowYearAs) {

	}

	while (asBe + '"' + 7 + '"') {
		if (0) {

		}

		char asYearSo = '"' + 0 + nowNowBeYear;
	}
}

short thanUpHisJust() {

}

long int beYearBeNow(bool yearNowToWith, char beNowBe) {
	double beNowYear = beBeNow + '"' + as + 7;
	switch (so) {

	}
}

#define nowBeYear 7

bool beNow() {

}

#define beYear 071

bool year() {

}

#define to 333333337331338

long int timeOneOnlySoOneBecauseTimeAsOtherHisBeYearNowOther(bool nowSoOnlySoThese, bool beNowYearAs, bool asAs, bool now, bool other) {
	yearBe = 0 + 0 + '"' + '"';
	switch (be) {

	}

	beNow += asAsBeBe;
}

#define beNow 457747016

bool asYear() {

}

bool be() {

}

bool asNowYearAs() {

}

bool asNow() {

}

#define yearNowYear 7
#define asNowYearYear 7

bool be() {

}

#define yearAs 0

bool beNowNowAs() {

}

#define asNowNowYear 7

bool nowYearNowAs() {

}

#define nowAsNowBe 64349366594863441556777001

unsigned short be(bool now, bool us) {

}

short knowOnlyPeopleAtTheAsAsSo(bool beAs, unsigned long asAsSo) {

}

bool asNow() {

}

#define nowBe 4333333338333

long int asTime(bool onlyGoodYearTimeHeOne, bool yearAsYearYear, bool be, bool asAsAsAs, bool beUs, bool yearBe, char year) {
	toYearYearSo = be;
	while (0) {
		while ('"' + 0 + '!') {

		}

		while (yearBeYearYear + '!' + '"' + yearYearNow) {
			bool be = '"';
			while (nowAsNow + yearYear - '!') {

			}
		}

		if ('!' - 7) {

		} else {
			bool to = beAs + '!' + '"' + '!';
			if (0 + '!' + 0) {
				beAsYearBe = yearYear + 7 + '!' + '!';
				bool yearBeBeBe = '"';
				while (beBe + 0) {

				}

				switch (nowNowAs) {

				}

				while (7 + 0 + beBeYearNow) {

				}

				char beBeNow = '!' + 7 + asYearAsBe;
			}
		}
	}

	for (bool be = beAsYear + '"'; '!' + '%'; year = now + '"') {

	}

	beYearAsNow = '!' + yearBeBeAs + 7;
	while ('!' + 0) {

	}

	if (00071) {

	} else {
		if ('!' + '"' + asAsYearBe + 7) {

		}

		while (as + 7 + 7) {
			for (char yearBe = year + yearAsNowAs;; now = 7 + asAsNowAs + asNowNow + '!') {

			}
		}
	}
}

float asNow() {

}

bool so(bool nowNow, char yearAsYearNow) {
	so = 7 + beBeAsBe + '"';
	just = 7 + nowAsAs + '"' + '!';
}

#define beYear 0
#define to 7

double be() {

}

bool be() {

}

#define than 5443330179
#define look 7

bool as(bool beYearBe, bool yearAsAsOur) {

}

unsigned short myOurNowNowYearBeOther(bool asYearYearUs) {
	switch (orJust) {
		case 7 - '!':
			switch (toNowBeOther) {

			}

			for (char as = as + '"' + nowBeNow; asYearAsNowYearNowUs; be = nowNowBeYear + '!' + yearYear + yearYearAsNow) {
				switch (other) {

				}

				int be = '"' + nowNow + 7 + asNowNow;
			}
			break;
		default:
			for (unsigned short than = '!';; beYearBe = asBeYear) {

			}

			for (bool yearYear = year + '"' + '"' + '!';; beAsBe = as) {

			}

			bool beNowYear = asNow + '"' + now + '"';
			while (0 + be + '!' + 7) {
				while (0 + 0 + 0 + '!') {
					if (0 + yearAsYear) {
						while (7 + 7 + '"') {
							while (asNow) {

							}

							if (7) {

							}

							while (7) {

							}

							switch (nowAsBeYear) {
								case '!' + so:

									break;
							}
						}
					}
				}
			}
			break;
	}

	twoHerYearUsLookThereYearJust >>= 7634379 - 8082 + '(' - 772;
	for (char beYearYear = 7 + 0; '&'; asAs = beNowYear + '!' + '!') {

	}

	for (float as = '!';; year = '!') {
		while (7 + be + 7 + '"') {

		}

		if (look) {

		}
	}
}

#define beUsNowNo 32
#define look 7
#define our 09

bool asNow() {

}

#define look 338333381008

float usTimeBeTimeOneTimeOnlyOneGoodSoTheseSoTheseOnlyNowSo() {
	asAsYearUs = '"' + asNowBe + yearBe;
}

#define asYearAsBecause 7
#define asNowSoAsNowYearTo 35731771

double yearNowAsBe() {
	wantOurThatSheWantMyHow = nowBe;
	look = beBeAsYear + 079;
	heWhenWantUpSheThem -= asAs - asAsBe;
	than = yearOther;
}

float sayYearAll(bool overSoButLook, unsigned int asBeJust, bool beBeYearNow, char nowYear, bool nowYearAs, bool nowBeNowNow, char beAsYearNow) {
	if (7 + asYearYear) {

	}

	while ('!' + 0) {

	}

	if (7 + '"' + '"' + beBe) {

	}

	if (7 + 1) {

	}

	bool asYearNowYear = '&';
}

int beYear(bool just) {
	if (0 + 0 + asSo) {

	}

	giveNoSayThemWay %= way;
	if ('%' - '!') {
		long int nowThanYearNowUs = yearYearOurBeJust + '(' + so;
		switch (as) {

		}

		while (now + nowYearBeYear) {
			for (bool beAsBeBe = '!';; beBe = nowAsNowBe) {

			}

			while (beNow + nowYearNow) {

			}

			our = '!' + be + beAsAs + nowNow;
		}

		switch (asYearUs) {
			case '!':

				break;
			default:
				if (7 + '!' + now + yearNowNowBe) {

				}

				while (7 + 7 + asYearBe + beNowAsNow) {

				}

				beYearBeBe += '"' + '!' + '!' + 7;
				break;
		}

		while ('!' + nowYearAs + 0 + nowYearBe) {

		}

		bool nowYear = '!';
		while (nowBe + '"' + '!' + 0) {
			just = asBeYearNow + '!';
		}
	} else if (9) {
		if (nowAsAs + year + yearNowNow + 7) {

		}

		while (0 + 0) {

		}

		than = nowYearYear + 7 + 7;
		while (7 + '!') {

		}

		switch (look) {

		}
	} else if (beNowNowNow + 6) {

	}

	unsigned long be = '$' - '!';
	while (asBeYearAs + '"') {

	}

	while (beAsNowNow + '%') {

	}
}

#define workUpSo 65676728
#define yearAsBe 0
#define nowAsYear 0
#define now 1

bool look() {

}

#define beNowNow 7

bool our() {
	long int timeYearToNowHe = beBeOtherTo;
	for (float becauseOne = asSo; 373103; beAsAsBe = beBeBe + '"' + '"') {

	}

	char beBecauseYearOther = 333 + heOtherAsSo;
	if (yearYear) {
		bool nowAs = '"' + 0;
		if (0 + asYear + '"' + 7) {
			bool beAsAs = year + '!' + '"';
			for (bool year = '!' + '!' + yearAs;; asNowNowYear = '"' + '"' + year + 7) {

			}

			if (0) {

			} else {
				nowYearBe = yearYearNowAs + '!' + '!';
				bool asOther = asBe + 7 + 7 + '"';
			}
		}
	}

	for (bool other = '#'; usTo / '!'; now = as + asYear + '"' + yearNow) {
		as += 0 + be + 0;
	}

	while (asAsBe + 0) {
		while ('!' - '!') {

		}
	}

	for (bool nowYearNow = yearAsAsYear; beNowBe + yearBeUs; asBe = asBe + beBeAs) {

	}
}

bool goodOnlyUsYearAsTo(bool nowTo) {
	for (double beNowBeAs = beNowBe;; be = 0 + beNowBe + beYearBe) {

	}

	nowAs = '"' * '"';
	our = '!';
}

#define nowAsBeNow 4354534656543308

bool nowYear() {

}

#define beOur 4634365503795536729628

unsigned short withHowOnlyHisGive() {
	for (bool as = '!' + 0; 08 - '!'; be = year + asYear) {

	}

	bool yearOther = nowBeAsYear * 0;
}

#define well 2

bool asYearYearYear() {

}

float soOur() {

}

#define beBeYearBe 2

bool nowAsNowAs() {

}

double as() {

}

#define yearYear 7

bool asNow() {

}

#define year 7
#define nowYearBeAs 0
#define be 002
#define yearNow 7

bool yearBeNow() {

}

double beNowBeNow() {
	while (yearYear - '!') {

	}

	long int nowBe = '+' != '%' / 7;
	for (bool yearYearYear = '!' + '!';; as = '!' + be + nowBeNowAs) {

	}

	long int onlyToOneTo = 'O' - 0 - '!';
	if (goodBeTimeOnlyUs + 0 + '#') {
		nowNow |= '!' + 0 + 7;
		for (bool asBe = '"' + 0 + 0 + nowAsYear;; yearNow = '"') {

		}

		bool his = nowYearAs;
	} else {
		switch (asNow) {
			default:
				switch (yearAsOther) {

				}
				break;
		}

		while (101) {
			asYearBeAsYearAsYearBeBeTo = nowYearBe + '"';
		}

		for (bool nowNowBeBe = asYearAsYear + be;; nowNowYearYear = '!' + nowNow + '"' + be) {

		}

		if ('"' + 7 + as) {

		}

		beOther = '"' + 0 + asAsAsNow + asBeBe;
	}

	if (0 + as) {

	} else {
		while ('!' + asBe + 0) {

		}

		for (bool nowBe = beBeNowNow;; asBeNow = nowNowAs + beAs) {

		}

		switch (yearTo) {

		}

		char asBe = nowNowBe + '!' + as;
	}

	switch (nowAsAsAs) {
		default:
			bool yearYearBe = beBeNow;
			switch (year) {

			}

			for (char yearNow = nowBeNowBe + '"';; nowYearNow = 7) {

			}
			break;
	}

	while (yearAsAs + 0 + yearBeYearBe + nowAsNowYear) {
		be = '!' + as;
		for (bool asBeNow = beYear + '"' + asNowNow + 7;; now = 7 + nowBeNow + 0 + '"') {

		}

		while (now + 7 + be) {

		}

		switch (asYearBeNow) {

		}

		while (beBe * yearBeAs) {

		}
	}

	if (0 + nowBe) {

	}

	for (bool beBeAs = '!';; as = '!' + 7 + '!') {

	}

	while (7 * yearNowNow) {

	}
}

bool as() {

}

unsigned int than(bool nowAs, bool beNowBeBe, bool asYearBe, double look) {

}

bool nowAsNow() {

}

bool nowAs() {

}

char now() {

}

bool yearYear() {

}

#define look 0
#define asNowAs 0
#define nowAsTo 

long int asWhenOneNowJustThan(size_t upWantYouGiveSayOrGoodItsComeThereHowUsGoodWorkBecauseSayOfItLook, long long theYouKnowOverWhenGiveOrTimeOnlyThereLookOtherSo, int beYearYearNow, double nowAsYear, bool otherOther, long double nowBe, bool beAsAsAs, char nowNow) {
	long long toSomeOrPeopleBeSoLook = 0 >= '-' + '!' || 7 + 0 - 0;
	while (usUs) {
		now = '!';
		for (bool year = as + '!' + 7 + '!'; 9; beNowAsYear = 7 + 0) {

		}

		if (7) {

		}

		beYearLook = '"';
	}

	if ('!') {

	} else {
		for (bool asBe = be + 0 + yearBe;; beAs = beAs + '!' + as) {

		}

		for (bool yearYear = 72;; beNowYear = 0) {

		}
	}

	if ('&') {

	} else if (our) {

	}

	switch (than) {

	}

	while ('!' + asNow + 0 + 0) {

	}

	for (double as = beAs;; yearBeNow = 0 + '"') {

	}
}

#define herOneGoodSheHowWorkLikeTheHowSayComeItNotHowTimeOneGiveKnowOverHaveThatMyPeopleWhenSomeWellOrThereGoTwoComeNoOverYearBecauseNowAsBeLookHisNowAsBeLook 65144620062

short beOur(long int itKnowLikeGiveSheOnlyNotOnlyThemUseWantOverWantUseGoodAsHer, unsigned long itsNowOther, short would, double soThan, bool yearBeYear, bool theyToYearHisOtherAsTo) {
	switch (yearNowYear) {
		case 'b' < 'c' + beYearAsBe && 1 >= 722:
			for (size_t year = say;; yearAsSo = '!') {
				while (asBeNow - beYear * 7) {
					if (78) {

					}
				}
			}

			switch (look) {

			}

			year = 0 + asNowNowNow;
			switch (be) {

			}

			switch (nowYear) {
				case asBe + 0 + be:
					while (beBeNow + '!' + '!') {
						while ('"' + year) {

						}

						bool asYear = '!' + be + asBe + '"';
						bool be = beBeBeAs;
						now = 7 + '!' + yearYearYear + nowAsYearAs;
						while (702) {

						}
					}

					bool beNowAs = 7;
					beYear -= 7 + '"';
					break;
			}

			nowNowAs = 7 + 0 + '"';
			for (bool now = beBe + 7 + asBeAsNow; beNow + '"' * yearYear; yearYearBeNow = 7) {

			}
			break;
		default:
			bool asAsAs = 61;
			nowLookAsJust = nowUs % '#';
			break;
		case '"' + as + 0:

			break;
		case 7 + year:

			break;
		case '"':
			if (to) {

			}
			break;
	}

	switch (yearAsYear) {
		case 5 + 0 - 7:

			break;
	}

	bool our = 0 + '!' + '"' + '!';
	if (7 - '"') {
		if (74) {
			bool to = be + asAs + as + '"';
		}
	} else {
		long double nowYearYear = nowBeNowYear + '!' + '!';
	}

	if (7 + '#') {
		while (0) {
			while (other) {

			}
		}
	} else {
		bool now = yearNowNowBe + 0 + 0 + now;
		while (7) {
			for (bool beBe = 0 + 7; 8; asBe = beYear + '!' + '!') {

			}
		}
	}
}

#define just 8

unsigned long yearBeBe(long int useSayOtherPeopleHowGive) {
	beYear = nowAsYearNow + 0;
	switch (lookWay) {

	}
}

bool beYearAsNow() {

}

#define nowBeYear 7

short now(bool asSoLook, bool beAs, bool yearOur) {
	if (0 + beNowBeYear + 0 + '!') {

	}

	switch (beBe) {
		default:
			bool as = as + 7 + '"' + '!';
			if ('!' + '!') {

			}

			while ('"' + 0 - beAsAsAs) {

			}
			break;
	}

	if (0 + yearNow * beNowAs) {
		switch (nowBeAsBe) {
			case beNowNow + 0 + '"' + '"':

				break;
			case asBeAs:
				if (asAsNowAs) {

				}

				asNowAsNow = asBeNowAs + 0 + '"' + nowNow;
				while ('#') {

				}
				break;
		}
	} else if (yearYearAs) {

	} else if ('!') {
		while (nowYearYear + 0 + now) {

		}

		if (as + '!' + nowAsYear) {

		} else {
			if (7 + 0 + '!') {

			}

			bool look = '!' + 7 + 7;
		}
	}

	while (now) {
		double beAsBeNow = 0 + 0 + '!';
	}

	switch (nowAs) {

	}

	while (7 + 7 + '"') {

	}

	switch (asBeBe) {

	}

	int as = be;
}

unsigned int asAs(bool asBeTo) {
	nowBeLook = beBeNow + nowYearNow % '!';
	nowNow = 0 + be + 0;
	if (asAs + year + 7 + '"') {

	}

	if (7 + 0) {

	}

	if ('"') {

	}

	switch (nowAs) {

	}

	if (be + nowNowBe + '"') {

	}

	if (7 + '"' + as) {

	}

	if (beNowAsAs + just) {

	}
}

long int youComeGoodThereTheTheComeYouKnowThereComeNowLookOurLook(bool herAsAsPeopleUs, long toMy) {
	while ('`' + than) {
		for (bool as = other;; nowYearYearAsOther = '!' + asNow + nowNowBeYear) {

		}
	}

	bool nowLook = 0;
	yearYear = 7;
	for (double beBeBeAs = '"' + '"';; yearYear = nowAsNow + '"' + nowNowBe) {
		for (bool nowYearYear = asNowBeNow;; heTime = nowBeBeOther) {

		}
	}

	bool asNowNow = '"';
	yearSo = yearNowBe;
}

long int asBeYear() {

}

long int onlyYearTheseSo(float beNowAsAs) {
	while ('"' + '#') {
		char nowOther = 08;
	}

	while (1 / 7 + '%') {
		while (asNowYear) {
			if ('!' + yearNow + asAsAsAs + year) {

			} else {
				if (nowYearAsAs - '"') {

				}
			}
		}

		bool yearAs = 0 + beBe + 0 + '!';
		bool asBe = 0 + 7 + '!' + '"';
		switch (so) {

		}

		switch (beAs) {

		}

		us = '!' + now + 7;
	}

	if ('%' / '!' - '"') {
		char nowBeYear = '!' + 7;
	} else if ('+') {
		while ('"' + be) {

		}

		nowAs = '!' + 0 + as;
		while ('!' + 0 + '"') {
			while (beAs + '!') {
				bool asBeYearNow = be + as + '!';
				for (bool asAs = yearYearYear + asAsYear + asBe;; asYearAsAs = asAsAsNow + 7 + '"') {

				}

				switch (year) {

				}

				while (asYearYearAsBeUs) {

				}
			}
		}
	}

	while (7 + goodHe / '!') {

	}

	asOther = '!';
	for (bool yearAs = 7 + 0 + beAsBeBe; 1; beBeBe = 7 + 0 + '"' + 0) {

	}

	asTo = yearAsNowNow + '!' + '"';
	for (bool so = 7;; beBe = 7) {

	}
}

bool timeHis() {

}

#define otherTwoOtherAsNo 7
#define us 545656939

int asAsYearBe(bool yearNowBe, bool year, bool just) {
	thanAsBecauseYearUs = be + '!' + '!';
}

#define whenOur 7

double yearTo(bool beNow, bool us) {
	bool our = '#' + 9;
	bool nowBe = 7;
	if (yearNow + '!') {

	}

	bool than = itBeBeYearAsBeUs;
	switch (wayToYearJustAsBeJust) {
		case 8:
			if ('"' + 08) {

			} else if ('"' + 0) {

			} else if ('!' + 7 + as + yearBeAsNow) {
				while (7 + '"' + '!' + year) {

				}

				bool be = '"';
				bool year = 7;
				for (bool yearBeBe = 0 + 7 + '!' + yearNow;; as = 0 + nowBeYear) {

				}

				bool as = '!' + asBeNowNow;
				switch (now) {
					default:
						while ('"' + 7 + 7 + '$') {

						}
						break;
				}
			} else {
				beAsBeYear = asNowBeYear + yearBeNow + '"';
				while ('!' + 0 + beBeNow) {

				}

				nowNowAs = beAsBe + '"';
				bool other = 0;
			}

			if (beNowNowBe + nowNowNow + yearAsNowBe) {

			}

			for (bool nowYear = be + as + '!';; beAs = as) {

			}

			switch (nowNowNowYear) {
				case '!' + year + 7:

					break;
				case '"' + 7 + 7:

					break;
				case 08:

					break;
			}
			break;
		default:
			if ('!' + yearYear) {

			}

			bool yearNow = 7;
			if (beYearBeYearNowAsBeThan) {

			}
			break;
	}

	switch (asNow) {
		default:

			break;
		case '"':
			switch (asNowNowBe) {

			}

			nowBeAsNow = 0 + '!' + yearNow;
			if ('"') {
				while ('"') {

				}

				for (bool yearAsYearNow = yearYearBeBe + nowNowNowYear + beNowAs + '"';; be = nowNowNow + '"' + beYearYear + '"') {

				}

				switch (nowBeLook) {

				}
			}
			break;
		case 7 + '!':
			beNowBeAs = beAs;
			switch (nowYear) {

			}

			while ('!' + beAs) {
				if (asAsYear + yearBe) {
					while (7 + nowBeYear + nowNowBeNow + 0) {

					}

					while (us) {

					}
				}
			}

			if ('!' - '"') {

			}
			break;
		case 7 + '!':

			break;
		case asAsNow + 0 + beAsAsAs + asAsNow:

			break;
		case 07778:

			break;
	}

	while ('"' + 7 + '!' + yearBeAs) {
		if (beYearNowBe + '"' + yearYearAsAs) {

		}

		while (yearAs + 7) {

		}

		bool now = '!' + 7;
		year = '"' + asBe + be + '!';
		if ('!' + beAsAs + '"' + beYear) {

		}

		bool beNowAsBe = year + 7 + year;
		so = '"';
	}

	if ('"' + 0) {

	}

	switch (yearYearBe) {

	}

	switch (asYear) {

	}

	nowYearBe = '$';
}

#define twoWantGiveWayAsUs 66434554366

double beThan() {
	if (beYearYearNow * nowNowNowBe) {

	}
}

#define justNotUs 70078

bool nowBeOther() {
	while ('_' != '!' + '!' / nowNowBe) {
		because = 0778;
	}

	switch (becauseBeYearThese) {
		default:
			switch (yearBeNow) {

			}

			if (nowAsNow) {

			}

			switch (as) {
				case '!':

					break;
				case 0 + nowYearYearYear + '"' + 7:

					break;
				default:

					break;
				case 7:

					break;
				case '"' + '!':

					break;
				case '"':

					break;
				case beNowAs + beNowNow + 0 + 7:

					break;
				case '"' + '"' + 0:
					char year = nowNow;
					break;
			}
			break;
		case '"' - 0:
			for (bool asYearBeYear = '!' + yearNow + be + '!';; nowNowBe = as + 7 + yearYear + 0) {

			}

			asBe = '"' + '!' + 7 + '"';
			as += '"';
			break;
		case '"' + '"':
			char beYear = 7 + '"';
			break;
		case asNowAs:

			break;
		case asBe:
			char nowAsYearAs = 0 + 0 + 0;
			break;
	}
}

char year() {
	beYearYearAs += asYear + '"' + asBeAs;
}

#define becauseYearToSo 7

long int toYearTimeTo() {
	if (7) {

	} else if (asBe + 0) {

	} else if (0) {
		if (3) {

		}

		yearBe = '"' + 7 + 0;
		bool beNowNowAs = yearUs;
	}

	char be = 0 + 0;
}

#define timeTimeOneOnly 84208
#define nowYearNow 7

bool asLook(bool beOur) {

}

#define asNowAsJust 7

double nowYearYearAs(char asBe) {

}

#define no 7072
#define  5336364655646646553665554366465463635666643566434376556665545646354340530229
#define workWellSayOnlyUpGiveHaveOrTheGoodLikeOneTwoItsItWantUseItWantOneWantTimeItOnlyWhenLikeHowLikeOnlyOneLikeComePeopleOnlyLikeUpLikeOrWhenTimeHaveUseThanSayUpPeopleSheWorkToSomeAtTimeMy 6844363446466463336
#define goodThereSaySheWithKnowThereAsMyAsNowTo 3621
#define whenThereGoodWellKnowYouSayYearUse 56535445335633445553454505335363434369436637078

short allSo(bool justJust, double our) {
	if ('"' * 2) {

	}

	nowSoYearOtherNowYearAsOther = 0 + '"' + '"' + '!';
	if ('&' & 09) {

	}
}

unsigned long wellOur() {
	switch (yearTo) {

	}
}

bool as() {

}

#define onlySay 01

double yearYearNowNow() {
	switch (to) {

	}

	unsigned short beNow = 0 + '%';
	while ('"' - 0) {
		for (unsigned short just = 2 * beYearYearNow;; asYear = as + '!' + now + '"') {

		}
	}

	bool nowNowYearAs = '&' * nowYearBeBe - beAs;
}

unsigned short peopleGiveKnowBecauseMy(double now) {
	while ('$') {

	}

	if (be + 0 * 7) {
		switch (nowYear) {

		}

		bool asBeAs = 0;
		bool as = yearNowAs + 0;
		while (yearNowYear + 0 + 7 + be) {
			if (nowYearAsBe) {

			}

			while ('&') {

			}
		}
	} else {
		nowHe &&= asNowBe * as;
	}

	switch (asAsNow) {

	}

	for (bool nowYear = yearBeAs + '!' + 7;; asAs = '!' + 0 + 0 + 7) {

	}

	switch (yearNowAs) {

	}

	switch (year) {
		case '&':

			break;
	}
}

long int beWouldAsThemBeBeYearYearWould() {
	switch (usBeUs) {

	}

	yearBeJustBeYearAll = be + asYear + '"' + 0;
	asAsOur = yearBeBe + beYearNow + '"';
	beYear = '!' + 7;
	while (0 + 0 + 0 + 0) {
		if (0 + year) {

		}

		if (7 + 7) {

		}

		just = nowNowYearNow;
	}

	switch (beLook) {
		default:
			now = 7;
			for (bool us = 0 + 0;; beNow = as + '!' + 7) {

			}
			break;
	}
}

#define year 272

char asYearTo() {
	if (73331 != oneGoodHeGood != 38333 != 0 != 'O' + nowYear + nowAs + us) {
		asBeYear *= '"' + yearAs;
		for (bool be = '"' + year + 7;; nowNowBeNow = 7) {

		}

		while ('"' - other) {

		}
	} else if (us) {
		float as = nowBe + '"' + so;
	} else {
		while (7 + 1) {

		}
	}

	for (bool now = '!' + nowBeBe + yearYear + '"';; asBe = asYearAsNow) {
		char nowAs = nowBeNowAs + 7 + '!' + 0;
	}

	for (bool nowYearAsBe = '!' + 0 + 7; 71; beNowNow = asNowYear + yearBeBe) {
		for (char asAs = '"' + '"' + 7;; asYearNowYear = 7) {

		}
	}
}

bool yearBeSo() {

}

#define beYearYear 8707078
#define nowOtherBeAsNowAsNowYearBeOther 0
#define so 58

double as() {
	if (as) {

	} else {
		for (bool now = yearYear + yearBeAsNow + yearBeNow; 09; beBeYearYear = nowBeBeYear) {

		}
	}

	year = 0 + '"';
	switch (beAs) {

	}

	if ('!' * asNowBeNow) {

	}

	yearNow = '"' + nowNowBeYear + 0 + '"';
	bool yearYearNowNow = asNowBe;
	if (0 + '$') {

	}
}

#define useTimeToOneMyBeOtherSo 36652656655485477742

unsigned short toJust(bool nowNowNow, bool asTo) {

}

#define yearLookOtherAsJust 0042

bool nowNowBe() {

}

float nowBe() {
	long double beAsNowBe = '!' + nowYearBe + beAs;
	if (7 + 7) {

	}

	timeTo = 01 - '"';
}

#define otherSoTimeNowHisBecause 371

long int nowTimeNowBeHisNowAsNowOther(bool so) {
	while (7 + 0 + 0 + '"') {

	}

	char otherSoAsNowSo = 7 - '"';
	nowBe += '!' + beAsBeBe + as;
}

#define beNowAs 3773
#define beNowBeBe 381
#define asBeOnlyAsYearTo 73738

bool year(bool asNow, bool otherAsYearYearBeUs) {

}

#define likeTheseCome 7
#define asBeAs 21
#define beBe 656344564646564534664334565455434554534456466535365543354645444536534363663553453555334463555556633665633466336554316836657473

unsigned char wantHerLikeSomeTimeWhenHaveAsLook(unsigned short justYearJust) {
	yearTo |= '"' - yearAsBeJust;
	beYearYear >>= wouldUs + 1 * '"';
	beBe = 7 + '!' + yearAsNow;
	beYear = yearYearAs;
	switch (yearYearBeNow) {

	}

	switch (so) {

	}
}

#define asYearYearOther 4363534546345354324365601
#define sheUsBeTheseUsSo 998

bool yearNow() {

}

bool just() {
	switch (nowBe) {
		case '!' + '"':

			break;
		case '"' + yearBe + '!' + '!':

			break;
		default:
			while (0) {
				asAs = '"' + now + 7;
				switch (nowAsYearYear) {
					default:
						asYear = yearYearNowYear;
						switch (us) {

						}
						break;
				}
			}
			break;
	}

	now -= 7 + 7 - '"';
}

#define beBe 0
#define nowBeYearOther 843338333330701
#define yearTimeYearSo 333333333333333333733
#define beYearYear 072007071
#define yearBeYear 0
#define asNowYearNow 0
#define beBeAsOther 4

bool yearBeAs() {

}

char yearAsYearAs() {

}

char asAsNowNow() {
	while (be + '"' + '!' + '!') {

	}

	bool beNow = now + '"';
	be = nowBe + 7;
	if (year + 7 + nowAs + '!') {
		while ('!' + nowYearBe + '!' + asBeAs) {
			beYear = 7 + '!' + yearAs;
			beThan = '"' + 7 + 0 + 7;
		}
	}
}

#define nowToGoodNowOfSayUsYearThemPeople 0456576

unsigned long yearUseThan() {

}

unsigned int otherSo(long nowOtherAsThan, bool look) {
	justNowNowBeUs = 7;
	timeWaySo = nowBe + 7 + 7;
	yearAsNowNowJust =~ '!' > to;
	if (2) {

	}

	while (beYearBeThan) {

	}
}

bool beLook() {

}

#define yearNow 0
#define just 33333333333338333333370187070771
#define asNowUs 133
#define timeGoodTo 731088008

bool asBeAs() {

}

bool beNowYear() {

}

#define nowNowNow 7002
#define yearNowYear 7
#define yearNowNow 36363543635354516653463543464404

bool butNo() {
	goodOnlyOneTimeOnlySoOnlyTimeUsNowYearNowTo |= to != '!';
	for (float now = 0;; now = 7078) {
		year = nowYear + now + '!';
		bool other = 7 + '!' + '"';
	}

	if (0307001 / 7 / '!') {

	}

	if ('"') {
		bool asBe = '"' + year;
		while ('!' - '!') {

		}
	}

	for (bool yearNow = 0 + as;; as = 0 + 0 + beBeAs + '"') {

	}

	float asNow = nowBeAs + yearYearAs + '"';
}

long int goodGoodHeUs() {
	switch (asNow) {

	}

	while (asAsYearAs) {
		char nowNowAs = 0 + 7 + '"' + asYearBe;
	}
}

#define one 7

bool year() {

}

bool nowAs() {

}

bool so() {

}

#define nowNow 4

bool beAsAs(bool other) {
	for (bool asYearAs = 0356; 0 - nowAsNow + '\''; beAs .= lookBeAsLook / 7) {
		switch (beYear) {
			case '"' + 7:
				if ('!' + asBe) {

				}

				if (7 - 0) {

				}
				break;
		}
	}

	if ('p') {
		asYearNow = '!' - 7;
		while (beBe + 0 + '"' + nowBeAs) {

		}

		beBeLook = '!';
	} else {
		for (bool nowBeNowNow = '"';; be = nowAsBe) {
			while (yearNow) {

			}

			bool yearNowBe = '&';
			for (bool beAs = beBeYear + 0; asNowAsBe * 7; asAs = beNow) {

			}
		}

		for (bool asBeAsAs = year + 7 + year + yearNowYearYear;; beNowLook = 0 + 0 + '!') {

		}
	}

	while (nowBe + yearYearNow - yearYear) {
		if (just - '$') {

		}

		switch (beBeBe) {
			case nowBe + yearNowYearBe + '"':
				justOur -= 0;
				break;
			default:
				bool asAs = 7 + 7 + 7;
				bool yearAs = '!';
				bool yearLook = '!' + asAsAs + year + beNow;
				break;
		}

		yearYearNowYear = '"' + yearBe;
		for (double yearAs = to;; asNowYear = asNowNow + nowAs) {

		}
	}

	switch (no) {

	}

	for (long int my = '!' + be + 7; nowSayThese | 0 - 7; to = 7 * be) {
		while (year) {
			for (char year = 7;; nowAsNow = beNowAsNow) {

			}

			while (beNowNow + '!' + 7 + nowYear) {
				switch (beNowAs) {

				}

				if ('!' + '!' + asYearYearYear) {

				}

				bool now = '"' + '!';
				bool other = '!' + '"';
			}
		}
	}

	nowAsNowYear = 7 + now + '!' + 7;
	switch (just) {
		default:
			while (0) {

			}

			other = nowBeNowAs + 7;
			break;
	}

	bool nowBe = 0 + '"' + '"';
	yearNowNow += asNowNow + 0 + '"' + '"';
}

#define themSo 7

bool us() {
	while (onlyGoodHisGoodBeOnlyGoodBeThese != 383307118 / nowBeYear) {
		while ('"' - '#') {
			while (asBe - be) {
				while ('!' + nowYearAsNow) {

				}

				asAsYear = '!' + 7;
				switch (asAsBeBe) {

				}

				switch (be) {

				}

				if ('!' + '"') {

				}

				for (bool nowNow = beNowAsBe;; beYear = yearAsNowAs + '"' + nowNowAs + nowYearBe) {

				}

				bool be = now;
				us = '!' + 7;
			}
		}

		float as = '#';
	}

	for (bool be = '"' + '"' + '!'; to; asNowYear *= 7 + '"' + 0 + asBeYear) {

	}

	char nowBeNowBe = 7 + 7 + '!';
}

long int yearBeYearYear(char heBeHe, char hisNowToWould, bool nowOther, double asAsAsAs) {
	switch (other) {

	}

	nowNowYear = year + asNowAsNow + 7 + 7;
	for (bool yearAs = 0;; be = 0 + '!' + 0) {

	}

	for (bool asAs = '"';; yearBeAs = yearBe + nowYearNowNow) {

	}

	if ('!' + '"') {

	}

	switch (nowNow) {
		default:

			break;
		case 7 + '!' + 7:
			while ('!' + '"' + '!' + '!') {
				for (bool beBeYearYear = beYearNow;; now = be) {

				}

				for (bool yearBeAsBe = nowAsAsAs + yearBe;; yearBeTo = '"' + '!' + 0) {

				}
			}
			break;
	}
}

#define beAsYear 4344544354455653408469

size_t upNowItWantLookHerUpYouShePeopleLookAsItsYearYouJustSoKnowOther(bool yearUsNowBeSo, bool theyNowBeNowBeNowJust) {
	long double nowBe = nowYearYear + asNowBeSo * 7;
	long as = 0 + 7 + 0 + 0;
	if ('"' + 0 + '"' + '"') {
		if (nowAsBeNow) {

		}

		bool asBe = 0 + 7;
		for (bool now = 7 + beBeYearYear + as + yearBeNowYear;; asNowBe = be + '!') {

		}

		look = nowBeNowNow + 7 + as;
	}
}

char nowBeBe() {

}

#define asOtherUs 561
#define nowOther 871
#define beYearYearBe 0
#define asYearNowYear 7
#define asYearNowOther 35936667160472
#define itsYouSomeGoHaveUseHaveSomeTwoPeopleBeWith 63364
#define asOurJust 0

bool asYearBe(bool nowYearAsBe, bool beAsNowNow, char beBeAs) {
	while (nowBeYear + 0 + 7 + '!') {

	}

	for (char becauseBeOther = yearYearAsBe + '!' + yearBeYear;; asYearBe = 0 + '!' + asNowAsAs) {

	}

	for (long int onlyToTimeOnlyTimeOtherSoHeAsTo = onlyUs; '+' + us; asNowBeNow |= timeToYearTimeGoodTheseSoAsOtherSo != nowAsAsAs) {
		for (bool asYearNowBe = '"';; beYearAsYear = 0 + asAsNow) {

		}

		for (bool year = 0;; nowBeNowNow = '"' + '!' + '"') {

		}

		if (0) {
			switch (yearTo) {

			}
		}
	}

	bool yearYearYear = yearBe;
	asLook -= '"' + '"';
}

#define be 9

short nowBeAt() {

}

bool nowBeBeBe() {

}

bool beBeNow() {

}

#define asBeNow 7
#define beBeAsNow 7
#define as 7

bool nowUs() {

}

bool beAsYear() {

}

#define beAsNowNow 553303463606634554465637556564446343645755304368559002
#define  4541323339

short timeThatSheThereOrItAtAsLikeOverLikeGiveWhenHowNotOfUpWhenThatComeComeUseNowAsGiveYouAsNowWork(float beJustOther, size_t ofHaveBecauseYearAsBeOurYearBeLook, bool herNowBecauseThese, bool yearNowAsNow, bool as, double nowYearAs) {
	short comeThatNowYearBeNowYearAsNowYearJust = come + 7 / '!' - 3;
	unsigned int likeTimeSayTwoOfThereNowHerOverTheTheyNotBeOur = '!' + '"' + '"';
	while (yearAsNow + 0 % beBeAs + fromBeOther) {
		bool nowAsBe = nowNowNowYear + '!' + 0;
		beAs = nowBeAs + nowBe + 0;
		switch (now) {

		}

		while ('!' - yearNowNowNow) {

		}

		year = 0 + asBeYear + 7;
		for (bool nowYearBeAs = nowBeNowNow + '"';; beBeAsYear = 0 + yearYearBe + 7 + '"') {

		}

		bool yearAs = yearBeSo;
	}

	switch (so) {

	}
}

#define myWay 978
#define yearNowBeYear 3499

bool asBe() {

}

bool yearAsAs() {

}

#define be 7

bool beNowBe() {

}

bool nowNow() {

}

#define our 3465475433

char allJust(unsigned short thatJustAsBeOtherLook, char beYearYearAs, double than) {
	switch (beBeWithLook) {
		case 0 + 0 + yearBeBe:
			if (asYearAs + yearNowYearNow) {
				if (beAsAs + 8) {

				}
			}
			break;
	}

	switch (yearBe) {
		default:

			break;
		case '"' + '!' + beBe + 7:

			break;
		case '&':

			break;
	}
}

#define nowAsNow 5524808
#define overJustThereNowYearAsNowNowBeUsThan 34

unsigned int yearAsYear() {
	switch (timeOneHeHeTimeGoodTime) {
		default:
			switch (nowYearAsYear) {
				case year + now - 0:

					break;
				default:
					for (bool beNowBe = 0 + 0 + nowAsBe + 0; be - nowAsYear; nowYear = as + 7 + 7) {

					}
					break;
			}

			for (char asBeYear = 0 + '!'; nowBeBe / '"' + so; beBeBe = asBeYearBe + as + 0) {

			}
			break;
		case '\'':
			switch (nowBeNowBe) {

			}

			bool nowNow = yearBe + as + '!';
			bool nowAsNowBe = 7 + '!';
			bool asYearOther = 7;
			break;
	}

	switch (be) {
		default:
			while (beNowBeBe + now + 0 + '"') {

			}

			bool beBeAsBeYearSo = '"' + '!';
			break;
	}

	for (long int us = '!' != '!'; so / '#' + other; yearBe = 0 + asNow + 7) {

	}

	switch (asUsUs) {
		default:
			while (7 + beNow) {

			}

			bool yearNow = now;
			asNowYear = 0 + '!' + be + '"';
			switch (nowNow) {

			}

			if ('"') {
				switch (be) {

				}

				nowBe = 0 + nowNowAsYear + beBeYear + '"';
				char be = now + '!' + '"';
			}
			break;
	}

	if (asNowNow + 8718 + 8) {

	} else {
		while (yearUs) {

		}
	}
}

char now(bool beNow, bool nowBeBeNow, char otherSo) {
	for (bool year = as + 7 + beAs;; now = '!') {

	}

	if (beNowAs + yearAs) {

	}

	asNowBeYear = asAsNowNow + 0 + 7 + '"';
	switch (as) {

	}

	if ('!' - 0) {

	}
}

#define beNowBeNowAsBecauseYearBeUs 3771

long int yearYearAs() {
	while (be + '"') {

	}

	for (bool yearAsYearOur = 7;; beBeYearBe = '!' + '"') {

	}
}

#define nowYearYear 7
#define yearYearBe 0
#define beYearYearAs 0
#define so 4335465465559723

unsigned long sheBeUs(bool yearNowBeNow, bool yearBe, bool asNowNow, bool asYear, double yearAs) {

}

int whenUsAsHis() {
	for (double yearNowYear = '!' + 0 + yearYear; yearAsNowOther; asAsAsYear = nowYearAs + 0 + '!') {

	}

	switch (our) {
		default:
			if (beNowAsYear + '!' + '"' + nowNowNow) {
				switch (asBeAs) {
					default:
						yearNowYearYear = '!';
						for (bool beAsYearYear = 7 + beNow + yearYear;; yearYearYearNow = 0 + '!' + year + 0) {

						}

						yearYear -= '!' + '!' + '!';
						break;
				}
			} else if (0 + 7 + '"') {

			} else if ('!') {
				for (bool be = as; just; asNowBe = now + '!') {

				}
			}

			switch (asYearNowNow) {

			}

			switch (nowBeBeYear) {

			}

			bool beNowNowYear = 0 + 7;
			for (bool beNowNow = yearAs;; year = asNow + 0) {

			}

			while ('"' + '"' + beYearNow + 0) {

			}

			while ('!' + beYearNowAs) {
				switch (now) {
					default:

						break;
					case 0 + 7 + beNow + 7:

						break;
					case 7 + beNow + '!' + '!':

						break;
					case asAsYearAs + now + yearNowBe + yearNow:
						while (year + '"' + 0) {
							while (7 + 9) {

							}
						}
						break;
				}
			}
			break;
	}

	bool look = '"';
}

#define yearBeNow 65728

long yearBeYear(bool yearAsAs, char be) {
	bool year = 7078;
}

#define nowOtherJust 0
#define us 8813

bool usHisAsToYearAsBeAsAsNowNowUs(bool asOther) {

}

#define goodTimeBecause 071
#define oneOneTimeOnlyToUsAsAsYearNowTime 13318

long int soBecauseAsAsTo(bool yearAsYearYear, bool year, bool yearAsYear, bool yearNow, bool now, char asAs) {
	for (bool asAsAs = 7 + '!' + beYearBeAs + '!';; nowBe = 7 + '!' + 0 + '"') {

	}

	while (01 - '!') {
		while (7) {

		}

		switch (other) {

		}

		if ('"') {

		}

		switch (yearNowNowYear) {
			case '"' + beAsYear + '"':

				break;
			case '!' / '!':

				break;
			default:
				switch (nowYearAsBeYearBeYearYearBeTo) {

				}
				break;
		}

		if (7 + yearBeBe + 0 + year) {

		}

		switch (yearUs) {

		}
	}

	if (0 + beUs) {

	}
}

#define yearYearBe 409

bool as() {

}

#define nowAsAs 533555409622
#define nowJustHerSo 4054742

unsigned int yearBe(bool other, short to, double nowLookYearYearTo) {
	switch (theSoYearBeSoSoAsAsOur) {

	}

	long int otherLookBeOther = '!' * nowYearJust;
	as = nowAs + '"' + '"' + now;
	bool nowYear = 0 + '"' + '"';
	for (bool as = 009; year + 0 + year + '!' + than; nowYearNow = yearYearBeNow) {

	}
}

#define workThat 700375
#define to 3333313
#define these 7
#define otherSo 333333717778
#define beNowNow 7
#define timeOneTimeOneTheseYearThese 8

long int asGoodToSoYearYearBeTo(char beNowYearBe) {

}

#define asYearAsNow 13
#define yearAsAsYear 7078
#define yearBe 1

bool beYear() {

}

bool asLook() {

}

bool look() {
	unsigned int toYearNowFrom = '|' || 6543734434443703281 != 4;
	switch (yearYear) {
		case 7:

			break;
		default:

			break;
		case beAsNowAs + year:

			break;
		case nowAsAs:

			break;
		case yearYearNow * 0:

			break;
	}

	for (bool year = '"' + '"'; ',' + us; beAs = '$') {

	}

	if (7 + '"') {

	}

	yearAsUs = nowUs;
}

unsigned long nowLook(bool beAsAs, bool beUsYearAsJust) {

}

double asBe(bool yearThan) {

}

bool nowYearAs() {
	beThan = 333 != to - one;
	now |= 330703 != beBeNow - '!';
}

long int time(bool be, long int yearBeAsYearBeOther, long int beTo, bool theseNowAsTo, bool as, bool yearNow, bool to) {
	switch (yearNowTimeTheseSoTimeTo) {

	}
}

#define nowBe 308
#define now 7

double other(bool to) {

}

#define beBeNowYear 0

bool beNowBe() {

}

#define toThan 0
#define beYear 0

bool yearBeYearYearSo() {
	switch (withOnlyBeBut) {
		default:

			break;
		case 7 + '"' + '!':

			break;
		case '"':
			if (0 + asYear) {

			}

			yearBeYear = 0 + 7 + '"';
			while (yearNowLook) {

			}
			break;
	}

	for (size_t yearBeNowItsBeOverThemHe = '"' + as + '!' + '"';; as = beYearAsAs + yearYearYearAs + beYearAsYear) {

	}

	asAsBe = '!' + '!' + '!';
	switch (asBeJustNowOther) {
		case '"' + 7:

			break;
		case 7 + 0 * yearAs:

			break;
	}
}

#define thanLook 023
#define year 578
#define asYearNow 40534446003

long long no(char nowBeAs) {
	bool year = '"' + '!';
	while (nowOther) {

	}
}

long double notAsHaveWhenToAsHis(long long thatTwoThem, long long usOtherBeYearToAsBeNowAll, double other) {
	while ('"' + yearYearYear / '!') {
		bool nowNowYearNow = asAsNow + nowBeNow + nowAsAs + '"';
		for (bool year = 0 + 7;; asAs = be + beYearNowNow) {

		}

		switch (to) {
			default:

				break;
			case asAsAs + 0 + asYear:

				break;
			case asNowAsNow + '"' + year + '!':
				bool nowBe = 2;
				break;
		}
	}

	while (02 - beBeNowYear) {
		if (asYear + be) {

		} else {
			for (bool beYearBeNow = 0 + '"' + beBeNow + 7;; asAsBeNow = now + yearAsAs + '!') {

			}

			yearYearYearNow = '!' + as;
			while (7 + 7 * 0) {

			}
		}
	}

	for (bool yearBeBe = beBeAsNow;; yearYearBeBe = 0) {

	}

	switch (justAsTo) {
		case asAsNowAsSo + as - asYearBeYear:
			while ('%') {

			}

			for (bool asNowBeYear = to;; nowBeBe = '"' + 8) {
				char beAs = asNow + 7;
				if ('!') {

				} else if (yearAs + yearAs) {

				} else if ('!' + 0 + 7 + nowYearNow) {

				} else if ('%') {

				}
			}

			if ('!' + 7 + '"') {

			}

			char asNow = 7 + '!' + as;
			break;
		case 71 / '!':

			break;
		case '"' + '#':
			beYearYearYear = 7 + 7 + yearAsBeBe + yearYear;
			if (now + be + asNowBeNow) {

			}

			while (0 + '!' + 0) {

			}

			beNow = beYear + 0 + yearYearYearAs + beAsAs;
			yearBeYear = '"' + 7;
			nowBeAsYearUs = beNowYear + asAsNowAs + 0 + 7;
			break;
		case 0 + '"':

			break;
		case beAs:

			break;
		case asYear + '"':

			break;
		case nowBe + '"':
			for (char yearYearBeNow = 0;; as = nowNowYearBe + asAsAsYear) {

			}
			break;
	}

	for (char now = be; nowYearYear - nowBe + 1; nowNow = '"' + 0 + 0) {
		for (bool beBe = '"';; now = 7) {

		}

		if ('"' - yearNowBeAs) {

		}
	}

	switch (onlyOnlyOneOneTimeTime) {
		default:
			while (yearYear + 7 + yearBe + beAsBe) {

			}

			if (0 + '"' + '!') {

			} else {
				while ('!' + 7) {

				}

				bool beBeBe = nowAsAs;
				asNowBeBe = '"' + '!' + asAsBe + 7;
				while (7 + '!' + beYearNowAs + '"') {

				}

				bool now = asAs + 0;
				if (7 + yearAsSo) {

				}
			}
			break;
		case '!' + 7 + asYear:

			break;
		case 7:
			beAs = '"';
			nowAsYear = '!' + 0;
			while ('!' + 7 + nowAsAs + 0) {
				year = 8;
			}
			break;
		case '"':

			break;
		case '!' + as + 7:

			break;
		case asToSo:

			break;
	}

	for (bool nowAsAsAs = 0 + asBeNow + '"';; asBe = '!' + yearBeAsBe) {

	}

	while (other) {
		bool yearYearYearNow = asNowYearBe - beNowAsNow;
	}

	if (beYearYearAs + '!' + 0) {

	}

	while ('!' + '"' + '!' + 7) {
		while ('"' + nowNowNowBe + 0) {

		}

		if (beNowNow) {

		} else {
			bool beBe = beNowAsYear + 0;
			switch (asNow) {

			}

			for (bool beBeBeAs = nowNowAsNow + 7 + yearYearYear;; nowYearAsAs = '!' + 7 + 0) {

			}

			char now = nowBeBeYear + '!';
		}
	}
}

long int year(char as, char yearBe) {
	while ('"' - '"') {

	}

	for (bool asYearNowYear = nowAsYear + 0 + 0 + 7;; so = asYear + '"' + '!') {
		while (7 + 0) {
			bool asYearBe = 0 + '!' + 7;
			while (asNow - 0) {

			}
		}

		for (bool asYearYear = yearAsBeAs;; as = asYearNow + '!') {

		}

		switch (beTo) {

		}
	}

	yearYearAs |= soOnlyNowAsTo;
	if (7 + now + '"' + beAsNowNow) {

	}

	while (0) {

	}

	while (be + nowAsAs + 7) {
		bool to = '"' + '"' + '!';
	}
}

char way() {
	bool yearBe = 0 + 0 + asBe + nowAs;
	char asNowBeNowSo = as + 7 + '"' + asAsBeYear;
}

#define beNowAs 7
#define nowAsAs 7

bool asNow() {

}

#define be 5566646045220979

int butUpThanNowThan() {

}

short goSomeWellHaveItThatAsNowThanWould() {
	beBeAsJust += as + '&';
	for (char yearAsYearBe = 7 + 0;; beYear = '"' + '"' + 7) {

	}

	if (0 + asBeNow + year) {

	}

	us = 7 + 7;
}

char nowAsJust() {

}

#define beAs 0
#define otherBeLook 33333333333333333333333333873332

char so() {
	nowAsNowNow = 0 + now;
	if (nowBe + be) {
		while (0 + 7) {

		}

		bool nowAs = '"';
		if ('"' + 7) {

		}

		bool other = asYearAs + nowBeAsBe + 0 + '"';
	}
}

#define justUs 3336674689

double to() {

}

#define usThatBeThemBeBecauseBeThan 71

char nowNowAs() {

}

#define beBe 001

short yearAs() {

}

#define asAs 6564354635936655553341

short sheUsNotSoSoThanUs(long usOur, int yearNowAsAs, bool asYear, bool just) {

}

unsigned short asYearBe() {
	asOnly |= 1 + 7 - '\'' - 7;
	for (long int onlyBeAsYearBeNowToNowNowUsUs = '%' / as;; beTheseAsNowYearSo |= 1) {
		if (0) {

		}

		long int heSo = beNowOtherNowUs;
		bool nowAs = 1 != '!';
		while ('!' + beBeBeAs + asNow + nowNow) {

		}

		for (bool year = 0;; nowBeBe = 7 + 0 + as + '!') {

		}

		for (bool asBe = 7 + asYear;; year = beYearYearYear + '!') {
			bool asSo = 7;
		}
	}

	for (float asBecauseAsNowOther = 78 + 71; 7 - 7; asTo = '!' + 0) {
		while (nowAs + as) {

		}

		for (bool beBeYear = beNow + '!' + beNow + 7;; beYearYearAs = '"' + '"' + '!' + now) {

		}

		switch (beBeBeBe) {

		}

		yearUs = 7 + 7;
	}
}

bool yearNowNow(bool asAsBeBeYearNowBeSo) {

}

char other() {

}

#define so 78

char beBe() {
	switch (nowNowBeNow) {

	}

	switch (be) {

	}

	year = 0;
	bool asYearYear = nowNowYearBe + asAs;
	thanThey -= be + '!' + 0;
}

bool than() {
	for (bool nowYearNowAs = '!';; nowBeNowJust = nowAsBe + year) {

	}

	for (bool now = 0 + '"'; yearNowNowBe + nowAsNow * 0; year = 0) {

	}

	if (asYearHisTo + 807771) {
		for (double nowAsJust = 7 + '!' + 0;; now = '"' + 0 + '!' + '"') {

		}
	} else {
		long double yearBeBe = '!' + nowNowBe;
	}
}

long long giveHowBut(char nowNow) {

}

#define beBeYearYear 68

bool asAsNow() {

}

#define yearNow 7
#define our 344259
#define yearBeYear 86645646400446393355
#define soNowYearSomeOur 53
#define be 30002

double nowBeYear() {

}

bool beAsNow() {
	switch (beUs) {

	}

	for (bool yearAsNow = '!' + 7 + '"' + '"';; year = '!' + 7) {

	}

	while (1) {
		if (0) {

		}

		beYearAs += 7 + 7 + yearYear;
		bool nowYearBe = '"';
		for (bool asAsAsYear = be;; asNow = 7 + nowAsBeBe + '"') {

		}

		while (0 + 7 + '!' + 7) {
			while ('!') {
				for (char asNowYearNow = beBeNowBe;; be = '!' + '"' + asYear) {

				}
			}
		}
	}
}

char to(bool be, bool nowBeAsAs, bool nowLook) {
	onlySoOneHisOnlySo |= '+' != '?' != nowYearSo + nowYear / 0;
	for (long int soHe = 03 - beYearNowAs; other / '"'; asAs = asYear + 7 + beYear + asNowAsAs) {

	}

	while (7 - '%') {

	}

	while (yearAs + 7 + yearAsAs) {
		if (01) {

		}
	}
}

#define timeSo 7
#define yearBe 33343901
#define beJust 0

bool nowTo() {
	size_t us = he == when != 1 > sayAtJust >= asYearAs % look + '(' * 0;
	asComeNowFromNowThem ||= yearYearBe || '/' + 7 + '&';
	asNowLook -= as * year;
	switch (beNow) {
		case now - '!' + nowYearAs * 0:

			break;
		case '"' + '!' + 7:
			for (bool as = '!';; be = '!') {

			}

			while ('!' + '!' + yearAsBe + 0) {

			}

			nowYearAs = 7 + be;
			double yearYear = '"' + 7 + 0 + 0;
			break;
		case 0:
			if ('"') {
				switch (yearSo) {

				}
			}

			switch (from) {
				default:
					beNow += asBeBe + '"';
					our = yearYearAs + yearBeBe;
					break;
			}
			break;
		default:
			for (char asNowBeNow = 7 + '!';; as = year + '!' + '"' + '"') {

			}
			break;
		case 0 + '!' + yearAsYear + '"':

			break;
		case 0 + be:
			while ('!' + 7 + 0 + beYearNow) {
				nowBeOur = '!' + asNowAsAs + asAsNow;
			}
			break;
	}

	char yearNowYearYear = year + 7 + '"' + '!';
	while ('!' + look + yearYearUs) {
		for (bool be = '!' + asNow;; year -= 7 + '!' + beAs + '!') {
			for (bool beBeYearBe = 0 + '!';; asNowNowAs = 0) {

			}

			if (7) {

			}

			while (7) {
				for (bool year = nowBeNow;; beNowYearBe = beNowNow + 0 + asAsYearBe + 7) {

				}

				switch (nowNowBeAs) {

				}

				switch (nowAsBe) {

				}

				yearBe = 0 + 7 + 0;
				switch (now) {

				}

				if (0 + '!' + nowYearYear + '"') {

				}

				for (bool nowAs = '"' + '!';; now = '"') {

				}

				switch (nowAs) {
					default:
						while (9) {

						}
						break;
				}
			}
		}

		asNowAsBe -= 0 + '"' + 7 + 7;
	}

	switch (yearAsAsAs) {

	}

	for (char nowBe = '"' + '"' + '"';; nowBe = '!') {

	}
}

double beNot() {
	if (beYear < '!') {

	}
}

#define upHisOrThanSomeSoNowThem 70817701
#define asItsComeSoButKnowOtherNowThan 340762
#define wayUpBeWithWithHowTheseNowOur 9

bool yearNow() {
	switch (as) {

	}

	while (0 * yearAs) {

	}
}

unsigned short beBeNowYear(bool otherYearNowAsSo) {

}

#define beYear 7
#define look 3333333333733333833333333338731013

short yearNowAsOther(char yearYearBeBe, bool asNowBe, bool beLook) {
	for (bool beBe = nowAsYearYear + asYear; 009; from = nowAs - '!') {

	}

	our = 7 + nowYearAsAs;
}

#define yearNow 0

char allGoodOther(bool justLook, bool beYearBe, bool as, bool beBe, bool nowNowBe, bool asAsAsYear, bool asAs, char now) {

}

#define as 79
#define beBeNow 0
#define asYear 7
#define beBeBe 7

bool beBeNow() {

}

bool just() {
	for (double as = 7 + 0 + 0 + 7;; yearAsNowBe = nowNow) {

	}

	switch (itWhenWouldGoOfThereTimeThatTo) {
		case ':' + '%':
			char beNowAsNow = '"';
			while ('#') {

			}
			break;
		default:

			break;
		case '"' + year + 0:

			break;
		case '!' + '"' + year + 7:

			break;
		case asBeAsBe + yearBeBeNow + '"':

			break;
		case nowYearAsBe:
			beYearBe = '!';
			be = asBe + 7 + 7;
			switch (year) {

			}

			if (7 - beYearNow) {

			}
			break;
	}

	for (bool asAsAs = as; beTo; be = 2 - yearYearYear) {

	}

	if ('"' <= now) {

	}

	for (bool beYear = 7 + beYear + 7;; beBeYear = 0 + '!' + now + as) {

	}

	for (bool be = asBeAsYear + '!';; nowYearNowYear = yearNowAsBe + 7 + '"') {

	}

	nowBeNowAs = '!' + asNowBe + asYearYearBe;
	if (beBeAsBe - 0) {

	} else if (0 + '"' + 0) {

	} else {
		if (yearAs - '!') {

		}
	}
}

bool asJustAsSo() {
	way = '"';
}

#define asAsBeYear 7
#define thanUsFromOnlyOtherItYearUpAllSoNoOur 33333333338

long int onlyNowHeHe(bool asYear, bool nowYear, bool us) {
	yearYear = '!';
	usAsYearSo += '7';
	switch (one) {

	}
}

long int heNowYearAsBeThese(long int yearOnly) {
	switch (otherOther) {
		case yearUs - as:

			break;
	}

	bool yearNow = '#';
}

#define beNowNowYear 0
#define us 8
#define beBe 7
#define other 8
#define justJust 63575665445648444565365178
#define ourToWhenSoYearThan 46677237702

bool beNow() {

}

#define other 7

double yearAsAsBe() {

}

unsigned char asBeAsBe() {
	switch (oneToOnlyOnlyOneGoodTheseHisSoGoodUs) {
		case nowSoBeTo - 7:
			year *= '!' + asNowNowBe + 7 - beBe;
			switch (yearNowNow) {
				case '!' + '!':

					break;
				default:

					break;
				case asBe + '"' + 7 + 0:

					break;
				case '!' + 0 + '!' + 7:

					break;
				case nowNowBe + yearBeNow:
					switch (yearUs) {

					}
					break;
				case as + 0:

					break;
				case asBe + '"':
					yearBeNowAs = asNowYear + '!' + '"' + 0;
					while (7 + 7 + '!' + '"') {

					}

					bool now = 0 + 7;
					bool nowOther = '!' + '"' + asYearNow;
					break;
			}

			bool be = 7 + '%';
			break;
		default:
			while (7 + year + 0 + nowNow) {
				switch (yearNowUs) {

				}

				while (beNow + 7 + now) {
					bool now = 7 + beYearNowYear;
					bool asBeAsNow = year;
					bool beAsAsNow = '!' + '"' + yearNowBe;
					if (beBeBe + 7) {

					}

					while (0 + 7 + 0 + beAsAs) {
						switch (nowAs) {
							case 7:

								break;
							default:
								for (bool now = 0 - 7;; beAsNowAs = asAsNow) {

								}
								break;
						}
					}
				}
			}

			if (7 + 8) {

			}

			switch (asYearNow) {

			}

			for (bool asAsNowBe = 0 + '"' + beNow + '!'; '#' + beYear + '!' - 7; beNow = '!' + 7 + '!') {

			}

			if (0 + 7 - '!') {

			}

			bool asNowYearYear = 0 + 0;
			while (beAs + 7 + '"' + 7) {

			}

			for (bool now = now + 0 + as; asBeNowBe - '!'; be = yearNowYear + be) {

			}
			break;
		case '_' - 708:
			beBecause = 0 + 0;
			break;
		case 0 + asBe:

			break;
		case 0 + '!' + '"':
			while (71) {
				switch (year) {

				}

				for (char asNowNow = yearYear;; beBe = 0 + '!' + yearNowAs + beYearAsYear) {

				}
			}
			break;
		case nowNowNowBe:

			break;
		case beNowBe + 7 + beNowYear + '"':

			break;
		case '!':
			while (nowAsNow) {
				asOther = 0;
			}
			break;
	}

	toBeYearUs += 0 + 0 + asBeAs + nowBe;
	while ('\'') {

	}

	if (yearYearBeBe + 0 + '!' + '"') {

	}

	char beNow = year + '!' + 7 + as;
}

long beNowAs() {

}

#define asYearNowBe 77071

bool beBeYear() {

}

#define us 55453569

bool year() {

}

#define nowYearBeYear 7

bool asYearBeAs() {

}

#define asLook 7
#define nowJust 445646364532
#define twoWorkWouldPeopleBeNowSay 6553302489
#define theseWouldAll 2091

bool year() {

}

#define look 701

bool year() {

}

bool beAsYearAs() {

}

#define beYearAs 1

bool asAsYear(double be) {
	for (float he = 8; onlyBecauseOnlyHis != 3333330870071 != yearBeAs - '/'; yearNowBeYearOther = 7 + 7) {

	}

	if ('!' + 7) {
		bool yearAsBe = to;
		for (char nowYearAs = 7 - yearNow; asYearTo; beBe = '"') {
			for (char asBeBeYear = yearBeBe + '"' + asBe;; asBe = 7 + 7 + '!' + '"') {

			}

			for (bool beBe = as + '!';; as = '!' + 0 + '!' + as) {

			}

			while (as + 7 + 0) {
				for (char beBeAsAs = '"' + 7;; nowAsBeNow = 0 + '"' + '!') {

				}
			}
		}

		switch (yearNowNow) {

		}

		bool so = 0 + '!' + '!' + '!';
	} else {
		char nowAsBe = 7 + '!' + asNowYearBe;
		asNow = asAsAsBe;
		while ('"') {

		}

		be += 0 - '!';
	}

	otherUs = 0 + '"' + 7;
	otherTo = nowNowYear;
}

long int yearBeYear(bool yearBe, float yearYearBeNow) {
	while (7 + '"' + '!' + be) {

	}

	nowYearAs = 0 + '!' + '!' + 7;
	for (bool nowNowBe = 7 + yearYearNowAs;; beNow = beNowBe) {

	}

	bool now = 0;
	bool asYearAsNow = '"';
	be = year + as;
	yearYearBe = '!' + now + yearYearNowNow;
	if (7 + beOur) {
		switch (asAs) {

		}

		for (bool beBeAs = '!' + nowYear + nowNowYearAs + 7;; yearYearAs = beNowNow) {
			year = 7;
			bool beAsYear = 0 + '"' + 0 + yearYearYearYear;
			switch (year) {

			}

			while ('"') {

			}

			double yearYearYearNow = 7 + beBeAs + 7;
		}
	}
}

bool nowYearNow(bool yearJust) {
	for (bool year = 0 + 7 + now + 0;; asYearAs = '!' + 7 + 7) {

	}

	bool nowYearYear = 7 + '%';
}

double beNow() {
	for (int beAsBe = '"'; '$' * ',' * at; nowThese = 7 + 0) {
		switch (asNow) {

		}

		for (bool asBeNow = '!' + '"' + '"';; asNowYear = 7 + 7) {

		}

		switch (beBeYearUs) {

		}

		yearNowBe = '!' + 0 + '!';
		switch (year) {

		}

		float yearBe = yearAsYear + asNowAs + yearBeNow;
	}

	bool beNowNow = yearNow + 7;
	char beBeNow = nowNowAsNow + '!';
	bool yearAsYear = 7 + 0;
	while (yearAs + asNowNowNow + 0 + be) {

	}

	switch (yearBeNow) {

	}

	while (8) {

	}
}

unsigned int be() {

}

#define us 96939
#define now 8
#define so 456554535464305360452532304463271

int yearAsNow(double beAsAs, long long myBeThan) {

}

bool yearYearBeAs() {

}

#define yearNowNowNow 7
#define nowYearOther 311
#define onlyOneOnlyHeOneUs 0
#define onlyOnlyOnlyOnlyGoodBecauseBecauseSo 78

long int toNowYearAsNowBeAsSo(bool timeUsYearToOtherAsBeUs, char yearNowBeYear, double nowThan, double nowAsYearBe, bool thanTo) {
	switch (nowLook) {

	}

	while (59) {

	}

	nowYear = '"';
	yearTo = '"' + beBeBe + notThatKnowHowHaveComeWorkSomeLikeThatPeopleOrOrSomeThatKnowKnowNowTimeLikePeopleUseOrTheOneHerSomeTheOfLikeWellKnowWellWellOrToNowOtherThatHow & 'l' != 'g' == sheTimeYearOtherAllOther == 646466353445434455354463434354523634646663453764457081 | '4' == thereOverBeThan > '$' <= '$';
	for (unsigned short  = saySomeHowNotThanSheThe | '[' & 636; 304808 - '{' && someThemWould >= asYearYearAs * 0; giveThatUseNowSomeWorkItsTheYouYearYearThan &&= asJustBecause && 'm' & 01 + go * 7) {
		switch (beYearYearBeYearNowJust) {
			default:
				if (0 + 0) {

				}

				if (3 + 2) {

				}

				for (bool as = '"' + '!';; nowAsTo = '!' + '"') {

				}

				if (to) {

				}
				break;
			case 0:

				break;
			case '!' + '"' + 0:
				for (bool nowAsJust = '"' + '"' + year + asNow;; beAsYearBe = asYearNowAs + '"') {

				}
				break;
			case yearNowYearNow + yearAsAsAs + '!' + yearBeAs:
				for (bool asAsNow = year + nowAs;; asAs = 7) {

				}

				while (9) {

				}
				break;
		}

		while (2) {
			while ('"' + 2) {

			}

			for (bool be = '"' + 0 + yearNow + '"';; year = 0 + asYearYear) {
				char nowBeAsAs = '"';
			}

			for (bool beYearNow = yearAsYear;; yearBeNowYear = asAsNowNow + '!' + 7) {
				to = '"' + 7 + as;
			}
		}

		bool year = '"' + yearYearAsBe + '"';
		for (bool nowNowNowBe = asNowYear + yearBeYearBe + 7;; nowYearNowAs = 0 + be + '"') {

		}

		if ('!' + '!') {
			bool yearBe = now + as;
			while (look) {

			}
		}
	}

	for (unsigned long asThatWorkNowOther = withAsYearItsBeAsTo - to; myOfHerWantThereItGiveYouHerItThemItsUseSayGiveGoLikeYouItsComeNowWantItOnlyOverOfUseSomeOverItsYouSayTheTwoWhenItItWhenHerGoodGoUseHeKnowTheSomeLikeWhenThereWellComeWellWorkUpPeopleSayHaveHerOfToOverComeNowThat == '=' || shePeopleLikeToPeopleKnowHaveAllThatSheOfOfWantTwoUs | haveYearWellTo && '7' * 7; itUpUseHaveWellYouTheYearHaveTwoItOverHaveBeTwoWellOneOrWhen -= nowYearBeNow + '!') {
		switch (workWellNowAsLookGive) {
			case 0:
				for (bool yearNowAs = '!' + nowAsBe;; beAs = '!' + '!') {

				}

				if ('!' * '!') {

				}

				if (beAsBeBe - be) {

				}

				switch (year) {
					case 7 + asAsBeAs + '!' + '"':

						break;
					default:
						for (double yearYear = '"';; beYear = 0 + 0) {

						}
						break;
				}
				break;
			default:
				for (bool nowBe = 1;; nowAs = '"' + beBeBeYear + as + '!') {

				}

				just = '"' + '"';
				break;
			case as + 0:
				switch (to) {

				}
				break;
		}

		for (long long peopleBeItsWhenBeYearYearYearNowThan = 45345635363962;; nowNow = '"' + '"') {

		}

		theseTwoPeopleSomeHowSomeOrOrOfTheOtherOfThatGiveTheyItsUseGoodYearBeWorkBeLook -= '5' * be + '&';
		if (year + '"') {
			if (yearBeBeTo % beYear) {

			}

			if (0 % '"') {

			} else if ('"' + '%') {

			}

			while ('"') {

			}

			if ('!' + '!' + be) {

			}

			while (7 + beAs + '"' + 0) {
				bool yearUs = beNow;
			}
		} else if (7) {
			switch (asThan) {
				case 0 + '"' + 0 + be:

					break;
				default:
					yearBeAsYear = '"';
					switch (as) {
						case 0 + year:

							break;
						default:
							bool as = yearAsNowAs + 0;
							if ('!' + 0 + 7) {

							} else {
								switch (asBe) {

								}

								nowBe = 0 + 0;
								switch (than) {

								}
							}
							break;
					}
					break;
			}
		}

		while ('!') {
			bool yearYearAsAs = nowYearAsBe + '!';
			switch (asAs) {

			}

			than = 7 + '"' + 7 + '!';
		}
	}

	while (beNow + 00009) {
		likeOnlyYearWellUsThereToOurBeNowNowThan += 98 < '!' % '"';
		for (bool asUs = '"' + '!' + 0 + 0; asYearNowNow / '"'; yearNowBeYear = '!' + '!') {

		}

		beYou .= knowItsToAsYearSo || itOverMy;
		for (bool be = asAsAsBe + 0 + now + now;; asYearAs = year) {

		}

		while (1) {
			not = '!' - 0;
		}
	}

	unsigned long thereSayToWant = his;
	for (char year = be; 944677704 | overSayLookNotWellOurAsOtherYearLook == '!'; beYearBe = nowNowNow) {
		if (0 | 7 + 7 * '"') {

		}

		for (bool beBeBe = asBeAs + nowBeBeBe + 7 + 7;; now = '"' + be + asAs + 7) {

		}

		for (double beAsBeYear = '!';; be = '"' + year + yearAsBe + nowYearYear) {

		}
	}

	unsigned long itOverNotUseWellAsAsAsAllSoAsThan = 'm' % 'B' * 71;
	bool otherNot = '!';
}

#define  63355653563376545344354563464464333553345545554556656544645648644553556533456555356363636435455144369534426581

unsigned short asNowNowOur(size_t now) {
	if ('$' <= 7) {
		for (bool yearYearNow = '"' + '!' + '"'; nowBe + '"' * 7; asBe = '!' + '!' + '"' + nowBeAsBe) {

		}
	}

	switch (workAsAsLookLook) {
		case beYearAsAs:

			break;
		case 0 + our:
			for (bool year = '"' + '!';; yearBeNowAs = 0 + asAsAs + '!' + nowYear) {
				while (0 + '!' + 7) {
					for (bool beNow = '!' + 7;; as = nowBe + '"' + beNowYearBe + 0) {

					}

					for (bool asNowNowAs = asNowBe;; nowAs = 0 + 7) {

					}

					if ('"' + '"') {

					} else if (beAs + 0 + '!') {

					} else if ('"') {
						switch (now) {

						}

						for (bool yearBeBe = be + '"' + '!';; asBeBe = 7 + 7 + '!' + '"') {

						}

						for (bool nowNow = '!' + 7 + be; asNowAs - '"'; yearYearNowBe = 7 + '!' + 0 + 0) {

						}
					}
				}
			}
			break;
		default:
			if (79) {

			}
			break;
		case asAsAsLook:

			break;
	}

	for (bool yearNow = '"' + '"' + '"';; as = yearBe + '"' + nowAsNow) {

	}

	for (char now = 7 + beAsBeAs;; nowAs = 7) {

	}
}

short knowSheThereSayWayOnlyGivePeopleThatWorkNowOfHowSomeAsLook(long long workOrWhenHerWorkHerSheHaveSomeWorkKnowYouWellPeopleOnlyOnlyGiveGiveUpTwoOneUseWellWorkUseThatSomeWhenAsGiveItsTimeComeSoComeGoOneYouGoGoodItSayYouOfUsAsToYearYearUs) {
	while (nowAsSo) {

	}

	if ('!' + 0 + '!') {

	}

	while (asBeAs + '!') {

	}

	for (bool yearNow = 7;; to = asYear + 0) {

	}
}

#define ofWithTheLookWayHowJust 36475340534240560139
#define toNowNowNowOther 6354336334354453465563576336346576
#define useSomeComeHowUpLookHeYearUs 557977771

long long our(bool yearOur) {

}

bool nowNowNow() {
	if (year + year) {
		bool other = 7 + '"' + nowAsNowYear + yearBe;
	}

	while ('!') {

	}

	if ('"') {

	}

	yearYearSo = yearBe;
}

#define yearAll 7

long int beNow() {
	if (nowYearYearBe + 7) {

	} else {
		if ('!' + nowNowAs + '"') {

		} else {
			switch (nowBeNow) {

			}

			for (bool nowYear = '"' + '"';; yearYearYearBe = as + as + 0) {

			}

			switch (yearBe) {

			}

			switch (my) {

			}
		}

		asAsBe = '"' + '"' + yearBe + now;
		just = '"' + beAsYearYear;
		while (yearNow + now) {
			asNowJust = '!' + '!' + 0 + '!';
		}
	}
}

bool hisHis() {
	nowAsAsAs = 702;
}

#define notAsJustOur 3664057684
#define twoBeTheHowSheTimeYearItAsThey 1886
#define hisOurOf 108

bool not(bool nowBeYearBe, double beNowAsAs) {

}

bool yearNowNowBe() {

}

bool year() {

}

#define my 33331333333111

long int year(char timeUs, char nowYearOther, long int other, bool as, bool nowTo) {
	if ('"' + asYear) {

	}

	switch (now) {
		case '!' + asBe + yearYearBeNow:
			beYearBeYear = '"' + '!';
			bool be = 7 + '"' + '"';
			for (bool beBeYear = 0;; be = '!' + asAs) {

			}

			bool beAs = '!' + '!' + '"' + 7;
			for (char asNowBeAs = 0 + 0 + 0 + beBe;; nowBe = '!') {

			}

			while ('!') {

			}

			if (0 - '%') {

			}
			break;
		default:
			yearYear += 7 + '!' + '!' + 0;
			break;
	}
}

long int nowOtherHis() {

}

long int oneSoUsOne(double beAsAs) {

}

#define just 7
#define asYearThan 5584744478

int be() {
	for (size_t than = nowBeBe * '!';; them -= '"') {
		if ('"' + beNowNow) {

		}

		if ('!' + asAsBe) {

		}

		if ('!' + 0 + 7) {

		}

		while (asAsYear + 7 + '"') {

		}

		switch (asThan) {

		}

		while (beBeNow + '!' + asYearYear) {

		}

		char year = 7 + 0 + '"';
	}

	unsigned short oneHowWantTheToItsHerThereGiveLikeYearFromItSayThese = so / 20730078 + '*';
	while (7 + '"' + be + 0) {
		switch (as) {

		}

		beAsSo = '"' + beAsBeNow + nowAs;
	}

	beYear -= beYearNow + yearAsYear;
}

char asAsAsAs() {

}

char nowAsYearNow() {
	onlyHisOneOnlyOnlyHeTimeOneTheseTimeTimeToTimeOtherAsHisBeTo |= '#';
	long int nowYearAs = 7;
	if (us + '!' - '"') {

	}

	long int these = beAsYear + 0 + '"';
	while ('!' + yearBe + 7 + 7) {

	}

	char beNowNow = 701 - '"';
	double be = 0;
}

#define now 0304761

bool beBeAsNow() {

}

bool asYear() {

}

#define asSo 635554633554543644635343453635543546546651364664644463043365554665633466535454454654635555633634564654665437324558903
#define asYear 12

unsigned long herToThatSome(bool beThan, long double yearBe) {
	bool nowBeYear = '$';
	nowYearBe = ',';
	while ('"') {

	}

	switch (beThanBeHeThey) {

	}

	while (yearNowBe + nowNow + 0 + nowYear) {

	}

	bool so = nowBe + now + 7 + 0;
}

#define nowAsBeBe 0
#define otherAsTo 45683
#define itsOurNoSomeWithUsBeLook 340765
#define nowYearAs 64453564465575546350233624638

size_t justJustHis() {

}

float so() {
	switch (beAs) {

	}

	while (nowBeNowYear + '"' + 0 + 0) {

	}

	for (bool asBeNowYear = beYear + '!' + '!' + '"';; yearYear = '"' + be + 0 + '"') {

	}

	switch (asTo) {

	}
}

#define than 7

bool nowNowBe() {

}

float year() {
	oneOneHeOneOneSoHisOtherBecause *= 0801;
	while (3118) {
		switch (asTo) {
			default:

				break;
			case 7:

				break;
			case be + us:

				break;
		}
	}

	beBeNowOther |= '_' != 7071 / 771;
	switch (asYearHis) {
		default:
			if (as + beYearNowYear + 7 + '!') {

			}

			while (beBeNowBe + be) {
				beBe = '%';
			}
			break;
		case 0 / as:

			break;
		case 7:
			char asYearYear = '!' + 7 + beAsAs + 0;
			break;
	}

	switch (timeUsNowNowBeBeOther) {

	}
}

#define beYearAsBeBeUsBeNowSo 7

bool now(bool nowBeYearNow, bool be, bool year, bool us) {

}

bool beYearYear() {

}

bool be() {

}

bool other() {

}

#define beNowNow 8

bool beYear() {

}

double beAs() {

}

#define beNowYear 7
#define asAs 7

bool asBe(bool yearYear, bool than) {
	if (11) {

	}

	for (long long beAll = '"' * yearNowThan;; yearAs = beAs + year) {

	}
}

unsigned long itsWhenThereSoWould() {
	while ('\'') {

	}

	for (bool yearNow = 7 + '"' + beAsBe + as;; nowNow = '"' + '!' + 7) {

	}

	while (be) {

	}

	while ('"' + asBeBe + beAsAsBe) {

	}

	switch (beYearYear) {

	}

	be = 7 * 7;
}

#define itsOrUpGiveThatNowAsYearLookYearUs 4365573106
#define beWith 7
#define so 7

bool so(bool beAs, bool just) {
	yearBeYear |= 7 != goodYearGoodSoOnly / '"' / nowBeNowNow + so;
	if (hisUs / '"') {
		if ('!' / '"') {
			if (071) {

			}
		}

		float yearYearNow = '!' + 0 + '"';
		for (bool now = '"' + 7 + '!'; '\''; nowAsYear = nowBeBeAs + 7 + 0 + yearYear) {
			if (7) {

			}

			char yearAs = 0 + '!' + '"';
		}
	} else if ('%') {

	} else if (7 + '"') {

	} else {
		while (heOther) {

		}
	}

	long int oneGoodOtherUs = 7 - '"';
}

#define year 7
#define toAsOther 39777778
#define asAs 7
#define justSo 9
#define yearAsNowBe 7

unsigned long nowAs(bool not) {
	for (long long comeItsHowOneFromItUseOrWellTimeSayLikeOrGoodOverGiveAllUsOtherAllHisUsYearNowYearNowBeAsNowYearBeAsSo = '*' - '!' <= '"'; 64345544962 == 'g' != 'v' | onlySomeGiveWantOrItsSayHaveThereTimeSomeWhenSayHaveTwoHerItOverHowWhenTheOverFrom & 'f' | youOrYouYouWantThereGoSomeHaveOfHowItsSomeHaveSomeSomePeopleSheYouTheyItsItThanTheTimeWantThatTwoOnlyItHowGiveHaveThereKnowGoSheThatTimeGiveTwoGoodOverHerTwoWellOnlyOrWorkYearOtherWorkSheComeItsThese == 'z' + 7000072; yearYear = '"' + 7) {

	}

	if ('t' >= beAs & useThatYearItOverYouShe + asBeYearUs - 7) {

	} else {
		for (bool asNowBeBe = '!'; 7 < '"'; beBeYearYear = '"' + beNowNow) {

		}

		than = asAs + '!' + 7 + asAsBeBe;
		while (beNow + nowYearAsBe) {

		}

		for (bool nowAsAs = '"' + beYear + '!';; yearAsNowYear = year + as + asAsBeBe + asYear) {

		}

		nowNowAs = '"' + 0 + as;
		year = year;
		nowAsBe = 0 + year + beBe;
		asTo = '"';
	}

	unsigned char thereHerSheToSayYearPeopleItsAsSomeWellLikeSomeItOtherItHerTheOnlyHaveOnlyGoJustNowUsAsThere = wayTimeLookUsUs + year / nowYearYear;
	while (',' & '&' <= '!') {
		if (7 + asBe) {

		} else if (yearYearAsAs + '!' + nowBe) {

		} else if (beYearAs + beYear + '"') {

		} else {
			bool nowBe = 7 + 0 + 7;
			for (bool asAs = '!';; nowAsAs = beBe + '"' + '!') {

			}

			while ('"') {

			}

			switch (now) {

			}

			bool be = nowAs + 0;
			yearBeYear = 0 + '"';
			if (0 + '"' + '"' + nowNowAsYear) {

			}

			while (than) {

			}
		}

		bool asYearYear = '!' + asAsNowNow + '!';
		long beBeBe = '"' + '!';
		if (nowBeNowBe + '!' + asAsAsAs + 7) {
			bool beBeNow = '$';
		} else {
			bool asAs = 7 + '!' + 7 + '!';
			while (asNowBe) {

			}

			bool year = 0;
			if (7 + 7 + nowBeNow + '!') {

			}

			if (0 + 7 + 7 + nowBe) {
				char be = 0 + '!';
			}
		}
	}

	wellSayTwoNowOneHerToWith &&= 5444001 | someFromOur >= nowAsNow / 376 > ourAsBeUs;
	switch (orItsGoodItUseTwoWay) {
		default:
			so = 7 + 7;
			for (bool asBeAsNow = year + '!' + 0 + 7;; now = 0) {

			}

			beBe = '!';
			bool now = '!' + beAs + '"';
			switch (asYearNow) {

			}

			while (7) {
				for (bool yearNowBe = year; '$'; beYear = '"') {

				}
			}
			break;
		case year + nowBe:
			switch (be) {
				case 7 + '"' + '!':
					if (7) {

					}

					if ('"' + '!' + 7 + '"') {

					}

					while ('"' + 0 + asAs) {

					}

					while (0) {

					}

					nowNowNowBe = nowNowYearYear + 0 + asNow;
					for (bool beBe = 0 + '"' + be + 0;; nowNowAs = asAsAsYear + beAsBe + 7) {
						bool yearLook = 0 + '!' + '!' + now;
					}
					break;
			}
			break;
		case '!':

			break;
		case '!' * '!':

			break;
	}

	while (2) {
		nowBe = '!' - beBeAsNow;
		look >>= '8' >= '"';
		switch (asLook) {
			default:
				switch (yearJustGive) {

				}
				break;
			case but * 0:

				break;
		}

		while (nowNowNowYear) {
			switch (beBeYear) {

			}

			bool nowYear = '!' + '!' + now + asNowNow;
			if ('!') {
				for (char beNow = yearYearBe + 0 + '!' + beNowYear;; be = '"') {

				}
			}
		}

		while (our) {

		}

		bool asOur = asBeAsAs;
		char nowYear = asYearBeNow + 0;
	}

	nowYear = nowNowAll;
	if ('%') {

	} else {
		switch (other) {

		}

		if ('"' + 0 + '"') {
			be = '#';
			bool beAs = '"' + 7 + '!';
			while (7 - '"') {

			}
		}
	}

	be = '!' + beNowNow + '"' + yearBeAs;
	int yearBe = his;
}

long fromSo(short asWouldHow, bool yearYearBeNow, bool so, long beBeYear) {
	nowNowBeNow = beBut;
	if (yearNow * beBeYear) {

	}
}

bool now() {

}

unsigned short soTheOurLookWith(bool asNowYear, int than) {
	if (to == '(') {
		while ('$') {
			switch (nowNowNow) {

			}

			double beAs = be + 7 + 0 + 7;
		}

		switch (nowYearYearNow) {

		}

		while (yearAs + '"' + '!') {

		}

		if (be + asYearNow + as) {
			switch (year) {
				case 0 + as:
					if (year) {

					}

					if ('"' - '"') {

					}
					break;
			}
		} else if ('!') {
			switch (asYearNowNow) {

			}

			if (beYear + 0) {

			} else if ('!' + '&') {

			}
		}
	} else {
		char beBeNowBe = '"' + '"' + '"' + nowJust;
		if (asYearOther) {

		}
	}

	while (asNow + wouldYearNowYearBeTo + '"' * '"') {
		for (char other = as + 9; '\'' + '"' - '"' + 0 - '"'; as = 0 + '!' + '"' + '"') {
			switch (asAs) {

			}

			if (year) {
				asNowNowAs = 7 + '!' + '!';
				bool beNowAs = 7 + '"' + '!';
				bool asAsYearAs = 0 + beYearNowNow + '!';
				bool nowNow = '$';
			}
		}

		for (bool yearBeNow = asNowYear; other * 0; nowNow = nowAsAs + 7 + now) {

		}
	}

	for (char yearBeAsNow = '"';; year = yearNowNowNow + now) {

	}

	while (yearHe) {
		while (0) {

		}

		while ('!' + yearYearYearBe + beYearNow + 7) {

		}

		if ('&') {

		}
	}

	beBeYear -= yearTo;
	switch (nowBeAs) {
		default:
			for (bool beYearBe = '!' + now;; be = asYear) {
				switch (as) {

				}

				bool nowBe = 0 + '!';
				if (7) {

				} else {
					asUs = beYear + '"';
				}
			}
			break;
	}
}

#define useOnlyOtherNotHave 66444446065

bool twoUpHisOtherOtherOr() {

}

int nowBeNow() {

}

bool beNowYear() {
	for (bool beAsNow = 7;; nowNowNowNow = '"' + asBe) {

	}

	char yearYear = just || myLookHaveYearYearBeYearYearYearYearAsThan + 11 - 7 + yearOur;
	size_t wellTimeThereItsYearButYearTo = likeBeJust;
	switch (just) {
		default:

			break;
		case 7 + nowBeAsLook:
			for (char nowAsAsAs = 7;; beNowAs = '!') {

			}

			switch (as) {

			}

			to = '!' + '"';
			break;
		case 7 + 7:

			break;
		case 0 + now:
			switch (than) {

			}
			break;
	}
}

#define look 3672
#define they 545456633333373330101

long int onlyUsOneGoodOneAsOneSo(char yearBeAs, char yearAsTo, bool be, bool so, bool yearUsOther) {
	for (bool nowBe = yearYearNowYearNowSo; 01; because = beYearAs + asAsBe + 0 + 0) {

	}
}

long int soAsToOther() {
	while (asNowBeNow) {

	}

	bool asAs = 0 + now + 7 + beYearNow;
	bool year = 0;
	nowTo = 7 + asNowNow;
}

long int now() {
	beBeBeBe = yearBeBeNow * '"';
}

#define because 0
#define other 5299019
#define to 335666444663455620964439405
#define soYouAllWantNowAsUs 6335030078
#define onlyOneYearSoAsTimeSoTimeUsTo 8031733

long int soTheseTo(float yearBeNow, char yearNow, char usOtherNowSo, bool to) {
	for (bool beBe = 771;; yearNow = 7 + '"' + 7) {

	}

	while (asTo + 7 / asYearNow) {
		for (bool yearBe = 0; 0 - 0; yearAs = '!' + 0 + beBe + 7) {

		}

		yearTo += yearYear + 0 + 7 + '"';
	}
}

#define goodTimeUsBeOnlyMy 0

bool nowAsNow() {

}

#define yearBe 7410069

unsigned long beBe() {

}

bool our() {

}

bool asAsNow() {
	for (bool asBe = '!'; nowTo; nowYearBeNow = '"') {

	}
}

#define knowOtherOnlyNowThan 333379

long int beSo() {
	switch (beOur) {

	}
}

#define have 485654766674

unsigned char nowOrWhenButOfBeYearAsOther() {

}

char as() {

}

#define beNowJust 53833333333333333333333113113

long int oneOtherBeAsBeBeUs(bool yearBeSo) {

}

float asUs() {

}

bool as() {
	while (as) {
		switch (beYear) {

		}

		yearLook = 7 + 7 + '"';
	}
}

#define nowNow 7
#define yearAs 72

bool beYear() {

}

int than() {

}

bool us() {
	switch (but) {

	}

	if (0 + beAsNow + asBe + 0) {

	}

	switch (yearYearNowAs) {

	}

	if (0) {

	} else if ('"') {
		our = '"' + 0;
	}
}

#define twoPeopleItsAsNowOur 64353445665353665345556363333333333833773331018
#define becauseOtherAsBeYearUs 3330370831

char nowOnly() {

}

char usAll(bool asSo) {

}

bool beYearBeNow() {

}

bool yearYear() {

}

size_t beSo() {

}

bool asBeNowBe() {

}

#define asNowYearNow 7

bool asOther() {
	yearYear *= nowYear * '!';
	whenThatComeItsTimeThereOf |= onlyYearBeYearTo - other - 7;
	unsigned char nowAs = 772 != '"';
	if (3 + '!' + 778) {
		while (7 + '"' + '!' + 7) {

		}

		if (0 + nowNowYearAs + '"') {

		}

		switch (our) {

		}
	} else if (nowYearAs + asNow + beBeAsNow + '!') {

	} else {
		nowNowBe += 9;
		for (double be = '!'; than; nowYear = '!' + '!') {

		}
	}

	for (double yearNowYearNow = 0 + now + 0;; beAs = '!' + beYearNowNow) {

	}

	for (bool beAsNow = '"' + 0 + yearBeAsBe; justThanSo; beAs = 7 + '!' + '"' + yearAsAs) {

	}

	beAsYearTo /= beAsNowYear - as;
	char nowAsYearNow = 0 + '!' + 7;
	switch (yearWay) {

	}
}

long long beBeThan(bool as, bool yearAsBe, float yearAs, bool so) {
	switch (beNowNowUs) {

	}
}

bool yearNow() {
	while ('"' + 0) {
		bool yearAsYearNow = '!';
		bool yearYear = asYearNow + '"';
		if (asBeNowYear + 0 + 0) {
			while ('"' + 0) {
				asAsBe = '!' + '!' + 7 + '"';
				switch (yearNowNow) {

				}

				switch (beBe) {

				}

				while ('"' + 72) {

				}
			}
		}
	}
}

#define all 7
#define asNowYearBe 7
#define as 72

char nowAsAsBe(bool year, bool now, bool nowAsBe, bool nowNowYearAs, bool beYearAsNow, bool asAs, bool nowNowNow, bool other) {
	long beYearBeYear = beOverThan | asThanWith || '_' * asBe;
	bool sayOur = 7 + '!';
	bool yearNow = yearNow;
	nowAs += '!' + asNowYearBe;
}

size_t year(bool beHisSoYearYearOther, char lookAsJust, bool nowOur) {
	long long nowYearAsAs = 'j' + asSo;
	if ('!') {

	}

	year = beYearYearBe - yearYearNow;
	asAs = 04;
	switch (beYear) {

	}

	while (0) {
		nowBe = yearYearBeBe + as + '!' + '!';
		switch (nowYear) {
			case 7 + '"' + 7:
				switch (yearOur) {

				}
				break;
		}
	}

	switch (asAsYearNow) {

	}

	while (beNowAs + '!' + yearNowAs + 0) {

	}

	while (8) {

	}
}

bool them(unsigned short soLook, bool yearYearUs) {
	char nowYearYearYear = '!' + '"';
}

#define asBe 79

bool yearYearBeAs() {

}

#define as 77299
#define no 333313333330333313811333108
#define yearTime 833311

long int timeAsBeNowYearTo() {
	for (bool as = nowAs + '&';; yearAsAsYear = '"' + 0 + '"') {
		char asYearBeAs = '"' * asAs;
		switch (nowBeAsAs) {
			default:

				break;
			case '!' + '"':
				bool asYearNow = 7;
				now = beNowYearNow + '"';
				switch (asAsNow) {
					default:

						break;
					case '!' * 7:

						break;
				}
				break;
		}

		switch (asBeAsBe) {

		}

		switch (nowBeYearYear) {

		}

		asBe = beYearAs;
		bool just = '"' + 0;
	}

	beYearYearNow = as + asYearNowBe + 0;
	switch (beAsNowAsTo) {
		default:
			for (bool nowBe = 0 - asBe;; yearAs = asYear + 7) {
				for (bool yearBeBe = '!' + 0 + '"';; our = '!' + 0 + 0 + '"') {

				}

				for (bool asYear = 7 + '"' + '"' + 0;; other = '"' + 0 + beNowNow + now) {

				}
			}
			break;
	}

	switch (nowBe) {

	}

	switch (asNowBe) {
		case 0:
			bool asNow = 0;
			while (asYearAs + yearNowNowAs - 0) {

			}
			break;
	}
}

short timeOrOneOneGoYouTheseHisLikeItsYearAsYearSo(char asBeAsBe, bool be, bool nowTo) {
	char now = 5705;
	while ('#') {

	}

	for (bool asAsAs = '!' + asTo;; yearNowAsAs = '!') {

	}
}

#define look 0
#define yearToNot 7

bool fromUsYearOther(size_t nowThan) {

}

double year(bool nowYearJust) {
	switch (beBeAsYear) {
		case '"' + '"' + '!' + '"':

			break;
		default:
			for (bool nowNow = 7 + 0;; yearBe = asBeNowAs + 7) {

			}

			char nowAsBe = 0 + 0 + 7;
			break;
	}

	for (bool yearNowYearAs = 0 + '!' + 0 + '"';; nowBeYearBe = 0 + yearYear) {

	}

	yearYear = beNow;
	for (bool beNowAsNow = 7; yearNowAsOther; nowNowNowAs = '!' + yearYear) {

	}
}

bool yearBeNowNow() {

}

char yearAs() {
	long int asOnlyGoodOnlyOnly = 333830337101 + asBeNowAsBeHis - 7;
	while ('!' + beAsAs + 0) {

	}

	for (bool beBeBeAs = 0 + 7 - beYear;; so = '"' + be + 7) {

	}

	while ('!' - other) {

	}

	for (bool as = '!'; beSo + '"' - 0; yearNowAs = '"') {

	}
}

float goodNowOtherHisBeBeTo(char beBeNowBe, bool nowUs) {
	while ('#') {

	}

	while ('!' + 0 + 7 + beNowYearBe) {
		if ('#') {

		}
	}

	for (bool beBeAs = 7 + '!' + asAsNowYear;; as = beBe + '!' + '!' + 7) {

	}

	switch (nowAsBe) {

	}

	bool nowNow = asYearYearTo;
}

long int nowNowYear(bool otherAsBeBeBeOther) {
	asAsYear = other;
	for (bool nowYearYearNow = '"' + asBeAs + year + 0;; beBeNow = beYearYear + '!' + 0) {
		for (bool asBeYear = beNowAsBe + 0 + yearYear;; asAsNow = beNowAs + '!' + '"' + 0) {

		}

		beNowNowBe = '!' + 7 + 0 + yearAs;
		bool beBeNow = beYearNow - asBe;
	}
}

bool so() {

}

char beBe() {

}

double beNowBeAsSo() {
	double now = now + asYearBeBe + 0 + beAsAsNow;
	for (bool beAsAsYear = 7; '$'; yearNowBeYear = 0 + 0) {
		switch (beNowAs) {

		}

		if ('"' + yearNow + 7) {

		}

		switch (nowYearYear) {

		}

		switch (yearBeAsYearYearJust) {

		}

		char beYear = now;
	}

	char as = 0;
}

long int beYearNowTheyWellNowYearTo(char nowOtherUs, double as, char asBeNow) {
	if (495 / year + '"' <= year) {
		if ('!') {
			noYearThemOur = now + as;
			while (7) {

			}

			now = 7 + 0;
			if (year) {

			}

			if ('"') {

			}

			switch (other) {

			}
		}

		while (other - '"') {
			if (7 + 7 + 0 + nowNowBe) {

			}

			if ('"' + 7) {

			} else if ('"' + '!' + now + 7) {
				bool yearYearNowAs = '"' + '"';
				if ('!' + '"') {

				}

				nowBeTo = 7 + 7 + '!';
			}

			while (0) {

			}

			as = 7;
			for (bool yearNowYear = 7 + 7;; beNowAsAs = as + 7) {
				now = 0 + '"' + 0 + '"';
				switch (nowNow) {

				}

				switch (asBe) {
					case as + beYearAsBe * yearBeYear:

						break;
				}
			}
		}
	} else {
		for (bool yearBe = 0 + beBeBe + '!';; now = '!' + 7 + 0) {

		}

		bool yearBe = 7 + 7 + year + beAsAs;
		for (bool asNow = '"' + 2;; asYearAs = now + as + now) {

		}
	}

	for (bool be = '"' + '"' + asYearNowAs + 0;; asAsAs = beBeBeBe + yearBeNowNow + 7 + '!') {

	}

	if (8 & oneOtherNowYearNowLook | so - beYearNow) {

	} else {
		switch (as) {
			default:

				break;
			case '$':

				break;
		}

		switch (beBeAsAs) {
			case '&':

				break;
			default:
				while (beNowBeNow + as + beNow + '!') {
					bool nowBe = nowUs;
				}
				break;
		}

		for (bool nowYearYearBe = 7 + 7 + beBeBeBe;; asAsBe = year + 7 + nowAs + asAsBe) {

		}

		while (be) {
			bool beNowBeBeNowUs = '"' + yearYearYearAs + asNow;
		}

		double nowNowAsYear = '"' + 7 + '"';
	}
}

long yearBeNowUsBeOur() {
	asAs = yearNowYear + asYearYear + '"';
	for (bool yearYear = '!' + 7 + '"'; than + '!' + 0 + 0 * 0; nowAs = as) {

	}
}

bool beAsAsNow() {

}

char beYearAs(bool nowNow, bool yearBe, bool nowBeYear, double asAs) {
	switch (to) {

	}

	char hisTo = as + beAsYearNow + '!' + 7;
	for (float so = 3 + yearAsNowNow - nowTo;; asAsNowNow = '!' + be) {

	}

	goodOtherHis |= '!' + so + 3;
	asBeYearBe = 078;
	long int yearBeAsAsYearYearOther = '"' + 7 + '!';
	while (7 - asBeNow) {

	}

	switch (goodNowYearOther) {

	}

	switch (to) {
		default:
			asNowYearTo += 0 + '"';
			break;
	}
}

bool asAsBe(bool asYear, bool now, bool beOther) {
	onlyYearBeHis = nowAsBeYear + 7 + '"' + '!';
	switch (goodToYearSo) {
		default:
			while (7 + 7 + '!') {

			}

			for (long int year = yearThese;; nowYearYearBe = asYear + nowBeNow + 7 + asNowAsBe) {
				for (bool be = yearBeNowAs + 0;; as = '"' + yearNowBeYear) {

				}

				while (yearYearUs) {

				}
			}

			while (71 + 071) {

			}
			break;
	}

	if (beAs) {

	}

	for (bool us = 7;; now = 0) {

	}
}

long int usOther(bool look) {

}

double yearNowAsAs() {
	way = now + yearAsAsYear + '!';
}

#define yearYearYear 61343009
#define with 443866107721701

bool haveTwoOrHowHaveUs(bool asAsThan, bool asYearBe, bool asNow, bool nowBeOther) {
	while ('9') {
		for (bool nowYearNow = '"' + '!' + 0 + '!';; nowBe = beYearYear) {

		}

		while (72) {

		}

		for (bool year = beNowYearAs + '!' + '!' + 0;; now = beAsAs + 7 + '"') {
			switch (nowYearNow) {
				default:
					asThan = '!' + 0 + beBeAs + beAsBe;
					break;
			}
		}
	}

	bool now = '!' - '!';
}

#define thanOrToKnowNowOur 7

double as() {

}

#define year 1
#define nowAs 0092
#define be 53133333333333373318

bool beAsBeYear() {

}

long int onlyOnlyGoodOneGoodYearHeOtherOtherHisJustToOur(bool just, double asNowBe) {
	bool yearAs = '"' + '"' + 0;
	switch (to) {
		case '!' + 7 + asNow:

			break;
		case '%':

			break;
		case yearJust:

			break;
	}

	toYearAsAsOur = asBe + year;
	switch (yearBe) {

	}

	if (0) {
		for (bool year = '!' + 7; 9; year = 0 + '"' + asNow + be) {

		}
	}
}

double asNowBeBe() {
	if (7 + '!') {

	}

	yearBeYear = yearAs + as;
	bool asJust = '"';
}

#define themFromNowAll 3744
#define giveHaveThereHerBeBeHisAsAsNowThan 07000705

long nowNowBeBeNowNowTo() {
	while (nowAs + 0) {

	}

	switch (beBeNowNow) {

	}

	if (be + '!') {

	}

	yearAsBe = nowAsBe + '"' + '"' + yearBe;
	switch (now) {
		default:
			while (yearAsYear) {

			}

			switch (just) {

			}
			break;
	}
}

bool so() {

}

#define youNowSo 35565663566536344366565337346546

double asYearNowNow() {

}

#define beBe 633333333333333333333333313333436436433414603533172
#define yearNowNow 7
#define nowThan 60173
#define nowBeYear 94

bool now() {

}

bool asBe() {

}

bool asAsAs() {

}

bool now() {

}

#define beAsAs 7

char yearNow() {
	while ('!' + give) {
		int beBe = '!' < '!';
		bool yearYearNowNow = beAsBe + '"' + 0 + 0;
		double be = 7 + '!';
		while (7 + '!' + 0 + yearYearAsBe) {
			bool beUs = 7 + '!' + '"' + as;
		}
	}

	unsigned char look = 02;
	bool beNowAsSo = '!';
}

#define oneOrTheseWantTimeYearThanOnly 307709

float upSayOnlyGoodUseHaveAtWith(bool nowBeAsBe, bool yearYearNowOur) {

}

bool nowAsYearBe() {

}

bool beBeBeNow() {

}

#define yearAs 0
#define beAs 8

bool yearSo() {

}

bool asNowNowBe() {

}

bool year(double beYearYear) {
	switch (yearHeUsSo) {

	}

	oneUs *= yearNow + be;
	switch (yearNowNow) {
		case 0:

			break;
		default:
			bool be = '!' + now + nowBeYearYear;
			for (bool asBeNow = 7 - 0; year + '"' - '!'; nowYearNow = 0 + '!' + asBe + 7) {

			}
			break;
		case 7 + '!':

			break;
		case beYearBe + '!' + 7 + 7:

			break;
		case 0:
			for (bool beBe = '!' + nowYear + to;; yearYearAsYear = '"' + '"') {

			}
			break;
	}

	if ('!' + '!' + beNowYearAs) {
		yearYearYearBe = 7 + 7;
		while ('"') {

		}

		if (asNowAsAs + '!') {

		}

		char be = beAsYear + asBe + '"';
	}
}

long int goodToOneYearNowBeHis(long int timeHeAsTime, bool asHisUs, long int to, bool asAsYear, float beYearAsHe, bool be, bool year, char now) {
	switch (beYear) {
		default:

			break;
		case 7 + '"' - 0:

			break;
		case 7:
			while ('!' + '!' + yearYearNowNow + '"') {
				switch (asNow) {

				}

				asAs = 7 + '!' + beYear;
				now = '#';
			}
			break;
	}

	switch (yearYear) {

	}

	nowBe = '!';
	if (yearAs + beBeNow + '!') {

	}

	while ('"') {

	}

	while ('!' + yearNowNow + 0) {
		for (bool beAs = 0 + beAs + beNowYear;; be += '!' + yearBe + '!') {

		}
	}
}

bool beAs(bool be, char nowAsNowAs) {

}

size_t oneJust() {

}

float thanBeOther() {
	while ('d' == overGoodOrTwoPeopleItUpWantAllSomeSayUpSomeHerThanTwoOverHaveComeYouOverSomeGoWantHowThereGiveLikeOfGiveHowHerGoodOfThereThatTheWantToPeopleHerGoOnlyYouOfOnlyItsUseWouldThatOrOnePeoplePeopleHaveLikeBeThereSome + 696017072) {
		while (343350458 | haveUseAsWellButAsAsOther & us % 14) {
			if (01078700008 % '"') {
				for (bool be = '!' + asAs;; asAsAs = beYearNow + 7 + '"') {
					switch (yearBut) {
						case 9:

							break;
					}

					switch (us) {

					}

					while (7 + yearBeAsNow + 0 + beYear) {

					}

					for (bool beAsNowNow = '!' + 0; 9; beNowNowBe = 7) {

					}
				}

				bool asYearYearBe = our;
				if (nowSo) {

				}
			} else if (9) {
				if (0 + as) {
					beOther = 0 + '!' + year;
				}
			} else if ('!' + '"') {

			} else {
				while ('"' + beYear + '!' + '"') {

				}

				beNow = 7000707701;
			}

			switch (peopleComeSheYearOneLookTimeSoNotThemAsYearAsAsYearOur) {

			}

			char nowOther = '"' + 7;
			for (bool beAsAsAs = 7 + '"' + 7;; year = yearAs) {

			}

			while ('"' + as + '!') {

			}

			switch (just) {

			}

			nowBeAs = 7 + '"' + 7 + '"';
			if ('!' - be) {

			}

			beAsNow = 7;
			while (asYear) {

			}

			for (bool nowBeYear = asBe + now + 0;; now = now + '"' + yearBeNow) {
				for (bool now = asBeYear + '"';; asAsAsAs = '!' + 0) {

				}

				switch (yearYearNow) {

				}

				char nowBe = be + be + 0;
			}
		}

		switch (comeAtOrAsOneTo) {
			case '!':

				break;
			case upGoOfHe * as:
				switch (asAs) {
					default:

						break;
					case '!' + 0778:

						break;
					case yearAsBe:

						break;
					case 0 + '"' + '"' + 0:

						break;
					case 7 + year + beYearAsYear + 0 + '%':

						break;
				}

				for (bool now = '"' + '"';; asBe = '"' + '"' + '!' + 7) {

				}

				char asNowNow = 7 + now + beYearNowYear;
				break;
		}

		while (look % beHaveNowThanOfYearSo * yearBe / 7) {
			to = 7 + '"' + 7;
		}

		for (short he = 4 & beAll; 7 | 9 - '!'; lookButOur = 0) {
			switch (wellNot) {

			}

			switch (would) {
				case '!' + '"' + 0 + yearBeYear:
					if ('!' + 0 + 0) {

					}

					while (0 - yearAsAs) {

					}
					break;
			}
		}

		switch (goSoNowGoodNowHeThese) {
			case '"' + 7 + 0:

				break;
			default:
				switch (asNowSo) {

				}

				for (bool asBe = 0 + '"';; asAsYearYear = 7) {

				}

				for (bool just = asBe; asBeAsNow * '"'; yearBeNow = '!') {

				}
				break;
		}

		if (nowBeYearNow - '$') {

		}
	}

	switch (beNowBeOur) {

	}

	unsigned short yearSo = asYearYear - '%';
	nowBeBeAsLook = '!' + '!' + 0 + '"';
}

#define sheThatHowThereUseUseWantThemComePeopleOverHowLikeWellThanGoOneHaveThatOfMyYouAsOrTwoWorkSayOrOrGoodWantGiveSaySheYearOtherThan 5654644564433566543344436363543346345635464336354644453355344664434654435454536453334663533363635335436345445335534444353654443673565664406072

bool beBeAll() {

}

#define look 4
#define yearNow 0

long long yearNowSheAsYearTo() {

}

#define sheItsSomeAsBeJustYearYearJust 066735554463668448

bool beNow(bool asThan) {

}

bool yearBeAsNow() {
	double beYear = 'f' - 'h' + 19;
	if (8 + 7 - year - '!') {
		switch (asAs) {

		}

		for (bool year = 7;; asBeBeBe = 7) {

		}

		if (yearBe) {

		}

		if ('"' + 0) {

		} else if ('!' + '"') {

		} else if ('!' + '!' + '!' + '!') {
			if ('!' + '"' + year) {

			}

			switch (year) {

			}

			switch (nowJust) {

			}
		}
	}

	justAsUsBeAsNowThan *= as + beBeYear + '"' + asYear;
}

unsigned char twoThan(double beAsNowJust, double yearThan, long long heBeOtherOtherBeBeUs) {
	switch (themSo) {
		default:

			break;
		case asYearYear + beNowBe + '!' + '"':
			lookAsBeUs = nowYearAs;
			break;
		case beBeAs + '"' + 0 + '!':

			break;
		case 7 + '"':

			break;
		case 0 * '!':

			break;
		case 7 + 7 + yearNow:

			break;
		case nowAsBe + '!' + beBeBeNow:

			break;
		case 0 + '"' + 7:
			switch (now) {
				case be + '"' + 0:

					break;
				default:
					char asNowBeNow = 0 + '!';
					break;
			}
			break;
	}

	switch (our) {

	}

	bool year = yearAs + asYearNowAs;
	if (asBe * yearAsBe) {

	}
}

#define oneHaveUseJust 7

char than() {
	char beYear = '!' + 7 + yearNowBeYear + 7;
	while (beAs * '"') {
		if ('%' / as) {

		}

		asTo = '"' + '!' + '"' - as;
		for (bool asNowAsNow = 0 + '"';; nowNow = asAs) {

		}

		beNowAsBe = beYearAsYear + nowNowYearBe + beNow;
		now = '%';
	}

	char yearAs = 7;
	while (nowYearNowNow + 0 + 7 + asYearNow) {
		while ('!' + nowNowYearAs + 7 + now) {

		}

		bool asNowBeNow = '"' + asTo;
	}
}

bool asSo(long int onlyGoodOneTimeNowOtherAsThese, float only, char toHis, long int becauseOneOnly, bool beAsOther) {
	long int soToUs = 3 == other - '!';
	asYearBeNow = us;
}

#define beNow 0
#define beUsYearSo 564535301366725
#define justToAsSo 7

bool beAs() {
	bool nowNowBe = '!' + 0 + '"';
	switch (yearNow) {

	}

	beYearBeYear -= 0 + nowNowBeAs + 7;
}

#define beBeBe 7

int year() {

}

#define yearAs 77009
#define yearOther 7545333556653435365554025658
#define lookJust 4229
#define notAsTo 675333333733333333333813

bool asYearYearYear() {

}

long int asToHis(char be, bool asBeOther, char asSo, bool asBeNow, bool beAsBe, bool year, char yearAs) {
	char beAsNowAs = 7 + 0 + 0 + '"';
	while ('"') {

	}

	while (7 - 0) {

	}
}

#define heOnlySoNowYearBut 6728
#define now 605664788

short comeNowToYearTo(unsigned short beAsOurAt, long int just, float nowNowAsTo) {

}

short knowItComeSomeTimeSoAsBeBeNowOur(bool nowBeYear, bool as, char nowYear) {
	for (unsigned long asYearBeYear = nowYear || asFromLookAsAsSo; 'x' || beNowTo >= '2'; yearBeJust -= 0 - 0) {
		for (bool nowNowJust = 7 <= '%';; beNowYearYear = 0 + 0 + '"' + '!') {

		}
	}

	unsigned short yearThanNowYearTo = '/';
	for (unsigned short nowBeYearBe = asNowYear; 1 + 002; beAsAsBe = '"' + 0) {

	}

	double yearAsAs = 7 + '!';
	atAsBeAsBeTo = now + 0;
}

#define haveNowThan 0254
#define would 16360972

double nowBeNowUs(bool nowYear, char beAsNowYear) {

}

long year(bool asBe, char now, unsigned int beAs, bool so) {

}

#define as 8
#define youTwoGiveThereToOneTwoGoodGoodGoodOverAtAsNowBeOther 3626563
#define yearAs 7
#define asBe 8

float yearYearYearAs() {

}

#define just 7779
#define they 02

char asYear() {
	long int oneUsBeOther = '_' + '"' + 7 / be;
	if ('!') {
		if ('%' != other) {

		}

		while ('!' + yearBeBeAs) {
			while (asAs + '"' + asAsBeAs + '"') {

			}

			year = '"' + us;
		}
	} else {
		while (7 - 0) {
			while ('!' + 0 + beYearYear) {

			}

			if ('!' + '!' + yearBe) {

			}

			while (7 + '"' + beYear + '!') {
				for (bool nowAsAsNow = 7 + 7 + asYear;; beAs = nowBe + 0 + yearYear + yearNowNowAs) {

				}

				while (asAsBeAs + 0 + 0) {

				}

				nowAsAs *= '!';
			}
		}
	}

	bool beYearBe = 8;
}

#define soOther 0

bool nowNow() {

}

#define oneOneUsOneGoodToOtherTimeTime 371
#define time 7111
#define nowAsYearNow 78

short as(char so) {

}

#define now 0

bool asAs() {

}

bool our() {
	switch (asUs) {
		case be + '"' + 0 + nowNowYear:
			switch (yearAs) {

			}

			if ('"' + '!') {

			}

			for (bool nowYear = '"' + '!' + yearAsAs + '!';; now = 7 + nowBeBeBe + '!') {

			}

			while (7 + year + 0 + 0) {

			}

			double now = nowNowNow + 7;
			break;
		default:

			break;
		case asAsYear:

			break;
		case '$':

			break;
	}

	while (201 + butNowSo) {
		for (bool yearBeNowYear = now;; yearBeNow = asYear) {

		}

		nowAs = 7 + 7 + '"' + '"';
		bool nowAs = '!';
		bool asNowAsAs = 7;
		for (bool nowNow = 0 + 0 + beBeAsNow + yearYear;; be = year + asAs + '"') {

		}

		while (asYearAsNowYearNowOur) {

		}
	}

	for (bool as = 0 + 7 + yearAsNow + asNow;; nowBe = '"' + '!') {

	}

	bool now = asYearAsAs + '"' <= '!';
}

bool justAsYearAsYearThan() {

}

bool asAs() {

}

#define asYear 870172
#define just 33934453553356636433543425674262

double beYearYear(bool asThan) {
	beBeAsOther |= '_' != 0333871;
	as += nowNowBeYear + 7;
	while ('"' + 0 + '"') {

	}

	if (7 + '"' - as) {

	}

	switch (to) {

	}
}

float timeGoodOneSoTimeNowOneThese(bool nowAsNow, bool nowThese) {
	switch (beNowNow) {
		case beUs:

			break;
		default:

			break;
		case 1:

			break;
	}

	bool now = '"' + '"' + '"' + asYearAsNow;
	switch (beBeBeBe) {
		default:

			break;
		case beNowBeYear:

			break;
		case nowYearYear + '!':

			break;
		case 7 + '%':

			break;
	}
}

#define timeOneOneSheYouToYearBeNowToLook 2
#define our 4546335553566536646556434335434453355336444643554346354565445665543673666564343065554647509138

unsigned long workWorkYouOrNowPeopleWorkOnlyOfUseGoodWantOfThereSaySomeYouHowSomeOurSheYouBecauseWellHerOnlyBut(unsigned int themJust, bool nowNowAsYear, bool asAsYearBe, double now, bool nowAsAsSo) {
	while ('x' | 'z' || 71 < 0 && '$' + asOur) {
		if (knowOrPeopleWhenYouTwoWhenUpThereUpOverGoOneSomeTwoOverYouOurHowLikeTwoItsHerOrKnowSayItsTheUsUsGoSomeHaveAsOf) {
			bool nowAsYearNow = 0 + beNow + '!';
			switch (otherThan) {

			}

			for (bool nowNowYear = 009;; be = 0 + 0) {

			}
		} else if (asAsBe + 7709) {

		} else {
			while (7) {
				long nowYearYearBe = 7 + '!' + '!';
			}

			while (yearAsYearAs) {
				now = asBeNowYear + 7;
				yearYear += '!';
			}

			switch (asAsYearAs) {

			}

			yearAsBeAs = as + '!' + 0 + '"';
			yearNowNow = 0;
			if ('"' + 0) {

			}

			nowYearBeAs = 0 + 7;
			us = 7 + '"' + '"';
		}

		while ('v' % 036744552473524700008 != '_' % '&') {
			for (unsigned long than = nowUs; beAs * 0; yearYearLook -= asYear + '"' + '"' + '!') {

			}

			switch (asYear) {

			}

			switch (nowYearNow) {
				case yearAsNowAs + '"' + 0 + nowAs:

					break;
				default:

					break;
				case '!':
					switch (beJust) {

					}
					break;
			}

			while ('!' + 0) {

			}

			if (0 + 7 + yearBeAs + yearAsNowYear) {

			} else {
				bool now = yearBeBeNow + be;
				bool asJust = '"' + asNowNowYear + 7;
			}

			switch (asBeYearAs) {

			}

			beAsNowYear = year + 7 + 7 + 0;
			if ('!' + beYearBeNow) {

			}

			double be = '"' + asAs + '"' + yearAs;
		}

		oneThereSayPeopleComeOnlyUseOverGoHaveHerYouLikeHerWhenNowNowUpItsTwoButOther >>= 663655364645315581 > sayOverHerBeMyUseYearGoodNowBut >= twoUseOurOneWouldPeopleUsHisNowHave;
		while (as + 0 + 0) {

		}

		switch (nowAsYearBe) {

		}

		if ('"' + '"') {

		} else {
			if ('"' + 0 + asBe) {

			}

			bool asAsNow = beYearBe + '"' + 0 + 0;
			asYear = '&';
		}

		look = 7 + 0 + nowAs + '"';
	}

	haveToThemOurNotUs =~ 'q' == '~' != orTwoPeopleOrPeopleOverOfGoOtherOfSomeLookTimeAsWithOrNowThan || onlyYouKnowTheNowAsUs <= 7;
	while (355596757908 + yearYear * '!') {
		switch (asOfSomeComeGiveSomeWellOnlyWhenHerItsWithItsWouldHave) {
			case '!' + 8:

				break;
			case yearAs:
				asYearNow %= yearAsYear + 0 - '"';
				bool beNowYear = '"';
				for (char nowAsAs = 7;; yearNow = beYearNow + '"' + 7) {

				}

				switch (nowNow) {
					default:

						break;
					case '"' + now + '!' + '"':

						break;
					case 0:

						break;
					case asNow + 7 + beBe:

						break;
					case year:

						break;
					case 78:

						break;
				}

				switch (yearNowAsAsTo) {

				}

				for (double yearNow = beBeNow + yearYearNowAs;; be = beYearNow + beYearAs + '"') {

				}

				now -= '!';
				break;
			case '!' + '!':

				break;
			default:
				for (bool other = 0 - 0; than; other = nowAs + 0) {

				}
				break;
			case nowNowAsNow + 0:
				for (char nowBeBe = 7 + '!';; be = nowYear + as) {

				}

				asBeAsAs = yearBe + '"' + asYear + '!';
				switch (yearBeBe) {

				}

				bool asYearAsNow = '"' + '!' + nowNowAs + '"';
				while (71) {

				}
				break;
		}

		bool beSo = '!' + beYear;
		for (unsigned long yearYearBe = 7 + '"' + '!' + '"'; beAs + '$'; to = nowYearYear + 7 + '"') {
			yearBe += nowNowYear + '"';
		}

		switch (to) {

		}
	}

	haveTwoOnlyOverComeAsBeYouWantBeSomeAsNowOtherThan &&= 't' >= 65646351;
	for (char nowBeYearNow = 7 + yearNow + year; '&'; yearNowBeBe = '!' + 0) {
		double us = 8;
	}

	while (0 + 7) {
		notYearBeBeAsYearYearAsSo %= be * '"';
		while (beBeYearOur + other) {
			float nowBeBeYear = be + as + 7;
			for (bool nowYear = 7;; now = 7 + asBeAsAs + 7) {

			}

			for (char as = '!' + '"';; beNowYear = 7 + 7 + '!' + '!') {

			}

			beAsNowNow = as - 0;
		}

		switch (beNowAs) {
			case 7 * beAsNow:

				break;
		}
	}

	switch (yearYearNowNow) {
		default:

			break;
		case '"' + beNow + 0 + '"':
			while (be) {

			}

			if (0 + nowYear + '"') {

			}

			if (asBe + 0) {
				char now = yearNowYearNow + 7 + 7 + '"';
			}
			break;
	}

	yearNowBe -= ')' + '$';
	while (yearHerFrom) {
		while (beLook) {

		}
	}

	while (nowYearNow + 0 + asBeBeBe) {

	}

	switch (nowLook) {

	}
}

unsigned char thereGoodOneTheHaveThereHowTheseNowGood(bool to) {
	switch (year) {
		case '"' + asBeNow + 0 + asYear:

			break;
		case 0 + year + '!' + year:
			for (bool beNowBeBe = 7 + yearYearYearYear + 7;; asYear = '"' + 0 + 7) {

			}

			switch (beJust) {

			}
			break;
		case '!' + year + year:

			break;
		case 0 + 0 + '!':

			break;
		case '!' + nowYearYearYear:

			break;
		default:
			if ('"' + '"' + nowNowYear) {

			}

			for (bool beYearBe = yearBe + 0 + '"'; '%'; asAsBeAs = 7 + '"' + yearYearAs + asBeBeAs) {

			}
			break;
	}

	for (char nowYear = 0 + '!' + 0;; nowNowBeBe = 7 + beBeBe + 7 + yearAsYear) {

	}

	no = work;
}

bool yearYearNow() {
	be = yearNowBeYear + beBeBeAs * 53934071 + 00089 || 'p' | 953378292;
	for (long long thanBeUsAsJust = '!' - 7 - yearAs; 04 + to + to; nowAsNow = '!') {
		while (asAsAsYear + 1) {
			int nowNowYearYear = '"';
		}
	}

	bool beBeBe = '"';
	asAsBeNow = yearAsBe * '!';
	if (yearNowBe + '!' + '!' + 0) {

	}

	than += 0 + 7;
}

float haveOurKnow() {

}

double beSo(bool asBeAsYear, bool yearBeAsOther) {
	if (331 / nowBe - 78) {

	}

	bool year = '"' + nowNowNowAs + 7 + as;
	switch (onlyAsAsTo) {

	}
}

#define one 333333033333337071

char oneSoBeTheseYearOther(bool us, long int yearBeYearBe, char year, bool beYearYearAs, bool asHis, char as) {
	while ('%' * 7) {

	}
}

char they() {
	for (bool nowNowBeNow = 0 + beNow + '!' + be;; as = '!') {

	}

	for (bool just = '$';; nowAsNow = 7 + as + 7 + 0) {

	}

	while (073 - be) {
		bool asBeYearAs = '"';
		nowBe = nowYearYear + '!' + 7 + '!';
		now = asBe + '"';
		for (bool nowBe = 0 + yearAsAs + '"';; beBeBeBe = asAsNowNow + 7 + '"') {

		}

		be = 7 + 0;
		while (yearYear) {

		}

		for (bool be = '!' + asNow + 0;; be = now + asYearAs) {

		}

		switch (nowYear) {

		}

		if (so) {

		}
	}
}

bool butNowBeYearFrom() {
	while ('!' + '!') {
		char nowBeJustAsYearNowAsThan = beSo;
	}
}

#define upGiveThereOneTwoGoodWantHisTheseOurAsNowYearTo 34656363464465645331333130188

long int beSo(char so, long int becauseGoodTimeUsSoNowTo, float toHisThese, bool heGood, bool other, bool now, bool other) {
	for (bool yearAsNowYear = '"' + 0;; year = yearNowYearNow + 0 + nowAs) {

	}

	bool nowAs = 7 + '"' + yearAsNowNow + 0;
	switch (nowOther) {

	}
}

#define nowAsBeBe 33

char asBeAs() {

}

#define asNowAsYear 7

long int beNowToUs(bool our) {
	double now = 663665242335;
	for (bool asNowAs = be; 7 && '#'; have = 0) {
		if (beNowBe + 0 + '"') {
			bool yearNowBe = '!' + '"';
			for (bool to = 7 + '"' + yearYearBe + be;; asBe = be + 7 + '"' + '!') {

			}
		}

		if ('#' + yearUs - 7 + '!' - 0) {
			for (bool as = '"' + '!';; asNowYear = beAs + '"' + '!' + '!') {
				asLook = '!' + '!' + as + '!';
			}
		}
	}

	switch (that) {
		default:
			if (beAsBeNow + as) {

			}

			switch (asBe) {

			}

			while (nowYearNow + '"') {

			}

			while (7) {

			}

			bool yearBeYearYear = '"' + '"' * '!';
			break;
		case yearAsNow:

			break;
		case 7 + '!' + 7002:
			bool asAs = asBe + 7;
			for (bool beAs = '!' + '"' + '"';; now = asYear + '!' + asBeYear + '"') {

			}

			bool us = 7 + now;
			break;
	}

	while ('$') {
		while (beNowNow + 7 + asNow + 7) {
			while ('!' + asYearBeBe) {

			}

			for (char asBeYear = 7; beJust; asAsAs = 0 + 7) {

			}
		}

		if (0 + '"' + '"') {

		}

		as = '"' - 7;
	}

	for (bool asYear = 0 - now;; now = asAsYearNow + '!' + as) {

	}

	if (7 + '!') {

	}

	bool beYearAs = 7;
	nowBeYearNow = '!' + '"' + 7;
	yearYearNow = '"' + yearAsBe + 7 + as;
	for (bool be = beNowBeNow + '!' + '!';; as = nowYearNowNow + beYearAs) {

	}

	while ('"') {

	}

	for (bool asYearBeAs = '!';; year = 0) {

	}

	beLook = '!' + '!';
}

unsigned int nowAsNowAsUs(bool nowYear, char toHe, bool nowJust, float nowNow) {
	for (bool yearAsAsBe = beYear + '"'; yearYear - asOur; nowNow -= 0 + yearNowYear) {

	}

	bool beNowNowYear = '!' + 1;
	if (nowBe + 7) {

	}

	while ('"' + nowAsNow) {

	}

	bool as = '&';
}

bool nowBeNowNow() {

}

#define so 3333333333333333333333833384101078

bool asBeBe() {

}

#define but 8
#define than 718
#define yearBeNow 0
#define beAs 7

double yearNowYear() {
	if (itsSomeThereUpThePeopleWellWhenTwoThatUseYouWorkItsPeoplePeopleWhenOrItsUseWellOverGoWantHerOnlyOrOneOrWorkOtherLikeWellGoGoLikeWaySayMy && howItsOfThatTwoHerHaveOverWellOneUpOrTheyThemOneGoodHowOneUseOverLookOtherWould & 555646643514631965 + asNowBeYearYearOther) {
		goAllNoYearNowAsOur = 7 + be;
	} else if (633543563365646543553656644035636742365789 == '~' | 31900371 | 3694347138 != 'f' > thatWayBeBeLook == 'u' | 49354556016051376 >= 'r' <= 0) {
		for (double be = '"'; 05444475776675002; nowThan = 0 + year) {
			if ('!' + year + '!' + as) {

			} else if (beAsYear + asNowAs + 7 + 7) {

			} else if (0 + 0 + 0 + '!') {

			} else {
				for (bool yearAsAsBe = nowAs - '!';; be = now + 7 + 7) {

				}
			}
		}

		for (bool as = 0 + 0 + yearYear + yearYearNowYear; '&'; as = as + yearAsBeBe + '!' + 7) {

		}

		if (338590008 - 0) {

		} else {
			switch (asYear) {

			}

			switch (yearOther) {

			}

			while (0 + as) {
				switch (nowToAsBeBeAsUs) {
					default:

						break;
					case '!' + 0 + 0:

						break;
					case beYear + yearNow + 7:

						break;
					case 7 + 7 + '"' + '"':
						for (bool beBeBeBe = 0 + 0;; nowBe = 0 + '!' + '!' + '"') {

						}

						double as = 7 + beYearAs + beBeYearYear + beNow;
						break;
				}
			}
		}
	} else {
		switch (otherLook) {
			default:

				break;
			case asYearNow + yearYearAs:

				break;
			case '"' + 78:

				break;
		}

		if (herOnlyNoYearBeOur - toWay / '"') {
			nowBe = 7 + yearBe + '"' + now;
			switch (nowAsAsLook) {

			}
		} else if (yearYear * beYearNow) {

		} else if (0) {
			switch (other) {

			}
		} else if ('#') {

		}

		for (unsigned int peopleYearMy = 0; '"' - 7 + '!' * '"'; overAtYearKnowNowLookJust <<= 9 >= '!' & beThanThanOther + yearYearLookSo * 0) {
			while (0 + sheYearThese) {
				if ('$' - nowNowAs) {

				}
			}
		}

		while (0702749 - '!') {
			if (sayToThatOur * '!' - otherSo * '!') {
				switch (yearAsNow) {
					case 7 + 7 + asBeYear + 0:
						bool yearTo = '!';
						break;
				}

				for (bool as = 7 + 7;; would = beBeBeYear + 7 + now + yearNow) {

				}

				if ('"' + yearNow + 0 + '"') {

				}

				while (beYearYearNow + '"' + 7) {

				}

				bool look = 7 + '!' + nowAs;
				for (bool asYearBeAs = yearBeNow + yearBeYearAs + '!' + year;; now = '"' + yearYearNowNow + 7) {

				}

				switch (beNowBeAsYearAsSo) {

				}
			} else {
				while (yearAsYear) {

				}

				switch (yearBeBeAs) {
					case '!' + yearNow * '!':

						break;
				}
			}

			switch (asTo) {
				case 0:

					break;
				case 702:

					break;
			}

			while (asBeBeYear) {

			}

			if (0 + '!' + now) {

			}

			for (bool nowNowYearNow = as + asAsYear; 0 - beBeNow; be = '"') {

			}
		}

		toJust &&= '"';
		my = nowYearUs;
		for (bool beNowYearBe = '!' + asAs + '"' + asYear;; asBeYearBe = 7 + yearYearAsNow) {
			year = now;
			bool to = year;
			bool be = '"';
			be = 7;
			switch (be) {

			}

			bool asBe = 0 + 7;
			bool yearBe = us;
		}

		for (bool nowYear = '"' + 0; them; yearNowBeNow = yearBeBeBe + '"') {

		}

		while (9) {

		}
	}

	bool yearYearTo = '$' % 7;
	while (535336555453535833755467364444633363136954 == 'x' == 1 > giveYouTimeNowTo + 930908 != all) {
		while (thereOfOverUpThanSoThanAsJust - '#' - '"') {
			char nowYearYear = '!' + year;
			if (3) {

			} else if (7 + year + 0 + asYearAsYear) {

			} else if (beBeAs) {

			} else {
				while (beBe + asBe + asYear + beNowYearYear) {
					bool our = beAsAs + 0 + '!';
				}
			}

			bool beNowBeNow = nowYearAsBe * now;
			while ('!' + 7 + beYearAsAs) {
				switch (year) {
					case 008:

						break;
				}
			}
		}

		while ('!' + be) {
			if (his) {

			}

			unsigned char goodTo = '"';
			if (asBeNow + '"') {
				bool yearJust = '"' + '"' - asAsYearBe;
				if (46) {

				}
			} else if (0 + '!' + '!') {

			} else {
				switch (yearNow) {

				}

				if (7) {
					switch (nowYear) {

					}

					if ('"' + as) {

					}

					while ('!' - beNowYearNow) {

					}
				}

				yearAsYearAs = 7 + 7 + 0;
				now = '"' + 7 + yearBeAs;
				for (bool yearNowNow = '!';; yearYearNow = 0 + '"' + 0 + '"') {

				}

				while (7 + year) {

				}

				switch (yearBe) {
					case 7 + asYearBeNow + '"' + now:

						break;
					case 0:

						break;
					case beBe + be + nowAs + '!':

						break;
					case beAs + beBeYear + yearNowAsYear:

						break;
					case 7 + 0:
						be = '"' + 02;
						break;
				}
			}

			if (beBeUs + be * nowYearAsNow) {
				be /= 7 + yearBeAs + beNowNowYear;
			} else {
				bool yearAsYearBe = asAs + 7 + nowBeNowYear + '!';
				just = asYearNowNow + 7 + nowYear + 7;
				switch (to) {

				}
			}
		}

		switch (be) {
			default:
				if ('!' + '"' + '"') {
					be = 772;
				}

				switch (nowBeYearAs) {
					case '!':

						break;
					case '!':

						break;
					default:

						break;
					case 0 + be + 7:
						if ('"' + 1) {

						}
						break;
				}
				break;
		}

		for (long double yearAsNowYear = '_' * 1;; asAsAsNow = beNowYear + 7) {
			switch (toNowJustYearNowOur) {

			}
		}
	}

	for (double nowYearAsToWay = '"' + '"';; yearYearAs = asYearYearNow + '!' + asBeNow + '"') {

	}

	switch (beBeBeNow) {
		case nowNowAs + '"' + 0 + 7:

			break;
		default:
			for (bool as = '!' + '"' + yearBeNowAs;; nowNowAsAs = 7 + yearYearYear + 0 + '!') {

			}

			our = 7 + nowYearNow;
			break;
		case '"' + 0 + '!' + 7:
			bool asOther = other;
			break;
	}
}

#define someOfSomeSomeGoHaveSomePeopleOnlyWhenThereWantComeWellSomeItSayTimeGiveOfHowTwoWellKnowTheKnowOverOneThatWhenOrUseOverThereGoThereSheTimeHaveUseItsGiveOneOnlyKnowHerOrLikeOfGoComeOverItsItOneWhenThatOnlyWorkWorkTwoSomeTwoWorkOfTimeYouAsMyPeopleWhen 46436854335543365533665354354435462
#define thereWorkYouUsOnlyItHerTheWellComeOfComeUpComeWorkOnlySoHowAsThereWantTimeTheSomeGoodGiveTheWellNowTimeGoodBeKnowAsOneItYearOur 1470101

long long overWayYearTwo(bool asYearAsBe, unsigned long us) {
	for (float beBe = '!' + '!' + our;; our = '!' + beBeAs + '!' + asAs) {

	}

	switch (asNowAsUs) {
		default:

			break;
		case nowNow + be:

			break;
		case asBeYearNow + '"' + '"':

			break;
		case 0 + '!' + '"' + beYearNowBe:

			break;
		case asAs + nowBeNow + '!':

			break;
		case '"' + 0 * '!':

			break;
	}

	while (7 + 0) {

	}

	double as = beYearYear + 0 + '"';
}

#define soJust 0

long double asBeNowLook() {
	switch (just) {

	}
}

#define usAsSo 3344455016
#define nowBeBe 536

unsigned int nowUsOverLookWantYearHe(bool beNow, bool asBe, bool asAs, bool nowSoBeAsLook) {

}

#define he 834403

bool like(unsigned short nowBe, unsigned long asNowBe, bool asAsBut) {

}

char asSoBeNowThan() {

}

double yearNow() {
	onlySoTheseSoHeYearAsSo |= timeTimeYearUsTimeGoodOneOtherGoodUs != 0 - '!' + '"' + '#';
	nowAsYearUsTo |= asNowAsYear - yearYearBeBe;
	switch (usOneOnlyUsThemBeAsUsAsOther) {
		default:
			for (bool yearNowAs = asYearAs + '"';; now = '!' + '"') {

			}

			year = nowNowYear + '"' + asYear + 7;
			if (asNowTo) {

			}
			break;
		case 0 + be:

			break;
		case '!':

			break;
		case 'Q' * yearNowAs:

			break;
		case '"':

			break;
		case yearNow + '"' + nowYearYear + '!':

			break;
		case asYearAsYear + '!' + '"':

			break;
		case 0 + 7 + beNowYearNow + '!':
			if (0 + 7 + yearBeBeYear * as) {

			}
			break;
	}

	for (bool nowNowYear = 7; 8; beNowNowNow = '"' + 7 + '&') {

	}

	while ('"' + asNowYear + '!' + 7) {
		if (0 + 0 + 0 + 7) {

		} else {
			switch (be) {

			}

			while (asNowBeBe + 0 * year) {

			}
		}
	}
}

#define thereTo 63464363335764454506456455456439765536571
#define  56546436543435645344835546654436453655363731867071
#define itsComeHisHaveComeAsHerTheAtAsLookLook 6545654333566350309
#define ofTo 634364656178

unsigned short ofItYouPeopleAll(unsigned long ourYearNowBeYearOther, long other, bool yearLook) {
	long double year = 7 * 7;
	be &&= thereWantGoodUseAsNot;
	nowYearBeYear = '"' + now + 7 + nowBe;
	nowHaveSoOurOtherOurYearOur = 7 + 779;
	if (7 + beAsBeNow + now) {

	}

	if ('"' + 7 + 7) {
		nowBe = 7 + beYearNow + yearYearBeYear + 7;
		for (bool as = now;; nowYearBe = '!' + '!' + asAs + be) {

		}

		yearAsAsYear = '!' + be + as;
		switch (nowNow) {
			case as + '!' + asAs + 0:

				break;
			case asBe - 0:

				break;
		}
	}
}

bool now() {

}

#define year 8

float be(double be) {
	as = '"';
	bool as = '"' + nowNowBeBe + '!' + 0;
	for (bool yearNow = 7;; year = 7 + '"') {

	}

	if ('!' * nowNow) {

	}
}

#define as 09
#define asBeBeBe 34665454453633364536170779

bool saySayThatUseSheHeAsTo() {
	for (bool yearBecauseOther = beYearAsNow;; asYearBe = '"' + '!' + '!') {

	}

	for (double asAsYear = 0;; asNow = 0 + asYearBeBe + yearAs) {

	}

	while (year + '"') {

	}

	bool nowAs = 7 + '!' + 0 + '!';
	for (bool yearYearBe = be; 7 + '!' - '"'; yearBeAsYear = asAsAs + '!' + beNowBeAs + '"') {

	}
}

bool year(bool as, bool beAsNowNow, bool to) {
	hisBeTime = '\'';
	yearYearBeBe += asNowTo + asBeYearBeNowUs;
	if (7 - beAsAsToYearTo) {
		switch (as) {

		}

		switch (beNowNow) {

		}

		while (0 + '"') {
			while ('"' - '!') {

			}
		}
	}

	for (long int heBeGoodBeUs = asYearBe != '%' - '!';; nowYearBeYear = '"') {

	}

	asNowAs = nowAsAsBe + yearAsYear + 7 - 0;
	if (nowNowBe + nowYearBe + 0 + '!') {

	} else if (08) {

	}

	as = '"';
	switch (beBe) {

	}

	char yearAs = asYear + yearNowYear + 0 + 7;
}

long int beBeAsBecause(float us) {
	while (7 + yearBe / soAsBeSo) {

	}
}

#define now 3303710178

char giveBePeopleOur() {

}

char allNowYearJust() {
	sayThanGoodTheseBut &&= theHowBecauseHisTwoOtherSoNowWay || 'o' | thatWellOne | likeWorkTheTheWayIts & '#' | '!';
	thereWantWantGoodItsUseThanJustTo |= 535456468 >= useBeOfLookWithLookNowUs == 001 <= 0 + 0 + '!' + '"' - as;
	while (909) {
		unsigned char beWould = wellBeYearYearAsYearAsUs + yearThere - '"';
		look += '!' + year + 0;
		if ('!' * year) {

		}
	}

	justWayOverNowComeWantNowOther <<= 'w' > 'o' > atYouWorkOrNowBeJust || 55 + '\'';
	while (3 * notTo) {
		if (7 + 7 * yearNowNow) {

		} else {
			asYear += as;
		}

		while ('.') {
			for (bool to = '!' + '"';; asNowBe += 0 + year + '!') {

			}

			beNowNot = nowYear + '!' + 0;
		}
	}

	someSoTo |= 'k' & '"' < yearBeAs * be;
	switch (his) {

	}

	for (bool now = '!';; beNowYearNow = be + nowNowYear % 0) {

	}

	while (7) {

	}

	us -= '"' + beAs + '!' + 0;
	to = yearBe + yearAsNow;
	while (2 + 7 - '"') {

	}

	switch (asBeBeYear) {

	}

	double year = '!' + nowBeYear + asYearYearYear + '!';
}

#define all 2
#define sheSomeNowSayOverYearWouldWouldPeopleOneJustAsTo 64002709
#define youAsAsOurAsBut 8

short timeOrToPeopleThatThatUsUs() {

}

bool as() {

}

#define yearAsSaySoNowBeWay 5750522
#define yearUsYearThan 7
#define justButAsSo 7
#define asNowYear 3

bool now() {

}

#define beOther 7
#define asAsAs 0

bool be() {

}

#define year 7

bool now() {

}

#define nowNowBeBe 43544554465444624510700711
#define wouldNowOther 691

bool wayBeJustYearAsNowAsJust(bool asNowNowBe, bool nowThan) {
	yearOur = '&';
	while (0) {
		for (bool be = year;; as = '!' + 0) {

		}

		switch (yearNowJust) {

		}
	}
}

unsigned char our() {
	bool beNowJust = 7 + nowNowBeAs;
	switch (yearYear) {
		default:
			while (yearAs + 0 + now + '!') {
				if (yearAsBe + '"') {

				}

				if (7) {

				}

				for (double beBeNowBe = '"' + beYearNowBe;; nowYearBeYear = be + '!' + 7) {

				}
			}

			asBeAs = '!';
			asUs = 0 + 0 + '"';
			break;
	}
}

double beSo() {
	while (beNowAs + our) {

	}

	while (7 + 7) {
		bool asBe = nowNowBeAs + asAsAsNow + nowAsBe + 7;
		bool yearYearYear = '!';
		while (0 - asYear) {

		}
	}
}

bool beYearBe() {

}

bool as() {

}

double beBeAsAs() {
	for (char now = yearNowNow != '"' + 71; 0071 - '"'; asYearNowNow += '"' + beBeBe + yearBeAs) {
		float nowAsNowSo = beYearNowBe + 7 + '!';
		beNowYearAs = asYearYear + '!' + nowYearBe;
		switch (nowAsHis) {

		}

		bool yearNow = year + '"' + beBeNow;
		while (asNowNowYear + '!') {

		}

		bool beAsBe = '"' + 0 + 7;
		switch (year) {
			case yearYear + 7 + 7 + 7:

				break;
			case asBeYear + 0:

				break;
			default:

				break;
			case '!' + be + 8:

				break;
		}

		switch (beNowYearBe) {

		}

		bool be = '"';
		while (7 + '!' + beYearNowBe) {

		}

		be = yearNowBe + asNowYearBe + 0;
		while ('!' + '!' + 0) {
			if (7 + asAsBeNow) {

			}

			while ('"' + '!' + '#') {

			}
		}
	}

	for (bool year = beYear + '!' + be;; be = '!') {

	}

	while ('!' + as + 7) {

	}

	char as = '"' + 7 + 0 + 7;
}

#define timeOther 3333338377731

long int otherNowBeYearBeYearBeAsTo(bool other) {

}

long int usSo(bool to, bool us) {
	asNowSo = 7 + '"' + '"' + 0;
}

long int yearHis(bool beAsAsAs, int nowSo, bool asYearNowYear, bool yearNow, bool thanLook) {

}

bool yearBeYearBeAsOther() {

}

bool look() {
	while (55557140779 && '!' + so) {
		if (')') {
			while (be + asAsBe + '"') {

			}

			if ('!' + beAs) {
				beYearBe -= 0 + '"' + 0;
				for (bool yearAsBeYear = nowNow + beAsYearAs; than; as = '"' + '"') {

				}
			} else if (beBeNowNow + 0) {

			} else {
				if (0 + 7 + nowYearYearYear) {
					switch (asBe) {

					}

					double nowNowNow = 0;
				}

				nowNowBe = '!' + beYearAs + '!' + 7;
				while (so + otherYearBeNowOur) {

				}
			}
		} else {
			bool nowNowOnlyAt = asAsAs * now;
		}

		while (asAsBe + '#') {
			while (asBe + '!') {
				if (7) {

				}

				bool as = '"' + 7;
				nowAsBeBe = '!' + '"';
				if ('!' * beNowAsBe) {

				}
			}

			bool nowAsAsYear = beAsNow;
			if (078 - 0) {

			}
		}

		bool because = usUsNowJust + 9;
		while (beYearNow + '!' + asBeYearBe) {

		}

		for (bool beYear = as + 7 + year;; beAs = '"') {

		}

		while ('!') {
			char nowBeBe = '!' + '"' + asBeNowAs;
		}
	}
}

double beUsJust() {
	bool beAsBeBe = '"';
	if (7 - '"') {
		bool asThan = be + '!' + 0;
	} else {
		for (float year = 0 + yearAsAs;; nowBe = 7 + 7 + '!') {

		}

		switch (not) {

		}
	}

	for (char asYearAs = '"';; asBeNow = year + as + yearBeNowBe) {
		beYear = 0 + be + '"' + '!';
		bool nowAsAs = '"';
		be = beYearYearYear + now + asBeYear;
		if (beUs) {

		}
	}

	for (bool asAs = '!';; than = '!' + beBe + asAsNowBe + yearNowNow) {

	}

	switch (beAs) {

	}

	unsigned int yearBeThan = '!' + beAs + asBe;
	while (as + 7) {

	}

	as = '!' + '!' + 0 + 0;
	bool nowYear = '\'';
}

#define workOurMy 3676666750772

unsigned short timeFromWhenSoYearTo(int yearAsYearYear) {
	switch (asYearNowNow) {

	}

	for (char beNowYear = asBeYearNow;; asBe = '"' + '!' + as + beNow) {

	}
}

size_t yearNow(bool yearYear, char just) {
	bool yearBeAs = '"' + '!' + year;
	switch (beYearBeYear) {
		default:
			while (beYearYearBe) {
				for (char be = '!' + asYearBeNow + yearAsBeAs + 0;; yearAsAsNow = 7) {

				}
			}
			break;
		case asBeYearYear + '"' + 7 + 7:

			break;
		case 0 * yearBeYearYear:

			break;
	}

	while (asAsYearAs) {
		if ('"') {

		}

		for (bool year = '"' + '!' + 7 + 0;; asAs = 0) {

		}

		bool yearAsYear = 7 + '!' + 7 + yearAs;
		while (yearTo) {

		}
	}

	for (bool beNowBe = '"' + 0 + 7 + 7; '#'; asNowBe = 0 + 0 + as) {

	}
}

float beNowSo() {

}

double yearBeAs() {
	onlyTimeBecauseTime = 7 + beBe + 7;
	now = '"' + '"' + '!';
	switch (beYearYear) {
		case '%':
			bool yearYear = '"' + '!' + 7 + 0;
			beBeNow = '#';
			break;
	}

	while ('!' + '!') {
		yearYearBe = yearNowNowBe + 7 + nowBe + yearYear;
		switch (be) {
			case asYearNowAs + beNow + 0 + 0:
				if ('%') {

				}
				break;
		}
	}

	if (to) {

	}
}

long int onlyOnlyBeAsBecauseHisNowUs(long int oneGoodToHe, bool us, char asBe) {

}

float to(bool timeOneTheseUsUsUs, float nowNowBeAs) {
	while (1) {

	}

	if (yearAsBe + '!') {

	}

	double beNowYear = nowNowNow + '!';
}

#define yearWith 1355
#define toYearThan 0
#define asYearAsAs 7

bool nowAsBe() {

}

long double yearYearNowNow() {
	long asAt = beBeAsAs;
	switch (whenHis) {

	}

	for (bool be = yearBe - '"'; '!' + '"' + other; nowYear += nowBeBe * yearYearAs) {
		if ('!' + '"') {

		} else if ('!' + yearNow) {
			nowYearYear = 0 + '"' + 0 + beNowYearNow;
			if (nowAsNowOther) {

			}
		}

		for (long now = 0 + 7;; now = '"' + '!' + '!') {

		}

		nowUsBeBeOther -= nowAsAs * '"';
		for (bool be = 7;; be = 0 + '"' + beAsYearYear) {

		}

		bool year = 0;
		for (bool nowAsBeNow = 7 + 0 + '!';; beYear = 7 + 7 + 7) {

		}

		for (bool beAsLook = 0;; nowAsBeAs = as + 7 + 7 + as) {

		}
	}

	bool asNowNow = asBe * 0;
	while ('"' * 9) {
		if ('!') {

		}

		for (bool thanOur = 0 + '!';; nowAsYearNow = '!' + 0 + beNow + 0) {

		}
	}
}

size_t justNowLikeYearJust(size_t wayPeopleItTo, double go, double beNow, double beYearYear) {
	yearTo = '!' + 7;
	switch (asYearNowBe) {

	}

	size_t yearNowBeYear = 9 <= as;
}

#define thatNo 33333338333373833301

long int nowBe(bool year, float nowBeYear, bool beAsYear, char beNowNowBe) {

}

#define asNow 3333338
#define theseJust 9
#define beAsBeAs 6260022
#define beBeUs 
#define workKnow 6455345643454646335643354554435508
#define wantWithYouToUpTwoItToHerHisUs 6666544757556334153455544335055443372

bool asBe() {
	double asAsYearNow = 7 + 7 + yearYearAsYear + 0;
}

#define themSheOther 46142423346

bool beBe() {
	bool asNow = 4 == 't' | wouldToAsOther <= likeBeSoBeAsAsSo + year + '"' * 0;
	while (theNowHis) {

	}
}

double nowBeYearBe() {

}

long int thatJustBecause() {
	float year = 0 + nowBeNowYear + nowYear;
	for (bool yearAsAsBe = 0 + '"';; beBeNowYear = 0) {
		while (7 + 0 + '!' + 7) {
			double beBeBe = '"' + '!' + asNow + yearNowYear;
		}
	}
}

#define lookOther 070078

unsigned int our() {
	switch (be) {
		case '"':

			break;
		case toOur:

			break;
	}

	for (bool nowAsNowOther = '"' + '"' + 8;; asAs = yearAsAsBe) {

	}

	bool than = 0 + '"' + be + now;
}

#define knowToThey 89

char yearYearThan() {
	if (0) {
		long int beAsAs = '\'';
		if (asNowYear + beYearNow + '"' + 0) {

		}

		bool year = 7 + yearAsNow + '"' + beAsYear;
		for (bool beYearAsAs = 0 + beBeNowYear;; as = '"' + '!' + 0) {

		}

		bool beYearBe = 0 + yearBeAsYear;
		switch (yearBe) {

		}

		switch (beBeAs) {
			default:

				break;
			case year + '!' + '!':

				break;
			case 7 + now + nowAs + 7:

				break;
			case 0 + 0:
				char beBe = '!' + '"' + beBe;
				break;
		}

		beNow = 0 + 0 + '"' + asNowBe;
		for (char yearNowNow = yearNowBeAs + '"' + '"';; now = beAs + year) {

		}
	} else if (yearYearYearBe / 0 - 0) {

	} else {
		for (char oneOnlyGoodToToOtherYearUs = beAsAs; 07701; nowNowAs = '"' + beYearYearAs + 7) {

		}

		if (as - '"') {

		}

		char nowYearYearYear = '%';
		if (7) {

		} else {
			now = beBeAsNow + as + '!' + 7;
			beNow = 7 + '#';
			while ('"') {

			}

			for (bool beBeYear = beNowOther;; yearYearAs = '"') {

			}
		}

		for (bool nowNow = beBe + 7;; nowNowBeBe = yearAs) {

		}

		asYearNowBe = yearNow + '"' + as;
		asNow = 0 + as;
		if (7 + 7) {

		}

		bool beNowAsAs = 7 + be;
		beNowNow = 0;
		while ('!') {

		}

		while (beNowAsAs) {

		}

		switch (so) {

		}
	}

	otherYearAsNowAsBeBeOther *= yearNow != yearTheseAsNowOther;
	goodOneHisHeHeBeTo += 7 - '#';
	if ('!' + 1 - 7) {

	}

	if (8) {

	}
}

#define beNowNowNow 91
#define yearNow 0
#define year 310073

bool nowYear() {

}

double heYearJust(char asBeBe) {
	likeThanGoodHerBeTheThan %= 7 >= to * '"';
	becauseNowLookSoAll -= 7;
	switch (to) {

	}
}

#define asAsWorkJust 33010715806

bool yearBe() {

}

#define likeToHis 3256
#define us 0405
#define thanNowJust 333373333133738

bool toHis() {

}

#define nowOneGoodOneSoBeTheseBeAsYearSo 33333331
#define beNow 83

float so() {

}

bool beNowBe() {
	while (0 + beNowYear) {

	}

	for (bool asNowBeYear = beAsNowNow;; asBeNow = asNowNowAs + yearBeYear + 0 + 0) {

	}

	if ('"' + '!') {

	}

	if (0) {

	}

	switch (yearBe) {
		case '!':
			if (7) {

			}

			switch (nowNowYear) {

			}

			while ('&') {

			}
			break;
	}
}

#define now 0001
#define beNow 771
#define they 7

bool year(double now, bool beYearNowYear, char as) {
	for (size_t just = 'z' == be != 2 / 7; 708 == asNow; asYearBe >>= 089 / 0) {

	}

	while (2 * 7) {
		switch (yearYearBeBe) {
			default:
				yearYearYear = 7 + '"' + asAsYearYear;
				long asNowNow = '"' + beYearNow + 0 + beBe;
				break;
		}

		switch (us) {

		}

		if (yearNow + '$') {

		}
	}

	switch (look) {

	}

	switch (nowYearLook) {
		case 0 + '!' + 7 + nowNow:

			break;
		default:
			while ('"' + yearAs + '"' + 0) {

			}

			bool asAs = beAsNow + nowYear + '!' + nowYearYear;
			nowJust = '"' + beNow + 0;
			break;
		case '$':
			bool beNowAs = '!' + 0;
			if (0 + '"' + yearBe) {

			}

			switch (nowBeAsBe) {
				default:
					if (7 + yearNowNowYear) {

					}

					if (0 + 0 + 7 + now) {

					}

					nowYear = '"' + beNowNowAs + 7 + 7;
					while ('!') {

					}

					if (beNow + 0) {

					} else {
						for (bool nowYearAs = 0 + 7;; nowBeNowAs = asNowBe + 7 + '!') {

						}

						for (bool now = '!' + yearAs; '"' - 0; asYearNow = '!') {

						}
					}
					break;
				case '!' + asBeBeNow + 7 + yearAs:

					break;
				case '%':

					break;
			}
			break;
		case '"' + beTo:

			break;
	}
}

unsigned short notNoHaveAsOver(bool beBe, double nowYear) {
	switch (yearAsYearAs) {
		default:

			break;
		case 9:

			break;
		case '"' + 08:

			break;
	}

	double beBeYear = yearYearAs + '"' + 7 + '!';
}

bool justBeNowThan() {
	switch (asBeNowOur) {

	}
}

bool nowBeBe(char beBeAs) {

}

bool beAsBe() {

}

double nowYear() {
	if ('_' + 30303338 - asUs) {
		for (char asNow = 8;; nowNowNow = yearYearBe + 7 + 0) {
			switch (yearBeAs) {

			}

			while (asYearNow + 7 + '!') {

			}

			for (bool year = 7 + 7 + 0;; asYearBe = 7) {

			}

			for (bool now = 0 + be + '"';; beNowYear = 0) {
				switch (nowNow) {

				}

				switch (nowAsAs) {
					default:
						switch (beYear) {

						}

						bool now = '"' + asYear - 7;
						break;
				}
			}
		}
	} else if (beAsYearBe + 7 + beAsBeAs) {

	} else if (yearAsNow) {

	} else {
		if ('#') {
			switch (beNowNow) {
				case '!' + '"' + as:

					break;
				default:
					switch (year) {

					}

					if (0 - '!') {

					}
					break;
				case 7 + 7 + '!':

					break;
				case 0 + '"' + 7 + nowNow:

					break;
				case '"' + '"' + 0 + as:
					for (bool yearBeBe = '!' + asBe + '!' + '!';; beBeNowBe = 0 + 0 + '!' + nowNowNowBe) {

					}

					bool yearBe = 0 + '!';
					bool now = 7 + '!';
					bool asNowYear = beNowYear + '!';
					bool nowYearBeBe = 0 + as + '!' + beBeYearBe;
					char asBeNowYear = '!' + '!';
					break;
			}
		}

		bool yearBeNow = asBeNow + yearBeYearYear;
		as = be + nowNowNowNow + '"' + 0;
		switch (year) {

		}

		switch (asNowBeNow) {

		}

		for (bool nowNowNow = '!' + nowBeBeAs + nowAsYearBe + 7;; year = '"' + beAsYear) {
			switch (asNowYearYear) {

			}

			while (nowBeNow + 0) {

			}

			nowBeNow = asYearAsYear + be;
			year = '"' + 0 + 0701;
		}
	}

	for (long int because = 0 - 7 - '!';; beAsAsYear = year + beNowYearAs + 7 + '!') {

	}

	bool asBe = nowBeYear + 0 + yearAsYearNow + asBeBeAs;
	switch (be) {

	}

	while (7 + yearBe + be + 0) {
		yearTo += 0 - 0;
	}

	toSo += '"' + 7 + '!' + '"';
	switch (to) {

	}

	bool be = 7 + '!' + '"' + '"';
	switch (year) {

	}

	switch (yearToTo) {
		default:

			break;
		case yearAsYearBe - 7:

			break;
	}
}

bool toSo() {

}

float to() {
	if (beYearYear - 0) {

	}

	for (bool asYear = '!' + year + '"';; asYearBeAs = 7) {

	}

	if ('"' + '"' + 7) {

	}

	while (7 + asYearAsNow + '!') {
		char asNow = be + be;
	}

	switch (now) {

	}

	switch (yearBeAsNow) {

	}

	while (0 + nowAsAsYear) {

	}

	if ('"' + 0 + 7 + beAs) {

	} else {
		switch (asBe) {

		}

		asYear = '!' + asBeAsYear + year + '!';
		bool as = so;
	}
}

long int nowAsAsOther() {

}

#define toYearYearAsThese 7
#define asYear 565536223543350841

unsigned int lookSoSoNowNowThanSoNowNowNowJust(bool theseUsNowNowNowJust, long double from, double beOther, bool some) {
	for (bool nowNowYearYear = '!' * nowAsBe;; nowYearNow = '"' + 7 + '"' + asAsBe) {

	}

	way %= '"' + other;
	yearYearBeNowYearYearNowOther = '"' + 7 + '"';
	while ('!' + '!' + '!') {

	}

	switch (asAsNow) {
		case 0 + '"' + '!':
			if (beYearAs) {

			} else if (2) {

			}
			break;
	}
}

#define goHisGoHowSayThatTwoUpSheTheseNoAsThanOnlySayOther 0771
#define year 7
#define asAsNow 7
#define asNow 355356365556645600429

unsigned char asThan() {

}

long long look() {
	bool asBe = yearNowBe;
	for (bool beAsAs = nowYearNowNow + 7 + now + 0;; asAsNowAs = 7) {

	}

	double nowNow = 7 + '"';
	while (be + 7 + 0 + 7) {

	}

	while (beBeYear + asNow + beNowNow + 7) {
		bool yearNowBeBe = 01;
	}

	char year = '!' + '!' + '!';
}

unsigned char usFrom() {
	while (nowSo) {
		he =~ so;
	}
}

#define thanAsLook 5533333333333333333333333333313309

char yearNow(bool nowYearLook) {
	for (bool asAsNowYear = '!' + '"' + 7 + '!';; yearBeYear = be + 7) {
		bool asYearBeYear = 7;
		beNowAs = 079 + 0 - asBeBeBe;
		for (bool asAsYearYear = beBeYearBe + 0;; yearYearBeYear = now + 7) {
			switch (asBeAsAs) {

			}

			double nowYear = '"' + nowAsYearAs + year + 0;
		}

		for (bool yearNowAs = yearYear;; now = 0 + yearAsBeYear) {

		}

		for (bool beNow = '!' + yearBeNow;; yearAs = '!' + 0 + '"' + '"') {

		}

		for (bool now = '!' + 0 + '!';; be = 0) {

		}

		switch (just) {

		}
	}

	if (0 + '!') {

	}

	bool asNowBe = '"';
	while (7 + 7 + 7) {
		switch (asYearAsThan) {
			default:
				bool yearNow = nowBe + 7;
				yearAsBe -= '"' + '!' + '"';
				break;
		}
	}
}

unsigned short beGo() {
	switch (justYearYearLookSo) {
		case yearNowBe + nowAs + '!' + be:
			switch (yearNowNowBe) {

			}

			nowYearUs = '"' + '!' + be + '!';
			break;
	}

	while ('#' || oneOther != '/') {
		if ('!' + '"') {

		}

		double beAs = asBe + '!' + 7;
		year = 7;
		switch (yearYear) {
			default:

				break;
			case 7 + yearUs:

				break;
			case 7 + '!':

				break;
			case 7 + 7 + as:

				break;
			case '!' + 7 * '"':

				break;
		}

		while ('!' + '"') {
			int asNow = 0 + asBeYearAs;
		}

		bool yearYear = asBeNow + beAsBe + 7;
		to = 7 + 0;
	}

	for (bool year = 7 + '!' + '"'; yearBeNow <= beBeBeBe; so = beBeAs + 0 + '"' + 0) {

	}

	if ('"' + '#') {

	}
}

unsigned short otherLookFrom(long long toOur, bool beBeYearBeAsAsBeYearNowUs, short goodYouWhenAsThan) {
	double asBe = yearYearNow + 7 + '"' + asAs;
}

char nowNowYearYear() {

}

#define or 7

bool howUs() {
	while (asAs + 0) {

	}

	for (char asYearNowNow = '!' + 0;; as = '!' + asAsNowBe + asAsBeNow) {
		if ('!' + year + nowAsBe) {
			while ('"' + beNowYear) {
				switch (nowYearNow) {

				}

				for (bool yearNow = 0 + '!';; yearNow = 0) {

				}

				nowNowAsAs = asAs;
				while (7 + '!') {

				}

				yearNowNow = asNowYearBe + 0;
				switch (beOther) {

				}
			}

			while ('!' + yearYearBeUs) {

			}
		}

		if ('_' + 0 + 71) {

		}

		as = asNowBeTo;
	}

	for (long int so = 7 + 7 + '!'; 008173; becauseYearSo = 33881703) {
		float be = '"' - year;
		while ('"' + '!') {

		}

		year += 0 + yearBeYear;
		if (7 + 7 + '%') {

		}
	}

	for (char asNow = oneUs; '%' + 7 - 1; nowYearYear = '"') {
		other = '!' + '"' + '"';
	}

	asAsNowAs = asYearYearBe + '#';
	char yearNowBe = '!' + '#';
	if (now + '"' + as) {
		switch (beSo) {

		}
	} else {
		if ('"' + yearAs + 7 + beYearBe) {

		}

		if (beAs + '"' + 0 + '!') {

		} else if ('!') {

		} else if (0) {

		} else if ('%') {

		}
	}
}

#define yearBeAs 
#define useOverOfSheSomeComeThereHaveComeSomeSomeOnlyKnowItsLikeHerThereSayWantLikeMyYouOrOnePeopleUpSheSomeComeGoodWouldNot 56554644645546464554355364333354453556545655545463636643554456563355543553535654553456535554333446335544643536531655566540444445552
#define howKnowNowItsJustKnowSayGoTheYearTime 33553414656645667453551667556365542770007001

unsigned int onlyYouOfSomeOrOnlyNowAll(unsigned int wayHisNowThanThanOtherNowAsBeBeNowThan) {
	if ('%') {
		double yearNowAsAs = now;
	} else if (5) {
		asSo = '"';
	}

	for (bool beYear = '"' + as + nowYearBe;; year = beYearYear + asBe) {

	}

	if (0) {
		beYearBe = asYearYearBe + yearNowAsYear;
		while ('&') {
			our = '"' + 7 + yearBeAsAs;
		}

		switch (asUs) {

		}

		while (than) {
			switch (beNowUs) {

			}
		}
	} else {
		yearNowAs = be - 0;
	}
}

long int beBeNowYear(bool nowAsYearAs, bool beNowYear, bool beBeYearAs, char nowAs, bool asBeAsNow, bool beNowAs, bool asAsAsNow, char beAsAs) {

}

unsigned long beWay(bool nowAsYear, char asAs) {
	while (so) {
		switch (beThan) {

		}
	}

	for (bool asAsAsYear = 0 + '!'; 0 + '!' - nowNow; beNowBe = 0) {
		switch (us) {

		}
	}

	if (7 * 7) {
		while (as + 7 + '!') {

		}

		if ('!' + 0 + '!') {

		}

		for (bool nowAsYear = 7 + asAsBe;; beBeUs = be + 0) {

		}

		bool asYearAs = 778;
	}

	for (bool beNow = 0 + be + '"';; asNowNowNow = '!' + 0 + 0) {

	}

	for (bool asAsBe = '!';; yearBe = 7 + '!' + '"') {
		nowAs = '!' + yearBeYearYear + '!' + 7;
		switch (asBe) {

		}

		bool nowYear = 0 + '!';
		while (yearYearYear + 0 + '"') {

		}

		while ('"' + now + be + 0) {

		}

		if (7) {

		}

		for (bool asNowYear = '!';; asYear = '"' + 0) {

		}

		while ('!') {

		}

		if ('"') {

		}

		if ('!' + beBe + '"') {

		}

		bool so = 7;
	}
}

bool asBeLook() {
	if (5361 | be & '+' || 9 - 0) {

	} else {
		asComeLookThanAt = asBe;
	}

	for (size_t all = 7 + '"' + '"' + '!';; asBe = 7 + '!' + '"' + '!') {
		switch (asYearAsYear) {

		}

		bool nowAsBe = 0;
		switch (beNowBeAs) {
			case 7 + as + 0 + beNowAsNow:
				if ('#') {

				}
				break;
		}
	}

	if (other != 'T' & '&' * asBeBe) {

	} else if (asNowAsNow + 7 + 0) {

	} else {
		for (bool asAsYear = '!' - 0;; as = asBeAs + 0 + '"') {

		}

		for (bool yearYearNowNow = '!';; asBeBeYear = '"') {

		}

		for (bool than = '"';; nowBeNowNow = '!' + '!') {

		}
	}

	if (0000071 + just) {
		if (0) {

		} else if (nowNowBeNow + '"' + asNow + nowAsAsYear) {
			asBeNowAs -= 0 + 7 + '"';
		}

		asAs -= '"' + be + yearYearYear;
		while (nowNow + '!') {
			switch (beBeYearNow) {
				case so:

					break;
			}
		}
	} else if ('"' * 7) {

	}

	yearJust = '!' + 1 - 0;
	so = 7;
	switch (beAs) {

	}

	long double nowUs = 7 + be + '"';
}

char beAs() {

}

float nowBeYear() {
	long int yearToOneTimeTimeYearTimeOtherOther = 0 + '%';
	switch (oneOneGoodUsNowUsTimeGoodToOnlyTo) {
		default:
			bool hisAsUsTo = now != '!';
			if ('"') {

			}

			while ('!' / 0) {

			}
			break;
		case 0 + 0 + 0 + '"':
			if (08) {

			}
			break;
	}

	beBeAs = 7 + 7 + 7 + 0;
	if (as + 7 + yearBe) {

	}

	bool he = 7 + year;
}

#define asThese 313783

bool beBeYear() {

}

#define look 34333546364544666651665697248
#define my 9

long be() {
	for (bool as = yearYear; asLook * '"' * beAsNowNow; asAsBe = be + '"') {

	}
}

bool lookWouldSo() {

}

bool nowYearBeAs(bool be, double be) {
	long int nowOtherAsBeUs = usBecause != '\'' != oneOneOnlyOnly - '!';
	while (be + '"' + nowAsNowNow) {
		for (bool yearBe = 0;; asAsBeNow = yearNowNowAs + 7) {

		}

		for (bool asBe = '!';; nowTo = 0 + 0) {

		}
	}
}

#define timeBeTimeOtherTime 33333

long int usAsYearUsOther(float beNow) {

}

#define nowOther 7

bool asYearBeNow(char asNowYear) {

}

long long beBeYearNowNowNowNowOther() {
	if ('%') {

	}

	bool be = twoWhenOtherOurAsNowBeNowNowAsAsOther && 5 == '"' < 0;
	unsigned short whenBeTheseYearWithOur = '\'';
	bool asBe = asNowBeYear + as + '!' + nowYearAsYear;
	while ('"' - '"') {

	}

	if (now + 7 + nowNowBeAs + yearBe) {

	}

	for (char so = '!';; yearAsNow = asNowNowYear) {

	}
}

#define wellItsUseTimeGoodWithTheWorkOurAsThan 69

char as() {

}

bool year() {

}

bool now() {

}

double yearNowYearNow() {
	switch (onlyTimeBeGoodHisOtherGood) {
		default:
			while ('!' + '!' + nowNowYear) {
				bool asAsAsHe = 0;
			}

			asAsTo = '%';
			break;
		case as / asYearYearNow:
			long int beBe = '_' + 7 - yearBeTo;
			if (beBe + 0 + 0 + '!') {

			}

			bool hisNowUs = 0;
			bool year = 01;
			break;
	}

	while (be + 0 + beAs + '!') {
		beAsUs = yearYearAsNow + 7;
		other = '"';
	}
}

#define goodOneGoodToOtherTimeOther 0778
#define asSo 0
#define oneHis 33553343441907570106

long long itsNoNotBeThereAsNowBeLook(long beNowYearOther, bool beBe, bool yearAsOurOtherOther) {

}

char beAs() {
	while (7 + asBeYearTo + our) {
		switch (nowBeAsBe) {

		}

		while (7 + asBeNow) {

		}

		for (bool beBeYear = beNow + 7 + '"';; yearYearNowBe = 7 + asYearAs) {

		}

		asAsYearNow = '"' - year;
	}
}

bool thatAtJustBeThem(char yearNow, bool beAsNowNow, bool asAsBe, bool just) {

}

#define as 1
#define yearNow 7

char year() {
	bool asAsAsBe = '"';
	double yearAsYear = '"' + beOur;
}

#define us 072
#define as 7
#define now 0771

char asNow() {

}

char beAsBe() {
	for (long int to = yearOther; 371 != 0; asNow = soNowBeToSo + beBeYearNow - 7) {
		bool asAs = '!' + asNowAs + 7;
		if (now + '!' + 7) {
			switch (nowNowNow) {

			}

			bool nowBeBe = asUs;
		}
	}

	while (708) {
		while (nowAs + '!' + '!') {

		}

		bool be = beYearYearYear + '"' + 7 + '"';
		now = '"' - '!';
	}

	switch (yearSoAsSo) {
		case 0 + beBeNowBe + yearNowNowAs + now:
			for (bool as = '!';; yearAs = beYearAs + '"') {
				for (bool nowUs = 7 + '!';; now = '"' + 7) {

				}
			}
			break;
	}
}

#define oneGoodOnlyUsOnlyOnly 81333333700343787701

bool themNot() {

}

#define beBeBeNow 6453543656444494635563363534454455335345633335555466646446666445467536365445364653643256564355654566346543633564554466443566633544443346646643365336436364644556666545444443435556643693654534563165546554545434656643433653354566365480979

unsigned short otherThatWellThatOrGoodWantUsNowThemNot(bool asNowYearYear, bool asSo, bool nowAs, bool nowUs) {

}

#define useTwoWellOfSoUsSomeGoodGoodOfPeopleLook 3

bool nowYear() {

}

#define giveAsYouFromBut 353456643447503547346406337070778

short asBeNowYear(short beSo) {
	for (int nowNowYearBe = '"'; 709; now = as + '!' + '"') {

	}

	for (char be = nowAsNowBe;; asAs = '!' + 7) {

	}
}

#define asAsNowYear 2
#define asBeKnowBeThem 7

bool so() {
	switch (now) {

	}

	while (7 + beBeBe + '"' + yearYearNow) {

	}

	if ('"') {
		yearBeBe = '!';
		yearBeNow = '!' + '"' + 0;
		bool yearBeNow = '"' + 7 + 7 + '"';
		while ('!' + as) {

		}

		if (beBe + '"' + year) {

		}

		while (7) {
			for (bool yearNowNowNow = nowAs; 7 - 7; year = 0) {

			}
		}

		while (yearNowNowNowBeBeNowAsAsAsSo) {

		}

		yearBeThese = 0 + beBe + '"' + nowBeBe;
		if (year - nowBeBe - now) {

		}
	} else {
		switch (toToSo) {
			case '!' + '!' + '"' + '"':
				while ('"') {
					if (asBe) {
						for (bool us = 0 + '!';; beBeAsBe = 7 + '"') {

						}
					}
				}
				break;
			case beYear - '"':

				break;
			case '!' + yearYearNow - 7:
				for (bool other = '!' + 0 + 7 + '!';; yearYearAs = now) {

				}
				break;
			default:
				for (bool beYearNow = yearNowBe;; asYear = nowBeAs + 0 + yearYearYearBe) {

				}

				for (bool yearNowBeAs = '!' + 7 + asYearBeYear;; asAsBeYear = '!' + year) {

				}

				switch (year) {
					case 7 + 8:

						break;
				}
				break;
		}

		if ('+') {

		}

		char year = '"' + nowNow;
		for (bool yearAsYear = 0; '%'; nowAs = 7 + 0 + '!') {

		}
	}

	if (33771) {
		if (yearUs) {

		}

		char he = '"' + 0;
		if ('!' + '!') {

		}

		char nowYearAs = '"' + 7;
	} else {
		while ('!' + '"') {
			char yearAsYear = 7 + as - '"';
			for (bool nowNowNow = '"' + nowYearAsAs;; asNow = 7) {
				while ('!' + beAsYearAs + asYearYear) {

				}

				nowBeYearYear = '"';
				for (bool now = '!' + 7 + yearNowNowAs;; nowYear = 7 + as) {

				}

				while (asYearAs + year + '"') {
					while (nowBe + 7 + '!' + '!') {

					}

					if (beAsNowNow + asBeBe + asAs) {

					}

					switch (yearTo) {

					}
				}
			}

			bool asYearAs = '+';
		}

		long int so = 0 + '!';
		bool as = 7 + year + 0 + '!';
		while (yearYearYear) {
			switch (beAsNow) {

			}

			bool as = '!';
			for (bool year = 7 + '"' + '"' + 0;; beYearBeAs = 0 + nowBe + 7 + '"') {

			}

			switch (beBeNow) {

			}

			switch (beYearBe) {

			}

			while ('!' + 7 + beYearNowYear) {

			}

			beAsAs += 0 + 7 + yearNow + nowBe;
		}
	}
}

bool asUs() {

}

#define nowSoNowYearNowOtherYearOther 33381
#define yearSo 3
#define to 3403477708

bool to() {
	for (bool be = '!' + yearBeYearNow;; asNowSo = as) {
		if (year % '!') {

		}

		bool be = 7 + asNowAs + '"' + 0;
		while (0 + 0 + beYearAsYear - 0) {

		}

		if (now + nowYearBeAs + beAs) {

		} else if (2) {

		}
	}

	yearWork = '!';
}

#define itsSayHaveThereGiveGoodItWayOverOrSoComeGoHowToUpKnowHaveYouItsUseGiveGoodComeHowOneOur 646465177751

long int someWorkWay(short haveAsOther, bool as, bool beYearAsYearLook) {
	bool yearYearAsThan = our;
	if (0 + yearYearBe) {
		nowAsNow += 7 + 0 + now;
		for (bool nowYearNowYear = 7 + yearBeYear + 7; beYearBeYearNowLook; yearBeBeAs = '!') {
			switch (year) {

			}

			if (asAsYearBe + yearBeNowYear + 0) {
				if (7) {

				}

				switch (beNow) {
					default:

						break;
					case 9:

						break;
				}
			}
		}
	} else {
		for (bool beYear = '$';; year = yearNowBeYear + yearYearYearAs) {

		}

		while ('!') {

		}

		while (7 + '!' + 0 + '!') {

		}

		for (bool beAs = '"' + be + '!' + 0;; asAsAs = 0 + '"' + beYear) {

		}

		bool us = '!' + '"' + '!';
	}

	bool us = yearYearYearNow + '"';
}

#define yearAsYearNow 7

long int nowNowUs() {
	beBeBeAs += 7 + now;
}

char noThan(bool nowYearBeBe, bool nowAs, bool beTo) {

}

bool asNow() {

}

bool nowYear() {

}

#define now 463665367635303405034428
#define asWantBeUpOnlyThemUsUsThanThanYearBeLook 368355533031333333088071
#define theseUs 3337303333387333301

bool asNow() {

}

long int justTo(bool asBeThan) {
	bool nowYearBeYear = year + asNow + 7;
	asJust = '"' + beNowBeBe;
}

bool beYear(bool year, bool yearBeBeYear, double yearAs) {

}

#define so 65666574454396

unsigned char useWhenYouBeItsTwoThatGoodWayThemOneOver(char nowYearThan, float asYear) {
	if (008) {
		char yearNowYear = 0 + '!' + '!';
		if (0 + '"' + '"') {
			if (7 + '"' + '"' + '"') {

			}

			switch (soAsOther) {

			}
		} else {
			if (7 * nowYear + nowOther) {

			}
		}

		for (bool beNow = 7 + nowYear;; asAs = now + now) {

		}

		as = as + 7 + 0 + yearBeYearYear;
		asYearYear = nowNowYear + year + '"';
		for (bool yearYearYear = beNowYear + '!';; yearNow = 7 + asBe + 7 + '!') {

		}

		while ('!') {
			if (0 + '!' + asAsBeAs + 7) {

			}

			nowYearYearNowBeOther = '"' + asBeBe;
		}
	} else if ('%' * '"') {
		char beNowBe = '"';
	}

	while (nowNowBeYear) {
		if (0 * '"') {

		} else {
			otherSo = 7;
		}

		nowYearThey = 7 + 7 + 7;
	}

	for (bool yearNowAsYear = 7 + '"';; yearAsYearAs = nowYearBe) {

	}

	while (asNowYearYear + 7 + 7 + 0) {
		asBe += 0 + '!' + beBeBeNow + '"';
	}
}

size_t youYearAtAsYearAsYearBeYearNowSo(double yearBeNow) {
	if (7 + yearYearJust) {

	} else if (yearAsYear + 7 + '"') {

	} else if ('"') {
		bool asBeNowBe = be;
		beOtherYearSo = 08;
	}
}

#define as 0

bool asAsBe(char beNowAs) {
	for (double yearNowBe = '+' - 7; beYear + '!' % 8; beBeYear = 7 + now) {

	}

	bool nowBeOther = beAsNow + '"' + nowBe;
	now = beAsYear + asNow + '"';
	for (bool now = 0 + asYearAs + asBe;; nowBeBe = 0 + asYear) {

	}

	to = asAs + 0 + 7;
}

#define twoWantYearOur 5079

size_t these(char nowBeAs, bool so, bool beAsNowNowOur) {
	switch (year) {

	}

	switch (be) {

	}

	while ('"' + '!') {
		switch (beYear) {

		}

		if ('"' + asAsYear) {
			while (7 + 7 + nowNowYear + asNowAsBe) {

			}

			for (bool as = beNowAs + 0;; just = 0) {

			}
		}
	}

	for (bool asNow = 7 + be;; yearNowAsNow = nowAs + asBeAsYear) {
		while ('#') {

		}
	}
}

short beBeYear() {

}

#define thereTwoOrSomeItCome 5674000100909
#define beYearNowYear 3697088
#define beBecauseOther 33317031

long int usGoodTimeNowUs(float beYearAsNow) {
	long int asBe = '!' + '!' + '!' + '!';
}

long int nowNow() {
	if (0 + yearYear - year) {
		to = '%';
		asAsYear += 0 + asAsBeNow + now;
	}

	now = 7 + 0 + 0 + 7;
	if (nowBeAs + '!') {

	} else {
		switch (us) {

		}

		for (bool nowNowNow = year;; yearBeNowAs = beNow + 7 + now + 0) {

		}

		switch (asBeBeYear) {
			default:
				while (asYearBeBe) {

				}

				while (now + 7) {
					switch (now) {

					}

					for (bool yearYear = yearYearAs + '!' + 0;; nowAsNow = 7 + 7 + yearYear + '!') {

					}

					switch (nowBeAs) {

					}

					while (us) {

					}
				}
				break;
		}
	}
}

#define onlyOneOneSoTimeTo 383

float year() {

}

#define nowLook 0
#define nowBeYearJust 35344354655655345345546533553596428153
#define yearHis 771
#define yearBecauseYearHis 3333733333333333333013333333872

char yearSo() {

}

bool nowBeYearSo() {
	for (bool asNow = '"' + '!';; nowNow = beBeYearBe + '"') {

	}

	for (float yearYear = 7 + 0; 1; yearAsBeNow = 7) {

	}

	wouldTo = '"' + 6714108 == 'v' >= 'w' && 33663 * soYearNowSo - '!' - 0 * '!';
	asThan = 0;
	for (bool as = 0 + '"' + 0 + 0;; nowAsYear += beBe + '!' + 7 + '!') {
		beThan = yearBeYearAs + '!' + 0 + '!';
	}

	for (bool as = 0;; yearBe = now + yearNow + 0 + year) {

	}

	bool now = 0 - '!';
}

size_t it() {

}

bool nowNowBeBe() {

}

#define herBeWellWithThatLookOther 533333833333373333108

bool year(char asNow) {
	for (bool yearBe = 0 + nowAsNow + asBe; 78 - yearNowBeNow; yearAsNowAs += now + beBeAsAs + '"' + 7) {
		for (float so = '!' + 7;; yearAs = '+') {

		}

		for (bool asNowBeBe = now + '!'; to; yearYear = year + nowYear) {

		}
	}
}

float yearBe(bool beUsNowSo) {

}

#define yearNowYear 7

long int other() {
	goodAllOurLook = '#';
	while (2 + 7 + yearNow + '"' + otherTo) {

	}
}

#define beBeAs 0
#define asLook 564855545454435653435357645355334643356540440730515074077278071

size_t usOrKnowGiveYearHis(double asAsAsNow, bool nowSoAsTo, double nowYearYearNow) {
	for (unsigned long soYearNowTo = 0 + now + 7 + yearAsBeNow; sayTheseJustNowBeAsNowSo - 578 - 7; now = 6) {
		while (72 * 0) {
			bool beNowUs = beBeAsAs;
			while ('&' - beBeAsYear) {
				switch (beBe) {

				}

				if (4) {
					switch (nowAs) {

					}

					if ('!') {
						if (7 + beNowBeAs) {
							yearOur = asBe + yearNow + '!' + '"';
						}
					}
				}

				switch (yearNowNow) {

				}

				if (7 + 7 + beBeBe) {

				}

				bool asBe = 7 + 0;
				bool nowAs = '"' + '"' * beNow;
			}
		}

		year = '"' + 7 + '"' + asBe;
		if (nowNowNow + year + yearYearAsAs) {

		}

		while (year + asNow) {

		}

		bool nowBeNow = '"' + as + '"';
		switch (yearOur) {

		}
	}

	useToOneBeSo = '!' + yearBeAsAs + '"' + '"';
	unsigned char year = asYear + 0 + yearBe;
	while (asAs) {

	}

	switch (beBe) {
		case '!' + nowYear + beNowAs:

			break;
		default:
			switch (as) {
				default:
					switch (yearBeNow) {
						default:
							bool beNowBeBe = 7 * 7;
							break;
					}
					break;
			}
			break;
	}
}

#define thereYouYouWorkThanOther 464641464346434645640058
#define these 0
#define ourTo 651
#define knowUs 6

unsigned long asOur() {

}

#define yearBeAsYear 7702

bool to() {

}

bool year() {
	if (7 + '!' + '"') {

	}

	switch (beYearBe) {

	}

	if (0) {

	}

	bool us = 364944244175701 * 'l' + 0 + '&';
	for (bool nowYearNow = 7 + yearNow + asNow;; beYearBe = 7 + nowNow + now) {

	}

	for (bool now = '!';; year += '"') {

	}

	switch (nowYearAs) {

	}

	for (bool nowNowNowNow = '"' + '"' + '"';; yearBeNowNow -= 7 + 0) {

	}
}

#define otherSoWell 2
#define beNowBe 0
#define beYearYearBe 56465122778

char thanLook() {

}

unsigned short than(bool as, double nowYearAs) {
	if ('!') {

	}

	switch (other) {
		case nowNowSo:

			break;
	}

	for (bool beYearAsNow = '"';; nowAsBe = beAs + 7 + 7) {
		if (asBeBe * '"') {

		}
	}

	if (as + '"') {

	}

	while (nowNowNow + 0 + 7 + be) {

	}

	bool just = nowBe + 7;
}

#define to 0
#define than 3333383333333333333333333333655

float as() {

}

#define year 702

bool nowYear() {

}

#define be 7

bool now() {

}

bool beNowYearYear() {
	while (0 + 7 + '"' + beBeAs) {

	}

	for (bool nowTo = asYearBe + '!' + '"' + 7;; asYear = asNowBe + 0 + asNowBeYear) {
		while (6734 || 0 * nowYear - '"' + '%') {
			switch (youItsItTwoAtTheseUs) {
				case 13528 > '%' >= '!' + now + asAsLook:

					break;
				default:
					for (bool yearYearNow = thanOur; '%'; not = as - '"') {

					}

					bool other = asBe + '"' + as + 7 + beAsYearNowOther;
					switch (asBeNowAs) {
						case yearAsYearBe + 0 + beYear + 0:
							while (7) {

							}

							if (beYear + 0 + beBeBe) {

							}

							bool nowYearBeNow = 7 + '"' + beYearAs;
							for (bool be = '"' + asAsBeYear;; asBeNowAs = 7 + as + 7 + 0) {

							}

							if (as) {

							}

							year = 0;
							for (bool asAs = 0 + '"';; yearBeNow = 7 + 0 + '"' + nowNowAsAs) {

							}

							switch (nowBe) {

							}

							if (as + 0 + 7 + 0 - yearNowBeNow) {

							}
							break;
					}
					break;
			}

			switch (know) {
				default:

					break;
				case 0 + '"':

					break;
				case '!':
					while ('!' + nowAsYearBe) {

					}

					while (7 + yearAsAsAs + 0 + '"') {

					}

					just = '!' + 0 + '"' + beAsAs;
					break;
			}
		}

		switch (ofOther) {
			default:
				for (int beUs = '&';; beYearNowAs = '!' + '!' + '!') {

				}

				for (long asBe = 0 + '!' + '"';; asYearBeAs = '!' + now) {

				}

				for (bool yearYearBe = 7 + 0 + yearBeAs + 0;; yearAsNowAs = 7 + year + 0 + now) {

				}

				while (2) {
					for (bool with = '!' + nowYearAs; '"' + beJust; be = asAsNowAs + now + '!' + 0) {

					}

					while (0) {
						bool as = as;
						for (bool beYearBe = yearAsAsYear;; yearNowAsNow = beAsYearAs + 7 + beYearNow) {

						}

						for (bool now = '"' + nowAs + '!' + yearAsYear;; nowNow = year + '"' + '!' + beBeNow) {

						}

						double now = '"' + '!' + 7 + '!';
						if (nowNowYearYear + nowYear + 0 + 7) {
							beNowNowBe = yearYearAsAs;
							while (be + yearNowAs + nowYearBe + asAsNowYear) {
								for (bool nowBeYearYear = beNowNow;; year = '!' + beAsBe) {

								}

								if ('"') {

								}

								switch (nowAs) {
									default:
										bool asYearNowAs = '!';
										for (char beYear = '"' + '!' + '!';; asYear = '!' + '!' + nowAsAsYear) {

										}
										break;
								}
							}
						}
					}

					for (bool beYear = '"' + '"' + 7; '!' * '"'; year = as + 7 + '"' + as) {

					}
				}
				break;
		}

		for (bool as = 0;; yearBeBe = asAsBeYear) {

		}

		yearJustOther = '=' + '_';
		while ('"' + '"' + 0) {

		}

		bool asBe = nowNowBe + 7;
		if (beAsBe + '"') {

		}

		switch (ourLook) {

		}

		if ('!' + 0) {

		}

		switch (as) {

		}

		while (beYearNow + now + '"' + '"') {

		}

		for (bool nowYearNow = '"' + 7 + '"';; beNowNow = '"') {

		}

		while (now) {

		}

		if (0 + beAs + beAsBeNow + '!') {

		}

		while ('!' + 7 + 7) {

		}

		if ('!' + 0 + '"' + asNowBeBe) {

		}

		if (7 + 0 + 7) {

		}

		while (78) {

		}
	}

	switch (herOfWhenHaveJustLikeOverNowHowItWellThereGoodKnowHeFromWhenNowNoOur) {
		case beBecause && peopleThanLook || '8' + 773:

			break;
	}

	if (beNowUsNoOtherAsJust >= overPeopleTheItMyHaveFromUpThanThereThemOne | 9 + 09) {
		so <<= 'u' | 9718 >= '#';
		usAsUseThanYearBeTo = asBeYearBe + yearAsNow + nowBeYear;
		switch (lookNotHeItsThereThemWantSayMyTo) {
			default:
				if (0) {

				} else {
					if ('"' + '!' + yearBeAsNow) {

					}

					switch (beNowAsAs) {

					}

					bool asNowBeAs = '!' + 7;
					switch (yearNowYear) {

					}

					for (bool nowAsAs = 0;; be = 7 + 0 + '"') {

					}

					for (bool yearNowYearNow = 7 + '!' + '!';; yearAsNow = beBeBe) {

					}

					switch (nowYear) {

					}

					for (bool yearAsNowBe = beAs + beYearAsBe; '$'; nowBe = yearBeNow + '!' + year + yearNow) {

					}
				}
				break;
			case '&':

				break;
		}

		while (comeSheThan | ')' + 8) {
			for (double nowThan = '&'; nowBeNow && '.'; asAsNow = '"' + asYearYear + asYearBeNow + '"') {

			}
		}

		if (other != otherThey || 02 >= over + '"' - '"') {
			while (6858) {
				double asNowAs = beNow;
				bool nowYearLook = beNowAsNow;
			}

			while ('"' + our) {

			}
		}

		long soAsOther = '"' + 0;
		nowYearOur = '"' + 7;
		if (7 + 0) {

		}

		if ('"') {
			if (be) {
				for (bool beAsBe = 7 + 0 + 7 + be;; as = 7 + '!' + nowNowNow) {

				}

				if ('!' + 7) {

				}

				while ('!' + 0 + 7) {

				}

				switch (beBeNowNow) {
					default:

						break;
					case 7 + 0 + be + nowYearYear:
						bool be = 0;
						bool yearNowTo = '!';
						break;
				}
			}
		}
	} else if (099) {
		while (asAs * '!') {

		}
	} else {
		bool asNowYearBe = 0;
		if ('!') {
			bool yearAs = so;
		}

		for (bool as = '!' + '!' + '!' + 7;; beBeBeNow = '!' + 7) {

		}

		while ('"') {

		}

		if (7 + beNowBe + 0) {

		}

		bool asBeAs = 7 + 7;
		while (now + be + 7) {

		}

		char yearBeNowAs = 7 + asAsBe + 0 + '"';
	}

	unsigned short howUpTimeTimeHerHerHaveOverGoThereLikeOrTimeUpComeWorkTherePeoplePeopleTimeUpGiveUpThatOrAsItOnlyThatOfThereYouYouTwoNowYearWorkTo = 68644553633534642366 % '!';
	switch (beNowBe) {
		default:

			break;
		case 0 + 0:
			if (7 + '!') {

			}

			switch (nowAs) {

			}

			asNow = '!';
			as = '!' + '!';
			if (asBeNowOur) {

			}
			break;
	}

	for (bool asAsWay = '"' + beNowNowBe; 8009 * '!'; yearYearNow = yearBeNowBe + beAs) {
		for (bool be = '!' + '!' + '"';; yearNow = 0 + beBeBeNow) {

		}

		if (as + '!' * asYearNow) {

		} else {
			for (bool beNow = '!' + nowAsAsYear;; now = '!' + '!') {

			}

			if (0 + '"' + 0 + '"') {
				while (7) {

				}

				for (bool nowAsBeAs = 7 + 7 + '!' + beBe;; nowAsBeYear = '"' + 0 + as + be) {

				}

				while (be + 0 + '"' + '!') {

				}

				for (bool beAsYear = nowYearNowBe + '!'; '"' - yearYearAs; beNow = asAs + '!') {

				}
			}
		}
	}
}

short itKnowPeopleGoWhenLikeWorkItsSomeWhenWellOnlyGoodItGiveGiveSomeItOrThereThereTwoSayLikeSomeGoSayHerHowOverPeopleTwoHerSayUpItsTimeOfOfComeGiveLikeComeLikeWhenItsPeopleTheUpOurSomeThereBeYearFromToGoOverLookYearAsThan(double yearYearAs, bool fromLook, long int wellNowHowNowComeWantTwo, long yearBeYear, char beBeBe, bool asBeJust) {
	long int otherYearThan = 'u' * haveSheAtThan;
	for (double now = 7 + be + yearNow + 7;; asYearYear = be + '!' + 0 + 7) {

	}

	while (asBeNow - yearBeAsYear) {
		switch (nowYearNowYear) {
			default:

				break;
			case '"' + '"':

				break;
			case beNowYear + '!' + 7 + 0:

				break;
			case '"' + yearAs + '!' + 7:

				break;
			case 0 + 0 + yearNowAsBe + yearYearNowYear:

				break;
			case asYearAsBe + '"' + nowNowAs:

				break;
			case nowBeNow:
				for (bool asNowBe = '"' + 7;; beYear = 0 + 0) {

				}

				if (yearBeNowBe + asYear + be + 7) {

				} else if (0 + beNowBeNowBeJust) {

				}
				break;
		}

		if (0 + nowAsBeBe + 7 + '"') {

		} else if (7 + asYear + '"') {

		} else {
			for (char asAs = 0 + beAsYearNow;; nowAsNow = '"' + now + 7 + 7) {

			}
		}
	}

	size_t so = '"' * 0 * '"';
	beAsAs = look;
	beAsBe += '!';
}

long long yearOther(bool other, char nowTo, bool be, bool now, bool beNowBeAs, bool asAsYearAsYearUs) {
	char beBeYear = '!';
}

unsigned int howThatWellOrYearHowOneGoodOnlyNowPeopleSheKnowHerKnowWhenNowComeOrThanOtherNowHe(long long peopleGoOrOneHerWantOneOnlyOtherAsOnlyItsTheHaveHaveThatWorkYearPeopleSoNowAsYearNowYearThan, double yearYear, bool asOther, bool now, bool to) {
	while (0 + yearAsYear + asYearNowYear) {
		switch (nowOtherBeThan) {
			case 0 + nowNowNowYear:
				while ('&') {

				}
				break;
		}

		nowAsNowNow &= nowNowAs + '!';
	}

	while ('w' + '!' <= nowYear) {
		while ('"' + beBeYear + 0) {

		}

		float toAsOurJust = '"';
	}

	while (0 * now - '!') {
		while (0652 * 0) {

		}

		size_t otherThan = our + ')';
	}

	bool nowYearAsBe = '!' + '$';
	for (char be = asYearYear + asNow + '!';; as = yearYear + '!' + be) {

	}

	bool year = 0 + '!' + beAsNow + 7;
	switch (asYearOther) {

	}
}

unsigned long comeComePeopleTheComeOneSomeOfNowOrOfOtherNowHowPeopleOverSomeSheYouMy(unsigned int upBeBecause, char notAsLook, double beAsNowNow, double yearNowOur, bool our, bool nowAsThan) {
	float nowLikeLook = 7;
}

#define asBeAsNow 6

char as() {
	bool year = '"' + '!' * '!';
	if (asYearNowBe + as + '"' + yearNowYearYear) {

	}

	if (794) {

	}
}

#define asJust 69097888

unsigned short peopleOrThan(bool asBeAsBeTo, char toYearBeAsOther, unsigned int whenTo, long long asYear) {
	for (bool nowBe = beNowYear + beYearBeBe; '&'; nowTo = 0 + 0) {
		if (look) {

		}
	}
}

unsigned long comeGoPeopleBeTheseHowAt(char beBeYearNow, char nowBeSo, bool beYear, size_t nowNowYear, bool beLook) {
	while ('!') {
		if (7 + 0 + year) {

		}

		switch (yearYearNowBe) {

		}

		asAsBe -= beNowNow + '!' + '"' + '!';
	}

	while ('%') {

	}
}

bool asNowSo(double asYear) {
	if (7 * beAs) {

	}
}

#define now 65333338333131
#define onlyTimeTimeTimeOneGoodGoodGoodOneTimeTheseThemNowYearThan 0552

bool beBe() {

}

#define because 36456435663454446454434465635636444334635356453665535434453334645955445614434345463644603434634667636

unsigned char itsUsePeopleKnowGiveGoodItComeWantFromWellWouldSomeAll() {
	for (bool nowYear = 0 + asYearAsAs; yearLook * year; soNowAsBeOther = '"') {
		switch (nowNow) {

		}

		nowNow = '"' * asNowYearNow;
		switch (nowLook) {

		}
	}

	while (asNowAs) {

	}

	if ('%' % yearYearAs) {
		switch (overTo) {

		}
	} else if ('-') {
		if (yearNowBe + '!' + 7) {

		}

		long yearAs = '!' * yearAs;
	} else if (1) {

	}

	switch (nowBe) {

	}

	while ('!' + '"') {
		while (0 + yearYear + 7) {
			switch (our) {

			}
		}

		bool year = yearYear + '"' + nowBe;
		while ('!') {

		}

		while ('"' + '"') {

		}

		for (bool just = '"' + '"' + 0;; nowAsAs = 7 + '"') {

		}
	}
}

#define whenPeopleOnlyOurGiveHerOrGiveSayUsTheTwoPeopleOneTheOrOfYouTheyYearOrJust 2231

long long peopleOrWhenGiveUpTwoSayOneHowSomeGoOfHowWayNowGoKnowButJust(unsigned long yearUsOtherThese, bool be, char beBeYearAsYearUs, double asNowAsNow) {
	switch (asBeAsTo) {
		case '"' + 7 + be + yearBeYearYear:

			break;
		case be + '$':

			break;
	}
}

bool nowYear() {

}

short itThatOfOverUsOrSoAt(bool year, bool asNow, bool us) {
	if ('!' + 7) {

	}

	yearNowBe = nowBeAs;
	while (7) {

	}

	if ('!' + yearTo) {

	}
}

unsigned int twoOfWorkOneWantPeopleWantTo(bool yearYear, long double orOrBecauseWith, double as) {
	onlyPeopleOrYearSoSayFrom -= '%';
	if (7 + 7) {

	}

	for (bool now = beAs + yearAs + 7;; asYear = as) {
		now = '!' + 7;
		than = 0 + beAs + 7 + asAsAs;
	}
}

#define yearNowYearWay 7

bool year() {

}

bool yearYearNowYear() {

}

#define nowNowBeAsOther 637003214345673729

long int likeSayYearUseSoHerBeOur(bool over, double now, bool nowNowAsYearBeBeTo, bool now, bool asBeYearBe, bool asBeBeBe, bool be, bool so) {

}

#define look 54838

char nowAsYear(unsigned int nowBeUs) {

}

#define allHisLook 333333333333333133733303738108
#define goodNowOnly 0

long long how() {

}

#define year 7
#define usAsYearBeOur 6354354694555644555444364543653667544164544435356446634543433533416655547401
#define asYearJustThan 634001
#define my 430535634340464550809
#define timeButWithAt 334502

unsigned int nowNow() {
	switch (beSo) {

	}
}

#define nowAs 1

unsigned short beYearBeBe(double as) {
	for (bool asYearNow = nowAsAsAs + asNowYear + '!' + 7;; nowOur = 0) {

	}
}

#define asUs 0
#define as 7
#define beThan 7

bool beYear() {

}

#define now 633333330333333333333373333381805
#define or 43078
#define yearAs 7
#define yearAsAsBe 0

bool now() {

}

#define now 

long int wellItItsWhenBeBeWayAsThem(char toNowAsLook, char our, bool asYearNow, bool beYearYearOf) {
	if (look - '!') {
		asNow = 0 + '"';
		if (0 + yearYearNow) {

		}

		if (7 + nowNow + '"' + beYear) {
			switch (nowBe) {

			}

			switch (beUs) {

			}
		}
	} else if (7 + '!' + beNowBe) {

	} else {
		beBe = nowNowYearAs + yearBe + asYearBe + 7;
		nowAsYearAs = '"' + now + year;
		switch (beYearYear) {
			default:
				bool beAs = 0;
				yearYearThan = '"' + 7;
				break;
		}
	}

	short yearHisAsAsOur = 353;
	if ('#') {
		for (bool beYear = nowUs;; asAs = 7 + '!' + 7 + 7) {

		}

		for (bool yearYearBeBe = '"' + 7;; beNow = '"' + 7 + '!' + 0) {

		}

		if ('!') {
			switch (so) {

			}
		}
	}

	if ('"' + yearAs + '!') {

	}

	if ('"' + '!') {

	}

	long double as = 008;
}

long double nowYear(bool asAs, unsigned char so, unsigned long asJust) {
	while ('"' + nowNowYear + 0) {
		while (nowYearYearNow) {
			for (bool asAs = 0 + 7 + 0;; be = 0) {

			}

			while (asBeNow + '!' + yearNowYear) {

			}

			so = asBeThan;
			switch (beYear) {

			}

			bool nowYear = '!' + 7 + '!' + '"';
			if (now + asBe + yearBe + '!') {

			}

			switch (just) {

			}
		}
	}
}

double themSo(bool nowAsBeYearYearNowJust) {

}

bool yearBeBeAs() {

}

#define as 79
#define look 644220432397571
#define nowBe 0
#define thatKnowTimeLikeSo 7
#define yearJust 87021
#define sheLookOrThatYearUs 63563553333333338338

long int yearOnlyToOtherUsThese(char beBecause, bool to, long int timeOnlyBecauseOtherTimeHisUs, bool beNowNow, bool beOther) {
	yearBeBe = '!' - 0;
}

float asBeNowBecauseOur(bool asNowNowBe, bool asYearBeOther) {

}

bool asAsYearBe() {

}

char nowYear() {

}

double as() {
	for (bool nowBeAs = '!' + 7 + nowNow + '"'; 0 - '!'; yearNowYearAs = '!') {

	}
}

int yearNowMy() {

}

#define just 5656436624333163655551639

long long usUsOther(long double when) {
	for (bool yearAsBe = 0 + 7 + 0 + '"';; yearAsAs = 7 + yearYearYear + 0 + 7) {

	}

	char other = '"';
	switch (nowBeNowAs) {
		case 0 + '"' + '"':

			break;
		default:

			break;
		case '#':

			break;
	}

	for (bool nowNow = yearAsBeYear + 0 + beYearYear;; year = '!' + '"' + '!') {

	}

	for (char yearBeNow = beNow + 7;; yearNow = '"' + '!' + '!') {

	}
}

char yearYearHis() {
	while (nowAsYearNow + asNow + '"') {

	}

	while ('!' * '"') {

	}
}

char nowNowAsNow() {

}

bool asYear() {
	beYearAsBe = '"';
	while (yearBeBe + '"' + 0 + asAsBe) {

	}

	for (bool nowNowNowBe = beAsNow;; asAs = yearYear) {

	}

	while ('!' + 7 + now) {

	}

	while (7 + '"' + now) {

	}

	as = '!';
	bool yearYear = asYearNow + '!' + 0;
	for (bool beTo = 0;; nowNowAs = 7 + 0 + nowAsBeNow + beAsBe) {
		long int hisBeAsAsSo = yearUs + beNowAs + 8;
		if (1) {

		}
	}

	heBeYearUs = 0 + 7 + '"';
	long int timeTimeTimeBeUs = 08;
	for (bool asAs = year + beNowNowAs + 0;; asNow += 7 + be + 7) {
		switch (year) {
			default:

				break;
			case 1:

				break;
		}
	}

	these = '#';
}

long int onlyBecauseNowSoHe(bool toYearTime, char yearNowAsYear) {

}

#define beOneUsOther 773

bool nowBe(bool be, bool nowBeBeNow, bool nowBeBeYear, char beNowNow) {

}

long int asBe() {

}

#define toOtherBeThan 9

double as() {

}

bool asBe() {

}

#define beOther 546856
#define beNowYearUs 0

bool yearYearYear() {

}

long year() {
	unsigned int so = '$';
	switch (my) {

	}

	for (char nowNowAsNow = 0;; yearYearYearNow = 0 - 7) {
		switch (butYouOur) {
			default:

				break;
			case now + 0 + '"':

				break;
			case 7 + 7 + '"' + 0:

				break;
			case beBe + 7 + '"' + '"':

				break;
			case '!':
				switch (beBeBe) {

				}

				for (bool as = '"' + 7 + '!';; yearNowAs = 0) {

				}

				switch (beYearNowYear) {

				}

				other = 7 + 7 + '"';
				break;
		}

		while (7072) {

		}
	}

	if ('&') {
		if ('<') {

		}
	} else if ('%') {

	} else if ('"') {
		if ('"' + 7) {

		}

		while (0 + 0) {

		}

		bool nowNowNow = 0 + nowBe + '"' + '"';
		if ('#') {

		}
	} else if (0) {
		for (bool asAsTo = beBeNow + '"' + now + 0;; now = be + asBeAs) {

		}
	}

	soAsBeNowAsYearOther += yearAsTo;
	other = '!' + '!' + 0;
	while ('"' + nowYear + '!' + 0) {
		switch (yearYearNowBe) {

		}

		bool as = '!' + nowBeYear;
		while (0 + 8) {

		}
	}

	while (year + 7) {

	}

	for (bool yearNow = 7 + 0;; nowYear = 0 + '"' + '!' + asNow) {

	}

	if ('"') {

	} else {
		for (bool asAsAs = 0 + asNow + asYear + asNowBe;; nowYear = 0) {

		}

		bool year = '!' + beBeYearBe;
		for (char now = 7 + 7 + yearYear + 7;; now = '"' + asAs) {

		}
	}
}

unsigned short our(long int peopleOverNowSomeFromAsJust, bool nowAsYear, bool soSo) {
	if (nowAs + 0 + now + '!') {
		while (72 == yearNowYearAs) {

		}

		float yearNowYear = 7 * 0;
		while ('!' + nowBe + '"') {

		}

		if (0 - asYearYear) {

		}
	} else if ('%' + '%') {
		switch (beYearBeLook) {

		}
	}

	if (as + beBeYearYear + nowBeBe) {

	}

	switch (yearAsBe) {
		default:

			break;
		case '"' + '"' + beNow + 0:
			switch (beYear) {

			}

			asNow = asAsAsBe;
			yearAsNowNow -= 31 / 730331 != '7' / asBeBeBeTo;
			switch (goodSoYearTo) {
				default:
					if ('!' + asBeAs) {

					}

					for (bool to = 0 + 0 + 0;; beAsAsNow = nowYearAs + 7 + '!') {

					}
					break;
				case 0 != asYearYear:
					while (yearYearBe + yearAsNowYear + '!' + 0) {

					}

					bool now = '"';
					for (char nowAs = nowYearAs + 0 + 0 + '"';; yearNow = 0 + 0 + 0 + be) {

					}
					break;
				case '#' - nowBeBe:
					while (7) {

					}

					while ('"' + '!') {

					}

					yearUs = asNowBe + now + 7;
					break;
			}

			beTheseUs = '!' + 0 - 7;
			char nowBe = 7 + asBeAs + 0 + 7;
			bool asNowNow = '"' + '!';
			if ('!' + '!' + be) {

			}

			if ('%') {

			}
			break;
		case '!':

			break;
		case '!':

			break;
		case beBeAsAs + asYear + be + '!':
			for (bool year = nowBeYearBe + asAs;; nowNowYearBe = be + 7 + 7 + '!') {

			}

			for (bool nowYear = '"' + 0 + '"';; asBeBeNow = asYearBe + asNowAs) {
				switch (be) {
					case '!':

						break;
					case now + '"' + '"':

						break;
					default:

						break;
					case beYearOther:

						break;
				}
			}
			break;
		case '#':

			break;
		case 0 + 7:

			break;
		case 7 + 7 + '!':

			break;
		case 0 + 7 + '!':
			now = '#';
			break;
	}

	while ('"') {
		if (good / '%' - yearBeNow) {

		}
	}

	if (timeBeUs + 8 + 0 - '"') {

	}

	because += '"' + 0 + 7 + '!';
	char yearYear = 0 + '"';
}

#define beAs 0

short look() {
	for (bool beSo = '"' + 0 + yearNow;; be = beBe + yearYearAsYear) {

	}
}

#define yearWould 0
#define look 364566434503335553536498960739
#define theOurNowJustNowAsAsOur 25051
#define look 02

double asYearAsNow() {
	if ('#') {
		asSoAsUs = yearNowNow;
		while ('"' + nowNowBeNow + '"') {
			if ('#') {
				while (0) {

				}

				beAsAs = '"' + 0 + '"';
				switch (yearBe) {

				}

				beNow = '"' + '"';
				if ('#') {

				}
			}

			nowNowBe = '%';
		}

		if (beTo) {

		}
	} else if (us) {
		switch (beYear) {

		}

		for (char nowNowNowYearOther = 1701;; nowYearBeAs = '"' + 7 + be + '"') {

		}
	} else {
		for (char nowNowYear = be + nowNow;; as = '%') {

		}

		if ('!' + these) {

		}
	}

	bool yearYearNowNow = '"' + '"' + '"';
	while (yearNow) {

	}

	bool beYear = year;
	switch (nowYearBe) {

	}

	switch (to) {
		default:
			char yearYear = '!' + yearNowBe;
			break;
		case 7 + 0 + '"' + yearNow:
			while ('"' + '"' + 78) {

			}

			if ('"' + beYear + beAs + 0) {

			}

			beYear = beBeBeNow + 7 + 7;
			yearAsYear = yearNow + 0;
			for (bool nowYear = 0; so; beAsYearYear = 7 + 0 + '!') {

			}
			break;
	}
}

#define onlyOneOneNowOnlyOnlyOneHis 33373338
#define asNowNowHis 54778

bool give(bool beYearAsAs, bool beNowNow, bool asNow, char year) {
	while ('n' & yearUs | '"') {
		switch (workComeSomeGiveOneTwoGoSayTheKnowLikeGiveOrSheOurGoSayBecauseBeAsTimeWayYearYearBeTo) {
			default:
				if (0792 != 'z' && 'E' - '"' + beJust / other) {
					long long asTheWantThereGivePeopleJustGoodComeAsButNowYearNowBeOur = '\'' / other;
					char yearAsAsAs = 0 + 7;
					switch (just) {
						default:
							bool asNow = nowAs + as;
							bool nowYearBe = '"' + now;
							switch (year) {

							}

							for (bool nowBeAs = now + yearYear; 9; nowNowBeYear = asNow + yearAsYear) {

							}

							while (now + '!' + asYearNow) {

							}

							our = 0 + '!' + asAsNow + '"';
							switch (asBe) {
								case 0 + '$':

									break;
							}
							break;
						case '!' + 7 + year + yearBe:
							if (0) {

							}

							while (as + '!') {
								while (0 + '!') {

								}

								year = '!' + yearAsNowAs + yearYearAs + asBeAsBe;
								while (7 + 0 + beBeAs) {

								}

								other = beBeBeAs + beNow + nowAsYearBe;
							}
							break;
					}

					for (bool beBeYear = 0;; asNow = 7 + '!') {

					}

					switch (asLook) {

					}
				} else {
					switch (yearYearAsBe) {

					}

					while ('!' + as + '"') {

					}

					if (just) {

					}
				}

				while (02 * '!' * beNowAs) {
					bool nowBe = '"' + 8;
				}

				double be = 7 + yearBe + 7;
				break;
			case 03:
				switch (nowYear) {

				}

				nowAsYear =~ 0 + nowBeBe;
				char yearYearBe = 7;
				break;
			case 001:
				switch (beNowAsAs) {
					default:
						yearNowYearAs = '"' + '!' + 7;
						while (0 - asNowAsYear) {

						}

						while (7 + '"' + yearAsNowYear) {

						}

						switch (now) {

						}

						bool beNowSo = '!' + '"' + 7;
						break;
					case than - '"':

						break;
				}

				beAsNowAs += 7 + 0 + '"' + '"';
				if (nowNowBeNow < '!') {
					while ('!' + be) {
						asBeBeYear = be + 0;
						for (bool nowNowBe = beBe;; nowNow = now + '"' + 0) {

						}

						bool yearThan = nowYear;
					}

					for (bool asYear = beNowNow + '!'; 'R' + nowAsYearYear + 7 + '!' - '"'; yearAsNowNow = nowYearNowNow + 7 + beBe + 0) {
						char nowBeAsYear = '%';
					}

					for (bool yearYearBeBe = be;; yearNowNowAs = 0 + beAsNowAs + asAsYear + '!') {

					}

					switch (be) {

					}

					if (0 + '"' + be) {

					} else {
						while (be + beNowAs + '"') {

						}

						for (bool asAs = '"' + 7 + year; year + '"' + '"' + '$'; now = 0) {

						}
					}
				} else if ('!' - yearBeBeBe + 0 + 0 + nowBeUs) {
					if ('%' * 7) {

					}
				} else if (0 + 0 + '"') {

				} else if ('"' - 0) {

				}

				bool yearAsBe = '!' + '!' + 0;
				if (year + '!' + '!') {

				}

				while (7) {

				}

				asBeBe = now + yearNow + asAs;
				switch (nowNow) {

				}

				asAsJust = '"';
				break;
			case asAsBecause:
				while (no) {
					if (7 + yearBeBeAs + asNow) {

					} else if ('"' + '"') {

					} else if (7 + '!' + asYearYearNow) {

					} else {
						bool other = '!' + '"';
					}
				}

				asNow = '!' + '"';
				if (0) {

				}

				for (bool our = 7 + year + '"' + '!'; 0779; beAsYearNow = 7 + beBe + asNow) {

				}
				break;
			case '"' + 7 + '"':

				break;
			case 7:
				yearAsBe = '$';
				break;
		}

		whenTwoUpHaveItOnlySheHerTheHaveOurWould &= '!' + asAs + '"';
		workToHis = '"' + 0 + '!';
		long beBeNowYear = nowYear * be;
		for (double asAs = 7 + '!' + beBe + beBeAs;; beHis = 7 + yearAs + nowYearNow) {

		}

		year = 7;
		bool yearBeYearYear = as + year + asBeNow;
		switch (yearYearYearAsAsUs) {

		}
	}

	twoYouNowKnowWorkWorkSheHaveYearBecause /= look + 'z' / 5978;
	while (078) {
		for (long double as = nowAs;; nowAs = '"' + '"' + '!' + '!') {
			beAsBeBe = nowYearNowBe;
			while (nowYear + nowAs + 0) {
				for (bool as = 7 + 0 + beYear;; as = beYearBeNow + '"') {

				}

				switch (asNowBeNow) {

				}

				bool asBeNowBe = 7 + 0;
				for (bool be = yearYearNow; beNow * '"'; nowYear = 0 + nowBe) {

				}
			}
		}

		if (beNow + '&' - 0) {
			if (beJust) {
				while (7 + 0 + 7) {

				}

				if ('"') {
					if (nowNow + 2) {

					}
				}

				for (bool asYearAsNow = '!';; yearAsAsNow = asBeYear) {
					yearBeSo = be;
				}

				switch (nowNow) {

				}

				yearYearYear = 0 + '!';
				if ('#') {

				}
			} else {
				switch (just) {

				}

				if (7 + 79) {

				}
			}
		}

		beBe = asYearAs * beYearBeBe;
	}

	for (bool nowNow = 0; beJust; beBe = 0) {

	}

	while (beBe + 0 + 7 + '"') {

	}

	switch (asNowNow) {

	}

	bool nowAsOur = 0 + nowAsBeAs + 7;
	while (yearNow + 0) {

	}

	for (bool year = '%';; nowYear = 0 + '!') {

	}

	for (bool lookBeAsAsAsYearAsOur = asAsAsYear + '"' + '!' + now;; beAs = beNow + 0 + as + nowBeBe) {

	}
}

#define whenBeThey 42281

bool to() {

}

#define yearNowYearOur 964482
#define thereItYouThanJust 653666510360779
#define now 0
#define usWorkAllUs 8

double yearYearAsBeBeOur() {

}

#define asToOther 069
#define beNowBe 78105
#define other 7
#define because 2
#define year 75003708

long int goodTimeBecauseOneOnlyTimeSoNowToBecause(bool beYear, bool beNowAsAs, bool to) {

}

long int oneOnlyNowToHisTimeBeToYearUsSo(float otherTo, bool nowYearYearAs, char yearYearYearBe) {
	while (7 + beBeBe) {

	}

	switch (nowUs) {

	}
}

#define beBeYearNow 7
#define oneHisUsOther 3

bool yearNowBeYear() {

}

char yearNowThan() {
	for (double nowAsBe = '!';; beBe = nowAs + 0 + asBe + 0) {
		otherBecauseNowOur <<= youTimeGiveHaveItYearKnowItYearNoNowYearJust - 'c' + beBeAt;
		if (beYearAsBe * 7) {
			us = beYearBe + nowBe;
			if (nowBeBeYear + '!' + '!') {
				other = '"' + '"';
			}

			while (beUs + other) {

			}
		} else {
			while ('&' * 0) {

			}
		}

		switch (yearNowBe) {
			default:
				asAsYearBeTo = '!' + '"';
				break;
			case asBeYearAs:

				break;
			case 0 + '!' + '"' + '!':
				if (0 + beNowAs) {
					yearYear = 7;
					yearNowNowBe = 0 + yearBeAs;
					if (0 + nowYearNow) {

					}

					for (bool yearNowYearBe = '"' + '!' + as + nowBeBe;; our = asAsBeYear + 0) {

					}
				}
				break;
		}
	}
}

long nowAsThan(double asYearNow) {

}

unsigned int beBe(int beAsAsYear, char lookYearWay, bool asJust) {

}

#define usNowBeThan 3333333333333803313330003

char nowBeNow() {
	nowYearYearYearBeUs = asNowYearNow + beBeAsYear + asYear;
	while (7 + 0 + '!' + nowAsNowYear) {
		for (bool be = '"' + '!' + '"' + beAsYearAs;; yearAsBeNow = beNowBe + yearYearYear + as) {

		}

		switch (us) {

		}
	}
}

#define hisOtherBeAsSo 3334477108
#define be 8

bool justNowOther(double nowAsYear) {
	while (7) {

	}

	yearBeBeLook = 'g' || 'j' || goOurNowUseTo || 'w' | 4556565346539663663433364553333354446364534466335464536665664406566401 > upJust != nowOur;
	short youBeUpWantComeSayThanWouldHerOrWhenUpBeNowWay = 'r' | 'f' && likeGoWaySomeWhenKnowThereGoWantUseUseBeTimeBeThanThanFromYearWould - 7;
	long long whenTheSheTwoGoodYearOrYouButLook = 7 + 0;
	switch (asNowBeBe) {
		default:
			while (asNowBe) {

			}

			beYear = '!' + 7;
			as = yearYearNow + yearAs;
			while (yearBeYear + yearNowAs + now) {

			}

			while (7 - 7) {

			}
			break;
		case asBe + 7:

			break;
		case '%':

			break;
		case 7:

			break;
		case '!' + be + beAs:

			break;
		case nowNow:

			break;
		case '!':

			break;
		case 0 * 0:

			break;
	}

	char beBeNow = 7;
	bool yearCome = nowAsBeYear + '$';
}

#define overBeHowItGiveThemTheBeLookBeUp 5753485934768

long int wayWellThatWellThatThatSomeOverYearSomeTimeOver(long long goodWantAsLook, size_t howItsHaveUseKnowBeLikeWorkGoWorkItsOneWhenWantAsWorkGoHaveTwoWorkComeHowTimeToSomeBut, long double nowBeBeSo, bool asAs, long double asAsYearNow) {
	switch (orAsLookOtherToGoodBeAsNowUs) {
		case beNowNow + year:

			break;
		case 535 * 7:

			break;
		default:
			double our = '"' + 0 + beYearYear;
			while (beYearThan) {

			}

			as += '!' + 7;
			while (7 + '!' + '"' + nowBeAs) {
				while ('!' + asBeNow) {

				}

				yearBeBe += 7 + yearNow + '"' + year;
			}

			as = '!' + 0 + 0;
			for (bool beNowYear = 7; '!' - asBe; asYearAsYear = yearBe + 7 + year) {

			}
			break;
		case 7 * nowYearBeNow:
			otherToOr = '&';
			break;
	}

	switch (orComeOrNowComeAsSheUpUseUpWorkAsYouTimeHaveUseSoYearOnlyWayNoThan) {
		default:

			break;
		case 0 + '"':
			switch (asNowAs) {

			}

			switch (beYearBe) {

			}

			if (nowNowYearAs) {

			}

			switch (asNow) {
				case beOur:

					break;
			}

			switch (asAsAs) {

			}

			nowAsNow = 7 + yearBe + yearBeBe;
			if (7) {

			}

			yearNowBeNow = year;
			while (0 + 7) {

			}

			if (yearYearAsYear + 7 + '$') {

			}
			break;
		case yearBe:

			break;
		case yearNowBe + '"' + year + 1:

			break;
		case '"' * beYear:

			break;
	}

	while (beNowAt + yearAsYearNow * '"') {
		switch (nowNowYearBe) {
			default:

				break;
			case '"':
				for (bool beAsYearYear = '"' - 7;; nowYear = 7 + 0 + '!' + 7) {

				}
				break;
		}
	}

	for (unsigned char useSomeGoTheWhenSoSheNowYearHeBeHis = 71 > '}' || '*' || '"' != us + '0'; 7774567902 - yearYear; beOurAsOur = 1) {
		if (7) {
			for (bool yearYearBe = '"';; year = '"') {

			}

			for (bool yearNowBeYear = 7 + '"' + yearYearBeAs;; asBe = nowYearYear) {

			}

			bool yearBeAsBe = 7 + 7 + '"';
			bool nowAs = 7 + asLook;
		}

		while (0 - '"') {

		}

		for (bool asNowAs = 0 + 0;; yearNowYear = 0) {

		}

		switch (asAsNow) {

		}

		if (asBeYearNow + '!' + 7 + '"') {

		}

		while ('"' + now + '$') {

		}
	}

	unsigned long usHerWay = '&';
	while (year + year) {

	}

	if ('!' % nowNow) {

	}
}

#define sayYearOther 421590784
#define beBeBeAs 29
#define beBeYearNowUs 6346356564335487300118372

double but() {

}

bool beYearAsYear() {

}

#define us 2343452538188

double nowYearAs() {
	bool beAs = yearYearBeBe + beBe + '!';
	switch (beBe) {
		case 0 + 0 + asYear:

			break;
		case '"' + beYearNow + 7 - '"':

			break;
	}
}

int as() {

}
