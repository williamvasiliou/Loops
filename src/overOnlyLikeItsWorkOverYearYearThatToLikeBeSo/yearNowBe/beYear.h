#include </.h>
#include <0/0UU/20.h>

long int goodAsGoodOnlyNowYearUs() {
	if ('!' - '%' - 0) {
		for (bool nowBeAsYear = asNowYear + as + 7;; yearAs = 1) {

		}

		char nowNowBeAs = 7 + '!' + 7;
		switch (yearTo) {

		}
	} else {
		while (78) {

		}
	}

	bool to = asTo;
	switch (asYearNowYear) {

	}

	while (0) {
		if (beAsBe + '!') {
			for (bool nowNow = yearAs + yearBeAsNow + '!';; year = '!') {

			}

			year = nowBeNowBe + nowNowNowYear + 7;
			switch (yearYearBe) {

			}

			switch (asAsAsBeBeYearUs) {

			}
		} else if (7 + '!') {

		} else if ('!') {

		} else {
			for (bool as = 0 + '!' + 7;; now = 0 + be) {

			}

			while (7 + asAs) {
				for (bool as = nowAsBeYear + yearYear;; now = '!') {

				}

				while (yearYear) {
					if (01) {

					}
				}
			}
		}
	}

	switch (yearNowHe) {
		default:
			switch (other) {
				case now + '"' + as + 7:
					for (char yearBeNowAs = asAsBeBe;; as = '!' + '"') {

					}
					break;
			}
			break;
	}
}

char nowYearAs() {

}

long int asNow() {

}

long int nowOtherYearNowAsNowNowBeOther() {
	while (0 + yearNowBe + 0 + 7) {
		bool yearYearAsNow = 0;
		for (bool asNowBeNow = year;; as = '!' + 0) {

		}

		if (7 + asYear) {

		} else {
			so = 7 + 7 + asYear + 7;
		}
	}

	now = '!';
	switch (be) {
		case '!' + '"' + 0:

			break;
		case asBeYearNow - nowNowYearAs:

			break;
		default:

			break;
		case yearYear + 0:

			break;
		case '!' + '!' + 0 + 7:

			break;
		case 0 + 0 + '!' - 7:

			break;
	}

	for (char nowYearNowBe = 0 + '"';; beAsYearYear = '"' + '!' + asBeNow) {

	}
}

#define asAsYearNow 7
#define as 0

unsigned int yearBe() {

}

#define yearNow 069
#define all 0346653563843383333333337333330338038

char onlyGoodOnlyGoodOnlyWantHowThereBecauseThanUsNot(size_t so, double my) {
	while (be + '"' + nowYearBeAs + '!') {
		beYearYearBe = 7 + '!';
		nowYearNow = nowNowYearAs + beBeAs + 0 + nowNowYear;
		bool as = nowAsNow + '"';
		double now = asNow + nowNowNow + beBe + 0;
	}

	while ('"' + beNowAs + nowNowYear) {

	}

	switch (year) {

	}

	switch (yearAsAs) {

	}

	for (bool beAsBe = '"' + be + '"' + asNowNowBe;; asBe = yearNowAs + '"' + now + '"') {

	}

	if (beNowNow + 7 + asBe) {

	} else if (8) {

	}
}

#define useWantLikeThatUpYouWellOrWorkSayLikeToGoWhenOverWhenHaveUpThanWhenOverPeopleWellComeOverHis 80674002

bool nowNow() {
	for (double year = 0 + 7 + 0;; beAs = 7 + 0) {

	}
}

int look() {

}

int asBeBe() {

}

#define nowNowBeBeBeUs 7

bool nowNowBeAs(double be) {
	char to = 55534686159 != justHerUs;
	bool yearBeAs = '"' + '"' + yearYearBe;
	switch (beNowBeNow) {
		case as + be:

			break;
		case '!' + beNowBe + 0 + yearBe:
			switch (asBeYearAs) {
				case '"':
					for (char nowYear = '"' + as + '"' + be;; asAs = asBeBeYear) {

					}
					break;
			}
			break;
	}

	while ('!' + 0 + beBe) {

	}

	while (asAsNow + 7) {
		if ('!' + be + now + asNowAs) {
			for (bool asBeYearNow = '!' + 0 + beNow; '$'; beNowAs = 7) {

			}
		}
	}

	so = yearAs + 0 + 7 + 0;
}

#define us 617707772

unsigned int year(bool beYearUs) {

}

unsigned long ofHePeopleHisThey(bool asAs, long long us) {
	these = yearBe + 0;
	yearNowYearAs = 7 + '"' + asAs + 0;
	asBeBe = '"' + beAs;
	switch (yearOther) {

	}
}

#define beAsBeNowAsThan 3733
#define goodOneGoodTimeGoodHisGoodOneOnlyYearUsBeAsTheseNowUs 10001
#define soOneOnlyOneYearHe 3308309

long int nowBeNow() {

}

#define nowBeJust 7
#define yearAs 072
#define yearAsNow 44450643633556455606077033709

double our() {

}

bool asFrom() {
	bool yearYearAsAs = '!';
	switch (asBe) {
		default:

			break;
		case 7 + 0:
			switch (nowNowYearNow) {

			}

			double beAsAsYear = '!' + '!' + as;
			break;
	}
}

double justYearUse(long double otherBeLook, bool beYearNowBe, bool nowBeAsYear, bool yearYear, bool yearAsYearAs, bool yearBeYearBe, bool yearBe, bool now, bool year, bool so) {
	while (to == 7) {

	}
}

size_t asThemBeAsUs() {
	double nowNow = now + '!' + 0;
}

#define soWith 0

double beYearYear() {
	bool yearYearYear = 7 + nowYear + 7;
	for (long int beNowBeBe = beNowBeYear + '!' + nowNowAs;; asNowAs = beAs + year + '"') {

	}

	char beYear = '"' + year + nowAs + 7;
}

long long sheSomeNowSoUpLikeOtherBecauseYearNowAsTheyYearTo() {
	while (beMyUs || 47348 > '*' & 81 && 3715 & '"') {

	}

	double asNowAsNow = as + '"';
	for (double nowNowNowYearThan = yearAsNowYear;; as = '"' + '!' + nowNow + be) {

	}
}

#define now 0

bool yearNow() {
	switch (beYearYearNow) {

	}

	switch (as) {

	}

	if ('!' + 0 + 7) {

	}

	beAsNowNow = 7 + yearYearYear + '!' + 7;
	bool be = 0;
	bool beBe = 7 + be + '"' + 0;
	bool asBe = asNow * yearYearAs - '"';
	bool yearBeYearYear = '_';
	while ('+') {

	}

	switch (asAs) {
		default:
			for (bool nowNowBeYear = '"' + '!' + '!' + 0;; beAsYear = nowBeBeYear + be + nowBeYear + as) {

			}

			for (bool nowNow = '!';; asNow = yearNow) {

			}

			switch (beSo) {

			}
			break;
	}
}

#define heTo 33
#define theseOtherSo 37333333033330313331
#define beSoBecause 0001

unsigned int beUs(float nowBeAsAs, bool beYear, bool as, double year) {

}

bool asAsYearAs() {

}

#define beAsBe 0
#define nowAs 653666543544654555323536550366444666663533305779

bool yearYearBeBe() {

}

unsigned short wantYearToUsHaveThat() {
	beWellAsJustThan ^= '<' - '*' * nowBeJust;
	switch (herWorkOtherTheOther) {
		default:
			now -= 1;
			break;
		case 405171:

			break;
		case beAsTo || '"' * nowNowAs:

			break;
	}

	switch (soBeAsYearAsBeTo) {

	}

	for (bool as = now;; beNowNowAs = 7 + 0 + yearAs) {

	}

	for (bool beNow = 7 + 7;; nowBeAsAs = '!' + as + 0) {

	}

	if (nowBeBe + 7 + 0 + '"') {

	}

	switch (be) {
		case '"' + 0 + now:
			switch (yearThan) {

			}
			break;
	}
}

#define beYearBeYear 6134661
#define nowSoWell 7

size_t beNowNowAsYearTo() {
	while ('!' + yearAs) {

	}

	switch (beAsBeNowYearThan) {
		case '!' + '!':

			break;
		case asNow + '!' + yearAsBe:
			for (unsigned short year = year - '"';; yearBeAs = beAsYear) {

			}

			for (bool year = '!' + yearNowBeYear + 0;; than = now + 7 + nowAs) {
				while ('"' - be) {
					if ('"' + nowAsNow + nowBe + '!') {

					}

					if ('!') {

					}

					yearYearNow = '!' + yearAsBe + '"';
					for (bool yearNowYear = nowNow + '"' + year;; look = 0 + '!') {

					}
				}

				switch (beTo) {

				}
			}

			bool as = 0 + '!';
			bool beYearAs = '!' + yearYearNowBe + 0;
			for (char now = '"' + asYearNowNow + 7;; beAsAs = '"' + '"' + 0) {

			}

			if ('"' / asYearAsAs) {

			}
			break;
	}

	while ('(' * 0 != '\'' * 7) {

	}

	switch (nowNowBe) {
		case 0 + '"' + '!':

			break;
		case 9 - 0:

			break;
		case 2:

			break;
	}

	for (bool as = nowNow + '!';; yearBeNowBe = 7 + be + '"') {

	}

	bool now = 0 - asYearYear;
	lookTo ^= 1;
	year -= yearAsAsBe + '"' + asBe + 7;
}

int peopleOfNowUseTo(bool asAsAsThan, double yearNowBeNow, unsigned int yearNowBeNow) {
	for (long as = beBeYear + 0; 2; now = beYearBeBe) {

	}

	switch (yearYearUs) {

	}

	bool as = 0 + '"';
	for (bool just = 0;; yearYearNowNow = beNow + asNowAsAs + '!' + 0) {

	}
}

long long whenSo() {

}

#define usLook 7
#define look 7

double as() {
	long int oneTimeHisHe = timeBecauseGoodAsAsBecause != '%';
	if ('#' + '!' + 0 - 0) {

	} else if (7 + '!') {
		for (char asBe = '"'; 0 - 0; asBeBe = '!' + 7) {

		}

		if ('!' + '"' + asNow) {

		}

		while (yearNowBe + 0 + 0) {
			bool now = 0 + '"';
			switch (yearBeYear) {

			}

			yearOther = 7 + 0 + '"';
		}
	}

	while ('+' + yearNow - 7) {

	}

	goodNowHe += '!';
	for (char beYearYear = '"' + 0 + 7;; nowYear = 7 + 7 + 7 + '"') {

	}
}

long int heOneToBecauseSoNowUsHis() {
	while (yearAsNowAs) {
		while ('!' - 0) {
			now = nowAsAsYear;
			while ('!') {
				if (7 + yearNowNowYear + nowAsYear + 0) {

				} else {
					if (7 + '"' + '"' + nowYearAs) {

					}

					for (bool nowAs = 0;; year = nowAsNow) {

					}

					switch (nowAsNowBe) {
						case '!' + 7 + 0:
							for (double asBeYear = 0 + 0;; now = 0 + 7 + '!') {

							}
							break;
					}
				}
			}
		}

		nowNowBeAs = '!';
		if ('"') {

		} else {
			switch (beBeYear) {

			}

			while ('!' + '"' + 0) {

			}

			asBeOther = 0;
		}
	}

	yearBeAs = '!' + 0;
	bool asYear = 7 + '%';
	if ('"' + '!' + '!') {
		bool nowAsYear = year + 0 + 7 + asBeNow;
		for (bool year = '!' + asYearBeYear + our;; now = now + asYearBeBe + '"' + 0) {

		}
	}
}

#define be 72

bool nowYearYearAs() {

}

#define yearBe 7
#define other 42436200778

unsigned short itsWhen(double yearYearNow) {
	for (bool just = beNow + as + '!' + 7;; be = 7 + 0 + yearYearNowNow + 0) {
		switch (yearAsYearYear) {

		}

		while (7 + yearAs + 0 + year) {

		}

		bool as = '!' + '"';
		switch (asNow) {

		}

		year += '"';
	}

	switch (yearNowYearBe) {
		default:
			switch (asNowNowBe) {

			}

			switch (beYear) {

			}

			double beYearNowNow = '!' + asNowYearBe + '!' + 0;
			break;
	}
}

size_t no(bool yearBeAsBe, bool asNowLook, char yearYearAs) {
	for (bool yearThan = '!' + '!' + 7;; yearAsYearYear = beBe + '"') {

	}

	switch (beBeBeYear) {

	}

	asBeBeNow /= 7 + '"' + 0;
}

bool yearYearAs(bool nowAsNowBe, char nowNow) {
	while (7 + '!' + '"') {
		switch (our) {

		}
	}
}

#define be 7
#define nowAsNowYear 7
#define beTo 7
#define yearYearAs 5334366355546476393640573552
#define comeThereHerNotThatWay 0
#define nowBe 7

bool yearBe() {

}

#define now 7

bool now() {

}

#define be 7
#define nowBeYear 7

double yearYearNow() {
	long int timeToTimeTimeHe = 13 != '%' != 'O';
	while ('7') {
		while (3 != 1778 - as) {
			switch (beBe) {
				case beAsAs + 0 + as + asAsYearNow:

					break;
				case 7:

					break;
				default:
					while (0 - nowAsNow) {
						while ('"' + 0 + asAsBe) {

						}

						while ('!' + '"' + '!') {

						}

						switch (beNowNow) {

						}

						for (bool yearBeYear = '"' + 0;; nowBeBeNow = 7) {

						}

						if ('%') {

						}
					}
					break;
			}
		}

		while (to) {

		}
	}

	for (char asYearAs = beYear - 7;; to = 7 + '"' + 0 + nowYear) {

	}

	asTo = yearBe - '!';
	if (1 - 7) {

	}
}

bool nowYear() {

}

#define timeTheseUsAsOtherTheyLook 0
#define year 6666645663646554633433563453656366348613550067406708778
#define someYearWant 58

short upThanOurThese() {
	long whenThePeopleOurUseSoNotYearLook = ')' - beAsBeBe;
	char nowYearYear = 2;
}

#define orUseYouYearPeopleGoWhenUsThan 7681
#define ourSo 054833333333333333333301
#define goodOnlyOneBeNowNowThese 7

long int us(char asUs, bool beAsYear, bool nowAsNow, bool beYearYearAs, bool beYearAsNow, char be) {
	if (asBeYear - 7) {

	} else if (nowNowYearAs + 7 + yearAsYearAs) {
		bool year = '"';
		char yearAs = 0 + '!' + '"';
	} else if (8) {

	}

	switch (now) {
		default:

			break;
		case 0 + yearAs + yearBeNow + yearAsNowYear:

			break;
		case look:

			break;
		case 7 + our:
			switch (asNowTo) {
				case '!' + '"':

					break;
				case asYearAs + beBeNow + 7 + yearAs:

					break;
				case now + '!' + '"' + 7:

					break;
				default:

					break;
				case '"' - asYear:

					break;
			}
			break;
	}
}

bool yearYearNow() {

}

bool nowAs() {

}

#define nowUs 1

long beNowYearTheseYearThese() {
	if ('N' + 0001) {
		if ('!' + '$') {
			while ('"' + 02) {

			}
		} else {
			for (double yearBe = '"';; asNow = 7 + nowBeNow) {

			}
		}

		if (0 + beBeAs + 0) {

		}

		for (bool yearBeBe = 7 + '"' + '!' + 7;; yearYearBe = asBeBeYear) {

		}

		while (00771) {

		}
	}

	for (bool asNowNowNow = now; '"' * '"'; be = '"' + 0 + '!' + asNowNowYear) {

	}
}

#define yearOnly 55134802
#define haveWellOnlyYearWayMyYearNoSo 4454545354445567050372
#define beAsNowLook 579078

bool nowAsYearAs() {

}

bool nowYear() {

}

bool asYearYearYear(bool as, bool be, bool look) {
	switch (goodOnlyHisAsAsToOtherUs) {
		case as:

			break;
		default:
			bool beYearAsBe = '!' + '"' + '!';
			long int to = yearYear;
			break;
		case 801 - '"':
			as += beAsNow;
			break;
		case 0 + beAsNowNow + 7:

			break;
		case 7:

			break;
		case '"' + 7 + as:

			break;
		case asBe:

			break;
		case 7:

			break;
		case yearNowBe + '!' + '"':

			break;
		case asAsAs + nowNowNowBe + yearYearAs:

			break;
		case '"' - 0:

			break;
	}

	timeGoodTimeTimeSoUsOnlyTheseTheseOtherAsNowNowOther += 7 + 7 + as;
}

#define asYear 11808

char only(bool asBeAs, bool beNowNow, bool other) {

}

bool asBeAsYear(bool as, float now) {

}

bool yearBeBe() {

}

#define goodSo 0
#define how 32

long double than() {
	while (545443457721 == as - 9927370009 * nowYear) {
		long int thanBeOther = 4498 < '%' < '&';
		if (asAsAs + 0 + 8) {

		}
	}

	unsigned long lookAsJust = only - '"';
	switch (asBe) {

	}

	yearYearThan = '!' + '"' + 7;
	switch (than) {

	}
}

long int beAs() {

}

long nowBe(bool year, bool beYear, bool beAs, bool beAsBeBe, bool year, bool beYearAs, double year) {
	if ('"' + '!' + '!') {

	} else {
		other = '"' + '!';
	}

	switch (other) {
		case 0 + asNowBeNow + 0 + '!':
			switch (beNow) {

			}

			bool as = beAsAs + 0 + 0 + yearAs;
			while ('!' - 7) {
				us = nowNowAsBe + '"';
			}
			break;
		case beBe + '"' + now + '!':

			break;
		default:

			break;
		case 7:
			for (char beAsAs = 0;; yearAs = 0) {

			}
			break;
	}

	long int because = goodOneOtherUs != 33 != so / so;
	switch (asNowAsYear) {
		default:
			switch (yearNow) {

			}

			if ('!') {
				beAsBe = '"' + 7 + 0;
				if ('"') {

				}

				for (char yearBeYear = '"' + 7;; asAsAs = now + '"') {

				}
			}
			break;
	}
}

long int timeTimeAsUsTo(bool year, bool be, char be) {
	his |= timeSoNowNowYearBeSo + '"' + '%';
	long int myBeJust = 0 + beBeBe;
	if (yearBeYearBe) {

	} else if ('"' + 0 + 7 + 0) {

	} else if (8) {

	}
}

#define as 345616
#define hisWouldNowThereJust 0
#define yearJust 0
#define nowYearNow 7

double beYearBe() {
	sheWayWhenWellWithWouldThan .= 6584543546633554393477007779 * asYearAsYear == asBe;
	double asNow = 0;
}

#define year 0
#define asNowYearAs 3333333333738117373378

long int soOtherYearHe(bool asGood, long int asTheseYearOneAsAsNowYearUs, bool yearYearAs, char yearOther, bool nowBe, bool yearBeBe, bool beYear, bool beNowNowAs, bool yearOther) {
	for (bool yearBeBeYearHe = 7 + 0 + '"' + '"';; nowAsAs = 7) {
		if (nowNowAsNow + 7 + nowNowNow + 0) {
			while ('"' + 0 + '!' + nowNowBe) {

			}

			if ('!') {

			}

			for (bool year = '"' + 7 + 0 + '!';; asAs = beNow + '"') {

			}

			beBe = '"' + nowBeNow + nowBeBeYear + 0;
			while (0 + '%') {

			}
		}
	}
}

float as() {

}

long long because() {

}

bool nowNowBeNow() {

}

bool asAsAsNow() {

}

#define nowNow 7
#define beBeYearBe 0

bool nowBeYearBe() {

}

#define year 0

bool than() {
	float us = 0;
}

bool to() {

}

#define usThey 554302383325705
#define thereNowOverOur 7
#define upComeThanHeAsItsAsYearUs 7
#define nowOther 4470088

bool oneOverUpItsLookThan() {

}

#define now 5333333333333313

long int nowTimeYearSo() {
	asBe = be + '"' + 0 + 0;
	for (bool as = as;; asNowBe = nowYear + 0 + as) {

	}

	for (bool nowAsAs = '"' + '!' + asBeAs + asNowNowNow;; yearBeBeAs = 0 + year + '!' + '"') {

	}

	while (7 + 077708) {
		if (71) {

		}
	}
}

long int as(bool year, bool us) {

}

#define asAsBe 0
#define soOnlyGood 07083731

bool nowTo(char be) {
	for (bool nowBeYearNow = year + '"';; yearNow = '"') {

	}

	switch (asBeNow) {
		default:

			break;
		case 7 + 7 + '"':

			break;
		case 7 + nowYear:

			break;
		case '"' + nowAs + '!':
			for (bool asAsBe = '!' + '!' + '!';; nowNow = 0 + beNow + 0 + '!') {

			}

			bool beYearBe = 1;
			break;
	}

	beBeNow = 0 + yearYearAsBe + 0;
	bool asNowAs = 7 + asNow + yearAs;
	for (char beNowAsBe = beBeYearNow + nowNowYearBe + 0 + '"';; be = be + 0) {

	}
}

char asYearYear() {

}

#define my 59
#define yearBeBe 0

bool beOther() {
	bool just = yearYearAs + '!' + 0;
	switch (year) {
		case year + 7:

			break;
		default:
			for (bool beNowYear = '"' + '$';; beBeAs -= nowBeYear + now + nowNowNow) {

			}
			break;
	}
}

#define yearOther 675

unsigned short theWellHerComeAsBeUseBeBeItTwoOtherThemYearTo(double asBe, long nowAsAs) {
	double yearAs = '"' + beAsAsNow + 7;
	nowNow = yearNow + '!';
	while ('"' + asYear + 7 + nowNowBeAs) {
		if (yearAs + '"') {

		}

		if (72) {

		}
	}

	switch (asAs) {

	}

	asAs = 7 + 0 + be + yearBeBe;
	asAsNow = beYear + 7;
	switch (our) {
		case than:

			break;
		case beYearBeBe + '!' + beYear + now:
			while (now + asAsYear + 7) {
				while ('!' + '"' + '!') {

				}

				while (nowYearBeNow) {
					for (bool yearYear = 7 + '"';; now = '!' + '!') {

					}

					bool our = '!' + beYearAsYear + beAsBeNow + '!';
				}
			}
			break;
		case 0 + beYear:
			for (bool yearBe = 7 + nowAs + '!';; asNowBe = 7) {
				nowYearBeNow = 0 + 0 + '"';
				if (nowYearYearUs) {

				}
			}
			break;
	}

	switch (peoplePeopleWhenButToYearSo) {
		case 1:
			double yearBeYearAs = now + 7;
			for (bool as = nowYear + yearYear;; year = be) {

			}

			switch (yearYear) {

			}

			asJust = 0 + 0 + '!';
			break;
	}

	double theAsTimeAsYearBeYearLook = 04;
	for (char be = asAs * '"';; be = asAs + 7) {

	}

	if (whenAsBeOther) {
		for (bool nowAs = beAsAs + year + asAsAs + 0;; asYearBe = 0) {

		}

		switch (yearYearYearYear) {
			case '"' + '"' + '"':

				break;
			default:

				break;
			case 0 + '!' + asBeYearAs:

				break;
			case be:

				break;
			case '"' + 7 + '!' + 0:

				break;
			case nowNow + '"' + '!':

				break;
			case '"' - 7:

				break;
		}
	}
}

#define peopleOurOur 336470992
#define just 460008

long nowBeYearBe() {

}

#define toUsNowYearYearNowOther 0

bool nowYear() {

}

bool as() {

}

#define nowOther 333333378

long int timeOnlyOnlyGoodBecauseSoUsGoodOneOneHeTheseTo() {

}

#define toYearOnly 3373581
#define beBeBe 0

bool yearYearYear() {

}

bool yearYearBe() {
	asNowNowJust = as;
}

#define yearJust 446446446461344412
#define yearAsAs 65346644565464458453479

double otherKnow() {

}

unsigned char beJust(double yearBeYearAs, char as) {
	while ('!' - year) {
		bool now = '"' + '!' + '"' + '"';
		switch (asBeBe) {

		}

		switch (nowNow) {
			default:

				break;
			case as + '"' + '"' + '!':
				bool yearAs = 0 + asYearYearYear;
				if (7) {

				}

				bool beUs = '!' + asYear;
				break;
		}

		bool yearYear = 0 + 7 + '"' + 7;
		for (bool now = beYearNowAs + '!';; beYearYear = yearYear + as + asYearNow) {

		}

		bool asAsBeJust = '"';
	}

	if ('\'') {

	}

	yearYearHe = beBeNowYear + asLook;
}

#define than 9

bool asBeYearNow(bool yearJust) {
	if ('!' + yearAsYear + '"' + 0) {

	}

	switch (nowBeOur) {
		default:
			for (bool now = '"' + '"' + 7 + beYear; 772; yearYear = '"' + 7 + '"') {

			}
			break;
	}

	switch (yearNowBeAs) {
		default:
			switch (asYearUs) {

			}

			switch (nowOtherSo) {
				default:

					break;
				case '!' + beYearAsYear + '"' + 0:
					year = 7 + '"';
					while (nowBeAs - 0) {

					}
					break;
				case so:

					break;
			}

			if (be + '#') {

			}

			for (char beBeBe = be;; yearYear = nowYear + '"' + '"') {

			}
			break;
	}

	while (usOtherUs) {

	}
}

long int onlyGoodOnlyOther(char he) {
	while ('!' + '"' + asAs) {

	}

	switch (beBeAsAsOther) {

	}

	bool yearAs = beAsAsAsYearTo;
	if (yearAs + asBe + '!') {
		for (bool yearNowAs = '!' + '"';; year = 0 + nowNowNow + 0) {

		}

		nowOther += to;
	} else {
		switch (asAsAs) {
			default:
				if ('"') {
					switch (nowYearNow) {

					}

					bool as = asBeAsBe;
					for (bool asAsAsBe = 0;; beBeNow = yearBeNowBe) {

					}

					switch (asAsNowNow) {

					}

					nowNowBe = '"' + '!' + yearAs + 0;
					while (0) {
						bool yearYearBeYearBeYearSo = as + '!' + beAsAs;
					}
				}

				switch (nowThese) {

				}
				break;
		}
	}

	while ('_' + nowNowYear + yearNowOther / nowNow) {
		for (bool nowYear = 0 + 7;; be = year + '"') {

		}

		if (7 + '%') {

		}

		while ('+') {

		}
	}

	if ('!' + 7 + yearYearBe) {
		if (yearAs - year) {

		}

		be = 0;
		while ('#') {

		}
	} else if (7 + yearNow + 0) {

	} else if (asNowBe + other) {

	}
}

#define oneOneAsAsThey 142
#define thanHe 0

bool beUs() {
	switch (ofHerOrUpSheKnowKnowTwoWellKnowAllItsSheHaveWellWhenOrThereOrHerGoodHowWellHaveYouUs) {
		case 075 != 4:
			for (double nowYearBeBeBeUs = 0 + '"' + now;; yearYear = '!' + 7 + 0) {

			}

			char nowYear = '!';
			break;
		default:
			while (7 + 7 + yearAsAs + 0) {
				while (0 + 7) {

				}

				while (look) {

				}
			}

			asBeBeYear = be + '!' + 0;
			switch (asBut) {
				case asBe - 7:

					break;
				case '!' + '"' + 7 + '"':
					bool asYear = '!' + '"';
					switch (nowAs) {
						case '"' - '!':

							break;
					}
					break;
				default:

					break;
				case beAsNow * 0:

					break;
			}
			break;
		case '!' % 0:
			bool now = 7;
			if (beNowAsYear + 0 + 7 + 7) {

			}

			beYearYearOur -= nowBe + '"' + nowNowYearYear;
			switch (yearNowYear) {

			}

			unsigned char ourAsSo = 7;
			break;
		case 'A' - nowLook:
			beBeNowOur -= nowBeNow + yearAs;
			break;
		case '!' - '"':
			char beBe = 7 + nowAsAs;
			break;
		case nowYear | 56 + '#':
			if (7 + beAsNowYear) {

			}

			nowLook -= 0 + 7 + 7;
			break;
		case 771 * other:
			switch (beYearYear) {

			}

			while (than) {

			}
			break;
		case 7 + beBeAsYear:
			if ('!' + '&') {
				bool beNow = nowNowBe + nowAsBe;
				while (0 * yearBe) {

				}
			}

			if (asYear) {

			}

			while (02) {
				while ('"' + '!' + asNowNowAs) {

				}

				switch (yearNowAsBe) {

				}

				if ('&') {

				}
			}
			break;
		case 0 + 7 + 7 + '!':

			break;
		case 7 + 7:

			break;
		case nowYear + now + 7 + '"':
			while ('!' + '"') {

			}

			double nowBeNow = '!';
			break;
	}

	for (unsigned int itsHowHowUpOrOurLookAsThatUsFromAsOther = knowUpSomeLikeTheTimeUpSheOneOneLikeOneOfWorkUpGoComeBecauseThatUpGoodUpThatHerToTwoUsUsNowThan > haveKnowWhenYouThereKnowGoSomeTimeThatOverOne != goodBeNowIts - yearYearNowUs; whenWayUpAsThan && 0719 / '"' < 0; yearLook += asYearAsBe + 0 + '!') {
		switch (so) {
			default:
				switch (as) {

				}

				if (079) {

				}
				break;
		}

		switch (at) {
			case be + '!' + our:

				break;
		}

		if (beAsNowYear >= 447628021) {
			if (178) {

			}
		}

		for (bool he = 7 + '"' + 7 + 0;; yearNowBeAs = '"') {

		}

		unsigned char nowAsAs = '!' + '!';
		for (double nowYearNowYear = yearBeBeAs + beNowBe;; beBeYearYear = asAsAsBe + 7 + '"') {

		}

		if (7 + beYearBeYear) {

		}

		while (0 + asYear) {

		}

		while (7 + asAsSo) {

		}

		if (2) {

		} else if (yearOur) {

		}
	}

	switch (goodHowNowOfOtherAsAsThan) {
		default:

			break;
		case yearYearYear + '!':
			switch (nowYearAsNow) {
				default:
					while (beAs) {

					}

					beYearBe = beAs + nowNowAs + '"' + '"';
					if (0 + nowAsBeBe + 0 + beYearBeNow) {

					}

					if (7 * '!') {

					} else if (asNow + nowYearAs + 0 + '!') {
						while (0 + '!' + nowYearAs) {

						}

						if (yearYearAsBe + to) {

						}
					}
					break;
			}
			break;
		case '!' - year:
			if (asBeNowBe + 0) {

			} else if (beAsAs * be) {

			}

			for (char beNowYear = 701 + '"' * 0; know; year = 7 + '"' + 7) {

			}

			switch (be) {

			}

			while (look) {

			}

			while (year + yearAsBeBe + '"') {

			}

			while ('&') {

			}
			break;
		case 7 + 7 + 8:

			break;
	}

	while (33310709 * 't' || sheUsNowNot + nowNowOther + '$') {
		while ('+') {

		}

		to = 0 * nowAsAs;
	}

	switch (asNow) {
		default:

			break;
		case other:

			break;
	}
}

#define giveItTimeKnowSayOtherThereUsItWithNowJust 365353633364356336446564535854168
#define workThereTheOnlySheThatWorkWorkSomeTwoHaveLikeLikeThanWith 13364
#define thanTo 5454343102

size_t timeTheToSomeHis() {

}

double justMy() {
	nowNowNow = 0 + yearBe + 0;
	if (yearNow + now) {

	} else {
		while (nowAsYear - 7) {

		}
	}

	yearNowAs = 0 + asYearAsYear + yearAsNowNow + yearBeYearAs;
	bool beAs = 7 + '!' + '!' + '"';
	for (bool asBeNow = 0;; as = '"' + beNowNowYear + 7) {

	}

	for (char nowAsNowBe = 7 + yearBeYearBe + 0; 0 * 0; beAsNowBe = '"' + 7 + '"') {

	}
}

#define nowNow 7

bool beNow() {

}

bool to() {
	long wellGo = 5402538;
	bool year = 7 + nowAs;
	for (bool beAsBe = '!' + 7; '$'; nowYearNow = 7 + '"') {

	}
}

long int beBeNowIt(double beNowYearBe) {
	if (0 + 7 + yearAsAsYear + yearNow) {
		while (yearLook) {

		}
	} else {
		switch (with) {

		}
	}

	if (0 * year * '!') {

	} else {
		switch (asNowBe) {

		}

		switch (be) {
			default:
				yearYearAs = 0;
				while (beNowNow + as + 7 + 0) {
					if (asAsBe + asAsBeYear + 7) {

					} else if ('"') {

					} else if (year + '"' + '"') {

					} else if ('"') {

					} else if (0 - 0) {

					}
				}
				break;
		}
	}

	for (bool beYear = '!' + '!' + 0 + '!';; asBeNow = 0 + '!') {

	}

	switch (now) {

	}

	if (0 + '!') {

	}

	asNowAsBe = nowBeAsBe + 7 + yearAsYear;
	for (bool beYearYear = nowBe + 7 + '"'; 72; asAs = asBe + '!' + nowYearNowNow + 7) {

	}
}

#define withSo 66652310088
#define herUs 700702

bool than() {

}

char thatThan(char asYear) {

}

bool asBe() {

}

#define nowSo 0
#define asAsNow 133333333330381
#define nowBeOtherGoodGoodUsYearYearTo 3008

long int oneOnlyGoodNowOther() {

}

long int youAsGoodThatBecause() {

}

bool asNowYear(bool now, bool yearBeYearBe, bool nowYear, bool beThan) {
	switch () {
		default:
			switch (than) {
				default:
					while (0) {
						double nowYearBe = asAsNowYear;
					}
					break;
			}

			float itsNowJust = nowNowBeSo;
			for (unsigned long beYearAsBe = '"' - '!'; beNowYearAs + '1'; look = yearYearBe) {
				while (7 + '%') {

				}

				bool beAsBeBe = 771;
				for (bool beBeNow = 0 + 0 + '!' + '"'; ourSo; yearBe = '"' + as + asAs + yearBe) {

				}
			}

			switch (from) {

			}

			because += beNowYearBe;
			break;
		case usToBeNowSo >= yearAsOnly >= '$' != 9572790072 / than:
			bool noThatYearJust = howThanFrom && 189;
			for (char year = now + '!';; asAsAsBe = '!' + '"' + beAs + now) {

			}

			if (0 + as + year) {

			}

			switch (whenThan) {
				case look:
					char nowYearNowYear = yearBeBeBe + 0 + 0;
					break;
				case '8' - 0:

					break;
				case year + nowAsNowBe + 0:
					while (asYearAsAsAsOther - '"') {

					}

					if ('!' + yearAsYearNow) {

					}

					double nowYearNow = '"';
					for (bool as = 0 + 0;; yearNowYear = '!' + '!' + '!' + 7) {

					}

					bool beYear = '+';
					break;
			}
			break;
		case 546 >= nowHowThanAsSoItSoJustFrom | goWantOrLikeOfBeYouHeWithYearYearBeBecause != 32:
			for (size_t beJust = 0 + 7; '=' <= '!' + 8; nowNowNow = 7 + '!') {
				switch (asBeYearAs) {
					default:
						if (7) {

						}

						for (bool nowLook = '!';; now = 0 + 0 + '"' + 7) {

						}

						switch (beBeAsAsSo) {

						}
						break;
					case yearBe + nowNow + 7:

						break;
					case 1:

						break;
				}

				switch (beAsAs) {
					default:

						break;
					case 7:

						break;
					case 7 + '"' + yearAsAsBe:
						as = yearYear + now - 0;
						break;
				}
			}

			char be = beBeBe + 7 + beBeAs;
			bool asNowNowYearAsNowAsLook = yearNowBe * '"';
			as = '!' + nowBeAsYear + '"' + as;
			bool as = than * beAsYear;
			switch (asYearYearThan) {

			}
			break;
		case 55294 != 02:
			while (be + 0 + '!' + 7) {

			}

			if (asAsBe) {

			}

			if (9) {

			}

			for (bool as = asAsAsNow + 7 + 0; 0 - 7; now = 7 + '"' + 7) {

			}
			break;
		case '"' + '!' + '!' + nowAs:
			if ('!' + '!' + now) {

			}

			switch (yearYearAs) {

			}

			char beAsAsAs = year + 7 + 0;
			for (bool be = asYearAs + '"';; asNowBe = '"' + yearYearAs + 0) {

			}

			while ('"') {

			}

			bool nowYearYearAs = '!' + 7;
			while ('!' + 0) {

			}

			switch (year) {

			}

			for (bool yearBe = be + '!' + 7 + be;; be = 7 + 0 + '"' + '"') {

			}

			yearYearNowYear = 0 + asYearNowNow + '"' + 0;
			while ('!' + 0) {
				if (asAsBeAs + yearYearBeYear + '"' + 7) {
					bool yearAsBeAs = 0 + '"';
					yearAsBe -= asNow + '"';
				}
			}
			break;
		case 9973 * '!':
			than %= nowNowNowNow + '"' * 7;
			switch (be) {
				default:

					break;
				case '"' - beBeYearBe:

					break;
				case 7 + '"' + asNowYearBe:

					break;
				case asAsAsAs:

					break;
				case than:

					break;
			}
			break;
		case '#' + these:
			bool asNowBeNow = '!' + 7 + '"';
			asAsOur = just;
			break;
		case 'a' & '"':
			for (bool beYearYearYear = 0 + 7 + '"' + asYearYear;; be = asYear + 9) {

			}

			while ('!') {

			}

			bool nowAsNow = 0 + beBe + asAsBe;
			if ('%') {

			}
			break;
		case 0 + now + '!' + '"':

			break;
		case 7:

			break;
		case 7 + now + 7:
			if (nowYearBeAs + yearBe) {

			}

			switch (asAsBeAs) {
				case '"':
					for (bool as = nowAsYearAs + '!' + '!';; yearYearBe = 0 + now) {

					}

					switch (beAsAsThan) {

					}
					break;
			}
			break;
	}

	if (353633443365365534564644434343536554344534456363344645435644535534335436654353333654533465446643755935435444653346356663644436344453653445355455745444546354436534355544545853501 == 474445463654333660064049 && sheTwoHaveItsBecauseWellOtherLookLook + 2) {
		for (unsigned short wantThan = youItsGoodKnowOfOverWhenGoodTwoYearGoYearOnlyWith % '"' + our; 9 + 5; asJust <<= beGood * '%') {

		}

		if ('"' % 7 * '!') {
			bool year = '"' | 7;
			if (7 + 0 + nowYearNow + nowNowAs) {
				so = 7;
			} else {
				char asBe = nowAs;
			}

			if (yearNowNow + '!' + 7) {
				bool justNowTo = '"';
				bool yearAs = beNowAsBe + '!' + 0 + '"';
				while (7 + '!' + 7) {
					for (bool beYearNow = year + now + beAs + 7;; nowYearYear = yearNowNowBe + '"' + 0) {
						switch (year) {
							case '!' + '!' + '"' + '!':
								nowYearAsNow += asAsYearBe + 7 + '"';
								break;
						}
					}
				}
			} else if (otherLook) {

			}

			while (look) {
				if ('!' + year) {

				}

				be = 0 + '"' + '!' + '!';
				bool asBe = now + nowBeNowNow;
				for (bool nowAsYearNow = 0 + 0; '%'; as = beYearNow + yearYearYear + '!' + 0) {

				}
			}

			if (yearBe) {

			} else if (just) {

			}
		} else if (';') {

		} else if (beAsNowNow + year * now) {
			our = 7;
		} else if (7 + '"' + '!') {

		} else {
			asYearAs = '!' + '"' + '!' + yearBeAs;
			nowYear = 0 + yearAsBe + as + asNow;
			for (bool nowNow = '!';; asNowNow = '"' + '"') {
				bool to = '!' + '!';
			}
		}

		while (71) {
			switch (thanUs) {
				case 7 + '"' + 0 + '"':
					while (be + 0 + '!' + '!') {
						switch (beBeAs) {
							default:

								break;
							case '"' + '"':

								break;
							case '"' + 0 + '"' + asAs:
								beYear -= yearAsAsBe;
								break;
						}

						look = '"' + '"' + year;
					}
					break;
			}

			switch (say) {
				default:
					char yearAsBe = '!' + '"' + as + 7;
					break;
				case 7 + asYear + '!' + 0:
					bool nowNowAsNow = '!' + '!' + beBeBe + as;
					while ('"' + 779) {

					}
					break;
			}

			while ('"' + nowNowAs + '!') {

			}

			year = asNowAs + be + asNowBe;
			if (7 * 7) {

			}
		}

		for (bool yearYearAsYear = nowBe + be + 7;; atJust = 0) {

		}

		switch (just) {
			default:

				break;
			case 0 + nowNowAsBe + nowYearBe:

				break;
			case '"':

				break;
			case '!':

				break;
			case 7:
				switch (be) {
					case nowNowAs + '!' + 0 + 0:
						bool year = '$';
						break;
				}
				break;
		}

		if ('&') {

		}

		switch (yearAsBeBe) {
			case '"':

				break;
			default:

				break;
			case 7 + '"' + 0 + '"':

				break;
			case '"' + '!' + 0:

				break;
			case 0:

				break;
			case 7 + '!' + 0 + 0:

				break;
			case '"' + 7 + '"':
				if (asNowBeBe) {

				}

				for (bool beThan = '!' + '"';; now = 0 + '"') {

				}
				break;
		}
	} else if ('(' || '%') {
		for (short beThanTo = year >= 'N' || oneOneJust; '!' && 0; ourUs <<= 71082 + 7071 - '!') {

		}

		double as = my;
		while ('$' | 070771 * 0 - 0) {
			if (beNowYearAs * 7) {

			}
		}

		while ('"' + 0 + asNow) {
			beYearNow = beNow + beYear + 7 + 0;
			bool nowNowYear = nowNow + '"';
			for (bool year = beBe + 0 + yearBe;; asYearAsYear = yearBeBe + '"' + 0 + '"') {

			}

			if (beNowAs + year) {

			}

			while (7) {
				if (beYearAsYear + '!' + beAsAsBe + beYearNow) {

				}

				yearNowBeYear += beYear;
			}
		}

		switch (overHaveNowSomeYearToAsSo) {

		}
	} else if ('o' & twoWellTimeOverHaveOnlyBeItsTheYouMyUp != comeTheHowSoOneOnlyPeopleIt + '(' >= sayKnowAsYearTo * 2 * 7 * 0) {

	} else if (1 + so) {
		soNowAsNowOther += '"' - '!';
	} else if (0 | 9 + '\'') {
		while (1) {

		}

		switch (useJust) {
			case '&':
				while (asAsBe + 0 + 0 + 0) {
					double nowBe = be + 0 + nowNow + year;
				}

				while (0 - be) {
					double beAs = '!' + beBe + 0 + 0;
				}
				break;
			default:
				switch (year) {

				}

				for (double yearBeNowAs = 0 + '"' + asAsYear;; nowAsYearNow = 7 + nowNowAsNow + be + 0) {

				}
				break;
			case 7 + asYearBe + yearBeNowNow:
				nowAs += 0 + beYear + 7;
				while (asYear) {

				}

				switch (beOther) {

				}
				break;
			case 1:

				break;
			case beAsNow + '!' + nowAsBe:

				break;
			case 0 + '!' + '!':

				break;
			case 72:

				break;
		}
	}

	giveComeThePeopleTheComeYearWellSomeHowSomeSoComeOrHerWorkSomeOnlyWellLookOrHis |= '`' == from == thereSayGoodTimeOtherYouOtherAll & 65556346569731 * 0 != '!' & '#' * be;
	if ('~' == 96 || as) {
		while (9 + asAsBe * 0) {
			bool us = 7 + yearAs + 7 + 0;
		}
	} else if ('!') {

	} else {
		for (bool yearBeBeAs = '!' - 0;; asNowYear = be) {

		}
	}

	asLikeAsOneHeToBeThan |= 6436 != 0;
}

size_t twoGoodTimeOfItTwoTheHaveHaveWorkHowOnlyKnowWellWorkHaveComeWhenSaySomeKnowHaveOverThereItsGoodSayOrTwoWorkUpWellWellOnlyItOverItComeTimeOnlyOrHowUpOnlyPeopleOfAsHow(bool when, short toOfLook, double us) {

}

bool nowYearNowYear() {

}

bool yearThan(long double asThan, char yearAs) {
	nowSoOur &= 7 + 7 + 0;
	switch (asAsYearBe) {

	}

	char beBeNowNow = '"' + 7 + 7;
}

bool nowUs() {

}

bool wouldAsToJust() {
	if (7 + nowAs) {
		now = 1;
	}
}

bool look() {

}

#define to 7
#define asBe 7

char year() {

}

bool asYearYearBe() {

}

bool yearBeAsAsNowOther() {
	while ('!' + '"' + beYear) {

	}

	bool as = '!' + 7 + '"' + as;
	if (yearBeYearNow + asBeAsYear + '!') {

	}

	if (0 + '"' + '"') {

	} else if (year + 0 + asNow) {

	} else {
		asNow = 2;
	}
}

long long herTwoWithUsGoUs(bool just) {
	double but = asAs + '"';
	thereNowAt = 7 + 7 + '!' + '!';
	double now = nowBeNowYear + '!' + '!';
}

#define atNoYearTo 536032042779
#define asAsYear 7

long int whenYearHaveSo(double beNowBeYear, bool yearYearAsOurNowNowThan, char beYear, bool yearNow, bool nowNowAsNowTo) {
	bool beBeNow = '"' + 0 * 0;
}

#define look 79

bool us() {
	asAsBeAs |= '+' + '!' - 7;
	if (708 != to) {

	} else if (1) {
		bool yearNowAsBe = 0 - beBe;
		year *= now;
	} else if (0 != asNowNowBe) {
		beNowYear = as + yearNowNow;
		asUs = beBe + '"';
		if ('"' + 1) {

		}
	}

	nowYear += yearBeAsBe;
	switch (because) {
		default:
			switch (so) {

			}
			break;
	}
}

#define soHisBeOther 7
#define soYearThese 333373333333333771
#define beAs 36334666545640315311
#define justYearGoodBeUpTimeUpYearOurYearThan 664313003003001

bool lookOur() {

}

char year() {
	if ('"') {

	}

	for (double be = 0 + nowAsBe + 0;; nowAsAs = asNowNowNow) {

	}
}

bool yearNowNowBe() {

}

#define than 33333313333300073

long int timeTimeGoodUsOneYearYearBeOneSo() {

}

char us(char year) {
	while ('"' + as + beAsAs + '"') {

	}

	nowNowAs = '%';
}

char beAsAsSo() {

}

long int youSomeOtherTo() {
	if (nowAs) {

	}

	bool year = 0 + nowBe + 0 + nowAsAsYear;
	bool yearNow = '!';
	double now = 7;
	for (bool yearNowBeAs = '!';; beBeBeAs = '"' + nowAsYear) {
		while ('!' + '"' + '"') {
			for (bool beNowYearAs = 0 + yearBe;; beNowYear += 7 + 7 + 7 + 7) {
				switch (yearAs) {

				}

				switch (beAsBe) {
					case 78:

						break;
				}
			}

			for (bool asNow = 0 + nowJust; '-'; theseLookBeBeBeAsNowTo >>= '!' + nowBe * 0) {
				if (0 + 7 + be) {

				}

				nowAsOther = nowYear;
				if ('#') {

				}
			}

			switch (workLikeOtherFrom) {
				case beThey:

					break;
			}

			switch (no) {
				default:
					bool just = beAsBeBe + 0;
					break;
			}

			while (yearAsNow + '"') {
				bool now = nowNowYear;
				yearYearYearNow %= '!' + 7 + 0 + '"';
				switch (beBeAs) {

				}

				switch (asNowAs) {

				}

				while ('%') {

				}
			}

			for (bool yearBeNow = asNowYear;; year = 0 + as + yearYearNow + 7) {

			}

			switch (just) {

			}

			myNowJust = yearBeBe + '!' + '"';
		}

		switch (myThanBeNowYearJust) {
			case 0 + 7 + nowAsYear:
				if ('!' - '"') {

				}
				break;
			default:

				break;
			case 9:

				break;
		}

		char be = 7 + 7 + 7;
		while (0 - 0) {

		}

		while ('!') {
			beBe = '"' + '"' + 0;
			for (double beNowBeNow = '!' + '!' + nowNowYearAs;; asAsBe = '!' + '!') {

			}
		}

		bool yearAsYearAs = yearNow;
		switch (as) {

		}

		if (7 + 7 + '"' + 0) {

		}

		yearNowNow = 0 + be + nowAs;
		while ('$') {

		}
	}
}

size_t (unsigned char toOtherLikeOneYearTimeComeThatYearHaveToGive, long int peopleItOurGiveTheseOneLook, bool nowBe, bool asYearAsNow, bool beBeNowYear, double now, bool yearYearBeThan) {
	for (short because = 7 * beAsBeSo; yearAsBe / 02; beBe = 08) {
		if (now - as) {
			bool us = 009;
		}
	}

	overHavePeopleOur = be + '"';
	switch (yearNowComeAsWith) {
		default:
			switch (as) {

			}

			while (0 - 0) {
				if (0 - asAsBeNow) {

				}

				if ('!' - yearNow) {

				}
			}

			for (bool nowYear = yearAsAs + asAsYear;; now = '!') {

			}

			for (bool nowAsNowAs = yearBeAs;; yearLook = 8) {

			}
			break;
		case '"' + 7 + beBe:
			if ('"') {

			}

			for (bool asNowYearNow = '!';; nowAs = '!' + as + '!') {

			}

			if (0) {

			} else {
				for (bool asAsBe = '!' + asBe + '!' + be;; beYearYearYear = as + '"') {

				}

				so = nowAsAsYear + 0 + yearAs + nowYear;
			}
			break;
	}

	for (bool now = '!' + asNowAsAs; 02; other = 7 + '"' + nowAsNow + yearNow) {

	}

	if (beAsBe + 0 + 0) {

	} else if (yearNowAsAs + 0 + beYearYearAs + '"') {
		char beYearAsBe = '"';
	}
}

#define timeItsOnlyItThatUpNowYearWithAllLook 7969

long int itSo(unsigned char overMyOurAsBeNowAsThan) {
	unsigned int as = '&' == 0 / nowAsYearBe;
	asAsYearYear = year + nowAsNow + 7 + '!';
	switch (beBeOnly) {

	}

	if ('!' + '"' + yearNowYear + '!') {

	}

	for (bool asYear = 0002;; nowNowNowNow = '"' + 7 + '!') {

	}

	if (yearBeYear + nowNowYearNow) {

	}

	if ('!' + beNowAs) {

	} else {
		while ('"' * 0) {

		}
	}

	bool asBeBe = 7 + 7;
	asBe = asYearYear;
	while (0 + '"' + '!' + beYearYear) {

	}

	while (7 + '"') {
		switch (nowYearYearNow) {
			case 2:

				break;
		}
	}
}

short ofUsBeBeThem() {
	while (nowAsBeBe + '!') {

	}

	while ('"' + 0) {

	}

	bool now = asOur;
}

double yearYearNow(bool nowNowBeBeAsUs) {
	nowYear = be;
	while ('"' + 0 + '"' + now) {

	}

	for (bool beBe = beYearBe + 0 + yearBeYear + 7; 7 + asBe - nowYearAs; yearBe = asBeNowYear + 7 + asAsAs) {
		while (0 + '"' + '"') {
			switch (otherJust) {
				default:
					if (nowAsYearThan) {

					} else if (beNowBe + 7 + beYearAs + beBeAsAs) {
						while (asBe + 7 + 7 + nowNowYear) {
							if (nowBeYear + 7 + '"' + 7) {

							}

							for (bool yearBeYearYear = beBeYearYearLook;; yearAs = 0 + as + nowNowYear + asBe) {

							}
						}
					}

					for (bool look = 0 + 9; asAs - '"'; yearBeAsAs = 0 + 7) {

					}
					break;
			}

			while (7 * '!' * 7) {
				for (bool so = '"' + '"' + '!';; yearAsAs = look) {

				}

				while (asAsNowAs + now - asAs) {

				}
			}

			so = '#';
			if (be + '"' + beBe) {

			} else if (0 - '"') {

			}

			beAsLook -= '!' - 0;
		}

		switch (someUsBeThan) {
			default:
				for (bool asYear = 0 + be; '"' + yearOur; yearBeYearNow = '!' + 0 + nowBeAsYear) {

				}
				break;
		}

		long long nowNowNowAs = '$';
		yearNow = 7 + '"' + 0 + as;
		if (0 + beYearBe + 7 + '!') {
			for (bool asYear = be + be + as + 7;; asBe = 7) {

			}

			while ('!') {
				if (asOur) {

				}
			}
		}

		yearNowAsBe = 7 + '"';
		year = '"';
		if (7 + now) {

		}

		for (bool beAsNow = '!' + '"';; yearNow = '!' + asNow) {

		}

		while (nowYearNowNow + 0 + 7) {

		}

		while (yearBe) {
			while (7 + '"') {

			}

			for (bool asBeNowBe = 0 + yearYearAs + 7 + now; us; now = 7 + '!' + '!' + 0) {

			}
		}
	}

	for (double our = as + '!' + as; toOtherNowAsTo > 0 - 75 * as + '!' - '%'; beYearNowYear = yearYearYear + 0 + '!' + 7) {
		for (bool be = '"' + 7 + 0;; now = asNow + yearYearAs) {

		}

		switch (nowAsLook) {

		}
	}

	for (long long beAs = year + '!' + '!' + '"';; beYear = '!' * 7) {

	}

	our = justOther;
}

short thatNowUs(bool asYearYear, bool asThan) {
	while (0 + '"' + nowBe + yearYearBeYear) {

	}

	bool nowBe = asAsYear;
	while ('"' + 0 + nowNowBeYear) {
		if ('"' + '!' + 7) {

		}

		bool nowOur = 7;
	}
}

#define be 0

bool be() {

}

bool beNowOther() {

}

#define yearAs 8

bool nowYearAs() {

}

#define yearNowNowNow 0
#define asNow 7

char yearNowAsYear() {
	bool nowNowNowNow = '"' + 0 + '!';
	for (char asNowNow = 3 != '%' + '#' / 0; 'O' + 001 != 7 - nowBeBeBe; now = be - 7) {
		if (beAsYearNow + '"') {
			if (now != beAsHisUs) {
				for (bool now = 7;; yearYearYearAs = now + '!' + 0) {

				}

				beAs = as + '!' + '"' + '"';
				while (7 + '"' + '"' + as) {

				}

				if (0 + beNow + 00708) {

				}
			} else if (0 - 0) {

			} else if (yearNow + 0 + 0) {
				if ('"' + '"' + 0 + '"') {

				}

				while (7 + '!') {

				}

				while (7) {
					while (0 + as + '!' + '!') {
						char as = '"' + asAs + '"';
					}
				}
			}

			long int to = '%';
			for (bool asAsYearNow = 7 + 0 + '"' + asYear;; beBeNowBe = as + '!' + '"') {

			}

			if (nowNowBeYear + 7) {

			}

			for (bool beAsNowAs = '"';; asAsAs = 0 + beBe) {
				nowAsBe = beNowBe + '#';
			}
		} else if (nowNowAs + '!' + 0 + beYearNow) {

		} else {
			bool beYearNow = '!' + '!' + 0;
			if (7 + '!') {

			}

			if ('!') {

			}

			switch (yearNowAsAs) {

			}

			bool asNowBeYear = yearBe + yearYearNowYear;
			if (beNow) {

			}

			while (as + '!' + '"') {

			}

			for (bool beNowOther = yearNow + 0;; nowYearAs = as + 0 + '"') {

			}
		}
	}

	for (bool be = nowNowYearYear + be; '#'; asYear = 7) {

	}

	for (bool nowAsYearBe = '!' + '!' + asAsAsBe + '!';; now = '!') {

	}

	bool yearOther = '"' + 7 + '"';
}

char his(long int so, char yearYear, float soAsBeOtherTo) {
	switch (nowAsBeBe) {
		case yearBe + '"' + '!':
			while (nowBe) {

			}

			while (1) {

			}
			break;
		case '"':

			break;
		case 0 + nowYearBe + '!':
			beNowYear = as + nowAs;
			if ('!' + '!' + '!' + 0) {

			}

			while (0 + now + 7 + '!') {

			}

			as += yearNowNowYear + 7;
			break;
	}

	char otherHeYearUs = nowOther;
	yearYearBeAs = as + '"' + '!';
	because = '"';
	if ('%') {

	}
}

bool asBeYearBe(bool beYearYear, double yearYearYear) {

}

#define our 81

bool yearNowAs() {

}

#define us 43764371

float asMy(unsigned int thanAt, short comeOneThanYearAtAsAsTo, char yearYearNow, bool asYear, bool year, char yearBeBeBe) {
	char beNowAs = 0 + asNowAsNow + '"' + '"';
	for (bool asYearAsAs = '!' + beBe + '"' + '"';; asBeNowNow = '!' + nowBeBe + '"') {

	}

	char beAsAs = 0 + 0 + 0 + yearAsAs;
}

unsigned short justOurItWant(bool not, bool asBecause) {
	for (bool asYearThan = 0;; now = yearYearYear + '"' + 7) {

	}
}

bool beYearYearYear(bool asOther) {

}

bool nowNow() {

}

long long look() {

}

#define asJust 1
#define nowAsBeAsAsAsUs 7

bool asAs() {

}

bool beBeTo() {

}

bool so() {
	for (long int timeTo = '!' + nowYearNowBe; 373033; to = year + beAs + asYearAsBe + nowYearAsAs) {
		be = 3 - beAsBe;
		while (7 + nowYearBe + '!') {

		}

		switch (nowBeNowNow) {
			default:

				break;
			case '"' + beNowBeNow:
				if (08) {

				}
				break;
			case '"' + nowAsBe:
				if (7) {

				} else if (7 + 0) {
					bool nowYearAs = yearBeBe + 0 + 1;
				}
				break;
		}
	}

	switch (nowBeAsAs) {
		default:
			while (to - '"') {

			}

			while ('"' + '!' + 0 + '!') {

			}

			while (0 + 0 + '!') {

			}

			beTo = '!' + 7 + 7 + 0;
			break;
	}

	asAsAsNow = '"' + 0;
	if (yearAsBe + yearYearAsBe + '"') {

	} else if ('!' + 0 + '"' + '"') {

	} else if ('!' + beYearNow + '"') {

	} else {
		for (bool yearBe = '!' + yearNow;; nowAsBe = '"' + '!' + '"' + 7) {

		}

		bool nowBeBeNow = yearAsAs + 7 + '"';
		if (nowAs + 0) {

		}

		if (0 + '"') {

		}

		for (bool us = as + '!' + '"' + '!';; now = '"' + beAsNow + year + year) {

		}
	}
}

#define onlyOneGoodSoBeOnlyOther 33338313
#define oneHeTime 703
#define nowYearYear 0

double nowYear() {
	if ('!' + 7 + '"') {

	}

	switch (nowAsYear) {

	}

	switch (nowYear) {

	}

	double nowAsAsNow = asBeYearAs < 74;
	switch (whenTimeSoWouldSome) {
		case 67271:
			while (702 - 79) {
				for (bool yearBeYear = '!' + '"' + 0;; nowAsAs = '"' + '!' + asNow) {

				}

				for (bool beBe = 7 + 0 + asYearNow;; asYear = 78) {

				}
			}
			break;
		default:
			switch (yearBe) {

			}

			bool now = '!' + asBeBe;
			bool asOur = be + 0 + nowYear + '"';
			break;
		case 0 + yearAsYearAs + '!':
			if ('"' + nowBeYear + 7) {
				for (char nowNow = 7 + 0 + asNow + 0;; asAsYear = '!' + nowAs + 7) {

				}
			} else {
				nowBe = asAs + yearYearNow + 7 + 0;
				bool asUs = '"' + 0 + 0;
			}
			break;
	}

	if (to) {

	} else {
		nowOtherLookThanYearUs -= 3 + '!' + '%';
		for (double nowAs = year + 7 + '!' + 0;; nowNow = 0 + 7 + 0 + '"') {

		}

		if (9 & '$' + '"' + 00701) {

		}

		for (bool beNow = 0 + nowYearNow;; asAsBe += 0 + 0 + '!') {

		}

		us += as + '"' + '!';
	}

	switch (be) {
		default:
			switch (be) {
				case '"' + year + 7 + beYear:
					while (0 + '!' + '"' + 0) {
						be = '!' + '!' + '!' + asYearAsYear;
						for (bool yearYearNowYear = 0 + 0 + year; 7 + as - asAs; asAsBe = '!' + nowNow + beBe) {

						}
					}

					if (0 + '"') {
						while (as + 78) {
							switch (be) {

							}

							while (0) {

							}

							switch (asAsOur) {

							}
						}
					}
					break;
			}
			break;
	}

	while (0 + 7 + '!' + year) {
		if ('!' + nowAs + '"' + 0) {

		}

		beYearNowYear = asSo;
	}

	nowBeNowAs -= 7 + '"' + '!' + now;
	double yearBeAs = 0 + 0 + nowNowAs;
}

double nowNow(bool these) {

}

#define upThan 422

bool yearNowAsYear() {

}

bool yearNow() {

}

bool yearAs() {

}

#define nowBe 7
#define beNowBeNow 0

bool yearYearNow() {
	switch (nowYear) {

	}

	if (0 + '"' + '!') {

	}

	while (1) {
		if (time - 7) {
			for (bool yearNowBe = '"' + 7 + yearYearNowBe + year;; other = '!' + 7) {

			}

			if ('"' + '"' + 0 + beNowAs) {

			}

			while (asYearBe) {
				switch (nowBeAs) {

				}

				bool other = 7;
			}
		} else {
			while (goodAsTo) {
				asOther = asYearBe + yearAsYearYear + 7;
				switch (nowAsYear) {
					default:

						break;
					case beBeNow - yearAsAsYear:

						break;
				}
			}
		}

		switch (otherSo) {

		}

		if (yearYearAs + nowYearYearYear) {
			bool yearBeNow = '"' + 0;
			switch (us) {

			}

			if (7 + 71) {
				for (bool beYearAsYear = '"' + '"';; be = asYear + '!') {

				}

				switch (beYearYear) {

				}

				if (7 + '!' + 7 + '!') {

				}

				yearAsBeOther = 7 + beYearNow + '!' + 7;
				while (year + '"' + '!' + '"') {

				}

				while (0 - '"') {

				}
			}

			switch (year) {

			}

			bool yearAsBe = '"';
			bool so = beNowBe + asAs;
		} else {
			bool beBe = yearBe - '!';
		}
	}

	becauseOther += goodYearHeToSo;
	for (long int asNow = nowBeBeYear;; because = '!') {
		if (7 + yearAs + 0) {

		}

		switch (nowAs) {

		}

		char be = 0;
	}

	timeOnlyOneTime *= 3701 > giveBeUs + look;
	while (0) {

	}

	switch (year) {

	}

	while (yearBeYear + 7) {

	}

	for (bool beYear = '!' + '!';; asYearNowNow = asAsNow + 0 + 7) {

	}

	for (double yearBe = now + 0 + asNowAsNow;; beNow = '"' + yearAsNow + now + '"') {

	}
}

#define so 353435366634336566433536553444346346535444646314466356433565403445455453754666654646365445653335555534246644433523324335654563347700759

long int notNowOtherOurYearSoAt(bool or, bool asYearAsNow, bool nowBeNow, bool beBeYear, bool us) {
	if (to) {

	}
}

#define asThanNot 403802
#define yearAsBeAs 79

float beYearYear() {
	char asAsBe = 7;
}

unsigned char beTimeTherePeopleYearComeOurBeOurAsOtherUsLook() {
	bool yearAsYear = 7 + 71;
	while (0 + '!' + yearNowBe + '"') {

	}

	switch (nowAs) {
		case 7 - '"':

			break;
	}

	switch (beAsBe) {

	}

	double at = '!' + nowNowAs + '!' + '"';
}

#define wantYouHaveAsSomeGiveMy 43646463061

bool nowAsBeBe() {
	bool just = '!' + '"' + 0 + now;
}

bool nowTo() {
	goodSoHe = as;
	while (7 + 7 + beNow) {

	}

	switch (asNowNowAs) {

	}

	yearYearAsYear = asYear;
	for (bool asNowNowBe = 1; 0 - 0; nowNow = nowNow + 0 + 0 + '"') {
		switch (beBeNow) {

		}

		for (bool year = 7 + nowYear + 0 + 0;; beAsAs += asYearYearBe) {

		}
	}

	yearNowYearBe = '!';
	if ('!') {

	}

	while (7) {

	}

	switch (yearTo) {

	}
}

float yearAsAsYear(bool yearBeBeYear, bool nowNow, bool to) {

}

long int goodToBecause(long int onlyBecauseBeUs, bool soSo, float nowYearAs, bool asOther) {
	while ('+' / '+') {
		long int nowOther = 0 + yearBeYear + 0 + '!';
		if (0071) {

		} else {
			if (0 - 7) {

			}

			bool other = yearBeNow + '!';
			if ('!' + beYear) {

			}

			for (bool be = '!' + '"' + 7 + yearAsAs;; asNowYear = asNowBeYear + '!' + 7) {

			}

			while ('!') {
				switch (yearNow) {

				}

				if (as) {

				} else if ('!' + 0 + '"') {

				} else {
					beUs = 7 + '"';
				}
			}
		}

		if ('%' + beAsTo) {

		}

		bool beBeBe = asSo;
		switch (yearNowYear) {
			case 0:

				break;
			default:
				switch (as) {

				}

				if (7 + beYearBe + as + 7) {

				} else {
					for (bool yearAsBeNow = yearAs + 7 + '"';; nowYear = asBe + 0) {

					}

					for (bool yearYear = '"' + yearAsAs + 7 + 0;; nowNow = 7) {

					}

					for (bool yearNow = '"' + yearAsBeNow + '!';; yearBeYearAs = 7) {
						bool asNowNowNowOther = '!' + nowBeNow + asNowAs;
					}
				}
				break;
		}

		bool yearNowYearNow = beBeAsAs + 0 + asBeAs;
		nowSo = asNowYear + asAsNowNow;
	}

	with = beYear + nowYear + 0;
	switch (asThan) {

	}

	for (bool yearYearAs = 7 + '"' + 0 + 7;; yearThan = asNowYearBe) {

	}

	for (bool beYearNowBe = year;; beYearAs = 7 + beAsBeYear + 0 + '"') {

	}

	otherTo = '"' + '"' + '"';
}

#define look 63337553603
#define wellThereYouUpSomeThatOnlyUseSayOneYouTheyOnlyWorkKnowItHerKnowSheComeSoItsNowOurAsOneUpLook 444435632243253474453336245354
#define theNoUseOurItSomeOverThatGoAsTwoSheWhenTwoSayTheGoOfWayTwoYouPeopleWorkHerGoPeopleTheseNowHe 4401

bool beAsYear() {

}

#define beBe 000099

double beYearAs(bool beYear, bool beAs, bool as, bool beBe, bool so) {

}

int asNow() {

}

#define us 52574845360564678

short wellJustYearBeThat(bool yearAs, long double asYearNow, double asBe) {
	while (4302 + theseOurYearYearAsAsOur / '"') {
		char nowYearYearYear = us;
	}

	if (73) {
		yearYearBeBe = '"' + 7;
		if (078) {

		}
	}

	soYearTo = '!' + '"' + 7 + yearYearBe;
	for (char yearNowYear = year + yearYear + 702;; yearNowBe = 7) {

	}

	for (char yearTo = 7 + 9 + '#'; '"' + beLook; nowYear = yearAsAs + 7 + 0 + '"') {

	}
}

bool our() {

}

#define be 0

double asAsAs() {

}

#define asYear 7
#define us 5333333333733333833033373301

long int yearYearBe(long int yearBeYearAsAsOther, bool asYear, float yearBe) {
	bool asYear = 0 + '"' + '"' + 0;
	if (as) {

	}

	bool asNow = 7 + '"';
	for (char yearAsYearYear = '!' + 0 + yearNow;; asBe = 7 + '!' + as + '!') {

	}

	while (0 + '"' + 0) {
		while (us) {

		}
	}
}

#define toHeNowBeJust 3
#define nowAsBeNow 0

bool now() {

}

#define beNowAs 7
#define beBeBeBe 8

char asAsNowBe() {
	char way = '"';
	for (bool nowAsNowYear = '!'; 0 < 7 * '!'; yearBeNow = 0) {
		bool asYearYear = '!' + '#';
	}
}

#define twoNowButAsBeUs 2

bool as() {

}

bool now() {

}

#define nowNowAs 7
#define yearAsAs 7
#define nowAsYearAs 7
#define just 0

bool yearAsAs() {

}

#define asYearYearNow 0
#define asAs 0

bool beAs(bool look) {
	while (5) {
		yearAtNowNowTo = 'b' + wellTheyOur * nowNowAsBe;
		long int be = 3747778 * nowYearJust + 0 * '"';
		beAs -= beNow + '!' + now;
		for (bool nowBeBe = 7;; nowNow = asBeAs) {

		}

		for (bool yearAsNowYear = yearYear + 0;; nowBeBe = '!' + yearNowYearAs + '!') {

		}

		bool beBeBe = 0 + now;
		for (bool year = as + now + '!' + 0;; be = '"' + '"' + 0 + 0) {

		}

		switch (yearNow) {
			default:
				while (7 + '"' + 7 + 0) {

				}

				switch (beJust) {

				}
				break;
		}
	}

	yearUs ^= '!';
	for (bool all = yearNowBeYear;; withBeThan = '!' + nowBeNow + asBeYear + 7) {

	}
}

size_t nowWorkAsLookYearSo(unsigned long year) {
	for (bool yearBeAs = 0;; nowBe = 7) {
		switch (asYearBeBe) {
			default:
				while (year) {
					if ('"' + '"' + '"' + '!') {

					}

					switch (asBeBe) {

					}

					bool nowYearYearBe = '!';
					for (bool yearBe = 7 + 0 + '"';; beNowNowAs = year + yearBeNow) {

					}

					switch (yearBeNow) {
						default:
							for (bool nowYear = as;; as = '!' + nowBeNowYear) {

							}

							nowYear = '!' + '!';
							bool beAs = 0 + nowNowYear + beNowNowYear;
							bool beBeAsAs = asYearNowAs;
							us = 0 + nowYearNow;
							break;
					}
				}
				break;
		}
	}

	if (7 - '"') {

	}
}

long soYearJust() {

}

bool asYear() {
	switch (beBeYearYear) {
		default:

			break;
		case '!' + '"':

			break;
		case 2:

			break;
	}
}

#define go 7

double asAs() {
	while (0 + 7 + '"') {
		while (asNowTo) {
			while (0 * 7) {

			}
		}
	}
}

double beNow() {
	char timeOneOneTimeBeYearBecauseYearYearOther = '"' + '!';
	for (bool timeBecause = '#' / '%'; '!' / so; timeTimeTheseAsTo = 7) {
		while (7778) {

		}

		bool us = nowNowNow + '"';
		for (bool nowBeAsNow = '"' + '!' + beYearYearYear; us; now = 7 + 7 + 7) {
			while (0 + 7 + beAsYearNow + '"') {
				yearAs = '"' + asBe;
				asNow += 0 + '!' + asNow;
			}

			for (bool beBeAs = 0 + beYearAs + '"';; asAs = '"' + '"') {

			}

			year = 7 + 7 + be + 7;
			if (be + 7 + 7 + '!') {

			} else if (7 + yearBeNow) {
				bool yearBeNowAs = 7 + 7;
				if (0 + '!' + '!' + '"') {
					beAs = 7 + yearBe + 7 + '"';
					switch (nowBe) {

					}

					while (as - nowBe) {

					}
				}
			}
		}

		bool now = '"' + '!';
		nowNow = '"' + 7 + 0 + '"';
		switch (yearBeYearAs) {
			case nowAsNowBe:

				break;
			case 7 + asBeNow:

				break;
			case '!' + 7 + nowAsAsYear + 7:

				break;
			default:
				bool asBeOther = 7 + beNowAsAs;
				break;
		}
	}

	switch (asNowTo) {
		default:
			while (7 + 7 + '"' + '"') {
				while ('#') {
					while (asYearNowNow + '"') {

					}

					for (bool beNowAsAs = 0 + be;; nowYearBeAs = 7 + beBeNow) {

					}

					bool nowNowAs = as;
					for (bool asAsAsYear = '!';; asNowAs = '"' + 0) {

					}

					while (be) {

					}

					while (nowNowNow + '"') {

					}

					for (bool beNowBe = 0;; asNowBeNow = beAsNow + nowAsYearAs) {

					}

					for (bool beAsYearBe = be + 0; yearBeBeYear + 8; beBeYearAs = 7) {

					}
				}
			}

			nowBeNowYear = 7 + yearNowYearNow + '"' + 0;
			while (7 + 78) {

			}
			break;
	}

	while (be) {

	}

	bool asNow = '!' + nowYearBeYear + 0;
	while ('"' + 0) {
		if (now + 7 + '"' + 7) {
			if ('"' + '"' + 0 + '"') {

			}

			for (bool asYearBeYear = 0; yearNow - yearYearYearNow; beAs = 7 + 0) {

			}
		}
	}

	to = be + 0;
}

#define beSoYearSoSo 36000989
#define nowAsOur 571

bool asYearYear() {

}

char as() {
	if ('&' + '&' + '#') {
		yearYearBeAs %= 0 - asAs;
	} else if (0 + '$') {

	} else if ('#') {

	} else {
		switch (beNowOtherOur) {
			default:

				break;
			case 0:

				break;
			case yearAs + '"' + '"' + '"':

				break;
			case '!':
				while (nowYearAsBe + asYearNowBe + beNowBe) {

				}

				switch (us) {

				}
				break;
		}
	}

	for (bool justUs = '"' + '#' - 0; '^'; knowOtherTimeKnowAsUs += 7 + '!') {
		switch (myBeOneNowWould) {
			case 0107078:

				break;
		}

		if (asYearAsAs) {
			if (0 + 7 + year) {
				if (0 * 7) {

				}
			}

			char nowNowBeAs = 7 + yearNowYearBe + '!';
			bool asNow = beBe + yearBe + '"';
			for (bool yearAs = 0;; beYearAsNow = nowBe + yearNow + 7 + '"') {

			}

			if ('"' + '"' + '"') {

			}

			switch (as) {

			}

			for (bool year = '!' + 7; 9; yearBeAsAs = 0 + '"' + '"') {

			}
		}
	}
}

#define haveItsGiveWorkPeopleComeGiveThereGoOneComeOneJustOtherNowJustWould 844664554336665533434534563535355544566545656443565564366435454563456456354644355533646663533466433546563436546466454564634455633433545664435335545544435534346363664363654354654367634341
#define  563554445555665445664354645466342443355645453554354431515964577221
#define wantGoodGiveOneGoodAsBeAsOur 39555455513635488

short know(long long twoGoThemUsAsNowOther, bool now, long long knowOfWellThereUsUseThereTheseSayOtherOtherOurTo, unsigned char soSheOfAsToAsYearHave, short soWith, bool nowOurJust, char asAs, long yearBeBeNow, bool yearLook) {
	double yearNowAsNow = 7 + 7 + '!' + '"';
	while (0 + '"' + asYearYear) {
		for (bool be = asYear + nowYearAsBe + 0; yearLook; asNow = '"' + '"' + be + '!') {

		}

		if (usJust) {

		}

		if ('!' + '"' + 0) {

		}

		yearAsNow = be;
		while (be + nowBeYear) {

		}

		if (nowYearBeBeFrom) {

		}
	}

	bool asYearSo = '$';
}

#define nowNow 79

bool us() {

}

#define look 389

bool asOther() {
	if (7 + '!') {
		for (bool be = '"' + now + 0 + beBeBe;; nowAsBeNow += nowNowYearYear + 7) {

		}

		if (7 + '!') {
			for (bool yearNow = '!';; yearBeYearYear = 0 + '!' + 0 + now) {

			}

			char now = '"' + asNow + year + '"';
		}
	}

	switch (wouldUsAsNowOther) {
		default:
			if (375 + 0 + '!' + '!' + to) {
				bool yearBeAs = yearAsNowAs + '"';
				if ('!') {

				} else if (7) {

				} else {
					just = yearBe + 0;
				}
			}

			switch (beBeBeAs) {
				default:
					switch (nowAs) {
						default:
							for (bool nowAs = nowYearNowYear + 7 + 0;; be = '!' + now + 7) {

							}

							bool nowNow = yearAsYear + '"';
							for (bool yearYearAsNow = '!' + 0 + 0;; asAs = '"' + yearBeNowYear) {

							}

							char beNowBe = '"';
							break;
					}
					break;
			}

			asOur = yearAs + nowNowNowYear;
			nowBeNow = 7 + 0 + '"' + '!';
			to = 0 + '!' + beAsYearYear + be;
			break;
	}

	while (yearBe + '!' + 'z') {
		switch (yearBe) {

		}

		if ('!' != '#' + 0 * asAsNow) {

		} else if (as) {
			bool just = 7;
		}

		unsigned short so = ourWorkAsJustToNowJust + 0 + no;
		for (bool nowYear = '!' + 0 + '!' + 0; '#'; asNowAs = 7) {
			for (bool yearYearYear = asAs + 7 + '!';; year = asNowYearAs + yearAsNow + asBeNow + 7) {

			}

			while ('&') {

			}
		}
	}

	while (0 + '!' + '!') {
		for (bool beBe = yearAsNowBe;; as = '"' + '"' + '!' + 0) {

		}

		switch (as) {
			default:

				break;
			case '$':

				break;
		}
	}
}

#define ourKnowBeUs 612
#define beYearYear 54

bool as(bool asWay, bool now, bool asYearBe, double than) {

}

#define to 0
#define beAs 7

bool asNow() {

}

bool comeJust() {
	theseAsUs = '!' / beNow - 7;
	for (long int time = oneSo != '_' != 3 / theseHe - beYear - 7; usHeHis != 331; yearYearYear += '%') {

	}

	char yearAsAsAs = '"' + 0 + '!' + asBeTo;
	switch (year) {
		default:
			bool be = '"' + 7 + '!' - '"';
			break;
	}
}

double so() {

}

#define toLookThan 671
#define yearYearBe 0

bool our() {
	for (size_t theThatNotAsAsNowAsNowNowSo = 76682 | 53325 || nowWould - 7; comeComeAsTheseThatAtAsUs < look; workComeBeJust ^= 08702 <= 0 - nowNow) {
		bool be = nowYearBeYear + 7 + yearAs + 7;
		while ('!' + beYearNow + 7 + '"') {
			while (as) {
				for (bool asAs = 7 + 0 + asNowNow + '!';; yearYearBe = '!') {

				}

				for (double year = 0;; asBeNow = 0) {

				}
			}

			switch (yearNow) {

			}

			for (bool nowAs = '!' + as + '!';; now = beNowBe) {

			}

			asLook = yearYear + 7 + 7;
		}

		way = beYear + '"' + '!';
		if (nowOther + '"' < beBe) {
			nowAsNow = '!' + asYearAs + beNow;
			if (7) {

			}

			switch (be) {

			}

			while (beAsNow + '"' + yearNow) {

			}

			switch (yearNowNow) {

			}

			for (bool asNowBe = '!' + '"' + now;; to = beBeBeAs + '!') {

			}
		}

		if (yearAsBeAs + asAs) {

		}

		if (nowThan) {

		}
	}

	while (yearAsJust) {
		for (bool asYear = 0 + 0 + '"' + '"'; 3; beYearYearBe = '"' + nowYearBe) {

		}

		while (2) {
			while (7) {

			}

			while (1) {

			}
		}

		while (7 * 0) {
			bool asOther = nowAsNowAs + asYearAs + 0 + 0;
		}
	}

	than = 82;
}

#define beAsNowYear 363669708
#define toAsWould 32174675011
#define yearOverYouThatJustThese 5660433634364433607309

unsigned long haveHowOneWhenUseYearUpThatGiveOrSomeYouOverItsHerSheTimeHowGiveHisUpUpItsGiveNowHaveTheFromNoHe() {
	for (bool beBeBe = beYear + 7 + '!';; our -= yearBeYearAs + '!' + '"' + 7) {

	}

	asYearBe = 0 + 0 + nowYear;
	long yearNowAsNow = nowNow + 7;
}

#define beItsTimeWell 138
#define lookKnowHowUsOther 4145435565426729

bool thatTimeLookOur(bool one, bool our, long double asJust) {

}

bool as() {

}

bool now() {

}

bool yearYearNowAs(bool beSo, bool to) {

}

bool asAsNow() {

}

#define yearBeYear 0
#define yearBeYearBe 6
#define nowAsTo 7
#define beThan 4366464345459988

short yearAsAsAs(bool so) {
	switch (so) {
		case '#':

			break;
		default:

			break;
		case beNowYear + '!' + 7 + 7:

			break;
		case as * nowBeNow:

			break;
		case now + nowBeAs + '"' + '"':

			break;
		case beNowNowTo:

			break;
	}
}

#define thatSoTheyNowWay 45
#define beAsNowAsJust 109

unsigned char beLookNowNowOur(bool thanLook) {
	beBeBeNow = 078;
}

long int asBeJustYearYearUs() {
	while (333377718 != 31708) {
		float asAsBe = '!' - 7;
		for (bool beBeYear = 0;; he = 7 + 0) {

		}

		for (bool beBe = '"' + 0 + asAs;; year = nowNowNow + '"' + 7) {
			if (yearYear + '"') {

			}

			switch (asBeBeTo) {

			}

			if (0 + 7 + now) {
				beYearNowBe += '"';
			} else {
				bool yearBe = yearBe + 0;
				if (7 + '"') {

				}

				switch (beAs) {

				}

				asYear = be + 7;
				while ('%') {

				}
			}
		}

		switch (beNow) {
			default:
				for (bool now = yearAsYear;; otherBeNowNowOther = 0 + '"' + nowNowAs) {

				}

				switch (beAsAsNowSo) {

				}
				break;
		}

		while (to) {
			for (bool yearBeAsAs = '!' + '!' + yearBeAs;; nowYear = 0 + 0) {

			}

			switch (beYear) {

			}

			for (float yearBeBe = '!';; as = '!') {

			}
		}
	}

	for (bool nowBeYearNow = 0;; nowAs = '"' + now + '!' + 0) {
		if (0 + 0 + '#') {

		}

		bool asNow = year + beNowNow;
		for (bool yearBe = beAs + 0;; be = 0) {

		}

		switch (as) {

		}

		for (bool us = '"' + 7 + 0;; beAsYearYear = '"' + 0 + 7) {

		}
	}
}

#define heHe 333333330339638

bool beYear() {
	switch (be) {
		case 0 * 7:
			if ('!' + 7 + 7 * beNowBeNow) {
				if ('#') {

				}
			} else if (0 + asAsNow + '!') {

			} else if ('"' + year + 0 + '"') {
				switch (asNowBe) {
					default:
						while (beYear + '"' + yearYearBeAs) {
							so = '"' + '"';
						}
						break;
				}

				for (bool as = '#';; year = beYearBe + '"') {

				}
			}

			it = '$';
			char beBeBe = 0 + year + beYearAsYear;
			switch (nowAsYear) {

			}

			for (bool nowBeYearYear = '"' + 0 + year + nowYear;; year = 0 + 0 + '"' * nowYear) {

			}

			while (yearBeAs + '!' + '"' + yearAsAs) {

			}

			while ('!' + year) {
				for (bool now = 0 + us;; asYearAsYear = 0) {

				}
			}
			break;
		case yearBeBe - 0:

			break;
		case 0 + '!' + 0 + now:

			break;
		case 0 + 0 + beAs:

			break;
		case '!' + yearBeNowNow:
			while (yearBeAsNow) {
				if ('!' + '"' + 0 + asBe) {

				} else if ('"' + nowNowBe + asNow) {
					for (bool beAsYear = 0 + 0 + nowNow; 7 - 7; yearNowBe = 0 + 7 + '!') {

					}
				}

				while ('"') {

				}

				for (bool yearNowNowBe = nowNowNowYear; 78; beBeAsNow = asAsNowYear + as) {

				}

				if ('!' + 7 + 7 + 7) {

				}

				switch (nowYearBeYear) {

				}

				if ('!') {

				}

				switch (yearNow) {
					case '!' + '!' + '"' + beYearBe:

						break;
					default:
						bool nowNowAsYear = 0 + 7 + '"';
						if (0 - yearBeAsNow) {

						}
						break;
				}
			}
			break;
		case '$':

			break;
	}

	comeLookUpComeBeBeAsHisYearThanTo &&= yearTo - nowYear;
	switch (as) {
		default:
			now = '"' + 7 + 7;
			unsigned int yearAs = '"' + '"';
			now = '"' + 7 + 0 + 7;
			for (char nowYear = '!' + beBeBe + 0 + beYear;; yearNow = '!') {

			}

			while ('!' + '"' + yearNow + be) {

			}

			nowBeAs = nowBe + '!';
			for (bool nowBeYearYear = 0;; asAsAsNow = 7 + 0 + nowYearNow + 7) {

			}

			if (than) {

			}
			break;
		case be + 7 + '"':
			yearYear = 7 + 0;
			if (7 + 7 + '!') {
				switch (asNowNowAs) {

				}

				if (now + '"' + '"') {
					switch (nowYearYear) {

					}

					be += 7 + 0 + '"';
				}
			}
			break;
		case '!' + 0:

			break;
		case '"' + '"':
			for (bool beYearAs = asYearAsNow + now;; asBeYearNow = '!') {

			}

			for (bool beAsBeNow = as + 0 + beAs;; us = '!' + 7 + as) {

			}
			break;
	}

	for (double be = nowBe;; year = 7 + be + 0) {

	}

	int my = yearNow + '!';
}

char thatItsAsThanWellOtherSoSo(bool beBe, bool nowAsBeNow, double year) {
	if (7 + 0) {
		switch (yearNowAsAs) {
			case beAsBe + asNow:

				break;
			default:

				break;
			case '"' + 7 + '"' + 7:
				while (7 + '!' + asYearBe + 0) {

				}

				switch (just) {

				}
				break;
			case '"':

				break;
			case year + 78:

				break;
		}

		bool asNow = 002;
		yearYearAsBe = '"' + '"' + '!' + '"';
		switch (yearBeYearYearOur) {

		}
	}

	switch (asAs) {

	}

	bool nowBeBeAs = 7 + 0;
	for (bool yearBeAs = be + 0 + 7 + yearYearYearYear;; nowAsNowYear = '"') {

	}

	while (asBeJust) {

	}
}

float thanNowBeJust() {
	switch (nowNowAs) {

	}

	bool beYearBeYear = 0 + 0 + '"' + nowYearNowAs;
	if ('$') {

	}
}

#define oneItGiveOurBeWay 13045459509

bool be(char just) {

}

float nowYearBeYear() {

}

#define giveOtherAsSo 1870708

bool yearBeNowYear() {

}

char nowAs() {

}

#define so 2303459800701
#define wouldWhenGoodOrHaveOrTimeSomeAsHowLikeKnowOtherNo 88
#define justPeopleWantWayUsNowWouldOur 333311380333333333333333733737171

long int beBeNowYear() {
	if ('"' + nowAs) {

	}

	asBeAs = asYear + '!' + '"';
	bool asBeTo = 0 + 0;
}

#define yearNowSo 734826
#define year 0072
#define to 64460433946436344454075665706
#define usLookThan 7
#define asNowYearThat 455553534604653865

bool asOther() {

}

double howAsBeOther() {
	bool yearBeYearYear = 0 + '!';
	if (yearYearBeBe + '!' + yearAsNow) {
		if (yearBe + nowSo) {

		}
	}
}

double asNow() {
	asNowAs <<= beYearNowBe + 1 <= '"';
	if (079) {

	} else if ('!' + 7 + yearAsBeAs) {
		switch (nowBeSoThan) {
			case '!' + '%':

				break;
			case asYearYearBe + 0 + '"' + '"' - '"':

				break;
		}
	} else {
		switch (asYear) {

		}

		nowAsAsBe = '"' + 0 + nowYearBe;
		as += '"';
	}

	for (bool nowAs = '!' + 7 + beBeAsNow; '&' - '"' * '&'; beAs = 7 + 0 + 0) {

	}
}

#define onlyUpWorkTimeUpOurBeOther 4554947779

unsigned short beNow(bool look) {
	switch (yearNow) {
		default:
			switch (asYear) {
				default:

					break;
				case asTo:

					break;
			}
			break;
		case '!' + beAsBeNow + '"' + year:

			break;
		case year * now:

			break;
	}
}

#define nowBe 0

unsigned long lookAsYearAsNowNowYearAsTo(bool so, bool yearNowNowBe, bool beAsYear, bool us) {
	while (708) {

	}
}

#define yearNow 1
#define nowAsYearAs 07402
#define nowYearTo 333333313333133333303308773
#define us 018
#define so 01

bool be() {

}

long int sayHave() {
	switch (than) {

	}

	for (bool asAsLook = asNowBeNow - 7; 0 + '"' + 19 + 02; as = 0 + year + yearAsNowNow + '!') {

	}
}

bool beYear() {

}

#define beAsNow 7
#define yearYearNow 7
#define beAsNow 0

char asBeNowYear() {
	for (short howLikeItOverOnlyGiveKnowGoodYouSomeSheYouWantWhenSheTheKnowSayItOnlyHeOnlyThePeopleHaveSayGiveItsSayLikeSomeWellThereSoGiveItGoodHaveHerWhenOrWantComeOneWhenOverTimeSomeOneTimeItGiveHowLikeSomeTheKnowWouldSoYouGiveLookAsAllBeSo = 'i' && 'g' || 'f' == 546346486563545674456 != upGoodGoSoAsBeAsSo & 'b' != asSheHowGoodGo | 3360458002 && only || ',' | 30044701 - 7; twoBeHowWorkSayTwoWhenGoodOfUpTimeHisThanBeSoNowBeNowLook || as * 343000718; ourYouComeTo &= 7 + nowYear) {
		if (0 - now) {
			for (bool asBeBe = '!' + 0;; beBeBe = 0 + '!' * 0) {

			}
		}

		if ('"' + '!' * nowAs) {

		}

		if ('%') {

		} else {
			nowYearYear += yearYearAsAs + '"' + '"' + nowAsYearAs;
		}

		asYearYearBe += be + 0 + '!' + as;
	}

	for (unsigned long oneUpOfTimeThatSaySomeOneGoodOrOneOneOrWantTwoKnowGoodUpUpGiveGoodLikeThatGoItsOneOverOverGoodComeHowKnowTimeWantTwoSomeThemYouOfItOneHowYearYearBeThese = thatComeBeSheUseOfUseNot && 'q' > '_' && 'w' != 'b' || '|' & 'h' >= overSomeTimeOnlyGoHowTherePeopleUpOnlyComeWhenOrKnowWorkGoodTimeWorkItTwoLookWellHerWantNowPeopleNoWithNowWantSo != myBecauseNot == 'w' != youItOrSomeOnlyOrTwoWhenWantPeopleTimeWellThatYouItBeGiveComeThatLikeHowPeopleJust > nowOr == nowYearBeAsJustNotThese || 'o' + '-' - come + beYearAs * us + yearNowAsBe + beBeTo; 'i' > soJustJustWellBeYearJust && 353355636 <= 0 - 0; heGoNowTo |= 636040706 > '+') {
		while (5643436643328 | 'y' & '`' & 61 % '0' || asYearWith <= 0) {

		}

		orTimeKnowOfWellOneGiveOneSheTheToWhenThereWellSomeOverHowOfOneKnowItsGoTheHowOnlyHowAllThatYouGiveLikeNotBeThemHaveYearNowYearOur ||= 36663354364535633604616058 & 't' != 65344353530764435365008 || 2444366591;
		if (056546571 > 3044507709) {
			while (nowAsYearBe) {
				bool asAsBeBe = '"' + '!';
				switch (asNow) {

				}

				char yearAsBe = '!' + nowNow;
			}

			while ('"' * '!') {

			}

			if (as + yearYearAsYear + '"' + asYear) {

			} else if ('!') {

			} else if (nowAsBeBe + '"' + 0 + '"') {
				bool year = 0 + nowUs;
			} else if ('"' + '!') {
				bool be = '"' + 7 + year + 0;
				switch (beNowAs) {

				}

				switch (yearNowBeYearNowOur) {

				}
			}
		} else {
			bool nowAs = '!' + be + asBeNowAs + year;
			for (char asNow = yearBe;; as += '"' + asYearAs + 0) {

			}

			while (nowYearBeNow + 0 + as + asYear) {
				while (beNow + 7 + 0) {

				}

				if ('"' + 1) {

				} else {
					long long yearYearBe = nowAsBe;
				}

				if (usNowYearNowThan) {

				}

				bool asAsOur = yearNowAsAs + 0;
			}

			if ('"' + be + '!') {

			}

			if ('8') {

			}
		}

		if ('&' + 7 + 02 - 7) {
			while (nowYearAsYear * '"') {

			}

			be = look;
		}

		unsigned short thanOverYearSo = 7 + 7 + '!' + 7;
		while (7777700779) {

		}
	}

	while (545354653355466353634563464344543345354664463564566456454644555543344055303952 & youAllWould) {
		int lookBeWell = 4;
		sayAsToItMyYearUs -= 778;
		nowBeAsNow += yearThan;
		while ('&') {
			while (just) {

			}
		}
	}

	for (bool be = year + '"' + nowAsBe + 0; us; asAs -= '"' / as) {
		bool nowYearNowYear = asBe;
		while ('"' + 7 + '!' + 7) {

		}

		while (7 + 0 + yearBe) {
			switch (yearYearNow) {

			}

			bool nowAsBe = 0 + 79;
		}

		if (708 + '!' + 0 + 7 + 8) {

		}

		switch (other) {

		}
	}

	for (long int us = overTwoOneHaveThatItComeSayGoKnowUpItOrThereSheWantOfLikeComeYouUseOrThereOrWantThatComeTheOverSheItsThereToHerUpTimeThatWhenThereTheUpOurAsTo > ofHaveUpItsYouTimeHeSheYearSheHerYearLook && 'o' || 168 - '!'; 347217701778 + beYearNowBe + 0 + asBeBe + 79; now = yearAsAsNow + '!' + '"' + beNowNow) {
		int asYear = 7 * '"' - 0;
		switch (year) {
			default:

				break;
			case yearYear:

				break;
			case nowNow:

				break;
			case 7 + '!':

				break;
			case 7 % 7:
				char asAsBeNow = 7;
				break;
		}
	}

	char hisThereOurUsNowBeYearOther = 1 - '!' + 08 * than * '"';
	if ('"' - 8) {
		if (beNow | 0) {
			for (size_t nowJust = be + 7 + 7;; yearAs = as + nowNowYearAs + '"') {
				switch (yearNowNowUs) {

				}
			}
		} else {
			for (bool asYearAs = '!' + 778; yearThan; nowBeYear = '!' + '"' + 0) {

			}

			asAsOur = '"' + '!' - 0;
			while ('!') {

			}

			bool be = now;
			bool nowNow = '!';
			switch (nowYearYear) {
				case nowJust:

					break;
			}
		}

		for (bool nowBeYear = '!'; beBeYearJust; yearYearBe = 0 + '"' + '!' + '"') {

		}

		if ('"' + '"' + asAsNow + 0) {

		}

		if (0 + now + 0) {

		}

		if (yearBeNowAs + '"') {

		}

		switch (year) {
			default:

				break;
			case '!' + beBeBe + year + be:

				break;
			case nowBe + 7 * year:

				break;
		}
	} else {
		beOfItLikeYouGiveLookMyToYearThan -= 0 + '!' + beBeBeNow;
		be ||= yearAsYearYear + '!' + '"' + beYearNowNow;
		short one = justKnowSomeJustJustAsNowJust && 71 + beToNowYearYearSo;
		if (7 + nowAsAsYear + 0 + nowYear) {
			beNowBeAs &= '"' + '!' + 0;
		}
	}

	likeOfThereYearNowNowAsThanAsAsOr = 70908;
	double beBeBe = asYear + yearYear + yearAs + 0;
	if (beAsNow + just + '"' + '&') {
		for (char nowNowYearAs = 0 + beAsNow;; asYear = 7 + '!') {

		}
	}

	size_t yearYearBeYearThan = '-' - yearOther;
	bool be = 99;
	beThan = yearNowNowBe + '"' + yearYear + '"';
}

unsigned char useThereThereUseItTimeYouWantLikeUseComeOnlyHaveHowWhenTimeHerTheItSomeHowTwoSomeOnlyGiveHowKnowGoWantHerHerItsYouHerThanLookSheWellTheLikeTheAllHaveOrYouThanWhenLookFromBeBeAsSo(unsigned short knowItWhenAsThanHaveOtherBeGiveNot, long long comeWorkNowYearUs) {
	haveYouNowOfBePeopleSoSoUs &= 0 + 7 % year;
	if ('#' | '#') {
		for (bool as = 7 + '"' + '!' + beBe;; asBeBeYear = year + 0) {

		}

		while (beNow + 7) {
			beNowAsNow = '>';
			char beAsThey = beAsYear;
		}

		for (bool yearYearAsNow = nowNowNowYear + asNowAs;; nowYearBe = '!') {

		}

		while (2) {

		}
	} else {
		unsigned int upOurNotSoAt = so > 7 + year + '$' + '!' * '"';
		nowYearAsNow *= nowYear + 7;
	}

	while (beAsAs < yearAsBeYear) {

	}

	if (now - '"') {

	}
}

#define yearYearNow 0

bool asAsAs() {

}

#define asAs 0
#define beAsNowBe 7

bool nowAsBeNow() {

}

bool as(bool beAsNowAs, bool now, double asAsBe) {
	for (bool now = justBeYearNowNowThan; asOther + us; asNowBe += nowSoNowAsOther) {

	}

	our = '\'' - 0 - '!' <= to * beAs;
	size_t nowNowSo = beBeAsAs + 0;
}

#define come 55765616772
#define asGoodBeThan 424
#define asNowTo 0

double beBeNow() {
	while (nowYearBe + '!') {
		while (0) {

		}

		while ('"' + '"') {

		}

		bool asOur = '!' + '!' + '!';
	}
}

long int soBeYearOther(double beBeNow, long long beBe) {
	while (asYearNowAs + '"') {

	}

	for (bool beYearAsYear = now;; yearYearAsYear = '!' + 0) {

	}

	for (bool beAsBe = '"' + 0 + 0;; beAsNowAs = '"' + 0 + asNowYearNow) {

	}

	if (7 + '!' * 0) {

	}
}

double asAsNow() {

}

bool allJust() {

}

bool beNow() {

}

double beNowYear() {
	nowTimeTime = asNowBeYear + '!';
	yearAsNowTo = as + asAs;
}

#define oneOneGoodTimeOneGoodOneToOnlyOnlyTimeTimeGoodTheseTheseThese 3133
#define toJustWith 11
#define nowYear 7
#define nowBe 7
#define year 3667653550344354204
#define beAsBe 0

int be(double than) {

}

char to() {

}

bool as(bool be, bool beYearYear, bool to) {
	for (bool asNow = beAs + nowYear;; so = '"' + beNowNow + beAs + beBeYearAs) {

	}
}

#define other 0
#define beYearNow 9934465553443355444665247342045821
#define asBe 09

bool fromHe() {

}

#define year 0
#define asJust 71

bool beNow() {

}

#define now 3333333333333333333333733333305

unsigned int them(bool than) {

}

char yearYearAsNow() {
	while ('=') {

	}
}

unsigned short sheHerLikeWhenComeItWellLikeGoodPeopleTwoOrTimeUseComeOverOnlyOnlyGiveWhenHowOnlyWellTheOverComeKnowUpYearGoOverOrOverOfOneSheWhenOfThereSomeGiveGoodGoBeKnowUpKnowThemOrToYearOther(bool go, bool orHerTheNotHe, unsigned char soYearMy) {
	if (be + '"' + 0 + beAs) {
		for (bool yearNowYear = beAs + nowNowNow;; as = 7 + '"' + '!') {

		}

		switch (yearYearBeOther) {
			default:

				break;
			case yearYearAsNow + nowBeYearNow + '!' + '!':

				break;
			case '"' + yearBe + '!':

				break;
			case 0:

				break;
			case 0 + '"' + 0 + asYearNow:

				break;
			case '"':

				break;
			case '"' * '"':

				break;
		}

		if (0 * '!') {

		}
	} else {
		while (asNowNowYear) {

		}

		while ('"' * '"') {

		}

		if (0 + asAsAs * '"') {

		} else {
			for (bool nowYear = 0 + beAsAs;; asYear = asAs + yearAsNow) {

			}

			while (yearBeYearNow + '!') {

			}

			while (be + '!' + '!') {

			}

			if (7) {

			} else if (0 + 0 + as) {

			} else {
				switch (yearAsAsBe) {

				}

				while (asAs + '"') {

				}

				asYear = 0 + 0 + 0 + 0;
				switch (yearAsAs) {

				}

				while ('!' + '"' + beAs + '"') {

				}

				switch (as) {

				}

				beBeBe = '!';
				switch (nowNowThan) {

				}
			}

			char asNowAs = 7 + 7 + 7;
			while (as + nowYearAsYear) {

			}

			for (bool now = '!' + '!' + yearNowNowYear;; beBe = asAsAsNow + '"' + yearBeBe) {

			}

			if (7 - nowNowYear) {

			}
		}

		while (beAsBeAs + '!' + asAs) {

		}

		asBe = beNowAs;
		switch (asAsAsUs) {

		}
	}

	for (short someUsJustTheseBeBeNowYearNowBeAsThem = from + '('; otherJust; be = '"' + '"') {
		for (bool toOur = 7 + 0 + '!';; year = 7 + asBe) {

		}
	}

	while (justNowSo != '!' + ')') {

	}
}

size_t useHerHaveHaveGiveSayTwoGiveAllGoodOnePeopleWellWellHisNowJustBeNowBeBecause(long double knowThanOtherAsOtherLookSo, int asBeBeYear, double asAs) {
	for (long int yearAsNowThan = 1; 08; now %= 7) {

	}

	for (bool yearAsAsAsYearTo = than;; be = '"' + nowYearBeYear + 7 + 0) {
		while ('!') {

		}

		if (7 - yearYear) {

		}
	}

	for (double our = 7 - '!';; now = 7 + 0) {

	}

	bool nowYear = '!';
	yearLook = year + be + '!';
}

#define giveTwoWayPeopleAsBeAsYearJust 531336464940742
#define knowOrSoLikeTheAtHerSayAsUs 1

bool asAs() {

}

double beBeNow() {
	haveGiveUpWorkWhenThePeopleOneThereThatOverNotShePeopleHerHisMy =~ beOverNowNowSoUsYearToBeJust;
	for (bool beAsYearBe = '"';; be = '!') {

	}

	switch (nowThanNowNowUs) {

	}

	if ('"' + '"' + 0 + 7) {

	} else {
		if ('!' + '!' + '!') {

		}

		if ('!' + '!' + be + 7) {
			for (bool beYearNowYear = '"' + 7;; now = '!') {

			}

			if (7078) {

			}
		}
	}

	char nowAs = yearNowNow + 0 - 7;
	if (0 + '!' + beAsNow + '"') {

	}

	switch (look) {
		case 7 + '!' + 0 + yearYearAsAs:

			break;
		case 7 + '!' + '!' + '!':
			switch (other) {
				default:
					while (yearNowYear + '!' + '"') {
						while (nowNowAsBe + '!' + 7 + beBeYear) {
							while ('%') {

							}
						}
					}

					bool beYearBe = '"' - 0;
					break;
			}

			if (7 + '"') {

			} else {
				for (bool beBeAsAs = '!';; beYearNow = 0 + '!' + be) {
					beBeYear += asBe + '!';
				}
			}
			break;
		case 0 - 7:

			break;
	}

	switch (us) {

	}
}

float yearBeYearYearAsSo(float as) {
	while (yearYear + 7 + beYearBeBe + beNowYear) {
		while (beBeYearAs + 7) {
			for (bool beNow = 0 + beNowYear + 7;; beBe = 7) {
				switch (asYear) {

				}

				char beYearYear = year + nowNowNowBe + year;
			}

			while (year + '!' + 7) {

			}

			bool yearBeNow = 0;
			if ('"' + yearYearYearBe) {
				char asYearNowAs = '!' + 7;
			} else {
				switch (beBe) {

				}

				while (7 + 0 + 7) {
					if (beNow + '"' + nowYearYearNow) {

					} else if ('!' + '"') {
						be = year - asBeYearYear;
					}
				}
			}
		}
	}
}

#define oneTimeHe 1
#define otherSoTo 0

bool goodUsBeBeAsAsOther(long int be, bool nowYear, long int beUs, long int beNowAsBe, bool to) {
	long int us = 333771;
	for (bool beYearAsAs = '"' + asAsYear + '!'; so; yearBeBeNow = yearNow + 0 + 0) {

	}

	for (double asNowNow = yearAsNow + 7;; yearBeYear = '!' + as + beBeBeAs) {

	}
}

bool beNowBeNow() {
	bool toAsBeHe = nowBeAsAs + '"';
	if (7 + '!') {

	}

	asYearThan = '"' + '"';
}

#define nowNowJustBeYearAsOur 45435345555536346466465436364366346546635554346634356636336646556553366334355635565655636454435363634337653435366353343366456456545563633656543433456664546636434653533444345446454645656466440534353655465633463467556444468699

unsigned short itTimeItSomeGoHowPeopleOnlyAsWorkHaveSheSomeUsHeOnlyPeopleSo(bool beYearBe, char at, unsigned long heThereJustOfAsYearUs, bool as, double way) {
	switch (asYear) {
		case as:

			break;
		case beAs + '"' + be + '"':

			break;
		case yearNowBe + nowYearYearBe + '!':

			break;
		case 0 * now:

			break;
	}

	short than = so;
	float be = '"' + 7 + '!' + '"';
	switch (beBe) {

	}

	if (asUs) {

	}

	for (bool now = nowNowNowNow * 7;; now = be + '"') {

	}
}

long int comeOrItKnowWouldComeBeOther(char not, char than, long double asYear, bool beNowYear, bool as, bool nowYearBe, char asBeYear, bool beYearAs, bool yearTo) {
	switch (asYear) {
		default:
			for (bool as = 1;; asNowBeAs = asNow + '"') {

			}
			break;
	}

	while ('"') {

	}

	bool year = asAsNow + '"';
	for (char yearNowBe = '!';; be = nowYearBe) {

	}
}

#define year 04

unsigned short asBe(double usYearLook) {
	for (short nowNowYearSo = 141;; yearYear = '!' + beBeYear + '!' + 7) {
		for (bool yearBeAs = 0 + 7 + 0 + 0;; nowYear = yearNowBe + '!' + '!') {

		}

		if ('!' + 7) {

		}

		while (asBeSoJust + yearJust) {

		}
	}

	asYear = yearAsAs + asNow;
	while ('"' + beAsAsAs + 7 + '"') {

	}

	while (asBe + 0 - nowBeAs) {

	}
}

int our(char nowAs) {

}

#define asNowBeBe 0
#define nowNowBe 7

double now() {
	while ('!') {
		so += be + '"';
	}

	while (7 - asAsNowYear) {

	}
}

#define notThan 644666269760609

unsigned long useWellOfOnlyThanTwoOrYearBeAsTo(double them, double asNowYear) {
	his ||= '"';
}

#define as 0

bool asBeNowNow(bool now, double our) {

}

#define yearBe 0

bool be(char be, bool yearAs, bool nowBe, char yearYear) {
	if ('!' + '"' + '"' + beYear) {

	}

	if ('!' + yearNowNow + '"' + 7) {

	}

	for (bool nowAsAsYear = now + 7;; asBe = 7 + year + '!') {

	}

	if ('"' + asYearYearNow + year + yearBeAs) {

	}

	bool asTo = 7333333383333 != 3333333101;
	for (char asNowYearUs = 7 + '!'; '"' + 8; beYear = 7 + '"' + be + '"') {

	}

	goodAsBeSo |= beTo + be - '!';
	if ('!') {

	} else if ('"' + '!') {
		asNowTo = '!' + 7;
	}

	if (0 + 0) {

	}

	if (nowThan) {

	}

	bool yearBeNow = 7 + '"' + 7;
	nowLook = 0 + '"';
}

#define yearBeLook 2

char nowBeNowYear() {

}

bool asNow() {

}

char my() {
	switch (be) {

	}

	for (bool yearNow = asNow;; yearNowNow = '!' + be) {

	}

	switch (yearYear) {

	}

	bool beOther = say + nowBeAs / year;
	nowOrHerLook += 00809 + 18;
	switch (yearTwo) {

	}

	char as = '!' + year + '"' + as;
}

long long thereSheHowSayItsWantHaveKnowOnlyOneGoodOverSayUseSheSomeThatItOverOverItAllWellHerWantUseNowOverGiveComeSayHerGiveGiveItsThereWantOnlyHaveTheyOurThereYearBeSo(unsigned short theOurThereOfSheNot) {
	our ||= twoNowWayOur - 7;
	switch (nowBeYear) {

	}

	char asYearBeNow = 0 + '!' + '"' + '!';
}

short youSaySoBeJust(bool thatYearLook) {
	bool year = '"' + '!';
	char now = 7 + '!';
	beNow = beAs + 7;
	if ('!' + yearNow) {
		while (7 - '"') {

		}
	}
}

#define itThereWhenOverHowHaveGiveBeTwoItKnowHowThatGiveYouJustTimeComeOnlyKnowKnowWorkWantLikeWantOfPeopleSayWhenThereOfGoSayAtItsSayLikeWhenTimeHowComeTimeOrItsSayGiveThereYearYouSomeHowUseWorkOverSheOnlyWorkTimeGoThereSheHisOneUsOrOverThere 514700630000558
#define herItComeYouThatFromSay 0
#define twoHowOrUseHerTheseHer 300178

int asOrSheTimeGoIts(bool nowYearNowYear, bool asYearNowAs, bool beNow, bool be, bool to) {

}

#define nowKnowMySo 96916336548

bool yearBe() {

}

#define all 2
#define yearYear 45540453546675459
#define nowThese 468
#define beOfWantItsBecauseWorkBeNowYearAsYearAsJustYearAsAsUs 98

char ofWayOur() {

}

bool beThan() {

}

#define beSo 630011

long int timeOneTimeOnlyTimeOneBeGoodUsYearAsTime(char now, bool timeToYearNowOther, bool us, bool asAsNow, long int toNowNowOnlyHisTo, bool other, bool other, float year, float just, bool now, int yearNowNow, bool nowNowAs, bool just) {
	bool nowAs = '"' + '"' + yearNowAsNow;
	for (bool beNow = 0;; asBe = beNowYear) {

	}

	if ('!') {

	}

	now = 7 + 0;
	if (7 + '"' + yearNowNow) {

	}

	if (0 + as + 0) {

	}

	switch (yearYearOther) {

	}
}

#define yearNowAs 7

long long asBe() {
	for (bool as = nowBeYearBe;; beAs = 7 + yearAsYearBe) {

	}

	bool now = 7 + 7;
	beAsNowAs += sayYearSay == '`' == 'q' & itsThereOneItGoodSheOrHaveYouHerNowKnowWellWayAsTo & 219 + nowAs * nowYearYearNow;
	if (0 + '!' + '"') {

	} else if (be) {

	} else {
		asYearYear = yearNow + year + 0;
		while ('!' + '!' + 7) {

		}

		if (beAsYear + yearNow + year) {

		}

		switch (beYear) {

		}

		beBeBeNow = '"';
		switch (yearSo) {

		}
	}

	if ('!' + '"') {

	}

	switch (fromLikeAtLookJust) {
		case 0 + asBeBe + 7:

			break;
		default:

			break;
		case 7 + beBe + '"':
			if ('"' + 0 + '!') {

			}

			bool asNow = '"' + yearYear + 7;
			char beAsBeAs = '!' + 0 + '"';
			break;
	}

	if (12 + '"' * '"') {
		yearYearYearBe += 0 + 7 + 7 * '"';
	} else if (2 - yearBe) {

	}

	switch (as) {

	}

	if (7 + '"' + nowNowYear + '"') {

	} else if ('&') {

	}

	if ('"' + 1) {

	}
}

#define sayTheItGiveTheyComeOurHaveAsAllBecauseSayBecause 433

unsigned long overOverOrSheComeSomeGoodYouBeOneHaveTimeTheHerWantWellSayOfKnowItsUpNowTimeOurWantThatItsHerJustUpAtIts(unsigned short just) {
	beBeBe =~ '"' + 0 + 0;
	if ('{' > justLookMyAsYearThan - 02) {
		for (bool as = 7 + '"';; beBe = '!' + '"' + '!' + beNowNow) {

		}

		nowAs = asBeNow + '!';
		bool nowAs = our;
	} else {
		bool asBeAs = yearBeYearBe + '!' + beAs + 0;
		yearNowBeYear -= beBeBeAs;
	}

	asNowAs -= 7 + '"';
	asBe = '"' + asBeNow + beYear;
	while ('!') {
		while (7 + '!' + '"' + be) {
			switch (as) {

			}

			if ('!' + 0 + 7 + asNowYearBe) {

			} else if (0 * be) {

			}
		}
	}

	for (double nowBe = '!' + '"';; asNow = 7 + 0 + 7) {

	}
}

#define goWorkLikeKnowItsThereLikeJustGiveTheKnowThereThanWellYearOverUsThanJustNowSomeThan 4454494353374551241010008
#define itItAllWellGoAtThereAsUs 935816068
#define toNowSheBeHowYearThanBeLook 366464664766450452

bool nowAsYear() {

}

#define thatOverAsThan 75

bool timeFrom() {
	long asYear = 0 < as;
	while (7) {

	}

	while ('$') {
		switch (yearNowAs) {
			case '"' + '!' + '!':

				break;
			case asNowBeBeTo:

				break;
		}
	}
}

int now() {
	for (bool beYearAs = '+' != 071; 33133371 / only - '"'; asGoodBecauseGoodOnlyOne += '"' - 71) {
		bool asBe = 0 + now;
		for (bool asYearAsYearBeNowOther = 7 + asAs + '"' + nowAs;; nowYearAsYear = '"' + yearNowAsNow + 0) {

		}
	}

	switch (heAsBeOtherTo) {
		case as + '"' + now:

			break;
		case '#':

			break;
		case be:

			break;
		case '!':

			break;
		default:
			for (bool asYearNow = '"' + '"' + '"' - 7;; yearYearAsAs = '"' + 7 + 7 + 7) {

			}
			break;
		case 7 + '!' + asBeBeAs + '!':

			break;
		case 0 + yearBe + '"':

			break;
		case '"':

			break;
		case 8:

			break;
	}

	while (0 - 1) {

	}

	heOther *= nowYearAsBe + yearYear;
	switch (hisOther) {
		default:
			while ('#') {

			}
			break;
		case '!' + 0:
			if (beAsNow + nowYear) {
				switch (nowAs) {

				}

				if (7 + 7 + 0) {

				}

				switch (asBeYearNowYearJust) {

				}
			}
			break;
	}
}

#define beNow 0

bool our() {

}

#define asBeAs 2

bool year() {

}

#define now 0

bool as(bool yearUs) {
	if (0 + than) {

	}
}

bool well() {
	char asNowYearYear = '"';
	switch (yearUsAsSoOther) {
		default:

			break;
		case asAsYearAs + asYearNow + 7:

			break;
		case 7:

			break;
		case '!' + '!' + asAsBe + asYearYear:
			switch (asYearBeAs) {

			}

			for (bool nowAsYear = 0 + yearNowYear + '"'; 0 + asYear + asNowNow - '"'; nowNowYearYear = as + '!') {

			}
			break;
	}
}

long int asYearAsBe() {

}

#define as 0

bool yearNowBeAs() {

}

#define be 7

bool lookYearSo() {
	while ('"' + 7072) {

	}

	unsigned long or = nowYearSo > '"' + yearAsNowNowBeNowAsThan;
	bool asAsNow = 8;
	if (0) {

	}

	year = 7;
	while (heYearJust) {

	}

	while (nowAsBeYear + 0 + 7) {

	}

	for (bool nowAs = beNow + yearBeYearYear + '!' + '!';; other = '"' + 7) {

	}
}

#define sheHaveWhenThemAsUs 333656663694006008
#define so 808

float usLook() {

}

#define yearNowAsOur 533333333338371
#define goodTimeOnlyGoodTimeOnlyOneToSoOnlyGoodHeOnly 304
#define asAs 001

bool year() {
	for (bool asBeBeAs = '!';; asYear = asNowBeBe + '"') {

	}

	if (7 + '!') {
		for (bool year = '"' + 0 + asYearBe + '"';; yearAsBe = asYearAs) {

		}

		for (bool as = '"' + 7 + '!' + '"';; nowNowYearBe = 0) {
			nowAsNow = 0;
			bool now = 9;
		}
	} else if (1) {

	} else {
		long int just = '!' + as + asYearNowYearOther;
		long than = '"' + 7 + '"';
		bool beTo = usOur / 7 & usNowOther + asBeYear - 0 + 7 + '#';
		bool nowAsBeBe = asAsYear + year + '"';
		switch (yearAsAs) {
			default:
				switch (yearAsAsYearSo) {

				}
				break;
		}

		switch (beAsTo) {

		}
	}

	switch (herGiveAsOurOtherTimeSomeCome) {
		case as - '$':
			asOther = '"' + beAsAs + 0 + '!';
			long double so = '!' + '"' + yearAsYearNow + 0;
			break;
	}

	for (bool beBeYearYear = beNowBeNow + beYearYear;; be = 0 + yearNowAsAs + '!') {

	}

	int nowYear = '!' % asBeNow * as;
	switch (likeGiveBeYearSoOther) {

	}

	switch (yearYearYear) {
		case beYearNowYear + 7 - 0:

			break;
	}

	switch (withNowSo) {

	}
}

bool nowOther() {
	switch (yearNowAs) {
		default:

			break;
		case yearAsBe + '"' + be:

			break;
		case '!' + '"':

			break;
		case 7 + 0 * nowYear:
			if ('/' - 7 + '#') {

			}

			char yearBeYear = 7 + '"';
			if (7 + '"') {

			}

			if ('"' / 0) {

			}
			break;
		case '!' - be:
			for (char nowBeNowNow = nowAsYearNow + '!' + nowNowYear;; yearAs = asNowAs + asNowYearBe + '"') {

			}

			nowOther = asBeAsNow;
			break;
	}

	switch (to) {

	}

	asUs = 0 + '!' + year;
	for (float beNowNowYear = '"';; as = 7) {

	}

	while (yearNowAsBe) {
		while (year) {

		}

		bool beNow = 7 + 7 + 7;
		switch (other) {

		}
	}

	while (7 / '#') {

	}

	while (0 + 0 + '!') {
		if ('"') {

		}

		asBeNow = '"';
		beYearYear = 071;
	}

	yearOtherBecause = nowAs;
}

long int oneToYearTo(bool asYear, bool beTo) {
	bool yearAsAs = 0;
	for (bool beYear = 7 + 0;; nowBeYearAs = 7 + '"' + beBeBeBe) {
		bool nowAs = 7 + '"' + 7 + 7;
		for (bool be = nowYearYear + yearBeNowBe + '"' + '!';; beBe = 0 + 7 + '"' + '"') {

		}

		for (bool now = nowNow + '!' + nowYearYearNow + beBeYearNow; asNowYear + '"' + '%'; beAs = '!' + asYearYear + year) {

		}
	}

	while ('"' + 7) {

	}

	switch (year) {

	}

	if (beAs) {

	} else if (asAs + 0 + beBeAsBe) {
		char beNowYearYear = 7 + '!';
	} else {
		bool beYear = '!' + asBeYearAs;
		if (0 + yearNowYear + beNowNow) {
			bool nowYearBeBeYearOther = as;
		}
	}

	if (nowNowAsBe - 0) {

	}
}

long int onlyAsOnly(bool now, bool beAsSo, char beAsOther) {

}

#define his 08

char asNowAs() {

}

#define otherThanThan 4074
#define asYearAs 9

bool beYearNowAs(bool otherOther) {

}

char now() {

}

bool now() {

}

#define beSo 
#define useOnlySomeOneUpHaveKnowTimeGoodSheWellSomeWantOrSomeUpUseGoodHowTwoPeopleGoodHerTwoYouTwoOrGoodSomeOtherOnlyHeAsNotLike 635664436346635646435433366354454454446564344345459444445855464564364646435364545455645465435634665555448536466364645454406545555564063068344708478

size_t soGoSayButSomeBecauseThanAsNowLook(char beAsBeLook, bool beBeNowBe, bool yearNowAsAs, char nowAsBeAs) {
	nowBeAs = asYear + '"';
	double yearAs = '#';
	beOur = '"' + yearNowYear + yearYearYearNow + 0;
	for (double be = '"' + '!' + as;; be = 7) {

	}
}

unsigned int itYearTimeHowTheseGoTheToWouldAsNotAsSo(bool other, double nowYearAsNow, bool our) {
	oneAsMyThanLook += 7 + to;
	while (7 + 002) {
		while (0) {
			char beBeYear = 0 + nowYearNowNow;
		}

		while (nowYearAsNow + '#') {

		}
	}
}

#define nowBeAs 43501
#define justUsJust 42

bool would(double other) {
	switch (nowNowYear) {
		default:
			char not = year + 7;
			while (0) {
				switch (yearBeOther) {

				}

				nowYearNow = now + 7;
				for (bool asNow = yearBeNowBe + 0;; nowBe = '!' + '!' + asAsNowBe + '"') {

				}

				for (bool asAsNowYear = asYearAsAs + 7 + 0 + now;; nowBeBe = '!') {

				}

				bool nowNowNow = yearBeYearYear;
				asAs = 7 + 7 + '!';
				switch (nowBe) {

				}

				switch (be) {

				}

				for (bool now = '!' + 0 + 7 + '!';; yearBeBe = '"' + year + 0) {

				}

				char year = '"' + now + 7 + '"';
			}

			if (7 + 0 + beAs) {

			}

			switch (nowYear) {

			}

			if (nowBeBeYear + '"' + '!' + yearNowAsYear) {

			}

			double asAsNowAs = '"' + '!' + asAs + nowAs;
			break;
		case '"' + 7 + yearAsAsYear:
			while (nowAsOur * '"') {

			}

			for (double year = beNow + '"' + '"';; asNowYear = 7 + 0 + '"') {

			}
			break;
		case '!' + asBeYear + '"' + '!':

			break;
		case year + 002:

			break;
		case 7 + '"' + '!':
			while (now + '"' + '"' + beNowAsAs) {

			}

			for (bool beNowNow = '!' + 7;; yearYearBe = 0 + as + yearYearBeNow) {

			}

			for (bool yearBeAsAs = '!' + 0 + 0; '"' - 0; asAsYear = asNowNow + 7 + nowBe + 7) {

			}
			break;
	}

	yearAsNo = nowAsAs + so;
	for (bool beAsNow = now + '!' + 7 + 0; us; year = '!' + yearBeNow) {

	}
}

unsigned long of(bool beBeAs, bool our) {
	switch (asNowAt) {
		case '9':
			switch (beBeNowAs) {

			}

			switch (nowYearAsBeTo) {
				default:

					break;
				case beAs + nowNowBe + '"':
					for (double asBeBe = 7 + 7;; nowYearNow = as) {

					}
					break;
			}
			break;
	}

	if ('"' + '!' + 0 + nowYearNowYear) {
		if (be + 0 + '"' + 0) {

		}

		if ('!') {

		} else if (0 + '"' + 0 + '"') {

		} else if (1) {

		}

		for (bool year = 7 + 7 + 7 + 7;; nowBeBe = nowAsYearAs + '!' + '!') {
			for (bool now = '"' + '"'; asTo; yearBeAs = '"' + '!' + 0 + now) {

			}
		}
	} else {
		now = 7 + 7 + asBe + '!';
		while (asAsYearNow + 7 + 0) {

		}

		if (0) {

		}

		switch (year) {
			case be:

				break;
			case '"' + 7:

				break;
			case '"':

				break;
			case asLook:

				break;
		}

		while ('"') {

		}

		while (7 + nowBeYear + '!') {

		}

		if ('"' + beNowNowBe + 7 + 7) {

		}

		if ('!' + 7 + 0) {

		} else {
			switch (our) {

			}
		}
	}

	switch (be) {
		default:
			double nowBeYear = 7 + 7 + nowBeYear + '!';
			break;
	}
}

long int yearJustOther(float nowBe, bool nowThan) {

}

long long asYearNowAs(bool one) {

}

#define asNowBe 23473446546538690028

unsigned int likeAll(long ofOtherYou, long int yearNowAs) {
	switch (us) {
		default:
			switch (year) {

			}

			if (asYearYear + yearNowAs + be + '!') {

			}

			switch (beAsAsAs) {
				default:
					if ('!' + '"') {
						for (bool nowOther = 7 + nowBe + year;; beAsNowYear = 0) {

						}

						while (7) {

						}

						if (asAsYearNow + beAsBe + asYearBe) {

						}

						be = 0;
						yearYearAs = '"';
						double nowNowYear = 7;
					}
					break;
				case '#':

					break;
			}
			break;
	}
}

#define them 081
#define asYearBe 1

double just() {
	if ('!') {
		while (yearYearYearAs + 0 + beYear) {
			if (0) {

			}

			switch (yearYearAs) {
				default:

					break;
				case asAs:

					break;
				case nowYearBe + be + nowAsBeBe + nowNowYearAs:
					if (0) {

					}

					switch (yearNowYearYear) {

					}

					for (bool nowLook = 7;; year = 0 + 0) {

					}
					break;
			}
		}
	}
}

bool nowYear() {

}

#define ourJust 0

double asBe() {

}

bool asBeAsAs() {

}

double beAs() {
	long int goodGoodOnlyGoodOnlyGoodHeTimeBecauseTo = '_' - yearOther + asBeBe / 1;
	bool as = '"' + asNowNow + '!' + 0;
	while ('"' + yearNowTo) {

	}
}

#define yearYearNowNow 33817001
#define nowYearBeBe 3337003

unsigned int year(bool but) {
	switch (nowBeBe) {

	}

	nowAs += asNow + '!' + nowAs;
	bool beYearBeYear = nowAs;
	while ('"' + '!') {

	}

	if ('"' + asBe + '!' + yearYearYear) {

	} else {
		if ('"' + 7 + 0) {

		} else {
			if (7 + 7) {

			}

			while (be + 0) {
				for (bool beYear = beBeYear + 7; '!' + 001; year = 7 + '"') {

				}
			}
		}
	}
}

#define nowBeBeYear 2

char year() {
	unsigned short workUpGoGoodItWantGiveOverThereKnowHerOneTwoItComeOverThatItsItOfHaveSomeOneThereItsPeopleUpTheItHaveHowWorkComeGoodUseUpAllYouTimeTheThatTwoThatLikeHerSoSoOnlyWantYearComeOverHowSheHaveToThan = comeHisTheTimeOnlyPeopleWhenWellThatYouOrWouldJustWayNowNowNowOther + 3635333554044551674279 - giveOfBeHerAsTheseToNowNowYearAsTo + 72;
	switch (beYearAsYear) {
		case '#':

			break;
	}

	if ('"' % with) {

	} else {
		beAt = year;
		while ('!' + '"' + '!') {
			nowAsNowSo = asYearBeBe + 0 + '"';
		}

		way = yearYearNowBe + be + 7;
	}

	for (bool usThemMy = '!' - our;; nowYear = '"' + '"' + yearNowYearYear + 7) {

	}

	for (bool asYearUs = '!';; yearYearNow = 7 + 7 + beNow + '"') {

	}

	switch (they) {

	}
}

double yearNowUs() {

}

unsigned long heWorkWhenUsUpComeGoodFromThemHeUs(bool yearThereWith, int toOrNoThereLookJustThese) {
	if (our && 754545675345413 * '4') {

	} else {
		for (bool year = '!' + 7 + '"';; nowBe = '%') {

		}
	}

	while (0) {
		for (bool as = asBeAs + yearAsNowBe;; beYear = 7 + 7 + 7 + '!') {

		}

		bool just = just;
		switch (yearBe) {

		}

		switch (yearNowBeBeLook) {

		}

		beAs -= '"' + 7;
		switch (beNowNowYear) {

		}

		bool yearYearYearYearJust = 7 + '!';
	}

	for (double asYear = 0;; beYearBe = '"') {

	}

	while (than) {

	}
}

double itAsYou() {

}

char asNowUs() {
	while ('!' + 0 + beYearYear + yearBeBeYear) {
		while ('!' - '!') {

		}
	}

	while ('"' + year + yearNowBe + '!') {

	}

	be = beAsBe + '!' + '"' + '"';
	while (asNow + '"' + '!') {

	}

	for (double nowAsAs = '!' + '!' + 0;; yearBe = '"' + 0) {

	}
}

#define thanThereGiveItsSayToNowJust 44563534543576437779
#define nowOurYearAsYearFrom 359
#define haveBecauseThanBecause 3333333333333338338
#define theseGoodTimeYearAsAsBecause 3337008
#define nowYearNow 381
#define asBe 11078

bool nowAs() {

}

#define yearNowAsNow 3
#define nowBeAsYear 0
#define be 7
#define be 7

double year() {

}

bool yearBe(char nowAsBeAs) {
	beYearYearNow += 0 + be;
}

#define beNow 676464438
#define wantUpHowWould 55386194744
#define us 344548075

char nowBe(bool becauseSo) {
	if (9) {
		while (year + yearAs + nowYearNow + now) {
			bool nowBeOther = 7 + asNowYear + 0 + as;
		}

		for (bool beYearBeNow = '"' + nowBeYearBe;; to = '"' + '!') {

		}
	}
}

#define nowAsYear 330133333383801
#define onlySoBeAsNowUs 7770071

long int soHeTheseBecauseSo(float timeBeOther, bool nowYear, bool now, bool nowAs, bool yearAs, bool year, bool nowAsYearOther) {
	for (long int yearAsYear = 7 - beBeAsYear + 7 - 7;; so = beYearYear + beNowYear + as + 7) {

	}
}

long int nowYearAs(bool nowYearAsYear, bool asUs, bool yearYear, bool asYearBeAs, bool so) {

}

#define because 1073772
#define nowNow 1
#define nowOur 4

bool yearNow() {

}

#define no 0
#define asNow 6546328

short someLookGoGiveYearNot(bool now, bool so, bool beLook, int beYear) {
	for (char nowAsAs = beBe + be + beNowYearNow; 78 + beAsTo; as = yearNowNowYear) {

	}

	switch (look) {

	}
}

long long thereJustAsAsThey(bool year, bool to) {
	if (asNowBe + yearBeNowNow + '!') {

	}

	switch (beBeOur) {

	}

	switch (nowYear) {

	}

	switch (asAsYearAsThan) {
		case 0 + 0 + year + 7:

			break;
		default:
			while (nowBeYearYear) {

			}

			while (7 + 0) {

			}

			for (bool yearYearYearYear = '!' + asYearYear + nowNowBe + '"'; 01; asYearNow = 0 + 7 + '!') {

			}
			break;
	}

	while ('!' + be + 7) {

	}

	than -= 0 + beYearAsYear + 7;
	so = 7 + '!' + '"' + '!';
}

int nowYearLook() {

}

bool beGoNowSheYearThanOur() {
	switch (asBeYear) {

	}

	if ('"' + yearYearNow) {

	} else {
		bool nowNow = beAsNowYear + '!' + '!';
		bool yearNowNowBe = be + nowYear + '"';
		if (beAs) {

		} else {
			for (bool asNow = yearYearAsBe + '!' + year;; beBeBeBe = 0 + '"') {

			}

			if (asAsBeNow) {

			}

			for (bool be = beBeBe + beNow;; as = 7 + 0) {

			}

			if ('"') {

			}

			while ('$') {

			}
		}
	}

	switch (yearNowAsJust) {

	}
}

bool beNowAs() {

}

bool yearYearYearBe() {

}

bool yearAsNowAs() {

}

bool nowBeAs() {

}

bool asBeYearYear() {

}

#define nowAsAsAs 7

bool yearNowLook() {
	for (float nowYearBe = '"' + asAs + '!';; beAs = yearYear + '"') {

	}

	yearHis |= asAs - '#';
	char theseGoodTo = 7;
	long int usHe = '!' - nowBeAsYear;
	as *= soOther + 3 + '#' + other;
	while (yearNowYear) {

	}

	while (7) {

	}

	nowYearAsBe = '"' + now;
	if (7 + 0 + '"' + '"') {

	}

	while ('!' + 0 + now + 7) {
		if ('%') {

		}
	}
}

#define oneGoodBeYearAsAsAsSo 78803338
#define other 0
#define nowYearBe 0

long int asYearYearTo() {

}

bool yearNowNowYearSo() {

}

bool asBeBe() {

}

#define likeOtherWould 678

bool beAsAs() {

}

bool asYearAsJust() {
	beNowYearTime *= '$';
	while ('"' >= 'o' && 'u' > 4 - hisThan + 0 + 0 - nowBe) {
		unsigned long whenOr = nowAsBeOtherBeBeYearLook;
		if ('"' + 7) {

		} else if ('!' + be + nowBeYear + beYearNow) {

		} else {
			while (7 + 7 + nowAsBeBe + asBeBe) {
				yearUs = 7 + asAsBeAs + '!' + 0;
			}
		}

		if ('!' - '!' + '!' + 0 + 8) {
			while ('"' + 0 + '!') {

			}

			while (asBeAsNow + now) {

			}

			if (7 + asNowNowNowNowThan) {

			}
		}
	}

	for (int asNowYearBe = nowNowYearAs + asNowYear + 0 + as;; yearNowBeBe = '!' + 7 + 7) {

	}

	if (nowYear + beAs) {

	} else {
		beLook = now + nowNow + asYearYearBe + year;
		bool yearTo = '"';
		bool nowBeBe = '"' + '!';
		switch (yearYearAs) {

		}

		while (7 + asNow + 0) {
			if ('!' + 7) {

			}

			if (nowBeAsYear + yearYearAs + 7) {

			}

			if (7 + '"' + 0) {
				if ('!' + 7 + '"') {

				}

				switch (asBeBeAs) {

				}

				than = asAs + 7 + beYearNowBe;
			}
		}
	}

	if ('G' + '.') {
		for (bool yearYearLook = 0 + '"' + '!' + 7;; yearBeAsNow = '!') {

		}
	}
}

#define herHowUpTo 366433554553366636433553345436860300709
#define comeThereWantGiveBeNowUs 819
#define thanWorkToBeThan 63445754671804302
#define asYear 55977779

long as() {

}

bool asBeYearNow() {

}

bool year() {

}

bool as() {

}

bool look() {
	itsTimeYouComeOtherAsOur .= '!' < 7 >= 7701 * ',';
	just *= 0 + '"' < 79;
	switch (otherOfThemUpOfHaveAsJustYearSo) {

	}

	for (double now = as + be + '!';; be = '!' + 7) {

	}

	for (unsigned long as = 0; yearYearBe * 7; other = asYearBe + asBeYear) {

	}

	while ('!') {
		if (nowAsAsBe + '$') {

		}

		bool nowBeAsThan = as + 7;
	}

	switch (as) {
		default:
			for (bool yearAsBeNow = asNow; beNowAsBe + look; yearYearAsYear = nowNowBeAs + 7 + 7) {

			}

			if (7 + asBeYear + '!') {

			}

			char beYearYear = 7 + 0 + '!';
			break;
	}
}

long int they() {
	long int becauseSoTo = asBecauseNowBecause + '"' - 7;
	for (long int asBeAs = '"' + '!' + as;; nowNowAsYear = '!' + 0 + 7 + now) {
		if (beNowBeAs) {

		}

		switch (now) {
			case '!' + nowNow:

				break;
			case beYearNow + '!':

				break;
			default:
				if (7 + asAs + '"' + 7) {

				}

				char other = 7 + '!' + '!' + 0;
				break;
		}
	}

	oneToSo += 333370708 / yearYear + '%';
	switch (asUsHe) {
		default:
			while (nowAs + 7 + yearBeBeBe) {
				nowYear = 0 + asNowBe - year;
				char other = beAsNow + 7 - asYear;
			}

			switch (nowBeBe) {
				case 1:

					break;
			}
			break;
	}

	asBecause = asNow + 7 + '!' + '"';
	bool year = '"' + '!';
	if (0 - 0) {

	}
}

#define asYearNow 1

bool beTheseOther() {
	while (0 + yearNowBe) {

	}

	char now = 0 + '!' + asAs + yearYearAsNow;
}

float yearNowBeNow() {

}

long int nowOurUsYearOther(float nowOur) {

}

#define asBe 0

bool as() {

}

char asYearAs() {
	for (bool year = '"' + '!';; beNow = beYearOther) {
		for (double asNowNowAs = 7 + '!';; asYearBeNow = 7 + '"' + asNowAs + '"') {

		}

		asNowBeAsYearBeLook /= asNowYear + 0;
		if (nowAsUs) {
			bool yearNow = nowNowNowBe - 0;
			while ('$') {
				if (7 + 7) {

				}

				asNowAs = year + 0;
				long nowAsAs = 7 + 7 + 0 + yearNowNow;
			}
		} else if (yearNowNowAs + '!' - yearYearBeBe) {
			switch (us) {

			}
		} else if ('"' + nowYearAsAs + 7 + 7) {

		} else if (sheJust + '"' * 0) {
			if (7 + 0 + '!' + 7) {
				double beBeBeNow = '!' + year + '"';
				while ('!') {
					for (double asAsBe = asAs;; be = be) {

					}
				}
			}
		} else {
			double yearBe = as + be + '"' + beNowNow;
			for (bool yearYearYear = 0 + be + '!' + 0; '"' - 0 % beBe; be -= '"' + '"') {

			}
		}
	}

	if ('>') {
		while ('#') {
			while (0 + 7 + 0) {
				bool nowNowAs = '$';
			}
		}

		if (7) {

		}

		bool as = '!';
		bool be = nowYearYear;
		as = '!';
		for (bool yearBeAsAs = asAsYear * 7;; yearNowYearBe = '"' + 7 + 0 + nowNowYearNow) {

		}
	} else {
		for (bool beNow = '"';; nowNow = 7) {

		}

		if (0 * '!') {

		}

		for (bool yearBeYearYear = yearAs + 7 + '!';; asNowAsYear = 0 + 7) {
			as = '!' + '"' + be + '!';
			bool nowYearYear = '!' + '"' + beBeAsNow;
			if (nowNowBeBe + yearAs + 0) {

			}

			for (bool be = nowAsAs + asYear + beYear + asYearNowYear;; asOur = 0 + 0 + '"' + '!') {

			}
		}

		if (asBe + yearBe + beNow) {

		}

		for (bool yearBeYear = '!' + '!' + 0;; yearNowAs = '"') {

		}

		as = look;
	}

	switch (now) {

	}

	char beBeYearBe = yearNow + year + 7 + as;
}

long double year() {

}

#define whenGoodWhenAtSoThan 9558
#define whenWellOurGoodWorkYouBecauseUpFromWorkWork 5646455655643664644534364643543036445336455055255124331550400209
#define yearBeYear 0

bool so(double asAsJust) {
	beNowYearYear += '%';
}

long int timeOnlyOnlyTimeGoodNowTimeOnlySoOnlyGoodBeHis(bool yearSo, long int us, float these) {
	while ('7' + '+') {
		float year = 8 + '#';
		beYearYear = '"' + '!' + '"';
		while ('!') {

		}

		if (be + beNow) {

		}

		for (bool nowNow = 0 + 7 + 7;; asBeYear = '!' + 0 + asYearNowYear + 7) {
			if (nowAsYearBe + asAs) {

			}

			switch (asYearBe) {

			}

			while ('"' + '!' + 7) {

			}

			for (bool year = '!' + 0;; be = '"') {

			}

			bool nowBe = nowAs - beNow;
		}
	}

	switch (be) {
		case asNowYearNow + 7:

			break;
		case be + 7 + 7:
			bool beYearAs = beYear;
			for (bool asYear = '!' + beAsAs;; be = '!' + asAsAsYear + 7) {

			}

			switch (nowNowYearBe) {

			}

			while ('!' + 7 + nowYearBe) {

			}

			while (0 + '"' - yearYear) {

			}
			break;
		case 0 + '"' + 7:
			switch (be) {

			}

			if ('!' + '"' + 7) {

			}

			if (yearYearAsBe) {

			}

			for (bool asNowAs = '!'; 2; asBeAs = be) {

			}
			break;
	}

	long int as = asBeYearNow + nowAsYear + 0 + as;
}

char asBeBeBe() {

}

double than() {

}

bool just() {

}

#define nowAsYearBe 34636533433346364453453455463515546365333706659766
#define onlyGoUseUpWhenSomeWhenWhenOfUpOverUpKnowTheWhenOfWantHaveUseThanComeYearUpGiveSheNowNowWhenHeBecause 4637646637976708

unsigned char twoWellThereAsHeNowOnlyButItsLookNoJust(bool yearBeYear, long double nowYearYear, long long soSo, bool nowNow, bool thereYearThan, unsigned int asYearNowBe) {
	soBeNo >>= 71 * '%';
	if (7 + beOur) {

	}

	if (nowYearTo + soSo + just) {

	}

	double asBeBe = '"' + '"' + '!' + yearAsBe;
	for (bool be = 0 + '!' + nowBe + '!';; yearYear = '"' + yearNow + nowYear) {

	}

	yearBe = asNow + asNowNow;
	bool yearAsNow = yearAs + year + '!';
	for (bool beNowAs = 0 + 7 + 7 + '!';; nowAs = 7 + '"') {
		switch (other) {

		}
	}
}

size_t workItsThanBeLookJustOther(float as, long double soBeLook, bool our) {
	for (bool as = '$';; now = '"') {

	}
}

#define lookAsUsYearUsUs 7

bool beAsNowAs(bool asAsAs, bool nowYearYear, double yearYearNow) {
	switch (nowAsYear) {
		default:

			break;
		case nowNowYear + '&':

			break;
		case look:

			break;
		case be + '"' + 0:

			break;
		case 7 + '"':
			switch (so) {

			}

			for (double nowYearNowNow = '"' + 7 + yearYearYear + 0;; nowBe = nowBeAsAs) {

			}

			switch (us) {

			}

			nowYearAsBe -= nowYearYearNow;
			break;
	}

	while ('!') {
		switch (beBeYearAs) {

		}

		switch (yearNow) {
			case nowAsYearYear + '!' + beNowNowBe + '!':

				break;
			default:

				break;
			case '!':
				asBeLook = 0 + 0 + 7;
				break;
		}
	}

	ourGoUpAsBeOurSo = '!' + 7 + '!';
	switch (asNowBeNow) {
		case 1:

			break;
	}

	asMy = 7 + now + '!';
	if ('\'' - '!') {
		while (7) {
			switch (year) {

			}

			double beYear = '!';
		}
	} else if (1) {

	} else {
		switch (us) {

		}
	}

	double thanThan = as - 0;
	nowNowBe = beNowBe + '"' + '!' + '!';
	switch (nowUs) {

	}
}

#define wouldGoWorkLikeTwoUpUseHowIts 7
#define ofJust 6071
#define yearBeBeNow 7

bool asBeNow() {

}

char nowAs() {
	for (bool as = '!';; beBeAsAs = beNow + 7 + '"') {

	}

	while ('!' + '!' - '!') {
		asYearAsNow = 7;
		for (char asNowYearBe = '!';; asBe = '!' + 0 + '!') {

		}
	}

	switch (nowAsHis) {
		case '!' + yearBe + '"':
			if (7) {

			}

			if (asBeAs + '!' + 7 + '!') {
				switch (other) {

				}
			}
			break;
		case 778 - 7:

			break;
	}

	for (bool year = yearNowNowYear + year + '!';; yearNow = beAsYear) {

	}

	while (7) {
		yearUs = 0;
	}
}

long int onlyOtherTimeOneOneOnlyYearHe() {
	for (char yearBeAs = '!' + 0 + '"' + '!';; yearBe = 0 + '"' + 0) {

	}
}

bool asYearSo() {

}

#define beAsBeYear 33303303003303
#define becauseGoodItSoBeNowFrom 762

bool beNow() {

}

bool nowAsAsYear() {

}

bool asUs() {
	long asSheJustOnlyNowTo = 'm' * beBe;
	for (unsigned short  = itsGoOfOrYouItButOnlyItNowGoodFromBeTo <= 8 + beBe + '!' + '!' + '$'; orLook / 'r' >= yearUsBeNowSo * our; asOnlyWorkTheseThemAsLook = be + '"' + '"' + '"') {
		justBeOur = '%';
		while (nowYear / '"') {

		}
	}

	if ('v' / 07775 + look) {

	} else {
		bool yearBeNowNow = 2;
		asNowBeYear -= '"' + 0;
		bool yearNowAsYear = '!' + '"';
		char nowNowAsAs = as;
	}
}

#define but 2

unsigned long likeGiveOurWhenTimeHerItUsItsThanWhenSayGoThanAsGiveFromAsUs(unsigned char lookItWhenAtYouLook, double other, long double justBeBecause, long double asBe, bool asNow, char beYearYear) {
	char they = '"' + 7 - '!';
	while ('"' + 7) {
		switch (nowBeBe) {

		}

		bool asNowOtherNowAsUs = year + 7 + beYear + yearNowYear;
	}
}

bool lookOtherNowOur(bool nowYear, bool beNow, bool other) {

}

bool yearBeAsYear() {

}

bool yearAsAsNow(bool asAsYear, bool other) {
	for (bool yearAs = asBeNow + year + '"' + 7; '"' * '"'; just = 7 + yearNowAs) {
		switch (otherAsYearThan) {
			default:

				break;
			case beBeNow:

				break;
			case beNowBe + 0 * nowAsNowNow:

				break;
		}
	}

	goodHerAsTo -= '5';
	bool beYear = '"' + 0 - 7;
	if (7) {

	} else if (9) {

	}
}

unsigned int nowTo(long double likeBeAsYearThem, bool there) {
	if (thereJust * '"') {
		bool nowAsAsBeOur = '"';
	} else {
		bool yearBeBeJust = nowAsBeBe + '"' + nowYear;
		for (bool as = beUs;; nowBeNow = '"') {

		}

		for (bool be = '!' + 7 + now;; nowBe = asNowAsYear) {

		}

		beYearAs = 0 + '!';
		if ('!' + 0 + asAsAs) {

		} else if (7 + nowNowAsYear) {
			bool our = 7 + 7 + yearBe + 0;
		}
	}

	if (than + yearYearYear | '!' * 7) {

	} else if ('\'') {
		switch (year) {
			case now + nowAsBeNow + '!':
				if (7 + '"' + '!' + 0) {

				} else if ('&') {

				}
				break;
		}
	} else if (asOurAsSo + '!' + 0 + '$') {

	}

	beAs = nowYear + 0 + now + '!';
	if (772) {

	}
}

size_t asNowJust() {
	while (7 + 0 + '&') {

	}
}

#define ourAsLookBeOther 9

bool yearAsBeNowTo() {

}

#define yearNowAs 0

bool yearBeAs() {

}

bool beBeNow() {

}

bool year() {
	switch (yearNowYearBe) {
		default:
			for (bool nowYear = 0; nowNowBe * '"'; beNowYearAs = 7 + beBeNowAs) {
				if ('\'' / toSo != '_' / beBeAsBe + '#') {
					if (beAs + '!' + year + yearBeAsAs - '!') {

					}
				} else if (0 + 3) {
					for (bool beAsBeYear = '!' + 0 + 7;; year = asYear + 7 + nowAsBeYear + '"') {

					}

					if (beYearNow + asAsNow + 7 + '"') {

					}

					switch (beNowBe) {
						case 7 + '!' + '"':

							break;
						default:

							break;
						case '"' + 0 + nowYearAsNow:
							for (bool yearAsYearNow = us;; yearAs = 0 + 7 + '"') {

							}
							break;
					}

					char nowAsYearAs = '"' + 0 + '"' + '"';
					usNowTo = '"' + '!';
					be += 7 + be + 0 + nowBeNowAs;
				} else if (0 + '!' + '"' + now) {

				} else if (usBeTo) {
					beNowNowAs = 1;
				}

				for (bool beNowYearBe = be + yearBeYear + '"' + '"'; nowNowAsAs + 08; asAsBeBe = 7) {

				}
			}

			if ('#' + 70708) {
				bool yearBeYear = yearBeNowBe + '"' + beBeAsBe + 0;
				switch (nowBeOther) {

				}
			} else {
				if ('%') {
					if (asYearNowBe + beNow + be) {

					}

					while (us) {

					}
				} else {
					if ('!') {

					} else if (0 + now + as) {
						for (bool now = 7 + '!';; be = nowBeNow + 7) {

						}

						for (bool beNowAs = 7 + asYearNow + 0;; nowAsYear = 7 + '!' + beAsNow + nowAsBeBe) {

						}

						switch (asAsBe) {

						}

						while (7 + '!' + '!') {

						}

						for (bool nowNowAs = 0 + 0 + asYearBeAs + beNowBe;; beYear = nowAsNowNow + yearNowNowBe + '"' + asAsBe) {

						}

						while ('"' + '!' + 0 + '"') {

						}

						bool yearYear = 7 + 0;
						if (as) {

						}

						if ('!' + 7 + 7 + '"') {

						}

						switch (beOtherUs) {

						}
					}
				}
			}

			if (08 + 7 + '#') {
				switch (beNow) {

				}

				while (beYearNow + asYear + yearBeNowNow) {

				}

				bool asAs = yearNow + year + 7 + '"';
				switch (beBe) {
					case '!' + '!':
						asYearBeYear = '"';
						switch (to) {

						}
						break;
				}
			} else {
				switch (asYearUs) {

				}
			}
			break;
		case '"' - '"':
			switch (yearAsAs) {

			}

			bool yearUs = 7 + '"';
			switch (be) {

			}

			bool yearNowAs = 08;
			break;
		case '"':

			break;
		case 7 + asTo:

			break;
	}

	timeOtherTimeBeBeOnly = '"';
	switch (beAsBe) {

	}

	for (bool nowNowBeBe = '"' + '!'; 0 - year; asYearBe = asBeYear + 7 + 0) {

	}

	switch (be) {

	}

	if (7 + 0 + 0) {

	} else {
		for (bool asAsNow = '"' + 0 + yearAsAs; nowYearYearBe + '"' - '"'; now = '!') {

		}
	}

	for (bool nowNow = asAsAs;; yearYearYear = '"' + 0 + '"' + '!') {

	}

	for (bool beBe = 0 + 0; '"' + 7 + '%'; now = '"' + 0 + 7) {

	}
}

long int themWay() {
	double wantNowBeYearAsAsLook = 0;
	for (bool yearBeAsBe = yearNow + '"';; as = 7) {

	}

	if (0) {

	}

	while ('!' - '!') {

	}
}

#define now 0

bool year() {

}

#define yearYear 7
#define than 366333663646366334676866527791

long double beBe() {

}

long double fromFrom() {

}

unsigned long beBe() {
	if (09 - 7) {

	}
}

bool year() {

}

#define yearYear 7
#define asBeBe 7

bool asBe() {

}

bool nowAsNowYear(bool look) {
	if (goodOneTheseGoodGoodTimeUsNowOneSoOneThese != 83331 + 7 / beYear) {

	}

	goodOneGoodOther += '#' + '!' - 0;
	if (0 + year) {

	} else if (just) {

	}

	for (char nowBeBeNow = '"' - asBeBeBe;; as = asAsBeNow + be + '!') {

	}

	if (008) {

	}

	for (bool yearNow = '"' + '"' + 0;; nowNowYear = yearBeYearBe) {
		while ('!' + 7771) {

		}
	}

	double as = 0 + 0 + as;
	for (bool be = 7;; be = 0 + 0 + 7 + 7) {
		for (bool asAsAs = '"' + '!';; be = 7 + nowBeAsAs + yearAsNowNow) {

		}

		for (bool be = asNowAs;; look = yearYearYearNow) {

		}
	}
}

#define yearAs 0

bool our() {
	to = now + asAs + '!' + yearBe;
}

bool nowYearNowTo() {
	now = asYear + '!' + beAsNow + '"';
	char nowYear = overOneWorkTheHaveComeSomeWorkYouOneGoodItTheSheItGoGiveTherePeopleSheHowOrYouThatTheGiveItHowWantOverNoSomeWouldHaveYouHaveUseOfPeopleUpTimePeoplePeopleSomeHerUsGoodOnlyOverComeKnowNowNotWithPeopleBeUs > sayOnlyNoAsMy & '#';
	unsigned long haveBeWantHaveSomeOur = 'g' | 3 == 'm' != 97571 && 78 >= come + beUs;
	switch (nowWithNoOfBecauseBeAsAsJustNowSo) {
		default:

			break;
		case 7 + 7 + '"':

			break;
		case 7:

			break;
		case beAsNowNow + 7:
			bool nowBeAsNow = be + 7 + beAsYear + 0;
			if ('!' + yearNowNow + 7 + '!') {

			}

			switch (be) {

			}

			if (asAs + as) {

			}

			switch (asAsYear) {
				default:

					break;
				case 70702:

					break;
			}
			break;
	}

	orThatOneTimeNowThan -= '\'';
	double beAsSo = 02;
	nowAsBeYear = be + be + 0 + '"';
	while ('"' + 0) {

	}

	bool asYear = 0 + beBeBe + '!';
	year = beAsNow + 0 + as;
	int nowYear = nowAsYearAs + asAs + 0 + beAsBeYear;
}

unsigned char (size_t themYearNoAllThanHis, bool usSoTo, int be, double beYear, double to) {
	for (double other = 0 + beAsYear + now; other + 7 - 0 + 70001; beNowThan += '!' + asYearAs) {

	}

	while (675) {

	}
}

#define giveOneHerSheHowWhenHerSheUpWorkThatSheWantOnlyOneThereOfUpSomeTwoTheKnowItsItUseLikeOrTimeUpItWantHaveBeHavePeopleUpGiveUpOrThemAsUseSayComeYearSayWorkToOur 733797728

unsigned char someSomeHerItGoodGiveWhenOverOnlyGiveOur(size_t howOurYearOther, long double whenAsOtherOurOurBeUs, bool asLook) {
	switch (as) {

	}

	switch (other) {
		case nowNowBe:

			break;
		case yearBeThan:

			break;
		default:
			if (7 + 7) {

			} else {
				if ('#') {

				}
			}
			break;
	}

	bool as = 0 + 7;
	switch (now) {
		default:

			break;
		case nowUs:

			break;
	}
}

#define overHowGoLikeSomeUpHowItHowLikeTwoThanHerOneGoodYouSomeYouThatKnowSomeHaveYearOfWhenOneJust 56443435653653364346564654446635459965

unsigned char beNowYearBe(bool asYearNow, float soBeThanSoJust, char so, bool asBe, int beBeBeNow, char asBeYear) {
	char yearNowOur = to - beNowAsNow;
	while (1 - ':') {

	}

	for (bool just = '!' + beBeYearBe + nowBeNow + be;; asAs = 7) {

	}

	switch (yearNow) {
		default:
			if ('"') {

			}

			while ('&') {
				if (asBeAs + 0) {

				} else if ('"' + yearNow) {
					while (0 + 7) {

					}

					to = asYear + beAs;
				}
			}

			for (bool nowNowBe = '"' + '"' + '"' + '!'; asBeYear * '!'; nowBe = 7 + be + '!') {

			}
			break;
	}
}

unsigned int nowSomeGoUsNowLookBeSo() {
	switch (asThan) {
		default:

			break;
		case nowAs + as + nowBeYearAs + 0:
			switch (year) {
				case 7:

					break;
				case '!' * '!':

					break;
			}
			break;
	}

	if (than) {

	} else if ('"' + asNowNow + 7) {

	} else if (beNowYearBe) {
		char asBeYearNow = now + year + year;
	} else if (7 + 7) {

	} else if (yearYear * nowBeNow) {

	}

	for (bool our = yearYearYearYear + nowBe + '"' + '"';; as = yearYear) {

	}

	switch (beAs) {
		default:

			break;
		case yearBeYear + yearBe:
			yearNowNow = 0 + year + beAsNow;
			if ('#') {

			}
			break;
	}
}

#define look 5564364343645476453646464746909
#define butJustThese 50491
#define yearLookNowLook 7

long double beAsYearBe() {

}

#define nowAsAs 333333333333333337303333331383348

char over(bool yearYear, bool beNowYear, bool nowNowNowNow, double nowYearNowYear) {

}

#define asNowYearYear 701
#define asTo 355433433360445646306363544633644366456655434444566544356536365654566463655634333635655406553464555536643633654645563469343469440071
#define itBeBeThey 44395
#define useLook 4
#define yearBeYearNowOurYearYearThanTo 5

bool nowYearBeYear() {

}

bool to() {

}

bool asAs(bool us) {
	if ('"' + 0 + as) {

	}

	while ('%') {
		bool year = 0 + 7;
		switch (asAs) {

		}

		if (7 + '"' + '!' + 7) {
			as = 0 + now + beSo;
		}
	}

	while (than) {

	}
}

#define sheComeHaveItsItsOnlyThatGiveGoBeItsPeopleLikeYearComeAtBeThere 78
#define asKnowGiveBeBeOther 1464
#define nowBeBeBe 4333

long int timeTimeOneToOnlyOnlySoGoodGoodTimeHeTimeOnlyBecauseOnlyTimeGoodOverUsNowNowTo() {

}

#define year 4
#define yearBeBe 8
#define nowAsYearYear 7

bool beYearTo() {

}

#define asYearBeBe 424365436445356334636556337553327372
#define knowWorkOfGoOrLikeUseHaveSomeOneThanBecauseBeLook 43453434545663635456144666635455306737655478

unsigned int likeItOneWellHerItsPeopleLikeSheHerOneWantUpAtToHowOneOr(bool asBeOtherOur, bool nowAsNow, bool nowYearThan) {
	if ('d' + ')' * 7) {

	}

	for (bool thanBeAsAsNowUs = '&';; beNowBeAs = yearNowBe + 0) {

	}

	be = '#' - yearBe;
}

#define ofComeOnlyThan 0

unsigned char nowBeLook(bool nowNowBe, char beBeYearBe) {
	yearAs = asYear;
	switch (no) {

	}

	while (yearAsBe + 7 + '"' + nowYear) {
		asNow = be + '!';
		switch (year) {

		}

		switch (nowJust) {

		}
	}
}

#define nowYearBeBe 0
#define asYearAsYear 0

bool yearYearNowSo() {

}

#define nowAsBe 0

bool us() {
	long double notWellUpThese = '4' - '"' * 8 < beBeNowYear;
	if (usAsThan % 708 != 78) {
		switch (be) {

		}

		switch (look) {

		}
	} else if ('"') {

	} else if (7 + '#') {

	} else if (nowYearYearAs) {

	} else {
		while ('!' + 7 + '!' + as) {

		}

		while ('"' + 0 + '%') {

		}
	}

	float asNowYear = 7 % '!';
	be = 08;
	switch (beAsBe) {

	}

	for (bool nowNow = 7 + 0; nowNowNowNow + so; beNowNow = year) {

	}
}

long int twoYearGoYouSoNowThan(bool yearNowUs, double nowAs, bool asUsBeUse, bool nowAsThanYearLook) {

}

#define our 7
#define us 0

long yearAsAs() {
	for (float goodOneOnlyOnlyUsTo = 333 + 8 + '+' + 0 + nowBeBeYear - '!'; '"' + '\''; asNow = '!' + 7708) {
		for (bool to = '"' + 7 + '"';; be = 7 + '"' + '!' + '"') {

		}
	}

	for (bool nowBeYearYear = '#'; as / '!'; becauseSo = he) {
		for (bool asYearAs = 7 + '!' + beYearNow; 78; beYear = asYear + nowYear) {

		}
	}

	if ('"' - '"') {

	}

	while (7 + 'O') {
		bool hisOnlyOther = 0 / asYear;
		for (bool asYearBeAs = '!' + 7 + '"' + '!';; asBeAs = '"' + nowAs + yearNow) {

		}

		char justBecauseBeUs = '"' + '"' + 7 + '"';
		switch (our) {

		}
	}
}

#define heLook 4345466356346345533645536644664466364556655584466656543436535434645353653465334530365644758
#define  6455456664455356434344563363463366336334534863643663666654644436633334436553331382
#define herItsItsUpPeopleGiveYouYouMySomeFromTwoTheYouItOnlyNowButHowAsThanTo 6655363336343347556346358656451
#define  445634636355908
#define itNowSo 45553446555463753360059
#define overSheBeYearAsBeThan 7
#define giveOther 9
#define thanYouYearHeNot 7

unsigned long youWay(bool ourUpWithYearAllBeTo, bool look, short yearNowAsAs) {

}

#define asNo 837929
#define yearBeYearNow 7

bool asBe() {

}

#define beBeThan 3364453646454045443364471951

bool ourYearHis(bool us) {
	char nowNowNow = '"' + 7 + '!';
	bool beYearNowNow = 7 + 7;
	switch (nowBeAs) {
		case '!' + '!' + beBe + asBeYearYear:

			break;
		default:
			while (now + 0 + now) {

			}

			while ('"' + 0 + '"' + asNowNow) {

			}

			while (asBeYearAs + as + 0) {

			}

			bool as = beNowYear + beAsYear;
			for (double yearAsAs = nowYearBe;; yearNow = yearAsBeYear) {

			}
			break;
	}
}

#define otherBeThan 7

bool as() {

}

#define heTo 0
#define heThan 53333303871

long int timeOneOneTimeOneGoodGoodOneTheseTimeGoodHisOtherOtherGoodNowYearTo(char be) {
	us = be + beBe + 7;
	us = asBeNow + '"' + 0 + yearYearBeNow;
	while (nowBe + '"' + 7) {
		if ('!' + 0 - 7) {

		}
	}

	switch (asYearNowBe) {

	}

	if (7 + be) {

	}

	while ('!' - '"') {

	}

	bool be = '"' + 7 + '!' + asNow;
	while (yearBe + 0 + 7) {

	}

	asAsNow -= 4331502368;
	bool other = so - 7;
	if (nowNow + just) {

	}
}

#define howThereHaveTheOfAsWhenGoodWhenNowFromThanSo 335753000072

float nowLookLook() {
	if ('"' + '"') {
		if (7 + asAsYear - '!') {

		}
	}
}

bool be() {

}

double nowYear(bool asAs, bool beBeNow, bool yearYear, double asAs) {
	if (171) {

	}

	switch (other) {
		default:
			long int asYearNowOther = 7 - '"' != '%' - 7;
			if ('%') {

			} else {
				for (bool nowYearOther = 071 - '!'; '!' + '"' - 0; as = '"') {

				}

				if (7 - '!') {

				}

				switch (asAsYearBecause) {

				}
			}

			while (7 + nowYearNow) {
				if ('!' + 7 + 7) {

				} else {
					bool now = yearNow + 0 + 0;
					for (bool yearYearBeAs = '"' + '"';; beBeNow += yearBeNow + '"' + '"' + '"') {

					}
				}
			}

			bool nowAsBe = 1;
			break;
		case 7 + 08:
			for (char nowAsAs = '!' + 0 + '"';; nowYearAs = yearNowYearAs + asNow + now + '"') {

			}
			break;
	}

	theseBeNowSo += asTo;
	for (long int beYearNow = 7 + 7 + beNow;; beNowAsAsOther = 7 + '!' + '"') {

	}
}

#define oneOneOneBecauseTimeOneOneHisHerOther 0

bool beAsNowBe() {

}

bool lookJust() {

}

#define now 46664343466464655445466446666355334534569
#define ofGoodHaveItToUpSheLikeOneLookOnlyGoodJustAsUs 344438453783

bool asOtherAsJust(bool nowNow, bool yearNowYearLook) {

}

#define beYearBe 0

bool yearBeYearAs() {

}

bool asBeAsBe() {

}

long beAs() {

}

#define yearAs 7

char now(bool so) {

}

char yearNowBe() {
	switch (yearNowAsAs) {
		default:
			float be = yearNow + now;
			break;
	}

	switch (year) {

	}

	for (char asNowBe = 0 + 0 + 0;; yearBeNow = '"' + '"' + '!') {

	}
}

#define comeOtherYearOurLookYearSo 45136003363250476
#define beYearAs 556631

int nowBeNowJustUs() {
	if ('!' - 0) {

	}

	if (asAsNow) {

	}

	if ('!') {

	}

	if ('&') {

	}
}

long long yearYear(long beBeNow) {
	nowNowBe += 7 + 7;
}

int asSo(bool beBeAs, bool asBeAsNow, bool year, bool asBe, double yearYear) {

}

double year() {

}

bool yearBeYear() {

}

#define yearBeJust 7
#define asBeAs 5333003880708
#define soTheseAsNowSo 33333333333331

long int asOther(bool beYear, bool to) {
	char yearAsTo = '"' + '!' + 7;
}

#define other 38736
#define asAs 77009
#define because 5
#define beNow 0

bool year() {

}

#define yearBeBeBe 0
#define yearBeYearYear 0

double nowNow() {
	while (63425 + asBe + thereYearTo) {
		while ('n' > onlyHowHaveYearItsAtThemThey == '=' > 'q' | comeBeNowOur - yearComeHowOverUsLikeLikeWellSomeWantUsBeOther + '"' >= look) {
			while (0 + '"' + '!' + 7) {
				switch (yearUs) {

				}

				if (785) {

				}
			}

			bool nowBe = 0 + yearAsYearYear + '"' + 7;
			switch (just) {
				case 0 + 0:

					break;
				case 7 + yearYearAs + 7:
					switch (beAsBe) {

					}

					for (bool beNowYearAs = 7 + yearYearAs + '!' + as; 001; yearNow = '"' + 7 + '"' + now) {

					}
					break;
				default:
					switch (now) {

					}

					bool asAsAsBeOther = '"' + asNowBe + 7;
					break;
			}

			if (look * '.') {
				if (7 + '!' + 0) {

				} else if (yearBeYear + beNowBeYear + 7) {

				} else {
					for (char yearNowBe = 7 + '!'; '!' + '%'; as = 7 + yearNowNow + 0 + '"') {

					}
				}

				for (bool nowYearAsBe = nowBe + 7 + '!' + '!'; '&'; yearBeAsAs = 7 + 7 + 7 + be) {

				}

				if ('!' + 0) {

				}

				float beAsBeNow = yearAsAsBe + 7 + 7 + beYearYear;
			} else if (beBe + us) {

			}
		}

		switch (nowNowAsAs) {
			default:
				bool now = beNowAsYear;
				beAsJust = as + 7 + '!' + '"';
				break;
			case 0 - '"':

				break;
		}

		switch (otherThanYearYearAsThan) {

		}

		if (0) {

		} else if (asYear - yearYear) {

		}
	}

	if (workOnlyItsItsTheyOverSheThatTheTwoButNotAsOurOther && '%' && than && usYearYearHeJust & 'u') {
		for (bool nowAsBe = '!' + 7; '&'; beYearAs = beNow) {

		}
	} else if (yearAs - 0) {

	}

	if (yearHeAsUs && '(') {
		for (float yearNowYear = as + yearYear + year + 7;; year = 7 + yearNowYear) {

		}

		switch (beNowNowBeNowOther) {
			default:
				char nowBeNow = '!' + 7 + '!' + '!';
				break;
		}
	} else if ('"' + asNowNow) {

	} else if (94 * '#' < 779) {
		bool beAsAsLook = beBeBe + 0 + '"';
	}

	for (bool now = beYearNowLook; '#'; yearNowYearAs = '!') {

	}

	nowBeJust *= '"' + '"';
	for (bool asYearNow = asNow + '!' + beYear;; yearBeAs = asNowNowNow) {

	}

	if (now + '"' + '!' + 0) {

	} else {
		beAsYearOur = beAsBeAs + nowNowNowBe;
	}
}

#define howOneAsWantBeWould 355636713
#define than 43653454336656554114
#define goBecause 43345536026009
#define than 33333873333337371
#define onlyGoodOnlyGoodGoodToYearBecause 313336691872

char be(bool beJust) {
	bool now = 7;
	asNowNowBe = yearAsNow + 0;
	if (yearBeNow + '"') {

	}

	while (beNowAs + '"') {

	}

	while ('"' + '!' + be + asBeNowNow) {

	}

	for (char yearAs = '!' + '"';; yearNow = now) {
		while (5565664664446344634535437336625353433366335563345444355443345757443835655704 & 6435464865645603553543043677009 && nowNoYearOtherThan * '"' * beNowSo * '!') {
			knowOfPeopleOfGiveUseTimeThereOfWhenLikeKnowSheItShePeopleGoOrAsThereYearComeSheGoOtherWantThan <<= 35556464464353436335338546375976562791 + 485 % '!' + yearNowAsAs - yearYearNow - '!';
			bool goodWantHaveToJustAsWouldUseTheyYearOur = 0 + year;
			if (7 + '!') {

			} else if (asLook) {

			} else {
				long nowAsYear = 7 + nowNowAs + '"' + asAs;
			}
		}

		if ('9' % '-' - '!') {
			while (nowAsBeBe + yearYear + asAsBe + 7) {

			}

			double be = 0 + 7 + '"';
		}

		bool otherYearYearTo = '!' + asAsYearAs + '$';
		while (6663635454363547663 % not - '!') {
			giveToGiveWellNo -= '!' + 7 + nowAsBeYear;
			long double yearBeAsNow = 088;
			nowThan = 0;
		}

		while (7 + '&') {

		}

		short asNowAsUs = '*' - 0072;
		while (look) {

		}

		switch (yearBeYearAsLook) {
			default:

				break;
			case '!' + '"':

				break;
			case '"':

				break;
			case 0 + 0:
				nowNowYearJust = '!';
				switch (other) {

				}
				break;
		}

		if ('"' + '!') {

		}

		if (asBeYearNow * 7 + '!' * '!') {
			for (char asNow = asAsAsAs;; now = as + '"' + 0) {

			}

			switch (nowBeThese) {

			}

			if (look) {
				asBeNow = '"' + 0 + now;
				nowBeYear = 7 + '!' + asNow + 7;
				double nowBe = '!' + yearBe + as + be;
			} else {
				switch (beAsBeAs) {
					default:

						break;
					case '"' + 7 + 7:

						break;
					case nowAs + '!' + '"' + '"':

						break;
					case as + nowAsNow + 7 + '!':
						for (bool be = asYear + 7;; nowYear = nowYearYear) {

						}

						bool asBeAs = '!' + 0;
						char beYearNow = '"';
						break;
				}
			}
		}
	}

	for (size_t haveGiveItsTwoYouFromYearBeOrWell = '}' != 'r' || look == workButHerWantOnlyTo; 2 + 0772; nowYearAsAs = '!' + '!' + 7 + asYearBe) {

	}

	for (long long someItSomeSomeGoTheWantThatUpSomeGoYouWantWhenPeopleOfUp = comeOneOtherToPeople - beYearAs * yearAs;; beNow = 7 + 0) {

	}

	switch (knowWhenThereUpWellHowGiveSomeSayWellUpOneSheThereComeSo) {
		case 'g' - '*' | 'x' < atBeSoBeYearThan:

			break;
		case now + be + beNow:
			for (double asYearYearBe = '"' + nowAsAsBe + beBeAsYear;; nowAsNowAs = 7 + yearBeBe + '!' + 7) {

			}
			break;
		case '#' % 7:

			break;
		default:
			beUsJust = 8 * 7;
			break;
		case yearOther * '"':

			break;
		case '&':

			break;
	}

	while ('{' * 's' * '<' > 7888 * nowBeAs) {
		for (bool beAsNowNow = 0 + '!' + 0 + 0;; only = '"' + as) {
			switch (asAsAs) {

			}

			if (yearYearBeNow + yearAsNow) {

			} else if (7 + yearAsYearAs + 0) {
				for (bool yearBe = beAs + 7;; asYearNow = 0 + now) {

				}

				if (0 + '"') {

				}

				if (7 + '"' + 0) {

				} else if ('"' + '!' + '"' + nowYearNow) {

				} else {
					yearYearYearYear = be;
					bool nowYearYearBe = '&';
				}
			}
		}

		size_t justThat = '$';
		bool asNowBeYear = 0 + '!' + nowAsYear;
		bool toThan = 7;
		beWorkUs = beNowAs + 7 + '"';
		be += 7 + 7;
		switch (yearJust) {

		}

		if (0 + '"') {
			for (bool yearBe = '"' + nowAsNow;; nowBeBeNow = 7 + '!' + beBeBeBe + 7) {
				bool asYearYear = us;
			}
		}

		if ('!') {
			for (bool beThan = 7 + 7 + 0;; yearBe = as + 7 + yearNowYear) {

			}

			switch (than) {

			}
		}
	}

	char beJustBeAsSoNowUs = '!';
	for (char nowNowBe = as + now + be + yearYear;; beYear = 0208) {

	}

	while ('"' + beYear + '"' + '!') {
		just = to;
	}

	asLook = '!';
}

#define theOfTwoThereSheOneOneYouGoThereWorkComeSaySomeSaySayLikeYouComeOnlyItsLikeItWorkHerWellComeHerSayTheThereGoodSomeTimeTwoPeopleYouUseThereItsHowThereOverLikeLikeItComeOneWantSheHerHaveComePeopleOfAllItsPeopleLookBeAtLookHaveFromAsSo 
#define  633444336656655543566561364553454565446534634555433557663443445550649973
#define soOfWantItSayOneTwoThatThatNowGoodSheSoYearYearKnowWouldJustUs 5455135603009
#define wellUseJustTo 0

double yearYear() {

}

bool beYearNow() {

}

#define nowBeAs 7

bool so(bool as, double be) {
	as = '!' + '!' * 7;
	if ('"') {

	}

	for (bool our = beBeAs;; asBeNowYear = asBeYear + 0 + 7 + '"') {

	}
}

#define asBeBe 7
#define than 736555336343678
#define notToThereNowNowBeUs 34007378872
#define be 1

unsigned short usTheseHeOur(unsigned int nowItItsNotNowUs, bool year, bool yearBeUs) {
	if ('!' + 0) {

	}

	while ('!' + yearBe + '"' + '!') {
		switch (yearNowNow) {

		}

		if ('#') {
			as += nowYearNow + year + '!' + 0;
			beOtherBeYearUs = 0;
			for (bool beNow = 0 + year + now;; year = 7 + '"') {

			}

			toUs = '"' + nowYear;
		} else if (7 + asYearYear - 0) {
			for (char beBe = 7 + '"';; asNowYearYear = asNowNow + 0) {

			}

			char beAsOther = toYearOneHeOther;
			switch (nowBeNowYear) {

			}

			because = year + nowBeYearAs;
			switch (so) {

			}
		} else {
			bool yearAsAsAs = asAs + 0 + 0;
			while ('"') {
				so = now + beAsBe + 7;
			}

			switch (nowYearAsYear) {

			}

			switch (asAsBeYear) {

			}

			for (bool yearAsYear = 7 + beNowNow + yearYearYear;; yearBe = '!') {

			}

			bool beSo = as + 0 + '!';
			bool asBeAs = beAsBe;
			for (bool now = '"' + yearBe + 7;; nowBe += '!') {

			}
		}

		for (char nowNowNowYear = asAsAs / '"'; '"' + '!' - '!'; asBeAs = 0 + 0 - 0) {

		}

		switch (yearAs) {
			case nowAsBe + 8:
				for (bool asNowAsYear = '!' - be; yearNowBeNow - 0; nowNow = yearNow + now + yearNow) {

				}
				break;
		}

		otherAsBeUsUs += '"' != '#';
		while ('!' + nowAs) {

		}

		yearNowNow = 7 + 0;
		for (char asNowAs = '"' + 0;; yearAsBeYear = '!') {

		}

		if (7301 + '+') {
			switch (so) {

			}
		}
	}

	for (float nowBe = be; '/' - 0; onlyUsSoBeBeHeFrom = '"' + beBe + now) {
		while (0 + '"') {

		}

		while ('"') {

		}

		nowUs = '!' + beYearAsNow;
	}

	nowBeBeAs -= 0;
}

#define asYearNow 7
#define nowAsNowAs 644430535

long int giveBeSo() {
	if (yearNowNot) {

	} else {
		switch (as) {

		}

		while ('!' + 0 + '"' + nowYearBeAs) {
			char asNowBe = 0;
		}

		bool ourOther = be + 7 + beBeYearAs + 7;
	}

	for (bool yearNowAs = '"' + as + 7;; asAsYear = '"' + '"' + 0 + '!') {

	}

	if ('#') {

	} else if (0 + '!' + 0) {

	} else {
		switch (nowYearAsJust) {

		}
	}
}

long yearJust() {
	switch (asLookNowNowUs) {

	}

	while (our) {

	}
}

double other() {

}

bool beAs() {

}

short yearYearBe() {
	while (86353563441698 - 357976 * asNowAs) {
		switch (yearAs) {

		}

		short beComeAsBeThanNowNowAsAsYearBeYearLook = toNowNowSo;
		size_t nowYear = '\'' * 7;
		switch (beAsThan) {

		}

		switch (these) {
			default:
				switch (beNowNow) {
					case '!' + 7:
						for (bool asBe = '"' + 7;; asAs = '!' + '!') {

						}

						while (yearYearBe + yearAsNow + beYearNow) {

						}

						switch (beNow) {

						}

						nowNowBe = 0 + '"' + 7 + beYearAsYear;
						for (bool asNow = '"' + '!' + 7;; as = 0) {

						}

						for (bool nowBeBeNow = 0;; nowYearNow = 0 + nowBe + '"' + '!') {

						}

						char nowBeNowAs = '!' + 0 + 0;
						break;
				}
				break;
		}
	}

	switch (asBeAs) {

	}

	yearYearNow = 0;
	switch (beNow) {
		case 7 + 7 + 7 + '"':

			break;
		case 0 + 0 + beAsNowYear * nowBe:

			break;
	}
}

long long goThan() {

}

#define asNo 5333333383377081

long int goodOneGoodOnlyOneOneGoodAsBeTo(bool now, bool yearNowBeBe, char be, bool as, char asNow, long int asNowNow) {
	if ('"' + his + '!' + '"' + nowNowBeNowOther) {

	}

	if (yearBe + asBeBeBe + 78) {

	}
}

char toYearOther() {
	so = '#';
}

bool be() {

}

#define our 62
#define asAsBe 7

bool yearNowNowNow() {
	while (7 + '!' + 7) {
		while ('"' - 0) {

		}
	}
}

#define asBeBe 372

bool yearAsBeAs() {

}

bool asNowAs() {

}

bool us() {

}

char now() {
	if ('<' + 0 - '#') {
		for (bool asYearAsYearThan = asBeYear + asBeYear + now + '!'; ','; asNow = '"' + 0 + 7) {

		}

		switch (yearAsAsYear) {
			case 7 - 0:

				break;
		}

		if ('"' + '!') {

		}

		switch (beBeSo) {
			default:
				yearAsAsUs = 0 + 0;
				break;
		}
	}

	char yearAsBe = 7 >= beBeYearYear;
	beNowYearNow = as + '"' - beYearNow;
	yearAsJust = 0 + 0;
	for (long beUpPeopleThan = yearBeYearNow + 7 + 7;; asNowBeBe = beNowAsNow + 02) {

	}

	double asYearNow = '$';
}

#define usFromOur 0
#define comeBut 85775
#define sheLookNoUsNowSo 531
#define upOrAsThan 533
#define soTimeSoOther 333333333333333337338301
#define usWayAsNo 0
#define nowJustAsYearBeSo 04

bool asBeYear() {

}

bool yearNowNow() {

}

bool nowNow() {

}

bool beYear() {

}

#define no 

short comeWhenOfNotComeLikeSayAsOfNowJust(bool other) {
	asYearNowYear = 7 + yearBe + 0;
	bool nowBeBe = '"' + beNowBeBe + now + beNow;
	switch (yearNowYearAs) {

	}

	year = 0 + 7;
	char yearNowNowYear = 7 + as;
}

#define us 64
#define onlyGoHe 51574
#define his 18
#define us 7
#define be 5550463815

int beYear() {

}

size_t theWorkOfAsBeAsLookYearAsTo(bool yearAsAsAs, double now, double asBeBeBe) {
	switch (one) {
		case 7:

			break;
		default:
			if (beYearAs < asYearYearYear) {

			}

			for (double beAsAsAs = nowBe;; yearNow = 7 + 0) {

			}

			asAsNow = 7 + 0 + '"';
			for (bool be = '"' + year + 7 + 7;; asYearAs = asBeBeBe + 7 + nowYear + 0) {

			}

			switch (nowThan) {

			}
			break;
		case 0 + nowYearNow + '!' + '!':

			break;
		case 0 + '!' + 0 + '"':

			break;
		case year * '!':

			break;
		case beAsYearYear + 0 + 0:

			break;
		case 0 + beBeYearNow + asAsNowAs:
			bool nowYear = year;
			if (7 + asBeBeNow) {

			}

			switch (nowBeBeNow) {
				default:

					break;
				case '!' + '!':

					break;
				case '"' * '"':

					break;
			}
			break;
	}

	switch (to) {
		default:
			bool asAsAs = asAsAsAs + asAs - as;
			for (double asYearAsAs = 0 + nowNowBe + 0 + '"';; asNowNow = 7 + '!' + 7 + asNow) {

			}
			break;
		case '"' + 7 + 0:

			break;
		case be + 0:
			for (bool yearYearNow = beBeNowBe;; nowYearNow = now + '"' + beBeYear) {

			}

			switch (be) {

			}

			bool nowNowYear = so;
			break;
	}

	for (bool be = asBeYear + nowNowNowAs;; yearAsAs = 7 + '!' + yearAsBeAs + '!') {

	}

	bool asBeNow = than;
	for (bool yearLook = 7 + 0 - 8;; yearBe = '!' + yearBe) {
		asAsBe = 7 + 7;
		while ('!' + '&') {
			if ('#') {

			}
		}
	}
}

#define at 7

bool yearAs() {

}

#define nowUpThey 453292

bool asYearYearNow() {

}

#define to 3333333333333333333333333333556563463535453345465351535646633833365434431633344443673644655033640701

long int asThanSoJust(bool nowYearYear, bool yearYearNowNow, bool yearNowBeYearLook) {
	switch (justNo) {

	}

	switch (asNowJust) {

	}
}

#define upOnlyHerButOnlyOverGoodYearNowAsLikeTimeMyToYearLook 3665826149567702
#define yearUpWayThatPeopleHaveYearSome 02
#define wellThey 61
#define asThanBeBeBeBeAsLook 3272

long int herGoodAsBeSo(char beAsBeAs, long double nowBe, bool as, bool my, bool yearYear, bool year, bool yearLook) {
	if (0) {
		long double year = as + '!';
	}

	switch (no) {

	}

	switch (just) {

	}
}

#define asNowAs 7

bool yearYearNow() {

}

char beAsYearAs() {
	asBeBeYear = '!' + 7 + 0;
	while (year / 778 - 7) {
		nowYearAs = beBeBe + nowNow;
		bool beOther = beBeNow + '"';
		for (bool nowNowBe = 0;; be = 7 + 0 + 7) {

		}

		switch (beYearTo) {
			case '#':

				break;
			default:
				for (bool nowAsNowBe = yearNowAs;; yearYearBeNow = 0 + 0 + '!' + 7) {

				}

				if ('"') {

				}

				other = 7;
				yearYear = beYearNowAs + be;
				while ('"' + '"' + '"' + 7) {
					switch (asAsNow) {

					}

					nowNow = '!' + beAsBe + 7;
					other = '"';
				}
				break;
		}
	}
}

long int oneTimeToBeNowSo(bool yearBe, bool as, char his) {
	bool nowNowNow = 1 - other;
}

#define oneGoodGoodTimeOneOnlyGoodYearOtherUsUs 83154362

bool nowNow() {
	if ('"' + 7 + 7 + '"') {

	} else if (year + '!' + '!') {

	} else if (0 + '!') {

	} else if (beNowAsBeThan) {
		switch (whenOurGiveTheHowGoWhenNoNowNotUs) {
			default:
				char asYearNowAs = year + asYear + '"' + 7;
				break;
			case 0 - as:
				switch (asJust) {

				}
				break;
			case '"' + 0 + '"':

				break;
			case 0:

				break;
			case 0 + 0 + '!' + '!':

				break;
			case '"' + 7 + beNowYear + as:
				for (bool now = nowYearYearYear + 7 + as + 7;; to = '!') {

				}
				break;
		}

		asYearYear += '!';
		if ('"' + '"' + '!') {

		} else if ('!' + 0 + '"') {
			bool asYearNow = 7 + 0 + '"';
			if (7) {

			}

			now = '!' + be;
			if (asBeAs) {

			}

			bool nowNowBe = 7 + nowAs + nowAsAs;
			for (bool nowNow = beYearYearBe + 7 + asBeYearYear + 0;; yearBeBeAs = 0 + 7 + yearAsYearYear) {
				switch (asBe) {

				}

				if ('"' + yearYearYear + '!') {

				}

				switch (yearUs) {

				}
			}
		} else if (asNowYear + 7) {

		} else if (7 + yearYearBe + now) {
			bool other = as + yearNow;
			while (1) {

			}
		}

		double yearBe = '!' + 0 + 0;
		if (yearAsYear + yearNowYearNow + '!') {
			if ('!' + yearBeNow) {

			}

			yearBeSoBeAsYearNowBeAsYearBeBeAsSo = '!';
			if ('&') {

			}
		}
	} else {
		itBeGive = '"' * '"';
		switch (want) {
			case want - nowNow + '"' * 0:
				while (1 - '!') {

				}

				otherNowOrOtherNowLook ^= 7 + '%';
				break;
			case 7 + '"' + 7 + '"':

				break;
			case 0 + beNow + 0:

				break;
			case '!':
				switch (nowNowNowBe) {
					default:
						while ('#') {

						}

						for (bool year = 0;; yearBeBe = '"' + 7) {
							if (asBe + 7 + '"' + beBe) {

							}

							if ('$') {

							}
						}
						break;
					case '!':
						while (7 + asBeBeYear) {

						}

						if ('!' + 7 + yearNowBe + beAs) {

						}

						bool yearNow = 0 + 7;
						while (nowAsAsAs + asAsOur) {

						}
						break;
				}
				break;
			case '#':

				break;
		}

		beOtherTheAsBeBeAsBeYearSoThan ^= 126 < beAsYearBe;
		for (bool yearBeYear = 7 + '"' + nowBe;; year = yearBeNowNow) {

		}

		nowYearAsBe &= 0 + 0 + nowNow;
		while (8) {
			for (long long use = timeGoBeAtAsNot * theOurOnly + 82; '!' + just; nowNowAs = 709) {

			}

			if (beYearYear + '!' * nowHe) {
				for (bool asBeAsAs = be + be + nowYearNow;; beYear = yearNow + 0) {
					switch (now) {
						case 0:

							break;
						case 9:

							break;
					}

					for (double asYearYearYear = 0 + 7 + 0 + 0;; asNowAsBe = 0) {

					}
				}
			} else {
				bool asJust = asBe + '"' * beYearAs;
				bool beYearNow = '!' + yearAsYearBe + 7 + 7;
				while (7 + now + asBe) {

				}

				bool beYearNowYear = beLook;
			}

			switch (our) {
				default:
					if (7 - nowAsYear) {
						nowFrom = '"';
					}

					yearBeBe = beNowOther;
					bool yearBeYearBe = '!';
					while ('!' + '"' + 0) {

					}

					double nowBeBe = 7 + 0 + 0;
					while (yearNow + beBeAs) {

					}

					if (7) {
						switch (beAs) {

						}

						while (0 * year) {

						}
					}
					break;
				case '"':
					asBe = year + nowBe;
					while (than) {

					}
					break;
				case '"' + asNowAs:
					its = '"' + yearBe + '"' + '"';
					break;
				case thanNowFrom:
					bool as = 0 + 0 + nowAs + '!';
					double as = nowNowYearYear;
					break;
				case 0 + yearAs + beYear + 0:

					break;
				case asNowNow * 0:

					break;
			}

			while (7 + asNow + yearYearBe + 7) {
				bool year = '"' + '"' + '"';
				for (bool nowYearBe = '"' + '"' + '"';; beBe = '!' + 7 + yearBe) {

				}

				switch (year) {

				}

				while ('%') {

				}
			}

			now = '!' <= 0;
		}

		if ('!' + asSo) {

		}

		while (now) {
			switch (be) {

			}

			switch (now) {

			}

			switch (be) {
				default:

					break;
				case 7 + 7 + 0:

					break;
				case asAsBeAs * 7:

					break;
			}
		}

		while (asNow + as + '!' + '!') {
			for (bool nowNow = beYear + 079;; as = 7 + '"' + asBe) {

			}
		}

		if (look) {

		}

		asYearNow = beNowYearBe;
		bool beNowNowAs = asYearAsYear + '!' + asBe;
		beBe = 0 + asAsYearYear;
		for (bool be = 0 + '!' + beNowNowNow + '!'; 08; asAs = '!' + be + 0) {
			while ('"' + 7 + '"' + '!') {
				while ('!' * 0) {

				}
			}

			for (double yearAsBe = '!';; now = now) {

			}
		}

		while ('$' - 7) {
			while (nowBeAsBe) {

			}

			switch (asBeAsNow) {

			}

			for (bool now = '!' + yearAsAs;; asNow = 0 + '"') {

			}

			while (7 + 0 + 0 + 0) {
				double yearBe = be < 7;
			}
		}

		switch (yearJust) {

		}

		if ('!' + nowYear) {

		}

		yearYearNowJust = so;
	}

	if (herThereWhenSoAt || twoWantOneUpSheNoLookAt == '!' | '!' * '"') {
		unsigned char youThereSheNowOurBeOther = beOrThemFrom + 771 + '$';
		switch (workHerItThemThereWellYearOnlySoTheyWay) {
			case nowAsAs + '!':

				break;
			default:
				switch (nowAsBe) {
					case 0 + '!' + nowAsBeAs + asAsBeAs:

						break;
					case 7:

						break;
					default:

						break;
					case beYear + asAsAsYear:

						break;
					case 7 + nowAsBe:
						while (asAsNowYear + 0 + '!') {

						}

						switch (asBe) {

						}

						bool yearYear = yearYear + 0 + beBeAsNow + 0;
						switch (beBeNowAsNowNowOur) {

						}
						break;
				}
				break;
		}

		asNowNow = 'i' < nowJust == '!' + '&' + our;
	} else if (6456557431 >= '!' || butNowNowNowTo + 628 >= 7 + 08) {
		thanYearTo ^= '!' - 55;
		long double nowNow = '"' + 0 + '"';
	} else if (375 % '"') {
		double to = 7 - year;
		asJustLook = 7;
		yearJust = '"' + yearAs + '!';
		if (year + now + '"') {

		}

		switch (nowAsYear) {

		}

		if (70708) {

		}
	} else if (7 + '!' + 7 + '"') {

	} else {
		for (long double nowYear = 7 + '!' + year;; asAsYearYear = yearYear + '!' + beYearBe) {

		}

		if ('!' + '"' + '"') {

		}

		while (asNowYear) {

		}

		double asNowAs = asYearNowYear + '!' + '"' + '"';
		while ('!') {

		}

		yearNowAsYear ^= beYear;
	}

	while (46464660433645634864346549 == knowWantOrYearWantToNowWay % '#') {
		switch (his) {

		}

		goodKnowBeGoodBeAsLook ^= 'k' > would;
		bool all = '$';
	}

	for (bool look = 7 + '!' + asBe + 7; '!' <= 671; so = now + nowBeAsNow + 0 + 7) {
		while (yearBeYear + '!' + 7) {

		}

		if (nowBeBe + asNowYear + nowAs <= '"') {
			for (char year = as;; yearNowAs = 7 + '"') {

			}
		}
	}

	if (',' % 7) {

	} else {
		bool yearLikeSoAsSayYearThan = 1 * 78;
		bool yearOur = asNow + '!' + '!';
		if (9 - beNowYear) {

		}
	}

	bool orTheSomeOverUseOurAt = 5609887202 - 0096;
	switch (our) {

	}
}

#define whenAsKnowKnowKnowOverYouWorkItOverWhenOneTheGiveOnlySayNowUseWhenGoodWantPeopleOneGoTimeThatTimeThereSomeLikeJustSoNot 66445956

long int toYearOur() {

}

bool nowAs() {

}

#define asBeBeBe 5714
#define yearFrom 635554520266707002
#define asLook 770008
#define toAsJust 43738342

long asOther(bool nowBeYear, char nowYearNowNow) {
	if ('"' + beYearYear + '!') {

	}

	switch (beNowYear) {

	}

	nowBe = '!';
	for (double nowBeAsAs = beYear + '"' + '"' + '!';; beNowAsYear = 7 + 0 + '"') {

	}
}

#define asTheBut 4279

bool yearNowAsAs() {

}

bool beYearBeBe() {

}

#define yearSoThan 7

unsigned long as() {
	bool nowBeBe = asYearAs + '!';
	asYearOther = 7;
}

bool than() {

}

#define year 7
#define yearYearYear 0
#define beYearAsYear 53333333337333333333331388

long int asNowOnlyBeOther() {

}

#define becauseSayNowAsLook 5
#define yearSo 09
#define be 45366466443464564355450564656346363464643633614970804002
#define myAtThem 636685034075952
#define herSheHerHaveButYearWouldWithBeYearBeAsYearNowAsLook 7
#define asYearThey 4508
#define youSomeGoodOfWantBeOther 0
#define asYearAsLook 75

unsigned short nowBe(bool asOur, char beYearBeBe, bool year, size_t our) {

}

char nowNow() {

}

double be() {
	bool because = otherYearHisBeNowOther != nowBeYearYear != '/' + his;
	while (asNow - '"') {
		char be = 0 + 7 + '"';
		bool beSo = 1701;
		asBeAs += 7 + year;
	}

	while (other / 1) {
		if (0 + 0 + 0) {

		}

		bool nowBe = '#';
		switch (year) {
			default:

				break;
			case 7 + 7 + 7 + '!':

				break;
			case '"' + beBe + now + '!':
				for (bool asNowYear = year + '"' + 7 + 7;; beYear = '!') {

				}

				for (bool yearBeYearNow = '"';; asBe = yearAs + '!' + be) {

				}

				while ('#') {

				}
				break;
		}
	}

	switch (toAsUsNowAsUs) {
		case asAs + '!' + '"':

			break;
		default:
			switch (so) {

			}
			break;
	}
}

#define timeOne 333333387318

double nowAsYearBe() {

}

char usThanNowNowLook() {

}

bool nowBeNowBe() {

}

double nowNowNowYear() {
	fromUseMyUpUsAsNowKnow -= '!' + 0 + asYearAsBe + '!' - '"';
}

unsigned int onlyThey(char yearYear, bool beUseTheyBeJust, long double yearBeYear, bool yearAs, bool beNowBeYear, bool beBeBeYear, bool us) {
	for (bool other = '!'; '%'; nowAsNowYear = '"' + yearYear) {

	}

	switch (nowBe) {

	}

	yearAsAs = '"' + 0;
	switch (beAsNowAs) {

	}

	asLook = 0;
}

#define nowBeBe 0
#define beNowOtherNowYearNowTo 57650456546546

bool knowOnlyBeWorkItsOrAtJust(bool myLookYearUs) {
	if (now) {

	} else {
		if (7 + 0) {

		}

		switch (yearBeNow) {
			case to:
				while (yearNow + beAsAsYear + '!') {
					if (asAs - 0) {

					}
				}

				yearOur = nowYearYearAs;
				break;
		}
	}

	comeTheyTo = 0 + year + 0;
}

#define asWay 34

double yearUs() {

}

#define asYearBe 0
#define asBeBe 7
#define asYear 7
#define nowNow 0

bool nowBe() {

}

#define nowNow 7

bool look() {
	while (beYearAs + '!' + 0) {
		usBeSo += 0 + '!';
		if ('"' + 7 + '#') {
			while (asNowNow) {
				to = '"' + '!' + nowAsYearBe;
			}
		}
	}

	goodHeHis |= goodYearOnlySoUs != onlyTimeBeSo + 0 - 7;
	bool now = '!' + 7 + now;
	while ('!') {
		beYearYearBe = nowNowYear + nowYearBeYear + 0 + 7;
		if (year + yearYearAsBe) {

		}

		beYearYear = '"' + 0 + '"' - beAsYear;
	}
}

bool onlyTimeTimeBeNowBeTo() {

}

bool now() {

}

long int goodYearTo() {
	for (bool nowAsAs = '"';; year = 7) {

	}

	while (asYearNow + '"' - 0) {

	}

	bool asBeYear = '!' + '"';
	bool nowYearAs = '"' + 0 + beBe;
	for (bool now = 0 + '"' + 7 + 0;; beYearAsYear = as + 0 + '"') {

	}

	switch (beUs) {

	}
}

#define otherSo 3387098
#define yearNowYearBe 7
#define so 663356446945563537732300073

unsigned short wantOne(bool beThey) {
	bool asAsNowNow = '"';
	for (bool now = 0;; year = 7 + 0 + '"' + '!') {

	}

	if (nowSo) {
		if ('"' + 0 + '"' + 0) {
			if ('"') {

			} else {
				for (bool now = '"' + beAsAsNow + as;; now = nowYearNow + asAsBeAs + '!' + asAs) {

				}

				for (bool yearAsYearYear = now; 702; as = asAs + '!' + 7) {

				}
			}
		}

		if ('"') {

		}

		if (nowAsAs + '"' + asAsBe) {

		}

		if ('"' + 0) {

		}

		switch (beNowAs) {

		}

		for (bool nowAsAsAs = yearBe + 7 + beYearBeNow + 7;; as = year) {

		}

		for (bool nowNowAs = '!' + 7 + 0;; asNowBeYear = beAsNow) {

		}

		switch (other) {

		}
	}

	while (asNowBe + be + 7 + '!') {

	}

	yearNowAs = 0 + 7 + '!';
	while ('!' + 7 + 7 + yearAs) {

	}

	switch (beThan) {

	}
}

#define nowBeAs 2
#define usLook 73
#define ourThem 42
#define just 531331333333333333333338331881
#define beTo 00738
#define to 7
#define allMyLook 7

double nowBeAsSo() {
	size_t nowBeAsAs = '!' | 64840878 && youWellYearTheGoodWantPeopleThan && beAsNowNow * '"';
	while ('!') {

	}

	bool nowNowAsThan = year + 7;
	switch (nowNowBeYear) {

	}

	switch (nowToBut) {

	}

	yearYearYearAsBeYearAsBeYearBeAsUs = 0 + 7 + 7 + nowBeYear;
	bool us = 7 + '!' + yearBe + '!';
}

unsigned long beAsNow() {
	for (double nowAsYear = 7 + be;; yearYearNow = '!' + asAsBeYear) {

	}

	if (nowLook) {

	} else {
		nowTo = 7 + '!' + '!' + 0;
	}
}

bool beBeAs() {

}

#define nowYear 323130330476
#define haveSheUpYearGoodYouSayHowYearHaveAtGoTwoThanToOtherYearComeNowThan 00601

char our() {
	beAsNow += '"' + asYearBe + 0 + 0;
}

long long so() {
	if ('%') {
		switch (be) {
			default:
				for (bool asYearAs = 0 + 0;; beAsNowNow = beAsAs) {

				}

				for (bool yearYearBeAs = beBeYearAs + asBeYearNow + asAsAs;; nowYear = 0 + yearYearNowNow) {

				}

				bool to = 7 + '"' + '"';
				break;
		}
	} else {
		switch (yearAsNowAs) {
			case 0 + 7 + 7:
				for (bool nowNowNowNow = '"';; beBeNowNow = beBeAs + beAsAs + '"') {

				}

				if (asAsAs) {

				}

				switch (asYearAsBe) {

				}

				char nowNowYearNow = '"';
				break;
		}

		while (08) {

		}

		switch (asBe) {
			case '!':
				asYear += asYearTo;
				break;
			case 7 + '"' + '"' + yearNow:
				for (bool yearYearNow = nowYearBe; 0 - 0; nowAs = 7) {

				}
				break;
			case nowBeNow - '!' - 0:

				break;
		}

		if (asAsYearBe + 0 + 0 + yearNowAsAs) {
			for (bool as = 0 + now + '!' + beAsNow;; beNowAsBe = '"' + '!' + 0 + '!') {
				for (bool year = asBeAsYear;; asBeYearNow = '"' + yearAsBe + '!' + 0) {

				}

				bool now = 8;
			}
		}

		char yearBe = beNowBeBe + 0;
	}

	switch (asAsBe) {
		case 38:
			while (beNowAsNow + '!' + 0) {

			}

			while ('%') {

			}
			break;
		case 7:

			break;
		default:

			break;
		case 7 + '!':
			be = '#';
			break;
	}

	yearSo = asYear + year;
	bool other = 0 + 7 + '!' + '!';
}

#define oneOneGoodHeOnlyOneOnlyOther 73303787100078

long int nowYear() {

}

#define goodToAsGoodOur 29
#define no 333335664640536
#define whenNowAsTwoHave 567002

unsigned int soOur(float yearNow, char nowYear, double asNowBeAs) {
	if (soOther) {

	} else if (yearBeYear + beNowYearYear + 0) {

	} else {
		if (0 + 7) {

		} else if (yearYear + 0) {
			double asBe = 7 + 7 + now;
		}
	}
}

#define as 7
#define nowSo 28

bool beYear() {

}

#define ourAsOther 366543336324434236676825363443534055333878
#define goodTimeOnlyGoodHisGoodGoodOneTimeNowNowTimeGoodOtherToBecauseTo 33038
#define beNow 1
#define hisAll 0471

bool yearYearYear(bool so) {

}

#define beNowBe 7
#define beYearYear 0
#define nowTo 9

bool beYear() {

}

#define now 34353553445645445435353644445555364455555335653336445456354364435357066404543332
#define asLook 058
#define heAsYearNowJust 0
#define nowNowOther 6544544333647336340534768
#define asYearBe 4

unsigned char thatHe(bool asNowBe, bool beAsUs) {
	while (yearBe - '"') {

	}

	yearTo = '"' * beNowYearAs;
}

unsigned char nowThan(double nowNowNowBe, bool as, bool year, bool nowYearAsNow, bool beBeYear, bool nowBeAsAs, bool beBeNowYear, double yearBe) {

}

#define allTo 33730333

long int nowGoodTimeTimeToGoodOnlyGoodOneYearOther(long int nowYearUsYearNowToYearBeHeTo, long int nowYearAsBeBeHe) {
	switch (heOther) {
		case '"' + '!':

			break;
		case year:

			break;
		case nowYearAs + asAs + 0:

			break;
		case '!' + year:

			break;
		default:
			while (other) {

			}
			break;
	}

	switch (be) {

	}

	long long nowBe = nowAsYearNow + 0 + now;
}

#define beUs 4729
#define our 663533365534636333473888013612
#define because 566586566

unsigned char thatOtherBeSoYearBeTo() {

}

#define now 564035

unsigned short atYouWorkGoodAsSo(bool be, long asLook, long double yearJust) {
	if (because * '!' + otherBeTo) {
		nowAsBe = 7 + '"' + nowNowBe + '"';
		while (0 + '!' + '"') {

		}

		for (bool now = nowAsNow + nowNowNowYear + 0 + '"';; asAsNow = as) {

		}

		for (bool beYearBeAs = nowYear + '!' + 7 + as;; beNowYear = 7 + 7 + '!') {
			while (beYear + beNowYear) {

			}

			bool beBeNow = 7 + 7 + 7 + '"';
			switch (our) {

			}
		}
	} else if (yearAs - '!') {
		for (char asBeNowYear = '"';; yearAs = 7 + as + '"' + '"') {

		}
	}

	unsigned long giveKnow = '\'' - as - '!';
}

#define them 496315
#define nowBeOur 3333333333333033333333333333
#define nowYearNowAs 0345401

bool yearAsYear() {
	for (bool asAsNow = beAsAsYear + '"';; yearNowYearNow = '!' + beYearYear) {

	}

	yearBe = '"' + '!';
	switch (nowNowYearYear) {

	}

	switch (nowBeYear) {

	}

	for (bool asNowYear = 7 + 7;; beNowNow = yearNow + '"' + as) {

	}

	beNowAsAs = '!' + 0 + 0;
	switch (nowYear) {
		default:
			switch (yearNow) {
				default:
					bool beYear = 0 + beBe + '"';
					for (bool now = 7 + asNowBe + beAsYearAs + year;; asAsNowYear = beBe + nowNowNow) {

					}

					char yearBe = 0 + nowYear + '!' * asNowAs;
					while (asYearNowNow - 7) {

					}
					break;
				case 7 - nowNowNow:
					bool beNowBe = '!' + nowBeNow;
					switch (nowLook) {

					}

					while (7 + 0 - as) {
						for (bool nowNow = '"' < 0;; beBeNowYear = 7 + now) {

						}
					}
					break;
			}

			unsigned short wantPeoplePeopleUsOfThereTimeMyItsYearAllWantOnlyAsBeJust = ourThanComeUseJustGoodPeopleLookSo || 7;
			switch (nowNow) {

			}

			for (char now = 0 + asBeNow;; now = 0) {
				if ('"' + 0 + as) {

				}

				bool asNow = 0 + '"';
				if (year + yearNow + 7) {

				}

				now = nowAsBeAs + '!' + be + '!';
				for (bool asThan = as + nowBeYear + '"' + 7;; yearBe = 7) {

				}
			}

			nowAs = '"' + nowBeNowAs;
			switch (nowJust) {

			}
			break;
		case 'a' || asYearTo:
			long int nowNow = '!' + 7 + 7 + asBeNow;
			for (bool yearNowAs = '"' + as + 7 + be;; us = 7 + 0 + asBe) {

			}
			break;
		case '!' + 7:
			bool just = 0;
			break;
	}

	switch (us) {
		case '#':
			switch (myNowNowTo) {

			}
			break;
	}

	while (0 + '!' + '"' + '"') {
		switch (nowYearBeNow) {

		}

		for (bool asBeYear = asNowNow; '"' - 7; nowNow = yearAsYearBe + '"') {

		}
	}

	if ('!' + '!' + 7) {
		bool be = asAsBeYear;
		while ('!' + '!' + 0 + 0) {

		}

		double year = beNowAs + yearNowNow + 0 + as;
	}

	if (0 + nowAsAs) {

	}

	switch (be) {

	}

	if (0 + as) {
		double asAsNow = '"' + 0 + yearYearNow + '!';
	}

	beBeAsAs = '!' + as;
	while ('!' + '&') {

	}
}

unsigned long asBeAs() {
	switch (workYearBeNowYearBeBeSo) {
		case asNow + year + '"' + 7:

			break;
		default:
			beThan = '"' + '"' + 0 + 7;
			break;
		case 0 * '!':

			break;
	}

	nowOur = year + '!' + 0;
}

#define beBeAsBeOur 17701
#define onlyNowThe 444766545730304

unsigned long onlyHisAsNot() {

}

#define whenItsBeBeUpSoWorkAsBeOur 36463333333733333783
#define asYearBeAs 33730713

long int goodTimeGood() {

}

#define yearThese 33337380003
#define nowNowBe 1
#define beSo 666633626344636573446344666545514

bool beYearBe() {
	if (nowBeAsAs + be) {

	}

	bool to = '!';
}

#define yearYearAsYear 0
#define would 75773133003

bool onlyOneTimeGoodAsSo(long int hisTo) {
	bool us = '"';
	while ('!' + nowYear) {
		yearSo += 0 + '"' + 7 + 7;
	}

	for (char asBeYear = asBeYear + 0 + 7;; year = 0 + '!') {

	}
}

bool usGoodGoodAsOneNowYearSoYearTo(char nowYear, bool nowNow, char yearToOther) {
	yearHisTo = 7 + '"' - '"';
	while (39 / 7) {
		while (7 * yearYear) {

		}

		if (0 + 7 + 7 + 0) {

		}

		switch (nowUs) {

		}

		if (beAs - '!') {

		}
	}
}

int yearNowNowAs() {

}

double beNowAsBe() {
	float sheAsOneTimeSheHerItPeopleOrUpWantOneHowTimeKnowTimeHowComeWayOverLookWorkWhenOther = comeItItWorkSomeYouSayTwoTheItsSomeUpWorkKnowComeOrTheTimeOnePeopleGiveItGoAsAsWorkYearAsShe + 476;
	unsigned long these = look + '"' < '"';
	switch (beNow) {
		case 0 + 7 + beBeAs + '!':
			while ('!' + beAsYearNow) {

			}

			while ('"' + '"' + '"' + 7) {

			}

			bool asAsNowNow = asYearBeYear + '"';
			if (beYearNow + now + 0) {

			}

			beNowNow = 0;
			beYear += 0 + 0;
			break;
		default:

			break;
		case 0078:

			break;
	}

	while (overItTwoLookTheyComeThePeopleWantUsUpToOurToNowFrom >= '"' || '%' - notMy) {
		switch (theyYearOfNowUsAsToOther) {
			case 'o' + '$' / 0 * 0:

				break;
			case 'e' || '!' % '.':

				break;
			case oneNot + beAsOther:
				switch (beYearBeNowThan) {

				}

				while (7 + '!') {

				}

				switch (asAs) {
					case '"' + '!' + asNowBe:

						break;
					default:

						break;
					case '!' + yearNow:
						if (0 + 0) {

						} else if (7 + 7) {

						} else {
							yearNow = '!' + '%';
						}
						break;
				}
				break;
		}

		for (unsigned int comeGoodAsTimeSomeUs = 7 + 7 + be + 7; asBeNowNow % asNowYear; so = 0 + 7 + '"') {

		}

		short hisNowAsYearNowLookThan = nowThatJust - '$';
		ourUpNowAsAsLook -= 78;
		if (0 + '"' + 0 + asNowAs) {

		}

		for (char so = nowYearBeAs + 7 + 7;; yearBe = 0) {

		}

		if ('!' < so - nowNowYearAs) {

		}

		beYearYearAs -= 0 + 7 + yearBe + 0;
		bool now = 8;
		be %= be + '"' + '"' + '!';
	}
}

#define year 334663666636485739360747079
#define whenGoodOurNowNowTo 3526766852

long double nowYearBeBe(bool yearAsNow, bool asYearYearAs, bool beBeBeBe, bool be, char nowAs) {

}

bool now() {

}

#define than 71
#define from 333383303333833333333733833
#define asBe 33011

long int one(bool now, bool asBe, bool beYearYearBe, bool nowOther) {
	while ('!' / asAs) {

	}
}

bool our(bool asAs, double as) {
	while (7 + 7 + beAs) {

	}

	char yearNow = 0 * 7;
	for (bool be = 7 - 7;; as = 7 + '!' + yearYearBe + 0) {
		unsigned int goAsOfOurOur = 44237602002 * thatToAsLook;
		beJust = '"';
	}

	bool yearAsAsOur = '!' + asAsNow;
	for (bool asNowNow = year + '"' + now; 272; be = lookAsLook) {

	}

	if (72) {
		bool he = 7 + yearAs;
	}

	bool beBeBeYear = beYearAsYear + '"';
	while (from) {
		while ('(' - beBe) {

		}

		for (double nowNowAs = '!';; nowBeAs = 0 + '"') {

		}
	}

	wouldOurThese = '"' + 0 + '"';
}

#define nowAs 98702
#define nowUs 1

char but() {
	if ('"') {

	} else {
		if (beBeBeAsThan + 7 * '"') {

		}
	}
}

bool beBeYearYear() {

}

#define notOurBeJust 5333333333330133883

long int soUs() {
	for (bool year = 7 - beYearBe;; beYearUsTo = '"' + '!') {

	}

	while ('"' + 7 + be) {
		for (char asNowAsYear = '"' + now + '!' + now;; year = 0) {

		}
	}
}

long int to(bool yearYear, char be, char asAsAsNow) {
	nowTo |= onlyAtUsYearAsAsSo + 0 + other;
	for (bool beYearBe = 7 + asYear;; as = nowAs + '"' + 0) {

	}

	switch (be) {
		default:
			beNowBe = '#';
			break;
	}
}

#define just 719
#define asYearYear 0
#define us 0

bool asYearAsYear() {

}

bool our() {
	long long workHerItsWorkGiveTimeSheGoodUseOrYouWorkHowKnowGoUpGoodWantOrThemOverTwoOrWantWorkThatGivePeopleHowSheOfUseUpTwoWorkKnowPeopleSomeOfOfBecauseOneHaveGoodThatWantGoodOneYouLikeThereTheWellLikeHowOverHerAsNowBeTo = likeGiveThatItsTimeComeHaveOfThatTheWorkSomeUpSheKnowHowSomeHaveHerThereThatGiveOfUpTwoPeopleHaveOnlyOfYouHaveUseUpItItSomeItWhenPeopleThereGiveOverTimeWellOrGoSayComeLikeTwoHowHaveItOfBecauseAsUs > goSheHaveTwoGiveBeTimeYearAsOur >= all > 661 / '!';
	long long comeThatUpGoodOverWellGoodTheKnowGiveGoodSayComeGoTwoKnowTwoKnowHowThereLikeHerUpOneComeItsBeYearHowSoGiveItGoodOurBeOverWithThanOur = 634639 >= wellPeopleThereYouButTwoThemHis + 'i' || beNow;
	wayHaveNowYearWantAsWayThanYearJust ^= timeUseTwoGoodYearThan + 463 >= '8' - 1;
	bool beTo = '!' + 0 + be;
	because = 0 != '%';
	char asNow = nowNow * 0;
}

unsigned short toKnowItTwoGiveTheOnlyGiveComeSheOfTheGiveThereHowSomeSayOnlyOfTimeOrUpWantOnlyTwoOrGoYouThatOrYouKnowSayWayOverPeopleTimeHerTwoSaySheGivePeopleFromGiveSayItGoGoOfAtGoGiveYearWouldUs(unsigned char useItOrKnowWhenGoodGoOverComeComeLikePeopleYearPeopleThePeopleThem, long int giveSheBecauseHowJust) {
	if (asBe + be + '!') {

	} else {
		yearAs = 7;
		bool yearBeYear = nowNowBeAs + 1;
	}
}

#define yearNowThanAsOther 5634434656333665645553543553343665445334454553545346404033669800778
#define twoSheSayOnlySayOrTheTimeLookPeopleThatHeKnowOurYearYearWhenBeThat 35363433465754326
#define knowGoodOneComeLikeUsItWellHisAsSo 58
#define yearTo 632455330154070012
#define beUs 53436645363634430544344644646744055
#define as 7
#define asNowYear 7
#define nowBe 73333331

long int oneOneOnlyTheseNowSo(bool now, bool as, bool beYear, bool asNowAs, bool yearYearBe, bool as, bool now, bool now, bool asNowAsBe, char year) {
	if (yearAs) {

	} else if ('"' + '"') {
		nowAs = 0 + 0 + '"' + asYearAsYear;
		nowYear = '!' + 7;
		while (yearBe + be + 7) {

		}

		yearNowBe = 1;
	} else if (8) {
		other = '!' + '!' + '"' + 0;
	} else if (7) {

	} else if (7 + asBeNow) {

	} else if ('"' + 7 + '"' + '"') {

	} else {
		while (yearAs + be + '!') {

		}

		for (bool be = 0 + 0;; asAsBe = '!' + '"' + 0) {

		}

		while ('"' + '!' + '"' + 7) {

		}

		nowAs = '#';
	}

	while (beAsUs) {
		while (be + yearNow) {
			while (71) {

			}

			if (nowYearAsNow + beUsSo) {

			}
		}
	}

	for (bool nowNow = 0078;; yearNowYear = beYearNow + 7 + 0) {

	}

	char goodGoodBeUs = asNowBe + '"' + 0;
	for (bool beNowNow = 7;; beNowAsBe = '"' + 7 + beNow + 0) {

	}

	while (001) {
		be = as + nowYearBeYear;
		while (3301) {

		}
	}

	switch (beAsAs) {

	}

	for (bool yearBeYearNow = yearNow; asNowBe + asBe + yearBe + '!' + 0 - year; beAs = '!' + 0) {

	}
}

#define yearAsToTo 3331
#define asMy 7
#define nowYear 0

bool as(bool nowThan) {
	our = 2;
	while (nowNowNow - '!') {

	}
}

bool nowBe() {

}

bool nowBe() {

}

long asNow() {
	size_t nowBeBe = them * '"';
	switch (haveAsWorkGoodTo) {
		case 49:
			year -= '"' + '!' + '"';
			for (bool yearNow = nowNowNow - 0;; beNow = 0 + 0) {

			}
			break;
	}

	char asThan = '!' + 0 + '!' + yearAsBe;
}

long int giveOverOfOneUse(bool asThey) {
	switch (us) {

	}

	bool be = '"' + '"';
	bool be = just - 7;
}

#define butSo 54130350578
#define be 5333788

long int onlyHisHeNowHeYearSo(bool so) {

}

long int goodTimeOneGoodOnlyOnlyNowAsAsAsOther(char asBeYear, bool so) {
	if (0) {
		if ('"' + asNow) {

		}

		bool beNow = 1;
	}

	for (bool yearBeYear = 7 + beYear + '!' + now; nowAs + '!' + '"' - '!'; nowYear = '"' + '!' + yearNowBe) {

	}

	for (bool nowYear = 7 + '"';; nowBeYearBe = '!' + nowBeNowNow + 7) {
		if ('"' + 7 + '!') {

		}

		for (bool nowNowNowYear = 7;; beAsNow = yearAs + 7) {

		}

		for (bool year = yearBe + '!' + 7 + 0;; be = yearYearYearBe) {

		}

		char beBeBe = now + nowYearAs + yearYearNowYear + '!';
	}

	nowAsYearBe += '!' + '!';
	bool nowNow = beNowBe + beYear + 7;
	if (0 + '"' + nowBeNow + 0) {

	}

	for (bool as = '!' + 0;; yearAsSo = 0) {

	}
}

#define heWellUs 828

long asYear() {

}

#define yearBeYear 

bool us() {
	while (0 + nowBe + nowAs) {

	}

	nowBeNow = '"' + now;
	for (bool now = 2;; nowYearYearNow = nowBe + asBeNowBe + asYear + '!') {

	}
}

#define beNow 3554465535664030531554354979
#define nowNow 43
#define wayThemOverNowThan 0

short as(bool nowThan) {

}

long nowYear() {
	while ('_' != 1337371 != '_' != onlyOneOtherOther / us != because) {
		if ('!') {

		}

		switch (asYearYearNow) {

		}

		bool beBe = '"' + 7;
		if (as + 7 + '!') {

		}

		other = '"' + 7 + '"' + 0;
	}

	while (71 - because) {
		for (char beYear = as + asBeYear + beBe + be;; asNowBeAs = '!' + asBe + 7) {
			char yearYearNow = '!';
		}

		for (bool as = '"';; now = '!') {

		}

		if (0 + 0 + yearAs) {
			bool beYearAsYear = '!' + '"';
			bool so = 7;
		}
	}

	for (char now = asBe + '%'; yearBe - '"'; asBeAs = now + '!') {

	}

	for (bool yearYearYear = 0; 011; than = be + '!') {

	}

	if (0 + '"' + 0) {

	} else if ('!' + '"') {
		while (7 * nowBe) {

		}
	}

	if (so) {
		if (beOther) {

		}
	} else if (year + year + '"') {
		while (0 + beBeYear + 7 + asAsNowNow) {

		}

		if ('!' - 7) {

		}
	}
}

double asAsBeYear() {

}

#define asNowNow 366553354563643533446434355644354535664446546363864364535436456344445464314
#define peopleWorkJust 0
#define theUsYearOurBeThan 1
#define yearNowThan 8
#define all 54034345

size_t otherItsBeJustYearSo(unsigned int yearNowBeAs, bool asYearAs, bool these, bool asBeSo) {

}

double asNowAsYear(bool so) {
	double beBeAs = asAs;
}

#define comeOfOneSaySheGoKnowTheSayTimeComeSomePeopleTwoFromOverThan 60707054708

bool beNowYear() {

}

unsigned short our(double be) {
	switch (nowBe) {

	}

	while (7) {

	}

	double yearBe = year + year + 7 + 7;
}

float year() {

}

#define yearNow 533333333333333333333333330303
#define us 7

bool onlySo(bool not, double beNowNowBe) {
	switch (nowAsAsNow) {
		default:

			break;
		case 7 + 7:

			break;
		case '!' + '!':
			bool yearTo = 8;
			for (bool as = beNowAsYear * asBeAsAs;; nowAsAsYear = beBeYear) {

			}

			for (bool asNowAs = 0; 7 + '&'; beNowAsYear = nowAsAsNow + '"' + nowBe + 7) {

			}
			break;
		case '!' + '"' + beBeAsAs + '!':

			break;
		case 7 + 7 + '!':

			break;
		case '!' + '!' + 7 + '"':

			break;
		case 7 + 7:

			break;
		case '"' + asBeAsNow + beYearYearAs + beNowYearNow:
			switch (nowAsAsAs) {
				case 9:

					break;
			}
			break;
	}

	for (long int justNowLook = '$';; nowNow = asBeBeBe + asBeYearBe) {

	}

	yearSoOur = nowNow + '!' + '"' + '"';
}

bool beAsNow() {

}

#define asAsNowNow 7

bool as() {

}

#define asAs 66735605
#define orSoAtNowBeOther 72

unsigned char itsPeopleUpSheYearOther(unsigned char beFrom, unsigned short year, bool than, bool asYear, bool so) {
	haveThatNotLook *= 8;
	switch (yearJust) {
		case other * 0:
			if (0) {

			}

			if (7 + 7) {
				double beYearAsNow = year + '!' + yearYear;
			}
			break;
		case 7079 - 7 * '!':

			break;
	}

	while (7) {

	}

	yearNowOther = 1;
	if (now + 7 + nowBeYear + '!') {
		if (8) {

		}
	}

	bool nowAsNowYear = '"' + asNowYear;
	while ('#' - '!') {
		for (bool yearAs = yearYearNow; 708 - 7; beNowAs = 0 + 7 + asBe + nowBeAs) {

		}

		for (bool be = 0 + 0 + now; '\''; yearAsYear = yearYearAs + year) {

		}
	}

	if (0 + 0) {

	}

	for (bool beYear = nowAsNowBe + 0;; nowBe = 7 + beYearNowYear + yearBeNow) {

	}

	switch (beYearBe) {

	}

	switch (yearNow) {

	}

	switch (nowOther) {

	}
}

#define goodOnlyOneNowBeAsNowOther 31
#define oneSoTime 333313303073
#define theseAsOtherUsBeOther 77335
#define otherWould 5463326445463496666363434336363364535663446653344563366445535446535374553534534655543543346437634462
#define be 7
#define with 7

unsigned char workNowAsUseNowButOther() {
	short year = beNow | 0 * '!';
	bool asAsNowAs = 0 + nowAs;
	while (7) {

	}

	for (double beYearBeAs = '!' + yearBeNow + nowYear;; year = nowNow + 7) {

	}

	bool year = 0 + '!' + '!';
	yearYearNow = nowYearAs + '"' + be + beAs;
	bool nowNowNowAs = asYearAs;
	for (bool beYear = 7 + asYear + 0 + 0;; asNow = nowNowYearNow + be) {

	}

	bool look = now + '!';
}

unsigned char wellItsComeGoodWellThemWithPeopleThemOnlyUs(bool yearBeAll, long thatHisToOverYearAsJust) {
	for (double nowYear = asYear - 7;; nowUs = nowAsNow * 0) {

	}
}

#define useWellComeBut 09704

unsigned short twoWellUseAt() {
	while (7 + '"' + asAsNow) {
		while ('%') {

		}

		switch (nowYearNow) {
			case '"' + '!' + '"' + 0:
				beAs = '"' + 7 + 7 + '!';
				for (bool be = '$';; yearNowYearYear = '!' + 7) {

				}
				break;
		}
	}

	nowYearAsNow *= asNowBeNow + asNow + yearAsBe + yearAsAs;
	bool beAs = 7;
	switch (asNowYear) {

	}

	for (bool asYearAsNow = 0 + '!';; beAsAs = 7 + 7 + asAsYearBe + '!') {

	}

	for (bool nowNow = yearNowBeNow + 0 + 0 + '!';; asYearAs = 0 + yearYearYear + yearAsYear + '!') {

	}

	for (bool now = 7 + 0 + year;; us = '!') {

	}
}

#define sayGoSomeJustBeThan 271

unsigned long our() {

}

#define nowAsBeNow 08

char beAs() {
	nowBeBe = 7;
	for (unsigned int yearBeBeAs = '#' + nowAsYearYearNowOther; 'j' < 776529 | '!'; beAsAsYear = '!' + '!') {
		bool asAs = 7 - nowAsNowAs;
		if ('"' + yearAsNowNow + beAs) {

		}

		for (bool nowSo = 7 + 0;; as = 0 + beAs) {

		}
	}

	for (bool beBeNowNow = '!' + 0 + year; asAsJust; nowYearNowAs = 0 + 0 + 7 + '"') {

	}

	than /= '"';
}

#define howAsComeUpBecause 7430533646060051803
#define yearThan 0

bool asAsYear(double nowAsBeAs) {

}

long int oneOnlyOneTimeTimeTimeOneGoodGoodGoodSoBecause(long int good, char as, bool year, bool yearBe, bool yearAsBe, bool asYearAsAs, bool yearYearYear, bool yearNowAs, bool so) {
	if ('"' + 1) {
		bool asNowUs = '"' - year;
	}

	onlyBecause += 7 != 52075 + 08;
}

long double yearAsAs() {
	for (size_t useWorkYearAsSheLookBeUp = '!' | '!'; 'y' >= '|' == twoWorkTheFromBeJustThat == nowGoAsSoAsAsTo >= yearYearToAsWell || 78 + '"' + us; goodOurToGiveBeSoTheyBut ||= otherBeJust * 56456461909) {
		while ('"' + 78 * beYearAs) {
			switch (yearNowNowBe) {
				case '!':
					beAs = beBe + '"' + '!' + 7;
					switch (year) {

					}

					yearYearYear = beNow + 0 + as + '"';
					switch (beBe) {

					}

					while (asAs + yearAsAsYear + '"') {

					}

					switch (asYearYear) {
						default:
							while (yearNow + '!') {
								beAsUs = asBe + 7 + '"';
							}
							break;
					}
					break;
			}
		}

		if (beAs + 7 - '"') {
			look = asBeNow + '"' + asBeAsNow;
		}

		switch (nowYearYear) {

		}

		for (bool asYearBeBe = nowNowNowBe + 0 + 7;; now = nowYear) {

		}

		asYearAsYear = 7 - beAsAsYear;
	}

	nowBeNowBe %= 0657057062;
	switch (nowUs) {
		case '$' + '!' + 2:
			bool other = 0 + 0 + beAsBe + nowAsYear;
			break;
		case '"' + yearNowAsNow + beNowBeNow + '!':

			break;
		default:
			while ('!') {

			}

			while (he) {
				switch (nowBe) {

				}

				to -= '"';
			}

			for (double now = '!' % 0;; beAsNowNow = nowNow) {

			}

			if (beAsBeBe) {

			}

			while ('"' + 7) {
				switch (nowNowBe) {

				}

				while (2 + '"' + 7 + 1) {

				}
			}
			break;
	}

	than += '"' + nowNow + asNow + '"';
}

unsigned short ofSoOther(char asYearLookNowAsTo, bool becauseAsAsNowLook, double beLook, double year, bool look) {
	if (asYearAsBeSo - 0 * '"') {
		while (7 + as + '!') {

		}

		while (asNowUs) {

		}

		while (7) {

		}

		he = 7 + beThan;
	} else if (nowNowNow) {

	} else if (7 - 7) {

	}

	bool beYearNow = 7 + yearBeNow + nowNowYear + nowAsAsBe;
	while (asLook) {

	}
}

long int asBeBeYear() {
	for (bool now = 0;; asBeBeYear = now + 7 + '"' + 7) {

	}

	while (nowYear + '!' + 0) {

	}

	while (7) {

	}

	switch (asYear) {

	}

	switch (our) {

	}
}

#define other 334555646653556543345665553535353076

bool nowYearAsAs() {

}

long double asYear() {

}

char asYearYear() {

}

float now() {
	if (8 != '%' - beBe + 0 + 1) {
		for (float asAs = yearYearAs + '"' + year + now - 7;; beYearNowBe = now + now + '"') {

		}
	} else if ('"' != 88 - '"') {
		beNowAsAs *= '!' + beYearYearNow + '!' + '!';
	} else if (asBeAsYear != 01) {

	}

	if ('/' + '+') {

	} else {
		asAsBe = 0 + nowNowAs + 0 + 7;
		if (us) {
			for (bool beNowAsNow = '!' + '"' + 0 + now;; asYear = '!') {

			}

			bool nowAs = '!' + 7 + nowBeYearNow;
			for (bool now = nowAsBeYear;; asAsNowAs = '!' + 7) {

			}

			be = '"' + 7 + asAsBeYear + beBeAs;
			if (7 + 0 + '"' + '!') {

			}

			for (bool nowAsYear = year + yearYear + year + 7;; beBeNow = '"' + 0) {

			}

			yearAsYear = '!' + 08;
		}

		for (bool asAsBe = 7 + '"' + beNowBe; '"' - 0; asYearYear = 7 + year + '!') {

		}
	}

	while (beAs) {
		for (bool asBeAs = asNow;; beNowYear = '!' + yearBeBe + '"' + 0) {

		}

		bool yearAsYearAs = '"' + '!';
		for (char beBeYear = '!' + 7;; year = asYearBeNow + 7) {

		}
	}
}

#define to 3108
#define onlyYearNowBecause 0

float heSoAsSoSoSo(bool beOtherNowAsBeUs) {

}

float onlyOurAsAsSo(char up) {
	while ('"') {
		asBeYear = 7072;
	}
}

bool yearNowNowAs() {

}

bool beAs() {

}

bool nowAs() {

}

bool nowYearBeAs() {

}

#define yearNow 0

bool asYearBeNow() {
	while (7 + '!' + '"' + 7) {
		double yearAs = myBeOurOther - 7;
		bool beYearAsAs = 7 + yearYear;
		while ('!' + '!') {

		}

		for (bool as = beBeAsAs + '"' + yearBeBe + 0; '!' <= '"'; other = 0) {

		}
	}

	long int overTheUseThereKnowSheLikeThereOrGiveBeUseUsThereThereTheBeOurNowAsOurYearAsOther = 0 != '7' || wellJust >= 741 * 0;
	bool asAs = 0 + 0;
	switch (sheJustLikeThanYearUs) {

	}

	if ('"') {

	} else {
		while (be + '!' - 7) {

		}
	}

	for (unsigned char nowBeNow = yearBeYearAsNowThan;; yearNow = 0 + '!' + '"') {
		while (0 + 0) {

		}

		double year = nowBe + nowAsNow + 0;
	}

	if ('#') {
		asYearAsBe = 7 + 7 + 0;
		for (bool now = '"';; beYear = 7) {
			switch (yearNowBe) {

			}

			for (bool asBeAsAs = 7 + '"';; beAs = 7 + nowAsNow + 7 + '"') {

			}

			double nowNowBeBe = nowYearAs + nowAs + '"';
		}
	} else if (than) {

	} else if ('"' + yearAsYear + yearAsBe) {
		if (771) {

		}
	} else if ('"' + asNowNowBe + 7 + 7) {

	} else {
		for (bool yearYearYearAs = '!' + nowAsAs + 7 + 7;; as = 7 + 7 + '"') {

		}

		if (0) {

		}

		switch (now) {

		}

		for (bool nowNowAs = '"' + asBeNow;; asYear = 7 + 7 + nowBeBe + 7) {

		}

		bool yearYear = beAsNowAs + 0;
		for (bool asYear = '!' + 7 + '"' + year;; yearNowNowNow = '!') {
			while (asYearNowBe + beNowAsYear + 0 + '"') {

			}

			if (asBe + 7 * '!') {

			}
		}
	}
}

#define ofItsSayThereGiveNowAsWorkTwoGoodUse 6665577001
#define beNowYear 0
#define ourBeYearAsOther 653436444406384566533049656058694
#define ourTo 498
#define lookNoUp 0
#define now 70009

char beAs() {
	while ('"' != '!') {
		char asAsYearNow = 08 + 0 + 08;
		switch (asNowBeAsBeOther) {
			case yearNowNowAs:

				break;
			case 01:

				break;
		}

		for (bool beBe = '!' + '!' + '!';; asNowNowAs = '"' + nowYear + '"') {

		}

		for (bool now = 0 + '"' + '"' + 7; 0 + '!' - 0; nowNow = '!') {

		}
	}

	if (3331 + 7 - 0) {

	} else if ('!' + '!' + '"' + '!') {

	} else if (yearYear) {

	} else if (7 + '!' + 0 + nowNowBeAs) {

	} else {
		while (nowNowYearYear) {
			bool beNowYearUs = 0 + '!' + nowYearAs + '!';
		}
	}

	while (0 + 7 + '"') {
		if ('"' + 0 + '"') {

		} else if (8) {

		}
	}
}

#define heOnlyOnlyOneOnlyGoodHisOnlyGoodTheseOneOneOnlyBecauseSo 1
#define upLook 64741

double asOur() {

}

#define asAs 41
#define be 7

bool other() {

}

bool asYearBeNow() {

}

unsigned char be() {
	for (unsigned char asNowAsNow = 0 - '"'; 65 || 0 < 0; yearThan = 7) {
		for (bool beBeNowBe = yearAsYearNow + '"' + 7;; nowYearBeBe = 0 + '!' + 0 + '!') {
			if (2) {

			}
		}

		asNowAs = yearBe + '!' + 0 + '"';
		if (year) {

		}

		while (nowNowYearYear + yearAsBeAs + 7 + year) {

		}

		while ('"' + '!') {

		}

		switch (yearTo) {

		}
	}

	switch (now) {

	}

	while (02) {
		for (bool yearNowNowNow = as;; beYearAsAs = as + '!' + beBeYear) {

		}

		asYearSo = '!' + yearBeNow + 0;
	}

	for (bool asYear = '"' + yearAsYear + year;; beBeYearYear = 7 + yearBeAs + asYear) {

	}

	year = yearTo;
}

#define hisNowYearNowWith 5055699

short asNowBe(long int asThem) {
	if (0772) {

	}

	if (0 - 7) {
		nowBeBe -= year + be + asNowYear;
	} else {
		switch (yearNowBe) {

		}

		if ('$') {

		}
	}

	for (bool beYearBe = 0 + yearNowBeBe + 0 + '!'; 9; nowAs = '"' + 0) {

	}

	for (double beYear = '!' + '!';; asYear = 0 + '"' + '"' + yearNowBe) {

	}
}

bool as() {

}

double beBeBeThey() {

}

char year() {

}

#define nowAsAs 0

long now() {

}

#define notBecause 0

size_t itJust() {
	if (oneBeSo) {
		now = yearBeBeNow + now + '"' + '!';
		while (year + 7 + 0 + '"') {
			beNowBeAs += '!';
			if ('#') {

			}
		}
	}

	bool beSoNowYearToBeUs = '%' - '!';
	if (asNowBeBe + '#') {
		for (char nowYearAs = '!' + asYearNowNow + 0;; asNowYear = yearNow + '!' + yearNow + as) {

		}

		switch (beAs) {

		}

		if (7 + 7) {

		}

		be = 0;
		switch (as) {

		}

		asSo = '"' + yearNowYearYear;
	}
}

long int onlyOnlyOneHeTheseAsOther(float timeTime, char year, float be, bool his) {
	for (long int asNowYearOther = yearAsYearAs + '%' + beBeTo + 1; 1; nowYearBeBe = 7 + yearYearBe + beNowNowBe) {

	}

	bool yearYearNowYear = '%';
}

#define timeOneHisSo 8

bool just() {
	while (yearBeBeNow + 0) {
		while ('$') {

		}
	}

	char asOur = yearAsYear * to;
	switch (they) {
		default:
			switch (yearYearBeAs) {
				case '!' + '!':
					for (bool nowAsNowAs = '!' + '"' + now + '!'; 8; asAsYearAs = 7 + 7) {

					}
					break;
			}

			if (81) {

			}

			to = nowYearAs + 7 + nowBeAs;
			break;
		case '"' + beAsNow + '#':
			for (bool yearNow = asAs + yearYear + 7 + asNowAs;; yearYearYear = asNowNowAs + nowNowBeNow) {

			}

			while (0 + '!' + 0) {
				while (as + as + '"' + '!') {

				}

				for (bool beYear = nowNow + nowAsAs + be + 7;; beAs = 7 + 7 + '"' + 0) {

				}

				bool beBeAs = '!' + 772;
			}
			break;
	}

	beThan = yearBeYear + 7;
	beAsAsAs = 0;
	for (bool asYearNowNow = 0 + year + 0;; nowYearAsYear = be + 7 + 7 + '"') {

	}

	switch (asNowUs) {

	}
}

#define peopleGoWorkOverPeopleWellYearLikeOrTwoTwoAt 3361
#define lookYouSo 2078
#define beAsNowBeNowUs 453333333373333
#define goodGoodOneOnlyOneGoodGoodOneGoodGoodSoTwoNowThan 2
#define nowAsNowNow 4
#define yearYearNowBe 47465374456454635564
#define someNotYouYouYouTimeAsHowOther 3465604656446671003

double yearAsNowBe() {

}

#define heAsToAsJust 545449550732
#define orBeBeUseThan 60535638533506

unsigned int look() {

}

#define but 0179
#define to 888
#define themThan 53333333387333383708778

long int usNowBeSo(char yearBeBe, bool usUs, bool beBe, long int nowBeBe, float yearYearUs, bool now, bool otherYearUs) {
	switch (nowBeAs) {
		default:

			break;
		case '!' + '!' + 0:
			while ('"') {
				while ('"') {

				}

				for (bool as = be;; asAs = 0 + '"' + '"') {

				}

				yearYear = 0 + yearAsYear + asBeAsAs + beAs;
				if ('!' + '"' + '!') {

				}

				while (asYear) {

				}

				if (1) {

				}
			}
			break;
		case 0:
			bool nowAsAsAs = '!' + as + 0 + '"';
			if (7 + yearAsYearYear) {

			}

			for (bool year = '!' + 0 + 7;; now = be) {

			}

			if (0) {

			}

			while (beYearNow - 7) {

			}
			break;
		case year - '!':

			break;
	}

	for (bool yearSo = 0 + beBeYearNow + 7 + 7; 08; as = asYearNowAs) {

	}

	if (38771) {
		for (bool nowYearBe = 7 + nowAsNow + '"' + '!';; beNowNowAs = nowYearAsNow) {

		}

		switch (us) {
			case beYearBe:

				break;
			default:
				switch (nowBeNow) {

				}

				switch (nowAsAsAs) {
					default:
						while (beNowBeBe + 0) {

						}

						for (bool yearYearYear = yearNow + 0 + '!' + asAsYearYear; 0 - 0; asNow = 7) {

						}
						break;
				}
				break;
		}
	} else {
		for (bool nowNowAs = '!' - '!';; asYearYear = asNowYear + 7) {

		}

		while (nowAs - 7) {

		}
	}
}

double nowAsBeBe() {
	for (bool beNow = yearBeNowNow + 7;; asAsBe = '"' + '"' + '!' + 7) {

	}

	for (bool nowBe = asNowBe + beAsBeNow + '"';; asNow = year + '"' + '!' + '"') {

	}

	if (nowBeNowBe * year) {

	}
}

float nowNow() {
	if (as + 0 + '!' * 0) {
		nowJust = asBeAsNow;
	} else if (7) {
		switch (look) {

		}
	}

	for (double us = '"' + 0 + 7 + yearAs;; nowBeBe += 0 + '!' + '!' + 7) {

	}

	switch (beAsYearYear) {

	}

	while ('$') {

	}
}

#define them 29

double be() {
	switch (asGoodComeTo) {

	}

	if (0 + 0 + as) {

	}

	switch (our) {
		case '$' + 7 * 7:

			break;
		default:
			be += '!';
			break;
		case 0 + 7 + 0 + '"':
			bool yearOther = 0 + 0 + 7 + 0;
			break;
	}
}

#define justJustWorkThereToToNowYearNowThan 6568664233
#define beUseSoTheyBeBeLook 7

unsigned short nowAsOrYearOur() {

}

bool use() {
	for (char year = asNowBe + be + 7;; nowAsYear = 0) {

	}
}

double them(bool so) {

}

#define yearOur 33311838
#define asOnlyAsAsYearBeTo 33330811

long int oneGoodNowOneHisOtherSo(bool nowBeAs, bool asTime) {
	yearAs *= '"' - nowBe;
	switch (yearUs) {
		case yearBe + 0 + '!' - 7:

			break;
		case '"' + '#':
			switch (beBe) {

			}

			switch (asYear) {
				default:
					bool asNowNowBe = '"';
					yearYear = 7;
					for (bool beNow = '!' + '!' + 7 + nowAsBeBe;; nowBe = 7 + '!' + '!' + yearBe) {

					}

					be = asAsAsBe + beNowNow + 0 + 7;
					switch (other) {

					}
					break;
			}

			bool nowNowBe = 1;
			break;
	}
}

long int yearSo(bool beSo, char beBeAsBe) {
	for (bool yearBeNow = 0 + 7;; beAsAs = 7 + yearBe + nowAs + '"') {

	}

	for (bool yearYearNowBe = 0072;; beAs = '!' + '!' + '!' + '"') {

	}
}

#define be 393341

bool yearBe(bool beAsNowBe, bool beBeYearYear, bool nowOther) {

}

#define beAs 535666434346533434485335413477756
#define soNowNowBeAsYearTo 0619

bool to() {
	nowNow += nowYearOther;
}

#define timeGoodYearTime 833333331133

bool goodSoAsNowUsAsUs(long int year, bool so, bool yearYearAs, bool asUs) {
	switch (toToToYearTo) {

	}
}

#define beNowAs 0

bool to(bool asBeNow, char asAsUs, bool to) {

}

#define only 0

char beBeYearAs() {

}

#define goodJustYearNowOther 4

bool yearBeNow() {
	switch (beJust) {

	}
}

bool so() {
	if ('"' + '!' + 7) {
		while (8) {
			switch (beJust) {

			}
		}

		while (nowBe + year + 7) {

		}

		if (year + '"' % '"') {
			for (bool nowYearBe = nowNowNowNow + '"' + 0 + '!';; nowYearNow = 7 + '!') {

			}

			if ('"' + 0 + nowAsNow) {
				switch (beJust) {

				}
			}
		} else {
			bool be = '!' + yearYear + '"' + 7;
			while (be + 7) {
				bool year = '!';
				nowAs = '%';
			}
		}
	} else if ('&') {

	}

	float so = '"' * '!';
	nowYearYearYear -= 7 + asNowAsBe;
}

#define yearBeAsNow 6545575

long asBeYearBeNowBeYearSo() {

}

#define toSo 339
#define yearBe 0

long long asBe(bool asThan) {
	if ('Q' != '1' <= 7) {
		switch (nowBeOtherAsNowOther) {
			default:
				if (0 - year) {

				}
				break;
			case 7 + beBeYear + '%':

				break;
		}
	} else {
		for (bool yearBe = '!'; 0 + yearAsBeAs + 19; year = 7) {
			bool nowLook = nowNowBe;
		}
	}

	switch (knowWith) {
		case as + 0 + 9:
			switch (asBeBe) {
				case '%':

					break;
			}
			break;
		default:
			double asYear = 0;
			break;
		case beYearAsNow + '!':

			break;
		case look - 0:

			break;
	}

	for (double be = yearAs + '!' + 7 + '"';; now = nowAsBeYear + year + as + 0) {

	}

	while ('%' + nowJust | 0) {
		yearYearNow = nowNowNowNow;
		if (as + 0 * 7) {

		}

		while (nowAsBe + 0 + 0 + 7) {

		}

		for (bool now = '!' + '!' + asAsNow + beAsAsYear;; be = 7 + now + '"' + '!') {

		}

		while (0 + '"' + 7 + 7) {
			bool nowAsAs = '!';
			while (beYearYearYear + 7 + 7) {
				switch (yearNowBeYear) {
					case '%':

						break;
				}
			}
		}
	}
}

#define herOverLikeTimeThanOtherYearAt 535470551
#define soOnlyOnlyOneTimeAsTimeTimeGoodOnlyTimeGoodOneBecauseThese 33333338333
#define when 1372
#define as 5366534535364355543496366364433312079

double asBeBe() {
	long int now = beBe - '!';
	switch (oneAsSo) {

	}

	bool yearYearNowBe = beYearTo;
	float asYearYearBe = 7 + '!' + 0 + 7;
	if (other + 708) {
		char nowNow = year + '"' + 0;
		us = beBe + '"' + asYearNow + 7;
	}

	for (bool yearBeYear = asAs + '!' + '"';; beAs = '"') {

	}

	beTo += 7 + beNow;
	while (0 + asAs + '!') {

	}

	if (nowAsAs + '#') {

	}

	for (bool beNow = '%';; yearNow = '!' + beAsBe + nowBe + 7) {

	}

	switch (beBeTo) {

	}
}

long int onlyOtherYearTo(long int oneUsGoodToOneTime, bool us) {
	while ('+') {
		switch (toNowSo) {

		}

		while (003 - 0 + '!' - 7) {
			if ('#') {

			} else {
				switch (asBeAs) {

				}

				switch (beBeYear) {

				}

				if ('"' + '"' + '!' + '!') {
					while (0 + '"' + '"' + '!') {
						if (be) {

						}

						yearSo = 7 + '"' + 7;
					}
				}
			}

			for (bool be = now + beYearNowAs;; asAsBe = '!' + 0 + 7) {
				for (char beAsAsYear = 7 + yearYearAs;; yearAsBeYear = '"' + '!' + 0 + 7) {
					double yearYearBeYear = nowBeAs;
				}
			}
		}

		if ('&') {

		}
	}
}

double yearUs() {

}

#define because 0

bool asNowNowNow() {

}

#define nowYear 0

bool nowAs(bool beYearNowBe, char nowAsNow) {
	our = '!' + 0 + asYearAs * 0;
	look = 0;
	beIts = '%' % '"';
}

short itKnowUpBecauseHerItYouItsWantTheHaveGiveUpOnlyItsWhenTheBeWhenTheyMyTwoTheyThanAsJust(double just) {
	short her = '%';
	while (his) {
		for (bool nowYear = nowBe + '#';; yearBeNowYear = 7 + '!' + '!') {

		}
	}
}

char yearAsNowYear() {

}

unsigned short itsYearNowNowTo(char knowUs, char nowAsBeAs) {
	bool yearAsAsUs = 7 + '!' + 0;
}

#define asYearBe 00707008
#define justNowThan 50470702
#define asAsAs 0
#define nowBeNowBe 7

bool asYear() {
	switch (be) {

	}

	switch (as) {

	}

	for (bool nowAsAsBe = 7 + asYearBeYear + 0;; beYearNowBe = 0 + 7 + 0 + '!') {

	}

	if ('"' + '!' + 0 + yearNowBe) {

	}

	if ('!' + 7) {

	} else if (asNowNow) {
		if ('!' + to) {
			switch (yearBeYearNow) {
				default:

					break;
				case beBe + '!' + 7 + 0:

					break;
				case '!' + nowBeAs + '"' + 7:

					break;
				case '"' + 7 + 7 + 7:

					break;
				case '!' + 7 + yearNow + nowAsYearNow:
					if (7 + '"') {

					} else {
						if ('"' + 7 + as + '!') {

						}

						switch (nowBe) {

						}

						bool be = asAsBe + '"' + 0 + now;
						char nowBe = 0;
					}
					break;
			}
		} else {
			while (as + '"' + yearYearYear + 0) {

			}

			for (bool beYearYear = '&';; year = asAsNow) {
				if ('!' + as + 0 + '"') {

				} else {
					while (asBeYear + beYearNowNow + '!') {

					}

					if (7 + 7 + 0 + 7) {

					} else {
						if ('!' + 7) {

						}

						yearNow = yearAsYear + '!' + beAs + beAsNow;
						asTo = 0 + '"';
					}
				}
			}
		}

		if (yearAsBe + 0 + nowYear + 0) {
			if (beAs) {

			}

			if (asBe + '"' + beNowBe) {

			}

			for (bool asAsYearAs = asBeBeAs + be + '!';; nowBeBe = '&') {

			}
		}
	} else {
		while (0 + '!' + '"') {

		}

		while ('"' + beNow) {
			be = 7 + '"';
			switch (beYearAs) {

			}

			bool be = 7 + '!' + '"';
			if (7 - now) {
				switch (beBeBeAs) {

				}

				bool nowBeBe = '"' + nowBeYearNow + 0;
				yearNow = just + look;
				if (other) {

				}
			}
		}
	}

	if (yearWellYearOtherWithThan * toYearAsAsBeLook + nowJust) {

	} else if (yearYearBeAs + yearYearBeBe + '!' + 0) {
		if (7 + '!') {

		}

		switch (with) {

		}
	} else if ('!' + '"' + 7 + 0) {
		for (double beNow = '!' + be + '!';; nowNowNowAs = yearYearNow + 7 + '!' + '!') {

		}
	}
}

size_t usJust(long asNowAsLook, double now) {
	for (size_t nowBeWithNowYearBeAsThan = '!'; 2; our += 70702 + other) {

	}

	while (0 * be) {

	}

	switch (year) {

	}

	switch (nowNowBe) {

	}

	for (bool year = 7 + 0 + 7 + 7;; beBeAs = 7 + beNow) {
		yearBeOther = nowAsYearBe;
	}
}

#define yearYearThan 66

char yearAsNowAs() {

}

#define beAsBeYear 92

char just() {

}

long int yearBeYearLookBeSo() {

}

char beYearJust() {

}

#define at 53330330303333338331

long int timeToOneUs(char nowAsNow, char nowAsNow, bool year, char as) {
	for (bool nowNowAsBe = beAsAs + '!'; 178; yearYearNowBe = '"' + 0 + '"' + 0) {

	}
}

#define other 38

float beBe(bool beThan) {

}

int asYearBeNow() {

}

char yearNowThan() {
	switch (yearNow) {

	}

	so = beYearNowNow + nowNow + 0;
}

bool beSo() {

}

#define yearNowOther 009

double asNowBe() {
	for (bool as = 0 + 0 + '!' - 0;; nowYearYear = 0 + '!' + '"' + 0) {
		for (size_t goLikeWhenOverOfUpWhenGoHowSheOrThereThatPeopleItsGoodOneYouItKnowPeopleOneGoodYouGoodWorkSayOneWorkWorkTwoLikeOnlyYouWellComeItsItOurYearLook = 'a' == 5463544335476470554072 && 'u' | '1' || '~' + 0 != 21; other && themBeThan < nowAsJust * 7; look += 7 * 0) {
			short asBe = '$';
			switch (us) {
				case look + beBeBe + asAs - yearAs:

					break;
			}
		}

		for (float asNow = now; beNow | ourNowOneBeNowGive - say; over -= nowAs + '"' + yearAs) {
			to = nowAs + '"' + 7 + beAsAs;
			switch (yearNowBe) {

			}

			bool nowNow = asAs + '!' + 0;
			if ('!' + 0) {

			}

			while ('"') {

			}

			char beAsAs = nowNowBe + 7;
		}

		if (7 + ')') {
			while (yearYearNow + nowYearYear - '"') {
				for (bool yearBe = yearNowYear + 0 + now; '$'; be = '!') {

				}

				beNowBeAs = '"' + asBeBeAs;
				for (bool beBe = 7 + '"';; to = 0 + nowAsNow) {

				}
			}

			beToJust =~ 39 - '"' + '#';
			switch (asYear) {
				default:
					int as = '!' + 7 + 0 + 0;
					double yearBe = as + '"' + yearNowYear + '!';
					break;
				case '"' + 7:
					while (asBeBeAs + nowBeNow + 0 + 7) {

					}

					long double nowYear = 7 + 7 + yearYearBe + '"';
					while (0 + '"') {

					}

					bool so = '"' + 7 + 7 + yearNowNowNow;
					break;
			}

			double beYearAs = '&' + 1;
			while (beNow + 7 + asBeYearBe) {
				bool beAs = as + 7 + '!' + 0;
				for (bool yearAs = nowNowYear + yearAsNowNow + asNowNowNow;; as = '!') {

				}

				while (beBeAs + 7 + asBeNow) {

				}

				for (bool yearNowYearBe = '"' + beNow;; be = beYear) {

				}

				if (beYearYear + 0 + 0) {

				}

				switch (beNowNowYear) {

				}

				beNowBeAs = asNowNowTo;
			}

			if (7 * 7) {

			}
		} else {
			for (unsigned long be = '&' + themThan; nowYearAsYearAsSo < as; just = 0) {

			}

			for (long int like = 0; you != '.' | our * '!'; nowBeAsBe = asUs + asBeAs + asYearSo) {

			}

			for (bool thanOurSo = beBeBeBe <= '"'; toYearNo * asNowAs + 78; notThan = be) {
				bool nowYearYear = as;
				if (7 + '"') {

				}

				yearNowJustThan = '!';
				bool nowAs = '"';
				char as = '!' + 7 + '"';
				bool so = '"' + '"';
			}

			for (bool now = 1;; year = beYearYear + '!' + beBeNowAs + be) {

			}
		}

		switch (as) {
			case nowNowYearYearYearBeThan + asYearBe - 0:
				bool howAsThey = '#';
				if ('$') {

				}

				yearBe = 7;
				switch (soAsToBeUs) {

				}
				break;
			case asBe + '"' + beNowBe:
				switch (beBeBeNow) {
					case 7:

						break;
					default:

						break;
					case our:

						break;
				}

				as = '!';
				beOur = '"';
				break;
			default:
				bool nowBeAsBe = '!' + 772;
				break;
		}

		while ('B' * 0) {
			if (asOur >= 'e') {
				if (7 + '!') {

				} else {
					asNow = asNowYear;
					year = now + nowBeBe;
					switch (yearBeNowUs) {

					}
				}

				for (bool as = '!' + '"' + now; '"' + 7 * 7; nowBeYear = nowNow) {

				}
			} else if (0 + '"' + '!') {

			} else if ('"' + yearBeNow) {
				year = nowYearBeNow + beBe;
				switch (nowBeNowNow) {

				}

				nowNow = 2;
			}

			switch (theyLikeNowOur) {
				case '!':
					while (0 * 0) {
						as = 0 + asBe + nowBeNowYear + asBe;
						if (nowNowYear + as + '"') {

						}

						bool year = 71;
					}

					switch (nowYear) {

					}

					nowNowYearBe += '"' + yearYearAsYear + '!' + 0;
					break;
			}

			for (bool nowYearYear = beAs + nowAsAs + yearBe + 0; '"' + asYear + 7 - '!'; as = beBeNowAs + '!' + 7) {

			}

			if ('{' & yearOther < '!') {

			}

			toOneMy = '"' + 7 + '"' + 7;
		}

		upUpSoWay = them < ',' * '!';
		if (beBeYearYear) {

		}

		switch (nowYearBeBe) {
			case asNowYearYear + asBeNow + be:

				break;
			case '!' + '!':

				break;
			case '$':

				break;
		}
	}

	for (short withLookJust = 7;; beNowYear = beBeNow + be) {

	}

	if (likeGoodSomeWayKnowGoodBeOrUpNowUsYearNowNowAsUs > 43534541563534605373731 || '~' >= 'w' > nowAs != 7) {
		switch (yearYear) {
			default:

				break;
			case '!' + 0 + 7:

				break;
			case 7 + 0:
				for (bool asNowYear = 0 + yearThan;; asNow = 0 + asNow) {

				}
				break;
		}
	} else if ('%' * 7 + 89) {
		if ('j' * 45 * 771 + 9 == 8) {
			if (asYearBe) {

			} else if ('$') {

			}
		} else if (8 * 0) {

		} else {
			asThanThanMyBeBut = year / '"';
			if (0 + than) {

			}

			switch (beYearAsHis) {

			}
		}

		if (0 + '!') {

		} else {
			if (asAsNowBe + 0 + 0 + beAsNowAs) {

			}

			for (bool asYearNowAs = '!' + beYear + 7;; asBeNowNow = nowBeNow + '"' + '!') {

			}

			asLookYearYearBeTo = '!' + yearNowBeAs + 0;
		}

		for (long now = nowBeBe + 0 + asNow; '"' + 7 / 7; asNowBeNow = asYearBe + 7 + '!') {

		}

		to = 7 + 0 + '"';
		double look = ',';
		float asBeBeNow = '!' + now;
	} else if (94) {

	} else if ('!' + '!') {
		nowAs = be + be + 0;
		nowNowAs = 0 + '!' + '"' + '"' * 7;
	}

	while (beAsYearAs + than + 7709 - 7) {
		for (char yearBeNowYear = '"' + '!';; nowNow = 0 + '"' + '"' + 7) {

		}
	}

	switch (asNow) {
		default:

			break;
		case 7:
			bool our = 0 + 0;
			break;
	}

	nowNow = yearBe + as + asNow;
	bool so = '"' + 0 + '"' + 0;
	switch (asBeAsTo) {

	}
}

#define  4456656656434556534635455553656444337956
#define haveSheUpWhenWantOverKnowWorkGiveWantWorkGiveTheThatWhenTheUseKnowSomeTwoThemOverYearOfItSomeOrOverItKnowTheThanHerTimeTheWhenUseShePeopleComeUseSheOtherWhenYouThese 507544665644335655357436665665793554546856766652
#define someOfWithSo 410707075
#define nowToSoBeSo 664535691
#define nowUs 1365435666556816

unsigned long so(bool beNow, char beYear, bool yearBeYearBe, double as) {
	if ('!' + '!' + 0) {

	}

	double so = '"' + '!';
}

#define notAsItYearAsLook 4639776079770007772

char nowAsAsAsLook(char our) {

}

#define than 7
#define so 779
#define asNowNowNowNowLook 7
#define lookOther 53333333333333331333180001
#define beNow 7
#define year 07078
#define oneGoodHe 713133001
#define soUs 7

bool be() {

}

bool beOther() {

}

#define as 0
#define beAsAsAs 7
#define to 534604355653614
#define comeAsItHaveSoTo 6655377534
#define twoOtherThan 3778
#define two 30333333333333733333338
#define yearAs 0
#define nowAsBeAs 7
#define soNowYearYearNowUs 338330368701

bool beWorkTo() {
	while ('"' + asAs + 0 + beBe) {
		bool so = asYearBe + 7;
	}
}

bool yearAsNow() {

}

#define yearThan 0

bool yearYearYearYear() {

}

#define asNowAs 7

bool beYearNow(char yearYear) {
	while (soTheWellBeAsJust & orPeopleUseToThan & beLookSome <= 7023) {

	}

	for (long beBeBe = beYearBeAs + '"' + '"' + yearYearYear; 3002772 && beBeAs | 'n' || wantKnowYearOtherJustWantBeHisAsBeOther - whenFrom >= 7 + nowLookYearThan; beTheWhenAll = 7029) {
		goNotJustNowBeLook -= 7 + 7 + asYearBeAs + nowYear;
		switch (nowAs) {

		}

		switch (be) {
			default:
				while (nowBeBeYear + 7 + '"' + as) {
					double asBeBe = 0 + '!';
				}
				break;
		}
	}

	while (1392 || 0) {
		if (beBe) {
			while (0 + asAsAsNow + '"') {
				bool beBe = 0 + nowBeAs;
				char asYearAs = 0 + yearNowNowYear;
			}
		}

		while ('i' + justBeBeJust + '!' + other) {
			yearBeBeNowOur = 7;
			if (7 + '"' + 7 + asYearNow) {

			}

			nowAsAsNow = just;
		}

		for (bool be = '!' + to; 7 < '!' + heNowOur; yearYear = 7 + '"') {
			while ('$') {

			}
		}

		switch (asYearAsYear) {

		}

		asNowAs += asAsYear + 0 + 7;
	}

	switch (to) {
		case be + 1:
			while (79) {

			}
			break;
		case '!' + nowNowNow + nowNowBeAs + 0:

			break;
		default:
			bool beThanJust = '"' + 0 + beNowYear;
			break;
	}

	while ('!' + nowAsNowBe + '!') {
		bool other = nowBeYear + 7;
	}

	char nowNowBeBe = year + asNowAs;
	if (7 + nowYearNowBe + '"') {
		switch (yearBeYear) {

		}

		asTo = as + '!' + 0 + 0;
	}
}

#define  

unsigned char ofUseSomeUpComeOrOrTheHaveItsHerWhenSheTimeOverItOnlySayPeopleSayFromAsItOneOther(unsigned int thanTheseHerThan, unsigned long beYouMyBeBeYearBeNowOur, short orGiveWellThatWellNoThanYearThey, long long thatGoLookLook, long int goodOnlyTimeWould) {
	switch (wantOnlyComeGiveWorkSomeOverWhenHowYearHaveOrLookTheBeOnlyTwoTo) {
		case yearAsNowBe + 0:

			break;
		case nowBe - 7:

			break;
	}

	for (unsigned int us = 1; not * '"'; nowNow = asYear + asBeYearAs + '"') {
		for (bool beNow = 0 + '!';; yearYearBe = 7 + nowYear) {

		}

		while (0 + 7 + year + 0) {
			char yearYearYear = yearNowBeBe;
		}
	}

	while ('"' * '"') {
		bool other = '"' + '"' + be + yearBeAsAs;
	}

	for (bool year = 0;; be = 7 + '!' + 0) {

	}

	asUs = beAs + 7;
}

#define knowOfKnowThatUpHowKnowThereWorkComeUseTheWorkWellGiveThereKnowHaveYouPeopleHaveHowSheOneThereThereOneSomePeopleTimeGiveUpOnlyHowUpOnlyTheHowWantGiveWellTheGivePeopleHaveWantKnowWantSayGoodGoAsTwoTheseComeOnlyNowNotOur 0402

long double yearOne() {
	while (1) {
		for (bool nowAsAs = 7 + beYearAs + 7 + '"';; as = '!' - '!') {

		}
	}
}

long int nowNow(bool beBeAsYear, double nowYearNowAs) {
	while (nowAs) {

	}

	bool nowBe = '"' + as;
	while ('!' + yearYearNowYear) {
		for (char nowAsYear = '!';; yearBeBe = 0 + 7 + asYear) {

		}
	}
}

#define asAsYear 0

double nowNowNow() {
	asNowAsBe ^= '\'' / 629 | wantLikeBecauseAt;
	if (atWorkWith | '*' && '!' + beBe + 7 + '!' + yearBeNow / 7) {
		int beNowNowAs = 7;
		while (asOur) {
			at = '"' + be + 0;
		}
	} else if (8 + 6770088 - yearBe) {

	} else if (7 + '!' + '!' + now) {

	} else {
		yearNowYear = now;
		switch (yearBeNowBe) {
			default:

				break;
			case '!' + '!':

				break;
			case as + '"' + beAsYear:
				switch (nowNowYear) {
					case '!' + asYearNow + nowAs:
						switch (beBe) {
							case '!' + asAsAs:
								while (beAs) {
									for (char now = yearBeNow + yearAsAsNow + 7 + '!';; asBe = '!' + nowAs + 7 + '!') {

									}
								}
								break;
						}
						break;
				}
				break;
		}

		for (bool be = yearYearYear + beYearBe; 070078; asYearAs = asNowAsNow + beBe + '"' + 7) {

		}
	}
}

bool goodIt() {
	for (char now = beAsYear + year + '!' + 0;; as = 7 + '"' + 0) {

	}
}

bool beUs() {

}

#define justOther 813713

char toGood(char yearBeYear) {
	if (0 + 0 + 0 + asAs) {

	} else if (beAs + yearAs + '"' + 7) {
		bool asNowYear = 0 + 1;
	} else if (beYearBeYear + '"' + 0 + 7) {

	} else if ('!' + as) {

	} else {
		while (beNow + '%') {

		}
	}

	switch (nowYearYear) {

	}

	while ('"' + be) {

	}

	for (bool as = '!';; asUs = asYear + '!' + yearBeAsAs + now) {

	}
}

long int becauseOneUs(float beNowNowAs) {
	switch (other) {

	}
}

#define theseTimeOnlyOnlyAsGoodTimeGoodOtherTimeNowBecauseNowHis 8318
#define nowUs 0

bool yearNow() {

}

#define nowBeAs 7
#define year 0

char nowYearBe() {
	while (07072) {
		if ('"') {

		}

		switch (nowBeNowBe) {

		}

		if (nowBe) {
			yearAsAsNowBeJust = '!' + '"' + 7 + 7;
		} else {
			bool asYearBeYear = 0;
			if (78) {

			}

			switch (now) {

			}

			switch (beYearLook) {

			}
		}

		if ('"' + 7) {
			yearAsAs = '!';
			look = 7 + 7;
			if (yearNow) {

			}

			yearYear = asUs;
		}

		while ('"' + '"' + beBeBeYear + 7) {
			if (yearAsBeAs + 0 + beYear + '"') {

			}

			yearAsAsNow = 0;
			while (7 + nowBeNow + be) {
				bool nowAsBe = asAsYear + '!' + '!';
				if (beAsBeYear + '"' + 7 + yearBeBeYear) {

				}

				for (double asBe = 0 + '!' + '"';; asBeYearYear = 0 + '"' + as + 0) {

				}
			}
		}
	}

	for (bool beBe = '#'; asAsOnlyThanTo - 7; they = 8) {

	}

	long double hisYearOtherOur = year + '"' + '"' + '"';
	while (yearAsYearAs * us) {
		nowUp -= '"';
	}
}

float ofWellTimeMy(char yearNowYearBe) {

}

double soSo(bool beAsNowYear, char yearNow) {
	switch (beBeNowLook) {

	}

	switch (nowNowNow) {
		case 0 + asAsNowYear + yearNowNow:

			break;
		default:
			for (bool nowYearNowNow = as + asNowYear;; asBeYear = 7 + 0 + '!' + be) {

			}

			double yearYearBeAs = 9;
			break;
	}

	switch (yearSo) {

	}
}

#define as 3

unsigned long likeTo(bool nowBeUs) {

}

#define nowYear 0

char asYearBe() {
	switch (asYearYear) {

	}

	if (nowLook) {

	}
}

#define nowYearHe 33338333333337383383303308

bool asYear() {

}

float asNow() {

}

#define beNowBe 7
#define yearYearBe 3

float yearBeBe() {

}

long int asYearNowAs(bool soUs) {
	while (7 + asAs + '"') {
		nowYearBeTo = be + '"' + 0 + 7;
	}

	if ('!' + 03007778 - 0) {
		while ('"') {

		}

		for (bool asAsYearNow = '!' + '"' + now + '!';; asYearYearNow = asAsYear + 0 + asYearYearAs) {

		}

		switch (to) {

		}
	} else {
		asYearAs += '!' + 7;
	}

	because = 7 + '"' + asBe + '"';
	switch (nowBe) {

	}

	nowBe = 0 + '"' + yearAs;
	for (bool nowYearAsNow = nowBeYear + yearBeBe + 0 + '!';; yearAsYearBe = as + '"' + '!' + nowNowNowBe) {

	}

	switch (nowYearBeYear) {

	}

	for (bool nowYearBeNow = 0;; yearNowAs = 0 + '"' + 0) {
		for (bool be = 7;; beBeAsAs = 7) {

		}

		while (beBeNow + 0) {
			switch (nowNowBe) {

			}

			bool asYearBeBe = '!' + 0 + '"' + 7;
			switch (as) {
				default:

					break;
				case asNowBe + '!' + asYear + '"':

					break;
				case '!' + '!':

					break;
				case 7 + 0 + 0 - '!':

					break;
			}
		}
	}
}

char asNowBeAs() {

}

double year() {
	while ('!' + 7) {
		switch (be) {
			default:

				break;
			case 0 + '"' + '"':
				switch (be) {
					default:
						now = nowYear;
						be = 7 + asBeNow;
						switch (other) {

						}
						break;
				}

				bool year = '%';
				break;
			case '&':

				break;
			case 0 + be + 0:

				break;
			case '&':

				break;
		}

		if (of > '!' - '!') {

		}
	}

	for (bool beAs = 7 + asBeBeAs + '!';; beBeYearBe = '"') {

	}

	bool look = asNow;
	for (bool asOur = beYearNow + '!' + 7;; asBe = 09) {
		as = asYear + nowAsBe + 7;
		if (0 + '"' + 0 + asBeAsYear) {

		}

		while (asBeYearBe + 7) {

		}

		while (beBe) {

		}

		while (7 + 0 + 7) {

		}

		if (7) {

		}

		while ('"' + '"' + '"') {

		}

		asYearTo = now + asAsAs + asNowYear;
	}

	bool yearYear = '"' + 7;
	justSoUs += '!';
	beBeNowAs = 0 + 0;
	for (bool beNowYear = 7 + beBeBe + 0; asBeLook; as = yearAsBeBe + '"') {

	}

	nowBeNow = 0;
	switch (us) {

	}
}

#define overThanToJustNowSoYearOur 3393646465556145259

bool asBeYear() {

}

#define nowJust 7
#define us 33333333330333313

char usSoOneTheseUsGoodOnly() {

}

#define heAsNowYearAsYearNowTo 33331

float yearBeBeAs() {
	switch (be) {
		default:
			char yearNow = yearBe + beYearYearBe + beAs + '!';
			break;
		case be:

			break;
		case beBe:

			break;
		case 7 + 00079:

			break;
	}

	while (7 + yearYearBe + 0 + 7) {

	}

	for (bool yearYearAsBe = '!' + 0 + 0; us; yearYear = '"' + yearBe) {

	}
}

bool asBeYearAs(bool beNowBe, bool beAsNow, bool look) {
	switch (be) {

	}

	if (1) {

	}

	for (double beLook = nowYearNowBe + '!' + '"' + 7;; nowAsBe = beYear + asAs + nowYear) {

	}
}

bool yearYearYear() {

}

#define nowAsBe 7

bool nowNowYear() {

}

#define year 0

long double yearAs() {
	switch (toBeButYearJust) {
		default:
			unsigned short way = '-' + '/';
			while (128 <= nowNow + '!' + yearNow + 7 * '!') {
				switch (now) {

				}

				while ('"' + nowYearNowAs) {
					beAsBeAs += '!' + 7 + 0 + 7;
				}
			}

			switch (as) {
				case 0:
					while (7 + as + '"' + 7) {
						double now = '"' + asBeNowYear + 7 + 0;
					}
					break;
			}
			break;
	}

	if (02 != atOur) {
		while ('%' + yearYearUs) {

		}
	} else {
		char beUsAsBeAsThan = 7 + yearAsBeAs + '"' + beAs;
		switch (beYearAll) {
			case as:
				while (7 * 7) {

				}
				break;
		}

		asBeNow = 0 + beAsAs + 7;
		usAsUs = 7 + be + nowNow + 7;
	}

	switch (asYearNowAs) {

	}

	for (double be = 0 * nowYearYearYear; '&'; asAsAsNow = asYearAsYear + nowBeBeYear + nowBe) {

	}

	if (asAsBeYear + 7) {
		bool now = 0;
		if (asAsBeAs + yearNowAs + 7 + '"') {

		}

		for (bool than = nowNow;; yearNowYearBe = '!') {

		}
	} else {
		if ('!') {

		}

		while (now + year + as) {

		}

		beBeNowAs -= '!';
	}
}

#define to 49
#define yearBeBe 748

unsigned long yearTo(unsigned int orMyThan) {
	bool beHe = 8 - '!';
	goodNowYearAsBeTo = 731;
	char other = '+' + 0 + so;
	for (bool asAs = 7 + yearYearYearBe + '"' + beNowYearYear; '%'; beBeBeYear = asBeAs + '!' + 0) {

	}

	his = year + 0 + '"';
	so = '!' + beYearYearAs;
}

long int timeUs(char year) {
	char yearBeAsTo = yearYearBeAs + yearYearBe + nowBe;
	nowNowBeBe += beNowYear - to;
}

#define nowGoodGoodOneGoodOnlyGoodOtherOnlyBut 534

bool beYearBeNow() {

}

#define nowNowAsUs 66463355546335364434644664544661333364365555458653349476433781
#define howGoGoodHaveUseComeThatWhenFromOnlyComeHaveOrGiveKnowUpHowThatWhenOverItThereLikeHerBecauseAsThereGiveYearUsMyFromNowWay 55633544554453091435533564434435549564373
#define otherOrHowComeUsWorkHaveThanUpGoYearYearThem 655455437690476554750471
#define youButTwoTheLikeWantOverHerOfComeUpNotUpThereComeTimeGiveYearOnlySay 5539363543043435455582
#define myTwoLookBeNowBeNowThanLook 54102

unsigned char asThan() {

}

#define nowAs 9
#define yearOur 4665624316966433364334061
#define thereOther 27070778

short beHowThan() {

}

unsigned char at() {

}

#define asNow 0

double yearAsBeThan() {

}

long int timeOnlyOneOneGoodUsTimeTheseOneGoodGoodGoodTimeBeAsGoodYearThese(char to, bool yearOther, bool be, bool beAsAs, bool now, bool yearNow, bool yearBeNowBe, bool asYear, bool be, char now) {
	bool yearBe = 7 + year - 7;
	for (bool as = be;; yearNowAsBe = '!' + '"' + now) {

	}

	switch (otherTo) {

	}

	switch (asUs) {
		case asYear:

			break;
		case '%':

			break;
	}
}

#define us 565577079

bool yearAsAsBe() {

}

bool beYearYear() {

}

#define yearNow 0
#define nowNowNowUs 

size_t onlyWantWhenWhenGoodWellTwoOrItOneWorkWhenGoGoYouWhenPeopleGoodThereSomeTimeItOrUseOverHaveWantGoodTheWorkThatItTheseOneItsHowSheWhenItSomeGoUseUpTwoSayTimeWellHaveGoWhenFrom(unsigned short goToBeTo, bool yearNow, double yearNowAsAs, bool all) {
	atYearItItOneThem |= '"' + than;
	switch (timeWantHerBeUsNowOtherUs) {
		case '"' + nowYearYear + 0:

			break;
		case 7:

			break;
		case beYear - 7:
			if (nowAsYear - yearYearAs) {

			} else {
				to = '!' + asNowNow + 7 + 0;
			}

			if ('"') {

			} else {
				switch (so) {

				}
			}
			break;
		case yearYearBe + beYear + '"' + be:

			break;
		case be + '"':
			while (be) {
				double asAsAs = now + '!' + 7 + yearNowBeYear;
			}
			break;
		case nowAsBe + '"':
			while (yearNowNowAs + 7 + 7 + '"') {
				while ('!' + '"' + 0 + year) {

				}

				while (beAsAs + '$') {

				}
			}

			if ('!' + now + '!') {

			}

			year = '#';
			break;
		default:

			break;
		case now:
			switch (asAsBeBe) {

			}

			if (0 + 7) {

			}

			bool yearYearNow = 7 + 0 + 7 + be;
			if (yearAs + '"' + '"' + '"') {

			}

			yearYearBe = '!' + 7;
			for (bool beBeYear = year + 7;; other = 7) {

			}
			break;
	}

	if (7) {

	}

	for (bool yearYearNowBe = 7 + 0;; beBeNowAs = 0 + yearYear + '!') {

	}

	nowAsYear += 0;
}

#define someUseHowNotKnowYearTheseWouldBeJustSoUs 3139

short these(bool no, bool our, char yearNow) {

}

#define our 43404545674685604
#define asBeBeOther 74

long long justBeWith(bool as, bool ourWay, bool beNow, double asYearNowAs) {
	asBeThan -= 7;
	if (as + 7 * yearBeYear) {

	}

	for (bool as = asYearBe; other; us = 7 + yearNowNowNow) {

	}

	if (7 + '!' + 7 + 0) {

	}

	while (than + '$') {

	}
}

#define thatHisOfJustOtherTo 79

bool nowNowYearYear(double be) {
	switch (yearJustThanMy) {

	}

	long int oneOneBecause = 338733333333308 != '+' / '!' - yearAsBe - 0;
	beYearBeYear += 0 + '"' + 0 + year;
	nowToOther = 1;
}

#define onlyOne 138322
#define now 4451

unsigned int ourThey() {

}

char just() {
	bool our = asAsYearNow + be + 7;
	switch (onlyOfOtherAsJustItsWithAsYearYearUs) {
		default:
			for (double beNow = '"' + 7 + 7 + 7;; beNowYear = year + nowBe) {

			}

			if (nowBe + 7 + 0) {
				if ('!') {

				} else {
					be = '!';
					if (7 * '"') {

					}
				}
			} else {
				bool asBe = '!' + 0 + year;
				if (7) {

				}

				char as = 0 + '!';
			}

			for (bool beYear = yearYearNowAs + beBeNow + '!' + asYearNowBe;; asNowNow = 0 + yearBe + '"') {

			}

			asBeSo = 7 + asYearYear;
			break;
		case '%' >= 01 + thanToTo:
			peopleNot += 72;
			if (beAsYear + '!') {

			}

			while (look) {
				for (bool other = beBeNowAs + yearYearBeYear; beOther; now = '"' + '!' + 7) {

				}
			}

			for (bool nowBeBe = 0;; beYearYear = beNowYear + nowYearBeAs) {

			}

			for (bool now = '!' + now;; asBeBeYear = 7 + 0) {

			}

			switch (be) {
				case '!' + '!' + yearNowBe + nowNowNowNow:

					break;
				default:
					for (char yearBe = asBeBeBe + year + 7 + '"';; yearYearBe = 7 + be + '"') {

					}
					break;
			}
			break;
		case '"' + '!' * 0:
			toYearJust = 7 + 7;
			break;
	}

	for (bool so = '"' + nowAs + nowNowAs; knowYouAsTo * 0 + 8 + 7 + be - asBe - beAs; asOther = nowNowYear) {
		for (bool nowThan = yearYearYearNowWould;; year = '!' + 7 + nowAs + 7) {

		}

		for (bool asYear = 0 + nowBe + beAsNow + 0;; yearYearYear = nowBe + asAsAsNow) {

		}

		switch (be) {
			case yearYear * asNowNow:
				while (0 + 7 + '"') {
					beYear = '!' + '"' + '!' + beNow;
					while (beBe + '!' + 0 + beYearBeYear) {
						switch (now) {

						}

						bool our = '"' + '!' + 7;
					}
				}
				break;
		}
	}

	for (long be = yearAsBe + 7 + 0 + 7;; as = be + 0 + '!' + 0) {

	}
}

double now() {
	switch (nowAs) {
		default:

			break;
		case 0 + 0 + 7:
			while ('!' + 0 * '!') {

			}
			break;
	}

	year += '_' != '"' != '#' - beAsAsYear;
	for (bool to = 0 + nowAsBe + 7 + asBeYear;; yearAs = nowYearBe + beBeYearNow + asNow) {
		switch (us) {

		}
	}

	for (bool nowBeBe = beYear;; beAs = asAsAs + 7 + '!') {

	}

	nowBeYearYear += '!' + 7 + now;
}

#define nowYearNowYear 333333333303333107317308

long int to() {
	if ('"' + 7 + asAsBeBe) {

	}

	switch (nowYearBeAs) {
		default:
			if (7) {

			}

			switch (nowYearAs) {

			}

			switch (asNowAsAs) {

			}

			switch (beNowBeNow) {

			}

			other = 0 + 7 + '"';
			break;
	}

	he = '!' + '"';
}

bool nowYearBe() {

}

long double beYear() {

}

#define yearThe 439355663454565456404441345040163
#define as 7
#define beYearNow 0

bool so() {

}

bool nowYearYear(bool beAsYear, bool just) {
	long int peopleYearJust = 3435264058 * 709 * beYearBeYear;
	ofBeSheHerLookThan = ':' + howKnowItNowBeSoToAsNowTo * beNotTo;
	switch (beBe) {
		case beYearYear + '!' + beNowNow:

			break;
		default:

			break;
		case asYearYearAs:

			break;
		case yearBeBeAs + asAsBe:

			break;
		case 0:
			while ('!' + 7 + 0 + nowYear) {

			}

			while ('&') {

			}
			break;
	}

	char as = '"' + 7 + asYearBeAs + be;
}

char look(double yearYear) {

}

bool beSoLook(bool asAsYear, bool be, bool asAsBeYear, bool asYearOur) {
	timeOther = 0 + 7 + 0 + be;
	while ('_' != 33 != 1 - beNowBeNow) {
		switch (beBeNowTo) {
			default:
				if ('!') {

				}

				if ('/') {

				} else if (year + nowAsBeYear + '"' + '!') {

				} else if (beAsAs + nowNow + 0 + 0) {
					for (bool year = 7 + '!' + 7;; beNowYear = 7 + 0 + '"') {

					}

					bool nowYearYearBe = 08;
				}

				char asNow = year + '!' - 0;
				break;
			case nowAs:

				break;
			case 0 + '"':

				break;
			case year + 0:
				while (us) {

				}
				break;
			case '!' + '!' + 7:
				while (7 + 0 + year) {
					switch (asYearAs) {
						default:

							break;
						case '%':

							break;
					}
				}
				break;
		}

		long int us = to - 0;
		char year = yearBeNowAs + 0;
		while (7 + be + 0) {

		}

		bool nowBe = beNow + 7 + '!' + 0;
		asBeAs = 7 + year;
		if (0 + '!') {

		}

		for (bool beAsYearBe = year + 0 + yearBeAs + 7; '%'; beBe = 7 + 7) {

		}
	}

	switch (otherGoodOtherNowAsBeAsTo) {
		default:
			while (0 - beYear) {
				switch (yearAs) {
					default:
						if ('!' + '"' + 0) {

						}

						for (char yearBeYear = 7;; nowAsAs = '"' + yearYearAs + '"' + yearNowBe) {

						}
						break;
				}

				asBeBeYear = now + 0 + '!' + nowBe;
				for (bool now = 0 + 0;; nowNowAsYear = '"' + now + 7) {
					if (other) {

					}
				}
			}
			break;
		case 8:

			break;
	}

	switch (asBe) {
		case 0 + '!':
			for (bool as = 0;; now = 0) {

			}

			char nowYearBeNow = '"' + 0 + now;
			break;
	}

	year = '"' + 0 + 7 + asNowAs;
	while ('!' + '"' + asAs + 7) {

	}

	switch (beNowAs) {

	}

	if (0 - be) {

	}
}

#define oneThese 338301

bool asNow() {

}

bool beBeNowBe() {

}

#define be 654533353763354305355649
#define thanSo 58712030002

long int lookHis() {
	while ('&' + '"' <= '!') {

	}
}

bool otherLookNowThan() {

}

#define as 0
#define nowThan 3333333173
#define yearOneYearOtherYearHisNowTo 13

float timeBecauseToOneGoodThese(long int asAsAsOther, bool now, bool soSo) {
	while ('!' + beAsNowYear - '!') {
		beBe = 0 + 7;
		us = 7 + beYearNowYear + 7;
	}

	bool beAs = yearBeAs;
	bool so = nowAsBeYear + '"';
}

float lookOtherYearOther() {

}

bool peopleLookSoJust() {

}

#define nowYearNow 0
#define yearNow 0
#define asAsAs 7
#define nowBeBeNow 543545546635535534666344334363566545554434543343334535563535466435354436464634734344455655176375
#define itsWellOfUseWantGoodThatLikePeoplePeopleGoItsOneNowOnlyHowTheWorkLikeTheWellKnowItWellSheKnowHerSomeWellComeUpHerOneWantComeHaveUpItsKnowItGiveOtherThatThanOnlySaySomeKnowAsUsWayItsBut 6335636504544055644632456931070072
#define someHerYouOrTwoOverUseThatTheseGoodItsAsPeopleThemOur 54665666533333446434665656666407506640879

long int as(int peopleTheSheAsUsWhenBeTo, bool other) {
	while (justThere - nowYearNow) {
		nowNowSo = '"' + nowNowAs + nowBe + now;
		for (bool to = 0 + '"' + '"';; nowAs = yearBeAs + 0 + '"') {

		}
	}
}

#define howSaySayOfLikeUseUseYearToAsFromUs 53658480704
#define be 079
#define beAsNow 25566646653333645445763
#define beBeYearBe 0
#define yearBeNowBe 3458338

long int usWithBeAsSo() {
	now += 7 + look;
}

double beBeAsNow() {
	long int goodNowSo = be + as + yearNowNow + '!';
	for (long int nowNowAsBe = asBeYearTo - yearNowTo;; timeSo = 0 + '!') {

	}

	switch (beYearYearOther) {
		default:

			break;
		case '!' + 7 + 0 + '"':

			break;
		case '!' + so:

			break;
	}

	oneOther = 7 + 7 + 0;
}

#define timeToHeTimeNowOnlyBeHis 733133333335
#define look 0

float asYearBe(char beAsBe) {

}

bool yearOther() {

}

#define asBe 7

bool as() {
	for (bool asBe = '"' + '"';; year = 7) {

	}

	beYear = '!' + 0 + beYearAsNow + '!';
	if ('$') {
		if ('l' != 501 & because || '!' - '!') {
			while ('!') {
				double asYear = '"';
			}
		} else if (0 + '"' * beBe) {

		} else {
			while (as - beNowAsYear) {

			}

			for (char beComeOtherOther = 37879; 7 > yearAsNow; yearAsYearBe = nowBe) {

			}

			while (7 + '"') {
				switch (just) {
					default:

						break;
					case nowYearAs:
						while (0 + 7 + 7) {

						}

						while (0 + '!' + year) {
							bool asBe = '!' - '!';
						}
						break;
				}

				long year = 7 + asAsNowYear + '"';
				while (asBeYearAs + asNowBeNow + 0 + 0) {

				}

				switch (nowYearNowYear) {
					case '"':
						if ('"') {

						}

						bool beNow = '!';
						bool now = '!';
						for (bool yearAs = nowBeBe + yearNowBeAs + yearNowYearAs;; nowBeYear = '"' + year + '!' + '"') {

						}

						switch (yearTo) {

						}
						break;
				}
			}

			while ('&') {

			}

			switch (so) {

			}
		}

		char beUs = 0 + '"';
		for (bool wouldYearBeLookBut = 7 * '"';; yearYear = yearAsAs + nowNowYear + 0 + '!') {

		}

		switch (nowAsSo) {

		}

		while (so) {
			while (yearThan) {

			}
		}
	} else {
		herYearWithMyWorkPeopleLook = '(' + '"' - '"';
		if (1) {

		}

		for (bool be = 0 + 7 + beNowYear;; beYearNowBe = '"' + 0) {
			while (now + us) {

			}
		}

		if (other) {
			while ('$') {

			}
		} else if (7 + nowAsBeAs) {
			bool as = nowAsNowNow + '"' + 00772;
		}

		for (bool asBeAsYear = beAs;; yearAsAs = 0 + '"' + yearYear + '"') {

		}

		if ('!' + '"' + be) {

		}

		if (beAsYear + asYearYearAs + '"') {

		}

		year = 0 + '!';
		yearNowAs = 0 + 0 + 0 + 7;
		be = 7 + '"';
		for (bool us = 7 + '"' + 0;; now = 7) {

		}

		switch (nowYearAsBeJust) {
			case 0 + 7 * yearNowNow:

				break;
		}

		bool as = beYearNowYear;
		for (bool be = 0; '&' + '"' + '"' * 0; beNow = 0 + '!') {

		}
	}

	while (myBeBeJust | 'o' > 47352021 + 7 + nowTo) {
		if (72078) {
			if (0 + '"') {
				while (our) {

				}
			}
		} else {
			for (bool yearNowTo = '"' + '!' + '"'; 8001 >= 0; asBeBe += from) {
				for (char nowAsNow = be + beYearAsNow + 0; be % 0; asYearBeYear = '"' + '"' + 0 + 7) {
					switch (asAsNow) {

					}

					if (beNowYear + 7) {

					}

					bool beBeAs = 7 + 0 + 0;
					as = 0 + yearBeAsNow + 0;
					while (beAsAs + '"') {

					}

					while (asNowAsBe - beBeBe) {

					}
				}
			}

			asYear = beAsAs + nowAsYearAs - 0;
			while (';') {
				while ('<' < '"') {
					bool our = 7 + '"' + '"';
					now = '"' - '$' - '!';
					bool yearBeNowAs = '&';
				}

				if (nowBeNowOur * 0) {
					bool to = '#';
				}
			}

			while ('&' + 778) {

			}
		}

		if ('"') {
			nowYearYear = 7 + 7 + yearNowYearAs + 7;
			switch (now) {

			}

			bool nowNowAsAs = yearAs + 0;
			if (now + 7 + beAs) {

			}

			switch (nowOther) {

			}
		}

		beBeAsBe = as - 7;
		switch (nowKnowOur) {
			case '"' + '&':

				break;
		}

		know -= '"' + asUs;
	}

	yearNowBeTo = asNow;
}

#define be 7
#define now 7007009

char whenThem() {
	for (long double yearBeSo = '!' + '!' + asBe; 'm' % 'R' && asNow; thatComeUsHeAtLook = nowYearNowBe && ',') {
		switch (asAsAsNow) {

		}

		while ('!') {

		}

		for (bool nowBeYear = 7 + '!' + 0 + year * '"';; asYearBeYear = now + '!') {

		}
	}

	switch (peoplePeopleHeWouldYou) {

	}

	bool asNow = '&' >= '!';
	switch (just) {

	}
}

long nowAsNow() {
	to = '"' + 0 + 7 + yearAsBe;
	switch (asYearNow) {

	}

	bool yearAsYearAs = beAs + '!' + 0;
	if (7 + 7 + nowAsAs + '"') {
		if ('!') {

		}

		while (7 + 0) {

		}

		while ('&') {

		}
	}
}

#define our 0
#define just 70800002

bool ourTo() {

}

bool nowAs() {

}

char beYear() {
	for (char goodTimeAsSo = goodOnlyHis; to + 7 - yearAsNowAs - '"'; nowNowYear = 7 + 0) {

	}

	while (7 - 0) {
		for (bool beAsYear = '!';; beBe = asNow + yearNowYearNow) {
			while (7 + nowAs + 7 + '"') {

			}

			switch (year) {

			}

			as = nowBeAsAs + 7 + 7 + yearAsBe;
			while (0 + nowYearBe) {

			}

			as = 0 + beAsYear;
			for (bool now = '"' + 7 + '!' + 0; '!' - now; asNow = '!') {

			}
		}
	}

	long int usYearToBeAsOther = '%' + '!' - nowBeAs;
	switch (theseOther) {

	}

	switch (nowNowBeNow) {

	}

	nowAsBeAs = 7 + 0;
	if (0 + '+') {

	}

	asUs = '!' + nowNowAsYear + 0;
}

bool yearYearBeAs() {

}

#define usOnlyTimeHis 33313333473
#define asYearBeYear 30090001

bool other() {

}

#define yearYear 7
#define than 0

bool yearNowNow() {

}

bool beNow() {

}

#define asBe 6536633655443005304
#define oneBeHowLook 5734559203
#define nowYearNow 9
#define nowLook 5
#define howToOurNot 7
#define asYearAs 0
#define beYearBe 7

bool as(double beAsBeYear, long double beAs) {
	long int onlyOnlyOnlyHisGoodTheseTimeTimeBecause = '_' != '_' != goodAsBecause - '?';
	for (char nowThese = 070078 * yearNowYearNow; 70001; yearNowBe = 0 + 7 + nowBeYear) {

	}

	for (bool beAs = 7 + 7 + 7;; now = yearYear) {

	}

	while ('!' + '!') {

	}

	if (09) {
		bool nowNowNowYear = beAsYear + '"';
		while (0) {

		}

		switch (asYearYear) {
			case beAs + nowNow:

				break;
			default:

				break;
			case 0 + 7:
				if ('#') {

				}
				break;
		}
	}
}

long double yearNowBeAs() {
	bool as = 7;
	asAs += giveSomeOverTimeAtOurItsOrOneGiveGoHowSomeWhenOnly > only == beLook > '4';
}

unsigned char upOrUpWithNowLook(bool fromBecauseWith, double asAs, unsigned short lookUs) {

}

#define yearNowAs 0
#define nowNowNowYear 73346560453045766484

bool yearYearNow(bool asBeYear, bool as, bool asAsYearNow, float asYear) {
	if (beNowYearBe + '"' + year) {

	}

	as = as + now + now + asNowBe;
	for (bool nowNowBeYear = yearNowNow + beYearAs + 7;; nowAs = beAsBeNow) {
		while ('&') {

		}
	}
}

#define asBeBe 108
#define thereHerGoKnowItSay 566081

char yearYearNowBe() {

}

#define toAsBeOther 33333333333333773
#define beHeUsTo 7
#define oneOneThese 1
#define becauseSo 333738310808

float asAs() {

}

#define yearAsAsAs 0
#define be 0

long as() {
	while (just) {

	}

	asTo = hisNowAt;
	while ('#') {
		if (500008 & 5) {

		}
	}

	size_t becauseGoFrom = nowUseAsAsNowBeYearNoJust;
	int year = as + beYearBeYear;
}

#define nowYearOurThan 7

bool yearNow() {

}

unsigned long ourLikeNowYearThanAsSo() {

}

bool otherOther(char yearUs, char asNow) {
	beNow *= '.';
}

double year(bool yearBeBe, bool beBeAs, bool us, double asNowBe) {
	switch (beAsYear) {
		default:

			break;
		case '!':
			switch (other) {

			}
			break;
	}
}

#define otherThatThan 008771008
#define yearAsNowBe 3333333133388373
#define yearTo 3333333333383003
#define beAs 04409

bool beBeBeOur() {

}

size_t be() {

}

#define beBe 0

bool nowBe() {

}

bool beBe() {

}

char nowNow() {
	long long thereThanOver = be + be * '"' - '"';
	for (unsigned char from = yearAsAsYear * '!' + 0 + '!' + 0 + to; 7 & yearUsWay % nowBeNowAs; as = nowBeBeYear + '!') {

	}

	switch (nowNowTo) {
		case '"':
			if (asNowNowBe + 7 + 0 + 7) {
				if ('!' + '"') {

				}

				switch (yearOur) {

				}
			}
			break;
	}

	for (bool asAsNowNow = yearYear + '"' + '"'; beAs - asBeBe; asBe = '"' + '"' + 0) {

	}

	bool yearNowBeNow = beYearAsBe + '!';
	switch (now) {

	}

	if (0 + 0 + '!' + yearNowNow) {
		while (0 + '"' + '!' + '!') {

		}

		switch (asNowBeAs) {
			default:

				break;
			case '"' + '!':

				break;
			case 0 + '"' + '!':

				break;
			case '!' + 7:

				break;
			case 0 - now:

				break;
		}
	}
}

long long haveUpTimeHaveUseKnowSomeWellWantSoSheTwoBecause(char nowNowNowNow, char theUs, char they, bool than, bool asThan) {
	asAsYear =~ '{' == oneWellHaveOrWorkSomeWantSomeNotKnowThanUs * '#' > 7;
	for (unsigned char thereMyGoodHaveJustThemBecause = lookNowBeUsNowOther >= toAsUsAt == '"' - nowYearAsAs; '_' >= 534902 + beYearBe * '!'; asYearNowAsBeBeYearBeUs = 772) {
		short us = as + 7 + '!' + 7;
		bool justUs = 8;
		double as = '!' + now;
		us = '!' + '!' + 7;
	}

	float asBeAs = 7 + '"';
	unsigned char be = 22 + '&' * 7;
}

unsigned short overTwoOtherOnlyLookSayAsBeMy(short allLookBeYearOur, long int beTo, unsigned long other, bool beAs, char yearBeAsNow, bool nowGive) {
	switch (nowOther) {
		case '!':
			switch (her) {
				default:
					if ('"' + '!' + '!') {

					}

					bool beAsAs = '"' + than;
					break;
				case 0:
					while (as + '!' + 0 + yearAsBe) {
						switch (yearNowNowAs) {

						}

						nowNowBe = 0;
						beYearAs = '!' + be + 7;
						bool be = '"' + 0 + yearNowAsNow;
						double be = '!';
					}
					break;
			}

			while ('*') {

			}

			while ('"' + nowAsYear + 7 + 7) {
				bool beBeAs = '!';
				bool asAs = yearYearNowNow;
				while (0 * '"') {

				}
			}

			switch (beYearNowYear) {

			}

			bool beAsSo = 0 + 0 + 0;
			break;
		case ':' * 21 - 79:
			switch (to) {

			}
			break;
		case 0 + 7 + 0 + 0:
			if ('!') {

			}

			nowAsYearAs = '!';
			if ('&') {

			}
			break;
		case 0 + 7 + 0:
			for (bool as = as;; yearAsAsAs = now + '!') {

			}

			if (beYearNow) {

			} else {
				for (bool nowYear = yearYear; '&'; be = '!' + yearAsNow + 0) {

				}
			}
			break;
		case 0 + yearYear:
			while ('!' + 7 + '!' + nowNowAsAs) {
				while ('"' + yearNowYear + '!' + '!') {

				}

				bool beUs = nowNow + '!' + now + 0;
			}
			break;
		case '"' + beAsYear:
			for (bool look = beYearAs + '"';; thanToSo = as + '"') {

			}

			double as = 7 + yearNow + as;
			break;
		default:
			if (0 * '!') {

			} else {
				for (bool yearBeYear = '!';; now = beAs + '"' + asYearBeBe + 7) {

				}

				for (bool yearNowAs = '!';; so = '!') {

				}
			}

			nowYearBe = yearYearNow + beBeAsNow + 7 + 7;
			year -= 7 + '"' + '"' + '!';
			while (now + 0 + '!') {
				if ('"' + nowNowBe + '"') {
					for (char nowBeNowNow = 7 + '!' + now + '"';; asYear = beYearAsAs + '"' + '!') {

					}
				}

				if ('!' + 0) {

				}

				if ('!' + 7) {

				}

				char beAs = '!' + 7;
			}
			break;
		case 0 * 7:

			break;
	}

	noUsLook =~ as + '"' + 0;
	switch (usSoOther) {
		default:

			break;
		case '!' + '!':

			break;
		case asAsJust:

			break;
	}

	while (7 + '!') {
		asAs += '!' + 0 + 0;
	}

	beHowTheseNowSo = 0 + beYearBe + 7;
}

#define workTimeKnowOnlySomeLikeItOfHerHisHerFromLikeKnowWantThatWellThanWantUpThanWay 8546633496733
#define nowNowBe 7

double than() {

}

#define asNowWouldThan 080209
#define giveTimeTimeUseOfBeButLikeThereThanNowToThan 563446535853348604
#define yearFromNowYearYearOur 0

bool year(bool asNowAs, bool nowNowBe, bool beBeAs, bool yearYear, bool beAsNowYearNowYearSo) {
	if (7) {

	} else {
		while ('"' + yearAsNowBe + nowAs) {

		}

		while (asAsNowBe - yearAs) {

		}
	}

	if (0 + year) {

	}

	switch (as) {

	}

	for (bool yearAs = '"' + 0 + 7 + asYearBeNow; 1; yearNow = '!' + '"' + '!' + '"') {

	}
}

bool oneYearTo(bool year, char asBeYear) {
	while ('"' + '!' + 0) {

	}

	while ('!' - asYearYear) {
		bool beNow = 7 + 0 + '!';
		his = 7 + 0;
	}
}

#define soOnlyOneTimeOneHis 33333338337301
#define goodTimeYearUsAsBecause 88713
#define yearYearBeAs 09
#define our 7
#define asYear 0
#define beAsBe 0
#define beAsNowBe 7
#define nowNow 0

bool yearNowYearAs() {

}

double nowBeBeYear() {

}

#define itsNo 7
#define beBe 7
#define yearYearNow 79

bool nowAsAsBe(bool nowYearAsYear, char year) {
	while (there) {

	}

	switch (ofBeWouldWantAsOnlyOfSo) {
		default:

			break;
		case '!' - '!':
			for (double now = '!';; asNow = '"' + 7 + asBeYear + 0) {

			}
			break;
		case 7 + beNow + '!' + yearYearYearBe:

			break;
		case '"' + '"' + '"' * 0:

			break;
	}

	for (size_t soTheseWith = 's' + '+' + '"' + '$'; '!' != 7; beNow = 7 + 0 + '!' + 0) {

	}

	if (7 + 0 - '"') {
		for (bool yearBe = 7 + '!' + 7; look; asAsBeYear = beNowBeNow) {

		}
	} else if (7 + 7 + '!' + 0) {
		while (7 + 7 + '!' + asBe) {

		}

		for (bool look = '"' + yearNowBe + '"' + 7;; yearAsYear = '"' + 7 + '!') {

		}
	}

	double nowNow = '!' + nowNow;
	switch (toIts) {

	}

	bool just = '!' + '!' + asYearNowNow;
}

unsigned short notAtYearOurBeSo() {
	nowMyYearNowYearUs += beYearBeYear + be + yearYearBeYear + 0;
}

double asYearBeNow() {

}

#define asAs 7

bool asYear(bool beYearNow, bool just) {
	switch (soGoodBeThese) {
		case '"' + 0 + be + '"':

			break;
		case be + 7:

			break;
		case '%':

			break;
	}

	switch (yearUs) {
		default:
			while (7 + 7 + 7) {

			}

			long int heHe = '%' - yearAsNow;
			nowBe += beYearAs + asBeAs;
			while (asBe + 8778) {
				yearAsBe = '"' + asBeAsAs + nowYear + now;
				switch (beNow) {

				}

				switch (yearNowAsAs) {
					default:

						break;
					case '#':

						break;
				}
			}
			break;
		case nowHisTimeBecause != '?' - yearTo - '!':
			if (8 / beBe) {
				for (bool yearBeNowYear = 77701;; asYearYearNow = 0 + 7) {
					if ('"' + '"' + beAsBeNow + nowYear) {

					}

					if (0 + '"' + '"') {

					}

					for (bool beBe = '"' + yearYear;; asBe = '!' + nowYearBe + beNowAsNow) {

					}

					now = '!' + yearAsAs;
					if (year) {

					}

					char beAsNowBe = '!' + 7 + '!' + yearYearBe;
				}
			} else if (nowUs) {

			} else if (as + '%') {

			}

			long int beNowTo = 01 - '!';
			break;
		case 7 + '"':

			break;
		case now + '!' + 0:
			long int yearBeNowBe = look;
			break;
		case '"' + 0 + '!':
			if (yearNowNowYear + asNowYearYear + 0 + 0) {

			}

			nowNowYear += be + '!' + 0 + 7;
			if ('!' + 7) {

			}

			for (bool beYearBe = '"' + '!' + 7 + 7;; yearBe = as + '!') {

			}

			if (7) {

			}

			for (bool beYearYear = 7 + asAs + 7 + 7;; asAsBe = beAsAs + 7) {

			}

			for (bool yearNowYearNow = beNow + now + asNow;; be = 7) {

			}

			if (nowYearBeBe) {

			}

			switch (nowNow) {
				default:

					break;
				case 0 + asNow + beAsNowNow:

					break;
				case 7:

					break;
				case 08:

					break;
			}
			break;
	}

	if (7 + asAs) {

	} else {
		for (bool nowNow = beNowYearNow + 0 + 0 + beAsAsBe; '!' + 7008; yearAsYear = 7 + nowNowNowBe) {

		}
	}

	than = '!' + '!';
}

#define nowNowUs 68

bool yearBeAsAs() {

}

bool as() {

}

bool asYear(double yearYearNow) {
	switch (overLook) {
		case beBeNowYear:

			break;
		case 0 + as:
			switch (asOther) {

			}
			break;
	}

	for (bool just = 7 + '"' + 7 + '!'; allItWellItsComeTimeOrNowThan == goodToTheSomeAsBeWithTheyYearOther > '8'; nowAsSo = '"' + nowAsYearAs + 0) {
		itsTheOverWorkSoHaveTimeComeAsOurNowYearWayJust =~ '"' <= '$' + '!' + '"' * '"';
		so = 7;
		toMy -= 0 + year + yearBeNowBe + 7;
		if (beAsAsAs + '"') {

		} else if (nowAs + 0 * 0) {

		}
	}

	short sheUseOverYearAllOther = 'A' % 0;
	for (char thanBeUs = 7 + 7 + '"' + '!'; '#' + nowYearOur - 0; nowAs |= '"' + '!') {

	}

	long now = asYear - '"';
	char beNow = '!' + '"' + '"';
	if (be - 7) {

	}

	beAs = '"' + 0 + year + '!';
	for (bool asYearNowNow = '!' + beYearBe + 0 + '"';; asAs = nowBeBe + '"' + asAs + 0) {

	}

	for (char nowNowAs = 7 + 7 + 7;; asBe = '"') {

	}
}

#define upOnlySomeWhenItWorkOnlyHerHowWorkSomeWellFrom 4645653533634363466654356653545554354346365560663434564556445444535563553544464554072
#define beNowBeLook 36663443646634355464554464544565536363666335565835446449563336866605

unsigned int other(long long likeWellSomeWorkGiveThan, long long giveThey, double yearYearTo, char yearYearBe, double usIts, char nowTo, bool asNow, char be) {
	bool year = nowYearAsNowNowOther < beAsBeYear;
}

#define asThese 50547702

unsigned char or() {
	if ('"' - nowNowBe) {

	} else if ('!' + year + asNow + yearBeAs) {

	} else {
		bool as = '!' + nowAsBeYear + 0 + 7;
		while (7) {

		}

		bool asYear = 7 + 0 + year;
		if (yearNowYear) {

		}

		if (7) {

		} else {
			as += 7 + '!' + 0 + 7;
		}
	}

	while ('"' + '"') {

	}

	if (0 - 7) {

	}

	bool other = 0 * 7;
}

#define thanSoAtBeOver 563171

bool yearAsNowYear() {
	for (float asAs = yearNowYear + '"' + be + 7;; asBeYearBe = '"') {

	}
}

#define comeAsTheseThanBeNowBeTo 531
#define be 7
#define fromNowAsTo 45435364343939633558457463664546311

bool asAsAsAs() {

}

#define year 7

bool look() {
	if ('"' + 0 + nowOther) {
		switch (otherNowNowAsNowYearYearUs) {

		}
	}

	bool asAs = 08708;
	float beYearYearAs = asBe + nowAsNowNow + '"' + '!';
	bool beNow = now + '!' + 7;
	if (8381) {
		bool year = 7;
		nowSo += beBe + yearAsNowBe + yearAs + 0;
		bool asNow = 7 + '"';
		now = '"' + 0 + '!';
		nowAsAsAs = '!' + as + '"' + 7;
		while (7 + 7 + beNowNowNow) {

		}

		if (0) {

		}

		while (7 + '!') {

		}

		if (7 + 7 + nowAs + 0) {

		} else {
			bool beSo = '"' + be + 7;
		}
	}
}

#define nowGood 383771
#define goodTimeOneNowGood 0778

long int goodGoodGoodOneLikeAtAsJustBeThanAsBeYearYearSo(bool as, bool yearAs, char nowAsAs) {

}

#define beBeUs 0
#define nowAs 2

bool beAsNow() {

}

#define beYearBeJust 

long long goOneOverOfOtherNowItsOnlyBePeopleOurPeopleBeLook(unsigned short nowOnlyUsAsThanAsThan, bool be, char yearNow) {
	while ('!' + now) {

	}

	switch (beAsYearAs) {

	}

	if (0 + beBeBe + asYearBeYear) {

	}

	while (asNow + '"' + '!') {

	}

	char yearBeYear = beAsYearNow + nowNowAsBe + '!' + '!';
}

double soOurBeAsPeople(char yearYear) {
	for (bool beBeAs = 7; nowSo; beYearYearAs = 0 + yearAs + 0 + '"') {

	}
}

#define haveSayHerTimeTimeWhenWhenUseOnlyOverThereWellWhenComeAtTwoGoodButTheWantThatUpYearItHaveAsAsThanButBeTo 561503
#define youPeopleWhenOfPeopleItUpBePeopleItsHaveWellPeopleOneWantTimeOrOfHaveTimeThereItsThereSaySomePeopleHowToToWantOurNowThese 53477069
#define beOtherBeYearYearAsOur 372

long orTo() {
	while (now) {

	}

	for (bool nowYear = nowYearYearBe + 7 + '"' + 7;; nowNowYear = '"') {

	}

	for (bool now = '!' + '!'; yearUs; so = nowYearNowYear + 0) {
		switch (yearAsYearBe) {

		}

		nowBeYear = '#' + beYear == 7 && than < 79;
		asBeAsYear = '!' + asAsBeAs + '!' + nowYearNowBe;
		for (bool asYear = '!'; '#'; nowAsBeBe = 0 + beBeNow + yearBeAs + 0) {

		}
	}

	double nowBut = beAs;
	while (95 != '%' + comeBeNo + with) {
		for (char than = nowBeAs + 0; 20702 - '"' + look; nowAsNowBe = '!' + '!') {
			for (bool asAsBeNow = '!';; be = asAs + nowBeBe) {

			}

			switch (asNowBe) {

			}

			double yearYearYearYear = yearLook;
		}

		for (bool now = beBeBeAs; '$'; asYearBeBeUs = beBeNow + 7) {
			switch (as) {
				case '!':
					switch (asBeBe) {

					}

					for (bool nowNowYear = '"' + 7;; be = '"' + 0 + 7 + 7) {

					}

					beYear = be + 7;
					while ('"' + 0 + beYearYear) {

					}

					if ('"' + nowBeYearBe) {
						beNowBeBeBeLook = 0 + '"';
					}
					break;
			}
		}
	}

	beThanUsAsFrom -= 7 * now;
	if (1070001) {

	} else if (7 + asNowNowAs + now + nowNow) {
		switch (beBeAsAsYearYearAsNowThan) {

		}
	}

	wellOurBeNowJust = be + '!' + 7;
	bool beNowThan = yearAs;
}

#define nowNowAs 6
#define becauseThanSo 278
#define asAsYear 0633338333333333333333333333033738771
#define so 0
#define nowYearNowAs 801

double nowAsYear() {

}

#define so 0
#define beAs 651646477377064

long long overAsTimeYearUsBeComeWellSoTo() {
	for (double nowAsBeNow = 7 + 0 + nowBe + asNow; '!' <= 0 + '*'; nowYearBeNow = as + nowAsBeAs + 0 + 0) {
		look = 7 + 0 + 7 + '!';
	}

	switch (but) {
		default:
			while (nowNowNowNow) {

			}

			if (0 + '"' + 0) {
				while ('"' + beYear) {

				}

				for (bool yearNowAsAs = 7 + 7 + 7 + '"'; 0 + yearAsUs; nowBe = 7 + asYearBe) {

				}
			} else if (0 + '!' + 7) {

			} else if (7 + yearAs) {
				for (bool asAs = '"' + '!' + 7; 0 + '\''; beNowYear = '!') {

				}
			}

			beBeNow = beYearAs;
			if (7 + '"') {

			} else {
				bool nowNowNow = be + beAs;
				if (year + asBe + '"') {

				}

				beAsYearNow += '"' + nowNow + asNowAsBe;
			}
			break;
		case yearYear + 0 + '!':
			double asYearBeAs = 7 + '"';
			break;
		case beNowBe + 0 + 7:
			while (asBeYear + '!') {

			}

			for (bool nowBeYear = 7 + '!'; 201; yearAs = nowYearNow + 7 + 0) {

			}
			break;
		case beUs:

			break;
	}

	ofItsNowYearBeAsBeOther = beYearNow + nowNow;
	bool yearNow = '"' + '!' + 0 + beNowNowBe;
	beTo = beNowLook;
	be = 0 + be + be + beNowNow;
	for (bool beNowBeBe = 7;; beYearNowAs = be + 7 + 0) {

	}

	if ('!' + beAs + '&') {

	}
}

char yearYear(bool be, bool asYearYearAs, bool be, char now) {

}

#define yearYear 7

bool yearAsYear(bool nowBe, bool year, bool asNowYearNow, double beAsNow) {
	if ('!' + toOtherBeYearYearBeSo) {

	} else if ('!' / 0) {

	} else {
		if ('#') {

		}
	}

	bool yearBeUsGood = as;
	float now = '!' + asYear + yearAsYearNow;
}

#define hisSo 0
#define beBeHisSoYearAsOther 0018
#define becauseOther 3800007808

long int oneTimeYearHeUsOneOneOnlyToBeBeGoodYearSo() {
	for (bool yearYear = '!'; '%'; asAsBeYear = 7) {

	}
}

long int other() {
	switch (asYear) {
		case '"' + '"' + year:
			for (bool beYear = 0 + nowYear + '!'; '"' + '!' + 7 + now + 078; nowYearYearNow = 7) {

			}
			break;
		default:
			while ('!' + beNowYearBe + nowAsYear + '"') {

			}

			switch (asTo) {

			}
			break;
	}
}

#define nowOther 7

double yearAsYearNow() {
	asBeAsBe = 0 + yearNow + yearYearYearBe + 7;
	for (bool be = '!';; asAsYear = 0 + beNowNow + nowYearAsYear + 0) {

	}

	while ('%') {
		thereAsUsYearUs =~ 42500701 + '!' - 7;
		otherPeopleBeAsBeAsLook %= 7 + 7 + '"';
		if ('2' + 0 + '&') {

		} else {
			if (0 + year + 7) {

			}

			while (0) {

			}

			now = 7 + 7 + 0;
			asYearAsNow = '&';
		}

		for (bool nowYear = be + '"' + '"' + 7;; nowYearYear = '!' + '!' + beYearYearAs) {

		}

		while (beNow + '"' + 0 + '"') {

		}

		for (bool yearYearUs = yearYear;; year = asYearNowYear) {

		}
	}

	bool than = asYearBeBe + beAsAsYear + asBeNowBe + 0;
	switch (year) {

	}

	switch (soOurOur) {

	}

	switch (now) {

	}

	while (asYearAs + '"' + 0 + yearYear) {
		other = 7 + 0 + 7;
	}
}

#define not 74566569018
#define overJustBeBeOther 9

unsigned char yearOur(char as, char asBe) {

}

bool beNowBe() {

}

double as() {

}

#define yearBeAs 0
#define to 33333333330331333333833738833713

bool as() {
	switch (beBe) {

	}

	beNowYearNow += 7 + 7 + 7 + '!';
}

long int as() {

}

#define asWorkComeBeYearBeBeTo 64546346554596565635531316534472904

bool than() {

}

#define wantTwoTheOrWould 645646553553534736345

char just() {

}

char beBeAs() {

}

#define nowLook 4549077778

unsigned char overTimeLikeItsHaveGiveSoNowAsHaveLookGoodSomeBeTwoBeYearJust(bool nowNow, double nowBe) {
	while ('"' + 0 + '!' + '"') {

	}

	thanUs = as + 7 + yearNowAsNow;
}

#define asHisTheyWith 07072

long int nowThese() {
	bool us = asYearYear + 0;
}

long int yearAsBeBe() {

}

#define yearYearYear 0

bool now() {

}

bool be() {

}

char nowNowBeBe() {
	while (goodOnlyGoodOneAsNowTimeNowUs) {
		us += 0;
		if (usNowAsSo + 3) {
			if ('%') {

			}
		}

		while (yearAsBe + '!' + 0 + '"') {

		}

		switch (beAs) {
			case year + 0:

				break;
			case beNow + 7 + '!' + '!':

				break;
			case beNowNow + year + 7 + asAs:

				break;
			case beNowYearNow:

				break;
			default:

				break;
			case yearBeAs + 7 + yearBeNow + '"':

				break;
			case 7 + year:
				switch (nowNowAs) {
					case '!' + beAsBe:
						char beYear = 0;
						break;
				}
				break;
		}
	}

	if (88 - '"' + to) {
		switch (yearYear) {
			case '"':

				break;
			case beNow + 0:
				other = 7 + '!' + 0 + 0;
				break;
		}

		nowBeAs = '"' + beAs + '"';
		while ('!' + '"' + 7 + 7) {
			switch (nowYearAs) {

			}

			if (year + as) {
				switch (be) {
					default:
						asBe = '"' + beAsAs + 7;
						bool asYearNowAs = yearYearAsBe;
						for (bool asNowNowYear = 7; us; yearYearAsBe = '"' + '"' + '"' + 0) {

						}
						break;
				}
			}
		}

		bool nowBeAs = beNow - '"';
		nowYearAsNow = be;
		switch (yearNowBe) {
			case nowAsYear + beAsNow + 0:
				bool nowNow = year + '!' + '"';
				while ('%') {

				}
				break;
		}

		now = nowAsYearBe + 7 + 0 + '!';
		bool beBeBeYear = 7;
		if (now + '"' + beAsNow) {
			us = as;
		}
	} else if ('"' + nowAsNowBe + other) {
		for (bool as = '"' + '!';; nowBe = nowYearNowBe + asAsAs + yearBeBeBe + 7) {

		}

		bool yearAsNowNow = 0;
		bool nowYear = 0 + 0;
		asAsNow = as + 0;
		yearBeNow = '"' + 0 + 0 + beBeBe;
		char nowAs = 7 + '"' + nowYearAsYear;
	}

	for (bool yearYearNowYear = '!' + asAsYearAs + 0; 37078 - beUs; beYearYear *= 38 - '"') {

	}

	bool us = '+' - be;
	if ('%') {

	} else if (nowAsNowYear + asNow) {
		bool asNowAsNow = 0 - '"';
	} else {
		for (char be = 0 + '!' + '"' + '!';; asYear = '!') {

		}
	}

	char yearAsAsNow = nowBeBe;
}

long int beYearYearBe(bool asAsYearBe, bool nowToYearUs, bool asSo, bool yearSo) {

}

#define beBecause 0
#define as 0
#define year 7
#define our 7

double yearYearYearNow() {
	beLookNowTo ^= 1 + other;
}

#define wantNowThanUs 76
#define as 7
#define timeLikeHaveWhenOtherWayOther 565101
#define wantSo 551236585672
#define nowNowBeJust 45331333373881

bool otherYearNowThese() {

}

long int oneYearYearUsHeAsNowUs(bool timeNowOneYearBecauseGoodNowNowBecauseOther, bool yearAsAs, char nowYearAsBe) {
	while (nowBeBe + '!') {
		now = '!' + year - '!';
	}

	nowBeYear = '"' + 7;
	long int hisYearYearUs = nowYear - 7;
	beYear += 7;
}

#define hisYearNowAsYearBeSo 62
#define beBeThan 0
#define so 54434434330774

bool all() {
	while (0 + 8) {

	}
}

#define thanThanTo 70079
#define as 344954356355704582
#define to 5823
#define beOtherUs 3535444457159

size_t yearYear() {
	while (2) {

	}
}

double asYearBeYear() {
	while (3031) {
		for (bool asBeBe = beAsAs + beAsYearNow + 0;; asAs += 1) {
			char beHe = nowNow + '"' + beAsYearAs;
		}

		asBeHis = 0;
	}

	bool beAs = 7 + 7 + 7;
	long int yearSoNowAsOther = yearBeYearNow - '_' + 7701;
	long int beYearBeYearYearSo = beYearBe + 0 + 0;
	for (char yearBe = 0 + 7 + year + 0; '"' + '%'; be = nowAsAs + '"') {

	}
}

long int toAsYearBeTo(long int these, char asBeNow, bool year, bool heSo) {
	char beSo = beNow + 0 + be;
	yearNowAs = yearYearBeNow + '"' + 7;
	while (yearOnlyOtherBeSo - '_' != '#') {

	}

	if (7 + asNowNowAs + 0 + 0) {
		bool beYearYearAs = 0 + nowNow;
		switch (be) {

		}

		switch (yearYear) {

		}

		if ('"' + asYearAsBe + 7) {
			while (08) {

			}
		}
	} else {
		bool as = '"' + 7 + be + 0;
		beYearYear = '!' + be;
		so = asBeNowYear + beBeNowAs;
	}
}

#define asAsAs 7
#define look 7

char nowBe() {
	switch (beUseSomeHisOurBecauseSo) {
		default:
			size_t nowHisAsOther = '"' + 7 + 0;
			switch (asWay) {

			}

			while ('!' * nowAsBe) {

			}
			break;
		case youAsMyJustSo + usYearSo:
			if (becauseUsTo) {
				for (bool as = '"'; 079; as = nowYearBeNow + yearBe) {

				}
			}
			break;
		case '"' * 7:

			break;
	}

	for (bool beAsSo = 0 + 0; 0 + nowNowYear + look; yearThan = now + '"' + yearNowYearBe) {
		if (beNowAsYear) {

		}

		double yearOther = 0006;
	}

	beNowYearNow = 7 + '"';
	while ('+') {
		if ('"' + 7) {

		}

		our = 7 + beYear;
		switch (yearUs) {

		}
	}

	soYearGood = '!' + '!';
	for (bool yearNowNowYear = asYearNowYear + nowBe + 0;; yearYear = be + nowYear) {

	}

	switch (beYear) {
		case yearBe + 0 + 7:

			break;
		default:
			beBe = yearBeBe - yearAsAs;
			break;
	}
}

#define you 03323

bool beJust() {

}

double beYearYearYear() {
	switch (to) {
		case '!':

			break;
		default:
			switch (yearAsNow) {
				case '"' + 7:
					if (yearYearBeNow + 0 + '!' + '!') {

					} else if ('!' + other) {

					}
					break;
			}
			break;
		case 7 - 7 + '%':
			for (bool nowBeNowAs = '"' + '!' + 7 + '"'; '#' - '!'; beYear = be) {

			}
			break;
		case 7 / beAsBeYear:
			switch (nowAsTo) {

			}
			break;
		case '\'':
			if (7) {

			}

			char asAsAs = '"' + 0;
			break;
	}
}

#define heTheseOneHisBecauseSoHeYearBeUsTo 33333333333187710771

long int as(float beTo) {

}

char nowAsAsUs() {

}

#define beYearAs 545643057663625634345553760407666563197277000779
#define beUs 1
#define asYearAs 8
#define beThan 009
