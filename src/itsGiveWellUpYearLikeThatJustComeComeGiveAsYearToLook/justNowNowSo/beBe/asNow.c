#include <2UUUU2E.h>
#include <EUUUU0020.h>

bool yearYearBeBe() {
	switch (now) {

	}

	for (bool beAs = 0 + yearYear + nowNowNowBe;; asBe = '"') {

	}

	bool nowBe = '!';
	nowYearBeBe += 0 + 7 + '!';
}

#define no 7

bool beNowNowBe() {

}

#define the 0

bool yearNowAs() {
	beAsNow -= year + beYearAsAsBeJust;
	bool at = 0 + asBeNow;
	while (these + '!' * beNowNow) {

	}

	while (beNowOur - yearYearYear) {

	}
}

long int theWithLook() {

}

#define yearOther 177702

unsigned int just() {

}

#define now 1
#define just 0
#define asNow 3161664346055646654501

long long itsJustWithThan(short beNow, bool now, double be) {
	unsigned short but = ')' < 9 * 7;
	bool asAs = '%';
	for (bool beYearAs = '!' + be;; than -= asNowYear + beAs) {
		for (bool as = now + 0;; year = '"') {

		}

		switch (toHis) {

		}

		if ('"' - 0) {

		} else if ('!' + beBeBeYear + beYear) {

		} else if ('!' + yearBe) {
			year = yearBeNowAs + '"' + yearNowYearNow + 0;
			while (8) {
				us = 7;
			}
		}

		so = 1;
		switch (beUs) {

		}
	}

	yearAs = 0 + '!';
	if (38171) {

	}

	for (bool year = nowBeNowNow + nowNow + '"' + 0;; nowNowAsBe = '"' + 0 + '"') {

	}

	while (0 + '"' + '!' + 7) {
		char asBeNow = asNowYear;
	}

	switch (other) {

	}
}

long int onlyOnlyGoodOneOneTimeYearBecauseOnlyOtherTimeTheseBeTheseNowNowUsAsTo(char year) {

}

#define nowOneGoodWouldBeNowUs 7

bool beNowNowYear() {

}

bool beYear() {

}

float year() {
	while (01) {
		double now = 7 + 0 + 0 + '"';
	}
}

unsigned short goodYouLikeWay(bool beNowYearBe, bool asYear, bool other, bool nowNowYearAs, bool nowNowYearAs, bool beAs, char asAsYear) {
	for (bool yearBe = 0 - 7;; nowBeYearNow = 0 + '!' + 7) {

	}
}

char our() {

}

bool beAs() {
	while (so) {

	}
}

char beBeNow() {

}

#define asOur 3344344544003378
#define youUseSheMyYearYearUs 365363543640645331333303
#define he 33713

long int oneTimeOtherGoodGoodBecauseBecause(bool asYear, bool he) {
	long int onlyOneSo = '"' - 7;
	unsigned int otherNowJust = '"' + 44666 | 601 % so;
	nowYearUs = '%';
	if (0 + '"' + 7 - '"') {

	} else if ('!' + 0 + '!') {

	} else if ('#') {

	}

	if (yearAsBe + nowBe + '!' + yearYear) {

	}

	for (bool asYearBe = 0; '%'; beYearNowNow = '!' + 7 + '"' + now) {

	}

	be = 0 + '!' + yearBeAs;
	while (beYear) {

	}

	so = 0;
}

#define asTo 0

bool ourTo(bool yearYear, bool beNowThan) {
	unsigned char or = 4;
	if (7 + 71) {

	} else if (yearYearYearNow + '"') {

	} else if ('"' + yearAsNowAs + 0 + '"') {
		if (0 + '"' + '!') {

		} else {
			for (bool be = year + be + yearNow + '!';; beYearYearBe = '"' + nowYearNowAs) {
				switch (to) {

				}
			}
		}
	}

	if (asAs + '!') {

	}

	bool asAsAsOur = '"' + yearAsNow;
}

#define asAsSo 7

bool beYearYearYear() {

}

#define nowThan 0

bool beYear() {

}

bool beNowYearNow() {

}

#define be 3338545665554533665836

bool beBeYear() {

}

long int upHave(bool nowNow, bool nowAsBe, char asNowYearYear) {
	for (bool nowAs = beYearBe + '!' + asNowAs + 7;; be = asNow + 7) {

	}

	if (be + '!' + '"' + '"') {

	} else if ('"') {

	} else {
		while ('"' + '!' + yearAsYear) {
			for (char now = yearYear;; yearBeNow = '!' + 0 + 7) {

			}
		}
	}

	switch (year) {

	}

	bool as = 7 + '!' + yearNowYearBe + '!';
	for (bool nowBeYear = '!' + 0 + '"';; asAsAsAs = be + '"') {

	}

	asBeAs += asYear + '!' + '!';
}

#define sheFrom 076

char toBeNowTo() {

}

bool be() {
	for (bool nowYearAsNow = 7 + 7; 71; as = 7 + 0 + asYearBeYear + '!') {
		bool beBeNowYearOther = 0 + beAs;
		for (bool yearBe = 778 + '"' - now; yearYearYear / beAsBeYear; asYearAs = beBeBeNow) {

		}

		if (7 + 7 + yearYearAs + year) {

		}

		while ('"' - asBeYearYear) {

		}

		while ('!' + asBeAs) {
			for (bool yearNow = '"'; yearOther; nowBeBe = 0) {

			}
		}
	}

	switch (nowAs) {

	}

	long int one = '"' / 0;
	for (bool asYear = '!' - 8 + '!' - nowYear;; yearBe = '!' + nowYear + as) {

	}

	switch (so) {

	}

	for (bool asYearBeYear = 7;; beBeBe = beNowYearBe + '"' + 0 + beBe) {

	}

	while ('"' + '"' + as + '!') {
		while (7) {

		}

		if (us + 1) {

		}

		bool now = '!' + as + '"';
		if (7 + 7 + beYearNowAs + asAs) {

		}

		if ('!') {

		}

		for (char nowYearNow = '"' + nowYear + '"';; year = beYear + '"' + beAs) {

		}
	}
}

long int so(float these, bool so) {
	float us = '"' + '#';
	switch (timeOtherUs) {
		default:

			break;
		case year:

			break;
		case '"' - '!':

			break;
		case 0 + nowBeAs:
			bool asNow = '!' + 7;
			so = 7 + '"';
			break;
	}

	while (beAs - 0) {
		for (char beAs = yearNow + 7 + 7;; asNow = 7 + asNow + 0) {
			now = 0 + as + beYearAs + '!';
			switch (year) {

			}

			nowBeNowBe = 7 + asYearAsAs;
			switch (be) {

			}

			for (bool asTo = 0 + beAsYearAs + '"' + '"';; asBeYearAs = asBeNowNow) {

			}
		}
	}
}

long int timeOtherYearNowBeBeSo(bool asNowBeBe, bool so, bool asTo) {

}

unsigned char asYearNowYear(char yearNow) {

}

short beYearNow() {

}

double yearAsAs(double beAs) {
	for (double butWhenNot = 79302002 && itsTheseWay & 71 * '"' - 0; 0 + yearLook % '!'; yearNowNow = '!') {

	}

	beNowBe = beYear + 7;
	while ('%') {
		switch (yearYearNowAs) {

		}

		for (unsigned int beLook = '%' + 0 + 748; ',' + asLook; asAsYearNow = 7) {

		}

		switch (butSo) {

		}

		for (bool to = '"';; nowAsYear = '$') {

		}

		as ^= '"' + 0;
	}

	size_t asAsSo = 7 - '%';
	double other = 72;
	double us = '"' + 7 + '"';
	if (beNow - '!' + '!' + 8) {

	} else {
		while (701) {

		}

		for (double beYearAsBe = '!' + 7 + 7;; yearBe = asBeAs + yearNow + 7 + 0) {

		}
	}
}

unsigned long wantSheWorkOnlyGiveHowKnowWith(bool whenFromWouldYearYearFrom, char with) {
	if ('"' + '!' + 7) {
		for (bool yearYearAs = yearBeBeThan * '"' + '$';; asYearNowYear = '"') {

		}

		unsigned char our = asAsToTo;
	} else if (0 + asAsYear + '!' + '!') {

	} else {
		for (bool beAsAsYear = yearYearYearNow;; asNowBeAs = '"' + 7) {
			if ('"' * asAs) {

			}
		}
	}

	if (our * '!') {

	} else {
		switch (be) {
			default:
				beBe = '!' + '"';
				while (7 + beNowBeAs + 7) {

				}

				asBeYear = '"' + '"' + beNowYearBe;
				while ('"') {

				}

				while (7 + '"') {

				}

				asYearYear = 0 + yearNow + nowNowYear + 0;
				for (bool beNow = us;; beYearNowBe = 7 + beNowNow + 7) {

				}
				break;
		}
	}

	while ('$') {

	}
}

long long timeHaveWellUpButWorkSayOneSomeTimeWellOnePeopleThatSheItsTheUpWhenThereOrThatYouWantSomeUsOverOneFromPeopleSomeButToSo(unsigned long theyTwoUseItSoPeopleOtherHaveNowYearThey, float wellYearYearYearJust, bool our, bool otherYearFromYearAsJust, bool beBeNow, bool nowBeBe, bool beUsBeYearAsBeJust, char yearNowBe) {
	for (long long asNowNow = 82 * asNowBeNow; 7 + asAs + 7 + 9; asNowThanAsNowTo = 0 + nowYearNowBe + yearYear + 0) {

	}

	withSo %= '`' | yearJust - nowJustNoAsUs * '"';
	for (unsigned long usSoJust = '!' + '"' - 7;; now = 7 + 0 + 0) {

	}

	bool asJust = nowYearAsYear * '!' + 0 * '!';
}

#define asAsLook 540037770078
#define itsToThereTo 54394693
#define nowNowAs 7477051
#define beWith 9
#define us 8

long int nowAs(long int oneSo, float nowYearAsYear, long int usSo) {
	for (bool nowBeSoTo = 0 + asBeAs + '"'; '+'; yearAs = '"' + yearNow) {

	}
}

long int goodYearOnlyNowSoAsBecauseNowNowAsOther(long int us, char year) {
	yearAsNow = 0 + 0 + beYearAs;
	while (asNowBecauseBeOther) {

	}
}

#define yearBeUs 0
#define nowSo 3330701
#define good 714

bool our(bool now, double nowBeNow, bool year, bool yearYearBe, bool beLook) {
	while ('!' + yearBeYear * '!') {

	}
}

bool nowYear() {

}

#define now 1
#define yearYearNowNow 55563543546643453551142

unsigned int overWellWorkOfYearThereThereBecauseThanGoUseJust(long double asThat, long long overToTheyJustItYouJust, bool nowThey, bool as, bool at, float yearYear, bool yearYearNowAs, bool now, char yearAs, bool nowOther) {
	char herTheyAsNowYearThan = '8';
}

bool youThanNowItsBeSomeBecause() {
	switch (asYearNow) {
		default:

			break;
		case '7':

			break;
		case be - yearYearAsBe:

			break;
	}

	if ('"' % '^' == '!' | 7) {
		switch (year) {
			default:
				bool beNowBeNow = '!' + '!' + 0;
				asNow = '!';
				while ('"') {

				}

				than = nowAsBe + '!' + '!' + '!';
				break;
			case '!':
				nowAsYear = 7 + '"' + 0 + '"';
				asBeYearBe -= 7;
				break;
		}

		switch (just) {

		}

		if (0 * 0) {

		}
	} else if (as + beNowSo) {
		for (bool yearNowAsYear = asAsNowNow + '!' + asNowAsBe;; asBeBeBe = '!' + nowAsBeAs + '!' + 7) {
			for (bool be = as + 0 + 0 + as; 8; as = 7 + '"' + '"' + 0) {

			}

			while (7 + beYear + 0 + '!') {
				bool now = 7;
				if (0 + yearYearNowYear) {
					while (be + asYear + '!') {
						while (beBe + '&') {

						}
					}
				}
			}
		}

		if ('!' + '"' + '!') {

		}

		just = 7 + as + 7 + yearBeNow;
	} else if (yearYearTo) {
		if ('!' - beNowBeYear) {

		}

		year = '"';
		switch (be) {

		}

		while (7 + '"' + 7 + now) {

		}

		switch (nowAsNowAs) {

		}

		while ('!' + yearNowBe) {

		}

		bool yearBeAsOur = now + as + 0 + yearBeYearBe;
	} else if (asYearAsAs + asYearYearBe + now + 7) {
		switch (beBeAsNow) {

		}

		long double asYearNowNow = 7;
	} else if (0 + 7 + as) {

	} else {
		bool nowYearAs = '"' + 0 + '%';
		for (bool as = beYearBeBe + nowYearYearBe * '"';; nowNowAs = '"') {

		}

		be -= beBe + 0 + 0 + 0;
	}

	long double asNowNow = 7 + yearAsYearNow;
}

#define peopleUpTo 75
#define howYearOneThem 3157008
#define asAsOur 09075

bool beBeWay() {
	bool us = '_' != 330713078 / '"' + us;
	switch (nowNow) {

	}

	while ('7') {
		while (now + 7 + '"' + '"') {
			if (0 + beNowBeBe + asBe) {

			}

			yearNowBeYear = 7 + as;
			bool as = now + 0 + 0 + '!';
			yearYear = '!' + '!' + 7 + '"';
			yearBeYear += '"';
		}
	}

	beBeNowYear = 7 + 7 + 7;
	for (bool to = '!' + '"' + '"'; 708; nowYearYearBe = '!' + 7) {

	}

	while ('"' - yearNow) {
		while (to + 3) {

		}

		while (0 + '!' / '"') {
			while ('"' + '"' - 0) {

			}
		}
	}

	while (beOther) {

	}

	switch (timeUsSo) {

	}

	if (0 + now + 7 + 7) {

	}

	for (bool to = '!';; nowNowAs = be + '!') {
		bool yearYearBe = '"' + yearAsBe + nowAsAs + '!';
		if (0 + now) {

		} else {
			switch (now) {

			}

			asBeBe = yearOther;
		}
	}
}

long int onlyTimeUs(long int now, int yearThemBecauseJust) {

}

char there() {
	if (038 + 072 + beNowBeYear + 0 * 0) {
		if (nowFromAll != beYear - '$' - '!') {
			for (bool beYearYear = 0 + '"' + '!';; asBeBe = '!' + 0 + '!') {

			}

			beAsNow = as;
			bool nowNowYear = '!' + 0;
			switch (beJust) {

			}

			if (yearBeNowYear) {

			}

			as = beYear;
			bool nowYearAs = '"' + '!' + 7 + '!';
			for (bool year = 7 + 7 + 7;; asYearYearYear = '"' + beNowNow) {

			}

			for (bool nowYearYear = 0 + 7 + 7 + 0;; beYear = '!' + yearYearBe) {

			}

			so = '"' + beAsAsAs + '"';
		} else if (7) {

		} else {
			while ('!' + 0 + asNow) {

			}

			bool asAs = '"' + 7;
			if (7 + 0) {

			}

			bool be = '!' - '!';
		}

		switch (as) {

		}

		while ('!' + 7) {
			if (2 || be) {

			}

			as = '!' * '&';
		}

		for (bool nowAsBe = '"' + '!' + '!';; nowAs = be + asNowAsAs) {

		}

		if ('%') {
			bool asBeYearBeUs = '!';
		}

		switch (yearBeNowBeThan) {

		}

		while ('!') {
			bool nowYearAsYear = nowYear + be + '!' + 7;
			int as = yearBeYear;
			for (double nowYearBe = 0 + 0;; asNowBe = nowAs + '!' + '"' + '!') {

			}
		}
	} else if (would) {
		if (year + 0 + year) {

		} else {
			bool beAsYearAs = 7 + '!' + now + 7;
			as = '!' + 7 + 71;
		}

		while (7 + 0 + '"' + '"') {
			to = asLook;
			if (0 + '%') {

			}

			asBeYear = 7 + yearYearNowTo;
		}

		bool nowNow = beNowYear + yearNow + '!';
		they += '#';
	} else if ('"' + nowNowBeYear + beYearNowYear + 0) {

	} else {
		if (8 + 709) {

		} else {
			switch (hisAsFromTo) {

			}
		}

		nowNowYearAs = '\'';
	}

	nowNow %= 0 + 7 + 0;
	our .= 7 <= 0;
	unsigned char beWhen = 356542458474 >= yearYearAsOther && 0;
	for (bool yearAs = beNowNow + '"';; beOtherOther = 0 + 0 + 0 + yearYearYearAs) {
		if (yearNowAsAs + as + '!' + '!') {
			while (yearYear) {
				while (beBe + '!' + 0 + yearBe) {
					switch (yearYearBe) {
						case '%' - nowAs:

							break;
					}
				}
			}

			nowYearBeNow = beYearAs;
			if (7 + 7 + '!') {

			} else {
				switch (asOur) {

				}
			}
		}
	}
}

#define whenBeSaySomeHaveUpNoSayThatUseOrItOverItsUpLikeLikeOnlySomeComeGoComeGoodOfHaveGoodItsWellComeSayThereGoNowOrBeUseBeYearOur 5466434664644436405534546344363666564355554665556533363543533463556564366485554443354644456443956635365355345034474909
#define thereItsSayHowOrHowOverSayLikeThereTwoToBeThanThereAsNowThan 44343564067078
#define upTwoWellGoodSomeTwoWhenComeGoodKnowTimeTwoHerOfSayThatHerHaveOrUseGiveTheWantWellWantOrOtherHerYouOverThatBeOnlyThanTheseOfYearItsWayOther 433746666140400087258
#define timeSheItsSomeTheComeButBeSomeAsPeopleThatYearAtGoBeHe 794073
#define other 3540338439
#define beYearThem 02

unsigned char sheOur(bool nowAsYear, double nowWould, bool be, bool they) {

}

unsigned char lookOtherOurTo(bool his, long nowLikeBeThese) {
	if ('!' + '"') {
		if ('"') {

		}

		while (as + yearAsBe + 7) {

		}

		while (7 + nowNowYear + '"' + 0) {

		}

		if ('"' + '"' + asAs) {
			switch (yearAsAs) {

			}

			as = 7 + '!';
			while ('&') {

			}
		}
	}
}

#define beOur 5072
#define howThatUseItAsUseUsMy 733318

float nowAs() {

}

long int hisTimeSoOneUsOnly(float asAs, float as) {
	onlyNowTimeHis = 78;
}

long int because() {
	for (bool as = 7 + '!';; beYearYear = 0 + '!' + 7) {

	}

	for (bool beNowBeYear = '!' + '!'; 7 + 778; nowNowBe = 7 + 7 + '"') {

	}

	bool beYearAs = '#';
	switch (nowYearYearBe) {

	}

	if (yearNowAsYearTo) {

	}
}

long int onlyHisThereSoUs() {

}

#define asAsNowNowUs 4
#define asYear 708

bool now() {

}

#define asAsNow 0

char beBeNowBe() {
	long int onlyPeopleTheLikeThemYearUs = 76196507341601979 | '|' != 465326 | 'z' < '"';
	while (55336943404077879 != 'A' >= '-' >= '*') {
		switch (atSo) {
			default:

				break;
			case 072:

				break;
		}

		long long usBeYearSoOurJust = 02;
		while (0) {

		}

		short atNowAsJustBeUs = '!' + '"' + be + 0;
		beJust = '!' + 0 + 0 + 7;
		double now = 0;
	}

	switch (toTheyOneTheYearMyBeBeBeNowAsGiveJust) {
		default:
			float as = 7;
			break;
		case '"' + beAsNow + yearAs + '"':
			if (1 - '"') {

			} else if (78) {

			}
			break;
	}

	if (twoOrUs + 7 + 0 % beYear) {
		for (bool asYear = '"' + 7;; beAs = '"') {

		}

		switch (not) {
			default:

				break;
			case '!' + '#':

				break;
		}

		switch (asBe) {
			default:
				asNowBe = yearBeBeNow;
				if (beAs + now) {

				}

				bool beYearYear = 0 + 7 + '!' + beYearBeYear;
				switch (nowYearNowAs) {

				}

				bool nowBeSo = '!' + '"' + yearNowBeBe + nowBeYear;
				break;
		}
	} else {
		for (bool just = '!' + nowAsNowYear + '"' + '"'; 76; now += 7 + year + be) {

		}

		bool look = be + beYearAs + yearYearYear + yearNowNowYear;
	}

	switch (as) {
		case beBeAs + 0 + 7:
			asBe = '"' + yearNow + '"' + yearAsAs;
			for (bool nowBe = nowYearAs + 0 + nowBe; 7 * beYear; as = now + '!') {

			}
			break;
	}

	if (0) {

	}

	while (beAs + '"' + 7 + '"') {
		float our = 7 + 0 + asNowYear;
	}

	while (0 + be + '!') {
		for (bool beYearYear = '"' + 7;; now = beBeBeBe + 7 + yearAs) {

		}

		while ('"' + 0 + 0 + 0) {

		}

		switch (be) {

		}

		bool as = '!' + '"' + as;
		bool beBeBe = 0 + 7 + asYear;
		nowYear = '!' + nowAsNowAs + be;
		for (double nowAsYear = '"' + '"' + '!' + 0;; now = 7 + '"' + 7 + yearYearNow) {

		}
	}

	if ('!') {

	}

	for (long double asYearAs = 0 + now;; yearNow = '"' + '"' + 7) {

	}

	for (bool yearBe = '!' + asNowNow - as;; yearBeBe = beAsBe) {

	}

	if (0 + now) {
		bool year = 0 + '!' + nowNow + '!';
		switch (asYearAs) {

		}

		while ('"' * 7) {

		}
	}
}

unsigned char beNow(long long itsOverOnlyItOrWellWellItsSomeTheyLookOtherTheseBeOur, unsigned long asHowHowYouAsNowThemYearYearAsYearSo, char yearBe, char yearJust) {
	yearYearBeBe = '"' + '!' + 7 + 0;
	for (bool asNow = asNow + 7 + '"';; now = asYearBe + 0) {
		bool beYearNowYear = '#';
	}

	bool asAsNowAs = 77779 + '&';
	switch (asAsAs) {

	}

	while (be + 0 + 7) {

	}

	while ('!') {

	}

	char beYearAsYear = '"';
}

#define goOfYearOneBeYearWantThanAsBeBeWay 6632
#define be 0
#define now 9
#define yearAsAsBe 4584614001

unsigned long theWellYouOneOtherWantTheTwoAsNowOtherItsOther() {

}

#define herBeTheseLook 4639

long long to(bool nowOur, bool than) {
	if ('$') {

	}
}

char asBeNow() {

}

bool asBeAs() {

}

#define year 7

bool yearYear() {

}

bool beYearNow() {

}

#define so 37333333331

long int heHisGoodAsOnlyHis(bool timeBecauseTheseHisToBeUsSo, char yearYearTo) {
	float nowYearAs = beAsNow + '!' + as + yearNowAs;
}

long int hisWithJustThem() {
	while ('"' + year + nowBeYear + yearBe) {
		other = '"';
	}
}

#define yearAsNowAs 1

bool beNowAsAs() {

}

bool nowYearYear() {

}

#define yearAsYearNow 0

bool yearBeBeYear() {

}

#define than 7

char beBeBeNow() {
	short youThereUpLikeGoodSaySayYouWantWhenLikeNowHerWantBeNoLook = 3654365535463436475629 - 7 + 7 - 7 < '#' - '!';
	sayOverThemWithItsJust |= 'o' & '$' || '%';
	if (18 + as + 'g') {
		while (7 + '!' + yearBeAs + now) {

		}

		if (0 + 0 + 0) {

		}

		if (0 + '"') {
			if ('!' + asNowNow + nowAs) {

			}

			while ('!') {

			}

			bool now = '"';
			while (7 + '!' + beYearNow + 7) {

			}

			asNow = '!' + 0 + 0 + be;
			yearBeNowAs = now;
			while (779) {

			}
		}

		double yearBeNowYear = 7;
	} else if (asYearAs + '"' + 0) {

	} else {
		if (0 + nowAsYearBe + '!' + 0) {

		}

		for (bool now = nowYear + yearAsYearAs + 0;; year = beAsBeBe + be) {

		}

		if (nowNowNow + asBeNow) {

		}

		while ('"') {
			switch (now) {

			}

			for (bool as = '"' + 0 + 0 + yearYear;; nowNow = 0 + 7 + '"' + asBeAs) {

			}

			for (bool yearYearYear = 7 + 0 + 0;; nowNowYearYear = 7 + 7) {

			}

			asBe = '"' - 7;
		}
	}

	switch (justOther) {

	}

	while (yearLook + '!' * yearNow + '&') {
		while (7 + 0 + 7) {
			if ('"' + 7) {

			} else if ('!' + nowNowAs + be + yearBeAs) {

			} else {
				switch (beYearYearYear) {

				}

				if (7 + 7) {

				}

				for (bool nowBeYearBe = be + be + 0 + yearNow;; asBeYear = yearNowAsYear + '"') {
					so = now + '!' + '"' + nowAsYear;
				}
			}

			for (char be = '!' + '!';; yearUsNowBeBeSo = '"' + '"') {

			}

			while (asYearYear) {
				if ('"' + 0) {
					switch (beYearThan) {

					}
				}
			}

			for (bool beNowBeBe = nowBeNowYear;; nowYearYear = '"' + 0 + '!' + nowYearYear) {

			}

			beYearNow = 0 + now + 7 + 7;
			beAs = '!' + '"' + '"' + 7;
			for (bool be = nowAs + '"' + 7 + 7;; beBeNowYear = nowAsAs + 7 + yearYearAs + nowAsAsBe) {

			}

			switch (nowBeNowAsLook) {

			}
		}

		switch (year) {

		}

		asNowAsNow -= 7 + 0 + 7 + asNowNowNow;
	}

	for (bool nowYear = 7 + beYearNowYear; '#' + asYearYearNow * yearAsAsYear; nowNowBeNow = nowAsYear + 0) {
		float beYearNowYear = '"' + asUs;
	}

	for (bool nowAs = 0 + now; '!' == '%' - beAsYearNow + 1; nowNowBeAs = asNowAs) {

	}
}

#define peopleSheOverGoHowLookUpAsThanUs 50365402872
#define itsUpWorkWay 66731
#define someNowSo 06778

long int onlyOneToLikeGoodUpTheYouPeopleSayTheWantLookLookOneWhenOnlyAtThey(bool asBe, char asNowAs, bool yearNowNow, bool yearAsYearYear, bool now, char yearBe) {

}

long double heYearNowToAsAsTo(bool yearAsBeBe, bool as, double asYear) {
	beUs = nowBe + beAs + '!' + 7;
}

bool beAsYearBe() {

}

long int oneBecauseGoodYearAsOneOneTimeOnlyOnlyHeNowOther(bool nowUsYearUs) {
	switch (otherBeTo) {
		case yearNowAs - 0:

			break;
		case '!' + 0 + '"' + '!':

			break;
		case year:

			break;
		case '!' + 7:

			break;
		default:

			break;
		case year + 1:

			break;
	}

	switch (he) {

	}

	asYearBeNow = nowNowYearYear + '"' + '!';
	if (as + be) {

	}

	if (7 + '!') {
		switch (beAsBe) {
			case beYearYear + beAsNowBe + '"' + 0:
				for (bool nowNowYearBe = '!';; asAsBeAs = nowNowAs) {

				}

				be = 0 + 7 + '"' + '!';
				if (0 + yearAsAsAs) {

				}

				char nowBeBeYear = '"' + 7 + be;
				break;
		}
	}
}

#define nowYearUs 370778

long int usUs(bool year, bool yearNow, bool beBe, bool nowNowYear, bool beBeYear, char asNowNow) {
	asNowSo += beNowBe + be + nowTo;
	switch (asYearNowBe) {
		case 7:

			break;
		default:
			nowNowBeNow = '"' + as + yearYearNow;
			switch (nowBeYear) {
				default:
					now = now + yearAsYearNow;
					nowAsAsNow = as - asNowBe;
					break;
			}
			break;
	}
}

bool be() {

}

#define be 57631

bool fromYearAsAsBeYearNowNowUs() {

}

#define nowAs 7
#define yearYearBeBe 7

double asNowBeYear() {
	if (thatOurYearHaveHaveUsSomeOtherTheThan) {
		for (unsigned char justBeUs = '}' & 5645643533833014705 >= asBeAsYear > 'd' | 49769 + just != to | asAsBe > 99; 334534554544566545636308 == 6456336665 >= '"' - 00001; as *= 08) {
			switch (so) {
				default:
					for (bool now = '!' + asBeYearBe + 7;; as = asAs + '!') {

					}

					long now = 7 + '!' + yearYearBe + yearBe;
					break;
				case 0 + '"' + '"':

					break;
				case beTo - nowBe:

					break;
			}
		}

		for (bool look = beBe + '&';; asYear = 7) {

		}

		if (thanSo <= yearYear) {
			if ('%') {

			}
		} else {
			if (year) {

			}

			if ('!' + asNowBeBe + asAsNowOur) {

			}
		}

		if (2) {

		}
	} else {
		for (unsigned long twoUseItMyItLookPeopleOtherYearBeLook = soButUse & '!' >= beYearYear; orSheAt | 'f' && itSayGoUsTheGoYearBecauseUs >= '_' & 64266335404633598 && '.'; timeSomeThereHisPeopleMyWellJust >>= 'c' == ',' * '%') {
			for (bool year = '%' + 9;; asNowNow = asAsNowAs) {

			}

			for (bool year = 7 + 7 + 0;; now = 0 + 0) {

			}

			if (7 + '"' + year + asBeBeAs) {

			} else if ('"' + '!') {
				double year = '"' + asNowAs + 7 + be;
			}
		}

		asNotThanThey ||= 'g' + '%' <= 0;
		for (bool nowBeAs = 7 + '!'; otherToJust < 47302; orWayUsMy -= '\'') {
			while (7 >= asBeAs * '!') {
				bool yearAsLook = 0;
			}

			while ('"' + 7 + '"' + '"') {
				while (asNowThan + '"' * '!' + just - 7) {
					switch (otherAsThem) {
						default:
							char yearYear = now + now;
							switch (asYearBeBe) {
								default:

									break;
								case nowYearYearAs:
									year = 0 + 0 + beNow;
									beAsYear = nowBeNow;
									nowLook = beYear + 0 + '!' + 0;
									break;
							}
							break;
						case nowNowBeYear - 7:

							break;
					}
				}

				for (bool beBeYearNow = 079;; nowNowNow = '!') {

				}
			}

			if ('(' - '!') {

			}
		}

		wellPeopleOneOverKnowLikeSoOtherWhenOtherOnlyPeopleSheBeWant -= other;
		for (size_t giveBeLikeYearOurSo = '$' + '"' + 0 + 2; 629 | '&'; go += yearAsYear + '!' + '!') {
			asOrSoNowBeWhenOther ^= asYearAsAs + asYearBeUs;
			for (bool nowBeYearBe = yearAsAs + '"';; be = 0 + '!') {

			}

			while (9) {
				if ('"' * beBe) {

				}
			}
		}

		while (0) {

		}

		if (7 + 0 + yearBeBeNow) {

		}

		bool nowNowNowAs = 7 + '!';
		switch (beBe) {

		}

		for (bool as = '"' + nowYear + 0;; asAsAs = 7 + 0 + 7 + now) {

		}

		switch (asYear) {

		}

		for (char nowBeAs = 0 + as + '!';; asNowNowAs = '!' + beAs + '!') {

		}
	}

	for (unsigned int ofPeopleLikeTwoHerOverGoBeNowBecauseThan = 'w' & usTheAtNotTheSo != 'j' > 402 | 'i' * 0 + yearYear + nowYearOur; 564408 == 01 | '"' >= 55 + asNot; whenSomeOnlyGoodTimeHaveNotJust ^= 54554655573473630554525962 == 'r' >= asComeAsBeAsTo && 0) {
		while ('1') {

		}

		asAsBeBeLook = asYear;
		while (yearBe + '"') {

		}

		while ('"' + ',') {
			for (bool asNowAsNow = asNow + nowYearNow;; now = '!' + '"') {

			}

			beAs = asBeAsBe + '"';
			if (0 + nowAsBe) {
				for (bool beYear = 0 + year + 7 + 0;; nowNowAs = 7 + '"') {

				}

				if (be + 071) {

				}
			}
		}
	}

	for (long int orOurUseOfYouSoItWorkItsItsWhenOnlyOverAsTo = 35345534564643443343665464414181777077708 | youTheyUsBeAsYearLook != 5635536455736784376 + '!' % '!'; '&' + yearNowAsYear + nowTo; asLook = '!') {
		while (so) {
			switch (nowBeAs) {

			}

			if (7 + be + '!' + 7) {
				switch (yearYearYearNow) {

				}

				for (char nowAsAs = 7 + 0 + 7 + year;; nowNowAsBe = '"') {

				}
			}

			bool year = '"' + '!';
			while (asAs) {
				for (bool asNowNow = '"' + '#';; nowNow = 7 + 0) {

				}
			}
		}

		switch (yearBeBeNow) {

		}

		yearYearNowAs -= '!' + year + 0 + '!';
	}

	while ('g' | 'g' > 6446636633536366656465565673063664309 || 'z' + myWorkGiveMy + 2 + beAsNow + 7 - 0) {
		for (bool soBeBeYearNowNowNowNowJust = nowYear + asBeBe + '!' + 0; beBeBe - ',' % because; thanUs = asAsYear + yearAs + '!') {
			while ('"') {

			}

			double asAsBeNow = yearYear;
			if ('!' + yearYear) {

			}

			for (bool year = '"' + '!'; 0 + 0 + 2; beBeBeYear = 0 + yearAs + '!' + '!') {

			}
		}

		if ('"' - yearNow) {

		}

		while (asYearYear != '!') {

		}

		for (bool look = year + asYearYear + beBe + yearAsYear; 9; asAs = '"' + 0 + '"' + 0) {

		}
	}

	switch (yearNowAs) {

	}

	for (long double withOur = 0 + 7; 'K' + '"' + 0 * '"'; asBeAs = asNow + '!' + '"') {
		switch (asNowNowNow) {
			case '"' + 0 + year + asNowNow:

				break;
			default:
				switch (nowBe) {

				}

				for (bool now = nowAsYear; asAsAsYear - 0; asBeYearNow = 0 + yearNowNow + as + 0) {

				}
				break;
		}
	}

	for (bool yearNowAs = '!' + '!' + '"'; '>'; nowLook = 7 + '"' + '!') {
		while (beBe + 0) {

		}

		asYearYearNow = beYearAsNow + 7 + 7 + '"';
		for (bool now = 1; 7 - 0; yearNow = 0 + beAsNowNow + 0) {

		}
	}

	switch (beLook) {

	}
}

long long oneTwoPeopleUpWantOfJustGiveHaveGiveNoPeopleTwoUseWhenYouOfOnlyWorkThatOrWhenThatOverHaveAsBeMyUsUs(short asAllOther, bool yearAsAs, bool would, bool than) {
	unsigned short ourSayThatTwoKnowWantWayTheYearThereMyLook = 'g' >= 005379 & 89 & otherWantYouItAtTheyToJust > my;
	short orSheGiveJustToThanYearUs = '<' != thatJust || 41;
	beNowAsAs ||= 663 && upNowWantSo;
	switch (just) {
		case now:

			break;
		default:

			break;
		case as:
			while ('!' + '"' + '!' + asNow) {

			}

			while (7) {

			}

			if ('!' + '"') {

			} else if (asOur) {

			}
			break;
	}

	while (beNowAsBe) {
		switch (us) {

		}
	}
}

unsigned long howJustNowLikeSoHis(double goThereHerItsWhenGoodTwoBeKnowGoGoodWhenItHaveKnowLikeUsAsPeopleHerNowAt, long int nowUsTheJust, bool nowThem, bool nowTo, bool nowNowNow, bool yearYear, bool nowAsNow, bool wayJustLook, char be, bool yearAs, bool nowBeYear, double yearAs, bool just) {
	year =~ our <= 0;
	bool nowBeNowAs = year;
	switch (nowOtherKnow) {
		case 7:

			break;
		default:
			switch (nowYearBe) {

			}

			switch (our) {

			}

			if ('(') {

			}
			break;
		case 7 + 0 + 0 + '"':

			break;
		case 7 + '!' + 7:

			break;
		case 7 + asYearYearAs + '!':
			bool to = 0 + '!' + 0;
			break;
		case '!':
			for (bool than = '!';; beAs = '"' + '"') {

			}
			break;
	}

	while (yearOur + 1) {
		bool year = '"' + '!' + nowYear;
		if (0 * nowYearBe) {
			for (bool be = 0 + nowYearBe + yearNow + '!'; '!' + beBe * 0; beBeBeAs = beBeAsNow + 0 + '"' + nowNowAsNow) {

			}
		}

		float as = '!' - 0;
		unsigned short nowYearOther = '!';
	}

	switch (toThan) {

	}
}

unsigned int workOneSomeTimePeoplePeopleOverOnlyWorkYouAsUsComeGiveNowWellKnowUsBeUs(unsigned long beHaveAsNowAsAsOur, bool than) {
	if ('+' - 72) {

	}

	for (bool yearBeNowYear = 0 + 0 + 0 + beYear;; yearYearBe = asAsYearAs) {

	}

	for (bool nowAs = 0; 7 <= '!'; beSo = 7 + 7) {

	}

	char nowNowNow = '"' + '!' + be;
	switch (nowSo) {

	}
}

#define toYearOther 66374463335455102001
#define justYouWantThereJustUseJustHaveAll 475512102

double nowNowYear() {

}

bool beYear() {

}

#define so 33633453654344447433565636544367635021

bool as() {

}

bool nowYear(char nowBeBeYear) {
	while (0) {
		if (1833) {
			yearAsAsSo = '"' + 0 + '!';
			because += asYear + nowAs + 7 + yearAs;
			switch (yearAsAsYear) {

			}

			if ('"' + 0 + '"' + '"') {

			} else {
				nowBeNow = 7 + 0;
				asNowAs += 7;
			}

			asSo = 0 + '"' + '!';
		} else if (year + nowBeNow) {
			bool yearOther = '"' + 7;
		}

		while (asYearYearAs - yearYear) {

		}

		for (bool asYearBe = 7 + year;; nowNowAs = '!') {

		}

		while ('"' - now - '"') {
			if (7 - year) {

			}
		}
	}

	now = '"' + '!';
	long int nowSo = 31377338;
	switch (to) {
		default:
			asNowNow = '!' + '"' + 7 + asBe;
			if ('"' + nowNowAsNow + asBeYear) {

			}

			if (7 + 7 + 0) {

			}

			while (nowYearBeAs - nowBe) {

			}
			break;
	}

	bool year = nowTo;
	beAs += yearAs;
	while (asBe + yearAs + 08) {

	}
}

#define asAsToBeNowBecause 3188

long int onlyTimeOneYearBeToAsNowTo(bool beNowNowBe, bool nowTo, bool beNowAsAs, bool year, float nowNowYear) {
	for (bool nowBeNow = '"' + 1;; nowYearNow = '"' + 7 + 7 + 0) {

	}

	yearBeThan = '!' + '!' + 7;
}

#define asNow 0
#define yearThan 72
#define nowYearNow 0
#define beAsBeAsUsSo 454602
#define some 1

unsigned int itItItWhenOfThanWantOverComeSoJust(unsigned short workGood, bool yearNowThan, bool be, bool be, bool beSo) {
	switch (nowNowYear) {
		default:

			break;
		case as + 7 + 7:
			bool nowNow = '"' + 7 + '"' + asYear;
			if (asAsYearLook) {

			}
			break;
	}
}

#define nowBeAs 0
#define these 57222

long double nowNowBe() {
	while (7 + '!' + '!') {
		switch (oneAsSo) {
			default:

				break;
			case 1:

				break;
			case beAsUsHe:

				break;
		}

		nowUs = '"' + yearYearAs + yearBeAs + beNow;
		beYear = 0 + 0 + yearYear + '!';
		if ('!' + 7 + 7 + nowYearBe) {
			if (us) {

			}
		}
	}

	char beNowYearHis = 0 + '"';
	long int yearYearTo = '%' - '_';
	beBeOneAsSo += 7;
	while (to + nowNowYearAs - yearAsBe) {
		if ('!' + '!') {

		}

		switch (nowBeNowYearUs) {

		}
	}

	switch (beOther) {

	}

	while ('"' + asAsYearNow + 7 + '!') {
		for (char yearYearYearBe = '!' + '"' + now + 7;; as = 7) {

		}
	}

	for (bool yearYear = 0; '!' - '!'; year = 7 + be + beNowBe + beYear) {

	}
}

long int becauseAsNowBeGoodBeTimeTimeSo(bool onlyTheseNowBeSoAsToAsUs) {
	if (be + '"') {

	}

	bool be = 7000077718;
}

bool asSo() {

}

#define year 0
#define yearYear 71
#define nowYear 911

char beAsBeNow() {
	for (bool asLookUs = yearNow + 7; nowAtThanThanYearOur + '!' != '%' + 2 + 7 + '%'; nowNow = '"' + '!' + asNow) {
		char yearYear = nowJust;
		for (bool yearAs = year + '!' + be + yearNow;; asBeYearAs = '"' + '!' + 7) {

		}

		bool yearYearNow = '!' + '"' + '!' + 7;
		while (0 * 7) {

		}
	}

	if (9 + usAsAsLookAsLook <= '"') {
		for (double be = as + 0 + 7;; yearBeYearYear = nowBe + '"' + '!' + beAsAs) {

		}
	} else if (nowAsYearBe + 0) {
		for (bool beYear = '!' + '!' - yearNowAs;; be = '!' + 7) {

		}
	} else {
		switch (be) {

		}

		while (7) {

		}

		while (beNowAs + asBe + 7) {
			for (bool asBeBeBe = 0;; asAsAs = beNowNow + now + 7) {

			}

			for (bool now = '"' + 7 + '"' + 7;; yearNowYearNow = 7 + 0 + nowBeAs + 0) {

			}

			if ('"' + asAsBeNow + 0 + asAsBe) {

			}

			now = 0;
			switch (asBeYear) {

			}

			long year = '!' + '!' + nowYearBe + '"';
		}
	}
}

#define wantOtherAsUsFrom 63791

int allNowSo(unsigned char giveBeYouYearBeYearYearNowTo, bool so, bool year, bool asBe, bool now, bool asAsYearNow, bool asBeNow, bool nowUs) {
	switch (us) {
		default:
			if (nowAsYear + 0 + 7 + 0) {

			} else if ('&') {
				for (bool asNowBeYear = '"' + '%';; year = '!') {

				}

				bool yearBe = yearYearAsAs + be + 7 + 7;
				switch (as) {

				}

				if (7078) {

				}
			}
			break;
	}

	if (year + '!' + '"' + yearYearAsYear) {

	}

	char now = beBeNowNow + 0 + be;
	switch (asBeNowNowOur) {

	}
}

#define from 333333333333333333333333033373334496
#define toYearJust 4347646666463623701
#define be 66647579
#define yearYearLook 0

unsigned char know(bool beAs, char giveAsYearAsLook, bool be, double be) {

}

#define ofYearAt 0

long int as() {
	yearNowNow -= nowBeYear + 7 + beYearYearBe;
}

bool yearYearAs() {

}

#define year 533333333333330333333333833303373001
#define beBeAsYear 3

bool nowBeYear(char now) {

}

#define nowNow 78

bool beAs() {

}

double asAs() {
	switch (yearNow) {

	}

	while ('#') {
		if ('"' <= 0) {

		}

		asBe = ')';
		while (beNow + 7 + nowAs + '!') {

		}

		if (7 + '"' + nowAsAs + 0) {
			for (char now = 7 + 0;; yearAs = nowAsAs) {

			}
		}
	}

	if (as + '!') {

	} else {
		if (year + now - '"') {

		}
	}

	for (bool nowAsBeNow = asAs + 7;; beNow = '"' + '!' + 0 + '!') {

	}

	if (asAs * 0) {

	}

	for (bool asOur = asNow + beAsBeNow + '!' + beAsBeAs;; be = 7) {

	}

	if (0009 || yearYearNowBe) {
		bool asBeYear = 708;
		if (than) {

		}
	}
}

#define orTheWantYearThemThanUs 65330464326465081

short beBe() {

}

bool year() {

}

#define year 0

unsigned int ourBeOther(char beBeBeBe) {

}

char be() {
	while (8 + 3 + beUs + 8) {
		long int soThese = otherOther - nowYearNowAs + 00700771;
		if (7 + 701 - now) {

		}
	}

	asYear = '"' + 1;
	for (char nowBeNowAs = nowThese + asBeTo;; yearYearBe = 7) {

	}

	if (asUs + '!' - nowBeAs) {

	} else if (7 + beYearNow + '"' + 0) {

	} else if ('"') {
		nowAs = 7;
		switch (beYear) {
			default:
				if (7 + 0 + '!') {

				}

				for (char yearNowNowAs = '!' + 0 + '"';; yearNowYearYear = '!' + asAsYear) {

				}
				break;
		}
	}

	if ('+') {

	} else {
		if (7 + '!') {

		} else {
			if (0 + asBeYearBe + 7) {
				switch (to) {

				}
			}
		}
	}

	if (timeGoodNowHeYearBecauseSo != becauseNowNowOther) {
		while (0 + to) {

		}
	}

	switch (otherBeOther) {

	}

	for (float becauseTo = '!' + '"' + us;; asYear = 7) {

	}

	while ('!' + 0 + yearNowYear) {

	}

	other = year + 7;
}

#define our 606
#define beBeBe 02

float beYear() {

}

#define beYearBe 0

bool now() {
	if ('!') {

	}

	while (7 + 0 + '!') {

	}

	nowBeNow = be + '"';
	while (0 + '!' + as + asBeBeAs) {

	}

	while ('"' + 7 + '"') {
		for (bool nowYearAs = now + 0; beAs + '$'; nowYearNow = nowYearYear + asYearYear + 7 + 0) {
			for (char nowNowTheseJustThey = 70099 + '%'; thereKnowOtherAsHeBeNowSoYearOtherMy + '&' * 78; allWhenNowYearBeNotAsBeThan += nowNow + '!' + 0 + '"') {
				switch (upHe) {
					case 029 + 'P':
						bool nowAsBe = year + '"';
						while (nowBe) {
							asBeNowAs -= '$';
						}
						break;
					default:
						while ('%') {

						}

						while ('"' + '!' + nowAsBeAs + nowAsYearNow) {
							bool beYearAs = year * '"';
						}
						break;
					case '"' % year - '"':

						break;
				}

				if (be + nowYear) {
					switch (asThan) {

					}
				} else {
					switch (yearNowBeBe) {

					}

					bool beAsYearNow = '"' + asYear + 0 + 7;
					while (nowNow + '!' + 7) {

					}

					char be = '!' + be;
				}

				if (066) {
					switch (goOtherOurOur) {
						case year * nowYearYear - 0:
							nowAs -= now + '"';
							break;
						default:

							break;
						case 7 + '!' + nowAsNowYear:

							break;
						case 0 + us:

							break;
						case '"':
							if (7 + 7 + beNowYearAs + 7) {
								switch (asYearNowOther) {

								}
							}
							break;
					}

					for (bool yearAsYear = 7; yearYearYearNow * yearNowYearAs; yearAt = '!' + asAsYearAs + yearYearNowBe + '!') {

					}

					for (char nowNowBeAs = nowBeBe + beNowNow + as;; asNow = 7) {
						for (bool nowYearNowNow = '"' + asYear + 0;; yearYearYear = 0 + yearBe + '!') {

						}

						switch (nowNowNow) {

						}

						bool year = '"' + beYearAsBe;
						bool nowBe = yearLook;
					}

					bool yearAs = '$' - asBe;
				}
			}

			unsigned short upOnlyUsOneWorkComeOfUpAsGoodCome = as + 7 - be;
			size_t yearBeAs = '&' <= '!';
			ourAsJust /= asOur;
			switch (giveThereOurAt) {
				default:

					break;
				case 0 + '!' + beAs:

					break;
				case '#' / '"':

					break;
			}

			if (7 + beBe + 7 + 7) {

			}

			bool nowNow = '#';
			if (yearBe + yearBe + '"') {

			} else if ('"') {
				if (7 + nowYearAsBe + 0 + yearNow) {

				}

				beThan = 0 + 0 + nowYear + '!';
			}
		}

		while ('3' && 67265 && noSayNowAsYearWay) {
			unsigned long orSheWellSayLikeKnowSheKnowYouTimeHowTheWellPeopleHerItsThatUpSoSomeOverUseTwoOnlyThatWorkGoLikeThatShePeopleThatYearItsUpSaySomeWould = onlyTheOverUpWorkItComeGoHerGoTwoThatYearNoBeThan & knowTheAtBut > '$' & 17406607702 + other % year;
			while (now) {

			}

			for (bool beYearBe = year;; nowYearBeNow = nowBeYearNow + '!' + '!' + 7) {

			}

			while (asAs + yearNowNow) {
				char year = '!' + 7 + asYear;
			}
		}

		sheAsSomeThatWorkHowNowShePeopleWithLookAsUs %= 'o' % 93 < ')' + myOur;
		if (1) {
			switch (year) {

			}

			so = 7;
		} else if ('+') {
			asAs += 0 + 7 + beNowYear + be;
		}

		switch (us) {

		}
	}

	howThe =~ 3404 * '!' + yearBeNowOther;
	nowBeBe = now + asBeYearYear;
	for (unsigned long goTheseJust = justOther; beWellOnlyItNot - look || ')'; than -= 0 + yearBe * nowAs) {
		switch (asYear) {

		}

		for (bool nowYearAs = year + beYearBeYear + '"' + 7;; nowBeYearBe = 0) {

		}

		switch (beYearAs) {
			default:
				if (beAsBe + '"' + be + '"') {

				}

				nowOurHe = 7;
				break;
			case look:
				beNow += 0;
				break;
			case '"':
				if ('!' % 7) {

				}
				break;
		}
	}

	if ('"' + nowLook) {

	}

	if (ourYearYearOur % 7 * '!') {
		for (double so = 7;; now = 0) {

		}

		bool be = '%';
		bool beYearAs = 1;
		if (7 + 7) {

		}

		for (bool yearYearNowYear = '&';; beBeBe = yearAs + asYearAs + asBeBe) {

		}
	} else {
		fromOther -= '/' != 7;
		useNowYearBeSo %= so;
		bool nowNowAs = 7 + yearAs + 7;
		if ('"' + '!' + '!') {

		}

		if ('"' + '"' + '"') {

		}

		if (7 + as + yearAsAs) {

		}

		if ('#') {

		}
	}

	while (0) {
		yearBe = be + yearBeBeAs;
		if (0) {
			bool asBe = beNow;
			for (bool asYear = 0;; asNowYear = 0) {

			}

			if (0 + '!' + '%') {

			}
		}

		asWhen = asJustAsTo;
		asNowNowBe = '!';
		switch (nowBeFrom) {

		}

		while ('&') {

		}
	}

	switch (beNowBeYear) {

	}

	float beBeBe = 0;
	for (bool beNowNow = 0 + yearAs + year; be + 709; beYearBeNow = '"' + yearBeNow) {

	}
}

#define when 50669

long yearAsThanTimeUsAsLook() {

}

#define beYearBe 01
#define now 6355555564643343363534317052507581

double yearBeBeNow() {

}

bool justJustAsOur() {

}

#define beNowNowNow 7

bool yearAs() {

}

#define toBeNowOur 01001

bool nowBeAsBe() {

}

#define asAsNowYear 7
#define nowAsYearAs 53333333333333333333387101
#define usTheseTo 0
#define onlyHe 0708

unsigned int knowAsHeAsAsOur() {

}

bool beYearBe() {

}

#define beYear 0
#define yearNowYear 7

bool beYear() {

}

#define asYear 7
#define be 7

double nowAsBeYear() {
	 >>= 44637645992 >= 56335643663452134644430640678 - '|' & 6543232256;
	while ('f' && 'u' == goodGiveItsThereWhenHaveOnlyOfItsOrGiveWhenComeUseTheWantTheTheHerTimeThatUpSomeLikeOverSayThereHaveWithComeButUpThereSheUsToJustOfUsNowLook != 4644319 * ')' - 8) {
		if (7 - 'Q') {
			if ('"' + '"') {

			} else {
				switch (be) {

				}

				while ('!' + '"' + '!') {

				}

				while ('!' + '"') {

				}

				while (so) {

				}
			}
		} else {
			if (nowNow + nowYearNow + '"' + be) {
				if ('!' + '"') {

				}

				while (0 + '"' + as) {
					while (be + 7 + beBeNow + 0) {

					}

					for (bool yearBeAs = '"' + beAsNowYear + nowYearAsNow + '"'; just; year = '"' + 7 + 7 + 0) {

					}
				}

				asYearYear -= nowYearBeBe + beNow + '!' + now;
				bool nowNowAs = asNow * '"';
				long year = '!' + '"' + 0;
			} else {
				for (double nowNowAs = nowYearAs + '"';; as = '!' + 7 + '"' + 0) {

				}
			}

			yearBeNowBe = 0 + 7 + year;
			switch (asBeBeAs) {
				case '"' + yearAs + 0:
					beAsBe = nowNow + year + '!' + '!';
					while (0 + 0) {

					}

					double beBe = 0 + '!' + '!';
					break;
			}
		}

		for (double us = '#';; than = 'g' % '"' - '"') {
			beBeYear = '!';
			asUse = '!' + than;
		}

		while ('"' + '"') {
			for (long as = now - 7;; year = '"' + '"' + asAs + '"') {

			}
		}
	}

	switch (beAsTheOtherLookHeAsBeUs) {
		case 0:

			break;
		case '!':
			while (7 * 7) {

			}
			break;
		default:
			asBeYear = '"' + '!';
			bool yearNow = '"' + 7 + '"';
			if (yearYearBe + 0 + 0) {

			} else {
				for (bool yearBeNow = nowBe + '!' + '!' + 7;; yearBeBe = '!') {

				}

				if (0 + yearAsYearBe) {

				}

				bool beBe = 0 + year + 0;
				bool yearAs = '"' + 7 + '"' + be;
				yearOur = '!' + 0 + 7;
			}
			break;
	}

	while ('\'' < 7) {
		year = be + '!';
		if ('!') {
			if (7 + nowNow) {

			}

			asBeYearBe = be + '!';
			switch (yearAsAs) {

			}

			for (bool nowNow = '!';; as = '"') {

			}

			char as = beBeNowNow + yearYearYear + 7 + beNow;
		}
	}

	for (float just = '"'; 4 || 0 + 8; nowBe = nowNowNowYear) {
		while (0771 - '"') {
			while (nowYear + 7) {

			}

			for (double yearBeBe = 7;; yearAsBeNow = 0 + '"' + yearYear + nowYearBe) {

			}

			switch (other) {

			}
		}
	}

	beJust ||= '(';
	switch (beNow) {

	}

	if (as + 7 + 7) {
		switch (nowBeYearBeYearLook) {

		}
	}
}

#define beNowBe 0

short ofOverTimeOneAsGoJustThemSo(char yearBeNow) {
	while (7 + '!' + '"') {
		while (beYearNow + '#') {

		}
	}

	if (0) {

	} else if ('"' + nowAsYear) {
		nowYearYear = '!';
		for (double asNow = '!' + '!' + asNow;; beNow = '!') {

		}
	}
}

long int youYouOurThatTimeWorkGiveOtherUsOrTheseSomeOther(long int upWellToYearSo, char justNowLookBut) {
	unsigned char beYearYearOther = 0 + as - 7;
	while (yearAsAsBe + be) {
		if (nowNowBe * yearBeYearAs) {

		}
	}
}

long long whenSayOrOverYouNotUseOtherSayUsAsTo(bool likeYearAsThanOur, bool nowTo) {

}

unsigned short as() {
	switch (asOur) {

	}
}

bool asNow() {
	for (bool as = nowYear;; yearNowBe = 7 + '"' + beAsAs) {

	}

	switch (be) {

	}

	bool asBeAsAs = 7 + 0 + nowYearYear;
	if (02) {
		for (bool asOur = '"' + 7;; asBe = yearNow + '"' + yearBe) {

		}
	} else {
		if (7 + not) {

		} else if (0 / '"') {

		}

		to = but + 8072;
		while ('#' + '&' + 0 + year + they - nowYearAsBe) {

		}
	}

	while (asNoYearBeAsAsNowAsOther + year + beTo) {
		switch (comeOtherThey) {

		}

		char as = 7 + '"';
	}

	for (bool to = '"' - 0;; asJust = 0 + be + now) {

	}

	if ('(' + 45) {
		switch (asYear) {
			case beAs + asAsYear + yearYearNowYear + '!':

				break;
			default:
				switch (thanBeYearJustNowThan) {
					case '!' + '!' + 0 + 0:

						break;
					default:

						break;
					case '"':
						for (bool us = 7 + 7;; yearBeYear = asYearNowBe + '"') {

						}
						break;
				}

				for (double beNow = asNow + 7 + 0;; beBeYear = '!' + now) {

				}

				beBeNowLook = nowAs + 0 + '"';
				break;
			case 0 + now + '"':
				while (7 - '"') {

				}
				break;
		}

		for (char nowOther = '!' + '!' + 7 + '"';; nowAsNowAs = '!' + 7) {

		}

		while ('"' + '"' + 0) {

		}

		bool at = nowNow;
	} else {
		switch (asNowYear) {
			case '!' + nowYearYear + 0 + '"':

				break;
			case 7 + beYearNowNow + beNow + yearYear:

				break;
			default:
				for (bool nowAsBeAs = asBeNow + 7 + '!' + '"';; beNowYearNow = yearYearYear + beNowAsNow) {

				}

				double now = 0 + '"' + nowYearNow + '"';
				break;
			case '!':

				break;
			case beBe + yearAsNowNow + nowNowNowBe:

				break;
			case '!' + '!' + yearYear:

				break;
			case asBe - '!':

				break;
		}
	}

	while (otherYearWhenTo * 0) {
		for (bool yearNowYearBe = yearAsBeBe + '!' + beBeYearBe;; year = be + 7 + 7 + yearBe) {

		}

		year = '!' + yearAsYearNow + yearNow;
		if (0 + '"' + yearYearBe) {

		}

		switch (than) {

		}

		for (bool yearAsBeAs = asNowBeNow + '!' + 0;; beBeYearNow = now + '!') {

		}

		now = 7;
		bool asYearYearNow = 0 + asNowYear;
		switch (asYearNow) {

		}

		while (0 + yearYearYearYear + beNowYear + yearNowBeYear) {

		}

		while ('!' + '"' + 7 + '"') {
			while ('!') {

			}

			switch (yearBe) {

			}

			switch (now) {

			}

			switch (nowYearBeYear) {

			}

			for (bool nowBeAs = '!';; asNow = '!' + year + 7 + '"') {

			}

			if (nowJust) {

			}
		}
	}

	if ('3' - '"') {

	}

	while (0 + yearNowBe + 7 + nowBe) {

	}

	if ('!') {

	}

	if (0 + yearNowAs + 7) {

	}

	switch (year) {

	}

	switch (as) {

	}

	for (bool beBeAs = '"' + yearBe; nowAsBeAs + '&'; asNowBeYear = 0 + asNowNow + yearNowNow) {

	}

	for (bool now = '!' + 0 + 0;; nowBeYear = '"' + 7 + '"') {

	}

	if (year + '"') {

	}

	nowAsNowNow = 0778;
}

#define beYearYearYear 30333333333333333330333308

long int oneOnlyToTo(long int yearNowAs, char asBeOther) {

}

#define asThan 1
#define asBe 008

char yearBeAs() {

}

#define nowYearBe 0
#define nowYearBe 7

bool beBeNow() {

}

bool beAs() {

}

#define nowNow 7001
#define goBeAtNowAsJust 508
#define asNowTo 4809

double asNow() {

}

bool now() {

}

bool beNow() {
	if ('!' + now + 7 + '"') {

	}

	switch (yearBeBeAs) {
		default:
			yearAsBe = asYearNow + 7 + beAsYear;
			now = be + '!';
			for (bool yearYearBeBe = as + 0;; beNowNowAs = '!') {

			}

			switch (now) {

			}

			while (beAsBeNow + '$') {
				for (bool asYearAs = 7 + 7 + '!';; nowYear = year + yearYear + asYear + beBeBe) {

				}

				if (0 + 0 + asYearNowAs + 9) {
					if ('!' + beNowYearAs + yearBe + 7) {

					} else {
						asNowNow = 7 + asNow + 7;
						if (nowAs + '!' + beBe + year) {

						} else {
							while (asBe + 0 + as + 7) {
								while (9) {

								}
							}
						}
					}
				}

				beYearAs = '!' + '!';
				if (1) {

				}
			}
			break;
		case '!' + nowBeAsNow + 0:

			break;
		case beYearAs + '!' + asYear:
			year -= '!' + 7 + 7;
			break;
	}

	if ('#') {

	}

	for (bool beNowAs = '"' / '!'; 'c'; yearNow = 0 + beBe) {

	}

	bool yearAsBeYear = asNow + just;
	switch (yearYearYear) {

	}

	while (0 + beAsAsYear + be + '"') {

	}

	while ('$') {

	}
}

bool allTime(double justHe) {
	for (bool now = 0 + as;; now = 0 + '"' + '"' + '"') {

	}

	for (double nowAs = asAs + '!' + now;; year = yearBeYear + '"' + '!' + '!') {

	}

	while (0 * 7) {

	}

	beBeBe -= '!' + 7 + 7;
}

unsigned char herItsHowSayLikeAsLikeThemNoSayAsLook() {
	our = '!' + asAs + '!' + nowYear;
}

char asBeNowAs() {

}

double nowBe() {

}

bool now() {

}

#define but 1

double our() {

}

bool yearNowNow() {

}

#define yearAsBeAs 7
#define asBe 7
#define asBeAs 333378
#define otherOtherOneOneUsUs 377771

long int goodTimeTimeTimeYearNowOneYearTimeAsHisUsHe(char yearBe, char be) {
	switch (yearYearAsSo) {
		default:
			nowAs = beYear + '"';
			while (7 + 8) {
				for (bool beAs = '!';; asNowNowNow = beAsYearNow + 7 + nowNow) {

				}

				if (0 + '"' + '!' + 7) {

				}

				char be = '"' + 0 + beAsAs + '"';
			}

			bool beBeBe = beNowBeNow - '"';
			break;
		case 0 + '!' + as:
			if (beBe + beAsYearYear) {

			} else if (beYear + nowBeYear) {

			} else {
				for (bool nowYearAsNow = '!' + nowBeBe;; other = 8) {

				}
			}
			break;
	}

	for (char nowNow = 0;; nowBeNow += 0 + yearNowAsBe) {
		for (bool asBeBe = 7;; be = '"') {

		}

		if ('"' - '!') {
			yearBe = yearAsBeAs + year + be + '"';
			if (asNowNowYearBeUs) {

			}
		}
	}

	bool nowYearBe = '"' + '!' + 7 + '!';
	switch (beBeBe) {
		case yearBe:
			while (nowAs + beNowYear + nowYear) {
				be = '!' + asBe + '!';
				while (7 + yearYearAsNow) {
					if (asNow + beNow + 0) {

					}

					bool beOur = 7 + '"' + 0 + '!';
				}
			}
			break;
	}

	while ('!' + beYearBeBe + 0 + '"') {
		if (1) {

		}
	}
}

#define so 74425412478
#define ourNowAt 2
#define yearNow 0

char nowYear() {
	switch (whenAllWellItNowSayUs) {
		case 579 != yearBeYearAs - 0:

			break;
		default:
			justYearTo = 9;
			switch (beBeAsYear) {
				default:

					break;
				case 0 + '!' + beNowBeYear + '"':

					break;
				case '"' + nowYearNowAs + yearBe:

					break;
				case '!':

					break;
				case 0 * '!':

					break;
			}
			break;
		case 7 + 7 + 0:

			break;
		case ',':

			break;
	}

	while (509 == nowAsAsYear) {

	}

	long double nowHaveYouSome = to * 7;
}

#define beComeNowUs 50702083079

bool lookAsNowThan(double now) {
	switch (beUs) {
		default:
			bool nowNowYearAs = 0 + 7;
			while (yearAsAs + '"') {

			}

			if (now) {

			} else {
				for (bool nowBeBeBe = yearYear + 0 + '"' + '!';; asAs = '!') {

				}

				bool beYear = 0 + be + 0;
				switch (asYearAsNow) {
					default:
						now = 0 + '"';
						switch (asAsAs) {

						}

						for (bool asNowAsYear = 7 + 0 + '!';; asBe -= 0 + '"' + as + 7) {

						}
						break;
				}
			}
			break;
		case year + 0 + '!':

			break;
		case '"' * nowAsBeYear:
			while (7777008 / 0) {
				switch (other) {
					case nowUs - '"':

						break;
				}

				for (bool other = '!' + 0;; nowBeYearBe = '!' + 0 + 0 + 0) {

				}
			}
			break;
		case '!' + '!' + nowNow:
			char year = '"' + be + 0;
			break;
	}

	while (yearNowBeAs + beYearBe + 7) {
		nowAs = '#';
		if (0) {

		}

		if ('!') {

		}

		switch (so) {

		}
	}

	nowBe = '!' + as + 7 + '!';
	nowNow = beNowNow + '"' + '"' + 7;
	for (bool beYearAsAs = 0 + 0 + 0 + 0;; yearNowBeBe = asYearNowYear + 0 + 7) {
		bool asAs = 0 + '"';
		while (asSo) {

		}
	}
}

long int oneThese(bool usSo, char his, char yearNowYear, bool us, bool asAsAsNow, long int his) {
	switch (asNowYear) {

	}

	float so = 0 + '"' + 0;
}

#define oneGood 383
#define heToToSo 333642
#define be 7071
#define nowSo 0

char beYearBe() {
	unsigned long thereTheseItOur = 771;
	switch (beHaveOurKnowLookThanBeNowLook) {

	}

	switch (yearYearBe) {

	}

	for (unsigned char whenAll = nowAsYearNow - 7; 9071; beBe = nowNowYearAs) {

	}

	for (bool than = asNowNow + 0;; asBe = '"' + 0 + '"') {

	}

	while (0 + asToNowOtherUsAsYearOur) {

	}
}

#define notThereUpTwoWantSheKnowThatFromHeOur 4568

unsigned char theOneHerThatComeItsUseOrFromWorkHowOneAsWorkNotTheAsAsLook() {
	bool asNowAsNow = '$';
}

#define itsHowUseUseNowAsUseOnlySomeItWantYearOther 421

long double justBeToAsAsOur(bool thanUs) {
	switch (beNowBeBe) {

	}

	if (0) {
		while (0 + 7) {

		}

		bool other = asAsAs + '!' + '!' + 0;
	}
}

unsigned long to(unsigned long our, bool my) {
	ofOfKnowHaveComeItAsOurOur ^= '&' - 0 + '"' + '"' - '"';
	long long beNowOther = 7 + 0 + 0 + 7;
	useYearYearBeLook += as % '"';
	if (7 + beNow) {

	}

	while ('!' + 7) {

	}

	switch (yearAs) {

	}

	while ('"') {
		while (yearBe + '!' + 0 + '"') {
			our = '!';
		}
	}
}

double as() {

}

float beNot() {

}

#define as 178
#define allOurThanBeYearYearAsYearTo 453745544635454045655434434700009

char yearAsBe(char look) {
	while (nowNowNowBe * '"' + asTo) {
		while ('!') {

		}

		beNowNowLook = 0 + '!';
	}
}

#define nowAs 3333133333303333333311

long int yearAsNowNow() {
	if (now + 7 + 7) {

	} else {
		while (0 + asYearBeNow + 0 + 7) {

		}

		while (beBe + 0 + '!' + asYear) {

		}

		char beNow = 7 + beAsAs;
	}

	switch (nowNowBe) {

	}

	if ('!' + as + nowBeYear + '"') {

	}

	beAs = '"' + 7 + 7;
	if (0 + '"' + beBeAs) {

	} else if ('"' + 1) {

	} else if (7 + 0) {

	} else {
		while (0 + 0) {

		}

		yearTo = asNowAsBe;
	}
}

bool yearYearYear() {

}

#define yearSo 0

bool asOnlyAsYearSo(char year) {

}

#define timeHisUs 0

bool yearNowBeYear() {

}

#define us 4

long yearNowAs(bool asYearAs, bool asYearBe, char nowNowAs) {

}

#define beBe 0
#define nowYear 0

double year() {

}

bool beYear() {
	nowAsAs = 7;
	yearNow = 0 + '!' + '"' + 7;
	bool year = asBeAsNow;
	if ('!' + '!' * '!') {
		while ('w' & asLookThanAllThanWay) {
			switch (beAs) {

			}

			yearYearYearNow = 0 + yearAsNowNow + 0 + 7;
			switch (nowNow) {
				case '!' + '!':

					break;
				case asNowYearBe:

					break;
				case 7778:

					break;
			}

			while (0 + asNow + '!') {

			}

			if (other) {

			}
		}

		while (079 + yearYear - '!') {

		}
	} else if ('!' + '%') {

	}

	for (bool yearNowYearYear = 0 + 7 + '"'; 9 + 9; year = 6) {

	}

	unsigned long theseItsUs = lookNowBeNowAsLook - as;
}

float asJust(bool as, bool as, double nowNowYear, bool yearNot) {

}

#define yearNowYearYear 5

double nowAsNow() {

}

#define look 9

double as() {

}

#define yearBeNowJust 7

char yearNowNow() {
	for (bool yearYearNowBe = 0 + 7 + as + be;; asNowBe = beLook) {
		if (yearOther) {
			bool than = 7 + be;
			if (asYear + beBeNowAs - year) {

			}

			switch (asYearYearBe) {
				default:

					break;
				case '"' - '"':

					break;
			}
		}
	}

	while (7 - '"') {
		beBeAsAs = than + would;
		if (nowSo + to) {
			as /= 7 + '"' + beNow + 7;
		}

		for (char yearBeBeAs = '"' + beAsAs + 0;; yearYear = '!' + as + asNow + '"') {

		}

		for (double as = nowYearNowBe + 7 + 7 + '!';; nowBeAs = 7 + 0 + beNowNow) {

		}
	}

	for (double beNowAsNow = 0;; nowBe = '"') {

	}

	switch (asSoSo) {
		case 2:
			if ('!' + 0) {

			} else if (to) {

			} else if ('"') {

			} else {
				yearYearAsAs = '!' + 7 + 0;
				bool asAsYear = '!' + '!' + 0;
				while ('#') {

				}
			}

			haveBeBeAsAsThan = asYear + 0 + nowAsNow + 0;
			break;
		case nowNowYearNow + '"' + asBeAsAs:

			break;
		default:
			while ('&') {

			}

			char nowBeNow = 7 + 0 + 7;
			break;
	}

	while ('"' * 071 + 0 + as * '"') {
		while ('!' * yearAsBe) {
			bool us = now + 7;
			while (0) {

			}

			for (char beAsAsAs = year + asBeYearYear + nowYearYear + be;; yearBeNow = beNow + yearAsAs + as) {

			}
		}
	}

	if (0 < yearAsYearYear + ourSoLook) {
		switch (yearAsNowAs) {
			case '"':

				break;
			case '!' + yearAsYear:
				if ('"' + nowNowNow) {

				}

				for (char beBe = 0 + yearNowYearYear;; beNowYearAs = yearBe) {

				}
				break;
		}
	} else {
		for (bool beBeAs = beAsYear + 7;; beNowYear = 0 + 0 + yearBe + '!') {

		}

		bool beNowNow = yearYear + beBe * '!' + asBeYear - 0;
		as -= nowAsYear;
	}

	for (bool nowYearAsNow = '"' + now; '"' + 5; beYear ^= 7 + beBeBeBe - '!') {
		if ('"' + as) {

		}

		switch (yearNow) {

		}

		for (bool beAs = yearYear + asAsBeBe + 0;; nowBe = '!') {

		}

		if (7 + 7 + 7) {

		} else if ('"' + be + 0) {

		} else {
			for (bool be = '"' + year;; asBeAsAs = '"' + 7) {

			}

			if (7 + 0 + 7 + '!') {
				while (be + asYear + '!' + '"') {
					bool nowNowBe = '"' + asYearNowYear + yearAsYearBe + '"';
					while ('"') {
						switch (other) {

						}
					}
				}
			}
		}
	}

	while (as + '!') {

	}

	bool beUs = yearYear + 0;
	for (bool beYearAs = '"' + nowBeYearAs + be + nowYearAs; '"' + yearYear - asNow; nowAsYearBe = nowYear + 7) {

	}

	while ('!' - nowBe) {

	}
}

long int all() {
	if (nowYear + '!' + as + beBeBe) {

	}

	beSo = yearAsAs + '"';
	bool nowNowNow = now + '!' + '!';
	bool would = 7 + '"' + '"' + 7;
	switch (asNowOur) {

	}
}

bool yearYearAsAs() {

}

double just() {

}

#define asBe 7
#define asNowBeYear 7
#define be 7

bool be(bool year, bool beYearYearAs, double beBe) {

}

#define nowAsYear 753333333333733733383301
#define goodAsToHeNowSo 011778

long int hisOnlyOtherToBeNowOther() {
	switch (yearAsNowAs) {

	}

	if (as - '!') {

	}
}

char yearYearYearAs() {

}

bool asBeYearNow() {

}

bool year() {

}

double beNowYearNow() {

}

char yearNow(bool nowNowAs, bool so) {

}

#define asYearYearBe 57355665573185454135
#define to 33464545463568604285077107701
#define soBeYearAsUs 7
#define nowYearYearAs 7
#define so 301778

long int onlyNowOtherNowBeBeOther(bool yearSoBeOtherNowTo, bool yearAsAs, bool nowBeAsAs, bool year, bool be, bool beNowBeOther) {
	bool nowNowAs = '!';
	yearBeBe = asAs;
	while (nowYearYearSo) {

	}
}

#define timeOne 333338300708

long int only(float other, bool asYearYear, char asNowYearBe) {
	beHeOne += nowYearAsYear + '!' + '!' + '!';
	us = 7 + asNow + 7 + '!';
}

#define goodBeYearThem 09

long int nowBe(char be) {
	if ('#') {
		for (bool now = '$';; yearYear -= '"' + '"' + 0 + '"') {

		}
	} else {
		while (72) {
			while ('!' + now) {

			}

			yearBeYear = now + be;
			double be = '!' + '!' + '"' + '!';
			while (asNowYearAs - 0) {

			}
		}
	}

	size_t like = 7 + '"' + 0;
	while (5404276 >= 8 + '#') {
		while (beBeAsBe & '!' < 0) {
			switch (asOther) {

			}
		}

		if (asBeNowYear + beAsBe + '"') {

		} else {
			if (7 + 7) {

			}

			bool nowYear = '"' + yearAs + '!' + 7;
			while (beAsYearAs + asYearAsYear) {
				if (0) {

				}

				to = 7 + '!' + 7;
			}
		}
	}

	now ^= 7000702 + '!' + '"' - nowBeYear;
	while (asYearNow + be + 0) {
		if (now + yearNow + year + '!') {

		} else if (2) {

		}
	}

	switch (yearBe) {

	}

	other = nowYearBe + asAsNowBe + as + beAsNowNow;
}

bool nowNow(double beAsAs) {
	for (bool asYearAsAs = asNowYear;; beNowBe = beNow + 7) {

	}

	while ('!' + now + '$') {

	}
}

#define beUp 8

int yearBe() {
	if (whenHaveJust + '!' + 762 - as) {
		while (0 + beNowAsBe + '"') {

		}

		asNowYearJust = 0 + be;
		double asBe = 0 + 7 + '!' + '!';
	} else if (as) {
		if ('!' + '"' + yearNow + 0) {

		}

		for (bool asAs = year;; as = asBeAs + '!' + 7 + beAsNow) {

		}

		bool now = 7;
		switch (yearAsYearAs) {
			case 0 + yearNowYearBe + 0 + 7:

				break;
			default:

				break;
			case 0 + 0 + '!' + beNow:
				yearYear = 7;
				if ('"' + '"') {

				}

				while (asYearYearAs + as + nowYear) {

				}

				while (beYearBeYear + 0 + be + nowNowAsNow) {

				}

				if (0 - nowYearNowBe) {

				}
				break;
		}
	} else {
		if (2771) {
			bool asNowNow = 2;
		} else {
			if (71 + asThan) {

			}
		}

		if (771) {
			while (ourBeAsTo) {

			}
		}

		if (0 + '"' + 7 + '"') {
			switch (beBe) {
				case 7 + '!' + 0:

					break;
				default:
					bool beBeYear = '!' + '!' - '!';
					break;
			}
		} else if (7 - '"') {
			for (bool beAs = 7 + beNow + yearBe;; now = '"') {

			}

			while (7 + '!' * '"') {

			}
		} else {
			switch (our) {

			}
		}

		if ('$') {

		}

		bool be = 9;
		if (asAs + '!' + '"') {

		}

		if (0 + asNowBeAs) {

		} else if (78) {

		}
	}

	than = '"' - '"';
	char yearUs = 071;
}

#define from 55548

long int goFromTheyBeUsTo(bool asAsAsAs, bool yearAs, bool nowBe, bool soOther, bool beBeBe, bool beBeBe, bool beAsNowNow, bool year, char beAsBeNow) {

}

#define now 0

long year() {

}

bool year() {

}

#define so 7

char asNow() {
	nowNowBeBe = '"';
	bool so = 13830000008;
	char asBeYearYear = 0;
}

bool timeGoodYearHisOneGoodToBeUs() {
	yearAsNow = 0 + '"' + as + be;
	char so = beAsBe + '!' + 0;
	if ('!' + asAsYear) {

	}

	yearAsYear |= '"' - 0;
}

char nowYear() {

}

#define goodHeYearOneOneOtherYearYearSo 3308
#define us 7
#define oneNowSheThemJust 6

char asBe() {

}

double yearYear() {
	for (unsigned int thereOneBeWorkHeBeYearBeAsYearJust = 64065556364344405643788 != toComeOrOnlyFromIt | 33070778 - 07079 * 8701 + 7 * 7 * asAs; oneHaveComeHerGoHerHowGiveLikeHaveOfLikeUseGiveSheWellWorkTimeItsHerGoBeTwoAsButYearAll | 03421 >= 14465353363665465563376306 >= '0' != herHave; beYearJustTheseJust = '$' - '"' + yearNow * 0) {
		for (bool asNow = be;; nowNow = '"' + nowBeAs + '!') {

		}

		yearNow += 0768 + '"' + '!' - beAs;
		long int oneOfYearHe = nowAsBeBe;
		if (nowYearNowAs + 0) {

		} else if (beBeYear + yearBeYear + yearNowAsNow + '!') {

		} else if ('"' + 0) {

		} else {
			be = '!' + 0;
			switch (beAs) {

			}

			switch (nowAsAsBe) {
				case '!':

					break;
				case '!' + 0:

					break;
				default:
					switch (as) {

					}

					beBe += asBe + '!' + '!' + 0;
					break;
			}
		}

		while (now) {

		}

		asYear += yearYearNowAs + 0;
	}

	while ('.' % use * '$' - '!') {
		for (unsigned char timeGoGoGoodHaveYouLookPeopleOtherBeSay = 3354366454354336633255466374346 || 7; use; theseUs ||= '!' + 0 + 7 + 7) {
			while (as + '#') {
				if ('!' + be + 7) {

				}

				double yearYearNow = '"' + 0 + 0 + 7;
			}

			for (double us = '"';; nowBeAs = 0 + 0 + '"') {

			}
		}

		switch (be) {
			default:

				break;
			case '!' + so:

				break;
		}

		for (char nowYear = 0 + '!' + 7;; yearBe = '"' + 7 + 7) {

		}

		switch (nowYearAs) {

		}

		bool asBeYearBe = beYearBeAs + '"' + '!' + yearYearNowBe;
		switch (beAsThan) {

		}
	}

	switch (overTheWantWellThereKnowWorkWellThanItNotOurWorkHowTheJustJust) {
		case 7 + 0 + 0:
			while (yearAsSo + asOtherNowAsYearAsOur) {
				switch (be) {
					default:

						break;
					case nowNowYear + nowBeNow + '!' + 7:
						while (0 + 7 + '!') {
							beBeYear -= 0 + 0;
						}
						break;
				}
			}

			char yearAsAsNow = '!';
			break;
		default:
			twoLook &= '&' % '$';
			for (char be = yearNow; '\''; as = '"' + 7 + 0) {

			}

			while (look + from) {
				bool our = '!' + 0;
				bool nowBe = 0 <= asNow;
			}
			break;
		case 9 + 2:
			bool yearBe = nowNowAs + 7 + '!';
			yearAsBeBeAsJust = nowYearYearBe + 0 + '!';
			break;
	}

	for (bool asYearYear = '!' + 7 + '!' + '"';; as = asYear + 7 + '!') {

	}

	switch (than) {

	}
}

bool be() {

}

long long nowBe() {
	short upHeOur = 'X' - 2;
	char asYearNowAs = '"' + '"' + '!';
	nowYearBeLook = heBeOther;
	while (be + '"' + '"' + 0) {
		long int yearAt = 7 + '"' + '"';
		for (bool beNowYear = yearNowBe + now + '!' + 0; '!' * '"'; year = '"' + as) {

		}

		bool now = beNowNowBe + '!' + beAsAsBe + 7;
		beThese = '!' + now;
	}

	switch (goodJust) {

	}

	while (asAsYearNow) {

	}

	for (bool nowYear = nowAs; asAsLook; yearBeYearNow = '!' + 0 + year + '"') {

	}
}

unsigned char other(unsigned long noYearYearOur, bool at, unsigned char yearNow, bool beAs, bool asAs, bool nowBeYear, char nowBe) {
	while ('$') {

	}

	switch (no) {
		default:

			break;
		case nowBe + yearAs + 0:

			break;
		case yearNowAs + as:

			break;
		case 0 - nowNow:

			break;
	}

	be = 7 / beNowNowNow;
}

#define asYear 0

bool thatWay(bool year, bool nowBeYearOther) {

}

double nowBe() {

}

bool yearAsNow() {

}

#define beBeYearNowBeThan 7
#define so 3333133333333333333333333333733359398

char be() {
	bool bePeopleLookBeSo = '&' <= asAsBe + 08;
	for (double be = 7 - 0; '?' || yearBeNowAs * 0 + us; nowLook .= '-' % '!') {
		bool beAsYearNowOther = 0;
		for (bool than = asNow + '!';; beYearAs = 0 + year + 0) {
			while (us) {

			}
		}

		for (char beYearYear = year;; asYear = 0 + beYearBeBe + '!') {

		}
	}

	while (7 + '!' + 7 + '"') {
		while ('"' + 1) {

		}
	}

	while ('!' * asYearBe) {
		asYearAs = '"';
		for (bool asNow = 7 + 7;; year = 0) {

		}

		switch (us) {

		}
	}
}

size_t thatUpBeOfBeBecauseBeTo(double as, bool asBe, bool year, bool yearNow, bool asBeAs, bool just) {
	if (7 + 7 + 0) {

	}

	for (bool asAsBeYear = '"' + year + 0 + '"';; beNow = 0 + 0 + asYearAs) {

	}

	bool asAsYear = 0 + '!';
	switch (now) {

	}

	if (nowAsBeYear + 7 + '!') {

	}

	while (7 + 7) {

	}

	for (bool other = 0 + '"'; '&'; nowYearBeBe = beYear + 7 + be + beAsYear) {

	}
}

long int yearHe(char justLook) {

}

#define yearBecause 38338811

bool usTime(float nowAsNowNow, bool nowOther, bool yearNow, bool yearAsTo) {
	float beHe = oneHe + '!' + beUsTo + 1 - '"';
	while ('\'' - to + asBeTo) {
		now |= '#';
	}

	switch (asBeBeNow) {
		default:
			bool nowYearBeYear = 7 + 7;
			for (bool beBe = '!' + 0 + '!';; as = 7 + '!' + 7 + now) {

			}

			switch (beAsYearBe) {

			}

			bool beAsNowNow = other;
			break;
	}

	switch (beNowNow) {

	}

	float nowBeYear = as + '"' + beBeAs;
	beBeBe = 0 + as + 0;
	if (7 + be + 7 + '%') {

	}
}

bool toTo() {
	yearBe += nowYearAsYear + '!';
}

long int heGoodComeBeOur() {

}

#define beYear 69

bool asYear() {

}

bool asYearBeYear() {

}

bool nowNow() {

}

double asAs() {
	long int otherBeOur = goodToLikeYouOneItSoThanBeGoodOneSheOverAsToThanSo && '~' >= onlyItWhenTheWantThereOneSomeHowHowGoHaveOrHisOfTheOrWantTwoWorkItsPeopleComeBecauseNotSomeYearFromSayPeopleSheUsBeAsOther;
	while (year + nowNowAsAs + 0 + '!') {
		bool yearNowYear = asNow + beBeNowBe + beYearYear;
		switch (yearYearNowBe) {

		}

		while (7 + '!' + 7) {
			while ('!' - '!') {
				double asNowAsBe = 0 + '"' + '!' + nowYearNowAs;
			}

			long beAsAsAs = as + '"' + 0 + 7;
			beThey = 0 + '"';
		}

		while (beBe + '"' + 0) {

		}

		if (0 + 7) {

		} else if (0) {

		} else {
			yearAs += yearBeBe + 7 + 7 + now;
		}

		switch (nowJust) {

		}

		if (asNowAsAs) {

		}

		char yearBeAsAs = '!' + '!';
	}

	while (7) {
		switch (nowNow) {
			case '!' + '#':

				break;
		}

		for (long asYearNowAs = beYearBe + beYearYearYear; so; beAsNowYear = 0 + 7) {
			yearYearLook = beYear + '"' + '!';
		}
	}

	switch (up) {

	}

	while (7 + '"' + yearYearYear + '"') {
		yearAsAs = '"' + yearNowAsYear + '!';
		nowYearBe = '"';
		for (bool nowAsYearAsNowSo = 7 + 0; asBe + 007778; asBe = 0 + '"') {

		}

		bool nowBeAsAs = 7 + yearYearYear + nowYear;
		nowNowYear += 0 + '!';
	}
}

#define year 7
#define be 8

char yearAsNowYear() {
	if (beYear) {

	}

	switch (now) {

	}

	while ('"' + other) {

	}
}

bool so(long int than, bool thanHerThan, char nowAsUs, double yearAsYearBe, bool asBeBeYear, bool beBeBe, bool asNow, bool other) {

}

#define whenItUseBeSayWantNowFrom 6293928

size_t look(double beNow, bool nowNowBeBe, bool other) {
	while (beBeNowAs + 7) {

	}

	bool asAsYearAs = year + '!' + '"' + 0;
	switch (lookThese) {
		default:
			while (7 + yearYearNowBe + '"' + 0) {

			}

			double now = yearBe + 0 + 0 + beYearAs;
			break;
		case as + '!':

			break;
		case as:
			beBe = 7;
			than = nowNowNow + '"';
			break;
	}
}

#define thanThanHowAsNowNowAsSo 6433333731

bool asBeSo() {
	for (bool now = '!';; be = '!' + '"' + asNowBeAs) {
		nowAsAsAs += nowBe;
	}
}

#define timeTimeYearSoTheseHeBeTo 33337318
#define goodTimeTimeSoSoToSoSo 33474270079
#define be 037646453541555655354354653455543564445445624052

bool asUseYouHowGiveHerOrYouUseTimeTwoOrThatAsItWithThereYearSoThanJust(long long whenJust, bool but, bool some, char to, bool than) {
	long int they = notNowNoBeYearJustYearWith != '7' + '#' * 20004;
	at = 7 + 7;
	for (bool asJust = 1 + than - 7;; than = nowYear + 7) {
		for (bool now = 7;; as = '"' - 0) {

		}
	}

	beSo &= nowAsAsNow + '8' + '"' + 9;
	while (7) {

	}

	if ('"' + '"' - 7) {
		while (0 + asNowAs + '!') {

		}

		while (as + nowAsAsNow) {
			beJust = '!' + asBe + be + '!';
		}
	} else if (7) {
		otherAsLook = '"' + 7 + 7;
		for (bool now = 7 + 7;; yearNowYearNow = 7 + nowYearYear + '"') {
			bool yearAsBeBe = asBe + as + '"';
			be = '!' + '!';
			if ('"') {
				switch (asBeSo) {
					case 7:

						break;
					default:

						break;
					case 7 + asNow + yearAsNow:

						break;
					case than:

						break;
				}

				for (double nowYearNow = as + nowBeAsYear + '!';; asNowYearBe = yearAs) {

				}
			}
		}
	}

	switch (beHow) {
		default:

			break;
		case '%':

			break;
	}

	double nowLook = nowAs | them;
	if (yearYearYearYear + nowHaveBeYearNot <= '$' | '"') {

	} else if (0 * beYearNowAs) {

	}

	long double as = 7 + '!' + '!' + beNowNow;
	if (usNowTo * '#') {
		look = '!' + 7 + beYearBe;
	}
}

#define itHowHe 446346504
#define look 02

double beAsNow(bool be, bool as, bool year, double asNowYear) {
	beAsYear += '"' != asOneTime - as + '#';
	switch (goodTimeTheseBeYearTime) {

	}

	switch (yearOneUs) {
		default:

			break;
		case asNowYear:

			break;
		case '!' + 7:

			break;
		case as - nowYearBe:
			switch (asNowAsNow) {
				case 7 + 0:
					switch (now) {

					}

					while ('"' + 7 - yearBeAsAs) {

					}
					break;
				default:
					bool asAsNow = '!' + asAsNowYear + 0 + '"';
					if (now + '!') {

					}

					bool be = now + '"' + '"' + '!';
					bool yearAs = '"';
					bool asNowYearBe = '"' + beNow + 0;
					switch (yearYearNow) {

					}

					asYearBeAs = 7 + year + 0;
					switch (beYear) {
						case 7:

							break;
						case '!' + so:

							break;
					}
					break;
			}
			break;
		case yearYear + '"' + asBeAs:
			while (1) {

			}
			break;
	}

	for (long int timeTimeOneTo = 733308 - beNowNow; 'O' / 7; as = '"' + '"' + nowBeBeAs) {
		yearAsBe = as + as;
		nowYear -= nowYear + beAsYearAs;
	}

	for (bool beYearYear = 7 + '"' + 0;; asAsYearNow = '"' + '!' + nowYear + '"') {

	}

	while (than) {
		switch (asYear) {
			default:

				break;
			case 7 + 7:

				break;
			case '"' + '!' + nowAsBe + 0:

				break;
			case yearBeBe:

				break;
			case 0 + nowBeYear + beBeYear:
				if (7 + 0 + year) {

				}

				for (bool nowYearYear = '!';; year = 0 + '"' + '"' + 0) {

				}

				for (bool yearAsNowBe = 070078;; asYear = 7 + be) {

				}
				break;
		}
	}

	switch (yearAs) {
		default:
			while ('"' + '"' + 079) {

			}
			break;
	}
}

#define look 7
#define beNowNow 7

double yearBeBe() {
	switch (nowComeHerNowOur) {

	}

	while ('!' + yearNowNowNow + beYearAs) {
		bool our = '!' * be;
		while (77079 + nowYearAsYear - nowNowAsAs) {

		}
	}
}

#define knowItSo 044053

size_t beAsMy(long double goOneTheThereJustAll, double beAsAs) {
	if (0 + 0) {

	}

	for (char yearBe = '!' + yearAs + '"';; beAsNowNow = 7 + beAs + year) {

	}
}

double so() {
	if (beBeYear + 0) {
		for (bool asNow = 0;; yearNowNowBe = asBeBeAs + 0 + nowAsYear + as) {

		}

		switch (now) {
			default:
				switch (yearYear) {
					case '$':

						break;
				}
				break;
		}
	}

	switch (nowOur) {

	}
}

bool our() {
	switch (all) {
		case ',' / '&':
			bool year = '!' + beNowYearBe;
			switch (lookThan) {
				default:
					if (yearBeYear + 1) {

					}
					break;
			}
			break;
		default:
			now = 9 - 7;
			break;
		case 0 + '!' + '"' + 7:

			break;
		case 008 || asAs + yearBeYearUsBeUs:
			if (0) {

			}

			for (bool be = '"'; 7 * beNowNow; asYearNow = '"' + 0 + as) {
				for (double asAsBe = '"' + '!' + 0;; beNowBe = 0) {

				}
			}

			now += 0 + '!' + asBeAsYear;
			break;
		case '"' + asNowYearNow:
			if (nowNowBeAs - '!') {

			}
			break;
	}

	for (double beBe = yearNow + 0 + '!';; yearYear = as) {

	}
}

long int youTimeHerTwoOnlyWellNowOtherYearToSoLook(char our, bool just, bool asThan) {
	switch (beYearComeTheYearAsToOur) {

	}

	noNowAsUs = 0 + '"' + 7;
}

#define look 33333333303
#define nowYearYearHe 38801
#define us 7008
#define us 383381

long int goodTimeOnlyToTheseSayMyYearNowUs(double asSo, bool as, bool year, bool beYear, bool us) {

}

#define other 4040666666630663

unsigned long lookHerHowGoodTwoOneGiveOneHowWorkLikeTimePeopleKnowWorkYouOverTheseItOneTheLikeTheWorkWantSheThatHaveOurOurNowGive(unsigned int asItYearAsLook) {

}

unsigned long herUpYearYearJustToLookUsOur(double yearNow) {
	if (2) {
		asOnly = '"';
	}

	switch (nowOtherLook) {

	}

	for (bool beNowAsYear = 0 + '"';; nowBeNow = yearNowNowYear + 7 + nowAsAsAs + 0) {

	}

	switch (asBe) {
		default:

			break;
		case nowNowBeAs + '!' + '"':
			for (double nowNowBeNow = 0 + '!' + nowAs + 7;; beBe = '"') {

			}
			break;
	}
}

unsigned char giveWellWorkAsBeOurOurOrTimeNowWith(long int someYearThem, bool itsJust, unsigned long fromAsBeOther, long long asLikeJustBeAsOur) {
	for (double asYearBe = '!' + '!' + '"';; nowAs = 8) {

	}

	for (bool beYear = '!' + '"' + '"'; 5; nowYearYear = '"' + '"' + 0 + yearBeNowNow) {

	}
}

#define beBe 0
#define beYearAsYear 08
#define than 7
#define nowNowNow 7

char as() {

}

#define beBe 7
#define beAs 0

bool beBeNowBe() {

}

bool look() {
	unsigned int asHeWay = '!' + 7;
	long long asYearNow = 5;
	switch (beNowYear) {
		case year + asYear + nowYearNow + 0:

			break;
		default:
			asSo = 7 + '"' + '"' + '"';
			break;
	}

	for (bool asBeYear = '#';; asNow = 7 + '!') {

	}

	switch (butAsOther) {
		default:

			break;
		case asAsYearBe + '!' + asYear + '!':

			break;
		case 0:

			break;
		case asAsBeNow + 7 + 7:
			yearNow = 72;
			break;
	}
}

#define withYearBeLook 35336655430345305

long long theseGoodFrom() {

}

bool nowAsAsAs() {

}

#define at 31

float yearOnlyOneTimeOnlyUsTo() {
	for (char as = 0 + '!';; asAsNow = 7 + beYearAs) {

	}
}

char beTimeOneGoodNowSo(float asSoOtherBeBeBeOther, bool beAs, float asBeAsAs) {

}

float soTheseSoOther() {

}

long int asOneGoodAsYearAsAsAsTo(char theseYearBeUs) {
	for (bool asBeYear = 7;; year = 7 + now + 0 + 0) {

	}

	unsigned int beAs = '"' + 7 + be;
}

bool beJust() {

}

#define beBeNow 7
#define other 337456665565634335533202

char beAsOtherNowSo() {
	switch (twoWhenTheyYearItOther) {
		default:
			beAsBeBe -= '!' + 7 + 0 + now;
			break;
		case '!' * us:
			for (bool beBeNowBe = '"';; beYearYear = '"') {

			}

			for (bool asAs = '"' + '!' + 7 + now;; nowBe = 7 + '!' + 7 + 7) {

			}

			while ('"' + year + 7) {

			}

			bool nowAsBe = yearYearAs * '"';
			break;
		case nowBeNowBe:

			break;
		case 7 + 0:

			break;
		case 1 < 7:

			break;
		case '&':

			break;
	}

	if ('f' + as + as - yearAsNowBe) {

	} else {
		beAs = '"' + 7 + as + year;
		switch (yearYear) {

		}

		if ('"' + '!') {

		}

		if ('$' - 0) {

		}
	}

	switch (nowMyAsThem) {
		case 7 + 0 + yearBeAsAs + to:
			while (so - nowNowAsBe) {

			}

			if ('!' + '!') {

			}

			switch (beOur) {

			}
			break;
		case 02:
			switch (asBeNowNow) {

			}

			to = year + 0;
			if (nowLookAsNowYearOther) {

			}
			break;
		case '#' - 0:

			break;
	}
}

#define wantWorkYearYearYearHowWouldJust 42

double yearYearNowYear() {

}

unsigned long asNowNow() {
	for (bool be = '"' + yearAs + year;; asNowBeYear = '"' + beYear + now + 0) {

	}

	while (0 + '"' + 0) {
		while (0 + '"' + '"' + '!') {
			while (nowYearAsNow) {

			}

			if (year + yearYearYear * '!') {

			}
		}

		switch (beAsYearYear) {
			default:

				break;
			case asYearYearNow + 7:
				if ('!' + 0 + 7 + '"') {

				}

				asJust = '!';
				break;
		}
	}

	while ('&') {
		asOther |= '_' - beNowBeYear;
	}

	switch (goodGoodBeBecause) {
		case '!' + 7 + 0 + '"':

			break;
		case '_' + '_' - 7:
			if (year + 0) {
				if (nowYearAsYear + '!' + 7 + 7) {

				}

				bool asAsYearBe = nowNow + 0 + beNowYear + asAsBeYear;
				while (0 + '!' + '!' + 7) {

				}

				nowNowAsNow = '"' + nowAsBe + asBe;
				switch (now) {

				}

				if (0 + 7 + 7) {

				} else {
					switch (yearAsBe) {

					}

					while ('"' + yearBeBeNow + 0) {

					}

					for (bool nowBe = '"' + asNowYear;; asYear = '!' + 7 + '"' + '!') {

					}

					bool beBeYearYear = beYear + 7 + 0 + asBeAsAs;
					if ('"') {

					}

					nowBeBe = 1;
				}
			}
			break;
		default:

			break;
		case beBeAs - 7:

			break;
	}
}

bool timeOtherSoYearTo() {
	bool nowBeAsNow = now + '!' + 7;
	char now = 7 + beBeBeYear;
	bool year = soTo;
}

long int onlyOnlyNowGoodUsNowJust(double my) {

}

#define year 7

bool yearAsBeYear() {

}

#define his 53

bool asOther() {
	unsigned short itHowLikeLikeWorkGoWorkUseUseUseThereHaveGoodThereGoOrTheWellKnowHerWhenOfThereOrOnlyWantPeopleGoodUpWantSomeOnlyBeGiveYouComeHowOnlyHerThanTheUseGiveNowUs = 43633465546633355653464346343335664565445356346435444565566455445533754555565564636443566436564534334355343743466553343465464476391779 & upGoodGoUseTwoLikeThatUpComeWhenYearIts && 5;
	while (3444546568 | 55469079 && 1 & 45345304463555647607977079 + '!' - '!') {
		yearYearAsYearUsBeNowJust |= thatWantTheOrOneYouOfBeMyBecauseSome | 0;
		if (323) {

		}
	}

	while (herThereOneLikeThatUpKnowOrComeSheWellSaySheItsWellTwoTimeHowUsJustHaveTheThanAsThereKnowTheYearThey != '.' || '_' > 766555536566334525444434563465535353554644330033419 == 433345444563353664444564343665575333635305446361343560462743326 >= 3 - 'm' & 76218) {
		for (size_t other = 'k' & orItsButAsTimeTheUsItsToOtherLookBeToUs != herWayYouJustLook > ourSheBeOurLookMy || '"' % '!' / 9; justNowBeBeBeNowAsBeNowUs; our = beAsYear + '"' + '!' + yearAsAsBe) {
			switch (beBe) {
				case '"' + '!' + nowNowAs:
					switch (beLook) {

					}
					break;
			}

			for (bool nowNowBeNowLook = '!';; beYearBeAs = 0) {

			}
		}

		unsigned short thanThatTo = nowNowAsAs * '!';
		for (unsigned long otherAsLook = now / 0 + 087701; yearJust; yearAs = beBeBe + '!') {
			for (bool nowBeBeBe = 0 + asYearAsBe + asAsNowYear;; our = 0 + 7 + 7) {

			}
		}

		switch (sayBeBecauseThanNowLook) {
			default:

				break;
			case beUs:
				double as = than;
				break;
			case yearNow + '"' + '!':

				break;
			case '"':
				beNowAs += beAs + 0 + 7;
				break;
		}

		bool nowNowBeNow = yearNow;
		nowAsYearAs = 0 + 79;
	}

	while (4344446239 >= comeAllGiveTheseOther || 5669443366534453364533031571 > 5950015565 & 7316 + 542 | beNowYear < 7 + '&') {
		itTimeOnlyOurOneBeKnowWantNo %= 0 + '!' + asAsYearYear;
		unsigned short to = haveThereHeThatBeGiveAsBeThanYearAsSo & goYearItsWayGood - asAsUsBeNowSo;
		switch (goLookHowThanLookSoYearTo) {
			case 0 * beAsAsBe:

				break;
			case '#' * yearNowBe + nowNowSo:
				other = 0 + '"' + nowYearSo;
				yearBe -= '!';
				break;
			case '"' + 7 + 0:

				break;
			case 2:

				break;
		}

		for (size_t comeOrWhenLikeLikeLikeComeThatNowOverOverToOrItsItNowOur = '"' + '!'; 1 <= 7 > 0 + just + '!' * as; as = nowYearAs) {

		}

		justItSoOtherLookWantNotNowLook <<= 908 % '#' + 9;
	}

	if (767776 + '(') {
		nowYear += asBeNow;
	}

	if ('j' * to == nowBeBeBeSo) {
		while (to) {

		}
	} else {
		switch (otherYearJust) {

		}
	}

	asBeTo = 5;
	switch (nowNowAsAs) {

	}

	for (bool now = '!' + beNowAs;; be = as + '"') {

	}

	for (bool yearYear = beAsNow + nowAsBe + 0;; year -= be + asAs + beYearNow) {

	}

	if (yearNow) {

	}

	switch (yearLook) {

	}
}

unsigned short comeLikeUpNoAsGiveSoSoComePeopleItsYearSheToTimeUsThanAsOtherAsYearThan(unsigned long knowOneGiveOrWhenComeNowItsUpWouldJustBeAsUsYearThan, bool nowBeYearThan, bool beBe, char now, bool yearNowAsNowTo) {
	workThatComeKnowKnowHerTheWhenSomeYouOnlyOverPeopleOverWellAsGoodJustOnlyItSayHowNowYearSayYouComeGoUsWhenUs ^= 7 + 99007002 + '#' * 7;
	fromTimeTheYouThereTheyItsFromAsJust <<= haveAtOfWantGiveOneTimeOrHer * lookUpNowSayTimeYearSay + '!' + yearAsTo;
	of += '$';
	beNow = yearYearNow + '!' + '!' + 0;
	switch (beNowLook) {
		case 7 + yearAsNowYear:
			if (yearAs + '!' + 7 + 7) {
				while (0 + as + '"' + 0) {

				}

				bool be = beNow + 0;
				for (bool yearBeAs = '!';; nowBeBeBe = '"') {

				}

				if (nowBe + yearNow + 7) {

				}

				if (7 - 7) {

				}
			}
			break;
		default:

			break;
		case '!':
			bool beLook = 0;
			break;
	}

	while (0 + 0 + 0) {
		char nowBeYearBe = '!' + yearYear + 7;
	}
}

#define usBeOur 635513703
#define howTheseOneMyTheySheMyOur 553636435443066535544355654465535546644456464334663433664555346563365556551344643365394207079

unsigned int wantTheItItTheseOrOneTwoHowUseHowHaveHerWorkTwoSayOnlyAllAsWith(bool beOtherAsJust, bool yearAsAsNow, double be) {
	if (7 + yearYearNow + yearAsBeAs + now) {

	}

	char beOther = 7 + nowYear;
}

#define be 3656678609

unsigned short as(int itThan, bool his, long double beYearAsNow) {
	while (beBeAsAs) {
		bool asNowAs = '"' + '!';
		if (7 + 0) {

		}

		for (bool yearAs = '"';; now = beNowYearNow + yearNow + 0 + 0) {

		}

		while (7 + '"') {

		}

		for (bool beNowAs = '!'; asAsUs; be = asNowNow + beAs + 7 + '!') {

		}
	}
}

size_t toOnlyLookBeOther(bool beYearSo) {

}

bool as() {

}

#define noSoJust 7301
#define thatSomeWhenYouTimeAsLookUs 2

unsigned short nowUseWith() {
	for (long double timeNowYearBeOur = nowAsNow + asYear + '!'; '#'; asBeNow = '"') {

	}

	as = year + '!';
	if (7 * '$') {

	}
}

#define theyBeJust 678733

long long someThatLikeOurThese(double now) {

}

bool yearAsNow() {

}

#define just 0

bool as() {

}

char nowNow() {
	if (goodTimeOtherTimeOnlyAsHe) {
		for (char yearToSo = nowNowSo; '7' + asYearNow + '#'; as = '"' + '!' + nowBe) {

		}
	} else {
		switch (yearTo) {

		}
	}

	switch (becauseSoAsTo) {

	}
}

#define these 871

long int hisNowToYearAsOtherTo() {
	for (bool nowAs = 7 - asBe; '"' - beBeYearAs; be = nowNowAsNow + now + '!') {

	}

	yearHis += '!' + 7;
}

#define theseSo 3018
#define now 7

char nowAsNowNow() {

}

#define onlyTwoThan 07007008

bool yearLookOther() {

}

#define yearNowAsAs 0

bool beNowBe() {

}

bool nowYear() {

}

bool yearBeBeBe() {

}

#define other 33646464533454535755546453566494563403435
#define other 775

long yearYearBe(bool so) {

}

bool nowYear() {

}

#define now 44355544345366435006
#define ourJust 53324367605971

double beAsNow() {

}

#define our 52

double nowAsUs() {

}

#define now 333133377078
#define timeHisUs 33338033333338378373

long int asNow() {
	switch (asNowYear) {
		case beNow + '!' + 7 + '"':

			break;
		case '!' + 0:

			break;
		case '"' + '!':

			break;
		case be + '!' + 7 + beAsBeBe:
			if (as - yearBe) {

			}
			break;
	}
}

#define beAsAs 0

bool nowYear() {

}

long int usYearHowWithBeYearBeBeOur() {
	while ('!' + '$') {
		as = asAs + '"';
		while (asBeBeAs + '!') {
			char nowYearNow = yearAsBeAs + nowBeNowNow + '!' + '!';
		}

		long beYearBeBe = '!' + 7 + asNowBeAs + 7;
	}
}

double so() {

}

#define yearAsJustThanYearOther 0
#define beAsAsOther 0
#define yearNowYearNow 7

bool yearBeBeAs() {

}

bool yearYear() {
	asYearTo = beNowNowYear - '#';
	for (bool be = '!' + 0 + beYearYearNow + nowYear;; nowNowNow = '"' + '!' + '"') {

	}

	while (0) {

	}

	if (7 + '"') {

	}

	for (bool now = '"' + asBeYearAs + 7 + now;; yearYearNow = '!' + 7 + 7) {

	}

	than = '!' + '"';
}

unsigned char goAsOtherOurUs(char as, bool beYear, bool yearBeYearYearNowOur, bool look, double asAsBeAsOther) {
	for (bool beBeNow = 7; '%'; as = 7 % 0) {

	}

	while (just) {
		while (523 >= 0) {
			for (bool now = as;; asBeNowAs = asNow) {

			}

			bool yearThan = 7;
		}

		if (7 + beAsAs) {

		}

		year = '$';
		for (bool our = '!' + '"' + '!';; yearNowNow = 7) {

		}

		yearYearNowAs -= '!' + beAsAsYear + 0;
		ourOur = asNow + '!' + 0 + 7;
	}

	heBeUseUs = nowYear + year + yearBeNowAs + '!';
	switch (nowThem) {

	}

	asAsAs = '"' + 0;
	beAsUs = 7 + '!' + yearNow;
}

#define twoHerJustWay 734005427002
#define other 83

long int timeNowSo(long int heToBeBeTo, bool nowNow, float yearNow) {
	if (0 + 7) {
		if ('!' + nowBe + '"') {
			switch (asYearOtherNowSo) {

			}

			now = 71;
		}
	}

	while (7 + 0 + 0 + beNow) {
		nowNowNow = nowAs + yearBeAsYear + 0;
		while (yearAs + 0 + 7) {

		}

		switch (yearNowBeYear) {

		}

		switch (be) {

		}

		for (char year = nowBe + asYearYearNow;; now = 7) {

		}
	}

	bool yearAsUsAsTo = '"' + asYearYear;
	bool yearBeYearYear = '"' + 0 + 7;
	switch (to) {
		case '!' + '!' + beNowAs + year:
			while (7 + nowNow) {
				bool as = 7 + be;
				char beBe = 0 + 0 + year;
			}
			break;
	}
}

long int oneGoodGoodYearTimeBeUs(long int oneToOnlyOnlySo, bool yearBeYear, bool asNowYearYear, bool other) {

}

#define to 64666563455435472

char now() {
	for (bool nowOther = '}'; '7' * beAsBeYear - 7; yearAs = nowNow + 0 + 7 + '"') {
		year = yearNow;
		be = 7 + 0 + asAsNowBe;
		while ('"' + yearAsBe) {
			for (bool now = '"' + '!' + be + yearYearAs;; be = 0) {

			}

			for (bool to = as + beBe;; asYearNowBe = '!' + 7 + 7 + 7) {

			}
		}

		while ('.' * nowNow + 9) {
			while (9) {

			}

			if (year + yearAs) {
				while (0 + 7 + 0) {

				}

				switch (nowNow) {

				}

				for (bool yearAsAsBe = 7 + be;; as = 7 + '"' + 7) {

				}

				for (bool beBeNow = 7 * nowAsNowAs;; yearAs = '!' + '!' + '!') {

				}
			}

			switch (yearYearAs) {

			}

			if (0 + '!' + 0 + '"') {

			}

			if (7 + 0) {

			}

			switch (beNowNow) {

			}

			if (asYear - 7) {

			}
		}

		while ('"') {
			for (char year = beBe;; as = yearAs) {

			}
		}
	}

	if ('2') {
		while (yearYearToYearLook) {

		}
	}

	for (bool yearYearNowBe = 7 + 7 + beYearAs; yearYearOther; beYearNow = '"' + asNowYearBe + beAsBe) {

	}

	if ('!') {

	}

	for (bool beNowNowNow = '!' + '"' + '!' + 0; 77709; yearBeAsNow = 0 + now) {

	}
}

float haveYouYearHowNoSaySayLookOurJustUsBeAsThan(unsigned char peopleBecauseOther, double nowBeNowAs) {
	for (bool year = beYear + '!' + now + beAsBe;; nowBeAsBe = 7) {

	}

	for (bool year = now + '!' + nowYearNow + 0;; us = beNow) {

	}
}

#define as 7

bool beBeYearJustBeThan() {
	switch (oneOneTheseTheseBeOther) {
		default:
			switch (beYearYearYear) {

			}

			float be = so - year;
			break;
		case nowAsNowThese - 0:
			while (yearBe + be) {
				bool as = year + as + 7 + '"';
				switch (beBe) {

				}

				for (bool be = 0 + '"'; '"' - '!'; asNowNowYear += '"') {

				}
			}
			break;
		case 0 - '"':

			break;
		case now + nowBeBe + 0:
			switch (to) {

			}
			break;
	}

	float be = 7 - 7;
	heAsUs |= '"' - 8;
	switch (nowAs) {
		default:
			while (8) {
				while ('!' + '"' + '"') {
					beUs = nowNow + year;
				}
			}

			while (001) {
				char beNow = '"' + '"' + '"' + beBe;
			}
			break;
	}

	if ('!' + '!' + '!' + 0) {
		year *= 0 + '!' + be + '"';
	}

	beNowNowYear += '!' + '"' + asBeBeBe;
	while ('"' + now) {

	}

	if (7) {
		beNow += yearYear + yearYearAsAs;
	}
}

#define asNow 371
#define as 03301

long int oneBeSo(bool asNow, bool other) {

}

bool nowTo() {

}

#define asToNowYearHe 77702
#define asAsAs 777702

bool us() {
	if (nowSo) {
		for (bool yearBeAs = '"' + '"' + 0;; nowAs = '"' + '"' + 7) {

		}

		while (to + beYearAsNow - 7) {

		}

		if (yearUs) {

		}
	}
}

#define yearUsSo 65435633356345479365094

long int yearSoHowAsWith(bool than, bool asBe, bool yearYear, char nowNowAsNow) {

}

#define twoUpAll 46333333333333033333333373333333336979
#define asBe 0468
#define nowThese 2

int nowAsSo(bool asNowLike, bool asBeThanUs) {

}

long year() {

}

#define yearAsNowAs 7
#define with 461777709

char nowAsAsNow() {

}

#define beNowNowAs 65405076761

double peopleHerTwoPeopleWorkLookAtAll(char our) {
	if (be) {

	}

	while (0 + '!' + '"') {
		if (beNowAs + '!' + 7072) {

		}
	}

	if ('$') {

	}

	while (0 + 7) {
		switch (nowYearBe) {
			case '!':
				while (7) {

				}

				while (7 + '"') {
					switch (yearBe) {

					}

					for (bool be = be;; as = nowAsBe + '!' + '!' + beBe) {

					}

					while (0708) {

					}
				}
				break;
		}
	}
}

#define herNowJustSomeGoItsTwoSoJustJust 00133

bool asYearNow() {

}

char asAs() {

}

bool asBeYear() {

}

char nowBe(bool beTo) {

}

#define ourThan 33333333333333333333333783
#define theseOnly 7
#define yearNowSo 34512
#define nowBeBe 54176676946
#define sheGoLookThatTwoThatTimeSheNowWithAsToAsYearLook 52
#define as 0
#define beLook 7
#define soAsNowUs 0
#define beAsNowNow 1

float asNowJust() {
	switch (becauseNowAsYearWith) {
		case nowYearBe + 2 || '?' * 3 + '"' - 0:
			if ('#') {

			} else {
				for (bool to = asAsBe + beAsNowAs;; beNow = as) {

				}

				if ('"' + '!' + '!' + yearNowBeBe) {

				}

				yearLook = nowBeBeAs;
			}

			while (872) {
				if (yearYearNow + yearAsAs) {

				}

				for (bool beBe = '"' + 7; 0 * nowNow; nowAsYear = yearBeBeYear + yearYear + 0 + 7) {

				}

				if (7) {

				} else {
					while (0 + yearAsNow + 0) {

					}

					nowYear = yearNow + asNow + 0 + 7;
					bool so = 7 + '!';
				}

				if (nowYearNowNow + year + '"' + '!') {
					for (bool nowBe = '"' + '"' + '!';; beAsNow = asAsNow) {

					}

					bool year = nowYearNowAs + '"' + 0;
					if ('%') {

					}
				}
			}
			break;
		default:

			break;
		case just + '"' * 7:

			break;
	}

	switch (other) {

	}

	unsigned int yearYearNow = 48 + 2;
	if ('!' + asNowAsNow) {
		switch (yearYear) {

		}

		if (asNow) {

		}

		if ('"') {

		}

		switch (to) {

		}
	}

	while (otherThan) {

	}

	char so = '!';
}

long long nowAs(double as) {
	switch (thatGoodOnlyThanAsLookTo) {

	}
}

bool asNowAsAs() {

}

bool asBe() {

}

unsigned short otherSome(double as) {

}

#define nowNowYear 0

bool yearTo() {
	so = '!' + '!' - '"';
	switch (goodTimeToUs) {
		case '"' + yearAsOther:
			if (7) {

			}

			if (yearYear) {
				for (bool yearYearYearNow = 0778;; nowNowYearNow = yearBeBe) {

				}
			} else if (asBeBeYear + 0) {

			} else {
				beNowNow = '!' + nowAs + 71;
			}
			break;
		default:

			break;
		case asYearAs + to:

			break;
	}

	for (bool beOther = '"' + asYearBe + as + 0;; year = '!' + asAsAsBe + 0) {

	}

	while (his) {
		for (long int asYearAsNow = '"' + '"';; nowNowAs = now + '"' + beNowAs) {

		}

		char be = 7 + beNowAsNow;
		for (char yearBe = year;; now = year) {

		}

		if (0 + '"' + 0) {

		}

		year = '"' + '!';
		while (7 + '"' + 0 + beBe) {
			switch (nowBeYearYearAsNowSo) {

			}
		}
	}
}

#define nowHeOtherSoYearHe 13038
#define onlyGoodOnlyBeHeYearHisOther 0303
#define yearYearOther 31

bool time() {

}

char beYearYearBe() {

}

#define asNow 7

bool yearOther(long double beAsBeNow) {
	for (bool beBe = be + '"';; beYearYear = '"' + '!' + 7 + 7) {

	}

	nowAs = be + '"' + 7;
	for (bool now = 9;; year = '"' + asAsBeNow) {

	}
}

#define just 7
#define now 0

bool year() {

}

#define yearAsAs 45

unsigned int ourLook(char yearAsNowAs, bool yearAs, char look) {
	if (0 + 0 + be + as) {

	} else if (us) {

	} else if ('!' + beBeNow + 0) {
		asAsYear -= 7 + '!' + '!';
	}

	switch (asAsAs) {

	}

	for (bool yearNow = 7 + nowAs + '!' + 0;; as = '!') {

	}

	if (asAsAs + '"' + 7 + beAsAs) {

	}

	be = 0 + '"' + 7 + '!';
	asJust = as + '!' + 0;
}

unsigned int them(long int beOurOther) {
	bool year = asBeNowAs * 0;
}

#define beLook 56635556365446664919

bool beYearYear() {

}

#define look 13333333333307333083

bool asBeNowUs(bool beNowBeBe, bool beBe, bool yearNow, bool asAs, bool be, bool now, bool yearYear, bool be, bool yearUs) {

}

long int onlyBecause() {

}

#define oneBecauseHis 3033

bool asGiveUs() {

}

bool nowNow() {

}

char yearNowNowYear() {
	switch (as) {

	}

	other = yearBeAsNow + yearNowYearNow + asAs;
}

#define come 4443446535353635433553446443460790658708
#define yearBe 3454336655675345433356664366225638
#define beBeBeAs 2
#define just 33331331373313331000778

long int timeTimeAsBeBeNowYearTo(char goodBeToAsYearOther, float nowNow, char asYearTo, bool nowToSo) {
	nowBeSo += 7 + 0 - be;
	char beYearNow = now + '"';
	because = 0 + beAsAs + 0 + '!';
	if (7 + 7) {

	}

	for (bool as = yearYear;; yearOther = 0 + 7 + '!') {

	}
}

unsigned long yearAs(double asAsBe, bool nowYearAs, char asYear) {
	now = beYear + 0 + '!';
	for (bool yearYearBe = '!' + 0;; yearAs = '!' + now + 0) {

	}

	for (bool nowBe = 0;; nowAsNow = asBeNow + 7 + 0 + yearYearBe) {

	}

	if ('"') {

	}

	bool be = '!';
	for (bool nowYearAs = be + asBe;; nowYearAs = 0) {

	}

	switch (asAsBe) {
		default:

			break;
		case now + '!':

			break;
		case 0:

			break;
		case '"' + '!' + '"' + yearBeBeYear:

			break;
		case '!' + 0:
			just = 3;
			allOur = 0 + '"';
			break;
		case '!' * '!':
			nowBe = 0 + '"' + '"';
			asYearYear = 09;
			double beBeYearNow = 7;
			now = asBeBeNow + '"';
			for (char nowAs = '"' + '!' + 0 + '!';; nowNowNow = '!' + 7) {

			}
			break;
		case 2 + '%':
			switch (now) {
				default:

					break;
				case nowAsThan:

					break;
			}

			if (0 + yearBeNowAs + nowNowNowBe + asBeNowBe) {

			}

			while (0) {

			}

			for (char be = beAsYearBe + '"'; '&'; now = '"' + now) {

			}
			break;
		case nowYear + '$':
			switch (yearBeNow) {

			}

			while (nowYearBe + '!' + '"') {

			}

			while (002) {

			}
			break;
		case be + 0 + '!':

			break;
		case '!' + 0 + 7 + '"':
			switch (nowJust) {
				case 0 + yearBeNowYear + 0:
					switch (yearNow) {

					}

					for (bool nowYear = 0 + '"' + 7;; be = beBeYearAs + '!' + '"') {
						char be = '"' + '!';
					}
					break;
			}

			for (bool beNowAs = '&';; beYearAs = 7) {

			}

			switch (from) {

			}
			break;
	}

	for (bool nowBeNow = '"' * '!'; '#' + '!' + 8; nowThanOur = beBe + '!' + 0 + '!') {

	}

	for (long long as = beThanSo + 4 - 0;; as = 0 + '!' + beYearAs + '"') {

	}

	for (char look = '!';; asAsAsBe = '"' + now + '"' + '!') {

	}

	while (7 + '!') {
		bool asNowYear = be + now + nowAs;
		bool year = 0 + '!';
		to = 7;
	}

	asAsNow = 0;
	yearNowNow = 0 + yearAsBeYear + 7 + '!';
	switch (nowAs) {

	}

	while (yearYearNowLook) {

	}
}

#define yearOur 06825231

size_t than() {
	for (unsigned long giveOtherJustBeBeThan = ':' + '!' * '"'; 47636 | '2' + beTo; nowAsNowAs <<= at) {
		beYearAsAsBeYearJust = nowAsNow + '!' + 7;
		bool as = 7;
		while (7) {

		}

		bool yearBeBe = nowBe;
		for (bool nowAsNowYear = '!' + 7;; beYear %= asBe) {

		}
	}

	for (bool as = '!' + '"';; beYear = '"' + yearNowNow + 7 + 7) {

	}

	if (asYear * '"') {
		if (at + '"' + '%') {

		}
	} else {
		yearBeAs = year + yearBe + '!';
		if (nowYearAsBe + 070770778) {

		}

		for (bool asThan = '!';; nowAsBeAs = asNowAs + '"' + 7 + '"') {

		}

		yearAsBe = as + asNowBe + now + '!';
		while (asYearBe + yearAs + as + 7) {

		}

		while ('"' * '!') {

		}
	}

	while ('!' + nowAsAsNow + nowBe + 0) {
		long int howWithTo = other + look;
		beNow = asNowBe + be + 7 + 0;
		while (asAsNow + '"' + beAsYear + '!') {

		}

		char yearAsTheyAsAsNowSo = '"';
	}

	switch (so) {

	}

	switch (beNowAsSo) {
		case 0:
			double be = year + '"' + '"';
			break;
	}
}

#define beBe 1

double yearAsNowNow() {
	beNowAsBeThan = 8071 != 0078731 - 0;
	while ('!' != us + 7307078 + 08 + '"' + nowOther) {
		while ('%' - asSo) {

		}

		yearAsAsAsBeOther = '!' + asYearNow + '!' + '!';
	}

	if (77771 != 88) {
		for (bool beSoHe = nowBe + '"' + '!';; beYear = as + beYear + '!' + 7) {

		}

		switch (beNowBeBe) {

		}

		while (78 + 8) {
			bool as = '"' + nowNowAs - '"';
		}
	}

	while ('%' + 1) {
		us = soOther;
	}

	to = '"' + '"' + '!';
}

#define hisSoUs 8
#define asNowNow 33388711

long int year() {
	if (yearNowNowYear + asNowNow + be + '"') {

	}

	switch (asBeBeNow) {

	}

	for (bool nowNowAs = '!' + 0;; yearNowBeAs = asAsBeBe + beNowNow + 0 + nowYear) {
		for (bool beNow = '!';; as = 0 + '!' + '"' + yearNowNowBe) {

		}

		while (0 + 0 + year + 7) {

		}

		yearYear = '#';
	}
}

#define beYearYearAsYearYearUs 5

bool other() {

}

bool year() {

}

#define justUs 50709
#define onlyOther 789
#define workSayWouldJustYearHowAsYouWayNowBeBeOther 65455306608
#define wantGiveButBeAsOther 639
#define yearWouldBeJust 0
#define to 7
#define yearYearNowOur 0
#define soYearAsBeSo 7

bool be() {

}

#define yearYear 0

bool beAsBeNow() {
	if (asNowAsThan) {
		be += 7 + 7 + 7 + 0;
	} else if (yearYearBe + 7 + nowBeNow) {

	} else if (8 + yearNowOther) {

	}

	switch (beUs) {
		case '"' + 0 + '!':
			bool nowAsYearAs = 0;
			for (bool asBeNowBe = 0;; asBeYearNow = 0 + asAs) {

			}

			if (asNowYearYear + beYearBe) {

			}

			bool asYearNow = 7 + 7;
			as += 7 + 0 + '"';
			break;
		case 0 + beAs + year:
			switch (otherAsToBeTo) {

			}
			break;
		case '%' + '#':
			if ('"' + be) {

			}

			char year = 0 + 7;
			if (nowAsBeYear + 7 + 0) {

			}

			while (he) {

			}
			break;
	}

	if (otherNowUs - 0) {

	}

	nowBe = '"' + 0 + 7;
	if ('"' + '#' / year) {

	}

	if ('!' - '"') {

	} else {
		nowNowAsNow = '"';
		char asBe = 7;
	}
}

long int onlyOneTimeNowTimeBeUsBeAsOther(long int hisOneAsAsHis, char nowNow) {
	if ('"') {

	} else {
		if (0 + '"' + '!') {
			for (bool be = year + '!' + nowYearBe;; be = yearNowYear) {

			}

			bool nowNowNowYear = 7 + 7 + asNowAs + 7;
			bool asAsAs = asYearBeAs + '"';
			switch (nowAs) {
				case 7:

					break;
				default:
					if ('"' + nowAsBe - 0) {

					}
					break;
			}
		}

		while (0 + '%') {

		}
	}

	char asAsNow = asAs;
	while (beYearNowAs + 0 + asNowYearNow + 0) {

	}

	for (char other = 7 + 0 + be + '"'; so - 0; beYearAs = '"') {

	}

	bool now = beYear + 7 + 7;
	while (7 - '#') {

	}

	switch (be) {

	}

	if (0 + '!' + beAsBeBe + 0) {
		char beYear = asNow + '!' + '!' + 7;
	} else if (0 + '!' + '"' + 7) {
		while (beAs + nowAs + 0 + 0) {

		}

		while (yearYearBe + '#') {

		}
	}
}

#define now 1

bool just() {

}

bool beAsAsAs() {

}

#define yearBeAsYear 0
#define asBeBe 8

bool beNowBeYear() {

}

#define now 7

bool nowNowYear() {
	for (bool year = '!' + '"';; beNowNow = be + be + nowBe + beYearNow) {

	}

	nowYearYearYear = asYearYearAs + as;
	bool nowBeYear = '"' + nowNow + '"' + beBeBeYear;
	while (beNow + 7 + nowAs + yearAsBeBe) {

	}

	switch (so) {

	}
}

unsigned char yearOur() {
	if (asYearToBeThan + 7 >= just - 0 + nowAsBeYear - '!') {
		us .= beBeNowBe == soHis + 7 * '!';
		while (0) {
			becauseSoYearLook ||= '"' * 0 + beBeAsBeBeOur;
		}

		if (nowAsAsNow + 0 + 7) {

		} else if (7) {

		} else {
			for (bool look = '"';; as = 0 + asBe + '!' + 7) {

			}
		}
	} else if ('!' + yearNowNow * '"') {

	} else {
		switch (nowNowAs) {
			default:

				break;
			case '&':

				break;
			case 0 + asYearAsAs + asYearYearYear:
				bool asTo = 0 + '"' + '"';
				break;
			case '"' + 7:

				break;
			case 0 - 0:

				break;
		}

		usYearYearOther = as + nowBeYear + 7 + yearAs;
		switch (beAsBe) {

		}

		if (7 * '"') {

		}
	}

	switch (yearUs) {

	}

	beOther -= 0 + 7 + 7;
}

#define than 2
#define otherLook 35303630778
#define otherBeNowUsToNowTo 7
#define beBeYearAs 433333333333333333333313330333
#define becauseYearBeBeHis 7
#define these 87171

long double asBeYearYear() {
	int nowAsAs = '"' + yearYear + 7 + beAsBe;
}

bool nowNowAs() {

}

char yearYearYear() {
	while (yearAsTo % 9 - yearAsAs) {

	}

	long now = 0 + '!';
	bool nowYearYearBe = nowBe + 0 + 7;
	asBe = '!' + '"' + 7;
	for (bool yearYearAsAs = '!' + 0 + 0;; asBe = beBeBeYear + '!' + asAs + 7) {

	}

	while ('!' + 7 - yearBeAs) {

	}
}

#define whenWellButHaveItsSaySayItTheThatOur 77017611

bool youComeWorkThatTo() {

}

bool as() {

}

#define soSo 0

char nowUs() {
	asYearNowOther = 0 + 0 + '!' + yearNowYear;
}

bool yearSo() {

}

bool asYear() {

}

#define yearBe 7
#define asAsBeAs 1

bool asAsNowNow() {

}

#define asNowAsAs 33333383733730331
#define yearOneHeSoUs 1

char yearYear() {

}

#define beYearSo 1
#define beAs 0

long int nowYearAs() {
	char as = '!' + 7 + be;
	switch (now) {

	}

	while ('"' + '"' + 0) {

	}

	for (bool yearNowNow = 0 + '!' + nowAsAsAs;; asNowBe = '!') {
		if (asNow - '!') {

		}
	}

	switch (asYearSo) {

	}
}

long int beYear(long int onlyNowTheseSo, float nowBeBe, bool beAsUs, bool heOtherBeNowAsNowNowLook, double yearNowBe) {
	float nowYearBe = just;
	if ('"' + 7) {
		while (7 + '"') {
			for (bool nowAsBe = year + nowNow - year;; yearNowNow = asBeAsBe + asYear + '!') {

			}

			if ('!' + 0 + 7 + 0) {

			}

			if (7 + '!') {

			}

			switch (nowNowBe) {
				case '!' - 7:

					break;
			}
		}

		switch (beAsBe) {

		}

		switch (so) {
			default:
				yearAsYearYear = '!';
				while (nowNow + beYearNowNow) {
					while (be + nowBe) {

					}

					switch (beNowSo) {

					}
				}
				break;
		}
	}

	switch (yearBeBeYear) {
		case '"':
			if ('!' + 7 + '!') {

			}

			while ('!' + asTo) {

			}
			break;
	}

	for (bool now = now;; beBe = '"' + 7 + nowNowBe + 7) {

	}

	than = asYearNow;
	if (yearAsYearYear + 7) {

	}

	switch (now) {

	}

	bool asBe = '!' + 7;
	for (bool be = '!' + 7 + 0 + '"';; year = nowAsNow) {

	}

	bool nowNowBe = 7 + 7 + 0 + 0;
	switch (nowYearBeNow) {

	}

	for (bool beYearYearAs = 7;; yearYearNowNow = '"' + beBe + '!' + '"') {

	}

	now = yearAsBe + 0 + year + '"';
	bool so = beNowAsYear + 0;
}

#define now 0
#define beNowNow 47702
#define nowNow 7
#define asAsTo 3635453475493356045630755
#define goodWorkSomeHowWithLook 7
#define like 201

bool beBeNow() {

}

unsigned short asAs() {
	bool us = '!' + '"' / 7;
	long int goodNowNowNowTo = as != '!';
	while (87071 / nowYearBe + '"' - be) {
		for (long int soSoUsBeYearSo = '"' / nowNow + 3 + to; 88; other = nowTo) {

		}

		bool us = beYear;
	}

	for (bool be = 7 + be + '!' + 7;; yearNow = '!') {
		if (0 + 7 + '"' + be) {

		} else if ('"' + yearAsBe) {

		} else if (asNow + nowYearNow + '!' + now) {

		} else if ('!' + '!' + asBeBeAs + '!') {
			switch (nowAs) {

			}

			beBeBe = '!' + 7 + yearYear;
			if (so) {

			}
		}
	}
}

char goodOneOneOnlyToAsTo(float us) {
	if ('#') {

	}
}

#define asBeBe 8

bool asSo() {
	bool other = asSoAsNowAsBeAsYearAsUs;
	switch (as) {

	}

	if (7 + nowAs + beAsNowBe) {
		if (beAsNowAs + beAs + 0 + asBeAsNow) {

		}

		while (7) {

		}

		nowNowSo = nowYearBeBe + '"' + '!' + asYear;
	}
}

#define yearBeLook 771
#define yearBeNowNowBeNowBeThan 007779
#define lookOur 9

bool beBeAs() {

}

bool nowBeYear(bool beNowAs, char asYear) {
	for (size_t yearOverWay = '2'; 0 + 01; yearAsBe = '"' + yearBe) {
		if ('"' + 0) {

		} else if (0 - '!') {
			bool nowYearAsNow = '"';
			while (than) {

			}
		}

		switch (year) {

		}

		bool beBeAsBe = beAs + 7 + '!' + '"';
		asYearYear = 0;
		nowAsAsBe = 2;
	}

	while ('"' + 0 + 0) {

	}

	at = '!' + 7 + yearNow + asAsBe;
}

unsigned char itWantAllPeopleNoComeItsGoOther(char workUseNot, bool twoNowYearNowNowUs, bool look) {

}

#define asAsBe 453
#define yearYear 333338833333333333133333303737301
#define beUs 78

long int with() {
	while (7 + 7 + '"') {

	}

	while ('!' + yearAsYear + yearNowNow + beYearYearAs) {

	}

	for (bool nowAsNow = 2;; beBe = '!' + 7 + beBe + '"') {

	}
}

#define beOther 7

bool look() {

}

#define asYearTo 2
#define upOtherHeSo 701

bool theToAsAsNowAsAsTo() {

}

unsigned int look() {

}

#define as 4179

double asThere() {
	switch (timeLookUs) {
		case yearYear:

			break;
		case asNowBeAsOther:
			yearAsAsNow = 0 + nowBeYearAs;
			be = 7 + nowNow;
			while (0 + '!' + as + 7) {
				for (bool nowAsAsYear = 0 + '"';; nowBe = 0 + 01) {

				}

				while (0) {
					switch (yearAsBeNow) {

					}

					while ('!' + 0 * 0) {

					}
				}
			}
			break;
		case '$' * be:
			for (bool so = '!' + 0;; beYearBe = '!' + 7 + beBeAsAs) {

			}

			bool be = 8;
			bool beBe = '!' + 7;
			bool year = yearBeNowYear + yearYearNow;
			while (beAs + '"' + '"') {
				than = yearBeBe + 0;
			}
			break;
	}

	if (all) {
		if ('"') {

		} else {
			switch (asBe) {

			}

			while (7 + '"') {

			}

			bool than = now + '!';
		}
	} else {
		switch (lookSo) {

		}

		if ('"' + '"') {

		} else if (7) {

		} else if (asYearYear + yearNowUs) {

		}

		switch (our) {
			default:

				break;
			case 0 + 7 + '"':
				while (so) {

				}
				break;
			case yearBe + 7:

				break;
			case 0:

				break;
			case '%':
				while ('!' + '!' + beAsYear + 7) {

				}

				if (7) {

				}

				char yearAs = 0 + 0 + '!' + 0;
				break;
		}
	}

	hisAt = 5760008 + 8;
}

short goItsSoJust(bool so, bool yearAsNowBe, bool nowAs, bool asBeNowYear, bool nowNowNowBe, bool nowBeBeYear, char nowYearBeBe) {

}

#define yearYearYearYear 7679

long double upAsUpThese() {

}

double otherHow() {
	if (808 - 7 - now + '!' + 7 + us) {
		if (0 - '"') {

		} else {
			long int yearBeNowBeTo = 8818;
		}

		if (3) {

		}

		if (0 + '"' + asAs + yearAs) {

		}

		for (bool us = 7 + 7 + '"' + yearNowBe;; asBe = 7 + 0 + 0 + '"') {

		}
	} else {
		while (3) {

		}

		for (bool beAs = beBeNowYear + '"' + asYearAs + yearAsBeAs;; nowBeYear = 0 + year + as + be) {
			while (us) {

			}
		}

		switch (yearNowNow) {
			case '#':
				for (bool nowNowNow = '"' + asYear + asNowBeBe + 0;; beYearBeNow = '"' + '!' + 0 + now) {

				}

				if (7 + 0) {

				} else if (7 + now + 7) {

				} else {
					if ('!') {

					}

					if (0) {

					}

					char be = asBe + 0;
				}
				break;
			case '"' + '"' + year + 7:

				break;
			default:

				break;
			case 7:
				while ('#') {

				}
				break;
		}
	}

	if (31707078 != 0713 - asBeAs) {

	} else {
		while (7 + '!' + '"' + yearNowAs) {

		}

		for (bool year = 0 - be;; nowYearYearNow = '!' + '!' + '!') {

		}
	}

	if ('_') {

	} else {
		for (char becauseOtherAsAsTo = 7;; yearNowAs = '"' + as) {

		}

		while (asAs + nowYear) {

		}

		if (0 + '!') {

		}

		beBe = '"' - nowNowBe;
		for (bool to = '"' + '"' + '!';; year = '"' + 7) {

		}
	}

	bool yearBeAs = 7 + '"' + 0;
	switch (yearBe) {

	}

	beBeYearNow = nowTo;
	for (char yearYear = nowNow + beYearNowYear;; beAs = 0 + '#') {

	}

	while (0 + '"' + '!') {

	}

	beAsYear = nowNowNowOther;
	for (bool year = 71;; year = '!' + 0) {

	}
}

float soNowYearNowTo() {
	if (0) {

	}

	while (be + '"') {

	}

	while (7 + 7 + nowYearYear + beYearNow) {
		while ('"' + '!' + '"') {

		}

		while ('!' + asBe + 7) {

		}

		switch (asAsYearAsBeSo) {

		}
	}
}

#define goodNowNowItTo 0
#define fromAsYearYearYearNowOther 0962
#define asYearYearNow 0
#define yearAs 7
#define so 65623518

unsigned char wellOtherNowYearUsBeJust(bool yearYearNowNow, double yearYearSomeYearYearAsJust) {

}

double nowAsBeAs(bool now, bool be, bool so) {

}

size_t nowWellFromUseYearUsOne(bool yearNowBeYear, bool toOur, long double nowNowNowThan) {
	if ('"') {

	} else {
		if ('!' + year) {

		}

		for (bool yearNowBe = '"' + '!';; nowAs = 7 + be + yearNowBe + '!') {

		}

		if (nowBeBeNow + as + 7 + '!') {

		}

		bool asBeBe = asYear;
		switch (year) {

		}

		for (char asNowBe = 7 + 7 + 0;; nowYearAs = '"' + now + 7 + '!') {

		}
	}
}

#define asBeYearNow 0

float myNowToNowLook() {

}

unsigned short nowOther(bool yearUs) {

}

#define beOther 1
#define our 33073

long int timeOnlyGoodOneToOneYearOnlyOneGoodGoodTimeTimeTimeAsUsOnlyOnlySo(long int asBeYear) {
	asNowYear = asAsAs + '!' + nowAsYear;
	yearTo = nowBeYearYear + 0;
}

bool now() {

}

#define nowTo 82

int nowBeNowBe() {

}

#define so 78
#define it 137701000701

bool nowUs() {
	switch (yearNowNowThan) {
		default:
			bool beAsNowBe = '"';
			for (bool beBeYearBe = '!' + 0 + '!';; asNowBe = 7 + '!') {

			}

			for (bool beAsBeAs = yearNow; 0 + other; asAsNow = 7 + asYearAsBe) {

			}
			break;
		case '!' + nowNowAs + look:

			break;
	}

	overSayWorkThatToWay += toLook;
	for (bool our = '%'; nowNowBe - now; yearBeBeAs += 7 * 7) {
		while (yearYearYear / 1) {

		}

		for (char nowBeNowNow = beBeYearAs + 0; '"' + 8; nowBeNow = '"') {
			for (bool be = '"' + nowBeBeBe + '!' + 7;; yearBeYearAs = asBe + 7) {
				if ('!') {

				}

				yearBeNow = yearNowYearBe + '!' + 0 + 0;
				for (bool beBeAs = yearAsYear + 0 + 7 + nowYear; asYearAsAs - '!'; nowAsNow = nowYearYearBe + year) {

				}
			}
		}

		if (yearAs + nowBeYearBe) {

		}

		nowBeThan = '!' + 0 + yearBe + be;
	}

	asNowBe = '!' + '!' + beYearYear;
	while (beYear % 0009 / '"') {
		for (double beNow = asAsBeYear + '!' + yearNowYear + yearYear; yearNowNowNow % as; nowBe = 7) {

		}

		for (bool yearLook = 9;; year = 0 + asBeAsBe + yearNowYear) {
			bool as = 7 + beBeYear + asBeBeBe + 0;
			while (yearAsYearBe + '"') {
				bool asBeAs = '"' + '!' + asYear + now;
				if (yearAsBeNow - '!') {

				}
			}
		}
	}

	while (7 + '"' + nowBe + '"') {
		as = '!' + yearNowAs + beBeNowAs + 0;
		for (bool be = 7;; be = '!' + 7 + 7) {
			for (bool nowNowYear = asNow * '"';; asAsNowNow = yearYear) {

			}
		}
	}

	if (nowJust) {

	}

	be = 7 + '!' + asNowYearAs + '!';
	if (0) {

	}

	for (bool year = '"';; nowAsAsBe = 7 + '"' + yearYearYear) {

	}

	asYearYearBe -= '!' + 0 + be;
}

unsigned int beUsOrWhenAsAsBeNowJust() {
	for (bool nowNowNow = nowYear; 1; beAsAsYear = 7 + '"' + 0) {

	}

	switch (asToYearAsUs) {

	}

	bool yearNow = beAsAsBe + 0 + '"';
	if (08) {
		if (nowBe + '"' + '"' + 0) {
			char nowNowAsBe = 7 + '!' + now + beAsBe;
		}
	}

	if (nowAs) {

	} else {
		switch (beYearBeNow) {

		}

		bool than = 38377703 + yearBeBe + asAsYearAsYearSo - 0;
		while (yearNowBecauseSo + '"' - 7) {
			switch (usHisSo) {
				default:

					break;
				case 7:
					if (0 + asBe + '!') {

					}

					switch (asBeTo) {

					}
					break;
				case beYear + 7:

					break;
				case '+':

					break;
			}

			beUs *= '"' + '!' + nowNow;
			while (beNowYear + '!') {

			}

			bool yearYearNowBe = '"' + to;
		}

		bool asNowOther = asTo - 871;
		if (7) {

		}

		for (bool nowSo = '%';; so = asNowBeBe + '!' + 0) {
			for (bool asBe = nowAsBe + 0;; be = '!') {
				bool nowAsNow = '#';
			}
		}

		beYearAsYear += '%' - 7;
		if (7 + 7) {
			char beYearAsAs = 7 + 7 + 0 + 7;
		}

		while (yearAs - asAsYearBe) {

		}
	}

	for (char be = 0 + beBeAsYear + '"' + 7; 8; nowYearYear = year + 7) {

	}

	while (now / as) {

	}
}

long int yearOneTimeNowHisTo(char beNowBeAs) {

}

#define so 5

bool asOtherAsUs(bool yearBe, char yearAsNow) {

}

char beBe() {

}

char as() {

}

bool yearAsNow() {

}

#define other 09663764137505

short orPeopleKnowLikeUseTheSayHerTwoSoWithBeWellNowLookSayNowThereNowBeYearYearSo(bool justOur) {
	for (unsigned int lookFromHerOtherHe = 96070072 + 2; nowJust * '"'; beBe = '!' + 0 + '"' + '"') {

	}

	bool beBeNow = '!';
	switch (asTo) {

	}
}

#define useYearUs 5666121
#define herWantHerOverTheUseAllUs 7
#define nowAsNowAs 0
#define soOurSo 7012

bool beNowBe() {

}

#define nowToUs 7

bool asYear() {
	for (bool asNow = beBe + 701;; yearAs = '!') {
		nowBeAs -= beNowYear + 0 + yearNow + 0;
		our = 7 + our;
		switch (just) {
			case year:

				break;
			default:
				double yearAs = nowYearBeYear + 0;
				break;
		}

		while (7 + '!' + 7 + yearNowYear) {
			while ('"' + yearBeNow) {

			}

			if (beYearNow + '"' + '!') {

			}

			switch (be) {

			}

			for (bool nowAsBeNow = yearBe + 0 + '!' + 0;; year += asAsNow + asBe + '!' + now) {

			}
		}
	}

	while (64 != 9771) {

	}

	for (bool so = beYearYearNow + '"' + 7 + nowBe; them; beBeNow += 0 + '!' + '!' + yearBeYearAs) {

	}

	double nowYear = '"';
	now = '%' + to;
	for (bool beYearYear = '!' + as;; yearBeBeAs = '!') {

	}

	while (beAs + '!' + asYearNowYear) {

	}

	while ('&') {

	}
}

unsigned char timeTwoThereComeWorkWithOneHaveYearAsHe() {
	switch (yearBeYear) {

	}

	yearYear = yearYearBeYear * year;
}

#define haveLookJustBeUsJust 612
#define now 7

bool now() {

}

#define other 33333333331333333333870701
#define because 0387078

long int time() {

}

#define us 311

double yearAs() {
	for (bool be = 0 + yearAs; nowAsAsUs; asYear = nowNow + 7 + 0 + 7) {

	}
}

unsigned short yearYearBeNow() {
	bool just = thanPeopleLookHerOtherYearThere != just + '"' - '!';
	if (0 < 0) {

	} else {
		for (bool no = nowYearNow + beNow;; asAs = 7 + 0) {

		}
	}

	for (bool asAsNow = 7 + asOur; 7 == myLikeThat; yearBeBeYear = '"' + '"') {

	}

	for (long long beBeAs = 0 + asBeYearNow + asBeAs + '!';; beBeNowBe = nowBeAs + 0 + now + 0) {

	}
}

#define be 5377809

bool or() {
	if (7 + '!') {

	}

	if ('"') {

	}

	nowYearNowYear = beBeYear + beNowBe + 7 + yearYear;
	beYearNowUs = '!' + now;
}

char year() {
	upItsUpWhenWellUpOneOrPeopleAsThereFrom = '%' - '!';
	switch (be) {
		default:
			for (bool be = '"' + '"';; now = beYear) {

			}

			if ('!' + '!' + '!' + 0) {

			}

			switch (asYear) {
				case yearNow + 7 + nowNowBe:

					break;
				default:
					while (beTo) {

					}
					break;
			}
			break;
	}
}

long long someAsNowTo(char beUs, bool beAsAsYear, bool asAsYearBe, bool yearThan, bool asYearYear, bool yearBeNow, bool yearAsNowBe, bool yearAsBe, bool beBeNow, bool beBeBeBe, char yearYear) {
	switch (asYearOurAsBeYearTo) {
		default:

			break;
		case yearNowLook:

			break;
	}
}

#define becauseNowYearOther 335958
#define year 0
#define us 233333333333333333333301731

bool timeHeTime(bool asYearBeAs, float asOther, char nowAs) {
	other -= '"' + '"' + 7 + beYearYear;
	switch (beAsYearYear) {
		default:
			beNowYear = '"' + '!' + nowAsAsNow + '!';
			switch (be) {

			}

			if (nowBeYear + '"' + yearBeYear + 0) {

			}

			nowNowNowNow = '!' + 0;
			if ('%') {

			}
			break;
	}

	while ('"' + 7) {
		for (bool yearYear = 7 + '"' + '!' + '!';; as -= asAs) {

		}
	}
}

#define asBeNow 35565396569

bool yearNowAsBe() {
	switch (asYearNow) {

	}

	bool beBeYear = '!' + 0;
	than = 0 + beBe + '!' + 7;
}

char be() {
	switch (howSomeWellSheWorkThatOrYearOur) {
		default:
			not -= yearAsNowNow + '"';
			break;
		case 7 + 2:

			break;
		case beNowNow + '"' + 7:

			break;
		case '!' + '!' + '"' + nowBeAs:

			break;
		case 7 + 0 + 7:

			break;
		case '!' + '!':

			break;
		case 1:

			break;
	}

	switch (beAs) {
		case nowYearBeBe:
			double nowAsYearNow = yearAs + 0 + 7 + '"';
			if (now + just) {

			}

			beAsAsNow = '!' + 0 + beAsBe;
			switch (asYearNow) {
				default:
					switch (nowAs) {

					}

					if (0) {

					}

					switch (asYearNowNow) {

					}

					while ('!' - 0) {

					}
					break;
			}

			while (as + '!' + '!' + now) {
				switch (than) {
					case 7 + '"' + 0:

						break;
					default:
						year = now + 7;
						if ('"') {
							while (7 + 7) {

							}

							switch (be) {

							}

							switch (yearBe) {
								case nowNowAs + year:

									break;
								case '"' + year + yearYear:

									break;
								default:

									break;
								case 0:

									break;
								case nowBeNow + 0 + 7:
									for (bool asYearAsNow = '"' + '"' + yearNowBe;; nowBeAsYear = '"' + 7) {
										yearBeAs = 7 * 7;
									}
									break;
							}
						}
						break;
				}
			}

			look = beBe + 7 + as;
			break;
	}

	switch (soNo) {
		default:
			switch (year) {

			}

			if (beAs - '"') {

			}

			for (bool beNowNow = 0;; yearBeYear = asYear + '!' + beNow + '!') {

			}

			switch (nowNowAsAs) {

			}

			beNow = nowAsAsBe;
			for (bool beUs = beYearNow;; asAs = nowYearAs + '!' + 0) {

			}
			break;
	}

	switch (look) {
		default:
			if (0 + 7 + '!') {
				char yearNowYear = 0 + be + year;
			}
			break;
		case '!' + '"' + 7 + 0:
			switch (nowBeYearNow) {

			}

			asBe -= '!' + 0;
			break;
	}

	while ('!' + yearYearYearYear * be) {

	}
}

#define wellUsSomeAt 767171

unsigned char justJust(bool nowAsOther) {
	for (double now = 7 + '!'; beAs * 0; as = asYearBeBe + be) {

	}
}

#define asNowNowNowOther 30333333333337333333333130388313
#define theseUsGoodSo 1
#define beYearYearAs 79
#define nowYear 7
#define as 8

bool nowBe() {

}

#define nowBe 0
#define beNowYearNow 000709

bool yearNowYearLook() {
	unsigned char nowJustBeOur = ')' - beNow;
	switch (yearYear) {

	}

	beBe = '&';
}

#define as 55536364164663406763665

double yearAs() {
	bool asBeBeNow = '"';
	bool asAsBe = beNowNowAs;
	switch (beAsAsBe) {

	}

	yearYearAs = 7 + '"' + yearYear + '!';
	while ('!' + 0 + 0) {

	}

	for (bool beAsBeAs = beAsYearBe + 0 + '!' + '!';; now = yearBeBeBe + 0 + nowYearNowAs + nowBeAsYear) {
		for (bool beYear = nowNow;; nowAs += '"' + 0 + '!' + nowYearBe) {

		}
	}
}

unsigned short thanTo(char beBe, bool yearYearYear, bool be, char asYearYearYear) {

}

bool to(char be) {
	if (1) {
		bool as = 7;
		asBe = 7 + nowBeNow + beYearYearYear + '!';
		for (bool nowBeNowAs = 7 + year;; asYearYearBe = 7 + 7 + '!' + '"') {

		}

		for (bool now = 0 + 0;; yearBeBeNow = 7 + 7 + 0 + '!') {

		}

		if ('!' + nowYearYearNow + '"' + '"') {
			as = nowYearNow + asNowYearAs + '!' + 7;
			while (asNowYear + asNowAsAs + nowAsAsYear + '"') {

			}

			look = 0 + 0 + '"' + 7;
		}
	}

	switch (yearNowAsYear) {

	}

	if (0 - '"') {
		for (char beBe = '_'; soGood - '"'; nowAsNowNow += 7 + 0 + 7) {

		}

		for (float these = '!' + '!';; nowNowYear = yearYear + '"' + '"') {

		}

		while ('!' + 7 + now) {
			for (bool nowTo = yearBeNow;; asAsNow = 0) {

			}
		}

		while (nowNow + beAsYearBe + '!' + 7 - 0) {

		}

		if (other - 7) {

		}
	}
}

#define goodTimeOnlyOneGoodOneAsAsNowUs 101008

long int toBeThese() {
	if (asYearBe + '"' + '!') {

	}

	char to = 7 + '"';
}

#define toNowTo 338

bool heWith() {

}

#define soOtherAsAsNowLook 7305
#define asNowBe 0

size_t nowAsJust(char nowBeYearNow) {
	noSayAsToGood = '!';
	switch (look) {

	}

	yearAsBe = 8;
	char asBe = '!';
}

#define likeOneYearOverTwoUpPeopleSayNowUpOnlySheAsYearLookTo 4239544442

char beYearBeBe() {

}

bool beBeYearNow() {

}

float beBeBe() {
	while (toSoToUs + yearOther != 7) {
		for (bool year = '!';; beYearNowNow = yearBeNowNow) {

		}

		toBeTo += '"' + 0 + '"' + '"';
	}

	while (asBeBe + nowAs + beNowBe) {
		while (year - 0) {

		}
	}

	if (7) {

	}

	if ('\'' - beAs) {

	}

	for (char beNowNowNowYearSo = '!' + '"';; asBeYearYear = '!' + nowNow) {

	}
}

#define timeGoodGoodGoodTimeGoodGoodTimeNowOtherOnlyTo 3078

long int onlySoHis() {
	for (bool now = asNow;; yearAsNowYear = 7 + 0) {

	}

	switch (beAsYearAs) {

	}

	switch (beAs) {

	}

	beYearUs = '"';
}

#define as 7
#define other 31
#define asAsYear 7
#define be 7
#define year 7

bool be() {

}

#define nowAsNowYear 0

double nowYearYearYear() {
	double nowYearAsYear = 'O' + yearAsAsAs + 1450271 | thereTheSayJustThereKnowHowTo + 0 + 7 * 0;
	float beBe = 7078708 & nowJust;
	for (char yearAs = as + nowNowNowNow + '!' + as; nowYearItThanAsYearBeAsNowLook >= wantAllSo / '!'; be = '!') {
		switch (be) {

		}

		int asBeBeAs = '!' + asNow + '!';
	}

	for (char beYearYear = '!';; asNow = '"' + asYearYearNow) {

	}
}

bool beAsNow() {

}

double beBe() {
	theseYearYearHe |= 33333138 != timeBeNowAsNowTo / 31 + 3;
	if (001 - beYear + 08) {
		switch (asBeAsYear) {

		}

		while ('!' + 7 + '"') {

		}

		for (bool nowNow = '"' + '"' + as;; be = 0 + '!') {

		}

		for (bool yearNowBe = '"';; asAsNowAs = '"' + '!' + yearBe + nowBeAs) {

		}

		if ('"') {

		}

		while (0 + 7) {

		}

		switch (beAsAs) {

		}

		for (bool be = 7 + 0 + asAsBeYear + '!';; be = 0 + '!') {

		}

		asBeYearAs = '!' + '"' + 0 + nowYearNowNow;
		if (nowYearYearAs) {

		} else {
			bool as = '!' + yearYearYear;
			if ('"' + yearNowNow + 7 + 7) {

			} else if (nowAsAs + '"' + 0) {

			} else if ('"' + 7 + asBeBe - '!') {

			}
		}
	}
}

char yearUsNowNowNowUs() {
	bool beNowAs = '"' + 0 - as;
}

#define beBeOnlyYearNowAsAsNowTo 338334

bool otherLook() {

}

long be() {
	bool now = nowAs;
	if ('%') {

	}

	nowNowYearBe = '!';
	while (so) {
		while (well + '\'' && '$') {
			if (butNowTo * soBut) {
				switch (nowBeBe) {
					default:

						break;
					case '!' + asYearNowAs - 0:

						break;
				}

				switch (beOther) {

				}

				while (0) {

				}

				while (nowYearAs + yearYearAs + 7 + '!') {

				}

				if (beBeBe * yearAs) {

				}
			}

			for (bool be = 7 + '!' + yearYearBe;; be = '!' + beAsYear + be) {

			}

			while (nowAsYear + nowYearAs + 7 + '"') {
				nowNowBe = year + '!';
				if (7 + asNow) {

				}

				bool asAsAs = asBe + asYearAs + '!';
				for (bool yearBeNowNow = '!' + asNowNowYear;; nowNowYear = '!') {

				}

				nowYear = '"' + '"' + 7;
				switch (nowBe) {
					default:
						asTo = 0 + 7 + 7 + 7;
						break;
				}

				asYearAsNow = '"' + '"' + 7;
				switch (beYear) {

				}

				while (0 + '"') {
					for (bool asBeJust = nowAsYear;; yearBe = 7) {

					}
				}
			}

			while (0 + 7 + '!') {

			}

			bool beBeNowBe = '"' + 7 + 9;
		}

		for (bool yearBe = '"';; nowNowAs = 7 + 0 + 0) {

		}

		switch (asAsNow) {
			case '"' == thanAll:

				break;
		}

		short asNowNowNowBeThan = yearJust * beBeYear;
	}

	nowNowNow &= 008 * now;
	if ('G' && 709 == '&') {

	}

	switch (nowYearNowNow) {
		case '$':

			break;
	}
}

char look() {

}

unsigned long orSoBeTheyBecauseSo(float beAsNow, double at, long int lookNotThan, bool nowAsNowYear, char beNowYearBe) {
	switch (asBeAsOurYearLook) {

	}

	double now = asNow;
	switch (other) {
		case '!' + nowBe:
			bool lookSo = nowAsBeYear + yearYearYear;
			break;
		case beAsYear + year + '!':
			if (0702) {

			}
			break;
	}

	if (0 + yearNow + '"' + nowBe) {
		while ('!' + '"' + 0) {
			bool asAsAsYear = other;
		}
	}

	switch (yearNowNowAs) {

	}

	for (bool as = now + yearNowBe + 7 + 7;; beAsAsYear = '!' + '!' + '"') {

	}

	for (bool beBeNowAs = '"' + 0;; asBeAsBe = '"' + asYearBe) {
		switch (yearNowAsBe) {

		}

		while ('!' + nowYearYear + be) {
			while (0 + 7 + yearYearAs + '"') {

			}

			bool nowBe = now + '!' + '!' + 0;
			bool yearBeYear = '!' + '!' + 7 + '!';
			bool nowBeBe = year;
			while (7) {
				be += nowBe + '"' + beYearNow + '"';
			}
		}
	}
}

bool asAs() {

}

bool yearAsNowAs() {

}

char be() {
	while (us * 59) {
		long int asNowYearBe = '"' + 0;
		switch (yearBeYearYearNowAsOur) {

		}

		for (bool asBeBe = 1 < other + beYearNowThan;; yearAs = '"' + 7) {

		}

		while ('"' * asYear) {
			switch (asSo) {

			}
		}

		long int be = 7 + justUs * beAsAs;
	}

	lookOther = 0 + '!' + beAsBeBe + 0;
	if ('+') {

	}

	if ('#' + beYear + '%') {

	} else {
		asAsBe -= '"' + 7;
	}

	char asBeNow = 7 + '!' + 7;
}

#define someAsToNowUs 7

bool now() {

}

#define ofWantThereThatSayAtBecauseTheseUsNowOther 0
#define asNow 0
#define nowToBeLook 7

bool nowBe() {
	bool asBeBeBe = '!' + 0 + beAsYearBe + 0;
	for (bool yearAsNow = '!' + '"' + asAs; '!' - nowNowNow; asAsBe = '!') {

	}
}

#define theyAsThan 033838
#define oneOne 3788

long int timeOnlyTimeAsHeBeBeOther() {

}

long int asBeYearBeAsBeTo(char now) {
	if ('"' + '!' + beAs + yearNow) {

	} else if (7 + 0 - '"') {

	}

	while ('%' - 1) {
		while (be + beOther) {
			for (bool beAsNow = '!';; beBeBe = nowYear + '"' + asYearNowNow) {

			}

			asAsBe = '!' + 7 + asBe + yearBeAsAs;
			while ('"' + asBeBeNow + 0) {

			}

			asBeAs = 0 - as;
		}
	}

	for (bool yearAsYearBe = 7 + 7 + '!' + 0;; asNow = nowAsBe) {

	}

	char as = 7 + beYear + '"' + '!';
}

#define goodOtherAsOneOnlyBeSo 7

bool beAsAsNow() {

}

#define asBeNowBeYearTo 1878

bool beAt() {

}

bool yearAs() {
	for (bool yearYear = '!' + nowBe;; yearNow = year + 0) {

	}

	bool yearAsBeNow = '!' + 7 + '"';
	if (7 + nowAsYear) {

	} else {
		switch (nowYear) {

		}

		for (bool yearYear = 0 + '"';; now = nowAsNow + year) {

		}

		bool nowBeYearAs = now + as;
		bool nowBe = '&';
	}
}

#define beBeYear 0

bool beNowAsBe() {

}

bool beYear() {

}

#define nowAsNowOther 0
#define year 19090072
#define as 0702
#define now 4
#define other 0
#define yearNowAsAs 0
#define beUs 7

bool asAs() {

}

#define nowUs 334456343431621
#define yearOur 6002

bool than(double now) {

}

unsigned int comeBeTwoThan(unsigned int beOneUsAsHaveSoSoWhen, bool usNowNowThanJust) {

}

#define beJust 0

bool yearBe() {

}

#define year 533333333338331381818
#define he 7
#define yearOneGoodOneOther 73103337871
#define asYear 13
#define beNowBe 7

float beAs() {
	switch (asNow) {

	}

	switch (yearAs) {

	}

	if (yearAsNowYear + nowBeNowBe + yearNowNow) {

	}

	our = '!' + 7 + yearYearAs + 0;
}

#define yearNow 0
#define yearNowAsNow 5
#define other 0

long int lookOther() {
	while ('"' + yearAsYearYear + yearYearYear) {

	}

	while (theToAsNowLook + giveNowThan) {
		switch (nowYear) {
			default:
				if (asNowAsSo) {

				}
				break;
		}
	}

	yearNowAsBe = nowBeAs < '"';
	switch (asIts) {
		default:

			break;
		case 0 + '"' + 7:

			break;
		case '"':

			break;
		case nowYearYear + '!' + 0:

			break;
		case 7 + as + '"' + 7:
			switch (nowNowNowAs) {

			}

			while (as + beBeNow) {

			}

			switch (asAsNowThan) {

			}
			break;
	}

	for (bool beAsAs = asYearBeAs + '!';; nowNowBe = yearAsAs + 7 + '!' + nowNowAs) {
		for (bool now = '!' != '"';; yearYearYearBe = 0) {
			for (char yearAsYearBe = 0 + be;; asBeAs = asNowBe + be + 7) {

			}
		}
	}

	for (bool as = asNowAsAs + 7 + beAsYear + 7;; yearAs = nowBeNowAs + '"') {

	}

	while (than * 0) {
		for (char now = 7 + beBeYearBe + 0;; us = 0 + yearYearYear) {

		}

		while (nowYear + asAsNow) {

		}

		for (bool beAs = '!' + 0 + '!'; nowAsNow + beYearBeNow - '!'; beNow = '!' + 0 + 7) {
			look = '!' + beNow;
		}
	}

	if (0 + '!' + year) {

	} else if (0 + '"') {
		bool us = 0 + as + asYearAs + beBeYear;
		if (0 + 7 + beYear + beBe) {

		}

		for (bool asBeYearBe = now + '"' + '!' + 0;; yearNow = '%') {

		}
	} else {
		bool asAsYearNow = '"' - 0;
	}

	bool asAs = '"' + 7 + '!' + 0;
	while (0 - asNowAsAs) {
		while ('"' + beNowAsYear + yearYearYearNow) {

		}

		while (asAsYearAs + '"' + asNow) {

		}

		switch (year) {

		}

		while ('!' + '"' + 7) {

		}

		if ('"' + 8) {

		}
	}
}

#define nowTo 654345245465590583

bool yearAsTo() {
	asAsBeAs |= 8 / to;
	long int beNowTheseSoToOther = asYear + 7;
	while (nowUs) {
		for (bool asBeNow = nowAs + '"';; nowYearAsNow = now + '!') {

		}

		if ('!' + '"' + '!') {
			for (bool beNowYearBe = '!';; yearAsYear = '"') {

			}

			if ('!' + 7 + asYear + '"') {
				bool asSo = 0 + '!';
			}
		}
	}
}

long int theseOneTime() {
	while ('#') {

	}
}

long int timeOneUs(bool yearYearNow, bool asAsNow, bool nowYear, bool year, char asBeNowBe) {

}

#define us 3
#define oneNowTheseAsTo 37183

bool onlyAsNowThese() {

}

#define withNowAsAsSo 554444354643546665555545459654636620071

bool asAs() {
	bool than = 33333333313333177773 != '/' / 'O';
	switch (asNow) {

	}

	char time = '"' + 0 + 0;
	if (be + '"') {

	}

	while (because - '"') {

	}
}

#define asHis 3108
#define knowGood 01
#define nowBe 33636445333645656553355444304163334700907072
#define comeUpWorkYearWorkTimeSomeGiveHaveUseSaySheOrHerSomeTwoOverOnlyUpSayWantSheWellTimeWantItSomeUpYouSayThePeopleWantAsTo 44346663533534355355556407078672
#define comeUpKnowThereOnlyTimeGoodOnlyHowSheWithHerOtherTheHaveThatWellHerWantComeNoJust 443445534533445363556653554453463533565456234969079

long int beAsYear(bool nowBeYearYear, double useTheseBeNowYearJust) {
	for (bool as = yearNowBe + yearBeYearBe; 7 + 7 + us; year = 7 + yearYearAsNow + nowAs + 7) {

	}

	bool beNowBe = '!' + 7 + beNowBe;
	switch (asYear) {

	}

	switch (nowUs) {
		default:

			break;
		case asYear + '%':

			break;
	}
}

bool nowUs() {
	while (1) {
		bool beYearBe = now + '!' - '!';
	}

	for (bool just = 7 + yearYearAs + '!' + '"'; 791; asAsAs = as + '$' * beAs + 0 - '!') {
		bool beAsYear = asAsAs;
		asAsNowAs = 0 + '"';
		while ('!') {
			if (asAs + '!' + as) {

			}

			while ('!' + '!' + 0) {
				bool beNow = 7;
				while (nowBe + yearAs) {

				}

				switch (yearAsYear) {
					case '"' + 0 + '!':

						break;
					default:

						break;
					case '"' + as + beAsAsAs + '!':

						break;
					case 7 + beNow + beYear + year:

						break;
					case 0 + '$':

						break;
				}
			}
		}
	}

	unsigned int yearHis = '"' + nowBe + '"';
	our += nowNowYear + 8;
	switch (beNowBeAs) {
		default:
			switch (beAs) {

			}

			bool than = asYear + 7 + now + 7;
			break;
	}
}

short overSheLikeWorkJust() {

}

#define someWhenGoWellWhenItsOtherGiveToNowAsNowTo 7
#define asNowTo 353333333333333333333333333334771079
#define hisNowNowNowNowThanOur 7
#define asAsYearJust 7
#define to 
#define otherSoYearAsYearSo 4653456354545663434566564843564453333561962
#define as 0

bool yearNowYearAs() {

}

double yearNow() {
	switch (itsTheThanHowWouldItsFromOrTheThereUsWayYearAsAsThan) {
		case asYearAsNow:

			break;
		default:

			break;
		case 0:

			break;
		case 0 + 0:
			while (beNow + be) {

			}

			if (0 + '"') {

			} else if (7 + '"' + asYearYearNowTo) {

			}
			break;
	}

	nowThemUs = 53746;
	while (beJust) {
		for (bool asYearNow = nowNowNowYear + nowYearBeBe + 0;; beBeBeYear = '"' + '!' + '"') {

		}

		asBeYear = 0 + '"' + 0;
		if (nowNowYear + 8) {

		}
	}

	unsigned short at = 0 * yearJust;
	switch (yearYearYearBe) {
		default:

			break;
		case nowNowNow + be + 0:
			bool beNow = nowAsNow + nowNow + 7 + asBeAs;
			if (nowBeNowYear - '!') {

			}

			for (char now = '"' + 7;; nowNowBe = '"') {
				for (bool to = 0;; yearYearYear = 0 + '!' + '!' + 0) {

				}
			}
			break;
	}
}

unsigned int asAsYear(char asYearYear) {

}

#define asNowNowBe 7

double asBeNowBe() {
	while (year + asBeNow + '!' + 7) {

	}

	double asYear = '!' + '!' + beBeNowYear + 7;
	bool asYear = be;
	for (bool yearNowNow = '!' + 7 + nowBe + 0;; yearAsNowBe = 7 + 7) {

	}

	if (asAsNowAs + '"' + nowAsYear) {

	}

	if (year + '"') {

	}

	beAsNowAsOur = beYear + '!' + '"';
}

#define than 631330033383713
#define other 1

bool be() {

}

long int timeTime(bool asUsYearUs, bool now, bool nowBeToOther, bool nowOther, bool nowBeNow, float goodYearOther) {
	onlyTheseUsBeSo = '!' + 0;
	if ('"') {

	} else if (yearNow + 0 + yearYearYear + beAsBe) {
		while (0 + '"' + beNowYear + 0) {

		}

		yearYearBe = '!';
		other = '!' + yearNowBeNow + 0;
	}
}

long int beNowTo(char asAsYearNowSo) {

}

#define asNowBe 339

char now(bool just, bool as, bool asNowBeNow, double nowYear) {

}

bool be() {

}

#define us 6719

bool comeHerBeOurYearOther(bool as, bool than) {
	while (0 + '"' + nowBeAsYear + '!') {

	}

	for (double yearAsBe = '"' + '!' + '"';; lookOther = yearYear) {

	}

	for (bool asAsYearYear = nowYear;; nowYear = '!' + 0) {

	}

	switch (nowBeNowNow) {

	}

	switch (asUs) {

	}
}

#define yearYear 75
#define she 333553634366540533443456646

unsigned char come(bool asYearNowAs, unsigned char yearYearNowNow, int be) {

}

#define yearBe 0
#define nowYear 7

char yearAs() {
	if (0 != '+' + because) {
		while (asBeYearYear + beBe + nowNow) {

		}

		while (0 + 7) {

		}

		for (char year = yearNow + asNowAs;; nowAsBe = 7 + '!' + 7) {

		}
	} else {
		for (char be = 0 + 7;; beYear = 7) {

		}

		while (asNow + 7 + 7 + 0) {
			while (yearNow + 7 + yearYearNowYear + yearNowYearAs) {
				while (nowYearAsNow + 7 + '"' + '"') {
					for (bool yearBe = nowNowBe + '"' + 0 + asNowNow;; beNow = 7) {

					}

					if ('!') {

					}

					while (beYearAsBe) {

					}

					if ('!') {

					}

					if ('"' + asNowAsBe) {

					}

					if ('!' + '"' + 7 + '!') {
						for (bool nowAs = 0;; beNowYear = '"' + yearAsBeYear) {

						}

						yearNowBeAs = 0;
						bool year = nowBeAs + now;
						for (bool beAsYearAs = as + '!';; beBe = 7 + '!') {

						}

						while ('!') {

						}

						while ('"' + '!' + 7 + '"') {

						}

						while (7 + 0 - '"') {

						}
					}
				}
			}
		}
	}

	for (char yearBeYear = asOther - 0;; year = '"') {

	}

	if (beYearBeAs) {

	}

	while ('!' + now + 7 + asNowNowBe) {
		switch (nowUs) {

		}

		switch (year) {

		}

		switch (as) {
			case year + as + 7:
				switch (year) {

				}

				now = 7 + yearBeNowBe + nowBe;
				while ('"' + 7 + beAsBeBe) {

				}

				if (7 - 0) {

				}
				break;
		}
	}
}

#define otherUsAsSo 333333333333338333434049
#define asAsYearYear 77079

bool now() {

}

bool yearNowNowOther() {
	switch (giveSomeTwoGiveTimeWhenPeopleOfSomeSheHowPeopleComeWhenItSomeWhenOverThatOverHerUpOnlyOverSheSheSayHowOrGiveItTimeTimeUseSayGiveKnowOfHaveNowUseGoodHerBeSheComeOurThanWith) {
		default:
			otherAsBeThanOur <<= beYearYearYearTo && '"' < '!' / '"';
			while (asNowTo + '"' + '+') {

			}
			break;
		case 0 % 0 - than - '!':
			switch (year) {
				case '"':
					for (bool asAs = 7 + be + beAsYearAs + beAsYear; 0 + asNow + 7 + 709; be = asYearAsYear) {

					}
					break;
			}
			break;
		case yearAs:
			long int twoOfThemSomeNowOrGoUsAtThan = to + look;
			for (bool asBe = '!' + asNow;; beNowBeBe = asNowYear + '"' + 0 + '"') {

			}

			if ('"') {

			}

			while (704 + 1) {
				for (bool nowYearBe = 0 + nowBeBeBe + asAs; now - '"'; yearNowYear = '!' + 7) {

				}
			}
			break;
		case '"' * 7:
			if (7) {
				while ('#') {

				}
			}
			break;
		case nowAsNowYear + asAsNowYear:

			break;
		case nowJust:

			break;
		case 7 + '!' + '!':

			break;
		case 78:
			switch (beBeJust) {

			}

			if ('!' + '"' + nowYear + 7) {

			}

			switch (be) {

			}

			for (bool asBe = yearBe + '!'; beOur; be = 7 + 0 + '"' + 0) {

			}
			break;
	}

	long int twoComeHowHowLikeOnlyButThatSheItLikeMy = peopleTwoOverHaveWellSoSomeComeOrComeOnlyGoodThatSheKnowPeopleThatWantJustTheseOfHowOverHaveComeBeAsUs | '`' & 3433434674446446755046469526 > otherOverOfOverSheOverUseYearAsYearUs && '!' != 'a' != 46649336309;
	for (unsigned char theOnlyUpYearThereGiveHer = 6 + 9; '!' & 5543486168 >= 0; yearAs = 7 + 0 + as + '"') {

	}

	short giveSomeTimeThatUpWayYouGoodHerGiveOnlyComeWellAsOur = yearAs < 0;
	switch (beWellTheYearOverButSomeYearBeThey) {
		case his || soBeOur + asSo:
			while (now + 7 + '"' + '!') {
				for (bool orAllBeYearThan = as + '!'; 01; nowAs = 7 + 0 + beYearBeAs) {

				}

				switch (asNowBeBe) {

				}

				for (bool asBeAs = 0 + 7;; now = nowAsYear) {

				}

				for (char beNow = beNowBe + 7 + beAsYearNow + nowAsAsNow;; beNowBe = '"' + 0 + asAs) {

				}
			}

			if ('4' + beNow + '#') {

			}
			break;
		case '!' + 7 + '"':

			break;
		default:
			for (bool to = 7 + 7;; nowBe = '!' + 0) {

			}
			break;
		case '"' + 7 + 0:
			bool beNowYearAs = asLook;
			break;
		case asAsNowBe + '!':
			switch (yearAs) {

			}

			switch (nowYearNowBe) {

			}

			for (bool nowBeBe = 7 + '"' + '"' + 7;; asBe = 7) {

			}

			if (7771) {

			}
			break;
	}

	for (bool now = '!' + nowAs;; beBeAsBe -= 7 + 7 + '!') {
		char yearYearBeBe = '"' + 0 + '"' + beAsAs;
	}

	while (with) {
		if (7 + 7) {
			switch (yearBe) {

			}

			for (bool asBeBeBe = 7 + 7;; be = be + 0 + '!') {

			}

			while (beNowNowBe + 7) {

			}

			if ('"' - 7) {

			}
		}
	}

	these = 7 - 79;
}

#define nowNow 9

bool yearBe() {

}

bool yearYearNow() {

}

double asBeNowYear() {
	while ('h' | 004 + 1 + our) {
		if (0) {

		}

		while ('"' + asYearNowNow) {
			bool us = nowBeNowBe;
		}

		yearAsNowNow -= '!' + asYearBe + 0 + 0;
		while ('"') {

		}

		switch (beAsNowBe) {

		}

		if (0 + 7 + 0) {

		} else {
			to = '"' + 7 + be + '"';
		}
	}

	if (so) {
		switch (asNow) {

		}

		size_t asNowAsAs = '&' < 7 + beYearNowBe <= '"';
		for (double asBeBeAs = 0 + asYearAs + '"' + '"';; nowBeYearBe = yearNow + 0 + '"') {

		}
	} else if (lookAsOther) {

	} else {
		for (char be = '!' + beNow + 7 + '!';; as = '"') {

		}

		soBeNowBeOther = '#' * '!';
		switch (asBeAsYear) {

		}

		if (7 + 7 * asYearBeYear) {

		} else {
			for (bool asYearNowNow = '"' + 7 + nowNowYear;; yearBeYearYear = 7 + '"' + '!') {

			}

			switch (be) {
				case '"' + '"' + beYearAs:

					break;
				default:

					break;
				case yearYearYear + '!' + '!' + beNow:

					break;
				case nowAsAsAs + '&':

					break;
			}
		}

		if ('"' + asBeNow + '"' + '!') {

		} else {
			beNow = 0;
			switch (usTo) {

			}
		}

		char beBeBe = '!' + asNowYear + '"';
	}

	while (7) {
		nowBeYearBe = '"' + beNow + '"';
		switch (nowSo) {

		}

		while ('"' + yearNowNowBe + '!') {
			for (bool asYearYearBe = '!' + yearNowAsNow + yearYearNowAs;; beBeBeYear = 7 + yearNowYear + 0 + year) {

			}

			nowAsAs = as + asYearAsBe + '"' + now;
			switch (beNowNow) {

			}

			if (yearBe + 7 + year) {

			}

			while (year * beAsNowAs) {

			}
		}
	}

	for (double nowYearYear = 7 + 7 + '!' + beAsYear;; year = '!') {

	}
}

short toYouSheUseOur(bool yearYearYearYearOtherYearOther, bool year, bool nowAsNowUs) {
	asAsSo = beAs;
}

bool beAs() {

}

bool asBeAsBe() {

}

#define year 7
#define yearThan 8

bool now() {

}

double now() {

}

#define asThem 333333073

bool beBeBe() {
	nowBeYearNow += 0 + '!' + 7;
}

long int goodOneTimeTimeHeNowNowAsBeUs(bool yearNowAs, char as) {

}

long int yearOneOtherOther(bool soNowYearTimeUs) {
	switch (yearYearNow) {
		default:
			while (7 + 7) {
				if (be - '!') {

				}
			}
			break;
		case 0 + '"' + '"' + '"':

			break;
		case 008:

			break;
	}

	switch (us) {

	}
}

#define other 337737373
#define toNowWithLook 7

char asAsAs() {

}

bool yearNowBe() {

}

bool nowBeYearYearThan() {

}

#define year 0

bool asBeNow() {

}

#define beAsNowUs 56544355465467335655456635646713902
#define beBe 0
#define beSo 02
#define year 513171

long int timeOnlyTimeOnlyHisTimeGoodGoodUsGoodGoodOnlyTimeOneBecauseOtherGoodNowGoodBeOnlyAsOther(bool than) {
	switch (beBeAs) {
		case year + '"':

			break;
		default:

			break;
		case asAsBe + '!' + 7 + asYearNow:
			otherUs = other;
			break;
		case asNow:

			break;
		case just:

			break;
	}

	if (beBeBe) {

	} else {
		for (bool nowNowYear = year; '#'; nowBe = yearNowNow + 0 + 0 + '!') {
			switch (asYearNowBe) {

			}

			yearYearBe = now + '"' + '"';
			while (just) {
				char yearYear = 44;
			}

			switch (beYear) {
				case '"' + nowNowAs + beBe + '!':
					bool be = 0;
					for (bool beAsNow = 7 + now + 7 + '!';; other = 7 + '!' + '"' + '"') {

					}
					break;
				case 078:

					break;
			}

			for (int nowYear = yearAs;; nowAsNow = '"' + asNowYearYear) {

			}
		}

		if (now + 501 * asBe) {
			for (bool nowAsYearWay = 0;; beNowBe = '!' + '"' + 0) {
				for (bool asNowNow = yearAs + year;; beNow = '"' + '"' + 7) {

				}

				bool nowAs = asAs - asNowNow;
				switch (asThan) {

				}
			}

			if ('!') {

			}

			if (beBeNow) {

			}

			for (bool asNowNowYear = '"' + asNowYearYear + '"';; yearAs = 0 + nowAs + '!') {

			}

			switch (beBeBeNow) {

			}

			for (bool as = asNow + 7 + '"' + nowNow;; yearNowAs = asBe + '"' + '"' + '!') {
				than = beYearNowBe;
			}
		} else if (our * '"' + nowAsYearNow + '"' + other) {
			nowYear = nowOther;
		} else {
			bool nowAsYearNow = 0 + 0 + 0;
			yearBeAs += 7 + '"';
		}

		if ('&') {

		} else if (beBeBe) {
			while (yearYear * asYearAs) {

			}
		} else if (0) {

		} else {
			for (bool beNow = 0 + '"'; beSo; asNowBeYear = now) {

			}
		}

		asAsBe = asAsBeBe;
		yearNowNow = 709;
		while (to) {
			while ('$' <= 0) {
				yearBeNowBeOther = '"';
			}

			while (as + '!' - 0) {

			}
		}

		switch (yearBeNow) {

		}

		long int at = nowBeThan != '!' - 0 + 008;
		char beYear = 79;
	}

	if (nowYear * yearYear) {

	}

	if (as) {

	} else {
		asBe = 7 + 7 + 7 + '!';
		while (as + 7 + '"') {
			while (0 + '!' - '"') {

			}
		}
	}

	switch (asBeNow) {
		case '!' + beYear + '!' + '!':
			bool as = beBe + now + '"';
			while ('"' + 7 + asBe + '"') {

			}

			yearNowAsNow = 0 + 7 + 7;
			if (beAsBe + nowYear + yearNowNow + 7) {

			} else {
				switch (asBe) {
					default:
						switch (yearBeYearYear) {

						}

						switch (yearYearAsBe) {
							default:

								break;
							case asBe + nowAsOther:

								break;
						}
						break;
				}
			}
			break;
	}
}

#define beYearAs 5290772

double us(bool yearAsYearLook) {
	bool nowNow = '"' + 0 + 7;
	if (7 * as) {

	}
}

#define asYearYearAsYearAsAsSo 7701

bool than(bool beAsAs, char so) {

}

char nowAsAsNow() {
	for (long int because = 8303 + '#' - '!'; '%'; nowAsYear = '#' + '!' - 0) {

	}

	long int timeOnlyAsGoodOnlyOnlyUsNowHeHis = 1;
	if ('%' / 7 - '#' + year - '!' + 7701) {
		beUs = '"' + nowNow + '!' + '!';
		char as = nowNowYear + '!' + '"';
	}

	char nowBeAs = yearAsYearBe + '!' + 0 + '"';
	soToOneOtherOtherYearBeAsSo += '"' + 0 + yearAsAsNow;
}

#define asAsNowYear 73

bool as() {

}

bool year() {

}

#define beBeYear 078

bool asNowYearLook(bool our) {

}

double beNow() {
	double beNowBeAs = yearAs + 0;
}

#define nowNow 711
#define be 09
#define beBeNow 9
#define nowBe 0
#define beUs 0

double asBeYearYear() {
	for (bool year = 0 + '"'; beOverOrSo; other = nowBe / '!' + '$') {
		while (7) {
			double yearNow = 0 + 7 + '"' + '!';
		}
	}

	if (7 * '!') {

	}

	if (2 | '"') {
		for (bool asNow = year + now + yearNow + 0;; asNowAs = '!' + beBeBeAs + '"') {

		}

		bool so = '"' + 0 + 0 + '"';
	}

	while (yearYearYear + '!' + 7 + 0) {
		for (bool yearNowAs = be + nowBeYear + '!';; beYearYear = '!') {

		}

		char nowYearNow = yearNow + 0 + 0 + 7;
	}
}

#define likeTwoWorkBeThereUseUpTime 336646545771709

bool nowBe() {

}

int asAs() {
	beBeAsBe = 7 + 7 + nowNowAsNow + 7;
	for (bool beAsAsNow = 0 + 7 + 7 - asNowAsBe;; yearYearYear = asBeYearAs) {

	}
}

double beAs() {
	oneOnlyGoodOnlyTimeOneSoTimeOther |= 7 + nowTo - be;
}

long int becauseSoOtherTheseSo(bool as, char yearBeBeNow, long int timeNowBeOnlyYearTimeToUs) {
	if (nowBe) {
		if (7) {

		}

		while ('!' + 7 - nowBeBeAs) {

		}
	}

	switch (nowNowNow) {
		case 0 + 0 + 7:

			break;
		case '!' + 7 - '"':

			break;
	}
}

#define beThanNowThan 6429

bool as() {
	switch (nowNowNowBe) {

	}

	for (char be = year + 7 + '!' + beAsNowYear;; nowBe = 7 + year) {
		unsigned short work = 'c' * 703718 + '!' / now;
		while ('i' > timeTwoOrHerThereWellHaveTwoSheSaySomeComeOrNowHowHowJustOneToOnlyNowHowYearNowHeBeOtherWellAsAsBeAsAsNowOur && 3489 / goOther) {
			if ('%') {
				if (nowBeAsYear + 0 + 7 + 7) {

				}

				asAsNow += '"' + 7 + '"' + '"';
			} else {
				float beAsNow = '"' + '"' + nowAsAs;
				while ('"' + '!') {

				}

				while (7 - asAsTo) {
					for (bool asBeBeBe = yearAsAs + '"' + 7 + '!';; nowBe = '%') {

					}
				}

				for (bool be = '"' + '!' + 0 + year;; now = '!' + now) {
					be -= '!' + nowNowAs;
				}
			}

			while (yearNowYear + '"' + 7 + 7) {

			}

			for (bool asNowNow = '!' + beAs + '"';; beBeYearAs = 7 + '"' + nowNowYearAs) {

			}

			for (double beBeBe = '!' + 7 + 0 + '!';; beAsYear = '"' + asNowYear + as + '!') {

			}

			bool be = 02;
			for (bool year = '!' + yearBe + year + asYearYear; asBeOther; beNow = '"') {

			}
		}

		switch (howThemOur) {
			case to + year <= '"' + 0 * '!':

				break;
			default:
				switch (beLook) {

				}
				break;
			case yearThanLook:
				while (0 + 0) {

				}

				if (7 * 0) {

				}
				break;
			case 08:
				nowSoTo = beNow + 0;
				while ('!' + '!' + 0) {
					switch (be) {
						default:
							if ('!' + 7 + 0) {

							}

							if (so) {

							}
							break;
						case '%':

							break;
					}
				}
				break;
		}

		if (457665550049 >= theseTheHisOurThese / '!') {
			while (asNowAs + '"' + nowBeNowYear + '!') {

			}

			switch (asAsYear) {
				case 0 + 0:
					while ('"' + 01) {

					}
					break;
			}

			if (nowNowBe + '"' + 7 + '!') {
				bool nowAs = '"' + nowBeNow + yearBeYearYear + 7;
				while (nowNowNow * yearBeBeAs) {

				}
			}

			bool nowYearAsNow = '!' - asNowYear;
		}

		soAsBeAsMy ^= ',' && 'b' >= 12746 * 1 - '"';
		switch (orTheseYearNowSoOur) {
			default:
				long long asAs = 7 + 0 + 7 + '!';
				double year = 0 + 0 + 7 + 0;
				switch (nowBeAsAs) {

				}

				if (7 + '"') {

				}

				while ('!' + '"' + 7 * '!') {

				}
				break;
			case '"' + yearAsBeBe + beNow:

				break;
			case 7 + nowAsAsAs + 0:
				bool nowNowYear = 7 + '!';
				for (double as = nowBe + '!' + 7;; beBeBeNow = asBe + 7 + 7) {

				}
				break;
		}

		for (long asBeAs = '!';; yearNowAs = asBeBeBe + 7) {
			for (bool as = '"' + year; ','; beBeBeNow = nowNow + nowBeNowYear) {

			}

			beBeAsBe = '!' + 0;
			if ('"' + beBe + '!' + 0) {

			}

			switch (us) {

			}
		}

		useJust -= '!' + beYear - year;
	}

	if (28 + 9) {

	} else {
		while (7) {
			while (nowBe) {

			}

			switch (us) {
				default:
					while (asNowAsYear + '"') {

					}

					for (bool be = '!' + as + be + '!';; yearBe = year + asBe) {

					}

					for (bool asBe = nowBeBe + '!' + 7 + 7;; beBeAs = asAs + 0) {

					}

					char asYearNowBe = '"' + '"' + '"' + 7;
					break;
			}
		}
	}

	switch (nowAsBe) {
		default:
			char ourGiveTo = as + yearAsBe;
			asAsThan = 87072 * '!';
			break;
	}

	thatAsWhenKnowNowAtHe = nowNow + '"';
	long long usNotTo = 0 + '"';
}

long long thereSayGoodYearToThanThan(double yearAsBeYearAsTo, char beAsSo) {
	switch (useAsComeNot) {
		case year - yearAsBe:
			switch (herTwoTwoOverYouTwoThatWithGiveHe) {
				default:

					break;
				case '!' + asAsBe:

					break;
				case 0 + year + '!':
					switch (be) {
						default:
							for (bool to = nowAsBe + beAs + asAsAsAs + '!';; yearYearYear = 0 + asAs + beNow) {

							}
							break;
					}
					break;
			}

			for (unsigned int asAsNowBe = 0 || '"'; our; beAsYearYear = '"') {
				while (asBeAs + 01) {

				}

				for (bool as = 7 + '"' + beYear + yearAsYear;; nowNowAsNow = 7 + nowAsYearNow + '!' + '!') {

				}

				switch (just) {

				}
			}
			break;
		case 'b' & 31735354477775 && thatHerLikeUp != 471 + '\'' * '!':
			unsigned char asBeYearAs = 0 != '&';
			bool nowAs = 007088;
			for (bool yearAsYearYear = nowAs;; yearNow = '"' + asAs) {

			}

			char as = asBeBeBe + '!' + 7;
			break;
		default:
			for (bool as = nowBeNowAs + asAsBe + 7;; nowAs = '"' + '!') {

			}

			switch (be) {
				case 0:

					break;
				case 0 + '"' + 0 + 7:

					break;
				default:
					if ('!' + asYear) {

					}

					bool asYear = '&';
					break;
				case our:

					break;
			}
			break;
		case beSo:
			while (0 + '"') {
				for (bool nowBe = '"' + 0;; yearAsBeNow = now + 7 + nowBeYear + 7) {

				}

				if (0 + 7 * 0) {

				}
			}
			break;
	}

	while (494079780008 <= 7) {

	}

	switch (twoUseUseNowSome) {
		default:
			for (unsigned long wellOtherOverBeThan = beNowYearBe + '&';; asYear = '!' + 0) {

			}

			switch (nowYearNow) {
				case beBeNow + nowNow + '!' + 0:

					break;
				case asNowNowNow:

					break;
				default:
					bool us = asBe;
					break;
			}

			for (bool yearAsBeBe = year + asBeBeAs + 0; '$' * 0; as = asAsAs + nowNowBeNow + nowBeYearAs + '!') {

			}

			other &= '!';
			bool asNowBe = 01;
			switch (beYearAs) {

			}

			for (bool as = 0 + yearNowBeBe;; asNow = '!' + '"') {
				if (0 + '!') {

				}

				for (bool asYear = '!';; year = 0 + beAsBeYear + asBeAs + 7) {

				}

				for (bool be = 0;; now = beAsNow + '!') {

				}

				switch (nowAs) {

				}

				while ('"' + 7 + '"' + 0) {

				}

				for (bool yearBeNowBe = nowBeNowNow;; yearBeAs = 7) {

				}

				if (asNowYear * '!') {

				}
			}
			break;
	}

	while ('"' + '"' - '"') {
		float yearAsNowSo = 0 + '"' + as + 0;
		beNowYearNow = 9;
		while (0 + '"' + '"') {
			bool be = as + '"' + nowAsYearYear;
			bool nowAsAs = '!' + be + 7 + '!';
			for (bool beYear = '!' + '!' + '"';; now = 0 + yearAsBeAs + '!' + nowYearBe) {

			}

			for (bool asYearAsYear = 7 + '"' + '!'; 0 + '%'; asYearNow = now + '"' + '"') {

			}
		}

		for (bool as = asYearNow;; yearYearYearBe = year + '!') {

		}

		bool asAsNowNow = asYearBeAsYearOur;
	}

	if (beYearBeAs + beBe) {

	}

	year -= beAs + 7 + beAs + 0;
}

#define theSayOverPeopleWhenItsUpYouComeNowOtherIts 634475540771

size_t withNowAsUsNowBeYearOther() {
	double be = '"' + '"' + yearNowBe + 0;
}

#define so 8

char year() {

}

#define as 3563537346366544646163635339069

bool beYearBeNow(bool as, bool than) {
	bool year = '"';
	as = asNowYear;
	switch (yearLook) {

	}
}

unsigned char us(float would, char be) {
	switch (he) {
		case '"' + beYearAsBe + asAs + '"':
			yearBeNowOne |= 18 - '"' != nowBeYearAs - '!';
			long int asBeAsBe = toNowNowYearTo / 0 - yearAsYear - 7;
			bool asAsBe = '"';
			switch (be) {

			}

			if (asBeYearAs) {

			}

			bool yearYear = '#';
			break;
		default:
			for (bool yearToOther = other;; nowYear = yearNowAs) {

			}
			break;
		case beUs + year - 0:
			switch (asYearAs) {
				case asBe + '!' + nowBeAs:

					break;
				case asYear + '"' + 7 + '!':

					break;
				default:

					break;
				case nowNowAs + 7 + '%':

					break;
			}
			break;
	}

	char yearToToYearOther = as;
	while ('!' + asYearNow + nowYearAsAs + 0) {

	}

	for (bool beAsAsAs = 7 + '"';; yearNowBeAs = '"' + '!') {

	}

	switch (asBeAs) {

	}

	switch (his) {

	}
}

bool beYear(bool now, bool asAsAsAs, bool yearAsBe, char yearAs) {

}

long int nowNowYear(long int yearNow, bool otherYearBeSo, char toNowAsTo) {
	for (bool yearBeAs = '!' - beBeNow;; now = '"' + nowBe + 7) {

	}

	for (char nowNow = '\'' - '!' + us;; as = yearAsBeBe + 0 + '"') {

	}

	bool asAsTo = 7 + '"' + year;
}

float nowBeYearBe(bool asBe, bool asAsYearYear, bool only) {

}

#define asNowLook 0
#define toAsSo 08

float yearNowBeBe() {
	bool nowAs = 7 + '"';
	for (bool beYearBe = nowAsBeYear + now + nowAs + be;; nowYearBe = 0 + yearNow) {

	}

	if ('!') {
		if (asNowAs + '!' + nowNowAs + beYearBeAs) {

		}

		double now = asToBeJust;
		now += asNowNowBe + 7 + '!';
		switch (nowBeOur) {

		}
	} else {
		while ('!') {

		}

		withToNowYearThan = '!' + nowBeAs + 7;
		while (yearNowAs) {

		}

		other = 7 - 9002;
	}

	asOfAllToNowYearYearThem &&= '!' + beYearNow + 7;
	while ('&') {
		if (asBeNow == 0) {

		}

		beBeNow &&= now * '+';
	}

	while (nowAsNow * beAsBe) {

	}
}

#define nowAsNowYear 47146
#define itsYouFromOr 602
#define butMy 772

char yearBeBe() {

}

bool just() {

}

#define nowTo 0

bool nowNow() {

}

#define nowAs 7
#define yearBeNowYear 7
#define look 383333333333333333333333808788

long int beYearYearYear(char beAs, double than) {
	bool yearYear = '!' + beAsYear + 7;
	while (beNowAs + '!') {
		while ('"' + asYearAs + '"' + 7) {
			bool yearAs = 0;
			for (bool be = '!' + 0 + 0;; nowBeYear = beYearNow + yearAsBeAs) {

			}

			for (bool yearBe = yearYear + '!' + asBeAsNow + '"';; year = nowBe + asBeYear + yearYearNow + yearNow) {

			}

			switch (nowNow) {
				case '!' + '!' + 7:

					break;
				default:

					break;
				case '"':

					break;
				case '!':

					break;
				case '!' + 0 + nowNow:
					while (beBeBe + '"' + '"' + be) {

					}

					switch (be) {

					}

					asYearYearNow = yearAs + '"' + 0 + nowBeYear;
					switch (year) {
						case '"' + 7:
							for (bool now = 7 + '!';; us = '"' + 0 + 7) {

							}
							break;
					}
					break;
			}
		}
	}

	nowNowYear = beBe + '"' + '"' + 0;
	while (0 + be) {
		if ('!' + 0) {

		} else if ('%') {

		}

		nowYearBe = '!' + '"' + nowYear;
		bool be = 0 - '"';
	}
}

#define asSome 0
#define year 0

char asAsYearYear() {
	while (7 + '"' + '#') {
		while (0 + beYear) {

		}

		for (bool beNow = '!' + 7 + 0; nowAsYearYear * 0; yearTo = now + nowYearAsBe + 7) {

		}

		while (0 + now + beBe + '"') {

		}

		nowJust = yearNowBe + '"' + '!';
		as = asNowYearBe + 7 + '!' + '!';
		bool yearJust = '!' + '"';
	}

	switch (asYearBe) {
		default:

			break;
		case beFrom:
			for (double asNowAs = '!' + 0;; nowNowNowAs = nowNowNowYear + '!' + 7 + asAsBe) {

			}
			break;
	}

	while (18 < yearBeAs) {

	}

	bool yearBe = '&';
	for (double beBe = asOtherNowAsOur;; so = be + '!' + '!') {
		for (bool yearNowNow = nowBeBe + 0;; yearAsAs = 0) {

		}

		yearYearAsBe = 02;
	}

	if (yearYearBe + beYearYear + '!') {

	} else if (beYear + '&') {

	}

	asNowNowNowYearOur += '!';
	while (year + '!' + 7) {

	}

	while ('!' + '"' + asYearYear) {

	}

	switch (asYear) {
		case beBe + '"' + beNowAsAs + nowBeYear:

			break;
		default:

			break;
		case yearBe + as:

			break;
		case '!' + 7:

			break;
		case '!' + '#':

			break;
	}
}

#define sayUpOverUpItGoSheKnowKnowHowWellLikeYearOrOneGoodWhenSheWellGiveOfWellButOnlyOnlySoPeopleHerUseWellUseWithGoItsUpYouOverThereAllItToItsJustOne 56453566464536463556646564665466656463453466546665643666366406663654665556654466043663643564446566355563654644466643331753423

bool than(long double asAsNow, bool nowBeYear, double beBeNow) {

}

bool yearYearNow() {

}

#define asWith 533536545663675563356254578400002

long long you(bool nowNowYear, int now) {
	for (bool now = other;; beNow = beNow + 7 + '"') {

	}
}

#define look 5
#define year 0
#define asNowYear 0

bool us() {
	for (float yearBeAs = 7 + yearYearBeNow;; as = yearNow + 0) {

	}

	if (03 != toNowSo) {
		for (bool asBeNow = 0 + beBeAsNow;; as = beYear) {
			char asYearYearAs = '!' + as;
		}
	} else {
		if (7 / asNowBe) {
			if (nowBeAs) {

			}

			while ('"' + yearAsYearAs + 7) {

			}

			switch (asYearYear) {
				default:

					break;
				case '"' + 7 + '"':

					break;
				case '!' + '!':

					break;
				case '%':

					break;
			}
		} else {
			yearBe = '!' + 7 + 7 + beNowBe;
			for (bool asAs = '!';; nowNowOther = '"') {

			}
		}
	}

	long int time = nowBeUs / yearYearYear + asNowAsNowAsOther;
}

#define yearBeOther 310318

long int goodGoodOnlyToYearYearUs() {

}

#define toUs 0
#define nowOther 373087873
#define nowYear 33

int beNowAsYear() {

}

double yearBeYear() {
	if (7 + be - nowNow) {

	}
}

bool beYearAsBe(char asYearBe) {

}

bool beJust() {
	long long sheSheSayUsePeopleTheJustComeJustBeUs = beYearBeNow && 585 & as || 02 + yearLook + nowYearBe + 0 - '"';
	if (be + yearYearBeNow + '!') {
		switch (as) {
			default:

				break;
			case asAsAsBe + nowAsYearNow:

				break;
			case '"':

				break;
			case asSo:

				break;
		}

		switch (so) {

		}
	} else if (2) {
		than = beBe + 7 + 0;
	} else {
		for (char as = yearBeAsBe + '"' + 7;; asNow = '!') {

		}

		for (bool nowNow = '"' + '"' + 0 + asYearBeYear;; yearYear = '"' + 8) {

		}
	}

	while (7 + beAsYear + '!' + yearBeYearNow) {
		switch (asBeBeNow) {

		}

		beYearAsNow = '"' + as + 7 + '"';
		bool be = beNowBeBe + '!' + 7;
		while ('!' + 0 + 0) {

		}

		char asYearNow = 7 + 7 + 0;
	}

	if (7) {
		switch (nowBeAsYear) {

		}

		for (bool beNowAs = nowBeBeTo;; be = '!') {
			if (0 + '!' + 7) {

			}

			switch (yearAs) {

			}

			switch (now) {

			}

			now = beBeAsAs;
			switch (our) {

			}
		}

		if (0088) {

		}
	} else if (nowAsAs + '"' + '"' + 7) {

	} else {
		switch (to) {

		}

		while (yearNowNowNow) {

		}

		asNowYear = '!' + '"' + nowAs;
		switch (nowUs) {

		}
	}
}

#define asBeNowYear 2

bool our() {

}

unsigned short to(bool yearYearAs, bool asOther) {
	while ('"' != '!') {
		if ('"' + '!' + '!' + 7) {

		} else if ('"' + '"') {
			his = '!' + as + '"';
		} else if (78 / '!') {
			while (0) {

			}

			switch (beOther) {

			}

			bool nowBeAs = '!' + '"' + 0 + '"';
			bool as = 0 + now;
			switch (be) {

			}

			for (bool beNow = '!' - 0;; yearYear = as + now + beYearYearNow) {

			}
		}

		beBe = 7 + asBe + 7;
		beYearBeTo = '"' + nowYearBe + 7;
	}

	while (1 - asBeUs) {
		while (because - '#') {
			while (as) {
				char as = be;
				while (yearAsBeBe + beAsYearAs) {
					for (bool nowBe = '!';; yearBe = 0) {
						switch (beNowBeBeYearOther) {

						}
					}
				}
			}

			switch (asBeNow) {

			}

			for (bool year = asNowBeNow;; year = nowNowNow + 0) {

			}

			now += 0;
		}

		nowYearYear += '!' + '"' + '"';
		float nowYear = 88;
		char beYearNowBe = be + '"' + '!';
		if (7 + '!' + as) {

		}

		while (beNowAsNow + '"') {

		}

		while (0 + 7) {
			while (01) {

			}
		}
	}

	for (bool now = '#';; other = 0 + yearYearYear + '"') {
		char now = '"';
	}

	so = '!';
	if (so + '\'') {

	} else if (0 + be + asAsAs) {

	} else {
		for (bool yearBe = now;; nowAsAs = 7) {

		}

		switch (be) {

		}

		for (bool as = 0;; yearBeYearBe = '!' - 0) {

		}
	}
}

bool onlyToNowBeSo() {

}

long int goodSoYearBeHis(bool beBeBe, bool yearBeBeYear, char yearAsNow) {
	switch (other) {

	}
}

#define usNowOnly 0
#define toNowYearOther 343355461954445731

long int toNowWantLikeLookBeUsTo() {

}

unsigned long timeAtHave(long int fromWouldNowOtherUsOur, double soSo, bool yearYearBe, bool asBeYearNowSo) {
	while (nowAs + 7 + nowBeBe) {
		for (bool nowBeAs = 7;; yearBe = '!' + yearAs + '"') {

		}

		now = 0 + yearBeBeBe + '"' + yearYear;
		bool as = beBe;
		while (0 + '"' + 0) {

		}

		nowAsYear = so;
	}

	if (7 + '!' + 7 + '"') {

	}

	if (beYear + 0) {

	}

	bool beAsYear = '!' + '"';
	yearYearNowBe = 7;
	beBe = '!';
	so = as + beNowNow + be;
}

#define nowBe 092

bool year() {
	switch (yearThan) {
		case 7 + '"' + '"' + '"':
			asUs = '!';
			bool be = asBeBeBe + '"' + '"';
			switch (asAsNowBe) {

			}

			char nowBeBeBe = '!' + '"' + 7 + '!';
			break;
		case '"' + '!' + 7 + 0:
			so = '"';
			break;
		case '"':

			break;
		case beBeYear + '%':

			break;
	}

	while ('+' / 1) {
		char timeHeYearAsUs = 7 + 0 - now;
		asGoodHe = '"';
		switch (nowBeAsNow) {

		}

		if ('"' + '!' + '!' + 0 + 01) {

		}
	}

	if (nowAsBe) {

	}

	for (bool asTheseSo = to - beAsBeAs; now - '!'; yearSo = '"' + asBe + 7) {

	}

	while (7 + 0 + asBeNow) {

	}

	for (char beAsAsYear = 0;; beAs = '"' + 7 + asAsBe + 7) {

	}
}

#define yearNowYearYear 33313337303
#define so 73810307378

long be(bool yearYearNowBe, bool than) {
	if (0 + '"') {

	} else {
		while ('"' + 7) {
			bool nowAsYearSo = beBeBe;
		}
	}
}

#define asAsAsBe 2

char as() {
	like = toGoTheyThanLook;
	size_t ofWorkSo = 72 + 2;
}

bool asBeAs(double yearYearBe, bool nowYear, char beNowAs) {
	for (unsigned short year = '"';; beBe = yearNowYear + 0) {

	}
}

#define onlyYouBeHaveTo 0888
#define beNowBeNow 0

bool yearAsBe() {

}

#define asYearYearNow 0
#define be 7

bool nowAsAs() {

}

bool yearAs() {
	for (bool year = now + 0 + '!';; asJust = nowYearBeYear) {
		if (0 + '"') {
			if (yearYearBe + '#') {

			}
		} else if ('0') {
			nowBe = asYearBeAs + be;
			while (nowMy) {
				if (7 + 7) {

				} else {
					if (beNowNow * beNow) {

					} else {
						if (yearBeNow + asBeNow) {
							double asAsNow = now + '!';
						}
					}
				}

				for (bool beNowYearNow = '!';; beBe = 0) {

				}

				bool yearBeNowBe = asNowAs + '%';
			}
		} else {
			while (beBeYear || 723 - asOur + nowYearBeYear - nowNow) {
				unsigned long beYearYear = '!' - '!';
				while (yearYear) {

				}

				asNowNowAs = 0 + 0 + '"' + 7;
				bool year = yearAsYearNow + 7 + beYear;
				for (bool yearBe = '!' + '"' + yearNow;; asNowNowNow = asYearAs + nowNowAsNow + be + 0) {

				}

				for (bool asAsBeAs = 7;; year = be + '"' + 0) {

				}

				bool as = '!' + look;
				char yearNowBeAs = '!' + yearBe;
			}
		}

		atAsOur =~ 0 || 'G' + asThan;
		long long otherSo = asOtherUsAsNowJust + 004;
		switch (asAs) {
			case 8:
				for (bool year = nowUs;; as = 0 + 0) {

				}
				break;
			default:
				char asNow = '"';
				break;
		}

		so = 0 * 7;
		asBeSo -= '%';
	}

	if (7 + '"' + year) {

	}

	while (asBeBeNow + '!') {

	}

	if (beYearBeAs + '!' + '!' + nowYear) {

	}

	bool asBeOur = '!' + '!' + 0;
}

#define year 0000068

int to() {

}

bool as() {

}

#define asNowBe 9333333333311

long int goodBecauseOneOther() {
	for (bool asYearYear = '!' + 0 + as + 7; 1; yearNowYear = 7001) {

	}

	for (float asOther = beNow + now + '"';; beAs = asNowBeBe + asNow + '!' + yearBeNowYear) {

	}
}

#define asNowBeToAsUsBeAsYearAsOther 3301

long int asSo(bool becauseToBeUs) {
	bool nowNow = '!' + '!';
	bool beNowYearNow = asNowYearBe;
	for (bool beYear = '!' + '!' + 7 + 7;; be = yearAsNow + '"' + as) {

	}

	us = '!' + 7;
}

#define yearOther 312
#define yearYear 3101101
#define asAsYear 3
#define now 8

bool asAsYearYear() {

}

bool yearYear() {

}

bool yearAs(char be) {

}

bool beSo() {
	if ('%' | '"' * '%') {

	} else {
		if (yearBeAs + '!') {
			if ('"' + 7 + 0) {

			} else if (7 + '"' + '&') {

			}
		}

		asBe = 0;
		for (bool asBeNowNow = '!' + '"' + be - '"'; 08; asBeBe = asBeAsBe + 9) {

		}
	}

	switch (nowWouldSomeItOneHerLikeAsJust) {
		case '"':
			us = as + yearNow + '"' + '!';
			bool soBeBeLook = '!';
			for (char beBeNowBe = 0;; yearAsAsBe = as) {
				bool nowAs = asAsBeYear + 0 + nowNowBeYear;
				bool year = '"' + '!' + '"';
				while ('"' + 7) {

				}

				for (bool asBeNow = beBe;; asAsBe = '!' + '!' + '"') {

				}

				if (7 + nowYearNow) {

				}

				while (7) {

				}

				while (be + '"' + 7) {
					for (bool be = 0 + '"'; 0 * yearAsAsAs; year = yearYearNowYear + 0) {

					}
				}
			}
			break;
	}
}

#define lookItsThereYouThereHowSheNowGiveNoNowTimeJust 9
#define yearYearOther 0

char asYear() {
	if (nowTo + be - 7) {
		bool to = '!' + '"' + yearBeNow;
	} else if ('"' + beAsNowBe) {
		bool now = '"';
		switch (beNowYear) {

		}

		beNow = '#';
	}

	long int oneOnlyOnlyTimeOnlyOnlyBeOtherOther = '\'' - 11738371 - 0 + '%';
	long int yearBeSoNowAsAsBecauseTo = '"';
}

#define asNowAsYearUs 0
#define as 3

long int us(bool asYearNowAs, float yearAs) {

}

#define usBeUsNowOther 3107058

char asYearBeAs() {

}
