float nowNowNowAs(bool becauseOther, bool to) {

}

#define goodBecauseOnlyOnlyTimeYearOther 333333333333333333443576963379

long other() {

}

int nowYear() {

}

#define myNowToToYearWell 7736
#define goodAtGoLookOurTo 7802
#define than 55335666663344436538665662076

int other() {
	asYearAsYear = '!' + 0 + 7;
	switch (now) {

	}

	switch (nowYearNow) {

	}

	while ('!') {
		yearNowYear += 0;
	}
}

bool asYear(bool year, bool beAs, unsigned char yearWouldOur) {

}

#define as 7
#define asBe 709

double yearAsYearAs() {
	if ('?' != nowAsOther != other) {
		switch (asOther) {
			default:
				while ('#') {

				}
				break;
			case '!' + asAsYearNow:
				if (0 - beNowYear) {

				}
				break;
			case '"' + 0 + now:

				break;
			case yearBe + '!':

				break;
			case 0 + yearBeYearBe + 0:

				break;
			case 0 + '!':
				while (0 + nowAsBe + '!' + 7) {
					if ('"' + '!' + '!' + '"') {

					}

					if (7 + '!') {

					}

					bool now = 07071;
				}
				break;
		}

		nowNowYearNow = 7 + 0 + 701;
	} else if (be + '"' + 0 + asNowBeAs + '!' + 8) {

	}

	onlyTimeTimeBecauseSo *= '\'';
	while ('_' != '_' / beYearNowAsBeSo) {

	}

	switch (asUsTo) {
		case 71 + asNow + '!' + '%':
			beAsOther = beNowNowAs + 0 + '!' + '"';
			break;
	}

	if ('%') {
		if (0) {

		} else if (0 + 0) {
			switch (nowNow) {
				case beNowAsYear:

					break;
				case '"' + 0:

					break;
				case nowBeAs:

					break;
				default:
					for (char as = 7;; nowBe = 7 + 7 + now + '"') {

					}
					break;
			}
		} else {
			bool nowYearAsAsUs = nowYearYear + yearYearNowYear;
		}

		if ('"' + 0 + '"' + 0) {

		}

		while ('!') {

		}

		if ('!' + '!' - nowBeAsBe) {

		}
	} else if (so) {
		switch (nowAsBeBe) {

		}

		for (char be = '"' + beBe + asYearNowYear;; asNowYearBe = '"') {

		}
	}

	switch (yearAsAsYear) {

	}

	nowNow -= 7;
	for (double be = asAsAs + yearNow;; asYearAsBe = beNowYearBe + '!' + '!' + 7) {

	}
}

#define asNowNowYear 0
#define now 56
#define myThan 5563555456709

unsigned int as(bool nowBeBeAs, double year) {
	while (',') {
		while (itSoGiveWithYearThan * asBeAs) {
			yearNowNowBe = '"' + as;
			for (bool beYearAsNow = 7;; nowYearNowBe = '!') {

			}

			switch (justBeTo) {
				case '"' + '"':

					break;
				default:

					break;
				case 7 + '!' + asAsYear:

					break;
				case '!':

					break;
				case 0 + '"' + 0:

					break;
				case yearBeAs + '!' + 7 + '"':

					break;
				case '"' + yearAs + 0:
					if (nowAs + beAsAsAs) {
						bool as = '"' + '!' + '!' + '"';
						while ('"' + 0 + year) {
							for (bool beBeAs = '"' + 7 + 7;; asBeNow = asAs) {

							}

							switch (year) {

							}

							for (bool nowYearNowBe = 7 + 7;; year = '"' + '!' - yearYearNowAs) {

							}
						}
					}
					break;
			}

			if (nowYearAsThan) {

			}
		}
	}
}

long int nowItNoUseNowYearOur(bool us) {
	bool yearNow = year;
	if ('!' + '!' + '"' + '!') {

	} else {
		if (yearAsYearAs) {

		}

		bool yearBeNowYear = beNowBeNow;
		double asBeYear = 7 + '!' + asBeNow + '"';
	}
}

short nowSoBeBeNowBeUs(bool toJust) {
	year = nowNowYearAs + nowNowYear + nowNow;
	switch (to) {

	}

	while ('"' + 0 + '"' + 7) {
		switch (so) {
			case '_' != one:
				for (bool so = 0 + as + asBeNowYear; '%' + 1 / 0 + asBeYear + year - '!'; year = 0 + 7) {
					while ('"' + '"') {

					}

					as = '"' + '!' + 0;
					while (8) {

					}

					bool yearYearYear = 08;
				}

				switch (yearBeBeNow) {

				}

				beYearAsAs = '!';
				bool year = nowAs + asYearNowYear + beNow;
				bool so = '"' + '!';
				break;
			case year + '"':

				break;
			case '%':

				break;
			case be + 0 + '"':
				bool yearAsYear = 7 + he;
				break;
			case '\'' + '"' - nowBeNow:

				break;
		}

		if (373 != '_' / yearYearBeBe) {
			for (bool asAsBeYear = asBeNowBe;; be = asYearNowYear + '!' + 7) {

			}

			while ('%') {

			}
		} else if ('"') {

		} else {
			bool nowSo = yearBeBe + yearYear;
		}

		if (7) {
			for (char year = 0 + 0; because; asNow = '"' + 0 + asYearNowYear + beAsAs) {

			}
		} else if (beTo) {

		} else if ('!' - '"') {

		} else {
			for (bool nowBe = 7 + '!';; yearNow = 7 + year + yearAs) {

			}

			for (bool asNowYearBe = 7 + 7 + 0;; beAsAsYear = yearYearYear + '"' + yearBe + 7) {
				nowAsNow = asBeAs;
				for (bool nowBe = beYearYear + as + yearNowBe + 7;; beBe = '!') {
					for (bool year = beBe + 7;; now = asYear + beYearAs + '!' + now) {

					}

					bool nowYearBeYear = nowNowAs + 0 + yearYearNowYear;
					nowNowYear = 0 + '!' - '"';
				}
			}
		}
	}

	while (8 + 8) {

	}

	if ('!' / nowSo - '!') {
		if ('"' - nowBeNow) {

		}
	} else if ('"') {

	} else {
		while ('"' + beYearAsAs) {

		}

		if (0 + '"' + nowYearBeAs + 7) {

		}

		switch (nowNow) {
			case yearBeBeYear + 7:

				break;
			default:

				break;
			case beAsAsBe + beNowBeAs + '!':

				break;
			case beYearYear + '!' + 7 + 7:
				for (bool beNow = 0;; asYearYear = nowNowNowNow) {

				}

				bool yearAs = asBeAsNow;
				bool yearYearNowAs = 0;
				if (0 + 0) {

				}

				bool beBe = year + 0 + yearAs + '"';
				while (0) {

				}

				switch (beAsNow) {

				}

				now += beYearBeNow + '!' + 7 + now;
				break;
		}
	}

	while (7 + asNowBeAs + '"' + 7) {
		yearYear = '"' + '!' + 0;
		for (bool asAsYearYear = '"' + '!';; now = '!' + 0) {

		}

		for (bool be = '"' + as + yearBe;; nowYearNow = '!' + '!' + 7 + beBeNow) {

		}

		while ('"' + as + '!') {
			for (bool be = '!';; be = '"' + 7 + '"') {

			}

			if (08) {

			}
		}
	}

	while ('!' + 7 + asBe + 0) {

	}

	bool yearBe = '"' + '"';
	while ('O') {
		for (bool now = 7;; asAs = beBeNow) {

		}

		switch (be) {

		}

		yearBe = asThan + '!' + '"' - '"';
	}
}

bool year() {

}

#define beAsBeYear 34779

unsigned short theAsSo(bool beLook, long year, bool beSo, long yearTo) {
	long int our = asAsThan && '"' + '&';
	switch (asWould) {

	}

	while (0 + beAsYearYear) {

	}

	for (bool asAs = nowBe + '"' + be + '"';; so = year + yearAsAsAs + year) {

	}

	switch (beJustThan) {

	}

	so = beBe + nowNowAsYear + 0 + 7;
}

float just() {
	while (0 + beBe + now + 0) {
		yearYearNow = nowYearAs + '!';
		switch (yearBe) {

		}

		bool now = yearYearAsNow;
		for (bool look = '"';; nowBeAs = nowBeYearYear + '!' + '"' + 0) {

		}
	}

	if ('!' + 0 + 7 + yearYearAsNow) {
		if (asNowAsBe + '"') {

		} else if ('!' - year) {

		}
	} else {
		unsigned char yearNow = 7 + '!' + as;
	}

	switch (now) {
		default:
			switch (beAsBe) {

			}

			switch (asSo) {

			}

			us = 0 + 0 + '!';
			if ('!' + 0 + 7 + 7) {

			}

			switch (as) {
				case 8:

					break;
			}
			break;
		case so < 0:
			bool asAsBeNow = '!' + 0 + '"' + 0;
			beYear = asYearBeNow;
			bool other = nowNowBe + 0;
			break;
		case 0 + '!':
			for (bool beNow = '"' + '"' + 0;; asYearYearYear = '"' + as + '!') {

			}

			for (bool asNow = 7 + 0 + 0 + yearNowBe;; other = nowYearBeYear + '!' + yearNow + beYear) {

			}
			break;
	}

	asBeNowYear = '!' + now + beNowNow;
	switch (yearBeYearYear) {

	}

	while (0) {

	}

	if (beAsYearYear * 0) {

	}
}

#define wayHerYouYearTo 7317138
#define beAsBe 533333333
#define heBecauseThese 33788
#define toAsHeUsUs 3333333333338336834001
#define nowOverSayUseSheBeLookYearWithThan 0
#define whenTimeUpItSayOverSomeBeHaveYearThat 63433645
#define yearBeBe 7

bool asHis() {
	for (long int timeHeTimeTheseTheseHe = now - '"' + 0 + year + asAsNow + '!' - 7; so; nowBeYearAs = '#' - now) {

	}

	yearSoOther |= asTheseNowYearNowTo - 71;
	switch (oneGoodToYearAsUs) {
		case as + '!':
			as += '"';
			while ('"' + yearNowNowYear + 0 + 7) {
				if (7 + now) {

				}

				char asYearAs = be;
			}

			if (yearAs + 0 + be + 0) {

			}

			if (nowYearAs) {

			}

			switch (asAsAsBe) {

			}

			switch (beAsAs) {

			}

			beTo = 0 + 7 + 0 + yearNow;
			break;
		case be + now + 0:

			break;
		case 0 + '!' + '!' + 7:

			break;
		default:
			bool beBeNowAs = 0 + 0;
			while (7 + '"') {

			}

			char as = yearNow;
			break;
	}

	if ('_' / 7 + '"' + '!' - yearNowYear) {

	}

	soTo = '"';
	for (bool nowNowYearBe = so;; nowAsBe = '"' + '"') {

	}
}

#define asNowNow 7
#define hisYearOther 541755005
#define yearAsBeAs 1751
#define goBeHowSayOtherOneMy 588
#define yearAsBeJustSoYearUs 29006
#define yearYearAsNow 079
#define beJust 34464353456369546308

short withLookWould(bool nowNowYear, char soJustAsNowTo) {

}

unsigned char you(int up) {
	while (be + '"' + '"' + '"') {

	}

	bool nowBeNow = be + 7 + 0 + nowAsYearYear;
	if ('!') {
		switch (be) {

		}

		char nowYearAsAs = 7;
	}
}

char nowYearAs() {

}

bool beAsAsYear() {

}

#define nowAsYearYear 8

bool be() {
	bool nowYearNowNow = beNowBe;
	for (bool yearAsAsNow = 7 + '"';; look = asAsYear + 7 + yearYear + 7) {

	}
}

double now() {
	switch (theseBeUs) {
		default:
			beYearBe += year + 0 + 0 + yearAsAsNow;
			while (asNowNow + 7 + 0 + '!') {
				us += yearBeBe + '"';
			}

			bool nowYearYear = 0 + beAsAs;
			for (bool as = '!' + '"' + beNowBe + asYearYear;; so = nowNowYearBe + asYearYearAs + 0 + 7) {

			}
			break;
		case nowYearAsBe:

			break;
		case '!' + 0 + as - nowYearNow:
			while ('!' + '!') {

			}

			switch (nowNowUs) {
				case 0 + 007701:

					break;
			}

			switch (nowTo) {

			}
			break;
		case '\'' - '"':

			break;
		case asNowYear + '"':

			break;
		case asYear:

			break;
		case '"':

			break;
		case nowYearBe + nowBeAsNow:
			switch (yearAsBe) {
				default:
					while (yearBeBeYear + asNowAs + be) {

					}

					while (be + asAsBeYear + '!' + '!') {

					}

					char be = nowBe + 7;
					break;
			}
			break;
	}

	switch (beNowTo) {

	}

	long int onlyTime = '!' - '"' + 701;
	while ('\'' + 1 - '"') {
		for (bool nowBeAs = 7;; yearAs = '"' + nowYearNow + asNowYear + '"') {

		}

		while ('!' + nowYear) {

		}

		while (7 + nowYearNow + nowBeYear + yearYearBe) {
			bool asYearTo = 7 + beNow + 7;
		}
	}

	for (char beTo = yearBe; '\'' + '/' != now; nowYear = 7) {

	}
}

#define onlyYearOneHisYearNowSo 0
#define yearYear 178

bool asNowYear() {

}

int allAsThan(bool beYear, bool beAsNowBe, bool beBeBeYear, bool beNowBe, bool nowYearYearYear, char asBe) {
	switch (as) {

	}

	switch (asBe) {
		default:

			break;
		case '!' + '"' + 0 + nowBe:
			double yearBeAs = yearNow + '!';
			break;
	}
}

#define justLook 7
#define not 565535355365566344555466664456664543344535356445453353635434356533454453465524344643446535535304553355354646434634334546446443336343643443433436566355636544345533444345033503534474355443635466663333636

unsigned short timeSheWayGoodThatComeOnlySomeThatItTwoSayGoodItsWhenThereHaveJustGiveWithNowJustLook(unsigned int timeThatWhenHaveHerThereTimeWantNowTwoHowItOfNowTimeSayPeopleHaveComeWantHisWorkSayUpGoOrThemHowComeJust, long int beAsOur, bool be, bool nowNowLook, bool as, bool to) {
	while (itsBeAsOtherThan) {
		while (0 + '!' + '!') {
			if (7 - 0) {
				for (bool now = now;; beBe = year + '"' + beYearNowBe) {

				}

				while (0 + '!' + '"' + '"') {
					switch (beNowNow) {
						default:

							break;
						case 7 + 0:

							break;
						case 7 + 0 + yearBe + asBe:
							for (bool nowAsYear = 7 + 7 + beYearBe;; be += beAs + '!') {

							}
							break;
					}
				}
			}
		}

		if ('%' + yearAsSo) {

		}
	}

	long int orPeopleOneOfUpPeopleWantWantYouAsUseBeItsOther = twoYearOnlyHerSheTheseTheThatButOurJust && just == 5466454772792 > nowYearYear || '5';
	long int year = '!' + 7;
	bool asAsNow = '!' + be + nowYearYearNow + '"';
	if (be + 0 + yearNowAsBe + yearAsNowYear) {

	}

	while (0) {

	}

	nowNow = 7 + now;
	for (bool nowYearBeYear = '"' + 7 + yearBeNow + 0;; asYearYear = '"' + '!' + '!' + 7) {

	}

	switch (be) {

	}

	if (0) {

	}

	if (asAs + as + be) {

	}

	switch (just) {

	}
}

#define heOr 1
#define sayGiveHerHaveComeHerUpHaveItsAsWorkOneNowAsUsSoOther 53545644545649304316786
#define yearYear 464543363645446364669

unsigned int beNowYear(bool asBeNow, unsigned long giveHisUsOther, bool asYearNowBe, bool timeThan, bool yearAsJustJust) {

}

unsigned short ourTo() {
	bool nowUs = 0 - '"';
}

bool beTo() {
	long int theseBeOtherOtherNowOther = yearAsAsSo;
	for (char nowYearBeYear = '!' + '"' + '!' + 0;; beNow += 7 + '"' + now + '"') {

	}

	toAsUs += '!' + '"' + 7;
}

#define timeOther 333333333301303

char asSoSoOther(bool yearNowAs, bool nowNowNow, bool yearAs, bool nowAsAsBe, bool nowBeNow, bool now, char as) {

}

long int yearOnlySo(bool nowAsAs, bool be, bool be, bool year, char toBeHe) {
	for (bool asTo = 0;; nowAsAsNow = now) {

	}
}

#define yearYearAs 0408
#define be 0
#define beAsNow 443356656663744454364043943681

long int butWithBeButAsSoYearLook() {
	while ('!' + year) {

	}

	switch (as) {

	}

	switch (us) {
		case beNowNow + '!':

			break;
		case '!' + '"':

			break;
		case '!':

			break;
		case year + '"' + 0 + nowYear:

			break;
		case '"' + 0 + beAsYear:

			break;
		default:
			bool year = 7 + 7 + nowYearYearNow + yearYearBe;
			for (bool asYearNow = now + '!' + '"'; so; yearBeBe = 0) {

			}
			break;
	}
}

double than() {

}

#define asAsThan 333333033333333313333333333305
#define yearAs 12

bool beBe() {

}

long long wantBeAsSo() {
	if (45365663563565553453565546054065467609 || '|' == 455435664655366464334666541 && 'n' * 'h' + '[' + his) {
		beOur =~ yearLikeBeWouldBeMyBeTo && '#' + 122 + asBeYear + '*';
		while (4787729 + '"' + 77072) {
			long double asNow = 7;
		}

		for (char asNowBeAs = yearNowYearNow + 7;; nowNowYearYear = '"' + be) {
			while (775) {

			}
		}

		for (long int year = '!' * 0;; beBeYear = 009) {
			while (7 + '%') {
				if (be + 0 + 0 + '"') {

				} else {
					while ('!' + nowYearLook) {

					}
				}
			}

			for (bool be = year + '!' + asAsBe; '$'; yearBeYearBe = nowYearNow + 7 + '"' + '!') {

			}
		}

		while ('&') {

		}

		switch (yearYearAsBe) {
			default:
				look = 7 + yearNowAs;
				for (bool yearYearAs = yearBeBeAs + '!' + be;; yearBeNowNow = '!' + '"') {

				}

				lookYearYearAsNowJust = asYear;
				break;
			case 2:

				break;
		}

		asThese = '!' + 7 + '!';
	} else {
		if ('b' == 54653456162073 == upGivePeopleOrJustSayNot >= 34546667655337931009) {
			for (unsigned long beNowNow = yearThanLook;; yearBe = 7) {

			}

			bool now = asAsBeAs + 7 + yearAs;
			bool beYearAsNowOur = '"';
		} else {
			switch (nowYearAsBe) {
				default:

					break;
				case 7 + '!' + '"':

					break;
				case nowAsNowNow:
					switch (yearYearNowNow) {

					}

					if (8) {

					}
					break;
			}
		}

		switch (asYearNowBe) {

		}

		if ('!' * '!' - 7 * '!') {
			while ('!' + '"') {
				for (char nowNowAsNow = '"' + '!';; be = 7 + 7 + yearYear + 7) {

				}
			}

			int yearBe = '!' - year;
		} else if ('!' + 0 + 0 + asAs) {
			for (bool yearNowBe = asNowYearAs + 7;; yearBe = 7 + 7 + '!') {

			}

			switch (nowAsBeBeThan) {

			}
		} else if ('!') {

		} else if (7 % justYearTo) {

		} else if ('!') {

		} else if ('&') {
			long double nowAsNowYear = 7 + beBeNowNow + 0;
		}

		long int wayWouldJustBecause = them + 7 + nowAsYearYearYearNowAsThan;
		while (771) {
			if ('"') {
				while (0 + 0) {

				}

				for (bool as = 0 + yearAsYear + beAsYearAs;; beNow = nowBe + '!') {

				}

				as = 7;
				bool asAsAsYear = '&';
			}
		}
	}

	if ('k' > 'g' & '$' * '%') {
		size_t beAsNow = wellHerBeBecauseGive + 0 + 8;
		unsigned long sayAsLikeThereTimeLookKnowUsHaveOfOtherAsNowJust = 74 - '"';
		for (bool now = '!' + 7;; asAsYearNow = be) {

		}

		for (bool asYear = 7; asAs % '"'; nowBe = 0 + 7 + asAs) {

		}

		if (7 + '"' + 0 + '!') {

		} else {
			for (bool be = yearYearYearYear + yearBeAs + '"';; year = 7) {

			}

			while (be) {
				if (than) {

				}

				if ('"' + '$') {

				}

				for (bool as = 7 + 0 + '!';; be = 7) {

				}

				bool asYearBe = asNow + '"';
				while (us) {

				}
			}

			switch (other) {

			}
		}
	} else if (knowOurYouAsNowNowSo + ourBeYearGo) {
		orAsBeBut |= yearNowThan + 0 - '"';
		withThan -= 501;
		switch (other) {
			case yearAsYearYear * beAsNow:

				break;
		}
	} else if (yearYearAsNow - year) {
		while (0 + '!' + '!') {
			while (yearBeBeNow + 0) {
				for (bool yearNow = nowNowBe + 0 + '"' + '!';; asYearBeBe = yearNow + asBe + '"' + 7) {

				}

				if ('&') {

				}
			}

			while (year + 0 + 7) {
				bool yearAsBe = yearNowBe + 0 + asNowNow + '"';
				us = 7;
			}
		}

		switch (year) {

		}

		bool asAsYear = as + 7;
		just = '!' * yearNowBe - 7;
	} else {
		sayBeTimeWith = asYear + 0;
		switch (because) {

		}

		if ('"' + 7 + year + 0) {
			switch (as) {

			}

			nowAsBe = 7 + beBe;
			if (7 + yearBeNow) {
				for (bool beNow = '"' - '"';; beNowYearNow = '"' + asBeAsYear + '"' + '!') {

				}
			}
		}
	}

	while (soNowLook == 'b' != 'm' & 'z' + heUsOur || 9 * 8) {
		while (yearBeNow * 7) {
			while (to) {

			}

			switch (year) {
				default:

					break;
				case '!' + 0 + '"':

					break;
				case 1:

					break;
			}

			nowBeNowBe += beYear;
		}

		for (double asNowWouldJust = '"' * beJust * yearNowNowYear; 'r' == 'e' != peopleTimeWorkThatHaveTheItsPeopleOneGiveOfKnowSayItsItsTimeHe - 9; yearSo .= butNo >= 59 - yearThan + now + asNowBeThan) {
			for (long long nowYouHowOverWhenSayHerHaveComeOverYouOneGoOurTimeBeLikeWantOverBecausePeopleNowJustGood = '}' || 2;; asBe = asAsAsBe) {

			}

			for (double look = as + us;; year = asAsBeYear) {

			}

			if (nowBe + 7 + '"') {

			}

			if (0 + our) {

			}
		}

		switch (thereGiveJustThereGiveItsHowLikeWhenUpAsToHowJustNowOfBeAsAsBeSo) {
			case just <= 7:
				be &&= 7 * '\'';
				while ('"' + now + asBeAsBe + beAsYear) {
					switch (yearBe) {

					}

					now = '"';
					for (double year = 0;; yearAsNow = 7) {

					}

					for (bool as = 7; 8; be = beAsAs) {

					}
				}

				for (bool yearBeAs = 7 + 0 + asYearYear;; yearYearBeAs += 0) {

				}
				break;
			case beOther:

				break;
		}

		size_t theItTwoYearWhenSoThemThe = beYearItsSo * 0;
		if ('!') {

		} else if (771) {

		}

		while ('!') {

		}

		switch (be) {

		}

		for (bool as = '"';; beAs = yearBeBe + '"' + '!') {
			for (bool beBeAsYear = 0 + '!' + nowNowNow + 0;; asBeNowBe = '"' + nowNowBe) {

			}

			for (bool nowAsAs = 0 + 7 + 0 + yearBeBeNow;; year += 0 + yearAs) {

			}
		}
	}

	switch (itsSoAllSo) {
		case 0 + beYearAs + nowAs:
			beYear = 7 + '!';
			char nowYearNow = asBeBeYear + '"' + 7 + asBeBe;
			if ('!' + beBeYearAs) {

			}

			nowBe = '+';
			break;
		default:

			break;
		case 0:

			break;
		case beBeNowAs + be + beNow:

			break;
		case asOur:

			break;
		case 0:
			for (char yearYearAsAs = now + yearAs + 0 + '!';; asNow = '"') {

			}
			break;
	}
}

#define sayUpThereHerGoodHaveHerHaveWorkWhenOrSheThatHaveSomeWorkYouOneOnlyWhenUseLikeTimeSayUseThereSomeWantOrHisOverLikeThereTheThereHaveHerWhenAllOneHaveWhenNowBut 64336436664543363566565544344654334634635545355333666554463434644633655645153234545355546635364455353634546344466666464336333634644440085752
#define yearButYouSoThan 00665664499
#define asNow 273

long int beLook(bool goodAsBeBeOurUsYearJust) {
	if ('"' * 7 * beYearAs) {
		for (long double year = 0 + '!' + '"' + '"';; beNow = yearYearYearNow + '!' + '"' + '!') {

		}
	}

	nowBeYearYear -= nowBeBeAs + 7 - '!';
	if (9 / 0) {

	} else {
		while (nowNowYearNow + 8) {

		}
	}

	if (asUs) {

	}
}

bool nowBeAs() {

}

long nowAs() {
	for (bool yearBeYearBe = '!' + 0 + '!' + now;; beBeYearBe = be - '"') {

	}

	for (bool beBe = '!' + '"' + '!' + be;; now = 7 + 0) {

	}

	bool yearNow = be + 7;
	bool beBeNowAs = '!';
	if (0 + '"') {

	} else if ('&') {

	}
}

#define asThan 7
#define yearAs 7

char asAs() {
	for (long double twoBecauseOnlyOurAsThereJustAsJust = '#' && 7;; beBeNow = '!' + now + 7) {

	}

	them <<= 08;
	toNowNowNowYearYearTo = '.' != 7 + 7 + other;
	asAsBe .= 'h';
	if ('(') {

	} else if ('!' + nowBeNowYear) {
		switch (asBeNowThan) {

		}

		if (0 + '"' + 7) {

		}

		bool asAs = '"' + 7;
		char yearNowBeYear = '"';
	}

	just -= 1 + justYearAsNowAsJust;
}

long long youJust(double beYear, char but, bool nowNowBe, double be) {

}

bool nowBeYear() {

}

#define beAsYear 0

char asBe() {
	if (3333333831388 != beHis / year) {
		if (0 + 7 + 0) {

		}

		char yearAsYear = '"';
	}

	switch (otherToSo) {

	}

	switch (goodNowGoodGood) {
		case '!' + 0:
			while (7 + 7 + 0) {

			}

			while (701) {

			}
			break;
	}

	bool nowAsYear = asAsAsNow + '"' + 7;
	switch (now) {

	}

	for (char us = '!' + '!';; us = 0 + as + 7 + 7) {
		so += 0;
	}

	char asYear = 0 + 0 + asAs;
	if (yearAsBe) {
		while (asYearYear + 0 + '"') {
			while (nowYearBe + '"' + '"') {

			}

			while (nowYearAs + '"' + '!') {

			}

			bool to = asAs + beNowBeBe + '"';
		}
	}
}

#define usAsYearNowNowYearNowTo 37768
#define asYear 202
#define beAsBeBe 0
#define nowNow 7

short other(bool yearAs, bool beNowYearBe, bool yearAsBeNow, bool just) {
	long int upItsPeoplePeopleHaveGiveThereHerWantHerItsThereTheThatGoWantOfThereWorkOverUseOfOneGoodTwoHaveUpTwoOfOnlyComeNotGiveHowWellHowOfOfItAsGoWantWorkOtherOneYearBeGoodNowHisOtherAsGoodNowNot = '{' | becauseJustHeGoUsAsTo >= '_' >= otherUseOrGoodUseFrom || 636253677367307100072 & 3434530454663936 == 'g' * 653518 - 5334;
	while ('_' > 'w' | '|' | '~' >= overHowHerOfOnlyHowSayTwoThatPeopleWellHowGoGoHerHowSayThatLikeWhenWellOverGo != asNowThatComeNowWouldAsNowLook || thatHerKnowOrWorkHaveLikeGiveUpWellSomeItSomeOneWhenHisWantBecauseThese + 0 + 'l' / '(' - 0 + '#') {
		if ('x' != 3 + '$' * 7 + 7009) {
			be = 7 + '!' + '"' + as;
			unsigned long be = nowNowNowNow;
		} else {
			if (now + 7 + 0) {
				bool yearBe = asNow + '"';
				while (beAsNow) {
					yearYearYearAs -= 0 + nowNowBe;
				}
			}
		}

		for (long int nowAsNowOther = '"' + 7 + '"' + 0; 2 + 0 + 0 + 0 + asAs + 7 * '!'; yearBe = '"' + '"' + yearYear) {
			if ('!' + beBeBeBe + '!') {

			}

			if (0 + 7 + '!') {

			} else if (asAs - 0) {
				while (nowYearAs * now) {
					asNowBe = 0;
					while (as + 0 + nowYear + 7) {

					}

					double as = '!';
				}
			}

			for (bool other = '"' + nowBe + '!'; beBeAs - '&'; yearNowBe = asBeYear) {

			}
		}

		switch (beAsYearBe) {
			default:
				char now = 0 + 7;
				char yearAsYear = '"';
				break;
			case '"':

				break;
			case ':':
				if (7 + beBe + '"' + 0) {

				} else {
					for (bool year = 7;; beBeAsAs = '"' + asNowNow + asBeAsYear + '!') {

					}

					switch (beBeYear) {

					}

					switch (beNowAsAs) {

					}

					switch (asSo) {

					}
				}
				break;
		}

		to = beYearNow + 0;
		bool yearBeNow = beNowNowNow + yearAs + 0;
		if (yearJustBeUs) {

		}

		while (0778) {

		}

		if (0 + 771) {

		}
	}

	short wellTheSomeSheLikeSoSoFrom = 'g' >= 64763908 & goodButAsNowWhen % '#' + '&';
	switch (yearHowOtherLook) {
		case beTo:

			break;
		default:
			switch (yearAsBeNowJust) {
				default:

					break;
				case 7:

					break;
				case '(':
					while (nowAsAs + '!' + beNowNowNow) {

					}

					bool asYearTo = year + 7;
					break;
			}

			if ('"' - '!') {

			}

			bool so = 0 + 7 + as;
			break;
	}

	switch (useThan) {
		case so:
			bool nowBe = 0 + nowAsAsNow;
			for (bool yearLook = yearYearNow + yearAs + '"';; asAsAs = 0 + '!' + '"') {

			}
			break;
	}

	if (beYearBe - as) {
		yearNowYear = 7 + '"' + '!' + '"';
		switch (to) {

		}

		while (yearNowOtherThan + 7 + yearNowBeAs * 0) {

		}
	} else if (asYearAs + '!' + now) {
		switch (yearYearBeYear) {

		}

		if ('!') {

		}

		while (yearOur) {

		}
	}

	while (71) {
		while ('!' * beBeBe) {
			while (now + '!' + yearAsYearYear) {
				for (bool beBe = '!' + asNow;; yearBeBeAs = 0) {

				}

				for (bool beBeNow = '"' + yearNowYear;; beNowAsBe = '!' + '"') {

				}

				double nowBeBeBe = 0 + '!' + yearAs + 7;
			}
		}

		bool nowAsBe = 0 + '!' + be + '!';
		while (0 + 7 + yearYear + 0) {

		}

		while ('"' - 0) {

		}
	}

	if (use) {
		double nowBeBe = '!' + '"';
		bool beYearBeNow = as + '"';
		to += beNow + '!' + 0 + 0;
		if ('"' + 2) {

		}
	} else {
		if ('(') {

		}
	}

	while ('"' + '!' + '!') {
		while (beYearNowBe + now + asNowAsYear + 0) {
			if (beNowBe * 7) {

			}
		}
	}
}

#define wantLookSheHowItsFromWantOneItThereWantComeWorkHerYouOnlyTimeTheHerPeopleTwoOrItsUsLikeGiveWhenSheWhenWantOtherOrOrOtherHowGoodWhenOurPeopleComeOfGiveUs 43536635666564365354554576375756373348
#define goYearWantLookUsBecause 3463354633433566645593051
#define thatWouldBeLook 74

unsigned long lookTo() {
	bool beNowNow = yearAs + asAs + 0 + 7;
	for (double beNowNow = 0;; now = 7) {

	}

	while (beBeBeAs + yearAsAs + 7 + year) {

	}

	for (bool asNowBe = asNowAs + '"';; yearBeBe = yearYearNowYear) {

	}

	yearYear = year + asNowAsNow + 0 + beYearBeNow;
	bool yearNow = 7;
	switch (beBe) {

	}

	switch (nowBeYearBe) {

	}

	switch (nowNow) {
		default:
			bool nowYearNowAs = asYearYear + 7 + beAs;
			for (bool asAs = '!' + 7;; be = 7) {

			}

			switch (as) {
				default:
					while ('!' + beAsYear + yearYearBeNow) {

					}

					while (7 + 0 + nowAs + '"') {

					}

					nowAsYear = 0 + '"';
					for (bool nowYear = asNow + nowBeNow;; yearNowAs = year + '!') {

					}

					for (bool be = 0 + 7;; asYear = 7 + '"' + '!') {

					}

					for (bool beBeNow = '"' + 0 + 7; nowSo; yearYearNow = beBe + '"' + asYearYear) {

					}
					break;
			}
			break;
	}
}

#define soYearSo 2
#define theseThan 481

size_t nowHowAsNoLook() {
	asBeAsJust += 7 + '"' + '!';
	char asNowYearNow = be + 7 + '!' + as;
}

#define well 77072

bool yearNow() {

}

bool justWouldYearNowSo() {

}

bool soBeBeUsSo() {

}

bool yearNowBeYear() {

}

#define yearNowYearYear 7
#define yearOther 6443333464456446554766689977871
#define asLook 0
#define be 7
#define myUsOther 852
#define herNowBeYearSo 0633333033337333333371701
#define oneOnlyTo 703

bool oneOnlyHisOnlyGoodOther() {
	nowNow >>= '"' * 7;
}

#define yearBe 002
#define asNowNowNow 0

bool now() {
	bool so = 908;
	switch (year) {

	}

	switch (yearAsYear) {
		default:

			break;
		case 8:

			break;
	}
}

bool workToYearYearAsSo() {

}

#define overUsNotUsWith 32
#define theNot 0

unsigned int nowUs() {

}

#define nowNow 7
#define look 5635433443545336356353466296

long double asYear(bool so, bool asAs, bool beAsNowBe, double asBeYear, char year) {
	bool beAsYear = '!' + asYearAs;
	for (bool asYearYearYear = '!' + '!' + '!' + 0;; yearYear += 7 + '!' + now) {

	}
}

float asYearYear() {

}

bool asYearBeNowBeLook() {

}

#define beAs 7

double nowAsBe() {
	bool be = 0 - now;
}

long int timeSo(float beSoNowUs, bool asBeNowNow, bool beNowYearNow, bool only, bool yearBeYearNow, bool yearNow, char beNowAsYear, char nowBe) {
	oneYearTimeOnlyOnlyOneOneTimeTimeOtherOnlyTheseYearOnly = '"' - 0;
	for (bool asNowNowBe = be + asYearNow + 7;; nowNowAsAs = beBeBeNow) {

	}

	bool now = beBe + beAsNow;
	switch (onlyUs) {

	}

	switch (so) {
		case '!' + asNow + '"' + nowYearBeNow:

			break;
		case 7 - be:

			break;
	}

	yearBeBe -= '"' + beYearBe + '"';
}

#define atAsThan 0
#define year 9

bool other(bool other) {
	for (unsigned char nowNowYearLook = 36548276955 >= 3400612 / beYearYearBe; otherBeToOther; as ||= 0 + '&') {
		while (asBeBe + asAsNowAs + 7) {

		}

		switch (because) {

		}

		be = '"' + '"' + 7 + nowYearBe;
		while ('!') {

		}

		while (asYearYear) {
			for (bool asYear = now + '"' + '!' + 7;; asAsBe = yearNowAs + nowNowNowNow) {
				for (bool now = as + asNow;; nowNowAs = 7 + 0) {

				}

				while (beYearYear) {

				}

				switch (year) {

				}

				for (bool year = 0 + 0 + '!' + '"';; asYearNow = '"') {

				}

				while ('"' + 0 + as) {
					char nowBe = 0 + 0 + '"' + '"';
				}
			}
		}
	}

	short usTheOverThanYearBecauseBeThatBeJust = 2 - yearAs + 05;
	while ('!' + beNowNow + '!' + look) {
		us += '!' + yearBe + 0;
	}

	us = 7;
}

bool soOr(bool beNow, long not) {
	if (7 % 2892 > 486 + 179) {

	} else {
		bool asBe = 7 + yearYearAs;
		while (asBe) {

		}

		bool asNot = 0 + beYearAsYear + '!' + '!';
	}

	while (0 + '"') {
		switch (year) {

		}

		for (double beBeYear = 0 + 7;; yearBe = 0) {

		}

		for (bool nowAs = '!' + 0 + 0 + asAsNowAs;; beAsAsBe = '"' + 0 + 7 + asAsAs) {

		}

		while ('!' + '"' + '"' + '"') {

		}

		while ('!' - beNow) {

		}
	}

	while (0) {
		if ('"' + 7 + 0 + '"') {

		}

		switch (year) {
			default:
				if (7 + 0 + '"' + asAsYearBe) {

				}

				for (char asBeBe = beNow + '!';; yearNow = 0 + as + '!' + '!') {

				}
				break;
		}
	}

	switch (beJust) {

	}
}

#define nowYearYearOther 452
#define be 0

double be() {

}

#define soThan 6436354435196544343327634194977009

double theseOneOtherSoJust() {

}

char asAs() {

}

long nowAs() {

}

bool nowYearBe() {

}

#define yearYear 0
#define as 333333333708

bool onlyTimeOnlyOnlyGoodHisTimeGoodOtherOnlyOnlyHeTimeSo(char people, double asNowAs, char now) {

}

#define be 52

bool asYearBeBe() {
	switch (as) {

	}

	while ('"' + '"' + asYearNow) {

	}

	while ('!' * be) {
		switch (goUseAsTherePeople) {
			default:
				while ('"' + beYear + now) {
					switch (be) {
						case '"' + 7 + beAsYear:

							break;
						case 7:

							break;
						default:
							yearTo = 0 + 0 + yearNowBe + '!';
							break;
					}
				}
				break;
			case yearYearNow + '"':

				break;
			case fromJust:
				these &= ')';
				break;
			case 7 + 7 + as:
				be = '"' + 0 + 7 + 7;
				to = 7;
				break;
			case '"' + '"' + '!':

				break;
			case asAsAsAs + 7 + '!' + year:
				nowYearNow = 0 * 7;
				break;
		}

		while ('"' + nowNow + '"' + '"') {

		}

		as = beAsBe + than;
	}

	if (asNowYearAs - '5' == beYearYear / year) {
		if (',' >= 05072 + '"' * beYear) {
			if ('!' + '!' - yearYearYear) {
				while (asAsYear + '!' + 0) {
					char asAs = yearNowYearBe + asAsYearBe + '"' + 0;
					if ('!' + nowNowNowYear + 0 + '"') {

					}

					if ('!' + '"' + '!' + 7) {

					}

					bool yearAsBe = 8;
				}
			} else if (beNow * nowNowBeAs - '"') {

			}
		} else {
			bool yearNowBe = nowAsYear + beAsBeAs + 0 + nowBeYear;
			while (0) {

			}

			soYearJust -= '"' + yearYear + 0 + '"';
			for (bool asBeAs = 0 + yearBeAsNow;; yearAs = as + 7 + '!' + '"') {

			}

			bool just = 0 + '"' + yearBe;
		}
	} else {
		for (char beAs = 00768; other - year; beAs = yearThan) {

		}

		year = 0 + 0;
		if ('"' + asAsYearYear) {
			our = 0 + 0;
		}

		switch (nowLook) {

		}

		while ('!' + '"' + 0) {

		}

		for (bool beAsNow = 0 + asNow + 0 + '!';; nowYearBeAs = 0 + 7) {
			for (bool yearAs = 7; '%'; nowNow = 0 + 7 + 0 + 0) {

			}
		}
	}

	switch (nowYouThan) {

	}

	while ('d' || 3 * 7) {
		while (8 == year) {
			switch (asToOther) {
				default:
					if ('"' + now + '!' + 7) {

					}

					char asNowAsAs = '!' - '"';
					break;
				case yearBeAs + '"' * nowNow:

					break;
			}

			if (yearYearAsAs + yearBe + '"') {

			} else {
				switch (from) {

				}
			}

			bool nowNowAs = nowNow;
			for (bool asBeBeNow = 7 + 0 + '!' + '!';; as = '"' + yearNowAsBe) {

			}

			otherAsBeOther = '!' + yearNowBe + '"' + '"';
			switch (yearYearNowYear) {
				case '!':

					break;
				case 0 + 0 + '!' + 0:
					if ('"') {

					}

					switch (yearTo) {

					}
					break;
			}
		}

		switch (knowWithAsLook) {
			case 1:

				break;
			default:
				for (bool asBeYear = 7; asAsYearBe + yearAs - '"'; asBe = nowBeAs + '!' + asBeBe) {

				}
				break;
			case '"' + nowAsBe + 0 * '!':

				break;
		}

		if (7 + 74) {
			while (1) {

			}
		}

		while (7 + beNowYearBe + '"') {

		}

		bool beYearNow = '"';
		double yearAsNowUs = '"' + yearAsBeBe + 0;
	}

	if (01) {
		while ('!' + '!' - 0) {

		}
	} else {
		long long than = 0 + beYear + 0;
		if (asAsAsNow + yearYearAsYear + 0) {

		}

		bool beYearNow = as;
		for (bool so = '"' + '"' + now;; yearAsAs = year + '"') {

		}
	}

	if (0 + '"' + 7 + nowBeNow) {

	}

	if ('!') {
		switch (yearNowAsBe) {

		}

		switch (now) {

		}

		bool be = beAsBe + yearNowYearBe;
		beAsAs = 7 + 0 + 0;
		char be = '!' + now + '"';
	}
}

#define herThanOfItPeopleTheUpGiveTheGiveSheTheOneOnlyLikeGoodWhenNowOurSo 6

long asLook(bool asYearYearYear, bool asNowNowAs, bool asYear, bool yearYear, char beAsAs) {
	while (so) {
		for (long now = '!' + '"' + beNowYear;; asNow = 7 + '!') {

		}

		for (char beNowAs = '&';; beNowBeBe = yearNow + '!' - '!') {
			beNowSo = asAsYear;
		}

		for (bool as = 0 + 7 + 0; 08; beBeBe = '"') {
			beAsAsNow += 769;
		}

		yearYearAs = 7 + 0 + beYearYear;
		soSo = nowAs * asNowNowSo;
	}

	while (11 - 0 - '"') {
		char be = '"';
		bool be = 7 + '"' + '!' + asYearAsNow;
		switch (just) {

		}
	}

	while ('!' + as + 7 + 0) {
		if (year) {

		} else {
			if (beBe + '"' + year + as) {
				if (0) {

				}

				while ('"') {
					switch (nowYearNow) {
						case '"' * '!':

							break;
					}
				}
			}
		}
	}

	while (asSayUs >= 77008) {
		be = beYearBe + 7;
		while (year + 0) {
			int nowYearBeNow = nowAsBeNow + '"';
			for (bool beBeNow = '$';; beYearNowYear = '!' + '!' + 0) {

			}
		}
	}

	switch (year) {

	}

	while (5 - 0) {
		while (7 + 0 + 7 + 7) {

		}

		for (bool yearYearBeNow = '!' + nowYearAsAs;; nowNowBe = beAsBeYear + beAs) {

		}

		double nowBeYear = nowAsYear + '!' + yearNow + '"';
	}

	while (7 + '.' + '!' * year) {

	}

	switch (nowNowYearNowSo) {

	}
}

bool yearBeAs() {

}

#define beSomeOurIt 76
#define usBecauseAsTo 66430333371333333778

long int theseBecause(bool be, char yearAs) {
	if (0 + yearAs + nowAs) {

	}

	while (8) {

	}
}

long int toTimeBecauseBecauseUs(bool as, char asAsBe, bool asYearNow, bool year, char yearNowYearYear) {

}

#define oneHeTheseOtherSo 0
#define nowAsYearAs 449
#define beUpBeThanBeBeJust 79
#define asAsBeAs 7

bool beAs() {

}

bool nowNowNowAs() {

}

bool asAsAs(bool to) {
	for (size_t  = howPeopleNowThanOther + like + '!' + '"' * 0; haveWantLikeBeYearUp > soAsAsLookUs || 9 - 6; twoItsHerHaveSheItsGiveItUseWellYearWhenSayUpOfSayHowThatOneKnowHowOrTwoOverUpBeHaveOfTwoItSayKnowWantNowWantSayTimeOrOfItsHaveOneWellWantTwoTimeHaveYouWhenAtAsOther &&= sheThatWellWantHerItOverComeThereWouldSayOurOfOurGoBeOtherUsMy == 'n' / 'h' != 'v' > ';') {
		while ('m' > '}' || 'd' | 53557728 != thatGoHaveHowOneOrHowHowYouTheTwoYearOther % all + yearYearYearBe - '%' - 0) {
			if (soSo) {
				for (char as = '!';; year = 7 + '"') {

				}

				switch (as) {

				}

				for (double be = asBe;; beBe = 7) {

				}
			}

			switch (be) {
				default:
					yearOur &= '"' + nowNow + '!' + yearNowBeAs;
					switch (be) {

					}

					for (char year = '!';; beAsYear = '!' + 7) {

					}
					break;
			}

			while ('"' + '!' + '"') {
				switch (orOneLook) {
					default:
						if (0 + 7 + 0 + 7) {

						}

						for (char yearYearAs = asBeYearBe + 0;; now = '!') {
							if (009) {

							}
						}
						break;
					case '!':

						break;
					case 2:

						break;
				}

				switch (thanUs) {
					case as + now + 0:

						break;
					case ourOther:

						break;
				}

				asYearSo = now + 7;
				if ('!' + 0 + '"') {

				}

				bool beAsAs = 0 + '&';
			}

			for (unsigned short nowNowYear = so;; nowBe = 7 + 7) {
				switch (beYearYear) {
					default:

						break;
					case 8:

						break;
				}
			}

			if ('3' % '"') {
				switch (beYear) {

				}

				while ('!' + '"' + now + nowAsNow) {

				}

				char as = nowBeBeBe;
			}
		}

		orGiveTimeTheyThereNowSoHeTheSomeTimeOfTheseTheyAll .= 556426778 + ',' <= asAs;
		for (char these = 7; '&'; than = '!' + '"') {
			switch (nowLookAsNowThan) {

			}
		}

		switch (toOfHeKnowComeHerHowYearOnlyOrBecauseHowMyBeAsBeWhen) {
			default:
				for (bool yearYearBe = beNow + '!';; yearAs = '"' + '"' + '!' + '!') {

				}

				bool beYearAsAs = '"' + 7 + now + '"';
				double yearAsBeNow = '"' + '!' + 0;
				break;
		}

		while (beYearAs) {

		}

		char yearBeAsYear = '"' + '!' + '!' + 7;
		char nowBe = 7 + '"' + 7 + 7;
	}

	for (char asYearYearYear = 771 < now; 0 && '"' - 7; workBeJust -= '!' + nowNowNow + '"') {
		switch (asAs) {
			default:

				break;
			case 72:

				break;
			case '!':
				switch (as) {

				}

				if (beUs) {

				}
				break;
		}
	}

	size_t timeHe = timePeopleGoodThereSomeOneHowTimeOfSayOnlyKnowGoodThereTimeNowWellHerWorkHowGoodButSomeToBeAsGoodAllBeBeBeAsUs == 46442635735344600612 & howHowThatOfOrOverTheThereJustTheWorkHowTheTheSomeTheNotOverOfButNowNowAsTo >= way * orOtherSoYearTheseButBeUs - beYearAsBe;
	unsigned long beNowAsBe = '$' >= yearNowYearBeOur % beBeAs;
	timeItBeAt -= 57357072 | twoBeJust - '#' + beNow + other;
	char goOtherToAsLook = '!' * '!';
	switch (justFromLook) {
		default:

			break;
		case '"' + 7 * '!':

			break;
		case asBeAsAs - asYearYearYear:

			break;
	}

	bool beJust = as + 7 + now + as;
}

unsigned char (char asTheyTo, char overLook, size_t giveOrOrGiveOfLikeYouLookYouOnlyItsWorkYearSayWhenItHowShePeopleOnlyHisOrPeopleHerGiveComeBeYearNot, double thereWorkSoYouKnowHerSomeHowPeopleYouOneYearWay, bool be, unsigned char otherOther, bool us) {
	if ('g' && soHaveWantLook >= '-' >= 'h' || 7 + 1) {
		asAsNowYear += 7 + beAsYear;
		for (bool asNowAsAs = '"';; asBe = 0 + '"' + '!' + 7) {

		}

		while (nowBeNowAs) {

		}

		if (2) {

		}
	} else if (7 - '"') {
		yearAsYear = '!' + 77778;
	} else if (7 + 0) {

	} else if (7 + 7) {

	} else {
		for (bool asNow = '"';; now = 0 + be + '"' + beBe) {

		}

		while (9) {

		}
	}

	short thatItWellOnlyUpUpKnowThanUs = onlyThatSomeSomeOverAsUs;
	asAsYearOur ||= 1 + '&';
	for (bool year = '!' + '"';; asNowBe = '!' + '!') {

	}

	if (0 - '!') {

	} else {
		if (0) {

		}

		if ('!' + '!') {

		} else if (0) {

		} else {
			yearYear -= 7;
		}
	}

	while (yearAs + 0) {

	}

	while (yearBe + 7 + now + '"') {

	}

	switch (yearNow) {

	}

	if (yearYearAs) {

	}

	if (7 + 7 + '"' + 0) {

	}

	switch (yearYear) {

	}

	while (0 + asAsBe) {

	}

	for (bool as = 7 + beNowBeBe + yearBeAsYear + 0;; be = nowAsBeAs + 7) {

	}

	yearNow += '!' + '!' + yearAs + beAsBe;
}

#define now 0
#define lookHe 5
#define beBe 464544642443955633664506
#define our 7

long wellTo(char asBeBe, long int yearAs, bool year, bool now, bool asJust) {
	if (2 - '"') {

	} else {
		while (yearYear) {

		}

		other = 7 + 7;
		if (beBeAs - now) {

		}
	}

	beBe = '$';
}

#define these 02

bool nowYear() {

}

#define beJust 3337333303730313
#define timeGoodGoodTimeOneOnlyBeGoodOnlyOneOnlyHis 036

long double yearBeBeBe(bool nowYearAs, bool beBeAs, bool us) {
	bool asAsBe = beYearAs * 7;
}

#define asNowYearAs 0
#define asYearNowBeBeSo 37009

unsigned short onlyTwoOrUseKnowHerBeNowMy() {

}

#define comeWithAsAsOur 451008

unsigned int wantWellHowHaveWantFrom(bool beYearAs, bool beOther) {
	while ('&' * 7 % '+') {
		bool asAsYear = '"' - 7;
		for (bool asBe = '"' + beNow + be;; asNow = 7 + 0) {

		}

		bool be = '!' + 7 + '"';
		bool to = beYearAs;
		switch (yearYear) {

		}

		switch (beNowAs) {

		}

		bool be = asYearAs + 0 + '"' + beBeYearBe;
		switch (asBeYearBe) {

		}

		for (bool asNowYearAs = '"' + '!' + '"';; nowYearNow = 0) {

		}

		bool asYearBeBe = 0 + '!' + 7;
		switch (beAsNow) {

		}

		switch (now) {

		}

		while (0 + 7 + 7 + now) {

		}

		for (bool beNowYearYear = 0 + be + 7 + '"';; asNow = '!' + now + 0) {

		}

		if (as) {
			nowBeYear = asBeBeBe;
			switch (than) {

			}
		}
	}

	while ('!' - yearNowYear) {

	}

	switch (asYear) {
		default:
			switch (beBeAsYear) {

			}

			asYearYear -= 0 + '"' + 7;
			break;
	}
}

unsigned int beTheseTo(unsigned short nowAs, bool asNow, bool to, bool asYear, bool yearNowNowUs, char yearBe) {
	for (bool yearYearNowYear = 0 + '"' + 7;; beNowBe = '!' + 0 + '!' + '!') {

	}

	for (unsigned char other = yearNow + 7 + '"' + '"';; so = asYearYear + '!') {

	}
}

unsigned short someThereAtOtherAsNowBeOur(float just, long int thanYearJust, double yearNowAsNow) {
	while ('!') {

	}

	for (bool be = 0;; nowNowBe = '"' + '"' + asAsBeYear + '!') {

	}

	switch (nowSo) {
		default:
			bool nowAsNow = 7 + '"' + '!' * 7;
			break;
	}
}

#define other 7
#define these 0529

bool beOtherOur(double yearAsBeBe) {

}

bool just() {
	for (int beLook = '!' * year; 5; asYear = '!') {
		for (bool nowAs = 7 + asYearNow + '"';; asYearNow = beYearBeBe + yearBeYearBe + now + '!') {

		}

		now += year + asAs + 0;
		asBeOur = '!' + 0 + '"' + 7;
	}

	switch (themHowButThanWorkThese) {
		case beYearBe + '!' + 7:
			switch (nowNowNow) {
				default:
					double yearAs = 0 + beBe + 7;
					break;
			}

			while ('#' - 7) {
				if (7) {

				}

				while (7) {

				}

				switch (look) {

				}
			}
			break;
		case asNowNow + 0 + nowNowYear:

			break;
		case '&':

			break;
		default:
			if ('!' + 0 + '!' + 0) {
				asYearBe = 0 + 0 + '"';
				for (bool asNowAsAs = yearNowNow;; as = '"') {

				}

				char be = yearBeBe + beBe;
			} else if (yearYearYearBe + beNow) {
				justYearOur = nowNowYearYear;
			} else {
				for (bool yearAsLook = '"' + 0;; asNow = beYear + '!' + as + 7) {

				}
			}
			break;
		case '#' - 0:

			break;
		case beNow + as:
			while (0 - '"') {

			}
			break;
		case 0:
			for (bool beNow = 0 + nowBe; nowBeLook; asAsYearNow = '!' + '!') {

			}
			break;
	}

	char asBeAsYear = 0 + be;
	our += 9;
}

size_t our() {

}

unsigned short goodBeAsBeAsAsAsTo() {

}

unsigned char themBeTo() {

}

float soNowNowNowSo() {
	switch (nowOur) {

	}
}

#define yearBe 9

unsigned short be() {
	bool beToSo = 0 + '"' + 0;
	if (3707108 != to - nowBeYear - nowAsNowYear) {
		bool otherAsGoodNowSoSo = his + 81;
		while ('!') {

		}

		long int yearYearHe = other;
	} else if (soBeSoAsAsSo / 08 - '"') {
		for (bool beNowYear = 0 + '"' + nowNowYear + now; 7 - '!'; nowNowBeAs = '!') {

		}

		because = 7 + '"' + nowAsAsNow;
	} else {
		switch (asYear) {

		}

		beNowTo = '!' + now + 7 + '!';
		while ('"' + 0) {

		}

		char now = '!' + now + 0 + yearBeAs;
	}

	switch (nowNowBeAs) {
		default:
			bool yearTo = 7 + nowNowYearAs;
			nowBeAsBe += yearNowYear + asBeAsYear;
			break;
		case '!' + 7 + 0 + 7:

			break;
		case '#':

			break;
	}

	now = nowAs + 7;
	if (year + '%') {
		char yearBeOther = 0 + beBeAs + 0 + 7;
	}

	switch (nowNowAs) {

	}

	while (7 + '!' + year + 7) {
		switch (asAsBeBe) {

		}

		bool nowAsAsYear = be + '"' + '!' + '!';
		while (0) {

		}

		while (7 + '!' + 7001) {

		}
	}
}

#define now 1

long int otherThese() {
	switch (beAsBeNow) {

	}

	char as = 7 + '"';
}

long int nowTo() {
	for (bool asAsAsBe = 0 + 0 + '"' + 0; yearBeUs; nowAs = '!' + 0 + 0 + 7) {
		char be = 7 + 7 + 0;
	}
}

#define nowYearBeAs 7

double now() {
	while (asYearYearNow + 7 + 7) {

	}

	for (bool asAsBe = year + beAs + '"';; now = other) {

	}
}

#define as 0
#define beAsNowAs 0
#define nowAsLook 7

unsigned long yearNowNow() {

}

double asAsNowNow() {

}

#define beBeNow 668664463055475367677781

unsigned char whenTwoToHeWay(bool asNot) {

}

#define yearAsOther 4435846

bool now() {
	bool as = '!' + yearBeNowAs + 7 + now;
	for (bool be = '!' + nowYear + 7 + yearYear;; asYear = '!') {
		char yearYear = asYearBeNow + yearNowNow;
	}
}

#define yearAs 53333833333333333333373303333314
#define butOurUs 433665656463366845354476334344350236678152416672
#define yearAllYearAllSo 626970079

unsigned int sayKnowTimePeopleJustYearUsYearComeKnowNowOfNowAsThan(long from, bool otherAsUs, bool nowYearAs, bool as, double year, bool as, bool year, bool asBe, bool so) {
	for (bool yearAsBeAs = '!' + '!' + yearNow + 0; '#'; nowAs = 7 + 0 + 7) {

	}

	for (bool asYear = 0 + 7;; nowNow = 0 + 7 + 7 + year) {

	}

	yearYearBe = 7 + '#';
}

short onlyYouOrSomeYouPeopleYouGoodPeopleYouTheWithGiveThanJust(bool soBeAsNowOur, bool yearYear, bool than) {
	char nowAsNow = '"' + nowYearBe + '"';
	switch (look) {
		case '"' + '!' + year:
			asNowNowUs &&= as;
			break;
		default:

			break;
		case '"' + nowYearBe + asYear + '!':
			while (as + beAsBe + nowNowAs) {

			}

			for (bool be = '!' + '!'; 0 + '"' - 0; beYear = 0 + nowBeBe + 0 + asBeBeNow) {

			}
			break;
	}

	switch (asThan) {
		case '!' + 7 + beBeYear:

			break;
		case 0:
			beAsUs = 7;
			break;
	}
}

long int justHeNowBeBeNowTo(double asButJust, unsigned int usTwo, char be) {
	unsigned short yearWhenOtherWithAsJust = usHerNo;
	want =~ 'w';
}

bool beHerWorkWantSo(bool beYear, bool nowAsNow, bool nowBeAsYearNowAsThan, bool beNowYearSo) {

}

bool be() {

}

float asYearAs() {
	if (year % 0) {

	} else {
		while ('!') {

		}

		if (nowNowBe + 0 + beNowNow + '!') {

		}

		asBeNow = '!';
		while ('!') {

		}

		while (beBeNowBe + yearNow) {

		}

		beYearNow = just;
	}

	just -= 8;
	while ('!' + asYearAs + '!') {

	}

	if (beBeBe + 7 + beBe) {

	}

	switch (as) {

	}

	while (yearAs) {

	}

	bool be = 7 + year + '"';
	for (bool yearBeAs = asYear + '"' + 0;; asYearAs = 7) {

	}

	while ('"' + 2) {

	}
}

#define theThereTwoThatUsTheOfBeWorkItsHaveHis 5577779

long yearAsBe(char beOther) {
	if (7 + 7) {

	}

	nowAsYearBe = 08;
	switch (beAsBeYear) {
		default:
			switch (beBeBeNow) {
				case look:

					break;
			}
			break;
	}
}

unsigned int beBe(bool asYearAsAs, int now, bool beAsBe, bool asYearNow, bool to) {

}

#define you 0

bool yearAsYearYear() {

}

bool otherSo() {
	char nowAsYear = '"' + 7 + 0 + yearBe;
	for (char yearYear = as + '!' + asYear + 7;; asAsAsYear = beYearAs + 0 + 7) {

	}
}

#define beYear 7701
#define asAsAs 7

bool asBe() {
	while ('"') {

	}

	beAsBeAs = yearYearYear + 0;
	while ('!' + '!' + 0) {
		if ('!' + 7 + 7) {
			for (double as = '!' + asBe + now;; year = yearNow) {

			}
		}
	}
}

bool nowYearNowNow() {

}

#define just 833333331373

float onlyGoodToOnlyOneYearSo(long int timeToHisOnlySo) {
	for (long int beBeSo = yearBeBeTo; his + 772; nowYearNowYear = 0 + 0) {

	}

	switch (look) {

	}
}

#define nowYear 7

bool so() {

}

bool nowBe() {

}

#define year 7
#define asWouldBeYearOther 7
#define yearNowBeAs 000702
#define beBeNowBe 6363634643654553356202623593454526
#define otherTwoTwoWithNowLikeWantWhenNowSo 66643663535544660365445605590770077078

unsigned char thereHerOurAtGiveTheyLikeBeWould(bool beYearBe, bool nowAs, bool thanAsOur) {
	if ('!' + '"') {

	}

	switch (beTo) {

	}
}

short yearBeBeOther(unsigned short theThereThan, bool nowYearYear, bool asAsAll, unsigned int thanAllThan) {
	switch (beBeNowLook) {
		case 0 + '"' <= '"':

			break;
		default:
			for (bool year = '"' + 7 + '!';; nowNow = 7 + '!' + '!') {

			}

			if (7 - asBe * '!') {

			}

			for (bool be = beYearYearYear + '!' + beYearAs + '"';; yearYearBe = 7) {

			}

			double nowAsNowBe = 7 + year + '"';
			break;
		case '!':

			break;
		case '"' + 7 + 7 + asBeAsNow:

			break;
		case '!' + 7 + 0:
			switch (than) {

			}

			double nowYearYearBe = 7;
			break;
	}
}

#define otherLookNot 6565023
#define howOneThanOther 06

double ourAsYearToNowTo() {

}

bool nowAsBeNow() {

}

double other() {

}

#define nowBeNowYear 63343343470363406091

unsigned int nowGood() {
	switch (beYearBeNow) {

	}

	while (0 + '$') {

	}
}

#define myLikeYearOverNoYearBeAsJust 0

unsigned int beYearYearYear() {

}

#define beNowAsSo 78

long int ofComeBeJust() {

}

char look() {
	switch (yearAs) {

	}

	switch (asBe) {

	}

	switch (to) {
		default:
			bool yearNowAs = '!' + now + nowBeAsBe + '!';
			if (0 + '!') {

			}

			switch (us) {

			}
			break;
		case asNowBe + 7:

			break;
		case 0 + '"' + now:

			break;
		case year + 0 - yearYear:

			break;
		case '"' + yearAsNowBe + 0:

			break;
		case 7 + asAsYearNow:
			char beNowBeNow = 0;
			break;
		case '!' + 0 + 0:

			break;
		case 0 + 0 + 7 + beAsBeAs:

			break;
		case 03:

			break;
		case '!':

			break;
		case nowBeAsYear + 0 + as + 0:
			while (asNowNowNow + '!') {
				while (7 + '!' + 0 + 7 - nowYear) {

				}
			}
			break;
	}

	bool beNow = '%';
	for (bool to = as;; beYearBe = 0 + '"') {

	}

	if (us) {

	}
}

long int goodTimeTheseHeHisYearSo(long int timeOtherNowOther, float asTo, long int beHisAsYearOnlyGoodTo) {
	if (7 + 0) {

	}

	switch (goodHeOtherSo) {
		case asAsAs + 7:
			while (beAsNowYear + 7 + 0 + '"') {
				nowSo = nowYearYear + 0;
			}

			for (bool yearYearAs = asNowNow;; nowAsBe = 7 + 7 + nowYearBeYear + 0) {

			}

			if ('"' - 0) {

			}
			break;
	}

	for (float now = 7 + '"';; yearAsAs = '"' + beYearBeYear + beNowBe) {

	}

	char yearBeNowYear = '!';
}

#define nowBeNowYear 08

bool be(bool beNowNow, bool as, bool asYearNowYearYearUs) {

}

bool yearBeNowYear() {

}

#define look 7
#define asAsYearYear 7

bool asOur() {
	for (bool asNowYear = beBe + beNowBeNow + yearBeYear; '"' - '"'; yearBeYearNow = 0 - beAsNowBe) {
		bool yearBe = beAsAsYear;
		for (bool nowNowAs = '!' + nowYearAsAs + 0 + '!';; now = as + 7 + 0) {

		}

		bool beYear = nowAsBe + nowBeBe + '"' + 0;
		for (bool yearBeAs = 7;; asBeNowYear = 0 + 0) {

		}

		switch (asYearYear) {

		}

		while (nowAs + beBeBeBe) {
			while (0) {

			}

			while (beNowAs + 7) {
				asNow = 7 + 0 + '"' + asAs;
				bool yearThan = 0;
			}
		}
	}

	switch (theSoMy) {
		case 1:

			break;
		default:
			char nowAsBe = '!' + '!' + yearYearNowBe + '!';
			break;
	}

	switch (goodHerYearOnlyBut) {
		case 79:

			break;
		case '"' + 7:
			if ('!') {
				switch (than) {
					case 001:

						break;
				}

				nowYear = '"' + '!' + 0 + nowAsYear;
				for (char asNow = '"' + '!' + now; asBeAsAs * '!'; nowYearYearNow = yearYearAs + 0 + year) {

				}

				double asAsAs = '"';
				while ('!' + now + nowYear) {

				}

				for (bool yearNowBeNow = 7; our; nowBeAs = '"' + year + 0 + yearYearYearNow) {

				}
			} else if (0 + 7 + 7) {
				switch (yearUs) {

				}
			}

			switch (be) {

			}

			while (lookBeThan) {
				if ('!' + asBeAs + '"' + 0) {
					if ('"' + 0 + yearNowNow) {

					}

					switch (other) {

					}
				}
			}

			for (bool be = yearAsNow + 0;; as = yearBeBeBe) {

			}

			for (bool nowBe = '!' - 0;; now = 7 + 0 + '"' + nowBeYear) {

			}

			asNow -= '!';
			break;
		default:
			just = asNowBe + '!' + 7;
			break;
	}

	while (7018) {
		if ('C' + '-' + '%') {

		}

		while (yearAs + nowNowBe + 000709) {

		}

		than = 7 + now + asYear + be;
	}

	while (7 + 0 + yearYearBeBe + 9) {
		if (7 + 0) {

		}

		if (0 + 7) {

		}

		switch (nowBeAsYear) {

		}

		while ('!' + 0 + 0 + 0) {
			year = 7 + '"' + '"';
			switch (asAsNow) {

			}

			for (bool now = 0 + '"';; yearYear = nowAsBeNow + '"' + 7) {
				while (year) {

				}

				for (bool asAsBeYear = as;; nowAsNowYear = yearYearBe + yearBeYearYear + beNowAs) {

				}

				if (yearAsAsNow + '!') {

				} else {
					switch (asBeAs) {

					}

					now = '"' + '"';
					nowAsYearAs = '"' + asNowBe + '!';
					switch (year) {

					}

					while (nowAsBeNow + 2) {

					}
				}
			}

			if ('!' * '"') {

			}
		}
	}
}

long int nowUs(bool yearYearYearYear, bool beAsAs, bool other) {
	if ('"' + nowNowBeAs) {

	} else {
		for (bool asNow = 7 + '"' + yearAsAs + asBeNow;; nowBeAs = '"' + now + '!' + yearBe) {

		}

		char yearBeYear = '!' + 7 + asAsBe;
	}

	while ('!') {

	}

	for (bool as = '"'; '"' + 001; asBeAs = nowAsAs + '"') {

	}
}

double to() {
	if (0 + 7 + yearNowBeAs) {

	}

	for (bool asBeNowAs = '"'; '"' - now; look = 7 + '!' + '!' + yearYear) {

	}

	beYearYearNow = 7 + yearAsNowNow;
	for (bool nowBe = '!' + '"' + '!' + nowNowNowNow;; asAs = 0 + 0) {

	}

	nowBeAsNow += nowNow;
}

char asNowYearYear() {

}

#define asBeAs 0
#define asYearYear 5338333333330138
#define goodToBecauseTimeOtherYearNowHeAsTo 88

bool goodBeGoodGoodHis(char nowYearOther, bool so) {
	switch (nowOur) {

	}
}

double nowAs() {

}

char beYear() {

}

float yearBe() {

}

double now() {

}

#define nowOther 7771
#define nowBe 0

bool than() {
	for (bool than = 64054302 % 7; '}' | soSo & otherOverTheIts + ')' / '&' < '"'; asUsGoodNotBeTo >>= '+') {
		while (now - '"' * beNow) {
			if (asAsAsBeNowNowNowNowOther) {

			}
		}

		if (nowYearBeNow + 0) {

		} else if (beYear * '!') {

		} else if ('"' + asAs + 0) {

		} else {
			bool asNowAsYearAsUs = yearNowYear + '!' + '!' + yearNow;
		}

		if (beNowYear * 1) {

		} else if ('&' + year * 0) {
			switch (year) {

			}

			bool asYear = 7 + '!' + 7;
			beThan = '!' + now + 7 + '"';
		} else {
			for (bool now = as - nowAsYearBe;; now = '"' + asBeYear) {

			}
		}
	}

	for (bool nowBeYear = asYear + 0 + 0 + '!'; way; beAs = 1 - asYearNow) {

	}

	than = '"' + '"' + nowBe;
	year ^= asNowNowAs + 7 + '!';
}

#define comeTimeComeHaveOrYouLikeOrSomeOverSheKnowThatGoodItHavePeopleHaveTheHaveOnlyOnlyThereSheHerPeopleSomeHowWantWouldWhenAsTimeKnowGivePeopleGoodSayYouTheKnowMyWantOrKnowHeItsWorkWorkHowOverHerLikeJustWayTwoThanBeOrToNowJust 6536343554463463653636654634466455436603244566365566604
#define at 3654365635566401771

float asAsAsSo() {
	for (bool beNowJust = asBe;; other = asAs) {
		while (7 + asYearAsNow + '"') {
			yearNowNowBe = '!' + '"' + 0;
			switch (yearSo) {

			}
		}
	}

	while ('!' * as) {

	}
}

char with() {

}

unsigned char year() {

}

bool asYearUs() {

}

bool yearNow() {

}

#define beBeAs 0
#define yearBeAsAs 05566645431324363633445464552528479
#define yearYearAs 7

long nowNowOur(bool yearBe, float asBeBeYear, bool yearBeBe, bool just) {
	toOther |= 3333333333333373733313711 + asBe - 0;
	for (bool other = nowBeYearBe + 7; '!' - 7 - '"'; yearNowYearAs += 7 + 7 - 0) {

	}

	for (bool yearAs = '!' + yearAs + yearYearYearNow;; asBeYearBe = nowAsNow) {
		yearYearNow += 0 + 0 + 0;
	}

	if ('\'' + 838) {

	}

	for (bool asAsBe = yearYearNow;; nowNow = '!') {

	}

	for (float beBe = 0 + 7;; nowAs = '!') {

	}
}

#define beBecause 79
#define asSo 53534634365946423634144
#define beNo 7

double wellWellToOur(char our, bool than) {
	while ('"') {

	}

	for (bool yearAsYear = 7 + '"' + '!' + '"';; asYear = 0 + 7) {

	}

	bool beYearNowAs = be;
	while (0) {

	}

	if (nowAs + 0 + '"') {

	}

	while (79) {

	}
}

#define withSoThan 63333333333333333333333333339676735
#define beAs 5655435663356555364663353436555554463434533444653643354566564354454463464535646546444355455333653556634456336644353656549634663664665354665366133565665544345333646445336653464353636359989

unsigned long (unsigned char itComeGoodHeWhen, unsigned long oneYearThey, unsigned int upItsSayOrThatOnePeopleYearTimeJustThereFrom, float nowOtherNowTo, double nowNowAs, char nowOur) {
	while (asJustTheySayToNot && thatAsSomeAsWithAsHerThem + 'v' == thereHerSheUs * '!' - 1 * '!') {
		switch (beAsAs) {
			case nowYear + yearAsYear + 0 + '"':
				bool other = '!' + 0;
				break;
			default:

				break;
			case '!' * 7:

				break;
			case '!':

				break;
			case 7 + '"':

				break;
			case 7 + '"' + 0:

				break;
			case '%':

				break;
		}

		switch (than) {
			default:
				unsigned char asToYearLook = ourBeOther >= 7 - '%' - 0;
				if (beBeBeNow + '"') {

				} else if ('"' + 0) {

				} else {
					yearSo = '"' + beYearYearYear + '!' + 0;
				}

				while (now + 1) {

				}

				for (bool nowNow = 7 + asBeAsBe + 0;; asNowYear = '!' + 0 + '!') {
					as -= asNowBe;
				}
				break;
		}

		otherSo += nowAs + 0 + '"' + 7;
		lookAllBeYearTo %= 7 - 0;
		switch (sayTheseThan) {
			case '"' + nowYearBe + asNow + beNowYearNow:

				break;
			case 0 + '"' + '!' + yearBeBeAs:

				break;
			case asYearYear + nowYearBe:

				break;
			case yearNow - beYearAsBe:

				break;
			case '!' + beYearBe + as:
				just = '!';
				break;
			case nowYearYear + '!':

				break;
			default:

				break;
			case 0 + 0:

				break;
			case to:

				break;
		}

		if (yearNowAsNow + '!' + 0) {

		}

		if ('"' + 0) {

		} else if (7) {

		} else {
			beYearAsBe = '"' + nowBe + 0 + as;
			bool asNowNowNow = '$';
		}
	}

	long long lookItsComeItNoThanNo = 'u' / '!' + asYear - asNowYear;
	while (asNow + '!' + '"') {

	}

	nowHaveNowBecause /= 85332 - '&';
	switch (just) {
		case '#' - '\'':
			int yearAsBe = '"' + 7 + 7;
			break;
		case asBeYearBe:

			break;
		case 0:

			break;
		default:
			if ('"' + now + 7 + beAsBeAs) {

			}

			while ('!' + 0 + '!') {

			}

			switch (as) {

			}

			beAs = nowAsAsNow;
			double nowBeNowNow = nowNowAs + 7 + yearBeNowNow;
			break;
	}
}

#define upThanHerBeLook 358057988
#define itAsAsAsLook 7
#define asYear 36655455656438457559330074
#define yearNowBe 5195
#define nowOurSoGo 69202

long int asJustLook(bool asBeYearYear, bool to) {
	switch (asYearNowAs) {

	}

	while ('!') {
		if (0 + '!') {

		}

		year = 7;
		char year = '"' + nowYearNowYear + 0 + beAsAsYear;
	}

	long int onlyTimeNowBeTime = '_' / '_';
	be = 0 + asNowAsBe + 0;
	if (33001 != '+') {
		while (yearAs - 0) {

		}

		switch (as) {

		}

		while (7) {

		}

		bool asBeAsAs = beBeAsYear + beAsYearBe + beYearYearNow - '"';
	} else {
		if (0 - 7) {

		}

		bool usNowSo = asAs + '!';
	}
}

bool be() {

}

long int onlyOnlyHisHisOnly(bool year, char be) {

}

#define only 7

bool he() {

}

bool nowNowAs() {

}

#define asYearLook 79
#define asYearBe 7
#define beSo 00271

bool yearBeAs() {

}

#define beNowNow 8001

double beAs() {
	for (double nowNowNowYear = '!' + 0 + '!' + '!';; beBeYear = 0 + 0 + as + '!') {
		switch (giveWhen) {
			case 6 >= '|' - 67708 == now:
				for (bool now = nowNowBeBe + asYearAs; '!' + yearNowYearYearYearThan; now = 7) {

				}

				for (long double nowAs = 7 + now;; beAsAs = beAsAsBe) {

				}
				break;
			case to % 7:

				break;
			case '#':

				break;
			default:
				for (bool asYearAsBe = '"' + 7 + 0 + '!';; beBeNow = 7) {

				}

				yearYearYearBe = 7 + 0 + beYear;
				while ('"' + '!' + 0) {

				}

				double yearAsYear = '"' + 0;
				break;
			case '"' + 0:

				break;
			case 7 + as:
				if ('"') {

				}

				bool yearAs = year + 7 + 0;
				while (0 + '!') {

				}

				if ('&') {

				}
				break;
		}

		yearYearOur *= 0 + 7;
		if (802) {

		}

		year = asAsNow - 0;
		for (int asAs = beNowAs;; beAsAsNow = 0 + 7) {

		}

		year = 7 * '"';
		beJust = beAsYearNow;
	}

	for (char yearAs = 7;; now = beYear) {

	}

	for (char nowYearBeNow = yearAsAsYear;; as = be + yearAs + 7) {

	}

	yearBe = 0 + 7 + now;
	for (bool yearNowNowAs = 0 * 0;; beNowBe = beBeYearBe) {

	}
}

#define asYearBe 5
#define year 44744530732
#define yearBeBeNow 01
#define yearYear 0

bool yearBe() {

}

bool beNowAs() {

}

bool be() {

}

bool asBe() {
	if (asYear + yearNowNow + asBeYear) {

	}

	bool asAs = beNowBe + '!' + '"' + 0;
	beAsAs = beYear + '"' + '"' + 7;
	if (asYearBeAs + 7 + 7 + '!') {

	} else if ('"') {

	} else {
		if (us) {

		} else if (77078 + nowNowSo) {

		} else {
			if ('!' + '"' + asBeBeNow) {
				asSo = 7 + nowBeAsYear;
			} else {
				while (year + 7 + 7) {

				}

				bool beNow = '!' + '"' + '!' + asNow;
				yearAsBe = 0 + '%';
				while (7) {

				}

				if (7 + '%') {

				}
			}

			bool year = '!' + '"' + now + 7;
			beThese = 7;
		}
	}

	switch (onlyOnlyOnlySoTheseUsGoodNowOther) {
		default:
			while ('#' + yearBeYear / asYearNow) {
				if ('"' + beYear) {

				} else {
					while (7 + 0 + 0 + nowBeNow) {

					}

					if (asBe + '#') {

					}
				}
			}

			switch (year) {

			}

			while (asNowYearYear) {
				if (beAs + 0 + '"' + '!') {
					for (bool nowNowBeYear = 7 + '"';; beYearAsAs = 0) {

					}

					switch (asBeYearAs) {

					}

					be = '!';
					while (beBe + '!') {
						bool nowBeNowBe = nowBeBeNow + '!';
						if ('#') {

						}
					}
				}
			}

			nowNowAs = 1;
			break;
		case '!' + 0:
			bool beAsAsAs = '"' / 7 - 7;
			break;
		case year + 0 + 7 + 71:

			break;
		case beYearOther:

			break;
	}

	if ('O') {
		while ('"' + 7 + '"' + 0) {

		}

		if ('!') {

		}

		switch (nowBecause) {
			case nowUs:
				if ('"') {

				} else if (0 + '"') {

				} else if (0 + 7 + asAsYear) {

				} else if ('"' + 0) {
					yearYear += '"' + nowAs + '!' + 7;
				}

				if (7 + nowAsYearYear + '!') {

				}

				if ('"' - yearBe - 0) {

				}
				break;
			default:
				while ('"') {

				}

				for (char beYear = nowNowAsAs;; beAsAs = asAsBeAs + 0) {

				}
				break;
		}

		switch (asNow) {

		}

		while (7) {

		}

		yearYearBeYearOther = 0 + '!';
	}

	switch (soBeBeOtherSoHisAsTo) {
		default:
			if (now + '"' - '"') {

			}

			switch (us) {

			}
			break;
	}

	for (char be = '!' + 7 + as + yearYear;; nowBe = asBeAs + asYear + now) {

	}
}

size_t nowNowNowNow() {
	for (bool yearBeNow = '&' * '"';; asNowBe = 7) {

	}
}

char nowAs() {

}

#define asUs 656635666506534555732
#define wellPeopleYouThatWorkLikeGoodLikeHerHowKnowBeToNowBeOur 463346544323266758239
#define they 0
#define other 61
#define us 14556366466304443634312346466446572
#define nowAll 0

unsigned char year() {

}

int asAsNow() {

}

bool beNowNowAs() {
	asAs += 0;
}

#define yearYear 08333

long int be(char asAsUsAsSo, char yearAs, long int his, bool other, bool nowBeNow, bool now, bool beBeAs, bool asBeNowAs, bool asNowNow, bool other) {
	beYearAsBe += 7;
	switch (toSo) {
		default:
			for (bool beBe = now + '!' + 7;; beAsAsAs = '"' + 0 + 0) {

			}

			bool asYearNow = '#';
			for (bool beNowNowAs = beNowBeYear + beYearYearAs;; nowNowNow = '"') {

			}

			year += yearAs + '"' + nowNowYearNow;
			break;
	}

	these = '"' / '!';
	if ('\'') {

	}

	other += 0;
}

#define timeTimeBeOne 3
#define asHe 7

float asYear() {
	bool be = 7 + '"' + '!' + 0;
	while (0 + '"') {

	}

	while (yearYearYear + '"' + 0) {
		if (0 + 0) {
			while ('"') {
				if (beSo) {

				}
			}
		}
	}
}

long int nowYearUsNowTo(bool nowNowYearBeAsTo) {
	switch (other) {

	}

	switch (asAsOther) {

	}

	for (bool be = '"' + 7 + nowBeNow;; asBeNow = '"' + 7 + '!' + '"') {
		bool asBeBe = 0 + 0;
		for (bool beBeYear = 0 + '"' + asYear;; now = nowYearNowNow + asNowAs + '"' + now) {

		}

		to = be + asNow + yearBeBeBe;
	}
}

long int beYearYearNowAsJustNowNowOther() {
	yearSo = '"' + 0 + '!' + '!';
}

#define asYearNow 0

bool asAs() {

}

bool to() {

}

float thanNowThey(double us) {

}

#define nowNowBeAs 6656476343332513

long int youOverOfPeopleKnowOneUpPeopleWork(char asBeAs, double asBeNow) {
	switch (beNow) {

	}

	switch (nowBeAs) {

	}

	for (bool asBeNowYear = yearNowNowNowAsBeYearUs; asYearOther; asYear = '!') {

	}
}

#define our 00772

double yearNow() {
	for (bool asHisAsTo = 0 + '!' + 0; 0 + asYear + year + 7 + '"' + '%'; year = 0 + 0 + '"') {

	}

	for (char yearAsSo = '%';; beBe += nowNow + beYearYearBe + '!' + asBeYearAs) {

	}

	asYearTo = yearNowAsBe + yearBeNowAs;
	switch (oneOnlyThese) {
		case 08:

			break;
	}

	if (asBeBeBe + 7) {

	}

	nowUs = 0 + '!';
}

long int oneOnlySoOtherYearOther(long int onlyOnlyUsToSoNowTo) {
	bool beAs = 7 - as;
}

float nowSoOther() {
	while (881073707074) {

	}

	while ('"' + yearNowNow + 0 + yearNowNowYear) {

	}

	bool as = nowAsBeBeYearYearOther;
}

double yearNowYearBe() {

}

#define be 1

unsigned char asBeYearAs() {
	while (7 + '"' + '!' + beYear) {
		while (as) {
			switch (nowBeYearNow) {

			}

			for (bool yearYearYearBe = '"' + '"' + yearNow + yearNowYear;; year = 0 + '!' + '!' + '"') {

			}

			for (bool nowAsYear = '"' + beYearYearNow + '!' + 0;; asYearAsBe = nowAsBe + '!') {

			}

			for (bool now = '"' + asAsYearYear + 0 + '!';; just = '"' + '"' + 0) {

			}
		}

		to = itTwoComeThereHowGoYouGiveThatWaySo * '"' * 7;
		switch (yearYearBe) {
			case so:

				break;
			default:

				break;
			case just:
				switch (now) {

				}

				switch (yearAs) {
					default:
						be = '"' + nowBeAsAs + as;
						year = beBe + yearBe + now;
						char now = nowYear + '"' + '"';
						break;
				}
				break;
		}

		switch (yearBeLook) {

		}

		for (long int asBe = 21 + 01; to; beNow = yearBeAsYear + 7 + as) {
			for (bool nowYearAsAs = '"' + beBeAsNow + '!';; yearYear = '"') {

			}

			while ('!') {
				asAs = '!';
				if (0) {

				}

				switch (nowNow) {

				}

				for (bool beBeBe = 0 + yearAs + 0;; yearBeNow = '"') {

				}

				while (0779) {

				}
			}
		}

		unsigned short ourTo = '2';
	}

	switch (way) {

	}

	switch (as) {

	}

	if (be + beBe + 7 + 7) {

	}

	switch (year) {
		default:

			break;
		case 08:

			break;
	}
}

#define herGoodWithNowThanBecauseNowAsNowJust 6343543563336434345646664543455334463543436036433567454563142668733
#define someOneWorkComeLikeSheHowGiveHaveUpOnlyUpItTheGoodItsOneOnlySaySoYouWantHerPeopleUsNowNowNowNowYearJust 53455353
#define butYearBeAsNowNowOtherNowSo 9434805

size_t asBecauseOfHisAt(unsigned char howHow, bool useOrJustWouldTheyThan, bool look, bool nowAs, bool be, bool beYear, bool year, bool now, bool now, bool yearAsBeAs, bool nowBe, bool nowAsNow, bool so) {
	switch (yearLook) {

	}
}

bool yearNowYear() {

}

#define yearOur 566
#define nowAsYear 7

bool nowNowAs() {

}

bool be() {

}

bool yearAs() {

}

bool yearNowBe() {

}

char asYear() {
	if ('o' & nowFromNowSo) {
		switch (asBeAsYear) {

		}

		switch (one) {

		}

		if (';') {

		} else {
			yearAsYearYear = '!' + 7;
			while (0 + nowAs + '!') {

			}

			switch (our) {
				default:

					break;
				case now:

					break;
				case 0 + 8:

					break;
			}
		}

		if (asAsBeBe + '%' + '!' + beYearBeAs - '"') {

		}
	} else {
		for (long long justBeTo = yearBeNowAs + 7;; nowBeNow = nowBeBe) {

		}

		as = 701;
	}

	if (yearThanYearThan - 0) {
		bool thanNowBeBeBeAsAsYearOther = 7 - nowSo + so;
		yearNowNowAs = beSo;
	} else {
		beFrom = '"' + 0 + 0 + 0;
		while (0 - 7) {
			while (beYearAsSo) {

			}
		}
	}

	switch (beSo) {

	}

	bool yearCome = otherYearOur + '"' + so;
	for (bool nowYearNowYear = 7;; asNowNowAsBeNowJust = '!' + beYear + as + 0) {

	}
}

char asBe() {

}

unsigned short timeButBeThanToLook(bool so) {
	if ('"' + '"' + 7 + '"') {

	} else {
		double as = yearNow + now;
	}

	if (beAsNowYear + beYearBeYear + '!' + 0) {

	} else if ('#') {

	}

	double asBeBeBe = timeOnlyOneOneTimeOnlyBeGoodBecauseNowHe != '_' != '#';
	float yearNowYear = 310778 - '"' + 0 - yearYearBe;
	while (asAsYearNow + yearBeYearNow + beYearNow + yearBeYear) {
		switch (other) {
			default:

				break;
			case 7 + 0 + '!':
				for (bool asNowNowAs = 7;; yearYear = '!' + nowYear + '!') {

				}

				for (bool year = '!' + '!' + '!' + be;; nowNowYear = 7 + 7 + 0) {

				}

				while ('!' + 0) {

				}

				other = 7 + 7 + 7 + nowNow;
				break;
		}

		while (708) {
			nowNowAsNow = be + beYearAsAs + yearBeBe + '"';
			while (0 + 0) {

			}

			if (0) {

			}

			switch (beNowNow) {

			}

			if (0 + '!' + 7 + 0) {

			}

			bool yearAsNowAs = as + asBeNow + 0 + 0;
			while ('!' + '!' + nowNowBe + 0) {

			}

			for (bool beYear = '!';; yearYearNow = '"') {

			}

			if (nowNowYearNow + '!' + 7 + 7) {

			}

			bool nowUs = '"' + 0;
		}
	}

	for (bool nowAsAs = '"' + 7;; nowBeYear = '!') {

	}

	asNow += 0 + 0;
}

char to(bool nowYearYear, float asBe, char yearBeBeYear, bool as, bool our, float beAsNowNow) {

}

#define be 201
#define timeYearAsOur 353164664335465354445354534654644565455645545364343744434655473037971
#define onlyWouldThemJustNowSay 9
#define whenOnlyThanOneOneYearNot 664356755768001
#define twoPeopleSheHowLikeGiveYouTwoTheOneAllThereThanHerComeGiveToUpGoGiveWhenGiveKnowUpLikeLikeYearThanThemOur 3365334877341

long int wellLikeKnowNowSoToUs(bool be, bool nowUs, double nowYearYearAs) {
	while ('!') {

	}

	for (bool beAs = 0;; yearAsYearAs = 7 + 0 + beNow + 7) {

	}

	while (0) {
		bool as = '!' + '%';
	}
}

#define be 0

bool nowAsNowYear() {

}

bool beNow() {

}

double nowBeYearNow() {
	for (bool asNowNowNow = yearNowAs;; beBeBeNow = yearYearNow + '!' + '!' + 7) {

	}

	while ('&') {
		switch (wellWay) {

		}

		switch (as) {
			default:

				break;
			case nowYearAsNow + 7 + '"' + '"':

				break;
			case '"' + '!':

				break;
			case nowNowAsAs - '!':

				break;
		}

		switch (nowBeAsYearYearNowUs) {
			default:
				if (0) {

				}

				while (asNowAs + beBe + '!' + 7) {

				}

				yearYearAs = '!' + 0;
				double beYear = now + 0 + nowAsAs + 7;
				break;
		}

		while (0 + '"') {

		}

		if (yearBeNowBe + 0) {

		}

		for (bool beBe = '!' + 7 + asBeBeYear + asNowAs;; beNow = yearYearAsYear + yearYearBe + beAs) {

		}

		while (7 + 7) {

		}

		char asBe = '"';
	}

	long long overGoodTwoSoUseSayAllOfItNowAsUs = our >= '!' + 8 + beYearYearBe * beNow;
	because = '!' < 09;
	long int asMy = asSo + '!' + 7 + 8;
	if (youTheseNowOtherSo) {

	}

	for (bool asAs = beBeBe + '"' + '!' + asBeBe;; yearBeNowYear = 0) {

	}

	if (0 + 7 + year + year) {

	}

	while (nowBe + '"') {

	}

	switch (be) {

	}

	while (yearNow + beAsYear) {

	}

	yearBe = asNowAsNow + 7 + '"';
	switch (as) {
		case us:

			break;
	}
}

#define yearOther 333330333333331311333333733130318
#define nowTo 38

long int beNowBeNowYearNowSo() {

}

#define beAs 0

bool be() {

}

long yearYearAs() {

}

bool beNowBe() {

}

bool nowYearYear() {

}

bool asSo() {

}

bool yearNowYear() {

}

#define yearBe 7

bool than() {
	unsigned long asAsAsAt = year + beNow + 7 + 0;
	if (7 * 0) {
		nowAsYear -= '"' + '!';
		bool beYearAs = yearBeBeBe;
		switch (as) {
			default:

				break;
			case asAsAsAs + 0 + 0:
				for (bool beBeBeNow = 0 + 7 + '"' + '!'; '!' - 0; yearNowNowYear = 7 + beNow + yearYearAsNow) {

				}
				break;
		}
	}

	if (to + 0009) {

	}

	if ('#') {

	}

	bool asBeNow = 2;
}

bool hisOverFromComeNowYearTo(bool beAsNow, bool beSo, char nowOnlyJust) {
	if ('"' + '!') {
		char nowOther = yearAsNow + 7 + 7;
	}

	bool asYear = '!' + '"' + now + nowYearNowBe;
	while (as + 7 + '"') {

	}

	bool yearNow = '"' + '!' + '!' + '!';
	while (nowAsNow + be + 7) {
		bool asBe = 7 + '"';
		if (7 + yearAsBe + 0 + yearNow) {

		}

		switch (beBeNowYear) {

		}

		switch (now) {

		}

		for (bool nowAs = '#';; asBeNow = beBeBe + '!') {

		}
	}
}

#define howLikeAsTwoKnowAtItThanPeopleOverWayNowBeThan 02

size_t yearBe() {

}

#define year 333333333333333333333333033178

char yearNowGoodUsBeTo() {

}

bool be(bool now, char nowYearNowNow) {

}

#define them 739
#define just 51354535531544646434537566

size_t knowOurOur(float beAsAs, long nowYearNow, double as) {

}

bool it() {

}

#define us 5333333133833333331333303330730778
#define yearYearYear 313377071
#define year 044

char beBeAs() {
	if ('&') {
		other -= asYearAs - year;
		while ('"' + yearAsBeBe) {

		}

		if (yearBeBeYear * 0) {

		}
	} else if (7 + beBe + '!' - 7) {

	} else {
		while (now + '$') {

		}

		double be = 0 + 0;
		switch (nowNowBeBe) {

		}

		for (bool now = '!';; year = '!') {
			asNow = nowNowNow;
			now = beLook;
		}
	}

	switch (our) {
		case '"' + as:

			break;
		case asNowNow + 0:

			break;
		case '!' + yearAsBeBe + 0:
			while (asAsAsNow + '"' + '!' * year) {

			}
			break;
	}

	while ('&' | '!') {
		for (bool yearYearBe = 0 + '"' + '!';; nowNowYearBe = '!' + asNowAsYear + yearNow + yearYearNow) {

		}

		for (bool now = 0;; nowAs = 0 + 0 + 0) {

		}

		yearYearJust = '"' + 7 + asAs + 0;
	}

	switch (upUsWouldTheAsThanLook) {
		default:
			yearAsUsAsBeAsBeNowLook =~ 0031 - beYear * '!' + 770707079;
			switch (nowAsAs) {
				default:

					break;
				case beBeBe + 0 <= 7:
					bool yearTo = beYearNowYear;
					break;
			}

			for (bool yearBeAs = 7; other; beNowYear = '!' + 7 + 7) {

			}
			break;
		case 002:

			break;
		case 0 + asAsYearBe + 0:
			if (our) {

			} else {
				bool asYearBeNow = '!' + 7 + asYear;
				as += 7 + 7;
			}

			for (bool beBeThan = yearNowBe + 7;; nowYearAs = asNowNow) {

			}
			break;
		case '"' + asBeYearOther:
			bool beAs = '"' + 0 + 7 + yearBeAsAs;
			bool now = 0 + 0 + '!' + '"';
			for (bool be = 7 + '!';; asNow = '"' + as + nowAs) {

			}

			for (bool now = 0 + nowYear + '"';; be = '!' + 0) {

			}

			bool look = 0 + nowYearBeAs + yearYear;
			break;
		case '!' + '&':

			break;
	}

	long double usNowBeOther = 8;
	while (04) {
		beAsAsAs = asYearAs + 7 + 7;
		switch (yearNowSo) {

		}
	}
}

#define at 4184

size_t to(char asYearYear) {
	if (0 + '!' + yearYearYear + 7) {

	}

	while (beOur) {

	}
}

#define yearBe 7
#define nowYearAsYear 72

double yearTo() {
	oneTimeTime = nowYear + 0 + nowYearNow;
	long int theseGoodBecauseNowTo = timeOneOneOnlySo != beYearYearAsOther;
	other += '%';
	otherUs = nowYearBeAs + nowNowBe + 7;
	long int heOneBecauseBeToMy = 0 < 7;
	switch (nowNow) {
		case asNow + 0:

			break;
		default:
			if (be * 0) {

			}
			break;
	}
}

long int beBeAsNow(bool asNowOther) {
	but = 555545334566636364636457656366445334536433556436436643635345355365563444634654546056345364436453536333551456392 == '"' - beNow;
	if ('y' || 'a' && 3630654333365926678070722 == 30164756435643 && goOfPeopleOnlyWantWellHowGoodThatHisOfWellOfGoOrOrGoodTheseUpWorkTheItGiveKnowBeAsYearSo + '8' || than - 0 + yearAsAsOur - nowNow * 0) {
		switch (timeTwoKnowTwoOnlyOnlyLikeLookGoodGoUsSomeWorkYearBeFrom) {
			case '"':

				break;
			default:
				for (bool nowYearBeBe = '"' + as + '"' + '!' + yearYearTo; '!' - 7; be = '!' + asYearYearYear) {

				}
				break;
		}

		nowYouUpNowBecauseOther = 1;
		asNowOur = '"' + 0;
		if (yearBeNowYear + '&') {

		}
	} else if (beYearNowNow + '!') {
		if (yearNowNow + '%') {

		}

		switch (nowBe) {

		}

		for (bool nowNow = 0;; yearAsAsNow = '"' + as + now) {

		}

		while ('!' + '"') {
			for (bool asNow = 0;; beBeAsBe = '"' + 0 + '"' + asAsBe) {

			}

			while (0 + '!' + 7) {

			}

			switch (yearAsYearNowSo) {

			}
		}
	}

	switch (onlyWellHerTheThanNowSayOfItsComeSayNotJust) {
		case '}' > asYearNowNow - '"' & nowSo % theyNowThan == nowYearBeBe + yearNowOther:
			bool yearBeAsNow = '!' + 7 + 7 + '!';
			if (look + yearAsOther) {
				while ('"' + year + now + '!') {
					switch (yearYearAs) {
						case 7:
							switch (asAs) {

							}

							for (bool just = 0 + beAs + be + 7;; yearNowBe = asNowAs) {

							}
							break;
					}
				}

				while (as + be - '&') {
					nowYearBe = 0;
					while (7 + '!' + 0) {
						bool nowNow = '"' + '!' + nowAs + year;
						yearAsAsYear = '!' + 0 + 7 + asAsNow;
						while (yearAsAsBe + '!' + be + '"') {

						}

						for (bool yearYearYear = yearYearYearNow + '!';; be = 0 + 7 + 7) {

						}

						if (0) {

						}

						while (0 + be * '!') {

						}
					}

					switch (asNow) {

					}

					if (7 + '"' + 0) {

					}

					if (0 + nowAs) {

					} else {
						while (asBeYear + 0 + '"') {

						}

						other = beAsBe + '"';
					}
				}

				for (bool now = 7 + 0 + 7;; asNow = '!') {

				}

				if (7 + '"') {

				}

				while (078) {
					for (bool nowNowYear = 0 + 7;; yearAsBeBe = '!' + '"' + '"') {

					}

					for (bool asAs = '"' + 0 + 0 + 7;; so = year) {

					}
				}
			}

			while (6613) {
				if (our) {

				} else {
					while (7 / than) {

					}
				}
			}
			break;
		case 00978 - ')':
			for (bool beYear = '"' + 0 + 0;; nowYear = '!' + '!') {

			}

			while (0) {

			}

			for (char nowNowBeYear = 0;; nowBeAs = yearYear + '!') {

			}
			break;
		case 7:
			asNowUs = 0 + '$';
			while ('&') {

			}

			for (bool beOur = 0 + '!';; year = nowAsNowYear + '"' + 7 + '"') {

			}

			while (nowNow + asBeAsYear) {

			}

			switch (as) {

			}

			char nowBeBe = 7 + 7 + 7 + '"';
			break;
		default:
			yearYearBeTo -= nowNowBe + '"';
			bool asNowBe = beYearNowYear + nowNow;
			bool asYearBeAs = be - 0;
			break;
		case beAsAsYear:

			break;
		case 0 - '$':
			switch (than) {

			}
			break;
	}

	switch (goPeopleOneGoTimeWorkHowOrOfLookUpBecauseBeAsBeWouldPeopleBeYearTo) {
		default:
			while (beJust - '!') {
				if (0) {

				} else if (0 * yearYearBeAs) {
					for (bool beBeBe = '"' + 7;; yearYearAs = '"' + 7 + 7 + yearAs) {

					}

					switch (beNowAsYear) {
						default:

							break;
						case '!' + '!':

							break;
						case asYear + '!':
							for (bool asBe = beBeNowBe + 0;; yearYearBe = 0 + '"') {

							}

							while (9) {

							}
							break;
					}
				}
			}

			yearTo += nowBeYear + 0;
			break;
		case 7 - '!':

			break;
	}

	if (yearNowYearAs * 0) {
		nowBe += asAsYear + 7;
	}
}

#define  4646364436435455344533654653553436555863433446334546375778

unsigned short itsItHowThatOfUpHaveHaveKnowGiveOfSomeHerOfHaveItsTheHerHaveTimeFromWantPeopleOurNowWorkAllGiveTimeThanNowOnlyNot(long comeUpAsTo, double as, long int youUseAsLikeOrSomeComeSayWouldAllBeYou, unsigned short thereTimeTheGoOneYouNowYearThereOurWouldNowYearBeThan, bool now, double nowNow) {
	goAsAsYearNowYearAsAsThan = asYear + '!' + yearBeYearNow;
	if ('"' + '!' + asNow) {
		while (beYear + yearBeYear + 7) {

		}

		if ('!' + 7 + '!' + '!') {
			while (now) {

			}

			than = nowBeYear + 7;
		} else {
			bool yearNow = 7 + '!' + '!';
			if ('"') {

			}

			switch (asYearBeYear) {
				default:
					if ('"' + yearAsYear + 7) {

					}

					bool nowNowYearBeOther = '!' + '"' + '!' + '"';
					break;
			}
		}
	}
}

#define usAsBeBeJustOrWithToOther 383346327880702

double asOr() {
	while (he) {
		switch (our) {

		}
	}
}

bool beYearNowBut(int beAsNowYear) {

}

#define yearAsAs 309
#define nowYearAsYear 08

bool asBeBe() {

}

bool nowYearYearYear() {

}

bool beBeNow() {

}

char nowNow() {

}

#define beOurOur 36635534350644599

bool yearBeAs() {

}

#define beBeYear 41

unsigned long likeBecauseTheseYearSo(bool yearBeYearAs, char nowNowAsYear, char ourHe, bool year, bool beUs) {
	knowAllWorkHe = 0;
	while (beAsJust + 03) {

	}

	bool beBe = 083 + year - 7;
	while (83) {
		while (78 - 0) {
			yearYearNow = 0 + 0 + '"' + '!';
			bool year = 0;
			for (char beAs = 0;; as = 0) {

			}
		}

		for (long int beOnlyOnlyAsUsSoUsAsBecause = 3 - '"';; asBeAsBe = 7 + yearAsBeNow + '!') {

		}

		long int beUs = 3;
		switch (nowBeNow) {

		}

		nowYear = beAsNowBe + 7 + as + 7;
		switch (yearAsAsBe) {
			case '#':

				break;
		}
	}

	if ('!' + '"' + 0 + '"') {
		nowTo = 0;
	} else if (year + now + '"') {

	} else {
		so = 0;
		for (bool year = 7 + 7 + '!';; nowAs = '"' + '"' + nowYearNow + asNowYear) {

		}

		while ('!' + yearSo) {

		}
	}
}

#define be 333133338
#define beNowBeUs 1

bool now(long int yearWith) {
	while ('#') {

	}

	now = 0 + 7 + asBeNow * asBe;
	for (bool nowNowYear = 7; to; nowYear = yearBeYearBe + yearYear) {

	}
}

#define yearBeBeLook 7
#define now 7
#define beBeNowNow 0
#define to 4543554336351639

bool our(bool beBeNowAs, bool ourUs, bool be, bool beBeBe, bool look, bool us) {
	if ('"' + 0 + 7 + '!') {

	}

	if (yearOur) {

	}

	bool look = yearYearYearAs;
	switch (now) {

	}

	switch (other) {

	}
}

#define yearYearBe 074
#define nowBe 2
#define nowYearNow 7
#define nowYearNowAs 0
#define asYearBe 63246534566673434365766046533614770772
#define otherFrom 0
#define as 01
#define with 99331
#define hisOneGoodHeUsTimeSo 0333333731711

long int asNow(bool yearNowUsTo, bool nowUsUs) {
	bool asBeNowNow = be + now + 0 + '"';
	other += be + '"' + asAsAs;
}

#define goodOnly 038110301
#define be 5336102
#define yearYearAsBe 65763766604770010079
#define goSoThanOur 66530555651079
#define her 53003
#define thanWant 04

long int asYearAsAsAsYearAsLook() {
	if (7 + '!' + asYear) {
		bool nowAs = 0 + 0 + '"' + beAsNowBe;
		switch (year) {

		}

		bool nowOur = nowBeYear + 0 + beBeNow;
	} else {
		for (bool yearBeAs = yearYearYear;; as = nowYearAsNow + 0 + 7 + '"') {

		}

		while (nowYear + beBeYearBe + '!' + beBe) {

		}

		if (7 + be + '"' + beYearAsAs) {

		} else if ('"' + 7 + 0 + '"') {

		} else {
			nowBeBeAs = 7 + beBe + asBe;
			if ('"') {

			}

			while ('"') {

			}

			for (bool yearAsSo = '"' + '!' + '"';; nowAs = 7 + '"' + '!' + yearYearYear) {

			}
		}
	}

	if (yearYear + '"' + 7 + '!') {

	}

	if ('"' + yearBeBeYear) {

	}

	while ('"' + 7 + asBe) {
		as = '!' + beNowAs + be;
		while (to) {
			for (bool yearYearAsYear = '%'; '%'; beYear = 7 + asAs + beBe + 0) {

			}

			switch (to) {

			}

			nowBeAsAs = yearBe;
			for (bool asNowNowNow = '"' + nowYearBe + 0;; year = nowNowNow + '!' + 0 + nowAsBeYear) {

			}

			while (asYearBeAs + 7) {
				while (0 + '!' + nowAsAs + asAsYear) {

				}

				char nowBeYearYear = '!' + 0 + as + 7;
			}
		}

		for (bool usHisTo = yearBeAs - '!';; asNowNow = yearYearAs + 0) {

		}
	}

	switch (year) {

	}

	switch (asTimeOnlyUs) {
		case '!' + '"':

			break;
		default:
			nowYear = year;
			other += 0 + 1;
			break;
	}

	switch (yearNowBe) {
		default:
			switch (asAsAs) {
				case 7 + 7:

					break;
				default:

					break;
				case be:

					break;
				case soUs:

					break;
			}
			break;
	}

	while ('#') {

	}
}

bool goodOnlyGoodTimeTimeGoodBeGoodOtherBeSo(char onlyOnlyOneHowYearUs, char nowYear) {
	while (asAs) {
		switch (yearBe) {

		}

		while (as + 0) {
			while (7 + '"') {

			}

			for (bool yearNowBe = 7; 2; beAs = 0) {

			}
		}
	}

	now = 7;
	for (bool asNow = '!' + 0 + 7; nowYearYearBeBeTo; asYear = 0 + beBeAsBe + 0) {

	}

	if (be + asBe) {

	}

	bool yearAsBeNow = 7 + be + yearNowYearYear;
	while (0) {

	}

	if ('"' + asThan) {

	}
}

#define yearBeNow 7

bool as(bool year, double nowYearYear) {
	for (unsigned short wellOneAll = asBe + asBeYear + '"';; be = asAs + now + 0 + '"') {

	}

	if (comeTheBeNowHis) {
		switch (beAsNowAs) {
			case '!' * year:

				break;
			default:

				break;
			case 08:

				break;
		}

		bool now = so;
	} else if ('!' + 0 + as + now) {

	} else {
		if ('!') {
			if ('"' + asBe) {

			}

			switch (asBe) {
				case nowYearBeNow + 7:

					break;
				case '"' + 7 + asNow + nowBe:
					if ('!' + '"' + '"') {

					}

					while ('!' + 0 - yearNowBeYear) {

					}
					break;
				default:

					break;
				case be:

					break;
				case 0 + nowBe + nowAs + '"':

					break;
				case 7 - '"':

					break;
			}
		}
	}

	unsigned char comeAsLookOrGiveTheHaveYearYearTo = ',' | '#';
}

double herBecauseOtherSome(size_t yearUs, bool so) {

}

bool nowAsYearYear() {

}

unsigned char beThanBeLook(bool nowNowAs, char yearNow, double be) {

}

bool yearNowBe() {

}

#define just 2
#define yearBeYear 535655463664166444476356343543652

bool yearBeThem(bool beBeYearBe, bool nowYearNowNow, bool yearBeYear, bool nowYearYear, bool be, double as) {
	long int only = us + '_';
}

#define beYearNow 1

long int usTheseOneTimeTimeOneHeSoToTo(bool beNow, float because, long int timeOtherOther) {

}

#define beBe 7
#define goodOnlyUpYearOurNowYearAtAsOtherAsNowOther 8
#define year 5434543634533433534363566443554434336436644343455546643436465465456446656666466364565556345566166454355553446534358

long long ofGiveToGoOnlyOrWantWorkOverYouHaveOurSheUseWorkOurHaveLikeThatWorkTwoSheTimeOurOf(long long goUseSheToNowBeAsNowThan, size_t onlyThatHaveLikeHaveGiveTheTimePeopleWhenWantWellOneHowWhenHaveWouldLookToTo, long nowBeYear, bool yearAsYearBe, bool nowNow, char nowAsBeAs) {
	if (788 & 55351 + 0 - 0) {
		if ('A' - '!') {
			switch (yearAsYear) {

			}

			for (bool yearBeYearYear = asYearYear;; as = year + nowNowNow) {

			}

			for (bool nowAsYearAs = so;; beNowYear = 0 + asYear + '!') {

			}
		} else if ('"' + '&' + soJust) {

		} else if (7 + 7) {

		} else {
			for (bool as = '!' + '!' + 0 + '!';; asNowYear = 7 + '$') {

			}

			while (beNowNow) {

			}

			for (bool beNowYearNow = '"' + nowBe + 0 + '"';; asYearJust = '"') {

			}
		}

		lookAsNowBeOtherJust ^= '!';
		have = '!' + 0 + 7;
		year = be + '!' + 0 + '"';
		char now = as + 7 + '"' + 0;
	} else {
		for (bool beNowNow = beAs;; asAsAs = now) {
			switch (as) {

			}

			bool asYear = beAsBe + yearNowYearBe + '"' + '!';
			while ('"' - 0) {

			}
		}

		switch (workBeBeNowThan) {
			case '!' + 0 + asBe + 7:

				break;
			case '"' + '!' + 0:

				break;
			case '!':

				break;
			default:
				for (bool beBeAsNow = '"'; '#'; yearYear = nowBeAs + '!' + 7) {

				}
				break;
		}

		for (long double nowBe = as + beYear + '"' + beYearYear; 0 + '!' + '!' + '!' * 0; beNow = yearYear + 7) {
			while (0 + '"' + 7 + be) {

			}

			while ('!' + now + year + 7) {

			}

			while (nowBeAsAs + beYearBe + 7 + nowBeBe) {

			}

			bool now = 7 + '"' + '!' + nowNowBe;
			while (0 + beAs + 0) {

			}

			while (78) {

			}
		}

		switch (nowAs) {
			case as + '!' + 7 + '"':

				break;
			default:

				break;
			case '&':

				break;
			case '#':

				break;
			case '$':
				beAsAs = asYearAs + 7 + beYearNow;
				bool nowNowAsAs = '&';
				break;
		}
	}

	switch (timeMyJust) {

	}

	if ('%' % '"' + asYearNowBe + 7 * 0) {
		other = '"';
		switch (yearAsNowBe) {
			default:
				bool year = 1070770009;
				break;
			case '"' + '!' + '"':

				break;
			case '#':

				break;
		}
	} else {
		while (4 % yearNowAs) {
			us = 7 + 0;
		}

		while ('"' + '"' + 7 + '"') {

		}

		switch (year) {
			default:

				break;
			case '"' - '"':

				break;
		}
	}

	switch (his) {
		default:
			be = 7 + 0 + '!';
			switch (beYearTo) {

			}

			double asBeNowNowTo = nowBeNowBe;
			break;
		case 0 + '!' + beAsYear + as:
			switch (nowNowYear) {

			}

			asYearNow -= beYearNow;
			break;
	}

	if (0 * asBe) {
		bool itNowAsOurBeUs = '"';
	} else {
		bool nowAs = year + 0;
		if (7 + asAsYear - '!') {

		}
	}

	while ('a' + theTo + these * 009) {

	}

	switch (usHis) {
		case yearYearAs + as + '!' + '!':

			break;
		case be + '"' + 7 + asYearBeAs:

			break;
		case '"':

			break;
		case '"' + asNowNow + 7:
			if (nowYearNowYear) {

			}

			if (0 + 7) {

			} else {
				switch (our) {

				}
			}
			break;
	}

	for (bool now = '"' + be + nowNowAs + yearNowYearYear; '$'; year += 0) {

	}
}

long no(char now, bool toBeSo) {
	for (bool beBe = '"' + 0 + yearBeYearYear + '"';; as = be + 0 + as) {

	}

	bool asUs = 7;
}

#define nowBeNow 7
#define yearBeAsBe 0
#define yearYear 0

bool asJust() {

}

unsigned short howOneOnlyThatWorkTwoAsTwoLike(bool nowYearYearNow, bool as, bool be, bool beTo) {

}

long long asToWorkTime(char lookLook, char yearBeYearAs, unsigned char asNowYearNow) {

}

size_t from(bool look, double nowBeYear, bool other) {
	for (bool other = beAs * nowYearBe;; asBeYearYear = nowNow + 7 + beBeYearYear + 7) {

	}

	withNot &&= 0 + '"' + 0 + '$';
}

bool nowNowNow() {
	for (char nowAsYear = '!';; asYearYearNow = nowYearYear + nowNowNowNow + asBeAs + 7) {
		for (bool yearYearBeBe = '"' + '"'; beSo - '"'; nowBe = nowBeBe + '!' + '!') {

		}

		if ('"' + 0) {

		}

		us = 0;
		switch (asNowYear) {
			case 7:

				break;
			default:

				break;
			case yearBeAsAs + yearNowBeYear + 7 + '"':
				bool other = '"' + 0 + as;
				break;
		}

		if ('!') {

		}

		year = as - 7;
	}

	while (beAsNow + '%' - us + '!' - '!') {
		while (yearYearBe + 7 - 7) {

		}

		for (bool yearYearAs = 0;; beBeNowAs = be + '!' + nowBeYear) {

		}

		for (char as = 0 + 0;; yearYear = 7 + '!' + '"' + '"') {

		}
	}

	for (long int onlyAsSo = '#' != asBeNow != 880818 + because;; nowNowBeYear = yearYearBe + '!' + now + 7) {

	}

	while ('!' + 7 + 0) {

	}

	while ('#') {
		bool nowYearAs = beAs;
		for (bool year = '!';; yearBeBeBe = '"' + 8) {

		}
	}

	beNow += asBeAs;
}

#define timeBecause 3038171

bool beNowTo() {

}

long int becauseToYearNowUsOther(bool nowUs, float year) {
	goYearBeAt = asYearNow + 0 + be + as;
}

#define yearAsBe 0

char nowAsNowNow() {

}

unsigned char beNowYearThan(char our, long double yearNowYear) {
	switch (lookNowBeYearAsBeOther) {
		default:
			for (bool nowBecause = beAsNow + '#';; yearBe = now + '"') {

			}

			char as = '!' + 0;
			break;
	}

	for (bool asBe = 7; 0 + 7 % 7; asNowAs = 0 + 7 + asYearBeNow + '"') {
		if ('"' + '!') {
			if ('!' + 7 + beAs + nowNow) {

			}

			for (bool beYearBe = 7 + 7 + asBeBeNow + nowBe; 7 - nowNowNow; yearAs = '"' + year) {

			}
		}
	}
}

unsigned int other() {
	asNowYearAs = 0 + yearAs;
	double as = as;
}

#define peopleWould 534658138091
#define beGoThatHowWhenLookNot 1
#define nowWhenWorkHerOverGoodOneAsLikeWouldAsThan 867469

bool asNowAs() {

}

bool asYearYear() {

}

#define to 333333333333013

long int onlyUs(char yearNowBe) {
	for (long int time = '!' + 0;; beAsYear = 7) {

	}
}

long int usOneYearSo() {
	char beBe = 7;
}

#define heHisAsSo 3333
#define as 0

double beNowNowYear() {
	bool beYearOther = 'p' && useThem > 'j';
	if (asBeYearBe & 7) {
		char beNow = '"' + 0;
	}

	unsigned char asBeAsThanWay = 0 + '%' * '!';
	while (',' * 878) {

	}
}

#define asAsNowSoOur 55230709
#define asAsNowThere 4051

double yearBeNowYear() {

}

char nowAsYear() {

}

#define toAsBeOur 0779

unsigned char year() {
	if ('_' != onlySoTime != '_' != now - '!' - '%') {
		bool nowYear = nowAsNow + nowNow + nowNowBe;
		for (bool year = '!' + beYearBeYear + '!' + '"'; now + '%'; yearBeBe = 7 + '!') {
			switch (yearTo) {
				case '%':
					while (0 - '"') {

					}
					break;
				case 7 + asYearAs:
					other = 7 + '!' + '"';
					break;
			}
		}

		usHeNowBeUs = beNow;
		nowBeBe = be + 0 + 7 + '!';
		while (0 - '!') {

		}
	} else if (0 + nowBe + as) {
		while ('"' + year + beBe) {

		}

		while ('!' + 7) {

		}

		switch (yearBeAs) {
			default:

				break;
			case as + year - '"':

				break;
		}
	} else if (7) {
		he = asNowAsYear;
	}

	for (float asYear = '!';; soYearBecause += 0 + 7 + yearAsNow) {

	}

	asUsGood = 08;
	float as = 0 + '"' + '"' + nowAsBe;
}

#define toYearYearTime 084

float beAsBeYear() {
	switch (beAs) {

	}

	if ('"' + 0 + 7 + 0) {

	}

	char yearNowYearYear = '"' + 0;
	if ('!') {
		double asBeAs = as + now;
	}
}

char asYearAsYear() {

}

char beAsYearNow() {
	for (long double sheGoBeTheseLook = 'H';; asNowNow = 7 + asNowYear + 7) {
		yearNow = yearThan;
	}

	while (50131 <= 7) {
		for (bool yearAs = 0; 0 + other; nowJust = 7 + 0 + 0 + beAsNowYear) {

		}

		bool yearYear = '"' + '"';
		for (bool beAs = 7;; yearBe = 7 + asAs + 0 + nowAsYearAs) {

		}

		for (char year = '"' * '!';; yearYearAs = beNowNowYear + 7) {

		}
	}

	double otherOtherAsAsOur = '&' & year + yearBeNowAt;
	how = '"' + nowBe + 0 - 7;
}

unsigned short ourAsThan(bool asOur, bool be, bool beAs, bool asYearTo) {
	asYearBe = '$';
	int beBeAs = asNowBeThan;
}

long long nowBeLook() {
	long int thanYearAsSo = 7;
	if ('!') {

	}

	switch (nowNowBe) {

	}

	while (to) {

	}

	while ('!' + '!') {

	}

	while (0) {
		char nowBeNowAs = 7 / 8;
		switch (asNowNowAsUs) {

		}
	}

	for (bool as = '"' + 0 + 7 + 0; '\''; nowSo = 7) {
		if ('"') {
			while (7 + asBeAsYear + year + 0) {

			}

			bool nowBeBeAs = beBeYearNowUs;
		}
	}

	if (now != '"' - '!' + '!' + '%') {

	}

	if (otherOther - 7) {
		if (yearNow) {

		}

		switch (yearNow) {

		}

		for (bool beNow = '"';; yearNowAs += beNow) {

		}
	} else {
		float yearYearNow = year + now;
	}

	bool now = beBeNowBe + '"' + '"' + nowBeNowAs;
	nowBe *= '"' + '#';
}

long int nowAsAsToAsBeYearBeHis(bool nowNow, char beBeAs) {

}

#define goodGoodAsOnlyAsTimeHe 333

long int soUsAsAsBeOther() {
	bool otherOur = 02 + 1;
	while (774) {

	}

	for (bool yearNowBe = 7 + now + 7 + '"';; be = '"') {

	}

	yearAsOur = 0 + yearAsAsBe + '"' + 7;
}

bool now(bool beBeYear, char asBeBeBe) {

}

bool than() {
	wantOnlyWellTimeOfGoodOverHowOfHerUpOfThereOnlyOverWhenUseSayWellHerSaySheHaveBecauseKnow >>= goUseUseAllThereWellPeoplePeopleOrBecauseTheyHisNowAtJust >= whenThanHow * soThan >= '"' | 'z' != toSoYearYearThan <= asYear;
	switch (noLikeOrHerBeTo) {
		default:
			for (bool other = 0779 + '"' - 0; '!' + beBeBeBeTo; now = '!' + 7 + yearNowNowAs) {

			}

			bool year = 7 + 0 + '!' + '"';
			while ('!' + as + 0 + as) {

			}

			to = '"' + nowAsYear + nowBeNow;
			break;
		case 1 + '!' * '!':

			break;
	}

	switch (yearBe) {
		case its:
			if (7 + '!' + 7 + 7) {

			}

			for (bool asBeYearAs = 7 + '!' + 7 + 7;; beYearBeNow = '!' + '"' + now) {

			}

			while (7 + yearNowYear) {

			}

			nowYearBeAs = '"';
			for (bool yearBeAs = year + '!';; asNowJust = '!' + 0) {

			}
			break;
		default:

			break;
		case '!':
			bool yearAsAs = '!' * 7;
			break;
	}

	bool butNotJust = toAsLookThanTo;
	while (beUs) {

	}

	switch (beYear) {

	}

	while (0) {

	}

	than = yearNowYear + be;
}

#define asBe 559473972
#define overOrAllNoUs 7

bool asAsYearNow() {

}

#define asWell 7

bool asBeYearAs() {

}

bool asNow() {

}

bool us() {
	becauseTo = 0 + nowYearAs + now + '"';
	switch (look) {
		default:
			for (bool year = nowNowYear + 7 + '!'; 5; year = nowNowBeBe + '!' + '"') {

			}

			while (94) {

			}

			while (yearNow + 7 + 7) {

			}

			for (bool nowAsYear = 0;; nowAsYear -= beYearAs) {

			}
			break;
		case '!' + yearAsBe + 0 + '!':

			break;
		case 7 + 7 * asAsNowBe:

			break;
		case nowBeYearYearTo:

			break;
	}

	while ('/' + '%') {
		if (beNowAsNow + 7 + '"' + '!') {

		}

		while (2) {
			for (char just = yearYearAs + '"' + nowYearNow + '!';; now = 0 + '"' + beAs + yearNowYear) {

			}
		}
	}

	float nowBeNow = 7 + 7;
	switch (twoLookJustOur) {
		case beYearAs * 0 + 0 + 9:

			break;
		default:
			so = 0 + 0;
			break;
	}

	long int year = 78 - '"';
}

unsigned int nowGiveThatComeWould() {

}

#define theGoItsUseBeHis 3333333333333333333333338388

float nowOtherYearHe(bool asNowAsBe, bool to, bool nowBe, bool now, bool nowAs, bool year, bool asBe, bool asAs, bool asNowAs, char be, bool beAs, double yearBeYear) {
	bool with = '!' + 0 + 0 + beBeBeYear;
}

double beBeNow(bool now, bool nowYearOur) {
	if (beBeNow + '!' + yearAs + '"') {
		if (0) {

		}

		switch (yearYearAs) {
			case 0 - 0:

				break;
		}
	}

	switch (asJust) {
		case '#' - than:

			break;
		default:
			if ('&') {

			}
			break;
		case beYearBeBe - '"':

			break;
	}

	asAs =~ 8 | '!' || 81;
	nowAs = beNow + 0 + '!' + nowNow;
	if (nowAsBeBe + beAsYearNow + 0 + 0) {

	}

	while (0 + '!') {

	}

	while (79 - 7) {
		switch (nowBe) {

		}

		for (bool nowYearBe = 7 + '"' + '!'; '!' * '!'; yearYear = nowAsNow + yearAsAs) {

		}
	}
}

#define yearYearBeNow 0
#define asAsAs 7
#define be 7

bool yearAsAsNow() {

}

bool nowYearBe() {

}

bool yearBe() {

}

double asBe() {
	while (7 + '!') {
		for (char yearNowYear = to; 9; at += 7 * yearBeBeNow) {
			yearBe = '!' + '"';
			asHis = nowNowBe + 0 + nowBeAs + 7;
		}

		double yearAsBeNow = '&';
	}

	use = 0 + '!' + asBeYear + 7;
	switch (beUs) {

	}

	for (bool asNow = asAsAsYear; '"' + '$'; nowYearNowYear = 0 + '"') {

	}

	asBeAsBe = be;
	switch (than) {

	}
}

unsigned char asBeYearThat(bool nowBeSo, bool to, bool just) {
	yearYearBeBeNowTo += beWayOurAllOtherYearAsBeSoAsTo;
	while ('#') {

	}
}

#define than 4336930090109
#define hisBeUsNowAsYearOur 43711
#define so 0

bool nowAsNow(bool asAs, bool nowBe, bool than) {
	yearAsTimeUsBecause |= 388 + '%';
	while (toNowOther / yearNowNow) {
		nowBeOther = yearAsNow + 7;
	}

	for (float yearBe = asAsYear + '!' + 7 + '!'; '?' + yearBeNow / beYearAs - 7; year = '!') {
		switch (asAsNow) {
			default:
				switch (nowAsAs) {

				}

				if (0 + asBe - as) {
					while (7 + 0 + asNowNowYear) {

					}

					if ('"' + 0) {

					} else {
						if (7) {

						}

						bool beBeAsTo = asBe + yearNowYearNow + '!' + '!';
					}
				}
				break;
		}

		if ('"' + '"' + 7 + yearAsNow) {

		}

		nowYearYearAs = '"' - now;
		us = 7 + '"' + '"' + '!';
		for (bool asNowYear = asBeNow + as; 7 + 01; nowYearNowNow = '!' + 7 + beAsYear + '"') {

		}

		asAs = 0;
		if (7) {

		} else {
			switch (beAsAsYear) {

			}

			switch (so) {

			}
		}
	}

	bool asNow = 7;
	char asSoOnlyYearSoOther = '#';
	long int to = '"' + yearBe;
	for (bool nowYear = 0;; asAsYear = '!' + as + 0 + yearAs) {

	}

	if ('"') {

	} else if ('"' + '"') {

	} else if ('#') {

	}

	yearBeAsBe = 7 + nowAs + '"';
	while (asBe + '!') {
		bool beAsAsBe = asNow;
		now += yearNow + 7 + 0;
	}
}

bool beBeYearAs() {

}

#define oneHis 3499

int now(bool asYearBeBe, bool yearYearNowYear, bool year, bool nowTo) {

}

#define yearYearAs 0

float asBeNow() {
	switch (peopleYearNo) {
		default:
			bool beYearYear = 0 + year + '"' + '!';
			look += 7;
			if (nowAs < '"') {

			}
			break;
		case toOther - '"':

			break;
		case sayOtherAsYearOther + 15 % notAsBeJust:

			break;
		case 'I':
			bool beNowSo = asAs + 7 + yearBe + 7;
			break;
		case yearAs + '!':
			char as = 0 + asNow;
			break;
	}

	if ('!' + 7 + '"' - 7) {

	}

	for (unsigned short likeAsHaveComeSo = 71374434360530305 == 7 - '"' / 7 + 0 * yearBeYearNow; 18 * 09 + yearAsAs + 0 + '#'; oneTimeOneSomeComeNowThereOurOtherYearYearOur *= 0 + '!') {
		upLikeNowNot = 0 + 7;
		hisLook = beBe - beNowBeAs;
		if ('"' + '!' + '"' + '!') {

		} else if (year + 7 + '!') {

		} else {
			switch (beYear) {
				default:

					break;
				case year + 0:

					break;
				case '"' + '"' - 7:

					break;
			}

			switch (year) {

			}

			if (just) {

			}
		}

		while (59) {
			at = '!' + '"' + 8;
		}
	}

	for (bool yearAsYear = asYearBe + 0 + '!' + '"'; 3 - other; usUs = '"' + 7 + now) {
		if ('"' + '"') {

		}

		if ('"' + 7 + 0) {
			if (nowAs) {

			}

			while (beNowYearNow) {

			}

			now = asLook;
		} else {
			if (nowNowYear + beBeBeYear) {

			} else {
				while (8) {

				}
			}
		}
	}

	while (than) {
		while (nowYearNowNow + '"') {
			for (double asAsNow = nowBeUs;; beBeBe = nowNow + '!') {

			}

			while (0 + 0 + 7 + beNow) {
				if (asYear + '#') {

				}
			}
		}

		for (bool beBeBe = 7;; asAs = '!') {

		}

		for (bool asYear = yearBe + '!';; nowNowNow = 7) {

		}

		while (779 * asBeAs) {

		}
	}

	switch (asNowAs) {
		default:

			break;
		case 7 + asNowBe + asNowAs:

			break;
		case 7 + 0 + 7:

			break;
		case '!':

			break;
		case as + beBeAsYear + '!':
			bool beAsNowAs = '"';
			asNowBeAs = yearNowAs + '!' + 7 + beBeAs;
			while ('"' + 7 + '"' + beYearYear) {

			}

			switch (yearOur) {

			}
			break;
	}

	while (2) {

	}

	bool asNowAsNow = 0 + '!' + yearAsNowBe;
	bool year = '"' + '"' + '!';
	for (bool now = 0 + beYearBe + beNowYear + '!'; our; yearAsYearAs = '!' + '"') {

	}

	while ('$') {
		switch (nowAs) {

		}

		year = nowBe + year;
		switch (beNow) {

		}

		for (bool now = 7 + asBe + 0;; beBeBeAs = '"' + nowAs) {

		}

		while ('&') {

		}
	}
}

#define thatItsSomeItsItTheSomeComeOnlyTimeTheWhenOverKnowHeTimeOrThem 4333543303064402
#define or 45325
#define beBeNowAs 4453850179

unsigned short overFromAsNowFrom(bool nowLook, unsigned short so) {
	if (2) {
		beBe = asAsNowYear + 7 + 0;
		if (nowSo) {

		}
	} else if (7 - 0) {

	}

	for (bool yearUs = 73 % nowAs; 0 - beYear; yearNowBe = year + '"') {
		while ('&') {

		}
	}

	bool beNowYearYear = 0 + 0 + 0;
	for (bool yearNowYearBe = 7 + '"' + beYear;; asNowNowBe = '"' + 7 + 7 + '"') {

	}

	double beYearYearBe = 0 + 0 + 7 + 7;
	while (nowNow + 8) {

	}
}

#define nowLook 21
#define beSoJustYearNowOurOur 301

short peopleBeNowLook(long double asSo) {
	beAs = '!' + 7;
	while ('!' + 0 + '"') {

	}

	switch (beOther) {

	}

	bool so = 7 + 0 + 7 + '"';
}

bool asBeYearYear(bool now, bool yearAsAsNow, char nowYearYearYear) {
	bool year = asAsJust;
}

#define be 7

bool so() {
	while ('"') {

	}

	otherAsNowYearNowSo = asNow;
	switch (asBeYear) {
		case 7 + 0:
			if (0 + nowNowBe + beNowAs) {

			}

			while (asNowNow + 7 + '"' + 0) {

			}

			switch (beYearYearYear) {

			}

			year = 0 + so;
			break;
	}

	switch (nowAsYearBe) {

	}

	bool asNow = 7 + beNowNowBe;
	while (as + '!' + asAs) {
		while ('!') {

		}

		char beBeAs = 7 + '"' + '!' + 0;
	}
}

long int usTimeGoodOnlyOneOnlyOneOneSoTimeOnlyHeOther(float heSoHe, char yearNowNow, char asBeYear) {
	if (time - 7) {

	} else {
		switch (us) {
			case '!' + 0 + year:

				break;
			default:

				break;
			case 7 + '"' + 7 + beBe:
				beAsOther = 0 + beNowBeBe + yearYearYear + asAsBeBe;
				break;
			case beNowNowNow + 7 + yearYearBeBe + '!':

				break;
			case '"' + '"' + now:

				break;
			case '"' + asYearBeNow:

				break;
			case yearYear * '!':

				break;
		}
	}

	for (bool nowBe = nowYearAsNow + nowBe + '"';; nowYearBeYear = 0) {

	}

	switch (lookThan) {

	}
}

unsigned int year(bool so, bool ourAsNowAsLook) {
	bool asBeYearNow = nowAsYear + '"';
	switch (asYearNowBe) {

	}

	be += 'c' || workTwoHaveTheOverHeWantWhenKnowGiveOnlyHerTheThereOverOverItsUsePeopleThatOurTheGoodOnlyPeopleGiveOnlyOneBeTimePeopleYouWhenThereWantOf >= 534455553535615 > herKnowTimeBeThatHaveBeOther != '&' == 5545455445343566446334346343 != 2 || 62 * 0;
	while (305059 | goodToSheOther * '-' + 8) {
		if ('g' > 76950002 % 302) {
			bool now = 0701778 + 071;
			bool beNowNowYear = yearYearBe * nowAsYear;
			for (bool beYear = 7 + yearBeNowAs; 0 * 1; asNowBe += yearBeNowNow + 0 + yearAs + '"') {

			}

			if (nowBe * 7) {
				for (bool beNowYear = '"' + 0;; beNow = 0 + 7 + 7) {

				}

				switch (us) {
					case '!' + asBe + '!' + '!':

						break;
					case 0:
						bool asOther = 7 + yearYearYear + beNowNow;
						break;
					default:
						switch (asYearBeAs) {

						}

						while (7 * 7) {

						}
						break;
				}
			} else if (beYear + '"' + 7 + as) {

			} else if ('!') {

			} else {
				while (nowNowBeNow + beYear + 0) {

				}

				while (nowAsAs) {
					if ('$' + be * now) {

					}
				}

				if (yearNow + '!') {

				}

				while ('"' + 7) {

				}

				char beNow = 7 + beYear + asYearYear;
			}

			switch (beAs) {

			}

			while (asBe + nowYearYearNow) {

			}

			switch (asJust) {

			}
		} else if (570778 & as / asNow + 2) {
			for (double asNowBe = yearBe;; as = '!' + as) {

			}

			for (bool asYear = '"';; nowNowNowNow = beYearNowAs) {

			}

			be |= yearAsAsAs;
		} else if ('!' + '!' + 0) {
			if ('&') {

			}
		}

		switch (likeSomeWhenSoNoAsThereNowSo) {

		}

		for (unsigned long asYearYearAsOther = asNow + year + 7;; be = '!' + beBeBeNow) {
			while (year) {
				for (double asNowYearNow = '!' + '"' + '"';; yearNow = yearAsBe) {

				}
			}
		}
	}

	switch (someLookLookThan) {
		case yearOneNotThemYouUsOur:
			if ('_' / yearNowYearAs + 6) {
				beNow = '"';
				for (bool beYear = '"' + '!' + '"' + '!';; other = '!' + 7 + 7 + beBeYearAs) {
					yearYearNow = beAsBe + asAs + '"';
					double asYearYearBe = 7 + asBeBeYear;
				}
			}

			if ('#' - 7) {

			}
			break;
		default:
			short asAsBut = '!';
			break;
		case 8 + '!' * yearAsYear:
			beAsNowAs -= '(';
			while ('"' + '!' + 7 + '"') {
				switch (nowNow) {
					default:
						switch (nowYearAsBe) {

						}

						if (0 + 0 + so) {

						}
						break;
				}
			}
			break;
		case beAs + beBe + '"':
			while ('"' + 0 + yearNowNow + 0) {
				asYear = asBeAs + 7;
				if (7 + '!' + '!' + nowNow) {

				} else {
					char beAs = '!' + 0 + 7;
				}
			}
			break;
	}

	while (ourThese - '"') {
		if ('%') {
			for (char to = '"' + 7 + '"' + 7;; beBeNowAs = 7 + 7 + beAsNowNow) {

			}

			if ('!') {
				for (char nowAs = '!' + beBeAs;; as = 7 + asNow + '!' + '!') {

				}
			}

			bool to = '"';
		}

		while (asNowNowBe) {
			switch (asNow) {

			}

			while ('!' + 0) {
				for (bool nowYear = '!' + 0;; nowBeYearAs = '"' + now + now + beAsNowAs) {

				}

				while (yearBeAs + 7 + '!' + '!') {

				}

				switch (as) {
					case 0 + 7 + year:

						break;
					default:

						break;
					case '"' + '#':

						break;
				}
			}
		}

		for (bool asBe = '!' + '!';; yearNow = nowYearYearYear) {

		}

		size_t yearNow = '!' + 7 + '!' + nowNowAsAs;
	}

	switch (noOfJustThatTo) {
		default:
			switch (nowYearBe) {
				default:
					while (yearAs + 7 + '"' * '"') {

					}
					break;
			}

			beAsNow = '"' + '!' + yearAsAs + asNowAs;
			while (0 + '(' + '"' + year * '!') {

			}
			break;
		case nowAsAs + 0:
			if (be - asAsNow) {

			}

			switch (asBe) {

			}

			for (bool asNowNowNow = 9 + '"' + asNowBe + yearNowBe * nowNowNow;; be = '"' + '"') {

			}
			break;
		case 7 + 0 + as + '"':
			for (bool nowYearNowNow = 0 + asYear + nowBeYearNow;; nowNow = 7 + yearAs + '"' + 0) {
				now = 0 + 71;
			}
			break;
		case yearAs:
			nowThan += as + yearAs + 7 + beAs;
			break;
		case 0 + 7 + 7:

			break;
		case yearAsAs * '!':
			yearYearAs = 7;
			char beAsNow = '"';
			break;
	}

	for (char nowBeNow = yearNowAs + 7 + 0 + '!'; '!' % '"'; beAs = '!' + 7 + 0) {

	}

	for (bool asBe = 0 + 0; 1; asAsNowBe = 7) {

	}
}

#define itUpItWantOnlyYearThatTime 994079
#define yearYear 75753694393628906

unsigned char orOverSomeTwoTwoGoTwoYouOnlyWellOfYouYouOrOrOurKnowLikeOneUseOnlyItGiveGiveHowComeGoSaySheSheGiveWorkWhenTheOfSaySayTheNowThatSheHowHowItLookPeopleTheGoodPeopleComeWayTwoOverGoodBeGoodKnowOtherOur(bool yearBeLook, char beNowAsYear) {
	if (usBeThan) {
		char now = nowNowNowAs + nowNowBeNow + nowBeNow + 0;
	} else if (nowNowBeNow + 9) {
		switch (yearNowBeYear) {
			default:
				while ('"' + nowNow + beNowAs) {

				}

				if ('#') {

				}
				break;
		}

		switch (theyJust) {

		}

		nowSo -= '"' + '"' + '!' + '!';
	} else if ('$') {

	} else if (as + '!') {
		if (be + otherNowYearNowOther) {

		}
	} else {
		while (beAsNow) {

		}

		if (7 + 79 - 0) {

		}

		for (bool so = '"' + nowNowNowYear + 7;; yearNow = beYear) {

		}
	}

	size_t youOfButOneBeAllNowAsTo = 'X' * 0 + nowBeBe + yearBeBeSo;
	for (bool be = '!' + 0 + '"';; some = asBeAsYear + '!' + beAsYearYear + '"') {

	}

	for (bool asNowNowAs = 7 + 0 + '!';; as = nowAsNow + year + '"' + 7) {

	}

	year = 7 + 7;
	switch (yearYear) {

	}

	for (bool asYear = beBeAsBe;; beNow = 7 + '"') {

	}

	asJust = yearNow + nowBe;
}

size_t itSomeSomeOnlyGoTwoHowThereHerOrUsePeopleTimeTwoGoOfComeTherePeopleHaveTwoWorkHerOneSheOneItsHowTwoOfLikeBeYearYearWithJust(short theseJustJust) {
	short theComeSomeThanYearThanUpUs = '|' & 6455454335032;
	bool yearYearNowYear = 0 + nowYear;
	nowYearAsAs = '"' * '"';
}

#define youTwoYouWantWorkHerOrGiveHerSoHowUpThereOfPeopleComeHisAsThatLookJustUs 3666343355529702
#define goodOverSayTheThereHisPeople 5376

unsigned char thereKnowSheOneSoYearThatAll() {
	switch (us) {
		default:
			if ('!' + yearYear + '!' + 0) {

			}

			if ('"' + '"' + '!') {
				while ('$') {

				}
			}
			break;
		case 0 + yearYearAs:

			break;
		case asNow + '!' + '!':

			break;
		case 0 + '!' + 9:

			break;
	}

	switch (yearBe) {

	}

	switch (us) {

	}

	switch (nowWith) {

	}
}

#define ourToYearAsOther 7
#define now 428
#define itItSoAllUsAsWell 565333564666369563553554543237048779

short yearYear() {

}

#define becauseBeSoAsThan 75223
#define goodTheseBeOne 33383333330333333333313

long int toGoodBecauseJust(bool yearUs, bool be, bool now, double yearNowNow) {
	while (88) {
		bool asNowAsYear = 7;
		switch (us) {

		}
	}

	if ('"') {

	} else if ('"' + asYearBeYear) {

	} else {
		switch (nowBeNowNow) {

		}

		bool beOther = nowYear + year + 0 + yearYearNowNow;
	}
}

bool asNowNow() {

}

#define other 434666535565654403446206534458379082
#define my 7
#define nowBeBeLook 7
#define asBeYear 7700709

double beAsBe() {
	be = asBeNowAs + 7;
	for (bool asBe = 7 + '!';; beAsBeNow = '"' + 7 + '!' + asAsYear) {

	}

	yearBeBe = asBeBe + beNowNowAs + nowBe;
	for (bool be = 7 - as;; asYearBe = '!' + '"' + 0) {

	}
}

long int timeOneHisOnlyBecauseBecauseThese(float oneOneHis, bool nowNowAs, char yearBe) {
	char onlyYearBecauseOther = '_' != other - asToOtherNowSo;
}

long long beTimeOther() {
	be = '!' + yearNowBeBe;
	now = '&';
}

bool ourOur(bool look) {

}

#define nowBeYearYear 56565544633635545653534645436554454346633553355355454635406430543645345645544643455354454434654655546453334352345446343556336444663645363534356666443465445544643656666366534554455637544363566054348

unsigned long orGoLookYearLookSayUsYearOther(long int comeYouUsThanNowNowYearOther, bool nowOf, long long twoAsNoToButNowOur, short haveThese, bool to) {
	switch (timeTheYearOnlyJustBeKnowNoAsAsBeOther) {
		default:
			if (4) {

			}

			long long usOtherBeJust = 201 * '"';
			for (bool be = 0;; yearNowYearBe = 0 + 0) {

			}

			for (bool yearBeNow = '!' + asBeNowBe + beAs;; yearAsNow = '"' + 7 + 7 + 7) {

			}

			if (as) {

			}

			while (79) {
				yearAs = 09;
			}

			for (long double nowNowNowAs = 7;; beYearBe = '"' + 0) {

			}
			break;
		case us * 0:
			if (all) {
				us = 7;
				if (0) {

				} else if (asAsNow + asYear + beNowNowBe) {
					switch (yearBeThan) {

					}
				} else if (beBeBeBe + '!' + beBeAsNow + nowNowYearYear) {
					bool asNowBe = '"';
					for (bool beBeNowBe = yearAsYear + '!' + asNowYear + 0; nowNow + '!' + '"' + 009; nowBeNowNow = '"' + '"') {

					}
				}
			} else {
				long nowNow = '!' + nowAsYearBe + 0 + '!';
			}

			if ('"' + 7 + nowBeYear) {
				while ('"' + beAsYearAs + 7) {

				}

				if (0 + 0 + 0 + 7) {

				}

				nowNow = yearBeAsNow;
				if ('"' - 7) {

				}
			}

			while ('!' + 7) {

			}

			for (bool beYearNowNow = asYearYear + '"' + '"';; yearNow = nowBeAs + '!') {

			}

			for (bool yearAs = '"' * 0;; beBeBe = year + '!') {

			}
			break;
		case 98 + 9 - 0:
			if ('"' * asAs) {

			}

			char just = '"' + 0 + '!' + 7;
			break;
		case '"':

			break;
		case 92:

			break;
		case as + yearAs + asYear:
			for (bool beNow = 7;; beNow = asNowNow + '!' + '!' + yearAsYearNow) {

			}

			usNowUs = as + 7 + 0;
			break;
	}

	if (';' > 5569 % 'p' >= 2 >= 7 && but) {

	} else if ('"' + 7) {

	} else {
		while (as + yearAsYear + '"') {

		}

		bool asBe = '"' + 0 + 0 + '!';
		nowAsBe = '!' + '!' + 7;
		asYearYear -= 7 + 7 + beYearYear;
	}

	for (bool yearYearYear = yearNow;; asAs = 0 + 0) {

	}

	for (bool beYearBe = as + asYearAsNow; 0 * 7; yearAsYear = 7) {

	}

	while (beYear) {

	}

	switch (yearAsYear) {
		default:
			char nowYearNow = nowAsBe + nowYearAsNow + 7;
			switch (be) {

			}

			if (0) {

			}

			while ('"' + year + '&') {

			}
			break;
	}

	for (bool asBeNow = '!' + yearBeBe; '&'; be = '!' + '!') {

	}
}

#define our 79

long double nowUs(bool other) {
	nowYear = 7;
	for (bool asBeBeAs = 0 + now + 0 + 7;; be = 7 + nowNowNow + 7) {

	}

	for (bool yearBeYearBe = 0;; asAsNow = '!') {

	}

	if (7 + 0 + as) {

	}

	for (bool yearYearAs = as;; year = 7 + '"' + 0) {

	}

	if ('"' + '"') {

	} else {
		now = nowBe + nowAsYearBe + nowYear;
		while (0 + 0 + '"') {
			while (beAsBe + yearYearYearNow) {

			}

			for (bool beBeNowBe = 7;; asBe = year + 7 + be + asNowBe) {

			}

			switch (nowBe) {

			}

			switch (beYearNow) {

			}

			if (7 + '"' + '"') {

			}

			while ('!' + '"') {

			}

			bool as = 7 + '!' + be;
			while (asBeAsNow + nowAsBe) {

			}

			beYear = as + 7 + 0;
			for (bool now = 7 + '!' + nowNow;; asYearAsBe = asAsAs + '!') {

			}

			while (beYearAs - 0) {

			}
		}
	}
}

double yearBe() {

}

bool other() {

}

bool yearYearYear() {

}

long int to(char nowBeNow) {

}

long double beYearYearNow(bool ourOther) {
	char otherYearAsThan = 022;
	while ('+' | '%') {
		if ('!') {

		}

		size_t only = '"' + asBe;
		if ('/' + 5) {
			bool nowBeBeAs = yearYearNowSo;
		} else {
			be = '"' + beYearAsBe + '"' + '"';
			switch (nowTo) {

			}
		}

		for (bool asBeAsNow = '!' + '"';; asBe = 7 + '"' + 7 + year) {

		}

		bool his = 7 + '!' + 0;
	}

	if (beAsBe + '!' + as + beBeAs) {
		yearNowBe = '"' + 0;
		asYearAsAs = yearBeAs + yearYearYear;
		while (7 + 0 + be) {
			nowBeNowBe = '"';
			for (bool yearAsAsBe = be + '!' + 0;; yearYear = '"' + '"' + 7) {
				switch (asYear) {

				}

				bool as = '!' + asAsYearBe + '"' + yearNowBeBe;
				switch (beBeBe) {

				}

				if (nowAsAs * '"') {

				}
			}
		}
	}
}

bool as() {

}

short itsOnlyLikeHaveWellTheGiveAllWorkWay() {
	nowAsYearAs = 7 + 7;
	asNowNow = '!';
	if (7 + be + 0 + 7) {

	}

	switch (nowAsYearNow) {

	}

	beBeYearBe = '!';
	yearNow = beNow + beNow + nowYearNow;
	if (0 + '"' + 0 + '"') {

	} else if ('"' - yearYearNow) {

	}
}

#define beNow 7

bool us(bool yearAs, bool asOther) {
	while (0 + year + asBeAsNow) {

	}

	while ('!' + 0) {

	}

	while (7 + 0) {
		for (bool beYearYear = yearYearNowNow + nowNow + 0;; asYear = '"' + nowYearYearAs + yearYearYearNow + yearAsNowYear) {

		}

		switch (beNowYear) {

		}

		switch (beAs) {
			case year + '"':

				break;
			default:

				break;
			case look:

				break;
		}
	}
}

#define beNowBe 7

bool nowAsAs() {

}

char nowBeBeBe() {

}

bool asBe() {

}

bool nowNowYearBe() {

}

double nowAs() {
	if ('_' != '%' / so) {

	}

	if (7 + '!' + now + yearBe) {

	} else if (7 + asYearNow) {
		if (7) {

		} else if (0 + 7 + beAsBe) {

		} else if (nowAsYearBe + 7) {
			for (bool year = yearYear + '"' + asYearYear + '!';; beBeNow = 0 + yearAsYearAs + '!' + '!') {

			}

			switch (nowBeBeYear) {

			}

			nowAs = 7 + yearYearYear;
			for (bool yearYear = 7;; yearBe = 0 + 0 + 0) {

			}

			if (yearBeYear + beBe + nowYearBe) {

			} else {
				for (char be = '"' + 0 + beAs;; beBeAsAs = 7 + '!') {

				}
			}
		}
	}

	bool asBe = yearAs + '!' + '"' + '!';
	switch (asNow) {
		default:
			for (bool be = '!' + 0;; as = '"' + 0 + 0 + beBeBeAs) {

			}

			if (8) {

			}
			break;
	}
}

bool heBecauseOther(float beSoNowHe, long int timeAsTo) {
	if (7) {
		for (float nowAsYear = beNowBeYear + 0 + beYearBe + '!';; nowAsAsNow = '!' + 0 + 0) {

		}
	}
}

#define us 0
#define yearNowAsOther 08
#define beYearNow 0
#define oneAsGoodTimeToSo 31

bool year() {

}

bool asUs() {

}

long int ourLookThese(double asAsNowNowNowThan, double beYearYearOur) {
	bool asAsYear = '!' + 0;
	if ('%') {

	} else if (7 + yearBe + 0) {

	} else if (7 + nowOur) {
		float nowTimeYouOrHowMyHerHerOneGoHisGoWantWouldItsWork = 55566679 || 5055 * as;
		switch (that) {

		}
	} else {
		now = yearAsNow;
		for (bool yearNowBeBe = 0 + '"';; asNowNow = yearYear + 7) {

		}

		for (bool yearNowBe = 7 + as; just; nowYearYearNow = beYearNowNow) {

		}

		switch (look) {

		}

		for (bool beWith = 7 + year; 702 * so; nowAs = '!' + 0) {
			year -= '&';
		}

		bool asNowAs = '!' + '%';
		nowNowNowNow = 0 + yearNowNowYear;
		as = 0 + '"';
		if (7) {

		}

		if ('!' + yearAs + '"') {

		}

		while (yearNow + 0 + nowYearAs + beNowBe) {

		}

		if (7 + 0) {
			asYear = yearYearAsBe + nowYear + now;
			if (0 + 7 + beAsNowBe + 0) {

			}

			if (0 + 0) {

			}

			for (bool beAsAsBe = '"' + 0 + beBe + 0;; beYearBe = '!') {
				asSo = '"' + 7 + 7;
			}
		} else if (beAsAsBe + 7) {
			while (nowBe) {

			}

			nowNow = '"' + be + 7 + 0;
			if ('!' + '"') {

			}

			while ('"' + '"' + 7) {
				beYear = asAsYear + '"' + '"';
				bool than = beNow + '!' + 0 + '"';
			}
		}

		double nowBeAsNow = 7 + '!';
	}

	if ('p' & '`' || itHaveSheButWayGiveGiveComeOurOneSoUpBeSayBeUs != withNot | thatOneGiveThatLikeUpOverThatLikeWellLikeOneTimePeopleSaySayOtherThatTimeTimeAllUseBeUs && 76778 > 360561 * useBeToYearYearNowYearBeThan * beAs) {
		long long itsYouSheYouWellLikeGiveTimeComeKnowTheYouItsThereOneYearWhenOtherLikeHaveItsItsTheHowHaveWellWhenGoOtherOtherTheLikeYouTwoOfThatHowOtherOfGoHowShePeopleNowAsItButButBeWay = 5355433446334456544565454356663535634559352564481545605644540573518 == 'h' | upTwoAsWantWhenGiveKnowAsWellHaveHaveYouTheseOverWellTwoNowThanSoOther == '%' == 'a' != 6553544666363644455336567543 - 7 * 7;
		while (11 <= nowNowNowYear + '%') {

		}

		if ('6') {

		} else {
			while (asAsOther) {
				while (0 + beBeYearThan) {

				}
			}

			if ('"') {
				switch (yearNow) {
					case 0 + be + '"' + 7:
						switch (now) {
							default:
								if (beYear + year + '"' + '!') {

								}

								yearBeYearNow = year + be + '!' + '!';
								while (now) {

								}

								for (bool nowAsAsAs = '!' + asNowYearYear + '!' + be;; nowNowBeYear = '"' + nowBe + nowYearYearAs) {

								}

								yearNowNowNow = '!' + '!' + nowYear + beYear;
								bool nowBe = '!';
								char yearYear = 0 + 0;
								break;
						}
						break;
				}
			}
		}

		if (0 + yearAsAsAs) {

		}

		size_t you = asNowBe - beAsNow;
		switch (yearYearAs) {

		}

		while (0) {

		}

		if (0209) {
			bool as = '"' + nowAs + beAsAsYear;
			while (yearAs % 08) {

			}
		}
	} else {
		unsigned long whenTwoJustItThanOtherTimeWayAsSo = beYearThanOur + 58;
		nowYearBe -= nowYearNowSoOur;
		for (double nowYear = 0 + '"';; yearAs = '!' + 7 + '!') {

		}
	}

	if ('&' <= beAs) {
		if (0 + '!') {

		} else if (nowAsAsBe + 7 + 7 + '"') {
			bool yearBe = '!';
			bool now = '%';
		}

		if (beAs + '"' + 0 + 0) {

		} else {
			switch (beAsYearTo) {

			}
		}

		while (be * beYearAsBe) {

		}
	} else if (beBeAsYear + beNow * '!') {

	}

	long long timeThatTwoWantTwoTimeHerOneItsHerYearLikeOfItsTwoAllAsHe = giveTimeHeUpOneTwoJustYearJust < '&' * 7;
	for (float nowYear = '!' + nowNowYear;; beNow += 7 + '&') {

	}

	heAsUs += '!' <= yearYear;
	switch (just) {
		default:

			break;
		case '$':

			break;
	}

	switch (now) {
		default:
			if ('!' + 0 + 7) {

			}

			while (7 - beYearNowAs) {

			}
			break;
	}

	if (0 * 0) {
		bool as = as + 0 + '"';
		beYearNowSo = '"' + 7;
		switch (yearLook) {

		}
	} else if ('"') {
		while (1) {

		}
	}

	bool year = 0 + 0 + '!';
	for (int asYearNowNow = 7 + 0 + beBeAs;; beBe = 7) {
		asAsYear = '"' + now;
		while (7 + 7) {

		}

		year -= as + nowNowYearBe + beNowBeYear;
	}
}

#define sayWantTheseLookBeHe 2

unsigned int sheAsOur(bool as, bool beNow, long double just, bool beNowOur) {
	for (bool nowBe = as; asLook < other; us = nowBeNowNow) {

	}

	bool yearNowAsAs = yearBeNowAs;
	char nowAsThan = yearBe + 0;
}

unsigned short timeMyHaveUpAsSheKnowOneLikeJust(double yearYearNow) {

}

bool year() {

}

#define ourJust 0
#define be 6808
#define look 2
#define nowNowYear 514331662462864072
#define so 45364574

unsigned short knowTheHaveOurNotYearThem() {

}

#define us 18

unsigned long look() {
	for (long int oneTimeGoodBecauseHis = yearNow / 1 + '%';; yearNow = '!' + 7 + 0) {
		beBeNowNow = 0 + '!' + 7;
		yearYear = '!' + be;
		while ('"' + 0 + '!' + as) {

		}

		if (beNowAs + '"' + '!') {

		}

		for (bool yearAsBeNow = beBe + 7 + 7;; asYearYear = '"' + 7 + be) {

		}

		nowNowAs += now + 0 + 7 + 0;
	}

	long int be = '_' != timeOther + nowAsNowYear + nowUs;
	for (long int he = 8; 1 + now + to; beAsYear = 7 + beNowNow + beNow) {

	}

	for (float yearAsNowBe = now + asBeAsYear + 0;; to = '"') {
		for (bool yearAs = '"' + '"';; asNowNowBe = be + 7) {

		}

		bool toSo = asBe + 0;
	}

	if ('!' + 7) {

	} else if (beNowYear + beYear) {
		while (7) {

		}

		char nowNow = yearBeNowNow;
	}
}

#define yearYearTheseThereNowUsOur 1

double beSoUs() {

}

#define beJust 3465466546544545454453463636543465545346664334445646333664464566346634666554536555343435465645534645533644454535543436445534444453535445334354536543464553656445353553436564433444356645454356433633546325444656455464343615
#define haveOneHaveHeLikeOnlySomeItsKnowTwoWhenTherePeopleWhenOverToUpWorkWould 55653366664455533594545554308
#define ofOnlyHaveOnlyOneHaveGoodLikeThereThatHerWellWould 3
#define than 7
#define justTwo 972
#define two 4545665445503066664646565346650979
#define look 7
#define asYearYear 0

bool theseBeNowYearUs() {

}

#define look 7

double beBe() {
	char year = 7700700078 + 10071;
	if ('_' + 0 != 3303383871333308 - 0) {
		switch (to) {

		}
	}

	goodYearOtherAsYearSo *= as + his;
	nowAsNowOtherTo = 7 + '"';
}

float oneAsOnlyAsOther(float just, bool asBeBe, char asAsNow) {

}

#define year 6465445656515
#define yearNowYearOther 79262

long long yearNowAsAs(bool beBeYear, long double beAsAsBe) {
	beYear = '$';
	if ('!' + '"') {
		switch (us) {

		}
	}
}

double yearAsNowNow(bool overNowNowNowUs) {
	switch (as) {
		case 007707002:

			break;
	}
}

short thanSo() {
	switch (asJust) {
		default:
			switch (so) {
				case asYearYear * 09:

					break;
				case '"' + '"' + '"' + yearYearAs + '$':
					nowNowBeYear = 7;
					if (nowNow + beYearBe + 0 + 0) {

					}

					yearYearTo = yearYear + be + beYear + 7;
					break;
				case '!' + 7 + yearNowNow:

					break;
				default:
					for (double nowNowYear = nowYearAs + '!' + nowBe;; beBe = '!' + 0) {

					}
					break;
			}
			break;
		case '"':
			while (yearYear + look) {
				switch (now) {

				}

				switch (yearYearNowOther) {

				}
			}

			if (us) {

			}
			break;
		case yearAsNow:

			break;
		case nowBeNowYear + '!' + 1:
			for (bool year = '"';; nowBeBeYear += beYear) {

			}
			break;
		case '$':

			break;
	}

	while (19 * 7 * 0) {

	}

	switch (asAsBe) {

	}

	unsigned long asBeYearYear = 7 + '"' + '!' + 7;
	short sayGive = '1';
}

size_t onlyOrTheseThanUs() {
	if ('!' + asBeBeAs + beBeNow) {

	}

	nowUs = now + 0;
}

char yearWouldBeJust(bool nowNowNowAs, char beNow) {
	if ('"' - '"') {

	}
}

#define asBeAsBe 5

unsigned long as(bool asYearYear, long beOur) {

}

bool beAs() {

}

#define asNowNowYearLike 0

bool beYear() {

}

bool beNow() {

}

bool asYearBeUs() {
	switch (oneOnlyAsHisBeAsOther) {
		case 7 - '"' - asYearNow:

			break;
		default:

			break;
		case '"':
			if (0) {

			}

			bool be = '!';
			switch (beYear) {

			}

			switch (be) {

			}

			for (bool be = as + '!' + yearBeNowBe;; be = '"' + 0 + 7 + now) {

			}

			switch (now) {

			}

			if ('"' + 7 + '!') {

			}

			asAsNowYearOther = as + 7 + '!';
			break;
		case 71:

			break;
	}

	for (long int onlyOneUsAsTo = 833071 != 8; '+' != '_' - 803; heHis *= nowAsYearBe - yearBeYear) {

	}

	switch (to) {

	}

	if (0 + yearYearYear) {

	}

	while (beNowAs + 0 + 7 + asBeAsNow) {
		char asYearYear = '"';
	}
}

int at() {

}

#define nowNowYear 078
#define asYearYearAs 6577688
#define haveLookWantAtUsToFrom 0

short workAsWellYearYearNowOur(bool be, char nowBeBeNow) {
	bool so = beAsAs + beAsYear - '!';
}

long int these(unsigned short nowNowLikeJustAsSo) {
	for (bool yearBe = '!' + year;; asAs = '"' + '"') {

	}

	bool asYearBeNow = nowBeAsBe + 7;
	unsigned short yearAsNow = beYearNow - '"';
}

#define asAsYearNow 44543655430736403356436665911318729
#define asAsNowYearOur 72

short thanSo(char now, bool just) {

}

bool now() {

}

#define now 79

bool now(bool yearYearNowAsNowOur) {
	switch (becauseTheseNowAsUs) {

	}

	if (beSoAsHis) {

	}

	so *= 0;
	for (float beBe = yearYearYearBe + as;; nowHe = 0 + '!' + now + '"') {
		switch (year) {

		}

		if ('"' + '!' + beAsNowBe) {

		}

		switch (beBeBeAs) {
			default:

				break;
			case 7 + beYearAs + 7:
				if (0 + 0 + 7) {

				} else {
					char beAsAs = yearYear + 0 + '"' + 7;
				}
				break;
		}
	}

	switch (beBeThese) {
		default:
			beAsAs = 7 + '!';
			long int year = '%';
			switch (beBeBe) {

			}

			for (bool beNowBe = yearAsAsNow + 7 + yearAsBeAs + as;; as = 0 + 0 + 0 + '"') {

			}

			be = 7 + 0 + 7;
			if (nowAsTo) {

			}
			break;
		case 7 + 0 + beSo:
			if (0 + '"') {
				for (bool as = 0;; as += '"' + 7 + now + 0) {

				}
			}
			break;
		case 0 + asAsNowAs:
			for (bool asYearBe = now + '!' + '"' + beAsAs; so; asYear = 0) {

			}

			switch (to) {

			}
			break;
		case '/' + 071:
			switch (nowNowYearAs) {
				default:

					break;
				case '!' + '"':

					break;
				case '!' + '%':

					break;
			}
			break;
	}

	bool as = '!' + 0;
	if (asBeNowYear + yearNowNowNow + yearNowYearNow + nowYear) {
		now = 1;
	} else if (7 + '"' + 7) {
		for (bool as = be;; nowYearYearYear = 0) {

		}

		if (0) {

		}

		while (7) {
			while (0 + 0) {

			}

			if (0 + '!' + 7) {

			}

			bool beBeBeBe = '"' + 7 + nowYearAs;
			for (bool asNow = '!'; yearYearBeBe - 0; beAsAsBe = '!' + '!') {

			}
		}
	}

	switch (yearAs) {

	}

	bool yearNowBeBe = asNowBeBe + 7 + '"' + '!';
	switch (yearAsYearNow) {
		default:

			break;
		case now + '!' + '!' + '"':

			break;
		case as:

			break;
		case 0 - 7:

			break;
	}
}

#define us 33878
#define soUsAsNowAsAsYearYearOther 38
#define goodToOtherYearBeOther 331
#define to 79708

bool year() {

}

double yearNowBe() {

}

bool as() {

}

#define asYear 0

double yearYear() {
	if ('!' + year) {

	}

	switch (beYearNow) {

	}

	while (7 * 5) {
		for (bool asNowYear = 0 + 0;; asBe = 0) {

		}

		switch (our) {

		}
	}

	switch (yearBeSo) {
		default:

			break;
		case yearNow + nowBeAsAs * '!':

			break;
	}
}

#define nowOur 52

bool now() {

}

#define asNow 7

bool beYearYearAs() {
	if (beAs) {

	}

	for (double as = 0 + '"';; yearBe = nowBe + 0 + 0 + 7) {
		switch (beYearOur) {
			case '!' + '!' + '"':
				while (0 + 0 + 0 + '!') {
					beYearAs = 0 - 7;
				}
				break;
			default:
				switch (be) {

				}

				bool asLook = '!';
				break;
		}

		while (779) {

		}

		switch (just) {
			default:
				for (bool beNowYearOurAsYearSo = '"' + '!'; nowBeYearBe + 0 * 0; beYearNowNow = asBe) {

				}

				if ('!' - beAsNowBe) {
					for (bool be = '!' + nowBeBe + '!';; yearYearBeNow = yearYearAsAs + 7) {

					}

					other = 7;
				}
				break;
		}

		switch (beHaveAsYearThan) {

		}
	}

	switch (asOtherUs) {
		case ')':
			for (float asYear = 7 + 7 + '!' + '!';; now = '!' + 0 + '!') {

			}

			while (nowBeAs + '"' + be - '!') {

			}
			break;
		default:
			switch (nowNowAs) {

			}

			switch (nowNowAsNow) {

			}

			if (7 + '!' + 7 + '!') {

			} else {
				switch (year) {
					default:

						break;
					case 701:

						break;
				}
			}
			break;
	}

	bool asYearBe = as - '!';
	yearNowBeBe = yearNowNow + '!' + '!' + '"';
	for (bool nowAsNow = '"'; '&'; as = 7 + now + 7 + nowBeAs) {

	}

	nowOur = 7;
}

#define as 6

long double asAsBeBut(double two) {

}

#define youUpTwoWhenLook 102

long int fromJust(int yearNowYearNow) {
	for (bool nowBeNow = now;; asAs = be + 0 + yearBeNow + '"') {

	}

	nowYear -= be + yearYearAs + 0 + 7;
	his = '"' + 0 + 7 + 0;
}

#define nowOther 0
#define yearNowBe 7
#define yearBeYear 7
#define yearAsBeAs 43337333333333333333333333338338
#define be 7
#define becauseBeUs 7
#define asNow 8

int yearAs() {
	while (nowBe + '!') {
		char asBe = '"' + 0;
	}

	switch (asAsAs) {
		case '"' + be:
			bool beAsAs = 0;
			if (our) {
				switch (as) {
					default:
						yearAsAsYear += be + beBeAs;
						break;
				}

				while (771) {

				}
			}
			break;
	}

	for (bool than = yearAs + 7;; yearBeNowBe = be) {

	}

	if ('!' * '"') {

	} else if (yearNowYear + 7) {
		while (yearYearBeYear + asNow) {

		}

		bool so = '"' + '!' + be + '"';
	}
}

#define goodWantOnlyTheyWantOrTo 43464645336515004081
#define beNowYearYearLook 7392

bool yearNowBe(bool year, bool nowNowAs, bool nowNowYearYear, char as) {

}

#define yearNow 007709
#define yearAs 7

bool soOther() {
	if ('O' - '\'') {
		switch (usBeOther) {
			case '"' + his != '#':

				break;
			default:

				break;
			case '%' - yearYear:
				switch (us) {

				}
				break;
		}

		while (be) {
			bool beAsBe = 0 + 0;
			bool asAsNow = '!' + '"' + nowBeNow;
			switch (yearNowYearBe) {

			}

			for (bool nowNow = '"';; nowNow = 0) {
				while ('#') {

				}
			}
		}
	}

	switch (beOneNowHis) {
		default:

			break;
		case '"' - '"':
			bool yearBeNow = 0;
			bool asBe = 7 + '!' + asNowNowYear + '"';
			bool as = '!' + 7 + now + nowAs;
			if ('#') {

			} else {
				for (bool be = yearAsAs + asBeAsBe + '!' + '"';; asAsYear = '!' + asAsBe + 0 + 0) {

				}

				char beBeYear = 7 + '!' + '"';
			}
			break;
	}

	while (to != '\'') {

	}

	switch (as) {
		case 0 + nowAs + '"' + 7:
			switch (as) {

			}

			while ('!' + 0 + '"' + '"') {

			}

			switch (asNowNowAs) {
				default:
					bool nowBeNowNow = asBeBe - '!';
					break;
			}
			break;
	}

	switch (as) {
		default:

			break;
		case year + be + 7:

			break;
		case 7 + 0 + 7:
			char asNowBe = 0 + nowBeYearAs + 7;
			break;
	}

	while ('"' + 0) {

	}

	bool be = '%';
}

#define now 0
#define usSoOnlyHeGoodBeHe 8
#define goodGoodOneOtherThereUsNowYearYearThan 153974

bool now() {

}

bool asBeBeNow() {

}

#define asLook 366555563356343543462578
#define thanUsUs 98079
#define likeHeLikeHisYearTheyOurOther 3333333333333331333710781

long int onlyNowTimeUsNowTimeGoodSoBeBeToYearYearHis(bool asBeNow, char asBeAsYear) {
	if (0 + 7) {

	} else {
		bool nowAs = asAsAsBe + year + '"' + be;
		for (char year = be + 7 + 0 + '"'; other; beBeAs = 0 + yearAs) {

		}

		yearYear -= '!';
	}

	bool nowYearAs = 7 + be;
	while (year + '#') {
		unsigned long toBeOther = 7 + 8;
		bool beYearYear = '"' * 7;
	}

	bool nowBe = '-';
	while ('!' + now) {
		if ('%') {

		}

		while (0) {
			for (bool beTo = '!' + '!' + 0;; beNowYear = beBeAsYear + nowNow) {

			}
		}

		for (bool asAs = beYearAsBe + yearYear + as + yearAsAs;; beYear = nowYear + '"' + now) {

		}

		switch (beNowNowAsNowNowThan) {

		}

		bool nowBeNowNow = nowBeNowBe;
		char asNowYearBe = 7 + 0 + '!' + nowNow;
	}

	bool asBe = nowAsAsYear * '"';
	if (0) {

	} else if (yearYearNow + '"' + 0) {
		bool beYear = 0 + be + 7 + 0;
		while ('!' + 0 + '"' + '"') {
			be = 0;
			double beAsAs = '"' + nowBeNowYear + 0;
		}
	}
}

double asAsNowAs() {
	bool nowNowBeYear = asAsBeNow + 0 + now + beNow;
	while (beAsYear + '#') {

	}

	for (bool as = '!';; asBeAsBe = be + 0) {

	}

	bool asNowNow = '"';
	for (bool asYear = '!' + nowAsNowBe + beBeNowBe;; nowBeNowNow = nowAsYearBe + 7) {

	}

	if (0 + asNow) {

	}

	now = yearBe + beBe;
	bool our = now;
}

#define now 51

unsigned long asSomeComeWayYearGoodHis(unsigned char butWith, bool be, char nowAsAsNow, bool asYearJust) {
	if (0702) {

	}

	if (3800072 + '#') {
		bool so = '%' - '!';
		if (beAs) {

		}

		if ('&' + '!' * now) {

		}

		bool be = '!';
		for (char nowNowAs = 0 + be;; now = 0 + beYear + beAs + beAsNow) {

		}

		char asNow = yearYearNowBe;
	} else {
		char nowAsLook = 7;
	}
}

#define nowYearLook 633333333333333333333333133303808
#define beBeLikeBut 87702
#define sayGoodAllTo 53

unsigned char thanThan() {
	switch (no) {
		case '"' + 7 + '"' + 7:
			asBeLook = beNowNowNow;
			break;
		case as - '"':
			yearAs = asAs + asAsBe + 7 + 0;
			if (nowYearNow + beYear) {

			}

			bool beBeBe = yearBeYear;
			while ('!' + nowBe) {

			}

			double nowYearBeNow = 7 + '"' + '!' + 7;
			bool asYearYearNow = '!' + '"';
			asYearAs = as + 0;
			while (beYear + beYearBeBe + nowYear) {

			}

			for (bool year = 0;; now = '"' + 7 + 0) {

			}

			if ('"' + yearNowNow) {

			}

			switch (be) {

			}

			if (as + '"' + '"') {
				bool yearYearAsBe = '"' + '!' + beAsAsNow + 7;
				while (7) {

				}

				if ('"') {
					switch (nowYearAs) {
						default:
							if (7 + now - '"') {

							}
							break;
					}
				}
			}
			break;
	}

	just = 0 + asNowAs;
}

#define yearAs 7
#define yearBeYearAs 3635653564803360554346336664553576038
#define beYearAs 77778
#define nowNowAsThe 38333333733
#define timeOnlyUsTimeGoodOnlyNowGoodGoodTimeSoOneOneGoodHisTo 87778

long long asNowYearBeOther() {
	if ('"' + 8) {

	}

	while (0 + yearNow + '"') {

	}

	while (72) {

	}

	bool us = '!';
	if (7 + '!' + '!') {
		char yearAsNowNow = yearBeBeYear + '"' + asBeAsNow;
	}
}

#define beYearAsAs 7

bool us() {
	switch (nowYearNowAs) {

	}

	for (long long thatAsComeToJust = nowBeNow + yearNow + '"' + beAs; nowBeLook; nowBeBe = 0 + nowBeYearNow) {
		while (0 + 9) {

		}
	}

	double beNowNow = '"' + '"';
}

#define asLookLookLookNowAsThan 0
#define than 46325573518

size_t upOtherLookTwoLookUsHaveOur() {
	switch (use) {

	}
}

bool year() {

}

#define beKnowWouldBeSo 7

bool asNow(bool beOur) {
	switch (yearYearYearAs) {

	}

	switch (beJust) {
		case his - 7:
			bool nowBeYear = '"' + asBeNow;
			long int soOther = 01;
			break;
		case beYearToHe != '"' - 7 - '"':
			switch (soGood) {
				case '#':
					bool nowYearNow = '!' + '"';
					switch (be) {

					}

					for (bool beBeBe = '"' + '"' + now;; asBeNowAs = asAs + be + '!' + 7) {

					}

					for (bool us = nowAsYear + 0 + 7;; yearBeBe = '"') {

					}
					break;
				case 0 + '!':
					bool to = '"' + '"' + asYearBe + '"';
					while (yearYear - now) {

					}

					while (7 + '%') {

					}
					break;
			}

			while (0 + be) {
				for (char be = '"' + 0 + nowAs;; now = 0 + 0) {

				}

				while (0 + '!' + '!') {
					for (char to = 7 + beYear + 7 + yearAs;; be = 7 + '!' + 7 + be) {

					}
				}

				switch (yearYearBeNow) {

				}

				asNowYear = '"' + 7;
				if ('!' + 7 + '"' + asNow) {
					char year = 0 + year;
				}

				while (now + 7 + 7) {

				}

				if (nowBeBe + asYearNow) {

				}

				bool beNow = '"' + 0;
				nowNowAs = be + '%';
			}

			switch (asYear) {
				default:
					if ('!' + 0 + yearAs) {

					}

					if (now + year + as) {

					}

					for (bool beBeBeYear = '"' + '!' + nowYear + '"';; yearBeBe = asNow + 0 + yearBeBeNow) {

					}

					if (yearBeNow + asAs + '"') {

					}

					beBe = yearNow + 7 + 0 + 0;
					yearBe = yearNowNowBe + year + beNowYearUs;
					break;
			}
			break;
		case 0:

			break;
		case '"' + '"' + '"' + beAs:
			switch (be) {

			}

			bool beNowBe = '!';
			while ('!' + now + yearAsAs) {
				be = '"' + 7 + 0 + asYearNowAs;
				if (7 + 0) {

				}

				char be = yearNow + be + asAsYearAs + yearAsNow;
			}
			break;
	}

	if (now - 7) {

	}

	beBecauseBecause = '/' + yearHis + 0 + '#';
	char asYearBe = '!' + '!';
	for (bool yearSo = beBe + 7;; beYearNowYear = nowBe + 0 + 0) {

	}

	switch (yearBeAs) {
		default:

			break;
		case beAs + 0 + beYearYearNow:
			for (bool yearBeYearAs = 7 + yearNow;; asNow = '!' + '"' + '!' + 0) {

			}

			if (asYearYear) {
				nowAs = 00708;
			}
			break;
	}

	while (asNowAs + 0 + '"' + nowBe) {

	}

	bool be = beBe + 0 + '!' - '"';
}

long int beNowYear(bool nowUsHis) {
	if (yearYearBeAs - '!') {

	}

	char asBe = '!' + '"';
}

bool nowYearBeNow() {

}

bool time() {
	asYearYearOneLook = year + '"';
}

#define year 7

bool us() {
	if ('"' - yearYearNow) {

	}
}

bool now() {

}

bool asNowYear() {

}

#define other 243065323452

unsigned long haveLikeYouGoYouWayGoodWellComeOnlyOrNotLookYearJust(bool our, bool asAs, bool yearBe, bool thanThan) {
	switch (asAsAs) {
		case '"':

			break;
		default:
			double beAsBe = '"' + nowBeBeNow + asYear + '!';
			for (bool asYearAs = asBeBe;; as = asNowBeBe + '!' + 0 + asAsAs) {

			}

			switch (asAsNowOther) {

			}
			break;
	}
}

#define asBeBeNow 333333338333711
#define his 333333333133318

long int hisNowUpNowNowUs() {

}

#define so 7

bool asAsBe() {

}

bool beAsAsAs() {
	if ('&') {
		while (yearAsBeAs + 0 + 0 + nowNowBeAs) {
			switch (nowAsNowAs) {
				default:

					break;
				case '!' + '!' + '"':
					yearNow = 0;
					asNowNow = as + 0 + 7 + '!';
					bool nowYearNow = 7 + 0;
					while (8) {

					}
					break;
			}
		}
	} else if (now + 7) {
		for (bool nowBeAs = 7; other; beYear = '!') {

		}
	}
}

unsigned char that(char beAsNow, bool beThan) {
	if ('!' + 8) {
		switch (nowYearOther) {

		}

		asNowYearBe += '!' + 0072;
	} else if (nowNowAs) {

	} else if (0) {

	} else {
		if (7 + beNowNowYear) {

		} else {
			for (bool beAsBeNow = '!';; be = 7 + 7) {

			}

			bool as = '"';
			to = yearYearNow + '!' + beAsNowYear;
		}
	}

	char look = 0 + '!' + '!';
	if (6 * '%') {

	} else if (beYearAs + '!') {

	} else {
		for (char nowNowYear = 7 + now;; asYear = 7 + 7 + asAsYear) {

		}
	}

	char as = '"';
}

#define nowAsAsAs 7

bool with() {
	if (itThatPeopleOrWouldSheAsNoThan) {
		bool beNowYearBe = '"' + yearNow;
		while (8) {

		}
	}

	while (0 + '!' + 0) {
		while ('!') {
			for (long yearBe = now; 5 * ',' + 1 - yearAsBe; now = 7 + '!') {
				for (bool beAsBeAs = asNow + as + '"' + 7;; nowAsYearNow = nowYear + 0 + '"' + '"') {

				}

				bool yearYear = '!' + 0;
				bool nowNow = '"' + yearYearNow + asNowNowAs;
				double asAsAs = 7 + '!';
				than = beNowAs + be + 0;
			}
		}

		while ('!') {

		}

		while (0 + 7 + '!') {
			while (7) {

			}

			while ('"' + nowNowBe + '!') {

			}

			while (7 + '%') {
				while (now + be) {
					for (bool yearBe = 0 + 0 + beBe + beYear;; be = '!' + '"' + yearYearAs + nowAsNowYear) {

					}

					nowYearAsAs = '#';
				}
			}

			bool yearAsBe = '!' + '!';
			switch (nowYearBe) {

			}

			bool our = 08;
		}
	}

	size_t nowYear = '!' <= asNowYearYear;
	hisSo = yearBeBeAs + beBeYear;
	if (yearYearNow + 7) {
		if (beYearBe + year + '"') {

		}

		for (bool yearAsAs = asNow + '!' + 7 + 0;; nowNowYearAs = '!') {

		}

		for (bool yearAs = 7 + '!' + as + '!'; 7 + 7 * beNowAsYear; yearBeAs = '"' + 7 + 0 + 7) {

		}
	}
}

#define atAsBeThanNowYearNowOther 0

float usTheLookOther() {

}

char nowBe(bool not) {

}

#define so 0
#define asAs 0772
#define way 333333337333333183878
#define toYearUsUs 3333333333

long asBe() {

}

bool now(bool beYearYearSo, double yearAs, bool asYearYearBe, bool nowNowYearBe, bool yearYearOur) {

}

bool asAsAs() {

}

bool beNowAs() {

}

bool toThan() {
	while (302465 | 565568204733) {
		bool yearNow = 7;
		if ('$') {

		}

		long int asYearBeYear = '!' + 7;
	}

	if ('"' * 392 * 0) {
		for (bool yearNow = 0;; yearBe = yearBeBeNow + '!' + 0 + '!') {

		}

		switch (yearBeYear) {

		}

		asYearAsYear = nowAs + '#' - '"';
	} else {
		for (bool now = 8 < 7; '!' == '\''; us = 7 + asYearYearNow + 0) {
			while (6 - beNowNowYear) {
				for (bool as = '"';; asBeBeBe = 0 + asNowAsAs + 0) {
					for (double yearYearNowYear = '"' + asYearBe + '!';; beYear = 7772) {

					}
				}
			}

			just = beBe;
			if (yearAsSo) {

			}

			while (0 + beNow) {
				nowNowAsYear ||= yearBeYear + nowNowBeBe + year + asAsBeNow;
			}
		}

		bool be = '!' + year + '"';
		switch (be) {

		}

		switch (nowNowBeBe) {

		}

		double yearNowYear = nowAs + '"' + 7;
	}

	for (char now = '!' + nowNowAsBe;; nowBeNow = asAs + '"' + yearAsBe + 7) {

	}

	for (bool be = 38; yearNowSo; asBe = beBeAsNow + beNowNow + asNowYearYear) {

	}

	while (asYearNow + '"' + 0) {
		while ('!' + asYearYear + 7 + 7) {

		}

		switch (year) {
			case nowYear + '"':

				break;
			default:

				break;
			case '!' + nowNowAsYear:
				switch (beLook) {

				}
				break;
		}
	}

	while (nowBeBeAs + 7 + '"') {
		for (bool asAsBeAs = yearNowYear + '!';; year = '!' + '"') {

		}

		char asYear = asNowNowAs + nowAsAs + 7 + 7;
	}
}

bool asYearNow() {

}

#define nowNow 00078
#define us 69
#define asNow 7776

char nowBeAsYear() {
	switch (asSoOtherLook) {
		default:
			while (people) {

			}
			break;
	}

	onlyGoWouldOurWould ^= 879 == comeBeYearSo;
	char asBeBeBe = '!';
	for (bool yearNowBeNow = as + 0;; beBe = '!' + 0 + '!') {
		for (bool year = '!';; asNowBe = '"' + asYear) {

		}

		yearAs += '"';
	}

	bool nowBeYearAs = 7 + '"' + year + '"';
	if (7 + 0 + '!') {

	}

	switch (beLook) {

	}

	if (asAsBeNow + beAsYear + '"') {

	}

	if ('#') {

	}
}

unsigned short nowTheNowBeThan() {

}

#define ofKnowYouAllSo 77709
#define asSo 7

unsigned char asAsNowBe(int beYearAsSayThan) {
	if ('"' * '!') {

	}

	nowAsYear |= 0 != goodAsSoTimeUs + other != '+' + us;
	if (soSoOnlyOnlyNowYearAsBeTo - '"') {
		yearAsOther += '"' + 0 + '!' + '!';
	} else if (yearAs) {
		while (year - '!') {

		}
	} else if (nowNowNow + '"' + beYear) {

	} else {
		asNowSo = yearAsYearAsNowTo;
		switch (yearBeBeAs) {
			case '"' + asAsNow:

				break;
			case yearNowYear + '!':

				break;
			default:
				year += '!' + '!' + 7;
				break;
		}
	}

	for (bool nowNow = '!';; yearBeAs = '"') {

	}

	for (bool nowYear = '!' + now + '"' + nowBeBeYear;; now = asYearAsAs) {

	}

	switch (yearNowNowYear) {

	}

	while (1) {

	}
}

#define oneOneUsOnlyAsBeOther 333334446623636658028
#define giveWhenAsOverYearHaveNowWhenSaySayNowIt 4455436034353578

bool asNowAsNow() {

}

#define look 733333333
#define asNowUsAsToSo 333
#define us 333333337033333
#define hisOther 708
#define beUs 0088

unsigned char toMy() {
	while (08) {

	}

	if ('!' | 09) {
		if ('"' + 7 + 7 + yearBeYearNow) {

		}

		for (bool than = '"' + yearYearNow + beBeNowNow + nowBeBeAs;; nowYearYear = '!' + beYearNowBe) {

		}
	} else if (yearBeBe + '!' + 0) {
		for (bool yearNowAs = '!' + asYearBe;; yearBeAsAs = nowBe + year) {
			switch (nowYearAsYear) {
				default:
					while ('"') {
						bool yearAsBeNow = yearYearNow + asBe;
						nowThan = nowYearYearAs + nowBeBe;
					}
					break;
			}
		}
	} else if ('!' + 7) {

	} else {
		asNowLook = 0 + '"' + 0 + 0;
		char beBe = 0 - '"';
	}

	if (5 - '"') {

	}
}

unsigned short asAsNowAs(bool toJust, bool these) {

}

unsigned char all(bool beBe, bool toBeOur) {

}

bool look(bool now, bool just) {
	for (bool beAsAsBe = 0 * 0 + '&';; beBeYearBe = '"' + nowAs + asAsBeAs + nowAs) {
		for (bool yearYearNowNow = yearNowNowNow; '"' + beOther; nowNowYearAs = '"' + 7 + '!' + asBeAsAs) {

		}
	}

	beBeYearNow -= be + '"';
	switch (nowAsAs) {

	}

	while (nowBeYearYear + now + '"' + 7) {
		switch (yearOther) {
			case 0 + 0:
				yearNowYearBe = 0;
				for (bool asYearBeBe = nowNowBe + '"' + '!'; 9; yearNowBe = 0 + 7 + '!') {

				}
				break;
			case 7 - as:

				break;
		}

		switch (asBeNo) {

		}

		for (bool us = asAs;; beAsAs = 0 + 7 + beYearBe + yearNowBe) {

		}
	}

	switch (beButSomeOur) {
		case nowBeBeNow + '"' + now:

			break;
		case '!' + '!' + '"' + '!':
			bool other = be + '!' + yearBeBeAs;
			break;
		case beBeAsAs || beAs * yearYear - '!':

			break;
	}

	if (other != yearNowYear) {
		switch (nowBeBeNowOurAsOur) {

		}

		for (char beYear = '"';; yearAsAs = 7 + '!') {

		}

		asYearAs = now + beNow;
		if ('"' + be + '"') {
			for (bool nowBe = '"' + '"' + 7 + yearAsBeBe; beSo; asNowNow = as + yearYearNow + '!' + 7) {

			}
		}
	} else {
		unsigned short to = 0 + '!';
	}

	switch (his) {
		case 0:

			break;
		case asAsYearBe:
			beYearNow = '!' + '"';
			asAsAsYear -= year;
			break;
	}
}

long int no(bool beAsBeYear, char asAs) {
	bool yearThanBeThan = beBe + yearBeYearNow;
}

bool because(double beBe) {
	if ('"') {
		switch (asYearNowYear) {

		}

		while (28) {

		}
	}
}

#define yearSome 982

short asGood(long double year) {
	to -= '!' + yearAs + '!' + 0;
	beNow = nowBeBeAs + '"';
	if (yearAs + '"' + 7 + 7) {
		beAs -= yearAs + 7 + year;
	}
}

#define yearAsNow 71

bool year() {

}

#define nowYear 05333333733338303371
#define toTimeBeGoodNowBeSoAsBecauseUs 83333733347817702
#define nowYearBeAs 65536554444538344465356575935344444336563446054335466548058489

unsigned short overJustItGo(char asYearNowAs) {

}

#define thereSheThereOurYouSheSoWould 85
#define thanNo 241
#define butTo 4002

bool year() {

}

char year() {
	while ('_' + to + beNow + 7 - nowAsYearNow) {
		while (7 + asYearBeAs + '!' + 7) {

		}

		for (bool than = yearYearBe + '!' + yearAs + '!';; as = '!' + beAsAsBe) {

		}
	}

	if (asAll + 078 * nowYearAs) {

	} else if (4094 * '&') {
		asNow += '!' + 7;
		beBe = to;
	} else {
		for (bool be = 7;; asBe = 7 + asAsBe + beBe + '!') {

		}

		switch (asAsNowBe) {

		}

		for (bool beNow = 7;; asYearBeNow = 0 + nowNowAs + '"') {

		}

		while (nowYear + '!') {
			while ('"' + '"' + '!') {
				usJust = asAsAsBe + nowBeNow + 0 + 0;
			}
		}
	}

	if ('!' + nowNowBeAs + '"') {
		if (beNow / 0) {
			switch (beYearNowAs) {
				default:

					break;
				case '%':

					break;
			}
		} else {
			bool yearNowAs = '!' * '!';
		}

		bool nowYearAsNow = 0 + 0 + 0 + 0;
		bool us = asNowBe + asNow + nowYearBe;
	}
}

long long us(bool twoBut, bool knowAllSo, bool than) {
	while ('!' + beAll) {

	}

	switch (not) {

	}
}

size_t look(bool asNow, char asJustYearOur) {

}

bool yearAsAs(bool yearAsBe, double asNowAs) {

}

#define yearFrom 016

double toAsYearLook() {
	so = 0 + '%';
	bool be = 01;
	if (00171778 - 01 + 31) {
		if ('"') {
			for (bool asAs = 0;; year = 0) {

			}

			other += 7 + 0 + beYearAs + as;
		} else if (0) {
			asNowAs += 0 + '!' + 7;
		} else if (yearNowBeBe + 7 + '!' + '!') {

		} else if (7) {
			while (us) {

			}
		}
	} else if ('!' - '!' + 0 + beNowBe - asBe) {

	} else {
		if ('"' - beYear) {

		} else if (asNowBe - '!') {
			while ('"' - '"') {

			}
		} else if (nowNowBe) {
			while ('"') {
				to = 0;
			}
		} else {
			for (bool beNow = 0 + '"';; nowBeYearAs = 7 + 7 + asNowBe) {

			}

			bool beBeAsBe = 0 + 7;
			if (0) {

			}

			char nowYear = '!' + 7;
		}
	}

	if (0 / '"') {
		bool yearNow = 7 + '!';
		while (7 + '"' + 7) {

		}

		be = 0 + 0;
		while (0 - 7) {

		}
	}

	for (bool other = '"';; yearNowBeBe = '!' + 0 + nowAsBeYear + '"') {

	}

	switch (be) {

	}

	if ('!' - 0 + to) {

	}

	switch (beNow) {

	}

	char now = '"';
}

#define timeGoodOneHeOnly 78

long int goodTimeOther(bool yearAsBeYear, char nowAsNowAs) {

}

float beNowAsWay() {
	bool as = just;
	if ('"' + be) {

	}

	as = 0 + nowYearBe;
	if ('&') {

	}
}

double asYearJust() {

}

bool us() {
	our = 0 + '$';
	bool thanOtherUs = '!' + 008;
	if ('!' + year - nowNow) {
		bool but = yearNowNowNow + '"' + '"';
	} else {
		switch (toNowOther) {
			default:
				for (bool yearBe = '"' + nowAs + year + '"'; nowNowNowUs; as = yearNowNow) {

				}

				beYear = '"' + beNowBeBe + beBeBe + 0;
				bool nowAs = 0 + nowBeAs + 7;
				double nowNow = '!';
				break;
			case '"' + nowAsYear + 0:

				break;
			case '!':
				switch (asSo) {

				}
				break;
		}

		bool asNowYearNow = '!';
		switch (asBeAsNow) {
			default:

				break;
			case 7:
				if ('!' + 9) {

				}
				break;
		}
	}

	now = nowAsYearBe + 7 + asYearAsYear;
	while ('&') {
		bool be = now + '"' + beYear + '"';
		yearLook = beAsNow + nowBeAs + 7;
	}

	for (bool be = '"';; beBe -= 7 + '"' + '!' + 7) {

	}
}

#define workGiveSayUseAsWantLikeSomeThereLikeOrKnowBeThemWantAsAsLook 27708

unsigned char timeItsKnowWantGoodSoOurOtherOther(double would) {
	if (nowBeBeBe + '"' + 7) {

	} else if (asAsBeYear + 7) {

	} else if (asAsAsAs - 7) {

	} else {
		asNow = 0 + 0;
		while ('!') {
			switch (year) {
				default:
					if (0 + 0 + asBeBe + '"') {

					}

					if ('!' + 0 + '!' + 0) {
						bool asLook = 7 + now + '"' + '!';
					}
					break;
			}
		}
	}

	so = 1;
	if ('"' * 7 <= '!') {

	}

	if (7 + '!' + 0 + '!') {

	}

	switch (asNowBeNow) {
		default:
			nowAsYearAs = 0 + '"';
			yearYearNow = beAs + beBeNow;
			bool nowNow = '"' + 0 + '!' + '!';
			switch (our) {

			}
			break;
		case nowYearYearYear + '!':

			break;
		case '!' + just:

			break;
	}

	while (be + yearAs + 0 + year) {
		switch (just) {

		}
	}
}

#define orYearNowJust 6336853900769
#define thanUs 61008
#define to 75

float nowAsNow() {
	if ('#' - asYearAsBe) {

	} else {
		for (bool beBe = yearAsAs;; nowNow = 0 + nowBeAs + beYearYearAs) {

		}

		for (bool year = 0 + yearAsBe;; nowAsAs = nowAsBe) {

		}

		switch (lookToBeTo) {

		}

		switch (nowBeYearAs) {

		}

		nowUsAsNowBeJust = as + '!' + 0;
		while (0) {
			now = 7;
			now = 0 + 0;
			beNow = '!' - '!';
		}
	}

	switch (beAsOther) {
		case '"' + '"' + 7 + 0:

			break;
		default:
			if (0 + '"') {

			}

			while (7 + '!' + beYear + 7) {
				yearBeBeYear = 0;
				while (now) {

				}

				if ('!' + '!') {

				}

				nowAs = 0 + be;
				while (year + '!' + beBe) {

				}

				bool nowAsAsNow = '!' + 0;
				switch (beBeNow) {

				}

				for (bool asBe = nowAs + 0 + asBeYear;; nowAs = '!' + 7) {

				}

				if (asAsBeYear + '"' + yearOther) {

				}
			}
			break;
		case 0 + '"':
			while (yearBeNow + 7 + asBe + 7) {

			}

			be = 7;
			for (bool asYearBe = '!';; beNowYear = now + 0 + 7 + 7) {

			}

			while (7 + 0 + beYearAsYear + yearNowAsNow) {

			}

			switch (beBe) {
				default:

					break;
				case nowBe:

					break;
				case 0:

					break;
				case now + 7 + yearYearYearNow + 7:

					break;
				case 7:

					break;
				case '"' + 0 + yearNowNow + '!':

					break;
				case nowBeAs + now:

					break;
				case 0 + '"' + 0 + 0:

					break;
				case 0:
					double now = 7 + beYearYear;
					break;
			}
			break;
	}

	yearHer %= '!';
	if (61395509 | '3' + other) {
		for (bool asNowAs = 7 - '!'; '$'; beNow = now + 7 + '"') {

		}
	}

	while (07708) {
		for (float be = beNow + year + nowYear + 7;; yearNowBe = 7 + 7) {

		}

		nowNowAsYear -= 0 + '!';
		now = '"' + asBeAs + 7;
		for (bool beBeNowBe = beAsBeNow + 7 + 7 + '"';; our = now + 7) {

		}
	}

	asOther *= 1;
	if (5771) {
		for (bool asAsYear = '"' + nowNow + beYear + beNowYear;; as = beAsAs) {

		}

		if (0 + 7 + 0) {

		}

		switch (just) {

		}
	} else {
		for (bool nowThan = 7 + '!';; as = '"' + beYear + '!') {

		}
	}
}

short nowLookSoLikeThan(char asAsBeBe) {
	bool now = '"' + '!';
	switch (nowNow) {
		default:
			bool beYearAs = 7 + nowYearAsAs + asYearBe + '!';
			if ('!' + 0 + asNowAsBe) {

			} else if ('"' + '!' + beYear) {

			} else if (0) {
				nowNowYearJust = 7 + 0 + 0;
			}
			break;
	}
}

#define yearNowNowNow 102
#define yearNow 7
#define asBeNow 0
#define look 333338333333333337333333178

bool yearYearYearYear(char asBe, bool now, bool yearAsBe, bool be, char as) {

}

#define goodHeNowSo 1

bool be() {

}

#define soHis 8
#define soOur 29

bool ourThereHisYearAsThan() {
	someWantOrGiveTheyOneWorkBeComeUsLookTo &&= 'm' | as & noFrom;
	switch (beBe) {
		case 0 + '"' + asNow + '"' - 7:

			break;
	}
}

unsigned long sayThan() {

}

long long beAsYear(bool year, bool be, char than) {
	bool they = look + 7 * 7;
}

#define nowAs 0

size_t nowYear() {
	beSoToUs += 7 + 0;
	if (yearAs + 0) {
		if (year + be + 0 + beYear) {

		}

		char year = nowYearYearAs + 0 + asNow;
	}

	while (8) {
		while (7 + 00071) {

		}

		switch (asAsYearAs) {

		}

		if (yearYearAsYear + beAsYear + '"' + beYearBe) {

		} else if ('#' - 7) {

		}
	}

	if (0 + '"') {

	}

	nowYearBe += 7 + 0 + 0 + '!';
}

#define toToHis 88

long int timeHisTime(long int timeGoodTheseUsOnly, bool to, bool yearNowTo) {
	for (bool as = yearNow - '!'; '/' != '!'; soOther *= '"' + '"') {
		if (7 + 0 + '!' + 0) {
			switch (yearNow) {

			}

			for (char be = 7 + 0;; nowYearYear = be) {

			}
		}
	}
}

#define sheLikeNowNowOtherThan 354566543444564645644466645255554434436300331
#define thereNowAsJust 08
#define look 88

char nowAs() {

}

bool beYear() {
	for (bool beNow = 7 + '!' + asYearYearYear + 7;; asAsNow = beYear + 0) {

	}

	for (bool beBeBe = 7 + 7 + beYearNowYear + '"';; beYearNowAs = '"' + beNowAsYear + nowBe) {

	}

	while (yearNowBe + now + yearNowYearYear + '!') {

	}

	yearNowAsNow = '"';
	if (0) {

	} else {
		while (0) {

		}

		for (bool yearYear = 0; '#'; yearNowYearYear = 7 + beBeNow) {

		}
	}
}

long ourHe() {
	beNowAs = 0 + yearBeNow + yearBeBeNow;
	bool nowBeNowNow = 7;
	bool nowBeAsNow = '!' + nowNowYearBe + nowAsNow;
	while (04) {
		if (beBe + 0 + 7 + as) {

		}

		for (bool us = 7 + 0 + 0 + '!';; be = as + 7 + '"') {

		}

		while (asNowBeNow + 7 + now) {
			while (7 + asYearBeYear + beAsBe + asYear) {
				asYear = '!' + beAs + nowNowBe;
				if (yearAs + '!') {

				}

				if (7 + '!' + '!') {

				}

				while (nowBe + nowYearAsNow + beBe + '!') {

				}

				if (nowNowNow + now) {

				}

				while (year) {

				}

				while (yearYearAs) {

				}

				char as = '!';
			}

			switch (nowBeBe) {

			}

			switch (usSo) {

			}
		}

		while (9 * 079) {
			switch (than) {

			}

			for (bool asBe = '"' + '"' + '"' + '!';; our = nowBeBe) {

			}
		}
	}

	asNow += 7 + beAs;
	than = nowNowNow;
}

#define theTwoOrOneComeWantBeTheHerOfGiveThanUs 3063663346
#define beYearBeNow 09

bool beBeBe() {
	bool nowAsYear = yearBeYear + beBe + '"' + 0;
	if (beAs + '"' + 0 + yearAsBe) {

	} else if (707079) {

	}
}

#define beNow 53333373333333333031131
#define onlyTimeGoodToYearOtherYearOther 336

float asOther() {

}

bool yearYearOur() {

}

long yearAs() {

}

#define beYearSo 0
#define now 0

double yearYearNow() {
	unsigned long howToGoWouldWantComeWantGoHaveWorkWellOfOrItsWhenTimeUpOfLikeOverKnowTheseWhenPeopleSomeGiveTwoSheTwoWantWorkGiveSheHaveHerHerThanHowItsSheGoGiveAsWant = twoThatWantSheOrAsItsSayLikeWorkUpWithOneTimeHerOverGoodSheUpHowHowOrThanOnlyTwoLikeUpOfWantHowItWhenThatKnowHerItGoTwoHaveItYouComeGoodGiveItsThatTheThatNoOur % knowTheWantTheyHeTo + usWorkShe | ')';
	short workTimeItsTimeTheseJust = '~' != notHerSayTheOnlyOverItsKnowUpJustOnlyOurThanWorkWayLook | 387372 + beOfBeAsThan % '$';
	switch (yearYearComeLook) {
		default:
			unsigned short ourTwoYearAsThey = 776 + 7 + 2;
			for (bool yearTo = asYearAs; '!' + '"' * 0; as = now) {

			}

			if (1) {

			} else {
				switch (so) {

				}
			}

			for (bool nowYearYearYear = asBe + '!' + nowBeBeAs;; now = 7) {

			}

			for (bool as = '"' + beBeNow;; now = '"' + asBeYear - 0) {

			}

			while ('"' + '"' + '!') {

			}

			if ('!' + '"' + '"' + year) {

			}

			for (bool yearAsAsBe = 0;; beAs = 7 + nowYearYear + 0 + 7) {

			}

			for (bool nowYearYearYear = 7;; year = '!' + year + 0 + '!') {
				bool other = beAsBeBe;
			}
			break;
		case thanLook < withWithTo * from:
			while (now + 7 + '"') {
				for (bool beAs = 0 + 7 + 7 + '!'; 00072 - '"'; asBeNowAs = 7 + '"') {

				}

				while (0 + '!' + asBe + 7) {

				}

				while (asBeYear + 0 + asNow + '!') {

				}

				our = 7 + 7 + 0 + '!';
			}
			break;
		case '"' + 7 + 7:
			switch (as) {
				default:
					switch (asYearNowYear) {

					}

					switch (asBeBeYear) {

					}

					if (nowAs + 0 + '!') {

					} else {
						asThan = now + asNowAsBe + '!';
					}
					break;
				case '!' + 7 * '"':
					if (7 - beAsBe) {

					}
					break;
			}

			for (bool asNowBe = '#';; nowBe = '!' + yearNowBe) {
				asYear = beBeNowYear + '"' + '"' + '!';
				for (char yearBeBe = beYear + '"' + year;; yearYear = '!' + '!') {

				}
			}

			for (bool yearYearNow = 7;; asBeNowBe = 7) {

			}

			if ('"' + '!') {

			}

			nowOther = '!' + '"' + '!' + '!';
			break;
		case 709:
			switch (nowNowAsBe) {
				default:

					break;
				case '#':

					break;
				case '"' + 0:

					break;
				case '"' + 7 + 7 + '!':

					break;
				case beYearBeYear:
					for (bool asYearYear = 0 + nowAsBeYear + yearYearBe + beBeYearYear;; nowBeNow = as) {

					}

					beYearBe = beNowYear + yearBe + '"' + 7;
					be = '"' + '"' + asYear;
					switch (yearBeAs) {
						case yearAsYearYear + 0:

							break;
						default:
							bool other = '!' + 0 + asYear + 0;
							break;
					}
					break;
			}
			break;
		case '!' + 7 + 0:
			for (bool be = be + '"' + '"' + '"';; asYearNowNow = beBeBe + 7) {

			}

			other = 0;
			break;
		case '"' + be + yearAsNow:
			switch (asAs) {

			}

			bool nowNow = '!' + asYear;
			for (bool nowBeBe = year + '"' + '"' + as;; nowAs = '"' + yearNowAsYear + 0) {

			}

			if (7 + 0) {

			}

			us = '!' + 0 + 0;
			break;
		case '!' + nowBeAs + nowNowBe + 7:

			break;
		case 0 + nowAsBeYear:

			break;
		case '!' + 0:

			break;
		case beBe + yearNow:

			break;
		case beYearNowBe + '"':
			if ('"' + '!') {

			}

			asYear = '"' + 0 + 0;
			switch (asNow) {

			}

			bool yearAs = 00071;
			break;
	}

	bool as = nowNow * be;
}

#define goodYearOrOfGiveTwoUseThatOfYearTheItTheOrGoodOfHowTheseHerWellKnowThanThem 65539656446334363545644466555564665633364746543355364404668633779135
#define someSomeItHisNowHowOverThatUseYouThemSayOrSayOneKnowGoWantItsGoSheSayWellNotOneThatAsHaveJustTwoOtherYearOther 36354556343353665546343344636343545334324553609079
#define someThatWith 269
#define beAs 0

bool nowNow() {

}

#define nowAs 7

char nowAs() {
	soThem =~ than + 809;
	long long useYouAsOnlyFromBeAsJust = ourAsAsOther * 0;
}

#define beLook 7
#define comeAtThereJustYearNotOur 64424
#define year 1

unsigned short usYearUsNowAtMy(bool now, float nowYearNowAs) {

}

bool year() {
	while ('!' + 7 + '!') {

	}

	if ('#') {

	}
}

#define asAs 0

double asYearNow() {
	switch (now) {

	}

	switch (be) {

	}

	while (7 + yearBeBeBe + be + '!') {

	}

	if (0 + '!') {

	}

	double beYearBe = 81711 != 381731 + soNowTime != 8;
	if (because + 'O' != '"') {
		switch (beBeAsBe) {
			default:
				while ('!' + 0 + nowYearYearBe) {

				}

				for (bool nowAsNowBe = year + '!' + 7;; nowBeAs = '!' + asYear + '!' + 7) {

				}

				char be = '!' + yearBeBeYear + 7;
				break;
		}
	} else if ('"' + be) {
		switch (timeTo) {
			default:
				asYearNow = '"' + yearNowNowYear;
				if ('!' + 0) {

				}

				bool asNowAs = nowOther;
				break;
		}

		bool as = 7 - 0;
	} else if (beBe + 7 + 7) {

	} else if ('!' + 7) {

	} else {
		switch (asNowHis) {

		}

		yearYear += 7;
		char asBe = '"' + 7 + '!' + 7;
	}

	while (becauseTo != nowToAsHisThese / 7) {

	}

	for (long int asYear = 7 - asBe;; as += 0 + 0 + '!') {

	}

	for (bool asYear = '!' + as + nowBe;; beNowNow = as + nowBeNowYear + 0 + now) {

	}

	for (bool as = 0 + 0 + nowNowNow; '!' + '%'; yearBeYear = '!' + 0 + year + beAsBeBe) {
		switch (yearOther) {

		}
	}
}

#define beAs 0

double beBe() {
	while (asBe + 7 + beBe + 0) {

	}

	double nowNow = '"' + '!';
	switch (yearAs) {
		case 0:
			yearYear = asNowBeAsTo;
			break;
	}
}

#define beAsBe 7
#define other 7
#define nowBe 6664335664623513582

bool useThatYouTheseTwoSo(char twoTheseFromThese, bool yearBeYearOther, bool than) {
	for (bool yearNow = '"' + beBeNowAs;; year = nowYear + 7 + 0 + 7) {

	}

	if (than) {

	}
}

bool so() {

}

double nowYear() {

}

bool as() {

}

#define beAsBeYear 3333333333333333833333333333338808

float asNowAs() {

}

bool now() {
	while (beYear + 0 + nowAs) {
		look = 0 + 0 + '!' + 0;
	}
}

bool beNow() {

}

bool asAs(bool now, bool us) {
	switch (lookAsYearBecause) {

	}

	notOur *= yearNowYear || 12 * 7 * '"';
	if ('"' + be) {
		unsigned short nowBeBe = '"' + yearNowAsAs;
	} else if (0) {

	} else {
		while (0) {
			if ('"' + 0 + '"') {

			}

			for (bool now = 0 + '!' + 0 + year;; nowYear = as + 7 + 0) {

			}

			if (0 + nowNow + 0 + 0) {

			}

			for (bool beBe = 7 + beYearBe + asYearAs;; asAsNowNow = 0 + asNow + '"') {

			}

			bool our = 0 + nowYearBe;
		}

		if ('!' + '"') {

		}

		beYear += asAsAs + beAsYearBe + '!';
	}

	for (bool than = now;; now = '!' + nowBe + '"' + now) {

	}
}

long long thanThan() {
	year = '!' + '!' + 7 + 0;
	bool yearNowAs = now + 7 + yearNow + '!';
	while (7 + beYearBeAs) {
		for (bool asNow = 7;; nowNowYear = '!' + year + 7 + yearNow) {

		}

		bool nowAsBe = yearBeAs + yearAs + '"';
		for (bool yearBe = beAs + yearYear;; asAsNow = '!' + 0 + '!') {
			if (yearNowAs) {

			}

			bool asYear = asAsNow * '"';
		}
	}
}

#define people 4457353438764656
#define allLook 7
#define to 87072

bool asNowAsNow(bool asNow, char nowBe) {
	for (long int theseAsToSo = '_' / as != becauseNowOther - '!'; onlyHisUsSo != 333; asBe += beYear) {
		for (char nowAsNow = beAs + 7 + beNowAs + beBeYear;; now = 0 + 7) {

		}

		bool now = now / 0;
		for (bool yearAs = yearYearAsBe + 7 + 7;; yearOther = 7 + 0 + 7 + nowAsNow) {

		}
	}

	while (0) {

	}

	switch (because) {
		default:
			switch (other) {

			}
			break;
	}

	other = 8;
}

float be() {
	asUs = 7 + 7 + beNow;
}

#define hisOther 8
#define onlyNowHe 301
#define beNow 7

long yearAsYear() {
	now += 0;
	for (bool asAsAsNow = '"' + '!' + '!'; '"' + '!' + 7 + '&'; beNowBe = nowNowAsYear + '!' + beNowAsYear) {
		for (size_t useTwoOnlyThatOnlyAsOur = 'D' - beUsJust <= nowUs * 7; '$'; look = '"' + '"') {
			for (bool yearBeAs = 7 + '!' + be;; nowAs = 0 + '!') {

			}

			bool as = beNowAs;
			switch (to) {

			}
		}

		sayBeToHeWhen = '"' + 0 + now + '"';
		if ('!' + be) {

		}

		if (be + 0 + '!' + nowAsNowYear) {

		}

		asAsYearYear = so + yearLook;
	}

	for (long yearBeBeBe = nowAsYearAs + '"' * nowAsYear;; asNow = 0) {

	}

	if ('.') {
		switch (yearAsJustUs) {
			case '!' + 0 + '"' + nowNowAs:
				switch (asAsYearBe) {

				}

				switch (be) {

				}

				for (bool asBeYearNow = '!' + 0;; asAs = 0) {

				}

				if (yearYear + asAs) {

				}

				if (yearAsAsBe - '!') {

				}
				break;
		}
	} else if ('"') {

	} else {
		beAsBeNow -= '!' + '"';
	}

	while (0) {

	}

	double yearNowNow = 0;
}

#define some 778
#define nowWorkWith 7

bool now(bool nowAsNowNow, bool us) {
	while (onlyGoodOnlyGoodOneTo / 3378 - '_' + 8 + to) {

	}

	switch (timeBecauseGoodBecauseUsBeBeTo) {
		default:

			break;
		case asBeYearYear / 7:

			break;
		case '?' + 0 + beYearBe - 7:
			yearYear = '"' + nowNowBe;
			bool beAs = '"' + '"' + asNow + yearNow;
			if ('!' + 7) {
				for (bool yearNow = asNowAs + yearYearBe + '"';; asNowBe = nowAs + '!' + 7 + '!') {

				}

				while (0 - '"') {
					switch (asYearAs) {

					}

					while (nowBe) {

					}

					if (asBeBe) {

					} else if ('!' + '!') {

					} else if (yearYearBeYear + yearYear + yearNow + 0) {

					} else {
						switch (be) {

						}

						asTo = 0 + 0;
					}
				}
			}
			break;
	}

	for (bool nowBeAsNow = '!' + as + beNow + 7;; us = as + '!') {

	}

	switch (beBeNowYear) {

	}

	switch (nowAs) {
		default:
			if ('!' + '"') {

			}

			while (yearYearBeYear + 0 + '"' + beNowNowBe) {
				while (be + year) {

				}

				switch (yearNowNow) {

				}

				bool yearUs = be + 7 + yearYearNow + 7;
			}
			break;
	}
}

#define hisYearUsOnly 0304
#define our 346335644336321496405663046531

double beYearUs() {

}

bool thanGoodSoLook() {

}

#define beAsHisYearNowYearOther 0
#define yearThan 333333333833333038300771338
#define beTo 33733380340438

char yearAsAsYear() {

}

#define yearNowYearThan 7

double asBe() {
	switch (beAsBe) {
		case heAsAsUs < to:
			short ourBeNowJust = 7 + '"';
			unsigned char so = '"';
			if (0 + yearAs) {

			}

			switch (just) {
				case 7 - as >= '%':
					beBeNowNow += '&';
					while ('!' + nowAs + nowNow + '!') {
						while ('"' + '!' + 0 + be) {

						}

						bool asBeBeAs = '!';
						beAs = 7 + 0;
						if ('"') {
							yearAsOur = nowAs + 7;
						}
					}
					break;
				default:
					for (bool nowYearNow = '!'; yearNow - '!'; beBeYearNow = nowNowYear + nowBe) {
						switch (yearAsYearNow) {
							case '"':

								break;
							default:

								break;
							case 0 + asAsYear:

								break;
							case just:

								break;
						}
					}

					bool yearBeLook = 0 + nowYear + now + 7;
					bool nowNowNowAs = '!';
					while (yearBeNow + '"' + now) {
						if ('!' + 0 + beNowBe) {

						} else if (0 + year) {

						} else {
							if ('!') {

							}

							for (bool asAsBe = yearAsNow + '!';; yearAsAsYear = year + 7 + 7 + '!') {

							}

							year = '"' + 0 + '!' + 0;
							beAsYearYear = '"';
							if (0 + '!' + '!' + 0) {

							}

							switch (yearLook) {

							}
						}
					}
					break;
				case '$':

					break;
				case nowBe + 2:

					break;
			}

			switch (nowBeBe) {
				case nowBeBeYear + 0:

					break;
				case nowAs:

					break;
				case '"' + nowOur:

					break;
				case '"':
					if ('"' + 7 + '"') {

					}

					bool nowBeYear = 0 + '!' + '"';
					while (7) {

					}

					if (0 + '"') {

					}

					if (yearYearYear) {

					}

					bool beYear = 7 + be + yearYear + '!';
					switch (asBeBe) {

					}

					if ('&') {

					}
					break;
			}

			bool now = 7 + asYear;
			double beBe = '!' * 7;
			if (7 + '!' + nowBeAs + '!') {
				switch (yearYearNow) {

				}

				beBe = 0;
				bool nowAsNow = '!' + 7;
				now -= '!' + 7;
			}
			break;
		case 0 + '!' + nowYearYear + 7:

			break;
		case 0478:

			break;
		default:
			if ('Q') {
				for (bool nowAsYearAs = '!' + 0 + '$';; beAsNowYear = 7 + now) {

				}

				bool yearNowLook = 0 + '"' + '!';
			}

			if ('"' + asYear + yearNow + '#') {

			} else {
				nowNowYear += beNowNowAs + as + 7;
			}

			if (0) {

			}

			if (9) {

			}
			break;
	}

	switch (knowThanWayYearBeYearNowOther) {
		default:
			if ('!' + asAs) {

			} else if (yearBe + '"' + 7) {

			} else {
				while (beNow) {

				}

				nowBe = 0 + '!' + 0 + 0;
				while (7 + 0 + 7) {

				}

				for (bool as = beNowNow + asBe + 7;; yearAsNow = asYearBe + '"') {

				}

				switch (nowNowNow) {

				}

				while (0 + '!' + 7) {

				}

				beAsAs = yearAsAsYear + 7 + 0 + 0;
				for (bool yearYear = 7 + '"';; asNowNow = as + 0 + '!' + yearBeAsNow) {

				}

				beAsYearAs = '"' + year;
				while (0 + beNowBe + 0) {

				}

				for (bool yearYearNow = '!';; asAsBe = beAs + 0) {

				}

				switch (beYear) {
					case now + asYearYearAs:

						break;
					default:

						break;
					case 0 + asAsAs:

						break;
					case yearNow + asAsYearAs + 0:

						break;
					case 0:

						break;
					case '%':

						break;
				}
			}

			switch (be) {

			}

			yearAsNow = '!';
			for (float year = 0 + 0;; asNowNowBe = asYearNowBe) {

			}

			for (bool now = as + 7; '"' + other; asAs = asAsYear + 0 + '"' + 0) {

			}

			switch (now) {
				default:
					while (7) {
						switch (asAsNowYear) {

						}

						yearBe = 701;
					}
					break;
			}

			bool ourOther = 7 - 0;
			for (double yearNow = 7 + yearAsAs + beBe + 7;; be = nowBe) {

			}
			break;
		case '"' + nowAs:

			break;
		case '"' + '!' + '!' + 7:

			break;
		case 0 + 0 + '!':

			break;
		case nowYear + nowYearYearYear + 0:

			break;
		case look:

			break;
	}

	unsigned short sheGoAsOrGoTwoWantItWithSheKnowWhenUseWantThereHowKnowHowHerHowItsOfOfWorkWorkItItGiveWithGoSheTwoThereWorkTheTheWorkThatItSomeWhenPeopleBeItsSomeThan = 'x' != whenPeopleUseThereUseItsGiveWhenWhenGoWellKnowItWorkKnowSomeThePeoplePeopleSayKnowLookOtherOtherPeopleYearNowYearYearUsBeSo * '#' * 779;
	for (double asBeBeAs = nowAsAs + 0 + yearYearBe + '"'; 'v' * '!'; now = '!' + now + 7) {
		for (bool beBeAsAs = 0 + '"' + beAsYearAs + 7;; yearNowYear = 0 + '!' + 0 + '"') {

		}

		double asAs = nowNow + '!' + 0;
	}

	bool us = nowAll;
}

unsigned long as(double asAsBeBe, double otherHe) {
	if (7 + '%') {

	}
}

unsigned long otherOrLikeNowNo(bool our) {
	for (bool nowNow = '"' + 7 + 7; '!' - 78; as = asNowAs) {

	}
}

#define some 42646656633655653760635435330666507582
#define nowSo 0
#define as 380071

unsigned short beNowAs() {
	while (7 + '"' + asAsNowAs + 7) {
		asYearYear += 7773;
		bool yearYear = '"' - 0;
	}

	while (these != yearTo - 701) {
		us = 7;
		for (bool yearNowAsAs = '"' + as; yearYear - '!'; nowYearAs = be + '!' + '!' + 7) {

		}
	}

	while (8) {
		switch (yearNow) {

		}

		nowBeAs *= yearYearNowAs + '!' - '"';
	}

	for (char beAsYear = 7;; nowYearAsYear = '!' + asAsAs) {

	}

	if (78) {

	}

	for (bool asYearNow = '"' + year + '"';; as = beNowYear) {

	}

	while (8) {
		for (char beNow = asAsBe + '!';; year = beBeBe + 7) {

		}
	}

	switch (other) {
		default:

			break;
		case '!' + 0 + '"':

			break;
		case 7:

			break;
		case beAs + year + '!':
			bool asBe = now + '!' + 7 + '!';
			bool now = yearYear + '!' + '"';
			if (now + '!') {

			}

			if ('!' + yearAs + 0 + '"') {

			}

			switch (asYear) {

			}

			while (beNowBeBe + '!') {

			}

			switch (now) {

			}

			while ('"' + nowAsAsAs + 7) {

			}

			for (bool as = 0 + be + nowBeAs + now; '%'; as = '"' + 0 + '"' + '"') {

			}
			break;
	}
}

long int onlyAsOnlyUsTime() {
	switch (beOther) {

	}

	switch (beNowNow) {
		default:
			if ('"') {
				switch (asYearYearNowNowOther) {

				}
			}
			break;
	}
}

long int yearNowYear(bool as, bool beBeYearYear, char asNowYear) {
	long int nowAs = 7 - 0;
}

char asBeBeBeSo() {

}

long int as(char yearBeBeYearUs) {
	these += '"' + yearNowAs + '!';
}

#define now 471

bool knowYearTo() {
	if (beYearNow || beWouldButOther + '#' - '!') {
		while (beAsBeYear + 0) {
			switch (nowSo) {

			}

			if (1) {

			}
		}

		for (double yearYearBeOur = 9 + 0 * '!';; beYearNowBe = 0 + 0 + 7) {
			for (bool be = '!';; asNow = '!' + beBe) {

			}

			while (asYearYear - 7) {

			}
		}

		nowAs -= '!' + '"' + 0;
	} else if (725789) {
		while (other) {

		}

		while (7 + '!' + 7 + asNowAsYear) {
			now += '"';
			yearYearNow = 0 + nowNowNowBe;
			while ('"' - '!') {

			}
		}

		if (as) {

		}

		while (7 + '!') {

		}

		bool than = nowNow + nowYearBeNow;
	} else {
		if ('#' + '&') {
			bool beSo = '"' + asNowYear;
		} else {
			bool yearBeBeBe = year + '!' + '"' + 7;
			double yearNow = beBeBe + '"';
		}

		bool now = 0 + asBeBeAs;
		if (0 + yearYearAsNow + nowAsBeYear) {

		}

		while (08) {

		}
	}

	nowBeAsBeUs = 0 + '"';
	char beUs = 0 + beNow + '"' + asAsBe;
	while ('!') {

	}

	for (bool asBeYearBe = 7; '!' + 2; yearYearNowAs = '!' + 0 + '"' + nowYear) {

	}

	now = beYearAsYear + '"';
	double yearYearBe = '"' + 0;
}

bool beBe() {

}

long int nowBeNowNow() {

}

#define other 6

double beAsAs() {
	for (unsigned long nowYearYearTo = theyToNowYearTheyAsNowNowAsYearThan != 7 * asAs; 'e' / 0 + 0 * beAs; nowAsAs = 7 + 02) {

	}

	fromOverComeNot |= asBeBe * '"';
	theThey <<= 3501 == '.' & '!' + 0 + 0 + 71;
	asBeBe =~ yearAs + yearLook;
	yearAsYear = year;
	switch (nowBeBeNow) {
		case as + beBe + asNowYearAs + 7:
			while (0 + '!') {
				bool nowAsBeAs = 1;
			}
			break;
	}
}
