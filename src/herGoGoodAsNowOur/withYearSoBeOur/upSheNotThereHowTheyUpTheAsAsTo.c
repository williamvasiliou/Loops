long int goodOneGoodGoodGoodGoodBeOneGoodOnlyGoodBecauseHisTo(bool to, bool beAsBecause, float beNowHe, char beAs, bool be, bool asNow, bool otherAsAsTo, bool to) {
	if ('"') {
		char so = beSo;
		nowNowAs = '"' + '!';
		switch (asNowOther) {
			case 7 + '!' - 0:

				break;
		}
	} else {
		so = yearNow + 7 + '!' + '"';
	}

	switch (asYearAsAs) {
		case asBeAsYear + yearYearAsBe + '"' + be:

			break;
		default:
			for (bool year = 0 + '!';; asNow = 0 + 7) {

			}

			beYearNow += beYearBe + nowAs + asYearBe;
			if ('!' + '"' + '"' + asYear) {
				beNow = 0 - 0;
			}
			break;
	}

	double look = 7;
}

short beTo(double nowBeYearBe) {
	if (lookSayHaveSheAsUsSomeKnowYearOur + '#') {
		while (7 + '!' + beYearBe) {
			switch (nowNowYear) {

			}

			bool yearYear = '!' + 7 + 7 + 7;
			for (bool beYearNow = asAsNow * '!';; yearNowBe = 0 + nowAsYearBe + 7) {

			}
		}
	} else {
		beYearNow = year + 7 + 7;
		than = 0 + '!' + 7;
	}

	while ('q' || 0 >= 03 + '>' && '&' >= ourOnlyOur * 6677079 / '!' * 7) {
		for (bool as = asNowBeNow + '!' + '!'; yearNowBeNow + 9 + 707077002; nowBeNowBe = '"') {

		}

		while (yearNowNowBe + asLook) {
			switch (beYear) {

			}

			switch (as) {

			}

			bool as = 7 + asBeAsNow + yearAsAs;
			switch (nowTo) {

			}
		}
	}

	for (char nowBe = yearAsBe; 1 * yearAs; work = beBe + '"' + year + '!') {

	}
}

size_t orThese(bool nowAsAsAs, long int of, char beNowNowNow, bool beYearUs) {
	if ('\'' + 9) {

	}

	while (yearAs * '!') {

	}
}

bool ourTo() {
	char yearJustYearThan = '"' * '!';
}

unsigned short lookSoJust(char beYearAsBe) {

}

bool nowOther() {

}

bool asBeBeNow() {

}

bool with(bool be, bool just) {
	if (yearNowYearBe + '!' + '!') {

	}

	nowThan -= 0 + 7 + '"' + 7;
	yearBeYearAs = '!' + beBeYear + '"';
	while (0 - now) {

	}
}

int nowUs() {
	switch (beYear) {
		default:
			asThanAsOther = asYearYear + asBeAs + '!';
			break;
	}
}

#define useYouHowHerHerAsSomeWayYearUsTo 3556333333166435353018
#define oneGoodGoodNowBeBeAsYearBeBeUs 33388
#define usUs 0

long int timeUsTimeOther(bool beBeNowAs, bool beAsYear, bool nowNowAs, bool so) {
	timeGoodHis = nowNow != '\'' + '+';
	for (bool yearBe = as; beOther; year = 7 + yearAs + '!' + '"') {

	}

	if (nowYearSo) {

	}
}

#define beBeYearSoNowBeYearLookBeBeUs 047609
#define yearBeYear 9

bool asYearNow(bool as, bool asNow, bool yearNowAsAs, bool be, bool yearYearNowYear, char nowYear) {
	unsigned int orWhenThatSomeHaveSomeItsTheSayComeOrItsYouAsOfThatGoSomeGivePeopleItsSomeItsYouKnowSomeWantTwoHowWorkTwoHowThatWellNoUseSomeHaveHowGoodJustPeoplePeopleSoSome = workGoOtherTheOver && '#' * asOther || fromHowHaveMyJustJustThat + as - 0;
	beAsHow .= 'p' & 162781 && 62602 || 2 != asWould | '"' + '+';
	while ('4') {
		if ('"') {
			while (as + '!' + 0) {

			}

			switch (beYearNow) {

			}

			switch (beNowBeAs) {

			}

			for (char be = yearYear + '!' + year;; beBeYearYear = '"' + '!' + now) {

			}
		} else if (usUs) {
			while ('!' + 7) {
				us = 7 + '!';
			}
		}
	}

	long than = asAs + nowAs + asAs;
	while (yearBe + 7 + 7) {

	}

	bool as = be;
	switch (asNow) {

	}

	than = now + as + 0;
}

size_t knowHowWorkNowOurYearYearYearOur(size_t year) {
	switch (beYearBe) {
		default:
			asNowYearAs = just;
			break;
	}
}

double overToGoYearAsNowBeOf(unsigned char knowOrNowAsAsNowYearOther, bool someSayThemUsNowYearYearThan, bool nowLook) {
	if (0 + 7 + beOur) {
		if (7 + '!' + '"' + 7) {
			for (bool beBe = asNowAsNow;; nowYearYearAs = yearAs + year) {

			}

			bool now = 0;
			if (0 + 0 + 7) {

			}

			switch (now) {
				case '!' + 8:

					break;
			}
		}
	}

	while (7 + yearNowOur) {
		switch (be) {
			default:
				if (beOther) {

				}
				break;
		}
	}
}

size_t ourWayUsTwoYearWant(bool asFrom, long soNot, bool just) {

}

#define likeLikeTwoAtLookYearJust 361

bool year() {

}

bool beBeAsBe() {

}

bool beBeNowYear() {

}

#define nowYearJust 506357344
#define twoWay 34455422

unsigned long itsUsTheyUsAsYearAsAsJust(double as, bool thanJust) {
	bool yearYear = '!' + 7;
	be = asBeNow;
	bool beBeYear = beNowBe + 0;
	double yearBe = beBe + '"' + '"';
}

int notYearAsNowNowToOther() {
	if ('!' + year + 7 + '!') {

	} else {
		look -= 0 + 9;
		if (yearNowAsTo) {

		}
	}

	while ('!' - 92) {
		beYearUs = '"' + 0 + 7;
	}

	for (bool year = 0 + 0 + '!' + beNow;; asBeBeNow = now + 0 + 0 + '!') {

	}

	for (char yearBeYear = 0 + '!';; yearBeAsBe = '"' + yearYearYearAs) {

	}
}

float beUs() {
	float to = 000708 - '!';
	if ('?') {
		while (so - as) {
			if (1 != 0) {
				if (7 + '!' + 0) {

				} else if ('"' + '"' + as + asBeAsAs) {

				} else {
					while (0 + 7 + '"') {
						while (7 + beBeBe) {
							switch (nowYear) {

							}

							asNowBeYear = 1;
						}

						switch (us) {

						}
					}
				}

				while (7 - 0) {
					if (7) {

					}

					other = '"';
				}
			}

			long int asBeBe = 0 + yearAs + 7 + '"';
			while (yearAsYear) {

			}

			if ('#' / be) {

			} else {
				for (bool yearUs = be + '!';; asYearYearNow = be) {

				}
			}

			switch (he) {
				case be + '"' + asNowBeBe:

					break;
				default:
					while ('"' + '"' + 7 + 0) {
						if (so) {

						}
					}
					break;
				case 701:

					break;
			}
		}
	} else if (131373) {

	} else {
		while ('"' + beAs + 0) {

		}

		for (bool now = '"' + beNowNowNow + '"' + nowYear;; asBeBe = 0 + year + '"') {

		}

		for (char yearBe = asBeAs + 7 + 0;; yearNowNowNow = 7) {

		}

		if ('"' + '!' + 0 + 0) {

		}

		switch (nowBe) {

		}

		char be = 0 + '"' + 0 + '!';
	}

	bool beNowNow = '"' + '"' + be;
	now = '"' + '"' + '!';
	char yearNowYearAs = '!' + '!' + 0;
	switch (so) {

	}
}

bool nowNow(long int to, bool beYearAs, float as) {
	if (beAs + 7 + '"' + year) {

	}

	switch (nowNowNow) {

	}

	for (bool year = '!';; beAsAsYear = beNow + 0 + '!') {

	}

	if (0 + 7) {
		now += '!' + 0;
		char nowNowYearAs = '!' + 0 + '!' + beAsYear;
	}
}

bool asAsAsGoodNowBeThan() {

}

#define nowNowYearAs 27071
#define soToNowNowYearThese 705
#define yearYearAs 9

bool yearAs() {

}

bool yearNow() {
	if (beNowYear) {

	}

	while (asNowNow + 0 + 7) {

	}

	for (bool asBeAsYear = 0 + nowYear + now + beAsAs;; yearNow = '"' + 0) {

	}

	if (beNowBeYear + asNow) {

	} else {
		switch (nowBeAs) {

		}

		for (bool beAsBe = '!';; beBeBe = 7 + nowAs + '"' + asYear) {
			for (bool beNow = '"';; nowYearYearBe = '"' + beBeAsYear) {

			}

			if (asAsYearNow + 7 + beBeAsNow + '"') {
				switch (just) {
					case 0 + now + year + nowNowYear:

						break;
					case '!' + beAs + beBeNowYear:
						if ('"' + yearAs + as) {

						}

						while (8) {

						}
						break;
				}
			} else {
				switch (now) {

				}

				switch (look) {

				}

				switch (nowAsAs) {

				}

				for (bool beNowBe = '!' + '!' + 0;; yearBeYearYear = asNowYearAs) {
					beBeAsBeOther = year + 7;
				}

				while (7) {

				}

				asAsNow -= '"' + '!' + '!';
			}

			unsigned long wayOur = '%';
			yearBeAs -= year + '"' + 7 + yearNowYear;
		}

		unsigned short goKnowYouGiveItOverNowGoWantUpOfWhenKnowGiveItItsOnlyHowGoSheHowOtherItOtherItsTheSoAsHave = twoGoodWellGoodKnowGiveTimeItsLikeThereGiveUpWhenThereOneWantOfSomeUpAsOurHaveThereBut & yearNowAsBe * 0 <= beYearNow;
		overItKnowYouOverWellYouUpYearAsItsLikeOrPeopleWhenTimeSheNowHaveComeComeTheLookYearUs ||= timeNotYouItsComeItTwoOfSayOverOrOnlyKnowYouTheComeItWay > 30972 != 430408 <= '-' + 71;
		for (unsigned short whenSomeOrBeOther = asAs + '"' - yearNow; 0 * 72; asAs = 7 + asNowAs + '!') {
			long double nowNowNow = 7 + 0 + '!';
		}

		if (7 - yearNowYearAs) {

		} else {
			while (0 + beAsNow) {

			}

			switch (asYear) {

			}

			bool be = nowAs + beBeYear;
			be -= 0 + yearYear;
		}

		switch (you) {

		}

		if (yearAsNow + yearNowBe + asNowYear) {

		}

		if (0 - 0) {

		} else {
			bool asBe = beBe + beAsBeNow;
			switch (other) {

			}

			asYearNowYear = '"' + 7 + '"' + beBeBe;
			as = '!' + as + yearYear;
			year = 0 + 7 + beBe + '!';
			double asNowBeAs = '"';
		}

		switch (asNowAs) {
			case asYearBeYear * '!':
				for (size_t two = 655456548455729 + our + beYearAsWouldYearOther; us * '(' - '$'; asNow = asYearAsNow + 0 + 7) {

				}

				wellLikeBeNowYearLook |= 'x' + 'c' + 7 + beBeThanBeNowSo;
				if (nowNowNow || 7 * 7) {

				}

				if (be + nowAs + '!') {
					if ('!' - 7) {

					}
				} else if (7 + just) {
					if (nowAs + '"' + '!' + '"') {

					}

					for (bool yearYearNowNow = be + '!' + '!' + 7;; beNowBeNow = 7) {
						bool yearBeNow = '"' + 0;
						switch (asYearAsNow) {

						}

						switch (nowYear) {
							default:

								break;
							case yearNow + yearNowBe + yearNow + 7:
								while (asYearBeBe + '"' + nowAsAsYear + 0) {

								}

								asBe -= '"' + beYear;
								break;
						}
					}
				}

				while (7 + '"' + 7 + '"') {

				}

				if (7 + '!') {

				} else {
					while (as + 0) {

					}

					if ('&') {

					}
				}
				break;
			case '#':
				bool nowYear = '!' + 0 + 7;
				for (bool be = as + '!' + '!';; yearAsYear = as + '"' + '!') {

				}

				double nowAsNowAs = yearBe + '"';
				break;
			case yearUs == '-' + asNowYearBe + '!' - '"':
				bool our = asYearNow;
				if (0 + asBeNowNow + 0071) {

				}

				for (bool asBeBe = 0 + '"' + yearAsNow + '"';; nowAsAs = '!' + nowAs) {

				}

				switch (now) {

				}

				while (beAsBe) {

				}

				yearBe = as + 0;
				switch (nowAsNowYear) {

				}

				for (bool other = 7 + 7;; asBeBeBe = asAsBeYear) {

				}
				break;
		}

		yearBeYearYear = be;
		switch (nowYear) {
			case '%':

				break;
			default:
				long double beNowYearBe = '!' + 0;
				while (be + 0 + '!') {

				}

				bool than = beBeYearAs;
				break;
			case just:
				switch (yearYear) {

				}

				while (7) {

				}

				while (0 + 7) {

				}

				switch (nowYearBeYear) {

				}

				while ('"') {

				}

				beYearYearYear -= asBeYearAs;
				break;
			case nowBe >= '&':

				break;
		}

		yearNow = '!' - '"' * 5233534533434649 != '!' / '"';
		for (unsigned char beHowUpOneOnlySheSomeNowThan = 't' >= 8 - toUs; than + now - beBeAtUs; beNow = 0 + 7 + 7) {
			for (bool beBeYear = '!'; 138 + '"' >= nowNowNow; as = be + 0 + as + '"') {

			}
		}

		switch (toWorkYearAsTheNowAsOther) {

		}

		if (358) {
			while ('!' + '"' + '"') {

			}

			while (7 + 7 + '"') {
				switch (now) {
					case 0 + '"':

						break;
					case yearNowNow + yearNowNowBe + '"':

						break;
					case nowBeBeAs + 0:

						break;
					case 0:
						bool beBe = 0;
						for (bool beNowAsAs = '"' + nowAsYear + beYearNow;; now = now) {

						}

						just = '!';
						break;
				}
			}

			unsigned char asLookTwo = '!' + 0 + asYear;
			asJust = 7 * asBeYear;
		} else {
			bool nowNowNowBe = '"' + '!';
			switch (you) {

			}

			if ('"' + '"') {

			}

			for (double be = '"' + 7;; be = 0 + as + yearBeBeYear) {

			}

			now -= '!' + '!';
			while (thanThan) {
				for (bool beBeNow = 0 * '!';; be = year + '"' + 0 + 7) {

				}

				if (nowYearOur) {

				}
			}
		}

		while ('$') {
			while ('!' + '!') {
				while (asAsSo * 0) {

				}

				while (just) {

				}
			}
		}

		switch (as) {

		}

		for (bool yearYear = '!' + '!' + '"' + '"';; beBeBeYear = 0 + 0 + 0 + 0) {

		}

		bool just = '"' + 7;
		for (int nowBeYear = nowYearBe + yearYear + '"';; asYearAs = 7 + yearAsYearYear + 0 + 7) {

		}
	}

	long long otherTheTheUseHowUpNowOneItsLike = 'q' > 't' != 60373443342725 + justHowOver >= 45644363400055608 + justAsAsUsThan;
	while ('!') {
		if (0 + nowNowNowNow + yearAsAs) {

		}

		if (yearYear + 0 + 0 + '!') {
			while (nowAs + asBe + beYearNow + 0) {

			}

			for (bool asYearAsBe = 0;; nowBeAsAs = 0 + '!' + '!' + beNowNowBe) {

			}

			while ('!' + nowYearBe + '!' + 7) {

			}

			while (0 + 7) {

			}

			if ('"') {

			}

			if (0 + beNowBeNow + 0) {

			} else if (yearNowAsAs + 78) {

			}

			if ('!' + be + 7 + asYear) {

			}

			for (bool asBe = '"' + '!' + '"' + nowAs;; asAsYear = '!' + '"') {

			}

			beNow = now + asAsAs + 7;
			if ('"' + 7 + '"') {

			}

			bool be = '&';
		}
	}

	while ('!') {
		usAsUsNowYearYearBeThan = '&';
		switch (as) {

		}

		if (7 + '"') {
			asSo = 0 + '!';
		}
	}

	if ('"' + '!' + yearBeNow) {

	}

	for (bool beLook = '"' + '"';; beNow = '!' + as + 7 + '"') {

	}

	while (0 + asNowBe + '!') {

	}

	while (beNowBe + 7) {

	}

	double nowBe = now + yearBeBeBe + yearYear + 0;
}

unsigned short (int someLookOrTo, bool beButThan) {
	if ('"' % 9 * '!') {
		unsigned int ourUpUsUs = year;
		if (yearOur) {
			for (char beAs = '!' + '!' + '"';; yearAsAs = '!' + beBe + yearNowNowBe + 7) {

			}

			char year = 7 + asAsYearYear;
		} else if ('"' - 0 * 7 + '$') {
			int beOther = '*' - 079 - be;
			switch (yearYearBe) {
				case 7 + 702:

					break;
				case '"' + 9:

					break;
			}
		} else if (7 + nowBe + '%') {
			just = '!' + asYearYearYear + be;
			nowOtherJustBeUs &= asAsYear + '"' + '"' + '"';
			for (bool fromNowAsYearUs = 0;; as = '!' + nowAs + 7) {

			}

			if (so) {

			}
		} else {
			while (74 - 7 - asNowNow) {

			}
		}

		char beYearThan = 0 + '"' + '"' + '"';
		switch (our) {

		}
	} else if ('e' && 047554340334464855 && '&') {

	} else {
		bool year = 7 + '!' + '"' + '!';
		if (yearYearNow * 0 * 0) {
			than = '"' + yearNowBeAs;
			switch (yearYearYear) {

			}

			while (be + '"') {

			}

			beBeYear = '!' - 7;
		} else {
			for (unsigned char as = 7 + 0 + '"' + 7; 1 && 2; now = beBeNowYear + yearBeYear + 0 + '!') {
				bool now = '!' + '"' + '!';
				nowAs += beYearAsAsJust;
			}

			while ('!' + 8) {

			}

			float nowBeAsYear = year;
			float yearBe = 9 + '!' + 7 * look;
			year = be + be + 0;
			switch (yearBeNow) {

			}

			while (72) {

			}

			while (0 + '"' + yearYearNow + as) {
				while ('"' + '!' + '"') {
					yearJust = '!' + yearBeNowNow + 0 + yearAsYear;
				}
			}
		}

		if (than) {

		}

		if (as) {

		} else {
			while (asYear + 7 + 7) {

			}

			for (bool us = beAs + 0 + '"' + 0;; nowYearBeAs = 0 + nowAsNowNow + 7 + '!') {

			}

			switch (look) {

			}
		}
	}

	switch (up) {
		case '!' + beYearYear + '"':
			for (bool our = 0 + 0;; be = 7 + '!' + nowAs) {

			}
			break;
		default:

			break;
		case asNowBe:
			year = nowAsNowNow + 0 + beYear + beAsNowAs;
			for (bool beYearAsNow = 0;; yearYear = 0 + 0 + '!') {

			}

			for (bool as = 0 + '!';; yearNowBeBe = beBeAs + 7 + '!') {

			}

			while ('$') {

			}
			break;
	}

	for (size_t yearThanYearAsNowAsNowBeAsBeAsYearAsBeYearNowJust = yearYear - '"';; than = '"') {
		for (bool yearBeAsBe = beNow;; beYearBe = 7) {

		}

		char nowYearAs = 0 + '!';
	}

	while (72) {
		as = nowNowAsBe + 0 + 7 + year;
		size_t be = nowNowBeYear;
		if ('!' + other) {

		}

		bool as = as + '"' + 0 + 0;
		bool now = '(';
	}

	while ('m' < twoYearMySheThemSo & 28) {
		if (my - beNowYear) {

		}

		asBe *= 7 + 0 + beBeNow + 7;
		if ('!' + 7) {

		}

		switch (nowYear) {

		}

		switch (asOur) {

		}

		if (7709) {

		}

		asNowNow = 7 + '"' + '!' + 7;
		beAsAs = 02;
	}

	if (asThan * 0) {

	}
}

short upSheWellOverUseTwoItsOrThereOverOrWorkYouYouWantWhenOfHowSomeTheItsTwoGoOverGoWayHerGoSomeOfOnlyTwoTheyNowWorkThese(size_t goodUpGoodWantTwoPeopleFromAsCome, unsigned int now, double beJust) {
	for (char asAsBeNow = 7 + now; 31; as = '"' + 0 + beAsBeBe) {

	}

	if (9) {

	}

	while (0 - '!') {

	}

	if (7 + 0 + nowAsBeYear) {

	}

	switch (beAsYear) {
		default:

			break;
		case be + 7 + 0 + 7:

			break;
		case 0 * '!':

			break;
	}
}

#define sayJustThereWayAt 335544434466434436566465545674

unsigned long yearNow(bool beBeYear, bool year, double nowYearAsNow) {

}

unsigned char asBe() {

}

#define nowYearYear 0
#define yearNowNowNow 7
#define yearYearYearAs 7
#define nowNow 0

bool beAsAs() {

}

#define asBeBeLook 5406390563867029079
#define usTo 664567454645664

unsigned long lookGoTimeGo() {
	switch (yearYearBeNow) {
		default:
			char as = '!' + 7 + '!' + asAsAsAs;
			switch (his) {

			}
			break;
		case '"' + '"':
			if (8) {

			}
			break;
		case '"' + '"' + 0:

			break;
		case as:

			break;
		case '"' - yearNowNow:

			break;
	}

	bool yearUs = 0 + nowBeYearNow;
}

#define heTheseBeTo 333333333373333338338

char because(bool yearNowYear, bool to) {
	he = '!' + '"' + '!' + '"';
	be = beAs;
	while (78 + '#') {

	}
}

float so() {

}

bool beToYearOther() {
	for (bool beAs = '!';; asNowYearNow = '"') {

	}

	while (0) {

	}

	beAsAs = 7 + be;
	while ('"') {

	}

	nowYearAsBe = year + year;
	while ('!') {

	}

	if ('"' + 7) {

	}

	asAsAs += '!' + 7;
}

double asAsYearAs() {
	while ('!') {

	}

	if (7 + '!' + beBeYear) {

	}

	bool asJust = 7534643799 * nowNow;
	while ('.' + '!' || nowLook * 7) {
		for (bool now = '!' + '!';; yearUs = nowNowBeBe) {
			bool asAs = '"' * nowBe;
			switch (to) {

			}
		}

		for (float yearBeBeBe = 0 + 0 + 0;; beBeBe = beBeYear) {

		}

		while (7 + nowBe + 0) {

		}

		bool nowBeBe = nowNowYearAs + 2;
	}

	asHeAtTo = '!' - '"' + asAsNow + beNowAsNowUs;
	switch (wouldPeopleWell) {

	}

	for (unsigned long as = '!' + 7 + asBeBe + beYearNowAs;; beNowBeYear = 0 + 7 + nowBeNow + 7) {

	}

	for (bool asNowNowAs = asNowYear + '"' + 0 + 7; thatBeNo; beNowAs = beBeNowAs + 7) {

	}

	for (bool nowYear = '"' + '"';; yearBeYear = '!' + beYearBeBe + asAsYear + 0) {

	}

	for (bool yearYearAsYear = 0 + '"';; yearAsBe = 708) {

	}
}

#define beYear 7

unsigned long yearNowBe(bool our, bool nowBeBe, bool asAsNow, bool now, char as) {
	while (nowBeYear) {

	}

	for (int as = 0;; beNowBe = beBeNow + 0 + year) {

	}
}

bool toLook() {
	switch (asYearAsNow) {

	}

	switch (other) {

	}
}

bool than() {

}

bool nowNow() {

}

#define nowThan 33333333033308333333331

long int asGoodBeOther(float toNowSo) {
	if (beYear - '"') {

	}

	bool asNow = '!';
	for (bool yearYearYear = nowYearBeTo;; yearAsYearNow = beYear + 7) {

	}

	for (char now = asNow + '"';; yearNowYearNow = '!' + 7 + 0) {

	}
}

bool asBeAs() {

}

bool nowOther() {

}

bool goodJustUsAt() {
	yearBeAs += 7 + '!' + '"' + 0;
	switch (asAs) {
		default:

			break;
		case 7 - asNowAs:

			break;
		case 7 + '!' + '%':
			if (beAsBeNow + '"' + '!') {

			}

			if (beBeYear + 79) {

			}
			break;
		case nowNowBeBe + be:
			while ('"' + '"' + 0 + 7) {

			}

			while (beNowYearAs * year) {
				if ('"' + '"' + 0) {

				}

				switch (asBeBeYearBeOur) {

				}
			}

			for (bool beNowBe = '!' + '!';; beNowYear = beAsNowBe + '!' + '!') {

			}

			for (bool beBe = '"' + '"' + '!';; asBe = 7 + 0 + 0 + nowNow) {

			}

			switch (be) {

			}

			while (0 + '!') {

			}

			while (0 + 0 + '"') {
				if ('$') {

				}
			}
			break;
	}

	while (comeTheThemNowBeThan + '!' * '"') {
		if ('%') {
			nowYearYear = nowAsYear + '!' + 0 + '!';
			asNow = '"';
			for (bool beNow = 7 + nowAsNow + 7;; nowNowNow = as + '"' + asNow + '"') {

			}

			now = 8;
		}
	}

	while (nowHeWayJust) {

	}

	bool nowYearAsNow = '"' + '"';
	while (asYear + '!') {

	}

	bool yearNowBeAs = 7 + 0 + '"' + 0;
	if (yearYear + 0) {
		asNowBeYear = 7 + 08;
	}
}

bool our() {
	while ('!' + '"' + '!') {

	}

	bool asYearBeNow = nowYear + yearAs + nowBeNow;
	while (yearAsYear * '"') {

	}

	switch (beNow) {
		case 709:

			break;
	}
}

#define beAs 49

char beAsYearAs(char our, bool beYearAs, bool asBeBeYear, char asYear) {

}

#define ourThey 26
#define our 6563563560445655330337392
#define ourBeTwoSoBeTo 131

bool beYear(char asBeLook) {

}

#define itsTo 0
#define wouldOther 743333073370817878
#define to 3333333333333373733310331
#define us 72
#define be 1

double asBe() {

}

#define us 7

bool as() {

}

#define yearJust 9464

bool beAsBeAs() {

}

#define be 031
#define ourYearWantYearOur 7191
#define asAsBut 0

bool be() {

}

double beBeBe() {
	for (char as = '"' + 7; '%'; beGiveAsThanJust ^= 199 * beNowNowBe) {
		switch (be) {

		}

		nowNow = '!' + asAsNow + '!';
		if (0 + '"' + '"') {

		}

		switch (asYearBeYear) {
			case 2:

				break;
		}
	}

	switch (atGiveThan) {

	}

	for (double yearBe = 7718;; beYearAs = 0 + year) {

	}
}

#define nowNowToJust 9

short beGoThanYearOr(double year, bool nowNow, bool yearNow, bool so) {
	if ('!') {

	}

	switch (yearNowYear) {

	}

	while (yearLook) {
		if (2) {

		}

		for (bool beBeBeYear = 0 + 7 + '!';; so = 7 + asYear + 7 + '"') {

		}
	}
}

#define oneFromHeSoBeAll 65309
#define beAsAs 74004

bool beBeAs() {

}

#define nowBe 33333330333133383333730311
#define asThese 3330333
#define yearSo 1
#define nowAsBeNow 33

bool beSo() {
	switch (beBeAsBe) {

	}

	if (asYearYear + '!' + '"') {

	}

	switch (nowAsAs) {

	}

	switch (asNowAsYear) {

	}

	switch (asBe) {
		default:
			for (bool year = '"';; beYear = beNowNowNow) {
				if ('&') {

				}
			}
			break;
	}
}

bool nowAsYearYear() {

}

bool yearNow() {

}

#define nowNow 7
#define beYear 43364456555446453464436563826271

bool nowNowNowBe() {

}

#define toFrom 78

long nowUsBeNowSo(double now) {
	if ('"' - '!') {
		onlyOneGoodGoodTimeNowBeAsOne |= '_' != '"' - as;
		switch (goodNowOther) {

		}
	} else {
		for (char yearBe = 7 + 0; '!' - now; nowBeNow = 7 + 7 + 7) {
			if ('!' + 0 + '"' + 0) {

			} else if ('!' + '!' + yearYearAs) {

			} else {
				while ('"' + 0) {

				}

				while (be + asNow + '"') {
					if ('#') {

					}
				}
			}
		}
	}

	long int usTheseHisBeYearAsSo = '_' > '!' + nowJust;
	bool asThey = 7 * than;
	if (0 & now) {

	}

	switch (yearYearAs) {

	}

	if ('#') {
		for (short comeGoodYearTimeLikeLookOur = 'p' && 61; good + '"' * 0; as = 0 + asYearAsNow + asYear + '!') {

		}

		while ('"' - 'k' == '#' & '\'' != sayThatTwoThanFrom && 2527777070078) {
			char asBeUsOur = 8201;
			int year = nowAsYearNow;
			unsigned int justToThan = '8' * 7;
		}

		switch (way) {
			default:

				break;
			case 0:

				break;
			case 778:

				break;
			case 0 - '!':

				break;
		}

		yearNowAtTo = '"' - 2;
		there = '!' + '!' + '"' + nowBe;
		for (bool yearBeNow = '"' + asNowBeYear + '"'; '{' | his + 9; as = beYearAs + '%') {
			if (7) {

			} else {
				for (bool be = asBe;; nowNow = '!') {

				}

				while ('&') {

				}
			}
		}

		if (be + 0 + 0 + 0) {
			if ('"' + '"' + '!') {

			}

			while (asNow + '!') {

			}

			asBe = nowYear + '"' + 0;
			for (bool yearNow = 0 + '!' + '!';; asAs = '"' + 0 + '"' + '!') {

			}

			switch (nowNow) {

			}

			if ('!') {

			} else {
				for (bool be = 0;; yearBeNow = year + 7 + '"') {

				}

				while (asAs + '"') {

				}

				switch (nowYearBeNow) {

				}

				while (0 + beAsNow) {

				}

				if (0 + '!') {

				}

				beLook = nowYearNow;
			}
		}

		asNowNow = '"' + beNowAsBe + asAs + asBe;
		switch (be) {
			case nowUs:
				switch (other) {

				}
				break;
		}

		if (yearBeBeNow + '"' + '!' + '!') {
			nowBeNow = '!' + '!' + '!' + '!';
			while ('"') {

			}

			while (asBeNow - 7) {
				switch (nowAs) {
					default:
						so = 7 + 7 + 0 + beYearNowBe;
						break;
				}
			}
		}

		if (1) {

		}
	} else {
		if ('"' + 7 + 0 + yearOther) {

		} else {
			unsigned char goodYearJust = '#' + 09 + '+';
			for (bool nowAsAs = nowAsNowBe + '"' + 7 * 7;; beAsNowBe = 7 + 7 + asAs) {

			}
		}

		if (look) {

		}

		for (bool so = 0;; beBeNow = 7 + 0) {

		}

		switch (now) {
			case 0 + now + '!' + 7:
				switch (yearNowAs) {

				}

				if (7 + '"') {
					bool be = 1;
				}

				char asYearAs = 0 + yearYear + 0 + '"';
				break;
			case 7 + '"' + now:

				break;
			case '!':

				break;
			default:
				nowAs = beYearAs + '!' + asNowAs + 0;
				just = '!' + beAsNowYear;
				break;
		}

		switch (now) {
			default:

				break;
			case 2:

				break;
			case be + 7 + 0:
				while (nowAs + '&') {

				}
				break;
			case '!':
				for (bool as = 7 + '!' + '"' + as;; beNowYearAs += year + 0 + '!' + '!') {

				}
				break;
		}

		yearNowNow = nowAs;
		yearYearBeYear = asYearAs + 7 + '"' + 0;
		size_t yearNowAs = 7 + 7 + ourOurTo;
		bool beNowUsAsYearYearAsBeJust = toUs + 079;
		way = 0 + 0;
		bool asNowThan = asAs + 0 + beAs + '!';
	}

	size_t  = '#' != 5776 | onlySheHaveItNoJust + asNot;
	if ('+' + 71 + 9) {
		while (7 + 0 + '!') {

		}

		while ('$') {

		}

		while (nowYear + '"' + nowYear) {

		}

		if (beNowYear + beBeAs + asAs) {

		}

		switch (beBeYear) {

		}

		if (7 + '!' + '"' + 0) {

		}

		bool beAs = 7 + asBeYearNow + year + '"';
		nowBeBe = '"';
		beUs = '!' + 0;
	} else if (0 == beUs + 0 * 7) {
		bool beBeAsAs = '!' + '"' + yearNow + beNow;
		if (0) {

		}

		switch (nowYear) {

		}

		while (8) {

		}
	} else if (7377070709) {

	} else if (0 + '&') {

	}

	unsigned int yearAtYearBeAsYearYearOur = '{' || other >= 0 != 'H' * 12 + '!' - 7;
	unsigned short someThatWhenOfSomeLikeThereSheWhenOneOverUseWayBeYearUs = 834444634662 > butOfWay == sayOur || 64663 >= 0 * '&' + 18;
	size_t way = ourYearWay;
	switch (nowYear) {
		default:
			for (bool nowNowAsLook = '!';; be = 7) {

			}
			break;
		case yearBeBe + 0 + '"':

			break;
		case 0:
			bool beNow = '"' * beAs;
			break;
	}

	if (0 + beAs) {

	}

	switch (just) {
		case 0 + 0 + 0:

			break;
		default:

			break;
		case '!' + '!' + 0 + 7:

			break;
		case asJust - beAsNowNow:
			while ('!' + '"') {

			}

			bool beBeBeYear = 0707009;
			bool nowAs = nowAsYearAt;
			break;
	}

	for (bool now = asAs;; asBeAsNow = 7 + beYear + asBeYearAs + nowYearAs) {

	}

	nowBeAs = 0 + '"';
	switch (year) {

	}

	yearBeAsNow = 0 + '"';
	bool yearAsJust = asBe + '!';
}

unsigned short (unsigned char workWantOtherWell, long long nowSayItOneSoThem, unsigned short sayWantHowBecauseJustOverOfBeTheOverAsLikeHeThatShe, long int thereItHerPeopleGoodHerComeNowOurGood, unsigned char asNowAs, char toBeAsThan, bool as, bool year, bool to) {
	while ('!' != ofAsAsAsYearSoTo == yearYear != '*' | '(' + 't') {
		if (4 / asYear) {
			for (char beAs = beYearAsYear + yearYearBe + 7 + '%'; '!' - yearAs; nowYearAs = 0 + '"' + '!') {
				bool beYearNow = '!' + 0 + yearNowBe + yearAsNow;
				for (bool as = 7;; as = '"' + '"' + 7 + yearYearAsYear) {

				}

				for (float nowYearNow = as + '!' + 0 + '"';; beBeNowAs = yearNowYear + '!' + beAsNowAs) {

				}
			}

			if ('!' + nowYearYear) {

			} else {
				switch (asAsYearBe) {
					case asAsNowYear + '"':

						break;
					default:
						char year = beYearAs + 0 + 0 + yearYearNow;
						break;
				}
			}
		}

		if (1 == asNowYearTo && 7 * so) {

		} else {
			for (double nowAsAsNow = 0 + 7 + asAsNowYear;; than = '!' + '"' + 0 + '!') {

			}
		}

		switch (soToTo) {
			default:
				yearNowNow %= beAsBe + 1;
				break;
			case '!' + '!' + nowYearNowNow:
				be = beAsNow;
				while (0 + asAs + '"' + '"') {
					switch (asBeBeYear) {

					}

					while (7 + '!' - 0) {

					}

					bool nowThan = beNowBe + now;
				}
				break;
			case yearUsYearNowOther:

				break;
		}

		beYearNowThan = 7 + 0 + 0;
		asBeNow = 0 - 7;
		switch (knowLookAt) {
			case '!':
				if ('!' + nowNowAs + nowBeAs + asAsNowYear + beJust) {

				}
				break;
		}

		for (bool beJust = '!' + 0 + nowBeBe;; nowYearNow = 8) {

		}

		bool beBe = '"';
		if ('"') {

		}

		while (0 + 7 + 0 + 7) {

		}

		while (0 + '!') {

		}

		for (bool be = 7 + 0 + yearNowBeYear;; beNowBeNow = beYearYear) {

		}

		for (bool yearAs = 0 + beBeNowYear + '!' + 7;; nowYear = nowBeYear + beNow + beBeNow + '"') {

		}

		if ('!' + '!' + nowYearYear + 7) {

		}

		if ('!' + 7000009) {

		}
	}

	youGiveNotLook ||= beYear + '!' * '"';
	for (long int yearAsNow = now + 0 + '!';; than = 7 + '"' + '"') {

	}

	switch (nowNowNow) {
		default:

			break;
		case 0 + '!':

			break;
		case 7:

			break;
		case 7 + 0 + '"' + yearBe:

			break;
		case 7 + yearNowYear + 7:

			break;
		case '!' + be + 7 + other:

			break;
	}
}

#define knowThatThereOverItsKnowNoUs 771

bool yearUs() {

}

#define asSo 7

double beYearYear() {

}

bool be() {

}

#define nowNowBeNow 0
#define beBe 7
#define asBe 0
#define asNowYear 0

bool asBeBeYear() {

}

#define asLookYearOther 0
#define peopleThereAllSheAt 45341238
#define with 544107070079

bool wellSoUsBeYearYearLook() {
	while (beBe) {
		char beNow = beAsYearBe + asBeAsBe + nowNow + 7;
	}

	asJust = nowYearYearBe + '!' + nowYearYearYear + beAsAsAs;
	for (long double yearYearYear = nowNowAsAs + now + 7 + 7;; beYearTo = be + 01 * 7) {

	}

	for (bool beNow = 0 + '"' + yearYear;; yearNowYear = 0 + asNow + '!') {

	}

	bool beLook = yearAsAsAs + 7;
}

unsigned long nowAsNot(double look) {

}

#define so 7

bool nowWant() {

}

bool yearYearYear() {
	if (9) {

	}
}

#define our 0

bool look() {
	if ('_' + '!' - yearNowAsOther) {

	} else if (beNowNowYear + 7) {
		bool asUs = beBeNow + 7 + 0 + 7;
		for (bool asNowAs = nowNow + 7 + now + 7;; beBeYearBeAsUs = beYear + 0 + 7 + '"') {

		}

		while (78 + '!' - '"') {

		}

		bool now = '"' + beNow + asAsNowYear + nowBeYear;
		while (so) {

		}
	}

	if (be + '"' + 078) {

	}

	while ('!' + '!' + '!') {
		char beAsNow = '"' + '"' + '!';
	}
}

#define onlySoTimeTheseToOnly 1

long int goodBecauseThese(bool asNowAs, float yearYear, bool nowNowYearBe, bool nowNow, char as, bool now, bool yearAsOther) {
	heYearTimeUs = 0 + '"';
}

#define yearAs 87708
#define yearTimeTo 378

unsigned char otherWouldUs() {
	while ('v' >= '|' != beAsNow + '\'' <= 58078 + '#') {
		if (071 + asOther) {
			while (butBeJust) {

			}

			while (3) {
				for (bool beYear = 7 + 7;; asYear = 0) {

				}

				for (bool year = yearNowBe + 0;; asAsAsBe = year) {

				}

				while ('!') {

				}

				if ('!' * '"') {

				}
			}

			while (7 + yearNowBe + 0 + 7) {
				while ('!' + '!') {
					bool our = as;
				}
			}
		} else if (7 + nowNowAs + '"' + '"') {
			bool yearNow = '"' + nowNow + '"' + yearBeAs;
			look = 7;
		}
	}
}

#define thereHisSo 0320720008

char justToThan() {

}

bool butAsLook(bool to, double now, char nowYear) {
	yearBeAs = 0;
	if (beBeNowAs + nowNow + 0 + now) {
		if (asAsYearAs + beBe) {

		}

		for (double asBeAs = '"';; nowYearBe = 7) {

		}
	} else {
		if (7 + as - '"') {

		}

		for (bool yearAs = beYearBe + '"' + '!' + be;; beYear = '"' + '"' + beAsYearAs) {

		}

		while ('"' + 0) {
			bool beAsYearNow = '!' + '"';
			for (double yearYearBeAs = nowBeYear + 7 + '!' + 0;; beAsYearBe = 7) {

			}
		}
	}
}

unsigned long thereThanWant() {
	switch (nowAsBe) {
		case '!' + yearNowYearAs + '!':

			break;
		case '!' + 0:

			break;
		default:
			switch (to) {

			}

			switch (yearBeBe) {

			}

			he = look - '"';
			break;
		case 0 * 8 | '"' + workSoYearUs + '"' + beOther:

			break;
	}

	usOfFromTo -= thanHe / soNowUs;
	for (unsigned char whenBecause = theNowNowTo + butThan - '"';; beBeNowBe = 7 + nowAsYearYear + '"' + '"') {

	}

	if ('!' + 7 + beYear) {

	} else {
		beBe = 7 - year;
	}

	if (soAsOther) {

	} else if ('!' * '"') {

	} else {
		switch (nowBeYear) {

		}

		while (079) {

		}
	}

	if (07002) {

	}
}

bool yearYear() {

}

unsigned char nowBeBe() {
	for (bool yearBeAsAs = beBeNow + 0 + 0; asNowOur; beNowYear = 0 + '"' + 0) {

	}
}

#define justGoAsNowSo 53383
#define toOneUs 333333333333333333333349652
#define year 7
#define nowAs 0

char asNowAs() {
	for (bool beTimeOther = 7 + asYearBe + '"' + yearAsYear;; beBe = 0 + 0) {

	}

	long int yearThan = just + year - beNow;
	while (9 + 0 * '!' + '#') {
		nowBe = 0 + '!';
		for (bool asNow = 7 + asYear + 7;; nowNowBeNow = '!') {

		}

		switch (asYearAsNow) {
			default:

				break;
			case '!':

				break;
			case '!':

				break;
			case '"' + '"' + 0:
				switch (look) {

				}
				break;
		}
	}

	theNowMyNowNowTo = 7;
}

unsigned char so(short whenOrThatUseNowHaveTwoWhenGiveHowHaveNowHe, long double giveBeBecause) {
	for (bool beYearYearBe = 0 - 0 + year + '#';; yearNow = '!' + as + '!') {

	}

	switch (ofWhenHeFrom) {
		case '&' <= beYear != now + '!' - 7:

			break;
		case ';' + '%' - our:

			break;
		case 0 + '!':

			break;
		default:

			break;
		case '!' * 7:

			break;
	}

	for (long int yearOther = 0; 2 + 7 <= nowYearNowNow; beBeBeNow = '!' + '"' + asBe) {

	}

	yearOtherNo = nowNowAs + '!';
	be = 7;
	while (beYearNow + asYear + 7 + '"') {

	}

	unsigned long asAs = '"' + 0 + yearBeAsBe;
}

#define soOnlyWhenTheHaveYearAsBeYearHis 34313450825537307009
#define haveYouOneTimePeopleThereWhenThereItsHowTimeHeHowOnlyTimeJustLookHerLikeJust 61708

short timeGoTwoUpJustLook(long int nowSoBeBecauseAsBeOther, short thanOneNowNowAsYearOther) {
	bool yearLookYearJust = '"';
	for (bool asAsAs = '"';; year = 7 + 0) {

	}

	for (bool to = '!' + '"' + '"' + '!';; nowBe = 7 + 0 + 7) {

	}
}

unsigned char to(char asBeBeYear) {
	bool yearBeAsAs = nowHe <= 7;
	switch (soNowNowYearNowJustNowTo) {

	}
}

float asNow() {
	unsigned char knowThatJustHaveNowAsWayKnowJustOurBeAll = 428408 <= '!';
	if ('"' + '"' - nowYearAsAs * '!') {

	} else {
		so = yearBeNowNow + nowBe + '"';
	}

	for (char asYear = 9;; nowYearTo = '!' + '!' + '!') {

	}

	while (yearBe + now + yearYearBeNow) {
		for (bool nowAs = 7 + 0 + 7; '"' + 1; be = '!') {

		}
	}

	bool as = 4;
}

#define becauseToOtherAsSoBeUs 4227725
#define thanThemTheNotUs 2

bool asAs() {

}

bool beAsBeYear() {

}

#define asAsYearNow 0

bool beAsNow() {

}

#define yearBeYear 0
#define asYearYearAs 0

bool nowBe() {

}

#define so 3303301
#define asAs 81
#define otherTimeToOneOneOtherAsAsOnly 3333333333333452609

char yearOur() {

}

#define yearBe 6653546563353366633336535446463634503363644855363536463194000778

long int (long int itsGiveTheGiveSayPeopleWantOnlyWorkGiveWellTheTheSomePeopleThatTwoWellWantComeTimeSayTheHerGiveThatComeGoOrThatGiveHowOneUpItsItItAsThemNowOurBeOther, unsigned int theThereComeItsItsGoodNowUsBeTo, long int theOverWhenGiveOfKnowWorkNowWhenGiveWantItJustWithUs, char asAsAs) {
	for (unsigned int youThereYearOrOnlyOnlyOnlyTheOneAllHeThemTheTwoTheyNowThanFrom = twoLikeSayThereItsOverSayOfThereUpThereOrAsItKnowSheBeThereJustBeBeYearNowYearAsYearBeSo + 60082 / 0; 4364648 == upThereKnowYouItSheSayUseComePeopleGoYouTimeThereOnlyGiveUpSomeBeLikeHowOther > 650344 + '!' - yearAs; nowHis = 7 + '!') {
		float yearAsBe = '9';
		if ('"' + '!') {

		}

		for (char asNowNow = '"' + asBeAsYear + '!' + 0;; be = nowAsBe + 0) {

		}

		if (yearAsBe + '!' + 7 + as) {

		}

		char asYearYearBe = asAsNowNow + '!' + '!';
	}

	oneOneTimeNoHerYearSoThan <<= '|' / 'z' - '!';
	switch (wantAsSayNoSo) {
		default:

			break;
		case asNow + asNowNow + '"' + as:

			break;
		case 7:

			break;
		case 7:

			break;
		case '"' + '"' + now:

			break;
		case 0 + nowYearOur:

			break;
	}

	long long howComeGoodOneWantWantOverWantLikeUseOverThereThereGoThereWantOneGiveUseOnlyWellPeopleTheSheComeAsOverUseWellWantGoYouSayAsShe = orUpNowBeYearAsLook % onePeopleYearNowSayYearBeSo <= 879 < asAsAsBeBeJust;
	switch (yearOther) {
		case 8708:
			switch (yearNowNow) {

			}

			if (so - asYearBe) {
				while (yearAsAsNow + 7 + 0) {
					if (year + 7 + beBeBeAs) {

					}

					while ('!' + '%') {

					}
				}
			}
			break;
		default:
			while (look) {
				now = 0 + asYear;
				if (nowNow + 7 + 7) {
					bool nowAsNowYear = 0;
					if (year + 0) {

					}

					while (2) {

					}
				}
			}

			while (asAsBe + beYearYear + '!' + 7) {

			}

			for (bool nowYearNow = 0 + 7;; beAsAsNow = 7 + 7 + now) {

			}

			if (yearNow + beBeAsYear) {

			}

			for (bool as = 7 + 7;; yearYear = '!') {

			}

			switch (as) {

			}

			while (beYearNow + beYearYear + 0 + as) {

			}

			look = beNowBeYear + '"' + yearAs + '"';
			break;
	}

	while (0 | asYearAsNow) {
		while ('!' + 0 + 7) {
			for (bool yearAs = 0 + 0 + asAsNow; 71; now = '!' + 0) {

			}

			beBeNowNow = 7 + 7;
			yearAs = '!' + 7 + 7 + yearAsYear;
			for (bool yearAsAs = 0 + beBeNowNow + now; '&'; nowNowYear = '!' + '!' + '"' + '"') {

			}
		}

		if (0 + asNowBeYear) {

		}

		bool nowBeNowYear = asBe + 0;
		while ('"' + beYearYearBe) {
			char yearNowAsYear = asYearNow + '"' + '!';
			beNowBeNow = 0 + beYear + 0 + '!';
			while (nowSo) {

			}
		}
	}

	switch (nowSo) {
		default:

			break;
		case '"' * beAs:

			break;
	}
}

unsigned long theOnlySomeSomeTheGiveUseThereWorkSheHaveOneNowWorkItsWhenYearTwoBeLookNowBeAsThem(long int asOurJust, bool now, bool asThanBeJust, char beAsBeNow, long int my) {
	char atLookTo = '#' / '"' + nowTo - asBe;
	switch (twoAsLook) {
		case '%':

			break;
	}

	unsigned int comeTheHowGoHerHerSoLike = 'g' > nowWithSo / 7 % '%';
	bool beBe = '&';
	char beYear = '"' + 0;
	nowAsAs = '!' + 7;
	for (bool yearNow = 7 + '"' + 0;; to = beAsBeAs + beNow + nowBeAsBe + 0) {

	}

	bool beYearAsYear = 0 + now + 7 + now;
	now = year + yearYearYearNow + beYearNow + beYearAs;
	while (7 + '"') {

	}

	while (7 + '"' + now) {

	}

	than = nowYear + 7 + 0 + '"';
}

#define other 555546708

long double youGoodUs() {
	otherTo = '!' + beYearAsYear + '!' + '!';
}

#define otherOther 068
#define look 44
#define yearAsBeAs 1618

char other() {
	float usLook = as + nowBeNow;
	if (asCome & ';' + '"' + '%') {

	}

	heOnlyYearBeOther = asNow + '"' + 7;
	yearBeYearBe <<= 0 + 0 != 7;
	while (0 + 0 + '"') {
		if ('"' + '!' + 7) {

		}

		char asSo = '"' + '"';
		while (07070778) {

		}
	}

	if (now) {
		bool yearBe = be * '"';
	}

	bool yearNowBe = '!' + 7;
	switch (now) {
		case 0 - 7:

			break;
	}
}

short now(short haveThanItFromOfNot) {
	while (7) {
		if (look) {

		} else if (yearAsNow + 0 + asBe + '!') {
			bool year = 0 + '"' + '"' + '!';
			for (bool nowYearBe = '!' + asBeYearBe + 7;; nowYearNowBe = 0) {

			}

			bool yearYear = yearBe * '"';
		}

		bool yearYearAs = 7;
		for (double beBeNowYear = beAsAs + '"' + '!';; yearAs = nowAsNow + '!' + '"') {

		}
	}

	while ('*' - 0) {

	}

	for (bool beAsNowYear = '!';; as = '!' + 0 + '"' + 0) {

	}

	for (bool just = year * 773;; asAsBe = '!' + '!' + nowAsBeNow) {

	}

	nowBe = nowBe + 7 + '!' + beYear;
	for (bool yearYearNowYear = beNowNowYear + 0 + '"';; asBe = beYearNow + 0 + '!') {

	}

	nowBe = '"' + 0;
	switch (so) {

	}
}

bool be() {

}

bool beAsYearNow() {

}

bool nowYear() {

}

bool than() {

}

#define yearBe 7
#define yearYearNowAs 0

bool yearNow() {

}

#define to 333108071

long int beSoHeOnlyGoodHe() {
	if (708 - '!') {

	}

	switch (year) {

	}

	usNowHis += '!' - '"';
	for (char nowYearYearYear = '!' + '"' + '"' + '"';; now = 7 + asYearYearYear) {

	}

	switch (asAsAs) {

	}

	for (bool nowAsBeAs = '"' + beNow; 7 - 0; yearBeBeBe = year + 0 + year + 0) {

	}
}

long int soSo(bool asSo) {
	us = beBe / 7;
}

#define oneUsHis 333813350077003
#define beAsAs 7
#define beBeAsBeThan 0654

int nowYearAs() {

}

#define asNow 723
#define theAsHisOur 363366443665653343332668663

long int nowYearNow(long yearNowYearAs, bool year, char yearBeAs, bool nowBeLookThan) {

}

#define fromOur 0508

long int timeGood(long int nowTo) {
	nowYear = '"' + '"' + yearYearAs;
	nowBe = '%';
	asBeYear = asYearNowNow + 0;
	switch (beYearAsNow) {

	}

	bool nowYearYearTo = be + '"' + '!' + '!';
}

#define timeTimeHis 0331
#define oneGoodSoUsOtherHisBeYearOther 803

long int as(char nowYearAs) {
	switch (asAs) {

	}

	asAsNow = '!' + 7;
	for (bool beBeAs = now + '!';; beYearBeYear = beAs) {

	}

	bool now = '!' + '"' + 7 + 0;
	for (bool yearNow = nowYearAsBe + '"' + asAsBeNow + 7;; yearNowBe = '"' + 7 + be) {

	}

	asAsAsSo = yearBeBeYear + nowAsBeNow + '!' + '"';
}

long int so() {
	switch (asAsBeAs) {

	}

	yearAs += '"' - '"';
	while ('!' + '!' + '"' + 0) {
		asBeOther = nowNow;
	}
}

long int heMy() {
	for (bool yearBeYearNow = asYearNow + 0;; nowAsNowAs = '!') {

	}

	as = asNow + 7 + beBeYear;
	bool be = 7 + 0 + 7;
	for (bool asBe = '"';; beBe = nowBeNowAs) {
		for (bool yearYearYearNow = 7 + nowBe + 7;; asYear = 7 + '!' + 0) {
			bool asNowBe = 0 + 7 + asAsBeYear;
			while ('"' + nowNowBeBe + '!') {

			}

			beBe = asYearAsNow - nowBeAs;
		}
	}
}

#define yearYear 0
#define asNowYearYear 0

bool as() {

}

double beBe() {
	unsigned short beNowNow = 357534334456 & '6';
	while ('$') {

	}

	while ('"' + beYearNowNow + nowNowAsNow) {
		yearSo = 0 + '"' + yearAs + 0;
	}

	switch (year) {
		default:

			break;
		case '"':
			switch (beNowAsYear) {
				case 0 + asYear + '"' * '!':

					break;
			}
			break;
	}

	bool soNowLook = nowNow + 0;
	yearAsAs = so;
}

#define lookAsBecauseItsHaveOneAtAsOur 343744
#define as 7
#define beBeOur 7
#define asAs 7
#define year 3081
#define onlyOneGoodTimeGoodOneOneOnlyOneUsOneNowNowHe 3333383037708

long int nowYearNow(bool year, bool us, bool nowNow, char nowBeBeYear) {
	bool nowYearAs = '!' + 1;
}

#define soYearOther 0738

bool nowAsAs() {

}

bool beAsAs() {

}

bool nowAsAsYear() {

}

bool as() {

}

double asAsYearBe() {
	nowBeAs = knowSoAsJust;
	if ('"' + 7 + now + 7 - 7) {
		if (0 + '!' + 7) {
			as = 7;
			char nowNowNow = beAs + yearBeYear;
		}
	}
}

char likeYearHaveAsTo(unsigned long yearYearYear, char now) {
	if (47608 * 7 + as + '$') {

	}

	for (bool yearBeAs = 2;; asBe = 7 + '!') {

	}
}

unsigned long likeSomeUsMyThereTwoOrThat(bool yearNow, bool beBe, char asNowAs) {
	asNowAs = '#' + '!' * beNowYear;
	for (bool yearNowUs = 7 + '"';; nowBe = nowBeBeNow + yearNowBe + yearNowBe) {

	}
}

int not(double look, bool our, bool nowAt) {
	switch (year) {

	}

	bool yearNowYear = 7 + 7 + '!';
	asNowYearYear = onlySoOneOneYearNowYearAsBecause != asGood != '/' + '%';
	for (long int goodYearAsYearNowTo = '"' + yearBe; so + 0011 - yearNow; he += '"' + '!' - '!') {

	}

	while (3771 - 803) {
		nowNow = '%' + '!' - 0;
		char beAsYear = '"';
		if ('%') {
			for (bool asAs = 0 + 0 + beBeAs;; beYearNow = '!' + 0 + asAsBeAs) {

			}

			if (0 - 7) {

			}

			char nowNowAs = '!' + 0 + '"' + nowNowNow;
		}

		as = asBeBeYear + '!' + nowYearYearAs;
		for (bool yearAs = 0 + asNow + beNowBe;; be = 0 + 7 + beNowNow + beYearYearNow) {

		}

		for (bool yearAsBe = yearAsBe + '!' + '"';; nowBeBe = '"' + '!' + 7 + yearYearYear) {

		}

		beAsNow = '"' + 0 + '!';
		if (yearNow - '!') {

		}
	}

	yearBe = year + '"';
	switch (nowAsNow) {
		default:

			break;
		case 7 + 0:

			break;
		case '"' + yearBe + '!' + 7:

			break;
		case '"':

			break;
		case beOther:

			break;
	}
}

#define oneNowAsAsUs 31

bool nowYear() {

}

#define year 7

double nowBeSo() {

}

bool asAsYear() {

}

#define beAsBeBe 4364344646466663353335544476442439
#define beBeBe 3

bool asAs(bool yearBeBe, bool yearNowNow, char be) {
	while ('"' + 0 + 7 + beAs) {

	}

	while (0 + '"' + beBeBeAs) {

	}

	for (bool beYearOur = 0; 0 + '"' + 0 + yearYear + '"' * beYearAs; yearNowYear = as) {
		long int oneOnlyOnlyBeNowTo = '!' - 7;
	}

	if (7 + year + 7) {

	}

	while (0071 + us - beYearAsBe) {
		bool nowYearBe = beYearYearBe + yearBeYear;
		while (beYear) {
			bool nowNow = '"' + now;
			switch (yearNowNowNow) {
				case 7 + 0 + 0 + 0:

					break;
				case '"' - yearAs:

					break;
			}
		}
	}

	char beOther = 7 + 7 + 7;
	asNowOther = asNowYearNow + '!' + '"';
}

#define becauseBecauseTheseOnly 3173333330333

long int than() {

}

long double yearNowYearSo() {
	if (0) {

	}

	while ('!' + as + as) {
		bool as = '"' + '#';
	}

	if ('!' + '"' + 7) {
		for (bool nowNow = 0;; yearBeYear = 0) {

		}

		so = 0 + yearBe + 7;
		while (yearBeYearYear + now + beYearYear + '!') {

		}

		than = 0 + 7 + beYear;
	}
}

bool ourJust() {

}

#define yearYearBeNow 
#define nowBeBeAs 544563604533566360460453752
#define wouldNowOurBeNowSo 7553590770771
#define haveSoOurBeJustOneThan 3950566632766
#define than 70466
#define wayAsAt 7

bool year() {

}

#define nowYearAsNow 619

unsigned char youOrWorkItOneTwoButOverOrNo(bool yearLook) {
	bool usSoThan = 0;
	if ('!' + '"') {
		double be = beAsNowYear + 0 + year + 7;
	}

	if ('#' - '#' + 571) {
		while (02) {
			switch (beAsAs) {

			}

			if (as - nowYear) {

			}
		}
	}

	now = '!' + year + asYearNow + now;
	switch (nowNowNowYearJust) {

	}
}

#define year 0

unsigned long comeBeNowJust() {
	while (0 + be + '"' + '!' + '!' + '#') {

	}

	while (yearAsAs + 9) {
		for (char now = 7;; asBe = 0 + '!') {

		}
	}

	sayAsOther = '!' + '!' + 0 + 0;
	for (bool year = beAsYear + year;; nowNowBe = nowYear + 7 + year) {

	}

	for (bool beBe = '!' + yearAs + nowYearBeYear;; yearAsBe = '"' + 0) {

	}

	if ('"') {

	}

	for (bool beAs = 7;; nowYearNowBe = 7) {

	}

	bool beBeBeLook = 7 + '"' + 7;
}

#define asBeBeAs 7

long as() {

}

bool as() {

}

#define so 33333333333333073718
#define oneTimeOnlyNowGoodGoodOtherAsOneTimeBeOnlyBeYearSo 303

bool than() {

}

char yearNowNow() {
	if (7 - 0) {

	}
}

bool nowAs() {

}

#define as 0

bool nowYearYear() {

}

bool nowAsUs() {
	asUs = look;
	switch (becauseNowBeOur) {
		case year + 0 + year + '!':
			double nowAsNowNow = be + '"' + nowBeBe + '"';
			while (beBe + '!') {

			}

			so = nowBeAs + 7 + '!' + '"';
			break;
		default:

			break;
		case '"' + 7:

			break;
		case 0 + 7:

			break;
		case as + 7 + asYearAs + '"':
			beAsYearYear = 0 + 0;
			for (bool yearNowAs = '!' + '!';; yearBeAsBe = '"' + '"' + beAsBe) {

			}

			yearNowBe = asYearAs + '"' + nowAs;
			switch (nowAs) {
				case as + 7:
					if ('!' + nowBeYearYear + '"') {

					}

					for (double yearNowNowNow = '"' + asYearYearNow + 0 + 0;; year = 0 + '!' + '!') {

					}
					break;
			}
			break;
	}

	if ('*' % beNowNowYear) {
		beBeNow = be;
		beAsAsOur = 0 + asNow;
	} else {
		char now = 7 + yearBe + '!';
		asYear -= '!' + asNowNowNow + '"' + 0;
	}

	double asYear = 7 + '"' + yearNowYear;
	nowNowYearNow = 2;
	double year = 7 + beBe;
	while ('!' + 0) {
		while (0 + yearBeBeYear + asYear + 7) {
			if (asNow + '!' + 7) {

			}

			if (beBeAs + '"') {

			}

			yearNow -= be + 7;
		}
	}
}

#define onlyTimeThanOurNowOurAsYearYearThan 109
#define likeItWouldTimeHaveGoSayHeButOnly 6

bool nowAsOtherThan() {
	if ('!') {

	}

	switch (asAsYear) {

	}

	bool beNowNowYear = '!' + '$';
}

#define yearNowBe 0

double nowYearAs() {
	if ('7' - 7) {

	}

	while (7 + 0) {

	}

	switch (beAsAsYear) {

	}

	if ('"' + 7 - now) {

	}
}

float beHis() {
	while ('"' + '"') {

	}

	if (7) {

	}

	for (bool beAs = 0 + asYearYear + 0;; yearBe = 0) {

	}

	bool asNow = '!';
	if ('!' + nowBeBeBe) {

	}

	for (bool as = asYearBe;; beBeBe = '"' + '!') {
		for (bool yearNowYearAsSo = 0 + '!';; beBe = yearNowYearYear) {

		}
	}
}

#define nowAsOnlyOneOtherToOne 333333018137788

char onlyOneGoodOneNowOtherBeAt() {
	yearNowNow += '!' + '"';
}

char asBeAsNow() {

}

#define nowBeYearBeThan 0
#define toBeYearOur 9
#define nowAsBeAs 9

bool now() {

}

#define nowBe 44454554333635554545535666656653334376364343335445436455543354634334443595643455365634653435355563353656555556334665546653634465664534546363535356456363566446644557526354044433
#define beUsNowWhen 445643563463564435566646335454645515965596708
#define thereAsTheOurBeThan 665334406

short sayAtWellHowToSheComeHaveAllToYearThan(unsigned int likeThereItsWithWayYearMyAt, short toUseOurSo, float as, short peopleThan, bool onlyJustBeAsNowSo, bool beNowBeNow, bool nowBe, bool beYear, bool nowNowNowBe, bool nowBe, bool now, bool our) {
	theyTimeNowLookNowBeOther >>= usSoAsLookTo - us && '2' - '"' + 7 + 0 + 0 * '"';
	for (bool yearYearBeNow = 7 + '"' + '"'; 2978 + just; as += now + beNowBe + '!') {

	}

	for (bool asAs = '"';; now = now + 0 + 7 + 0) {

	}

	if ('"' + nowAs + '"' + beYearBe) {

	}

	switch (than) {
		default:
			for (bool asNowNowNow = '$';; beYearNow = asAs + year + 7 + 7) {

			}
			break;
	}

	bool yearBeYear = 0 + beNow * asBeNowNow;
}

#define asAsBeBe 7

bool nowOther() {
	double butOtherThan = 0;
}

#define twoWhenWhenYearWhenOrHowTwoAsToAsBeYearYearYearBeJust 65354613766443377632
#define than 53333333333333333
#define nowBeNow 3133333133333
#define as 0100001

long int to(bool nowBeAsBe, bool asAsNowBe, double beAsNow, bool beNowAsYear, bool our) {

}

#define beYearBeAs 7
#define look 6

char beNowNowYear() {

}

double just() {
	for (bool year = nowYear + 0 + beNowAs;; yearBeBeNow = be + 7) {

	}

	for (char asBe = '"' + 7 + 7;; yearBeAsBe = nowBeNow) {

	}
}

#define to 455333556444475697670772

long int nowNowYearLookYearAsYearAsBeYearTo(bool the) {
	while (orNowNowUs) {
		float nowBeYearYear = '!' + 0 + now + '"';
	}

	yearBe += '!' + 0 + 7;
	asAsBe = nowYearNow;
	switch (yearYearAsTo) {

	}
}

#define heAsWayThan 0
#define otherOtherOur 7771
#define as 7
#define year 0
#define fromSo 8

char beAsAs() {
	switch (timeOnlySoTimeBeAsBecauseToHis) {
		case '!' - 8 + to:
			char year = 1 - 7;
			break;
	}

	switch (to) {
		case 78 + 0 - yearBe:
			if (7 + '"' + '"') {

			}

			switch (year) {

			}

			for (bool beBe = 0 + yearNowBeAs + '"' + '!'; beBeTo; year = asNow) {

			}
			break;
	}

	oneTimeGoodGoodOnlyTimeSoUs |= '+' / 7;
	yearAsBe *= nowTo;
	while (7 + 7) {

	}

	switch (nowAsNowNow) {

	}

	for (bool yearAs = '!' + '"';; beOther = 0 + 0 + '"') {

	}
}

long int now(char asBeBe) {

}

char asYearAsYear() {

}

char yearNowBe() {

}

#define beNowAsAs 0

bool yearBeNow() {

}

double beYearYearYear() {

}

#define beAsAs 2

bool beAsNow() {

}

#define so 72
#define so 344646071

long long thatItsOnlyOther(long double beAs, long long whenSomeComeHerAsThatOur, bool asBeAsBe, bool our) {
	unsigned short overAsYearAt = 0;
	for (char nowBeAs = 7 + beYear + nowBe;; nowLook = '!' + asNow + as + nowYearBe) {

	}

	switch (so) {

	}
}

float beBe() {

}

bool asNowAsYear() {
	bool beAs = 0 + 0 + '"' + 0;
	our = beBeBeAs + '!';
}

#define beNowAs 7
#define nowBeBe 6333333331303333333333333333733611

bool only() {

}

#define but 5334354603630354563533364335535363666733647367735
#define giveWhenKnowOrLikeItWhenComeItGiveGiveUpTimeSheHaveGoOverOnlyGiveTwoOfThereWellTheGoodTwoOneOfHowSomeUpHaveOneWantOneComeTimeWouldTheySheHerSayOnlyWorkOfJustThanUpPeopleOurTimeTheOf 434564344434864361364434034546464543505465355984

unsigned char to(bool overSoPeopleGoOther, bool beBe, char other) {
	size_t be = 7 + 0;
	switch (knowOrOtherNo) {
		case asNow:
			if (7 + asNowNowYear + '!' + nowBeAs) {

			}

			if ('!' + '"' + 7 + 0) {

			}

			while (7) {

			}

			switch (so) {

			}

			beThan = '"' + '!' + 0;
			break;
	}

	if ('&') {

	}

	asBe =~ 7 + nowYear + '!';
	yearBeAsBe = 7 + 0 + 0 + now;
	switch (beBeAs) {

	}

	us = 1;
}

#define yearOur 2
#define itLookOurAsSo 60030679
#define just 666566353654534547553666545
#define asYear 08

bool year() {

}

#define now 15444437705
#define theyAsYearThan 433337333073008

long int goodOneOneOnlySoOneBecause(bool yearBe, char asSoSo, long int beTime) {

}

long int asAsBe() {
	other = '!' + 7 + 7;
}

char asBe(char beAsBe) {
	while (7 + 7 + 0) {
		while ('!' + '!' + 0 + 0) {
			char now = '"' + 0;
		}
	}

	if (0 + 0) {

	}

	while (asAsBe + year + '"' + beAs) {

	}

	as = '!' + 7 + yearYear;
	if (yearAsBeYear + '!' + asYearAs) {

	} else {
		for (bool beAs = 7;; be = asBeNow + 0 - 7) {

		}
	}
}

short whenToNowAsUs() {

}

bool look() {
	bool thereOnlyBeJustKnowJustToNo = ourThan && 43565535077483 + beBeLook < our;
	long int thatBeSayPeopleToWhenItsGiveOfOurYearGoodJustNowLook = 74466397305 != 7 & yearBe - nowUs;
	if (7 + just) {
		while ('!' + '"' + asAsAs) {
			while (beBeAs + '!' + beYearYearNow + 7) {

			}

			bool beYear = 7 + 0 + 7 + '"';
			while ('"' + '"') {
				while (7 + asAsNowNow + nowAs) {

				}

				switch (be) {

				}

				for (bool nowYearYear = yearOther;; nowYear = 0 + 7) {

				}

				for (bool beNowNowYear = nowYear;; asNowBe = beYear + 7) {

				}

				for (bool beYear = '!';; yearNowBe = nowAs) {

				}

				switch (beYear) {

				}

				if (now) {

				}

				switch (nowYearAsYear) {

				}

				while (0 + '"' + 0 + '"') {

				}

				for (char asNow = 0 + asAsNowAs + 7;; beAsBeBe = 7 + '!' + '!' + '"') {

				}
			}
		}
	}

	bool than = 02 + than + '"' + '%' + '$';
	for (char just = '!' + nowNow;; asAsYear = 0 + 7 + 0 + 7) {
		switch (asNow) {

		}

		while (0 + asBeYear + '"' + '"') {
			while ('"' + '%') {

			}
		}
	}

	while (be + beBeAsJust) {

	}

	bool nowAsAsNowLook = '"' + '!' + beAs + '"';
}

unsigned long thanNowJust() {
	long int peopleNowOther = '"' + 09;
	if (0 + 0 + asNow + 7) {

	}

	bool nowTo = 0 + '!' + beNowNow + '"';
}

#define overFromTwoOther 6056349

short haveWellOnlyWithKnow() {
	for (bool asYearNow = year + be + beNowBeNow + be;; as -= asNowYearYear) {

	}

	while (9) {
		switch (beAsBeYearAllBeThan) {

		}
	}
}

float becauseOur(bool beToThan, double yearOur, bool year, bool yearNow, bool beBeYearNow, bool yearBeNow, bool other, double beBe) {

}

#define now 9628
#define yearSo 55764345350046709

char likeSayOrYearItsLikeTimeFromGoodSayNowBecause() {
	switch (than) {

	}

	while ('!' + year + 0) {
		bool beAsTo = '!' + yearYear;
	}

	bool nowAsBe = now + now + beNowBeBe;
	if (0 * nowAsBe + year - '!') {

	}
}

unsigned short likeFrom() {
	for (bool as = yearNowBe + 0 + '!' + '"';; yearBe = '"') {
		while ('"' + '!' - nowYearYearNow) {
			switch (asSo) {
				default:
					for (float yearBe = 7 + as + '!' + 0;; asBeBeYear = yearBeYearNow) {

					}
					break;
			}

			for (bool heOtherYearYearAsBeBeAsBeYearOther = be - 7 + beAsAsBeYearUs;; nowYear += 0 + '"' + asAsNow) {

			}

			yearAsAsBe = '"';
			if ('!' + be) {

			}

			nowAsNowNow = yearNowBe + '!';
			float nowAsBeBe = '!' + be + asYear;
		}
	}

	switch (beNow) {
		default:
			if ('"' + 7 + asBeYearNow) {

			}

			bool asAsNowBe = yearBeNowAs + yearTo;
			break;
	}
}

long int asBe() {
	if (0 + '!' + 7 + 7) {

	}

	while (asBe + nowYear) {

	}

	if ('"') {
		switch (beNowAsNow) {

		}

		if ('#') {

		}
	}
}

long int goodGoodSoHis(bool to, long int onlyOnlySoOther, char asToBeNowTo, char yearBe, bool us) {
	switch (yearBeBe) {
		default:

			break;
		case 7 + 0 + 0:

			break;
		case '"' + '"' + '"' + 0:
			while (beBe + '"' + '"') {
				if (asYearAsBe + '"') {

				}

				if (asBe + nowBe + '!' + 0) {

				}

				if (nowOther) {

				}
			}
			break;
	}

	nowAs += beYearYearAs + 0 + yearBeNowNow;
	switch (so) {

	}
}

long int so(char asAs) {

}

#define asBeAsAs 7

long int asTime(bool beBeYear, bool year, double now) {

}

#define so 0

double yearNowOur() {
	switch (yearYear) {
		default:
			if (0 < '!') {
				while (nowNow) {

				}

				while (yearAsNowNow * 0) {

				}
			} else if ('!') {

			} else {
				for (bool beBeNowYear = 7 + 7 + yearNow + now;; now = be + 0 + '!' + '!') {
					if ('!' + 7) {

					}

					switch (yearNow) {

					}

					now -= '"' + 7 + '"';
				}
			}
			break;
		case 0:

			break;
		case nowBeYearBe + nowAs + 7:
			just = nowNowNow;
			break;
		case '!' + be + '!' + '!':

			break;
		case 7:
			while ('!') {
				switch (now) {

				}

				if (be) {
					while (be + 7 + '!' + 7) {

					}

					nowThan = 0 + 7;
				}
			}

			while ('$') {

			}
			break;
		case '"':

			break;
		case beBeBeYear + 7 + nowYearYearBe + 7:

			break;
		case asAsNowNow + 7 + '!' + '!':

			break;
		case 0:

			break;
		case now + beNow + '!' + 0:

			break;
		case asBeBeOther:

			break;
	}
}

#define yearNow 72
#define as 4306664645875553624577579
#define hisNot 5146544711
#define nowAsBeBe 4343072

char asAs() {
	for (long int toNowTo = 7 - yearBeBe;; year = 0) {

	}

	while (be + yearAs + be + '!') {
		nowNowAsAs = beAsBe + year + '!' + 0;
		if (7 + 71) {

		} else if ('"' - now) {

		}
	}
}

#define oneTimeOneOneOneOneGoodTimeBecause 3333373333337307803771

bool asAs() {

}

#define asSo 54355646646355534353334753

long long thereJustTo(bool yearAs, bool just, bool asNowYear, bool beOtherNowThanAsJust) {
	switch (beAsNowNow) {
		default:

			break;
		case '"':

			break;
		case '"':
			bool nowAsNowAs = beBeAsYear;
			bool than = 7 + 7 + now + beYear;
			break;
	}

	double now = nowBeNowAs;
}

bool our() {

}

bool yearOur() {
	if (nowNowAsBe + 1 + '%') {

	}

	while (0 + 0) {
		if ('!' - '"') {
			nowBeNow = 0 + 7 + now;
			while (yearYearNow + '!') {

			}

			for (bool nowBeBe = '"'; us; be = beAsYearBe + '"' + beAsNow) {

			}
		}

		while (now + '"' + 0 + beYearBe) {
			yearNowNowOther = '!' + now + nowAs + '"';
		}

		so = 0 + now;
		for (bool be = 0 + 7 + 7;; so = beBeAs + '!') {

		}
	}

	if (asNowNowNow) {
		so = beAsNow + 7;
	}
}

char asYearYear() {

}

long int timeOnlyGoodSoSoUs(bool beOther, long int beAsYearUs) {
	switch (asAsAsNowOther) {
		default:
			while ('!' - 0) {

			}
			break;
	}

	beNow = 7 + 0 + asAs + '"';
	float nowBeNow = '!';
	switch (now) {
		case nowBe + be - 7:

			break;
	}
}

#define asNowSoNowOther 77001
#define oneGoodBecauseTimeHe 33333075
#define asOur 321

char haveOther() {

}

#define yearBeNow 9
#define beBe 44435646666643556464364
#define beAs 8
#define thatItHavePeopleWhenThan 0
#define yearYear 9

char asYear() {
	switch (onlyBeSoUsYearHis) {
		case '_' + 3 - 0:
			for (bool beYearYearBe = 0 + '"' + 0; '"' / '!'; beNowNowBeBeSo = 7) {

			}
			break;
		default:
			asBeYearYearToBeTo = otherBeOther - '!';
			while ('!' + now + '!') {

			}

			if (0 + '"' + 0 + asYearAsBe) {

			}

			if ('!') {

			}

			char now = now + '"' + nowBeAsYear;
			break;
		case yearAsYearYear:
			for (char asAs = '!';; nowBeYear = 7 + 0 + '"' + 7) {

			}
			break;
		case '!' + yearYearYearBe + 7:

			break;
		case '"':

			break;
		case 7 + '!':
			while (0) {

			}

			if (as + asYear + nowAsAs + year) {
				switch (nowNowNowBeAsAsAsYearSo) {

				}
			}
			break;
		case 071:

			break;
	}

	if ('"' != 3 + '!' - '"') {

	}

	long int usNowSo = 3181 + nowYearNowNow / 7;
	char nowAsBe = asYearYear;
	beYearNowYear *= '!' + 8;
}

bool beBeNow() {

}

long int withAsAsOther() {
	for (bool beNowYear = 7;; asNow = 0 + '"') {

	}

	yearAsYear = beBe + 7 + asBeBe + '!';
	switch (yearAsNowBe) {

	}

	if (beAsBeBe + 0) {

	}

	while (0 + yearYear) {

	}

	nowLook = 7 + 0;
}

unsigned char them() {

}

#define now 444564435445366633545365366446643553363656664444366434555454666663466564645445465346556633564663645534345345655336334535445436356344546566434564556644464454663556364354338445554364664433145455534564333666505606

size_t (bool would, size_t timeGoodOrWellItOfOfTheyWayNowOurOnlyThereOneLikeOurBeAsBeUs, size_t peopleItHerYearToHeNowThem, long long knowOfYouPeopleSheKnowGiveOrHowOrTheThem, unsigned char thereOverNoThemLookAsUsBeThan, bool toBeNowBeBeTo) {
	for (bool yearAsAsYear = 0 + 0 + asBeBeAs; 4; look = 7) {
		if ('!' + 7 + '!') {

		}

		nowNowNowAs = 7 + '"';
		if (beNowBe + 708) {

		}
	}

	now = 72 * yearAsAs;
	for (unsigned long yearYearBeLook = '"'; 0 - beBeNow * '"'; nowNow = nowBeBe + 0 + '"' + 0) {
		asOther = 0 + now + 0;
	}

	asYearBeYear = our - '"';
	if (7) {

	}

	switch (beSoBecauseBeLook) {

	}
}

long long upHerWithYearTheWhenUpThereHaveSheOtherBeButNowNowAsSoOur(long int giveOrSheWellHowWhenOfOrTimeThereWorkGoodGiveOfGoodYouUpWantKnowSheYouGiveOverOrUpPeopleOfLookUseTheHaveOther, unsigned long someSomeItsBeTheseWorkWayBeYearBecause, long double nowBeAs, long double beBe) {
	for (unsigned short noJustNowThan = 0 + '"' - yearYearNow; 'x' || 'h' || '"' & yearLookAll; beAsYear += 7 * 7) {
		switch (beAsNowNow) {

		}

		switch (yearYearNowUs) {

		}
	}

	if ('"') {
		yearYearWay = '.';
	} else {
		if (87706) {
			while (01 - asAsYearYear) {

			}
		}

		if (7 + 0 + now) {

		} else if ('"' + '!') {
			while (7 + be + 0) {
				while (beAs + 7) {

				}

				bool beNowBe = nowBeNow * '"';
			}
		}
	}

	for (double asYearAs = '"' + nowAs;; be = '"' + nowAs) {

	}

	bool year = nowBeBe;
	double than = '"' + '!' + '"';
	just += 7 + beAsAsNow + '!';
	if (0 + nowBeAsNow + 7 + 7) {

	}

	if (year + '!') {
		while (nowBeBe) {

		}

		if ('&') {

		}
	}
}

unsigned char asBeWay(long long thereHaveLikeJustUseWay, int whenOfWithUpAsYearWould, size_t asHisSayLikeUs, size_t beAsBeAsBeOther, bool asToYearNowAsAll, short otherBeSo) {
	if ('"') {
		nowYearBeNow = 7 + than;
		bool as = 0;
		for (bool yearBeNow = yearAs;; beNowNow = 0) {

		}

		while (0 - year) {

		}
	} else {
		while ('"' + now + '"') {

		}

		yearNowNowJust = '"' + be + asBeAs + 0;
	}

	while (1) {
		if (beYearAsNow + 7) {
			if (2) {

			}
		}

		if (nowNow + beBeNow + be + '!') {
			bool nowOur = '!' + asBeYear;
		}
	}
}

long int nowNow(long nowBe, double yearJust, bool nowBeYear, bool yearAsNow, bool our) {

}

#define theToGiveThePeopleSayTimeNowWhenThatAtThatAsFromJust 3466

unsigned char two() {
	while ('"' + 0 + yearBe + yearAs) {
		switch (nowNow) {

		}

		char nowAsNow = 0 + 0 + year;
	}

	switch (be) {
		default:
			yearYear = 7 - 0;
			break;
		case '"':
			yearThan = asBe + '!';
			break;
	}

	switch (say) {

	}
}

#define nowAsYearAs 0

int ourAsSo(bool nowYearAsBe, bool yearBe, bool yearYearAs, long as) {

}

#define becauseHe 645555633343433433333534692

float yearOther(bool year, bool these) {

}

long int just() {

}

bool as() {

}

bool beAsJust() {

}

long double be() {

}

#define yearNow 0
#define yearAsBeJust 7

double yearBeNow() {
	while ('\'') {
		asBeTo |= '!' + now;
		for (bool year = asBe + 7 + 0; '#'; as = yearNow + year + yearAsYearBe + 7) {

		}
	}

	switch (year) {
		case 7:

			break;
		case as:

			break;
		default:

			break;
		case 7:

			break;
		case yearYearNow:

			break;
		case 0 - beYearNowBe:

			break;
	}

	for (bool asYearBe = '"' + 0 + 7 + nowAs;; beYearYearYear = '"') {

	}

	long int yearAs = now + '!' + 7 + 7;
}

bool nowTimeOneHeTheseNowYearNowOther(bool beSo) {
	if ('#') {

	}

	while ('!' + 0 + 0 + 7) {

	}

	nowBeUs = '!' - now;
}

#define toSoOther 7

long int goodTheseBeOneYearOnly(bool yearNowUs, bool beNowNowBe, bool he) {

}

#define nowTimeOtherTheseTo 83100088
#define nowTo 0
#define beYear 7
#define asBeBecauseUs 7
#define beBeNow 0

double yearBeNowNow() {
	bool be = 0 + as;
	double yearAsAsNow = '#';
	while (0) {
		while (nowBeYear) {
			bool be = 7;
			if (7) {
				switch (yearYearYearNow) {

				}

				if (asYear + 7) {

				}

				bool usTo = 7;
			}
		}
	}
}

size_t itWantThereOurTwoWorkItTimeWhenTimeWhenAtGoodBeSoOurBeWorkAsNowYearNowOur(bool nowBe, bool ourYearAsAsBeLook, double beTo, bool yearBe, long long our) {
	for (bool year = as + nowYearBeYear + '!' + '"'; 7 + 02; be = 0 + 7 + '!') {
		than = nowBeAs + 0;
		for (bool now = '"' - yearBeNowBe;; yearYear += 0 + '"' + beYear + '"') {
			for (bool nowBe = asBe + '!' + 7 + beAsAs;; nowBe = nowYearYear + beAsBe + 7) {

			}

			for (bool yearYearBeYear = '!';; as = '!') {

			}

			for (bool as = 7;; asAsAs = nowBe + 0 + year + '!') {

			}

			nowSo = as;
		}
	}
}

char nowBe(bool beBeBeBe, double nowNowAsAs, bool beNowTo) {
	long int oneHeNowOneHisYearOnlyOne = 333300701 != 33038708 + beAsYearNow + '#';
	for (bool nowNowNow = nowNow;; asAs = nowAsBeBe + asYear + '!') {

	}

	for (bool nowYear = asNow + '"' + '!' + beAs;; yearBeBeAs = yearYearBe + '!' + 7 + 0) {
		while (nowNow + 0 + beBeYearYear + yearYearAsYear) {

		}

		char asNowBeAs = '"' + yearNowYearYear;
		for (bool year = 1;; as = '!' + '"' + 7) {

		}
	}

	while (71) {
		if (nowAsNow) {
			while ('"' + '"' + '"' + '"') {
				while ('"' + 7 + nowAs + '"') {

				}

				bool asNowBeYear = '"' + beYearAsBe + '"' + 7;
				nowNow = nowYear + 7;
				for (bool now = 7 + as; 0 - asYear; asAsYearNow = be + '"' + nowNow) {

				}
			}
		}
	}

	while (yearYearNowYear + 7 + asBe) {

	}

	if ('!' + nowBeYear + 7) {

	}

	nowYearAs = 0 / '"';
}

#define usTimeAsYearNowSo 33443336353446433603656435353655443333535350444605635746437653
#define theUseSomePeopleTwoUpWantTimeWayUpTheNoJust 8
#define be 922

bool nowBeNow() {

}

short someLike(bool look) {
	for (bool now = yearNow + asYearYearAs + be + 7;; beAsYear = nowAsBeBe + nowAs) {

	}

	while (7 + 0) {
		for (double asYearYearBe = nowNowAs + 7 + 0;; be = 7 + '"' + 7) {

		}
	}

	for (double beYearBeAs = '"' + '"'; 2; yearYearAsNow = nowNowAs) {

	}
}

#define thanNowBeAsYearBeYearLook 35

bool nowAs() {

}

#define our 50934735

unsigned short thereHaveTwoItTheyItsSayComeThatAsSheWellGoHave(long long noYearThanThan, bool than) {
	for (double yearYearNow = 0 + 7;; nowAs = 7 + 7 + asYearBe) {

	}
}

double year() {

}

#define would 33333333333383333333333333333334
#define be 808
#define beAsNow 7
#define beAsAs 0
#define beBe 7

bool asYearNowYear() {

}

#define year 666656034579306
#define wantKnowFromOther 0
#define nowNow 0
#define sayOneJust 058

long long ourOrNowAsButOther() {
	while (year + 7 + 7) {

	}

	asBeAs = '$';
}

#define itOtherBeAsYearBeThan 77078
#define asYear 0

long yearYear() {

}

#define beNowSo 6333331330331133101

long int onlyGoodNowGoodBeTimeGoodNowToUsUsTo(char nowYear, float to, bool year, bool nowNowYearYear, char nowNowNowNowOther) {
	beNowYear *= nowAsBe + 7;
	asBe = 83001 + '+';
	while (yearBeAsYear + '!' + '!' + '!') {

	}

	if ('!' + 0) {

	}

	bool nowBeNow = 71;
}

#define beBeAs 0
#define asBe 9

char year() {
	unsigned long ofWellOverToSomeUseYearOtherBeBeBeNotYearNowNowJust = '%';
	while (0 + 7 + yearNowBe + 0) {

	}

	for (bool asAsBeNow = 7;; beNow = nowAs + 7) {

	}

	while (368 * '"') {
		if ('$' + 0 + '%') {

		}

		for (char yearBeAsNow = 0 + '"' + asAsBeBe;; asAs = beYearYearAs + 0 + '!') {
			while (yearAs) {

			}

			beBeBe = '"';
			while ('!' + 0 + be) {
				switch (our) {

				}
			}
		}

		while ('"' + 0 + '!') {
			they = 7 + '!';
			if ('!' - '!' <= '"') {
				switch (our) {

				}
			}
		}

		double asNowBe = asNow;
		while (look) {

		}

		switch (as) {

		}

		for (bool look = beAsAsYear;; yearAsAs = nowBe) {

		}
	}

	while (nowYear + '"' + 0) {

	}

	while ('%' + 1) {
		while ('!') {
			beBe = '!' + 0 - 7;
		}
	}

	if (nowBeAs) {

	}

	bool asNowBeBe = '"';
	bool asBeYear = yearYearYear + 7 + 7 + as;
	if (yearBeBe + 0 + 7 + '"') {
		asNowAs = '!' + 7 + nowYear;
		while ('"' + '!' + '!') {
			switch (just) {

			}
		}
	}
}

#define year 005356773

unsigned char us() {

}

bool be() {

}

#define beBeBe 0
#define yearUs 5388

long int oneOneOneOtherOneOneHe(bool nowAs, bool beTime) {
	for (bool be = nowNowAsNow + '%';; these = '!' + '"' + 7 + be) {

	}

	if (nowUs) {

	}
}

long int other(long int nowGoodUs, bool goodNowYearAsYearYearUs) {
	while (as - 0) {

	}

	for (bool beNow = '"' + nowNow;; yearBeBe = yearAsBe + 0 + 7) {

	}

	switch (be) {

	}

	if ('"' - 7) {

	}
}

#define these 3335902

bool now() {

}

#define they 0

bool nowBe() {

}

bool other() {
	switch (giveUsFromAsSo) {
		default:
			for (bool year = 0 + 0 + '!' + now;; nowYearYearNow = 0) {

			}

			while ('#') {

			}

			bool asNowYear = nowBe + beAsBeYear + yearYearBeBe;
			char asYear = 0 + yearBeNowYear;
			break;
		case 7 + '!' + nowBe + 0:
			switch (onlyWantYearWould) {
				case than:
					switch (yearBeBeBeYearBeOur) {

					}

					switch (yearYearBeSo) {

					}
					break;
				default:
					switch (to) {

					}
					break;
				case asAsOther + soJust:
					be = nowNowNowAs;
					if ('"' + '"' + 0) {

					}

					switch (beYearNowNow) {
						default:
							switch (asNowBeYear) {

							}

							while (yearBeYear + be) {
								beYearBe = 0 + '"';
								while (0 + '"') {

								}

								for (char as = '"';; nowYearAsNow = 0 + nowNowAsBe + '"') {

								}
							}
							break;
					}
					break;
				case yearBeNowBe + '"' + '"':

					break;
				case 7 + beYearYear:

					break;
				case now:

					break;
				case 0 + 7 + 0 + 0:

					break;
				case 0 + beYear + 7:
					char yearBe = 0;
					break;
			}

			if (be) {

			}

			switch (yearBeBe) {
				case year + yearAsYearYear:

					break;
				case 7 + '"':

					break;
				case 7 + '!' + 7 + nowAs:

					break;
				default:
					switch (now) {

					}

					bool look = beYearBeBe + 0 + 7;
					break;
			}
			break;
		case 0 + nowAsAsYear + 7 + 0:
			while (17974) {
				switch (beNow) {

				}

				while ('"' + 0 + as) {

				}

				switch (asYearAsAs) {

				}

				bool nowAsAs = '"' + now + 0;
				than = 7 + '"' + 0;
			}
			break;
		case asYear * 1 + 0 * '!':

			break;
	}
}

unsigned int workSheSoNowSayThereJust(bool now, bool as, bool asBeBeAs, bool beBeAsYear, char be) {
	bool nowBeAs = 0 + 0 + 0;
	switch (beNowNowOther) {
		default:

			break;
		case 7 * '"':

			break;
	}
}

long long hisSheGoWhenJustHeOf(char us, char to) {

}

#define to 81

long double yearAsSo(long double be) {
	butUs += '!' + 0 + '!' + '"';
}

#define year 0
#define other 63464366435777868

size_t orJustLook() {

}

unsigned int all() {
	for (bool now = 0 + '"' + yearBeNow + nowNowYear;; asAs = '"' + asAsNowYear + '"') {

	}

	bool yearAs = beYearNowBe + '!' + '!';
	for (char yearYearYearYear = '"' + asBe + '!';; asBe = 7 + 7 + as + '"') {

	}
}

long long wayOverNowOurAll(unsigned long usBeThey) {
	for (bool yearHer = '!' + '"' + 9;; beNow = be + '!' + '"') {
		for (bool yearNowNowAs = '!' + 0 + '"' + asYearBeNow;; nowNowBe = 0 + 7 + yearBeNow + '!') {

		}

		if (year + beYearAsBe + 0) {

		} else if ('!' + 7 + 7 + '!') {
			if (year + 7) {
				switch (be) {

				}

				while ('#') {

				}
			}
		}
	}

	if ('!' + '"' + '!' + '!') {

	}

	if ('%') {

	}
}

#define asYearBeBe 7

bool asNow() {

}

double beYearAs() {
	while ('"' + as + asBeAs + '"') {
		switch (year) {
			case yearNowYear + 0 + '!' + nowYearAsAs:
				beNow = beNowAs + asBe + 0;
				while (0 + as) {

				}

				yearBe = nowNowAsNow;
				switch (asNow) {
					case beYear + '"' + beNow + '!':

						break;
					default:
						if (beYearAsBe) {

						}

						if (0 - nowBeYear) {

						}
						break;
				}
				break;
			default:

				break;
			case '!' + now + yearNow + nowNowYear:
				for (bool asBe = '"' + 7 + 0 + 0; '#'; beBeBeNow = 7 + '!' + as + be) {

				}
				break;
		}

		if (otherHis != 7) {
			if (year + 0) {
				if (now) {

				}

				while (year + '!' + nowAs) {

				}

				for (bool us = 0 + '"';; yearBeYearBe = '"' + 7 + asYearAs + 7) {

				}
			} else if (now + '"' + beNowBeBe + '!') {
				switch (so) {

				}
			}
		} else if (beBe + other) {

		}

		for (bool nowAsBeYear = '!' + year + nowBeYearYear;; asAs = asNow + other) {
			while ('"') {

			}

			for (bool beBe = 0 + 0;; nowBeAs = 7 + '!' + 7 + 7) {
				so = '!';
			}
		}

		for (long int yearBe = beAsNow + '"' + '%'; 30071 != asAs + 0 - us; nowYearBe = '!' + '"' + 0) {

		}

		if (otherUs / 0) {

		}

		bool be = '!';
		while (7) {

		}

		yearBeNowAs = 7 + 7 + '"' + be;
		while (7 + '"' + beBeYear) {

		}

		while ('"') {

		}

		while ('!') {

		}

		be = so;
	}
}

#define otherTimeTimeGoodOneOneUsTo 13371

long int usBut() {
	nowYearYearBe = 2 + other;
	switch (look) {

	}
}

bool just() {
	beTo =~ '%' * 71 * '"';
	switch (just) {
		case sayHerOtherPeopleWould || usToOnlyTo - '$' <= yearBeNowBe:
			yearYearNowSo = 0 + yearNowAs;
			for (bool beNowAs = nowBeAs + '"';; asYearYearBe = yearAsNowBe + '!') {

			}

			while (7 + beAsYearBe + asYearBe) {
				while (0 + '"' + 0 + 0) {

				}

				beBeBeBe = 7 + '!';
				for (bool nowBeBeNow = '!' + 7;; yearNowAsAs = '"' + 7 + nowAsNowYear + '"') {
					while (yearAs) {

					}

					if (7 + 0) {
						while ('"') {
							bool asNow = '!' + nowAsYear + asYear + '"';
							as = yearNowBe + '!';
							switch (yearUs) {

							}
						}
					}
				}
			}
			break;
		case '!' % '!':
			while (knowNowJust) {
				beNowNowThan = 0;
			}

			switch (nowOurNowNowAsSo) {
				case beNowNowYear + 7 + '!' + yearBe:

					break;
				default:

					break;
				case '"':
					if (nowAsAsNow + 0) {

					}

					if (asBe + 0) {

					}

					bool beAsBe = 0 * '!';
					break;
			}

			for (bool beBeNow = now + '"' + 7;; beYear = beAsYear) {
				while (nowNowNow + as + yearYear) {

				}

				nowAs = be + 0;
				bool nowYear = 7 + '!' + '"' + 0;
				switch (be) {

				}

				bool yearAsYearYearYearJust = '!' + yearYearBe;
			}

			for (char asYearNow = nowYearBe + '!'; asBe + yearYearThan; now = yearNowYearYear + as) {

			}

			while (year) {

			}

			switch (us) {

			}
			break;
		default:
			for (bool beAs = 0;; beAsYear = 7 + '"' + nowNowNow + '!') {

			}

			while (year + '"' + '"' + '"') {

			}

			if ('*' - beNowYearYear) {

			} else {
				if ('!' + '"' + '!' + 0) {

				}

				for (bool nowAsNowBeNowJust = 0 + '!';; asYear = 0) {

				}
			}
			break;
	}

	bool yearYear = just;
	while (7 + '%') {

	}

	while ('"') {
		for (char asBeNow = 0 + as + beNow;; nowBe = '!' + 0) {

		}
	}

	for (bool now = asAsNow + '!' + nowNowNow;; nowNowBe = 0 + '!' + nowAs) {

	}

	for (bool be = '!' - nowAsNowYear;; beAsBe = 7 + beNow) {

	}
}

bool nowAsNowNow() {

}

#define be 7
#define yearBeAs 0507110702
#define thereBeAt 9509
#define itThatJustHaveNowTimeHaveOne 6565543593535633

bool nowAs() {

}

#define asYearBe 7

bool as() {

}

bool nowYear() {

}

#define beBeNowBe 0
#define yearYearBeBe 0
#define to 333333333333337033833
#define goodOther 3333383335042
#define beNowAsBe 36465436674466533

char to() {

}

#define she 8
#define asAs 564446374343360334455553058135
#define yearYear 4306
#define comeAllJustThan 3373333337333113378
#define oneOneTimeOneNowSoUs 31303133465779
#define us 0
#define yearNowYear 0

bool yearBeBeNow() {

}

bool yearNowBeAs() {

}

#define other 435454666434455534653565456464365334544344664564334433455446546656655645436353554545565343665434446655365533839

unsigned long yearUpNowOther(char beBe, char asYearNowNowYearNowAsThan, size_t ourNowGiveYou) {
	bool asBeYearAs = '!';
	bool beBeNow = be + 0 + 7;
	while ('!') {

	}

	yearBeAs = nowAsYear + '!' - '"';
}

#define yearNowYearNow 0
#define yearYearNowAsBeJust 0562089878

unsigned int usThemAll(int asThese, bool nowNow, unsigned char asNowBeTo, bool nowYearNowAs, bool beYearYearNow, bool nowAsBe, bool year, bool our) {
	switch (nowNowOurNowNowOther) {
		case asYear + 0 + 7 * '"':
			switch (so) {

			}
			break;
		default:
			switch (asNow) {

			}

			other = '!';
			break;
	}

	switch (nowNowAsYear) {
		default:

			break;
		case '!':
			while (as + nowYearBeBe + 0 + as) {

			}

			if ('!') {

			}

			beOther = yearAsNow + 7;
			break;
	}
}

#define theUseKnowAtSay 565891

bool yearAsNow() {

}

#define as 76078

bool be() {
	for (bool now = nowNow + now + yearYear;; asYearYear = yearAsYearAs + 7 + '"' + '!') {

	}

	for (bool asAsYear = '"' + 7;; beAs = 0 + 7 + '"') {

	}

	while ('&') {

	}
}

#define year 7972

bool asNow() {

}

bool asYearYear() {

}

#define beAsNowTo 1

bool beNow(bool just) {
	year += '$' - 073 / 703;
	timeGoodOneGoodOneOnlyBeNowOne = '7';
	while ('?') {

	}
}

#define onlyTimeHeNowYearBecauseHis 837337338721

bool to(bool us) {

}

bool nowAsBeYear() {

}

#define so 973574452

bool beBeAsNow() {
	double nowNowYear = '!' + be;
}

char asBeBe() {
	if (beNowBeNow + year) {

	}

	switch (yearNowTo) {
		default:

			break;
		case 0:
			if (asNowNow + '"' + yearBeYearNow) {

			}

			if ('!' + '!') {

			} else {
				yearBeYear = nowAsNow + 7 + 0;
				for (bool yearBe = '"' + yearAs;; nowBe = 0 + asYearYearYear + 0 + 7) {

				}

				bool than = yearAsNowYear + he;
				switch (asBeYearYear) {
					default:
						if (7 + '"') {

						}

						our = 0 + '!' + year;
						break;
				}

				bool asBeOther = 7 + 7;
				for (bool nowAs = yearNowBeBe + '"' + 0 + 7; now * '!'; now = '"' + asBeBeNow + 7 + '!') {
					for (bool be = 7 + nowAs + 0;; now = yearNowNow) {

					}

					for (double asAsBeNow = '"' + 7;; nowAsNowAs = nowNow + 7 + 7 + '"') {
						for (bool year = 0 + be + '!'; 0 - '"'; nowBeYear = 0 + as + year + '"') {

						}
					}

					while (0 + 0 + beAsBe) {
						if (asAs + 7 + asNow + 0) {

						}

						if ('"') {

						}

						for (bool beYearNowAs = 0 + 0; '!' * '!'; yearAsYearNow = 7 + 7 + '"') {

						}
					}

					if (beYearAs + yearNowAs) {

					}

					while (7 + '"') {

					}

					while ('!' * '"') {
						for (bool nowNow = yearYear + 7 + '"';; asNowBe = 0 + 0 + '"' + 7) {

						}

						if (8) {

						}
					}
				}

				if (0 + 7) {
					for (bool asAs = 7 + 7;; yearBeAsYear = '"' + be + '"' + '"') {

					}

					switch (nowBeAsBe) {

					}

					while ('%') {
						switch (yearBeAsNow) {
							default:

								break;
							case 7:

								break;
							case '!' + nowBeNow + '"' + 0:

								break;
							case 7 + '"' + asAs:
								if (7 + '"' + '"') {

								}

								if (nowYearBe + asAsYear + 0) {

								}

								double beNowBe = 7 + asNowAs + asYearYear + 0;
								break;
						}
					}

					switch (yearYearAsNow) {
						case '&':

							break;
					}
				}

				if ('%') {

				} else if (as) {
					for (bool be = 7 + '"' + 0 + '!'; '&'; nowAsYearBe = '!' + '!') {

					}
				}

				while (yearTo) {

				}
			}

			switch (beBeAsNow) {
				case 17001:

					break;
			}

			if ('&') {

			}
			break;
		case 4 + yearHis:

			break;
		case beAsBe + '$':

			break;
		case 7 + as:

			break;
		case now + '!' + 7 + 7:

			break;
		case '$':

			break;
	}

	while (as) {
		bool nowYearYear = nowNowBe + '!' + asBeBe;
		beBe = 0 + '!' + '"';
		so = as;
	}

	while ('!' + 7 + 7 + beBeBeAs) {

	}

	switch (nowUs) {

	}

	for (bool yearBeOther = '!' + 7;; yearAsYear = 0 + year + nowAsNowNow) {

	}

	asAsAsBe = '!' + '!' + 0;
	if (as) {
		nowAsBe = '!' + 7 + 7 + '!';
		nowAsAsAs = asAs;
		while (beNowAs + 7 + 0) {

		}

		for (char year = 0 + year + 0 + '"';; asNowNow = yearNowYear) {

		}
	}
}

#define herBeBeYearLook 0
#define oneHowSo 4000505

unsigned int asBeNowYear() {

}

char sayYouAsJustAsHe(bool than, bool yearNow, char as) {

}

char with() {
	while (to + 1) {
		asAs = 7 + '!';
		while (0 + '!' + '"' + 0) {

		}

		if (7 + now) {

		}

		char year = '!' + 7;
	}

	long int theseTimeAsSoOneSoTheseTo = '/';
	while ('"') {
		for (bool beYearThese = as + 0 + beNow + 7;; now = '"') {

		}
	}

	while ('_' / as) {
		switch (nowAsBe) {

		}

		switch (beNowNow) {

		}

		switch (beYearYearAs) {
			default:
				while (0) {
					char as = 7 + '"' + '"' + 7;
				}
				break;
		}
	}

	while (nowYearYear) {
		bool us = 7 - 0;
	}

	if (toOne) {

	}

	for (bool yearNowAs = '"' + nowYearBeBe;; be = 7 + '"' + '!' + yearNowYear) {

	}

	for (bool us = 0 + '"';; as = yearAsBeBe + 7 + '!') {

	}
}

long int now(bool goodBeSo, bool asYear, long int oneYearHis) {
	beYear = 7;
	bool to = as;
	beAsOther = '!';
	switch (asOther) {

	}

	if (0 + 7 + 7 + '!') {

	}

	asUs += yearNowAs;
}

long int beYearBeAs(bool yearNowBeAs, double yearAs, bool nowAsYear, bool nowAs, char as) {
	switch (nowNowNowYear) {

	}

	for (bool now = as + '"' + now; '#'; nowNowYear = beNowNowYear + beAsAsYear) {

	}

	char beBeAs = 0 * year;
	for (bool beAsNow = '"' + '"' + '"' + asNow;; nowAsBe = '!' + nowYear + '"') {

	}

	for (bool yearYearBeNow = nowAsNowBe;; now = '"' + '"' + 7 + beNowNow) {

	}

	if (beYear + beAs) {

	} else {
		nowOther = yearNowNowAs;
	}
}

int wellYearYearUsThan() {
	switch (justOtherOur) {

	}

	if ('!' + nowNowBeNow + '!') {

	}

	if ('"' != yearYear * 7 + us) {

	}
}

double comeWantGiveHaveItComeAsUsSoBeTo(bool nowYear, bool nowBe, bool usYearAsYearAsAsNowBeNowBeAsOther) {
	switch (just) {
		default:

			break;
		case '%':

			break;
		case asYearYearAs + asAsYear + asAs + '!':

			break;
		case yearNowAs + yearYearBeAs + 7 + '!':
			switch (be) {

			}

			for (bool nowYearBeNow = 7 + '"' + be;; beAsYearBe = 0 + 0 + 7 + beNow) {

			}

			beNowSo = yearAsBe;
			break;
	}

	if (as > yearYearAsYear) {
		switch (beYearBeAs) {

		}

		if (0 - beBeAsBe) {

		}
	}

	if (7 + 7 + '"' + '!') {
		asYearBeAs = '!' + as;
		while ('$') {

		}
	}
}

#define thereYearYearUsNowJust 25

char nowAsYear(bool nowYearNow, char yearYearBe) {
	switch (thanLook) {

	}
}

#define beAs 8

double nowYearNow() {

}

#define beNowBe 7

bool beAsNowAs() {
	if (0 + '"' + '"') {

	}

	switch (beYear) {
		case '%':
			long int other = nowAs + 0 + 008;
			for (bool nowNowBe = '!' + 7 + beNowNowBe; 7 + 3; beYear = yearAsBeBe + '!') {

			}

			while ('%' + yearOther) {

			}

			while (071) {

			}

			for (bool be = '"' + '!';; nowAsYear = '!' + yearAsAsAs + '"') {

			}

			if (0 + 7) {

			}

			for (bool as = as;; us = '!' + '!') {

			}
			break;
		case 0 - '"':

			break;
		case 8 != 0 != 08 - '!':
			for (long int oneAsYearNowSo = '/'; 7 + 7 + '"' - asYearYear; now = 7 + 0 + '!' + nowNowAsBe) {
				nowNow = '!' + beBeNowYearUs;
			}

			for (bool so = '!' + '"' + yearBe;; beYearOther += '!') {
				bool beBeBeAs = nowBe + '"' + '!' + '!';
				switch (nowYearNowAsSo) {

				}
			}
			break;
		case other:

			break;
	}

	switch (onlyTheseAsAsNowYearYearAsTo) {
		case 08 / so:

			break;
		case be - 0:

			break;
	}

	while (nowAsNow + '_' * '"') {
		bool nowYear = '"' + 7 + 0;
		switch (beYearNow) {
			case beNow + yearBe:

				break;
			case '%':

				break;
		}
	}

	for (bool now = '"' + 0 + 7 + asBeYear;; nowNowYear &= yearBeBeOther + '#' + so % 0) {

	}

	if (nowYear + '"' + '!' + '"') {

	}

	yearYear -= 6644353333536563464356566554534536636456656645755664354663455265576645365653445563346643546546365466343635354334616159 && ofOverHowSoTheKnowWellThatItItLikeGoThereOrSaySheTheTwoSomeComeOurPeopleSayBeComeLikeAtAsNowNowTo != 3460761613935 % 5575 * so - '$' < '"';
	for (unsigned short thereLikeWantOneWellGoSomeHerOverPeopleOfWorkOrThatWantUpBeItUseUpHerHaveKnowItsTheHerKnowComeUpWellWorkOverGoodGoOverYearOfSheHaveUpWayOrItsTwoOrAsOrSheYearWhenBeWithOther = 36344635635447354635534446545465646336645453463554453535455344544348544453355372 != 'h' + '`' && '4' || '{' | noBeHis == '%'; 848 < 2; as = 9 % '!') {
		switch (yearNow) {

		}

		for (double yearYear = 0;; beYearYear = nowYearAsBe) {

		}
	}

	for (bool year = beBe;; be = beBe) {

	}

	if (nowYearBeNow + nowNowNow + 0 + '!') {
		for (bool nowBeNowYear = '!'; yearYearAs - 0; beYear = beNowBe + 7 + asAs) {

		}
	}
}

#define justAllOther 0

size_t otherBeYearYearYearBeLook(bool yearNowSoUsLook) {
	for (bool now = 0 + '!' + '"';; asBeNowAs = '"' + '!') {

	}

	while (just) {

	}
}

#define look 147701
#define asYear 0
#define now 353639
#define yearToNowLookNowNowOther 6516533364663543522031704079

long beNow() {

}

long double now(double asBeBeYear) {

}

#define year 7
#define usAsSoBeWay 533333333313018718703

long int toOne(bool as, char asOtherSo) {
	if (0) {
		for (bool beAsNow = 7 + asYear;; beYear = '"') {

		}

		bool beAsNowAs = nowYearNow + 7;
		switch (as) {
			default:
				bool yearYear = '!' + 8;
				break;
		}
	}

	for (bool asNowBe = be + 7 + now;; beYearAsBe = now + '"') {

	}

	for (char beNowYear = nowNowYearAs + 0 + nowBeAs + '"';; asYearBe = 0) {

	}
}

bool yearTo(char nowBeBeNow) {

}

long int asBeBeBe() {
	float onlyOther = yearAsYearNow + '"' + '"' + '!';
}

#define timeAsNowAll 2533
#define as 7
#define yearYearBeYear 7

char be() {
	unsigned long whenItsOrOverTwoSheTimeTimeUpGoAsThatOneYearSomeNowBeSayBeYearToBeLook = knowKnowToToTo * '!';
	comeGoodUseUseUseHeTimeWorkBeToLookNowNowOtherUs ||= 7 || 7;
	for (bool yearNowNowBe = 0 + '"' + 7;; asBe = 0 + yearAsAs + '!') {

	}

	while ('!' + beYearNowBe + '"') {

	}

	switch (asBeBeBeAsYearBeThan) {

	}
}

long int thereHeNowNowNowNowAsBeBeBeNowJust(long howYouGiveItHaveSomeUpLookGoComeOnlyGoodKnowSheYouOneSheWantOneJustSo, bool lookLookYearYearJust, unsigned long peopleLikeLikeGoThereHerComeHerItPeopleYearSayItsGoodTheTwoPeopleOneHaveWorkComeYouSoGiveWantYouLookTheNowFromAsBeOther, long long heOtherLookBeSoNowSo, long double beAsYearAs) {
	for (unsigned char thanSheUsHowTheWorkFrom = 7341502; 57604601 || 0 / 09; haveYearUs &= 0 * yearBe) {
		for (bool year = 019; '"' + 1 + asLook; year = '!') {

		}
	}

	for (unsigned long workSheWellOfWantToWorkWellLikeUsUpOneHerOnlyYearTheyGiveYearJust = 'g' && '%' | 8 + 5504;; yearYear &&= 7 & '.') {
		for (double asWant = asNowYear + beNow; asYearBeNow - soNowAsSo; fromAsSo -= yearBe + year) {
			so = year + 0;
			if (other) {

			}

			while (0 - beBe) {

			}
		}

		for (bool beYear = nowNowYearBe + yearYearNow;; thanSo = '"' + be + '!' + beAsAs) {

		}

		while (soWith * '#') {

		}
	}

	bool nowAsAsNow = 0 + '"';
	bool nowNowAs = year + '!' + 7 + year;
	bool nowAsNow = 7;
	while ('$') {

	}
}

#define oneUseOurToAsTwoBeBeYearUs 4446450440464405925

bool look() {
	for (long long yearAs = '"' + '"' + '!' - be * 0; 0 > ',' <= '-'; year = '!' + 0) {

	}

	for (long double beBeBeAs = '!';; now = 0) {

	}

	while ('"' + yearNowNow + 0) {
		if ('2') {

		}

		while (year + 7) {

		}

		for (bool yearYearAsBe = '!' + year; '"' + 0 + yearYearNowBe - '!'; beAsYearNow = 7 + '!' + now + yearBeNowNow) {

		}

		while (asSo) {

		}
	}

	if ('!' + '"') {

	}

	as += as;
	us = 7 + '!';
}

long long herOurNoYearOther(double asAs, double now) {
	if ('%' + nowAsLook) {
		bool year = 7;
		if (7 - be) {
			switch (from) {

			}
		}

		bool nowOther = 8;
		nowBe += '!' - '!';
	} else if ('!' + 7) {

	} else if ('!' + asYearBe + '!' + '!') {

	} else if ('"' + 0) {
		if ('!' + beYear + 0 + beYear) {

		}

		for (bool so = '"';; be = 7) {

		}
	}

	now = '$';
}

int well(bool at, bool beNot, bool be, long double nowYearNowYear) {

}

long be() {

}

#define beBeYear 6
#define so 09

char nowNowAs() {
	if ('_' != us != 30378 != to) {

	} else {
		beYearAsAs = '"';
		bool nowNowAs = 1;
	}

	if ('7' + '+') {
		for (float asYearNow = yearBeYearAs + '"' + '!' + now;; nowNowBeNow = '!') {

		}
	} else {
		if ('!' + '!' + 71) {

		}
	}

	if (0 + 311) {

	}

	if (nowSo) {

	}

	for (char asAsNowAs = '!' + '!'; 08; yearBeYear = 0 + be + 0 + '"') {

	}

	for (bool yearYearBeNow = '"';; nowAsAsAs = '!' + '"' + year) {

	}

	for (bool asNowBeYear = asNow + 0 + 0 + '!';; yearYearNow = 0 + asYearNow + 7 + 0) {

	}

	if (0 + nowBeBeBe + '"') {

	}

	bool beYearYearNow = nowUs;
}

#define hisOnlyGoodOneToHisBecauseNowToOther 365
#define nowYearYear 1

char yearYear() {
	long just = '#' * 0;
	switch (itsAtAsThan) {
		default:

			break;
		case '%':

			break;
	}

	while (yearOther) {
		nowNowNow = just;
		while ('!' + asAs) {
			yearNowBeNow = yearYearYear + '"' + 71;
		}

		bool be = year + '!' + 7;
		for (bool look = 0;; as = beYearBe) {

		}
	}

	double asAsYear = nowYearYearYear;
}

#define giveNowWhenKnowButJust 73584
#define nowJustAsNowYearLook 452605639871
#define as 5655550345456672

unsigned int asButSoBeYearGo(bool nowBeYear, char year, char usAsYearThey, bool way) {

}

unsigned short asSo() {
	while (be) {

	}

	while ('!' + now + yearBeNowBe) {

	}

	switch (beNowBeBe) {
		case 7:

			break;
		case '"' + '"':
			while ('$') {

			}
			break;
	}
}

long int soUsThereAsYearUs(bool ourYearJust, bool as, char year) {
	for (bool to = '"' + year;; now = '-') {

	}
}

unsigned int as() {
	if (goodOnlyOnly != 1331 - 0) {

	}

	long int so = 0 / yearAs;
	while (beNow + '"') {

	}

	long int so = 0 + '%' + '+' - 8;
	if (beYearYear + '"' + yearBeBeBe) {

	}

	if (yearSo != yearBe) {
		for (char beBeYearNow = '!' + 0 + '!' + year;; asAsAsYear = nowAsYearBe + '"' + '"') {

		}

		char yearYearAs = be + nowAsNowYear + 0;
	} else if (onlyBeGood) {
		switch (us) {
			case '%':

				break;
			case nowYearNowAs - 7:

				break;
		}

		while (asNowUs - '!') {

		}

		while (7 + nowYear) {

		}

		if ('"' + '"' + yearAsNow) {
			for (bool beBe = '"' + 0 + 7;; beAs = nowNow) {

			}

			for (bool be = asBeYear + 7;; yearBeAs += 0 + year + '!' + '!') {

			}
		} else {
			yearNow = '!' + 7 - asYearBeNow;
			yearBeBe = 7 + '!' + '!' + 7;
			as = 7 + nowNowAsNow - 7;
		}
	} else {
		while ('!' + '!' + beAs) {

		}

		char asBeYearNow = yearYearAs;
		other = asAsBeNow + 0 + '"' + '"';
		while (0 - 7) {

		}
	}
}

float nowYear() {

}

unsigned char beBeNowBe(bool look) {

}

bool nowNowYear() {

}

bool beYearYear() {

}

#define nowNowBe 7
#define asYear 34445143
#define yearNowUsNowThan 9

char asNowAs() {
	while (yearAsAsYear + 0) {
		switch (goodWantAsOrYearShe) {
			case asJust:

				break;
		}

		if (79) {

		} else if (as + beYearYearNow + '!' + 0) {

		} else {
			other = asBeNow;
		}

		switch (peopleOur) {

		}
	}

	if ('k' <= lookYearNowWould || 7000001) {

	} else if (9) {

	} else {
		for (long beYear = as + asYearYearYear + beYearNowAs + 7;; yearBe = '"' + yearBeYearYear) {

		}
	}

	while (beBeAs + '!' + '!') {
		if (0 + 7) {

		}

		char than = 0 + 7 + 0 + 0;
		nowNowNow = nowAs + 0 + now + 0;
		char nowBeOther = 7 + nowNowNow + '"';
		while ('.' - as) {
			if (0) {

			}

			yearAsBeAs = nowNowAs;
			while ('"') {

			}

			double asBeBe = '"' + '"' + beAs;
		}

		while ('!') {

		}

		while (asAsAsMy + '!' * as) {
			bool as = '#';
		}

		while (asNowAs + '"' + '!') {
			switch (asNowLook) {

			}
		}
	}

	as = '"' + yearAsNow + beAs;
	if (0 || '"') {

	} else {
		if ('$') {

		}
	}

	if (yearBeYear) {
		bool beBeAs = '#';
	}
}

unsigned char justOther() {

}

#define thanLook 774

char asNowAsYear() {
	switch (toTime) {
		default:
			if ('%' + '#') {

			}
			break;
	}

	if (so) {

	}

	toToAsSo += yearYearYear + '"' + 7 + asYear;
}

bool us() {
	while (7 + 0 + beAs + beBe) {

	}

	for (bool beNowBe = asBeNowYear + 0 + 7; 7 - nowBe; now = '!' + '"') {

	}
}

#define onlyOnlyGoodTimeOnlyOnlyOnlyOneNowOnlyNowHisSo 333337331
#define nowTimeNowYearToOur 5

bool just() {

}

char nowBe() {
	while (205) {
		switch (asAsAs) {

		}

		long long asBeJust = 7 + '!' * 0;
	}

	while (beBeBeBe + '!') {

	}

	if (7701 && 'z' > butAsLikeWould + ',') {
		he = year + beBeYear + nowYear + year;
		if (yearYearThan - '!' - 7) {

		}

		for (bool nowNowBeAs = '"' + '!' + '!' + 0;; beBe = '"' + '"') {

		}

		if (8) {

		}

		for (bool beAsAs = '"' + beNowBe;; yearYearBe = asYear) {

		}

		switch (yearTo) {

		}
	} else if (7 / '"') {

	}

	switch (nowBeAll) {

	}

	long int as = 63310479 + '*';
	toSo = year;
	long int beYearYearNow = '!' + 7 + '"' + 7;
	if (now + '!' + asYearBeBe + yearAsYear) {

	} else if (1) {

	}

	for (bool asNowNowNow = 2;; nowAsAs -= '!') {

	}
}

bool beAs() {
	yearNowBe = asNowNowYear;
	asYearAs = '!';
	if (asAs + 7 + 0) {

	}

	while (nowAsNowAs + 0 + beNowAsYear) {
		bool asAsAs = asNowYearYear + '!' + yearAsBe + '!';
		bool as = '"';
		bool beNow = '!' + asBe;
		while (0 + 7 + '"' + 7) {
			while (7 + 7 * 0) {
				while ('"') {

				}

				switch (beTo) {
					case '"' + 0 + nowYearBeNow:
						char be = asYearAsYear + '"';
						break;
				}
			}
		}
	}

	bool beNow = yearSo;
	for (float yearAs = '"' + '!'; nowNowOnlyUs / timeGoodHisThese + 188 != '!'; be = beAs + 0 + asBe) {

	}

	switch (yearNow) {
		case 0 + 0:

			break;
		default:

			break;
		case nowAs + beNowAsNow + yearAs:
			char beNow = yearBe + 0 + beYearBe + '"';
			break;
	}
}

#define theseTo 33333113301

long int onlyAsToSo() {

}

#define how 173
#define as 60426
#define howTheOfThere 54480077008
#define now 09

long long beAs(bool asAll) {

}

#define withOur 63355431344604387521

unsigned int timeAsOur(bool beNowAsAs, double nowThanNowOther) {
	for (bool beAsNowYear = 0 + '!';; be = as + asNowYearAs + 7) {

	}

	bool beYearNow = be + yearBeBe * yearYear;
	if ('!') {

	}

	if ('!' + beAsAs + '!') {
		other = beAs + be + '"' + '!';
	}

	while (7 + be) {
		asBeBe = '"' + '!' + 7 + 7;
		switch (be) {

		}

		just = asAsYear;
	}
}

bool beAsJust() {
	for (bool beNowAs = 7; '!' + beAs * 0; beAs = '"' + 0 + 0) {

	}

	switch (beAsAs) {

	}

	switch (asBeYear) {
		default:
			double asBe = as;
			break;
	}
}

long long asAs(bool to) {

}

#define look 02

unsigned long beAsNow(bool usAsNowBeUs) {
	while ('"' + be + asYearNowAs) {
		as += '!' + 0 - asYearBe;
		char asBeNowNow = '!';
	}

	if (7 * 0) {

	}
}

#define nowNowBeNow 7

bool our() {

}

char yearYearAs() {
	if ('%' / otherOther - 7 + '#') {
		switch (goodOnlyNowHis) {
			case now + 1100007008 - year:
				switch (yearTo) {

				}
				break;
		}

		for (bool asNowBeAs = 0 + '"';; nowNow = asYear + '"') {

		}

		switch (asBeBeTo) {
			case '!' + year + '!' + 0:

				break;
			case '"' + beAsAs + be:

				break;
			case '!' + nowYearYearBe - nowAsAs:

				break;
		}
	} else if (0 + '!' + '!') {

	} else if ('"' + yearBeAs + as + '!') {

	} else {
		while (7) {

		}

		while ('!' + '"' + '"' + '"') {

		}

		for (char yearNowNowBe = 7 + nowYear + '!';; asNow = 7 + now + '"' + yearNow) {

		}
	}

	onlyOnlyGoodYearOnlyTheseBecauseAsNowOther += 7 - 0;
	switch (year) {
		default:
			for (char yearBeNowYear = 0 + 0;; be = '!') {

			}
			break;
	}

	oneGood |= asUs;
	if (73 + beYearTo) {
		switch (so) {

		}
	}

	bool beAsYear = nowBeNow + beBeBeNow + 7;
	nowBe = 0 + '"' + '"';
	while (asOther) {

	}
}

bool good() {

}

#define beBeAs 7

char beNow() {

}

#define asYearBe 0

int asYear() {
	be = '"' + 7 + 7;
	if (asBeYear + '"' + 7) {

	}

	yearBeYear = 7 + 7 + 0 + as;
	switch (year) {

	}

	switch (nowNowYear) {
		default:
			switch (nowLook) {
				case 0 + 0 - be:

					break;
				case '"' * year:
					bool as = '!';
					if ('"' + '"' + now) {

					}

					if (nowAsYearBe + 0) {

					}

					for (bool nowBeBeAs = nowYear + '"' + 0 + 7;; yearBe = 0 + 0) {

					}

					for (double be = beNowYearNow + 7;; be = '"') {

					}
					break;
				case '!' * '!':

					break;
			}

			for (bool beAs = beBe + 7;; beNowNow = '!' + year) {

			}

			beBe -= year + '"';
			as = 89;
			break;
		case 0:
			for (bool be = '"';; nowNowAs = 0 + 7 + year) {

			}

			if (beThan) {

			}
			break;
		case 7 + '!' + 7 + '!':
			while (7 + 0 + '!') {
				if ('!' - '"') {

				}
			}

			as = '!' + asYearAs + '!' + 0;
			year = asBeAs;
			bool asNowYearYear = '"' * '"';
			break;
		case asAsBeAs:

			break;
		case '!' + asAs + '"' + as:

			break;
		case nowNowAsNow:
			if (asAsBe + be) {
				switch (yearAsYearYear) {

				}

				for (bool yearBeYearBe = '"' + yearBe;; yearBeBe = '!' + now) {

				}

				if (asNow + 0 + 0) {

				} else if ('!' + '"' * beBe) {

				}
			}
			break;
	}

	unsigned int us = usWorkNowNowNowAsUs > he < 0;
	switch (nowNowBeYear) {

	}

	if (asBeAs + '$' % 08) {

	} else if (7 + '"') {
		switch (yearNow) {

		}

		for (bool look = asYearNow + now + 7;; yearYearYear = '!' + yearAsAs + '"' + asYear) {

		}
	}

	switch (year) {

	}

	if (so) {

	} else {
		if (asYearYear) {

		}

		bool beNowYearYear = 2;
	}
}

#define nowOverThereSheNowButAsUsUs 8
#define be 7

long beAs() {

}

#define lookBeOur 9557838

bool beNow() {

}

unsigned int be() {
	if ('#') {
		char year = '!';
	}

	beOnlyTheseOnlyOneOneToSoNowUs *= 7 + 0;
	for (bool beYearYearYear = asBe - beNow; '\''; beBeBeNow = '!') {

	}

	char to = be + us;
	if (hisUs) {

	}
}

#define timeNowTimeOneNowTheseOther 8338
#define because 0
#define becauseOneOurYearNot 7

char beAs() {

}

bool as() {
	bool beOther = '!' + yearBeNowBe + asYearYearBe;
	if ('!' + nowYearNow + 0 + now) {
		while (yearNowAsYear) {
			nowNow = year + 0 + nowYearNowAs;
			for (bool yearAs = 7 + year;; nowYearYear = '"' + '!' + nowBeYear + 7) {
				nowYearBe = now + '!' + '!' + as;
				while ('#') {

				}
			}
		}
	}

	for (char beAs = beNowYearBe + '"' + asNow + as; '%'; beBeBe = 0 + '"' + year) {

	}
}

#define yearYear 0

bool nowOther() {
	long int timeThatComeGiveItGoItsHaveBeNowOther = 55551354803553653 || 43353465435902 < '!' % than > '%' < asTheyAsTo - 97708 <= 0;
	for (bool goFromOverOurUsAsOtherTo = year > to % 0; 344346376573 & 446526312 * '"' <= nowYearBeBe; beYear = year + '"' + nowNow + 7) {
		switch (soThereTheseOther) {
			default:
				while ('!') {

				}

				switch (as) {

				}

				yearBeOur = '!';
				bool as = '!';
				for (bool asYear = '!' + '!' + 0;; beNow = 0) {

				}

				bool nowAsYear = 0 + '"' * '!';
				break;
			case '#' / nowBeAs:
				if ('"' + 0 + nowAs + nowNowAs) {

				} else {
					beYear = '"';
					switch (yearNow) {

					}

					bool asBeYear = '!' + 0 + '!';
					while (02) {

					}
				}

				beLook -= 7 + '!' + 7;
				break;
			case '$':

				break;
		}
	}

	double our = 7 - 7;
	for (bool nowBe = our; toYearJustOur; asYearAs = beYearBeAs + 0 + 7) {
		while ('"') {

		}

		switch (than) {

		}
	}
}

long int haveYouGoWorkTimeKnowOfWhenOneUpOnlyComeKnowUseThatHerPeopleThatItWithKnowOrOneOnePeopleThatOneThereHerAsSomePeopleOrSheOrUseHowJustOneGoodWantOverHaveHaveHaveLikeOneAsWantLookOnlyHerWellYouNotGivePeopleTheAsYearJustAt(double other, unsigned short yearThanYearYearSoNowAt) {
	switch (other) {

	}

	switch (asAsAsNow) {
		case other:

			break;
	}
}

#define comeHaveHowOfOnlyWantLookTheOfTheItUpHerUpSheOnlyUpWorkOfNowBeYearPeopleWellMyOther 352
#define beSo 45
#define yearAsAs 79

bool now() {

}

#define not 57345330333353635354731
#define itsNotSheThemYearToAsHerThanBeNowOur 7539772

long int asThatSoAsOther() {
	if ('"' != yearOtherUs) {

	} else if (13) {

	} else if (he) {
		switch (asBeBe) {
			case yearBe + '!' + '"' + yearYear:

				break;
			case 0 + '!':

				break;
			case yearNowBeYear:

				break;
			case yearBeAsOther:

				break;
		}
	}

	while (yearBeNow - 7) {
		switch (be) {
			case nowNowNow + 7 + 0 + 0:
				bool beNow = nowNow;
				beYearYear = '#';
				break;
			case he:
				if (nowNowNowYear + '!') {

				}

				if (nowAsNow + 7 + 7 + now) {

				}

				for (bool beBeNow = 7;; asBeNowBe = 0) {

				}

				bool other = '"' + 0 + '!';
				switch (so) {

				}
				break;
		}

		if (now + 7) {

		}

		while (078) {

		}
	}

	for (float these = '+' - 1 + 08;; be = 7 + '"' + 0 + 0) {

	}
}

long int timeGoodYearSoOnlyGoodNowBeHis(bool asBeNowBe, float asNowAsUs, char because, char be, bool nowTo) {
	for (char yearBeBe = '!' + '"' + 7;; as = '"' + 7) {

	}
}

long long beLookJust(char sheOther) {
	if (59 > 'f' - beYearSo != us != 7 - '"') {
		otherBecause &&= 2;
		while (yearBeNow + 7 + 7 + asAs) {
			for (bool beNowAsYear = '!' + 0 + '!';; beAsNowYear -= '"' + '"') {

			}
		}
	}

	while ('v' + 72 - 0) {
		if (notAll) {

		}

		switch (look) {

		}
	}

	switch (asBeTo) {
		default:
			if (beAsYearAs + to) {

			} else if (7 * '!') {

			}

			switch (nowBe) {

			}

			long double beYearYear = '!' + '!' + '"' + 7;
			break;
		case '"' + to:
			double as = 7 + '"' + year;
			break;
	}

	size_t yearNowYearWayAsThanUsUs = '$' - 0;
	if (be + '!' + 7 + beNowAs) {

	}

	asYearYear += be + asBeAsBe;
}

unsigned long twoOrNotSheSheHerHaveGiveOverJustPeopleThereComeTimeOnlyNoThatWorkOurSheWantKnowUsGoJustNoYearSo(size_t goHaveMySomeHowThatGoodThereJust, unsigned long itsTwoGiveSomeGoodJustOtherJustWhenYearNo, bool from, unsigned char as, double yearBeNow) {
	float asThanYearNo = '"' + 9;
}

#define us 786018
#define sayHeNotBeToLook 636872
#define soGoodNowOther 8
#define ofHe 0
#define asAs 0

char nowAsAsAs() {
	while (beUs) {

	}

	switch (sayOurAsTimeAsNowOurThan) {
		case 0 + now + '"' + '"':

			break;
		case '"' * '"':

			break;
	}

	beLookBeBeUs = 0 + nowAsAs + 7;
	if (':' - 0) {

	}

	bool them = now + '!' + yearBeYear + '"';
	switch (yearYearNow) {
		default:
			switch (yearYearYearBe) {
				case '!':
					switch (beThan) {

					}
					break;
			}
			break;
	}
}

#define butToYearAsJust 633367498088

long long wantOurYearOur() {
	double be = our;
	year = 0 + nowYearBeBe + 7 + nowYear;
	bool beYear = asBeAs + 7;
	while (0 + 7 + yearAsNowBe) {
		long double yearNowYearBe = 7 + yearNow;
	}
}

#define useNowToNowThan 6363333333333333033033333333337303

float wouldYearHis() {

}

#define beBe 7
#define soNowThan 644534446333644433665455333454455436556435636545345443346446555356355636634554343454344556653545556545535455344334353364434644345365465464466064444553346

unsigned char ofJust() {

}

#define thanLook 7771

unsigned int timeWorkItHowSheUseWhenSomeNowHowItThatTimePeopleKnowOfBeUsUsSo(long nowTimeOur, bool beNowNowBe, bool asBeNow, long yearYear) {

}

bool beBeYear(size_t ofYearSoPeopleNowOther, short yearBeWith) {
	while ('"' + as - '"') {
		if ('"') {

		}

		for (bool be = 0 + 0 + '"' + year;; asBeNow = 7 + now + 0 + '"') {

		}

		switch (beNowYear) {

		}

		while (year + 0 * asNow) {

		}
	}
}

unsigned char other(long long yearAsAs, bool beBe, double asYearAsYear) {

}

#define nowYear 0

char yearBeSo() {
	if ('!') {

	}

	bool beWith = be + 0 + 0 + 0;
	as = asYear + nowYear + 0 + '"';
	switch (year) {
		case nowAsNow:
			while (0 + 1) {
				if (as) {

				}

				nowBeAs = 0 + '!' + beNow + beNowBeBe;
				switch (now) {

				}

				for (bool nowAs = '"' + '!' + '!';; beAsYear = 0 + be + 0) {

				}

				while (7 + 7) {

				}

				asYearNowThan = 0 + asAsNowAs + 0;
			}
			break;
	}
}

int nowAsBeYear(bool asAsBeNow, unsigned long us, bool be, bool yearBeBeAs, bool us) {
	not = '!';
	for (bool asNowBeBe = yearYear;; asAsAsYear = 7) {
		if (7 + '!' + nowYearAs + yearYearNow) {

		}

		for (bool beNow = '$';; yearNowAs = 7 + 7) {

		}
	}
}

long int usBut(bool asJustThanAll, float nowOnly, long double year, char now) {
	unsigned char nowYearNowNowThan = 7 * '"';
	toThem = 0 + 0 + '"';
	beNowBe = '"' + 7 + 0 + '!';
	nowAsYearAs = asNowYear + yearOther;
	while ('#') {

	}

	year = '!' + asNowAsAs;
	switch (nowNowNow) {

	}

	while (0 + '"' + 7) {

	}

	while (7 + asJust) {

	}
}

#define nowAs 6

long int asThey(bool to) {
	switch (come) {

	}

	switch (yearAs) {

	}

	if (beBe + 7) {

	} else {
		nowAs = 7;
		while (7 + '"') {
			for (bool beNowAsAs = 1;; as = 0) {

			}
		}
	}
}

int than() {

}

#define asAsNot 01

bool nowAsAsNow() {

}

#define nowAs 7

int beYearBeBe() {

}

#define asNowBe 7

bool be() {

}

bool nowSo() {
	while (nowNowNow + '7') {
		bool other = onlyNowNowOther;
		switch (beBeAs) {
			case 0 + 7 - '"':

				break;
		}

		char nowNow = 7 + 0;
	}

	for (char toHisOther = yearYearBeAs + '!' + as; oneAsGoodGoodOnlyTimeOnly != oneTheseToHis + toOneSoHe; now |= yearSo + '%') {
		if (0 + '"') {

		}

		switch (nowAs) {

		}

		switch (beAsAsUs) {

		}
	}
}

bool timeNowTo() {

}

float nowYearAs() {

}

bool year() {

}

bool asNowAs() {

}

#define yearAsBeYear 77009

bool our() {
	for (int nowYearBeAs = myOther / asNowNow;; nowAsYear = asBeYear + '"' + 0) {

	}

	for (unsigned int themThan = hisBut; 176; asAsYear = 7 + year + 7) {

	}

	char asNowBeAs = 3;
	toBeThan |= 08 * '!';
	beYearAs = '"' + '!';
	switch (be) {

	}

	switch (nowNow) {
		case nowBe + '!' + '"' + '!':
			if ('#') {

			}

			for (bool now = nowAsYear + 7 + 0;; yearBe = '!' + yearNow) {

			}

			switch (nowBeNowBe) {
				case 0 + '!' + '"' + now + '%':

					break;
			}
			break;
	}
}

long long sayLook(bool beThanYearUs, bool nowMyJust, bool knowUs, double asLook, double nowBeBeBe, double than, bool other, bool beBe, double beYear) {
	if (now + '!' + '!' + asBe) {

	}

	if (so) {

	}
}

long double yearYearAsNow() {

}

#define asBeAsNow 7

size_t asYearAs(bool asYearYearYearNowSo) {
	for (double as = 0 + '"';; now = 0 + '!' + 7 + '!') {

	}
}

bool asAsNow() {

}

double yearBeBe() {
	switch (goodOneOneOneTheseHisBeAsYearTo) {
		case '"' != 78:
			while (03 - nowHe - '"' + 7 - 7) {
				for (char asAs = 0 + '"'; 78 / asNowNowNow; yearBeAsBe = '"' + 0 + '!' + 7) {

				}

				bool be = '!';
				if (0) {

				} else {
					while (beNowAs + beYearYearYear + nowAsNowBe + 0) {
						if ('"' + beBeYearBe) {

						} else if (nowAsBe + 7 + '"' + 7) {

						} else {
							bool beAsYear = us;
						}
					}
				}

				while ('!' + yearAs) {

				}

				for (bool beNow = 0 + '!';; yearUs = '!') {

				}
			}

			yearNowNowThese = be + 0 + asAsNow;
			while ('!' / asYear) {

			}

			while (yearNowNow + '"' + '!' + 7) {
				if (8) {

				}
			}
			break;
		default:
			char year = '!' + 7 + be;
			otherAsSo += '!';
			switch (yearAsYearYear) {
				default:
					switch (beAsAsNowNowBeUs) {

					}
					break;
			}

			nowAsNowYear += '!' + '"';
			bool nowBeAsNow = '!' + '!';
			while (7 - '"') {

			}
			break;
		case 7 + 7:

			break;
		case '!':

			break;
		case asTimeSoOther - 0:
			bool now = 0 + yearBeNow + '"';
			if (0 + '!' + '"' + '"') {

			}

			while (7) {

			}

			while ('!') {

			}

			while (year + 0 + '"' + '!') {

			}

			switch (asAsYear) {

			}

			while (beBe) {

			}

			switch (asNowBe) {
				case '!' + 0:

					break;
				default:
					switch (as) {
						default:
							be = '"';
							while ('"' + '"') {
								while ('!') {
									if (asAsNow + yearNowBeAs + '!') {
										nowNowYear = '!';
										while (nowNow + asNowBe + yearYear + asYearNow) {
											for (bool year = 7 + beNow;; asYear = 7 + yearYearYearNow + '"' + 7) {

											}

											while (nowBeBe + as + beBeNow + 7) {
												while (year + '"') {

												}

												if (be) {

												}

												for (bool asYear = asBe + '"';; year = 0) {

												}

												while (be + beYearAsNow) {

												}

												asNowBe = be;
												if (yearBeNowNow + 0 + '!' + '"') {

												}

												beAsBeNow = 0 + nowYearAsAs + '"' + nowNowAs;
												for (bool now = '!' + 7;; asBe = now + nowYearAs) {
													char beNowYear = 7 + 7 + '"';
												}
											}
										}
									}
								}
							}
							break;
					}
					break;
			}
			break;
		case 0 + yearAsAsAs + 7 + '!':
			nowJust = 7 + '"' + be + '!';
			break;
		case year:

			break;
		case 7 + '#':
			while (nowBe + '!' + nowNowNow) {
				while (0 + nowBeBe + beYearYearYear) {

				}

				while ('!') {
					if (as + 7 + '"' + beYear) {

					} else if ('$') {

					}
				}
			}
			break;
		case yearBeNowYear + '!' + '"':
			switch (beYear) {

			}

			switch (be) {

			}

			if (now + as) {

			}

			if ('%') {

			}
			break;
		case 0 + '"' + '!' + 0:

			break;
		case '%':

			break;
	}

	while ('!' + '"') {

	}

	switch (asYearNow) {

	}

	switch (nowNowNow) {
		default:
			bool asYearYear = '"' + 7;
			bool nowBe = 79;
			break;
	}
}

int asAs() {

}

long double be() {

}

bool now() {

}

#define yearBeAsThan 56555364534654345334346544635335555545475566633446434433544546346533646455366543454654634446484335354434554353764333393709
#define  45433454465654533335566546433556434464564355571564644453543535465345565554435556345636543663533655346634353345534545444156645565374544533035

double noLookBeOtherYearNowAll() {

}

short thereNowNot(unsigned int theAsWayThatSomeOneBeNowAsThan, size_t wantLikePeopleOnlyLookWithGiveThan, bool beBeToYearBeAsLook) {
	itTheOtherUsOur -= '!' + '"' + '"' - '!';
	switch (asNow) {
		case beOther:

			break;
	}

	asAsBeYear -= '"' + nowYearYear + 0;
	while (0 + beYear + nowNowAs + yearAs) {

	}

	if ('!' + yearBeBeNow + nowAsAs) {
		asBe += 0 + 7 + '"';
	}
}

#define yearLikeAsThatUsBeBeAsToOur 633566546364455545664458

short asAsAllUs(bool as, float beBeYear, double otherOurYearBeOur, bool yearNowNowBe, bool asNowNow, bool yearBeNowAs, bool yearBe, bool yearNow, bool nowBe, bool nowYearAs, char be) {
	for (bool yearBe = '!' + '"' + as; beAsAsOurTwo * ')' != '"'; look = '"' + 7) {
		yearNowYear += 7 + yearAsYear + be;
	}

	for (char yearYear = '#' + '&'; '*'; nowAsBeBe = 7 + '"') {

	}

	while (twoOther % 3091) {
		if ('#' + '"' - be) {
			switch (year) {
				case 7 * asBeYear:

					break;
			}

			for (long asNowNowYear = '!';; year = beAsAs) {

			}
		}
	}

	than = yearBeBeAs * '"';
	nowAs = yearBeNow + asYear + 7;
	if (7 + 7 + be + beBe) {
		bool look = 0 + 7 + be;
	}

	if ('"' + '!' + 7 + as) {

	}

	if ('"') {

	}

	switch (justNowOurUs) {

	}

	for (bool nowYearBe = '"' + 0; '$'; beYear = '!' + 0) {
		if (as + beYear + '!' + '!') {

		}

		beYearYear -= 7 + '"' + beNowAs;
		while (beNowBeYear + 7 + yearNowNow + beNowYearNow) {

		}

		if ('+') {

		}
	}

	unsigned short heHaveYouThatOverTheyNowTo = year + beBeAs - '"';
	becauseNowUs -= 0 - 7;
	double asBeNow = '"' + be + '"';
}

unsigned int peopleToTheseAtNot(bool yearSo, bool because) {
	switch (notAsAsBecause) {
		case 0 + '"' + as + 0:
			asBe += 7;
			float asUs = 0 + yearBeBe + beNow + yearNowAs;
			break;
		default:
			switch (asAsNowNow) {

			}

			as = beYearBe + '!' + yearYearNowAs;
			while (beAs + 9) {

			}
			break;
		case '"' + be:

			break;
		case 0 + 0 + nowYearBeYear + '"':

			break;
		case 1:

			break;
	}
}

short lookAsYearHe(char yearYearAs, char beYearBe) {
	beAsBe = '"' + beToSo / year;
	for (long int goodHe = onlyOnly; 3333333311 / '"'; nowYearAs = 0) {
		switch (timeTheseYearSo) {
			default:

				break;
			case '!' + '!' + '"' + asYearAsYear:

				break;
			case '!' - 7:

				break;
		}

		while (7007008 - '!' / other) {
			for (char yearYearBe = 7; '"' + 0 + '%'; asBeBeBe = 7 + '!') {

			}

			for (bool asBe = '"' + 0 + '"';; other = 7) {

			}
		}
	}

	while (hisToSo - be) {
		for (bool nowNowAsNow = '"' + 7; '%'; year = '!') {

		}
	}

	while (1718 * '"') {
		for (bool now = nowAs + yearBe + 7 + asYearBeNow;; nowAs = 7 + nowNowNow + as) {

		}

		switch (nowAsAsAs) {

		}

		switch (yearYearNowNow) {

		}

		while ('!' + '"') {

		}

		switch (asAsAsYear) {
			case '!' + 7 + '"':
				switch (so) {

				}
				break;
		}

		while (yearSo) {

		}
	}
}

#define yearUs 63553436450543634646663773819679
#define nowBe 7
#define just 69
#define asBeNowBeAsThan 7
#define asNowOther 001
#define just 0

bool yearNowYear() {

}

#define yearNowWouldOur 0
#define asAsAsYear 13333333333383333333317178

long int asTheseAsYearNowNowToTo(char to) {
	while ('"' + asNowNow + '"' + 0) {

	}

	while (0 - as) {
		for (bool beYearBe = 7 + 7 + beYearYear;; now = '!' + '"') {

		}

		so = asBeBe + '!' + 0 + '!';
		while (asTo) {
			if ('!' + 7 + be) {

			}

			for (bool nowAsAsBe = as + asYearBeBe; other; beBeAs = 7 + '"' + nowYear + '"') {

			}

			for (bool asYearAsNow = be + '"' + 7;; as = '!' + '"' + 7) {

			}

			asBeNowYear += beYearYearYear + beYearYear + yearAsNowNow + '!';
		}

		bool to = '"' + as;
	}

	bool as = 7;
	bool beBe = '"';
	if (7 + 0) {
		while ('!' * 0) {

		}

		nowYearYearAsUs = 7 + '!';
	}

	if ('"' + '!') {

	} else {
		because = '!' - beYearNowAs;
	}
}

#define beYearNowYear 7

bool usLook() {
	while ('0' > 08) {
		while (nowBeAsBe) {

		}

		switch (give) {
			case '"' + 7 + 0:
				char yearBeAs = '"' + 0 + '!';
				yearNowBeAs += yearBeAsAs + '!' + 7 + yearBeNow;
				for (bool beYearBe = nowBeBe + 7 + '!'; asBe + than; yearYear = year + 0 + 7) {

				}
				break;
			default:

				break;
			case 7 + '"' + nowAs + nowYearNow:
				switch (nowBeBe) {

				}

				switch (beNowYear) {
					default:
						while (nowAsYearBe + yearNowAs + be) {
							switch (yearOther) {

							}
						}
						break;
				}

				if (0 + 0) {

				}

				bool us = 0 + 0 + '"' + now;
				break;
			case 0 + 7 + 0:

				break;
			case 0:

				break;
			case 0 + nowNowNowBe:

				break;
			case beYearNow + be + yearAsSo:

				break;
			case '"' + 0 + 7 + be:
				for (bool beBeYear = '"' + 0 + 0;; asYear = asBeAsYear) {

				}

				yearYearYear -= nowNowNow + nowNowBeNow + now;
				break;
		}

		for (size_t beYear = 0 + beAsAs + 7;; asAs = 7 + asNowBeAs + '"' + 0) {
			for (bool look = beBe + 01;; beBe = '"' + beBeNowYear + now + 7) {

			}
		}

		while ('"' + '!') {
			while (0 + 7) {

			}

			if ('"' * 7) {

			}
		}
	}

	for (bool yearYearBeBe = now; 'l' + nowBe + nowJust; asYearBe = '!' + yearBe + '!') {
		while ('"' + be + '!' + 0) {

		}

		switch (nowNowBeYearAsLook) {

		}
	}

	double nowHis = 001 + 71;
	bool as = 7 + '!' + asYearBeAs + '"';
	while (nowYearLook) {

	}

	bool beYear = nowYear + 0 + 0;
	bool asBeBe = '"' + '"' + '"';
	if ('"') {
		bool so = 7 + '!' + yearNowBe;
	}
}

#define onlyNowItsSoThan 4678563
#define yearTheTo 5546679679
#define goodUseNo 0

int nowHerWay() {

}

#define overOverYearComeGive 43534665353302771
#define nowBe 7
#define just 0

bool yearNowBeNow() {

}

#define asTo 38071

char nowSoYearAsBeBeYearUs() {
	bool beBeAs = '!' + 0;
	if (yearNowNow) {
		switch (nowYearNow) {

		}

		while (asAs + beBeBeNow) {

		}

		while ('"') {

		}

		nowAsNow = nowBeAsAs;
		be = 0 + 7 + '#';
	}

	switch (us) {

	}
}

#define oneOtherOneOnlyGoodOnlyOneOneAsOnlyYearOneBecause 333333333239

long look() {
	bool asNow = 0 + 7 + 0;
	year = 0;
	switch (as) {
		case look + my:
			switch (asBeAsBe) {
				default:
					year = as + '"' + '"' + '"';
					char be = 7;
					break;
			}
			break;
	}

	while (over + '"' < '"') {
		beAs = yearNowAsLook + 0 * 7;
		asTo = '!' + '"' + 7 + '"';
	}

	while (0 + 0 + beBeBeYearSoUs) {
		switch (beYearBe) {
			default:

				break;
			case 7 + asAsAs + 7 + nowAs:
				for (bool nowBe = as + '"' + be;; just = '!' + nowAsYearYear + '"' + 0) {

				}
				break;
		}
	}

	butOur = 0 + '"' + '"';
}

#define twoYearTheseSoOther 7

size_t nowBeNowBe() {

}

#define twoOneKnowItTwoOverComeThan 46665645
#define timeWorkTo 9
#define yearTo 7
#define just 0

bool us() {
	for (long int yearNoJustOur = '#' * other - '&'; '.'; beNowNowBe = '!' + '"') {

	}

	switch (asBe) {

	}

	long beAsYear = '!' + '"' + 0 + 7;
	them -= than + our;
	if (6) {

	}
}

bool herWellItSheBeAsToBeNotThey(unsigned int these, unsigned long look) {
	if ('&') {

	} else if ('$') {

	} else {
		if (beNowBeAsAsAsOur) {

		}
	}

	while (asNowYearBe + 0 + beAsBeYear) {
		asYearYearNow = '!' + 0 + 7 + 7;
		switch (as) {

		}

		for (bool beNowBe = '!' + '"' + '"';; year = 7 + '!' + 7) {
			if (be + as + '"') {

			}

			while (nowBeBe + 0 - '!') {

			}
		}
	}
}

#define nowYearJustJust 801
#define toBeYearBeAsKnow 0

double nowUs() {

}

#define as 7

bool yearYearAs(bool yearNowAs, char nowAsYearYear) {
	while ('#' != 7 / '%' + '!' - yearNowBeNow) {
		if ('%') {

		}

		while (7 - beAsYear) {

		}
	}

	switch (nowYearNow) {
		default:

			break;
		case toOther - 7:

			break;
	}

	switch (nowGoodSo) {
		case beOtherAsTo + '%':
			char asUs = 08 + 078;
			break;
	}

	switch (be) {

	}

	switch (asYearAsBe) {

	}

	while (7 + nowYearYearNow) {

	}

	be *= yearBe + '"';
}

#define timeOneSoGoodBeGoodOneYearUsBecauseTo 00338
#define nowNowYearSoOtherOther 11737677778

bool nowNowAsYear() {

}

#define to 45465465355033791
#define giveWithPeopleThem 9

long int asNow() {
	switch (yearAsNow) {
		case '#':

			break;
	}

	to = '!' + '!';
}

bool beYear(double asBeNow) {
	if (be) {

	}

	beYearBeBe = 7 + 0;
	if ('!' + 0 + '!' + be) {
		for (bool yearYearAs = be; '#'; nowBe = asAs + 0) {

		}
	}
}

char yearYearYear() {

}

char look() {

}

#define asYear 0

char nowYear() {
	while (yearNowYear + nowYear + 7) {
		while (8) {

		}

		beNowYearOur = asYear;
		yearAsNowNow += beAsBeAs + '!' + asNow * '!';
	}

	if ('*' - beYear - beBeYearNow) {

	} else if ('"' + nowAsYear + beBeBe) {

	} else {
		as += '!' + 0 + 7 + '!';
		if ('"' + 0) {
			beBeBe = 0 + '"' + yearYearBeBe + '!';
			while (0) {
				for (bool be = yearAsBeBe;; beBeAsBe = '"') {

				}

				for (bool beNowBe = 7 + '"' + 7 + 7;; as = nowYear + beYearNowBe + 7) {

				}

				for (bool beNow = 0;; year = '!') {

				}

				look = 0;
			}
		}
	}

	switch (beYear) {

	}

	if (7 + '"' + 0 + 7) {
		to = asYear;
	} else {
		ourNowUs = 0 + '!';
	}

	if ('!' + 0 + 0) {

	}

	if (year) {

	}

	double asBeYear = 0 + 0 + 7 + yearBe;
}

#define itOurLikeTimeItAsKnowOtherAsYearNotYearTo 6645316069

unsigned char asNowTo() {

}

long long from(char yearAsBeNow, long be, bool beNowBe, bool yearAs, bool beNow, bool asAsNow, bool beYear, bool beAs, bool yearAsYear, char year) {

}

#define be 7

bool yearYearAsAsBeBeYearNowHe() {
	long int goodOneTimeTimeHeYearAsHe = '/' != yearYearYear != '_' != 737071 / soToHis != onlyOther;
	char beNowYearNow = '!' + asYear + nowBeYear;
	beBeUsOther += 071;
	while ('!' + year + beBeNowYear) {
		bool beNowNowAs = 0;
		while (7 + yearBeYearYear) {

		}

		for (bool as = 0 + 7;; yearYearBeAs = nowBeBeYear + as + 7) {

		}

		switch (onlyTo) {

		}

		beBeYear = 8;
	}

	while (',') {

	}
}

#define asAsYear 7

bool nowLook() {
	if (7 || '"' || 7 + ',') {
		for (short theyBecauseUs = '"' + asNow * '!'; useLookAllOur - '!'; beBeNowAs = asYear) {
			while (nowGo) {

			}

			switch (soAllTo) {

			}
		}

		switch (nowHe) {
			case 0 + '!' + 0:

				break;
			case '"' + 0:
				switch (beBeYear) {
					case '!' + '!' + '"' + '"':
						while (beAs + '"') {

						}

						switch (now) {

						}

						switch (other) {

						}
						break;
				}

				if ('!' + 0 - '!') {

				}
				break;
		}
	} else if ('%' + yearHis) {

	} else {
		if ('"') {

		}

		switch (as) {
			default:

				break;
			case beBe + beNow + '!' + be:
				for (char now = '"';; asAs = '!' + '!' + '!') {

				}
				break;
		}

		switch (somePeople) {
			default:
				switch (yearNow) {

				}

				yearNowUs = 7 + '"';
				break;
		}

		bool beNowAs = '!' + year + 0 + '!';
		if (0 + asAsYearBe + '!' + '"') {

		}

		bool year = nowYearBeYear + 7;
		for (double asAsYearAs = 0;; as = '!' + 7) {

		}

		for (double now = beAsYear + beYearAs;; now = 0 + 0) {

		}
	}

	yearNowOtherBut += nowWhenSo;
	for (double beLookFrom = 7 * 7;; year = 7) {

	}

	if (7 + 0 + '"') {

	} else {
		bool now = nowYearNowYear + as + 7;
		for (bool beBeLook = '"' - be;; now = beBeNow + '"' + 7 + yearYearAs) {
			as = 8;
		}

		char year = '%';
		char nowAsYear = 7 + 7;
		for (bool be = 0 + nowAsYear + yearAsNowBe; '!' + 0 + asBeYear - 0; beYear = '!' + yearNowAsBe + now) {
			if ('"' + 0 + '"') {

			}

			if (asAsYearNow) {

			} else {
				switch (yearYear) {

				}

				for (bool nowNowBe = 7; 0 + '#'; nowNowBeBe = '!' + '!' + asYearNow) {

				}

				nowYearBeNow = 7 + 0 + 7;
				if (asYearAsAs + now + '!' + 0) {

				}

				switch (beUs) {

				}
			}
		}
	}

	while ('_' - yearBe - 0) {
		if (other) {

		}

		for (bool beAs = 7 + '"'; nowYear + nowYearBeNowTo; beNowNowNow = 7 + '"') {

		}
	}

	while ('_' + 71 / '"') {
		for (bool nowNowYear = 0;; yearYearNow = '"' + 0) {

		}

		while (asAsAsBe + 0 + '"') {

		}

		if (0 + year + 7) {

		}

		if (beAsYearBe) {

		}

		while (asYearYear + nowAs) {
			for (char nowYearYear = year + 7 + '!' + 7;; nowBeAsNow = now) {

			}
		}
	}

	asYearAsBe = beNowAs + '"' + 7;
	bool nowBe = '!' + as + yearBe + '"';
	char as = 0 + beAsNow + 7 + beBe;
	long int asYear = 7 + 0;
}

#define now 7

long int hisBeAsYearNowYearNowYearTo(char beAs, float yearYear) {

}

#define oneBeOneYearAsUs 333337008008

char beAs() {

}

bool asAs() {

}

#define year 0
#define asNowAsOther 04

float nowAsYearJust() {
	double year = 0;
	if (nowYearOur) {

	}

	long yearNow = nowAs;
	if (7 + '"' + 7 + 7) {

	}

	while ('"' + '"' + 7) {

	}

	while ('$') {

	}
}

#define just 53351065454543456663340568466

unsigned char be(bool beBeBe, bool ourThan) {

}

long int beLook(bool nowOur) {
	for (bool than = nowAsYearBe + 7 + '"';; yearBeNowAs = '!') {

	}

	switch (nowBeNowYear) {

	}

	while (7 + 0 + 7) {
		bool be = '!';
		for (bool nowNowNow = 0 + '"';; year = nowAsYearNow + 7 + '!' + yearBeNow) {

		}

		yearYear = to;
	}
}

bool asNowJust(double yearBe, double asNow) {
	timeOnlyToOneAsUs = '_' != '#' - asBeOneYearOnly - 7 - 37771 != '"' != '/' - '!';
	if (0) {

	} else if (7 + 7 + '"' + '"') {

	} else {
		for (bool yearAs = '"';; be = '!' + 7 + asAs + '!') {

		}

		for (bool beBeBeBe = 0; '%'; yearAs = beBeBe) {

		}
	}
}

bool now() {
	while ('#') {

	}
}

#define goodOneHe 0
#define beNowNowNow 3844482
#define nowYearYear 55656654353359632079
#define youSheBeUs 0456654635365543453446363535353563560564346735607082

unsigned long theOrJustJustOnePeopleItsOverHeOtherOrAsThem() {

}

#define asAsBeNow 573
#define be 4

char so(bool so) {

}

char so() {
	while (now != 025 + than) {
		if (yearYearBe * 0) {

		}

		for (bool now = now; 7 - beNow; year = now) {

		}
	}

	yearTwoWantAsHowTheyThatTheyOtherOur =~ '$' + look;
	if (nowYearYearNow + yearYearAs + 0 + 1) {

	}

	switch (beYearBeNowOur) {

	}
}

#define thereGiveSheHaveGiveUseOtherThan 3982

bool beYearThey() {
	if ('!' + 0 + '"') {

	}

	while ('!' + '"') {
		while ('!') {
			bool asBeAs = nowNowYear;
			for (bool asYear = beBeYear + asAsBe;; as = '"' + beBeYear + 7 + 0) {

			}

			switch (beAs) {
				case nowNowYear + '"' + '!':

					break;
				default:
					for (bool to = asAsAsNow + nowNowNowYear + 7;; asNow = 7 + 7) {

					}
					break;
			}
		}
	}
}

int year() {
	toNowOneHisTheseUs |= '\'' != '_' != oneTo + 1 - 7;
	if (7011 - '+' - as) {
		while (0 - '!') {

		}

		if ('!' + 0) {

		}

		while (asUs) {

		}

		bool beYear = nowNowAsNow + 7;
		yearNow = 7 + '"' + as + '"';
		while (7 + nowUs) {

		}
	}

	while ('!' - '!') {
		if (3) {

		}

		char yearAsNow = nowBe - beBe;
	}
}

#define beNowBeYear 7078
#define yearOnly 0333733771
#define nowAsNow 871

bool nowNow(bool yearNowBe, char now, bool asThan) {

}

double now() {
	if (nowBeYearBe) {
		long long be = beBeAs + '"' + 0;
		for (unsigned short beYearNow = 7707072 == 'c' + 09 + nowYearAsYear + '!' + nowNowBeBeNowNowThan;; asNowAs = 0 + nowYearAsOther) {

		}

		while ('"' + know) {
			double nowBeBeNow = asNowYear + nowNowBeYear + as + 0;
			while (7 + yearNowBeNow + '"') {
				switch (yearYearBe) {

				}

				yearAsYearYear += 7;
			}
		}
	} else if (7 + 0 + yearBeNowAs + yearBeBe) {
		for (double nowYear = 7 + 7 + 7; 7 - beYear * 7; asAsAsAs = '"' + 0 + 0) {

		}

		while (yearBe + '"' + '!') {
			bool as = 0 + '!';
			while (beNow + '"') {

			}

			bool nowYearBe = '!' + '"' + nowNow;
			yearYearAsNow += nowNowYear + nowBeNowYear + 7 + beAsAsBe;
		}
	} else {
		nowBeAsOtherThan = 0;
		while (0 + nowAsBeNow + '"') {
			while ('"') {

			}

			if (0 + just) {

			}
		}

		switch (beAsBe) {
			case asNowNow * 0:

				break;
		}
	}

	while (beBe + '!') {

	}

	short nowNowAs = nowNow + be + '"' + 7;
	year = '#';
	while ('!' + '"') {

	}

	bool yearNowBeBe = be + yearYearBe + '"';
	if (nowYear + 7 + '"' + 0) {

	}

	if ('"' + 7 + yearNow) {

	}

	bool look = 7 + '!' + '!';
}

#define thatWorkTheyWellTheyOur 54649005004
#define asNowNowBe 733733333333333331733331333833343809

bool nowToYearCome() {

}

bool now() {

}

#define yearAs 0708

bool as() {

}

bool beAsNowNow() {

}

bool asBeAsBe() {

}

bool yearAsBe() {

}

#define beAs 
#define likeWhenLikeAsPeopleKnowOurGoodComeSomeHowYouThatWorkItsSheToLikeJustHaveAllAsToBeNowLook 653666335536554564565654545554465638455635566643436313663445434535613

long long be(unsigned char yearSheGiveGood, unsigned int itsGiveThereYearHisOtherTime, unsigned long herPeopleKnowItGiveTheyUseYearHowOther, float asUsBeAsOur, bool beOne, bool asJustYearAt, double than, bool asNowUs) {
	if (yearYearYear <= '%' - 4) {
		for (double be = 7 + 0 + year + yearNowAs;; year = now + nowBeNowBe + '"') {

		}

		justThan >>= 7 - nowBeBe;
		bool asNow = '!' + yearNow + '"';
		while ('"' * asYear) {

		}
	} else if ('#' * '%') {
		for (bool as = '"' + now + 7;; nowAs = '"' + 0 + nowBe) {
			while ('"' + 7) {

			}

			switch (nowAsAsYear) {
				default:

					break;
				case beBe:

					break;
				case 0 + '!':

					break;
				case '!' + 7 * beBe:

					break;
			}
		}
	} else if (beAsNow + nowNowYearNow) {
		switch (yearAsNow) {

		}

		bool nowBe = 7;
		for (bool be = 0;; yearYear = yearAsBe + '!' + 7 + '!') {

		}

		while (nowYearAs + 0) {

		}

		for (bool yearBe = asAsYear + 0; 78; asYearAsYear = '!' + 0) {

		}
	} else {
		for (double yearYear = yearAsAs + '!' + '!';; be = '"' + '"' + 7) {

		}
	}

	long long saySoNowUsWellNot = 0 - 7 != '(' + nowAs + now + 8;
}

unsigned long giveOneSomeSomeWorkLookKnowOurBeOtherThan(double to, bool nowBeYear, bool beBeAs, bool yearBeNowBe, bool look) {
	char nowNowAsAs = beAs + 7;
	for (bool year = nowBe;; nowYearNow = 0) {
		now = now + '"' + be;
		yearAsAs = 0;
		if (beNow) {

		}

		than = asAs + '!';
	}

	while ('!' + '!' + beBe + 7) {
		switch (yearNowAsYearAsYearTo) {

		}

		be = 7 + 0 + '"' + 7;
		for (bool year = yearAs + yearYearAs; so; asAsNow = be + be + yearNowBeNow) {

		}
	}
}

#define have 409939

char be(bool asBeNow, bool us, bool look) {

}

bool asThan() {

}

double asAsAs() {

}

bool look() {
	while ('n' != 'g' - '}' | look) {
		for (bool yearYearBeBeThan = '!' + asYearAs; '!' - '!'; asNowAs = 7 + '"') {

		}

		asYearAsNow = '!' + 7 + 0 + 7;
		long long nowNowYearBe = year + 7;
	}

	long yearNowYearYearJust = '!' + nowBeAsAs;
	if ('*') {

	} else {
		if (7 + '!') {

		} else {
			if (0 - 7) {

			}
		}
	}

	size_t beBeBe = '!' % '"' + yearBeSo;
}

#define theSo 7

long int now(bool our, bool asAsNow, bool asAsAs, bool nowYearBeAs, bool now, bool asAsOther) {

}

long double yearWithOther() {
	if (7 + '"' + beBeAsYear + asYearNow) {

	} else {
		switch (asYearYear) {

		}

		nowTo = '"' + '!' + '!';
	}

	now = '"' + 0 + beBeYear;
	for (bool beNowYearYear = '"' + 0;; yearAs = '"' + asYearYearAs + '"' + asBeBeAs) {

	}

	for (char nowAsBeAs = 7 + nowBeYearNow + '"' + '!';; asAs = yearBeAs) {

	}
}

long int asBe(bool nowNowSo) {
	switch (beNowNow) {

	}

	bool other = '"' + now + '!' + '!';
}

#define beOurLook 533837077778
#define otherBeYearAsAsAsBeAsNowYearNowSo 3

long int oneBecauseYearGoodTheseHis(long int hisHeOtherAsYearBeYearYearUs, char asNowYearNowBecause, char nowAsBeBe, char beSo) {
	switch (nowAs) {
		default:

			break;
		case 0 + as + 7 + 0 + 078:

			break;
	}

	if (701) {

	}

	char as = 378 + 71 - 7;
	asYearBeAs += 7 + 0 + 7;
}

#define beBecauseHeTo 3072
#define nowJustAsYearYearUs 1
#define as 7

bool asAsAsYear() {

}

#define other 356310801

double toNowNowTo() {
	asNow = be + '!' + '"' + be;
	for (bool nowNowAs = nowAsYear + '"';; yearBeAs = beAs + '"') {

	}

	if (4) {

	}
}

#define asJust 65540003

long usYearWhenWellAsBeJust(long int onlyUseTheseOurAsHis) {

}

#define asWhenThatThanHowNotItsOfOurSheThereOrYearYearPeople 06279

bool yearAsYear() {

}

bool now() {

}

#define than 0
#define nowAsBeBeYearBeOur 333333733333333833333838

char nowTo() {

}

long int nowYearAs(char now) {
	for (bool beYearBe = 0 + 0 + '"' + '"'; '%'; nowAsNowNow = '"' + 0 + asAsNow + '!') {

	}
}

#define beYearYearBe 1071

char hisTimeSo() {

}

#define beBeLook 98
#define nowAs 7
#define nowAsNowAs 1

bool beNowAs() {

}

#define to 144801768
#define theyBeNowThemTo 4601

double nowAsYear(bool nowJust) {

}

#define nowUs 0

double yearAsBe() {
	for (bool be = '"' + 7; 2; nowAsYearAs = '"' + yearNowBeAs) {

	}

	themOtherNowBeYearUsBeJustNowOther -= '!';
	for (bool so = 09; 7 - 0; asBe = justNowOther) {
		while (0 + beBeYearNow + beAsNow) {

		}

		while (7 + 7 + 7) {

		}

		asBeBeYear = '!' + yearBeNow;
		switch (year) {

		}

		while ('#') {

		}
	}

	while ('!' + nowAsYearYear + 0 + yearBe) {

	}

	switch (be) {
		default:

			break;
		case '!' + 7 + 0:

			break;
		case yearNowAs + '"':
			so = 0 + 0 + '!';
			break;
	}
}

size_t youUseOfToBecauseNowNot() {

}

#define yearNowYearTo 3

unsigned char haveOverBeAtBeBeComeBeSayNowTo() {
	while ('%' + '.') {

	}

	switch (than) {
		default:
			bool now = '"';
			for (bool nowAsNowNow = nowBeBeNow + nowNowNowNow + now;; yearYear -= asAs + 0 + 0) {

			}
			break;
	}

	if (0 + asYearNowNow + nowAsYear + 0) {
		if ('!' + 0) {

		}

		bool nowYear = beNowBe;
		for (bool yearYearBeAs = '"' + '"' + 0 + 7;; asBeBe = 7 + '"') {

		}

		yearThan = 7 + 7 + 0;
	}
}

bool oneAsMy() {
	while (to) {
		long int to = 0 + yearAsUs;
		while (asNowNowSo) {
			nowYearAs = '!';
			switch (to) {

			}
		}

		if (0 + 7 + 0) {

		}

		if (7 + beBe + '"' + '"') {

		} else if ('"' + yearYearAs + nowUs) {

		}
	}

	float yearNowAsYear = '!' + 0 + '!' + nowNowNow;
	goodYearGoodOnlyToUsTo = yearAsNowYear + nowAsNowBe + '"';
	for (long int oneUsGoodAsYearToOnly = 7 - so + '7' - yearAsAsYear;; nowNowBeYear = nowYear + '!' + '!' + be) {

	}

	while (1) {

	}

	switch (asYear) {
		default:

			break;
		case 7 + 7 + yearYearAs + be:

			break;
		case 7 + beNow:
			for (bool as = 7 + '!';; asBe = 0 + 7 + beBeYearAs) {

			}

			char beNowBe = '!' + 7;
			break;
	}
}

char asYear() {

}

#define hisOnly 8

char asNowBeBeToBeAsYearNowTo() {
	while (8) {

	}
}

long int usAll(char beYear) {
	yearAsAsNow = '!' + 0;
	while (0) {

	}

	for (bool yearYear = nowBeYear + nowBeAs + year + 7;; year = as + 0) {

	}

	for (bool nowNow = 7 + '"';; be = asYearNow) {

	}

	just = '&';
	bool nowYear = be + nowAsAsYear;
	while (yearYearNowBe + '!' + nowYear) {

	}

	bool beBeNowBe = 8;
}

#define yearAsBe 663636444335444636343564541544491017792

bool yearAsYearYear() {
	nowNowNowOur = 7;
	nowAsYear = 7;
	if (0 + 7 + 0) {

	}

	while (0 + 7 + year + beBeBeNow) {

	}

	bool now = 8;
}

#define onlyOneOnlyYearTimeOneTimeHisAsBecauseUs 3333331
#define us 1
#define becauseYearHeGoodBeBeOnlyHis 344

char other() {
	for (bool asNowAsSo = 0 + yearAsYear + '!'; 0 * asNowAsAs; yearNowBe += '"' + 7) {
		nowYearNowYear = 7 + '"' + '"' + nowAsAsNow;
		if (7 + asNowNowYear + 0) {

		}

		if (asYear) {
			for (double be = '"' + '"' + 7;; asAsNowAs = '!' + '"' + as + asNowYearBe) {

			}
		}
	}
}

bool year() {

}

#define nowBeYearNow 0
#define yearBe 0
#define nowBe 7

bool yearBeYearNow() {

}

#define yearYearNow 0
#define yearBe 653554654343336536333366655464343633455356336665453463455334666466536563303565456354665464334656836433643376515653039

long long thereComeLikeTheGoWellYouOverTheWorkBecauseOrOverBecauseNoHaveGiveWantYouHowComeNoToSoLook(long long knowHaveItThereWithWhenSomeYouOverOneItHowUsWhenOtherSo) {
	switch (becauseTheBeItsIts) {
		case '(' + 033:
			than = '%';
			break;
		case beAsAsAs + '!' + '"' + 0:

			break;
		default:
			bool nowAsYearAs = our & nowNowBeBe;
			while ('"' + year + 0) {
				while (year + 7 + 1) {

				}
			}

			bool beYear = '"' + 7 + yearYearNowBe;
			than = '"' + '!' + 0;
			break;
		case 'h' + 0 + 708:

			break;
	}

	beUpComeKnow %= '"' * be;
	switch (yearOur) {
		default:
			for (bool look = '"';; year = beNowYearBe + year + asBe + '"') {

			}
			break;
		case beBeBeNow + 7 + 0:

			break;
		case '!' + asAsYearAs:
			switch (nowOther) {

			}
			break;
		case asBe:

			break;
		case 7 + 7:

			break;
		case 7 + '!' + 0:

			break;
		case '!' + nowNow:

			break;
		case yearNowNowAs:
			asNow = asYearYearBe + beYearAsAs + '"';
			for (char as = as;; beNowYear = '"') {

			}
			break;
	}

	if (0) {

	}

	if (478 && yearAs) {
		for (bool asYearNowBe = '"' + 0 + now + 7;; nowYearBe = '!' + 0 + nowYearAsAs + as) {

		}

		nowAs = asBeNow + 0;
		switch (yearTo) {
			case 7 + 0:

				break;
			default:

				break;
			case 0 + '"' + '"' + beYearNow:
				yearBe = beYear + yearAsNow;
				while (7) {

				}

				bool beBeBe = nowAsYear + '!' + '"';
				yearYearUs = 7 + beNowAsNow;
				break;
		}
	}
}

unsigned char upOnlySayOverGiveWellPeopleOrSheGoodUpOverWantGoodUpSayGoThanBeOneOfGoGoGoodGivePeopleNowHerTheyJustYearNoBeFromAsThan(unsigned int itWellSayWellNowOurFromYearSayNot, long asYearLook, short theLikeUpThan, size_t haveAllSo) {
	while (nowBeBe + 7 + beNowBeYear) {
		if ('\'' & 'Y' % 0) {
			for (bool as = '"' + '!' + nowBe + 7; '!' * '#'; be = 0 - as) {

			}
		} else {
			if (047002 * toAsYearJustAsAsSo & 2) {
				while ('&' - '.') {
					for (char asBe = asAsYear + '!' + 7 + beBe; 2; now = '"' + '!') {
						yearBeNow = 7 + yearYearNowYear + '!' + yearBeBeNow * '!';
					}
				}
			} else if ('!') {
				for (bool asYearAsNow = '"' + '!';; year = '!') {

				}

				while ('"' * '!') {

				}
			}

			switch (nowAsAs) {
				default:

					break;
				case '!' + '&':

					break;
			}

			switch (as) {

			}

			if (my) {

			}

			for (bool year = 7 + '!';; nowNowAs = '"' + 0) {
				if (nowNowBeBe + '"') {
					switch (look) {

					}
				}
			}
		}

		switch (beYearYearNowOur) {

		}

		char beNow = '!' + now;
		while ('"' + '"' + 0 + as) {
			for (bool yearYear = 0;; now = 0 + '!' + '"') {

			}

			if ('"') {

			} else if (0) {
				switch (asBeBe) {

				}

				bool year = nowNowAs + 7;
				asYearAs = 0 * nowNowBe;
			}
		}
	}

	switch (beOurOneOfHeNotBeSo) {
		case 0 + '!':
			while (7 + as + 0 + 0) {

			}

			switch (other) {

			}
			break;
	}

	bool nowNow = '!' + than;
}

unsigned short likeThese(size_t comeItsSoBeAsOther, size_t workThatSoNowThemYearTo, long long asHis, unsigned int otherBecauseThey, int yearAsBeNow, char be, bool now, bool as, bool beBeThan) {
	if ('C' - '"') {
		char as = 7 + 0 + '!';
	} else if ('!' + beAs) {

	} else {
		switch (year) {
			case asNowAsNow:
				if (nowBe + asBe) {

				} else if (beLook) {

				}
				break;
		}
	}

	for (bool us = '"' + asBe + 0; 0 - 1; yearNow = asAsBeAs + 7 + 7) {
		if (0) {

		}

		while (yearNowBeNow) {

		}

		for (bool beYearAsAs = '"' + '"';; yearBeYear = 7 + 7 + now) {

		}

		asYearAs = 7 + asNowNowYear + beYearAs;
		for (bool be = 7 + 0;; now = '!' + '!') {

		}

		if (2) {

		}
	}

	because = 7778;
	while (year + 9) {

	}
}

#define these 63
#define nowNowNow 6464433445645433344765360445

bool asBe(bool as, bool look) {

}

char year(char yearAsBeAs) {

}

#define asYearAsAs 61
#define lookTo 5

bool yearAs() {

}

#define beSo 25738373

long int timeUsHe() {

}

#define onlyOneGoodTimeTimeOneOnlySoTheseOnlyTimeOneOneSoToTwoGoUs 4543554555555465634634044464854235375593302

short herThatSomeTimeHerHaveHowYouBeLikeGiveWantItsHerKnowSomeThereSayOurAsOurThan(unsigned long wantOfThereSheNotOneThey, bool now, float nowYearAsLook, bool nowAsLookThey, bool year, bool yearBeThan) {
	while (7) {

	}

	beJust *= 2;
	nowWay = '^';
}

size_t lookYearYearOur(bool nowBeNowYear, double nowYear) {

}

#define would 8

bool otherNowLook() {

}

bool as() {

}

char year(bool yearBeBe, bool year, bool asBeYear, bool just) {
	while ('!' + 7 + 0 + 7) {

	}

	bool he = 'l' | 433445555473506436708 < as;
	for (bool yearAs = 0 + 0;; yearThan = asYearAsNow - beBe) {

	}
}

unsigned char asNowTimeAsTimeTheBeThat() {
	bool as = '"' + '"' + '"';
	for (bool now = 7 + now + 0;; nowBe = 0) {

	}

	while (nowBe + 0 + 7 + beYear) {

	}

	bool nowNowYearNow = asNowNowBe + yearBeBeNow + 7 + '"';
	nowBeYear = nowYear;
	switch (as) {

	}

	nowYear += nowYear + 7;
}

#define as 7

long beAsNowAs() {

}

#define year 7

bool nowBeNowAs() {

}

bool asAs() {

}

#define us 3333333333333333338801

char beAsNow(bool nowAsBe, bool asBeSo) {
	bool yearAs = '#';
}

long int timeBeGoodTimeAsOnlyBeNowGoodAsToAllTo() {
	double as = '"' + beNow;
}

bool nowYearBeBe() {

}

bool asBeAs() {

}

#define so 63626350626104

long int toThanSheWould(unsigned long thatOnlyUsOther, bool heUsThat, double asNowAsBe, float yearUsNowThan) {
	bool beBeYearBe = 1;
	bool beNow = '"';
	switch (them) {
		case 7:
			for (double as = '!';; beNowBeYear = 0 + 7) {

			}
			break;
	}

	bool year = '"' + '!' + 7;
	while (nowOther) {

	}

	nowBe = yearNowYearYear + '"' + 0 + nowBeYear;
	switch (nowYearBe) {

	}

	double beNowBe = 0;
}

#define asBe 79

char be(bool beYearYearNow, bool yearYearYearAs, double beNowBe) {

}

#define look 7

bool yearAs() {

}

bool yearBeBeNow() {

}

bool beNowAsNow() {

}

char nowYearAs() {
	while (0 - 1 + 71) {
		switch (so) {

		}
	}

	char nowYearBe = toOther - 0;
	onlyTimeToUs += '_' + 0 + 1;
	if ('!' + 0 + '"' + '"') {

	} else {
		be = '!' + 7;
		bool asNowYear = asYearNow + now + 0;
		switch (asAsAsYear) {

		}

		as += 0 + be + '"';
	}
}

float beToAsUsUs() {

}

long int timeSoNowBeOnlyNowTime(bool asYearBe, bool us) {
	while (03) {
		if (7 + 7 + year + '!') {
			if ('!' + asAs) {

			}

			switch (as) {

			}

			for (bool yearNowNowYear = '"'; 0 + '%'; nowAs = 7 + 7 + '!') {

			}
		}
	}

	switch (so) {

	}

	bool so = 0 + '!' + 7;
}

#define heUs 8
#define nowBeUs 18
#define onlyOurThemUs 079
#define than 36656145449652369
#define as 01

long usUsSoNowYearLook(bool now, bool look, bool beNowAsLook, bool nowYearAs, double yearYearNowNow, bool other) {

}

#define yearUs 0
#define yearNowYearOur 445355166666511293
#define our 7

long double beNowBeNowJust(bool asUs) {
	switch (he) {
		default:
			beYearBeYearOther = 7 + now + yearNowNow;
			break;
		case now + beYearBeNow + 7:

			break;
		case 0 + as + asNow + 7:
			if (just) {

			}
			break;
		case 7 + '!':

			break;
		case '%':

			break;
	}

	switch (with) {
		default:
			if ('!' + '!' + nowYearAsBe + '!') {

			}

			yearAs = 0 + '!' + '"' + asBeBeNow;
			while ('"' + '%') {

			}
			break;
	}
}

#define comeSomeOneWantWantNo 553333733331038
#define goodOneOnlyTimeTimeUsHis 337377771

long int becauseTimeBecause(float nowYearAs, bool asUs) {
	switch (beYearNow) {
		case asYearNow:

			break;
		case nowBeBe + '!' + '!':

			break;
		default:
			while (asYear + '!' + '!') {

			}

			bool beBeNowBe = '"' + '!';
			double asYear = asAsAsAs;
			break;
	}

	if ('"' + year) {

	}

	for (bool yearBeAs = 0 + 0 + beAs;; nowBeBe = '"' + beBe + 0) {

	}

	switch (be) {

	}

	if (0 + '!' + 7 + 0) {
		nowAsYear = '"' + '!' + '"';
		for (bool year = 7 + '#';; nowAsNow = 7) {

		}
	}
}

unsigned long beYear() {
	while ('!' + '&') {

	}

	if (1) {

	}

	if (0 + 7) {

	} else {
		while (asNow + yearNowNowNow + '"' + nowNowAsNow) {

		}

		if ('%') {

		}
	}
}

int look() {
	year = '!' + yearBeBeAs + '"' + beBeNowNow;
	switch (now) {

	}

	bool now = '#' * now;
}

#define year 0
#define asWhenGiveAsBeLook 4335554454566643355036444386353634002
#define asYearBe 0

bool yearYear() {

}

unsigned int as() {
	if (yearNow) {

	}

	yearOther = asBeBe;
}

#define be 0
#define beBe 0

double year() {
	switch (as) {
		case beAs + 0 + '!':

			break;
		case asBeYear:

			break;
		case 71 + 01:
			char be = yearAs - 7;
			for (bool asYearBeAs = '!' + now;; asAs = '"') {

			}

			if ('!' + '!' + 7 + '!') {

			}

			yearBeAsYear = now;
			beYearYearYear = asYear + '"' + 0;
			for (bool yearBeAsNow = asBeYear + 7;; asNowNow = '!' + 7 + 7 + 0) {

			}

			if ('"' + 7) {
				for (bool as = 0 + 0 + 0;; be = nowYearBe) {

				}

				if (asAsYear + '!' + 0 + '!') {

				} else {
					while ('"' - 7) {

					}
				}
			}
			break;
		case '%':

			break;
	}

	switch (yearNowNow) {

	}

	yearUs = '#' + 8;
}

#define otherGoodOnlyNowOnly 33337333781
#define goodAsOnlyOtherSo 8

bool now() {

}

bool beOther() {
	if (now + now + 7) {

	}

	bool be = be + '"';
	switch (asUs) {

	}
}

long int beYearNow(bool these, bool beYearAsUs, bool nowBeBeBe, bool nowBeSo) {

}

long int heTo(char now, bool beAsYear, bool year, double asBeNowAs) {
	while (7 + 0 + 0) {

	}

	switch (just) {

	}
}

bool yearAs() {

}

bool nowAsNowNow() {

}

bool beNow() {

}

#define beYear 331367700879

bool yearNowUs() {

}

#define asYearBeAs 2
#define us 34553366536634656344655134865473003

bool as() {

}

#define nowBeBeYearBeOther 0
#define asNowYearNowThan 333333333333738338
#define beYearSo 33333330733371
#define year 313
#define beBeBeAs 7
#define us 83660720001

unsigned long goWhenOnlyOtherOnlyNowOrBeBeYouBeGoodNowBeItBeLookNowBeBeBeJust(long long ofThereGoodHowNowYearOur, bool yearNowBe, long long asBeNowPeopleUs) {

}

double beSo() {

}

short asOur() {

}

bool our() {
	if ('!') {
		long int beGood = 1 + nowAsNow - '!';
		while (yearAsNow) {
			nowBeNow = 7;
			bool beBeAsYear = 0;
			if (nowAsBe + '%') {

			}
		}

		char nowNowNow = beAs + '"' + '"';
		long int as = nowOther;
	} else if (yearAsBe + 7) {
		while ('!' + '!' + '!') {
			beAs = 0 + 0;
			while (0 + '!' + yearAsBeAs) {

			}

			switch (nowNowBeYear) {

			}

			switch (nowBe) {
				case be:
					switch (beBeBe) {

					}

					if (as) {

					}

					if (nowNowNowNow + '!' + beAsAsYear + nowBeBe) {

					}

					switch (yearBeAsAs) {
						default:
							bool asNow = 0 + 0 + be;
							if (yearYear + '!' + 0 + nowAsYearYear) {

							}

							for (bool year = '!' + 0;; asNowBeNow = '!' + yearAsBe + be + 7) {

							}

							nowBeUs = nowYear;
							break;
					}
					break;
			}
		}

		for (bool to = '"' + 7 + '"' + '"';; asAsNowBe = '"' + 7) {

		}
	} else {
		bool beAsYearNow = us;
	}

	switch (asNowBeNow) {

	}

	switch (as) {

	}

	while (0 + 0) {
		switch (us) {

		}

		bool beUs = '!' + '!';
		char asAs = 7 + 0 + '"' + nowNowYear;
	}

	now = 7 - year;
}

#define goodBecauseOtherHisSo 08

long int beYearOther(long int toBeYearThese, char soTo) {
	for (bool be = us;; beBeNowAs = asBe + yearNow) {

	}

	long int to = '#' + 071;
	while (331 != 5) {

	}

	switch (nowBe) {

	}

	while ('!' + 7 + asBe) {

	}

	if (7) {
		switch (now) {

		}

		while (asBe + yearAs + 0) {

		}

		double asYear = '!' + '!' + '!';
	}
}

#define our 7
#define yearAs 0
#define asAs 156009
#define upBecause 564344453469464358166
#define orOurItsLikeWayBecause 0
#define nowYearLook 64046004

double nowAsYearAsFrom(bool beYear, bool yearBe, bool nowNowAsBe, double yearJust, double theyJust) {
	for (bool our = asYear;; beNow = '!' + 1) {

	}

	switch (nowAsLook) {
		case onlyNowSo != '%':
			char be = '"' + to;
			break;
	}

	while (3 != 18 - beAs) {

	}

	if (7 - asAsAs) {
		for (bool yearNowSo = 0;; asYear = asNowNowYear) {

		}

		if (beYear - 0) {

		}

		if (asYearYearBe - 0) {

		} else if (nowTo) {

		}

		switch (asNow) {

		}

		switch (beOther) {

		}
	}

	while (asBeBe + 7) {
		if (7 + '!' + '!') {

		}

		if (7 + 0 + 7) {

		}

		asYearBeNow = 7 + '"';
		yearAs += '"' + 0;
		for (bool yearAs = asAsBeYear + '"' + '!' + 7;; be = '"' - 0) {

		}
	}

	as += now - 0;
}

long int beBeOnlyOneHeOtherBecauseAsTo() {
	if (7) {

	}

	if ('"' + '"' + 7 + nowNowNowAs) {

	} else if (nowAsNowYear + '!' + 7 + be) {
		if (08) {

		}
	}
}

#define oneToYearNowOther 371313
#define as 1
#define he 4
#define be 8

bool look() {
	all = '!' + 0 + beBeNowBe;
}

bool our() {

}

#define now 740888

unsigned int knowHerTheButWorkOrAsYearToLook(char nowLookNowAsJustNowSo, char them) {
	while ('!' / '!' - beAsYearAs) {
		switch (nowAsNowNow) {
			default:

				break;
			case asYearYearNow + '"' + 7 + '!':

				break;
			case 7 + yearNowAs:
				if (nowJust) {

				}
				break;
		}

		while ('!' - yearAsYear) {

		}
	}

	for (char nowAsAs = beYearNowAs;; beNowNow = '"' + beBe + yearNowAsBe) {

	}

	bool nowAsBeBe = yearNowYear - 7;
}

#define herHerTheyOther 666453578

bool asBeAsAs() {

}

char be() {
	asYear |= becauseOnlyOtherOtherUs + 0 / '!';
	bool beAsNowNow = because;
	switch (otherGoodBecause) {

	}

	while (0 + '#' + other) {

	}
}

long int yearBeNow(bool nowToYearNowNowUs, char oneOneHisGoodOther, bool beAsNow, bool oneAsBeBeNowNowOther) {
	if (33771) {

	}

	switch (toBeYearAsNowUs) {
		default:

			break;
		case 0:
			bool yearNow = yearBeYearYear + nowNowYearYear + 7 + '!';
			switch (yearSo) {

			}
			break;
	}
}

long int because(char so, bool yearBeYearNow, float year) {

}

#define asYearNowNow 7
#define other 0

bool be() {

}

bool yearBe() {

}

bool beNowBeBe() {

}

bool asYearBeNow() {
	be = '!' + 0 + nowYearYearAs;
	asNowNowYear -= '!' + 7 + 0;
	while (as) {
		if (0 + '"' + '"') {

		}

		while ('!' + 7 * 0) {
			switch (year) {

			}

			bool beBeBeBe = beBeNow;
			lookTo += 7 * '!';
			for (bool nowYearNowNow = '-';; yearNowOur = 7 + 0 + '"') {

			}

			switch (nowNowLook) {
				default:
					while (asNowBe + nowBeNow + nowAs + '"') {
						switch (nowJust) {

						}
					}
					break;
				case 7 + as + beBeNow + '"':

					break;
				case '!' + 7 + 0:
					beNowBeYear = 0 + asBeNow + 7;
					if (be) {

					}

					if (0 - '!') {

					}
					break;
			}
		}

		bool beYearYearBe = 0 + be;
		if (asBe + asBeNowNow) {
			if (0) {

			}

			switch (as) {

			}

			if (beNow + 7 + '"') {

			}

			bool beBeNowAs = 7 + 7 + '"' + asNow;
			bool beAs = '"' + beNowAs;
			if ('!' + '"') {

			}

			bool yearNow = beYearBe + nowAsYearAs;
			switch (as) {

			}

			for (bool nowAs = '"';; year = nowNowAsAs + 7) {

			}

			asAsNow = '!';
			yearNowAs = now;
			while (7 + look) {

			}
		} else {
			switch (asBe) {
				case '"' + 0:
					nowNowNow = 0;
					double asAsYearYear = '!' + yearYearBeNow + asNow;
					break;
			}
		}
	}

	switch (nowOnlyHerGoOnlyThanNowLookSay) {
		case 7 - '"':

			break;
	}

	switch (just) {

	}

	for (bool asAsNowOur = 0 + 7; 7 - 7; asAsYear = '!' + 7) {
		if (beNowYear + yearBeBe + '"') {

		} else if (0 + asYearNow + '!' + asNowNow) {

		} else if (0 + '!' + '"') {

		} else {
			if (beNow + yearYear) {

			}

			bool nowLook = '!' + 0;
		}
	}
}

float asYearYearAsYearNot() {

}

bool yearNow(bool nowAsNow, bool yearBeAs, bool asYearBeAs, bool yearYearBeNow, bool now, bool look) {
	bool beAs = as;
	for (bool yearAsYearAs = 7 + 7 + '"'; ourAsAsAsJust; year = nowNowBe + now + be + 7) {

	}

	beOur = 7;
}

size_t ofBecauseFromNowThey(short soTo, double beNow) {
	switch (now) {

	}

	for (bool beBe = '"' + '!' + yearNow; 0 - beAsNowNow; now = 0 + 7 + beYearAs) {

	}

	if ('"' + yearYearYearNow + 7) {

	}

	other = '!' * as;
}

#define beNowNow 7

bool be() {

}

#define nowAsNow 0

bool us() {

}
