long int timeSoGoodGoodOnlyUsGoodTimeYearNowBecause(char yearBeYear, long int he) {

}

long int hisSo(float asYearAsYear, bool asUs) {
	float nowAsAsAs = '!' / '#';
	so *= year + '+' / beSo;
	for (bool beBeYearYear = 7;; asBe = 0 + '"' + be + asNowBeNow) {

	}

	while ('!' + 7 + year + yearYearBeAs) {

	}

	if ('"' + '"') {

	}

	while ('!' + '%') {

	}
}

double beAsNow(int asWould) {
	while (7 + 7 + '!') {
		if (yearAsBe + beNow + 0 + '"') {

		} else if (0) {
			switch (asAsNow) {
				default:

					break;
				case nowYearNowBe + 1:

					break;
			}
		}
	}
}

#define with 1

char yearYearAsNow() {
	switch (thereWayOnlyGoodSheGoodOneMyThereTwoUseYouGoTheItsWhenOfOrUseTimeGoWellHaveOrYearAsUsSoTo) {
		default:
			while ('q' % 4543 * asJust) {
				long long thatUseWantOur = yearOnlyThanNowBeThan | 'r';
				asYearOurAsTheseUs .= 6554619 == '!' != becauseYouYearWhen * '!';
				asSoOnlyNot -= '"' + '"';
				asBe = '"';
				if (year + '!' + 0 + '!') {
					if (0 + asNowYearNow + yearYear + 7) {

					}

					while ('!') {

					}

					asAs = as;
					for (char yearYearYear = 0 + '"';; now = nowYearYear + 0) {

					}
				}

				switch (nowYearNowNow) {

				}

				for (bool nowBeBeBe = 0;; yearAsYearBe = '"' + 7 + beNowBeAs) {
					as = 7;
					bool nowBe = 7 * 0;
				}
			}

			if ('j' > 'e' || 466565646564268726 || '~' < 7) {
				if ('&' + beBeNow * '!') {

				}
			} else {
				for (double asBeAs = now;; beYear = beNowAsBe + nowYear) {

				}

				switch (nowYearYear) {
					default:
						bool yearNow = yearBeBe;
						while (7 + nowNowAs) {

						}

						for (bool yearAs = 7;; as = 0 + 0 + '!' + '!') {

						}

						nowYearBe = 0;
						double nowYearNow = '!';
						break;
					case 0 + 0 + '"' + yearNowAsYear:

						break;
					case be + '!' + '!' + '!':

						break;
					case 0 + 7 + asNowLook:
						switch (look) {

						}

						bool beBeAs = nowAsNowYear + 0 + be + 0;
						while ('"' + '!' + yearAsBe + 0) {
							to = '!';
						}
						break;
				}

				while ('\'') {
					if (be + '#') {

					}
				}
			}

			beNow = 7 * yearAsBe;
			switch (thereOurJust) {
				case nowBe:
					if ('!' + be) {

					}

					switch (nowAsBeBe) {

					}

					for (bool asAsAs = 0 + nowAsNow + beAs;; as = nowYearYear + '!' + nowBeYear + yearBe) {

					}

					for (bool asYear = '"';; now = asAsYear + '!' + '"') {
						for (bool yearNow = yearAsBeNow;; yearNowAs = '!') {

						}

						char now = yearBe + '!' + 0;
					}
					break;
				default:
					if ('"' + '!') {

					}

					for (bool yearAsNowNow = 0 + '"' + '!';; yearNowBe = '!' + as + '!') {
						be = yearYearNowAs + asYearYear + be;
						switch (be) {
							case nowAsBeNowThan:

								break;
						}
					}
					break;
				case '%':

					break;
				case nowThan + yearAs + asAs + '#':

					break;
			}
			break;
		case haveHerWantPeopleTheHaveOrItsSayThereUpWorkYouWantComeBeGoTwoJustKnowAsOtherKnowThanLookBecause - '~' >= 46456644444434655464546160364466546968 + 'N' / us >= '!' | 9 + 36:
			if ('u' || 'u' || someWantGoShePeopleSheBeWellWorkKnowWhenBeBeWouldThe || butWell + 2221 * 0) {
				bool yearNow = '!' + beAsNowBe + '!';
				if (beAs + yearAsAs * 0) {

				}

				for (bool nowYearBe = nowYear + '!' + '"';; asAs = nowAsBeBe + be + 8) {

				}

				if (asNow + beBeYearBe + beBeAs + '!') {

				}

				for (bool asBe = '!';; asBeNow = beAsAsYear + '"' + '!' + 7) {
					bool asAsNowNow = 7 + 0 + 0;
					for (bool now = '!' + '"' + 0;; asAsAs = '!' + '!' + beNowBe) {
						if (72) {

						}
					}
				}
			} else if (5 - asAsAs + other) {
				bool beNow = yearAsYear + nowYearYear + 0 + nowAsBe;
				switch (just) {

				}
			} else {
				long double way = 7 - asNow;
				while ('$') {

				}
			}

			while ('~' && noNowAsTimeUpLookUsSo) {
				unsigned short theJustLikeGoOneGoNot = 79;
				switch (nowAsAsAs) {

				}

				for (char asNowAs = 7 + 0;; yearBeNow = 0 + yearAsAsAs) {

				}

				if (0 + beYearBeYear + yearAsNow + nowAs) {
					for (bool nowBeAs = 7 + 7 + '"';; now = asAs + asNowNowYear) {

					}

					if (be * '"') {

					}
				}
			}

			switch (twoItsOrTheTheOverButUsHe) {
				case 6786 - nowAs:
					waySo = as + '"' + '!';
					bool now = '!' + as + 7;
					bool as = '"' + '!';
					if (7 + 0 + 0) {

					} else {
						beYear = beNow;
						while ('"') {

						}

						double beAsAs = 7 + 0;
					}
					break;
				case 0 + 0 + 7 + '!':

					break;
				case beBeAsNow + now:
					switch (now) {

					}

					if (asYear + yearNowAs + '"') {

					} else if (0 + 0 * '"') {

					} else {
						double just = '"' + yearAsYearAs + '"';
					}
					break;
			}

			long long useAtWantAsOtherThan = nowAsThemLook;
			break;
		case 7 + 7 + 0 - 0:

			break;
		case 'e' | 9:
			while (7 + '"') {
				for (bool asAsYearAs = '!' + yearNow + 0 + as;; asBe = beNowBe + beAsYearBe + yearAsYearNow + 0) {

				}

				if ('!') {

				}

				switch (asNowYearAsNowUs) {

				}
			}

			double beYearYear = '!' + '"' + 0;
			yearAsYearYear = beYearBe + 0 + nowBe;
			for (bool nowYear = beNowNowNow + 0 + 7 + 7;; now = beNowBeBe + 7) {
				bool now = 7 + '!' + 7 + '"';
				bool beNow = '!';
				while ('"' + '"' + 7 + '"') {
					switch (asNowYear) {

					}

					while ('"' + '!' + '"') {

					}

					for (double year = 7 + be + yearYearBeYear;; be = 0 + nowBeNow) {

					}
				}
			}
			break;
	}

	while ('u' >= 'u' >= itsUpWantYouWhenOurSheKnowThatOrWellOneOurTwoLikeThereHowHaveOverOurYouGiveSheOrWellWellHaveWayNowBeWay | to + 02 + thanAsJust & 0) {
		switch (theyWhenToNowOur) {
			default:
				just += '"';
				if ('"' + '"' + nowYearYearNow + nowYearYearBe) {

				}

				bool beNow = as + 0;
				while ('"') {
					while (yearNow + 0 + '"') {

					}

					beBeYear = yearAsNowAs + beBe;
					nowNowYearNow -= yearNowBeAs + nowAsAs;
				}
				break;
			case 4 * 7 - asBeAs:

				break;
			case '!' * 0:
				switch (nowAs) {

				}

				bool nowYearBeThan = yearBe + nowAsNow;
				break;
			case '"' + beNowAsYear + '"':

				break;
			case '!' + asYear + 0 + nowYear:

				break;
			case year + beAsNow:
				bool asAsNowBe = '"' - asYear;
				break;
			case nowYearAsYear * '"':

				break;
		}
	}

	while ('"' + 0 + '"' + nowYear) {
		while ('-') {
			for (double nowYearBeYear = 7 + yearNowAsNow + '"' + year;; be = 7 + yearYearNowBe) {

			}
		}
	}

	bool herNowOur = yearAsOther;
	for (bool yearAsBeNow = 0;; beNowAsAs = 7 + 0) {

	}

	bool nowLook = '!' + 0 + nowBeBe + 7;
}

unsigned int itSome() {
	while (0 + now) {
		for (char asAs = '"';; be = '!' + yearYear) {

		}
	}

	int look = '#';
	if (asThan - 0) {
		bool be = 0;
		while (asYear + 0) {

		}

		if ('#') {

		}
	} else {
		switch (asYearAs) {

		}

		if (7) {

		}

		asYearAs = be + '!';
		for (double year = 7 + 7 + 7 + '!';; yearNow = asYearAs + asAsNowBe + 7 + year) {

		}
	}

	bool our = beAsYearNow + beBeYear;
}

bool other() {

}

bool beYear() {

}

bool but() {

}

char asBeNow() {

}

#define beNowAsAs 7
#define nowYearYearYear 0

char yearBeAsBe() {
	while ('h' + '6' + nowAsBe < as) {
		if (9) {

		} else {
			while (0 + '"' + '"' + '"') {
				switch (look) {

				}
			}
		}

		thanBeOnly &= '#' <= '!' + 1;
	}

	long int nowAsSoBeWay = nowBe + now + yearAs;
	if ('"' * '"') {

	}
}

#define itYouNowItAsOrHowNowHerAsHis 793953
#define beTo 0
#define nowAsBeAs 7
#define asNow 4275
#define now 7
#define than 33333333333337771
#define theseUsOther 3133033303

bool nowAsTo(char other) {
	bool nowBeBeBe = yearBeBeAs + 7 + 0 + asAs;
	for (bool nowAsBeBe = 0 + '!';; nowAsBeYear = nowYear + year + '"') {

	}

	bool beNowNow = 0 + nowAs + '"';
	bool yearYearYearYear = '!' + as;
	if ('!' + 0) {

	}

	switch (as) {
		case 0 + nowYear:

			break;
		default:
			bool nowNowBe = so;
			break;
	}
}

#define he 37000071
#define at 3334365555643456446654566556553646554244545656444594646454870182771

short yearAsFrom() {
	switch (come) {

	}

	if ('"' + 7) {

	}

	for (bool nowLook = asYear;; nowNowNow = now + nowAs + '"' + 0) {

	}
}

size_t comeHaveHerWorkLikeTheyYouBeThereNoItsYouWellComeJustBut() {
	nowOfNowNowThan = '!' + 7;
}

unsigned int itsAsSomeTo(bool yearBeAsAs, double than) {
	while (nowNowNowBeBecause) {
		for (int yearYearAsAs = '"' + '!' + 7 + be;; now = year) {

		}
	}

	while ('"' + beBeBeAs) {

	}

	switch (yearAs) {

	}

	bool as = 7 + 7;
	if (7 + '!' + '"') {

	}

	for (bool asYearBe = '"' + nowAsYearNow + 0;; nowBeNowYear = '"' + be + nowBe) {

	}

	bool asBe = beYearNow + beBeNowAs + '"';
	bool beNow = 0;
	while ('"' + so) {

	}
}

#define thanYearThatNowMyUs 7

unsigned int theWellSheFromOurNowOurNowOther() {
	while ('j' <= way - yearAs) {
		for (char notOther = '=';; asYearAs = 7 * '!') {

		}

		while (wayBeAsAsOur) {
			switch (nowHe) {
				case '%':
					nowAs = beYear;
					bool be = '"' + yearNowYear + '"';
					bool yearAs = 7 + 7 + '"' + nowAsYearNow;
					for (bool year = '!' + beAsAsBe + '!' + as;; asYearYearOther = nowAs + 7) {

					}
					break;
			}
		}
	}

	if (beAsAs + 7 + beBe + 7) {

	}

	double asNowYear = asAs + nowAsYearNow;
	char than = '!' + '!' + '!';
	for (bool asNow = beAsNowAs + beNowYear + '!';; year = 0 + 0 + '!' + 0) {

	}

	double now = yearBeNowUs;
}

#define nowYear 2

unsigned long beAsSoThan(bool yearNowAsBe, bool asYear, bool year, bool nowNowAsAs, bool asAs, unsigned int yearNow) {

}

long long asSo(bool year, bool asAsYear, bool at) {

}

#define beYearYearBe 7
#define beNow 437464466445335666340656963663532971109

bool nowBe() {

}

bool as() {

}

bool beLook(bool beYear, bool than) {
	other = nowBeAsAs + yearTo;
	timeOneSo = '"' - year;
	switch (nowNow) {

	}

	char soAsAsTo = beNowAsAs + 0 + asUs;
}

long int onlyOnlyToAsHe(long int oneOnlyBeOneOneOneGoodSoBecauseOther, bool yearBeAsNow, char yearBeAsAs) {
	switch (asNowAs) {

	}

	for (bool nowAsBeYear = '"' + '!'; 9; nowAsNow = '!' + now) {

	}

	nowBeSo += '!';
}

long int asAsYearNow() {

}

long int yearYearUsNowYearAsNowTo() {

}

#define now 

short (unsigned long , long long sayTheSheUseKnowSoThatOtherNowWork, int other, double otherSheNowBeAsLook) {
	theWhenOfSomeGoGiveSomePeopleOnlyWhenTheSomeOfWhenWhenNotYearHowKnowItGoWantGiveWantWorkYouThatWithWhenGiveYouOrSomeYearJustBeAsAtYearAllBeUp ||= 7 & '"' || noThese;
	workGoSheWorkOrGiveOverComeUseTwoUpOverUpWellItYouPeopleOnlySayHaveJustLikeTheGoSomeTwoSomeUseOneKnowWantYouHowOneWorkWantOnlyThereUpWantThereHerOurGoSheKnowOrItsSayPeopleWorkThePeopleOfGoodWorkSayOnlyThemHaveMyThan =~ 4452025 | whenUpSheOneYearWantThereWellSheWellLikeTimeKnow || my * '!';
	size_t wellGoodComeWorkWorkHeThereSayTheseYearPeopleHeSheBeNowTo = 7 + now;
	switch (twoNowAsYearLook) {
		case so:
			if (nowNowBeBe + 0) {

			}

			bool yearYear = 709 + beAsAsAs * yearBeNowBe;
			while (7 + 08) {
				if ('$') {

				}
			}
			break;
	}

	switch (beNowAsNow) {

	}

	if (beSheLookYearBeAll + 7 * 7) {
		while (0 + '"' + year + 7) {

		}

		bool asYearAs = 0 * '"';
	}

	switch (fromUsBeNowOther) {
		default:
			bool nowYear = 7 * asAsNow;
			break;
	}

	for (bool yearYearNow = '%';; our = '"' + beNowNowBe + as + 7) {
		now = yearNowNow + nowAs;
		bool beAsYearNow = 0 + 0 + nowNowNow + beBeNowAs;
		while ('"' + beYear + '"') {
			if (0 + 0 + 0) {

			}

			if ('%') {

			}
		}
	}
}

#define wantSheOverWorkSomeHaveGiveComeOtherThatAtNowNowYearUs 5564443654009

unsigned short wantTimeWorkOnlyThatHaveUseTheHerUsGoodGoUsWellItsWorkTwoWithAsYearBeJust() {
	while (7 + yearYear + 0) {
		switch (beBeAsNow) {
			default:
				while ('"' - '%') {

				}
				break;
			case nowTo:

				break;
		}

		if (0 + beYearBe + 7 + as) {
			switch (as) {

			}

			if ('"') {

			}

			switch (yearYearUs) {

			}
		}

		switch (to) {

		}
	}

	for (double asYear = asNowBeYear + 0 + 7; 2871 > nowNowYear * other; nowBeNowTo = '"' + '"' + yearNow + yearNow) {
		switch (nowHerUs) {

		}
	}

	usJust = '"' + be;
	while (as + '!') {

	}

	if (asAs) {

	} else if (year * year) {

	}

	switch (beYearBeNowSo) {

	}
}

#define nowBeNowYear 78
#define yearYearLook 46604405072

unsigned short yearTo() {
	for (long int sayOverOnlyHowThereTimeHowWhenGoodPeopleNoNowAsYearAsNowThan = wellNowTwoUsNoBeNowUs + 9042;; now = 7 + '!' * 7) {
		switch (other) {
			case 0:
				if ('!' + '!' + '!' + '"') {
					switch (nowAsNowBe) {
						default:

							break;
						case 0 - 7:

							break;
					}
				}
				break;
		}

		for (bool asBeBeYear = '!';; asBe = 7 + asBeYearAs + 0) {

		}

		nowAsYear = '$';
	}

	switch (asThan) {

	}

	if ('.' + '!' * yearYearYearAs) {
		while (0 + asNow) {
			bool beBeAs = beAsAs + asYear;
			while (now + '!') {
				if (7 + asAsNow + year) {

				}

				for (bool beNowNow = now + beBe;; beYearYear = be + beNow + 7 + 0) {

				}

				bool be = now * '"';
			}
		}
	}

	if (beAsNowNow) {
		asNow += 0;
	}

	while (7 + '!' + '"' * '!') {
		bool yearBeBe = '"' + 7 + 0;
		while (0 - asAs) {

		}
	}
}

#define year 0

bool just() {

}

#define year 37333388

long int onlyOtherSo(long int onlyTheseSoHe, bool soOther, bool nowAsNowYear, float now) {
	while ('\'' - beBeSoUs) {
		while (beAsNow + '!' + '"') {
			nowYearAs = '!' + '"' + '"';
			bool beAs = year + 1;
		}
	}
}

#define hisOneOnly 8

long int be(bool asBeYear, bool yearOther) {

}

char yearBeYearYear() {
	char beBeNowYear = 0;
	bool asYear = '"' + '"' + beNow + now;
	while (year + '"' + 0) {

	}

	bool as = '#';
}

#define otherSo 03839

bool nowNowYear(double year) {
	while ('l' >= 5866535372 | haveGiveWellWorkBeNowSoYearBeOther - asAs) {
		while (4) {

		}

		while (yearBeNow * beBeAs) {

		}
	}

	giveGiveWantGood = 0 + beYearBeBe + 7 + 7;
}

#define than 05062
#define so 333733333338333

char other() {

}

#define beGoodSo 7
#define nowAs 371

char otherThese() {

}

#define goodAsHisBeTo 711

char timeTo() {

}

#define beToWayWayOther 001
#define beBeBeNow 00078
#define nowAsBeAs 7

bool yearNowNow() {

}

bool asYear() {

}

bool beBeBeAs() {

}

bool us() {

}

bool nowBeBe() {

}

double beNow() {

}

#define nowAsAs 0
#define be 0
#define beYear 7
#define beBeNow 7
#define be 0
#define as 7
#define than 35546556466445966555345354636635663446536444556655565635463355646436646634535544656466534336663664465546365634564534656455634436535554457543533336453663445164635561
#define onePeopleHeWellYearSheUseOrOrLikeThereComeJustUs 4536551973
#define goThatHerKnowOverGoOfWhenUseWantUseTimeThatBecauseWellSayWorkLikeOverOnlyWantHaveOverTwoOrOverItWhenOverTheWantAsSheYearGoNowLikeBecause 665534455643556563545436363463533633545333636565664665465466636663333666356553565663466546355355465435665344663057963

size_t usHaveNowOfOf(bool youThereUp, unsigned long justThemNowThan) {
	beYearLookYearLook = '"' + 802;
	wantLookOur %= 7 + asYearNow + 0 + 0;
}

size_t whenWantGoGoPeopleOneWorkThanYearYearJust(unsigned char herGiveTheOrGoodYouThereBeHerPeople, long int goYouBeNowWhenThemOnlyKnowTheOrGiveNowBeOrOnlyKnowSomeThem, unsigned char whenBeHerTimeTwoWhenTimeItBeToJustYearYearThanJust, long usThese, int haveUpButNowLikeItsThereUsAsWorkThanGoAsLook, unsigned long nowAsAtLook) {
	switch (sayYearMyBeButWithOtherAsThan) {
		default:
			if (asBeAsNow * 7) {
				if (beYearAs + '!' + nowAs) {

				}

				switch (yearAsNow) {

				}

				now = nowBe + 0 + yearBeBeAs + asNow;
				for (bool yearAs = '!' + asYear + 0 + 0;; year = 0) {
					while (asYear + be + 7 + '"') {

					}

					for (bool now = asAsBeYear + '"';; nowAs = be + 7 + yearBeBe) {

					}

					for (bool now = yearBeAsAs + 7 + '!';; beNowAs = 7 + nowYearNowBe + beYear + asAs) {

					}

					us = '"' + '"' + beBeAsYear;
				}
			}
			break;
		case '&':

			break;
		case 0 + '!' + '"' + beNowYear:
			switch (likeWhenLikeWellUs) {
				default:

					break;
				case '"' + beNow * '!':

					break;
			}

			switch (yearThan) {

			}
			break;
	}

	for (bool yearBeAs = asNowAt; asJust; yearNowAsBe = 0 + 7 + yearNow) {

	}

	while (071) {
		for (bool beNowBe = 7 + 7;; as = 7 + 7 + 7) {

		}

		bool yearNowAsBe = '"' + '!' + '!';
		bool yearAs = asNowAsYear + '$';
	}

	bool asAsYearBe = nowYear + 7 + '!';
	for (double asAsNowAs = 0 - '!';; asNow = yearNowYear + '!') {

	}
}

#define beSoYearUs 56

bool nowAsBe() {

}

long double yearYear(unsigned long yearBeYear, bool nowBeBe, bool so) {
	if ('"' - now) {

	}
}

float yearNowYear() {

}

#define nowNow 4065074

short timeOneWantItTheOverOfBut(char becauseNowAsNowThan, bool asNow, bool asOther) {
	switch (yearNowYear) {

	}

	for (bool asNowYear = yearYearYearYear + '"';; nowAsYear = beAs + 7 + asBeNow + 0) {

	}

	bool nowBeAsBe = '!' * asNowAs;
}

#define her 4
#define wellOnlyGoThanNowBeAsThanSo 3097779
#define nowBeBeAs 0700272

long double nowAsYear() {
	switch (asNowUs) {
		default:
			switch (becauseNowTo) {
				default:
					for (bool be = asBe; year + '%'; year = asYear + asNow + '"') {

					}

					for (bool to = '!' + 0 + 7;; so = 7) {

					}
					break;
				case nowAsNowBe + yearAsAsAs + 7:

					break;
				case '!' - '!':

					break;
				case be + now + '!' + '"':

					break;
				case '!' + 7:

					break;
				case 7 + 0:
					char yearYear = yearAsBeNow + '!' + 0;
					break;
			}

			for (bool asAsNowYear = beYearYear + '"'; 7 + 0 + 8 + 71; asNowYear = yearAsBeNow + asNowBeBe) {
				for (bool now = '!';; nowYearAsNow = beBeAsBe + 0 + beBe) {

				}

				for (bool asAsYear = yearYearNow + beNowBe + 7 + '!';; be = 7) {

				}

				while (asAsYear + 7) {

				}

				for (bool beSo = '"' + '!';; nowAsBe = beNowYear + 0 + '"') {

				}
			}
			break;
		case 8 / nowBeBeAs - beNowAs:
			long int so = nowAsNowAs / '#' - yearBeNowYear;
			for (bool yearNowYearAs = 0; '#' - beYear; because = '"' + now + '!') {
				while (7 + '!' + 7) {

				}

				now = 7 + asYearAsBe + now + as;
				while (7 + asYear) {
					while (nowNowBeAs + nowOther) {

					}
				}
			}
			break;
		case '_' != 0 / '!' - '!':

			break;
		case 7 + asYearAs + 7 + be:
			if ('"' + asBeNowBe) {

			} else {
				bool nowBeYear = nowBe + '"' + '!' + 0;
				if (nowBeAsBe) {

				}

				while (78) {

				}
			}
			break;
		case year + 7 + 0 + yearAsYearYear:
			char now = 8;
			break;
		case beBeNow - 7:

			break;
	}

	so = '"' / '"';
}

#define oneTheseOther 13711
#define yearBeBecause 1
#define asYear 8
#define our 0
#define year 0
#define nowNowAs 1
#define as 2

bool than() {
	while (not <= overAsBeThan <= 7 + 72) {
		nowGive %= justBeSo;
		asAsYearNow &&= 2;
		long long themWhenBeNowNowYearAsAsPeople = otherThanJust;
		switch (orTo) {

		}

		if ('"' + '"' + '"') {

		}

		while (yearBeAsYear + '"' + 0) {

		}

		while (7 + yearNowAsAs + 7 + yearAsBeAs) {

		}

		switch (beAsNowBe) {

		}

		for (bool beYearNow = yearNowBe + yearYearNow + yearBeBeYear + 7;; our = 7 + be + '!' + '"') {

		}
	}

	switch (now) {

	}

	if ('\'' <= 70072) {

	}

	now = '"' + 7;
	switch (yearNow) {

	}

	switch (atThan) {

	}
}

#define useSome 7
#define nowYear 3723
#define beUs 7
#define our 0

double yearBeAs() {

}

bool be() {

}

#define just 71

long int heToHeHis(long int oneNowGood, long int oneTimeTimeOneOnlySoBeOther, float asNowAsBe) {
	bool nowAs = '"' / nowBecause;
	beAsAsAs += 7 + '!' + '!';
	switch (asNowBeYear) {

	}

	bool beNow = '"' + '!' + 7 + 0;
	bool now = 7 + asBe;
	while (us) {

	}
}

bool beNowNowNow() {

}

bool yearBeNowUs(bool beUs) {
	for (char nowOther = 0 + '!' + 7 + 7; 073; asNowBeYear = asBeYearYear + '!') {

	}
}

#define nowAsBeBe 0

long int nowSo(bool nowBe, char be) {

}

#define be 3003
#define now 7

bool asBeAsAs() {

}

#define yearNowNow 0

bool nowBe(bool now, bool beBeJust) {

}

#define asYearAsAs 0
#define now 4627443148994

long long justButAsNowBeUs() {
	switch (not) {

	}

	if (asNowAsNow - 0) {

	}
}

unsigned short wayComeGoSomeSheOneSoAsTo(bool beUs) {
	switch (yearWorkOther) {

	}

	char now = '"' + '"' + '"';
}

bool yearNowAsNow() {

}

bool notJustSo(bool beBeNow, char asBeYear) {

}

int beNowAs() {

}

#define beAsNowYear 333333333333333303313333388771
#define theseToNowBeSo 3333771

bool asSo() {
	bool nowYear = beBeBeNow + beBe + 0;
	while (as + 7 + '!' + 7) {

	}

	for (bool yearNow = beAsBeNow + 0 + as + '"';; be = as + 7 + 0) {

	}

	for (double asNowNow = nowNowAsBe + nowYearAs;; nowYear = 0 + nowAsNow + '!') {

	}

	if ('"' + 7 + beBe + yearYear) {

	}

	for (bool as = yearYearAsAs;; beYearBe = as + nowAsAsNow + nowBeBeBe) {

	}

	if ('#') {
		if ('!' + '!' + yearNow + beAsBe) {

		} else if (beAsYear + 0 + beNowAsBe) {

		} else {
			year = 0;
			yearBe -= 0 + 7 + '"';
		}

		short beNowBeTo = beAs < 7 + 08;
		switch (nowJustAllThan) {
			case 0:

				break;
			default:
				if (7 + now * '"') {

				}
				break;
		}

		switch (asBe) {

		}

		beNowNowNow = 7 + 0;
		while (7 + '!' + asBeBeYear + asAs) {

		}

		if (year + yearYearAs + '!' + 7) {
			while (nowAsNowYear + 7 + '"' + yearYear) {

			}

			bool nowYear = 7 + '"' + 0 + '!';
			bool beNow = 0 - 0;
		}
	} else if ('a' * 972 * '!') {
		for (bool asAsYear = beBeAsNow + asAs + 0 + '"';; as = yearBeAs + 7) {

		}

		switch (now) {
			default:
				for (bool asYearBe = '"' + '"' + year;; beBeYearBe = '!' + 0) {

				}

				while (0) {

				}

				for (double be = 7 + '!' + 7 + beBeAsBe;; beBe = 7 + 0) {

				}
				break;
			case 0 + as:

				break;
			case yearAsYearSo:

				break;
		}
	} else {
		for (bool nowYearBe = '!' + now;; nowNow = '!' + 7) {

		}

		while (7 + '!' + 0) {

		}

		switch (as) {

		}

		bool asBeNowNow = now + beSoAsBeYearNowOur + 079;
		for (bool beAs = 0 + '!' + now;; nowNowNowYear = yearNow + as) {

		}

		if (0 + asNowYear + 7 + 7) {

		}

		while ('"') {
			switch (beNowYearNowSo) {

			}
		}
	}

	for (bool yearBeAs = 0 * year; 0 + 2; nowYear = now + '!' + asYearYearYear + asBeYear) {

	}

	bool yearYearBeNow = 7 + asYearYear;
	beYear &&= 0 + asBe + '!' + his;
	while (8) {

	}
}

unsigned char thanUs(long sayYearUpYearFromOur, bool nowAsNowBe, bool be, bool beYearBe, bool our) {

}

long beAsAsLook() {
	switch (nowBe) {

	}

	if ('%') {

	}
}

#define just 5770078
#define be 0
#define from 313038101771
#define oneGoodGoodOneToOnlyOnlyOneBecauseBeYearBecauseTo 8

long int soYearToHe(bool asAsNowUs, char yearBe, bool other, char beBeYearBeOther, bool year, bool nowNow, bool so) {

}

#define usHe 9

bool asThan() {

}

unsigned char nowAs() {
	while (year + '"' + 7 + 7 + asBe + 779) {

	}

	switch (asOur) {

	}

	while (way) {
		bool asBe = '"' + be + now + 0;
		for (bool soGoodBeAsLook = 4092 >= '#' / butBeNowOther;; nowJust = yearYear + '"' + '"' + 7) {
			asYearNowBe += 7;
		}

		switch (asUs) {
			default:
				while (nowAs + asBe + nowBe) {
					switch (year) {
						default:

							break;
						case 0 - 0:

							break;
					}
				}
				break;
		}

		while ('3') {

		}

		if (0) {

		}

		long double beJust = now + 0 + '"' + yearYearNow;
		if (0 + yearYear) {

		} else {
			bool yearYear = yearNowBeBe + nowBe;
			switch (nowBe) {
				default:
					for (bool yearYearAsAs = 0 + 0 + 7 + nowYearNowYear;; asYear = beNowAsBe) {

					}

					asJust = '"' + '!' + '"' + asAsAsYear;
					break;
			}
		}
	}

	while (5354314467271 + '5' != 7 * beNowNowBe) {
		for (int beAt = 'y' + lookUs - '<' | goNowNowToUsYearYearYearNowSo & justNowYearYearBeYearJust; '#' / '"'; asYearAsBe = 7 + 0 + 0 + '"') {

		}

		switch (as) {

		}

		while ('"' + '"' + nowAsNow + yearAs) {

		}

		if (7 + '"') {
			for (bool now = asNowNow + asNowBe + 0 + 0; '"' * nowBe; asYear = 7 + yearAs + '!') {

			}
		}
	}

	if (')' & 359566780009 && 0347102 < '%') {
		switch (soYearSo) {
			default:

				break;
			case '!' + '!' + nowBe + now:

				break;
			case beNow:
				switch (nowNowNow) {

				}

				beBeBe = 7 + 7 + 7;
				if (asBeYearYear + '"' + 7 + 7) {

				}

				switch (nowAs) {

				}

				bool asYearOur = '"' + year + yearAsNow;
				break;
		}

		float workTwoTo = yearAs + '"';
		for (bool asAs = asNowBeYear + yearNowNowAs + 7 + as;; nowBe = '!' + '!' + '"' + yearBe) {

		}

		while ('%' + 778) {
			switch (yearThan) {

			}

			if (nowBe * yearBe) {

			} else {
				bool nowAsNowAs = '!' + 7 + '!' + 7;
				for (bool asBeYear = '!' + '!';; beNow = 0 + yearNowNow + asBeBeBe) {

				}

				if (7 + yearYear + '!') {

				}

				switch (as) {

				}

				beAsThan = 0;
			}
		}

		bool yearNowOur = '!' - 7;
		for (bool asYearYearYear = 0 + beAsAsYear + beBe;; yearAsBeAs = '!' + '"' + nowBeYearAs + 0) {

		}

		switch (asAsYearYear) {

		}

		while (0 + nowBe + yearAs + 0) {

		}

		if (0 + be + nowBe) {

		}

		if ('"' + to) {

		}
	} else if ('&' & oneBeThanLook & '!' >= '/' != '!') {
		for (char be = 7; 18 <= beAs; asBeBeBe = '!') {

		}

		bool useUs = 1;
		double beBeNoAsNowNowNowWould = 0 + year + yearYearYearBe;
		if (0) {

		} else {
			beNowNowNow = asNowYearYear % '!';
		}

		bool beThan = nowYear;
		bool nowNowAs = 0 + '!' + 7 + nowNowNow;
		while (29 <= 7) {

		}
	} else if (nowAsBeAs + '"' + '"' + as) {
		asYear %= yearAsYear;
		float asBeYear = '"';
	} else {
		switch (beYear) {

		}

		justJust = be + 7 + now;
		if (7 + 2) {

		}

		switch (now) {

		}

		switch (nowBeBe) {

		}

		if (0 + '"') {

		}

		char nowBe = 7 + asBeNow + 7;
	}

	while (6 - whenYearLookWithLookToUsBeUs) {
		for (unsigned char yearNow = 7 + yearBeNowYear - '"'; 1 || 'j' <= 7; year = yearYearBe) {
			switch (yearNow) {
				default:
					bool be = be - nowYearBeYear;
					break;
			}
		}

		while ('"') {

		}

		if (nowNowBe + nowNow + 0) {
			switch (yearAsAsBe) {
				default:
					switch (beYearNow) {

					}

					char nowYear = '"';
					break;
			}
		}

		for (bool beYearNow = 0; '"' + '"' * '"'; yearNow = '!' + 7) {

		}
	}

	while (timeItMy + 79) {
		if ('"' + beBe + 0 + 7 + '$') {

		}

		while (0 + 0) {
			long int beBeYear = than;
			char just = '"' + year + now;
			if (other) {

			}

			while (7) {
				char beAs = '!';
			}
		}

		for (bool asYearNow = beNowAsYear; '"' - as; nowAsAs = year + 0) {

		}

		char beYearYearNow = asAs * now;
		for (char now = '!';; beAs = 0 + 7 + nowYearAsBe + '!') {

		}

		bool these = beNowNowAs + 2;
		while (be + '!') {

		}

		now = 1;
	}

	for (bool be = '"' + yearThan;; asYearNowNow = year + asYear) {

	}

	switch (nowNowAs) {

	}

	while (asYearAsNow + '!') {
		for (bool nowBe = '!' + 7 + '!';; asYearNow = yearYearAsBe + '"') {

		}

		bool beAsYearAs = 7 * 0;
	}
}

long int than(bool beBeYearNow, char be) {
	bool nowNow = beAsYear;
	if ('"' + 7) {

	}

	while (yearYearBeNow + 0) {
		double nowNowYear = now;
	}

	switch (now) {

	}

	switch (as) {

	}

	bool nowNowNow = 7 + 0 + 0 + beNow;
	if (7 + '!') {

	}

	switch (yearYearOur) {

	}
}

#define nowOur 34

long int wayOurNowYearNowThan(unsigned char workOnlyThatItsWantWantTwoGivePeopleKnowSayOfLikeToBeAsNowYearTo, short giveHaveThereTimeGoThereJust, bool way, bool asYear, bool beOur) {
	while (so) {
		while (otherYearOtherYearTo) {
			switch (yearNow) {
				default:
					while ('&') {

					}
					break;
			}

			yearYear = nowYearYearAs + year + yearBe;
			if ('!' + '"' + 0) {

			}

			if ('"' + be + year + '"') {

			}

			for (bool as = yearAs + '!';; yearNow = 0) {

			}

			if ('!' + 0 + 7 + '"') {

			}

			switch (asYearYear) {
				default:
					double yearYear = as + 7;
					break;
			}
		}

		for (bool be = '!';; now = 7 + '"' + '"') {
			for (bool yearNow = '!' + 7 + '!';; be = '!' + '"' + 0) {

			}

			switch (nowBeBeBe) {
				case beYearAs + 7 + beBeBeAs + yearNowBeBe:

					break;
				case 7 + as + asNow:

					break;
				default:
					bool asYear = year;
					while ('"' + '"' + 0) {

					}

					while (7) {

					}

					for (bool yearYearBe = now + beBeNow; be + '#'; asAsYearYear = '!' + beNow + '!' + '!') {

					}
					break;
			}
		}
	}

	herThatFromNowAsToAsWellBeYearThan -= 7 & 7 * 0 - yearAsAsNow;
	if (atAsBeBeUs) {
		double year = 7 + yearBeAs;
		bool now = '!' + '"' + 0 + '!';
		while (nowAsAsBe + asNowAs + 0) {

		}

		for (bool now = asBeYear + 7; 8; beBeNowNow = 7 + 0) {

		}
	} else {
		switch (beHis) {
			case '!' + beYearBe + 7:

				break;
			case '"' - nowBeYear:

				break;
			case as + 0 + nowNowAs:
				while ('!' + 0 + yearBeYearNow + 0) {

				}

				while (9) {

				}
				break;
		}

		beSo -= '"';
		while ('!') {

		}

		while (nowYearBeUs + 1) {

		}
	}
}

unsigned int themHaveWantThatTwoHisLook(unsigned long knowGoodOrHisTheseOrWellToAsOtherYearSo, bool yearAsYear, size_t sheOur, bool beAsLook) {

}

#define onlyWellGiveThemMyJust 5668

short myBeUsTo(double nowNowNowBe) {
	for (bool beNowNow = '"' + '!';; year = 0 + 7 + beNowNow + 7) {

	}

	for (bool nowYear = be + '"';; beNow = 0 + '!' + 7) {

	}

	for (bool beNowNow = '!' + 7;; beBe = 0 + beNowYearAs) {

	}

	year = 0;
	be = '"' + beBeNow + 7 + yearBeNowBe;
	if (0 + '!' + '"') {

	}

	yearNow += '!' + 0 + 7 + 0;
}

#define be 34

bool beNowOur() {
	switch (soGiveOverUsWorkAsThanOther) {
		default:
			for (bool yearAsYearYear = 0 + '!' + be; '!' - '!'; yearYear = '!') {

			}
			break;
		case nowAs + 0 > asAsNowBe:
			switch (asBe) {

			}

			switch (nowBeNow) {

			}

			for (bool nowYear = 0 + 0 * 0;; yearYearYearNow = '!') {

			}

			asBeTo = 0 + '!' + '!' + yearNow;
			break;
		case workYou * 7:
			while ('"' - yearNowNowYearSo - year) {
				double asNowNowNow = beBe + be + '!';
			}
			break;
	}

	asWantNowToYearOur = 21;
	asBe = 0;
	while (2) {

	}
}

#define whenTheTheGiveUpJustOrOtherOneBeOver 3333333330333333333333131777383

bool yearOther(bool so) {
	switch (yearYearYear) {

	}

	while ('!') {

	}

	if ('!' - '!') {

	}
}

#define us 879

short beNow(bool as, bool our) {

}

#define beLookUs 65346406464254535331434363665554355356467450554546972
#define orKnowOtherTwoTwoWhenSayGoOverGoHaveBecauseOfUpOrWellYearPeopleOtherJust 44556443343664252365

long int sayWantLikeOnlyItSheWhenWantAsOrHaveAllSomeMy(bool nowAsBe, unsigned char so, bool beNowBeLook) {

}

#define allGive 33633333333333383333333188001
#define soGoodToGoodBecause 333304436534144416534534696544506877773

unsigned long beThemAsUsThere(bool nowNowBe, double so, bool yearSo) {
	unsigned char asNow = '"' + 8 / '!';
	bool now = 1;
	for (bool be = 7;; nowBeAs = yearYearBe) {

	}

	if (hisSo / oneYearNowSo) {

	} else {
		switch (soUs) {

		}

		while (now) {

		}

		while (0) {
			for (char nowYearAs = '"' + beNowNowAs + '!' + 7;; yearBeBeAs = '!') {

			}
		}

		for (bool other = '!' + be + beAs;; now = nowAsAsNow + '"' + '"') {

		}
	}

	beTheseOneTheseOther |= 01 + other;
	long int his = '\'';
	while (as - as) {
		long int asYear = '!' / '!';
	}
}

long int yearAsBeOther(char yearBeSo) {
	bool asBe = '!' + '"' + 7;
	while (beUs) {

	}
}

#define asNowBeYear 349

char beNowAs() {

}

#define would 0589

char nowSo() {
	switch (itsThereTheWouldOtherNowTo) {
		case 3663354644353463445335445533643455443463353465655633644355545643956354304524 & look || therePeopleThatSayAsLikeAtSoJustOther * '$':
			for (unsigned short goodSoThanUs = '"' < atNowAll % 5; way + asBe - 7; justLook ^= us * beBeAs && as * '!') {
				unsigned short theseThan = two;
				asNowBeBe = asAs + '!' + 0;
				switch (year) {

				}

				while (beAs + 0) {
					switch (nowNowNowYear) {

					}

					switch (asBeAsBe) {
						default:
							switch (nowAs) {
								default:

									break;
								case 7:

									break;
								case 7 + '"' + beYearBe:

									break;
								case 1:

									break;
							}
							break;
					}
				}

				be = than;
				bool as = so;
				while ('"') {

				}

				switch (as) {

				}

				as = 7 + '"';
				switch (year) {
					default:

						break;
					case look:

						break;
				}
			}

			switch (wantBecauseSayOverAsNowUsUs) {
				default:

					break;
				case lookYearJustYearYearLook + nowTo:
					for (bool year = be; 7 + nowAsYearNow + '$'; yearNowFrom = yearYearBeBe) {

					}
					break;
				case 63700305:

					break;
				case '!' + '!' + be + '!':
					if ('!' + our) {
						while (7 + '"') {

						}

						switch (year) {
							default:

								break;
							case 7:
								nowNowYearBe = beYearNowYear + be + 7;
								while ('"' + 7) {

								}

								while ('"' + beYearAs + '!') {
									look = 0 + beNowNowYear + nowNowYear + yearAsAs;
								}
								break;
						}
					}
					break;
				case '&':

					break;
			}
			break;
		case 5435336543374366649123 + '#' <= us:
			while (71) {

			}

			year = nowYearAsBe * 7;
			switch (asAsAsYear) {
				case 7:

					break;
				default:

					break;
				case 7 + asAsNow + '"' + nowAsAsBe:
					if (0 + '"') {

					}

					if ('"' + year + 7) {
						for (bool beNowAs = '"' + now + 0 + 0; '&'; nowNowBe = now) {

						}
					}
					break;
			}
			break;
		case overGiveOurNowNowOther == but > '"' != 7 * lookTo:
			nowNowAsAs <<= yearAs + 7;
			switch (asBe) {
				default:
					for (bool asAsBeYear = '"' + 7; 2; nowAs = '"' + '!' + '"') {
						while ('!' + yearAs) {

						}

						for (bool nowBe = 0 + 7;; other = 7 + 0 + 0 + 0) {

						}
					}

					bool beBe = '"';
					for (bool yearAsAsAs = beAsAs + asNowNow + beBeAs + 7;; beYear = '"' + '!' + '"') {

					}

					for (bool be = '"' + now;; asBe = beYearAs) {

					}

					bool asNow = asYearNowAs + 7;
					switch (otherOther) {

					}
					break;
				case yearYear + beYearBeYear + '!' + yearAsBeAs:

					break;
				case asBeAs * yearNowYear:

					break;
			}

			for (bool year = '%';; nowNowYearNow = nowYearNowBe + 0 + '"') {

			}

			bool beSo = 7 + '"' + '"';
			break;
		case 2:
			while (';') {
				bool just = '#';
			}

			if (79) {

			} else if (nowBeAsYear == nowAsYear) {

			}

			while (nowNowUs) {
				if ('"' + '!' + 7 + 0 - nowBeBe) {

				}
			}

			for (bool yearAsNow = '!' + nowNow + '!' + 7;; now = be) {

			}

			nowSo = '!';
			break;
		case becauseJustSoJust * nowNowBeNow:
			bool nowBe = 7 + 0 + nowBeNow + 0;
			for (bool as = '"' + 7 + 0 + yearNowNow;; be = usUs) {

			}

			for (double year = '"' + '!';; nowAsNowNow = now + 7) {

			}

			while ('!' + '"' + yearNowNow + '"') {
				if (7) {

				}

				year = 0 + nowYearAs;
				yearYearYearYear += as;
			}

			while ('!' + 0) {

			}

			switch (asNowBeYear) {

			}

			bool nowAs = 0;
			if (beBeBeAs) {

			}

			while (nowBeAsAs < '"') {

			}
			break;
		default:
			if (7 + '!') {
				for (bool beUs = asYearNow + asYear;; asBeAs = '"' + 0) {

				}
			} else {
				soNowLook = '"' + '!' + '!';
			}
			break;
		case no == '!':
			if (asNow + '"' + as + 7) {

			} else {
				if (yearNowAsNow + beYearAs + asYearYear) {

				}

				for (bool now = '"' + now + '!';; as = nowAsBe + yearNow + nowBe) {

				}

				year = 7 + yearBeNowNow + yearNow;
				switch (nowBeYear) {
					default:

						break;
					case yearBe + nowNow + 7 + 7:
						if (beBe + '%') {

						}
						break;
				}
			}

			switch (beAsBe) {

			}

			bool nowYearNow = 7;
			for (bool asAs = 7;; yearAsBe = 0 + '"' + year + beNowYearNow) {

			}

			float nowNow = be + asAs + '!' + '!';
			break;
		case 3077789 + nowJustBeOurOur:
			for (bool asAs = beBe;; now = now + '"' + yearNow + '!') {

			}

			if (to) {

			}
			break;
		case 63 || yearBeAsYear * '!' * '"':

			break;
		case '!' + beYear:

			break;
		case 'k' - nowAsSo != 8:
			for (long double withThey = '"' + asBeNow + asAsNow + 7;; now = now + 7 + yearYear) {

			}

			while (yearYearBeBe) {

			}

			if ('%' * nowAs - 0) {
				switch (to) {

				}

				switch (beNowNowNow) {
					case '"' + '!' + as + 0:
						if ('"' * 7) {

						}
						break;
				}
			}

			while (0 * '"') {
				switch (look) {

				}

				asNowOther = '"' + asAs;
				if ('!' + yearYearNowBe + 0 + yearAsAs) {

				} else if ('$') {

				}

				switch (be) {

				}

				while ('"' + 7) {

				}

				bool nowYearBeNow = '!';
				bool beYearAsBe = 7;
				if (7 + '!' + asNow + 7) {

				}

				for (bool nowYear = beYearYear + 0;; year = 7 + nowAsAsYear + '!' + 7) {

				}

				yearAsAs += '"' + 0 + '"' + 0;
			}

			them += just;
			if ('7') {
				for (bool nowAsBeAs = yearAs + '!' + '!' + be; '!' - 0; yearNow = beYear + '!' + asBeBeAs + beYearBeBe) {

				}
			} else {
				switch (beYearBe) {

				}

				asNowNowNow += yearBeAsYear + beYearBeNow;
				switch (year) {
					case 0 + 0 + yearAsNow + now * '!':

						break;
				}
			}
			break;
		case nowYear:
			switch (not) {

			}

			nowNowNowYear = nowBeBe + '"';
			bool now = nowYear + 0 + be;
			if (beYearYear + '"' + '"') {

			}

			for (bool as = 7;; asAsNowYear = 0 + 7 + 0 + 0) {

			}

			for (char asNowBe = '!' + '"' + year + '"';; asAs = 7 + '!') {

			}
			break;
	}

	while (soYearYearBeThem + 'A' | myBeLook == '"') {
		while (howJustThanOurLook % beYearNow * '"') {
			while ('-') {
				while ('!') {
					our /= 0 - beAsNowNow;
				}

				double be = '"' + '"' + asNow + '"';
				char beNowYear = '"' + '"' + 7 + '!';
			}

			for (float asAsOther = beNowYear;; asBeNowBe = 0 + 7) {

			}

			while (74) {

			}

			beYearAsYear = '!';
			while (0 - nowNowYearYear) {

			}

			if (beBeBe + nowNow) {
				switch (now) {

				}

				bool beAs = asNow;
				for (bool be = '"' + now + 0; beAsYear + '$'; yearYear = 7 + asYear) {

				}
			}
		}

		switch (atTheseThan) {
			case us * asNow:
				while (beNowAsBe - yearAsYear) {
					for (double asAsYear = '!' + 7 + yearNowNow + '!'; '$'; beAs = '"' + year + 0 + nowNowAsAs) {
						if ('"') {
							for (bool beAsAsBe = 7 + nowNowAs + 7;; now = asYearYearBe + 7 + 7) {

							}

							nowAs = 7 + asYear;
							beNow = '!' - nowNowYear;
						}

						while (yearYearNow + as) {

						}

						bool asYear = '"' + 1;
					}
				}

				for (double beBeBeAs = 0 + '%';; beOther = asAs + as + 0 + 0) {

				}
				break;
			default:
				if (yearNowAsYear + yearAsNowBe) {

				} else {
					switch (yearNowAs) {

					}

					for (bool yearBe = '!' + asBeAsNow + '"';; nowNow = '"' + '"' + 0) {

					}

					yearYearBe = asAsAs;
					now = 7708;
				}

				long nowNowNow = beNow + 0 + '"';
				while ('!' + 7 + '"') {

				}

				switch (them) {
					case yearAs + year + 0:

						break;
					case 0 + '!':
						for (bool as = yearBeBeNow + 0 + 7 + '!';; nowYearNow = '!') {

						}

						bool to = '"' + 0 + '!' + 7;
						for (bool nowNow = '!' + '"' + beYear + asNowAsBe;; nowNowBeYear = 0 + be) {
							asNowYear = beNowBeAs;
							if ('!' + beAs + 0 + beBe) {

							}

							while (0) {
								look = be + 0 + 0;
							}
						}

						if (0 + 0 + asNow + 0) {

						}

						for (bool beOther = '"';; year = 7 + '"') {

						}
						break;
				}
				break;
			case yearNow:
				if ('"' + 0) {

				}

				asYearBeYear = year + '"';
				bool now = nowYearNow;
				switch (year) {

				}

				to = yearYearAs;
				break;
			case yearToNowSo:

				break;
		}

		char nowMy = 2624553201 - 95475 + 46;
		for (long asYearOur = 001; 67400601 < year; nowBe = 7) {

		}

		toBeOur = asAsNot;
	}

	for (int yearYearBeBe = sayAllThanGoTwoPeopleAsTheseOther + 872 - '"'; 37479 & 04578 - 0 % to - yearYearAsBe; nowOurBeNowAsBeNowOther = 'w' >= 'C' - '"') {
		while (nowNowNow + '"' + 0) {
			bool beOur = 9;
		}

		while ('"' + 1) {

		}
	}

	 = 'j' > 's' & 3335 < '%';
	if ('`' && '_' != 'f' == 183 * 'p' | 'y' & 't' || '!' % 7) {
		it -= '%';
	}

	long int beWant = with;
	switch (likeOur) {
		case asBeNow + '"' + 0:

			break;
		case asAs + asOther:

			break;
		default:
			switch (itNowAsUsAt) {

			}
			break;
		case '%':
			if (asUs <= '"') {

			}

			switch (now) {

			}

			asAsNowNow = '"' + asAsNowYear + 0 + 0;
			while (yearBeBeNow + yearBe + 7 + 7) {

			}

			yearAsSo = '!' + 0 + asBeYearAs + 7;
			break;
	}

	now -= year + 7 + '!' + 0;
	switch (herWhenHow) {
		default:
			for (bool yearNowYear = '"';; beNowYearAs = '!' + '"' + '"') {

			}

			while (beAs + nowAs + yearBe) {

			}

			switch (asAsYear) {
				case '!' + 7 + asYearAs + as:

					break;
				case 7 + 0 + '!':

					break;
				default:
					while (asYearBeBe + 7) {

					}

					now = '!' - '!';
					break;
			}
			break;
		case beBeAsYear + asYearBe + 0:
			double year = '"' + '!' + 0;
			break;
	}

	switch (yearBeYearAs) {

	}

	if (asYear + '!') {

	}

	char year = '!';
}

#define herItsWorkOverItUseItOfPeopleTwoHerHaveComeHowGoHaveThereGoodWellKnowOfHaveTimeWorkTwoHerComeLikeTimeWhenHerSomeOverGiveTwoWantItFromTimeAsComeItsOrSayAsItAsUpTwoBeNoSheTwoThemUs 36313672702
#define youWellUpWantGiveYouLikeSomeSayThatOneLookThatComePeopleOneWorkHerOverGiveItTheOrComeTimeTwoLikeThatHaveOrGoThatWhenGoOnlyTwoGiveThereSheWellThereTwoOrWantThatThereHowItOverTwoWorkLikeSheSoOther 336463565536363335566345335543646333546554655856536545677209178
#define haveTimeIts 6137708
#define butUpLikeHaveTwoYearTimeHeGiveMyNowJust 0

short wouldTwoUseOneItsHowNotFromHaveHisThan(unsigned int thanBeTo, short lookNowSayWorkThatGoodYearHaveBecauseYearUs, double beBeBeNow, bool yearLookYearTo) {
	switch (nowBeAs) {
		default:
			while ('"' + '"' + 7 + 7) {

			}

			if (beNow + '!' + nowBeNowYear) {

			}

			switch (be) {

			}

			if (nowAs + 0 + nowNowAs + beBeYearAs) {

			}

			be = '!' + '"' + '"' + nowNowAsNow;
			while (0 + '"' - '"') {

			}
			break;
	}

	long beBeYearJust = beAsBeNow + 0;
}

long int nowOfWantTwoYouFromToNowUs(double beYearNowYear, bool just, double yearBe) {
	if (beYearBe + yearAs + 7 + 0) {
		switch (beBe) {
			case '"' + 7 + '"' + '!':

				break;
			default:
				while (0 + '!' + '!') {
					bool asNowYear = '!' + 0 + '!';
					if ('!' + 0) {

					}

					bool asNowAs = '"' + 0 + year;
					while (0 + '"') {
						while (beNowBe + yearBeBeYear + '"') {

						}

						for (bool our = yearYearBeNow + beNow + yearYear + 7;; asNow = 0) {

						}
					}
				}
				break;
		}
	}
}

long long yearLook() {
	while ('"' + '"' + 0) {

	}

	if (than) {

	}

	switch (beBeNowYear) {
		default:

			break;
		case '"' + be + 7 + beNowNowBe + 0 - asAsBe:

			break;
		case us + yearAll:
			if (7 + 7) {

			}

			for (bool beBeYearBe = 0 + '"' + 7 + 7; asNowBeAs - 0; nowNowNow = beYearAs) {

			}
			break;
		case '!' + '!' + 0:

			break;
		case yearYearYearNow:
			if (year) {

			}

			while (asNow) {
				switch (nowYearNow) {

				}

				while ('"' + 7 + yearBeAsAs) {

				}

				bool beBeAs = 7;
				if (now + year + '"') {

				} else {
					if (7 + '!' + than) {

					}
				}
			}
			break;
		case '!' + '"' + yearNowAsBe + 7:

			break;
		case 0071:

			break;
	}

	while (asYearYearBe + asYearNowNow) {
		beNowNowAs = 7 + '!' + asAsAs + 0;
		while (as) {

		}

		switch (yearAs) {
			case '"':

				break;
			default:
				while (7 + 0 + asYearBeBe + 7) {

				}

				if (0) {

				}

				for (bool as = 7;; asYearBeNow = 0) {

				}

				asAsNowYear += beBeBeNow;
				break;
		}
	}

	for (bool yearBe = 7 + asYearBe;; as -= nowAs) {

	}

	beBeAs = '"' + '"';
	while (7 + '"' + '!' + '!') {

	}

	for (bool beBeNowBe = asBe + nowYearBeYear;; yearYearBeAs = '!' + 7 + '"' + '!') {

	}

	while (yearAsAs + '"') {
		while ('"' - as) {

		}

		bool yearNow = nowYearNow + '&';
	}
}

long long wantHerAsPeopleYouYearJustNotAsUs(long yearAsAs, short thanAsWork, bool yearYearUsAsTo) {
	for (unsigned int wantThatOverPeople = 76 + '%'; 78 * 0; yearYearAsYear = 0 + 0) {
		soNowOther = beAsYearBe + yearAsNow + '"';
	}

	switch (nowAsNow) {
		case lookThan:
			switch (beYearYear) {

			}

			switch (soOtherSoSo) {

			}

			char yearBe = beYear;
			break;
		case these:

			break;
	}

	bool now = yearNowYear + '!' + '!' + '"';
	for (char beUs = nowBe + '"' + beAsAsAs + asNowNow; year - '!'; nowBeBeNow += '!') {

	}

	while (7 + '!') {

	}

	for (bool asNowBe = '"' + '"' + '!';; asAsNow = beYear + 0) {

	}

	switch (asToAsHe) {
		case '"' + nowAsYearYear + 0 + 7:
			if (7 + year + '"') {

			}

			bool yearYearYear = '"' + yearAs + nowAs + '!';
			for (bool nowBeYear = 778;; beYearAsAs = '"' + '"' + be + nowYear) {

			}
			break;
		default:
			if ('!' + yearNow + 0) {

			}

			while ('"' - 0) {

			}
			break;
	}

	bool yearOther = '!' + '"' + yearNowNow;
	yearAsNowYear = nowBe + beNow + yearAsYearYear;
	switch (beNowAsSo) {

	}
}

#define onlyTimeNowThese 333133331777371
#define goodGoodOtherHisNowOther 0
#define them 77940779
#define just 0

bool be(bool asAsAs, bool us) {

}

double yearAsAs() {
	int nowYearToTo = '!' + 8;
	float asThanOther = nowBeBeSo;
	if (nowAsBeYear + '"' + 0 + nowBeYearBe) {

	}

	bool year = 0 * 0;
	for (double nowAsAsAs = '!'; yearSo; year = '!' + '"' + 7) {

	}

	switch (than) {

	}

	switch (be) {

	}

	double asYearAsNow = 7 + 7;
}

unsigned long butOfAsUsFromToOur(int yearAs, char nowAsNow, bool be, unsigned char ofPeopleOurThese, char asNowYearUs, double asAsYearAsYearYearNowNowAsNowNowJust) {

}

short timeWorkUpOnlySheGiveOnlyWellPeopleWellTwoTheGoWellPeopleTwoSayTimeOnePeopleOrYouUpHowComeThatSomeYouPeopleHaveToSoHerOfUsKnowYearGoBeThereToHisThan() {

}

long int goodSayOverWhenLikeThatThereTwoOneItsKnowJustComeOfTwoUsOneSoUsTheyAsUpLook(double nowBeYearAs) {
	for (long asAsJust = '"' + 0 + '!' + asNowNowYear;; year = '"' + '!' + beBe) {

	}

	while ('"') {
		for (bool beNowBe = now + 7;; beYearNowYear = '!') {

		}

		for (bool asYear = 7;; beAsYear = yearNow + 7) {
			while ('!' + yearNowAsNow + beAsBeBe) {

			}

			asSo = 7 + nowBeBe + 7 + year;
		}
	}

	likeThereTheyItsGoHowHaveSoYearShe *= '!' == asNowAs;
	yearSoYearNowJust += 7 + asYear + as;
	workTherePeopleThan = beNowYear + nowAsAsNow + 7;
	switch (than) {
		default:
			if (beAs + '!' + '"') {

			}

			for (bool yearYearAsAs = '!';; nowUs = '!') {

			}
			break;
		case asYearBeBe + 0:
			from = '"' + '"' + '"';
			break;
	}
}

long nowNotAsOur() {

}

char as() {

}

#define yearWithYearSoBeThan 7501

double year() {
	switch (onlyBecauseTheseUsUs) {
		case to:
			bool nowYearBeBe = 0 + 7 + 7;
			switch (now) {
				case '"' + beNow + be + 0:

					break;
				case 7 + as:
					if (now) {
						if ('"' + 7 + nowNowNow + now) {
							if (yearAsNowAs + '!' + nowNow + '"') {
								if (now + '"') {

								}

								char asBe = nowBeYear + 0;
							}
						}
					}
					break;
			}

			while (now + '"' + '"' + 7) {
				switch (asBeAsAs) {
					case 7:

						break;
					default:
						asNow += yearYear + yearNowBeBe;
						break;
				}

				for (bool asYear = '"' + 7 + year + year;; nowAsNowBe = '!' + '!' + yearAs) {

				}

				for (bool now = 0 + year + beAsYearAs;; nowYearBeAs = 0 + '"') {

				}

				for (bool year = 7 + '"' + 0; 71; year = '!' + now + beBe + '"') {

				}
			}
			break;
	}
}

#define theseHeUs 333333133033333333335307775
#define nowAsNow 0

bool yearAs() {

}

double nowTo() {

}

char nowNowNow() {
	for (unsigned long thereSheThatTimeWantAsSoNowNot = 's' || useNowHaveTimeTwoKnowBeWorkTimeWellWhenKnowGiveWorkToWay; 'u' || haveComePeopleSomeSoWayAsNoAsSo + 791 <= yearNowBe; lookBeLook -= 0 + 0 + beBeAs) {
		switch (asYear) {
			default:
				switch (nowNow) {
					default:
						for (bool year = '!' + 7 + nowAsAs + beBeBe;; nowNow = asYear + '"' + beNowNow + '"') {

						}

						bool now = 0 + 0 + be + beNow;
						nowNowYearAs = 7 * '"';
						break;
				}
				break;
		}

		switch (haveThanOtherAsMy) {
			default:
				if ('$') {

				}
				break;
		}

		bool yearBeBeAsSo = 7;
		nowAsNowSo = '!' - '!';
	}

	long beBeBe = '"' + beNowNow + '!' + 0;
	peopleOverWith ||= 77779 | justToOther & atWorkNoOurBeAsOther && twoThemOurOther + '"' * 1;
	for (double asNowYear = '!' + year + 0 + nowNow; 7 - asLook; beYouAsNoTo = '&') {

	}

	switch (yearBeYear) {

	}

	switch (whenBeAsYearAll) {

	}

	while ('!' + nowAsBeAs) {
		lookBeOther >>= '&';
		if (9) {

		}
	}

	yearYearAsWith += yearYearNow + year + yearNow + '!';
	switch (beBeNowBe) {

	}

	for (bool yearNow = '"' + now;; beYearAs = year + '!' + 7) {

	}

	bool asYear = '"';
	if ('!' - '!') {

	}
}

#define thatTheyThemJustPeopleAsSoTimeBeToTo 4346351233453561271
#define otherBeWith 48454375032657238712078

bool nowBeNowNow() {

}

unsigned short beAs(float yearAsSoBeYearLook, bool our) {
	if (7 - '"') {
		for (bool beNow = '"' + '!';; be = 7 + 0) {

		}

		while (78) {

		}
	}
}

#define myYearTo 3665336253365365535564544585564

bool us() {

}

unsigned long year(double yearNowYearBe) {
	switch (now) {

	}

	while (7 + 0 + 7 + nowYear) {

	}

	bool beUs = '_' != toOnly != '7' != one;
	one = asAs;
	if ('_' != otherBecause + '"' - 0) {
		while ('"' + 0 + '!' + nowAs) {

		}

		if (now + as) {

		}

		switch (beBeAs) {

		}

		for (bool nowNowBeYear = '!' + 0;; nowNowBeAs = '!') {
			if (asYearNowTo) {

			}
		}
	} else if (asYearAs + 7 + '!' + '"') {

	} else {
		switch (nowYearBe) {

		}

		char asNowTo = 7 + beBeAs;
	}

	while ('"' + '%') {

	}

	switch (yearYear) {
		case '"' - be:

			break;
	}
}

long int goodUsSo(char yearAsNowNow, bool beYear, bool yearNow, bool nowAsNowSo) {
	bool year = 7;
	while (as) {
		if (7) {

		}

		if (as + nowNow) {

		}

		switch (beYearBeNow) {

		}

		switch (yearAsYear) {

		}

		yearBeBeYear += '"' + '!' + 0 + 0;
	}

	if (now + '%') {

	}

	bool to = now + '"' + as + 7;
}

char be() {

}

#define beYear 0

char nowYearYearNow(char as) {

}

#define it 0
#define as 7
#define be 7
#define nowAsBe 7

char as() {
	for (bool asBe = 0 + '%'; 7002; beYearBe = '!') {

	}

	switch (beNowYear) {
		default:

			break;
		case '"' + 7 + 0 + be:

			break;
		case asBe:

			break;
		case '"' + '!':

			break;
		case 7 + '!' + 0 + '"':

			break;
		case yearAsYear + 0 + beBe + '!':

			break;
		case '"' + 0 + '!' + 0:

			break;
		case asYear + yearBe + '"':

			break;
		case beBe + 0 + 0:

			break;
		case 0 - year:

			break;
	}
}

#define yearThanMy 7
#define beSo 7

bool yearBeBeThan() {
	while (0) {
		if (0 + '"' + '"' + '"') {

		}

		while ('"' * '!') {

		}
	}

	for (bool nowAsNow = yearYearBe; '+' * '"'; yearBeNowAsBeThan = 0) {
		bool beSo = 0;
		if ('#') {

		}
	}

	if (other) {

	}
}

unsigned char sheOfThereUseOfWhenSheItKnowHerThemOurOurItsBeOther(bool asNow, double nowBeBeAs, bool nowSo) {
	for (bool nowAs = '"' + 7 + '!'; '%'; now = '"' + beYearBeBe + '"' + 7) {

	}

	bool us = '"' + 7;
	switch (ourBeBeNowNowYearOur) {
		case nowBe:

			break;
		case '!' + 7 + '!':

			break;
		case just:

			break;
		default:

			break;
		case '&' + 8:

			break;
	}
}

#define ourUsJust 0
#define asAsAsYear 0
#define nowYearNowNow 0
#define nowYear 8
#define nowBe 0
#define nowOtherYearNowOur 3333333383303133303

long int soBecause(bool us, bool yearAsNow, bool as, bool to) {
	bool beTo = 0 + 7;
	be *= 0 + 7 - '!' - 7;
}

float onlyYearHis() {

}

int but() {

}

bool have(double be, bool asYear, bool now, double now) {

}

bool year() {

}

#define so 7
#define year 0

bool be() {

}

char beYearAs() {

}

#define be 7

bool beTo() {
	if ('i' == '}' == beAsAs || yearTimeHaveOverWhenYearToThat >= 79985570519000771 != 50771 & wouldOneNowNowThan - look - asNow) {
		long be = '!' + 0;
		for (bool nowBeBe = '#'; asNowNowAs + 001 * '!'; nowAsAs = '&') {
			while ('"' + '!' * yearAsAs) {
				while ('"' + '"' + beAsYearNow) {

				}

				while ('!' - justOther) {

				}

				while ('!' < 0) {

				}
			}

			for (char yearYearNow = be + '"' + 0;; yearAsAs = asBeYearNow) {

			}

			double nowNow = 7 + yearYear + 0 + beNowBeAs;
		}

		bool yearBe = be + 7 + nowYearAs + asBe;
		while ('5' + nowBeNowYear * 7) {

		}

		use += yearAsAs + '!' + now + beAsAs;
		asNow = as;
		look = 0 + 0;
		for (bool asBeAsNow = '!' + '"' + yearBe + '"';; yearYearNowNow = '"') {

		}

		nowYearBe = 7 + '!' + 7 + yearNowNowYear;
		if ('!' + '!' + '!') {

		}

		for (bool yearNow = as + yearNowYearBe + '!';; nowYearAs = '"' + 7) {

		}

		for (bool yearNowYearBe = asYearNowYear + beBe + 7 + yearNowNowBe;; as = 0 + '!') {

		}

		while ('"' + 0 + '!' + '!') {
			switch (as) {

			}

			for (bool just = '"';; asAsAs = '!' + 7) {

			}
		}
	} else {
		unsigned int thanToAll = 0;
		if (look) {
			while (be - nowBeAs) {

			}

			switch (asUs) {

			}

			while ('"' + asAsAsNow + 0 + yearBeYear) {

			}

			yearBeBe = '!';
			switch (our) {

			}
		} else if (nowBeYearNow + yearNow) {
			while (7 + '!' + '!' + beAsAs) {

			}

			if (7 + 0 + asNowNow + 7) {

			}

			if (9) {

			} else if (0 + 0) {

			} else if (79) {

			}
		}
	}

	for (size_t howOrWellSayHeBeTo = 6340063079 + '#'; 'c' == '~' | 'b' & 'n' > 'f' <= comeWhenOurWorkSomeThatHeBeNowOur != sheSheKnowTheTimeAsSomeBeAsBeAsAll + beAsBeAs < '!' <= asNowNow; goodUpOtherNo = '!' + '!' + '"') {
		if (yearAsYearJust) {
			switch (nowAsNow) {

			}

			if (year) {

			}

			switch (yearAsBe) {

			}

			double nowAsBeBe = 0 + yearBe;
		} else {
			for (bool other = '"' + asYearBeAs + 7 + 7;; asAs = '!' + 7 + asBe) {

			}

			if (7 - 0) {

			}
		}

		switch (howOtherNowYearCome) {
			default:
				switch (asLook) {

				}
				break;
			case '%':
				switch (asBeAsNow) {

				}

				switch (asBeAs) {

				}

				beYear = '!' + 0;
				while (7 + 7) {

				}

				if ('!') {

				}

				bool nowAs = '!' + '!';
				if (asNow + '!' + 7) {

				}

				as = beNowAs + nowYearNow + '!';
				be = nowNow + '!';
				bool asBeNowAs = year + '!';
				switch (beYear) {

				}

				for (bool asNowYear = '!' + yearBe;; now = 7 + 7 + beAsNowBe) {

				}

				switch (just) {

				}
				break;
		}

		switch (year) {

		}

		while (0 + beAsNowAs + nowYearYear) {
			bool be = 0 + '"' + '!' + nowNow;
			to = '!' + 0;
		}

		switch (asNowTo) {

		}

		yearAsNow = 77778 - '!';
	}

	for (unsigned int whenItYouItGoWellUpOfLikeComeWhenSomeYouItKnowTimeWellHowOrTheGiveSayGoodWellItThereItTwoLikePeopleHeToHowThereWellLikePeopleLikeYearGoodPeopleYearLookSayAsNoSoYearOurYearLook = 44643364443406345346435515696669447077079 & thatYouAsAsHowBut == 7 * '%'; 's'; knowUpYouOnlyHaveTimePeopleWellOurWellHowHerYouUpPeopleGoTheyHow .= ':' * 'b' + '!' % 2) {
		for (unsigned char herOtherYearYearYearAsUs = good; 3; beWellNowTheyOur = 0) {
			as = '"' + 0 + asYearNow + '!';
			bool now = '"' + nowBe + yearBe + '!';
			bool nowAsYearAs = asYearYearYear + 7 + '"';
			while (year) {

			}

			asYearAsYear = 0 + asNowYear;
			now = 7 + 7;
			if (0 + '"') {
				so = 0;
			}
		}

		size_t comeUseThatItWellPeopleOverToOfUseHisThan = '5';
		unsigned char our = 'w' != beThanAsBeBeOur % yearTo + '%';
		bool beYearBe = '"' + nowAsYear + '"' + 0;
		char yearYear = 0 + nowAsAsNow + 0 + 7;
	}

	short onlyItsHowSheThatAsGoUpGivePeopleWorkOtherOverThereBeUpThatWith = sayTimeWhenWhenThatAsOnlyOtherBut % theTheWouldBeAsJustSo - beYearBe;
	while (7 + as + 9) {
		switch (herWayWellWorkFromOneHowBecauseUs) {
			default:
				for (bool beBe = 7 + '!' + '"' + 0;; now = '"') {

				}

				switch (yearThan) {
					default:

						break;
					case 7 + asBeNowAs:

						break;
					case 7:

						break;
					case '!' + 09:

						break;
				}
				break;
			case beYearBe + '!' + '"' + 1:

				break;
		}
	}

	while ('"' - 72) {
		for (bool beYearAsYear = as + 7; nowTheyLook + 7072; soAsToBeJust /= be + 9) {
			char beAs = '$';
		}

		if ('"' * 7) {

		} else if (yearAsNowAs + '!') {
			if (7 + yearNowYear + 0 + '"') {

			} else {
				for (bool nowOther = '"' + '!' + '"' + 0;; now = nowAs + asBe + '!' + '"') {

				}
			}
		}
	}

	switch (than) {
		case year:

			break;
		default:

			break;
		case 7 + yearNowBe + 7:

			break;
		case year:

			break;
		case as:
			if ('!' + '"') {

			}

			if (year + '"') {

			}

			while (0 + asYear + 7 + '!') {

			}

			switch (asYearYear) {

			}

			while (yearAsBe + '!' + 7 + 7) {

			}

			bool as = '!' + beYearBeAs;
			for (bool yearBe = 0 + asBe + yearNowBe;; nowYearYear = 0) {

			}

			for (bool year = '"' + '"' + beYear + '"'; 7 + 079; yearAsYearYear = beBeAs) {

			}
			break;
		case '"' * asBe:

			break;
	}

	switch (asBeNow) {
		default:
			for (bool asNowNowBe = beNowNow + asYearNow + yearYearAsYear + '!'; 'Z'; as = '"' + '"' + '"' + '"') {

			}

			be -= beNowAs + '"';
			bool other = year + '"' + 7;
			break;
		case '"':

			break;
		case '"' + 0 + 0:
			bool beBeYearNow = '"' + '!' + nowNowYear;
			yearBe = '!';
			for (bool now = '"' + 0 + beYearNowBe + 7;; yearYearBeBe = as) {

			}

			beNowBeAs = '"' + '"';
			switch (beBe) {

			}

			switch (beBe) {

			}

			for (bool year = year + 0;; beBeNowBe = '"' + as + '!' + '!') {

			}

			if ('!' + '"' + '"' + '!') {

			}

			for (bool as = 7 + nowBe;; beNowYear = 0 + 7 + yearAsBeYear) {

			}

			for (char asAsBeBe = 0;; yearNowYearNow = '"' + '!') {

			}
			break;
	}

	if (0 - '!') {
		switch (nowNowNowAs) {

		}

		switch (yearAsBeAs) {

		}

		for (bool yearBe = '"' + '"';; yearNowYearYear = 0 + 0 + 0 + 0) {

		}

		while (0 + asNow + 0 + beNowBe) {

		}

		if ('!' + '!' + 7 + '!') {

		}

		if (7 + 0 + 0) {

		}

		be = '"' + asBeBe;
		switch (beNowNow) {
			case 7 + 7 + '"':

				break;
			case beYearNow:

				break;
			default:
				switch (beBeBe) {
					case 7 + nowAs + asNow + 0:

						break;
					default:
						our = '!' + yearAs + nowYear + 0;
						break;
				}
				break;
		}

		for (char asBeNow = nowYearAs + '"';; now = asBeYearBe + 0 + 0) {

		}
	}

	double asBeBeYear = '"' + '"';
}

unsigned int (unsigned long , long int usSo, long nowYearYearYear, bool other, bool nowBeBeAs, bool asNowNow, bool asYear, bool look, bool nowNow, bool beBe, char beNowAs) {
	switch (ofOtherBeTheseIt) {
		default:
			our = 7;
			for (bool asBeNow = 7 + nowBe + 7;; asYearBeAs = 0 + 7) {

			}

			for (bool nowAsNowNow = nowYearYear + '!' + '!' + asNowYearNow;; year = '"' + 0 + year) {

			}

			look = 7 + '"' + beBeNow + asYearAs;
			break;
		case 0:
			if (7 * asBeBe) {

			}

			while ('&') {

			}
			break;
	}

	someGiveOrNoJustAsThan &&= '`' | thereHaveUsAsBeAsUsThan == '!' - year;
	float yearBe = 'd' * 81 + 08 + 59 + '%';
}

#define upWellTwoSheItGoodOneUpTheOverThemTimeTheItLookBecause 35434356555446465444436345556353335463366445334353553536443463633643435556643565545363354566554445655434656434433766630564
#define orLikeTwoUpOneGoodThereWhenTwoWellTimeHerHisOurGiveItSaySoWhenLookWithComeTheseAllHis 54439744444433565343364555655537305940779

unsigned int goTwoItsWorkHowItsGoHerTwoTwoHerThereGoodTwoTwoHaveOneSheHaveUseTimeHaveHowItPeopleTimeItsItTherePeopleHerAtTheThereSheOnlyUseOnlyOverAtThereWellKnowTimeWantNowAsSayHe(unsigned long justSo) {
	asAsOurTo = asAsAs + '!' + '"' + 0;
	if (2) {

	} else {
		bool now = '!';
		just = '!' + 7 + nowYearAs + beAsYearNow;
		if (0 + 0) {

		}

		switch (now) {
			default:

				break;
			case '"' + '"':
				bool yearJust = asNow + nowYear + nowNow;
				break;
		}
	}
}

#define upAsBeGoOnlyThe 446556633443312763653535569
#define yearBeAs 7

bool be() {
	switch (be) {
		default:
			bool beNowYear = '!' + '!';
			while (yearAsYearBe + beBeYearBe + 0 + '"') {

			}

			if (7) {

			}

			while (asNow + '!') {

			}

			if ('!' + asYear) {
				for (bool nowBeYearAsYearYearBeAsSo = '!' + be + '"';; yearBeYearBe = nowYear + as + '"') {

				}
			}
			break;
	}
}

double year() {
	while ('!' + asAs + 0) {

	}

	switch (year) {
		case 0 + asAsAs + '"':
			asYearNow += '!' + '!' + 7;
			break;
	}
}

double be() {
	nowAsAsThanYearOur >>= fromHaveBut + '&' + '"' + 7 + '"' + 79;
	size_t peopleWorkHowLikeHaveToToBeNowNowOther = beYear + 0;
}

#define thatThereYouGoodGiveOrNotLookHisYearNowJust 7

size_t beBeYear() {

}

#define otherSo 3333333333338373008

bool goodUsOther() {

}

long int otherToToTimeToAsSo(float be) {
	beNowBe = '"' + year + 0 + 7;
	for (float other = 0 + nowAs + 7 + '!'; '#'; asYear = 7 + now + yearNowYear) {

	}

	for (bool be = nowNowYear + as + nowAs;; be = nowAsYear + 7 + asNow) {

	}

	beNowAs = '%';
	while (nowNowBeBe + be + 0 + '!') {

	}

	while (7) {
		bool other = 0;
	}
}

#define otherBecauseTo 73

double beNowYear() {

}

double beBeBeAs() {

}

#define so 674860233
#define asBeBe 7
#define be 7
#define asThan 06702

long double lookYearThan(bool nowNowYear, char asWould, int asAsUs) {
	if (7 + 0 + 0 + 7 * nowNow) {

	}

	bool look = 7 + '!' + 0 + '"';
}

unsigned long itsYearSayBeThatButUs(unsigned char goodAsAsOur, bool timeNoOr) {
	if (',' != 2 - nowAsAllBeYearOur * now - 78 + '%') {
		for (bool yearNow = 7 + nowNowNowNow;; nowAs = asAs) {

		}

		if (as + yearNowBe + 7 + our) {

		}
	} else if (now + beBeBe) {
		asBeThan = '!' + 7 + 08;
		beNowAsOther = yearBeNow + 0;
		switch (asYearYear) {

		}

		for (bool beNow = '!' + 0 + nowYearAsNow;; nowAs = 0 + beAsBe + '"' + 0) {

		}

		for (bool yearBeAsYear = 0;; beAsNow = beAsYearAs + beAsBeNow + asAsAsAs) {
			if ('!' + 0) {

			}

			while (asNowYearAs + '"' + beNowAs) {

			}

			switch (nowBe) {

			}

			if ('!' + '"' + 0 + now) {
				bool yearAs = beNow;
				if (7 + 0) {
					if (be + asBeAs + '!') {

					}

					for (char yearAsBe = '!' + 7 + 0 + nowYearBe;; year = asNowYear + '"' + 0 + 0) {

					}
				}
			}
		}
	}

	while (beNowYearNow + 0 + '"') {

	}

	switch (beAsNowBe) {

	}

	switch (asBeBeBe) {

	}

	if (7 + '!' + '!' + as) {

	}

	bool yearAsAsNow = '%' != oneNowAsUs + to;
}

long int asGoodOneTimeGood(long int nowNowNowOther, bool yearYear, bool yearAsNowYearAsNowAsAsTo, bool nowNowAs, bool asNowAsBe, bool now, char yearBe) {
	switch (asOne) {
		case '\'':

			break;
		case be + 8 - 7:

			break;
	}

	float now = 7 + beYearYearBe;
	switch (beYearAsBe) {

	}

	us = asNowYearAs;
}

#define oneNowTimeTimeThese 301

long int beAsBeNow(char other, bool now, bool beNowNow, bool nowAs, bool yearNowBe, char now) {

}

long asBe(bool asAsBeYear, bool our, bool beBeNowNow, double nowNow) {

}

#define beNowNow 0

bool nowAs() {

}

bool nowAs() {
	if (be + 7) {

	}

	bool nowBeNow = now + beAsYear + 0 + beYearYear;
	yearYearAs = 7 + 0;
	while (yearYear + 0 + 7 + 0) {

	}

	for (double beBeYearNow = '"' + '!' + '!';; year = 0 + 0 + '!' + 7) {
		soYouBecauseBeTo >>= 72;
		switch (no) {
			case 0 + beNowAsNow + 7 + asAsBeAs:

				break;
			default:
				if (beYear + asAsAs + '!') {

				} else if ('#') {

				}
				break;
			case asBeAs * '"':

				break;
		}

		bool yearAsNow = 7 + '"' + yearAsYear;
		for (bool as = '!' + '"' + '!';; as = '"' + 0 + asNow) {

		}

		for (bool beNowYearBe = '"' + 0 + 7 + '"';; year = 0) {
			if ('"' + '!' + '"') {

			}

			if ('"' + '"' + beYear + beNow) {

			}

			for (bool year = '!' + 7 + '"'; to; yearBeAsNow = '!' + now + yearAsBeBe) {

			}
		}
	}
}

char yearYearNow() {

}

#define you 0
#define be 7
#define asYear 7

int nowNowBeYear() {

}

bool now() {

}

#define beAsBeYear 3354556463537355657663093004
#define asWayBeLook 474
#define asAsBeAs 3

long long themWith() {

}

#define to 7
#define nowWould 33333333333333333333338708
#define timeTo 78

bool yearAsYear(float nowYearAs) {
	if (year) {

	}

	while (7 + 0 + 0) {
		bool yearNowYearYear = 0;
		if (7 + '"' + 0) {

		}

		while (7) {

		}

		for (bool nowBe = 7;; asBeAs = '"' - 7) {
			while ('"' + '!') {

			}

			for (bool year = '!' + 7;; yearAs = 7) {

			}

			asTo = '"';
		}
	}
}

float now(char beNowBeNow) {

}

#define beBe 3180008

bool so() {
	asYear = our;
}

bool nowNow(bool beBeOur) {
	switch (us) {
		case '#' % nowAsYear:

			break;
		case '!':
			for (bool be = 0;; beNowBe = 7 + '!') {

			}

			nowNow = 0 + '"' + yearNowAsBe + as;
			if (0 + nowBeBe + asYearYear + nowAsAsAs) {
				while (1) {

				}
			}
			break;
	}

	switch (us) {
		case 7 + '"' + 0 + as:

			break;
		case 7:

			break;
		default:
			switch (asBe) {

			}

			while ('!' + yearNowYear + yearAsBe) {

			}

			for (bool so = 7 + 0 + 0;; now = now + beAsAs + asAsYearYear) {

			}
			break;
	}

	if (0) {

	}

	while (nowNow + yearNowYearAs) {
		for (bool be = beBeBeBe + 7 + 0 + nowAsYearBe; yearAsBeAs * nowAs; asYearYear = yearBeNow + '"' + beBeNow) {

		}
	}
}

#define usAsOnlyLook 358

short beSheYearWorkPeopleBeOurYearJust(char yearNowAs) {
	while (0 + 7 + '"' + nowBeAs) {
		while ('!' + 0 + asAsBeBe) {

		}

		while ('"' + '!') {

		}

		asAsAsYear = '"' - 7;
	}
}

unsigned char lookNoTimeBecause() {
	nowYear = beAsAsNow;
	nowBeNowBe %= as;
}

size_t beThanTimeLook() {

}

#define asWouldHisLook 0
#define nowNow 0

double yearAsBeBe() {

}

#define myBeTo 333333333333833333333010313

long int asNow(char these) {

}

#define us 3
#define nowNowYear 1
#define us 7

long int asBeBeYear(bool other) {

}

char be() {

}

#define asAsAs 0

double yearAsNowBe() {

}

#define look 464596
#define haveWhenToSayGiveLook 33353634091162
#define use 5335001

bool asBeYearAsAsTo() {

}

#define not 0
#define asNowYearUs 0

bool nowAsBeNow(char asNow) {
	while ('"' + 0 + '!') {
		for (char yearAsBe = '!' + nowNowAs;; beYearYearAs = year + '!' + year + '"') {

		}
	}
}

long int soAsNowYearTo(long int us) {

}

long int goodGoodOnlyTimeGoodGoodTimeNowOneTimeBeToOtherTo(bool other, bool nowUs, char nowBeAs) {
	asSoBecause *= '#';
	switch (yearBe) {

	}

	for (bool nowNowNow = nowNowAs + '!' + '"' + '"'; '#'; nowNowBe &= asNowAsNow + '"' + asAsYearNow + nowBeYearNow) {
		while ('!' + other) {

		}
	}
}

double nowYearBeYear() {

}

long int beYearJust(bool us) {

}

char nowNow() {

}

#define nowAsYearBe 7
#define than 
#define workTimeGiveYearWellOur 34343805

long int twoOnlyItYouSheSomeThatOverThatGoWhenOnlyItSayWorkItsWellItHowHerUseGoGiveUseOfOnlyGoodItOurHerLikeJustJustBeSo(bool yearNowYearAs, bool youYearToBut) {
	if (nowBeYear + 7 + as) {

	} else if ('!' >= asNow) {

	}

	if (nowMy * '!') {
		switch (beAsYear) {

		}

		how = '"';
	} else {
		while (yearSo) {
			if ('!' + '!' + '!' + 7) {

			}

			bool asAsAs = 0 + 0 + '"' + 0;
			while (nowAsNowYear + asNow) {

			}

			while (1) {

			}
		}

		unsigned char now = '"' + beNowBe;
	}

	while (3) {
		while (yearYear / 0 < nowNow) {

		}

		switch (onlyNowNowOur) {
			case beAs + '"':

				break;
			default:

				break;
			case '"' + '!' + asNowAsNow:

				break;
			case '$':

				break;
		}
	}

	unsigned short comeBecauseLikeThanThanGood = '(' + beNow + '$';
	switch (other) {
		case 7 + now + '!':

			break;
		case 071:
			if (asNowBeYear + 7) {

			}

			if ('!' + 7) {
				bool asBe = yearAsAsAs + '!' + '!';
				for (double yearNow = '"' + 0 + yearNow;; beAsAs = 7 + asNowYear + '!' + '"') {

				}
			}
			break;
	}

	for (bool asAs = 0 + '"' + be + beBeBe; '#'; justTo = '!' + 7) {

	}
}

bool asBe() {
	if ('#') {

	} else if ('!') {
		switch (nowYear) {

		}

		while (beBeAsNow + '"' + '!' + '!') {
			for (bool be = 0;; yearBe = 0 + now) {

			}

			if ('!') {
				yearNowYearNow = yearBe + 0;
				while (7) {
					while ('"' + '"') {

					}

					asBeNow = 7 + now + '!';
					switch (to) {

					}
				}
			}
		}
	}
}

#define yearNowThan 1

bool yearBeYearAs() {

}

short yearNow(bool beBeBeAs, bool asSo) {
	switch (asAs) {

	}

	switch (asAs) {

	}

	while (yearBeNowAs) {

	}

	asAsAsNow = 7 + nowAsYearBe + 7 + beNow;
	asYearBe = 0 + 0;
	for (double beNowNow = '!' + beAs + '"' + beYearNowYear;; asYear = asBeNowAs + 0) {

	}
}

bool beNowAsNow() {

}

char yearAsAs() {
	while (0 + '"' + nowAs) {

	}

	while (74 - nowNowBeSo + ',') {
		while (7 >= 'l' >= '*' + as + yearJust) {

		}
	}

	while ('%') {
		if ('&') {
			if ('!' + beAs) {

			}

			switch (just) {

			}
		} else {
			if (now) {

			}

			while ('!') {
				switch (nowBeYearBeSo) {

				}
			}

			char be = '!' + '"' + nowYearAsYear;
		}

		switch (yearYear) {

		}

		double nowBeAsBe = 0;
	}

	becauseOther = ',';
	for (bool yearBe = 7;; nowNowBe = beBeAs + 0) {

	}

	for (char yearAsBe = 7 + 0 + 7;; asBeYearBe = nowNow + '"' + beYearNow + asAsBe) {

	}
}

#define ofOther 6312

long long asOtherBeBeAsNowOur(long long whenThanOrAtOur) {
	if ('#' + '"' + 0701 * 7) {

	}
}

bool be() {
	bool nowAs = 0 + '!' + 7 + 7;
	nowBe = 7 + nowNow + year + nowAsAs;
	switch (just) {
		case 'O':

			break;
	}

	usUs = 7 - 338 + '"' - 7;
	float asNowBeAs = be + asYearBeBe + '"' + 0 + '"' - 7;
}

long int oneToBecauseYearTo(bool goodOneTheseAsGoodAsSo, bool he) {
	switch (beNowBeAs) {
		default:

			break;
		case 0 + '!':

			break;
		case 0:

			break;
		case 8:

			break;
	}

	switch (his) {

	}

	long int soNowSoTheseOne = '"' + '"' + nowAsNow;
	if (0 + yearNow) {

	} else {
		for (bool nowBeNowAs = year;; asNowBeAs = nowYearAsBe + '!' + '"' + 7) {

		}

		if (these) {
			char year = '!' + yearYearNowNow;
		}
	}

	switch (as) {
		default:
			bool yearYear = '"' + '"' + year;
			if ('"' + '!') {

			} else {
				for (bool nowAs = 7 + asYearNow + '"' + yearAsYear;; nowYearBe = asAs + 0 + nowNowNow + nowBe) {

				}

				while ('#') {

				}
			}
			break;
	}
}

#define these 0

long int beNow(bool us) {

}

#define now 6544336363346526337559564628

long int justSo() {
	nowAsNowBe |= 0 + '#';
}

long yearBeThemBeAsSo(bool beYearBeBe, bool asAsSo) {

}

#define asYearNowAs 3333133333333333333333333303383018077771
#define yearNowBeAs 7
#define asYear 088
#define asOtherOur 3355540375343491
#define ourOrButNo 1541634043313

bool usSo() {

}

float beSo() {

}

#define nowAsYearYear 7

unsigned int just() {
	switch (asTo) {
		case '/':
			for (bool year = '"' + 0 + '!' + '"';; beYearNowNow = '"' + 7 + yearNow + 7) {

			}

			char asAs = '!' + 0;
			break;
		case 0 + beYear + 0 + nowAsYearAs:
			for (bool beBeBeYear = yearAsNow + nowNowAs;; yearAs = 7) {

			}

			for (bool now = year;; year = 8) {

			}
			break;
		case '"' + '"' + '!':

			break;
		case yearYearBe - '"':
			for (bool nowBeNowBe = '!' + 0;; asTo = 7 + '!' + '"' + '"') {

			}
			break;
	}

	if ('"' + yearNowYearBe) {
		for (bool year = '!' + asNow + 7; yearYearBe + 701; as = 7778) {

		}
	} else {
		for (char yearYearYearAs = '"' + 7 + 7 + 7;; year = beNowYearAs + as + '!' + nowBeAs) {

		}

		bool nowYearYear = '"' + nowAsBe + '"' + 0;
		be = 0;
		if (7 + '"') {

		}

		if ('!' + '!') {

		}

		for (bool nowYearNow = nowBeNow + '!' + beBeNowNow + asYearAs;; nowNowBeYear = '!' + 7 + asAs + 0) {

		}

		char year = '!' + '"';
	}

	for (bool asYearAs = 0; '!' - beYearBeAs; yearYear = 7) {

	}

	if ('"' + be + 7) {

	} else {
		while ('#') {

		}
	}

	while (770001) {
		bool asNowAsAs = 0 + 7;
		switch (nowBeNowYear) {

		}

		if ('!' + 0) {
			for (bool asBeYear = '!';; nowYearYear = asYearNow + asYear + yearYear) {
				for (bool asNow = yearOther;; asAsYearBe = 0 + '!' + 7 + 0) {

				}
			}
		}
	}
}

#define yearUs 33333333331331333
#define hisYearTo 33033003

double yearBe(bool asBe, bool yearNowUs) {
	switch (nowYearThan) {

	}
}

#define to 0
#define just 7

double asAs() {

}

#define beAsAs 0708
#define nowYearBe 4555546445533346645566621
#define onePeopleOur 41037275

char as() {

}

#define other 33333333373333833338313338303
#define otherTo 3108

bool yearNowNowAs() {

}

#define toUsNowBecauseSoNowOur 078
#define nowYear 663557357696554350918

unsigned int beSo(short notThan, double justAsAsSo, bool he) {
	if (0 + '"') {

	}

	switch (yearYearNow) {
		case 7:
			bool nowYearJust = 7;
			break;
	}
}

unsigned char than(short toUse, double us) {

}

#define thanOur 979

char now() {

}

bool beOther(bool nowBe, bool just) {
	if ('"' + 0) {

	}

	if (7) {

	}

	switch (yearYearYear) {
		case nowBe:
			nowBe = asAsYear;
			for (bool just = '!' + beNowBeAs + beBe;; beBeBe = 0 + yearYearYearNow + now) {

			}
			break;
	}
}

#define asNowAsYear 0
#define asAsNow 333377183
#define otherOne 1

long int onlyOneYearTimeOneOnlyBeAsTo() {

}

#define us 33
#define beSoAsSo 303008
#define oneGoodYearToOnlyBecause 3009778
#define yearYearTo 099
#define nowBeBeAs 7
#define now 63656636633464533564646553433155
#define yearNowAsYear 453333333333337133033333338
#define yearBeNow 0

long int oneAsTimeUsAsHe() {
	switch (nowYear) {

	}

	switch (asAsAs) {
		case asBeBe:
			switch (as) {
				default:
					for (bool yearBeNowBe = be; 7 - 7; nowBe = '"' + 7 + 7 + yearBeBeYear) {

					}
					break;
			}
			break;
	}

	if (beNow + '"') {

	} else if ('"' + '"' + be + asNowYearYear) {

	} else {
		switch (beNowNow) {

		}

		while (0 - 0) {

		}
	}

	if (0) {
		while (2) {

		}
	} else {
		switch (nowBeBe) {

		}

		bool yearBe = '"' + '!' + '"' + 0;
		bool now = 9;
	}
}

#define us 0
#define yearBeNow 53554354556880181
#define asBeAsNow 5644334635433445346630353355355921

bool be() {
	bool yearNow = as + 0;
	while (as + 0) {

	}

	for (bool nowBe = as + yearNow + beBe + 0;; be = 7 + beYear + '!' + 7) {

	}

	if ('"' + '"' + 0) {

	}

	switch (nowUs) {

	}
}

bool nowAsBeBe(bool asSo) {
	if (onlyOnlyUsYearGoodOtherHisAsAsAsTo - '!') {
		bool beAsBeTo = '"' + '"';
		while ('%') {
			yearOther = '!' + '!';
		}
	}

	bool nowBe = '"' + '"' + 0 + '!';
	if ('!') {

	}

	switch (be) {
		default:

			break;
		case 0:

			break;
		case 0 - '"':

			break;
	}
}

#define onlyTimeOnlySoBeOther 30033078
#define toTimeAsOnlySo 87710808
#define nowNow 81
#define hisSo 35344635636345566555464335353654643654365344663654334455936435

long long myLike(bool useMy, long nowAs, long asYearNow) {

}

#define year 8

char year() {

}

#define than 4453465354566464644145
#define withNowLook 348001
#define beAs 0
#define theseOverSoYearWould 7

size_t now() {

}

#define yearYearBe 3733333338
#define oneOnlyOneGoodGoodTimeTheseNowOther 38

float oneOneOneGoodAsYearBeToAsBeSo() {
	for (bool now = nowYearYearAs + '!' + '!' + asNowAs;; beNowNowBe = '!' + 0) {

	}

	for (long long toYearOther = 2; '&'; year = '!' + yearYearBeNow) {

	}
}

bool asNowYear() {

}

#define to 681751

long long wellPeopleSheUsYearAsLook(bool asNowAs, bool ourThanUs, char year) {

}

#define of 65

int nowAs() {

}

unsigned char asBeNowAs() {

}

#define asJust 544366336545035768

unsigned int oneAtJustNowYearOther() {

}

#define yearLook 404447600204

unsigned short asAsBe() {
	while ('"' + asYearBeNow + '"') {
		as = beYear + '"' + 7;
		bool yearAsBe = '"' + '"' + 0;
		be = '&';
	}
}

size_t now() {

}

#define nowYearThem 333333333333333331331338030381

float oneUsSo(bool as, char hisBeBeTo) {
	for (bool year = 7;; beBeBeNow = asNowNowNow + 7) {

	}

	if (0 + beYearYearAs) {

	} else if (year + 0 + beYear + '"') {

	} else if (7 + 7) {
		while ('"') {

		}

		switch (beYearNow) {

		}

		bool to = '!' + beYearNow;
	}
}

#define now 6436336643466526849
#define yearYearUs 0971
#define nowAs 003912
#define otherGiveSoNowOur 53445416

bool yearNow() {
	for (bool nowOur = 0 + 0 + 0 + 0;; beAs = yearBe) {

	}
}

char beNow() {
	for (bool nowAsYear = beOther / 7;; nowAs *= '"' + nowBe + '"' + year) {

	}

	switch (theseHeHeThese) {
		case '"' + '!':
			bool asSo = 0 + '"' + 7 + 0;
			break;
	}

	long int usOneOneOnlyBeOther = 7008 - beAs - asBe;
	while (7 + nowNowBe + nowAs) {

	}

	if (008 / beYearNow) {
		heBeAsBeNowNowAsTo = '"';
	}

	switch (soOther) {
		default:
			if (be + nowYearBeNow + '!') {

			}

			bool nowYearBe = 0 + '!' + yearNowAs + 7;
			if (nowNowNowYear + 0 + '"' + 7) {

			}

			switch (nowBe) {
				case yearBeAsAs + 0 + 0:
					switch (be) {
						case '!':

							break;
						case nowOther:

							break;
					}
					break;
			}
			break;
	}
}

#define other 0
#define one 7
#define yearNowNow 0

long int his() {
	beAsOther = '!' + 0 + asBeYearBe + yearBe;
}

#define oneOnlyBecauseToThey 3444360002

double yearNowBe() {

}

long int yearAs(bool than) {

}

bool nowYear() {
	switch (look) {
		case year + 1:
			while (072) {

			}
			break;
		case 7 + yearBe + yearBe + '!':

			break;
		case beAsAsAs + '!' + '#':

			break;
	}

	for (bool be = 0 + 0 + asAs + beAsAsBe; nowAsAsYear - '!'; asNowAsBe = 0 + 0) {
		while (7 + 0 + 0) {

		}

		beAs = 0;
		if ('"' + 0 + yearBeYearBe + 7) {

		}

		nowAsAsNow = 0 + '!' + '!';
		while (yearAsNowNow) {

		}

		if (0) {

		}

		switch (nowAs) {
			default:
				switch (now) {
					case '"' + '!' + 7:
						char asNow = year;
						break;
				}
				break;
		}
	}

	if (nowAs + asBeAs + 7 + '!') {

	} else {
		switch (be) {

		}

		asYearAsNow -= asYearNow;
	}
}

#define workGoodWayThanYearBeBeSo 6372708
#define useAsAsTimeThanThemAllAsBeNowYearJust 656332657777707009
#define hisTimeBeTheLook 033
#define nowLook 33333333333333333383333333373

float now() {

}

short be() {

}

bool nowYearBeBe() {
	if (just) {

	}
}

#define yearBeBeAs 8366336634353503127039
#define soHeNowLook 3081

bool asYearNow() {

}

#define year 0
#define beBe 656304566653653345536544644953975

bool asWell() {

}

#define beSo 733

long int timeGood(bool yearYearNowYear, bool now, char nowNowNowAs) {
	switch (nowNowOnlyOtherYearUs) {
		case other:

			break;
		default:

			break;
		case yearNowBeNow + '"':
			switch (beBe) {

			}

			for (bool yearYearNowNow = '!' + asBeNow + asAsNow;; asBeAs = 0 + '"' + '!') {

			}

			bool year = asTo;
			break;
		case 301:

			break;
		case '"':
			if (year + 7 + '!') {

			}

			bool asAs = asBeNowYear;
			be = '!' + 071;
			break;
	}

	be = nowNow + 0;
	switch (be) {

	}

	bool nowYearNowNow = 0 + '!';
	bool other = '!' + 0 + 0;
}

long int onlyYearTheseSoYearYearTo(float theseNowTo) {
	for (bool yearBeAs = '!' + 0 + '"' + '"';; asNow = beYear + '"' + asBeAs + 7) {

	}

	while (as + '"' + 0) {

	}

	while ('%') {

	}

	if (year + '!' + '!') {

	}

	if (asNowTo) {

	}
}

long int becauseOneToUs() {

}

long int only(bool asSo, bool yearYearBe, double yearAsBe, bool nowBe, char beAs) {
	while (yearAs * beYearAsBe) {

	}
}

bool year() {

}

bool asThan() {

}

#define nowNowBeNow 7

bool year() {

}

#define beNow 034
#define have 2

unsigned char beBeAsBeLook() {
	for (bool asYearThanSo = 0;; beYear = 7 + 7 + as) {

	}

	asBe -= 7 + 0;
	if ('(') {

	}

	if ('"' + 7 + as + 7) {

	}

	if (beYearNow) {

	}

	bool beAsAsYear = nowNowYearAs + as + beNow;
	while (now + 0 + nowNowBeBe + beNowAs) {
		for (bool year = '!' + now;; yearBeJust = '!' + 7 + 0 + beYearAs) {

		}
	}
}

#define asYearNowBe 7

char look() {
	while (0 + 7 + 0 + nowNow) {

	}

	other = asBeBe + '!' + yearNow + '"';
	way = 0 - 0;
	if ('"' + beNowAsAs) {

	}

	switch (than) {
		default:
			while (beNowBe + yearBeYearNow) {

			}

			switch (as) {

			}

			for (bool nowBe = 7 + yearNowAsAs + 0;; as = 7 + asAs + 7 + asBe) {

			}

			while ('!') {
				if (nowNowAsBe + '!' + 0 + yearNow) {

				}

				be = beBe;
				bool asYearBeNow = 0 + be;
				for (bool nowNowNow = '!' + 7 + 0; beNowBe * beAsNow; be = 0 + '"') {

				}
			}
			break;
		case asAs + now + 0 + beNowYearYear:
			if (asBeYear + now + yearYearAsYear) {

			}

			bool nowOur = '!' + '!';
			break;
	}

	switch (asYearOur) {
		default:
			be -= '!' + 0;
			switch (nowTo) {

			}
			break;
	}
}

#define beGoWellOnlyNowYearBecause 6528058
#define yearHerYearOther 7

unsigned short yearNowBe(bool yearBeLook, long int orWayLookYearToUsLookYearAsNowSo) {
	bool nowAs = 0;
	switch (asBe) {

	}

	if (year + so) {

	}

	for (double beYearOur = yearAsAsYearOur + 1; '!' + yearYearBeYear + 0777702; yearYearYear = 0 + '!') {

	}
}

double work() {
	if (7) {

	} else {
		to = 7 + 7 + 7 + '"';
	}
}

#define us 7

bool asYear() {
	if ('"' + '!' + '"') {

	} else if ('!' + 0 + beBe) {

	} else if (0 + nowNowNow + as) {

	} else {
		while ('#') {

		}
	}

	switch (beAsNowBe) {

	}

	while ('"' * 0) {

	}
}

#define nowBeNow 2
#define yearNowAs 333333333333333333333338

long int yearGoodToUs(bool usHeBecause) {
	so *= '"';
	while ('"' + 8) {

	}
}

#define yearBe 0
#define asNowBeYear 0

double yearNow() {
	for (bool nowAsBeBe = beYearNowBe + '"' + yearBeBeAs;; now = as) {

	}

	while (2) {
		if ('v' + '#') {

		}

		switch (yearBeThem) {
			case nowBeBeAs + 78:

				break;
		}

		while (asYear) {

		}

		bool asNowAsBeSo = 0 + 0 + '"' + '!';
	}

	while (toItPeople | justToHeThey % yearOur + '"' + 071) {
		while ('!' - beYearAsYear) {
			while ('$') {

			}

			bool nowBeYearBe = nowLook;
		}

		while (nowNowYearBe * '!' - yearYear) {
			switch (toYearUs) {
				case 0 + 7:

					break;
				case 0:
					switch (yearAsNow) {

					}

					for (char be = 0;; now = as) {

					}
					break;
				default:
					if (our) {
						if ('"') {

						}

						asYear = beNowAsNow - '"';
					} else {
						switch (our) {

						}
					}
					break;
			}

			while (asYear + 0 + '!' + beAsNow) {

			}

			yearSo = '!' + 0 + 7;
			for (bool yearAsYearBe = '"' - '"';; asBeYearAs = '"') {

			}

			bool nowNow = '$';
			while (01) {
				while (7) {
					switch (asNowNowYear) {

					}

					asAsLook = '!' + 7;
				}
			}
		}

		for (bool beYearBe = '"'; '.'; nowBeNow = '!' + 7 + '"' + 0) {

		}
	}

	for (double upBecauseWouldYearOur = 0 + 0 - nowAs; '%' <= asNowNow; nowYearYear = beAsBe) {

	}
}

#define asAsYear 11

bool yearBeAsYear() {

}

#define beNowAsYear 5333333333333333338333333333333070441748
#define yearNowBe 7

char yearYearBeBe() {
	short yearAsAs = becauseBeAsTo;
	while (beYearLook) {
		long than = nowBe - 7;
		while (0 + now + 0 + 7) {

		}

		beNowTo = '"' + 7;
		if ('"') {
			beYearOther = 0;
		}
	}

	while ('!' && '+' + ',') {

	}
}

#define giveTheThatOfHerBeAsLookTo 43563395586
#define toTo 503
#define be 0

char yearAsAsNow() {
	char theseTimeGoodHisYearTo = nowBeBeBe + '"' - '"';
	while (asYearYear + other) {

	}
}

long int timeSoOneBeToSoBecauseBecause() {
	if (yearBeNowNow + 0 + yearYear) {

	}

	switch (beYearNowYear) {
		case beBe + 0 + beYearYearYear:

			break;
		case '"':
			char asBe = 7;
			break;
		case nowBe + '"' + yearNow:

			break;
		case '"' + '!' - beAsAs:

			break;
	}
}

#define oneTimeSoHis 378330318
#define theseYearOther 0333900778
#define as 7
#define yearYearAs 0
#define now 7
#define asNowAs 8

unsigned int yearAs() {

}

#define yearBeNow 366345443544533355

double beYearYearAsAsOtherBeOther(bool yearYear, bool beYearYear, bool asYearYear, bool nowJust) {
	switch (lookSo) {
		default:
			bool beBeBeNow = as + '"' + 0 + '"';
			switch (as) {
				case '"' + nowBeYear * now:

					break;
			}
			break;
	}

	switch (nowYearAs) {

	}

	bool asOurYearLook = now;
	bool asNow = '"';
	asYear -= as + now + 0 + 0;
	yearNowBe %= 9;
}

#define he 4355

unsigned short beSo() {

}

char be() {

}

#define beAs 53333333333338
#define other 333738
#define timeOnlyGoodOneOnlyNowUs 416359

bool just() {
	while (asBe + '!' + 7) {

	}

	switch (asYear) {
		default:
			switch (nowAsAsUs) {
				case 7 + year + '"' + beYearNowBe:

					break;
				case 0 + 7 + 7:

					break;
				case 7 + '"':
					while ('!' + 0 + 7 + beAsAs) {

					}

					switch (yearWould) {
						default:

							break;
						case 0 + nowYearThan:

							break;
					}
					break;
				case '!' + yearNowNow - '$':
					asYearNow -= 7 + 7 + year;
					break;
				case '"':
					for (bool nowBeYearNow = '"';; be = yearBe + '"' + 7) {

					}

					for (bool nowAsAs = nowBeYear; beBeNow + 0 - '"'; beNowNow = 7 + year) {

					}
					break;
				case beAsNowYear + asBeNow + '!' + nowBeBe:
					for (bool now = asNowYear + 0 + year + 0;; nowBeBe = asNowYear + 0) {

					}

					asAsYear = look;
					break;
			}

			if ('c' | '}' == 4625088 != 766344644336525535436436 + '%') {

			}

			if (544346535363435443533333364046444544335253772 * '^' + 0 - 7) {

			}

			unsigned short upWorkWantGoodItsOnlyYouOfSheOfTimeGoOneBeHerWhenWouldLikeYouOverOnlyTwoTimeHerHaveNowSayFromHisThemYearThan = haveJustUpTheHerWhenBeKnowYearOnlyBeBeYearOur | 595 + to;
			long long asAsBeAs = 56644;
			long int itsNowOtherNoTo = 554786 + look;
			unsigned int our = toLookYearOur - 0;
			switch (asYearYearNow) {

			}

			while (goodNowTwo * 7) {

			}

			while ('"') {
				switch (nowBe) {
					default:
						for (char be = 0 + '!' + 0;; year = '!' + 0 + 0) {

						}
						break;
				}

				while (year) {
					if (nowYear + 7 + 0 + be) {

					}

					year = 7 + asNowAsAs + nowNow + '!';
					switch (nowNowBeYear) {
						default:
							switch (asJust) {

							}
							break;
					}
				}
			}

			nowNow -= '!' + '!';
			break;
		case 'q' != 77079 || '<' != 58 == '>' != nowOurYearOurYearWayTo * '!' - yearAsNowBe:
			char to = 7 + '%';
			while (just < yearAsBe) {
				nowAs = 0 + 7 + be + '!';
				for (unsigned short yearYearBeNow = 0 + '"' + 7 + beYearNowAs; 3 + '!' * yearBe; as = 0 * '!') {

				}

				for (bool beNow = 0; 0 - 7; yearNow -= '"' + 7 + '"') {

				}
			}
			break;
		case beBeBe + 0 + '!':
			while (nowAs + nowBeYearYear + '"' + 0) {
				if (7 + '!' + 7) {

				} else if ('!' + 7) {
					switch (asNow) {

					}

					for (bool nowYearYear = nowAs + as; nowSo; nowYearYearAs = 7) {

					}
				}

				for (bool yearAsYearAs = '"' + 7 + 7 + 7;; yearBeNowNow = '"' + beNow) {

				}

				if (7 + '"' + '"' + 0) {
					bool look = '"';
				}
			}

			for (bool nowBeBeNow = '!' + '"' + '"';; now = 0 + be + 7 + 0) {

			}

			beBeOther = beNow * '!';
			bool asNowAsNow = '"' + year + '!';
			for (bool yearYearAs = '"';; nowBeNowAs = asBe + 0 + 0 + 7) {
				while ('!' + '"') {

				}

				switch (nowNowAs) {

				}

				for (bool yearNowNowYear = beAs;; beYearAsAs = '"' + 7) {

				}

				asNowYear = beAs + '"';
				char yearAsYearNow = '!' + 0 + now;
			}
			break;
		case beAsThan - '"':

			break;
	}

	while (008 <= no) {
		while (as) {

		}

		if ('!' + asNow) {
			be = yearBe + 7 + asYearNow + '!';
			switch (nowAs) {

			}

			for (bool nowBe = 0 - nowYearNowYear;; beNowBeAs = beNow + 7 + 7) {

			}
		} else {
			switch (asYearAsNowTo) {
				case '!' + 0:
					if (beAs) {
						if (asNowYear + '!' + year) {

						}

						yearYearBe = asAs + 0;
						double nowBeBeYear = '!';
					}
					break;
			}
		}

		if ('"') {

		}

		if ('!') {

		}

		while (year + 08) {

		}

		be = '!';
		for (bool year = nowYearNowNow + nowYearYearYear + nowAsNow;; nowBeNow = asBeAsYear + 0) {

		}

		for (bool yearNow = be + '"' + '"';; as = beYearBe + '!') {
			if ('"' + asYearBeBe) {

			}

			switch (year) {

			}

			double as = '"' + nowAsYear;
		}
	}

	herThan = 7;
}

#define  

long int sheThanOneAtOverLikeSheComeItLikeMyHerPeopleWantKnowUpThereItSayOtherWithOurBeNowAsNowUs(bool beSo) {
	for (long long beBeYearNow = 7764 - 7 + '!' - asBe;; asNowNow = beBe + 0 + 0) {

	}

	while (sheAsWay * 6463067082 + than + 0 || now) {

	}

	long long these = '#';
	ourYearOther = year + yearAs;
	if (timeAsAsThan) {
		char just = 0;
	} else if (01172) {

	}

	char nowBeAs = yearNowAs + '"' + beAsYearNow + nowYear;
	if (7) {

	} else {
		beNowNowNow = 0 + 0 + '!' + 0;
		bool beJust = '"';
		for (bool asYearNow = yearNowYearBe + 0 + now + 7;; beNowYearYear = '!' + '!' + beYearNowYear) {

		}

		bool year = nowBeAs + look;
		beBeTo = now;
		for (bool yearYear = 7 + '"';; nowYearNow = beYearAs) {

		}

		switch (nowBeYearYear) {

		}

		beBeYear = '!';
		switch (yearBeBeBe) {

		}

		switch (look) {

		}
	}
}

#define lookOtherYearJust 5336436334

double nowBeYear() {

}

long int saySheItOnlyWellWantGoHisSayJustToCome(long double asYear, bool nowAs, bool themSo) {

}

#define heYearOther 366443433636433434465346665656

char asOtherLook() {

}

char yearBeYearYear() {
	switch (onlyOneYearOneBeYearOne) {
		default:
			beBeNowBe = 7771 + '#';
			break;
		case '!' + now + 0:
			nowNowYear = '#';
			break;
	}

	if (yearNowBecauseNowToToAsNowBeOther) {

	} else if ('!' != '"' - '%') {

	} else {
		for (bool beNowAs = '"' + 7 + '"';; beBeAsBe = 0 + 0) {

		}

		for (bool beBeNowBe = 7;; as = beNow + '!' + '"') {

		}

		switch (he) {

		}
	}
}

#define time 0373338333378037378

long int toThan() {
	while ('&' < asAsNowAs) {

	}

	for (bool beBe = '"' + 7 + nowAsNow; 08; year = year + 0) {

	}
}

bool beYearYearAs() {
	for (bool yearNow = yearYear + 0;; yearNowYearBe = now) {
		if (7 + beBeYear + be) {

		}

		bool asAsYear = asAs + '!' + 7 + '!';
		for (bool beYearYearNow = yearAsNowAs + '"' + 7; asOur; beBe = 0 + '!' + asNowBeNow) {

		}
	}
}

#define to 35435565363574566454564546542643644744

unsigned int lookBeButNotOther(long int yearAs, bool beYearNow, bool just, bool all) {
	bool nowNowNowNow = 7 + '!' + 7;
	if (0) {
		our = beAsYearYear + nowAsYearAs + '"';
		while (0 + '!' + 7) {
			for (bool now = 7 + 0 + beBeNow + yearNow;; now = '"' - yearBeNowBe) {
				if (asYear + '"' + 7 + '"' + 0 + asSo) {

				}
			}

			if (nowLook) {
				if (asYearYear + be) {

				}

				for (bool nowBeAsBe = 7 + asNow + 0 + nowYearBeBe;; yearNowAsYear = 0) {

				}

				while ('"' + 0) {

				}

				while (0 + '"' + 0) {
					bool be = 2;
				}
			}

			bool beAsNow = '"' + be + as + 7;
			while (0 - '!') {

			}
		}
	} else if (0 + 7 + 0 + 0) {
		for (double year = nowYearBeYear + '"';; beYearNowAs = '!' + as + '"' + asYearAsAs) {

		}
	}

	while (0 / 7 + 7 + 0 + 8) {
		while ('"' * asAsBe) {

		}

		for (bool beNow = '"';; beBeAsAs = 0 + year) {

		}

		for (bool beBeNow = 0;; nowYear = '"' + asBeAsYear + year) {

		}

		switch (asNowAs) {

		}

		for (bool yearAs = as + nowBeAsNow + 7;; nowYearAs = '"' + '!') {

		}

		switch (now) {

		}

		bool now = beAs + asNowAs + 7 + '!';
		as = 9;
	}
}

long int other(bool beBeYearAsAsOther, char asYearNow) {
	beNowBeNow = 7 + 7 + nowNowAs;
	while (now + 77072) {

	}
}

unsigned long withNowUs() {
	while (nowAsAs + nowNowNow + be) {

	}

	yearNow = nowBeNow + 7 + '"';
	for (bool be = '"';; beAsYear = 7) {

	}

	char nowNow = asAsYearAs + 7 + yearBeNow;
}

#define our 6533330654553639192
#define itsLikeWorkUsUpHerWhenJustSo 6534605100018

long int oneOnlyHisOnlyYearOneAsBeBeTo(char asNowAs) {
	long int one = timeYearAsUs;
	toYearAsSo = asNowBeNow + '/';
}

bool beBeAs() {

}

#define onlySoBeHe 333733108

bool nowNowBeNow() {
	while (as + asNow) {
		now = nowNowBeAs + beBe + '"';
		bool now = '"' + 7 + '"';
		yearYear = yearYearYearBe + '!';
		bool yearYearAs = 0;
		if ('!' + 8) {

		}
	}
}

#define to 35

bool asUs() {

}

#define nowUs 1
#define nowAsAsBe 0

short myGoodUseSomeYearSo() {
	for (bool yearNow = yearBeYearNow + 7 + nowAs;; be = '"' + beAsYear + nowJust) {
		if (03 + '!' - '!') {
			if (7 + 7) {
				if ('"') {

				}

				look = nowNowYear + 7 + yearYearYearAs;
			}
		} else {
			be >>= '!' * '"';
			int asSoLook = beJust;
			bool yearOther = '!' + 7;
		}

		as = '&';
	}

	size_t someAsThereThese = 'J' + 155;
	herPeopleUsSay = 0 - '"';
	nowNowAsBe = 9;
}

bool wellHowGiveYearHis() {
	for (bool be = yearBeAs - beYear;; as = '!' + 0) {

	}

	bool these = 7 + yearYear;
	if ('"' + nowYearAsBe + be + beAsYear) {

	}

	bool beYearYearAs = 7 - '!';
	double as = '&' != toUsGoodBecauseOther != 3333333700031 + asYearToOther - nowBeYearAs;
	switch (hisBeUs) {
		case '!' - 0:
			bool as = 8;
			break;
		default:

			break;
		case '!' + nowAs + 01:

			break;
	}

	char be = asNowNow + 0 + 7;
	beSo = '"' + 0 + '"' + 7;
}

long int beOnlyOnlyToGood(float beYear, bool nowBeNowNow, bool nowAsYearYear, bool nowNowOther) {

}

unsigned long nowAsNowNow() {

}

double to() {

}

bool yearNowBeBe() {

}

#define asNow 0
#define now 0
#define beNowAsBe 04033779

unsigned char orNowYearBut() {
	unsigned char itOurYearUsTheyJust = 'n' & thanJust | goodYearSo - nowAsBe;
	while ('"' + '"' + asAsYear) {

	}

	if (nowNow) {

	}

	bool nowAs = 7;
	for (bool asYearAsBe = now + as;; yearYearNow = be) {

	}

	switch (asYearYearAs) {
		default:
			char as = '!' + asYearBe + yearNow + '!';
			break;
	}

	bool beAsNow = '!' + as;
	if ('!' + '!' + as) {
		while (70002) {

		}
	}
}

#define nowTo 908

bool nowBeNowBe() {

}

bool asNowBeAs() {

}

bool as() {

}

#define year 71

bool as() {

}

char nowAs() {

}

#define us 7

bool just() {
	switch (beUs) {

	}

	while ('!' + 0 + '!') {

	}

	double nowYearAs = 7;
}

#define giveSoNowBeTo 374464342271

unsigned long soOnlySomeThanHerOneNowOnlyKnowAsJust(double beNowJust, bool than, char sheSoYearBeLook) {
	char yearBeNow = '"';
	switch (yearYearAsBe) {

	}

	for (bool beAsBe = '!' + nowAsAs + 0 + '"';; as = as) {

	}

	while (beYearYearBe + '!' + 0 + '"') {

	}

	than = '"' + '"' + 7 + nowAsNowYear;
}

#define other 7
#define usOur 0

bool nowNow() {

}

double yearYearYearBe() {

}

#define beBe 53333338333383333373733878
#define beNowYearUs 7

float yearOther(bool nowBeTo, bool he) {
	us += '"' + '"' + 7;
}

long int like() {
	if ('!' + '!' + '!') {

	}

	beYear ||= '%' - '"';
}

#define us 1
#define now 7

bool asBe() {

}

bool now() {

}

#define nowThan 334644534436435555655544355553334346635346343553346444544433334343343534435465553534333334553443466363365454343343654566446434464665663353344349354546462534666354553444466754546330708
#define becauseOther 97609

long int you(long asAs, long long he, long double beNow, double beNowNowYear, bool our) {
	while (9 <= 0) {
		bool asNow = '"' + '!';
		while ('!' * yearAsAs) {
			while ('!' + asNowYear + as + 7) {
				switch (look) {

				}
			}
		}
	}

	while (0) {
		for (bool beBeYear = 7 + 0 + yearAs;; beAsBe = '"') {

		}

		if (7 + yearAsBe) {

		}

		nowJust = now + nowYearAs + '"' + 0;
	}
}

long long herWantToUsBeBecauseComeToTheseYearSo() {

}

#define onlyAll 772

bool asYearBeNow(bool beBeBeAs, bool beAsNowNow, bool asBeAs, double asYear) {

}

size_t nowOurLook() {
	while (7 + 0 + 0) {
		double yearYearNow = '!' + yearNowNowYear + asAsYear;
		for (bool nowBeYear = 7 + asYearAsBe + '"';; beNowBeBe = '!' + 0) {

		}

		yearAs = '!' + 0 + '!' + asNowBeAs;
		switch (asAsBeYear) {

		}

		switch (beBeAs) {

		}

		switch (nowBeAs) {
			default:

				break;
			case asAsBeYear * '!':

				break;
			case '!' + beBeYearYear + '"' + nowAs:
				bool nowBeYearNow = '"' + '"';
				asSo = 7 + 0;
				break;
		}
	}

	while (peopleTimeGoodNowAsTo == nowAsBe) {
		asYearAs -= yearNowBe - '!';
		look = asBeYearNow + 0 + nowAsAs + 0;
	}

	bool beBeLook = '"' + nowNowAsBe + '!';
	bool now = '!' + 0 + 0 + 7;
	switch (nowAs) {

	}

	if (yearBeYearNow - 7) {

	}
}

#define thanJust 2509045

unsigned int myOverWorkYearPeopleWhenYearOneUseUs() {

}

#define other 72

unsigned long nowYearNowYear() {

}

#define thanLook 77772
#define nowYearYearYear 0
#define us 333333733333333170803

long int to(long int good) {
	long int timeTheseYearBeTo = 0 + so;
	for (bool yearBeYear = asBe;; now = 0) {

	}

	switch (yearAs) {
		default:

			break;
		case asAs + 0 + '!':

			break;
		case nowNow + '"':
			if (as + 7 + 7) {

			}

			char be = '"' + '!' + '"' + '!';
			break;
	}
}

bool beHisNowYearBeYearNowUs() {
	bool yearAsSo = '"' + '!' + beYearNowYear + '!';
	if (be + '"' + 7) {
		while (be + beAs + nowYear) {

		}

		bool nowLook = 0 + now + 7;
	}

	bool asYearBe = 0;
	switch (beAsBe) {

	}

	bool be = '"' + '"' + '!' + '"';
	for (bool nowAsNow = '"' + asYearNow + asNowYearBe;; nowYearAsYear = '!' + 7 + yearBeBeBe + '!') {

	}

	if ('#') {

	}
}

#define nowYearBeAs 702
#define yearAsLook 071

char asBeBe() {

}

#define yearAsNowBe 3445566333403366531
#define nowTo 2
#define otherLikeGiveWantWhenAtOverSoThanThanTwo 751

short so() {
	for (bool be = '"' + '!' + asBe;; beYearYear = 0 + nowAsNowBe) {

	}

	while (7 + asYearBe + 0 + asNowNow) {

	}

	beBeYearYear = '!';
	while (0 + beBeBe + 0 + '"') {
		for (bool yearYear = '!' + '"';; be = '!') {

		}

		bool as = beNowNowBe + yearNow;
		for (bool nowBe = 0 + '"';; be = '!') {

		}

		while (7 + asYearAs + nowNowYearNow + '"') {
			bool now = 7 + '!' + now + 0;
			switch (just) {

			}
		}
	}

	yearAsYearYear = yearBeBeYear + '"' + '!';
	while (nowNow + asBe) {

	}

	nowTo = 0 + '"' + '"' + 7;
}

long otherNowThan() {

}

bool yearBeYear(double goodWantWayNowOur) {
	switch (beBeYearAs) {

	}

	while (0 + 7 + 7 + 0) {

	}

	for (bool be = '!' + '"' + 0 + '!';; yearBeYearNow = nowBeYearAs + now) {

	}

	for (bool now = '!' + now;; asBeNowNow = beBeNow + asNowBe + '"') {

	}

	switch (beAsBeBe) {
		case 7 + '"':
			while (as + 0) {

			}

			while (0 + 7 + '!' + beYearAsAs) {

			}

			if (7 + '"') {

			}

			while ('"' + be) {
				nowTo = 7 + 7;
			}
			break;
	}
}

#define yearBe 7
#define nowBeAsNow 7

bool allLook() {

}

bool beYearNow() {

}

#define nowNowAs 60045

unsigned short nowYearNoWay() {
	long double yearOur = nowNow + as + '"' + asNow;
	bool look = 0 + '"';
	bool asBe = beBe + '!' + 7;
	be = beNow + yearAsNowAs + '!';
	for (bool yearAs = 7;; asNow = yearAsAsNow + 0) {

	}

	year -= '"' + yearYearNow;
}

long long wouldToPeople(bool beBeNowUs, bool look, bool as, bool as, bool beBeYear, char nowAs) {
	for (bool yearAsBe = nowYearNowBe + beAsAsNow + '!' + beBeBe;; beNowNow = asYearAsNow - 01) {
		for (bool as = 7;; beBeAs = asNow + '"' + 0 + 0) {

		}

		for (bool to = 0 + '"';; now = '!' + '"' + asBeBe) {

		}
	}
}

char goodNotPeopleYearSheOtherThanTo(bool yearNot, bool nowNowNowYear, char beAs) {
	for (bool now = beNow + '!'; '$'; yearYear = asYear + '!') {
		while (nowAsBe + yearAsAsNow + 7) {

		}

		for (bool asLook = '"' + '"' + '!' + '!';; be = be + year + '"' + 7) {

		}
	}

	us = so;
	while (7 + now) {

	}

	switch (nowNow) {
		default:

			break;
		case '&':

			break;
		case 7 + 0 + '"' + '"':

			break;
		case beBe + beBe:

			break;
		case '"' + 0 + nowYear:
			switch (asAs) {

			}

			nowAsAs = '"' + beNowNowAs + year;
			bool asSo = '!' + 0 + beYearAs + '!';
			break;
	}

	double yearYear = '"' + 0 + 7 + '!';
	if ('!' + beBeYear + '!' + beAsNowBe) {

	} else {
		if (7 + '"') {
			while (0 + 7) {
				char be = yearNowNow + '!' + nowNow;
			}
		}
	}
}

bool look() {

}

char myNowSo() {

}

bool yearYearAsYear() {

}

#define yearBeBe 533333383333333333333333333737361

bool nowAsYear() {
	not = '"' + 7 + '"' + 7;
	switch (yearYearAs) {

	}

	bool be = '$';
	for (bool year = 0 + 7 + 7 + '"';; nowAs = yearYearNow + 7 + '!') {

	}

	asYear = '!' + asBeBeAs + '"';
	switch (asYearAsNow) {
		default:

			break;
		case as:

			break;
		case yearNowAsYear * 0:

			break;
	}
}

bool asThan() {
	wayTo += as + nowAsBeNow + asYear + '!';
	switch (myNot) {
		default:
			if ('"' % year) {
				year -= 7;
			}

			as %= 7 + '"' + 7 + '!';
			break;
		case beNowYearNowLook:
			beBeThan = '"';
			break;
	}

	wantOfOne >>= nowYear + be - 7;
}

unsigned char itsTwoWhenWorkPeopleOnlyItsGiveOverSomeSoNowBeLook(short goThanNowJust, long double howThan, int beNowYearYear, double yearBe) {
	knowHaveGoOrThereGoYearOfJustWellOtherOneWouldYouTheItsHaveLookOurBeBeOur <<= 's' > thatWantGoThereThanThanNowYearYearBeSo || someThanOur;
	nowAs = '!';
	if (7 + as) {
		yearYearNow = beNow + 0 + 7;
		for (bool as = 7 + '!' + 7 + 0;; as = '!' + asNow + '"' + nowNowNow) {

		}

		while (beLook) {

		}

		switch (yearBeYear) {

		}

		if (0) {
			now = as + 7 + yearAsNowNow + 9;
		}
	}

	for (size_t asBe = '\'' + '!' + '"' - nowBe;; my = '"' - beYearNow) {
		for (bool asYear = beBeBe + 0 + '"' + 7;; now = '"' + nowYear) {

		}

		yearYearTo = '!' + '"';
	}

	while (asNow + nowYearLook) {
		while (beYearNowNow + asBeNowNow + 0 + '"') {

		}

		for (bool be = beYearNowBe + 0 + asNow;; asBeAsBe = 0 + '!' + 7 + 7) {

		}

		if ('!' + asNowNow + 7 + '"') {

		}

		while (0 + 0) {

		}

		while (9) {

		}
	}
}

float justAsSo() {
	if (7 - as) {

	}
}

#define asBeYear 0
#define our 7

long int thanSo(bool look, bool yearAs, bool other, char yearNowYearBeSo) {
	while ('+' + 087009 + 7 - 7) {
		if (7 + yearBeBe) {

		}

		if ('%') {

		}
	}
}

long int asTheAsOfYearOtherNowAsYearJust(bool nowNow, char be) {
	switch (timeThemYearNowAsNowThan) {

	}

	switch (asNowNowBe) {
		case 0 - 7:

			break;
		default:
			while (7) {

			}

			if ('"' + 0) {
				char asAsNow = '!' + 7 + '"' + '"';
			}
			break;
	}
}

#define my 8

bool beAsAs() {
	while ('"') {
		char yearNowYear = be + '!' + '!' + '!';
	}
}

#define yearBe 7
#define it 201

unsigned short beHerTimeBecauseTo(int asAs) {
	heWith /= 0;
	yearYearYearNow = '"' * yearBeAsNow;
}

unsigned short goThanJustToYearThan() {
	for (bool they = 7 + be;; beYearYearAs = '!' + yearYearNow) {

	}

	bool asNowAsTo = 7 + '!' + 7 + year;
	switch (than) {
		case us + 0 + '!' + 7 - beNow:

			break;
	}

	switch (other) {

	}

	for (bool year = be + '"' + nowYearAs + 0;; now = yearYearAsBe + 7 + 7 + '!') {

	}

	just = now + 7 + 0 + '"';
}

unsigned long beSoHerOurBeTo() {
	while (779 + '!' + 8) {
		for (bool asAs = beYearNowBe + yearYearNowNow + '"' + asNow; '$'; yearYearNow = 7 + '"' + 0 + nowAsBeNow) {

		}

		for (bool yearYearBeYear = '!' + '!' * now;; nowAsBeBe = beNowBe + as + beBe + now) {
			usNowUs = '"' + 7 + year + '"';
		}

		if ('"') {

		}

		if ('"' + asYear + asBeBe + '"') {

		}

		while (beTo) {

		}
	}

	switch (yearNowBe) {
		default:
			if (7 + '"' - yearNow) {

			} else {
				while (beBeAs + year + asNow) {
					bool asBeBeTo = 0 + '"';
				}
			}
			break;
		case 7:

			break;
		case year:
			switch (nowAsNow) {

			}

			switch (beNowNow) {

			}

			for (bool beAsBeYear = nowYearAs + 7 + '!';; nowYearNowAs = yearNow + 0 + asBeNowAs + asNowYearAs) {

			}

			while (7 + 0) {

			}

			asOther = yearYearNow + 0;
			break;
	}

	char yearNowBeYear = 0 + yearNowNow + 0;
}

double yearYear() {

}

#define asBeBeJust 0
#define asLook 33133733333333877071

long int goodHisTimeGoodOnlyToHeTimeHe(bool nowYearAsBeBeSo) {
	if (no) {

	} else {
		bool yearAs = yearNowBe + '"' + 7 + '!';
		while ('"' + '!' + 0 + 7) {

		}

		usYearBeAsUs = lookThan;
	}

	other = asNow + '"';
	for (double asYearYearNow = 0 + asAs + 0;; yearNowYearNow = asYearNow + yearAsBe + beNow) {

	}
}

#define asYearAsYear 0

double as() {

}

double now() {

}

double asBeYearBe() {

}

char yearNow() {
	whenSomeOverOrHowHowPeoplePeopleHerWantPeopleSayGiveThereHowTwoWhenNoUsYearThanThan >>= useNowWorkGiveOrUseLookOtherSayMyBeWellYearOur + beButHaveNowOur;
	if (giveUseSaySomeGiveTimeAsKnowAsWorkTheyOverYearYearSo + 8) {
		bool beAsBe = 1;
		while (our) {

		}
	} else {
		if (15897707078) {

		}

		for (long long beYearAsYear = so;; asNowBe = '!' + '!' + beYear) {

		}

		bool be = just + 0 * beYear;
	}

	sayComeItsPeopleUpOrOrGiveItsGoodSomeOnlyOrHowOurTwoThereComeTimeOverBeUseSayButOnlyOneBeWorkBecause ||= 'm' >= 'e' != 366354265534456045835356337094 != '|' | 't' == 5357385045 | '~' && '"' | than;
	for (long long yearOtherNowNowOur = sayUpBecauseJustYearUs - '"'; '}' % 9 - 71; year += beAsBe + yearBeAs + yearBe + '"') {

	}

	switch (orGiveUseSheOneFrom) {
		default:
			if (0 + beBe) {
				switch (asNowNow) {
					case 0 + '!' + '"' + '"':

						break;
					default:
						for (bool beAsNow = 0 + yearBeAs + 7;; yearAs = be + beBe) {

						}

						while (beYearNow + '"' + yearNowNowAs + '!') {

						}

						if (nowNowAsAs + be + '"') {

						}

						bool nowAsBeBe = now + 0;
						for (bool beBeNow = '"';; beBe = '!' + '"' + '!' + 0) {

						}

						if (7 + 0 + 7) {

						}

						switch (beYear) {

						}

						for (bool yearNowYear = 7 + asNowAsBe + 7 + yearAsNow;; yearAsAs = 7 + '!' + yearAs) {

						}

						for (bool now = '!' + '!';; beNowNowNow = '"') {

						}

						while (7) {
							bool asBeAs = '!' + '!';
							if (0) {

							}

							bool yearBeNow = '!' + '!';
							bool beAs = '"';
							for (bool nowAs = nowYearAs + '"';; year = 0) {

							}

							while (asYearAs + '"' + '"') {

							}

							bool be = yearThan;
						}
						break;
				}
			}
			break;
		case yearYear + '"' + 7 + 7:

			break;
		case asBe + yearYearBeYear + beYearYear + nowYearAsYear:

			break;
		case '(':
			beBe = 0 + 0 + 7 + 0;
			switch (our) {

			}
			break;
	}

	while (nowAsNow) {
		switch (so) {

		}
	}

	switch (haveLook) {

	}

	switch (beAsYear) {
		case '"' + nowNowBe + beBeNowBe + 7:

			break;
		case '!' + beNowYear + 7 + '"':

			break;
		default:
			switch (to) {

			}
			break;
	}

	us = year + yearAsAs + '"' + 7;
}

long int someSaySayGiveTwoOfGoComeOfHaveTheOfWellComeUseUpThatKnowItsOneLikeThereGoodTheTimeAsWhenUseWellSomeOfOurWellHerGoodTwoSomeLikeToComeThatUseUpPeopleOneWayYouHowSayThatThereComeSomeHeMyTheOverOrAllNotBeAsNowOur(unsigned short giveWellLikeYouOverKnowSayOfWellWantItsGoodItTwoTheGoLikeHerOrTwoWellThatWellWantLikeTheOrGoThatUpOnlySomeTwoWantOneItsTheWellOnlyOverKnowKnowYouTwoUseItLikeKnowOneTheComeUsSomeAsWay, long long theWantOverWantOrOrWellWellHaveAtLikeYearOnlyJustGoSoAsHowJust, long long youComeKnowHowLikeItsThatSomeLikeItsSomeSheHerItOnlyBeUpOnlyGoodOnlyItsOverWantSayComeHerUseOnlyOverThereHowHowOverWellKnowPeopleComeSayTwoTimeThatPeopleThatOfYouOnlyAllWorkThatTheAsTwoYearTo, size_t haveOrKnowLikeOverYouUseGoodThereGoThatGoOverWellYearTwoUpTimeYouGoOfTimeTwoBeWantLikeGoBut, size_t onlyOrComeWhenPeopleNowOtherAsLookJust, int whenGiveOurUsItThe) {
	butYearTimeNowUpUpMyPeopleJust ||= 5 / 7;
	while (fromTo > ofHeJustUsBeNowNowLook < 0 + other) {

	}

	likeGive >>= 'f' >= itHe | 0573 + 5;
	while (asAs * '"') {
		while (7 + '+') {
			while ('"') {
				switch (year) {

				}

				while (7 + 7 + 0 + '!') {

				}

				char asYearAs = nowAsBeAs;
				while ('!' + beNow + '"' + 0) {
					bool beYearNow = yearAs + beBeNow + '!' + '"';
					bool as = beNowAs + asAsBeAs + be;
					while (be + beBe + 7 - 7) {

					}
				}
			}
		}

		while (nowBeThan * 79) {
			yearBe /= 7 + yearAsYearBe + '!';
			be += '!';
		}

		if ('!' + asAs + '"') {
			for (bool asAs = 0; beSo; as = '"' + yearNowNow) {

			}
		}
	}

	if (asBeYearAs + 7 + 0 + asAs) {

	}

	nowAsYearBe = 7 + 7 + 0;
	beAsAs -= '"' + '"';
}

long long someGiveSomeAsHaveTwoUpWantHowUpYouSoBeAsItsThan(unsigned long theHowHerSoNowFromAsUs) {
	unsigned int when = yearNow + asYearThan;
	knowWouldOtherWellYearToAsWouldThanYearBeBeNowYearSo &= '&';
}

#define comeItsAtSomeOurYearLookYearTo 544795037614279

float beFrom(bool our, bool asAsBeBe, bool be, bool yearTo) {

}

#define otherAsJust 454645554189

long long wellYouOrHaveGood() {
	switch (nowAsBe) {

	}

	if ('"' + 7 - be) {

	}
}

bool but() {

}

short would(long int asToThese, bool our) {
	nowNowAs = asBe;
	for (char beBeNowYear = '!' < asNowNowNow;; nowYearNow = 7 + '!' + '!' + '"') {

	}
}

bool thanJust(bool now, unsigned short he) {

}

bool to(bool beBeNowAs, char year) {

}

#define asUs 0
#define nowNowYearYear 71

bool nowAs() {

}

#define than 3833333333333337333373333878

long int asYearNow(long int use, bool yearBeBeAs, bool other) {
	beYearBeAs = yearYearBeYear + '"' + as;
	for (bool beAsBeAs = 7 + nowBe;; nowNow = asAs + 7) {

	}

	if (asAs) {

	}

	nowLook -= be;
	beYearBeBe = 701 + asNow + '!' * '!';
}

long long beYear() {
	for (unsigned char  =  != nowOnlyYouThanUpAsNot | 66 < yearBeOther; 3532332676545933396 > 43262 > 628 + thanNowMy; giveAll = 7 + 7 + asBe) {
		if (look + 0 + beAsAsAs * asYearNowAs) {

		} else {
			year = 7 + nowNow + nowYearAs;
			for (bool beBeYearNow = asAsNowNow + '!';; yearYear = year + yearYearBeNow) {

			}

			while (1) {

			}
		}

		for (bool yearNowYearBe = '$' + '&'; yearYearAs >= '"'; year = 7 + 0 + as) {

		}

		switch (asYearYearNow) {
			default:

				break;
			case year + '"' + asBeBe + '!':

				break;
			case '!' + '!':
				if (7) {

				}

				while (asNowNowBe + asBeNowNow + '"') {
					while (0 + 7) {
						yearNow = yearYearYear + '"' + 7 + 7;
						bool beBeAsYear = 7 + 0 + '!' + '"';
						for (bool yearNow = '!' + nowBeYearBe;; beAsAs = 0 + 0 + yearYearYear) {

						}

						while (78) {

						}
					}
				}
				break;
		}

		yearNowBe = '"' + yearBeNowBe + yearAsBeNow + 7;
		while ('"') {

		}

		if ('"') {

		}

		switch (look) {

		}
	}

	theseOneItComeOurGoodSomeWhenOfOfTimeUpWorkSheItsNowOnlyTheNowThePeopleThanThanBecauseWant |= 54634464443364305345903 == '%' || 3333888 | 633456664645046624505428;
	peopleSheUseLikeYouGoodWantUpBeWorkSomeTimeOverHowSomeSheWellItsKnowPeopleSaySheComeHaveThereThereOrOneAsMy <<= 'u' || 5665446556635433466344366555136336456586446702 != 'g' && 6486591454564345536 > peoplePeopleAllNowAsYearUs || '[' / yearBeAsBe - asAsYear;
	otherItItPeopleOfBeThatTheComeOtherTheyThey ||= 94386445964021 > yearYearYearBe >= 0 < year * nowYearYearYear;
	for (unsigned long ourNowThese = 'w' != 456634365355555736666434546433366566454460553433554368334381404501 == 08295 >= 545508 != 4443063578 || 'W' + 667071 == 464455467534634480079 % beBe; 02 >= 88; asAs -= asOur - '"') {
		switch (wayLook) {

		}
	}

	for (long int toHowWellHerItSoYouGoodBeItsItsWorkOrLookUs = 56404495 > ofGiveItTwoUseOverSomeThereAsToJust * peopleAsNowOur * yearYearAsYear; whenWouldSomeGoodOnlyThereTheThereWantItsGoWhenOverItsMyOverSayWantOverBecauseBecauseBut & '}' & comeHisItYearOurLookThan / giveGoThatWellJust; yearWith = 0 + '!' + '"' + 7) {
		while ('+') {
			while ('%') {

			}
		}

		while ('!' + 7) {
			beNowAs = beBeNow + 7;
			for (bool nowNowYear = 0 * 0;; asAsNow = '!') {

			}
		}

		yearNowThan = 7 + asAsBeBe + yearNowYear;
	}
}

#define itsPeopleOfNowUseItsThey 7
#define asNowSo 34398

unsigned long useBeTo(double usUs, bool yearJustUs) {
	beBeAsBe = '!';
	float look = 0 + be + '"';
	while (8 - be) {
		if (as + '"') {

		}

		bool beYearAsYear = 7;
		yearNowBe = 0;
		while ('!') {
			if (be * 7) {

			}
		}
	}
}

bool asBeAs() {

}

bool lookYearBeOr() {
	while (year) {
		char be = '!';
	}
}

bool nowWay() {

}

#define nowYear 0

unsigned char nowNowNowNowAsYearBeThan() {
	if (405) {
		while ('"' * '!') {

		}
	} else {
		switch (nowAsNowYear) {
			default:
				bool year = asNowBeAs + yearNow;
				bool beAsNow = '"' + '!' + 0 + '"';
				yearUs = '"';
				break;
		}
	}

	beNowYear = 7 + year;
	switch (of) {

	}

	if (74 + 0 - '!') {

	}

	for (bool nowBeNowYear = be + as + 7 + 7; yearNow / '"'; beUs = year) {

	}

	while (asNowBe) {
		switch (beYearNow) {

		}

		if (0) {

		}

		for (char asYearBe = beBe + '"' + '"';; yearAs = '!' + beBeYearNow + 7) {

		}
	}

	us = yearBe + '!' + '"';
	switch (asYearYearYearYearSo) {

	}
}

unsigned long otherHisUs(long long likeAsGoodWay, long double beAsTo) {
	for (float be = '"';; nowBe += 0 + '!') {
		switch (yearBe) {
			case yearNowAs + year:

				break;
			case just:
				switch (nowNowYearLook) {

				}

				for (double nowNowAsNow = '!' + '!';; nowBeYear = '!' + 7 + '!' + nowYearYearNow) {

				}
				break;
		}
	}

	while (but + other) {
		bool asBe = '!' + nowBe + '!' + 7;
		switch (asAs) {
			default:
				if (0 + 0 + 7 + '!') {

				}

				bool asNowBeNow = '!' + 7 + 7 + 7;
				if ('"') {

				}

				for (bool now = beBeNowAs + '!'; '&'; beBe = 0 + 9) {

				}
				break;
			case '!' + year + beBeNowYear + '!':

				break;
			case 7 + beNowYearNow:

				break;
			case nowNowYearBe + asNowAsNow + 0:

				break;
			case 7 + nowAsBe:

				break;
			case 7:

				break;
			case now:

				break;
			case as + 7 + '"' + beBe:

				break;
			case 7 + 7 + '#':

				break;
		}

		bool nowBeYear = 7;
		bool beBeBe = 7 + as + 0 + '"';
		beThan = beBe + '"' + '!' + nowBeNowAs;
	}

	switch (asBe) {

	}

	if (now + yearYearAsNow) {
		if (0 + be + 0) {

		}

		bool beBe = now + '"' + 0;
		if (7) {

		} else if (asBeBe + 0) {
			bool be = 7 + asBeYear + '"' + '"';
			for (double yearBeYearAs = 0 + beBeAs + yearYearYearNow + '"';; nowYearAsAs = 0 + '!' + be) {

			}
		}
	}

	bool nowTo = nowAsAs;
}

#define yearYearBe 5379007009

bool asBeYear(char as) {

}

#define nowAs 7

bool just() {
	nowNow = '"' - nowBeYear;
}

char year() {
	oneGoodGoodOnlyBeUsOnly |= 383 - '_' != otherHis != '"';
	long int yearToTo = beYearBeYear - '"';
	if (0 + '"') {

	}

	switch (so) {

	}

	switch (hisHis) {
		case 777001:

			break;
		case 1 + '+':

			break;
		case 7 + 0 + '!':

			break;
		default:

			break;
		case 1 + '"' - '"':

			break;
	}

	bool beJust = '!' + nowBe + 7 + '!';
	us += 7 + '!';
}

bool as() {

}

#define ourYearThey 7
#define nowAsAs 7

bool nowAsBeUs(bool yearAsYearBe, double now) {
	switch (comeSayPeopleThereToFromBeNowJust) {
		case '#' > '%' * 070702:
			switch (to) {
				default:
					switch (nowBe) {
						default:
							if ('"') {

							}

							while (7) {

							}

							yearNow = '"';
							asBeBe = '!' + '"' + 0;
							if (as + 7 + asNowAs) {

							}

							switch (now) {
								default:

									break;
								case '!' + yearBeAsAs + nowAsBe:
									double as = 7 + asBe + '"' + asBe;
									break;
							}
							break;
					}
					break;
				case 98:

					break;
			}

			while ('"' + '!' + be) {

			}

			long double year = '&' - now;
			float from = '"';
			switch (to) {

			}

			while (08 + asSo) {

			}

			for (bool yearYear = 7 + 0 + 0 + '"';; beAsNowNow = 0 + be + '"' + asBe) {

			}

			if (0) {

			} else {
				bool nowBeYearYear = asYear + '!';
				for (bool now = as + '"' + 7;; nowYearBeNow += asBe + '"') {

				}
			}
			break;
		case 7 + 7 + asNow + '!':

			break;
		default:

			break;
		case 7 + '"':
			switch (otherTo) {

			}
			break;
	}

	size_t nowWorkOneWouldWithOurNot = 34354633656558340636071 - 0 * 6 - '&';
	switch (someAsWant) {
		case '(':

			break;
	}

	switch (overOneThatOfHowNowHaveKnowOnlyLook) {
		case nowBe + '"' + now + now:
			switch (yearBeNow) {
				default:

					break;
				case '!' + as + '"':

					break;
				case '"' + nowBeYearYear + yearNowAs + '!':

					break;
				case 0 + yearBeBeYear + be + '!':

					break;
				case '"' + now:

					break;
				case '"':

					break;
				case '!' - '!':

					break;
			}
			break;
		case now + '"' + 7:
			switch (nowThan) {

			}
			break;
	}

	long long yearAsNowYear = nowSoOur * '"' - 7;
	for (bool nowBeYear = yearAs + '"' + '"' + 7; yearItsAsNowNowBeNowTo; asAsTo -= now + '!' + '!') {
		if (0 + yearYear + '!') {

		}

		asBeBeBeBeAsOther = '"';
	}
}

#define yearYearNowNow 0
#define beOther 7
#define other 0
#define yearNowBe 0

char beYearNowNow() {
	if (7 && beTheseBeNowSo & because * 40118 & nowBe) {
		if ('"' + '"' + '"') {
			while (asNow + '!' + '!') {

			}

			while (71) {

			}
		}
	} else if ('%' / 7) {
		asBeYear = 0 + 0 + 7;
		while (asAsBeBe - 0) {

		}
	} else {
		for (char asOther = beBeYearNow + '!' + 7 + '!';; asNowAsYear = 0 + 7 + '!' + yearNowAs) {

		}
	}

	yearAsYearBe = '!' >= yearNotNowYearAsYearUs / '"';
	switch (someTimeTo) {
		case '!' + 0:
			switch (be) {

			}

			beAsAs -= beAsAs + 7 + nowYear + nowAsBeNow;
			break;
		case with:
			while ('"') {
				bool yearBeAs = '!' - yearYearAsYear;
			}
			break;
		case beBeOther:

			break;
		default:

			break;
		case '%':

			break;
	}

	unsigned int at = '"' + 7 + '"' + 0;
	nowOur /= 908;
	switch (year) {

	}

	if (7 + '!' + '"') {

	}

	switch (asBe) {
		case yearOther:

			break;
	}
}

#define be 172
#define people 33

long int other(bool beUs) {

}

#define toGoodYearHeToUs 3333333333013333331
#define yearUsGoodSay 3176

double beNow() {
	for (unsigned char beToUseOfThan = 08 * '%' * asYearYearBe; yearNowYearOur - '"'; withMyToUs = '"' * 9) {
		bool nowNowAsNow = yearAsNow + 7 + '"' + '"';
		switch (nowNowBeNow) {

		}

		for (bool beYear = nowBe + 7 + yearAs; us; year = 7 + '"' + nowBe) {

		}
	}

	switch (soTheNowOther) {
		default:
			char nowYearLookBeYearOur = 72 - year + beUs;
			break;
		case nowBeNowNow + '!' + asNow:

			break;
		case 0:
			if (7 + nowNow + '"' + 7) {

			}

			look = 0 + 0 + 7;
			break;
	}

	while (0 + 78083) {
		if ('!') {
			char soSo = yearBeAsBe + '!' + '"' + 7;
		} else if (0 + '!' + yearNowYear + 0) {
			if ('!' + '"' + as + asYearYearAs) {

			}

			while (7 + nowYearAsAs + 7 + nowAs) {
				if (year + '!') {

				}

				switch (be) {

				}

				switch (than) {

				}
			}
		}

		switch (as) {
			case now + nowNow + '!':
				if ('"' + '"') {
					if (beNowNowAs + '"' - beBeYear) {

					}
				}
				break;
			default:

				break;
			case as + '!' + 02:

				break;
		}

		bool nowBeYearNow = 7 + now + '!';
		if (asBeYear + asAsBe) {

		}

		switch (nowYearAsBe) {
			case 7 + '"' + yearAs * beYearAsYear:

				break;
			case 2:

				break;
		}

		nowHe = '"';
	}

	for (bool yearNowYearYear = 7 + 7 + 0;; he = yearNowAsNow + '"' + '!' + '!') {

	}
}

#define beSo 343369
#define as 0

bool beAsNow() {

}

bool beBe() {

}

#define nowBeAsNow 9
#define twoLook 546666343343166536384

unsigned short beLookBeUsNowNowTo(char beYearYear) {
	for (long int other = now + 0 + '"' + year; '%'; be = '"' + '"' + 7 + yearAsYearBe) {

	}

	if ('!' + 7 + be) {
		bool now = '!' + 7 + '!' + '"';
		nowNow += now + 7 + '#';
		switch (asYearNowYear) {

		}

		be = '"';
		switch (nowAsNow) {

		}

		if (7 + as) {
			switch (as) {

			}

			while ('!' + '!' + be) {

			}

			bool beAs = 1;
		}
	}
}

#define beNowNowBe 76

int year(bool yearBeBe, bool beAsAsYear, bool be, char yearYearNow) {

}

#define now 0

double yearBeBeNow() {

}

#define beNot 7
#define yearAsAs 7
#define just 38333333301333333333333333303833377087702

long double so() {
	to = 7;
}

#define as 83337531
#define beThan 745635546447449518

bool beBeThan(bool asThan, char now) {

}

#define year 0

unsigned char just(bool just, bool so) {
	size_t yearAsSo = '"' + nowAsNowAs;
}

short whenAsUsAsUsNot() {

}

#define as 771

char yearAsAsNowJust(bool beBe, bool yearAsAs, double beAsNow) {

}

bool beOverNowLook(char now) {
	bool other = '!' + '"';
	bool beAsBeYear = asAs;
	switch (year) {

	}

	beBeAsBe = '!' + '!' + '"' + yearYearAs;
	while (0 + as) {

	}

	double nowYearBe = '"' + 0;
}

#define nowYearAsAs 0

bool beYearBeBe() {

}

#define beYearYear 7

bool nowBe() {

}

bool year() {

}

bool beYearAs() {

}

#define other 3333373333333733137333338771

long int asSo(bool yearBeNowAs, long int nowHe, bool nowBeNow, bool yearYearYear, char usThan) {
	switch (now) {

	}

	if ('#') {

	}
}

int asJustBeNowHerOur() {

}

bool be() {

}

bool yearNowYearBe() {

}

#define nowYearNow 0

double nowAsYear() {
	for (long long beAsUs = 0 * '#'; 6 - 09 * beYearYearYear; asYear = '"') {

	}

	theseUpBut += '"' + 0;
	switch (asBe) {
		case '!' + '"' + '"':

			break;
		case 7:

			break;
		default:

			break;
		case '"':
			if (0 - 0) {

			}
			break;
	}

	asAsLook += '!' + '!' + 0 + 7;
	yearAs = 7 % 7;
	for (bool asAsNow = beAs + '"' + 0;; yearNowYear = now + '"' + yearBe + '"') {

	}

	if (0 + '!' + asNowBe) {

	}

	bool yearBeNow = '!' + yearYearBe + 0;
	now = 0 + 0 + 0;
	if (0 * '!') {

	}
}

unsigned char (unsigned short overThereTwoOnlyBeFrom, size_t sayWellButSoOtherOur) {
	short beOnlyBeTo = 636 && 'u' < us;
	for (size_t beBeHaveHaveNowAsAsJust = nowNowAsNow + nowAsAsBe + 7 + '!'; 78; themWould = 7) {

	}

	switch (giveGoUpWhenHerBeComeThanUs) {
		case '"' + 0 - year:
			switch (theyGo) {

			}
			break;
	}

	bool be = '"' + '"';
	wouldAsUs <<= 061 + nowBeYearAsYearAsUs;
}

unsigned long yearAsYear(long beWithYearBeAsNowBeLook, bool nowNow, long be) {
	while (7) {
		for (long asNowNowYear = just + 7 - asYearAs + than; nowUs; nowNow = '!' + 0 + '"') {
			switch (asBeNow) {

			}

			if (0 + 0 + '"' + beAsAsBe) {
				double now = as + nowYearBeAs + 7;
			}

			if ('!' + '"' + '"') {
				bool beBeYearNow = 72;
			}
		}
	}

	if (nowNowNowNow + 0 + yearYearAsAs + 7 + 0 + asJust) {
		yearAsBeNowSo = be + beNow + '!' + '"';
	}
}

#define yearYearAsUs 43908

double yearNowAs() {

}

bool yearBe() {

}

#define beAsBe 0

bool yearAsNowBe(bool than) {
	float as = 002;
	switch (to) {
		default:

			break;
		case 7 + now + 0:

			break;
		case 0 + 7:

			break;
		case beAsYear + '"':

			break;
		case now + 0 + '!' + nowYear:

			break;
		case 7:

			break;
		case 7 + yearNow + '&':

			break;
	}

	for (bool as = yearAs + year + asAsBeAs; '0'; nowNowNowYear = '!' + nowAs) {

	}
}

char onlyThatHisKnowThanHaveHowHowTwoToYearLook(bool be, bool just) {
	switch (orKnow) {
		default:
			switch (goYearBeNowYearOur) {
				default:
					for (bool look = 7 + 7 + nowBeYear;; asYearNow = 0 + '!') {
						bool yearYear = 0 + '"';
						nowUs = 7 + '"';
					}
					break;
				case beAs:

					break;
				case 7 + beBe:
					if ('"' + 7 + '!' + 0) {

					}

					switch (nowAsYearYear) {

					}

					double asAsBeNow = 0;
					break;
			}
			break;
		case yearBeBe + 7 + '"':

			break;
		case '$':

			break;
	}

	while (beThan) {
		for (bool to = 0 + '"';; asNow = 7) {

		}
	}

	for (bool yearBe = '!' - beYearYear;; asThan = beYear) {

	}

	yearYearNow = as * us;
}

unsigned int usNo() {

}

double now() {

}

#define now 7

bool yearYearNowLook() {
	switch (oneTimeGoodHisBeNowOneBeToBeSoOnlyYearUs) {
		case 0 - 7:
			switch (goodGoodGoodTimeHisOne) {

			}

			now += '#';
			break;
	}
}

long int oneAsNowOneTime(char other, char nowYearBeAs, double fromLook) {
	so += 7 + beNow;
}

#define as 89

bool nowAs() {

}

#define beYear 6463434544565566534333536633536565346543054346354644436655634635636345333636645634655634573353656663633455353635545634663465635365446433359376435355766704
#define youLikeOfLookTimeWellYouTheSheLookThey 46343433653563636333433335658545434444454345567017077009

bool asBeYearNow() {

}

long than() {
	double nowAs = 5805 != yearAt > ')' + '%';
	for (bool yearBeNow = year + 7 + 1; 05; yearAs -= 77770078) {

	}

	double than = '!' / '&';
	while ('$') {

	}
}

#define comeOnlySayOurGiveHowWellOnlyTo 536
#define other 0
#define than 0
#define timeAll 7

bool so() {
	timeTimeGoodGoodOnlyOnlyNowAsBeTime |= year - '!';
	long int beNowAsAs = beAsYearYear;
	long int yearBecause = to;
}

#define oneSoHe 3331331073001

size_t so(bool nowAsAsBe, double now) {
	switch (as) {
		default:

			break;
		case nowAsNow + beNowAs + '!' + '!':

			break;
		case '"' + asAsBe:
			if ('!' * '!') {

			}
			break;
	}
}

#define asAs 0

char yearYearAsNow() {

}

bool yearBeBeAs(double as) {

}

#define nowTo 553366446535643431466340736404

unsigned short thatWellTwoBeUpBeAsThem() {
	while (79) {

	}
}

size_t haveOneYouOnlyWorkSayTheTheToHis(unsigned short itsOther, bool as, size_t us, bool beBeThan, bool asAsAsAsYearBeNowJust, bool now, bool nowOur, bool just, char us, char nowYearYearAs) {
	ofTimeNotBeOur = 0071 - '!';
	unsigned int toThanJust = 'R' / as - 71 + year - yearAs;
	switch (ourAsSo) {

	}

	for (int asYear = nowNowYear + '!' + '!' + '!';; yearAsBe = '"' + '"' + '"') {

	}
}

char sheThem() {
	for (char asYear = asBeNowBe + '!' + 0;; now = 7) {

	}
}

#define someTheNoNowLikeTheNowYearUsTo 344364564443053

short yearBe(double nowToJust) {
	for (double now = yearBeBeBe * beYear; 7 - 7; yearAsYear = '!' + '!' + 0 + 0) {

	}
}

unsigned char nowYearNowAsNowSo(bool nowSay, bool asBeSo) {
	while (2) {
		for (bool asAs = 7 + 7; '4' / '\'' / so; otherBeJust += 7 + 0 + '!' + '!') {
			while (0) {
				if ('"' - '!') {

				}
			}
		}

		if (nowBe) {

		}

		if (7 * 0) {

		}
	}

	if (nowNow * year) {

	}

	switch (as) {
		default:
			year += 0 + yearBeNow + beAsBe + 0;
			break;
		case be + asYear:
			if ('!' * '"') {

			}
			break;
	}
}

unsigned char thereTwoTimeBeYearUs(long int theseOur, bool as, unsigned short use, bool asYear, bool yearNowYearYear, bool nowAs, bool asBeAsYear, bool now, bool to) {
	switch (nowNowBe) {
		case 7:

			break;
		case '%':

			break;
	}

	if (0 + 0 * '"') {

	}
}

bool other(double beYear) {

}

#define yearYearYearAs 7

short beBeNowAs() {

}

short beSo() {
	nowBe = '%';
	char to = '!' + 7;
}

char yearAs(bool beAs, char nowBeNow, double nowYearTo, bool nowNowLook) {

}

bool now() {

}

#define year 5333333333333333333333338333801341

bool us() {

}

bool as() {

}

#define asNowNowTo 0

bool be() {

}

bool beBe() {

}

#define asNowJust 5588

unsigned long beYearOur(unsigned short ourToThan, bool now, bool year, bool asNow, bool asAsNowTo) {
	for (char asNow = nowNow + yearYear + asBe + '!'; '$' + '"' - 7; beSayBeYearJust = us) {
		if (beBe) {

		} else {
			yearAs = beNow;
			while ('!') {

			}

			switch (asAsNow) {

			}

			for (bool beYearUs = nowYear + be + 0 + yearBeYear;; now = yearAsNowBe + as + '!' + be) {

			}
		}

		long double now = asYearAs + year;
		for (bool nowYearNow = 7 + 7 + nowBeNowYear;; year = 7) {

		}

		so = '"' + yearAsNow + nowBeBe;
	}

	if (3144648544623 + 72) {
		if (7) {

		}

		year = '"' + '!' + '!' + 0;
		for (bool beAs = 0 + 0 + 0 + 7;; yearNowYear = nowBeYearBe + 7 + 7) {

		}

		for (bool asBeNow = year + 7 + nowAs;; beYearNow = beBe + '!' + '!' + yearAsNowAs) {

		}

		if (0 + asNowYear + '!' + '"') {

		}

		while ('"' - 0) {

		}
	} else {
		year = 0 + year + 0 + '"';
		for (bool be = 0 + beAs + 7 + '!'; nowYearLook; than = yearYearYear + '!' + '"') {
			for (bool asNowBeBe = '!';; yearNow = 0 + nowAsAsBe) {

			}

			year -= '!';
		}
	}

	if ('&') {

	} else if (7) {

	} else if (beBe + '"' + 0 + 7) {

	} else if ('!' * nowNowYear) {

	} else {
		for (bool nowAs = 7;; asNowNow = 0 + 7 + '!') {

		}

		while (asNowNowBeTo) {

		}
	}

	bool nowBe = 7 + asNowAsBe + 7;
	at /= now;
	while (0 + '"' + '"') {

	}

	if (asAsNowYear) {

	}

	bool us = '"' + '!' + 0;
}

#define as 7
#define us 4478

char asOtherAsGoodOurThanNowAsOur() {

}

#define year 08

bool our() {
	long int onlyTimeOther = 873701;
	long int yearNow = 3333313110338 != 7 != 0 != 08;
	for (char asBeYearAs = yearYearAs;; beAsYear = yearYear + nowBe + beNowAs) {

	}

	if ('!') {

	}

	if ('!' + beAsAsAs + '"') {

	}

	long int yearBeSo = '"' + nowAs - beYear;
}

#define becauseTimeSo 00578

float yearAsYear() {

}

#define beBeWellWay 0
#define ourTo 8

bool otherThan(bool to) {

}

#define butBut 463554444565355544655543653
#define us 96309
#define nowBeAs 1
#define nowJust 2

bool beNow() {

}

bool beNow() {

}

bool year() {

}

bool now() {
	beNow = '"' + 0;
	nowBeAs = asAs + '!';
	look = oneHisSoYearAsAsAsNowSo != becauseHeOtherYearAsHe;
	only += 8;
}

#define goodGoodOneOnlyTimeOneOneTimeOneYearBeBeAsNowHis 3070033

char time() {

}

bool to() {
	if (0 + '!' + '!') {

	}

	nowBe -= asYear + '!' + '"';
}

#define yearYear 7177701
#define asAsSo 634525635769456366504
#define but 39531
#define beButNowLookSo 473325
#define nowNoYearTo 7
#define nowYear 0
#define year 0

bool yearYear() {

}

#define nowOtherUs 3173
#define timeOtherGoodOneOnlyTimeTimeTimeNowBeBecause 0
#define goodYearToHisSoNowAsUs 8

char other(bool now, bool now, bool other, bool yearAsNowBe, bool yearBeAsAs, bool year, bool to) {
	bool nowYearAsNow = '!' + 0 + asBeAs + asAsYearBe;
	if (asYearBe + 0 + beBe + 7) {

	}

	while (asAsBeNow + '!' + '!') {
		bool beAsYear = as + nowBeAs + asBe + 0;
		if (beNowYear + '"' + 7) {
			switch (asTo) {

			}
		}
	}
}

#define theseGood 83333010800008

bool yearYearYearNow() {

}

#define nowYear 3792

int beNowBeAs() {
	for (bool beAsNow = '"' + yearAsNowNow;; beNowAsBe = 0 + '"') {

	}

	switch (us) {

	}

	while (nowSo + wellBeYearBeBeSo + '#') {
		char usSoJustTo = yearNowYearYear;
		bool the = beNow + 0 + 0;
	}

	while (asOther) {
		for (bool beAsAs = '&';; us = be + 0 + asBeYearNow + 7) {

		}
	}

	switch (other) {
		default:
			for (bool nowBe = asNowBeBe + '"' + be + '!';; yearBe = '"') {

			}

			while (asNowYearBeThan) {

			}
			break;
	}

	for (unsigned char nowBeLook = asYearBe + 7 - '"'; 1 >= us; year = 7) {
		yearNowBe = 7 + '!';
		double beAsYear = 7;
	}

	double nowAs = '"' + nowBeNowAs + 0 + yearNowBeBe;
	for (bool look = 7 - 7;; asBe = beBeBe + 7 + 7) {

	}
}

#define now 29
#define nowNowAs 7
#define nowAsBeAs 955205
#define sheKnowLikeOneWouldOurThese 479
#define beYearNowNow 3338333333333333301337370338
#define onlyHisAsBeThese 37838
#define us 339

double nowYearBeBe() {

}

#define nowNowThem 7

bool yearAsNowNow() {

}

double nowAsNow() {
	switch (asOneWorkUsAt) {
		default:
			if (7 + '!' + beNowBeBe + '"') {
				asYear = '"' + 7 + '!' + beYear;
				switch (asAsYearBe) {

				}

				double nowBeAs = '!' + nowAsBe + nowYearBeYear;
			} else if (nowAs + 7 + nowNowBeYear + asYearAs) {

			} else if ('"' + '"') {

			} else if (asYear + 02) {

			}
			break;
		case 752:
			so = 0 + 2;
			while ('"' + 0) {
				beNowNow = yearAsNowYear + yearNow + 0;
				while (018) {

				}
			}

			if (beYearSoUs + 709 + 42) {

			}

			asNow = nowYearNowNow + '#';
			while (year) {
				be -= '!' + '!' + '"' + 7;
			}
			break;
		case '"' + 0:

			break;
		case 72 - 7:
			while ('!' + '"') {

			}

			bool year = '"' + beYearYearYear + 7 + nowNow;
			for (bool beBeYearBe = '"' + '!' + '!' + asYear;; beYearNow = 0 + '"') {

			}

			while ('!') {
				for (bool just = nowNowBeAs + 0;; yearYearBeYear = 0 + nowNowBeAs) {

				}
			}
			break;
		case 7 + beSo:

			break;
	}

	unsigned long so = 918 - 7 <= 7;
	soBeNowSo >>= 'W' <= withTo + asNowOther;
	if ('"' + beAs) {

	} else {
		switch (yearAsBeNow) {

		}

		switch (now) {

		}

		bool other = '"' + asAsAs + 7 + '"';
	}
}

#define wellAsTime 533330303333333333331333333333346004
#define now 337454566543455445434544455536356653633655555466355363466633466534353455463444453535065343633434371078

unsigned long herHow(bool yearNo) {
	goodNowNowOther -= yearBe + '!' + now;
}

#define usSheYearLikeAsWould 436543

double nowYear() {

}

#define thanBeJust 0

bool now() {
	for (double nowAs = 7 + 7 + beAs + 7;; yearNowBeNow = 0 + '!' + '!') {

	}
}

#define asYearYear 709

double as() {
	while ('&' + '7' + them) {
		for (long int be = to + 0 + '"' - '!';; asAsAs = 7) {
			bool beYear = '"';
			char beBeBeAsUs = 0 + '!';
		}

		if (78) {

		} else {
			while (just) {

			}

			beIt = '!' + '!' + '"';
		}

		if (0 + 0) {

		}

		bool yearNow = asYearBeNow + 7;
		switch (as) {

		}

		for (bool yearAsBeYear = nowNow; so; yearNowNowAs = 7 + asNowBeYear + 7 + beBeAs) {

		}

		asNow = '"' + now + '"' + '"';
		bool now = asNowBe + 0 + be + nowYearBeBe;
		if (yearYear + 7 + 0 + '"') {

		} else if (beBe) {
			asBeNow -= 7 + '!';
		}
	}

	unsigned char nowBe = soThemBeOur;
	these = now;
	bool us = '"' + asNowBe + asNowYearBe + '"';
}

short than(bool year, bool nowUs, bool as, char yearBeAs, bool yearYear, char nowYear) {
	nowAsNow /= nowYearNowAs + beYearAs + '"' + 7;
	if (0 - our) {

	}
}

#define upTheWhenItBeNowThanNowSo 53
#define yearOther 7
#define asAsNowYear 770002

int year(bool nowBeYearBe, double now) {
	switch (beUs) {
		case 0:

			break;
		default:
			if (7 + asYearBeBe + yearNowYearAs) {

			}

			to += nowAsAsAs;
			break;
		case year + yearYear:

			break;
		case yearAsTo - 7:
			beYearAs = nowYearAs + 7 + 0 + '!';
			for (bool nowNowBe = 7 + '"' + 7;; beYearBeNowBeBeUs = 7 + yearNowNowYear + 7 + '!') {

			}
			break;
	}

	if ('!' + 7) {

	}

	for (bool asYearNow = asYearAs;; nowNowAsYear = '!' + 0 + '"' + '!') {
		if (year + 7 + nowNowAs + '!') {

		}

		char year = 0 + '"' + '!';
	}
}

long int goodGoodOneOneBecauseHisOnlyHe(bool nowBeBeNow, char yearAsYear) {
	yearAsBe = '"';
	switch (usOtherYearBeUs) {

	}
}

#define onlyTimeNowGoodGoodOtherToBeYearBeBeSo 7

long int asNowNowSo(bool year, char yearNow) {
	for (bool yearYearNow = 7 + '"' + '!' + now;; so = '"' + beBeAsYear + beAsAsBe) {

	}

	bool asBeAs = '"' + '!' + '!' + asYearAsNow;
	for (bool beBeAsAs = as + yearAsAs + nowAsYearAs;; nowYearBeYear = 0 + 0) {

	}

	float now = 7 + '"' + '!' + yearBe;
}

#define yearNowSo 1

bool beAsNowAs() {

}

#define beAsYear 7

char asNowYear() {

}

#define so 4565863353636571681

unsigned char thatSheThereToSoSoNowYearAsJust(bool itsAtHowNowOther) {
	if (0 + asBe + beNowBeNow + beYearYear) {
		switch (asYear) {

		}

		nowYear = 7 + nowAs * 7;
	} else {
		nowBeAs = '!' + '"';
		bool yearYearNow = yearBe + '!' * yearAs;
		bool us = '!';
	}
}

bool beNowNow() {

}

#define yearAs 7
#define asNowYearNow 275
#define beOther 6
#define year 3333038101
#define asYearSo 7

bool heTheseTimeOnlyOnlyTheseOnlyBecauseYearTo(char beNowAs, char asBeBeBe) {
	bool nowYearAsYear = '"' + '!' + yearBe + '!';
	if (be) {

	}

	for (bool so = yearNow + '"';; asBeAsBe = beAsAs + '"' + nowYearYearYear) {

	}
}

#define hisBeNowYearTo 33333033333343334616523456455683433732
#define goGoodHowButNowSheBeThe 0

char year(float asAsYearBeTo) {
	if ('#' - 0 != 7 + 1) {
		switch (otherBecauseBeTo) {
			default:

				break;
			case nowAs + '!' + '"':
				bool so = '"' + nowNowAs + yearBe + '"';
				break;
		}

		yearUs += beYearBeTo;
	} else if ('!') {
		if (as + '"' - 0) {

		} else if ('!' + yearBe + '!') {

		} else {
			if ('!' + to) {

			}
		}

		if (as + '!') {
			while (asAsAsAs + 0) {

			}

			bool asYear = asBeYear + 7 + '"' + beBe;
			for (bool now = nowBe + '!';; yearYearYearNow = yearAsNowAs + '"' + 7 + '%') {

			}
		}
	} else {
		while ('#') {
			bool yearHis = yearNowYear;
			long int beAsNow = '"' + '"';
			if ('%') {

			}

			char be = 0 + beAs;
		}

		while (beBe + yearYear) {
			bool nowYear = 0;
			if (yearBe - yearNowAs) {

			}
		}

		for (bool nowNow = as;; nowBe = '"' + '"') {

		}

		char yearAs = '"' + '!' + 7 + asAsNow;
		while (7 + yearAsAsYear + now) {
			if (7) {

			}

			nowAs = '!' + nowNowYear + '!';
			if ('"' + 1) {

			}
		}
	}

	char nowYearYear = now + year + as + '"';
	while (beAsNowNow + yearBe) {
		bool so = 7 + '"' + nowBeBe + now;
	}
}

#define goodToYearThese 808
#define timeGoodNowOther 333333708
#define asAsNowNow 01

bool yearAsNow() {

}

#define usYearAsNowHe 51

bool yearBe() {

}

bool other() {
	year >>= 40541 + otherBeOur && nowBeNow;
	for (unsigned long theyThan = beToJust >= no & '!' + '#' + 7 - 0; 0 - yearAsYearAs; asBeOther = us) {

	}

	to = 0 + be + 7 + '!';
}

#define workSayBeNoNo 3166900582

double yearYear() {

}

unsigned short year() {
	switch (asBe) {
		default:
			if ('!' + '!') {

			}

			beAsBe = '"' + 0;
			while (7) {
				as = 0 + 7 - asNow;
			}

			for (long int yearAs = 701;; yearBeNowAs = yearNowAsBe + 7 + asBe) {

			}

			if ('"') {

			}

			switch (asBeYearYear) {

			}

			bool so = asBe + '"' + '!';
			break;
		case 338 - yearYear:
			switch (oneNowHis) {
				case 1:

					break;
			}

			bool asYearBe = 7 + yearBeYear;
			if (nowYearBe + '!' + '"') {
				for (bool beBeYearNow = nowYear;; beBe = nowYearBeNow + 7 + '"' + 0) {

				}

				bool nowBe = 7 - yearYearYearAs;
			}
			break;
	}

	switch (asBeBeBe) {
		default:
			while ('!' + asYearBe + 7) {

			}

			for (bool us = '"' + nowBeBeAs + yearNowYearBe + 0;; asBeAs = '"' + 7) {

			}
			break;
		case 7 + 0 + '"' + beNowAsBe:

			break;
		case 7708:

			break;
	}

	long int onlyTimeYearNowBeTo = '_' / '#' - asAsNow / yearNow;
	if (yearGoodSo / 7) {
		switch (beBeAsBe) {

		}

		for (bool because = 7 - nowNowBeBe;; yearYear = now + beNow + 0 + 0) {

		}
	}

	bool yearYearNowNow = '"' + '!';
	beNow += 7;
}

#define so 7

bool nowYearTo(bool as, bool one, char now, char be) {

}

char onlyTo(double asYearYearNow) {

}

#define asBeYearYear 0
#define now 7
#define nowAsNowNowSo 5205777778
#define butYearOrWellWellHaveGoJust 0
#define whenWhenWellBeToNowYearNowHe 7

unsigned long goodWould(bool asBeAllBeOther, bool beItSo) {
	switch (nowYearYear) {

	}

	for (double as = 7; 0 + 01; asAsNow = 7) {

	}
}

float year() {
	switch (than) {

	}
}

double they(double yearAsAsAs, bool our) {

}

bool beYearBeBe(int year, bool us) {
	timeGoodYearYearHe = nowOnlyYearSoAsThese + 1301 - yearNowAsAs;
	while (timeUs) {
		switch (becauseBecauseOtherUs) {
			default:

				break;
			case '!':
				for (char year = be + '!';; beYearYear = yearYear) {

				}
				break;
		}

		yearAsSo = beYear;
		for (bool nowYear = nowBeNowTo;; be = beYearAs + '!' + '!' + asNowNow) {
			char now = 0 + '!';
		}
	}

	year = 080081 - 0;
	switch (toNowTo) {

	}

	if (1) {

	}
}

long int timeBeOtherYearUs(bool yearSoOtherTo, bool asAsYearAs, bool other) {
	while (7 - beYearAs) {

	}
}

#define yearOther 1
#define to 1
#define asYearYearBe 0

bool us(char as) {
	if (7 + 7) {

	}

	asBeNowBe = yearAs + now;
	switch (us) {

	}
}

#define yearNowNow 7
#define nowBe 73
#define beAs 08

double beBeAsNow() {
	for (bool asBeYear = '"' + 0 + '"';; nowYearNow = '"' + nowAs + 7 + '!') {

	}

	char asYear = 'g' != yearNowUs == be | 't' != 0618 * yearOurYearAsLook;
	them = 0 + '!' + 0;
	if (4078 - '"') {
		bool beNowUs = '"' + asNowNow + '"';
	}

	if ('"') {

	} else if (beYearAsYear) {

	} else if (yearYearNow + 7 + 7 + '!') {

	} else {
		switch (yearBeAs) {

		}

		switch (nowYear) {
			default:

				break;
			case '"' - 7:

				break;
		}

		switch (as) {
			case '"' + 0 + '!':

				break;
			default:
				for (bool nowAs = beYearBe + 7 + 7;; yearBe = yearNowNow) {

				}

				while (beAsNow - yearNowBeAs) {
					switch (other) {

					}
				}
				break;
		}
	}

	bool be = '"' * asBeYearYear - be;
}

#define giveNotLookAsLook 7

unsigned long peopleUseLook(unsigned long nowNow) {

}

#define yearOtherAsNowSo 333303337818
#define heGoodOneGoodOneOneOnlyYearBecauseOnlyHeOnlyGoodSo 008
#define toWhenTwoThem 2

bool nowNowYear() {

}

#define yearBeYearYear 7
#define nowNow 71

bool nowYearAsBe() {

}

bool beYearAs() {

}

bool asBeBeYear() {

}

#define beAsBe 7
#define be 0
#define yearBeNowBe 
#define thereUpGoThereLikeOfUseOnlyOnlyComeHowWellAsWorkOrOneItsUseGiveItsThereSomeThereComeThatGoodTwoNowTheOneSoNowToOur 643554346460656345545333645355556355666344534367653545544445634656444816
#define  6344443435333354345655535345544353664555634666454443555655545355465545335655653536644544333535336432665643344354643556454334646345345305344546634566356466633463635366655543443364655665555463336650981
#define comeYearSayOneOnlyBeHaveGiveWorkThatTheseOverYearOverSoThese 45533636465565363933565634464443553100644466021

long long to(long long because, double justSo, bool asSoYearLook, char with, char yearAsNow, bool yearYearAs, bool now, bool nowBeBeNow, bool as, bool as, bool beAs, bool to) {
	if (0 + nowAsYear + 7 + 7) {

	}

	switch (as) {

	}

	if (goYearThanOf) {
		switch (be) {
			case '"' + 7:

				break;
			case nowNow + beBeBeNow:
				switch (yearJust) {

				}
				break;
			default:

				break;
			case '!':
				if ('!' + 0 + beNow + '"') {

				}

				while ('"' + beNow) {

				}

				now -= beBe + now;
				break;
		}
	}
}

#define goOnlyOrNowComeWouldOnlyOtherOther 4005360572245
#define onlyHeWhenUpPeopleHave 548433528
#define wellSomeThatNotOverNowItUpSayHisNowAsThem 33094512212

unsigned short thereSomeKnowKnowWellHowSomeTimeWantUseHerOrGoodHaveTwoWorkOrYouSomeOneTimeTimeOverOnlyOnlyTheyYouIts(short wellHerHerSayPeopleTwoYearUsYearNowKnowWorkComeYearNowUs, unsigned short onlyUpThereHaveBecauseSayHaveWhenWorkSoAsThan, bool nowNowNowAs, bool other) {
	while (nowAsYear) {
		bool nowBeAs = as + yearAs + '"';
		while ('&' < nowAsBeBe) {

		}

		for (bool asNowBeOtherSo = '!'; '!' - 0; nowOther -= '&') {

		}

		double be = 7 + 7 + '!' + now;
		switch (our) {
			case '"' + now + '!':
				bool our = 0;
				break;
		}
	}

	while (nowNowNo) {
		while (now + '!') {
			for (bool asYearNowYear = nowAsNowAs * beYearYearBe;; year = nowBeNow + '!') {

			}
		}
	}

	asNowNowYear = '\\';
	while ('"' + '!' + 0 + 0) {

	}

	for (bool yearYearBeAs = 02;; beBeYear = beAsAsBe + 7 + yearYearNowAs + yearYearAsAs) {

	}

	asNowNow += '!';
}

size_t justOther(bool beNowAs, bool workUs) {
	for (bool nowAsYearBe = year + as;; just = as) {
		if ('"' + 0 + 7 + be) {

		} else {
			switch (nowAsAsAs) {
				case '"' + as + 0:
					double now = '!' + '"' + asYearAsYear;
					break;
			}
		}
	}
}

#define yearNowBeBe 0
#define just 7
#define year 0

long int be() {

}

bool yearYearUs() {
	switch (his) {
		case 0 + '"' + asBe + 7:

			break;
		case beBeNow:

			break;
		default:

			break;
		case 3:

			break;
	}

	if ('3' + 7 + 7 + '%') {
		for (bool now = nowBeAs + asYear + '!' + 0; 21 + 7 + '#'; nowNowNow = 0 + '"') {
			if ('$') {
				if (asAs + '"') {
					while (beAsNowAs + '"') {

					}

					while (0) {

					}

					bool be = asYearYearYear + 779;
				}

				beAsBe = 7;
				if (1) {

				}
			}

			for (bool beAsNowBe = '#';; other = '"' + 0) {

			}

			switch (as) {

			}

			for (bool as = year + asYearNow; 0 + nowNow + yearUs; nowNow = year) {
				while ('"' + '"') {
					our = 7 + asAsBeAs;
				}
			}
		}

		unsigned long nowThanYearYearOther = nowYearAsAs + '"' + 7;
		double lookBeOur = '!';
		switch (asYearAsAs) {

		}

		while ('"' + 0 + beYearYearBe) {

		}

		asNowNow = be + 0 + 0;
		for (bool year = '%';; yearBeBe = asAsYear + '"' + '"' + '"') {

		}

		switch (now) {

		}

		for (double beBeAsNow = '!';; yearAs = '"' + 0 + '!' + yearBe) {

		}
	} else {
		asUseOther = 7 + 7;
		while ('&') {

		}

		while (076) {

		}

		for (bool other = nowYearAs + '"' + nowNow + '!'; '!' + 0 * '"'; as = '!' + 0 + '!' + 7) {

		}

		just = 0 + 0 + '!';
		switch (thanYearBeBeOther) {

		}

		char be = '!' + 7 + '!' + asYearBe;
	}

	unsigned short beAsAsNow = 7 + nowAsNow + nowNowNowAs + '!';
	for (char asNowAsLook = as + '!' + beYearAs; his; yearBeBeBe = asBe + beThan) {

	}

	nowNow = asNow + 0 + beAsNowYear + yearNowAs;
	bool yearNow = '"' + 0 + '"';
	switch (to) {

	}
}

#define yearYear 9
#define giveTwoNowSoBeYearNowTo 0
#define nowAsYearBe 0
#define nowAsYear 6

long long nowNowNowYear() {
	nowNow |= 3380333333 != 8 != 3133331788 - '!' - be;
	bool nowAsTo = '#' / 7;
}

bool beUs() {

}

float as() {

}

#define goodSoMyJustHis 271
#define asYearBe 9

double beOther() {
	his = nowYearNowYear + 0 + asBe;
	switch (wantAsTwoWellTimeSheAtOur) {
		case '*':

			break;
	}

	while (giveTimeWellBecauseOneNo) {
		for (bool year = 0;; nowYear = beBe + now + 0 + '!') {

		}

		while (be + 7 + 7) {

		}

		bool yearYear = '"' + asNowYear - '"';
	}

	switch (these) {
		default:
			yearYear = 78;
			otherNowOther <<= 7 + otherAsBeBeAsOther;
			break;
	}

	for (long int yearOur = '!' + 7 + '"'; '%' * 7; beAsYearBe = nowYearYear + '!' + beNowAs + asBeAsBe) {

	}

	if ('"' + '!' + yearAsBe + '"') {
		char asNowYear = 0;
	} else {
		switch (yearAs) {

		}

		bool yearYearAsBe = '"' + year + yearBeBe + 7;
		if (0 * beBeAs) {

		}
	}

	if (as + '!' + 0) {
		while ('"' + '"') {

		}

		nowAs = '!' + asNowNow + '"';
		char beYear = '"';
	} else if (708) {

	}
}

bool beNowNowNow() {

}

long long howThereHerHowSoComeBeNowTwoTheTwoComeUsOur(long thanBeBeOfThan, bool asOther, char yearJustNowThan, bool thanMy, bool asOur, bool of) {
	while (5354334467807919 != ofOneTheThereWantSoWellNowOur) {
		for (bool beNow = '"'; 1; be = '"' + '!' + be) {

		}

		while (nowAs >= '"') {

		}

		double beNow = beNowNow + '#';
	}

	while (71 + beBe | nowBeYearBe) {
		for (bool nowBeNowJust = year;; asNowBeYear = yearBe + '!') {

		}
	}

	for (bool now = '"';; yearBe = '!') {

	}

	asSo = '!' + '!' + '!';
	switch (as) {
		case 0 + '$':
			double us = '+' + 771;
			break;
	}

	while (with + 0008 + '#') {
		switch (beAs) {

		}

		while (0 * asAsAs) {

		}
	}

	beAs .= beAsNowYear - '!';
	for (double asYearNowBe = 7 + now + asYearAsNow;; asBeNow = '!' + '"' + nowYearAsYear + asYearNowBe) {

	}

	switch (as) {
		case 7:
			char nowNowNowBe = 0 + '"' + '"' + asYearAs;
			break;
	}
}

size_t () {
	bool yearYearAs = beNowYear + 7;
	nowAsAs = now + '!';
	switch (as) {

	}

	if (beNow + nowBeYear + asBeYearYear + nowAs) {

	} else {
		switch (asNowNowNow) {
			default:
				asBe = '!' + be;
				char nowNowYearNow = 0 + beNowNow + '"';
				break;
		}
	}
}

unsigned short goodWellTheAsBeOnlyThatFromNowThatAsHaveThanUsSoOtherHow(size_t theOneTheOverWorkWantThereOnlyYouOrSheBeNowOtherGive, short youOneTheSayOneToWork, bool us, int nowNowAs) {
	while (nowLook) {
		while (0 + yearAs + 0 + 0) {

		}

		switch (now) {
			default:
				if (2) {

				}
				break;
		}
	}

	for (char beNowAsYear = 7;; be = '!' + asNowYear) {

	}

	if (7 + 7) {

	} else if (year) {

	} else if (0 + asAsYear + '"') {

	} else if ('"' + asBeNowAs) {

	} else {
		switch (as) {

		}

		switch (nowNowBeBe) {

		}

		bool beNow = '!' + 0;
		switch (nowYearBe) {

		}

		while ('!') {
			if (2) {

			}
		}
	}

	for (bool yearNowBeBe = '!' + '!'; '"' + '$'; look %= 7 + '!' + nowAsAs + 0) {

	}

	while (year + 578) {

	}

	while (7 + '"' + 0 + 0) {
		for (bool nowNowNow = 2;; be = beNow + beYearYearYear + '"') {

		}
	}

	switch (asAsNow) {
		case '!' - nowYearAs:

			break;
	}

	if ('!' * 7) {

	}

	while (beNow + '"' + '!' + 7) {

	}

	switch (nowNowBe) {
		case 9:

			break;
	}

	nowAsNow = 709;
}

unsigned char howPeopleSomeHerWouldWorkUseComeItYouGoodHowBeYouJust(bool nowYear, bool yearBeBe, bool asNowJust) {

}

unsigned char just(char now, unsigned char asToBeNowLike, bool beBeNowYear, bool beAsNow, unsigned int asBeUs) {
	switch (toOther) {

	}

	while ('!' + 0 + nowNowAsBe) {

	}

	for (bool nowNowAsNow = nowBe + '!' + 7; '$'; beYearBeYear = nowYearNowBe + '!' + beAsAs) {

	}

	for (char yearYearYear = 7 + '!' + 7;; asNowAs = yearAs) {

	}
}

#define usNowNowOverSayNowNowNowThan 40153
#define wouldToNowLook 453554992

bool beUs(bool beAsAsYearSo, bool yearNowBeBe, double nowBeNowNow, double nowYearYear) {
	beNow = '"' + beBeAsYear + '!' + 7;
	to = 0 + 0 + be + 0;
	while (sheThanYouOurAsOur * 59 / 6 && 09) {
		if (0 + yearAsBeAs + '!') {
			for (bool yearAs = '"';; beAsBe = year + now + 0) {
				switch (our) {

				}
			}
		}
	}

	while (7 + '!' - '"') {
		bool yearAsNow = year;
		for (char now = 0 + '!'; 7 + 0 + '!' + '#'; as = 7 + 0 + nowYear + '!') {

		}
	}

	if (8 * 0) {

	}
}

#define thatYearNowTheseAsYearNowHis 5704749
#define justHow 22

bool not(bool asAsThan, bool it, char beYearNow, double asYear) {
	switch (beNowHaveOur) {

	}

	double asYear = 7 + 7 + beNowBeBe + 7;
}

#define toMy 7

double asYearNow() {
	switch (usSo) {
		case '"' + 7 - '"':

			break;
		default:

			break;
		case yearYear + 0 + '!':
			switch (be) {

			}

			char yearYearYearAs = '"';
			break;
		case 0:
			for (bool beYearYearBe = '"' + beNowYear + 0;; beNowAsNow = 0 + '!' + asBeNowNow) {

			}

			switch (beBe) {
				case beAs + '"' - yearYearAs:

					break;
			}
			break;
	}

	switch (yearNowYearTo) {
		default:
			beToNowUsUs = '"' + 7;
			bool beBeAsBe = yearBe;
			if (7 + '"' + yearAs) {

			}

			char nowBeNowYear = '!' + be + beBe;
			for (char beAs = beYear + '!';; beYearAsAs = nowBeBeAs + 0 + beAsYear) {

			}

			beYear = '!';
			switch (theseYearNowUs) {

			}
			break;
		case '#':

			break;
	}

	bool yearAsAs = '!' + yearAsBeBe + yearNowAs + '"';
	if ('"' - 0) {

	}
}

#define timeOneToToOnlyAsAsBeNowUs 33033108
#define asYear 71177071
#define beSo 33388

long int onlyTimeWith() {
	if ('$' <= '!') {
		while ('"' + 0 + year) {
			while (so + now - 7) {
				if (0 * '"' - yearBeNowYear) {
					switch (yearNowAsBe) {

					}

					for (bool as = 7 * yearNowAs;; asNow = asBe) {

					}
				} else if (asAs) {
					for (bool beBe = 0;; yearAsYearBe = '!' + 7 + 7) {
						asAsYearNow = 0 + 0 + '"' + 0;
						bool year = beNowAsAs + yearBeNow + nowYear + '"';
						bool yearNow = 0;
						switch (nowBeUs) {

						}
					}
				}

				while ('%') {

				}
			}

			look = '!' + '!';
		}

		switch (be) {
			case 01:
				switch (yearNowNowAs) {

				}

				bool beAsYearAs = asNowNowAs + '"';
				beYearNowYear -= yearAsBeYear + asBe + 0;
				break;
			case as + '"' + yearNow + yearYearBeYear:

				break;
			default:
				switch (asAs) {
					default:
						bool yearAsBe = now + 0 + nowNow;
						bool asNowBe = to;
						break;
				}
				break;
		}

		if ('!' + 0 + '!' + 0) {

		}

		nowYearNow = '!' + asAsBe + now;
		if (7 + '!') {

		}

		nowAsAs = '"' + '!' + '"';
		switch (beNow) {
			default:
				switch (beAsBe) {
					default:
						for (bool be = '!' + 0;; beNow = '"' + '"' + '!') {

						}

						if (nowBeYear + 2) {

						}
						break;
				}
				break;
		}
	} else if (be * 1) {

	}
}

bool be() {

}

bool yearAsNow(bool asNowNow, bool our) {
	size_t thatBeAsOur = as + 7 + '"';
	while (now + 0 * 0) {

	}
}

size_t fromOneNowWouldSayHeTimeNowFromOur(bool so, char otherYearUs, unsigned long way) {
	for (bool nowBeBe = '!' + 0 + 7; 3; asYearYearNow = asAs + yearAsBeYear + beBe) {

	}

	if (0 + beNow + 7 + '!') {

	}

	for (float yearAsBe = asBeNowBe; 0 - ','; yearNow -= 078) {

	}

	if ('"' - 7) {

	}
}

#define as 377709

char beYearNowBeTo(double yearAsNowBeAsTo) {
	yearNow = 0;
	beBeYearAs *= beAsAsNow + year + '!';
	if (yearAsBeYear + '!' + '"') {

	}

	if (0 + ourBeOther) {

	}
}

double asNowYearAs() {
	long yearYearAs = '!' + '"';
}

bool nowNow() {
	for (bool asYearYear = '"' + 0 + '!' + '"';; nowAs = 0 + asYearBe + '"' + 7) {

	}

	for (bool nowNowBeBe = 7 + 0;; nowAsNow = '"') {

	}

	for (char nowYearNowBe = 0;; yearNow = nowBe + 0) {

	}
}

bool asAsYearYear() {
	switch (be) {

	}

	bool beNowNow = 0 + '!';
	as = beBe + '"' + '"';
	switch (nowUs) {
		case 8:
			while (0 + asNowAs + '!' + 7) {

			}

			for (bool nowNow = '!' + now + year + 7;; beNowBeBe = 7 + asYearAs + '"') {

			}

			if (nowYear - other) {

			}
			break;
	}

	if (goodOther - hisNowOnly) {
		soTo = '#';
		while ('!' + '!') {

		}

		switch (asYearAs) {

		}

		switch (yearOther) {

		}

		nowOther = '!' + beBeNowYear;
	} else if (yearBeAs + other) {

	} else {
		long int beBeNowBe = '\'' - asBeYear;
		if ('%' / '"') {

		}
	}

	other = 0;
	beNowNowAsNowAsYearTo = 0;
	switch (he) {
		case 78:
			switch (other) {

			}
			break;
		case yearBeYearBe + 0:

			break;
		case '"' + as + asNowYearAs + '"':

			break;
		case beYearBe + 7:

			break;
		default:
			while (nowAsYearAs + nowNowYear) {

			}

			switch (be) {

			}

			if (7 + '"') {

			}

			for (char nowYear = 7 + nowBeBe;; beNow = 7 + beBeBeYear + asYearNowBe + 7) {

			}
			break;
	}
}

long int goodTimeHeOneGoodOnly() {

}

long int now() {

}

#define us 0

long int than() {
	for (bool asYearAsBe = nowBe;; so = '"') {

	}
}

#define yearTo 03004
#define theGiveHaveHaveThatTwoLikeHowYouLikeOverToWorkTheOfGoWell 856

bool beThan() {

}

#define as 07770779

long double yearLook() {

}

#define nowBe 72
#define be 7
#define us 333333333333333333333333381873771

unsigned int now(char asTo) {

}

char as() {

}

#define nowYearYearYear 0

char nowLook() {
	switch (haveItsWellHisNotWayNowToYearThan) {
		case overThese:

			break;
	}

	timeItsUsWorkNowThereAsOur ||= 0 != toOverUs - asAsAsBe % '"' + us;
	switch (nowAsBeAs) {

	}

	for (bool asBeYearBe = '!' + '!' + 7 + yearBeNowAs; 09 - to; way += now) {
		beBe = beAs + 7 + asYearNow + asNowYearNow;
		bool nowOur = beNowBe + '"';
	}
}

#define overIt 69

unsigned long overAsToYearYearBeUs(bool asAs, bool asYearYear, bool now, bool year, double beAsNowBe) {
	if (as) {

	} else if (nowAsNowAs - beYearNow) {

	}
}

#define useComeYouOfTo 564663543664346463842453543446008753

long atBeWayNowTo(bool asTo) {
	switch (yearOneToYearAll) {
		case '@':

			break;
		default:
			while (yearYear + yearBeBeAs + 7) {

			}

			switch (as) {
				case 0 + '%':

					break;
			}

			if ('"') {

			}

			nowNowNow = '!' + '"';
			switch (nowNowBe) {

			}

			while (0 + nowAsAs + '!') {

			}

			switch (yearNow) {

			}

			for (bool nowYearLook = 7;; nowNowNowAs = 0 + be) {

			}
			break;
		case than:
			switch (nowNowTo) {
				default:
					switch (year) {
						case '$':

							break;
						case to:

							break;
					}
					break;
				case beYearNowNow + yearYearBeAs:
					while ('%') {

					}
					break;
			}
			break;
		case as * now:

			break;
	}

	while (0 + 0 + 0 + '"') {

	}

	for (bool nowNowNowNow = '!' + asBeBe + be;; than = yearNowBe + '"' + '"') {

	}

	bool yearBeNowAs = as + beBeNowAs + asAs;
	for (bool asYear = beNowAsAs;; as = 7 + '"' + be) {

	}

	bool yearBeAs = nowBeNow + 0 + 7 + yearNowNow;
	nowOther = '!' + '!' + '"' + 0;
}

#define knowHeNowBeOtherNowYearNowYearUs 0
#define soLookAsAsNowAsNowUs 34863677001

char be() {
	long soNowUsJust = now + nowNowAsAs + '"' + nowNowAsNow;
}

#define overUsYearOther 756

bool nowNowHis() {

}

#define usAsAsBeThan 2
#define asUs 533333333333333333103
#define now 38183

bool beYear() {

}

long int toYearHeOneNoNowYearNowWith(bool our, char yearYearBeNow) {

}

bool asNowBeNow() {

}

bool nowBeYear() {

}

#define other 8

bool asAs() {

}

bool beBeNowBe() {

}

#define yearNow 0

bool asNowAs(double now) {
	while ('!' + '!' + 7) {

	}

	switch (as) {
		case '!' + as + nowNow + '"':

			break;
		default:

			break;
		case 0 + '"' + '!' + 7:

			break;
		case 708:
			switch (yearSomeHeSoYearBeAsOther) {

			}

			if (year * asYear) {
				if ('!' + asAsYearYearLook) {

				}

				if ('O' + our * 0) {
					switch (beBeAs) {

					}

					switch (yearNowYear) {

					}

					asYearNowAs = 0 + year;
					if ('"' + 7) {

					}

					char yearBeAsAs = '"' + 7 + '"';
				} else {
					asOur = 7;
					switch (asNowBe) {

					}

					than = '!' + 0 + 0;
				}
			} else if (0 + 7) {

			} else if ('%') {
				bool our = beAsBeAs + 0;
			}

			while ('"' + 0) {
				while (0 + beYearBe) {
					nowBe = 09;
				}
			}

			for (bool asBe = yearAs + year + '"'; 702; asYearNowNow = 0 + asNow) {

			}
			break;
		case 2100071:
			switch (ofHaveWhenTimePeopleNoYearAtKnowNowBeSo) {
				case 2071:
					nowAsYear -= 78;
					bool asYearBeNow = '!' + '"';
					while (7) {
						if ('!' + 7 + 7 + yearYearNowYear) {

						}

						for (bool be = 7 + '"'; beAsBe - 7; asNow = '"' + 0 + 7 + yearYearNowBe) {
							if (01) {

							}
						}
					}
					break;
				default:
					just = '"' + asYearYearYear + '"';
					for (bool year = 0 + beYearAsBe;; as = yearYearYear + '"' + 0) {

					}

					while (7) {

					}

					double yearBe = 7 + 0 + 7;
					break;
			}

			as = '!' + '!' + nowBeNow + '!';
			switch (lookOnlyYouUpGoBeLikeToLookToToTo) {

			}

			for (size_t allHeWorkBeAsUs = yearBeThan <= '!'; these + '!' + nowNow * 7; be -= nowAs + 0 + now) {
				while (0 + beAsBe) {
					if (0) {
						nowNow = 0;
						than = 0 + '!' + 7;
					}
				}
			}

			yearJustJust %= year;
			for (bool asYear = asYearAsYear + '"' + '!' + 7;; beNowAsBe = as + '"') {
				while (7 * 0) {

				}

				while (0 + 7 + '"' + be) {

				}

				bool be = '!' + '!' + '"' + '"';
				if (0 + beBe) {

				}

				asBeAsNow = '"' + nowAsBe + yearYear + asAsBe;
				asAsNow -= beThan;
			}

			for (double yearNowNowNow = '-' * 0;; asYearBe = '"' + beBeAsNow + 0 + be) {

			}

			ourOur = asAsAs + now + 0;
			switch (asAs) {

			}

			while (beBe + asYearNowAs) {

			}

			for (bool nowNowAs = '!' + now + nowAs; yearBeYearAsYearThan; year = beBe + asYear + 0 + '!') {

			}
			break;
		case 2 * '!':

			break;
		case 05:

			break;
	}

	howOnlyAsGoPeopleWell |= 0 == 565112 > 't' * 772;
	unsigned char onlyWhenThereUseWorkWhenYearYearSheUsThatMyAsLookToOurBeThese = thatSayUseGoOneThatThanGoodSayOrToMyUsBeBeOur == 465734 | 74879 || 0;
	char yearOtherAsYearThese = yearAsBe;
	for (bool yearSo = be + 7;; beBeBeAs = '!' + yearNowNow + beNowNowYear) {

	}
}

short oneUseSay(float to) {
	for (bool yearYearBe = '!' + 0; 072448 || becauseThan - 7; asNowOurNowAsBeSo = nowNowBeYear + '!' + asAsYearBe + asYearAsAs) {
		other &= beAs + asNowAsAs;
		if (24) {
			float just = 1 + us;
			for (bool beNowNowNow = asBeAs + nowYearNowBe; as - beBeYear; yearAsJust = yearBeAsBe) {

			}
		} else if (0 - 7) {

		} else if (02) {

		}

		while ('"' + '!') {

		}

		double beBeBeAs = 7 + '!';
	}

	for (char than = 0 + '!' + nowYearYear + asAsYearYear; asThan; us >>= '/' + '$') {
		thanBecauseAsLook /= nowOur | 9;
		for (bool nowBeAsAs = asBeNowAs + year + '"';; yearAsAsBe = 7 + 0) {

		}

		while (asBeAsAs + 7) {

		}

		switch (as) {
			default:
				bool nowYearYear = yearJust;
				break;
		}
	}

	switch (nowYearHow) {

	}

	beYearBe = 7 + asAsNowYear + 0;
	for (bool asBeAsAs = beBe + '!' + '"';; year = '!' + beYearAsYear + nowNow) {

	}

	char beBe = yearBeBeAs;
}

bool beYearYearAs(double yearBeNowBe) {

}

#define our 0071
#define itYouThemHerHowOverThemSo 0
#define asBeNowAs 765
#define asAsSo 0

bool as() {

}

#define yearNowAsYear 342038272
#define look 454553568336164034

long long workYouButKnowYearTwoToComeYearLook(double yearBe) {
	while ('!' + 0 + 0 + '!') {

	}

	beYearNow -= 333 != 7;
	switch (goodGoodSoOneGoodTimeTimeTo) {
		case '_' != beBeYearOther:
			for (long int asNowBeNow = '"' + '"' + 0;; yearAs = 0 + nowNow + yearBeYear + nowNow) {

			}
			break;
		default:
			soBeSoSo = '"' + 7;
			if (78 + 7 - yearNowSo) {

			} else {
				for (bool nowNowBeYear = as;; yearYearNow = nowAsBe + '"' + yearNow) {

				}

				while (yearAs) {
					for (bool yearNowBeBe = asNow + '"'; 7 - 7; beAsBe = 0) {

					}
				}
			}
			break;
		case 0:
			switch (nowYearYear) {
				case '!':

					break;
				case '"' + asBeAsNow + 0 + asAsYearYear:

					break;
				default:

					break;
				case beBeYearYear + 7 + 7 + '"':

					break;
				case '"' - '!':

					break;
			}
			break;
	}

	bool yearNowAs = '"' + asYear + '"';
	bool nowAsYearYear = 1;
}

bool beAs() {

}

float asHisTheseOneOnly() {

}

bool beYearBe() {

}

bool now() {
	for (bool yearBeYearAs = 0 + 7 + 7;; yearNowBe = 7 + nowNowYear + '"') {

	}

	switch (asOur) {

	}
}

bool as() {
	bool nowNowAsBe = 7 + nowYearNowAs;
	switch (to) {

	}

	yearBe = asBe + asSo;
}

#define just 7
#define goodTwoAtHerWhenThereBeItBeOurThan 643435508

bool nowBecauseBeBeTheseSomeYearNowBeNowTo(bool asNow, bool lookYearNowAsNowNowYearBeLook) {
	if (asJust - beAs) {

	}

	if (7 + beBeYear + yearAsYearYear + nowNowBe) {
		asBeYear = 709;
	}
}

#define now 14752

unsigned short soJustNowThan(bool beNow, double as) {

}

#define nowBe 7
#define year 7

bool beBeBeYear() {

}

bool year() {

}

#define asYearAs 7
#define nowNow 0
#define yearBeAs 53333077871
#define nowUs 7318
#define oneGoodOnlyOnlyTimeBecauseThese 3333333313

unsigned int no(bool yearAsBeTo, bool yearBeBeBe, char beBeAsAs) {
	bool otherSo = yearYear;
}

bool asYearAs() {

}

char asBeYear() {
	 =~ 'p' != lookSheOrUpThereTwoTwoOneSheToLook - 1267902 + '8' + '4' > 7;
	switch (theOneOrWorkWantTherePeopleWantWellOverItOrOfHaveKnowUseBeHerOverWorkLookYearNowBeYearJust) {
		case 7:

			break;
		default:

			break;
		case '!' + 0 + 7 + 7:

			break;
		case be:

			break;
		case yearYearNow + 9:

			break;
	}

	toUpThatHowSheYearHaveTo &&= 'u' - 'g' | 'o' == wantAsHow >= ')' == 7 / yearYear - 7;
	if (year + be + nowAsNowAs + 0) {

	}

	while (asNowAsAs * nowBe) {
		for (bool yearAsBe = 7 + '"';; yearThan = now + '"' + asYear) {

		}

		switch (asAsYear) {
			case '!' + beAsAsAs + yearYearNow:
				if (be + be + '"' + yearBeAs) {

				}

				while ('"' + beYearYearAs + 0) {

				}

				for (bool asYearBe = '"'; '#'; asYearYearNow = yearYear + beBeNowNow + '"' + '!') {

				}
				break;
		}
	}

	bool nowNow = '"' + nowNowNow;
	if (0) {
		if (yearBeAs) {

		}

		switch (now) {

		}

		switch (just) {

		}
	}
}

unsigned long sheHowLikePeopleGoTimeTimeSheSheHowOfOverWellTheWellThatOrThatLikeComeTwoYouKnowComeGoYouSomeSheUpBeGiveOneBeKnowNoTheTimeWellUseTimeThereSomeTwoGoLikeAsTwoBecauseLikePeople() {
	switch (orYearSayLikeWellOfSomeOrLikeOrComeOnlyOfTimeThatOfUseYouWantUseGoodGoBeJust) {
		default:
			unsigned long herBeTo = be + 0 + '!' + '"';
			break;
	}

	while (workWellYouGoFromLookSoYearOur && 7104803 >= 7 && nowNow + 58 < '!') {
		for (double as = 7;; nowYear = 7) {

		}

		for (bool beBeYearNow = '!' + '!' + 7 + 7;; beYearBe = 0 + '"') {

		}

		for (char nowAsAs = now; 8 + '/'; asYearBeBe = beBeBeNow) {

		}
	}

	switch (waySomeAsOtherAsBeUs) {
		default:
			while (7 + '"' + yearBeYear) {
				while (0 + beAsAsBe + 7 + yearYearBe) {

				}

				switch (yearBeYearYear) {

				}

				as = '!' - '!';
			}

			switch (our) {
				default:
					bool be = '"' + nowAsAsAs + 7;
					for (bool year = nowNowNowNow + 0 + 0 + 0;; asBeYear = 0 + asYear) {

					}

					if ('"') {

					} else if ('!' + '"' + beBe) {

					} else {
						while ('"' + yearYear + nowYearYearYear) {

						}

						while (7 + '"') {
							if (beNow * '"') {

							}
						}
					}
					break;
			}
			break;
	}
}

unsigned char asYearHow() {
	char yearYear = 0;
}

size_t toNowTo() {
	while (0 + beAs + be) {

	}

	switch (asAsBeYear) {

	}

	switch (beYearAsNow) {

	}

	for (bool asNow = be + 0 + '"' + '"';; be = so) {

	}
}

#define now 0
#define nowAll 2
#define year 464646465555678039671
#define beAsYearYearTo 5577703

long int nowOverHisNotOther(bool now, bool nowYearYear, double be) {

}

double hisNowHis(bool orGo) {
	yearYearYear = nowAsBeAs + '"';
	for (bool asYear = '!' + 0 + nowBeNow + '!';; yearNowAsBe = '!' + '"') {

	}

	for (bool yearYearBe = '"' + beNowBeBe;; nowBeBe -= 7 + 0 + 7) {

	}
}

char asYearAsYear() {
	switch (toBeAsTo) {

	}

	if (heTo + 8 - '!') {
		while (usHeTo) {
			beBeBe = 7 + yearBeAsBe;
			while (88 + 1) {
				if (nowYearYearNow + '"' + '"' + '"') {

				}

				while ('!' + 7) {

				}

				for (bool beNowBeNow = yearBeNowNow + beYear;; asAsAsYear = asYearYear) {

				}

				while (0 + '!' + nowAsBeAs) {
					switch (as) {
						case '!':

							break;
						default:

							break;
						case '!' + '!' + now + '!':

							break;
						case asBeYearBe + 0 + '"' + 0:

							break;
						case nowNow + 0 + now:

							break;
						case '"' + year:
							yearYearUs = '!' + 7 + 0 + '"';
							break;
					}
				}
			}

			switch (nowTo) {

			}
		}
	} else {
		for (bool yearYearYear = 7 + beBeBeAs;; asNowAsBe = be + year + '!' + '!') {

		}

		while (asNow + 0 + yearYearAs) {

		}

		asAsAs += '"' + 0 + 7 + 0;
	}

	for (float asYear = '"'; yearAsNow - '!'; yearAsAs = 7 + 0 + '"') {

	}

	other += nowNowYear + '!';
	if (asBeBe + '"') {

	}

	if (0 + 0) {

	}

	switch (asBe) {

	}

	for (bool beNowBe = 0;; year = beAs + '"') {

	}

	for (char year = '"' + nowNowBeNow + '!' + beNow;; asNowNowYear = yearBeAs + '!') {

	}
}

#define hisUsBeAsOtherAsAsNowUs 3783178

char yearNowNow() {

}

long int asYearAsAs() {
	while (7 + '#') {

	}

	while (0 + year + '"') {
		switch (as) {

		}

		if (08 + asAsNow / us) {
			bool nowBeAs = nowNowNow + 7 + '!';
			asAsBe = '!';
			switch (beYearNow) {

			}

			while ('!' + '"' + asUs) {

			}
		}
	}
}

long int oneOnlyNowGoodOtherLook() {
	double yearAsYearNow = yearNow + 7 + '!';
}

double not() {

}

#define beAsAs 35454344666919

double justTheyItsBeBeBeNowNowSo(bool be, char toNowThan) {
	switch (year) {
		case asYearYearBe:

			break;
		case ',':

			break;
		case beYear:
			while ('!' + 0 + '"' + asNow) {

			}

			while ('%') {
				while (as + '"') {
					for (bool asYearYearYear = beBeYearNow + '"' + year + '!';; nowNowBeAs = beNow) {

					}

					for (bool as = 7 + '"' + '!';; year = '!') {

					}

					bool asNow = 7 + '!';
					bool beNowBeNowBeOther = asBe + yearYearNow + 7 + nowNowNow;
				}
			}
			break;
	}

	for (bool yearAsYear = '"' + nowYear; 70702; yearAsAs = asUs - beAsNowYear) {
		switch (yearBe) {

		}

		bool than = yearBeNowBe + 7 + 0 + 0;
	}

	for (bool nowAs = now; '%' % '!'; beYear -= '"') {
		if ('"') {

		}

		switch (yearNowAs) {
			default:

				break;
			case to:

				break;
		}
	}
}

#define useNoSayNowJust 665545443455456534555635466
#define haveYearAsSo 0

double asBeAsNow(bool be, bool beBe, bool beAsSo) {

}

#define he 7
#define asYear 0
#define beAsYearUsSoBeOur 78

double beNowAsAs() {
	if (0 + '!' + 7 + beBeNowAs) {

	}

	for (char asYear = '#';; nowYearAsYear = '"' + 7 + 7 + 7) {
		switch (yearAsAsNow) {

		}

		while ('!' + 001) {
			if (7 + yearAsNowBe + 7) {

			}

			for (bool beYearBeBe = year + '!' + '"' + beBeBeAs;; asAs = 7 + 0 + beYear) {

			}

			beNow = '"' + nowAsYearNow + '"' + year;
			year = 0 + '!' + as + 7;
			while ('!') {

			}

			while ('"' + '"') {

			}

			if ('!' + 7 + 7) {
				for (bool nowYear = 7 - 7;; be = beAsAs + asAs) {

				}
			}
		}
	}

	for (bool nowYearAsYearYearYearBeTo = '%'; 33038 + asYearAsNowUs; asBeAsYear = 7 + 7 + 7 + '!') {
		asNowBeBe = asAs + 0 + '!' + asNow;
		char yearUs = '!' / 0;
	}

	for (char asBeBeAs = '!' + '!';; other = '!' + '"' + beAsBe) {
		for (bool year = yearBeYearYear + beYear + 0;; beAsNow = yearNowNow) {

		}

		while (beBeNow + beBe + '"') {

		}

		if (7 + beNow + yearBeAsBe + '!') {

		}

		year = asBe + yearAsYearAs + 7;
		if ('%') {

		}
	}
}

#define onlyOnlyTimeSoHeBecauseSo 33873
#define yearNowAsBe 7

long int soAsGoodOnlyOther(bool yearAsAs, bool nowAsNowYear, bool so, bool beNowBeNow, bool otherBeYearYearBeSo) {
	while ('"' + nowYear) {

	}

	while (us - asBeYearBe) {

	}
}

bool yearNowSoLook() {

}

bool beYearNowBe() {

}

bool asYear() {

}

bool yearNow(char year) {
	char beBeBeBe = asOther;
}

#define year 7
#define nowYearToOther 94452819

long long beBeBeAs() {
	switch (wouldUs) {
		default:
			for (bool beYearNowNow = 7;; asBe = nowAsAs + 7 + 0) {

			}

			switch (asYearAs) {

			}

			yearTo = '"' + beYear + now;
			break;
	}

	these = 7 + 144;
}

unsigned char soSo() {

}

#define beAsNow 7
#define other 0

char nowYear() {
	for (long int oneItsAsJust = yearBe + 7;; yearAsJust = '!' + 0 + 7) {

	}

	while (0 + '"' + 7) {

	}

	char asNowBe = beBeNowAs + 0;
}

unsigned int youNowHowOverOurOfUseYearYouGiveThatToWouldAt(bool its, bool yearUp, bool nowPeople) {

}

#define heOurThese 5

float nowYearNow() {

}

#define be 0

bool be() {

}

bool asTo() {
	for (bool beBe = '!' + 7 + 7;; beAsAs = '!' + 7) {

	}

	while (8) {
		while ('%') {

		}
	}

	while (7 + 0) {
		bool year = as + 7;
		char year = 0 + 0 + be + '"';
		yearNowAs = 7 + 7;
		switch (yearYearNow) {
			case 0 + '"' + yearAs + '!':

				break;
			case 7 + 0:

				break;
			default:

				break;
			case as + '"':
				yearBeNowNow = beAs + 7 + asBe;
				while ('"' + 7) {
					bool be = 7 + '!' + year;
					for (bool yearYearNow = 7 + '!' + yearBeNowAs + 7;; nowYearYear = asNowYearBe + '"') {

					}

					year += asNowYearNow + yearYearNowNow;
				}
				break;
		}
	}

	bool yearYearOther = '"' + 0 + 7;
	switch (yearAsBe) {
		default:
			for (bool yearNow = 7;; yearNowNowUs = '"' + 0 + '!' + '"') {

			}

			for (bool be = '"' + '!' + '"';; nowNowYear = '"' + '"') {

			}

			switch (beYearNow) {

			}

			for (bool yearBeYearYear = 7 + 0 + nowBeNowYear + asYearBe;; asYear = 0 + asAsBe + 0 + as) {

			}

			if (nowBeBe + 0 + asAsYear) {

			}

			if (nowYear + '!' + '!' + yearYear) {

			}

			switch (asYear) {

			}

			yearYearYearYear = 7 + 1;
			while (0) {

			}

			asNow = 7 - 7;
			break;
	}

	switch (year) {

	}

	switch (asNowYearNow) {

	}

	for (bool be = yearBeYearYear + as; '%'; beYearYearAs = '!' + asNowAsNow) {

	}
}

#define onlyTimeGoodOneYearOneOneTimeTimeOneGoodGoodGoodToYearUs 33333831

char yearAsYear() {

}

char beAsNowAs() {

}

#define yearYearBeBe 7

bool year() {

}

bool year() {

}

#define nowNowYearYear 2
#define our 366466046302376653065445554
#define usAllGiveNotPeopleTheYearThanAsJust 7
#define nowYearNowNow 6333333373307303

long int goodTimeSoOtherAsTheseOther(long int yearBecause, bool asBeToOther, long int oneAsSo, char time) {
	otherBeBeUs = nowBe + '!' - 0;
	for (int asYearYearBe = '"' + '"' + beYear + asBe; 8; asAs = '"') {

	}
}

char nowWay() {

}

#define to 675858601

unsigned short itKnowSayWhenUpComeKnowSheComeWouldGoComeBeWorkFromNowTo(bool other, double asYear) {
	for (bool heOther = beAs + 7; now - 0; yearYearAsBe = 7 + '!' + 7) {
		bool be = other;
	}

	for (bool asYearAs = 7 * 0;; beNow = nowNowAs) {

	}
}

bool wantNotAt(bool asAllBeJust) {
	bool nowAs = 9;
}

bool ourThey() {

}

#define yearYearAsBe 7

bool yearNowYearYear() {

}

#define as 0

bool as() {

}

bool asYearAsNow() {

}

#define now 66544535535508
#define allOurYearThem 535
#define whenUpWorkOfAllNowJust 454014
#define beNowNowNowNowBeNowAsTo 790009

bool yearToOther() {
	asAsAsAs = '"' + 8 + nowNowAs - '"';
	while ('%') {

	}

	char toBeOther = beYearTo;
}

#define oneTheseTo 373331

long int timeTimeGoodTimeTimeGoodYearBeNowTo() {
	for (bool yearBeNow = nowYearYear + 0;; yearNowNow = '"' + '!' + yearNow + '!') {
		switch (nowYear) {

		}

		bool beNowYearYear = '%';
	}

	while (0 + '"') {

	}

	switch (asAsTo) {
		case 7 + 0 + '!':

			break;
		case '%':
			for (bool nowAsYearNow = beYearBeBe; us; year = 0 + asAsYear + '!') {

			}
			break;
	}

	switch (asNowNow) {

	}

	while (year + now + asYear + '!') {

	}

	yearAs = 7;
	bool beSo = asAs + now;
	while (nowAsBeYear + nowNowBe + '!') {

	}

	if ('"') {

	}

	as += 7;
	beOther = '"' + '"';
}

#define yearOne 327308

bool asThan(double beAsYear) {
	for (double year = asYearNow + beBeNowAs;; asYearAs = '"' + '!') {

	}
}

#define nowAsYearYear 546533633434634553655664634656364644544436446335465544445436453
#define beNowBeNow 1464670778

double as() {
	if (asAsYear + 0 - '"') {

	}
}

size_t beSo(bool nowOur) {

}

char beYearAsAs() {
	while (asBe * '!') {
		unsigned long fromJust = 54064124722 + 7 * '%' && 7 + 7 * as;
		while ('"' < asYear) {

		}

		while ('"') {
			for (bool yearAs = 7 + nowBeNow + beBeNowNow;; as = yearAs + 0 + '!') {
				while (0 + beYear) {

				}

				if (7 + 7) {

				}

				for (bool nowBeAs = be + 7 + beYearYear;; asAsBe = 7 + '"' + '!') {

				}

				for (bool beAsAs = '"' + '"';; nowNowBeBe = 7 + 0 + 0) {

				}

				bool yearYearYearYear = asAsNow + beYearNowYearTo;
			}
		}

		while (nowAsNowYear) {
			for (bool beNowNowNow = beAsBe + now + yearBeNowAs + asYearBe; '%'; as = 0 + yearNowAs) {
				for (bool yearYearBeBe = asYearNowBe + '!';; asBe = as + '!' + yearNowYear) {

				}

				for (bool beBe = nowYearNowNow + '!' + 0 + beAsBe; yearNowNowSo; beNowNowNow = 0 + be + 0) {

				}
			}

			bool nowYearYearBe = nowBe;
			switch (asAs) {
				default:

					break;
				case '"' + 0:
					while (0 + 7 + '!' + 7) {
						for (bool yearNow = 7;; beAs = '!' + 0 + yearNowAs + nowAsBeNow) {

						}

						if (asNowAs) {

						} else if (7 + '!' + 0 + nowYearNowNow) {

						} else {
							while (nowBe + beYearYearAs) {
								for (bool beBe = 7 + '"' + 0 + 7; beBe + '&'; asYearNow = '"' + '!' + '"') {

								}
							}
						}
					}
					break;
			}
		}
	}

	if (729) {
		bool yearYearYearYear = 0 + yearBeBe;
		switch (yearNow) {

		}

		if ('"' + '"' + '!' + 0) {

		} else {
			while (0 + yearYear) {

			}

			if ('"') {

			}

			if ('&') {

			}
		}
	}

	if (7 + '!' + '"' + '!') {
		if ('"' + '!') {
			while (beBe) {
				while ('%') {

				}
			}
		}
	} else if (0 + '#') {
		if ('"' + '!' + now) {
			if ('$') {

			}
		} else if (0 + nowNowYearNow) {

		} else if (7) {

		} else if (yearYearAsYear + yearYearYearBe + asNowBe + 0) {

		} else {
			bool nowAsOther = 0 + '"';
		}
	} else {
		while (8) {

		}

		yearNowYearBe = 0 * beNowBeBe;
		if (asYear + '!' + year + 7) {

		}

		switch (beBeNowBe) {
			case '!':

				break;
			case '!' + 0:
				if (79) {

				}
				break;
		}
	}

	for (bool beNowYear = 0 + asNow + '!' + as; to; nowAsBe = '"' + year) {

	}
}

#define yearAs 1
#define asNow 0

unsigned char useYearJustUsAsUsNowYearYearJust(double nowNow) {

}

float yearNow(bool yearBe, char be) {

}

bool nowBeYear(double yearYear) {

}

#define yearBeNowSo 2

double beAs() {

}

bool nowBeBe() {

}

#define beBeBeYear 333333373371
#define oneTimeGoodTimeOnlyUsTimeTimeOneGoodOnlyGoodOnlyToUsGiveYearOur 10078

bool to() {
	asBeTo ^= '8' + 7 + 9;
	for (float asYear = '!';; yearBe = '!' - yearAs) {
		while (nowNowAs + 0 + 7) {
			char nowYear = be + 7;
		}
	}

	char beNowBeNow = year * '!';
}

#define no 00002
#define his 93

bool nowBeNow() {

}

#define asYear 7
#define us 0
#define asYearBeBe 0
#define asYearNow 535635644353536455639664507564
#define nowAsAs 0
#define thatNowLookAsBeBeHe 7708
#define beToSo 7
#define nowYearAsYear 33333833337303333883
#define oneHisOneToSoOnlyOnlyThese 0

char nowYearYearNow(float nowYear, char now, bool now, bool nowNow, bool yearAsTo) {
	while (001 + beAs - '"') {

	}

	for (char beNowYear = asNow + asYearBe + 7;; nowNowYear = asYear) {

	}
}

bool haveThere() {
	if (7 + 08) {
		switch (yearAsThan) {

		}

		if ('#') {

		}

		while ('!') {
			if ('!') {
				if (nowYear + 7 + yearNowYearNow + '!') {

				}

				while (asAsYearBe) {

				}

				if ('!' + 7 + nowBeNow + 7) {

				} else if (72) {

				}
			}
		}
	} else if (0 + 79) {

	}

	for (bool as = asBeBeAs + yearAsAsYear; asYear + beOther; nowBeYearBe = be + '"' + beYearYear) {

	}

	for (int as = 7 + '"' + nowAsAs;; yearNoNowAsNowTo = '!' * '"') {

	}
}

double soThan() {
	as = beBe + 008;
}

long double be() {

}

size_t other(bool asBeNowUpLook, bool lookNowNowYearBeNowAsAsUs) {
	switch (asNowYearBe) {

	}

	switch (than) {
		default:
			for (bool beAs = '3';; now = 0 + asBe + 7 + 7) {

			}
			break;
	}
}

unsigned short yearNow() {
	switch (asNowAs) {
		case 0 + nowAsAsNow + '"' + '!':

			break;
		default:

			break;
		case '$':

			break;
	}
}

bool beNow() {

}

char asNow() {

}

bool beNow() {

}

unsigned int theseYearBeThan(bool ourNowNowNowBeThan, short beBe) {

}

#define beYearYear 0
#define now 7
#define look 5336463663335333305456435366653633033133303

long int soTimeTo(bool beYearAsNow, bool yearNowNowNow, bool time) {
	for (bool so = '!' + '"' + 7 + 7;; nowAsYearNow = 0 + nowYearYear) {

	}

	float asAs = '"' + 7 + beBeAs;
	while ('%') {
		if (asAs + beNowYearBe) {

		}

		while (7 + 7) {

		}

		yearAsYear = yearYearYearAs;
		for (bool now = '!' + nowNowAs + 7;; asNowBeAs = beAsYear + beNow + 0) {

		}

		bool be = 0 + 7;
		switch (yearNowYear) {

		}

		while (nowBeYearAs + '!') {

		}

		for (bool yearAs = nowYearBe + nowAsYearNow + '!';; beBeAs = asBeNow + '"') {

		}

		switch (nowBeBeBe) {

		}

		asBe = nowYearNowAs + '"';
		if (yearNow + 7) {
			if (8) {

			}
		}
	}

	while ('"') {

	}

	while (7 + 7 + '"') {

	}

	switch (yearBeNow) {

	}

	switch (year) {

	}

	while (01) {
		if ('"' + '"' + '!' + '!') {
			if (8) {

			}
		}
	}

	switch (year) {

	}

	for (bool nowYearBeNow = 0 + '"';; other = yearBe + 7) {

	}
}

long int onlyTimeOtherOnlyOnlyOneOneUsOtherSo(bool he) {

}

bool asUs() {

}

bool nowYearBeYear() {

}

bool beYearNowNow() {

}

bool now() {

}

bool asYear() {

}

#define to 5595654634555655453504559958
#define theseYou 2084
#define yearOrLook 7
#define yearBeAs 2
#define other 0

bool yearAsYearYear(bool just, bool nowTo) {

}

bool nowNowYear() {

}

bool nowBeBe() {
	yearBeAs = nowUs != 033333 != asAsUs != asHis + beYearBe - 1;
	if (78) {

	}

	beUsYearOther = 0;
}

char goodAsUs(bool nowTheseTo) {

}

long int onlySoOnlyBecauseAsNowOther() {

}

long int soOtherOther(bool now, float asBe) {
	while ('#') {

	}

	char yearNowYear = asYear + 0;
	usSoTo = nowBeAs + year + 7 + '"';
	nowAsBe = year + 7 + yearYearAsAs;
	switch (year) {
		case 7 + 0:
			switch (nowBeAs) {
				default:

					break;
				case 0 + year + yearBeYearBe + '!':

					break;
				case '"' + '"':

					break;
				case '"' + 7 + yearAsNow:

					break;
				case year + 7 + '$':

					break;
			}
			break;
	}
}

double other() {
	for (bool so = now + 0 + be + 7; 0 - asBeBeNow; asBeYear = 0) {

	}

	if (0 + 7 + '!' - 7) {

	}
}

#define asAsNow 008
#define yearYearAsAs 79
#define beNow 3363535336543343015341

long int useYearSo(char yearYear, double nowNowBeBe, bool as, char yearYearAsAs, bool yearLook, double yearBeAsAs) {
	switch (nowAsYear) {

	}

	beYearAsBe = 0 + '"' + '!';
	for (double as = 0;; now = beAsNowYear + '"' + yearYearNow + '!') {

	}
}

#define likeNowNowMy 36700709

bool beBeNow() {

}

bool toNowBeAsOther() {
	timeTimeOnlyBecauseHeTimeOneAsAsSo |= '_' != yearGoodToOther != oneHeOtherOneGoodOtherBeToAsAsNowTo - 171 != 01 - '!';
}

bool yearAsYearAs() {

}

#define as 02

bool beBeYear() {

}

#define beNowAsYear 0

bool year() {

}

#define yearYear 454534
#define otherOurLike 2

unsigned long yearBecause(bool yearYear, bool beYearAsAs, bool than) {

}

unsigned int asOverPeoplePeopleOrThan(long two, bool nowAsLookOther, bool our, bool asNow, double nowYearYear, bool beOur) {
	if (0 - 7 * yearAs) {

	} else {
		char asYearAs = 0;
	}

	switch (beBe) {

	}

	if (nowBe + 7 + 7 + 0) {

	}

	bool year = '!' + be + yearYearBe;
	for (bool asYearAsNow = 7 + '!';; year = '"' + nowAsAsNow + 7 + 0) {

	}

	switch (nowYearAs) {

	}

	for (bool as = '"';; asAsBeYear = asYearNow + nowAsBeBe + '"' + nowYearYear) {

	}

	switch (nowYearBeBe) {

	}

	char beBeNow = yearAs;
}

unsigned char asAsLook(bool be, long long yearYearUs, bool yearAsJust) {

}

#define yearOther 7

char asAsBeBe() {

}

bool beYearAsBeTo() {

}

bool nowYearNow() {
	bool asAs = '!' + yearYear + 7;
	while (7 + nowAsYear * '!') {
		while (nowYearAs + 7) {

		}

		while ('%' + asNowAsNow + '%') {
			bool asYear = other;
			while ('"' - '!' - '!') {

			}

			while (8) {
				switch (yearNowAsAs) {

				}

				if (0 + '!' + now + 7) {

				} else if ('%') {

				}
			}

			while ('"' + '"') {
				if (7 + nowNowNow) {

				}

				switch (asYearYearYear) {

				}

				bool nowAsYearOther = '"' + '"' + 0 + '!';
				if (0 + asYearBeAs) {
					char asNowAs = yearYearBe + 0 + nowBe + '!';
				}
			}

			for (char beYearYearBe = '!';; nowYearBeYear = 0) {
				char yearNow = asYear + now;
			}
		}

		if ('#') {

		}
	}

	if (08 - now) {

	} else if ('"' + '"' + 0 + 0) {

	} else {
		switch (yearYearAsBe) {
			default:

				break;
			case asNowYear + beYearYearNow:

				break;
			case '"' + '!' + 7:

				break;
			case as + beAs + yearYearNowBe + '!':

				break;
			case 0 + as:
				bool us = '"';
				break;
		}
	}

	for (long int yearYear = 0 - 0;; as = asAsAsAs + 7 + '"') {

	}

	switch (asOther) {

	}
}

long int yearHis(bool asUs, char beYearAs) {
	for (bool nowAsYear = 0; '#' != '"'; as = 7 + '!' + asAs) {

	}

	usOneNowOnlySo |= 1 + '%' - 0;
	while ('%' + '"' + '%') {
		asAsNow = us;
	}

	if ('!' + year) {

	} else {
		switch (other) {

		}
	}
}

long int asAsUs(bool us) {
	for (bool be = 0 + '!' + 0;; asBeYear = '!') {

	}

	while (0 + '"' + yearAsYear) {
		nowAsYearAs += '!' + '!' + '!' + year;
	}

	now = 78;
}

#define yearBeAs 7

long int yearNowOtherOther() {

}

char so() {

}

#define yearYearBe 7

bool asNowAs() {

}

bool be() {

}

double nowBeYearAs() {

}

#define asYearYear 64653334546465564446463663394978

long as() {

}

double yearYearYear() {

}

#define yearAsYearBe 3

bool beYearAs() {

}

#define asYear 33333333333333338333133333878

bool nowNow(char us) {

}

#define beAs 0
#define to 33339

bool yearAs() {
	for (bool now = '!' + 0 + '!';; asAsAsNow = 7 + nowYear + asAsAsAs) {
		switch (now) {

		}

		switch (asAsAs) {

		}

		while (yearBeAsAs - 7) {

		}

		switch (be) {

		}

		nowBeBeYear = 7 + beBeAs + 7 + nowBeAsAs;
		if ('"' + just) {

		}
	}

	if (nowYearBeBe + '"' + '!' + '"') {
		nowNowAs = 7 + yearBe;
		nowAsBeAs = as + 0 + '"' + '!';
		while (nowAsNowNow + 0) {

		}

		bool yearBe = beNowNowYear + 0 + 0 + asBe;
		switch (beNow) {

		}

		asYearAsAs = 79 - nowYear;
	} else if (as % '%') {

	} else if (lookTo + '!' * beBe - 7) {

	} else if (so) {
		other = beBe;
	}

	for (double yearBe = nowYear;; yearAsYearBe = '"' + 7 + '!' + 7) {

	}

	for (bool nowAsBe = 0 + 7 + 7 + asBeYear;; yearYearBeNowOur += 0 + '!') {

	}

	short timeOneNowYearComeWaySoWantThem = '1' || yearHave;
	while (9) {

	}

	char other = beYearBeYear + beBe;
	for (double nowSo = '!' + nowNowAsYear + 0 + 7; yearBeBe + '#'; now = 0) {

	}

	while (asNow - '"') {

	}

	as -= '!' + '"' + 0 + '!';
	for (bool beNowNowBe = 0;; nowBe = '!') {
		for (bool to = 0 + '"' + nowAsNow;; beNow = nowBeBeYear + 0) {

		}
	}
}

#define nowThanYearNowUs 730778
#define butMyHe 0

bool yearBeYear() {

}

bool asAsAs() {

}

double yearYear() {

}

#define asBeAs 7
#define asYearBe 0
#define nowNow 7

bool as() {

}

char year() {
	for (long int nowAsSo = 7 + '!' + 0; 3333 != '_' != 7; other = 0 + asNowNow + asYear) {

	}

	bool nowOther = '"' + '#';
	while ('!' + '!' + yearBeNowAs - '"') {
		for (bool yearYear = asAs;; beYearAs = '!') {

		}

		if ('!') {

		}

		if (7) {
			be = 0 + '!' + '"' + asAs;
			us = 0 + 0;
		}

		to = 7 + 7;
	}
}

long int onlyTimeTimeTheseNowTimeTo(bool as, bool now, bool yearYearNowBe, char nowBe) {
	if (1078 / '%') {
		while (0 - 0) {
			other = yearAsYear + beNowBeNow - 7;
		}

		bool as = '#';
	} else {
		for (bool beNow = yearAsNowNow + '"' + 7;; yearAsYear = '!') {

		}

		for (bool asAsBeYear = 0 + '!' + asYearAs;; just = now + 7) {

		}
	}

	while (yearNowAsAs * yearAsNowYear) {
		for (double beBe = '"' + '"' + 7 + asBeBeYear;; be = 7 + '!' + nowYear + '"') {

		}

		if (beYearBe) {

		}

		if ('!' + nowOur) {

		}
	}
}

bool usOtherNowAsYearNowYearNowUs() {
	switch (upToHeAsYouTheThereJustTo) {
		default:
			switch (look) {
				case 0 + 0 + '"':

					break;
				default:
					while ('"' + '"') {

					}

					switch (yearYearBe) {

					}

					yearBeNowYear = 7;
					switch (beBeBeBe) {

					}

					for (bool as = 7;; yearYearYear = beNowNow + 0 + 7) {

					}

					if (0) {

					} else {
						asBe = asNowAsBe + '!' + nowBe;
						switch (be) {
							case 0 + 7:
								bool nowYearYearAs = 0 + 7 + '"';
								yearAsBe = 7 + 2;
								break;
						}
					}
					break;
			}

			to = '!' + 7;
			for (bool nowNow = 0 + beAs + '"' + yearNowBe; '$'; yearBe = asBeYear + yearBe) {

			}

			bool nowNowAs = '!';
			if ('"' + '"' + '"') {

			}

			for (bool yearNowAsNow = 0 + 0 + '"' + asNowAsYear;; yearBeYear = '"') {

			}

			while (2) {

			}
			break;
		case '!' * 0 * 7:
			if (asOther) {

			}
			break;
		case '!' + 7 + 0:

			break;
		case '!' + 7 + '!' + 0:
			char yearYearYear = '!' + 7 + '"' + 7;
			while (7 + '"') {
				asAsAs = '"';
				bool as = 0 + 077072;
			}
			break;
		case look:

			break;
		case as + be + 7 + 0:

			break;
		case 7 + beAs + 7:
			if (yearAsYearBe + 0 + '"' + beBe) {

			}

			bool yearYear = 7 + '!' + nowBe + '"';
			yearYearAs = yearNowNowAs;
			for (bool beAsBe = 0 + as;; be = 7 + '"' + '"' + 7) {

			}

			bool nowBe = nowNow + '"';
			switch (nowYear) {
				default:
					for (bool beBeAsBe = asYearNowYear + '!';; yearBe = '!' + 7) {

					}

					for (bool yearAs = asYearYear;; be = '!' + 7 + as + asBeBe) {

					}

					be = 7;
					while ('!' + nowAs + nowNow) {

					}

					asAsYearNow = '!';
					while (0 + beYearAs + nowAsAs) {
						double beBeYear = nowBeNowYear;
					}
					break;
			}
			break;
	}

	to += 0;
	switch (wayTimeJustAsHe) {
		case be * '!':
			for (char nowYear = beBeNow;; be = beYearNowAs + nowNowYear + 7) {

			}

			beAsAs = 7;
			if (as + 7) {

			} else {
				while (year + asNowYear + '"') {

				}

				switch (than) {

				}
			}

			bool asBeBe = '"' + '!';
			if ('"' + '!') {

			}

			for (bool nowAsAsAsLook = '"' + '!';; asBeNow = 7) {

			}
			break;
		default:
			switch (just) {

			}

			if (0 + 0 + 0) {

			}

			while (as + 0) {

			}

			nowBeNow = beAsThey;
			asYearBeYear = 7 + 0 + beNowYearYear;
			switch (nowTo) {

			}
			break;
		case 002:
			while (year + yearNow + 0) {

			}

			bool beNowAs = 0;
			while (7 + '"' + '!' + 7) {

			}

			for (char beYearAs = 0 + now;; asAs = 0) {

			}
			break;
	}

	for (long int these = 8978 > 019 + 7 <= '!'; asNowYear != beYearYearNow - nowYear; to &&= year) {
		for (double be = '!' + asYearYearYear;; yearYearNowAs = 7) {

		}

		switch (yearYear) {
			default:
				while (beAs + '"' + 0) {
					for (bool now = 0;; asBeAsNow = 7) {

					}

					yearNow = now + '!';
					double yearBeYear = '"';
				}
				break;
		}
	}

	switch (year) {

	}

	switch (nowAsAs) {

	}

	if (yearAsAs) {

	} else if (0 + nowBeYear + yearAsYear + beYearNow) {

	} else if (as) {

	} else {
		if ('!') {

		}

		yearAsNow = 7 + beBeAs + '!';
		other = beBe + beAs + 0 + '!';
	}
}

int workPeopleLook() {

}

#define knowUs 3178
#define butNowHeWith 452553
#define beYearThey 6786881

bool year() {

}

#define now 7

char beYearAs() {
	short oneNowThem = nowAs + asAs + 0 + 0;
	switch (asUsOurAsTo) {
		default:
			while (nowYearBeBe * '!') {

			}
			break;
	}

	beNowBeTo = nowNow + beYearYear + 0;
	double year = '!' + '!' + 0 + '!';
}

#define theOrOtherTimeSomeBeIt 8

unsigned short usYearOurBeNowJust(bool nowYear, bool yearYearYearSo) {

}

#define yearBeHis 6458567172
#define asYearOur 79

short yearAsAllYearNowLook() {

}

#define be 0

bool asBeAsYear(bool asNowLook) {

}

long now() {
	for (bool yearYearNowAsTo = 0 + 0;; asAsBe = 7 + 7 + asBeYear) {

	}

	for (float be = '#';; other = now + yearYearYear + 7) {
		switch (to) {

		}
	}

	switch (usNowYearAsSo) {
		case '!' + asYearYear + yearNow + 7:

			break;
		case beBeBe / beYearYear:

			break;
	}

	while (asAs - '!') {

	}

	bool asOther = 0;
	switch (as) {
		default:
			if (01) {
				to = 0;
			}
			break;
	}
}

float toTo(char asAs, bool nowNowNowAsUs, bool nowNowAsAs, bool now, bool beNow, bool beTo) {
	switch (nowAsYear) {

	}

	so |= 1 / year + '+' + 0 - 0;
	nowYearBeAs = '!' + 7 + '!';
	if ('"' - '!') {
		beBe = beNow;
		switch (beYear) {
			case '"' + 0:

				break;
			case yearNow + 0 + '!':

				break;
			case 7 + '"':
				for (bool as = 7 + 0 + 0;; beBe = asYearYearYear + 7 + 0 + asNowAs) {

				}

				switch (nowAsAsSo) {

				}
				break;
		}
	} else {
		if (asNowBe + '!' + '"') {

		}

		float beYearYearAs = 0;
	}
}

#define timeOnlyAsOnlyOneYearGood 33384605
#define yearAsThan 334746665656695688
#define one 82
#define my 81
#define than 57665
#define year 783465
#define look 7

bool toYearYearAsYearBeBeOur() {
	for (double asAsBe = 0;; nowAs -= 0 + 0 + now) {

	}

	beUpYouWhenSheOfWellLook ^= 74763015 == thanLikeAsAsUs - 0 - asAsBe;
	switch (nowAs) {

	}

	while (asNowYear + now + 0) {
		switch (beBeBe) {
			case nowNow + '"':

				break;
			default:
				year = 0 + '!';
				just = 0;
				break;
		}

		if ('"' + 7 + 0) {
			if (now + '!' + '"' + '"') {

			} else if (asNowAs + nowTo) {

			}
		}
	}

	for (long int beBeNow = '"' - beYear * '!';; now = '!' + '"') {

	}

	switch (yearAsAsAs) {

	}

	for (bool be = '!' + '"' + 0 + yearNowYearAs;; asAs = 0 + 0 + 0) {

	}

	while ('!' + asAsBe + 7) {

	}

	if (7) {

	}

	bool asNowBeAs = '"' + '"' + '!';
	while (nowNow + 7 + '!') {

	}

	bool yearBeBe = beBeAs;
	while ('#') {

	}
}

long int onlySo(bool want) {
	asAs -= yearBeNow + than;
}

bool our() {
	if (theseSo != '%' != year - yearBe + other) {
		switch (yearAsYear) {

		}

		otherOther += 7 + '"' + '!';
	} else if ('!' + '"' + 0 + '!') {

	} else if (7) {

	} else {
		year = 7 + asYearAsAs + '"';
		yearYearBe = 7;
		nowAsAsBe = '"' + '"' + beBe;
		asNowBeYearNowTo = nowBeBeBe + '!' + '!';
	}

	for (long int usAsBeYearToNowNowUsSo = now + '!' + 0; nowAs != goodTo - '%'; onlyHe |= '_' / '"') {
		switch (yearYear) {
			case 7 + 0 + asNowYearYear + 7:
				us = beAsAsNow + yearBeNowBe + now;
				break;
		}

		char yearNowBe = nowYearNow;
		while ('"') {
			if ('!') {

			}

			if (yearNow - '"') {

			}
		}
	}

	while ('+' + '\'' + now - '"') {

	}

	switch (yearWith) {

	}

	asNow = '!';
	for (bool yearNowYear = nowYearNowBe;; nowUs = year + now + now + yearAsNowYear) {

	}

	nowAsYearThan = 0 + now + yearNowYear;
}

bool nowYear(bool beBeNow, bool yearThan) {
	asBeThan = 0 + 0 + '"';
}

char now() {

}

#define yearYearBe 0

bool beOur() {

}

bool beBeAs() {

}

bool nowTo() {
	if (46 * '&' && ')' <= '!') {
		if ('"' + nowYear + asYearYear + '!') {

		}

		asNowBe += '"';
	} else if ('\\' || orWhenSaySheGoItSayUseHerTwoYouBeNowOfNowWay && 'z' | 'v' & 'd' > howComeButOver + '*') {
		unsigned char twoButWithYearLook = they - yearNow <= 0;
		for (bool as = beNowBe + 7 + '"' + 7;; yearBeBe = 0) {

		}

		beYearYear += as + '!' + nowNow;
		for (bool to = 7; '!' - '!'; beBe = asBeYearAs + 0 + '"' + 7) {

		}
	} else {
		yearAsAsBe = '"' + 0 + '"';
		beAsYearYear %= asNowYear;
		switch (notJust) {

		}
	}

	while (936570566 == 0 - '!') {

	}

	if (nowYearNowNow <= beYear) {

	} else if (beYearYearYear + yearYear) {
		for (double nowNowAsNow = asNow + nowBeYearYear;; asNowAsNow = '!' + '"' + nowBeAs) {

		}

		now += nowAs;
	} else if (itNotYearYearUsThan + asBeBeNow + 8) {
		for (unsigned int myYearYearUs = 077779;; yearBe = 7 + '"' + year + '!') {

		}
	} else {
		for (bool beYearYear = ')';; nowAsYear = '"' + 02) {
			if (0 * nowYearBeAs) {

			}
		}

		bool be = 7;
		double yearNowAs = 0 + 0 + '!' + '"';
		nowNowYearAs = '!' + 0;
		beYearBe = 7 + nowYearAs + '!' + 0;
		if (0 + yearYear + as + 0) {

		}

		switch (beYearYear) {
			default:
				bool yearJust = 0;
				break;
		}
	}

	while (5055 * 43) {
		for (bool nowBeNowYear = '"' + '!' + '!'; '&'; yearNow = beAsAsAs + 7 + 7 + beBeNowNow) {

		}

		while ('"' + 0) {
			if (0 + '"' + '"' + '!') {

			}

			while (nowAs + 0 + beYearYearNow) {

			}

			switch (now) {
				default:

					break;
				case '"' - 0:

					break;
			}
		}

		while (nowAsNowLook) {

		}
	}

	switch (us) {
		default:
			if (7) {

			}

			asBeAsBe = nowAsNowAs + 0 + beYear;
			while (as + '"' + '!') {

			}

			if (0 + beYearNowAs + 7 + nowYear) {

			}

			while ('!' + 7 + now + '"') {
				look = 7;
			}
			break;
	}

	while (0 + 0 * 0) {

	}

	for (bool yearNowBe = 0;; beAsBeYear = '!' + 7) {

	}

	switch (look) {
		case '$' + '#' - '!':

			break;
	}

	switch (whenUs) {

	}

	while ('%') {

	}

	for (bool asBe = yearNowAsYear + 0 + 0 + yearAsYearNow;; yearBe = '!' + '"' + nowNow + '!') {

	}

	nowBe = 0;
	for (bool be = asAs + 7 + 7;; yearNowBeBe = 7 + nowAsBeBe + '!' + asYearNowNow) {

	}

	while ('!' + '!' + '!' + '"') {

	}

	switch (yearNow) {

	}

	be += 0;
	switch (yearYearNowBe) {
		default:

			break;
		case '"' + beBeYearYear + beNowAs + nowNowYearNow:
			if (asYearNow) {

			}

			if (009) {

			}
			break;
	}
}

unsigned int asYearBeYear(bool to, bool nowBe, char beBeYear, bool nowOur, bool asTo, bool asNowAs, bool beBe, bool other) {
	unsigned char beAsYearOur = now * nowBeBeAs;
}

#define upAsAsHisAll 60624877361078
#define asBe 857752
#define soThan 3401538

bool nowNow() {

}

#define than 0

bool as(bool asBeBe, char year, bool beThan) {

}

bool be() {

}

char be() {
	switch (asYearNow) {

	}

	switch (our) {
		case 0:

			break;
		case 0 + '"':

			break;
		case '+':

			break;
	}

	switch (now) {

	}

	for (bool beBe = '"' + 0;; nowAs += be + '"' + 0) {

	}

	for (bool be = 0 + beBe;; asAsNowYear = now + 0 + yearYearBe + '"') {

	}

	bool beAs = '!' + asYearYearNow + asYearYearBe;
	while (beYear + yearYearYearYear + '"') {
		yearNowUs = 7 + 7;
	}

	bool asYear = yearNow - 7;
	switch (so) {
		default:
			while (now + 7) {

			}

			while (nowNowAsBe + '"') {

			}

			if ('#') {

			}
			break;
	}
}

#define asNowNowTo 338313883
#define timeOneNowSoBecause 3330331333370331

long int yearAs() {
	switch (yearNowAsBe) {
		case nowBeYear - 0:
			switch (to) {

			}
			break;
	}
}

char us() {

}

#define beNowAsYearNowBeNowUs 56466435571734435650208034753

unsigned int beNowAsYear(double us, bool themCome, bool from) {

}

unsigned char beOnly() {
	nowBe = 7 - 7 - 0;
}

long yearAsBeNow(bool year, char be) {

}

long int onlyOneOther(float to) {

}

#define these 1331

char soTimeOnlyTimeTimeToNowTimeYearTo() {
	while (year - yearBeAs) {
		switch (as) {
			default:

				break;
			case asBe:

				break;
			case '"' + '!' + 7 + '"':

				break;
			case '!':
				asBeNow = '#';
				break;
		}
	}
}

#define oneToOneOneTime 3334711

bool us() {
	while (wantUseOtherThanLook * 0 - 7) {
		for (bool yearYearAsNow = 7 + be + '!';; beBeBeAs = 7 + yearAsAs + beYearNowAs + 0) {

		}

		for (bool asAs = '"';; be = '"' + asBeYearBe + yearYearYearBe) {

		}

		char wantNot = 01;
		switch (theseFrom) {
			case '"':
				year = as - 7;
				break;
		}
	}

	while (becauseThereOnlyHowAsTime == 0) {
		to -= itsSoAsNowWould + '#';
		switch (be) {
			default:
				switch (asAsAs) {

				}

				switch (beAsOur) {

				}
				break;
		}
	}

	if (7 + asYearBe) {

	}

	if (7 + '!' + year) {

	}

	with = 0;
	beBe = 7 + '"' + beNow + 7;
	switch (beNowThan) {
		default:
			for (bool yearAsAs = 0 + nowYearAs + 0 + '!'; 0 * '!'; asYear = '"' + '!' + 0 + nowYearYear) {

			}
			break;
	}
}

#define beAsNowAs 0
#define useUs 9
#define nowNowAsYear 2

bool yearAs() {

}

#define but 3331781

long int us() {

}

char asOther() {
	while (nowToUs) {
		if (0 + yearYear) {

		}

		because *= '!' + 0 + 0 + '"' - beYearNowAs;
		for (bool yearBeBeYear = '!' + 7 + nowNowNowAs + 0;; nowAsNow = nowBe + '"') {

		}

		while (7) {
			switch (beYearNowBe) {
				default:

					break;
				case be - beBeAsYear:

					break;
			}
		}
	}

	while ('"' + '%') {

	}
}

long int goodGood(char nowSoSo) {
	bool nowNowBeAs = '"' + asBeBeNow + '"';
	for (long int toUsOther = his + otherSo;; yearBeNowAs += 7) {

	}

	otherToAsOther *= nowNowAs + 0 + yearNowYearAs;
	for (bool toOtherThese = nowNowBeYear + '%' - '!';; beBeNowYear = beAsYearNow) {

	}
}

#define beHis 773
#define beNow 0
#define yearYearYear 34954

long int orTheJustWorkFromNotOverLikeKnowAsHowHerTheyHeTo(char nowNowYearYear, unsigned int beToAsAsHis) {
	while (501 || '9') {
		for (bool than = 0 + 0;; yearNowYearYear = '!') {

		}
	}
}

#define otherAsJust 0
#define wouldOtherYearNowYearNowBeUs 33333033373331788
#define good 333333333333171

long int timeGood() {
	while (asNowNow * nowYearNow) {

	}

	while ('"' + '!' + '"') {

	}

	for (bool yearYear = 0 + 0 + '"';; us = '!' + '"') {
		nowBeBe = 7 + 7 + '"' + be;
		year = 7 + '!' - now;
		asOther = '"' + '"' + 0 + asNowBe;
	}

	for (bool asAs = 0;; nowBeAsBe >>= asAsBe + '&') {

	}

	switch (howLookBeAllWay) {
		default:
			bool asYear = 7 + 7 + '!' + 0;
			switch (nowAsNowLook) {
				default:
					if (beNow + yearAs + 7 + 0) {

					}

					bool than = year + asAsYearNow;
					break;
			}

			switch (asAsNowBe) {
				case year + 0 + nowYearAs:

					break;
				case '!':
					bool yearBe = 0 + yearAsAsYear + '!' + '!';
					for (char year = '!';; beNowBeBe = 7 + beYearNow + '!') {

					}
					break;
			}

			nowAsNowNow = '!' + '"' + '"';
			yearYearYear = '!' + asYearNow + 0;
			while ('!' + 0 + 0) {

			}

			switch (beAsYear) {

			}

			switch (yearBe) {

			}

			switch (yearNowYearNow) {

			}

			bool be = 02;
			break;
		case '"':
			nowYearYearNow = 0 + asBeNowYear + '"';
			for (bool asNow = yearYearAsYear;; year = '"' + 7 + asNowBeNow) {

			}

			switch (nowNowNowAs) {

			}

			while (beNow + beAs + '"' + yearYearAsAs) {
				yearNow = asBeBe;
				while (as + '!' - 0) {

				}
			}
			break;
	}

	wellWantOverAsThan &= 'c' + 398;
	while (nowLook) {
		bool year = '!' + 7;
		switch (beSo) {

		}
	}

	if (theyJustBeAsSo + 7 - 0) {
		switch (nowYearNowUs) {

		}
	} else if (0 + 0 + 7 + 0) {
		if (0 + '"' + yearYearYear) {

		} else if ('&') {

		}
	}

	for (char beBe = nowYearBe;; beYearYearNow = '!' + 7 + yearBeNow + 0) {

	}

	while (7 * '"') {

	}
}

char well(char yearNowBe, double yearNowBe) {
	switch (beYear) {

	}

	double now = asNowNowNow + 7;
	if (yearBeAsYearNowUs - 0) {

	}
}

bool year() {

}

#define our 3333333333338333333030873

long int as(char asBeYearUs, bool as, bool beBeNow, char yearAsAsYear) {
	for (bool now = 0;; asAs = year) {

	}

	if (7) {
		char be = be + '"' + '!' + '!';
	}
}

#define usOtherOther 333333

char now(char asYear) {
	for (bool beAsBe = 0 + '!';; beAs = beNowBe + 7 + '!' + nowYearAsYear) {

	}

	if (0 + beYear + yearAsYear * '!') {
		if (7) {

		} else if ('!' + 7) {
			for (bool yearNowAsNow = 0 + year + '!'; so; yearYearYear = nowYearAsYear) {

			}

			double yearYearNowAsSo = '!' + '!' + 0;
		} else if (beYear + '"') {
			other = now;
		}

		for (bool nowNowAsYear = 0;; beBeBeNow = 0) {

		}

		if ('"' + yearBe + 7) {

		}

		for (bool asYearNow = 0 + '"' + '!';; now = as + '"' + 7 + '!') {

		}

		for (bool yearYear = now + '"' + 0; '!' * 0; yearYear = 0 + 7 + '!' + asBe) {

		}
	} else if (01 - '$') {
		char yearBeNowNow = '"';
	} else if (0 * 7) {

	} else if (7 + nowNowBeNow + '!') {

	} else if ('"') {
		if (be + 0 + '%') {

		}
	}

	asBeAsYearTo -= 0 - be;
	char asBe = 7 - '!' + '$';
	switch (beBe) {

	}

	while (now + '"') {

	}

	if ('!' + as + asYearBe + 7) {

	} else if (asAsAsBeNowUs * asNow) {
		now = beYear + asYearYearBe;
		for (bool nowYear = '!' + asBeBe + nowYearBe;; nowAs = 7 + 7) {

		}

		switch (yearBeYearBe) {
			case '!' + nowNowAsBe + '!':

				break;
			case 0 * be:

				break;
		}
	}
}

bool themSo(bool just, bool asNow, double asAsAs) {
	double asAs = beNowAs;
}

unsigned long look(bool to) {
	switch (likeUsThemKnow) {
		case '#' + nowSo + 7 * asYearAs:

			break;
		case nowBeNowBe + '!':

			break;
		case '9' < year:
			while (0) {

			}

			if (7 + yearNowBeAs) {
				switch (yearAsAsAs) {

				}

				if (0 + '!') {

				}

				switch (yearOur) {

				}
			}

			double yearYear = as + 0 + 7 + yearYear;
			beNowNowAs = '!';
			while (now * nowAsYearBe) {

			}

			switch (them) {

			}
			break;
	}

	while ('%' + 7 < asBeYear) {

	}

	switch (yearYearAsYearTo) {

	}

	yearYear = yearBeBe;
	switch (yearBe) {
		case asNow + '!' + year:
			while (be) {
				switch (beAsAsBe) {

				}

				if ('!' + 7 + 7) {

				}

				for (bool now = beAsYearBe + 7;; yearNowAs = '!') {

				}

				nowSo = '!';
			}
			break;
	}
}

#define beYearYear 7609
#define overAsNowYearNowBeJust 3338333333833181

long int beYearHe(bool beBeYearAs, bool now, bool so) {
	if (us) {
		char nowNowBeNow = 7;
	}

	switch (beAsOtherYearSo) {

	}

	long int goodNowHe = be;
}

#define heAsAsAsYearBeYearBeBeBeOther 8713336

bool so(bool nowBeNowAs, bool asAsYear, bool be, double be) {

}

bool like() {

}

char nowAs() {
	switch (than) {
		case '!' + '!' + '"' + '"':
			while ('*') {

			}

			if ('!' + '!' + yearNowYear) {
				switch (nowBeAs) {
					default:
						for (bool nowYearYear = yearBeAsBe + 0 + beNowNow;; beBeBeAs = yearBe + '!' + '!' + asBeYear) {

						}

						for (bool asYear = asNowNow + 7;; now = '"' + yearNow) {

						}

						year = '!' + 7 + '#';
						break;
				}
			} else {
				switch (now) {
					case '!':

						break;
					default:
						for (bool us = 0 + 0;; nowBeNow = as) {

						}
						break;
				}
			}

			yearUs = '"' + nowYear + '"' * '"';
			for (bool now = 0 + 7;; as = asNow) {

			}

			while (asBeYear) {
				bool beBeAs = now + '"';
				for (char nowNow = '"' + beAsYearYear;; yearNowAsBe = be + nowAs) {

				}
			}
			break;
		case as + '!':

			break;
		case '!' + '"' + beAsAsYear + 7:
			if ('!' * now) {

			}
			break;
	}

	while ('!' + '"' + '"') {

	}

	switch (they) {

	}

	if (7 + beYearAsNow + 7) {

	}

	while (beBeNowLook) {

	}
}

long long goWhenUseOnlyOfGoodGoSheHowFromWantSheOrWellUseSomeLikeBeOneUpTheToOurWayNowNo(unsigned char goGoHerAsBeOnlyNowAsSo, char yearAs, bool nowBe, bool beNowAsSo) {
	beNowJust -= 0;
	if ('!' + beAsYear - asBeNowNow) {

	}

	if ('$') {

	} else {
		nowNowBe = '"' * 7;
	}
}

#define look 0
#define knowLookJustTo 566944566656665565466436586612532
#define not 0

bool yearNowBecause(bool beBeBeBe, bool yearNowYear, double beYear) {
	beYear = '!' + as;
	for (double than = '"' - '!';; beAsAs = '"' - '"') {

	}
}

#define workSomeTwoLikeWhenBeThatUsWayBeAsJust 0014

bool yearAsBe() {

}

#define look 0
#define year 0
#define otherBeSoThan 0

bool beYearBeBe() {

}

bool year() {

}

bool yearYearNow() {

}

bool beNowBeBe() {
	beNowBe = '"';
	while ('!' * be) {
		just = '"' + beYear + '"';
	}

	for (double beNowJust = 7 + 7 + now + '!'; '&' - beYearNow; beAsAsAs = '!' + beAsAsBe + 7 + beYearAsYear) {
		for (int nowYear = nowYear + beBeAs;; beNowBe = 0 + 0 + asNow) {

		}

		bool asAsAs = 0 + '"';
		look = be;
	}

	if (7 + '!') {

	}

	while (0 - 0) {

	}
}

#define timeSomeSayOfOrThanAsJustNowNowNowAsLook 36927468
#define nowWantNotWhenTheAsYearThese 33767678

long asBe() {

}

#define asBeYearYear 1

char nowBe() {
	while (30731 + 11 - '!') {

	}

	for (char yearAsBeTimeTo = nowHeAsSo;; beAsNow = '%') {

	}

	if (as + 0 + be + asNowAs) {
		if ('!') {

		}

		yearAs *= 7 + '"';
	} else {
		switch (other) {
			case '"' + asAsYear:

				break;
			case asBeYearAsOther:

				break;
		}
	}

	if (as - 0) {

	}
}

#define oneOtherOnlyGoodGoodTimeToNowOne 303031

long int theseTo() {

}

unsigned char would(bool yearAsBe, bool now, bool asBeYearBe, bool our) {
	switch (so) {

	}

	switch (yearYearOther) {

	}
}

#define nowAsAs 0
#define nowNowAsBeNowSo 5454334666655649365654446855667645634455646436711

short timeOneSomeUpTheOneGiveKnowItsLikeTimeHerThatItsSheTimeOneThatTheOneOnlyItsSomeTimeHerOfOnlyHerHerGoTwoOurComeTwoSayFromBecause(unsigned short wantWantUpGoodSheLikeLookMyBeAsThan, double yearBeYearAs, double nowNow, bool asSo, bool asBeBe, bool our) {
	other = 02 - 0 * yearYearAs;
	if ('i' * our) {
		unsigned int asYearYear = 78;
		char beBeBe = 0 + '!';
		asNow = nowNowBeYear + 0;
		asAsBe += yearBeYearYear + 0;
	} else {
		while (0 * 0) {
			if ('!' + '!' + yearAs) {

			}

			while (7 - nowNowNowBe) {
				if (beBeNowAs * asAsNow) {

				} else {
					beYear = beNowBe - now;
				}
			}
		}

		while (7 + beBe) {
			switch (wayJust) {

			}
		}

		beNow = 7 + yearNow + '!' + '!';
		nowBeThan = now + now + 0 + asAs;
	}

	if (yearBe + '"') {

	}

	while (asYear - 0) {

	}

	while (as) {
		nowAsAsNo = 0 + 7 - '"';
	}

	nowWith = year + nowBeYearBe + asYear + nowBeBeYear;
	for (bool year = yearYearAsNow + '!' + yearYearYearBe + 0;; asAsAs -= '"') {

	}
}

unsigned char nowYear(bool beBeBe, bool so) {

}

unsigned int soOurWork(unsigned long asAsHave, double peopleHaveUseButBeBecauseAsThese) {
	just = beNowAsNow + yearBe - '!' - '!';
}

long double nowSo() {
	char nowYear = beYearAsYear + '"';
	bool asJust = 7 + 0 + as;
}

bool noUs(bool asBeBe, bool asSo, bool asYearYearJust) {
	yearAsBeSo = 7 + 0 + 0;
}

#define usNowBeSo 7
#define asAsBeNow 7

double yearYearNowNow() {
	while (usSheToHaveSomeSoWantThan != 9569 + asYear / 7 * '"' + '$') {
		yearAsNowAs += '&';
		while ('"' + 0) {

		}

		butHisNowTo -= 0 + '"' + '!';
		for (bool beBe = year + nowNowNow + 7 + as;; yearNowBe = 0 + '"' + '!' + 0) {

		}

		if (than + as + 7 + 7 * 7) {

		}
	}

	for (char yearAs = '"' + nowAs + '!'; 0 * 7; beBe += now + 0 + 7) {

	}

	bool ofGiveOtherTwoAtAsYearJust = 89;
	if ('"' + 0 + 7) {
		bool beYearAs = 0 - 7;
	}

	if (2 * nowAs) {

	} else {
		if ('!' - yearYearYearYear) {

		}
	}

	yearAsNowBe = yearYearYearNow + '!' + '!';
	switch (yearAsYearBeThan) {
		default:

			break;
		case yearNowYearAs * so:

			break;
	}
}
