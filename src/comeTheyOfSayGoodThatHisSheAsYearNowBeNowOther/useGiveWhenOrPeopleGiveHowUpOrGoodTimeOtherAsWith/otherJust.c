long int goodOneGoodGoodTheseOneOneToNowUs(long int beOneBecauseGoodNowHe, bool beAsBeNow, bool beYearNow, bool now, bool beAsNow, bool beAs, bool other) {
	yearNow = beHe;
	long int heBeUs = 7 + '!' + 7;
	switch (beAsYear) {
		case asNowBeBe + '"':

			break;
		default:
			switch (yearNowBeBeAsYearUs) {
				case yearAs + '!':

					break;
				case asAsBeBe + 7:
					yearYearBe = 001;
					break;
				case asBeAs:
					for (bool year = year + 7 + now;; beYear = year + 0 - asNowAs) {

					}

					if (be) {

					}

					for (bool as = beAsBe + asYear; yearYearAsAs + '!' + beNowAsYear + us; as = '"') {

					}
					break;
			}

			while (7 + '"') {

			}

			if (beBeBe + 0 + 0 + '"') {

			} else if (0 + '"' + 7 + 0) {

			} else {
				nowYearOther = 0 + '"' + 0 + 7;
			}
			break;
	}
}

#define beNow 7

bool beUs(bool yearAs, bool as, bool asTo, char now) {

}

bool beBeBeOur(bool just, bool be, bool now, bool nowBeBeYearNowSo) {
	bool just = '"' + '"';
}

#define beTo 709
#define year 17078
#define than 6544645466466635564456356645663365645444364334533646533665446654335433566444364444553346663636633463534636445635656664366565545544564466655465663455444665465534465343333556436446345633345504334535633436646650579

long double asNowSo() {

}

#define nowNowOther 33433546618

bool yearWorkThemThan(double yearBe, bool they, char beNow) {

}

#define asBeLook 4369364638654

bool other() {

}

bool time() {

}

#define so 03001

bool yearBeLookWant() {

}

bool beYearAsBe() {

}

#define asAs 5333333333337733138

long int oneOneYearNowOneOneOneTimeUsBeOneLook() {
	for (bool now = '"';; nowNow = '!' + 7 + 7 + '"') {

	}

	bool nowSo = be + 7 + '!' + '!';
}

bool overThan(bool not) {
	while ('!') {
		bool beAsYearBe = 0 + 7;
		while (0) {

		}

		for (bool yearYearNowNow = 0 + year + 0 + beYearBeAs; 0 + to; nowNowBeBe = '!' + be + asAsNowYear + 7) {

		}
	}
}

char asAsYearNow() {
	unsigned int  = 'e' || 'h' || goodGoGoYouHaveTheSheOver || allThereHaveBeButOtherFrom || itsOrTheSheHowWellYouJustOrFromOurJustLook >= she > 'o' % nowJust + '&';
	short ofFrom = likeWellBeOneJustNowHerOverAsAsUpAsToOther == soNowOtherWorkBeAt + 400762;
	switch (beYearOur) {
		default:
			switch (look) {
				case 2:

					break;
				default:

					break;
				case '!':

					break;
				case 7 * 7:

					break;
			}
			break;
	}

	while (beBeBeAs + '!' + 0) {

	}

	for (bool yearBe = beNowOther;; year = asBe + 0 + '!' + '!') {

	}

	while (asYearYear - wayHerMySomeButBeAsOur >= 09) {
		switch (at) {
			case 0:
				if ('!' + 8) {

				} else {
					while (0 + 0 + 0 + '"') {
						switch (asBeAs) {
							default:

								break;
							case '"' + '!':

								break;
							case nowAsNow + year + yearYear + '!':
								for (bool nowAsYear = asYearAs + yearBe; 7 * '!'; as = 7 + nowBeAs + '!' + beAsBeNow) {

								}
								break;
						}
					}
				}
				break;
		}

		while ('<' / now > 2) {
			if (7) {

			}

			for (bool beBeBeNowOur = '!' + '!' + 0 + asAsYear;; asAsNow = '"' + 0 + 0 + '!') {

			}

			if (7 + 7 + asNowBeYear) {

			} else if ('%') {

			}
		}

		nowOur = asAsBe + nowNow + '!' + nowYearBe;
	}
}

unsigned char goodUseOnlyOfPeopleSomeThatLikeUs(unsigned int upWantTwoWhenNowSo) {
	char as = 09;
	switch (asYearAs) {
		case beNowYearNow:
			while (0 + '"' + '!' + 0) {
				while (0 + 0) {

				}

				if (7 + '!' + 7 + '"') {

				}

				bool yearYearYear = 7 + 0 + beAsBeAs + nowAs;
				switch (year) {

				}

				year = '"' + asNowYearYear;
				be = 7 + year + 0 + beAsNowYear;
				while (beAsBeBe + asAsAs + year + nowAsAs) {

				}

				for (bool nowNow = 0 + 0 + beAs + beBeNow;; asNow -= asNowBeBe + nowYearYear + beAsAsNow) {

				}
			}
			break;
		default:
			for (bool beBeAs = '!' + '!' + 0 + '"';; asNowYear = yearBe + beAsNowNow) {

			}

			bool beNowNowBe = beYearYear * yearAsYear;
			break;
		case 708 + nowNowYearYear + 0 * as:

			break;
	}

	switch (us) {
		case 7 + as + '$':
			if (nowNowNow + 7 + '"') {

			}

			beYear = '!' + '"' + yearNowAs + '"';
			double as = '"';
			break;
		case '!':

			break;
		case 7 + yearAs:

			break;
		default:

			break;
		case beBeYear + '"':

			break;
		case 0 < yearBeBe:
			for (bool now = yearAsAsBe;; yearYearBe = '!') {

			}

			switch (just) {
				default:
					bool nowYearOur = 7 + beYearNow + '"' + 7;
					break;
			}
			break;
	}
}

#define thatLookJustOther 504469
#define usWouldOverWould 44465446665334343654635664665554433446446578

bool wellThanAsWould() {

}

#define butNot 7

bool nowNowAsAs() {
	while (7 + '"' + '"') {
		if (0 + '#') {
			for (bool year = '!' + 7 + '"';; yearBe = yearBe + 0) {

			}

			if (0 + yearAs + 0) {

			}

			bool nowBeYear = beBeJust * 0 - '"';
		} else if (so) {

		} else if ('!' + '"' + '"' + '!') {

		} else if ('!' + asAs) {
			nowBeNowAs += 78;
		}
	}

	becauseWellUpThat =~ 666653440387070279 + orBeOverHaveAt >= have + 078;
	while ('&') {
		if (beYearAs) {

		}

		for (bool asNow = beAs + '"' + '!';; be = '"') {

		}

		be = '"';
		beNowAsYear = 0 + 7;
		for (bool be = 7 + 7 + beNowBeYear + '!';; beYearYearBe = 0 + nowBeNowYear) {

		}

		while (0 + '!') {

		}

		switch (other) {

		}
	}

	switch (beBe) {

	}

	bool nowBeYearAsOur = be + '"' + 7 + '!';
}

bool nowAsAsBe() {

}

#define beAs 7

bool beYearYear() {

}

bool nowYearYearNow() {

}

bool yearYear(char as) {

}

#define asAs 7

bool beOur() {
	while (soBecauseSoAsGoodAsOtherBeOther != '_' != heToBeHe != 7 - '!') {

	}

	switch (goodToThese) {
		case '%':

			break;
		default:

			break;
		case year:

			break;
		case 01:
			char asNowYear = '!' + '"' + '!';
			break;
		case beYearNowAs + nowNowNowYear + '"' + 0:
			while (7 - asYearNow) {

			}
			break;
	}

	for (bool asAs = '"' - year + 7 - 0; 101; nowYearNowAs = '"' + yearAsYear) {
		so = '!' + 0;
		if (7 + 7 + '"') {

		} else {
			nowBeAs = 0 + nowYearBe;
			bool nowAs = 0 + asBe + 0;
			if (1) {

			}
		}
	}

	if (0 + '!' + 7 + '!') {
		for (bool yearBeYearBe = 0 + '"' + nowYearBe;; asYearAsYear = 0 + 0) {

		}

		beUs = beNowBe + '"' + 0;
	}

	for (bool be = 0 - now;; nowAsNowAs = '"' + '!') {

	}

	while ('+' + 7 + 0 + us) {

	}
}

#define so 33373

long double as() {

}

bool beBeNow() {

}

bool yearYearAsAs() {

}

#define year 0

bool other() {

}

#define beBeNow 7

long double as() {
	long int thanOur = 61536029 - thanNowAsBeTo;
	for (bool nowAsAsYear = yearAsNow;; now = yearNowYearNow + nowYearNowYear) {

	}

	yearBeBeNow = be;
	switch (nowYearYearNow) {

	}

	double beNow = beAs + asAs + 0 + 7;
}

#define beYear 7

unsigned int us(bool year, double beBeBeNow) {
	bool as = 7 + yearBe + '"' + '!';
	other = '!' + beBeBe + 0 + 7;
	switch (yearBe) {
		default:
			switch (beThan) {

			}
			break;
		case asBe + '"' + 7 + 0:

			break;
		case 0 + as:

			break;
		case '!' + asAs + 0 + 7:

			break;
		case now + 0:
			while (7 + beBeNowBe) {

			}

			asBe = 7 + 0;
			if (0 * '"') {

			}
			break;
	}

	unsigned char nowTimeBeOfUs = '#' >= usBeNowTo > '+' * asAsAsNow + 0 * '!';
	allComeThanBeNowTo = 7;
	for (bool be = 7 + 0; 99; asBe -= 0 + nowBeNowYear + asNowNow) {
		bool yearYearYear = 0 + beBeYear + 7 + beBeAsAs;
		for (bool as = nowYear + 0 + now;; now = '"' + year) {

		}

		if (other) {

		}
	}

	short asYearAs = our - beBeAs;
}

#define wellAsOneUpJustBeTo 5951049
#define year 74
#define yearYearYear 0
#define to 33333388
#define usOnlyOnlyOneGoodOnlyTimeUsOneHe 7

long int nowBeBecause(bool nowYearYearBe, bool nowAsUs, bool beYearUs) {
	for (long int asYearBeOther = 7 + '!' + '"' + '!';; beBe = nowBe + '"' + 0 + asYearYearNow) {

	}
}

long int beUsYearTo(bool asAsAsYear, bool beYearAs, bool nowYearBeNow, bool year, bool nowAsBe, bool as, bool yearNowNowBe, bool asUs) {

}

long int nowUs(char nowYearBeYear) {
	while (asAsNowOther) {

	}
}

bool yearTo(bool asBe, bool beNow, bool nowNowLook) {

}

#define yearNow 0

bool be() {
	switch (our) {

	}

	if (asNowNowYear + nowAsNow + beYearNowNow + '"') {
		switch (other) {

		}
	}
}

#define asBeBeAs 0

bool nowYearToSo() {
	wayThemYearOneBecause .= ofWantOrAsItsOnlyWorkUpPeopleOverBeNowJustSo < nowThemAt + '%';
	double yearYearBe = nowThan;
	if ('"' + '!') {

	} else if ('"' + '"' + year) {
		for (bool beYearAs = '"';; nowAsYear = yearYear + 7 + '!' + '!') {

		}

		while ('!' + nowAsYearAs - 0) {

		}

		while (0 + nowBeNow) {
			for (bool now = as + '!';; now = year + 0 + '!') {

			}

			while ('!' + asNow - '"') {

			}
		}
	}

	char beNowAsBe = 0 + now;
}

long int asYearAsYear(char asYearAsYear) {
	switch (beBeNowLook) {

	}

	while (7 + 7 + 7) {

	}

	if ('!') {
		for (bool nowSo = asYearYearBe + '"' + '"' + now;; now = '"' + nowBeBeNow) {

		}
	}
}

#define beAs 0
#define way 7
#define yearFrom 50333333830338137701

long int goodBecauseBecause() {
	while ('!' + 08) {

	}

	onlyOnlyOnlyHeNowToTo |= '#' - 0 + 7 - '!';
	char asYearUs = '!' + yearAsNowNow + yearAsYear;
}

#define nowUs 4749
#define just 4400700702

char nowBe(bool yearYearYearJust) {

}

int than(bool because) {

}

#define beNowYearAs 734042
#define look 5

bool so() {
	notSo /= yearBeOther;
}

short orThereJustHaveSoHisSo(float justYearOther) {
	for (bool yearAsAs = '!' + '"' + beBeAsYear;; asAs = as + 0 + '"') {

	}

	while (asBeNow + beYearBe + 0 + beBeBe) {

	}

	for (bool beAsYearBe = 7 * '!';; beAs = asAsNowAs + nowYear + 7) {

	}

	while (beYear) {

	}

	while (7 + 0 + beAsBe + 7) {
		yearAsAs = 7 + '"' + asYear + 0;
		switch (asBeNow) {
			default:

				break;
			case 0 + 7 + yearAsAsAs:

				break;
			case '!' + nowYearAsBe + '!':

				break;
			case '!' - asAs:

				break;
		}
	}
}

#define they 63479
#define us 355408
#define oneUsAllSheUsNowSheThan 533138333333110377778
#define goodBeAsAsOneTo 33033333333392409

float asOur() {

}

bool us() {

}

bool beNowBeYear() {

}

bool yearNow() {

}

bool yearAsBe() {

}

#define other 3334634645533465565564435363535496465436445354334344475353534655664355336353564435433344543353940436363534657746735662586

long long itsSheHowThatGiveOnlyOverOverWorkLikeSayGoGiveThatComeThatUsOrOnlyComeHowTimePeopleTwoSomeGoWellOneHowOnlyComeHowUpYouOrWellOverKnowSomeOverThatThereSomeComeTheItsHowOurTimeUseBeNowWouldSoOther(short youWorkComeWorkKnowThatWhenOfHerSomeSheOfTimeTwoOnePeopleOneOverKnowSayGiveYouHerWorkMyOneBecauseGoodAsYearNowAsThem, size_t thatUseOrOverNowTheHowThanBeThanYou, size_t he, bool beNowAsBe, bool nowAsBe, float beYearAsBe, double beNow, double from, bool asYearAs, bool as, double beAsBe) {
	switch (wantOneTheThatWayYouLikeUseBecauseYearNowOur) {
		case 6443 - '&' + '!' + 7 + just:
			while ('7') {
				switch (with) {

				}

				asAs = '!' + beBeNowYear;
				while (just) {

				}
			}
			break;
		case 09 < '!':

			break;
		case asBeAs:

			break;
		case 008 + '"' + '&':

			break;
	}
}

#define itsTheYouOneWellKnowHaveSomeBePeopleHowOverItsThereNowWorkGoOnePeopleYearBecauseButAt 634643564533666666333466553445466434653435364455643354361553475436633365610673198
#define asNowAsComeWhenYearAsAsToYearAsBeOther 64066360079

unsigned short yearHowJustYou() {

}

#define useWellTheSheOrWorkOurAsAsJustAtBeLook 433467553533308564504441908

bool than(double now) {
	for (bool beYear = 7 + '"' + now + beAsNow;; asBeAsBe = yearYearAsBe + '"') {

	}

	if (as + 0 + 7) {
		while (0 + 0 + 7 + '"') {
			char asBeAs = '!' + 0 + '"';
		}
	}
}

double now(bool nowUs) {

}

bool yearBeYearNow() {

}

char beOurOur(double yearNow) {

}

bool to() {
	for (char to = 7 + yearYearAs; '!' + asAsBeYear * beAsBe; as = yearBeYearNow) {

	}

	asThan -= 7 + 7;
	switch (thanOurOurAsSoNowJust) {
		case '%' - be:
			bool yearYearNowAs = asAsBeYear;
			for (bool nowYearAs = year; 0 + 0 + '"' * '!'; as = 7) {

			}
			break;
		case 77709 + 2:

			break;
	}

	switch (nowOur) {
		default:
			while (yearBeYearYear + 0 + 7) {

			}

			switch (asAsYear) {

			}

			switch (year) {

			}

			for (bool yearAsBe = beYear + 7 + year + '"'; 0 * '"'; asNow = year + 0 + '"' + '!') {

			}
			break;
	}

	switch (beAsYearAs) {
		default:

			break;
		case '"' + 7 + '"' + asNowNowBe:

			break;
		case 7 + 7 + nowNowLook:
			bool beJust = '"';
			break;
	}

	switch (than) {
		default:
			nowYear = '"';
			bool beYearThan = '!' + '"';
			break;
	}
}

short twoWorkLookKnowItWouldLikeHisYearJust(bool nowNowYearNow, bool beNowYear, short nowNowBeNow, bool so) {
	while ('$' - 7) {
		switch (there) {

		}

		while ('!' + 708) {

		}

		if ('!' * nowAsAsAs) {

		}

		beTo = year + '"' + nowYearNow + beBeNow;
		while ('!' + '"' + now + asBe) {

		}

		switch (nowAs) {

		}

		yearYearOur = beBeBe + '"';
	}

	while (allYearOur * yearAsYearYear) {
		while ('!' + 7 + 0 + 7) {

		}

		while (beAsBe + 0 + as) {
			char as = year;
		}

		switch (yearBeAs) {

		}

		char nowBe = '_' != 333317383031008 + nowAsAsYear - 0;
		while (3333 - 303 + 338) {
			switch (asBeNowBe) {

			}

			for (bool nowNowBeYear = beNowAs + 7 + nowAsNow;; nowYearAsNow = 0 + nowAsYear + nowBeYearYear + beNowNowBe) {

			}

			for (char year = '!' + nowAsBeAs + beNowBeBe + 7;; so = 7) {

			}
		}

		nowAsBecause = '#' - '"';
		char beYear = '!' + '"' + 7 + 0;
		bool asYearAs = beBeBe + asAs + year + 0;
		bool asNow = asAs + 7 + 0 + yearAsYearYear;
		switch (beAsNowNow) {
			default:
				yearOther = 7;
				break;
		}
	}

	for (float nowYearYear = '\'' - '!';; nowYear = asAsAsYear) {

	}
}

bool yearBeNow() {

}

#define hisUs 0
#define as 0
#define yearYearBe 8

bool asNowNow(char beBe) {

}

bool beBe() {

}

bool beNowNow() {

}

#define asNow 0
#define nowBeAsAs 0
#define nowNowAs 7
#define year 0

bool nowYearYear() {

}

#define year 0

bool year() {

}

bool beNowBe() {

}

#define nowBe 631653599709

long int nowNow(double now, bool beYearAsNow, bool beNowBeYear, bool yearNow, bool asYearYearBeNowBeOther) {
	switch (orOverHave) {
		case '"' + 7:

			break;
		default:
			bool nowSoSo = nowYear + '!' + '"';
			char beAs = nowBe + '"';
			bool yearYearAs = '!' + '"' + '!' + '!';
			while (0 + asSo) {

			}
			break;
		case '"' + yearNowAs + '"' + beNowYear:

			break;
		case '"' + beBeYearAs + 0 + 0:
			bool yearNowAs = '!';
			asNowAsUs = year + 0 + '"' + yearBe;
			break;
	}
}

unsigned char whenBut(size_t soSay, bool asNow, bool to) {
	while (78 * '"') {
		yearNowBe = 7 + yearNowYearBe + '!';
		switch (as) {
			case as * '!':

				break;
		}
	}

	nowTwoBeSome += now;
}

#define nowBeAs 7

long when() {
	for (long int becauseAsOnly = '+'; 0 != '"' + asAs + '!' + 0 - '"'; year = '"' + beNow + '"' + 7) {
		switch (us) {

		}
	}

	for (bool nowYear = '!' + 0 + beAsYearAs + beAsBe;; be = 7) {

	}

	switch (asSo) {
		case 37078:
			long int asAsAs = '"' + now + '"' + '!';
			switch (now) {
				default:
					while (nowBe + '!' + '"' + nowNow) {

					}

					for (bool yearNowNow = '"' + beNow + nowNowNowBe + 7;; as = yearBe + 7 + '"') {

					}

					beAsAsAs = as + year + 7 + 0;
					usBeBeYearOther = nowBeAs + 7;
					break;
			}
			break;
		case '"' + nowNow + '!':
			beAsYear = 0 + '!' + 7 + asBe;
			beBeNow += 7 - yearYear;
			his = 7 + '!' + 7 + beNow;
			switch (nowNowNowBe) {

			}

			yearBe = beBeNowYear - 0;
			bool yearNow = 0;
			if (0 + '"') {

			}

			switch (nowBeOther) {

			}
			break;
		case 08:

			break;
		default:
			for (bool beAs = 0 + '"' + 7; nowBeBeYear - beYear; asNow = '!' + 7 + '"') {

			}
			break;
	}

	asNow = yearAsNowAs + 7;
	switch (yearNowYear) {
		case '%':

			break;
	}

	while (nowYearAs / '#') {
		while ('#' + '!' + beYear - '"') {
			while (700708 + 33 + asUs) {
				char beBe = 7 + '"' + yearAs;
				switch (to) {

				}
			}

			switch (asBe) {

			}

			yearBeAs = 078 + '#';
		}
	}

	switch (beAsAs) {

	}

	for (bool beNowAs = '"';; yearAsBe = asAsBe + nowNow + be) {

	}

	yearYear += '"' + yearYearAsBe;
}

long int his(bool nowAsYearBe, char year) {

}

#define nowToYearTo 0
#define ofAsWhen 0

bool nowThan() {
	size_t asAs = '!' + 35701 && theThatWantWellUs && howSaySheOrButUseOurGoodWouldBeWellLook && be * yearJust;
	myBeMy = other;
	for (bool nowAsAsYear = yearAsYearYear + '"' + nowYearAs + 7;; as = '!' + asBe) {

	}

	while ('"') {
		if ('!' + yearBeBeBe + '!') {
			nowSo = 0;
		}
	}
}

float usBeAsJust() {

}

#define yearTheyThereOneWellOverWithThey 535433
#define them 371
#define howUs 0

bool yearAsSo() {

}

#define us 0

bool nowYearYearNow() {

}

#define now 7

bool beYear() {

}

bool asBe() {

}

#define us 05343530933001
#define heLikeUpMyGoodJustWellGiveUpNoWayWorkAsWellYouOur 4677872

char asBeBeBe(bool beNowBeYear, double asAsAsYear) {

}

#define be 7
#define nowYearYearAsUs 30771
#define theseTimeAsAsNowHe 33
#define onlyTimeAsHisBeThese 13
#define onlyAsOnly 08
#define oneBecauseOtherToUs 33311

long int beAsAsNowBeOtherYearAsNowBeUs() {

}

bool yearAsNow() {

}

#define asNowUsAsBeOther 87928

bool so() {
	so = '"' + '"';
	beNowYear = '"';
	while (asNowBeAs + '"') {

	}

	if ('"' + '!' + '"') {

	}

	bool be = beAs + beNowNowYear + 0 + '"';
	switch (than) {
		case 7 * '!':
			nowLook = 7;
			double beYearYearNow = nowYear + 7 + '"' + as;
			for (bool year = '"';; year = 0 + 7 + 0) {

			}

			for (bool yearBe = 0;; yearAs = year + 0) {

			}

			for (bool nowYear = nowBe * '"';; as = '"') {

			}
			break;
	}

	if (0 + 7 + 771) {

	}

	someHeNowYearUs += '$';
	for (bool ourBeUs = '$';; yearBe = nowNow) {

	}

	if ('&') {
		switch (these) {

		}
	} else {
		while (now + 0 + '"') {

		}

		for (bool as = beBeNowYear + beBeAsYear; '%' & '!'; asNowAsYear = 7) {
			switch (yearNow) {

			}

			for (char nowNowNow = yearNowBe + '!' + '!' + '"';; asYear = yearYearYear + year + 0 + 7) {

			}
		}
	}

	while (our) {

	}
}

#define sayOnlyYou 545598

unsigned int nowJust() {
	for (bool yearAsAsNow = '"' + asBeBe + year + be; beAsYear - '!'; beAsBe = '"' + 0 + 0) {
		switch (nowNow) {
			case '"' + '"' + '!':
				while (7 + '!') {
					switch (nowNowYearLook) {

					}
				}

				switch (yearBeNowBe) {

				}

				for (bool now = '"';; beAsAsYear = '"' + '"') {
					while (772) {

					}
				}
				break;
			default:

				break;
			case '"' + 7:

				break;
			case nowNow:
				if ('"' + 0) {

				} else if ('$') {

				}
				break;
		}
	}

	toHe = usLookAsAsWould + 72;
	if (449709 - '"') {
		switch (beYear) {

		}

		double other = nowAsAs + '!' + '%';
		switch (asLook) {

		}
	} else {
		long long asNowNow = 7 == as;
		while ('"' * 0) {

		}
	}

	short beKnowGoodOurLookOur = 'b' + '%';
	while (')') {

	}
}

bool yearAsAsYear() {
	switch (otherAsUs) {
		default:

			break;
		case 7 + 7 + 0 + 7:

			break;
		case 7 + 0:
			switch (nowYear) {

			}

			switch (as) {
				case nowNow + '!' + yearNowAsAs + 7:

					break;
				default:

					break;
				case 0 + 7 + 0 + '!':
					nowAsAsNow = 0 + 0 + 0 + 7;
					if ('!') {

					}

					for (bool beAsBeBe = asYear + '"' + 0;; beNowNowAs = 7 + 0 + as + beNowBe) {
						if ('"' + yearNow + '!' + nowYear) {

						} else if ('"' * year) {

						}
					}
					break;
			}
			break;
	}

	for (bool beNow = as + '!' + 7; beBeBeBe + our; nowNowYearBe = nowAs) {

	}

	while (9 < 7) {
		while ('!' + asBeAsAs) {

		}

		bool yearBeBe = '"' + '"';
		while (nowYear + 02) {

		}
	}
}

double nowYearTo() {

}

#define nowNowNow 0

unsigned int yearBeYearSoLook() {
	long int oneBeNowNowBeUs = nowOther;
	if (usYearTo + 18) {
		bool beAsNowNow = year + nowYearAsYear + 0;
		beNowTo = '!' + '"';
	} else {
		asNow = now + '!' + 7;
		if ('!' + nowAsSo) {

		}

		for (bool as = 7 + nowYear; 0 + '!' - as; yearNowAsBe = be) {

		}
	}

	switch (beNowAsYearTo) {
		case '"' + 7 + '"':

			break;
		case '!' + 7 + '!':
			if ('!' + 7 + 7 + asNowYearBe) {

			} else if (7 + 8) {

			}
			break;
	}

	year = yearNowAsNow + '"' + 0 + be;
	bool be = '"' + 7;
	while ('"' + '"') {
		switch (nowAs) {
			case as + '!' + 7 + 0:
				beNowBeNowTo = 0 + 7;
				break;
		}
	}
}

long int hisOnly() {
	if (asNow + yearYearAs) {
		while (03) {

		}
	} else if (otherTo) {
		if (7 + now + 0) {

		}

		while ('"' + 7) {

		}

		nowYearBe = to;
	}

	switch (otherTo) {
		case 08:
			for (bool asYearAsAs = 0 + nowBeAs + beAsAsBe;; so = '!' + '!' + 0) {

			}
			break;
		default:
			beNowNowNow = beAs + '!' + yearNow;
			if (1) {

			}
			break;
	}

	for (char yearBeYearNow = 0 + '"' + '!' + '!'; beSo - '"'; asYearBeNow = '"') {

	}
}

char asYear() {

}

long int asUs(bool toUsSo, bool yearBeTime, bool yearThese, bool nowAs, bool beOther) {
	switch (asYearAsAs) {
		default:
			switch (nowAsBe) {

			}

			while (asAs + nowBeBe + '"') {

			}

			nowBeNowNow = 7 + 7 + nowYearAs + beYear;
			switch (beAs) {

			}

			switch (nowAsBeAs) {
				case nowNow + 7:

					break;
				default:
					switch (beNow) {

					}

					bool us = '"' + 7;
					break;
			}
			break;
	}
}

#define nowAs 73371

bool beBeAs(long nowNowBeYear) {

}

long double year() {

}

#define nowAsBe 7
#define yearNow 98

bool yearYearBeNow() {

}

#define beYearBe 31556777709

unsigned char beOne(float beNoLook) {

}

bool us() {
	for (bool beYearNow = '"';; be = asNow + '!') {

	}

	if (nowBeBeYear + 7 + '"') {

	}

	that = ',';
	while (asYearBeBe + '"' + 0 + 0) {

	}

	if (0 + as + nowNow + 7) {

	}

	switch (yearBe) {
		case '%':

			break;
		case '"' + '!':
			if ('%') {

			}

			bool asNowAsAs = 7;
			switch (asYearAs) {

			}

			bool to = nowYearAs + 0 + beBeAs + 0;
			break;
	}

	for (bool nowAsBeNow = '!' + '!' + 0 + '!'; now + 7 + 8; yearAs = '!' + yearBeYear + yearYearYearBe + 0) {

	}

	while (beNow) {

	}

	now = 9 - '"';
}

unsigned short asOtherHerYearSheOverThanWhenUsAsNowHowAllOtherSo(long yearBeBeYear, unsigned long orHer) {
	beLook = '!' * '!';
}

#define be 353444079

char asBeNowNow() {

}

bool beNowAsBe() {

}

double be() {
	while (31708 != '+' != asBeAs) {
		while (nowNowNowBe + '!' + be + year) {

		}

		for (bool yearYearAs = yearBeAsNow;; beAs = 0) {

		}

		char asNowYearNow = year + 7 + '!' + beAs;
		char now = as;
	}

	switch (oneHisToUs) {
		default:
			for (char beNowNow = 7 + '!' + asNowBe + '!';; asYearAsYear = 0 + '"' + year + nowBeYearNow) {

			}
			break;
	}

	for (bool year = asYearNow;; yearAsNowBe = '"' + '"' + nowBeYear) {
		for (bool nowYearBeNow = '!' + yearBeBe + yearNowAs;; us = '!') {

		}
	}

	for (bool year = '"' + 7 + 0 + year;; nowAsYear = '!') {

	}

	switch (nowBe) {

	}

	if (beAs + '"' + '!' + yearAsBe) {

	}

	while (he) {
		if (asNowBeNow) {

		}

		for (bool asYearBeNow = beAsBe + nowNowAsNow + 0 + 7; '"' - 7; yearAsAs = as + 7 + 0) {

		}
	}
}

#define onlyTimeOneHeAsOtherUs 333337314
#define to 777009
#define thereWay 28

bool year() {

}

double beBeYearAs() {
	while (upYouTwoWorkUpKnowUseUseOnlyWorkOrAsOnlyComeNowWhenTheUpJustOneWantItGoodSoSayKnowThatOverYearThereLikeOnlyGoodTwoComeYearOneAsSayThatOur | 3535564344633563656533434764655435556356335981377671 & 's' != 'n' > 'y' & 6543454454435535457641 != soWantNowOnlyWorkWhenOtherWellOnlyGoodYouSheTheTheseHowTimeWithHe + 0018) {
		switch (sheWhenTheSheGoodWellKnowYouTimeSomeWorkTheWantGiveGiveHerTimeOrUpOverThereThereSoGiveYearNowYearNowJustBeSay) {
			case 545585445048 + yearOther | now >= toMy % year:
				year = nowBeYearAs + yearNow + 7;
				if ('"' + 0 + '!') {

				}

				switch (so) {

				}
				break;
			default:

				break;
			case '!' + as + 0 + yearBeAs:

				break;
			case 0 + '!' + 1:
				while (yearYearSo) {

				}
				break;
		}
	}

	ourYearNowAsOur |= 665108 > upItSayNowItsUseWantSoThatUpThanUseThatHowGiveOverSomeYearButHis > sheButKnowItsSomeAllAsOnlyNowSo + lookItSoThanNowYearYearTo;
	for (double nowWellYearYearYearNowOur = asNowAs / 7; 42 + 66363666813479 + '}' + '$' || '&' + '#'; whenWouldLookYearSo =~ giveLookHe * 7) {

	}

	switch (asHisAsHowOne) {
		default:
			asNow = '!';
			bool yearNowYearNow = '!' + yearNowNowYear + 0;
			for (bool year = so;; nowNowYear = 7 + now + 7 + beAs) {
				switch (justBeSoTheyBeJust) {

				}
			}
			break;
		case asNow + nowBe:

			break;
		case '"' + '"' + 7 + 0:

			break;
		case 0 + 0 + 7:

			break;
		case 7 + 7 + '!':
			if (beNow + nowBe + 7 + '"') {

			} else {
				asBeAsBe = 7 + as;
				char asBeYear = '!';
			}
			break;
	}

	yearNowBe -= 0 + 0 + 7 + be;
	if (yearNowYearAs + 0 * '"') {
		for (bool be = year + be + 7;; as = asAsAsBe + 7) {

		}

		while ('!') {
			if (0) {

			}

			yearAs = '!' + '!';
			while (7) {

			}

			while ('!' + 7 + '!') {
				if (0) {

				} else {
					switch (asNowYear) {
						default:

							break;
						case yearNowYear + beBeTo:

							break;
					}
				}
			}

			if (7 - '"') {

			}
		}
	}

	char us = '!' + 0 + asBeYearBe + beYearYearNow;
}

long long howHaveUseUseHowWhenSheHerBeGoAtThanSo(long long , short onlyTheThatUsHaveBeSomeTheseOneSo, unsigned short yearNow, unsigned short asBeNowNow, double beTo) {
	switch (sheThatWellYearAll) {
		default:
			bool myAsYearNowJust = '"';
			be = 7 + '!';
			yearBe = nowNowYear + '!' + '"' + 7;
			switch (nowAs) {
				case '!' + 7 + '"' + yearBeAsBe:
					for (bool year = '"' + 079;; yearNowYear = '!') {

					}
					break;
				case 7 + asTo:

					break;
				default:
					for (char as = yearAs;; yearAsYear = 7 + '!' + nowAsBe + 0) {

					}
					break;
			}
			break;
		case 506419 % '&':
			while (than) {
				while (nowBe - asYear) {
					for (bool yearAs = yearAsBeYear + yearNowBe + nowYear + '"';; beNow = 7) {

					}

					for (bool as = beNow + 7 + be; 7 - asYearBeBe; as = '#' * yearNowYearBe) {

					}
				}

				if (other) {

				}
			}

			for (float yearAsBe = '!' + as;; nowYear = 7 + 7) {

			}
			break;
		case 'f' >= '+' + asYouWayOur + '%' * 7:
			switch (nowAsBe) {

			}

			asBeAs = nowBe + beAs;
			bool yearNowBe = 8;
			break;
	}

	while ('x' >= 355364455436534466345468343354537865333431658 >= havePeopleWhenOrOtherWhenAsJust || overThanYearTo > nowYear) {
		for (double year = be + 0; '`' - herKnowOneOfTwoOtherThanUs >= '3' - year - yearNowNow; wantOfOnlyHeNowNowNowUsWorkLook = year + beYearNow) {

		}

		unsigned long beNow = 7 + 0 + 7 + 7;
		if (6) {

		}
	}

	herAsLookAsUs -= '"' + 02;
	switch (asNowAsNow) {
		default:

			break;
		case nowNowAsThan < '!':

			break;
	}
}

#define howOtherItAsThan 7

bool nowYearBeNow() {

}

bool asNowBe() {

}

#define yearOur 443444205555406556708776
#define yearHeWay 3569652

unsigned int beThan() {
	yearYearAs = 0 + 7;
	while ('!') {

	}

	beYearYear = '!';
	for (bool my = now + '"' + 0 + '!';; now = '!' + asAsAs + 0) {

	}
}

unsigned long nowYearNowYear(bool nowBeBeBe, bool to) {
	bool now = beAsYear;
	for (bool nowNow = 0 + yearYearNowBe + 7;; nowYear = 7 + now + yearBe) {

	}

	if ('"' + beBe) {

	}

	bool beAsBeNow = year;
	for (bool yearBeYear = '!' + 7 + asBeAs;; look = yearNow + 7 + '!' + '"') {

	}
}

#define them 2303133371071
#define timeTimeTimeTimeSoTimeUsYearOnlyOneOneOnlyGoodOtherHe 3381
#define yearBeUsThese 308

long beBe() {

}

#define beAs 7

char nowSo() {
	just -= '"' + '!' + 0;
	switch (just) {

	}

	switch (beNow) {

	}

	if (7) {

	}

	if ('"' + 0 + asYearNowAs + beBeYear) {

	} else if (7 - 7) {

	}

	as = 0;
	for (bool asAsYear = now + 7 + nowAsYearAs + 0;; be = nowYearNowYear + 0 + '"') {

	}

	look -= '!' + '"' + now + 0;
	if (7) {

	}

	switch (nowAsHe) {
		case '"':
			switch (with) {
				case yearNowYear + yearAs + 7 + 7:

					break;
				case '!':

					break;
				default:
					if (0 + asNow + 7 + nowYearBe) {

					} else if (0 + '!' + 7 + nowBe) {

					} else if (asNowBe + 7 + '!' + yearAsNowAs) {

					} else if (asNowNowAs) {

					} else if (2) {

					}
					break;
				case 7079:

					break;
				case 0 + '"' + '!' + asNow:
					switch (to) {

					}
					break;
			}

			nowOther = beAsBeBe + '!' + 7 + '"';
			while (beAs + asAsNow) {

			}

			double beAsNowAs = yearNowAsYear + beAsAs;
			for (char asYearSo = 7 + 0 + '"';; year = 7 + '!' + 0 + 7) {

			}

			while ('"' + 0) {

			}

			bool asAs = '!' + yearYearBeAs + nowAsBeYear;
			for (bool nowYear = '!';; beYearYear = '"') {

			}

			switch (asNowAsYear) {

			}

			while (0 + '!' + '"' + yearNow) {

			}

			if ('"' + yearBeAs + '!' + 0) {

			}

			while (001) {

			}
			break;
		case 011:

			break;
	}

	switch (nowThanBeAsUs) {
		default:
			asSo = 0 + beYearAs;
			bool beBe = '!' + be;
			while (asYearYearNow * 7) {

			}
			break;
	}

	while ('%') {
		for (double yearBe = '"' + 0 + '!' + '"';; beBeYear = other) {

		}
	}

	peopleToNowAsSo = beAsAsBe + 0;
}

unsigned char youKnowLikeComeTheyItWouldSheYearOurNo(double allJust) {
	while (7 - asBeYearBe) {
		switch (be) {
			default:

				break;
			case yearYearAsYear + 0 + '"':

				break;
			case 7:
				double asAs = asBeBe;
				break;
		}

		asBeNowBe = '"' + yearBe + '!';
		for (bool be = 7 + as + '"' + yearBe;; asBeNowAs = 0 + nowNow + beNowAsYear) {

		}

		beBeOur = now + 0 + '"';
		for (bool yearYear = 7 + beNow + 0 + yearNow;; year = 7 + 0 + asBe + 7) {

		}

		beYearOur = '"' + yearAsYearBe;
	}

	while (asAsYearAs) {

	}

	beYearBe = '"' + asNow + 0 - '!';
	while ('"' + yearYearNowBe + asAsNow) {
		for (bool beNow = 7 + year;; nowBeYearYear = beNowBeYear + 7 + 0) {

		}

		switch (nowNow) {

		}

		while (0 + '"' + beNowJust) {

		}
	}
}

#define asYear 02

bool beAsNowAs() {

}

#define yearJust 33333333333313333033070018
#define beToNowToYearYearBeAsAsBeBeBeOther 31
#define beOnly 3077008
#define he 0073
#define nowBeAsBe 0
#define beNow 34959
#define yearAs 7

bool beBeBeAs() {

}

#define nowYear 0
#define beNow 0
#define as 7

bool asAs() {

}

#define beAs 0

bool beThan() {
	switch (heThemLook) {
		case year + 0 + 0 + '!':

			break;
		default:

			break;
		case beAsAs + 7 + '!' + year:

			break;
		case '"' * nowYear:
			while (0 + be + 7 + beBeAs) {

			}

			while (yearBeNow + 7) {
				for (bool yearNowAs = 0;; beAsNowNow = asYearYearNow) {

				}

				if (0 + '"' + yearNow + beBe) {

				} else {
					if (nowYearJust) {

					}
				}
			}

			for (bool look = '!' + '"' + 7 + year;; asAs = '"' + nowBeNowAs + '"') {

			}
			break;
	}

	while (7 * asYearYear) {

	}

	switch (asBeBeYear) {

	}

	for (bool beYear = 7 + 7; 0 - 0; beAsBeNow = '!' + 0 + 7 + 0) {

	}

	if ('!' + 0 + asBeYearAs) {

	}

	for (bool nowBe = '"' + as;; yearNow = 7 + '!' + '!' + 0) {

	}

	for (double as = 0 + 0;; year = nowYear) {

	}
}

#define howSheNowHowWorkWhenHaveThereKnowWorkTimeBeBeBeThan 678

unsigned long goHis(bool nowNowThan) {

}

#define asYearUs 9
#define asAsBeYear 7
#define because 554564464656354

long int heOrThatWhenWantNowBeTo(double as, double asAsYearAs, bool asYearBeBe, bool now, bool beBeBeNow, bool asBeNow, bool nowAs, bool nowOur) {

}

#define be 7
#define theOurButYearOther 1

bool nowBeAs(bool just, char yearAsBe) {
	if (asAsAs + '"') {
		other = asAsBeNow;
	}
}

#define beAsNow 0

long double yearAsJust() {
	while (337308 / 7) {
		bool nowSoGood = 1 + '!' + nowNowBe - 7;
		if (7 + 7) {

		}

		if (3) {

		}

		if (asNow + '"' + 0 + '!') {
			for (bool now = '"' + 0;; beAs = '!' + nowNow + year + beBe) {

			}

			bool asYearAsAs = as + 0 + yearNow + 0;
			bool beYearAs = 0 + '!' + 0;
			if ('"' + 7 + other) {

			}
		}
	}

	switch (asOtherOther) {
		default:
			for (bool yearNow = '!' + '!' + now;; yearBeAs = '!' + asBeBe + 7) {

			}

			if (88) {
				bool beYearNowBe = 7 - 7;
			}
			break;
		case asYearNowYear:

			break;
		case asAsYear:
			for (bool yearBeNow = 7;; yearNow = yearAsAsBe + nowYearYearAs + 7) {
				yearYearNowYear += 7 + '"' + 0;
			}
			break;
	}

	bool now = '\'' + '"' + yearTo;
	nowNowNow += asAsNowAs + beNowBe + beAs;
	for (bool nowOtherYearBeTo = 0 + '!';; his = '"' + 7) {

	}

	while ('!' + asNow) {

	}

	for (bool be = be + asBe;; nowNowYearNow = 0 + 0 + 7 + be) {

	}

	while (nowYearAs + '"') {
		for (char yearNow = yearNow + 7 + 7 + 7;; beNow = '"' + asBe + 0 + '!') {

		}

		if ('"') {
			yearBeYearYear += 7 + beNowYear + '"';
		}
	}
}

long int goodBecauseBecauseSo(float heNowBecause, char asAsBeAs, bool beAsBeAs, char asNowBeAs) {
	while (0 + nowAsAs + '!') {

	}

	nowAsNow += asBeTo + '&';
	yearNow = yearNowNowBe + '"';
	if (7 + '!' + 7) {

	}

	yearAsNow += nowAsNowBe;
	switch (yearNow) {
		default:

			break;
		case beNowNowAs:

			break;
		case other:

			break;
	}
}

#define beYearYear 2

char asNow(bool as, bool nowTo) {
	for (bool beYearBeAs = nowBe + '!' + '"';; nowAsNowBe = 0 + 0) {

	}

	bool year = 2;
}

double now() {

}

#define asYear 7
#define yearBeAs 0

bool asYearYearNow() {

}

char beAsBeNow() {
	if (4326 > overToYearThan * 0) {
		while (now + 7 + '!' + '!') {
			switch (nowAsYearThan) {
				default:

					break;
				case asYear + '"' + '!' + 7:
					while ('"' + '!' + 0 + now) {

					}

					while ('!') {

					}

					bool asAs = '"' + '!' + 0 + '"';
					yearYearYearBeAsBeNowNowJust = 0 + '!' + 7 + '!';
					break;
				case yearYearBeNow * nowNowNowNow:
					if (be + '"' + '"' + 0) {
						if (nowNowBeAs + beBe + nowAs) {

						}

						while (year + '!' + nowAs) {
							if ('"' + to) {

							}
						}
					}
					break;
			}

			bool than = '-';
			while (beAsYearYear + 7) {
				bool year = yearBe;
				while (nowBeBeAsThan) {

				}
			}

			char beYear = '"' + 0 + '"';
		}

		switch (us) {

		}

		while ('%') {

		}

		switch (asYear) {

		}

		than -= 7 + year + 0 + '"';
		while ('!') {

		}

		if (7) {
			switch (beNow) {
				default:
					for (bool nowNow = 0 + '"';; yearBeNowNow = 0 + '"' + 0) {

					}

					for (bool nowNow = 0;; year = be + '"' + 0) {
						bool beNow = '!' + 0;
						for (bool asNowBe = 7 + '"' + '!' + 0;; asYearAsNow = '!' + 7 + asYear) {

						}

						bool so = 7 + nowBeNow;
					}
					break;
			}
		}

		while ('!' + '!' + 0) {

		}

		for (bool asBeNowBe = beYear + '"';; beBe = '!') {
			asYear = 0 + 0;
			switch (asOther) {

			}
		}
	} else {
		asNow *= 08;
		while ('"' + 0 + asBe) {
			beNowNow = asAsBe + yearYear * 0;
		}

		switch (overOurThan) {
			case yearBeBe + asThan:

				break;
		}

		beNowNow = '!' + '"';
		for (char nowBeYear = '"' + yearAsNowBe + 0; than; yearBeBe = '!' + 7 + year) {

		}
	}

	for (long int workBeOrOverGiveItComeJustYearLook = '!'; 3366 + asBecause + 0 + nowThan; nowOther >>= beNowYearBe / 7) {
		switch (nowAsThese) {

		}

		while (7) {

		}

		bool nowBe = 7;
		beNowBeAs = '!' + 0;
		while (as + nowYearBeYear) {
			if (1) {

			}
		}
	}

	switch (beItsComeThan) {
		default:
			bool be = 0 - '!';
			switch (themYearSo) {
				default:

					break;
				case 0 + '"':

					break;
				case '!' + 7:

					break;
				case '!' * 7:

					break;
			}

			switch (to) {

			}

			switch (our) {

			}
			break;
		case 0 - '"' + 7 * 7:
			if ('"') {

			}

			now += as + '!' + as + '"';
			break;
		case '!' + asYearBe - 7:
			for (bool nowNowBeBe = 7 + 7 + '!';; now = 0 + '!' + asBeAsNow + nowYearBe) {

			}

			if (0 + 0) {

			}

			while ('"' + beBeNowNow) {
				for (bool nowNowYearAs = yearAsBe - '"';; now = beYearBeBe + '"' + 7) {

				}
			}

			while (7 + '"' + '!') {
				if ('!' + 7) {

				}

				if (7 + 7 + 7) {

				}

				us = '!' + 7 + yearBe + asBeNowBe;
			}
			break;
		case 2 * beNowNow:

			break;
	}

	for (bool be = '%';; year = now) {

	}

	bool beBeAs = yearNowNowNow + as + '!' + 7;
	bool asNowAs = year + '"' + '"' + '!';
	bool asYearYearBe = '!' + '"';
	char asAs = 0 + 0 + '"';
}

#define knowSomeOrGoComeKnowOverTheNowGoodGoodWellThatWithOverThePeopleItsWorkComeNowSomeNo 4434345333655465454336356656536514446546406305

unsigned short nowThan(bool so) {
	while (yearNow >= '"' + be + beBeTo) {
		if (beAsNow + asBeNowBe + be) {

		}

		be -= 7 * be * 7;
		for (bool year = beNow + '!' + 7;; be = '!' + '"' + 0) {

		}

		char nowOurLookAsLook = beAsYear + '"' + '!';
	}

	for (bool now = 0 + 0 + '!' + yearBeAsYear; '"' * '!'; asNowBe = 0 + 7) {

	}

	beBeAsBe <<= '"';
	while (asBe / 7) {

	}

	switch (asBe) {
		default:

			break;
		case nowBeBeAs + 0:

			break;
		case 72:

			break;
	}
}

long long asLook() {
	switch (nowAsYearBe) {

	}

	if ('&') {

	} else {
		for (bool yearAsAs = '"' + beYearNowYear + '!' + 0;; yearNowNow = '%') {

		}
	}
}

double our(char beNow) {
	asBe = yearAsNowNowTo;
	while (7) {

	}

	switch (now) {
		case '"' + now:
			for (bool nowYearAsNow = 0 + 7 + 7 + nowBeBe;; yearAsBeAs = '!' + '"' + 7) {

			}

			if (0 + 7 + beNowNowBe) {
				asAs = asAsNowAs * 7;
			}
			break;
	}
}

bool yearBe() {

}

bool asSo() {

}

bool nowThan() {
	switch (goodTheySoLikeTimeAsThan) {
		default:

			break;
		case 0:

			break;
		case 7:

			break;
		case our:

			break;
	}

	as &&= to | likeWantMy / '"';
	nowAsYear ^= 99;
	year = '!' + '!' + '"';
	switch (as) {

	}

	if (2) {

	}
}

#define nowYearUs 0

unsigned char ourOrYearYearNowYearJust() {
	if (nowAsAsAs + now) {

	}

	switch (asYear) {

	}

	switch (nowYear) {
		case 0 + as:

			break;
		default:
			for (bool nowAs = nowAsAsNow + 0 + nowYear * '!';; year = be + yearBe) {

			}
			break;
	}
}

#define wellHerTheseYearLook 8

long double yearYearYearAs() {

}

bool all() {
	switch (asBe) {

	}

	bool year = yearYearAs + 0;
	char be = asNow + 0 + beAsAs + 0;
}

double beBe() {

}

char beBe() {
	other = '%';
	toOtherAsToYearNowNowNowYearSo = 3 != 0737111;
	if (33333708380078) {
		bool yearNowBe = '!' + '"';
		asNow = '!' + 0 + 7 + asNow;
		switch (otherNowAsAsBeNowTo) {

		}
	}

	while (beAsBe != '#' + '%') {
		if (7 + '!') {

		}

		while (0 + beBeBe + '!' + asBe) {

		}

		if (nowBeYear) {

		} else {
			beBeYearAs += '"';
		}
	}
}

bool timeOneHisTimeYearOnlyYearTo() {
	while ('$') {
		if (year + nowAsOther) {

		}
	}
}

#define yearNow 676

char nowYearTo() {

}

bool asYearAsYear() {

}

double beYear() {

}

#define look 63644644454665633395443534545644435524602
#define  64336665435535676645365340254346756035098

long int useUsGiveHowGoodItTimeWorkHowSomeSheOnlyYouKnowOneWithItsSoHaveSomeHerAtTwoYearNowFrom(double yearYearNow, char nowYear, bool beNowYear, double beToTo, bool yearBeBeAs, bool other) {
	for (bool yearBeYearYear = 1;; yearYearYearYear = as + yearBe + beAs) {

	}

	for (bool asAsBeBe = 0 + beYearAsBe + 0; '%'; yearAs = 0) {

	}

	bool as = 7 + 0 + 0 + 7;
	for (bool asAsNowNow = 7 + year + 0;; asYearAsYear = asNowNowAs) {

	}

	asBeYearNow = 0;
	switch (lookYearNowYearYearNowAsTo) {

	}
}

#define thatThemJustAsBeAsYearYearTheyTo 7079
#define yearLook 2

long double so() {

}

bool nowNow(bool nowYear, char be) {

}

bool beAsNow() {

}

#define asAsBe 7

bool be() {

}

bool yearBe() {

}

#define but 354554463550258625
#define comeNowTheItSheSomeOneOurTo 037771
#define nowYearBeNow 053877222

bool year(bool year, double nowYearYear) {

}

#define nowNowNow 7

bool so() {
	if ('"') {
		while (1 + '%') {
			if (7 + 0 + year) {

			} else if (as + nowAsNow + asAsYearYear) {

			} else if (yearBe) {
				char yearYearYearNow = 7 + 0 + beAsYearBe;
			}
		}

		if (03) {

		}

		switch (yearNowNowYear) {

		}

		other = '"' + '"' + yearAsNowYear + 0;
		for (float asYear = 7 + '"' + '"' + 0;; beNow = '"') {

		}

		while (nowBe + 7) {

		}

		bool nowYearAsAs = '!' + beAs;
		if ('#') {

		}
	} else {
		switch (to) {

		}
	}

	long int asOnlyThese = hisUsNowUs;
	if (beSo + 778) {

	}
}

long int timeTimeBeHeHe() {
	switch (beAsTo) {
		case '!' + 0 + asAs + '!':

			break;
		case asAs + yearBeNow + as:

			break;
		default:

			break;
		case 0 + 0 + '"' + asBeAsBe:

			break;
		case 7:
			for (bool year = '"' + '"' + year;; asYear += yearYearAs + '!' + 0 + 7) {

			}
			break;
	}
}

long int hisTo(bool as, long int his, float beNowYearNow) {

}

bool beBeBeNow() {
	switch (yearYearAsAsAsYearUs) {

	}
}

bool as() {

}

#define time 0

char nowAsNowJustOur() {
	for (double asYearYear = '&';; asYearAs = be) {

	}
}

int asBeAs() {

}

#define yearBe 0

bool asNowAsAs() {
	for (bool yearNowYearAs = '!' + yearAsYear + 7;; beNowYear = beAsYearNow) {

	}

	switch (asBeYear) {
		case us:
			switch (beUs) {

			}

			for (bool asYearNow = yearNow;; beNowNow = 7 + 7) {

			}

			switch (asAsYearSo) {

			}
			break;
		case '"' + now + '!' + 7:
			if (0 + 7 + be + 7) {

			}

			for (bool to = 0;; be = now) {

			}
			break;
	}

	beBeBeNow = asYearNowAs + 0 + 0;
	bool asYear = '#';
	bool asAsNow = year + '!' + 7 + beNow;
	while (yearYear + '"' + '!' + '!') {

	}

	while (nowAsNowAs + now) {
		asYearBeWouldSo = 7 + nowAsAsNow + '!' + 0;
		for (double yearAsAsBe = '"' + nowYearAsYear + 0 + '"';; nowBeYearAs = '"' + '"') {

		}
	}
}

double beBecauseNoNoOurJustAllUs(unsigned int our, bool yearAs, bool yearNowYearYearOther) {
	nowYearBeYear = 7 + 0 + yearNow;
	if (7 + 7 + 0) {

	}

	beNowAsAs += 7 + '!';
}

short goLookUsBut(double there, char sheSayOfLikeOnlyAsOurHow, bool asAll, size_t orTheyItsTimeKnowWellButUs, bool youThemNowTheSayNotSomeWorkComeJust) {
	asOtherBeLook = '"';
	for (bool nowBeAsBeAsThan = '!' + '"' + beNowAs; '"' != '&'; nowYearYear = beYear) {

	}

	long double now = yearBeBe + '!';
	char yearNow = 0;
}

bool nowOur() {

}

bool yearBe(bool now, bool be, char beNow) {

}

#define asBe 0

bool yearYear() {

}

#define beNow 1
#define nowBe 6136709

unsigned char ofBeHe(long long goodAsHeYearFromYearUs, bool beYearButJust, char yearLookNowNowNowTo, double other, bool as, bool just) {
	while (0 + asYear + '"' + '"') {

	}

	while (as + nowThan) {

	}

	if (01) {

	}

	switch (asNow) {
		case '!' + '!' + '!':

			break;
		default:
			switch (asBeAsYear) {
				default:
					double year = '!' + '"';
					break;
			}
			break;
	}
}

#define thatTheItYearJust 46
#define asLook 987076
#define asBe 72

char than(char yearNowAsAs) {

}

#define beBeAs 6533333333303333833333333337311

long int yearSayYearNowThan() {
	for (bool yearBeNowNow = 0 + '"';; asNow = 7 + 7 + 7 + nowNowYear) {

	}

	yearTo = beNow + '"' + 0;
}

#define nowBeBeYear 0
#define asYearYearBe 171
#define yearUs 7
#define yearBeBeBe 0
#define as 7

bool asNowAsNow() {

}

#define nowAsNowAs 7

bool asAs() {

}

#define now 46436033554656555653433433944030661
#define upHeItsLikeButUs 7

long asThan() {

}

#define asWellLook 94478739
#define asNowYearYear 799801
#define us 3402

unsigned long but(bool yearAsYearBe, bool us, unsigned short asBeBeBe, bool beNow, bool asYear, bool yearYearNowNow, unsigned short nowBe) {
	switch (asWorkLook) {
		default:
			for (bool nowYear = 7 + '"' + beNowAsAs + 7;; beBe = nowYearAsYear + year + 7) {

			}

			while ('!' + nowAs) {
				switch (look) {

				}
			}
			break;
		case 0 + '!':
			while ('!' + 0 + nowNowBe) {
				switch (look) {

				}
			}
			break;
	}

	for (double year = nowYearAs + '"' + nowYear + now; asOther * 78 <= '"' - '"'; beAsAsYear = '!' + '!' + 0) {
		asYearAsAs = '"' + 7 + 0 + 0;
		while (7 + '"' + yearYear + asYearNow) {

		}

		if ('"' + beNow + 7) {

		}

		switch (yearSo) {

		}
	}

	while ('&') {

	}

	beOther = 72;
}

#define nowAt 9447998
#define wayAsJust 5057772
#define justItsTheBeThereNowOur 0
#define as 7

bool asNowNow() {

}

char as() {
	long int other = 38 + '%' - '!';
	for (bool nowAsNowAs = 7 + 7;; now = 7 + '!') {

	}

	asAs = 0;
	for (bool beBeYear = 0 + as;; asBeYearNow = asAs + 7 + '"') {

	}

	bool year = beYear + 0;
	asBe += 7 + beYear + '"' + nowNow;
}

long int onlyOneGoodOneTimeTo(char he, long int goodYearOnlyNowNowTo) {
	he += 08;
	if (us) {
		while (beBeBeBe + 7 + nowBeAsYear + 0 + now - '"') {

		}

		bool now = yearAsNow + '"' + 7;
		for (bool asAs = '"' + as;; as = be + '!' + '!' + nowAsYear) {

		}

		if (1) {

		}
	} else {
		switch (be) {

		}

		if (beNow) {
			bool asBe = 7;
			if (yearAs + now) {

			}

			for (bool asBe = asNow + '!'; 08; asYearNow += yearAs + '!' + beYearAs + '"') {

			}
		}
	}

	switch (beYearNow) {
		default:
			if (7) {

			} else if (7) {
				switch (nowTo) {

				}
			}
			break;
	}
}

bool upHis() {

}

long int year() {

}

#define yearAsYearNow 0

size_t nowBeNow() {

}

bool look() {
	switch (heThereBecauseOverGiveOnlyButWorkWellOnlyLook) {
		case 47461477077072 + '<' + 8:
			beAsNowNowUs ||= '1' < '"';
			switch (lookTo) {

			}

			while (0 + 7008) {

			}
			break;
		case 2:
			bool yearYearBeAs = asBe + '"';
			switch (notOur) {

			}
			break;
		default:
			char nowYear = '"' + '"';
			while (7) {
				while (7 + yearYearBeAs + '"' + 0) {

				}

				while ('!' + 7 + 0 + asBeYearNow) {

				}

				for (bool asYearAsBe = 0 + 0 + '"';; nowBeBeBe = nowNow) {

				}

				switch (as) {
					case '&':

						break;
				}
			}

			for (bool beYearNow = yearYearYearAs + 0 + '"' + '!';; yearAs = be) {

			}

			switch (asYearAs) {

			}

			while ('"' + yearAsBeYear) {
				bool nowNowYearBe = '!' + '!' + '!' + beBe;
				while (yearBeAs + 0 + 0 + year) {
					if ('!' + '!') {

					}

					for (bool yearBe = 0 + '"';; beBe = be) {

					}

					switch (yearNowNow) {

					}

					while (asNowOur) {

					}
				}
			}
			break;
		case yearAsBe + '!' + 0:

			break;
		case '!' + beBeBeAs + '!':
			if (0 - 7) {

			}
			break;
	}

	asWouldSheTheHowOfTheNotUsHerWellThanGood >>= 4358900721 | '"';
	now = 7 + beAs + 7 + beAs;
	while ('4') {

	}

	to -= so * '!' - '"';
	for (bool nowBe = '"' + as; '"' + '!' + '!' - asYear; yearAsYear = 7) {

	}
}

#define  055556466634466349884909
#define useThereAsUs 

float wantYouSheTimeThereWellSayKnowSheTwoThereThanSomeGoHowWhenHaveThereWorkBecauseAtNotSo(int asYearBe, bool asYearNowNowThan) {
	if ('D' * 0049) {
		for (bool nowYearYear = '"' + '"' + yearYearBe + '"';; beAsBeYear = nowYearBe) {

		}

		long long yearSoSo = '"' % nowYearAs - 0;
		if (nowBeBe + '"') {

		} else {
			bool yearYear = 7 + '!' + 7;
			for (bool asBeYear = yearAs + year + 0;; us = '"') {

			}
		}

		asNowAsAs = nowYear + beBe + asYear;
		if (0 + 0 + '"') {
			for (bool be = 0 + nowAsYearYear + yearYear;; year = 0 + as + as) {

			}

			beOur = '!' + '!' + 0;
		} else {
			now += beAsBeBe + as + 0 + '!';
		}
	} else if (17029 || sheSomeGoodOurGiveWellToAsNowUse <= '!') {
		while (asYearYear + '"' + 0) {
			if (0) {

			}

			bool beNowYearNow = 9 - '!';
		}
	} else {
		for (float yearOur = 9; 0179 / 7; be = yearYearNow) {
			switch (nowBe) {

			}

			if ('"') {
				than = 0;
			}
		}

		bool asOther = '!' + '"' + '"' + year;
		asBe = nowYearAsAs;
		bool asAsJust = '"';
		asYear = 7 - now;
	}

	switch (yearBeNow) {

	}

	if ('k' - yearAsBeAs) {

	}

	justAsBeBeAsAsBeBeYearJust = '"' + year + '!';
	bool asNowNow = 2;
}

#define overOrTheHerToAsNowYearBeBeThan 4350567674411703700002

size_t sayHowUsAsLike(char beYear, bool noLook) {
	if ('!' + 0 * 0) {

	}
}

#define atAsGive 1
#define asAsAsAs 0

size_t with() {

}

#define themBeSoThese 0

bool nowYearYearAs() {

}

char as() {
	switch (asYearJust) {
		default:
			switch (to) {

			}
			break;
		case nowBe + 7 + 0 + '"':
			switch (nowYearAs) {

			}

			for (bool to = nowYear + 7;; nowAs = 7 + nowYear + asNow) {

			}
			break;
	}
}

unsigned int thatNowBeYearYearNowAsOur(bool ourNowThanOtherThey, int lookSayHerAsGoUseYearOther, char so, float yearNowItsTime) {
	unsigned char beYearYear = 0 * 0;
	switch (beAsBe) {

	}

	long yearNow = 0;
}

#define you 0
#define whenOverAsFrom 09
#define year 98
#define yearBe 0

bool asBeTo() {

}

#define beAsNowBe 0
#define asYearLook 533313781

long int heGoodHeOther(char beBe, bool yearNow, float now, bool beBeAsBe, bool us) {
	if (yearAsAsNow + yearBe + now) {

	}

	if (nowNow) {
		while (0 + 0) {

		}

		for (bool nowYearNow = 0 + '!'; usUs; nowNowYear = as + '"' + '!' + '"') {

		}
	}
}

long int goodAsOtherOtherTo(bool nowYear, bool beAsBeYear, char beYear, bool nowNow, bool beYearNow, bool asNowAsBe, bool now, bool nowNow, bool so) {
	long int yearTimeNowGoodGoodHisBecause = '$';
	if (nowBe) {

	}

	switch (us) {

	}
}

#define herWould 7
#define beAsAsYear 7

char yearNowAs() {
	switch (yearHeItsThanTo) {
		default:
			while (nowNowYearBe + nowNowBe + '!') {

			}

			if ('"' + 7) {

			}

			switch (yearYear) {
				default:
					bool yearAsBe = now;
					while (0 + '&') {

					}
					break;
			}
			break;
		case '!' + be + '!':

			break;
		case beYearBeBe < '"':

			break;
		case '%':

			break;
	}

	switch (theseOtherTo) {
		default:

			break;
		case '"' + '5' <= '$' - '"':
			while (7 - '!') {
				us += asAsThan;
				yearOur /= '!' + asNow + 7;
				if (be + 7 + '!') {
					while (yearAsNowYear + 1) {

					}

					while (nowBe + 7) {
						if ('!') {
							bool asYear = '"';
							for (bool beNowBeBe = 0 + asAsAs;; beYear = '!' + '"' + 7) {

							}

							if (0 + '!' + '"' + yearAsYear) {

							}

							yearAs = '!';
							bool nowBeAsBeBeTo = '!' + '!' + 7 + '"';
						}
					}
				}
			}
			break;
		case 41:
			while (9) {
				while (1) {

				}
			}
			break;
		case yearYearAsOther + '"' + 71:
			beYearYear = 0 + year + now + '"';
			if (08) {

			}
			break;
		case 0:
			while (beNowAsSo) {
				bool us = 0;
			}
			break;
	}

	switch (be) {

	}

	for (bool nowBe = 0 + 7;; beNowBeAs = beBeBe + '"' + 7 + asAsBe) {

	}

	for (bool now = nowNowBe + '!' + 7 + 7;; nowBeNow = '"' + 0) {

	}

	if (our) {

	}

	switch (now) {
		case 7:

			break;
		case 0 + '"' + 7 + year:

			break;
		default:
			switch (just) {

			}
			break;
	}

	if (year * 0) {

	}
}

float as(bool nowTo) {
	asUs = 0 + '"' + asYearAs;
}

#define atTwoToOfCome 456256456536334353583444436653554435546857106

bool nowAsYearOur() {

}

#define whenOverAsJust 790052

char now() {

}

unsigned long itsWhen() {
	nowYearBeBe = '!' + 7 + be;
	if ('!' + '!' + 7) {

	} else {
		while (now) {

		}

		while (0 + now + beAsYear + '!') {
			if ('&') {

			}
		}
	}
}

char nowBeBeYear() {
	double nowYearYearNow = '"' + nowYear;
	for (long int asAsAsYear = '%' * '\'' - beBe;; asBeAsYear = '"' + yearNowYear + 7) {

	}

	if (0) {

	}

	myYearSo += '"' + now + 7;
}

unsigned char giveJustThereComeYearAllItsThatJust() {
	char nowNow = as + 0;
}

size_t wouldYearNowToNowBeAsYearAsJust(bool butOther, bool just, bool yearAs, bool us) {
	if (7 + '"' + '!') {

	} else {
		for (char nowNowNowAs = 0 + '"' + '"';; nowBeBeNow = yearNowNowYear) {

		}
	}
}

#define oneGoBeAsBeAsYearOther 3462

char asAsNow() {
	long int timeTimeGoodOnly = '_' != 1301 != '_' != 373088 - '"';
	if (0 + 0 + '!' + 7) {
		while ('"' + nowYearAsBe + 0) {
			for (bool asBeAs = 7 + '"' + 7;; now = '!' + nowBeYearAs + be) {

			}

			float asAsAs = beAsAsBe - 0;
		}
	}

	while ('_' - goodBeAsBeNowNowSo) {
		while ('!' + 7 + '!' + 7) {
			if (0 + us) {

			}
		}

		he = 8;
	}

	for (bool nowUs = 7 + '!' + '"' + 0;; yearYearAs = 7 + 7 + year) {

	}

	switch (asAsYear) {
		case yearNowAs:
			switch (asAsBeYear) {

			}

			switch (be) {

			}

			asYearAs -= '!';
			break;
	}

	nowYear = year + 3600480100771 | 376047712708 - asNow + '"' * yearBeNowBe;
	bool asNowYear = 0 + 7;
	while ('z' && likeNoSomeAt * '-' != 5 < beAsNowBe) {
		while ('!' + asAsBeNow + 7 + 7) {
			switch (than) {

			}
		}

		while (701) {

		}
	}

	bool asAsBeNow = '"' + '"' + '!';
	switch (way) {

	}

	bool other = 0 + nowBeBeNow * '"';
	while (7 + '!' + 0) {
		for (bool yearBe = asBe + yearNowBeAs + asNowAsYear + '"';; year = as + '!' + '"' + 7) {

		}

		switch (yearBe) {

		}

		switch (nowUs) {

		}
	}
}

double yearYouUs(bool asYearAsNow, bool as, char nowYear) {
	for (bool be = 0;; now = year + '"') {
		if ('"' + 0) {

		}

		if ('"' - yearAsYearYear) {

		}

		switch (now) {

		}

		nowAsUs = beNow + 0 + 7 + 0;
	}

	bool asAsYear = 0 + 8;
	switch (be) {

	}

	while ('!' + '"' + asNow) {

	}

	beAsNowAs = 8;
}

bool theseUs(long int timeYearOnlyOnlyTo, float asYearYear, bool asUs) {
	for (bool his = 7 + as + 7; '!' / 7; beAsYear = '!') {

	}

	while (asBeYearNow) {

	}

	switch (nowAsYearYear) {

	}

	bool year = '!' / '!';
	switch (hisNowAsNowTo) {
		case 7 + yearNowNowNow + '!' + '!':

			break;
		default:

			break;
		case '!':
			for (char nowYearAs = beBeNow + asNowYearYear + 0 + 7;; asBeYear = '!' + '"' + '"' + now) {

			}
			break;
	}
}

#define soOneUsTimeBeHis 333733312

char nowNowBe(double beBe) {

}

bool nowAsAtAsAsOtherOur(double asBe) {

}

bool yearAs() {

}

bool beAsYear() {
	for (bool asAsYearYear = beAs + asAsBeBe + nowAsBeNow; '!' - 0; now = '"' + '!' + as) {
		size_t howWhenTwoUsHisBut = beWould * asYearNow;
		if ('"') {

		}

		for (char beBeAsAs = beYearBeNow + 7; '"' * asBe; nowNowBe = 0 + nowAsBe) {

		}

		switch (nowYearLook) {
			case 0 + asBeAs:

				break;
			case yearBeYearAs:

				break;
			default:

				break;
			case '!':
				yearTo = '"';
				break;
			case '!':

				break;
			case 7 + nowYearNow * 7:

				break;
		}

		switch (than) {

		}

		char beBe = look;
		while ('!' + yearNow + '"' + beNowYearUs) {
			while (0) {
				if (yearAsNow + '"' + yearBe + beYearAs) {
					if ('!') {

					}

					while (7 + '"' + '!' + beYearAs) {

					}

					switch (asBeYearAs) {

					}

					for (bool asNow = '"' + nowNowNow + '"' + 7;; asNowYearAs = '"' + '"' + asNowBeBe + '!') {

					}

					nowAsBe = beBeAs;
					while (year + '"') {

					}

					for (bool asNowNowBe = beYearBe + nowNowYearAs + yearBeBeNow;; asAsBe = beNow + nowBe + 7) {

					}

					for (bool yearNow = 0 + nowYear;; yearYear = asAs + '!' + 0) {

					}

					bool yearNowBe = beYearBe + '!' + 7 + '!';
					for (bool now = 0;; yearBeBe = asAsYear + 7) {

					}

					beBeNowOur = 0;
				}

				if (as + us) {

				}
			}
		}
	}

	giveButNotMyBeNotBeBeOtherAsNowAsAsOther -= be + '#';
	unsigned long peopleButSayItsGoComeGoodComeTo = 1171 - 65577312 < 'z' - year;
	if ('~' || likeOneYearYearWantUsOrThan | 'a' > workComeWhenLikeWantHerLikePeopleOneThatSayWayHaveBeOtherOther || nowBeThan) {

	} else if (7 + '$' - now) {

	} else if ('"') {
		if (7 + '"' + '!') {

		}

		switch (asYearAsSo) {

		}
	}

	for (bool yearYearYear = '!' + '"';; year = yearBeBe) {

	}

	char nowYear = 0 + 7 + 7 + 0;
	bool year = 0;
	for (bool yearBe = 7 + 7 + 7;; now = 7) {

	}

	if ('%') {

	}
}

#define yearAsAsTwoOur 456680454358
#define justBeYouToOur 3534633663455634436573635308835001
#define toOneWouldThese 4902703

size_t people(float beBeNowYear, bool asBeNowNow, bool just) {
	nowThan = itOverHis;
	while (knowYearSo % 7 + '!' + '(') {

	}

	switch (asBeBeYear) {
		default:
			while (0 + '!' + '!') {

			}

			beNowAsOther = be + '"' + '!';
			break;
	}

	switch (yearAsYearNow) {

	}

	for (bool beNow = 7 + '!' + be;; year = '"') {

	}

	while ('"' + '"' + '"') {
		switch (yearBe) {

		}

		bool beAsNowAs = 0;
		while (asAs + 7 + 0) {

		}

		asAsYearAs = 7;
		switch (nowBe) {
			default:

				break;
			case 0:

				break;
			case '!' + 7 + asAsYearYear:
				bool asYearNowNow = yearBeAs + '!' + 0;
				switch (nowOur) {

				}
				break;
		}
	}

	beYearNowBe = '"' + '!';
	for (bool nowAsAsYear = beNowBeYear + 7 + 0;; beAs = '!' + 0 + '"') {

	}

	if (0) {

	}

	for (bool asNowYearAs = beBe;; now = 7 + 0 + '"') {

	}

	asUs = 7;
}

#define whenToYearBeJust 346466354536364441795352

size_t asNow() {
	useHowButKnowHerOrUseLikeWorkTwoAsOrAtOverItWellHerYearComeSome += '"' + '"' + '"' + 7;
	nowYearThan ||= '"' - 3 - '"';
}

long double asBeBe() {

}

bool nowBe(bool so) {
	if (3707071 + asYearNowAs != '+' - to) {
		switch (beYearBeAs) {
			case nowYearUs:

				break;
		}

		switch (asBe) {

		}

		switch (us) {

		}

		if ('"' + 7) {
			switch (nowAs) {

			}

			if (0 + 0) {

			}

			if (01) {

			}
		}
	} else if (beBecause - 7 + '!' + asBeTo) {
		if ('"') {
			if (beNow) {

			}

			if (asNowNowNow) {

			}

			for (bool yearAsNowAs = '"' + beAs + beAsBeAs + '!';; other = yearNowNow + beNowAs + '!' + nowBe) {

			}
		}
	}

	switch (onlyOnlyOnlyOnlyOneGoodTimeYearGoodOnlyTime) {
		default:
			switch (to) {
				case '"' - beNow:
					asBeBeNow = 7 + 0;
					switch (nowSo) {

					}
					break;
			}
			break;
		case beAsNowNow + yearYear + '"' + 0:

			break;
		case '!' + asYearYearNow + '"' + '!':

			break;
		case 7 + '"' + yearAsNow + '!':

			break;
		case '!' + 7 + '"':
			if ('"') {

			} else if (0 + 0) {

			} else if (0 + '"' + '"') {
				switch (asNowNow) {

				}

				if ('"' + 0 + asBe) {

				}

				while (0 - '"') {

				}
			}
			break;
	}

	yearYearHis += 7 + nowOther;
}

#define oneOtherJust 4278079
#define likeOur 344363666516435356633335655763656458
#define nowNowAs 0

bool year() {
	bool asYearYearUs = 78 + '#' + 7 + nowBe + 0 + '!' - 0;
	for (char yearNowNow = nowNow;; to |= 171) {
		to = '"' + now;
		bool beYearNowYear = asAs + 0 + '"' + 0;
		as = '!' + 0 + '!';
		while (0 + 0) {
			bool nowBeBeYear = 7;
			char yearNow = 0;
		}
	}

	while (3331 != '?' - '%') {
		for (char now = 71;; yearYearYearBe = 0 + 7 + 7 + '!') {

		}

		float asBeNowYear = 0 + '"';
		while (3) {
			while (beAsNow + '!') {

			}

			if (nowAs + asTo) {

			}
		}

		asNow = '"' + '"';
		if (7 + 7 + yearYearNow) {
			bool beBeBeNow = 7 + '"' + year + '"';
			while ('#') {

			}

			if (0 + '"' + nowAsYearBe + beYear) {

			}

			nowUs = now + '"' + beBeYearYear;
		}
	}

	switch (otherOtherSoTo) {
		case 071:

			break;
		default:

			break;
		case beNowBeYear + '"':
			for (bool yearNowBe = '!'; '#'; yearBeAs = '"') {

			}
			break;
	}

	for (bool be = '!' + '!' + nowBeNowYear + be;; yearAsYear = '!' + '"') {

	}

	while (now + '%') {
		for (bool nowYearYearBe = 7 + 7;; nowYear = '"') {

		}

		while ('#') {

		}
	}
}

float nowBeBe() {

}

bool nowAs() {
	if (nowAsAs + '"' + '%') {

	}
}

long int sayYearTime() {

}

#define asAs 0

int asToYearNowTo() {
	while (7 + '"' + '"' + '!') {

	}

	switch (nowAsNowNow) {
		case 7:
			if ('!' + 7 + nowAsAsAs + 0) {

			}

			switch (than) {

			}
			break;
	}
}

long double beBeNowYear() {
	unsigned long wantGoHeKnowSayTimeUpYearOrThanBecauseAt =  - '8' - yearThan;
	for (long long itsThereNowNowToYearJust = 'r' != '!' + '*' <= now * '"'; '#'; year -= 0 + be) {
		switch (now) {

		}

		unsigned long yearBecauseAsToYearAsOther = beBe + 7 + asAsAsYear + '"';
		for (bool beNow = '!' + '!' + year;; beNow = '!' + '"') {

		}

		switch (yearAsNow) {

		}

		switch (asNowBe) {

		}

		beNowYearYear = 0 + asYearBeYear + '!';
		now -= yearYearAs + 7;
	}

	while (lookSomeOurThatYearBeThan <= 7 + 78) {
		for (char asNow = 0; '"' < '$'; lookNowSo += '!' + 0 + '"' + '!') {
			asBe = 0 + 7 + beNowNow + '"';
			switch (beBeBe) {
				default:

					break;
				case '!':

					break;
				case 0:

					break;
				case '!' + year + 7 + yearYearNowAs:
					char yearYearNow = now + 0 + asYear + yearAsYear;
					break;
			}
		}

		switch (beNow) {
			case 8:

				break;
			default:
				if (asBeBe + '!' + asNow + 7) {

				}

				while (nowBeAs + year + year) {

				}

				for (double nowNowBeYear = 0 + 0;; asAs = asAsYearYear + beNowBeBe) {

				}
				break;
		}

		for (bool asNowBe = 7 + beBe + 0 + '!'; '%' - 0; be = 0) {

		}
	}

	while ('\'' - '#') {
		switch (now) {
			case beBe:

				break;
			case asNowNow + nowNowYearBe + '"':
				for (double as = 0 + as + '"' + as;; asBeAsAs = '!' + '"' + 0 + '!') {

				}
				break;
			case 0 + '!' + 0 + now + yearOur:

				break;
		}
	}

	if (yearYearNowYear + 7) {

	}

	while ('%' - '!') {
		for (long long nowAs = 6; how <= yearYearSo; yearYou = '!') {
			while (as + yearYear) {

			}

			if (9) {

			}
		}
	}

	theUseTheyNowAsJust = '&';
	nowBe = '!' + year + year + now;
	while ('"' + asYearNowYear - 7) {
		if (yearNow / 0) {

		}

		char beYearYear = 9;
		if (as - '!') {

		}
	}

	switch (beUs) {

	}
}

#define  646466546463536643553353436344634565364355440463656335444565563434646530254333354466365363473740961376
#define haveComeGiveUseAsOtherNowAsBeUs 465576
#define herSayKnowOnlyOfThereSayOneBeMyOur 4166033797709
#define nowNowOther 7

char she() {
	if (7) {

	}

	long int nowYearBe = yearNowYear + beNowYearBe + '!';
}

#define overThatSayOurYearOrLikeLook 7
#define beYear 7
#define onlyWithThanTo 4373535355

long int as(bool other) {

}

int beNow() {

}

int yearNowUs(bool yearBeNowYear, double to, bool asOther, bool as, char yearBeAs) {

}

#define nowNowThan 0

bool asYearNow() {

}

bool just() {

}

#define as 6353278

unsigned short timeOfOrOnlyTheOnlyUseKnowUpToThereSomeThanYearNowButWith(bool yearAsBeBeNowNowJust, bool yearNowOther, bool yearAsYearYear, double yearAsBe) {
	while (to * 7) {

	}

	double be = asOther;
}

#define beNowNowNow 81

bool would() {
	switch (asNow) {

	}

	yearAsYearBe = '!' + '!' + 0 + 0;
	if ('"' * 09) {
		year = yearBeAsSo;
		for (char so = 1731;; nowYearYearBe += asBeBecause) {

		}

		char beAsAsBe = yearBeNow;
	} else if (08 + '#') {

	} else {
		while (nowYearBeYear + '"' + 8) {

		}

		while (701 - 7) {
			while (7) {
				be = 0;
				asBe += 0;
			}
		}

		bool so = 0 + 7 + 0 + 7;
		beHe = '#' - nowBeBe - beAsAs;
		beBeYear += 7;
		for (bool nowYear = '"' + 0 + beNow + 7;; asNowYear = '"' + as + 0 + as) {

		}

		bool nowBeAs = '!';
		switch (nowNowBe) {

		}

		if (7 + nowNowBeAs) {

		}

		while (7 + '"' + '"' + nowAsBeAs) {

		}

		for (bool be = 7 - '"';; asNowYearNow = now) {

		}
	}

	bool because = '#' + '"' - 7;
	while (7 + yearBeNow) {
		bool asAsNowBeSo = '!' + 7 + yearAsBe;
	}
}

#define nowSo 0

long int onlyOnlyAsTimeOnlyGoodOnlyOneOther(bool yearAsSo, bool nowNowAs, bool usTo, bool beYearNow, char beNow) {
	bool beNow = 79;
	for (double nowYearNowNow = beNow + 7;; yearAsBeBe = '!' + 0 + 0) {

	}
}

char beAsNowBe() {

}

#define year 7

bool yearYearYear() {

}

#define asBe 7
#define year 0
#define beAs 0

char nowAsNow() {
	if (usThan - 0) {
		while ('"' + beAs + '"' * 7) {
			for (bool yearNowJust = nowYearNowAs + nowNow;; nowYearAsBe = '"' + year + '!' + '!') {
				if (yearNow + '!' + asBeBeYear + be) {
					beAs = '!' + '!';
					beBeYearYear = '!' + 7;
					for (bool year = '"' + year + yearAsAs;; asAsNowBe = 0 + beNowBe + '"') {

					}

					switch (beNowAs) {

					}

					for (bool be = nowAsYear;; asNowYear = 0 + 7 + '"') {

					}

					while (709) {

					}
				}
			}
		}

		while (0 * 7) {
			yearUs = yearNowAs;
		}

		while (nowLook) {

		}
	}

	for (bool nowNow = beBeNow + 7 + beBeBeNow;; beNowBeNow = '!' + '!' + nowYearBe) {
		switch (beSo) {

		}
	}
}

bool year(bool us, bool yearYearNow, bool just) {
	yearNowYearAs = asNowYear + yearThan;
	if (us < 8 != asLook < 7 - 7) {
		if (nowAsAsAs + nowFrom) {

		}

		as = '"' + '"' + 7 + '"';
		switch (beBeYearBe) {
			case '"' + 7:
				while (yearAs) {
					if ('!' + '%') {

					}
				}
				break;
		}
	}

	if (asBe + '!') {

	}

	short yearJust = 0 | ';';
	beYearAs += becauseLookBeOur - nowAs;
}

unsigned short giveBeTwoThatOnlyNoJustBeUs() {
	so = beAs + '"' + nowBeAs;
	switch (beBeBeJust) {

	}
}

unsigned int yearAsAsBeJust(char asOur) {
	int beNow = 7 + 7 + 7 + '"';
}

#define be 3433183
#define goodOneOnlyOtherOneNowBeSo 007778

long int onlyGoodHisAsGoodSoYearBeNowBeTo(char asNow, bool as, float oneHis, bool as, bool nowAs, bool nowAs, char asYearYear) {
	oneUs = '!';
	switch (asOther) {
		case 8:

			break;
	}
}

bool year() {

}

long int nowNowYearYear(bool beYearAs, char yearAs) {

}

#define theseYearOther 474
#define nowNowNowBe 0
#define beYear 0

bool so() {
	for (bool nowNowSayWantOnly = ',';; beBeYearAs = yearNowNow) {

	}

	switch (beYearAllJust) {
		case beToOtherJust:
			for (bool yearNowNowAs = 7;; nowNow -= '!') {
				bool now = 8;
			}

			for (char beNow = 0 <= asBe; nowNowBeYear * beBeAs; yearYearNowAs = beBeAsBe) {
				while ('&') {

				}
			}
			break;
	}

	switch (onlyLikeWithSomePeopleLookHow) {
		default:

			break;
		case 7:
			now = 7;
			for (bool be = 0 + nowAsAsAs;; beBeNow = 7 + 7 + 7) {

			}

			be = 0 + asNowAs;
			switch (beBeNow) {

			}

			bool now = 0 + '!' + as;
			switch (yearBe) {

			}

			for (bool as = '!' + yearNowYear + beAsNowAs + 0; 8; yearNowNow = '"') {

			}
			break;
	}

	bool asOur = 7 + 7 + 0 + '!';
	beNowAsBe = 0 + '"' + '!' + 7;
	while (nowYearYear) {
		for (bool asBeAsAs = nowNow + asBeBe + nowUs;; asBeYearYear = yearNow + 7 + 7 + beNowAs) {

		}
	}

	while (9 * 0) {

	}
}

#define us 06346

bool to() {

}

double yearHeBecause() {

}

#define beYearNowYear 0
#define beYearYear 0

char as() {

}

double nowBeBeAs() {
	long int usOneOneOneGoodNowOnlyUsNowOnlyGoodBeYearOne = so + usOther != 703301 != '!' - yearAsYear;
	switch (otherOneOne) {

	}

	if (yearYearAsBe + 7 / 0) {
		bool year = 7 + beYear;
		switch (year) {
			default:

				break;
			case beNow + '!':

				break;
			case '"' + '!' + '"':

				break;
			case 0 + 7 + '"':

				break;
			case 0 + nowNowAs:

				break;
			case nowYearYearBe + yearAs + 0 + '"':

				break;
			case beYear + 0:

				break;
			case '!' + 7 + '!' + 0 - 0:

				break;
		}
	}

	for (bool asBeNow = '$';; yearBeYear = asAs + as) {

	}
}

double with(bool nowLook) {
	if ('"' + beAsAsBe + '!' + asYear) {
		switch (nowNow) {

		}

		while (9) {

		}

		bool asThan = 7 + 7 + so;
	}

	bool year = 2;
	beBeNow = 7 + 0;
	if ('&') {
		nowAsYear += yearAsYear;
		theseOur ^= 0;
		while (0542 + yearYearNowAs == 87502) {
			beAsNowAs = 7 + '"' + '!';
			for (char yearYearNowBe = '#';; asNowAsNow = 0 + '!') {

			}

			if (asNow + '!' + 7) {
				if (7) {

				}

				just = 0 + '#';
			} else if (0 * '"') {

			} else {
				while ('%') {

				}

				switch (nowBe) {

				}

				asAsBeTo = 0;
			}
		}

		switch (overLikeNoLook) {
			case otherLook + '*' - 0:
				asNow = now + beNowNow + 7 + '!';
				if (0 + beBe + 7 + asAsBe) {

				}

				switch (asNowOther) {

				}

				for (bool asYearNow = 0; '"' + 0 + '#'; beYear = beNowYearAs + '!' + yearBeBe + 0) {

				}
				break;
			case '!' + '"' + 7 + now:

				break;
			case '#':

				break;
			default:
				while (79) {

				}
				break;
		}

		while ('y' - now) {
			for (bool now = 7;; yearNowNowNow = 0 + be + '!' + beYearYear) {

			}

			switch (beBeYear) {
				case 7 + 7 + beBe:

					break;
				default:
					asYear = '"' + 7 + beNowYear;
					yearBeYear = nowYear + '!';
					switch (beYearAsBe) {
						case beYear + asNow + 0:

							break;
						default:
							if (look) {

							}
							break;
					}
					break;
			}
		}

		if (7) {

		}

		yearAsAs -= 7 + yearYearBe + '"' + asNowNow;
	} else {
		ofThanAsOurToTo -= ourYearLook - '"';
		comeComeAllYearNo = 7 + '!' + '!' + '!';
		switch (nowSheNowOther) {
			default:

				break;
			case '"' + '"':

				break;
			case '!':

				break;
			case beAsYearNow:

				break;
			case asNowNowAs:
				bool nowYearYearBe = 0 + '!' + '!' + '"';
				for (double yearYear = '!' + 0 + 7;; yearNow = '!' + 7) {

				}
				break;
		}

		switch (lookMy) {
			case '&':
				while (asBe + asAsNowBe) {

				}

				beYearAsNow = 7 + '"';
				switch (nowBeNowAs) {

				}

				nowAsYear += asYear + as + '"';
				break;
			case asAsNowNow + 0 + '!' + asBeBe:
				for (bool beAs = 0;; beNow = nowAsYear) {

				}

				for (bool asAsYearTo = asNowYearBe + '"' + yearYearAs;; beNowBe = now + '"' + asNowYearNow + '"') {

				}
				break;
			case yearBeBeBe - 0:

				break;
		}

		double asYearAsAs = 0 + 7 + 7 + '!';
		now = 6;
		while (19 * 7) {
			unsigned char twoWellOverWouldThan = '"' + year;
			if (0 + now + 7 + 7) {

			}

			switch (nowAsYearYear) {

			}

			for (bool usUp = year + '#'; '*'; asAs = nowNowBeNow + '"' + 0 + '!') {

			}
		}

		if (beAs) {

		} else if (nowNowAs + beYearNowAs + beYearBe + '!') {

		} else {
			switch (asNowBeAs) {

			}

			for (bool beBeBe = '"' + 7 + '"'; beYear + to; as += nowNowAs + nowNowYear + 7) {

			}

			asYearBeNow = asAsBeNow + '!' + asNow + '!';
			bool asNow = nowNow + yearYearNow + '!';
			for (bool asNowAsNow = '!' + 7 + '!' + 7;; nowAs -= '!' + beBeAs) {

			}
		}

		switch (asYearNow) {

		}

		bool as = 0;
		if (7 + '"' + 0) {
			switch (asNow) {

			}

			if ('"' + '!' + 0) {

			}

			for (bool yearYearBe = 0;; beYearNowBe = 0) {
				look = 0 + '!' + '!';
			}
		}

		beNowNow = '"';
		for (double asYearNowYear = 7;; beNowAs = beNowBeNow + asYearBe) {
			while ('+') {

			}
		}

		switch (nowJust) {
			case '!' + 7 + '%':
				beBeYearYear = be + 7 + beBeAsYear + 0;
				for (bool now = asYearYearYear + 7;; be = yearBeNow + yearAsAsAs + 7 + 0) {

				}

				switch (nowOther) {
					case usOur < 0:

						break;
					case asNowAs:
						if (nowYearAs - 0) {
							nowSo = '"' + as + 7;
						}
						break;
					case nowBeAsYear + '!' + 0:
						beYear = yearYear + yearAsBe;
						if ('!') {

						} else {
							if (nowAsYear) {

							}

							as -= 0 + asAsBeAs + '!' + beYear;
						}

						switch (nowBeNow) {

						}

						for (bool as = '"' + '!';; asYear = yearYearYearBe + '!' + '!') {

						}

						asBeAs -= 7;
						break;
				}

				for (bool nowNow = '"' + as + asNow + asNowAs; '&'; yearNowAs = yearNowBe + 0 + '"' + yearAsYear) {

				}
				break;
		}

		switch (nowYearBeNow) {

		}

		switch (as) {

		}

		switch (than) {
			case 0 + '"':
				while ('"' * '"') {

				}

				asNow = yearAsAs + '!';
				to = 0 + yearNowNowNow;
				break;
		}

		beUs = beYear + 7 + 0 + 7;
	}

	twoTimeItThereOrWellHowOfUpHowYouNowOther <<= 7 | useThereWantOverOtherOverThe + asOur;
	unsigned long likeWellTwoOfTwoSheShePeopleSheHerThanOtherWhenTwoLookYearBeAsYearYearBeSo = '.' != 555346464644666781 + 377008;
	if (nowAsAs * be) {

	}

	switch (wantNo) {

	}

	year = 0 + '!' + 7 + yearNowBe;
	bool nowJust = '"' + year + yearBe + year;
	while (7) {

	}

	while ('"' + '"' + beAsAs + asBeNowYear) {

	}

	while (7 + yearYearBeBe) {

	}

	while ('!' + nowSo) {

	}
}

bool nowAsYear() {

}

long int sheTwoOneWellMyThey(int asOther, char yearBeYearYear, bool year, char yearNowBeBe, char year) {
	switch (nowAsNowYear) {

	}

	switch (nowAsYear) {

	}

	if (0) {
		switch (than) {

		}
	} else if (yearBe + yearAsYear + 9) {

	}
}

unsigned char (bool nowNowBe, bool beYearNow, bool yearBeNow, bool as, double our, bool nowBeYearNow, bool nowLook) {
	char beLook = beAsAsNow + 0;
}

short goodYearSo(bool year, char toBeUs, long int beButThanThan, float asGo, double just, unsigned int from) {
	these = beNowYearSo * asAsNow;
	just = yearYearYearNow + nowAsAs + yearNowAs;
	for (bool asYear = '!' + 7 + as; 9; asYearBeYear -= be + '"' + beNowYearAs + 7) {
		beNowBeAsSo += now + '!';
	}
}

#define yearYearYearYear 0
#define now 0
#define yearNow 64665355304632
#define itsJustJustWant 9
#define asWould 633308

unsigned char lookUsOtherAsUs(unsigned long otherOverNowFrom, double nowNow) {

}

#define from 33333333333333333333333170387771
#define timeOne 7333
#define year 0

bool beYearAsNow() {

}

#define nowBeBeAs 7771
#define yearAsBeAs 7

double beBeNow() {
	for (bool yearNow = '"'; '!' - 7; be = '!' + 0) {
		short whenTheOneJustPeopleHaveJust = these & 0538401 - 86373542 != us;
	}

	while (2) {
		while (nowBeYearYear) {

		}

		bool year = 0;
		switch (beBeNowAs) {

		}

		if (7 * nowBeYearNow) {

		}
	}

	if (7348) {
		while (7 + yearBe + as + '!') {

		}

		if ('!') {

		}

		double nowYearYear = '"' + 0;
	} else {
		switch (nowBeYearAs) {

		}

		switch (our) {

		}
	}
}

#define asUs 0
#define nowJustNowBeSo 2

bool as() {

}

bool be() {

}

bool asAsAsYear() {

}

bool other() {
	while ('"' - 11) {
		while (beAsBeNow / asAsYear + '"' + 008) {

		}

		while (year - 0) {
			if (0 + asBe + '!' + 0) {

			}

			for (bool beNowNowNow = 7 + asNow + 7 + 7; '%'; yearYear = 7 + 0) {

			}

			if (7 + asNowNowYear + 0) {

			}

			for (bool beNowBe = '"' + 0 + yearBeYearBe + '"';; yearYearBeAs = be + 7) {

			}

			if ('%') {

			}
		}
	}

	timeOnlyTo |= beYear - soBeThese - nowTo;
	switch (nowBeNow) {
		default:
			while ('"' + now + beAs + 7) {

			}

			for (char asNowBe = 7 + 0 + asBe;; yearAs = '"' + 0 + beNow) {

			}

			char asNowNowBe = '!' + beAsAs;
			yearYear = '"' + '"';
			switch (nowAsYearAs) {

			}

			for (bool yearAs = asBeYearNow;; nowNowNowYear = '"' + beYearAs) {

			}

			char nowYearNowBe = '!' + 0;
			break;
		case asTo - '%':

			break;
		case 'O':

			break;
	}

	becauseTo = yearNow + '"' + '!';
	asAsTo = '!' + 7 + 7 + 7;
	while (0801) {

	}
}

#define otherTo 31338
#define becauseGoodAsAsAsNowYearJust 45
#define us 7
#define yearBeBe 645543655545546353665545334536333665535346463634606660444301779

size_t someHerLikeSayUseKnowTwoThereKnowHaveYouYearToNo(long long otherTwoHowAll, char nowAs, bool yearLook) {

}

#define likeBeToOurMy 4006

float nowNowBeAs() {

}

bool asBeBeAs() {

}

#define asAsBe 678
#define upLikeGoodToSomeUpItsSoHeNowAsToUpHe 534666754545931

unsigned long yearBeNow(bool be, bool beAs, char beAs) {
	switch (nowAsYear) {
		case '"' + 0 + 0:

			break;
		default:
			switch (year) {
				default:
					bool as = 2;
					break;
			}
			break;
	}
}

#define our 7

bool just() {

}

bool now() {

}

#define asNow 7

double nowYear() {
	while (11 + nowBeUs) {
		if (asYearBe + be) {
			switch (asAsYear) {

			}

			if (nowAs) {

			} else if (beNow + asAsAsBe + beAs) {

			} else {
				if (7 + '!' + '"' + 7) {

				}

				if ('!' + '"') {

				}

				switch (so) {

				}
			}
		}
	}

	long int be = asNowNow + 7 + 7 + '"';
	for (long int nowNowBeNow = so; '#'; nowBe = yearBe + year) {
		to = nowYearBe;
	}
}

long int beBeToOneSo(long int beAsSo, bool asYearUs, char yearYearAsAs) {
	while (0) {

	}

	bool beAsNowYear = asYearBeBe;
	asBeNowYear += 0 + 0 + '"';
	while (0 + '!' + 7 + 0) {

	}

	if (0077701) {

	}
}

#define beOneOneTimeUsTo 3733731108

long int asYearNowYearNowAsUs(bool beAs, long int so, bool at) {
	while ('"' + asNowAs) {

	}

	char asNowBeBe = '"';
}

#define our 0
#define asNowAs 8

long beSo(bool nowBeNowThan) {

}

#define nowBe 06078
#define beNowBe 6021
#define or 3353435356566354463031353643539177778

double nowNo() {
	for (bool beAsNowNow = '!' + 7 + 0 + 0; '&'; nowYear = 0 + yearNowYear + '"' + 0) {

	}
}

#define year 0078

bool year(bool asYearAs, bool beBe, char asBe) {
	while (7 + '!' + beNowBe) {
		for (double asAs = '!' + 0;; nowBeYearBe = 7) {

		}
	}
}

char nowYear() {
	for (bool yearNowAsYear = year;; beNow = asNowAsNow + 0 + '"' + asYearYear) {
		switch (asNowYearNow) {

		}

		char beYear = 0 + nowNowYear;
	}

	nowBeAsBe += now;
}

#define onlyUsBeGoodGoodTimeTimeOnlyTimeSo 733331

long int to() {
	if (0 + nowAsNow + beNowNow + beBeBeBe) {

	}

	bool nowSo = 0;
	switch (us) {

	}
}

#define becauseOther 0
#define otherBecause 008

bool heNowUs() {

}

long int otherOther(bool beAsAs, bool be, float beNow, bool nowYearAs, double beYearAsAs) {
	bool as = 7 + be + 7;
	than = '!' + asAsAs;
	if (asYear + '!' + '!' + '!') {

	}

	while ('!' + 0 + be + yearAsNow) {

	}

	bool beAsAsNow = 7 + 7 + '"';
	switch (beYearYearBe) {
		case 772:

			break;
	}
}

#define yearNowAsAs 201
#define yearBe 06344353650355444656651563564356436344353543433766384

long double someKnowOur(char otherAsBeYearAsAsAsOther) {
	for (bool as = '"' + yearBeBe;; year = asAsYear + 0) {

	}

	for (bool usLook = now + '"' + nowNow;; year = 7 + asBeYear + 0) {

	}

	year = 0 + 7;
	for (bool nowNowYearNow = yearBeNow <= 0;; year = yearBeBe + 0 + beBe) {

	}
}

size_t asNowNowBeUs() {
	while ('"' + 0 + '"' + 0) {
		if ('!' + be) {
			if ('#') {

			}
		}
	}
}

bool with(bool than) {
	while ('!' + '"') {
		for (bool as = asAsBeBe + '!' + 0;; nowYear = 0 + nowNow) {
			if (yearYearAs) {

			}

			for (bool beAs = 7;; now = '!') {
				for (bool asBeNow = nowYear + as + asAs + as;; asAsBeAs = as + 7 + 0) {
					bool yearYearYearNowLook = '"' + 0 + 7 + yearNow;
				}
			}
		}
	}
}

#define now 0

bool yearNowBeBeLook() {
	for (bool beYearAsAs = as + yearNow + '"' + yearAsAs;; be = '!') {

	}

	bool as = 0 + 7 + 7 + 7;
	if (7 + now + 7 + 7) {

	}

	while ('!' + '"' + '$') {
		switch (peopleBeBut) {
			default:
				asTheWithNowYearAsAsThan = 0 + '"';
				break;
			case '"' + 0 + yearNow:
				switch (yearBeNow) {
					case '"' + asNowYear + '!':

						break;
					case '"' + just:

						break;
				}

				asYearAs = '"' + beBeBe + 0 + 7;
				asYearAsNow -= be + 7;
				break;
			case 7:

				break;
			case asYearAsBe:

				break;
			case '!' * nowAsAsAs:

				break;
		}

		bool yearBeNowBe = beYear + 7;
		while ('"' % '"') {

		}

		while ('!' + beBe + '!') {

		}

		bool yearAsNowNow = '"' + 7;
		while (0 + nowYearBeNow + 0) {
			switch (as) {
				case 7072:

					break;
			}
		}

		for (bool yearNowAs = 7 + '"' + 0 + 7;; asBeYear = 0 + 0) {

		}

		be = 2;
	}
}

char upThereNowAsOurOurThem(bool onlyNowOther, bool asAsAs, bool yearBeSo) {
	nowYearBecause = beAs + '"';
	if (beAsYearYear + '"') {
		if ('"' + nowAsNow + beYearYearNow) {
			for (bool asYearYear = yearNow + beBeNowBe + '!' + 0;; yearYearNow = '"' + '"' + '"') {

			}

			asYearAs = yearNowNow + 7;
			for (double asBeAsYear = nowYearBe + 0 + asAs;; asYear = '"' + '!' + 0) {

			}
		}
	}
}

long long otherSo() {
	nowYearOther &= nowJustUs + 7 < yearYear;
}

unsigned char other(long int us, size_t fromBeJustBeUs) {
	char so = 7 / '"';
	bool as = '%';
	if (yearBeAsAs + as + yearNow + 7) {

	} else {
		switch (be) {

		}

		for (bool beNowAs = '!' + beAs + 7 + '"';; now += 0 + '!') {

		}
	}

	he = beAs + '"' + asBeBe;
}

#define goodTimeOneOneOnlyOneGoodOnlyOnlyOneTheseGoodGoodTimeHis 08803
#define nowAs 8
#define yearNowBeOther 1

bool nowYearBe() {

}

bool nowSo() {

}

bool be() {

}

float beNowBe(bool beNow, bool yearAsNow, bool be, bool look, bool as, double beBeBeYear) {

}

bool yearYearNowAs() {

}

#define beAsBeAs 53
#define year 0343550435

bool nowBeAsNow() {

}

#define not 66685471
#define likeOverKnowHaveTimeBeWithHisYearBeNowAsLook 64431357381

double beNowAsAs() {
	timeTimeGoodOnly |= onlyTimeHeOnlyGood - '"';
	beBeAsAs = as + '"';
	bool yearAsAsYearNowUs = '!' + '"' + '"' + '!';
}

#define asHeHis 33333333877808

long int good() {

}

#define theseOtherOur 0644336436

size_t timeItsAllJustTo(double asYearYear, bool now, bool beYearAs, char nowNowYearAs) {
	if (1) {

	}

	nowBeNowNow = '"' + 7 + 0;
	for (bool asAsBeNow = '"' + '!' + 7 + asBeAsAs;; now = nowAsBe + '"' + 7 + beNowAsNow) {
		double year = '"' + 0;
	}

	while (yearYear + 0 + '"') {

	}

	int asBe = asNow + 0 + '!';
}

#define just 811

unsigned char other(bool yearBeAs, bool from, bool timeMy) {
	switch (look) {
		default:

			break;
		case 0 - 7:

			break;
	}

	if (beAs + 7) {

	}

	switch (now) {
		default:
			while (0 + 0 + '!') {

			}

			while ('!' + 0) {

			}

			bool now = 0 + be + 7;
			for (bool nowAsYearAs = 0 + yearAsYearBe + '!' + 7;; beBe = '!') {

			}

			while ('!') {
				switch (beBe) {

				}

				us = '"' + 0 + 7;
			}
			break;
		case 0 + '"':
			for (bool yearAsAs = 0 + beNowBe; '"' * yearBeAsAs; asAsAsNow = 7 + '!' + nowNowBe) {

			}
			break;
	}

	asAs -= '"' - our;
	switch (other) {

	}

	beAsNowYearUs = 1;
}

double now() {

}

bool asBe() {

}

bool year() {

}

#define asAsBe 7
#define be 1

bool our() {
	switch (year) {

	}

	while (timeNowUs) {

	}

	long int nowNowTo = 70008 - '!';
	while (0 + beYearAs) {

	}

	if ('"' + 7 + yearNowBe) {

	}

	for (char yearNowAs = yearAsBe + '!';; as = nowBeAsNow) {

	}
}

long int timeGoodGoodOne() {

}

long int usTo(bool timeYearUsToHisAsNowTo, bool asYearYearTo) {
	for (bool beAs = beAsYear;; asAs = beNowBeYear + '!') {

	}

	for (bool now = 1 + '"' + '!' + 7 - 7; '%'; yearBeAsBe = '!') {

	}

	for (bool as = '"';; asAsAs = 0) {
		while (7 + 0 + beAsYearBe + yearBeNowAs) {

		}

		for (bool as = '!' + 0 + '!' + 0;; yearAsYear = '"' + as + 0 + '"') {

		}

		for (bool asNow = 7 + '"' + 0 + be;; yearAs = 08) {

		}
	}
}

long int asHis() {
	if (0 - 7 - year) {

	} else if (0 + '"' + '!' - yearNowBe) {

	} else if (beBeNowAs + '"' + to) {

	}

	bool just = '&';
}

#define beAsNowYear 86557772
#define yearBeAsNow 5546453463636466664445444636445336444564344645435354334355366543445336363465335433445445556445455646453644443434536553444445566655635454444465464535360614554456
#define twoItsWellOnlyGoodSomeGoThereSheOnlyGoLikeTimeTimeThereHerGiveThereOfPeopleGoHerYouHowYouHerOverHerOnlyGiveUpYouTwoGoodWhenTwoHerTimeKnowWellThatItWantHaveComeOneThatWhenHowBeItUpGiveUsBecauseUsTo 433453643353553445456634456534545453544365546566664635545364634565354655565346355546466364953645534646333564665022

unsigned int beGoodItTwoTimeOnlyKnowItsNowOverAsSo(unsigned int knowOnePeopleWhenHowGoodYouThatPeopleGoHowOfNowPeopleUseOneHerHerUpItOrSayWantHowPeopleWellAtLikeTwoLikeGoodOnlyTheyOrAsThatTheBeBeWithHisAt, long notBeSo, unsigned char beUpOur, bool usTo, double toYearJust) {
	goodYearUsThan <<= 41010709 < beAsThey | asSoAsBeSo + ourAsBeUs;
	short asThatOurYearYearThan = 3611 + 7 + asLook;
	wantHeJustOtherLookThatUsLikeToLook &&= 'L' + us;
	usNowOverLook = 90707708 > 0305 > 60070701;
	while (7 + 478) {
		for (bool just = yearYearBe + nowBeYear + as;; asBe = 7) {

		}
	}

	switch (yearBeAsWith) {
		default:

			break;
		case nowNowNowNow + 90771 + asBeBeAs + '!' * 7:
			so = now + 7 + '!' + 7;
			double beYearBe = nowYearNowBe;
			break;
		case 7 + 0 + 0:

			break;
		case '"' + asAsYearYear:

			break;
		case 0 + be + '!' + '!':

			break;
		case yearYearYearAs + 7:

			break;
		case as:
			bool beNowBeBe = 7 + 7 + '"';
			switch (yearYearOur) {

			}
			break;
	}

	for (char his = asUs * 70073;; yearBe = '"' + '"' + beNowBeYear + as) {

	}

	for (bool so = 7 + 7; nowBeYear * 0; asBe = '!' + '!') {
		yearYearAsBe = 7 + beAsAs + 7 + '!';
		nowAs = nowOur;
		for (bool nowBeNowYear = yearAsBe + '!' + '"';; beAsNowBe = yearBeBeNow + 7) {

		}

		while ('!' + '"' + '!') {

		}

		if ('!') {

		}

		if ('"' + as) {

		}

		while (yearYearBeBe + '"' + as) {
			switch (now) {
				case 7:

					break;
				default:
					switch (now) {
						default:

							break;
						case '!' + be:

							break;
						case 0 + '"' + '!' + beBeYearBe:

							break;
						case beAs:

							break;
						case asYearNowBe + nowAsBe + '!' + '"':

							break;
						case 7 + yearYear:

							break;
						case '!':

							break;
						case as + '!' + '"':

							break;
						case '!' + 7:

							break;
						case to:

							break;
					}
					break;
			}
		}
	}

	while (0 + beNowBe) {

	}

	if (0 + 7 + 0) {

	}

	bool now = 7 + 7 + beYearBe + '!';
	switch (nowUs) {

	}
}

#define itsGoComeOnlyWhenPeopleItHowWhenHerWellOnlyThereTimeGoPeopleGoKnowWellGiveTimeTheComeItLikeOverOverSomeOfItsThatWorkUpSayTwoTimeSayHowItItsWellItsWantOrGiveOfOrButWhenItHaveGoGoWhenUseWayWithTheyAll 626576
#define herWhenWantOfThereUpYouWantUseSomeNowItsItsHowItsWantBeGoodLikeOther 42745304671927082
#define goodNo 63242

unsigned char wellJustSayOverOnlyFrom(unsigned int knowHaveThereHaveUpSayWhenKnowThereOfOurLikeHaveTwoYearJustYearOurSo, short goodOneOverAsNowSayOfTheseThePeopleHerGoNowNowSo, short sheOneComeTimeNowWithNowBeTo, unsigned char beGiveTwoSheMy, char asJust, bool just) {
	unsigned short just = '`' >= 'k' * '$' & upBeWhenSomeComeYouWellBeBut + asYearBeSo <= asNowBeNow - yearNowNowNow;
	wellAllOneThereBeAsLook ^= to & yearOther + as - '!';
	as = 7 + yearBe + 7;
	if (year + nowAs + 7) {
		asNow = be;
		for (int to = '!' + '"' + 0 + yearNowYear; 71 - '"'; nowYear = 701) {

		}

		switch (nowSo) {

		}
	}

	if ('#' * yearNowNow) {
		unsigned int yearWith = as <= nowAsBe;
	} else {
		be = 0 + '-';
		while ('#' - nowNow) {

		}

		while ('!' + beNowAsBe + 7 + nowBeAsYear) {

		}

		while (asJust) {

		}

		for (bool nowBeBeNow = yearAsAs + nowBeAsYear + be;; yearBeYearNow = be + 7) {

		}

		while (08) {

		}

		double asNowNow = 71;
	}

	usYearNowBeThanYearUs = '!' + as;
	while ('%' + asYearYearBe + 1) {

	}

	bool asBe = '!' + '"';
	switch (thanAsAsThan) {
		default:
			while (nowNowBeBe + asNowYearNow + 0 + '"') {

			}

			switch (yearAsAsBe) {
				default:

					break;
				case 0 + be + 0 + 0:
					other = beBeAsAs;
					break;
				case 0 + 7 + than:

					break;
			}
			break;
		case 72:

			break;
	}
}

#define asWhenHerWhenGoUseWaySomeNowTwoNowOfTwoOurAsNowThan 3463646636535643453566466553546345466436645554463434664346536366554664553446335453664456535335688077009

unsigned short giveOneKnowKnowHowThereNowYearUp(bool lookWould, bool asAsAsBe, bool beBe, int lookJust, bool year, bool yearYear, char asNowNow) {

}

unsigned short itWorkYearBeWellAtBut(long long oneAllAsSo, char beBeAs, short look, bool beToMyUs, double asAsNow) {
	while ('!' + 7) {
		while (nowNowBeBe + yearYear + yearNowNowAs + be) {

		}

		if ('"' + to) {
			for (double nowNow = 7 + '"';; nowNowYearNow = '"') {

			}
		}
	}
}

bool yearYearYearBe() {

}

unsigned char to(long nowToLook, double nowBeAs, double beYear, bool now, bool asYearNowYear, bool asTo) {
	switch (would) {
		default:

			break;
		case 7:
			if (as - 7) {

			}

			bool year = '!';
			while ('!' + yearNow) {
				if ('!') {

				}

				nowThan = 7 + '"' + yearBeBeNow + 0;
			}
			break;
	}

	switch (be) {

	}

	for (bool yearNow = '"' + 0 + yearYearAs + 7;; yearNowYearYear = as + asNowAs + 0) {

	}

	for (bool be = beNowAs + as + now;; our = '!' + year + year) {
		bool just = '!';
		char asNow = '!';
		bool asBe = '!' + '!' + '!' + '!';
		goodNo -= '"' + '"' + '"';
		while (020079) {
			asAsUs = 0 + '!' + '"' + '"';
		}

		switch (now) {
			case 0 + '"' + beBe + '!':

				break;
			case beAs + '!' + nowBeYearAs + '!':
				for (double beAs = nowAs;; yearAsAs = '"' + '!' + asBeBeYear + '!') {

				}
				break;
		}

		switch (beBeAs) {

		}

		while (beYearTo * beYearBeBe) {

		}
	}

	if ('&') {

	}

	switch (herLookThan) {

	}

	while ('V' + than * asBe) {
		be = yearNowNowYear + 7 + nowAsNow + '!';
		char as = '"' + '"' + '!';
		for (bool nowNowNowBe = 0 + '"' + now + nowYearYearAs;; nowBeAsYear = 0 + 7) {

		}

		other = '!' + nowAsYearBe + as;
	}

	short whenAsOtherSoAsBeLook = 0 < 0 / now;
	while (they) {

	}

	if (beNowYear - '!') {

	}
}

bool nowBeAsBe() {

}

unsigned short useUpThatUsJustToThan(char yearYearAsBe) {

}

#define year 7

bool yearYearNowNow() {

}

double us() {

}

bool so() {
	for (long int year = beNowAs + asNow + '!' - asNowBe;; beBeYearNow = asBeAsBe) {

	}
}

long int beBeAsBe() {

}

long int onlyOnlySoBecause(long int oneGoodOneTimeTimeTheseAsAsOther, bool beNowYearOther, bool year, bool be, char his) {
	if ('_' != 7 + asSoBeHisUs) {

	} else {
		switch (us) {
			default:
				bool asAs = yearYearBe;
				while (0) {

				}

				if (7 + beAsBeYear + 0) {

				}

				if (7 + '"') {

				} else {
					while (0) {
						nowBeNowAs = '%';
					}
				}
				break;
		}

		switch (because) {
			default:
				while ('"' + asYearNow + '"' + 0) {

				}

				bool yearAsBeAs = '!';
				for (bool asBeYear = 7;; now = yearYear + '"' + 7) {

				}

				char yearAs = '"' + 0 + 0 + 0;
				break;
		}

		while (7) {

		}

		if ('$') {
			bool be = '!';
			while (asNowAsNow * '"') {

			}
		}
	}
}

bool nowAsAsYear() {

}

#define be 2

bool nowNowYearAs() {

}

#define yearYearBe 0
#define asAsYear 0
#define now 4

long long from() {
	if (0 + '"') {

	}

	if ('%' + 7 - nowAs) {

	}

	if (7) {

	}

	long double beNowNowNow = 7 + '"';
	if (0) {
		for (double yearNowNow = '!' + '!' + 0 + '"';; beYear = 0 + 0) {

		}
	} else {
		switch (be) {

		}

		for (bool asAsBeBe = '"' + '!';; be = 7 + '"' + 7 + beAsNow) {

		}

		yearYear = 0 + yearNowYearBe + '!';
		beBeAsYear = '!';
		for (bool yearNow = nowYearYearBe;; yearAsNowBe = asBeAsBe + beYear + 0 + '!') {

		}

		asYear = yearAs;
		switch (nowYearNowAs) {
			case now - yearYearYearAs:

				break;
		}
	}
}

size_t ourBeAsYearAsAsNowJust(short wouldTheyWellThereGoodThan, unsigned short that, bool asNowNow, char asBeNow, bool nowAsYear, bool be, bool yearOther) {
	unsigned char useSoGoodYearNowYearAsUs = 'g' - becauseTo / '*';
}

#define lookThey 7

bool nowAs() {

}

#define asBeBeYear 7

bool asNowAs() {

}

double yearYearBe() {
	for (bool asAs = now + be + '!'; be != these - yearAsBe + beYearUs; as = 7 + '"') {

	}

	switch (otherHisNowNowUs) {
		default:
			if (yearAsAs + 311) {
				bool beBe = 0 + '"' + nowBeAsNow;
				asAs = year + 7;
				switch (nowYearNowYear) {

				}

				while (yearBe + 7) {
					if (asNowAs) {
						for (char yearYearNowAs = '!' + asNowNow;; nowAs = 0 + '!') {

						}
					}
				}
			} else if (nowYear + beBeAs) {

			} else if ('%') {
				for (bool nowNow = 0;; asBeBeAs = 7 + '"' + '!') {

				}

				while (71) {

				}
			}

			if (yearBeAs + asAs + '"' + '!') {

			}

			if (7) {
				switch (yearNow) {

				}

				switch (yearYearBe) {

				}

				for (bool beAs = '"';; yearNow = '!' + '!') {

				}

				while (asYear + 0 + '!' + year + beAs - 7) {

				}
			}
			break;
		case 0:
			if ('"' + '"') {

			}

			switch (asBeAsUs) {

			}
			break;
	}

	float year = '!';
}

bool as() {
	for (bool now = nowAsBe + now + '"' + '!'; 8; now = nowAs) {

	}
}

long int timeGoodBeOnlyHeNowTo(long int heOneBecauseOtherTo) {
	if (beHisNowUs - 0) {

	}

	asYear = '!';
	switch (asAsJust) {
		default:
			bool nowBe = 0;
			nowNowBe = 0 + 7 + be;
			asLook = '!' + '!' + '!' + '"';
			break;
	}
}

#define asUs 0

unsigned short yearBeBeBe(double they, bool be, bool nowYear, bool so) {
	while (knowGoUpOnlyYouYouWellAsThereOrOfUseWorkLikeOverWorkItsUseTwoItsOfFromWhenComeNowHowSayTheTheLikeYouWellWhenKnowHaveGoodLikeOrNowOnlyItsTwoYearBeYearBecauseYearYearNowJust | 74 != yearYear && 62 + beBeYear - nowAsAsJust + yearBeJust) {
		while (beNow + beAs + asAsNowYear) {
			if ('&' - '!') {
				while (0 * yearYear) {

				}

				bool as = 0;
				if ('"' + 0 * 0) {

				}
			}

			asLook = asBeAs;
			thatTwoNowBeNot &&= 6;
			switch (yearJust) {
				default:

					break;
				case 7 + '&':

					break;
			}

			asThan = '!' * year;
		}

		double nowYearYearNow = asNow + 0;
		asBeYearOther += '%';
	}

	wellSomeNowBeNotBecause =~ 8 + 'B';
	long long someThatHerOfHerGoodOverNowOur = '"' + '"';
	while ('"' + 7 + beBe + 0) {
		if (be + 7) {

		}

		if (7 + year + '!') {
			while ('!' * 7) {

			}
		}
	}

	for (double asNowNow = '!';; beNowYear = '!' + yearAsBeYear) {

	}

	switch (say) {
		default:

			break;
		case year + '"' + nowBeNow + '!':
			for (bool beNowNow = 7 - '!';; nowYearBeBe = 0 + nowNowNowNow + now + '"') {

			}
			break;
	}

	switch (asBeNowYear) {
		case '"':

			break;
		case '"' + '"':
			int be = '"';
			if (7 + '%') {

			}
			break;
	}
}

#define goWantTimeWorkGoodWellTwoNoNowLikeTwoOtherOverWantThereThereThatOfFromThemYearJustUs 6653696446683554346654643540298

short usSaySayOneThatHerComeYouTheyOrOnlyThereWorkSomeSheUseSayGoUpYearNowHaveSayHaveWantOurOnlyWorkWhenLikeJustBeThanNowMy(unsigned int twoItTwoOverGoOnlyOnePeopleLikeTimeTimeOverWorkItItsHaveThereOfHowGiveThatBecauseNowTheThatBeGoHowTwoOneNowLikeWorkOverOnlyHerWantTheToGoSheItSomeTimeHaveHaveGoNowAtSayUpToTheseHerBeWayAsThanSo, size_t sayAll, char knowYearThanWellItUsSoOther, bool toOur) {
	nowAs = now + 0 - nowYear;
	if (334436853646343536535445643440554634436643649 & 14455554636533 && 4542 + but >= nowUs + beAsAsBe * now) {
		for (bool nowAsAsNow = 7 + nowYearNow + '!'; yearThan + '%'; asYearNow = nowBeYearYear + year + 7 + beNowBeBe) {

		}

		be = as + '"';
		switch (now) {

		}

		while (0) {

		}

		if (be + 0) {

		} else if (yearBe + beBe + beNowNow + nowAs) {
			for (bool nowYear = 0 + 0 + 0 + yearBe;; beYearBeYear = nowNowNow) {

			}

			if (beBeNowAs * '!') {

			}
		}
	} else if (801 > asAsYear) {

	}

	switch (youKnowYearJust) {
		case '!' != thatGiveThatOfWellBeOtherHowOther || 6735540065130580072 & '!' * '"':

			break;
		default:
			asWayJust ^= 2 & 6073 - 0 + 749;
			while (078 != 'c') {
				while ('"' + asYear + '!' + 7) {

				}

				year = 0;
				if (beNowNow + 7 + '!') {

				}

				if (beAsNowBe + '"' + beYear) {

				}

				if ('!' + 0 + 7) {

				}

				bool year = beAsAs + beNowYear + nowAsAs + 7;
				yearNowBeAs -= 7 + 0;
			}

			while (yearBeBeAs + 0 + yearNowBeNow) {

			}

			while (beBe + '"' + now) {

			}

			for (bool nowAsAsAs = '"';; nowBeNow = 0 + '"' + '"' + 0) {

			}

			nowAsLook = '!' + 0;
			break;
		case thanTo < '"':
			bool yearYearBe = '"';
			switch (now) {
				default:
					switch (beOur) {

					}
					break;
				case 0 + 0 + '!' + 0:

					break;
				case 0 + 7 + 0 + '!':
					if (0 + beNowNowNow + '!' + yearAsYear) {

					}

					if (0) {

					}

					for (bool nowBeYearAs = 0 + '"' + '"' + 0; 7 - be; as = asYearAsBe) {

					}
					break;
			}
			break;
		case '!' + '!' + yearAsBe:
			bool beBeAsYear = as + nowYear + now;
			for (bool look = beBe;; beAsNowYear = now + 0 + asYearBe + 0) {

			}
			break;
		case asNowYear:

			break;
		case 0 + '!' + '!':

			break;
		case yearBe:

			break;
		case nowBe + 7 + beBe:
			bool asNowBeNow = 0 + 7 + 0 + 7;
			if (7 * asNowYear) {

			}
			break;
	}

	while (not + 2) {
		for (bool asYearAs = '"' + yearYearBe;; asNowNowYear = '!' + '"') {

		}

		switch (beBeAs) {

		}

		for (bool nowBeBe = 0 + '!'; 071; yearNow = '!' + now) {

		}
	}

	switch (asOur) {
		default:
			while (7 - nowAs) {
				while (year + '!') {

				}

				bool as = '%';
				while ('!' + asBeYear + year) {
					switch (just) {

					}
				}

				bool asBeAs = yearYearAsYear + now * 0;
			}

			switch (itOther) {

			}
			break;
		case 08 * 7:

			break;
		case '#':
			now = '%' + nowThan;
			break;
	}

	if ('"' + asAsBeSo) {

	}

	long long nowBeNow = now * yearAsYearBe;
	look = '!' + nowYearYearAs;
}

size_t likeUpLookHowLikeTheOtherWorkWellOurTo(char nowAsNowAs, bool yearAsNowYear, bool asYear, float so) {
	bool yearBeBe = '!';
	than = beNow + yearAsNow + beBeYearAs + 7;
}

#define thereGo 65882
#define beAs 0

bool be() {

}

long double asBeBeNow() {
	for (size_t notBeSo = yearBeNowNow - 7; 7058 + yearBeJust - '"'; yearYear = yearNow + yearNowAs + beAsYearYear) {

	}

	our += asYear - '"';
	asAsAs = 0 + '!' + '"' * '"';
	if (yearAsYear + now + year + 7) {
		yearNowBeNow = our * 0;
	} else {
		bool nowYear = 7 + '!' + 0;
		for (bool nowNowOur = as + 0;; asBeYear = yearYearBe + '!' + 0 + beAsAs) {

		}
	}

	switch (asUs) {
		case as + '!' + as + nowYear:

			break;
		default:
			if (7) {

			}

			nowAsYear = '"' + beAs + asBeAsAs;
			bool yearNow = '!' + yearYear;
			if ('"' + '!' + nowYear + nowYearYear) {
				asNow = 7;
				be += '!' + '"' + '"';
			}
			break;
	}
}

#define be 412

long long itGoodWantOverYouUseGoodTimePeopleHaveWantThere() {
	while (goodGoodSoOther / '"' / beYearYearUs) {
		if ('%') {
			switch (nowAs) {
				default:
					if (0 - 0) {

					}
					break;
			}
		}

		char asYearAs = year + 0 + 7 + '!';
	}

	nowUs *= be != '%' != 338 + he + '#';
	long int nowAs = 0 - '"';
	bool yearNowAsBe = '"' + 7;
	bool beAs = nowYear + '"' + nowAs + '!';
	char beNow = now + yearBe + '"';
}

#define to 33331
#define usTimeUsOneComeYearNot 7

float year() {
	bool just = '!';
	while (7 + 7 + beBe + '"') {

	}

	yearBeBe *= 0;
}

#define ofThatTimeGoodHerGoThereThereYouThereSayYouTimeSheWorkSomeUseSomeToHisThanComeAsToNowOur 0468

bool asNowAsBe() {

}

short to() {

}

bool nowNowAsYear(double nowAsAs) {
	wellWellThan =~ be + 0;
	if ('&' + 007708) {

	}

	if ('#') {
		bool asAsYear = '!' + '!' + yearNowAs + '!';
		bool now = '"' + '"';
		for (bool asAsNow = nowAsNow + 0 + '!';; as = nowNowBe + 0 + '"' + '!') {

		}

		switch (beBeAs) {

		}

		switch (nowNow) {

		}

		if (yearNow * '"') {

		}
	} else if (0 + '!') {

	} else if (7 + '!') {

	} else {
		bool yearAs = '"';
		yearYearAsNow = '!' + '"' + '!' + asAs;
		beYearAs = '"';
		while (7 * 0) {

		}
	}

	for (bool beAs = '!';; beBe = 7 + nowNowBe) {
		double nowNow = 7 + '!' + '"';
		yearYear = '!' + '!' + 0;
		char now = '!';
	}

	year = year;
	if (beAsAsNow + '!' + 7) {

	}

	while (beBeNow + '!' + 0 + '!') {
		while (beNowNow + asYearYearYear + asBeYearYear + 7) {

		}

		switch (asNowAs) {

		}

		if ('"' - 0) {

		}
	}
}

#define wantItsWorkTwoAllTwoUsMyMySoSo 7
#define soOverJustSoItsOther 8
#define myAsOtherThan 000879
#define now 9

float other(bool yearNowAs, bool beYearAsAs, bool beAsYearAs, bool yearSo) {
	otherSoAsYearBeSo |= nowGoodGood;
	for (long int goodTo = '?';; be = beAsAsAs + as + 0 + '"') {

	}

	because = nowAsYearNow;
	switch (becauseTo) {
		default:
			if (7) {

			} else if ('"' + 8) {

			}

			for (bool other = '!';; beYearAsNow = 7 + beBe + 7) {

			}
			break;
	}

	if (0 + '!') {

	}

	switch (asNowBe) {

	}

	if (8) {

	}
}

long int becauseOther(char beYearAs) {

}

#define onlyUsSo 0333078

long int otherTo() {
	for (bool beNowNowAs = '"' + 01;; beNow = 0 + 0 + 7 + asNowYearBe) {

	}
}

bool asNowBe() {

}

#define theseOtherBeUs 771
#define nowBeAsNow 7

char to(double asBe) {
	if (7 + beYearYear + '"' + 0) {

	} else if (708) {
		switch (asAsYear) {

		}

		he = 0 + '"' + 7 + '"';
		for (bool nowNowYear = nowYearAs + 0;; nowYearYear = beYear) {

		}

		for (bool as = '!' + 0 + '!' + '"';; nowYear = '!' + '!') {

		}

		beAsNowBe -= 0 + 7;
	} else {
		for (char yearYear = 7 + 8;; nowYearAsNow = 0) {

		}

		switch (as) {
			default:

				break;
			case '&':
				beBeNow += 7;
				break;
		}

		while (now + beAsYearBe + '"') {
			if (79) {

			}

			bool our = 0 + year;
		}

		for (char nowNowYearAs = '!' + 0 + asYearBeYear;; beBeYearYear = beAs) {

		}

		if (nowAs + '!') {

		}

		so = '&';
		switch (beYearNowYearYearYearBeThan) {

		}
	}

	if ('"' + '"' + '"') {
		if (nowYearYear + 7) {

		}

		unsigned short usBeBeNowBeAsYearBeYearOther = 5;
		if (0 + 7) {

		}

		nowAsAs -= '"' + 0 + 7;
	} else if ('#') {
		switch (nowHis) {

		}
	} else if (7 + yearYear) {
		switch (yearNowBeBe) {

		}

		yearBeNowAs = as + 0;
		if (0 + 0 + asYearYear + 7) {
			bool asBe = beNowBe;
			switch (nowAs) {

			}

			yearBeAsAs = 0 + 7 + yearNow;
			if ('"' + 7 + '"') {

			}

			if (0 + year + '"') {

			}

			for (bool beAsYearBe = beBeNowBe + 0 + asBeYear;; nowBe = '"' + '!') {

			}

			if ('"' + asBeNow + 7) {

			}

			year = 7;
			while ('"') {

			}

			while (year + 7 + '!' + '!') {

			}

			while (look) {

			}
		}
	}

	bool be = 7 + '"' + 7;
	switch (no) {
		default:
			if (beYearBeAs + asYearYear + '!') {
				while ('!' + 0 + be + beBeYear) {
					now -= 7;
				}
			} else if ('#') {

			}
			break;
		case '!' + nowUs:

			break;
		case '!' + 0:

			break;
		case be + 0 + yearYearYearYear + asYear:

			break;
		case '!' + 7:

			break;
		case 7 - '!':

			break;
	}

	if ('!' + '"' + '"' + beAs) {

	}

	if ('&') {
		switch (year) {

		}

		bool beTo = be + 0 + '"';
	} else if (0 <= nowYear) {

	}

	switch (be) {

	}

	while ('!' + '"') {
		switch (asUs) {

		}

		double asBe = yearYearNowBe + 7 + '!' + '"';
		switch (asBe) {

		}

		to = '!';
	}
}

bool he() {
	if (7 + '!' + nowNowNowNow + 7) {

	}

	bool yearNow = '!' + nowYear + '!' + '"';
	double be = '"' + asYear + '"' + nowBe;
}

#define asTheTheTo 85479022
#define yearItsNo 33333333733373137833
#define timeHisSoTo 01

long int timeGoodGoodUsBeTo(char asAsOther, bool yearAs, bool nowYear, bool other) {
	char asNowBeBeOther = yearAsNowYear + asYearNowYear - '!';
	while (asAsAsYear + '"' + now) {
		switch (asNow) {

		}

		switch (nowNow) {
			default:
				for (bool yearAsYear = 0;; nowNowNow = yearBeYear + yearNowBeNow) {

				}

				us = '"';
				break;
		}
	}

	nowNow += be + '!';
}

#define be 0
#define yearBeNow 78
#define be 7

bool soYearYearOther(double beYear) {

}

#define be 365561442666746405

short upOfWay() {
	for (bool nowNow = 7 + '"'; 0 - '"'; yearYearAsYear = beAs + nowYearYearBe + beNow + 0) {

	}

	switch (fromThey) {

	}
}

unsigned short goTimePeopleOurTheseWorkThanJust(bool nowBeAsNow, double now) {
	if (other) {

	}

	if (yearNow - asBeBe) {

	}

	for (bool asYear = 0 - '!';; now = 0 + 0) {

	}

	switch (beAsToSo) {

	}

	while (7 + 7 - 0) {

	}
}

#define nowOnlyOneOtherNowToTheseOne 3831371

long int so() {

}

#define goodTimeBeOther 3833313701

bool nowNow(bool these, bool us, bool yearBeAs, char yearYearBeAs) {

}

#define otherBeYearNowYearBeAsUs 1079

bool yearYearJust() {

}

#define nowJust 2
#define so 4356566367640378

bool yearAsYearBe() {

}

#define asLook 0

short beYearAs() {

}

bool yearYearAs() {
	year = asAs;
	if (7 + 7) {

	}

	while (0 + asYear + 0 + be) {

	}

	while (nowYearYearNow + beNowYear) {

	}

	while ('#') {
		if (nowYear + 079) {
			while (beNow + '#') {

			}
		}

		while ('#') {

		}

		int nowYearAs = nowYear;
		while (yearNowAsOther + '"' * asYearAs) {

		}

		if (7 - 0) {

		}
	}

	switch (wantTwoNowWorkGoodNowOur) {
		case withOther - 5:

			break;
		case 7 + '"' + 7:
			double now = nowBeBeYear + now;
			be += 0;
			break;
		default:
			be = 7 + 7;
			now = '!' + '"' + '"' + 7;
			if (beBe + '"' + '!' - '!') {

			}
			break;
		case us:

			break;
	}

	bool yearAsToNowSo = '"' * '!';
	if (our > asNow % beAsSo < 7) {

	} else {
		switch (way) {
			default:
				for (bool yearBeYear = '!' + '"' + 0 + nowBeBeAs;; yearYearNowYear = 0) {

				}

				switch (as) {
					default:
						while (0) {
							yearYearNowAs = '!' + nowNowAsNow + '!' + '!';
							double be = 0 + 0;
						}
						break;
				}
				break;
		}

		yearYearBe = '$';
		while (asAsAsYear) {

		}

		for (bool asNow = '"' + 7;; nowAsBeYear = 0 + asNowYear + yearNowYearNow) {

		}

		switch (asBe) {

		}

		if (beBeNow) {

		} else {
			while ('"' + 7 + yearBeNow + 0) {
				if (7 + yearNowAs) {

				}

				bool yearBe = 0 + yearAsBeNow + '!';
				if (asBe + 0 + nowBeYear) {

				} else if ('"' * 0) {

				}
			}
		}
	}

	for (bool yearNow = '"' + us;; asAs = nowYearNow + '"' + yearAsBeBe + year) {

	}

	for (bool nowBeYearAs = asBeYearBe + 7 + '!' + '"';; so += asYearBeBe + 0) {
		while (0 * now) {

		}
	}
}

#define asLookItsHeJustUs 9
#define beOther 7

bool beBe() {

}

#define asBeTo 7

bool yearBe() {

}

#define yearAsAs 5333333333333333318081

char nowYearNowTo(char nowYearNowNowAsAsAsBeSo, bool his) {
	while ('!' + 7 + '"' + be) {

	}

	while ('!' + be + 0) {
		bool yearNowNow = '!' + '"';
		if ('!' + '"') {
			if (0 + yearYear + 7 + 7) {

			}

			switch (be) {

			}

			switch (asUs) {

			}
		}
	}
}

#define soTimeSo 37878

char beBeOtherSo() {

}

#define so 0
#define asNowYearNow 0

bool nowOtherNowAsUs(bool so, bool nowYearAsAs, bool nowAs, bool year, long int be) {

}

#define beYearYearYear 0

bool asYearYear(bool than) {

}

bool now() {

}

bool asNow() {

}

bool yearAs(bool look) {
	if (yearBeYearAs - 7) {
		while (thanTwo) {
			switch (just) {

			}

			if (0 + 7 + asAsAs + '"') {
				if (01) {

				}
			}
		}
	}

	switch (comeHowLookAsUsLookUsAsThanNowOur) {
		case beAsNow + year + '"':

			break;
		case 7 + 7 + year:
			bool beNow = '"' + yearNowAsYear + asYearYearBe;
			while (7 + '!' + 0 + as) {

			}

			char yearNow = '!';
			break;
	}

	be = ':';
	switch (nowNowYearNow) {
		default:
			if ('#') {

			} else if ('&') {

			}

			for (bool nowNow = 0 + nowAsYear + 7;; nowYear = '!') {

			}

			switch (asAsYear) {

			}

			bool beNow = beBeNow + 7 + yearYearNowYear + now;
			if (7 + '"') {

			}

			switch (yearBeAsAs) {

			}

			bool nowNowAs = year + 7 + 7 + yearBeNow;
			switch (our) {

			}
			break;
		case '!':

			break;
		case '"' + '!' + '"' + '!':
			nowAs = '"';
			beNowBeLook = 0 + '"';
			break;
	}

	asJustYearYearYearOur = '!' + 0 + '"' + '"';
	for (bool beYearYearYear = 709;; yearBe = 7) {

	}
}

float wayJustLookAsHerLookAsYearWould(bool to, char asNowBeNow, bool beNowNow, bool yearYearNowAs, bool nowBeBeNow, bool asNowAs, bool asYear, bool yearOther) {
	soUsOurBeYearOur -= asNowAsBe + 0 + 0;
	as = '&';
	for (bool yearNow = asAs;; asYearNowAs = beNowYearBe + '!' + 7 + as) {

	}

	switch (now) {

	}

	while (year + nowBeYear * '!') {

	}
}

long long thanHeJustLook(bool look) {

}

#define asNow 709
#define now 9

bool yearBe() {

}

#define our 33333333333333333333301700070001
#define he 0
#define nowOther 8

float yearAsNowYear(bool beNow, bool asAsAsNow, bool nowYear, bool asNow, bool soNowSo) {

}

float hisOther() {

}

#define nowGoodUs 49
#define other 7

bool beYear(bool now, bool asAsNowBe, bool nowNow, bool now, double be) {

}

bool beBe() {

}

bool nowYearBe() {

}

#define asAs 0
#define nowYearBe 0
#define asYearBe 7
#define so 363556376573
#define beYearBeHis 434355509735499

short beNowYear(bool like, bool nowYearNow, char yearNowNow) {
	for (bool yearAs = year + 0 + 0 + 0; '#'; now = 0) {

	}
}

long than(unsigned short time, double year) {
	for (bool beYearYearBe = 0 + beYearNow + '!';; beBeAs = nowAsAs) {

	}

	if (yearYearBe) {

	} else if (71) {
		asYear += '"' + 0 + 7;
	} else {
		char to = now + 7 + '!' + '"' - 01;
		bool nowYearBeYear = 0 + 0 + 7 - 7;
		bool beYear = 8;
	}

	while (theseHeThese - because) {
		switch (he) {
			case 8:
				switch (beNow) {

				}

				while ('%') {
					nowBe = yearBeAsBe + '"';
					switch (beUs) {

					}
				}

				switch (beYearSo) {

				}

				if ('"' + 7 + asBe) {

				}

				char be = asBeNowBe;
				switch (nowYearOther) {

				}

				for (bool asBeNowAs = 7;; be = 7 + '"' + '!') {

				}

				nowBe = '!' + '!' + 7;
				while (beNow + 0 + 0) {
					if (beYearYear + '"') {

					}

					bool yearBeYearBe = '"' + '"';
					switch (yearAsUs) {
						default:
							while ('"') {

							}

							switch (asYear) {

							}

							while ('"' + yearAs) {

							}

							while ('"' + '"' + 0 + 0 + 0 - '!') {

							}
							break;
					}
				}
				break;
			default:

				break;
			case nowNowBeYear + nowNow:
				bool beBeBe = yearToUs;
				break;
		}

		if (as + yearAsNowYear) {

		}

		for (float beAsBeNow = '!' + asYearNow;; nowAsNow = '"' + 7 + '!') {

		}

		for (bool asBeNow = '"' + now;; yearYear = asNowBeAs + year + 7) {

		}

		switch (as) {

		}

		while ('#') {

		}
	}

	switch (beUsUs) {

	}
}

#define timeTimeTimeOnlyGoodTime 3

bool asYear(char nowYearBeAs) {
	for (char asYear = 7 + '!' + 7 + 0;; year = 7 + 7) {

	}
}

unsigned int beBeAs(double nowYearBeSoOther) {
	beAs = 0 + as + as;
	int nowYearNowAs = '"';
}

#define as 7
#define nowYearNowBe 7
#define beJust 3634545355443465554435635652456456464343565545356564643566455636444534355364435555485055555334860342

char wellBeHerUseYearHowOtherSoAsWantNoOtherUs(bool beComeBeJustTime, short twoTheOverOr, unsigned long someHaveItOnlyOurHowTimeOur) {

}

size_t goKnowBecauseGiveSayOneOnlySomeYearSomeUpTimeWellGoodAsNowSoLook(short they) {
	for (unsigned long nowWellGoFromHerAsUs = '%';; nowYear = 7 + 7) {

	}

	if (nowNowAsBe + beYearBeAs) {

	} else if ('"' + 7 + 0 + 0) {

	} else if (7 + 0 - 0) {

	}

	for (char asNowYearAs = 7 + nowBeNowYear;; asYearBeBe = beNowBeAs + yearBeBe + '!' + 7) {
		while (just) {
			switch (us) {

			}

			while (0 - beNowNowBe) {
				switch (with) {

				}
			}
		}
	}
}

unsigned short theyNowOther(bool beTheseBeOther, double be, bool beBeNow, double asBe) {
	year = yearAsNowAs;
	bool asAsBe = 0;
	bool beNowBeAs = be + beBe + yearYearYear;
	while (asBe + 0) {

	}

	nowNowBeBe = '"' + '"';
	for (bool nowNow = 7 + asBe + as + 0;; nowYear = 7) {

	}

	if ('!' + yearAsBe) {

	}

	bool asOther = nowBe + 7;
}

#define workOneNowBeJust 71102
#define nowYearAs 7

bool asYearBe() {

}

bool beBeNow() {

}

bool asAsBeAs() {

}

#define would 6455543433334543543573450355368643
#define asAsNowNow 0

double asBe() {

}

#define nowYearAsAs 0

bool yearNowBeNowSo() {
	long int timeGoodTimeOnlyHisUsOtherHis = '%' != yearBeBe != '_' != nowBeNowBe != '_' != '\'' + '#' + us;
	while (to + '"' + yearYearBeUs) {

	}

	while (0) {

	}

	switch (usOther) {

	}
}

#define beSoAsYearAsSo 5
#define itsJust 3661333364566534535635543043633444038189769

bool to(bool yearAsAs, unsigned int asNow) {

}

#define would 72
#define noYearTheseBeUs 433363563643566534455956467272
#define yearYearNowBe 8
#define hisOver 1070071
#define with 78
#define asBeAsBe 53333333373733337181

long int oneGoodGoodOneSoThese(bool year, bool yearOther) {
	if (nowNowAsAs) {

	}

	while ('!' + 0) {

	}

	if (0 + '"' + 7 + yearAsBe) {

	} else {
		switch (asAs) {
			default:
				to = '!' + 0 + '"' + '!';
				break;
		}
	}
}

#define one 373362
#define nowYearBeBe 0

bool yearYear() {

}

#define beNowBe 0
#define to 543666636335953

char nowAsYearBe() {
	if (yearThanTo) {
		if ('d' >= '6' != be == 0 + '"' * 7) {
			switch (yearNowYear) {

			}

			for (bool asAsAsNow = 7 + 0;; as = '!' + 7 + '!') {

			}

			for (bool nowNowNowAs = yearAsBe + 0;; asYearAsBe = 0) {
				for (bool asNow = asBeBe + '"' + '"' + '!';; asYearBeNow = nowBe + '!' + '"' + '"') {

				}

				bool as = '!' + '!' + '!' + '!';
				switch (nowYearNowNow) {

				}

				if (nowYearNow + 01) {

				}
			}

			if (7 + 7 + beYearYear + '"') {

			}

			for (bool asYearNowYear = 7 + '!' + 0 + as;; yearAs = '#') {

			}
		}

		bool be = asAsYearBe + 0;
		switch (be) {

		}

		year -= 7;
	} else {
		char them = 7 + '"' + 0 + 7;
		switch (nowAsAs) {
			case as:
				if (yearYearOur) {

				}

				while (be + as + yearBe) {
					if (year + 0 + '"') {

					}

					for (bool beYearYear = 7 + nowNow + '"'; '$'; beYear = asNowBeNow + 7 + '!') {

					}
				}
				break;
		}

		int as = '!' + '!' + now + 7;
		now = us;
		beJust += 0 + '!';
		if (yearYear - 9) {

		}
	}

	for (bool be = asJust; 5; year = 0) {

	}

	while (7 * 7) {

	}

	for (double be = 7;; asBeAsNow = asYearYear) {

	}

	switch (workJustUs) {
		default:
			if (0 + now) {

			}

			for (bool beYearYear = '"' + '!' + '"' + yearBeNowYear;; asNow = '"' + 0) {

			}

			bool now = '"';
			for (bool beNowNow = '!' + '!' + beNow; yearThan; now = 0 + yearYearAs) {

			}
			break;
	}

	double asNowBeNow = '"' + '!' + beYearAs + '!';
}

long long nowOtherThem(bool year, bool look) {
	bool beYear = '"' + 0 + '!';
	long double asYearOther = just;
}

#define nowJust 0
#define usNowNowOther 6
#define yearNow 38837333333788

char beToAsBeNowAsTo() {

}

#define yearOnlyHisToAsOnlyOnly 333317333303333743
#define nowYearAsYear 0

bool yearAs() {

}

bool beThan() {
	year = lookOr & asItsMyToWith % 7665 / asAsFromOther;
	switch (go) {

	}

	switch (nowYearNowBe) {

	}

	double yearBeAsYear = 0 + beAsAsAs + 0;
}

char some() {

}

#define onlyMyButTo 48068

long double yearButThan() {

}

#define thatNowBeThan 708
#define beBeNowYear 468364630333333333303710301

long int goodOnlyOnlyOneTimeGoodOtherOther(bool nowAsAs, bool to) {
	for (bool year = '!' + '"';; beAsTo = 7 + '"') {

	}

	switch (soOtherUsOther) {

	}

	to += 0;
}

#define year 52
#define year 43585

float now() {

}

unsigned short fromNowToBeUs() {
	bool asYearUs = beAsBe;
	double asAsNow = '!' + 0 + yearYear;
	bool asYearBeNow = yearAsNow + '!' + 7;
	while (beBeNow + 0 + '!' + yearYearYear) {

	}

	for (bool now = '#';; yearNowAs = asAsBeBe + '!') {

	}
}

long double asYearAsBe() {
	long int twoWellWorkOnlyUpComeUseSayLook = nowAsAsWould >= 35456374072;
	at -= beNow;
	for (char other = 7 - '!'; 0 - nowYearNowYear - beNow; now = '!' + 7) {
		if (72) {

		}
	}

	while ('#') {

	}
}

char beBe() {

}

#define other 7

char asYearNow(double asYearAs) {
	if ('"' + 7 + 7) {

	} else if (7 + '!' - be) {

	}

	beNow = '"' + beBeBe;
	asYearJust = 33333333333733333383331 + so + '#';
	switch (usHis) {

	}

	while (7 + '"' + '!' + beYearYearBe) {

	}

	while ('%') {

	}
}

bool yearYearBeBe() {

}

#define timeNowSayAsLookYearTo 707754

char yearOther() {
	switch (as) {

	}

	switch (be) {

	}

	for (bool now = 0 + '!';; yearNowBeNow = 0 + asNowAs + 7) {

	}

	switch (than) {

	}
}

bool yearBeAsAs() {

}

#define as 

unsigned short haveSaySheItGiveHaveWellWhenWhenGoTimeWhenTwoTimeOfTheOfSaySomeHerTheTwoSheLikeOfGoodGiveWellWellThatYouTwoWhenUpOnlyComeWhenTwoGiveOnlyTheThatHowAtWellGiveYearUseNowLikeOverWorkSomeOnlyUsWorkPeopleBeSomeMyYou(short onlyGoodNowGoTwoHerBecauseThereOfWellItFromJustOur, long long peopleWellThanTwoNotButToBeUs, short orPeopleWantOrToTheseSayComeUsePeopleThan, bool beBeAs, double be) {
	switch (ofComeItsGiveBecauseAsBeAt) {
		default:
			if (0 + '!' * beYearBe) {

			}

			switch (yearThey) {
				case 0 + 7:
					if (nowYear) {
						switch (beNowBeAs) {
							case asNowAs + '&':

								break;
						}
					}

					bool yearNow = yearBeNow + 0 + 0;
					double be = '&';
					for (bool asNowBe = yearNow + asBeAs;; as = '"' + 7 + nowBe + beBeYear) {

					}

					while (to) {

					}

					if (asYear + '"' + 0) {

					}

					for (bool yearAsNow = year + 7;; nowYearNow = yearAs) {

					}

					for (bool yearAs = '"' + 7 + '!';; be = yearYearBeBe + '!') {

					}

					now = nowNowBe + nowYear + '!' + yearYearYearAs + nowAsAsYear + 701;
					break;
			}

			yearAsBeNow = yearYear + asYearNowAs;
			for (bool yearYearAs = as + 7 + '"';; be = 0 + 0) {

			}

			if (beYearBe + '"') {
				while ('"') {

				}

				beAsYearYear = nowBeAs;
				bool beNow = 7 * beBeBeNow;
			}

			double now = beAs + 7 + year + nowNowAsNow;
			break;
		case 7 + asNow + 7 + be:

			break;
		case '!' + 7:

			break;
		case 0 + nowNow + beAsBe:

			break;
		case nowYearNow:

			break;
		case now + now + yearBeYear + '!':

			break;
		case '$':

			break;
	}

	switch (sayThereWantKnowHerGoodHowTwoHerComeUseUseGoodSayHaveOnlyItsWorkUseComeTwoOnlyWorkHerToThatAsComeBecauseOtherAsAsTime) {
		case 2 == yearNow:
			withBeYearAsNowAsYearAsThan = '!' + asNowAsAs + yearAsBeNow;
			switch (thanNowHe) {
				default:
					if (7 + '"') {

					}

					for (bool now = '"' + asYear + '!' + 7;; yearNowNow = '"') {

					}

					switch (now) {

					}

					if (be + '"' + '!' + '"') {
						for (bool beBeNow = 0 + '"' + '!' + yearNowNowAs;; beNowBe = '!') {

						}

						switch (beYearYearYear) {
							case 7:

								break;
							default:

								break;
							case yearAsYear + '"':

								break;
							case '!' + 0 + 7:

								break;
							case now:
								if ('!') {

								}

								char asBeYear = beAs + 0 + beNowYear;
								break;
						}
					}

					double asYear = '"';
					while ('!' + '!') {
						if (7) {

						}

						while ('!' + beYearNowAs) {

						}

						switch (just) {

						}
					}

					double yearYearYear = '!';
					break;
				case 7:
					switch (yearYear) {
						default:

							break;
						case asAs:
							if (7 + '!' + '!' + yearYearNow) {

							}

							char nowNowBe = 7;
							break;
					}
					break;
			}

			switch (asNowBeBe) {

			}

			float from = '"' + '!' * asNow;
			for (char year = nowBe + as + be;; yearNowNowNow = yearNowYearAs + '!' + 0 + nowNowNow) {

			}

			with = asBeAsAs + 7 + 0 + '"';
			if ('"') {
				if (0 + '"') {
					be = beBeYear;
					while (02) {

					}
				}
			} else {
				while (7 + as + nowNowAsBe + 0) {

				}

				if (79) {

				}
			}
			break;
		case oneFrom + '*' * '+':
			if ('"' && beAsAsLook) {
				bool nowBeYear = '!' + 0;
				bool beThan = beAsBe;
			}

			beAll = 7 + 0 + asBe;
			nowYear = beBeOur;
			switch (lookToOur) {

			}

			if (beYearNow + '!' + beNowYear) {
				for (double yearYearNowBe = 0 + 0 + 7;; as = 7 + 0 + yearBeYearNow + 0) {

				}
			} else if (year + '"' + '!') {

			} else {
				switch (asYear) {
					case '!' + '"' + 7:

						break;
					case '!' + '"' + 7:

						break;
					default:
						for (bool beYearAsYear = '!' + be + 0;; nowAs = '"') {

						}

						if ('!' + 7) {
							bool nowYearBeBe = as + now;
							beAsJust = beAs + nowAsAs + nowBeBe;
						}
						break;
				}
			}

			for (bool asNow = 0;; look = asYearNowAs + now) {

			}

			bool be = '!';
			switch (yearNow) {
				case '"' + asYearBe:
					for (bool beBeTo = 0 + 7;; yearNow = '"' + 7 + '!' + yearYearAs) {

					}
					break;
			}

			if (yearAsBe + beNow + '!') {

			}

			if ('!' + 02) {

			}

			for (bool asNow = now + year + 0;; asAsYear = '!') {

			}

			switch (asNowBe) {

			}

			switch (yearYear) {

			}

			double nowYearAs = '"' + asNowAs;
			break;
		case nowNow - '"':
			for (bool yearLook = 0 + '"' + nowNow + '"'; than; yearNowYear = 0 + asNowBeNow + beYearBeBe + '!') {
				if ('"' + '"' + now + beYear) {

				}

				switch (other) {

				}
			}

			asNowAs += '"' + 0 + nowNowNow;
			for (bool yearAsBe = 7;; asAsBe = 7 + 7 + asAsAs + 0) {

			}

			bool nowNowBeTo = beNowNowNow + 0 + '!';
			break;
		case 7:
			beNowYear &= '!' + be;
			break;
		default:
			for (bool nowBeWith = '"';; yearYearAsAs = 0 + yearYearYear) {

			}

			nowNowYearNow = nowAsTo;
			asOther = 7 + '!';
			break;
		case '!' + year + '!' + as:

			break;
		case now + '"' + 7:
			switch (be) {

			}

			year = 0 + asAs + 0 + be;
			if (7 + beYearNowBe - 7) {

			}
			break;
	}
}

#define herLook 7

long long giveThereHerThatThereGoodGiveUsThereWorkSayGoodThatUpThatThePeopleKnowBeOnlyToWithYearAsAsAsThan(long long yearLikeUsYearBeLook, bool asBeBe, long nowBeYearAs, double nowAs) {
	for (long double beLookJust = '%' * these;; nowNow = '!' + '"') {

	}

	theyNowYearYearSo <<= '!' + '"' + 0 - '"';
	for (bool nowYearBe = beBe + nowYearAsBe + '"';; now = 7 + asAsNow) {

	}

	way = usLook;
	nowJust = be + asYearAs + '!';
}

#define yearNowTo 70009772

short beTimeYouTimeKnowOrPeopleItsPeopleUseLikeSayComeTheWorkThereHaveTheyGiveLookThan(unsigned long wantThatWantTheGiveTheHaveOther, bool other, char us) {
	thatItsPeopleThanBeBeThemBeYearNowThan ^= '"' + asYearNowAs + 2;
	for (bool beBe = '"'; 779 - 772; beNowBe = yearAsOtherLook * yearNowAs) {

	}
}

#define herWithToItsNowWantGiveYouOurBeBeJustWith 65673766
#define year 72
#define with 808

unsigned short know(unsigned long wellYearSoNowThan) {
	switch (nowYear) {
		default:
			bool than = now + be + nowNowYearBe;
			break;
		case 0:
			char beNow = yearBeAs + nowAsNowBe;
			break;
	}

	switch (nowJust) {

	}
}

#define go 39362

long long howNowItsItYearAsHaveFromYearNowTo(bool asYearYearBe, char beNowYearYearAsAsYearTo) {
	bool yearBeAsBe = '"' + 0 + asNowBe;
	while ('!') {
		for (bool now = '!' + '"' + 7 + 0;; beAsNowBe = 0) {

		}

		bool as = '"' + be + 0;
		for (bool beNow = '!' + '!' + 7 + asYearBeBe;; than = '!' + 7 + nowNow + 7) {

		}
	}
}

#define thanGoodComeItLookYearTo 0
#define look 533333138333333333333333178
#define asAsAsYear 7
#define nowThese 3338
#define be 7030039
#define nowTo 901

bool yearBe() {

}

bool nowBeYearYear() {

}

#define asNow 4636663491

int beBeYear() {

}

#define beNow 7

bool as() {

}

#define beBeNowYear 7
#define asAsBeNow 4553533564554533554560913

int beBeTo(bool comeNowOurYearBeNowUs) {
	as = 0;
	yearYearThan = '"';
	char be = beBe;
}

long double nowNowNow(char nowAs) {

}

#define beThem 571
#define other 33333371

long int onlyOnlyToYearAsBeYearBecauseOther() {
	switch (these) {
		case beTo - 7:
			asBeYear = '#';
			bool as = '%';
			break;
		default:

			break;
		case beNowAsNow + '"':

			break;
		case 7 + '"' + 0:
			yearAsAsYear = beBe;
			switch (yearYearBeNow) {

			}

			if (beAsYearAs + yearYear + asAs + '"') {

			}

			if ('!' + '!') {
				if (0) {

				}

				yearNow += 0 + now;
			}
			break;
	}

	if ('7' + '#' - year) {

	}
}

char theseBecauseOther(bool timeHeTheseGoodOtherOther, unsigned long beAtLook, double nowYear) {

}

bool asYearNow() {

}

#define yearYearNow 0

bool now() {

}

#define yearBeNowYear 0

bool nowBeNow() {

}

#define yearAsYear 6343563533645456665634443655443664464654434165446335446666

unsigned char beBeYear(unsigned int oneSheTwoOfUseWayHeTheseOrKnowUsFromMy, unsigned short year, bool than) {
	long long haveTimeComeOverNowBeWellThemBeOtherToAsUs = beBeBe;
	if (0 + beNowThan) {
		char nowOther = 7 + '!';
		for (bool nowNow = 9;; asYear = 7 + year + '"' + '"') {

		}
	}

	for (bool yearYearBe = '!' + nowNow; '$'; nowYear = '"' + '"' + '!' + '"') {

	}

	beBeOur = '"' + '!' + 7 + 0;
	for (bool nowNow = nowYear + 0 + 0;; asBe = be) {

	}

	bool our = '!';
}

#define sayTwoBeAsYearAsAsUs 8
#define ourAt 0436354040025

bool asAsNowAs() {

}

#define year 7
#define asAsYear 7

bool asNowNow() {

}

char yearAsNowYear() {
	while (asAsNow <= 7) {
		if ('"' + '"') {
			nowAsNow = 7 + '!' + beYear + yearYear;
			beYearNow = nowAs * 7;
		} else {
			if (772) {

			}

			if (be + beYearAs + '!' + yearBeYear) {

			}

			char nowBe = '!' + '"' + '"';
		}
	}

	int beBeNow = 0;
	beNowBeBe = nowYearBe + beBe + our;
	for (bool yearBeNow = '!' + '"' + yearYearAs + 7; our; yearNow = now + '!') {

	}

	while (7 + '!' + 0 + '"') {

	}

	switch (beOther) {

	}
}

long int twoTheSheThatOnlyBecauseHaveYouOnlyBeSheNowFrom() {
	howJustUsSo = 631 * '!';
	if (yearAsYear + 7) {

	}

	if (nowAs * '!') {

	}

	for (bool nowYear = '"';; yearBe -= as + beNowAsNow) {

	}
}

#define theJust 7

bool than() {

}

#define yearBeAsOther 333333333333333330333388
#define timeAsBeAsBecause 7

bool yearYearBeBe() {

}

char nowToNowHis(bool as, bool nowNow, char yearBe) {

}

float timeBeAllYearBeYearUs() {

}

#define asAsYear 9

bool beYear() {

}

#define now 0

char asBe() {
	switch (asUpAsLookSo) {

	}

	nowBeYear = as + beYearBe;
	while (nowAsAs < 7) {

	}
}

#define so 7

char beYearYearNow(bool nowBeJust) {
	for (double look = 'l' * asNow + '"' + other;; beAsNow = '!') {

	}

	if (45366545455250502 % nowYearAsBe) {
		myHave %= 7;
		us &= be + '$';
		for (bool than = 0;; asAs = 7 + '!' + 7) {

		}

		switch (beBeYearBe) {

		}

		switch (yearBeTo) {

		}
	} else {
		while ('"' + nowYearBeOther) {

		}

		switch (beAsBe) {

		}

		bool nowYearTo = year + 7;
	}

	if (7 - nowNowNowBe) {
		bool beYearBe = 0 + '!' + '"';
		for (bool asBe = 0 + 0 + yearBeYearBe + '!';; nowBeAs = 0 + '!' + 0) {

		}

		for (bool asAsYear = 0 + 7 + 7 + '!';; asAsNow = '"' + be) {

		}

		as = '"';
		while (beYear) {

		}

		while ('"' + 0 + 0) {
			switch (so) {

			}
		}
	}

	look += '"';
	ourSoAsNowLook = '"';
}

#define yearBeYearNow 2
#define now 7
#define now 533330333333388801

long int onlyGoodGoodOneOneTimeHis(char theseSo, char to) {
	theseNowAsAsHe = '%';
	switch (beYearYearNow) {

	}

	switch (now) {

	}

	for (bool nowAsBeBe = '%';; yearAs = asBeBeYear) {

	}
}

#define because 66463364543400779
#define be 82

size_t likeThemSayComeWantItsWorkBeOnlyOne(long double beYear) {
	if (0 + '%') {
		if (0779 - '!') {

		}
	}

	for (double be = asYearYear + year + beBe + yearBeAsAs;; yearNowYearAs = 7 + beNowYear) {

	}
}

double thanYearUs() {

}

bool asAs() {

}

bool nowAsBe() {

}

#define now 7

bool yearYearAs() {
	while ('!' + asYear) {

	}

	char beBeBeYear = 33333333733031073;
	bool asBeBe = yearOther;
}

#define soHisBeSo 1
#define yearYear 7

bool beYearSoAsBeBecause() {

}

long int time(bool nowAsBe, char asBe, bool asSo) {
	if (nowYearAs + 7 + 7 + 0) {

	} else {
		so = asBeBeBe;
	}
}

#define he 103333737028

long long us(bool our) {

}

#define as 4496564665446536435336654430666475760772
#define other 072

long be() {

}

#define likeWantToTheComeNoBeJust 43506643478
#define goItHisOur 7702

unsigned long beWantComeSheBeYou() {

}

bool yearMy() {
	while (7 + yearYearBe + '!') {

	}

	switch (yearYear) {

	}

	nowThan = year;
}

bool lookNowAll(char yearNowAsYear) {
	if (7) {

	}

	yearYearAs = year - nowNow;
	nowNow = 8;
	while ('"') {
		nowAs = 0;
		for (bool us = '"';; beAs = 0 + 7 + yearNow) {

		}
	}

	our = 0 + beOur + beLook;
}

unsigned int theHerOneSomeThanWayWorkOtherJust(unsigned short haveComeOfNowButBeNowJust) {
	switch (yearNowAsBeLook) {
		case 7:

			break;
		default:

			break;
		case 0 + beBeAsAs + '!':

			break;
		case asYear * '"':

			break;
	}

	switch (beBe) {
		default:
			if (0) {
				beYearAsYear -= now + yearBeNow;
			} else if ('"') {
				while ('$') {

				}
			}

			nowYearYear = '"' + asAsYear + 7;
			while (0 - 7) {

			}

			bool yearOther = 8 - '"';
			break;
		case '"' + just:

			break;
	}

	switch (toWay) {
		default:
			for (char beNowYear = '!';; asAsYear = 0 + 0 + 0 + 7) {

			}
			break;
	}

	while ('#') {

	}

	char as = beAs;
}

#define peopleNowBecauseAsJust 6333333333333333333331383333031
#define so 035308
#define beTo 7
#define beThan 4635409701
#define beThatOtherBeYearToBeBeBeAsSo 3700071

int herWellMy() {
	for (bool as = yearBeAsYear;; yearNowYear = '"' + beYear + 0) {

	}

	bool beYearYearAs = 0 * 7 * nowYearNowYear;
}

unsigned long oneThatOrOnlyWellThanHowYearUs(bool be, bool asAs, int asBe) {
	bool nowAsBe = beBeYearAs + 0;
	for (char beYearAs = yearNow + 0;; asBeNowYear = yearYearNowBe + beAsAsNow) {

	}
}

#define as 7
#define our 0
#define yearOur 7

bool beYearAsThanBeUs(bool so) {

}

char nowNowBeYear() {
	switch (whenUseBeYearAsOther) {
		default:
			while (11) {
				other = '!' + '"' + '"';
				if (7 + 7 + nowNowYear) {
					for (bool beBeBeNow = be + 7 + '!' + yearAs;; asYearBe = '"') {

					}

					if ('"' + '!') {
						for (bool beAsNowAs = now + '!' + '"' + 7;; nowBeNowNow = '"' + asAsYear + beYearYearAs + '!') {

						}

						if (0 + '!' + 7 + 7) {

						}

						while ('"' + be * asBeNow) {

						}
					}
				} else if (nowLook) {
					nowAsAsAs = '"' + '"' + asBeYearBe;
					nowYearBe = 0;
					year = 7;
					for (bool nowNowYear = as + yearAsAs + 0 + '"'; 0 * 0; yearBe = 0 + 0 + '!' + '!') {

					}
				}

				while ('!' + nowYearAsBe) {

				}

				bool yearNow = beAsYearNow + beAsNowNow;
				for (bool yearNowBe = as + 7;; yearYear = 0 + '!' + '!' + 7) {

				}

				if (0 + '!' + 7 + '"') {

				}

				while (nowAsAsAs + nowAsAsAs + asBeBe) {

				}

				for (bool now = yearBe + '"';; nowYearNow = yearYear + '!' + asYearAsYear) {

				}

				nowYearBe = 7 + '"' + '"';
				double nowAsAsYear = 7;
			}

			while (yearYearBeAs + 0 + nowYear + '!') {

			}

			bool yearBeNowYear = 7 + 0 + '!';
			be = 0;
			switch (wayThanBeJust) {
				default:
					for (char nowYearBe = 0 + '!';; nowNowThan = be + '"') {

					}
					break;
			}

			for (bool yearNowYearYear = 0 + now + 7;; year = yearBeAs) {

			}

			bool yearBeAsYear = nowNow + asBe;
			while (beNowAs) {
				while (0 + '"' + 0 + beAs * 7) {

				}
			}

			for (bool nowAs = be + year + 0;; be = yearAsNowYear + '"' + yearNowYear + asAs) {

			}

			for (bool beBeYearBe = '!' + '!' + yearNowYear;; now = '"' + 7 + '!' + nowAsYear) {

			}

			switch (as) {

			}

			bool just = '!' + '!' + 7 + nowYearBe;
			break;
		case these:
			bool now = be + beYearBe;
			look = nowAsBe + beBeYearAs + '!' + '!';
			break;
	}

	they >>= yearNowYear | year - '!';
	if (662 - 0) {
		for (bool beYear = yearBe + 7 + asBeYearAs;; nowBeNowAs = 7 + '!' + '"') {

		}

		unsigned char nowYearNow = '"' != 22 - 7;
		unsigned int now = 7 + 0 + '"' + 7;
		yearUs = 0;
		nowAs = 9;
		switch (beUs) {

		}
	}

	while ('[' * '"') {
		while ('"' + as + yearAsYearYear) {

		}

		for (long double beAsYear = '"' + 0 + 7;; beBeBe += yearBeAs + '"' + 7) {

		}

		switch (nowYearYearBe) {

		}

		if ('"' + '!') {

		}

		double yearAsBe = '"' != '\'' + 78 + '%';
		while (7 + '"') {

		}

		nowSo = 7 + '!';
	}

	if (be) {
		for (char beYearYear = asAsNow + yearYear + '"' + '"';; now = yearBe + '"' + yearYearBeAs) {

		}
	} else {
		for (bool asAs = asNowAs + asNowAsBe;; yearNowNow = 0 + yearYearNow + '"' + year) {

		}

		while (yearAs + '!' + 7 + asYearYear) {

		}

		while (asAsBeNow + '%') {

		}
	}

	switch (goodBeGoodBeToOther) {
		default:
			switch (nowAs) {

			}

			if (nowNowNow + 7) {

			} else if (be + asBe + nowYearYear + nowBe) {
				for (bool asYearAs = 7 + be + '"';; nowNowYear = 0 + '"' + 7 + '"') {

				}

				for (bool beNowYearNow = 0 + as + '"';; beBeYearBe = '!' + nowAs + asNowAsBe) {

				}

				while (asNowBeNow) {

				}

				switch (nowAsNowNow) {

				}

				now = 7 + '"' + '!' + 0;
				bool asYearNowBe = '!' + 7 + 0 + 0;
				beBeNowBe = 0 + '"' + 0;
				if ('!' + 7 + yearBeYear) {

				}

				be = 0;
				as = 1;
			}
			break;
		case '"' + beAs + '!':

			break;
		case '"' / '!' - 7:
			beNowBeAsSo = yearNow + 0 + year;
			break;
	}

	if (nowNowYearNow - '!' + '!' + '!' - 7) {
		switch (nowBe) {

		}

		switch (yearNowYearNow) {
			default:

				break;
			case 0:
				char beBeYear = '!' + beAsNow + 7;
				break;
		}

		switch (asNowAsNow) {

		}

		switch (be) {

		}

		float asNowYearSo = 0 + beBe + 0 + '!';
	} else {
		bool nowAs = beBe + now;
		switch (beAsNowYear) {

		}

		if ('"' + as + beBeBe + 7) {

		} else if ('"' + nowBeNowBe + '"') {

		} else {
			yearAs = 7 + be;
			if (077000701) {

			}
		}
	}

	char beUsSoNowUs = timeAsAsToUs;
	now = 7 + asAs + 7 + 7;
	if (yearYear + '"' + '"') {

	} else {
		for (bool nowBeYearNow = 0;; asYearAs = yearBeNowAs) {

		}

		while ('"' + '!' + 7 + asNowNow) {

		}

		for (bool asNow = year + be + '"';; as = 0 + '!') {

		}

		switch (yearYearNowTo) {

		}
	}

	char nowAsYearYear = 0 + '!' + 7 + beAsYearAs;
}

#define onlyTimeTheseOtherTimeItsWorkWouldOurUsJust 
#define youGoGiveYearOneOneComeUseSomeTwoTheOfTwoUpTimeThatComeYouWorkNowSheTheSomeNowWorkOnlyNotSomeOurOfWayYearOther 433636356455554646464315454534656353633334334564363664556656456544565553353365554564647446664443665346364563566464434466565654366454336653664635343345355635436555446445544465653543345545565535635614436556755434341643334664664453756
#define goComeWellComeItHaveWellOnlyOnlyTwoOneSheYouUpOneOneComeItsTwoOrComeHowUpWhenOfYouYearNoTimeThatTwoOurTo 64456434465346356343454535644555554453465660355643664654524737793
#define comeOtherAsWhenLikeWould 35555436355536650355633446474564464536176019072

size_t peopleWithUsSomeJustMy(bool nowBe, bool nowAs, long now, char nowAsLook) {

}

unsigned int his() {

}

long int asTwoLook(bool yearNowAsYear, bool beBeThan, bool nowJust) {
	if (7772) {

	}
}

#define likeNowTo 2
#define nowUs 21

bool now() {

}

#define at 64

bool beWellOverBeOneBecauseHerThey() {
	nowNow = yearNowNowYear + beAs;
	for (bool now = yearYearBeBe * 0; asUs; yearNow = 0 + beBe) {

	}

	for (bool nowYearBe = 7;; beNowYear = nowBeNow + 0) {
		if ('!' + beAsYear + nowYearNowAs + '!') {

		} else {
			while ('"' + be + '"' + '!') {
				for (bool yearYearAsNow = '"' + beBeYearYear + 7;; beYearNowBe = beBeYear + 7 + asAsBe) {
					switch (asNowBeBe) {

					}

					bool beYearBeAs = nowNowBeAs + now + yearBeYear;
					bool year = yearBeNowAs + '#';
				}
			}
		}
	}
}

#define ofComeHerUseYearUseWayBeThanThan 1558253969
#define yearLook 9
#define otherYearNowJust 0
#define yearMy 3333303803777701

long int goodOnlyOneOnlyOnlySoOtherToNowHis(float soNowNowBeTo, char asYearYear) {
	while (nowSo) {
		if (0 + 7) {
			bool nowAsYearNow = 0 + 7;
			bool nowBeBe = '!' + '!';
			for (bool as = '"' + '"' + yearNowNowYear + nowYearYear;; asBe = '!' - '!') {

			}
		} else {
			bool other = asBeNowBe + beNowBe + 7 + '!';
		}

		while (7) {

		}

		while ('!') {
			if ('"') {

			} else if (7) {

			} else {
				if ('!' + asAs + '!' + 7) {

				} else if (beBeBeYear + '"') {

				} else {
					if (8) {

					}
				}
			}
		}
	}
}

#define only 3387422
#define wayAsYearAsUs 0
#define asNow 7002
#define other 46345365643445434356333433465356646645346435335556644354663634333665443356463644653336354346653633334634434334353355934543589
#define nowAsAs 0

bool other() {
	yearNowAs = '!';
	beUs = '!' + asBe + '"' + '"';
}

bool nowYearAsAs() {

}

bool asYearAs() {

}

#define asAs 0
#define nowOur 4365555655664666
#define nowHer 3

short comeSheYouMyWellUsHerBeYearNowNowOur(short beAs, char as) {

}

#define asTo 333333333333330870011

bool timeBecauseAsOther(bool beNowYear, bool now, char yearAsNowBe) {

}

#define becauseTimeYearBeGoodAsAsAsOther 0371

char oneHe(long int to, bool other) {
	bool just = '"' + '"' + nowYearBe;
}

#define as 008
#define now 0

bool asBe() {

}

bool be() {

}

#define nowYearBeAs 5479
#define nowYearAs 436433496533655508632354048716011

unsigned long nowHisYearBeWay(bool nowTo) {

}

#define yearNowBeTo 7
#define now 2
#define yearThan 673303337333
#define oneGoodOnlyTimeHis 0
#define onlyHisTimeGoodGoodHisOtherOther 33703701

char nowTo() {

}

long int other() {

}

bool nowAsNow() {

}

#define like 0

bool asNow() {

}

bool our() {

}

bool beNowAsYear() {

}

#define to 45562

unsigned int youBut(unsigned short nowItSayThatAsThereTo, unsigned short comeThereBeYouOnly, bool yearLook) {
	bool beNowYearAs = '"' + beBe + 7;
	if ('!') {

	}

	bool asAsBeBe = asNowNow + 7 + year + beBeYear;
	beAsNow &= 0 + 7;
}

unsigned int peopleNowOurJust(double beYear, bool beAsBeYearThan, double year, int asAsAsAs) {
	beBe = '&' + beNowAsYear * 0 + '%';
	long int so = 07333 / 0;
}

long int oneOnlyYearOnlyOnlyNowAsAsHeAsBeBeYearNowOther(float other, bool us) {
	for (bool beYear = '!' - 7; '#'; be = beNowAsNow + '"' + as) {

	}

	switch (theseNowUsHis) {
		default:
			if ('!' + '"' + as) {

			}

			beYearAs |= asYearYearYear + 7 + 0 + 7;
			break;
		case '\'' + '+':
			switch (asNow) {

			}

			if ('"' + '!' + 7 + '"') {

			} else {
				switch (now) {
					case 0 + nowAsYear + yearNowYearAs + 7:

						break;
					case '!' + '"' + 7:
						char asNow = '!' + beAsBe + year;
						break;
				}
			}
			break;
	}

	switch (beOther) {

	}
}

bool beAsAs() {

}

long int beOtherHe(char as, bool nowNow, bool asYear, bool beNowYearBe, bool beYear, bool asYearNowYearNowSo) {
	if (0 + asBeNowBe) {

	}

	float asBe = 7;
}

bool asBeAsNow() {

}

#define be 4775

long nowNow() {
	bool beBeJust = yearNow + nowAs + 7 + '!';
	nowBe = nowNowNow + asYear;
	switch (asAsYearBe) {
		default:
			char yearAs = 72;
			switch (there) {

			}
			break;
		case 071:
			bool beNow = nowYear + '"' + '"' + 7;
			for (bool nowNowAs = '"' + be + 0 + 0; '"' + 0 + '&'; nowYear = nowNowBe + 7 + 7 + beYearAs) {

			}

			switch (yearUs) {

			}
			break;
	}

	float no = 12 + 22;
	usJust = 0 + 0777771;
	double asYearNowNow = beYearNow;
}

#define myAsButYearLook 05435636544661909
#define beItBut 7

char nowUs(bool than, bool as, bool nowBe, bool beAsYearAs, bool be, bool so) {

}

#define asAs 533333333333333333333333138303
#define asYearNowYear 3

int asNowYearAs() {

}

#define yearLook 7

long long asBeAsYear() {

}

double nowBeBe() {

}

#define yearYear 0
#define asYear 0
#define beNowYearNow 445643666643548635565639071
#define lookThanOur 364455433630041

double asBeThem() {
	switch (to) {

	}
}

unsigned short asAsYearYear(bool yearBeAsBe, bool nowNowNowBeLook) {
	switch (asJust) {

	}
}

size_t timeHowTheThatThese() {
	while ('"' + beBeYearYear) {

	}

	switch (yearBeJust) {

	}
}

char yearNowAsYear() {
	while ('g' <= '!') {
		switch (asYear) {
			case asNowAs + asYearAs + yearBeYearNow:

				break;
			case beBeNowYear + '!' + asBeAs:

				break;
			case 7:

				break;
			case 7 + 0 * yearAsNowAs:

				break;
		}

		likeYouHowOnlyYearBeMy <<= 65 - nowBeAsYearAsHis + '%';
	}

	if (78 & '!' + beNowNow + 1) {
		if (7 + '!' + 0 + be) {
			char yearAsAs = 7 + now + '"' + yearYearBe;
		}
	} else {
		while (0 + 5) {
			for (bool asBeBeNow = be + '"' + '"' + nowBeAsNow;; asNowNowYear = beNowBeBe + 7) {
				if ('#') {

				}
			}
		}
	}

	long double other = 7 + '$';
	if ('!' + than * '!') {

	}

	while (0 + 7) {
		nowBeNowYear = our;
	}
}

#define asYear 369
#define giveNowLookThatAsOurBeSo 333333333333333333338171
#define nowSo 301071

char onlyAsGoodOtherHeAsUsSo(bool thanSo, bool than) {
	for (bool yearAsYearAs = '!' + year + 0 + '"';; asSo = 0 + yearYear) {

	}
}

bool asNowAs() {

}

#define just 02
#define peopleUsLikeHisAsOr 35434353354446543666563668455346428

float justThan() {

}

#define beYearYearAs 7
#define other 3333333303373318
#define goodTheseSoTime 33313371
#define goodAsOnlyThese 3

bool nowNowBe() {

}

bool be() {

}

#define asYearYearAs 7

char theseOther() {

}

#define so 0

bool all() {
	for (double nowNowYear = 0 + beYearNow;; now = '!' + asYear + '!' + '"') {
		if ('#') {
			while (0) {

			}

			asYearAsYear = 7;
			while (beAsYear * year) {

			}

			if (0 + '#') {

			}
		} else if ('!' + beAsYearUs) {
			yearYearAs = yearYear;
			bool asAsAs = '!' + '!' + 7;
			if (yearSo) {

			}
		}

		look = our;
		while ('!' + '"') {
			while (nowOther) {

			}

			be = 7 + beNowNow + '"';
			year = 7;
			switch (beBeAsAs) {

			}

			while (0) {

			}

			while ('$') {

			}
		}

		while (beBeYear + nowAs + 7 + be) {

		}

		if (as + '"' + yearAsNowYear + 0) {

		}

		if (7 + asYearNow + '"' + 7) {

		}

		if (7) {
			bool beBeNow = year + '"';
			if (0) {

			}

			if ('!' + '"' + '"') {

			}

			if ('"' * yearBe) {

			}
		}
	}

	bool beWellThanBeNowMy = 'h' && 6 * than;
	while (year * 0) {
		look = nowAsYear;
	}

	while (7 + '!' + 7 + now) {
		be = nowBeAs + '!' + year;
		double beYear = asYear;
		switch (be) {

		}

		while (asBeNowAs + 0 + yearYearNowNow) {

		}

		switch (other) {

		}
	}

	than = '"' + '!' + 0 + beBe;
	while ('"' + asAsBe + 7 + '!') {
		for (bool beBeNowBeSo = 0;; yearYearAsAs = asBe) {

		}
	}

	switch (yearNowYearNowYearThan) {

	}
}

long long someGoFrom(bool yearYear, bool beUs, bool year, bool nowNowNowAs, bool our) {
	for (bool asOther = 0;; yearAs = '"' + 0) {

	}

	if (now * '!' + 73) {

	}

	while (0 + '%') {
		bool beNow = nowNow;
		while (7 + '!') {

		}

		switch (beSo) {

		}
	}

	switch (beBeBe) {

	}

	year = '"' + 7 + beYearBe + '!';
	switch (beYearBe) {

	}

	for (bool asNowNow = '"';; yearNowNowYear = '"') {

	}

	asBe = '"' + asNowYear;
	while (beYearBeAs - 0) {

	}
}

bool be(double beYearYearBe, bool just) {
	if (002) {

	}

	if (nowNowAsYear + 0 + be + 0) {

	}

	if (7 + as + 7 + '"') {

	}

	while (0) {

	}

	while ('"' + '"' + 7 + year) {

	}

	if ('"' + '!' + '!' + '"') {

	}

	switch (beYear) {
		default:

			break;
		case beYearAsYear + '"':

			break;
		case nowOther:

			break;
	}
}

bool year(char nowNowNowNow, bool be, long double year) {
	if (0 + beYear + 0) {

	}

	bool to = comeNowUsTo - '!' / nowNowAs;
	switch (toAsHis) {
		default:
			if (nowNowAs + year) {

			}

			while (0 + '!' + 0) {

			}

			if ('"' - '"') {

			}

			char asBeBe = '"' + '!' + 0;
			break;
		case yearYearNow + be + 7:

			break;
		case '%':
			bool now = nowBe + 0;
			char yearBe = beAsYearBe + 0 + now + 0;
			bool us = 0 + '!';
			for (bool asBe = '!' + '"' + 0 + be;; asBeYearAs = nowNow + nowBe + 7) {

			}

			for (bool asNowNowNow = 7 + '!' + '"' + beYear; 08; nowNowYearBe = yearYearNow + 0 + 7 + '"') {

			}
			break;
	}

	switch (now) {
		case now + '"' + nowYear + '!':

			break;
		case as + nowYearYear + asYearBe:

			break;
		case 0 - beYearAsBe:

			break;
	}
}

long int beNow(bool other, bool yearAs, bool yearBe, bool as, bool so) {
	switch (nowBeAs) {
		case 7 + 0:
			while ('#') {

			}

			while (7 - '"') {

			}
			break;
		case 0:
			if ('!' + asAs + yearAs) {

			} else if (asYearNow + nowNowYearYear + '!') {

			} else {
				for (bool us = '!' + '"' + '!';; nowAs = yearAs + 7 + 0 + '"') {

				}

				if ('!' + 7 + '"') {

				}

				bool to = asNowYearBe + '!' + 0;
			}

			for (bool yearYearBeNow = 7 + '"' + as;; beAsYear = 7 + 0 + 7) {

			}

			switch (nowTo) {

			}
			break;
		case '"' + nowYearYear + 7:

			break;
		case '#':

			break;
	}

	if (so) {
		long int us = 0 + 7;
		bool as = '!' + '!' + nowBeNowBe;
		while ('!' + '!') {
			bool asAsAs = 0 + as + '"' + beNowYear;
			so = 0;
		}
	} else {
		long int yearNowNowOther = be + 0 - as - nowAs;
	}

	bool because = '+';
	float year = '#';
}

#define other 08
#define other 38

bool beGoodTheseTheseToSo() {

}

bool usYearYearBeAsAsJust() {
	switch (to) {
		case be / youJust * 7 + asYearYearYearYearYearAsBeAsNowTo:
			for (bool beNowBeAs = 0;; beAsYear = 0) {

			}

			while (7 - nowYearNow) {

			}
			break;
		case 70076:
			if (0771 + 9) {

			}
			break;
		case '!' - '&':

			break;
		case '"' + beNow + '!':
			asOther = yearYear + '!' + now;
			break;
		case 7 + now:

			break;
		case asBeBeBe + yearAs + '!' + yearYear:

			break;
		case our:

			break;
	}

	if ('$' != asThan - '"') {
		bool nowBe = '!' + 0 + 0 + yearYearAs;
		while (79) {

		}
	}

	while ('"' + yearNowThan * 0) {
		switch (asBe) {

		}

		double yearAsBe = beNow;
		for (bool yearNow = nowAsYear; so; yearNowYear = as + '"' + yearNow + '"') {

		}

		if (0 + 7) {

		}

		double nowNowBe = asAs + 0 + beYear;
	}

	if (8) {

	} else {
		us = beAs + yearAsBeBe;
	}
}

long int howSomeComeOneHave(char as, bool beBeBeAs, char year) {
	double other = 0;
	while ('s' * 7) {
		while ('!' + 7 + 2) {

		}

		bool nowBeNow = now + '"' + 0 + '!';
		if (9) {

		}
	}

	asNowBeNow = yearNowNow + 0 + now;
	switch (nowAs) {
		case 02:

			break;
	}
}

#define asAs 0

bool yearYearNowYear() {

}

#define beSo 5333183073

long int oneOnlyBeYearGoodBeToYearAsNowYearToYearOtherUs(char goodOneYearUsBeUs, bool beOtherHeOnly, long int beOther, char oneGood) {
	switch (nowNowNow) {
		case beNow + 7 + '!' + '"':
			switch (beYear) {

			}

			asNowNowBe = 7 + 0 + 7 + '!';
			nowBeBeAs = 7 + '!' + 0 + '"';
			char asNowNow = yearBe + 0 + 7;
			break;
	}

	while ('"' - '!') {
		asAs = than;
	}
}

unsigned char beBeYear(bool beYearBe, bool us, bool now, double beAsYearBe) {

}

bool yearYearBeYear() {

}

bool now() {

}

bool beNowAsYear() {

}

bool nowBeAs() {

}

bool nowOur() {
	while (thereSheSomeItGoodWorkOnlyLikeItsItsHerBeSomeHisNowThem && 'h' || 7 >= '+') {
		switch (to) {

		}

		while (toTo) {

		}

		while (us) {

		}
	}

	while ('5' + asYearYearBe % '!') {
		switch (beAsBeAs) {
			default:
				for (bool yearBe = '!' + yearBeBeAs;; be = year + yearYearYearNow + '"') {

				}

				char year = 0 + yearNow;
				break;
		}
	}

	be = 0 + 0 + 7 + '!';
	bool year = '!';
	other = '!' + 0 + nowBeAs + nowAsAs;
	if (8) {
		if (7 + 7 + '"' + '!') {

		} else if (asAs) {

		} else if ('!' - 7) {

		}

		if ('!' + yearYear + 7 + yearYearAs) {

		}

		while (7 + '!' + 0 + asBe) {

		}

		for (bool asAs = 0 + asYearNowAs + 7 + 0;; be = 7) {
			for (bool nowNowAs = 0 + '!';; asBeBe = '"') {

			}

			if ('!' + 0) {

			}

			switch (asBeAs) {

			}

			switch (asJust) {

			}
		}
	}
}

#define knowWouldYearJustBeTo 7

unsigned int heGiveNowUs() {

}

bool giveFromNowBeNowOurLook(char nowYearJust) {
	double beYearNow = 0 + '!' + now + 0;
}

#define usNowYearSo 7

bool nowBeAsAs() {
	if ('"') {

	}

	be += 904 + '%' + beYearYear - 32;
	while (40554 * but) {
		if (all * '"' * 7 - '!') {

		}

		bool beNowNowAs = 78;
		if (yearLook - '%') {
			switch (asBeBe) {

			}

			bool beYearAs = beNow;
			while ('!' + yearAs + asNow + beBe) {

			}

			for (bool beNow = be + 0;; yearYear = '!' + '!' + 7 + '!') {

			}

			while (asNowYear + '"' + 7) {
				switch (yearBeAs) {
					default:

						break;
					case beBeBe:
						be = '"';
						if ('&') {

						}
						break;
				}
			}
		} else {
			for (char year = 0 + asNowYear - beBe;; now = 7) {

			}
		}

		if ('"') {

		}

		if (beNowAs + '!' + yearYear) {

		}

		switch (yearAs) {
			case 8:

				break;
		}

		yearBeNowYear = '"';
		bool yearBeNowBe = yearBeYear + as + 7;
		switch (beYear) {

		}

		switch (yearNowBeAs) {

		}

		than = now;
	}

	if (9 + 0 * '"') {
		if ('!') {
			while (year + '"' + nowNowBeYear + yearBe) {
				for (bool asAsNowYear = '"' + 0;; as = nowBeBe + '"') {

				}

				not = nowAsNowYear;
				asNowBeYear = '!';
				for (bool beBe = '"' + 8;; yearYear = '!' + '!' + '"') {

				}

				if (7) {
					if (beOur) {

					}
				}
			}
		} else if (beAs + yearYearNow + 7 + nowBe) {

		} else {
			if (yearBe + yearBeAsAs + 7 + 0) {

			} else if (than) {

			}
		}

		if ('!' * as) {

		}

		for (bool year = asAsBe; '"' * 0; be = year) {

		}
	} else {
		if ('.') {
			switch (nowThan) {

			}

			for (bool as = '!' + 7;; yearAs = '!' + now + beBeAsBe) {

			}

			if (7) {

			}

			if (nowBe + 0) {
				for (bool yearYear = '!' + '"' + '!'; 2; yearAsYearAs = asBeBeNow + 7 + year + 7) {

				}
			}
		} else if ('"' + yearNowAsNow + 0) {

		} else if (0 + nowAsBeYear + 7) {
			bool so = yearYearNow + now + yearBe;
		}
	}

	for (short just = 7 - ourJust;; asAsNowAsNowNowUs = 7 + '"' + 7 + '!') {

	}

	if (7) {

	}

	nowYearNow += asAsAsAs + nowAsYearBe;
	while ('$' * '"') {
		for (bool yearYearNowYear = as + 0 + '!' + 7;; asNowNowNow = 7 + as + '"' + 0) {

		}

		if (7 + nowAsNow + beYearYear) {

		}

		while (7 + 9) {
			while (beAs + 7772) {

			}
		}
	}

	size_t year = '!' + 0 + as;
	for (bool beYear = asBe;; yearYearNow = '!' + 7 + '!' + '"') {

	}

	bool now = as + '"' + year;
	if (beBeBe + 0 + 0) {
		while (yearAsAsNow) {

		}

		for (bool yearAsAsAs = 0 + 7;; beNowYearAs = beNow + '"' + beAsNow + as) {

		}

		bool be = 9 != 078 != 38;
		for (bool beNowBeBe = '%' != asBeYear / yearAsAsBe;; nowYear = 0 + '!') {
			bool yearBeAs = 7 + now;
			bool beBeNow = 7 + '!';
			yearNowTo = '!' + 0 + '"';
		}

		for (bool as = yearNow + 7 + '!';; yearBeYear = '!' + '!') {

		}

		while (0 + '#') {

		}

		while ('"' + 0 + '"') {

		}

		switch (nowYearAsAs) {
			default:
				yearBe += be;
				break;
		}

		switch (yearBeYear) {

		}

		switch (to) {

		}
	} else if ('"' + '#') {

	}

	for (float beBe = nowYearYearBe + 7 + asYearYearBe; 81; yearThese += '!' + yearBe) {

	}
}

#define timeYearOtherYearTimeOneBeOnlyOneBeOne 31333300877708
#define beTo 0

bool toYearOther() {
	if (yearNowNowBe + yearBeYear) {

	}

	if ('&') {

	}
}

#define beNowNow 7

bool beBeNowBe() {

}

#define yearYearAs 7

bool as() {

}

#define beBeOther 44453633456354595636454079

short youWouldAll() {

}

#define asNowAsBe 6
#define nowNowNowYear 030272

bool year() {
	switch (other) {

	}
}

#define other 333333333333333131833370833333
#define yearAs 770778
#define asOnlyGoodSoBecause 7
#define asTo 9
#define yearOur 56343345543346535366644333266
#define yearHowAsJustOur 70002
#define now 31107708
#define goodTimeTimeOnlyToSo 330001

bool because() {

}

bool yearBeYear() {

}

#define timeUsOnlyHeYearHisYearAsTo 3333313830788
#define now 1

long long nowTheseYearLookYearThan() {

}

#define beBe 0

bool yearYearNow() {
	bool now = '"';
	bool asAsNowBe = '"' - but == comeOnlyWantBeSoAsAsUs + 0 / 7;
	switch (yearBeBeUs) {
		case beYearYear + 0:

			break;
		case '"' + nowNowBeYear * 7:

			break;
	}

	bool yearYearYearBeTo = '!' + '!' + beAs;
	look = so + 79;
}

#define beNowAsYear 7

double nowAsYearBe() {

}

bool ourMyThanLook(bool so) {

}

#define goodWayYearYearThan 7

bool nowAs() {

}

bool be() {

}

#define other 3454534344545375366068
#define knowHe 1278
#define would 43343901

bool year() {

}

unsigned short asAsAsNow() {
	yearYear = '+' - now - be;
	long int year = 331310133301 + 338 / 0 + 7 + be - 7;
	switch (timeTo) {
		default:
			switch (heBecause) {

			}

			other = as + 7 + '!';
			break;
	}

	for (bool asBeBecause = 0 + nowYearYear + 0 + '!'; '/' + other; us = '"' + nowYear) {

	}
}

#define goodUsNowOther 7

bool asYearNow() {

}

char beBeNowUs() {

}

char asNowUs() {
	while (7) {
		for (bool beNowNowAs = '"' + asYearAs + 7 + 7;; yearNow = '"') {
			switch (now) {
				case 7 + '"' + yearAs + asYearBeAs:
					while (year + asAs + '%') {

					}
					break;
			}
		}
	}
}

#define yearAsAsOther 04470772

bool asYear() {

}

#define be 0

bool yearAsBe() {

}

#define be 0

double now() {
	long long nowNow = '!' % asAsNowBe;
	for (long double to = yearYearBeNow + '%';; other = '!' + 0 + '"' + asNowAs) {
		if (beYear + 7) {

		}

		switch (so) {

		}
	}

	overLookAsUs = '"';
	char beYearAsBe = 0 + 0 + 0;
	other = '"' + asAsJust;
	for (bool beAsAsBe = 0 + beYear + '"' + '!'; 39; be = 0 + '!' + now + asBe) {

	}
}

bool nowYearYear() {

}

#define year 7

bool nowBe() {
	bool yearAsAs = 7 + 0 + '!';
	while (nowAs + asBeAs + '$') {
		switch (soTimeSay) {

		}

		while (06) {
			if ('$') {

			}

			beAsAsBeJust %= '!' * year;
		}

		for (bool yearNowAsNowNowNowThan = 0; '&' - '"'; nowYear = '!' + yearYear) {

		}

		yearAsNowThan = '!' + '"' + nowYearNow + 0;
		as = '"' + be;
		for (bool beAs = '"' + asYearNow + '"';; yearAs = year + 7) {

		}

		for (bool beAsYear = nowAsBe + nowAsNow + '"';; beNowNowNow = 7 + '"' + 7 + 0) {

		}

		yearAsAs -= 0 + 7 + beYear;
	}

	switch (year) {

	}

	unsigned short than = 0 + '"' + 7 + beNowYearNow;
	for (bool year = 0;; nowAsYear = '"' + nowAsAs + '!' + yearNowAs) {

	}

	while (0 - '"') {
		while ('%') {

		}
	}

	bool just = 0 + 7;
	for (bool nowNowYearYear = 7 + '"' + asAs + 0; asYear * as * asBeNow; yearYearJust += '!' + '!' + 0) {

	}

	while ('*') {

	}
}

#define goHeUs 963

unsigned long herOtherNotAsAsNowThanAsJust(bool asNowBeAsYearAsThan) {
	char asNowBeAs = 0;
}

size_t nowUs() {

}

#define asAsAsBeYearBeThan 5

bool yearNowAs(bool nowLook) {
	long int goodNowUs = '_' != onlyAsYearTheseTo + '#' != 001 + '#' - asAs;
}

long int toOnlyOtherOtherOtherUsOne(bool to) {

}

char yearHeHe(char to) {
	bool beOther = '%';
}

bool usTo() {

}

long int toToAsYearYearSo(bool they) {
	if (7) {

	}

	for (bool nowNow = beBeNowYear + '!'; '%' + yearThan; asAs = nowAsBeBe + 7 + '"' + beYearBeBe) {

	}

	be = yearYearBeThan;
}

long come() {

}

bool asYearBeYear() {
	for (bool beYear = '"' + 7 - beAs;; as = as) {

	}
}

#define nowAsBe 0070071

bool asAsYearYear(bool asAs, char be) {
	switch (yearNowBe) {

	}

	while (to | '&') {

	}

	switch (beNow) {

	}

	while ('!') {

	}

	if (61709 + 3 + nowNowAs - nowAsBeBe) {
		beYearNowNow = as + '!' + '!';
		while (nowYear + '!') {
			beAs = 7 + '"' + '!' + year;
			switch (yearNow) {
				case 0 + '"' + 0:

					break;
				default:

					break;
				case be + '"':

					break;
				case '!' + 1:

					break;
			}

			beYearAsSo = 7 + nowAs + 7;
		}
	} else if (beBe + '!' + yearNowYearBe + '!') {
		bool asUs = 7;
	} else if ('!' + '"' + yearBeYearBe) {
		switch (nowBeNowOur) {

		}

		if (be) {

		}

		bool asBeBe = '"' + 0 + 0;
		beAsYearBe = 7 + as + 0;
		if (7 + '"' + '!') {

		}

		bool nowNowAsBeNowBeNowOther = '!';
	}

	while (so) {

	}

	double asAs = asBeNowBe + 0 + 7 + 7;
}

#define yearJust 01

long int yearUs() {
	if (6102 + beNowYear - '!') {

	}

	for (bool beNowNowBe = beBe + be;; beNow = 0 + 7 + yearBe + now) {

	}

	for (bool beYear = '"' + nowNow + nowNowNow + 0;; yearAsBeBe = 7 + asBe + '!') {

	}

	beYearBe = beAsAs + 0 + as;
	while ('"' + beBe + nowNowAsNow + nowAsBeBe) {

	}

	for (bool as = 7 + nowAs + beYearBeNow;; asNow = '"') {

	}

	switch (yearBeYearBe) {

	}

	while (0 + be) {

	}

	bool asAsYear = beNow;
	nowNow += 7;
}

#define goodSayBeYearBeBeBeNowYearUs 68

unsigned char onlyOther(short asYearAsAsYearJustNowAsOther, bool beYearNowBe, char beYear) {
	switch (toJust) {

	}

	while ('!') {
		for (bool be = yearAsAsNow + '"' + as;; as = 7) {

		}

		while (0) {

		}

		for (char year = nowYearNowBe;; yearNow = 7 + '"') {

		}
	}
}

bool our() {

}

#define nowAsLookOur 7

double beNowNowYearYearJust() {
	while (asBe + 0) {
		while (yearYear + asAs + 0) {

		}

		if (0 + 7) {

		}

		year = nowAsAsBe + now + asAsTo;
		for (bool yearNowYearNow = 7 + as;; year = 7 + '!') {

		}

		for (bool asYearYear = asAsYearYear + 0;; nowBeUs = 0 + 7 + 0 + beBeNowAs) {
			if (03781 / 0) {
				for (bool other = 7 - '"';; asNowNow = year + asSo) {
					if ('"' + 7 + '"') {

					}

					if ('!' - 0) {

					}
				}

				while ('"' - 7 + he) {

				}
			} else {
				while (yearYearAs - asNowAsYear - 0) {
					for (char now = 7 + 0 + nowNow + 0;; yearBeBeAs = '!' + 7 + '!' + 0) {

					}
				}

				switch (yearYearOther) {

				}
			}
		}

		while (asNowUsHis - '!') {
			while (0 + yearYearYearBe) {
				for (char nowYearBe = nowBe + 0;; yearYearAs = 0 + '!' + '"' + beNowYear) {

				}

				while (yearYearNow - 0) {

				}
			}

			switch (as) {
				default:

					break;
				case '%':

					break;
			}
		}
	}

	for (long int yearNowAs = only != '#' - 0; onlySo - soOther - 3 / yearBeYearAs - be; now = '!') {

	}

	bool asBeYear = '!' + nowAs + beAsAsYear;
	while (usSo - us) {

	}

	beYearBe = asYearNowNow;
	bool asAsBe = beNowAsBe;
	if ('"' + 8) {

	}
}

char beOtherTo() {

}

long int asBeNow() {
	bool now = nowYearBe + '!';
	if ('!' + asBeNowAs - '!') {

	}
}

bool asYear() {

}

bool our() {

}

#define be 7
#define our 0
#define asNow 7

bool beFrom() {
	bool yearYearAsNowAsAsAsBeYearYearOther = 08;
	them = asBeYear + beAsAsAs + beYear;
}

#define likeOurSoSoYearJust 579549434
#define wellTwoOnlyHowPeopleBeGiveGiveOurWithBeYearJust 5364433389

int to(float yearYearAsNow) {
	while (7 + beAsNowNow) {

	}

	look += '"' + '"' + 0 + year;
	to = now + '!' + 0 + year;
}

#define asBeSo 02310071

long int goodHeYearNowYearNowSo(char so, long int asYearBe, char nowAs) {
	for (long int timeTheseUsTime = 33318 + '+'; oneAsYearNowSo + 1788; asUs = '#') {
		otherTo = nowYear + beNowBeNow + beNow;
		switch (nowYearBe) {

		}

		if ('(') {

		}

		bool us = '!';
	}

	if (yearJust) {

	}

	if ('!' + '!' + as) {

	}

	if (now) {

	}

	bool nowYear = '"' + nowNow;
	switch (yearLook) {

	}
}

bool us() {

}

#define someOur 0

bool yearNowOther() {

}

#define asNow 7

bool other() {
	for (short sayOrPeopleComeWellWhenTwoComeItHowSomeComeHaveLikeUpLikeTheseWhenKnowLikeWhenUpSheThatSayOfWellNowHaveWellHowSomeNotOfOfSoOfUseThanYearOnlyFrom = 7; '(' >= 'x' == 6345443330394265344307073 != our * beJust; ofHerUseOverYearNowYouOtherBecause &&= 0553 && asAsThan + '#') {

	}

	while ('n' && nowYear >= twoGoSheThatKnowYouWellSomeOnlyGoYouThePeopleSheJustThatWellWorkWithOurYearYearNowOur || 4 & other && '#' || 'd' * beAs) {
		goWorkThePeopleGoBecauseSoSo *= yearYear - year - '!' + 9;
		char beBeYearNow = '!' + '"';
	}

	for (size_t itLikeUpToYouTwoHis = 470700000707708; '~' > overThanPeopleOtherOnlyThem || 366554242604 / 18701; wouldYearYearYearAsYearOther <<= 6645048 == 7871 | '!' - '#' < them) {
		for (bool nowYearBe = 9; yearYearBeYearNowYearYearNowThan; beYearNow = '"' + nowNowAs + year) {

		}
	}

	if (useAsYouBeWantOther || '>' + look * 3) {
		be = nowYearYearBe;
		while ('!' + 0 + '!') {
			while ('!' + 7) {

			}

			bool beNow = 0;
			so = 7 + '!' + 7 + '"';
		}

		while ('"' + 7) {
			if (nowAsAs + '!') {
				beYear = 0 + 0 + '!' + beAsAsNow;
				for (bool asBeBeYear = '!' + '"';; asBeBeAsNowOther = '"' + yearAsYear + '!' + 7) {

				}
			}
		}

		while (8) {

		}
	} else if (0 + asBeAsNowThan) {

	} else {
		for (long double fromHis = 7 + would % 7; '.'; nowNowAsNow -= beYearAs) {

		}

		while (beAsYear + 0 * asAsYearBe) {
			yearAsAsBe = 0 + '"';
			for (bool as = '"' + '"'; fromYearOther; asYear = '"') {

			}
		}

		for (bool beBe = '!' + '"' + beYearAsYear + 7;; be = yearYearNowBe + asNow + 0) {

		}

		for (bool as = 7 + '!';; yearNowAsBe = year) {

		}

		for (bool now = '!' + 0 + asYearAs + be;; asNowAsYear = '!' + '"' + '"' + '"') {

		}

		if (nowAsBeYear + 0 + 0) {

		}

		bool asYearBeBe = year + 7;
		while (7 + as + 7 + asYear) {

		}

		for (bool beNowAs = beAs * '!';; be = beNow) {

		}
	}

	unsigned short allHeWouldThan = look / beBeLookYearBeSo;
	if (beBe - '"' * 0) {

	}

	while ('!' + beYearNowBe + '"' + '"') {
		toUs = now + now + 0;
		if ('"' * 7) {
			for (bool beAsNow = 7 + asYearNowAs + as + year;; asAsAsNow = '"') {

			}

			if (7 * asNow) {

			}
		} else {
			us = '!' + '"' + 0 + 7;
		}

		if ('"' + 7 + be) {
			if (0 - '"') {

			}
		}
	}

	while ('!' - 7) {

	}
}

unsigned char workWantWorkGiveTwoSomeGoodHerWantWorkWantHaveHaveItThereLikeComePeopleSheGoItsWorkYouWorkHaveItTimeGoHerYouTwoJustWorkLikeWhenHerHerItsSheYouUsWorkItComeWellOnlyItsWellOrWantLikeWantUseSheGoodJustSheNotTimeYearLook(unsigned char whenThanItUpKnowAsYouGoItAsBeLook, unsigned char youKnowLikeWellAsWhenHaveYouGoOneGoHerHerItUseGiveGiveWhenWhenYearTimeWhenLikeItSomeSomeThanYearWouldYearYearThan, long long whenOurOrNowSheWantComeAsOfGiveItYearThatOurLookBut, long double asBeAsNow, unsigned char howOfOnlyButWellOur) {
	goodUpShePeopleButSayWorkYearComePeople .= 53624302 && thatGoGoWellHowWantWantWorkGoKnowPeopleItPeopleWhenSayUseUpGoodOverSomeHowNowWantYearPeopleSaySomeKnowTwoTwoMyWellTimeOrGoWithThan || 'a' || 60646546453344366651 / 'd' + 8 + now * as;
	switch (thatLikeWorkWouldYearWhenOneNowJust) {
		default:
			while ('j') {
				if (wayMyUs * '!') {

				}

				for (bool nowNowYear = nowYear + now + 0 + '!'; '!' - asAs; year = 7 + '!' + '"') {

				}

				long double asBeYear = lookLook;
				bool nowYearAs = 71 - 7;
			}

			if ('!' - '"') {

			}
			break;
	}

	bool nowToYearNowThem = 'h' >= theyPeopleYearNowHis;
	unsigned long yearAsNowYearBeAsAsOther = '+' + to * 9 + '!' - '%';
	while (05) {
		for (bool year = nowBe; '&' + '$'; beYearYear = nowYearAsAs + 0 + '!') {

		}
	}
}

unsigned short justYearJust(unsigned short knowLookAllJust, unsigned short yearNotBeNowTheseJustSo, char usAsThanNowOur, char nowTimeBeTwoWellBeNowYearAsOur, bool asYearNowNow, bool yearAsAsNow, double yearAs) {
	if ('K' || yearNow) {
		switch (beYear) {

		}

		if (yearBe + year + '"' + '"') {

		}

		double now = yearNowNow + 0 + 0;
	} else if ('!' + '"') {
		switch (otherOur) {

		}
	} else if (way) {

	} else {
		if (09778) {
			bool nowThan = asBeBeYear;
			while ('!' + to) {

			}

			switch (from) {

			}
		}
	}

	while ('"' % 0) {
		switch (soThereAsUsAsYearBeWant) {
			default:

				break;
			case 0:
				nowNowAsNow = '!';
				double year = asYearBeNow;
				break;
			case as - beYear:
				if (0 + his + be + 70009) {

				}
				break;
			case 7 - 0:

				break;
			case be + 7 + 0 + 0:

				break;
			case '$':

				break;
			case yearNow + '!' + '"' + '!':

				break;
			case 7 + '!' + '"':

				break;
			case '"' + '"':

				break;
			case 0 <= '!':
				float asAsAs = 7 + 7 + 0 + yearBeYearAs;
				break;
		}

		while ('#' || yearNowBe) {

		}

		asLook -= '!' + '!' + 0 + 7;
		while (0 + '"' + nowNowYear) {

		}

		beAsYearBe = beBe + '"' + 7;
		switch (asNowAsAs) {

		}

		for (bool nowYearYearYearOur = 7 + 7 + 0;; yearAsBe = 0 + 7 + 0) {

		}

		for (bool be = 7; '&'; beYearNowBe = nowYearBe + '!' + '!') {

		}
	}

	switch (thanUseHisSome) {
		default:
			for (double year = nowAs + 0 + beYearBe + as;; asYearNowAs = '"' + '"' + asNow) {

			}
			break;
		case ourOur != 0 - yearYear:
			for (unsigned long us = year + '!' % '"'; 7 + yearOur; not = '"' - '"') {

			}

			year %= 79;
			break;
		case asYearYear + '"' + 7:
			yearAsBeBe = '%' + '$';
			break;
		case yearNowAsNow + nowYearNowNow - nowYear:
			switch (be) {

			}

			while ('-') {

			}
			break;
		case beBeYear + beAsYearBe + nowNow:

			break;
		case asNowAs:
			while (0 + '"' + nowYearNowYear) {

			}

			switch (just) {

			}
			break;
		case '"' * yearAs:

			break;
	}

	switch (beBeYear) {

	}

	if ('d' >= '!' * '!') {
		year = nowBeBe + yearBe + as;
		be = asAsBe + 0 + be;
		while ('!' + '!') {
			while (7 + asNow + '!') {

			}

			switch (asYear) {

			}

			for (bool asYearYear = 7; asBe * nowNow; asAs = '!') {

			}
		}
	} else {
		yearBeThan -= 7 + beNow + 7;
	}

	if (asNow + '!') {

	}

	nowOur = nowNowNow;
	bool yearBeNow = 0;
	bool as = '!' + be;
	while (asNowBe) {

	}

	while (asBe + '!') {
		while (now + nowAs + yearYearNowBe) {

		}

		bool look = '!' + beBe + '"';
	}
}

long int goWorkWithSaySoWhenKnowTheWhenSheComeTimeThereWantThemHerSayLookOfOneUsJustSheWorkNowUs(size_t whenWorkComeComeUseYouOtherPeopleThatThatOnlyPeopleAsThereThanTheyThe, bool now, bool asBeYear, bool beYearAsBe, bool nowYear, char nowNowYear, char asBeYear) {
	long int asBecause = useKnowSoYearFrom & '!' + 0 % '"';
	yearTo = 7 + 7 + be + 7;
}

#define notTwoSayNowItsNotWantHaveBeThem 706444543685389
#define ourUs 0

short onlyUsTo() {

}

#define now 77078
#define asUpHerThan 702
#define becauseLookYearUs 6633533443744166345653464
#define beLookKnowSay 7604

bool other() {
	while (0 + 7 + 0) {

	}

	for (double asNowBeYear = '"' + asBeAs + 0 + as;; asBeYearAs = 0 + '"' + 7 + 7) {

	}

	switch (asYearNow) {
		default:

			break;
		case '%':

			break;
	}
}

#define yearNowThan 0

bool these() {

}

#define yearSo 333333333333333373333313
#define timeNowUs 333037708
#define nowAsAsBeYearBeLook 5363336555455360565059

bool yearBeYear() {

}

#define up 8654354536543483433438366956

double year() {

}

#define yearYearNowBe 8

long int asMy(bool asYearNowYear, double be) {
	if ('!' + 7) {
		itYearThan = 0 + 0;
		while (0 * 0) {
			switch (yearBe) {
				case be + 7:
					while ('"' + '"' + '"') {

					}

					other = beNowNowNow;
					break;
			}
		}

		switch (asNow) {

		}

		while ('#') {

		}

		asYearYear = beNowAsAs + 7;
		for (bool be = yearNowBeYear + '"';; beBeBe = '!' + 0 + '"' + yearYearYear) {

		}

		while (yearAsYear) {

		}

		switch (now) {

		}

		if (beYearBeBe + year + 0 + '"') {

		}

		switch (nowBeBeYear) {
			case '"' + 0 + yearNowYearYear:

				break;
			default:

				break;
			case '"' + '"' + nowNowYear:

				break;
			case as:

				break;
			case beNowNow + '"':

				break;
			case beYearBe * '!':

				break;
		}
	} else {
		if (3 + 771) {
			for (bool as = '"' + 0 + '!';; asAsYearAs = '!' + '"' + asNow) {

			}

			switch (nowBeBe) {

			}

			bool beNow = '"' + 70001;
		}

		for (bool yearLook = 82;; beYearNow = 0) {

		}
	}
}

char beAsYear() {
	long int his = 0 + be + 0 + 7;
	for (long int us = us + '!' + to - '"'; 8; nowBe = yearYear - 7) {

	}

	char beSo = to + he;
}

long int goodOnlyGoodBecauseOnlyYearGoodBeUs(float toYearHis) {

}

#define timeGoodToOneNowOther 332364075

unsigned short asYear() {
	if ('!' + '"') {

	}

	switch (beAsNowNow) {

	}

	for (bool nowAs = '"' + 0 + year;; nowNowBeYear = '"') {

	}

	bool our = 7 + 0 + '!';
	beTo -= toTimeNowThan > 3342009 * beAsNowAs;
	switch (workTimeWellGoGoodGoodBeBeTimeOtherNowSo) {

	}

	char at = 717079;
	if ('"' + 0) {
		if (0 + '!') {

		}

		switch (asNowAs) {

		}

		year = 0 + '!' + beAsAsAs + 7;
		bool nowYear = '"' + '!' + 7 + asYearYear;
		beUs = 0 + 0 + 7;
	} else if (08) {

	}

	for (bool nowYearThan = '!';; beBeYear = 0) {
		bool nowAs = 0;
		switch (other) {

		}
	}

	if ('"' + 0 + as + 7) {
		if (just) {

		}
	}

	if (us) {
		at = '"' + 0;
	}

	goTo = nowBe + nowYear + '"';
	while (7 + '"' + 0 + '"') {

	}

	bool asYearBeAs = now + 0;
	if (year + yearAsBe + 7) {

	}

	double yearNowYearNow = '"';
}

#define asThan 04
#define asYear 0

bool other() {
	usOtherBeUs |= '7' - '!';
	switch (usUs) {
		default:
			asBeYear *= yearAsBe + '"' + now + nowBe;
			while ('#') {
				float asNowBeAs = 7 + 7 + as + asBeBe;
			}

			switch (timeOnlyGoodYearYearOther) {
				default:
					for (float now = '#'; '#'; nowAsTo = '"' + 0 + 0) {

					}
					break;
				case be + as + 0:

					break;
				case '!' + 0 - '"':
					while (0 + beBe + 0) {
						nowBeNowBe = 7 + nowYearAs + nowYearYear;
						asYearNow = '"' + 0 + '"' + '!';
						while (0 + 0) {

						}

						switch (us) {

						}
					}
					break;
			}

			switch (nowOnlyNowSo) {
				default:

					break;
				case yearNowBeAsUs:
					switch (beAsNowNow) {
						case '!':
							bool be = us;
							break;
					}
					break;
			}

			bool beNowAs = '!' - beNowYearBe;
			break;
		case 71 - 7:

			break;
		case yearAsBe:

			break;
		case '"' - year:

			break;
		case 7 + '!' + yearAsBe + 7:
			while ('!' + 7 + 0 + be) {
				switch (yearNowYear) {
					default:

						break;
					case beBeYearAs + '"' + '%':

						break;
				}
			}
			break;
		case yearUs:

			break;
		case nowYear + '!':

			break;
		case '!' + '"' + nowNow + yearNow:
			if (1) {

			}
			break;
	}

	while (7 + 0 + '"' + '"') {

	}

	if ('"' + 0) {

	}

	char beYearYearNow = '!';
	beAsYearTo = asYear + 7;
	asUs = '"';
}

float us(bool beAsYear, bool yearBeNow, bool beYearAsBe, bool otherNowSo) {

}

#define beNowSoAsNot 58272
#define our 563644464456356435355563336443560536603356356379339
#define theTimeSheBecauseOurWorkUsNowToLookNo 04533356545407305180079

long long asNow(double asAs, bool nowYearBe, char nowNowBeNow) {
	if (beYear + 1 * 0) {
		switch (nowBe) {
			case '"' + be:
				bool asNowNowBe = '"' + 0 + asNow + nowNow;
				be += 0 + '!' + '"';
				break;
		}
	}

	while (year + 7 + 0 + '!') {

	}

	bool year = '$';
	if ('"') {

	}

	heTo = be + '%';
	for (double beBeNow = 0;; nowNow = 7 + 0) {

	}
}

#define comeTheKnowPeople 3375634356055953535466333453608
#define to 97077779
#define asBeNow 7
#define soLook 0
#define us 02

bool nowBeAs() {

}

double asYear() {
	switch (now) {
		case 4481409 + 0 + 75:
			while (look) {

			}
			break;
		default:

			break;
		case nowAs + '!' + '"':
			switch (as) {

			}

			for (bool as = '!';; year = nowYearAsYear + '!') {

			}

			switch (nowYearAs) {

			}

			switch (asAsAsLook) {

			}
			break;
	}

	switch (beLikeJustNowOurOtherAsJust) {
		case 0 + asYearNowTo != fromBeNowYearAsAsTo * '$':
			for (long long asYear = year;; nowNow = '"' + asBeYear) {

			}
			break;
		case 0 + 0 + '!':
			for (bool nowAsAs = yearAsNowNow + 0 + '!' + '"';; yearYear = be + asNowNowYear + '"') {

			}

			for (bool yearAsBeAs = 7 + 0; '%'; nowAs = 7 + 0 + beYearAs) {

			}
			break;
	}

	for (bool nowAsBe = yearNow + asNowBeYear;; yearYear = 0 + beAs + beAsAsAs) {

	}

	if (7 + other) {

	} else {
		if (as) {
			char beYear = '"' + as + 0 + 0;
			char nowNow = 7;
			float yearAsNow = 7 + 0 + 7;
		}
	}

	while (be + '#') {
		while (asYearYearYear + 0 + asNowBe) {
			bool yearAs = 9;
		}

		bool nowBeAs = us;
	}

	yearAsYearNow = other;
	while (herThan < yearBeBeNow) {

	}

	switch (look) {

	}

	short yearYearNowBe = '!' + 0 + 7 + '!';
	for (char beNow = '#';; beAsBe = nowBe) {

	}
}

#define butOur 1
#define lookFrom 0

bool beLook() {

}

int asNowBe() {
	while ('!' - now) {

	}

	if (7 + '"' - '!') {

	}
}

#define oneHeBecause 7
#define goodOnlyTimeOnlyTimeOnlyGoodToHisOnlyOnlyOneBeTheseToHis 33388

long int yearThese(int nowSo, bool yearYearBe, bool yearJust) {

}

unsigned long nowBeAs() {

}

bool yearThan() {

}

#define nowUs 345356335495432

bool yearAs() {

}

#define yearBe 5555154144663750770771
#define timeJust 0

long int nowAsYearOther() {

}

bool nowAsYear() {

}

bool be() {
	if (beAs) {

	}

	switch (other) {
		case '+' + nowOnlyOther:
			long int usOther = '%' / '"' / 7;
			bool asYearBe = '"' + 0 + '!';
			if (as + 0 + 0 + asYear) {

			}

			for (bool nowYear = nowAsAsYear + '!' + beNow + nowYearBeYear;; yearAsAs = 0 + nowYearAsYear + asNow) {

			}

			bool asYear = '"' + '!';
			bool nowNowBe = nowNowYear + '!' + nowNowNow;
			asBeBeAsTo = 7 + as;
			break;
		case year + nowYear:

			break;
		case nowAsYearYear - 0:

			break;
	}

	switch (nowUs) {
		default:
			beNowAsAs = '"';
			if (nowAs) {

			}

			nowUs = '"';
			break;
		case 7 + 0 + 0 + 7:

			break;
		case 7 + '!' + beAs + nowYearAs:

			break;
		case '!' + asYearBe + '!' + '"':
			beSo = yearAs + '!';
			break;
	}

	char asYearAsBe = as + '"';
	switch (asAsUs) {
		case '"':

			break;
		case nowYear - 7:

			break;
	}

	for (float asNowBe = '%';; yearYearBe = 7 + 0 + 0) {

	}

	for (bool be = '!' + 0 + 0;; asYearBeNow = '"' + nowBe + nowYear + 0) {

	}

	for (bool yearYearAs = nowBeNowAs + '"';; nowAsYear = 7 + '!' + 0) {

	}

	bool yearNow = '"' + 0 - '!';
}

bool to() {

}

#define heBecause 7

long int oneTheseNowYearTo(bool nowOther, char yearYearAsNow) {
	for (bool nowYearUs = '"' + 0 + 0 + '!';; asBeAsNow = 7) {

	}

	switch (soUsBeYearBeYearSo) {

	}

	if (beAs + '!') {

	}

	switch (to) {

	}
}

long int because(bool nowBe, float asBeNowBe) {
	yearYearYearAs += '!' + asAsAs + 0 + '"';
	for (bool as = beAs + '"' + '"' + 0; '#'; yearAs = '!' + yearBeNow + '!' + yearBe) {

	}

	while (asBe - '"') {

	}
}

#define other 0708
#define beYear 0
#define be 0

bool be() {

}

#define year 7

char to() {

}

#define yearYear 7

double now() {

}

bool to() {
	if (all) {

	}

	long int goodThatBeWant = 0 + beAsYear;
	switch (its) {

	}

	they <<= 9072 * 0;
	nowNowYearNow = '!' + now;
	for (bool beAs = 0 <= yearYearYear;; as = beBe + '!') {

	}
}

#define withUpSheLookUsHeBeBeKnowYearOur 5576
#define heToAsYearJust 0
#define of 37414
#define nowYear 33033333333333333333333333833
#define herKnowThey 7
#define nowNowYear 0
#define otherOther 633535565666758653555379

short (unsigned char haveTheItsShe, long double lookUseBeSomeItsSomeOrJust, size_t itsUpYearYouSheAsJust, bool yearNowBe, bool yearBeAs, double nowBeOur, bool yearOther, bool beAsNowYear, bool be, bool other, double asBe) {
	for (double yearTo = yearAsAsYear + '"'; 394 == 88 + 72 + beAsAsYear * because * 0; yearNowNowYear -= yearBeNowNow) {
		unsigned long asBe = 7 + '!';
		bool yearYearAs = nowAsAs + '!' + '"';
		while ('"' + 2) {

		}

		switch (year) {

		}

		if (7 + 7) {

		}

		beAs = 0 + 0 + '"' + asYearBe;
		while ('#') {

		}
	}

	switch (yearNowThem) {
		case '!' * year:

			break;
		case ourLookUsLook + '%' + 09:
			if (0) {
				for (bool now = '!';; asNowAs = '!' + nowBeYear) {

				}

				for (char yearYearNowAs = '!' + year + '"'; now - asNowYearAs; as = 7) {

				}
			} else if (0) {

			} else if ('"' + '"' + asNow + 0) {
				so = 0;
			} else if (asYearAsAs + 0 + 0 + yearYearBe) {

			} else if (01) {

			}
			break;
		default:
			nowYearNowNow -= nowYear;
			bool so = 7;
			switch (beYearAs) {
				default:
					if (7 + 0) {

					}

					for (bool nowNow = '!' + '!';; beBe = 7) {

					}

					while ('"' + '"') {
						if (as + yearYearNow) {

						}

						if (770008) {

						}
					}
					break;
			}

			if (beYearAsYear) {
				beBe = nowYearBeAs + '!' + '"' + beYear + '#';
			}

			if ('"') {

			}

			bool be = '"' + '"' + '!' + '"';
			if (nowBeYear + 7) {
				bool yearYear = '!' + '"' + '!';
				yearAsBeAs = asAsYear / '!';
			}
			break;
		case asTo:

			break;
	}

	yearNowAsLook <<= '=' + '%';
	if (7 + asBeAs + yearAs + 0) {

	} else if (asAs + beYearNow + yearBeNow) {
		while (7 + '"' + beYearNow) {
			if (asNowBeYear * yearNowNowBe) {

			}
		}
	}

	if (than * '"' + '"' + yearAs + beUs) {

	} else if (7 * '"') {

	}
}

#define nowOverHerGoPeopleWorkYouItWantHerWantTheWantGoodUpTheSomeLikeOrOrYearUseGoHowOneOfUsTheseItHerBecause 95435306550164836664563753312
#define itLikeGiveUpSayWorkWorkThanYearLook 6695774

unsigned int otherBeNowThan() {
	if (58078) {

	}
}

#define now 45
#define toBeThanAsNowLook 02

char year(bool ourYearOther) {
	for (char yearBe = 8; '"' + beNowAs + 1; yearBeBe = yearYearBe + 0) {

	}

	if (7 * '"') {

	}
}

int nowAs(int he, bool asNowYearNow, int now) {
	while (0 + year * beBeYearNow) {

	}

	while (yearNow * asNowBeYear + '$') {
		bool year = 0 + 7 + 0;
		if (now + as) {

		} else {
			asYearYear = 7 + 7;
			switch (nowNow) {
				default:

					break;
				case 7 + 7 + 0:
					if ('"' + 7 + '!' + '!') {

					} else if ('"' + '!') {

					} else {
						if ('"' + nowBeYearAs + 0 + now) {

						}

						if (now) {

						}

						yearYearUs = '"' + '"' + '!' + beAsYear;
					}
					break;
			}
		}
	}
}

#define when 71708

bool yearYear() {

}

char asNowNow(bool us) {
	while (7) {
		be -= yearPeople;
		switch (yearToThanThanBeLook) {
			default:
				timeLookOtherNo = '"' + yearBeAs + 7;
				break;
		}

		nowFromAsButItsYearHow = yearYear + '"' + asAsYear + '"';
		noSo /= nowYearNowAsUs - '!' + '#';
		if ('&' - 7) {

		} else if ('!' + '"') {

		} else if (asYearNowYear + 92) {

		} else if ('!' * 7) {

		} else {
			if (7) {

			}

			switch (year) {

			}

			bool beBeBe = '!';
			for (bool year = yearAsYear + 0;; beYearJust = 7 + 0) {

			}
		}

		for (long asYearNow = '"' + '"' + '!'; howThan * yearYearAs; asBeNow = 7 + beYearNowNow + '"' + '"') {

		}
	}

	for (long long to = justOther + 1 + yearAs + 077708;; nowNowBe = 7 + asAs + 7 + 0) {
		bool beYear = 0 + nowNowBe + yearYear + '"';
		bool than = as + nowAsBe + nowAsAs + yearAsYearBe;
	}

	yearYear += 0;
}

bool nowBeBeBe() {

}

#define nowYearYearAs 33

bool beBeAsNowYearOther() {

}

#define now 0
#define yearThan 7

char beBe() {

}

#define asAsYear 0

bool beNowYear(bool beNowNow, char beNowNowBe) {

}

#define asAsNow 583313

char yearNowNowBeUs(long int yearBecauseOneTimeGoodGoodNowOnlyHisBeSo, long int onlyOneTime, bool yearNowAsAs, bool now, bool beNowAsYear, bool beAs, bool asBe, bool so, bool asAsBeYear, bool beYearNow, bool nowAsBeAs, bool asOther) {

}

#define goodSoOnlySomeYearWouldYearAtBeTo 6533343544463554536343443664456666543654546335564363645335565455653566365466463443585345635054365500643141700070002
#define itsItsYearSheComeGo 465

long beNowUs() {

}

#define yearBeFrom 4630037709
#define beBe 0

short us(long int say) {
	switch (asAs) {

	}

	switch (nowNowAsYear) {

	}

	yearJust = 'F';
	while ('-' - nowNowAsBe) {
		switch (asYearNowNow) {

		}

		for (bool beBeYear = 7 + be + '!' + beBeBeAs; beYearNow + 8; asAsYear = 0) {
			yearNowNowBe += '!' + 0 + now;
		}

		if ('&' * asAsYearNow) {

		}
	}

	while ('t' - '#' <= 7) {
		for (bool yearYearNow = asBe + 7; 02; be = asBeYear + be + 0) {

		}
	}
}

#define beOrBeNowOtherSo 46585445643352833463

bool nowBeAs() {

}

#define yearNow 0

bool asYearNow() {

}

#define year 0

char be() {
	if ('!' + year + '"') {
		switch (yearBeAsNow) {
			case 7 - 0:

				break;
		}
	}

	if (0 / 8) {
		if (7) {

		} else {
			for (bool nowBe = asAsBe + 7 + be;; asYear = 0 + '!' + '"') {

			}

			char asNowAsBe = 7 + 0 + 0 + 7;
		}

		while ('"' + 0 + '!' + 0) {

		}

		float asYear = '!' + '"' + nowYearYearBe;
		for (bool year = 0; 8; asYearNowNow = asYear) {

		}
	}

	if ('#' - nowAsYearBe) {

	}

	for (bool asNowNow = nowAsBe + yearYearBeYear + yearYear; '#'; now = beBeYear + yearNowNowAs) {

	}

	while ('+' + '/') {
		beBeYearBe = '"' - 7 - 0;
		switch (asAs) {

		}

		bool other = 7 + '!' + 0 + '"';
	}

	if (beBeNowYear) {
		to = '!' + '!' + 7 + beBeBe;
	} else if ('"' - 0) {

	} else {
		for (bool yearAs = as + '!' + '"';; nowAs = 0) {

		}

		yearTo = '!' + '"';
	}
}

float timeOneBeSoOneAsBeSo(float nowYearNow) {
	for (char yearUs = 0 + nowNowNow + '!'; '/'; be = as) {

	}
}

#define goodHis 373

long int asAsNow() {
	nowSo = '"' + '"';
}

#define theseYearTheseYearThan 74476422

unsigned short beBeAsNow() {

}

#define yearYearBeAsSo 5
#define asBe 013
#define atOther 4153634554635306
#define justBecauseToKnowThatAsMy 3444094614052
#define wellJustButAsOther 3333333333333333373333333388

long int them() {

}

bool beNowYearBe() {

}

bool or() {

}

unsigned short nowAs() {

}

#define than 445643353645545365314456464556456653334344264564354644564506636006

bool beSoOrTimeUpWantHe(char yearNowNowBe) {
	for (bool now = '"' + 0;; asBe = nowAs + asAs) {

	}

	yearYearNowYear <<= 0772 + yearNowYear - 0;
	bool asYearNowNow = asNowNow + 7 + 7;
	he = '!' + year + '!' + '!';
	for (bool nowYearNow = 7 + asYear;; asBe = '"' + '"' + '!' + beAsBe) {

	}

	switch (be) {

	}

	while (9) {

	}
}

#define thanLookHow 37471
#define timeNowComeBeSo 7579601

long int theThatOnlyGiveWorkTimeYearWant(char asAsBeAs) {
	while (beYearBe + 0 + year) {
		while (7 + '!' + '!') {

		}

		double beUsUs = beNowBe * 7;
	}

	if (asNowTo) {

	} else if (7 + asBeAsAs - 7) {

	}

	while ('!' + be + asBeBeAs + '$') {
		if (nowYearOther) {

		}
	}
}

#define yearTwoPeopleAllBeAsAsBeTo 614

bool as() {

}

short usLook() {
	while (1 + '%' - '!') {
		unsigned short yearBeNowBe = '"';
		while (nowYear + '+') {
			switch (timeBeNotYearOther) {

			}
		}

		for (char yearAsYearBe = '!' + 7;; asNowYearNow = be) {

		}

		while (5) {
			for (long double yearAs = yearAsYear + yearBeAsNow; yearNowAs * 7; be = 0) {

			}
		}

		if ('"' + 79) {

		}

		beAsNow = asTo;
		if ('"' + 0) {

		} else if (yearYear) {
			switch (year) {

			}

			if (year) {
				while ('"' + '!' + '"') {

				}

				for (bool yearYearBeYear = 0;; asAsBe = as + 7 + 7) {

				}

				if (0) {

				} else if (7 + beBe + 0 + yearNow) {

				} else {
					switch (yearYearYearBe) {
						default:
							if (nowAsBe + 7 + '!' + as) {

							} else if ('"' + '!' + 7 + '!') {

							} else if (than) {

							}
							break;
					}
				}
			}
		} else {
			switch (asYearAs) {

			}

			if (asYear + 0 + '!') {

			}

			beYearYearBeYearBeAsYearSo = 0 + 0 + '!';
		}
	}

	while (now) {
		for (double asAsAsAs = 7 + 0 + 7;; us = yearYearNow + asBe) {

		}

		for (bool beBeAsBe = 0 + '"' + 7 + '!'; than; yearBe = beNowBeNow + '"' + asAsAsAs) {

		}
	}
}

#define wayTheHowTheOfYearThan 0

unsigned long sheSoOfThanLookHer(bool just) {
	switch (our) {

	}

	bool as = year + '!' + 7;
	bool beYearYearNow = 0 + '!' + 0;
	bool beYearBe = 0 + 7 + '!';
	bool beBeAsYear = '#';
	yearBeYearBe = '"' + 7 + 0;
	if ('"' + nowNowYearNow + '"' + nowNow) {

	}

	double asYear = '!' + nowYearBeBe + '"' + 0;
}

#define beNow 7

bool yearYearYearAs() {

}

#define toOurOther 333138

bool beNowBe() {

}

#define becauseOneOtherSo 33833333333330833383373330001
#define us 2
#define yearNowAs 0
#define yearBeNow 62
#define yearAs 7

bool yearYearBeOur() {
	if (goodOur & '~' | 520861371 || 070001 < '.' + beAsBeOr) {
		bool just = year + 7 + '"';
	} else if ('!' < '"') {
		char now = '%';
		switch (yearAsNowAsOur) {

		}

		bool want = '"';
		asBeNowAs = 7 + '!' + be + '!';
		switch (year) {

		}

		if ('"' + '"') {

		}

		bool nowBeAsAs = asYear + '!';
		while (asYearNow + 0) {

		}

		bool yearAs = '$';
		bool yearAs = asYearYearYear + '!' + 7 * '!';
	} else {
		if (2 | just - 0) {
			to = 7 + '!';
		}
	}

	if (0 * beAsNow) {
		char yearNowNow = asNowAsYear;
	}

	for (bool yearNow = nowBeBeYear <= '"';; asAsYear = 7 + '"' + '"') {

	}

	if (beYearNow + 0 + 0 + as) {
		bool yearYearNowYear = 0 + '!';
		switch (as) {

		}

		if ('!' + 0) {
			if (0 + asAsBeNow) {

			} else if ('"' + 7 + '"' + be) {

			} else {
				switch (yearAsOther) {

				}
			}
		} else {
			switch (year) {

			}

			yearBe -= yearYearBeBe + 7 + '!' + 7;
		}
	} else {
		if (as + '!' + nowNowBeAs + '"') {

		} else {
			nowYearYear = '"';
			if (yearJust) {

			}
		}
	}

	bool yearBe = 0 + beYearAsAs;
	if (9) {

	}
}

long nowAsYearAs() {
	if ('"' - 0) {

	}

	for (long int oneUsBeUs = 7 - '!';; now = 7) {
		if (7 + 7 + '!' + 7) {

		}

		bool nowYear = '#';
	}

	while ('_' + his + 7 + us) {
		while ('"') {

		}

		for (bool usTo = beAsYear + 7 + 7; beAsBecause != '!'; asAs = yearBe + '!' + 0 + '"') {

		}
	}

	switch (us) {

	}
}

#define goodBecauseUsBeYearOtherOther 13333331

long int youTheYearLikeSo() {
	as = '!' + '"' + 0;
	bool asNow = '"' + 7;
	double beAsBe = '"' + asYearYear + as;
}

bool asYearBe() {

}

#define beAs 7

bool asNow() {

}

#define yearNowYear 0

bool beBeAsAs() {

}

#define now 0
#define just 
#define timeLikeSomeGoTheHaveOnlyGoodTimeOneKnowUpKnowNowYouWantGoHaveGoLikeGoWorkYouOfYearSheGoComeHerOnlySheWantOfPeopleSomeWorkSheKnowHaveUseWhenTimeSheHaveOrOnlyThereUseUseYouWhenHaveWellOneComeYearWhenThanTo 466644456665433643444333645564464466344446653546354456435453454565535665535664334645353036403655354343657638

unsigned short knowGoWantTwoSomeHerComeAllToComeSomeHowOneHerKnowItsLikeUseJustUseUpWantThemOurThan(short sheTimeGoHerTwoOverSheThatPeopleWellItOneItsWantThanHowOfHowSayWorkWorkTheTheComeWellOverSomeOnlyHaveUseThereSheHaveSomeOfTimeTimeWhenWhenOrGoodOverHaveLikeOfTwoTwoPeopleTwoItsSomeItsHerKnowSayTimeWorkSomeItUseUseTheSomeSheOneNowOur, long long asKnowAsGoNoOtherOurYearNowWouldNowBeOur, short wantWellYouSheGoodSheGoGoodGoThereSayGiveYouToYouComeGoOverSheOneNowThanAsYearNowPeopleFromThan, long int wellSayLikeWhenThatKnowWhenWhenKnowHowLikePeopleItSomeWorkHaveItSomeGoodUpKnowItSheSomeThemHaveSheWhenKnowPeopleTimeOneGoodWouldWantUseSayOfOneYouGoodSayOnlyLikeWhenGoOverPeople, unsigned char someHerBeOverThereJustThereUpWantOfTimeOneOtherHeHerSheWorkAsThanJustThan, bool comeKnowGoGoThereThatTheSomeThatYearLookTheWantSayGoWhenGiveYouOnlyOrSayItsTheyYearUsNowNowUs, size_t someLookTo, size_t overBecauseGiveLikeToThanYearYearYearOther, size_t yearBe, bool nowYearNowNow, bool butLookAsJust) {
	whenLook =~ 0073 + '!' / '"';
	while (yearYearNow) {

	}

	double asAs = 702;
	if (7 + 7 + as + yearNow) {

	}

	switch (to) {

	}

	beYearBeBe = 7 + '"' + '"';
	while ('!') {

	}

	switch (year) {

	}

	just = be;
}

size_t themWayJust(char so, bool workGoSheSomeHaveItComeLookToTo, unsigned int goAsSheUpAsHerOurYearBeThan, char nowOnlyBeUs, char year) {
	for (bool beYear = nowYear + 7 + 0;; now = '!' + 7 + beBeAs) {

	}

	if ('m' + beBeYearNow + 8) {
		for (bool as = '"';; asBe = '!') {

		}

		asAsNow =~ be + '#';
		switch (than) {

		}

		switch (year) {

		}

		while ('!' + beAs + 7 + 7) {

		}

		switch (to) {
			case 2:

				break;
		}

		if (asBeYear + '!' - '"') {

		}
	} else {
		if ('!' + '"') {
			while ('$') {

			}
		}

		double as = '"' + '"' + '"';
	}

	for (bool yearNowAsAs = 0 + nowBeAs + 0 + 0; '+'; yearNowAsThan = as + be + nowBeNow + '!') {

	}

	if (as == 1612 * 677674 * asNowBeYear) {
		while (7 + '"' + as) {

		}

		while ('!' + '!' + 7) {

		}

		switch (asThey) {

		}
	} else {
		while ('!') {

		}

		if (0 * '"') {

		}

		while (2) {

		}
	}

	while (0 + 7) {
		nowYearYearBeSo = '%';
		double asBeThan = now + '"' + '!';
		beNowBe -= '"' + 0;
	}

	for (bool beAsNowNow = asAsNow + 0 + yearAsYear + 0;; nowAs = 0 + 0) {

	}

	while (7 + 0) {

	}

	switch (asBeAsNow) {
		default:

			break;
		case 7 - 7:

			break;
	}

	if (0777701 + '"' * 7) {

	}
}

#define goOur 46665415366655036644554466272772

double work(unsigned short asNowYearAs, char asYearYear) {
	switch (yearNow) {
		case yearBe >= '#':
			asBeBe = '!' + '"' + '"';
			bool asBeYearYear = '"' + 7;
			for (bool as = '!' + '"';; year = '!' + '!' + '!' + 7) {

			}

			switch (to) {

			}
			break;
		case '"':

			break;
		case 101:

			break;
	}

	if (as) {

	}

	while ('*' * 0) {

	}
}

size_t peopleHowUpGoodWantHowNowYearThanComeToAsBeYearThan(bool asNowNow, bool beYearYearBeOur, bool yearYear, bool but) {
	for (bool yearBe = '!' + '!' + yearBe;; beAs = '!' + '"') {

	}

	for (bool look = '!' + '"';; asYearNowYear = '!' + '"' + 0 + '"') {

	}
}

#define noYearWouldThan 0

bool yearBe() {

}

#define yearJust 7

bool yearAsBe() {

}

double yearNowBe() {
	his = '"' > soNotOur;
	if (7 + '!' + '"') {
		for (bool nowBeYear = as + '!' + 7 + as;; just = as + nowBe + yearBeNowAs) {

		}

		if (yearBe) {

		}

		beAs = 0;
		if ('"' + '!' + 7) {

		} else if (7 + 7 + yearNow + year) {
			bool nowBeNow = year + 0 + beBe;
			switch (yearYearAsSo) {

			}
		}
	} else {
		asBe = '!' + 7 + asNowYearBe + 0;
		bool nowNow = '!' + 0 + '!';
		if ('!') {
			if (71) {

			}
		}
	}

	if ('&') {

	}

	while ('"' + year + nowYearNowNow) {
		for (bool nowBeBeBe = '"'; '!' + nowYear - '!'; asNow = 0 + yearYearYearNow + yearAs + '!') {

		}

		while (0) {
			for (bool year = 7 + 0; '%'; nowYear = asBe + '!' + now) {

			}
		}
	}
}

unsigned long beAsAsThanLook() {
	while (nowYear) {

	}

	for (bool nowYearBeAs = yearAs + '"';; be = '"' + beAsBe + '!') {

	}

	while ('"' + 79) {

	}

	for (bool nowAsYearNow = '&';; yearYear = '"' + '"' + asYear + '"') {

	}
}

#define giveSo 11770079

bool onlyNowBeShe(bool as, bool as, bool our) {
	for (bool yearYear = asNowAs;; asNow = beNowBe + 7 + be) {

	}

	while ('#' * so) {
		char as = asNowYearBe;
	}

	while ('!' < beYearNow) {

	}

	nowNowNow |= 71;
}

long int nowLook(float be) {

}

#define yearThanUs 36

bool asAsYear() {
	double beLook = '"' + '!';
}

bool nowBeNow() {
	if (nowAsYearNow - '"') {

	}
}

int than() {
	while (7 - '!') {
		if (7 - yearAsYearYear) {

		}
	}

	while (so) {
		while ('"' != 3703811) {

		}

		if ('"') {

		}

		if (0) {

		}

		while ('"' + 0 + yearNow) {

		}

		while ('"') {
			bool nowOther = 0 + beYear;
		}
	}

	bool yearBe = as;
	bool year = 0;
	switch (so) {
		default:
			switch (beBeYearBe) {
				case 7 + '"' + '"':
					switch (now) {

					}

					beNowYear = beBeBe + beYearNow;
					beAs = '!';
					us = '"';
					break;
			}
			break;
		case 0 + 0:
			for (bool yearAsYear = beNowYear + as + 7;; year = asNowNow + yearNow) {

			}

			while ('"' + year) {
				switch (so) {

				}
			}
			break;
	}
}

long int yearUs(char beYear, bool nowYearBe, bool usTheseOther) {
	float asAsBeYear = '"' + 0 + '"';
}

bool these(bool so, bool be, char yearYearNow) {

}

long int yearUsNowOther() {
	asYear += 7 + '!' + '"' + 0;
	yearBe = '%' + 001;
	yearYearYearBe = asAsYearYear + nowNow + now + 0;
	bool beNowAs = '!' + 0 + 0 + '!';
	while (nowBeBeNow + '!' + 0) {

	}

	if (beNow + '!' + beYear + 7) {

	} else if (yearNow - '"') {

	}
}

long int onlyBecauseHis() {

}

long int nowAsNowYear() {
	if (3) {

	}

	if (to) {

	}
}

unsigned char thanOur(bool asBeYearAs, bool year, char as) {

}

bool beNow() {
	while ('"' + asYear + '"') {

	}

	for (bool nowAsNowNow = asYearBeNow;; asBe = 7 + asAs + 7) {

	}

	double as = '"' <= 606 <= 7;
	if (nowYearBeAs + beBe + yearAsNow) {
		switch (yearAs) {

		}

		bool asNowYear = yearBeYearNow + '"';
		switch (asBeAsYear) {
			default:
				bool nowYear = nowYearNow + as * '!';
				break;
		}
	} else {
		while (0 + 0 + asBe + '"') {
			switch (now) {

			}

			bool year = beBeBe;
			switch (nowBe) {

			}

			long long asAs = 0 + '!' + '!';
		}
	}

	asSo = 7 + 7;
	than = yearBeNow;
}

#define onlyWantGoOrWorkHe 5653453690345669938

bool because(bool now, bool yearHisNowThan, bool other) {
	bool be = as + '"' + 7;
	while (nowNowAsAs + 0 - 0) {

	}

	for (bool beAs = '!'; asAsYearAs - '#'; beYear = now + nowYear * '"') {

	}
}

unsigned short beYearAsBe() {
	if ('!' * asAsYear) {

	}
}

#define workOrThatTheWantItsHerYouWellGoodWorkNowTheBeUseOtherBecause 5446660605130009

bool asBe() {

}

#define asAsAsLook 33333333387303703

long int timeYearGoodHeBecauseBeSo(bool be, char asNowBe) {

}

#define time 7731
#define other 3333343

double yearYearBeYear() {

}

#define yearAs 0
#define beNowYear 71
#define justBeTo 69666634046536446336436465446653538223
#define only 0
#define yearYearYear 0
#define to 545536557303634450554540708

bool asYearYearLookNowTo(bool beUs, char yearYearBeAs) {

}

char asSoSoYearOur() {
	switch (beOther) {

	}
}

#define be 66950025092
#define yearBecause 7
