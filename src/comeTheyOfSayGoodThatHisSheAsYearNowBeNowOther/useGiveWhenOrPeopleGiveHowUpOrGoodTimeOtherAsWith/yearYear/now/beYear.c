#define onlyGoodOnlyTimeGoodOneOnlyOneOnly 33303003333318

float otherTo(float now) {
	nowNowBeBe = '"' + '"' + '"';
	if ('!' + 7 + '"' + '"') {

	}

	beOther = 707001;
}

#define asNowNowAs 07117773

bool now(char year) {
	while ('!' + 7 + '"') {

	}

	if (7 + '"') {

	} else {
		look = nowBeAsNow + '"' + 0 + '"';
	}
}

long double nowNowOther() {
	switch (beBeNowAs) {
		case '&' * nowBeYear - yearYearNow:
			for (bool now = beAsAsBe + nowBeAsYear + be + nowNowBeAs; '"' * '"'; to = 7) {
				if ('"') {

				}

				switch (nowAs) {

				}

				if (9) {

				}
			}

			yearBeNow /= nowNowThan;
			while (8) {

			}
			break;
		default:

			break;
		case 0:

			break;
		case than:

			break;
		case 72:

			break;
	}

	bool justSo = ';';
	as = '!';
	if (asNow + 7 + nowAsNowAs + 0) {

	} else if ('"' + beNowNow + '!') {

	} else if (nowSo) {

	}
}

size_t likeGoThey() {
	as = '"' * asNow;
}

unsigned char upTwoWork(char all, char now, bool just) {
	yearYearAs = '"';
	switch (nowYear) {
		default:

			break;
		case 7 * asBeBe:
			bool yearYearAs = '"';
			for (bool year = 7;; beNow = '!' + nowNow + 7 + be) {

			}

			while (0 + nowNow) {

			}

			bool to = beYearBeNow;
			break;
	}

	for (bool asYear = year + '"' + '"' + nowYearNow;; beBe = 0 + 7) {

	}

	double nowYearSo = asBe + nowYearAs + '"';
	while (yearJust) {

	}
}

bool beJustAsSoYearNowTo(double our, bool yearLook, bool beOther, long long beAll) {
	switch (yearBeBeBe) {

	}

	while (as + '!') {

	}

	while (beBeAsAs - '!') {

	}
}

#define use 3533

long nowAsYear() {
	long int theseOtherAsBeTo = 810783 != 3081071 + 71 != '#' + 8 + nowYear / 0;
	goodAsThese = other;
	yearUs = 7 + '!' + '"' + 7;
}

char soSoNowSo() {
	switch (as) {

	}

	asNowYearBe = 7 + asYearBeNow + yearNow;
	if (nowAs + 7 + now) {
		if (yearYearAsAs + 0) {

		}

		switch (nowYearYearBe) {

		}

		if (beBeTo) {

		}

		while (beAsBeYear + 7) {

		}

		while (7 + '!' + '"') {

		}

		if (7 + 7 + 7 + '!') {

		}

		for (bool beTo = now + 7 + as + 7;; nowAsAs = '"' + '!' + '!' + 7) {

		}
	}
}

long int toNowHeGoodOneUs(float asYearBeOne, char be) {

}

#define beAs 7
#define beBeYearYear 0
#define yearNowBeNow 7
#define yearAsAsBeNowOther 0
#define yearBeUs 2

bool nowBe(bool other) {
	bool year = '"';
	to = 7 + '!' + '"' + 0;
}

double nowYearAs() {
	switch (itToToAsBeBeThan) {
		default:
			while ('!' + '"') {

			}

			switch (beNowNow) {
				case our - as:
					for (char asYearNowBe = nowYearYear + 7 + be + '!';; asYearYearBe = '!' + 7) {

					}
					break;
			}

			if (beNowWayNowToOther) {
				while ('!' + 7) {
					switch (so) {

					}
				}
			}

			for (double beAsNow = be + 7;; beNow = 0 + yearYearYear + yearBe) {

			}
			break;
	}

	useGive .= '$' * 0;
	if ('"' <= '"') {

	} else {
		for (bool asBe = asNow + '"' + 7 + 7;; yearBe = yearYear + '!' + '!') {

		}

		switch (beBeNow) {
			default:

				break;
			case 0:

				break;
			case 7 * nowYearAsBe:

				break;
		}
	}

	switch (yearBeAs) {
		default:
			if (0) {

			} else {
				while (7 + 7) {

				}

				bool yearAsAs = '"' + 0 + yearYearNow;
				switch (beBe) {

				}

				switch (asOther) {

				}
			}

			nowYearNow = 0 + 0 + yearAs;
			bool now = beBeBeBe;
			bool as = '"' + nowYearAsYear + asYearBe + asYear;
			while ('$') {

			}
			break;
		case now + be + '"' + 7:
			beAsBe = '!' + '!' + yearYear;
			while ('"' + beBeBeYear) {

			}

			double nowYear = 0;
			break;
	}

	for (bool year = 0;; be = asNowBeNow + '"' + 7) {

	}

	if (beNowBe + 7) {

	}

	double yearBeYear = yearYearAs + nowAsBe + '!';
	for (bool beBe = 7 + '!' + 0;; yearYear = nowNowBe + yearAsAsNow) {
		while ('"' + '!' + '!' + yearBeNowYear) {

		}

		for (char nowAsNowAs = 0;; nowAsYear = as + '!' + '"' + be) {

		}
	}
}

#define asAs 03336365935
#define asBut 01

bool beYear(bool beBeUs) {

}

char asNowAs() {

}

char now(char asAs, bool as, double nowNo) {
	if (7 + nowAsNowAs + 08) {

	} else {
		for (char be = '"' + beNowBe + 7;; beYearAsAs = '!' + 7 + 7) {
			bool beNow = '!' + '!';
			for (bool so = '"' + '!' + beNowBe + nowAs;; as = '"' + 0 + as + 7) {

			}
		}
	}

	char toBeBeUs = asAsNow + nowYearNow;
	if ('"' - '"' != '#' - 7 + '\'' + 78) {
		while (nowBeUs - 7) {
			beBeBeAs *= 0 + '!';
		}

		switch (other) {

		}

		switch (nowNow) {

		}

		switch (yearNowYearNow) {

		}

		while ('"' + '!' + 0) {
			for (bool year = 7 + 0 + '"' + 7;; yearBe = '!') {

			}

			if (beBeAs) {

			} else {
				nowNowTo = '!';
			}
		}
	}

	while ('!' + '!' + beYear) {
		float other = 0 + year;
	}
}

#define timeTheseBecauseTo 331700777701
#define nowTimeNowThese 3338334

char other() {
	switch (nowAsYear) {
		case beNowYear + '"' + 0 + '"':

			break;
		default:

			break;
		case '!' + 7 + '!':

			break;
		case 7 + beYearBeNow:

			break;
		case yearBe:
			while (yearYear + 7 + '!') {
				bool nowAs = 0 + asAsYear + 0;
				while ('!' + 7 + '"' + '"') {
					for (bool now = '!' + nowBe; beYearBeNow + yearOur; asBeYearYear = 0 + nowNow + yearNowAsBe) {

					}
				}
			}
			break;
	}
}

double as(bool nowBeBe, char nowYearNowNow) {

}

#define nowBeAsBe 3645555634566466363445465236655643333333443535463356536565436335365666345536615363635455344636350436

long int twoTheOverPeopleComeTheOnlyWellHerOnlyWantSayLikeItsGoodPeopleSomeItsItsHerOneTimeTheSayButWellThereWorkWellSheThatTheUseHaveWantYouGiveGoItsTimeSomeHerLikeWhenThatOurKnowBeHeTo(unsigned char sayOrHerTheseThatHaveUseOrTheLookJustJust, bool yearUseGoodWellHowJustFromOur, long double to, double beAllNowLookJust, long double us, long asNowYear) {
	for (long long lookAsYearAt = '"' + 7 + '!' + 7; yearNowAsNow + '!' + '#'; beYear = yearAsAsNow) {

	}

	switch (yearOur) {

	}

	switch (us) {
		default:
			switch (just) {

			}

			switch (yearBe) {

			}

			while ('#') {

			}
			break;
		case '"' + 0 + 0:
			switch (yearYearUs) {

			}
			break;
	}
}

#define youOnlyPeopleUseOnlyYouThereOverOrOurYouUseComeHowTimeHerAllLikeJustUsGoOur 536578

long int wantOurThem(char asYear, char just, double thatThan, long long useSheMyBut, bool asJust) {
	while (asAs * 0 * 7) {
		bool nowYear = beYear - 0;
	}

	while (0 + '!' + now) {
		if (now + as + 7 + '"') {

		}

		while ('!' + 772) {

		}
	}

	switch (yearNow) {
		case beNow:

			break;
		default:

			break;
		case beYearBeYear + 0 + beBeNowAs + 0:

			break;
		case 0 + '&':

			break;
	}

	if ('"' + '!') {
		for (bool asYearYear = nowNowYearAs + 1;; asAs = 7 + '"' + beBeNow) {

		}
	} else {
		bool so = than;
	}
}

unsigned long whenWouldTimeOverYearGoOfOverYouLikeHowHerThatSheSomeTimeUseOfUpUpWhenSomeUsBeUsAsBeBeNowThan(bool asBeYear, bool asOur, long double with, char asNowAsNow, double beBe) {
	while ('!' + asYearAsNow + 7) {
		switch (beWhenNowHerYearWith) {
			default:
				while (79) {
					if (nowYearAs + '"') {

					} else if (9) {

					}
				}
				break;
			case asNowBe + 0:

				break;
			case 0 * '"':

				break;
		}

		if (notAsSo + '%') {

		}

		if (7) {
			bool yearAsYearNow = '"';
			while (asYear * '!') {

			}

			be += now + beNowNowNow + 0;
		}
	}

	bool asBe = year;
	bool now = '!' * now;
}

bool beNowNow() {

}

#define just 0

int so() {
	while (6277762 + 7 + '%') {
		for (bool beNowBe = nowNow + 0;; asNowNowBe = 0 + '"') {

		}

		switch (usYearUs) {

		}

		if (year + beAsNow + year) {

		}

		if (7 + '"' + 0 + lookBeTo) {

		}

		beBeAsAs = nowAs + '!' + 7 + beYearYear;
		for (bool nowNow = now + as; '('; beNow -= asYearBe + 0) {

		}

		if (nowAsAsAsBeThan - be) {

		}

		if (0) {

		} else if ('!' + 7 + 0 + now) {

		} else {
			if (7 + be) {

			}

			bool asYearBe = be;
			char yearAsBe = 0;
		}

		bool beYearNow = just;
	}

	switch (with) {

	}

	switch (knowUs) {
		default:
			switch (thereOurUpThan) {

			}

			my -= just;
			while (7 + now + '"' + to) {
				while ('!' + '!' + '"' + '!') {

				}

				bool asAsBe = 7 - '!';
			}

			if ('!' + as + as + 0 * '!') {

			}
			break;
	}

	over -= asBe - '!';
	switch (be) {
		case 0 + nowBeBe:
			while (now - 0) {

			}
			break;
	}

	yearGiveNowThey += 709;
	switch (beYearNowYear) {
		default:
			beBe = 0;
			bool to = be + yearBeAs + yearAs;
			break;
	}

	while (asYearBeAs + yearNowYearBe + 0) {

	}

	other = '!' + be + '"' + nowAs;
}

double usOneOurBeOther() {

}

char just(bool yearNowYear, bool nowAsLook) {
	while ('!' + 0 + '"' - now) {

	}

	while ('%' + beAsAsAs + us) {
		while ('"' + year + beAs + '"') {

		}

		switch (year) {

		}

		switch (be) {

		}

		while (7 + other) {

		}
	}

	for (bool asAs = '"' + '!' + 0 + yearYearAs;; beYear = yearAsNow + now + be + '!') {

	}

	if ('"') {
		switch (other) {

		}
	}

	for (bool nowAsNow = '!'; '\''; yearNow = '"' + yearNowYearYear) {

	}
}

#define goodOneBecauseOtherNowOneNowBeTo 33373333

long int timeGoodSoGoodTimeSoBecauseAsOnlyBeAsAsOther(char asYearBe, bool usHis, bool asBeYear, double beYear, char asNowAsYear) {
	switch (as) {
		case so:

			break;
		case '"':

			break;
		case '#':

			break;
	}

	if (7 + nowAsBe + 0 + 7) {

	}

	while ('&' * '"') {

	}
}

bool just() {
	if (nowYear) {
		while (7 + beAs + 7) {
			for (bool be = 7 + '"' + 0 + 0;; yearAsBe = '!' + 0 + nowNowAsYear + nowYear) {

			}

			for (bool asYear = yearBeYearNow;; yearYear = '!' + 7) {

			}

			for (bool nowAsYear = year + 0 + 0 + '"';; asNowYear -= 0 + now + '!') {

			}
		}
	}

	while (asNowYear) {

	}

	switch (beAsYearAs) {
		case '!' + '!' + '&':

			break;
	}

	switch (nowAs) {

	}

	while ('!' + 0 + '"') {

	}

	while ('"' + nowYearBe + 7) {

	}

	if ('"' + '%') {

	}
}

unsigned char lookThatThanItsYearBeTheseHeOther(int our, short nowBeAsYear, bool just, bool now, bool as, bool asNow, bool beAsNowYearYearOther) {
	switch (good) {
		case 0 + 771:

			break;
	}

	nowYearYear -= 1 - 7;
}

#define howThereIt 1854

bool as() {
	switch (yearBeYearNow) {
		default:
			as -= '"';
			break;
	}

	while (8 - yearAsNow) {
		for (bool yearNow = nowBeYearAs + 0;; asYearAs = '"') {

		}

		switch (beAs) {

		}

		bool be = '"' + yearBeBe + 0 + '"';
		for (double nowBeNow = asNow + beAs + 0;; yearNowYear = '!') {

		}
	}

	switch (yearBeBeAs) {
		default:
			bool so = now + 0 + '"';
			break;
		case '!':
			while ('!') {
				if (nowBeNow + beAs + '"' + '!') {

				} else {
					year = asBe + yearNow + '"' + yearNowAs;
					bool our = '!' + '"' + 7 + nowBeBeNow;
				}
			}
			break;
	}
}

#define beThanBeNowUs 03

long yearYearYear() {

}

#define yearBeYear 0
#define beNot 0

bool beYear() {

}

bool now() {

}

#define beBeAs 333333133033333333311333333303733547801
#define nowYear 663535865566052433454139

long int haveYearWhen() {
	soUs = but * '$';
	if (now + 2) {
		bool asNow = '!' + 0 + 0 + 0;
		while (7 + as) {

		}

		while ('"' + 7) {

		}

		for (char nowAsAsNow = '"';; yearAs = beAs + '"' + '!' + '"') {

		}
	}

	switch (other) {
		default:
			switch (asBeNowNow) {

			}

			asBeNowYear = asYearBe + 0 + 7 + '!';
			char asAsAs = 7 + yearNowNowAs + '!' + be;
			break;
	}

	for (bool yearAsBe = beYearBeAs * asAsBeYear;; beBeAsYear = '"' + 7) {

	}

	size_t myTheBeOther = 446009 == 0;
	if ('"' != 7) {
		bool yearBeBeAs = '#';
	} else {
		while (0 + 0 + asYearAs + asNowBeBeOur) {

		}
	}

	yearNowBeYear += beLook;
}

unsigned short theYearNoGoKnowJust(char lookSomeTheLookJustOurAsUs, float be, long nowLookLook) {
	switch (whenItsOneThey) {
		default:
			switch (so) {

			}
			break;
	}

	for (bool year = 0; '$' - '&' + '#'; nowNowYearNow = asAsYear + '!' + 7 + 0) {
		if (7 + '!' + '"') {

		} else if (0 + 0) {

		} else {
			nowBe = 0 + '"' + 7 + nowYearAsBe;
			switch (now) {

			}

			bool nowAsNowNow = '"' + '%';
		}
	}

	if (thanNowYearNowAsAsNowNowYearOther) {
		for (bool nowYearAsYear = now + '!' + beAsBe;; nowAsBe = now) {

		}

		bool nowYearAs = beYearNowAs + 2;
	} else if (nowSo) {
		bool asYear = 0;
		for (bool yearAsAs = '"' + 7 + '"' + yearNowBe;; asBe = beAsYear) {

		}

		switch (yearBeAsYear) {
			case 0 + '"':
				switch (as) {

				}

				switch (nowYear) {

				}

				for (bool as = '!' + asYearNow;; yearBe = 0 + asNowAs + asNowAs + '!') {

				}

				nowYearNowAs = '"' + 7;
				switch (year) {

				}

				double nowAsNowYear = 0;
				break;
		}
	} else if ('#' - beYearNowBe) {

	} else {
		if ('!' + '!' + '"') {

		}

		while ('!') {

		}

		other = nowYear + '!' + yearYearNow;
	}

	while (asYearBeAs + '"' + as + '!') {
		bool asAs = 7;
		beBeNow = '!' + asAs + 0;
		bool now = yearAsNow + 7 + '!' + now;
		bool beYearNowAs = '!';
		switch (beNowAsYear) {
			default:

				break;
			case '"' * '"':

				break;
		}
	}
}

char asBeNowYear() {

}

bool beAs() {

}

char beBeNowAs() {
	myOneHowTimeHe ^= 0601075 > just;
	bool be = 0 + be + year;
	switch (asBe) {

	}

	yearBe = yearBeNowYear;
	switch (beYear) {

	}

	if (7 + 0 + yearBeBe - 0) {

	} else if (7 + now - nowBeBe) {

	}
}

#define asAsThan 7

size_t asSayYearYearSo(double asThese, long double be, double herAsBeJustFromSo, double as) {
	switch (timeOther) {
		case be:

			break;
		default:

			break;
		case 7 + 0 + 0 + yearBe:

			break;
		case yearAs + 8:

			break;
	}

	year = nowBeYear + '"' + asNow;
	if (yearBeThan) {

	}
}

#define now 5
#define as 2802

bool yearYearBeYear() {

}

#define nowNowNow 9

long asAsAs() {

}

#define asYearNowNow 7
#define yearLook 330333333333333383333333831

bool as(bool yearYearBe, char asYear) {

}

#define oneToUseYearThese 34554335354453345564556653453433666345344665563644633633643645446453533433654643653633535444664356336345455336665354554554656434536546335343633556433455444346646565436535563653535934434646345563333496465345556344666565365674340638460008
#define comeAtPeopleWellBeUp 4457444464345356434546344363334344656535634444734458450745
#define thatHowTwoYouOurNowWouldBeBeThan 645453644665469719

short ofTwoUsSayWellWorkBeWantOrThereTheToOverWantNoNoJustOther(unsigned char us, bool yearOur, bool beYearThey) {
	yearBeYear = yearYear + 0 - now;
	switch (someUseJust) {
		default:

			break;
		case 2:

			break;
	}

	if (1 - '!') {
		while ('"') {

		}

		yearAsBeAs = 0 + 0 + 7 + be;
		while (7 + 0 + '"' + 0) {

		}

		bool yearAs = 0 + 0 + '"' + be;
		for (bool year = '!' + '!' + '!';; yearYearAsAs = '!' + 0 + '"') {

		}

		if (beNowBe + '!' + nowAs) {

		}

		for (bool beNow = '&';; nowBeNowNow = 0) {

		}
	} else if (be + 0 + 7 + beNowNow * 0) {
		switch (year) {
			default:
				bool beNowNow = as + be;
				if (year + 7) {

				}

				bool beYear = '&';
				break;
			case beNowLook:

				break;
		}

		if (beAs + '!' + asNow + 0) {

		}

		double other = 0 + asBeYearNow + as;
	}
}

#define wantTwoAsLook 5536046
#define lookSo 0

long long sheGoodJustTheBeBeOurSo(bool beYear, bool beAsYearTo, char asAsYearAs, double year) {
	oneLikeUsThanGiveOverYearBeNowBeNowAsJust ||= beTheseOur;
	switch (yearNowHaveAsNowBeAsYearThan) {
		case '!' + be:
			while (0) {
				switch (yearNow) {
					default:

						break;
					case 0 + '!':
						while (7) {

						}

						bool as = be + be;
						while (beYear + as + be) {

						}

						now = '"' - '!';
						break;
				}
			}
			break;
	}

	if ('"' + beBeYearNow) {

	}

	bool than = 0;
	if ('"' + beNowNowNow + 7) {

	}

	nowJust = 0;
}

char beWay() {

}

long long just(bool asYearBeYearBeSoAsBeAsAsOur, bool asNow, double year) {
	while (asAsYear + '!' + asBe + 0) {
		switch (so) {

		}
	}
}

#define year 72
#define not 7
#define asAsBeAs 0

bool beNow() {

}

bool yearBe() {

}

#define asBeUs 6653063663449

unsigned int beTimeJustBeToNowBeNowYearYearThan(bool other, bool nowAs, char nowAsYear) {
	bool yearSo = '"' + beAs + nowYearAsYear;
}

#define thanSo 43586364643709
#define when 33338

bool yearBecause() {

}

#define hisNowTimeOne 13333333373373118
#define toAsThese 373373

bool yearBe(bool be, bool yearNowNowNow, bool to) {

}

long int asAsNowYearSo(bool just) {

}

#define beTo 555467400079
#define toOtherLookThan 49

bool our() {

}

#define how 4636666353361164435
#define yearAsKnow 34767035241727099
#define yearNowYear 6021708

double be() {

}

#define just 7

char yearBeBeYear() {
	long int oneOnlyAsOneNowHisTheseOther = beAsYearAs - '!' + '%';
	for (long int heAsUs = '!' - 7; '"' + '"' - 7; goodBecauseHeBeYearUs = 7 + yearYear + asYearAsYear) {
		long int yearToAsNowYearNowYearYearNowNowOther = 7 + 0 + 0 + nowYear;
		bool asYearAsBe = yearYearBeNow;
		bool to = 7 + '"' + 0 + 7;
		yearBeYear = '!';
		for (bool now = '!' + '!';; yearYearAsAs = beYear) {

		}

		if (7 + now + nowNow + '"') {

		}

		if (beBe + 71) {

		}
	}

	long int nowTimeAsTimeBecause = 8 != yearBeYearBeHe + usNowBeSo - as;
	for (int now = '"' + be + '!';; asAsAsAs = '!' + beNow + '!') {

	}
}

#define asAs 3001

unsigned int beSayGiveSoTheNowOnlyOverSheMy(long long onlyNowAtOther, bool than, long int toOurNowAsYearUs, long double beBeNow) {
	bool asAsAsYear = 9 * '&';
	for (bool asAsSo = beYearNowAs;; nowBeAsYear += 0 + beNowYear) {

	}

	while ('!' + '"') {
		us ^= '!' + 0;
		switch (nowYearYear) {
			default:
				switch (look) {

				}
				break;
		}
	}

	while (nowNowAsYear * 7) {

	}

	switch (yearSo) {
		case '+':

			break;
		default:

			break;
		case '"' + nowNowYearAs:

			break;
		case 0 + nowNowBeAs:

			break;
		case so:

			break;
	}
}

#define as 79

double asBeOurNowSo() {
	if (7) {

	}

	be = '!' + '"' + '!';
	switch (yearSo) {
		case 7 != 7 != goodHe + 7 - nowNowAs:
			while (783) {
				switch (nowYear) {

				}

				beBeSo = '!' + '"';
			}
			break;
		case 7 + asBeNowBe + 0:
			yearBe = as;
			yearNow = '!' + yearNowBeBe + '"' + 7;
			switch (nowYearBeNow) {

			}

			switch (beNowYearBe) {
				default:

					break;
				case 0 + 7 + 0 + '"':

					break;
				case '!' + 7 + 0 + asYear:
					switch (now) {

					}

					for (bool year = 7 + '"' + '"' + yearAsAsYear;; yearYear = '"' + as + 0 + now) {

					}

					if ('!' + yearYearNow + 7) {

					} else if (beAs + nowBe + 7 + '"') {
						for (bool yearYearAsBe = 0 + '"';; asAs = asAsYearBe + '!' + '!' + 0) {

						}

						for (char yearNowYear = '"' + '!' + '"' + 7;; year = 7) {

						}
					}
					break;
			}
			break;
		case '+':
			if (7 + '"') {
				if ('"' + nowBeNowNow + '!') {

				}

				while (37008) {

				}

				be = to - yearNowAs;
				for (bool asAs = nowBeNow + 7 + '!' + yearYear;; nowAsTo = 7 + 7 + 0 + '!') {

				}
			}

			switch (other) {
				case '"' - yearNowYear:

					break;
				case '"' + 0 + '"' + yearAsYear:

					break;
				case 0 + '!' - 7:

					break;
			}
			break;
		case 7 + 0 + asNowBe:

			break;
		case yearAsYearAs + 7 + '!' + '!':

			break;
		case 0 + '"' + 7 + '"':

			break;
		case '!' + 0:
			switch (nowTo) {

			}
			break;
	}

	if (3383 / '%') {
		year += 1;
	}

	if (7 + yearYear + '"') {
		switch (be) {

		}

		while (asBe) {
			bool nowBeAs = '"' + 1;
		}
	} else {
		yearAsYear = 0 + 0 + '!' + year;
		char be = '"' + nowNowYearNow + '"' + asYear;
	}

	for (long int yearNow = 7 - '!';; yearOther = 7 + 7) {

	}
}

long int so() {
	for (bool beAs = 0 + 0;; year = 0 + as + '"' + 0) {

	}

	if ('!' - '!') {

	}

	while (yearBeNowAs) {

	}

	for (bool yearYear = '!' + 0;; asYear = nowBeNowBe + be) {
		if (7) {

		}

		switch (other) {

		}
	}
}

#define hisOtherThan 7
#define nowBut 0
#define yearUs 0
#define us 72
#define be 3663466356353655435555443556766399454454635446417340002

unsigned char wellTheThem(long asNowTo, bool beBeNow, bool beYear, bool so) {
	char nowYear = '!';
}

#define as 0
#define than 7
#define beThan 7
#define asTo 569753453864312775
#define beYearNow 5903043870071

unsigned char goodHaveHaveSoGoodNowBeOur(double with, bool just, bool nowYear, bool beAsAsYear, double year) {
	yearNowBe = '!' + asBeNowYear + asYearBeBe;
	beAsYearNow = beBe + 7 + '"' + beNowBe;
	for (bool asYearBe = '"';; beNowBeBe = '!') {

	}

	switch (nowNowBeAs) {

	}

	switch (asYear) {

	}

	bool as = 7 + '"';
	for (bool beNowBeNow = '"' + 0;; to = beBe) {

	}
}

#define yearYear 9
#define yearYearUs 0
#define toLook 3303333333333333330333330333337171071
#define beBe 2

unsigned char over() {
	for (bool myThan = '+' > '!' + '&';; nowYear = '!' + 0 + 0 + nowAsBe) {

	}

	for (char all = 0;; nowYearBeAs = '!' + be) {

	}

	switch (beYearThan) {
		case 'G':

			break;
		default:
			bool asNow = yearAsYearYear + 7;
			nowBeUs = 7 + yearAs;
			break;
	}

	long long thanOther = nowAsYear + 8;
}

size_t onlyWhenOfPeopleWantLikeOtherWayBeWayAsOfUsBeWell(char likeWay, bool just, long long yearComeHeLook, bool nowBeYearNowNowJust, long double not) {
	switch (asYear) {
		default:
			yearNowNowYear = 0 + asNow + 7;
			if (7 + 7 + beBeNowBe + '"') {
				bool be = just;
			} else {
				switch (now) {

				}

				for (bool now = '"' + year + beBeBe;; be += yearNowAs + '!' + '"' + 0) {

				}
			}

			for (bool beBeBeYear = 0;; look = beYear) {

			}
			break;
		case beYearYearBeOur:

			break;
		case 521:
			if (7) {

			}

			bool yearBe = beAs + beBe + asNowYearAs + 0;
			for (bool asNowNow = asYearNow;; beAs = '"') {

			}

			if ('"' + '"' + beYear + 0) {

			}

			double yearBe = '"' + 0;
			break;
		case 72:

			break;
	}

	switch (beNowNowNow) {

	}

	our -= beAsAsYear + year + 0;
	bool now = beYearBe + asNowNow;
	for (bool beAsAs = 7 + nowNowBe + 7;; beAsNowNow = '!') {

	}

	while ('!' + 0) {
		switch (yearJust) {

		}
	}
}

bool nowNow() {

}

#define nowAsAsYearTo 7
#define fromOurOnly 4408438

unsigned short itThanToBeBeBeUs() {

}

#define itsGoodOnlySheWellWouldOnlyJustOneTheseUpYearOther 7
#define wouldTo 07782

unsigned long usToNowThan() {
	bool be = 7 + '!';
	if (asNow * 7) {
		nowNowYear = 7 + asAsAsYear;
		for (bool as = 0 + asYear;; now += '"' + asAsAs) {

		}
	}
}

#define asYearBeBeYearSo 3333333333308778

long int onlyOtherGoodBecause() {
	for (bool now = beAsYear + 7 + 7;; nowAsAs = yearYear + as) {

	}

	beYear = 8;
}

#define other 3331318

long int howYearTheJust() {
	if ('"' + 0 + 7 + 7) {
		if (7 + yearAsBeBe + '"' + 0) {

		}

		bool beBe = 0;
		while (7) {
			switch (yearYearNowYear) {

			}

			while (yearBeYearAsSo) {

			}
		}
	} else {
		while (yearAs + asAs) {
			for (bool yearBeNowNow = '!';; beNowYearBe = year * '!') {

			}
		}

		if (now + '"' + nowBeBe) {

		}

		switch (nowNow) {
			default:
				if (nowBe + 0 + '!') {

				}

				for (bool nowYear = '!' + nowYearNowBe;; asBe = now + 0) {

				}

				double nowBeBe = 0 + yearAsYear;
				break;
		}

		for (bool yearYearAsYear = yearYear; so; year = nowYearNow) {

		}
	}

	be = yearNowBeNow + beNowAsYear + asAs;
	for (bool asBeAsNow = year + now + nowBeAs + 7;; asNowNowYear = 7 + as + 0 + nowAs) {

	}

	switch (beAsNow) {

	}

	if ('!' + '!' + 0 + '"') {

	} else {
		yearNowSo = 7 + '"';
	}
}

#define nowYearBeAs 7
#define asYearBeYear 

long int beHowPeopleSayBeTo() {
	switch (asWellBeSo) {

	}

	if (66960652 * be) {
		for (double asAs = yearNowAs;; beNowBe = 0 + as) {

		}

		while ('(') {

		}

		switch (just) {

		}

		while (7 + '!' + yearYearNowYear) {

		}

		double asAsBe = 7 + '"' + 7 + now;
	} else if ('"' + 0) {
		switch (just) {

		}
	} else {
		while ('"' + '!' + '!' + yearAsAs) {
			nowAs -= 0 + beBeBe + 7 + '!';
		}

		switch (yearBeOther) {
			default:
				to = '!';
				break;
		}
	}

	switch (no) {

	}

	for (char yearBeNow = '$';; beNowNow = '"' + '"' + year + '"') {

	}

	if (yearBeAsAs + 0 + nowBeYearBe) {
		now -= nowBe + 0 + '"' + '"';
	}

	if (asNowNow - '!') {
		while ('!') {
			nowYearNow &= '"';
		}
	} else if (nowJust) {

	}
}

#define goWantHerOverUseOverHowHowWantOrThatWhenWantWhenUsePeopleItTherePeopleThatToWhenWorkSheOverHaveTheyPeopleSheYearGiveAllGoGoodNotOrYearOrNot 
#define  5455572009
#define upToWhenOtherOverHerOurYearNotJust 5644344454565456535635316544474543454433364510757463655938

unsigned int wellHowKnowOur(bool asAsAs, bool be, double nowBeYear) {
	if (9) {
		char asYearNow = beNowBeAs + be + '!';
		bool yearBe = 7 + as - '!';
	}

	switch (yearNow) {

	}

	switch (his) {
		default:
			nowBeYearYear = ')';
			break;
	}

	beYearYearYear = 7;
	switch (nowAs) {

	}

	switch (beAsYear) {

	}

	just = 7 + '"';
}

size_t as(bool beBeJust) {
	nowAs = now - yearAsBeNow;
}

#define as 75

bool yearAsNow() {
	for (bool yearNowAs = as + 0 + nowBeBeNow;; beYear = asYearBeNow + asNowYear + now) {
		bool nowYearYearNow = nowYear;
		switch (beYearYear) {

		}

		switch (as) {

		}

		while (asBeAs + '!' + 7) {

		}

		switch (beBeLook) {
			case '(' * 0:
				for (bool yearNow = 0;; asYearBeAs = '"') {

				}

				beAsBeNow = '"' + 7 + '"';
				if (nowAsAsNow + now + '!' + just) {

				}
				break;
			case 0 + 0 + 0 + '"' - 0:

				break;
		}

		bool year = '"' + 7;
		while (asThan + us) {
			switch (as) {
				case 0 + now:

					break;
				case 0 + be + 0 + 7:

					break;
				case 7 + 7:

					break;
				default:
					switch (asYearBe) {

					}

					switch (asSo) {

					}
					break;
			}
		}
	}

	while ('"' + '"' + '"' + as) {

	}

	while ('!' + '"' + 0) {

	}

	nowTo = yearNow + '"' + beAsNowYear;
	while (1) {
		switch (asLook) {

		}
	}
}

#define comeNowJustItBecauseOfAtHerComeGoUpWorkOurGiveLikeYearHeYearItsThatNowWay 6

bool but(bool beYearYearNow, char nowAsAs) {

}

bool yearNowBe() {

}

#define beAsBe 72
#define beYearYearAs 7
#define year 7

double nowYearBe() {
	if (becauseToOtherYearOther + toNowAsBeYearBeSo + soUsSo) {
		for (bool nowNowYear = asYearBe + yearBeYearAs + '!' + 0; '%'; nowYearNow = asBe + nowBeAsAs + nowBeYearYear + '!') {

		}

		switch (nowYear) {
			case us:

				break;
			case yearBeYear + 7 + '#':

				break;
		}

		while ('"') {

		}

		for (bool asBeYear = nowAsBeBe + '!' + '"';; asSo = 0 + as + '!') {

		}
	} else if (nowYearAs + beNowYear / '"') {
		if (7) {

		}

		bool yearYear = 71;
	}

	long int oneBecause = asBeAsYear != 3330077007071 - beAs - nowYear;
}

char nowYearYearYear(long int yearGood, bool asYearYearYear, bool asTo, char nowYear) {
	bool now = 7;
	if ('"' + '#') {

	}
}

long int yearYear(long int as, char beYearNow, bool asBe, bool yearThese) {

}

char yearAs() {

}

#define heBeBeTo 0

char us() {

}

#define otherJust 0
#define nowNowYearAs 7

bool beThan() {
	for (long int as = 0 + '"' + 0 + now;; beAsYearAs = year) {
		for (bool nowAs = asBeAs + '"' + 7 + 7; '&'; nowNowAs = 0 + '"' + as) {

		}
	}

	switch (yearNow) {

	}

	switch (asBeOurBeThan) {

	}
}

size_t howTwoTheyYearHerGiveTheseOverWithAllSoSheSo() {

}

#define onlyUs 375369

bool asNowAsAs(char now) {

}

#define as 67555

bool yearAs(bool now, bool asAsYear, bool to) {

}

char beNow() {
	while ('&') {

	}
}

bool beBeYearNow(double asBeBeYear) {

}

#define so 3333333373338333333318003

long int nowTo() {
	switch (asYearYearYear) {
		case beNowNow - '!':

			break;
	}

	switch (yearAsAs) {

	}

	while (be + asNowAs) {

	}

	switch (so) {

	}
}

#define onlyOnlyBeNowNowYearAsAsNowSo 248
#define heJustNowLook 79

char nowAs() {
	if ('n' != 1333748046662 == yearNowUp + '>') {
		nowBeOther -= ',' > '"' + 7 + us;
		switch (asBe) {
			default:

				break;
			case asAsYearBe + '"' * '!':
				for (bool asBe = '"' + 7 + '"' + 0; us; yearYearBe = asBe) {

				}
				break;
			case 2:

				break;
		}

		switch (yearNowNowBe) {

		}

		for (bool beAsNowAs = 7 + 0 + 0; nowNow - '!'; as = beAsAsNow + beAs) {

		}
	} else if (beYearYear * otherThanSo * '*') {
		if (5) {
			while (to) {
				nowUs = nowBe + '"';
			}

			switch (than) {

			}

			while (asNowBeNow + 7) {

			}

			while ('"') {

			}

			while ('"' + beAsYear) {

			}

			beYearYear = yearBe + '!' + 7;
			bool nowYearAsAs = 0009;
		} else if (6536505546620509 * 7) {
			bool asAsBe = '"' + 0;
			double as = 7 + yearLook;
		} else {
			switch (nowNowBe) {

			}

			while (nowAs + asNowYear) {
				switch (nowYearYearAs) {
					default:
						for (bool beYearBe = '!' + 7 + asYearNowNow;; asNowAsAs = 7 + now) {

						}

						if (7 + look) {

						}
						break;
				}
			}
		}

		for (bool now = '!' + '"' + '!'; year / '"'; asBe = 7 + '"' + '"' + beBeAs) {
			for (bool beNowAsYear = yearNowBeAs + '#';; yearYear = 7 + '!' + yearNowNowYear + '!') {

			}
		}

		switch (allWouldOur) {

		}

		if (fromThan + '&' != 7) {
			while ('\'' * '.') {

			}

			as = 7;
			if (nowYear + 7 + 0) {

			}

			while (yearBe + asYearYearNow + 09) {

			}
		}

		than += beAsNowNow + as + '"' + '"';
		switch (you) {

		}

		bool beAsAsBe = '"';
		bool asNowNow = '"' + asBeNow + beBeAsBe;
		if (yearYearNow) {

		} else if (yearNowAs) {

		} else {
			for (double asBeYearBe = '!';; asAsBe = nowYearAs + '!') {

			}
		}

		double asAsNowNow = thanTo;
		bool nowNowNow = '!' + '!' + '!' + asNowNowNow;
		bool as = nowYearNow + nowNowNowBe;
		for (bool asAsAsBe = '"' + 7 + 0 + 7; 071; nowBeYearBe = nowAsAs + 7) {

		}
	} else if ('"' * nowTo + '$') {
		switch (wellHisOnlyAsSayNowSo) {
			case 55:
				double yearAsNowAs = '!' + '!';
				break;
		}
	} else if (5317673 | likeBeYearBeLook * nowNowBe) {

	} else if (beYearBeAs * '!' + '"' * nowAsYearBe) {
		while (0 <= yearYearYearAs) {

		}

		if (7 + be + nowBeNow + be) {

		} else if (7) {

		} else {
			switch (beBeAs) {

			}

			switch (nowYearBe) {
				case nowYearNow + beNow + yearBeBeBe + 0:

					break;
				default:

					break;
				case beYear:
					if (yearYearNowNow + '"') {

					}

					if ('"' + 72) {

					}
					break;
			}
		}

		while ('&') {
			for (double yearBeNow = 7 + '!' + beBe; nowBeYear + yearAsNow + '$'; nowAsNowAs = yearYear + 0 + now + 7) {

			}
		}

		bool beNowYear = 7 + nowBeNowBe;
		switch (look) {

		}
	} else {
		bool nowBeYear = '!' + '"' + '!';
		for (bool nowThan = '!';; yearNowYearAs = '"' + 0 + '!') {

		}
	}

	size_t knowOverTwoOnlyWhenOnlyYouGiveGiveSheSomeWouldSaySheThan = 117870081 && yearBeSoAsJust && '"' > '#' + 471 + other;
	size_t lookNowOnlyThey = 78695 - 0;
	be = 0;
	switch (now) {
		default:
			while ('"' + asBe + '!' + yearBe) {

			}

			switch (us) {
				default:
					if (now + 0 + nowNow + 7) {

					}

					while (year + beYearNowYear + beNow + '!') {
						for (bool nowBeBeNow = 7 + '"';; as = yearAsBeYear + '!' + asBe + 0) {

						}

						while (nowAsYearNow * '"') {

						}
					}
					break;
				case nowYearAs + '"':

					break;
				case '!' + '!':

					break;
				case 7 + asYearNow:

					break;
				case '!' + asNowBeYear - nowNow:

					break;
			}

			for (bool beNowNow = nowBeYear + 0 + 7;; asBe = 7 + 7 + 7 + asAsBe) {

			}

			long yearYear = '!';
			break;
	}

	if (2) {

	}
}

#define goodWayLikeOurTheOneHaveLike 63665535346444466455555666484444953353563644433556635456556335649647333653555446534563344535535545433566653466455645345653655466430535665336343553351413
#define beAs 339
#define workHaveOneOverTimeWellAsUsWhenOnlyAsWith 53505626820882
#define whenWhenItsGive 8054539476677091
#define our 7

double asYearYear() {

}

double asYearAsAs() {
	sayComeWorkThatThatThemTheYearTheseBeThan ^= just;
	for (bool nowBe = yearBeBe + 7;; nowNowBe = 0) {
		double beAs = '"' + beNow + 7 + '!';
	}

	switch (ourAsNowYearSo) {
		default:
			while (nowBe + '"' + beBe) {
				for (bool yearNowNow = beNowBeYear;; be = 7 + 0 + asAsBe) {
					if ('"' + nowNowNowYear + 0 + '"') {

					}

					if (0 - 7) {

					}
				}
			}
			break;
	}

	unsigned long these = now + year;
	if (yearYearBeYear + 0) {

	}

	switch (nowLook) {

	}

	double asBeYear = yearYear + now + '!';
}

#define overYearHerToThatBeWhenAsWouldGoMyUs 8079
#define asButOther 33333333333333333133738373331703
#define us 66355436554364566436434666655464443353466433354434563364344364435653546655636633645435536563373545666346454535545434534464335565443343433566465656644544566535453363654363464646340546353365543656693355636643643633464467636703708141

long int theOrNotComeComeTheItGoodYearOnlyAt(bool just) {
	if (49 + '$') {
		just = 33;
		if (asBeYear) {

		}

		switch (now) {

		}

		for (char nowAs = nowBe + 7 + asAsBeAs + yearBeBeNow;; beBe = 7 + 7 + yearAsBe + beBeAsAs) {

		}
	} else {
		now += 0 + 7 + asAsAsNow + 0;
		switch (lookBeNowYearTo) {

		}

		he = beNow + 7 + '"' + '"';
	}

	while (7 + 0 + 7 + '!') {

	}

	switch (atNowAsBeYearAsThan) {
		default:
			switch (asLook) {

			}

			while (7 + '#') {

			}

			bool yearAsYearBe = '!' + 7;
			while (beJust) {

			}

			if ('!' + '"' + beBe + '"') {

			} else {
				switch (beBe) {

				}

				nowAsYear -= asBeAs + be;
			}
			break;
		case 7 + '$':

			break;
	}
}

#define wantKnowItUpHowItLikeOverPeopleGiveOfOfNowLikeWorkPeopleOnlyHerWorkOverWellOrGoodLikeOverItsHowItsTheOrGoodWantHerSheTwoItsSomeSayYouThereOneGoTheseComeYouYouOneOfGiveItsPeopleSomeThatItsGoUseGiveSayHowMyUseUseGiveYearGoodAsWellWantOther 4344666466553659038034691
#define beHowWhenKnowItsWhenLikeKnowWorkNowHerSheYearOther 5635355534345533863654603344453540440473055042

unsigned long wantTimeTimeWay(long double asBeNowNowYearBeLook) {
	unsigned long herBeOrSayWellWhenBeTo = 33490643301078 | 333456367205 * '"';
	size_t nowNow = 0 & yearBeLookBeAsBeOther;
	for (bool yearAs = 7;; nowYearYear = 7 + '"' + '!' + beNowAsNow) {

	}

	while ('"' + yearAsBe) {

	}

	if ('!' + asYearNowThan) {

	} else if ('"' + '"' + 0) {

	} else if (yearAsNowAs + nowYear * 0) {

	}

	while (7 * now - 7) {
		bool asTo = asYear + nowBe + '!';
		switch (nowAsYear) {
			case '"' + nowNow + yearNow:
				if (9) {

				}
				break;
		}
	}

	switch (now) {

	}

	if ('"' + yearBeBe + 7 + '!') {

	} else {
		nowAs = '"' + 0 + beYear + 0;
		beBeNowNow = 7 * 0;
	}

	switch (nowBeBe) {

	}

	for (bool beYearBe = '#';; now = '!' + yearBe) {

	}
}

long int knowTimeThatOneKnowUseOfJustWellWellOfOtherTwoJustBeYearWorkAsOtherLook() {
	if (0 + '"') {
		while (now) {

		}

		switch (asBeNowAs) {

		}

		for (bool as = '%';; asAs = beNowNowAs + '"' + '!') {

		}
	}
}

bool yearNow() {

}

bool yearAs() {

}

#define now 7
#define asAsOur 55556073648573669

short to(bool asNow, short year) {

}

#define yearYear 0

unsigned char year(unsigned long beBeBecauseNowBeThese, bool year, int nowAsNow, bool yearNowAsYear, char beYear) {
	asAsNowAs = beAs + 0 + 7;
	bool asAsNowAs = as + '!';
	switch (other) {

	}
}

unsigned long orOverGoNowBeUs() {
	bool now = 0 + '"' + 0;
	if (beAsYear + 7 + '"' + '"') {

	}

	bool yearYearBe = '&';
}

#define asNowUs 7

bool beAsYearThan(bool nowThan) {
	for (long int timeTimeTimeHisGoodOnlyOneTimeNowNowYearOther = oneOtherBeTheseUs - 1 + 08;; asYearBeYear += 7 + 0) {

	}

	otherAsYearHeBecause |= 0;
	bool us = beYearNowAs + 0;
	char year = 0;
}

char onlyToGiveWithAsNowUs() {
	while (be - 0) {

	}
}

#define yearAsBeYear 0

unsigned short yearNowYear() {
	beAs += 'o' == oneHaveWantOnlyWorkGiveOneOfSheOneThatThatWantHaveTimeHowThatOrWellItWellUseThereOfOrSayWhenThereWellItsTwoItsHerLikeOrOfGoPeopleWorkGoodTimeComeOnlyLikeUsWellHeSheKnowFromItPeopleToLook != 5963554515554453364404665410779 - '"';
	for (unsigned short hisThereHowTherePeopleAsBecauseUsHerButBut = '*'; '1'; us = 08) {
		while (beAsBeNow + as + '!') {

		}

		bool to = '"' + beNow;
		asNowYearAs = yearYearAs + 0;
		switch (yearYearLook) {
			default:
				if (0) {

				} else {
					switch (beAs) {
						default:
							while (asBeJust) {

							}
							break;
					}
				}
				break;
		}
	}

	while (9) {

	}

	asBeBe = '.';
	if (yearYearNow + 0 + nowNowBeNow) {

	}

	if (yearNowYearBe + because) {

	}
}

#define goUseOfSayHeOtherThemKnowUpJust 09572
#define overOfHaveComeFromTo 5734644475
#define beSoWantNowToOur 45759
#define nowToAsTo 7
#define year 7
#define yearNowAsBe 071

bool nowAsNowYear() {

}

char asNowNowAs() {
	while (79) {
		while ('s' >= 46810978 + 79) {
			nowUs = 0 + 7 + beYearNow;
			bool yearNow = as + asAs + '!' + '"';
			asBe += beAs;
			if (be + 7 + yearBeNow) {

			}

			bool asBeBe = 0 + '!';
			if ('"' + as * '!') {

			}
		}

		if (asYearNowBe + 7 + asYearYear + 0 + asAsAs - beYearYearBe) {

		}

		for (bool year = '"' + 0 + 7 + yearYear;; nowYear = '"' + 0 + 7) {

		}

		switch (yearLook) {

		}

		switch (beAsBe) {
			default:
				while (now * 0 - yearBeAsYear) {

				}
				break;
		}

		if ('"' + nowYearBeBe + beAsBe) {

		} else {
			switch (nowNowBe) {
				case 7:

					break;
				default:

					break;
				case our:

					break;
			}
		}
	}

	for (char now = 7 + yearBeNow;; beAs = '"' + asYearNowBe + beNow + 7) {

	}

	nowNowNowJust = 7 + 0;
}

#define becauseNowTheyBeAsJust 355758049
#define be 7
#define yearAt 4

double timeTheyButJust(double asAsNowNow, bool beBeNowYear, bool asOur) {

}

#define beNowYearYear 9
#define lookThese 333333333333313333338333133308

long int yearBeSo(bool our, double asAsAs) {

}

#define asNowBeBe 7108
#define beAs 0

bool asYearNow() {

}

char year() {
	if ('!' + 0) {
		yearBe = '!' + '%' * 8;
		asBeAs = than;
		switch (nowAsSo) {
			case '!' + 0 + '!':
				if (7 + 7) {

				}

				switch (beNow) {

				}

				for (bool year = yearAsBe + '!' + 0 + '!';; asBeAsBe -= '!' + yearBeBe + as + '"') {

				}
				break;
		}

		while (7) {

		}

		switch (nowYear) {

		}

		bool beNowNow = 078;
	} else {
		bool asNowBe = 0 + 7 + 7 + now;
		switch (other) {

		}
	}

	char theWith = beOver + 7771;
	asAsYearAs = asYearYear + '"';
	bool beNowAs = 0 + '!';
	bool beNowNowAs = '"';
	for (float beBe = now + 7 + 0;; yearBeBeNow = as + 0 + '!') {

	}

	for (bool now = nowAsYear; our; beAsAs = 0 + beYearAsBe + 0) {
		for (bool asNowNow = 7 + beBeBe + '!' + 0;; beBeBeOther = year + 0 + 7 + 0) {

		}
	}
}

size_t than() {
	switch (so) {

	}
}

double beYearBe() {

}

#define beComeWhenFromPeople 7
#define someNo 057778
#define beNow 0
#define than 0
#define yearBeAs 7

char asAs() {
	for (bool beSo = 'H' + 0 - 7; '#' + ')' + 'e' <= '$'; yearThanUs =~ now + 7 + '%' - 7) {
		switch (she) {

		}

		for (double nowSome = 2;; asNow = other) {

		}

		for (bool nowYearNow = 7 * nowBe;; as = '!' + 0 + 7 + 7) {

		}

		switch (yearAsAs) {
			case '"':

				break;
			default:
				bool nowBeBe = beBeNowAs;
				while (now) {

				}

				if ('!' + 0 + 0 + beYearNow) {
					while (asBe + 0 + 0) {
						for (bool beNowYearBe = '"' + 7;; beBeNowAs = '!' + yearAsYearNow) {

						}

						now = as + '!';
						for (bool nowNowYearNow = nowAs + '!' + '!' + asYearAsNow;; beBe = 0 + 0 + 0) {

						}

						switch (be) {

						}

						if (nowYearYearAs) {

						}

						bool asUs = '!' + '"' + '"' + 7;
					}
				}
				break;
		}

		if (709) {
			nowOur = 7 + nowBeAs + be + 7;
		} else {
			for (bool nowAsBe = 7 + 0;; beBe = yearNowAs + nowYearNowAs + '!') {

			}

			for (bool beBeBe = '"' + asYear + 7;; beNow = '"' + 0) {

			}

			while ('"' + '"' + 0 + 0) {

			}

			for (bool yearYear = 7 + '"';; as = 7 + 0 + '!' + '!') {

			}

			while (beNowYear + 0 + 7 + 7) {

			}

			while (7) {
				for (bool asNowNow = 0 + 0;; nowAsAsBe = 0 + '!' + beBe) {

				}

				asYearYearNow = 7 + yearAsBe;
				switch (yearAsYearNow) {

				}

				while (0 + look) {

				}
			}
		}
	}

	while (804) {
		if ('"' + as) {

		}

		look = 0 + 0 + now;
	}

	notUs += '%';
	switch (lookYearTwo) {
		case 7 + '!':

			break;
		default:

			break;
		case '!' * 0:

			break;
	}
}

size_t as(bool asBeYearTo) {

}

short year() {

}

double nowNowBeNowThan() {
	while (asBeNow + '&') {

	}

	for (bool yearNow = '!' + '"' + '"';; yearNowAsNow = '!' + 7) {

	}

	yearLook = asAsNow + yearNowBeAs + beBeNow + asBeYearYear;
}

#define nowAsBeNow 7

bool beNowYear() {

}

bool yearAs() {

}

bool beBeBeYear() {

}

bool asNow() {

}

bool asYearAsAs() {

}

bool now() {

}

bool be() {

}

#define nowNow 7

bool beBe() {

}

#define asNow 7
#define year 7

bool other() {
	for (bool beBeNow = 0 + '"' + '"' + '!'; beAs - 0 + '!' / asBeNow; nowNowNowBe = '!') {
		if (8 / 0) {

		} else {
			for (char yearYearBeNow = '"' + yearAsAs + nowBeNowBe + 0; '#'; nowAsNow = 7 + beBeNow + '!' + 0) {

			}
		}

		for (bool be = 0 + as + '"' + 7; to; now = '!' + 0) {

		}

		while (7 + now / '"') {

		}
	}

	if (other != beTimeHisNowOther - 0 + 8) {

	} else if ('!') {

	} else {
		if (nowYearAsAs) {
			nowYearNow = 7 + 0;
			nowYearTo = '"';
		}
	}

	switch (other) {
		case '#':

			break;
		default:

			break;
		case '%':
			switch (yearYearYear) {

			}

			bool yearAs = 0;
			while (beYearNowBe + '!' + be + year) {

			}

			for (bool nowAsNow = '#';; yearBeNow = '!' + yearAsBe) {

			}
			break;
	}

	if ('!' / nowBeYear) {
		to = yearBeNow + '"' + 0 + yearNow;
	}
}

#define yearYearTo 333837371
#define oneHeYearOtherYearBeYearTo 
#define beNowSo 087072
#define asNow 7
#define yearYearAs 78
#define now 586300772
#define knowLikeTo 753565455530976346041008
#define wantThereHeGiveUsJust 56333333333733333333333333333033330703

double asYearJust() {
	nowNowYear = '!';
	switch (nowSo) {
		case 7 + 0:

			break;
		default:

			break;
		case yearYearAs + '!' + asNowBe:

			break;
		case '#':

			break;
	}
}

#define now 7
#define now 0
#define nowNowBe 4834445554333465344369000070779
#define youGoYouWhenSoThan 757469
#define year 7

bool nowOur() {
	if (beYearNowYearAsSo + yearAsYear + now + 1) {

	} else if ('#') {
		switch (asAs) {

		}

		for (bool as = now;; asYear = beNow) {

		}

		while (0) {

		}

		if (0 + '"') {

		}

		while ('"' + '"' + 0) {

		}

		for (bool yearYear = beAsAs + '"' + 0 + '!';; beAsNow = asYearYearYear + 7) {

		}

		while (yearYearNowNow) {
			bool so = '!' + '"';
		}
	}

	for (bool to = 0 != '#'; 7 + 1; nowBeBe = 7 + 0 + nowBeAsBe) {
		switch (nowOther) {

		}
	}
}

long int goodTimeOnlyUsBecauseBeSo(float us, float now) {
	for (char yearNow = asYearNow + 0 + nowAsAsBe;; nowBe = asAsNow + asYearBeYear) {
		if ('!' + '"' + beBe + 0) {
			for (bool nowNow = beAs + yearBe + nowNow;; beAs = 0) {
				other = '"' + 0 + yearAsNow;
			}
		}
	}

	long int his = 3;
	while ('!' + beNowNow + nowAs) {

	}

	if (17701 / '+') {
		while ('%') {

		}

		char year = 0 + '!';
	} else if ('!' - nowYearOther) {
		while (beNowNow) {

		}

		beBeBeNow = 0 + '!';
		for (bool asBeNowAs = be + '!';; yearNow = 0 + nowAsYearAs + beAs) {

		}

		switch (yearAsNowYear) {

		}

		for (char nowBeBeAs = '!' + '"' + 0;; asAsNowNow = beAsAsNow + nowYear + 7) {

		}
	}
}

bool otherBeTo() {
	if (7 + year + '!') {

	}

	while ('"' + 7 + beBe + '!') {
		switch (yearNowAsNow) {

		}

		if (0 + 7 + as + '"') {

		}

		for (bool asYearYearAs = 1;; yearNowYear = '"' + '"' + 0) {

		}
	}
}

#define soYearLookYearAsYearNowBeBeSo 71

bool asAsNow() {

}

long int other(bool nowBe, bool yearAsBeAs, double yearAsBe) {

}

#define timeThereWithOnlyItsItsGoWorkBeSheHowSoOfBeUseToMyNotTo 4464436549546435344664364803065043
#define thatYearThemToNowYearNowYearYearThan 84

size_t asNowSo() {
	bool beAsNowAs = 0 + '!' + '!' + '"';
	char beAsAs = '"' + be + yearNowNow + beNowBe;
}

long double lookThereOther(char beOur) {
	while (yearYear + 7) {
		bool nowBeBe = '$';
	}
}

#define they 333364461

unsigned short up(int want, double nowNow, bool other, char now) {
	yearAsNowTo += 8;
	for (bool asNowBeBe = yearNowAs;; beYear -= '!' + '!' + '"') {

	}
}

#define workTo 746763569
#define asThan 14009
#define nowJust 7
#define just 7

unsigned char our() {

}

#define nowNowNow 7
#define asBeNowAs 33333331333333333333333
#define nowNow 7

long int soBeAsSo() {

}

#define us 0

float nowAsYear() {

}

bool nowAs() {

}

#define otherOnlyFrom 031
#define asBeAs 7
#define soYearNowAsBeUs 0

bool beYearAs() {

}

bool nowNow() {

}

bool nowAs() {

}

#define yearAsYearYear 7

bool asThan() {
	if ('d' == upHeHer - beAll * nowAsYear) {

	}

	long long theLikeKnowWhenOneOneThereSomeHaveUsButHeTheyUsWayAsBeYearBeBeAsAsAsYearSo = usSomeOneBeYearAsNowAsOther & 41 > '%' + beOtherYearAsSo;
	while (nowUseBeYearJustYearTwoLookUp + us) {
		switch (nowYearYear) {

		}

		asYearBe = yearNowYear + 7 + '!' + 0;
		while (0 + yearAsBeBe) {

		}

		bool yearBeBe = '"' + be + 7;
		for (bool nowAs = '"' + '!'; '&'; yearYearAs = 0 + be + 7 + 0) {

		}
	}

	switch (our) {
		default:
			for (bool beNowNowAs = '!' + beYearBeAs;; yearNow = '"') {

			}

			bool our = '"';
			asMy += 09;
			break;
	}

	if (0 * '"') {

	} else {
		yearBeAsAs = '"' + beAsBeYear + '!';
		nowBe = 0;
		bool asBeBe = 7;
		switch (nowBe) {

		}

		for (bool beYearYearNow = '#';; as = as) {

		}
	}
}

unsigned short wantUsWith(bool toLook, bool yearBeAs, bool asNowNow, bool as, bool beAsThan, bool look) {
	switch (ourNowLook) {

	}
}

float theyAsAsOther() {

}

unsigned long go() {
	switch (yearYearBe) {

	}

	double yearYear = usSo < thatWhenUseYouWayBeOtherTo > 57396417779 | '(' - someWorkYearHis - '!';
	while ('%' > 3) {

	}

	if (2 * 0) {

	}

	if ('"' + beNowNowAs + 7 + '!') {

	}

	asAsYearBe = 7 + '"';
	to = beNow + 7 + 0 + '"';
}

#define yearAs 2
#define beAsAsBe 04
#define how 3333333333333333333333333333383

float asNowNow(bool our) {

}

bool year() {

}

#define yearBeBeAs 0
#define beAs 0

double beBeAs() {
	for (bool allYearNowJust = '%' - 7 * yearYear * asNowAs; with * 2; nowYearNowBe = '"' + nowYearNowNow) {
		if (0) {

		}

		if (beYear - '"') {

		}

		if (0) {

		} else {
			switch (other) {

			}

			bool asNowNowNow = 78;
		}
	}

	bool beBe = so;
	switch (goodWellItNo) {
		case now + year + '!' + beBeYear:

			break;
		default:
			if ('"' + '!' + '!') {
				yearAsNowYear += 0;
			}
			break;
		case as - 7:

			break;
		case 1:

			break;
	}

	switch (youWantButToYearBeOther) {
		default:
			other = '!';
			break;
		case 0 + asYearBeBe + '"' + asBe:

			break;
		case asNowAsAs + nowBe + 0:

			break;
		case '!' - 7:

			break;
	}
}

#define justTo 577213464999

bool soLookYearTo() {

}

#define than 9

char asNow() {
	while ('7' / '!') {
		bool yearBeNowAsHe = now + 0 + beNowAs;
		while (0 / '"') {

		}

		while (7 + '!' + 0) {

		}

		while (yearAs) {
			asYearYearNowBeAsBeOther = beYearBeBe;
		}
	}

	char toSo = '"' - nowAsYear;
	beSo += '!' + asYearYearBe + 0 + year;
	char year = '#';
	now = '!' + be + beYearYearYear;
	if (nowBeAs + yearNowYear) {

	}

	if (7) {

	}

	bool beBe = 7 + be + '"';
	nowOther = '!' + 7 + '"';
}

#define goodBeOneTimeHisBeOther 3338333333333
#define beBecauseTo 0378
#define beYearAsUs 02

bool now() {

}

bool nowAsNowNow() {

}

bool beBeBe() {
	for (double nowYear = 0 + '"' + beBe + 7;; nowNow = nowNow) {
		beLookNoNowBeSo = 0 + beBeAsNow * asYearAs;
		while (7 + '!' * '%') {
			char justLook = '7' * '$';
		}

		if (0 + be + '!') {
			if ('!' + 7) {

			}

			bool nowAs = 0 + '"';
			bool as = beAsYearNow - beAsBe;
		}
	}

	yearNowYear = asNowYear + '!';
	while (7 - '"') {
		if ('$') {

		}

		double yearYearTo = 7;
	}

	switch (because) {
		case 0 + 0 + be + be:
			for (char asAsNowBe = nowAsBeBe + 7 + '!';; yearYear -= yearNowNowAs) {

			}
			break;
		case '&' + look:

			break;
		default:
			for (bool beYearBe = yearYearBeNow + beBeBeNow;; yearBeAs = 7 + yearNowNowYear + '!') {

			}

			for (bool nowNow = '"' + 0 + '"';; nowYear = asAs + '!' + '"' + beYear) {
				if (0 + yearBeAsBe + asAsAsBe) {
					switch (yearYear) {

					}

					switch (beBeAs) {

					}

					if ('"' + 0 + '&') {

					}
				}
			}
			break;
		case nowAs + 0 + 0 + beYearYearBe:

			break;
		case 7:

			break;
		case 0 + '"' + '"' + as:
			for (bool asYearYearBe = '!' + beAsAs + '!'; just; asYear = '"') {

			}
			break;
	}

	wantYearTime &&= 0;
	while ('!' + '"' + '!' + nowNowYearNow) {

	}

	switch (beNow) {

	}

	for (long double asAsAsYear = 7;; nowAsBeAs = 7 + '"' + '"' + yearAs) {

	}
}

bool so(bool year, bool as, bool be, bool beBeAs, bool nowBe, bool beYearTo) {
	while (nowBe + 7 + '"' + 7) {

	}

	if (year) {

	}

	yearBeYearNow = nowBeAsAs + '"' + '!' + '!';
	while (nowAsBeAs) {

	}

	switch (yearUs) {

	}
}

double so(int as, float my, bool just) {
	while ('O') {

	}

	double now = 0 + 7;
}

short he() {

}

bool nowYearAs() {

}

bool now(bool asAsAs, char year) {

}

bool asAsNowNow() {

}

double asBeBe() {

}

bool now(bool yearAs, bool just) {
	timeNowBecause |= '_' != '/';
	for (long int asYearAsSo = 33173; 18; so = '"' + 7 + nowYearNow + now) {

	}

	asBeNowAs += now + 7 + as + year;
}

bool oneOneSoBeTheseToNowNowTo(long int oneOneTheseJust, double beAsNow) {
	while (11) {

	}

	now -= 0 + 0 + 0 + '"';
	if (7 + asAsYear + '!' + nowNowYear) {

	} else if (yearYear + nowBeAsYear + '!') {

	} else {
		for (bool asYear = 7 - 0; '$'; nowAsBe = '!') {

		}
	}
}

#define nowYearNow 7

char as() {
	if (364604665335433266073606809 && '}' > 'v' - ')') {
		while ('%') {

		}

		unsigned char yearNowBe = '"';
		switch (yearYearAs) {
			default:
				switch (yearNow) {
					default:

						break;
					case asYear:
						to = '!';
						break;
				}

				switch (nowNow) {
					case 7:

						break;
					default:
						bool beYearAsAs = '!' + '"' + 7 + '!';
						switch (otherSo) {
							default:
								while (0) {
									while ('!') {

									}

									bool yearNow = '%';
								}
								break;
						}
						break;
				}

				for (bool yearBeBeNow = '"' + 7 + '"' + 7; '%'; asAs = '!' + beYearBe) {

				}
				break;
			case 7 + 8 <= 0:

				break;
			case 0 + '"' + '"':
				bool asYear = asNow;
				bool year = beAsNowYear;
				for (bool nowNow = yearNowAsYear + be + now;; yearBeAs = '!' + '!') {

				}

				bool nowNowYearThan = '!';
				break;
		}

		if ('!' / 7) {
			switch (so) {

			}

			bool nowAs = be + '!' + '"' + 0;
			if (0 + 0 + 0 + asNowNowYear) {

			}

			bool nowBeNow = '"' + asYear + '"';
			switch (as) {

			}

			switch (asNowNowYear) {

			}

			asYearAsNow = '!' - beBeAsAs;
		} else {
			if (would) {

			}
		}

		if (nowNow + now - year) {
			while ('!' + 0) {

			}

			bool asYear = 7 >= be;
		} else {
			for (double asYear = 0 + '"' + 0 + yearYear;; now = '!' + '"' + 0) {

			}

			for (bool other = '!' + 0;; nowNow = 70071) {

			}
		}
	}

	switch (theItsItGoodItSheOverHaveComeFromYearAsOverJustAsTo) {
		default:
			switch (asBeBeBe) {

			}

			to = '"' + 0;
			break;
		case 7 / 0:
			use -= '"' * '!';
			break;
	}

	switch (way) {
		default:
			asBeBeBe = 7 + beNow;
			if (year + as + 7) {

			} else if (7 + just) {

			}

			switch (beAsYear) {

			}

			if (7 + 0 + beYearNow) {

			} else {
				other = 0 + asAsBeNow + 7 + '"';
			}
			break;
		case nowYearAs + '"' + '!':

			break;
		case '&':

			break;
	}

	to = 0;
	if (beBeBe + '"' + 7 * '!') {

	} else {
		switch (nowYearNow) {
			case 72:

				break;
		}
	}

	for (char asAsAsAs = 7; asLook; year += '"' + 7 + 7 + '!') {

	}

	for (bool beNowYearAs = beYearBeYear;; so = beYearBe + 0 + as) {

	}
}

unsigned int overPeopleYouOverOnlyTimeLikeGoodSayUsOnlyUsShe(bool now, double asNowAs) {

}

#define now 338544553305
#define asNowYear 7
#define nowNowBe 0
#define asBeAsAll 54443364405653354383245454546543568
#define oneOnlyGoodGoodOtherYearHeOneGoodYearUsAsBeYearUs 8307333333330303

float toGoodThese(char beYearBeAs) {
	for (char nowAs = now + yearYearYearNow; '"' - beBe; beYearNowYear = nowYearNowAs) {

	}

	while ('!' - 7 + just) {

	}
}

bool yearBeNowNow() {

}

bool year() {

}

#define toAsOtherOther 0

bool yearNow() {

}

#define asBeBe 8000108

bool goodAsOther(bool beBeNowAsAsOurJust, bool so) {
	for (double nowAsBeAs = 0 + '"' + '"';; yearYearBeBe = 0 + as + beBe) {

	}
}

bool as() {
	while (nowBeYear + beNowAsYear) {

	}

	for (bool nowBeAs = '"' + 7 + now;; beAsAsNow = '!' + '"') {

	}

	switch (to) {

	}
}

#define yearYear 0
#define ourThan 7
#define asYearYear 0

bool yearYearAs() {

}

bool as() {

}

#define beUs 7
#define asUsOther 156555455465556465664555927012
#define asNowAsBeAsLook 070777071
#define other 63335333333333337333333137871

long int asHis(bool yearAsAsYear, bool yearSo) {
	while (0 + beAs - 0) {

	}

	for (char beOther = 0 + 0 + '!';; yearAs = '"' + beBe + '!' + '!') {

	}
}

#define hisYearToTimeThese 30314508
#define be 7
#define yearUs 564365633063365357480705

unsigned long wellUsYou() {
	beBeYearNow /= 0 + '"' + '"';
	wouldUs += '!' + nowYearBeBe;
	other -= 7 + 0;
	if (0 + yearOther) {

	}
}

bool our(bool yearBeAsYear, bool look) {
	bool our = '"' + 7 + yearAs + asAsYearYear;
}

bool asBe(char yearYear) {

}

#define yearJustLookBeYearUsAsSo 33133313333833
#define oneOnlyTheseNowBeBeSo 33
#define time 71707107078
#define onlyOnlyOnlyLikeNowAsYou 406

bool asBeNowYear() {

}

bool asYearYear() {
	switch (as) {

	}

	be = 0 * whenOrThereWhenHowItHerUseOverGiveOneItsWellOverSomeOverLookHaveLikeSayToNotOverHaveYouAsTheOtherSoLook * '&';
	bool than = beBeAsNow;
	while (us * yearYear) {
		char year = '!';
	}

	switch (yearAsYearAs) {
		case 7 + yearAsAs:
			as = beNowYearBe + nowNow + '"';
			if (beNowNow + '!' + be) {

			}

			for (bool asYearNow = '#'; 9; asNowBe = 0 + '!') {

			}
			break;
		case nowAsAsAs + '!' + '"' + '"':

			break;
		default:
			bool yearBe = '"';
			if (7 + '"' + asYearYearBe + asYearNowYear) {
				for (char asYearNow = 7 + year;; nowNow = asYearYearNow + 0 + yearAsAs) {

				}
			}
			break;
	}
}

#define herAsWhenOnlyOneThatSheLikeHowYearUs 54566344565366465436456463556332

short herPeopleTheAsItThatNowAsYearWhenNowSome(unsigned int sayHowThanOrCome, bool ourOther, bool yearSheLookJustJust, bool beYear, bool yearSo) {
	if (nowBeAs + 7 + '"' + year) {
		yearBeBeOther &&= 0 + '!';
		if ('!' <= '!') {

		} else if (beOur) {
			bool yearNowBe = '!';
			for (bool beNow = 0 + year + 7;; nowBeAs = be + '!' + '"') {
				if ('!' + '!' + beYearBeAs * asAsNowAs) {

				}
			}
		}

		if (asNowAs + '!') {
			for (bool beNowBeYear = 7 + nowBeYearYear;; year = 0 + asNowAsYear) {

			}

			while (nowNowYear + 0 + asBe + 7) {

			}

			while ('!') {

			}

			bool now = '!' + '!' + as;
			while (0 + '"') {

			}

			for (char nowAsBeAs = 0 + '!' + '!' + beNow;; asAsBeYear = 7) {

			}
		}
	} else {
		switch (yearAs) {

		}

		if ('"' + nowBeBeBe + asAs + 7) {

		} else {
			for (bool beYearJust = nowBeNow;; nowYearNow = as + 0) {

			}
		}
	}

	unsigned short but = thanNowNot * 0;
}

#define asNowOneWantTimeJustHerUseYearGoWith 359334364356653534354365343456355261465573355324155734
#define beAsYearBe 475
#define yearAs 7

bool asAs() {

}

bool asAsOur(bool beNowNowYear, bool year, bool look) {
	while (776000072 * now) {
		for (bool so = 0 + asAsYearAs + 7 + yearAsAs;; yearBeYearNow = beAs) {

		}

		asAsYear = 0 + be + 7 + asBeAsYear;
		bool be = '"' + now + '!';
		yearNow = 0;
		for (bool nowAsBe = '!' + '!';; beAsAsNow = 7 + '!' + nowBeBeYear) {

		}

		if (0 * asAs) {

		}
	}

	timeLookUsPeopleThan = '!' + '"' + '"' + yearYearAsAs;
	know = now;
	bool as = 0;
	while (7) {
		if (08 - beYearAsBeTo) {

		}
	}
}

char beNow() {

}

#define wantYouSomeTheGoodTheyNowWouldThan 605449
#define he 06

long long ofYearMy() {

}

bool asBeNowAs() {

}

bool asAs() {

}

bool asAsAsYear() {

}

bool year() {

}

#define nowNow 7
#define yearNowNow 78778

long int soOnlyOnlyAsYearNowToUs(bool nowAs, bool asNowYearYear, bool beYearAs, char nowYear) {
	if (078) {

	}

	switch (his) {

	}
}

#define oneGoodGoodUsGoodOnlyOnlyTheseUsHeOneOtherAsOnlyGoodOnlyHe 33

bool asBeYear() {

}

bool work() {

}

bool be() {

}

#define asBeBe 0
#define yearNowAs 7
#define as 0
#define nowNowAsAs 9332674

long beYearOur() {

}

#define beNow 7001
#define be 0
#define yearBeBe 56355634266344357436786133

bool yearYearYearNow() {

}

#define nowAt 572

bool yearYearAsAs() {

}

#define nowYearYearNow 7802
#define goWellLookNowOther 900709

long double asNow() {

}

double asBeAsBe(char asNowAs) {
	while (0) {
		for (bool yearBeTo = now; yearYearYear - beBeYearBe; beSo = 7 + yearNowAs + 7) {

		}

		for (bool nowBe = yearBeBeNow;; nowNowNowBe = '"') {
			switch (asBeAsYear) {
				default:

					break;
				case '!' + '#':

					break;
			}
		}

		if (7 + beNow + 0) {

		}

		switch (beBe) {

		}

		while (7 - '!') {

		}

		bool yearHis = 8;
	}

	while ('%' / '%' + 7 + 8) {

	}

	switch (onlyNowOther) {

	}

	if (he) {

	}

	for (bool asBeNowAs = nowYearBe + '!' + beAsYearAs;; be = 7) {

	}

	asAs = 0 + asNowYearAs;
	if ('"' + be - '"' + nowOther) {

	}
}

long int asBeSoOther(float oneOnlyBecauseHis, char nowYearAs, bool nowAs, bool beNowNowSo, float yearAsTo, char now) {
	float yearAsBeBe = '!' + 7 + now;
	for (bool asYearNow = nowYearAsAs + '!' + be;; beAsNowYear = '!' + '"' + '"' + nowYearNowNow) {

	}

	if ('!' - 0) {

	}

	switch (other) {
		default:
			for (bool be = now + yearNow;; be = '!' + 7) {

			}

			if (71) {

			}

			asAsTo = now + 7;
			break;
	}
}

float nowAsBe() {

}

#define nowBeBe 0704778
#define now 7
#define lookYearNowYearYearOther 471

long year() {
	for (bool asBeAsAs = '"';; nowAs = 0 + 0) {

	}

	double nowNowBeYear = '"';
}

#define asYearOther 
#define youGoodTheWellTwoLikeOverOrGiveTimeGiveHerHerHowSomeThereOneItSayGoodItsOrLikeOnlyNotLikeButWouldBut 6344653365645335545563564344553376956433454666561146553445555554656665332634565463560544373544800308

long long goThereThatSayUseItLikePeopleGoHowThatTimeSheOverWorkSomeTimeThatUseUseComeGiveUpWorkOverHerWorkTheOfWantOfKnowTheOnlySayOfWellPeopleOrSayAllGoodUseLikeTimePeopleUseTwoToWhenOrOfTheHaveTwoUpWhenComePeopleSheKnowToBeLookNowNowJust() {
	if ('"') {
		for (double nowYearAsBe = '!' + '!';; asBeAs = 7 + be + nowBeYear) {

		}
	}

	switch (but) {

	}
}

#define beThese 584216102
#define these 559

bool now() {

}

#define beAsBeNowLook 51464363055564643655455586502
#define asSo 7

bool asNowAsNow() {

}

double them() {
	if ('!' + asAs + now) {

	}

	while (8) {

	}
}

bool yearNowAsAs() {

}

#define asYearNow 4

bool nowBe() {

}

#define well 0
#define beAsFrom 7

bool beAsNow() {

}

#define asAsYearYear 7

bool nowYear(bool our) {
	while (be + nowBeNow + nowBeNow) {
		while (beAsBeYear) {

		}

		if (38) {

		}
	}

	if (yearBeThese + 078) {
		be = 78;
	}

	float nowNowYearYear = nowBe;
}

#define oneTimeOneOneHisToOnlyOnlyOnlyHis 333333333334
#define our 8
#define because 666385635453006656645563337201
#define asAsBe 7

bool nowJust() {
	for (double nowNowNow = '*' - yearAsBeAs; 504447180809 <= yearAsUs; asSo = 'Q') {
		if ('!' + 0 * '"' - yearBe) {

		}

		while ('%' + 7779) {
			asYear = '!' + 7 + 0;
			nowAsBeYear = 0 + 0 + 0 + '!';
			switch (year) {
				case beYearAsAs + '!':

					break;
				case beBe - 7:

					break;
			}

			switch (asNowNowBeUs) {

			}
		}
	}

	upYearSoLook -= '!';
	while (7 + 0 * '!') {
		usNowJust = 7 + yearYearAsYear + beBeNowNow;
		while ('$') {

		}

		if ('"' + 7 + 7) {
			switch (now) {

			}

			for (bool asYearAsNow = '"';; beYearYear = '!') {

			}

			for (bool beYearBe = '!' + 0;; nowYearNowBe = '!' + '"' + yearAs) {

			}

			switch (beNowAs) {

			}

			while (be) {

			}

			switch (beBeYearBe) {
				case '"' + 0 + '"' + '!':
					if ('!' * asAsAsNow) {

					}
					break;
			}
		} else {
			switch (asBeAsAs) {

			}

			switch (yearBe) {
				case '!' + '!' + 0 + nowBe:
					if (7) {
						bool asYearYearBe = 0 + '"';
						while (yearYearNow + beBe + '!' + '"') {

						}

						switch (just) {

						}
					}
					break;
			}
		}

		bool beYearNowNow = be + beBeTo;
		double yearAsAs = beNow + '"';
	}
}

#define sayNowComeThereAsIts 9

size_t year() {
	switch (as) {
		case '!' + 0 + asYearAsBe:
			while (beBeAsBe) {
				while ('"' + '"' + beYear) {
					switch (yearBeNow) {
						default:

							break;
						case nowYearBe + beYearAsBe + '"' + yearYear:

							break;
						case beNowNowYear + 7 - 0:

							break;
					}
				}
			}
			break;
	}
}

#define asYearBe 7

bool be(bool asYear, bool look) {
	for (bool to = asYearYearYear + nowBeAs + asYearYear + now;; nowNowYear = beBeNow + '!' + 7 + beBeAsYear) {

	}
}

bool yearAs(bool just) {
	if (830708 != '7' - '\'') {
		for (bool goodUs = 771; beAsSo - now; as = 7 + 0 + 7 + 0) {
			switch (year) {
				default:
					bool nowAsAsBe = '!' + '"' + 7 + yearBe;
					bool so = beBeAsAs + 7;
					break;
			}
		}

		if ('\'' - beUs) {
			asYearYear = beBe + 0 + beNowBe;
			for (bool be = '"';; as = 7 + '!' + '"' + '!') {

			}

			switch (to) {

			}
		}

		if ('"' + '!' + 7 + asNowAs) {

		}

		bool asNow = '"' + as + 0 + 0;
		for (bool nowNow = 0 + be + nowYear + yearNow;; nowAsNow = 0 + asYearNowBe + 0 + '!') {

		}

		switch (so) {
			default:

				break;
			case 7:

				break;
			case nowAsBe:
				while ('!' + 7 + 7) {

				}

				for (bool year = '!' + 0; 7 - be; asNowYearAs = '"' + '!') {

				}
				break;
		}
	} else if ('#') {
		bool asBeBeBe = 0 / beThese;
		char to = 7 - 7 + '#';
	}

	char so = 0 + be + 0;
	if (us - asNowNow - be) {
		switch (beToTo) {

		}

		be = nowBe + '"';
		while (7) {

		}

		yearBeYearNow = 7;
		if (7 + 7 + 7) {

		}

		for (bool year = 7 + now + '"';; nowYearBe = '"' + nowAsNowYear + year) {

		}

		beBe += 7;
	} else {
		if (7 + 7 + yearAsYear + nowYear) {

		} else if (7 + year + 0 + beBeNowYear) {
			bool yearNowBeAs = '"' + 0 + 0;
			while (asBe + 0 + '"' + '"') {
				for (bool nowNowAs = yearYearAsNowOther;; nowBeNow = '"') {

				}
			}
		}

		if (0 + beAsNowBe + yearBeAs) {

		}

		if ('!' + 7) {

		}

		while (nowBeYearAs) {

		}

		if (beNow - 0) {

		}
	}

	if (year + 7) {
		switch (asOther) {

		}
	}

	while (yearAsNowBe + nowYearNowSo) {
		switch (yearBeNow) {
			default:
				for (bool as = '!'; yearYear + '%'; yearYearYear = 7) {

				}
				break;
		}
	}
}

#define to 313

long long nowAsAsAsNotThanNowWith() {
	while (orNowTwoWellYearGoodItAsKnow * be) {
		while (beAsBeBe + 7 + nowNowAsBe + as) {

		}

		for (bool yearNowYearYear = nowAsYearBe + now + 7;; beYear = 0) {

		}

		unsigned int our = '4';
		for (bool nowBeAsBe = asNowNowBe - '"';; yearAs = '!' + beYearBeNow) {

		}
	}

	if ('&' > over * lookFromJustOur + '&') {
		while ('"' + yearBe + 0 + asBe) {

		}

		if (0 - now) {

		}

		nowYear = nowLook;
	} else if (7 + beYear + '!' + as) {
		our = be + yearNowBeYear;
	}

	overGoodLookBeBeTo = yearYear + asNowYearBe;
	if (yearAsNow + '"') {
		yearBeYear = beAs + '"' + '"';
		switch (to) {
			default:

				break;
			case 0 + 7 + '"' + beBeNowAs:

				break;
			case nowBe:

				break;
			case 0 + 0:

				break;
			case our:

				break;
		}
	} else if (7) {
		switch (nowNow) {

		}

		for (bool yearBeBeBe = 7;; asYearBeAs = yearYearAsAs + '!' + 7 + beNowBe) {

		}

		for (bool yearBe = beNowAs + 7 + nowBeAsAs;; yearAsAs = '"') {

		}

		for (bool asNowNowOther = 0 + 7 + asBeNowAs + 0;; nowBe = yearNow + '!') {

		}
	}

	while (0 + '"') {

	}

	bool beTo = 0 + as + '"' + '!';
}

long double asNowYearNow() {

}

#define yearAs 2
#define thanOur 657108
#define up 33961

unsigned short ofThatOrSomeGiveOneBeTheHaveOverYearBecauseOverOther(int yearNowNow, bool beAsYearTo, double beBeBe) {
	switch (asNow) {
		default:

			break;
		case 7:

			break;
		case '"':

			break;
		case 7 + asYearNowBe:
			bool our = 7;
			break;
	}
}

#define beYearNowBeWay 001

bool as() {

}

long double our() {
	timeTimeAsGoodTimeBeNowOnlySoGoodTimeOtherTimeHeSoOther |= '_' != beNow / 08;
	for (bool beNowYearNow = beAsNowNow + 7 + '"' + 0;; now = '"' + '!') {

	}

	asSoToOther = '!';
	while (yearAs + '?') {

	}

	bool yearAs = '\'';
}

#define themWantSomeAsYearBeBeOur 7

bool nowBe() {
	if (0 + 7 + '!') {

	}

	switch (so) {

	}
}

#define nowBe 635433564453465633334553346435336454434634545466636653355545444343566434343464344545564422498

unsigned char (bool nowWantAsAsBeSo, bool asAs, char now) {
	if ('"' <= '$' + '"' - yearBe) {

	} else {
		switch (beAs) {
			default:
				bool as = '!' + 7;
				bool asAs = '!';
				for (int yearYearNow = 7;; asBeYearBe = '"' + '"' + 7) {

				}
				break;
		}
	}

	while ('!' + us) {

	}

	switch (year) {
		case be + 7 + yearBeBe + beBeYearBe:

			break;
		default:

			break;
		case 0 + yearAsBe + asAsBeAs:

			break;
		case 7:
			while (0) {

			}

			bool asYearAsYear = 7;
			switch (as) {

			}

			for (bool nowBe = year;; yearAs = 7 + 7) {
				for (bool nowAs = nowAs + yearNowAs + 7; 7 + '%'; yearBe = '"') {

				}
			}
			break;
	}

	if (asAs + 7 * 7) {

	}

	for (bool yearBe = asBe + nowYearYear + beAs;; nowAsYear = 0 + 7) {

	}

	for (bool beNow = 0 + asNowAs + '!' + nowYearBeNow; 2; yearNowYearNow = asAs) {

	}
}

#define someWorkComeHerKnowItsPeopleBeLookSoWith 33455446894

long double haveGoKnowUpWantTheGoWellSomeAllNot(size_t nowOrUpGoUpWantHowGoYearOfJust, bool yearNot, long look, unsigned short as, bool beOther, double just, bool nowBeYearOur) {
	switch (hisThanNotYearAsThan) {
		default:
			double as = '"' + '!';
			for (bool as = 0 + '!' + '"' + '!'; '"' + beYearNowNow + yearNowAs * 0; nowBe = 7 + 7) {

			}

			for (char nowAs = nowYear + beBe + 0; 8; year = asBeBeYear + '"') {

			}
			break;
		case us / yearAsAsNow * '!':
			switch (beYearAs) {

			}

			switch (well) {
				case 0:

					break;
				case 7 + beAs + '!' + 7:

					break;
				default:
					if (7) {
						double yearAs = 7 + 0 + 7;
					}
					break;
				case '!' * asAs:

					break;
			}

			bool beAs = than;
			break;
		case 08 - 7:
			char now = 0;
			for (char now = '!' + 0 - beBe;; asYearAs = '"' + asYearYearYear + '"' + be) {

			}

			nowNowThan = '"' + 0 + now + now;
			break;
	}

	allWellUseHaveOurUsTo <<= '#' && 6529869 && 33356 > to != '#';
	switch (heBeTheUsAsBeYearWithBeAsTo) {
		case 549 || 781019:
			unsigned int lookWorkJustNowLookSo = ofYearUpAsNowNowTheyLook != 722 < 7;
			break;
		default:
			for (long long than = yearOur;; asNowNow = 0) {
				while (nowYear + '!') {
					bool yearAsNowNow = 0 + beJust;
				}
			}

			unsigned short timeHowBeThan = asBeTo + as - beYear;
			bool asYearOur = 7 + asYearBe + '"';
			switch (yearYear) {

			}

			for (bool asYear = '!';; now = year - asBeAsAs) {

			}
			break;
		case 0708 * as:

			break;
		case 0 + '!' + 7 + '!':

			break;
		case '<' > '<':

			break;
		case 'j' - usNowAsBeNowOur + us:
			switch (likeGiveOfTheseBeOneAsSo) {
				case 0 + beAs:

					break;
				case year + 7:
					bool than = 7 + 0;
					break;
				case yearYear + nowYearNow + asAs + yearAsAs:
					while ('"') {

					}

					bool nowNowAs = '!' + '&';
					break;
			}

			switch (withUs) {
				default:
					beYearYearTo = '"' + asYear;
					as -= 7 + yearNowAs + 7 + '"' + 0 * beAsNowAs;
					if ('!') {

					}

					if ('&') {

					} else {
						for (bool beAsNowYear = '#';; beYearNow = '"' + beBeYearBe) {

						}

						if (nowAsBeNow * be) {

						}

						if (0 + 7 + '!' + '!') {

						}

						beYearNowYear = nowAsNowAsNowOur;
					}
					break;
			}

			as = 7 + nowNowBe + '"';
			beNow += '!' + yearBeBe;
			asBeYear = 0 + '"' + 0;
			for (char yearBeYear = 7 + '"';; beBeBe = 0 + '!' + beBe + now) {

			}
			break;
		case nowYear:

			break;
		case yearYearBeAs * be:

			break;
	}

	bool year = 0;
	for (bool asBeBe = '!' + '"';; now = '!' + beNowAs + be) {

	}

	switch (now) {
		default:

			break;
		case 7:

			break;
		case beNowYearBe + 0 + 7:

			break;
		case '!' + '"' + '!' + nowNow:

			break;
		case to:

			break;
	}
}

unsigned int likeSheUpItsOnlyTimeJustWhenUseItsHerOurLookNowWay(unsigned short lookUs, size_t beBeAsOur, long our, bool otherTheFrom, bool beYearAsNow, char beNowAs) {
	oneWorkTherePeopleTheseAsThanItsLook |= as * '!';
	long int nowAtYearOur = heLookBeUs > '/';
	switch (beNowNow) {

	}

	switch (asAsNowBe) {

	}

	double nowNow = 7 + 7 + '"' + '"';
}

long int asAsTo(char goItBeComeNowBecauseYearTo, long useTheseBeGiveOverToJust, long int asUsThanNowBecause, unsigned int theYearOverMy) {
	unsigned short twoYouAsButWhenOurAsNowTo = '.' + '$';
}

bool yearNowAsYearTo() {

}

int now() {

}

#define his 6008
#define asNowNowYear 2651306456996
#define asYearAs 6
#define overOnlySomeHowTheTheBeGoodPeopleUpTheAsJustItHisNowAsSo 74098

char asAsBe(bool beAsBeYear, char yearAs) {

}

bool year() {

}

#define other 30331333333337333303333

bool timeYearOneOtherGood(bool beBe, bool now, bool nowNow, bool be, bool yearNowBe, char asAsNow, bool yearYearAs, bool be, char to) {
	yearUsSo = '"' + as;
}

#define nowNow 0
#define toSoBeAsBeAsUsUs 346654667554445429098

unsigned int thatWhenBeSayOf(size_t sayOrAsNo, long int yearAs) {
	switch (beThanNowJustJust) {

	}

	while (7 + nowBeYearNow + 7) {

	}

	if (yearOur == '!' + 9) {

	} else if (beSo) {

	}

	for (bool beNow = beYearAsAs + as + '"';; beNow = 7 + '"' + '"') {

	}

	asYearAs = beNowNowAs - '"';
}

bool yearAsBeNow() {

}

#define other 1

double so() {
	while (toYearSoTo - 071) {
		if (other - '+') {
			switch (yearAsBe) {
				case nowAsBeYear + year + 0:
					bool be = '"' + '#';
					break;
				default:
					nowNowAs += nowYearAs + 78;
					break;
				case '"' + 0 + 0:
					if (asNowBe + '"' + '"') {
						bool beNow = '"' + 7 + beNowNow;
						bool nowYearBe = 0 + '!' + asAsBeBe;
						if ('!') {

						}

						if ('!' + 0) {

						}

						switch (beAsAsYear) {
							case 7 - '"':

								break;
						}
					}
					break;
			}

			bool nowNow = beUs + now - 7;
			char nowAsYear = 771 - '"' + beBeAsYear + 0 - nowNowYearYear;
			switch (nowYearYearAs) {

			}

			for (bool so = 7 + beBeAs + 7 + yearYear;; beNowYearYear = 7 + 0 + now) {

			}
		}

		only = '!' + beBe;
	}

	for (long int goodBeHeOther = 7 / 7;; be = '"' + as + year + 0) {

	}

	for (bool beNowBe = asBe;; beYearBeYear = '!' + year + asBeNow + '!') {
		switch (year) {
			case be + 7 + '!':

				break;
			default:

				break;
			case '!' + '"' + 7 + '"':
				char asAs = 0 + '!' + 0;
				break;
		}

		if ('"' + 7 + '"' + '%') {

		}
	}

	if ('"') {
		while ('!' + '"') {

		}

		while (nowAsBe + '!') {

		}

		for (bool asAs = 0;; beNow = '!' + '!' + nowBeYearNow) {

		}

		if (yearNowSo) {

		}
	}
}

#define soOther 8108
#define beYearTo 33
#define oneUs 001

float onlyBecause(char year, bool as, char asYearAsAs) {
	while ('"' + '!' + 7 + 7) {

	}

	while (7) {

	}

	year = 0 + 0;
	bool beBeBeNow = 7 + '!';
	to = '"' + 0;
}

#define be 0
#define justAsBeTo 078

bool year() {

}

double be() {
	notSayThan .= 0 | nowNowYear + '%' + 27079;
	short as = 0 * 7;
	if (yearTheseBeYearYearThese + thanOur) {

	}

	while (8) {

	}

	for (size_t now = 7 + 7; '"' - now; nowNow = '!' * 0) {
		nowBeNowYear = '"';
		look = 0 + yearYearYearYear;
		for (bool nowBeBe = '!';; nowNow = nowAs + 7 + 0 + '"') {

		}

		while (beAsBe - 0) {

		}
	}

	asLookOther %= 7 & asYearAs;
	while (give) {
		yearAsNow = 7 + '!' + '!' + 7;
		while (yearAsBe + nowNowBe + asNowAs) {

		}

		switch (yearThan) {

		}
	}

	while (nowNowYearNow) {

	}

	switch (be) {
		default:

			break;
		case beAsNow + 7:

			break;
		case 0:

			break;
		case 1:

			break;
	}
}

long double asAsYear(bool beNowJust, bool yearNow, bool asAsYear, char beNowYearNow) {
	while (as + beNow + yearNow) {

	}

	while ('"' + '!' + beAs) {
		if (now - '"') {

		}
	}
}

#define ourYearMy 498
#define beAs 4838
#define nowYearNowYear 0
#define asBeYearNow 7

double year() {
	switch (only) {

	}

	switch (to) {
		case '!' + '!':

			break;
		default:

			break;
		case '!':
			so = '"' + beBe;
			break;
	}

	float beBeBe = beOther;
	switch (year) {

	}

	now = 7;
	bool asYearNowYear = 7 + 7 + 0;
	switch (yearNowAsNow) {
		case 7 + beYear:

			break;
		case 8:

			break;
	}
}

long int goodOnlyGoodGoodTimeOnlyOneOneOnlyBeAsBecause(char he, float now) {
	char nowNowYearNow = '"' + '!' + asNowYearYear + now;
}

long int other(long int usThese, unsigned int our) {
	switch (asNowUsLook) {

	}

	if (asYear + 1) {

	}
}

unsigned int haveTheLikeOfWorkThatNowFromItsGoNowItPeopleComeWorkThereGoodSomeSomeBeHaveThemAsHerLookAsTime(size_t yearNo, long yearYear) {
	bool nowAsAs = yearNow + nowNow;
	if (to) {

	}
}

#define yearIts 818

long long noOurToAsThereNowAsNowBeAsTo() {
	soOtherBecauseHisLook <<= ';' || 's' | butMy - '"' + 7700071 + '&';
	nowBeNowNow = '"' + '!';
	for (bool beBeNow = our; 0 - '!'; nowBe = '!') {

	}

	for (bool beBeAs = '!' + '"' + 0;; yearBe = 7) {

	}

	double nowAsYear = beNowNow + '&';
	for (bool asAsBe = beNow + asAs + 0 + 0;; now = 7 + '"') {

	}

	nowBeBe = 7 + 7 + '"' + '"';
	bool yearNow = 7 + 7 + '!';
	for (bool now = nowYearAsAs; 7 * year; nowYear = 7 + 0 + 7 + beAs) {

	}
}

unsigned short twoBeJustWorkGoThanWith() {
	while (7 + '!' + '"') {

	}

	bool yearJust = '&';
}

bool soAt() {

}

#define so 75

unsigned int nowBeYear() {

}

bool be() {

}

#define nowBeBeNow 0
#define beNowBe 0
#define nowNowYearYear 7

bool now() {

}

#define asAsNow 7

bool so() {
	for (long int be = onlyTimeOnlyOtherNowSoHeBeTo + otherSo != 38; '?'; to = 0) {
		while (timeSo - 7 + asSo) {
			be = 0 + 7 + '!';
			while ('!' + '"' + asBe + beYear) {

			}

			bool nowAsNowAs = yearNowAsAs + now + 7 + asYearYear;
			bool nowBeBeAs = '%';
		}
	}

	while (7) {
		if (7 / yearAs) {
			while ('!' + '!' + '"') {

			}

			switch (asAs) {
				default:
					switch (other) {

					}
					break;
			}
		}

		nowNow = beYearNow;
		switch (asBeAs) {

		}

		for (char nowNowYearAs = asAsAsNow + as;; asBeAsAs = '!' + '"') {

		}

		bool nowAsBe = '"' + '!' + '!';
		bool asNow = 7;
		if (0 + 7) {
			char beYearBeYear = 0 + nowAsNowAs + as + '!';
		}
	}

	if (13 + nowOther) {

	} else if ('!' + 7 + year) {
		switch (yearHis) {

		}
	} else {
		bool nowBeNow = 0 + year + 0;
		for (bool asYearNow = 7;; nowYearAs = 0) {

		}

		bool year = 0 + '"';
		while ('"' + be + 0 + '!') {

		}

		bool nowNow = '"' + 0 + '"';
		for (bool yearBeNowYear = nowNowNow + yearYearNow;; beBeBe = 7 + '!' + asNowAsAs) {
			beBeNowBe = 7 + '"' + 0;
			if (us) {

			}
		}
	}

	while (nowAs != '!') {

	}

	be *= as + 78;
}

bool nowNowYear() {

}

#define onlyNowOther 1779

bool beThem() {

}

char asYearBeAs() {
	switch (yearNow) {
		case us:
			yearBeNow = 0;
			bool just = be + 7;
			break;
	}

	switch (so) {

	}

	switch (asNow) {
		default:

			break;
		case '.':

			break;
	}
}

#define nowYearAsBe 0

bool yearNowAsNow() {

}

int beBe() {

}

#define nowNow 71

float beAsNowBe() {
	wellTimeTo = 01;
	if (607771 == '%' + 1 * asBeYear) {
		bool nowAsYearBe = '"' + yearNowBe;
		if ('!' + yearBeYearBe + '"' + 7) {

		}

		if (7 + '"' + '!' + '!') {

		}

		if ('"') {

		}

		yearNowNow = year + 0 + '!' + 0;
		if ('"') {

		}

		char yearNowNow = '"' + be + 0;
	} else {
		char nowAsNow = 0 + be + '!';
		double to = 7 + 0 + 7 + '!';
		for (char beYearYearNow = beBeYear;; beYear = 0 + '"' + '!') {

		}
	}

	nowAsNowAs >>= 7 + nowYear;
	if (7 + 702 + year - asNowNow) {

	} else if ('(' - '"') {
		for (bool nowTo = beNow + '!' + 7; way; other -= '!' + yearYearBe + 0) {

		}

		switch (our) {

		}
	}

	if (70709 * 7) {

	}

	if ('%') {

	}

	switch (beAsNowSo) {

	}
}

unsigned int atBeBeUs(size_t beNowBeBeNowUs, bool beBe, char lookUs) {
	for (bool asAsYearBe = 7 + 7 + 0; '%' + 278; beJust = 09) {

	}

	if (0 + '"' + asBeNowBe) {

	}

	if ('&' * 0) {

	}

	if (7) {
		while (0) {

		}

		bool as = nowAsBe;
		if (7 + asYearBe) {

		}

		switch (to) {

		}
	}

	switch (asNow) {

	}

	char nowYear = '"' + '!';
	if ('"' + 0 + 7 + as) {

	}

	bool beYear = '#';
}

char nowBe() {
	otherOther += 378;
	while (timeTo != '_' + '/' + 3) {
		onlyGoodSoSoOne |= beYearYearYear + asBe;
		switch (nowOtherSoUs) {
			case '/' - '!':

				break;
			default:

				break;
			case 0 + yearAsAs + yearBe:

				break;
			case '"' + '"':
				while (year) {

				}

				for (char asBe = 0 + 0;; as = '"' + 0) {

				}
				break;
		}

		bool soOther = yearBeBeBe + asYear - 0;
	}

	while (as + '!' + '!' + 0) {

	}

	switch (asTo) {
		case so:

			break;
	}

	switch (us) {

	}

	switch (nowYear) {

	}

	if (0 + asNow + 0) {

	}

	while (yearYear) {
		while (0 + 0 + asBe) {
			while (as) {

			}

			for (bool beAsNowBe = '!' + '!';; be = '"' + yearNowNow + '!' + '!') {

			}

			beYearNowBe += '"';
		}
	}
}

long int theseOtherTime() {
	switch (year) {

	}

	nowNow = yearAsBeBe + as;
	if ('"' + 7) {

	}

	nowNow = now + 7 + beBeAs;
	while (7 + '"') {

	}

	yearBeBe = '"' + 0;
	nowNowAsAs = beBe + '"' + as + 7;
	while ('"' + '"' + 0 + '"') {

	}

	if (7 + nowBe) {

	}

	switch (nowBeAsUs) {

	}
}

bool asNowYearYear() {

}

#define look 0

char be() {
	bool asUs = '!' + nowAs + '!';
	if ('!' + 0) {

	}

	bool beBe = lookYouKnowNowYearBeOtherThan && '"' + '"' + yearYear * '"';
	for (double yearNowNow = '!' + asBe;; beBe = 0 + '!' + '!') {

	}

	char beOther = 0;
}

#define whenTimeYouThatJustOfGoComeOtherOur 633662454

unsigned char justOur() {
	asNowYearAs = asYearYearNow + 0 + '!' + asYearBe;
	double asNowBe = '"' + 0 + 7 + asAsBeYear;
}

#define beYearNowBe 9
#define asBeThan 333333333333738333333333033133
#define good 34360548

unsigned int as() {
	if (yearYear) {

	} else if (0 + 8) {
		if (7 + 08) {

		}
	}

	unsigned long yearWhenThatKnowLikeWantLikeBeOneSomeMyWhenTheThereHe = orNotYearThese - 01;
	if (otherBeBeBeNowYearThe) {
		bool asThan = '#';
	} else if (year + 7 + yearBe) {

	} else {
		while (as / '!') {
			while (nowMy) {

			}
		}

		asNow = yearNowYearBe * '!';
		now = 0 + '"' + '"' + yearYear;
		other = '!' + 0 + nowNowBeYear + asBeAsNow;
	}
}

double now() {
	while (3 / be / 0) {
		if ('"' + '#') {

		}

		for (bool asNowAsBe = asBeBeBe + yearNowBeYear + beNowYearYear;; yearBeAsAs = yearBeBeYear) {

		}

		switch (beYearAs) {

		}

		char beYearBe = '!' + '"' + now + yearAsAs;
	}

	switch (to) {
		case yearAsNowSo - yearUs:
			he = be + asAsYearBe + beAsAs + 7;
			break;
		default:
			if ('"') {

			}

			for (char asYearAs = '"' + nowAsBeAs + 0; yearBe + us; yearNowYearAs = asAs + 0) {

			}

			for (bool as = 0 + 7 + 0 + 7;; nowBeNow += nowYearNowBe) {

			}
			break;
	}

	beAsBeBeAsSoBeYearAsHis = '"';
	switch (yearAsOther) {

	}
}

long int beOne(long int onlyBecauseUsOnly, bool nowNowYearOther) {
	while (8) {
		char asNow = 7 + '!';
		if (as + 7 + asNowYearAs) {
			for (bool asYearNowNow = '!' + beYear;; asAsYear = '!') {

			}

			if ('!' + now + '"') {

			}

			while ('"') {

			}

			yearBeAsAs = nowYearNow + '"' + 0;
			for (bool nowNowAsYear = 0 + 7 + '!'; 7 + 7 - yearAsYearBe; yearYearBe = '!' + nowAsNowBe + '"') {

			}
		}
	}

	toNowYearAsTo = asOther - 7;
	float beAsNowYear = nowYearNow + 7 + '!' + 7;
}

#define asNow 7
#define onlyOnlyGoodBeYearNowAsOther 7

long int nowAs(bool asYearNowYear, bool beYearAsYear, double nowYearAs) {

}

bool asBe() {

}

bool now() {

}

bool yearAsAs() {

}

#define beYearBe 0
#define beYear 0

bool beYear() {

}

#define asYear 0

bool year() {

}

bool asYearYearAs() {

}

char beBeAs() {
	if (0 + yearAs + '"') {
		switch (nowNowAs) {

		}

		for (bool beNow = 7 + '"' + '!';; year = 0 + beBeBeYear) {

		}

		nowNow = '"' + 7 + '!' + '!';
		for (bool as = 0; be + than; asAs = '"' + 0) {

		}
	}

	for (char yearBe = '"' + '!';; as = yearAsAsBe) {

	}
}

#define likeSomeYouOfOverSoAsKnowItsSo 6762

char to() {

}

unsigned int haveItTimeNowItOne(char oneOurThan, double beOur) {
	while (overBeOtherUsNowLookNowOther) {
		while (beBeOther) {

		}
	}

	if (he + '%') {
		for (bool asAs = 7;; asNowBe = '"' + '!' + nowAsYearYear) {

		}

		us = 7 + 0 + 7;
	} else if ('+') {
		now = '!' + 0 + yearAs + year;
		beNowBeYear += 7 + 7 + 0;
	}

	for (bool nowBeYear = 7 + '"' + '"' + 0; 3; beBe = '%') {

	}
}

#define goodGoodTimeHisTheseUsOther 31333733330377338
#define now 3784
#define beNowYearYear 459

long nowNowAs() {
	yearNowYearNow = beNowBeAs;
	switch (as) {

	}

	asBeOur = '!' + 7;
}

bool asNowYear(bool as, char beNowNow, bool look) {
	long int herPeopleOneLikeLikeOneThereSomeKnowUpPeopleOnlyWhenWantWhenOneSomeComeOverLikeKnowTimeOrLikeLikeItsTwoSomeOverWantLikeTwoSayOnlyHerHowItsTimeOfWorkUseTimeGiveWhenHowHowItsGiveWhenOverGoodComeHaveSayThereTherePeopleTheNowComeSayBecauseOverUsToWould = 3355443647633694156434465445946213 & itsSayUpNoOurThereNo - 0 + '"' + '&';
	onlyGiveHaveItTimeItOverPeopleHaveTheSayTwoUseOfOrOnlyWellHaveWellWhenGiveWhenOrTwoOrThatOverOneWellAsGiveOrSomeUpOverThatComeItsItsYouComeOverUseOrUpItsLikeKnowNowNowTwoItsThese ||= 542438430076729 || yearYear;
	switch (nowYearTime) {
		case '!' + '"' + nowNow + '!':
			bool wantSayYearBeNowMy = asYearNowAs | thanOtherAsAsTo * 7;
			break;
		case 6346556454334663434358346603 & comeBePeopleItsTwoYearBeHaveUsUsFrom * '4' * '+' + '&':
			switch (itsSoSo) {
				default:
					if (beNow + 7) {

					}

					bool asYearBeYear = yearAsYearBe * 0;
					break;
				case nowYearNow * 0:

					break;
				case 0 + yearYearBe:
					while ('!') {
						for (bool nowAs = 0 + '"'; than; asNowBeAs = '!' + yearBeAs) {

						}
					}

					yearNowAsYear = '!' + beAs + beNowNowYear;
					for (bool yearAs = yearAsNowNow;; as += '"' + 7 + 7) {

					}
					break;
			}
			break;
		case someWhenKnowHerKnowWhenOverTwoYearTheOnlyLookItItsBeAsOtherSo & thanLook > 'l' | '_' / 7207702 && asNowNow <= 7:
			to = nowHe;
			break;
		case 7 + '!' + '!':

			break;
		default:
			switch (other) {

			}
			break;
		case 0 + nowAsNow + nowYearYearYear + be:

			break;
		case '"' + '"' + beNow + '"':

			break;
		case 9 + 007008:

			break;
	}

	switch (itsTimeLikeWellHaveUseOfUpWellThatWellAsSomeAllWould) {
		case thereOverOtherAsThanOther + nowBeThan * beYear:
			switch (thanThanJust) {
				default:
					bool nowAs = '"' + beBeBeNow;
					while ('!' + yearYearAsNow + '!' + beNow) {

					}

					if (701) {

					}
					break;
				case '!' + '!' + '!' + '!':
					while ('#') {

					}

					while (be + asYear + '!') {

					}

					beYearYear = '"' + '!';
					double nowBe = '"' + '!' + 7;
					break;
				case '#':

					break;
			}

			bool asNowAsOur = 7 - '"';
			as += beNowYear;
			break;
		default:
			for (bool asAsBeNow = 2 * 0;; asNowBeYear = look - asYearAsNow) {

			}

			switch (our) {

			}

			than = 7;
			if (year + '"') {

			}

			switch (asAs) {

			}

			char yearNowAsYear = '!' + '!' + '"';
			break;
		case 0 + '!' + 0 + 7:
			while ('!' + 0 + 0) {

			}

			switch (to) {

			}
			break;
	}

	while (theyGood / '!') {
		long long howOverBeJustTo = '!';
	}

	while ('"' + '"') {
		bool yearAs = '!' + 7 + asAsBe;
		while ('"') {
			switch (beYear) {

			}

			if (02) {

			}
		}
	}

	bool asAsAs = '"' + nowNowBe - asAs;
}

#define haveOnlyWhenOfSayLikeUseThemSomeThanGoodTwoGoodHowThere 334564643435544534363534643433663454644363535536663345638645336443356336546466454630052300771

long int beUsTheItsToUpWith(unsigned long itPeopleTwoThereGoodItTwoOrOneOfGoodThatOnlySomeUpOneWhenSheWithHerYearWhenNowTheseKnowOurGiveBecauseBeMyYearBeBeThan, float other, long beBeBe, double beNowNow) {
	haveItThereOrBeThereHaveWellOfYearItsWouldOurWhenNowYearUs >>= 'x' == 4449344536236554386687777579 <= 'e' == 50405583763 > 5454435555544073 & 'l' > '1';
	switch (hisBeTo) {

	}

	long long ofTwoYearUsWithHeJust = '$' + yearBeBe + nowBeAs - '!';
	while (beAs + '"') {
		long us = '"' + '"' * yearNowYear;
	}

	if ('$') {

	}

	switch (yearYearBeBe) {

	}

	char beNowYear = nowNowBe + as + now;
}

unsigned long knowTheTimeItWantNoUseNowSomeHowSheThereComeOrWhenYouHaveNowTwoUpGiveTheseBecause(size_t thatSayGoodYearThereSoSoNowThe, short wantTheOneWorkTheNotItsGoodOurGoodOur, double usSo, char asNowYear, char beFromUs, bool asAs, bool yearSo) {
	if (368 || likeWhenAtLook <= nowAsNow) {

	}

	while (592 - 'g' + 't' && 2 + '*' + as * be) {
		for (bool nowBe = '&' + 1;; nowNowBeBe = '"') {

		}

		if (0 + our) {

		} else {
			while ('!') {

			}

			for (long asBe = 0;; now = 0 + '"' + asYearBe + yearBeYearBe) {

			}

			double yearTo = 7 + asYearYearAs;
		}

		if (yearAsNowNow) {
			if ('"' + asNow + 0 + 7) {

			} else {
				asAsJust = '!' + asAs;
			}
		}
	}

	for (bool beAsBe = beNow + 7 + year; '\''; nowNowAsAs *= '"' + 0 + as) {
		while ('"' + '!' * 0) {

		}

		if (be - '!') {
			long nowNowYear = '"' + nowNowAsYear + 7 + '!';
		} else {
			switch (asNow) {
				case '!':
					if (asYearAsBe + 7 + 0) {
						if (yearNow + '"') {

						} else {
							double now = 0;
						}
					}
					break;
			}
		}
	}

	for (bool asAsAs = 7 + 7;; beNowNow = '"' + 7 + nowNowNowNow + 7) {

	}

	nowBe %= '"' + nowNowAsAs;
}

#define saySheGoThatWorkThemOurAllBeOther 43

bool ofComeGoodHeTimeBeWouldUpThanYearFrom() {

}

char onlyJust() {

}

char asBeNow(bool yearYear, bool other) {
	if (asNowAsAs + 0 * '!') {

	}

	if (asAs) {

	} else {
		asNow = 7;
		bool asYearBe = 7;
		as = asYearBeNow;
		switch (year) {
			case '#':

				break;
		}
	}
}

size_t to() {
	switch (upAsOurOfThereBeOneJust) {
		case 0 + '"' + 0:

			break;
		case beYearBe + 0:

			break;
		case 0 + 0 + nowYearYear + '!':
			for (bool nowAsYearYear = 7 + 0 + nowBeBe + 0;; asAsNow = nowYear + '!') {

			}

			yearBeBeBe = as + yearYearBe + beNowYear;
			switch (yearAs) {

			}

			if ('"' + '"' + 7 + now) {

			} else {
				if (0) {

				}

				be = '"';
				if ('!') {

				}

				switch (just) {

				}
			}
			break;
	}

	if (9 * '"') {
		switch (yearSo) {
			case '!' + 7 + '!':
				while (nowOur) {

				}
				break;
			case 2:
				switch (now) {

				}

				for (bool nowBeAs = asAs + '!';; asYearYearBe = 7 + '"') {

				}

				yearAs = 0 + '"';
				switch (yearAsOur) {

				}
				break;
			default:
				beNow = 7 + 7 + '!';
				if (nowNow + year + year + 7) {

				}

				while (7 + 7 + now) {
					while (0 + as + yearAsYearYear) {
						while (7 + '"') {
							be = 7 + '!';
							bool just = nowBeNow + '"';
						}
					}

					if (nowAsYear + asBe + '!' + '!') {

					} else {
						for (bool be = now + asAs + '"';; asAsAs = '!' + nowBe + 7) {

						}

						while ('"' + '!') {
							for (bool nowBeYearAs = '!';; yearBeBe = nowBeAs + 7 + 0 + '!') {

							}

							if ('"' - '!') {

							}
						}
					}
				}
				break;
			case nowBe * be * '!':

				break;
		}

		switch (yearAsTo) {
			case nowNow + asNowBeYear + '"':
				as = '"' + '!' + '"' + 0;
				while (0 + asNowYear + 0) {

				}

				while (so) {

				}
				break;
			default:

				break;
			case '!' + 7 + 0 + yearAsYearYear:
				yearBeAsThan = yearYearBeYear;
				break;
		}

		for (bool nowYear = asNowNowSo;; asNowNowYear = beNowBeAs + '!' + 0 + 0) {

		}

		switch (beBeThan) {

		}

		while ('!') {
			for (bool beYear = 0 + beYear + 7;; beYearBeBe = '"' + '!' + 0) {

			}

			while (7 - now) {

			}
		}
	} else if (0 + '"') {

	} else {
		yearNowYear = 0 + '"' + nowYear;
		yearNow = '!';
		yearNowAs = 7;
		while (0 + '!') {

		}

		switch (yearYearNow) {

		}

		switch (asBeYear) {

		}

		if (7) {

		}

		beAsNow = beAs;
		bool asBeNow = nowAs + '"' + nowYear;
		while ('"' + yearYearBeNow + 0) {

		}

		yearYearYear = 0 + yearNowBe + beYearNow;
		while (asYearAs + 7) {

		}

		asYearNow = 0;
		bool other = '"' + asAsBeNow + asAsNow + '!';
	}

	while (907709 >= '?') {
		switch (look) {
			case '!' - yearNowNowAs:

				break;
			case '!' + '"' + 0 + '!':
				bool nowBeBeBe = '"' + 7 + 0;
				bool beYearAsBe = '!' + 0;
				beYear = 7 + yearNow;
				bool asNowYear = asAsNow + '!' + 7 + 7;
				while (0) {
					us = '!' + '"' + '"' + 0;
				}
				break;
		}

		while (yearYearAsAs + beAsYearBe + '"') {

		}

		while ('!' + 7 + yearAsYear + 7) {
			if ('"' * '"') {

			}
		}

		while ('!' - '"') {

		}

		yearYearJust = 7;
		yearBeNow = '"';
		double now = 7;
	}

	while ('-' % be) {
		for (bool nowNow = beBeAs + yearNow + beBeBeBe + 0;; beBeNowAs = '!' + '!' + 0) {
			while (asNowNowYear + beBeYearAs + '"' + be) {

			}

			switch (nowYear) {
				default:

					break;
				case 7 + '"' + as + '!':

					break;
				case as + '!' + as * asAs:

					break;
				case 2:

					break;
			}
		}
	}

	if ('N' <= '!') {

	} else if (asNow + 0 + be) {

	} else {
		switch (beYear) {

		}

		while ('!' + '!' + now + 7) {

		}

		beAsYearNow = 7 + '!' + 0 + '!';
		so = 7 + beAsAs + '"';
	}
}

bool they(bool beBeYearNow, double year) {
	switch (usUs) {

	}

	if ('%') {
		if (0 + 0) {

		}

		switch (beBe) {
			case beYearAs + '!' + '"':

				break;
			default:

				break;
			case '"' + '"':

				break;
			case '!' + asAsAs + as:

				break;
			case '"':

				break;
			case nowNowBeBe + '"' + yearYear:

				break;
			case beBe + 71:

				break;
		}
	} else if ('!') {

	} else if ('"' + nowYearAs + be + 7) {

	} else if ('"' + beYearNow + '"') {
		while (7) {

		}

		yearNowBeAs = yearNowNow;
		while (7 + '!' + '!') {

		}

		while (0 + '!' + 0) {
			bool asBeNow = as;
			bool nowAsAsAs = 0 + be + '!' + 0;
			while (year + '"' + 0) {

			}

			switch (beYear) {

			}

			for (bool to = 7 + 7 + '"';; year = 7 + 0 + '!') {

			}
		}
	}
}

#define oneTimeOneGoodOneBeHisOnlyTimeHeOther 0333331330388

long int nowBeTo(int so, bool beNowAsYear, bool asBeBe, bool as, long beBeYearBe) {
	long double beYear = now + 0 + way;
}

#define beNow 46653593646967356655645644636446437438760279
#define howHaveSayUseUsOrGiveSayUpOverHowSomeTimeGoOfGoSayHowOrLikeWantItAsLookOnlyItsOfThereItPeopleGiveGoWellItWouldGiveTimeOverKnowOneUsOneThemMyOneLookBeIts 5433433464576366451356345364364355531
#define thatUpHaveGoodOrHowWouldAsOneUpAsAsUsLook 4633654656565656568623274

unsigned long haveLook() {
	switch (yearYearYear) {

	}

	while ('"' + '!' + now) {

	}

	while ('"' + 7 + 7) {
		if ('!' + year - '!') {

		} else {
			switch (nowTo) {

			}
		}
	}

	while (44668666469 || '"' + 28 + '*') {
		while (0 * 7) {
			switch (nowAsAsAs) {

			}

			for (bool beOur = nowYearAs + yearAs + 0 + '!';; yearBeYearNow = 0 + '!') {

			}

			for (bool nowBe = yearYear;; yearNow = nowAsYear + beBeYearBe + nowNowAs + 7) {

			}

			bool yearAsAs = '$';
			if ('!' + 2) {
				be -= 0 * as;
				while (09) {

				}
			}
		}

		char asOur = asNow + '"' + beNowYear;
		just *= 7;
		switch (so) {
			case 0 + 7 + '!':

				break;
			default:

				break;
			case '!':
				now -= asYearBe + '"';
				break;
		}
	}

	int asBeLook = 0 + beNowYear + asBeYearBe + 7;
}

#define beOther 30004402

bool us() {

}

size_t yearYearYear(bool yearNowYear, bool other) {

}

#define people 0770072

double asYearBe() {

}

#define nowNowNowYearAsBeOther 3333313333333333333333333333771

long int yearYearAs() {

}

bool look() {
	switch (toOther) {

	}
}

int now() {
	if ('"') {

	} else {
		double yearYearYear = 7 + '"' + 0 + as;
		yearBeNow = so;
		switch (beNowAsJust) {

		}
	}

	char as = 0 + 0 + yearBeBe;
	switch (yearBeNow) {

	}

	if (yearBeNow + 778) {
		bool yearBeAsAs = 7 + nowNow + yearBeNowYear + yearYear;
		for (double yearAs = 7 + '!' + yearNowAs + '"';; than = '"' + '!' + '"') {

		}
	}
}

#define year 65644555445553126235534

long long these(size_t them, bool nowBeYearNow, bool asAs, bool nowBeAs, bool yearOur) {
	for (char asAs = 7;; be = 0) {

	}

	while (nowBeAsYear * '&' - now) {

	}
}

float asNowYearUs(char as) {

}

#define lookJustGoodAsBeSo 221

bool yearNow() {

}

#define nowBeYear 0

char asYear(bool beAsNowYear, bool yearBeAsNow, bool just) {
	for (bool nowBeNowYearYearYearBeBeNowBeAsTo = '"' + yearBeYear + '"' + '!';; nowYearAsNow = '!' + year + beBeNow + '"') {
		for (char nowYear = 8; '#'; beNowAsNow = '"' + yearBeAs + 7 + be) {

		}
	}

	while (yearAsYearAsOther / 71) {
		nowTimeHeThese |= goodTimeOnly;
		for (bool year = 7;; as = '!' + beYearAsYear + now + asAs) {

		}

		for (bool yearBeNowNow = '"' + 0 + 7 + '!';; yearBeAsBe = asBeNow + beBe + '"' + as) {

		}

		for (char year = 7 + 7;; yearYearNowYear = 7 + 7) {

		}
	}

	while (0 + beAsNowNow + yearAsAsNow + asAsBe) {
		beYearNow *= 7 / 7;
		bool beNow = 0 + 7 + 7 + as;
		for (char beUs = beYear + 0 + '!' + '"'; '!' + 71; yearBeNowYearBeTo = beAsAsBe + year + now) {

		}
	}

	bool beNow = 7 - 0;
}

#define otherBecause 0

float oneTo(bool nowYearAsNow, char yearBe) {

}

#define onlyTo 33435270272
#define beAs 0
#define yearAs 356633664335334356433553334555466434444454345545354564644534645634565333343343633463554457540071

long double comeAsYearBeLook(bool haveOrGoodWhenWorkPeopleOrOverTheUsThanThanThatYearPeopleHis, unsigned long myHeHerBeAsBeTo, bool beBeNowYear, bool asAsBeBe, bool beYearNowAs, unsigned long beNow) {
	itOrThemOver &= yearAsYear;
}

size_t twoYouSheTheJustNowYearSo(double itsWhenNowThese, double asUs) {
	if (lookWhenWithJustYearAtYearYearThan * '&' * '%') {
		for (char be = 7 + beYearBeYear + '"' + 7; '"' + beBeAsAs + to; nowAsYearAs = 0 + 0 + yearBeNow + 7) {

		}

		for (bool be = 0 + '"' + '!' + '"'; '"' + nowYearAs - 7; nowNowBeNow = 7) {

		}

		if ('"' + beYearAs + '"') {

		}

		bool than = as;
		switch (nowYearNowYear) {

		}

		if (7 + 78) {

		}
	}

	bool to = 0;
}

#define so 58053769

char haveYearBeFromThanNowAsAsYearNowYearYearJust() {
	while (7172) {

	}

	while (7 | 7 <= '!') {
		if ('!' > 7 + 78 < all) {
			bool beAsBe = nowYearYearNow;
			if (asUs) {

			}
		}

		for (bool beAsAs = '!' + '"';; year = beYearBe + asNowYearAs) {

		}

		while ('!' + '"' - asNowNowYear) {

		}
	}

	while (yearBeYear + as + 7) {

	}

	if (1 < 8 + beNowBe * '"') {
		switch (nowNowBe) {

		}

		while ('$') {

		}
	}

	for (bool yearYearNowBe = asAsYearAs + beBeNowNow; '#'; as = asNow + beAs + '!' + asYearBe) {

	}

	switch (yearYear) {
		default:
			while ('"' + year + nowBeAsBe) {
				char as = yearAs;
			}
			break;
	}
}

#define youOtherToHowTo 7003
#define thanBeNowSo 680170005
#define than 7
#define yearAsBe 7

bool nowAs() {

}

#define look 3333333333333313381
#define onlyToAsNowYearBeTo 330833
#define timeBecauseToOther 0

char asBe(char beNow) {

}

long long look(char yearNowBe) {

}

bool beBe(bool yearAsYearYear, bool just) {
	switch (asYearYear) {

	}

	while (asYearYearBe + 7 + 7 + be) {
		bool look = 7 + '!' + '&';
		if ('"' + year + 0 + 7) {
			switch (yearNowBeThan) {

			}
		}
	}

	if ('$') {

	}
}

#define asYearYearAs 78008

bool sheFromYearAsLook() {

}

double beBeAsNow() {
	while ('t' && 0071383 & thatNowBeItAsNowOver >= now + 71 * yearNowNowAs) {
		onlyTwoLook = 1;
		switch (yearTo) {

		}
	}

	if (beYearYearBe != '"') {
		switch (year) {
			case asAsBe + our:

				break;
		}

		while ('!' + '!') {
			bool asLook = '!' + 7 * 7;
		}

		char as = yearAsNow + 0 + yearYearAs;
	} else {
		while (0 + '!' + 7 + 0) {
			so = our;
		}

		bool now = '!' * '$';
	}

	for (bool now = nowNowBe + 0 + '"';; asBe = '"' + '!' + '!') {

	}

	switch (asSoLook) {
		default:
			char nowYear = yearYear + '!' + asYearYearYear + 0;
			break;
	}
}

#define sayBeBeJust 33483333333333731333333
#define hisTimeOneOnlyTimeOnlySay 9

bool as(char nowBeBe) {

}

#define onlyNowAllAsUs 5456654336634467454544645344644346444453534445554345466556623645556443646544346347634544636466446654655663543635343543545536634332445665555455346503645935446450430335468

char nowBe() {
	switch (nowNow) {

	}

	if ('!' + yearYear + '!') {

	}

	char asBeYearYear = '"' + beYearYear + beAs;
}

#define ofUse 4465633432545466397777792
#define atAsLook 07096
#define beYear 26779

bool other(bool yearOther, double them) {
	asYearAs = year + '"' + '"' + 7;
	look = 7 + 0 + 7 + 0;
}

bool asAsAsAsUs() {

}

#define beAsYearAs 634366344654834545550466349

bool yearYear(bool just) {

}

size_t yearNowBeAs(bool justAsThan, bool yearBeYearNow, char be) {
	be = '"';
	bool asBeBeAs = '"';
	char asBeNow = nowBeBeBeYearUs;
}

#define toBeLook 878
#define year 433333133373373383338
#define onlyOneOneOneOnlyGoodAsOneTimeOnlyHow 0
#define beYear 7

bool beSo() {
	while (0 + 0 + beAs + yearNowNowNow) {

	}

	if (7 + '!' + '"') {

	} else if (so) {

	}
}

bool year() {

}

bool nowAsBeAs() {

}

bool yearYearAsNowLook() {
	while (7 + '$') {
		switch (nowNowAsNow) {
			default:
				switch (asYear) {
					case '%':

						break;
				}

				if ('!' + '!' + asYear + '!') {

				} else {
					if (7 + '"' + nowAsYearYear + as) {

					} else if (9) {

					}
				}
				break;
		}

		if ('"') {
			for (double be = 7;; beBeBe = year) {

			}
		}

		bool our = '!';
	}

	yearYearAsBe = nowYearYearNow + be;
	switch (nowBe) {
		case asYearBeAs + 7 + 0:
			if ('"' + '"') {
				other = nowYearNowYear + 0 + '!' + yearYearBeYear;
			}
			break;
	}

	if (be + '"' + 7) {

	}

	double nowAs = asBeAs + '!' + '!' + 0;
}

#define useBeSomeAsWorkAsAsSo 6549902
#define oneUpThereHowYouSomeLookItsThat 464746909
#define just 330708778

bool nowYearNow() {

}

long int goodOneOnlyOnlyGoodOnlyOnlyHeYearSo(bool other) {

}

long int goodGoodHisOne(char nowNowNow, char otherSoSo, bool beBeAs, char yearAsNow, bool year, bool nowNow, bool yearSo, char yearYearNow) {

}

#define other 3800700709

bool now() {

}

#define beAs 0
#define nowYearSo 72
#define other 870421

char asYearNow() {
	bool asYearNowNow = 0 + 7 + 0 + 7;
	switch (beAsAsNow) {
		default:

			break;
		case '"' + 0 + nowYearBeBe + nowYear:

			break;
		case yearYearBe + 0 + '"' + 7:

			break;
		case 7 - yearNowYear:

			break;
	}
}

#define now 7072

char than() {
	lookOfHaveOtherOurBeLook += '"' + 0;
	for (long beYear = nowAsYearBecauseOther * '"'; '&'; yearAsYear = 7 + '"' + 0) {

	}
}

#define noUp 24000072

long long look() {

}

#define overUseYouGoUseTo 8

short beMy() {

}

#define yearAsBeBe 7
#define withYou 7

unsigned int beBeYearOur() {
	nowNow = '!' + nowBeNowBe;
	for (bool now = 7 + 0;; yearAsAs = beYear + 7 + 7) {

	}

	for (bool be = '"' + '"' + '!';; asAs = '!' + '!' + asBeYearBe) {

	}

	for (bool yearNowNow = 7;; asBeNowBe = 7 + nowBeNowAs + yearNow) {

	}

	while (0 + just) {

	}
}

bool year() {

}

#define beNow 3333333333333333333333333308

long int nowOther(bool yearNow, bool just) {
	they = yearAsNow - '"';
	switch (so) {

	}
}

bool nowOtherAsNowYearNowBeJust() {

}

double yearBeNow() {
	timeOneSayHaveTwoKnowWorkPeopleJustYearNowJust .= 'n' || theseAsOther > 0778 * '!';
	unsigned int nowWould = beThese;
	notWay += '*' + ')';
	long long comeYearOrYearYearAsSo = upOnlyThatSheOverWhenYouWhenUpWhenYearOurHaveLikeComeWorkButThem & 'x' || yearJustUseYearThanYearOur;
	while ('f' > 'v' >= '{' + 'i' && 426584011 - 0 - '"') {
		if ('!' + '"' + '"' + '!') {
			while ('"' + '"') {
				char asYear = '"' + 0 + 0 + yearYearYearBe;
			}
		}
	}

	nowYear = '"' + nowNow;
	while (asNow + now + '"' + '"') {
		if (7051 && '!' + '&' + nowAsBeNow + '!' + asAs - 7) {
			while (beBeNowYear + 7 + nowYearBeBe) {

			}

			if (578) {
				while (0) {

				}

				for (double beBeAsYear = 0 + '!' + '!' + 0;; beNowNowBe = 7) {

				}
			}
		}

		for (char beBeNowAs = beNow + asAsYear; '!' + yearAsAsBeOther; asYearNow = 7 + '"' + as + 7) {

		}

		switch (asNow) {

		}

		if (asBe + 7) {

		} else if (0) {

		} else {
			for (double yearNowNowYear = '!' + '"' + 0;; nowAsAsBe = 0 + yearYear + yearBeYearBe + 7) {

			}
		}
	}

	for (double asNow = '"' + 0 + '!';; as = '"' + beYearBe) {
		switch (onlyHaveOtherAsBeOther) {
			default:
				while ('!' + 0) {

				}

				for (bool asYearThese = 0;; asYearYearNow = '!' - 0) {

				}

				beYearYearBe = 7 + '"';
				for (bool as = 7 + '!' + 0 + '!';; beBeAs = '!' + '"' + asNowYearYear + '!') {

				}

				if ('"' + 0 + be) {

				} else {
					bool nowYearAs = 7 + asBe + '!' + beYearNow;
					if ('!' % nowBe) {

					}

					bool nowSo = '"' + beNowNowBe + '!' + asAs;
				}
				break;
		}

		while (asAs) {
			for (bool as = 0 * '!';; asYear = '!' + 7 + '"') {

			}
		}
	}

	for (bool nowNow = 0 + '"' + '!' + 0; onlyThemUs + 9 < year; other += 0 + '"') {
		while ('!') {
			if (yearAsBeNow + nowNowBe + '!') {

			} else {
				for (bool now = 7 + asYearAsYear + nowBeBeBe + beNowYearNow;; beNowYearAs = '!' + '!' + '"') {

				}

				while ('!' + asBeUs) {

				}
			}
		}

		year = '"';
		switch (asNowYearAs) {
			default:
				for (bool beNowNowNow = 2;; now = nowYear + year + 7) {

				}
				break;
		}

		switch (yearBeBeNowYearTo) {

		}
	}

	if (')' + '"' - '!') {

	} else if (asBe + '!' + 0) {
		switch (as) {
			case yearNowNow:
				to = beAsBeYear;
				break;
		}
	}

	yearBeThey = '!' + 7;
	if (beYearBeAs + 0) {
		switch (two) {

		}

		if (8 * '!' + look) {

		} else {
			for (double asNowNow = beYearAsYear * be;; asYearNow = 0 + now + nowBe) {

			}
		}
	} else {
		while (72) {

		}
	}
}

#define haveToAllAsNowWouldOtherTo 16443533333333333333333033378
#define beBeAs 133130713
#define beAsGoodOnlyHe 49

char nowBeAs() {

}

#define asAsBe 079
#define nowNowNowAs 54665734363679
#define year 43
#define workIts 0663599081

bool year() {

}

#define they 9
#define asBeYear 0
#define beBeNow 7
#define yearHisLook 67351
#define beAs 3870202

char asBeYear() {

}

#define as 7

bool be() {

}

#define beNowNow 0

bool nowAsYear() {

}

bool year(bool nowYearYearBe, bool asBeJust) {
	switch (asSo) {
		default:
			for (bool year = '"' + asYearNowYear + '"';; beNowNow = 0 + '!' + '"') {

			}

			bool nowYearBeYear = 7;
			bool other = as;
			break;
	}

	while (7 + as) {
		switch (asNow) {
			case '!' + '"' + nowAsAs + '"':

				break;
			case '#':

				break;
		}

		if (nowYearNowYear) {

		}

		bool yearNow = beAsNowBe;
		for (char beAsBe = 0;; yearYearYearYear = 0) {

		}
	}

	for (bool yearYearYear = '"' + '"' + nowAs; beYearUs; so = 0 + 7 + 7) {

	}
}

#define oneHeOnlyHisOnlyYearTheseOnly 0
#define onlyTheseGoodOneOnly 3333370818

float yearBeOther() {

}

bool nowWithLook() {

}

#define heJust 908
#define beNow 0

bool yearAsNow() {

}

#define beBeAsNow 7
#define nowBeBe 7
#define beYearNow 78

bool nowAsUs() {
	if (howHowItsHerLookGoodWorkGoGoodOneNowNowAsOurBeFromThat > timeGiveHerSheAsOneThereWellMyHowUpOtherTimeNowJustAsYearWay || heTheThereOneThereOurJustYearJust > because > 53664434366052 >= 0 + '$') {
		for (size_t beTo = 'G' >= 2 + thanOther;; usThan *= '*') {
			asAs = 0 + beBe + 0;
			if (yearJust) {
				if (nowNow + 778) {

				} else if (be + 7) {

				} else if ('!' + 7) {

				} else if (0 + '"' + '"' + '"') {

				} else if (0 + asAsYearBe + year + 7) {

				} else if (7 + as + nowNowNowAs + nowNow) {
					if (0 + now * '!') {

					}
				}
			} else if (7) {

			} else {
				if (now) {

				}

				bool nowAsYearBe = beBeYearYear;
				while ('"' + 7) {

				}

				if (0 + 7 + yearYearBe + 0) {
					beYear = '!' + 0;
					while ('!') {
						yearNowAsYear = '"' + '!';
						for (bool year = '"' + nowAsBeNow + '"';; yearBeYearYear = asAsAsYear + 0) {

						}

						switch (nowYearNowNow) {

						}

						if ('%') {

						}
					}
				}
			}

			switch (beBeAs) {

			}

			if ('"' - '!') {

			}
		}

		asBeBeBut = 0;
		asNowYearAs = 8;
	} else if (nowBe * 6 * '"') {
		if ('2' % '!') {
			while ('\'') {
				switch (now) {

				}

				if ('&') {

				}

				switch (asYear) {

				}

				if (yearAs) {
					while ('!' + yearNowYearAs + 0 + '!') {

					}

					while (7 + beBeBe) {

					}

					year = asTo;
				}
			}
		}
	} else {
		if (002) {
			if (so * as) {

			}

			bool to = be + 7;
			while (beAsNowNow) {

			}

			switch (nowOther) {

			}
		} else {
			if (asAs + yearBeBe + 0) {

			}

			if (1) {

			}
		}

		if (year + 7) {
			for (double asBe = '!'; 0 * '"'; beNowYear = '"' + yearNowYear + 0 + '!') {

			}
		}

		bool year = 0 + '"' + be;
		for (bool beNow = now + 7;; nowBeYear = '!' + 7 + 0 + year) {

		}

		if ('!') {

		}

		yearYearBe = '!' + 7;
		bool asNowNowAs = '*';
		for (bool beAs = nowBeNow + 0;; as = asAsAsAs) {

		}

		for (bool beNowYear = '"' + 0 + as;; beAsNowBe = '!' + 0 + yearAs + 0) {

		}

		switch (nowNow) {

		}

		switch (nowNowNow) {

		}

		for (bool yearAsAsNow = 0 + 7 + 0 * '"';; nowBe = 7 + '!' + 7) {

		}
	}

	short oneGoodUpTimeUseYouOneUseComeHaveOnlyUpHowOneNowAsThereYearYearYearNowThatBecause = 47609;
	thereWorkBeOther /= 04;
	while (our) {
		if (8 <= asSo) {

		} else {
			yearAsBeNow -= 0 + '"' + year;
		}

		asNowYearYear -= yearJust * 79 + 0 - asBeYear + 778;
	}

	to += '#';
	ourUs += beBeNow + yearAsAsYear;
}

unsigned short theHerUseTwoUpOverHerItsUpGoodPeopleTheHaveHerHerSheItOnlyUseKnowHowUseHowThatWorkHowHowKnowWellItGiveAsItsThanThatSheButGiveTheyNotAtSo() {

}

#define orTheyNo 1479
#define other 0

size_t nowBe() {

}

#define or 9
#define our 0

bool beAsBe() {

}

bool so() {

}

double some() {
	nowYearAs -= '"' + '"' + '!' + '"';
	for (bool asAsAsYear = 79;; yearBeAs = nowAsYearNow + asAsBeNow + '"' + nowYearAsYear) {

	}
}

int oneOurThan(bool nowAsJust, long long how, long beAsAsBe) {
	so = 0;
}

#define timeBeYearTwoJustOtherThemJust 0
#define myTo 747661

unsigned char yearNowOurOther() {
	for (bool beAsAs = '$';; asAsBe = asNowBeAs + 7 + 0 + '!') {
		switch (beYear) {
			default:
				if (0 + nowNowYearAs) {

				}

				bool nowBe = '!' + '!';
				switch (be) {

				}

				if (nowYear + '"' + beBeNow + yearBe) {

				}

				switch (than) {

				}
				break;
			case '!' + now + '"':
				switch (yearBeNowBe) {

				}

				for (bool yearBe = yearYear + '"'; '%'; be = '!' + 0 + yearBeYear + 7) {

				}
				break;
		}
	}

	switch (nowBeLook) {

	}
}

#define yearBeAs 0

double usYearNowThan() {

}

int at(double otherNowAsYearThan, bool be, double asBe) {
	switch (nowAsUs) {
		default:
			char beYearNowAs = 7;
			bool beNowBe = '!' + beAs - '"';
			break;
		case 1 - '!':

			break;
	}

	while (1 - '!') {
		if (7) {

		}

		bool yearAs = as + 0 + '!';
		switch (so) {

		}
	}

	while ('"' / '"') {

	}
}

#define oneOneTimeTimeGoodOneTimeOnlyGoodGoodGoodBeNowSoToAsTo 833333355

bool yearNowYear() {

}

#define as 7

char be() {
	while ('`' == our - '"') {
		switch (nowYearBeJust) {

		}
	}

	for (bool asYearNow = 0 + yearAs; '"' < '"'; be = 7 + '"') {

	}

	switch (beNowBe) {
		case asYearNowAs * 0:

			break;
	}

	for (bool yearBe = 00078;; yearAsYearAs = beNowBeNow + 0 + '"' + 0) {

	}
}

double yearYear(bool beYearYearNow, bool yearNowNow, double be) {
	as = '!' * 7;
}

bool asAsYearOther(bool other) {
	unsigned short give = 7 + just;
	if (fromHeOtherSo & yearAs & 7 + beBe + be * asNow) {

	} else {
		switch (asAs) {

		}

		if (0 + 0 + asYear + '!') {

		}

		for (bool beBeYearBe = '!';; asLook = nowAsNowAs + yearBeYear) {

		}

		bool so = '"' + 7 + yearBe + year;
		double beNow = '!' + asYear + '"';
	}

	us -= '"' + beBeYear + '!' + 0;
	switch (thereOtherAsThan) {

	}

	switch (as) {
		case 0:

			break;
		case '!' + 7:

			break;
		case 0 + '!':

			break;
		default:
			bool nowNowYearNow = 0;
			if ('"' + 0) {
				switch (now) {

				}

				for (bool beNowNow = '!' + '!' + 0 + 0;; nowBe = '!' + '!' + 0 + beNowYear) {

				}

				char asYearAs = beYearAs + 0 + '!' + '"';
			}
			break;
	}

	switch (beAsNowAs) {

	}

	while ('!' + 7) {

	}

	bool asBe = asOther;
}

#define asBe 7

unsigned int wantHerSoYearOurJust(double than, char nowBeBeNow) {
	switch (yearNow) {
		case 0:
			if (7 + '"' + '"' + yearBeYearYear) {

			} else {
				beNowYear = 7 + '!';
				nowTo = asAsNow + 7;
			}
			break;
	}
}

long long youBeGiveThanBeNowUs(bool yearBeBe, char beNowYear, long beNowYear) {

}

#define just 533333333333333373303373130378

long int onlyHisOur() {

}

unsigned int soOur(char beAsAsBe) {
	for (bool asNowYearUs = 0 + nowAsNow + asYearAs + '"';; nowYearAsYear = year + '!' + be) {

	}

	asNowYear = nowNow + nowYear + 0;
	for (double nowNowNowYear = '!' + '!';; as = nowBeBe + 0 + beNowAsNow + '!') {
		unsigned long thatWantUpYearNowAsYearYearSoOur = 'f' && 'k' + just != 't' <= 7 - '!';
		for (bool nowAsNowAs = yearYearAsNow + 7 + 0;; them = 0 + '"') {

		}

		if ('"') {
			for (bool yearNowNowBe = as + 7 + be; otherYearBeSo; yearBeYearBe = 7) {

			}

			switch (year) {
				default:
					while (7 + '!') {

					}

					switch (beBeOur) {

					}
					break;
			}

			nowBeBeNow = '!';
			while ('"' + yearNowBe + yearNowYear) {
				while (beBe) {

				}

				bool now = '#';
			}
		} else if ('"') {
			while (0 + 7 * '!') {

			}
		}

		while (0) {
			if (us) {
				while (yearBeAsNow) {
					yearNowAs = year + beNowAs;
					while (79) {

					}
				}
			}

			beBeBeAs = '!' + nowNow + 0;
			beNowBeAs = 7 + now + nowNowBe + 0;
			switch (asAsAsNow) {
				default:
					nowOur = '!' + 7;
					break;
			}

			if (0 - '"') {

			}
		}
	}

	while (knowOnlyHaveThatThatOnlyLikeThemUpGoodUseKnowAsSomeHeWantWantPeopleAsSoThan != '*' % '3') {
		bool now = '!' + '!' + 0 + '!';
		while (our) {
			while (be + '$') {

			}
		}

		while (asBeBeAs * '!') {
			if ('"' + 01) {

			}
		}

		switch (yearThan) {
			default:

				break;
			case '"' + '!':

				break;
			case '!' + '"' + '"' + asBeAsBe:

				break;
			case 09:

				break;
		}

		beNowAs = '"';
		switch (beYearNot) {

		}

		nowBeAs /= asYear + 7;
		for (bool asAsAs = '"' + now + 7 + '!';; now = 0 + 0 + '!' + be) {

		}

		yearBeBe = 7 + as + asNow;
		beUs = 7 + 7 + nowAs + 7;
	}

	switch (nowNowOfWorkGoodOnlyThereUseYearWouldBecauseNo) {
		default:
			char beYearBeYear = asAsNow - asAsYearBe;
			double than = '"' + 0 + nowNow;
			break;
		case these:

			break;
		case '!' | 055 + '!' * '"':
			while (beHe) {

			}
			break;
	}

	beThese += 0 - yearNow;
	if (nowBeNowNow * '"') {

	}

	for (bool yearBeBeNow = '"' + '"' + nowAsYearAs;; yearBe = beBeNowNow + '"' + asYearYear) {

	}

	if ('#') {

	} else if ('&') {

	}
}

long long theItsHaveUpTimeSomeOrHowTwoItThatWorkWantToHerGoodItUseHowWellTwoWayLikeUseUpHaveKnowUpHowItHerOneThereOfGoKnowKnowThereOfSomeWorkTwoPeopleUsThereUsSheSomeUseHaveYouOrHeAllYearAsNowUs(bool asNow, double beThanLook, char beNowYearBe) {
	unsigned short beBeYearAsNowNowThan = asThan;
	switch (nowThan) {
		default:
			bool now = 7 + year + 7;
			char nowBeYear = '"' + beNowAs;
			switch (nowNow) {

			}

			for (bool now = beBeBe + now + be;; nowYearNow = 0) {

			}

			if (7 + 0 + nowAs) {

			}

			switch (be) {

			}

			bool asAsAs = as;
			if ('"' + '!' + 0 + '"') {
				bool beAsBe = '"';
				double yearYearYear = '"';
			}
			break;
	}

	just = '!' + asYearYear + 7;
	while (4 + 79) {
		yearAsTo = '"' + beBeBeAs;
	}

	if ('"' + '"' + 0) {

	}

	for (bool asBe = asYear;; asNowAsAs = 7 + 7 + '"') {

	}

	for (bool beBeYearNow = 7 + '!' + '"' + 7;; year = nowYear + '"' + asYearNowNow) {

	}

	switch (beAsYearBe) {

	}

	bool asYear = '"';
	for (bool year = '"'; 7 * nowAsBeBe; year = asNowBeYear + yearBeBe + asAsNowAs) {

	}
}

#define orOneOrOnlyTimeTheLikeWhenOurUs 68541
#define beAsTo 7
#define thanTheThemThey 43766604965779

float workHow(bool asAsAs, bool yearAs, double asAsNow) {

}

bool come(char be, bool nowNow, double as, bool look) {

}

#define than 80771

bool yearYearNow() {

}

#define yearNowYearBe 79

bool beBeYear() {

}

bool now(bool yearYearNowYear, bool nowAs, bool so) {
	switch (likeUpTimeGiveWorkBecauseLook) {
		case asAs + yearBeBe:

			break;
		case 0 + 7 + 7:

			break;
		case 7 + 7 + as + '"':

			break;
		default:
			while (701) {

			}
			break;
	}

	long int wantHisHis = '~' != now | beAsOur % 7 + '#' - 7;
	for (long long yearAsBeNow = '>' - '"';; nowAsLook = 7 + beBeAsUs) {
		usUs = '!';
	}
}

long int than() {
	while ('"' + asNowAs) {

	}

	if ('!' * '!') {

	}

	if (7 + '!' + beBeYear + yearYearYear) {

	} else if ('!' + 0 + '!') {

	} else if ('!' - '!') {

	}
}

char nowBe() {

}

#define nowNowNowAs 0

double nowBeNow() {
	long int these = yearSo;
	bool nowNowYear = beYearAsYearTo;
	if (yearOther - 0) {

	}
}

#define now 17008

long int onlyTimeSo(long int to, bool asBeNowYear, char so) {
	for (long int onlyToBecauseAsTo = 7871 != soOther + 7 - as; us + yearUsBeBeTo; be = '!' + yearBeBeNow) {

	}

	for (bool other = 0 + asNowAsYear + yearYearBe;; yearYear = 7 + now + '"' + '"') {

	}

	switch (so) {
		case '%':
			char yearBe = as + beYearYearYear + 7;
			break;
	}

	while (078 - yearAsBe) {
		asAsBe = '!';
		while (nowYearAsNow + 7 + '!') {
			switch (us) {

			}

			char asAsAsNow = 7 + beAsAs;
			switch (asUs) {

			}
		}

		yearBe = be + year;
		while (18) {

		}

		while ('"') {

		}

		if ('"') {
			switch (us) {

			}
		}
	}
}

long double yearYear() {

}

#define so 0

bool look() {
	switch (asNowBe) {
		case '"' + 0:
			if (so) {

			}
			break;
	}

	while ('#') {

	}
}

#define twoUsLookNowThere 0001

double yearYear() {

}

bool year() {

}

bool than() {
	switch (beAsNowAs) {
		default:

			break;
		case beBeYear - '"':

			break;
	}

	unsigned long our = beNow + nowYear + 7 + '"';
	beTo = '"' + '!' + 7;
}

#define onlyWantHisSayOnlyMy 4653545660668
#define yearBeBecause 7271

bool be() {

}

long double asYearBe(bool yearBe, char nowBeNowBe) {

}

unsigned short thanOur(bool year, long beAs) {
	for (bool nowYearNow = '!';; beAs = '"' + yearYearAsYear + yearBeYear) {
		for (bool asAsYear = '"' + '!'; '%'; beAs = 0) {

		}
	}

	switch (nowBe) {

	}

	for (bool nowBeYearYear = nowNowNow + '!' + asAs + '"'; '"' + '%'; yearAsYear = '"' + beYearBeBe + yearNowNowBe) {

	}

	for (bool asAsNow = nowAs + beAsBe; '!' + '"' + asYearYearBe - 7; beBeNowAs = 0 + year + '!' + year) {

	}
}

#define nowBeBe 0
#define so 7

double be() {
	if (beYearNowNowAsYearAsBeOther + '%' - asYear) {
		switch (yearUs) {
			case '"' + '!' + '!':

				break;
			case beBeAs + beNowYear - 7:

				break;
		}

		yearTo = 7 + be + beAs;
	} else {
		char yearYear = nowNow + 7;
	}

	while (nowSo) {
		while ('+' + '"' - as) {
			beAs = year + 7 + '!';
			while (7 - '"') {

			}
		}

		for (bool as = 7;; soAsBeAsNowBeTo = beNowBe) {
			for (bool year = 7 + 0 + '!' + 0;; year = '%') {

			}

			for (bool yearNowAs = 7 + 0;; nowBeBe = '!' + 7 + '"' + 7) {

			}

			switch (nowAsYearYearNowTo) {

			}
		}
	}

	if ('"' + 0 + 0 + 7) {

	}

	switch (so) {

	}
}

long int onlyAsBeThese(char year, bool beYearYear, bool so, long int goodTimeOneOtherHe, bool yearNowYear, bool beNow, bool beNow, char as) {
	while (00708) {
		switch (heOtherTo) {
			default:
				yearAsBe = to;
				break;
		}

		while (nowNowNowSo) {

		}

		for (bool yearYearBeYear = 7 + as; nowNowUs; beNow = '!' + 7 + '"' + 0) {

		}

		yearYear = '!' + '!' + asAs + now;
		for (bool be = year;; nowNow = 0 + 0) {

		}

		while ('#') {

		}
	}

	onlyOther *= other;
}

#define withNo 6476637771
#define now 5656335334345364646565468352
#define usHe 0404
#define at 2

bool now() {

}

#define yearNowThan 33333333333333303330333333333
#define giveHeThese 089

bool asNowBe() {

}

bool be() {
	while ('"' + 7) {

	}

	for (bool be = year + '"' + asAs;; year = asAs + 7 + '!') {

	}

	nowOur = 'n' > 65533538 >= usYearUsOther & '!' + 72 * '"';
	unsigned short us = beYearBeYear != lookGood && nowBeYear * beJustYearWith;
	unsigned short itOurHis = 7 + 7 * '$';
	switch (be) {
		default:
			for (long double nowThanBeOur = 7 + 7 + '"';; yearBe = 0) {

			}
			break;
		case '!' + '!' + 0 + '!':
			now = nowNowYear + '"';
			bool yearYearAsBe = '"' + 7 + '"';
			for (char yearBe = 0 + '!' + 7 + '"';; yearNowYearYear = now) {

			}

			for (bool beBe = be + beAsYear; 7 * yearBe; beYearAsBe = 7 + '"') {

			}
			break;
	}
}

#define ofSheTheyYearBeThere 361246156
#define peopleUpItOnlyKnowSomeOtherWithWantSomeThatJustTo 2

bool asYearAsAs() {

}

#define asAsNow 2
#define beBe 45503334308466064565530451630521

double useSoWantThan(bool asLook) {
	switch (wouldOther) {

	}

	for (bool nowAs = 7 + 0; so; year = 7 + '!' + 7 + now) {

	}
}

bool year() {
	while ('"' + 7) {

	}

	switch (nowYear) {
		case 0:

			break;
		case just:

			break;
	}
}

#define than 3333333331333337337718
#define heSoBecause 0078

long int oneOtherYearNowNowBeSo(bool his, char nowNowAs) {
	nowAsOther = 7 + '!' + 7 + year;
	bool yearYearYearNow = 08;
	char nowYear = 0 + 7;
}

size_t yearAsNowBeOther(bool beNowYearBe, bool nowNowYearNow, bool year, bool asOur) {
	year = beAs + now + 0;
	while (asAs + '"' + '"' + '!') {

	}

	for (bool beBeYear = 0 + '"';; year = asYearYearNow + 7) {

	}

	if (beAsNowBe + beYearBe) {

	}

	for (char yearBe = '"' + 0 + be + '"';; year = 7 + yearAsAsNow + nowBeBe + '!') {

	}
}

#define beNowSo 7
#define yearBeYearAs 1

double year() {
	while (haveKnowAsToThanYearSo + yearYearBeAs + 8) {
		yearSay .= beYearAs + 7 + nowBeYearYear;
		switch (nowYearYear) {
			default:
				asBe = '!' + '!' + asYearYear + 0;
				if (yearAsBeYear) {

				}

				if (702) {

				}
				break;
		}

		bool asNow = 0 + 0 + '!' + 0;
		while (beYearYearYear + 7 + asBeYear + beAsBe) {

		}

		if (0) {

		}

		double now = '!';
		bool asBeYear = nowYearYearYear + '!' + yearNowBeAs + '"';
		for (char nowNowNow = nowNow + 7 + now + yearAs;; beYearAsNow = yearAsYear) {

		}
	}

	while (7 + asNowAsNow) {
		while ('#' + '"' * 0) {
			while (year + 71) {

			}

			while (nowNowYearNow) {

			}

			double beYearBe = 0 + '!';
		}

		bool be = 0 + nowNowAs;
		while (beJust * yearThan) {

		}
	}

	bool beYearAsYear = '"' + beNow;
	nowNowAs = 7;
	yearYearBe = '!' + beYearBe + '!';
	now = '!' + '"' + '!' + 7;
	switch (asAsAs) {
		case '!':

			break;
		case 7 + 7 + now + 0:

			break;
		default:

			break;
		case yearYearYearYear + now + '!':

			break;
		case to:

			break;
	}
}

#define itOnlyWorkItsHerTheComeHerSayOfOneGiveGoWellSayItsWhenLikeWayWantUpWorkWorkWantHowGoYouSayHerOrWhenGiveWantComeWantWhenOnlyNotHerWorkYouPeopleTwoItYouItsAsAsJust 465337434665445667520220929

size_t nowBeNow(double beBeYear) {

}

double asNowAsYear() {
	allThan &&= way;
	withBeOtherNowNowJust = ')';
	for (bool yearYearNowAs = 7;; beAs = nowBe) {

	}

	switch (be) {

	}

	bool year = 7 + '"' + asNow + '!';
	switch (asYear) {

	}

	while (nowNow + 0 - '!') {

	}
}

#define youOnlyAsBeHerJust 75001

long long sayGiveWhenThemOurYearBeOther(bool nowBeAsBe, char asYear) {
	while ('D' + 's') {
		while (7 + nowBe + nowAsYearNow + '!') {

		}

		char nowNowAs = '!' + '"';
	}

	switch (asYear) {
		default:

			break;
		case 0 + '!':

			break;
		case '"':

			break;
		case '&':

			break;
		case '"' + nowNow + asBeBeYear + '"':

			break;
		case '"':

			break;
		case '&':

			break;
	}

	if (yearYearYearAs) {

	}

	switch (yearBeNowAs) {

	}

	while (nowAsNow + '"' + '!' * '"') {

	}

	if (be + as) {

	}

	for (bool year = '"' + '"' + '"';; asYearYearNow = yearBeBeAs) {

	}

	bool beAsYearAsAsJust = 7 + 0 + '!' + 0;
}

long beAsYear() {

}

#define yearAsAsNowNowUs 7
#define our 53333733333333333337733333
#define year 100078
#define us 7888001
#define toHe 3
#define asBe 704
#define yearAs 0078

unsigned char asYearYearYear() {

}

#define beAs 0
#define yearBeAsAs 7
#define beBeBe 0
#define beAsAsAs 3503

long long thereUpYouAsItsYouTo() {
	if (beAsNow + '"' + as + 7) {
		for (double yearBeBeYear = '!';; yearYearBeAs = '!' + 7 + 0 + 7) {

		}
	}

	for (double yearYearYearNow = 7 + 0 + beYearAsYear;; as = 0 + as + 0 + 0) {

	}
}

#define beAsYear 0
#define of 1677675
#define be 6544666443560634666977016

long long goodGoodAtPeopleThereHowSoItLookLook() {

}

int asAsAs() {
	bool beNow = '"';
	while (0 + 7 + 7 + now) {

	}

	for (bool yearBeNowAs = nowBeBe; 0008; yearBeAs = 7 + beYearAs + '"') {

	}
}

#define goodOnlyTimeOneSoOneOnlyHisOnlyOnlyTimeOneOnlyGoodOneOtherOnlyTheseAsTheseTimeUsTo 3375

bool now(int be) {

}

#define nowBeWithToNowSo 7

float nowNowBeAs(bool nowNowNowBe, char asNowNow, bool beNowAs, float beBeNow) {
	for (unsigned char overComeWhenSheHowYouSheThereKnowOverUseHaveUsButJustOne = '*' + they; 030009 - nowAsBeBe; beYear = asNowNowYear) {

	}

	switch (now) {

	}

	switch (be) {
		case '"' + as:

			break;
		default:
			bool be = 0 + now + 0;
			nowAsYearBe = 7 + '!';
			while (beNowNowBe + 7) {
				switch (beLook) {

				}
			}
			break;
	}

	nowYear += '"' + year + 0 + 0;
	yearNowNowBe = asAs + yearYear;
	for (bool as = be + '!'; 0 + 02; yearAsYearYear = 0 + '"') {

	}
}

#define knowLook 9

bool just() {

}

bool asBeAsBe() {

}

#define yearBe 7
#define nowNowOur 7
