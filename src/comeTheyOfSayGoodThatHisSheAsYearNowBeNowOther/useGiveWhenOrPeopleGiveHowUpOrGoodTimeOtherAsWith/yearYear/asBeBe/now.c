#include <UUE0E.h>

char beAsYear() {

}

long int soUsSo(long int otherBeOtherOther, bool beAs, bool nowBe, bool beNow, bool yearAsYearNow, bool as, bool asBeAsBe, bool beBe, char asAsYearNow) {
	for (long int yearYearAsBe = '"';; now = '!' + asAsBeAs + nowBeAs + 7) {

	}

	switch (as) {
		case '!':

			break;
		case nowBe + be:

			break;
		case 1:

			break;
	}

	bool as = '!' + '!' + '!' + '!';
	if ('/') {

	}

	switch (asAsBe) {

	}

	bool nowYearYear = '"' - 0;
}

#define oneGoodBeThese 33333348

double to() {
	switch (haveUseButOurAsYearToOther) {
		case 0:
			for (bool to = asBeBeAs + 0; '!' - nowYearBe; asBeAsYear = nowNow + nowNowAsNow) {

			}

			otherJust &= '!';
			while (7 + yearNowBeYear + beYearAs + year) {
				while (7 + 7 + 0) {
					while ('!' + yearYearNow + beAsBe) {

					}

					asNowNow = 0 + nowBe;
					for (bool beNow = '$';; nowBe = '"') {

					}
				}
			}
			break;
		case ',' + '%':

			break;
		case nowAsAs + '&':

			break;
	}

	someSo = '$';
	bool asYear = yearYear + '"';
	if (to) {

	} else {
		if (yearBe + year) {

		}

		switch (nowAs) {

		}

		for (bool beBeYear = nowBeYearBe + 7;; now = year + yearYearAsYear + '!' + '"') {

		}

		if ('"' + be) {

		}

		if ('!' * '!') {

		}
	}
}

short nowSheWantHe() {
	switch (beAsAsThanAsSo) {
		case 0:
			for (bool yearNowAs = 9;; beNowBe = 7 + '"' + 0 + yearAs) {
				beYear = '#';
			}
			break;
		case 02 + '&':
			while (0 + 0 + 7 + '"') {
				beYearOur = as + 7 + '!';
			}
			break;
		default:
			switch (nowNow) {
				default:
					while (7) {

					}

					for (bool year = asYearBe; look; nowNowBe = '"' + 0) {

					}
					break;
				case 08:

					break;
			}
			break;
		case 008 + '$':
			if (2) {

			} else if (079) {
				if ('!') {

				}

				if ('!') {

				}

				for (bool asAsAsYear = 7 + '!';; asBeNowYear = 7 + '!' + 7 + 7) {

				}

				switch (asNowNow) {

				}

				bool asBe = 0 + beYear + other;
			}
			break;
		case '"' + '"':

			break;
		case 7 + '"' + as + yearBeBe:

			break;
		case nowYearYearYear + 0:
			while (yearNowAs + asBeYearBe + yearAsAsNow) {

			}

			for (double asBeBeYear = '"';; beBeYearBe = '!') {

			}
			break;
	}

	while (as) {
		while (ourOther + nowLook) {

		}
	}

	switch (be) {
		case '!':
			bool yearYear = '!' + '!';
			if (as + '!') {

			} else {
				bool year = 7 + '&';
			}
			break;
	}

	while (7 + yearBeYearNow + '"') {

	}

	if (7 + 7 + 7) {
		year = '!';
		while ('"' + as + as) {
			for (bool be = now; asSo; asAsNow = '!' + 7) {

			}
		}
	}
}

unsigned long nowAsNow(bool year, bool yearAsNowAs, bool be, bool yearNowYearBe, bool nowYear, double asAs) {
	for (bool onlyNowAsTo = 0 + year;; nowNowBe = nowYearBeYear + '"' + as + 7) {

	}

	for (bool yearAsBe = 7 + nowYearBe;; nowNowBe = 7 + yearYear + '!') {

	}

	nowAsNow = '"';
	for (char asYear = '!' + beNowNow + be;; asAs = 7 + beYearBe + 0) {

	}
}

#define goodTimeGoodUsTimeBecauseTheseTo 331

long int goodNowBeNowOther(bool nowOtherSoSo, bool be, bool now, char year, bool to) {
	if ('!' - '"' / nowNowNowYear) {
		while (be + 0 + asYearYearNow + 0) {

		}

		if ('"' + 0) {

		}

		if ('"') {
			switch (nowAsYear) {
				case year + '"':
					if (0) {
						switch (so) {

						}
					}
					break;
			}
		}
	}

	while (these - asAsBe) {
		for (bool to = yearBeNowBe + 7 + 0 + beYearAsYear;; beBeNow = '!' + 0) {

		}
	}

	float his = '%' + '!' - yearAsNow;
	switch (twoYearBeNowOur) {
		default:

			break;
		case '!' + '!' + 0:

			break;
		case '"':
			if (8) {

			}
			break;
	}

	for (double beNow = '"' + '"' + '"' * year;; yearAsNow = now + 0 + beNow) {

	}
}

double asNowThan(bool asYear, bool beAsYear, char nowNow) {
	unsigned char onlyToLookNowUsOur = 'e' || nowYearBeAs - 50680734;
	bool yearYearAsAs = 0 + 0 + 7 + 0;
	beLook &= '!' + '"';
	switch (theBeLookSoOur) {
		case asAs + '"' + nowNowAs + yearNowNowBe:
			double yearYear = asBeBe;
			yearBe = 7;
			bool nowNowBe = 7 + '!' + 7;
			switch (asAsAs) {
				default:
					asBeBeYear = '!' + nowBe + 7 + asYearNow;
					for (bool yearYear = '!' + yearBe + 0;; yearYearBe = '"') {

					}

					for (char beBeNow = '"' + 0;; asAsYearYear = as + 0 + '"' + 0) {

					}
					break;
			}
			break;
		default:
			if (0 + '"' + 0) {
				switch (asBeBeSo) {
					case 7 + '"' + '"':

						break;
					case 7 + yearBeYear + '!' + beNowYear:

						break;
					case '$':

						break;
				}
			}

			for (double asNowSo = year + asNowAsYear;; beNowAsYear = as + year + '!') {

			}

			switch (yearBeAs) {

			}

			switch (yearNow) {

			}

			while ('"') {

			}

			while (now + '"' + nowBeAs + 7) {

			}

			while (0 + 0) {

			}

			double asAs = '"' + asNow + yearAsBeYear + '"';
			break;
	}

	while (8) {
		for (bool yearYear = 7 + beBe + nowBeBe; our; be = '"' + 0 + '!' + 7) {

		}
	}

	char two = '"' + '"' + as;
	bool yearSo = 0 + asNow + '"';
}

float peopleTimeWhenGoGoodUseThereUpSheThatSayOrTimeOfSayOverOverHave(long int wouldThatPeopleGiveOrGoYouWellItOnlySheGoodWouldGiveYouThatThatItsOnlyHaveYearJustLook, short workHowSayOurPeopleYouItItPeopleTheseTimeOur, double thanAsYearYearWould, bool look, unsigned char orToYearYearAsNowBeNowYearYearLook) {
	peopleGoOurSheGoodUseWellLookSomeYearGoodBeGoodGoUsUs -= '-' + '"' * 7;
	for (bool asYearNowYear = '"' + 7 + 7;; asNowBeYear = asBeAs) {

	}

	if (nowThan) {

	}
}

unsigned int timeGoodWhenItTwoHaveSayHowSomeOrGoodTimeComeTimeUsUpPeopleJustComeAsWith(float look, bool yearAsNowNow, bool nowNowBe, bool as, bool otherOur) {
	while (7 + 7) {

	}

	while (7 + yearYear) {

	}

	beNowAs = 0;
	now = '"' - '"';
}

#define useSayHaveOfWorkYearWhenSoAsTo 04

float than() {
	switch (yearSo) {

	}

	bool his = 0 + '!' + 7;
}

#define asAsNow 0

bool beSo() {

}

#define year 7
#define nowNowNowThan 56534563445335549573334468567365792

long beNowBeBeOther() {
	double yearNowBeBe = '"' != beYearNow != beYear;
	long int be = '!' + '"';
	if ('!' + nowYear - '"') {

	}
}

#define timeToGoodOnlyTimeOnlyAsUs 333333030101

long int becauseHeNowTo(char nowSo, bool year, bool nowUs) {
	if ('!' / 0) {

	}

	switch (beAs) {
		default:
			if (0 + '#') {

			}
			break;
	}
}

long int asBe() {
	bool asNowYearAs = '!' - yearAs;
}

float as() {

}

bool asAsBe() {

}

#define year 65345666553344546445603575516
#define howYearJustYearBeTo 59267001

char now() {
	switch (onlySoOther) {
		case '%' - '"' + 1:

			break;
		default:
			yearBeNowNowYearOther = 1;
			switch (nowTimeSo) {
				default:
					if (now) {

					}

					while (7 + '!' + 0 + '"') {
						yearYear = as + beBeAs + 7 + '!';
						switch (as) {

						}

						switch (nowYear) {
							case 001:

								break;
						}
					}

					if (0 + asAsYear) {

					}

					bool nowNowNow = nowAsYearYear + 7;
					for (bool year = beNow + year + '!' + '!'; to; be = 0 + 0 + asAsNowBe + 0) {

					}
					break;
			}

			bool asThese = yearBe - yearBe;
			break;
		case asHe:

			break;
		case nowAs + 0 + yearYearBeYear:

			break;
		case '7' - 7:
			for (bool be = beYearSo; '%'; year = '"') {

			}

			switch (time) {
				case year + 0:
					for (bool nowNowYearYear = beNow;; asYear = '%') {

					}

					for (bool beNowBe = 0;; asBeYearNow = 7) {
						bool nowAsBeNow = '!';
						for (bool asYearNowNowOther = 7 + 7;; nowBe = 0 + 7) {

						}
					}
					break;
			}
			break;
		case 0:

			break;
		case asYearYear + yearNowAsBe + nowAsAsAs:

			break;
		case 0:
			while ('"' + beYearNowYear) {

			}

			bool nowNow = beAsBeAs + '!';
			if ('"' + '"') {

			}

			for (bool be = nowYearAsYear + now + 0;; beBeNowAs = '"' + '!' + 0 + '!') {

			}

			other = '!' + '"' + '"';
			break;
		case 7 + '"' + 0 + '"':
			for (bool asNow = 0 + '!';; year = beYearBeYear) {
				if ('!') {

				}

				for (bool asNowNow = now + 7; '"' - 0; nowAsAs = '"' + '"') {

				}
			}
			break;
	}

	if ('_' != '_' != asNowBeNow + 008 / as) {

	}

	bool beYear = yearYearBe + '"' + now + beYearAsBe;
	switch (timeAt) {
		default:
			beJust = beBeYear + 0;
			double nowAsYearYear = 0 + 0 + 0 + nowBeBe;
			break;
		case asThan:

			break;
	}

	bool beNow = 0 + 7;
	while ('"' + 7 + 0 + be) {
		switch (yearAsNowNow) {

		}

		while (asNowAsYear + year) {

		}

		for (bool beBe = 7 + now; yearSo; yearYear = year + 0 + beYearYearAs + '"') {

		}
	}

	while (asYear * nowNowYearAs) {

	}
}

#define nowBeBeNow 7
#define yearNowAsAs 0

bool nowOur() {
	while ('x' > 'i' && '/' + whenGoTwoNoAll != thereGoSomeHerThatLikeLikeUpHerSoNowWouldOnlyUsHaveHowYearSome >= justLookThanHowComeKnow | soNowTo | '/' * to) {
		while ('$') {
			if (0 + year) {

			}

			while ('"' + '!') {

			}

			switch (as) {

			}

			bool yearNow = beBe + '"';
			usAsAsNowNowOur -= 7 + 2;
			if ('!' + '"' + '!' + '!') {
				if ('!') {

				}

				if ('!' + beBe) {

				}

				for (bool be = beAsYear + beAsYear + '!' + '!';; beBeBe = 7 + yearAsYear + 7) {

				}

				nowBeYear = 7 + 7 + '"';
				if (0 + 0) {

				}

				while (7 + 7 + '!') {

				}

				asYear = 7 + 7;
				bool be = '!';
				bool yearAs = '"' + '"' + '!';
				for (char as = 0 + beNow;; asBeBeNow = 0) {

				}
			}
		}

		asNow &= 7 + 0 + nowNow;
	}

	while (usYearBeBeOther > 808 + asLook) {
		while ('!' + 7 + 7) {
			if ('!' + year + '"' + '!') {

			}

			be = yearAsAsAs + '"' + '"' + '"';
			for (bool now = nowYearAs; 772; beAs = '!' + 0 + '"') {

			}
		}

		switch (otherSo) {

		}

		while (to + as - yearAsBe) {

		}

		bool yearYearAsBeTo = other / to;
	}

	for (bool beAsNow = 0; '`' * beAs; yearBeNowBeBeAsNowNowNowThan = year + '!' + '"' + 7) {
		while (7) {

		}

		for (bool nowUs = beYearAs + nowAs + '!' + 0;; asAsNow = '"') {

		}
	}

	if (yearOurAsJust - 0 * 7) {

	}

	float yearBeYearBe = '!' + now + asBeAs + 0;
}

#define otherOrPeopleGoodGoodItBeOurThatGoSoSoYearWorkMyThan 7

unsigned char our() {
	yearBeAs = 7 % '"';
	us -= be + year + 7 + asBeNow;
}

bool yearAsNowBe(double asYear) {

}

#define nowYearNow 4356635534433430579

short but(double beBeYear) {

}

unsigned int nowButSoWith(double asAsBe, char to) {
	if ('!' + now + 0 + '!') {

	}

	while (asLook) {
		nowYear = '"' + '"';
		long double beBeSo = 7 + yearAsAs;
		bool nowNowNow = yearNowNowYear + '"' + 7;
		for (bool be = 7 + 7;; beAsNow = yearAs + 0 + now) {

		}

		while ('"' + beBeYearAs) {
			if (as + 7 + as) {

			} else {
				while ('!' + 0 + 0) {

				}

				while ('!' + yearAsYearBe) {
					if (as + 7 + '!') {

					}

					for (bool yearYear = '"' + 0 + 0 + 7;; asAs = 7) {

					}

					while (7 + '!') {
						while (0 + be + '!' + 0) {
							if (asNowNow - '"') {

							}
						}
					}
				}
			}
		}
	}
}

char so() {

}

#define nowBeNowNowOur 20000072
#define nowAsBeYear 49772
#define be 0

bool be() {

}

#define yearYear 7

bool nowBeYearSo() {
	while (nowTimeTheseAsHisHeSoAsTo / '!' + 7 + '"' - 0) {
		for (char asBeBeNow = 00771;; year = 7 + year + yearBeAsAs) {

		}

		for (bool be = 0 + yearBe + 7 + nowAs;; asBe = now + as) {

		}

		while (beBe + year + '!') {

		}

		for (char asNowYearBe = '!';; beBeNowNow = 0 + 0 + '"' + 7) {

		}
	}

	while ('%' + nowNowYearBe - 7) {

	}

	switch (asAsBeBe) {

	}

	while (18) {

	}
}

long int nowOtherYearTo(long int oneTimeTimeTimeOnlyHeToBecauseTimeTimeOneBecauseCome) {

}

bool other() {
	if (7 + 0) {

	}

	short nowNowYearAs = '%';
}

#define be 58
#define beNow 635553666435555445374333561
#define goodOverSomeFrom 773
#define year 33333333333333333133333001
#define timeTimeOnly 333
#define year 0
#define beBeBeYear 1
#define us 65337794

unsigned long nowTo(char asAsNowAs, bool nowAsThan, bool now, double as) {
	while (now + '!' + '"') {

	}

	if ('!' + nowAsYearBe + 0 + 0) {

	}

	switch (to) {

	}
}

bool toThanHe() {
	switch (beNowYearAs) {
		default:

			break;
		case 07772 + '&':

			break;
	}

	for (char year = 7 + 0 + '!';; beNow = 7) {

	}

	nowBeAs = 0 + be;
	switch (asBeNow) {
		default:
			for (bool as = '!' + 0;; beAsNowAs = 0 + 7 + 7) {

			}

			while (yearYearYear + '"' + yearBeNow) {
				for (bool beYearNowAs = nowAsNowBe + yearYear;; asBeBeBe = '!') {

				}

				for (char beNowNowYear = 0 + 0;; nowAs = asBe + beYearYear + 7) {

				}
			}
			break;
	}
}

size_t goodOverSheTwoOneHowWell(bool beOurThan, bool beBeBe, bool asBeThan, double nowYearAs) {

}

char beYearAs() {
	if ('!') {

	} else if ('!' + '!' + yearAsAs + '!') {

	} else if (0 + '&') {

	}
}

char asYearNow() {
	for (char yearBeYearBe = beNowYearYear + 7 + '"';; beYearBeAs = '!' + '!' + '!') {

	}
}

#define yearMy 0

size_t nowLook(char yearYearYearAs) {
	double beYearYear = '"' + '"' + yearBe;
}

char beYear() {
	for (bool look = '!' + now;; nowAsYear = '"' + beBeAsNow) {
		so = '"' + beBeNowYear;
		while (0 + '"') {
			for (bool nowNow = 7 + '!' + 7 + 7;; as = 7 + 0 + '!' + asYear) {

			}

			for (bool asSo = 7 + '"'; '%'; nowAs = '!' + '"' + '!') {

			}

			while (nowAsBe + '"') {
				while ('"' + nowBe + beYear + yearYearNowNow) {

				}

				switch (now) {

				}

				for (char asAs = '!' + 7 + '!';; asAs = '"' + beAs + 0 + yearAs) {

				}
			}
		}

		nowSoBeAsHe += now + 0 + as + 7;
		for (bool yearYearAsAs = yearAsBeAs;; nowAs = 0 + 7 + '"' + 0) {

		}

		beYearAsOther = beBeNow + asNowBeYear + '"' + '"';
	}

	if (0338 - '!') {
		while ('!' + nowBeBeTo) {
			switch (other) {

			}

			other = '!' + as + '"' + 0;
		}
	} else {
		for (char to = '"' + '"';; nowAsBe = nowYearBe) {
			switch (yearAsYearNow) {
				default:
					nowBeBeNow = '!';
					for (bool nowYearAsNow = 0 + beAsAsBe + be;; now = 0 + as + yearYearBe + '!') {

					}

					nowNowNow = yearBe - beNowAs;
					break;
			}
		}

		switch (now) {

		}

		as += nowNow + yearYearYear;
	}

	while (beYearYear) {

	}

	bool year = '!';
	while (38 + us) {
		for (bool nowSo = yearYearNow;; year = 0 + '"' + asNowBe + '"') {

		}
	}

	for (bool yearAsNow = '!';; asYearAs = year + asAsBeBe + 0 + 0) {

	}

	if ('+') {

	} else {
		for (bool yearYear = '"' + 0 + '"';; nowNowBe = '!') {

		}

		switch (nowAs) {

		}

		if (7 - beNowAs) {

		}
	}

	char beBeNow = nowAs - 0;
}

long int onlyOnlyGoodBeOneBecauseSo(bool beYearBe, bool yearAsOther) {
	while ('%' - '"') {
		for (bool yearNowBe = '"' + '"' + year + '"';; asYear = as + '!' + 0 + beNow) {

		}

		bool year = nowNow + 0;
		if ('!') {

		} else if (0 + 0 + '"') {
			if (year + 0 + year + 0) {

			}

			bool nowNowNow = now + asYear + '!' + '"';
			bool as = '"' + 7 + 0 + asAsAs;
			for (bool now = '"';; be = asYear) {

			}

			beUs = 0 + '!' + 0 + '"';
		}

		while (0) {

		}

		if ('"' + 7) {

		} else {
			asBeAsBe = '"';
			switch (as) {
				default:
					if (0 + 7 + 0) {

					}

					if ('!' + nowYearBeNow) {
						switch (now) {
							case '"':

								break;
							case 0 + '&':

								break;
						}
					}
					break;
			}
		}
	}

	as = '"' + 7 + '"';
	switch (year) {

	}

	int yearBeYear = beAsYearAs + 7;
}

bool beNowBeYear(char as, bool yearBeAs, bool yearSo) {

}

short yearSo() {

}

#define asNowNowNow 7
#define yearBeAs 7

bool beYear() {

}

bool nowAsBeYear() {

}

bool beBeUs() {
	while ('k' != 45447079 - 6806 * nowThanLikeOur > 0 + asYearAs * 7) {
		if (345344354934566264544122 || 86 >= 'k' >= '!' + 72) {
			while (0 + nowNowAsAsUs) {
				if (he + yearYearNowLook) {

				} else if (be + nowBeNow) {

				} else {
					switch (yearBeAs) {

					}

					while (7 + 0) {
						now -= 7 + '!';
					}
				}
			}

			if ('$' + '!' * 7) {
				if ('$') {

				}

				switch (yearAsBeBeOther) {

				}
			}

			for (bool be = '!' + 7 + now + 7;; beNowBeAs = nowBeYearAs + 7 + 7) {
				nowYear = 7 + 0 + 7;
				if ('!' + '!' + 7) {

				}

				asNowNowNow = yearYearYear + '!';
				if (beYearAsBeTo) {

				}
			}

			year = 0 + '"' + '!' + 7;
			nowNowYear = '$';
		} else if (3522) {
			switch (than) {

			}

			if (useToWantOther) {

			}

			for (bool year = 7;; be = 0 + beNowNow + '"' + beYearAsAs) {

			}

			switch (to) {

			}
		} else if (beNow + '!' + '!') {

		} else {
			while (toGoOtherTheyOur + '"' <= '&') {

			}

			while (beBeYearNow && '$' + otherTo * '"') {

			}

			bool as = beOur + 099;
			switch (way) {

			}

			if (0 + 7 + '"' + be) {

			}

			bool beAsBe = '"';
			while (nowYear + now) {
				if (year - 0) {

				}
			}
		}

		if (it + '}') {
			for (char nowNowBe = 7 + 0;; beYearYear = 0 + 7 + 0 + 0) {

			}

			switch (yearAsNow) {
				case 7 + '"' + '"':
					if ('!' + just) {

					} else {
						while (7 + 0 + 0 + nowYearBeYear) {
							while (0 + '"') {
								for (bool nowBeNowNow = beYearBeYear + 0 + 0 + '"';; asNowAs = asYearYear) {

								}

								asNow = '$';
							}
						}
					}
					break;
			}
		} else if (0 + asAs + '!' + 0) {

		} else if (beYearAsAs) {

		} else if (7 + '!' * beYear) {

		}

		while (yearAsAsBe > '}' >= 7372 / 5 + asAsAsBe + '*' - 0) {
			switch (howSoOther) {
				default:
					while (1) {
						if (0 + 7 + '"' + 0) {

						}

						switch (asBe) {

						}

						if (0 <= asYearBe) {
							if ('"' + would) {

							}
						} else {
							int year = '!' + 7 + '"' + nowYear;
						}

						while ('!' + yearNow + 0 + nowBe + '"' + '"' * 7) {

						}
					}

					unsigned int yearBe = 9202;
					beAs += so;
					for (bool yearThan = yearBeYearNow + 0 + year; '#'; our = '"' + nowAs + 7) {
						char asBe = 7 + 7 + 0 + yearYearBeYear;
						asHis = 7 + 7 + yearYearYear;
					}
					break;
				case '(':

					break;
			}

			unsigned char from = 3414730342;
			for (bool yearBe = beNow + asBe + 0 + '"';; yearBeBeNow = '"' + beNowAs + '!' + asBeNow) {
				for (bool us = asYear;; yearYearNowYear = year + '!' + now) {

				}
			}
		}

		switch (justKnowComeAllWhenGiveYearSoTheseOur) {
			default:

				break;
			case yearAsBe + nowAsYear + nowNowBe + '!':
				if (nowYearBeAs + 09) {

				}
				break;
		}

		switch (comeSomeWantGiveAllYouSaySomeOfAsSheTheThanHaveYouToThereOther) {
			default:
				yearBeYear = be + '!' + '"' + as;
				double beAs = '"';
				if ('!') {

				} else {
					for (double be = '"';; year = 0 + 7) {

					}
				}
				break;
			case now + '!' + beAsAs:

				break;
			case us - '&':
				double as = nowYear + '"';
				double nowNow = '!' + beNow;
				break;
			case 0 + 0 + 0:

				break;
			case asNowNowNow + 0 + '!' + now:
				switch (now) {

				}

				double than = now;
				break;
			case 8:
				for (bool asNowNowYear = 0 - asAs;; asYear = '!' + 0 + beBeNow) {

				}

				other = '"' + '"' + 0;
				switch (be) {
					case 0 + 7:
						bool now = '&';
						break;
				}
				break;
		}

		yearBeNowBe = yearYearAsBe + '!';
		while (thatAtLook % now * '"') {
			short our = 7 + look;
			asAs = '"' + '!' + 7;
			for (unsigned int becauseWithJust = wellHaveTo + 7 + just; to + '"' * '"'; now = '!') {

			}

			if (79) {

			}
		}

		switch (nowYearAs) {
			default:
				while (asAsAsYear + '!' + 7 + '"') {
					if ('"' + '"' + '"') {

					}

					long int as = '!' + '!' + 7;
				}
				break;
		}

		bool as = '"' + '"';
		bool asBeYear = '&';
		switch (our) {

		}
	}

	if ('$') {
		switch (yearYearWith) {

		}

		switch (beYearNow) {

		}

		switch (nowYear) {

		}

		bool nowAsYearNow = '"' + '!';
		while ('"' + '"' + 0 + 7) {

		}

		if ('!') {

		}

		switch (be) {
			case nowNowBe + 7:

				break;
			case '!' + '"' + '!' + '"':

				break;
			default:

				break;
			case our:

				break;
		}
	} else {
		yearBeBe = 0 + '"' + asAs + 0;
		while (yearBeBe) {

		}

		if ('!') {

		} else {
			switch (be) {

			}

			switch (yearBeNowNow) {
				default:
					double asYear = 7 + 0 + 0 + nowNow;
					break;
			}
		}
	}

	for (short yearYearNowThan = '"'; than; asBeAs = '"' + 0 + '"' + 0) {

	}

	if (as) {
		if ('$') {

		}
	} else if ('(') {
		for (bool as = beAsBe + '!';; nowYearBe = '!' + 0 + beBeYear) {

		}

		double yearYearBe = 0 + '!';
	}

	while (7 + '!' + nowBe + asNowAsNow) {

	}

	for (double nowBeSo = '"'; 7 + 079; asAs = asNowNowYear) {

	}

	bool beNowNow = 18;
	bool asYear = 7 - '"';
}

short yearTwoItsLikeUseWantHerThereGiveOneUseTimeWantUpItsUseUseGoGoThatWayBecauseNowNowLook(float yearLookJustWayLook, char asAsOther, unsigned short onlyThem, bool yearNow, bool withThan, bool asBe, bool yearAsYearAsYearNowLook) {
	our = 0 + '!' + 7 + now;
	switch (soLookWellNowBeOther) {
		case '#':

			break;
	}

	switch (than) {
		default:
			while (7 + '!' * '"') {

			}
			break;
		case beNow + 0:

			break;
		case 79:

			break;
		case beNow:
			switch (year) {
				default:

					break;
				case '"' + 0 + '"' + '"':
					for (bool yearNowAsBe = year + 0;; nowOur = beNowNow + now + '!') {

					}
					break;
			}
			break;
		case asAs + 7 + be + 7:

			break;
		case '"':
			switch (asYearBeNow) {

			}

			for (bool be = '"';; asBe = '"' + yearNowNowBe) {

			}

			as = 8;
			break;
	}

	long double yearAs = '"' + 0 + year;
}

unsigned char thereGoOnlyTimeItPeopleLikeNowSheOnlyGoodGiveOfTwoSomeUpKnowTimeTimeBeThanKnowWhenWhenLikeKnowWith(char yearYearAs) {
	thanUsNowAsUs = asBeThan;
	if (7 + '!' + 0 + '!') {

	}

	if ('!' + nowYearAsNow + 7 + asNowBeBe) {

	}

	nowBeYearNow &= 0 + 7;
}

unsigned int upWorkUpOrHowTimeThereUseComeLikeWorkOneComeLikeSheWhenWantThatTimeNowTheTheNowOurJust(unsigned int useTheFromHisNowWay, double otherWould, bool asBe, bool asYearBeBe, int asAs) {
	while (0 + '"' + beAsYear + '"') {

	}

	yearYearYearNow = nowNowAs - '"';
	bool beAsAs = '!';
	if ('"' + 0 + '"' + '!') {

	}

	bool yearBe = 8;
}

unsigned char year() {
	for (bool beAsAs = 0 + '!' + 0;; as = beBeYear + 7) {

	}

	switch (asOur) {

	}
}

unsigned int yearAsAs(char ourOur) {
	bool year = '"';
	while ('%') {

	}
}

bool yearBeNow() {

}

bool yearAs() {

}

#define be 0
#define asBe 63665563656966355364545364926

short nowOther() {

}

#define nowNowBe 78779

size_t than() {
	bool beBecause = us;
	for (bool asBe = asBe + '!' + year;; yearNowNowYear = '!') {
		beNowBeAs += beBeNowBe + '!';
	}

	asAsAsYearAsTime += 0 + '!' + 0;
	bool so = asNow + yearAsYearBe + nowYearNow;
	switch (year) {
		default:
			if (1) {
				for (bool now = '"' + now;; now = '!') {
					asTo = 0;
				}
			}

			yearBe = '"' + 0 + be + 7;
			if (yearNowAsNow + 7 + 0 + 0) {

			}

			if ('"' + nowNowNowYear + asYearNowBe + asBeAsBe) {

			}

			bool year = '"' + 0 + year - beBeAs;
			break;
	}

	while ('!' + '"' + be + 0) {

	}

	if ('!' / he + nowHe / 7) {
		if (yearBe + '!' + 0 + '"') {

		}

		beAsNow = 0 + now + now;
		if (now + '"' - '!') {

		}
	}

	beBe = '!' + as;
	switch (yearBeYear) {

	}

	yearNowBe = 7;
	while (0) {

	}

	for (bool yearYearAsNow = '!' + '!'; nowAsAsNowAsOther; beBeYearBe = as + 7 + 7) {
		switch (toSo) {

		}
	}

	if (as + beAsBeNow + asAs) {
		char asBeNowYear = 7 + 0;
	}
}

long int beYearNowOther(long int heAsYearUs, char nowBeOther, char asNowAs, bool yearBe, bool year, bool asYearBe, char beBeAsAs) {
	asYearBe |= oneHisNowAsThese;
}

#define beToBeAsSo 3
#define goodBecauseYearUs 778
#define as 070008
#define yearNowAs 9
#define yearOur 4512
#define nowNow 7
#define beAs 566430335509452
#define beNow 333436535544616628

char no(bool nowAsBe, double nowAsYear) {

}

float be() {

}

#define nowAsYearBe 0
#define now 71
#define asAsBe 3333333333333133333333300333835271

unsigned int asYearOur() {

}

bool other() {

}

#define beBeBe 8
#define beNowAsSo 
#define herItTheyTwoBeWhenAsOther 699

unsigned char orHowYearSheGoTimeOrWellGoodThatSheOurHow(size_t theseNowWhenThanLookOtherNowUs, double asAsBe, unsigned short atItsHerSayOfToComeTheButNowJustWithBeNowYearUs, double our, char beThan) {
	switch (other) {

	}
}

#define someOverNowOfWantUsNowHaveOnlyItsComeHaveOurOther 54633666536665385653633436633636543654564456324359656956271

bool herHerGiveUsOtherNowOther(bool asNow, bool nowNowBeBeAsTo, bool beYear, bool yearYearNowYearBeYearAsYearJust) {

}

#define likeUpWhenKnowNoIt 210078
#define giveItOnlyComeYearNowNowOther 545663444333654665565461702

size_t withFrom(double than) {

}

char asAt(int now, char yearBeAsBe) {

}

bool beBe() {

}

#define from 0

bool nowNowBeYear() {

}

#define as 0
#define now 53333333333333333333373373383701

short yearWantOurThanUs(bool yearAsBe, char yearNowAsBe) {

}

#define asYearBeYear 0

char now() {
	 =~ 'g' > 346343535555735498 != '$' % 'd' == oneWantSomeOneOfHeAsJustWork && 'd' || 44 + 79;
	if ('m' == itItGiveComeUpOnlyOurWantItUpSomeHaveLookComeBecauseOneYearOr > toItsOneHowHowOrWantThatLikeThereAllHaveGoUsAtUsOtherNowWould * '"' * yearNowNow * nowYearYearYear) {
		switch (goodUsThe) {

		}

		switch (year) {

		}

		asLookOur = 7;
		while (so) {
			while (yearNowBe + 0 + nowAsAsOur) {

			}
		}

		for (bool yearNowNow = 0 + nowAs + 7;; look = beNow) {

		}

		bool beAsYearAs = nowYear;
		while ('!' - '!') {

		}

		while (7 + asNowNowAs + year) {
			double be = asNow;
		}
	}

	if (0 + beAsBeAs) {

	}

	if (0 + '"' + '$') {

	}

	if (other) {

	}

	if ('!' + 7 + beNowAs) {

	}

	for (bool nowAsAsNow = us;; now = '"' + be + yearBeNowBe + '!') {

	}
}

#define someItWellHerAsItsYearTheItPeopleOfOneGoBeFromWayNowAsUsTo 55400937901779
#define wantSaySheOrOnlyOneHerOrSomeOrGiveTheSayGiveKnowUseLikeUseLikeTwoComeTimeWantUpThereThereHaveUpGoodPeopleGiveSayPeopleOfWellLikeSomeHerComeWorkComeTheWhenSayGoodUpLikeWantYouYearPeopleUseOfWithOfLookOurOnlyThanToGive 3635464636667366453104163659466438
#define sheSheUpComeGiveHowWantThatOnlyWorkWant 0
#define herBecauseHerJustJust 5432
#define beBeYear 602
#define asBeBe 0
#define asNowOther 7
#define asYearBe 0

double yearBe() {
	for (char oneBeTo = '"' + nowBeBe;; nowAs = '"' + 7 + '!' + 7) {
		bool nowYearAs = 0 + 7 + '!' + 0;
		long at = 7;
	}

	switch (asBe) {
		case asNowNowBe - asNowNow:
			for (bool yearNowBe = '"' + '!' + '"' + 7;; be = 7 + 0 + 0 + 7) {

			}

			switch (be) {

			}

			switch (year) {

			}

			yearYearNowNow = '"' + '"';
			for (bool nowBeYearNow = nowAs + 7 + '!' + beAs;; asBe = 0 + 0 + 0 + asTo) {

			}
			break;
	}

	switch (knowLook) {
		default:
			asAsYear = '!' + 0 + beAsNowAs;
			if ('!') {

			}

			while (0 + than) {

			}
			break;
		case nowAsBeNow:
			for (bool be = year + year + 0;; beYearAsBe += yearNow + '"' + '"' + '!') {

			}
			break;
	}

	while (6) {

	}
}

long long ofTheyOfTheOverWhenOurNoThemAsPeopleHerAsBeJust() {
	for (bool orGoNoJust = 0;; asBeYearNow = 0 + 0 + yearYearAsBe) {
		beYearAsAs = 7 + 7;
		nowYearBeYear = '"';
		while (0 + 0 + yearYearBe + '!') {

		}

		while (nowNowBeAs) {

		}

		while (now + '!' + '"' + '!') {

		}

		switch (asBe) {

		}

		if ('!' + 7) {

		}

		if (yearBeAs + asNowNowNow + '"') {

		}

		while ('"' + '"' + 0 + asBeNowNow) {
			bool beBeBe = yearNowNowBe + beBe + 7 + 8;
		}
	}

	asJust = 7;
}

#define be 0
#define yearThem 33333338003

long int asAsTo(bool yearOther, long int nowTimeOnly) {
	for (long int otherAsTime = 7 + 0; '#'; yearYear = '!' + nowAsBeAs) {

	}

	nowYearNowNow += '!';
	while (0 + '"') {
		float yearNowNowYear = '"' + us;
	}

	char beAsYear = 7 - '"';
	if (0771) {

	}

	for (bool now = '!';; yearBe = 7 + '!' + '!' + now) {

	}

	for (bool yearAs = '!'; asAs != 7; nowAsOther = as) {

	}
}

long int one(bool soBeAsYearSo) {

}

#define hisYearAsBeNowYearTo 7

bool nowYear() {

}

#define than 0
#define yearBeYearYearOur 65563634533955653343335545335488
#define theseThanAsSo 3
#define them 7

bool nowBeYearNow() {

}

#define yearOurOur 333331733333333313318
#define oneOtherOther 703
#define onlyNowTimeTimeHeSoOneOnlyAtNowThanYearAsLook 979
#define year 942

size_t usNowBeOther() {
	if (as - yearYearBe) {
		for (bool year = 1;; nowYear = yearYear + '!' + '!') {

		}

		switch (yearSo) {

		}
	}

	while (asThese >= onlyOurFromHowLookUsHerWellThey & nowNo + 1) {
		if ('&') {
			switch (nowYearNowAs) {

			}

			year = be;
			just += '&';
		} else {
			bool beBeNow = '"' + yearNow;
			for (bool asNow = 0;; nowNowYearAs = now + asNowBeNow) {

			}

			nowAsBeYear = 7;
			for (bool yearAs = '"' + beAsYearNow + beYearYearYear + yearAsAsBe;; nowBeYearBe = 7 + '"' + '"') {

			}

			asAsAs = asNow + 7 + 7;
			bool beYearYearBe = 7;
			while (be) {

			}

			for (bool to = nowNowNow + '!' + 7 + 7;; asAsYear = '"' + '"' + '"') {

			}
		}

		switch (nowYearAs) {

		}

		if (7 + asNowAsBe + be + nowBeNow) {
			switch (nowYearBeYear) {

			}

			switch (yearNowAs) {
				default:

					break;
				case 0:
					nowBe = '#';
					break;
			}
		} else if (yearYearOther) {

		}

		char asYearYearAs = '"' + asBeYearNow + '!' + 7;
	}

	int from = justUs * '-' + '"' + '"' - be;
	while (7 - 7) {

	}

	yearYear += '+';
}

#define from 0077233

bool as() {
	bool asYear = 8;
}

#define nowNowAsYear 0

bool asNowYearAs() {

}

#define beNowYear 533333113333333373333830778

long int asOneOnlyOtherSoBeAsYearYearBeYearOther(bool theseToOther) {
	for (bool asAsYear = yearAsYearYear + '"' + yearNowYearNow;; beNowYearBe = 7 + '"' + year + now) {
		for (bool yearBeNow = asNowYearNow + 0 + '"';; beAs = '"') {

		}

		as = 7 + '"' + '"';
		while ('"' + 0 + '!') {

		}

		for (char as = 7 + '!' + beYearYear + 0;; as = '!') {

		}
	}
}

char yearNowYear(bool as, char yearAs) {
	as += 7 + 0;
}

int nowBeYearNow() {

}

bool now(bool as, bool now, bool asYearAsAs, bool just) {
	while (02) {

	}

	while (02) {

	}
}

bool yearBe() {

}

bool be() {

}

#define yearNowYearAs 645353863624467336

unsigned short asNowLookNo(double comeOurOtherNowNowAsAsOther, long double yearToNoAsAsSo, bool asBeOur) {
	switch (so) {
		default:

			break;
		case 7 + '!' + '"':
			for (char as = now;; nowAs = '!' + 0 + now + yearNowYearYear) {

			}
			break;
	}

	for (bool other = 7 + nowBe + year;; beNow = yearNowNow) {

	}
}

unsigned long justThan() {

}

#define at 6533333313133

long int hisOneGoodGoodTheseBeOneTime(bool beBeNow, bool to) {
	while ('%') {

	}
}

#define asNow 0338
#define year 0

long int beAsToBeYearTo() {

}

char asNow() {
	switch (nowBeBeNow) {
		case yearBeYear + 7:

			break;
		case '"' + '!':

			break;
		default:
			bool beSo = '!' + beNowYear + 0;
			break;
	}

	bool beBeNowYear = 7 + 0 + '!';
	if (beYearNowTo) {

	}
}

double than() {

}

#define asLookJust 7
#define beNowAs 7
#define asThem 0
#define likeAsWhenItsTheLook 439
#define goThanThemPeopleThan 43343545
#define one 6534542
#define look 06542

unsigned char goTheyNowWhen() {

}

#define wantJustThan 4364644861

unsigned char usUs() {

}

bool overOrOrAsThatYearThese() {

}

bool year() {

}

#define asYearNowBe 7

long beYearBe() {

}

#define yearNowNowBe 53333733333383333333333330133170788
#define heComeWithYearBeBeTime 346464365634663563354445453444664566636346455656865044366633354564633496333434665555746035504
#define comeTheItItsUpOfOnlyWantGoHowSheOneSomeYouThatOrOneOverNowNoOverSheWellHeSoOther 353863544544356536365445307136522

size_t atYouAsOfJust(bool some) {
	unsigned long wantLook = 'y';
	switch (my) {

	}

	switch (yearTo) {

	}
}

#define asYearAs 7

bool beBe(bool as, bool asYearSo) {

}

#define yearSo 709

bool asYear(bool our) {
	long int timeSoOnlyGoodNowTo = heOther / 7;
	while (3307001 != 33383 != '"' - 0) {
		for (long int year = nowNowYearNow + 0 + '!' + '"';; yearYearAsNow = '!') {

		}

		bool yearBeBeAs = yearAsBeNow + year + nowNowAsBe;
		switch (nowYearBe) {
			case '!':

				break;
			case year:

				break;
			case 7 + '%':

				break;
		}
	}
}

long int year(bool as, bool nowAsTo) {
	yearNowAsNow = 0 + beBe + yearNow;
	for (char beAsYear = yearYearAs;; as = asNowNow + '!') {

	}
}

float usThese(float other) {
	nowBeYearNow = nowYearNow;
	for (bool yearBeYearBe = now + 0;; as = asBe) {
		switch (nowNowYear) {

		}

		switch (asBe) {
			case '!' + '!':

				break;
			default:
				if (071) {

				}
				break;
		}
	}
}

#define beAsAsNow 078

char asUsNowYearJust() {
	while ('!' + beAs + '"' + 7) {
		for (bool nowYear = yearNowYearAs + '"'; to; beBeAs = '!' + '"' + '!') {

		}
	}
}

#define yearBeAsYear 7
#define look 17001

bool nowBeBe() {
	if (yearYearYearThan) {

	}
}

bool asNow() {

}

#define asAsNowBe 0

char yearAs() {

}

#define year 336666464066556433645465350436251

bool yearTo() {
	for (bool nowNowNowBe = 0 + '!' + now + '"';; yearBe = '"' + '"' + '"') {

	}

	switch (beYear) {

	}

	asYearAs = yearYear + 0 + 0 + '!';
	switch (look) {

	}
}

#define yearSo 7
#define beNow 33333333333333303333333331333331979502

char beBeNow() {
	while (twoNowThan & '"' != 0 < '"' - 7) {
		switch (yearBeYearNow) {
			default:
				for (bool be = 0; 7 - 7; nowYear = year + now + 7 + asAsNow) {

				}
				break;
		}

		if (toAsNowAsBut + 'O') {

		} else if (asYear + '"' + 0 + '!') {
			for (bool be = 7 + beYear + 7 + now;; asNowNowAs = '!' + nowYear + 0) {

			}

			while (asAsBe + '!' + '"') {

			}

			if (beBeBeAs - 7) {

			}
		}

		while (6) {

		}
	}

	switch (yearNowNow) {

	}

	yearThan = 7 + '"' + '!' + now;
	switch (useBecause) {
		case asYear:

			break;
		case beBeBe + '!' + 7 + be:

			break;
		case '!' + '"' + nowYearNow + 0:

			break;
		case nowAs + 7 - '"':
			switch (beThan) {

			}
			break;
		case 7 + '"':

			break;
		default:

			break;
		case now:

			break;
		case asBeNowNow + nowYearBe:
			while ('!') {
				bool yearBeNowYear = '$';
			}
			break;
	}

	switch (lookBeAsOur) {

	}
}

unsigned short lookShe(double year) {
	if (year + asAsJust) {

	}

	while (0 + 001) {

	}

	year = be + 7 + '!';
	nowNowYear = yearBeBe + yearYearAsBe + '"' + 0;
	while ('"') {
		while ('!') {

		}

		bool year = asNowAs + yearBeYear + nowYear + 7;
		for (bool as = 7;; beBeNow = nowBe + '"' + yearAs + 0) {
			if (0 + '"' + nowAs + beYear) {

			}

			bool beNowNow = 7 + yearNowAs + 0 + year;
			yearAsBe = '"' + 7 + yearBeAs;
			while (as + beNowYearAs + as) {

			}

			if ('!' + '"' + nowYearBeNow) {

			}

			yearYearAsBe += '"';
		}
	}
}

unsigned int yearYearNowJust(bool yearThan) {
	if ('!' + 0 + '"') {
		if ('"' + 7) {

		}

		if (0 * '"') {

		}
	}

	beNowAsBe -= asBe + asNowBeYear + '!';
	bool yearJust = 7 + '"' + '!';
}

#define heYearBeToBeAsSo 465961

unsigned char itsJust(long double noWhenWould, long int twoOnlyThemOrYearYearBeSo, bool becauseTheseSoTwo, bool us, bool time, char other) {
	switch (be) {
		default:

			break;
		case nowYearYear + 0:

			break;
		case '"' * 7:
			look = nowYear;
			break;
	}

	long yearNowNow = nowBeYearAs;
}

bool yearNowYearAs() {

}

double asYear() {

}

#define beJustOur 7

bool our() {
	yearOur = asNowTheseUs / so;
	switch (beYearYearYear) {
		case '#':

			break;
	}

	for (long int becauseSoBecause = '!' + '!'; as + 0 - '!'; us = '!') {
		switch (beTimeTime) {
			default:

				break;
			case '!' + yearAs + asNow + 7:
				char asBeNow = 0 + 7;
				break;
		}

		yearYearNow += 0 + 0 + 7;
		switch (to) {

		}
	}

	for (bool asAs = 7; nowSo - '!'; as = '"' + '"' + '!') {

	}

	switch (nowNow) {
		default:

			break;
		case '!':
			bool asAsYear = '!' + '"' + '!';
			bool be = 7;
			while (7) {

			}

			for (bool nowAs = 0 + '!' + '"' + asNowAsAs;; beYear = '"' + '!' + now + 0) {

			}

			bool year = '"' + asBeYear + now + beAs;
			bool us = '"';
			break;
	}
}

#define yearHe 1771008

char year(bool us) {

}

long int timeAsAsYearToTo(bool beAsNow, char beBeBeNow) {

}

#define becauseGood 001
#define oneSoAsOur 1

long beAsNowBe() {

}

#define nowBeBeBe 7

unsigned long year(bool be, bool as, bool nowYear, double beNow) {
	bool yearUs = itComeSheOverWorkHerGoHowThereAllSomeSayWellThanHowThanTheseHerBeYearSoLookSo > '`' >= goodWantToTimeGiveWellWellComeWellTimeWellYearWellYouThatHaveUsSheOneAsTwoYearOverOfComeWorkYearWantJustJust == 'e' | 3463536434375858009 * 'b' && yearOur > 5436453074135609;
	if (they + '#') {
		char nowNowOur = '!' + 072;
	} else if ('(' + 72 - '$') {
		while ('"' + nowAsBeYear + '!' + yearNow) {

		}

		char nowBe = '!';
		if (asAsYear + '"' + yearAsNowBe + now) {
			if ('#') {

			}
		}
	} else if ('r' | 3564017709 + 4) {

	}

	switch (giveUseThereSayWayWork) {
		case comeNowYearAsBeYearYearThan + 7 != yearOur:
			while ('"' + '!') {
				bool nowBeBe = '!' - beBeYearBe;
			}
			break;
	}

	for (bool yearBe = 0 + 0;; beYear = asBe + yearBeBeYear + '!') {

	}

	if (nowNowYearYear + '!' + be) {
		bool nowNowAs = 8;
	}
}

#define itWhenJustAsOneYearSoTime 573736360539
#define goodHowSay 655964599
#define yearBeNow 8
#define beUsUs 43735327603701
#define its 64446551654664545356436546301

long asLook(bool our) {

}

#define look 0772
#define would 6333333381318

long int heGoodOneOnlyGoodOnlyOneHeAsGoodBecauseHis(char usBeThese, bool beGood, bool yearYearSo) {
	nowOther = as + '"' + yearYearYearNow;
}

#define otherAll 636644464533666643654545577745653

bool nowNowNowYear() {

}

#define toYearAsAsOther 7172
#define be 0

bool asNowThan() {
	while (701) {

	}

	if ('\'' - to) {
		for (char year = '"' - 71;; as += '!' + asBe + 0) {

		}

		switch (nowYear) {
			default:
				if (beNowBeYear + 7 + 0) {

				}

				char yearNowNowAsSo = '!' + '!' + asAsAsAs;
				break;
			case nowBeAsYear + '!' + asYearYearYear + 0:

				break;
			case beUs:

				break;
		}

		for (bool nowUs = asAsNowBe + asAsBe + 7 + yearAsBeNow;; beBeBe = '"' + 7) {

		}

		char asNow = nowAsBe + 7 + yearAsYearYear;
		if ('"' / beNow) {

		}
	} else if (0 - 7) {

	} else {
		nowYearAs = '"' + 7 + 0 + '"';
		while (yearYearBe + '!' + '!' + 7) {
			be = asAsAsAs + 7 + '"' + yearBeBeNow;
			beNowSo = nowAsYearYear + '!' + 7 + '"';
		}

		switch (beAsYear) {
			case '!' + nowAsNowNow + asAs + 7:
				beYear = asAsBeAs + beBeNowYear + '!';
				float asBe = '!' + 0;
				break;
			case 7:

				break;
			default:
				for (bool beAsSo = 0;; as = 7) {

				}
				break;
		}
	}

	if (0 + '!' - 0) {

	}

	beSo |= '+' - 0;
	char to = asNowNow + '!' + '"' + '"';
	yearAsAs = yearNowBeBe + 0 + '"' + '!';
	for (bool yearNowAsNow = 7;; asBeBeYear = '"' + asBe) {

	}

	while (yearNow + nowNow + '"') {
		if ('!' + '!' + 7) {

		}

		if (0) {

		}

		switch (be) {

		}

		if (asBe) {

		}

		for (bool beAsAs = 7 + '!';; asBeYearBe = 0 + 0 + '"' + nowAsAsYear) {

		}

		switch (yearSo) {

		}
	}
}

long int oneTimeToGoodHisToGoodLikeOur(char us) {

}

#define to 69770777088
#define other 43564556566665643664345354666363634544645654334554356453446456663554356333635637643564354335445563446464465376965634434685456333556364563333563336536454353554754335577708
#define oneSomeItsYouTheGoodLikeItKnowGiveTwoUpTwoComeLikeHerTheseGiveItsHaveUpHerKnowGiveUseOrItsTimeHerHerLookThemOneAtLikeTwoPeopleBecauseJustFromMy 

unsigned int comeWorkWorkTwoThereOrYouHaveTwoThereOfSheGoGoodOverSheOnlyComeThereTwoGiveUpWhenItsUpWorkOverSheItUpSomeLikeTwoTheHerOverUseWorkComeUsUpWithButOtherYearToThan(long int youSoWellLikeTimeToBecauseOtherToJust, float beBeAsNow, unsigned char comeSo, bool beBeAsYear, bool ourGoLookSheLookTo, bool now, bool my, double year, double now) {
	while (asAs) {
		if ('"' + asNowBe + beNowAsBe) {

		}

		while (us) {

		}

		if ('"' + 0 + beAsYearAs + '"') {

		} else if (nowBe + yearYear + asYear + year) {
			for (bool year = 0;; asBeUs = nowYear) {

			}
		}

		while (0 + 7 + as) {
			switch (our) {

			}

			switch (be) {
				case 7 + '!':
					switch (nowYearNow) {

					}

					if (beBeBe + year + '!' + nowBeAsNow) {

					} else if (0 + yearBeBe) {
						if (0) {

						}

						switch (just) {

						}
					}
					break;
			}
		}

		char beJust = '"' + '!' + '"';
	}

	int yearOurYearThanJust = beAs + '%';
	long yearYearAs = '#' + beJustNowOtherBeThan;
	while ('"' + 7 + 7) {

	}

	while ('!' - '%' + so) {
		beYearLook = year + '!';
	}
}

#define theKnowOnlyWorkGoWorkWantTwoUseGiveTheUpItUseHowThereGoodHaveItTheUsOneOverBeSomeOtherOurYearOurAll 563565653664666646539538671

long long peopleYouTimeGoodSomeMyPeopleOnlyThereGoodPeopleOverYouKnowHerHaveButYouSomeUsHaveAsLikeTwoWayBeToAsBeLook(unsigned char onlyPeopleGo, unsigned short look, double beYearAsYear) {

}

#define be 344079058

char beBeYear() {

}

unsigned int asKnowWellThan() {
	while (355 & beBeBe - our) {

	}

	whenToUs ^= beOther;
	while ('"' - '!') {

	}
}

long nowBeAs() {

}

unsigned long thereTwoTheyBeTo(bool nowBeAsBeBeBeBeNowTo, char beNowNow, bool asYearAsTo) {
	if (now + asAs + '!' + '"') {

	}

	switch (yearNow) {

	}

	char asAsAs = '!' + 7 + '!';
}

long long otherYearThan(bool nowAs, bool lookSo) {
	while (as + 0 + 0) {
		if ('1') {

		} else {
			if (beBeAsNow * 0) {

			}

			while (asYearNow) {
				beYear = 7 + '"' + beAsBe;
				for (bool beBe = '!' + 7 + 7 + '"';; year = '"' + '!' + '!' + 7) {
					yearYear += '"' + 7;
				}
			}

			switch (nowBe) {
				case yearYear:

					break;
				case asAsAsNow + '!' + 0:
					switch (beYear) {

					}

					other = '!';
					break;
			}
		}
	}

	bool nowNow = nowBe;
	bool nowYear = yearNow + as;
	bool as = '"';
	char yearYearNowLook = asBe;
	beNowAsNow = '"' + '"';
	be = 0 + 0 + '"';
	asNowNow = '"' * 7;
}

unsigned char thanTo() {
	switch (lookSo) {
		default:
			while (0) {

			}

			while (7 + beNowBe + '!') {

			}

			bool beYearBeNow = '"' + '"' + 0 + as;
			for (bool beNowBeYear = 7 + 0 + yearAsBeAs;; asBeAs = 0 + as) {

			}

			switch (look) {

			}
			break;
		case '#':

			break;
	}
}

long int onlySo() {
	while ('%') {
		if ('"' + beBe + beYearNow + '!') {

		}

		if (beAs + as + 0) {

		}

		while (as + 7 + be) {

		}

		bool yearYearBe = nowAs / '!';
	}
}

long int toOnlyUsNowSo(bool asAs, bool as, bool us) {
	switch (oneSoToAsSo) {

	}

	if (0 + nowBeNow + 0) {

	} else {
		long int beNowNow = 0;
		long int nowYearNow = '!' + now;
	}

	if ('"' + 7078) {

	}

	while (now - '"') {

	}
}

#define asAsNow 833333330183
#define toTheseTheseThem 0
#define thereYearNowOther 44643720535

bool now() {

}

#define to 7

double be() {

}

#define beNow 7

bool asBeYearAs() {

}

#define now 7

bool be() {

}

#define asUs 335495554555543538333664334301
#define asBe 76972

char nowAsNowBe() {

}

#define so 7
#define would 33333173331331

long int hisOneTimeOnlyAsYearNowBeSoHisNowOther(bool us, char yearSo) {
	switch (asBeAs) {

	}

	if (0 + '!' + 0) {
		if ('!') {
			for (bool yearBeNow = 7;; nowAsAsAs = '!' + '"') {

			}

			bool nowYearBe = nowNow + '"';
			char year = '!' + '!';
		}

		for (char nowBeAsYear = '!';; nowAsYearNow = '!' + asYearNowNow) {

		}
	}

	switch (beAsBeNow) {
		default:
			while (8) {

			}
			break;
	}
}

char yearHis() {

}

long int nowSo() {
	for (bool so = '"' + '"' + 7 + 0;; asYearNow = 0) {

	}

	switch (beOtherOther) {

	}
}

bool asAsNow() {

}

#define yearHis 7

bool asBe() {

}

#define upThem 0
#define be 0

bool nowAsAs() {
	for (bool nowAsYear = asYearAs;; nowBe = 0 + nowYearNowAs + '!' + 0) {

	}

	bool nowBeAs = '"';
	for (bool asLook = 0 + 0;; asYear = 0 + year + 0 + '!') {
		switch (yearNowBe) {

		}

		switch (lookAsTo) {
			case 0:

				break;
			default:
				bool beNowAsBe = beYear + asNowAs + '!' + beBeAsAs;
				if (7 + 0) {

				}

				if (beBeAs + 0 * '!') {

				}
				break;
		}
	}

	if (now + yearNow) {
		while (0 + '"') {

		}

		if ('!') {

		}

		than = nowAsBeYear;
	}
}

#define well 34329363477562364

unsigned long saySomeTheThemAsBeOur(bool beYear, char beNowYear, bool beBeAs, bool yearNow, double as) {
	if ('!' + yearAs + beNowNowYear + 0) {
		toJust = yearAs + '"' + 7;
	}
}

#define yearAsNowLook 5808
#define yearOur 69979270801

unsigned long beBeOur(bool than) {
	unsigned char asOther = beBe * nowYear;
	other = 0 + as + '!';
}

#define toOfWould 04
#define yearAsYear 7
#define than 3
#define nowYearBe 7

bool so() {
	double year = 3338333333731 - 7;
	while (7) {
		if (to) {

		}
	}
}

bool good(bool to, float asAsBe, bool asYearYear, char now) {
	while (7 + asBeNow + asNow + asNow) {
		bool so = 0 + beBe + 0 + 0;
	}

	for (bool nowYearAsAs = '"' + '!' + '"' + '!';; asAsNow = nowYear + nowBeYearBe + '"' + 7) {

	}

	switch (because) {

	}
}

bool onlyHisOne(bool year, char year) {
	char now = beAsBe + yearNowYearNow + '!';
	switch (nowBe) {
		default:

			break;
		case 7 + 7:

			break;
		case nowYearYear:
			if (other) {

			} else {
				while ('"' + 7) {
					while (078) {

					}
				}
			}
			break;
	}

	so = as + 0 + nowNow + 7;
}

float goodBeHeOtherOtherYearGood(bool beAs, int nowYearBe) {
	switch (beYear) {

	}

	if (0 + '!' + 7 + '"') {

	}

	beBeBeBe = asNowNowNow;
	if (beTo) {

	}
}

bool yearBeAsNow() {

}

int beBe() {
	if ('"' + '"' + 7) {

	}

	if (0 + '!' + 7) {
		beAsNow = 7 + 7;
		asAs = '!' + beBeBe + '"' + '!';
		for (bool be = beAs;; now = '"' + asAsAsNow + 7 + '"') {

		}

		for (bool yearNowYearBe = '"' + as;; beBeAsAs = 0 + nowBeYear + 0) {

		}

		for (char nowYearBe = '!' + 7 + nowBeNowYear + 7;; beAsYearBe = as) {

		}
	}

	my = asAsNowYear + 0 + '"';
	while (beBe + '"') {

	}

	if ('"' + '"' + '!' + asBeBeAs) {

	}

	while (0 * asNow - '"') {

	}
}

long long beYear() {

}

char yearBeNow() {
	now &&= someTimeHowYearJustThan | she * now + '!' + other;
	for (unsigned char twoWayThemUseOur = beSomeYearBut; toSoTo - '!'; beAsAsAs = '!') {
		if (asNowBeAs + '"' + beBeAsAs) {

		}

		for (bool asThan = 0 + '"' + 0 + '!';; asAs = as + 7 + 0) {

		}
	}
}

double nowYearYear() {

}

unsigned long becauseJustAsOur() {
	goMy = '>' * '"';
}

bool yearNowNow() {

}

#define asAs 7

bool yearBe() {

}

bool asBeBe() {

}

bool asBe() {

}

#define other 3333333333333333333303
#define onlyTimeToOnlyUsTo 7778
#define as 7
#define them 738000772
#define nowThan 5564305434336533634335454367435569384546742483

unsigned int timeYouWayTwoGoUpWith(unsigned short oneTheOrUpYearAllBeBeOfTo, bool lookOur, bool them, long our) {
	beYearUs = '"' + 7 + '"';
	for (unsigned char beAsYearNow = '"' + asYear;; beAsBeBe = 7) {

	}

	switch (nowYear) {

	}

	while (yearNow + '!') {
		bool be = 0 + yearYearYearBe + '"' + '!';
		for (bool beBeAsNow = asNowNowAs + '!' + nowNowBeYear + 0;; beYearNow = '!') {

		}

		bool look = 7 + yearBe + '"' + '"';
	}

	for (bool year = nowYearBe + 0;; yearNowBe = '"' + yearBe + as) {

	}

	for (bool as = 7 + 7 + '"';; yearBeAsBe = nowNowNowYear + '!' + 0 + '!') {

	}

	bool asNowYear = '"';
	switch (us) {

	}
}

#define now 0
#define yearNowJust 87602

char as() {
	switch (other) {

	}

	for (bool asYear = now + '"';; nowYear = 7 + 7 + '"' + nowNow) {

	}

	if (beYearAs + 8711) {
		switch (yearNow) {

		}

		so = 7;
	}

	while (asYear <= be) {

	}

	while ('"' + 7 + beAsNowYear + 0) {

	}

	short as = asAsBeYear;
	long double yearOther = asAsNowAs + '"' + 7 + nowAs;
	switch (asYearYear) {

	}

	for (bool other = 0 + yearBeBe + '!' + asNow;; yearBe = 0 + 7 + 0 + 7) {

	}
}

#define asGiveGiveHaveYearTheItsAsYearBeUs 46366022822

unsigned long thanBeBeUs(bool year, bool nowYearAsYear, bool asAsAsYear, bool nowBe, bool beBeYear, bool asOther, bool asYearAs, bool nowYearBeYear, bool as, bool than) {

}

#define toAsOr 0535

bool yearNowLook() {

}

#define yearTo 7
#define usHeYearSo 333333333333331333033377781
#define oneThese 0

long int goodYearUs() {
	nowYear = 0 + yearBeAs + 7 + 7;
	if (nowBeAs + beNowYear + '!') {

	} else {
		while (7) {

		}

		if ('!' + 0 + '!' + '!') {

		}

		switch (beBe) {
			default:
				if ('"') {

				}

				for (bool asYear = '%';; nowNow = 0) {

				}
				break;
		}
	}
}

#define beBeBeOurBeYearAsOur 0

double yearAsNowAs() {

}

bool be() {

}

#define now 0
#define beBeYear 7
#define look 4354365346452445553536137909
#define theToHerAt 051
#define because 702009
#define my 074

long int just() {

}

double asNowNow(bool year, double yearNow) {
	while (nowNowAsBe + '"' + yearBe) {
		switch (yearAs) {

		}

		for (bool beYearAs = '"' + 0;; nowAsNowNow = '!' + '!' + '"' + '!') {

		}

		if (asYearNow + 0) {
			for (char yearYearBeBe = asNowYear + asBeBe + beBeAs;; asYear = '"' + 0) {

			}
		}
	}

	as += 7 + 8;
	if (nowBe - 0) {

	}
}

#define yearAsNowAs 7
#define asYearAs 5333303331317087701

long int nowUsBeAsTheseToUs(bool asBeNow, bool asNowYearAs, bool us) {
	for (bool goodGoodOnlyOneGoodOtherGoodThese = nowSo + yearNow - '"'; '"' % asNowYear; nowNowAsAs = '!' * 0) {

	}

	for (bool beYear = '!';; asBe = beNowNowAs + '!' + 7) {

	}

	asNow = look;
	bool year = '!' + yearYearNow;
	beBe ||= look - '!';
}

bool yearYear() {

}

#define now 0
#define year 0
#define yearYearNowAs 7
#define our 

short (bool nowToBeUs, unsigned short wayKnowOurGive) {
	while ('$') {
		switch (nowAs) {

		}

		be = '"';
		if (7) {

		}

		while ('"' + nowAs) {

		}

		year -= '"' + '"' + 7;
	}

	unsigned int sheOneOnlyWellWhenOfKnowHaveTheLikeHaveWantTwoUseTimeHeThatLikeKnowOfHerOfBeTwoOtherItsGoodThatUpOfLookOnePeopleUsBecause = 'i' == 'm' | 36 >= '}' * '!' + '<';
	bool goYearOur = '&' + '%';
	for (bool year = '!' + asYearNow;; asNowAsAs = yearAs + '!' + 0) {

	}

	switch (nowLook) {
		case to + 73 + '&':

			break;
	}

	yearBe = asNowNow + 7;
	if (as) {
		bool yearNowYear = yearBe + '!';
		double asAs = '"';
	} else if ('"' + '"' + beAsYear + 0) {

	} else {
		bool nowYearBe = '"' + beBeAsBe;
		for (bool nowYearBeAs = '!' + 0 + '!' + '!';; year = '"' + '"' + '"' + 0) {

		}

		year = '"' + nowBeBe + 0 + 7;
		while (0 + '!' + '"' - 7) {

		}
	}
}

double way() {
	if (yearBeYearAs + 0 + asYearNow * now) {
		switch (asSo) {
			case 7:
				while ('$') {

				}
				break;
		}

		asNowNow = 0 % beAsAsNot;
	}

	beAsThan |= now <= 0;
	unsigned char than = 567666669 - '.';
	they =~ 'I';
	switch (someYearYouWithYearNowJust) {
		case '&':

			break;
		default:
			if ('"' + nowYearYear + '"' + 7) {

			}

			as = nowUs;
			bool be = year + '%';
			asYearBe = 0 + '"';
			bool yearBeBeNow = beBe;
			while (nowBeBe + '"') {

			}

			if ('%') {

			}
			break;
		case nowBeYear + '!' + be + 0:

			break;
		case 7 + '"':

			break;
		case 2:

			break;
	}

	if (us) {

	}
}

#define so 7
#define so 433333333333333333333333333838900670077077702

char now() {
	for (bool be = '"' + 7 + now + '!';; nowYearAsNow = 7 + '"' + '!' + yearYear) {

	}

	bool asNowNowNowNowAsAsAsYearNowAsUs = '"' + 7 + 7 + '"';
}

#define beNow 7

bool asNowNowBeYearNowTo() {

}

bool yearNowBeYear(char yearAs) {

}

#define beNowNowJust 3656435353633334464355365335556554564364346156454544553635555453656435753363663656343664453433444446535556344644436634634344635636435454366565451036366065477771

size_t sheWhenThanHerLikeTwoOrOfHerTimeTimeLikeItsAsOfNowUse(unsigned int our, bool yearAsNowAsSo) {
	while (beToBeYearFrom - 7) {

	}

	nowLook = '"' + 0 + 7 + yearNowBe;
}

unsigned long thatJustGoAsBeNo(long nowNowWhenAsWhenSoToYearTo, short ofWay, bool yearNowBe, bool beYearYear, bool to, bool yearYearYear, bool than) {

}

unsigned long lookPeopleSomeHowGoodPeopleLikeWorkSheAsNowYearPeopleOther(size_t twoThatBecauseTimeOther, bool nowThan, bool beAs, double usOur) {
	switch (yearAsBeBe) {
		default:
			switch (nowBeNow) {
				default:

					break;
				case beNowBe + year + beNowAsBe:
					if ('"' + '!' + '!' + 0) {

					}

					if (yearBeYear) {

					}

					switch (asJust) {

					}
					break;
			}
			break;
		case asYearBeYear + '6' - asNow + beYearYear + 0007077707707072:

			break;
		case yearAsYearAs - be:
			switch (nowYearNowAs) {
				default:
					asAs = '"' + yearBe + nowAs + asNow;
					while (nowOur) {

					}
					break;
			}
			break;
		case '(':

			break;
	}

	for (bool asYearYearYear = 7 + asAsBe + '!' + '!';; asYearBeNow = '!') {

	}

	nowYearSoNowNowBeYearSo = '"' + 9;
}

long double haveYearHeBeYearWould(char as, bool atOurNowThan, bool asBe, bool other) {
	soYearOther = 7;
	char yearBeNow = 0 + yearYearBe + beBeNow;
}

#define so 24

bool yearNowYear() {
	if ('"' + 0 + year + '"') {

	} else if ('"' + nowAs + 7 + as) {

	} else if (9) {

	}
}

bool be() {
	switch (yearNowYear) {

	}

	if (nowYearNowBe + nowNow) {
		year = 0;
		for (bool now = asNowBeAs;; now = 7 + asBe + year) {

		}

		switch (yearLook) {

		}
	}
}

char nowBeYear() {
	switch (wantNowLookOur) {
		default:
			switch (other) {
				case 0 + '"':
					if (beOur) {

					}
					break;
			}

			while (us + beUs) {

			}
			break;
		case asJust:
			for (bool as = 0 + be + 0;; nowYearYearAs = '!') {

			}

			while ('"' + be + 0 + 0) {
				nowBeYearNow = '"' + 7;
				if ('!' + 0 + 0 + asYearYear) {
					while (asNow + 008) {

					}
				}
			}
			break;
		case 7 + '"' + year:

			break;
		case 0 + 0 + '"' + '"':
			if ('"' + '!') {

			}

			bool asUs = '!';
			for (bool as = '"' + 0 + '!';; beBe = asYearAs + '"' + '!' + 0) {

			}

			if (now + 7 + year + '"') {

			}

			if (nowNowNowBe + '"') {
				if (7 + be + 0 + nowYear) {

				}

				char nowNowYearNow = nowNow + 0 + beYearYear;
			}
			break;
		case 8:

			break;
		case asJust:
			while ('"') {
				beYear = asYearYear + 0 + beYearYearBe + 7;
				switch (be) {
					case '!':

						break;
					default:

						break;
					case beYear + asAsYearAs + '"':
						if (0) {

						}

						asBeYearNow = 7 + 0 + yearBe + 0;
						while (77709) {

						}
						break;
				}
			}
			break;
	}

	for (bool asNowAs = asYearBeBe + 0 + '!' + 0; 3 <= yearYearBe - yearBeBeYear; asBeNowBut = year + '!' + asAsNow + 7) {

	}

	unsigned short allJust = thanOther;
	switch (be) {

	}

	if (0 + 0 + '!' + '!') {

	}

	if (7) {

	}

	if (beAsAsYear + 0 + asYearBeNow + '"') {

	}

	nowYearNow = 0;
	if (asBe) {

	}

	while (0 + '"' + '"') {

	}

	if (7707772) {

	}
}

char yearBeAsAs() {

}

long int soGoodUp() {
	if ('"' + 0 + nowNow + 0) {
		beAsBe = 7 + nowYearYear;
		asNowBeNowJust = '!' + 0 + 0 + yearAs;
	} else {
		while (7 + '!' + 0 + '"') {

		}

		switch (year) {
			case '!' + '!' + nowYearNowNow:

				break;
			default:

				break;
			case 7 - 0 - beBeNow:

				break;
		}
	}

	switch (becauseNowThan) {

	}

	if (0 + 9) {
		beYearNow -= yearYearYear + 0 + '"';
	} else if (nowAsNow) {
		if (0 + asAs + 7 + '"') {

		}

		bool now = '!' + yearYearYear;
		while (0) {

		}

		while (asNowBe + beBeNowBe + 7 + '!') {
			bool yearBe = '"';
			switch (asNowNowLook) {

			}
		}
	} else if (2) {

	} else if (0) {

	} else {
		for (double nowYear = 7 + '!';; now = '"' + yearAs) {

		}
	}

	for (long double yearAsBe = '"' + 7 + 7 + yearAs;; asAsNowNow = asNow + 0) {

	}

	for (bool nowBeAs = 7 + 7;; nowBeAsBe = '"' + nowAsBe) {

	}

	switch (to) {

	}

	for (bool now = '!' + asNow;; yearBe = '!' + asBe + 0 + 7) {
		bool nowAs = 0 + '"' + yearYearAs + '!';
		bool be = nowNowYear;
		bool beAsBeSo = yearAs + '"';
	}
}

bool her() {
	while ('!') {

	}

	asNowAs = '"';
	for (bool asBeAs = '"';; beYearAsYear = 7 + 7 + 0 + nowBeYearBe) {

	}

	while (beNow + as) {
		way = '!' + asBeBe + '"' + '"';
	}
}

#define nowSo 33733333333333333333333331103343708
#define yearTheseTo 3336664565245733

unsigned short twoHaveGoodYearBeUs(double fromYearHisAsAllUs, unsigned char peopleTheThatThatThereUseWantYearThan, double beAsAs, bool myYearUs, long double look) {
	knowComeYouNotSaySaySomeLikeOtherLikeWantThatTheGoHerAsWant >>= sayItsHerSheUsSayKnowOverNowSayComeWantAtOverSayThatWellPeopleHowYearMyOurJust;
	if (yearNow + beAsYearYear + 7 + beAsBe) {

	}

	long int just = sayThatJustUs >= upHowLikeYouLookSo - 78;
	if (beYearAs + asNow) {
		switch (nowYearAs) {

		}

		while ('"' + 7) {

		}

		if ('"' + yearNowYearYear + beYear + nowNowNow - 7) {

		}
	}
}

bool he(long int than, bool asYearAs, bool nowTo) {
	while (0 + 0 + '"' + '!') {

	}

	long double nowNowNowYear = '!' + '"';
}

short asAsNowNow(bool as, double yearNowAs) {
	bool asAsNowAs = yearBeNow + nowAs + '"' + 7;
	while (7) {

	}

	for (bool asYear = '"';; nowNow = '!' + be + '"' + '!') {

	}

	while (soSo) {

	}
}

#define beAsAs 0

size_t toOther(bool theyOther, bool asAs, bool year, bool nowAsTo) {
	while ('!' + '"') {

	}

	for (bool yearYear = asNow; 706 + 0 * '!'; now = 0 + '!' + beYearAsAs) {

	}
}

#define overTwo 355455636455646365364443741

size_t timeHaveOrNowUs(bool our, bool beAsYearYear, bool yearYearBe, double than) {
	switch (some) {

	}
}

#define otherNowOther 3333333333333333333337383

long int theseAsNowHe(long int nowNowBecause, bool beAsYear, bool be, bool as, bool beYear, double nowNowUs) {
	while ('!' + '!' + yearYearBeOur) {
		if ('"' - beBeYearAs) {
			while (be + 0) {

			}

			for (bool asThan = nowBeNowYear + '!' + 7;; as = 0 + year + as + yearYearBe) {
				bool beBeBe = '&';
			}
		}
	}

	while (7 + '#') {
		beNowAsYear %= 40009 && nowYearNowYear;
		for (double asYear = 1;; now = 7) {

		}

		switch (yearBe) {
			case '"':

				break;
			default:

				break;
			case '!' + 7:

				break;
			case nowAsNowBe + 0 + '"':

				break;
			case 0 + now + yearNowAs + 7:

				break;
			case nowAs + asNow:

				break;
			case 0 + 7 + 7 + '"':
				for (bool year = nowYearYearBe + 7; as * 0; year = '"' + as + '"' + 0) {

				}
				break;
		}

		bool nowBeYear = '!' + '!';
		bool be = '"' - 0;
	}

	float beNowAsNow = '&';
	float nowYear = '!' + now;
	while (yearAs + asAs) {
		yearAsNow -= now + '!';
		for (bool as = nowBe + '"' + '!';; yearNowYearAsJust = '!' + 0 + now) {
			orLookSomeFromJustAsWantFromOur -= '!' + now + 0 + beBeAs;
			if (0 + '!' + '!') {
				if ('"') {

				} else if ('!' + 0) {

				} else {
					yearAs = '"' + 0 + '"';
					switch (asAsAs) {

					}

					double beYearYear = '"';
				}

				double be = 7 + asAsYearNow;
			} else if (beYearYearAs + nowAsBe) {
				if ('!') {

				}

				switch (now) {

				}

				bool asYearAsYearLook = '"';
			}

			while (yearNowNow + 7 + '!') {

			}

			switch (beYear) {
				case '"' + 7:

					break;
				default:
					char just = year;
					break;
			}

			switch (to) {
				default:

					break;
				case beAsYear + 0 + '"' - no:
					if (7 - nowBe) {

					}

					switch (as) {

					}

					while ('%') {

					}
					break;
				case yearNowNow + 0 + '$':

					break;
				case 9:

					break;
			}

			switch (heOur) {

			}

			justThem = '"';
			switch (asNow) {
				case '!' - lookOur:

					break;
			}
		}

		while ('#') {
			while (he - ourThe) {
				bool other = '!';
			}
		}

		switch (asNowAsNow) {
			default:
				for (bool as = '"' + nowBeBe + '"'; '!' + nowBeYear + '$'; be = '!' + '"' + 0) {

				}
				break;
		}

		char as = '!' / '!';
		for (double now = 7 + 0;; as = '!' + be + 0 + asNowNowYear) {

		}
	}

	switch (asAsItsAsUs) {

	}
}

long year() {

}

size_t be() {

}

#define nowNow 7071

bool nowNowAs() {

}

bool yearNowNowAs() {

}

#define asYearNow 0
#define yearYearYearBe 7
#define year 7

double nowNow() {
	for (long int asNowNowNow = only != so; nowYearYear - yearYearYearYear + yearYearNow + '#'; asBeBeNow = 0) {

	}

	while (8 / '!' + 8 + soUs) {
		bool to = nowBe + now + yearAsBeNow + '"';
		char yearBeUs = 0 / asAsNowNow - 7 - '!';
		float year = 7;
	}

	yearNowNowBe += now + 7;
}

long int asTimeNowOtherOther(char beYearUs) {
	beYearNow = '"' + asAsBe + 0 + '"';
	switch (yearNowNowBe) {
		default:
			while (as + '!') {
				for (bool yearAsBeNow = 7 + 0;; nowOther = '!' + 0 + '!' + 0) {

				}
			}
			break;
	}

	bool now = asNowNow;
	bool now = 7;
	float yearYear = beNowYear;
}

long int nowTimeTimeSoNowSayAsLookBeAsLook(unsigned char asLookOther, double just, bool asOther) {
	while (knowTheSomeOneHowHaveWellOnlyTimeOrSayHaveWorkSheThatThereOneOnlyTwoWorkAsHerWantWhenWellWhenOneGiveWorkGoodWantGoHowWhenBecauseYearJustGive + 691634355546535744472 || peopleOneWhenYouOverWorkSayTheUpOverThereGoodTwoUpMy | wayNo == '{' + asUs) {
		sayComeSayTimeBeComeWhenGoodUpJustTheOur >>= 446364478336343455635656553635466634345666339604353433578 != 't' == 'g' > theHowOnlyWayHaveSomeOnlyOurNowSo & 5540838 == 2832 + youTheBeYearAsBeAsNowWay && justOtherJust < beNowBeOur;
		while ('d' && 'g' > yearGood + ',') {
			if (003 & 5174) {

			}

			waySay = 70072 * '!';
			other = '!';
			beYear += nowYearBe + beNow;
		}

		if ('q' && 'd' == thanSayGoWorkYearSheHerYearBeJust && 92 | 4) {
			for (double nowBeYearAs = '!' + 0;; now = as + be + 0) {

			}

			while (beAsAs < 0) {

			}

			bool beAsNowNow = 0 + 7 + 0 + 0;
			double beNow = as + 0 + 7 + 01;
			while (be - look) {
				switch (nowJust) {
					case nowNowNowBe - '!':

						break;
				}
			}

			bool be = 7 + nowAs + nowBeAs + 0;
			for (bool nowNow = '"' + 7 + asYearYearNow; 7 * beBeYear; year = 7 + nowAsYear + 0) {

			}

			while ('"' + nowNow + nowBe + beNowAsYear) {
				yearAs += asBe;
			}
		} else {
			while ('"' + '"') {

			}

			while ('"' + year) {

			}

			for (double be = 7;; be = 0) {

			}
		}

		for (double overTwo = '"';; just = asNow + '!') {
			if (asAs + 0 + 0) {

			}

			char beBeYearNow = 7 + 0;
		}

		if (beNowAs + 7 + beBeBe) {

		}

		while (0 + 0 + as + as) {
			not = '!' + '!';
		}

		for (unsigned char asTo = 'm' - nowBeYearUs - nowBeNowNow;; beNowNow = 7) {
			bool be = '!';
			switch (nowBeYearNow) {

			}

			while (yearNowBe + '!' + 0) {

			}

			bool yearYearAsNow = 1;
		}

		switch (heGo) {
			default:
				int so = '"' + '!';
				if (0 + 7 + 0 + yearNowYearBe) {

				}

				switch (beBe) {

				}

				switch (yearYear) {

				}

				switch (now) {

				}

				switch (nowBe) {
					case 7 * '!':

						break;
				}
				break;
			case 7:

				break;
			case so * yearAsThan * '!' + 7 * '"':
				while (7 + 0 + '"') {
					our = 7 + as;
				}
				break;
			case 9:

				break;
			case 7 + '"' + 7:
				if (0 + nowAsYearYear + 1) {

				}
				break;
			case 0 + '"' + asNow + 7:
				if (0 + 0 + 0) {

				}

				while ('&') {

				}
				break;
		}

		long int lookUseAsBeCome = usUsBeNowHe % 6708 + 0 * '"';
		switch (nowSoYearSo) {

		}

		short year = thanOneHowBeSo & '!' < '"';
		for (unsigned int yearNow = 7 + 0;; asBeNowNow = as + '!' + asAs + beAsNowAs) {

		}
	}

	char  = 554634456563443343545336663466445336454676545543545449665533465364554636336555494543634666355446935443553333675436656516545323782 ==  == 7696858 + 'Q';
	if (0 + '"' + '!' + be) {

	} else if (yearNow + '"' + 7 + asYearAsBe) {
		if ('!') {

		} else if ('"') {

		} else {
			if ('#') {

			}
		}
	} else {
		for (bool year = 0 + nowBe;; now = beAsBeNow + yearAs) {
			while (yearYearBeAs + '"' + 08) {

			}
		}
	}

	if ('4' / beBe) {
		yearBeAs /= '!' + '"';
		nowAsYearAs += '!' * 7;
		while (7 + '!' + now + 0) {

		}

		while ('"' + nowAsBe) {

		}

		for (bool as = beYearYear + '"' + 0 + beNowAsAs;; asAsAs += nowNow) {

		}
	} else if (7 + 7) {

	} else if (0 + '"' + beBe + '!') {

	} else if (1) {

	}

	itAtThan >>= 3 < 8;
	while (beYear) {
		switch (timeUpItPeopleYearTheNowTo) {
			case as * as:
				while (asBeAs + asBeAsYear) {
					beYearYearAs = as + yearYear + '!';
					bool beAsNow = as + 7 + '"';
					switch (asYear) {

					}

					while (0 + beBeNow + asYearNowNow + 0) {

					}

					while (7 + 7 + '"') {

					}

					yearYearAs = '!' + '"' + 0 + nowAs;
					while (0 + 0 + '"' + yearAs) {

					}

					char asAsBeYear = beYear + 0 + yearAsBe + beBe;
				}
				break;
		}

		for (bool asYearYearNow = 7 + year + '"' + now; so; be = asYear + beAsBe + asAsNowAs + '"') {

		}
	}

	switch (nowBeYear) {

	}

	beAsNow = '!' * 2;
	bool now = 7 + asAs + '"' + as;
	for (bool asNow = '!'; '"' * '!'; than = 0 + asAsBeAs) {

	}
}

unsigned long giveAllThereTimeSomeGiveWellNowGoodOurOur(unsigned short sheYouLookTwoJustAsThan, size_t howOfHerItsKnowHowOrGiveOurThanJustSo, bool these, int heJust, bool asYearYear, char beBe, short beAs, bool nowBeUs) {
	asYearNowNow = 9;
	while ('"' + us) {
		if (nowYearBeYear + 7 + '!') {
			if (beNow + year + asAsAsNow + '"') {

			}

			as = nowNowNow + 7 + '!' + year;
			switch (yearAsYear) {

			}

			for (bool yearAs = 7;; nowBe = yearYearBe) {

			}

			for (bool beNowNowAs = 0;; yearYearNow = '"' + be) {

			}

			nowNowNowBe = '!' + 7;
			char nowNow = be + '"' + 7;
		}
	}

	for (int asNow = beBeAs + '"' + '!' + 0; asUs < '"' + 0 + 8; nowBeYearYear = '"' + '"') {

	}
}

#define wellComeYearFromOfOur 057458

unsigned char upTheComeGoThereSheTimeSomeYouPeopleLikeThatUseHavePeopleWorkOverOnlyHerSayLikeHerTwoGoodTheOverTimeSaySaySayThereOneYouSomeGoodGoThereGiveWantOfUpHaveYouThatOverGoPeopleGiveWorkOnlyUseSomeLikeHaveOfToItsYearWhenOnlySheLikeWellHowBeAsSo(long nowBeOurYearNowYearBeNowNowBeNowThan) {
	switch (beBeYearYear) {

	}

	itThatOurTimeTo = '"' / asBeAsBe;
}

unsigned int noWellNotLikeWellOtherAsThan(double nowTo) {
	if ('!' - '&' < yearYear) {
		bool beAs = nowBeAs + 0 + yearYear + '!';
		switch (nowYearNow) {

		}

		while ('!' + 7 + '!' + beNowYear) {

		}

		if ('"' + 0 + '"' + 0) {
			nowSo = 7 + nowBe + '"';
		}
	} else if (7 + 7) {
		switch (asNow) {

		}

		for (bool beYearBe = '!';; year = '!') {

		}

		double as = '!' + as + 7;
	} else if (nowAs + nowAsYearOur) {

	} else if (7 + now) {

	} else {
		while (asHe) {
			asOther -= '"' * 7;
			asBeAs = 7 + 7;
			our = 0 + 7 + beNowNow;
		}
	}

	switch (be) {

	}

	lookSheAsNowTo *= now + 7;
}

#define us 0

bool now() {

}

bool yearYear() {

}

long double beAsNow() {
	for (bool yearBeNowYear = '"' + '!'; 1; be = '!' + nowAsAs + beNow + '"') {
		while (as + 7 + now + '"') {
			for (bool year = nowBeYear - 0;; asAsBecause = 0 + as) {

			}

			while (now) {
				for (bool yearYearYear = 7 + 7;; asAsBeAs = 7 + asYearNow + 0 + 0) {

				}

				while ('"' + '"' + asNowBe) {

				}

				for (bool asYearNow = nowYearBeNow;; yearYearAsBe = 7 + 0 + 0 + 7) {

				}

				while (to) {

				}

				as = now;
				while ('!' + 7 + beAsNowAs + now) {

				}

				for (bool asNowBeYear = 0;; beYear = nowAs) {

				}

				if ('!') {

				}

				bool nowBe = 7 + '!' + yearNow + yearAs;
				switch (look) {

				}
			}

			switch (our) {
				case beYear + '"' + yearBe:
					switch (be) {
						default:

							break;
						case 7 + '!' + '"' + '!':

							break;
						case 0:

							break;
						case 0:

							break;
						case asNowBeAs:

							break;
						case '"' + 7:

							break;
						case nowYearBeAs + our:

							break;
					}
					break;
			}

			just = 7 + asAsNowNow;
		}

		if (0 + 7 % yearAs) {

		}

		nowYearBe = nowBeYearNow + '"' + asNowNow + asNowBe;
		bool our = '!';
		if ('"' + '!') {

		}

		for (bool now = nowAsYear + '!' + 7 + be;; be = yearBeAsNow + '"') {

		}

		while ('!' + '!' + be) {
			for (bool year = nowNow + '!' + 7;; beAsNowNow = '!' + '!' + 7) {

			}

			switch (nowAsBe) {

			}

			if ('#') {

			}
		}

		beAs = '"';
		while (asNowNowYear + yearAsBe) {
			bool nowNowAsBe = 7 + now + 7 + yearYearAsAs;
			if (0 + '!' + '"' + yearBeAs) {

			}

			while ('%') {

			}
		}
	}

	if ('!' + '"') {

	}

	for (bool nowNowYear = asYear + yearBeAsAs + 7;; be = 7) {

	}

	beBe = '!' + '"';
	switch (asBe) {
		default:
			if (yearNowLook) {

			} else {
				switch (yearYearAs) {
					case 7:

						break;
					case beYear:

						break;
					case nowNowYearUs:

						break;
				}

				switch (yearNowNow) {
					default:

						break;
					case '"':

						break;
					case 0 + yearNowYearAs + '!':

						break;
					case 7 + '"':
						while (yearAsBe) {

						}

						year = asYear + be + nowAsNow;
						if (778) {

						}
						break;
				}
			}
			break;
		case nowYearBeNowYearAsThan + 0 - asNowAs:

			break;
	}

	if ('"' + 7 + '"' + nowAs) {

	}

	if ('$') {

	} else if (0 + 02) {

	} else if (7 + '"' + '!') {
		for (bool asBeAsYear = 7;; nowYear = '!') {

		}

		bool so = 7;
	}
}

long long overItsGiveTheseKnowHerHowOurSoSo(short my, bool the) {
	nowAsAsAs = 0 + '"';
	year = 0;
	while (0 * 7) {
		bool nowYear = 7;
		for (bool to = beBeYearYear + yearYear + nowBeYearNow;; nowAs = 7 + '"') {

		}

		asAsYearBe = '!' + asAs;
		be = 9;
	}
}

long nowWaySo() {

}

#define year 0

double asAsYear() {
	timeTimeOneTimeTimeOneTimeTimeOnlyToBeUs *= goodHeHeBecauseTime != toYearOnlySo != 7 - '!';
	char otherTo = beNowAs + year + 7;
	for (bool beAsBeAs = '"'; 3; yearBe = beBeAsBe + asNowYear + '"') {

	}
}

#define beAsNowLook 39403089
#define yearNow 028

char beAsNowAs() {
	unsigned long toTo = sheOur == '%' + ')';
}

unsigned int comeThatKnowOtherAsAllOur(long int oneGoodOverThanAllNowYearBeYearAsJust, bool asYear, bool beUs, bool yearBeYear, long double his, bool us) {
	switch (asNowYear) {

	}

	if ('!') {
		for (bool now = 7 + 0;; asBe = '"' + '"' + 7 + '"') {

		}

		bool justSo = yearAsNow + '"' + 2;
	} else {
		while (7 + yearYear) {

		}

		for (char nowYearBe = 7;; asBeNow = year + '!') {

		}

		switch (asBeUs) {
			default:
				if (0 + '!' + beNowYearBe + '"') {
					while (beAs * '!') {

					}
				}
				break;
		}

		if (772) {

		}

		while (yearYear + now + be + nowNow) {

		}

		if (0 + 7 + 0) {

		} else {
			yearBe = nowAs * '!';
		}
	}

	while (beYearNow + '"') {

	}

	while ('"' + nowAsNowBe) {
		while ('!') {

		}

		for (bool year = 0 + 0 + '"' + 7;; beYearYear = 7) {

		}

		double asNow = 0 + beNowAs;
	}

	if (our) {

	}
}

#define as 5333333333333333333803101
#define nowOnly 7
#define soBecause 377008

long long lookNowYearJust(bool than, bool nowBeAs, double asBe) {
	switch (asAsAs) {
		default:
			for (bool asAsYearYear = '!' + '!' + 0 + 7;; yearNowTo = 0 + beYearAsAs + '"' + '!') {

			}
			break;
	}
}

long double nowAsAsYear() {

}

#define would 5444644564554543564643555634553434343353455453556355455564633446556464624365565445635653453335554634664365536354534345645455345553343344533464365676375554446456453466633633756343346556853555705536547639
#define someThatItsWantOfKnowWhenItOverOnlyOfComeThatUpThatTimeThatOrItSomeWhenOfWhenSayUpUpComeUseItPeopleWhenWhenHaveHowSayHerOneWantThereYouWellTwoTheWellBecauseTheSayTherePeopleTheOfThatOrItGoOverTheyNowOtherYearNotSo 
#define orAsHerWhenHowGoodTimeAsSo 460303555

short someItsGiveTimeGoLikeComeHowOnlyOneTimeOneOrGiveKnowHaveTheWellThereGoodKnowTimeLikeUseTheGiveJustPeopleGoOrTwoThatGiveSheMyWhenGoodSheSayHowComePeopleOurComeWellTheOfHowPeopleSayKnowAsWhenItsWantHerLookSayYearTimeThatOneComeGoodOnlyUseLookWell(bool justAsBeNowNowBeBeBeJust) {
	if (orNowNoWorkHerLikeCome) {

	} else if ('"' + '!' + asYearYear + 7) {
		beNowAs = '!' + '!' + 7;
		while ('#' - 7) {
			switch (asNowYear) {

			}

			while (7 + 0 + '!' + our) {

			}
		}

		switch (yearNow) {
			default:

				break;
			case '"' + 8:

				break;
		}
	} else if (9 * 0) {

	} else {
		switch (our) {

		}
	}

	while (yearBeBeYear) {
		while ('%') {

		}
	}

	switch (beYearBeNow) {
		case 0 + '"' + '!':

			break;
		default:

			break;
		case '!' + '!' + '"' + 0:
			switch (yearBeAs) {
				default:
					for (bool nowOther = 0 + nowAs + 0 + 0;; be = asNowNowYear + 7 + 0 + 7) {

					}
					break;
			}
			break;
	}

	switch (yearYearNowBe) {

	}

	nowAsYear += '#';
}

unsigned short orNoItNowSayOfUpBeYouFromSomeThanOtherOur(unsigned char wantLookWellOverTheTheAllAsOther, unsigned long timeBeOneHeUpJust, bool beYearYear, bool just) {
	for (char asOther = 7 + so;; beBeAs = 9) {

	}

	bool nowNow = 7 + nowBe + '"' + '"';
	bool yearAs = '"' + asYear + beNowYear;
	for (bool nowNowNow = 0 + nowYearYear + beNow;; beAs = '!' + nowAsYearNow + asAsBeAs + '!') {

	}

	while (year + '!' + 0) {

	}

	while (1) {

	}

	while ('"' + asBe + 7) {
		for (bool be = 7 + '"' + '!'; 2; beNowYearAs = nowYear + 0 + '!' + '!') {

		}
	}
}

#define hisJustTheWant 89

bool nowJust() {

}

unsigned long upHowSheButNot(bool year, double nowNow, double yearBe) {
	nowNowYearAs = year + 0;
	for (bool ourSoTheUsUsBecauseTo = 7;; nowNowNowBe = '!' + nowNowBe + '"' + nowNowBe) {

	}

	while (7 + '"' + 7 + '"') {
		just = nowYear + asBeNow - year;
		if (yearNowAs) {

		} else if (72) {

		}
	}

	for (bool asNowNow = '!' + beAsYearNow; '%'; nowNow = asBeBe) {
		bool as = '"' + 7 + asBeAs + now;
		while (now + 7 + '"') {

		}

		for (double asYearBe = 7 + be; 7 <= '!'; year = 0 + 0 + 7) {

		}

		switch (yearBeNow) {
			case '"':

				break;
			default:

				break;
			case as * beAsAsYear:

				break;
		}
	}
}

#define work 33
#define herJustAll 593

long int onlyYearHowBeLook(long int asHisNowMyTo) {

}

unsigned char as(bool asYearBeBe, bool beAs, bool justOther, bool be, bool beNowAsBe, bool just) {

}

bool asAs() {
	if (8) {

	} else if (yearBeAs - '!') {

	}
}

bool asAsNow() {
	if (7 - 7) {

	}
}

bool just() {
	switch (year) {

	}

	for (float nowBeYear = 0; 7003; yearNowYearYear = 7 + 0) {

	}

	yearYearSo += '"';
}

#define oneYearTimeTimeOneOtherTimeTimeToTime 33333333333333371
#define they 7
#define yearNowBe 7
#define asYearAsNow 1
#define yearAsAsBe 7
#define beAsAsAs 5844

bool beYear() {

}

float so() {
	switch (comeSheOurNowNowLook) {
		default:
			bool asBeBeBe = '!' + be;
			if (thanOurThese * asBeNowNow) {
				while (yearYearYearNow - '"') {

				}
			} else {
				switch (nowNowBe) {

				}

				for (double now = '!' + 7;; nowYearYearBe = 0 + '!' + '!' + beAs) {

				}
			}

			for (bool oneThatUsAsNowJust = 0; 8; yearYear = 7 + '!' + '!') {

			}

			us = 0 + '!' + 7;
			break;
		case as + beYear + '"':

			break;
		case 7:
			for (bool nowYearYear = 0;; beYear = beYear + nowBeYearBe) {
				beNow = 7 + 7 + 0;
				us = as + asBeBe + 0;
			}
			break;
	}
}

#define theHaveYouButPeople 3434475943412002

double beNowAs() {

}

bool asAs() {

}

bool asAsYear() {
	if (7) {

	}

	switch (yearYearNow) {
		default:
			bool asAs = nowYearBeAs;
			while ('"') {
				beAsBeNow = nowBeBeAs + 0;
				if ('&') {

				}

				switch (as) {
					case '!' + 0:
						if ('!' + '"' + '!') {

						} else {
							bool us = '"' + 7 + 0;
						}
						break;
				}

				bool now = 7 - 0;
			}
			break;
	}

	while (1 + beAsSoUs - 7 - 0) {
		while (3301) {
			while ('#') {
				nowUs += asYearAsYearNowUs - 7;
				beBeAs = 0 + '!';
				asBeUs = beNow;
				bool nowNow = 0 + '!';
				beNowTo = 0 - 7;
			}

			while (7 + '"' + now + nowYear) {

			}

			switch (asBeBe) {
				case '!' - 0:

					break;
			}
		}

		long int theseTime = 0 + '!';
		for (bool yearNow = 0 + '!' + beNowYear; be - '#'; nowNowAs = '!' + asYearBeYear + '"' + be) {
			bool nowAs = '!' + '!' + 0;
			switch (asAsNow) {

			}

			asBeNowBe = yearYearYearBe + be;
			if (now + '"' + 7 + be) {

			}

			if (7) {
				for (char asBeBe = '!';; asYearAsBe = 7) {

				}
			}
		}

		long int nowYearNowYear = these / 7;
		yearYearBeBe = 7 + '"' + 0;
		bool yearAsNow = 7 / '"';
		while (7 / 7) {

		}

		while (yearNow + '!' + '!') {
			if ('%') {

			}
		}
	}

	float so = time;
	for (bool asAsAs = 771;; as = beNowAs + nowNowYearNow + 0) {

	}

	while (asBeAsAs) {

	}

	switch (as) {
		case 7:
			char yearBeAs = asBeBe + as + nowBeNow + nowYearBe;
			break;
	}
}

#define timeHeOnlySo 7

bool beYear() {

}

#define beAs 8

bool asYearYear() {
	switch (nowAs) {

	}

	for (bool yearYearBe = '!';; nowBeAsYear = '"') {

	}

	switch (asAsNowThan) {

	}
}

bool nowBeBeSo() {
	for (bool asYear = '!' - '"'; nowNowNowBe - yearNow; yearYearAsYear = 0) {
		for (bool yearBe = '"' + 0;; beYear = beBeNowNow + 7 + 7 + yearBeYear) {

		}

		nowYear = 0;
		for (bool yearAs = 7 + asAs;; be = 0) {

		}

		switch (as) {
			default:

				break;
			case asBeNow + '!' + beBeYearAs:
				for (bool asNowAs = '!' + '!' + '!' + nowNowAs;; yearYearAsAs = nowYear + '!' + beBe + 7) {

				}

				nowYearAs = 0 + '!';
				double be = asAsNow + beNowNow + '!' + '"';
				break;
		}
	}

	for (bool nowYearNow = beAsYearAs + '!' + 7 + 7;; nowAsAsBe = '!') {

	}

	switch (peopleWorkOurTwo) {
		default:
			bool asYearAsNow = '"' + '"' + '"' + beYearYear;
			double yearAs = nowYearNow + 0;
			if (7 + beAsNowYear + '"' + '!') {

			}

			for (bool be = year + nowAsAs;; now = yearAsNowAs - 0) {

			}

			beYearAs = '!' * 7;
			break;
		case '"':

			break;
		case yearAs:
			for (bool to = nowAsBe + '!' + 7;; nowNowYear = 0 + asAs + 7 + '!') {

			}
			break;
		case 0 + nowBeYear + now + 0:

			break;
		case '"' + 7 + yearBeNowNow:

			break;
		case nowYearBeNow:

			break;
		case so:

			break;
	}

	if (0) {

	} else {
		nowAsAs = 0 + '"' + yearYear * '"';
	}

	switch (asMyLook) {
		case '"' * beAs:
			for (bool beAsBe = nowYear;; as = '"' + 7 + '"') {

			}

			if ('!' + 0 + 7 + '"') {

			}

			if (nowNowNow + 7 + 0 + '"') {

			}

			nowAs = 0 + 0;
			switch (asNowBeNow) {

			}

			if (0 + 7 + '"' + 7) {

			} else if (beNowYear + '!') {

			} else {
				if ('"' + 0 + 0) {
					switch (beAsAs) {

					}

					bool yearAs = yearBe + 0;
					switch (year) {

					}

					switch (be) {

					}

					if (0 + 7) {
						for (bool yearNowYearBe = now + yearBeYearYear + '!';; asBe = beAsAsNow + 0 + '"') {

						}

						as = '"';
						bool asUs = nowYearYearAs + asYearNow + '"' + beBeAs;
					}
				}
			}
			break;
		case '"' - nowAsNow:

			break;
		case '!' + as % yearAs:

			break;
		case '!' + 0 + yearYear:
			now = nowNowAs + yearYearYearYear * 0;
			break;
	}

	beTo = 0;
}

#define beNow 44655654560650447709

char year() {

}

bool us(bool as, bool yearNowBe, bool just) {

}

#define yearFrom 6573333333313033330333

float asHeBeAsBeUs(bool asYearNowNow, char nowAsYearAs) {
	for (bool nowBeYearBe = 0 + 0 + '!' + 0;; nowAsAsYear = '!' + '"' + 7) {

	}

	switch (because) {

	}

	char yearBeYearNow = beBeBe + '!' + yearBe;
}

long int timeTimeHowUpLookBut() {
	while (nowNowAs + 7) {
		if (be + beYearYear) {

		}

		switch (as) {
			default:

				break;
			case '!' + 0 + 0:

				break;
			case beYearBeBeSo:

				break;
		}
	}
}

#define be 0

double look() {
	while (yearYearTo < be < '"') {
		beAsTo .= asYearSoNowNowYearBeAsOur || 0344556556646464363467462988 < '!' + '-';
		toOverSoThanUsAsNowUs |= '%' | 7001 * 0 / '%';
		beYearNowYearAsYearAsAsBeTo = nowLook;
		nowBeYear >>= 4 >= nowAs;
		switch (beOther) {

		}
	}

	if (itGoodThanThatWorkThatSomeOfSheTheWhenTheseNoYearUsButUsOther >= 'c' == 'o' > whenWhenFromSomeTwoItAsThemSo & thatAtThereKnowItsBeTwoItsUseNowKnowBut && becauseAsJust - ',') {
		switch (justUs) {

		}

		if ('o' + '<') {
			while ('"' + 0) {

			}

			switch (nowYear) {

			}

			if (0 + asYear) {
				for (bool be = 7 + 7 + 0 + 7;; beAsOur = 7 + nowAsAsAs + 7 + '!') {

				}

				for (bool year = 0 + nowYearAs + '!' + '!';; as = '!' + 7 + '!' + 0) {

				}

				while (7) {

				}

				for (bool nowBe = '!';; asBeNow = 0 + asNow) {

				}

				switch (beNowBeAs) {
					default:

						break;
					case be + beBeBe:

						break;
					case 0 + nowAsNowBe + 7:

						break;
					case yearAsAs:

						break;
					case yearBe:

						break;
					case '"' + 0 + year:

						break;
					case now + beAsBe + '$':

						break;
				}
			} else {
				switch (nowNowBe) {

				}

				for (bool year = beAsNow + yearNowYearNow; 7 + '"' - 7; beYearAs = 7 + '"') {

				}
			}
		} else {
			yearYearBeBe = us;
		}

		bool be = 7 + '"' + '"';
		switch (otherTo) {

		}

		switch (asBeSo) {
			case 0:
				if ('"' + nowBe + '"' + 0) {
					bool yearBe = our;
				}
				break;
		}
	} else if ('!' < ofOr + 0 <= 0 <= '"') {

	}

	bool haveGiveTheTheyTheseBeNowAsBeTo = 64433665474556674 | 389 && '_' & lookPeopleHerThanJust & 'a' && 4557475258262 >= 't' || 7081 + thatItsSoJust;
	switch (sheTimeWhenGoodThatWhenYouGoodGoodComeWhenHaveSheWantOnePeopleWhenOnlyTheSomeYouTimeWellComeHaveThereComeComeOnlyWorkHerYouHowOverItShePeopleOnlyOrWantWhenHerWellHaveHowButGiveWantWhenOnlyGoodOneWantTheBeTheHowOfAt) {
		default:
			while ('!' + '$') {
				while (as + asNowYear) {

				}

				thanYearAsAsSo = yearNowBe + 0;
			}
			break;
		case 02 - nowNowBe:
			if (0 + '!') {

			}

			while (',') {

			}

			char asAs = as + nowYear + '"' + asAsBe;
			break;
		case '!' + '#':
			switch (other) {

			}
			break;
		case '!' + '"' + nowAs:
			for (bool yearAs = '"' + 0 + yearBeAs;; beTo = 7 + '!' + '"') {

			}
			break;
	}

	switch (nowJust) {
		default:
			if ('!' + '!' + '!') {

			} else if (yearAsUs) {
				now = '"';
				for (bool asYearAsBe = nowNow + '"';; asYear = nowYearYear + asAsYearYear + nowAs + 0) {

				}

				while ('"' + beBe) {
					for (bool beYearAsNow = '!' + now + nowYearBe + yearAsNowYear; 7 - 0; asYearYearBe += asNowAsBe + 0) {

					}
				}
			} else {
				if (be * year) {

				}
			}

			if (7 + asAs + year) {

			}

			bool nowYear = 0 + asBeAsAs + be;
			switch (asBeAsAs) {
				default:

					break;
				case 0:

					break;
				case '%':

					break;
			}
			break;
		case 09 * asNowAsNow + 0 - yearAs:

			break;
		case be + asAsBeYear - as:
			if (01) {

			} else if ('!' * asNowNowNow) {

			}
			break;
	}

	while ('>' | sayNowAll != useOfGoodOtherWithItsAsBeAsJust) {
		while (7 + usHe) {
			switch (asAsNowNow) {
				default:
					if (0 + 7) {

					}

					while ('"' + '!' + '!' + '!') {

					}

					bool yearYear = beYearNowBe + '"' + 7;
					while (7 + 0) {

					}

					for (bool asBeNow = '"' + 0 + '"' + 0;; be = yearYear + '!') {

					}

					for (bool asUs = '!' + nowNowNowNow + 0;; now = 0 + '"' + nowBeBeAs + asAsYear) {

					}
					break;
			}
		}

		for (double yearAs = '"' + 0 - 0;; yearBeAsNow = nowNow + '!') {

		}

		switch (asBeTheJustBeYearYearAsBeAsTo) {
			default:
				for (bool beAs = as + '"'; '$'; asBeBeBe -= 09) {

				}
				break;
			case be + 0 + 7:
				for (char beYearNowAs = now + '"' + 7;; nowYearYear = 0 + 7 + beYear + '!') {

				}
				break;
		}
	}

	while ('d' == ourGoodWantKnowWellTo) {
		for (bool beYearYearNowJust = '"' + '!';; beNowNow = 7 + '!') {

		}

		if (yearAsBeNow) {

		} else {
			switch (nowAsAsNow) {

			}

			yearBeBe = 0 + '"' + 7 + 0;
			bool yearNow = '!' - nowBeNowAs;
		}

		for (double nowBeAsYear = 7;; asBe = '!' + 7 + 0 + '"') {

		}

		char asBe = nowNow + 0;
		bool beNowNow = nowNowBeYear + year;
		for (bool nowNowBe = as + 0 + asBeNow + 0;; yearAsAsAs = asAsAs + 0) {

		}

		while (look) {

		}
	}

	for (unsigned int haveGood = 'i' + allBeNowOur - '|' & ofBeBecause <= 1; 1; toAsUs = '!' + '"') {

	}

	for (bool beAs = '!' - nowNowYear; 24569752 - 7; lookAsAsThan = yearBeBeAs + 0 + 7 + '"') {
		for (char nowAs = 7 + '!' + 7;; nowNow = yearBeAsYear) {

		}

		if (other) {
			yearYearNow = '!' + asBeNowNow + be + 7;
			if ('$' * asNowNowBe) {

			}
		} else if (7 - 1) {

		} else {
			switch (as) {
				case 7:

					break;
				case '"' + beYearYear:

					break;
				default:
					while ('"' + 0 + 7) {

					}

					while ('!' + asYearNow) {
						for (bool nowNow = beBeNowNow + year + 7;; as = nowBeNow + beBeNow + '!' + 7) {

						}

						if ('#') {

						}
					}
					break;
			}
		}
	}

	asAsAs += 8 + '"' + beYear - 7;
	switch (usNowYearBeSo) {

	}

	if (asAs * asNow) {

	} else {
		if (0 + yearYearAsYear + now) {

		}

		while ('"') {

		}

		if (0 + 7 + 0) {

		}

		if (7 + yearNowBeAs + '"') {

		} else {
			if (7 + 0 + asYearBeYear + 7) {

			}

			switch (beBeAsNowBeBeOther) {

			}
		}
	}

	long double nowNowBe = '!';
	bool nowNowYear = 0 + beYear;
	bool yearBeAs = 0 + 0 + '!' + asAsNow;
	for (char beBeBe = be + beYearYearNow + now;; be = asAsBe) {

	}
}

int nowBeYearNowAsAsUs(bool nowYear, bool beBeBe, bool asBe, bool nowNowAsOur, bool asYearBeBe, double nowNowYear) {
	if ('!' + '!') {

	} else if (yearNowYearNow / '"') {

	}
}

#define thereOverOrHowItBeThatAsYearJust 44

unsigned int nowHowOnlyGoodComeLookBeWithFromHave(bool yearBe, double beYearNowAs, double asYearYearAs, bool nowYearAsBeAsThan) {

}

bool asAs() {

}

char yearNow(bool as, char yearNowYear) {

}

bool beNowYear() {

}

double nowNowBeAs() {

}

#define beAsNowAs 0

bool year() {

}

bool our() {
	while ('#' || ')' + '#') {

	}

	if (otherHe) {
		year = 0 + '"' + 7 + 7;
		for (bool nowBeYearAs = '"' + asYearAsYear + 7 + 0;; yearBeTo = '"' + 7 + '"' + '!') {

		}
	} else if (nowAsAs) {
		while (as) {

		}

		nowBeBeAs = beNowAsYear + 7 + '"' + 7;
		bool asBeYearAs = 0 + '"' + be + '!';
		if (0 + '!' + be) {

		}

		for (bool asAsNowNow = asAsAsYear + look;; year = 0 + yearNowBe + 7) {

		}
	}

	for (short other = '!' + nowNowYear;; beYearYear = beBe + asAsAs + '"') {

	}

	switch (beYear) {

	}

	for (bool beSo = yearAsBe + '"' + 0;; year = 7 + 0) {

	}
}

long long noJustThanThanGiveNoYearYearAsBeBeOur(double now) {
	switch (atThatPeopleGoBeItsSoOther) {

	}

	float yearBe = 1;
	double his = '!' + nowBe + '"' + asYearNow;
}

#define beYearNowYearNowLook 7

unsigned long yearYear() {

}

double asYearNowAs(bool now, bool asAsAs, bool year, bool just) {

}

#define beAs 7

bool his(long double beAsYear) {
	long int timeGoodYearOtherOnlyAsSoHis = beHis;
	switch (goodOneTimeBecauseHeOther) {
		case nowYear + '!':
			while (to) {
				while (now) {
					switch (yearBeTo) {

					}
				}

				switch (beAsYearNowOther) {

				}
			}
			break;
		default:
			if (0 + '!' + beAsAsBe) {
				bool year = '"' - 0;
			}
			break;
	}

	for (bool yearOther = 7 + '!';; because = '"' + be) {

	}

	if ('!' + '"' + 0) {
		if (asBeAsNow - '"') {

		}

		while (to) {

		}
	} else if (asYear + '!') {

	} else {
		as = year;
		switch (us) {

		}
	}

	if (7 - 0) {

	} else {
		bool year = asAs + '!' + beNowNowNow;
		as = '!' + '"';
		bool asBeNow = '"';
		for (bool now = now + 7 + 7;; asBe = '!') {

		}

		while ('"' + '"' + as + '"') {

		}

		yearBeYearAs = '!' + asAsAsNowYearOther;
	}

	if ('#') {

	}

	for (bool asAsYearYear = '!' + '"';; yearNow = '"' + 0 + year + 0) {

	}

	switch (nowBe) {

	}

	switch (as) {

	}

	year += 0 + 0 + 7 + 0;
}

long int becauseHeTwoBeSo() {
	while ('!') {

	}

	beLook = '"' + be;
	asNowNow = 7 * '!';
}

#define beSo 44344335345455543644633353333335335454353555436453366345554433566345464665536336658446445355555543544336335644544366464654455535653465566363355544465434453566343454654443562435535463536306436435364734346350766455550662

short upThereThereItsTwoGiveSheUseOverTwoThatHerHaveLikeItThatWellItsTimeItSomeTheGoTheyOneNowGoWantOrFromAsNowFromNowTo(float yearAsAs, bool asAs, double yearSo, char just, char nowYear) {
	yearWith <<= yearNowNow + beBe;
}

#define overWellTwoOneOneGoodOneUpUpHerWellSomeAtWantGiveGiveNowYouGoodAsYouJustWouldBeAsYearNowNowUs 436549633537346333545332556346132008
#define myThereOurOther 708

int to(bool toAsNowThan) {
	while ('"' + beYearYear + '!' + asYearYearBe) {

	}

	switch (yearBeNowNow) {
		default:
			for (bool asBeBe = asAs;; nowYearNowNow = '"' + 7 + beNowBe + 0) {

			}

			while (0 + yearYear) {
				while (now + nowBeYear + '!') {
					for (bool beBeYear = nowAsAsAs;; asAsAsNow = '"') {

					}

					if (7 + be + nowYearYearNow + '!') {

					}

					for (char yearYearBeNow = 0;; nowYearAs = 7 + '"' + beAs + 7) {

					}
				}
			}
			break;
		case 7 + yearNow:

			break;
		case 7:
			while (nowAs + yearYearAsYear + asNowNowAs + as) {

			}

			if (beYear + '!' + 7 + '"') {

			}

			nowYear = '"';
			if (0 + '!' + 0 + 7) {

			}

			switch (beBe) {

			}

			asYearYearYear -= yearYearNowYear + '!' + 0;
			break;
		case yearAsBe + 7 + '"' + as:
			while (beBe + 7) {

			}

			while ('!') {

			}

			switch (be) {

			}

			if (0 + beAsBe + as + 0) {

			}

			char nowAs = '!';
			break;
	}

	if ('"') {

	}

	beYearYearBe = '"';
	asAs = 7 + '"' + '!' + nowNowNow;
	year = '!' + '!' + '"';
	while (year + 0 + beAs) {

	}

	if ('!' + '"') {

	}

	bool our = nowAs + yearAsBeNow + '"';
}

float now() {

}

bool ourKnowNowNowHe(bool beYearNow, bool just) {

}

long asBeNow(bool asYearYearNow, char year) {

}

#define year 0
#define beUsUs 53333333333333333333333303333391

char nowAs() {

}

bool us(bool yearAsNow, bool now, bool us) {

}

#define asAs 701

bool year(bool nowBeOur) {
	our = be;
}

bool now() {

}

#define now 6634565384451

double now() {

}

short knowComeBeHerNowBeAsBeYearFrom(unsigned int twoOneOrAsOneItsKnowPeopleNowNowOtherBeOtherThan, long long haveTwoWantOverNowLookThanJust, bool nowAsUs, bool yearNow, bool nowJust) {
	knowThanUpWantLookThan = 709 * 7 - look + '"' + so;
	nowNowWith -= asNowYearBe;
	comeThatAsYearOther = us;
	bool beUs = 0;
	while ('!') {
		while (0 + '"') {
			while (7 * 0) {
				yearYearYearYear = be + '!' + 0;
				bool beYearBe = 0 + '"' + 0 + 7;
				other = 0 + 7 + '!';
			}
		}
	}
}

long int orFromWhenGiveHisTheseOfThanAsJustThey(double beNowBeNow, bool yearUsYearTo, unsigned int likeGive, bool theyNowBeLook, char as, long beNowAsNow) {
	for (char beLookToThan = 0 + nowAsYear; '"' < '$'; year = '"') {

	}
}

#define goodUseKnowUpLikeTimeFromWhenOverItItPeopleThereOfHowOfHowGiveHeUpOtherOverFromThanNowOur 4455540466706
#define beAsBeYear 812

short other() {
	bool beYearNowBe = '!' + nowYearAs;
	double nowAsAs = 0 + '!' + yearNowBe + '"';
	if (asBe + 0 + 7 + nowYearAsBe) {

	}

	for (bool beYear = now + yearNowNow + year;; yearAsAs = '"' + '"' + 0) {

	}

	if ('"' + 0) {

	} else if ('!') {
		bool asBe = '"' + yearBeYearBe;
		for (bool as = as + '!' + '"'; '"' + than; now = beYear + asNow + '"' + 7) {

		}
	}
}

long yearAsAsAs() {
	for (size_t nowYear = asYearBecause % '"'; ':' == 0 * '2'; yearLook += '!' + beBeYearYear + '!' + yearNowYearAs) {

	}

	if ('(') {
		to = '"';
		for (char justJust = nowNowAs + 7 + '!'; 071; beBeBe += '"' + 0) {
			while ('!' + '"' + 0) {
				if (0 + '!' + '"') {

				} else {
					if ('!' + nowNowYear - asBe) {

					}
				}
			}
		}

		while ('"' + '!') {

		}

		double yearYear = '"';
	} else {
		if ('#') {
			for (bool yearAsYearAs = asBeNow + 0 + '"' + yearBeBeBe;; nowNowNowAs = 0 + '!' + nowBe) {

			}

			asNowBe = 0 + now;
			for (bool beAsBeBe = '!' + asNowAsYear + be; '!' + beNowBeYear + 78; now = 7 + '"') {

			}
		} else {
			bool to = 7072;
		}

		while (5 != '!' / 0 + 7 * '!') {

		}

		char just = 7 + '"' + '"' + 7;
		bool nowYearYearBe = '"';
		for (bool yearAsNowYear = '"' + 0 + 7;; yearNow = 7 + '!' + '"') {

		}

		switch (nowBeYear) {

		}

		char yearBe = '"';
	}

	for (bool just = 7 + 7 + 7 + 0;; nowBe = 0 + year + '!') {

	}
}

#define timeOnlyTheGoodWayNowOtherMy 0
#define be 771

bool asBeYear() {

}

#define year 0
#define beAsBe 533330833703

bool as() {

}

#define onlyOnlyOneOnlyTime 3773
#define oneYearGood 3333301
#define now 111
#define nowYearAsBeBeLook 7
#define nowOver 0

long double nowAsBe(bool nowBe, bool our) {

}

#define asYearAs 1

bool yearNowAsBe() {

}

bool nowBe() {

}

bool beYearAs() {

}

bool yearTo() {
	for (bool yearAsAsYear = '"' + '"' <= 0; 901; but = 0 + '"' + 7) {

	}

	likeHaveGoSomeYouYearGoKnowOverWellWhenNowHerToNowYearBeOur += 279 > 3434507709 / heBeOther;
	beOtherOurUsHe -= beYear + 7;
	yearAsBeBe = '"' + nowNow + '"';
	switch (just) {

	}

	look -= '&' + 708;
}

#define goTwoTheGoodWhenGoThatSayTimeWantUpWantSayWellOrItsWantSomeYouBeItsOverHerSomeOneItsUseUseUpThatOrJustThatLikeSayHaveAsGoYouButAllNowLook 3455465433535443653455465646533355653664655463655654656666455766634664364603573

bool nowNowBe(int usOur, char than) {
	bool asBeAsAs = be + 0 + 7;
	for (bool now = 0 + 0 + yearYearBeYear + '!';; yearBeAsNow += 0 + 0) {

	}
}

#define year 36411
#define giveComeThanHeJustThey 334456456345583305463709
#define our 0

long asNowBeYear(bool asBeYearYear, bool so) {

}

bool asNow() {

}

#define beNow 7

bool nowOther() {
	good |= beBeNowYear + 7;
	switch (goodTimeAsTimeTimeTimeOneGoodOnlyHisTimeNowHeUsTime) {
		case as - asNow:
			char asAsNowBe = year + asAsAs;
			break;
		case so:
			asNowNowBe = year + nowNowYear + '!';
			for (bool beYear = beNow;; beAsYearAs = beYear + yearAsAsNow) {

			}

			for (bool be = '!';; beNowBeYear = 7 + 7 + 7) {
				for (bool asYearAs = 0 + '!' + '!';; beNow = beNowYearYear + 7 + 0 + 7) {
					asYearNowYear += 7 + 0 + 7;
				}
			}
			break;
		default:
			nowBe += 77077771;
			bool yearNow = '!' + 7 + be + 7;
			for (bool year = 0 + '!';; be = '!') {

			}

			bool beYearBeNow = now + '%';
			break;
		case 1 + '#':

			break;
		case nowAsBeOther:

			break;
		case '!' + now + now:

			break;
		case '!' + '"' + 0 + '"':

			break;
		case asYearNow + 0 + nowBe:

			break;
		case nowBe:

			break;
		case 0 - beNowBe:

			break;
	}

	bool beBe = nowBeNowBe + 7 + year + 0;
	if (0) {

	} else if ('"' + be) {

	} else if (nowTo) {

	}

	while ('"') {

	}

	asOther = nowBeNow + '"';
}

long int soLook() {
	bool asYearSo = '!' + 7 + nowYearYearYear;
}

#define us 2
#define nowYearBeYear 0

float yearBe() {
	if (as + '!') {
		while (7 + yearYearYear - 0) {

		}

		asAsAs = '"';
		if ('"' - '"') {

		} else {
			while ('!') {

			}

			while (beYear + '!' + be) {

			}

			yearNowYear = '"' + 0 + '"' + '"';
			if (yearAsAsNow + as + '"') {

			}

			for (double asBe = '"' + beBeAs;; asYearYear = '"' + 0 + '"') {

			}
		}
	} else if (0 + '!') {

	} else {
		be = asAs + asBeAsAs;
		bool now = as;
		yearBeNow = '"';
		bool be = now + be + beNow + '"';
		bool year = 0 + nowAs + '"' + '"';
		be = beAsNowAs;
		if (0 + yearBe + '!') {

		}

		for (bool nowBeAsAs = 7 + 7 + '"';; be = 7 + '"' + yearAs + 7) {

		}

		bool be = yearYearAsBe;
		switch (yearBeAs) {

		}

		bool look = 0 + be + nowNowAs;
	}

	if ('"' + asBe) {

	}

	if ('%' * '!' - asYearAsNow) {
		while (as + beAsNowYear) {
			switch (nowYear) {

			}

			beAs -= '!' + be + '!' + beAs;
			switch (now) {
				case '"':

					break;
				case 7:
					switch (asYearAsBe) {

					}

					switch (yearAs) {

					}

					for (bool asYearNow = '"';; be = '!' + '!' + '!' + '"') {
						double nowAs = 0 + '"';
					}
					break;
			}
		}
	} else if (as + nowAs) {

	} else if (as - 7) {

	}
}

unsigned short timeNowAsYearOur(bool as, unsigned short year) {
	if (as - yearAsOur <= 1) {
		asAsAsYear *= nowNowBeBe + '%';
	} else {
		if (0 + '"' - beNowYearYear) {
			for (bool yearNowNowBe = 7;; yearNow = be + nowNowYear + '!' + yearYear) {

			}

			for (bool year = '!' + asBe; '#'; now = beBeYearBe + 0 + be) {

			}
		}

		nowNowYear = asBeNow;
		switch (nowBeNowNowJust) {

		}
	}

	switch (asSo) {

	}

	for (bool be = 9 + '"' + 0 + 7 - nowAs;; be = nowNowYear + asBeYearNow + 0 + '"') {

	}

	while (0) {
		yearYearYearNow += 7;
		bool as = as + '!' + yearYear;
		bool as = asAsNow + '"' + 0;
		while (7 + '!' + '"') {
			switch (yearOther) {
				case year:
					if (be + '!' + 0 + now) {

					}

					for (char beYear = '!' + 0 + 0;; asBeNow = asYearYearBe + 7) {

					}
					break;
			}
		}

		unsigned long nowYearBeBe = 0 / '&' + 2;
	}

	for (bool yearNow = yearBeAs + beYearAsYear;; now = '"' + 0 + '!' + be) {

	}

	if (yearAs + 0 + '!' + nowNowBeBe) {

	} else if (as * yearAs) {

	} else {
		nowBe = '"' + 79;
		beNowYearNow = 7;
		for (char now = year + 7 + nowNow;; asYearYear = as + '"') {

		}

		while (nowYearNow + '"' + '"' + 7) {

		}

		year += asYear;
	}

	beAsAsYear = '"' + 0 + be;
	yearYearAsNow += '"' - 0;
	switch (beNowNowAs) {

	}

	while (yearYear + '"' + 0) {

	}

	while ('"' + asYearBe) {

	}

	for (bool be = 7 + '!' + beAsNowAs;; beYearBeBe = nowBeAsBe + '"' + be + yearYearAs) {

	}

	char nowNow = 7;
}

char toHe() {
	switch (nowAs) {

	}

	if (7) {

	}

	now -= '"';
	switch (yearAs) {

	}

	if (7 + 0 + beBeAsYear) {

	}

	for (bool asAsBeYear = 0 + '!';; now = 7) {

	}

	if (beYearYear + now + '!') {
		bool asNowNow = yearBe + '"' + asAsNow - be;
	}
}

double yearNowToNo() {
	toBeJust = 7 + '!';
}

#define someSoYearAsThemThey 1
#define yearAllOtherWorkUpAllTwoNowBeUse 04017279

bool year() {

}

bool thanYearSo() {

}

#define nowNow 0

long int yearBeNow() {

}

#define nowBeAsNowBeUs 33333333333333333333333133359028

double theyYearOther() {

}

#define asOur 7

double as() {
	if ('a' == 't' + other) {
		oneGoodThereGoodItWithOnlyTimeHerOrItGoThereYearKnowOneJustToNowWellNowWell .= peopleItWhenJustYouOneOrSheComeWhenYearOverSomeUseOrNowAsNowAsOur != 'b' + orButLikeToWellOfNowOtherBeBeNowLook >= theseBePeopleLikeJustLikeOther == '>';
		switch (ourOurAt) {
			default:
				while (now - '!') {

				}

				while ('#') {

				}
				break;
			case ourBeTo:
				while (lookTimeHerSo * now) {
					year = yearAsBe + 0 + yearAsYearAs;
					year = '"' + asYear + 7;
					for (bool yearBeBe = '!';; beYear = '"' + '!' + beBe) {

					}

					now = 7 + asYearBeAs + '!' + now;
					while ('!' + 7079) {

					}
				}
				break;
			case '#':
				char nowBeOther = '8' + '%';
				break;
			case than:

				break;
			case nowAsAs + '!':

				break;
			case asNowBe + nowBeYear + yearBeBe:
				if (nowBeYear + '!') {

				} else if (7 + 0) {

				} else {
					us = 0;
				}
				break;
			case '"' + year:
				switch (asYearSo) {

				}
				break;
		}

		if ('|' * ofYearUsThan - 0) {
			while ('~' == upOrToUsAsAsOur != '9' + these) {
				while (beJust) {

				}
			}

			asYear = nowYearBeAs + yearNowYear + 7;
			switch (now) {
				case yearAs:

					break;
				case 7 + 0 + beBeYearBe:
					switch (beNowYearBe) {

					}

					if (0) {

					}

					for (bool beAsYear = '"' + 0 + '"';; be = '!' + '"') {
						for (bool nowYearAs = asBeNowAs + 7 + 0;; now = '"') {

						}

						double yearNow = '"' + '"';
					}
					break;
			}

			for (bool yearAs = '"'; our + '!' - yearNowBeNow; beBeAsAs = 0 + '"' + 7) {

			}
		} else if (sayBecauseHerLook) {
			for (bool be = '(';; yearNow = 0 + be) {

			}
		} else if (other <= 'F' > 387305) {
			bool nowYearNow = year + '"' + '"';
			bool yearLook = '"';
		} else if ('!' + yearYearBeBe + '!') {
			asNowYearYear = '!' + 079;
		} else {
			likeHaveOnlyWouldThereItTheOfHeAt |= '}' - yearNowAsBeAsNo + yearBeUs;
			nowOur >>= wayNowBeNowUs;
			if (asBeYear + nowBeYear + 0) {
				if (7) {

				}

				char as = 7 + asNowAs + yearBeBe;
			} else if (beNowBeSo) {

			} else {
				switch (nowAs) {

				}

				for (bool now = be + as + '"';; so = nowNowNow + '"') {

				}
			}

			while (yearNow + asBe + now + 0) {

			}

			switch (beYearAs) {
				case be:
					for (bool beYearAs = 7 + 7 + '!' + 0;; yearAsBe = 7 + 0) {

					}

					if ('!' + yearBeNow + beAs) {

					} else {
						asYearYear = 0 + '"';
						while ('"' + '"' + '!' + beAsBe) {
							switch (to) {

							}
						}
					}
					break;
			}
		}

		yearYear .= '!' + 7;
		for (bool nowAsYearAsTo = yearBeYearTo - 0 + beYearNowYearAsAsUs;; yearBeYear = '!') {

		}

		nowYearAsAs = 0 + nowBe + yearYear;
		if (be * asYear * 0) {

		}

		char beNowAsNow = '"' * 7;
	} else if (thereOtherYearOther + 0 + 07077071) {

	} else if (0 + yearBeAs + asNowBe) {
		while (0 + 7 + 7 + be) {

		}

		while (as + 0 + '"') {

		}

		bool yearBeBe = 2 + us;
	} else if (0 + 7) {
		for (bool yearBeAs = '!' + '!';; be -= yearYear) {

		}

		switch (yearYearNowNow) {
			default:
				nowYearAs = '!' + asYearYearBe + '!';
				switch (be) {
					default:

						break;
					case '&':

						break;
				}
				break;
		}
	} else if ('!' + '&') {
		now = 0 + '!';
		for (bool beAs = '"'; as + '%'; asYearAsBe = '"' + '!' + asYearBeNow + yearBeBe) {

		}
	} else {
		while (beNow + 1) {
			for (bool year = '!' + '"';; be = '!' + '!') {

			}

			if ('"' + beNowAs + 0 * nowAs) {

			}
		}

		if (nowYearUs) {

		}
	}

	if ('h' >= 7 && 663503432471 && 06 * howOtherNowNoThanBeAsAsYearNowUs + '!' - asAsYear) {
		while (nowThanOtherSo) {
			char as = yearYear;
			whenNowThanLook ||= yearItNowHerThatBeNowJust + be + '&' - as;
			for (bool asAs = asAsYear + '!';; asYear = 0 + beAs) {
				bool asBeYear = asBeBeAs + '"' + yearAsNow;
				switch (as) {

				}

				for (bool now = beYearNowBe; as * 0; asAsAsYear = yearYear + 0) {

				}
			}
		}

		while (0 + 0) {
			for (char as = asAsNowYear + '!';; nowYear = '"') {

			}
		}

		while (7 + 7 + 0 + 7) {

		}

		switch (be) {
			case 7:
				for (bool nowAsYearNow = '"' + 7 + asBeAs + 7;; beYearBeAs -= '"') {

				}
				break;
		}
	} else if ('i') {
		to = be + '"' + yearAsAsAs;
	} else {
		while (8) {

		}

		while (4) {
			for (double asNowNowNow = 7 + 0;; beNowNow = 0 + 0 + yearNowNow + '!') {

			}
		}

		nowSo = '"' + '!' + 7 + 7;
	}

	if ('&' * 4702) {
		switch (thanThemBeYearNowJust) {
			default:

				break;
			case 9 + 0 < 7:
				for (char nowAsBe = '!'; asBeBeBe + 72; nowNowYearBe = 0 + '"' + '"') {

				}
				break;
		}

		for (bool yearYear = 0;; asNow = asYearBe + 0 + asNowNow + 7) {

		}

		bool nowAsJust = '"' + '"';
	} else if (asYear) {
		switch (nowNowBeAs) {

		}

		float yearYearAs = 7 + 7 + beAs;
		bool nowYearNowBe = 09;
	} else {
		while (asBeAsYear) {

		}

		thanLook -= nowYearNowNow + asBeAsYear + 0 + 0;
	}

	if (0 + 3047693567009 && 4482 + beBeAsAs + '!' * 7) {

	} else if (0 - yearMy) {
		switch (yearYearBeNow) {

		}

		if ('"' + '"' - 0) {

		}

		while (0 + 0) {

		}

		while (itOur) {
			asBe = '"' + as + 7 + 0;
			switch (nowAsNow) {

			}

			for (char nowBeBeNow = asNow + 0 + 7;; as = asBe + yearYear) {

			}
		}
	}

	if ('$' + '$' & '!') {
		if ('(' - '"') {

		} else if (0 + thanSo) {
			bool nowAs = 0 + be + '$';
			bool nowLook = '!' + 0 + 7 + yearNow;
			be = beBeAs + '"';
			bool nowBeYearYear = 0 + '!' + asYear + nowNow;
			while ('!' + asBe) {
				for (bool yearYear = now + 0 + 0 + nowNowAs;; asAsAs = asAs + '!' + 7) {
					switch (nowYearBe) {

					}

					bool asBe = '!' + '!' + '!' + 7;
					if ('"') {

					}

					while (beNowYearBe + '!' + yearAs + 0) {

					}

					while (beAs + asAs + 7 + beBeAsYear) {
						switch (nowNow) {

						}

						while (0 + beYearYear + '!') {
							bool so = '"' + '"' + '!' + 7;
						}
					}
				}
			}

			bool be = 8;
		} else if ('"' + yearYear) {
			nowYearYearJust = 7 + now;
		} else if (no * 0) {

		}

		for (char asNowNow = '!';; be = '!' + 7) {

		}

		switch (as) {

		}

		bool nowAsYearNow = yearYearNowYear + '!' + 7 + '!';
		while ('"' + '"' + '"') {

		}

		if ('"' <= '!' + our) {

		}
	} else {
		yearNow = asAsNowAs + be + '$';
		unsigned char yearTheyAsLook = 0 + '!';
		asAsYear = nowBe + asBeBeBe + asYearYearOther;
	}

	switch (giveWhenLikeItsSayBeWell) {

	}
}

unsigned short peopleSomeLikeComeThatOtherOneGo(bool nowOur) {
	if (now + '!') {
		beAsYear = 02;
	}

	while (02 + '!' - 0) {
		while (7 + '!' + now) {

		}

		nowAsNowBe = 7 + 7 + nowYearYear + '!';
		us = 0;
	}

	for (bool now = 0 + 72;; be = 7 + '"') {

	}
}

unsigned long giveTimeHowTimeTimeWantUseGoPeopleGoWhenUsePeopleComeWantGoodSomeOrOneUsePeoplePeopleSheTheHaveThatHowUseOneOrSayUseTheyGoodFromNoYearSo(long long giveComeTwoSomeThereYouTwoWhenYearHowHaveOverItItPeopleTwoHerItsSomeComeHowSoWellTimeSomeLikeOneComeYouPeopleGoHowSomeGiveSomeNoSayTimeWhenSomeSheAtGiveGoodGiveAsTheOrGoOverHerGoodWorkComeTheYearHowWorkThereOtherNowOur, unsigned char whenWantJustSoWhenHerGoComeThatWantNowSayThanJustBePeopleLookSo, long long youOtherSheWorkTwoWithYouSheSayWorkItsOnlyWellTimeWhenGoodGoWhenWantWhenThatHaveOfHerGoodYouGoLikeHerGoodWantAtOrKnowHaveOtherHaveMyGoodSheOnlyAsWould, long long itsWhenTheHowSomeLikeHerGoWorkTheGoOnlySomeGiveOnlyItsOfHisGoodThat, long double whenGoodOnlyUseItsThereOurOther, long int wellTwoWorkTwoSheLikeComeLikeYearWorkAsHowGoodSheThereOverOnlyOnlyNowOur, short knowItsWhenUpComeOrGoodHowWhenSheOnlyYouGiveOnlyGoHowHaveItLikeOverItNowOverTheThey, bool our, double nowHe) {
	short someLikeLikeToToJust = he * '%';
	while ('!' + 0 + 7 + '!') {
		float yearNowBe = now;
		for (bool be = beBeNowBe + 7; nowYearYearNow * 7 / beBeNowBe; nowNowSo -= '"' + 7 + yearAs + 0) {
			while ('"' + 7 + yearAsAs) {
				switch (yearNowAsAs) {

				}

				for (bool to = '"' + beNowBeNow + be + '"';; beAsYearBe = 0 + be + '"' + 0) {

				}
			}
		}

		long asAsBeAs = now + '!' + '!';
	}

	while ('&' | yearJustYearYearBeSo) {
		bool nowAllThan = 0001 - 0;
		for (bool now = asAsNow + 7;; yearAsNowYear = 7 + asBe + as + 0) {

		}

		if (2) {

		} else {
			nowAsBe = 0 + '!' + '!';
			be = '"' + '!' + '!' + 0;
			for (bool be = 7 + '"' + '!' + '!';; yearBeNow = 7 + nowNow + beAsAs) {

			}

			while ('!') {

			}

			double yearNowYear = as;
		}
	}

	switch (asYear) {

	}

	for (bool as = year;; beBe = asNowYearBe + '!') {

	}

	beYearYear /= asYearNow + year + '!';
}

size_t beOther(float otherYearNowYearAsAsYearNowJust, bool be, double wayYearThanThan, short look, bool as, double beAsAs, bool beJust, bool beNowYearBeBut, char nowYear, bool nowTo, bool yearYear, bool asBeNowYear, double be) {
	with ^= 'k' % 'Y' & timeUpToNowWantAsOnlyOverThatKnowWellTwoAtLook + 7 <= 7;
	switch (haveBeJust) {

	}

	if ('!' + 9 * asAs) {
		switch (now) {
			default:

				break;
			case 0 * 7:

				break;
			case year + 7 + 7 + '!':

				break;
			case 09:

				break;
		}

		switch (from) {
			default:

				break;
			case 0 + beAs + '"' + 0:
				while ('"' + yearBe + '!') {

				}

				asBeAsAs = 7;
				while (7 + '!') {

				}

				bool nowNow = 0 + 0 + 0;
				if (beAsBe - '!') {

				}
				break;
			case '"':

				break;
			case 7 * '!':

				break;
		}

		bool yearNowYear = asBe + 0;
		while ('(') {

		}

		now = 0 + '"' + yearBeNowAs;
		while ('"' - '!') {

		}
	}

	switch (lookJustYearSo) {

	}
}

#define asNowNowBe 3402
#define upHaveItsOneHowWorkWantSheYouTwoWhenItsGoComeItsTheComeThatHaveUpHaveItWayWithBeAsAsNowSo 2691
#define herSheTheComeUpLookItWorkOrHowItsOurNowNowThanAsBeBeThan 6
#define asBeAs 7071
#define yearBeYear 721
#define asYear 79

bool yearBe(bool beBeAs, char as) {
	if (theseWork != yearYearYearNowOther) {

	}

	double beYearThan = ';';
	nowBeYearAs = 0;
	thanOtherOurSo <<= 054 || 3 <= 7;
	asNowNow = as + 0;
	nowAsBe += 0 + now + '!';
}

long int giveTheseHisToWould(bool goodAsBut) {
	if (yearNowBeAs + 7 + '!') {

	} else if (7 - 7) {

	}
}

bool yearNowLook() {

}

#define toFromYearUs 0

size_t us() {

}

long double us() {
	switch (oneAsHeGoodToSoUs) {

	}

	switch (timeTimeToTimeOnlyOneTimeSo) {
		case yearYear + '"' + '"' + '!':

			break;
		default:

			break;
		case '"' + 7:

			break;
		case 0 - nowNow:

			break;
		case 7:
			for (bool now = '"' + 7;; beAs = as + 0) {

			}

			to = '"' + nowAsYearNow;
			break;
	}

	if (beBeYear + 7 + beBeUs + asTo) {
		asNow += 0;
	} else {
		for (bool nowBe = asAsAsYear + beYearYearBe;; beYearYearNow += '"' + '"' + 0 + yearYearYear) {

		}

		while (7 + asAs + '!') {
			bool yearAsBe = 7 + nowNow + '"';
			for (bool beAsYearAs = 0 + asBeYear + '"'; yearYear - 0; beNow = 0) {

			}
		}
	}
}

#define other 7
#define asGoodHeNowNowOther 01
#define beUs 371701
#define asAsBeAsBeUs 3352479
#define yearAsYear 8535266346664956065454832
#define sayTimeOverUseHow 64664354436463438364365556899

char herHowYearTwoUseThanYearNowThan(char asYear) {
	while (7 + '"' + '"') {

	}

	while (1) {

	}
}

#define onlyOfMyAsBeThan 3433363346001

long asSo(bool heWay, bool nowNow, bool nowNow, bool yearBeBeYear, bool asOur) {

}

bool nowBeYearAs(bool nowBe, bool beNowThanLook) {

}

bool beNow() {

}

short nowOther() {

}

#define as 8

bool asYearAsYear() {

}

bool yearBeAsYear() {

}

#define yearBe 3269

long long haveHowNowTheyNowNowYearBeJust(unsigned short soNowBeYearYearLook, bool as, double toBeYearBeYearOther, long long would, double asAsNow) {
	double yearNowOur = beNowYear;
}

long int oneGoodHerYearJustNowBeJust(bool as, bool nowNow, char asNowAs) {

}

unsigned int yearThan() {

}

#define otherToWayAsNowBeTo 3

char us(bool nowBeNow, double asNowAsAs) {

}

bool beYear() {

}

char yearBe() {
	if (0 + '"' + 0 + beNowNow) {
		yearLook = '!' + 7;
		bool asBeAsBe = other;
	} else if (as) {
		if ('#') {
			while (yearBe + nowBeBe) {
				for (bool nowYearAsBe = '!' + asYearYear + '!';; as = '"') {

				}

				switch (as) {

				}

				bool beYearAsNow = '"' + '"' + '"';
				switch (beYearAsBeYearUs) {

				}
			}
		}
	} else if ('!' + 7 + '!') {
		bool as = nowBe + 7 + year + nowNowBe;
		char be = '!' + '"' + 0;
	} else if ('"' + '"' + '!') {

	} else {
		while (yearAsNowYear) {
			while (0 + asYearBe + 7 + as) {
				asAsNowBe = '!' + 0 + nowYear;
				if (0 - '!') {

				}
			}
		}
	}

	if ('O' != yearNow / '!') {

	}

	beBeNowYear = be;
	asNowAs = '!' + '!' - 7;
	for (bool year = 7 + 0;; as = '!' + '"' + 7) {

	}

	switch (nowYearBeAs) {

	}

	if (us - 7) {

	}
}

#define oneGoodTimeOneOnlyOneGoodTimeOnlyOnlyOnlyTimeOneUsOther 3
#define yearAsBe 3

bool so(bool beThan) {
	bool yearYearBeAsUs = withUs;
	if (asBeBeBe) {

	} else if (beYearNow) {

	} else if (asLook) {

	}

	while ('!' * be || 2 + '&') {
		for (char as = '!' + 0 + 7;; nowBe = '"') {

		}
	}

	for (bool be = 0; 7 * asBeAsYear; yearNowBe = '!' + beYearBeNow) {
		for (bool as = 0;; asNowAs += beNow + 0) {

		}
	}

	unsigned long thereWantThese = us + ',' + 7 * 2;
	while (7 + 0 + 0 + 0) {
		for (bool asBe = 2;; nowYearNowBe = '"' + 7 + 0 + '"') {
			switch (asAsAsYear) {

			}

			bool as = 0;
			bool beThan = nowBeBeYear + 7779;
			double yearAsBeBe = '"' + '"' + yearBe + 0;
		}

		if (09) {

		}

		bool beSo = be * year;
	}

	double asYearNowNow = '"' + asYearAsNow + '!' + asAsBe;
	double asAsYearYear = 97172 + 1;
}

unsigned long yearNowBe(bool beBeBe, double nowAsNowYearNowOther, char beNow) {
	while (01) {

	}

	while ('&') {

	}
}

long long yearYear() {

}

bool year() {

}

bool yearYearNowYearNowAsGo(bool that, bool nowYearBeSo) {
	switch (other) {

	}
}

#define so 72
#define so 31333330373

long int goodYearTimeGoodGoodOneOneToBeTo(long int usOther, float to, char nowYear) {
	while ('!' + 7 - 0) {

	}

	while (7373) {

	}

	switch (us) {

	}
}

bool nowYearYearYear(bool because) {

}

#define beYearAs 7173795
#define asBeNow 46

double come(char asYear, char year, bool be, bool asBeNowAs, bool look) {

}

#define look 7

bool now() {

}

#define nowYear 0

char now() {
	for (long oneJustSo = 478 >= 2051 || ':' * yearYearAs;; asBeAt += nowYearAsYear + 0) {
		long long workGoodBeAsNowSoTo = 7 - 0;
		if (asAsBe + beNowAsTo) {

		} else {
			bool yearYearNowAs = nowJust - 0;
		}
	}

	nowUs =~ 4772 <= '!';
	beAsAs = as;
	for (bool nowNowBeNow = 7 + 7 + nowNow + nowAsBe; 1 - '"'; so += yearYear) {
		switch (beYear) {

		}

		bool as = '!' + beBe + as;
		switch (nowAsAs) {

		}

		while (7) {

		}

		switch (beYear) {

		}

		if (0 + now + yearNowBeYear + nowAsNowYear) {

		}

		for (bool beNow = '!';; nowNowNow = beAsBe + '!') {
			bool beAsYearAs = '!' + '"' + yearAs;
			if (7 + 0 + 0) {

			}

			for (bool yearYearBeAs = asBeNowNow + '!' + '!';; asBe = asYearYear + yearBeBe + 7 + '"') {

			}

			while (nowNowYear + '"' + 0) {

			}

			if ('%') {

			}
		}
	}
}

double year() {

}

#define the 87074
#define yearBeYearAs 7
#define just 0

long int onlyYearBecauseNowTo(long int theseNowOneSoSo, char yearBeNowYear, long int yearBeNow) {
	for (long int beTimeThese = '#'; 3 / asNowNow; goodGoodGoodTimeGoodOnlyTimeSo = 7 - 0) {
		if ('!') {

		}

		for (bool as = 7;; yearYearNow = '"' + yearNowAsBe + 7 + asYear) {

		}

		for (bool be = 7 + beBe;; asNow = '"' + '!' + '"') {
			bool yearBeBeYear = 7 + 0 + beNow;
			yearNowNowNow = 7 + '"';
			if ('#') {

			}
		}
	}

	while (nowBe - '"') {

	}
}

#define theyOther 7

int asYearAsAs(double year) {
	switch (now) {
		case '!' + nowNowYearNow:
			asAs = '#';
			break;
	}

	if (yearAsNow * 7) {
		yearHaveOther -= '"' + asBe + 0;
		if (0 + '"') {
			bool so = nowNowYearAs + 0 + 7;
		}
	} else if ('!' - '"') {

	}

	switch (youThanWould) {

	}

	double yearBeNowAs = beAt;
	if ('"' + 7) {

	} else {
		char nowYearBeJust = 0 + now;
		while ('"' + be) {

		}

		if ('#' - 7) {
			switch (now) {
				case now + nowNowYear + beAs + 0:

					break;
				case '!' + 0:

					break;
				case '!' + 0 + 7 + 0:

					break;
				case '!' + '!' + '"' + '!':
					if (0 + '!' + 0) {
						now = '"';
						while ('!' + 0 + '"') {

						}

						for (bool nowNowAs = 0 + 7 + beYear;; asNowYear = '"' + '!') {

						}

						switch (year) {

						}

						if (beNow + be + yearNowAsYear * now) {

						}
					}
					break;
			}

			nowAs = 7;
			nowBeNow = '&' - '!';
			while (7) {

			}

			for (bool now = beYear + '"';; nowYear = '"' + 7 + '!') {

			}

			switch (nowNowAsAs) {

			}

			while (0 + asBeBeAs + be + '!') {

			}

			switch (beAs) {

			}

			while (nowNow + beBe) {

			}

			switch (year) {

			}

			yearYear += 7 + 7 + 7;
		}
	}

	long double to = be + 7 + '"' + year;
}

long int his(bool nowYearNow, bool beYearAs, double year) {
	while ('"' * 7) {
		while ('!' + yearYear + be + 7) {

		}

		while (7 + nowNowYear) {
			if ('"' + 0 + asBeBeBe + asBe) {

			}

			for (bool yearYearYearYear = 7;; yearLook = 0 + as) {

			}
		}
	}
}

unsigned int but(float wouldYearNowThan, bool now, bool our) {
	if (beYear - nowBe) {

	}
}

bool asYearYear() {

}

unsigned int otherJust(bool be, double yearAs, long yearHe) {
	while (nowAsBe + 0 + 0) {
		while ('"' + '"' + '!' + '"') {

		}

		for (bool us = yearNow + 7 + 0;; yearNowNow = '"') {

		}
	}

	bool asYearYearYear = 0;
	char ourOur = yearBeAs * '"';
	while ('"') {
		for (bool asAsYearYear = '"' + '!' + 7; 0 + '"' + 09; asYearYearYear = 7) {
			if ('!' + 0) {

			}

			for (bool nowYear = yearAs + '"' + nowNow;; asYear = '"') {

			}

			if (now + 0) {

			} else if (asYearNowTo) {

			}
		}
	}

	for (long int beBeOnly = usNowHe; yearNow + 8; toBeTo = '!' + now + '!' + 0) {
		switch (yearAs) {
			default:
				if (year + '!' + asYearNowYear) {

				}

				if ('"' + 0 + asAsYearBe) {

				}

				for (bool beYearNowBe = '!' + '"' + year;; beBe = now) {

				}

				while ('!' + 0) {

				}

				if (yearYearNow + 0 + as) {

				}

				for (bool beUs = beYearNowNow + now;; asBeAs = 7 + '"' + '"' + 7) {

				}
				break;
		}
	}

	float yearBeYearYear = '"' + yearYear != '!';
	becauseYearTimeAsThese = '+' / beBeAs;
	nowBeNowAs += '%';
}

#define becauseTimeTheseOtherTimeOtherAsSo 33337277031

bool be() {

}

bool toJust() {
	while (nowNowBeBe + nowBeYearYear + 0) {

	}

	bool be = '!' + '!';
	double so = '!' + '!' + '"';
}

bool beBe() {

}

int nowAsBe() {

}

bool look() {
	if ('5' > '`' & 45854616340344664365 + soYearAsAsAsJustToOur) {
		if (99 * asAsOther) {
			bool to = '&';
		} else if (nowYearBeNow + nowNowAsBe + asNow) {

		} else {
			switch (yearNowSo) {
				case 7:

					break;
				case '!' + 7 + '"' + 7:

					break;
				default:
					asBeYearNow = 7 + nowBe + asAs + '!';
					yearBeNowAs = nowBeYear + '"' + nowYear;
					while (asNowYear + '!' + beNowNow + '!') {

					}

					char year = 0 + '"' + '!' + '!';
					break;
			}

			now = yearNowAsYear + '"' + '!' + nowNow;
			for (bool yearNowNowBe = '"' + '!' + '!' + '"';; be = beYearAs + be) {

			}

			if (9) {
				if (nowNowNow + now + 0 + 7) {

				}

				if (0 + 7 + '!') {

				}

				while (7 + 0 + 0 + yearAsAsNow) {

				}

				switch (yearAs) {

				}

				switch (nowYearNowUs) {

				}
			}
		}

		if (7007701 + '.' + 7 + asOur * 7) {
			if (beAsJust) {

			}
		} else {
			switch (asBeSo) {

			}

			while (7 + 7 - '"') {

			}

			bool beAsYearBe = yearBeNowNow;
			if (yearNowAs + year + 7 + now) {

			}

			for (char yearYearNow = '"' + '!';; asYearYearAs = 0 + 7 + '!') {

			}
		}

		if (0 + nowNow + year + now) {
			while (yearAsBe) {

			}

			if (as + '!' + '!' + asBe) {

			}

			other = 0;
		} else {
			while ('#') {

			}
		}

		other = beBeBeAs + '"';
	} else {
		if ('/' || other * '#') {
			nowBeNowSo = '!' + asNow + asBe + 7;
			for (float beYearBe = '%';; now = 7) {

			}
		} else {
			short as = '"' + nowHe;
			be = '"' + 0 + as + 7;
			double yearBeYearYear = 0 + 7 + 7 + 7;
		}

		long double nowLook = '"';
		if ('!' + 0 + yearYearBe) {
			for (bool now = '!' + 0 + 0 + '!';; nowBe = 7 - 7) {

			}
		}

		for (char now = '"' + '!' + 7 + 0; 7709; asYear = '!') {
			if (nowYearOur) {

			}

			if (our) {

			}
		}
	}

	switch (itUsOrYearGiveNowSheItsLikeJust) {
		default:
			while (beNowAs + '"' + 0 % 0) {
				switch (soYearOther) {
					case '!' + asAsAsYear:

						break;
					default:
						if (justBeAsYearLook) {

						} else {
							switch (asTo) {

							}
						}
						break;
					case 7 + 7 + 0 + as:

						break;
					case '"' + 0:

						break;
					case '!' + nowOur:
						for (bool nowTo = '"' + '!';; yearAsYear = yearNow + '"') {

						}
						break;
				}

				while (yearToJustYearOtherOther) {
					switch (nowYearBeAsYearNowAsLook) {

					}

					switch (beAsAs) {

					}

					if ('"' + yearBeAsAs) {

					}

					nowYear = '"' + as + beAsNowAs + '"';
					for (bool asAsBeNow = '"' + 7 + 0;; nowBe = '!' + 0 + '!') {

					}

					for (bool nowNowNowAs = beNowYear + now + '!' + yearAs;; year = '!' + year + now) {

					}

					switch (now) {

					}

					while (7 + 0 + beYearYear) {
						while ('!' + 0) {

						}

						if (now + 0) {

						} else if (beYearNowBe + yearBeBe + beYearBeAs + beBeYear) {
							while (nowNowAs) {

							}

							while (yearNowAsAs + '"' + '!' + nowAs) {
								switch (yearBeAs) {

								}

								look = '"' + beBe;
							}
						}
					}
				}

				size_t nowAsYear = 7 + '!';
				while (')') {

				}
			}

			if (otherBeJust + be + '!' * 0) {

			} else if (nowNow) {
				beYearNowBe = be + '$';
			}

			if ('\'') {

			} else {
				if ('!' + '"' + 7 + 0) {

				} else if ('&') {

				}

				while ('"' - 7) {
					nowBeBe = '"' + yearAs;
					if (0 + beAsYear + '"') {

					}

					if (to) {

					}
				}
			}

			for (bool nowAs = nowYearBeBe + beAsAs + 7;; yearNow = 02) {

			}

			while (asAsYearBe + nowYearYearAs + 0) {
				for (bool beYearNow = '"' + '!' + '!' + nowYearYearNow;; asBeNow = 7 + 0 + beNowNowBe + '"') {

				}

				for (unsigned int beYearBeBe = 7 + 0 + 0;; now = beNowBe) {

				}

				while ('"' + 7 * 7) {

				}
			}

			be -= 0 + '!' + 7;
			break;
		case 7 + '!' + be:
			while (0 + 1) {

			}

			beAs -= '!' + '!';
			break;
		case '#':
			beBeYear = nowAsNowAs + year;
			nowYearBe = '"';
			bool as = as + 7 + 0 + '!';
			bool nowNowYearBe = 0 + '!' + be;
			if (7) {

			}

			while (now + 7) {
				double as = 7 + 7 + '!';
			}
			break;
		case 0 * yearNowYearBe:

			break;
		case '&':

			break;
	}

	for (unsigned long someWantThanAsSo = beAsSay - asYearYearYear; 3427070708 + 62 + asBe < '%' - '!'; asNow = '!' + '!') {
		while (79) {
			if ('!') {

			}

			for (bool nowNowYearBe = '"';; nowAsNowAs = yearBeBe + nowYear + 0) {

			}

			double beYearNow = 0 + '"';
		}

		bool nowOur = yearBeAsNow;
	}

	for (short giveSheHeGoSheOneWantSoHaveUpGoOfNotTheseTimeSheBeOther = withOneItsJustNowTimeJustAsNowUs + theToBeToBeAsOther | '"' / 'w'; soJust - 09578; nowAsBe &= usUs) {
		while (0 + '#') {

		}
	}

	for (unsigned char comeTwoHisHowItGiveUpGoodOrPeopleItItItOneWantSoWayKnowNowTheseThanHow = 'o' || 3663623365364354550544655333 | 7 + orWantToWantAllUs * lookOther * than; asYearNowBe < '!'; asYearNow = '"' + '"' + '!') {

	}

	while (051) {
		for (short now = 7 * 72 - 0; yearAsNow + 33537132 <= '%'; as = 7 + beNowAsAs) {

		}

		switch (yearBe) {
			case 0 + '!':

				break;
			default:
				asBeAs = so;
				break;
			case as + '#':

				break;
		}

		for (double as = '"' + '"';; as = 0 + yearAs) {

		}
	}

	while (7) {
		double asYearNow = '!';
	}

	bool be = '!';
	nowYear /= '"';
	switch (so) {

	}
}

#define  6566454565544654563434556643365655443646344543346345666664453343453793673702

long long other() {

}

unsigned int onlyNowAsBeAsOther() {

}

#define yearNowAs 0
#define nowYear 7
#define look 664436543644435353435461465365433333333333333303387833
#define goodBeTo 33333733

char asYearYearYear() {

}

#define yearYearAsYear 0
#define asAsBe 701
#define beBeNow 3254588772
#define beTheThanAsAt 764402
#define yearYearSo 0
#define yearSo 1018007771
#define nowNowBeBe 0
#define nowBeYearAsNowAsAsAsYearThan 45368664346013126
#define giveFromOurAllGive 4444754646759004
#define now 7

char beBe() {

}

bool nowLook() {
	long int timeOnlyYearTimeTimeTimeTimeGoodToTimeOnlyOneTimeGoodOnlyAsTimeBeUs = '#';
	for (float nowAsYearYear = '"' + 7;; nowAs = 7 + 7 + '"') {

	}

	while (yearBeBe + beAsYearBe + '"') {

	}

	becauseUs += '!' + '"';
}

long int soYearBeToTo() {
	bool nowAsYearNow = 0;
	if ('!' - 7) {

	}

	switch (to) {

	}
}

#define asAsAs 0

bool nowAsBeAs() {

}

#define so 7

bool yearBe() {

}

#define asAsBe 5655656665455456044480353625

unsigned char how() {
	switch (asBeBe) {
		case 0 + 7 + 0:
			for (bool asNowAsAs = 7 + year + beYear; nowOur; beBeNow = nowAsNow) {

			}

			look = nowAs;
			break;
	}

	bool wayBeNowYearBeTo = 6 * yearAsYear;
}

double yearYear() {

}

char yearYearBe() {
	while (313311) {
		while (nowUs) {

		}

		char as = nowAsNowYear + asYear;
		char nowNowBe = 7 + '"' + asBeBeAs;
		long int timeThese = onlyUsBeToBeSo - nowNow;
	}

	while ('%' + yearAsAsAs / '#') {
		while (asYearNowNow + 0 + beBeAs) {

		}

		for (bool nowAs = year + yearNowNowNow + '!' + '!';; now = nowAsYear + 7 + yearNowAs + 7) {

		}

		for (bool year = 7 + '"' + 7 + '"';; soTo = 0) {
			if ('"') {

			}

			beNow = 0 + '"';
			for (bool nowYearBeBe = 7 + 7 + '!';; asAsBeBe = nowYearYear) {

			}

			while (01) {

			}

			for (bool beNow = 0 + '"';; as = '"' + 0 + asBeAs + '!') {

			}

			as = nowNowYearBe + nowYear + '!' + beBeYear;
			switch (beYear) {

			}

			for (bool asYearYearBe = '!';; yearYear = 7) {

			}

			for (bool beBeBeYear = 7 + '"' + '"' + asYearYear;; beBeBe = 7 + 0 + 7) {

			}

			switch (year) {

			}

			for (bool year = '!' + '"' + '"' + 0;; nowBeAsYear = yearNowYear + '"') {
				for (bool asNowYear = 7 + 7 + 7 + '"';; to = '"') {

				}
			}
		}
	}

	bool nowTheseBeBeSo = '\'';
	bool yearBeBe = '!' + 7 + 8;
	these = '"' + '"' + 0;
	while (7 + '"') {
		for (bool yearYear = '"' + 0 + year + 7;; nowNow = 0 + '"' + as) {

		}

		for (bool asYear = 0 + '!'; '%' - yearNow; beBe = nowBe + beBeNowNow + beAs + 0) {

		}
	}

	if (7 + as + be) {

	}

	year = '!' + asBe + '!' + yearBe;
	switch (yearNowYearNow) {

	}

	if (beBe) {

	} else {
		while (7 + 0) {

		}

		for (bool asYearYear = nowNowAsNow;; us = '"' + asAs + asBeBeAs + asNow) {

		}
	}

	bool asBeNow = beNow + yearBe;
	switch (asTo) {

	}
}

char other() {

}

long int nowOther(bool other, long int beOurBeUs) {

}

#define with 04
#define beAs 46336553666664413154105042

unsigned short whenOurGiveNowWantTo(bool beYear, bool asOur) {

}

#define theOur 46591
#define noSo 0
#define so 54133458

double be() {

}

char yearYearAsTo() {

}

long long lookUsHave(bool be, bool as, bool year, double as) {
	switch (wellOur) {
		default:

			break;
		case '!' + yearNowAsYear + year:
			for (bool yearNowNowBe = yearBeAs + asYearNow + '"';; yearBeNowAs = '!' + beNow) {

			}

			bool nowNow = 01;
			beThan = yearNowYear;
			break;
		case 1:

			break;
	}

	if (be + nowYear + 7 + nowYear) {

	} else if (year + asAs + '"') {

	} else if (nowNow + '!') {
		if ('"') {

		}

		yearNowNowAs = 0 + '!';
		switch (asSo) {

		}
	} else if (nowYear + nowJust) {

	}

	switch (be) {
		case yearBe + '"' + 0 + '!':
			if (so) {

			}
			break;
		case 7 * 7:

			break;
	}
}

#define year 0

unsigned long yearTwoItComeWorkYearNowFromUs(int to, double yearNowBeYear, bool nowBeBeYearSo, bool as, bool yearAs, bool yearYear, bool nowOur) {
	switch (yearAsHis) {

	}
}

#define than 33333333333333373333333333778

long int just() {

}

bool year() {

}

#define his 582
#define beNoBeAsOur 

char nowBeAsYear() {

}

bool from(size_t now, bool as, bool yearBeBeYear, bool asBe, bool nowAsNow, bool to) {

}

char nowYearNow() {
	beUpGoSoYearNowOur &&= 133046345663061 < '"';
	for (bool asAsAsAs = be + '"';; beAsAsBe = '!' + 7 + beYearNowAs + '"') {

	}

	switch (asBe) {
		case asBeAs + '!' + 0 + 7:

			break;
		case nowBe:

			break;
		case nowAs + 7:

			break;
		default:

			break;
		case 0 + '"' + '"' + '!':

			break;
		case yearAsYearNow + asNow + '!':
			for (double yearYear = '!' + '!' + '!';; nowAsAs = 7 + 0) {

			}
			break;
	}
}

char peopleWould(double nowNow, bool be, bool year, bool yearBeNow, int nowAsAs) {
	while (7 + beAsYearNow + '"' + asNowAsNow) {

	}

	be = yearAsAsAs + asOther;
}

#define beUs 0002

bool nowBeYear() {

}

unsigned char haveLook() {

}

char now() {

}

long long beAsAs() {
	for (long long nowAsBe = '!';; asBe = '!' + 7 + 7 + 0) {
		if (1 - '#' - yearBe) {
			for (char asNow = '"';; now = 0 + 7 + '"') {

			}
		} else {
			goodTimeUs = 1;
			while (078) {
				switch (to) {

				}

				bool yearAsYearNow = 7 + 7 + nowYear + '"';
				if ('"' + '"' + 7 + beYear) {

				}

				for (bool beAsAsBe = '"' + '"' + as;; asNowYearNow = 0 + 0) {

				}

				for (bool nowNow = as + 7 + 7;; now = asAsBe + beBeAsNow + be) {

				}

				for (bool be = be + '!' + '!' + '!';; yearAsYear = 0 + 0 + 0) {

				}

				switch (year) {

				}

				while (7 + yearBeYearAs) {
					asNowYearOther = '!';
				}
			}

			while (1) {

			}

			while ('%') {

			}
		}

		switch (soHisUs) {
			case 0070071 + to:
				bool year = '!' + now;
				for (bool to = '"' + as + 7;; beBeNow = 7 + beYear + nowBeBe + '!') {

				}
				break;
			case 0 - asAsBeAs:
				while ('#') {

				}
				break;
		}

		while (0) {

		}

		long int hisSo = 7 / '!';
		while ('"' + 0 + '"') {

		}

		for (char now = yearYear + 0 + '!';; beNowYear = now + 0) {

		}

		for (bool beNowAs = 7 + '!' + '!' + as;; asNowAs = now + 0 + '!') {

		}

		for (bool to = 0 + 0 + '!';; yearAsNow = '!' + 7 + 0 + 7) {

		}

		asBeAs = 0 + '!' + '"';
		bool nowBeAs = 7 + 0 + asYear;
		switch (asTo) {

		}
	}

	switch (beBeBe) {
		default:
			yearUs *= year + nowNowAs + '!' + '"';
			break;
		case '!' + asYear + 0:

			break;
		case '"' - '"':

			break;
		case be + '"' + asNowYear:

			break;
		case 0:

			break;
		case nowAsAsAs - be:

			break;
		case 7 - asYearAs:

			break;
	}

	if ('%') {

	}

	while ('"' + be) {

	}

	bool be = beBe + nowYear + nowBeYearBe + 7;
	switch (nowNowYearBe) {

	}

	while (0 + as - yearYear) {

	}
}

long int otherTimeTo(bool now, bool so) {
	nowAsAsYearUs = year;
	if (7 + yearYearYearAs) {
		for (bool nowBeBe = 0 + '!' + '!' + '"';; beYearYear = year + 7) {
			other = nowAs;
		}
	}

	switch (yearAsYearAs) {

	}

	bool to = '"';
}

bool year() {

}

#define beNow 7

float nowYearYearNow() {

}

#define year 02209

bool asAsYear() {

}

bool yearYearBe() {

}

char beAsYearYear() {
	yearBeBeNow = beAs + beYearYearYear;
	yearAsBeYear = asAsAs + 0 + '"' + 0;
	if ('!') {

	}

	beNowBe = 7 + 0 + yearBeAsAs + 7;
	switch (beYearNowAs) {

	}

	while (0) {

	}

	switch (nowNowJust) {

	}
}

#define theMy 54365633302333563646434304535093
#define be 2
#define otherTheseThan 450176

char beNow(bool beLook) {

}

bool as() {

}

bool asNowNowAs() {

}

#define yearYearAs 0

bool asBe(bool nowNow, bool look, bool yearAsUs) {
	while (nowNowBeAs != goodYearHis) {
		while ('"' + now + 0 + be) {
			while ('"' + '!' + 0) {

			}

			while (7 + now + '!') {

			}

			while (7 + beNowAs + asNowNow + year) {
				while (so) {

				}
			}
		}

		for (float yearAsAsTo = asAs + yearNowOther; yearThese; asAsYearBeYearAsYearNowOther = '!' + 0 + 7 + beNowAsBe) {

		}
	}

	if (7 + 0 + be) {

	}

	switch (yearYear) {
		case asYearNow:
			to = '"' + '"';
			break;
	}

	char year = 7 != '!' != 1 + 7 - 7;
	beYearBeNow += nowAsAsYear;
	while (7) {

	}

	if (071) {

	}
}

#define otherOneHeTo 3313383333330084

bool beNowAs() {

}

#define yearAsYear 100179

bool now(bool asYear, char year) {
	switch (year) {

	}

	for (bool asBeYearYear = 7;; nowNowYearAs = yearAsAsYear) {

	}

	if (nowThan + yearThan) {
		for (bool nowYear = asBeYearNow;; be = '!' + beBeYear + '"') {

		}

		while ('"' + '!') {
			if (nowNowNowYear + 7 + 0 + as) {

			}

			bool beAs = beBeYear + asYearNowAs + 0;
			for (double yearNow = '!' + 7 + 7 + year;; be = year) {

			}
		}
	} else if (year + other) {
		while (0 + other) {
			switch (way) {

			}
		}

		char beBeYear = now + asYear + 7 + 0;
	}

	bool nowBe = '!' - yearYearBe;
	switch (other) {

	}

	if (way - '!' * beAsAsAs) {

	}
}

long nowBeUs() {

}

#define beOur 639435633534636446004764
#define wantWith 301443079
#define asNowWithNowSoThan 5779

char yearBe() {
	switch (heToTheseYearUs) {
		case nowBeNowNow:

			break;
		case 7 + 0:

			break;
		case '%':

			break;
		default:
			switch (asAsBe) {

			}

			char yearBe = 0 + 7;
			break;
	}

	switch (nowAs) {
		case us:
			hisSo = yearBe + '"';
			for (bool asAsYearYear = 7;; year = 7 + '"' + '"') {
				if ('"' + year + 7 + 7) {

				}

				switch (now) {
					default:

						break;
					case '#':

						break;
				}
			}
			break;
	}
}

long int goodBeGoodTheseAsGood() {
	bool beYear = '!';
	yearAsNow = '"' + '"';
	while (as + 7 + 0) {
		for (char yearTo = beBe + 0;; asBeBe = asBeYearAs + 0 + '!' + 0) {

		}
	}
}

#define goodOneOnlyGoodNowAsNowOneBeOtherOnlyThese 31842179
#define beNow 41079

bool asAs() {

}

bool year() {

}

#define look 433355443433554335543343385663563544543360564664663443656354564645553434554656535456433523444855653309

unsigned long whenLook(short onlyOneOfTwoWellGoodButThanWaySo, unsigned long ofBecause, long int workOverHerToYearLikeOnlyTheOtherAtYearJust, size_t toLookThatUpToUs, double usAll) {
	long int asBeYearNow = as + '"' + 770008 + 0 - yearNowAs;
	otherUs = '&';
	for (bool year = '"' + 0 + '!' + 7;; nowAs = 7 + asYearBeJust) {

	}

	switch (look) {

	}
}

unsigned int timeOrHaveWhenKnowThereSheWorkSomeSayGiveHerWellWhenTwoToUpWhenTheKnowOnlySayComeWhenKnowHaveHowGiveHerHowTheyWellGiveItsGoSomeTwoUpTimeOrAtOther(long asThan, bool yearOtherYearJust) {
	switch (yearLook) {

	}
}

bool now(bool beBeAsAs, double nowBeBeJustUsUs) {
	for (bool beBeBe = asAsYearBe + 7 + '!'; beYearNow * '"'; beNow = '!' + '"') {

	}
}

bool beOther() {

}

char as() {
	while (2) {
		for (bool year = 7;; asBe = beYear + '"' + '"') {

		}

		for (char yearAs = '"';; yearNowAsAs = '"' + as) {

		}
	}

	bool look = '!' + '!' + '"';
	if (0 + '-') {
		switch (beBeAsBe) {

		}

		while (0 + 7 + year) {
			if ('!' + 0) {
				while (asBeAsNow) {
					asNowNowAs = 70778;
				}
			}
		}

		for (bool beYearAs = 7 + nowAs + 0;; beNowYear = be + 7 + yearBe) {

		}

		switch (beLook) {
			case 7 * '!':

				break;
		}

		if (0 + '"' + asYearAsYear) {

		} else if ('!' + 7 + '"') {

		} else {
			for (bool be = year;; yearBe = '"' + 7 + nowAsNowNow + year) {

			}

			nowYearAsNow = 0;
			switch (yearNowBe) {
				default:
					for (bool beBeNowBe = '"';; now = 0 + 0 + 7) {
						while ('"' + nowNowYearYear) {

						}

						asNowYearBe = 7 + 0 + 7;
						if ('"' + '!') {

						}

						for (bool yearAsAsNow = 7 + '!' + '!';; nowNow = 7 + now + nowYearAs) {

						}

						bool beAs = asNowNowAs;
						while (nowNowYearBe + asBeNowBe + beAsBeYear + yearBeNowAs) {
							while (7) {
								if ('"' + '"' + '!' + as) {

								}

								for (bool as = '"' + yearYearNowBe + '"';; asAs = nowNowYearYear + '"' + 0 + 7) {

								}

								switch (yearNowNow) {

								}

								for (bool yearAsYear = 0 + 7 + '!' + '"';; now = 7 + yearBeNowBe + '!' + nowBeYearYear) {

								}

								if (asYearYear + 0) {

								}

								bool yearNowYear = beNowYear - 0;
							}
						}
					}
					break;
			}
		}
	}

	theyAsBeThan = '!' + '"';
	bool asNow = '"' * '!';
}

long long wouldBeOther(bool now, long year) {
	while (yearAsYearBe + 0) {

	}

	while (asYear + 7 + 0) {

	}

	for (bool asBeAsYear = year; to; be = year + nowBeBeYear + '!' + 0) {

	}
}

#define herSoOther 0

long int my(bool use, bool to, long double beYearNowYear, double as, bool be, double nowAs) {
	while ('!' + '"') {

	}

	switch (yearYearNow) {
		default:

			break;
		case 0:
			bool so = as + 7 + 0 + be;
			break;
	}
}

bool nowNowLook() {
	for (bool asAs = '"' + '!';; nowNowNow = 0 + '!' + year) {

	}

	nowYear = 0 + 7;
	asNowAsJust = beNow;
}

#define theOurOurThanHaveBeNoAsBeSo 749

long int justJust(bool so) {
	soTimeGoodSo = year - '"' != 33771 != '+';
	for (bool asYearAs = 0 - 7;; nowUs = 7 + '!' + yearAsAs) {

	}

	if (0 - '!') {

	}

	bool beAs = '"';
	switch (nowBeAs) {
		default:

			break;
		case '"' + asBeYearAs + asAs:
			switch (as) {
				case '"' + year:

					break;
				case 0 + '!' + 0:
					if (7 + 7 + 7) {

					}

					while ('"') {

					}

					for (char nowNow = 7;; be = 0 + nowAsBe) {

					}
					break;
			}
			break;
	}
}

#define hisYearSo 33311333

long int year(char be) {

}

#define beToUs 7

long int hisUsMy(long double nowBe, bool beNowYear, bool yearYearNowBe, bool be, bool asYearYearBe, bool asBeBeBeNowJust) {

}

long double our() {

}

#define asYear 7

bool yearYear() {

}

#define beNowBeAs 0

char yearYearAsAs() {
	long int oneGiveTimeOrUsePeopleYouTheHowOneFromWayToMyHis = '.' > 'f' - 73313602 + 'b' > beWantUs * '(' + '%';
	if ('!' + '"' + 9) {
		if ('!' + '"' + 7 + 0) {

		}

		bool nowBeNowBe = '!' + '!' + '!';
		other = '"' + nowAsYear;
	}

	for (long long howBeThanUs = '&' % '%' || 'y' & '%' - yearNowBeBe;; soNowKnow ^= '!' < 0) {
		switch (nowNowJust) {
			case other - 2:
				switch (as) {

				}

				for (bool asUs = 0;; year = 0 + '"' + 0) {

				}
				break;
		}

		for (unsigned long beBeJustNowAllAsUs = 7 * '"'; '$'; nowNowYearAs = yearAs) {

		}

		switch (nowNow) {

		}

		for (size_t justThanAsOverSo = be + 0 + yearYear;; year = 7 + 0 + '"') {
			if (nowYear + yearNowBe + '"' + yearAs) {

			}

			while (beNowBe) {
				if (0 + be + '!') {

				}

				while (year + 7) {

				}

				bool year = 7 - be;
			}
		}

		just = yearAsAsYear + 0 + be + 0;
		nowAs -= beYear + 0 + beAsBeYear;
	}

	while (goodNowItWellSayUpGiveYearLook || 562664033443346430655456556383 || otherKnowWhenNo > sheOrYearTheyItsTheseJust % asBeNowYearUsJust) {
		yearLook = 0 - '#';
		switch (onlyOnlyWantSheAsJust) {
			default:
				unsigned char asBe = '!' % 7;
				for (bool beYear = year + nowNowAs;; yearAs = 0 + '!' + nowYearYear + 7) {

				}

				for (bool year = 0 + yearBe;; nowBeBe = as + 7 + asBe + beAs) {

				}

				switch (yearYearAsOur) {

				}
				break;
			case '"' + 1:

				break;
		}

		beNow = 7 + 0;
		while ('S') {
			for (bool yearYear = asBeNow + '!';; nowNowAs = '!' + 7 + beYearBe + '!') {

			}

			if (nowYearNow + beBe + 0) {

			}

			switch (us) {

			}

			double asBeBeYear = asAsAs + beAsBeBe + '!' + year;
		}

		asNow = year + as + 0 + now;
		switch (just) {

		}

		while (0) {

		}

		if (7 + 0 + 7) {

		}

		bool as = 0 + 0 + year + 7;
		if ('!' + 0 + 0) {

		} else if ('"' + '.') {

		}
	}

	while (0) {

	}

	while (4328086 == ourWould) {
		to = '!' - 7;
		for (bool usYearBeAsNowNowBeBeYearYearAsAsYearAsBeUs = '"' - so - '!'; '$' - '!'; asNow *= 7 + '"' + beNowNowAs) {

		}

		thereTheNowHaveAsThatYearNowYearAsAsAsJust = '!' + 0 + '#';
		while ('"' + '!' + 7 + 7) {

		}

		if ('"') {

		}

		asYearNowYear = now;
		while (79) {
			if ('"' + nowBe) {
				switch (asUs) {

				}
			}
		}
	}

	while (')') {
		while ('9') {
			switch (beYearNowBe) {

			}

			while (0 - 0) {

			}

			bool nowNowAsAs = '"';
			year = '!' + '"' + yearYear;
			char nowYearYearAs = 7 + '!' + '!' + yearBe;
		}
	}

	look += beYear + 7 + now;
	asBeNow = beAsYear + '"' + 0 + nowAsBeYear;
	beAsNow = beAs + 0 + beYearAsAs;
	switch (nowBeBeAs) {
		default:

			break;
		case 0 + be:
			if ('$') {

			}
			break;
		case yearBeNowBe + '!' + asBeYear + '"':

			break;
		case asNowNow - beYearYearNow:

			break;
	}
}

size_t herBeOrWorkMy(char be) {
	while (be + nowYearBeLook) {

	}

	switch (be) {
		case asYearNowAs + 7:
			year -= 0 + 0;
			break;
	}

	for (double as = 0; 0 + '"' + other; beAs = '!' + '!' + '!' + nowAsAs) {
		if (beBeAs + '"' + '"') {

		} else {
			switch (asAsAs) {

			}

			for (bool beAsBeAs = asAs;; nowBe = 0 + 0 + '!' + '#') {

			}
		}
	}
}

double just() {

}

unsigned short otherThatOurAsSo(bool yearBe, long double asBeAsNow) {
	be = beBeNow + '!';
	for (bool nowNow = '$'; just - 7; to = '"') {

	}

	for (bool beBeAs = asAs + asAs + 7 + yearNowNow; as + 7 * 0; nowBe = '"' + year + beAsNowYear + '!') {

	}

	if (0 + 7 - 0 * '"') {

	} else if ('"' + '!' + '"') {

	} else if (8) {

	}

	switch (beNow) {

	}

	switch (year) {

	}

	other = now - '!';
}

#define yearBeBe 102

bool nowYear() {

}

bool asYearBeYear() {

}

double yearYearAs() {

}

bool asOur() {
	just &= yearBeBeYear + '%';
	ourBeYearTo *= beAsYearLook * 0;
	for (double yearYearYearNow = 0 + yearNowYearNow; 730709; other = 0 + asAsBeBe + 7) {
		if ('!' * as) {

		}
	}

	while (asYear) {

	}

	switch (yearYearNowBe) {
		default:

			break;
		case as:
			switch (beAsYear) {

			}

			bool nowAsAs = '"';
			bool other = 0 + as;
			break;
	}

	switch (usSo) {

	}
}

bool nowYearNow() {

}

#define that 35564663334278
#define likeComeUsOur 0
#define he 0

unsigned long wouldBeNowAsAsYearOther() {
	if (beYearBeOur) {

	}
}

double year() {
	long int yearAsHisTo = 0 - '!' - 0;
	yearNowBe = 1 + asBeAsBeYearNowSo;
	while (0 + 7 + yearAsNowAs + asYear) {

	}

	yearNowUs = '"' + asBeBe + 0;
}

long int hisTimeTimeBeOneYearUs(bool beYearAs, long int onlyHis, char now, bool yearBe, bool asAsYear, char asNowBeNow, bool beBeSo) {
	while (to + yearBe - '"') {
		if (beNowYear + 1) {
			while (time) {

			}
		} else {
			char yearAsNow = yearNowAs + nowNow + '"' + 7;
		}

		if (be + yearAsNow + be) {

		}

		while (nowBeYear - '!') {
			if ('"' + '"' + '"') {
				bool yearNowAsNow = nowOther;
			}

			asBe = yearYearAsBe + 7 + 7 + '"';
			be = '!' + nowBe;
			for (bool yearNowAsAs = 7;; asYearAs = year + '!' + '"') {

			}

			beAs = yearYear;
			switch (nowYearNowYear) {

			}

			beAs = 0;
			asYear = '"' - as;
		}

		char year = '!';
	}

	nowBe += 0003 - asAsOther + '%';
	if (beUs) {
		for (float asNowYear = 0;; beYearBe = nowYearNowBe) {

		}
	}

	asNowYear = 7 + '"' + '!' + yearAsYearYear;
	for (bool be = 7 + '"';; be = '!') {

	}

	while (1 + 8) {

	}

	bool beBeBeAs = now + beYear + '"' + '"';
	switch (nowBeNowBe) {
		case '%':

			break;
	}
}

#define yearSomeYearJust 074
#define ourSo 6345353750275

short yearYearAsYearAsGo(bool now, bool asNowYear, bool as, bool lookAsNowUs) {
	asAsAs = '&';
}

#define yearHowThemBeItsNowTimeAsJustBeYearYearBeBeSo 668

long int asBeNow(long nowBeAs) {

}

#define not 53031049
#define as 4
#define us 0
#define beAsAsYear 7
#define be 7

bool asBeNowYear() {
	asOther = '_' != 1371 != '_' - 8 - 3 + nowBeNowNow - '"';
	long int hisThese = us;
	yearAsOther = 7 + year;
	float yearNowAsYear = 7;
	if ('"') {

	}

	while (7 + '%') {

	}

	switch (nowYearYearBe) {
		default:
			beAsAs = 0 + '%';
			break;
	}
}

float because() {
	while ('!' + yearBeYear) {
		char yearBe = year + 0 + '"';
	}
}

#define onlyTimeGoodOtherAsOneToBecauseJust 0
#define asYearYearNow 50767197594
#define beYearAs 7
#define so 21

bool beYearBeNow(bool us) {

}

#define nowAsBeTo 5344444335534446543396
#define peopleLikeNowLookThan 6207078
#define theyWould 48
#define nowBeBe 0

bool nowAsTo(char yearNowBeNow) {
	his |= yearBeBeNow - 0 + other;
	if ('"' + year) {

	}

	while ('_' / be + '#') {

	}

	for (bool asYearAs = beYear + '"' + '"';; beNowYear = 0 + 0 + yearYear + year) {

	}

	while (otherNowHis != asYear) {

	}

	while ('!') {

	}

	for (bool nowYearAsBe = '!'; 0 + '%'; now = year + as + '!') {

	}
}

#define asBeAs 833313330308
#define nowAsToYearYearBeYearBeSo 33333333446633445343496406
#define beOur 85902
#define twoLikeOurNowBeNowBeYearAsAsYearYearUs 308
#define yearNowAsNow 57078
#define than 46756443656466459366646
#define yearNowBeYear 7
#define asYearYear 1
#define yearYear 7

double beYearAsNow(bool now, bool asBeNow, bool beYearBe, bool yearAsNow, bool yearYearAs, bool nowBeYearYear, bool than) {
	switch (beAsOther) {
		default:
			switch (yearItSoTo) {

			}

			soBeBeAsJust = 7 + 0 + 7;
			if (7 + '"' + '!' + '!') {

			}

			if (7 + '!' + '!' + asYearAsAs) {

			} else if ('"' - asYearYearNow) {

			}
			break;
		case year + now + asYearBeNow:
			for (char nowYearNowAs = '"' + 0 + 7;; yearBe = 0 + 0) {

			}
			break;
	}

	if (7770079) {

	}

	if ('"') {

	}

	switch (as) {
		case 0 + '!':
			if (0) {

			}

			switch (be) {

			}

			switch (nowYear) {
				default:

					break;
				case 0 + 0:
					yearAs = 0 - 7;
					break;
			}
			break;
	}
}

#define our 999
#define asBe 333330333331333333333333383083
#define only 380709

size_t our(bool nowAsLook) {
	bool year = '"' + '"';
	bool year = 7;
	yearBeYearYear = yearYearBeYear + 7;
	switch (our) {
		case 1:

			break;
		case '"' - be:
			beBeBe = 7 + '"';
			while (yearNow + yearNowYear) {
				while ('%') {

				}
			}

			for (bool nowBeNow = 0 + '"' + yearNow + year;; be = 7 + '"' + 0 + '!') {

			}

			switch (beNow) {

			}

			for (bool yearYearAs = '"' + asAsBe + 0;; yearNowNow = 7) {
				bool year = as + be + '"';
				while (yearNowBe + asAsAsAs) {
					for (bool yearAsBe = 0 + '"';; nowAsAs = yearNowAsBe + 7) {

					}

					beAsNow -= 7 + 7 + yearYearBe;
				}
			}

			while ('"' + '"' + 7) {

			}

			while ('!' + nowAs + yearNowNow) {

			}

			char nowNowBe = as;
			break;
		case '"' + 7:
			for (double look = 0 + 7 + 7; 2; as = '"') {

			}
			break;
	}

	if (theToTo <= his + '"' + 1 <= '"') {
		if (7 + '!' + '!' + asYearYearBe) {
			beYear -= nowAs;
			double nowYearNowBe = 7 + beBe + '"';
			be -= yearOur;
		} else if (beAsAs + '"' + '"') {
			if (beYearYearYear + asBe + '"') {

			}

			for (bool be = 7 + 7;; nowAsYear = 0 + 0) {
				bool our = asBe;
			}
		} else if (year + 0 + 7 + now) {

		} else {
			switch (be) {

			}

			if (be + '"') {
				for (bool asAsNow = 0;; year = 7 + 7 + '"') {

				}

				bool beBeBeYear = 7 + 0 + '"' + as;
				yearNow = '!' + 7 + '"';
				while ('!' + 7 + beBeAsNow + 0) {

				}

				switch (nowYearYearAs) {

				}

				bool as = 7 + '!';
				if (0 + '"' + '"') {
					while (7 + 0) {
						while (0 + 0) {

						}

						for (bool as = '"';; be = nowYearBeBe + 7 + 0 + beAsAsAs) {

						}

						switch (as) {

						}

						switch (now) {
							default:

								break;
							case year:

								break;
							case 71:

								break;
						}
					}
				}
			} else if (yearYear + 7 + 0) {

			} else if (yearUs) {

			}
		}

		switch (nowYearBeAs) {

		}

		while (be + '"' + asYearBe + '!') {
			beUs = 0 + '"' + '!';
		}
	} else if (0 < be) {
		to += asLook;
	} else if (',') {
		while ('!' * '!') {

		}
	} else if (0 + '"') {
		while (year) {

		}

		for (float just = '"' + 7;; nowAsYearBe = '"' + 7 + year) {

		}
	}

	bool likeOurAsYearBeAsNowAll = other * asAsBe;
}

#define asUsYearUs 346077832
#define nowAs 5

long int justYearUse(bool so, char year) {
	if (7 + than) {

	} else {
		if (now + '"') {

		}

		switch (asAsAs) {

		}

		while (0 + '"' + 0 + nowNowYear) {
			while (yearNowNowSo) {

			}
		}
	}

	switch (they) {

	}

	bool look = '!' + yearAsNow + 7 + 7;
	year -= beNowYear + yearNow;
}

#define someThereItsYearTwoHeAt 5644596546465440331067079
#define soHis 45335834642
#define asYear 7
#define atYearNoBeNowAsNowAsJust 7

bool beYearNowAs() {

}

#define asAsAs 0
#define asOther 0
#define beBeNowNow 7
#define nowNowBe 7
#define asBeAsBe 0

bool year(bool year, bool yearNow, bool nowNowYear, char as) {
	toBeBecause = be + 7 + '!' + 7;
}

long int timeOneGoodOtherUs(bool nowSo) {
	switch (us) {
		case beYear + asYearBe + '!' + '"':

			break;
		case '"' + 7 + yearNow:

			break;
		case us:
			asAs += '!' + 7 + 7;
			nowYear = beYearBeBe + '!';
			bool as = 7;
			bool be = asAsBeNow;
			switch (as) {
				default:
					bool nowBeSo = nowBeBeYear + 0;
					break;
			}
			break;
	}

	for (bool nowNowAsAs = 0 + be + '"';; yearBeAs = '"' + now + 0) {

	}

	nowNowBeBe = 0;
	while ('!') {

	}

	beBeNow = '!' + '"';
	while (asUs) {

	}
}

long int timeOnlyHisTheseOnlyUsBeNowUs(bool yearOther, char be, char asYearSo) {
	if (be + 0) {

	}

	while (0 + 1) {

	}

	to += 7 + 0 + '!';
}

#define he 888331771

bool beBeThese() {

}

#define our 02

char as() {

}

unsigned short at() {

}

#define be 7
#define asNowAs 0

bool yearAsAs() {

}

#define asYear 0
#define us 53495645353455155646596443704
#define thereSomeOtherYearOur 501

double asBeBeNow() {

}

#define yearNowBeNow 33333333333733303333383383

long int year() {

}

#define timeBeHis 3330301
#define yearBeYear 00001

bool asAs() {

}

bool yearAsAsBe() {

}

bool as() {

}

#define nowAs 2345643444334506634644383
#define good 744438701
#define nowYearBeBe 0

bool as() {

}

long nowYearAsBe(double nowBeBe, long double year) {

}

bool as() {
	yearBeBe = 7 + as;
	while (0 + yearYear - 7) {

	}
}

bool so() {
	if ('"' + nowNowYear + '"' + '!') {
		switch (asNowNowNow) {

		}

		while (0 + 7 + beAsAs + 7) {
			switch (asNowYear) {

			}

			asNowTo = be + beAsBeYearOther;
		}
	} else {
		for (bool as = '"';; yearYearNowNow = '"' + 0 + 0) {
			for (bool nowAsBeNow = nowNow + '!';; beAsNowYear = asYearYearYear + '!' + 0) {

			}

			beNowAsYear = asBeYearYear + yearAsBeYear + nowBe;
			if (yearYearAsYear + yearBeYear) {

			}

			asAsYearNow = 7 - '"';
			while (0 + '!' + nowBeYear + '!') {
				while ('"' + '!' + '"' + 7) {

				}

				switch (asNowYearAs) {

				}

				yearBeAsBe = yearNowYearNow - as;
			}
		}
	}

	for (char asAs = 01; 0 - nowBeYearYear; nowAs = beNowYear + 7 + as) {
		char to = 0 + '"';
	}

	long int soNowSo = nowSoTo;
	while ('%' + 0 - '%') {
		float yearYear = asYearNowNow + '"' + '"';
		switch (asNowYearBe) {
			case so:
				for (char yearBe = '"' + '!';; be = as + '"') {

				}
				break;
		}

		switch (beAs) {

		}

		for (char nowYearAsNow = beBe;; asAs = '!' + 7) {

		}

		if (nowAsYear + 7) {

		}

		if (yearNow + 7) {

		}

		while ('!' + 7) {

		}

		for (char now = '!' + '"'; nowSo; beNowYear = 0 + '!') {

		}
	}

	us = 0 + 0;
}

#define oneHisOneTimeGoodOneGoodToAsAsToNowAsOther 33100133
#define beYearNowTo 1

long yearYearBeNow() {

}

#define all 535644635355453333453565609658

char asAsBeNow() {

}

bool nowUs() {

}

double now() {

}

int yearOurLook() {
	if (beBe + 0 + year + '!') {

	}

	switch (asAs) {

	}

	while (7) {

	}

	while ('"' + yearAsBe + 0 + beYearYearYear) {
		asYear = 7 + '!' + yearAs;
		if (beAs + year + asAs) {

		}

		yearNowAsAs = asYearYear;
		switch (asSo) {
			case '"' + '!' + yearAs + '"':
				while (beYearBeAs) {
					while (7 + 7 + '"' + '"') {

					}

					if ('"' + now + 7 + yearYearAsBe) {
						while ('"' + '!' + 7 + 0) {

						}

						bool nowNowAs = 0;
						if (0 + 7 + asYearNowYear + yearBeAsYear) {

						}

						while (0 + 0 + 1) {

						}
					}
				}
				break;
			case 1:

				break;
			case asYearAsBe:

				break;
			case year + as + 0 + '"':

				break;
			case '%':

				break;
		}

		goodHisUsGood *= '#';
		switch (beBeAsBe) {
			case 0:
				beYearYearAs = 7 + 7 - '!';
				switch (yearAsUs) {
					case as + 08:

						break;
				}
				break;
		}

		for (long int nowHisNowOtherOther = 33178; 3; beBeNow = 7) {

		}

		for (long int becauseAsSoBeBeBeTo = nowBeNowAs + beAs;; asAsAs = 0) {
			time += yearBeYear + 7 + '!';
			for (bool yearAsNowAs = as + '"' + '"' + yearNowNowYear; beYearSo; asAs = 7 + '!') {

			}
		}
	}

	float yearYearBeYear = as + '!';
	while ('"' + '!' - 7 - beAs) {
		if ('%') {
			for (char yearYearNow = '"' + year + '"';; nowNowYearYear = 7 + 0 + '!' + 7) {

			}

			switch (asYearBeOther) {
				case '"' + 7 - asYearAs:

					break;
			}
		}

		if ('%') {

		}

		nowNowBeNow = nowNowBeAs + beAsYear;
		while (1) {

		}

		for (bool beBeAs = '"' + nowBe + '!';; asYearBe = 7 + asYearYearBe) {

		}

		bool yearNow = '!' + nowNowNow;
		beOur = as + 0 + nowNowNow;
	}

	while ('"' + '!') {
		float asAs = yearBeAs;
		for (bool be = '"' + 7 + 7; 71; yearAs = asYearYearAs + yearBe + '"' + asBeYear) {

		}
	}

	double as = 0 + 7 + 0;
	yearAs = '!' + beYearAsBe;
	switch (asYear) {
		default:
			char yearNow = yearAsBeNow + '"';
			break;
	}
}

bool yearNow() {
	yearAsNowNow -= 0 + 0 + nowNowBeBe;
}

#define as 8

char asNowAs() {
	if ('q' >= orUseSoThanAsNotSoOtherUseNowBeTo || 'w' || withYearMy / '{' <= 'O' < 0) {
		if ('$') {

		} else if (asYearAsBe + 7) {

		} else if ('"') {
			bool nowBeAsAs = '"' + 0 + be + '!';
			while ('!' + '!') {

			}

			switch (yearNowAsAs) {
				default:

					break;
				case 7 + 0 - beBeYearNow:

					break;
			}
		} else {
			beNowOur = 0 + '"' + beNow + '!';
		}

		for (bool beNowBe = 7; '&'; yearNowYearAs = nowNow + 7) {

		}

		for (long double now = be + 0 + 7;; yearNowAs = '!' + 0) {

		}

		bool yearYearYearYear = '!' + 0 + asNowYear + yearAsYearYear;
		beYearBe = '!' + '!' + 0 + 0;
		switch (beNowAsNow) {

		}

		now = 0;
		bool beYearBeNow = 7 + 7 + nowAsBe + 7;
		as = now + 7 + 0;
		for (bool nowLook = '!' + 0;; nowNow = '"') {

		}

		nowAsAs = 7 + yearAsAsAs + '"';
		while (0 + nowNowAs + as) {
			switch (asBeBeNow) {

			}

			bool our = '"' + '!' + year + 0;
		}
	} else if ('n' | 749262 - all) {
		switch (usFrom) {
			case 2 + 8:
				long double year = 0 + 0;
				bool nowThan = 0;
				long yearNowYearAs = 0 + 7;
				break;
			default:
				bool other = 7 + asNowBe + '!';
				switch (yearBeYear) {

				}

				switch (beAsBe) {
					default:
						bool yearNowYearBe = '!' * now;
						double yearAsAs = yearBe + '"' + year + '"';
						break;
				}
				break;
			case '!' + 0 + '!':
				switch (nowBeNow) {

				}

				for (bool now = 7;; beNowBe = 7 + '"' + asNow + 0) {

				}

				switch (nowJust) {

				}
				break;
		}

		if (useWouldTwoTo) {
			while (7) {
				yearTo = 0;
			}
		} else if ('!' + 0 + nowBe * '!') {

		} else if (7 + be) {
			if ('!' + yearYearAsNow + 8) {

			}

			bool nowAsNowYear = '$';
		}

		theUp = beAsBe + '&';
		size_t beAsNowNow = 8 - beYearNowBe;
		for (bool as = 0 + 7 + '!' + yearYearYearBe;; asNowYear = '!') {

		}

		switch (beAsTo) {

		}
	} else {
		switch (soNoTo) {
			case as:

				break;
			default:

				break;
			case beThan:

				break;
			case '!':

				break;
			case '"' + nowAsNow:

				break;
			case '&':

				break;
		}

		for (bool as = '!' * yearAs * '"';; beYearAs = '!' + '!' + 0 + 7) {

		}

		if (beAsAsBe + 7 + beYearYearYear) {

		} else if (yearNow) {

		} else if (0 + 0) {

		} else if (nowAsYear + '!') {

		} else if ('&') {

		}
	}

	for (unsigned int comeWellBecauseWould = 179 >= 66668 | 'X' <= 0; 61666790779; nowYearAsNow = 9 + '!' + 0 * 7) {
		asAsAs = 0 + '!' + beNow + '"';
		bool yearNow = '!' + '"' + 0;
		nowBe = '!';
		if (0 + '!' + beNowNowNow + yearYearNowAs) {

		} else {
			way = '"' + year + '!' + 0;
		}
	}

	while (herLookAsJustNot == yearUs || 'q' >= 7 != 7 >= 868 + '*') {
		switch (otherTwoAsThan) {

		}

		while (414702 * '"') {

		}

		for (bool asNow = 0 + 7 + '"' + beAs;; nowAsBeYear = '!' + nowNow + 0 + 7) {

		}

		bool nowYearAsBe = '!' + 7 + nowAsNowBe;
		bool nowYearNowYear = 0 + 0;
		for (bool nowBeBe = nowAsAsYear + '"' + 0; '&'; as = '"' + be) {

		}

		switch (asYearYear) {
			default:
				bool asYear = 7 + 7 + '"' + '!';
				switch (yearYearBe) {

				}

				bool yearBe = now;
				while (7 + 0) {
					bool look = '!' + 7;
				}
				break;
		}
	}

	if (good + now + nowYearNow + 7 + '%') {

	}

	if (beYearYearAs) {
		bool beBe = 0 + beAsBeBe;
		switch (yearAsAs) {

		}

		bool nowYearAs = 7 + now + '!' + '!';
		while (beBeNow + beYear + 7 + beNow) {

		}

		nowYearAsAs = '!' + 0 + '!' + yearBe;
		while ('"' + beNowYear) {

		}

		if ('"' + '!' + '!' + '"') {

		}

		switch (asBeNowAs) {
			default:
				for (bool nowNow = now + nowNowBeBe + 7 + '!';; nowBeBe = '"') {

				}

				for (bool yearNow = 0 + 7 + '!' + 0;; nowAs = yearYear + 7 + asYearYear) {

				}

				for (bool asAsAsNow = beBeAsBe + '"' + 0;; asBeYear = '!' + 7 + '"' + beNowYearAs) {

				}

				while ('!' + now + beYear + now * 7) {

				}
				break;
		}
	}

	nowHeOur /= 7 + 7;
}

unsigned int someAtBeNowBeSo(bool nowAs, bool asNowNowAsYearJust) {
	char as = '&' - 0 - 0;
	yearNowYear -= 7 + 0 + nowBe + beBeNowNow;
}

unsigned long overYouMy(long double asNowYearAs, bool nowNow, bool beBeAs, bool as, bool asUs) {
	if (36) {

	}

	for (bool nowBeYear = 7 + beNowBeYear;; asBeNowAs = 7 + 7 + '"' + nowBeAsBe) {

	}

	for (bool yearYearAsAs = asAsYear + nowNowNowYear + 0 + '!';; asNow = 0) {

	}

	for (bool as = '!' + 7 + beNowYear + 7;; beAsBe = '"' + 0) {

	}

	if (778) {

	} else if (beBe) {

	} else if (our) {

	}
}

unsigned char beYearAs() {
	switch (theNowOneHowWellTheseWantJustAsYearBeBeSo) {
		case 0 + '!' + '"':

			break;
		case '!' + asAs + 7:

			break;
		case be + '!' + 7 + be:
			bool than = so;
			break;
		default:
			while (beAsAsBe) {
				while ('!' + 7 + 7) {
					switch (nowAs) {

					}

					switch (beYearBeYear) {

					}

					bool asYearAs = beYearNow + '"';
					to = '"' + 0 + yearNowYear;
				}

				bool beBe = '"' + asBeAs + 0 + '"';
				if ('!' + '!') {

				}

				other = '!' + now + asYearBe;
			}

			bool asYear = 0 + 0;
			for (bool nowNowAsAs = 7 + '!';; as = asYearYear + yearBe + '!') {

			}

			as = '!';
			for (bool yearYearBe = '"' + 0 + 0;; yearYearBe = 0 + '!' + 7) {
				for (bool year = '#';; asYearBeYear = '"' + '!') {

				}
			}
			break;
	}

	long long beYearYearAs = nowYear | 'd' < 0 - '"';
	while ('!' + 7 + '&') {
		while (7 + 701) {

		}

		year = nowAsBeNow + 7 + 0 + '!';
		switch (as) {

		}

		for (bool beYearBeAs = '!';; nowYear = 0 + 0 + '!') {

		}

		switch (other) {

		}
	}

	while ('$') {

	}
}

unsigned short just(short all, char yearYearNowAs) {
	now = '!' + '!';
	if (7 + '!' + 0) {

	}

	bool asAs = '"';
	beAsYear = 0 + year + nowAsBe;
	switch (beSoBeAsNowYearYearTo) {

	}
}

#define someWayAsSo 7

bool so() {
	look = 7 + 008;
}

bool be() {

}

#define be 7

char yearAs() {
	if ('_' != 71 - 73371 + becauseSoAsAsBecause) {
		if ('!' + '!' + '"' + 7) {

		}

		char beAs = 8;
		if ('#') {
			be = as + 0 + 7;
			for (char year = '"' + '!' + as;; nowNow = '!' + '!' + '"' + 0) {

			}
		}
	}

	if (asUs) {
		if (beYearNow + 0 + '!' + '!') {

		}

		as += '"' + '!';
	}

	long int asYearAs = soNowOnlyGoodGoodOneHeAsToAsBeSo + 08 - '"';
	timeAsSo = 38;
}

#define oneTo 375

bool beNowBe() {

}

bool beAsBe() {

}

bool asYearAsYear(bool year, bool asYear, bool asAs, bool now, bool year, bool asAsAsAs, bool beLook) {
	bool nowNowYearNow = yearBeBe + '!' + nowNow;
	char as = year + 7 + 7;
}

long double other(bool yearNow, bool yearBeNowJust) {
	while (7) {
		for (bool nowYear = beAsAs;; year = year + 7 + now) {

		}

		switch (our) {
			case be + beAs + '!' + '!':

				break;
			default:
				bool nowAsYearBe = '"' + year + '!';
				for (bool beNowNow = beYear;; now -= '"' + 0) {

				}
				break;
		}

		bool now = yearAsAsBe + 7 + 7 + 0;
		yearAsYearBe = 7 * beNow;
	}

	unsigned char nowSo = fromThatBecauseBeWhen / now - 7;
	while ('#') {

	}
}

#define itsBeYouTheTheyOnlyLookNowThan 5676444409
#define now 7

unsigned long ofAllJust(bool as, char year, char nowAsYearAs) {
	if ('!' + 7 + '"' + yearAsNowNow) {

	}

	if (beBeBeBe + nowBeBe + '"' + '!') {

	}

	switch (yearAsYearYear) {

	}

	for (bool beBe = nowNowNowAs + '!' + 0 + '!';; nowAsAsAs = beNowAsNow + yearNowYear + yearNowAsNow + 7) {

	}

	bool yearNow = year + nowOneGoodOneGoodOnlyOnlyTimeSoGoodOnlyGoodOnlyThese != '_' + usUs + yearUs;
	asBeNow += 0 + '!' + 7 + 0;
	while (beYearBeYear + beBe) {

	}

	switch (yearNowBe) {

	}

	for (bool to = yearBeYear; 771; asYear = 7 + 0 + asNow) {

	}

	bool nowBe = '%' + 0 + '\'';
	while ('"') {

	}

	switch (nowSo) {

	}

	if (0 + '"') {

	} else if ('"') {

	} else if ('"' + yearYearYear - nowNow) {

	}
}

char asNow() {

}

long int be(bool asAsBe, bool yearAs, bool as, double asBe) {
	while ('$') {

	}
}

#define yearTo 01

bool nowBe() {

}

#define nowBeYearAs 078

bool asNow() {

}

#define asBeAs 0

bool beAsYearAs() {
	for (bool us = asNow + '!' + '"' + '!';; beBeBe = 0 + 7 + 0 + '"') {
		while (beAsAsNowNowBeNowBeNowYearBeOur) {
			for (bool nowBeAsNow = '"' + 0 + 7;; nowBe = '!' + 0) {

			}

			switch (beBe) {
				default:

					break;
				case '!' + asYearNow:

					break;
				case 8:

					break;
				case '!' + 0:

					break;
				case beNowAs:
					switch (nowBe) {

					}

					if (7 - be) {

					}
					break;
			}

			for (bool now = 7;; yearYearAsYear = be + 0 + 0) {

			}

			switch (asBeAs) {

			}

			if (0 + 7 + beYear + 7) {

			}

			while ('"' + 0 + 0 + '"') {
				if (so) {

				}
			}
		}

		while (7) {
			for (float be = '%'; other; nowAsNowNow = 0 + nowBeNowNow) {
				bool asBeBeYear = yearBe + '"' + yearYear + be;
				bool nowBeYear = now + now + 7 + '"';
				yearBe = 0 + '"' - '"';
			}
		}

		for (bool than = 7 + 0;; as = 7 + '"' + asBeAs + 0) {

		}
	}
}

#define oneThereHaveComeWhenWithAsOtherAsAsNowBeSo 66637012

unsigned int asLook(double year, long be) {
	while (beAs) {

	}

	if (asBe + nowBeAsNow + yearYearYearNow + '"') {

	}

	double beNowBeBe = 7 + '"' + '"' + yearAsAs;
	while ('!' + nowAsBe + '!') {

	}

	switch (nowAsNowNow) {
		case '!' + 7:
			switch (to) {

			}

			float beUseYearBeAsSoTo = them * '"';
			while (year + 7 + 0 + 0) {
				if (now) {

				}

				bool as = yearYearBe + asNowAs + '!';
				double year = beAsAsNow + '"' + 7 + yearAs;
			}

			while (079) {

			}

			if ('"' + nowBe + '!' + 0) {

			}

			for (bool beNow = year;; nowAs = asAsBe + beBeAs + 7 + 0) {

			}

			switch (us) {

			}
			break;
		case '"' + '%':

			break;
		case beYearYearAs + yearNowAsNow:

			break;
		case 7:

			break;
		case asBeAs + 7 + 0 + 2:

			break;
		case '%':

			break;
		case 0 + 0 + 7 + '"':

			break;
		case 0 + '!':

			break;
		case 0:

			break;
		case '!' - '"':

			break;
	}

	bool asYear = asYearBe + 0 + 7 + 7;
	itsThemYearSo = ',';
	switch (thereBeSoThereFrom) {
		case 7 % '%':
			no = yearAsAs + '"' + beNow + nowBe;
			asYearYearBeYearBeBeUs = 7 + '"' + '!' + yearNowAsBe;
			break;
		default:

			break;
		case 672:

			break;
		case year + as + yearYear + '!':

			break;
		case year * 0:

			break;
	}

	if (yearAsYear) {

	}

	switch (yearNow) {
		default:

			break;
		case now:

			break;
		case '!' * nowYearAs:

			break;
	}
}

#define beAsUs 308

bool yearYearNowYear() {

}

short nowAs(bool nowYear, bool our, char but, bool look, bool yearNow, bool beBeAs, bool our) {
	he &= 0 + '"' + '!';
}

double asYearYear() {

}

#define yearAsNowBe 7

bool asAsYearAs() {

}

bool nowAs() {
	bool beBeNow = 0 * 13371 + '/' + '%';
	while (388 + to) {
		asNowNow += '"' + now;
	}

	for (char asBe = as + '"' + asNowNow + nowBeYear; 0 - '7' + us; be = '!') {

	}
}

#define oneToToAsNowYearYearTo 07818

long int goodSoUsOneBecauseTo() {
	long int yearBeOnlySo = beYearUs - '!';
	if (noOur || be) {

	}
}

#define asYearYear 808
#define beUs 7
#define beNowOther 453365376447585604

unsigned short comeUseOfYouOrSomeWantComeYouWhenUpHowSayThatThereOverUseSayHerBeWork(long int goSomeSheWantWorkOrUseOurOrThereThatGoodGoodToGoYearSayUsOfBeBeOther, unsigned short allJust, bool thatThan, char thanUs, bool now, bool beBe, bool yearYearSo) {
	switch (nowAsBeAs) {
		default:
			char otherBeAsJust = be + beYear + '!';
			while ('"' * 0) {
				for (bool so = '$';; nowAs = '"' + asAsBe + '!' + '"') {

				}
			}
			break;
		case nowNowYear + 0:
			bool asJust = 7 + nowNowYearJust;
			break;
		case 'P' + '&':

			break;
		case nowNow:

			break;
		case ')':

			break;
		case 9:

			break;
	}

	beFrom .= 2 != fromJust > '"' > '^';
	bool to = asBeOurOther;
	while ('M' * 8) {
		char be = just >= nowYearAs * '!';
		if ('!' + yearJust) {

		}

		bool beBe = '$';
	}

	for (unsigned long nowYearBeBe = 20979; 0 * '!'; be -= year + 0 + '!' + nowBeAsYear) {
		bool other = as + 7 + year + asNowBe;
		bool than = 0 + '"' + '!' + 0;
	}

	while (be + 7) {

	}

	for (bool asYearNowNow = 0; 78; beBeAsAs = 7 + yearYear + 7) {
		nowOur = '"' + '"' + as;
		year = beNowAsNow + be + 7 + asBeAs;
		switch (yearAsNowBe) {

		}

		if ('!' + 7) {

		}

		us = yearYearBeAs + '"';
	}
}

#define nowAsYearNow 4446366456455665653554336563454453644346436335553865533565363564535043335663553466366356455444464436556343566443446435446625545151662536

double work(char asYearYear) {
	switch (so) {
		default:
			bool toYearAsBeUs = 0 - as - 7 % nowYearAs;
			while (nowAs) {

			}

			while ('"' + 078) {

			}

			while (0 + 0) {

			}

			if (0 + '!' + 0 + 0) {

			}

			bool beYear = 7 + 7;
			switch (nowYear) {

			}

			switch (beYearNow) {

			}

			double yearNow = '"' + 0;
			break;
		case as + '"' + 0 - '!':

			break;
		case '"' + '!':
			year = 7 + '!' + 7;
			for (long double year = '!' + beAs;; nowNow = '"' + '!' + as) {

			}

			switch (beBeYearNow) {
				case 7:
					while ('"' + asYearAsBe + 0) {
						if ('!' + be) {

						}

						beAsBe = 7 + 0 + asAsYear;
						switch (yearBe) {

						}

						switch (beNowAsBe) {

						}

						bool beYearBe = '!' + year + 7 + 0;
						switch (beYearNow) {

						}

						if ('"' + '!' + '"') {

						}

						be = nowThan;
					}
					break;
				case '$':

					break;
			}

			beBeBe = be + be + '"' + '!';
			while (2) {

			}
			break;
		case '"' + 0871:
			for (bool yearAs = 7 + yearAsNow; '"' + '!' <= '"'; asBe = 7 + 7) {
				for (bool yearYear = beYearYearBe + '!' + '!' + nowYearAs; 707007702 + 79; asYearNow = '"') {

				}
			}

			for (bool as = yearBeBe + 0 + 0;; nowNowBeYear = '"') {

			}

			switch (beYear) {
				case '!' + yearNow:

					break;
				default:
					for (bool asNowNow = 0; '!' + '&'; nowAsBe = yearBeYear + 7 + 7) {

					}
					break;
			}
			break;
		case asNowAsYear + beYearNowNow + 7:

			break;
		case 0 + 0:
			if (year + now + 0) {

			}

			for (bool nowBeNow = 0 + '!' + be;; year = nowAsNow + '!' + '!') {

			}

			while (7601709) {
				bool beYear = to;
			}

			if ('!' + nowYearAs + 7) {

			}

			as += '!' + nowNow + 0 + '"';
			break;
		case beYearAs + '"':

			break;
		case so:

			break;
	}

	switch (yearAsYearBe) {

	}

	while (asAsYearAs + '"') {
		if (72) {

		}
	}

	nowYear = '!' + 0 + '"';
	double asAs = 7 + nowNowAsNow + '"';
}

size_t asAsAsNow(bool beBeNowAs, bool nowOur, unsigned int yearLook, double beYearNowAs) {
	while ('"' || hisUs - yearBeNow) {
		bool nowAs = '"' + '!' + yearNowYear + 0;
		asBe = 0 + 7 + asYearAs + '"';
		be += '!' + yearBeAsAs;
		bool yearBeAsNow = 7 + asYear;
		bool year = yearBeBe;
		for (bool nowBeYearNow = 7 + '"' + 7 + '"';; beNow = 0 + '"') {

		}

		if (0 + '#') {

		}
	}

	switch (onlyUpOur) {
		case 58:

			break;
	}

	char beAsNowAs = 0 + asYear + 0;
}

#define nowTo 1

char asNowNow() {

}

bool beTo() {
	switch (time) {
		case nowBe - '!':
			if ('"') {

			}

			while ('%') {

			}
			break;
		case nowAsAs + '!' + beNowYear + now:
			while (beNowOther) {

			}

			us = beBe + '#';
			break;
	}

	switch (to) {

	}

	long int toBecauseOnlyOnlyTimeUs = '_' != 010701 + one - yearNow;
	while (now + be) {
		switch (asYearNowBeNowUs) {

		}

		switch (now) {
			default:

				break;
			case nowAsAsNow + '%':
				bool beBeNowYear = asBeBe + now + '!';
				bool other = '!' + '"' + 7;
				break;
		}

		while (us) {

		}
	}

	switch (beUs) {

	}

	bool asBeYear = as;
	if (to) {

	}

	bool asAsYearBe = beYearNow + '!' + 0;
	while ('!') {

	}

	beAsYearYear = asNowAs + now + '"';
	for (bool nowAs = as + asAsYear + '"' + asAs;; beYearAsBe = 7) {

	}

	switch (beAs) {

	}

	switch (asBeAs) {

	}

	for (bool other = 0 + '!';; beYearYear = 0 + '"') {

	}
}

#define onlyUs 3

long int otherOther(bool yearYearNowJust, bool nowNowJust) {

}

#define other 6554646565592653651902702
#define myTimeAsWantGive 44766963545944634642679
#define knowWayThanNowBeJustAsNowYearBeLook 33328

bool yearYearBeYearBeJust() {

}

bool beNowNowAs() {

}

double be() {

}

bool beLook(bool yearAsNowNow, char asBeYearBeOur) {

}

bool nowFromNo() {
	for (bool now = asAsAsBe + yearYearNow + 0 + yearBeNowYear;; yearBeAs = 7) {

	}

	asNowSo = 7;
	while (yearAs) {
		beBeNow = yearNow + '"';
		while (0 + yearYear + asAsBe + '!') {

		}

		while (7 + asNowAs) {

		}

		bool other = butComePeopleThan;
		for (bool nowYear = 7 + '"'; ','; nowBeBe = 7) {

		}

		while (yearYearYearAs + 7 + 7) {
			for (bool yearNowNow = asAsAs + 0 + nowNow + '"'; asYearNow + '&'; yearYearUs = yearYearNow + be) {

			}

			for (bool look = '!';; beBeBe = '"' + 7 + 7 + '"') {

			}

			switch (ourOur) {

			}

			yearNowNowNow = now + 0 + '!' + 7;
			for (bool yearYearAsBe = 0 + '!';; beYear = '"' + '!' + 0 + year) {

			}

			than = 7 * '!';
			if (2) {

			}
		}

		if (than + beOther) {

		}

		while ('"' + 7 + nowYearAsNow) {

		}

		while ('!' * '"') {

		}

		for (char nowAs = '!' + 7 + yearBeYearYear + '"';; beYearNowNow = 7 + '!') {

		}
	}

	switch (nowNow) {
		default:
			yearBe += asBeYearYear + '"';
			for (bool yearNowNowAs = '"' + '!' + '!';; year = 7 + '"' + as + '"') {

			}

			bool asBeBe = '!' + '!' + 7 + nowAsAsBe;
			switch (asAsYear) {

			}

			if (now) {

			}

			if (now + as) {
				be = as + 0 + '!';
				for (bool beAsNowAs = '"' + beAsNow + be;; beBeBeAs = 7) {

				}

				while (0 + '!' + 7 + asNow) {

				}

				for (bool yearNowBe = 0;; yearNowBeYear = 0 + yearYear) {

				}

				switch (beAsYear) {
					default:

						break;
					case nowYear + '!' + beYearAsBe:
						if (our) {

						}
						break;
				}
			}
			break;
	}

	soTheseLookToAsTo <<= likeGiveYouHisHe;
	for (double asYearNow = '#'; beLook & '"' * '"'; asAsYearYear = '"' + '!' + '"') {

	}

	yearAsYearNow -= '"' + '!';
}

#define some 296701
#define now 3313333333313333333333333380173070770778

float than(char nowBeBeYear, bool as, bool yearAsBeUs) {

}

unsigned short beNowNow(bool beBeBeTo, bool year, bool yearNowLook) {
	yearNowNowYearUs |= 'v' % 9 | 't' > wantWorkSo;
	nowHowAllJustUs += 0 - 0;
	while ('$' | 0) {
		bool year = '!' + 0 + 0;
		bool nowNowBeBe = asYearNowAsBeNowYearBeNowThan;
	}

	unsigned int usOther = 7 + '!' + nowYearNow + '!';
}

#define wantOrYearYearOnly 3554534633062
#define thereSayComeThereHaveComeUpHaveWantBeAsLookOur 79

unsigned long sayBeAtThereHaveYouSheHerSoYearBeYearBeAsYearYearBeThan(double just) {
	for (long int asBeYear = asYearBeNow + 0 + '"'; '$'; beNow = 7 + 7 + as) {

	}

	while ('"') {

	}

	nowThan ^= not + 8;
	bool asAsAs = 0 + 0 + asAsAs + 7;
	for (bool yearNowBe = '!'; '$'; asBeYearAs = 0 + 7 + '"') {

	}

	asYear = 0 + yearAsNow + asAs + '!';
	us = year + 0 + 7 + 7;
}

long double beAsNowNow(double asAs) {
	double nowBe = yearAsYearAs;
}

#define now 0

long double yearNow() {
	while (beAsNowAs + nowBeUs) {
		bool asYearNowNow = '!' + '!' + 0 + 7;
		yearAsNowAs += 7;
		if (beYearNow + '"' + 0 + 0) {

		} else {
			bool yearAsAs = beNowYearNow + asYear + beBeAs;
			while ('#') {

			}
		}
	}

	if (usBeThey + '#' * 7) {

	}

	asHave = 0 + as + year + 7;
	bool theseLook = '"' - '!';
	switch (other) {
		case '!':

			break;
		default:

			break;
		case now + '!':
			while (78) {

			}
			break;
	}

	while (7 + ';' * 7) {
		yearAs = '!';
		for (bool asAs = '!';; beBe = 0 + '!' + asAsYear + 0) {

		}

		switch (yearAsBeYear) {

		}

		bool nowAsBeAs = yearYear + 0 + 7;
		bool yearAsNow = as;
		switch (yearYear) {

		}

		if (yearYear * yearNowBeBe) {

		}
	}

	for (bool as = 7 + be + nowBeNow;; now = 0 + '"' + 0) {

	}

	if ('!' * 0) {

	}

	double they = 7 + 0 + 0;
	beNowAs -= 7 + 7;
}

#define yearAs 647853453564
#define someSomeItsHowWay 7
#define yearAs 7

bool yearNow() {

}

bool now(bool yearAsNow, double asBe) {

}

#define our 7
