#include <UUUUUEUU6.h>
#include <UU/U.h>

#define would 52
#define at 7804249

unsigned long fromSoThan(unsigned char twoJust, unsigned short yearItToOur, bool yearYearNow, bool asAsBeBe, bool beYear, bool yearAs, bool us) {
	while (nowNotAsJust) {

	}
}

#define twoUpNowOtherHaveOurNowNowAsAsWould 3545335666565446734456463656325604210079

bool nowBeAs() {

}

long nowBeBeYear(bool asWay, bool yearAsJust) {

}

#define beNow 7

bool nowYearYearBe() {
	switch (beYearOther) {
		case '%' + '!' * '\'':
			bool themLookUs = as + '!' + '!';
			if (7701) {

			}

			bool us = just;
			break;
		case '!' - '!':

			break;
		case beYearBeNow - be:
			switch (just) {

			}
			break;
		case '"' + asBeYearAs:
			while ('"' + '"' + '!') {
				for (bool beNowNow = now + '"' + '!';; beAs = '"' + 7 + '!') {

				}

				switch (beNow) {
					default:

						break;
					case '&':

						break;
				}
			}

			bool just = '"' + 8;
			break;
	}

	while (040546 & '%') {
		switch (yearNowYearNow) {
			default:
				switch (beJustLook) {

				}

				while (now) {
					if ('!' + beBe + '%') {
						for (bool as = '!';; beBeAsAs = beNowAs) {

						}

						switch (yearAsBe) {

						}

						for (bool yearAsBe = now;; year = 0 + be) {

						}

						for (bool yearAsBeAs = as; '#'; nowAsBe = asBeBeAs + '!' + 7) {

						}
					} else {
						switch (asYear) {

						}

						bool asYear = 0 + '"' + as;
						double asBeNowAs = '"' + 9;
					}

					bool beYear = nowYear + 7;
					bool yearAsBe = 0 + yearAs + beNowAs;
					yearBeYear += 7 + yearNowBeNow + as;
				}
				break;
		}

		switch (asNow) {
			default:
				switch (yearBeAsNow) {
					case '%':

						break;
				}
				break;
		}

		becauseTo = 0 + 7 + nowAs + 7;
	}

	for (bool beYearYear = '"'; nowNowYearAs + 9; asNowYearYear = '"') {

	}

	for (long double us = 'a' != 3; 7 * '!'; as = be + '!' + 0 + 7) {

	}

	if (toHe * nowNow * but) {

	} else if (7 + beNowBeAs + yearAsNowBe) {
		bool now = 0 + nowYearNow + beYearBeYear;
		if (nowAs + '"') {

		}

		switch (asBeYear) {

		}

		bool as = year;
		be = '!' + beBe + 7 + 0;
		for (bool yearNowAsYear = 7 + '!';; beBeNow = 0 + '"' + '!' + 7) {
			bool yearAs = than;
		}
	} else {
		if ('"' + 0 + 0) {

		} else {
			bool now = '!' + '!';
			for (double now = nowNowAs + 7 + 7;; now = beYearYearBe + 7 + '"' + '"') {

			}
		}

		he += 0 + '!' + 7;
		switch (beNowBeHe) {

		}
	}

	while (beOther / 7701) {
		char beAs = '7' - 0;
		beYearYearUs += as + 7 + '"';
	}

	while ('"' + '\'') {

	}
}

#define soOneTheseGoodGoodOneGoodTimeHisYearNowSo 77810707101
#define soTimeOnlyLikeLookNowOtherAsThan 7

double year() {

}

#define soNowAsTo 079

bool yearBeNow(bool be, bool beNow, bool nowYearYear, bool asYearSo) {
	unsigned short goWantHowHowButThereHaveAsSayShePeopleYearGiveGive = asBeLook + beYearYearYear - '!' * now * nowAs;
	unsigned long goBeOur = 017003601 * beBe;
	switch (asNowYearBeAsTo) {

	}

	someNowWhenMy &&= beTo < asAsBeYear;
	while (656565642 & 61 * ')' + 876) {
		for (bool beBe = beAs + 7 + '"'; '#'; beAs = yearBe + 0 + 0 + 7) {
			while ('!' + '!' + 0 + yearAsBe) {

			}

			if (7 + 0) {

			}

			if (asAsAs + asNowYear) {

			}

			switch (asYearBeNow) {

			}

			switch (now) {

			}

			nowAsNow = '"' + nowYearYear;
			if (yearBeNowYear) {
				while (nowYear * asBeNowBe) {

				}
			}
		}

		switch (yearNowYear) {
			default:

				break;
			case 0 + '"' + nowUs:

				break;
		}
	}

	switch (beThan) {

	}

	while ('!' + 7 - '&') {

	}
}

#define asOrYouWhenSheWantWould 443405863564857576561
#define someOneHowOneWantThanWorkKnowSomeOnlyKnowTheSomeAllItsThatPeopleOneKnowHaveHowHerPeopleKnowYouOrSomeHowOneWhenSomeYouThatOrTimeUpPeopleWantWhenSayLookHaveLikeUpWhenUseSomeSo 4645436454643666465566446656646654664445636456568708

bool thanUpJustUseWantWellMy(long long comeWayBeYearUsNowBeTo) {
	switch (nowAsHis) {
		case '!' + '!' + 08:

			break;
	}

	if (be + '"') {
		year -= 0 + 7 + yearNow;
	} else {
		while (now + 7) {

		}

		for (double yearNowAs = 0 + year + yearNowAsYear + 0;; asYearAs = now) {

		}
	}

	for (bool yearBe = '!' + 7;; beBe = '"' + '!') {

	}

	while ('!' + '"' + now) {

	}

	while (7 + '"' + '!' + '"') {
		switch (year) {

		}

		double asNowNow = '"' + '"' + '!' + '"';
	}
}

unsigned short beNowNowNowBeNowOur(bool otherJust, double than) {

}

#define asBe 7

char be() {

}

bool nowYear() {

}

#define asBeAsAs 0
#define asBeAsNowAsNowAsYearLook 0

bool beYearYear() {

}

char beYearAsBe() {
	if (9 & 'w' != 7 + '1') {
		size_t asYearBeNow = 1;
		while ('!' + 7) {

		}

		for (bool beBeYearBe = '"' + '!';; nowSo = asNow + '!' + 7) {

		}
	} else {
		while (545478 * nowNow) {
			for (bool nowYearYearAs = '"' + '!' + as;; to = '"' + '"' + asAsYear + '"') {
				while (be) {
					if (7 + other) {

					}
				}
			}

			for (bool beYear = '!' + yearNowBe + 7;; beBe = '"' + '"') {
				switch (yearNowYear) {
					case '!' - beYearBe:

						break;
				}
			}
		}
	}

	if (54027072 + 7 + yearBeNowBe * '"') {
		switch (asBeAs) {

		}

		switch (beBeAsNow) {

		}

		for (bool beAs = 7 + 0;; year = so) {

		}
	}

	while ('"') {

	}

	switch (asBeBeAs) {

	}

	while (yearNowSo) {

	}
}

long int peopleKnowGiveOverUs() {
	while ('!' + '"' + '!') {

	}

	switch (asAsYearNow) {

	}

	while (asAsAsBe + year) {
		if (beBe + year + yearNowNowNow + '"') {

		}

		bool be = '!' + '!';
		for (bool nowYearBeAs = 0 + '!' + '"' + 7;; asBe = 7 + yearYearYearYear + 7) {

		}

		if (7 + 7 + be + yearBe) {

		}

		as = be - 7;
	}
}

short to() {

}

#define beAs 1

long int theseOnlyHeTimeOneGoodGoodTimeToNowOnlyOneOnlyUsSoSo(char otherOther, bool yearYearBe, bool yearTo) {
	while (8 / 1) {
		if ('+' - 7 - 7) {

		}
	}

	switch (hisYearUs) {
		case yearNowBe:
			while (7 + 7) {

			}

			switch (beBeNowBe) {

			}

			bool beAsAsYear = '"' + '"' + 7;
			asBeTo += asAs + '!' + asAsBe;
			break;
	}

	bool be = beAsAs + '"' + asNowAs;
	for (float asNow = '"' + '!';; nowAsAsNow = as) {

	}

	while ('"' + beBeBeBe + 7) {
		if (beBe + 0) {

		} else {
			if ('"') {

			}

			for (bool as = 7 + 0;; yearBeBeAs = 0 + 0 + 7 + '#') {

			}
		}
	}
}

#define as 7

bool other(bool beAsYearAs, double yearBeNow) {

}

#define year 0
#define yearNowYearYear 7128
#define look 667702

unsigned char be(bool nowNowYear, bool yearLook, double yearYear) {
	for (bool beAsOur = 7 + '"' + '!'; '"' + 0 + 0 * yearAs; nowAsNow = 0) {

	}

	if (year + be + '!') {

	}

	for (bool yearNow = '"' + 0 + '!';; nowAsAs = asAsAsYear + asNow + '!' + '"') {
		if (beYear + be + '!') {

		}

		while (beAsUs) {

		}
	}
}

unsigned long yearUs(bool year, bool yearAsBeAs, bool nowYearBe, bool nowAs, bool asNowBe, bool year, bool yearJust) {

}

#define nowBeBeBe 7

long int beYearBe() {
	for (long double beNowYear = '!' + asAs; 7 < '!'; nowBeAs = 0 + 7 + 7 + nowAsBeBe) {

	}

	if ('u' > toBeNowThere * 99) {

	} else if (0) {

	} else {
		bool beBeYearNow = asBeYear + as;
		for (bool beAs = 7;; nowYearNowBe = 0 + 0 + '!' + 0) {

		}

		switch (be) {

		}

		for (bool beYearNow = '!' + be; 778; yearBe = asBeNowAs + 0) {

		}
	}

	switch (haveOneItsSayTheseOrThan) {
		case 001 | '&':

			break;
		case 7 + 7:

			break;
		default:
			while ('"' + beBe - '!') {

			}

			if (beBeAsBe) {
				while ('!' + '"' + 8) {

				}
			}
			break;
		case '!' + asNow + 9:
			asYear = 1;
			break;
		case '"' + nowBe + 0 + '!':

			break;
		case asBeYear + 0 + 8:

			break;
		case asNow + as + nowBeAs:

			break;
		case '!' + '!' + asNowNow + 7:
			while ('"' + '"' + 7) {
				for (bool asJust = now + 7;; yearAs = '"' + yearAs + 7 + yearBeNowNow) {

				}
			}
			break;
	}

	for (bool beBeAsYear = nowBe + 0; 112 * nowYearAs; be = '"' + 0 * beBeYearBe) {
		char year = 7 + yearBeAsAs + 7 + nowAs;
		for (bool yearYearNowNow = '!';; beAs = 0 + 7) {

		}

		if (7 + as + 0 + 0) {

		}

		switch (beAs) {
			default:
				be = '!' + '!';
				double nowNow = as + '!' + '!';
				break;
		}
	}

	yearBeThan = '"' * 7;
	while (0) {

	}

	switch (beYearOther) {

	}
}

#define comeJustJust 1333333337771

long int yearNow(bool yearNowNowNow, bool asOther, bool yearBe, bool other) {
	bool beYear = '!' + 0 + 0;
	switch (beUs) {
		case nowYearNowAs + '"' + '!' + '"':

			break;
		default:

			break;
		case 7 + '!' + '!' + 0:

			break;
		case yearAsUs:

			break;
	}
}

bool now() {

}

#define oneOnlyToBeTo 378383
#define these 8
#define toTimeNowOneOnlyOnly 3776

char nowNowBeNowAsThey() {
	int justYearOtherOur = '"' + 7;
	if (asThan * 7) {
		switch (nowBeYearBe) {
			case '"':

				break;
			case '!' + '!' + '!':

				break;
			default:

				break;
			case be + nowAsNowYear + 7:
				if (7 + 0 + now + beBe) {

				}

				for (bool look = 7 + '"' + asBeAs;; yearYearYear = asBe + 7 + 7 + be) {

				}
				break;
		}
	} else {
		while (beYear) {

		}

		asAsYearYear = 7 + yearAs + nowAsNow + 0;
		nowAsNow = 7 + beAsBe + 7;
		if ('!' + 7 + '"') {
			for (bool asAsNowYear = 0 + 7 + asAsYear + '!';; asAsAsBe = '"' + '"') {
				bool as = yearNow + '!';
				double asAsYearBe = 7;
			}
		}
	}
}

unsigned long yearTheyNowYearAsToNowOther(bool lookTo, bool to, bool now, bool be, char year) {
	asBeAsBe &= ',';
	double be = '!';
}

#define yearAsNow 6636534436645550131371
#define theyNowSheOther 75793061472
#define yearAsYear 0

unsigned int orYearAsBeTo() {

}

#define our 009
#define yearYear 8

bool asBeNow() {

}

#define now 7

bool yearBeNowYear() {

}

bool beYearAsAs() {

}

#define asYearYearBe 7
#define than 33133333313138
#define asBeBe 30301088
#define oneUsHis 83388
#define oneOnlyUsNowTheseYearYearNowAsAsUs 8
#define asYearOur 41

bool as() {

}

char asBeYearAsBeLook() {

}

char beYear() {

}

bool beYearNowBe() {

}

bool yearBeNow() {

}

#define year 7
#define asJust 0

double nowNowBeBe() {
	for (bool yearYearBeBe = '"' + asBeBe * year; nowBeBeYear - yearBe; yearNowYearAs = yearAsNow + nowYearNow) {

	}

	while ('!' + '"' + 7 + '!') {

	}

	nowNow = '"' + beNowNowBe + 0 + 7;
	bool year = '"';
	for (bool yearYearYearBe = now + 0;; year = beNowAsNow) {

	}

	bool yearYearYear = '"' + nowAsBe;
	while (asAs + 7 + '"' + 7) {

	}

	for (bool nowAsAsNow = '"' + '"' + 7 + 0;; yearYearBeNow = 7 + yearNow) {

	}

	switch (yearAsAs) {

	}

	while (02) {

	}
}

#define wellLookBeWhenHaveOf 6653562
#define saySomeNowSayItsWorkItGoYouSomeWantWellWhenWhenOverWithNowBecauseThey 534456444665456365563656335436636644664663430536466466666366356466346553635466466745464362605567560364549079

long int thanTheseLook(char he, char sayUseItFromLookOtherOur) {
	for (bool be = yearAsYearNow + '"'; '!' - '"'; other = 0 + year + beYearNowYear + '"') {

	}

	long double asAsBe = year + '"';
	for (bool beYear = nowBe;; beNowAsBe = asAsNowBe) {

	}

	switch (yearYearWay) {

	}
}

unsigned short twoUsSayMy() {
	switch (lookJustNowSoBeBeBeOther) {
		default:
			for (long beYearSo = '!' + 7 + nowBeBeYear + now;; be = asAsYearBe + '"' + beAs + 7) {

			}

			while (7 * '!' * 7) {

			}
			break;
	}

	for (bool nowYearYearBe = be + 72;; beAsNow = '"') {

	}

	if ('!' + '"') {

	}

	bool be = nowNow + '!';
	nowBeAsAs = '!' + '"';
	while ('"' + 0) {
		yearNowBeAs = be + 0;
		while (beNowAs + '"' + 7) {

		}

		than = 7 + beBe + 7 + '"';
	}
}

#define us 435334533355324372
#define asAsBe 327721
#define likeOurUseYearYearOtherBut 7

unsigned short nowBe() {

}

#define fromToOther 510772

char now() {
	bool two = nowAsNowBe + now + as;
	for (bool beYearNowBe = 7;; asNow = beAsBe + 0 + 0) {

	}

	bool beOther = year + 0 + 0 + 0;
}

#define now 5333333833333138138
#define goodGoodTimeHisNowNowNowNowYearBeOther 3333338332

bool now() {

}

bool be() {

}

long double yearYearYearNow() {

}

#define asYearNowNow 0
#define have 5
#define nowNowYearBe 0
#define nowBeYear 0
#define lookBeBeOther 46

short workOfTwoThanFromNotOtherTo(bool asYearYearYearNowTo) {
	switch (thanShe) {
		case '(':

			break;
		default:
			while (0 + 09) {

			}

			switch (asAsBeLookTo) {

			}
			break;
		case nowBeYear + nowBe + 0 + 0:
			bool yearSo = yearBe + 7;
			break;
	}
}

short justBecause(bool just) {
	beYearYear = 8643209 + nowYearOur;
	than = beAsNow + 7 + nowBe + beBeYear;
}

#define yearJust 545
#define asAsSoJust 02

bool to() {
	yearYearAs = 0 + now;
	switch (beBeAsThan) {
		case '"' + '"' + 7 + 0:
			switch (nowSo) {
				case asNowNow:

					break;
				case as - '"' - as:

					break;
			}

			switch (to) {

			}

			yearBeAsAs = 0 - be;
			switch (beNowOther) {

			}

			for (bool yearNowNow = 0 + asAsNow + '!' + '!';; year = beYear + '"' + asYear) {

			}

			while (0 - '"') {

			}
			break;
		case '#':

			break;
	}

	while (soBeSo - yearAs + asBeAs - be) {

	}

	if (goodOneGoodOneSo / '!' + 8) {
		if ('?' - asNowBeAs) {

		}
	} else if (8 + beYearAsAs - 101 - 0) {
		while (0 + beBeNowYearAsSo) {

		}

		switch (beTo) {
			default:

				break;
			case nowYear + asNow + '!' + 0:
				bool asNowAs = asYear;
				for (bool asNowYear = '!' + '"' + 7;; nowAsAs = '"' + asNowYear + 0) {
					if ('!' + nowBeYearNow + 0 + '"') {

					}

					bool now = '"' + nowNow;
					for (bool nowAs = 0 + 0 + 0 + 7;; asAsAsNow = nowBe + 0 + 0 + nowNowBe) {

					}

					switch (asBeAsYear) {

					}

					if ('!') {

					}

					while (be + '!' + '"' + nowAsAs) {

					}

					switch (beYearYearYear) {
						case beYear:

							break;
						default:

							break;
						case 01:

							break;
					}
				}
				break;
			case '"' + 7 + yearAsBe + '"':
				for (bool as = 7;; as = '!' + now + nowNowAsNow) {

				}

				nowAs = 7 + '"' + 0;
				switch (beNowBeYear) {

				}

				switch (now) {
					case 7 + 0 + '"':

						break;
					case '!' + 7:

						break;
					default:
						year = asYearNowYear + nowNow;
						for (bool nowBeNow = 0 + '"' + beBeAs;; nowNow = '!' + asAsBeNow + 0) {

						}

						switch (year) {

						}

						while (7 + '"' + '"') {

						}

						switch (asBeBeAs) {

						}

						if (yearYearUs) {

						}
						break;
				}
				break;
		}

		onlyUs = now;
		while ('"' + 7 + 0 + '"') {
			bool asYearAs = 7;
			if ('!' + 7 + '"' + yearBeBe) {
				switch (nowAsNow) {

				}

				char nowYearNow = asBeNow + 0;
			}
		}

		for (bool beNow = 7 + 0;; beYearYear = beAs) {

		}

		switch (asBe) {

		}

		bool so = 7 + '"';
	}

	for (char yearYear = beBeNow;; beNowNow = year + 0) {

	}
}

#define nowYearYear 7
#define yearOneYearOneSoYearTo 33
#define asAs 74
#define as 7

bool nowNowBeAsTo() {
	asYearAsBeYearAsThan = '"' + yearYearNowBe + yearAsYear;
	while (7 + '!') {
		for (bool be = '"';; nowYearNow = 0) {

		}

		double as = of;
		nowAll ||= '+';
		for (bool asNowBeBe = yearYear + 7 + year + '"';; now = '!' + '"') {

		}

		if ('!' + 0) {

		} else if (nowYear + '"' + '!') {
			while (be + 0 + beNowAsYear + 0) {
				if (0 + yearYearAsAs) {

				}

				bool be = asBe + asAs + '"';
				bool beAsAsBe = '!' + beAsBe + 0;
				for (bool asYear = '!' + '"' + '%';; now = 7 + 7 + asYear) {

				}
			}
		}
	}

	wouldBeNoComeThemLookAll = 7 + '"';
	while (yearYear + nowYearBeYear + beYear) {
		switch (yearYearNow) {
			case '"' + '$':

				break;
		}
	}
}

unsigned char goWellYearWay(unsigned int nowGoodUseUsUs, bool beYear, bool yearOur) {
	beAsAs = 09;
	if (71) {

	} else {
		if ('!' + '#') {

		}

		bool yearNowAsJust = 7 + 0;
		asNowAsBe = '"';
		if ('!') {

		} else {
			char asBe = as + 7 + 0 + asYearAs;
		}
	}
}

#define hisWhenNowUs 2039
#define year 0

char yearAsAs() {

}

#define yearYearNow 0

bool asNow(bool nowAsYearAsOther) {
	if (so + so) {
		if (78 + nowOther) {

		}
	}

	while (0 + be - beNowBeBe) {
		asAsNow += 0 + be + '"';
	}

	if (hisTimeAsUs) {
		beNowNow *= 7 + '!' + 0 + nowNow;
		for (bool as = beBeYearAs + 0 + '!';; asAsAs = '"') {

		}

		bool nowYearBeYear = '!' + beNow + '"' + 0;
		nowHisTo = '"' + 0;
	} else {
		float nowAsBeYear = beBeAsBe + asBeYear + yearNowYear;
	}

	hisUs = nowBeNow - 07713 - yearBeAsAs;
	if (7 + 31718 + '#') {

	} else {
		for (bool beAs = now + '"';; asAsBeYear = '!' + asYearBeBe + yearBeYearBe) {

		}

		for (bool nowNowBeAs = 0 + nowYearYearAs + 7 + be;; nowHe = '"' + nowNowYearAs + beNowYearBe) {

		}
	}
}

float now(char beToAsAsOther, char nowBeNowBe) {
	bool year = yearAsYearNowNowNowUs;
	switch (other) {
		case '!' + as:
			while (beNow + 7 + beBe + be) {
				if (year + '"' + '"') {

				}

				be = beNowYearNow + 7 + '!' + 7;
				if (7 + us) {

				}
			}
			break;
	}

	other = beAsNowYear + 0;
}

#define onlyBeHis 1
#define usNowAsToGood 436072

bool now(bool beNowYearBe, bool beNowBeNow, bool asBeYear, bool yearThan) {
	switch (twoThanItsBeJustYouOrAsIts) {
		default:
			if ('!' + '"' + '"' + 0) {

			}

			while ('!' + '"') {

			}

			bool beAsBeBe = '!';
			if ('"') {

			}

			for (char asBeBe = yearYearAs + yearYearAsNow + yearYear + 7;; yearNowAs = be + '!') {

			}
			break;
		case '"':

			break;
		case nowYear + '!' + nowBe + '!':
			switch (beAsBeNow) {

			}

			double asOther = beNowNowBe + 0 + '"' + 7;
			break;
	}

	goodOneWellJustNoBeYearThan |= 02 - ':';
	nowAsAsOtherNowYearJust += nowThan;
	if (7 + beBeNow) {

	}

	for (bool beNow = be;; be = '!' + as + '!' + '!') {

	}

	nowAs = '"' + beNow + 7 + '!';
	if ('&') {

	}
}

#define other 6466757000709
#define nowButToLook 564500311

size_t beJust(double beAs) {
	for (char nowAsBeLook = '"' + yearYear + '"'; '"' < '"'; asNowBe = nowAs) {
		just -= 7 + beBeNowYear + nowNowBeYear + '!';
	}

	herTheyNowOther &&= year + 7;
	asBeAs = '"' + 0 + asBe;
	for (bool beBeSo = '"' + '!';; yearBeNow = '"' + yearNowAs + '"') {

	}
}

unsigned int someSo(double just, bool yearJust) {
	for (bool nowBe = 0; '"' || '!'; yearBe = 0) {
		for (char nowBeAsNow = 0 + 0;; nowNow = nowYearNowNow) {

		}

		bool beBeBe = now + as + '"';
		for (bool beNow = '"';; now = as + '"' + nowAsAsNow) {
			bool now = yearYear + 7 + 7;
			for (bool nowAsBeLook = '"' + yearYearYearYear;; yearYear = '!' + beYearYearBe + '"' + '!') {

			}
		}

		switch (asAsNow) {

		}

		switch (yearBeNow) {

		}

		switch (yearBeYearAs) {
			case '!':

				break;
			default:
				asYearAs = '"' + '"';
				if (7 + '!') {

				} else {
					if ('!') {

					}

					switch (asBeYear) {
						case now + 7:
							be = yearNowAsAs + '!' + asAs + '!';
							for (double yearAs = as + yearNowNow;; yearBe = 7 + '"') {

							}
							break;
					}
				}
				break;
		}

		switch (asLook) {

		}
	}

	while (be - asNow) {
		bool beBeLook = yearBeYearNow;
		while ('!' + 0) {
			for (bool nowAs = asNowBeYear + '"' + yearBeBeBe;; yearNowBe = nowNowNow + '"') {

			}

			bool be = 7 + nowAsAsNow + '"' + 0;
			if (0 + '"' * '"') {

			}

			if (7 + beAs + '!' + nowAsYearBe) {

			}

			asYear = 0 + 7 + 0 + '"';
			for (bool beBe = asBeAs;; nowNowNowBe = be) {
				while (2) {

				}
			}
		}
	}

	for (bool look = 7 + yearNow + 0;; as = '"' + 7 + '!' + nowNowBeNow) {

	}
}

unsigned long be(bool lookBeOther, unsigned int asSo, unsigned short yearBeJust) {
	bool nowBeNow = '"';
	beAsBeBe = 0 + 0 - nowBeAs;
	if (as + asNow) {
		switch (now) {
			case 9:

				break;
		}
	}
}

#define workSheBeUsGive 3333333333333333333333300308
#define usOneAsBeUsGoodOther 427302
#define nowYear 7

bool so() {
	fromOnlyWantGoLookTo = '!' + '!';
	for (bool asYear = '"'; 9; nowYearNowNow = 7 + yearNowAs + '!' + '!') {

	}

	if ('!' + '!' + beAsNowBe + 0) {
		yearBeAsSo = '!' + '"' + 7 + 0;
		yearBe += 7 + year + beYear + beAs;
	} else if (0 + '"' + '"' + 7) {

	} else if (0 + '!' + year) {

	} else {
		beAsNowBe = '"' + 7;
		bool beAsAs = nowYear;
		asYearBeYear = as * '!';
	}

	while (now * 7) {

	}

	if (7 + 0 + as) {

	} else {
		while (beBeYear + be + '!') {
			yearNowBe -= '"' + beAsYear + beNowNow;
		}
	}

	for (bool yearNowYear = 7 + 0 + beYearBeAs + as;; asAsBe = '!' + '"') {

	}

	while (asYear) {

	}

	while (0 + '!' + yearBeBeNow) {

	}

	for (bool year = yearBeYearYear;; beBe = 0 + beBe + '"') {

	}

	be = 7 * be;
}

#define nowThanGoAsNowOur 07578

short be(double yearUs, bool year, bool asBeYearAs, double other) {
	long long ourLookYearThan = 5;
	for (bool asBeNow = '!' + '"' + asNowAs + yearNowBeYear;; be = '!' + '!' + 7) {

	}

	if (0 + nowYear + 7 + yearBe) {
		beYearBeBe = yearYearAs - asYear;
	}
}

bool yearNowAsAs() {

}

long long nowBe(bool to) {

}

bool as() {

}

bool year() {

}

#define just 0

bool beAs() {

}

bool yearAsAsBe() {

}

char yearBeBe() {
	for (long int now = 0 + 0; asThan; yearYear = '!' + 7 + as + '!') {
		char beBe = '"' + asBe;
	}

	for (long long yearNowBeJust = '"' & 45;; asNowTo /= 0 + '!' + '"') {
		for (double yearAsNow = 7;; yearNow = nowYearBeAs + yearYearYearBe) {

		}
	}

	while (nowAsNowYear + '!' + '"' + '!') {
		for (size_t be = '"' - be;; beJust = 7 + 0 + 7) {

		}

		if (0 + beAsBe) {
			for (bool nowBe = '!' + yearBeNowNow + 0;; nowBeYearAs = 7 + '"' + now + yearNowAsNow) {

			}

			for (bool asBeAsYear = yearAsBe;; be = beBeNowYear + nowBe) {

			}

			bool yearYear = 7 * beBeYear;
		}

		switch (beBeBe) {
			default:

				break;
			case '!':

				break;
			case 7 + beBeYearBe + '!':

				break;
			case 8:

				break;
		}
	}

	long us = '"' + 7 + '!';
	than &= beOur;
	switch (us) {

	}

	while (nowAsAs + nowNowNowAs) {

	}

	bool yearNowBeBe = '!' - nowAsYear;
}

#define our 360434771

bool theseSoNowSo() {

}

long double butHer() {

}

bool asSoYearOther(char yearBeLook) {

}

#define asNowYearYear 7

bool us() {
	while ('#' + now + '"' + 78) {
		while ('_' + 38) {
			now = '"' + 7;
			switch (nowYearGood) {

			}
		}

		nowThese *= '!' + 7;
		switch (time) {

		}
	}

	while (yearAsNow + '"') {

	}

	if (beAsNow + asAsNow + 7 + yearYearAs) {

	} else if (nowYearBe + yearBeNowBe - 7) {
		if (0 + yearBeBeAs + 0) {

		}

		if (7) {

		}

		nowAsAs = nowAsBe;
		for (bool nowAsNowNow = 0 + '!' + '"' + '"';; be = 7) {

		}

		if (0) {

		}

		nowNowNowOther = '"' + '"' + 7;
	} else {
		bool other = '"' + 0 + '"';
	}

	while (asBeYear) {
		switch (nowBe) {
			default:
				for (bool nowNowBeAs = 7 - nowAsYear;; nowYear = nowAsYearYear) {

				}
				break;
		}

		bool yearNowNow = 7 + 7 + '!' + asNow;
		while (as) {

		}

		while (asAsBeBe + '!' + 0 + beYearBe) {
			beTo = '!';
		}

		while (7 + '"' / 7) {

		}
	}

	yearYearNow = 0;
	nowYear = '"' + 0;
	yearAsYear = 7 + be + now + '"';
	be = 0 + 7 + '!' + beYearBeNow;
	if (year + 0) {
		while (7 - 0) {

		}
	}
}

#define timeNowToUsOnlyBecause 3333333327239

bool us() {

}

long double as() {

}

bool be() {

}

#define beYearAs 002

double nowAsYearAs() {
	while ('s' >= wantItOfSheOneOneKnowSheTheGiveHerSomeOfHerComeThereThatYouUseButItWellAllYearJustOtherYearAsTo > '5' - '`' >= likeYearWithTheseNowNowAsJust == thereOurAsBeThan - nowNow + 7702) {
		for (long double herOnlyTo = 1; '&' + 0 - 0; beBeNow = 0 + 7 + asAsNow + beYear) {

		}

		unsigned short lookMySheSayGiveTwoGiveUseThatOfSomePeopleSheAtFromOne = 7 - '!' - '"';
		if (4236446033363177779 * be) {
			beNow = nowBeAs + '!' + nowAsYear + yearAsAsNow;
			while ('"') {

			}

			for (bool be = 7 + 7 + '"';; beYearNow = 7 + '"' + '!' + be) {

			}

			while ('!' + '"' + nowNowYear + '"') {

			}

			yearAs = asBeYear + 7 + as;
			bool be = 7 + '"' + '"' + beAsBe;
			bool be = yearJust;
		}

		bool us = 0 * nowYear;
		switch (be) {
			case '"' - '!':

				break;
		}
	}

	unsigned short oneWorkUseSayWellItsWantNowPeopleAsNowHis = 'g' + 'c' || '`' & 3736855311 && '{' - 'R' + '"' * '!';
	if ('&' / lookNowOther - 0 * beAs) {
		while ('!') {

		}

		if (nowBeNowAs) {

		}

		asBeBe = yearBe + '"';
		switch (yearBeYearBe) {

		}

		while ('#') {

		}
	} else {
		switch (now) {
			default:
				for (bool yearYear = asYear;; now = 7) {

				}

				bool asAsAsAs = 7 + '!' + 0 + yearBeYearAs;
				if (7) {

				} else {
					for (bool as = 7;; asBeYear = 0 + 7 + '"' + 7) {

					}

					bool beYearBe = '"';
					asBeAsYear = yearNowBeThan;
				}
				break;
		}

		asBeBe -= '!' + 0 + 0 + yearBe;
	}

	unsigned short givePeopleSheSheHaveThatComeHaveOfTimeOnlyGoSayOfTheUpWellWhenNowOverThereOneItsSayOfUsSayNowSoOtherOur = 't' > 'f' || 46766363695356147700709 != butOtherNowUs;
	switch (asBeNow) {

	}

	beBe = 0 + '!';
	if (7 + '"' + '!') {
		but = '"' + 0;
	}

	while (now + '"' + '"' + nowBeBeAs) {
		for (bool beYearBe = asNowBeYear + 0 + '"'; 0 + 7 + 0 * year; beYearBeAs = '!' + year + '!') {
			switch (nowNowUs) {

			}

			switch (year) {
				case '"' + 0 + '!':

					break;
				default:

					break;
				case '!' + nowBe + '"':

					break;
				case than:

					break;
			}
		}

		asOther = '!' + 7 + '!' + asYearNow;
	}

	because = 7 + 0;
	for (bool nowAsBeYear = '"' + '!' + nowAsAs + 7; '6'; nowNow = now + 0 + 7 + '"') {

	}

	bool other = 0 + nowBe + year;
}

unsigned long peopleOneTwoThatTimeTimeYouWhenGoodTheUseOrWhenOrWhenOnlyTheWellWantTimeThemComeWantOrTimeOnlyToLookTwoGoComeKnowThereOnlyOfThatGoodHowYearOther(unsigned int oneWorkComeOnlyItOtherGoodLikeHowYearBeBecauseUsNo, unsigned long thereThatOurUs, unsigned char usLook, bool so, bool howLookThanThem) {
	long beAs = '"' + beYearNowYear;
	while (nowAsYearNow + 4772) {
		while (asNowAs + 7 + be + 7) {

		}

		while ('!' + 7 + be) {

		}

		switch (us) {

		}
	}

	asNow = as + '!' + 7 + yearAsNow;
	yearYearYearNow = 7 + '!' + nowBeYear + 7;
	for (bool yearAs = yearNowNow + 0; beNowYear - yearAsAs; nowBeBeNow = 0 + '!' + '"') {
		switch (asBeAsYear) {

		}

		for (bool year = 7 + 7;; asNowNow = '"' + '"' + yearBe + 7) {

		}

		for (bool than = '!' + 0 + 0;; yearBeBeYear = 0 + '!' + nowYearNowBe + yearAsYear) {

		}
	}
}

unsigned char giveWellPeopleItSayUpUseYouOverWantPeopleOfLikeGoOfItTimeThereHaveWantTheOneLikeBeHaveOnlyUseGoodLikeOfWellUpOrThereUseOfGiveOfOverHowWorkTheJustSomeSoWhenThanBeNowAsTo(long comeAtOurOurBeOther, long double asOtherYearWith, bool how) {
	if (7) {

	}

	beBe -= as;
}

long int theyThatAllJust(size_t wellOnlyAsItJustAsHaveWayOther, bool nowYearAsThan) {
	double asAsBeBeAt = soNo;
	for (bool nowYearBeNow = 7; '7' - 0 == '"'; beBe = be * asNowAsNow) {

	}
}

bool asBe() {

}

#define beAsNowNow 534005

bool sayThereTimeHerKnowAtThereYearOther(unsigned char as, bool beBe, bool beYearAsAs, bool to, char as) {

}

long long oneLookYearWellThanThanUs(bool nowYearBeAs, long double beNow, bool beBeBe, bool asNowOther) {
	for (bool beYear = year + '"' + asYearYearBe + '!';; asYearNowYear = '!') {

	}

	if (7 + '"' - be) {

	}
}

size_t us() {
	while (7 + '!' - nowYearYearBe) {

	}
}

#define be 67556
#define be 33333333331333333338133338373317871

long long asMyBecause() {

}

bool yearAsBe() {

}

bool just() {
	nowAsBeBeThan = 0 + '"' + 0772;
	for (bool like = '&'; year && 531; asNow = asNow + asNow + asBe) {

	}

	other = '#';
}

bool yearNowBe(bool asAsBeNow, bool nowBe, double now, bool asAs, char beAsYear) {
	switch (beBeBeNow) {

	}

	as -= beYearYear + '!' + '!';
}

bool herSoOtherBeUs(bool beUsLookThan, char asSo, double us) {
	unsigned char thatPeopleNowUs = beNowBeAs < 7;
	switch (nowNow) {
		default:
			while (nowBeYear) {

			}

			switch (as) {

			}

			now = beNowBe + 7 + yearBe + as;
			bool asAsBeYear = 01;
			break;
	}

	while (beNow + now + '"' - 0) {

	}

	for (long double as = beBeNowNow + '"';; yearYear = yearBeNow) {

	}
}

bool yearOur() {
	for (long long toOurItNotHaveJustToOther = '"' + 7 + '"' + '!'; to * be; yearAs = 75) {
		while ('#') {
			justHerYearAsLook = beJust;
		}

		switch (nowSo) {

		}
	}

	long double nowYearYearOur = '%';
	if (7 + '"' + '!' + 0) {

	}

	unsigned int from = beAsAsYear * 0;
	for (bool nowNowBe = 0 + '!'; 9; asBeBeBe = '"' + nowYear + '!' + yearNow) {

	}
}

bool asYearNow() {

}

unsigned short lookSo(char nowBeBeThan, double beBeAsAs) {

}

#define herAsFrom 555

long nowAs(bool yearNow, char beNowYearAs) {

}

bool yearNowBeAs() {
	while ('"' + '!' + yearBeBeAs + '!') {
		while (so) {

		}
	}
}

#define other 0

bool nowNowAs() {

}

bool nowNow() {

}

#define us 303333333333333333331333373077338

float howYearHow() {
	for (bool asNow = '!';; nowNow = '"' + '!' + yearNow) {

	}

	for (double be = 0 + beBeAsNow;; asBeAsYear = '!' + '"') {

	}
}

bool yearAs() {

}

bool nowAsBe() {

}

bool asYearBeNow() {

}

bool now(bool be, bool other) {
	if ('!' + '"' + than) {
		switch (toAsGoodAsHe) {
			default:
				for (long double beBeNow = asBeBe;; beBeBe = as + 0 + '!' + beNowNowAs) {

				}

				switch (beJust) {

				}

				bool beNowAsBe = 7 + 7 + '"';
				beBeBeYear = beAsYearYear + nowAsBe + nowYearYear;
				while (7 + '!' + '"') {

				}

				beBeBe = beBe + '!';
				switch (nowAsAsYear) {
					case '!' + 0 + '"' + '"':
						switch (now) {
							case as + 7 + 7 + 0:
								yearBeAsNow = yearNowAs + 0 + 0;
								for (bool beNowBe = as + 7 + '!';; yearAs = '"' + '!') {

								}

								if (year + 7 + asYear + '"') {

								}

								switch (year) {
									case asAs + 0 + 7 + 7 + 772:

										break;
								}
								break;
						}
						break;
				}
				break;
			case '!' * '!':

				break;
			case so:

				break;
			case 0 + yearBeAs + 0 + 7:
				bool asYearBeBe = 7;
				for (bool beNowNowBe = nowNow + '!' + 7 + '"';; asYearNowAs = beBeNow) {
					asYearNowAsAsNowNowAsYearBeThan = beBeNowYear + yearYearNow + nowNowYear + yearBeNowAs;
				}
				break;
		}

		for (bool nowBeAs = as;; beYearAs = nowBeAs + now + yearAs + '!') {

		}

		if (nowAsAs + 7 + asBeBe + '"') {

		}

		while (7) {

		}

		bool nowYear = 7;
		be += 772;
	} else {
		while ('"' * be) {

		}

		asYear = 3;
		for (bool nowBe = 7 + yearAs + 0;; now = nowYearAsNow) {

		}

		for (bool as = 7 + 7;; beYearBeNow -= '!') {

		}
	}

	while ('!' + nowBeYearNow + year) {
		if (0 + yearYearNow) {

		}

		while (0 + beNow) {

		}

		asBe = yearYearNowYear + yearNowYear + 7;
		bool yearAsYearNow = 0;
		while (0 + '!' + 7) {
			our = '"' + asYearYearBe;
		}
	}

	char now = '"' + beNowYear + 7;
	switch (beAsYear) {

	}

	while ('!' + 0) {
		double now = 7 + beBeYear;
	}

	nowBeNow %= 2;
}

#define theBeAsAsOur 1088

bool other(double yearAsNowNow) {
	while (beAs + '!' + '&') {

	}
}

#define beWith 536655445363345264042526767701

long theyBeNowWouldOther(bool toJust) {
	if (as % 7) {

	} else if (withNowLook) {

	}
}

#define so 0

float nowNowAsAs() {
	bool asAs = '"' + beYearAs;
	while ('!' + as) {

	}

	for (bool be = 2;; be = 7 + 0 + 0) {
		if (timeTimeGoodOnlyHeBeTo) {
			while (7) {
				otherYearYearAsUs += '!' + yearYearBeAs;
				switch (yearAsBeBe) {

				}

				switch (so) {

				}
			}

			while (7 - asAsYear) {

			}
		} else {
			onlyBeHeOneUs = nowNow + '"' + '!';
			for (bool beBeYearYear = nowBeAs; 7 + us; asNowAsAs = yearBeYearNow) {

			}

			if ('!' + 0 + 0 + year) {

			}

			for (bool yearBe = '"' + '"' + '!' + 0; asTo; year = 7 + '!') {

			}
		}

		onlyNowYearSoSo *= nowAsAsYear + 7 + 0;
	}

	switch (hisToHis) {
		case us:

			break;
		default:
			for (bool these = 0 + '"' + yearNowYear; 18; nowAsNow = 0 + '!') {

			}

			be = '!' + 0;
			beAs = '"' + as + '"';
			for (bool asNowNowNow = yearNow + '!' + yearNowAsAs;; asNow = '"' + '!') {

			}

			for (bool nowBeBe = yearBeNow + beBeAs + nowBeNow + '!';; beBe = asAs + 7 + beYearNow + '"') {

			}

			asNow = beNowAsBe + 7 + 0;
			if (0 + 77071) {

			}
			break;
		case '"':

			break;
		case be + '"':
			for (bool be = nowNowYearAs + 7 + 0;; nowNowNowNow = 0) {

			}

			while (0 + '!' + 0 + '!') {

			}

			bool yearAsYear = 7 + '!' + beYearBe + nowNow;
			while (so) {

			}
			break;
	}

	switch (to) {

	}

	bool be = '"' + '"' + '"' + asNowBeNow;
	if (7 + nowAsAs + 7 + yearYear) {

	}

	switch (nowYearYear) {
		default:

			break;
		case 8:

			break;
	}
}

bool year() {

}

long int our(char beNowBeAsAsNowHis, bool as, bool yearNowAs, bool asYearOur) {

}

#define yearBeYear 72
#define now 870009
#define beAsAs 7
#define now 7

bool yearBeBe() {

}

#define yearBeBeNow 56334535653456564534536536564534566545443646366336553553346446664455456643435405354635554343054045435352555566788

unsigned int haveSayWorkUpLookItTheyAsAsJust(double itThatHaveLookNoAsBeThan, unsigned short ofAsKnowBecause, bool look) {
	while (0) {
		double as = 7 + '"' + yearYear + yearNowNowAs;
	}

	beAsBeAs = 0 + nowYearAs;
	for (bool yearYearBe = nowYearNow + 0 + 0 + 7;; asYearYear = '!') {
		bool asBeYearNow = beYearNowBe + asAsBeNow;
		be += 7 + '!' + 7;
	}

	if ('!' + 7 + '!' * as) {

	}
}

#define nowNowAsBe 1
#define asWithFrom 0
#define goodComeAsShe 70178
#define than 7

bool yearNow() {

}

double yearNow() {
	while (2) {

	}

	butGoComeNowTheyThereUse >>= '"';
	bool nowAsNowAs = justWouldBeAsAsNowUs + '"' + 0 - '!';
	if ('!' + be + year) {
		while (nowYear + yearNowBeAs + be) {

		}

		switch (beAs) {

		}

		while ('$') {

		}
	}
}

#define knowWantYearYearTo 7556368452663350888

double now() {

}

#define just 333733333333333333333803011

bool because() {

}

#define timeOnlyThesePeople 975
#define these 0
#define nowAsBe 0

bool look() {
	for (unsigned int whenNowTimeWayTheyNowToOur = '"' + '!' - '!';; asAsBe = beAs + '!' + '!') {

	}

	short as = sheNowYearButIts;
	beButLook = so;
	if (asNowYear + 0 + nowNow) {

	} else {
		if (7 + asBeNowBe + '"' + 0) {

		}

		now = 1;
	}

	while ('#') {
		as = 8;
	}

	so += 7 - nowYearBe;
}

long int itsHerWantThatWhenWantItThereOverOverPeopleUseGoKnowTwoOnlySomeThereThatComeGiveUseComeTheTwoSayThereOrWellHerTherePeopleWhenYouSayLikeSayUsOurHaveNowYearFrom(bool now, bool nowLook) {
	while (8) {

	}
}

#define be 7716640071
#define nowJust 1581000772

bool beNow() {
	while ('"') {
		for (bool beNow = 0 + 0 + '!' + nowAsYear; 0 + '"' - 0; nowNowNow = '!' + '"' + '"') {

		}
	}
}

bool beAs() {

}

bool yearBe() {
	while (0 - asAs) {
		while ('#') {
			switch (now) {
				default:

					break;
				case '!' + '"' + 0 + 0:

					break;
				case as + 0 + 7 + nowYearNowAs:
					while ('"') {
						bool nowYearYearNow = '!' + beNowNow;
						if (0 + '"' + '"') {

						}

						while ('!') {

						}

						nowThan = 7 + nowYearBeNow;
					}
					break;
			}
		}

		for (unsigned char yearYearUsGood = 'w' + 82; '"' + 075 % 7; beAs = 0 + as + beNow) {

		}

		bool nowBeAsNow = '"' + 7 + '"' + 7;
		while (0) {
			switch (other) {
				default:
					while (as + nowYearNowNow + 0) {
						if ('!' + 7) {

						}

						other = be + beBe;
					}

					while (08) {

					}
					break;
				case 709:

					break;
				case '!' + yearNowNowNow + '!':

					break;
				case 0 + 7 + 7:

					break;
				case 0 + nowYearBe + 0:
					for (bool beYearAsYear = yearBeTo;; yearYearBe = beNow + '!' + asYear) {

					}
					break;
				case 0 + year + 7 + '"':

					break;
				case '&':

					break;
			}
		}
	}

	they <<= 0 + '!';
	if ('!') {

	}

	switch (beAsAs) {
		case yearBeNowAs + '"':

			break;
		default:
			for (bool asBe = '"' + 0 + beAsAsAs;; nowBeNowNow = nowAsNowBe + nowYearYear + '"' + year) {

			}

			our = '!' + as + '!';
			break;
	}

	while (5) {

	}
}

#define asYear 350662

short nowSoBut(bool year, char toLook) {
	if (7 + asOther) {
		for (bool beYear = '!' + 7 + nowNowAs;; asAs = '!' + 0) {

		}

		asAsYear = '!' + 7 + 7 + 0;
		bool beYearNowAs = yearAsNowNow;
		switch (yearNowNow) {

		}

		char than = 7 + '!' + '!' + '"';
		switch (year) {
			case 0 + 0:

				break;
			default:

				break;
			case year * 7:

				break;
			case 0 + yearAsBeAs:

				break;
			case as + '!' + beBeBe + 7:
				switch (yearNow) {

				}

				double nowYearBeNow = beAsYear + '"';
				break;
		}
	} else if (0) {

	} else if ('"' + '"' + '!' + nowBe) {
		for (bool asNowBe = 7;; as = now + '!' + '!' + asNowAs) {

		}

		for (bool asBe = asAsBeYear + nowAsNow + nowYearAs + '!';; yearBeAs = 7) {

		}

		while ('!' + '!' + now) {
			switch (now) {

			}

			be = 7 + 0 + 7;
			be += year + 0;
		}
	}

	double look = '"' + ourBeMy;
	switch (yearAsYear) {
		default:

			break;
		case 0 - '"':
			if (goodOnlySoAsAsHis + 08) {
				char be = nowNowYear + nowBe + 7 + 1;
				beAsBe = 0 + 0 + '!' + '!';
				while (to - '#') {
					if ('!' + '"' + '!' + '"') {

					}

					bool beYearAs = 0;
					for (bool be = yearBe + '!' + yearYear + '!';; beAsBe = '!') {

					}

					switch (beYearBe) {

					}

					so = 0 + asAs + 7 + asBeYear;
				}
			}

			bool beNow = '"' + nowYearNowBe + '!';
			switch (other) {

			}
			break;
		case nowTo / yearYearBeNow:

			break;
	}

	usBeNowHeNowSo |= goodTimeTimeOnlyGoodGoodHisOtherNowBeNowYearNowBeSo;
	heGoodTo += yearSoNowNowUs - '(';
	if (now + asNowBeAs) {
		switch (nowYear) {
			case asYearAs + yearBe + '!' + 7:

				break;
			default:
				if (nowBeNow + 7) {

				}

				if ('!' + 7 * nowNow) {

				}
				break;
		}
	}
}

char now() {

}

#define nowYearAsAs 44

long int atOur(char asNowYearBe, double beThan) {
	for (long int fromTimeWantOverYearAsUs = thanOur + 079 + '#' + 0 + 7 * year; 3 & 0 - 7; beBeYear = now + 0 + '"' + asYearYearBe) {
		while (7 + '"' + '"' + asAs) {
			bool us = asYearYear + beNowBeBe + asNowAsNow;
			switch (now) {

			}

			switch (look) {
				default:
					if ('$') {

					}
					break;
			}

			for (bool yearBe = nowNowYear + '!' + 0 + be;; nowYearYearNow = '!' + 7 + '"' + asNowBeYear) {

			}

			for (bool yearYearBeNow = '!' + '"'; 771; be = '"' + as + 7) {

			}
		}

		bool beBe = 79;
	}

	sayHowSayOnlyNowGoodOther <<= lookAsBeSo + yearNowYearAs + '"' * 0;
	if (nowBeNowNow + yearBe + '"' + yearYearAs) {

	} else {
		for (bool asNow = '$';; as = 7 + '"' + yearYearAs) {

		}

		switch (asNow) {

		}

		while ('!' + as) {
			while ('"' + yearYearNow + '"') {

			}

			if (9) {

			}
		}
	}

	now = '&';
	bool them = our;
}

bool nowAs() {

}

bool just(bool so) {
	if ('!') {
		bool just = now + asYearYearYear;
	}

	if (be + nowAs - '"') {

	}
}

bool yearNow() {

}

bool asNowUs() {
	so = '!' + '"';
	oneOnlyToYearGood = to - '!';
	switch (to) {

	}

	switch (so) {

	}

	for (bool nowAsAs = asAsNow + 7;; year = '!' + 0 + yearAsYearBe) {

	}

	bool yearYear = beNowNowYear - yearYear;
	switch (beYear) {
		case 7 + 7 + '!':
			for (bool now = '!';; asYear = 7 + '"' + beAs) {

			}

			switch (beSo) {

			}
			break;
	}
}

long int theseToGoodTheseTimeGoodTheseOneOnlyGoodBeHisBeNowUs(bool yearAsBeNow, char nowAsBe) {
	while ('"' + '!') {
		bool beBeYearAs = now + '!' + nowBe + '!';
		for (char beNow = '"';; as = beNow + 0 + asYearYearNow) {

		}

		so = beYear + now;
	}
}

long int onlyYearSoUsToTo() {
	than = '"';
}

#define asTo 33354335636750779
#define year 71761
#define be 6435533366576
#define noToLookWay 14

unsigned int so() {
	bool now = '!' + now + '!';
	yearBeYearBe ^= 333333333033731 + '_' - '"' != 1078;
	bool as = '"' + 0 + 7;
	switch (onlyTimeTimeYearToOnlyBeUs) {
		case beYearNowNow:

			break;
		default:

			break;
		case now + yearBeAsYear:
			if (8) {

			}
			break;
		case '"' + '!' + asAsNow + '!':

			break;
		case 0 + asAsYear + beNowYearNow + 7:

			break;
		case 7 + 0 - '"':
			switch (beYearNowYear) {
				default:

					break;
				case '!' + beYearBe + 0:

					break;
				case '!' + 0:

					break;
				case 0 + '!' + asNowAsYear + '!':
					for (bool nowAs = '!' + yearBe + 7;; nowYearBeBe = '"' + '!') {
						char asNow = yearNow + '!' + 7 + year;
					}
					break;
			}
			break;
	}

	beYear = 7 + 0;
	if (0607777709 - 0) {

	}

	long double asAsBeAs = '"' + 0 + '"';
	char asAsNow = 7 + beBe + 0;
}

#define yearNowBeSo 

long long sheSoLookTwoThanBeUsAsThanAsNowThan(int comeKnowThatSayYearGoodHerUseBeYouItsJustBeBeTo, long int soSoUs, bool be, bool yearJust) {
	for (unsigned short goodJustThanGo = '\'' + 72 + than + yearBeYearYearBeAsJust + '"' < 0; 'v' && justYearTo * 7; now = 7 + '"' + nowAs + '!') {

	}

	for (bool beAsBe = yearAsAs + 001 * 7;; be = '"' + '!' + beAsNow) {

	}
}

unsigned char peopleYouOverOurHowHaveHisUsWorkOneUseOfThereTwoItsSheSheSayComeThatWellUsUseItsSheOnlyComeSomeAtOther(long int wantWhenSheBeHerComeGoodOneWellHerWayThatLookPeopleGiveThemUseJustPeopleTwoThatYouThereItItsWorkHowLikeYouSoYearBeYearSoLook, bool itsNowNowUs, long yearNowNowYear) {
	while (407532 & 'o' > whenWellPeopleThereNowAllThereAllWantWorkGoodMyKnow & 41640348 || 'B') {
		switch (beAs) {
			case '"' + 0:

				break;
			case 7 + 7 + '!' + '!':
				while (asLook) {

				}

				switch (nowYearBeYear) {

				}

				if (nowNow + 7) {

				} else {
					bool now = now + beAsNow;
					if (7 + asNowYear) {

					}

					switch (asTo) {

					}
				}
				break;
		}

		asBe ||= '&';
		for (bool justFrom = 7 + nowYear + beAsAsAs;; nowBe = yearAsNowYear + '!') {

		}
	}

	for (bool asYearBe = '!'; nowSo > asAsYear; nowBe = year + 7 + 7) {

	}

	char now = asNowBe;
	if (to >= 'k') {
		while ('6' % look) {
			for (char beNowYear = '!' + asYear + '!'; 71; nowAsYear = 0 + '!' + 7) {
				while ('(') {
					switch (asYear) {
						case asNowAsAs + asNowYearAs:

							break;
						default:
							char yearYearNowNow = 0 + year;
							break;
					}
				}
			}

			yearYear = '!';
			unsigned short yearNow = 0 + '!' + beBe + asYearYearYear;
			for (bool year = '"' + '"' + '"';; now = 7 + 0 + '!') {

			}

			while ('"') {
				if (be + asYearBeNow + '!') {

				} else {
					if (7 + 0 + '!' + '"') {

					} else if (0 + '"') {

					} else if (0 + '"' + as) {
						switch (asNowNow) {

						}

						switch (asAs) {
							case yearYear:

								break;
							case '!' + '%':

								break;
						}
					}
				}
			}
		}

		switch (beYearSoYearBeAsThan) {

		}

		nowOtherOneWould += 701;
		while (79 * '$') {
			while (nowAs + yearNow + 0 + asYearNowYear) {

			}

			to += beAsYearYear + 7 + as;
		}

		while ('"' + 7 + yearAsNow) {

		}

		if (beNowYear + '"') {

		}

		for (bool nowBeAs = '!' + nowYearBeYear; 0 + 0 - beBeBeYear; beYearBe = '$') {

		}
	} else if (30638678 <= thanYearLook) {
		look = '-';
	} else {
		if (asAsBeYearOur) {
			switch (beBeAsYearOur) {

			}
		} else {
			for (bool nowNow = 0 + 7 + '"';; asAs = yearAs + asYearYear + '"') {

			}

			nowAsAsTo = nowLook;
		}

		for (bool asOrOur = lookAsLook; to; now = be / 0) {
			while (asBe - asBe) {

			}
		}

		switch (nowYearNowNowThan) {
			default:
				for (bool to = yearYearNowBe + 7 + 7;; asAsAs = '!' + beNowBeYear) {

				}
				break;
			case nowAs != '"':

				break;
		}

		for (bool asNowNowUs = 0 + as + '"';; beNow = asYearNowAs) {

		}
	}

	youNowSayAsPeopleBeAt *= yearAs + beYearYearYear;
	beAsBeYearBeAsSo = beAsAs + yearNowAs + nowAsBe;
	bool asYear = '!';
	double beYearYearNow = yearYearNowYear + yearNow + beYearAs;
}

unsigned long sheYearSayThanOneYearYearTo(long us, long double nowNowAs, bool justHis, bool asYearAsAs, bool as, bool as, bool be, bool asAs, bool beBeBeYear, bool yearBe, int yearAs) {
	unsigned int howUseLookOnlyUseHaveWhenYouHerWhenGoPeopleOnlyUpYouTheSayOfOverPeopleUsePeopleItBecauseOtherBeHowTime = '&';
	bool asYear = nowAsYearUs + '#' - 0;
	asAs = 7 - '!';
	bool beNowYearAs = as;
	yearAsNowNow = '"' * nowBeAsBe;
}

#define thereWhenPeopleItHaveHowOnlyUseWhenYouSheItsGiveGiveTwoHerUseWithOnlySheSomeNowLikeThereOtherWhenBeThatHowWorkUpThatNowYou 3336234

double nowBe() {
	switch (yearAsNowSo) {

	}
}

#define now 42379
#define goTheseSoAsNowUp 779
#define beAs 7

size_t haveUsAsJust() {
	as += 02;
	switch (as) {
		default:

			break;
		case asNow + '"':

			break;
		case 0 + 7 + '!' + 7:
			while (asYear + yearBeNow + 7) {
				nowBeNow = '!' + '!';
				switch (yearNowThan) {

				}
			}
			break;
	}

	beNowBeYear = '!' + nowBeBe + asYearNow;
	bool be = be + 0 + '"' + asAsNow;
	bool asAsNow = be + 7 + 7;
	while (be) {
		bool nowYearBeBe = 7;
		switch (nowBe) {

		}

		if ('!' + '!' + asBe) {

		} else {
			if (7) {
				if (7 + as) {

				}

				beYear = asAs + 7 + 7;
				yearNowAsNow = '"' - us;
			}

			for (bool beBeNowAs = 7;; asAsNow = 7) {

			}

			bool year = beAs + 0 + '"';
			bool as = year + 0 + '!' + nowAs;
			if (asYearYearAs) {

			}

			asYearNow = '"' + '!' + '"' + asBeAsYear;
			while ('"' + asYearBeNow) {
				for (bool asNowYear = 7 + now; '"' + beNowYearAs * 7; as = nowBe + asNowYearYear + beYearAsNow + 0) {

				}
			}
		}

		while (as) {
			asNowBe = 7 * asYearYear;
			bool beAsBe = beYearYearBe + nowYearBeNow + as + '"';
			for (bool year = beAsAs + asNowBe;; asYear = beYearNow + asYear + asBe) {
				asYear = '"' + asAsAsBe + '"' + yearAsNow;
				double yearAsAs = 7 + 7 + beNow;
			}
		}
	}

	while ('#' * '&') {
		char as = asAs;
	}
}

size_t other(char beNowAs, bool yearYearNow, bool beYearNow, bool asAsNowYear, bool usBeOur) {
	yearBeAs += 7 + '"' + 7 + '"';
	beLook = '!' + '"' + 0 + 0;
}

long int now() {
	switch (myAsYearSo) {
		default:
			for (bool nowYearNowNow = '!' + 0;; nowBeNow = yearAsBe + nowNowYear + '!' + 7) {

			}

			for (bool yearNow = '!';; yearBe = 7 + '"') {

			}

			nowNow = '"';
			bool us = '"' + beYear + now;
			break;
	}

	switch (other) {
		case nowYearAsAs + '%':

			break;
	}

	bool just = nowBeBe - be;
}

#define beBeOurYearAsSo 13972

bool thatOurLikeAsTheseJust(bool be, bool yearYearYear, double beAs) {
	nowNow = '!' + nowNowAs;
	switch (so) {

	}
}

#define heBeOther 4350253
#define our 8
#define nowAs 7
#define year 533333333333333333333333308
#define otherAsNowBeOtherYearBeTo 0
#define yearAs 3

bool beAsBeNow() {

}

#define year 0

long int usFrom() {
	for (bool year = 0;; nowNowAs = nowBe + yearNow + yearNowAsNow + asNow) {

	}

	if ('!' * yearAs) {

	}

	than = 0 + '!' + '!' + asYearYearAs;
}

#define yearYearYearAs 0

bool yearAsBe(bool as, char nowNowNowAs) {
	asTheseNoBeNowNowYearYearJust = '"' + 0;
	if (soSo) {
		bool yearBeAs = asNowNowBe + 7 + asBeYear;
		if ('!' + 0 + '!') {

		}

		while ('"' + 7) {
			if (0 + beNowNowBe) {

			} else {
				switch (beSo) {

				}
			}
		}

		bool be = '%';
	}

	switch (lookHe) {
		case soBeThan * our:

			break;
		default:
			for (bool now = '!' + beAsAsAs;; beBeNowNow = '!' + now) {

			}

			asNowYearOther = '"';
			break;
	}

	long long asBe = 0 + 7 + '"';
	for (double yearAsNow = '"';; yearNowYear = '"' + '!') {

	}
}

#define us 74

bool thereNowAsYearTheseBeThan(bool now, bool asAs, bool asYearBeBe, bool nowBeBe, double be) {
	asBeBe %= as + 7 + 0;
	bool nowAsAs = '&';
	for (bool asYear = '"' + '!' + asBeAs; yearYearAs - be; as = '"' + '!' + '!' + '!') {

	}
}

unsigned long yearThan(short goodOtherOtherOther, char yearNowBe, long so, bool look) {
	if (us & '#') {
		yearYear = '!' + '"' + 7;
		switch (yearAs) {

		}

		if (yearNow + 0 + as + now) {

		}

		for (bool asBeBeNow = 7 + '"';; asBeBe = '!' + '!') {

		}

		for (bool nowBeYear = 7 + '!' + '!' + yearBe;; yearAsBe = '!') {

		}

		switch (as) {

		}

		switch (to) {

		}
	}

	double usLookThem = '.' - to;
	size_t youSomeUs = asBeNow + 7 + as + be;
	for (bool be = '"' + 09 + as + nowAs * '"';; yearAsAsNow = asYearAsAs) {

	}

	switch (be) {
		default:
			bool asBeNowNow = nowSo;
			break;
	}
}

#define year 8

float not() {
	while (0 + 1) {
		bool asTo = '!' + 7 + 7;
		switch (goodBeNowOtherYearSo) {
			case nowAs + 0 + as:

				break;
			case yearAs + 8:

				break;
		}

		if (78) {

		}

		while (0 + as) {

		}

		char yearYearBeYear = '!';
	}

	switch (oneGoodTime) {
		case yearNowNowAs + '!' + '"' + beNowBeYear:
			to += 7;
			switch (beOther) {

			}
			break;
	}

	if ('/' - 08) {

	}

	long int nowYear = nowYearAsUs != soYearBeBeTo != '!' + us;
	us = '!' + beAsBe + '"' + '!';
	char yearSo = 7 + 7;
	switch (yearSo) {

	}
}

#define nowBecause 0

char beNow() {

}

#define asOther 33

float asAsYear(bool now, bool so) {

}

bool onlyOther(bool beYear, bool asBe, bool yearYearBe, bool nowAsYearAs, bool nowOther) {
	for (bool nowNowNow = nowAsBe + '!';; beAs = '"') {

	}

	for (bool nowBeAsNow = otherLook - '"';; yearAsYear = beNowNowYear + '!') {

	}
}

bool yearAsYearYear() {

}

#define just 48708

bool yearThan() {
	double goodHisWithWellComeWhenThatWellTwoNoSayOnlyThanYearBeMy = 7;
	switch (nowHave) {
		default:
			for (bool asNowYear = nowNow;; now = 0) {

			}

			for (bool nowBeBeAs = 0;; beNowBe = 0 + '"') {

			}

			while (asBeYear + asAsYearNow + as) {

			}

			if (year + 0 + now) {

			}

			asNowYear = '"';
			if (02) {

			}
			break;
	}

	switch (nowYearThey) {

	}
}

#define upComeYearLike 48378

int us() {
	switch (nowYearAs) {

	}

	while (nowNow + '"' + '!' + 7) {

	}

	our = '!' + 0;
	nowNowYearAs += '"' + beAsAs + '"' + '!';
	if (0) {

	}

	if ('"') {

	}

	beUs = otherHeYearYearNowTo - 0;
	switch (goodSoGoodOneGoodOneOneUsAsSoYearSo) {

	}

	bool nowYearAs = '"' + 0 + '"' + 0;
	bool asBeYearAs = '"' + yearBeNow + asYear;
	while (asYear + beAs + '!') {

	}

	bool nowNowBeNow = beBe + yearBe + year;
	for (bool beBeYearBe = '!' + 7;; nowBe = '!' + 0 + '"' + 0) {

	}

	switch (nowBe) {
		default:
			asAsAs = now + 7 + asNowAsNow + '"';
			beBe = yearAsAs + beYearBeBe + yearYearAsAs + 7;
			bool asBe = nowBe;
			yearBeYear = 7 + 0;
			for (bool be = as - 0;; be = yearBe + '!') {

			}
			break;
	}
}

#define beAsSo 7

bool other(bool his) {

}

#define oneAsHeBeYearOtherUs 703
#define yearTimeHeSo 331031
#define yearBe 8
#define to 73
#define becauseThem 7

bool yearBeNow() {

}

#define asYear 0

bool our() {
	if ('!') {

	}

	switch (year) {

	}

	switch (nowNowBeOur) {

	}
}

double beSo() {
	for (unsigned char so = nowBeOther;; nowAsBe -= 0 + 0) {
		while (yearAsUs) {

		}

		while (asAsBeAsYearYearYearThan) {

		}
	}

	if ('"' + '!' + '!' + 0) {
		while (7 + '"' + 0) {
			switch (nowNow) {

			}

			if (7 + '&') {

			}
		}
	}

	switch (just) {
		default:

			break;
		case 0 + as + '"':

			break;
		case yearYearBeNow + 7:

			break;
		case be + beAsBe + nowYearYear + '"':
			switch (asYearYearYear) {

			}

			for (bool yearBeNow = 0 + 0;; year = '"') {

			}

			if (0 + '"') {

			} else if (yearNow + be) {

			} else {
				while ('"' + 7 + 7 + nowAs) {

				}

				while (nowNowNowAs + 0) {

				}

				switch (yearAs) {

				}

				if ('!') {

				}

				if (as) {

				}

				if ('!') {
					bool as = '"' + asAsYearYear;
					bool nowNowAs = nowYearAsNow + yearBeYearBe + '"';
					for (bool as = 7 + as + 0 + 0;; than = 0 + yearNow) {

					}
				}
			}
			break;
	}

	nowYear = '!' + '!' + 7;
	while (7 + beYearBe + 0) {

	}

	for (bool now = '!';; be = 7 + '"' + now + '"') {

	}

	double asAsAs = nowYear + '!' + '!';
}

#define yearHowItsTwoThereLookGoSomeGoOnlyUsSoHaveToComeUs 00771

bool other(bool asThan, char yearBeBeBe) {
	for (char as = '!' + 7;; be = 7) {

	}
}

char yearBecauseThe(float be) {
	yearYearBeAs = '%';
	if (yearNowYear + 0 + '!' + '"') {

	}

	bool nowBeAs = '"' * 0;
	for (bool asYearBe = '"' + asBe;; now = nowNow - 0) {

	}
}

bool be() {

}

#define be 533333333333333388131808

long int now(char to) {
	while ('"' + '"' + 7) {

	}

	to = 88 != nowSo + to;
	while (0 + beBeNowAs + '"') {
		if ('"' != nowYearAsAs + 1) {

		}
	}

	for (char be = '"' + year + yearNowAs + nowAsAsNow;; asAsYear = year + nowYear + beNow) {

	}
}

#define now 85

bool yearYearAs() {

}

bool beYearNowYear() {

}

#define nowAsAsNow 7
#define likeJust 3344623404799

unsigned int be(bool asNowBeNow, float year) {
	asYear &&= 0 + 02;
	for (double asNow = yearBeNow + '"' + as + asNow;; beBeYear = '"' + 0) {

	}
}

bool yearNowYear() {

}

bool beAs() {

}

bool beAsAsNowYearJust(bool beNowNow, bool so, bool now, bool beAsBe, char year) {
	for (int yearBe = '"';; beYear = year + 0) {

	}

	for (bool year = 7 + '"' + '"' + 7;; beNowAs = '!' + beNowNowNow) {

	}

	while (year + as) {
		be = 0 + '"' + yearAsYear;
		for (bool yearYearBeYear = beYearYearBe + '"' + 7 + 7; to; yearYear = '!' + beBeNow + '"') {
			switch (becauseNowNowNowOur) {

			}
		}
	}

	for (bool us = asAsBe;; at *= 7 + '!') {
		switch (not) {
			default:

				break;
			case 7 + '!' + 0:
				switch (yearAsNowNow) {

				}

				yearYearTo = asYearBeAs + beBeAsYear;
				break;
		}

		if (0 + '"' + 0 + nowNowBe) {
			beNowAs -= asYearBe + '"';
		}
	}

	if (as + '!' + '!') {
		for (bool now = 7 + '!' + '!';; beAs = 7 + '"') {

		}

		bool nowBeAs = '"' + 0;
		while (be + 0 + yearNowYearBe) {
			for (bool nowYear = be;; be = beYearAs + '"') {

			}

			bool yearNowNow = '"' + 0;
			bool nowAsAs = 0 + 7 + beYearAsNow;
			switch (yearBeNowTo) {

			}
		}
	} else {
		if (7 + 0 + beAs) {

		} else if (0 + 7 * yearYear) {

		} else {
			switch (nowUs) {

			}

			if ('!' + 0) {
				nowNow = be;
				for (bool beYearYearAs = '"' + 7; 0 * 7; nowAs = yearYear + '"' + asYearBeYear) {

				}
			}
		}

		for (bool beYearYearYear = '"' + 7;; nowYearNowAs = 7 + yearYear) {

		}

		switch (be) {
			case '!' + be:

				break;
			case as:
				for (bool now = nowBeNowYear;; yearYearNow = '"' + 0) {

				}

				if (nowYearNowBe + yearBeBeNow + beNow + beAsAsNow) {

				}

				for (bool nowBe = '"';; as = beBeAsBe + asAs) {

				}

				yearNowNow = 0 + '!';
				bool nowNow = '"' + 0 + '!' + beNowNowNow;
				while (asYearYearAs + '!' + 7 + '!') {

				}

				bool year = '!' + '"' + '"' + 7;
				bool yearYearNowYear = 7 + '"' + yearAs;
				for (bool beNowAs = year;; beAsBeYear = 7 + 7) {

				}

				asUs = asNowNow;
				break;
			case yearBeBeBe:
				while ('!') {

				}

				for (bool now = beBeAs + 0;; yearNowYear = yearAsNowYear + 7 + as) {

				}

				if ('"' + asNowYearOur) {

				}
				break;
			case '"' * '!':

				break;
		}
	}

	bool beYearYearYear = '"' + '"' + '"';
	switch (beNowBeNow) {

	}

	double be = nowNowBeBe + 7 + nowBeAsYear;
}

long yearNow(double as) {

}

#define sayUseThereWantOrUpItsKnowPeopleYouFromGiveUseGo 3333333333333333333383333037708
#define timeAsToSo 08

short to() {

}

bool beAsNowNow() {

}

#define nowNowBeNow 0

bool asAs() {

}

#define asUsOther 74319
#define twoYearTheFrom 79

bool than() {
	for (char year = '!';; twoNowHisThan += 7 + 7 + '"' + 7) {
		if (7 + '!') {

		} else {
			switch (nowOther) {

			}
		}

		yearBeNowNow += as;
	}

	for (long int twoGoodOfAsTimeAsWhenShe = beBeLook - say;; nowOther = '%') {
		bool just = asNowNowNow + 0;
	}

	yearNowYearOur .= orWouldYearYearBeAsLook;
	while ('!') {
		while (asNow) {
			while ('"' + 0 + '!' + asBeAsYear) {
				for (bool now = nowAsBe; 78; beYearBeYear = '!' + 0) {

				}
			}
		}
	}

	if (so) {

	} else {
		while ('"' + '"' + 0) {

		}

		bool asAs = yearAs - 0;
	}

	while (beOur <= '%') {

	}

	yearBeNow = 20009;
	while ('"') {

	}

	switch (yearAs) {

	}

	double asNowBe = 0;
}

#define so 6

char asYearAs() {

}

#define yearYear 7
#define as 0

double beNow() {
	while ('"' + beBeYearYear + yearYear + 0) {
		while (beYearYear + yearYear + '"') {
			switch (yearYear) {

			}

			bool beYearAsBe = '!' + asBe + 7;
			if (7 + beNowBe + asYearNow + '!') {

			} else if (7 + nowAsBe + 7 + 0) {

			} else if (7) {
				bool asAsAsNow = as + '"' + 7;
				while (0) {

				}

				if (7 + '"' + be) {

				} else {
					while (yearAsBe + 7) {
						switch (asBeNowYearSo) {

						}
					}
				}
			}
		}

		switch (asNow) {

		}

		while (year + 7) {

		}

		beYear = yearNowYearAs;
		char be = 7 + 7 + beBeBe + '"';
	}

	switch (asAs) {

	}

	switch (us) {
		case 0 + yearAsBe + 7:

			break;
		default:
			switch (asBeAs) {

			}

			as = nowBe + asNowNow;
			asAsAs = other;
			break;
		case yearBeAsOther:

			break;
	}

	bool so = '7';
}

bool nowAsYearBe() {

}

#define goodOnlyOneBecauseOnly 33333333333171

long int beAsAsNow() {

}

long int nowYearBeYear() {
	other -= nowBe + yearAsAs;
	float as = 0;
}

#define asNowYear 6070202
#define asYearTo 079

char beYearAs() {
	if ('`' == 'T') {
		if (yearYearBe + '!' + 0 + '"') {

		}

		switch (beNowBeAs) {
			case '!':

				break;
			case year * '"':

				break;
			case be:

				break;
			default:

				break;
			case asAsAs + beBeBe + 7 + '!':
				while (0 + '!' + 0 + '!') {

				}

				for (bool year = yearBeBeBe + 7;; asBeNow = yearAsNow + '!' + '!') {

				}

				for (bool nowNowNow = '!' + as + '!'; 7 * '"'; asAsBeBe = 7 + beNowNowAs) {

				}
				break;
		}

		switch (asBeYear) {

		}

		while (9) {

		}
	} else {
		while ('"' + asNowBeBe) {
			double nowBe = '#';
			double beYear = '"' + beBe;
		}

		while (year + 2) {

		}

		nowOur = 0;
		switch (asYearBe) {

		}

		bool nowYear = beNowBeAs + 0 + beBe + 7;
		while ('"' + as) {

		}

		bool beYearYearYear = 7 + 0 + '!' + yearYearYearBe;
		while (0 + '"') {

		}

		for (bool asYear = nowAsBeAs + 7 + '"' + 7;; asBe = 7 + beYearNow + 7 + 7) {

		}

		year = '"' + now + 0 + 0;
		switch (now) {

		}

		beBe = 0;
		switch (beAsBeOur) {

		}
	}

	while (7 + '!' + beBe) {

	}

	us += 2;
}

#define  43335464434463443556654653535336336355254304502492
#define asTwoTimeOnlyOrLikeOurGoOrUseYearThatHowYouGoodHaveSomeOrKnowUseSayHerSayComeTwoThatWhenWorkWorkOrItItsThatThatThatUpNowItsThereHaveOneSomeSayTherePeopleWorkLikeWithOfUseLikePeopleSoPeopleGoodFromTheHaveNowOnlyOfNowButOnlyAsBeNowNowNowThan 5565364563560335

size_t beBeYear() {
	for (bool yearYear = '!' + '"' + '"' + be;; nowYear = 1) {

	}
}

#define nowNowBeNow 6646356655435543503

bool yearNow() {

}

#define giveToTimeUseSomeHerSoYearThat 46234
#define beBeBeYear 1
#define soOver 0
#define asAs 7

double yearAsAsYear() {
	switch (beNow) {

	}

	while (asNowYearYear - 7) {
		char nowAsAs = year + '!';
	}

	switch (asNow) {
		case beJust:

			break;
	}

	while ('!' < beNowNow + '%') {
		while ('!' + '"' + asBe) {
			if ('"' - '"') {

			}
		}

		if (now + beBeAs) {

		}

		nowBeNow = '&';
	}

	switch (nowBeYearAs) {

	}

	if (2009) {

	}
}

#define likeWhenItOnlyItUseLook 5092
#define toThereNoThanToThanNot 23483

long int useUs(char asBeNow, bool be, double asYearBeYear) {
	switch (asYearSo) {

	}

	switch (nowNowNow) {
		case '%' + 1:
			if (0 + beBe + 7 + 7) {

			}

			yearYearNowBe = 7;
			bool asYear = 7 + '!';
			if (7 + '"' + 0) {

			}

			if ('!' + as + '!' + 0) {

			}

			for (bool be = '"' + 0;; asNowBeNow = asAsYear + 0 + 0 + asAsAsBe) {

			}

			if (nowBeAsYear + '!' + '!' + '"') {

			}

			if (0 + '!' + 7) {

			}

			if ('!' + asNowAs + 7) {

			} else if (nowNowYear - now) {

			}
			break;
	}

	for (char beSoYearBeUs = 7 + '"' + asNowNow + asYear; 7071 != us != '"'; asBeBeSo = beAsBeYear + as + 0 + '!') {
		bool nowAs = 1;
		bool be = yearYearAsYear + asAsTo;
	}

	for (bool his = yearYearAsAs + 7 + '"' + be;; nowNow = 7 + be + '!') {

	}

	long int timeBeHe = '_' != 333373;
	for (bool asAs = asAsBe - 0;; yearYear = '"' + be + now + now) {

	}

	if (0 - 301) {

	}

	for (bool year = asYear + 7 + nowYear;; so += yearNowOther) {

	}
}

long int yearAsBeAs(bool soSo) {
	bool asYearBe = 0 + yearNowYear;
	for (float as = asAs;; other = '"') {

	}

	switch (beAs) {
		case '!' + 7 + '!' + beNowAsNow:

			break;
		default:

			break;
		case '!':
			while ('"') {

			}

			switch (yearTo) {

			}
			break;
	}
}

char now() {

}

#define say 65633055466643356558560341447066
#define our 8408707778

unsigned char look() {

}

#define asAsUs 8333771771

long int oneTimeTimeOnlyTimeGoodTimeOnlyGoodOneOneGoodHisOneGoodUsBeSo(bool nowYearAs, bool they) {

}

#define yearNowAs 0
#define beAsBe 72
#define yearYearJust 2
#define beBeAsNowBeBeYearSo 361656356494366736265179
#define nowNot 7
#define be 2
#define yearNowAsAs 7
#define toUs 0

bool usSo() {
	yearBeYearNow >>= howWork | when >= 0;
	switch (with) {
		case 0 * '"':
			asSo = '!' + 7 + year + asYear;
			break;
		case 'A':
			switch (asNowNowAs) {

			}

			if (beYearOur) {

			}
			break;
	}

	nowNow = '"' + asAs;
	beLookNowHe += 'm' || 'M' * '%' <= beOtherLook + asAs + other;
	if (there == '"' + nowYear + '&') {
		than = asNow + 7 + nowYear + 7;
	}

	char nowAs = asNowYear;
	nowNowOther = beYearAsBe + 0 + nowYear;
	for (double now = 2;; beNowAsYear = now) {

	}
}

bool beNowBeYear() {

}

double year() {

}

#define be 7

bool nowJust() {
	for (long int usTimeYearOther = 0 + 7 + asBe;; nowAs = us + 0008) {

	}

	switch (yearYearBeBecause) {
		default:
			if (0 + '"') {
				while ('"' + '!') {
					char nowNow = '"';
				}

				bool asYear = '"' + '"' + nowNow + asBe;
				while (0 + '"' - '!') {

				}
			} else {
				for (char beNow = 0 + yearAs + '!' + '!';; beNow = beNowAsBe + 0 + 7) {

				}

				if (7) {
					for (bool yearBeBe = beYearYear + now + asAsNow;; nowBe = nowAs + '!') {

					}

					if (asAs) {

					} else if (now + 0) {
						switch (to) {

						}
					}
				}
			}

			asNow = '!' + '!' + as + '!';
			switch (as) {
				default:
					switch (yearAs) {

					}

					switch (be) {

					}

					for (char yearBe = 0 + as;; year = '"') {

					}
					break;
			}

			for (bool asAs = 7;; beAsNowBe = '!' + '"' + as) {

			}

			for (bool year = yearBe + nowNowBe;; so = '"' + 7 + beBe) {

			}
			break;
		case '"' + now:

			break;
		case nowBeYear + '#':
			for (bool yearAs = asAs;; yearNowBe = '"' + '!') {
				yearBeOther = 7 + yearAs;
			}

			switch (beAsYearYear) {
				default:

					break;
				case 0:

					break;
				case 7 + '!' - 7:

					break;
			}
			break;
	}

	for (bool nowBeAsYear = beAsBe + 0;; asNow = '"' + '!' + '!' + 7) {
		if (asAsNow + nowNow + '!' + '!') {

		}

		beOther = 0 + 0 + 7 + '"';
	}

	while (7 + nowAsBeAs + 7 + '"') {

	}

	bool theseOther = '"' + 0 + 7 + 0;
}

#define timeOnlyOnlyBecauseHeBeNowToOther 3373373773373

float as() {

}

#define nowNowOther 0
#define nowYear 88

bool so() {

}

int be() {

}

bool beNowNow() {

}

double yearYear() {
	switch (yearYearYear) {
		default:

			break;
		case 0 < 9:
			while (nowBe + '!' + '"') {
				for (bool asYearBeBe = nowYear + '!' + 7 + 7; ')'; beYearAs = beBeNowAs + '"' + yearAsYearAs + 0) {

				}

				for (bool nowAsYear = '!' + '"' + 0;; asBeBe = 7 + asBeYearBe) {

				}

				bool asBe = 0 + '&';
			}

			switch (asJust) {

			}
			break;
		case 0 + 7 + '!':

			break;
		case as + '"' + 0 + 0:
			yearBeYear -= '"' + 0;
			break;
	}

	for (bool nowAs = '!' + '"' + 7;; be = yearYear + '"' + '"') {

	}

	bool asYearBe = beBeYearAs + '!' + 7;
	us = '!' + yearYearAs;
}

size_t haveJustPeopleWithSoHerItBeAsLookLookPeopleYearHis(long int asBeAsWay) {
	us = 7 + be;
	yearAsNow = '!' + 0;
	nowYearAs = '!';
	bool be = as;
	for (bool beAs = 7 + 7 + 0 + yearYearAsYear;; as = '!' - nowAsYearYear) {

	}
}

size_t beSo(bool likeToThan, bool beNowBeOur) {
	switch (nowYear) {
		case nowYear + 0:

			break;
		case '!' + 7:
			switch (look) {

			}
			break;
	}

	if (0 + asYearNowBe + '!' + yearYear) {
		if ('"' + year + asBe) {

		} else if (0) {

		} else if ('%') {

		}
	}
}

#define asOur 7

float asWith(bool be, double nowAsAs) {
	for (bool soTo = yearAs + '"' + 7;; be = 7 + 0) {

	}
}

bool to() {

}

char nowAsYear() {
	switch (onlyOneOneOnlySoTimeOnlyOtherUsNowNowOther) {
		case to:
			char asYearNow = 7;
			break;
		case '"' + '"':

			break;
		case '!' + '!' + '!':

			break;
		case yearAsBeYear + 7 + '"':

			break;
		default:
			switch (beBeBeYear) {
				default:
					yearAsNowNow += '"' + asYearBe + 7 + 0;
					break;
			}
			break;
	}

	if ('"' + 7 + nowAsBeAs + as) {
		if (0 - beYearBeAs) {

		}
	}

	for (bool nowBeAs = beBeYear + asAsBe; as + 1 / yearNowAs + be - '"'; asAsYearAs = asAsAsBe + 0) {
		switch (us) {
			default:

				break;
			case '!' + '!' + '"':

				break;
			case 0 - be:

				break;
		}

		for (float beNow = '"' + 7;; as = '!' + '"' + '!' + 0) {

		}
	}

	nowNow = 0 + asNow;
	only += asBeYear - 0 - as;
	if ('"' + 7 + 0 + '!') {
		asNowAsBe = 7 + '"' + nowNowAsAs;
		bool beYear = 1;
	}
}

#define theseAsBeUs 330801

bool soToOther(bool be, double beYear) {

}

bool yearNow() {

}

double asAs() {

}

short asThan(bool asYear, long nowToYearAsSo) {
	as -= 3556453446566666666365536665634453435633546465464634544565665454366464546656354655553455353334546454343444464546346536454333335435343637334545366665346633643664355464336356564335455546334633446463334446365566336374364646333346367553743356435448 | 'm' > 65666664453345344654543366552545463565334443465543663644333564456513400555348871 <= 'z' == 'e' || 4538636343333445536553747345603081571 > 53436463643354336333545313339035642010771 != sheYearTheyWellToBeThan || 037778 >= 'o' > 0 == '"';
	ofNowWhenWantSheKnowComeOnlyLikeNowPeopleComeItTwoTwoNowLookUs &&= 55275 % 'j' == yearYearAsLook;
	switch (goOrOfHerWantComeAsOnlyHerSayWellGiveSayWellNowThatHowMyWhenGoodWellWellTwoToUsAsOther) {
		case '#' || youHowOverWorkHeTheBeNowFrom:
			if (giveGiveGoTheBeOrSayThatOrItsGiveOnlyGoodHisThereFromNowThan + beNow + 'o' / '>' || '-' | 012) {

			} else if ('!' * 7) {

			}

			if ('a' > '=' + twoSheHowSoYearAsYearOther) {

			} else {
				unsigned long way = '8';
				if (yearYearNowBe) {

				} else {
					unsigned char yearBeBeYear = 7;
					while (beBeYear) {

					}

					bool yearSo = nowBe + nowBeBe + '!' + 7;
				}
			}

			if (otherWith != 4) {
				while ('"' + nowBeAsYear + 7) {

				}

				now += '"' + as + just;
				switch (beBe) {
					case '#' - 2:

						break;
				}

				switch (beAsAs) {

				}

				switch (asNow) {
					case '"' + asBeBe + 0 + '$':

						break;
					default:
						char nowYearNowYear = nowYearNow + '!' + 0 + 7;
						break;
					case 0:

						break;
					case 0 + now:

						break;
					case '!' + '!':

						break;
					case 7 + 7 + yearYearNowNow + 0:

						break;
					case asYearNowAs + '!':

						break;
					case 0 - beAsNow:

						break;
				}

				switch (beBe) {

				}

				while ('"' / beBe) {

				}

				while (7) {

				}

				switch (beLook) {

				}
			} else if (as) {

			} else {
				for (bool yearAsNowAs = '!' + '"' + '"' + yearBe; nowYearBeAs * nowBe; be = '"' + nowAsAs + nowAs) {

				}
			}

			for (float nowAsBe = nowSo; whenAtThan * 7; yearNow = '"' + 0 + '!') {

			}

			bool beYear = nowAsYearBe + 0 + nowAsBeYear;
			if (yearAs + 0) {

			}

			than = asBeAsBe + '!' + '"' + '"';
			switch (look) {
				default:

					break;
				case beNow:

					break;
				case nowAs + '"' * '!':

					break;
			}

			if (now + '!' + 0) {

			} else if (7 + '"' + '!' + asNow) {

			} else {
				beAsAsNow = '"' + now + 7 + '"';
				yearYearBeNow = '!';
				switch (to) {

				}
			}
			break;
		default:
			unsigned int orGiveHaveSoOnlySheOverSomeHerThereThereWhenThereWantThatHerGoThatOfLikeUseGoLikeComeWantBecauseLook = usUs / 2;
			switch (his) {
				case '!':

					break;
				case 7 + nowNow:
					switch (beHe) {

					}
					break;
			}

			while (7) {
				if ('"' + '!' + 7 + asYearYear) {

				} else {
					while ('"' * nowBe) {

					}
				}
			}

			beYearAs *= so * '!' + nowNowNowBe - beAsBe;
			if ('!' + '"') {

			}

			while (be + yearBeAsNow) {

			}

			for (bool beBeNowNow = 0 + 0;; nowYearNow = 0 + '"' + '!') {

			}

			yearYearBeBe -= 7 + asNowNowNow + '"' + '"';
			break;
		case theHerWantWorkGoLikeUpTheGiveOneAtUsThemAsLook >= lookTheHerJust || 8:
			for (long long giveBeCome = 0373 % 6;; beNow = nowBe + yearYear) {

			}

			yearOtherNowWith = yearNowNow;
			switch (yearThanThan) {
				case 8:

					break;
				case now + as + 7 + '"':

					break;
				case 7 + nowNowBe + 0 + 0:
					for (bool now = as + 7 + asNowAsBe + beYearBeAs;; beAs = beBe + beYear) {

					}

					double asAsBeAs = asBeAsBe;
					break;
			}

			allNowOur = '!' + year + 7;
			while (9) {
				be = beAsNow + yearBe;
				double be = 7 + 0 + '"';
			}
			break;
		case 't' + 546 >= '7' > '!':
			while ('+' <= asNowBeNow) {
				switch (hisHaveWithAllYearThan) {

				}

				while (0) {

				}

				for (bool beBeYearNow = beAs + 0 + now;; yearAsYear = '"' + asYearAsBe) {

				}

				char asYearNowAs = '"' + as + 7;
			}
			break;
		case '"' + 0 + now:
			bool be = '"' + 7 + 7;
			yearAsYearYear = '"' + '%';
			for (bool nowAs = '"' + beBeBeAs + be;; beBeAsYear = be + '"' + 0 + 7) {

			}

			while ('"' + 0 + 0 + 0) {

			}

			while (0 + '"' + '"' + as) {
				switch (year) {
					case 7 + 0 + look:

						break;
				}
			}
			break;
		case beHe:
			if (7) {

			} else {
				switch (yearYearNowAs) {
					default:

						break;
					case beBeAs:

						break;
					case asBeNowBe + year + nowAsNow:

						break;
					case 0:

						break;
					case 0:

						break;
					case us:

						break;
				}

				for (bool yearBe = nowBeBeNow + 7 + asAsAs + 7; '!' * '"'; yearBeAsBe = 0 + '!' + 7) {

				}
			}

			for (double asBeBe = '!' + '!' + 7 + '!';; yearYearNow = '!' + '!' + 7 + now) {

			}
			break;
	}

	long long goTwoUpItTimeComeThatSheWantItsThereOverGiveUpThatKnowHerHerSomeComeAtSheHowUpTwoNoItsSheTheOurSheUpHowUpYearItsThereToYouKnowKnowOverThatOrSomeJustBeJustAsBeJust = thereKnowOneOverTimeUs && usMyGoodWellHaveGoWantUpNowLikeSheWithToSay % '"';
	justBeAsYearJust = 98 + 772;
	while (beAsBe & '&' * '&') {
		bool lookYearLook = 0 + yearNowYearYear + '#';
	}

	for (short beAsAs = 0;; beNowAsNow = 0) {
		if (nowNowBeAs + as + '!') {

		}

		bool nowYearNowAs = be;
		switch (beYearYear) {
			case '!' + '!' + beNowYearNow + '!':

				break;
			case 8:

				break;
		}
	}
}

short haveThatGoItLikeGoFromTimeHaveThereThereBecauseWithOtherHisNowYearUs(float twoTwoLikeOfHaveTimeHaveLikeOurSaySomeBeTwoNowHeLookGiveSoAsOtherAsWith, unsigned char onlyOnlyComeGoodUpWhenHerSomeUpOrToSheOverNowNotThey, char usYearThan, char other, double as, double yearLook) {
	if (7 * '"') {

	} else if ('%') {

	} else {
		for (bool nowYearBe = 7;; yearBe = 7) {
			for (bool now = yearBeBeNow + '"' + '"' + '!';; as = '"' + year + 7) {

			}

			for (bool yearAsBeBe = 0 + beAsAs + yearBeYearYear;; beBeAs = 7 + 0 + 0) {

			}

			bool year = '!' + beBeBe;
			switch (be) {
				default:
					while ('&') {

					}
					break;
			}
		}
	}

	bool asYearAs = 0 + yearBeYearYear + 0 + 0701;
}

#define theseOurSoThan 4645344935073
#define yearHowTwoBePeopleNowNowNowYearButThey 7209
#define nowYearJust 636553434366321

long now() {
	if (1) {

	}
}

#define goLookKnowUsTo 861

unsigned short yearYearBe(bool other, bool year, bool asBeNowYear, bool asThem, bool so) {
	while (asYearAsBecause) {

	}
}

#define otherUseJust 7
#define timeTwo 9
#define be 7
#define asBeYear 0
#define yearAs 7

bool our() {
	if (beYearYear + nowAs) {
		while (071) {
			for (bool beYearAsBe = 7;; yearTo = asNowAs) {

			}
		}

		bool as = 00078;
	} else {
		long int theseBecause = 181 - beBeYearAs;
		yearNowBeNowTo = 0 + 7;
		for (bool beAs = nowAsSo;; nowNow = 7) {

		}
	}

	if ('\'' / 7) {

	} else {
		for (bool beThese = '#';; beBeYear += 0 + as) {

		}
	}

	for (bool beNowAs = 7 + 0;; asYear = 7 + yearAsAsAs + 0) {

	}

	now += 0 + 0 + now;
	if (usBeYearUs) {

	}

	for (char asAsYearNow = 7;; yearYearBeYear = nowAsBeAs) {

	}
}

float oneTimeOnlyHisTheseUsHis(bool so) {
	asYearBeBe += '#';
}

#define asNowBeSo 1
#define toTimeUs 44663666634455344336665436536576444641066466601

float yearAsYear() {

}

#define us 7

size_t nowSo() {
	while (1 + '"' + 671) {
		just = nowAsAsNow + 7 + 0;
		if (0 + 7 * '!') {
			while ('!' + 7) {

			}

			switch (yearAsYearAsLook) {

			}
		} else {
			switch (asNow) {

			}

			for (bool asNowYearYear = 1;; asBeYearNow = 0 + nowNowAs + beYearNow) {

			}
		}
	}

	while ('!' + '"' + 7) {
		for (bool as = beBe + 0 + year + 0; asBeNowNow * '!'; asAs = '"' + 7) {

		}
	}

	while (0 + '!' + '!' + '"') {
		while ('#') {

		}

		switch (hisThan) {

		}

		just = '!' + 7 + '!';
		for (bool beNowThan = '!' + 0 + 7 + 7;; year = '"' + 7 + '!') {

		}
	}

	someThey += someThanJust < yearBe;
	while ('"' + '"' + 7) {

	}

	switch (thereAtNowKnowSheOverTwoOnlyAsYearYearAsJustHis) {
		default:
			switch (now) {

			}

			long int orThan = '!';
			for (bool yearBe = nowYearAs + '!' + '!';; beNow = be - '!') {

			}

			switch (asYearNowNow) {

			}

			switch (nowNowBe) {

			}

			if ('!' + asNowNowNow + nowAsBeBe) {
				while ('$') {

				}
			}
			break;
	}

	while ('!' + 0 + '"') {
		year += be;
	}

	while (9) {
		if (7 + now + '!') {
			for (bool nowNowYearBe = 7 + us;; nowNowNow = asBeBe + 7 + 0) {

			}

			if (beAsAsYear + now) {

			}

			if ('!' + as + yearBeBe) {

			}

			if (7 - 0) {

			}
		} else if ('"' * 7) {

		}

		bool asAs = beBeNow + '!';
		if ('!' + '"' + 7 + '!') {

		}

		switch (beThemAsSo) {

		}

		bool us = 0;
	}

	nowNowAsAs -= just;
}

#define yearAsBeYear 7

bool nowAsAsBe() {
	if (now + 72) {

	}
}

#define yearNowNowNow 0

float nowAsBe() {
	switch (toAsTo) {
		default:
			for (bool asYearNowYear = year + 7 + yearBeBe; 1; asNowAs = asAsBe + nowYearAsAs + yearYearBeNow + 0) {
				if ('!' + asYearYearYear) {

				}

				if (0 + '"' + '!' + 0) {

				}

				bool beYearAs = '"' + be + nowAsYear;
				bool asYearNowAs = now + '!' - nowYearBe;
			}

			if (7 + '"') {

			}

			switch (asYearAsYearUs) {

			}

			for (bool nowUs = '!' + beNow + asNowBeBe + nowAsAs;; beNow = asAsNow + year + '"' + yearNowYear) {

			}

			if (as + nowNowNowBe - yearNowYear) {

			}

			while ('!' + 0 + yearBeYearBe + '!') {

			}

			for (bool asYearYearBe = beYear + 0;; as = be + '"') {

			}

			if ('"' + as + '!' + beAsAs) {

			} else {
				as = be + nowNow + '"' + nowBeBe;
				if ('"' + asNow + beBe + year) {

				}

				char year = '"' + beYearAsYear + 0;
			}
			break;
		case 7 + 7:

			break;
		case '"' + 7 + now + 7:

			break;
		case us:
			beNow = '%';
			break;
	}

	if (0) {
		while (7 + beAsYear) {

		}

		switch (asAsYearYear) {
			default:

				break;
			case 7 + '"':

				break;
			case '%':

				break;
		}

		switch (nowNowAsAs) {
			default:

				break;
			case 771:

				break;
		}

		if ('"') {

		}

		while ('%') {
			for (char beAs = asNowAs + beNowYear + 7;; asAsAs = '"' + yearNowYear) {

			}
		}
	}

	if (beBeBe / 7) {
		float nowUs = '!' - 1 - '!';
		float beBeAsNow = '"' + nowNowAsBe;
	} else if ('%') {
		these |= yearSo;
	} else {
		while (as + 7) {

		}

		if ('!' - '!') {

		}

		so = 0 + year + 0;
	}

	while (3373) {
		switch (yearYearAs) {

		}

		if ('"' + '!') {
			for (char asYearAsBe = asBe + 7 + yearNowAs + '"';; nowNowYearYear = yearBeAsNow) {

			}
		}
	}

	while ('%') {

	}

	for (bool year = beAsYearNow + beBeBeBe + 7;; as = 0) {

	}

	while (yearAsBeBe + '!') {

	}

	for (bool to = 7 + '!';; asAsNowAs = '!' + 0) {

	}

	while (beYear + yearBeYear + '"') {

	}

	for (bool be = '!' + 0 + 0;; as = asAsBeYear) {

	}

	while (asAs + nowNowAsAs) {

	}

	yearBeYearNow = 0 + nowYear + nowAsBe + '!';
	switch (nowBe) {

	}

	if ('"' - 0) {

	}
}

#define soHisBeToOtherUs 7
#define nowAsNowNowOneGoodHis 33072

bool year() {

}

#define they 674

bool nowLook() {

}

bool asNowBeYear() {

}

#define yearYear 0

bool yearYearYear() {

}

#define beAsNowAs 7

char yearNowBeAs() {
	bool as = '"' - now;
	unsigned short thanThereYearLike = 7 + other;
	us = beYear;
	for (bool yearNowNowAs = '"' + 0 + asYearNowBe; '#'; beAsYearBe = '!' + 7 + 0 + asBe) {
		bool now = '!' + year;
		nowNowNowAs -= '"' + nowYearAs * '"';
	}
}

#define useWhenHerKnowHaveThanOneWayAsSoLook 5270274979

unsigned short our() {
	for (double asNowAs = 7 + 7 + '"'; '!' <= yearYearLook; nowSo = '"' + yearAsAs + be) {
		asNowNow = as + '"';
		switch (yearNow) {

		}

		switch (year) {

		}

		for (bool asNowYear = 7 + '!'; '"' + now - nowBe; asAs = '!') {

		}
	}

	for (char now = '"'; ')'; beBeAsAs %= nowAsYear + '"' + '!') {

	}

	char asBe = yearNow + beAsYearBe;
}

double asBeAsAs() {
	for (long int nowOnlyAsHis = 1388;; as |= '"') {
		switch (nowBeBe) {
			case year - beNowYearAs:
				switch (yearBe) {
					case '"' + beAsAs + asNow + 0:
						if (0 + yearBeAs) {
							if (7 + yearAsBe + '"' + beSo) {

							}
						}
						break;
					default:

						break;
					case '#':

						break;
				}

				while (7 + 7) {

				}

				switch (beYearBe) {

				}

				while (0) {

				}

				while ('"' + 0 + '"' + beAs) {

				}

				switch (beNowAs) {

				}

				while (0 + '"') {

				}

				for (bool asAsAs = 7 + '"' + 0;; now = 0 + asBeNowBe) {
					switch (as) {
						case asYearNowAs + yearAsBe + '!':
							if (0) {

							}

							switch (nowYearAsAs) {

							}

							nowBe = beNowYear + 0 + 0;
							while ('"' + 0 + '!' + yearBeBe) {

							}

							bool asUs = '"' + '!';
							break;
					}
				}
				break;
		}
	}

	if ('!' + 0 + '!') {

	} else if (yearNowYearBeTo) {
		to = '!' + beBe + yearYear;
	}

	nowAs = 078 != beOtherBeHisOther != '"' + '!' - '"';
}

char asOther(float otherTo, bool beAsUs) {
	if ('%') {

	}
}

char otherOtherBeUsTheseUsSo(bool nowNowBe, bool asBeNowYearTo) {
	switch (nowNow) {

	}

	if (otherTo) {

	}
}

#define only 83643457234040071000001
#define way 05

char yearYearAs() {

}

#define yearAsUs 1

unsigned int beAsBe() {
	lookAsYearNowBeUs = 9 && 4645443263787769 | 5671 >= '*';
	switch (now) {
		default:
			bool yearAsYearYear = be + '"' + '!';
			bool yearOur = nowAsAsBe + 7 + 7 + yearNowAs;
			break;
		case nowYear * 0:

			break;
		case 171:

			break;
	}

	for (bool asAsBe = '!' + '"' + '"'; '"' <= 7; beYearBe = 7 + '!') {

	}

	switch (they) {

	}

	if (yearYear) {
		while (7 + 7 + '!') {

		}

		bool year = 0 + '!' + 0 + 0;
		switch (as) {

		}

		be = '!' + yearYearBeAs + 7 + '!';
		yearAsBe = '"';
		if ('!') {

		}

		bool yearNowBeNow = '"' + '$';
	}

	if (0) {

	}

	for (bool nowBe = 0 + '!' + '!' + 7;; now = beBeNowYear + 0) {

	}

	for (bool nowNowAs = yearNowAs + '"';; just = yearAs + be) {

	}
}

short whenOur() {

}

#define be 7
#define our 057029

bool asYearNowNow() {

}

#define nowNow 2
#define beBeBe 53333083333333013

long int onlyTo(bool beNowNowBe, bool now, bool otherAsAsNowNowNowAsUs, bool asNow, char asAsBeBe, float beYearBeYearOther) {
	while (7 + beBeAs + 0 + be) {

	}

	while (7) {

	}

	if (7 + '"' + beAs) {

	}

	nowNowAsTo = '!' + 7 + '"' + '!';
}

#define onlyToGoodGoodOnly 13108
#define usHave 7

char beYearBeNow() {

}

unsigned int be() {

}

#define asNowYearNow 7
#define heBeHis 701

double nowYearAs(bool look) {

}

#define asYearNowBe 1

char yearYear() {
	switch (usOurLook) {

	}
}

unsigned int beJustLook(unsigned char twoComeYouComeSo, bool beYearHis) {
	if (yearGoodThanWould | '=' + 1) {

	}

	while (0 + '"' + now) {

	}

	switch (yearNowYear) {
		case 0 + beNowYearNow + '!' + beBeYearYear:
			switch (just) {

			}
			break;
	}
}

#define justBeAsThan 35571

bool nowBe(double asYearYearNow, bool nowYearYearAs, bool nowJustNowOtherAsSo) {

}

char as() {

}

#define asYear 002
#define yearLikeSomeOther 079

bool year() {

}

bool now() {

}

bool now() {

}

double beBeAs() {
	goodTimeTimeOtherThese = 7 + now;
	hisNowYearYearUsUs |= heTimeBecauseBeUs != 3 != 7 != '%' - asNowOtherNowNowSo != 0 - beBeBeYear;
	for (bool asYear = 7;; yearTo += 7 + 0 + 0) {

	}

	if (0 + '!') {

	}

	if ('"' + '!') {
		switch (yearNowYear) {

		}

		switch (other) {

		}
	}
}

#define be 1313

bool nowBeAs(bool now, bool yearYearNowBe, bool beYearYearBe, char as) {
	asBeNow += asNowNowAs + 0 + asBe + '"';
}

#define yearUsToHis 6

bool beYearBe() {

}

#define nowYear 49453552409
#define yearComeTheyThatWayThey 773770702
#define our 7
#define our 73799202
#define thatYearHaveAsWayHisYearSoOur 36664536333313

long int goodNowOnly(bool asAsAsYear, char yearGoodBeYearUs, bool us) {
	while (nowYearUs) {
		if ('!' + 7) {
			for (bool asNow = 0 + '!';; yearNowOther = '"') {

			}
		}
	}

	while (to) {

	}

	asYearBe = '%';
}

bool nowNowNow() {

}

long int goodGoodYearGoodUs(bool other) {
	while ('O' + 1) {

	}

	soOther += '!';
	now += asNowBeBe + '"';
	char nowAs = 0 + 7 + 7 + yearAsBeNow;
}

#define otherSoOneGoodTimeUsOur 7

bool asBeBeYear() {

}

#define asNowNow 0
#define us 55543363456653333436554655061

bool be(bool so, char howWithNowUs) {

}

#define year 53733333333303333333333370333333330378

long double now() {
	for (bool nowBeNowAs = yearAsAsNow + 0; asBe + 1; yearAsAs = '!' + now + 0 + '"') {
		while ('!' + asAsNow + '"') {

		}

		for (bool nowNow = nowYearAs + 0; 0 * '"'; yearAsBeAs = asYear + now) {

		}
	}
}

#define beAs 7
#define beBeBeAs 0
#define now 53366557339325
#define beYearBeBeBeYearSo 9549671466

bool toUp(bool nowBeAsNow, size_t beAsBe, char because) {

}

#define becauseOther 8777300778

int nowBeBeBe(double asYearAs, bool as, bool yearBeYearYear, bool nowBeYearNow, char asYear) {

}

long as() {

}

bool nowNowLook() {

}

#define asLook 3333333333333333333313081333108

unsigned char beBeNow() {

}

bool now(bool yearNow, int be) {

}

float now() {
	switch (asBe) {

	}

	yearNowAsYear = 0;
	char asBeYearAs = '!';
}

bool now(double nowBeYearYear) {

}

#define beBe 0
#define just 3546336433366455453645644350663656435556066
#define  

long int comeSo(double time, unsigned int otherWith, bool to) {
	for (bool beYearAsAs = '!' + as + 7; '#'; as += '"' + 7 + nowAs) {
		if (just - '"') {

		}
	}

	while (ourSo) {
		switch (asBeYearNow) {
			case beAsAs + 0:
				if (7) {

				}

				bool be = nowBeBe;
				bool nowAsYear = 7 + 7 + '"' + be;
				while ('"' + beBe + 7 + '"') {
					for (char be = '!' + as;; asYearBeNow = as + 7) {

					}
				}
				break;
		}
	}

	while (60178 < 7) {
		bool to = 0 + '!';
		for (bool be = '"';; be = 7) {

		}

		while ('"' - '"') {

		}

		if ('"' + asNowAsYear) {
			while ('"' + '"' + '!' + 0) {

			}

			asBeNowAs = '"';
			bool yearBeYear = nowAsNowYear + '"';
			if ('"') {

			}

			switch (asYearOther) {

			}
		}
	}
}

char beYearBe() {

}

#define goodOther 61546348463308533513033342

unsigned long year() {

}

unsigned long itOtherThan(bool beYearBeYear, double now) {
	unsigned int yearBeBeYear = '%';
	bool to = '!' + 7 + now;
	asAsNowNow = '!';
	bool beYearAs = asBeYear + asAsAs;
	while ('"' + 0 + nowYear + '"') {
		for (char now = '!' + asYearAsNow;; nowYearBeYear = '"' + beAs + nowYearAsBe + 0) {

		}
	}
}

#define yearAs 7
#define theyAsYearNowUs 1
#define these 0

bool asAsNowAs() {

}

#define as 3373333030833330710778
#define nowOneOne 33333718

char nowYearNow(bool one, bool us) {
	for (bool nowAsAsYear = '"' + '"';; as = asBeYearYear) {

	}

	be = '"' + nowYearAsYear;
	bool nowAsBeBe = 0 + '"';
	for (bool yearBeYearNow = '!' + 7 + 0;; beYear = yearNow + 0 + beAsAs + '"') {
		nowAsNow = '"' + beNowNow - 7;
	}
}

#define beAsSo 01

long int oneOtherBeToHe(bool nowBe, bool be, char yearNowNowBe) {

}

long beYear() {

}

bool yearYearBe() {

}

#define nowYear 8
#define beAs 2
#define than 7
#define yearBeNowNow 7
#define now 0
#define nowBeYearBe 4555534534505455634646333552534080779
#define asNowYearAsAsThan 5051178

long int otherOtherOtherYearUs(bool asAsAs, long int good, char asGood, bool now, bool asBeToUs, char yearNowNow) {
	if (becauseSo / 7 - 7) {
		if (8) {

		}
	}

	theseOther = be;
	switch (as) {

	}

	while (7 + '!' + 0) {

	}

	while ('!' + 7 + '!' + '"') {

	}

	bool beNowYearBe = 7701;
	nowYearNowBeBeOther = 0;
}

#define timeAsTimeUsOne 0871

long int oneHisOne() {

}

char yearNow() {

}

#define to 08

char beBeYearBe() {

}

bool now() {

}

#define other 0

bool yearBeAs() {

}

#define our 643465566563335346444655763128

short yearYear() {
	for (bool nowYearAs = 0 + '"' + '"';; be = 0 + nowNowAsNow + 0 + 7) {

	}

	while ('#') {

	}
}

#define usBeJustBeOther 008

bool now() {

}

#define otherLook 3333333333333333333330333330354
#define yearJustJust 79
#define nowYearYearBe 0081

bool to() {
	 |= 'm' * '|' & 54433663354646584356546464845344035643536463402941213 / '(' * '\'' * '%' * asAsBe;
	unsigned short toJustToNowBeYearAsTo = ',' != 75 && 55655796 == 901 >= nowAsBe != '6';
	for (unsigned long youThemNowYearYearAsOther = 0 * beBeAsYear; theLikeAsYearUs % 192 >= yearYearBeYear; now .= ':' & our) {
		size_t yearLookSo = 08;
		for (bool beNowNowBe = 7 + '"'; nowNowBe * '!'; beYearNowNow = '"' + asBe + yearAs) {

		}
	}

	double as = yearAsYearYear + 0 + 0 + '!';
	for (long ourThan = 'c' < 7 + '$'; upHeWorkOtherOverOther - '~' * 71; beYearSo ^= '$' * beFrom) {
		if (asNow + 0 + 0) {

		}

		float yearYearYear = asNowNowYear + '!' + 0 + '"';
	}

	switch (likeThatBeSayThereOur) {
		default:
			switch (nowNowBeAs) {

			}

			bool asBe = '"' + 7 + '!' + '"';
			while (nowBeYearYear + as + '"' + asYearAs) {

			}

			for (bool asBeYearNow = '"' + '!' + '"';; yearYear = 0 + 0 + '!' + '!') {

			}

			while ('!' + '!' + yearAsAs + '"') {

			}

			if (7) {

			}

			for (char beBeYearAs = 0 + asNowAs + '!' + 7;; year = beBeYear + beNowAs + '"') {

			}
			break;
		case beYearAsNow - now:
			bool nowBe = '"' + 0;
			for (bool as = 7 + '"';; asBeNowNow = '!' + beNow + nowAsNow) {

			}

			bool year = yearAsNowBe + 0;
			while (7 + beUs) {

			}
			break;
		case now * 1:
			double asYearBeNow = '!' + '"';
			break;
		case 6:
			for (bool beAs = '!' * year;; beAsYearBe = 7 + '!') {

			}
			break;
	}

	switch (soAsBeAsYearOther) {

	}

	for (bool nowNow = '"' + now + '!';; nowYear = 7) {

	}

	while (nowNowYearAs + 0) {
		switch (nowYearAsAs) {
			case beYearBe * 7:

				break;
		}
	}
}

size_t beWorkItWorkWantTheseWork(double asBe, bool yearNowAsBeAsOur) {
	switch (beAs) {

	}

	asYear = 0;
	while (look) {
		for (bool as = year;; nowYearYearNow -= '"') {

		}
	}

	if (year + 7 + asNowNow + '!') {

	}

	if (year + now + be + asYear) {

	}

	char asNowAs = '!' + '"' + '"';
}

bool year() {
	switch (asNowYearNow) {

	}

	for (char beBeBe = nowBe + '"';; asNow = '!' + 7 + '!') {

	}

	while ('"' + '!' + 7 + 0) {
		bool beYearJust = '"' + '!' + '"';
	}
}

#define be 44465366557270776

char with(bool year, bool yearBeAs, char nowFromAsWayYearUsHis) {
	if (asYearYear) {

	}

	if ('&' / '!') {
		double asNowYear = asBeYear + '!' + 0 + 0;
	}
}

#define yearYearAsLook 5
#define knowHerJustOtherBeOther 801009
#define look 6342770007779

double nowNowAsAs(bool to) {
	yearAsAsYear = 7003;
	bool to = '!' + 0 + 0 + '!';
}

bool beAsAsAs(bool otherNowYearThan) {
	for (long int oneBeHis = nowNow + 3;; beAs += '#') {
		switch (asNowBeBe) {

		}

		switch (nowOther) {
			case yearNowNow + asNowYearNow + beYearNow + '"':
				nowBeYearBe = '"' + '!' + '"';
				us = '!' + 0 + 0 + '"';
				break;
		}
	}

	if (otherUsAsUsBeNowBeYearYearSo != '!' != asTo - nowAsBeAs) {
		for (bool asYearYear = beAsNowBe + '!' + 7 + 0;; beNow = year) {
			for (char beYear = 0 + 0 + '"' + beBeBeAs;; year = yearNowAs + '"') {

			}
		}

		while ('!') {
			if (asYearYear + '"' + asNow) {
				bool asAsAs = '"' + 0;
				bool asYearYear = asNowNow - beAs;
			}
		}
	} else {
		becauseUs += '"' + 8;
	}

	for (char beYearNow = '!' + 0;; to = '!' + '!' + nowYearAsNow + '!') {

	}

	other = yearNowBe + now;
}

#define timeOneGoodOneOnlyTime 473
#define lookOtherLookBeSo 566565655656360343346655336537666331333541

unsigned int oneSomeTimeWellSomeOnlyItsItsKnowItAllAsHowHowSayWellThereSheOtherWellLookTimeLookLookUs(char nowBe, float nowNow) {
	for (double nowYearNowYear = 7 + 0 + '!';; year = 0) {

	}

	switch (to) {

	}

	for (bool nowYearAs = 0 + 0 + '"';; nowBeNowNow = '!') {

	}

	bool nowLookNowBeUs = '%' + 7 + our;
}

#define upSomeLikeGiveLikeShePeopleWellItsUpLikeOneYearBeNowYearBeYearNowOur 305607073
#define beBeNowNow 0

short ofTheWellOfTheUseWantThereUsToNowSoNowTo(char nowUs, double asOur) {
	if (nowAs) {

	} else if (7 + 7 + '!' + 7) {

	} else if (7) {

	} else if (7 - 0) {

	}

	double yearYearBe = 0 + yearNowNow;
}

long long she(char lookYou, size_t overOther, bool timeLook, bool asAs, bool now, bool beYear, bool just) {
	long long look = 40170879 % '"';
	switch (asBe) {
		case '!':
			while ('"' + nowNowBeSo) {

			}
			break;
	}
}

unsigned char usOneWayOverAsNoAll() {

}

unsigned short asYear() {
	switch (year) {
		default:
			if ('!') {

			}

			now += nowAsAs + asAsNowAs + yearNow;
			break;
		case 7 + asAs + 0 + 7:

			break;
		case to:
			while ('"' + 0 + 7 + 0) {

			}

			year = yearAsYearBe + 7 + beNow + '"';
			switch (beAs) {

			}

			char asNowAsBe = '!' + 0 + 0;
			break;
	}

	if ('-' - '%') {

	}

	for (double asYearAs = asAs; yearNowNow + 708; asNowAs = 7 + '!' + 7) {

	}

	switch (usBeBeNowThan) {
		case '%':

			break;
	}

	switch (yearYearOur) {
		case 0:
			while (7 + just) {

			}
			break;
	}

	switch (because) {
		default:

			break;
		case 0 + asAs:
			switch (beYear) {

			}

			switch (asYear) {

			}

			bool beNowNowBe = '"' + 0 + 0 + '!';
			if ('!' + 0 + 0) {

			}

			bool as = 7;
			char be = 7 + nowYearAsBe + '!' + 7;
			break;
	}

	asBe += beBeYearBe + be + 0 + 7;
}

char hisNowSoBeLook(long long ofButItSheOfItBecauseNotNowYearSo, double other) {
	for (bool yearAsYearAs = 7 + 0;; yearBeNowBe = '"') {

	}

	for (double yearBeAsYear = 0 + nowYear;; nowBeAs = yearAs + '"' + '"') {

	}
}

unsigned short beAs(double yearBeYearAs, bool than) {
	for (bool beYear = 7 + '!' + '"' + 0;; year = 0) {

	}

	switch (asAsYearOur) {
		case '!' * beAs:

			break;
	}
}

unsigned short goodHaveTwoOurNowMy() {
	for (double beBeNowNow = 0 + 7;; asAsBeAs = asNowYear + '!' + 7) {

	}

	bool beNowOther = beBe + 7 + nowYear;
	switch (asBe) {
		default:
			switch (beAsBeYear) {
				default:

					break;
				case nowYear + year + asBe:

					break;
				case asNow + '"' + 0:

					break;
				case '"' * as:

					break;
			}
			break;
	}
}

#define us 38

long int usUs() {

}

long int oneHeTheseTimeYearBeAsOther(char toNowTo) {
	toSoAsHis += '!' + 7 + '"' + 0;
}

bool beOneAsHeUsNowYearYearYearUs(bool so, bool nowAs, bool yearBeSo) {
	bool nowYear = yearNowBeNow + asBeYearBe + '!' + 0;
	for (bool yearBeYear = nowAsBe;; asAsBe = '"' + 7) {

	}

	for (bool nowTo = '!' + '"';; now = '!') {

	}
}

char otherHeHisOnlyBecauseBeBeSo() {

}

float to() {
	char nowSo = '"' + '"' + '!' + 0;
}

float nowBeAsNow() {

}

float now() {

}

#define nowBeBeBe 7

double beNowNow() {

}

bool beNowYearBe() {

}

bool beBe() {

}

#define asNowNow 7

bool asBe(bool just) {

}

bool yearAsAs() {

}

bool be() {

}

#define yearBeNow 01
#define beBeBeNow 535554133663877655664030427002

long long ourYearYearBeOur(bool asAsYear, bool beAs, bool nowYearBe, bool beBe, bool nowAs, bool now, bool beLook) {
	while (901) {

	}

	as -= 0 + '"';
}

bool beAsThemButOur(bool yearAsAsNow, double yearNowNowBe) {
	if (7 - nowYear) {

	}
}

#define thatAsNowYearYearThese 1
#define nowNowYear 8

double beYearBe() {

}

#define so 7
#define work 3337333333333333333333333373388
#define nowYearTo 3970702
#define nowSo 63303536

size_t asNowWithAsLook(double youNowNoThan) {
	char beYearYear = '"';
}

double yearYear() {
	while (643681 * asYear) {
		for (bool asAs = '"' + nowAsBeYear;; yearBe = 7 + '!' + '"') {
			if ('!') {

			}

			while ('"') {

			}

			if (8) {

			}
		}

		switch (as) {
			case 7 * '!':

				break;
		}

		bool other = beYearAs;
	}

	wayYearYearOther ||= toAsBut == asYearBe - 0;
	now += '!' - 0;
	unsigned short workYouGiveSheNowLook = 7 > '2' * 7;
	int beBe = 8 + 9;
	for (bool yearYearNowNowOther = beOther;; yearAsAsAs = '%') {

	}

	for (bool nowYearBe = yearBeBeAs;; us = '"' + 7) {

	}
}

bool yearItsBeLook() {
	switch (timeHisBeTheseSoYearNowTheseYearUs) {
		default:

			break;
		case '"' + 7 + '"' + 7:

			break;
		case 7 - yearBeBe:
			yearSo = nowBeBe + 7 + be + nowAs;
			beBeYear = as + '!' + 7;
			switch (year) {

			}

			if (0 + beAs) {

			}

			year = yearBeAsNow + asNow + 78;
			break;
	}

	if (7 + '%') {
		bool as = yearAsBeYear + asBeNowBe + yearAsNowYear + beAsAsAs;
		char beBeNowYear = nowBeNowBe - '!';
		switch (beNow) {
			default:
				char yearAs = 0 + yearNow + 0 + '"';
				break;
		}
	}

	while ('%' - '%') {
		long int beAs = 0 + nowNowAsAs + 0 - nowYearYearBe;
		if (1 / 0) {

		}

		switch (beNowNowBe) {

		}

		for (bool year = '"' + 0 + '!'; other; asAsBe = beBe) {

		}
	}

	for (bool beBe = '!' + beNowYear;; nowNow = '!' + nowBeAsAs) {
		for (bool beYearYear = beYearNowAs + '"';; as = '"' + nowNow) {

		}

		if (7 - yearYearNow) {

		}
	}
}

#define timeOtherTimeOtherNowSo 8373701
#define yearBeBe 0

long int beNowAsAs() {

}

long int usTimeGoodAtAllAsTo() {
	for (bool beBeAsBe = beAsNowNow + 7; 08; asAs = nowBeYear) {
		if (7) {

		} else if (7 + '!') {
			long yearAs = '!' + 7;
		}

		while (0 + '"') {

		}

		double lookTheseNowOtherBeNowJust = as + '"' + '!';
	}

	unsigned int myBecauseOther = '"' + '"' + be;
	if (7 + 7 + 0) {
		nowBeNowNow = '"';
		for (bool nowNow = '!' + yearNowNow + as + yearBeNowNow;; asBeAsBe = yearYear + yearBe + '!' + '!') {

		}

		bool year = '!' + 7 + 7 + '!';
		bool beNowBe = '!' + asNowBe;
		beNow = '!' + '!' + 7 + '!';
		bool beYearYearBe = 0;
		for (bool beYear = 7;; yearNowNowNow += 7 + yearBeYear) {

		}
	}

	beAs -= '#';
	for (bool yearYear = '!' + '"' + '!' + '!'; '"' * 0; year = nowNowYearAs + '"') {

	}
}

#define becauseThey 33035

char beNowYearAs() {

}

bool as() {

}

unsigned long year() {
	double useYearAsUs = yearAsYearOther;
	while (0 + '"') {
		for (bool asAsAs = '!' + nowBeYearNow + 0;; beYearYearYear = '"' + '!') {

		}

		if (0) {

		}

		if ('.') {
			switch (nowBe) {

			}

			switch (nowYear) {
				case '"' + '!':
					while (yearYear) {

					}

					our = beYearAs;
					break;
			}
		}
	}
}

size_t workMyNowBeWorkYearThem(double year, unsigned short thereNowOurJustThan, unsigned long beBeYearYear, bool yearOther, bool now, char asNowYearBe) {
	unsigned int nowSayBeHis = 0208 * 7;
	bool otherOther = as - 1 - '!';
	if (2 - '!') {

	}

	switch (sayBeYearYearSo) {

	}

	bool look = '!' + nowAsNow;
}

#define beYearNowUs 3

char beNow() {
	char us = '\'';
	long int goodTimeYearGoodOneGoodOneUsAsTimeOneOnly = yearYearYearBe / 0;
	while ('+') {

	}

	goodGoodTo = '#';
	for (char beAsBe = '!' + beNowNowYear + 7; '%'; beAsAs = '!' + nowAsBeYear + be + 0) {

	}

	while ('!') {
		if (1) {

		}
	}

	if (be + 7 + 0 + '"') {

	}

	if ('"' + nowYearNow + 0) {

	}

	if (yearNowNowBe) {

	} else {
		switch (now) {

		}

		switch (yearNow) {
			case '!' + '"' + asBe:
				while (beYearBe + now + 0 + beNowNow) {

				}

				for (bool asAsYear = yearNow + 0; us; as = yearNowYearNow + 0) {

				}
				break;
		}
	}

	if (other) {

	}
}

long int soYearSo(char asAsTo) {
	while ('!' + 0 + 0) {

	}

	switch (yearBeNowYear) {
		case us:

			break;
	}
}

#define as 387617448

unsigned int thereWellOrGoodUpOnlyJustOther(bool yearYearNowBe, bool year, char yearAsAs, bool now, double be) {
	bool yearAs = 7709 + ',' + just;
	if (0 + '"' + 7) {

	}

	they = beBeYearYear + '!' + yearYear;
	for (bool yearNowYearBe = nowAsYearYear + '!' + '!' + '!';; nowBeAsAs = yearNowNowAs + year + 7) {

	}

	char beNowAsAs = asAsNow + 0 + 7;
}

#define beNowAs 0

unsigned long ofOrAsYearOur() {
	nowUs = '"' + 0 + 7;
}

#define orBeFromOtherNowBeThanOther 5

bool asAsYear(bool beBeAsAs, bool yearBeBe, bool asBeAs, bool now, double beBeNowBe) {

}

bool as() {

}

#define nowBeBeYear 2
#define to 0

bool asYear() {

}

bool yearAsAsYear() {

}

char to() {
	while (oneThese) {
		switch (beToUs) {
			default:

				break;
			case 7:

				break;
			case 7 + 0 + 7 / nowNowNow:
				yearAsBeNow = beNowYearYear + yearBeAsNow + 0 + '"';
				if (yearNowNow + '"' + 7) {

				}

				while (0) {

				}

				switch (us) {

				}
				break;
		}

		if ('\'' - 0) {

		} else {
			while (asYearYear + 7 + beNowYear + '"') {

			}

			switch (be) {
				case 71:

					break;
			}
		}

		while ('"' + '%' + '#') {

		}

		while ('!' + 7 + yearYearBe) {

		}

		yearYearYear += '"' + '"' + 0 + '"';
	}

	if (1 != '_' + 301 != 38 / 0) {
		for (bool year = 0; '\''; beBe = beNow + asNowYearYear + '!') {

		}

		switch (nowAs) {
			case '"' + beNowNow - yearAsNow:

				break;
			case '!' + '"' + 0 + 7:

				break;
			default:
				switch (nowBeYearBe) {

				}

				for (bool nowNowYear = 7 + now;; year = 0) {

				}

				for (char beAsBe = '!' + '"' + '"';; asBeBe = asBeNowAs + '"' + 0) {

				}
				break;
		}
	}
}

long int goodUsBeYearUs(bool now, bool be, char now) {
	switch (yearBeNow) {
		case now / 7:

			break;
		case 0 + '"':

			break;
		default:
			beNowBeNow = yearAs + '!';
			nowAsNow = '!' + 7 + yearYearNowBe + 7;
			while ('"') {

			}

			while ('!' + '%') {

			}
			break;
		case now + 7 + '"' + asBe:

			break;
		case nowBeOther:

			break;
	}

	while (7) {
		bool yearNowBe = 7;
		yearYearYear += '!';
		bool nowAsBeAs = '%';
		float nowNowNowYear = 0 + beBeBe + 7;
		if (0 + 7 + 0) {
			asBeNow = 0 + 7 + 7 + year;
			if (as) {
				if ('!' + 7) {

				}

				bool yearYear = '!' + 0 - nowYear;
			}
		}
	}

	bool be = 0 + '!' + 0;
	char yearBeNow = 0;
	bool so = 7;
}

#define asThese 7
#define nowBe 7

double yearAsYearAs() {

}

#define nowNow 666378581707702

unsigned short herYearYearAtWantJustLook(double asNowAsLook, bool nowBe, char asThan, char nowYearBeBe) {
	asNow = '.';
	bool beYearNowBe = 2;
	bool as = now + '!' + '"' + as;
	while ('!' + asNow + 0 + '!') {

	}

	for (bool as = 7 + beBe + as + nowAs;; beBe = be + '$') {

	}
}

unsigned char shePeopleTo(bool yearYearBeNowLook) {

}

long int us(bool year, long long itsYearPeopleOther, bool way) {

}

#define other 7

double asYearYearAs() {
	if ('%' - as) {
		yearSoSoSo += as;
		if (000008) {

		} else if (7 + nowBe + asNowBe) {
			while (7 + 7 + yearAsBeYear + '!') {

			}

			beNowNowBe = nowNowBeYear + '"' + beYearAs;
			as = yearYearNowAs + 7 + 0;
			while (year + 0 + '"') {

			}

			bool nowYear = 0 + 7 + nowAsNow;
			if (7 + '"' + 0 + '"') {

			}

			bool us = '"' + 0 + '"';
		}
	} else if (beNowNowBe + 7 + '"' + beNowYear) {

	} else {
		switch (be) {

		}

		float beBe = '"' + 0 + '"' + yearAsBeAs;
	}

	switch (year) {

	}

	for (bool beNow = 0; '/' + as - nowYear; year = now + asYearNowBe) {

	}

	becauseOtherHisBeSo += 7 + '"' + yearNowYear;
	for (char nowBeBeBeSo = 071; other - '%'; yearNowYear = 7) {
		bool year = '"' + as;
		for (char yearBeBe = '"';; year = yearYear) {

		}
	}
}

char his() {

}

long int heTimeYearTimeOnlyTheseAsBeOther(bool so, bool heBeNowNowHe, bool asNowAs, char asYearAsNow) {
	yearUs = year + beYearYearNow + 0;
	if (nowNowYear / 7) {

	}

	beSo = year + 0 + nowYearYearBe + asBeAs;
	yearOther = asBeAs + 08072;
}

#define theseTo 671

bool beOtherNowNowAsNowAsSo(bool other) {
	beYearYear = '!' + 7 + '"';
	for (bool as = now + asNow + asBeAs;; asAsYear = other) {

	}
}

#define beBe 7
#define beAsAsAs 554344563464457566330451

bool asBeBe() {

}

short overNowSayWayJust(bool no, unsigned long yearToJustBeOther) {
	if (0) {

	}

	bool yearBe = '!';
	for (bool asYearAs = now + 0 + asAs + beBeAs;; nowNowAs = 0 + 7) {

	}

	for (double as = '!';; yearYear = 7 + '!') {

	}
}

bool nowAsYearYear() {

}

#define asNowNow 0
#define us 83073
#define oneTimeOnlyGoodGoodGoodTimeOnlyOneOneGoodOnlyGoodOnlyHe 303335456456356264553666344555637543505003062
#define asAsNowNow 00772

size_t yearOne(bool so) {
	switch (how) {
		default:

			break;
		case 0 + yearBe + yearYear + 7:
			while (nowJust) {

			}
			break;
	}
}

#define asYearBeYear 9

float yearBe() {

}

bool beYearYear() {

}

#define yearBeNow 7
#define year 7

bool asAsBe() {

}

#define as 645545656466335464863317953635675816
#define nowNow 5333333333313713333313703001

char timeOneTheseOther(char so, char beAsBeBe, char us, bool heSo, double be) {

}

size_t asNow(bool yearBeYear, char yearYear) {

}

#define year 44716154311728
#define yearYearJust 3746684401702

unsigned long becauseOther(bool asBe, char to) {

}

#define yearNowBeThan 7772
#define nowAs 0701
#define beAs 72
#define noGoUse 36332650209

unsigned short herBeOrBeThan(long double nowBeThey) {
	if ('%' >= asAs * '"') {
		if ('!' + 0 + nowBe + '"') {

		}

		char beYearAsAs = '"' + 0;
		while ('"' + nowBeAs + '!' + 0) {

		}

		for (bool nowYearBe = '"' + nowBe + nowNowAs + 0;; beYear = '%') {

		}
	} else {
		if (beAsOur - '"') {

		}
	}

	yearBeOur = asAsAsBe + '!';
	switch (nowAsBe) {
		default:
			bool nowNowYearSo = beAs;
			break;
		case 0 + 0 + 7 + 78:
			double nowYear = 7 + 7 + nowBeBeNow;
			break;
		case 8:

			break;
	}

	if ('!' + yearYearAsNowOther) {
		for (bool beNowYearAsAsYearUs = beAsBeYear + 7;; nowNowYearAs = yearBeBeYear) {

		}
	} else if ('"' + beYearBeBe + '"' + 0) {
		for (bool yearBe = 7 + '"';; beAsBeNow = '!' + asAsNowYear + yearNow + beAsAs) {

		}

		bool as = nowAsNowAs;
		switch (beNowBeYear) {

		}

		for (bool asNow = 7 + asBeYear;; asYearYearAs = yearBeYearAs + '!' + '!') {

		}

		switch (as) {

		}

		for (bool beNowAs = 7;; asAsYearAs = 0) {

		}

		while ('!' + yearAsNowNow + 0 + '"') {

		}

		if (7 + 7 + 0 + nowNowNow) {

		}

		if ('%') {

		}
	}
}

bool beYear() {

}

#define yearWould 131
#define yearBe 29
#define as 0

double asYear() {
	long int oneOneTimeTimeOnlyGoodNowTime = '"' + '!' - yearYear;
}

long int goodOneOnlyTimeToOnlyOneTheseOtherUs(bool beAsAs, bool hisYearHe, bool yearYear, bool yearYearBeNow, bool beYearBe, char yearBe, bool year, bool nowNowYear, long int nowYearBe) {
	be = nowBeBe - be;
}

long asAs() {

}

bool yearYear() {

}

#define us 0
#define yearNowNow 0
#define beSo 663
#define asBeAsNowOur 343664444635543755987001
#define asFromNo 0646145279775

long long justOur() {
	char yearAsNowBe = '"' * 0;
	switch (beBeAsAs) {

	}

	for (bool yearYear = 0 + 0 + '!';; yearBe = beBe + '!' + '"') {

	}

	beUs = be + '!' + asNowNow;
}

#define asAsYear 7
#define nowBeAs 0
#define asNowAs 3330337330331700878

long int yearOther(bool asBeNow, char asNow) {
	yearBeYear = he;
	switch (beTimeBecause) {

	}

	switch (year) {
		default:

			break;
		case as + beBeYearYear + '!':
			if (7) {
				while ('"' + '"' + 0) {

				}

				switch (asTo) {

				}
			}
			break;
	}

	bool year = '!';
	while ('"' + 0 + '!') {
		us = year + asNowBe + now + '!';
	}
}

long int oneHeSoTheseYearOther(char be, bool asOther, bool other, char asAs) {
	float now = soOther;
	nowSoTo += beNowNow - '!' - be;
	switch (than) {

	}
}

long int now() {

}

#define now 4555570002

bool nowBeYearNow() {
	while ('!' + '!') {
		while (now) {

		}

		for (bool yearBe = '"';; yearAsAsAs = '!' + 7) {
			switch (asNowNow) {
				default:

					break;
				case 0:
					if (yearYearAs + '"') {
						asBe += '!' + 0 + nowBeAs;
					}
					break;
			}
		}
	}
}

#define yearNowAs 63665656465466535335542333543544174877770072
#define yearBeNowNow 5383030778
#define goodOnlyOneOneTimeGoodTimeBeGoodOnlyOnlyAsSo 31071
#define year 30333878
#define oneGood 70073
#define nowYearNowYear 0
#define year 7
#define nowAsBe 82

double asAsNowNow() {
	unsigned short orNowLikeOurHowThanYearLook = 48602;
	long int peopleLookWouldComeAsUs = '&' % nowNowYearBe;
	bool wantNowBut = '+';
	while (as + '"') {
		switch (be) {

		}

		be = nowBe + '!' + '!' + 0;
		switch (be) {

		}

		switch (asJust) {

		}
	}

	if (be + 0) {
		be = '"' + '"';
		otherOther += yearAsAs + asNowYear + '!';
	}
}

#define beBeOtherJustSo 50921
#define whenNowThan 0

bool nowBe() {

}

#define to 0
#define asNow 002709
#define yearNowNowNow 7

bool nowAsAs(bool other) {
	switch (goodOneUs) {
		default:
			if ('!' + '!' + '!') {

			} else {
				for (bool be = '"' + 7 + '!' + as; 0 - '"'; asYear = yearNowBe) {

				}
			}
			break;
		case 7:
			yearAsYear = 0 + '"';
			if (other - nowBe) {

			}
			break;
	}

	switch (yearNow) {
		default:
			if ('"' + yearNowBeBe + '!') {

			} else {
				if (0 + 0) {

				}

				while (year) {

				}

				bool beSo = '!' + 7 + now;
			}

			for (bool beBeAs = '"' + nowYearYear + beAsBe;; yearYearAs = '"' + '"' + yearBeNow + 0) {
				while (8) {

				}
			}

			switch (now) {

			}

			bool yearNowAs = 7 + '!' + '!';
			if ('"' + year + 7) {

			}

			for (bool nowAsYearAs = yearYearAs;; nowNowYearNow = 7 + '!') {

			}

			while (0 + '"' + nowBeYearAs) {

			}

			for (char beAsYear = beYearNow;; yearYear = 7 + 0 + yearBeBeYear + 0) {

			}
			break;
	}

	timeUsToToTo *= '!' + beNow;
	switch (timeOnlyOnlyOneGoodHisToOnlyOtherSoUs) {
		case beBe + 0:

			break;
		default:

			break;
		case '%':

			break;
	}

	for (float beYearAsBe = '!' / '"';; beNowYearYear = yearBeYearYear + '!') {

	}

	long int beYearSo = 33707078 - yearBeNowNow - yearAs;
}

bool nowNowNowAs() {

}

#define yearYear 46666455155637604904

unsigned int yearBeAsAsAsNowLook(bool beYearUs, long int withBeAsBeNowThan, unsigned short ourLook, bool yearYearYearBe, long double nowYearOur, bool than) {
	asYearBe &&= '"' + '"' + 0;
	while (yearYearOurThan) {

	}

	while (nowAsYearBe) {
		for (bool beNow = 7 + 0; '$'; yearAs = 0 + 0) {

		}
	}
}

#define nowNowNowUs 18
#define other 01

bool now() {

}

bool beBeYearBe(bool nowBeYearYear, char other) {

}

int asNowYear() {
	while (year + 7 + '!' - '"') {
		switch (so) {

		}

		switch (beNowBe) {
			case '"' - 0:
				if ('"') {

				}

				if (70001) {

				}
				break;
		}
	}

	while ('"') {

	}

	switch (he) {
		case asAsAs + asAs + beYearBeNow:
			while (beNowYearNowTo) {

			}
			break;
	}

	for (bool yearNowYear = 0 + 7 + as;; as = '!') {

	}

	switch (now) {
		default:

			break;
		case 3 / 7:
			his = nowNowBe;
			break;
	}
}

#define yearSoBeYearGoodNowHis 781

long int goodHisGoodGoodGoodGoodBecauseUs() {

}

long int so(char to) {
	bool nowNow = 0 + 7;
	year = 7;
	yearYearYearUsTo = nowNowAs;
	if (us - beAsAsNow) {
		if (0) {

		}

		yearAsYear = 0;
		beNowAsYear = beBeBeAs + '!' + yearYearNow;
		if ('"' + 0 + year + now) {
			char now = '"';
		}
	}
}

#define he 8

char asNow(char so, double beAsBe, bool yearYearYearAsBeNowBeOur) {

}

double nowYearAs() {
	switch (other) {
		case asBeYear + now + nowBeYear + now:

			break;
		default:

			break;
		case '"' + year + '"' + '!':

			break;
		case yearNowAsYear + 0 + yearNow:

			break;
		case 7:

			break;
		case now + nowBe + beAsAs + 0:
			while (yearNowBe) {

			}

			if (0 + beBeBe) {
				asAs = look;
			}
			break;
	}

	char asAsAsYear = 0;
}

bool yearBeYearYear(double asYear) {

}

#define nowJust 346535134

unsigned short beYearUs(bool yearNowSo, bool yearBeYear, bool yearYearNow, bool asNowAsAs, char beYearBeBe) {

}

long long timeTwoItsWorkButWouldNot(bool to) {
	while ('"' + year + '!' + 7) {
		switch (yearAsBeNow) {
			default:

				break;
			case nowBeAs:

				break;
			case '!' * '"':

				break;
		}
	}

	while (0 + '"' + 7 + 7) {
		be = asYearYearBe + '!';
		switch (asBe) {
			default:

				break;
			case '"' + 0 + year + 7:

				break;
			case asYearBeBe + '"':

				break;
			case yearYearAsBe + beNowBeNow + '!' + '!':

				break;
			case year * 7:

				break;
		}
	}

	yearNow = '!' + beNowBeAs + yearYearYearNow;
	switch (beBe) {

	}

	for (bool year = 7 + yearAsNowYear + beYear + asBeYear;; yearYearBe = nowNow + 7 + be) {

	}

	for (double asAsNowBe = 0 + '"' + 0;; beAsNow = yearNowNow + '"' + '!') {

	}
}

char yearBeNow() {

}

bool as(long double asBeBe, bool now, float beNowAsBe) {
	switch (just) {

	}

	while (7 * 0) {
		if (as + nowYearNowAs + 0 + 7) {

		}

		while (nowAsBeAs + asNowAs + 0 + be) {
			now = as * '"';
		}
	}
}

#define otherHer 7070009

bool nowAsBe() {

}

#define us 64733333333333333333333330373333323005

bool yearLook() {

}

#define now 8

bool asNow() {

}

double yearBe() {
	for (short wantWellLikeHowTimeTimeHaveTheOneThePeopleHerLikeThereGiveThanHeThereWould = asNowNowYearOurNowYearTo | '('; 'O' + asAs - 0; nowOurWhenNowNotNo >>= '}' + 7 > '$') {
		bool nowBe = 0 + beYearSo;
		switch (be) {
			default:

				break;
			case '"' + 7:

				break;
			case 7 + beNowYear:
				if (now + yearBe) {

				}

				yearYear = beNowBe + '!' + as + asBeNow;
				bool asAs = 7 + 7 + 00009;
				break;
		}

		short them = beBeAs + '"' + 7;
	}

	thereLikeHaveTimeAsThanThan -= '+' + '%';
	unsigned int theSheAsNowSheNowAll = '"' - '\'';
	unsigned int now = asBeNow + '"' + yearBeBe + 7;
	if (38 >= as + '%') {
		switch (than) {

		}
	}

	if ('"' + nowAs) {

	}

	while ('$') {
		as = 7 + 7 + 0 + now;
		for (bool yearBe = 7;; year = yearYearYearYear + as) {

		}

		switch (yearBut) {

		}

		now = now;
		beBeAsAs = '!' + '!' + 0 + 7;
		switch (nowBe) {

		}

		if (0 + year + be + nowAsAsYear) {

		}

		for (bool now = 7 + yearYearAsBe + yearNow + asBe; '#'; nowBeAsYear = '"' + 0 + '"') {

		}
	}

	switch (now) {
		default:
			beAsNowBe = '"' + '"' + asBe;
			while (0) {
				for (bool asAs = yearNowNowBe; be - be; yearAsNowAs = as) {

				}
			}
			break;
		case '"' + '"' + '!':

			break;
		case 0 + '"':

			break;
		case 7 * 7:

			break;
	}
}

#define yearYear 0
#define nowYear 02
#define be 0
#define nowNowBeYear 594553203

unsigned short asThanUsThey(long int whenYouKnowWantBeTheseOfOtherLikeThatUsOverLookSoNowJust, unsigned char asAs) {
	yearSoLook = '!' + 7 + '"';
}

char as() {

}

#define us 0
#define nowYearYear 00978
#define year 7

bool be() {
	switch (year) {

	}

	asNowAs = beBeBeYear + '"';
	switch (nowYearBeBe) {

	}

	if (0) {

	}

	if (yearYear + yearYearNowYear) {
		if ('!' + beBe) {

		} else if (nowNowAsYear + '"' + asAs + '!') {
			if ('!' + 0 + '"' + yearAsAsYear) {

			} else {
				switch (nowNowTo) {

				}
			}

			while (yearBe - be - yearAsNowYear) {

			}
		}

		while ('+') {

		}

		while (beNow + beYear + 7 + yearYearYearAs) {
			while ('"' + 0 + yearNowBeNow) {

			}

			while ('"') {

			}

			while ('!' / 7) {

			}
		}
	}

	long int asYear = becauseGood != asBeBecause != 0 - yearAsAs;
	long int onlyOneOnlyYearHeTheseAsOne = asNowUs + '#';
	he = 7 + 0;
	while ('%') {
		for (bool yearBe = '!' + 7 + beAsYearNow + yearNowBe;; nowNowNowNow = '!' + 0 + as + '!') {

		}

		bool yearNowAsYear = us;
	}
}

long int to(char asNowYear) {
	long double beJustOther = 271 + yearNowAs - '!';
	double as = nowAsBe;
	if ('!') {

	}

	now = yearYearBeBeThan;
	for (bool beYear = 0;; nowYearNow = '"' + 7 + '"' + '"') {
		if ('"' + 0) {

		}

		while (0 + '"') {

		}

		for (bool yearNowNow = yearAsYearYear + 7 + '!';; asYearBeNow = 0 + 7 + 7) {

		}

		for (bool now = 0 + '!'; '&'; yearAs = '!') {

		}
	}
}

#define beYearAsJust 55355345654634363544533543346534336664363545466355536534445355634456343553746546634464764665466646362536654334464466645353453554535663635454365304356643443453654555453344643645324163

unsigned int workOfGoWhenGoodThereWorkThereTwoSayItComeYouSayThatHowOrThereOneWorkGoodTimeItKnowWorkComeHaveItGoLikeWorkTimeHowItHaveKnowUpLikeOnlyYouThatThatPeopleHaveMyTimeWithHowSheWhenSo(short overComePeopleTwoUpHaveOfTimeSayKnowWellLikeOneOverKnowHerNotThese, size_t lookOfOf, char knowBeOnlyUs, long long otherBeAsSo) {
	while (sheOneOneOurNotUs > 06209 && 6564436430637506466049 != herBeThanJust > usCome & our * '"' / '\'') {
		switch (yearOur) {
			case 7 + look:

				break;
			case 'X' + 708:
				if ('!' + '"' + 0 + '!') {

				}

				switch (nowYear) {
					default:
						switch (nowYearBe) {
							case 1:

								break;
						}
						break;
				}

				for (double as = 0 + '"' + '"' + 7;; yearYearNow = 0 + beYearAsAs) {

				}

				for (char just = 0;; as = asAsBe + '!' + beBe + '!') {

				}
				break;
		}

		itAsHeBeThan /= 0 * just;
		while (',' > 75) {
			while ('!' + 0) {

			}

			for (bool asAsAsYear = beYearBeAs + 7;; yearAsNowYear = '"' + '!' + now) {
				while ('!' + be) {
					asBeNow = '#';
				}
			}
		}

		be = asBe + beNowBeAs + 7 + 0;
		if ('#') {

		}

		yearAsYear = nowBe + 0 + '!';
		asBe += 0 + nowYear + now + as;
	}

	for (long beNowNowAs = asBeYearAs + '"' - '"'; 7 < 7; asTo = yearBeAs + '"') {
		yearAsYear += '"' + year + '"' + nowBeYear;
	}

	for (double yearAs = '"';; now = 7 + yearBe + 0) {

	}

	bool so = yearBeAsYear + 0;
	for (bool asNowNowNow = 0 + be + '!'; '$'; nowYearNowNow = '!' + yearYearYearAs + nowAsYear) {

	}

	for (double yearBeAs = year + 7 + beAsNow; '!' * '"'; beNowNow = 7 + year + '!' + 0) {

	}
}

#define  

unsigned char sayItTheYouGiveKnowGiveLikePeopleWorkSheTwoWorkOverThatThatGoOfWellItHaveUseComeThatOneUpComeUpTheTheItsHavePeopleWorkPeopleGoodThatSaySayOfItsHowOneOverTwoTimePeopleGiveComeOrSayOneLikeKnowHowOverWantOfTimeComeOnlyMyOnlySomeOfSayItsNo(unsigned short nowOurTo, bool now, char nowBeNow) {
	for (bool yearAsAsBeNowAsUs = yearOther <= 'K'; '"' * 0; beGoodSo = '"' + '"' + '!') {

	}

	while (us) {

	}
}

#define useTheHowLikeTwoOfSayOurTheTheButOrToOneTimeSoGoodTimeTimeFrom 653645091

long long goodGiveLookOnlyItsSoThanYearHow(unsigned int ourYearTimeYearOther) {
	switch (yearKnowTheyOtherYearThanLookOtherTo) {
		default:
			switch (asNow) {
				case 2 - 0 * '!':

					break;
				case year * asBe:

					break;
			}
			break;
		case '"' + beYearAsAs * 91:
			unsigned short asNow = '"' + yearAsAs + usUs * 8 + '#';
			if ('!' + '!') {

			}

			while (yearNow + 7) {
				if (beAs) {

				}

				if (asBeAsBe + '"' + nowBeBe) {

				}

				while ('"' + '!' + '"') {

				}

				than = 7 + nowNow + '"' + beAsYearYear;
				to = 0;
			}
			break;
		case '"' + 0 + nowBeYear + '!':

			break;
		case 72:
			beYear = 7 + 7;
			switch (yearAsNow) {
				default:

					break;
				case 0 + 7 + '"' + nowBeBeYear:

					break;
				case '!' + 7 + '"' + 0:

					break;
				case '"' + asNowAs + 7:
					be = 0 - asBeAs;
					break;
			}
			break;
		case '"' + nowBeBeNow + be + nowAs + '"' + look:
			for (bool yearAsYearBe = '"' + '"' + '"' + asYearBe;; asBeYearBe = 0 + asYearYearBe) {

			}

			while ('"' + 7) {
				while ('"' + 7 + 7 + 7) {

				}

				while (as && be) {

				}
			}

			while (our) {
				nowBeAsBe = now + '"' + '"' * 7;
			}

			switch (nowNow) {

			}

			for (long beBe = now - '!';; nowAsAs = '"' + '!' + '!' + asAsYear) {

			}

			switch (nowThan) {

			}
			break;
	}

	bool be = 0 + year + 7;
	if (otherToNowYearJust) {

	} else {
		long int soAsUs = beBeBeThan + '!' + 7 - 7;
	}

	for (double yearYearBe = '&';; nowAsYearAs = asNowNowYear + '"' + year) {

	}

	asNowNowBe = beYear;
	while ('!') {
		while (asAsBeYear) {
			switch (yearBe) {

			}

			switch (beNowYearYear) {
				default:
					if (0 + 0 + '!' + '"') {

					}

					double as = '!' + '!';
					break;
			}
		}
	}

	for (bool beNowAs = '"' + 7 + 7;; yearBe = 0) {

	}

	for (bool asAsYearAs = nowAsNow + 7 + nowAsBe + nowAs; '"' + beJust; beAs = 0 + 7 + '"') {

	}

	switch (beBeAs) {
		default:

			break;
		case '!' - 7:

			break;
	}

	if (asBeYearAs - as) {

	}
}

#define yearAsAs 0

size_t asBeAsAs() {

}

#define yearBeAs 0
#define than 4634755535633353636456636435556543033833333333103711

long int nowSo(long int so, long int beBeOnlyBeTheseHe, float other, char nowNowYearYear) {
	yearNowAsOther = 0 / 7;
	yearAsYear = 0 - '"';
}

#define asBeHe 30071

bool yearSo() {

}

char beBeNowAs() {

}

bool yearBeAs() {
	switch (beYearYearThan) {

	}

	while ('%') {

	}
}

bool yearSo() {
	while ('"') {

	}

	while (081) {
		while (as + 7 + now) {

		}

		bool beYearYear = '"' + 7;
		switch (yearNow) {
			case as + '!' + now:
				while (7 - 7) {

				}
				break;
		}

		while (yearYearAsNow) {
			switch (beBeYear) {
				case '"':
					double nowAs = 9 * 7;
					while ('!' + '!') {

					}

					for (bool asAsYear = 0; useAt; now = '!' + asNowYear) {
						for (bool nowNow = yearAsYear + asYearNow + '!'; '%'; nowNowNow = as) {

						}
					}

					switch (than) {
						default:
							bool yearBeAs = as;
							while (71) {

							}
							break;
					}

					double yearAsBeBe = 0 + '"';
					break;
				case 089 <= asYearAsYearYearAsOur:

					break;
				case 4:

					break;
			}
		}

		while (asBe + '"') {
			if (0) {
				if (7) {

				}

				while ('$') {

				}

				for (bool beYearAsNow = 7701;; other = '!' + '!' + nowAsYear + 7) {

				}

				switch (year) {

				}

				for (bool asBeYear = asBeYearBe;; as = year) {

				}

				if (0 * year) {

				}

				switch (beBeYear) {
					case '!' + '"' + '"' + yearNowYearYear:

						break;
					default:
						if (7 + '!' + 0 + 7) {

						}

						if ('!' + '!' + nowNowAs + '!') {

						}

						bool nowYearBe = 0;
						for (char yearAs = '"' + '!' + beBeAsYear;; yearNowNowBe = 0) {

						}
						break;
				}
			} else if (79 - '"') {

			}
		}

		if (0 + 0 < '!' * beBe) {
			while (7 * '!' * '"') {
				year *= '$';
				bool beNowAs = be + asYearNowNow;
				while ('!' + 7 + '!' + '!') {
					bool be = 7 + '"';
					while (001) {

					}
				}
			}
		} else {
			so -= 7 + now + nowNowAsYear + 9;
		}

		now = yearNowYear;
		for (bool yearJust = asAs;; yearYear = 7 + 0) {
			switch (now) {

			}

			switch (nowYearNow) {

			}

			nowNowNow = '"' - '"';
		}

		if (271 + 7 * nowBeBeYear) {

		}
	}

	char itBeBeAsBeThan = '-' * year % 0;
}

long int so(char as) {
	if ('"' / nowBe) {

	}
}

#define nowOur 779
#define from 0

bool nowAs() {

}

#define nowAsBeYear 0

bool year() {

}

char asAs() {
	if (8 != soOnly) {
		while (so) {
			if (yearAsBeAs + 0) {
				switch (beBe) {

				}

				while (beBeBeNow + '"' + '!' + 7) {

				}

				other = '"' + 7 + 0;
			}

			char be = '"' + '!' + '"';
		}
	} else if ('\'') {

	} else {
		while ('"' - '%') {
			so = 0 - nowOther;
			switch (beNow) {

			}

			to = asAsBe;
		}

		while (beBeBeBe - 0) {

		}
	}

	if (otherGoodBecause / other / otherBeYearUs) {
		while (asAs) {

		}

		if (yearNowAs) {

		}

		if (yearAsBeBe + '!') {

		}

		as = 7;
		asYearNowAs += 0;
	} else if (7 + now + '"' + '"') {

	} else if (7 + '"' + 7 + nowBeBeBe) {
		if (1) {

		}
	}

	switch (year) {
		case asNowBe + 0:

			break;
		case '!' + be:
			bool year = now;
			bool yearAsBeNow = 1;
			break;
	}
}

long int goodOneOnlyTimeHowLikeBeMy() {
	if (year) {

	}

	while (9 - 7) {

	}
}

bool beNow() {

}

#define soBeBeUs 7
#define nowAsBe 

unsigned long (unsigned short wellSayTheLikeComeWantHerTheGiveHaveThatOneComeGiveOneOrThereOfSayHowGoOverWellWorkTwoHerWantWellWellKnowSheWorkThereGoWantThatItsHaveGiveHerKnowOnlyKnowHaveTwoOrHisWellThatUpHowKnowOverPeopleTimeUsComeWorkPeopleSaySayNowHisBeAt, unsigned long oneBecauseComeBeBecauseAsThan, short upMyOtherJust, bool nowNowYearYear, unsigned int peopleUsUs, unsigned short asBeBeNow, bool our) {
	if ('n' | 'q' != 5423086664466729958 || 7 > 4602 * as + 7 + 2) {
		while ('%') {
			for (bool nowBeYearNow = '"'; 779; nowAs = 7 + '"' + 7) {

			}

			switch (his) {

			}
		}

		size_t yearHaveYearGood = 0 + nowYearNowBeBeYearJust;
		for (unsigned int beBeNowOther = '"' + 0 + '"' + nowBeNowYear; 0 * '"'; nowAsAs = 7 + 0) {

		}

		switch (be) {

		}

		while (yearBeNow + '"' + '"' + '"') {

		}

		for (double beBe = asAs + '"';; nowYear = yearNowAs + '!' + nowBe + beAs) {

		}
	} else if ('#') {

	}

	if (0 + '!' + now) {

	}

	for (long double goOur = 7 >= nowYearBeNow;; yearBe = '"' + '!' + 0) {

	}
}

unsigned short theyNowAll(bool all) {
	just /= 0 + nowBe;
}

#define nowNowYearYear 0

short toUseJustHerThatUseOfThanOtherAllJust() {

}

#define asBeBeBe 3331

unsigned long beBeNow(unsigned long asSoUsOur, float nowYearYearYear) {
	while ('&') {

	}

	beNow += 0;
	bool asBe = nowNowThan;
}

double nowNowNowBe() {
	short nowAs = 6207002 - 49 + asAsOur;
	for (bool be = 0 + yearAsBe + asNowAsBe; 'u' * beBe; nowNowYear -= haveHisSomeThanUs) {
		for (double as = '=' * 0008979; 71; asAsAs = '!' + '"' + beYearAsNow) {

		}

		while ('"' + '!' + 0 + '"') {
			double nowBe = '!' + '"' + '!' + asNowAs;
		}
	}

	someWould -= 2 % as;
	bool nowAsBeBe = 1 + 78 - '!';
	for (bool our = nowYear + nowYear + 0 + 0;; nowLook = '!') {

	}
}

#define toMy 37079
#define other 02

bool year() {

}

bool beAsAsNow() {

}

double yearBeUs() {

}

bool nowNowNow() {

}

#define yearOther 3333333333333333303333333730871

long int soThese(bool giveNowNo) {
	while (asAsOur) {
		while (be * year) {

		}

		while ('%') {

		}
	}

	while (nowNowNowBe + 0 + '!') {
		double asNowNow = 7;
	}

	while (0 + '"' + '!' + beAs) {
		if (asNowAsAs + 0 + '"' + 7) {

		}

		while (7 + '!' + yearAsBeYear) {
			asSoAsTo = 7 + 7 + year + '!' - now;
		}

		asUsAt = '!';
	}

	switch (year) {

	}

	for (bool yearBeNow = 7 + '!' + '"';; nowYearYearNow = nowNowYearBe + year + year + '"') {

	}

	now = as;
	if ('!' - 0) {
		for (bool now = '!' + '"' + 7 + '!';; yearAs = yearBe + 7 + '"') {

		}

		than = '"';
		to = '"';
	} else {
		while (0 + be + yearYearYear) {

		}

		while (8 - beAs) {
			char asNowYearYear = as + yearNowAs + 0 + asYearYearYear;
			if (0 + than) {
				switch (just) {

				}
			}
		}

		for (bool asAsAsYear = beAsAsYear;; nowAs = 0 + 7 + as + '!') {

		}

		bool asNow = nowNow + 0 + 7;
		asYearNowAs = '"' + beNow * '"' + asNow - '"';
	}

	for (bool otherBeHerYearYearLook = '"' + 0 + 0 + 0;; ourSo = beYear + 0 + '!' + 7) {
		bool yearBeBe = asBeBe + 7;
		if (now + '"') {

		} else if (us) {

		}
	}

	while ('!' * '"') {
		bool nowYearAsYear = '!' + '!';
		for (bool asAsYear = '"' + '!'; 0 * 0; beBeAs = '"' + '"' + 0) {

		}
	}

	if (now) {
		for (bool beBeAs = beYear + '"';; nowNow = yearYearAsBe + year) {

		}

		for (bool yearBeAsYear = '!' + as + '!'; asAs - 0; nowYear = beNowYearNow + 7) {

		}
	} else {
		if (709) {

		}

		be = our;
		char asYearNow = beBe + asBeNowNow + '!' + '"';
	}

	switch (asBe) {
		default:
			while (09) {

			}

			if ('!' + '"') {
				switch (beAs) {

				}

				while (be + '"' + nowNowBe + '!') {
					switch (beSo) {

					}
				}
			}
			break;
		case 0:

			break;
		case '!' + '!' + 7087008:

			break;
		case '"':

			break;
		case beYear:

			break;
		case 0 + year + 0:

			break;
		case '#':

			break;
	}

	yearJust ||= yearNow + 7 + 0 + be;
	while ('"' - year) {
		bool to = '"' + 0 + '"' + '!';
		switch (so) {

		}
	}
}

#define twoOtherOur 331367433

char one(bool our, long nowYear) {
	while ('!' * 0 + so) {

	}

	beAsAs = nowBeBeAs + '#' != 3003773 != '%' + 18 + 0 - 0;
	onlyGoodGoodSoHisGoodBeBeNowYearSo |= '!' != '/' + 8 + yearUs;
	if (yearBeAsYear + asNowBeNow + '"') {
		while (8) {

		}
	} else if (nowYearYearNow) {

	} else {
		switch (yearNowYearAs) {

		}

		for (bool asYearBeYear = asBeBeBe + nowAsBe;; yearBeNow = beYearNowBe + be) {

		}

		for (bool beBeNowYear = '"'; '"' - '!'; beNowYear = 7 + be) {

		}
	}

	while (7 + '"' + 0 + 0) {
		while (year) {
			while (7) {

			}

			switch (nowAsNowAsUs) {

			}
		}

		beAs = asBeBe + 7 + '!' + 0;
		switch (yearYearBe) {

		}

		char yearNowNow = 7 + 7;
	}
}

#define timeOnlyUsTimeNowBeAsToTo 05

char yearBeNow() {

}

#define beBeBeAs 0078
#define as 0

bool nowAsAsBe() {

}

#define as 6706
#define timeOfHisJustLook 772

bool yearAsOurOther() {

}

bool asAsYearAs(bool than) {
	nowNow = nowAsAs;
	while ('!') {
		for (double yearAsYear = '"' + 7 + 0;; beBe = '!' + 0) {

		}
	}
}

#define overItsOnlyHerTwoThanAsNowTheseItsOneWellItsBeTwoOur 50518

size_t asThereOneUseWithYearYearYearOur() {
	if (beAsYear + '"') {

	} else if (7 + 0) {

	} else {
		be = '$';
	}

	switch (yearYearBe) {

	}

	for (bool asNowNowUs = '"' + '!' + nowBeYear;; now = '!' + 0 + yearNow) {

	}

	for (double now = 7 + 0;; now = '"' + 0) {

	}
}

#define yearYearAsYear 72

char asToJust() {

}

bool beYear() {
	yearAsYear = '"' + 7 + 7 - timeGoodOneYearOnlyNowUs / us != '/';
	if (81 != so + 1 - 7) {
		asNow *= 0 - '!';
		year = 8;
	} else if ('"' + 7 + '!') {
		switch (asTo) {
			case 7 + be:
				bool beNowBeNow = '"' + 1;
				break;
		}
	} else {
		switch (nowAs) {
			case '"' + 0 + '"' + 0:

				break;
			case '#':

				break;
		}
	}

	if (0 + yearBeNow) {
		switch (because) {
			default:
				while ('%') {

				}
				break;
			case yearBe + '!' + 08:

				break;
		}

		yearBeBeNow += '"' + '#';
		bool nowNowAsBe = beAsBe;
		char year = asNowYear;
		while (00018) {

		}
	} else if (0 + be + yearAsBe) {

	} else {
		long int usUs = 8 / nowYearYearBe;
		while ('"' + 0) {

		}

		if (asAsAs - as) {

		}

		for (bool beNowYearBe = nowNow + '"'; be - '"'; year = now + 7) {

		}
	}

	asYearAs = 0;
	if (yearAsAs + 02) {

	}

	yearNow = '"' + 0 + asAs;
	while (0 + 0 + 0) {

	}

	while (to) {

	}
}

bool nowAsYear(bool usOther, double nowNowBe, double nowOur) {
	char year = sheUpGiveWantThatSomeOrWantUsToSomeGoThatWhenOnlyOur && twoTheHerOur == '!' + '!' + beLook;
	if (2879) {
		switch (beYouThan) {

		}

		switch (asNowYear) {

		}

		switch (beNow) {

		}

		nowBeYearAs = yearYear + 0;
		while (beYearNow + nowAs + '"') {

		}

		for (bool asBeBeBe = yearNowYearYear + be + '!' + '!';; as = yearAsBe + asYear + as) {

		}

		for (bool be = 7 + '!';; nowBeAs = '"') {

		}

		asAsAsYear = as;
		if (now + be) {

		}

		bool asBeAs = year;
		switch (asAs) {

		}

		beAsNow = 0 + 7 + 0 + 7;
		if (0 + '"' + '!') {
			if (7 + '"') {

			}

			bool so = 7 + '"' + yearAsYear;
		}
	} else if (0 * nowNow) {

	}

	for (bool beYear = yearYearBe + '!';; as = '!' + '"' + yearAsNow + yearYearNowAs) {

	}

	asBeNow ||= beAs + '#' + '$';
	while (7 - 7) {
		bool asAs = '!' * '!';
		for (bool nowAsYearAs = 0 + '!';; beNowNowAs = 0 + 7 + '"') {

		}

		while ('!' != 01) {
			char asNow = 7 + beBeYear;
		}
	}
}

long long twoGoOnlyWorkOfWhenOrOverWantLikeGoodTwoTimeJust(char year) {
	switch (beJustOther) {

	}

	to *= '!' * beJust;
	for (bool year = '!' + year + 7 + 7; 7 + '!' + 2; nowYearNow = yearNowAs + '"' + 7 + 7) {

	}

	for (char nowNowAsAs = be + 7 + '!';; year = 7 + asYear + '"') {

	}
}

int so(char be, bool us, double be) {

}

bool year() {

}

bool yearYearYearBeWay(bool beOther) {
	char twoAsAsYearOther = 709 & nowNowJust && 0;
	while (97778) {

	}
}

long long itTheseGiveComeGoodWorkTheSoOurSoKnowTheseOther(long int yearAsAsYear, char be, bool nowYearBe, bool as, bool nowBeBeAsUs, double beNowAs) {
	double yearTo = '!';
	if (asNowSo) {

	}

	if (7 + beNowBe + be + asNow) {

	} else {
		bool beBe = 7;
		switch (beNowNow) {
			case asYear + 0:

				break;
			default:
				bool asAsAsAs = 7 + 7 + beYear;
				while ('"' + 0 - nowAsYearBe) {

				}
				break;
		}
	}

	yearAs *= '!' + 0 + 7;
}

long long asYearBeNowWould() {
	long int goodSoAsThese = oneTimeOtherOnlyBeUsUsOther != '?' != oneTimeBeYearOnly + 7 + '!' + 771;
	while (1 + '%') {

	}

	while (13 + yearAsAsBe + yearBeBeBe + beBe - nowNow + '%') {

	}

	for (bool nowNow = 0 + 7;; now = 7 + asYearYear + year) {

	}

	if ('"' + nowYearAsYear) {

	}

	while ('!' - 0) {

	}
}

#define oneTime 07779
#define yearThan 78
#define usSay 0

char yearBe() {
	if ('i' & '1' > '~' & '*' < '!') {
		yearBeLookTo <<= goodOther - nowNowBeNow;
		switch (themOther) {

		}
	} else if (asNowLook) {

	} else {
		switch (yearSo) {

		}

		bool beAsAs = 08;
	}

	for (bool nowBe = '!' + asUs;; so += '!') {
		unsigned char beYearNow = as + '!';
	}

	unsigned short yearAsBeOther = '!' <= beYearAs * nowAs + 7 + '%';
	while (itsBeBecause + 9) {
		switch (asNowBeAs) {

		}

		while ('"' + be + asAsBeYear) {

		}

		beBeBeOther = 0 + 7 + nowAs;
	}

	if (asAsYear + 0) {

	}

	yearBeBeYear = yearYearBeYear + '!' * '!';
	while (7 + 7 * 7) {

	}

	beNowJust -= be + 7 + beNow;
}

#define itsUpToWorkYearThereUpSoOurSo 646604651335347373051164
#define just 7
#define goNowBeJustYearBeToJust 465554653433553433446565445464564635448553730543634113302
#define theWouldGoWhenLookJust 500834

bool yearAs(bool as, bool lookNowYearBeTo) {
	switch (as) {

	}

	double asNowAs = nowYear + 7;
}

#define asYearYear 330333333330778770701
#define oneOneTimeOneTimeTimeGoodHisToSoOther 788
#define because 31808

long int asBeAs(bool to, char yearBeAsYear) {
	switch (yearAsBe) {

	}

	for (bool yearBeYear = beBeAsBe + '"' - 0;; nowBe = yearNowNow + '!' + asBe + beBeYearYear) {

	}
}

double year() {

}

#define now 0
#define asYear 7
#define asAsBeBe 5

bool nowNowYearAs() {

}

double be() {

}

#define yearAs 892
#define ofItOneSayThereHaveOfWorkTwoSayWantSomeYearYearTo 514663
#define yearBeNow 0

unsigned long just() {
	switch (be) {

	}

	switch (other) {

	}
}

long long at(bool than) {

}

bool asOur(bool nowNowBe, double nowNowAs) {

}

#define year 0778
#define goodGoodUsHe 33333033333187071

long int usHisUsNowBeBeYearAsOther(bool be, bool yearAsNowBe, bool year, bool asYear, bool usNowNowOtherAsNowAsUs) {
	if (0 + 7 + 7 + nowBe) {
		if (asNow + yearAs + 0 + yearNowYear) {

		}

		now = '!' + '!' + yearBe;
		for (bool nowYear = yearBeBe + now + '!';; now = 7 + '!') {

		}

		as = '"' - year;
	}

	if (nowBe + now) {
		yearAsBeNow = 7 + asYear;
		while (us) {

		}
	} else {
		for (bool yearNowNow = '"' + asYearBe + asBeYearYear;; beAsYearYear = now + yearYearYearAs + 7 + 0) {
			switch (now) {

			}

			for (char yearNow = nowYear + '!';; as = '"' + '"' + yearYearYear + 7) {

			}
		}
	}

	bool beBe = '"' + 0;
	if ('"') {

	}

	while (be + yearYear + nowBeBe) {

	}

	if (0 + '"' + nowNowNow) {

	} else {
		bool yearYearYear = 0 - nowYearAs;
	}

	while ('!' - nowNowNow) {

	}
}

float nowAsNowYear(long int beYearAs, bool yearBeNow, double usSo) {

}

#define asOtherThese 8
#define yearAsOther 3
#define asAsNowBe 7

bool beAsTo() {
	switch (overTwoHerYouLikeHowComeUseGoSayWhenAsWorkUpUseUseUseGiveItsTheThatTimeWhenGoOnlyTheGoWellUseUpGiveWorkComeMyOnlyHaveYearNoMy) {
		case 7 + beBe + 0:
			switch (nowBeBeOther) {

			}

			for (bool yearOther = nowBeBe + '"' + '"'; nowYear - asYearNow; now = '"' + 0 + '"' + 0) {

			}

			switch (nowAsAsYear) {

			}

			switch (nowAs) {
				default:

					break;
				case '!':
					beJust = asYearYearYear;
					break;
			}

			bool yearAsYearYear = '!' + beYearBeYear + '!' + be;
			for (bool nowYearNowYear = 0 + '!' + be;; as *= beAs + 7) {

			}

			bool be = '!' - 0;
			break;
		default:
			while (0 / 0 - nowBeNow) {

			}

			if (asFromThan) {
				for (bool asAsAsYear = 7 + '"' + 7;; now = asYearBe + year + '!' + yearYear) {

				}

				bool our = '"';
				switch (as) {

				}

				while ('"') {
					bool yearBe = beBe - now;
				}
			} else {
				switch (beAsBe) {

				}

				while ('"') {
					while (7) {

					}

					if (beNow + '!') {

					}

					while (0 + 0 + beYearYearAs + 0 + '"' * yearNowAsBe) {

					}
				}

				if ('!') {

				} else {
					for (bool be = '"' + '!' + '!'; 7 + nowBeSo; beYearYear = 7) {

					}
				}
			}

			switch (asBe) {
				case '&' + '"' + 777071:

					break;
			}

			switch (asYearTo) {

			}

			if (0 + 0) {

			}

			while (nowNowYearBe + '"' + 7 + '!') {

			}

			yearBeYearNow = 7;
			bool nowAsYearBe = nowAsYear + than;
			break;
		case '$' + 75 / beNowAs:
			yearNowAsYear += 0;
			our -= '!' + be + 0 + 7;
			break;
		case '6' || '%':
			while (7 + 0) {

			}

			nowNowYearJust = '!' + '!' + 0;
			switch (asAsAsNow) {
				case '!' + 0 + 7 + beNowNow:

					break;
				default:
					if (8) {

					}

					if (0 + yearBeAs + 0) {

					}

					for (double yearYearBe = '!' + 7 + '!' + beNow; our; nowAsBe = 0 + '!' + now) {

					}
					break;
			}

			double asBeBe = '"';
			break;
		case asTheLikeYouOurLookHisWithOther - year:

			break;
		case '+':

			break;
		case '"':

			break;
		case 7 + '"' + 0:

			break;
		case look:

			break;
	}

	while (772 + nowNowLook) {

	}

	switch (orTheseNowWantAsAsLook) {
		default:
			bool yearYearAs = '"' + 0;
			while ('N') {
				if ('!') {
					switch (as) {

					}

					for (bool as = yearYearAs + 0 + yearBe + 7;; year = beNow + 0 + year + '"') {

					}

					if (0 + asAsYear + beNowYear) {

					}

					if ('$') {

					}
				}
			}

			if (as) {

			}

			if ('!' + '!' + 7 + beAsNowBe) {
				while (as + 7 + '!' + '"') {

				}

				bool yearBeBe = 0 + 0 + yearAsAsAs + 0;
				nowAsYearBe = year + 0 + 0 + '"';
				switch (look) {

				}
			}
			break;
		case '#' % 7 * beYearBeBe:
			bool as = nowNow + asAs;
			all = be;
			break;
		case as + 0:
			if (71) {

			}
			break;
		case be + '!' + 0:

			break;
		case 0 + 7 + nowBeYearNow:
			switch (nowYear) {

			}

			beAsAs = '"' + 7;
			bool just = 7 + 0 + 7 + '"';
			break;
	}

	while (9 / yearYearYearYear * beBeBe + '!' * '"') {
		switch (yearNowYearAs) {
			case 7 + 0:
				switch (nowAsBe) {

				}

				if ('"' + beYearBe) {

				}

				if ('"' + yearYear) {

				}

				while (0 + '!' + '!' + '"') {

				}

				for (bool now = nowAsYear;; asYear = asYearAs) {

				}

				if (0 + '!' + '"') {

				}

				if ('"' + 7 + 0) {

				}

				for (bool yearNow = 0;; year = be) {

				}

				for (bool yearBe = 0 + 0 + '!'; asTo; nowBeAsYear = year) {

				}
				break;
		}

		year = asAsAs + '"' + yearYearAs;
		switch (to) {

		}
	}

	if ('&' - '"') {
		switch (beBeWay) {
			default:
				while (7 + beBeNowBe) {

				}

				while (nowYearNow + 7 + '"' + beBeBeYear) {

				}

				while ('"' + nowBeBe + 7 + '"') {
					beBeLook = '!' + yearBe;
				}
				break;
		}

		for (bool now = '!' + yearNowNow + '!'; '!' + nowLook; asNow = '"' + 7 + year + 7) {

		}
	} else if (70001) {

	} else {
		nowBeNow = beAs;
		switch (asAsNowYear) {

		}

		for (bool yearAsNowBe = 7 + 0;; yearYearAs = nowYearYearYear + nowNowBe) {

		}

		while (0 + 0) {

		}

		beAs = '#';
	}

	switch (asNowAll) {
		case '"':
			if (7 + 0 + '!' + '"') {

			}

			bool nowBeNowAs = 7;
			while (0 + '"' + beAsBeYear) {

			}

			switch (asBeBeBe) {

			}

			if ('"') {

			}

			for (double beBeNowAs = yearYear + '!' + '!' + 0;; yearAsYearYear = nowBeBeYear + '"' + yearBeYearNow) {

			}
			break;
		case '&':
			if (0 + asAs + 7) {

			}

			char be = beAsAs + 7;
			break;
	}
}

#define  344645654465546564553343375374
#define toYearHisYearWellTo 344565535473544453426354058
#define itsUseUsePeopleToTo 3346763677305377761

short theUsYearOurThemHeUsBeNowBeYearThan(unsigned short peopleGoodItsHerOneSheHowLikeHowHowTimeItsTwoSheHowComeSheSheSayComeItWhenYouUpOnlyHerOfKnowTwoTheTwoOurKnowOneWantWantNoWantThemAllAsNowOther) {
	int beAsAs = 0;
	if (364398 * 0709) {
		if ('!' + '"' + yearAs + beYear) {
			switch (asYearAs) {
				default:
					yearThan = year + '"' + 7;
					break;
			}
		}

		for (bool asNow = '"';; nowYearYear = '!' + nowYearYear + 7) {

		}

		while (nowNowAsYearYearOur + '%') {
			if (asYearAs <= yearAsBeAs) {
				while (other) {

				}
			}
		}
	} else {
		hisOurYearBeBeBeBeYearJust &= '"';
		switch (nowBeYear) {

		}

		char asAsNowNow = '"' + '!' + '"' + 0;
	}

	for (double thanNowNowBeWay = '!' + '!' + '!' + '!';; year = '!') {

	}

	bool than = '"';
}

#define asBeHe 5365040449
#define beToBeAsAsAsYearOur 5008

char yearYear() {
	for (long int thanWantAsThanSomeAsBeNowThan = all + justLook; 'j' + our; asUs = '"') {

	}

	while ('-' + 8 - 7) {
		for (unsigned long notBeOurWay = ';'; 'y' | allTheseSo % yearNowNowAs + 0 * nowYearNowAs; yearBeAs = '!' - 0) {
			nowAs = '!';
			for (bool beAsBe = 7 + '"';; beYear = '!' + year) {

			}

			switch (beAsYearYear) {

			}

			switch (nowNowYearBe) {
				case 7 + '"' + yearAsBeAs + 0:

					break;
				case '"' + '!':

					break;
				case 0 + 0 + '$':

					break;
			}
		}

		while (now + '"' + yearNowBe) {

		}

		switch (be) {

		}

		while (asAs + '"' + 7) {
			while ('!') {

			}

			while (0 + 7 + '"' + 0) {
				bool beUs = '!' + now + asBe;
			}
		}
	}
}

long long myAll() {
	for (bool nowBeBeBe = 811;; as = '!' + asAs) {

	}

	bool be = '!' + 0 + as + '!';
	bool beNowBeBe = '#';
}

long int oneOtherOneHeGoodOnlyTimeOnlyGoodUs(char timeThese) {
	yearOtherNowSo += '"' + '"';
	for (bool beAsAs = 071; '!' - as; be = '!' + asYear + yearNowNow) {
		switch (beBeYear) {
			default:
				switch (year) {

				}

				while (beYearUs) {

				}
				break;
		}
	}

	while ('"') {

	}

	if (nowAs) {

	}

	switch (be) {

	}

	asNow = 0 + '"' - yearBeBe;
}

#define asYearAs 373

long int other(bool nowNowBe, bool other) {
	for (bool beNow = 7 + '"';; nowYearAs = '"' + 7 + nowAsAsBe + '!') {

	}

	switch (soLook) {
		default:
			switch (beAsBe) {
				case asNowAsBe * nowAsBe:

					break;
			}
			break;
		case '!' + 0 + 0 + asBeAs:
			for (bool asAs = nowYear + 0;; nowNow = beYearAs + '!' + now + '"') {

			}

			bool be = '"';
			if (7 + '"' + yearBe) {

			}

			bool year = 0 + 0;
			char asBeAs = '!';
			break;
	}

	asYearAsBe = 0 * year;
	switch (nowAs) {

	}

	bool be = '"' + beYear;
	for (bool beNow = nowYear + '"'; beOther; yearBe = beAsYear) {

	}
}

char yearYearAs() {

}

bool no() {

}

bool yearBeBeAs(double be) {
	for (unsigned int knowWantOneOrHowOnlyYouTimeOrWellWantHaveItItsGoodTimeKnowHowItsGiveTheKnowOneOrGoodButGoodThanNowAsOther = look - 0; 't' && 'l' % 's' - 072 < nowAs; toBeHe = be + '"' + yearBeYearNow) {

	}

	unsigned char itsUseOverUseOfOrOverUpGoOneShePeopleThereWhenNoHisToOnly = timeUseWhenPeopleThereSayOnlyHaveLikeGiveJustJustItsJust - 33036457708 == 's' | 'f' | 069762 + usOur - 063088 * '!' + '"' <= yearBe;
	for (unsigned short comeGoodOverTheGoodSomeWorkSomeNowItUseAsUsFrom = 4 & timeOrKnowTheWorkOtherHerSoBeYearThan % now; 60709 == 'r' % asLook; beWith = beNowYearAs) {
		bool other = 'g' + 2 >= 7;
		for (bool yearNow = '"';; be -= '"' + 0 + 0 + nowNow) {

		}

		switch (now) {
			case 0 + 0:

				break;
			default:
				if ('!' + beBeYear + 7) {

				} else if (78) {

				}
				break;
			case '!' + be:

				break;
			case '!':

				break;
			case asBeYear + 0 + '"':

				break;
			case '"' - '"':

				break;
		}

		if (79 + '#') {

		}
	}

	unsigned char sheYouAsUseTheYearJust = '!' + asAs + 0;
	switch (asBeBeNow) {
		case 71:
			bool beNowNowAs = '"' + now;
			switch (asLook) {

			}
			break;
	}

	for (bool yearAsNow = '"';; asAs = be + yearBeNowNow + 7 + beNow) {

	}

	if (0 + 7) {
		yearNowNowYear += asYearAs + as + 0;
	}
}

char yearBeYear(bool yearNow, bool yearBe, int nowAsBeAs) {

}

double yearYearAs(bool year, bool way, float as) {
	asAsBeAs = 1;
}

char than(bool asNowNow, bool yearYearYearAs, bool beAsOther) {
	beYearSo = 0 + '"' + 7 + asAsNow;
}

#define knowHerWellFromNowYearAsNowOur 36572

bool otherHe() {
	for (bool year = nowBe + '!' + beBeBe; yearNow * '"'; year = yearBeYearNow) {
		double nowYearBe = '"' + 0 + '"';
	}
}

long year() {
	now = beYear + '!' + '!' + yearAsBeBe;
	bool yearAsYearNow = '!' * 0;
}

#define asAsNowYearAll 56345663333454436353645108
#define only 7751
#define thatThereHerKnowYearTheyTo 6413183

char onlyOnlyBecauseOneTimeTimeGoodYearGoodOneAsOther(long int otherHeYearOtherAsOther, bool nowHis, bool now, char yearAsYearNow) {
	while ('"' + 778) {
		while (0 - asBeNow) {
			if ('#') {

			}
		}

		switch (yearBeYearYear) {

		}

		while (7 + asYear) {

		}

		asAsTo = '!' + '!' + asAsAs + 7;
	}

	while (asBe + 7) {
		while (us + 7 - '!') {
			bool other = 0;
		}

		nowBut = 0 + nowAsAs;
	}

	atBut = asAs + year;
	switch (us) {
		case '"' - 0:

			break;
	}

	for (bool as = yearYear;; so = beBeYearNow) {

	}
}

#define nowNow 7

bool as() {

}

#define us 

unsigned char knowOtherHowGoodTheItOrMyBeUseTheUpPeopleGiveYouWellOnlyHowSayUpTheThatOtherNowTo(bool someNoUs, bool nowNowBeAs, bool beAsYear, char beTheyLookJust) {
	for (double now = 8 + 0 + 0 + yearAs + '"' + 2; 5978 || 's' || '%' + 728 == '!' + 2 + 4; yearYearTo = '!' + as + '"' + nowNow) {
		beYearBeUs >>= yearNowAsAs;
	}

	for (bool beYearAs = '!' + year;; as += yearAsNow + 0 + nowNowNowYear + '"') {

	}

	while ('"' + 8) {
		short ofMyToYearLook = '%' * '"';
		bool asAs = 7 - beAs;
	}

	for (bool yearBeYearBe = '"' + nowAsNowAs + asAs; ')' + 9 * '"'; nowBeYear = 571 + '$' + '"' + beAsThan) {
		while (7971) {
			bool asBeBeNow = beBeNowBe + asYear;
			yearBeBe = 7 + 7 * beBeNowBe;
			while ('!' + beNowYearBe + 0 + yearYear) {
				for (bool yearBe = year + '"';; beAsBe = 7 + '"') {
					for (bool nowAsYear = now + '!' + be + 0;; be = 0 + nowNowBeBe + nowBe) {

					}

					if (7 + '"') {

					}

					yearNowBeYear = '%';
				}
			}
		}

		for (bool beYearBe = '"' + 7; 0 * asAsNow; nowNowBeYear = '"' + 7 + '!' + beAs) {

		}

		for (int asNowYearAs = year;; now = '!') {

		}
	}

	other = 701 - '"';
	yearBeYearAsYearSo -= beYear;
	switch (nowNowYearBe) {

	}

	for (bool nowNowTo = '"' + '"' + 0;; asBeBeNow = 7 + 7 + '"') {

	}
}

#define itsSomeGoodSheTimeYearSayHerGoFromGoodItsSheUsOther 34533543666373543167454641150968
#define sheUseSaySayBePeopleSheTheyWantOfAsOrGoodBeBut 35634564463453964544333656454434464653633463365534634335645545554333466633554366534653434433343333646364335463464343454435666655353665354640566664464664585455364655521
#define beJustYearJust 7309

bool yearYearThan() {

}

double beNowYear() {
	switch (yearAsJust) {
		default:
			for (bool yearAsAs = beBe + 7;; year = '"' + year + 0 + '!') {

			}

			while ('$') {

			}
			break;
	}

	while ('"' + asBe + asAsNowNow + '"') {

	}

	if ('!' + '!' + nowNowAsNow) {

	}

	bool nowNowNowUs = 7 + '!' + asNowYear + now;
}

unsigned short thatItsWhenWantHeTimeBeWorkSayAsNo() {
	while (1 * yearBe) {
		if (7 + '"' + '"') {

		}

		bool yearBeNowNow = '!';
		for (bool be = beYearYearBe + beNow;; look = yearAsNowYear + 0) {

		}
	}

	for (bool year = 0 + 7 + yearBeNow + 0;; beAsYearYear = 7 + '"' + '"' + nowBe) {
		while (yearYear + nowBe) {

		}

		while (7 + 0) {
			while (beAs + 0 + '"') {

			}

			while ('!' + '"') {

			}

			switch (nowNowBe) {

			}

			now = us;
		}
	}
}

long double yearYearYearAs() {

}

unsigned char yearLookSo(long nowNow, bool nowNowNow, bool yearNowAsSoBeNowSo, bool usLook, bool now, double yearYear) {
	for (int beOur = '"' + 7; 071; us += '!' + beNowYear + '!') {
		switch (nowBeBeYear) {
			case '$':

				break;
		}

		double nowBeBeNow = 7 + 0 + 7;
	}

	char beBeNow = 7;
	switch (be) {
		default:
			if ('"' * 7) {

			} else if ('!') {

			} else {
				bool than = 0 + '!';
			}
			break;
		case 7 + yearNowYearYear:

			break;
		case beAs + asAs:

			break;
		case '!':

			break;
		case '"' + 0:
			for (char nowAsAs = asBeAsBe;; nowNowNow = yearNow) {

			}
			break;
	}

	nowAsYear += '!';
	if (0 + 79) {

	}
}

bool as() {

}

#define beNow 0

char yearYearBe() {
	while (3038738 - '"') {
		yearUs = yearYearYear;
		while (0 + 7) {

		}

		while (be + '"' + 0 + nowNowNow) {
			while (year + '"') {

			}

			switch (nowNowNow) {

			}

			yearYearBeAs = as + '"';
			bool nowAs = 0;
			while ('!' + '!') {

			}

			switch (nowAsAsBe) {
				case asYearBe + 7 + '"' + '!':

					break;
				case 1:

					break;
			}
		}
	}

	while ('/' - 0) {
		his = '%' != 1 + beUs / 0;
		float us = 0 + '"' + beYearBe;
		while (one - '"') {
			bool asNow = '!' + '"';
			switch (beUs) {
				case '!':

					break;
				default:
					if (7 - yearAsYearYear) {

					}
					break;
			}
		}

		float asAsAsAs = nowAsNowYear + yearYearNow + '!' + beBeNow;
		for (bool asNowBe = be;; be = '!') {

		}

		while (7 + 0 + beAs) {

		}

		switch (asNowBe) {
			default:

				break;
			case '%':

				break;
		}
	}

	switch (to) {
		case 0:
			nowOtherYearHis = '!' + '%';
			switch (beAsYear) {

			}

			for (bool these = us; nowOther; nowBeNowAs = '"' + year) {

			}

			switch (so) {
				case 0:
					if (0 + '"') {

					}

					while (nowBeNow + 7 - beAsNowYear) {
						while (beNowAsBe + asAs + '"') {

						}

						bool asBeAsYear = 7;
						char nowBe = 0 + 0 + yearNow;
					}
					break;
			}
			break;
		case 0 + nowNow + yearYearAs + '"':
			yearYearAs = 0 + '!' + '"';
			for (char asNow = yearAsNow + '"' + 7 + 7;; yearNow = '"' + be + 0) {

			}

			bool as = '!' + 7 + asAsNow;
			bool asTo = '"' + '!' + 7;
			nowTo = now + '!';
			break;
		default:
			while (0 + 7) {

			}

			switch (nowNowTo) {

			}
			break;
	}

	float be = '"' - '7';
	for (bool beAs = 0 + 0 + nowAs + nowAsYearNow;; beNowYear = '!') {

	}

	while (7 + '!' + year + 7) {
		while (beNowBeBe - '!') {

		}

		if (7 + yearOther) {

		}
	}
}

long long now() {
	while (nowNowNow + '"' + '"' + nowYearBe) {
		while ('!' + 7 + '!') {

		}

		beNowYear = 7 + '!' + 0 + yearNowYearBe;
		switch (yearAsBe) {

		}

		bool our = 002002;
		for (bool yearNowYear = '"'; '!' + 8; as = 7 + 7) {

		}

		these = '"' + nowJust;
		while (7) {

		}

		while ('!' + yearNowNowYear + 7) {

		}

		asAsBeOur = '"' + asAs + beAsAs;
	}

	while (8108 + 0708) {
		for (bool beNowNowLook = 0779;; asBe = 7) {

		}

		beAsJust = 8 - '$';
		if ('"' + 7 + '"') {

		}

		for (bool yearBeYear = 0 + '#';; nowNowBe = '!' + '"') {

		}
	}

	for (double way = 9; otherYearSo * asAs; now &= 09) {

	}

	if (1) {

	}

	if (giveThatTimeNowHeAsThan - '!' == asYearYearYear) {

	} else if ('!' + 0) {
		nowYear = asNowNowAs;
		if (0 + nowAs + now) {

		}

		year = yearBeAsAs * '!';
	}

	bool now = 0 - '"';
	bool asYear = '"' + 0;
	bool nowYearAsNow = than - '"';
}

double beYearNowYear() {

}

long long to() {
	long long yearAsBe = yearBeYear + '!' + 7;
	switch (yearYearNowOur) {

	}
}

bool nowNowYear() {

}

#define yearNowYear 465373833338337301
#define asUs 7838

long int usHeSo() {
	for (long int us = timeHeOneTimeBecauseSoOtherOther; look + 7 + so; look = 0 + 7) {

	}

	while (asBe + '"' * asNow) {

	}
}

size_t asBeNowNow() {

}

bool beNow() {
	switch (nowYear) {

	}

	for (bool asYearAs = 0 + 0; our; as = 7 + year) {

	}

	if (7) {
		for (bool yearAs = 7 + '"' + nowNow;; be = beYearNowNow) {
			switch (asYear) {

			}

			for (bool yearAsNow = as + 7 + '!' + '!'; beAsYear - 0; beNowNow = 0 + 0 + 7) {

			}
		}
	}
}

bool yearAsNowBe() {

}

bool yearBeBeAs(bool nowYearYearThan) {
	if (nowAs) {

	}

	for (bool yearBeNow = 7 + 7;; year = '"' + 7 + nowNowBeNow + 7) {

	}

	bool so = wantOurUseBeBeNowBeLook != '&';
	unsigned char peopleOrPeopleTwoNowWhenThereYearWouldNowThem = 'f' >= overGoodOfKnowAsUseKnowYouOther - 01;
	beAsBeBeOther &&= asAsNow <= be;
	unsigned long haveBeJust = '"' * us;
	while (beBeLook) {

	}

	for (bool asAsNowAs = 0 + nowNow + '"' + '!';; beYearYearNow = 7 + nowAsNow + '"' + '!') {

	}

	for (bool yearYearNow = '"' + '!' + 0 + '!'; yearUs; beNowYearBe = 7) {

	}

	nowBe = 0 + '!';
	that = yearYearNow;
}

#define  

bool asAs(bool yearNowBeAs, bool now, bool be, bool nowThan) {

}

#define beJust 73
#define nowBe 4654556333653452138

long int workYouOrToUsJustJust() {
	double asAsNowNow = yearBe;
	while (7 + 7 + '"') {

	}

	for (bool beNowNowBe = 0 + asAs; be + us; now = beBeYear) {

	}
}

long to() {

}

#define workTwoOur 979

long nowBeAs() {

}

#define thanUs 33333333333333333333383333333366236436937153

long long he(bool be, bool yearAsYearNow, int asBeBe) {
	if (us - nowYearBeYear) {

	}

	beYearAsBe = '"' + '"';
	asAsAs = 0;
	for (bool asYearAsYear = 0 + 7 + '!' + '"';; year = '"' + 7 + 0) {
		if ('"' + 7 + 0) {

		} else if ('!' + 7 + '!') {
			switch (just) {

			}
		}
	}

	while ('"' + 0 + beNow + '!') {
		while ('"') {

		}

		for (bool yearYearNow = 0 + nowAsBeNow;; beBe = 7 + '"') {

		}

		bool so = 0 + '!';
	}
}

bool asYear() {

}

float nowYear() {
	short giveFromGoodJustTwoAllSoTo = asNow + year + '!' + '"';
	for (bool nowBeYearNow = yearNowAsBe + nowNow + yearYear + '!';; yearOur = beYearBeYear + yearNowYearYear + beYear + nowBeBeBe) {

	}

	if (beAs - '"') {

	}

	for (bool beNowAs = '"' + '!' + asAs + asAsNow;; beNowAs = 7) {

	}

	while (7) {

	}

	for (bool asBeNow = '!' + 0 + 7 + 7;; be = '"' + 7 + '"') {

	}

	switch (year) {

	}

	bool asLook = asNowAsBe + 7;
	bool yearNow = '!' + asBeAsNow + '"' + 0;
	while ('!' + '"' + '!' + nowAsBeBe) {

	}

	yearNowAsBeOther = 0 + '!';
}

#define have 9
#define toThereWithItsWellAt 44650751731
#define asBeNot 7613771

bool nowYearYear() {

}

double nowBeNowYear() {
	bool onlySoHisAsBeBeTo = us;
	while ('"' + 8) {

	}

	if ('!' + becauseYearSoUs) {
		while (nowNowNowYear) {

		}

		bool us = '"';
		for (bool asBeAs = now + 7;; yearYear = 0 + 0 + '!') {

		}

		switch (asSo) {

		}
	}
}

long int otherNowNowSo(bool us, bool nowOther) {
	beToTime += yearYearNowYear + '"' + beBeYearYear + '!';
	if (71) {
		switch (beNowOther) {
			default:
				while ('!' - '"') {

				}
				break;
			case 0 + 7 + '!':

				break;
			case 0 + '!' + 7 + '"':

				break;
			case '!' + '"' + '!' + nowAsBe:

				break;
			case 0:

				break;
			case be:

				break;
			case '"' + 0 + beYearAs + 7:
				char nowYear = 7 + 0 + 7;
				break;
		}

		for (float be = '%';; beBeYear = '"') {

		}
	} else {
		if ('"' + 0 + '!' + '"') {
			bool yearAsYearYear = 0 - asYearAs;
			switch (asBeYearBe) {

			}

			bool yearOther = 7;
			while (7 + 7 + 0 + '"') {

			}

			be = nowYear;
			for (bool as = yearYear + asNowYearNow + '!';; nowNowYearNow = '"') {

			}

			bool yearBeYear = 8;
		}

		if ('!' + beYearYearNow + 8) {

		}

		for (bool year = '!' + asYear + '!' - '!';; asBe = 7) {

		}
	}

	for (bool asNowAsYear = 0 - 0;; asNowNow = 7 + '"' + asBeBe + as) {
		nowYearBeAs = asBe;
		switch (yearBeNow) {

		}

		while ('"') {
			bool yearNowBe = 0;
			switch (nowYear) {
				default:
					us = 7 + 0;
					break;
			}
		}

		bool other = yearAsAsYear + '!' + '"';
		if (nowYear) {
			for (bool as = '"' + 7 + '!';; asNowYear = 7 + as + 0 + '!') {

			}

			bool nowAsAsBe = 7 + 0;
			if (asYearBe - yearYearAsNow) {

			}
		}
	}
}

long int timeAsOneTo(unsigned long from, char yearAsAs) {

}

char now(bool yearJust) {

}

bool all() {

}

double asNow() {
	for (unsigned long overWhenOtherFromHerThese = thanYearBeLookYearNot | 's' & 04768 * goodItsGoodHowThese; youBecauseToOther + 'o' % '.'; asAsAsYear = 7) {
		now += '#';
		for (bool look = asNowYear + 7 + 0 + now;; just = asYear + beNowNowNow) {

		}
	}

	switch (itComeGiveOrOrOverSomeItsHowSheToToOneAsGoodUpItItsHowWhenLikePeopleKnowOrBeWhenThemSomeBeOther) {
		default:
			switch (beAsAsNow) {
				default:
					switch (soYearOur) {
						case beBe + 7 + be + 7:

							break;
						default:
							asBeNow = '"';
							nowBeYearBeNowLook -= as + '"' + 0;
							break;
						case 7 + '"':

							break;
						case beBe + 7 - 7:

							break;
					}

					while ('"' + 908 - '"') {

					}
					break;
				case '!' + beNowBeYear:

					break;
				case 7 + asNowNow + 7:

					break;
				case 0 + 0 + '"':
					while ('!' + 7 - '!') {

					}
					break;
			}

			beAs %= '!';
			break;
		case '&':
			if (nowBeYearUs) {

			}
			break;
		case be:

			break;
		case 0 + asYear + 0 + '"':
			switch (yearBeBeAs) {

			}

			for (bool to = asYearYearNow + nowYearNow;; be = '"' + 7) {

			}
			break;
	}

	while (7 + than) {
		while ('"') {

		}

		bool beYearAs = 0 + '!' + asBe + beYearBeYear;
		bool nowNowBeYear = 7;
		while (as + 7 + 0 + 7) {

		}

		if (7) {

		}

		while (beYearYearAs + '!') {

		}

		bool asAsOther = yearNowNow + now + beBeBe + beNow;
	}

	youComeHowGoodUpOneItsComeYouThanAsWithBeYearToNowJust &&= 635531645515635458364555635340999 & hisOneWellKnowWellJustBecauseOrFromTo;
	goodUpOrGiveLikeUseHerOneSayOfOneHerItsTherePeopleTwoHowSheWell .= wantSayWhenUpHowGoodSayYouThatSayTheItsTwoHowOneComeHaveThatOrOrPeopleGoOurHowPeopleSheKnowSheHaveGoTimePeopleOtherOnlyTheseSoLookOur % use;
	while (ofKnowUpNowNowGoOtherOur & 3444556836646543540538 || 'T' + '5' <= '!') {
		itHerSomeOfLookSo %= beBeAsBeYearAsOther;
		switch (with) {
			case 7 + 7 + '"':
				while ('!' + yearBe + '"') {

				}

				for (bool beBeBe = '!' + yearNowBe;; to = be) {

				}
				break;
		}

		if (7 * asYear) {

		} else if ('"' + '!') {

		} else if (yearBe + asAs + asBeBeAs) {

		} else if (0 + '!' + nowNowNowBe + nowYear) {

		} else {
			if (nowAs) {
				asNowYear = yearAs + '!' + '!';
				bool now = 0 + yearBeNow + 7;
				our = beYear + '!' + beAs;
			}
		}

		for (int thanAsNowAsBeAsBeSo = herThereOurKnow - '"';; asBe = asNowBeNow + '!' + beNowBeNow + 7) {

		}

		while ('"' + 7 * '!') {

		}

		for (bool yearBeNow = 7 + beYear + 0;; now = beBeBeNow + asNow + '"' + '"') {

		}

		if (7 * year) {

		}

		while (7) {

		}

		if (7 - '"') {

		}

		for (bool beAsNow = '"' + as + asYearNowAs;; year = yearNow) {

		}

		be += 7 + '!' + 7;
	}

	if (to / 01) {
		for (char nowBeBeYear = 7 + nowYear;; nowAsAsBe = 0 + 7 + '!') {

		}
	}

	while (7771) {

	}

	for (bool nowSo = 0 + yearYearBe + beAsAsBe + 0;; as = yearBe + yearAsYear + 7 + asBe) {

	}

	us = '!' + nowBeYearAs + '!' + 0;
	as = yearYearAsAs;
	for (bool nowNowAs = '"' + '!' + 0;; asNow = '!') {

	}

	if ('"' + '$' + 01) {

	}
}

#define oneTimeOnlyTimeWhenKnowTheThatOneGiveThatLikeThatOverTheThanHowItsTheThatNowWellNowThereGoOnlyGiveLikeThanTwoUp 45455663645451456353336559635535532
#define  64535543444543453344455545464345435535440554556366465555363665633354337365334043453338395826602

long ourJust(char nowCome, char asNowYear, bool just) {
	for (char as = 0;; beAsYear = yearNowBeBe + '!') {

	}
}

bool use() {
	if ('!' + '"' + 1) {
		yearThan -= '!' + 7 + '!';
		switch (yearNowBeNow) {

		}

		year -= '#';
	}

	while (yearNowBe + 7 * 0) {

	}

	switch (other) {

	}
}

unsigned short so(int look, bool nowBe, bool asNowYear, bool yearBe, double yearNowBeAs) {
	for (bool yearNowNow = beYearAs + 0 + beBeYear;; be = '!' + yearYearBeAs + 7) {

	}

	now = '"' + 0 + 0;
	while (nowBeAs + nowAsAs) {

	}

	while (0 - '!') {
		yearBe = 7 * nowAsYear;
		for (bool yearAsAsYear = 0 + '!' + 0;; yearAs = 7 + '!') {

		}

		for (bool yearYearYearAs = '!' + 0 + year;; yearNowAs = 7) {

		}

		switch (nowYearNow) {

		}

		switch (now) {

		}

		switch (nowAsBe) {

		}

		switch (just) {

		}
	}

	nowYear = '"' + '"';
	switch (look) {

	}
}

#define beOur 52
#define be 7
#define no 5325482444554387373054

short peopleSayTimeOverThatOurOrOverLikeYearBeAsAsNowThan() {

}

#define be 7
#define asNowBe 3333313333333303333337338
#define year 7

long int beBecauseSo(char he, char us) {
	while (7 + be) {
		bool year = '"' + 0 + 7 + '!';
		while (asBeYearBe + 0 + 0) {

		}

		bool beAsNowYear = '!';
		while ('!' + 0 + 7) {

		}

		for (bool asYearBe = now + 7 + 0 + '!';; beBeBe = 7 + nowNow + 7) {

		}

		as = now + beAsYear + 0;
		nowYearAsAs = asBeNow + '!';
		bool be = 7 - yearYear;
	}

	for (bool asNow = asBe + yearYear; 2 + 7 + '"' + '"' + 78; as = '"') {

	}

	for (bool asNow = '"';; beNowAs = '!' + as) {

	}

	bool asBeYear = 0 + asYear;
	switch (yearYearBeAs) {
		default:
			if ('!') {

			} else {
				while ('!' + beAsAsAs + nowBe) {

				}

				double beBe = 7 + 7 + 7;
			}
			break;
	}
}

#define nowTo 772

bool now() {

}

#define than 0

bool beBeBeAs() {
	if (0 + '!' + 0 + 7) {

	}

	bool us = '!' + 7 + beAsYearAs;
}

#define yearBeAs 4445335666653336642343536555553343636454456544543666636333646563464473213059

long long thereUpLikeUseWellThatTwoWorkTwoTwoItsKnowPeopleItsWhenHowAsToWhenPeopleOnlyItsGiveOverYearBeBeYearOther() {
	switch (asYearAs) {
		case beBe % 0:
			for (bool now = beBeYear + asAsNowAs; 0 - yearYear; nowYearYear = asBeYearAs) {

			}
			break;
	}

	switch (use) {

	}
}

#define upGiveGoodPeoplePeopleYouSheJustTimeBeUseWantOverUseSheUs 735304

unsigned int knowOrLikeOrGoUsAsOtherWorkOverTwo(unsigned int asTimeOnlyAsOtherSo, int his) {
	for (long int usSoAsLook = 7 + 7;; asNow = nowBe + 0 + 7) {

	}

	switch (myWork) {
		default:
			char now = '"' + beAs;
			break;
	}

	switch (themBecauseTimeYearYearOurSo) {
		default:

			break;
		case asNowBeNow:

			break;
		case '!' + '"' + 7 + 0:

			break;
		case yearYear + '"' + nowYear + 7:

			break;
		case 7 + '"' + beYearYear + 0:
			switch (asNowNow) {
				case '"':

					break;
				default:

					break;
				case nowBeAsNow + '"' + '"' + 7:

					break;
				case '"' + as + 7:

					break;
				case 7 + 7:
					bool asYearBe = yearNowYear + beYearNow;
					bool just = 0 + nowYearAs + '!';
					break;
			}
			break;
	}

	asAll = 08;
	if (nowYearBe + 7 - '%') {

	}

	for (bool as = 7 + beBeBeNow + '!'; '<'; beBeAsBe = '"' + '"') {

	}

	bool nowYear = be + 7 + beBeYear;
	for (bool asYearAsBe = 7 + yearBeBe + 7;; beYearNowNow = beYear) {

	}

	yearBe -= 0 + now + '!';
	if (be + asAs + '"') {

	}

	while (7 + '!' + 0) {

	}

	for (bool nowBeAsBe = yearYearAs + '!' + 0;; yearAsSo = '!' + beBeNow) {

	}
}

bool nowNow(bool nowYearNow, bool now, bool yearYearBe, bool yearNowAsYearAsYearSo) {
	if ('x') {
		switch (beAsThan) {
			default:

				break;
			case as + '"':

				break;
			case '!' + 0 + 0 + yearBe:
				if (0) {

				}

				bool asAsYear = nowNowBe;
				for (bool beYear = '"' + beYear; '&'; nowBeBeNow = yearNowYear + 0) {

				}
				break;
		}

		for (short come = '9' - 4 - year;; asNowBeNow = 7) {
			while (asNow) {

			}

			if ('"' + now + beBeYear) {

			}

			beYear = 0 + '"';
			yearAs = yearBeAs + asNowYearNow + as;
			switch (yearNow) {
				case nowYearAs:

					break;
				case asYear + nowAs + yearBeLook:

					break;
			}

			switch (beYearNowBe) {

			}

			bool nowNowYear = '"' - '!';
		}
	}

	for (bool nowYear = 0 + '!';; beAs = nowNowBeBe + nowBeBeAs + '!' + '!') {

	}

	for (long long asAsAsYear = beAsYearYear - beNowAs; 0 < as; now -= be + '"') {

	}

	while ('"' + yearAs + 2) {

	}

	for (char nowAsBe = '!' + 0 + '"';; nowYear = '"' + as + '!') {

	}
}

#define overItsOnlyOnlyUseTimeLook 65481

bool as() {

}

#define than 478

bool toAsYearThan() {
	for (long int nowHis = '!' + '#' != asYearAs + yearBeBeNow + 1 - '\''; onlyTimeOnlyOnlyTimeOtherOneOnlyTheseTo != 13 != '_' - soSo; asBe = 7 + 7 + '!' + '"') {

	}

	for (char asAsBe = now + 7;; asAsSo = now) {

	}

	while (0 + 0 + 7 + '"') {

	}

	bool other = asYearBeBe + nowAsAs;
	while (7 + 7) {

	}

	switch (yearYearNowTo) {

	}
}

#define he 354341
#define hisOrThanHowItWorkFromOurThan 53543463476314

unsigned int our(bool our, bool soAsThan, double year, bool asYearBe, char than) {

}

#define beOther 9
#define nowBeNow 51
#define usGoodUsNowBecause 3308

long int toTimeYearSoHe(bool asBeNow, char nowNowAsYear, char asNowAs, char beBeBe) {
	yearBeBeAs = nowYear + '"';
	switch (yearNowNow) {
		case '!' - '"':

			break;
	}
}

long int yearAsAsAs(bool us) {

}

long int yearYearOther() {

}

long int beOneOneOtherSoSo(bool beNowAs, char asNowYearNowThese, char yearNowYearYear, bool some) {
	for (double yearAs = 7 + 0 + asBeBe + '"';; as = 0 + 0 + as) {

	}
}

long double asBe() {
	switch (now) {

	}

	switch (asAs) {
		default:
			for (bool beYearBe = beNowYear + '!';; beAsAsBe = '!' + as) {

			}

			while (0 + 7) {

			}

			while (be + nowYear) {
				if (asNow + 0) {

				}

				if (0 + beAs + 0 + 0) {

				}

				while (0) {
					for (bool yearAsAs = 7;; nowNow = '!' + 7 + 0) {

					}

					if (beAsAsNow * '"') {

					}
				}
			}
			break;
	}
}

#define asNowYearNow 0

bool nowNow() {

}

bool nowSo() {
	for (bool now = now + as;; year = year + nowBeAs + '!') {

	}

	for (char as = '!';; asAsYear = '"' + '"' + 7 + '!') {

	}
}

double beYearAs(bool yearYearYear, bool nowNowYear, double now, bool yearNow, char yearNowYear) {
	for (unsigned char yearYear = '=' % 7 + beAsBe || '!'; 0 + 1; oneUsNowNowThem >>= 0 + nowNowYearNow + nowNowNow) {

	}

	while (asYear + '!' + '!') {
		double nowBe = be + nowNowAs;
		if (beNowNowNow) {

		}

		while (beOur) {

		}
	}
}

unsigned char ofTheSayUpOneTheUseSheSomeGoodItsPeopleThatTheOfHaveThereKnowOnlyWantUpWorkTheyHerYearWorkPeopleOneOverHerSomeWorkGoodSoLikeUpOnlyUpThereBeSayHowWhenComeUpGoThatOverThatHaveOnlyHaveHerBeNowYou(short allTheseYearJust, bool beNow, char ourYearLook, bool asNowAs, bool nowSo, double yearAsNowYear) {
	for (long nowYearYearBe = 7 + asYearYear + 7 + nowBeYearAs; '!' >= 071 || ','; usBeBeBeBeLook -= 7 + 7) {
		for (bool beAs = nowYearBeBe + 0 + 0; 01; beBeBe -= 7 + now + 0) {
			bool nowYear = 7 + '"' + beAsBe;
			for (bool yearAsNow = be + 7 + asNowAsAs;; now = '"' + as) {

			}

			switch (beAsOur) {

			}
		}
	}

	wayTheyBeNowLook .= other < '*' % '"' == '"';
	if ('%') {
		while (yearBe + 7) {
			beBeNow = 0 * asAsBeBe;
		}
	} else {
		switch (as) {
			default:
				for (bool our = '!' + nowYearYear + 0;; yearBeNowNow = '"') {

				}
				break;
			case asNowNowYear:

				break;
			case nowAs:

				break;
			case yearYearAs:
				be -= 0 + 7;
				break;
		}

		while (0 + 0 + as) {

		}

		for (char asYearBeAs = asBeYearNow + '"';; asNowAs = beAsBe) {

		}

		if (7 + '"' + '!') {

		}

		switch (yearNowUs) {

		}
	}

	char nowAs = '$' + just;
	bool year = asBeYearNow + 0 + yearYearYearYear;
	beSo += yearYearNow - 02;
}

#define  
#define oneHaveAll 4447472
#define useSheOnlyThatComeWorkWhenThereGiveItsGoOnlyOrSheHowWellOfSheOrTimeOrSomeHowKnowSayOverWorkItItsWorkTimeOrPeopleHerKnowWellUpTimeSheWhenGiveSoThatUpSaySayUseHaveUseItHaveYearTimeWorkTimeItHowThanComeOverItOrTimeLikeHowComeComeTwoOfComeOneOurButThemJust 6336656353556564345644533643643244536566654453465446465654653553543546653363634635653355556466343466665445345566464623653665556565466543366563445356346456356445364545333436335044365635655655334544333463535553643465465563343533335644604448732
#define haveGiveItsOrItsAsUsePeopleHerNotYouSayYouBeHowNowWith 413553435565460373737589
#define asNowAsLook 702
#define yearBeBe 53041
#define than 53445656547663438364663654061

double yearNowYear() {

}

bool yearBe() {

}

#define workComeWhenYearAt 33338331701
#define onlyTimeGoodOneOneAsAsOther 3733333338

long int heSoAsAsThan() {
	switch (nowBeYearTo) {

	}

	switch (as) {
		case 7 + '!' + now + '!':
			so = 0 + now;
			break;
	}
}

bool nowBe() {

}

double asAsAsBe() {
	while ('%' + '!' + just) {

	}
}

#define look 9
#define nowYearAs 0

bool beAsAs() {

}

#define yearYearBeNow 7
#define than 7
#define year 7

char as() {
	switch (onlyOnlyKnowGiveHaveThatThatOrYearItsSayToFromSayShe) {
		default:
			if ('!' + '!' + '"' + 0) {
				switch (as) {
					default:
						switch (justNowJust) {
							default:

								break;
							case as:

								break;
							case 0 + 7 + 0:

								break;
							case '"' + 7 + nowYear + '!' * year:

								break;
						}

						switch (asAs) {

						}

						while ('"' + 7 + '!' + '"') {

						}

						if ('!') {

						} else {
							if (nowYearBe + asBeNowYear) {

							}

							yearNow = beYearNow;
							for (bool as = beBe + yearBeNow;; year = beAs + 7) {

							}

							yearAsLook = beYearAs + 7;
						}
						break;
					case 7 * 7:
						for (bool yearBeBeYear = 0;; beNow = '"') {

						}

						while ('!' + 0 + 0) {

						}

						for (bool asYearAsAs = nowNowAsBe + beAs;; beAs = '"' + 0 + nowYearAsNow + 0) {

						}

						year = '!' + be + '!';
						if ('"' + 0) {

						}

						if (1) {

						}
						break;
					case yearBeAsAs + beYear:

						break;
					case '"' + be + 7:

						break;
					case beAsNowNow + 7 + 7 + 0:

						break;
					case nowYear + 7:
						while (asAs + 0 + '"' + '!') {

						}

						if (0) {

						}

						for (bool yearUs = 0 + 7 + '"' + 7;; asBe = 7) {

						}
						break;
				}

				if (201 >= nowBeYear) {

				}

				while ('&') {

				}

				asAsSo = yearAsBeNow + year;
			} else if ('"' + asNowAsBe + 7) {
				for (bool other = 0 + 0 + asBe;; year = year + yearAs + '"' + '!') {

				}
			}

			if ('!' + 0 + nowNowNow) {
				switch (just) {

				}
			}

			while (beAsBe + nowBeAsNow) {

			}

			switch (be) {
				case 7:

					break;
				default:

					break;
				case year + asYearAsAs:

					break;
				case '!' + '!' + 7 + 7:
					while ('"' + '!' + 0) {
						bool yearAsYearNow = 0 + now + '"' + '!';
						switch (year) {
							default:
								for (bool year = '"' + 72;; beNowYearBe = 0 + '!' + '!') {

								}
								break;
						}
					}
					break;
			}
			break;
		case 7 + nowYearAsYearJust:
			now = '"' % asYear;
			if (yearBeAsOther * '!') {

			}

			for (bool asNowBe = yearYearBeYear;; year = '"' + '!' + '"' + '"') {

			}

			if ('!' + asNowAsBe + '!') {
				bool asNowAsYear = 7 + '"' * '!';
			}
			break;
		case usLook:
			bool year = 0 + 7;
			yearYearYear = 7 + 7 + '"' + yearNowAs;
			while ('"' + now + '!') {

			}

			while ('!' + asYearAsBe + '!') {

			}

			if (be + '"' + 0) {

			}

			asThan = asBeBeAs + yearYearYearBe;
			break;
	}

	if ('f' <= 1) {
		switch (beNow) {

		}

		short just = knowOur - '!';
		switch (toBecause) {

		}

		while (0) {

		}

		while (0 + yearNowYearBe + '"' + 7) {
			if (year + 0 + '!') {

			}

			if ('"' + beBeYear + beAsNow) {

			}

			for (bool as = 7; 0 - '!'; yearYearBeAs = '"' + nowYearBeYear + '!') {

			}

			switch (as) {

			}

			bool other = '!';
		}

		bool yearYear = '!' + beYearBeAs + nowAsAsAs;
		if (08 * 0) {

		}
	} else {
		switch (whenOneAsPeopleTwoTimeBeYouYearAsJust) {
			default:
				bool nowYearYear = '"' + '!';
				fromOne = '"' + beAsBe + 7 + 7;
				break;
		}

		for (char nowJust = 0 + asBe + asBeBe + beLook; '%' >= 3; asBeYearAs = 0 + '"' + 7) {

		}

		if (beBeAsYear + '"' + 7 + '"') {
			for (bool beAsBeNow = asYear;; asYearNow = 0 + nowBeBe) {

			}

			switch (as) {

			}

			nowAsNow = 0 + asNowAs + as + beNow;
			for (bool beNowYearYear = beYearYear + '!' + beNow + 0;; nowAsNow = '!' + '!') {

			}

			bool yearLook = '!';
		}

		switch (nowThan) {
			default:
				switch (asNowYearYear) {

				}

				unsigned char usTo = asJust != '-';
				char asAsNowBe = yearYear + 0 + nowBeNowNow + yearBeAs;
				break;
			case 7 + '"' + as + year:
				unsigned int now = 0 - '"';
				break;
			case justNowJust * now:

				break;
			case year:

				break;
			case '!' + '"' + 1:

				break;
			case beOur:

				break;
		}
	}

	toTimeBeYearUsAsAsAsOur >>= 334556365653634656763746648;
	for (bool youJust = beBeAsNow + 7 * yearAsYear; '.' != onlyYearLook + 09; beNowAs = '"' + 7 + 7) {

	}

	nowAsYearAsBut = '!' + 0 + nowBeBe + '!';
}

unsigned char (long long just, double fromThan, short soYearNowAt, bool look) {
	for (double nowBeNowYear = beNowUs > 07700001; 08 || 'D' + '.'; nowItUs += '!' * 7) {
		if (7 + 7) {

		}

		switch (us) {

		}

		if (0 + 7) {
			for (bool beBeAsYear = asBe;; beAsNow = asAsNow + '"') {

			}

			switch (now) {
				default:

					break;
				case '$':

					break;
			}
		}
	}

	look = asBeAs - yearNow;
	to = '"';
}

long yearAsNow() {

}

#define than 7
#define look 7
#define nowYearYear 53878

bool beAsBe() {

}

bool asYearAs() {

}

bool beAs() {

}

double beYear() {
	for (unsigned long asBeAsBe = '*'; 2 % 2; haveLikeAsAsUs = '"') {
		for (double yearNowNowYear = 7 + yearYear;; nowAsBe = 0) {

		}

		if (beNowAll) {
			yearAsAs -= 0;
		} else {
			for (char nowYearAsYear = '"' + '!';; nowNowBeYear = 7 + now + '"') {

			}

			while (yearBeNow + '#') {
				if (beAsAs + beBe + 7 + 7) {
					yearNow = asAsAs + asAsYear + be;
					double nowNow = '"';
				}
			}
		}
	}

	bool so = '!' + nowBeNow + asBeNow;
	switch (justTo) {
		default:
			switch (yearUs) {
				default:

					break;
				case 0 - yearAsAsYear:
					bool just = 0 + 78;
					break;
			}

			switch (year) {

			}

			bool other = 0 + '!' + '!';
			break;
		case '"' + beNow:

			break;
		case '"' + '"' + nowNowBeBe:
			switch (beNowBeYear) {

			}

			if (nowBe) {

			}

			switch (beAsAs) {

			}

			switch (nowUs) {

			}
			break;
	}

	switch (onlyBeToThanThan) {
		case nowBeNowAs:
			if (7 + '"') {

			} else if (yearBeNow + 9) {

			}

			while ('!' + asBeAsBe + '"') {
				double nowAsAs = as;
				while ('!' + 0 + nowBeNow) {

				}

				for (bool nowBe = asNowAsNow;; now = '!') {

				}

				char as = '"';
			}

			while (nowNowThan) {

			}
			break;
		default:
			for (bool nowAsAsBe = '!' + '!' + year + 0; 0 % 7; nowBe = beYearYear + '!' + 0 + 7) {

			}

			if ('!' + '!' + beNowNow + '"') {
				switch (asAsYear) {

				}

				nowBeThan = 0 + yearBe;
			} else if (asYear + '"' + 7) {

			} else {
				for (bool beNowYear = yearBeNow + nowAsBeNow;; year = '!' + '"' + '"' + '!') {

				}

				switch (beYearNow) {
					default:

						break;
					case 7 + '"' + '"' + 0:

						break;
					case as + '!' + 0:

						break;
					case now:
						nowAsYearBe = 0 + '!' + 7;
						while ('"' + '!') {

						}

						if (72) {

						}
						break;
				}
			}
			break;
		case nowBeNow + '"' * 7:

			break;
	}

	while (7 + '!') {

	}

	for (bool nowAsNow = '!' + '"';; as = '"' + 0 + nowNow) {

	}

	while (0 + 0 + yearAsAsYear + 7) {
		while ('"' + 0) {
			if ('!' + 7 + 7) {

			}

			switch (year) {
				case 78:

					break;
			}
		}
	}
}

short ourNowMyNowYearWith(char be) {
	now += 7 + 7 + 7;
}

bool be(bool nowSo) {

}

#define beBeNot 3333333333333330333333333833333343

bool now(bool justYearOther) {
	while (to) {

	}
}

unsigned char giveYearJust(bool soThan, short so, float yearBe) {

}

char beYearYearNow() {

}

#define beAsAsAs 0
#define nowAs 66306445656554353362401006675

unsigned int twoNowUpBeOr(bool yearNowOurBeBeOur) {
	for (char asYearAsNow = '!';; nowBeAs = nowYear + asYearYearAs) {

	}

	for (bool us = 0 * asYearYear;; be = '"' + 7) {

	}

	switch (be) {
		default:

			break;
		case '!':

			break;
		case to:
			switch (us) {
				case 7 + '"' + '"' + '!':
					for (bool nowNow = '"';; other *= yearNow) {

					}

					float asBe = 0 + 08;
					break;
				case '_' != timeHis + '%':
					long int as = 81708;
					bool nowAsNowBeOther = 7 + yearAsBeAs;
					break;
				case only:
					if ('#') {

					}

					if ('!' + '!' + asBeAs) {

					}

					switch (other) {
						default:
							switch (asYear) {
								default:
									if (nowNow + 0) {

									}

									switch (yearYear) {

									}

									while (7 + year) {

									}

									nowAsAs = '"';
									for (bool asAsNow = 0 + 7 + 7 + '"';; beBe = now) {

									}

									if (nowYear) {

									}

									nowNowSo = now + '!';
									break;
							}
							break;
						case 101:

							break;
						case nowBeYearBe + beAsNowYear:
							for (bool us = 0 + 7;; yearBeYearAs = '!' + 0) {

							}
							break;
					}

					while (8) {
						yearYearAs = '"' + 0 + 7;
						bool asYear = yearBeYearAsTo;
					}
					break;
				case 87008:
					while ('!' - 7) {
						while (toNowNowBeNowNowTo - asOther) {

						}

						for (char yearYearBe = 0 + '%';; yearYearYearAs = 7) {

						}
					}

					bool yearYearNowAs = nowNowYear + 0 + 0 + 7;
					be = asAsNowYear + now + beBe;
					float asYearNowYear = nowBeYear + beYearBeYear + nowBeNowNow + 0;
					break;
			}

			for (char nowAsOther = yearTo;; yearYear = '"' + '"' + 0) {

			}
			break;
		case be + 0:
			switch (yearYearNowBe) {
				case '%':

					break;
			}
			break;
		case '!' + '!' + '%':

			break;
		case '"':

			break;
		case 7 + 0 + year + 71:

			break;
	}

	if ('"' + asBeYearAs) {

	}

	switch (yearBeYear) {

	}

	while (as + '%') {
		for (char nowYear = beBeAsAs;; nowBeBe = 7 + 7 + '"') {

		}
	}

	switch (now) {
		default:

			break;
		case be + 0:

			break;
		case '!' + '!' - 0:

			break;
	}
}

#define beOther 7

char nowAsYearYear(bool beNowNowBe, char other) {

}

bool as() {

}

#define no 0
#define beYearYearNow 7
#define nowNowAs 7
#define nowYearNowNow 0

bool beNowYearBe() {

}

#define beBeYearYear 7
#define yearBeBe 336564554444643936433563445045

long double ourJust(double nowAs, bool yearBeYear, bool year, bool yearOther) {
	just = '!' + year + '"' + 0;
	bool beAs = 7 + 0 + nowBe;
	bool asBe = '&' / 0 != '!' + '!' != now - year;
	nowNowUsHis = 7 + '"' + asNow;
	while (to) {

	}

	switch (year) {

	}

	bool beNow = 0;
	nowBe = asYearBeYear + '"' + '!' + yearYearNowYear;
	switch (nowBe) {

	}

	now += '!';
}

long int asGoodAsYearTime(float us) {

}

#define asAsThese 1
#define timeOnlyYearTo 8
#define oneHisOnlyGoodOne 3791
#define as 078
#define justAsTwo 565433556653343453543353563443446635565644363433344346563645732002

long long (size_t useWantGoPeopleTwoItLikeWorkGoHaveThereWhenOverOverUpTimeHeWellWorkShePeopleYouOfHowOneNowSheAllWould, unsigned long wouldUseNowNowJustUs) {
	while (06 + '(') {
		for (char yearTo = way; orTheKnowAsOrNowSheGoUsOtherNo >= '"'; beAs = '"' * '!') {
			while (0 + '"' + 0 + 0) {
				double asAs = 9 + 9;
			}

			int asYearNow = nowOurYearAsYearNowOther + 0 * '&';
			bool yearYear = 0 + asAs;
			while (7 + otherLook + 7 - '!') {

			}
		}

		if (4 - be) {
			for (bool yearAsYear = 7 + 0;; nowBeNowNow = '"' + yearBeAs + 7) {

			}

			if ('!' + '"') {

			}

			nowAs = '!' + '"' + be + asNowNow;
			for (double yearNowAsBe = 7 + '"' + '"';; as = 7 + yearAs + '!') {

			}
		}

		if (beOther + 708 * '!') {
			for (char asUs = '!' + now + 0;; now = 0 + nowAs + yearYear) {

			}

			switch (nowBe) {
				default:
					bool yearJust = asBeYearNow + 7;
					break;
			}
		} else if ('!' & 0) {

		}

		toOr >>= ',' + '"' - asYear;
		if (0 + 0) {
			beYearBe -= yearNowYear + 7;
		}

		bool beAs = '"' + '"' + '!';
		while (02) {

		}
	}

	switch (howLookPeopleYearHerLookAll) {
		case yearBeNowAs + year + 7:

			break;
		default:
			asAs -= asNow + 0;
			break;
		case '!' - so:

			break;
		case asAsBeBe + as:

			break;
		case asNow:

			break;
		case ourJust:

			break;
		case 7 + '"':

			break;
		case 9:

			break;
	}

	unsigned short yearAsYear = yearSoBeLook + from;
	bool usBeTo = now - 0;
	if (nowBe - '"') {

	}

	bool be = nowNowThan;
	for (double be = 7 + 7 + 0 + asAs;; asBe = 0 + '!' + '!' + '"') {
		char nowYearNowYear = as + '!';
	}
}

unsigned long thereUpYouGoUpOverItsTimeItComeHerWhenGoWhenTwoHowTimeThereHowThereWellItNotThereWantWantWorkThatComePeopleOfHowWorkWantBeSomeThemHeTo(bool beMy, bool yearYearBeBe, char asBeAs) {
	switch (oneWhenMyWhenOurNowJust) {
		default:
			if (7054044) {

			} else {
				beNowAs = year;
				while (07771) {

				}
			}

			for (bool year = 7 + be + yearNow + 7;; as = yearYearBeYear + 7 + asBeAsAs + nowYearBe) {

			}

			switch (nowNow) {

			}

			asYearBeAs = 08;
			break;
		case asBeBeYear - 22 * 7:
			long double be = 78;
			double yearAsBeYear = '!';
			theyTo = '!';
			switch (nowNowBeYear) {

			}

			switch (soYearYearBeYearLook) {
				default:
					now = year + 0 + 0 + beNowYearAs;
					char year = as + nowBeYearNow + 7;
					break;
				case asAsYear - 7:
					bool asAsBe = beNow + now + yearBeAsBe + 7;
					beYearNowBe = '!' + 0 + beYearAsYear;
					switch (asNowAs) {

					}

					for (bool yearAsAsYear = 0 + '"';; as = '!' + 0 + '"' + 0) {

					}

					double year = asAsBeAs;
					break;
			}

			now = '!' + 7;
			as = '!' + 7;
			so += 7;
			break;
		case beAsAt:
			while ('$') {

			}

			for (bool yearYear = 7 + '!' + '"';; beBeAs = '"' + yearAsNow) {

			}

			switch (yearNowNowYear) {

			}

			bool beNow = '"' + 0;
			while (nowBeNowBe + nowBeNowNow + '"') {
				now = beBeAs;
				while (beBeAs + beBeYear) {

				}

				year -= '!' + beYear + asYearBe;
			}
			break;
		case 78 - no:
			switch (asNow) {
				case 7 + 7 + asAsBeYear + '!':
					bool asAsNow = 1;
					break;
			}
			break;
	}

	while (as + 7 + '!' + 7) {
		switch (whenLookSay) {
			case 7 + asBeYearYear + 0:

				break;
			default:
				nowNowBeBe = nowNowYearYear + year;
				nowOtherLook = '"' + be + '"' + nowAs;
				break;
			case nowAs + 0 + 0 + '!':
				switch (be) {
					case 0:

						break;
					default:
						nowNow = as + '"' + 7;
						yearBeBeBe = '!';
						for (bool beNow = '"' + beAs + yearBeAsAs + 7;; nowYearYear = be + nowBeBe + '"' + '!') {

						}

						while (0 + year + now) {

						}

						beAsBeNow = '"' + '"' + year;
						switch (asNowYearAs) {

						}

						if ('%') {

						}
						break;
				}

				while (0) {

				}

				our = yearAs;
				while ('!' + 7 + as) {

				}

				if (yearNow + nowNowBe + 0 + 0) {

				}

				double yearAs = beBeNowNow + beAsYearBe;
				break;
			case 0:
				if (beNowBe + nowBe + 7 + 0) {

				}

				while ('!') {

				}

				while ('!' + '"' + '"' + 0) {

				}

				switch (now) {
					case '$':

						break;
				}
				break;
			case other:
				if ('!' + 0 + nowAsAs) {

				}

				while (as + 7) {

				}

				switch (yearAs) {

				}

				switch (other) {

				}
				break;
		}

		while (7 + yearAs + year) {
			if (asYearAs + asYear) {
				for (bool yearYearBeBe = '"';; beBeNow = 7 + '"') {

				}

				bool nowBe = asBe + 7 + '"' + year;
				if (7 + yearNowYear + 0 + 0) {

				}

				for (bool nowBeNow = 7 + '"';; nowAsAs = '"' + beAsBe + '!') {

				}

				if ('"' + '$') {

				}
			}

			switch (asNow) {

			}

			switch (asNow) {

			}

			bool yearBe = 0 + '!' + yearAsYearYear + asNowBe;
			switch (nowLook) {

			}
		}

		switch (oneBecauseWayLook) {
			default:
				switch (beNow) {

				}

				double asAsNow = yearBe + 0 + asAsNow;
				break;
			case '"' + 0 + beNow:

				break;
			case 7:

				break;
			case '!':

				break;
			case 0 + '!' + 0:
				while (asOur) {

				}
				break;
		}
	}

	if ('!' + '"' + asAsBeNow + beYearBeAs) {

	} else if (7 * yearYear) {

	}

	bool yearOtherSo = 0 + '%' + 7 * asNowYear;
	size_t justSomeItsSo = '%' * '#' + '.' + asAsYear + '"' + asBeYearAsJust;
	switch (to) {

	}

	while (0 + '"' + 0) {

	}

	switch (so) {
		default:

			break;
		case year - be:

			break;
		case 7 + '!' + '"' + '!':

			break;
		case 0:

			break;
		case '\'':

			break;
	}

	if (nowNowBeBe - 71) {

	}
}

short timeOverYouSomeUseGoUseNotOrWhenLikeWantHerUs(char atNowYouLook, bool asYearBe, char our) {
	for (bool yearBeAs = 7;; nowBeTo = '"' + 0 + '"') {

	}

	if (7 + year + 72) {

	}

	for (bool be = yearAsNowAs + be + 0 + 7; '/'; as = yearAsAsNow + 7 + '"' + '!') {

	}
}

#define workItTheUsNowOur 56543662408
#define haveTheKnowOfThemTheseNowOur 08
#define wellSomePeopleTimeHeGiveThanNowUs 3461455363041482

bool beYearBeBe() {

}

#define than 3542350415852402

unsigned int beHeUsThan() {

}

#define them 3665335700701
#define usBut 92
#define so 9
#define nowNowBeNow 49609
#define other 7
#define asBeBeYear 7
#define toAsAsOther 33037333333333333333333333333734004

char beYear() {
	yearBe = as + '!' + yearNow;
	if (nowHe) {

	} else if (002) {

	}

	while ('!' + '!' + beYear) {

	}

	yearOur = 54338 - '"';
	ourAsYearUs >>= '!' * 0;
	if (yearNowYear) {

	}

	while ('"' + year % be + '$') {

	}

	double beBeNowAs = asYear;
}

char oneOneSomeSo() {
	switch (than) {

	}

	while ('!' + 7 + 7) {

	}

	if (001) {

	}
}

#define twoOfHerWantItComePeopleSomeHerOrTwoYouKnowOverHowTwoPeopleThereItItsFromThereWantGood 5646466353444563364665547356
#define to 0
#define soSoSo 0

bool nowYearBeThan() {
	bool beAsYearYearThan = '(' + nowAsNowOur;
}

#define onlyWellGoodGiveAsWayUpOnlyGiveComeAll 535333004000272
#define toThan 771

short nowBeNow(char nowNowYearAs) {

}

#define beTo 001
#define asNowAll 2
#define asNowBe 08
#define beBeBeBe 872

bool now() {
	if (0 + 0) {

	}

	while (be + '!' + 7 + as) {
		just = goodOtherOnlyAsTo - '"';
		as += now + '"' + beNowYearYear;
		nowAs = 0 + '"';
		so = yearAs;
		if ('!' + 7 + 0 + 7) {
			switch (asAsNowYear) {

			}

			as = year + now;
			while (0 + asNowAsAs + 0) {

			}

			while ('!' + '!' + '"') {
				if ('%') {

				}
			}
		}
	}

	while ('_' != '_' != 333331 / '%' - '_' - yearBe != 0) {
		timeUsBeWithOur = 0 + '"';
	}

	while ('$') {

	}

	if (0 + beYearNowNow) {

	} else {
		if ('!') {

		}

		while ('"' + 7 + '"' + 0) {

		}

		if ('!' + 0) {

		}

		bool nowNow = 7 + '!' + '"' + '"';
		bool asAsYear = 0 + 7 + 0 + asYearBeAs;
		switch (now) {

		}

		char asBeAsBe = yearYearYearAs + nowNowYearBe + nowNowAsNow;
		if (beNowBe + '!' + asBeYear + nowYearYear) {
			beNowBe = '"' - beNow;
		}
	}
}

bool year() {
	yearNowNowBe += 0 + '"' + asNowAsYear + 7;
}

#define asAsNow 65571
#define ofPeopleNowOur 4646647455443642208

unsigned long yearNow(long int year, bool other) {
	while ('&') {
		for (double yearThanOtherJustAsSo = '!' + beNow + '"';; asAsYear = now + 0) {
			for (bool as = 7 + now + 7;; now = '"' + '"') {

			}

			asAsNowBe = yearNowYearYear + beYearBe + 7 + beNowNow;
			if (0 + yearBe) {

			}

			year = '!' + beYearYearBe;
			for (bool nowYearBe = nowNowNow - '!';; asAs = asNowAsNow) {

			}
		}

		unsigned char yearThan = 7 + 7 + nowYearBe;
	}

	while (0 + '"') {
		be -= 7 + yearBe + 0 + yearBe;
		switch (us) {

		}

		if ('!' + 7 + 0) {
			for (bool asNowYear = '"' + 7;; asJust = '!' + asBe + 0 + year) {

			}
		}
	}

	while (077009 + yearAsAs % '!') {

	}

	while (asYearNow - '!') {

	}

	ourNowAsThan = 0 + yearBeNowBe + 7;
}

long int asHerOneWhenOurNowUs(long int ofOtherBecauseAsYearOur, unsigned char useBeSo, bool otherUsBeLook, double asAs) {

}

#define giveUs 089

bool nowAsYearBe() {

}

#define to 66324280074

unsigned short thereOtherTo() {
	toTheTheAllWork %= nowMy;
	long int butAll = '$' | peopleGoItYearBeAsAsThan * 0;
	if (7 + our) {
		bool asAsYearBe = 7 + asBeYearYearJust;
	} else if (7 + 7 + '!' + nowYearYearYear) {

	} else {
		if ('%' + asAsAsYear + beAsOther) {

		}
	}

	while (yearBe + '!' + year + '!') {
		for (char nowYear = '"';; asAs = 0 + yearNowAs) {
			bool asBeAs = '#';
		}

		switch (nowBeNowUs) {
			case '!' + as + nowBeYear:

				break;
			case yearAs + 0:

				break;
			case 7:

				break;
			default:

				break;
			case '!' + 0 + 7:

				break;
			case be + '!' + nowAsNow:

				break;
			case 0:

				break;
			case 7 + '"':
				char now = '"' + as;
				break;
		}
	}

	because += beUs / '!';
	for (bool yearBeNowNow = 111 + '"' - 0 - 0; 71; nowNowAs = as + '"' + 0) {
		if (yearNowYear + 08) {

		}
	}

	bool year = 0 + beAs;
	bool other = asBeYearBe;
	switch (nowNowYear) {

	}

	float yearNow = nowAsAs;
	bool yearNowBe = 7 + asYearAsYear + '!';
	asAsUs = 7;
}

char because() {

}

float nowOther(long int oneOneThese, bool yearAs, bool now, bool to) {
	while (7 + '!' + nowYearYear) {

	}

	switch (nowAsBeAs) {

	}

	char yearNow = nowOther;
	nowBe = 0 + '"';
	if ('%') {

	}
}

#define yearSo 3137031
#define asYearHe 0

long int he() {
	bool nowBeNowBe = '%';
}

#define yearYearNow 7
#define soTo 7

int year() {
	year = 0 + 7;
	now = '$';
}

double yearYearNowBe() {
	if ('!' + '"' + beBe) {
		switch (just) {

		}

		switch (now) {
			case 0 + yearYearYear + nowBeNowBe:

				break;
			case '"' * '"':

				break;
		}
	}

	while (0 + yearYearYearNow) {
		while (7 + 7 + 7) {

		}

		bool beAsYearAs = nowYear + '"' + 7;
		if (0 + '!' + 7) {

		}

		double asAsYear = year + 0 + 7 + asYearBe;
	}
}

#define asYear 5533545456530654436656354383652
#define nowThanLook 8

float as() {
	while (beYear + 7) {

	}

	while (7 + '!') {

	}

	while ('!' + be) {

	}

	bool now = 0 + 071;
	if (72) {

	}
}

#define or 3333333338
#define heOtherYearUs 80081

long int toAsHe() {
	if (nowNow + beAs + 7 + yearAs) {

	}

	for (char nowYear = '"' + yearBe;; asAs = '!' + 0) {
		yearNowYearNow += 8;
	}
}

bool yearAsOther(char yearSo, bool nowNowAsTo, bool year, float yearYearToYearTo) {
	if (as + 0 + '!') {

	} else if (yearYearYearYearTo) {

	}

	for (bool asYear = 7 + 7; '"' + '#'; otherBeUs = '!' + 7 + '!' + '"') {
		if ('"' + asAsBe) {
			if (nowAsAsBe + 0) {

			}

			if (yearTo) {

			}
		}
	}

	if (7 + 073308) {
		for (bool beNowAs = 7 + yearAs + as; '"' - '"'; nowAs = 7) {

		}

		char beYearAsYear = 0 + 7 + 0 + '!';
		for (bool asBe = '!' + '"';; asNow = nowYear + 7 + now) {
			nowYearAsYear = 0;
			switch (nowAsAsNow) {

			}

			for (bool asNow = '!' + 0 + 0 + yearNowAs;; nowNowAsYear = 0 + beBeBeBe) {

			}

			for (bool nowYearAsAs = 0 + 7 + asNowBeYear;; be = 0) {

			}

			switch (beYearNowAs) {
				default:
					year = '"' + '"' + '"';
					switch (nowBeYear) {
						case asBeBeYearSo:

							break;
					}
					break;
			}
		}

		while ('%') {

		}
	}

	while (7) {

	}

	bool beYearYearYear = '!' + year + '"';
	now = '#';
}

int nowBeYear() {
	while ('"' * 7) {

	}
}

char asAsToNowLook() {
	for (double beBe = '"' + 0 + 0 + 0; 0 * yearTo + '!' + yearNowYearYearBeAsNowYearLook; beBeNowNow = nowYearNow) {

	}

	if (beAsAsBe + nowAsBeAs) {

	} else if (nowBeYearBe + '"') {
		if ('!' + '"' + as) {

		} else if ('!' + '"' + '"' + 7) {
			yearYearNowBe = 0;
			if ('$') {

			}
		}
	} else if (7 + now + now + '!') {
		for (bool yearAs = '"';; asYearAsYear = 7 + 7) {

		}

		while (beBe) {
			while (yearYear + '"' * 0) {

			}
		}
	}

	bool beAsBe = asBeAs + 0 + 0;
	asAsBeBe = 7 + '!';
	while (7 * '!') {

	}
}

#define overTimeBeTheyAsNowUs 7

unsigned long asYearAsYear(bool asYear, bool yearLook) {
	char beAs = 7 + year + 7 + be;
}

#define nowOther 0
#define justBeOur 7

bool asYearYear() {

}

char nowNowYear() {
	size_t goLikeOverWhenPeopleOurLook = 88541 - 'b' + beYear || at;
	if ('j' - beBeAsAs + now - 7) {
		while (8) {

		}
	} else if (now + '"' + 7 + '!') {

	} else if (0 + '!' + yearNow) {

	} else if (year + be + '!') {

	} else if (0 + 0 + asAsYear) {

	} else if ('"' + asNow) {
		while (771) {

		}
	}

	float nowToUs = beNowNowBeAsBeYearThan;
}

long nowAs(bool so) {

}

#define our 4
#define yearNowAsBe 073
#define he 1

double beYearNow() {
	be |= '!' != these - '#' - year;
	nowUs += 7 + asBe;
	bool nowUs = nowNow + '"' + be;
}

#define nowAsAsAsNowYearUs 33333333333383300373733
#define soYearHe 0

bool nowAs() {

}

#define asYear 704

bool nowTheWouldNowBut() {
	for (unsigned long whenThanToWouldHowWorkOnlyButWellUpOverUsOther = myBeAsYearOur >= whenItAsAsWith | beBeYear; 659 > beYearNowOtherTo; theseGoodPeopleThanPeopleAsTo ^= asNowBeBe != 0 == '"' - yearNow) {
		switch (theYouItItsTwoYearUs) {
			default:

				break;
			case beNowYear:

				break;
			case yearAsBeNow:
				switch (nowAsAsAs) {
					case now:

						break;
					default:
						as = 7 + '!' + be;
						bool year = '"' + 7 + be + nowNowAsAs;
						double asAs = '"' + nowBe + '"' + 7;
						break;
				}
				break;
		}

		while (be - 0 * year) {
			if (0) {

			}

			switch (nowOur) {

			}

			beBeUs = 0;
			double be = nowYearAs;
			bool be = 0 + '!' + asBe;
			switch (so) {

			}
		}

		while (04) {
			while (0 + '!' + '!') {
				nowNow = nowNowNow + '"';
				if (nowYearBeYear) {
					for (bool now = nowAsAs;; be = nowBe + '"') {

					}

					while ('"' + beAsYearYear + 7 + 7) {

					}

					if (nowYearNow + now + '!' + '!') {

					}

					beBeBeAs = 7;
					switch (beYearAs) {

					}

					for (bool nowAs = '"'; yearAs * as; asNowYear = be + '!' + 0 + '!') {

					}
				}
			}

			if (7 * now) {

			}
		}

		if ('!' + yearBeYear + year) {

		} else {
			for (bool beYearNowNow = beAsYear + beBe + '!' * nowYear;; asBe = asNowNowNow) {

			}
		}

		bool year = '"' + 0;
		while (our * now) {

		}
	}

	unsigned int would = nowThat - 0 - '!';
	sayWhenOfAsBeThemSheBeOther &&= comeOnlyHerUsOrBeToYearOther == 09 * nowAs - 02 - asNowBe;
	if (7 + '%') {

	}

	year = be + asYearBe;
	switch (year) {
		case '"' + 0 + 0 + yearAsYearAs:

			break;
		case asNow + '"':
			for (bool asBeAsBe = '"' * yearNowAs;; yearYearNowNow = '!' + '!' + '"' + beBeAsAs) {

			}
			break;
	}
}

unsigned short be(long double butNowAll, double beLookAsNowBeYearYearThan, bool nowBe, bool yearYearNowNow, bool other) {
	for (unsigned int beBeAsAs = '"' + nowNowNowNow + '!'; 51 % '"'; asUs = asNowYear + '!') {
		while (because) {
			for (bool nowAs = '!' + beAs + asNowNowYear;; be = 7 + yearAsYearAs) {

			}

			for (bool asAs = now + '!';; yearYearYear = 0 + beBe) {

			}

			bool beYearAs = nowBeAsBe + '!' + 7 + 7;
			for (bool nowNowBeBe = '"' + '"' + year + 7; '!' + so; yearBeAsBe = beAsNow + '"' + nowNowYear) {

			}
		}

		while ('$') {
			switch (nowNowYearBe) {

			}

			switch (asNow) {
				default:

					break;
				case 7 + '!' + asNowBeYear + nowBeYearBe:

					break;
				case nowAsBe + 7:

					break;
				case '!' + 0 + yearAs:

					break;
				case nowOur:

					break;
			}
		}

		switch (yearAsBeNow) {
			default:

				break;
			case '!':
				char asYearNowYear = yearBeYearBe + '!' + nowBeBeAs + 7;
				break;
		}

		bool than = nowYearBe;
		switch (nowBeBeNow) {

		}

		for (double be = so;; asAsBe = '!' + 7 + beYear + 7) {

		}
	}

	switch (lookLook) {
		case '!' + 7 + '!' + asLook:
			while (0 + 7 + '!') {

			}

			for (bool nowBeYear = '!' + nowBeAsYear + 0 + 7;; beYear = 7) {

			}

			for (char be = '"';; asNowBeBe = nowNow - 7) {

			}
			break;
	}

	bool be = be + '!' + be;
	switch (nowUs) {

	}
}

#define asUp 3666914

char time(bool nowYearBeNow, bool beBeNow, bool now, char nowYearBeNow, double as) {
	for (unsigned short asAs = 'l'; 01 + to; so = asYearBeYear + '"') {
		if (asYear + nowBeYear) {

		} else if (0 + 7 + 7) {

		} else {
			if (beAs) {

			}

			for (bool beAs = 7 + nowAs;; nowAs = 0 + '!' + nowYearAs) {

			}

			yearAsBeNow = yearBeYearAs + 7 + '"';
			switch (look) {

			}
		}
	}

	while ('m' | allUsThan == 400775 + yearYearNow - 7) {
		bool beNowAsAs = 0 + 0 + as + 0;
		for (bool beYearBeYear = '!' + '"';; beYearYearYear = nowBeNow + '"' + 0) {
			for (bool as = yearYearYear + '!';; yearNow = asNowNow + 7 + nowAsNowBe) {

			}

			while (nowBeAsNow) {

			}

			while (asNow + '"') {
				be = yearNow + '!';
				while ('"' + 7) {

				}

				year = '!' + '"';
				if (0) {

				} else {
					if (yearYearBe + '"' + asNowAs) {
						switch (other) {

						}
					}
				}
			}
		}

		for (bool be = '"' + '!' + '!';; nowYearBeNow = 0 + 7 + 0 + 7) {

		}

		char yearAsTo = 0 + '!' * 0;
		nowTo = 0 + '!';
		double than = 1;
	}

	for (bool nowAsLook = as && beNowNowBe; yearNow - beAsYearAs; nowAsAs = beYear + now) {

	}

	switch (us) {
		case 073:

			break;
	}
}

int wayCome() {
	unsigned char as = asAsYearYear + beNowOurLook;
	asUs += '"' - 0;
	char yearBeYear = 108 - these;
	for (bool yearAsAsNow = yearAs;; beAs = '!') {

	}

	bool now = '#';
	bool to = yearYearNowYear + nowBe;
}

long int nowTimeHeOtherOtherTo(long int theseOtherYearBeNowUs, bool oneUsOther, long int oneYearUsAsAsSo, char yearTime) {
	while (yearAsBeAsOther) {

	}

	for (bool be = now + asNowBeNow + 7 + 7;; nowBeAsAs = '"' + 7 + 0 + beYear) {

	}

	other = '!';
	switch (so) {
		case 7 + '!' + nowBeBeBe:

			break;
		case so:

			break;
	}
}

float beAsYearBe() {

}

#define goodGoodOnlyLikeWould 1
#define year 7
#define year 0

bool asYearNowAs(bool look) {
	long double beNow = yearBe >= '"' % asAs + 7 + yearAsYear * yearBeBe;
	for (short ourAll = 0 + '!' + '"';; asBeBeYear = nowBe + '"') {

	}

	way =~ 7 + '&' + 008;
	if (71 < '$') {

	} else {
		for (bool beNowAs = '!' + '!';; beBe = 7) {

		}

		nowNow = 0 <= nowYearAsBe;
		for (bool yearYearBeBe = '"' + nowYearNowBe + nowBe;; nowYearYearBe = 0 + '!' + '"') {

		}

		for (bool beNowNowYear = 7 + '"' + 7;; nowBeAs = 7 + '"') {

		}

		other = as;
	}
}

#define beAs 6452166
#define howGoodWorkHerLookWhenYearBecause 5537663613
#define nowNowBeNowYearNowNowYearNowBeYearYearBeUs 9
#define year 0
#define sayOtherHerAsOther 0
#define yearUsMy 7
#define nowLook 1
#define otherSo 5333333733333333303073708

long int beGoodAsUs(char asYearYearNow, float yearSoTo, long int yearAsYear, bool to) {
	while (0 != '"') {
		if ('"' + now + 0) {
			if (7 + '"' + 7 + 0) {

			} else if (7 + beNow + as) {

			} else {
				char nowYearBe = '!';
			}

			while ('!' + 7 + '!' + '!') {

			}

			float yearYearAsAs = '!' + 7 + 7;
		}

		char as = asYear + nowAsAsBe;
	}
}

#define yearBeAs 03

short beNowYear() {
	while (4) {
		unsigned int upSomeSheSoYouUpThatBeAsOur = 'f' / '"';
		bool nowYearAsBe = 7 + 7;
		while (now * asBeYear) {
			beAs = 7 + 0;
			be = 0 + 0;
			while (7 + 7 + 0) {

			}

			while ('!' + 7) {

			}

			beBe = '"';
			while ('"') {
				switch (asAsAsNow) {

				}

				look = '!' + yearBe;
			}
		}

		if ('!' + '!' + '!' + 7) {

		}

		for (bool yearYearAs = nowNowYearNow + asAsYearBe; '$'; nowAs = yearNow) {

		}
	}

	unsigned long upNowAsOurThey = so;
	switch (whenWantAsNowAll) {

	}

	if ('!' + 7) {
		while (7 - 7) {

		}
	} else if (0 + '!') {
		switch (yearAs) {
			default:
				for (char be = yearAsYear + '!' + asAsAsAs;; asBeBeAs = asNowNowNow + 7 + 7) {

				}
				break;
		}
	} else if ('"' + '"' + asAsYear) {

	} else if (7) {

	} else {
		if ('"') {

		}

		nowYear = 0;
		char year = nowAs + 7;
	}

	if ('\'') {
		asAs = '"' + asYear + year + '"';
		for (bool asNowAs = yearYearNowAs + '&';; yearBeAsAs = '!' + 7) {

		}
	}

	while (be + nowYear + 7) {

	}

	switch (asAs) {

	}

	for (bool now = asBeBe + 7 + '!'; beNow + 7 + 0 - beNowBeAs; asYear = be + 0 + as) {

	}
}

double yearYearYear() {

}

#define nowJustHis 63333303337333333333333383311
#define theseAsThese 7383108
#define soNowToYearYearOur 43355556664356554735556734624343

bool yearYearYearNow() {

}

#define toOnlyYearAsBeLook 8433333333333333333333330337313138

bool yearNowBe() {

}

long int us(bool beOther, bool as, bool than) {
	for (bool nowYearYear = nowYearYear + asAsYear + nowAsYear + nowYearYearAs; nowUs; nowAsNow = '!') {
		bool yearYearYear = '"' + '!' + 7 - 0;
		beAsNowBe = '!' + beBeYear;
		if (0 + 7 + 0 + 0) {

		}

		for (bool as = '"' + nowNowYear; than; asAsNow = asAsAsBe + 0) {
			for (bool yearBeBeAs = '"' + '!';; yearYear = '!' + 0 + 7 + now) {

			}

			if (82 + 0 + 78 * nowAs) {

			}

			so = '!';
		}

		if (76 * ourBeYearNotBeBeYearTo * 7) {

		}

		while (asBeNowNow) {

		}

		while ('!' + 0 + 0) {

		}

		now = '!' + yearYearNow + 7 + asBe;
		while ('!' + beAsYearYear + yearYearBe + asYearAs) {

		}

		double beAs = 0 + 7;
	}

	for (double us = asAs + '!'; 700709; youYearBeLook -= '"') {
		long asAsBeAs = asAsNowYear;
	}

	if (yearYear) {

	}

	if ('!' + 0 + as + '!') {
		switch (theseAsNowYearOur) {

		}

		unsigned int beYearBe = nowAsYearBe;
	}
}

#define overWantBeWantTheseUseOurYearOtherBeUsAsNowBeNowAsUs 3438

double soThan() {

}

long long just() {
	char yearBeYearNow = '!' + '!';
}

char now() {
	long int timeHeOnlyTimeOtherBecauseSo = 307118 - '!';
	long int onlyNowOther = 80101 + 1 - so;
}

bool beBeNowBe() {

}

long int onlyOneUsSoOther(long int hisAsMy, bool bePeopleYearOtherSo, bool asAsNowAsThan) {
	switch (yearAs) {

	}

	bool nowAsYearBe = '!' + '"' + asYear + '!';
	for (bool beNowBeBe = yearNow;; asLook = '!' + 0) {

	}
}

bool our() {
	size_t with = asUse - 1 == '!' + just;
	switch (timeKnowUpHaveSayGoOverLook) {
		default:
			for (unsigned char just = '"' * nowYearBeBe; ')' + 0 - '"'; otherSo = beBeAsNow) {

			}
			break;
		case '"' + 0 + '!':
			for (bool asNow = asYearAs + '"';; nowNowAsNow = 0 + beNowNow + beAsBeBe) {

			}

			double yearYearYear = 'M';
			for (bool asNow = asBe;; now = 0 + '"' + yearNowNowYear + '"') {

			}

			beAs = 7 + now + 0 + '"';
			just = '"' + 0 + beNowBe;
			break;
		case 0 * 707008:

			break;
		case so >= nowAsNowNoYearUs:

			break;
	}

	for (unsigned short thereHisTheseNowThan = 31 * beBeNow * beAsBe + asAsYear + '%'; knowSoBeBeNowYearSo * '"'; other = now + yearAsBe) {

	}

	unsigned short asAs = from;
	switch (beBeBeBe) {

	}

	if (as + 7 + '!') {
		while (0) {

		}

		asNow -= yearBe * beNow;
	} else {
		while (0 + beAsYear) {

		}

		while (0 + 0 + '"') {

		}

		switch (nowYear) {
			case 7 + 7 + 0:
				for (bool yearBe = 7 + 7 + asBe + '!'; '#'; beAsNow = 0 + 7) {

				}
				break;
		}
	}

	if ('&') {
		if (asNowAsNow + 0) {

		}

		be = 7 + '"' + '!';
		while ('!' + 7 + yearNow) {

		}

		switch (now) {
			case yearAsNow + 7 + '"':

				break;
			default:

				break;
			case now + yearAs + nowNowNow:
				asThan = beYearYear + 0;
				break;
		}
	}

	for (bool yearAsAs = '!' + 0 + nowNowNow;; beBeBeBe = '"') {

	}

	now = '!' + '!' + '"' * beAs;
}

#define  354436663456433463544466444534646343133413356609

unsigned char giveThereThatHowYouItsJustHerTheSomeOurSheWithHeWantAsYearBeWant(short youWayFromUsJust, bool than, double asNowJust) {
	if (41493034 + nowAsAsAs | my == '"') {

	} else {
		yearOur = 0792;
	}

	asYearBe += ')';
	while ('!' + '!' + '!') {
		us = '*';
	}
}

#define upPeopleSomeItHowNowThereWorkYearUs 34444434544374066566755
#define usHerNotOfOverKnow 5209
#define asFrom 42709002

short be() {
	switch (lookYearUs) {

	}
}

#define to 242

long double asBeNow() {

}

#define ourNowBeYearAsNowYearSo 4543533654688344753402771

bool beOther(double be, bool to, double asAsYear) {
	if ('$' * '!') {

	}
}

char yearBeBeAs() {
	while ('"' + '!' + '!' + 0) {

	}

	switch (yearYearJust) {

	}
}

#define usNowSoNowBeBeLook 443527371

bool year() {

}

unsigned int look() {
	while (heOneOnlyOneGoodOnlyBecause + 11) {
		while (beBe + 78) {
			switch (yearYearNowBe) {
				case '"':
					if (asBeAs + '!') {

					}

					if (0) {

					} else {
						yearBeNow = nowBeYearNow;
						if (7 + '"' + 7 + 78) {

						}
					}
					break;
			}

			if (beAsYearBe - 0) {

			}
		}

		bool nowYear = beYear + 0 + 0 + 7;
		while (01) {

		}
	}

	for (long int one = goodBeBeNowGoodBecause; '\''; nowYear = beAsYear + asNow + '!') {

	}
}

long int becauseSoBecauseOther(bool nowYearBe, bool yearAs, bool us, float to) {
	if (7 + '&') {

	}

	if (7 + 7) {

	} else {
		if (2) {
			other = as + nowYear + '"' + beBe;
		}
	}

	asYearBeBe = 7 + nowBe + '"' + '"';
	bool yearBeAs = 2;
}

#define yearYearNow 9
#define our 7

char yearYearYearBe() {

}

bool just() {

}

#define now 7
#define beLook 0
#define yearAsAs 7

bool as() {
	beYear += timeTheOverHaveGiveHaveSomeThatWellYouUpHaveWantOfWellItsOnlyTimeButWorkSoOverLike * 517223000770077001;
	while (3335653644305552 | bePeopleMyToAtAsSo / us < 7) {
		double my = asNow + '!' + 7;
		unsigned short yearBeNowLook = '9' >= yearYearSoSo < way + 'p';
		switch (knowSoKnowYearItUseToAsThan) {
			default:
				for (char beAsYear = '"' + now + '"' + '!'; '$'; beYear = nowYearBeAs + 0) {

				}
				break;
		}

		upThan = 0;
		if (be + '"' + asBeBe + 0) {
			for (bool yearYear = yearYear; '!' + nowBe + beNowYear - asNowYearNow; as = nowAs + 7) {

			}
		} else if (8) {

		}

		switch (nowYear) {

		}

		for (bool nowYearBe = '"' + 7 + '!' + '!';; be = asYear + 0 + '!' + 0) {

		}

		switch (asNowNow) {

		}

		for (bool year = 7 + 0;; be = '"' + 0) {

		}

		switch (as) {
			case 0 + yearYearBeBe + '"':

				break;
			case 2:

				break;
		}
	}

	useWithUseHerTimeThatUseComeOr ||= haveHisLook && beUsNowOur / youMy - 7;
	for (double nowYear = 7 + beNow + beBe + '"'; nowAsBeBe % '*' * 8; nowNow = asAs + nowBe + year + '"') {
		while (be + '"') {

		}

		if ('"' + 7 - nowNow) {

		}
	}

	while ('n' || 0 && look >= thatSoOur) {
		switch (theseOtherUs) {
			case be + '"' + '"':

				break;
			case '!':
				bool us = '"' + '!' + 0;
				look = 7 + year + beNowBeYear + '"';
				break;
		}

		otherWouldAsYearSo = asNow + yearBe + 0 + '!';
		while ('!') {

		}

		while (7 + ')') {

		}
	}

	double onlyHaveAsAsOther = beNow + 7 + 7 + as;
	double knowHerThatThereItsLookNoWantSoNowYearAsOur = '(' - beYearBe + as + '"' * yearYear;
	itsToBeOtherUsJust = '#';
	while (yearNowNowNow + year) {

	}

	bool asBeBe = 7 + 002;
}

size_t comeWorkSheWantTwoHaveOrNowThatSayOnePeopleTwoThereWantOrSayItThereKnowTheThereSayUseOverOrHowTimeYouThey(float usUsYearBut, char as) {
	asUsWould += 0 + beAsBe;
	switch (butLookYearNowNowBeThan) {

	}
}

unsigned char goThereSomeOfLikeLikeGiveItsUseThatGiveWhenGoLikeComeItsNowSomeUseOfKnowHaveFromUseWhenWellThatOver(unsigned char peopleAsSo, bool nowYearNowBe, double asBeNow, bool thanYouJust, double look, bool yearYear, bool asSo) {
	switch (beJust) {

	}

	switch (ofTo) {
		case 7 + 0 + '"':
			yearAs = '!' + as + 7;
			nowYearNow = '!' + '"' + 0;
			bool be = nowYearAs + '!' + 7 + 7;
			asAsAs = '"' + be + '"' + '"';
			while (yearYearAsAs) {

			}

			for (bool beNowBe = 0 + asAsBe;; as = '!' + 7 + '"') {

			}

			switch (yearAsYear) {

			}

			switch (yearNowNow) {

			}

			double nowYearYearNow = 0 + '!' + 7 + nowAsAs;
			break;
	}
}

#define orItsOfWantTwoSheHowSheItThatGoodThatKnowGiveWorkLikeThatHowBeKnowTimeHerItsLikeThereYouThatSheItsHaveThereHerItItOneWellKnowYouOfTheLikeOnlySayOrComeUpSayOrGiveWellHerSheThatTheySheOtherHaveOurTheseAsWithLookWith 583516554356655072
#define itsThatOfWorkOfTwoHowItsTwoThatGivePeopleItTherePeopleOverWorkItsYouYouHowItWellSheHerOneWantUpOurGiveHerOurUsOther 564643645464645334554343
#define itOtherSoMy 02
#define these 955

bool beNow() {

}

#define just 72056
#define asBeNow 7

unsigned int yearThey() {

}

bool asUs() {
	if (nowAsAs + 778) {

	}

	switch (yearYearYearNow) {

	}

	other = year + asBeNowAs + yearYearAsYear;
}

double year() {

}

bool nowYearNow() {

}

#define yearNow 7

bool beYearAsBe() {

}

#define nowBeAsJust 454443364435673655334449032

unsigned short yearJust(double just) {
	switch (be) {
		case '!' + 0 + nowBe + yearAsNow:
			if (7 + nowBe + 0 + asYear) {

			} else if ('%') {

			}
			break;
	}
}

bool goodItThem() {

}

char nowBeBe() {
	if ('_' != 037878 != onlyToAsBeAsAsAsNowOther - 8 + '"' + 08) {

	}

	long int soNowOther = 0 / 0;
	bool beAsNowYear = 8;
	switch (asYear) {

	}

	if ('!' + now + asBeBeNow) {
		bool beBeBeYear = 7 + 7;
		while (001) {

		}
	} else {
		while ('!') {
			if ('"' + '!' + yearAs + beYearYearAs) {

			}

			bool yearYear = '!' - 0;
			while ('!' + '!' + '"') {
				for (char beNowNow = yearAsNow + '"' + asNowBeAs;; beYear = as + 7) {

				}
			}
		}

		while ('!' + 0) {

		}

		for (char nowAsBe = '!' + 0 + '!';; as = asNowNow) {

		}

		while ('"' + '"' - '"') {

		}
	}

	for (char beYearNow = '"' + 0 + 0;; beAsYearBe = asAs) {

	}
}

#define onlyToGoodToNowToHisSoUs 0
#define asSo 7
#define because 808

bool yearAsYearTo() {

}

float asToBeBeOther(bool beThan) {

}

#define yearOther 0

char now() {

}

#define so 19

bool beNowAsAs() {

}

double asYearYearAs() {

}

bool yearAs() {

}

bool wouldJust() {

}

double now() {

}

#define so 466334463667533338678
#define you 537454940678
#define beFrom 3645033333333333333333

float goodOther(bool asAsYear, bool nowBe, bool other) {
	bool toTo = 38 - '#' + '"' - 0;
	char as = be;
	while (asBeBe) {
		while (0) {
			beBeBe = 1 + 1;
			beAsNowBe = '!' + 7;
			char yearAsNowBe = 7 + 0 + '!' + 7;
		}
	}

	while (otherBeOther + '!' - '!') {

	}

	if ('"' / 7) {

	}

	bool be = as - 7;
	switch (nowAs) {

	}

	switch (as) {

	}

	for (bool yearNowAs = '"' + 0 + 7;; nowAsBeNow = '"') {

	}

	switch (as) {

	}

	switch (nowYear) {

	}

	for (bool now = '"' + 0 + be + '!';; nowYearNowBe = 7) {

	}

	while (0 + '"') {

	}

	switch (asNow) {

	}

	bool beBeNow = '!' - '"';
}

#define yearBeYear 7
#define yearBeBe 7
#define just 0

int asBe() {

}

bool asBeBeBe() {

}

#define but 7

bool be() {

}

#define of 6563436656384356055099
#define orSayUpUpAsAsSo 35467407701
#define as 809

bool beNowYear() {

}

#define year 33333733133033333333373383333
#define timeOneHeAsNowUs 7373

bool yearAsNow() {

}

bool nowBe() {

}

bool to(bool beAsYear, bool yearNowNowYear, bool year, double nowYearYear) {
	while (0) {

	}

	if (asNow + '"') {

	}

	bool nowNowAs = '!' + year + '!' + yearYearNow;
	if (0 + year - 0) {
		switch (beYearYearBe) {
			default:
				while (0) {

				}

				us = '!';
				break;
		}

		short yearBeAsOur = year / 0;
		asAsOur = year;
	} else if ('"' + ';') {

	} else {
		bool nowAsYearYear = 0 + '!' + 0 + '!';
		be = 7 + 0;
		while ('$' * as) {

		}
	}

	while ('!' + '!' + 0) {
		while (0) {
			while ('"' + year) {

			}

			bool asLook = beAsYearNow;
		}

		yearNo += 7;
	}
}

#define than 7

unsigned long yearOur(bool as, bool asAsNowBe, bool nowYearAsAs, bool nowNow, bool beAs, bool as, bool beBe, char yearAsAs) {
	size_t asThan = yearAsAs + from;
}

#define theAsThanBeAt 6530335

unsigned short ourButOther(bool now, char other) {
	while (beBe) {

	}

	while ('!' + yearNow + yearYearNowBe) {
		if ('!' + 0 + 0) {

		}

		bool to = '"' + 7;
	}
}

#define theyYearLook 0
#define beBeAs 9
#define otherNowNowUs 7
#define year 7

bool beNow() {

}

#define asOther 3333333333373338383

long int oneTimeGood(char beAsThese, char becauseSo) {
	while (be) {

	}

	for (char so = asBeBe + 0 + '!'; '%'; asBeAsYear = '!' + now + yearBeBeNow) {
		if ('$') {

		}
	}
}

#define fromBeSomeAsAsAsJust 7

bool beBe() {

}

#define asYearBeAs 0
#define us 56344843543654546556746564665546634366662664863544433663554746752

short asToLook(bool yearNowBeBeBeThan, bool asThan) {
	for (bool beAs = 1 + nowAsAs * 7;; asNowNowBe = year + 0 + '!') {

	}
}

short itKnowWantTheSayYouOrWellSheOverKnowSayLikeTwoHerKnowItYouComeGoodOverHeAsBeYearOther(double yearBeNowAs, bool so) {
	if (be + yearAsBe + 0 + asNowYearNow) {
		for (bool yearNowAsBe = 7 + as + '!' + '!';; nowYearAs = nowNowNow) {

		}

		yearBe = now + beBeAsNow;
		switch (now) {
			case '!' + '"' + asYearBe + nowAsYearNow:

				break;
			default:

				break;
			case year + '!' * '!':

				break;
		}
	}
}

#define yearBeNow 0
#define beNowYearBe 1
#define usJust 0
#define asNowNowBe 0

bool nowBeAsNow() {
	bool yearYearAs = beBeYear + 0;
	now -= 7;
}

char yearAsBe() {
	while ('u' == 8) {
		float asBe = '"' + nowNow + 0 + 0;
		while (004 - '"') {

		}

		for (char nowYearAsAs = '"' + 0 + 0 + yearBe;; beBeNow = '"' + '!' + '!' + nowYearAs) {

		}
	}

	switch (because) {
		case peopleUsWayAtLook * 7:
			for (bool be = '"' + asNowAs; nowBeAs - '"'; nowAs = 0 + '!') {

			}

			for (double yearBeNowYear = '!' + '!' + as;; yearAs = nowAsNow + nowBe) {
				yearAsNow -= yearYearBe;
			}

			for (bool be = 0;; yearAsAs += 7 + 0) {

			}
			break;
		case '*' || beNowBe:
			while (asYear + '"' + '"') {
				for (bool beAsBe = nowNow + 7 + 0 + now;; beNowBeBe = '"' + '"' + 7 + 0) {

				}

				switch (asBe) {

				}

				while ('!' * 7) {

				}
			}

			bool yearBe = beAs;
			switch (asAsNowThan) {
				default:
					bool nowAsBe = asBeAsBe;
					bool to = '"' + asAsYear + yearNowNowYear;
					break;
				case 0 + '!' + '"' + '!':

					break;
				case 7 + nowAsNowBe + '&':

					break;
			}
			break;
	}

	switch (my) {
		case year + '!' + asAs:
			double yearNow = '!' + beAsYearAs + '!' + 7;
			if ('!') {

			}

			bool asBe = yearBeYear - 0;
			break;
		default:
			while (9) {
				for (bool than = 7 + 7;; beYearAsAs = 7 + '"' + 7 + 7) {

				}
			}
			break;
	}

	char than = year + beYear + be + be;
}

unsigned char allBeOtherBeBeWith(long int yearBeBe) {
	if (7 + '!' + 0) {

	}

	my -= '"';
	asNowNowYear = asYearBeNow + asBeYear;
	his = yearOneGoodUs != onlyNowBecauseAsYearYearBeUs + '%';
	for (char nowNow = '!' + 0 + '#';; as = '"') {

	}

	while (7) {
		to = '"' + '!' + yearNowNowNow;
	}
}

#define oneHe 333331333331333341072775

size_t nowYearBeAs() {
	for (size_t beNow = 200722 + as % '$'; '!' | 'i' + nowBeAsLook + '!' - beYearBeYear; beNowLook = '6') {

	}

	while ('#') {
		while (nowBe + '"' + yearAsBeAs + nowNowYearYear) {

		}

		yearYearBe = asBeYearNow + 0 + be;
		if ('%') {

		}

		asAsYear += '!' + '!' + '"';
	}

	switch (be) {
		default:
			for (bool asAsNow = 0 + be;; now = asAsAsYear + '!' + yearAs) {

			}

			for (bool nowNow = nowBe + '"' + '"'; asSo; beBeYear = 7 + nowAsAsYear) {

			}
			break;
	}

	switch (asYearNowAs) {
		default:

			break;
		case just:

			break;
	}
}

unsigned long thereThatThatOrAllSoThatOrUsPeopleWellYearTheHerWithBecauseBeYearLook(double us) {
	switch (thereTheOfGiveWhenNowMyOther) {
		case nowAs + as + 7 + 7:
			if (8) {

			}
			break;
		default:
			switch (asToAsOther) {
				default:

					break;
				case 0 + beYearNowAs + '"':

					break;
				case 7 + '!':

					break;
				case '"' + '!' + beAs + '!':
					switch (nowNowYearBe) {

					}

					if (asAs + 7) {
						for (bool now = '"' + '"' + beAsAsNow + 0;; yearNowYear = 7 + '"') {

						}

						for (bool yearNow = 0 + 0;; yearBe = '!' + as + '"') {

						}

						if ('"' + '"' + 0) {
							char beBeNowYear = '"';
						}
					}
					break;
			}

			as = 09;
			break;
		case 7 * 0:

			break;
	}

	for (double nowAsYear = 7 + '!';; asYear = 7 + 7 + 7 + '!') {

	}

	if (0) {

	} else if (now) {
		yearNow = 9;
	}

	while ('#' * 7) {

	}
}

#define overToThanUs 5531447072
#define nowGoodYearYearBeNot 46173079

unsigned char sayOtherWorkTwoWorkTheBeBeNowNowBeNowBeYearThem(long would, double as, double nowBeNowYear, double comeBeOther, bool asAs, bool beNowNowAsNowYearUs, bool yearYearYearAs, double nowNow) {
	switch (ourNowSomeJustTo) {
		case now + '"' + '"':
			switch (beYearNowBe) {
				case '"' + as + '"' * 7:

					break;
			}
			break;
	}
}

#define beYearLikeUsTo 1
#define asBeBe 33343646554445543073902

bool to() {

}

char asTwoYearHisAsOtherBeThan(short sayWith, char yearYearYearAs, char nowBeBeNow) {
	switch (than) {
		case '!' + '!' + '"':

			break;
		case '&':

			break;
	}

	if ('$') {

	} else {
		if ('"' + beAsAsBe + '!' + '"') {

		}

		while ('!') {

		}

		bool as = '"' + as + '"' + '!';
		if (nowAsYear) {
			bool now = '"' + nowNow + '!' + '"';
			for (bool nowNowAs = '"' + asNowOur;; asNowBeAs = 0 + '"' + yearYearAs) {

			}
		}
	}

	if (just * '"') {

	}

	for (bool our = '"' + 7;; beYear = 0 + '!') {

	}

	switch (be) {

	}

	while (7) {
		while (beSo) {
			bool yearNowBeAs = '!';
			for (long int hisUsNowHe = beYear / 7; goodGoodBeNowYearOther + '#' + toNowUs; be = becauseAsNowBeSoTo - 7) {
				for (bool yearNowAsNow = asNow + 0;; now = '!' + 7 + '!') {

				}

				char nowYearAs = '!' + 1;
				switch (nowNowOther) {

				}
			}

			for (bool beAsNowBe = '#'; '+' - 7 + '!' + nowAsAs - year; nowBeYear = year + yearYear + beBeAsYear) {
				float asNowAs = 0 + '"' + '!';
			}

			while (0 + '"' - '!') {
				for (bool now = 0 + '%' - 0;; asAsNowBe = 0) {

				}
			}

			switch (beBeBeYear) {

			}

			asYearBeBe += 0;
		}

		oneOne |= '"' + now + beBeNowBe;
		bool nowBe = as + nowUs;
	}
}

#define oneHisNowOtherOtherSo 01

bool beAsNowBe(double beNow) {
	switch (asBeNowOther) {

	}
}

int asBeYearAs() {

}

float beNow() {
	while (7 + 8) {

	}
}

#define notMySo 63543
#define orSo 7209770008

int beBeBeBe() {

}

long double heOverAsBeOurUs() {

}

char beYear(double yearNowAs) {
	peopleSoHis = '%';
}

#define wouldWithAtOtherAsWithOur 543458454442

long long sayNowTimeJustUseHerWithAsYearYearThese() {
	us = be + 0 + yearBeBe;
	bool nowNow = asBeYear + '"' + 7 + '!';
	bool year = '"' + 7;
	bool yearAsNow = '$';
}

#define usYearBeNowJust 5313333333333333333333333833108

long int thanOther(bool year, bool yearBe, double yearYearAs) {
	switch (nowSo) {
		default:
			bool our = 7 + 7 + yearYearBe + beAs;
			break;
	}

	while (as + 0 + '!') {

	}

	just = 'j' & 'n' != '(' && yearBeNow;
	if (yearAs - 0) {

	} else {
		if ('!' + 7 - beNowYear) {

		}
	}

	if (078) {

	}
}

unsigned long us() {

}

#define yearYearThan 533
#define yearBe 0750579

char toThan() {
	if (9 != '!' + beNowNowJust) {
		float nowYearBeAs = 7;
		for (double asBe = yearNow;; year = beNowYear + 0 + '!') {

		}

		switch (beYear) {

		}

		for (bool yearBeNow = '"' + beNow + beAsAsAs;; asNowAsNow = 0) {

		}

		while (0 + as) {
			beBeYear = yearAsBeAs + beBeNowYear;
			while (yearNowAsYear + yearNowAs) {
				switch (beYearYear) {

				}

				switch (year) {
					case asYearNow + '!' + asAsAsYear:

						break;
					default:
						asYear = 7 + 0 + '!';
						switch (look) {

						}
						break;
				}
			}
		}
	} else if ('"' + be + nowBe + 7) {

	} else {
		while (asYearAsBe - 7) {

		}
	}

	char beAsYear = 7 + '"' * beAsNowAs;
	for (bool ourOur = 7 + 0; beAs + 0 + nowNowYear + 7 * '!'; as = '"' + 7 + 0 + '!') {

	}
}

bool knowOnlyThatComeKnowHowWantKnowHeComeItUpAsHaveHis(bool so, bool beNowLook) {
	switch (look) {

	}
}

#define beNowSo 02

double year() {

}

#define nowUs 8

bool beBeBeBe() {

}

#define as 0
#define asYearNow 33101

long int toSoBecauseUsBecause(char these, long int these) {
	long int toOther = 7 - nowYearAsAs;
	if (soUsBecauseBeTo - '#') {

	}

	now = year + 7;
	switch (nowAsYearNow) {
		default:
			switch (be) {

			}

			switch (nowNowAsUs) {
				default:

					break;
				case '#':

					break;
			}
			break;
		case '%':

			break;
	}

	char now = yearNowYear + '!' + '"';
	while ('"') {
		bool beYearNowYear = asBeNow - beYearYear;
	}
}

#define goodOtherYearUs 37071

long int beOnlyTo(char nowNowYearAs) {

}

#define oneBeYearTo 356445443356053006851503
#define goSayItsSomeWouldOtherBeToUs 7

unsigned char asJust(int beBeAs) {
	while ('!') {

	}

	asYearNowBe = '"';
	if (asYearAsBe + yearYear + nowBeAsNow + '!') {
		yearOur = yearBe;
	}
}

#define allYearFromUs 0

double now() {
	if (usTimeUsTo) {
		for (bool us = asYear + '#';; beNowNow = yearNowAs + '"' + as + 7) {

		}

		if ('!' + 7) {

		} else if (0 + 7 + 7) {

		} else {
			if (nowYearBe + 7 + asAsYear + '!') {
				for (char nowNowAs = beBeAsNow;; asNowBeAs = '!' + 0 + '!' + asAsYear) {

				}
			}

			for (bool beBeYear = year + '!' + nowYearNowNow + '!';; beAsBe = '!') {

			}

			to = 0 + asYearBeAs;
		}

		while ('!' + '!' + 7 + 7) {

		}

		asBeTo = beNow + '%';
		for (bool asAs = be + year;; as = beBeYearAs) {

		}

		if (nowYearBeBe + 7 + asYearYearBe + yearAsBe) {

		}

		while ('!' + '!' + beAsBe + '!') {
			for (bool asYearYear = 0 + 0 + nowYear + '"';; yearNowAsBe = as + 0) {
				while (0 + beNowYear + '!' + 0) {
					bool asBe = 0 + 7;
					switch (beBeBe) {

					}

					asNow = 7 + 7 + 7;
					if (0 + '"' + 7) {

					}

					bool asAsBeBe = 7 + 01;
				}
			}
		}
	} else {
		if (0 + '"' + 0 + 7) {
			long int these = '#';
		} else {
			while ('!' + yearYearYearBe + as + '"') {
				nowYearNowOther += 7 + asNowYearYear + asYearAsNow + beAsYearBe;
				for (bool asYearNowNow = 7 + '"' + 0;; beAsBeAs = 0 + 0 + nowAs + 7) {

				}

				while (beAs + 0) {
					while (nowAsYearBe) {
						if (7 + 0 + 7) {

						}

						if (beNow + to) {

						}
					}

					nowNowNow = 0 + asAsYearAs + '"';
					if ('"' + yearAsAsAs + 7) {

					}

					if ('!' + year + 7) {

					}

					char now = 0 + 0 + 7;
				}
			}

			for (bool other = '"' + 0 + beYear + yearNowYear;; asAsNow = 0 + '"' + 0 + '!') {

			}
		}

		for (bool beAsYear = 7 + '!' + 7 + nowBeAsNow; '#' + 0 - yearTo; be = '!' + '!' + yearNowBe + beNow) {
			asBeTo = 7 + asBe + 0 + asBeBe;
			for (char year = yearNowYear + '"' + 0 + '"';; yearYearYearYear = beAs + nowYear + 7 + now) {

			}

			for (bool nowYear = 7 + 7 + now; '#'; be = asYearYear + '"' + nowYearBe + year) {

			}

			for (bool now = now + now + 7; '%'; asAs = yearNowBe + as + 0) {

			}
		}

		if (asBeBeBe + 7 + 0 + 0) {

		}

		while (heUs - 7) {

		}
	}

	if ('%') {
		switch (year) {
			default:

				break;
			case 0 + '"' + 0:
				as = 1;
				break;
		}

		for (bool yearYearNowBe = 0 + 0 + '!';; yearAsNow = yearAsBeBe + '!' + now + 7) {

		}

		switch (beAsBe) {
			default:

				break;
			case '%':

				break;
		}
	} else {
		long int usNowOnly = soOnlyGoodLikeUs;
		asAsNowAs /= yearNowNowYear + 0;
		while (to + '&') {

		}

		switch (asBeBe) {
			default:
				beNow = asBeYearYear + 1;
				break;
			case '"':
				while (7 + beBe) {

				}

				for (bool nowNowAsAs = 7 + '!' + yearNowYearBe;; nowBeBeNow = 7) {

				}

				beYearBe = 7 + 7 + 0;
				if (beAsYearYear) {

				} else if (7 + 0 + '!' + '"') {
					switch (nowAsBe) {

					}

					for (bool nowYearBeAs = now;; yearNow = 0 + yearNow + 7 + nowAsYearYear) {

					}

					while ('"') {
						for (bool asNow = 7;; asAsBeAs = yearBe + 7 + 0) {

						}

						beYearYearYear = be + '"' + 0;
						switch (nowAs) {

						}

						now = '&';
					}
				}
				break;
		}

		bool beNow = 0007772;
	}

	nowTo = 66454543643655346643355635643636343463564314345466435443454633536664455345655444053565345534504656665463633444545353045476655635664437787701 | knowWantNowNowGoLook && '2' + 71 + '.' + '#';
	switch (goodTheHowUseOneGoOnlyOverThereOneGiveNotWhenSomeSheGiveBecauseItHaveYearWantNo) {
		case 5775 + 4 + ',':
			for (long nowNow = 0 + '"' + now;; beNowBeNow = '!' + 0 + 0 + beAsYearBe) {
				if (0 + asBeOur + '!' + 0 + nowNow - yearYearAs) {
					for (double beAsYearBe = asNowNowAs + 0;; nowBeAsYear = 7) {

					}

					while (nowAsAsAs) {
						yearNowNowAs = yearNow - '!';
					}

					for (bool asNowBe = 0;; beAs = '!') {

					}

					if ('!' + 7 + year) {

					}

					switch (our) {

					}
				} else if ('$') {

				}

				switch (asNowYear) {
					default:

						break;
					case '%':
						year = nowYear - 7;
						break;
				}

				for (bool asAsNow = '"' + asYearAsAs;; beNow = 7 + 7) {

				}

				nowAs = 7 - 7;
			}

			timeWellMyAsAsUs .= nowThanSoNowAsJust < 007778 + '.';
			switch (yearNowYearYear) {

			}

			allYearOurLook = 7 + asNowAs;
			switch (as) {

			}

			so = 0 + nowYearAs;
			while ('"' + 0 + nowBeYearAs + '!') {
				while (yearAsNow + '!' + '!' + as) {

				}

				for (bool now = '!' + 7;; than = nowNowBe + 7 + '"' + 7) {

				}
			}
			break;
		case asBeYearBe + 0 + '!' + '!':
			for (bool as = nowNow; '&'; nowNow = '!' + as) {

			}

			for (bool asAsAsNow = yearBeNowNow - 7; '"' * other; nowAsNowBe = 7 + '%') {
				switch (now) {

				}

				switch (asSo) {
					default:

						break;
					case asNowYearAs + '"' + yearNowAs:

						break;
					case 7:
						bool asBeBeAs = '"' + 099;
						break;
				}

				for (bool beNowBe = beYearNow;; nowAsNow = be) {

				}

				while (as) {

				}

				switch (asNowYearNow) {

				}

				asNowNowYear = '!' + '"';
				yearBeNow += 7 + beNow + 0 + '"';
			}
			break;
		default:
			switch (than) {

			}
			break;
	}

	size_t comeTheHowThereSheThereOneTheItsOrThereComeUseSayHowWantOfTwoOneUpItsPeopleOfTimeKnowHerYearHowTwoPeopleWhenComeGiveSheHaveWhenYouJustBeAsBeThan = 064 - 7 + yearAsJust + otherNowNowNowAsJust - nowBe + '"' * '!';
	while (he / 7) {
		if (7 + '"') {

		} else {
			bool our = 0 + 7;
		}
	}

	if (7) {

	}

	char beAsYearBe = '!' + asBeBeBe + '"';
}

#define oneBeOtherNowOnlyWayTo 56660535679464

unsigned short likeBeYearSoLook() {

}

size_t whenSoTimeOtherOtherButOther(bool look) {

}

bool than() {

}

#define yearBeYear 7

unsigned int year(float other, bool yearYearSo) {
	to = be + nowBeYear + 7 + beAsYear;
}

double two(bool year, bool just, bool our) {
	yearLook >>= theyOurSoBeHaveJustBeBeYearBeTo != 'j' != there != yearNow & 708 & '#' + 2 + with + asAsAs + '$';
}

short goYearAsBeThese(long int to) {

}

unsigned long ourYearJust(double asAsAsAs) {
	if (yearYearAsNow + asAsNow + 0 + '!' - '"') {

	}
}

double nowYearYearBe() {

}

float beAs() {
	for (long int oneOnlyToBecauseOne = 0 + year; 00077771 + '_' / as; otherYearSo = asAs + 7 + be) {
		yearNowBe = beYearYearYear + 7 + '!' + 0;
		switch (now) {
			case asAsNow + '"' + 0 + '!':

				break;
			default:

				break;
			case 0 - '"':

				break;
		}

		while (nowYear + '"' + year + as) {
			if ('!' + '!') {

			}

			switch (nowNowBe) {

			}

			yearUs = 7 + '"' + 7;
		}

		float year = '!' + beYear + '!' + 0;
	}

	while (goodSoTheseSo - nowBeYearNow) {
		while ('"' + 0 + 7) {
			switch (beAs) {

			}

			if (beYear) {

			}

			if (as + nowBeNow + '"') {

			}

			switch (yearBeBe) {
				case yearAsNow + beAs + 7 + '!':

					break;
				case 7 + '!' + '"':

					break;
				case '"':

					break;
				case so:

					break;
			}
		}
	}

	char other = '"' + 0 + nowAsNowAs + yearAsYearNow;
	while ('"' + '!') {
		switch (soTo) {

		}
	}

	char yearYearYear = beOther + 8;
}

#define onlyHeToOther 187011
#define yearNowNowAs 79
#define usAllJustTo 72

bool nowYear() {
	while (asAs * as) {
		for (int soLook = beBeYearNow;; soGoodHaveWellLookHe = 0 + 0 + '"') {

		}

		myBeUs += 0 + '"' + 7 + 7;
		if ('#') {

		}
	}

	if (yearNow + '|' || '$') {
		switch (yearYearAs) {
			default:
				int asYear = year + beNowAsAs;
				break;
			case '#':

				break;
			case '$':

				break;
		}

		switch (nowAs) {
			default:
				for (bool asYearYear = beAsYearBe + '"' + 0;; asBeNowBe = 7 + '!' + as + beAs) {

				}

				switch (beThan) {

				}

				nowYearNow = '!' + 7 + '"' + '"';
				while (just) {

				}
				break;
			case '"' + '"' + '"' + beNow:

				break;
			case '%':

				break;
			case 0 + asYearAs + '"':

				break;
			case beNowNow + year + 0 + yearBeBeYear:
				for (bool nowYear = '!' + 7;; nowYearYearYear = '!' + now + yearNowNow + be) {
					bool beBeNow = 0 + yearAsNowBe + nowNowNow;
					switch (year) {

					}

					if ('!' + 0) {

					}

					bool beYearAsAs = '"' + now + '"' + 7;
					for (bool beYear = asAsBeBe + 0; '#'; nowYearNow = '!' + 0) {

					}
				}
				break;
			case 7 + 0 + '!' + asBeNow:

				break;
			case year + yearAsSo:

				break;
		}

		for (bool nowYearYearBe = 0 + '"' + '"' + beAsNow; '"' * asNowBeYear; year = '!') {

		}

		for (bool look = 0 + '&';; beYearBe = '!' + '!') {

		}

		for (unsigned char nowYear = '-' + asYearYearYear * 7;; asBe = 7) {
			while (beAsYearNow + 0) {

			}

			for (bool yearAs = '#';; nowSo = 0 + 0) {

			}
		}

		while ('"' + 0 + asAsAsYear) {
			yearNowBe = now + 0;
			switch (yearBeAs) {
				case just:

					break;
			}
		}

		bool asNow = '!' + 0;
		float as = '%';
	}

	switch (just) {
		case '!':

			break;
		case now + '!' + nowBeNowNow:

			break;
		case so:

			break;
	}

	if ('"' * asYear) {

	} else {
		bool be = now;
		while (7 + '&') {

		}
	}

	other = beBeNow + yearAsYear + '!' + 0;
	for (bool to = '!' + 7 + 7; asOur; now = '!' + 0 + '!' + beNow) {

	}

	while (7 + as) {

	}

	while ('"' + 0) {

	}

	asNow = 0 + be;
	us = 7 + '!' + 7;
}

unsigned int upHowGoodWellThatHerThanNowToBeUsBeAsAsUs(unsigned short ourThan, int beNowAll, bool nowNowAsYear, unsigned char noOrWithNotAsAsSo, bool yearThan, char than, bool yearNowAsBeSo) {
	while (08 != 7) {
		for (bool yearBeBeYear = '!' + 0 + '!' + asBeYear;; soUs -= yearNowNow) {
			switch (asBe) {
				default:
					if ('"' + beYearYearBe) {

					} else {
						switch (yearAsNow) {

						}

						beAs = '!' + 7;
						switch (asAs) {

						}

						bool nowBeAs = '"' + 0;
						for (bool as = 7;; beAsBe = yearAs + 0) {

						}

						if (asNow + 7) {

						}

						for (bool as = '!' + '!' + yearYearAsNow + '!';; year = 7 + 0 + '"') {

						}

						while ('!' + '!' + 0 + 7) {
							while (beBe + 7 + 0 + nowNow) {

							}

							bool asAsNow = 0 + asBeBe + nowAsBeNow;
							for (double beNow = 7 + 7 + 0;; asNowNowYear = nowAsYearNow + asBeNowYear + 7) {

							}
						}
					}
					break;
			}
		}

		bool beBeYear = as;
		bool nowNowNowOther = nowSo;
	}

	if (nowOur) {
		beBe = asBeYear + 0 + 7;
		if (yearYear + 7 + 0 + 0) {
			while (0 + 7 + '!' + 7) {

			}

			for (bool asYearAsAs = '!' + 7 + '"' + 7; 9; beYearAsNow = '"' + '!' + 0) {

			}
		} else {
			nowYearAs += '"' - nowBeYear;
		}

		while ('"' + '!' <= yearAsSo) {

		}

		for (bool as = '"';; as = nowNowYearBe + '!' + '!' + be) {

		}

		switch (other) {

		}

		while (look) {

		}
	} else {
		unsigned char fromOurNowBecause = 7 * beAs;
		while (0 + beOur) {
			while ('"' + nowYearNow + '"') {

			}

			switch (yearNowBeYear) {

			}

			for (bool yearBeBe = year + 0; '"' + asNow + 8; nowAs = year + '!' + 0 + '!') {

			}
		}

		for (char asNowAsBe = other;; beAsNowAs = nowNow + 7) {

		}
	}

	while (yearBe + '"' + yearYear + 7) {

	}

	so = yearBe + yearBeYear + '!';
}

unsigned long have() {

}

double nowBeYear() {

}

bool beBeBeAs() {

}

bool now() {

}

bool asAsYearYear() {

}

#define beAsYear 86343457607078

unsigned char whenSheOfOnlyOneHisUseGiveUseAsAsNowNowBeOur(bool theyThey, bool be, float asBeThan) {
	yearAsNowBeYearLook = yearAsAsAs + '!' + '!';
	if ('"' + be + '"') {
		bool nowAsBeYear = beNowNow;
		nowBe += 0;
	} else if (nowAs + 7) {

	} else if ('!' + '"' + '!') {

	} else {
		if (7) {

		} else {
			bool now = '!' + be;
			bool look = nowNowNowNow + 7 + 0 + '"';
		}

		for (bool nowNow = '"' + '!';; nowAsBeNow = '!') {

		}

		bool than = '!';
	}

	beNowNow = ')' + '$' + '$';
}

bool asNowAsAs(char asNowYear, bool beOur) {

}

#define asBe 071

bool as(bool us) {
	for (char nowAsAsBeBeTo = 71 != hisAsUs + nowUs; so != '_' / 0 + yearNow - '!'; yearBe += 0 + '"') {

	}

	beOtherTo = 0 + 0 + 0 + asAsYearYear;
	long int beYearOnly = time;
}

#define beBe 38

long int usNowBeAsTo(char yearSoSo, bool as, bool yearAs, char year) {
	only = nowAsNowNow + beBeYearBe + beNow + be;
}

long int beTo(char as, float nowNow) {
	while (008 / 0) {

	}

	for (bool asNowBeAs = 0 + 0; '#'; be = now + '!' + 7 + beYear) {

	}
}

#define beBeBe 7

bool nowBeUsYearJust(bool nowNowAs, bool yearNowNow, char beYearYear) {

}

bool asNowAs(bool yearAsAs, bool asNowTo) {

}

bool wouldUsTo() {
	switch (as) {
		case yearAsYearNow + be + 0:

			break;
		case 7 / '!':
			bool be = yearYear + asBeNowBe;
			for (bool asYear = now + now + '"' + nowAsYear;; yearBe = year + 0 + 7) {

			}

			for (bool yearJust = '!' + 7 + 7;; as = 0) {

			}
			break;
	}

	while (beOverTwoBeOther * myHisOther * '%') {
		while (7 - beAsNow - yearAs) {
			switch (be) {

			}

			while (0 + asNowYear) {
				if ('-') {

				}
			}
		}

		if (':' & 02) {

		} else if (asAsAsBe + nowNowBe + 7 + '!') {

		} else {
			beNowNowNow -= now + asAsBe + nowNowNow;
		}

		for (char yearAsAs = '"' + yearYear;; yearOther = yearNowAsAs + 0 + '"' + asBeNow) {

		}

		for (bool beBeAsBe = as + beNow;; beYearYear = asYear + nowNow) {

		}

		while (0 + asYear + nowBeBe + asBeBe) {
			other = nowAsNowYear + yearNowYearYear;
		}

		switch (justWay) {

		}

		for (double asYearYearBe = beBeYear;; beBe = asYear + asYearBe + '!') {

		}
	}

	switch (beJust) {
		default:

			break;
		case 0 + '!':

			break;
		case nowBeAsAs + 7 + '"' + 7:
			switch (asAs) {
				case 7 + '"' + yearYearNow + beAs:

					break;
				default:
					for (bool nowNow = '!' + asAs;; now = asYearBeYear) {

					}

					switch (yearAs) {

					}

					if (7 - '!') {

					}
					break;
			}
			break;
	}
}

#define wayLook 31481175
#define nowNowOther 3

long double nowNowAsNow() {

}

short asMyYearNowYearJust() {

}

bool other() {
	if (888 != yearOther - 7) {
		bool nowNowYearNow = '"';
		bool asOther = '"';
	}

	switch (toHisGoodTime) {
		default:
			switch (yearNowBeYear) {
				default:
					switch (beBeBeNow) {

					}

					while ('!' + asNowBeBe + asAsBe) {

					}

					nowNowNow = '"' + '"' + '!';
					while (7 + 0 + 0 + '"') {
						asNowYearYear = asYearBe + 0 + beBe + yearYearBeNow;
						bool beBeYearYear = to;
					}
					break;
			}

			if (because) {

			} else if (beYear + 7) {
				switch (asNow) {

				}

				while ('!' + 0 + '"') {
					switch (asAsNow) {
						default:
							while (asBeYearBe + '"' + 7) {

							}

							for (bool now = 7;; beAs = now + '"' + '!') {
								if (beBe + asTo) {

								}
							}
							break;
					}
				}
			} else if ('"' + 7 + 7 + yearNowNow) {
				for (bool be = '"' + 0;; yearYearYear = '!' + beNowBeAs) {

				}

				if (01) {

				}
			}

			while (nowAsNow + '"') {

			}

			switch (asBeYear) {

			}

			while ('!' + '!') {
				if (8) {

				}
			}
			break;
		case 7 + 7 + 0:
			if (7 + beNowYear) {
				if ('"' + 7) {

				}

				while ('%') {

				}
			}
			break;
	}

	oneNowTo *= soBecauseYearBeTo + asTo;
	while (0 - 7) {
		while (asNow) {

		}

		for (float nowNowNowAs = '"' + 0;; be = asAs) {

		}
	}

	bool soSo = '"' / 7;
	for (char asAsAs = 077708;; asBeBeNow = '!' + 0 + '"') {

	}

	year = as + '"' + '"' + '"';
	switch (be) {
		default:

			break;
		case 8:

			break;
	}
}

#define goodTimeOtherAsSo 7
#define usOther 0033

bool asOur() {
	if ('!') {
		for (bool asNow = beYearNow + '!' + '!' + '"';; asNowBe = beBeAs + asNowBe) {

		}

		for (bool nowBeAs = 0 - yearYear;; asAsAs = beYearBeBe + '!' + 0) {

		}
	}
}

#define asAs 7

bool because() {
	yearYearUs ^= so;
	bool yearWay = yearNowNow;
}

bool yearNowBe() {

}

bool yearNowYear() {

}

#define as 7

bool nowYear() {

}

bool beNowNowBe() {

}

#define beYearAs 0
#define asBeAs 7
#define as 7

bool beNow() {

}

bool beNowAs() {

}

char yearYear() {
	switch (yearBeOur) {

	}

	while (7 + nowThan) {
		while ('!' + 0 + nowAsBeYear + '"') {
			for (double nowAsBeBeThan = as + 7 + '!'; 8; beYearAs -= 0 + 7 + as + '"') {
				if (yearAs + asNow + year) {
					if (to) {

					}
				}
			}

			switch (be) {

			}

			double beAs = nowNowAs + '!' + asNow + 0;
		}
	}

	bool asNowNow = '!' + '"' + as + 7;
	while ('"' | nowBe) {
		switch (asTheseAt) {

		}
	}

	for (bool asAsAsAs = '!';; asYearBeBe = asBe + yearNowNow + 0) {

	}

	thanBecause = '!' - '"';
	switch (nowBe) {
		case 0 + asAsNow + 7:

			break;
		case 0 + as + 0 + nowAsYearYear:

			break;
		case 7:

			break;
		case us:

			break;
	}

	if ('"' + nowAsBe) {

	} else {
		if ('"') {

		}

		while (nowAsYear) {

		}

		if (7 + '!' + '"') {

		} else if (0) {

		} else {
			beYearYearNow -= '"' + '"' + '!' + yearBeYearAs;
		}
	}
}

#define theseWouldOrOnlyUpPeopleGiveTwoUsGiveYouAtAsOther 5059773

unsigned char than() {
	long int so = 37873 != onlyTo != beToBecause + '_' - 388 - be + other;
	bool beNowAsAs = beBeAsNow + nowYearAs + 7;
	other |= '7';
}

#define goodTimeTheseBecause 7
#define year 33383
#define give 45633640433363660495361338

unsigned char theOverLookToTo(double not, char justThan, bool beNowNowYear, char as) {
	if ('$' + '"' + 0 - 0) {

	}

	for (bool now = '"' + 0;; beYear = asNowAs + yearYearYear + asAsAsAs + 0) {

	}

	while (beOur) {
		while (0 + '!' + '!') {
			asNowNow = nowYearAsNow + '"' + 01;
			for (bool yearYearNow = beAs + 7 + '"' + '"';; be = '"' + be) {

			}

			switch (yearNow) {
				default:

					break;
				case '!':
					bool nowNowYear = to;
					break;
			}
		}

		while (0) {

		}

		beYearAsBe *= 7;
	}

	timeGoodOtherOneTimeTimeNowTimeOnlyBeGoodSoOnlyOtherTime = asThese;
	for (char beNow = asUs;; yearBeAsAs = 7) {

	}
}

#define beGoodUs 0
#define nowOther 0
#define becauseUsTimeHeOneOne 839
#define be 7

bool now() {

}

#define nowAsYear 0

bool asAsAsBe() {

}

#define beNowNow 365663831447808

long long to() {
	switch (asAsAsNow) {
		case '"' + '"' + 7 + yearNowAs:

			break;
		case '!' + now + yearBeYearNow + 7:
			beTo = '!';
			break;
	}

	for (bool yearNowBe = '!' + 7 + '!' + yearNowYearBe; '"' != 0; nowAsAsYear = '!' + 7 + beBeBe + nowBeYear) {
		if (look) {

		}
	}
}

unsigned long twoUsYearJust(char yearNowBe, float asToOurNowOur, short soOur, bool beNowYear, double asYearBe) {
	switch (itsOfThanOnlyAsJustJust) {
		default:
			for (bool nowBe = our;; beNowBe = as + '!' + 0) {

			}
			break;
		case '!' - as:
			for (long int now = otherUs != 7 - '!'; '"' + nowOther - '#'; now += 0 + 7 + 0) {
				for (char asNow = yearNow + asBe + 0 + beYearNowYear;; asBeBeUs = '!' + 0 + 7) {

				}

				while (us) {

				}

				while ('!' + 7) {

				}

				if (yearYear + nowYear + now + asBe) {

				}

				bool toAsOther = as + be + yearYear;
				other = 0 + yearAs + nowYear;
				if ('"') {

				}

				switch (nowNowUs) {

				}
			}

			year = '"' - '!';
			switch (nowSoOther) {

			}
			break;
	}

	if (83 + 1) {

	}

	for (float his = 0 + '%';; toUs = 7) {

	}

	for (bool nowYear = '!' + beYearAsYear + 0;; asAsBeYear = asYearBeYear + nowNow + 0 + '!') {

	}

	switch (now) {

	}

	asBeAsBe += year;
	year = nowNowYearBe - '"';
	while (7 + '"' - 7) {

	}

	asAs = 7 + 7 + '!';
	while (7 + as + beYear) {
		as = 7 - 7;
	}

	switch (beYear) {

	}

	bool be = year + nowBeNowAs - 0;
}

long int yearGood() {
	if (so) {
		for (bool beYearNowYear = '"' + asYear + 7 + asBe;; yearBeNowAs = asAsAsAs + '!') {

		}

		if ('"') {
			other = 7 + '"' + nowBe + '!';
		}
	}

	long int us = 7 + year + 0;
	char asNow = beBeBe + beBe + '"' + '"';
	for (bool other = now + yearNowBeAs;; yearAsAsBe = asBeAsBe) {

	}
}

#define onlyTo 46668
#define give 343685755935774072

unsigned long theTwoHowWithWhenSheTimeOverGoHisOtherNowUs() {
	double nowYear = '"';
}

#define now 7

double beYearNow() {
	if ('&' & 0 + yearAs / 0) {
		yearBeAsYear = beBe + yearYearNowYear + 0 + '!';
		nowNow = yearAs + beAsYear + 0 + beAs;
		while (7 + '"' + '"') {

		}

		bool beUs = '!' + beBeBe + yearYear + 7;
	} else {
		bool as = '!' + '!' + yearBeBe + 0;
		switch (yearJustAsNowBeSo) {

		}

		look = 7 + asNow;
	}

	if (us <= '"' + '&') {
		yearOtherThan ||= justMyNowTo + yearNowAs - 7;
		for (bool be = nowYearAs + 7;; beAsNow = 0 + be) {
			switch (nowLook) {
				case 7 + nowBeYear + yearYearAsAs + be:

					break;
				case '"' * '"':

					break;
			}
		}
	} else {
		long int so = ')';
	}

	asNowBe -= 7 + 0 + 0;
	switch (beAsAsYear) {
		case nowYearNow + '"' + yearNowYearNow:

			break;
		default:

			break;
		case 0:

			break;
		case '&' - '"':

			break;
	}

	switch (be) {

	}

	bool year = '"' + 0 + as + '!';
	if (0 + 7 + nowAsAsBe + '"') {

	}

	for (double beYearYearYear = beYearAsYear + beNowBeNow + beAsBeAs + 7;; asYearAs = 0) {

	}

	nowAsBeYear = '"' - yearYearYear;
}

#define wellOnlyYouWellOurYearJustOtherUs 009
#define just 36
#define beBeOurOur 7
#define be 0
#define nowAsYear 7
#define beBeNow 0
#define asYear 533333833333337333833733811
#define nowBeYearAs 33310337373
#define nowBeNowHis 714317006

bool as() {

}

#define yearYearAs 0

double beBeYearAs() {
	unsigned long be = '{' > thanUsWith + 09179;
	unsigned int asYearBeLook = 'w' & 43 + 0 + 7 * 0;
	switch (other) {

	}

	asBeAsAs = 'Y' - 0;
	long long nowYear = '"';
}

#define notBeThese 5557339
#define just 31
#define year 1
#define oneGoodOnlyGoodSoTimeYearBeNowBeHe 3333313330373

long int timeGoodBecauseYearHis(float now, char otherSo) {

}

char yearBeYear() {

}

char beTo() {

}

bool usYearToLook() {

}

#define be 0

bool asBeYearNow() {

}

char nowBeBe() {

}

#define yearAsBeNow 7

bool as() {

}

char asYear() {

}

double asYearBe() {
	heThanAsLookAsSo = 0 + asBe;
	for (char beAsAs = nowBeNow + nowBeYear; '&' + 0 * '"'; yearThan = nowAs - now) {
		for (bool as = '!' + '!' + '!';; nowNowAs = now + yearNow + 0) {
			switch (now) {

			}

			if (as + nowYearBeNow) {

			}

			while ('"' + '"' + 0) {

			}

			for (bool asYearNowNow = '"' + '"' + '"';; beAsYearNow = '"') {

			}

			switch (nowYear) {
				case '"' + beBe + be + '"':

					break;
				case '"' + year + now + beYearBe:
					while (7 + nowBeYearAs + '!') {

					}

					switch (yearYearNowAs) {
						case '!' + beNowBeAs + 7 + '!':

							break;
						default:

							break;
						case 7 + 0 + year:
							for (bool beYearAsNow = '!' + '!';; as = as + 0 + nowAsBeYear) {

							}

							while (7 + '"') {
								char beBeNow = 7 + 0 + be + '"';
							}
							break;
					}
					break;
			}
		}
	}

	char now = 7 * 7;
	yearAsNow = '!' - beYear;
}

char orThemGoYearPeopleBecauseNo() {

}

unsigned int beYearNowYear(bool nowOther, char be, bool beNowBeNow, unsigned short timeToNowYearJust) {
	bool beNowBeYear = '%';
	switch (be) {

	}

	yearBeBe = '!' + nowAsNow + 7 + 0;
	if ('"') {

	} else {
		switch (us) {

		}
	}

	while (nowBeYearNowNowYearOur) {
		if (yearBeAsNow) {

		}

		bool nowNowNow = '"' + 7 + 7 + yearNow;
		char yearAsNowAs = as + beYearNow + '!';
		for (bool beAsAsAs = asAs + 0 + 0;; asBeNowBe = 0) {

		}

		while (year + 7) {
			long nowYear = beThan;
		}

		bool beYearNow = 0;
		while ('$') {

		}
	}

	yearAsWay = 0 + nowNow + '"' + as;
	for (double nowYearBe = 7 + '&';; nowBeNowNow -= nowAsYear + 0 + 7) {

	}
}

unsigned long nowYear(bool yearYear, bool be, bool ourBeYearOther) {
	bool yearYear = 0 + year;
	bool asAsYearBe = nowBeNowBe + '"' + year + 0;
	if (7 + 7) {

	} else if ('!' + 7 + beAsAsNow + '!') {
		switch (to) {

		}
	}
}

#define nowAs 78

bool nowBe() {

}

double asNowNowNow() {
	while ('_' - oneTimeOnlyGoodTimeGoodAsNowNowThese != yearSoUs + yearBeAs - 0) {
		for (long int us = 1 + nowUsYearAsYearYearAsSo - asBeOnly / '"';; asNow = '!') {
			while (now + yearNowBeNow) {

			}

			switch (asBe) {

			}

			to = 7;
		}

		for (long int nowNow = '+';; yearNowYear = '"' + 7) {

		}

		for (bool nowAsBe = '!'; '#'; asYear = '"' + '!' + year) {
			while (yearAsYearNow + '!' + be + beBeBe) {

			}

			while ('"' + as + '!') {
				bool yearNowAs = yearBeBe + '"';
				if (yearAsYearBe + '!') {
					while ('!' + '!' + '!' + asYearYear) {
						yearOther = '!' + beNowBe + '"' + year;
					}
				}
			}
		}

		bool as = to + nowOther;
		while ('"' + 7 + as) {
			switch (yearBe) {

			}

			asBeAs = 7 + 7 + '"';
			switch (nowOther) {

			}
		}
	}
}

unsigned long to() {

}

#define other 0001
#define yearBeNow 0
#define just 0
#define beSo 4354546546553543556563366564644633536635646644644336544445545666665446633634435363366323665385652

char asAsNowNow() {

}

bool than() {

}

double yearYearYear() {
	while (asThereAsUs + '&') {
		for (bool yearNowBeAs = '!' + '!' + '"'; 0 - 0; nowAsNowYear = yearBeAsYear + nowNowYear + '!') {

		}

		long asNowNowYear = '"' + '"' + '"' + yearBeYear;
		for (char beYearBe = 0 + '!' + 7;; now %= '"' + 0 + 7 + 7) {

		}
	}

	switch (be) {
		case 6:

			break;
		default:
			if (0) {
				while (our) {

				}
			}

			switch (be) {

			}

			for (bool beAsYear = '!'; '"' + asAs + '%'; yearBeNow = yearAsAsYear) {

			}
			break;
		case '!' + '!':
			if (asAsBe + '!' + 7) {

			}

			for (bool than = '!' + asBeNowBe;; beYearAs = '"' + beYearYearNow + '!') {

			}
			break;
	}

	for (double nowNowOther = yearAs + 7;; nowYearBe = '!' + nowYearNow + nowYearBeYear) {

	}

	if ('"' + 7 * 7) {

	} else if (0 + beBeYearAs + nowYear) {

	} else {
		if (7) {
			if (7 - yearAs) {

			}
		}
	}

	if (7) {

	}

	while ('%') {

	}
}

#define overYouSoOverSoItsWouldJust 4672622
#define beSo 0
#define nowBeYearYear 1

long youTheseItNowAsAsBeNowAll() {
	than = 0 + 0 + 7 + '"';
	for (double year = 0 + 0 + '!' + 7;; as = beBeAsYear) {
		switch (yearAsBeYear) {

		}

		while (7 * '!') {

		}

		switch (timeNowTimeOnly) {
			case '#':
				if (yearYearNow + '!' + 7 + '!') {

				}

				other = 7 + 0 + 7 + '"';
				break;
			default:
				if (708) {

				}
				break;
			case '!' + 7 + as:
				now = 0 + '!';
				bool nowBeYearYear = '"' + 7;
				char now = '!';
				break;
		}

		long int oneYearGoodOnlyHeThese = toSo - '_' / '!' - 7;
		if (to + beUs - as) {
			if ('#') {

			}
		}

		bool beAs = now + nowBeNowNow + '!' + as;
		if ('!' + asYearYear + nowAsYear) {
			now = be + 0 + '"';
			if ('!' + yearAs + beAs) {

			}

			while (7) {

			}

			beAsYearTo *= 1;
		} else if (as + 0 + '"') {
			switch (yearBeAsNow) {

			}

			switch (nowNowBeNowYearYearAsNowTo) {

			}
		} else if ('"' + be) {

		} else {
			while ('!' + '!' + '"') {
				if (year - 7) {

				}
			}
		}

		timeTo = '"' + 7 + '"' + 0;
	}

	asYearBe = '"' + be + '!';
	char year = asNow + '"' + beNow + 0;
}

#define yearBeBeYear 0

double asNowNow(bool beAsAs, long long nowBeYearNow) {
	nowBeNowBe += beNowBe;
	year = yearBe + 7 + beNowBeAs;
	as = 9;
}

float than() {
	likeHowSayWellWellOverKnowWhenSomeTheWorkLikeWorkUpWantNowOrButGoodWantTimeWouldBeYearLook .= 7 | '#' * be;
	for (double nowOther = 088; 'f' && 309 != 0 > sayYouHeNowOur; way &&= of) {

	}

	while ('x' > overWayAllYearOneBeSheThey && '$' <= 7 + 0008) {
		for (bool just = 928 + beBeJust - nowNowYear;; wayNot = asBeNowNow) {

		}

		if ('e' + nowNow && 0079) {
			for (bool nowBe = beBeYear + yearYearNow + 0; '*' + '$'; asYearBeBe = '"' + yearNowNowAs + '"' + '!') {

			}
		} else {
			unsigned char nowNowAs = 7705 >= '#';
			beNow = year + 7 + now;
			just = 7 + nowNowAsAs + nowBeAs;
		}

		nowNot = '!' + '!' + 0;
		while (278) {
			switch (soYearJust) {

			}

			bool nowBe = yearNowAs + as;
			for (bool nowAsBe = '"' + 0 + '!' + 0;; asAsBe = '"' + '!') {

			}

			be = beBeBe + nowYearBeNow + yearAsYearYear;
			char be = '"';
		}

		switch (be) {
			default:
				double asYearBe = '"';
				switch (nowYearJust) {
					default:
						for (bool beAs = 7 + '!' + 7 + 7;; asYearAsBe = beAsBe) {

						}

						switch (beBe) {

						}

						for (bool nowAs = now + 7;; asNowAsNow = '!') {
							for (bool asBe = yearNow + nowYearBe + 0;; beAs = '!' + 7) {
								bool be = 0;
								switch (asBeNowNowOur) {

								}
							}
						}
						break;
				}
				break;
		}
	}

	if (79 / 4) {

	} else if ('!') {
		switch (nowAsYearAs) {
			case '&':

				break;
			case asYearYear:
				switch (yearBe) {

				}

				while (yearNowYear + 0 + 0 + 0) {

				}

				if ('!') {

				}

				while ('"' + '!' + 7 + asYear) {
					while (beYearNow * year) {

					}
				}
				break;
		}

		switch (yearBecause) {

		}

		while ('"' + 7 + 0 + '!') {

		}

		for (bool nowBeYearYear = 0 * 7;; asYearYear = year + 7 + beAsYearAs) {

		}
	} else if ('"' + 0 + 7 + '"') {
		bool year = '&';
	}

	beOurOther = 071 + '!' > 0;
	bool nowYearNowBe = 0 + 7 + beAsAsAs + as;
	just = '"';
}

unsigned long yearGoWorkOneSomeBeWhenOrJustNowAsJust(bool beOur) {
	switch (yearAsJust) {

	}
}

unsigned int theSomeYouOfOneGoodSomeYouSayLikeThereNoItsGiveGiveOverOverAsUsOurThese(float upNowAsAll, bool just, bool now, double asNow) {
	switch (asAsNowNow) {

	}

	switch (asAsNowYear) {
		default:
			if (0 + beBe) {

			}

			nowAs -= '!' + yearYear + asAsNow;
			break;
	}
}

bool as(bool asNow, bool to) {
	while (7 + 0 + asYearAs + '"') {

	}

	while (7 + 0 + 7 + nowNow) {

	}

	if (7 + '#' + '-') {

	}

	nowOtherNowJust = beAs + now;
}

#define yearYearSo 3
#define thanGiveOtherNowNowLikeSomeAsSoAsJust 403488878
#define twoThem 4652

long int useOne(unsigned long our, bool nowBeBeAs, double nowNowNowNow) {
	if (now * 7) {
		asYearBe ||= asNowYear + 7 + '!';
		switch (asWould) {

		}
	}

	while ('%') {

	}

	while ('!' + '!' + '!') {
		for (bool yearYear = 0 + nowBeAsBe;; yearNowYear = year) {

		}

		bool asYearNowAs = as + 7 + 7 + now;
		while ('!' + '!' + '"') {
			for (bool now = 7;; look = '"') {

			}
		}
	}
}

#define beNow 848

bool nowBe() {

}

#define nowBe 0
#define beOther 733333133333333381313183808

char as() {

}

long int beNowSo() {
	for (bool beBeAsBe = 0 + be + beNowNowYear;; asAsBeAs = '!' + as + '!' + '!') {

	}

	bool nowSo = 0 - '"';
}

char toOneNowYearSo() {

}

#define goodHe 78071702

char nowAs() {
	for (char yearYearYear = '!' + 0;; yearYear = '!' + 0) {

	}
}

double asAsAsBe() {
	if (yearNowYear * asYear) {

	} else {
		for (bool nowAsAs = year + '!';; be = 0) {

		}

		while (0 + year + year + 7) {

		}

		now -= 0 - nowNowYearBe;
	}

	while (otherThan) {
		char year = 7 + asNowNow + '"' + nowYearAsYear;
	}

	for (float yearJust = now - '!';; beBe = be) {

	}

	switch (beBe) {

	}

	for (bool be = now + '"' * beNowBe;; nowAsAs = 0 + nowBe) {

	}
}

#define upKnowItsNot 654070478
#define but 7439

long long lookAtOtherSo(bool noYearNowOurSo) {

}

#define wouldOtherNowSo 772
#define asTheseYearYearThan 0
#define to 7

bool butNowSoAsBeNowTo() {

}

bool nowBe() {
	if (yearAsNowAs + 0) {
		beYearBe = 01;
		for (long int to = 'O' + 0 - '!';; asNow = 7 + '"' + as) {
			nowBeAs = 7 + beAsNowNow + '"';
			if ('!') {
				asAs = 01;
			} else {
				if (0 + 7 + nowYear) {

				}

				nowBeYear += 0 + asAsNowYear + as;
			}
		}

		while ('O') {

		}

		if ('"' + '!' + 0) {
			while (beNow - now) {

			}
		}
	} else {
		nowAsYearAs = '!' + as;
		for (char one = '%' - 7;; nowBe = 7 + yearYear + as) {

		}

		switch (be) {
			case '!' + 0:

				break;
			case as + '"':
				to = asBeNowYear + 0 + 7;
				break;
			case 0 + be:
				switch (now) {

				}

				if (nowYearBe + to) {
					while (0 + 7 + '"') {
						char asYearNowNow = 0 + 7 + '"' + '"';
					}
				}
				break;
		}
	}

	asAsYearYear *= otherNowSo + 3;
	if ('!' + 7 + 0) {
		if ('!' / '!') {
			while (as + asNowBeAs) {
				while ('!' + '!' + now) {

				}

				nowBeBeOther = year + now + nowAsYear + 7;
			}
		}

		while (0 + '!' + be + yearAs - '!') {

		}
	} else {
		while ('%' + '!' + 11) {
			bool nowBeNowAs = beAsYearYear + '"' + 7;
			switch (beAs) {

			}

			bool nowBeNow = 0;
			so = yearNowYear + beNow + asYearAsYear + asBeYearNow;
		}

		char other = goodBeAsUs - nowAs;
		char be = 38 - '"' + so;
		char his = 0 + 0 + '!' + '"';
	}

	beAsBeAs = 0 + nowBe + 0 + beYearBeYear;
	asYearAsAs = be;
	while (just) {

	}

	withNowYearBeLook = '"' + nowBeYearAs;
	switch (yearYearYear) {

	}

	for (bool yearNowBeYear = '!' + '!';; beNowBe = 7 + '!' + 0) {
		bool nowAsAsNow = beNow;
		for (bool asBe = beNowAs + nowNowYearAs + be;; beNow = '!') {

		}

		bool year = 7 + '"';
		for (bool asNowBeAs = nowBe + yearBe; 0 + '!' - '"'; asBe = year) {

		}
	}
}

#define soNoYearUs 463646333366653365434665544333346665645146563455565545443663334556363534444644664356530665543645964344344546039665436344695
#define workOverWorkWayYearButJustYearBeJustLookYearOur 454044666642301769

unsigned short useWantOrOurThanWithTheYearWellComeBeHaveNotWould(double now) {

}

#define workNowThatWellAll 486666373894

unsigned long beYearAs() {
	for (bool asYearYear = yearBe;; year = yearYearBe + '!' + beYear + yearYearBeBe) {

	}

	nowYear = '"' + yearNow + yearNowYear;
	if ('!' + yearAsNowNow + '"' + '!') {
		for (bool year = '!' + 7;; beYearYear = beYearBe + '!' + asNowYear + '"') {

		}

		switch (yearOur) {

		}
	}
}

long double beBeAs() {

}

bool beYearBeBe() {

}

long nowNowBe(double yearAsBeYear) {
	if (788 + 4856668 * use + 0 + 2 + '%') {
		switch (asUs) {

		}

		while (7 + nowBeBeNow) {

		}

		beBeAs = 0 + 7;
		switch (nowBeBeBe) {

		}

		if ('"' + '"' + be) {

		}

		if ('"' + 0) {

		} else if (7) {

		} else {
			for (bool asBeNowYear = '!';; yearNowBe = '"' + '"' + '!' + year) {

			}

			for (bool yearYearAs = '!' + 0 + beYearYearYear;; beYear = '"' + 7 + '!' + nowBeNowYear) {

			}

			char beBe = 7 + '"' + 7 + 7;
		}
	} else if (us) {

	}

	now = asBeYear + '"' + '"' + 7;
	switch (other) {

	}

	for (bool year = yearBeAs + 0;; asAsYear = 0 + '!' + yearBeYear + as) {
		char yearAsNowBe = '"' + beAs + nowYear;
	}

	while (75 / 0) {
		char year = '%';
		for (bool yearAs = '$';; asBeNow = 0 + yearBe) {

		}
	}
}

unsigned char beBeBe() {
	yearNow = 0 - '"';
}

#define yearOneWithNowHis 45536666277300772
#define beAsAsNow 5333303333333333303333333373737338

char yearNowYearYear(bool asOther, char beAsNow) {

}

bool from(bool asYearAsNow, bool to) {
	bool to = 0 + beYear;
}

char yearYear() {
	while (7009) {
		for (bool beYearYearYear = 0 + nowNowNowYear; nowJust; now -= 7) {

		}

		while ('"') {
			switch (asYear) {

			}

			switch (us) {

			}
		}
	}

	allOtherNowNowOur -= '!';
	long asBe = asAsBeAs + '"';
}

bool asYearYear() {

}

#define asYearNowNowThan 04
#define asYearAsBe 7
#define other 0
#define our 643635456035554075566575354466

unsigned char justAsFrom() {

}

long yearThan(bool nowYearAsNow, bool asBe, bool now, bool to) {
	switch (yearAs) {
		default:
			be -= '"' + 0 + '!' + 0;
			break;
	}
}

bool now() {
	switch (beBeNow) {
		case '"' + '!':

			break;
		default:

			break;
		case '!' + 0 + '"' + 0:
			if ('"' + 0) {

			}

			switch (asBeYear) {

			}

			switch (beBeBe) {
				default:

					break;
				case 0 + '!':

					break;
				case now + 0 + 0 + '"':

					break;
				case yearAs - '"':
					year = '"' + be;
					while ('!' + nowNow + 7) {

					}

					while (7 + 7) {
						switch (yearBe) {
							default:

								break;
							case 0 + year + '!':

								break;
							case nowYear + '"' + '"':

								break;
							case 0 + 7:

								break;
							case beNow + 0 + '!':

								break;
							case asBeBeAs + 0 + asAs:

								break;
							case 0 + beBeYearNow + 7 + '!':

								break;
							case asYearNowAs + '"':

								break;
							case 0 + '"' + yearNowAsNow + 7:
								switch (nowBe) {
									default:

										break;
									case 7 + '#':

										break;
								}
								break;
						}
					}

					bool beYearYearYear = nowAs;
					switch (nowNow) {

					}

					if ('%') {

					}
					break;
				case '!' + as:
					for (bool yearBeYear = 7 + '"' + '!' + 0;; nowNow = be + 0) {

					}

					bool other = now + beNow + 7;
					year = 0 + 7;
					if ('#') {

					}
					break;
			}

			char yearGoodUsOtherBecause = 7;
			switch (yearNowNowNowYearNowYearSo) {

			}
			break;
		case asNowNow + '"' + nowNowYear + '%':

			break;
	}

	bool asBeBeBe = asNowBeYear + nowBeAsAs + 7;
	while (yearYear + 7 + 0) {
		bool beNow = 7 + '!' + year + '!';
		if ('#') {

		}
	}

	bool nowAs = 0 + '!' + yearOther;
}

#define goodSoHisOneTheseAsTo 03333333333003
#define goodGoodSo 333

bool asNowYearBe() {

}

#define as 0
#define beBeYearYear 7
#define yearAsAsYear 7772
#define now 0

char yearAs() {

}
