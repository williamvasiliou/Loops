float asBeBeAs() {

}

#define hisGoodToTimeAsBeTo 131373383071

char beNow() {

}

char timeBeBeSoTo(char yearHe, bool nowYearYearYear, bool nowAs, bool nowBeAsAs, float yearAs) {
	year = asBeYear - 7;
	char beBe = '!';
	switch (yearAsYearUs) {
		default:

			break;
		case '!' + 0 + beBe:
			for (bool yearAsAsAs = 7;; year = 7 + 7) {

			}

			nowBe = yearYear;
			as = '"';
			beNowBeBe = '!' + asNow + '"';
			switch (nowBeAsBe) {
				default:
					switch (asNow) {

					}

					switch (nowYear) {
						case 7:
							while ('"' + '"' + '"') {
								if ('%') {

								}
							}
							break;
					}
					break;
			}
			break;
	}
}

#define us 731373703708
#define itsLookSo 6
#define to 01

bool our() {
	while ('"') {

	}

	bool beNow = '!';
	switch (yearBeNow) {
		case year + '!' + yearBeBeYear:

			break;
		case '!':

			break;
		case be + asBeYearYear + yearNowYearNow:

			break;
		default:
			if ('!' + '!' + '!') {
				yearYearNowNow -= 3476650352 & 'c' != onlyBecauseOneOneTimeTimeOnlyAsOnlyGoodBeNowUsBeTimeOnlyBecauseYearBeUs - '#' - yearAs;
				timeOnlyOnlyGoodSheItsHowOneOrItsWhenSomeUpHerItItGiveThan >>= 'g' + be * asYearBe;
				for (unsigned long nowWhenOtherOtherNowFrom = 7 % 0; now * 2072; beYearYear = asNowNowNow + beBe + '"') {

				}

				if (5256045353596 + 's' - 'b' || 7 >= '"' % year + yearOther) {
					bool nowBe = '!' + yearBeAsBe + yearAsNow + asNowAsAs;
					switch (beNowJust) {
						default:
							bool beOur = nowNowJust;
							switch (beYear) {

							}

							nowAsAsBe = '%';
							break;
					}
				} else {
					unsigned long nowBeAsYear = '!' + 7 + '"' + 0;
					bool nowAs = '!' + asNowNow;
					beBeSo = 7;
				}

				to = '!' + '!' + 7 + 0;
				bool as = 7 + nowYearAs * as;
			} else {
				switch (sheTimeLikeTimeUpGoodJustItsNotTimeOnlyHerOnlyLikeHowThanThan) {
					case 347793:
						now = '!' - beNow;
						switch (noBeYearJust) {
							case 0 + asBeYearAs:

								break;
							case 2:

								break;
						}

						while (beBeBe + 7 + 7) {

						}

						while ('!' < '"') {

						}
						break;
				}

				if (8) {

				} else if (7 * asBeNowYear) {

				} else {
					for (bool nowBe = 7;; yearYear = '!' + beAsAs + '"') {

					}

					bool yearAsYearYear = now;
					asYearYear = 0 + 7 + '"';
					for (bool year = 71;; be = 7 + 0 + yearBe + asYear) {

					}
				}

				while (7 <= 0) {
					nowBe /= asAsNow + now;
					switch (nowUs) {

					}
				}

				if (asYearNowBe + 0 + '"') {

				}

				if ('"' + asNowAs + nowBe + beYearBe) {

				}

				if (0 + just + '!' - beYear) {
					switch (nowAsTo) {

					}

					switch (yearNow) {

					}

					for (bool asAsYear = be;; yearYearYear = '"' + 7) {

					}

					if (0 + 0 + 0) {

					}

					bool beLook = '!' + beBeAs;
				} else {
					char beBeAsBe = beNowNowAs + 0 + 0 + asYearNow;
				}

				unsigned short goodComeGoThese = 3111 || beYearAs;
				switch (yearBecause) {

				}

				switch (oneComeThatThatPeopleWellSomeUsOrUpAsHisYearWellBeNo) {
					default:
						asNowAs = 03;
						bool year = 0 * 0;
						break;
				}

				switch (beYearAsYear) {
					case other + '&':

						break;
					default:
						if ('!' + 0 + 7) {

						}

						switch (beAsBe) {
							case '"' + '!' + 7 + '!':
								while ('"' + asNowAsAs + yearYearBeNow + '!') {
									switch (nowUs) {

									}
								}
								break;
						}

						while (nowYearYear / be) {
							nowYearNowAsAsOur = beAsYearBe;
						}
						break;
					case 8:

						break;
					case '&':

						break;
				}

				while (8) {

				}

				while (0 * asBeNowAs) {

				}

				long us = beNowNowNow + nowNowNow + 7;
				switch (nowNowNowBeHe) {
					default:
						while (beAs + 7 + '!' + 7) {

						}

						switch (nowNowYearNow) {

						}

						while (0 + 01) {

						}
						break;
				}

				bool beYear = asAsBe;
				switch (yearBeBe) {
					case 0 + beNowNowNow + nowYearYearYear:

						break;
					default:
						switch (than) {
							case yearNowNowYear:

								break;
							case yearAsAs:
								asYear = '!' + 7 + as;
								switch (yearBeYearAs) {

								}

								switch (yearYearYear) {

								}

								bool be = '"' + '!';
								double nowAsBe = beBeYearYear;
								if (yearAsNow + 0 + '"' + beNow) {
									switch (as) {
										default:

											break;
										case '!' + '"':

											break;
										case yearYearAsNow + 7 + nowYear:

											break;
										case '"' * '"':

											break;
									}
								}
								break;
							case thanHis * 7:

								break;
							case 7:
								bool now = 7;
								while (0) {
									yearNowBe = asYearBeYear + asNowYear + as + 0;
									so = 0;
									bool year = 7 + now;
									switch (nowYear) {

									}

									asJust = beNowAsNow + nowBeYearAs + yearAsYear;
								}
								break;
							case as + 0 + '"':
								nowYearAs = 0 + 0;
								while ('"') {

								}

								look = be + '!' + yearNowAsYear + '"';
								break;
						}

						for (bool asBeNow = '"';; asYear = 0 + nowBe + '"' + '!') {

						}

						if (us) {

						}
						break;
					case '#':
						if ('"' - '"') {

						}
						break;
					case yearFrom:

						break;
				}

				double year = '"' + '"' + 0;
				yearBeAsYear = year + 0;
				while (7) {

				}

				if (asAsAs - '!') {
					double beBe = yearYear;
					for (bool nowYear = beAsAsNow + '!' + asNow + 0;; nowYearAs = 7 + 0 + '"' + beYearAsAs) {

					}

					for (bool yearAsNow = 7;; asYearYearOur = year + '!' + 0 + 7) {

					}
				}
			}

			if (465635633656346353447434053334455415 >= someThereOtherOverNowTheseUs > 55555552) {
				while (4336653072 / 0 / 0) {
					bool asWork = 7;
					if ('$') {
						switch (nowBeNowAs) {

						}

						be = 7 + '!';
						for (bool as = 0 + '"' + 7;; beAs = 7 + year + '!' + now) {

						}

						switch (nowYearYearThan) {

						}
					} else if ('!') {
						switch (nowAsBe) {

						}

						for (bool as = beNow - 0;; beNow = 0 + 0 + beAsAs) {

						}
					} else if ('!' + yearAs + asYearAsAs + yearAs) {
						for (bool as = nowNowNow;; just = '!' + 7 * asBe) {

						}
					}

					bool yearBe = be;
					while (yearYear + 0) {

					}

					if (0 + beAsBe + yearBe + beNowYear) {

					}

					bool yearJust = '"' + '"';
				}
			} else {
				beSo -= 0 + '!';
				while ('!' + be + asNow + yearYearNowAs) {

				}

				if (000073 * asOther + beBeYear - be) {

				} else if ('$') {

				}
			}

			for (bool nowBe = as + asNow;; asBeAsBe = 0 + 7) {

			}

			for (bool year = nowNow + 0;; nowBe = 0) {

			}

			be = asBeAsBe;
			nowYearNowBe += beAs + 7;
			break;
		case he || nowBeBeWould <= 09:
			while (year + 3) {
				bool youThese = be > that - 508;
				while (0 + 0) {
					be += '!' + yearAsBe + yearAs + yearBeBe;
					for (bool be = be + '!' + 7 + 0; '$'; beAsAs = 7 + year + now) {

					}
				}

				for (bool nowNowYear = 0 + nowNow + '!' + 7;; beNowAs &= '&') {
					while ('!' + '!' + now + now) {

					}

					while (beAsBe + yearNowBe + 0) {

					}

					switch (yearNow) {
						case 0 + '!' + 0 + 7:

							break;
						default:

							break;
						case nowNow:

							break;
						case 070001:

							break;
					}
				}

				switch (justSo) {
					default:
						switch (just) {
							default:

								break;
							case 9 + nowJust:

								break;
						}
						break;
				}

				if (nowAsYearAs) {

				}

				asAsBeBe = asAsAs * beBeNow;
			}

			switch (soWorkWorkAsWorkOneWithSayOther) {
				case 1 <= be:

					break;
				default:

					break;
				case asNowAsAs:
					switch (than) {

					}

					while (7 + 7 + nowAs) {
						switch (year) {

						}

						nowYearAsNow -= 7 + '"';
					}
					break;
				case '"' + beBe + 7:

					break;
				case asAsBeAs + beYearNowBe + 1:

					break;
			}

			switch (whenNowThan) {
				default:
					beBeAsBe = '"' * asYear + '!' * beYearBeAs;
					break;
			}

			while (year * '"') {

			}
			break;
		case knowKnowThereUpGoWantThereOneComeTheGiveWhenHerTwoYouOfKnowSheItsPeopleWellItsOnlyHaveWorkOneThereComeSayPeopleBecauseOrOurBeAsOther * asAsUp & 4569904 && '"' + 8:
			for (long int beThan = 'o' != 264553; 356567355635563566354543654574564495565192 || '!' * '!'; workKnowMyButOurNowWhenOther = beYear + 0) {
				bool as = asBeBe + '!' + asBeAs + '"';
				bool beYearAsBe = 41;
				for (bool beAsBeYear = '!' + '!' + 7; be % '&'; asBeAs = year + asYearBeNow) {
					for (bool be = asYear + yearNowAsBe;; asNowNowYear = '!' + 7 + '"') {

					}

					if ('"' + to) {

					} else if ('"' + '!') {

					} else if (0 * 7) {

					}
				}

				yearBe = '!' + '"';
				switch (asLook) {

				}
			}

			if ('!' + 0 + yearYearNowNow + 0) {

			} else if (7 * yearYearAs) {
				switch (asAs) {

				}

				asNow = 0 + beNow;
				if (beNowAsYear + '"' + asBe) {

				}

				just = 0 + nowAsNowYear + '"';
			} else {
				for (bool nowYearYear = 0 + '"';; beAsBeBe = 7 + '"' + 0) {

				}

				bool now = '!' + 7;
				while (now) {
					while ('!') {
						for (bool nowYear = nowBeNow + beAsBeNow; '!' + yearNowNow + 2; nowAs = '!' + as) {

						}
					}
				}
			}
			break;
		case 36644656666663465105 && '.' == 'z' == nowThanThere > nowFromOur * 0 * 7:
			for (short whenThatYearYouThemOther = yearBe <= 0; nowOurNowNowYearThan + '0'; beYear = yearAsNowAs) {
				if (0 + 0 + 7 + 0) {

				}

				while ('!' + '!' * yearYear) {

				}
			}

			if ('}' >= goodUsJustOrYearLook == 6446646646963654208 % '%') {
				switch (yearNowTo) {
					case '%':

						break;
					default:

						break;
					case nowNow + '"' + '"' + '!':

						break;
					case nowYearNowNow + '!' + asNowAsNow + '!':

						break;
					case beBeAs + 0 + '"':

						break;
					case asBeYear + 7 + 0 + now:

						break;
					case 0:
						beYearJust = 0 + '"' + beYearYearYear;
						break;
				}

				if ('!' + '"' + 9) {
					while ('#' + 8) {

					}

					to = beYear;
					asYearUs = asYearYearBe + beNow + year;
				} else if (than) {
					switch (other) {

					}

					while ('!' + asNowNow + '$') {

					}
				} else {
					for (bool nowJust = 7 + asYear; '"' + lookUs; nowAs = asBeNow + '!' + 0) {

					}

					switch (as) {

					}

					if (0 + yearYearNow + be + beAs) {

					}

					asBeJust = asAsYear;
				}

				if (beNowNow + '!') {

				} else {
					nowYear = '!' + 0;
					bool beNow = '"' + 0 + 0 + 7;
					switch (yearYearAsNow) {
						default:
							switch (now) {

							}

							bool year = 771;
							break;
					}
				}

				while (271) {
					if ('"' + 7 + beNowNowSo) {

					}
				}
			} else if (0 + beBeAsNow + '"') {
				bool asBeBeAs = '!';
				switch (thanOurSo) {

				}
			} else if ('&' * be) {

			} else if (0 + 771) {
				while ('&' + beAsNowYear - 7) {

				}
			} else {
				if (beAsBe + 0 + yearNowBe) {

				} else {
					beNowYear = '"' + 0 - 0;
				}

				for (bool nowBeBe = '"';; beYearBe = beNow + 7) {

				}

				bool nowYearNow = '!';
				double nowYearNowBe = yearNowNow;
				bool asNow = '"';
				year -= 7 + asYearYearAs + '"';
			}

			for (bool nowNowAsYearNowAsUs = '!';; year = 7 + 0) {

			}

			while (0 - asBe) {

			}
			break;
		case 7 | 66657066703:
			asNow = 7 + 0 + '"';
			asYearLookNowBeLook -= '.' - our;
			for (long long beNow = 'u' < 7 - '!'; just; goYearYearSo = 0 + 1) {
				switch (look) {
					case year + 9:

						break;
				}
			}

			if (asAsYear + 0) {

			}

			bool beNowNow = now;
			for (bool asAsBe = 0 + '!';; nowNowAs -= 0 + 7) {

			}

			for (bool year = now * 9;; nowAs = beYearBeBe + beYearAs + asYearBeAs + '"') {

			}
			break;
		case 7 + '&':

			break;
		case 466666612:
			switch (thanOther) {

			}

			switch (nowAs) {
				case 7:

					break;
				case 0:

					break;
				default:

					break;
				case '&':

					break;
			}
			break;
		case '!' + nowNowYearBe:

			break;
		case '!' + be:

			break;
		case 7 + nowAsAs + beBeYearBe:

			break;
		case 7 + 0 + nowYearYear:
			if ('!' + '!') {

			}

			yearAsBe = 0 + '"' + '"' + nowBe;
			switch (beThan) {

			}
			break;
	}

	lookHowWorkOfYouOurHer =~ 78 % 8772 * 7;
	while ('"' + '$' * asYearNowYear) {
		peopleWantAsNowHave = 7 + beNowYear;
		switch (yearBeThan) {

		}
	}

	unsigned short yearYearBeNow = '.' + asBeBe * beYearNow;
	long double yearAsNow = asYearOur;
	nowYear = '"' + yearBeBe;
	yearBe &= as + from + yearBeYear <= year;
	bool yearYear = 01;
	to -= '!' + 7 + asYearAs + '!';
}

#define peopleTwoJustOfHowGiveHaveWorkSomeOrLikeComeWantThereWorkOfTimeSomeUpShePeopleOfGiveTwoUpItsSheThereWellWellWantSomeUpSoYearItLookHerWantBeHe 65547664762
#define whenWantSheOrWhenToThatAllItHowFrom 0
#define as 30704
#define whenItSomeOneGiveItHaveOrOrThereItOrItsItsLikeWorkHerGiveComeLookThereUpOrSomeSheTheUpUpGoSheUpUseTwoUpGoSheSoGoGoWellComeThereThemTimeThem 665446641465564446
#define knowOur 8167555601

bool asAsBeYear() {

}

bool yearYearNow() {

}

bool our() {
	while (thanAsThem / 57071) {

	}

	if (nowYearNowBe < '"') {

	} else {
		switch (nowAsBe) {
			default:

				break;
			case yearYear:

				break;
			case '"' + asYear + '!':

				break;
			case '!':
				switch (asBeJust) {

				}
				break;
		}
	}

	switch (beBe) {

	}

	bool yearYearNowAs = '&';
}

#define onlyKnowBecauseTheSomeSomeGiveGoAsItsBeThanItsThanOurNowOur 5

long double he(bool just, bool as, bool beYearNowNow, bool to) {
	if ('"' - as - '"') {

	}

	if ('!') {

	}

	asAsAsAs = yearNowYear + asBeNow + '!';
	if (0 + 7 + '!') {

	} else {
		while (beNow + 7 + 7) {
			switch (now) {
				case beBeAsAs + '"' * yearNowAs:

					break;
			}
		}
	}
}

float yearBeNowNow() {

}

double nowNowBe() {

}

#define beBeOur 0

bool justNot() {

}

#define yearBeYearNow 0

bool nowYear() {

}

bool nowAsNowAs() {

}

#define yearNowNow 53333333833333
#define goodUsHeGoodGoodHisOneOneAsTheseYearSo 333405

unsigned char asBeNowYear(bool beThan) {
	for (char nowAsNow = now + 7;; asNowAsYear = beAs + 0) {
		while (wantAsThereSomeWhenWorkOverSoJustKnowWellWayBeYearJust || 09 * 7) {
			while (asYearAsBe * 0 + 09) {
				switch (as) {
					case '!' + '!' + '"':
						for (bool as = 0;; asAsNow = '&') {

						}
						break;
				}
			}

			switch (justOver) {
				default:
					for (bool beYearNow = 7 + '!';; beAsYearYear = '!' + '!') {
						bool beBeAsBe = nowNowBe + yearAsYear + yearNowAs + yearBe;
						switch (yearAs) {
							case '!':

								break;
							case nowBe + year + '"' + 0:
								while (7) {
									char nowNowBe = year + 0;
								}
								break;
						}
					}
					break;
				case nowAsAsBe + nowBeAsAs:

					break;
				case 01:

					break;
			}

			if (7) {

			}

			look = nowAsNowYear + yearNow + yearBeNow + asNow;
		}

		while (0 - '%' + now + otherUsAsUsBeUs + toBeSo) {
			if ('#') {

			}
		}

		if (602 && year + 009 + beNow - 0) {
			bool now = 079;
			while (beBeNow * nowBeYear) {

			}

			while (0) {

			}

			bool nowYearNowNow = 0 + '"';
			while (7) {

			}

			while (0 + '#') {

			}
		} else if (asBeYear || 04) {
			float asBe = '!' + '!';
			switch (nowBeYearAs) {

			}

			switch (ourUs) {
				case our <= to:
					switch (just) {

					}
					break;
				default:

					break;
				case 0:

					break;
				case '!' + nowBeYear + 0:

					break;
				case 1:
					bool beNowYearBe = '"' + '"' * be;
					break;
			}
		}

		sheNowGoodTheseTwoAllMy *= '!' + beBeBeYear + '"' + '"';
	}

	for (size_t someWorkOtherAt = nowTo * yearBeNow * '"'; 0091; yearYearYearOur = 7) {
		switch (now) {
			default:
				switch (yearAs) {
					default:

						break;
					case '%':

						break;
				}
				break;
		}
	}

	if (0) {
		for (unsigned short nowBeLook = yearNowBeAsAsYearAsBeLook;; as = '!') {

		}
	} else {
		if (7 + yearAsBeBe + nowYearBe) {
			char beYearBe = beNow + '!' + '"';
		} else {
			if (nowAsBeBe) {

			}

			while (year + '"' + '!') {

			}

			for (bool now = '"' + '!';; now = '!' + '"') {

			}

			year -= '!' + 0 + '"';
		}

		switch (otherSo) {

		}
	}

	while (nowAsNowAs) {
		long double yearYear = '"' + '"';
	}
}

#define thatNowAsSoHe 631445485
#define overGoodYouYouThatOurHaveOneJust 0766973000079
#define thanTimeSomeHaveGoBeOtherAllComeNowSoOur 371

int beNowYearAs() {

}

#define beTo 0

bool beThan() {
	switch (so) {

	}
}

#define yearBeAsAs 7
#define beNow 7

bool nowAsAsNow() {
	asBeYearLook = '"' >= '$' % '\'';
	for (unsigned int yearBe = 16 + '#'; 'i' != 43613102 - 0 - 0; now = '!' + 7 + '"' + '!') {

	}

	for (bool be = 079;; nowBeBeNow = 71) {

	}

	asAs -= '!' + '"' + 0 + 0;
	they = 0 + '!' + '!' + '!';
	asSo = 0;
	be = '"';
	us = 0 + nowNowAs + 0;
	while ('"' + '"' + 7 + 0) {
		char asBe = be;
	}
}

bool two() {

}

#define comeSayYouWouldOther 0
#define asNowNow 0

unsigned int now() {

}

char nowYearNowYear(bool yearAsBeAs, bool nowTo) {
	onlyGoodOneOnlyGoodGoodTimeGoodBeTimeAsBecauseOtherAsBeTo |= '#';
	long int oneGoodNowOneTimeTimeBeYearNowBeBeTo = '"' + yearBe + 7 + yearBeBe;
}

#define look 7

int otherLookBeFromLook() {

}

#define because 

unsigned int overBeHaveYouOverSayNoTimeSheOtherTheyAsAsNowOur(double overBeOtherNowBeAsAsYearAsNowUs) {
	if (7 + asYearYearYear + 0) {

	}

	soLook = '!' + asNowYear + '"';
	bool asBeYearNow = 7 + 0 + '"';
	yearUs = be;
}

size_t thereOur(char nowBeYearBe) {
	while (nowAs + '!') {

	}

	as = '!' + 7;
	while (nowAs) {

	}

	switch (nowBe) {

	}

	if (yearBeYear + beNowBeAs + beAsNow + '!') {

	}

	for (double year = yearYear + 0 + 0 + 0;; nowAsYearNow = 0 + '!' + '!' + 0) {

	}
}

#define asBeNowBe 4665364334754654511
#define soThan 799
#define asThereSomeBeBeTheyLook 36644545308037370088

long int onlyOneOnlyOneTimeOneBeGoodBecauseBeAsTo() {
	switch (asBeAs) {
		case '"' + nowAs + '"':
			yearYear = '%';
			to += 7 + beAsNowAs + year + 7;
			break;
	}
}

bool timeAsBeNowBeNowUs() {

}

char timeHe() {

}

#define soAsTo 100778
#define onlyGoodOnlyNowNotSo 7
#define yearOur 6469

size_t nowNowAs() {

}

bool asBeYearAs() {

}

char to() {

}

bool asYearAsNow() {

}

bool nowAsAs() {

}

bool nowYearYearYear() {

}

double yearYear() {
	switch (asThanLook) {

	}

	unsigned long upWouldYearItBeYearBeJust = nowOther;
}

long long becauseOur(char asBe, double noYearAsTo, unsigned long giveOurJust, double you, double yearNowTo, bool yearNowAs, size_t otherAsBeOur) {
	for (bool beYear = ')' + '&'; yearBeNowAs - 7; beAsAsNow = '"' * 0) {

	}
}

size_t becauseThatAtYearOther() {

}

bool than(bool to) {
	while ('%') {
		switch (because) {
			case '!':
				nowAsBeAs = asNowYearNow + 7 + '"';
				bool as = 8;
				break;
		}

		if ('"') {
			nowThese = now + '!' + 7 + '!';
			while ('!' + 1 + 701) {

			}
		} else {
			for (bool nowBeYear = yearAsNowBeYearNowYearUs;; be = nowNow + '!' + as + 7) {

			}
		}

		while (beYear + nowNow + nowYearYearAs) {
			nowOneYearBeAsAsTo += '\'';
			other = '!' + '"' + 7 + '!';
			beAsBeYear = yearBeYearNow + '"' + asYearBe;
			long int beOther = otherAsSo;
			while (asNowYear + yearNowYearNow + '"') {
				beYearAsNow = '!' + '"' + now;
				bool beNow = '\'';
			}
		}

		if ('%' + '"' + '%') {

		} else {
			for (bool nowAs = 7 + '"' + '!' + 7;; asNow = '"') {
				bool yearBeNowAs = '"' + '!';
				if ('#') {

				}
			}
		}

		if (0 + year + beAsNow + '!') {
			switch (so) {

			}
		}
	}

	if ('_' - '_' / 7) {
		char nowYearAs = be - 7;
		while ('"' - be) {

		}

		if ('O' / nowAs) {

		}

		long int as = '#';
		while (7) {

		}

		char yearAsAs = 009;
	} else if (591 - yearYear) {

	}

	if (they) {

	}
}

bool nowBeBeBeNowBeJust() {
	for (unsigned int yearNowTwoNot = be + '"'; 9; yearNowBeNow = yearNowAsAs + beNow) {
		switch (yearBe) {
			case '!':

				break;
			default:
				for (double asBeAs = now + beYearAs + 7 + '"';; now = '"' + as + '"') {

				}
				break;
		}

		for (bool nowAsAsNow = 0 + beAsNowYear + 7;; as = '!') {

		}

		if ('"') {

		} else {
			than = now + '!' + '!';
		}
	}

	switch (nowBeThemYearOther) {

	}

	while (to) {
		if (now + now) {

		}

		while ('"' + 7 + 0 + 7) {

		}

		double yearYear = 7 + 0 + 7;
	}

	sheSoThan = '$';
	yearYearNow = look;
}

#define  35653465556646555233645463644664443336343544636334533344554660853561761

int herKnowUseNowThanAtFromThese(bool asYearJust) {
	double nowYearAs = '!' + 7 + yearBeNowAs;
}

bool than() {

}

bool beBeNowBe() {

}

#define year 0
#define now 34343665430635564364

double fromUseAsKnowShePeopleAsNowYearOther() {
	if (0) {

	}

	while (7) {

	}

	switch (beYearYear) {

	}

	beAsYear = '!' + asYearNow + '"';
	switch (beAsAs) {
		case 0079:

			break;
	}
}

float yearYearYear() {

}

bool give(bool as, bool beBeAsYear, long double asBeAsAs) {
	switch (yearUs) {

	}
}

#define nowAs 43333333333333333333708
#define to 0
#define usYearTheseOneOtherTo 03831071
#define nowAsAs 0

unsigned int other() {
	switch (just) {

	}
}

double beYearNow() {

}

char they() {
	while (67543466733445415089 >= 8548 * us) {
		for (bool nowAsBeBe = '#' - '"'; asAsBeOur; be += year) {

		}

		if ('%' + 7343) {

		}

		if (asAsBe + asBeNowYear + 7 + '"') {
			double as = '!' + year + '"' + yearAsYearBe;
		}
	}

	for (long int haveThereUseThereHaveYouBeYouYearSheYearGoThan = '2' - 353506 + '$' > 771 + 0 - asBe; '!' < beNowBe; now = 0) {

	}

	unsigned long nowToBeThatToSoFrom = 34555636653655634545456349646610715 || 657543255738 >= onlyOverHowUseOverTheHaveOrOrGiveSomeBeOfSayYearNowSoTo >= 44435446543444535363422 <= 278 > '"' * 7;
	short soOnlyFrom = '!' + yearAsNow + nowNowNow + year;
	switch (year) {

	}

	for (char as = '"';; be = yearNowBeYear + 0 + nowYearBeYear) {

	}

	bool us = '!' + '!';
	while ('!' + 7 + 7 + '!') {

	}

	for (bool be = 7 + 0 + as; yearAsAsBe + '"' - 7; nowAsAs = asAsBe + '"' + beYear + yearYearAsAs) {

	}
}

bool hisHave(bool as, bool beAsBe, char yearAsAs, bool asTo, bool asNowYear, bool nowAsAs, bool year, bool asYear, bool beBeYearNowNowSo) {
	nowAsNow = '"' + 0 + '"' + '!';
	while (7 + nowNow + 7) {

	}

	switch (other) {

	}
}

bool justPeopleWellWith(short soNowAsNowNowJust) {

}

#define itsYearThan 3
#define our 7

int yearNowNow() {

}

#define asBeYear 0

bool nowNowNow() {

}

char asYearYearAs() {
	if (soAsUs) {
		if (02) {

		}

		switch (nowBeYearYear) {
			default:

				break;
			case 0 + 7 + us:

				break;
		}

		for (bool asBeYearAs = '"' + '"';; yearYearBe = 2) {

		}
	} else {
		switch (yearNowYear) {
			default:
				if (beBeYear + 9 / yearBeNowAs) {
					if (usYearAsBeOur) {

					}

					if ('"' + '"' + '"') {

					}

					if (7) {
						switch (asBeNow) {
							default:
								while (0 + '"') {

								}

								nowNowAs = asBeBeAs + beAsYear + 0 + 0;
								for (bool nowOur = '!';; beAsNow = '"' + '"' + be + 0) {

								}
								break;
						}
					}
				} else if (7 + '"') {

				} else if ('!') {

				} else if (at) {

				}

				switch (asBe) {
					case 0 + beNow:
						for (bool asNowOther = 71;; yearAs = 7 + 0 + '!' + nowAs) {

						}
						break;
				}

				asYearLook = asBe + 0 + beAsBe + '"';
				switch (yearAsYear) {
					case 7 + '"' + beYearYear:
						year = '%';
						break;
				}
				break;
		}

		if ('"' + '"' + beBe + asBe) {

		}

		double beBe = '!';
	}

	while ('"') {

	}

	switch (yearBeYearYear) {

	}

	switch (want) {
		case be + '!' + 0 + '"':

			break;
		default:
			if ('!' + 7 + '!') {

			} else {
				double nowBe = '"' + '"';
				while ('"' + yearAs + now) {

				}

				if (7 - 0) {

				}
			}

			double beNowBe = 7 + 7;
			break;
		case 7772:

			break;
		case '!' * as:
			asYearAs = '!' + year + beAs + be;
			bool nowYearBeAs = '"';
			for (bool year = now + asNowNow + beNow + 7; 7 + '#'; asNowAsBe = 7 + yearBe + nowYearYear) {

			}
			break;
	}

	switch (be) {

	}

	switch (beYearAsOur) {

	}
}

#define our 324563355
#define thereKnowYearHeBeNowYearYearNowThan 74671
#define yearYearNow 79
#define yearAsNowSo 9
#define beYear 0
#define yearYear 7
#define asNowYearBe 53331313108877001
#define goodUs 3833
#define yearYearSo 011

long int us(char he, bool yearAsAsBe, float beOther) {
	switch (beNowYear) {
		case 0:
			if ('!' - '!') {

			}
			break;
	}

	if (7 + asBe) {

	}

	switch (beAs) {
		default:

			break;
		case '!' + '%':

			break;
	}
}

#define asBecauseTimeOtherHeOneNowTo 8373003

bool asNowBe() {

}

bool nowNowAsNow() {

}

#define be 0

bool asNow(bool now, double our) {
	yearBe = '"' + 7 + '!' + '"';
	yearAsAsBeTo = asAs + year + '"';
	switch (nowBe) {
		default:
			switch (yearTo) {

			}
			break;
	}
}

#define asYear 4350439

char beYear() {

}

#define yearNow 597770708
#define withAsSayFromOurThan 5436463553355544024
#define than 61

bool nowYearNowAs() {

}

bool yearNowNow() {

}

bool asUs() {
	switch (timeYearNowAsBeSo) {
		case 3:
			while (7 + '!' + 0) {

			}

			bool asNowNowBe = nowYearYear + '!' + 7 + '"';
			for (bool now = 7 - nowBeNow; 7 - 0; asAsAsNow = '"' + '"') {
				for (char yearBeYear = '!' + 7;; asYearBe = beAsYear + 0 + '!' + '"') {

				}
			}
			break;
		default:
			asYearNowAs = '"' + '"' + '!' + '"';
			bool yearAsNowYear = '!' + '"' + be + 0;
			beToNowAsAsAsUs = be + '!' + nowAsAsNow;
			if (asBe + '!') {

			}

			bool beAsBe = 0;
			bool yearAs = 0 + 7 + 0;
			for (bool asNowYearNow = as + as + '"';; as = yearYear + be + asYearAsAs + nowNowNow) {

			}

			if (asBeNow) {

			} else {
				while (1) {

				}
			}
			break;
	}

	bool yearTo = '!' + 7 + '"';
	if (nowYearAsYear + 0) {
		switch (nowYearNowAs) {

		}

		bool to = '!' + nowNow + asNowYear + yearNowNowNow;
	} else {
		for (bool be = '"' + '!' + 7; yearYearAsAs - 1; asNow = 7 + '!' + be) {

		}

		if (08 + 78) {

		} else {
			while (8) {
				switch (nowBeNowTo) {

				}
			}
		}

		for (bool nowBeYear = '!'; '"' + 7 - '!'; yearYear = 7 + 0 + nowNow) {

		}
	}

	for (long int otherOtherUs = 0 + '"' + 7; '/' - 17008 != 8 / '+'; beNowNowYear = nowNow + '#') {

	}

	bool year = '"';
	for (bool be = beYearNowBe + nowNowBeYear + nowYearAs + '!';; yearNow = 0 + 7 + '"' + '"') {

	}

	for (bool be = 71;; asYearNowAs = nowBe + '"' + 7 + 7) {

	}

	bool beYearYear = '!' + beAsAsBe;
	if ('!' + be) {

	} else {
		for (bool beYearNowBe = 0 + '"' - yearBeBe;; nowAsNow = yearYear + beAsBe + '!') {

		}
	}

	char nowYearBe = nowAsBeNow + '!' + 0;
}

long int toOnlyAsOnlyBeOnly() {
	while (yearBe + 7 + '!' + 7) {
		for (bool asBeBe = 0;; asBeNowYear = yearYearBe) {

		}

		switch (so) {

		}
	}
}

#define now 770078

bool other(double asBeYearNow) {
	for (double beBeYearBe = asAs + '#';; as = 0 + 0 + '"') {

	}
}

#define ourThan 7
#define nowNowYearNow 0
#define asNowBe 00761

bool now() {

}

#define than 6303706
#define itGoWhenOverAsSoThey 5345664476584

unsigned int toNowAsLook(bool yearNowBe, bool other, bool asBeBeYear, bool nowJust) {
	while (toYearLook) {
		switch (nowAsAs) {
			default:

				break;
			case 2:

				break;
		}
	}

	switch (look) {

	}

	if (yearAsBe + 0 + 0) {

	}

	switch (nowAs) {

	}

	switch (asYear) {

	}

	for (bool yearYear = '!';; as = 0 + 7 + asYearBeYear) {

	}

	bool than = '"';
}

#define now 85

bool asBe() {

}

bool as() {

}

long nowBe() {

}

bool nowBeNowBe() {

}

#define now 0
#define yearAsNowYear 0

bool as() {

}

#define our 33333333138333333300303771
#define nowYear 0
#define usGoodNowOther 3331313366
#define yearAsFrom 536455643341365636555363785

bool with(char beNowThan, bool yearThan, bool yearYearOur) {
	for (bool asBeBeAs = 0;; beYearAsBe = '!' + '"' + 0) {

	}

	asYearNowAs = 0 + 7 + 0;
	for (bool beAs = 0 + '!'; nowYearNow % beAsNow; beYearAs = 7 + nowBeYearBe) {
		while (nowAs + 0) {
			switch (to) {

			}
		}
	}

	bool asAsNow = beBe + 7 + 0;
	switch (asNowYearAs) {
		default:

			break;
		case 9:

			break;
	}
}

#define as 01
#define nowBeYearBe 54426364466456335435565643
#define noJustTo 29874

bool be() {
	for (bool asBeBe = yearNowBeBe + 0 + year + asAsYear;; nowAsBe = '"' + 0) {
		if ('!' + asAs + as + be) {

		}

		nowAs = beAsNowAs + asAsAs + '!' + beBeBeNow;
		if (asOur) {

		}

		yearYear = '"' + beYearNow;
		if (7 + yearNowBe + 0) {
			nowAsNowYear = '&';
		}
	}

	while (to) {

	}
}

bool as() {

}

bool so() {

}

#define nowNowBe 7
#define asNowAs 0
#define look 33333333333333333333333333013031

bool now() {

}

float usAsJust() {

}

#define be 078

bool beNowNowNow(bool yearAsAsTo) {
	double yearNowYearYear = yearNow;
	while (beAsYear + '"' + '"' + 7) {
		for (bool yearNowBe = 7 + '!' + 7 + now; just; beAsBe = 7 + 0 + '"' + to) {

		}
	}

	switch (yearBeAsAs) {

	}

	asThan = '/';
	beBe = 777072 - year - '!';
	char justJustBeFrom = nowAs + beBeAsYear + yearYearAsAs;
	for (bool nowBe = 7;; yearAsAsBe = yearYear + '!' + '"') {

	}

	switch (nowBeBe) {
		case '!' + 7 + '"' + as:

			break;
		case be:

			break;
		default:

			break;
		case 0 + asBe + nowYearBeAs:

			break;
		case '!':
			while (nowNow + '!' + '"') {

			}

			switch (as) {
				default:

					break;
				case 7 + 7:

					break;
				case '"' + 0 + '"':

					break;
				case '!' + '"':

					break;
				case '!' + now + as:

					break;
				case 0 + beNowNow + 7:

					break;
				case asBe:
					us = 7 + beAsNowNow + now;
					break;
			}
			break;
	}
}

#define itsSomeSayThereMyThey 09

bool nowTo(bool year, bool as, bool asYear, bool yearAsBe, bool yearThan) {
	for (double nowBeYear = 7 + as + '"';; nowYearBe = yearAsYearNow) {

	}
}

unsigned char wellGiveBeUsBeAsToWould(double beThem, bool asYearYearBe, double asAs) {
	if ('%') {

	} else if ('&') {
		bool yearNowAs = yearYearBe + beYear;
		so = year;
	}

	if (beYear) {

	}

	while (asThan) {
		double nowYearAsAs = knowOur;
	}
}

#define to 0
#define yearBeSoAsYearLook 3311
#define onlyTimeOnlyTimeOnlyGoodTimeTimeTimeHisOneOnlyBecauseOnly 3833730701
#define onlyGoodAllBeSoYearNowOtherNowYearBeBeYearYearYearJust 375973
#define yearYearBeNow 7078
#define yearBeBeYear 7
#define nowBeNow 7
#define nowBeYearBe 0

bool yearNowAsNow() {

}

#define yearYearNow 535365343676563646863456643775349

bool nowBeYearNow() {

}

char howOther(bool beOur) {

}

char nowBeBeBe() {
	long int asBeOther = '_' != 3330318133 != '"';
	hisYearHeYearThese = '"' - '"';
	asOneOtherOther = '7';
	while (0 - 0 - '"') {

	}

	for (bool asYearAsNow = 7 + yearYearBeBe; 1; nowYearNow = yearAs + '!') {

	}
}

long int toHe() {
	bool us = beBe + nowYearAs;
}

long long heNowBeSo(double nowAs) {
	bool year = 0;
	for (bool beBeYearNow = as + '!' + now;; nowAs = '"' + 0 + 7) {

	}

	switch (nowNowYear) {
		default:

			break;
		case 0 + be + 0 + be:

			break;
		case as + '!' + '!':
			for (double year = beAsYearBe + asBeBeNow + year;; as = 0 + '"' + '!') {

			}
			break;
	}
}

#define beNow 7

bool beNowNow(bool yearBe, bool our) {
	for (bool workToBeLookWouldYearOther = 78; 8 + 0 + 1; as = as + 7 + beAsNowNow) {

	}

	if (7 + as) {
		switch (beAsNowUs) {

		}
	} else if (yearNow) {

	} else if (0 + nowBeYear + 0 + '"') {

	} else {
		asYearJust = 0 + 0 + '"';
	}

	for (char as = 7 + nowAsAsBe + 7 + '!'; 7272; asAsYear = asBe - 0) {
		other -= '!' + asBe + 7 + '"';
	}
}

#define theTimeGoTheKnowAsNowGoodYearThan 69356045336417391
#define asAsYearBe 7
#define notThat 7

bool yearNowAsNow() {
	bool nowAsNow = 70779;
}

#define nowNowBeAs 0

bool be() {

}

bool asYearNowNow() {
	nowBeYearBe = 0 + beBe + '"';
	while ('"' + 0 + 0 + beYear) {
		while (0 + yearYearAsAs) {
			bool asNowNowAs = 0 + asNow + asBeBe + 7;
			beTo = 7;
		}

		for (bool yearSo = 0;; year = '!') {

		}

		switch (now) {

		}

		while (beYearYearUs) {
			our &&= year + asYearBeYear + '!';
			if (asAsYearNow + as) {

			}

			be = 0 + asAsNow + 0;
			if (0 + '!' + '!' + '!') {

			}

			switch (beAsAs) {

			}

			if (7 + beAs + '"' + nowBeBeBe) {

			}

			year = nowNowAs + be + '!' + 7 - 7;
		}
	}

	unsigned long asBeNowBe = '.' | 2 + beYear - yearYearNow + nowNowYear + '%';
	unsigned short nowAs = 7 || '!' + 7 * year;
	asNowUsLook = 7 + 0;
	if (1) {
		ourUs = now;
	} else if (7) {
		switch (nowJust) {

		}
	}

	while ('"' + '!') {
		for (bool asAsNow = beAs + 7 + 7 + 7;; beAsAs = '!' + '!' + '!') {
			for (bool beYear = year; yearBeYearSo; beAsNowBe = '!' + asAsAsBe + nowBeAsYear) {

			}
		}
	}

	asSo = 7 + 7;
	bool as = '$';
}

#define asSoThan 477071
#define beAsNow 7
#define asThemJustTheyFrom 0
#define asYearThan 6465342004

bool year() {

}

bool yearNowAsYear(bool beNow, bool to) {
	for (bool beYearBe = be + yearNow + 0;; look = be + 7 + 0 + year) {
		bool now = '"' + asBeNowAs;
		bool nowBeAs = yearTo;
	}

	while (asBeNowBe != '/') {
		switch (beNowBeTo) {

		}
	}

	switch (asBe) {
		case '%':

			break;
	}

	timeHe = 0 + 0;
}

long int yearBeUs() {

}

#define beBeYear 30178
#define asBeUs 3333373830773878

long int onlyAsToYearSo() {

}

#define becauseOtherYearBeAsTo 78

char asNow() {
	if (078) {

	}
}

#define yearSo 7
#define nowAs 7118
#define nowAsBe 0
#define asUsYearWith 0

bool as() {

}

#define yearAs 63464443543146564356335343434

double be() {

}

#define nowBeYearLookThan 90929
#define as 3333333333333333383838

long int goodGoodTheseToYearYearOther(long int giveFromYearBeLook) {

}

#define other 7

bool nowAsYearBe() {

}

bool be() {

}

#define nowNowYearNow 63531535452765353639
#define soOne 4357318

bool beBeNow() {

}

long long nowBeYearTo() {

}

char yearBeNow() {
	switch (beYearAs) {

	}

	for (unsigned short goWantGoThereAsLikeWithThere = '&' + '!' - '"';; beAsBe %= 0 + asAsAsNow + '!') {
		switch (allThan) {

		}
	}

	bool yearYearOtherOur = as | just - '"' * '&';
	switch (nowTo) {

	}
}

#define thatAsBeBeAsNowYearOur 434336611

int yearNow() {

}

long double nowAsAs() {
	goodSoAsNowAsBeOther |= 01;
	switch (becauseOnlyUsOneBeToToAsOther) {
		default:
			year = asYearBeYear + '"';
			for (bool asNowYear = 7 + 7 + '"' + 0; 0 + '!' - be; now = 7) {

			}
			break;
	}

	if ('#' + asYear - '"') {
		if (asNowSo) {

		}
	} else if (3783 + 0 + toAsYearBeUs) {
		for (bool asBeNow = 0 + nowNowYear;; beNow = '!' + '"' + year + '"') {

		}

		long int asBeNow = 8;
		while (yearNow + now + 0 + '!') {

		}

		while ('!' + '"' + 0) {
			if ('%') {

			}

			so = '!' + be + 7 + yearYear;
		}
	} else if ('!' + '#' - '"') {

	} else {
		char beNow = 7 + nowBeYearYear - beAsAs;
		while (beAsYearNow + 7 + 7 + '"') {

		}

		switch (beOther) {

		}
	}

	if (11) {
		theseBeTimeBecause = be + yearYearBeYear + 7 + '"';
		if (toUs != '"' * 0) {
			bool yearAsBeNow = nowNowAs + '"' + '!';
			if (7 + 0) {

			}

			yearNowYear = 7 + '!' + '"';
			while (70071) {

			}
		} else {
			switch (beAs) {

			}

			for (bool beBeYearAs = '!' + now;; nowAs = '!' + 0) {

			}

			switch (beUs) {

			}
		}
	} else if (0 * 7) {
		while (nowNowNow + 0) {
			for (char yearBeNowNow = '"' + 7 + '!' + 0; '%'; as = 7 + 0 + 7) {

			}

			bool year = '"';
			yearYearAsBe += '"' + '!' + '!';
		}
	} else {
		bool beYearBeYear = to;
	}

	bool as = 7 + yearBeAs + beYear + beYearNowAs;
	if (one - 51) {
		while ('!') {

		}

		if (so) {

		}
	}

	while (beBeNowYear + yearYearNowAs + '!') {
		for (bool asNowNow = 7;; nowNowYearAs = 0 + '!') {

		}

		switch (yearBe) {

		}

		beAs = now;
		while (yearNowAsAs + '!') {
			while ('"' * 0) {

			}
		}
	}
}

#define workWorkOverOneOverHerOrJustTimeHaveOfUseOneGoodWantBeOverSo 3555517103

char yearAs() {
	unsigned char ofGoHeKnowOurAsBeBeLike = '"' + 7;
}

long long haveKnowFromWithTimeLikeToSoOurWould(bool yearNowBeAs, float asYearNow) {
	if ('&' + nowBeBeBe + 7 - '"' - 7) {
		int nowNow = 7 + yearBeYear;
	}

	if (so - 7) {

	} else if (year + asBeBe) {

	} else if (771) {

	} else {
		char yearNowYearNow = '"' + '!' + 0;
	}

	bool yearYearNowAs = '"' + 7 + asNow;
	for (bool nowBeAsYear = 076 + 1;; yearNow = '!' + 7) {

	}

	while (asBe + '!') {

	}

	bool nowBeYear = now + 7 + 7 + '!';
	bool now = 7 * as;
}

#define asButWayWould 7

char asAsNow() {

}
