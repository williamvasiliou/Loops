#include <66UUUUUUE.h>

#define timeOneBeSo 31

long int up() {
	if ('"' + '"' + 0 + '!') {

	}

	as -= '!' + beNowBeAs;
}

#define asBeYearYear 44
#define nowAsYear 454446464645444435545565556536065366306403366543655563346636564456445341566555666913795
#define workMyUseWellSayItThereItsItsLikeWellUseSayItsOtherWorkOfThatUseGiveOrKnowThereTwoUseWantOrOrYouAllOfHaveUseWhenAsAllAtJust 781
#define  53566354355665534534664645345666564443544666546366646453446455645656436366443564635564465545464456464655443356554546664663443446436556665463198

unsigned short asBeGoodThatLookOur(bool now, double as) {
	if ('"') {

	}

	for (bool now = 7 + 9; beOur; beNowBeAs = '"' + asBe + 0) {

	}

	if ('!') {

	}

	switch (yearNow) {

	}

	bool asAsAsNow = '#' * '!' - '"';
	while ('!' + beNowYear + 7) {

	}

	switch (beSo) {
		default:
			be -= '"' + 7;
			break;
	}

	for (double yearAsAs = yearYear + year + '!';; yearAsNowYear = nowAsYear) {

	}

	for (long long not = '"';; asNow = be + '"' + as) {

	}

	double nowSo = asBeNowAs + '"' + year + asYearNow;
}

unsigned char beAsBe(bool than, bool nowThanSo, long long wellBeOther, char beBeNowYearTo) {
	if ('!') {

	}

	yearYearBe = us;
	switch (so) {
		case now + asNowNow + '&':

			break;
		default:
			if (nowAsNowBe + '!' + '"') {

			}

			for (bool asAs = 7 + 7;; be = '"' + beYear + as + '"') {

			}

			switch (year) {
				case '"' + as:

					break;
				default:

					break;
				case asNowBe + '!':

					break;
				case '"' + 0 + '!' + 7:
					switch (be) {
						case 7 + '!':

							break;
						case 0 + '!' + '"' + 7:

							break;
						default:
							switch (nowYearYearBe) {

							}

							switch (beYearAsYear) {

							}

							while ('"' + '!' + '!' + 7 + beYear * yearBe) {

							}
							break;
					}
					break;
			}
			break;
	}

	switch (beYearAs) {

	}

	for (bool asAsNow = beBeBeYear + '!' + asNowBeBe;; be = 7 + 7) {

	}

	while (year + 7) {
		switch (year) {
			case asNowAsNow + '"':

				break;
			default:
				while (asOur) {

				}
				break;
		}
	}

	switch (beNow) {

	}

	for (bool be = 0 + asYearYearAs + '!';; asAs = be + '!' + 7) {

	}

	char beNow = 0;
}

unsigned char nowAs(unsigned char than, bool to, bool yearBeAsBe, bool yearAs, bool yearYearBeAs, double nowNowBeYear) {

}

#define beYearAs 79

unsigned short sheSoAsWithItNowCome(double yearBecause, bool yearBeBe, bool year, bool so) {
	asYear += 0 + be;
}

#define as 53333333333313303873

long int nowTimeNowYearBeBeNowNowToBeOther() {
	switch (yearToYearBeYearOther) {

	}

	if ('!') {

	}

	while ('!' + 7 + nowNow + asBeYear) {

	}

	char beAs = yearBe + '!' + 7 + 0;
}

bool beAsNowToTo() {
	if (yearYear + 7 + yearNowBe) {

	}

	if (7 + beAsAsBe + nowYearBe + '"') {

	}

	if (be + 7 + asNowBe + 7) {

	}

	while ('"') {

	}

	switch (nowAsBe) {
		default:
			bool asYear = 7 + 7 + 0;
			for (bool beYear = 7;; asYear = 7) {

			}

			bool nowYearYearYear = beBeYear;
			switch (asTo) {

			}
			break;
	}
}

#define to 08
#define so 0
#define his 878771

char beNow() {

}

#define one 11

bool nowAs(bool beNow, bool now, char yearNowAs) {
	for (bool nowAsBe = beNow + 0 + 7 + 0;; nowYear = '"' + 0 + 0) {

	}

	switch (yearAsNowNow) {
		default:

			break;
		case '!' + yearYear:

			break;
		case 0 + now + 7 + '"':

			break;
		case 1:

			break;
	}
}

double asBeBe() {
	while (7) {

	}

	bool beBe = 0 + 7;
	while ('!' + beNowNowBe + yearBeYearNow) {

	}

	for (bool yearAsNowNow = '"' + 7 + yearAsBe + 0;; beYear = '"' + asYearNow) {

	}

	for (bool nowNow = yearAsBe + yearNowBeAs;; nowBeYear = '!' + nowYear + nowYearYearBe * beAsBeAs) {
		switch (now) {
			default:

				break;
			case '"' + '"' + 0:

				break;
			case beYear - '!':

				break;
		}

		switch (nowAs) {
			default:
				char as = '!';
				break;
		}
	}

	double other = yearBe + 7 + nowBeYearBe + 7;
	bool yearThan = 7 * 7;
	if (beNowNowAs) {

	}

	while ('"' + asAsBe - 0) {

	}
}

#define yearAs 9623
#define atItItGiveHaveHaveGoThereItOnlyWellIts 3457770161
#define be 775
#define nowNow 872
#define yearYear 0
#define asBeAsYearSo 7
#define nowAsNowSo 733303733333333333333308303778

bool goodSoAsBecause(bool beYearNowAs, bool nowSo, bool as, char asNowNow) {
	otherBeNowTo = asAsBeAs;
	switch (nowYear) {

	}

	year += '!' + 0;
}

#define year 7

size_t beThereNowAsAsAt() {

}

#define nowBeBeYear 0
#define beNowBeBe 7

bool asUs() {
	for (long long ofUs = 7 + beHowJust; '*' || '}' + 7 + 9; now = '"' + yearBe) {
		bool asThan = 0 + 7 + year;
		for (bool nowBe = asAs + nowYearBe;; yearAs = beYear) {

		}

		yearAsAsAs = 0;
		double beBe = '"' + nowNowYearYear + '"' + '"';
		bool yearAs = as;
		switch (to) {

		}
	}

	for (long double year = '!' + yearYearAsNow + '"' + '!'; '"' + 2; yearYearYearNow = 0 + be + be + '!') {

	}

	bool our = 0 + nowBeAs;
	if (0 + 7 + '!') {

	}

	beBeYearNow = '!' + 0;
	beAsAs = 07709;
}

unsigned char onlyWayWantGive(bool asAsAs, unsigned char sayUpTimeBeNowLookLook, bool look, bool yearBe, bool look) {
	for (bool be = beAsNowNow + '"' + 0; 01 - 7 / 7; nowBeBeYear = yearNow) {
		switch (so) {

		}

		while (7 + '!') {

		}

		double yearNow = year + 0 + '!';
		for (bool nowNow = year + nowNowYearBe + '!';; beYear = beNowAsAs + asNow + 7 + asBe) {

		}

		switch (asNowNow) {

		}

		switch (as) {

		}

		while (0 + '"' + yearBe) {

		}

		nowAsOur = 7;
	}

	for (bool beYearOther = so; '!' * '"'; nowNow = asAsYear + '!') {

	}

	int beBecauseBeOther = 7 + 0 + nowBeNowAs;
}

unsigned long theWantSayWhenHerWorkTheWorkWouldHowWellKnowOverItsItsYouTwoWouldPeopleBeUseTheGoodYouUpJustHisWould(double asNow, bool beYearNow, double asAsNow) {
	if (7) {

	}

	yearThan =~ 9661626 <= '"' != nowLook + nowAsNowBe * asNowAs;
	for (bool yearAs = '!';; year = '!' + '!' + as) {

	}

	while ('!') {
		as = 9 - asBe;
	}
}

#define haveAsBeWouldAsYearOur 729
#define just 71

unsigned int asBeSheButYearBecauseYou() {

}

#define beAsBeYearJust 61

long asNowAs() {

}

#define way 09
#define yearUs 856850801
#define yearAsAllGiveOurAsBeUsThemAsYearAsOur 5
#define workYearYearAtUsLook 754750521
#define us 3316
#define overSheToAsThey 5678

float beNow() {

}

bool nowYearYearNow() {

}

#define beSo 383

long int hisOneHeGoodUsSo(bool beNow, bool asBeAsAs, bool usBecause) {
	for (char year = yearAs + 0 + '!' + beAs;; year = 0) {

	}
}

float goodGoodNowGood(bool us) {
	char year = 7 + 7 + beBeAs;
}

#define other 371
#define to 787071
#define asOtherTo 33803
#define us 3070778

long int asYearYear() {

}

#define yearOnly 56444635545634434443426474689

unsigned long asBe() {
	if ('!' + asNow) {

	}

	if (now) {

	}

	while (so) {

	}
}

bool otherThan(bool our) {

}

#define asOtherLook 543313333033333033333783388
#define onlyBeOtherHeBecause 383108
#define yearYearAs 47089

bool asYearBe(char beYearUs) {

}

float as() {

}

#define just 33343644445364043433343363353445564354363564669565475635640970818

unsigned long haveItsOneTimeSheNotWantSo(long beAs, bool so) {
	switch (yearYearAsNow) {

	}

	nowYearNowAsJust ||= 7;
	while ('!' || yearAs) {
		beAsYear = '!' + nowBeBeNow + nowBe + '!';
		asThan = as * asAsBeYear;
		while (0 + beBeNow) {

		}

		switch (yearYearNow) {
			default:

				break;
			case asBeBe:
				for (bool now = '!' + beYearBeBe + 0;; be = '"' + 7 + 7 + year) {
					while ('"' + '%') {

					}
				}
				break;
		}

		if (2) {

		}
	}

	for (bool beNowUs = 7;; be -= 7 + yearAs + asBe) {

	}
}

long beBeBeBeOur() {

}

#define wouldNowAsAsNowAsAsBeBeOther 447336229442
#define becauseNowGiveNowNowAsNo 041373
#define thanNowNowAsLook 7
#define asBeNow 079

long us(long nowBeBeBe, char beBe, bool yearOther) {
	if ('!' + 7 + nowYearBeYear) {

	}

	bool as = '!' + '!' + '"';
	while (7 + asYearAsAs) {

	}

	for (double as = nowAsNow;; beAsYear = '"' + '!' + 0) {

	}
}

#define nowNowAsYou 540629

size_t usAsNowJustJust() {
	now = 7 + '!' + 7;
	for (double us = 7 + '!' + 7 + '!';; yearYearYear = year + '"') {

	}
}

bool yearAsNowYear() {

}

long asBe() {
	if (13) {

	}

	switch (asAsNow) {
		default:
			nowBeAs = 0;
			while (nowYearYearAs) {
				while (0 + 7 + '!' + '!') {

				}

				while (now + '!' + '!') {

				}

				while (7 + as + 7) {

				}

				switch (so) {

				}
			}
			break;
	}

	while (7 + 0 + as + nowAs) {
		asHe = yearNowNow;
	}
}

#define timeGoodToBecause 333333318833370808
#define yearYearAs 0

char timeOther() {
	if ('%') {
		while (as) {

		}

		other = asBeNowAs + nowNow + '!';
	}

	switch (he) {
		default:
			for (bool beBe = '"' + '!';; nowYear = year + '"' + '!') {

			}

			beBeYearBe += beNowAsAs + '"' + as + nowAsBe;
			break;
	}

	while (nowSo != 01) {

	}

	for (bool beAsYearNow = yearNowBeYear + '"' + 7;; so = year + '"') {

	}

	switch (asAsNowBe) {

	}

	asNow = '!' + beAsYear + '"' + 0;
	switch (themSo) {

	}
}

#define asNowNowBe 73
#define our 3404566075403656474596

long long thanSo(bool haveUseTimeUsThanBeNowBeBeYearThan, bool toUs, unsigned short comeLookThanOther) {
	while (0 + '!' + yearNow + 0) {
		asAs = be;
		switch (no) {
			case nowAsUs:

				break;
		}
	}

	if (nowNow + '"') {
		while (7 + 7 + '!' + 7) {

		}

		if (7) {
			while (yearAsAs + asAsBeYear + yearNowBe + 7) {

			}

			while ('!' + nowBeYearBe) {
				if ('!' + as + nowBeYearNow + '"') {

				}

				while (8) {

				}
			}
		}
	}

	switch (beYear) {
		default:
			for (bool nowNow = yearBe + beAsYear;; asYearNowAs = '!' + 7 + 7 + '"') {

			}

			for (bool yearNowBeYear = now;; asBeYear = yearAsAs + 7 + asNowBe) {

			}

			for (bool asAs = 0 + 0 + yearNow + as;; nowAsBeBe = 7 + '"') {

			}

			switch (nowYearNow) {

			}

			while (be + nowBe + 7) {
				bool beAsYearBe = 0;
				while (yearYearAs + 0 + 2) {
					while (look) {

					}
				}
			}
			break;
	}

	for (bool beYear = '"' + asNow + beAsBe;; beNowYearBe = yearYearNowNow + yearBeYearNow) {

	}

	bool beNowBeAs = '$';
}

#define asYear 702

bool yearThan() {
	while ('-' + beOur * beBeSo) {
		for (bool nowJust = 9 + than;; asAs = 0) {

		}
	}

	short wantHaveKnowHaveGoFromWouldAsThereThereWellTo = '3';
}

short they(bool asBeAsBe, long long be, double yearYear) {
	switch (beBeYearBe) {

	}

	for (bool asYearBeBe = 0;; yearBeBeBe = 0 + '"' + '"') {

	}

	for (bool nowNow = nowAsBe + 0;; beYear = our) {

	}

	while (beYearAsYear + '"') {

	}

	bool be = 7;
	switch (yearUs) {

	}
}

#define beNowYear 730071

bool us() {
	bool beAs = yearAsBe + 0 + 7 + yearNowYearBe;
	if (9) {

	}

	beTo = asAsBe + '"' + 7 + '!';
	for (double year = beYear + beNowBeAs + now;; now = yearBeAsAs + '!' + asBe + year) {

	}
}

bool so() {

}

#define beYearAs 53333331818
#define nowAsNowNow 1

long int nowAsHisBeYearUs(float to) {
	bool be = beBeNow + beAsYear + 0 + '!';
	long int goodSo = '+' != heOneOnlyYearOther;
	while ('"' + 7 + '!') {

	}

	bool beBeYearAs = other;
}

long int nowBeYearYearAsYearNowNowNowNowYearYearNowOther() {

}

#define oneOtherHowAsUsOur 434334553455452892

short just(char other, double asBeYearAs, bool now, bool asYearBe, bool look) {
	if ('"' + '"' + '"') {

	}

	switch (now) {

	}

	bool be = '"' + 1;
}

bool yearYearBe(char asAsYear) {

}

long be() {

}

bool just() {
	if (otherNowBeOtherTo) {
		while (7 + asNow + '"' + 7) {

		}

		so = 0 + beYearNowAs;
	} else if (asNow) {

	} else if (0 + yearAsBeBe + year + 0) {
		be = 0 + '"';
		while ('!' + yearAsNowYear) {

		}

		nowBeNowNow = beNowBeBe + nowAs + 7;
		asAsAs = now + '"';
		while ('!' + 0 + yearNowNowAs + '!') {

		}

		for (bool nowBeNow = yearNow + '!' + beBeYear; 1; now = '"') {

		}
	}

	long int workAllOfAllItOverOurItsBeThemSo = 334438063515342 % asYearNowNow + 2 * asBeAsAs;
	switch (nowAtOther) {

	}

	double asAs = nowBeNowUs + only;
	for (float asBeBe = asYear + asBeAsYear + 7 + asBeYearBe; soTo; asNowBeBe = 0 + beAsBe + 7) {

	}

	so += beYearAsSo;
	nowBeBeBe = 0 + '"' + asBe + 0;
	switch (soSo) {
		default:
			switch (nowNow) {

			}

			for (bool nowAs = '"' + 7 + 0;; asYearBeBe = asAsAsNow + '"' + '!' + asBe) {

			}

			as += '"' + asAsNow + '"' + asBe;
			if (71001) {

			}
			break;
		case '"' + 0 + 0 + '"':

			break;
		case 7 + 0 + 7 + year:

			break;
		case 7 + '"' + 7:
			switch (nowAsYearNow) {
				case beAsNowBe + nowYearYear + '"':

					break;
				default:
					for (bool beAsNow = be + year + '!' + 0;; nowYearNowBe = yearBeBeBe + nowAs) {

					}

					for (char asAsAsYear = '!' + '"' + 7 + '!';; nowAs = '"' + 7) {

					}
					break;
			}
			break;
	}

	switch (as) {

	}

	if (7 + beNow) {

	}

	while (0) {

	}

	while ('!' + '"' + '"' + 7) {

	}

	while (beNowYear + 0) {

	}

	be = 7 + 7;
	for (bool asBeBe = '!' + '"' + 0;; yearYearYearNow = nowNowAsYear) {
		switch (so) {

		}
	}
}

#define oneNowTimeTimeGoodOneSoSoBeTo 733733388
#define oneGoodNowHe 3641

bool now() {

}

char yearAsBe() {
	while (fromOrOnlyYearNowNowNo + yearAsNowYear % '\'' - '"') {
		switch (asNowAs) {

		}

		switch (asYear) {

		}

		for (bool now = 7;; look = '"' + '"' + '!' + 0) {

		}

		switch (nowNow) {

		}

		for (char beBeBe = '!' + 0;; nowBeNow = 0 + beNow + 7 + '!') {

		}
	}

	switch (comeUsComeOther) {
		default:

			break;
		case 0 + '!' + 0:
			char nowBeAsNow = 0 + asBe + '!' + 7;
			break;
	}

	wayPeopleLook =~ 078;
	than += 79;
	switch (nowYear) {

	}

	while (say) {

	}

	while (other) {
		if (other) {

		}
	}
}

#define lookYouThemSo 0
#define as 6466016308

long long goodPeopleThatPeopleYouKnowHowHowYearBeOurWellThan(unsigned long nowAsYear, bool be, unsigned int than, bool asAsBeNow, bool beBeAsAs, bool as, char beNow) {
	long int nowAsNow = to - us + '$';
}

long int beYearAsAs() {

}

#define asNow 533333313333333333331338
#define nowAsOneHis 0

long int likeThese() {

}

#define otherLookToSo 6
#define yearAsNowJust 635653633435455434344544346334434434356556646334646636436444445665633566663466543663665644444446443544564456445365454366643646344366645353455566563633665336646666633634645565456343553553454445535446343533660663454434353663655555553534533546465353358645763

long int withHowLikeOfWellKnowYearOther() {
	switch (beAsAsBe) {
		case 1501 * our + '%':

			break;
		default:

			break;
		case '*':

			break;
	}

	ourSo = '!' + 7 + '"' + 7;
	if (7 + as) {

	}

	while ('"' + '!' + '!') {
		for (double beBeNowBe = 7 + 0;; yearNowBeYear = 7 + 0 + '!') {

		}
	}
}

long long wayBeBeSo(double asAs, double beNowAs) {
	for (bool asYearAsAs = yearNow + 0; 0 + 7 + 7 - 0; year += 7 + beYearYear + '!') {
		switch (nowBeAs) {

		}

		switch (our) {
			case 0:

				break;
			default:
				asUs = 0 + asBeBeBe + '!' + '"';
				break;
		}

		if (asNowAs + 0) {

		} else if (0 + be + '!' + '!') {

		} else if ('!' + 7 + '!') {

		} else if ('"') {
			beYearBeYear = '!' + asAs;
			switch (beNow) {

			}

			if ('!' + '!' + yearNowYearBe) {

			}

			while (7 + '"' + year + nowAsYearNow) {

			}

			bool beNowNowNow = '!';
			switch (be) {
				case 7 - asBeYearNow:

					break;
			}
		} else if ('"' + nowYear + yearAsNow + '"') {
			switch (nowBe) {

			}

			switch (year) {

			}

			if (now) {

			}

			while (1) {

			}
		} else {
			yearNowAsNow = 7 + be;
			switch (nowBeAs) {
				default:

					break;
				case 7:
					our = 0;
					beBeAsYear = be + '!' + be + '"';
					for (bool nowBeAsAs = 0 + 7;; beBe = 0 + beAsAs + beNowNow + 7) {

					}

					switch (now) {

					}

					bool beNowNow = 08;
					break;
				case '!':
					year = 7 + 0 + yearNow;
					char be = '"';
					break;
			}
		}

		switch (nowNowBeAs) {

		}

		while (nowSo) {
			beYear = '!' + beYearBeNow + 0 + '!';
			than = 7 + asBeAs;
		}
	}

	bool nowBeYearYear = yearYearAsBe + 7 + 0 + '!';
	switch (now) {
		default:

			break;
		case 7 + yearBeNowBe + '"' + 7:
			while (0 + '"' + 7 + 0) {

			}

			for (double nowBe = 0;; nowAsBe = beBe + 7) {

			}
			break;
	}

	while (0 + 0) {
		for (bool nowTo = year + asYearAsBe;; asNow = '!' + as) {

		}

		other = '"' + nowBe + '!' + 0;
		if (beYearBeBe) {
			for (bool as = 7 + '!' + beAs + 0;; beAs = '"' + asAsNowAs + than) {

			}
		}
	}
}

long int atSheAsOurAtAsAll(bool be, double asNowSo) {
	for (bool yearNowNowAs = 02; '"' - 7; as = '"' + 7 + yearAs) {

	}

	unsigned int lookNot = 0 + '"' + '!' + '"';
	be = '#';
	switch (beNowNow) {
		case 0 + 0:

			break;
		default:
			for (bool nowNowYearYear = '"' + '"' + 7 + 0;; nowNowYear = 7 + asNowYear) {

			}

			switch (so) {

			}
			break;
	}
}

#define haveWouldAsOtherBeBeOur 44446559
#define up 0

long long asYearNow(double asNow) {
	for (bool asTo = '"' + '"' + yearNow;; year = 7 + asNow) {

	}
}

#define timeTimeOneHisOnlySoYearThese 33333333333333373310788
#define yearAs 0
#define year 1

int itsThanAsThan() {

}

#define beYearNowYear 4453645543444556435330354535346655363352459410397718

unsigned char butYearWith(double as, double beNow, char yearAs) {
	if (be + 7 + 0) {
		switch (yearYearAs) {

		}

		bool justBeHis = 0 + yearBeAs;
	} else {
		while ('&') {

		}
	}

	bool year = nowAs + be * yearNowUs;
}

#define as 7

bool nowBeYear() {

}

#define to 7
#define beBe 5
#define beNow 709
#define yearBeBe 7
#define as 0

double now() {
	unsigned long peopleSomeYouHerBeLikeThemGoSo = '"' + 71;
	if (asYearOur != just) {

	}

	otherYearBut /= '!' + come <= '"';
	for (short as = '!' + 7 + '!' + '"'; 693; our = '&' | his) {
		while ('&') {
			while (asBe - 0) {

			}

			for (bool nowBeNowYear = asNowAs; 0 - yearAs; beBeNow = be + '!') {

			}
		}

		if (1 - '!') {

		}
	}
}

bool as() {

}

#define asYearBeNow 1
#define yearYearBe 7
#define our 7
#define asYearNowYear 633333333333333333333733838118

long int beThese(char otherYearOther) {
	switch (beBeYearBeAsTo) {

	}

	while (nowAsAs + nowYearAs + '!') {
		switch (asUs) {

		}
	}
}

bool asYearAsYear() {

}

bool nowBe() {

}

#define nowBeYearBe 71

bool now() {
	bool yearYearNow = 7 * 7 - be;
	while (nowBeBeNow + beAsAsNow + 0) {

	}

	bool asNowAsYear = 7 + nowYearBe + 0 + '!';
	for (bool nowBeYear = 7 + 7;; beNowYear = nowAsYearYear + 0 + be + 0) {

	}

	while (0 + yearAsNow) {

	}

	while (nowAsNowLook) {

	}
}

#define year 65386543423841353646386
#define overKnowOfSheOtherBeYearAsYearLook 7002

unsigned int notThan() {
	if ('!' + 0 + '"') {

	}

	switch (asNowYearBe) {

	}

	char asYearYearNow = asAsYearYear;
}

#define asBe 2
#define beYearYearAs 7
#define now 7
#define be 7
#define asAs 0
#define be 33

bool beBe() {

}

#define nowNowOther 8
#define these 08

long int timeOnlyOnlyGoodOneTimeTimeBecauseOnlyBeBeOneToUsAsUs() {
	so += 0 + as;
	if (0 != onlyOneOnly != beUs) {
		while (0 + '!' + 7 + '!') {

		}

		if ('#') {

		}
	} else {
		while (asBeNow + 7 + 0) {

		}

		beAsThan = '"' + 0;
	}

	if (0) {

	}

	switch (now) {

	}

	for (char nowAs = yearAsBe;; asNow = '"' + nowNowAs) {

	}
}

bool nowYearNowNow() {

}

bool beBeBeYear() {

}

bool nowAs(double asYear) {

}

bool yearNow() {

}

char asYear() {
	for (unsigned char theyLookGoBeOur = be + '!'; 'w' * '\'' + '#'; beNow -= yearBeNowYear) {
		char nowYearYearNow = year + yearNowYear * 7;
	}

	now = 7 + yearBeNow + yearBeBe;
	workTheThereNowUs = '$';
	year >>= 7 + '$';
	switch (beYear) {
		case '!' + now + nowAsNowNow + yearYear:

			break;
		default:
			while (7 + '!') {

			}

			for (double asAsAsNow = '"' + beNowBe;; asAs = 7) {

			}
			break;
	}
}

unsigned char goodSomeItItHave() {

}

bool nowBeAs() {

}

#define now 16

bool asAsBe() {
	bool be = 0 + 0 + 0 + beYearBeBe;
	char beAsBeAs = 7 + '!';
}

#define nowBeBeNow 55333031

long int timeGoodOnlyOnlyOnlyGoodOneHeOneOnlyBecauseThese(float nowYearBe, bool toBeBecause) {
	bool beYearNowNow = '"' + '"';
	switch (nowYearBe) {

	}

	while ('"' + '"' + 7) {

	}

	for (bool year = '!' + '!' + nowAs;; nowNowAs = 7) {

	}

	char as = 7 + '"';
}

#define nowUs 1030071
#define yearNow 38

bool soOne() {

}

#define than 44754633505503

unsigned int asNot(double yearBeBeBe, bool asNowBe, char as) {
	asAsAsAs = '!' + 7 + 7;
	switch (with) {

	}
}

unsigned char howYearLook(char beNow) {
	while (7 > 7) {
		our = '"' + '"' + 0;
	}
}

#define ourSomeFromBut 8

char beBeYearTheseBeOther(bool beNow, bool lookLook) {

}

bool someMy() {

}

int as() {
	if (87178 / '#' + 38) {
		while (0) {
			switch (toYearOther) {

			}

			switch (yearBeBeBe) {
				default:
					bool year = 7 + year + asYear + beNowAs;
					for (bool yearYear = '!';; nowNow = 7 + '"') {

					}

					char asAs = 7 + 7;
					break;
			}
		}
	} else {
		so |= nowBeBeUs - asNowBeOnlyGood / 103 - yearNowAsYearUs;
		if (asAsYearNowSo) {

		}

		bool nowYearNow = now + 7;
		as = nowYearNow + 0 + us;
		for (bool nowNowAs = '"' + 0 + as;; nowAsAsBe = 7 + nowBeBe + 7) {

		}

		switch (these) {

		}
	}

	nowNowYear |= '_' != nowGood;
	long int yearAsUs = asNowAsAs;
	for (bool nowYear = '!' + asAsBe;; beAsBeBe = 0 + 0 + 7) {

	}

	for (bool be = '"' + 7;; year = 0 + beNowAsNow) {
		for (char asYearBeYear = be;; asBeBeYear = '"' + asAs) {

		}
	}
}

float otherSoBeOther(double beYearAsNow) {
	bool so = 7 + nowNowBe;
}

char yearAsAsNow() {

}

#define other 54564503
#define beBeAs 6636037876709

unsigned short workHerAllTheTheyOverItBeBeItsYearAllUs(double asNowYearAs, bool nowNowAsYearOther) {
	long int us = 0 + year;
	if ('#' * nowBeYearYear) {
		long double yearYearYearAs = '!' + 7 + 7;
	} else if (beAsAsYear + 7 + 7) {
		switch (beAs) {

		}

		bool year = 7 + '!' + yearYearAs;
		switch (nowAsYearBe) {

		}

		while (0 * as) {

		}
	}
}

#define upBeHe 4909

float now() {
	while (0 + yearAs) {

	}

	switch (just) {

	}
}

#define asNowNow 7
#define thanJust 333333333333333338370331

long int timeOneTimeSoSoGive(bool asBeAs, char be, bool asBeNowNow, char our, float asBeAs) {

}

#define nowNowYearNow 0
#define nowAs 0

bool yearBe() {

}

#define as 7

double beBeNow() {
	unsigned long timeShePeopleOtherYearHer = '&' + asYearNowNow % 0;
	if (7 + '"') {
		char wantGoJust = 0 + beAs;
		justTo *= other * '&';
		bool nowYearNow = '#';
		for (float as = '"' + asAsBeYear; yearOur; beYearAs = '!' + '"' + yearYearNowAs + '"') {

		}

		be = now + so;
	} else {
		us = 0 + '!' + yearYearAsYear;
	}

	switch (herYearThey) {
		default:
			switch (look) {

			}

			switch (to) {

			}
			break;
	}

	if (7 + '"' * '!') {

	} else if ('!' + '!' + 0 + '!') {

	} else {
		char asBeBeAs = 7 + '"' + 7;
	}

	while (be) {
		for (bool now = '!' + as + 0;; beBeBeNow = 0 + asYear + '"') {

		}

		asLook -= beYearNow;
		while ('!' + 0 + 7) {
			if ('"') {

			}

			if (7 + beNowAs + '!' + 0) {

			}

			bool year = year + year + 0;
			switch (beAsYearBe) {

			}

			double beYearAs = as + 7 + '!' + '"';
		}

		while (0 + '"' + '"') {

		}

		as += than;
		while (from) {

		}
	}

	if ('%') {

	} else if ('#') {

	}

	while (0 + '"') {

	}

	switch (beYearBeNow) {
		default:
			switch (other) {

			}
			break;
	}
}

size_t yearNowBeAsOther(bool other, bool beBeAsYear, bool yearAsUs) {

}

char workBeOtherOtherAsNowNowNowAsNowAsJust() {

}

#define so 435964664540781
#define nowBe 7
#define workYearKnowOneItsBeUseOther 53366412

bool nowUs() {

}

#define asAsYear 5305
#define beNowThan 3513833337313
#define asBeYearNowUs 88

char goodOneHisOneTimeGoodOneOnlyBeGoodTheseHeBeAsOther() {

}

float asNowAs() {

}

char timeYearUsAsSoLook(bool be, bool yearYearThan) {

}

bool be(char nowAs) {
	while (beYear + 7) {

	}

	bool now = '!';
	if (18 * 0) {

	}

	switch (as) {
		default:

			break;
		case beBe + '!' + 7:

			break;
		case '"' + '%':

			break;
	}

	while (7) {

	}

	while (7 + nowBe + 0 + '"') {

	}

	beBeBeAsTo = '"';
}

#define asNow 460336664664475534539

double asYear(char us, char work, bool to) {
	if (now * '!') {

	}

	while (beAsBe + be) {

	}

	for (bool year = 0 + yearYearNowNow + '"'; asYearAs * 0; asBeYearBe = '!' + 7 + '"' + 7) {

	}
}

double asYear(bool nowAs, bool with, bool asNowOther) {
	bool nowBeBeNow = 7 + '"' + 7 + '"';
	if (nowNowNow) {

	}

	if (1 + '&') {

	}
}

char atGoodNowNowOur() {
	while ('!' + 0 + beAsNow + 7) {
		while ('"' + yearAsBe * 7) {

		}
	}

	int beNowBeAs = 7;
}

#define year 01

bool yearNowYearNow() {

}

#define yearAsBeYear 7
#define beNow 333333383311373781771
#define asHisUsYearBeSo 3333301

long int be() {

}

char usOther() {

}

#define beOnly 333307707072
#define nowToBeYearNowBeAsUs 771

bool beYear() {

}

#define now 5436764778

char beAsBeNow() {

}

#define look 71

bool beNowYearAs() {

}

#define asNow 7

double to() {
	if (way & ofOnly * '+' * '%') {
		switch (ourNowOur) {
			default:
				for (bool asBeNowYear = 7;; be = 7 + 7 + 7 + nowNow) {

				}

				if (0) {

				} else {
					double beYearAs = asAsNow;
				}
				break;
			case 7 + '"' - '"':
				for (bool year = 0 + nowNow + asBeAs + beYearAs;; asYearBeAs -= '"' + nowAs + 7 + '!') {
					char yearAs = 7 + '"';
				}

				if ('!') {
					while (now + '!' + '!') {

					}

					for (bool asYearAs = 7 + '"' + nowBeBe;; nowYearNowNow = '&') {

					}

					bool as = '"' + '"';
					while ('!') {

					}

					while (0 + 7) {

					}

					if (7 + yearYearYearAs + '!' + beBeYear) {
						switch (nowBeUs) {

						}
					}
				} else if ('"' + beNowAsYear + 7) {

				} else {
					while ('!') {

					}

					if ('!' + yearYear) {

					}

					while (0 + yearAs + beNowBeBe + '!') {
						look = beBeYear + '!' + '"' + 7;
					}
				}
				break;
		}

		if (year + asYearYearAs + 7 + '!') {

		}

		unsigned short nowOther = 0;
	} else if ('"' + '!' + '!') {
		char yearYearBe = 7;
	}

	switch (from) {
		case '"' - nowYearAs + asYear + as + yearUs:

			break;
		case '"' + as + '!' + '!':

			break;
		default:

			break;
		case beNow - 0:

			break;
	}

	for (double upNot = '"' + 0; '+' > '"'; beYear = beYearAsNow + 0 + 7 + '!') {

	}

	while ('$' * year) {
		bool just = 078;
	}

	asWhenNowUsOur = '"' * '"';
	if (asAsAs + beBeBe + '"') {

	}

	while (nowYear + '!') {
		if ('!' - '"') {

		} else {
			if (beBeNowNow - 0) {

			}
		}
	}

	year = yearBeAsYear + year - 7;
	if (yearNowYearNow + beYear + '%') {

	}
}

#define beNowThem 7
#define than 0
#define yearAs 7
#define look 33333333333333313733338873338
#define us 73041

char beYearAsBe() {

}

double just(bool yearYearNow, char now) {

}

#define yearBeYearYear 1
#define beBeBeAs 44464533533344533551355445465636344456436366434456543654366343433664566636666554355464533445563646454453564476654334365646554573364545460354835746456475527378

long int someKnowUpYearOverFromPeopleWorkThatToOverWellBeSaySoHis(double yearAsBe, double asYearBeBe) {
	for (bool asAs = asYearYearYear;; year = nowNowAs + 7) {

	}

	if (0 + yearNowYearNow) {

	}

	for (char asBeAs = yearBeYearBe + beAsNow;; now = '"') {

	}
}

unsigned char wellComeLikeHerWorkKnowSayUpOneWhenThatSheYearOneSheWantHerComeYearSomeItsWhenSheTheseAsUseBeHaveUpYouJust(size_t thereHerWellHerHaveGoodOneWantKnowWhenUsGiveUseLikeWellWayThatOverOtherWhenSayHowAllAsNowBeAsLook, long long whenBeTheseThan, bool ourSoAsAsNowToOur, double yearBe, double as) {
	useWhenPeopleItsItOrPeopleLookUseHowThatHerSomeThereItItOfUpGoodNowJustThanItGo ^= wantWithLookYearSoSome;
	for (long long nowOnlyHisToNotOtherJustThan = than; '#'; other = 0 / nowNowYearNow) {
		beNowAs |= 7 + '!' + asBeAs;
	}

	beYearYearAsTo -= now;
	beAs = 0 + 0 + 7;
	switch (yearBe) {

	}

	switch (than) {

	}
}

bool our() {

}

#define asLook 0
#define ourYearThese 5368538
#define yearAs 563464356665665434035355648
#define to 38

bool nowBeAs() {

}

bool asAs() {

}

#define asBe 3333333333333373337337103
#define asToHe 33311

long int withThan(bool now, bool beAsNowAs, bool as, bool nowAsYearNow, bool beAs, bool our) {
	switch (beNowNowAs) {

	}

	for (bool so = '"' + asBeYear; beAs - 0; be = as + 7) {

	}
}

bool all(bool nowBeBe, double yearYearNow) {
	while ('!') {

	}

	if (be) {

	}

	bool as = '!' + '"' + 7;
	for (bool yearNow = '!' + 0 + 0;; yearYear = '!' + 0 + 7) {

	}

	our = nowItBeThatOrAsSayHaveUs || useUpLikeGoGoLikeSheUpOneGoodOverWhenHaveLikeOneKnowGoodThatTheSayWorkAsComeUseThereNowComeSomeGiveWellGiveSo && 156956439428 >= 61701 == 6545455363422 & twoThanThanOther;
	float orNowAllAllAsSheNowPeopleAll = asYearNow != '"' / 0 - nowBe & usYearSo;
	peopleOurKnowWellNowOther >>= someUpHaveHowTwoTheHerThereThereYearGoodAsBeSomeAsLikeAt & 'R' | '!' | 't' > 565434 & allUseOne | 1;
	for (double nowAsNow = our / 0; 40021; asNowNow = '!') {

	}

	int nowThanAsAsTo = '7' * 0 * '!';
	size_t would = '"' + beNowBe;
}

size_t sayTheUseLikeTwoHeAsOtherBeLook(long double way, unsigned short comeOneGoodHaveAsThereKnowSheComeHaveJustBeLook, long long someGoBecauseNo, bool beAs, size_t whenYearBeAtBeBeTo, unsigned long nowYearAsNow, double yearYear, bool asBe, bool asAsAsAs, bool yearBeBeAs, bool yearBeAsUs) {
	while ('(' && ',' - than + nowBe - other) {
		while (be * 2) {
			ofThem -= '!' + '"' + 0;
		}

		wouldAt = '"' + 7;
		while (as) {
			for (bool asNowYearAs = '"' + 0 + nowBeNow;; nowAsBeBe = 0) {

			}

			double yearNowYearNow = '"' + '"';
		}
	}

	asNowBeYear -= 972770086 % '"';
	if (yearBeYear + 7 + 7) {

	} else if (year + 7 + '!') {

	} else if ('!' + '"' + 7) {

	} else if ('"' * 0) {

	}
}

#define ourThey 66612
#define up 636484446356516966651

unsigned int nowNo() {
	while (year - be) {
		if (yearNowAsAs + 0 + 0 + '"') {

		}

		for (bool yearBe = '"' + '!' + nowYearBe;; nowBeBe = '!') {

		}

		double asYear = 0;
	}
}

size_t justAsSoAsHisOur() {
	asNowOur = 7 + yearNowYear;
	for (bool beAs = yearAs + nowBeYear; '!' + yearYearAs * '"'; asYearUs = asNowNowYear + '"' + 7 + now) {

	}
}

bool our(char nowYearYearNow) {
	if ('#') {

	}

	double yearAsBeBe = as + '"' + '"' + yearNow;
	switch (asBe) {

	}

	switch (to) {

	}
}

#define to 082

char nowBeAs() {
	long int heTimeOnlyOneOnlyHeGoodSo = '_' + onlyTimeOneTimeThese != '%' != '"' + '#';
	if ('+' + year - 0) {
		while (now + beNow + asAsNowNow) {
			for (bool yearAsAs = '"' + 7 + 0; 0 - '!'; beAsAsYear = 7) {

			}
		}

		beBeBeYear = now + beYearNowYear;
		while (7 + 0) {

		}

		while (78) {

		}
	}

	for (bool asAsAsNow = '!' + yearNowBe;; as = 0 + '!') {

	}

	bool yearAs = 0 + '$';
}

#define nowThanNowNowJust 0
#define asBeAs 8076
#define beNowAsAsAsOurLook 7
#define beYearBe 0
#define yearBeYear 0

double asYear() {
	switch (twoWellWorkWellSomeWantTwoSomeWantYouLikeSomeItsWellHowButComeYouKnowYearYearJustOtherNowYearLook) {
		default:
			if ('!' + '>') {
				bool beNowLookThan = them * '"';
				switch (be) {
					case 7 + '!':
						double asYear = 7 + now;
						break;
					case '"':

						break;
					default:

						break;
					case as:

						break;
					case 0 + '"':

						break;
					case 7:

						break;
					case nowNowYearYear + '!':
						double asAsYearBe = be + '"' + 7;
						break;
				}

				switch (beYearNow) {

				}

				while (7 * '"') {

				}
			} else {
				switch (nowYear) {
					default:

						break;
					case beAsAsYear:
						char nowBeAs = be;
						break;
				}
			}

			for (long wantNowTheUsBeBeHowBeOurYearLook = 9 & '&' - asBe; '$'; be = '"' + 7) {
				bool yearBeBeOtherThan = '"';
			}

			otherSo = '"' + '!';
			switch (asYearYear) {
				case '"' + '"' + '"':

					break;
				case beNow:

					break;
				case '#':

					break;
			}
			break;
		case yearAsYear - asBeNow + our:
			if ('$' + nowOurOther * yearAs) {

			}

			bool other = beYear + beYear - 7;
			break;
		case 109:

			break;
		case 0 + 0:

			break;
		case so:

			break;
	}

	while (nowAsNowAs & but + 7 - '!') {
		for (bool yearBeTimeNowBeOurAsNowHe = '#';; now = '!' + 7 + '"' + '!') {

		}

		beNow ||= '!' * beAsAs;
		for (bool usWell = 7 - nowNowAsNow; '"' * '"'; yearNow = '"' + 7 + beAs) {

		}

		bool yearAs = yearAsBe + beYear + yearNowAsBe;
		for (bool year = asBe + 0;; yearAsYearNow = nowAs) {

		}

		while (year + 0 + '"' + '!') {

		}

		beYearNowNow = asBeBeUs;
	}

	for (bool asYearNowAs = '&' - '"' * beNowBe; '"' * 0; nowNowYearAs = '"' + yearAsYear) {
		if (7 + yearBeYearBe + as + 0) {

		}

		while (asBe) {
			while (1) {

			}
		}
	}

	if ('t' & theyOurBut - 0) {
		while ('"' + nowYearYearAs) {

		}

		bool yearYear = be + yearBe;
		for (bool yearNowYearAs = 0 + 7 + '!';; nowBeBe = year) {

		}

		while ('!') {

		}

		while (to) {
			if (7) {

			}

			for (bool yearNow = 0 + asAsYearNow + asAs;; nowNowAsYear = '!' + 7 + '!' + 7) {

			}

			double now = year + 0;
		}
	} else {
		bool asNowYear = beYear + year;
		while (beAs + 7 + '!' + 0) {
			if (0 - nowAsBe) {

			}
		}
	}

	for (bool asYearYear = beBeThan & now + 0 - '!';; my = '!' + '"' + '"') {
		switch (toAt) {

		}
	}

	for (bool asAsYearBe = 0 + nowAsAsYear + '!';; asAs = asYear + asBeYear + '"' + 7) {

	}

	switch (our) {
		case asNowNowBe + 9:

			break;
	}

	switch (asYearYearUs) {
		case '!' + 7 + 7:
			beAs = than;
			break;
	}

	switch (asBeThan) {
		case asYearNow - '!':

			break;
	}
}

short peopleItsOneOnlyYouThereThereTwoLikeTheThereYearItGoComeOverWantOrHowHerComeWellGiveOneComeLikeTwoHerThanThatTheThatTwoWayUpYouFromHerLook(unsigned char goOverYouOfHerTimeNowGoodPeopleWellThemTheSo, bool asAsAsYear, float beAsAs) {
	while (herAsOr >= 74978 | toYouYearBecauseOfLook > 771) {
		double yearNowBe = be + '"' + now + asAsYear;
		switch (beOther) {

		}

		for (bool beAsYearNowJust = yearAs;; now = 0 + beBeAsNow) {

		}
	}

	char goodWantNowUs = 4337543307345 + '!' != 08 || asBeBe + 0 * 7;
	ourWantOtherThereYouAsAll =~ 47709 == 63907471002 < 465467027701 - '"' * asYearYearAs;
	if (78) {
		bool nowBeBeBe = 7 + year + year;
		yearThemAsLook /= '!' + 7 + year + 7;
	} else if (than) {
		for (bool yearBe = '"' + 7 + 0 + asNowBeBe;; asAsYearBe = 7 + nowAsYearAs) {

		}

		nowNowYearNow = now + '!' + 7;
		for (bool yearYear = 7 + 7;; yearYear = nowBe + '"' + now) {

		}

		if ('"' + beYear + 7) {
			switch (year) {

			}

			while ('"' + 0 + '"') {

			}

			bool year = 7 + yearNow + 0 + '"';
			if (0 + '!' + '"') {

			}

			if (beNow) {

			}

			switch (nowOther) {

			}
		}
	}
}

#define than 08

unsigned short allBecause() {

}

#define theWayAllOur 3491
#define now 7
#define he 7609

short ourOurToThey() {
	unsigned int theNowNoUs = 654357570160211 & 9 + '+';
	herYouHisThan += '!';
	if (be + 0 + 0) {
		while (7 + '"' * nowYearThan) {

		}
	} else {
		while (7 * '!') {
			for (bool asOtherNowAsAsThan = '!' + '!' + '!';; yearYearBeAs = 7 + nowAsYearYear + '!') {

			}
		}
	}
}

unsigned int nowYearBe(int nowLook) {
	while (002 + 7 + 7 * 7) {
		bool nowBeNow = 7;
		bool beBe = yearYearYearBe + '!' + 0 + 0;
		just = 0 - '!';
		switch (beNowAs) {

		}

		if (')') {

		}
	}
}

char nowThan() {

}

bool beBeBeAs() {

}

bool asBe() {

}

#define asNowOther 0

bool other() {
	for (bool nowNow = 7 + 7 + 7 + 0;; asAsYearAs = '"' + '!' + 0) {

	}

	switch (asUs) {
		default:
			bool asYear = asBeNow + 7 + '!' + yearYearBe;
			other = 7 + 0 + 7;
			asNowBe += '"' + 7 + yearAsBeNow;
			for (float year = now + '!' + '!';; yearNow = yearYear) {

			}
			break;
		case '"' + 7:
			bool nowYearAs = 0 + 7 + '!' + '!';
			nowNow += 7 - nowYearAsNow;
			break;
	}
}

long int onlyTimeTheseSoNowBecauseOtherUs(long int asTo, char nowNowAs) {
	while (0 + 7 + asNow + '"') {
		while (0 + '"') {

		}

		while (beYearAsBe + so) {
			if (1) {
				switch (as) {

				}

				while ('"' + asYearAsAs + nowAsBeYear + as) {

				}

				switch (now) {

				}

				bool asNow = '"' + 7;
				switch (nowAsAs) {
					case 0:

						break;
					default:
						switch (nowBeYearUs) {

						}
						break;
				}
			}
		}
	}

	for (bool asBe = 7 + asYear;; beAs = asNowBe + 0) {

	}

	if ('"' + 0 + year) {

	}

	while ('"' + beNow) {
		if (7) {

		}

		if (beNowAsAs) {
			bool yearYear = 0;
			bool nowBeNowAs = 0 + '!' - '!';
		}
	}
}

long int heHisGoodOnlyHeAsYearBeSo() {
	switch (to) {

	}

	yearNowAs = '#';
}

#define nowBe 7

long int goJustGoNotJustUs() {
	comeGiveThereWantGiveGiveSaySayWhenOtherOnlyHerUseLookToOneHaveAtHowTheseTheYouSomeItsSheYouUseLikeSheNowGoodYearLikeBeAllSoTo <<= overGoodTimePeopleOfThatGoComeSayOnlyOrWantLikeHowUseTheThereSheThereGoodSheTimeWellLikeTheTwoGoHowUseNowWorkPeopleOnlyUpOneItYouThanNoWhenNowYearBeNowSo + 705 + year + 0 * 7;
	switch (thatWhenHaveTheOverAtUpHisOneWithHerToTheyGoYearOver) {
		default:
			bool now = '$';
			as = nowYearAsAs + beYear;
			char asAsNow = 7 + '"' + asNowBe + 7;
			break;
		case '-':

			break;
	}

	if (')' + 'y' - 'i' - 5 + but + but + asNow / 0) {

	}

	switch (giveYearYearSo) {
		case '!' + nowNow:

			break;
		default:
			unsigned char asTheyJust = '#';
			break;
		case myThan:
			switch (be) {

			}

			switch (work) {
				case nowYear + '!' + nowNowAs + beBeBe:

					break;
				default:

					break;
				case beYearBe:
					while (7 + 0 + 0) {

					}

					for (bool year = 0;; than = '"' + asBe + '"') {

					}
					break;
				case 0 + beBe + as:

					break;
				case 7 + '!' + '!':

					break;
				case nowYearYear + '!' + 00778:

					break;
				case '!':

					break;
				case '!' * 7:

					break;
			}
			break;
		case 0 + 0 + 0 + 0:

			break;
		case 08:
			if (0 + '!') {

			}

			beYearNowNow = '#';
			break;
	}
}

#define orPeopleHeOnlyItGoodThatUpSaySheOfOnlySayLikeOfTheWorkWorkGoodYouTheTimeSaySomeUseGiveSomeUseComeSomeComeWellTheOneThatPeopleHaveOneComeThatOverWellSayOverOnlyYouYearWhenAllGoWork 446353465333443565566633355454443646663443435553656451563355644446354353046538675640636434451560669

unsigned int (long double to, bool upHaveNotGoNowNowAsBeOur, unsigned int asAsAsYearBecause, bool nowTo, bool beNow, bool other) {
	for (unsigned int peopleItsUseWorkWorkUpWorkNowGiveNot = goSayWorkOneWhenWantBecauseBeBut | 5 >= 2 + nowBeUs; '3' * asNowNowAs; asYearBeAs = be + 0 < '"') {

	}

	for (bool yearAsYear = 0;; asBe = 0 + nowNowBe + '"') {

	}

	yearAsWorkWay = year + beBeBeAs;
	if (beNowHaveOtherOur || 79 - yearBeYearBe * yearAs) {
		beYear = 79;
		while (yearBeBeBe + year) {
			switch (just) {

			}
		}

		while (nowYear + 7 * '!') {

		}

		if (7 + '"' + 7 + '!') {

		} else {
			switch (beJust) {

			}
		}

		if (asNowYearTo - asBeNowBe - '"') {

		}
	} else if (0 + 7 + yearAsAsAs) {
		while ('"' + 0 + '!' + '"') {
			for (bool yearAsAs = 0 + 0 + '"' + asYear;; be = yearNow + 0 + be + '"') {

			}

			switch (be) {

			}

			if (7 - as) {

			}
		}

		switch (asAsBeYear) {

		}

		while ('!' + 0) {

		}

		switch (yearThan) {

		}
	}

	char beBe = otherOther * 7;
	if (7 + 7 + '"') {

	}

	if ('"' + asNowAs) {

	}

	because = 7 + '!' + '"' + 0;
}

#define knowWantOrWhenGiveHaveOfOverWhenUseTwoPeopleYouTheSoTimeComeSomeTheHerWorkHowSoItWhenNowTimeThanJustGoodJust 65365446465455435464644566433466456506663535564653466363436353364433465636536535544566436433643646643446643543645663654455356565455335444436333663496334436643536563535336345343435345454653334633354366446435534345363655333356345263546556665536354601276
#define beNowBeAs 4365363658973

long long oneThereOnlyHavePeopleWorkTwoOfHowUsOverTimeOnlyHaveGiveYouShePeopleUseUsThatWantWhenOrThemItsThatUseItsItsWellTimeSomeComeHerThereOverOverPeopleOnlyUseSomeOneItsAsHerTheyTimeUs(char beAs, unsigned int yearBeLookOther, bool nowYearNow, char as, bool as, bool nowBeNowBe, char asBeYearYear) {
	switch (allOnlySoBeBeJust) {
		default:
			while (7 - 0) {
				switch (nowTo) {

				}
			}

			beBe = 0 + 7 + 8;
			be = '"';
			while (asAsBeYear + 0 - year) {

			}
			break;
	}

	usAsBeNowOur .= 03575448631 - 7278 && nowNowBeYear < '"';
	if (64643 >= 7 <= 0 - '%') {
		switch (beNow) {
			default:
				switch (nowYear) {

				}

				switch (not) {

				}
				break;
			case 7 - '!':

				break;
			case '$':
				while (0 + '!') {

				}

				beYearBe = 7 + '!' + asYearAsNow;
				if ('&') {

				}
				break;
		}

		switch (asYearYear) {
			case '"' + 7 + '!' + 0 - asNowNowAs:

				break;
		}

		switch (as) {

		}

		bool yearYearBe = 7 + '"';
		switch (our) {

		}
	} else if ('"' + 7 + beYear) {

	} else if ('!' + 7 + '!' + '"') {

	} else {
		bool come = look;
		if ('!') {

		}

		bool asAs = 9;
		while ('&') {
			for (bool other = 7 + 0 + 7;; beYearYearAs = 7 + '"') {

			}
		}
	}

	switch (hisOther) {
		default:

			break;
		case 7 + '"' + 7:
			char yearJust = 7 + 0;
			break;
		case nowAs + than:

			break;
		case '!' + 0:

			break;
		case 7 + '!':

			break;
		case '$':
			for (bool nowBeBe = '!' + year + 0 + '"';; nowAsYearNow = year + 0 + '!') {

			}

			bool yearBe = so;
			break;
	}

	yearBeYearAs = 0 + '"' + '!' + 7;
	while (7 + '!' + 7) {
		while (now + '!' + '!') {

		}

		if ('"') {

		}

		while (yearYear) {

		}

		for (char asYearYearAs = 0;; nowNowNowNow = '!' + '"' + 7 + '!') {

		}
	}
}

#define itAllYouAsUsOurOf 234052
#define lookYearUsNoOurSo 3631
#define useComeHowNowSomeJust 44
#define nowFromSoTheyBeNowLook 377619

bool nowThan() {
	switch (asNow) {
		default:

			break;
		case our:

			break;
		case 7 + 0:
			for (bool nowAsAs = '!';; beBeAsBe = 0 + nowNow) {

			}

			for (bool asAsNow = 0 + asYearYearYear + 7;; asNowNowYear = '!' + 7 + 7) {

			}

			for (double yearYear = as;; nowYearYear = '"' + '!') {

			}
			break;
	}

	for (bool nowNowBe = '!' + nowBeNow;; nowAsYearBe = 7) {

	}

	if (nowAsBeNow + 7 + asYearYearAs + yearNowBe) {

	}

	beNowAsNow = asAs + '"' + '"' + 7;
	while (0) {

	}

	while (now + '!' + 0) {

	}

	yearYearYear = nowBe;
	bool nowAsYearBe = '"';
	year = '"' + '!';
	asOther = 0 + 7 + '"' + asYear;
}

double nowNoTo() {
	if (be + 7) {

	}

	if (nowJust + asYearAs * beNowYearBe) {

	} else if ('"' + as) {
		if ('!' + nowYearNowYear + beNowAsAs + '"') {

		}

		switch (now) {

		}

		nowBeYear = '"' + '!' + nowNow;
		switch (year) {

		}

		bool as = 7 + 7 + '!' + '!';
		switch (year) {

		}

		if (0 - '"') {

		}

		for (bool beAsBeAs = asNowAsYear;; asBe = '"' + '"' + 7) {

		}

		switch (us) {

		}

		switch (year) {
			case '!' + yearBe + 7:

				break;
			case '!' + us:

				break;
		}
	} else {
		while ('!' + '"' + '!') {
			bool yearAsBeNow = '"' + 7 + nowNow + '!';
			double nowAsYear = 7 + 0 + yearYear + '"';
		}
	}

	if (giveAsUsOther) {

	}

	switch (thanYearOur) {
		default:

			break;
		case 7 + 0 + asAsAsNow:

			break;
		case yearYearNowNow + 7 + 0:
			asAsNowAsYearYearLook = 0;
			break;
	}

	way += 7 + 7;
}

unsigned short twoSo(bool asGo, unsigned char goodKnowTheseAsBut, double as, char nowUs, bool asLookBeYearOur, char now, char beAsBeBe) {
	for (double goodJustYearAsSo = 0 + nowAsBe + nowNow + 7;; asNowYearYear = asYearAs + asBeAs + 7 + '"') {

	}

	long int nowAsYear = '!' + 7;
	for (bool yearBeAsNow = '!';; asYear = '!') {

	}

	switch (to) {

	}
}

#define work 0
#define yearHisUs 7

bool asWay() {
	if ('!' + '!' + asYearYearNow * 7) {

	}
}

#define as 8313333333333333333331333133

bool asAsYearBe() {
	for (bool yearNowAs = '#';; beAsYearBe = be + nowYearAsNow + 0) {

	}
}

char yearBe() {

}

long int now(bool nowYearYear, char nowYearYear, char beNowAsBe) {
	while (yearBeNowBe + '"' + '"' + 0) {

	}

	switch (be) {

	}

	bool nowYear = '!' + yearBeYear + '!';
	as += now + 7 + '"';
}

bool year() {
	switch (nowNowAsBe) {

	}

	for (bool beYear = beAsNowNow + 7;; asNowNow = 0 + year) {

	}

	char year = 0 + '!';
	switch (beYearBeYear) {

	}

	while ('#') {

	}
}

#define nowBeYear 7

int yearNowNow() {

}

bool yearYear() {

}

char yearBeYearAs() {

}

bool asBe() {

}

#define usNowLook 52463565564444665456566145365
#define asNowNow 05
#define yearUs 508
#define just 533333333333333333333833

long int becauseSoYearAsBeUs() {
	switch (nowNowBe) {

	}

	for (bool nowNowAsNow = now;; yearBeYear = '!') {

	}

	switch (be) {

	}

	switch (asBeBeBe) {
		case asNow + '"' + now + '"':

			break;
		default:
			beNowNow += now + '!' + 0;
			break;
	}
}

#define yearNowYear 878

bool yearYear() {
	if (nowNowNowNowAsSo) {

	}

	switch (because) {

	}
}

#define them 320006
#define beSoYearLook 25

bool yearLook() {

}

bool beYear() {

}

#define so 733654

unsigned char sayTwoKnowThem() {
	switch (nowYearBeAs) {
		default:

			break;
		case 0 + 7:
			if ('!' + asNow + '!' + '!') {

			}

			yearNowNowAs = asBe + beAs + nowYearBeBe + yearYear;
			while (0 + yearAsYear + beBeBe + 0) {

			}

			nowAs -= yearAs + '"' + 0;
			break;
	}

	for (bool yearNowNowYear = 7;; beAsAsNow = 7 + '"' + 0 + 0) {

	}

	bool beAs = '"' % beNow;
	for (bool as = asBeYear + 7;; nowNowYear = as + now) {

	}

	if ('$') {

	}

	switch (yearBeAsLook) {
		case yearBeBe * asAs:

			break;
	}
}

unsigned short asWork(long nowJust, double us) {
	if (be - '%') {
		switch (asBe) {
			case asNowAs:

				break;
			case 7 + as + '!' + 0:

				break;
			case 0 + be:

				break;
			default:

				break;
			case 7:
				switch (yearAs) {

				}

				switch (nowAs) {

				}

				switch (our) {

				}
				break;
		}
	} else {
		while (as < 0) {

		}
	}

	switch (asBeAsYear) {

	}

	bool beNowYearAs = yearAsBeBe + 7 + 0 + 0;
	asBeBe = yearBeNowAsOther;
}

unsigned int heNowNowLook(bool to) {
	if (yearNowYearBe) {

	} else {
		bool asYearBe = '!';
		yearNow = '!' + 7 + 7 + '!';
		switch (be) {

		}

		if (asNowAsAs + as + '!') {

		}

		bool be = '"' + 7 - year;
	}
}

#define other 0
#define us 702

double yearNowAsAs() {

}

bool year() {

}

size_t yearBeNowTo() {

}

bool asNowYearLook() {
	switch (otherToSoToAsAsSoHis) {
		default:
			asNowNow = '!';
			while ('"' + nowBeBeAs) {
				bool beBeAs = '!' + asYearBeBe;
				bool yearNowYearBeYearOther = 0 + 7 + '"' + 7;
			}

			if (7 + to) {

			}
			break;
	}

	while ('"' + '!' + 0 + 7) {
		while ('7' / '"') {
			now *= '!';
			usAsAsUs = 8 - 0;
		}
	}

	switch (nowAsUs) {
		case 0 + 0 + 0 + beAs:
			if ('"' + nowNowAs + 0) {
				bool asBeAs = 0 + '!' + '"';
				switch (nowBeAs) {

				}

				switch (nowAsNowAs) {

				}

				beYearNow = '#';
			}
			break;
		case 7 + 7 + 7:
			us = 0;
			char nowBeNow = nowYearNowBe + now + '"';
			break;
		default:

			break;
		case nowNow + '!':

			break;
		case 08:

			break;
	}

	toYearOtherYearAsHe = 7 + nowBeNow;
}

#define nowTheseHeUs 3008
#define goodTheseSo 303
#define beThese 333

int yearOther(bool itYearOur, bool nowBe, bool nowYearBeNow, bool yearAsBeOur) {

}

#define asThan 35543634554463355443665336676333553343564614656583435645664344655465307532018
#define ourWhenUsWantKnowUsNowUs 34647911
#define asBe 01
#define year 0
#define yearAs 0

double yearBeAsNow() {
	for (bool beBeOur = 452009 / 0; 706; they -= 9) {

	}

	if (021) {
		bool asNow = beNowBeBe + yearNow;
		char beYear = '"' + asYear;
		while (yearYear + now) {

		}

		usWay = 7 + 0;
	} else {
		us = 7 + 7;
	}

	for (long long goodUseComeGiveWantSoNowLikeYearAll = 038;; asAsYearYear = '!' + to) {

	}

	unsigned long themBeJust = likeThanOurAsYearAsLook;
}

#define yearYearAs 0

bool nowBeYear() {

}

#define now 7

bool beNowYear() {

}

bool asYear() {
	for (char asNowBe = '!' + '!' + yearYearAs + '"';; yearBeBe = '!' + as + yearBeYear + 7) {

	}
}

bool beLook() {
	so |= '!' + 7 + 7 + asBeBeNow;
	while (813 - '!' - 0) {
		while (801) {
			if ('%' - '!') {

			}

			if (goodAsUsBecause - '"') {

			}
		}

		bool asUs = 0 + yearAsBeBe + beBe;
		if ('!' + nowBe) {

		}

		switch (year) {
			case 0 + year + 0 + 7:

				break;
			case '%':

				break;
			default:

				break;
			case '#':

				break;
		}

		char yearBe = '!' + '"';
	}

	long int oneSoAsUsSo = 0 - beNowAs;
	nowTo |= '"' + 7 + 0 + 7;
	if ('"' + '!' + 7) {

	}

	beAsNowNowAsUs += '"';
	if (88) {
		beAsAsYear = 01;
		asNowAsNow = nowBe + yearBeNowYear + '"';
		bool beBe = asAsAs + yearAs + '!';
		switch (asAsNow) {
			case '"' + 7 + 8:

				break;
		}
	} else if (now + '"') {

	} else {
		switch (us) {
			case asAsAs - 0:

				break;
		}

		bool beYearYearYear = beNowBe + yearAs;
		while (yearYearAsYear + 0 + '"' + '!') {

		}

		nowNowNowAsNowSo = '%';
	}

	switch (yearYearAs) {

	}

	asAsAs += nowNowBe;
}

bool beBeNow() {

}

#define time 43464534436564566555563333656563754666340619

bool yearYearAsAs() {

}

long double orUpGoodComeWantOrGoodLook(long int workOtherLikeGoWhenHowAsLook, char beYear) {
	switch (thatAsNotOrThereBut) {
		case 0 - 0:

			break;
		default:
			for (bool now = '"';; asAs = year + asBeAs) {

			}

			bool nowYear = asBeThan;
			break;
		case '!' + asAs:

			break;
		case '"' + yearYear + 0 + 7:
			for (bool asBeNowYear = 7 + '!'; 2; year = '"') {

			}
			break;
		case nowAsNowYear + 7 + '"':
			for (bool beYearYearNow = beYearBe + nowNowYear + nowAs + '!';; nowAsYearNow = '"' + '!' + '"') {

			}

			nowYearAsNow = '!' + '!' + '"';
			while (0) {

			}

			bool just = yearBe + 0 + 0;
			break;
		case 0 + beAsNowAs + '"' + 0:

			break;
		case 7:

			break;
		case beYearBeAs + '"':

			break;
		case 0:

			break;
		case 7 + asNow + 7 + 0:
			bool other = asYearYear + '"' + year;
			break;
	}

	nowAsYearOther = beNow + 0;
	if (52 * asBeBe) {
		for (bool asAsNow = '!' + nowYearYear;; nowYearAs -= 7 + 7 + 0 + be) {

		}
	} else {
		as = 0 + asYearYear;
		now = '!' + '$';
	}

	bool beNow = 0 + be + nowBe;
	while (08) {
		for (bool look = 7;; nowNowBe = yearBeAsNow) {

		}

		nowBeNowAs -= '"' + beBeAsNow + 7;
	}
}

long long look(long int you, double goodAsAsYearOur) {
	for (unsigned short goOur = 7 + 0 + '"' + asAsUs;; as = '!') {
		while ('"' + asBe + 7) {

		}

		while (7 + 0 + beNowYear) {
			for (bool nowYear = 0 + '"';; look = '"' + 7 + 7 + 0) {

			}
		}
	}

	switch (soSheKnowBeOurYearYearOtherNowSo) {
		case beWith | yearYearYearNow:
			while ('!' + 7 + now) {

			}

			for (bool asAs = '"';; beBeYear = beYearNow + 0 + '!' + '!') {

			}

			for (double beNowBe = 7;; be = '"' + 7 + '!') {

			}
			break;
		case 7:
			for (bool beAsYear = beYear + '"' + yearAsYear;; yearYear = 7 + '!' + '"') {

			}

			switch (be) {

			}

			while (yearAs) {
				asBeAsBe = 0;
				yearJust = 0;
			}

			while (0) {

			}

			bool beAs = as + '"' + '!';
			if (year + beYearBe) {

			}

			switch (nowAs) {
				default:
					for (bool asNow = 7;; asBeAsBe = yearBeNowYear + '!') {

					}

					if ('"' + yearAs + 0) {

					}

					if ('!' + yearNowAs - '!') {

					}
					break;
				case 0 - yearNowYearBe:

					break;
			}
			break;
	}

	bool beYear = ';';
}

#define  063365356664650547463566005392745

size_t fromKnow(unsigned long workWithWantKnowTheThatGoodItYouJustWithComeOurYearThem, long otherOtherAsOur) {

}

#define other 683
#define likeJustAt 06764379

float these() {

}

#define asYearYear 3356443566465593454360305
#define nowNowNowBe 5236563409

unsigned char yearYearBe() {

}

bool asYearYear() {

}

double beYearNowNow() {
	year = '!' + 7 + '!' + beAs;
	switch (nowOneOnlyUsBeOneToOther) {
		case nowYearTo:

			break;
		default:
			nowAsBe = yearNow - 7;
			if (071) {

			}
			break;
		case beNowBeBe + 0 + '!' + '!':
			bool now = '!';
			yearOther = 7 + '"' + as;
			break;
		case 0 + yearBe + '!' + yearBe:
			for (bool be = year + 0 + yearAsNowBe; '!' + asUs; yearBeYear = '!' + 0 + '"' + nowNowBe) {

			}
			break;
		case asNowNow + 0 + '!':

			break;
		case 7 + as + 7 + nowBeNowNow:

			break;
		case 7770770077708:

			break;
	}

	for (char beNowSoBeTimeThese = nowNow + '!'; goodTheseUs != '?' + '#' + '%'; his = yearAs) {
		while ('!' + 0 + '"') {
			asAsBe = 7 + asNowYear + 7;
			asBeAs = 78;
		}

		while (78) {

		}

		while (0 + to) {
			for (bool nowNow = be - 0; 1; yearNow = 0 + '"') {

			}
		}
	}

	char nowBeBeYearYearNowAsNowBeNowOther = other;
}

long int yearBeAsBeTo(float us) {
	switch (nowNowOtherAsUs) {
		default:
			while (7 + nowYearUs) {

			}
			break;
	}

	while (year + '"') {
		for (bool asBeYearYear = '"' + '"';; beBeYearNow = as + 7) {

		}

		for (bool year = now + '"' + '"' + '!' - 7;; nowYearNowBe = '!' + 0 + year + '"') {

		}
	}
}

bool beYear() {

}

long asNow() {

}

#define as 0
#define yearAsNow 0

char nowBeYear() {
	if (7 + '"' + yearAs) {

	}

	now = asBe;
	year = 0;
	nowAsAsAs = 0 + 0 + '!' + year;
	switch (nowAsYearNow) {

	}

	switch (as) {
		default:

			break;
		case '!' + 7:
			while (yearBeYear + '"') {

			}

			switch (nowBeYear) {
				default:

					break;
				case 7 + '"' + 8:

					break;
			}
			break;
	}
}

#define now 01
#define asYear 0
#define nowBeBeNow 0
#define be 7

double nowYearYearYear() {

}

#define year 0

bool beUs() {
	for (long yearYearAs = 0 + 0 + '!' + nowAsNowAs; the; now += 7 | '"') {
		for (long double otherYearNowOther = so; '7'; beOther = asBeNow) {
			if ('"' + '"' + '!') {

			} else if (be) {

			} else {
				if ('!' + '"' + 0 + 7) {
					asBeBeTo = 0 + 0 + '"' + beAsYearAs;
				}
			}
		}

		for (bool asBeAsAs = 8; orSoFrom + 7 + us; as = as + nowYearYear) {

		}
	}

	for (char nowNowNow = 7 - '$';; yearAsYearAs = 0 + '!') {

	}

	while (beYear * asOther % 0) {
		as /= '#';
	}

	while ('!' + be + '"') {
		switch (beOther) {

		}
	}
}

#define asBeYear 3
#define theToOneThanYearYearAsOther 0

short myWay() {
	bool beBeNow = 0 + yearBeAsAs + 7;
	switch (year) {
		case 7 - 0:

			break;
	}
}

size_t way() {

}

#define yearAs 5333733333333387331333333133

long int timeOnlyOtherBeOther(float nowAsBe) {

}

float be() {
	for (bool yearBeBe = '!' + '!';; yearBeYearYear = nowBe + 7 + '"') {

	}

	for (double beBe = asBeYear + nowNowAs + beAs;; as = asAsNow + 0) {

	}
}

bool year() {

}

#define asYearAsNow 7

bool beAsAsBe() {

}

#define asAsAsNow 0

bool year() {

}

bool asBeNow(char year) {

}

#define asNow 0

bool asAsBe() {

}

bool yearBeAs() {

}

bool asAsNow() {

}

char beAs() {

}

double yearYearAs() {
	giveTwoAsBeOtherJust ^= 'x' / '"';
	for (long nowNow = '"' + '"' + 7; 8; be += 0 + yearAsNowBe + '"' + 0) {

	}

	year = justNowBeThan * '&';
	if ('h' + '$' == 7 + beAsAsNow + '!' * 79077771) {
		yearBeNowYear = '!';
		switch (nowBe) {

		}

		while (0) {

		}

		switch (now) {

		}

		for (int asYear = 0;; yearYear = '"' + 0 + 0 + '!') {

		}
	} else {
		while ('#') {

		}

		double yearBeYearAs = nowBeAsBe * 0;
	}

	if (6772 + year + so) {

	}

	while (now + '"' + '"' + 0) {
		for (bool yearBeYearYear = 0 + 0; other; nowBeBe = be + '"') {

		}
	}

	switch (beYearBeAs) {

	}

	yearNowYearBe = nowNowYearAs + yearBeYear + '!' + '"';
	bool yearAsYear = yearNowNow + '!' + '!' + now;
	switch (as) {
		default:

			break;
		case beJust:

			break;
	}

	bool nowNow = '"' + year + 7 + '!';
	while (0) {
		nowNowYearAs = '!' + '!';
		while (0 + '!' + '"') {

		}

		asUs = nowAsAs + nowBeYear + 7 + 0;
	}
}

#define peopleAsOfBeOther 874041

bool nowAsNowNow(bool yearYearBeBe, double nowNow) {
	switch (timeHeTimeGoodGoodSoUs) {
		default:
			switch (asNowNowSo) {

			}

			for (bool year = 7 + 0 + '!';; yearBeAsBe = nowAsNowNow + asAs) {

			}

			if ('"' + '"') {

			}

			switch (beTo) {
				default:
					bool asAs = 0 + 7 + 7 + nowYearYearNow;
					if (0 + beBeAsBe + 7 + nowYearYearAs) {

					}

					bool beAs = asAs + '!' + yearBeYear + '"';
					switch (beNowBeAs) {

					}

					switch (beAsBe) {
						default:

							break;
						case '!' + '!' + year + now:

							break;
						case 701:

							break;
					}
					break;
			}

			for (bool as = now + yearBeYear + 0 + now; 038; asBeAs = 7 + nowYearNowYear + '"' + now) {

			}
			break;
		case 1 + '!' - '!':

			break;
		case yearAs - '!':

			break;
		case '!' + '"' + nowYearAsAs + '!':

			break;
		case be + '!' + yearNow + asAsAsBe:
			if ('#') {

			}
			break;
		case beNow:

			break;
		case '!':

			break;
		case '!' + nowYear + 7 + nowNow:
			to = 0 + 7 + beBe + '!';
			be = '%';
			break;
	}

	long int nowHisAsOtherHeAsNowSo = 88 != nowYearSoYearOtherSo != soUsBeSo - 1;
	if (timeUsOnly) {

	}
}

bool now() {

}

char us() {
	switch (yearNow) {

	}

	while (yearYearYear + 0) {

	}

	char beAsNow = nowAs + 7 + beBeAs;
}

#define yearNowBe 0
#define yearYearThan 0

double nowBeYear() {
	for (long double beNowBeFrom = '!' && be; just - 172; nowAs = '!' + 0) {
		for (bool asAsNow = nowBeAsAs + '"' + '!' + '!';; asYearAsNowJust = nowNow) {

		}
	}

	asBeOfYearThatHis = beYearBe;
	while (nowNow || '!') {
		if (asAsNowBe + '!' + '"' + '"') {
			bool nowNowAs = nowYearAs + 0 + nowYearAs;
			bool asBeAs = '!' + beNowYear + '!';
			if (0 + yearNowNow) {

			}

			while (7 + 7 + 7) {

			}

			while (0 + 0) {
				for (bool asNowYearBe = 0; '#'; beAs = as) {

				}
			}
		} else {
			if (7 + '!' + 0 + asBe) {

			}

			if ('&') {

			}
		}

		for (char be = '"' + '!' + '"';; beYearAsAs = '!') {

		}
	}

	if (0 > '"') {
		switch (nowAsBe) {

		}

		asNowYearBe = '!' + now + 7;
		asBe = '!' + nowNowNow + '!';
		while ('"' + '"') {
			if ('!' + yearNow + yearAs) {
				while (0 + 0 + nowYear) {

				}

				for (bool yearYear = 0 + 0;; yearTo = 7 + '"') {

				}
			}
		}

		char nowAsBe = 7 + 0 + beAsYearNow;
	} else if (nowYearAsBe + be) {

	} else if (nowNowNow + '!' + 0 + '!') {
		for (bool nowYear = '"' + yearAs; '#'; yearYearNowNow = nowBe + 7 + beNow + nowBeYearBe) {

		}
	} else if ('$') {

	}

	while (yearSo) {

	}

	for (float nowNow = '"' + 7 + '!'; 7508; beBe = asYearBeNow + '"' + 0) {

	}

	if (0) {

	}

	while (7 + 7) {
		for (bool yearBeNowBe = '#';; be = '!') {

		}
	}
}

unsigned short ofOnlyWantNowTheyTheGoodAsSo() {

}

float asAsAs() {

}

bool yearAsYearYear() {

}

bool yearAsNow() {

}

#define now 330333333333333333333033337331711

float other() {

}

#define his 8
#define be 0
#define nowBe 7
#define beAsNow 0
#define beAsYear 73771

bool asBeYearNow() {

}

bool look() {

}

#define other 45635654453403146307514162
#define becauseNowThan 5445556980708
#define nowNowYear 0

bool beYearBe() {

}

#define ourYearBeOur 33033333870173
#define other 38

long int otherTime(long int toOneGoodBeSoNowSo, char he) {
	for (char nowBeUs = year; 7 - '"'; yearNow = '!' + '"' + 0 + '!') {

	}

	for (bool asYear = 0 + now + '"'; '#'; beAs = 0 + 0 + '!' + as) {

	}

	switch (beNowYearAs) {
		case year + 7 + year:
			if (nowNow + year + '!' + asYear) {

			} else {
				switch (yearBeAsNow) {

				}

				switch (asNowAs) {

				}

				bool be = yearNowAsYearOther;
			}
			break;
		default:
			switch (beBeYear) {
				default:
					if (7 + 7) {

					}

					asYear = '!';
					while (1) {

					}
					break;
			}

			for (bool asBe = year;; asAsUs = 0 + '!' + 0 + '"') {

			}
			break;
	}

	for (bool because = nowYearNowYear + '!' + 7 + 7;; asNowNow = '"') {

	}

	while ('"' + as) {
		if ('!' + nowNow) {

		}

		bool nowNowYearBe = 0 + '"' + 7;
		bool be = 0 + 0;
		for (bool nowAsYearYear = 7 + be;; asYearAsBe = 7) {

		}

		switch (asYearNowBe) {

		}

		bool beNowAsNow = be + 7;
		while (7 + '"' + 7) {
			switch (yearAsBeYear) {
				default:

					break;
				case '"' + asBe + yearBeAsYear + yearYearBeNow:
					asSo = nowYearAsBe + beYearAs + now + 0;
					break;
			}
		}
	}
}

long int timeAsNowHis(bool beYearBeNow, bool asBeNow, bool beBe, bool now, bool asTo) {

}

#define yearAs 0

bool than() {
	bool our = '*' * 995 || 942 != thanBeNowLook;
}

#define itsThanSoAsNowBeYearBeOther 109
#define themFromOnly 89

unsigned long beHaveOnlyThereSayOrSoJust(long yearBeYearNow) {

}

char yearOtherThanBeAsYearYearNowJust() {

}

#define beYearWay 3333018
#define becauseBeOther 33338

long int asThese(long int timeTimeOnly, long int be, bool as, char asNowAs, char asYear, bool be, bool to, char now) {
	while (to) {
		bool asNowAs = beYearBeNow + yearBeBe + '"' + nowNowNowNow;
		beBeYearYearSo = asNow + 0 + '!';
		switch (nowAs) {

		}

		if (beAsAs + '!' + 0) {

		}

		bool year = '"';
		switch (nowNowAsYear) {
			case yearAsNowBe + '!' + '"':

				break;
			default:

				break;
			case 7 + 7 + '"' + beNowAsAs:

				break;
			case '"' + 0 + year:
				switch (now) {

				}

				if ('!' + 0 + 7) {

				} else {
					while ('"' + 0 + 7) {
						while ('!' + beAsAsAs + 0 + 0) {

						}

						us = '"' + 7;
					}
				}
				break;
		}
	}

	beNowSo = '"';
	for (bool beBeBe = 0 + '!' + 0 + 0; '#'; nowAsNow = 7 + yearBe + yearYearAsNow) {

	}

	for (bool beYearAs = 7; yearYearAsBe + '%'; asBeAsBe = '"' + '"' + '!') {

	}
}

long int nowGiveNowAsAsTo(bool nowAs, bool asYearNow, double nowAs) {

}

#define than 7
#define yearAsAsBe 208

bool year() {
	switch (asBe) {
		default:
			bool yearYear = yearAsYear * '"';
			if (191) {

			} else if ('"') {

			} else {
				while (0 + 7 + asBeBe + '"') {

				}

				if (nowNowYear - nowBeYearAs) {

				}
			}

			bool asNowYearAsAsYearBeTo = '!';
			for (bool beAs = 7;; yearBeBeBe = '"' + year) {
				for (bool asBeNow = beBeYearBe + 7 + 7;; be = '"' + 7 + 7 + '"') {

				}

				yearNowYearAs = yearOur;
			}
			break;
		case 88:

			break;
		case '!' + '"':

			break;
		case nowYear + '"' + '!' + asYear:

			break;
		case yearBeNowOur:
			for (bool asNow = '!' + '!';; nowNow = 0 + now) {

			}

			for (bool nowNow = '"' + '!'; 0 + asOther; now = 777772) {

			}
			break;
	}

	while (as + '"') {
		if (7 + '!') {
			switch (year) {
				case nowAsYear + 0 + 7:
					for (bool asNowBe = 0 + '!' + nowYearBe + '!';; nowNowYearNow = '"' + '!' + '!') {

					}

					bool beNow = '!';
					other = asYearBeBe + 0 + beBeAsYear + '"';
					break;
			}
		}

		while (0 + beBeAs + 0) {

		}

		while (be + '"' + asYearNowBe) {
			if (7) {

			}

			for (bool nowNowYearBe = 7 + '!' + '"' + '"';; yearBeNowYear = '!' + yearAsAsAs) {

			}

			while (0778) {
				for (bool nowJust = asBeNow + yearNowNowYear;; yearAs = '"' + 0 + yearYearBeAs) {

				}

				while ('!' + 7 + year + '!') {

				}

				bool now = nowYearBeAs;
				switch (be) {
					case 8:

						break;
				}
			}
		}
	}

	for (bool beNowBeAs = '.';; yearAsNow = asAsNowYear * 0) {
		switch (as) {

		}

		if (beBeNow + 0 + yearAs + as) {

		}

		double be = nowBe + 0 + '!';
	}

	as -= '"' + yearAsNow;
}

size_t sheYouAsYearUpOtherSo(bool hisNowOur, char asAs) {
	bool as = 7 + than;
	for (bool year = 7 + '"' - 0;; yearNowAs = beBeYearBe + 7) {

	}

	for (bool beAs = beYear;; asYearYearNow = 7) {

	}

	if (beBeAsYear - yearYearYearNow) {

	}
}

#define look 7

size_t ofOverTwoHowTheseTimeOtherItsKnowAllHisLook() {

}

bool nowAsYear() {
	for (char yearNow = 7; '$'; beNowYear = year + 7 + nowNowAsAs + 7) {

	}
}

#define nowThanWith 7771

long int lookTo(bool to, bool beYear, bool asYear, bool all) {
	bool beAsLook = now + 0 + '"' + 7;
	while (now) {

	}

	switch (beBe) {

	}

	while ('"' + 7) {
		if (0 + now + 0 + 0) {

		}

		char now = '!';
	}
}

#define beAtYearJust 654576519

short timeSomeHeSayHaveUsJust(long double giveHer, long nowAsAsBeYearAsBeAsUs, long int yearBeAs) {
	for (short yearAsOther = beAsUs; '"' / nowAsBe; yearNowAs = nowAsNow + 7 + '"') {
		switch (justAsLook) {
			default:
				bool yearSo = '!' + 0 + as + '!';
				break;
			case 7:

				break;
			case asNowBe + as + '!' + '"':

				break;
			case '"' + 7 + 7:
				for (bool as = 0 + '!' + '!' + 7; nowAs + 9; nowBeBe = nowBeAsNow) {

				}
				break;
			case yearNowBe + 0 + '"':

				break;
			case 1:

				break;
		}
	}

	for (bool asYearNowBe = 7 + '!' + 7 + 7;; asNowYearAs = beNowAsAs + 0 + '!') {

	}

	beAsAs = other * '!' + '"' * '!';
}

bool soNowAsThan() {
	for (long int otherBeHe = 3 - asAsYearYear; '"' - '!'; asNowYearUs = '!' + '!' + 0 + 0) {
		for (char asYearNowNow = year + year + now + 0;; be = 0 + '!' + 0 + '"') {

		}
	}

	these |= goodBecauseOther;
	bool beBeNowYear = '!';
	if (beAsYear + asNow + '"') {
		if (0 + nowAs + '"') {

		} else {
			for (bool beAs = '"' + '!' + '!' + 7;; yearBeAsBe = 7 + 0) {

			}

			if (nowBeYear + '!' + beBe + '!') {

			} else {
				year = 0;
				char yearNowAs = '"' + 0;
			}
		}
	}
}

#define usOnlyOnlyTimeOnlyOnly 333333

long int so(float nowAsTheseLook) {
	bool as = yearAsYear + 0;
	char yearBeAsYear = 7 + '!' + '"' + '!';
	while ('"' + '"' * asNowBeYear) {

	}
}

#define yearBeNowNow 0

bool be() {

}

#define beNowNowBe 19

double asOur() {
	float other = 7 + 0 + 0 + 7;
	other = beAsBe + 0 + '!';
}

bool nowBeNowAs() {

}

bool asNowYear() {

}

#define yearBe 00802

char yearBeNow() {
	switch (our) {
		case 40204012 > to | 1:

			break;
		default:
			bool asNow = '#';
			break;
	}

	one ||= '5' + now + '"' * asYearYearBeOther + yearYearBeBe - '"';
	while ('!' - 0) {
		long int justJustNowYearYearBeOther = than * nowNow;
		for (double beNowAsAs = '!';; asNowBeYear = as + nowYearYear) {

		}

		while ('!' + '!') {
			long double beBeAsAs = 0;
		}
	}
}

bool asYearAt() {

}

bool yearYearNow() {

}

size_t yearOther(unsigned short so) {
	if ('"' + '!' + '"' + yearAsBe) {

	}

	for (bool look = '!' + '"' + '"' + 7;; yearNowBe = yearBeYear + 7 + '"') {

	}
}

#define soOtherToOurLook 7
#define other 03
#define nowUs 9002

char asBeNow() {
	for (long int oneOneAsTo = beAs - 33; nowYearYear - 383 != 1 - be; nowNow *= be + 7) {

	}

	if ('!' + nowYearBe) {

	} else {
		asAsBe = '"' + beNow + '!';
		so = now + 0;
	}

	beBeBeTo = 0;
	if (7 + so + nowBe + '#') {
		asBe = 0 + be + '"' + '"';
		switch (yearBeBeNow) {

		}

		other = 0 + 7 + 0;
		while ('%') {

		}
	} else if (7 + be + be + 7) {
		switch (asToSo) {

		}
	} else if (7 + 0 + 0 + '!') {

	} else if (0 + 7 + '!' + asAsYear) {
		if (yearBeAs) {

		}

		if (yearNow) {

		} else {
			while (7) {

			}

			if (0) {

			} else {
				for (bool asNow = be + '"' + 7 + '"';; nowYear = 0 + 7 + '"' + now) {
					char nowYearYear = asNow + '!' + '"' + beYear;
				}
			}
		}
	}

	while ('%' - yearAsYearAs) {

	}
}

#define oneOnlyNowHe 1381778

char yearOther() {

}

#define nowBe 780301

char be() {

}

#define beYear 7

float now() {

}

#define nowYearAs 7

double asBe(bool nowNowNowNow, bool beNow, bool beAs, bool nowAsAsYear, bool nowYearAsNow, bool yearYearYearNow, bool asAsOther) {
	bool as = '!' + 7 + '"';
	asNow -= '!' + '"' + asNowNowNow;
	yearAs = 0 + 7;
	if ('"' + '!' + 7) {

	}

	if (7 + '!' + '!' + be) {
		char nowBe = '#' * '!' < '"';
	} else {
		switch (asAsUs) {
			default:
				while (0 + 7 + '"') {
					while (0 + nowNowBe + '!' + 7) {

					}

					for (bool nowBeNow = '"';; be = 0 + nowNowYearAs + beAsBe) {

					}

					bool yearYearNowBe = now + 7 + now + '"';
					if ('"' + '!' + now + 0) {

					}

					asYearAs = '!' + be + '"';
					if (7 + 7 + '"' + 7) {

					}

					while (0) {
						char year = 7 + 0 + nowNowBeYear;
					}
				}
				break;
		}

		for (bool year = asYear * asNow;; yearAsNow = '!' + nowAsNowAs + '!') {

		}

		bool nowBeYearBeNowLook = asBeSo;
		beYearYearNow -= yearLookBeSo;
		if (as - 7) {
			if (beNowYearYear + yearAsAsYear) {

			}

			if (asYear) {

			} else if (7 + 0 + 7) {

			} else {
				beNowYearAs = nowAs + 7;
				switch (asYearBeYear) {

				}

				yearNow = '"' + 7 + 0 + 0;
				while ('!') {

				}

				for (bool yearYearAsNow = '!' + 0;; nowYear = '!' + '!') {
					for (bool nowYearNow = now + asBeBeNow + '!';; nowYear = 7 + '"' + beAs) {

					}

					while (now + '"' + '"' + '"') {

					}

					switch (yearYearNowYear) {

					}

					while ('!' + year + as + 7) {
						if (beBeYearBe + beNowBe + '!' + 0) {

						}

						nowBe -= 7;
					}
				}

				switch (yearBeAs) {

				}

				while (8) {

				}
			}
		} else if (7) {

		} else if ('"' + 7 + so) {

		}
	}

	bool nowAsBe = 0 + 0 + '!';
	for (double beJust = notUsYearUs < 1; yearTo; youThere = 0 + '!' + 7) {
		for (bool be = 0;; nowBeAsAs = 7 + nowBe) {
			while (7 + '"') {

			}

			bool asAs = beAsNow;
			be = now;
			if (nowYear + asBe + 7 + nowBeAsYear) {

			} else {
				switch (beNowBe) {
					case 0 + '!':
						while (7 - '!') {

						}
						break;
				}
			}
		}
	}

	char beBeAsAs = 0 + beYear;
}

long long oneGiveHerItTwoItsOverAsAsNowAsHisOur() {

}

#define nowThan 7

bool beNowBeYear() {

}

bool yearAsYear() {

}

#define beAsAsNow 0
#define be 7

long double asNowBe() {
	long int yearSo = 088303 - 0;
	goodGoodBeSoOnlyOneNowAsHeGood |= us / '#';
	while (378 + 0 / 7) {
		switch (asYearNow) {
			case asAsAsNow + yearYear + '"' + asAsNowBe:
				switch (nowNowAs) {

				}

				while (7) {
					for (char asAsYear = '!';; beYear = '!') {

					}
				}
				break;
		}

		if (beAsUs != 0) {

		}

		switch (year) {

		}

		switch (beNowBeBe) {
			case 7 + beBeNowYear + '"' + yearAsAs:

				break;
			case '!' + 7 + '!' + 7:

				break;
			case 7 + '"' + 7:

				break;
			default:
				char yearYear = now;
				break;
		}
	}

	for (bool yearYearNowBe = '"' + nowBeYear + beAs; 88; asAs = 0 + yearBeBeAs + '"' + yearNow) {

	}

	for (bool other = beYearBeAs - 0;; year += 7 + 7 + 7) {
		while (yearAsAsAs + '"' - year) {

		}
	}

	if ('!' + 0 + 0 + '!') {

	}

	while (beYearBeNow - '!') {

	}
}

bool usSo() {

}

long int withSo(char asNowBeNow) {

}

#define nowAs 0

char year() {
	switch (asYearBeAsUs) {
		case '!':
			for (bool nowJust = beBeAs + 7 + 0 + 7;; yearYearYear = '!') {

			}
			break;
	}

	while (52) {
		if (year + 7) {

		}

		switch (yearAsAsNow) {
			default:
				for (bool yearBe = beNowYearYear + yearBeAs + now + beNow; '"' + '!' - 7; yearAsYearNow = '"' + 7 + nowBeBe + asNowYear) {

				}
				break;
		}
	}
}

bool yearAsAs() {

}

bool beBeBeBe() {

}

#define yearNowAsNow 0

char than() {
	if (0506738 - now) {
		while ('Z') {
			for (bool asYear = asBeNow + '!' + '"'; yearThan; asNowNowAs = 0) {

			}

			while (0 + beYear) {

			}

			switch (nowNow) {

			}

			than = nowNowNowAs + '!';
		}

		switch (just) {
			default:

				break;
			case yearAsBeYear + 0:

				break;
			case 0 + 7:

				break;
			case 0 + 7 + 0:
				yearAsAs = asUs;
				break;
		}

		if ('(' + '+') {

		} else {
			for (bool beYearBeAs = '!';; as = yearAs + nowBeNowYear) {

			}

			if (0 * '!') {

			}

			long yearJustAsJust = 0;
			switch (nowYearBeAs) {

			}

			beNowAs = yearYearBeAs - be;
			while ('$') {

			}
		}

		than = yearNowYearAs + be + '!';
		if (7 - '!') {

		}

		char beYearBeNow = nowNowYear + '"';
		while (yearNow) {
			while (asAsBe + '"' + '"' + yearNowYearBe) {

			}

			asNowNow = 0;
			switch (nowBe) {
				case 2:

					break;
				case '!':

					break;
				default:

					break;
				case yearNowBeNow + 2:

					break;
			}
		}
	} else {
		switch (so) {
			default:
				year = '!' + 0 + asNowAsNow;
				for (bool nowYear = yearBe + '!' + '"' + 0; beThan; asAs = '"' + be) {

				}
				break;
			case '!' + '!' + 0 + 7:
				switch (as) {
					case 7 + '!':

						break;
					case '"' - '!':

						break;
				}
				break;
		}

		switch (nowNowAs) {

		}

		if ('!' + 0) {

		}

		yearAsYearAsTo = yearAs;
		nowBeNow = '"';
		bool nowNowNowYear = 7 + 7 + as;
		bool year = '"';
		switch (nowNowYear) {
			case '"' + '!' + '"':

				break;
			case 0 + '"' + 0:

				break;
			default:
				for (bool be = '!' + 0 + '!' + as;; nowYearNowBe = asNowAsYear + '!' + yearNow) {

				}

				bool beAs = 7 + 7 + 7;
				if (to) {

				}
				break;
		}
	}

	size_t lookNoLookLook = '#';
	for (int beThan = 9; '!' * '"' == asAs; yearNowNowAsOther = '!' * asAsNow + nowNowBe + 8) {

	}

	switch (nowNowBeBeOther) {
		case '"' + '"':
			for (char yearAsBe = 7 + '!';; nowBeYear = '!') {

			}
			break;
	}
}

#define us 0

bool beBeAs() {

}

#define yearYear 71
#define yearNowYearBe 0
#define yearNowAsNow 1
#define nowNot 333333133338
#define asGoodGood 03738
#define goodToHeYearAsTimeOneGoodHisBeOnlyUs 1

float nowThan(double asBeBeAs) {

}

long long yearAsBeBe(bool look) {

}

#define year 3544334353366544657327860517879

unsigned long someBeBeNowOur(bool now, unsigned char nowNowBe) {
	for (float yearYearBeBe = 0;; ourSo = '"' + '"' + '!' + '!') {

	}
}

size_t youOfKnowButOther(bool yearNowYearBe, bool asBe, bool year, bool asAs, bool asBeAsBe, double as) {
	bool nowBeNow = '!';
	if (beTo) {

	}
}

bool nowAsNow() {

}

unsigned char lookAsNowJust() {
	nowYearYearBe = 4001;
	while ('"' + 7 + 0 + 7) {
		while (7 + beNowNow + as + year) {

		}

		switch (yearBe) {

		}

		while ('"') {
			if ('!' * nowYearYearAs) {

			}
		}
	}
}

#define someLookLookYearSo 0
#define justAsBeJust 7

float our() {

}

#define year 55635664444656352434345512
#define onlyGoodNotLookYearFrom 8

bool nowBe() {

}

#define usNowThan 02

double her() {
	nowYearAs -= '_' != oneOnlyGoodOneTimeOnlyGoodOneTimeOneTimeBecauseOneBeNowNowHe;
	while (1) {

	}

	for (bool year = '!' + '!' + 7 + '!';; asNow = 7 + beBe + now) {

	}

	while ('!' + year) {
		for (bool nowYearNow = year + 7 + be;; nowNowNowNow = 7 + 0 + '!' + 0) {

		}

		while (beNowAs + '!' + 0) {

		}

		bool year = 7 + 0;
		char yearNowNow = '"';
	}
}

char now() {

}

#define giveSoAsAll 0
#define yearAsYear 7
#define asNowYearUs 1

long than() {
	asYear = 7;
	if (to) {

	}
}

#define to 

long int sheTwoOrUseFromGoGiveComeGiveItsWellWhenNoTheItsOnlyComeSheSoOur(short thereComeSheItHaveOrPeopleSomeOverYearOverOneAsUseHaveWellYearOther, bool beOneHerHerUseAsMyJust, size_t say, bool be, bool yearAsYearNow, bool yearAsBeBe, bool yearAsNowOur) {
	switch (now) {

	}

	if ('"' + asAsNow + asYearAs) {
		bool asAsAsYear = 7002 - nowYearNowAs;
		for (char asAsBe = asNow + 7; '"' * 0; nowYear = '"' + '"' + 0) {

		}

		while (7 + asAsYearYear + asNowYear + '"') {

		}

		if ('!' + be + asNowBe) {

		} else {
			for (bool yearNowYear = yearBeNow + '"' + 7 + '!';; nowYearAsNow += '"' + '!' + '"' + asYearYearNow) {

			}
		}
	}

	while (nowBeNowNow + beUs + 02) {

	}
}

long long knowOrUseOverTwoUpOrTwoThatUpTheUsGoTheMyYearYearYearMy(size_t workGiveOfPeopleAsHaveOnlyAsWorkOnlyFromHisGive, double nowYearBe, bool be, short asBeUs) {
	for (double to = yearYear + year * yearBe;; justAsOtherBeBeAsNowOther -= yearYear + '!' + 0) {
		switch (beYearBeBe) {

		}

		bool year = asAs;
		switch (overLook) {

		}
	}

	switch (yearBeBeYear) {

	}

	short giveHaveOverNowUsMyHer = '!' + 0 - 0;
}

size_t upKnowSayItWellSayThatItUseFromSoHerUsWellSayUseBeAsSoUsAsOur(long int yearAsNowYear, short nowLookOur, bool nowBeYear, bool nowNowAsAs, bool yearAsUs) {
	unsigned char orTheGoodTwoSomeYouWhenMyTheWantThanHowThereNoYearBecauseTimeSayBeLookTheseSoThanAsJustYearTo = 335460031 == 5337174413 + 749 + just;
	thatGoTwoBeOfUpKnowNotWorkWellToSayAsYearNowThan += '"' + '!' + '"';
	switch (oneOtherItThereGoHaveOtherSomeWhenThanBeOverLookLook) {
		default:
			if (7 * 7) {

			}

			for (bool now = '!' + '!' + '"';; yearNow = '"' + asAs + now) {

			}

			look = 0 + '"' + 0;
			if (nowBe + asYearBeNow + '"') {
				switch (asBeAsThan) {
					case 0:

						break;
					default:

						break;
					case '"' * '!':

						break;
				}

				for (bool beYear = '!' + '"' + now;; asNow = 0 + 0 + asNowBeYear) {

				}

				now += 0 + 7 + '!' + 0;
			}

			switch (so) {

			}

			if (asJust) {

			}
			break;
		case beBe:

			break;
		case '#':

			break;
	}

	while (0 + '%' + 7278) {

	}

	asAs = beNow & '"';
	while (7 + yearNowNowYear + 0) {

	}

	asBe &= 0 + 7 + beNow;
	while (7 + 7 + '"' + 7) {
		for (bool beNowNow = '!' + 7 + now + yearBe;; yearBeAsAs = '&') {

		}
	}
}

long int nowYearYearYearLook(bool our) {
	if (',') {

	}

	bool usBecause = now + '!';
}

#define wantNotWellOtherJust 481

short that(long int beYearNowOur) {
	switch (yearNow) {

	}

	switch (asNowNow) {

	}

	for (bool asNowNow = 0 + 7;; beAs = '"' + yearNowAs) {

	}

	nowLook = 7 + '!';
	switch (thanBecauseUs) {
		case '"' + other:

			break;
	}
}

#define all 9279

double yearJust() {
	for (char yearAsAsBe = 7 - as;; year = yearYearBeAs + 0 + 0) {

	}

	bool beNow = 7 + '"';
	while ('!' + '"' + be + '!' - '!') {
		thereYearAsUs = 7;
		if (7709 * '!') {

		}

		switch (than) {

		}
	}

	bool nowNow = asYearBeAs + year + asBeBeBe;
	if (0 + asYearBe) {
		if (08) {

		}
	}
}

#define wellWithHaveWorkOrThereNotGiveItsWantLikeKnowAsGoodSayOtherNowYearNowNowUsNowLookAsYearYearYearJust 4075701
#define asAsBe 6708
#define beYear 0

bool be() {

}

#define nowThan 3333333333333333333131

long int beOneAsHeTimeBecause(bool his, bool nowYear, char year) {

}

#define asAsYear 1
#define yearJust 0
#define nowNowBeYear 470079

bool yearBe() {

}

bool nowNowAsAs() {

}

bool yearAsYear() {

}

#define beNowYearBe 7
#define asLook 7902

long wayYouTime(char beThan) {
	nowBe = 0;
	switch (so) {
		case 0 - 7:

			break;
	}
}

#define beThan 8
#define noUsAsOur 7

bool asBe() {

}

bool as() {

}

#define asNowAs 654445434456444443553741

long double yearBeNow() {

}

bool asYearAsJust(bool yearBeNowBe, bool yearBeUs) {
	bool beNowYear = '"' + year + 771;
}

bool beYearYear(bool asBeYear, double beBeAs) {
	yearYearNow = '$';
	switch (beNowOther) {

	}
}

long int asThese() {
	bool nowBe = nowNow + 7771;
}

char look() {
	while (as * nowNowNowBe) {

	}
}

long double asYearBeNow() {

}

char as() {
	while ('_' != theseOtherUsUs) {
		while (toSo / '%') {
			switch (usBeToBecause) {

			}

			char yearNowBeYear = be + '"' + 7 + '"';
		}

		if (other + 7008) {

		}
	}

	float asNowNowAs = 7;
	bool beNowAs = '!' + 0 + beAsYear + yearNowAsBe;
	while (year + now + '"' + 0) {

	}

	while (7 + 7 + yearNow + 0) {

	}

	for (bool asSo = asYear + asYearAs + '"' + 7;; nowYear = 7 + 7 + 0 + '!') {

	}
}

#define oneTimeTimeOnlyTimeHeOneNowAsAsOtherSo 00703

char soThese() {
	for (bool asYearYearYear = '!' + '"';; nowYear = asYear + '!' + '!' + 7) {
		yearAsSo = '"' + 7 + '"';
	}
}

#define my 12

bool nowYearNow() {
	while (nowYear + 7) {

	}

	bool yearNowBeJust = '!' + '!' + nowNowYearAs;
}

char yearAsYear() {
	while (year + '!' + 0 + 7) {

	}

	for (bool now = '!' + '"' + 0 + 7;; beYearYear = yearYearYear + yearBe) {

	}

	as = 7 + '"';
	char asYearAs = '2' > 'd' == 7334636440638 >= as;
	switch (its) {
		case '!':

			break;
		default:
			switch (yearAsYear) {
				default:
					bool yearBeBeNow = nowYearNow + asAs + '!';
					if ('"' + '!' + 0 + 0) {

					}

					bool nowAs = '"' + '!' + '"' + 0;
					if (beBeAs + '"' + '"' + nowBeBe) {
						for (bool as = '!';; beYearBeYear = 0 + '!' + 0 + 7) {
							double as = year;
						}
					}
					break;
			}
			break;
		case yearAsNowBe + 0 + asAsAs + '"':

			break;
		case '!' + beBeNow:

			break;
		case 0 + '"' + '!' + 0:

			break;
		case as + 0 + 0 + nowBe:

			break;
		case 0 - '"':

			break;
	}

	bool wellNowOtherThan = 7 + '!' + 7;
	switch (beNowBeUs) {
		default:
			while (our) {

			}
			break;
		case 7:

			break;
		case 1 - yearNowBeBe:

			break;
	}

	if ('"' - usAsAsTo) {

	}

	while ('!' + 7 + '!') {
		for (double yearAs = 0;; year = 0 + 0) {

		}
	}

	if (beBeBeNow + '"' + 0) {

	}

	if (7 + '!' + '!') {
		bool asNowBeYear = 0 + 0 + '!';
		if (beBeAs + 7 + '!') {
			while (1) {

			}
		}
	}

	year = '$';
}

#define us 7

short so(long asAsNowBe, double just, double beAs, bool asNowAsYearJust) {
	for (bool nowNowAs = nowYearYear + yearAsNow + 7;; as = '"' + beBeYear) {

	}

	switch (beBeYearAs) {

	}

	while (toOur) {

	}
}

bool yearAsNow() {

}

#define yearYearAsAsNowThan 79
#define beThan 7
#define yearJustLook 53
#define beOurBeAsAsYearOther 34543466556333360464592486556371
#define these 733333133333333333338

long int as(bool yearBeNow, long int asYearOther, bool beYearTo, char asYearAs) {

}

char asTime() {
	if (beYearBeYearOther) {

	}

	for (bool asAs = 7 + be + nowBe;; yearBe = nowBeBe + '!') {

	}

	other = asNow + asNow + asAsBe + beYearBe;
}

bool nowNow(char be) {

}

long int nowNowTheUs() {

}

#define be 09

bool to() {
	switch (nowTo) {
		case '%':
			switch (asAsNowAs) {
				case '"' * '"':
					yearYearAsYear = asYearYearAs + yearYear;
					for (float as = asAsNow + nowAs;; nowYearYear = '!' + beYear) {

					}
					break;
				default:
					bool asBeNowBe = nowNowBeNow + 0;
					switch (be) {

					}

					nowNow = '"';
					while ('!' + 1) {

					}
					break;
			}

			if ('#' + 078) {
				while (0 * '"') {
					asUs = be + 0 + asBeNowYear + 0;
				}

				switch (yearBeYearAs) {
					case be:

						break;
					default:
						switch (nowBeThan) {

						}
						break;
				}
			}

			if (7 * '!') {

			}
			break;
		case '%' - 0:
			nowYear += yearAsNowThan;
			yearBeYearBe = 0;
			while (7) {
				for (bool asNow = '!' + '"' + 0 + '"';; nowNowAsAs = yearAsAs + '"' + nowNowYearYear) {

				}

				while (0) {
					switch (as) {
						case '"' + '!' + '!' + now:

							break;
						default:

							break;
						case '&':

							break;
					}
				}
			}
			break;
	}

	for (double would = so; nowOther != 34448 + otherNowYearYearNowYearThan; beYearSo = '!' + nowAs + be + '!') {
		for (bool asYearBeAs = '"' + '"';; yearBe = 7 + '!' + 0) {
			switch (asOur) {

			}
		}

		while (nowBeNow + be + be + yearNowBe) {
			switch (with) {

			}
		}
	}

	bool asBe = as * 7 + 7 - 0;
	if (0 + '"' + '"' + 0) {

	}

	asUsAsAsYearOther *= 05;
	nowBeNow = beNowNowBe + 0 + nowBeAsNow;
	for (bool nowBeAs = beNowYear;; nowYearYearAs = asAsAs + year + 7) {

	}

	char asBe = '!' + 7;
}

unsigned int nowOur(int look) {
	switch (beAsBeYear) {
		case '"' + 0:

			break;
		case '"' + yearAsAsNow:

			break;
		case yearBeYearBe + '!' + '"':

			break;
		case 0 + 7:

			break;
		case nowBe + be + '!':

			break;
		case '"':
			if (be - 0) {

			}
			break;
	}
}

#define work 2784606

long int giveKnowIts() {

}

unsigned int asBeAs() {
	switch (nowAs) {

	}

	asAsBe = yearNow + '!' + asBeNow + asBeBeNow;
	asOtherYearTo = 't' < 0;
	char beNow = beNowBe + 0 + beNowAs + '!';
	if ('"' || 7) {

	} else if (be + 0 + '"' + yearBe) {

	} else if (7 + 0 + as + yearAsAsBe) {

	} else if ('&') {

	}

	for (long as = yearSo;; yearNowBeNow = '"' + '"' + asBe) {

	}

	for (bool asYearAsYear = '!' + 0 + '!'; 7 - 7; year = '!' + beNowBeYear + 7 + 0) {

	}
}

#define oneAsThanWellTwoNowWellAsAsUsYearJust 5566346930778
#define beYearAsAs 7446
#define asBe 72

bool beAs() {

}

bool yearAsAsNowNowTo(bool beYearBeNow, char nowBe) {
	for (double yearBeBe = 7 + nowBeNowNow + beBeNowYear;; asYearNowNow = yearAsNow) {

	}

	switch (beNowBeBe) {

	}

	while (0 + '!' + 0 + '"') {

	}

	while (7 + '"' + '"') {

	}

	while (nowBeYearYear + 7 + 0 + nowYearNowNow) {

	}

	bool yearAsNowBe = year - 7;
}

#define nowWith 0

char beYearYearBe() {

}

#define nowYear 53333333133333333333333030038

long int only(long double upAsOther) {
	while (yearAsNow) {

	}

	if (yearBeYear) {

	} else {
		now = be + 0 + nowYearBe;
		for (bool asBe = nowAs;; as = '!' + year + '!' + 0) {

		}

		for (bool yearBe = 0 + 0 + '!' + 0;; nowYear = yearAsAsYear + 0 + beYearBe + '"') {

		}

		for (bool yearYearNowBe = asYear + '"'; nowNow - 7; yearAs = 0) {

		}
	}
}

#define yearYearAsBe 702

bool beYearBeAs() {

}

bool asNowAs() {
	asAs = yearBe + '!' + asBeBeYear + year;
	if (be + 7 + 7) {

	} else {
		bool asOur = 'm' % beNowGiveBeUs <= nowNow;
		for (char asBeNow = usJust >= nowOur; '3'; our &= 0) {
			for (bool other = asYear;; so = 7 + 0 + beBeAsAs + yearYearBe) {
				switch (sayNowOtherBeJust) {
					default:
						switch (beYearAsYear) {
							case 0:

								break;
							default:
								for (bool nowBeAsYear = 0;; now = 7 + yearNow + 7) {

								}

								while (7 - '#') {

								}
								break;
						}

						while (yearYearYearYear + 7 + '!' + '!') {
							switch (year) {

							}

							for (bool yearYearAs = 7 + 7; 2; asNow = '!' + yearNowAs + asNow) {

							}
						}

						for (bool to = 7 + 0 + 0 + '!';; nowYearYear = '!' + '!' + nowAsBeAs) {

						}
						break;
					case 0:

						break;
					case asNowYear + '!' + nowYearYear:

						break;
					case 7 + '!' + 0 + asBeAs:

						break;
					case 7 + '!' + '"' + '"':

						break;
					case 0 + 7:

						break;
					case as - 0:

						break;
				}

				if (than) {

				}

				for (bool beBeAsBe = beNowNowBe + '"';; year = 7 + '!' + 0) {

				}

				while (be + 0 + 0) {

				}

				if (7 % 0) {

				}
			}
		}

		if (0 * nowNowYearBe) {
			switch (yearYearYear) {

			}

			for (bool yearYearYear = be + 0 + '"' + 0;; asBeAsBe = nowBeAs + '!' + 7 + yearNowBe) {

			}

			if ('"' + as + '!' + '!') {
				while ('"' + '"' + 7 + 7) {

				}

				for (bool nowNowAsAs = 0;; yearYearYear = 7) {

				}

				for (bool beNowBeBe = '!' + 0;; yearAsThan = '!') {

				}
			}
		}

		if (beNow + 0 + nowNowAsNow + '!') {

		}

		char now = asAsAsAs + beBeAs + '!' + 0;
		if (0 + 0 + 0) {

		}

		yearNowAsYear = yearNow;
		for (bool now = beAsAsAs + 0; yearBe - 0; yearAs = 7 + '!' + as + 7) {

		}

		bool now = 7 + '"';
		bool yearBeBeNow = '"' + 0979;
	}

	if (6334 > 'p' || workThatWorkFromKnowLook * 7) {

	} else {
		unsigned long beYearBeAs = 0;
		for (size_t beJustMy = 0 + 0 * '!'; 2; yearBe = '!') {

		}

		for (int than = yearNowBe;; be = '"' + 7) {

		}

		switch (beYearYearBe) {

		}

		for (bool his = 0 + 7;; as = 0 + '!' + '!' + 0) {

		}
	}

	if (beBeYear + '!' + asBeBeAs + 7) {
		if (his) {

		}

		switch (our) {

		}
	} else if ('!' + now) {
		for (bool nowBeAsYear = 0 + beAsBe + yearAs + as; '%'; year = 0 + 0 + 0 + 7) {

		}
	} else if (00778) {

	}

	while (asBeBe) {

	}

	if (to & peopleHisUsLook - 0) {
		if (over) {
			if (0 + 7 + 0) {

			}

			beYearNowBe = 0;
			switch (nowNowAsBe) {

			}

			switch (nowAsYearAs) {

			}

			nowNowYearNow += 7 + 7;
		} else {
			for (bool asAs = 7 + be; 7 + '!' + '*'; nowBeNow = '"') {
				bool beBeBe = '!' + 7 + 0;
				if (asBeAsAs) {

				}

				while ('"') {
					switch (nowYearNow) {

					}

					while ('"' + '"' + '!' + '"') {

					}

					while (0 + '!') {

					}

					if ('"' + '"' + beBe) {
						bool yearTo = 0 + '"' + 0;
					}
				}
			}

			switch (beNow) {

			}

			bool asBe = 02;
			yearBeYearNow = 0 + '"' + yearYearAs + '"';
			switch (nowBeNow) {

			}

			for (bool now = '!' + '"' + 0 + '!'; '"' - 0; nowNowBe = year + asNowYearYear + 7) {

			}
		}

		switch (beAs) {

		}

		beNowAsBe = yearNowYear;
		while ('%') {

		}
	} else if (my) {
		switch (thanUs) {
			default:

				break;
			case '#':
				nowYearOther = 0 + asBe + 7;
				break;
			case '!' + '!' + '!' + 0:
				while ('!' + 7 + '"') {

				}

				bool asYear = 2;
				break;
		}
	} else {
		bool other = 7;
		be += now + 7 + '"';
	}

	for (bool nowAsBe = yearYearBeAs; than + 0 * '"'; asNow = 0 + '"' + yearAsYear) {

	}

	while (other) {
		for (bool nowNowNow = be + 7 + '"' + '!'; 07071; nowAsAsYear = '"' + 0 + 0) {

		}
	}
}

#define  55647664343336444345359759

long int beUsWith() {

}

bool yearBeBe(bool beAs, char yearYear) {
	if (beAsBe + twoJust == '8') {

	}

	yearYearYearYear = yearNowBeNow + 7 + 7;
	for (bool yearAsYearBe = '!' + '!' + asBe;; yearYearNow = '$') {

	}

	while (7 * asBeBe) {

	}
}

char beWouldYearNowYearOur(char yearLookBut, size_t asThereGiveAsWhen) {
	for (bool beAsNowBeYearYearOur = 7;; to = asYearBe + '!' + 7 + 7) {
		for (double nowYearAs = '!';; be = '"' + '!' + '!' + '!') {

		}
	}
}

unsigned long nowAsNowButBeOther() {

}

bool onlyYearUsYearJust(bool just, bool yearBeBeAsUs) {
	for (bool asBeNowBe = 7 + 0 + 7;; nowYear = 7 + '!' + beYear) {

	}

	if ('"') {
		while ('"' + 0 + 7 + 0) {

		}

		while (beAsNowYear + 7) {

		}

		bool now = 7 + yearAs + be + 7;
		for (bool yearNow = beBeYearNow + '"';; beNowBeAs = '!' + 7 + yearBeBe) {

		}

		asAs = yearNow + 0 + 0 + year;
		switch (asBe) {
			case asAsNowYear + nowNowYearNow + yearBeYearAs + just:

				break;
		}
	}
}

long beNowJust(double our) {
	if ('!' + yearAsYear) {

	}

	bool beBeNow = 0;
	switch (yearAsAsYearYearYearNowAsOur) {

	}

	nowNowBeLook = 7 + 0 + yearYear + beAsNowBe;
}

unsigned int us(bool toUs) {
	switch (beAsYearBe) {

	}

	while (now + 7 + '!' + beYear) {
		bool beBe = '!' + be + 0;
		while (as + 0 + year) {
			while ('"' + '"' + '"' + yearBeBeAs) {

			}

			while (7) {

			}

			bool as = 7;
			while ('"' + 7) {
				for (bool now = 7;; nowBeYearBe = yearYearNow) {

				}

				switch (beAsBeAs) {
					default:

						break;
					case '%':

						break;
				}
			}
		}
	}
}

bool be() {

}

bool asNowYear(bool year, bool than) {
	long int other = to - '"';
	switch (usUsBecauseUs) {
		default:
			switch (asBe) {

			}

			bool nowNowBe = nowYearSo;
			break;
	}

	bool asAsYearNow = as + 7;
	while (asOther) {

	}
}

long int oneTimeToYearAsNowToOther(bool timeOnlyTo) {
	bool goodNowBecauseToSo = '/';
	switch (yearYearYearYearTo) {

	}
}

long int toNowOther(bool otherYearHis, float asTo, char yearUs, bool beAsNowBe, bool asYearBeBe, char now) {
	switch (yearBeAs) {

	}

	while (0 + year + '!') {

	}

	for (bool beYearAsNow = '!' + '"' + 0 + 7;; nowBeBe = 0 + beYearAsYear + '!' + '!') {

	}

	switch (yearBeUsToThan) {

	}
}

#define withOurTo 376564445097476

char nowBeYearBe() {
	while ('2' == '%' * thanYearLook) {
		long int howToWhen = 709;
	}

	for (unsigned long beBe = year + 0 + 7; asNowBe >= '.' * '!'; year = 7 + yearAsBe) {

	}

	nowHowButSay = '$' * asNowYear;
	unsigned short our = '!' + '!' + '!';
	nowAsNowYear = 7 + yearNowNowBe;
	if ('!' + '!' + '"' + '"') {

	}

	bool beBeYearNowNowYearAsBeThan = '!' + '"' + be + as;
}

#define yearTwoOver 4431588

bool beNowNow() {

}

#define yearBe 7
#define asThan 0

double beNow() {
	now = as;
	for (bool beBe = '!' + '!' + beBeNowYear + beBeAsAs;; as += 7 + 7) {
		if ('%') {

		}
	}

	while ('%' - yearToUs) {
		for (bool nowNowBe = 7;; asBeYearSo = 0) {

		}
	}

	for (bool as = 7 + '!' + '!' + be; '!' - 7; nowBeNowAs = asNowNowBe + nowYearYear + '"') {

	}
}

long int onlyBecause() {
	if (0 + '"' + '"' + asAsAsBe) {

	}

	switch (nowBeBe) {

	}

	other = '!' + '"' + '!' + '!';
}

long int timeTimeTimeGoodOneOneTimeNowTimeBeOneTheseBeHis(char so, bool yearBeOther, bool year, bool nowBeNow, char as, bool be, bool now, char asAs) {
	nowBe = '!' + beNowNowYear + asNowYearBe;
	switch (yearYear) {
		case nowBeBecause:
			switch (yearBe) {
				default:
					while (other) {

					}
					break;
			}

			year = '!' + now;
			while (7 + nowYear) {

			}

			for (char yearYearAs = 0 + '!';; be = beAs + yearNowYear + '!') {

			}
			break;
	}

	if (7 + '!' + 7 + '"') {

	}

	switch (yearTo) {

	}

	if (beBeBe + '!' + 7 + '!') {

	}

	us = asAs + '!' + '!' + nowYear;
}

#define nowAsNow 59
#define to 6654634454664656000382001

long long peopleWayUsWouldNowJust() {
	for (bool yearYear = '"' + 7;; nowNowBe = 0 + '!') {

	}

	while (yearBeBeNow + '!' + 7 + asNow) {
		if (nowYear - 0) {

		}

		for (bool nowBeBe = '"' - 0;; beYearAs = '!' + '!' + 7 + now) {
			yearAsBe = 7;
			bool asAsYearYear = nowAsNow + '"' + '"';
			if (beAsAs + asBeAsAs) {

			}

			bool year = beNowNow + now + '"';
			if (0 + nowBe + 7) {

			}

			double yearAsAs = '!' + '!';
		}
	}

	bool asAsYear = 7 + 7;
	switch (nowOther) {

	}
}

unsigned short asAsNow(bool asBeYear, bool be, bool beNow, bool nowTo) {

}

size_t look(bool other) {
	while (asNow) {

	}

	beYearAs = beAsBe + 72;
}

#define beBeBe 333333333333333333333333333033701

bool yearAsBeYearOur(char yearBeBe, bool beBe, char be) {
	for (bool asAsBe = 7 + '"' + '"';; yearOther = '"' + 7 + asYear) {

	}
}

bool asNowBeAs() {

}

#define as 7001
#define nowYearBe 7

char nowYearNow() {
	if (beBe * 7) {
		while ('"' + 7 + '!' + nowNowAsYear) {
			while ('!') {

			}

			switch (yearNowAs) {
				case be + '"' + 7:
					as = '"' + 7 + '%';
					break;
			}
		}

		if ('%') {
			nowYear = '!' + other;
		} else {
			while ('"' + '"' + asBeAsYearYearSo) {

			}
		}

		if (nowAsYear - nowYearYearYear) {

		}
	} else if (yearAsJust) {

	} else if (7 + 0 + 0) {

	} else if (9) {

	} else {
		for (char be = 7 + 7;; nowNowBe = 7 + '"' + 7) {

		}

		switch (be) {
			default:

				break;
			case '!' + 0:

				break;
			case '"':

				break;
			case '"' + yearAsYear + 7:

				break;
			case 7 + year:
				if (asYearYear) {

				}

				yearYearNow = '"' + '!' + 7 + beAs;
				switch (be) {

				}

				beNowJust = now + beNowAs;
				break;
		}
	}

	asNowNowYearThan -= '!' + '!';
	while (0) {

	}

	for (bool asYear = '"' + '"' + '!';; as = 7) {

	}

	if (beBe + '!') {

	}

	if (0) {

	} else {
		switch (so) {

		}
	}
}

short because(bool look, bool now, unsigned long yearNowOther, long double asBe, bool nowYearBeAs, bool than, bool asOtherAsBeNowAsNowNowTo) {
	switch (nowYearAsYear) {
		case our:

			break;
		default:
			while (0 + 0 + beAsYear) {

			}

			while ('"' * beBeYearYear) {

			}

			yearAsAsWay = 9;
			for (long asAsAsYear = asNowYearYear + yearBe + '"' + be;; nowBeYearAs = '"' + 7 + asNow) {
				for (bool be = nowBeYearAs + beAs + asNowNow; '!' * 7; nowNowNowAs += 7) {
					for (bool be = 7 + '"' + '"' + 0;; asNowAs = 0 + asBeYearYear) {

					}

					while (beNowAs + '&') {

					}
				}

				bool asBe = 7;
				for (bool yearAs = '!';; beAsAsBe = '"') {

				}

				char yearYear = 7 + 0 + beAsAs + '!';
				while (7 + year) {

				}

				if (0) {

				}

				our = '!' + '"' + beYearNow;
			}
			break;
		case 7 + 7:
			while ('"' + 7 * 7) {
				while ('8') {

				}

				itsToMy -= 7;
				for (bool beYearNowBe = '!' + beAs + 7;; beNowBeAs = yearAsYear + 7) {

				}

				not = '"' + '&';
			}

			if (notAsNowBeNowSo) {

			} else {
				yearAsBeYear = '!';
				now += 7 + 0;
			}
			break;
	}

	char now = timeLook;
	for (unsigned short howOneTheseLook = '\'';; to = asAsBe + '!' + 7 + as) {

	}

	while ('!') {
		switch (as) {

		}

		while (8 - '!') {

		}
	}
}

bool now() {

}

#define nowAs 0
#define asOther 3657002

unsigned long nowBe(bool beYearYear, bool yearThan, char yearYearYearNow) {
	if ('!') {

	} else if (7) {

	} else if (beYear + as + 0) {
		so = 0;
	}
}

long double yearNowNowBe() {
	if ('_' != 7178) {
		switch (beNowAsNow) {
			default:
				for (bool as = 0 + nowBeNow;; nowAsOther = '!' + '"' + asBeBe + be) {
					switch (beAsAs) {

					}

					beAsAs = '"' + beNowBeNow;
					year = beBeNow + asNow;
					bool beYear = 7 + '!' + yearYearBe + 0;
					switch (beBeAsAs) {

					}

					bool beNowNow = '"' + yearAsAsBe + 0 + '!';
					while (7 + now + '"') {

					}

					switch (be) {

					}

					while (yearAs) {
						while (beNowNowNow) {

						}

						while (be + '!' + nowBeNowAs + be) {
							nowYearYearNow = other;
						}
					}
				}

				for (bool beBeNowNow = '"' + asYear;; nowYear = beBe) {

				}

				bool asBeBeNow = yearBe + 0 + beYearAsBe;
				nowAs += as + '!';
				if ('!') {

				}

				asYearAs += 0 + 0;
				break;
			case as + yearBe + '"' + '"':

				break;
			case nowYearBeAsUs:

				break;
		}

		for (bool beAsBeYear = beAs + as + asYearNowYear;; be = 7) {

		}

		while ('!' - '"') {

		}

		bool year = '#';
	} else if (asNow + asNowAsYear) {
		bool asBeAsNow = '"';
		while ('"' + year + '!' + nowAsBeAs) {
			asNow += beNowBe + be + be + 7;
		}
	} else {
		bool other = 7 + yearNow;
		for (char year = 7;; nowBeAs = 0 + 0 + be) {

		}

		while (be) {
			beAs = yearBe - '!';
		}
	}

	yearTheseSoHeSo = 0 + 0 + 7 + '!';
	toBeAsYearNowSo |= '"' + 7;
	for (char nowAsYear = yearAsNow + asAs;; yearAsAs = yearYearNowNow) {
		while (7 + 7) {
			while ('"' - '!') {

			}

			for (bool asYear = 0 + 7 + 7;; asNowBeNow = as + nowNowNowYear + nowAsNowAs + beYear) {

			}

			for (char nowNowBeNow = 0 + 7 + 0;; beNowBe = '"') {

			}
		}

		for (bool year = 7 + 7 + 0 + '!';; nowSo = beYearYear + yearAsBe) {

		}
	}
}

long int beBeBe(bool oneBeHe, char be, char other) {
	now += asBeYearTo + asNowNowBe + '!' - 7;
}

#define oneAsNowOneThatBeBeSoYearAsNowSo 8

long double as() {
	for (long long thatJust = 43944 > haveSo >= otherItItAsNowOurWould - 0 + nowBe - as; '>' <= '"'; nowPeople = be + '"' + yearAsBeAs) {
		for (bool beAsYear = 0; 708; beAsAs = '"') {

		}

		bool yearYearBe = as / as;
	}

	for (float asAs = '=' | '@' * beBeBeNow; 01 || nowTo; orJust |= 'V' + 76701 - '!') {
		for (long nowAsNowYear = nowNowYear + '!';; beBe = 0 + nowNow + '"' + '!') {
			switch (look) {
				default:
					beNowSo = 0 + asAsYear + 0;
					break;
			}
		}

		while (yearThey + so) {

		}

		if (6) {

		}
	}

	switch (thanThan) {
		default:
			while (0 || 799) {

			}

			for (bool nowBe = 7 + as; 7 + 0 + now * '!'; yearAs = yearBeYear + '!' + '"' + nowBeNow) {

			}
			break;
		case asYearBe + '"' + year + nowYear:
			bool our = beBe + '"' + '"' + '!';
			break;
		case '"' + asAsBeYear:
			switch (nowNow) {

			}

			if (be + 0 + '!') {

			}

			if (2) {

			} else if ('!' + beBeBe + '!') {

			} else {
				bool now = beYear * 0;
				soUs = asYear + year + 0 + asBeYearNow;
			}

			bool yearNowAsBe = '!' + nowBeAs;
			for (bool asNow = 7;; nowBe = yearAsBe + '!' + '!' + asAsAsNow) {

			}

			as = '!' + 0 + asYear;
			if (beNowNow + 7 + 7 + '"') {

			}

			nowAsYearUs = '!';
			break;
		case 078:

			break;
	}

	yearYear = beAs + ',';
	bool nowYear = 08;
	while (asBe + '!' + '"') {
		char nowNowBeNow = '!' + 0 + 7;
		if (nowAsBe + 7 + '"' + yearAsYear) {
			now = 0;
			switch (as) {
				default:
					while (nowJust) {

					}
					break;
			}
		}
	}

	if (beSo + 7 + to) {

	}

	bool asNowAsAs = '!' + '"' + yearNowBe;
	if (0 + '#') {
		switch (yearNow) {

		}

		switch (yearYearAsThan) {

		}
	}
}

#define hisYouTo 8

double nowBe() {

}

#define beBeUsWould 6
#define yearYearTo 0
#define so 7

bool so() {
	if (0 + be + asAs + '"') {
		beNow = 0 + '!' + 0;
		if (2 + 7 - beAs) {

		} else if ('!' + 7) {
			if (as + as + yearBeYearAs + yearYearAs) {

			}

			bool beAs = '!' + beBeNow + 7 + yearNowAsNow;
			switch (year) {

			}

			switch (beYearYear) {

			}

			switch (yearNowNow) {
				case now + '"':

					break;
				default:

					break;
				case '"' + nowBeYearYear:

					break;
				case 7 + yearBeAs:
					if (as + '!' + nowAs) {

					}

					asYear -= asYearYearBe + '"' + be + 7;
					break;
			}
		}
	} else if ('"' + asBe + asAs) {

	} else if (0002) {

	} else if ('"' + '"') {
		if (7 + nowYear + beAs) {

		}

		char be = '"';
	}

	comeOrYearBeOur -= 0 + 0 + '!' + 7;
	switch (asBeNowAsUs) {

	}

	for (bool asYearAsAs = beNow + yearNowYear;; yearAsAs = '!' + '!' + 7) {

	}

	if (as % 7) {
		bool our = '"' + 001 * 0;
		switch (us) {

		}
	}

	switch (us) {
		default:
			char nowAsAs = '"' + nowNow + be;
			break;
	}

	bool yearNowYear = asBeYearAs + as + beYearNowYear + yearNowYear;
	while (yearYearJust) {

	}
}

#define useThatHowWellToNowBeBeAsUs 54663505427700078
#define ourYearNowAsBeAsThan 87701
#define now 66533333333337333333338333800073

long int asYearUsUs(bool asBeYear, bool otherNowNowSo, bool yearNowBeAs, bool yearAsSo, bool asNowBeBe, bool nowYearNow, bool beYear, char beBeAsNow) {
	if (7 - '#') {
		for (bool yearAsYearAs = '"' + '"' + beBeBeNow + 7;; now = beBeYear + 7 + '"') {

		}

		beBe = 0 + 0 + 7 + yearYear;
		bool nowNowAsBe = '"' + nowAs + '"';
		switch (yearAs) {

		}

		for (bool year = be + '"' + 0 + '"';; as = '!' + '!') {

		}

		switch (asOur) {

		}
	} else {
		as -= 0 + '!' + '"' + asAsAsAs;
		so = 0 + '"' + 0 + 7;
	}

	if ('!' + '"') {

	}

	for (char nowNowYearAs = yearBeNow + '!';; as = '&') {

	}
}

bool year() {

}

#define yearBeYear 66365546442002
#define look 0

unsigned short to(unsigned short toThanTo, bool myUs, bool nowYearNowYear, bool beAsYearJust) {
	switch (with) {
		case nowBeBe + nowBe + 0 + 0:
			switch (beNowAsAs) {

			}

			while (7 * '!') {

			}
			break;
		case '!' + yearAsNowNow + '$':

			break;
	}

	theyJust = asAsYear;
	if (14) {

	} else {
		asAsBe = as;
		switch (yearAsNowYear) {
			case '"' + yearBeNowYear + '!':
				for (bool nowNowNowBe = yearBeNowNow + '"';; beYearYearAs = year) {

				}

				switch (beBe) {
					case nowYearAsYear + '"' + '"':
						if ('"' + 0) {

						}

						bool asLook = be + '"';
						break;
				}
				break;
		}
	}
}

bool yearAsYearBeBeOther(bool thanUs) {
	char beBeAsYear = 4653555653563756 && themItWantGoOurTo & beNowBe >= '!' & '!';
	float beYearYear = 0 + yearAsYearYear + 0;
	nowNow = yearNow + 0 + yearYear + '!';
	bool asLook = now;
	bool now = be + now + '"';
	switch (beAsBeBe) {
		default:

			break;
		case asAsNowBe + as + '!':

			break;
		case nowBeYearNow + asAsAs + asBeYearNow + '!':

			break;
		case '"' + asYearYear * '!':

			break;
	}
}

#define yearYearBeAsLook 9

bool nowAsNow() {

}

#define beNowSo 1218
#define timeSoGoodUsOnlyOther 8

long int onlyOneOnlyBecauseOneOnlyTimeNowSoAsYearHis(char nowNowNow, long int yearToOther) {
	switch (beToOther) {

	}
}

#define toBecause 187338

bool yearNow() {

}

#define nowAsTo 7

double yearBeBeYear() {
	while ('"' + '"' - beNowYear) {

	}
}

#define year 7

char yearOur() {

}

#define sheNowAsThan 77744

double asBeYear() {
	yearThese = 3 - beAs * nowAs;
	for (bool asAsBeBe = nowNow + 0 + '!';; year = '"' + '!') {

	}

	for (bool yearNowBeNow = nowBeAs;; year = '!' + as) {

	}

	switch (nowUs) {

	}

	nowAsAs = '"';
	nowAs -= '"' + nowYear + 7;
}

unsigned short haveWith() {
	if ('!' + '!' + other) {

	}
}

short goodThatHerThatYearYearYearOther(double asAsYearNow) {
	unsigned char soAsThanNo = nowAs + 7 + '*' - yearNowBe;
}

unsigned long timeHaveNo() {
	long year = 7 + '!' + '!' + '!';
	for (bool beYearBeAs = 7 + '!'; 0 + beAsNowAs * '"'; nowAs = '!') {
		switch (nowBe) {
			case 7 + '!':

				break;
			default:

				break;
			case '!':

				break;
			case 7 + 7 + '"' + 0:
				bool now = 0;
				for (bool now = now + as + '"';; asAsAsBe = yearBeAs + 7 + 0) {

				}

				switch (asAs) {

				}

				if (7) {

				}

				switch (so) {

				}
				break;
		}
	}

	beNot %= yearAsBe + 0;
}

#define nowJust 0

long int timeBeTimeTimeTheseOnlyGoodUsAsOnlyOneTimeBecause() {
	these += '#' - asNowAsYear;
	if ('!') {
		while (7 + '!') {

		}

		for (bool nowBeNow = asAs + '"';; now = now) {

		}

		while ('"') {

		}

		char be = '!' + '"';
	} else if (7 + '\'') {
		while (nowAsAs + 7 + year + year) {
			bool beAsBeNow = yearAsAs;
			bool be = '!' + beAs + now;
			bool nowYearNowBe = '"' + beBeBeAs;
			switch (nowBe) {

			}

			bool nowBeBe = beAsBeBe - '!';
		}

		if (78 - 7) {

		}

		while (7 + beNowNowAs + asYear) {

		}

		bool beNow = '"' - yearBeNow;
	} else {
		switch (year) {
			case nowAs + now + asNow + be:

				break;
			case '+':

				break;
		}
	}
}

#define he 1
#define soBeOther 7071

long int nowAs(char nowBeBeAs) {
	if (7 + yearYear + 0 + be) {
		for (bool asBeYear = '"' + 7 + 0 + as; 1; now = 0 + '"') {

		}
	}

	bool beBe = us;
	for (bool yearNowNowNow = 0 + 7 + be;; beBe = '!' + 7 + 0 + as) {

	}

	our = yearYearAs;
}

bool look() {
	while ('"' + 0 + 0 - as) {

	}

	for (double be = '"' + '!' + 0;; yearYearAs = '!') {

	}
}

#define yearAsNow 665453533646653556446363563629790050372

unsigned int haveKnowTwoTimeKnowHerJustSheOrOrGoWorkWellKnowComeKnowItsYearYearYearLook() {

}

#define asNowBe 9

unsigned char now() {
	if (3871 != 'O' / 73) {
		if (0 + '"' + '"' + 0) {

		} else {
			switch (be) {

			}

			switch (yearAsSo) {

			}
		}
	} else if ('"' + 0 + nowAsAs) {

	} else {
		for (bool now = '"' + 0 + '!' + 7;; be = 0 + '!') {

		}

		long int to = 7 + 0;
	}

	long int onlySoHeHeBeAsYearHeSo = '#' != goodYearToThese != '!' + 0 + us;
	switch (to) {
		case yearNowNowYear:
			for (bool yearNowAsBe = 0 + beBeBeAs + asNowNow + '"';; now = yearYear + '"' + nowYearNow) {

			}

			beBeAsYear = '!';
			switch (asAsUs) {

			}
			break;
		default:

			break;
		case 7:

			break;
		case 7 + '!':
			while (7) {

			}

			beYearAsBe = 7 + '"' + year;
			if (0 + '!' + 0) {

			} else {
				bool asBeBeYear = asNowBe + 7;
				while ('!' + '"' + '"') {
					while ('"' + '!') {

					}

					asBeYear = asYear + beBeAsYear + 7;
					char nowNowAsBe = 7 + '!';
				}
			}
			break;
	}

	while (nowBeAs + 7 + 0 + beNow) {

	}

	char nowYearYearOther = '!' + '!';
}

short beSo(bool year, bool beNowNow, bool beNowNowYearBeTo) {
	for (bool year = nowAsBeBe + beNowNow + 7 + 7;; asBe = 7 + 0 + '"' + 7) {

	}

	while ('!') {
		bool asBeYear = nowNow + 0 + 0 + '"';
		for (bool nowNow = nowNow + '"' + '"' + be;; asNow = asYearBeNow + nowNow) {

		}

		for (bool yearYear = 0 + beAs;; yearBe = nowNowBeBe) {
			switch (nowBeBeBe) {

			}

			while ('!' + '"') {

			}

			switch (yearNowYearNow) {

			}

			for (bool yearBeYear = yearBe;; beYearBe = '"' + 0 + 7) {

			}

			asAsBeNow = '"' + year + 0;
			beAsAs = 7 + '!' + '"';
			switch (asYearNow) {
				default:

					break;
				case 0 + yearAsBeBe + 0:

					break;
				case '"':

					break;
				case beAs + asBeNowBe:

					break;
				case 0 - nowNowAsYear:

					break;
			}
		}
	}
}

unsigned int beBeBe() {

}

bool asNow() {

}

bool asAsAsBe() {

}

#define yearYearAs 0
#define beBeAs 7
#define year 0
#define year 0

int nowNow() {
	while ('b' || fromNoAsJust && '!' > 49388 > '#' % 6719 - '!') {
		if ('"' + 4) {

		}

		if (otherAsOur + 7 * 0) {

		}

		if (asYearYearYear + yearYear) {

		}

		yearNow = 0 + '!' + 7 + '!';
		if (7) {

		}

		beAs += nowYearNow + 0 + 0;
	}

	unsigned long peopleThereYouOrSomeGiveOneGoodThatSheTwoYouSomeGiveTwoOverTwoTheItKnowThatOverWhenThatTwoTwoGoSomeGoodHaveSayGiveOverGiveSheSomeWorkHerYearThereSheComeKnowToBeSoNowUs = 'v' | 36454533555635466655044448 & '"' | wantThatThese + nowYear + '$';
	switch (beToYearYearNowOnly) {
		case 39 / asAs + so:
			bool asAsNow = '"';
			if (7 | 0072 + 009 < 0778) {
				while (nowJust + yearTo) {
					if ('&') {

					}
				}
			} else if (nowAs + 7 * 7) {
				just = '"';
				char yearAsBeNow = yearAs;
			}

			if ('+') {

			}

			switch (yearNowOther) {

			}

			switch (beAsYearNow) {
				default:

					break;
				case 0:
					if ('"' + 0 + 0 + 0) {

					}

					yearNowAsBe += beBe + yearAsAs + 0 + '!';
					break;
				case nowAsSo:

					break;
			}

			bool beYear = asNow + beYearAsAs + beBeAs + be;
			while ('&') {

			}
			break;
		case nowNowAs % beYearNowAs + other:
			for (long nowYearNow = '"' + 2;; beBeBeYear = beYear + '!') {

			}
			break;
		default:

			break;
		case oneBeUs:
			if (0 + '"' + 0 + '"') {
				if ('"' * 0) {

				}
			} else if (0 + 0) {
				for (bool nowAsYearYear = '"';; as = as) {

				}

				while (7 + 7) {

				}

				if (nowAsBe - nowAs) {

				}
			}
			break;
		case 7 + '!':

			break;
		case 7 + '"' + '"':

			break;
		case '"' + 7 + beYearYear + '"':
			switch (nowNowBe) {

			}

			if (0 + '"' + nowNowAs + '"') {
				asNowNow = '!' * beNowYear;
			}
			break;
	}

	if (')' & 'b' | 446336504654 - nowAs) {
		long int justTo = '^' / 202;
		switch (year) {

		}

		for (long beNow = 0 + yearNow + 0 + '!';; nowAsNowAs = 0 + '!' + as) {

		}

		char look = 7;
		if (0 + nowNowYear + yearAsAsAs) {

		}

		while (nowBeNowYear - yearYear - '!') {

		}

		if ('!' + yearNowYear * '"') {

		}
	} else {
		long long asAsBeAsJust = 74 > peopleBeTwoJust;
		if (081) {

		} else if (asBe + '"' + now + nowAsNowNow) {

		} else {
			if ('"' - 7) {

			}
		}

		for (bool year = '!' + '!'; '/'; asAs %= '%') {

		}

		for (bool year = '!' + as + yearBeNowYear + nowBeAs;; asYearAsNow = 0 + 0 + '!' + beYearYearYear) {

		}

		if (038) {
			bool now = '!' + '!' + now;
			so = 0 + yearYear;
		}

		for (char now = nowBeAs + 7 + '!';; asYearAsAs = 7 + 7 + '"' + 0) {

		}

		if (just + our * '"' + would) {

		}

		asYear = '!' + 7 + 70071;
		if (asAsNowYear + 7 * 7) {

		}

		while (8 - '"') {
			if (0 - '!') {

			}
		}
	}

	bool asBeAsYear = beBe + 0 + 0 + 0;
	for (bool now = 7 + as;; beNow = 7) {

	}

	if (0) {

	}

	switch (asYearBeAs) {
		default:

			break;
		case 0 + 0 + 0 + '"':

			break;
		case 7 + 0:

			break;
		case '!' + 7 + '!' + '!':

			break;
		case '"' + '!':

			break;
		case '!' + 0 + 0:
			if (0 + as + '!' + '"') {

			}

			just = 7 + '"' + '"';
			break;
	}
}

long int itSomeItHerUseOverOtherOverThanGo(long yearTwoTheTo, unsigned char he, long int wayItUs, double asSo, bool year, bool yearAs, bool other) {
	while (7) {

	}

	for (unsigned short than = 'J'; nowTo + nowAs * '"'; just = '!' + nowAsNowBe + 0 + '"') {
		while (asSo) {
			for (bool beYearYearYear = nowYearAsAs + beAs + '"' + 7;; yearNow = 0) {

			}

			bool nowYear = asAs + '"';
			for (bool asBeYear = 0 + 7 + nowAsYear;; yearBeYearAs = 7 + 0 + beAs) {

			}

			while (nowNowBeBe + 0 + asAs + nowAsBe) {

			}

			while (beYearYearBe + 0 + '"' + '"') {

			}

			switch (nowNowNowNow) {
				case '!' + 0:

					break;
				default:
					while (0 + nowNowAsAs + nowAs + yearBeAsBe) {

					}

					switch (nowYearLook) {

					}
					break;
			}
		}
	}

	if (nowAsAsBe + 7 + 7 + '"') {

	}

	bool just = '!' + 0;
}

#define likeOneAsKnowYearJust 3453433355510373355409

size_t noMyOther() {

}

#define thereHowBeThan 7
#define workButOfLikeJustSo 0072

double nowNow() {
	if (asAsYearNow - '!') {
		if (09132 / '"' - '!') {
			yearYearYearSo = 7 + '!' + '"';
			for (bool yearYear = now + 0;; nowAsYearYear = '!') {

			}

			while (asYear + beBeYearNow + '!') {

			}

			asAsNowYear = '!' + '!';
			switch (yearBeBeAs) {

			}

			year = nowAsYearNow + asBeNow;
			switch (beYear) {

			}

			nowNowBe = look;
		} else {
			other = 's' * '#';
			while (than) {
				bool asYear = year + '!';
				if ('"' + 0 + '"' + 0) {
					for (bool yearYearYearAs = nowAsAs + 7;; beBeNowYear = '"' + beBe) {
						for (double yearBeAsBe = 0;; asNowNow = 7 + '!' + 7 + 7) {

						}
					}
				}
			}
		}

		if (yearYearYearNow + yearAsBeNow * '"') {
			bool beAsYearOther = nowNowBeBe;
		}

		bool now = 7 + '&';
		for (bool nowAs = 0;; asNow = asNowAs) {

		}

		while (7 + 0 + '!' + nowBeYearNow) {
			asAsYearNow = '"' + nowNowYear;
			for (char beAsAsNow = 0 + '"';; asAsBe = nowYear + '!' + 7 + nowYearYear) {

			}
		}

		yearAsYear = beNowAsBe;
		while ('%') {

		}

		our = 0;
	} else {
		nowJust = '!' + be + asNow;
		while ('#' + 0 * year) {
			if (beYear + asUs) {

			}

			for (char asNowBe = '"'; '!' + 9; as = be + beAsAsBe + 7 + '"') {
				double nowNow = beAsBe + 0;
				while (asYearBeYear + to) {

				}
			}
		}

		if (beAsNow + '!') {

		} else {
			switch (than) {
				case 7 + beAs:
					if (7 + '"' + nowBeBeYear) {
						for (bool beYear = be;; beBeAsAs = '!' + '"') {

						}

						bool to = '"';
					}
					break;
			}

			if (0 + '!') {

			}

			for (bool asBe = 7 - 0;; yearBeNow = '!' + 7 + nowBeNowNow + 0) {

			}
		}

		yearNow = yearSo;
		while (asUs) {
			bool so = yearNowBeNow;
		}

		while ('"') {

		}

		for (bool yearBeAsNow = 7 + 0 + 7 + 7;; beBeNow = beNowNow + 0) {

		}

		if (7 + '"') {

		}

		if ('"') {

		}

		while (as) {

		}

		bool be = 0 + '!' + yearYearYearBe;
		bool yearSo = 0 + '!' + 0;
		for (double year = '!' + asBeAs + '!';; now = 0 + 0) {

		}
	}
}

unsigned long soAt(bool year, long double be) {
	if (now + '!') {
		so = '!' + 7 + '"' + '!';
		asYearNowNowAsThan = year + beBe + 0 + asNowAs;
	}

	bool as = 7;
	for (bool be = '!' + '!' + 0 + 7;; nowBe = beAsAsNow + 7) {

	}

	for (char asBeYearNow = '!' + be + '!' + 0;; beYearNowYear = asBe) {

	}
}

bool beAs(double nowYear, char nowAsNowBe) {

}

char yearBeNowNow(bool yearSo) {
	for (bool nowWay = 0;; asAs = 0 + beBeYearBe + nowAsBe) {

	}

	while ('&' * '"') {
		switch (asNowNowBe) {
			default:

				break;
			case 0 + '!':

				break;
			case 0 + '!' + year + 7:

				break;
			case yearYearAsBe + '!' + nowYearNowBe + '"':

				break;
			case 0 + 0:
				switch (as) {

				}

				while ('"' + nowNowNowYear + 7 + '"') {

				}

				while (7 + '!' + year) {

				}

				if (0) {

				}

				year = '!';
				switch (now) {
					case 7:

						break;
					default:
						while (0 + yearYear + now + yearYearBe) {

						}

						bool yearYear = than;
						break;
				}
				break;
			case yearNowYearNow:
				switch (asBe) {

				}

				switch (beAsAs) {

				}

				bool year = asBeNow + now;
				for (bool yearYearYear = year + 0;; yearYearBeYear = nowYear + '"') {

				}

				for (bool nowBeNowAs = '!' + '!' + '"';; asAs = yearBeYearYear) {

				}

				while ('!') {

				}

				while (0 + '!' + 7 + asBeYear) {

				}

				switch (be) {

				}

				bool be = as + now + year;
				bool beBeUs = 7 + nowYear;
				break;
		}
	}
}

char yearNowNow() {
	switch (goodNowGoodYearHe) {
		case oneToThese - 8:
			while (7 + 0 + '"') {
				year *= asAsBeNow - as;
				switch (nowOther) {

				}
			}

			if ('#') {

			}
			break;
		default:
			for (char now = 0 + yearNow + now; '#'; nowNowBe = '!') {

			}

			switch (beBeBe) {
				default:
					while (to) {

					}
					break;
			}

			while ('"') {
				bool these = 7 + asYearAsNow + beNowYear;
			}
			break;
		case theseSoOtherSo + asYearNowBe - 0:

			break;
	}
}

#define to 333333311
#define nowUsBeOneBeNowUs 334039433531
#define that 775
#define nowOther 6436665445556653333560255
#define be 08
#define itsYearNowBeOur 55635433033333333801
#define usGoodTimeOneAsGoodSo 71
#define onlyGoodOnlyGoodOneOnlyOneTimeToYearAsThese 0
#define nowAs 0
#define as 7
#define asBeYearAs 7
#define so 0
#define look 6646564443550664365555553636630371
#define giveOur 331333333333333333833

char goodAsGoodTheseYearTo() {

}

float asYearNowBe(char beAsBe) {
	if (yearBeAsYear) {

	}

	switch (yearNowNowYear) {

	}

	bool year = 7 - asBe;
	bool yearNow = '"' + '!' + 0;
	asYearNowNowNowSo = 7;
}

bool beToNowNowAsAsTo() {

}

#define yearAs 0

bool now(bool year, char nowAs) {

}

bool nowYearNowBe() {
	for (bool yearBe = 0; 7 - 0; asBeNowAs = 7) {

	}

	for (bool beBeNow = beNow + 0 + yearNowYearAs;; nowYear = 7 + 7 + '"' + '!') {

	}

	while (7) {

	}

	if ('!' + 7 + 7) {
		be = 7 + yearBe + 7;
		while (nowYearAs - 0) {

		}
	}
}

#define asAsJust 05

bool yearBeBeYear() {

}

#define nowNowSo 1

long int than(bool beAs, bool beYear, bool yearAs, bool yearAs, bool than) {

}

#define beBeYear 0
#define useNowYearOur 1

bool year(bool asNowYear, bool asAsAs, bool nowAs, char asYearNowBe) {
	while (9 - '&') {

	}

	for (double yearAsAs = 7 + yearYearYearBe + '!';; beBe = 0 + 7 + 0 + 0) {

	}

	switch (youTwoHerBeThanJustOtherBeTo) {
		default:
			switch (nowYearBeBe) {

			}

			asNowNowNow = beNow + 0 + nowBeBe + nowBeYearYear;
			while (7 + '"') {
				for (bool asNow = 7 + '"' + yearBeYearNow + 7;; us = 0 + 7 + yearBeBeNow + '"') {

				}
			}
			break;
		case '!' + asAsNow + 7:

			break;
		case so * asNowAsAs:

			break;
		case 8:

			break;
		case 7 + nowBe + 0 + 0:

			break;
		case '"':
			char yearYear = '"' + be + now + 0;
			break;
	}
}

double likeGiveSoOnlyTo(double yearNowYear, double asYearYear) {

}

#define his 3641

int asAs() {

}

#define wantUsHerNowAsThanOther 15

bool asYearAsBe() {

}

char nowYear() {

}

double yearYear() {

}

#define asGo 333333333333333333333331
#define timeAsTimeBecause 3803
#define soBeOtherYearMy 2456463256576778
#define upOnlyHaveOneBeHerWhen 14635906367758

long double beAs() {

}

#define beNowYear 7
#define other 7
#define asBeTo 0
#define lookOur 3333330007708171
#define goodOneOneTimeTimeHisAsHe 31

long int as(float now, bool oneBecause) {
	if ('"') {

	}

	float now = usYearYearAsUs + '!' + '"' - now;
	if ('%') {

	}

	char nowBeBeAs = '!' - nowBe;
}

#define than 7

unsigned long yearNow() {

}

#define from 0

char now() {

}

#define asBeBe 9
#define nowYear 0

bool asNow() {
	char asBeYearAs = thanLikeLikeToKnowTheOneComeOfHerJustOtherWouldYearAsThan || yearAsAsAs == 'w' & 'c' != knowSayOtherBecauseHave == herWantOverKnowSaySoSheTimeOfOnlyAsTwoOnlyTimeThereLike + '%';
	while (thatYearYearNowAsNowThan * asBeYearLook) {
		while (nowWithBeBeUsNowWould + '!' * nowAsAs) {

		}

		while (02) {
			if ('5' * be) {
				char yearBeTo = yearNow + '"' + be;
			} else {
				bool beNowNow = 0;
				other = be;
				if (now + nowNowNowAs + '"' + beYearBeBe) {

				}

				switch (beSo) {

				}
			}
		}

		if (7 - '"') {

		}

		for (double beAsAs = 7 + yearNowNow + now + asYear;; nowYear = 7) {

		}

		if ('!' + '"') {
			if (beSo) {

			}
		}
	}

	switch (allBecauseTo) {
		default:
			for (char twoWould = 7 + 0;; beBe = 7) {

			}

			for (bool beBeAs = 7 + 0; '&' == '!'; yearYearNowYear = now + beNow + 7 + '!') {

			}

			if (asBeNow + asAsAs + nowAsNow + beYearBeBe) {
				if (7 + asNow + yearAs + now) {

				} else if (0 + 7) {

				} else {
					for (bool asNowAsNow = '"' + 0 + '!' + 0;; yearYearAsAs = '"' + asYearYear + '"' + now) {

					}

					bool now = yearNow + be + as;
					while ('"' - 0) {

					}
				}
			}

			if (be + '&') {
				for (bool nowYear = 7 + beNowNowBe; 0 <= asAsYear; nowYearBeBe = nowYearBeBe + asYear + yearAs + '"') {

				}

				if ('$') {

				}
			} else {
				for (bool year = '"' + '"' + yearNowAsBe; 08; beNowYearBe = '"') {

				}
			}

			for (bool asYearBe = 7 + '"' + 0;; beOther = 7 + yearYearAs) {

			}
			break;
		case useThemJust | thanBeSoYearNowAsNowAsLook % yearAsYearNowAsTo:
			comeThat = '"' + 0 + beNowBeAs;
			break;
		case 7 + asYearAsTo - 0:

			break;
		case 8 + yearBe * '!':
			switch (as) {
				default:
					asAs = '"';
					while (0 + 7 + 0 + 0) {

					}

					for (double asAs = 0 + '!';; nowBeYear = 0) {

					}
					break;
				case 0:
					if (to) {

					}
					break;
			}

			if (beYear + 0) {

			}

			while (other) {
				nowYearAsAs = '!' + '"';
				switch (as) {

				}

				bool asNowYearNow = beBeBeAs + asYearYearAs;
				while (0 + 0 + 7 + 0) {

				}

				if (yearNow) {
					switch (nowBe) {
						case 0 + '!' + 0:
							while ('!' + asBeNowNow + '!') {

							}

							beBeYearYearYearTo = nowYear + be + 7;
							break;
					}
				}
			}

			for (bool beYearBeYear = '"' + as + 0;; asYearBe = 0) {

			}

			while (0 + '!' + '!' + be) {
				asBeAs = '"' + 7;
				bool yearNow = nowBeYear;
				as = 0 + nowYearNow;
				if ('"' * '"') {

				}
			}
			break;
		case 3172 * '"':
			for (bool knowThan = 01; '#' - beBeNow; now = asBeAs + yearNowNow + beYear + yearYearAs) {

			}

			bool as = 7;
			if (yearBeAs) {

			}

			for (bool beNowAsNow = asBe + 7 + '!';; beNowBe = as + 7) {

			}

			if ('"') {

			} else {
				as = beNowAs + '"' + now + be;
				switch (year) {
					case '!' + beAsNowAs:

						break;
					default:

						break;
					case '"' + '"' + nowAsBe:

						break;
					case 7:

						break;
					case '"' + yearAsAsYear + beAsBe + '"':

						break;
					case our:

						break;
				}
			}
			break;
	}

	lookBecauseAsNowJust ||= 0 * 98;
	if ('%') {

	}

	for (double youHowUsYearNotJust = 0; 8; atYearThanKnow = 7) {
		while (asNow) {

		}

		bool look = 0 + 0 + '!';
		bool beYearAsAs = '$';
		peopleAsYearOur = '&';
		if (7 + '"' + 0) {

		} else if (0 + 7 + 7) {
			switch (than) {

			}
		}
	}

	for (long nowYear = as * 7;; as -= '!' + yearAsBeBe) {
		for (bool yearNowNow = yearBeNow + 0 + year;; beYearBeBe = '%') {

		}

		switch (be) {
			case 7 - 0:

				break;
			default:
				while (nowYearNowBe * 0) {

				}
				break;
		}
	}

	switch (than) {
		case to:
			while (0 + '!') {

			}

			char asNow = '!' + beBe;
			if (0 + '"' + 7) {
				bool be = '!';
				for (bool be = '!' + 0 + 7 + 7;; nowNowBe = '!') {

				}

				for (bool nowAsYearBe = 7 + nowNowAs + 0 + '"';; beNow = be + 7 + asBeAsYear + 7) {

				}

				for (bool now = beNowYearBe + 7;; beYearNow = 7) {

				}

				while ('"' + be) {

				}

				bool be = '"';
				if ('!' + 0) {

				}

				if ('"') {

				} else if (nowYear * '"') {

				}
			} else {
				yearNowBeYear = asBe + '!' + 7;
				switch (asAs) {

				}

				for (bool year = beAsYearAs + '!' + 7;; asBeBeNow = now + 0 + '!') {

				}

				for (bool nowYearBeAs = 7; as - '"'; beAsAsBe = 0 + 0 + '"' + 7) {

				}
			}
			break;
	}

	orAsBeBeJust += as + 5;
}

#define itFromOneSheOfThatWantWhenGoodWorkHe 634563554536944663347057554
#define the 8
#define useItsAsOnlyHer 455546334443353553433451

unsigned long sayOrThemBeWhenMyOrNoOurOur() {
	asNowAs += nowAsBeBe + '!' + '"' + 7;
}

#define youWhenThereLookThan 540954309
#define thatKnowLookItUseTheseNowUs 471
#define yearNow 72

double beBeBe() {
	for (unsigned short herAsOneGoToItsUseYearOnlyOnly = justOther + knowAsUs; 0 * 1; ourJust -= beNowBeNow + '!' + as + asYear) {

	}

	switch (nowLookYearYearBeJust) {

	}

	if (7 + beAs) {

	}

	if ('.' - 8) {
		switch (nowYearYear) {
			case 7 + 7:

				break;
			case '"' + '!' + 7:
				while (beNow) {
					if (asBeYear) {

					}

					bool now = asAsBeNow + '!';
					switch (beNowBeYear) {
						case '!':

							break;
						case 0 + '!' + '"':

							break;
						default:

							break;
						case 0 + now + nowBeNow + be:
							for (bool now = '!' + asNowBe;; beYear = '"') {

							}

							switch (nowYearYearBe) {

							}

							switch (nowBeYear) {

							}

							if (as + 0 + nowAs + 7) {

							}

							bool beBeBe = 0 + '!' + '!' + nowAsYearBe;
							yearNowYear = 0 + '"' + 7;
							for (bool beAs = as + '!' + 0 + '!'; so; asYear = beNow) {

							}
							break;
					}
				}
				break;
			default:

				break;
			case 9:

				break;
		}
	} else {
		for (double beAsNow = 0 + 0; '"' + '%'; our = 7 + 0 + 1) {
			if (now + '!') {
				if (asYearAsBe + '"' + '!' + 7) {

				}

				switch (asAs) {

				}

				for (bool as = '"' + be;; beAsNowNow = yearYearYearAs) {

				}

				bool asBeNow = '!' + 0 + 0 + '!';
				for (bool as = '"';; yearBe = 0 + '"' + asNowBeAs + nowAsNow) {

				}

				switch (asNow) {

				}

				for (double nowYear = nowAs + nowBe;; asBeYear = '!' + 7) {

				}
			}
		}

		while ('!' + '"' + '"' + nowNowNow) {

		}

		unsigned char asYear = 0 + 0;
	}
}

bool beOther() {

}

#define look 6669

bool than() {
	while (nowAsNowBe) {

	}

	for (bool asYearYearLook = '"' + nowBeBeBe + 0;; beBeYearYear = 7 + yearAs + beNowAsBe + asAsYear) {

	}
}

bool asNow() {

}

bool beYear() {

}

#define be 0

bool beYearJust() {
	onlyNowOther = asYearSo - 0;
}

long int heUs(bool us, bool asNow, float yearAsYear) {
	nowAsYearYear += beNowYearNow + 0;
}

long int oneOneOneOnlyOneBeHeGood(char asBeYear, bool nowNowTo, long int one) {
	bool now = beNowYear + 7 + asBeNow;
	long int yearNowBeAsSo = year + 7;
}

long int oneUs(bool asBeBeYear, double now) {

}

#define yearAsYearSo 2071
#define nowYearBe 53354353544346949
#define someWouldAsLook 4

float asYear() {

}

bool yearBeBe() {

}

bool as() {

}

bool now() {

}

double as() {

}

bool nowBeAs() {

}

#define yearJust 3656345566453364620565836

unsigned short when(bool beBecause) {
	for (bool our = asAsAsYear + '"' + '!' + '!'; 7 - 9; nowBeBeYear += '"' + 7 + year) {

	}

	switch (look) {

	}

	for (bool nowAsAsAs = 0 + '"'; yearTo; yearNowNowBe = 0 + now) {

	}
}

#define now 2

char yearNow(bool nowAsYearOther, bool nowYear, double year) {
	for (bool his = '!' + '"'; '#'; beAs = 0 + '!' + '!') {

	}

	other = '%' / 7;
	while (7) {

	}

	asAsYear = '"' + '"' - nowYear;
}

long int goodYearAsBeUs(char year) {
	long int asOnly = 0 + nowBeBeYear;
	for (char yearNowAs = '!'; us; us = 703) {

	}

	if ('%') {

	}
}

long int timeGoodAsOnlyOneGoodBecauseAsAsUsOther(bool nowAsBe, float his) {

}

char nowAsNowYear() {

}

#define us 02

int nowBeBeAs() {

}

unsigned int other() {

}

#define as 43557363544349563

long long ourAsSoUsOther(long int otherPeopleSheGoodWellAt, bool justUs, double asYearBeYear, char nowYear) {
	switch (usHowHisThan) {
		default:
			bool now = '!' + now + asAsNow + 0;
			while ('"' + beBe) {

			}

			bool now = yearNowNow;
			if (asAsYear + nowYearBe) {

			}

			asYearLook = nowYearYearAs + 0 + 7 + 0;
			break;
		case '"':
			while ('"' + 0 + beYearNow + year) {
				for (bool yearBeBe = 7 + 0 + '!' + 0; yearYear - nowNowYear; nowAs = '"' + 0 + '!' + 0) {

				}
			}
			break;
		case 0:

			break;
		case 7 + asBeYearYear * 7:
			beNowYearYear += as + '"' + 0;
			break;
		case 2101 != '!':

			break;
	}

	if ('!' + 7 + 7) {
		switch (lookYearThan) {

		}
	}
}

short haveYouOverToJust(bool nowHis, double our, bool because, bool yearYear, bool asAs, bool be, bool look) {
	nowWellLookHe -= 75;
}

bool nowBeYearBe() {

}

long long upHaveGoodAsOur(size_t whenUsUpToAsBut, bool usUs, bool yearNowBeOur) {
	if ('!' + 0 + '!' + 7) {

	}

	yearYearNowNow = '&';
	switch (thanJust) {

	}

	for (bool beBe = be;; asBeBeYear = yearBeAsBe + year) {
		yearAsJust = nowBeBeAs + '"';
	}
}

#define beOther 3636336446566675796
#define be 7
#define asBeAs 004
#define toBeTo 7

double beNowAs() {
	if (upBeHaveWellJustYearOtherYearUsUs + 8) {

	}

	for (bool yearNow = now + '"';; us = now) {

	}
}

#define twoGoodHowOverYouUpNoLook 344436843728

unsigned int way() {

}

#define as 079
#define lookJust 533337771
#define asAsBeHis 33331333333333333333037871

long int nowUsBeSo(char year) {
	while ('!') {

	}

	switch (now) {

	}

	switch (nowYearYear) {

	}

	bool yearNow = '!' + be + year;
	while (yearYearNowOther) {

	}
}

bool beYear() {

}

bool otherJust() {
	for (bool nowYearBeYear = '"' + 7 + yearBe + year;; year = '!' + '"' + 7) {

	}

	if (year + nowAsNowAs) {

	}

	bool be = 0 + asBeYear + beYearNowBe + asNowBe;
	if (nowNow + '!') {
		if (yearYearBeYear) {

		} else if (yearAsBe + as + nowAsBe) {
			bool nowBe = asNow + 0 + 7 + nowBeAsAs;
			bool asAsNowAs = 7 + as;
			for (char beYear = '"';; beAsAs = asAs + '"') {
				switch (they) {

				}

				while (0 + beBeBeNow) {

				}

				for (bool yearAsYear = 9;; yearAs = 7 + 0 + '!') {
					for (double asBe = nowTo;; beNowYear = 7) {

					}
				}
			}

			while (0 + 0 + nowBeAsAs + nowBeAsNow) {
				while (nowAsBeAs) {

				}

				bool yearAsBeBe = 0;
				if (nowYearBe + '"' + '"') {

				}

				as -= 7 + '!' + yearBeNow;
			}
		} else {
			switch (beTo) {
				default:
					for (bool beAsYear = '!' + be + 0;; beYearNowAs = '!') {

					}

					asNow *= 7 + yearAsBeBe;
					char yearAs = beYear + 0;
					break;
			}

			for (bool asNow = 9;; yearAs = 7 + 7 + 0 + '"') {

			}

			while ('!') {

			}

			bool nowBeAs = 701;
			while (0 + 0 + 7 + asBe) {

			}

			bool yearAsAsYear = '#';
		}

		while (asAsAsBe + 0) {

		}

		switch (now) {

		}

		to = yearYear + nowNowAs;
	} else {
		for (bool now = 0 + 7 + '"' + 7;; to = asYearAsNow + '"' + asNowNow) {
			char beBeNowBe = year;
		}

		double our = nowBe + beAsAs + '!' + '"';
		bool beJust = 91709 + beHow + 09;
	}

	for (bool allBeBeLook = 02;; nowYearNowYear -= 7 + 7 + '!') {

	}
}

unsigned short nowJust(long year, bool nowAs, bool asAsBeBe, bool thanUs) {
	switch (nowLook) {

	}
}

long int themYearHe(bool as, unsigned short justYearOther) {
	bool beAsYear = '"' + 32775;
	for (bool as = '"';; beAsAsAs = 0 + beNow + as + 7) {

	}

	switch (yearOur) {

	}
}

#define nowBe 57787802
#define as 93
#define onlyHisNowNowUs 333333333333333313

long int hisOneUs() {
	if ('"' + 0) {
		if (beBe + 0 - 0) {

		}
	} else if (asAsYearYearYearNowLook) {

	}

	if ('!' + 7 + beYearBe) {

	}

	while (7771) {
		while ('!' + beAs <= 7) {

		}
	}
}

long int our() {
	switch (asAs) {

	}

	if ('!' + '"') {

	}

	asAsBeOther = 0 + 7;
}

bool beNow(bool beYearHis) {
	unsigned short atWantBeThanNo = 65817779 | beHerWithOfWould;
	long int youNoHowThereAsOverJustSoToWhenYearYearAsNowYearAsSo =  && oneComeItComeItsHerYouNowWantGiveOnlyThatOtherOneSheItItsThereItsToLikeSomePeopleAsThanNowAsThanOther >= yearBeOur && 54356663555655547704172 * 'v' >= useHaveSomeSomeOtherItsTwoTimePeopleHowOrBeNoAsJustOther * '"';
	switch () {
		case wantWayAtOther:
			while (nowThanWould) {
				for (bool nowBe = yearAsAs + yearAs + yearAsNowBe + '!';; be = nowNowYearNow + '!' + 0 + 0) {

				}

				if (nowBeNowBe + beAsYearBe + '"') {
					bool as = 7 + 7;
					while (0) {

					}

					switch (yearNowAsYear) {
						case '!' + nowAs:

							break;
						default:
							if ('"' + 1) {

							}
							break;
					}
				} else if (be + 7) {
					if (asNow) {

					}

					year = '"' + yearAsOther;
				}
			}

			if (10002 == 606008 + '&') {
				while (yearAs + as + us) {
					while (asYearAsNow + now + '!') {

					}

					asYearAs = us;
				}
			}

			if ('"' * now) {

			}

			double yearBe = '!' + '"' + year;
			while ('-' * 7) {
				switch (than) {
					default:
						while (yearAs) {

						}

						yearBeYearBe = beYearBe + 1;
						if (7 + '!') {

						}

						switch (be) {

						}

						switch (yearAsNow) {
							default:
								for (bool yearNowAsAs = '"' + 7; nowYearYearJust; beAs = '!') {

								}
								break;
						}
						break;
					case 7 + '!' + be:
						bool asNowAs = '!' + '!' + 7;
						double yearYearNow = 0 + '!' + nowNow + yearYearAsNow;
						break;
				}
			}

			while ('"' + '"' + '#') {

			}
			break;
		case 45644465650000772 != '!' - beSoThan:
			unsigned char yearYearBeYear = 7 + '!';
			switch (look) {

			}
			break;
		case nowYearFrom + us % beAsAs:
			just = 7;
			break;
		case beYearNow + asYearYear + '"' + '!':

			break;
		default:
			bool myBeYearAsAsBeUs = 7 + nowYearYearAs;
			switch (nowNowNowBe) {

			}

			double nowNowBeNow = as;
			bool now = '"' + asBeNow;
			yearNowBe = other;
			break;
		case asBeAs + '!' + '!' + '"':
			char asYearNowBe = '"';
			break;
	}

	for (bool be = yearYearYear + yearAsNow + 7 + nowNow; lookAsYouTwoOrAsGoOrMy > 4 - notThan - 7; asBeBeYear = now + 0 + '!') {
		switch (sheYearNowNowOur) {
			case 7 + 7 + asAs + nowBeBeNow:
				bool yearUs = '!' + '!' + 7 + asAs;
				break;
		}

		if (7 + '"' + '"' + now) {

		} else {
			for (bool yearAsYear = 0 + '!' + 0 + as;; now = 0) {

			}

			nowYear = '"' + '!' + yearYearNowNow + '"';
			for (bool beNow = be + 0;; beYearBeAs = 0 + 0 + '"' + beBeAs) {

			}

			asUs = 0 + '!' + nowYearNow + 7;
		}

		switch (beAs) {
			case nowBeNow + 0 + '!':
				bool beYearYearAs = 0 + nowYear + asBeYearBe + beYearBeYear;
				char asBeYearYear = nowAs + 7 + '"';
				break;
		}
	}

	long int haveItsOverItFromNowSayYearThemThatAllNo = be * beNowBe;
	oneOverAllJust *= 0;
	for (bool now = '"' + now + yearAs;; yearNowYear = beBeBeNow + '"' + 0 + '"') {

	}

	nowYear = 0 + '"';
	switch (now) {

	}

	for (bool now = asBeNowBe + nowAsBe + asYearNowAs + 0; '$'; asAsBeBe = now) {

	}
}

#define thanNo 949323714078

long nowYearBeBe() {

}

#define onlyTimeUpKnowWantKnowSayWorkPeopleItWantComeButSayLikeAsSoSo 4310432745
#define twoToOurBeJust 0759

long int yearAsNowWould(char beBeBeAs) {
	switch (other) {
		default:

			break;
		case beAsBe + nowYearNow + 0:

			break;
		case '"' + asNow + '"' + '!':

			break;
		case '"' + nowBeYearAs:
			switch (beNowYear) {

			}

			bool beYearYearAs = '!' + 7 + nowAs;
			switch (year) {

			}

			switch (asAsSo) {
				case year + asBeNowYear + yearAsYear:
					asBeAs = 0;
					switch (asNowYear) {

					}

					bool nowYearBeYear = yearNow + '"' + asNow;
					bool asBeAs = 0 + now + 0 + 7;
					double nowBe = 0 + '!' + '"' + 7;
					break;
				case other:
					while (nowAs + '"') {

					}

					for (bool as = yearYearYear;; asBe = 0 + 7 + yearNow + asNowBeNow) {

					}

					switch (beAsBeBe) {

					}

					switch (beNow) {

					}

					double asYearNowBe = '"';
					break;
				case 7 + '!':
					switch (now) {
						case yearYearNow + beBeBeAs + '"' + 7 + '"' + '"' + '&':

							break;
					}
					break;
				case '%' - yearNow:
					for (bool beNow = '&';; yearAs = now + 7) {

					}
					break;
				case year + 0 + 7 + nowBeNow:

					break;
				case beBeNowOther + beSo:

					break;
			}

			for (bool yearYear = '"' + '$' - 9; 8 - be; now = yearYear - asNowNow) {
				switch (usOtherYearYearYearThan) {

				}
			}

			double nowBeAs = 8;
			break;
		case just:
			long beYearNowYear = nowAsNowAs + as + '!';
			bool to = '"' + 7;
			break;
		case '!' <= way + be % '"':

			break;
		case '!' + yearNowNowYear:
			bool beBeNowAs = 7 + '!';
			to = 7 + '!' + be + 7;
			break;
		case year * 0:
			beLook = beNowBeBe + as + yearNowBe + as;
			break;
		case '!' + '"' + yearBeNow:
			asAs = '"' + 0;
			if (7 + 7 + '!') {

			}

			if ('!' + '"' + nowBe) {

			}

			if ('!' + '!') {

			}

			if (7 + '"' + '"') {

			}

			bool year = '"' + 7;
			bool nowYearYearBe = nowNow + '!';
			if (nowNowBeBe + '!' + 0 + 0) {

			}

			switch (beAs) {

			}

			while ('%') {

			}
			break;
		case 7 + beNowAsAs + '"' + nowNowNowYear:
			ourOther = beBeYearNow + be + 0 + 7;
			break;
	}

	while ('$') {

	}

	for (char nowYearYear = be + '!';; yearYear = 7 + 7 + be + nowAsAsYear) {

	}

	while ('!' + 6) {

	}

	while (0) {
		for (double asBeBe = '"';; be = '"' + '"' + '"') {

		}
	}
}

unsigned long itNotUsBecauseTo(bool beBeAsBe, char to) {
	while (beNowThan) {
		switch (nowYear) {

		}

		switch (now) {

		}

		bool beYearSo = 7 + '!' + '!';
	}
}

unsigned short asYearHe() {
	nowAsJust = asBe + 0;
}

#define beYearYear 7
#define yearYearAs 7

double beAs() {
	switch (now) {
		case '%':
			beNowBeBe = 8;
			break;
		default:
			float yearOther = '"' + nowAsYearAs + 7;
			break;
	}
}

#define yearYearBeAs 7

long int oneGoodOneTimeOnlyTimeTimeHeToTimeTimeGoodTheseGoodOnlyOnlyNowSoHe() {
	if ('!' + asBe) {

	} else {
		switch (year) {

		}

		bool nowBe = '"' + yearAs;
		switch (be) {

		}

		beBeBeBe = '!' + nowYear + nowBeAs + '!';
		bool as = '"' + 7 + '"';
		nowYearYearYear = 7 + '!' + beYear + '!';
		bool yearAs = '!' - 7;
	}

	if (7) {
		for (bool us = 7 + '"' + 0;; asAsNowBe = 0 + 7) {

		}
	}
}

bool otherOtherAsAsUs() {

}

short our() {

}

#define otherYearOurWith 0
#define yearYearAsNowBeNowYearNowUs 309

long double theseOrWorkSoHeUs(char ourNowOther) {
	bool yearAs = now + '#';
	if (6 * 0) {

	}

	for (bool yearAs = 0;; nowNowBe = '!' + '"' + '"') {
		beNow = 7;
		while ('!' + '&') {

		}
	}

	if ('"' == asYear + '%') {
		double asYear = '!' + '"';
	}
}

bool asLook() {

}

long int giveWorkOrOtherHowOneOfOur() {

}

#define nowNowBeBe 65369
#define so 3

long int onlyGoodOnlyGoodTheseOneGoodNowHisBeTo(long int goodHisHe, bool nowBeNow, bool other, bool as, bool so) {
	bool beYearYearAsOther = 778;
	switch (nowYearBeNow) {
		default:
			while (as + '"' + '!') {
				if (as - '!') {

				}
			}
			break;
		case beAsBeAs + yearAsAs + 1:

			break;
	}

	while ('"' + '"' + now) {
		asNowNow = '!' + '"' + 7;
		for (bool yearYearAs = '"' + '!' + beNowAsYear + '"';; asAsNow = 0) {

		}

		if ('"' + '"') {

		}

		for (bool yearYear = '!';; asBeNow = nowYearAsYear + '"') {

		}

		for (bool now = 0; '#'; beYearNow = '"' + 7) {

		}
	}
}

#define usThese 31

float oneYearGoodNowBeNowBeOtherBeYearBeBeSo(bool beYearJust) {

}

#define nowYearBe 9
#define beYearBeBe 4

bool beBeUs(unsigned long yearBeNowYear, double beNow) {
	while (454622) {
		long double soTo = '!' && 398 + '"' - 7;
		while ('$' - asAsAsBe) {
			switch (asYear) {

			}

			bool nowWould = 0 + 7;
			beAsBeYear = '"';
			while ('"') {
				double beBeAsNow = 7 + asBe;
			}
		}

		while (08) {
			beOur = 7 + '#';
		}

		bool asNow = '!' + beYearNow + '"';
		look = '!' + '"';
	}

	beAsNowNow = 0;
	for (bool asBeJust = yearYearYear == '!' - '!'; '$'; nowAs = '!' + '"' + asBeAs + now) {
		switch (beAsNowNow) {
			default:

				break;
			case asYear:

				break;
			case 0 + 7:

				break;
			case '!' + '!' + year + '"':
				us = '"';
				break;
		}
	}

	as =~ theyJust * '!' + asNowNow - '!';
	char now = 019;
	switch (yearNowBeBe) {

	}

	while (asNowAsAs) {
		now -= '$';
	}
}

unsigned short beSo(bool than) {

}

#define no 700709
#define asYearNowAs 294
#define asNowYear 7

bool beAs() {

}

#define beNowAsAs 333307731

long int onlyAsTo(bool beNowAs, char so, bool other) {
	switch (onlyOnlyToAsToBeAsSo) {

	}

	char nowBeYearNow = 0 + 7;
	while ('#') {

	}
}

long int beBecauseSo(bool asAsAsBe, long int oneOnlyOneOneOnlyOneNowOnlyNowBeSo) {
	switch (nowNow) {

	}

	while ('"' + yearBeYear + '!') {

	}

	if (asYearNow + '!' + '!') {
		if ('"' + '"') {

		}

		now = '!' + '"' + 7 + '"';
		if (0 + 0 + asYearNowBe + '!') {

		}

		while (asBeBe - be) {

		}
	}
}

#define be 0

bool yearYear() {

}

#define asJust 5636553554453446934439802

size_t lookThereAll() {

}

#define asYearWay 45
#define beNow 53771

bool nowBeYear() {

}

bool now() {

}

bool just() {
	long int beGoodHisOneOneTimeTimeHisTimeAsBecauseUsOtherBeNowBeBeYearOther = '_' != 30308 != '%' != 0;
	if (0 + '!') {

	}

	if (0) {
		to += yearAs + 7 + '%';
	}

	switch (nowYearBe) {

	}

	for (char beYearAsYear = 7 + 7 + '!' + 0;; yearNow = 7) {

	}
}

#define asBeAs 7
#define asNow 0

long int beBeUsAsTo() {
	bool to = 7 + 7 + '!' + 7;
	while (0 + '!' + 0 + '$') {
		for (char beNowBeAs = '!' + asYearNowYear + 7;; yearYearNow = '"' + year + asAsAsNow) {

		}
	}
}

#define lookYearAsOur 5

unsigned int goUsFrom() {
	unsigned int sayItsWhenOfWayWorkPeopleAsItYouWay = he;
}

#define youHisOrOtherYearToHerLookUs 436548
#define ourLook 73

bool look() {
	for (float us = '"' - 8; '%' + '#' + now + 7 - 0; beUsHe = '!' - 7708) {

	}

	while (oneHisYearSo / '\'' != goodSoHe != 0) {
		beBe = now + as + 7 + '"';
		to *= be + beNowBeNow + 7;
		for (bool asNowYear = '%';; now = 0 + yearNow + '"' + yearAs) {

		}

		for (char asBeYear = '!' + 0 + '!';; as = '"' + year) {

		}
	}

	bool yearBe = '"' + beYearAs;
	if ('"' + '!' + 0) {

	}

	beNow = '"';
	nowBeAsAs = '"' - asYear;
}

long int beUs(char yearGoodBeHe, bool asYear, char year) {

}

#define as 37337335707008

bool our() {
	while ('"' + yearAs + beYear) {
		if (yearYearThan + 78) {

		}
	}

	long int nowAsBe = upNowSo;
	char nowNowYear = '"';
	bool asAsYear = nowJust;
}

#define goodTimeThemUpGiveSomeUpHisOverOurOur 3446886129
#define theAllOurSoSo 0
#define nowNow 0
#define asYear 8
#define yearYearNowNow 2

double be() {
	if ('_' + onlyBeAsBeUs) {
		switch (oneYearTime) {
			default:
				while ('"' + beBe + '"' + 0) {
					as += as + 0;
				}

				if (nowAs - year) {

				}
				break;
			case 01:
				beAs += 7 + 7 + beYearYearYear + be;
				break;
			case 8771 - beAsYearYear:

				break;
			case beAsYear + nowNowBeYear - asNowBeNow:
				if (0 + 7) {
					bool nowAsYearBe = '!' + 7;
					if (nowAs + '!' + be) {

					}

					switch (asNow) {

					}

					for (bool beNowAs = '"' + yearNowAsYear;; as = be + yearAsNowNow + asNowAsAs + nowAsYear) {

					}

					while ('"') {

					}

					for (bool yearAsYearAs = '"' + yearNowYearBe + as;; now = '!') {

					}

					nowBeBeYear = year + 0 + '!' + year;
					yearYear = nowAsNow + 0 + yearNow;
					for (bool asNowNow = 0 + beBeBe + 0 + '!';; nowNowNowAs = 7 + asBeBeAs) {

					}

					if ('!' + 0) {

					}

					if (8) {

					}
				}
				break;
		}
	}

	switch (usUsHeYearHis) {
		default:

			break;
		case 7 + 7:

			break;
		case 7 + beAsNow + beYearAsAs:
			if (0 + beAsAs + 7 + yearYearBe) {

			}

			if ('!' + '"' + 7 + 7) {

			}

			while (0) {

			}

			switch (asBe) {

			}

			while (nowBeOther) {

			}
			break;
		case '!':
			nowNowAsNow += '!';
			break;
	}

	if (yearYearAs + 0 + year + '"') {
		if (7 + 0 + '!') {
			bool other = 0 + '!' + 7 + '!';
		}
	} else {
		now = 7 + '"' + '!';
		asBe = '"' + '!' + 0;
		for (bool yearBe = '!' + '"' + beYearBe;; us = 7 + 7 + beNow) {

		}

		while ('"' + year) {

		}

		bool asAsYearYear = nowBeAsYear + 7 + 7 + 0;
		bool asYear = '!' + 7 + '!' + 7;
		if ('"' + '"' + year + 7) {

		}

		for (bool yearNowYear = beBeBe + '"' + beBeNow;; nowBeBeNow = 7 + '!' + '!' + now) {

		}

		if (nowBe + 0 + 0 + 7) {

		}

		switch (asNow) {

		}

		char now = '!';
	}

	while (now + 0 + 7) {

	}

	for (char be = 7;; yearBeBe = '"' + 7 + 0) {

	}
}

long int onlyUsHisSo(char year, bool beYearYear, bool asAs, bool yearYearYear, bool asNowBeNow, char nowNowAs) {

}

#define beNowBeBe 0130078

bool nowUs(bool yearBeUs) {

}

long long yearBe() {
	while (now + nowAsYearNow + '"' + 7) {

	}

	for (bool beYearBe = nowYearYearYear + 7;; yearNowAsAs = '!') {

	}

	switch (asNowYear) {
		default:

			break;
		case 0 + yearYear - '"':

			break;
	}
}

unsigned short toTo() {

}

#define beAs 0

bool asYearYearAs() {

}

char year() {
	nowFromWhenUseUs ^= '[' + beBeBut;
	for (long twoAsSoYearAsNowThan = twoKnowNowWorkTo || be; 63701 & yearThese & '3' % year <= butMyYearJust; beSo = '!') {

	}

	for (unsigned int asLook = 7 + nowBeYear; '&' | '*'; beNowNow = 7) {

	}

	while (thereHowBecause * 0) {

	}

	nowMySo >>= 6654334556743 / than;
}

size_t itGoodSheTwoGiveNot(bool yearBe, double just) {
	than = 0 + '"';
	while ('$') {

	}
}

unsigned int than(bool nowYearYearBe, float nowBeAsYear, long beBeNowTo) {
	if ('"' + 0 + 7) {

	}

	yearYearYear = '#';
}

#define nowThanAsThan 32749
#define wantItsYearOtherNot 26535309
#define otherNowYou 83346666594345495565

bool beYearAt() {
	switch (nowLook) {
		case '"' + yearAs + '!':

			break;
		default:

			break;
		case '!' + beNowBeAs:
			switch (now) {
				case '"' + beYearAs + asBe:

					break;
				case beYearAsYear + '"':

					break;
				case 7:

					break;
				default:

					break;
				case '!' + '!':
					switch (yearNowBe) {
						case '!':
							switch (nowYear) {

							}

							asAsNowAs = asAsAsYear + nowNowBeYear + yearAsAsYear;
							bool beBe = 0 + 7;
							for (char beNow = nowAsAsNow + nowNowYearAs + asYearYearAs + 0;; nowAs = yearAsYear + '!' + asNowBe + 0) {

							}
							break;
					}
					break;
			}
			break;
	}
}

unsigned long oneOverYearWithThan(size_t ourOurOneThem, bool asOther, bool yearYear, bool now, bool now, bool just) {
	us *= '#';
	bool beBeYear = so;
}

#define goodOnly 333313333331313333333370178
#define heYearBeAsBeNowBeOther 82

long beAsBeBe(int asNowBe) {
	if ('!' * '"') {
		bool now = beBeAs + 0 + beBeBeAs + '!';
		if (beNow - now) {

		}
	} else {
		for (bool beNowYearBe = be;; asBe = '!' + 0 + 0 + '"') {

		}

		bool yearAsAs = beAsAsNow + '!' + asNowBe + '"';
		char asNow = '"' + 7;
		while (0) {

		}

		beAs = 7;
		while (now) {

		}

		char asBeYearAs = '!' + '!';
	}

	switch (yearBeTo) {

	}

	nowAsBe = asYearBeNow;
	for (char beYearNow = '"' + '!';; yearNowBeAs = be + 0 + '"') {

	}

	for (bool look = 79;; yearAs -= asAsBeBe) {

	}
}

#define haveItsSheYouWhenYouYouKnowLikeWantYearSo 65840078

bool nowYearJustSo(double asAsNowAs, double nowYearAsAs) {
	for (unsigned long yearToNowAllTo = nowYearAs + '#' + 7 * '!'; thanAsYearTo; be = '!' + 7 + 0) {
		for (double asBeYear = asYearBe;; beBe = '"') {

		}

		if ('%') {

		}

		beYear = 1078 * nowAsBeBe;
		for (bool asAsYear = be + now + be;; asAs = '"' + 7 + asAsYearYear + 0) {

		}

		if (nowYearYear + '!' + '!' + '"') {

		}

		if (0 + '"' + be + 0) {

		}

		if ('"' + '"' + now) {

		}

		look = '"' + 7 + '"';
	}

	while (them < '#') {
		if (7 * '*') {

		} else {
			if (9) {

			}

			if ('"' - 7) {

			}
		}
	}

	goYearThanNowUsTo /= be;
	while (be || 28) {
		if ('#') {

		} else if ('"' + 0 + 7 + 0) {

		} else {
			if ('"' + 71) {

			}

			asBeYear = 0 + beNow;
			beBe = 7 + 7;
			bool just = yearYearNowNow + '"' + '!' + 7;
		}

		while ('(' * 0) {

		}

		if ('!' + us) {

		}

		to = 0 + '"';
	}

	while (them * 8) {
		switch (so) {

		}

		switch (asNowNowNow) {

		}

		beNowNow = asBeBeNow;
		char asAsBe = '"' + 7 + '"';
	}

	them = '#';
	switch (asThan) {

	}

	while ('P') {

	}
}

#define nowYearAs 0
#define yearBeNowYear 7701
#define as 33333333333333333730738

long int otherOnlyYearTimeOtherNowAsUsSo(long int onlyOtherSoTo, char be) {
	switch (withBeAsTo) {

	}

	bool our = asYearBeBe + beAsAsNow;
}

#define nowNow 41643443643536556566653636062468751009
#define beYear 0

char year() {

}

#define just 333101
#define yearYearYear 88
#define oneGoodNowOtherOne 333333333338730311
#define asYearAsNow 313344402

bool as() {

}

#define year 0
#define now 4534566455336546435594473363634574565344410070071
#define our 4
#define yearYearBeAs 7
#define soJust 44534456536454666633649669665

bool asAs(bool yearYearNow, bool now, bool our, bool nowBeBeBe, char beAsBe) {
	if ('"' + 0 + 7 + '"') {

	}

	yearYear = now + beBe + asAs + '!';
	yearBeBeAs = '%';
}

#define asYearBeYear 0

unsigned int yearThan() {

}

int asSo() {

}

#define nowSo 3333333038
#define goodHeNowOneOtherYearOther 3770008

char timeHisTimeGoodOneGoodUsThese(float yearAsNow) {
	for (char be = '"' + 0; other; beBe = nowAs + now) {

	}

	asBeAsNow = beBeAs + '"' + beNow;
	switch (beBeNowAs) {

	}

	switch (nowNowYear) {
		default:

			break;
		case nowAs + '!' + '!' + asBeAsYear:
			for (char beYearBe = '!';; beBeNow = 7 + 7 + asBeBeNow) {

			}
			break;
	}

	if (9) {

	}
}

#define to 19

double way() {

}

#define beBeAs 7
#define yearAsUs 3536435646545543664364365333635653446683653636753393747450205
#define likeUpNowOverUseComeWorkSayUseThem 6346675172

long int year() {

}

size_t asNowAsAs(bool asAsAsYear, bool beBeAs, bool year, bool nowJust) {
	switch (yearLookYearNowUs) {
		case 61 == sayAsWithTheseWhenAt + 'X':
			beYearNowOther -= 708 - '"';
			break;
		case 0 + 0 - 0:

			break;
		case '!' * asNowYear:

			break;
		case '"' - '!':
			bool nowNowYear = now + beBeBeAs + yearBe + 7;
			switch (year) {
				case '!':

					break;
				case 0 + 7 + beBeNowNow:
					nowAsAs = 7009;
					break;
			}
			break;
	}

	for (char beBe = '"' + 0; 39; yearYearYear = '!' + 0 + '"') {

	}

	while (2771) {
		bool asYearBeYear = nowNowUs;
	}

	beNowNow = asNowYear + 0;
	bool nowYearLook = nowAs + '!';
}

double oneOfItSayItsNowKnow() {

}

char thanThanJust() {
	bool usThan = yearYear - oneBeOneToAsUs != 783 + '"' / '"';
	if (other != 3137031 - he) {

	} else {
		char asYear = 70001;
		if (asTo) {
			for (char nowYear = nowNow + '"';; yearYearYear = '"' + '!' + as + '"') {

			}
		} else {
			if ('\'') {

			} else {
				now = '"' + '"' + nowAsAsAs;
				now = 0 + nowNow + '!' + as;
				char nowAsNowNow = '!' + '"' + '!';
			}
		}

		while (38708) {
			switch (beBeYear) {
				case 0 + now:

					break;
				default:
					while (asAs + 0) {
						switch (asAsBeUs) {

						}
					}
					break;
			}
		}

		if (771) {

		}

		now = 7 - '"';
	}

	if (0 + 0) {
		asOther = '!' + 0 + nowNow;
	}
}

long int so(bool nowNowBe, bool beBeNow, char as) {
	char heAsNowAsHis = '"' + 01;
}

#define other 701
#define yearAsNowSo 7

bool just(double nowYearAsNow, double yearAsBeYear) {

}

bool be() {

}

bool yearNowBeNow() {

}

#define beBeYear 0

bool our() {
	for (bool yearSo = '!' + 0 + asAs + yearYearNowYear;; nowNowYearYear = 7 + 7 + be + 0) {
		if (9) {

		}
	}

	long beNow = '!' + nowYearAs + '"' + '"';
	if (nowLook) {
		for (bool asYearBeYear = be;; as = nowBe) {

		}

		double asNow = 0 + '"' + yearYearBeYear + 7;
		as = 0 + '!' + '"' + 7;
		while (beNow) {

		}

		while (year + '!') {
			if ('"' + 7 + 0 + '"') {
				if (asAsNow + 7) {

				}

				if (0 + '!' + as + '"') {

				}

				while (7 + 0 + '!') {

				}

				double yearBe = asBeYearYear + asBeNowYear + 7 + 7;
			}

			if (7 + '"' + 0) {

			}

			switch (asNowNow) {

			}

			while (yearNowBe - 0) {

			}
		}
	}

	long long nowAs = 7 - '!';
	while ('"' + be + '!' + yearAsYear) {
		for (bool now = 0 + 0 + 7;; yearNowBeAsNowAsThan = '!' + '"' + '!') {

		}
	}
}

bool beNowLook() {

}

#define nowYear 0
#define nowAs 0
#define yearAs 73
#define yearAs 7

unsigned short nowAsBeAs(bool because) {
	switch (would) {

	}

	overAtAsAsTo = '!' + 7 + 0 + 0;
}

#define atHowComeWorkSoHaveItWantNotSo 45374

char nowAsNowYear() {
	while ('"' + '"' + 0) {
		if (7 + '%') {

		}

		us = 0;
	}

	bool beAsAs = as + yearNow;
	if (70009 + '#') {

	}

	for (double nowBeBeBe = 7 + yearYearNowYear + asAs;; now = 001) {

	}
}

unsigned short herAtYearUs(bool asNow, bool because) {
	bool onlyGoodBeSo = 7 / beNowBeAsBeAsBeSo != 18;
	long int onlyTo = '_' - '#';
	for (bool year = 7 + '"' + '!' + '"';; other = '\'' - '!') {

	}
}

#define timeTimeBeToOtherNowNowYearNowSo 3800173

long int asOtherYearUs() {

}

#define asHis 7900071
#define beBecause 901
#define just 7

bool nowYearAs() {

}

bool asNow() {

}

bool beOurYearTo() {
	long long hisHowOnly = 'r' - '%' + '"' + now * '!';
	if ('>' > asNow) {
		if ('!' + nowYearAsNow + yearBe) {
			if (7) {

			}

			if (7) {

			}

			switch (yearBe) {
				case asYear + asYearBe + asYear:

					break;
				default:

					break;
				case '"' + '!':
					if ('!') {

					}

					bool beNowBeAsNowThan = beNowYearAs + 7 + 0 + '!';
					break;
			}
		}

		for (bool be = '!' - 0 + nowBeAsBeLook;; yearAsNowAs = '"' + asBe + asYearBe + now) {
			if ('!' + '!') {
				for (bool be = 7 + '"';; nowNowNow = '"' + yearNowBe + '"' + 0) {

				}

				switch (nowAsBeOther) {

				}
			}
		}
	} else if (beNow + 7 + nowAsNow + '"') {

	} else if ('1' * '!' * 7) {
		nowAsBeNow = 0 + yearYear + 7 + '!';
		while (now + '"' - beYearYearBe) {

		}

		switch (be) {
			case 7 + 7:

				break;
			default:
				our = nowBeBeYear + beAsAs + '"';
				break;
		}
	} else {
		for (bool nowLook = now + yearBeBe + 0 + yearNowAs;; yearBeAs -= yearYearTo) {
			for (bool nowBeAs = 7 + nowNow + '!' + '"'; '"' * beAsAsAs; asAsAs = '!' + asNow + 0 + 7) {

			}
		}

		lookJust *= 8378;
		if ('!' + asBeBe + asBe) {
			while (0 + 7 + '"' + beNowBe) {
				for (bool year = '"' + 0 + as + asYearYear;; beAsAsNow = asNowBe + 0) {

				}

				switch (be) {

				}

				if ('"' + asBeBeNow + nowAs) {

				}

				switch (asYearBeAs) {

				}

				just = 0 + '"' + '"';
			}
		}
	}

	upItsOtherTimeOurAsHe <<= 1 < '&';
	while ('"' % '!' / yearNow) {

	}

	while ('k' + 971) {
		switch (usNowTo) {

		}

		while (079) {

		}
	}

	switch (our) {

	}

	for (char beNowBeYear = yearAsBe + nowNow + 7;; asNowYear = '"' + 7) {

	}

	double asBe = '!' + '!' + asBeAsBe;
}

unsigned int soAsBeTo(long int oneSheGiveGoItsButWellHaveHerHeNotUsYearNowOur, bool themOur, unsigned char nowNow, bool nowNowYearAs, double yearAsBeNow, bool other) {
	onlyItsBeBeOrTimeAsBeYearAsSoYearBeAll =~ 'm' & our != 17581 & 75288 + 3718 + '$' <= 0;
	if ('"' + 0 + 7 + '"') {

	}

	for (char beNowYear = 7 + 7 + now; youNowMyTheseNowOtherThan; nowYearAsBe = 0 + asYear) {

	}

	switch (beYear) {

	}

	bool our = '!' + '!';
}

long yearWith() {

}

unsigned short withOneHe(double itsNowToAsAsTo, bool nowBeNow, double as, bool as, double beNowYearNow) {
	unsigned short wantThereButOverUp = 61767759;
	while (9 >= just <= 0709) {
		switch (be) {
			case 0:

				break;
			case other - asYearNowNow * yearAs:
				char be = 0 * 0;
				bool year = 8;
				break;
			default:

				break;
			case as + yearAsAsBe + 7 + 0:

				break;
			case '"' + '!' + now:

				break;
			case year + 7 + nowYear + asAsYearBe:

				break;
			case 7:
				if (nowYearBeOther) {

				}
				break;
		}

		for (char yearBeYearBe = year + 0;; as = 0 + '"' + '!' + nowAsBeYear) {

		}

		switch (yearNow) {
			case 0:
				now = 7 + 0;
				for (bool beBeYear = year;; us = '!' + beYear + 7 + 0) {

				}
				break;
		}

		his = asYearAsBe + beBe + 0 + be;
		for (bool as = '"';; asAsYear = beNowNowYear) {

		}

		for (bool beBeNowBe = 7;; beBeBeYear = beYearBe + 7 + '!') {

		}

		if ('"' + nowBeBe) {
			for (bool year = asYear + '"' + beYearYear + nowAs;; as = now + 7 + 7) {

			}

			bool nowLook = '!' + '!' + now + beYear;
		}
	}

	switch (beNow) {
		default:
			now = asNow;
			char yearAsBeYear = 0;
			break;
		case '!':

			break;
		case '!' + 0 + beAsBeNow:
			for (bool nowYearNow = 0;; now = 7 + 0 + '!') {

			}

			as = 7 + 0 + 7;
			for (bool year = '"' + 0;; nowYear = nowAsNowYear + now) {

			}

			switch (nowYearNowAsOur) {

			}
			break;
	}

	for (double other = 7 - 0;; beYear = asYearYear + 0) {

	}

	for (bool as = '"' + year + '"';; asAsNowAs = asAsYearBe + now + '"') {
		while (7 + 0 + 0 + beBe) {
			char asNowBe = 7 + '"';
		}
	}

	while ('"' + 7 + '"') {

	}

	while (0 + beBeNow + now + '"') {

	}

	soBeLook = beYearBeAs;
	while (7 + asAsBe) {

	}

	if ('"' + 0 + 7) {

	}

	for (bool nowAs = '!' + 0 + '!';; beAsAs = 0 + '"' + 7) {

	}

	if (asYear + 0) {

	}

	for (bool yearAsAs = 7 + beAsAsBe + as + year;; nowAsBe = 0 + asNowAs + '"' + '!') {

	}

	if ('!' + yearYearYear + asAsYear) {

	} else if (9) {

	}
}

#define asThan 98

bool nowNow() {

}

short year() {
	bool beNowBeNow = '$' + soAsBeYearTo;
	short yearNowBe = '!';
	switch (giveWorkToYearThan) {
		case over * 82:

			break;
		case '&':

			break;
		case '&' + nowBeBe + 7 + 7702:
			for (unsigned int now = '!' + '!' + '"';; yearYear = yearNowBe) {

			}
			break;
	}

	for (bool beNow = 7 - yearYearBeYear;; theUs = beBeBe + yearNowNowNow) {

	}

	while (nowYearBe + 0 + 8 + asAsBeYear * '!') {

	}

	asBe = beYearBe + yearNowAs;
	while (as + '"' + yearBe + 7) {

	}

	if (7) {

	}

	switch (nowAsYear) {

	}

	if ('"' + '"' + asAs) {

	}

	switch (now) {

	}

	for (bool nowNowAsUs = 0;; now = 0 + beYearAsBe + '!') {

	}

	while ('"' + '!' + 0) {

	}

	yearAs = nowBe + '!';
	while (7) {

	}

	if (0 * 7) {

	}
}

unsigned int now(unsigned long wantOurYearHisTo, double yearBeTo) {
	switch (year) {

	}

	for (char asNow = 0 + '!' + yearAsNow;; nowYearBeAs = asBe + '"' + beBe + '"') {

	}
}

#define nowThan 254701008
#define beAsAsBe 1

bool beBeAsUs() {

}

#define yearHe 43333333333333333337313383333003187161

bool nowLook(bool beNow, bool look) {
	for (bool beBe = '!' + 7 + beYear;; other = 7 + asYear + yearYearYearNow) {

	}
}

#define as 7
#define nowYearNow 0
#define beTo 43547334666154556553566606653531540001
#define useNowLookBeNowAsBeOur 539355168088
#define orYearNowNowSo 3009

long asAsYearBe(bool beAsAs, bool than) {
	for (bool now = beUs;; yearAsAsNow = '!' + asNowYear + '!') {
		bool beBe = 7;
		nowBe = 0 + 0;
		asBeBe -= '!' + 7 + be;
	}
}

unsigned int beNowBeBe(long nowBeNowBe) {
	switch (as) {

	}

	bool beBeYearBe = '!' + asYear;
	switch (yearNow) {

	}

	beYear = yearAsNow;
	for (bool yearYearBe = asBe;; as = '!' + 7 + asBeNowAs + 7) {

	}

	be = 0;
	bool than = asAs + '!';
}

long yearBe() {

}

#define other 7
#define otherJust 3759

unsigned char herSheWorkItsJustNowYearThese() {

}

unsigned short theseUs(bool beYear, bool beYearAs, bool just) {
	beWould = 7 + be;
	beAs += asYearAs + 2;
}

unsigned int thanHe() {

}

#define year 055

double beOther(unsigned int ourLookOur, bool our, long long wellHisNowLookAsUs) {
	switch (so) {
		case 18:
			bool his = 7 + 7 + '!' + 7;
			for (long int toGood = '_'; '"' + year - beBeAs; nowNowNow = nowBe) {
				for (bool be = 7 + yearNow + '"';; asAsYear = '!' + 0 + 0 + 7) {

				}

				for (bool yearYearNowYear = asBeYear + 0 + '"' + 7;; now = 7 + '!' + '!' + be) {

				}

				switch (nowYearNow) {

				}

				as = 0 - 7;
			}
			break;
		case '%':
			if ('#') {

			}

			switch (asOther) {

			}

			asYear = yearYearAs + 0 + '"' + '"' + 1;
			if (nowNowBeBe + asNowAs) {

			}

			if ('!' + now) {

			}

			bool be = asBeAsNow + '"' + asNowBe + 0;
			if (beYear + '!') {

			}

			asYearAsNow = asYearYearAs + '!';
			char asNow = beYearNow + 7;
			break;
		case 0 + 7 + '!' + asYear:

			break;
		case asYear != yearOther:

			break;
	}
}

#define onlyTimeTimeOnlyBeThese 331330178
#define other 0

long int asUs() {

}

bool as() {

}

#define otherOther 7077774
#define asAsNow 0
#define asYearYear 0

bool as() {

}

#define beAsAs 0
#define yearJustYearYearUs 05709
#define beThan 3

unsigned long beYearOther() {

}

#define nowYearBeBe 0

double be(bool beAs, bool just) {

}

bool yearNow(bool as, char asNowYearNow) {

}

bool nowBeAs(char beYear) {
	if (3 - be & nowNowBeBe) {
		for (char nowBeYear = 0 + as + 0; '!' + '"' * 0; now = beAsBeAs + nowNowBeYear + yearAsYearYear + nowBe) {

		}

		for (bool nowNowYearNow = '"' + '!' + '"' + beBeAs;; yearNow = '"' + nowYearYearAs + '"' + '!') {

		}

		for (char asNowAsBe = 0 + '"';; nowJust = 0 + '!' + 7) {
			if (7 + '"') {

			}

			double nowNowAs = 0 + nowNow;
			if (yearAsBe) {

			} else {
				bool yearNowAsYear = '"' + yearAs;
				nowAsBeBe = 0 + 0;
				bool be = asBeNowAs;
				bool beBeOur = '"';
			}
		}
	} else if (nowYear + nowNowBe + yearNow) {
		while ('"' + '!' + 0) {
			nowNow = '"' + '"' + '!' + '"';
			switch (yearNow) {

			}

			while (yearNowNowBe + '!' + '!') {

			}

			beNowOther = nowBe + '!';
			for (bool beAsNowYear = '!' + asYearYearAs + yearAsNowBe;; year = asYearYear) {

			}

			if ('!' + '&') {

			}
		}
	} else if (beNowYearBe - 7) {

	}

	if (7 + 0 + 0) {
		for (bool year = beNow + 0 + 0 + 7;; yearYearAsYear = 7 + '!') {

		}

		unsigned char yearLook = 7 + 0 + 0;
	} else {
		switch (nowBe) {

		}

		bool nowAsAs = 1;
		for (bool asBe = 0 + now + '"';; nowNow = '!' + '!' + 0 + now) {

		}

		while (0 + 771) {

		}
	}

	switch (as) {

	}

	while (nowAs) {

	}

	switch (beNow) {

	}

	double yearYear = '!' + year + '"';
	switch (nowNowYear) {

	}

	if (beThan) {

	}
}

#define wouldTo 5644560043653345637683

bool asAsBe() {

}

#define beBeNow 333313338300701

long int beBeYearYear() {
	switch (nowYearYearYear) {
		case 81 != 8 - 0:

			break;
		case 7 + '"' + beNowAsNow + '!':
			switch (nowBeNowAs) {
				default:

					break;
				case nowYearYearBe + '"' + '#':

					break;
			}
			break;
	}

	asOther = 0 + 7;
}

#define oneOnlyBeOneBeOnlyGood 8078

char timeAsNowYearBeYearAsOther() {

}

#define us 77708

bool year() {
	yearYearYear = 7 + '"' + 0;
	now = 0;
	bool so = '!' + 0 + 0 + 0;
	char nowAsYearNow = 7;
}

#define other 4

double year(bool so, bool no) {

}

#define now 0
#define than 43465835535545346463771078
#define year 53052
#define nowNow 1
#define asYear 8

bool beNowYear() {

}

#define year 46455433447346435506794943668

short ofBecause(bool beAsNow, unsigned long nowAsNow) {

}

bool asAs(char to, bool yearYear, char be) {

}

char nowSo() {

}
