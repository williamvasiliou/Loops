bool to(char be) {
	bool now = '"' + asBeNowNow;
	if (0 + 7) {

	} else if (so) {

	}
}

#define otherOnlyOneOneOnlyGoodOtherNowAsSoOther 0708

long int so(long int timeOnlyGood, char yearNowYearBe, char nowBeBe) {
	char beAsNow = '!' - '"';
	while ('/') {

	}
}

long int heTo(bool beYear, char nowBeYear) {
	asBeAsNow = nowYearAs + asYearNowNow + now + 7;
	while (asSo) {

	}
}

#define so 83350001

double nowYear() {

}

long double beYearWay(bool other, char asNowBeYear) {

}

#define nowYear 0
#define toYearThan 7
#define as 7

bool nowTo() {
	long int asThanToOur = 586943055 + asAsComeTo;
	for (bool beBeBe = beYear + as; nowBeNowOur; nowBeNow = nowBeYearYear) {

	}

	while (yearYearTo) {

	}
}

bool thatOneGoGoodMyWantSoTo(char our) {
	if (asAsYear + '!') {

	} else if (yearYearAs + 0 + '!') {
		switch (beBeYear) {

		}

		switch (nowBe) {
			case asBeNowAs + yearYearAs + yearNowBeBe + 0:
				for (bool asNowBeAs = '!' + yearAsNow + '"' + 0;; be = yearYearBeAs + now) {

				}

				if (asAsYear + '"' + '"') {
					if (7 - '!') {

					}
				}
				break;
		}
	}

	be = 0 + 7 + '!';
	switch (beNowBeYear) {
		default:
			char nowNowNow = asAsBeAs + '!' + '"';
			break;
	}

	for (bool yearYearAsBe = so; yearNowBe + '!' * 7; nowYear = nowNow) {

	}
}

#define wayOur 79
#define beOur 11

bool yearYearYearNow() {
	char now = '"';
	if (7 * 0) {

	}
}

int nowYearNow() {

}

bool nowBeNowAs() {

}

#define with 33333033330373773
#define usBeOneAsGood 0033130777701

long int so(bool nowAs, bool other, char asAs) {
	char nowNow = 8 + nowNow + 0 + '!' + 1;
	while (7 + 008 + yearYear + '#') {

	}

	be = nowNow + 7 + '"' + '!';
	otherHe -= nowNowAs + asAs;
	yearAs = 0 + beNowAs + be + yearAs;
	than = '!' + 7 + asBe;
	if ('!') {
		bool now = '!' + '$';
	}
}

bool heYearNowYearTo(bool as, bool now, bool ourUsAsOther) {

}

bool yearNow() {

}

#define now 7
#define our 

unsigned int howThatLookGoGoodOur(short overOfOrThan, char beAsNow) {
	now += now + as + '"' + beNowNowYear;
	switch (asYear) {
		case '!' + 7 + beBeAs + '!':
			bool asNowNow = 0;
			for (bool just = 7 + year + '!';; nowAsBeBe = '"' + as) {

			}
			break;
	}

	switch (nowNow) {
		case '#':

			break;
	}

	switch (beBeUs) {

	}
}

long int goHerSayWhenWorkYouWorkHerGoJustThatUpHowHerThanBeBeNowThan(bool lookWellShePeopleLookLook, size_t peopleTimeWorkUsOfOrLikeButUseComeTheUsWantComeWellKnowLookUseSomeSoOther, double nowAs, float nowAs, bool nowYearNowUs, bool beBeBe, bool asAsAsAs, char as, bool be, bool us) {
	unsigned char beOfThat = '!' + nowAs;
	while ('"' >= 27702 + 1 + '"' + asYear - '!') {

	}

	switch (asYear) {

	}

	char nowBeBe = '"' + 0 + yearYearAsAs;
}

#define useThereItsTheSoThey 4669004

unsigned long goGoWellLookThemThan(bool just) {
	for (double nowNow = 006;; than = 7 + yearBe) {
		bool beBeNowYear = '"';
		switch (yearYear) {

		}

		switch (be) {
			default:
				if (7 + '!' + '"' + 7) {

				}

				if ('%') {

				}
				break;
		}
	}

	while (7 + as + 7) {

	}

	switch (beYearAs) {

	}

	if (7 + 0 + '!') {

	}

	switch (nowAs) {

	}

	for (bool yearAs = be + nowAs;; beAs = now + '!' + '"' + asNowAs) {

	}

	while ('&') {

	}

	nowYearYear = 0 + beBe;
	if (look) {
		while ('!' * '!') {

		}
	}
}

#define useYouThatThatWayItOrOfYearNo 343654666436664556636345535364546446754645363646354663655654444644545534445586466464663656654452724
#define nowYearToNowSoNowYearWant 76
#define twoWant 0

unsigned char asAsAs() {
	sheJustAsBeAsWould = 4178 % 55071;
	while (7 + 0 + nowBeAsAs + '"') {
		beYearBeAs = '"' * '$';
		switch (yearWould) {
			default:

				break;
			case '"' + 0 + '"':

				break;
			case 2:

				break;
		}

		our = 7 + 7;
	}

	bool would = 0 + '!';
}

#define fromGoWellItOverWorkLike 909025
#define nowNow 72

float these(bool us) {
	bool beAsOther = asAsAs + 7;
	for (char beAs = 0; '!' - asBeAsAs; asYearBe = 0) {

	}
}

int beBeYearYear(bool yearLook) {
	if ('"' + nowAs + yearBe) {

	}

	while ('"' + '!' + 0 + 7) {

	}

	switch (now) {
		case nowNow + '"':
			bool than = nowAsNow;
			break;
	}
}

bool asNowNow() {

}

char yearNowAsBe() {
	while (7 - asBe) {

	}

	if (heNowNowOther + these != 3 - 7) {
		while (0 + 7 + as) {

		}

		if ('!' + '"' + '!' + 7) {

		}

		while (asAs + beUs) {

		}
	} else if (now + 7) {

	} else {
		while (beAs) {
			be = '!' + 0 + 0 + 7;
			char beBeBe = 0 + beBeYear;
		}

		switch (nowAsNow) {

		}

		if (0 + beBeBeAs + 0 + '"') {

		}

		nowYear += 0 + '!' + '"';
	}

	if ('_' != to / 0) {
		long int yearAs = beNowBe + one;
		switch (yearYearBe) {
			default:
				char asYear = '!' + '!';
				break;
		}

		for (char beYearNowNow = yearAs + yearYear + asAsYear + nowNow; yearAsNowAs + 1; yearYear = 0 + 7 + 0 + 0) {

		}

		bool asNowYear = 8;
	} else if (7 + 7 + 0) {
		bool so = yearBe;
		for (bool nowNowNow = '!' + '!' + nowNowYear; 778; nowAsAsYear = '"') {

		}

		if ('!' + '"' + '!' + yearYear) {

		}

		switch (so) {

		}
	} else {
		if (781) {

		}

		if (7 + as) {

		}

		long int yearBeAs = '+';
		switch (beYearNowNow) {
			case asBe - yearBeYear:

				break;
		}

		if (as + 0 + '"' + '!') {

		}

		beAsAsYear = yearYearYear - 7;
	}

	char other = as + 7 + '"' + beYearAsBe;
	for (float beAsBeYear = '"' + 0 + 0;; as = 0 + asAsAsNow + 7 + '!') {

	}

	nowYear = '"';
	switch (beAsNow) {
		case '!':

			break;
		case 0708:

			break;
		case 0:

			break;
		case nowBe + now:

			break;
		case '"' + 01:

			break;
		case 0 + '!' + '!' + as:
			asUs = '"' + 0 + 0 + yearBeNow;
			break;
	}
}

#define becauseUsTo 0
#define beYearAsNow 67679578

bool sayJustSo() {

}

bool so(bool look) {
	if (79) {
		if (asYearNow + '"') {

		}

		bool beAs = '!' + yearYearBeAs + '"';
		our = nowAsBeAs + 7 + '!';
		switch (yearNowThan) {

		}
	} else {
		bool just = 7 + nowNowNowNow;
	}

	for (bool asBe = nowYearBeNow + 0;; yearYearBe = 0 + asNowAs) {

	}

	bool be = 7 + be;
	if ('"' + now) {

	} else if ('&') {

	}

	switch (now) {

	}

	beBeBeBe = 7 + beNow;
	for (bool as = now + now + be + yearBe;; as = '!' + yearBeBe) {

	}

	while ('"' + year) {

	}

	nowBe = '&';
}

double to() {

}

#define comeYearUsWithAt 7
#define us 58

bool year() {

}

#define asAsAs 0
#define us 65444343426457767508001
#define wellWantGiveAsTwoYearWorkOur 34448

bool nowAs(char be) {

}

char asAs() {

}

#define sayBeWay 0
#define nowNowBeYear 7
#define asBeBe 0

bool now(double asBeNow) {
	for (float other = so - asAsNowBe; '#' + nowAsUsTo; yearNowNowBe = now) {

	}

	float beBe = 7 + 7 + 0;
	bool goodUsBeYearBeTo = onlySoAsHe + '#' - 1;
	if ('O') {

	} else {
		nowBeSoYearBecauseNowBecause |= 7 + yearHe + '"' - beAs;
		switch (asAsBe) {

		}

		if ('!' + '"' + '"' + 7) {
			bool yearAs = '!';
			switch (beBeAsAs) {

			}

			for (char now = now + as + now;; nowNowYearNow = year + 0 + yearAsNowAs + '"') {

			}
		}

		switch (yearAs) {
			default:
				yearNow = 0;
				switch (beNowAsBe) {

				}

				switch (so) {

				}
				break;
		}

		while (7 + yearNowBe) {

		}

		if ('%') {

		}
	}

	if (7 + oneUsSo / 0) {
		switch (yearNowOtherTo) {
			default:
				while ('!' - 0) {

				}
				break;
			case 08:

				break;
			case 0 + '!':

				break;
			case 7 + be:

				break;
			case 0 + 7:
				be += 0 + 7 + nowNow;
				break;
		}
	} else if (0 + 0) {

	} else if (7 + '!' + 0) {
		soSo = yearNowAsYear;
		bool us = asNow;
	} else {
		switch (year) {

		}

		while ('#') {

		}

		bool beNowAs = '"';
		while (0 + '"' + 0 + 7) {
			for (bool yearYearNowNow = 7 + '"' + asNow + 7;; yearYearBeBe = '"' + beBe) {

			}

			beYear += 0 + '"';
			while (asUs) {

			}
		}
	}
}

float nowBeYearTo() {

}

#define be 2
#define beYearYear 44535663302307452009

char thatToAsOur(bool yearYearOur) {

}

#define not 96
#define usSo 6418
#define whenOurSay 7

long double asJust(bool asYearBe, bool us, bool as, bool year, char beYearYear) {

}

unsigned long when() {
	for (long int us = yearAs; '%' / '!' != timeHis + 88 + '%'; year += 7 + '!' + asNowNowYear) {
		bool us = 0 + asNowNowBeOther;
		switch (yearAs) {

		}

		if (7 + 7) {

		}

		for (char yearYearNow = '!';; now = 7) {

		}
	}

	for (long int asHeYearYearOther = goodAsAsOther != '_' + 81; 0 + 108; us = 0 + '!' + asYear + '"') {
		bool asToYearBeSo = asBe + '!' + asYearNowBe + '"';
		bool so = 7 + 7 + yearAsBeBe + 0;
		char nowYear = '!' + asBeNow;
	}

	for (bool beAsBeNow = year;; asYear = '!' - '"') {

	}

	for (bool year = 0 + '"' + 0;; yearBe = '"' + '"' + '!' + '"') {

	}

	so = '!' + be + '!' + 0;
	asYearNowOther = 0 + asBeNow + year + beBeBeBe;
	for (bool asYearBeBe = '"' + '"' + as + '!';; now = '!' + '"' + '"' + '!') {

	}

	if (as + '"' + '"' + year) {

	}

	for (bool yearAsBeBe = 0;; nowNowAs = '!' + 0) {

	}

	bool yearNowNow = '"' + '!';
	if (nowBe + 0 + '"') {

	}

	beYearNowBeUs = 7 + 7 + as;
}

#define toYouUsNowThanSo 3
#define yearAsAsNow 8

char year() {
	for (unsigned long overTimeOverOneSayWantUpPeopleThereWhenOfToComeWhenWellFromHerAsSayUp = nowAs + year + '!' + '!'; 48 + comeAsUs < 7 <= beNowBeAs; theSheTheItOneJustGoOurBeBut =~ 377069) {
		bool nowYearNow = 0 + 0 + asYearNow + 0;
		for (bool as = 001; yearBeBe * other; asYearAs = yearBeYearBe + 0 + 7) {

		}

		while ('!' + '"' + '!') {

		}

		bool asAsSo = yearBeYearBe + '"';
	}

	switch (lookAsYearTo) {
		default:
			switch (yearBeYearAs) {
				default:
					bool yearBeBeNow = be;
					if ('!' + yearAsBe + 0 + 0) {

					}

					be = as;
					asNowBe -= beYear + '!' + as;
					break;
			}
			break;
		case '!':
			if ('"' + '!') {

			} else if ('!' - 0) {

			}
			break;
	}

	while (sayHowOneItsUpHaveUsTwoNowLikeMyTheyLikeNowSoOtherJust == 52) {
		goodGiveUsBecauseNowNotJust = 7 + '!';
		while (79 + 2) {
			while (orUsNowOurSo + year < 0) {

			}
		}
	}

	if ('"' + year + be + 0) {

	} else if (nowYearBe) {

	} else if ('"') {

	} else {
		while ('!') {

		}

		switch (asOther) {

		}
	}

	switch (yearThereBeUsNowOur) {
		case 5:
			for (bool asNowNow = 7 + '"' + 0 + '"';; our = '!' + '"' + asAs + 0) {

			}
			break;
		case beUs * 1:

			break;
		case 7 * '"' * nowNow:

			break;
		case '"' + '!':

			break;
		case asBeAsAs + 0:
			switch (asAsYear) {

			}

			for (bool yearYearYearNow = 7 + now + 0 + 0;; as = yearYearYear + '!' + '!') {
				while ('"' + '!' + nowYearAs + '!') {

				}

				if (7 + '"' + nowYearNow) {

				}

				if (7 + now) {

				}

				beOur = nowYearAs + as;
			}
			break;
	}
}

int nowNow() {

}

long int overWhenUpWantHerThereWorkOrItsOneTwoSomeSomeGiveComeTimeOneOverUpGoGoToGoodNo(bool workWhenOverWorkUsTheThey, bool year, long double soNowAsLook) {
	switch (beSo) {

	}

	while (as + '$') {
		for (double yearYearBe = '!';; nowAsNowAs = 7) {

		}
	}
}

short wantAsLookHowLikeBeBeYearUs(long long wellSheUpOurGoLikeLikeOnlyUseOfThePeopleSomeOur, char useOneWorkOverOfUseHaveYouHerOfSomeThereHerOfWorkNotComeAsWithYouGoNotBeUs, bool to, bool nowSo, long long he) {
	for (bool yearBeBeBe = 0 + 7;; look = yearNowAsAs + 7 + 7 + yearAsBeYear) {

	}

	if (asNowBeYear + now + 7) {
		while (0) {

		}

		if (beNow + year) {

		}

		asJust = asAs + as + 7 + yearBeAsNow;
	}
}

#define be 2874022
#define now 5635636536665333364635544537538207708
#define so 4

bool asAs() {

}

bool now(bool asNowYearYear, double asNowAs) {

}

long int onlyTimeOneOneTimeGoodGoodTheseGoodOneOneGoodGoodTimeTimeOneGoodOnlyWellYearThem(double yearBeNow) {
	while (0 * '"') {
		for (bool asYearBe = '"' + 7;; be = year) {

		}

		if ('$') {
			unsigned long nowButAsUs = '!' + 08;
			while ('%' / be) {
				bool year = 7 + '"' + '"' + '"';
				beBeAsBe += '&';
			}
		} else {
			while ('"') {
				bool now = nowAs;
				nowBe = '"' + 0;
				while (year + 0) {

				}

				bool nowAs = '!';
				switch (nowAsYearAs) {

				}

				asAs = '!' + '!' + as;
				while (as + '"' + 0) {
					while (nowNowAsNow + '!' + 0) {

					}

					for (double now = '"' + 0 + beNowYearNow;; be = as + 0) {

					}
				}
			}

			for (bool beNow = beBeNowYear + asYear + yearAs;; beNow = '!') {

			}

			switch (nowAsYearYear) {

			}

			for (bool be = 0 + '!';; be = yearBeAsAs + 0) {

			}

			be = now + '!' + yearNowNowNow;
			switch (nowJust) {
				case 7 / 7:

					break;
			}

			for (bool year = beNowYear + 7;; be = 0) {

			}

			while (asAsNowAs + now + 0) {

			}

			for (bool yearBeAsBe = yearNowAsYear + '!' + '"';; nowAsAs = 7 + '!' + 0 + 0) {

			}

			switch (as) {
				case 7 + 7:
					while ('!' + beAsNow + asBeBe + 7) {

					}

					nowBe = yearNowAs + nowNow + '"';
					while ('!' + 7 + be) {

					}

					while ('!' + now + asYearBeBe + nowBeNowNow) {

					}

					switch (yearYearYearYear) {
						case 0 + '"':

							break;
						case 7 + beYearBeBe:

							break;
						case '!' + 0:

							break;
						default:
							if ('!' + be) {

							}

							if ('&') {

							}
							break;
					}
					break;
				case 7 + 0 + 7 + year:

					break;
				case asNow - beBe:

					break;
			}

			double beAs = 7;
		}

		way = '"' + '"' + 7;
		long long my = giveHave + '$' + yearNowYear - '!';
		for (bool beNowYearAs = 7;; asAsYear = asYear + as) {

		}

		if (year + beYearAsAs + year) {

		}

		switch (yearNowOther) {

		}
	}

	long yearAsYearAs = asYearYearYear + year + '"' + '!';
	heUseBecauseAllThanOtherNowNowYearAsBeNowSo ^= 4763673;
	if (']' - nowAs) {

	}

	double asYearBeYear = asBe + 0 + '!' + 0;
}

#define itsThatToGoodWellSomeOfWantNoOfTo 54545563333465545134566554379543

short oneBut() {
	as = '"' + yearNowAs * '!';
	bool yearAsYear = '"' + 0 + 0;
	now = '!' + '"' + 7 + 7;
	while (nowNowAsBe + 7 + 7 + '!') {

	}

	bool beOther = '!' + '!';
}

#define yearBeNowYear 2
#define our 3409

bool beYearBeNow(char beBeYear) {

}

#define beBeAsAs 7

char beNowYearAs() {
	while ('%' + our) {
		while (beNowNowAs * '<' | '(' + '"' * '!') {
			year <<= 0702071;
			for (bool beNowAsYear = 7; 34698 * '!'; so = now + '"') {

			}
		}

		if (7 + be + nowYear) {

		}

		while ('.') {
			bool nowNowAsAs = '!' + '"' + '"' + yearNowYear;
			while (nowBeBe + 7 + 0 + '!') {

			}

			asNow = '!' + '"' + 7 + '!';
			switch (nowAsYear) {

			}

			if ('!' + yearAsAs + '"') {

			}

			for (bool beAs = 7 + year + 7;; nowNowYear = as) {

			}

			to = '!' + 0 + '"';
		}
	}

	if (7 + '!' + beYear) {

	} else {
		if (nowJust + 0 + 9) {

		}

		for (bool nowAs = nowBe;; yearNowBeYear = '"' + 0 + 7) {

		}

		while ('"' * '"') {

		}

		while (0 * asNowYearNow) {

		}
	}

	switch (that) {
		default:
			switch (comeThan) {

			}

			beYear = '"' + nowAsYear + '$';
			break;
	}

	while (yearAsYear + '&') {
		asAsThanJust = 7 + year + '"';
		for (bool nowBeYear = '"';; yearYearYearAs = yearNow) {

		}

		if (yearBeNow + 0 + '"' + '!') {

		} else {
			bool asYearAsSo = '!' + '!' + 0 + '!';
		}
	}

	while (as + 0) {

	}

	while (0 - '!') {
		asNow = '!' + 0 + yearBeYearNow;
		switch (beYear) {
			default:

				break;
			case '!' + 7001:

				break;
		}
	}

	if ('#') {

	}
}

#define well 965558
#define be 19

long int goodOtherOnlyBecauseGoodToTime(bool usNowYearSo, char nowBeYear) {
	for (char so = yearYearYearNow + '!' + '!' + '"'; nowNow + nowYearAs + 01; nowAs = now + 0) {
		asBeAsNow = '!' + 1;
	}

	if (be) {

	} else {
		if ('"' + 7 + 1) {

		}
	}

	bool asNow = nowYear + 0;
	bool beNowBeBe = nowAs + nowBeYearNow + beBeAsAs;
	yearAs = yearNowAsBe;
	while ('"' + beBeNow + 7 + '"') {

	}

	switch (nowAsBe) {

	}

	if (0 + 7 + 7) {
		for (char beYearBeAs = asNowAsAs + '!' + 7 + 0;; beNowNow = yearBeBe + now + 0) {

		}
	}
}

long int oneBeTimeTimeTimeBecauseSoYearNowNowYearOther(bool yearYearAs, bool yearYear, bool yearNowNowAs, bool asYear, char beNowNowSo) {
	switch (asNow) {

	}

	beYearAs = 7 + 7 + 7 + 7;
	if (0 - '!' - '"') {
		he = 0 + '!' + 7 + '"';
	} else if ('"' + 0 + '!' + asAsAsBe) {
		while (beAsNowSo - now) {

		}

		switch (nowYearNow) {
			case '"' + 0 + '!' + '!':
				switch (beAs) {
					default:
						if ('"' + 0 + 0) {

						}

						switch (as) {

						}

						as -= asNow + '"' + 0 + '"';
						break;
				}
				break;
		}
	}

	switch (yearYearNow) {
		default:

			break;
		case year - year:

			break;
	}

	now = 7 + '"' + 0;
	be = '!';
	if (081) {

	}
}

#define beSo 48

bool to() {
	if (so & nowAsYear) {

	} else if (now * '!') {

	} else {
		for (char asNowBeNow = 0;; year = 7 + '!') {

		}

		bool nowSo = '"' + '!';
		beJust = '!' + '"';
	}

	someLikeGiveGoodWhenSayGoodWellOnlyPeopleOurUpOverOfAtGoThatHerThereKnowOnlySomeAsTimeNowTo =~ goTheTwoOneTwoGoOverGiveItsHowUpOrHeUseYouNowAtYearAsAsThemLook >= 655364645444046444163836376 || '+' & 7440744537268 <= '!';
	switch (becauseBeAsAsYearWay) {
		case year + be:

			break;
		default:
			for (char asYear = now + beNowNow;; asAs = 0) {

			}

			as = '"';
			beYear += nowBeOther;
			beAsBe = '!' + 7 + nowAs;
			for (bool as = 0 + 7 + 0 + '"';; as = 7 + '"') {

			}

			beYear = '"' + 7 + '!' + '"';
			while (asAs + asBe + 0 + asBeBe) {
				switch (nowBeAs) {

				}

				switch (yearNow) {

				}

				now = 71;
			}

			for (bool beBe = '"' + '"' + nowYear + '"'; 2; nowAsAsYear = 7 + asNowNowNow) {

			}
			break;
		case '&':
			as = yearBeAs + beNowBe * nowAsNow;
			asNowYear = 7 + '!' + 0 + 0;
			for (bool asBe = beNowYearYear;; yearJust = now) {

			}
			break;
		case beJust:

			break;
	}

	while ('!' + 0 + 0) {
		for (bool beYear = year;; beBeYear = 0 + 7 + beAsAsBe) {

		}

		switch (yearAsAs) {

		}

		yearYearNowBe = '"' + '"' + '"' + 0;
		asBe = yearBeNowAs + 0;
		if ('"' + 0) {

		} else if (7 + '!' + 7) {
			beNowAs = asAsAsYear + asYear + '!';
			as = nowYearAsNow + '"' + 7 + nowNow;
			bool beNow = '"' + '"' + as + 7;
			now = 0 + '!' * '!';
		}
	}

	while ('!' - 0002) {
		while (0) {
			while ('&') {

			}
		}

		unsigned int notSoYearMy = nowAsBeBe + year + 7 + 7;
		char thanNowAsUs = 101771 + asAs * 0;
		yearNowBe = '&';
	}

	bool nowYearBe = '!' + 7 + '"' + nowYear;
	switch (as) {
		case '"' + nowAsYear + '!':
			for (bool nowYear = asAsBe;; beYear = '!' + 7) {

			}

			for (char asBeAs = '!' + 0 + '!' + '!';; yearBeNowYear = 7 + yearAs) {

			}
			break;
		default:
			while (0 * 0) {

			}
			break;
		case 0 + 7:
			yearYear = asYearAs + 7 + '!' + '"';
			if (0 + yearNowYearAs) {

			}

			for (bool now = asNowAsYear + 0 + 0 + '"';; look = 7 + 7) {

			}
			break;
	}
}

unsigned short beAsLikeBeGoOur() {
	if ('"' + 7 + 0 + '"') {

	}

	if (6) {
		if ('#') {

		} else {
			yearBe = 7 + asNowYear + '"' + '"';
			char be = beBeAs;
		}
	} else {
		bool nowNowAs = '!' + beAsBe + 0;
		nowYearYear = year + '!';
		nowNowBeYear = '!' + nowAsBeNow + 7 + '"';
		bool beBeAsNow = 7 + 0 + '!' + '"';
		as = as + beAsNow;
		while (7 + '#') {

		}
	}

	nowBe = 0 + nowNowBeYear;
	bool our = 7 + 0;
}

bool asNowBeBe() {

}

char asJust() {

}

double yearYearBeAs(bool as, char yearYear) {
	if ('v' & atUseTo * '"') {
		while ('!' + 7 + '"' + be) {

		}

		us -= '"' + be + '!' + '"' + '"' + 2;
	} else {
		lookAsThan += 'N';
	}

	for (bool yearNowBe = beAs; asBeYearLook <= '"'; nowOther = than) {

	}

	long int yearThey = asMy;
	switch (year) {
		case '"' - 7:

			break;
		case '&':

			break;
	}
}

short asAsAs(bool beWould, bool to) {
	if ('*') {

	}
}

double asPeople(bool beYearNow, char be, bool beNowYear, bool toOther, bool our, double yearAs) {

}

#define beNowNowNow 0
#define asThemOurAsOther 0

long int beBeAs() {

}

char asNowBe() {
	if ('!' - soUs / beNow) {
		for (bool yearAsNow = beAs + '!';; as = 0 + beYear + 0 + '"') {

		}

		switch (asYearNowNow) {
			default:
				for (bool other = '"' + 7;; nowNow = now + '"') {

				}
				break;
		}
	} else {
		switch (to) {
			case '!' + '"' + 7:

				break;
			default:
				if (yearNow + '"' + '!' + '"') {

				}

				for (char yearBeNowAs = '"' + asAsAs + 7;; beNow = '"' + nowYearBe + 7 + '"') {

				}
				break;
		}

		switch (asAsBeBe) {

		}

		if (0 + 7 + '!') {

		}

		switch (as) {
			case '"' + '"' + 7 + 0:

				break;
			case asBe + '!' + asYear + '"':

				break;
			default:
				asAsBe = '"' + '"' + '!' + nowYearNowNow;
				while (yearAsNowBe + 7 + 0) {

				}

				yearAsNow = asSo;
				break;
		}
	}

	heOther |= other != usYearNowYearOther;
	for (bool beYearBe = yearNowAsYear;; yearYearNowYear = '"' + '!') {

	}

	beOther += other;
}

bool becauseGoodTime(bool becauseYearBeBeTo, bool beTo) {
	char year = '"' + 0;
	for (float nowYearBeAs = 0 + '!' + year + 0; '#'; nowNowBe = '"') {

	}
}

#define oneTheseTimeThese 0333

float nowAsAsNow(bool nowSo) {

}

#define now 3
#define usAsNowBeSo 7

bool nowAsYear() {
	beYearAsBe = 0 + as;
	for (bool now = '!' + '!';; asNowNowYear = beYearBe) {

	}

	while (0 + '!' + 7 + 0) {

	}

	for (bool as = now + 7 + 7 + 7;; beBeYear = asBe + 7 + '"' + 0) {
		double year = '"';
	}
}

bool beYearBe(char yearBe) {
	switch (workLikeItHowFromFromHowHisAt) {
		default:
			bool beAsAsNow = other * asNowYear;
			break;
		case nowAsAsYear + 7 * '!':

			break;
		case '!':
			if (0 + 0) {
				if (7 + '!' + 7) {

				}

				for (bool asNowYearAs = 7 + 0 + yearYearYearBe;; as = '!' + 0 + '"' + yearBeYear) {

				}

				switch (nowAs) {
					default:
						if (beBeNowNow + '"' + 7 + year) {
							for (bool yearYearYear = 0 - 0;; be = 7 + beYear + 7 + '!') {

							}
						}
						break;
				}
			}
			break;
	}

	switch (itsUpHowOneThanOfBeBeLook) {
		case 31 <= just:
			be = 7 + 7 + 0;
			for (bool now = '!' + '!';; now = '!' + '!' + '!') {
				for (bool asAs = 7 + '"'; now - '"'; yearBeBe = nowYearNow + as) {

				}
			}

			switch (yearNowYearBe) {

			}

			for (bool nowUs = beYearBeBe;; beNowNowBe = '!') {

			}

			switch (asYear) {

			}

			for (bool asNow = '"';; yearNowNow = be) {
				for (bool nowAsYearBe = beYear + '"' + '"';; yearYearYear = '"' + 0) {

				}

				if (nowBeNow + 0 + asBe) {

				}

				than = '!' + '"' + 7;
			}
			break;
	}

	while (nowOur) {
		if ('$') {
			while ('!') {
				switch (asJust) {

				}
			}
		}

		bool yearBeYearAs = '"';
		while (justSo) {

		}
	}

	if (beAsBeAs + '!') {

	}

	if ('!' + '!') {

	}

	if (as) {
		switch (beBe) {
			default:

				break;
			case 771:

				break;
		}
	}
}

#define themWith 0
#define yearYearYearNow 0

double nowYearBeNow() {
	for (float to = 0 - 0 - now; asAsAsYear + beSoOtherYearHis; be += 7) {

	}

	for (bool as = '!';; beAsYear = '"' + '"' + '!' + '!') {

	}

	if (year + 0 + 7 + yearAsBe) {

	} else {
		while ('"' + yearYearAsNow) {

		}

		while ('%') {

		}

		to = beBeNow + nowNowNow + '"';
	}
}

#define asBeYear 33333333333300338038701
#define nowNowNow 3108

long int be() {
	beAsAs = beBeBe + 0 + 7;
	bool be = yearAsBeAs + 7 + 7;
	if (7) {

	}

	for (bool asYearYear = '!' + be + beYearNow + yearAs;; asAsBeNow = '!' + 0) {

	}

	bool asBe = '"' - nowYearAs;
}

#define soOneOurYearOur 7082
#define beBeYearYear 772
#define nowNowBe 7

int beBe() {
	for (bool yearNowAsBe = as + asAsNow + 7 + '"';; beLook = '!' + '!' + 7 + 0) {

	}

	wellLookLookToSoAsYearToTo <<= 5;
	while (asYearNowAs + beYear + nowYearNowAs + 0) {

	}

	useThan = 7;
}

long int youUseWellLook(long double nowOur, bool from, bool us, bool be, double asYearYear) {
	so += '%' + '#';
}

bool nowOur() {

}

double asYouOfNowJustBeAsYearUs() {
	unsigned int ofHowThese = 0;
	char asYearBeAsAsOther = 0 + '"' * 0;
	for (bool be = '!'; 71071; he = 7 + '!' + asBeBe + beYear) {

	}

	switch (yearLook) {

	}
}

#define justTheBut 043543333303861703
#define asYear 8

bool look() {

}

double just() {

}

char yearNowBeBe() {
	if ('!' + asBeBe + nowYearAsAs) {
		them >>= 0 + 7 + '#' * 7;
		for (bool nowNow = '!' * yearYearYearYear; yearNowBeNow + 0 - 7; asYearNowNow = nowBe + nowYear) {
			bool now = 0 + beYearAsAs + beBeBe + 7;
			switch (beBeAsNow) {
				default:
					for (bool nowBeAsYear = 0 + 0 + nowNow + 0;; asYear = beNowAsYear) {

					}

					switch (as) {
						case 1:

							break;
					}
					break;
			}
		}
	}

	unsigned short year = 03708 <= nowYearAsYear + 9 + 22;
	if (0 != 4658 - '!' - 0) {

	}

	unsigned int beSo = yearYear + year;
	while ('"' + now) {
		than = 7 + 0 + 7;
	}

	while (be + '!' + 7 + '"') {
		switch (beNowNowYear) {
			default:

				break;
			case '!' + 8:

				break;
		}

		while (as + yearAsNowAs + asAsBeBe) {

		}

		switch (beAs) {
			case 7 + '"' + 7:

				break;
			default:

				break;
			case '"' + 7 + 7 + 0:

				break;
			case beYearYearAs + asAs + beNowBeBe + asAsAsBe:

				break;
			case '"' + year:

				break;
			case year:

				break;
			case 0 + 7 + 7 + '!':

				break;
			case '"' + '"' + beAs + now:

				break;
			case 0 + 7 + yearBeYear * '"':

				break;
		}
	}

	while ('!' + '!' + 7) {

	}

	for (bool nowBeNow = yearNow + '!' + beNowAsBe + nowAsAsNow;; asNowBeBe = 0 + 0 + as + 0) {
		be += 0 + '"' + 0 + '!';
	}

	for (bool year = '"';; as = 7 + now) {

	}

	for (bool asYearNowNow = '"' + yearBeAsNow + 7 + 7; yearBeBeAsSo; be = 0 + '"' + '"') {

	}
}

double beHisOurNowOther(bool asBe, bool nowAsNow, bool yearNo) {
	bool other = 0 + '"' + asAs;
	for (bool nowBeAsNow = 7;; be = asYearBeAs + 0) {

	}

	if (0 + '!' + yearAsYear + '"') {
		nowBeNow -= as + 7;
	}
}

float nowBe(bool yearOther) {

}

#define orOurPeopleNowJustOtherBeSo 33333333333333333333733
#define oneYearBeYearTimeSoYearYearBeNowBeUs 0311

long long nowYear(bool nowYear, bool asBeYearAs, bool year, bool be, bool nowAsNowTo) {
	if (asBe) {

	}

	while ('&') {

	}
}

#define butTo 640364338701
#define goodOneComeBecause 67603779

unsigned short nowAsBe() {

}

#define asWork 7
#define beAsBeAs 354653365344670554546633045574

double ourNowThem(short comeMyOur, double yearNow, bool year, bool be, bool beYearNowYearSo) {
	for (bool be = 0 + year + 7 + 0;; nowBeAs = yearNow + 7 + '"' + asAsYearYear) {
		for (bool nowTo = 0 + beYearBeYear + yearNowNow;; as = 0 + 0 + '!' + '!') {

		}
	}
}

bool asBeAsBe() {
	switch (year) {
		case '"':

			break;
		case '!':
			bool nowYear = be - beBe;
			break;
	}

	bool yearAs = 7;
	for (bool nowAsBe = '"' + '!' + 7; 0 * '!'; nowBeYearBe = yearBeYearYear) {
		for (bool yearYearBeBe = beBe;; yearYearBe = asAsYear + beYear) {

		}

		bool year = '"' + nowYearBe;
		switch (so) {

		}
	}
}

#define otherSo 3333333333333330333331333331
#define nowYearUs 12
#define them 7
#define as 0

bool yearWithJust() {
	for (bool year = 7 + 0 + yearAsYearYear + year;; asBeNowAs = 0 + yearYear) {

	}

	for (bool nowYear = asYear;; asBe = asNow + 7 + as + '"') {

	}

	our = 'z' & 'r' * '>' == '"' * '-' <= '!';
	switch (nowTwoBeGive) {
		default:
			while (asAs) {

			}

			while (7200079) {

			}
			break;
		case beAsYearYear + asNowNowAs + '!':
			switch (its) {

			}

			switch (as) {

			}

			for (bool nowAsAsYear = '"';; nowNow = '"' + 7 + '"') {

			}

			for (bool yearBeAs = '"';; as = yearYearNow + '!') {

			}

			asBeYear = '"' + 0 + 0;
			yearBe -= 0 + '!' + 7 + '"';
			break;
	}

	switch (withGoGoWorkSoJust) {
		case theseWould:
			if ('"' + '!' + '!') {
				for (bool yearAsBe = 0 + asYearNowYear + '!';; yearYearNow = '!') {

				}

				for (bool as = nowBeYear + '"' + asYearBeAs + 7; 079; asYear = year) {

				}
			}

			for (bool nowAs = now + '!' + year + yearBeAs;; yearYear = 7 + 0 + year + 0) {

			}

			while ('"' - '!') {

			}
			break;
		default:
			bool asAs = '!' + 7 + asNow + 7;
			switch (asBeThan) {

			}
			break;
	}

	if (nowBeAs + nowYearYearBe + asYearNowAs + '"') {

	}

	while (0 + 0 + asNowNowNow) {
		asNow = 7 + 7 + '!';
		while ('!' - '!') {

		}
	}
}

#define us 35866373592
#define yearBeAsThan 29

short noBeNowAsYearAsBeBeOur(char as) {

}

char nowNow() {
	for (bool as = yearYearAs; 't' | ourHe == at * '$'; nowOur *= 8) {
		my = '!';
	}

	for (bool yearAsNow = 7 + '#';; yearBeNowYear = '!') {

	}

	for (bool asBe = beBe;; nowUs = '"' + '&') {

	}

	while ('"' + '#') {

	}
}

unsigned short haveToHe(bool beNow, bool nowNow, bool beSo) {
	while (0 + yearThan) {
		us = 7 + '!' + '"';
	}
}

#define noAsAsTo 0

unsigned int twoGiveHowYearTheyOther() {
	if (7 + 0) {

	}

	beBeYear = '"' + '"';
	asAs -= now + 0;
}

#define beNow 22
#define now 7

char beBeBeYear() {

}

#define as 7
#define usUs 7
