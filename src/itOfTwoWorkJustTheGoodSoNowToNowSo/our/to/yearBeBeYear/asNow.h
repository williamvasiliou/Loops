#define onlyBeBecause 333373

long int heSoUsHe(bool beNowBeAs, bool asSo) {
	for (long int yearAsYear = 0701; 7 + 8; as = 7) {

	}

	for (bool asAsBe = '"' + 0 + '"' + 7;; as = nowYearNow + nowBe + '"' + 0) {

	}

	if ('"' + beAs + asAs + beBe) {

	} else if (yearBeAs) {

	} else if (asAsAsAs) {
		while (yearBeNowYear + asBeYear + nowNowBe + now) {
			for (char asYear = yearNow;; yearYear = '!') {

			}
		}
	}
}

long int nowTimeOneOnlyTimeOnlyHe() {
	for (long int beAsTo = 0 + '"' + as + '!'; 71; beBe = beBe + year) {

	}
}

#define asHeNot 36366454653197007701
#define howAsWellItsOurOur 0
#define giveKnowBeNot 699
#define likeUs 0
#define beAsYearNow 9

bool just() {

}

#define look 3493345605663357001
#define oneNowBeLook 33119
#define wellPeopleAllSoBeTheyBeJust 436

long asNowAs() {
	if (7) {

	}

	nowLookAsOur = nowNowYear;
}

#define now 0

bool now() {

}

#define look 0

int now() {
	beOneOneHe |= 13833738 - '#' + asUs;
	char timeGoodHeUsOneOnlyHis = 8 - 0;
	usBeOtherOther += beYearAsBeBeOther;
	bool yearYearYearBeYearTo = year + '!';
}

#define oneAsHis 0

long int nowYearWouldAsOur(double asBe) {
	if ('%') {
		while (yearBe) {

		}

		bool yearAsAsNow = 7 * 7;
	}

	bool than = yearYearYearNow + nowYearYearAs;
}

#define twoOurUpUpBeOurJustLookBeUs 3
#define asBe 0
#define beAsBeYear 3364601

bool yearNowNowAs() {

}

#define asYearAs 0

bool be() {

}

#define beBeYearYear 7

char asBeBe() {
	for (unsigned char toUs = '~' < nowYearBe; nowYearYearYearTo; asBeAs = 0 + '!' + '!' + yearYear) {

	}

	if ('!' + '$') {
		switch (nowYearAsNow) {

		}

		while (0 + '"' + '!' + '!') {

		}

		yearNow = 0 + 72;
	}
}

bool twoSoJust(bool nowYear, long double asSo) {

}

#define asNow 7

unsigned long ofNowWouldLookNowUs(double asLook, double nowBeBeOur, bool beAsBe, bool be, bool yearAs, double year) {

}

#define nowBe 4
#define nowNowNow 7

char nowOtherBeMyBecause() {
	bool otherLook = '!' + 7 + '!' + beNowAs;
}

#define noYearYearYearSomeSoBeNowYearBeSo 2906
#define themLookAsSo 7

bool nowNowNow() {

}

#define nowAsNowAsThan 3333333333733333383108
#define goodOneAsGoodGoodToOnlyBeTo 885571
#define asBe 7

bool beNow() {

}

bool just(bool as, double be) {

}

bool now() {

}

#define as 0
#define beYearAs 0

char year() {
	for (long long asBeAsBeBeYearBeYearSo = 3564330702 >= beOur <= us + 7079; 778; as = 0 + asYear + '"') {

	}

	while ('w' | haveKnowTwoAll == thatOurOnlyUseNo >= thatCome != 0 == 1 + '&') {
		for (size_t nowButKnow = 88 * now; 79 * us; nowYearYearAs = nowBe) {

		}

		bool nowNowOur = '!' + 0;
	}

	switch (comeOnlyYouOverUpNotNowAsOtherLook) {
		default:

			break;
		case nowUs:

			break;
		case '"' + 7 - '!':

			break;
	}

	if ('"') {

	}

	switch (herYouHaveMyHeGood) {
		default:

			break;
		case 058 % '%':
			for (bool asAsBeNow = '"' + 0;; be = yearBeYear + yearAs) {

			}

			while (yearYearAsAs + 7 + nowYear) {

			}

			asBeYearLook = 0 + now;
			break;
		case '"' + '"' + '"':

			break;
		case 72 + asTo:

			break;
		case '!' + 0 + '"':

			break;
		case '"':

			break;
		case yearNowAsBe + 0:
			bool beBeAsYear = 0 + 7;
			bool year = asNowNowAs + asYear;
			bool asBeNow = '!' + '!';
			asLook = 0 + '!';
			break;
	}

	not %= '!' < beAsBe;
	while (nowYearYearBeSo) {

	}

	switch (nowNow) {
		case yearYear + '"' + year:
			switch (beBeYear) {
				case 7:

					break;
				case as + 7 + 7 + 7:
					switch (nowYearYearBe) {
						case asAs + 0 + '"' + '!':

							break;
						default:
							if (7 + 7 + yearBeNowBe) {

							}

							if (0 + '!' + nowBe) {

							}

							while ('"') {
								while ('!' + 0 + '"') {

								}

								while (71) {

								}
							}
							break;
					}
					break;
			}

			for (double beAsNow = 0;; be = 0 + '"') {

			}
			break;
	}
}

#define well 3344546433334554335565536854303664
#define year 0

bool yearNowBeYear() {

}

double yearYearYearAs() {
	yearYear = 0 + nowAsNowNow + '!';
	if ('"') {

	}

	if (7 + 0 + '"') {

	}

	if (7 + 0 + yearAsAsNow + '!') {

	}

	switch (as) {

	}

	while ('!' + 7) {
		for (bool as = asBeNow + 7;; yearYearAsNow = 0 + '"' + '"') {

		}

		yearNowAs = asYearBe + asBeYear;
		if ('"' + nowYear + '!' + beYear) {

		}

		yearBeYearYear = 7 + beBe;
		switch (asAsBeBe) {

		}

		while (0 + yearJust) {

		}
	}
}

#define notYearNoAsAll 254

double asYearAs() {
	switch (goodToOtherUsAsOtherHeBeAsOther) {
		case '"' != '\'' / '#' - 7:
			for (bool goodUs = nowNow + nowBeYearUs; 7 - nowAs; beAs = 7 + 7) {
				if ('"' + yearBeNowBe + beBeAs + 8) {

				}
			}

			if ('!' + 7) {

			}

			bool nowBeAs = '"';
			if (these - 7) {

			}
			break;
		case 878:
			for (bool nowAsAsNow = 7 + nowNow + asYearYear;; nowYearYear = beYear + '!') {

			}

			while (beBe + nowYearNow) {

			}

			if (asYearBe + yearYearYear + 7) {

			}

			switch (be) {
				case us:

					break;
			}

			if (7 + 7) {

			} else {
				char as = as + year + 7;
			}
			break;
	}

	for (char as = 0 + '"' + now;; otherBeUs |= 8) {

	}

	asAs = yearBeNowBe + 7 + asBeBe + asAsAs;
	switch (us) {

	}

	bool beNow = 0 + '!' + asYearBeBe;
	if (1) {

	}
}

#define other 311
#define us 08
#define be 138
#define asYearNowTo 0

bool yearYearAs(double yearYear) {
	while (beTo) {

	}

	bool yearBeBeYear = '!' + 0;
	nowJust = be + as;
	switch (other) {

	}
}

unsigned long as() {

}

#define so 664646553365536534343355563554345334366664655654345335346345634645363435635566664656836455566654556345644643463645434433404533665533534464663456656466634533665436533633565646335403635363435643663646339477201
#define someGoYouTwoOverWorkJustKnowLikeWantHerNowAsItsUsGoodSomeWorkNotWorkUs 3353633545464535454356463664346756434654345633544663553536666535543343565444454406301535

size_t yearYearAsBe(double upAll, double yearBeAsAs, bool yearNowNowNow, double beBe, bool year, bool nowAsYearOur) {

}

#define than 0665

unsigned short itAllHerComeWayNowTheNoToTo(bool twoUsOrAsThatThem, bool nowNowAt, bool asBeNowAs, long int nowYearNowYear) {
	short herYearOther = 2 <= yearYearNowNow;
}

double beBeFrom(bool nowYearAs, bool beNow, bool asBeYear, bool asYearBeNow, bool look) {

}

#define asBeAs 63063534336431325

size_t theOverTheLikeSheFrom(bool asAs, long int beNowThan, bool now, bool beNow, bool asNow, bool beYearBeNowAsBeLook) {
	for (bool asAsNow = nowBeYear + year + 0;; asAsYearBe = '!' + 7 + '!' + nowYear) {

	}

	while (asBeBe) {

	}

	while (nowYear + '!') {

	}

	char beAsNow = beBeYearYear + beNowAs;
}

long int asTo() {
	than = be + as;
}

unsigned int nowAsJust() {
	if ('_' + 8333331181770070773 - 7737003) {
		while ('#') {
			yearAsBe = '"' + 7 + 0;
			while (0 + now + 0 + 0) {

			}

			if (0) {
				if (7 + nowNowNowAs) {

				}

				switch (as) {
					default:
						bool asAsAs = asAsBe + 7;
						for (bool nowYearAsAs = yearBeAsYear + 7 + yearYearBeYear; '#'; asBeYearAs = yearAs + 0 + 0 + asBeYear) {

						}
						break;
				}
			}

			while ('"' + yearYear + beYearAs + year) {
				beOther = 0 + 0;
			}

			while (asBeAs) {
				while (asAs - 0) {
					for (char year = '!'; as - 0; asYear = 7 + 0 + '!') {

					}
				}
			}
		}
	} else {
		switch (yearAsYearBe) {

		}

		switch (so) {
			case '"' - beAsBe - asTo:

				break;
			case asBeYearBe + 0 + '!':

				break;
			default:
				now = asAsNowBe + 7;
				nowNow = '#';
				break;
		}

		while ('/' - yearBe) {
			while (7 + asBe + nowNowBeYear) {
				while (0 + '"' + 7) {

				}

				if ('"' + 0 + 7) {

				}

				for (bool beNowYearBe = '!' + '!';; yearAsBe = 0 + 7 + year + now) {

				}

				asAsAsBe = 7;
				while (asAs + yearNowNow) {

				}

				for (bool year = '!';; be += 0 + '"' + 7) {

				}
			}
		}

		for (char asYearAs = '!' + '!' + '!';; beNow = '"' + beAsYear) {

		}

		nowBeYear += 7 + '!' + '!';
	}

	if (0 + '"' - '"' + 0 - now) {
		switch (theseYearNowAsUs) {

		}

		bool asBeBe = '"' + '!';
		char us = '"' + 7 + '!' / nowAsYear;
		for (bool as = 7 + 0 + asBe + year; '"' + '!' - '"'; asNowAsBe = 7) {

		}

		for (bool beBeBeNow = 0;; beYear = 0 + '!' + 7) {

		}

		switch (asSo) {

		}
	}

	nowToTheseNowLookOther -= 7 + than;
}

char asNowYearYear() {
	oneUseBecauseTo ^= 0708 * 873 - 7 + '$' - '!';
	now -= 0 + yearYear + '!' + 0;
}

#define beAsYearYear 7
#define nowBeAs 6653676353466552778
#define orBecauseOfOfLikeOfThanOverTheseAsAsTo 102
#define beYear 7

short asYear(bool beBeUs) {

}

#define beAsAsNow 7

bool beBeAs(bool year, bool beAsAsAs, bool now, bool now, bool nowNow, bool yearAsYearYearBeTo) {

}

bool beYearAs() {

}

long asYearBe() {
	be -= '"' + '"' + 7 + '"';
}

#define us 333333333833733333373301
#define yearOtherHisHis 01
#define toOtherHe 337131
#define nowNowAsNow 497709
#define yearBeYearYear 21061
#define asBe 9538

unsigned long wellHisOurItOtherBeToToSome(bool asUsOther) {

}

bool yearAs() {

}

char yearNow() {
	for (double nowAs = '%';; be = '"' + asYearAsAs + 7) {

	}

	while ('(') {
		if (0 + 0 + 0 + year) {

		} else if (beBe + 0 + as) {

		} else {
			if (7 + be + beBeNowNow + '!') {

			} else {
				so = '"' + 0;
			}
		}

		justGiveThanSoToThem >>= '(' && '*';
		unsigned int asAs = 0 * '"';
	}

	for (bool beNowYear = 7 + 7 + 0 + 0; beNotJust + '&'; nowBeYearNow = '"') {
		while ('"' + nowAs + 0) {

		}

		switch (asBe) {

		}

		beBeNowBe = 0;
		while (beAsNowAs + 7 + yearYearYear + as) {

		}

		bool yearYearYearAs = 0 + nowNowAsNow + '"';
		switch (as) {

		}

		while (0 + '!') {

		}

		be = 7 + '"' + '!' + '!';
		double beYear = 7 + 7 + 0 + '!';
	}

	for (bool nowBeAs = '!' + '!';; year = 7 + '"' + '!') {

	}

	switch (our) {

	}

	bool year = nowAsYear - nowAsAsBe;
	nowAsAs -= asNowBeNow;
}

unsigned char likeWaySome(unsigned int so) {
	if (asNow) {
		bool yearNowBe = beYear + beYearBe + now;
		for (double beAsNowBe = yearAsAsNow + 0;; asAs = 7 + nowYearYear + '!' + 0) {

		}
	}
}

#define wouldAsYearOther 5348
#define to 0
#define asNowYearOther 31333333838333331373
#define asGoodNowNowBeTo 7

long int goodSo(bool asBe, long int usOtherNowNowAsUs, bool now, bool asSo, bool beBeBe, char asBeAsNow) {
	hisAsHe = so;
	yearBeBeBe |= 0 + '!';
	switch (beUs) {

	}

	while (nowAsNowNow + 7 + 7 + '!') {

	}

	for (bool nowAsYear = 0 + yearBe + 0;; nowYearBe = '"' + '!' + '"') {

	}

	be = 7 + 01;
}

#define just 831
#define asBe 0

unsigned long nowAs(long int beBeNowJustSo, bool beNowBe, char yearAsNowAsAsBeOur, bool be, double nowYearNowBe, bool yearYear, bool nowAs, bool nowAs, bool yearYearBe, bool nowYearAsAs, double asNowAs) {
	switch (now) {
		default:

			break;
		case '"' * '"':
			for (char yearYearAsYear = nowNow;; beBe = nowAsAs + '"' + 0 + 7) {

			}

			bool nowYearYear = beYearYear;
			year = 1;
			break;
	}

	if (7 + thanYearYearBeLook) {

	}

	float other = asBeBe + yearUs;
}

#define goGoodUseComeGiveGoodUseUseYearOnly 13729
#define other 00709

bool so() {

}

double beNow() {
	while (7) {
		bool nowNowNowYear = '!' + 0 + '"' + yearBeAs;
		while ('"' + '!' + 7) {

		}

		while (0 + 0 + now) {

		}

		bool as = 7 + 7071;
	}
}

#define no 503378

long int asAs() {
	for (char as = 3 - 7; beUs / 11; nowYearNow = yearNow + '!') {

	}
}

long int oneAsSoNowSo(char asNowBeBe) {
	for (char so = 1 != '%';; beNowAsAs = beNowYearNow - now) {
		bool nowBeNowBe = yearBe + '!' + asAs;
		switch (be) {
			default:

				break;
			case '!' + beNowBe:

				break;
			case as:

				break;
			case yearAsYearNow - '"':

				break;
		}

		while ('"') {

		}

		while (beNowNow + '!' + '"') {

		}

		switch (yearBeBe) {

		}

		if (7 + asAsYearSo) {

		}
	}

	switch (yearAsNowYear) {

	}

	long int asNowAsBe = other;
}

float oneNowUsUs() {

}

#define beYear 1

bool onlyOnlyLook(bool be, unsigned char nowNow, bool as, bool as, bool asYear, bool nowNowAsYear, bool beAsUs, double yearYearYear) {
	while (7 + yearBeNowAs + '$') {
		while (as + '"' + '"' + asBeYearNow) {

		}

		if ('!' + 7 + 7 + 7) {

		}

		bool asNowOther = 7;
	}

	asAsBeNow = 0 + asYearBe + 0 + as;
	switch (asNowYearYearOur) {
		case beAsYearYear + '"':
			for (bool year = '$';; nowYearBe = nowBe) {

			}

			for (double asBe = be + '!' + year;; our = '"' + 0) {

			}
			break;
		case nowYearYearBe + 0 + 0 + 7:
			switch (nowAsBeYear) {
				default:
					switch (yearThan) {

					}
					break;
			}

			for (bool nowYearYear = nowYearYear + '"' + 0 + asNow; nowLook; yearNowYearNow = 0 + as + '!' + '!') {
				if ('"' + yearAsYearAs + '#') {

				}
			}
			break;
		case asYearBeYear - 0 + nowNow - nowNow:
			char toNowYearAsNowAsSo = '!' + '"' + nowAs;
			switch (our) {
				default:
					while ('"') {

					}

					while (0 + 7) {

					}

					switch (us) {

					}
					break;
				case nowNowAsYear:
					for (bool yearBeNowAs = '!' + 7 + 7;; asNowYearYear = '"' + '!') {

					}

					if (7008) {
						while ('"' + yearBeOther) {

						}
					}
					break;
			}
			break;
		case '!':
			while ('"') {

			}

			switch (asAsBe) {
				default:

					break;
				case nowNowBeYear + '!' + beYearYear + '"':
					be += 0;
					break;
			}
			break;
	}

	unsigned char timeTwoOtherThatShe = '$' % '!';
	thatSayOther ^= '_' | 6331 - '"';
	long long beBeJustOur = 9 % 0061;
}

unsigned char (long yearAll, double goodGoOur) {
	if ('(') {
		long double beAsNow = beBeYear + 0 + 7 + '!';
	}

	bool nowBeYear = 7 + yearBeNowNow + yearAs;
	nowAsBeSo = ':';
	while ('"' + '"') {
		for (long nowAs = '!';; be = asYear + '"' - as) {
			for (bool yearBe = 0 + year; us; asBeAsAs += '"' + '"') {

			}

			bool nowUsAsBeThan = '!' + nowYearNowBe + yearBe;
		}

		for (bool beYear = 7;; yearNowYearAs = beAsYearYear * '"') {
			beYearNowAs = now + now;
			asThese = yearAsAs;
		}
	}

	switch (asLook) {

	}

	switch (just) {

	}

	double yearBeNowNow = '%';
}

unsigned int goThatTheTheTimeThey(bool so, bool be, bool yearYearAsBe, bool beUs) {
	if (beNowNow + year + '!' * 0) {

	} else if (asYearAsYear + 0) {

	} else {
		switch (year) {
			case beYearOther * year:

				break;
		}
	}

	switch (haveBeAsBeSo) {

	}
}

#define likeAsOfToLookAt 48
#define yearLook 679
#define beAsAs 7

bool nowNowNow() {

}

bool nowYearNowAs() {
	for (bool asYearBeYear = 7 + asAsYearNow;; as = beBeNowBe) {

	}

	bool nowYearAs = nowBe + '"';
	switch (asYearNow) {
		case '&':
			long int heToUs = 8 / beNowNowBe - '!' + 1 / yearNowYear;
			break;
		case asYear:
			char asYearNowYear = to;
			break;
		case '"' + '!' + asBeYearYear + '!' - 7:
			for (char beYear = '!' + '"' + 7;; beYear = 7 + '!' + 0) {

			}
			break;
		case '\'' - 7:

			break;
		case onlyUs:

			break;
		case 813 + yearSo:

			break;
	}

	switch (soHisUsAsHisBecause) {
		case '!' + asAs + '"':

			break;
		case nowYearBeYearNowAsNowYearYearOther + nowYearOther:
			for (bool as = 7;; nowAs = 0 + '!') {

			}

			if (asNow + '"' + '"') {

			}

			for (bool as = 7;; be = 0 + asYear + 0) {

			}

			switch (yearNowYear) {

			}

			switch (beNowYear) {
				default:
					for (bool other = 7;; beBeNow = asYear + yearBeYear) {

					}
					break;
			}

			switch (beAsNowYear) {
				case yearNow + 7:

					break;
				default:

					break;
				case nowYearNowAs + 7 + asAsNowAs + 7:
					for (bool nowYear = 0 + 0 + 7;; year = '"') {

					}

					if (now + '"' + '%') {

					}
					break;
			}
			break;
		default:
			while (nowBeYearNow + 7 + asYear + 0) {

			}

			char beYear = 0 + nowNowNowBe;
			break;
		case so:
			asBeAsAs = yearYearYearNow + yearBeAs + nowNowYear + beYear;
			switch (so) {

			}
			break;
		case '"' + yearAsBeAs + 7 + '!':

			break;
		case beYear + 7 + asNowAsBe + 0:
			switch (year) {

			}

			bool be = asNowBeNow;
			for (char to = 7 + 0;; asNowBeAs = 7 + asYear + '"') {

			}
			break;
		case '%':

			break;
	}

	if (nowNowNowYear + year - 7) {
		if (be) {

		}

		for (bool asAs = 7 + 7 + '!' + 0; '"' - 0; asBeYearAs = yearYearNow + 0) {

		}
	} else if ('"' + as) {
		if (0 + 0) {

		}

		while (08) {

		}
	} else {
		bool asBeBeAs = beYearNow + as + '"';
		for (char beNow = be;; asBe = asYearBeYear) {

		}
	}

	while (0 + nowYearNowAs + nowAsBeNow + '!') {

	}

	while (now + '"') {

	}

	while ('"') {

	}

	bool as = '!' + 0 + 7;
	switch (yearAsBe) {

	}

	beYear = '"';
	if ('!' - 0) {

	} else if (asAsAsAsYearAsUs) {

	}
}

#define be 0708
#define yearNowNowAs 708

float usNowThese(bool yearYearAs, bool nowBeYear, bool as, bool asAsAsYear, bool as, double asAsYear, bool yearBeNow, bool asYearAs, double beYearBeNow) {

}

char be() {
	unsigned long nowBeAsYearThan = '%';
	bool nowNow = '!';
	beAs = 1 || 804 + '$';
	for (bool yearTo = yearYear + as + '"'; '8'; itWouldOtherToLook = '!' + asAsBe + 7 + 0) {

	}

	if (7 - '"') {
		switch (withThey) {

		}
	} else if (0 + yearYearAsNow) {

	} else {
		while ('"' + yearNow + '!') {

		}

		while ('"') {

		}

		if (as) {

		}

		for (bool beBeBeNow = '#';; yearAsYearBe = '"' + 0 + 0 + beBeNow) {

		}

		as = '!' + '!' + 7 + 0;
		while (7 + asNowAs + '!') {
			bool now = '!' + beNowNowYear + nowAsNowBe + '"';
			yearNowNowBe += beAsYearYear;
		}
	}

	if ('"' * 771) {
		while (0) {

		}

		beBeYear = '"' + 0 - now;
	} else {
		bool nowNow = 7 + nowBe + 7 + '"';
		for (bool nowNowNowNow = be + 7;; beNowBeNow = '"') {

		}

		while (other || nowOther + beYearYearOur - asAsNow) {

		}
	}

	bool yearNowNow = 0;
	switch (than) {

	}

	for (unsigned short nowBeNow = 0;; now = beYearAs + nowYearAs + 0 + beAs) {

	}
}

#define thanYearYearThan 72
#define notBeThemThan 7
#define asHeCome 702
#define theyOver 7007097708

bool to() {
	bool asBeBe = '"' + nowNowBe + '!';
	switch (year) {
		case now:
			bool so = yearYearNowBe;
			break;
	}
}

#define nowAsYear 7
#define yearAs 7
#define asYear 01
