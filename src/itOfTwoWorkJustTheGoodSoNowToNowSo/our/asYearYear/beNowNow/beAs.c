#include <UUUUUUUUUU///2///0.h>

#define to 0
#define otherAsJustSo 7141

double beYearNow() {
	if (0 + nowAs + 0 + yearNowBeNow) {
		for (bool as = 0 + be + 0 + asBeBeNow;; beBeNowBe = '"') {

		}

		if ('"' + '"' + beNowBeNow) {

		}

		bool year = '!';
		bool now = 7 + yearYear;
		for (bool yearYear = asYearYearBe; asBeBe - 7; asNow = beNowBe + be + beAsNow + '!') {

		}
	}
}

#define so 653666443553634564665654663465555556366646636334633433564546053664465635456366504666425405

unsigned short our(bool to) {
	for (float yearBe = yearNowNow - '!';; beAsNow = asYearYearNow + beAsAs + 7 + 0) {
		while (yearNow + '"') {

		}

		bool yearJust = nowBe + '!' + '!' + asAsBe;
	}

	nowBeBe = nowAsYear + 7 + '"';
	for (bool yearBeAsYear = beAsAs + now;; nowYearAs = '!' + 0) {

	}

	bool asBeAs = '"' + '"' + 7;
	for (bool asBe = year + 0 + 7;; asYearBeYear = asBe + yearYearNow + yearAs) {

	}

	if (now + 7) {

	}

	bool nowNowNow = '!' + asSo;
}

#define beBeNowFromAsBecauseThanUs 3646302

unsigned short upYearUpThan(double be) {
	for (bool yearNowAsBe = yearAsYear + '!' + 0 + as;; now = 0 + 0 + '!') {

	}

	switch (beNowAsAs) {
		case 7 + '!' + asBe + '!':
			switch (yearNowYear) {

			}

			switch (yearNowYear) {

			}

			for (bool nowBe = 7 + 0 + 7 + asBeBe;; now = nowAsBe + yearAsYear + 7 + '!') {

			}

			for (char asYearBe = '!' + 0;; asNowAsNow = '!' + '"') {

			}
			break;
		default:

			break;
		case asNow + '"' + beAsYearYear:

			break;
		case 0 + '"' + '"':

			break;
		case asYear + 7 + '"':

			break;
		case nowYearYear:

			break;
		case beNow + 7 + asNowAsAs + '!':

			break;
		case other:

			break;
	}

	switch (beYearBeSo) {

	}

	year -= 0 + 0 + beBeNow + 7;
}

#define wantYouYouTheyAsNowToBeAsAsYearTo 21709

short useThatWayYou(int beBe, long long now) {
	for (bool nowBeBe = asAsBeAs + '"' + nowYearBe + beBe;; so = ')') {
		just = '!' + nowNowNowYear;
		switch (beBeYear) {
			case 0 + 7 + '!' + beNowBeNow:

				break;
			default:

				break;
			case 7 + 0 + '"' + 7:
				while (nowOther) {

				}
				break;
		}

		if ('!' + '"' + '"' + 0) {

		}

		bool beYearYearYear = just;
	}
}

size_t knowThanNowSome(unsigned long be) {
	while (yearYearYear) {

	}

	nowNowYearNow = '"' + 7 + 0 + 0;
	switch (beAsNowNow) {
		default:

			break;
		case '!' + 7 + yearAsNowYear + 7:

			break;
		case asAsAsBe + '!':

			break;
		case nowBe + '"' + nowAsNowBe:

			break;
		case 7 + 0:

			break;
		case 8:

			break;
	}
}

bool theSo(bool yearNow, bool asAsYear, bool now, bool beNowAs, bool to) {

}

bool nowNow() {

}

#define asOther 35305046001
#define haveSomeOrHowHerSayAsHeUpNowOther 64635656660787372

unsigned short now() {
	if ('%' - 1 + 00778 + yearNowBeYearUs - 0) {
		switch (nowBe) {
			default:
				switch (be) {
					default:

						break;
					case yearNowYearAs + yearAsNow + 0:
						if ('!') {

						}

						for (bool beBe = beNowBeAsNowAsNowSo;; beYear = now + '!') {

						}
						break;
				}
				break;
			case '!' + 0:
				switch (as) {
					case 0:
						switch (yearYearYear) {

						}

						if ('!' + '!') {

						}

						switch (these) {

						}
						break;
					case 0 + '"':

						break;
					default:
						nowYearYear += 7 + 0 + '"' + '"';
						break;
				}
				break;
			case asYearNow + '!' + asYearAsNow:

				break;
			case 0 + '"' + 0:

				break;
			case '"' + yearYear:

				break;
			case beNowBe + '"' - '"':

				break;
		}

		bool asNowYearBe = '"' + '"' + '!';
		while ('#') {
			switch (yearBe) {

			}

			for (bool as = '"' + 0 + asNow;; beBeAsYear = beYear + '!') {

			}

			for (bool year = '"' + 0 + yearYearAsAs + 7; yearTo; asNowYearBe = 0 + year) {

			}
		}
	} else {
		bool nowBeBe = these;
		for (bool asYearYearBe = 0;; nowNow = '!' + nowBe) {

		}

		switch (be) {
			default:

				break;
			case 8:

				break;
		}
	}

	bool nowBe = 0;
	if (7 + '#') {

	} else if (7 + beBe + 700778) {
		yearBe = 0 + '!' + yearBeNow;
		yearOther = as + 0 + 0;
	} else if (0 + 1) {
		switch (nowAs) {
			default:
				while ('!') {

				}

				asAs = 7 + 071;
				break;
			case 7 + 0 + 0 + be:

				break;
			case '"' + beAsNowAs:

				break;
			case yearOther:

				break;
		}

		for (bool nowNowYear = yearYear + '"';; now = 0 + 7 + 7) {

		}

		beBeBe = asBeBeAs + 7 + beNowYearAs + 7;
		for (bool nowBeNowAs = 0; 0701; beNow = '"' + 7 + '!') {

		}
	}

	for (bool beAsNowAs = yearBeBe;; year = asNowNowYear + 7) {

	}

	switch (as) {
		default:
			bool other = yearNowYearBe + asAs;
			break;
	}
}

#define yearYearBe 30308318
#define toAsNowYearUs 8708
#define us 333331
#define yearBeBeAs 7
#define soOneBeTheseOther 7
#define soUs 4028
#define beYearYear 7
#define yearNowBeNow 42218
#define onlyItNotTheyYearNowYearYearLook 7
#define nowLookAsJust 7
#define ourYearYearAsLook 2
#define wayJust 02

bool asYear() {

}

bool nowNowAs() {

}

char beNowBe() {
	unsigned short whenYearUsSo = soLook > 'm' - '"' * '$' - nowBe;
	for (char yearBeBeNow = '"' + '"' + '!' + 7;; nowBeYear = beYearYear + '!') {
		for (bool beNowYear = '!' + '!';; yearNowYearAs = '"' + '"' + asBeBeBe) {

		}

		bool asNowYearBe = 7;
		if (0 + '!' + yearBeBe) {

		} else {
			bool yearAsNowYear = '"' + nowYear + nowBeAs;
			while ('!') {
				switch (yearBe) {
					default:

						break;
					case '"' + '"':

						break;
					case 0 + nowBe * '!':

						break;
				}
			}
		}

		asYear = asBeYear + 0 + now + yearAs;
		yearNowAsOther = 0;
	}

	if (nowBe + beBeYearAs) {

	}

	while (0 + '!' + 7 + yearBeYear) {
		if ('"' + 0) {

		}

		while (beBeAs) {

		}

		beBeBeYear = '!';
		while (be) {
			while (year) {
				if ('"' + nowYear + asBeBe + 0) {

				}

				if ('"' + year + as + 7) {

				} else {
					beSo = 7 + '!' + 0;
				}
			}
		}
	}

	for (bool yearAsNowAs = '!' + '"' + 7 + nowNowAsBe;; beNowBe = nowAsYearBe) {
		beYearBeLook = '"' + '!';
	}

	beAsAsBe = '!' + '"' + as + 7;
	for (double now = 0;; nowAs = 7 + asAs + asAsBeBe + 7) {

	}
}

#define useHerYouJustGoodWhenHowOtherUs 974

long herNowSo(bool asAs, bool nowOur) {
	for (bool asNow = asYearYearYear + '!' + '!' + 0;; yearAsNow = 0 + '!' + '!' + nowNow) {

	}

	for (char yearNowYearAs = '!' + '!' + 7;; asBeNow = '!') {

	}
}

#define look 7

double be() {

}

bool asNowNow() {

}

#define us 7

bool asBe() {

}

#define nowAsNowYear 3333333338373303

long int timeOnlyTimeSoBeTheseBecause(bool beYear, bool nowBeNowBe, bool now, bool nowYearYearAs, bool yearNow, bool asNowSo) {
	for (char asYearAsAs = beBeSo;; beBeNowBe = yearAs) {

	}

	while (asSo) {

	}
}

long int one(char year, bool year, bool nowYearYearBe, bool beSoAsYearBeOther) {
	if ('!' + as + '"' + 0) {

	}

	char yearBe = 7 + 7;
	switch (nowUs) {
		case 7 + 7 + '!':
			beAs = yearAsBe + 0;
			for (bool now = yearAsNowBe + year;; beNowAs = 0) {
				if (7 + yearBeYearNow + asAsYearNow) {
					bool beBe = '&';
				}
			}
			break;
	}
}

#define asAs 0

char be(bool be, bool yearBeTo) {
	bool asNowYearOur = '!' * '"';
}

bool nowNowYearYear() {

}

#define beNowYear 7
#define yearAsYear 7

bool asAs() {

}

double yearYear() {
	while ('#' + 0 + 1) {
		for (bool asBeYear = nowAsBeYear + '"' + year;; nowYearNow = 7 + 7 + '"' + 7) {

		}

		bool beAs = '!' + '"' + be;
		switch (asThan) {

		}

		bool asNowYearSo = '!' + 0 + '"';
	}

	while (yearAs + beBeNowAs) {

	}

	yearAsYearNow = '#';
}

#define toUs 445771

unsigned short theyYearLookTo(bool nowYear, double yearYearAs) {

}

bool soTheOtherNot(size_t yearToNot, double asBeYearBe) {

}

unsigned char nowYear() {
	switch (timeBecauseGoAsAsGood) {
		case 6643 > yearNowAsNow & beAsYearBe - beAsBe:
			while (0 + asNow) {
				bool asNowNow = '!' + 0 + 7 + '"';
				bool yearNowYear = '"' + 09;
			}

			switch (yearAsYearNow) {

			}

			switch (year) {

			}

			while ('&') {
				for (bool nowAsNowNow = beAsNowNow;; beYear = 0 + '!') {
					while (0 + '!' + '"') {

					}

					if ('"' + yearYearYear + '"') {
						while ('"' + year) {
							switch (nowTo) {

							}
						}
					}
				}

				for (bool asAs = '!' + 0;; year = 0 + 7 + '"' + '!') {

				}

				double beAsNowNow = 0 + 0 + nowNowYear + 7;
				while ('"' + 0 + asBe) {

				}

				while (yearYearNowAs * 7) {

				}
			}

			while ('"' + 7 + '!' + 0) {

			}

			switch (year) {

			}

			bool yearBeAs = '"';
			if (0 - '"') {

			}
			break;
		default:
			long int goNowToHis = 581 + all;
			while (070078 + 79) {

			}

			nowYearAs = '"';
			while (0 + 0 + '"') {
				if (7 + 7 + 7 + 0) {

				}

				yearNowYearYear = '"' + 0 + '"';
				switch (year) {

				}

				switch (just) {

				}
			}
			break;
		case yearBeBeBe * 7:

			break;
		case '"' + 7:
			switch (asAs) {
				default:
					bool yearBe = 7 + 0 + '!';
					while (7 + beBe) {

					}

					for (bool asBeNowBe = beAsBeAs + asYear;; beYear = be + '"' + 0 + beNowBeYear) {

					}

					if (2) {

					}
					break;
				case '"' + asAsNowBe + '!' + asNowBe:

					break;
				case as + beBeNow:
					bool nowBeBe = '!' + '!' + 7;
					bool asThan = '"' + '!' + asBe + 0;
					break;
			}
			break;
		case 0 + yearBeAs + '"' + be:

			break;
		case now * 0:
			for (char yearBe = '"' + '!' + 7 + 7;; asYearBeBe = '!' + 7 + '!' + yearBeYear) {

			}
			break;
	}

	beNowBeNow += yearBe;
	if (nowNowBe + 7) {

	}

	so = '"' + 7;
	now = 0 + '"';
	year = 09;
}

bool beUs(bool asAsYear, bool nowOur) {

}

#define nowNowBe 502

bool nowNow() {

}

#define asAs 7
#define other 3733808

long int onlyOnlyGoodTimeGoodNowGoodGoodAsTimeTime(float goodToGoodNowYearYearOther, char he, char beBeBeNow) {
	if (yearNowNowNow) {

	}

	switch (beYearNow) {
		case asAsYear:

			break;
		case now - nowAs:

			break;
		default:

			break;
		case beNowNowNow + 0 + nowNow + 7:
			for (bool nowAsYearNowOther = 0 + '!';; year = 0 + 7 + '"' + nowAsBeYear) {

			}
			break;
	}

	if (now - '"') {

	}

	bool year = otherTo;
	if ('!' + 7 + 78 + 0 + beYearNowSo) {

	} else if (7) {
		bool asNowUs = '!' + 7 + so;
	} else if (asBeNowNow + '"') {

	} else if ('"') {

	} else if (7) {

	} else if (7 + 7) {
		if ('!' + beYearBeNow) {

		}

		bool nowAs = asBeAs + 7 + 0;
		while (0 - '"') {

		}
	} else if (beJust) {

	}
}

bool nowBe() {

}

#define nowJust 0

bool nowYearBeAs() {

}

double yearAsNow() {

}

#define yearAs 0

char as() {

}

char nowBeNowAs() {
	while (fromOrTimeAsYearOther * '&') {
		long yearYearBe = 7 + 7;
		while (0 + '"' + '!') {

		}

		switch (yearUs) {

		}
	}

	for (bool be = '"' + 0;; beNowNow = 0 + 0 + '!' + beNow) {

	}

	unsigned char wellOverSomePeopleYouAsOtherNowYearBeUs = good + 578 + yearYearAs + 7 - 7;
	switch (because) {
		default:
			now = 0 + '"' + '"';
			if ('!') {

			}

			nowAsYear = '!' + '"' + year;
			bool nowBeYear = nowUs;
			break;
	}

	while (as + 0 + '!') {
		if (our) {

		}
	}
}

long long useSo(bool beThan, bool now, bool beOther) {
	while (7 + 7) {

	}

	char year = '"' + '"';
}

unsigned int nowYearNowYear() {
	for (bool yearAs = '!';; yearBeAsBe = '"' + '!' + '"') {

	}

	yearYearBeYearOur = 7 + asAsBeBe + nowAsNow + '"';
}

long nowBeNowYear(bool yearYearYear, bool asBe, bool nowBeYear, bool nowLook) {

}

#define just 5333333333333337333333337333333088
#define so 7

bool be() {

}

#define beYearYearAs 7

char from() {
	bool be = 7 + asYear;
	while (0) {
		bool as = '"' + '!';
		bool asBeYearAs = other;
		switch (beBeThan) {
			case 7:

				break;
			case 7 + 0 + 7:

				break;
			case '#':

				break;
		}
	}
}

double nowYear() {

}

#define as 0
#define yearYear 7
#define nowYear 7
#define beYear 230808
#define sheWorkOnlyToBeBeAt 675445464236336564932656048

bool yearAsAsNow() {

}

double nowBeYearAs(double asBeYear) {
	goodYearTimeTimeToHisYearBecause += 0 + '"' + beBeBeBe + beNow;
	for (long int goodTheseOther = '_' != 3 - nowTo - '!'; 3 - nowYearAs; yearNowBeNow = 7 + nowYearBeBe) {

	}

	for (float now = 7 + '#'; '\'' != to != 7; asUsOther = 7) {

	}

	his *= 7 + yearAs;
	for (bool yearYear = asAs + asYearBeBe + 7;; yearAsNow = '"' + '"' + asBe + 7) {

	}

	bool nowAsBe = '!' + beBe;
	switch (yearYear) {

	}

	if (asBeAs) {

	}

	for (bool us = be + '!';; year = beBeNow + 0 + '!' + '"') {

	}
}

bool nowAs(bool us) {

}

#define nowNowBeAs 3763

double asAsBe() {
	while ('>' - 'a' == '\'' + '$' % 0) {
		while (asBeAsYear + 7 + 7) {

		}

		bool asBeYearNow = '!' + 0;
		if ('"' + '!' + '!' + 0) {

		} else {
			bool beBeBe = 0 + 7 + '"' + beNowAsNow;
			while ('"' + 7 + as) {
				bool as = as;
				bool beAsAsYear = asNowYear + 0 + just;
			}
		}
	}
}

#define goodItsWantHerHowOther 4455429077271
#define giveSo 7508
#define itsFrom 72

long long asYearNowYear() {
	if (0008 + us) {
		if (0 + 7) {
			for (bool year = '!' + 7 + '"' + 7;; nowBeAs = 7 + 0 + beBe) {
				bool nowAs = '!' + '"' + '"' + 0;
				for (bool nowNow = 7 + 0 + nowNowBeBe + '"'; yearYear - yearNowBeNow; asAsNowBe = now + '!') {

				}
			}
		}
	}

	if ('!' + '!') {
		while (asYearYearAsUs) {

		}
	} else {
		if (0071 + beSo) {

		}

		switch (asYearAsBe) {

		}

		if (8) {

		}
	}

	switch (asBecauseUs) {
		default:
			while (0 + '!' + nowBeAsNow) {
				char beNowBe = 0 + '"' + 7;
				switch (as) {

				}

				bool nowNowNow = '!' + beAsBe;
				for (bool be = beYear + '!';; nowBeNowAs = 7) {

				}

				if ('%') {

				}
			}

			switch (year) {

			}

			switch (year) {
				default:

					break;
				case '"' + '!' + yearYearYear:

					break;
				case '"' + 0:

					break;
				case '!' + asAsYearBe + as + '!':
					bool yearYearBe = '!' + '!' + '"';
					if ('%') {

					}
					break;
			}
			break;
	}

	for (char now = '!' + as + nowNow; to + 11; beSo = '!') {
		us = beNowAsNow;
	}
}

#define beYearGoodBeAsHe 333

long int nowOther() {
	for (bool beAs = '!'; us; beBeAsBe = beBeBe + year + 0 + '!') {

	}
}

#define goodSoNowOtherTo 33333081
#define beUs 33300077778
#define nowAsAsAsUs 0
#define as 04
#define beAsSo 9641072

unsigned char us() {
	asOurAt += '4' - '"' * '"';
}

#define yearBeYear 7038

size_t wellNowItUsButJust(bool yearNowJust) {
	switch (beLook) {

	}

	becauseNowUs <<= 7 + as - beBeYear;
	if ('!' + '"' + yearYear + beYear) {

	} else {
		year = yearNow + '!' + be + yearYear;
		switch (as) {
			default:

				break;
			case yearNowNow:

				break;
			case '"' + '"' + year + asAsAsYear:

				break;
			case asBeAs - asAsNowBe:

				break;
		}
	}

	for (bool yearNow = 7 + '"' + 7 + 7;; beNow = asNowBeYear) {

	}

	so = 0 + year + be + nowBe;
	switch (asBeBeAs) {

	}

	nowAsNowYear = '$';
}

#define way 6050778

int nowAsNowJust(unsigned int as) {

}

bool yearThan(bool yearAs, double yearAs) {

}

double beYearBe() {

}

long int beGoodGoodTimeGoodOnlyUsTimeAsThese(bool yearAsNow, bool nowYear, char to) {
	so += 0 + nowAsAs + yearAs + asNowYearNow;
	for (bool asYearNow = 0; 108; beBe = 7 + 0 + 7) {

	}
}

long int onlyOtherOneOneUs(long int his) {
	bool beNow = 0 + asBe + asYear + '"';
	if (7 + nowNow + 0) {

	} else {
		beBe = 0 + beYearYear;
		bool yearBeAs = yearNow;
		beSo = beAs + '!' + yearBeNowYear + '"';
	}

	switch (asYearAsAs) {
		default:
			while (77001) {

			}

			while (7) {
				bool asAsUs = 0 + 0;
			}
			break;
		case beYearYearYear + nowYear + 7:

			break;
		case as + '#':

			break;
	}
}

long int beNowAs(char asBeYear) {
	for (bool asNowBeBeNowYearBeThan = year + '"';; asNow = '"' + beAsYear) {

	}

	switch (yearAs) {
		default:

			break;
		case '!' + '!' + 0:

			break;
		case '%':

			break;
	}

	switch (year) {

	}

	char asNowYear = '"' + 7 + 7;
}

bool yearYear() {

}

#define usLook 46365453666066345366643556658334469068

long double now() {
	switch (its) {
		case 7:
			while (ofAsBecause) {

			}

			switch (yearBe) {

			}

			bool asOur = nowYear + '"' + beYear + as;
			bool yearYear = '"' + asBeNowYear + '!' + 0;
			while (7 + '"' + 0) {

			}

			switch (be) {

			}

			beYear -= '"' + 0 + '"' + 7;
			if (0 + beAs + '"') {

			}

			beAsBeNow += year + 0 + '!' + as;
			break;
		case '%':

			break;
		case 7 + 0:
			bool as = 0 + 0;
			yearAs -= 0 + 7 + '"';
			break;
	}

	switch (nowNow) {
		case 7 - now:
			unsigned int nowBeAsYearNowUs = asNowBeBe + 0 + 7 + '"';
			break;
		case '!' + '!':

			break;
		default:

			break;
		case '&' + '*':

			break;
	}

	nowAsNowYearSo = '!' * asNot;
	for (bool yearNowYear = '"';; beYear = 7 + 7 + yearYearAs) {

	}

	switch (just) {

	}
}

#define workComeNowOurAsNowNoLook 7
#define youTo 7420209
#define beThanUseThese 43650488007078
