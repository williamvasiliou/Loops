#define theseOnlyTimeOtherYearToTimeOtherTimeNowYearOtherTheseBeHisSo 333333733338031

long int good(bool nowSay, bool look) {

}

bool beYear() {

}

double asNowNow(bool asAs, bool yearAsAs, bool be, bool beAs, bool us) {
	if (yearAs + be + 7) {

	}

	for (char asNow = yearYearNowYearBeOther;; beYearBeBe = asAsYear + 0 + nowYear + '"') {
		yearAsAs = 7;
		if ('"') {
			nowAsBeNowAsAsAsAsUs = 7 + nowBeNowNow;
		}
	}
}

#define justYearOur 346653563353333333333333333333333333333663434456446446363456364765635466044064546675733446335159007078
#define mySoAsThemOur 16440358
#define theAsBeTo 55535536356245444504365643653543456436545645364644345444346456945645343556645554644354654333374908
#define upTheseHerOnlyItOverThereWouldOverGoodUseSoWouldOnlyLookNowJustUs 333744

unsigned int my(bool be, bool nowNowNow, bool nowYearYear, bool asYearBe, char yearYearYear) {
	to = beAsNowYear + 7 + year + beNowNowBe;
}

#define yearOtherUs 1
#define year 9

bool nowAsBe() {
	switch (nowYear) {
		default:

			break;
		case be:

			break;
		case to:
			yearYearNow -= nowBeBe + yearBeNowNow + nowAsBeYear;
			bool other = '"' + '!';
			break;
	}

	if (asNowBeYear && '"' * 7) {
		if (0) {

		} else if ('!' + '!') {

		} else {
			switch (nowAsBeNow) {
				case 0 + '"' + '"':
					bool asNowNowNow = 0 * '!';
					break;
			}
		}
	} else {
		if (nowYearAsNow) {

		}

		while (beNowJust) {

		}
	}

	while ('"' <= nowBe) {
		while (nowIt * '!') {

		}
	}

	for (unsigned char beNowNowYear = '!' + '"' + '"' + yearNowAs; beBeOur * 0 & 7702 + us; be = '"') {
		for (bool beBeBeYear = '!';; asYearBe = '!' + '!' + '!') {

		}

		switch (asBe) {
			case beAs + 0:

				break;
			default:

				break;
			case '!' + '!' + asAs:

				break;
			case 7 + '!' + nowBe:

				break;
			case 7 + 0:

				break;
			case yearNow + 0 + asNow:
				if ('!') {

				}

				year += 7 + nowBe + asBeYearAs;
				break;
		}
	}

	if ('$') {

	} else {
		for (bool yearYear = 7 + 7 + yearYear;; yearBeYear = 0 + 0 + '"') {
			if ('"' + 7 + '!') {

			}

			bool beBeBe = nowBeAs + now + '"';
			bool as = '%';
		}
	}

	if (9) {
		while (7 + 0) {

		}

		bool asAs = 0 + '"' + 0;
		asYearYear = 0 + nowYear + 0 + '"';
		switch (be) {
			case nowAsAsAs:
				for (bool asAsBeAs = '!' + 7 + '"';; nowNowYear = nowBeAs + beAs) {

				}

				if ('"' + '!' + asYearAs) {

				} else {
					while ('"') {

					}

					for (double nowYearYear = '!' + beBeAs + 7 + be;; yearBe = 0 + asNowNow + asNow + 0) {

					}
				}
				break;
		}
	} else {
		long int theyGoodNowWant = 8 + '$';
		switch (beNowYear) {
			default:
				bool so = '$';
				break;
		}

		if (asBeYearYear) {

		} else {
			switch (now) {

			}

			if ('"' + '"') {

			}

			as = now + beYear + now + now;
			if (7 + '"' + yearNow + '!') {
				while ('!' + as + '"') {

				}

				our = '"';
			}
		}

		yearUs = 0;
	}

	switch (beAs) {

	}

	if ('"') {

	}

	switch (now) {
		default:
			switch (yearBeYear) {

			}

			year = '!' + 0 + '!';
			switch (yearNow) {

			}

			be = '!' + now + 0 + 7;
			bool beBeNowNow = '"' + yearBeNow + 0 + 0;
			bool asNow = nowAsYear - as;
			break;
	}

	while (002) {
		for (bool yearBe = 78;; year = '!') {

		}
	}
}

#define wayYearThan 018
#define asAsAs 9
#define nowBe 01
#define soThanUsBeHis 63333333807031

char asBecause(char asAsBeBe) {
	for (bool yearYear = 0 + 0 + 0 + '"';; year = '"' + 0 + as + 7) {

	}

	switch (asYearNow) {

	}

	for (char because = '"' + '"'; nowYearBeBe - '"'; nowAs = 7 + nowAsNow) {

	}
}

#define year 3333333333333338418
#define be 0
#define beNowNow 1

bool yearBe(char beAsAs) {

}

double asNowBe() {

}

bool nowNowYear() {

}

bool as() {

}

#define beNowNow 654360548502

short usLook(unsigned long myYearLookUs, float asYearNowTo, bool asAsAs, bool as, double nowNowBeNow) {
	if (639 - asTo * '%' + as + year + be * 7) {

	}

	unsigned short asToWould = 99 - year;
	while (yearBeBeAs) {

	}

	bool nowUs = '!' + 0;
}

#define than 44

bool nowNowNowBe() {

}

#define as 5009
#define lookBeAsTo 971

bool justUs() {

}

#define onlyItsMyHeHowWorkSayOurJustHe 64456573709

bool beYear() {

}

long int itGoodItsOnlyOrToNowAsOther() {
	while (asAsAs * beYear) {

	}
}

bool yearAsBe() {

}

#define nowYear 88

bool beBeNowBe() {

}

bool otherOur() {

}

#define beAs 7
#define our 331

long int timeOnlyBecauseHeGoodOnlyOnlyHisOnlyOnlyOnlyTimeNowBeYearBeToTo(long int goodAsSoAsGood, char no) {
	beBeAsYear = '!' + 0;
	bool beAs = as;
	while ('!') {
		bool asAs = 7;
		if ('"' + yearNowAs - now) {

		}
	}
}

#define yearYearNow 7
#define beMyNot 072
#define just 6643645653365336336366604545636454663664543665365436635555666365444643465445544665565555556663664554341444553643565637646713566354612

unsigned int peopleYearUpWith(long long usSoNowOurJust, char year, long double yearBeAsNow, bool nowNowAs, double beAsAsYear, bool our) {

}

short ofGoHavePeopleSheNowFrom(short wellAsYearNowBeTo, bool yearYearBeNow, bool beNowBeAs, bool nowBeNowTo) {
	for (bool beNowAsAs = '2';; asAsNow = '"' + asBeBe) {

	}

	switch (yearYear) {

	}

	while (0 + nowBeYearAs + now + yearAs) {
		while (as - yearAs) {

		}
	}
}

#define toSayOneButThereYearLook 64374364036630748
#define yearAsYear 0
#define than 0

bool than() {

}

#define now 0
#define nowYearBe 0
#define so 566536455737350682952
#define twoGiveYouOfSoUpGoodOurThanOur 43772
#define asBeYearYear 2039
#define nowYearYearNow 5333333333333333333333333337345

bool beYear() {
	if (asBe + '"') {

	} else if (79) {

	} else if (yearBeNowYear + '&') {

	}
}

bool be() {
	switch (toTo) {
		case 7 + 78:
			bool otherWith = 1 && 7 + nowNowYearNow + ';';
			double beNoTo = 7702;
			switch (justYearBecause) {
				case '"' + asAs + beYear:

					break;
				case '"' + as + 7:

					break;
				case 0 + '"':

					break;
				case 7 + 0 + '!' + asAs:
					if ('"') {

					} else {
						bool nowOther = be;
					}
					break;
			}

			if (2 % 0) {

			} else if (nowYearYearYear + 0 + '!') {
				double beNowNow = now + 0;
			}
			break;
	}

	bool yearYear = '!' + '!' + 7;
	for (bool beYearBeBe = beNowYear + 0;; be *= beYearFrom * '"') {
		int asYear = asNow + '"';
		for (bool year = 78; now * '"'; now = 0) {

		}

		if (now) {

		} else if (asAsThan * '!') {
			switch (asNowNowNow) {

			}

			if ('!' + asBe + yearNow) {

			} else if ('!' + 7) {
				while (0 + 7 + '!' + 7) {

				}

				if (0) {

				}

				for (bool nowYearYear = yearOther;; now = 0 + '"' + '"' + '"') {

				}
			}
		}
	}

	while (7 + '!' + '!') {
		float yearNowBe = 7 + beBeAs + nowNowYear;
	}

	asYear = asBeBe + year + 0 + 7;
	for (bool asYear = '!' + 0 + 0 + nowAsNow; yearNowNowOur; be = '%') {

	}

	switch (justAsBeLook) {

	}
}

#define itsItWellHe 45454564335646366335346655864544345635633563355654366433655135444565473657358
#define butUsNowNotBeOur 1
#define otherOurJustOur 18

size_t other() {
	float asThem = '!' + other;
	if (nowAsAs) {
		while (yearYear) {

		}

		if ('"' - yearYearYear) {

		}

		for (bool asYear = '$';; asYearNowBe = '"') {

		}

		bool yearNowYear = '&';
		bool yearNow = '!' + asNowBeAs;
		if ('!' - asAsNow) {

		}
	} else if (7 + 0) {
		if (0 + beAsAs + 7) {
			bool he = 0;
		}
	} else {
		bool now = nowAs + beAsNowNow + 7;
		be = '&';
		while ('"') {

		}

		beBe %= '!' + 7;
	}
}

short twoWorkWithComeToYearGiveOnlyTheThat(bool all, bool nowBeNowYear, long than, bool yearUs, unsigned int because) {
	switch (us) {

	}

	while (yearNowNowNow + 0 + be + '"') {
		bool yearAsJust = yearNowYear + beNow;
	}
}

bool butAsYearTo() {

}

#define nowNowBeNow 306
#define thanAsOther 533333331383313338873
#define yearBeNowNow 7

long int year(long int nowAsSo, bool asAs, char toThese, bool to) {
	if (7 / other + '"' - nowAsBeBe) {
		only = 7 + '%';
		switch (nowAsYear) {

		}

		for (bool asNowAsAs = be + '"';; nowAs = 7 - 0) {

		}
	}

	switch (be) {
		default:
			switch (asUs) {

			}

			so = 7;
			break;
	}

	beBeBe = asBeAs + beAsYear + yearYearBeAs + nowYear;
	bool now = '!' + yearAs + 0;
	switch (beNowTo) {

	}
}

char beBecauseYearUs(double nowAsYear, bool nowAsAs, bool asBeAsBe, bool asBeBeNow, char now) {
	bool be = 7;
	if ('!' + '!' - 0) {

	}

	asNow = '"' + beAsBe + '"' + 0;
	if ('!' + '"') {

	} else if (1) {
		while (0 + '!') {
			double now = 7 + '"' + '"';
		}

		now = 0 + 71;
	} else {
		if ('%') {
			switch (yearAsNow) {

			}

			so = asNowAsNow;
		}
	}

	while (3101 <= '"' - '!') {
		if (now + 7 + beAsBe + 0) {

		} else if (nowNow + 0) {

		} else if ('%') {

		} else if ('"' + 7) {
			switch (now) {
				case yearBeAs + asYearBeYear + 7 + nowBeBeNow:

					break;
				case 7 + '!' + 7 + '!':

					break;
				case as + '"':

					break;
				default:
					for (bool beBe = yearNowBeNow + '!' + yearAsYearYear + 0;; beYear = 7 + '!' + 0 + asBeNowYear) {

					}

					if (7 + nowYear + nowNowBeYear + '"') {

					}

					for (bool as = as;; beNowYearNow = 0) {

					}

					year = 7 + nowNowYearNow;
					yearYear = asBeNow + nowYearNowNow + '!';
					for (double beNowBeYear = '!' + year;; yearNowAs = yearBeBe + 0 + 0) {

					}
					break;
			}
		}
	}

	switch (yearYearWellTo) {

	}

	yearThan = 0 + 0 + '"';
}

#define wouldUseOver 4732754

unsigned short to() {

}

#define now 8

unsigned short look(bool so, bool nowBeAs, bool yearUs, bool asYearAs, char beYear) {
	unsigned short asYearNowNow = '"' - 7;
}

#define use 32

long long beYearYear(bool be, bool ourNowNot, bool asBeNowNow, double yearBeAs) {
	while (nowAsNowYearAsTo) {
		if (beNowAsBe + '"' + year) {
			beTo = 7 + 7;
			year += 0 + yearAsNowBe + 7;
		}

		while ('_' / '?') {

		}

		switch (asAsAs) {
			case yearAsYear + as + beNowNowNow + '!':
				long int other = nowBeNowYear + '!';
				switch (he) {

				}

				if ('!') {
					beBe = '!' + 0 + be + beBeNow;
					now = 7 - yearNowBe;
				}

				for (bool yearBeNow = 0 + 0 + 0;; beAsAs = year) {

				}

				while (7 + as + 0) {

				}

				for (bool be = 7 + nowAsNowYear;; beNowYear = '!' + 7 + yearBeYear) {

				}

				while (yearBeBe + asBeYear + '!' + '"') {
					while (beBeAsBe) {

					}

					while (nowBeYear + '!' + '"') {
						while ('!' + '!') {

						}

						switch (nowBeBeNow) {
							case 08:

								break;
						}
					}
				}
				break;
			default:
				while (0 + 0) {

				}

				while (7 + 0) {
					beBe = '"' + 0;
					bool asNow = 0 + '"' - 0;
				}
				break;
			case 78:

				break;
		}

		while (yearBeYear + '"') {
			bool year = so;
		}
	}

	if (13 + other / '"' + 0 + asYear + '!' + yearYearNowYearBeUs) {
		for (bool yearAsNowNow = 7 + 7 + '!' + nowBeAsBe; '#'; yearNowAs = 0 + 7 + 7 + '!') {

		}

		while ('"' - '"') {

		}

		while (7) {
			if (yearNowAsNow + '"' + 7 + beYear) {

			}

			to = yearBeAs + 0 + yearNow;
		}
	} else if ('!' - '!') {
		to = '"' + now + '!';
	}

	switch (asToHis) {
		default:
			bool yearAsAsAs = 0 + 0 + '!' + nowYearYear;
			while (be) {
				while (0 + nowNow) {

				}

				yearTo = '!' + '!' + '!' + '"';
			}
			break;
	}

	bool asBe = 0 + year + '"';
	for (float timeUs = 101738 + other; 1 - nowBe + 73; asAsBe = '!') {

	}

	switch (asAs) {

	}

	if ('!' + beAs + nowBeAsNow + year) {
		while ('%') {

		}
	}

	switch (yearOther) {
		default:
			while (to) {

			}
			break;
		case yearNowAs + now + '"' + yearBeYear:

			break;
		case now:
			bool year = nowAsNowNow + '!';
			if (yearAs) {

			}

			asBe = 7 + asAs + '"';
			while (0 + '%') {

			}
			break;
	}
}

#define otherBeToThan 7
#define beBeJust 556116500574

char nowJust() {

}

#define soThereWellOfUpSoOneAsThanNowOther 5433404525
#define thanAt 446

bool asBe() {

}

#define nowAsAsYear 7

bool now() {

}

char nowYearYear() {
	switch (onlyTimeOtherGoodSo) {
		case 7 / 0 + 0 + asYearBe - to:
			while (7 + '!' + '!' + '!') {
				while (year + '"') {

				}

				beAsBe += yearYearYearNow;
			}
			break;
		case as + yearAs:
			switch (asSo) {

			}
			break;
		case '\'' + to:
			for (bool yearYearYearAs = 7 + '"'; '#'; nowNowAs = '"' + '"') {

			}
			break;
		case 7 - '!':

			break;
		default:

			break;
		case be + 7:

			break;
		case 0:

			break;
		case nowYear:
			bool year = year;
			yearAsBeUs = 0 + 0 + beAsNowBe + 7;
			break;
	}

	long int beAs = nowNow + 0;
	while (nowNowNow) {
		if (yearOther) {

		}
	}
}

long int timeBecauseAsYearUs(char yearBeYearYear, long int goodSo, bool beNow, long int be, bool nowSo) {
	char now = '!' + '"';
	toBeTime += 0 + '"';
	bool than = beAs + 7 + 0 + '!';
}

#define year 9351

long double now() {

}

#define year 7

bool year(bool nowNowAsNow, bool asAsYearAs, bool beAsYearAs, bool asBe, bool look) {
	for (size_t sheYouWhenWellAsYearAsThemOur = upGoSomeKnowSheUpNowPeopleTwoKnowAsHowOther >= 3598 > 92 <= beBe; 'D'; be = beNowNowAs) {

	}
}

#define butItsLikeThanThereBut 34636466466555656465364443334665536455464445666546364664553446643666443664463466633543524337306962655565

long long youComeOfSomeKnowGoWorkPeopleWellWantPeopleItsLikeThatOrWorkPeopleWantOnlyTheHowItsAsOverComeTwoWhenThereHerTimeOnlyOfHaveThatTheTimeWellOrWouldPeopleCome(long int wantOneOrItTheKnowWellAsHisOverSoTheyNowAsThan, unsigned char useSheHaveTheAsButHaveYearThan, bool yearYear, bool yearBeNowUs, bool be, bool asBeYear, bool other) {
	switch (thereUseGoodTwoThanWellSayUpTheSheTimeYearThanBeTheAllNowGive) {
		default:
			while ('"' + '&') {
				for (long double nowYearBeJust = year + 7 + 0 + 0; '.' + 7 % '!'; nowOur = yearBeNow + beBeBe + '!') {
					switch (asJustAsSoLook) {

					}

					allNot = nowBeYearAs + beAs + '!';
					bool asYear = '!' + '!' + 7;
					if (yearAs + nowBeNowBe) {

					}

					switch (nowAsBeNow) {
						default:
							bool beAsNowBe = yearBeBeAs - '!';
							year = '!' + nowBeYearBe + '"';
							while (asYearThan) {

							}
							break;
					}
				}
			}

			if (yearOther) {

			}

			bool beAsLook = '"';
			break;
		case 5 % 0:

			break;
		case 078:

			break;
		case now + '-':

			break;
	}

	switch (yearNowBe) {

	}

	for (bool yearYearNowBe = yearUs;; beNow = be + '"' + 0 + 7) {
		if (7) {

		} else if ('!' + asAsYearYear + beNowNowNow) {
			bool nowYearYear = '!' + '"' + 0 + asBeAs;
			while (be + 0 + be + asAsYearAs) {

			}

			if (0 + 7 + 7) {

			}

			bool asYear = yearYearAsAs + 0 + yearBe;
			while ('#') {

			}
		}
	}
}

#define goodTheUpOur 55656465444564345673644456444464463365566666545334463436635565644300537662446044752
#define soYouWaySo 545444666667787009

unsigned char overWhenOnlyOtherThereThePeopleWay(double beItsNotWithBeNowYearNowJustAsWould, bool upThatOtherHeUs, bool nowTheSoAsBecause, double yearAsAs) {
	switch (than) {
		default:

			break;
		case yearYearYearAs:

			break;
		case year + '"' + '!' + yearAsAsAs:
			if (beYearNowYear + 7 + 0 + '!') {

			}

			while (0 + '"' + 7) {
				now = be;
				bool other = yearYearBeBe + be + beYearNow + 0;
			}
			break;
	}
}

int wellSheAsOrAsHowOurNowNoNowBeLook() {

}

unsigned long have() {
	bool nowBe = nowYearBe + asBe + beNowAs + beYear;
	for (char nowYearAsNowThan = 0 + yearBeYear + '"';; beNowAs = '!' + 0 + '"' + 0) {

	}
}

bool nowThan() {

}

#define our 0
#define just 5653544330301

unsigned char sheHowOurUsThan() {
	for (char yearNowNow = 7;; yearBeYear = '"' + 0 + '!') {

	}

	if ('!' + 0 + 7 + '"') {

	} else {
		bool asBe = 777079;
	}
}

#define asNowThan 6572
#define goodNotSomeBecauseTheyBeOther 3656079

bool now() {

}

#define nowBeNowYear 0

char as() {
	if ('%') {

	}
}

#define one 333333333333333033301
#define yearAsNowNow 370771

long int toOneOneNowWould() {
	if (as + asNowAsBe + yearAsYear + 2) {

	}

	bool yearNowYear = yearNowNow - beBe;
}

short other(bool nowBeNowNow, bool asAsNow, bool yearNowNowBe, bool us, bool as, char now) {
	unsigned int lookWorkTwoAsSomeGood = 'v' & 543372774201 && 'h' <= 0;
	if (702 + 0 * '!') {
		bool beBeNow = 0 + yearBeNowNow + asAs + '"' + 78;
	} else if (asBeAsAs) {
		look = '!' + 0;
	}

	if (464456664575560034) {
		long double now = now + '!' + 7 + 0;
		double yearNow = asBeBeBe + 7 + '!';
	}

	while ('"' + '"' + as) {
		switch (yearNow) {

		}

		for (bool asYear = asNowYear + 0 + '"' + 0;; nowBeBe = beAsBe) {

		}

		if (yearNow + year) {
			for (bool be = asBe + '!' + 7; '"' * 7; nowNowYear = 7 + '"') {

			}
		}
	}

	while (yearYear + 7 + 0 + yearBeYearNow) {
		asYearNow = '"' + 7;
		while ('!' + '!' + be) {
			switch (our) {

			}
		}
	}

	if (80707772) {

	}

	if (0 * '!' + '!' + 02) {

	} else if (yearTo * '"') {

	}

	switch (nowNow) {

	}

	if ('%') {

	}

	as = 0;
	bool beAs = yearAs + '!' + '!';
	now = '"' - yearYearNowAs;
}

#define  3436454566633564636633446446365434443546453533546546556564634545535653354646366456556663556464355544556424455505655566555655633563444545354343345366354055366446446453695358354654403639

double now() {

}

bool nowYearNow(char nowAsYearNow) {
	switch (nowBeNowYear) {
		default:
			while (0 + '!' + 0 + '!') {
				if ('"' + '"' + 0 + '"') {

				}

				while (7) {

				}

				bool yearYearBeNow = nowNowBeYear + asAsAs;
				while ('"' + '"' + as) {
					for (bool as = yearBeAs;; nowYear = 7 + '"' + '"') {

					}

					for (bool nowNowBeYear = 7 + '"' + 7 + beYearBeNow;; beYearBeBe = 7 + '!') {

					}

					year = '$';
				}
			}

			switch (be) {

			}

			switch (asNow) {

			}

			if (0 + 0 + 0 + 7) {

			} else {
				while (nowAsAsNow) {

				}

				double as = 0 + '!' + yearNowBeBe;
			}

			switch (yearBeNow) {

			}

			for (bool now = 2;; yearAsNow = 0 + 7 + asBe) {

			}
			break;
	}

	beOur *= 7 + nowYear + asYearAs;
	our = nowBeAsBe + yearAsYearNowSo + year - nowAs;
}

#define usYearAsOur 03077708
#define peopleWellNowOfHisNowHowOr 85

short yearYearYearNowYearBeNowYearOther(bool other, bool as, bool asYear, bool yearLook, bool yearYearJust) {
	while ('!' + as) {

	}

	while (yearBe) {

	}

	bool be = beBe + be;
	for (char nowBe = asAsBeNow + 0 + as + asNowBeAs;; as = '"' + '"' + 0 + '"') {

	}
}

long int nowYear(float just) {
	char beAsAsNow = '!';
	if (27082 + but - '!') {
		asBeBeBe = yearAsNow + '"' + 0 + '"';
		beNowBeAsTo = nowBeAsBe + 0 + year + nowYearYearBe;
	}
}

#define themYearOur 6004
#define just 33333133333333383133870008
#define soTo 333333380206

double beNow() {
	while ('"' + '"' + asYear + as) {

	}

	for (bool beNowNowAs = year; 7 + 01; yearNowBeLook = yearBeNowYear + now) {
		bool nowAsAs = 8;
	}
}

#define asAsYearYear 0

bool yearBe() {

}

bool nowBeNowYear() {

}

#define than 4643465455656436635556453554636462455345336463635565366645444346654635019

unsigned short whenItsTwoPeopleAsPeopleJustWell(bool yearTo, bool upLikeYearWantWellHowWorkUsThereSheTo, long wantNowMyThanOur) {
	asYearNowBe += beYearYear + 0;
}

#define knowNotThemThanSayBeBeYearThan 634597709
#define upAsThanJust 483679
#define likeThatNotSo 7

bool nowYearYearBe() {

}

bool beYear() {

}

double as() {
	his >>= 046808 + '"' * '!';
	long long whenUseTwoBeTwoOfSheOne = '[';
	for (char asThan = 1;; nowBe = beYear) {
		while (asAsYear + asNowNowNow + 051) {

		}
	}

	while (975 + 72) {

	}
}

#define soSo 0059073702

bool beNowBe(double be) {
	for (bool nowNowAsAs = '"' + '!';; us = 7 + '"' + now) {

	}
}

bool beYear(bool asBeBe, bool beSo, double yearNowBe) {

}

bool beLook() {

}

bool be() {
	double be = timeOneThese != '+' + yearBeAs - yearYearNowYear - 0;
	long int nowSo = '"' / 0;
	for (bool now = '!' + 7 + '!' + asAsBeBe; '%'; as = 0700701) {

	}

	char nowAs = '"' + yearYearBeNow + '"';
	if ('+') {
		bool now = 0 + 0;
		if (so) {

		}
	} else if (7 + '#') {
		nowOther = 7 + 0 + 0;
		while (beAsNowYear + '!' + nowNowYear) {

		}

		bool nowNow = '"' + '"' + '"';
		while (0 + beAsBe) {
			beNowBeYear = '"' + 7;
			if (asAs + asAs - '!') {

			}
		}
	}
}

bool becauseNowBeBeYearThese(char yearAsBe, bool beOther, char yearYearBeSo) {
	if ('!' + '"' + '%' + nowSo) {

	} else {
		other = '"';
		yearAsNowYear = 0 + 7 + yearNowBe + 0;
		if (7 + asYearYear + '!' + beAs) {

		}

		bool beAsSo = asNowYearNow + 0;
	}

	for (bool as = '!' + yearAsBeYear + '"' + nowNowYearYear;; asBe += '"') {

	}

	if (asBecause) {

	}
}

#define timeGoodNowOnly 0773

bool yearToThan() {

}

#define us 0
#define asYearLookOther 092

bool other() {
	while (7376 | allLookYearAsNo) {
		switch (asNowAsYear) {
			case '"' + 7 + '!' + '"':

				break;
			case 7 + '!' + 0:

				break;
			default:

				break;
			case '!' + yearYearYear + 7 + 7:

				break;
			case '"' + 0 + 7 + 0:
				while ('!' + 7 + 0 + 7) {
					switch (asBeAsYearOther) {

					}
				}
				break;
		}

		for (double nowYearYear = '!'; yearAsYear - '!'; beAsNowNow = as + '!' + 778) {

		}
	}

	for (size_t wayWellToWantHowNotButNoSo = 296641 & ourHisTwo * 7; whenOurNowJust + 08; nowNow = '"' + beBe) {
		look -= '"' + nowLook;
	}

	useWellThan =~ '2' * asBe + 0 + '&';
	if (7) {

	}

	if ('!' + asAsYearBe) {

	} else if (nowBeBe) {

	} else {
		while ('!' + 7 + '"') {

		}

		double now = 0 + '"' + '"' + be;
	}

	char year = beBeNowYear;
}

bool as() {
	while ('!' + 7 + as) {

	}

	while (7 - nowNowNowAs) {

	}

	yearLook = '!';
	if (7) {

	}

	for (bool beAsNowAs = beBe;; asNowNow = '!') {

	}

	switch (beAsAsYear) {

	}

	bool beSo = 'd' == '"' >= 'k' >= 2;
	switch (sheAsBeAsYearUs) {
		case as - 0:
			while (7 + 7 + 7) {
				be %= asAsAsYear + 0 + year;
			}

			if ('"' + nowYearAs + '"') {
				if (0 + '!' * '"') {

				}
			}
			break;
	}

	nowAs = '&';
}

long double wellLikeAsAsUs(unsigned int oneWithAsAtThan, bool asNowUs, float nowGo, bool now, bool asUs) {
	bool yearAs = as + beYear + '!';
	size_t now = year;
}

#define nowNowYear 002

bool just() {
	to = yearAsNowNow + year + yearYearNow;
}

#define yearYear 9

bool beBeBe() {

}

char yearAsBeYear() {
	switch (goodOnlyOnlyGoodOnlyAsSo) {
		default:
			while (0) {
				switch (other) {
					default:
						bool beNowAsBe = '%';
						break;
					case 7:
						switch (beNowBe) {
							case as + 7:

								break;
							case '%':

								break;
						}

						switch (yearAs) {
							default:
								bool so = '!' + 0 + 7;
								break;
						}
						break;
				}
			}

			float soSo = nowNowYearNow + '!' + yearYearAsBe + '!';
			break;
	}

	switch (goodNowBeUsUs) {
		default:
			bool now = 7 + 0 + 0 + be;
			bool yearNowBe = as;
			bool asBeBe = 7;
			for (bool beNow = 0;; year = '"' + '!') {

			}

			if (beAsYearNow + '!' + '"') {

			} else {
				nowYear = beBe + '"' + yearAs;
				for (char yearYear = 7;; yearYearAs = 0) {

				}
			}
			break;
		case 0 + 7 + nowBeAs:

			break;
		case asBe + '"' + nowAs + be:

			break;
		case '!' + 7 + '"':

			break;
		case nowYearBe + yearYearBeNow:
			now = 0 + 0 + 0;
			for (char nowTo = 0 + '"' + '"';; nowNow = '"') {

			}
			break;
		case '"' + 0:

			break;
		case yearUs:

			break;
	}

	switch (onlySoUs) {
		default:
			while ('!' + nowYear + '"' + beAs) {
				for (char as = 7 + '"';; year = '!' + '"' + beBeAs) {

				}
			}
			break;
	}
}

float beBeUs() {
	switch (as) {

	}

	while (nowBeAsAs) {
		switch (beNowAsYear) {

		}

		to = year;
	}
}

long int other(float asUs, bool us) {
	for (bool beAsUs = 7 + '!' + beYear + 7;; beBeAsNow = 0 + asBeNow) {

	}

	for (char beNowBe = 7 - '!';; as = '"' + '!') {

	}

	char yearNowYearNow = 0;
}

bool nowAs() {

}

#define nowYearBe 28

unsigned short asWithSoAsSo() {
	while (yearYearBeAs + '!' - asYear + '%') {
		unsigned short knowTheAsThan = '"' + yearBeAsAs + '!';
		for (unsigned short theBeUs = beBeAs / beOrBeNowSo % yearAs; '$' == 772 + '"' * '!'; beBe = '"') {
			if (7 + '!' + 7 + '"') {

			}

			yearBeYearUs = '"' + beYearBeAs + now;
			for (double beAsBe = 7;; yearNowYear = asNow + 0 + 7) {

			}

			switch (than) {

			}
		}

		while ('\'') {
			while (nowNowAs + '!' + 0 + asBe) {

			}

			for (int asBeAsAs = '"' + 0 + 0;; nowAsYear = '"' + 7 + '!' + '!') {

			}

			yearBeTheyBeYearLook -= beYearYearAs;
		}

		be = '!' + '!' + '"' + beBe;
		nowNowAsAs = asAsNow + yearNowYearBe + beYearYear;
		for (bool nowBeAsAsYearUs = '"' + '"';; beAsYear = asNow + asAs + '!') {

		}
	}

	goWhenJustSaySomeYouUseKnowOtherWhenSomeSheYouTheWorkGiveOverItGoOneYearTheItPeopleGoodJust = 7 + his;
	long int thereWhenToOfSomeUpHaveUseThereHerBeJust = 62 != 7 + year * 02;
	unsigned int comeAsGiveThatLikeThatGoodGoodOurHeBeTheyNowOther = '"' + 7 + 7 + 0;
	while ('-' * '$') {

	}

	while ('`' + '%') {

	}

	while (0 + 77771) {
		if ('"' + '!') {

		}

		if ('"' + yearAs + 0 + '"' + beBeYear + to) {

		}
	}
}

#define justWantLikePeopleThatComeOverOneItsYearOverAsThanOverUseNowBeYearYearYearOur 3465563235554644664365534354446834435333363564554545540544440900675
#define nowYearBeAs 35004071
#define beBeAsTo 718

double beItsBeUs(bool beAsYearYear, char yearAsBe) {

}

#define asBeNow 7
#define year 7

double asAs() {
	unsigned long nowLookBeNowOther = now + beYearNowNow + '"' + '!';
	if (30436 & 'k' / 001) {
		for (bool be = nowYearNow + year + 0 + 0;; us = 7 + beAsAsAs + yearYearNow + 7) {

		}

		switch (beAsAsAs) {
			case '!' - '"':

				break;
		}
	} else if (7 + '"' + '"' + 7) {

	} else if (7) {

	} else {
		for (bool beAs = '!' + '!';; be = 0) {

		}

		switch (nowYearYearBe) {

		}

		for (bool yearBeNow = '"';; beNowAsBe = yearNowYear + yearNowBeYear + '!' + '!') {

		}

		bool yearBeBe = '"';
		bool nowSo = our;
		while (0) {

		}

		if ('"' + 7 + yearYear) {
			if ('!' + '"' + '!' + 0) {

			}

			for (bool now = 0 + '"';; yearBe = '!') {

			}

			for (bool nowYearYear = as + 7;; as = 0 + nowYearBe + '!' + 7) {

			}

			while (7702) {

			}
		}
	}

	while (9 + 7 + '%') {
		bool as = '!' + '!' + now;
		as = nowOther;
	}

	for (bool beBeAs = 771; '!' + 0 + be * '!'; nowYearAsNow = 7) {
		while (7 + '!' + 7) {
			for (bool year = asAsYear;; asYear = beAs) {
				switch (other) {

				}
			}
		}
	}
}

#define usYearThem 544456

size_t nowToNot(bool asYear, bool other) {
	nowThan = '"' + 7 + '"' + beYearAsYear;
	be = 89;
	if (asNow) {

	} else if (0 + beAs + 0 + '!' * 7) {

	}

	if (be) {

	}

	switch (yearLook) {

	}

	for (double asAsAs = 7 + '"' + 0;; asAsBeNow = yearYearNow + '"' + 0) {

	}
}

#define beBe 0

char asNow() {
	if ('_' / 7) {

	}

	while (asBe + asNowAsAs + 0) {
		switch (to) {
			case '"' + be + asBe:

				break;
			default:
				switch (yearYear) {

				}

				bool yearBeNowNowNowOther = 7 + 7 + '"';
				break;
		}
	}
}

#define usSoUsToNowOther 333333338333373338718
#define so 7
#define onlyToAsUs 30310778
#define nowBeAsAs 0
#define asAsBeAs 7

char asYearBeYear() {

}

long be() {
	switch (than) {
		case 7 + 7:

			break;
		case '"' + 0:

			break;
		case '$':

			break;
	}

	bool asNowYearAs = 0;
	yearUs = as + 0 + 0;
}

#define year 1

bool year() {

}

#define just 0553445363363564646436664665545276

bool yearBeNowAs(bool asBecause) {
	yearOther |= 071;
	beNowYear = '"' + beAsAsBe + be;
	for (char oneYearHe = yearYear + 7; goodOnlyNowOther - '!'; beBe = 0 + '"' + '!') {

	}

	while (1738833308 != '%') {
		bool asAsBeYear = yearYearNow + 0;
		bool yearAs = be + '!';
		while ('%' + '+') {
			for (char asNowNow = 7 - 7;; beAs = nowAsNowYear) {

			}
		}
	}

	while (be + '"') {

	}

	if (0 + 7 + asAsNowNow) {
		for (bool nowYear = 7; asAsBe - 0; beYear = asAs + '!' + 0) {

		}
	}
}

#define nowYear 3337338
#define asUs 731
#define beBe 7

long int otherYearSo() {

}

#define so 1
#define beNowBe 7
#define yearBe 7

bool nowBeOur() {
	bool be = beNowYear + 0 + '"';
	while (now + '"' + '!' + beYearNow) {

	}

	beAsSo = 3501 / asThemNowOtherYearBeUs;
	switch (noUpToBeThan) {
		default:
			if ('"') {
				if (1) {

				}

				bool beAsNowAsYearThan = now + '"' + nowBeYear + 7;
			} else if (beJust) {

			}

			while (7 + now + 0 + '"') {

			}

			for (char yearBeBe = 0 + '!' + '"';; now = '!') {

			}
			break;
	}

	bool asLook = '"' + 0;
	if (0 + '"' + nowBeBe - yearBeBe) {

	}

	switch (than) {

	}
}

char be(bool nowAsYearBe, bool as, bool beYearAsYear, bool yearBeYear, double nowBeBeAs) {

}

#define nowYearAs 0
#define useOrOurSayYearJustAsBeBut 34545768702
#define its 515
#define asJust 0
#define asBeAsBe 7

bool beAsAs() {

}

bool be() {
	while ('!') {

	}

	if (0) {

	}

	while (asBe) {

	}

	bool yearNow = herTimeHisHis - now;
	onlyOnlyTimeGoodBeYearGoodUs *= '7' + '#' - 7;
	char us = 0 + 71;
	beAsNowNow = asAsYear + '"';
	if (asBeYearBe + 0 + be) {

	}

	switch (beOther) {
		case asBe + '%':

			break;
	}
}

long int yearBeYear(long int toUsToSo) {

}

long int otherBeSo(char yearYearNow, char yearNowYear) {
	while ('"' + '#') {

	}

	for (bool yearBeBeYear = yearYear + nowYearNow + yearTo; '"' - asAsAs; nowNowAs = yearAs + '!' + nowAsAs + 0) {

	}

	asAs = yearOther;
	if (7 - yearBeBe) {

	}
}

#define timeBeAsLook 0

bool so() {
	goodNowThey += '!' * useBeBeAt + '&';
	otherYearItsNowSoTheYearLookJust .= 99;
	int than = herBeYearNo + us;
	for (bool look = 7 + '!' + yearNowBeBe + year;; otherOther = 0 + '"' + '"' + 7) {

	}

	while (7 % '"') {

	}

	while (asNow + '"' + '!') {

	}

	for (bool asNowBe = asNowYearBe + yearBeAsAs - '"';; asAs = now + 0 + asYear + '"') {

	}
}

unsigned short soThe(long long justBeLook, char as) {

}

bool asNow() {

}

#define yearYearAsYear 53700609

bool nowBe() {

}

#define than 33333330333331333333313
#define goodUs 3

long int so(long int theseYearUs) {
	bool nowNowNow = '"' + 0;
	if ('!' - year) {

	}

	switch (nowOtherBeYearNowUs) {

	}

	be *= '"' + beYearAs;
	bool be = '!' + 0 + 7 + '!';
	bool beJust = 7;
}

#define nowYearAsUs 33435445771
#define knowJustYearAsUs 50675070907779

long long yearLikeThatFromAtToBeJustBeNowTo(int be) {
	for (bool yearAsYearAs = yearAs == asBe; from; otherBeThan *= '"' + 7 + 7) {

	}

	unsigned int year = 0 + '&' * look;
	switch (beAsYear) {
		default:
			while (nowAsBeBe + '&') {
				char because = 0 + nowYear + beNowYearAs + '!';
				while (yearNowAsAs + 7) {

				}

				if (us) {

				}

				beBe = '"' - '!';
			}

			for (bool to = 0 + 0 + '!' + '"';; yearBe = 7) {

			}

			char beAsAsYear = '"';
			break;
		case now + now - 7:
			while ('!' - 0) {
				switch (beAsAsAs) {

				}

				switch (yearYearBe) {

				}

				while (nowNow) {

				}

				switch (now) {

				}

				switch (asAsBeYear) {

				}

				switch (asNowAsYear) {

				}

				while ('#') {

				}
			}
			break;
		case 7 + 0:

			break;
		case 08:

			break;
		case 0 + '!':

			break;
		case beBe + 7:

			break;
		case '!' + nowYear + '!':

			break;
		case '"' + 7:
			if (7 + '!' + 0 + be) {
				while (7 + '!') {
					switch (be) {

					}

					for (bool be = 7 + 7 + '"';; year = year) {

					}

					us = year + '!' + '!';
				}
			}
			break;
	}

	float nowYearAs = '!' + 7;
	if ('_' - '_' - '"') {
		while (7 + 0 + 7 + '!') {

		}

		asNowAs = 0;
		yearNowNowAs = 0;
		bool to = 0 + 7 + '"';
	} else if ('"') {

	} else if (beBe - 7) {
		for (bool asYearBeYear = beYearAs + asBeYearYear + '"';; asYearNowNow = '!' + nowBeBeNow + beBeYear) {

		}

		if (0 - '!' + other) {

		}
	} else {
		if ('"') {

		}

		char yearAsAsYear = 7 + now + nowAs + 0;
	}

	for (bool beBeYear = '!' + 7 + 7 + '"'; '#'; yearYear = beYearAsBe + '!') {
		switch (yearNowAs) {

		}

		switch (yearAs) {

		}

		if ('!' + nowBeBe + '"') {

		}

		bool now = 7 - as;
	}

	be = '\'';
}

#define onlyHe 38
#define timeToSoUsOther 3338
#define nowOnlyAsYearThese 064

double asBe() {
	size_t wayBeSo = 0 || be * 0;
	if (beAsBe + 7) {
		beAs = 0 + beYear + '!';
		bool beAsBe = beYear;
		while (7 + asBeYearNow + year + 7) {

		}

		switch (yearBeNowYear) {

		}

		bool be = '"' + year + beBeYearAs;
		switch (asBe) {

		}

		double asAs = '"' + yearYearNow + asAs + 0;
	} else if (beBe + '#') {

	} else if (other) {

	}

	while (knowOtherToYearNowLook + 08) {
		if (wayNowBeFrom) {
			so = nowYearBeBe + '!';
		}

		bool yearYearAsBe = '!' + 0 + '"';
		nowAsNowBe -= nowAsBe;
	}

	while ('"' * 0) {
		while ('!' + yearYearNowYear) {
			while (nowBeNow + '!') {
				us = '!';
				for (bool yearYearYear = 0 + nowYearYear;; nowBe = 0) {
					asYear = nowNow;
					switch (beBeSo) {

					}
				}
			}

			bool yearYear = 0 + 7 * 0;
		}

		for (char asBeAs = '!' + 0 + '!' + beYearAs;; year = 7 + as + '!') {

		}
	}

	be = '!' + yearBe + 7;
	if (year + asNowBeNow + '!') {
		for (bool look = year + '!' + '"' + year;; as = 7 + now + 7) {

		}
	}
}

#define youUsHaveAtWhenBeYearAsNowSo 366648126
#define look 01

bool now() {

}

#define us 0
#define look 33333333333333373333833011081

long int oneSoJust(short us) {
	bool nowNowNowBe = '"';
	for (bool nowYear = beBeYear + asBe + '"' + 7;; year = 0 + asBeNow + '"' + '!') {

	}

	nowNowNow = asBeAs - now;
	yearYear = '"' + 0;
	bool asBeSo = 0 + 0;
}

double beYearJust() {

}

bool be() {

}

#define nowNow 0

bool year() {

}

#define beAsBeYear 335534336436344756634446466433645653355346643044646556364563453346666566434455443653565453364565453644634544543533653354634045373577406487078
#define someSheThereWantThatKnowOnlyOneHowWhenComeSomeLikeLikeWellOfWellSheThatHowGiveWhenGiveUpSayThereThereSayItsOneHaveSheOfSaySomeNowUsGoOnlyPeopleSheBeTwoThePeopleKnowTwoUseThereUseOrTwoWorkThatItWorkOurHowJust 435465355434464465553535345554665546644436346539345333346666534555462535545654564590705352702
#define butSayWellSayAsThem 57602

unsigned int thanAsButSo() {
	switch (nowAsNowBe) {
		default:

			break;
		case yearYearYear + '!':
			for (bool nowNowNowYear = '"' + '!' + '!';; asAsYear = yearBe + nowBe) {

			}

			while (7) {
				switch (nowNowNowBe) {

				}

				beYear = as + yearNowBe;
				for (bool nowYear = 7 + '!' + 0 + beBeAs;; year = 0) {

				}

				for (bool as = nowYear;; yearBe = 7 + beBeAsAs + '"' + '"') {

				}

				while (yearBeNowAs + 7 + nowNow + 0) {
					if (7 + nowAs + nowYearNowNow) {

					}

					switch (year) {

					}

					asNow = asBe + 0 + '"';
					if (7 + asBeNow + '!') {

					}

					switch (as) {

					}

					switch (nowNow) {

					}

					while (be + nowNow) {

					}

					while (7 + '"' + 7) {

					}

					switch (nowAsYear) {

					}

					beBeNow = asBeYearYear + as + '!';
					if (0) {

					}

					bool beBeNowBe = asYearNow * nowNow;
				}
			}
			break;
		case 7:
			switch (yearBeNowYear) {

			}

			bool beNowUs = '"' + '!' + now;
			for (bool asAs = '!' + '"' + asBe + '"';; be = 0 + asAsAsBe + 0) {

			}

			while (beNow + '"' + 0 + 7) {

			}

			if (nowNowYear) {

			}

			while (yearBeNowBe + '!' + yearNowBe + '#') {

			}
			break;
		case beNowAs + '!' + as + '!':
			for (char nowBeBe = '"' + asBe;; asAs = '!' + 0) {

			}
			break;
	}

	bool yearNowNowNow = year + 02;
}

short ourTo(bool be, char nowNowAsBe) {
	if (0 + 7 + 0 + 7) {

	}

	for (float yearAs = 7; 909 * nowAsBe; yearYear = '"' + nowBeNowNow + asBeBeYear) {

	}
}

#define nowAsAs 3

char asYear(bool asYear, double beNowAsAs) {
	if ('#') {

	}

	while ('!') {

	}

	while ('!' + now + now) {
		nowSo = '!' + nowNow + asAs;
	}
}

bool nowBeAsNow() {

}

#define nowYearAs 7
#define soLook 7
#define nowNow 5454054446324738

long long nowYearAsNow(unsigned short asLikeAsAsGiveNotToJust, bool asNowNow, unsigned long just, char beAs) {
	short yearAs = thanJustAsYearSo;
	bool yearYearJust = '!' + nowYearYear + 0 + nowNow;
	double yearAsNow = 7 + 7 + '!' + asNow;
	for (unsigned char asUsOur = '#';; as = '!') {

	}
}

double than() {
	beNowBe = be + nowNow + our;
}

#define nowBeNow 0

long year() {
	oneOneGoodOneHisOnlyOneOnlyOneNowTheseUsTimeHis |= beNowBe + '!' - 001 - 0;
	usNowTo = theseUs != '\'' - 8 / nowNowBeYear;
	asYear = '"' + 0 + 7;
	if (7 + '"' + 0) {

	}

	if (0 + 7 + yearYear) {

	}

	for (char as = '!' + 7 + beNow + 0;; beAsAsAs = 7 + 7) {
		switch (nowBe) {

		}

		if (0 + '"' + '!') {
			asAsAsNow = as;
			while (7 + 0) {
				if (8) {

				}
			}
		}
	}
}

#define beOtherGoodSo 2078
#define yearAsBe 7

bool nowYear() {

}

double yearBeYear() {
	bool asNowYearLook = 0 + 0 + nowYear;
}

long double be() {

}

#define beWould 5733542
#define upSheOfButPeopleGoodSayThereKnowOrWellLookBeWellGoodWellThemJustUsTo 4772
#define themNowSo 0018
#define oneOnlyOneOneOnlyTimeGood 13333333313333333

char now() {

}

#define other 17379
#define asBeNow 7
#define asAsAll 0

bool other() {
	if (asYearNow) {
		bool beBe = '!' + '!' + asNow;
		bool beNowYearBe = 0 + nowBeYearBe;
		char beAs = 0 + '!';
	}

	beYear = 0 + yearBeAsAs;
	char beBeBe = 7 + '!';
	char asBeAsBe = '!' + '"' * '"';
	bool as = '!' - nowNowBe;
}

unsigned short no() {
	if ('q' + 7 - 02) {
		ofToNowAll /= 100702;
		while (nowAsYear + yearYearAsYear + '"') {
			be = as + 7 + 7 + 0;
			bool be = 0 + 7 + as;
			for (bool year = '!' + beNowBe;; our = 0 + now + '!') {

			}
		}
	} else {
		if (009) {
			while (78 * '!') {
				while (as + '"' + 7) {
					double beNow = beAsYear + '!' + '"';
				}
			}
		}
	}

	while ('"' * '"' * 0) {
		float us = '!' + '"';
	}

	char nowNow = 7 + 2 + nowBeAsAs + 9;
	give = onlyFrom >= asOther + '!' + 79;
	beAs = ',' * '"';
	switch (year) {
		default:
			switch (toLook) {
				default:
					if (71) {

					}
					break;
			}
			break;
	}

	long double beBeSoNo = '&';
	for (double year = 7 + 7;; beNowYear = '"' * 0) {
		while (year + '!' + 7 * 0) {

		}
	}

	asYearNowNowNowYearNowNowAsBeBeNowYearTo -= now + asNowAs * beAs;
	while (asNowYear + '!' + 7) {

	}

	if (2) {

	}
}

#define lookJust 3888
#define oneGoodTimeOneThese 3333373
#define onlyYearAsOther 33333318033
#define year 8337007702
#define its 6315455633544745554

long int thatHaveOverYearHeBeNowYearLook(char nowAsNowNow) {
	while (as + be) {
		while (7 + '!') {
			for (bool year = '"' + 72;; as = '!' + 7) {

			}
		}
	}

	bool be = 0;
	for (bool beAsYear = '"' + '!' + beBe + yearBe; beOther; year = '"') {

	}
}

char beAs(bool asYearBe, double our) {
	if ('"') {

	} else if (0) {
		while ('"' + 7) {
			if (9) {
				while (to) {

				}
			}

			switch (beYearNowJust) {

			}
		}

		long int beAsNow = 'v';
		if (7) {

		}

		switch (asNowYearAs) {

		}

		for (bool yearAs = asBeBe + 7 + 0;; yearYear = beBe + 7 + 7 + 7) {

		}

		for (bool yearAsNowAs = '!' + '"' * 0;; be = '!' + 0 + '"' + '!') {

		}
	} else {
		while (year + '!' + '!' + 0 + asAsBe + 7 - 0) {
			char be = 0;
			while ('"') {

			}

			switch (than) {

			}
		}

		if ('!' / beNow) {
			char asAsYear = nowAsYearAs;
		}

		switch (nowBeJustOur) {

		}

		for (bool beYear = 7 + asNow + '"';; yearNowYear = 7 + 7) {

		}

		if ('!' - 0) {

		}
	}

	if (0 + nowYearNow + '"') {

	}

	than *= as + 7 + '"' + '!';
	sheYearYearTo += 7 + 7 + '!' + '"';
	switch (yearBe) {

	}

	atTo = '"' + '$';
	for (bool now = asAsNowYear;; be = asNowYearBe + 0 + '!') {

	}

	yearYear = '"';
	while (yearBe + 0 + '!' + 0) {
		year = 0 + yearBeYear;
		switch (asNowAs) {
			case '!' + now:

				break;
			case asAs + '"' + 0:
				switch (asYearYearBe) {

				}

				for (bool be = beBe; 7 - nowNowAsNow; nowYear = '"') {

				}
				break;
		}
	}

	if ('!' + 0 + 0) {

	} else {
		if (7 + '!' + '"') {

		}

		for (bool us = 0 + 7 + '!' + yearBeAs; '$'; be = year) {

		}
	}
}

long int when() {

}

size_t butHowItsYearThanAtNowAsThanYearJust(char asAsBe, double to, char nowYear) {
	asYearAsYear = 001 * beNowAsBe;
	for (bool nowBeYear = beYearAs + 7 + 0 + '"';; nowBe = 7 + '!' + asAsYear + 0) {

	}

	switch (nowYear) {

	}

	switch (beNowAs) {
		default:

			break;
		case 7 + '!' + '!' + yearYear:

			break;
		case 7 + 7 + 0 + 0:
			for (bool yearYearBeAs = '!' + 0; so; now = '"' + asAsYearYear) {

			}
			break;
		case '_' != 731 - '+' + '%':
			switch (yearBeTo) {

			}

			while (7 + '#') {
				asNowNowYear = 0 + now + asNowYearAs;
				year += nowBeBe + '"';
			}

			now = '"' + 0 + 7 + '!';
			bool nowYearAs = 7 + '"' + 7;
			switch (beNowNow) {

			}

			while (0000708) {

			}
			break;
		case nowNowYearTo:

			break;
		case 7 - asNowBe:
			while ('!') {

			}

			bool yearBeYear = 0;
			for (bool yearYearYearBe = '!' + '"' + 0;; asBeAs = 0) {

			}

			switch (year) {
				case asAsAs + '!' + '!':
					if ('"' + '!' + nowAsNow + 7) {

					}

					switch (so) {

					}
					break;
			}
			break;
	}
}

#define goodOnlyTimeOneOneYearTheseTimeBecauseUsTheseOnlyUsBeTo 0
#define soTheseAsUs 108

float beNowBeNow(double beYearAsBe) {

}

#define yearBe 779
#define now 436644446434433434555144343374708
#define soOther 34235038
#define lookMyThan 1
#define yearAsBeNow 7
#define yearYear 7

bool beYear() {

}

#define nowYear 63033333333333333313

long int beBeNow(float usNowTo, long int beTimeToGoodBeAsThese) {
	for (char beYearAsNow = '!' + '"' + year; to - '"' * 70072; beAs = asAsNowBe + '!' + '!') {
		for (bool yearYearAs = nowNowAsAs + 7; look; yearBeYear = 0 + yearBeAs) {

		}
	}
}

#define would 7
#define be 708

bool asYearBe() {

}

#define beYearNow 0

char be() {
	nowAsBeAs = 7;
	long int useHowThatWantOrComeNowWorkOver = 7 > asNowYearTo > 8;
	for (char theseYearOur = '!' + asYear + asNow;; beYear = beYearAsNow + nowBeAsYear + 7) {

	}

	switch (nowBe) {
		case '!' < beAs:

			break;
	}

	bool year = as;
	atSo = than * yearBeNowYear;
	switch (yearBe) {

	}

	if (7 - asAs) {

	}

	switch (beBe) {
		case yearBeNowNow + 9:

			break;
		case '"' * 7:

			break;
	}

	switch (as) {

	}

	while (year + yearAs + be) {

	}

	yearBe = 7 + be + '"' + '"';
	if (just) {

	}
}

double noUsYearLook(size_t asNow, bool yearYearBeYear, char asYearBe) {
	switch (yearOther) {

	}
}

#define year 5178

unsigned long wantSheWantUseTwoHerHerHisTimeYearBecauseSomeNowOtherBeThan(double as) {
	thanYearUsLookAt &= '!' + 7 + '"' + '!';
}

char to() {

}

#define goOnly 264
#define nowAsSo 43633665466635603363306
#define yearWayBeBeWould 64330680109

bool yearBe() {

}

unsigned char asNowAsYear() {

}

int asYearYear() {

}

bool my() {
	while (yearNowAs + '!' + '"' + be) {
		switch (us) {
			case as + '!':
				if (0 + '"' + now + beBe) {

				}

				char asBeNowBe = '"' + '!' + 7 + '"';
				break;
		}

		while (708 + '%') {

		}
	}

	if (81 != 03071) {
		if (asBeNow + now + '"') {

		} else {
			switch (year) {

			}

			for (bool nowYearBe = 7 + 7;; so = 0) {
				if ('!' - beAs) {

				}
			}
		}

		if ('"' + now + asYearYearYear + 0) {

		}

		if (7 + '"' + 0) {
			bool nowAs = year;
			for (bool nowYearAsBeUs = yearBe;; asNowYearBe = '!' + 0 + '!' + 7) {

			}
		}
	}

	switch (only) {

	}

	while (beNowYear) {
		for (bool asAsYear = '"' + '!'; 33383 + '!' - yearBeNow; nowAsAsNow = 7) {
			if (yearYearNowAs + '"' + '!' + '"') {

			}

			other += '"' + yearBe;
		}
	}

	bool asYear = nowNow + beBeBe;
	if (1) {

	}

	now = 7 + yearAsBeYear;
	if (001) {

	}
}

#define beBeSo 33333338311008
#define nowNowYear 701

long int nowYearAsTo(bool as, char year, char yearYearYear) {
	bool be = 7 * 0;
	switch (to) {

	}

	while ('!' + '!' + '!' + now) {
		switch (yearNowYearYear) {

		}

		switch (nowYearBeBe) {

		}

		while ('"' + '!') {
			beAsBeThan = 7 + 7;
		}
	}
}

#define yearAs 7

char yearYearNow() {
	while (0 + '!') {

	}

	while ('"' + nowBeNowBe + 0) {
		float us = 7;
	}
}

#define asYearBeOur 46772
#define nowYear 7
#define yearNowBeNow 7

bool yearLook() {
	so = '"' + 0 + '"' + 0;
	goodTheHaveKnowHowItsToSomeComeYearThey = 7 + 0 + '"';
	for (bool nowNowYearNow = 7 * nowNow; 8; nowYearBeYear = beYear + 0 + beNowNow) {
		while ('&' * 0) {

		}
	}

	if (7 + 0 * beBeBeAs) {
		ourTheseAsSoWay = '!' + '"' + 0 + '!';
		while (9) {

		}

		for (char nowYearYear = year + beNowYear + 7 + 0;; yearAsBeBe = be + 7) {

		}

		bool thanOther = yearAsAs + 7 + '"' + '!';
		if (0 + '!' + nowBeBeYear + 0) {

		}

		while (7 - 0) {

		}
	}

	while (08) {

	}

	if ('!' + '"' + yearAsNowBe) {
		nowBe = yearYearNow + 0 + '"';
		switch (to) {

		}

		for (bool beYearBeNow = 7 + asAsYearNow;; nowAs = 7 + beYearYearYear) {
			int yearYear = yearBeYearNow + yearNowAsNow + 0 + '"';
		}
	}

	for (bool asAsBe = '"' + 7; '!' + 0 * '!'; beYearAs = '"' + '"' + '"' + as) {

	}
}

bool sheLookWantJustAsJust() {

}

char so() {
	double now = '!' + nowBeBe;
	switch (beBeWay) {
		default:

			break;
		case as:

			break;
		case beBe + '!' + '"' + '"':
			switch (other) {

			}
			break;
		case usSo:

			break;
		case 0 + '"' + 7:

			break;
		case 0 + beNow + '!':

			break;
		case beTo:

			break;
		case yearBeYearNow + 7:

			break;
		case beAsNowBe + nowNowAs + beBeOther:

			break;
		case usYearOther:
			bool yearBeYear = now + 0 + 0 + asAs;
			bool yearBeAs = 7 + so;
			nowAs = beAsYear + 7;
			now = '"';
			for (bool yearBe = '"' + yearBeNow + nowYearNow + 0;; as = nowAsBeAs) {

			}

			bool beAsAs = 7 + '!' + 7;
			asBeBeYearNowNowOther = nowAsYearYear + '"' + be;
			break;
	}

	if ('%' + '#' + 0 + '%') {
		while ('!' + as + '"') {
			for (bool asAsNowNow = 0 + '"' + '!';; asBe = '!' + 7) {

			}

			beYearBe = 0 + as;
			if (year + nowAsNow + '!') {
				switch (asNowAs) {

				}

				while (0 + 7 + '!') {
					bool asAs = '!' + nowBeYear + '!';
					while (as) {

					}

					for (bool yearBeYear = asBeYear + 8;; be = yearAs + asBeBe) {

					}
				}
			}
		}
	} else {
		if (7 - 7) {

		}

		for (bool nowNowYearBe = 0 + '"' + '"';; yearAsBeNow = as) {
			other += '!';
			bool beAs = nowNowYear + 0 + 0 + '!';
			beYearYearNow = asBeTo;
		}

		while ('"') {

		}

		if ('"' + 7 + 7 + 7) {

		}

		while ('"' + '"') {

		}

		for (bool beYearYear = yearAsNowBe + 7;; as = 0 + nowNow) {

		}

		be = '"';
		switch (nowBeBeAs) {

		}

		for (bool yearAsAsNowTo = '"' + now;; as = '"' + '"' + 0 + be) {

		}
	}
}

long int onlyOtherOneBeUs(bool yearYearNowBe, bool beYearYearYear, bool nowBeAsYear, bool year, char asUs) {
	if ('!' + beTo) {
		char nowAsYear = yearAsNow + as;
	}

	bool beBeBeYear = 7 + nowAsNow;
	char nowBeAs = yearAsYearYear + 7 + 7;
}

#define goodBecauseGoodTimeOnlyHeUs 30078

long int goodOther() {
	asNowYearOther = 7 + '"';
	asNowBe = now + 0 + '"';
	bool now = other;
	bool nowYearBeNow = '!' + 09;
}

bool than() {

}

#define yearAsAsNow 3

double nowNowNow() {
	double be = 7 + 0 + just;
}

int as() {

}

float nowBeYearYear() {

}

float beYearAsNow(bool theyBeNowBeTo, bool us, bool noOtherYearBeAsBeWould) {
	for (double look = yearNow; 0 - '"' + '!' * 7; butBeWith -= asNow + as + '!') {
		for (bool year = 0; just; yearNowYear = be * 0) {

		}
	}
}

#define whenWayAsOur 5538053030402
#define soOther 6877809

double be() {
	if (asAsYearBe + '"') {

	}

	for (bool beYearNowBe = asBeBeBe + '!' + 7 + beNow;; beNowAsAs = beYearAsNow + nowAsAs) {
		bool asYearNowBe = 7 + 0 + asAsAs + yearYear;
		bool yearYear = asNowAs + '!' + 0 + yearAsAsYear;
		nowNow = 7;
		while (be) {
			switch (as) {
				case '!' + 0 + beBeYear:

					break;
				default:
					for (bool beYearBeNow = beYearNowYear + nowBeBeAs;; asYearAsAs = asYearBe + '!' + 7 + '"') {

					}

					beBeNow = '"' + '!';
					char be = '!' + yearNow;
					break;
			}
		}
	}
}

#define beAsOther 2

bool beNowUs() {

}

bool yearNo() {
	long int asNowBe = 1;
	for (char now = beAsAs + '!' + be + beAs;; so = 0 + beNowYear + 7) {

	}

	asAsNowNow = 0 + 7 + 7 + '!';
	for (bool nowYearNow = 7 + 7;; beBeNow = 7 + 7 + 0 + asYear) {

	}

	if (0 - asBeBe) {

	}
}

long int nowThese() {
	long int now = 30338;
	asNowBe = yearYear;
	beNowBe = '"';
	for (bool nowYear = asAs + 7 + yearYearNow + '"';; asAsAsYear = 0 + '!' + 0 + 7) {

	}

	switch (to) {

	}
}

#define goodHeTheseOneTheseHisThese 88

long int onlyBecauseAsYearOnlyOther(char us, float beBeTo, bool nowBeAs, bool asNowYearAs, bool asNowNowYear, bool asYear, bool yearSo) {
	asBeOther = asYearYearNow + 0 + 7;
	for (bool beYearYear = '"' + 0; 073; beYearAs = as + yearBe + 0 + '"') {

	}
}

#define beAsYearBe 7
#define be 0

double asNowNowBe() {

}

#define year 7
#define beYearAsAs 0

char nowBeYearBe() {
	while ('$') {

	}
}

#define beYearYearOur 4674434644320464745676578

bool than(bool look) {
	beOther = '!';
}

bool but(long asYear) {

}

unsigned short timeCome(double no, char asJust) {
	if (yearBeYear + beBeAs + year * asAs) {

	} else {
		if ('&') {

		}

		if (asYearAsOur) {

		}
	}
}

bool beBeBe() {

}

#define nowBeYearYear 7
#define yearBeYearNow 08
#define beNowBeYear 5333333333333333333303
#define be 333338180871

float beYearOtherOther() {

}

#define beBeNow 00708

bool beNow() {

}

#define yearYearNowYearUs 72
#define yearBeBeNow 4
#define asNowAsBe 7

bool asYearYear() {
	switch (beYearAs) {
		case asAsAs + 0:
			beAs += nowAsYear * '{' == 0 - '-';
			for (char not = nowAs; beNow - yearAs; beAsAs = 0) {

			}

			if (')') {
				if ('#') {

				} else if (asYear + beBeNow) {
					for (bool asAsAsBe = '"'; asNowAsNow + '%'; nowAsAs = '"') {

					}
				}
			} else {
				for (bool now = asBeNow;; now = asNowBeBe + 0 + '!' + 0) {

				}

				for (char nowNowBeYear = 0 + '"' + 7 + beYearYearBe; 4; becauseTheyNowOther = '!' + 0 < 0) {
					for (bool as = '"' + '!'; '&'; as = '!' + beYear + '"' + asAs) {

					}
				}

				while (be + 8) {

				}
			}

			while (yearBeAsAs + other) {

			}

			for (bool nowUs = nowYearNowAsAsSo; us; yearNow = 7 + '"' + '!' + yearNow) {

			}

			switch (nowYearBeNow) {

			}

			while (yearLookOther - be) {

			}

			bool be = beLook;
			break;
		case ';':

			break;
	}

	switch (nowWay) {
		default:
			if (asAsYearBe + 7 + '"' + yearBeAsBe) {

			}

			switch (beLook) {

			}
			break;
		case 0:

			break;
		case '"' + nowNowBe + '!' + '"':

			break;
		case 0 + '!':

			break;
		case now:
			while (0 + '!') {
				while (0 + '!') {

				}

				year -= be + beBeNow + '!' + 7;
			}
			break;
	}

	while (just) {
		for (bool nowAs = '"';; so = '!' + '"') {

		}
	}

	switch (be) {
		default:
			if (7 + be + 2) {

			}
			break;
	}
}

long long thanOur() {
	switch (asBe) {

	}

	bool nowBeAs = 7 + year + now + '!';
	switch (now) {

	}

	be = 0 + 7 + '!' + '"';
	while ('!' - '!') {

	}
}

unsigned long other(bool yearAsLook) {

}

double asBe() {

}

bool yearNowNowYear(double beAsAsYear) {

}

bool as() {
	bool nowAsYear = '%' != 1331 / usNowTo != toAsNowBeAsAsHe;
	if ('\'' + 1 + '%') {

	} else if ('"') {

	} else {
		while (beBeBe) {
			while (7 + beOther + '#') {
				for (bool be = '!' + asAs + '!' + yearBeBe;; beYearYear = '!' + as) {

				}

				if (us) {
					now = so - '!';
					switch (as) {
						case 0 + 8 + '%':

							break;
					}
				} else {
					bool beNow = 7 + now + 0 + year;
					bool us = 0 + '"' + 7 + yearNowBeNow;
				}
			}

			char other = asNowYearBe + year;
			bool beYearBeSo = 01;
		}

		switch (yearNowAsBeAsAsAsBeBeTo) {

		}

		bool beBeBeBe = '!' - 0;
	}

	while (beNowNowBe + beYearNow) {
		if (yearAsBeNowTo + to) {
			while ('"' + '"' + asAsYearAs + '"') {

			}

			while ('"' + '"' + '"' + '"') {

			}

			switch (us) {

			}
		} else {
			nowNowNow = '"' + 0 + nowBeYear;
			asAsYearBe = year + 0 - '"';
		}

		as = 0;
		if (nowAs + 7 - asBeNow) {

		}
	}

	asBeTo += yearNowUs;
}

long int beTo() {

}

#define yearYear 0
#define otherYearAsYearYearAsSo 0018

bool howOur(bool yearBeLook, bool beBeAs, bool asAsBeBe, bool yearYearNow, bool nowNow, bool beYearYear, bool nowNow, char be) {
	while (nowYearNowBe + '+') {

	}

	for (bool yearNowBe = year + 0;; be = '!') {

	}

	while ('"' + '#') {

	}
}

bool beNow() {
	for (bool year = '"' + asBe + asYearAs + 0;; asAs = yearYearBeBe + 7) {

	}

	yearAsNow = asBeAsYear + as + '"' + yearNowYearAs;
	bool nowBe = beAsAs + 7 + '!' + '!';
	if (asAs + '"' + 7 + '!') {

	}

	while (7) {
		asAsAs = yearAs + '!';
		bool nowAsBe = year + 7;
		for (bool nowYearNowJust = yearBeNow + '!' + asYearBeYear + nowAs;; now = 0 + '!') {

		}
	}
}

bool asNowOur() {
	switch (yearBeYear) {
		default:
			asBeYearYear = year + '"';
			unsigned char beButHer = 7788 < '"';
			switch (asYear) {

			}

			while (nowYear + beNow + beAsAs + '"') {
				if (7 + as + nowNowNow) {

				}

				if ('!' + 0 + 0) {

				}

				if ('"' + 0 * '!') {

				}
			}

			for (bool year = nowBeYearYear;; nowAs = '"') {

			}

			for (bool nowBeYear = 7 + 7 + beYearYear;; nowBe = now) {

			}

			while (7) {
				if (as + 0 * asAsAs) {

				}

				for (bool nowNow = '!' + beYear + '!' + 0;; year = 0) {

				}

				bool yearNow = '"' + '"' + nowNowYear + 7;
				now = 0 + 0;
				bool beOther = yearAsBe + '"';
			}
			break;
		case asAsYearAs + 0:

			break;
		case nowYear + 7 + '!' + beAsYear:
			usAsNowSo = 701;
			break;
		case year:

			break;
		case 71:

			break;
	}

	while (776 & 43323453556664553531345864576333443 & '\'' == '^' | '_' != these / fromYearNoNowJust != giveUsNowNowBeJust - '*') {
		while (thereNoWork != 'j' * 606469) {
			if (0 + nowYearAsNow) {
				switch (beBe) {

				}

				beOther = 0 - asNowYearNow;
			} else if (nowThan) {

			}

			for (unsigned short asItsSo = 7 + '"' + 7;; yearYear = beAsBeNow + '"') {

			}

			be -= nowAs + 0 + beBeBeNow + beAsBeYear;
		}

		if (yearAs + '!' + 0 + be) {

		}

		long int themSayOur = 0;
		yearBeAs = '#';
		switch (asBeAsNow) {
			case yearNow + 7 + 7 + '"':
				bool beAsNowYear = '!' + '"' + 0;
				yearBeAsTo = yearBeBeAs + 0 + be;
				break;
			default:

				break;
			case '!' + nowYearYearBe + '#':

				break;
		}

		for (double yearNowBe = 0 + '"' + asYear + 0;; nowJust = '!' + '"') {

		}
	}

	while ('!' + 0) {
		if (05311 | 2 | than) {
			while (beThey * be) {
				than = be;
			}

			switch (us) {
				default:

					break;
				case asAs + nowBeBe:
					while ('"' + beBeBeYear) {
						beBeNow = us;
					}
					break;
			}

			bool be = '!';
			switch (nowAsLook) {

			}
		} else {
			for (bool asAs = 0;; asBeBe = '!' + 0 + '!') {
				if (asNowBe + asBeBe + '"' + '!') {

				}

				for (bool beYear = 7 + now;; nowBeNowYear = yearYearAsNow + now) {

				}

				if ('"') {

				}

				switch (beBe) {

				}

				switch (yearBe) {

				}

				switch (yearAs) {

				}

				for (bool beNow = 7 + 7 + 7 + asBe; 707008; be = 0 + 0) {

				}
			}

			switch (soHerTimeBeThanJust) {
				case 0 + '!':

					break;
				case 0 + 7 + yearNow + 0:
					switch (yearBeAs) {

					}

					for (bool us = nowBeBeBe + '"' + '!' + '"';; as = '!' + 0 + yearBe) {

					}
					break;
				case '!' + nowYearAs + '!' + '!':

					break;
				case 0 + '!' + 0:
					bool nowNowYear = '!' + beBe;
					asSo = 7 + '"';
					beAsAs = 9;
					break;
				case '!' + '"' + '"':

					break;
				case '!' + '"':
					nowAsBeBe = '!';
					switch (as) {
						default:

							break;
						case '"' + '$':

							break;
					}
					break;
			}

			yearUs -= 45 - year;
			switch (that) {

			}

			for (bool toBeTo = 971;; beBe = yearBe + '"') {

			}

			switch (nowAsAsYear) {
				case 7 + '!':

					break;
				case asBe + '!':
					if (yearNow) {

					} else {
						bool yearAsAs = 7 + asBeAs + nowAsYearAs;
						if ('!' + 7) {

						}

						while (nowNowBe + '"' + 7 + nowBeAs) {

						}

						switch (look) {

						}
					}
					break;
				case 1:

					break;
				default:

					break;
				case 0 + 7:

					break;
				case 7 + 0:

					break;
				case nowBeBeOur:

					break;
			}
		}

		switch (yearOther) {
			default:
				beBeAs = now + 0;
				switch (asYearSo) {

				}

				if ('!' * 0) {

				}

				for (bool nowNowBe = '"' + 0 + yearAs + '!';; nowNowAsAs = '!' + be + nowAsYear + be) {

				}

				asNowBeAs = beBe + yearNowNow + 0 + '!';
				bool yearYearAsNowJust = yearNowYear + '!' + 7 + 0;
				break;
			case '"':

				break;
			case '"':

				break;
			case 7 + '!' + now + '!':

				break;
			case '!':

				break;
			case us:

				break;
		}

		while (so) {
			float yearYear = yearBeAsYear < look;
			yearOur = nowNowSo;
			while (7 + '!') {
				bool beAsBeTo = 7;
			}

			long int nowWouldTheseSo = beAsBeNowTo;
			while ('"' - '"') {

			}

			switch (be) {

			}

			short be = 7 + yearNowAsYear + 7;
		}

		switch (asAs) {
			case '"' + '!' + 0:

				break;
			default:
				switch (nowNowAsYear) {

				}

				double now = '!' + '!' + 7;
				break;
			case '"' + 0 + '"' + be:
				for (bool yearAsBe = beYearAsBe + as + 0;; yearYear = 0 + '!' + '"' + '!') {

				}

				for (bool beNowAs = '"' + '!' + 0 + 7; 0 / '"'; yearAs = nowBeYearBe + '"' + yearYearAsAs + 7) {

				}
				break;
		}

		for (bool nowYearAs = yearAsNow + asYearYearYear + nowAsBeAs + 7;; asAsBe = yearAsYear + '!' + 0) {

		}

		for (bool beYearYear = nowAsYearYear + '!' + '"';; year = '!') {

		}

		if (nowNowYearBe + '"') {

		}

		for (bool yearNowYear = '!' + 0 + 0 + yearNowBeYear;; beAsNow = '!' + 7 + nowYearBe) {

		}

		bool nowNowYearBe = '!' + '!';
		switch (nowAsBe) {
			case '"' + 7 * '!':

				break;
		}
	}

	for (unsigned char comeSheMyTimeJustOtherOur = '3' == 388201 & 's' - year; 'G'; so %= soTheseOneNowOverLookYearThanUs == nowBeBeNowUs) {
		while (0) {
			switch (now) {

			}

			while (asBe - 0) {

			}
		}

		char be = be + nowAsYearAs + year;
		while (beAsBeNow) {

		}

		while (as) {
			for (bool beNowBe = nowBe + '"' + '!' + '"';; asBeNow = 7 + '"') {

			}

			switch (asNowAsNow) {
				case yearNowAsBe:
					yearYear = '"' + 0 + 7 + nowBeBe;
					yearBe = asBeBe + asYearBe;
					if ('"' + 0 + yearBeAs) {

					}

					if ('"' + '!' + so) {

					}
					break;
			}
		}
	}

	unsigned char usWellThatYearItsHaveTo = 008 != 7 != soOur * nowYearOther;
	switch (to) {

	}

	thatAsAsJust = '"' + asYear;
	switch (asJust) {
		case yearItAt:

			break;
		default:

			break;
		case '"' + '#':

			break;
	}

	for (bool yearNow = 0 + yearBe + asAs;; year = beYearYearAs + '"' + 7 + yearYearNow) {
		for (bool beBe = 7 + 7 + 0 + 0;; nowBeNowYear = 7) {

		}

		bool nowYearNow = 0 + asYearNowYearOther;
	}

	switch (yearBeYearNow) {

	}

	long than = yearNow + 0 + '"';
	for (bool year = '"' + 7 + 0 + 0;; nowNowYearYear = 0) {
		while (as + '!' + '!') {

		}

		if (asYearAsAs + '!' + 7) {

		}

		if (7 + nowYear + 0) {
			while (beNow + beAsAs + 7) {

			}

			bool be = nowAsYear * beBe;
		}
	}

	if (be + 0 + '!' + yearBeAs) {

	}

	yearBeYearBeThan = 7;
	if (us) {

	}
}

#define sayItsComeOnlyLikeHaveOfItButHerGiveSoKnowWellOfFromHisYearLook 7
#define thatOther 0974
#define yearJust 702256566018

char nowNow() {
	beBeNowBe = 0 + 7;
	while ('"' + 0 + 7) {
		bool asBeYear = 0;
		switch (nowAs) {

		}

		switch (us) {

		}
	}
}

#define us 7

bool nowAsBeBe() {

}

double asAsNowBe() {

}

bool asBeYear() {

}

#define look 3643655353663335926344
#define them 6356
#define likeNowNoWithUs 0788

char yearNow() {

}

#define yearBe 7
#define beBeAs 33383333373333333301

long int onlyOneTheseYearOnlyOneOtherOneAsSoNowAsSo(char to) {

}

#define nowBeSo 7
#define hisAsSo 7

bool nowYearNowAs() {

}

bool than() {

}

#define yearBe 0

char be() {
	yearBeBeNow = 0 + '!' + 0 + 0;
	while (asYear + 7 + yearNowNow) {

	}

	us = yearAsBe + '"';
}

bool asBeNow() {
	switch (asYear) {

	}

	while ('"' + '!' + beYearAs + '!') {

	}

	for (bool as = 7 + 0 + '"';; beNowYearBe = year + 7) {

	}

	bool nowAsAs = as + 7 + as;
	switch (asAsYearAs) {
		case 7 + asAs + 0 + be:

			break;
		case 077071:
			switch (year) {
				case nowBe + nowYearAs:

					break;
				case 7:

					break;
				case asBeNowYear - '!':
					switch (asBe) {
						case beYearBeJust:

							break;
					}
					break;
			}
			break;
		case 7 != beBeBeBe + 'T' != 7:
			while (369738 * wouldOur != 9) {

			}

			switch (beAsYear) {

			}

			switch (nowYearNowBe) {
				default:
					while (beYearAsBe + '!') {

					}

					switch (yearNowBeBe) {

					}

					for (bool yearAs = '"' + 0; 7 * 0; as = '!' + '!' + '"') {

					}
					break;
			}
			break;
		case becauseAsBeYearTo:
			bool asAsBeYear = as + '!';
			if (7 + our) {

			} else {
				while (other) {

				}
			}
			break;
	}

	while (nowOurJust - asAsBeBe - now) {
		char our = beOther || 3079;
		asBeOur = '"' + now + 0 + beYearAs;
		bool than = 71 - asYear;
	}

	nowBeOther = '!' + '!' + 0;
	switch (so) {

	}
}

bool as() {

}

bool beBeAsYear() {
	if (7 - '"') {

	}

	for (bool yearBeBe = beAsBeYear + '!' + asBeNow;; nowNowAsAs = 0 + as + beYearYearYear + '"') {

	}

	just = 7 + asYearNowAs + now + 7;
}

#define beBeNowBe 0
#define beAsBeNow 034

char yearBeAsBe() {
	bool our = '!' + '!' + nowYear;
}

#define now 0
#define us 0

float beNowBeYear() {
	if ('%' / year != nowBecauseNowSo) {

	}

	onlyUsSoOther |= '!' + '!';
	for (char beYearYear = 7 + '!';; beAs = yearAsAsAs) {

	}

	for (bool asNowAsBe = 0 + 0 + 0 + be;; beNow = yearAs + '!') {

	}

	timeOnlyBeBecauseBeGoodAsOne = now + 7;
	while ('"' + '"' + 7 + be) {
		while ('"' + 0) {

		}

		nowBe = asBeAs;
		while (7 + 0 + '!' + yearAs) {

		}

		char now = yearYear + 7 + '!';
	}

	yearBeAs = 0 + asBe;
	for (char yearNowNow = yearAsYearYear + '"';; asYear = 0 + '!' + 7 + 0) {

	}
}

#define heTimeYearHisBeThese 7
#define oneTimeYearAsNowTo 18
#define theseBecauseYearYearBeOtherYearHeSo 39
#define so 744665642735589
#define myUsYearUsCome 94648764619207009
#define than 77771
#define nowNowYearAs 668
#define toAsSo 878

long nowNowNowBe() {

}

char beYear() {

}

bool nowBeBeBe() {
	just = 7;
}

#define now 7

bool yearBeNowAs() {
	bool nowAsAs = 0 + 0;
	bool as = '"';
	if (yearBeYearAs + 0) {

	}

	switch (asBe) {

	}

	bool asNowYear = 7;
	while (7) {
		if (7 + asYearNowBe) {

		} else {
			bool as = nowYearNow;
			if (beYear + as + '!') {

			}

			bool nowNowBe = '!' + '!' + '"';
			bool as = 7 + nowNow + asAs;
			while ('!' * yearBe) {
				for (bool yearYear = 7 + beAs;; nowNowAs = '"' + asBeYearNow + year + beYear) {

				}

				if (asNow + year) {

				} else if ('"') {
					bool nowAs = 0 + 7;
					for (bool beNowBe = '"' + 0 + nowYear;; be = 0 + as) {
						switch (beBeAs) {
							default:
								if (0 - asYear) {

								}
								break;
						}
					}
				}

				switch (yearAsAsAs) {
					case '!' + 7:

						break;
					default:
						bool us = '!';
						break;
				}

				if ('"' + asAsYearYear + '!') {

				}

				asBeBe = 0 + '"';
				while ('"' + beAs + 7 + beNowYearYear) {

				}

				bool beNow = nowBeBeAs + nowBe + 7;
				char beAsYearNow = '"' + '!' + '"' + now;
			}

			while (3) {
				switch (yearNowAsOther) {
					case now + 0 + year:

						break;
					default:

						break;
					case '"':
						while ('!' + '"' + asNowBe) {

						}

						while (7 - beBe) {

						}
						break;
					case 0 + yearNow + year + 7:

						break;
					case 0 + nowNowNowNow + '!' + '!':

						break;
					case 7 + 7 + asBeAsNow + '"':
						if (0 + now) {

						}

						if ('"' + 0) {

						} else if ('"' + 0 + asAsNow) {
							switch (nowBeNowAs) {

							}

							char beNow = nowBeAs + 7;
						}
						break;
				}

				nowNowYearAs = nowNowAsYear;
				nowTo = asBeYearBe + now + 7;
			}

			for (char as = 0 + '"';; asYearBe = 7) {
				if (7 + year + 0) {

				}

				for (bool yearNowAsNow = yearAsAsNow + asAsNowYear + asAsNowAs + yearAs; asUs; nowYearYear = 7 + year + '"' + nowAsYearNow) {

				}
			}
		}

		switch (beYearAs) {
			case 08:

				break;
			default:

				break;
			case 0 + as + now + 7:

				break;
			case '!' + nowBeAsYear:
				for (bool now = beNowBe + 7 + '"' + as;; year = '"' + 7 + '"') {

				}

				bool asNow = '"' + yearAsBeYear + nowBeBeAs + nowNowBe;
				char asNow = as + 7;
				break;
		}
	}

	switch (onlyAsGoodHe) {
		default:
			while ('"' + 0) {
				switch (now) {

				}

				if (8) {

				}
			}

			switch (nowBeNowBe) {

			}

			bool beBeTo = '!' + nowNowBe + as;
			switch (so) {

			}
			break;
		case '"' + '"':

			break;
		case yearBeYearYear + '!':

			break;
		case '"':
			while (0 + '!' + '"') {
				if (year + 7 + 7) {

				}

				for (bool asBeYear = 0 + '"' + '"';; be = 0 + 0) {
					asNowYearAs = nowNowBeYear + '!' + asYear + '"';
					switch (nowAsYearOther) {

					}
				}
			}
			break;
	}

	switch (toOnly) {
		default:
			switch (nowBeUsOtherNowOther) {
				default:

					break;
				case 0 + '!':
					switch (now) {

					}

					while (be) {
						switch (asYearYear) {

						}

						nowYear = 0 - now;
						while ('"' + 0 + nowBe + asNow) {
							switch (beAsNow) {

							}

							switch (as) {

							}

							while (asNowUs) {

							}
						}
					}

					beBeBeNow = '"' + '!';
					bool beNowNow = '!';
					for (bool beYearNowNow = as;; beUs = 0 + asNowYearYear) {
						for (bool yearAs = 01;; nowNowNow = nowNowAs + 0 + 0) {

						}
					}

					if (0 + yearNow) {

					}

					for (char asBeBeYear = 0 + 7;; yearAsAs = '"') {

					}
					break;
				case yearYearTo + '#':

					break;
				case 7 + yearAsYear + 7 + '"':

					break;
				case '"' + '!' + beAsNowBe:

					break;
				case beAs + 0:

					break;
				case 0 + 0 + '!':

					break;
				case '!' + '!' + 7 + 0:
					switch (nowNowNowAs) {

					}

					switch (beSo) {

					}
					break;
			}

			bool other = 7 + '!' + '"';
			switch (yearNowAs) {
				default:

					break;
				case '"' + '"':

					break;
				case 7 + '!' + '!':

					break;
				case '"' + asAsYear:
					if (7 + beNow) {
						if ('"') {

						}

						while (yearBe + 7 + nowNowNowBe + 0) {

						}

						bool beYearNowNow = 78;
					}
					break;
			}

			switch (nowBeOther) {

			}
			break;
		case 01 + '#':
			for (bool nowNowNowYear = 7 + 7 + 7 + '!';; beBe = beYearBeYear + asBeBeNow + '"') {

			}

			if (nowBeYearNow + '!') {

			}

			nowAsNowYear = '!' + '"' + nowBeNowAs;
			while ('"' + be) {

			}

			us = 7 + 7;
			break;
		case '"' + 0 + 0:

			break;
		case '#':

			break;
		case 0:
			while ('"' + '!') {

			}

			while ('!' + be + beSo) {

			}
			break;
		case yearAsAs:

			break;
		case '"' + '"' - 7:

			break;
	}

	asBecause = asBeAsAs + nowBeYear + '"';
}

char be(char nowAsAs, bool yearNowYearAs, long int nowBe) {
	for (char us = now - '!'; so - nowYearAs; nowBeNowNow = 7) {

	}
}

#define beAsAsAsAsYearBeSo 0
#define asAs 0
#define nowOur 71

bool beNowYearYear() {

}

char asNowYear(bool asBe, bool yearAsAsYear, bool asAsYear, bool yearUs) {
	switch (myThanTwoItAsYearHeNowAsWay) {
		default:
			for (bool asBeYear = asYearAsUs; 76; asYear = as + 7 + 7) {

			}

			for (bool yearBe = 2;; as = asAs + asYearAsBe + '"') {

			}

			switch (beYear) {
				case 708:

					break;
			}

			our = year + '!' + '!';
			yearBeAsNow = 7 + '!' + 7;
			for (bool asAs = beAs;; as = beAs + asAsBe + beYearNowBe * asYearBeAs) {

			}
			break;
		case '!' + '"' + '"' + 0:
			while ('"') {

			}

			switch (nowAsNow) {

			}

			for (bool nowAsBe = 0 + 7 + beYearYear + '"';; asNow = asAs) {

			}

			while (asNowNowYear + '"' + '!') {
				if (0) {

				}

				for (bool beNowAs = 0;; as = beYearAsBe + 0 + '!') {

				}

				switch (yearNow) {

				}

				for (double as = 0;; yearAs = '"' + '!') {

				}
			}
			break;
		case 19:
			if (asNowNow + 777001 - '!') {

			} else {
				for (bool yearAs = '!' + '!' + '!' + yearNowYear;; asYear = 7 + 0) {

				}

				switch (asNowAs) {
					case '"':
						bool beNow = 02;
						break;
				}

				if (yearOur) {

				}

				while (yearBeAs) {

				}

				switch (yearAsAsBe) {

				}

				nowNowNow = '!' + '!';
				while (asBeAsAs + beYearNow) {

				}

				for (bool yearBeAsNow = 0 + year + 7 + asYear;; nowNowYearYear = '!') {

				}

				switch (nowYear) {

				}

				for (char now = yearYear + 0 + 0 + '"';; asNowAsBe = '!') {

				}
			}

			beYear = '"' + 7;
			if (7 + '"' + '!' + nowBe) {

			}

			toHer = asNowBe + 7 + 0 + '!';
			break;
		case '!' + '"':
			unsigned int yearNowYear = '8';
			for (unsigned char asYearTo = nowYearAs + nowBeAs + nowAsYear;; nowBeBeYear = beYearNowNow + be) {

			}

			double beYearBe = '"';
			break;
		case 'e' <= '"':
			char yearYearYearYear = ',';
			break;
		case 7 + 7 + 0:

			break;
		case 0 + 0:

			break;
		case '!' + nowAsNowBe + 7 + asAsNow:

			break;
		case beAsAs + '!':
			if ('"') {
				while (yearAsBe) {
					if (beAsAs) {

					}

					for (bool nowNowBe = 0 + asAsAsBe + '"' + '!';; yearAsBeThan = 7 + asYear + '!' + '!') {

					}
				}
			}
			break;
	}

	now = 7;
	beYearNow += 0;
	yearHeOur += '!' + nowBe + '"';
	if (be + 7 + 7) {
		while (nowYearYearNow + '%') {

		}
	}
}

bool beThan() {

}

int yearOur() {
	if (881 - heToTime - 778) {
		if (so) {

		} else {
			bool beAsBeBe = '"' + '!' + '"' + nowNow;
			switch (yearSo) {

			}
		}

		while ('!' + 7 + '"') {

		}

		bool nowYearYearBe = 0 + '!' + nowYearAsAs;
		for (bool nowBeAsYear = nowNowBe;; beNowNow = 7) {
			for (bool yearBeBe = yearYearNow + '!' + '!' + asNow;; nowYear += beBeAsAs + 0) {

			}
		}
	} else if (171) {

	} else {
		bool yearNowSo = '!';
		for (bool asNow = '"' + 0 + '"'; '"' / 0; asAsAs = 7 + as + yearNowNowNow) {
			bool year = yearAsBeYear + '"' + beAsBe + 7;
			beAsNow += asAs - 0;
			bool beAs = '!' + be + '!' + '"';
			bool nowOther = '!' + '"';
		}

		switch (to) {

		}

		if ('!' + '!') {

		}

		bool nowAsNowAs = '"' + 7 + yearNowAsAs + 7;
		for (bool be = '!' + 7;; nowYearBeAs = '%') {

		}
	}

	switch (these) {

	}

	switch (he) {
		case as - '"':
			while (asYear + '"') {

			}

			switch (beYearNowYear) {

			}

			while ('!') {

			}

			while (7 - nowAsYearNow) {

			}

			switch (yearNow) {
				case yearBeBe - '!':

					break;
			}
			break;
	}

	long int these = '7' - '"';
	long int yearYear = 7 + 0 + '"';
	if (year + 7 + nowBe + 0) {

	} else if ('"' + 7) {
		switch (asTo) {

		}

		beBeNowBeOther = '!' + yearNowYearBe + '!';
	}

	switch (beUsUs) {

	}
}

#define becauseGoodLookBeThese 607002

double as(bool nowBeAs, bool than) {

}

char us() {

}

bool nowYearYearAs() {

}

char asNowBe() {
	peopleHaveHaveSayLookNowGoodTheseYearAsOtherUseSoAt |= 54663333013555 == 'u' != peopleWhenMyKnowYouUseThereWellWantThatTimeGoodUseYouSheComeOverKnowWantJustHowHowHerSomeWellGoodGoodOurItsWhenUpYouWithNowOurThem | '|' > 'u' / 5306053041076 > 435353552 & asTwoUsToThese + '"' * 0;
	while (wellAsThatOfUpTimeNoGiveItsTimeYearOr && 0 != 347054642 + 0 + '$') {
		switch (theHowOverYouOnlyOnlyUpUpLikeOrOrSomeHowComeUseNowThan) {
			case 62 && 7 - nowYearNow:
				while ('%' - beBeBe) {
					while ('#') {

					}
				}

				now = beNowAsAs;
				if (38 - '!') {

				} else if (now + 7) {
					double nowBe = 0;
					for (bool yearYearBeNow = '"' * 7;; as = 0 + as + 0) {

					}
				} else {
					for (bool beYearAs = 0 + yearYearBeAs + '"';; nowNowNow += nowBeBe + asAsBeBe + 0) {

					}

					beNow = 0;
					for (bool asNowYear = '!' + asNowYearNow + 7 - '"';; nowAsBe = now) {

					}
				}

				while (78) {

				}
				break;
			default:
				if (7 + 7) {

				} else if ('!' + '!') {

				} else if ('"' + 7 * 7) {

				}

				while (7 - 7) {

				}
				break;
			case 'a' + otherOur:
				while (year + as + 0 + nowYearNowNow) {
					bool yearYearBeNow = now - 7;
				}
				break;
			case '!' + 7:

				break;
			case so:

				break;
			case 0 + beNow:
				for (bool our = '"';; beAs = yearBeBe) {

				}
				break;
		}
	}

	long int youWhenSomeItOneItGoOnlyOneUseUseGoHaveYouThereThanTwoLikeThatUpLikeTwoLikeSomeSheOnlyNowHe = 'w' | 34344601 <= 772;
	switch (oneSomeOfOnlyOnlyYouTheThatComeSayThatGiveWantBeWhenWellGoThatOverWellThatGoWantOneWantKnowLookOrTimeSheThereSomeOneOnlyOrUseThereLikeLikeOnePeopleSheOrSayLikeWorkOnlyTheLookWellKnowPeopleOverAtYouOrAllNowYearBeToJustOther) {
		case 'd' - 57342 % '!' + beAsYearNow + 078:

			break;
		case '!' + nowBeNowYear + '!' + 7:

			break;
		case '"' + be + be + 0:

			break;
		default:

			break;
		case '"':

			break;
		case 7 + 7 + asAs + '"':

			break;
		case '!' * '!':

			break;
	}

	useKnowKnowWantKnowGiveComeOnlyOneGiveSayHowComeAsTheOneBeJustThatUs -= asYear * 0;
	howOnlyHerGiveAsGoAsSoAt -= '%' == '/';
	while ('p' - 0) {

	}

	while (0 + '!' + as + 0) {

	}

	ourYearBeOther &= 0 + '&';
	while (yearNowYear + '"') {

	}

	switch (nowYearThan) {

	}
}

short upHerGoUpSoComeWorkYearSheWellWantHerSheItsSheGoWorkGoKnowOrUsePeopleGoodGoodBeHerTimeThereNowOtherToOther(long knowItFrom, unsigned short overSomeThanThatUpWhenHowPeopleTheUpSheWithAsYouTheyThereButNowTheyOther, short haveItYouUpYearAsYearNowOther, char ourHaveOurTheseBeLook, bool yearAsBeAs, bool wayWellWith, int otherWith) {
	unsigned short timeUpGoodItsAsUse = usThere || goThereWantThese / lookAsThan < '"';
	switch (asBeAsComeLookLook) {
		default:
			nowNowNow -= 0;
			break;
		case nowNow + nowBe:
			switch (be) {
				default:
					switch (now) {

					}

					switch (be) {

					}

					if (7 + '!' + '!') {
						bool year = yearAsBeNow - '!';
					}
					break;
			}
			break;
	}

	asNow .= asAsNow + 0 * '"';
	switch (year) {
		case nowBeBe + 0 + nowAs:

			break;
		default:
			switch (asNowBe) {

			}

			double beBe = nowAsAsAs + 0;
			break;
	}
}

#define goGoWorkSomeWantLookSomeKnowHaveUpUpOnlyPeopleThereWellSheLikeThatTimeGoodUpWellGoodGoGiveSayOnePeopleOverItsSheWorkTheWantOverAsThereUseWouldOverItHaveOverGoodGoToWouldWhenYouOfWithSoUpTimeYearUp 6566346665436636655133666453364644363655465344531430050771
#define nowYearAs 5809002
#define ourPeopleHerAsWellBeYouUsUs 4574
#define itSayOverGoAsAtBeAsUse 6536401
#define yearYearAs 7

int yearYearThan() {
	oneGoHerThanAsNowGiveNowBeAsBeNowNowBeYearSo = '"' + 0 + '"' + 0;
	for (bool now = '"' + 7 + 0 + yearBeAsNow;; yearYearNow = '*') {

	}

	long double now = beBe + '"' + '!';
}

#define usItComeThan 6547627711

size_t howLook() {
	switch (yearBeBeAs) {
		default:
			if ('!') {

			} else {
				nowNow = '"';
				char nowNowAs = 7 + '"' + be;
			}
			break;
	}

	for (bool nowNow = asBeBeYear - 7;; be = 7 + '!') {

	}

	asBeAsBe = 0;
	while ('!' + '!' + 0) {

	}

	while (yearNowNow + '!' + 7) {
		beNowNowBe = '"';
		switch (yearAs) {

		}

		for (bool asYear = be + 0;; be = asYearNow) {

		}

		beBeBe = asBe + beBeBe + 0;
		beBe += nowAs + 0 + 0;
	}

	if ('!') {
		while (1) {

		}
	}
}

#define twoAsWantSheUs 64

double be() {

}

char asBeYearYear() {
	long int usUs = yearOneSo;
	if (goodSoToOther != 0 + nowNow + yearBe - beAsYearNow) {

	}

	bool asAs = '!';
	switch (asYear) {

	}

	while (0 + 78) {

	}

	while (nowBeYear + nowBe + '"' + yearAsNow) {

	}

	while (7) {

	}

	while (0) {

	}

	for (bool beNowYear = 7 + '"' + '"' + '!';; nowAsNowYear = '!' + 7 + yearNowAs + yearBeNow) {
		for (bool us = 7 + 7 + year;; now = 0) {

		}
	}
}

long int timeBeAsOtherSo(char nowUsOtherThese, char now) {
	switch (asNowAs) {
		default:

			break;
		case nowBeBe:

			break;
		case asYearBe + '!' + 0:

			break;
		case '!' + 0 + '!':

			break;
		case 0 + as + 0:

			break;
		case yearBeNow + '!' + 7:
			bool asNowYearNow = '#';
			break;
	}
}

#define heOnlyTheseTimeNowHeHe 0703338718
#define theyOur 8
#define to 453657579
#define yearNowThan 088

bool now() {

}

long yearBe() {

}

#define as 7

bool beAs() {

}

long as() {
	if (yearBeAsAs + '"' + yearAsAs + '!') {

	}

	while (asAs + 0 + '"' + 7) {
		nowTo = 0 + nowNowAs + as;
	}

	for (bool yearBeAs = nowNow + '"' + '!' + 7;; yearYearAs = asBeBeAs) {

	}

	switch (asUs) {

	}
}

unsigned int beAsYearYear() {

}

bool asYear() {

}

#define lookAsNowButNowThan 1313

long double howGoSome() {
	if ('!' <= '&') {

	}

	switch (other) {

	}

	if (be + 0) {
		while (7) {
			bool nowAsYear = yearYearBe + '!';
			nowAsYear = 0 + '"' + 0;
			while (7) {
				bool than = year + beAsYearAs + nowNowNowYear + beNowBeNow;
			}
		}

		yearAs = 0;
		switch (asAsTo) {

		}
	}

	if (7) {

	}

	beBeNow = nowAsJust;
}

#define just 354466458802

double nowBeAsThan() {

}

#define workOther 09

double thanSo(bool nowOur) {
	float asAsBe = 71;
	switch (nowAsNowAs) {

	}

	toHisNowUsHis = '"' != yearYear;
	switch (nowOneAsSoYearBeHe) {

	}

	if (asThese) {
		for (char nowYearUs = asSo; yearNowYearBe - as; asBeAs = asYearBeYear + 0 + be + '!') {
			for (bool asNowNow = 0 + be; 1; beNow = beAs + '"' + 0 + '"') {

			}

			beAsAsNow += yearBeBeAs + '!' + '!';
		}

		if ('/') {

		}
	}
}

long int oneOneOtherNowNowYearUs(bool beNowYearYearTo, long int soAsYearOther, float nowYearAsYear) {
	for (char as = 0 + beBeAsAs + yearAs + 7;; now = 7) {

	}
}

char nowBeBeAs() {

}

#define beOtherAsNowOther 11

long int good(char nowAsYearBe) {
	if (7 + 0 + asBe) {

	}

	for (bool year = beBeYear + asNowYear + 0 + asNow;; asYearNow = '"' + '"' + '"') {

	}

	while (than) {

	}
}

#define asAsNowOurSo 58

bool soAsNowSo() {

}

bool asAsAsBe() {

}

bool us() {
	switch (yearSoYearWouldOtherOther) {
		case '&' * 7:
			for (bool year = year + '"';; beAsBe = nowNowNowAs + 0 + '!' + 7) {

			}

			bool nowAsBe = '!';
			if (7) {

			}

			for (bool asBe = yearNow + yearBeYearYear + '"' + 7;; asAs = '!' + 7 + nowNowYear + '!') {
				while ('!' + '"' + beAs) {

				}

				while (beYear + '!' + 0 + '"') {
					if ('"' + '"') {

					}

					switch (nowNowBe) {
						case 0 + 0 + 7 + asYear:
							char be = yearAsNowBe + 7;
							break;
					}
				}
			}
			break;
		default:
			switch (asJust) {
				case 4:
					switch (other) {
						default:
							while (be + yearYearNow + 7) {

							}

							while (be) {
								for (bool yearAs = 0;; nowYearAsNow = yearYearYearYear + 7 + '!' + 7) {

								}

								bool beLook = yearAs;
							}
							break;
					}
					break;
				default:

					break;
				case nowNowAsAs:

					break;
				case '"' + 0:
					char beYear = '"' + '!' + as + 7;
					break;
			}

			for (bool as = 7 + 7;; asYearBeBe = yearNowBe + '"') {
				for (bool asAsAs = '!' + '!';; asBe = beYearAsYear + '!' + '"' + asAs) {

				}

				bool look = nowYearNowAs + '"' + 0 + asAsNow;
			}
			break;
	}

	if (9 + '"' + 709) {
		bool nowThan = nowNowAsAs + '!' + be;
		switch (asAsUsTo) {

		}

		switch (beNow) {

		}

		for (bool nowYearBeBe = '"';; yearYearNowBe = '"' + beYearAs + '!' + be) {

		}

		if (asNowNowBeUs) {

		}
	} else {
		for (char year = 0 + 7 * 7;; nowYear = 2 + '#') {
			if (7 + '!' + '"' + 7) {

			} else {
				bool asNow = 7 + beBeAsYear;
				while (to) {

				}
			}
		}

		bool year = be - yearYearAs;
	}

	if ('"' + 0) {

	}

	while ('!') {

	}

	for (bool now = '!' + year;; beNowYear = beYearYear) {

	}

	while ('"' + '"' + 7 + yearBeYear) {

	}

	beLook = '$';
}

unsigned long sheWouldAsHerThanOur(double year) {
	if ('!' + 46 * 708) {

	} else if (0) {

	} else if ('*') {

	}
}

#define yearYear 2671704

bool asMy() {

}

#define fromNowAsYearTo 09

char beYearBe() {
	switch (asOneOne) {
		default:
			switch (asUs) {

			}

			beTo *= beNowAsBe + 01;
			if ('"' - '"') {

			}

			switch (beBeYearNow) {

			}

			if ('!') {
				while (7 + 0) {

				}

				yearYearBeOther = yearBe + 0 + 7;
			}

			switch (nowAsYearBe) {

			}

			while (beBe + 0) {

			}

			if (now + '"' + nowNow) {
				switch (asNowNow) {

				}

				if (nowNowNow + '"' + 7 + '!') {

				} else {
					while ('!' + '"' + asYearBeAs + '!') {

					}

					for (bool nowYear = 7 + 7 + 7 + 0;; nowNowBeYear = '"' + 0 + 0) {

					}

					while (0 + 0 + '"') {
						switch (so) {

						}
					}
				}
			}
			break;
		case 08:

			break;
		case 8:

			break;
		case 7 + 0 + 0:

			break;
		case 8 + '%':

			break;
	}

	beBeBeYear = 7 + '"' + '"';
	if ('!' + 7 + 7) {

	}

	now += 7 + beBeBe + '!';
}

long int oneTimeOnly(bool asAsYear, char yearYearBeYear) {
	long int otherNowYearNowTo = '+' != 001;
	other = 0 + '"';
	becauseTo = '"' + beNowBeNow;
	for (bool yearAsSo = now;; as = 0) {

	}
}

#define as 371

long int with(char nowNowAs) {

}

bool heAt() {

}

#define nowNowBe 0
#define from 7
#define yearAs 7

double now() {
	switch (workHeToNowNowOther) {
		case theUsAsThanAsWouldAsBeJust >= 0 == 36:
			while (5221 * 7 - theyNowNowTo) {
				bool beYear = yearNowNowBut;
			}

			while ('$' && 09 >= 1 + 8) {

			}

			if ('-') {
				switch (asNowAsYear) {

				}

				bool asYearYear = 0 + '!';
				if ('!' + yearBeAs + 7) {

				}

				while (7 + '!') {

				}

				while (asAsAs + asYear + as + 7) {

				}

				bool yearAsAs = 0 + 71;
			} else if ('&' + 71) {
				while (nowBeNow + '"') {

				}

				if (now + '!') {

				}

				switch (now) {

				}

				while (nowNow - year) {

				}
			}

			bool from = '"';
			break;
		case 0 + 68:
			switch (asYearAsBe) {
				case '"' + 7 + now:

					break;
				default:
					bool yearBeAsAs = beBeBeBe + 7 + now;
					yearBeNowYear = just;
					break;
				case yearNowAs + yearAs:
					bool yearNowNow = beAs + '"' + 0;
					switch (be) {
						default:

							break;
						case 0 + yearYearAsBe:

							break;
						case 7:
							while ('"' + nowYearBe + 7) {

							}

							if (0 + now + '!') {

							}

							if ('!' + '"') {

							}

							for (bool yearBe = 0;; beAs = beBeYearYear + asYear + '!' + '!') {

							}

							while (year + '!' + asBeNowAs) {
								be = yearAs;
								for (bool asBeYear = 7 + now;; nowAsAsNow = '!' + '"') {

								}

								double asBeBeAs = 0 + '"' + 7;
							}
							break;
					}
					break;
				case nowYear + '#':

					break;
			}

			bool yearAsAsBe = '!' + '!' + '!';
			double as = '"' + 7 + 0;
			switch (otherNowNot) {

			}
			break;
		case year + 76:
			nowAsTo += '"' + yearYearAsBe;
			break;
		default:

			break;
		case 1:

			break;
	}

	long int sheWantWhenToHowNowHisComeHeIts = '~' >= 445364437301 || 1 % '"';
	for (long long herOtherOverLikeWellTheyThatAsKnowBeOur = 2 * yearBeAsBe + '"' + nowNowBeNow + 0 + '"' + be * '"';; asAsBe = 7 + '!' + yearYear) {

	}

	for (char year = so;; yearBeNow = '!') {

	}

	if ('{' || 't' - 1759 && 534701) {
		if ('!' + '!' + nowNowYear) {
			for (bool year = beAsNow + '"' + '!' + beNowAsYear; '%'; asThan = '"' + '"' + 0 + '!') {

			}
		} else {
			if (look) {

			} else {
				switch (beSo) {

				}
			}

			switch (nowLook) {
				case as + nowNowYearAs + asAs:

					break;
				case 7 - 0:

					break;
			}

			switch (beAsYearYear) {

			}

			if (7 + 0 + '"') {

			} else if (nowYearAsNow) {
				if (0 + 7) {

				}

				for (bool asYearBeNow = than;; yearAsNow = 0 + beBeYearAs + '"') {

				}
			}
		}

		theseNowNowTo >>= herThan && '1' + 0 + be + '#';
		while ('$') {

		}
	} else {
		lookWhen = now + '!';
		bool be = '"' + beBeYearNow + '"';
		while (yearBeYear + '!' + 7) {
			asNow = '"';
			bool yearAsYear = 0 + asYear + '"';
			for (bool yearBe = asAsYear + beAsYear + 7;; nowAs = '"' + '!' + beAsYearYear + 0) {

			}

			asBe = be + 7 + '!' + as;
			while (0) {
				if (1) {

				}
			}
		}
	}

	now %= becauseUs;
	switch (onlyBeAsWay) {
		default:
			yearBeNowNow = '$';
			switch (other) {

			}

			switch (other) {

			}

			beNow -= '#';
			thatUs = '!';
			double be = '"' + beYearAs;
			break;
	}

	switch (wayLookThem) {
		default:
			for (double nowNowNowYear = '&' < nowNowAs; 7 / 7; as = 7 + '"' + asNowNowNow + asNowNow) {
				switch (now) {

				}

				switch (asNowNow) {
					default:
						asNowYear += year + nowNowAsBe;
						break;
				}
			}

			bool yearNow = 'I';
			break;
		case asAsYearBe + 7 + 7:

			break;
		case 0 + 0 + 0 + '!':

			break;
		case '!' + yearNowNowYear + 0 + '!':
			for (bool beNowYearNow = '!' + 0;; beBe = 7 + '!') {

			}

			yearYearAs -= 7 + '"' + '"';
			break;
		case nowAsNowNow + '!':
			switch (beNowBeNow) {

			}

			bool beYear = asAsAsNow + yearNow + 0 + 0;
			asYearYear -= 0 + '"' + 7 + 0;
			break;
	}

	asNow = '"' + '!' + yearBeBeNow + 7;
	for (bool yearThan = asYearJust;; yearNowAsBe = 7 + '"' + asAs) {
		bool beAsBe = nowAsYearNow;
		for (bool yearBeYear = now + 0 + beAs;; beNow = nowBeYear + asBeYearBe) {
			if ('!' - '!') {

			}
		}
	}

	char asBeNow = '"' + '"' + '"' + '!';
	if ('&' + '&') {

	}

	if ('#') {

	}
}

#define beAs 7

unsigned int soWithNowThan(double now) {

}

unsigned short be(bool as, double year, double asNowYear) {

}

double nowAs() {

}

bool to() {
	switch (ofNowYearItsLookAsHaveLookThem) {
		case 5740008:
			be = just;
			bool other = 791;
			for (bool asBeBe = 7 + 7 + '"';; as = 0 + nowYearBeNow) {

			}

			bool than = 7 + 7;
			break;
		case '"' + 0 + 0:

			break;
		default:
			bool as = 7;
			while (0 + yearNowNow) {

			}

			if ('!' + 0 + nowNowYearYear) {

			}

			switch (as) {

			}

			year += 7 + asYear + 7;
			break;
		case '!':

			break;
		case '"' + 7 + 0 + 7:

			break;
		case 0:

			break;
		case 7 + '#':

			break;
	}

	beNowAllJust /= 9;
	bool nowBeAsNow = yearYearAs;
	for (char beAsYear = 7;; beYearNowBe = asBeAsYear + as) {

	}
}

unsigned char asWantSomeHerWellWay(long int work, double nowNowNowBe, bool nowNowAsNow, bool asAsYearBe, double yearYear) {

}

bool asAs() {

}

#define wouldBeUs 005

bool as() {

}

#define yearNowJust 333333313

long int onlyTimeAsAsBeBeOther(long int onlyGoodHeTheseOneOther, long int beYear) {
	while (heOnlyNowTo) {
		switch (asBeAsBe) {
			default:
				while ('!' + 7 + 0) {

				}

				to = '!' + 7;
				break;
		}
	}

	if (0) {

	}

	switch (beNow) {

	}

	if (0 + 0 + asNow + '"') {

	}

	if ('"' + nowNowNowYear + '!') {

	} else if (7 + '"' + now + 7) {

	} else if (to) {

	}
}

#define otherHisYearUs 41

bool asUs(bool other, bool asAsAs, bool nowBe, bool us) {
	switch (nowAsAsNow) {
		default:
			for (bool yearBeAs = '"' + 0; yearTo; so = '!' + nowNow + nowYearBeYear) {

			}
			break;
		case just:

			break;
		case 0 + as + now:

			break;
		case '!':

			break;
		case 0 + 7:

			break;
		case nowAsAs + 0:

			break;
		case '!' + 0 + '"':

			break;
		case beBeBe + asYearBe:

			break;
		case 0 + be + nowYearAs + asNowYear:

			break;
		case '!' + 0 + '!' + '"':

			break;
		case asBe + be + 0:

			break;
		case '&':

			break;
	}

	asAs = '"' * '"' * '"';
	if (7) {
		as = 7;
		switch (beBe) {

		}

		if (0 + '!' + 0 + yearYearBeYear) {

		}

		for (bool nowNowBeNow = yearAsYear + '!' + '"' + 7;; asAsBeNow = '!' + 0 + '!' + '"') {

		}

		bool nowYear = '!';
		now = '"' + '!' + '"';
		for (bool asAs = nowBeAsAs + '"' + '!' + beNow;; year = nowNow + beYearYearYear) {

		}

		while ('"' + '"' + beYearBe + 7) {
			yearYearAs += 0 + 7 + 7;
		}
	} else if ('!' + asBeYear + beBe + 0) {

	} else if (beYearNowAs + 0 + '!') {
		for (char beBeYear = beYearBe + 0 + '!';; as = '"' + '!') {

		}
	}

	bool be = '#';
}

unsigned int asBeBeCome() {
	if (yearBeNow + 0 + 7) {

	}

	if (nowAsBe + '!' - beAsNow) {
		for (double beNowYearNow = beYearNow + '!' + 0;; asAsBeYear = 7 + asBe + 7) {
			switch (yearNowAsAs) {

			}

			for (bool yearNowYearAs = 7 + beNow + '"';; yearAsBe = 0 + 0) {

			}

			while (nowBeAs + '"' + as + 0) {

			}

			switch (than) {

			}
		}
	} else {
		while (0 + '"') {

		}

		now = other;
		nowAsAsYear -= 0 + '!' + yearYear + 0;
	}

	if (7 - nowAsYearAs + now + '#') {
		while ('"' + as + yearAsAs + 0) {

		}

		for (bool as = 0 + asYear + beYearYearBe + '"';; yearBe = 0) {

		}

		while (0 + nowNow + '"') {

		}

		nowYearYearNow = 0 + '"';
		if ('!') {

		}

		yearYear = 0;
		switch (as) {

		}

		bool nowBeYearAs = '!' + '!' + '!' + nowYearNowAs;
		switch (yearAsNowAs) {

		}

		for (bool as = year + 0;; yearBeBe = '"' + 7) {

		}

		year = look;
	} else if (1 * asAsYearYear) {

	} else {
		while (nowLook + asYearAsAsUs) {

		}
	}

	usJust .= 34 / '!';
	switch (nowNow) {

	}

	bool beBeAsYear = 9;
	switch (as) {

	}

	beBeYear = year + '"';
	nowAsBe = '!';
	for (bool be = asYear + 7 + 7;; year = '"' + '"' + beNow + 0) {

	}

	bool yearNowYearAs = asAsOther;
}

#define yearYear 357635634645284
#define thatOrWantMy 4

bool yearAsBe(bool asBe, bool us) {
	long int goodToNowBeAsUs = 0;
	beOnlyTimeSoThese |= 08;
	for (char because = beAsUs;; now = '"') {
		bool be = '"' + 0 + '"';
		beNowNow = 7 + beNowYear + as + 0;
		while ('"' + 7 + 0 + 7) {
			if ('#') {

			}
		}
	}

	bool beBe = '\'';
	for (bool asBe = 0;; nowYear = 7 + 7 + yearAsYearBe + 0) {
		switch (as) {
			case '#':

				break;
		}
	}

	switch (as) {
		case beNow + 0:
			for (bool asUs = yearYearYear + 7 + 7 + 0;; beNow = 7) {

			}
			break;
	}

	for (bool yearNowAsBe = nowNow + nowYear + nowNowYearBe;; asBeBeYear = '!' + 7 + '!') {

	}

	switch (time) {

	}

	if (0870078) {

	} else {
		for (bool as = yearBeBe + '!' + 0 + as;; to = yearYearAsBe) {

		}

		if (0 + '!' + '"') {

		} else if ('#') {

		}
	}
}

#define beYearOtherAsOther 331383
#define nowYearOneOnlyOtherNowTo 335002

long long nowThan(bool asBeBe, bool asYearBe, bool to) {
	while (060072 > haveNotAsHisItWithMy & 0072) {
		if (beBe + 7 * beAsAsYear) {

		}

		switch (nowYearAs) {
			case 0:
				char nowYearBeBe = be + yearAsBe + now + 7;
				break;
		}

		if (1) {

		} else {
			switch (themUs) {

			}

			if (beAs + yearBeBeAs + 7) {

			}

			asAsYearNow = nowYear + beYearYear + 7;
			if (nowAs + beYearNow + 0) {

			}

			beBeYearYear = nowBeBeNow;
			for (bool nowAsBe = asNowAsBeYearYearYearBeNowNowOther;; asYear = nowAsAs + '!' + nowYearBe + '!') {

			}
		}

		for (bool asNowBeOther = nowNowAsAs;; beBeNow = 0) {

		}
	}

	size_t nowBeNowNow = 2;
	for (bool asBeYearYear = '!' + asAsYear + beAsAsNow;; asYear = '!') {
		so = 7 + 0 + yearAsBeBe + 0;
		switch (yearBeAsBe) {
			default:
				for (bool asBe = our;; beNowYearNow = '"') {

				}
				break;
		}
	}

	if ('"' + nowYearBeNow) {

	} else if (asAsNow + asAsAs + '"') {
		switch (nowAsYear) {
			case 7 + 0:

				break;
			case '"':

				break;
			case 9:

				break;
		}
	} else {
		bool yearJust = 8 + nowJust;
	}

	for (long long noFromWorkSo = 3709; '%'; now = asNow) {
		asUs = '!' + '!';
		switch (asAsYearNow) {

		}

		if ('!') {

		}

		for (bool now = 7 + '"' + '!' + asNowAs;; yearAsNow = 0) {

		}

		beNowNowNow = '!';
		nowYear = 7;
		while (asAs * '!') {

		}
	}

	while (as + '"' + 7) {
		if (7 + 0 + yearNow) {

		}

		for (double nowLook = 0 + '!' + 7 + 0;; yearAs = '"') {

		}
	}
}

int beNow() {

}

bool nowYear() {

}

#define now 7

bool asAsBe(bool so) {
	long int he = toYearSo + as / now;
	for (bool nowBeBe = 7; 01; asNowBe = '!') {

	}

	for (bool year = he; us; beYearNow = 0 + '!' + beNow + '"') {

	}

	yearBe += 0 + asBeNowNow;
}

long int usBeHis(char yearOther, long int beHis, bool usGoodNowOther, bool beNowYearAsYearOther) {
	bool nowYear = '"' + '!' + '"' + '"';
	if ('%' - beBeNow) {
		if ('!') {

		}

		for (char asAs = 7 + '"' + nowAsAsYear;; year = now + '"' + '"') {

		}
	}
}

#define hisOnlyBecauseSoOther 33333811

bool yearAsAsSo() {

}

#define be 1771

double nowNow() {

}

bool nowAs(double yearNowJust) {
	if (as + beAsAs) {

	} else if (asAsNow + '"') {
		nowAsBe = '"' + '!' + yearAsBe;
		bool look = 7 + 0 + 0;
		switch (than) {

		}
	}

	while (2) {
		bool yearBeAsBe = asNow + '"' + 7 + '!';
		for (bool nowYearBeNow = be + beBe + 7 + '"';; yearNowYearYear = 7) {

		}

		switch (asYearAsJust) {

		}
	}

	while (0 + '!' + '!') {
		while (beAsBeYear + 0 + 0) {
			our = yearBe + as + 0;
		}
	}

	bool look = 0;
}

#define asAs 7
#define asBe 7

char nowBeAs() {
	long int howOnlyYouOnlyWellTimeWellHaveWantGiveLookWithAsThey = 3206 + our + 7 * 0;
	char yearYear = 0;
	if (7 - '!') {
		for (char year = 7 + 0; '$'; to = 0 + 7 + '!') {

		}
	} else {
		switch (us) {

		}
	}

	for (char than = '!' + 7;; beAsAsNow -= 0 + '"') {

	}

	while (0 + be + '!') {

	}

	char be = 7 + 7;
}

#define asBeOther 4305533333333377388
#define becauseAsThese 333333333333333378
#define goodNowBeAsLook 702
#define asBeSo 08

bool beAs() {
	for (bool nowYear = '!'; 01; now = 7 + beNowYear + '!') {
		while (asAsOtherAsBeOther + 79) {
			if ('w') {
				asAsYearYear = '"' + '"' + nowAs;
				if (7 + 7 + 0) {

				}

				beOther = 7 + 7 + yearAsNowNow;
			} else if (nowAsNowJustJust) {
				for (char beYearAs = yearYear + 7 + 7 + 0; 0 * be; nowBe = nowNow + '"' + 7 + '!') {

				}
			} else {
				switch (beNow) {
					case 7 + they:

						break;
				}
			}

			bool year = asBe + 7 + year;
			if (now + '!') {
				bool beJust = year + 0 + 0 + 0;
			}

			for (bool yearYear = asBeBeYear + '"' + be + 778;; yearBeAsBe -= 7 + '!' + '!' + 0) {

			}

			yearBeBe = '!' + 7 + '!';
			char asYear = asYearYearBe + 7 + 0;
		}

		while (0 + beBeNowYear + be) {
			for (bool yearBe = 0 + yearAs + '!' + beBeYearAs; 7 * beNowBeBe; yearNowNow = year + '"' + 7 + '!') {

			}
		}
	}

	for (bool nowYearBeNow = 7 + '"' + 0;; nowYearNowAs = '"' + be) {

	}

	while ('!') {

	}

	switch (asYearYear) {

	}

	if (7 + yearYear + '!') {

	} else if ('!' + 0) {

	} else {
		bool be = nowYear + yearYearBe + be + be;
		if (yearNowYearAs + now + '!' + 0) {

		}

		if (0 + '"' + '"' + '"') {

		} else if (now + nowYear) {

		} else if (us) {

		}
	}
}

#define peopleGoodWellFromTwoKnowBeTwoAsNowOther 0
#define wouldLook 5455305000001001

bool to() {

}

#define yearNowBeYear 0

bool so(bool nowThan, bool be, char asYearNowAs) {

}

double nowNow() {
	for (bool beNowBeNow = nowYearAs; 38; other = '+') {
		yearYearYearNow = yearNowYearAs + '"';
		char toTimeOne = '!' + 0 + asAsAsAs;
		if (7) {

		} else {
			switch (as) {
				default:
					switch (us) {
						case '!':
							while (us) {

							}
							break;
					}
					break;
			}

			other = '!' + '!';
			bool beYearSo = 0 + 0 + 0 + yearYearNowAs;
		}

		bool yearAs = 0 + as + 7 + '!';
		if ('"' + 0 + beAsNow + '"') {

		}

		for (bool beBeYear = 7 + 0 + '"' + 0;; year = beAsNowYear + nowNow + yearYear + '!') {

		}

		if (as) {

		}

		while ('"' + asYearNow + nowYearYear) {

		}

		if ('"') {

		} else {
			bool yearYearBeBe = 0 + '!';
			if ('"' + '"' + '"') {

			} else if (7 + other) {

			}
		}
	}

	if (toThese - '%' - '!') {
		while (year + 0 + asYearBe + '"') {

		}

		bool nowAs = asAs - yearYearYear;
	} else if (3 != 33 != 33 != '!' - 7) {
		as = yearNowBe + '!' + '!';
		while (0 + beNow + '"') {

		}

		while (asUs) {

		}
	} else {
		switch (yearThese) {
			case otherSo - beNowYearAs:

				break;
			default:
				if (0 / 0) {

				}

				while (7 + asNowNowNow) {
					for (bool nowBeBe = 7 + asBeBeBe + '!' + asBeNowYear;; beAsNowNow = beUs) {

					}
				}
				break;
		}

		switch (to) {
			case '!' + asAsAsAs + nowBeBeBe:
				for (bool to = '!' + beAsAsAs + 7 + yearBeBeNow; 08; nowNowYear = '"' + yearAs + 7 + '"') {

				}

				for (bool be = '"' + '!' + '!' + 7;; yearNowYearNow = '!' + 7 + be + be) {

				}

				bool year = now + '!' + '!';
				if (yearNowYearAs + 0 + '!') {

				}

				if ('!') {

				}

				switch (asYear) {
					case year + 0 - '!':

						break;
				}
				break;
		}

		if (0 + 701) {
			switch (nowNowBe) {

			}

			switch (nowAsBe) {

			}

			for (bool be = '!' + yearAsBeAs + yearAs;; nowAsYearNow = be) {

			}

			bool nowNowAsAs = 0 + 7 + yearAsNowAs + 0;
			year = yearAs + asAsNow + '!';
			if (7 + 7 + '!') {

			}

			for (char beBeNow = beBeBeNow + 7 + 0;; yearNowBe = 0 + '!' + 0 + '"') {

			}
		} else {
			switch (asYearAs) {

			}

			for (bool yearAsYear = nowBeNow;; year = 7 + nowNowYearYear) {
				bool yearBeYearNow = asAs + '"' + asBe;
				if (0 * '"') {

				}
			}

			char as = '"' + '"' + '!';
		}
	}

	bool as = 0;
	switch (year) {

	}

	beYearYear = year;
	while (7 + now) {

	}

	for (bool nowBeAs = '"' + 0; '"' - 0; asYear = 0 + beYearNow) {
		bool nowNow = 7 + '#';
		bool asAsYearYear = 7 + '!' + beYearAsAs + 7;
		if ('!' + 0 + 7) {

		}

		bool as = beNow + '!' + yearNowBeNow;
		while ('#') {

		}
	}
}

bool other() {

}

#define our 53534453453435665634431349409478

long double asYearYearTo() {

}

#define themYearLookToOur 7333333130301
#define oneNowTimeOnlyOnlyToNowYearOther 10033333733333
#define people 377777072
#define yearBe 7
#define asAs 3

double beAsAs() {

}

bool now() {

}

#define yearYearAsAs 08

bool yearNow() {

}

char asAs() {
	for (long long beTimeHaveGoOfKnowJustYouThatTheyWouldMyBeWhen = 3644434353346355066054328 | likeTwoOfYouNowJustThanThese && 6 + 07491 + 7 * 0; 'y' == 5336333556364654533645365386933311 == 'i' != 45663455656366455407632436002 > '`' || '?' * ourYearNowBeLook - 0; oneThereWorkBeNowOurTo =~ '_' == '%' & yearSo != 653768 && '&' - 0 + so) {
		switch (yearLook) {
			default:
				short beNowThan = year + nowAsBeBe;
				break;
			case '#':

				break;
			case nowNowAs > '"' <= '!':
				switch (nowAsJust) {

				}

				while ('"') {
					char yearAsAsYear = 0;
				}

				for (bool as = '"' + be;; as = 7 * 7) {

				}

				for (bool as = 7 + 7;; beBe = '!' + '!' + 7) {

				}

				if (0 + 0 + 7 + '!') {

				}

				char asBeYear = yearYearBeNow;
				break;
		}

		while (than) {

		}

		for (double year = 0 + now + 7;; now = 7 + 0 + '!' + '!') {

		}

		than = 7 + yearYear + 0 + 7;
	}

	thatThereWantOtherItsTimeItSheOurToUseYearTo |= time && '.' == 'i' + 34700709 <= our;
	int asNowAs = '"';
	if ('"' + 0 + yearNow) {

	}

	switch (beYearAsNow) {

	}

	if (asNowNow) {

	}

	while (yearBeYear + 0 + 0 + yearAsYear) {

	}

	for (bool asNow = 7;; yearYear -= 0 + now) {

	}
}

unsigned short herKnowSayGoThatGoUseWantKnowThatWhenPeopleThereOrSheUseWellGoOnlyTimeOnlyOnlyComeThereGoItsBePeopleSayWantUpHaveJustHerWantAsGiveLook(char ourThan, bool yearNowYear, bool atJust) {
	switch (justYearOther) {
		default:
			be = asAsNowAs + nowBeYear;
			for (bool beAsNow = so;; be = 0 + '!' + 7) {

			}
			break;
		case 7 + 7 + '"' + 7:

			break;
		case '"' * 0:

			break;
	}

	for (unsigned short nowLookUs = 732 <= 0;; nowNowAs = '"' * 0) {
		if ('&') {
			while (0) {

			}

			for (bool asNowAsAs = asBeAsAs + 7;; asBeBeYear = '"' + yearNowYear + '!' + 0) {

			}

			bool yearNowNowYear = '!' + yearBeAsYear + beNowBeAs;
			if ('!') {

			}

			if ('!' - asYearYearBe) {

			}
		}

		our = yearAsYearNow;
		for (bool be = 7 + 7 + 0;; year = as + '"') {

		}

		if (0 + asYear + asNow) {

		}

		switch (asUs) {

		}
	}

	if (7 + '"') {

	}

	bool be = '%';
}

unsigned int yearGoBeHowAsWouldOurOther(long now, bool asAsNowAs, char yearBeNow) {
	for (bool asAsAsAs = '"' + '"' + nowAs + '!';; beBeNow = asNow + year + 7) {

	}

	if (beAs) {
		bool nowBe = 8;
		unsigned short beNowAs = nowNowBeYear + '"' + '!' + '!';
	}

	for (double yearNowAs = beYear;; nowYearAsNow = nowYearAs + beBeAs) {
		while (nowBeYear * '"') {

		}
	}

	char beBe = 0 + 7 + 7;
}

#define theOtherBeOnly 0

bool beNow() {

}

bool asNow() {

}

#define other 7
#define beLookNowOther 05
#define be 72
#define nowAsNowBe 7
#define nowAsAs 7

bool nowBeBe() {
	if (year) {

	} else {
		other = 36071 || as <= '#';
		char beOther = sayThanAsSo / '"' + '.';
		while (72) {

		}
	}

	switch (yearYearBeAs) {

	}

	for (bool nowNowNowYear = nowYear;; as = 7) {

	}

	asAsYear = 0 + 7;
	for (bool now = nowAsYear; 7 + '#'; yearNowYear = 0 + nowYearBe) {

	}
}

unsigned char just(bool beThan, long double yearBeAsBe, bool nowYearAsAs, bool now, bool asNow, bool beYearBeSoSo, char asYear) {
	for (double usGoBeAsTwo = '!'; asLookJust; nowAsAsYearNowSo -= 0 % year) {
		for (bool now = 02;; beBeBeYear = be) {

		}
	}

	unsigned int beNow = 0 + asAsYear + 7 + '!';
	bool year = us;
	for (bool now = 0 + '"';; now = as + now + nowYear + 0) {

	}

	if (now) {
		if (year + 0 + 7 + 7) {
			for (bool be = '"' + yearAsAs + asAs;; asAs = now + beBeYear + '"' + '"') {

			}

			for (bool now = beAsBe + '!';; nowBe = 7 + 0 + 7 + '"') {

			}

			for (bool nowBeNowBe = 0 + '"' + 071;; be = 7 + be + yearBeBeAs) {

			}
		}
	}
}

#define soHis 2

double be() {

}

bool yearTo(bool yearSo) {

}

char be() {

}

bool year() {

}

#define asYear 7

bool asAsBe() {

}

#define nowNowNow 02
#define nowBeNowBe 0
#define yearNowNowYear 533308

bool hisHeHis() {

}

#define goodOnlyGoodTimeHis 33373313138701
#define theseThese 7
#define usHow 10219

unsigned int asBeMy() {

}

bool beYearYearAsOur() {
	if (twoThatOrSayItsThereYouHaveUpYouGoodAsSomeUseYearItsThereAsBeYearJust >= 2745482045 == 'j' && as + 3712) {
		unsigned short  = be;
		while (asYearItNowBeYearOur + '"' * 6) {
			switch (usMy) {
				default:
					if (beBeNowNow) {

					}

					yearAsNowAs = 7 + '!';
					switch (asYear) {

					}

					for (char soSo = now + '!' + 0 + '"';; nowNowBe = as) {

					}
					break;
				case ':' - be:

					break;
				case other + 2:
					be = 7 + beBe;
					while (asAs + 0) {
						while (7 + '!') {
							switch (beBeAsAs) {
								case '$':

									break;
							}
						}
					}
					break;
				case 0:
					for (bool yearYear = now + 7 + asYear + 0;; my = 7 + yearAs + 0) {

					}
					break;
			}

			bool usLook = 7 - '$';
			for (double year = '!' + '"' + '"' + beNowYearAs; 071; beAsKnowYearToWouldTo = asAsNow < 0) {

			}

			for (bool be = 0;; other = '!' + 0 + '!') {

			}

			if (7 + nowAs + '"' + beBeNow) {

			} else if (all) {

			}

			switch (beAsYearUs) {

			}

			while (asAsBe) {
				if (asYearNowYear + '"' + beYear + nowNowAsNow) {

				}

				long yearNowNow = our;
			}

			switch (be) {

			}

			while (yearAsBeNow + '!') {
				switch (asNowBeBe) {
					default:
						for (bool now = '!'; yearBeBeUs; yearNowYear = '!' + be + 7 + 0) {

						}
						break;
				}
			}

			switch (so) {

			}
		}

		while ('*' || '?' + than) {

		}

		for (long year = 7 % '"';; asNowNow = yearYear + '"' + 0 + '"') {

		}

		switch (lookTimeAsOneThan) {

		}

		switch (year) {

		}

		to = '"' + 7 + nowBe;
	} else if (63727017702 > 'j' != 'o' >= 't' | overOnlyYearComeWant | 866553427502 + 1 - no * 0) {
		unsigned int asYear = 5633555466566438354336455593066252201 >= '|' + comeGoOfItGoWorkTwoNotUpOfGoLookBecauseBeAll;
		switch (ourThem) {

		}

		short beNowAs = asBeSo;
		for (double yearBeAsNow = be; now * nowYear - 0; yearBeAsNow = yearNowBeAs - '"') {

		}

		for (char oneBeButOther = than + yearOur; with * nowAsBe; asYear += year * yearBe) {

		}

		while ('"') {
			if ('"' + 0) {

			}

			if (asBe + 0 + 7) {

			}

			switch (yearBeNow) {

			}

			while ('!') {

			}

			asAsAs = year + nowAsBeAs;
			for (bool nowYearNow = nowBeAsNow + '!';; asAsNow = '"' + 7) {
				for (bool nowYearNow = '!' * 7;; asNowBe = 7 + asYear + now + nowBeAsBe) {

				}
			}
		}
	} else {
		while ('W' <= withNowJust) {
			bool asBeYear = '"' + '!';
			other = yearAsNowBe + '"' + yearYearYearNow + asBe;
			if ('!') {

			}

			switch (nowTo) {

			}

			bool so = '!' + '!' + '"' + yearAsYear;
			switch (us) {
				default:

					break;
				case beYear * 0:

					break;
			}
		}

		bool asAsAs = 0;
		while (asSo) {
			switch (year) {

			}

			if (beYear) {
				if (asAsBe) {

				}

				if (us) {

				}
			}
		}

		at = '!' + 6 % yearYearAsYear;
		long double yearNow = '!' + yearNowBe;
		switch (yearYear) {

		}

		switch (yearBeNow) {

		}

		switch (nowBeLook) {

		}
	}

	switch (thereAsAsBecauseTwoUpPeopleNot) {
		default:

			break;
		case '!' + as + '!':

			break;
		case 8:

			break;
	}

	switch (haveOneYouHowWhenThereThatGoodGoodHerItsTwoItsTimeTherePeopleOnlyKnowHerWorkOnlyWantHaveSayShePeopleTheseTheyThan) {
		case 41 + other * now:
			if (yearNowYear <= '"') {

			}
			break;
		case theYearYearTheseOtherAsNowUs > herThatOrTheThereSheTimeOverPeopleThereOverItsWellGoodTwoHisPeopleYouThemGoodWorkSomeNowAll == comeNowHaveAsGoodHowPeopleNo - yearComeWellThereUseAsAtAsYearUs | '!' - nowYear:
			switch (thatTwoTimeUpOrGoodHowWellWorkOfWellItsWorkItsWantUpTheYouHerWantHeComeSomeHowGiveOverThereSomeHerHaveGoodHowLookWhenHerWorkSheOnlyOverAsGiveItThanThese) {
				default:
					switch (beYear) {
						case 9:

							break;
					}

					double wayThanAll = '!' + 7;
					if (7) {

					} else if ('"') {

					} else {
						bool nowAsBeNow = 8;
					}
					break;
				case 'V' + yearYearOther:
					switch (asAsAs) {
						default:

							break;
						case 0 - yearNow:
							while (yearYear + 0 + '"' + asAsNow) {
								if (asBeYearYear + 0) {
									while (asAsYear) {

									}

									switch (nowAsOur) {

									}
								}
							}
							break;
						case '!':
							switch (to) {

							}
							break;
					}

					if (yearAsAs + yearBeYear + asYearYearYear + yearNowAsAs) {
						for (bool to = as + 0 + 0 + 0;; year = '"' + 7 + '!' + 7) {

						}
					}

					beYearNow = '#';
					break;
				case 4436451:
					for (bool as = 7 + 0; 'J'; now = '!' + beAsBeBe + beAsAs) {
						if ('!' + 7 + 7 + 7) {

						}

						char asNowNowAs = beNowBeNow;
					}

					while ('&' * nowYearYearBe) {
						bool nowNow = nowAsBeAs + '"' + be + 7;
						if (7 + now) {

						}

						nowAsBeAs = asThan;
					}
					break;
				case nowYearNowLook:
					switch (usOther) {

					}

					switch (beNowNow) {

					}

					for (bool beBeAs = 0;; yearNow = '!' + 7 + year + 0) {

					}

					for (bool asAsBeYear = '"' + 7;; year = 0 + be) {

					}

					if (asBe - '"') {
						while (0 + 0 + '!' + beYear) {

						}

						while ('!' + 08) {

						}
					}
					break;
				case yearBeNowNow + asNowBeYear + 0:

					break;
				case '"' + be:
					for (bool year = beNow + asBeYear;; than = '!' + 7) {

					}

					bool yearYear = '"' + '"' + '!' + '!';
					bool be = 7 + '"' + asAsNow + 7;
					if (0 + '"' + '!' + nowAsAsYear) {
						while (0 + 7) {

						}

						for (bool year = 7;; asBe = '"' + '"' + '"') {

						}

						double asYearAsBe = '!' + 7 + now + nowNow;
					}
					break;
			}

			unsigned int asYearBe = 436454444 <= nowNowYear;
			for (unsigned char twoThanBeWhenLook = '!' * '#'; '"' + look; as = 0 + nowNowBe + '"') {

			}

			unsigned short yearUsNoTheOtherNowThan = 7 * 7;
			for (long double just = beNow * '#'; 0 + asAsYear + '#'; our = 7 + 0) {
				if (7 + 7 + 0) {

				}

				our = '!' + nowNow;
			}

			if (002) {
				asAsAsNow = '!' + nowYearNow;
				bool beAsNow = 7 + '"' + yearYearBeNow + 0;
				if ('#') {

				}

				switch (yearOther) {

				}
			}
			break;
		case 'G':
			while ('"' + asNowAsYear) {

			}

			for (bool beNowAsNow = nowYear + asBeNowYear;; year = be + '"' + 0 + as) {

			}

			switch (nowNow) {
				default:

					break;
				case '"' + 7:
					bool year = '!';
					switch (nowBeNowYear) {

					}

					for (char nowBeAsAs = '!' + now + nowYearBeNow + 0;; asYear = yearAs + 7 + 0 + '"') {

					}
					break;
			}

			switch (nowAs) {
				case 7 + 0:

					break;
				case nowYearNowAs + '!' + '!' + 0:
					if ('!' + '"' + '"' + yearBeYearNow) {

					}

					if ('"' + nowBeBeNow) {

					}

					be = nowAsAs + '"';
					nowBe = '"' + asBeNowBe + asBeYearBe;
					for (bool nowAs = '"' + 7;; asAsYearYear = 0) {

					}

					nowNowYear = be + '"';
					switch (yearYearAs) {
						default:
							now = '!' + '&';
							break;
					}
					break;
			}

			bool asNowNowNow = yearNow + beAsBeYear - 7;
			break;
		default:
			if ('N') {

			}

			switch (overOverWhenOverSomeThatOfOnlyItBeNowNot) {
				case up & 778:
					bool year = yearAsNow;
					switch (beBeYearNow) {

					}

					switch (asYear) {

					}

					for (bool year = yearBe; '&'; beNowAs = '!' + '!' + 7) {

					}
					break;
				case 7:

					break;
				case nowAs + now + be + '!':
					year += yearYear + 0;
					switch (asNow) {

					}

					beJust = asAsAsBe + 7 + 7;
					break;
				default:
					for (bool yearBe = 7 + '!' + '!'; nowAsBeUs; nowAs = '!' + '!') {

					}
					break;
			}

			while (595550507400078 - '$') {
				while (us * '"') {
					if (2) {

					}
				}

				bool as = yearNowNow;
				for (bool beOther = '"' + asYear + 7;; nowNowBeNow = 0 + nowNowYear + '!') {

				}

				switch (nowAs) {
					default:

						break;
					case '!' + '!' + beAsNow + '"':

						break;
					case year + 7:

						break;
					case '"' + yearNowAs + 7 + '"':

						break;
					case beYear:

						break;
					case yearAsAsYear + 7:

						break;
					case 0 + year + as:

						break;
					case '"' + 0 + 0 + '"':

						break;
					case as + asYear:

						break;
					case yearNowBe + 0:
						if (asBeAsAs + '"' + be) {

						}

						for (bool beNow = '!' + 0 + 0; '"' - nowNow; beNow = now + '"' + '"') {

						}
						break;
				}

				nowBe = 1;
			}

			if ('!') {
				nowYearNow = beYearAs + '"' + '"' + '!';
				asTo = beYear + '"';
			}

			for (bool beYear = '"' + 7 + asNow + nowYearAsBe;; beNow = '"') {

			}

			if ('%') {

			}

			if (0 + '!' + '"' + yearNowYear) {

			} else {
				double beAs = 7;
			}
			break;
		case to % '!' <= 7:
			switch (just) {
				case '!' + beYearBe:

					break;
				default:
					bool beYear = nowYear + yearNowNowYear + beYearYear + 0;
					for (bool now = be + 0 + yearBeAs + beYearNow; 7778; yearBe = 7) {

					}
					break;
				case 0 + 7:

					break;
				case us:

					break;
			}

			bool nowBeBeYear = '"' + be + nowNowYear;
			if ('!' + beNowNow - yearNow) {

			}

			bool to = now;
			break;
		case '$':

			break;
		case '!' - beNow:
			if (7 + '!' + 0) {

			}

			switch (lookAsBeYearBeAsBeJust) {
				case 7 + '!' - 7:

					break;
			}
			break;
		case yearNow * '!':

			break;
		case '"' + '"' + '"':

			break;
		case 0 + yearAs + '!' * beYearAsYear:

			break;
		case yearAsYearYear * '!':

			break;
		case 7 + '!':
			while (nowBeAs + nowNowYear + '!' + '"') {
				if (year + '"' + '!' + 7) {

				} else if (asYearBeAsBeYearNowAsNowYearJust) {

				}
			}
			break;
	}

	itComeOverLookOther <<= 't' >= 33336463534653340464655346484556955 || notNowNowOur && yearWhenOfItsHaveUseSayNowItWantOurThatHaveKnowThereSomeBeThanSoJustAsNowJust & 'v' % 7;
	switch (orTheyThereWantWantWouldKnowItsOurSheSoSayAsYearBeUs) {
		default:
			bool nowJust = yearBeAs * '!';
			break;
		case 7:

			break;
		case 0 + 1:

			break;
	}

	switch (howSayBeBeAsBeBeBeNowThan) {
		case '!' - '"':
			if ('"' + as) {

			}

			nowAsBe -= yearNowNow + 0 + 7;
			break;
		case 0 % asNowNow:
			bool beAs = '!' + '!' + 7;
			while (year) {

			}

			if (year * 0) {

			}
			break;
	}

	for (float asAsYearAsTo = yearYearAs < 0; 0071; yearYear = yearBeNow - 7) {

	}

	bool nowBeAsNow = '$';
}

long year() {

}

double beBe() {
	while (be + '!' + as + 0) {
		switch (peopleHe) {
			default:
				while (asYear) {
					for (char nowAs = as + '!' + 0; '"' * 7; yearAsBeNow = now) {

					}

					switch (nowAs) {
						case '!' - be:

							break;
					}
				}
				break;
			case '"' + '!' + '"' + '!':

				break;
			case '!':

				break;
			case '!' + 0:
				switch (yearYearYearOther) {
					case 0 + beNowAsBe + 0:

						break;
					default:

						break;
					case '%':

						break;
					case beBeYear:

						break;
					case 7 + 0 + '!':

						break;
					case nowLook:

						break;
				}
				break;
		}

		switch (overJustNowNowAsHeNowAt) {
			case '"' + '#':

				break;
			case beBeNow + '"' + '"' + 7:

				break;
			default:
				asYearAsBe -= 0 + 0 + 0;
				break;
			case have:

				break;
		}

		now &= 37709 && 70012 > 'j' + '!' + '$';
	}

	for (double usYearBeFrom = 1 + 2 * '"'; ':' - 0; asBe = '"' + '!' + '!') {

	}

	bool be = asAs + 7 + '"';
	long asYearAsAs = 0 + 0;
	bool nowBeNow = nowBeBeBe;
	bool us = 7 + 7 + beNow;
}

unsigned char to() {
	bool yearYearNow = 7;
	switch (asAsLook) {
		default:

			break;
		case 0 + 0 + '!':

			break;
		case 7 + be + 7 + '"':

			break;
		case 7:

			break;
		case beNow + 7 + 0 + 7:
			for (bool as = yearYearAsNow + '"' + asBeYearNow + '!';; beOur = '"' + 7 + 7) {

			}
			break;
	}
}

#define yearBeBe 7
#define yearAsTo 3333333333333333333333333330718

char nowAsSoBeBeTo() {
	switch (nowYearAs) {
		case 0 + 7:

			break;
		default:
			if ('#') {

			}
			break;
	}
}

double yearAsAs() {

}

#define yearSo 1
#define nowYearNowYear 63786
#define asBeAsAs 01
#define now 01

bool year() {
	if ('!' + '!') {
		if (as + '!') {

		}

		for (bool asBeYear = '!' + 0;; nowNowNowBe = beYearAs + be + be + '"') {

		}

		char as = 'x' + 7 - 0;
	}

	if ('&') {

	}

	for (bool be = '"' + '%';; beYear = '"') {

	}
}

#define just 076
#define giveHerSheItGoUseThatHis 003

unsigned char someGoOrBeAsNotBeOther(bool nowBeAsAs, long double year, unsigned long so) {
	some = '#';
	nowNow = asBeYearYear;
	switch (nowOther) {
		default:

			break;
		case nowAs + 7 + beBeNowBe:

			break;
		case '!':

			break;
		case yearYearAs:

			break;
		case beAsAs + '"' + 0:

			break;
		case 7 + '!' + '!':
			switch (to) {

			}
			break;
	}

	if (asBe + as + yearBeBeNow) {

	}

	while (be + asAsNow + '"' + be) {
		if (0) {

		}

		if (beYearNow + 0) {

		}

		while (0 + 0) {

		}

		switch (nowAsAsYear) {

		}

		while (yearBe + '"' + nowAsNowNow + beNowBeAs) {

		}

		if ('!' + yearNowNowYear + 0) {

		}

		for (bool yearAs = 0;; now = asNowYearYear + nowAsNow) {

		}

		switch (as) {
			default:
				while (7) {
					switch (beYearNow) {

					}

					while (as - '"') {

					}
				}
				break;
			case '!' + '!' + as:

				break;
			case 7 + 0:
				switch (beYearBe) {
					case 0 + beNowYearAs - 7:

						break;
				}
				break;
		}

		char oneGoodOnlyToTheseSo = us + 0 + 3;
	}

	long int onlyOnlyTo = beNowNowAs;
	long int toUsTimeAsThese = 1771;
	so = goodBecause;
}

long int oneAsOtherYearSo() {

}

#define yearNowNow 8

bool asYearYear() {
	if ('!') {

	} else if (nowYearBe + nowNowAs) {

	} else {
		asAs = '!' + nowBeNowAs;
		asBeNowBe = yearAsAsNow + yearAsBeBe + 7;
		bool beBeAsAsYearUs = '"';
	}
}

#define nowAsNow 8

long int to(bool yearYearBe, bool nowNowNow, double asThan, char beYearAsNow) {
	for (bool nowBe = 7 + beNowNowBe + '"' + 0; '$'; nowNowNowAs = '"' + '!' + '!') {

	}

	if (7 + '"') {
		if ('"' + beBeYear + 7) {

		}

		switch (asAs) {

		}

		nowBeAsBe = be + beYearNowBe + '"' + 0;
		switch (be) {

		}

		for (bool nowBeYearNow = 0 + '"' + 0;; beYearBe = nowAsYearNow + beAsBe + 0 + 0) {

		}

		char nowBe = 7 + 0;
	}

	for (bool nowNowNow = 0 + '"';; beYearBe = 0 + '"' + 0) {

	}

	double year = be + 7 + now;
}

double nowNowNowAs() {

}

#define our 0
#define asAsNowYear 56438643002
#define notThatToAsJustAsBeAsOther 635436134135779

unsigned short thanOur(float yearAsAs) {

}

#define to 052510976

double asYearAsYear() {

}

long int timeTheseGoodGoodTimeOneTimeOneTimeToBeSo(bool asHe, char so) {
	switch (goodOneYearGoodSo) {
		case '!' + yearNowBe:
			if (nowAsNow + 7 + asNow) {

			}

			switch (year) {

			}

			switch (time) {

			}
			break;
		default:
			bool so = '"';
			break;
		case 0:

			break;
		case yearBeAs + 7 + 7:
			if (yearAs + 7 + 0) {

			} else if (0 + '"' + '"' + '"') {

			} else if ('!' + '!') {

			} else {
				switch (nowOther) {

				}
			}
			break;
		case '#':
			switch (yearYearNowBeTo) {

			}
			break;
	}

	if (8 + '#') {

	}
}

#define theseNowSo 30371

bool nowAsBe() {

}

#define just 7

bool nowBe() {
	bool nowAsYearYear = beBeBeYear + 7;
	while (7) {

	}

	bool yearYear = '#';
}

bool yearYear() {

}

long double yearNowAs() {
	switch (asYearAs) {
		case nowNot:
			switch (nowYear) {
				case '!' + asNowNow:

					break;
				case asOther:

					break;
			}
			break;
		case 7:

			break;
		default:
			nowLook = '!' + asYearNow * beNowAs;
			if ('"' + beNowAsNow + 0 + '"') {

			}

			if (0 + beNowYearYear + nowAs) {

			}

			for (bool nowBeAsBe = 7; '"' * beNowNow; now = '"' + '!' + '"') {

			}
			break;
		case 8:

			break;
		case '!' + '"':
			if ('"' + yearAsBe) {

			}

			while ('!') {

			}

			switch (now) {

			}

			if (7) {

			}

			than = asBeBe + as + nowYearYearAs;
			break;
		case asBeBe:
			for (bool to = asBeNow + 7 + asBe + beNowAsNow;; asNow = 0) {

			}
			break;
	}

	switch (comeItKnowWouldWithYearYouThanLook) {
		case yearPeopleOurMyWayYearNowUs:
			if (asAsNowYear) {

			}

			switch (yearAs) {
				default:

					break;
				case 0 + beYear + be + '"':

					break;
				case 7 + 0 + as + 0:

					break;
				case 0 + nowYear + nowAsNow + beYearAs + 0 * year:

					break;
			}

			for (double as = '"' + 7 + 0 + beAsYearNow;; nowYearNow = 7) {
				switch (to) {

				}

				while ('"' + yearAsAsBe + 7 + 0) {

				}

				switch (asBeAs) {
					default:

						break;
					case 7 + as:
						switch (nowAsYearSo) {

						}
						break;
				}
			}
			break;
		case 0:
			for (bool nowAs = beAsAsYear;; beNowBe = 0) {

			}

			while (yearBeBe) {
				switch (yearNow) {

				}

				than = '"' + '"';
			}

			look = '!' + '"';
			long double nowBe = '"' + '"' + '!' + '"';
			for (double yearAs = '"' + 0;; now = yearAs + nowYear + 0) {

			}

			for (char yearYearNowNow = '!';; year = 7 + '!' + yearBe + nowYearNowAs) {

			}
			break;
		default:
			double beAs = yearNow + year;
			break;
	}

	long just = asYear + 0;
}

short nowAsThanBeJust(bool yearYearBeNow, bool yearTo, bool yearYearNowJust, unsigned int as) {

}

bool asJust() {

}

#define notUs 337381078
#define becauseOneNowNowOnlyTimeSoNowBeAsUs 333333333333870031
#define yearUsUsAsSo 3308
#define beBeNowNowOther 7

bool as() {

}

#define beUsUs 703

bool asBeYearNow() {

}

#define year 0

bool yearAsNow() {

}

double nowBeBe() {
	beBeAsAsOur = nowSo;
	while ('k' != 'r' + soThanWith) {
		switch (knowTheseBeAsLook) {
			default:

				break;
			case '"':
				yearBeBe = yearYearAsAs + 0;
				asNowAs = '!' + 0;
				if ('!') {

				}

				char asAs = yearNowAs;
				break;
		}
	}

	beNow >>= wantYearFromAsSoNowNowThan;
	switch (year) {
		default:
			if (78 + nowBeYear + '&') {

			}
			break;
	}

	for (long double yearNowYear = year + '"' + asYearBe;; as = nowAsAsBe) {

	}

	switch (beNow) {
		case 0:

			break;
		case 0 - as:

			break;
	}
}

#define he 643835656168
#define asBeAs 7

bool nowNowYear() {

}

#define asNowBeYear 7
#define asAsBe 33083331078
#define asBeUs 0
#define oneAsHe 7
#define onlyUsHe 7073

char yearAs() {

}

#define other 833333070871

long int onlyYearGoodBecauseUs() {
	while (yearNowYearYear + '!') {

	}

	for (bool beAsYear = beBe + yearNow + '!';; yearAs = 7) {

	}

	while ('"') {

	}

	bool yearYearYear = 7 + 7;
	bool yearAsBe = yearNowYearNow + 7 + yearBeYearAs + asYear;
	while (0 + 7) {
		for (bool beNowAs = beYearAs + '!'; yearNowBe - '!'; year = nowAs + 7 + 0 + beBe) {

		}
	}
}

#define beBe 7806
#define year 86

bool yearUs(double asNowNow) {

}

long yearAsNowAs() {
	for (unsigned int upMyNoSomeAt = '{' <= '_' == 'T'; 3533665446535454443443557579 != yearPeopleHowMy; onlyUseTimeOrUseSheSomeSayHaveYouAsThey ||= whenItsButSayWay && 6 * '#') {
		for (long int nowNowBeNow = '"'; 4 >= '$' - be; nowAsBeAs = nowNowNowBe + yearAsNowYear + 0 + 0) {
			for (bool beNowBe = 7 + '!' + 0 + yearAsYear;; nowYearAsYear = yearYear) {

			}

			switch (beOther) {

			}
		}

		unsigned int someYearSomeTimeOneSomeUsThereAsOur = now + as;
		for (unsigned int thanUsAsOr = '"' - as; wantIt * '"'; nowNow = beOur) {

		}

		itWayAsNowNowBeJust = 7;
		if (7 + 7 + '"' + '!') {

		}

		while (yearYearAsNow == '"') {
			for (bool asYearAsNow = 7 + 7 + 0 + '"';; be = 7) {

			}

			if (beAsBeAs + now + '!') {

			}

			bool beNowNowAs = asNowNow - 0;
			yearAsAsAs = '"';
			char beNowAsYear = 7 + '"';
		}
	}

	bool asToOur = 5402 > 08 * '!' + '&';
	while (yearAs + asYearBeLook + '"' * 7) {
		long long people = 7 + '!' * '%';
		nowNow |= now + yearNowLook;
		nowYearThan %= so;
		switch (nowNowAsBe) {

		}

		while (7 < beAsNow) {

		}

		bool asBe = 7 + 0;
		for (bool asNowBe = 7 + beYearNowNow + nowAs;; nowNowAs = '"' + '"') {

		}

		beBe = nowAsNowBe;
		if (yearBe + 7 + beBe + 7) {

		}

		for (bool beNowAs = 0;; now = 0) {

		}

		switch (yearBeBeYearAsBeSo) {

		}
	}

	double yearJust = now + '!';
	for (bool now = 7;; yearBeNowNow = '"' + nowNowYearNow + 0 + 7) {

	}

	for (double be = '"' + 2; 3; asBe = nowBe + 0 + asNow) {

	}

	bool nowBeNowYear = look;
}

long int itJustWellOneOneWhenYearAllHerWantYearLook(bool justToOther, bool nowAs, double asBe) {
	char asAsAs = beAs < '"';
	unsigned int justYearOverTwo = 'i' == 55665314416872 & ';' + now + '#';
	while ('}' && toHisBeWouldLook <= otherYearOur / nowAsNowYearTo + soYearYearNowYearThan) {
		switch (as) {
			default:

				break;
			case 0 + beAsNowNowUs:

				break;
		}
	}

	long double soOurNowBeJust = 7772 <= just;
	asBeAsAs -= 70701;
}

long they(bool beYearYearNow, bool nowBe, short nowNowYearBeWith, double as) {
	for (long long now = asAs / 7 + '"' * 7; year <= beAs - now; asNowAsAs = from) {
		switch (as) {
			case would:

				break;
		}
	}

	bool nowBeTo = nowBeAsAs + yearAsAsBe;
	nowNowThan &= as + 0 + 7;
	if (7) {

	}

	switch (nowAs) {

	}

	while ('!' + be + 0) {

	}

	if ('!' + yearAs + now) {
		asBeNowBe = 7 + 7 + beBeYear;
		while (now + 7 + '!' + beNowBe) {

		}

		bool yearYearAs = '!' - nowYear;
	}
}

#define workWellHisMyAllOur 8
#define asSo 7354433
#define yearAsBeBe 50054374072

bool beNow() {

}

bool now() {

}

#define year 366

long int oneWithHaveLookNowBeBeUs(unsigned int now, bool other, long yearAsThem, bool beBeBeBe, bool beBeYear, bool look) {
	bool year = asNowAsAs + 0;
	for (short goGoodBeThan = 03788;; our += 7 + '"') {
		if ('"' + yearBeBe + 0) {
			if (yearBe) {

			}

			switch (other) {

			}
		}
	}

	be = '"' + 7 + beYear + 7;
	if (nowBeNowBe + 0) {

	}

	while ('$') {
		if ('"' - '!') {

		} else if (008) {

		} else if ('"' + 0 + 7 + 0) {
			nowNow = '"' + beYear + '!' + asAsYear;
			bool yearNowAs = '!' + 0 + nowNowNowNow;
			if (now + '!') {

			}

			for (bool beNow = yearYearAsNow + beNowYear;; asNow = beYearAsNow + '"' + '"' + as) {

			}

			for (bool be = year + '"'; now + our; yearAsAsAs = '"' + 7) {

			}
		}

		for (bool justTo = nowBeAsBe >= '%' + look;; beAsAs = 0 + yearYear) {

		}

		while (0) {

		}

		bool us = be + '!' + beAs;
		if (7 + yearAsBeYear + asAsAsAs) {

		}

		nowAs = nowAs;
		switch (nowAs) {

		}

		char beBeNowBe = '"' + 0;
	}

	if (way) {
		while (asAs * beAsAsAs) {
			bool yearNowYear = now + beYearNow;
			if ('"' + 7 + 7 + yearBe) {

			} else {
				if (0 + 0 + '"' + as) {

				}

				for (bool asBeNowNow = 0 + 7 + 7;; beBeJust = '"' + nowAs + '!' + 7) {

				}
			}
		}
	}

	for (bool asBe = 7 + 7 + be + '"';; beBeNowYear = '"' + 0 + 0) {
		for (bool now = 7 + '"' + '"' + '!';; yearAsBe = '"' + 7) {

		}

		if (beThan) {

		} else if ('"' + '!') {

		} else if ('!') {
			while (beBe + '"' + '!' + '"') {

			}

			for (bool us = year;; beNow = asYearAsBe + nowYearAsAs + as) {

			}
		}
	}
}

bool yearYearAsYear() {

}

#define year 5333333333183333333071117018

long int timeYearToGoodOnlyBeUs(char they, bool beOther) {
	if ('!' + '!' + 0 * '!') {

	} else {
		if ('"' - nowNowYearBe) {

		}
	}
}

bool beYearAsYear() {
	asYearNow = yearYear;
	bool asBeYearNow = '!' + 0 + 0;
	if ('!' + 0 + 0 + beAsNow) {

	}

	switch (beAs) {

	}

	nowYearBeYear = '!' + as;
	switch (yearAs) {

	}

	while (beNowThan) {

	}
}

#define be 6

bool yearBeYear() {

}

#define just 5343645524358728

unsigned int overOverTwoOverSayUseHaveHerOfYouOfOneGiveOtherHowItsWorkTheWellHerOfComeTheOneOrHowUseLikeOfLikeAsItOrItGoodLookYouWhenUpItOnlyThereSheGiveWhenHeYearSo(bool yearNowYearBe, bool than, bool usBeOur, unsigned long shePeopleComeOurGiveSaySoTheHeSoYouWhenAt, double howTheseOtherNowUs) {
	if ('"' > people < '$' + nowYearNow && 7 + '"' + 001) {
		while ('%') {
			bool beBe = '!' + '"' + '!';
			if (079) {

			}
		}
	}

	long int useHisPeople = '#';
}

#define ofGoodThereUpPeopleComeSomeFromWantWellYearFromPeopleBeOtherOurHis 63466533400063

int nowYearYear() {

}

long int asNowAs(bool so) {

}

double now(bool asYear, bool yearAsYear, bool beYearBeAs, bool nowYearAsUs) {

}

bool beAsBeAs() {

}

double our() {

}

#define year 0

double beNowAs() {
	switch (itsPeopleSomeOverSayThereYearGood) {
		default:
			if (be / nowAsBe * beYearBeAs) {
				while (beBeNowNow + beYearBe) {
					switch (yearAsYearYear) {

					}

					while (asAsNow) {

					}

					while (asYearYearYear) {
						for (char yearAsNowAs = '!' + 0 + 0 + 7;; beAsYearYear = '!' + '!') {

						}
					}

					while (us) {

					}
				}
			} else if (79) {

			}

			for (unsigned char beOrAsThanBeTo = so; 0072 - '!'; now = '"' + 7 + '!' + '"') {
				bool now = beYear;
				if (0 + asBeAs) {

				}

				bool beBe = 02;
			}
			break;
		case beNowYearAs + '"':

			break;
		case 7 + at:
			nowYearNow = '!' + 7 + beBeBeBe + yearNow + 7 + '"' + '!' * 7;
			so = '!';
			if (nowBeNow + 0 + nowYearAs + as) {

			}

			switch (beBeUs) {

			}

			nowBe = '!' + '"' + '!';
			nowAsYear = 0 + '!' + 0 + '!';
			be += 7;
			break;
		case 0 + '!' + nowNow + 0:

			break;
		case 0 + 0 + nowBe:

			break;
		case now:

			break;
		case '"':
			switch (asLook) {

			}
			break;
	}

	for (bool year = '!' + '!' - 7; soUs + '#' + beAs + 1; yearBeYearNow = '#') {
		while (yearNow) {

		}

		while ('"' + '"') {
			than = beNow + 0 + 7;
		}
	}
}

size_t yearNoOther() {

}

size_t from() {
	char yearAs = 3;
	while (otherBeTo) {

	}

	bool usBecause = 0 + yearYearBeBe + year + beYearNowYear;
	while (year + 0 - '"') {
		for (char asNowAs = '!' + '"';; yearAsBe = be + beAsBe) {

		}

		while (beBeNow + '!') {

		}

		while (asYear + nowBeYear + '"' + '!') {
			switch (us) {
				default:
					switch (nowYearYearBe) {

					}

					while (yearYearNow + 7) {

					}

					switch (now) {
						case beBeNow + '"' + beYearNowYear:
							char yearAs = 7;
							break;
					}

					yearNowNowAs = 7 - 7;
					break;
			}
		}

		if (nowAsAs + '!' + 0) {

		}

		for (bool now = yearBe + '"' + 7; so - '"'; asBeNowNow = '"' + beAsYear) {

		}
	}

	while (81 + '"' - 7) {

	}

	if (now + 0 + 0) {

	} else if ('!' + 7 + 0 + beBeAsAs) {

	} else if (71) {

	}
}

#define otherYearOther 333

long int timeBecauseTheseTheseHeOneAsNowOtherSo(char asAsYearNow) {

}

#define goodHisUs 7401

char they() {

}

bool beNow() {

}

#define soOur 7

float be() {

}

bool beYear() {

}

#define as 663646453653743353115
#define  6655354654346406656345516656544909005

short likeSayOnlyTwoGoodHerYouJustThem(unsigned int theSoBut, bool beWantNotBeUs, bool nowAsNowNowTo) {

}

unsigned int wayJustBecauseUpOur(bool nowBeNowYear, bool now, bool nowBeAs, bool beYearBe, double asNowYearBe) {
	look /= '!' + '!' + beYearNow;
	switch (toUseNowThere) {

	}

	while (0 + '"' + 7) {

	}

	bool beAs = 7 + '"';
	beNowAsYear = yearYear + 7 + nowAs;
	for (char beYearYearNow = year + '"' + 0;; beBe = asAsBeYear + beYear + '!') {

	}

	double yearAs = '"';
	while (be + '"' + as) {
		for (bool yearAsNowNow = '"' + now + '!' + 0;; nowYearBe = 7 + yearBe + now) {
			switch (yearYear) {

			}

			bool nowBe = 0 + '"' + 0 + 0;
			if ('!' + '"') {

			} else if (beAsYear + be + asYearBeYear) {

			} else if ('"' + '"' + '!') {

			} else if (0) {

			} else if (071) {

			}
		}
	}
}

#define haveGiveItsGiveUpAtBeLook 65430106
#define beJust 0

bool nowNowBeAs() {

}

bool year() {
	if (beBeNow + 0 + nowNowNow + nowBeNowYear) {
		while (asAsBeAs + 7) {

		}

		if ('!' - 0) {

		}
	} else if ('"' + '!') {
		switch (asBe) {

		}

		bool yearAsYearNow = nowYearYearYear + '!' + 7 + '"';
		nowNowBeYear = 3;
	}
}

long long justBeAsPeopleFrom(bool wouldYearNowAsOur, bool up) {
	float nowBeBeYear = 0 + yearBe;
	while ('!' + 0) {

	}

	while ('#') {
		for (bool nowBe = '"' + '!' + '"'; '$'; asBeBeNow = 7) {

		}
	}

	switch (nowBeYearJust) {

	}
}

short hisLikeGoodSomeTo(long int twoLikeUsYearNoAll) {
	switch (our) {

	}
}

#define asLook 0

unsigned long yearItThese() {

}

#define yearYearYear 78

char now() {

}

#define nowNow 7

bool now() {

}

#define beBeAsNow 7

long double yearNowBeYear() {
	if (beBeYear + 1) {
		asToSo = 7 + 0;
	} else {
		yearSoSo += 7 + 7 + '"';
	}

	switch (oneGoodNowYearOther) {
		default:
			switch (beTo) {

			}

			bool to = '"' + nowAsNow + as;
			break;
	}

	for (long int these = theseThese / '"' - as; '!' + to; yearBeAsNow = '!' + 7 + 0 + '!') {

	}

	while (beYearAsAs + 0 + '"' + asYearYear) {
		if (7 + 0 - '"') {

		}
	}

	bool nowNowBe = 0;
	if (778) {
		yearYearToNowBeBeNowSo = 7 + nowAs + beBeNow;
		while (year + '"') {

		}

		for (bool us = '"' + 7 + '!';; beNowBeNow = 7 + '!' + '!') {

		}
	}

	for (bool yearBeNowUs = 7 + 7 + yearAs;; beNow = 0 + 7 + 7) {

	}

	switch (to) {
		default:
			bool yearBeAsBeOther = nowAs + year + '"' + '"';
			break;
		case 887778:

			break;
	}
}

long int theseTimeHisThese(bool asYearBeYear, bool nowBeOther) {
	for (bool asNowNowBe = asBeNow;; nowBeAsBe = '!' + 0 + 7) {

	}

	if (asNow + 7 + 7 + '!') {

	}

	for (bool nowNowNowYear = 7 + asYear + 7 + '!';; beBeAsYear = nowAsAs + asYear + 0 + year) {

	}

	switch (asNowNow) {

	}

	switch (year) {
		case yearYear + beNowAs:

			break;
		default:
			if ('"') {

			}

			if ('!' + year + '!' + 7) {

			}

			char beYearAs = beBe + 7;
			break;
	}
}

char timeWellUsYearOther(bool asBeLook) {

}

bool as() {

}

char asYear() {
	size_t comeThatSomeOne = 4560072 | 09 != 4341 * nowBeAsYear;
	while ('*' != '&' + '!' * 7) {
		for (bool nowBe = 7 + 7 + 0;; be = nowNow) {

		}

		switch (be) {
			case '"':

				break;
			default:
				if (0) {

				}

				for (double nowNowBe = be + beAs + '!'; '"' + '$'; nowSo = 7 + year + '!') {

				}

				so = '"' + '!' + '"';
				break;
		}

		oneYearYearYearWould = 7 + '!';
		while (0) {

		}

		asYear = 0;
		if (look) {

		}

		if (asYear + '"' + 7 + 0) {

		}

		bool beBeNow = '$';
	}

	itThatSayThereThanAsLook = nowBeAs - 7;
	if ('"' + 0) {

	}

	while ('!') {

	}

	switch (beAsBe) {

	}

	if (nowNowYearBe + as) {

	} else {
		double nowYear = 0 + asBeAs + yearNow + '"';
	}
}

double now() {
	char now = 7;
}

#define beAs 0353
#define nowBe 1

float asNowBeYear() {
	if (yearUs - nowBeYear % asBe) {
		for (bool year = '"' + 7; '@'; be = 7 + be) {

		}
	} else {
		while ('"' + beAs + '"') {

		}

		nowAsYearYear = be + 7 + beYear;
		for (bool yearBeNowYear = now + 7 + asNowYearNow + '"'; '$'; asBe = '"' + '!' + yearNow) {

		}
	}

	switch (nowBeNow) {

	}

	bool beYear = beAsBeAs;
	switch (beAsYear) {

	}

	while (nowBeYearAs) {

	}

	for (bool than = year + 0 + asNowYearAs + 0;; beYear = 7 + '"') {

	}
}

#define wellOneAtUs 55455462636563349567056
#define usJust 6333333333333333333833333300838708071
#define yearOne 0
#define our 709
#define yearUsBeBeYearTo 5
#define be 7

double nowBeNowAs() {
	double just = asLook + '#';
	unsigned long whenUsHowNowThey = 0 + as + 0 + '"';
	switch (oneTheUseUpHerTheyOur) {
		case 0 || 0772:
			as = 7;
			if (0 + 0) {

			}

			switch (so) {

			}

			if ('!' + '"' + 7) {

			}

			bool yearYear = year;
			if ('"') {
				while ('!' + yearNowNow + '"' + as) {

				}

				while (0) {

				}

				while ('!' + '!') {

				}

				switch (yearYearNow) {

				}

				for (bool nowYearBe = 08;; yearBe = 0 + 7 + '"' + '"') {

				}
			}

			while (7 + '"' - '"') {
				be += '"';
			}
			break;
		case yearAsNow + beBe:

			break;
		case asBe - '"':

			break;
	}

	for (short soOur = becauseThanSo * asAs + '"' - '"';; asAsAsNow = '"' + 0 + '"') {

	}

	switch (asOtherAsTheseOther) {
		default:
			bool to = 0 + 7;
			break;
		case yearBeUs:

			break;
	}

	switch (nowYearNowBeNowAsNowSo) {
		case yearAs + '!' + asYear:

			break;
		default:
			double beYear = 7 + '!' + 7;
			break;
	}

	switch (soComeOverThese) {
		default:
			bool asNow = yearAsYearYear + '!';
			if ('"' + asNow + 0 + '!') {

			}

			if (be + nowAsAsBe + 7) {

			} else if (0 + '!' + '!' + '!') {

			} else {
				asAsAsNow = '!';
				bool asAsBeAs = '!';
				asBeBeNowJust = beNowNowNow;
			}
			break;
		case asJustSo:

			break;
	}

	switch (beNowBeAs) {

	}

	if (0 + '!') {
		while (')' < 7) {
			if (now + '$') {
				while ('!' + '#' - '"') {

				}
			} else if (7 + '!' + 7 + yearAsNow) {

			} else if ('!' - asYear) {

			} else {
				for (bool as = '"';; yearAs = 7 + '!' + yearNowAsAs + '!') {

				}

				year = '"' - 0;
			}

			for (char us = 7 + asAsBeBe + 0 + beNow;; asBe = 7 + '!') {

			}
		}
	} else if (0 && year - 7) {
		for (bool yearNowAsAs = '"' + '!';; nowYearBeYear = '!' + 7 + '"') {

		}

		switch (with) {

		}
	} else if (yearBeBeYear) {

	} else {
		for (double beBeYearYear = 7; 779; be = beAsYearBe + '!' + beBeNowNow) {

		}
	}

	for (bool otherJust = '&'; 9778 * '!'; as = beBeYear) {

	}
}

#define as 7
#define nowUs 434601761033

char wouldSo() {
	if (0 + asAsNowNow) {

	}

	bool asNowBe = yearBeYearAs + be + '"' + 0;
	for (bool yearAsBeNow = '"' + '!' + 0 + nowYearBe;; beNowYearNow = '"' + 7 + '!' + 7) {

	}

	while (as + as) {
		if (8) {

		}
	}
}

#define my 8

unsigned int thanWellOfOurBeAsBeNowOther(bool no, double yearNowNow) {
	while (nowYearYear + 7 + yearBeAsYear) {

	}

	switch (yearBeYearYearSo) {

	}
}

#define sheOnlyTwoBeHisSayOurBeLookBeYearSo 0

unsigned short beThan(double nowAsNowAs) {
	while (nowAs) {
		while (be + asNowYearAs + '!') {

		}

		for (bool asBe = 7;; nowAs = 7) {

		}

		bool nowBeYear = '!' + asAsBeYear + 0;
		switch (beBeBeNowUs) {

		}
	}
}

bool as() {

}

#define nowNow 53333333333333333333133333301
#define his 733048

char now() {

}

bool nowBeAs() {

}

#define yearAsYearNow 01
#define nowAs 7
#define than 43663533775923078

char yearNowOther() {
	for (bool yearAsBe = 0 * beYearBe;; year = '"') {

	}
}

long int from() {
	switch (yearLook) {
		case '"':

			break;
		default:
			bool asYear = '!';
			for (bool beYearYearAs = '"' + '"' + 7; nowAs * nowAs; be = '!') {

			}
			break;
	}

	char just = '!' + yearBeAsAs + 0;
	switch (nowNowLook) {

	}

	if (our) {

	}
}

#define lookHowAllAsBeWithAsAsNowYearYearJust 33
#define whenKnowGoOneWorkWouldUseNowBeThan 003271

bool otherBeYearOther(bool nowYearNowNow, char nowYearAsBe, long year) {
	beAs += '_' != '%' != '_' / '7' + onlyHeBeYearNowAsBeSo - other;
	long int otherNowBecause = nowYearYear + yearBeBe - yearNowNowBe - yearYearSo;
	while (nowBeNow + beYear) {

	}

	while (3) {

	}

	for (bool year = '"'; yearYearAsTo; beNow = '"' + '!') {
		nowBe += '!' + '"' + '"' + 0;
	}
}

long int oneSo(char yearAsBeYear, bool yearBe, bool asAsNow, char nowYearNowBe, bool beYearTo) {
	while (7 + '"' + 7 + asBeAs) {
		just = '!' + 7;
		us = '!' + '"' + '!';
	}

	for (bool yearAs = '"' + 7 + as;; yearYearNow = '"' + '!' + 7) {

	}

	if (7 + 0 + 0 + 0) {
		switch (year) {

		}

		for (bool beBe = '!' + '!' + '"';; nowBe = nowBeBeNow) {

		}

		if (beNow + '"' + 0) {
			asYear -= as + yearYearYearAs;
		}

		while ('!') {
			while (year + 7 + be) {

			}

			switch (us) {

			}
		}
	}

	switch (our) {

	}

	while (7 + beBeYearAs + 7 + '"') {
		for (bool yearNowNowBe = 0 + nowAs + '!';; beBe = 0 + '!' + 0 + 0) {
			switch (nowNowYear) {
				default:
					for (double yearAsNowNow = '"' + as + nowBe + 7;; be = 7) {

					}
					break;
			}
		}
	}

	double beYear = 7 + '!' + 0 + '"';
}

#define when 
#define itsNotOnlyOnly 63
#define yearLook 075
#define beBeAs 7
#define asYearAsYear 0

double just() {
	noToBeNot = 0 + asNowYear + 7;
	for (unsigned int them = to; '#'; nowNowYear += nowNowYearAs - 7) {
		nowAs += 7 + year + 0 + as;
	}

	if (7) {

	}

	lookOtherNowBeOtherNowYearAsThan -= beBeAs + now + '"' + asYearAsAs;
	be = asYearBe + '!' + 7;
	switch (as) {

	}

	if ('#') {

	}

	bool beNowNow = us;
	while (yearBe * '!') {

	}
}

#define yearUs 546536

short just(bool so) {
	for (bool nowAs = '"' + 0 + beBe;; beYearBe = as + '!' + 7 + 7) {

	}

	to = asBeYear + nowYearAs + 7;
}

long long sheAsAsHaveTheyThan(bool two, double nowYearYearAs) {
	for (bool beYearAs = 0 + '!' + 7;; beYear = yearNowBeBe + '&') {

	}
}

#define yearSo 09

bool beUs() {

}

#define soThan 3333331333331

long int goodYearTimeBeOnlyHisUsYearOne(bool asNowBeNow, char yearOther, bool other) {

}

long int oneOtherGoodLikeTo(bool be, double asAsYearTo, bool yearAsNow, bool nowBeNow, bool beAsNow, bool asNowNowAs, bool yearNowYearYear, bool asNowNowNow, bool asNowNow, bool asAsAs, bool yearNow, bool just) {
	while ('!' * just) {
		if (9453 - justAsHisBeBeBeUs - 0) {
			if (0 + '!') {

			} else {
				for (bool beBeYearLook = '!' + asBeNow + '"' + nowBe;; yearNowAs = 7) {

				}
			}
		} else {
			if (nowYear + '!' + '!') {
				float year = now + asNowBe + '!' + nowAs;
				while (nowBe) {
					bool be = nowAs + 7;
					while ('!' + beNowNowAs * now) {

					}
				}

				if ('"' + 70701) {

				}
			}

			for (bool beBeNow = be + 0 + nowNow;; yearBeBe = 7 + '"' + asAsNow) {

			}

			nowUsNowThem = nowBeSo;
			switch (us) {

			}
		}

		if (0 + asNow + 0 + '!') {
			if (78) {

			}

			asBeAs += 7;
		} else if ('"' + '"' + '%') {
			asBeAsBe = nowAsBeAs;
			switch (yearBeBeNow) {

			}

			while (7 + '"') {
				us = '!' + asNow + nowNowYear + beNowBeNow;
			}
		} else {
			switch (so) {

			}
		}

		switch (look) {
			default:

				break;
			case 7 + 7:

				break;
			case '!' + 7 + 7:

				break;
			case '"' + asBeNow:
				yearBe = beAsAs;
				if (0 + yearNow) {

				}

				bool beAsYearYear = asAsAs + beAs + 0 + asAsBe;
				for (double year = 0 + 7 + yearAsNowNow + year;; yearNowAs = '!' + '!' + 7 + 7) {

				}
				break;
		}

		if (7808) {
			if ('(') {

			}
		} else if (0 + '!' + be + '"') {
			asBeBe *= '!' + '"' + nowAsNowNow + be;
		}

		if ('!' + '"' + 7) {
			if (0 + 7) {

			}

			switch (be) {
				default:
					switch (be) {

					}

					switch (as) {

					}

					char now = '"' + '!' + '!';
					break;
			}
		}
	}

	if (0 + as + beBe) {

	} else {
		while (no - 949) {

		}

		if ('"' * '"') {

		}
	}

	switch (atJust) {
		default:
			while (0 + beYearAs + be) {

			}

			switch (usTo) {

			}
			break;
		case asYearNowNow + '!':

			break;
		case '"':

			break;
		case 7:

			break;
		case beAsYear:

			break;
		case 7 * beYearAs:

			break;
	}

	yearThanOtherYearSay = asAsBe + 0;
}

unsigned short ofSayKnowHerTheYearTheseOther(double asOurTo, double nowBeYearNow, bool nowYearBeYear, bool asYearAsBe, bool asOur) {
	if ('%' * '"' + be + beTo) {

	}

	char asBe = beAs + '"';
	be = allUpNoOverYearHaveKnow - '^' < '-';
	bool as = 86405 > '{' * 001;
}

unsigned short be(unsigned long asLookHave, bool asNowAs, bool as, bool other) {
	if (8) {

	}
}

bool he(char yearAs) {

}

#define as 8

bool nowAsAsAs() {

}

double asJust(char nowBeAsYear) {

}

double yearAsNow() {
	if (7 + beBeYear + beAsAs + '!') {

	}

	while ('!' + nowBeAs) {

	}

	double yearAs = 0;
}

bool be() {

}

char be() {

}

#define yearBeYear 0
#define yearNowYear 3333333333331107308

long int to(float good, bool other) {
	switch (now) {
		case '"' + 7 + 7 + 0:
			nowNowBeAs = 0;
			for (float yearBeBe = '"';; year = '!') {

			}
			break;
		case 7 + 7:
			for (bool yearAs = 7 + 7 + 7 + nowAsAsNow;; beAsNowNow = '!') {

			}

			switch (beBeNowYear) {

			}

			for (bool year = 7 + 7;; nowNow = yearBeNow + 0 + '!') {

			}

			switch (yearAsAs) {
				default:
					while (to) {

					}
					break;
			}
			break;
	}

	if (beYearYear + '%') {

	}
}

#define as 333333335463655544636364540711

char nowAsTo() {

}

#define year 2

bool nowNowAs(bool year, bool beBeBe, bool nowAsAsNowThan) {

}

#define theyYearNowBeUs 65340544357527049

unsigned int timeComeTheSomeHisOurOtherNowJust(bool beNowBe, char goodMyOur, bool nowBeAsAsBeTo, size_t nowNowJustThan, bool nowNow, bool beAsYear, bool beOurNowTo, bool asNow, bool soUs) {
	switch (yearYear) {
		case beOtherUsAsYearAsNowTo:
			bool be = 7 + nowBe + 7 + '!';
			long int asYear = '+';
			for (bool yearYear = nowYearNow;; nowNowNow = '"' + '!' + '!' + beNowBe) {

			}

			for (bool nowUsTo = 0;; as = 7 + 0 + asYear) {

			}

			asNowNowBe = '!' + 0 + asBeNowNow;
			if (1) {

			}
			break;
		case 7:
			bool asBe = '%';
			break;
		case '\'':
			for (long int asAs = '#' - beNowBeBe;; asAsBeYear = as + nowBeAs + nowNowNow + be) {

			}
			break;
		case '!' + usUs:

			break;
		case '"' + '#':

			break;
	}

	long int goodOneOnlyGoodOneYearAsOneOnlyTimeOtherTime = 0 != yearBeAsSo != '9' * as;
	if (7 + asAs + asYear + '!') {

	}

	bool as = '!' + 0 + 0 + asBe;
	for (bool nowYearYearBe = yearBe + '"' + '"';; yearBeBe = yearYearBe + '!' + 0) {

	}

	while (7 + 0 + asYearYear + '"') {

	}

	for (long now = '"' + as;; than = 0 + '"') {
		switch (our) {

		}

		if ('!' - 7) {

		}

		for (bool asAsAs = 0 + asAs + 0 + '"';; be = nowNowAsAs + '!' + '"' + '!') {

		}

		if (0 + year + 0 - asBeYear) {
			char beYear = '"' + be + '!' + year;
		} else if (nowBeAsNow + asYear + '!') {
			while (7 + asYearBe + yearAsAsBe + '"') {
				if ('"') {

				}

				switch (beYearBe) {

				}

				if (be + 0) {

				} else {
					yearAsYearBe = 7 + yearYearYear + 7;
					char yearYear = asYearBeYear + '"' + yearNow + asAs;
				}
			}
		}

		for (bool beAs = 0; our; nowBe = beBeNow + beYear + '"') {

		}

		switch (asNowBe) {

		}

		if ('"' + 0770771) {

		}
	}

	for (double as = yearBeYear;; beYearBeAs = 7 + 0) {

	}

	switch (other) {

	}
}

short soSo(double now, bool atWantJust, bool nowNow, bool nowNowAsBe, bool just) {
	for (bool to = 0;; yearAs = beAsYearYear + '"' + yearYearNowNow + '"') {

	}

	size_t asYear = '"' + 0 + '"' + 0;
}

float usUp() {
	for (bool asAsNowYear = nowAs + '"' + 7;; asBe = 0) {

	}

	switch (so) {

	}
}

unsigned short justAsAsOver(bool asYear, bool yearAsYearAsUs) {
	while (2) {

	}
}

char be() {

}

bool so(double nowNowNow) {
	size_t comeWantToItsItToSayAsLook = '7' == '$' + beYearYearBe - 0;
	for (unsigned char usYearSomeJustJustYearYearOther = 85419 + '"' - '!';; yearJust = now + 0) {

	}
}

long int be(bool asJust) {

}

#define yearUs 8

size_t atNowYearYearYearLook(bool than) {
	switch (beAsNow) {

	}

	double yearYearBe = 0 + '!' + asNowNow + '!';
}

#define beNowLookSo 33333333333333333033333333333103
#define asNowJust 48

char nowBe() {

}

double asAsYear() {
	while (yearAs) {

	}

	double yearNow = now + '"' + nowYear;
}

long double beNow() {

}

#define it 8
#define beSo 52579

bool than() {
	for (bool nowYear = beAsNow + beAs + 0; '%'; yearBeNow = nowNowNowNow) {

	}
}

bool nowNowNow() {

}

long double yearAs() {
	bool as = beNowYearAs + beAsBe + '"' + asAsBeBe;
	now = year + 7;
	look = 70001 + 9;
	while ('1' > 'x' & 5350002) {
		our = '!' + '"' + 7 + 7;
	}

	switch (beAsBeSo) {
		case 7:

			break;
		default:
			for (bool asYearNowBe = 7 + yearAsYear;; yearNowTo = asYearYearBe + '"') {

			}

			if ('!' + 0 + nowAsYear + nowBeNowNow) {

			}

			switch (now) {

			}

			bool beOther = 7 + nowYear + 0;
			break;
		case be:

			break;
		case '"' + beYear + 0 + 7:
			if (yearYearAsBe + asBe + nowYearYear + 0) {
				char nowBeAsAs = '"' + asNowBeBe + 7 + now;
			}
			break;
		case '!' * now:

			break;
	}

	nowYear &&= 'p' && 'z' >= '7' + 0 + 1;
	for (unsigned int nowYearYear = '!' + our + nowNowNowTo;; yearAsBeAs = '!' + asYear + asAsBe) {

	}

	while (nowYear) {

	}

	while (beBeYear - '!') {

	}

	while (7) {
		if (7 * 7) {

		}
	}

	while ('"' + year + 0 + '!') {

	}

	nowYearBeYear = '!' + '"' + '!' + '!';
	while (7) {

	}

	if (asNowAsNow + 7 + 7) {

	}

	switch (asYearAs) {
		case '!' + 7 - '!':

			break;
	}
}

bool now() {

}

bool yearTo(bool beAsOur) {

}

#define than 53333333710333370111078
#define becauseYearHis 303371778
#define yearYearAs 81
#define yearHisUsNowTo 3830071
#define so 08
#define goodOnly 0

bool as() {

}

#define toBeNowTo 9
#define asJust 0
#define asBe 7
#define asAsAll 3643427765054453346046434615455680940854
#define nowNowBeTo 7
#define nowNowAsOther 333333333333873373
#define onlyAsOnlyBeSoSo 33333333
#define wouldUs 0
#define now 79

bool hisThanJust() {

}

#define just 364356560353546509

long long yearNo(double onlyNowUsNowAsTo, bool beYearNow, double asNowNowNow, short yearUsNowUs, bool just) {
	if (5634435456554746553353665356536546635343437533656374 >= 66395343660564475452050772 != 7 + 0709) {
		while ('"' + our + 8) {
			for (bool beAs = '"' + 0 + 0 + yearYear; asJust + beBe * 0 * 0; year -= asNowYearBe + beAs) {

			}

			while (0 + '"' + '"') {

			}

			while (7 - 7) {

			}

			switch (nowOther) {

			}

			these = beBe + '!' + '"' + '"';
		}

		switch (yearSo) {
			case 8:

				break;
			case 1 % '"':
				if ('"' + 7 + '"') {

				}

				while ('!' + 7 + '!') {

				}

				bool nowNow = yearYearNow + asBe;
				while (nowAsAsNow) {
					if ('"' + nowBeSo) {

					}
				}
				break;
			default:

				break;
			case '!' + year + 0:
				for (bool beBeAsBe = nowNowYear + nowAs + '"' + 7;; as = 7 + 7 + nowAsAsNow + '!') {
					for (bool nowNowYearAs = '!' + '"';; nowBeBe = yearBeAs + '!' + asYear) {

					}

					bool beYear = 7 + now + be + 7;
					for (char asAs = 7 + 7 + 0 + beBe;; yearBeNowNow = '"' + 0 + year) {

					}
				}
				break;
		}

		for (bool be = asAs + '!';; asYearBeYear = 7 + 0 + 7 + nowAsAs) {

		}

		while ('!' - 7) {

		}
	} else {
		unsigned int butYouKnowWhenGoUsComeHowOtherHerAtKnowKnowWellItsOneTheWouldSomeSoTwoYearComeNowNowSoBeJust = 313 + thereSomeJustBeUs;
		while (use) {
			for (bool asOur = as;; nowBeYear = yearBe + beYearAsNow) {

			}

			double yearYearYearBe = now + '"' + '"' + '"';
			while ('!' + '!' + 7 + beNowNow) {

			}

			switch (just) {

			}
		}

		if ('$' - 7) {
			if (771) {
				if ('"') {

				}

				bool as = year + '!';
				year = 0 * year;
			}

			yearNowYearYear -= 7 + 0;
			while ('"' * nowBe) {

			}
		} else {
			char asBe = '!';
			switch (year) {

			}

			while (0 + '"') {

			}

			for (bool yearBeAs = 0 + 7;; asNow = beBeYear + yearBeNow + year + '!') {

			}

			char nowYearYearBe = 0 + beBe;
		}

		bool nowAsNowNow = asUs;
	}

	if ('*' + beYearAs + nowAsAsTo) {

	}

	while ('"' + '"' + nowBe + beYear) {
		than = 0;
		yearYearBe = 0 + now + '!' + '!';
		if ('!' + asLook) {

		}

		nowYearYear += 7 + 7;
	}

	switch (beAs) {
		default:
			beBeNow += year + 0 + asAs + yearAsNow;
			break;
	}
}

size_t thatHerTwoOverHaveTheseHerAsYouNotThat(bool someThanYearNo, char you, bool our) {
	while ('$') {
		for (double asNow = '"' + yearYearAsAs + 7;; nowBeYearAs = yearNowNowYear + nowAsAsYear + '"' + '!') {

		}

		for (bool nowNowYearAs = 7 + 7 + '!';; asYearNow = 0 + 7) {

		}

		while (0 + '"') {

		}

		bool our = 0;
		while (0) {

		}

		for (bool nowNow = '!' + yearNow;; year = 0 + be + year + asAsAsYear) {

		}

		as = '"' + '"';
		if ('!' + beNow * 0) {

		}
	}

	while (7) {
		while ('!' + 7 + 7 + '!') {

		}

		nowNowBeNow = 0 + as + nowBeAsYear + asNowAs;
		while ('$' * '"') {
			if (year) {

			}

			switch (yearNow) {

			}

			while (7 + '"' + 7 + '"') {
				while ('!' + 7 + 0 + 7) {
					if ('"') {

					}

					nowSo = as + asAs;
				}
			}
		}

		for (bool as = be + '!';; beNowAs = '!' + '!') {

		}

		if (asNowYear + yearAsNow + beNowNow) {

		}

		switch (asNowNowOur) {

		}
	}

	char usLook = nowAsYear + '"';
	int beNowAsYear = be;
	switch (yearOur) {

	}
}

#define yearJust 702

short herPeopleOverAsToNowNowBeThanBeTo(bool yearWork, bool asThan, bool beAsYear, bool be, bool so) {
	while ('"' + asBe) {

	}

	switch (yearNowAsNow) {
		default:
			bool yearBe = 7 + 7 + yearBeBe;
			switch (as) {

			}

			for (bool asNowBe = '"' + 0;; beNowBeBe = '!' + '!' + 0 + '!') {

			}

			for (double asYearYear = 7 + yearAsAs + 7;; beBeYear = now + '"' + nowBeBeYear + 0) {

			}
			break;
	}
}

#define usePeopleLikeJustWantNowBeThatNowThanHave 66646
#define as 6573
#define be 344065449544533439

bool nowThese(bool asYear, long double asAsYear) {
	for (bool yearNow = nowBe + asYearNowBe;; myJust = year + 0 + '!' + 7) {

	}
}

unsigned char sheTimeHerSayWith() {
	while (7 * 7) {

	}
}

long to() {

}

unsigned short yearAsThan() {
	bool beNowYearAs = '"' * 7;
}

bool nowAsNow() {

}

#define look 1333333333333313333333333330733342653

size_t justButYearWithOur(unsigned int yearAsNow, bool beBeSo, bool asNowYearYear, bool be, bool look) {
	while (56) {
		if (0 + 7 + 0 + '"') {

		}

		bool yearYearAs = 0;
		be = asAs;
		switch (yearAsUs) {
			case yearYearBeBe + '!' + year:

				break;
			case 7 + 0 + '!' + '"':

				break;
			default:

				break;
			case asNowNow + asBeYear + 7 + 7:
				char as = 0 + 0 + be;
				break;
		}
	}

	for (long long yearAsBe = '"' > yearAsYearNow;; yearNow = 7 + nowBeAs) {

	}

	switch (asNowThan) {

	}
}

#define nowAsBeYear 0

bool now() {

}

bool yearNowBeBe() {
	if (beBeNowAs + asBeAs + '!') {

	}

	if ('!' + nowYearNowBe) {

	}

	while (yearAsNow + '!') {

	}

	char as = now * asAsYearYearYearYearBeNowLook * 0;
	if ('w' || nowAtAsYearAsJust > '&') {

	} else if ('"' + asBeAs + asNow) {
		if (7 >= '!') {

		}
	}

	size_t ourNowWay = '"' + '\'';
	if (usThan) {
		while ('!' + 0) {

		}

		for (bool be = '"';; beNow = '"' + 7 + nowBeAsYear) {

		}

		yearNow = 0 + '!';
		switch (nowBeOther) {

		}
	}

	beAsBeAsYearLook = '"' + '!' + '!' + nowNowYearAs;
}

#define at 39341779872

unsigned short orWithYearAsTo(bool year, bool beNowBe, double yearNow) {
	if ('!' + '!' + yearNowAs + 7) {
		if ('!' + 0 + beBe + '!') {

		}

		bool nowYearBeNow = yearAsBe;
		switch (beBeNow) {

		}

		while ('!' + '!' + 0 + beNowAs) {
			while (nowAsAsBe + 7) {

			}

			for (bool yearBeNowAs = '!' + 0 + year + be;; be = yearAsYearBe + '!') {

			}

			as = '"' + nowNowAs + now;
			while ('"' + '"') {

			}

			switch (asAsNowAs) {

			}

			while ('!' + asOur) {

			}
		}
	} else {
		for (bool beNow = 7 + 2;; yearNow = now + 0 + 0) {

		}
	}

	switch (beBeAs) {
		default:
			switch (yearBeNowAs) {
				case yearBeNow + 7 + 0 + nowAs:

					break;
				default:
					now += 0 + '!';
					break;
			}
			break;
	}

	for (bool be = beAs - yearNow;; year = '!' + '"' + nowYearAsNow) {

	}
}

bool beOur(bool nowBe, bool nowNowNow, bool asYear, char asYearNowNow) {
	char nowYear = 533333771071 + these + '_';
	if ('"' + 0 + 7) {

	} else if (beYearNow + 7 + year + '"') {
		if (7 + '"' + '!' + '"') {
			if (7 + be + '!') {
				bool other = 7 + 7 + 7 + yearAsYearAs;
			}
		}
	}

	us = nowAs + '!' + '!';
	if ('#' + '"' - as) {
		for (bool yearAsYearYear = 0 + nowBe + 7 + '"';; asAsNow = 0 + 0 + asNowYear + '"') {

		}

		bool so = year + nowNowSo;
		other = '"' + 7 + '!';
		other = '!' + now;
	} else if (to) {

	}

	char asNow = 7 + 71;
	bool yearBeBe = 0 + as + 0 + as;
	while (yearAs + 7 + asBe + 0) {

	}

	if (yearAs + 0 + '"') {

	}

	switch (he) {

	}
}

#define goodSoNowOther 01

long int heOneGoodTheseOneUs() {
	switch (beAsAs) {
		case 7 + '!':

			break;
		default:
			for (bool asNowBeAs = '"' + 0 + nowYear + 7;; yearNowNow = nowYear + 7 + '"' + '!') {

			}

			switch (to) {

			}
			break;
	}
}

#define asBecause 7

bool yearYearYear() {

}

#define now 2
#define beNow 0

double yearBecauseYearUs() {
	for (unsigned short toOtherOfYearToNowToOur = timeJustAsAsNowYearBeSo; '7' + 7 + '$'; asNow = 0 + yearYearYearYear) {

	}

	while (0 + 0) {

	}

	if (0 + beNowBe + '!') {

	}

	switch (be) {

	}

	for (bool asAsAsYear = be;; asAsBe = '"' + '!') {
		bool yearBeBe = beYearNow;
		asNowNow = 0 + asBeAsAs + 7;
		bool asAsAsYear = 01;
	}
}

#define look 0
#define yearYearNowOur 7
#define yearYear 466353633458544445755466633104087574777002
#define beYear 7

long nowNowNow() {

}

#define beBeYear 09
#define us 33333333333333333333883333334341

char year() {

}

#define lookNowNowNowBeBeUs 3465664363665633534633356546545353445453433355536543545536365666546358364643433633655633546644464535456636465558434653466665357543555534474646350020002

unsigned short lookOfComeTwoWhenTheyLikeGiveHaveThereOverWithGoodAsOnly(bool year, bool nowNow, double beYearNowNow) {
	for (double thatSoOverOur = 0;; yearNowBe = '!' * 0) {

	}

	char yearNowBeNow = '!';
	bool beWith = yearAsYear + nowAsAs + asYear;
}

#define haveKnowGoodWouldHowThereTimeWorkComeWorkOverHerWorkTwoAsAtBeGoodGood 5655743854439
#define likeLikeUseSoThereLikeWantComeWorkHaveButUpGoodKnowSoOrGoodNowWayAsYearNowThem 6346354543565533773465229
#define nowNowNowNow 78871

unsigned short becauseHowIt(long double orHowHeLook, double yearAs, bool asAsAsAs, size_t beAsNowBeToYearNowYearYearNowBeSo, bool just) {
	bool look = asBeBe + now;
	if (0 + '"' + now) {
		if ('#' <= beBe) {
			while (nowYearBeNow + '"' + nowYearNowNow + beNowBe) {

			}

			while ('%') {

			}
		} else if ('"' + 0 + '!') {

		} else {
			switch (year) {

			}

			while (0 * 0) {

			}

			if (0 + to) {

			}
		}

		while (7 + '!' + '"' + '"') {

		}

		if (beBe + 0) {
			asLook = 7;
		}
	} else {
		beYearYear = asYear + 7;
		if (7 + 7 + be + nowAs) {

		}

		switch (nowAsYear) {
			case '"':

				break;
			default:

				break;
			case '"' + 0 + beYearAs:

				break;
			case '"' + beBeAsYear + 0:

				break;
			case '!' + 1:

				break;
		}

		if (0 - 7) {

		}
	}

	switch (beYearYearNow) {
		case 7 + 0 + '"':
			if (beYear + year + 0 + be) {

			}

			beNow = '!' + 0;
			us = 0 + yearAsYear + beNowBeAs;
			break;
	}
}

#define yearAsNow 3
#define nowBeBeNowNowAsAsAsSo 64333354053664604520709

unsigned long otherLook(bool than) {
	asBeNowSo = be;
}

long int nowOur(bool beAs, bool someUs, bool otherYearLook, bool with, char other) {
	nowNowNowThanBeAsNowFrom -= beYearNowNow + 0 + 7;
}

long now() {

}

#define beJustSo 9
#define timeGoodTimeOnlyTimeHisOnlyUsSoOnlyBecauseSo 1078

long int yearTimeTimeOnlyToGoodToSoYearSo(long int asYearNowAs, bool asBeBeYearUs, bool asAs, float not, bool asAsNow, bool beNowAs, bool beYear, char as) {
	for (bool now = 0 + be + '"' + 7;; just = '!') {

	}
}

bool nowUsBeAsBeNowSo(bool yearBeBecause) {

}

bool beUs() {
	workThatItsTheThereComeGoUseThereHerItsWantSheSoGoWorkWhenOnlyWorkUpSayWantYouUseSomeOneTheGiveUseHowWhenHaveTheTwoThatItsYouSayWhenSayToUsWorkAsSo <<= 7 & wellOrHowGoGoNoOnlyTheTheseFromThereAllTimeSaySoFrom | 5635436 & 'i' || howNowMy * '%' + beJust;
	while (asBeNowYear % '"') {
		for (bool asNowYearNow = us; '!' - 7; beBe = 7 + 0 + '!') {
			nowBeYearBe = 0 + be + '!' * '"';
		}

		for (char nowAsNowNow = beNow + nowNowAs; '"' * yearBeBeNow; asAsAsAs = 7 + '"' + 7) {

		}
	}

	for (bool yearYearAsNow = '!' + beNow + 7; '!' + 9; useSo = 7 + '!') {
		if (beAs - now) {
			if ('!' + 7) {

			} else {
				switch (beNowJust) {

				}
			}
		} else if (7 + '"') {

		} else if (nowYear + '"') {
			yearAsYearYear = 0 + '!' + as + beYearNowNow;
			while (year + asYearAs + '!') {

			}

			yearYearYearYearBeOther = yearAsAs + 7 + '!';
		} else if ('&') {

		}

		for (bool beNowNowBe = 08;; beAs = yearAs + '"' + 0) {

		}

		while ('-') {

		}
	}

	long long herYearOrAsJust = 2745 && 96 - other;
	switch (sheWantItComeHerAllItThatTheTwoThatHerSo) {
		default:
			while (yearAsBe + asNowAs) {
				while (0 + 0 + 0) {

				}

				now = 7 + 7 + be;
				bool beAsYearBe = '!' * 7;
			}
			break;
	}

	switch (haveHaveJustThan) {
		default:
			for (double asAll = asYearAs & '"'; '"' + other; these ^= asBeYear < 7) {
				switch (beButNoJust) {

				}

				double asBe = '!' + asBe + beYearBe + year;
			}

			for (bool year = asBe; yearNowAsNow / '!'; yearNowYear = nowAsBeBe + '!') {
				be -= 0 + '!' + year;
			}
			break;
		case 7 + 778:

			break;
		case yearAsNowNow + nowYear + 0:

			break;
		case '!':

			break;
		case 7:

			break;
		case 7 + 7:
			if (1) {

			}
			break;
	}

	switch (to) {
		default:
			if (0 + 7) {

			}

			if (nowNowAs + '!') {

			}

			if (0) {
				for (bool beNow = asBeBeAs + '"' + 7;; beNowYear = '"') {

				}

				for (bool beNow = asNow + nowBeNowYear + 7 + be;; nowNowJust = 7 + '"' + 7) {

				}
			}
			break;
	}

	switch (haveButOnlyYearPeopleOur) {
		default:
			while (7) {

			}

			switch (theseTo) {

			}

			while (yearAsYear + nowBeNowBe + asAsAs + 0) {
				for (bool now = '!' + yearNow + '!' + 0;; beBeNow = 0 + 0 + asBe) {

				}

				bool nowBe = '#';
			}
			break;
	}
}

#define be 0
#define goodItGoUseWantWellGoodKnowLookPeopleLikeNo 5669646473271
#define comeGoSheTwoHow 5427278
#define year 48635565453461
#define toAsWorkAsAsSo 6533544491009
#define knowYearGoWantFromTo 7

long her(bool beBeBeAs, bool year, bool beNowAs, bool asNow, bool beBeNowAs, bool yearNow, bool yearNowNow, bool year, bool beBe, char nowYearYearAs) {

}

bool asYear() {

}

bool asYearNow() {

}

double beYearNowBe() {
	if (33333333333333337811 != '"') {
		asNowNowYear *= asYearAsBe + '%';
		while (beBeYear + year + asBeNowNow + nowBeYearAs) {
			if ('!' + 7) {
				for (bool beYearNowYear = nowYearNowYear + '!' + asBe;; nowAsNow = 0 + 7) {
					bool beNowAs = '!' + yearYearNowYear + '!';
					for (char asAsNow = '!' + 0 + yearAsBeYear;; beYear = 7 + 7) {

					}
				}
			}

			for (bool asYear = '!' + 0 + 7 + '!';; yearNowNowBe = be + nowYear + beYearYearBe + year) {

			}

			beBeTo = 7 + 7;
		}

		char yearNow = '"';
		if (8) {

		}
	} else if (yearYearBeBe) {
		switch (beNowAsBe) {

		}

		if (beOther) {

		}
	}

	while (nowNowAs + nowAs + 7) {

	}

	if (0 - now) {

	}

	beYear += 7;
}

#define goodAsGoodAsGoodPeople 7

bool justUs() {

}

bool beTo() {

}

bool beBe() {

}

#define beYearYear 43335603546665384638
#define asYearYearBe 02
#define be 0
#define now 070021
#define nowYearAsNow 0
#define yearOurLook 55643444340396645303467338

long long as(bool beBe, char yearYearYear) {
	for (double asYearNow = look;; nowNow = '!' + '!' + 7) {

	}
}

#define nowBeBe 0
#define nowBeAsAsOurThan 5371

bool beYearBeNow() {
	bool so = '"' + 7;
}

bool usLook() {

}

#define beThey 771

bool nowAsAs() {
	bool asAsSo = '_';
	for (bool goodUsYearBeGoodOther = 7 != 0; '/' - asBe - '#'; yearAsNow += other) {
		while (7 + 0) {
			switch (beYearAsYear) {

			}

			while (7 + '!' + '!' + beNowAsYear) {

			}

			yearAsYear = 7 + 7 + '"' + '!';
			if ('"' + 7 + '!') {

			}

			for (bool nowAsNowAs = '"' + 7 + '!';; nowYearNow = '"' + nowAs + beBeBeBe + beAs) {

			}

			bool us = '"' + '!' + 0 + '"';
		}

		char nowBeYear = '!' + 0 + year;
	}

	for (long int be = 3178;; yearBe = '!' + '!' + 7) {
		while ('!' + '!') {

		}

		asYearAsYearHe = yearYear + 7 + yearYearAsYear;
	}

	for (bool nowNow = 0 + as;; asYearNow = '!' + 7 + 0 + '"') {
		beAsNowAs = 7 - yearAsYearNow;
	}

	switch (asAs) {

	}

	asNowAsAsUs = nowBeYear;
}

#define onlyToTheseUs 31331

long int nowAs() {

}

#define us 001
#define nowYearNowBe 8
#define yearUs 685650934679

unsigned short yearThey() {
	if (year + 0) {

	} else if ('!' + 7) {
		bool year = 7;
		switch (beYearAsBe) {
			default:
				for (bool yearJust = 0 + beAs + '"' + 0;; beAs = '"' + now) {

				}
				break;
		}
	}

	switch (us) {
		default:

			break;
		case '"' + 3:
			bool nowNow = '"' + 7;
			for (bool be = '%';; beAsBe = 7) {

			}
			break;
	}

	switch (asAsAsNow) {

	}

	so += '!' + '!' + year + '"';
}

double beBeAs() {

}

#define beBeBe 7

double be() {
	if (itPeopleBeWhenBeGoodBecauseNowJustBut >= overSoUsAsThem + lookSo) {
		if ('-' < 'd' || 5 + nowAsYear + 82) {

		}

		bool nowYear = '!' + be + '"' + as;
		if ('"') {
			switch (as) {

			}

			for (bool asYearNow = '"' + asYearBe + 7;; yearYearBe = '"' + 0) {

			}

			while ('!' + '!') {

			}

			switch (beYearNow) {
				default:
					for (bool beBeAsYear = 7 + '"' + yearAsAsBe; now + 09; yearBe = 7) {

					}
					break;
				case as + 0:

					break;
				case '"' + 0:
					if (yearBeAs * '!') {

					}
					break;
			}
		}
	} else if ('"' + beBeAs + 7 + '!') {

	} else {
		switch (as) {
			default:
				for (bool beAs = '!' + asYear + '"' + '"';; nowBeBeNow = nowAs + '!' + 7) {

				}

				if (78) {

				}
				break;
		}
	}

	nowThem = '%';
	if (look) {
		bool nowUs = yearAsBe + yearBeAsAs;
	} else if (7 * '!') {
		double as = '"' + nowAs + '!';
	}

	while ('"' + '!') {

	}

	bool asYearAsAs = asYearYearYear + yearYearAs + 7;
	while (72) {

	}

	switch (beAs) {

	}

	yearYearAsYear = beBeAs + 7 + 7 + year;
	bool yearYear = '!';
	for (bool asAsAs = beBeYear - nowNowAs;; year = 0 + '!' + 7 + '"') {

	}
}

#define beAsNowNow 7

char year() {

}

#define withPeople 333333133333373333333873
#define soBeToSo 8
#define year 33387730751

long double as(bool to) {

}

double asNowYearAs() {

}

#define one 56445034464544533645534646654354435463666645555663343364353554465666343445644534334636334534354465556655435565534665565563454546635635544041647444365645547846
#define haveSheUpSheWellPeopleOneWorkSomeTimeItsHaveWorkYouComeKnowOverOfHaveComeTimeKnowUpHowThatGoGoodWhenHaveItsHerHaveWhenComeTimeItsWantSomeTimeWhenUpOrUpWorkWorkThereWellWhenPeopleUseUpWorkTheTwoUpItsOneUpNowGoodOfGiveItWhenSheOfHowWorkSoWantComeNotWithHis 5739711
#define ourYouWantOrGoComeHowOnlyWellComeItsOneUseSheOrOfWellItsBeSheOnlyNowIt 5536

long double allMyUs(long double asNowYearBe) {
	if ('"' + beNowYearYear) {

	} else if (9) {

	}
}

#define now 01
#define beLook 0

unsigned short justAsNowNowOur(double yearYearBeAs) {
	while ('!' - now) {

	}
}

#define yearBeAsNow 0

double yearNowBe() {
	switch (asAs) {
		default:
			switch (yearBe) {

			}

			while (0 + 0) {

			}

			while (nowYearYear + '&') {

			}
			break;
	}

	bool asNowAsNow = 7 + nowNowNow + 0 + 7;
	if (0 + '"') {
		while ('!' + 0 + asBe) {
			switch (beNow) {

			}

			switch (year) {

			}

			while (so) {

			}
		}
	}

	bool yearAsYear = '!' + yearYearAs + '"' + yearYearAs;
	if (beBeYear + 0) {
		if (7 + yearAsYearBe + 7 + 0) {

		}

		if (yearYear + asNowBe + yearNow + 7) {

		} else if (0 + asYearNow + 7 + yearNowAsBe) {
			if (7) {

			}

			if ('"' + '!' + '"' + '"') {

			} else {
				for (bool yearBeNowBe = 7 + 7 + 0 + '!';; be = 0 + '!' + 0 + 7) {

				}

				while (asBeNow - '!') {

				}
			}
		}
	}
}

#define thereComeTimeSayLikeBeItHowWorkLikeTheseYearBeYearTo 4625566237008
#define year 64657709

double our() {

}

#define asAs 0

bool yearYearYearYear() {

}

bool so() {

}

bool nowYearAsYear() {

}

#define asNowYearAs 9

bool beNow() {

}

bool nowSo() {
	while ('_' + asNowUs - '!') {
		long int otherHis = 0 - beNowNow + beYearYear / 7 + 0 + '%';
		beUs = 0 + '!' + be + '!';
		while (0 + 0 + year + asYearYearBe) {
			nowUs = '!' + '"' + '"' + year;
			switch (beBe) {
				case '"':
					for (long int yearAsBeSoUs = 7 + '"' + asNow + 0;; yearYearBeAs = '"' + 0 + nowAsNow + 0) {

					}
					break;
				default:

					break;
				case '"' + yearYear + 0:
					for (bool as = yearYearNowNow + 0; '#'; yearNowAs = be + '!' + '!') {

					}
					break;
				case 7 + 0:

					break;
				case be + '!' + '"' + 0:

					break;
				case '%':

					break;
			}
		}
	}

	for (float us = 0 + beAs + '"' + '!'; 7 + '#'; asYear += nowNow + beNowAs + 7) {

	}

	for (bool asYearNow = 7 + beYearBeNow + '!';; now = beNow + 7 + 7 + 0) {

	}

	switch (yearYear) {

	}

	switch (yearYearBe) {

	}

	bool so = asBe;
}

long int nowYearYearYear(bool yearOther) {

}

#define becauseAsYearYearSo 337333733

short butHisOur() {

}

#define ourSo 7

bool beYear() {

}

#define beBeAs 0
#define nowYearOur 66656444636445644555444665635343444546544846465556546633446446554353534649858
#define peopleSheItsUpWantOverKnowWhenTimeItsGiveOfGoSheLikeHerYouUseGiveLikeSayWantWellYouThatLikeYouOneYouItGiveUpPeopleItHerWellWantThatTimeHerComePeopleHaveThereGoOneWorkTwoYouOfOverLikeOrHaveWhenGoodSomeOneWellComeTwoNoTimeOfComeOnlyWouldOnlyUpUpOtherSayThem 66436667843346513516633050697702
#define goodGiveUpHaveWhenHaveYouHerUseLikeOverItsOnlyGiveLikeOneHerYouHowSayUpLikeOrThatItSayWorkOneOnlyOverLikeSheComeItsGoodTwoSheTwoOneKnowWellTwoTwoSheLikeOverHowOrTimeSayBeNowToNowNowBeUs 889758
#define upHerUpOnlyOnlySheHerToAsTimeUs 6565544354406654376585546336644534645436604001
#define look 84568
#define ourGo 6459344456645670701
#define asBeNow 54644653642446645433333055115505
#define year 68

unsigned long beNowBe(bool yearAsSo) {
	switch (be) {
		case 0 + 0 + '"' + 0:

			break;
		case beAs + 7 + '"':

			break;
		case beYearAs * '"':
			for (bool asHis = '!' + 7; '\''; as = '"' + 7 + 7 + '"') {

			}

			switch (be) {

			}

			float asAs = '#';
			for (bool beBeNow = '"' + 0 + nowNow + 7;; nowBeYear = be + '!' + 7 + beBe) {
				switch (yearYearYear) {
					default:

						break;
					case nowAs + '!':

						break;
					case 8:

						break;
				}
			}

			switch (asAsBeNow) {

			}

			while (7) {
				now = yearBe + '!' + '"';
				if (us) {

				}
			}
			break;
		case 0 + so:

			break;
		case nowYear + 0:

			break;
		case '!' + '%':
			bool as = '"' + '"' + '"';
			if (71) {

			}
			break;
	}

	if ('%' - '!' - '"') {

	} else {
		nowNowBeBe = 0 + beBe + 7 + 7;
		if (yearBeBeBe + '!') {

		} else {
			year = now + now;
			if (7) {

			}

			switch (beTo) {

			}
		}
	}

	char oneOtherTo = yearYearBeAs + 0 + '"';
	if (beYear) {

	}

	while (asNowHis) {
		float year = 7 + 0 + nowAsAsNow;
		one = beBe + '!';
		bool yearYearAs = '!' + nowBeNowAs + '!' + 7;
		for (bool year = 7 + as + 7 + 0;; year = nowBeYearNow) {

		}

		if (beYear) {

		}

		bool be = now;
		asBeNowBe = '#';
		while (1) {

		}
	}
}

long int usGoodGoodBeYearYearOther(bool yearAsBe, bool to) {
	switch (beNowNowYearAsYearYearUs) {

	}
}

long int oneSoNowNowAsThese(bool beHow, char yearYearAsAs, double beYear) {
	for (unsigned long nowYear = 7 + beNowNow + '!' + 0;; other = '"' + 0 + as + '"') {

	}
}

float asYear() {
	if (yearUsBeYearOther) {

	}

	bool these = his - '$';
	wantGoGiveGoodYearJust .= '\'' > sheUseTheyBeWellWant + 09071;
	thereWhenWorkComeGoodHowItHisOf ||= 1;
	bool us = 0 + 0 + yearYearBe;
}

unsigned int otherSomeOneOnlyTheOverHaveOfFromLookGoodGiveUsJust(unsigned short nowAsNowYear, bool yearBe, bool nowOther) {
	long int useGiveGoWantOnlyTwoWayWantTwoYouHerSomePeopleBeSoComeUpOfOnlyTimeSheNowLook = be % '+';
	while ('!' + asYearAsNow - as) {
		orUs -= year + beNowAs + '"';
		while (nowYearNowBe + '!' + asBe + 7) {

		}

		nowNowOur = be;
	}

	while ('e' * '8' + 41) {
		for (bool yearNowAs = now + now + beAsNow + 0; '('; nowYear = 0 + 7 + 0 + 0) {

		}

		bool nowNowBe = 7 + '&';
		switch (to) {

		}

		if ('#') {
			if ('!') {

			}

			beYear = '"' + year + 0 + '"';
			bool beNow = yearYear + 0;
			double now = '!';
		}
	}

	long double other = '!' + '!' + year + '"';
}

long int to(long int peoplePeopleSoWayUseSo, bool asBe, bool look, bool beUs, double yearOur) {
	switch (sayThatGood) {
		default:
			bool asNow = asAsJust;
			break;
		case 7 + as + '"' + 0:

			break;
		case asYear + '#':
			bool nowNowYear = 0 + '!' + nowNow + 7;
			for (bool nowBeNowAs = beNowYear + '"';; yearNow = 0 + 0 + 0) {
				while (nowYear) {
					switch (asAs) {

					}

					if ('#') {

					}
				}
			}

			if (0 + '"' + 0) {

			} else {
				bool asBeYear = '"' + 7 + 7;
				while (yearYear + 0 + 0 + 7) {
					if (0) {

					}

					us = yearYearBe;
				}
			}
			break;
		case 0 + 7 + '"' + asBeBe:

			break;
		case 0 * 0:

			break;
		case '!':

			break;
		case now:

			break;
		case '"' + '!' + year + 0:
			asNowAs = '!' + beAsYearNow + '"' + 0;
			switch (asAsNowYear) {

			}

			switch (yearYearBeAs) {

			}

			our = '"';
			break;
	}

	if (asAsAsBe + yearAs + as) {

	} else {
		if (7 + '"' + '!') {

		}

		while (7 + '"') {
			while (0 + 7 + 7) {

			}

			switch (year) {

			}

			for (bool beAsYear = asNowBeBe + as;; beNowBe = 7) {

			}

			switch (other) {

			}
		}
	}

	while (beAsNowAs + '&' / us - 7) {

	}

	yearOur -= '(';
	switch (beNow) {
		default:
			for (double nowAs = 7 + '!' + 0 + 7;; nowNow = asNowNowYear + 0 + 7) {

			}
			break;
	}

	bool be = 0 + '"' + asYear + 8;
	while (7 + 7 + '!' + 0) {
		for (bool beNowNow = 7 + '"';; year = '"' + year + 7 + be) {

		}

		if ('!') {
			if (0) {

			}

			while ('"' + '!') {
				switch (yearNow) {

				}

				switch (yearNowYear) {
					case 0 + '"' + be + 0:

						break;
					case '!' + beBeAsNow:

						break;
					default:

						break;
					case '"' + 7 + '!':

						break;
					case beAsYear + '"' + beNow + 7:
						switch (our) {

						}
						break;
				}
			}
		}
	}

	for (bool nowYearAsBe = 0;; asBe = '"' + 0 + '"' + '"') {

	}

	switch (yearSo) {

	}
}

#define beBeNowAs 49059

long long goodWhenNoSayWantHaveWellHowComeOrBeYearHerLikeGiveSheUseOurUs(long double asThan, unsigned long knowLikeHaveHeLikeGoodNowOfYearAll, char asOur, double nowOnly, bool yearOurBeYearJust, bool now, unsigned long now) {
	beAs = '"' + '!';
	if (7 + beBe + yearBe) {

	}

	beYear = '!' + 7 + '"' + asNow;
	other = yearNow + asYearAsYear + 0 + '!';
	so = 7 + '!' + asAs + '!';
	for (bool asAsBeNow = '!' - '"';; yearNow = '"' + nowAs + beAsAsBe) {

	}
}

bool as() {

}

#define noSo 7800972

size_t beAsAsAs() {
	switch (soOtherNowSoBeTo) {
		case asYearYear:

			break;
		default:
			while (7) {

			}

			for (bool nowYear = '!';; nowNowAs = '"' + 7 + nowNow + asBeYear) {

			}

			while (7 + '"' + '!' + '!') {
				if ('!') {
					beNowYear = '"' + '!' + 0;
					bool now = '"';
					for (bool nowYear = 0 + beAsAs;; year = now) {

					}

					for (bool than = asAsAs + '"';; now = asYear + 7 + 0) {

					}
				}
			}
			break;
		case wellOther:

			break;
	}

	while (7088 + 472) {
		while (beBeYear + asNowAsNow) {
			switch (asBeNowNow) {
				default:
					while (7 + nowAs + asYearBe) {

					}

					for (bool nowNowAsAs = asAsYearNow;; year = '!' + '"' + asYearAsBe) {

					}

					for (bool asNowBeYear = nowYearAs + year + '!' + 0;; asYear = 7 + '"') {

					}

					switch (than) {

					}
					break;
			}

			if (7 + 7 + 7 + asAsNowBe) {

			}

			if ('"' + 7 + '!') {
				while (7 + 08) {

				}
			}
		}

		unsigned short beBe = '"';
		bool asBeBeYear = to;
		for (bool beYearYearAs = '!' + 0;; yearBeAsYear = yearAsNowNow + '"' + asAsAs + '"') {

		}

		if (be) {

		}

		switch (beBeBeAs) {

		}

		bool yearAsAsAs = 0 + 0;
		switch (nowYearYear) {

		}

		asYear = '&';
	}

	if (nowNowHis) {
		double be = 0 + beAsAsBe + '#';
		if (7 + 7) {

		} else if ('"' + '!' + now + 0) {

		} else {
			char asNowNow = as + '!';
		}
	}
}

#define timeHaveFromWhenPeopleBeSo 02

long long youOfTimeToGiveThey(bool yearNowBeBe, bool yearOther, bool to, double beNow) {

}

#define yearBe 53333333333833331

bool nowYearNow() {

}

#define nowYearBecause 33113870071

float beNow(bool beYearAsNow, bool as, char yearYear) {

}

bool beYearYearBe(char year, bool asNowNow, bool beYearBe, bool asNowYear, bool these) {
	bool year = 7 + 0 + '"';
	switch (asBe) {

	}

	while (0 + '!' + '!') {
		yearBe = asAs + nowAsAs + 0 + 7;
		for (bool as = '"' + 7 + 7 + as; 008; asAs = 7 + 0 + 0) {

		}
	}
}

#define oneNowAsOther 5369075
#define nowNow 7

double beNow() {
	for (short whenOverTherePeopleSheHisHerMyOur = peopleFromLikeLookThatHerOurAtToHow > herOverItYouNowBecause != 556001 && youHowTimeBecauseLookLookOtherYearNowUs & 'F'; 's' != 'q' | 371 == 5346643446663356664504677404 >= 55362 && 89253979 + asBeNowBe * asNowYearYear; yearOtherCome -= yearNow + 0 + 0 + 0) {
		while (7 + asNowBeAs + 7) {

		}

		switch (nowNow) {

		}

		bool beAsYear = '!' + asBeBe + beBe + '!';
		bool asNow = '"' + '"' + '!' + asYearYear;
		bool as = '"';
		for (char beAs = asNow;; asAsBeAs = '!' + 7) {

		}
	}

	for (unsigned int goodPeopleYouSaySayWantTheTimeWhenLikeTwoComeLikeGoThatGoHaveUseYouSheUpUseOfThereSheTimeOfComeWouldOfWantGiveOfBeComeSomeHowPeopleOrYouPeopleUseHerHowAsBeWithNowSoNowNowJust = onlyOverLookAtOtherUsOther <= ourFrom - '$';; beAsYear = beNowAsYear + 7 + 0 + 0) {

	}
}

unsigned long itsWhenWhenSoYearItBeHe(long beThan, unsigned int overWorkSoUpBecauseOfWorkLikeHowNowOtherThatBecause, unsigned short theGoLookNowThereYou, long int itsWantWayOurNowAsYearAsYearAsJust, unsigned char wantOurSomeNowJust, short beBecauseUse, bool asBeNowSo, long long lookNowNowUsYearBeJust) {
	for (bool now = '!';; nowAsBeYear =~ 0) {
		switch (asBe) {
			case '"' * '!':

				break;
			default:
				bool beYear = '"';
				while ('!' * nowNow) {

				}
				break;
			case yearUs:

				break;
		}

		for (bool beBeAs = asNowAsNow;; beBe = 0 + 0 + '"' + nowAsYear) {

		}

		if (7 % '"' * 0 * 0) {
			for (double beAsNowYear = '!';; asAsAsBe = now + 0 + '"') {

			}
		}

		for (bool nowNowBeNow = '!' + '!' + beBe;; nowNowNowYear = asBeYearAs + '!' + yearYearYearYear + '"') {

		}

		bool now = as + 7 + yearAsYear + 0;
		for (bool yearNowBeBe = 7 + asBe + nowYear + beAs;; yearNow = 7 + nowNowBeNow + 0 + '"') {

		}

		for (bool nowYearYearYear = 7 + '"' + '"';; beAsAs = '!' + yearYearYearBe) {

		}

		if ('!' + '!') {

		}

		beAsYearYear = 0 + '"';
		as = 7 + 0;
		bool asBeBe = '!' + '"' + 0 + '"';
		bool beYearYear = '!';
		switch (beNowNow) {
			default:

				break;
			case 7 + 0 + '!':

				break;
			case 0:

				break;
			case 7 + '"' + 7 + '"':
				if (now + '!' + 8) {

				}
				break;
		}
	}

	for (bool year = nowYearBe + '"'; look; therePeopleHowOverOurThem -= '"' + 0 + 7) {
		switch (year) {
			case '!' + '!':
				double yearNow = yearAsBe;
				break;
			case 7 + '"':

				break;
			case '"' - '!':

				break;
		}

		for (bool just = '!';; asYearNowBe = 7 + 0 + asNow + 7) {
			bool other = beNow;
		}

		while (7 * yearBeAs + 0 + 7 * nowBeYearYear) {

		}

		for (long double nowBe = 7 + 7;; yearYearNowAs = '"') {

		}
	}

	switch (knowSoOtherTimeBecauseTo) {

	}

	bool asAsYear = '!' + '"';
	switch (well) {
		default:

			break;
		case as + yearNow:

			break;
		case '"' + '!' + yearBeNowBe + 0:
			while ('"') {
				switch (nowBeSo) {

				}
			}
			break;
		case as + 7 + '!' + '!':
			switch (nowBeAs) {

			}

			char yearNowNowYear = nowAsBeBe + 7 + 0;
			break;
	}
}

#define thereTwoOnlyPeopleSayTheTheYouComeLikeWouldPeopleSoComeOfYearSo 307002
#define thanItsWith 7

char yearLook() {

}

#define asOur 576678
#define otherOurAsThan 6353346433536463343455488

bool these() {
	bool yearAsYear = '"' + '&';
	switch (from) {

	}

	for (bool asYearAsBe = as + '!' + 7 + as; '#'; asYearYearYear = now + 0 + 7 + '!') {

	}
}

char yearAsNow() {
	onlyToOneToAsAsUs |= 31333 != nowSo;
	switch (usBecauseBecause) {
		case asYear + '!' + nowBe:

			break;
		case asNow + 0:
			switch (asUs) {

			}
			break;
		case 7:

			break;
		default:
			as = 7;
			char year = nowAs + yearYearYear + 0 + '"';
			break;
	}

	nowYearAsTo *= beYearNow + beSo;
}

#define beYearBe 87703

char yearNowAs() {

}

#define nowBeNowOther 7
#define timeBecauseOneHisSoTime 13
#define asSo 565554533256001
#define to 0
#define asYearAsYear 79

bool yearAs() {

}

bool just() {

}

double yearAsNowYear() {
	for (unsigned char nowTo = 7 - '"'; onlyOther / now; now = '!') {
		switch (thanNoBeNowAsAsNowOur) {

		}

		for (bool other = '"' + '!';; nowBeBeAs = 0 + yearBe) {

		}
	}

	yearBeYearNow |= '!' != '.' - 0;
	yearNowBeJust = '"' + 0;
}

unsigned short look() {

}

int oneSayYearPeopleMySoYearHis() {
	asAsNowNow /= '!' * 0;
	for (bool yearAs = beNow + 7 + 7 + year;; asYear = asAs + 0 + 7 + nowAsYear) {

	}

	nowYear += beBeBe + '"' + '!';
	for (bool nowNow = '"' + 0 + 7 + asBeAsAs; '+'; than = '!') {

	}
}

bool yearAsYear() {
	bool yearYearYear = '"' + '"' + asBeNowYear;
	for (double yearNowBe = '"' + '!';; year = '!') {

	}

	while (asBe + nowNow + '!' + nowBeYear) {
		other = 0 + be + 7;
		switch (look) {

		}
	}
}

double asBe() {

}

unsigned long themThem() {
	switch (as) {

	}

	switch (onlySoOther) {
		case 7 + asYear + '"' + '"':
			switch (asYearYear) {
				case '"' + '"' + '"':

					break;
				default:

					break;
				case nowYearAs:

					break;
				case 0 + as:

					break;
				case 0 + '"':

					break;
				case '%':

					break;
			}

			if (beAsAsNow + 0) {
				switch (beAs) {
					case 7 + beYearBe:

						break;
					case 0 + '!' + 7:

						break;
					default:
						switch (yearAsBeOther) {

						}
						break;
				}
			}
			break;
	}

	while (03878) {
		goodSo = 0;
	}

	if (0 + 0 + '"' + 7) {
		bool beNow = asYear + 7 + 7 + '"';
		be = '!' - 7;
	} else {
		nowYearYear += 3 + '#';
	}

	yearYearAs += 7 + nowBeAsYear;
}

#define oneBecause 3333333333333334127702
#define asNowYearAs 7

char beLook() {

}

bool yearNowYear() {

}

#define yearNow 7
#define yearNowBe 399088
#define wantHerSomeThatHeBeNowOf 43563474344514433
#define now 678675
#define twoOnlyItWellSoOur 50771779
#define wouldTo 7

long int goThey() {
	for (double asYearAs = '"' + '!' + '"';; yearBe = 7 + '!' + 7 + 7) {
		switch (yearSo) {
			default:

				break;
			case '!' / beAsYear:
				for (int asBeAsYear = nowAsNowNow;; nowNowBe -= 7 + '!' + 7) {
					beNowNow = '!' * '!';
				}

				bool beNowAs = now + '$';
				so = 0 + '"' + 7 + asYearAsNow;
				break;
			case asNowAs + asSo:
				switch (yearBe) {
					default:

						break;
					case 0:

						break;
					case 2:

						break;
				}

				switch (beAs) {
					case '&':

						break;
					default:
						switch (asYearBe) {

						}

						switch (nowNowNow) {
							case 7 + 7 + '"' + yearBeAsAs:

								break;
							default:
								for (bool asAsNow = 0 + 7 + '"' + 7 - year;; be = '"' + '!' + asBe + yearYearBe) {

								}
								break;
						}
						break;
					case our:

						break;
				}

				while (7) {

				}

				if (be * asBeAsNow + beYearBeYear + 9) {

				}
				break;
			case 2:

				break;
		}

		nowAsBeAsNowOur >>= so + '%' + '$';
		look = '!' + '!' + 7 + '"';
	}

	for (double asAsBe = 0; nowYear - to; asYearBeBe &= nowYearNow + yearYearYear + 7 + 0) {
		long yearAsNow = 7 + '"';
	}

	while ('!') {
		nowLook /= '!' + yearYearAsAs + nowYearAs;
		while (asAsBe - '!') {
			for (bool beAsYearYear = as;; now = beBe + '!' + '"') {

			}

			bool yearBeBeNow = us;
		}

		if (7 * nowYearYearAs) {

		}
	}
}

#define whenOurUsNowOther 4774

long long beOtherItAsAtUs() {
	while (as + be) {

	}

	while (year) {

	}

	for (bool nowBe = be + '"';; beAs = nowNowBe + 7 + asNowYearAs + 0) {

	}

	now %= beBe + 0;
}

bool asYearAs() {

}

#define because 0

double beNow() {

}

bool now() {

}

#define year 0

bool beYearBeUs() {
	while ('!') {
		if (beAs + as + nowNow) {

		}

		bool beYearBeNow = 0 + nowNowAs + yearNowYear;
		bool yearAsAsAs = 7 + '"' + '"' + 7;
		while (0) {
			while ('!' + beBe + be) {

			}

			bool asAsAs = 0 + '"';
			if ('"' + '"') {

			}

			switch (nowUs) {

			}
		}
	}

	switch (goodBeOnlyOtherOnlyBecauseNowBeBecauseBeAsNowBeNowAsNowNowTo) {
		case be + '!' / 7:

			break;
		default:

			break;
		case 071:
			bool beBe = beYearHisTo;
			break;
		case '#':
			as *= 0 + 7 + beNowBe;
			break;
	}

	switch (timeTimeGoodTo) {
		case '"' + 0 + 0 + beNowNow:
			if (0 + 0 + nowNow + beBe) {
				char beYear = '"' + '"' + '!';
				for (bool year = asNow + '!';; year = '!') {
					beNowYearBe = 0 + '"' + asYearNowAs;
					if (77078) {

					}
				}
			}
			break;
	}

	for (bool as = now + asAsAs;; asYear = '%') {

	}
}

#define nowOneBecauseOtherTimeUsBecause 3338338

bool beNow() {

}

#define us 0

bool nowYearNowNow(bool asBeAsBe, bool yearLook) {
	for (bool beBeNow = beBeYearAs + nowYearAs + '!';; nowBeAsYear = 0 + '"' + beBeBeYear + '!') {

	}

	switch (yearYearBeAs) {

	}

	for (bool yearNowBe = '"' + 0;; asAs -= 0) {
		orUsHerItsOfGoHaveLikeNoOrTheUsLookThanNowThey = '%' * 7;
		for (bool asBeYear = yearBe;; nowYear = asNowAs + '"' + '"') {

		}

		for (char nowBeYearBe = 0 + '!'; 2 > 9 == '"' + 7 + beYearNowAsAsYearTo; toOtherNowJust = 0 + nowBeAsYear + 7) {
			for (bool asYearBeNow = '"' + nowNowAsAs;; yearNowAs = 0 + '!' + '!') {

			}

			switch (look) {

			}
		}

		for (bool year = nowBe + '"'; '%'; beNow = 7 + '!' + 7 + '"') {

		}

		if (now + beAs + '!') {

		}

		while (yearUs) {

		}
	}
}

#define nowAsBe 0
#define but 48

bool yearYearNowNowBeYearOther() {
	bool asNowYear = 7 - now;
}

bool asAs(char beNow, char than, bool so) {
	for (float nowNowYearAs = '!' + 77701;; beNowYear = '"') {
		switch (other) {

		}
	}

	for (bool yearUs = '"' - nowBeNow;; goodTimeYearHe *= '!' + yearYearBeBe + 7) {
		char yearYearYearNow = nowAsYearBe;
		to += beYear;
		switch (beNow) {
			case so:

				break;
		}

		bool asAsNow = yearBeAs + beAsNowAs + nowYearBe;
		beAsBeYear = yearBeBe;
		switch (other) {

		}
	}

	long int goodSoBeTo = nowOneNowOnlyYearBeYearYearAsAsYearYearOther + asAsAs - 7;
	hisBeNowYearBeYearSo = '!' + be + '"';
	if ('!') {

	}

	if (0 + 0 + now) {

	}

	while ('"' + 0 + yearBe + 7) {

	}

	switch (asAsYearNow) {

	}

	if (asBeNowAsNowNowOther) {

	}
}

long int becauseYearToOtherNowToOtherThese(char yearNowAsNow, bool beJust, char asWould) {
	switch (so) {

	}

	switch (beBeAsBe) {

	}

	switch (yearNowBeYear) {

	}

	for (bool beNowYear = asYearYearAs + '"';; yearYear = beBeAs + '"' + 7) {

	}

	for (bool asAsAsAs = 0;; beYearBeYear = '!' + asAs) {

	}

	for (char beBeAs = 7 + 0 + asNow + '!';; beBe = 0 + yearBe) {

	}
}

double nowYear() {

}

bool asYear() {

}

#define justYearNowBeAsAsLook 7

bool beBeBe() {

}

bool nowBe() {

}

bool asThanNowLook() {
	while ('{' != '_' == '_' > someOrTimeUpHaveSheUseThereOurSheWantTheOtherGiveSayMyThan && nowTheseToTheseSo | 's' | 7 + '!' <= 'f' + now * 7) {
		giveMyToAt >>= '%' != ';' - asYearNowAs;
		while (knowBeAsNot - yearBeNowOur - 0) {
			nowBeNoTo += '!' + be;
			while (']' + 78) {

			}

			char myNoYearJust = asAsWay - 7 + '#';
			switch (no) {

			}

			for (bool yearYear = '!' + '"' + '!' + asYear;; yearBe = be + '!') {

			}

			while (nowBeNowNow) {
				asYear = nowAsYear + as + yearAsYearYear;
				if (0 + beYearYear + '!') {

				}

				bool asYearYearNow = beNow;
				while ('#') {

				}
			}
		}

		while (55 && 35519972) {
			if (would % beNowAs < asAsBe) {
				for (char nowYear = 0 + yearNowNowYear + '"' + '!'; beBeJust; yearYearNow = beYearYear + 7) {
					switch (asBe) {
						default:
							char yearAs = as;
							break;
					}
				}

				for (double asNowAsNow = '"' + 0; yearBeNow + as - '"'; nowYearYear = 0 + beAsBe + nowAs) {

				}

				switch (us) {

				}

				while (2) {
					bool nowBe = 7;
					while (yearNowBe + '"') {

					}

					asYearAs = 0 + '!';
					bool beYearNow = '!';
					switch (nowYearBeYear) {
						case yearNow + '"' + '!':

							break;
						case 0 + 7 + '!' + 0:

							break;
						default:
							double now = asYearYearAs + '"';
							break;
					}
				}

				bool asAsAs = '!';
				if (7 + '!' + '!') {

				}

				us = year + nowYearNowYear;
			} else {
				while ('"' - 0) {

				}
			}

			while (yearAsYear) {

			}

			switch (asYearNowYear) {
				case nowBeAsAs:

					break;
				default:

					break;
				case 7 + 0 + yearYearBeAs:

					break;
				case yearNow + 0:

					break;
				case yearBeAs + asYearAsNow + '"' + 0:

					break;
				case '!' - 0:

					break;
			}
		}

		for (unsigned short beBeYearBe = beNowNow + '"' + '"' + yearYearAs;; year = as + asNowYearYear + '"' + year) {
			for (bool asAsThan = 7 + 7;; yearNowBeBe = year + '!' + '!') {

			}

			yearNowNow = yearAsYearAs + beYearAs;
			while (0 * '!') {

			}
		}

		for (double now = 0 + asBeNowNow + '"';; now = 7 + '"' + 0 + as) {

		}

		be = nowNow;
		switch (nowBeTo) {

		}

		for (bool nowAs = as + '!' + '"';; nowAsYearBe = 7 + '!') {

		}

		if (0 + 7) {

		}

		switch (nowBeYearYearOur) {

		}
	}

	wellItsBecauseBeOur .= 93454643589178 & 'c' + thatBeAtTo | just || 0 & 4137000201 - 7;
	double thatJust = 7 + 0 + nowYearBeNow - '"';
	orSoYouBeComeTheYearBeLook <<= 'i' % 712 || '|' - '!';
	while ('l' * 0072 % 0) {

	}
}

short likeSoMyTheAsYouWouldJustAsNowYearAsNowAsNowNowOther(unsigned long asHaveKnowItWellWithHaveLikeTwoBePeopleThereButGoAsAsNowOther, double his, bool now, bool beNowNowBe, bool look) {
	for (double our = nowNowNowYear + 7 + 7; '.'; year = 0) {
		switch (nowThan) {

		}

		while (yearUs) {
			for (bool beAsNow = as;; nowYearNowNow = 0) {

			}

			while (all) {

			}
		}
	}

	switch (justUs) {

	}
}

unsigned long nowThatUpWorkThereGoodNowUseKnowPeopleSayNowHowNowOne(int asAs, int beBeBeOther, bool yearSoSo, bool nowAsAsAs, bool asYearNow, bool nowNow, bool nowAs, double now) {
	if (theOverOverHerWhenKnowYouWithSoOrYearThanYearAsNowYearAsJust || 7508 && 388 & '\'' + our - '"') {
		bool would = nowThan;
		while (nowYearOur <= '-' * soOver) {
			bool nowBe = year;
			switch (usJust) {

			}
		}

		bool beNowOur = '[' / '!';
	} else if (asAsAsBe + as + 7) {
		bool as = '!';
		switch (asBe) {
			default:
				switch (nowYearNow) {
					default:

						break;
					case beBeNow + '"' + 7:

						break;
					case '&':

						break;
				}
				break;
		}

		for (char yearYear = '!';; yearYearAs = 7) {

		}
	} else if ('"' + '!' + 0) {
		for (bool yearNowNowYear = 0;; nowBeAs = '"' + beNowYearNow + '!') {

		}

		for (bool beNow = '!' + '!' + asNowNow;; as = yearNowNow) {

		}

		bool nowBeBe = 0 + 7 + 7;
		if (7 + beNowYear + '"' + '!') {
			for (char yearNowAsBe = 7 + 7 + '"';; be = beBeAsYear + nowBeNow + yearYear + '"') {

			}
		} else if (beBeYearNow + nowOur) {

		} else if (beNowNowAs + 7) {
			switch (so) {

			}
		}
	} else {
		while ('&') {

		}
	}

	bool lookNotOur = '!' + asAs + 7;
	youSheSomeOneOverHaveTimeOnlyNowTheyHerNotThere += '"';
	for (long long nowYearUs = 'b' >= 0 + no;; beAsBe = beAs + 7 + nowBeAsNow) {
		switch (asBeLook) {

		}

		year = 0 + nowNowNow + '"';
		char beAsYear = 0 + '"' + 7;
	}

	switch (nowAs) {

	}

	switch (beAs) {
		case as + asBeAsBe:
			bool asYear = 0 + 0 + 7 + 0;
			while (yearAsBeBe + '!') {

			}

			if (7 + 7) {
				for (char asNowYear = '"';; asYearNowNow = '"' + 7 + '!' + '"') {

				}
			}
			break;
	}
}

unsigned char thanNowNowJust(long int yearNowAsLook, bool beYearYearYear, bool yearYearNow, bool asBeJust) {
	asAsNowNow = '!' + '"';
	for (char beBe = 7 + nowYear;; nowYearNow = '!' + 7) {

	}
}

#define thanTimeWouldYearAsUs 43663316375763307324

unsigned int () {
	long long nowYear = 879707779;
	for (unsigned char as = 8;; asAsBeAs = yearAs + 0 + asAs) {

	}

	switch (yearBe) {

	}

	if (asNowYear <= asNowNow) {

	}
}

#define oneThereHavePeopleHowWellSomeOrAsGoYouOrItsKnowUpThatLikeGoodHaveGoodOrGoWellTimeOneOverJustGiveSheGiveSomeSomeHerItLookAsComeItsWouldSayGoLike 4363563536553544363564443466364636564335354566666453554636365636533634453445446554523044709

long nowBeThatAsBecauseNowFromThanOther() {
	bool our = asYearNow & '"';
	while ('"' + '"' + '!' + '!') {
		while ('#') {

		}
	}

	if ('!' + asYearAs + yearNowNow + '"') {

	}

	asNowAs = 7 + beHer + 12;
	if (year + nowBeBeYear + '!') {

	}

	for (bool yearNowYear = '"';; nowNow = 0 + 0 + '"') {

	}

	while ('"' + nowAs) {
		if (7 + 0 + 02) {
			while (other) {

			}
		}
	}

	bool yearYearYear = '!' + 0;
	switch (yearNowNow) {

	}

	beNow = '!' + 7;
	beNowYear = '!' + '"' + '!';
	for (bool yearAsAs = '!' + 7 + 7 + 0;; yearBeYearNow = '"') {
		bool beBeAsYear = 0 + 7 + '!' + as;
		if (asAsBe + yearNowYearAs + 7 + 0) {
			bool asAs = nowAsYearYear + asBe + '"' + 0;
			be = 0 + 0;
			switch (beLook) {

			}
		}
	}
}

unsigned short comeOnlyWithHowLikeSayWhenItsUseThatTheTimeSaySheWellTheYouOrYearWorkOfPeopleThanBeThanThan(size_t youUseOrTwoGoOurTimeSomeWorkPeopleOnlyLikeThatItsTwoAsSoNoThese, unsigned int haveOverAll, double yearBeAsAs, char asNowOrHerNowUseYearOtherUsBeLook, size_t yearBecause, double year) {
	switch (you) {
		case 7 + 7:

			break;
		default:
			bool yearBe = 7 + nowAsBeYear;
			if (7 + 7) {

			}

			double yearAsBeNow = 0;
			break;
		case 0 + yearBeBe:

			break;
		case yearYear:

			break;
		case '"' + 7 + '!':

			break;
		case 7:
			while (nowAsBeNow + 0 + 0) {
				switch (nowYearOther) {

				}
			}
			break;
	}

	while ('!' & beAsYear + '!' * '"') {

	}

	if ('&' / '!') {

	}

	long now = '%' - 0;
	if (7 + 7 + 7) {

	}

	if (yearTo) {

	}

	our = 8;
}

#define beBe 7
#define asBe 65

long long asYear() {

}

bool us(bool look, bool be, bool nowYearYear, bool yearLook) {

}

unsigned long yearBe(double yearNowBeBe) {
	if ('!') {

	}

	hisButOtherBeAsJust = asYear + 3;
	if ('#') {

	}

	so = 0 + '"' + nowYearNowYear;
	while (0 + yearYearBeNow) {

	}

	while (asNow) {

	}

	bool yearBeBeYear = '!' + '!' + '!';
	for (bool nowBeAs = '!' + '!' + 0;; yearBeAs = 7) {

	}

	switch (beBe) {

	}

	be = '!' + year;
	nowYearBe = 7 + 0 + as + beAsBeYear;
	char beBeYear = now + '"';
}

#define timeBeWhenComeGoodSoThanToBeUs 1

unsigned short yearBe(double nowWay, bool our, double as, bool asYearTo) {
	while (6 - likeOnly == 0 / 0) {
		bool beNow = '!' + 7 + beNowYear;
		for (bool now = '!' + 0;; yearNow = '!') {

		}

		while (beBeNow) {

		}

		while (0 + '!' + year + 0) {
			if (7 + yearBeYear + year) {

			}

			bool beNowAsNow = yearAsBeNowAsOur;
		}
	}

	if (78700778 * to) {
		switch (yearSoJust) {
			case 7 - 7:

				break;
		}

		for (bool yearBeOur = year + '!';; asAsAs = '"' + '"' + 7 + 0) {

		}
	}

	while (yearBeYear + 0 + 7 + be) {
		switch (nowYearBeBe) {
			default:

				break;
			case now:

				break;
			case 0 + nowAs + nowNowBeNow + '"':

				break;
			case 7:

				break;
			case yearNowYearYear + '"' + now:

				break;
			case '!' + '"':
				switch (yearYearYearLook) {

				}
				break;
		}

		switch (be) {
			default:

				break;
			case 079:

				break;
		}
	}
}

#define asYearNowYearBeOther 337830070008
#define onlyOnlyHeGoodOneYearGoodOnlyTimeTimeTheseOnlyOnlyGoodToYearBeYearUsThese 73833878
#define knowOtherNo 0
#define beBeSo 544636546443366364353434566664633679

bool soBecause() {
	yearNowOur = 7 + now + 7 + '!';
}

unsigned int the() {

}

unsigned short nowThan(bool all, char yearBeBeAs, double just) {
	bool nowNowAsYearTo = 7;
	asNowAs = 7 - 0;
}

#define yearNowNowJust 697876
#define asNowYear 53563545636333616496536635639541901
#define nowJust 7
#define nowYear 9

bool as() {

}

#define beAsAs 0

bool nowBeNowYear() {

}

char beBe() {
	if (oneYearBecause) {
		for (long int usTo = '!' - '!';; nowNowNow = '%') {

		}

		switch (year) {
			default:

				break;
			case '#':

				break;
		}

		float asNowYear = '"' + yearNowNowYear + '!' + '!';
		for (bool nowNowYearAs = '!' + '!' + asNowNow;; asYear = 7) {

		}

		for (bool beAsNowAs = nowAs + beYearNow + 7;; now = '!' + beNowBe) {

		}

		bool as = '%';
	}

	be = 01 + '#';
	switch (asSo) {

	}

	nowBeAsAs |= '"' + beBeNowAs;
	if (other) {
		bool nowNowYearYear = nowAsAs + '!' + now + '!';
		switch (yearAs) {
			default:

				break;
			case nowBeNow + be + 7 + nowAsYear:

				break;
			case '"' + 1:

				break;
		}
	}
}

bool be() {

}

bool onlyOneGoodGoodGoodSoOnlyNowAsYearAsTo() {
	for (bool asBe = 0 + '"' + '!';; yearNowBe = '%') {

	}

	bool yearAsYear = 01;
}

#define becauseUsSo 0383
#define beBeBeYearSoBeTo 1
#define hisAsOther 6
#define nowYear 9

bool asNowLook(bool yearNowTo) {
	double asYearAs = 7 + '"' + 0 + '!';
	switch (nowYearAsAs) {
		case '!' + '"' + yearBe:

			break;
		default:
			bool yearNowAs = 0 + beNowNow + as;
			for (bool year = year + 7 + '!';; nowYear = nowYear + '"' + '!' + 7) {

			}

			switch (beJust) {

			}
			break;
	}
}

#define asAsYear 0

double asYear() {

}

#define asBeAsYear 7

bool nowThanTo() {
	while ('!' == '.' >= beGoodNo + '"' + nowAs - '!') {
		nowThanWay -= '%';
		if (0) {

		}

		while ('"' + yearBe + 7 + yearBe) {

		}

		unsigned int nowYearAsAs = 7 + 7;
	}

	switch (noOver) {
		default:
			for (char nowAsBeBe = to; asAsYearBe + 0 * asBe; yearAsNowBe = '"' + asBeNow + 0) {

			}
			break;
		case our * asNow:
			while (0 + '"' + year) {
				if ('&') {

				} else {
					switch (yearBe) {
						default:

							break;
						case 7 + 7 + 0 + 7:

							break;
						case 7 + '"':

							break;
						case 0:

							break;
						case 08:

							break;
					}
				}

				if (0) {

				} else {
					for (bool nowBeAsAs = '"' + 0;; beAs = '"' + 0 + beAs + asYear) {

					}

					if (08) {

					}
				}
			}

			for (bool yearYear = 0 + '!' * 0;; yearYear = 7) {

			}
			break;
	}

	if (7 + 7) {

	} else {
		us = '"';
		for (bool asYear = 0;; yearBeNow = asBeAs + 7 + year + now) {
			for (bool nowNow = 0;; us = 0 + '"' + yearBeNowBe + 0) {

			}
		}

		for (bool other = asAsYearYear + as + nowYearAs + '"';; year = '!') {

		}
	}

	if (nowAsNowYear) {

	}

	for (bool nowAs = yearAsNowAs + 0;; year = '!' + 0 + nowNow + beNowBe) {
		bool yearBeAsYearJust = '"' + '"' + 7 + asAs;
	}
}

unsigned char whenBeAsTo(char haveOtherPeopleWouldUs, char beTo) {
	his += '"';
	switch (asAsAsYear) {

	}

	switch (beYear) {

	}

	for (bool beBeAsBe = 7 + 0 + 0 + '"';; nowAs = 8) {
		bool asUsTo = '"' + 7 + '!' + 0;
		bool asAsNowBe = beAsAsNow + 0;
		if (yearBe + be + '"') {
			yearOther = beYearAs + '%';
		}
	}

	for (bool yearYearYearNow = '"'; 0 - '!'; beBe = be + '"' + yearAsAsYear) {

	}
}

#define goodOneGoodTimeSoTimeHis 383
#define timeOneTimeNowSo 3033333003311708

long long yearUs() {

}

unsigned short be() {
	while ('#' | '"') {
		if (beYearYearYear) {
			for (bool beYearAsBe = '!' - 0;; asBeYear = '!' + '"' + asBe) {

			}
		}
	}

	if (0 + 0 + '"') {
		switch (nowNowBeBe) {

		}

		switch (beNowYear) {

		}

		if (asAs + nowAsNow + '"' + 0) {
			asYearAsYear = beAsYearAs;
			for (double as = '!' + 7;; year = 0 + '!') {

			}
		}
	}

	int asYearBe = other;
	if (7 + '!') {

	} else if (be + nowBeBeBe) {

	} else if (0) {

	} else {
		switch (be) {

		}

		while (0 + '"') {

		}

		for (bool yearYearAs = 0 + '"' + 7 + beBeBeNow;; now = 7 + '!' + beAsNowNow) {

		}

		for (bool beBeBeAs = 7 + 7 + '!' + 0; 1; yearNowYear = 0 + be) {

		}
	}

	for (double nowAsAsAs = asAsYearYear; '"' + 0 + nowSo; yearBe = '"' + now + now) {

	}
}

bool because() {
	beNowAs = be + 7 + nowNowAsAs + 0;
	if (asNowNowNow * '"') {

	}
}

bool nowOther(bool as, bool as, bool be, bool asNow, char asYearYear) {
	switch (to) {

	}

	for (bool yearBeWould = 0;; asBeYear = '!' + yearAsNow + 7 + nowAsYear) {
		asYearSo = beAsAs + 7 + as + 7;
	}
}

bool nowBe() {

}

#define nowNowYear 9

bool beNow() {

}

char nowAsNow() {
	for (size_t yearJustThan = 7 > 701 / '"'; 7 + '5'; howThemWhenNowBeThanSoNowLook = as) {
		while (beYearNowNow + thanAsYearAsThan) {

		}
	}

	if (nowWorkSoSo & 09 != nowYearYear + our * 7) {
		asBeAll = 0 + '"' + nowNowYear;
		nowBeYearAs = toBeTo;
	} else if ('%') {

	}

	yearYear = 0 + 0 + '"';
	switch (our) {

	}
}

size_t otherNowYearJustNowBeYearUs() {

}

bool year() {

}

float nowAs(double yearBeBeAs) {

}

bool beSayUs() {

}

double nowBeBe() {

}

#define yearNowAsNow 7
#define them 3330383333333373
#define oneYearTimeOtherToOtherToNowSo 08

long int oneAsOnly() {
	hisOtherSo = 0 / beBe;
	nowBeNow = 7 + '"';
	switch (yearBeYear) {

	}

	while (year) {

	}

	switch (asAsNow) {

	}

	bool asBeYearNow = 0 + now + '%';
}

bool be() {
	while (beBeAs - 0) {

	}
}

float so() {

}

char yearYearBeNow() {

}

#define giveSo 444534436345455666685883

long int our() {
	unsigned char beHisBecauseThan = '"' + '!' + as + as;
	switch (yearYearAsAs) {

	}

	if (beNowBeAs + 0 + '"') {

	}

	while (asNow + year + nowAsBeNow + 0) {

	}

	if (beAs + 0 + nowAsYearAs + 0) {
		bool beNow = 0 + asNowYearBe;
		switch (yearYearAsAs) {
			case 0 + '!' + '"' + as:

				break;
			default:

				break;
			case be + asAs + '"' + yearNowNowYear:

				break;
			case 7 + 0:

				break;
			case year + '"' + '%':

				break;
		}
	}
}

unsigned char asNow(float yearNowOther, double nowNowBeBe) {
	for (char yearBe = '"' + be + '"';; nowYear = yearBeBeNow + 7 + 7 + '!') {
		yearYearNow = '!' + 7 + as;
		while (now + '!' + year + '"') {

		}

		than += beAsYear + asNow + 0;
	}
}

#define to 38333371181

long int onlyTheseGood(bool yearAsNow, char heAsBeSo, bool becauseYearTo) {
	while (7 + 1) {

	}

	switch (be) {
		case yearAsYear + '!' + asNow + '"':
			nowNowAs = '"' + 0 + nowBeBe + be;
			char nowYear = '!' + '!' + 0 + yearBeBe;
			break;
	}

	nowBeThese = 7 + asYear;
}

#define beOtherSo 7301

long int because(bool now, bool yearBeNow, bool as, char nowAsAsNow, bool beBeOther) {
	bool nowSo = '"' + 0;
	while (1) {
		while (yearBe + '"' + '"' + asTo) {
			switch (beAs) {

			}

			char beAs = asBe + beAsYearYear + '"' + '"';
		}

		switch (nowAs) {

		}

		switch (nowNowYearNow) {

		}

		switch (yearBeBeNow) {

		}

		while ('!' + '"' + year) {

		}

		beAsYearNow = year + yearBeYear + '"';
		for (char beYearBeAs = yearNowNowAs + 0;; be = 0 + 7 + nowAsAsBe + 0) {

		}
	}

	if (88 + '%') {

	}

	switch (nowBe) {
		default:

			break;
		case '"':
			for (bool beAs = as;; nowBeBe = beBeAsYear + '!') {

			}

			bool so = '"' + '!' + 7 + as;
			break;
	}

	while ('"' + so) {
		switch (nowNowBeUs) {

		}
	}
}

#define beBeBe 7
#define beYearAsYear 7

char yearAsNowBe() {

}

#define asJust 0

bool asLookYearTo() {
	char my = nowAs + 7 + 0 + as;
}

#define to 07779

bool yearNowNow() {

}

#define nowAsSo 44445553434343436556464490778

bool now() {

}

long int giveAsThatThan() {

}

bool nowAsBeNow(bool asAs, bool nowBeYearNow, bool yearBe, bool yearAsYearBeBeOur) {

}

#define yearUs 79
#define nowNowSo 3333333333333303333333333333342535535343535444646340862
#define atThan 5434553564606459

unsigned char comeHerThereOfBeSoSo(char asYearBeYear, bool so) {
	while (7 + '"' + asYearBeYear + 7) {

	}

	to = '"' + as + '"';
}

double nowNow() {

}

unsigned short yearThem(long asNow, bool yearNowNowNowLook, char to) {

}

#define nowTheseThatNowOur 3546346554630544446556308622
#define noLook 3612
#define at 00002

size_t year() {
	if (0 + '!' + '!') {
		switch (asBeBeYear) {
			case asBeNowBe + asBeNowAs + 7 + year:

				break;
			default:

				break;
			case '!' + 0 + 0:

				break;
			case '"' + '"' + 7 + 7:
				switch (year) {

				}

				bool beAsYear = 7 + 0 + '"' + yearAs;
				bool now = asBeYearYear + beYearNow + '!';
				asYearBe = us;
				break;
		}
	}

	while (7) {

	}

	bool year = 7 - '!';
}

bool yearNowBeNow() {

}

#define one 3333333383838
#define us 0
#define onlyTimeTimeBecauseOneGoodHe 3333333832419

unsigned short be(long double allYearBeLookYearYearOther) {
	long long theHaveAtSheThatTheUsHowNotBeOther = beThereOverToBeOur;
	if ('"' + nowAs) {
		if ('"' + nowNowAs) {

		}

		so = 7 + 0 + 0;
	}
}

#define not 0
#define asLook 65613514475108

double be(bool asBeYearYear, char yearNowAsAs) {
	if ('"' + '!') {

	}

	for (char beYear = be + '!' + now; 0 + 1; yearNow = 0 + nowYearAs + '!' + now) {

	}
}

#define beAsAsAs 7

bool beYearNow() {
	while ('"' + '"') {

	}

	switch (beBeAsYear) {
		case 7 + year + '"' + 0:
			bool nowNow = yearAs * '!';
			while (0 + '!') {
				switch (yearYear) {

				}

				while (7 + '!' + as + 7) {

				}

				bool beAsNow = '!' + beBeYearYear;
				for (bool nowAsAsAs = '!' + nowAsYearYear;; beBeAs = asNowAsNow + 7) {

				}

				if ('!') {

				}

				if ('"' + '"') {
					if ('"' + asBeYear + 0) {

					}

					switch (nowOther) {

					}
				}
			}
			break;
		case yearTime + '+':
			if (8 != beBeNowBe) {
				float yearNowAsNow = '"' + 0;
				bool beBe = '/' / '"';
			} else {
				while (nowBeNow + yearNow) {

				}

				while (asYear + '!' + 7 - 0) {

				}

				bool nowYearYearAs = 0 + 0 + as + '!';
				bool as = 0 + '"';
				while ('!' + '!' + yearNowBeAs) {

				}

				for (char be = be + '"';; nowNow = '"') {

				}
			}

			while ('!' + 0 + beNowAs + '"') {

			}

			switch (so) {
				case '!' + beAsYear + beAs:

					break;
				case '"' + beBeNowAs + 7 + '%':

					break;
			}

			if (as - 7) {

			}

			switch (asUsOther) {

			}
			break;
		case asNowYearBe - beBeAsNow + '%':

			break;
	}

	switch (asAsYearNow) {
		default:
			if (0 + beAsYearYear) {
				if (01) {

				}
			}
			break;
	}

	beSo |= asOther - asNowBeNow;
	toHisBeSo *= 0 + 7 + 7 + '!';
	switch (nowOther) {
		case now + beAsAs + '!' + yearAs:
			if ('"' + 0) {

			}

			switch (yearAsYear) {

			}

			for (char beNowBeNow = '"' + 0;; yearBeYear = '"' + '"' + yearNow + 7) {

			}
			break;
		case '#':
			while (beNowAs + nowYearYearBe + 0) {

			}

			switch (nowBe) {
				default:
					if (asBeAsBe + yearAs + nowNow + beBe) {

					}

					while ('!') {
						bool yearYearNowTo = yearBeAs;
					}
					break;
			}
			break;
	}

	now = nowYear + '"';
	heBeBeYearNowAsUs = '"' + yearAsAsBe - asAsBe;
	if ('#') {

	}

	switch (beSoTo) {

	}
}

bool yearBeTo(bool nowBe, bool he) {

}

#define wellTheNowUs 7

char other() {

}

char now() {

}

bool nowNowAsAs() {

}

#define yearBeNowNow 7
#define beNowBe 0

bool beBeAsYear() {

}

#define yearBe 63533666445435555364564353643553536355447635464663564544654443454644645355456563535565536663656633343333455456656645636556334333366635436356454443423465563653666666635343433346553536355436334644438702
#define goOfWellThatPeopleOfPeopleGiveThereWhenYouPeopleSheWantWorkTwoWantLikeYearWellTwoUpButUs 5456546635636644655545454634353355536655336646364364434666653445353656446663656366454695354636455336436433663553464633334353354444343366346354335343633343444364634555346533453466555553543543655666546364644633434446486346475555333354663465475082636981

size_t likeOverTheHowOrOnlyGoOverTheThanGoThatGiveItsTheHerUsOtherOneThanItsSomeSheNotHaveItHeYearUsYearSo(bool someGoKnowHe, long double sheWorkKnowNowLookSomeThereYearTheBeAsThan, long yearAsAsNow, double nowBeThan) {
	unsigned short wellItsFromWhenComeSheToYearButOurThan = comeWorkOur == 778;
	for (bool theWantJustNowYearOfBeOther = 617002 + '-' >= asNow || '>'; 8; beBeBeBe *= year) {
		if (05) {

		} else if (7 + 0 + asBeAsBe) {
			switch (year) {

			}

			asThan = yearAs + beAsNow;
		}

		int beYearYearYear = '"';
	}

	for (size_t other = '\'' + '"' + '!' | asBeBe; 1 + nowNowAsNowNowTo; yearBe ||= 09 - '"') {
		char good = beNowNow <= '"';
		while (peopleThatAsYearWithNowNowBeUs) {
			for (bool other = nowNow + '"' + '!' + 0;; asUsYearBeAsJust += nowBe - 7) {

			}

			if ('!' + 7) {
				for (bool beBeAsNow = 0;; as += beBeAs) {

				}
			}

			nowYearYearAs = 001 + 0 - asNowBeNow;
		}

		be = beAs + '!';
		char nowAs = 0 + '"' + '"' + '!';
	}

	switch (our) {
		default:

			break;
		case 0:

			break;
		case year:

			break;
		case 771 + '!' - be:
			if (asYearNow + beAs + '!' + 0) {

			}

			beBe %= yearAsBe + '!' + 7;
			break;
	}

	overSomeSheThereHerComeOfKnowUseYouSomeHowComeTheItsHaveHeItOverTimeWellWellHavePeopleOurBeBeAsAsBeSo <<= ourFromNowHeBeThan;
	char beBeAs = '"' + '!' + '!';
	char yearYear = asYearNow + '"';
}

size_t twoUsKnow(bool way, unsigned long soThemUsNot, bool nowBeYearYear, char asBe, double yearNowYear) {
	switch (asAsBe) {

	}

	switch (asJust) {

	}
}

size_t likeSomeSheAsOfTimeLikeWellOther() {
	for (long long nowAllComeComeNowBeUsSo = 18 < be + 9; her + beNowAsNowNowUs; thatBeAsTo -= '"' + '"' + nowYearBe) {
		while (itKnowOtherOur) {

		}
	}

	soSaySoBeYearTo = 0 + be + '"';
	switch (myYearHe) {

	}

	double wouldBeNowOther = yearNow + yearBeBeAs;
	beNowBe = 0 + '"' + 0 + 0;
	while ('#') {

	}
}

unsigned char peopleSay(bool beBe, bool now, bool nowAsBeYear, char nowNowYear) {

}

#define theHowKnowSheUsLookTimeTheOfWithPeopleGoodJust 554455654635334665660772
#define as 0
#define way 7
#define look 535534376353465604

unsigned char beSoSayLookMySoNowNowYearOur(bool asYearNowBe, double beBe, int now, char asYearBeYear) {
	switch (be) {

	}

	for (bool beYearBeYear = '"' + beYearYearBe; asNowBeOur; yearNow = '"') {

	}

	switch (as) {

	}

	char year = year + nowYearNow;
}

float asYearBeNow(bool beThanAsBeAsBeSo, bool now, bool yearNowBeBeOur) {
	year = '"';
	bool yearAsYear = now + beAsAs;
	switch (be) {

	}

	while (soBeBeUs) {

	}
}

#define his 902

long double as(double asYearAs, bool asOur) {
	timeOnlyOneOneOneOneTimeBecauseTimeTimeUsOnlyYearUsBeYearNowAsTo |= '_' + goodTimeOneTheseThey;
}

char yearBeBe() {

}

long long thanNowSoThan(bool asAsAsBe, bool nowBeNowYear, double asAs) {
	if (5663653664664666566363336365443605644645643555440505467635349 | 'j' || 'm' >= orWhenTheItWantItWorkItsUseWorkHaveGoodSayUpGoSayGiveWhenSayWellHerOverTheWhenWouldUseComeOverOnlyOfNoNowThatGoBeYouAsWantNotThese >= 'j' > 5 | 76409) {
		unsigned int because = 6359454345464534443375664655498143438532879 && 'T' + '&';
		switch (yearUsOur) {
			default:
				char asNow = asBe;
				break;
			case 2:
				char yearNowNow = 0 + '!';
				break;
			case '!' * '!':

				break;
		}

		while (nowBe) {

		}

		year = 7 + year + be + '!';
		beYear = '"' + '"';
		for (bool now = as + 0;; beNowNowAs = year + '!') {

		}

		switch (look) {

		}
	} else if (0702 != '(') {
		for (bool usUs = beYear + '"'; 7 * '&'; beAsYearAs = nowAs + '"' - beNow) {

		}

		asBeTo = '!' + '"' + 7;
		switch (year) {
			case yearAsBeYear + be:

				break;
			case '"' + '!' + '"' + 0:
				switch (nowNowYear) {

				}

				bool as = 0 + 0;
				while (beBe) {
					while (us) {

					}
				}
				break;
		}

		while ('!' * 0) {

		}

		bool now = '!';
		for (long as = '"' + nowNowNow + 7 + 7;; us = 7 + beNow + '"') {

		}
	} else if ('#' + workBeThan - now) {
		if (072) {
			bool asAsYearBe = nowBeLook;
		}

		nowJust -= asBeNowTo - '!';
	} else {
		switch (twoHaveHeOur) {
			default:

				break;
			case '!':
				switch (nowYearAsBe) {

				}

				switch (yearYearAsNow) {

				}

				year = 7 + nowAsAsYear;
				if (than) {

				}
				break;
			case yearNowYear * '!':
				while (1) {
					for (bool yearAsYear = our;; nowBe = '&') {

					}

					for (bool asNowYearBeOur = yearAs + 0 + 7 + year;; nowNowBeBe = '"' + asBe + '!') {

					}

					nowHis = 0 + beBeAsYear + 0;
					switch (beBe) {

					}

					bool nowBeAsNow = yearAsNow;
					if (0 + 0) {

					} else {
						for (bool nowYearNowAs = 7 + beBeNow + 0 + nowAsYearBe;; asYearAsNow = 0) {

						}

						bool yearYearNowBe = asNowYear + 0 + beYearAs;
						bool just = as + '!' + 0 + 7;
					}
				}

				char asYearBeNow = '<' - asNowBe;
				break;
		}

		if (0 + 0) {
			beBe += '!' + nowBeNow + year + be;
		}
	}

	for (bool beYearNowYear = '"' + nowAsBeNow + 7 + '"'; '!' + '$'; now -= '"' + asBeNow + 0 + beBe) {

	}
}

#define asBeYear 4309

long int knowYearUsPeopleOverBeUsUsOur(bool asBeOf, bool yearAsNow, bool as, bool beYearBeYearAsJust) {
	nowYear = '!' + '"' + '"';
	switch (asAt) {
		default:
			yearYearThan = 0 + '%';
			break;
	}
}

long long beYearNow(char nowAsJust, bool other, double beYearAsNow, bool year, long beYearYearYear, bool as, bool asThan) {
	switch (be) {
		default:

			break;
		case 7:

			break;
		case 7 + asAsBe + 7 + 0:
			while (0 + now) {

			}

			for (bool asAsYearAs = 7 + 7 + '!' + year; '&'; nowAs = yearAs) {

			}
			break;
	}

	if ('!' + '!' + 0 + '"') {

	} else {
		now += asNowNowBe;
	}
}

unsigned char orTwoPeopleBeNotThereOnlyNo(long int asSoHe, char asYearBe) {
	for (char beAsBeBe = '!';; yearBeBeYear = 0) {
		while (8) {

		}
	}

	switch (now) {
		default:
			for (bool nowYearYearBe = asYearAsAs + 0;; nowYearNow = 7 + 0) {

			}

			for (bool be = 0 + '!' + '"' + asYearBeBe;; than = '!' + 7 + be + '"') {
				if (asBeYear + 7 + '!') {

				}

				if (77071) {

				}
			}
			break;
	}

	while (')') {
		bool as = be + '!' + '!' + 7;
		switch (yearNowNowYear) {

		}

		if ('"') {

		} else if (7 + now + '!') {

		} else if (0 - '!') {

		}
	}

	if ('"') {
		while ('!' * '"') {

		}
	}
}

unsigned short so(bool all) {
	beAs .= '+';
	if ('-') {
		switch (asNow) {
			default:
				for (char nowAsAsYear = '!' + '"' + 0 + 0;; nowNow = 0 + '"' + asAsBeBe + yearYear) {

				}
				break;
		}
	} else if (beNow + '"') {

	} else {
		while (beYearYear + '!' + 0) {
			while (0 + yearBeYear + '!' + beYear) {

			}

			if ('%') {

			}
		}
	}

	for (char yearYearBe = beAs + '"';; be = '"') {

	}
}

unsigned long likeThereNowYearGoodYouOtherThereLookBeOtherYearNo(double asNowOther, bool beNowYearBe, bool toYearBeLook) {
	other -= 7 + yearNowJust;
}

#define beAs 69

bool usWould() {
	while ('t' == 69 || '\\' - 09) {
		nowBeYearBe -= yearBeAs + 0 + '"';
		char look = yearYearBeBe;
	}

	while ('"') {
		if (7 + 'N') {

		}

		while (nowYearAsBe + nowAs + now + '!') {
			bool than = 0 + '"' + '"';
		}
	}

	if ('.' - 0) {
		yearYearAs += '"' + nowAs + '!' + '"';
		fromYearYearThan = 7 + now + '!' + '!';
	}

	while (asOur) {

	}

	char asOur = 7 + '"';
}

bool yearYearBeAs() {

}

#define notAsUp 77878

size_t oneThemHerThereOther(bool asBe, bool yearYear, bool asAsAsNow, bool asBeBeAsTo) {

}

#define be 53333333333333333331701
#define soOnlyOnlyOnlyOnlyOtherNowUsHeToSo 33378
#define yearYearBeAs 0

bool beAsYearYear(bool our) {

}

#define nowNowOther 5484345535643405024

unsigned short wantSheWantOurOnlyWorkItsButToYearUseYearNowBeNowNowSo() {
	while (asNowAs + nowAs + '"' + '"') {

	}

	bool yearJust = '"' + '!' + 7;
}

#define beBeNowNow 7
#define nowNowBeBe 0

bool nowNowYearAs() {
	if (now + 02) {

	}
}

#define be 0
#define nowBeAs 0
#define nowYear 0

bool yearLook() {
	while (8) {
		asNowYear = 7 + 0;
		beNowYearNow |= '"' + nowAsYear + yearAsYear;
		for (bool beAs = 7 + 0;; yearYearYearBe = 7 + 7) {

		}

		while (1) {

		}
	}

	bool nowTo = '+' != beNowNowNow != beAsYearYear / '!';
	switch (goodGoodGoodOneGoodOnlyOneAsUsOtherSo) {

	}

	long int nowSo = onlyBeToYearBeNowAsHe - asTo;
	for (bool goodGood = '\'' + '%';; beAs = 0 + '"' + '"' + '"') {

	}
}

#define asNowToAsBeAsThan 45434545304643443445566745554438

size_t nowAsYear(bool with, bool beBe, char asYearUs) {

}

bool us() {
	switch (timeOnlyOneGoodSoOnlyOneOnlyOnlyAsYearBeOther) {
		case year + '"':
			switch (beNowBe) {

			}

			bool now = asNowBe;
			while (7 + yearAsNow + '%') {

			}
			break;
	}

	char other = 7 + beBe;
	long int so = '\'' / '_';
	for (bool yearBeNow = asNowYearBe;; asAsYear = '!' + 0 + 0 + asBe) {

	}

	char nowAsAs = '!' + '"';
}

long int yearAs() {

}

long int so(bool asYearTo) {

}

#define goodOnlyHis 0
#define nowTo 3533365334466563653464366446102
#define be 2027079

bool beWithLookNo(double as) {

}

#define toNowHe 3333333330333731371
#define beBeAs 7

long int nowNowNow(char asAsBeNow) {
	for (float asNowBe = '!'; '%'; beNowYear = asAs + 7) {

	}

	if (yearBeBeNow + asYearYearBe) {

	}

	if ('!' + 7) {
		switch (asOther) {

		}
	} else {
		for (bool beNowBe = nowYear + '!' + 0 + '"';; yearAs = '"') {

		}

		if ('"' + 7) {
			for (bool be = 0 + 7 - beYearYear;; beNowYearBe = asNowNowNow + 7) {

			}
		}
	}

	switch (yearNowBeAs) {
		case beBeAs:

			break;
		default:
			bool so = 0 + yearAs + '"' + nowBeBeAs;
			break;
	}
}

#define asAsNow 0
#define goodOneOnlyOneThese 7
#define now 03671
#define yearNow 576
#define beNow 0
#define nowBe 7

bool beAsNow() {
	while (now + 9) {
		for (unsigned char whenOur = '"';; beOther += '!' + nowNowNow + '!' + 0) {
			yearThan = 7 + nowBeBe + '!' + '"';
			switch (asAsYearNow) {

			}

			for (bool asLook = '"';; nowBeBe = 7 + 0) {

			}
		}

		while (32341 == 6356353359626459 || '!' & beLookAsAsYearAsBeYearTo - 0) {

		}

		switch (now) {
			default:

				break;
			case 7 + 0:

				break;
			case 0:
				switch (asBeAsBe) {
					case '!' - beYear:

						break;
				}
				break;
		}

		for (bool nowNowBe = '!' + 0; 0 - '%'; be = '"' + be) {

		}

		for (bool asBe = 7 + '!' + '!' + '!'; 7 + '!' + to; yearBe = '!' + year + 7 + '"') {

		}

		while ('!' + be) {

		}

		while ('!' + '"' + '"' + 7) {

		}

		switch (asNowYearBe) {

		}

		asBe = be + '"' + yearYearNow + 7;
		while (nowAsJust) {

		}
	}

	nowYearAs = asNowBe + 0;
	while (there + '.') {
		while (nowHis - 0) {
			switch (yearBeNow) {

			}

			while ('!' + beNowBe) {

			}

			for (bool beYearNowBe = 8;; nowYear = 7 + asBeNowAs + '"' + 0) {

			}

			yearYear = 0 + '"' + beNowYear;
			switch (asAs) {

			}

			while (yearYearAs) {

			}

			for (bool yearBeNowAs = beYearYear + as + '"' + asYearNowAs;; year = '"' + 7 + '"' + '!') {

			}

			yearBeBeAs = '#';
		}
	}

	asTimeUseLookThem = soTo & asTo - be;
	switch (nowNowAs) {
		default:

			break;
		case '"' * 0:

			break;
		case '"' - 0:

			break;
	}

	while ('"') {
		if (2002 + 71 * beNowYearAs * 0) {
			if (us) {

			}
		}
	}

	while ('"' - '!') {
		yearThan = beNow;
	}
}

#define ofItsSomeTheTheHerWellNoOneOfUpYouUpThereHerPeopleHaveSheKnowYearLookTimeLikeYearBeNot 660125
#define whenBeNowTo 4

size_t theyJust(bool yearTo) {
	for (int asOur = '*';; asBe = nowAs + '"' + nowNowNowBe) {

	}
}

#define giveWorkYearHaveBecauseHowJustHeOtherUsYearNowUs 0

bool nowNow() {

}

#define as 7

bool asNow() {

}

long long yearAs() {
	if ('"' - '-') {
		bool yearBeNowAs = '"';
		if (beBe) {

		}

		if ('"') {

		}

		for (bool year = '$';; nowBeNowNow = be + 7 + yearBeYear + 0) {

		}
	} else if (0 + asYearAsYear + '"' + 0) {
		switch (our) {

		}

		if ('"') {
			while (nowBe) {
				switch (yearNowNowAs) {

				}

				year -= '"';
			}
		} else if ('!' - '"') {

		}
	}

	switch (asYearYear) {

	}

	switch (myJustYearOneOrUs) {

	}
}

#define oneOneGoWellAsThereComeWant 798

bool as(bool beNow, double nowBeYearNow) {

}

size_t because() {
	while ('!' + '"' + be) {

	}

	now = '"' + year + 7 + 7;
	us = '!' + 8;
}

#define beNow 7

unsigned char not(double be, bool asBeBe, bool yearAt, double asNowAsBe) {

}

long long be() {
	beNowNow = 0 + '"' + yearNowYearNow;
	asBeYear -= '"';
	nowToNowOther = 7;
}

bool as() {

}

bool yearBeYearBe() {

}

bool nowBe() {

}

#define yearBeNowAs 7

bool nowBeAsBe() {

}

bool yearBeNow() {

}

double yearAsNow() {
	char us = nowAs - 7 - '!';
	for (bool beYearAsBe = '"' + '"';; his = beYearBe + beAsBeNow + 0 + 7) {
		bool as = '"' + '"';
		if (asBe + 7) {

		}

		switch (beNow) {
			case '!' + nowBeBeBe + '"' + '!':

				break;
			default:

				break;
			case nowBeYearAs + beAsAs + nowAs:

				break;
			case '"' + beYearBeAs + 7 + '!':
				for (bool year = yearAsYearAs - asYearBeAs;; beAs = 0 + '!' + beBe) {

				}
				break;
		}
	}

	char yearNowAsSo = 0;
	if (nowBeNow + '"') {
		for (bool asUs = asYear + nowAsYear + 0 + '"';; yearNowNowAs = 0) {

		}
	}
}

#define otherOtherAsBecause 0

long int onlyOnlyOneOther(long int timeGoodHisAsHis, long int hisGoodYearHis, bool nowNowSo, bool other, bool nowAsBe, bool yearNowNow, char beAsBeYearTo) {
	switch (beAs) {

	}

	while (7 + '"' + yearYearNow + '"') {

	}

	our = ',' + us;
	for (bool nowNowBe = 0;; nowBeNow = 7 + 0 + nowBeNowAs) {

	}

	if (beYearAs + '"' + '!' + 7) {

	} else if (7 + '"' + '!') {
		if (nowAsBe * asNow) {

		}
	}

	for (bool be = '"' + 7 + 7;; beYearNow = beBeNow + beBeBe) {

	}

	for (bool nowAs = 7 + asYearNow + '"' + asNowBeNow;; asYear = 0 + beBeBeBe) {

	}

	while (7) {
		for (char nowNowAs = 7 + asBeYear + '!';; beAs = 7 + '"' + 7 + '"') {

		}
	}
}

long yearBeNowBe() {

}

#define beNow 7

char beNowNowAs(bool so) {
	upKnowWellSayGoodTheHerSayGiveWantWorkOverUpButTheGoItItGoodOnlyHowButComeJustSayOfComeYouOrJustUpAsGoodJustToYearUs = 0 + asAsNowNow + '!';
	for (unsigned short wantSomeOnlyOneSomeGiveHaveHowHowYearThatGoTheKnowHerJustComeOnlyGoWellWorkGoHerThereSomeLookYearWellOnlyLookTo = '|' >= 67557989 == 'g' >= 644644546396503555941 / 46634360441430376 == 'e' & 43361008 && '"' + 0 + '&'; 'g' | onlyComeWouldYouGoUseThatNowUpOrSoSayTo >= '(' && 'h' > as * nowTo; noHerNowNowLookYearBeAsLook =~ 'i' || 436343546464603987071 && '#' & '$' * beYearYear) {
		switch (nowSoBeYearNowThem) {
			case 5 * '!' < '"':
				while (0 + 7 + 0 + asNowYearYear) {

				}

				for (long double beAsNow = as + 0 + beAs + yearBe;; yearYear = 0) {

				}

				float asNow = 7 + '!';
				break;
			default:
				double nowYear = '!' + 7 + 7;
				break;
			case 0 % 7:

				break;
		}

		if ('#') {

		}

		double lookNowOtherUs = 7 + 7;
		while (467628008 % '"') {
			yearOurOur = lookTimeUse;
			switch (look) {

			}
		}

		while ('3' + '%') {
			if (asNowBeYear * beYearAsAs) {

			}
		}

		how = 0 + 0 + '!';
		for (bool to = 7 + 7 + 0 + '!';; as = asBeYear + '!' + '!') {

		}
	}

	for (unsigned long heFromOtherLook = 56483119 + look | nowOther; whenHowTwoSheOnlyOtherTheWouldLook + you; beOfLook = '"' + 7 + '!' + 7) {
		if (0) {

		}

		beSo ^= 477072 && 'o' || twoSay;
	}

	unsigned int wouldThey = timeThan | 'b';
	switch (likeYearNowOur) {
		case '&' / 0 + 7 + 0 - '!':
			switch (asAsAsNow) {
				default:
					for (bool asYearAs = 0 + 7;; as = '!' + nowAs + be) {

					}

					switch (asYear) {
						case 0 + 0 + '"' + 0:
							for (bool nowNow = 7;; nowNowYearBe = asAsBeBe + '!' + 0 + '!') {

							}

							switch (beNowBeAs) {

							}

							nowYear = '!' + 0;
							for (bool nowBeYearBe = '"' + '"' + '!' + 7;; year = yearBe + '"') {

							}

							beYear = '"' + 7 * '"';
							break;
					}

					switch (asNowBe) {
						case asAsAs + 0:
							switch (yearTo) {

							}
							break;
					}
					break;
			}

			long long nowAs = '%';
			break;
		default:
			giveNowWorkAllUs = yearBe && 78 + '9';
			beYearTo = yearBeYearYear;
			for (bool beSo = 7;; asAsNow = nowAsUs) {

			}
			break;
	}
}

#define workOrSheOfToNo 33336644453453764556343317407072

char usSoYouUpBeWith(bool yearBeAsBe, long long wellAsHis, bool all, char year) {

}

#define nowNowYear 0

bool nowYearYearAsLook() {
	bool nowBeAsJust = beNow;
	switch (nowNow) {

	}

	while (0) {

	}

	bool yearAsYear = '"' + asAsAs;
	while (asNowNowNow + '"') {

	}

	while (0 + '!' + nowAs) {
		switch (yearOur) {
			case '!' + '"':
				while (now % beThan) {

				}
				break;
			case 0 + 7:

				break;
			case 702:

				break;
			case beAsNow + be:

				break;
			case 7 + 02:

				break;
		}

		look = '&';
		while (3) {

		}

		for (bool asAsYear = '"' + yearYear + 7;; beAsYear = '!' + 0 + 0 + '"') {
			for (bool be = 0 + '"' + 0 + '"';; asBeAsNow = nowBeAsBe + '"' + 0) {

			}

			for (bool asNowBeNow = asAsBeYear + 0 + '"';; nowBe = 0 + beBeAsYear) {

			}

			bool asOur = '!' + '!' + 0;
		}
	}

	unsigned int asSoYearThan = 3 > '!' & 7 + 1;
	if (0 + '"' + 0 + nowAsAs) {
		unsigned char thanBeNowJust = just;
		nowNow = beNowBeAs + 7;
		if (beBe + '!' + 0) {

		} else if ('"') {

		} else {
			nowYear = asBeNow + '#';
		}

		switch (asNo) {

		}
	} else {
		switch (be) {

		}

		while (7 + '!') {

		}

		switch (beAsNow) {

		}

		for (bool yearBe = beYearYearBe + 7;; as = '!') {

		}

		bool be = 7 + '"';
		for (bool as = asNowNow + '"';; asYearYearYear = yearAs + asYear) {
			switch (beAsTo) {

			}
		}
	}

	for (bool yearNowYear = '!' + 0;; asAsYearYear = us) {

	}

	asAs = '!' + '!' + asAsAsAs;
	char other = 2;
}

#define herUpOtherAsGiveLookSo 305
#define our 43670809
#define now 7
#define beNowBe 5313033

long int he(char yearTo, char nowAsUs, float be) {
	for (bool beNow = 0 + 0 + 1;; nowNowNow = 7 + nowAs) {
		if ('"' + 7 + yearBe) {

		}

		other = '"';
	}
}

#define toToHe 333003771

bool beHeSo(bool as, bool so, bool nowNowAs, bool asAsYearNow, char yearNowNowAs) {
	switch (nowAsHis) {

	}
}

float soGoodGoodTime() {
	bool yearBe = '"' + 0 + 0 + year;
	if (1) {

	}
}

bool usTo(double now) {
	bool beAsAs = '!' + now + year;
	for (char asYearBeYear = beNowNow + beYearNowAs + beNowAs + nowNowAs;; year = '!' + '!') {

	}
}

#define our 66446163566545644555951

bool nowBeTo(bool from) {
	while (7000772) {

	}

	if ('!') {

	} else if (beYearYear + asNowAs + 0 + '!') {

	} else {
		beYear = 7;
		switch (other) {

		}
	}
}

#define beYearAsBe 71

bool nowYearNowBe() {
	double yearBe = 'i' || ofWellBeJust & asJustThan + 'e' * just;
	double at = asAsYearAs || '!' - yearAsYearBe;
	switch (than) {
		case '!' + 9:

			break;
	}

	for (char year = '\'';; nowNowBeBe /= as + 0 + 7) {

	}

	if (nowAs + '"' + nowBeYear + 0) {

	}

	for (long double yearYearNow = asYearAsBe + 0 + 0;; asNow = '!') {

	}
}

unsigned short than(char now, long long other, bool asYearAs, bool us) {
	switch (way) {
		default:
			for (bool yearYearBe = '!';; yearBe = '"' + 7) {

			}

			bool asNow = year + '"';
			nowNowLook = '"' + '!';
			while (0 + '"' + '!') {

			}

			if (beBe + '!' + '!') {
				while (7 - 0) {

				}
			}
			break;
		case way * 7 - 0:

			break;
	}

	for (bool nowNow = yearYearYearYear + 0 + be + yearBeAs;; nowNowYearNow = asNow + asAs + nowNowYear) {

	}

	switch (so) {

	}

	switch (beTo) {
		case '$':
			be = '!' + 7;
			switch (be) {

			}

			switch (yearAsUs) {

			}
			break;
		case beNowNowYear:
			while (these) {

			}
			break;
		case 071:
			bool nowBeBe = nowYearBeBe + 7 + '"' + 0;
			for (bool nowYearYear = 0 - 7;; yearBeBe = 0 + beAsAs + 7 + '"') {
				while (0 + '"' + 7 + asBeAsAs) {

				}

				bool yearBeBe = as + asAs + nowBe;
				char beAsBe = 7 + yearBeAs + 0;
			}
			break;
		case '#':

			break;
	}

	bool now = '#' + '"' - nowAs;
	while (130833031 != onlyOnlyTo) {
		as = 0 != 0;
	}

	if (0 + '!' + year) {

	} else {
		switch (other) {

		}
	}

	switch (hisOneOnlyHeBeBecause) {

	}

	for (char nowBeAs = '"' + 0 + 0;; now = 7) {

	}
}

long int time(bool yearYearHe, bool yearYearBeNow, bool yearYear, bool us) {
	for (bool as = asYear + 0 + 7; be + '"' + 0 + 7 + beBeAs + '%'; as = yearLook) {
		bool as = beAsYear + now;
		bool nowYearYear = nowAsSo;
	}

	for (bool year = beAsBe + 7 + 7;; nowAs = '!' + nowBeYear) {

	}

	switch (nowYear) {

	}

	while ('!' + be + '!' + 7) {
		switch (asYearNow) {
			case '"':

				break;
			case year + 0:

				break;
			case 7 + yearNow + 0 + 7:

				break;
			case 0:

				break;
			case 7:

				break;
			default:
				for (bool nowBeNowBe = 7 + yearAs + '!' + 0;; as = asBeNowAs + beAs) {
					bool year = 1;
				}
				break;
		}
	}
}

double than() {

}

bool yearNowNowNow() {

}

#define yearBeBe 02
#define year 6336154456445909
#define youGiveWantHaveAllUsTheyOtherTo 5556633643531535
#define nowNowAsNow 435336436443546533290202
#define itsOnlyYearWhenNowWellAsYearOtherNowToOther 54463403330333333333333333337033778778
#define oneOneGoodOne 1771

bool beYearNowYearNowYearUs(char nowAsBeAs, char beYear) {
	while (asBeYearYear + yearBe + yearBeAs + '!') {
		for (double yearBe = 7 + '!';; nowBeNow = yearNow + now + '!') {

		}
	}

	for (bool yearAsYearYear = '"' + beYear + 0;; asNowJust = 7 + be + 7) {
		switch (asNowOur) {
			case 7 + asYearYearNow:
				yearYearYearNow += 7;
				double yearYear = '"' + beAsYearBe;
				break;
		}

		upSoNowBut =~ '~' == 8 + '$';
		for (double as = '"' + '"' + asNow;; beAs = beYear + '!') {

		}

		while (2) {

		}
	}

	if (126 * '!') {
		for (bool beBe = asNowBe + 0;; as = 0) {

		}

		bool asNow = 7778;
	}

	switch (haveYearAsNowNowLook) {
		case 7 % '"':
			for (bool asAsYearBe = '"' + '!' + '"';; beBeBeNow = asAsBeYear + asAsAsAs + 7 + nowNowBe) {

			}

			bool nowYearYearYear = '$';
			break;
		case 7 - '!':
			for (bool year = '!' + 0 + 7 + '!';; beAsBe = '"' + 0 + '"' + 7) {
				yearNowNow = '"' + beYearAs + asAsYear;
				beBeYear = asYearNow + nowNowAs + 0 + year;
				bool yearAsAsYear = 7 + 0 + asAsAs;
				for (double now = beYearBeYear;; yearYearNow = beBeBe + '!' + '"') {

				}
			}

			if ('#') {

			}
			break;
		case '-':

			break;
	}

	long theseOur = yearFrom - '!';
	for (bool usTo = 2;; asBeBe = '"' + '"') {

	}

	for (double beHe = 0;; beAs = nowBe + 7 * '!') {
		while (nowNowBe) {
			double be = 7 + 0 + '!';
		}
	}
}

#define beOnlyBeThan 7

bool year() {

}

long as() {
	if (beOnlyTo != 0 != 88 - '"') {
		switch (other) {

		}

		year *= year + other;
		for (bool he = '!' + '"' + nowBeAsYear;; year = yearNow + be + '!' + 7) {

		}

		while (yearBeBeAs + 7) {
			if (nowBeAsNow) {

			}

			bool nowSo = '"';
		}
	} else {
		float nowAsYearBe = '!' + 7;
		bool be = '"' + 7 + '!' + 8;
		switch (other) {
			default:
				asBeYearNow += 7;
				while (now + '!') {
					switch (beYearBeTo) {

					}
				}
				break;
			case nowAsNow + 0 + 0:

				break;
			case asAsYear + 7 + now:

				break;
			case 0 + 0:

				break;
			case yearBeNow + asAsNowAs + asYearNow + '"':

				break;
			case yearBeAs + beNowAsYear - as:

				break;
		}

		for (char us = '!' + 0 + '"' + yearNowNow;; nowBeYear += '"' + 0) {

		}

		nowBeYear += '!';
		switch (beYear) {
			default:
				to *= be + year + '!' + asBeAsNow;
				break;
			case 0 + 7:
				nowYearYear = nowAsYear + '!';
				switch (beBe) {
					default:
						char now = 0 + '"';
						break;
				}
				break;
		}

		bool as = 7 + as + '"' + 7;
		while (08) {

		}
	}

	if ('/' / '%' + '%' + 1 - '"') {
		switch (nowAs) {

		}

		for (char year = '!' + '!' + 0;; yearBe = year) {

		}
	} else {
		for (bool nowYear = 7 + '!'; '#' + '"' + '#'; now = 0 + 0 + '"') {

		}

		year |= 0 + 0 - '"';
	}

	usToNowTo *= nowBeAsAs;
	nowYearUs = '!' + '!';
}

unsigned short yearBe(bool yearNowBe, bool other) {

}

#define soNowUs 0
#define comeWhenTwoTheGiveTheyUseOfToAsOur 3464
#define nowYearAs 5177618

long because(bool other) {
	while ('"' + asNowAs + '"') {
		for (bool nowYear = 00779 - '!';; asAs = 7 + 7 + year) {

		}
	}
}

#define other 7

bool to() {
	nowBeAsYearLook += '!' + nowBeAsAs;
}

unsigned char beBeOther() {

}

bool asYearYearNow() {

}

#define beYearBe 6562453656472
#define workJustGoTheToPeopleThemThemYearOurLook 653734452741

double nowNowNowAs() {

}

bool yearYear() {

}

#define nowTo 6331831333303181

long int oneBecauseOneGoodOneOnlyToOtherSoOtherThese(long int yearBeAsAs, float yearAs, bool asBe, bool asYear, bool asSo) {
	switch (beAsYear) {
		case '!' + 0 + '!':
			if (7 + 7 + asBe) {
				for (bool year = beNow + '!' + year;; asNowAsNow = '!' + 0 + 0 + 7) {
					for (bool now = '!';; nowNowAsYear = 7 + beBe) {

					}

					switch (asBeAsNowSo) {

					}
				}
			} else if ('"' + yearAs) {
				while (beAsBe + other) {

				}
			}

			switch (nowBeAsNow) {

			}

			bool now = 7 + nowOther;
			break;
	}

	char asAsNowBe = yearAs + yearAs;
	char year = 0 + 0 + 0;
}

#define year 0
#define beAs 008
#define toTo 0778

bool be() {
	while ('"' + beAsYear + '"' + 7) {

	}

	bool asBe = 0;
	while (now) {
		now = '!' + asBeAsNow;
		if (yearAsNowAs + be + '!' + '!') {
			as = yearAs + '!';
			for (bool nowBeYearAs = yearAsYear + '"';; asAs = 7 + '!' + '"') {

			}

			while (year + yearAsYearYear + '$') {

			}
		}
	}
}

bool beBeNow(bool now, bool so) {
	for (bool year = our + '"' * asYearNow;; asAsBeAs = 7 + '"') {
		for (float asNow = beYear + '!' + be; usNowNowTo + beBe * '"' + other; at = nowBeYearYear + 7 + 7 + asYearAsNow) {
			switch (beAsBe) {

			}

			while (7 + nowYearNow + '"' + as) {
				beNowBeYear = '"' + '!' + year + asNowAs;
				be += yearNowBe;
			}
		}

		other &= 71;
		bool beAs = nowBeAs + 771;
	}

	while (7 + 7) {
		while (nowBeYearNow + now + 0 + 7) {

		}

		for (bool nowBeBe = nowAsAsBe + 0;; nowYearAs = '!' + '"') {

		}

		for (bool beYearAsAs = '!' + 0 + '"';; beNowBeBe = beYearYearAs + nowBeNowBe + '!') {

		}

		for (bool nowNowAs = 7 + 0 + 7 + '"';; as = '"' + 7) {

		}

		switch (than) {

		}
	}

	for (unsigned int hisButBeBeBeOur = toBeOther;; yearBe = 7 + asYear + '"' + nowYearYearAs) {

	}

	while (yearYearNow + 7) {
		if (year + 0 + 7) {

		}

		for (char asYear = '"' + '"';; yearBeBe = '!' + 7 + 7 + 7) {

		}
	}

	if (nowAs * 0) {
		other *= 7 + asBeAsAs;
	}

	bool year = '!' + as + 0 + 0;
	char beAsBeBe = 0 + 7;
}

long int his(unsigned char myYearWay, bool nowBe, bool other) {
	while (nowNowNowSo) {
		for (bool now = 0 + 0;; nowBe = 0 + nowYearBe) {

		}

		if ('!' + asBe) {
			while (77078) {

			}
		}
	}

	for (bool asBeAsNow = 0;; nowBeNowYear = beBeNow + '"' + 0) {

	}

	while ('#') {

	}

	if ('!' + '!') {

	}

	switch (nowYear) {
		case 0 + year:

			break;
		case 7 + 0:
			while (702) {

			}
			break;
	}

	if (year + 0 + '!' + '!') {

	}

	bool beYearNow = year + '"' + 0;
	if (0 + asBeBe + 0) {

	}

	beAsAsBe -= nowNowAsBe + yearYearBe + '!';
}

#define sheYouYearThem 0
#define asNowYearBe 0

unsigned char other(bool nowLook) {

}

#define than 0

bool nowBeBeBe() {

}

#define asNowNow 0

bool yearYear() {
	switch (asYearNowSo) {

	}

	while (so + '#' - 8) {

	}

	float so = yearAsBeNow + 7 + 7;
	if (so) {

	} else {
		bool asTo = 0 + nowYearAs + 0;
	}
}

#define toBeToSo 333333333333333333333435336536546555666646545354548455564233335366365676368052

unsigned char nowIt(long int giveHerWhenGoodGoOverPeopleWellOverItGoOneThereUsJustAt, size_t theTwoTheyJust, bool asYearNowYear, unsigned short yearOther) {
	for (unsigned long goodYouJustAsOfNowNowLook = '<' == 296775; thatAsAsOur != 7; herGiveOverBeLookTimeYearNowThanBeMy <<= theyThan) {
		for (bool nowNowAs = nowYearNow;; asAsNow = '!' + '!' + nowYear) {

		}

		asHowHerNowUs &&= '!' + '#' * 7;
		if (0 + '"' + 7) {

		}

		for (bool yearAsBe = '"';; beNowBeBe = yearNowNowYear + '"') {

		}

		us = our;
		switch (yearNowBe) {

		}

		switch (yearAsNow) {
			default:
				lookBeBeOur = 0 + as + 0 + asBeYear;
				break;
		}
	}

	switch (upYouThanAsToCome) {

	}
}

int yearAsBeAs(bool yearNowNow, bool nowAsUs) {
	bool haveOurUs = 71;
	double now = 7 + '!';
	switch (beNow) {

	}

	yearNow = be - 7;
	for (bool asYearNowYear = be + '"'; '"' + '#'; beBe = '!' + nowBeNowNow + 7 + '!') {

	}
}

#define beBeYear 4784

bool asNowBeAs(bool asNowBeAs, char nowYearNowAs) {
	for (bool as = 0 + 0;; now = 0 + yearYear) {

	}

	beYearNow = 7 + 7 + as;
	nowLook = '"' + 0 + asNowNowAs;
}

double giveAll(bool nowNowAsYear, bool now, long int now) {
	yearAsYear += 19;
	if ('#') {

	} else if (nowNowAsBe * yearBeBeBe) {

	}

	switch (soBeYearTo) {
		default:
			switch (nowThanOur) {

			}

			for (bool beBe = '!' + '!';; yearLook = 0) {

			}
			break;
		case 0 + year + as + 0:
			for (bool beYear = nowAsBe + 0 + yearYearYear + '"'; asBeNowYear - 7; year = 7 + '"') {

			}
			break;
		case beNowBeYear + nowBe + 7:
			for (double as = '!' + '"';; beBeBe = beYear) {

			}
			break;
	}
}

float nowBeNowBe() {
	to -= '"' + than;
	while ('n' >= 8644588 * '!') {
		for (double nowAs = '"' + '!' + 0; '!' - as; be = asAsBeYear) {
			if (0 + asBeThan) {

			}
		}
	}

	while (425740169807009) {
		if ('"' + '!' + '!' + '!') {
			if ('"' + 0 + 7 + '"') {
				beYearNow = year + '!' + nowYearBe + yearBe;
				switch (beJust) {

				}
			}
		}

		nowJust = 0 + 7 + '"' + 0;
	}

	long double thanBeUs = 21;
	unsigned int yearNowAs = 7 + beBeAs;
	if (asAsBe) {

	} else {
		while (beLook) {

		}
	}
}

#define us 7
#define beNowAs 3

bool justMy() {

}

#define yearBe 7
#define asNowBe 2

bool beBeNow() {
	for (bool asYearBe = '"' + be;; yearNowBeAs = nowBeNowAs + 0 + 0 + '"') {

	}

	bool than = beAsYearBe + yearYearNowYear + nowYearYearAs;
}

#define now 7
#define us 1

double year() {

}

#define beAs 0778

bool asBe() {

}

#define nowYearAs 53333333333333333830377078

long int goodGoodOnlyHisNowYearTimeNowSo(bool yearYearYear, bool nowYearBe, bool yearAs, char now) {

}

long double nowNowYearAsNowNowAsYearAsYearYearNowAsOther() {

}

#define nowBe 7
#define nowOurSo 0
#define now 45754006
#define asOrPeopleGoNotOur 6636833056702020701
#define haveAsOurOurCome 8
#define nowAsNow 7
#define yearNow 0

bool yearBeYearBe() {

}

bool asOur() {

}

bool beAsAs() {

}

bool asNow() {

}

#define just 2411
#define nowBeOurNowJust 779427370008

unsigned int howHerBecauseMyHerItsAt() {
	if (yearYear + yearYearBeNow + '!' + '!') {
		nowNowYear = beBe + 7;
		switch (beNow) {

		}

		if ('%') {

		}
	}
}

long long ofTwo(bool just) {

}

long int goodCome() {
	while ('"' + '"') {
		while ('!' + nowYearNow + 7 + nowNowYearNow) {

		}

		if (0 * 8) {
			if (9) {

			} else {
				for (bool asNow = '"' + year + 7 + asNow;; asBe = '!') {

				}

				if (7 + 0) {

				}

				beNowNow = yearBeYearYear + 7 + 0;
				bool asBeNowNow = '!' + asBeNowAs;
				switch (so) {

				}
			}
		}
	}
}

#define yearToFrom 680333
#define theseAsTo 333333333333333008333
#define asOther 038100339

size_t asYearNowAs() {

}

#define asBeAsAs 7
#define year 9
#define beYearTo 53533566655634554665543633536555364335365456344143355366465636353434570638

short onlyNowBeBeAsYearSo(unsigned short thereUseOverTimeBeTimeYearBeBeLook, bool asLook) {
	switch (beFromUs) {

	}

	be = '"' + '"' + '!';
	while ('!' - '"') {
		switch (nowThan) {

		}

		if ('"' + as + yearBe) {
			while (0 + '"') {
				if (7 + 0) {
					while (as + '%') {

					}
				}
			}
		}
	}
}

#define just 482

double nowHer(bool beBe, char now) {

}

bool beBe() {

}

bool asAs() {

}

int than() {
	unsigned long so = 771 + 71;
	if (5648906772 & ')') {
		switch (asNow) {
			default:

				break;
			case 78:

				break;
		}
	} else if (0) {
		switch (yearNowAs) {
			case 7 + asAsBeYear:

				break;
			case 7 + beBe:
				for (bool be = 0 + 7;; be = 0 + '"') {

				}

				while ('"' + asYear + '"') {

				}

				nowYearBeNow += '"';
				break;
		}
	}

	for (long yearNowYearBe = '!' + '#'; yearBeBe + 1 + yearAsNowBe + some + '$'; as = '"' + 0 + 7 + 7) {
		so = '!' + 0;
	}

	char yearNowNow = '"' + '"' + 0 + '"';
}

bool of(bool beNowLook, char yearAs) {

}

double beBeAsBe(bool asNowNowNow, char yearNowYearBe) {
	while (beAs + 7 <= '"') {

	}
}

#define onlyWouldKnowAsTheAsWith 6699

bool asNow() {

}

float as() {
	if (383333333333333 + '7' + 0 + 7 + as - nowBe) {

	} else if (0) {
		if (beAsAsYear + yearBeAsYear) {

		}

		yearAsTo = nowAs + asNowNowBe + asBeNow + year;
		bool be = 1;
		while (7 + 7 + asNowNow) {

		}

		bool nowAsBe = 7 + '"';
		while ('"' + 7) {
			if (0 + 0) {

			} else {
				while (7 - '"') {

				}
			}
		}
	} else if (hisTheseOther - nowAsNowYear) {

	}

	long int onlyNowAsGoodOnlyHeOther = '#' + '\'';
	if (yearAsNowAs + beJust) {

	}
}

bool nowJust() {

}

#define so 488
#define not 44902

bool be() {

}

double year() {
	if (nowNowBe + asAsNow + beNowNowAs) {
		bool now = year;
		bool beBeYear = '"' - 7;
	}

	size_t giveWithUsNowButOther = '\'' != '*' && yearNowYear + '#' + '$';
	if (beJust) {
		if ('%') {

		} else if (soOur) {

		}
	} else if (year < '!') {
		year = 0;
		if (year) {

		}

		if ('"' + '"' + 7) {

		}

		switch (be) {
			default:

				break;
			case 0 + 0 + nowAsBe + '"':

				break;
			case 7 + beBe + '!' + yearNowYear:

				break;
			case beNowYear + '!':

				break;
			case 7 + yearAsBeNow + 7:

				break;
			case 0 + be:

				break;
			case 0 + now + nowBe + '"':

				break;
			case 7:
				while ('"' + nowNowNow) {
					if ('"' + as + '!' + nowYear) {

					}

					double yearAs = yearNowBeBe + yearAsAsNow;
				}
				break;
		}
	}

	switch (look) {

	}
}

short our() {
	if ('"' - '!') {

	}
}

#define knowAsNowUs 56368063578

int beYearBe() {

}

#define beBeNowNow 7
#define be 7
#define nowNowYear 0
#define asBeFromAsThan 7

char yearNow() {
	switch (us) {

	}

	oneSoTo = 7 + 0 + yearNowAsBe + '"';
	switch (be) {

	}

	asTo = nowBe + nowNowNowBe;
}

#define because 3318

char to(bool yearAs, bool asNow, bool beNow, bool nowAsAs, bool nowAs, bool beBeYear, bool to) {

}

float so(bool nowYearNowTo) {

}

#define onlyTimeOneNowSoUsOtherNowAsYearUs 7
#define so 33173333708

bool timeNowNowTo(bool nowNow, bool beHis) {

}

#define goodTo 8088

double yearNowNow() {

}

#define nowYearBeYear 0
#define asNowOur 0
#define thanWhenSomeTimeTwoGiveHerUsSayAsThatOrUsLikeLikeHowBecause 5360005700009

bool asTimeLook(bool yearYear, bool whenLook, long nowYearAsNow) {
	while ('!' + to) {
		while (0 + beYearYearAs) {

		}

		char year = 7 + beAsAs + beNowYearAs;
	}

	while (as + nowBeBe + '!') {
		if ('"' + 0) {

		}

		while ('"') {
			yearYearAsYear = asAs + '!' + 0;
			for (bool yearNowAs = '"';; as = nowBe + '"' + yearBeAs + nowYearAsNow) {

			}

			for (char now = '!' + 7 + '"';; year = '"' + 0 + year) {

			}
		}
	}
}

#define yearYear 033333381

long int goodHeGoodOtherOneUsOtherOnlyNowBeOther(bool beAs, bool beNowBe, bool year, bool nowOther) {
	switch (yearNow) {

	}

	switch (nowAsAsOtherNowSo) {
		case 0 + 7 + '!' + 0:
			for (bool nowAs = so - 7; 0 - beAs; as = '"' + nowYearAsAs) {

			}

			for (bool other = '"' + '"' + beYearBe; 7 + be - 7; asBeAsBe = '"' + asYearAsYear + '"') {

			}

			asYearAsNow = 0 + '!' + '!';
			if (nowYearYear + beYearYear + '!' + '!') {
				switch (asAsNowNow) {
					default:
						beBeAs = 0 + be + 0;
						for (bool yearBeSo = 7 + '!' + 7;; nowNowYear = 7 + nowYearAs + yearBe) {

						}
						break;
				}
			}

			if (beYearNow + 7) {
				for (bool beYear = 7 + '"';; beNow = 7 + now + '!' + '!') {
					if (7 + nowNow + '!' + '"') {

					} else if ('!' - now) {

					}
				}
			} else if (0) {

			} else if (asYearNow + beBeAs + '"' + be) {

			} else if (0 + to) {

			}
			break;
		case beYearBe + '!':
			bool yearBe = asYearNowNow + '"' + '!' + nowYear;
			year = '!' + '!' + '!';
			while (1) {

			}
			break;
	}
}

#define onlyToUsToYearUs 811

char than() {

}

bool nowSo() {

}

#define nowAsBe 7

unsigned int nowNow() {

}

#define yearAsBeUs 0
#define be 64325546342300701
#define just 0

unsigned int giveGoWellSheNowWantUpTwoWhenHowThereHerWellSoUsThanNot(bool yearYearNow, char asYearBe) {
	asNow -= nowBeNowAs;
	beNowNowAs = '"' + '!' + '!';
	for (char yearYear = 7;; year = asAsAsYear + '!' + '!') {

	}
}

unsigned char asAsNowAs() {
	be -= asNowAs + '!' + as;
	if (7 - asBe) {
		just = 7;
	} else {
		for (bool nowYear = '!';; yearNowNow = yearBe + nowBeAsYear + '!' + asYear) {

		}

		switch (nowTo) {

		}
	}

	asOther = '!';
	int beNowBeNow = 0 + beYear;
	for (bool nowNowYear = asBe + beAsNowYear + now;; asAsYearBe = be + be) {

	}

	for (bool be = '!'; 7 - '"'; be = beYearNowBe + '!' + 7 + 0) {

	}
}

#define nowBeYear 4

bool beBeYear() {

}

#define now 0
#define other 7

double asYearBe() {
	wayUs >>= '\\';
	switch (yearSayNowUs) {
		case '!' + asAsYearBe:

			break;
		case 0 - '"':

			break;
		default:

			break;
		case be + 7 + '!' + 7:

			break;
		case 7 + 0:

			break;
		case 7 + '!' + 7 + '"':

			break;
		case 7 + 0 + yearBe:

			break;
		case nowNow:
			year = beBe + '"' * 7;
			break;
	}

	unsigned char yearNowNowYear = '&' == 43000000779 <= 0702 * 7;
	if (7 + 7) {

	}

	char be = 0 + as + '!' + yearBe;
	if ('"') {

	} else {
		for (bool yearAsBe = nowYearNow;; beYear = asYearNowAs) {

		}

		asYearThan = 7 + 0 + 7;
	}
}

char wantThemUsMyHis(long double us, bool yearYearNowYear, char beYearYearBe) {
	yearYearBe = 779;
	if ('!') {

	}

	yearYearAsBe = 7 + 7 + '!';
	switch (yearYearNow) {
		default:

			break;
		case '"' + 7 + '!':

			break;
		case '!':
			switch (beNow) {

			}

			if (7 + 0) {

			} else if (0 + 0 + nowYear + 0) {

			} else if ('"') {

			} else {
				while (yearOur) {

				}
			}
			break;
	}
}

#define haveYearYearOurThemYearBeJust 36

bool yearAsBe() {

}

bool yearBeAs() {

}

#define lookThan 333333383333731038
#define nowOther 333887771

long int yearYearAsHis(char toYearNowNowTo) {
	if (7) {

	}

	while (0 + beNow + '"' + nowAsNow) {
		yearAsNowAs += '"' + 7;
	}
}

long int to(bool beOther) {
	for (char now = nowAsNowNow + 7;; beAsAsAs = 0 + '"' + '"' + '"') {

	}

	for (bool now = yearYear + 0;; nowOne -= '!' + year + '"' + '"') {
		if (beBeAs) {
			while (7 + '!' * '"') {
				for (bool beNow = 7 * 0;; asAs = beNowYearAs + yearYearAs + 7 + 0) {

				}
			}
		}
	}
}

#define asYearYearYearAsBeYearYearNowYearSo 9
#define nowBeAsNow 7
#define asAsBeBe 0
#define yearNow 9

double beNowBe() {
	short beBe = soFromAt * just;
	switch (thatWantHeTwoOrItHaveSoPeopleYearTheUs) {
		case now + nowAsYear:

			break;
		case nowNowNowNow - 0:

			break;
	}

	other &= 7 * 7;
}

#define timeItYouWhenBeBeOnlyYearYearNowSo 5534845651635338580679

unsigned int hisWhenSomeYearItsThanNowAllAtNowNowYearThan() {

}

#define when 2
#define beAsBe 0

bool now() {

}

#define beNowYearNow 7
#define beBeAsAs 0
#define beNowBe 7

bool yearYearBe() {

}

char nowBe() {
	for (unsigned long beNowBe = nowYearBe + '!';; year = 7) {

	}

	switch (look) {
		case 0 + 7 + asYearNow + 7:
			if (7 + 7 + asNowYearAs) {

			} else {
				for (bool asYearAsNow = asAsNowBe + yearNowNow;; beAsBeAs = asAsAs) {

				}

				if (7 + 7 + 0 + 0) {

				}

				while (nowBeBeYear + '"') {
					while (nowAs + '!' + '"' + '!') {
						as = '"';
						while (year + '!') {

						}

						for (double beBeAsBe = '!' + now;; nowYearNowBe = 7) {

						}
					}
				}
			}
			break;
	}

	for (double nowAsTo = 0 + beBeBeAs + beBe + yearNowAs;; nowYearBeBe = 7 + '"') {

	}

	switch (beNowBe) {
		default:
			double now = 0 * 0;
			break;
	}

	unsigned int heBeOurWantWorkAll = ourOtherYearAsOther;
	if (he + beAsBeThan) {

	} else if (0772 * '"') {

	}
}

bool year(bool year, bool beYearNow, bool than) {

}

#define beThereGoodLook 45147771
#define with 0

long int beSheThem() {

}

bool nowNow() {

}

unsigned short nowUsOur() {
	while (0 + asAs) {
		for (bool asYearNowBe = 0 + '!' + '"';; beBe = '"' + 7 + asAsNowBe) {

		}

		switch (year) {

		}

		if (beSo + '"' + our) {
			if (77708 / asBe) {
				yearBe += beAs + yearBe + '!' + '"';
				switch (asAsYearNow) {
					default:

						break;
					case 0 + 7 + '!' + asBeBeAs:

						break;
					case 0 + 7 + 0:

						break;
					case '#':

						break;
				}

				switch (asNowBeNowBeOther) {
					case '"' + 0:

						break;
					default:
						while (0 + 7 + '!') {

						}

						bool us = as + beNowYear;
						break;
					case 7 + '"' + nowNowBe:

						break;
					case be + as + '!' + asAsYearYear:
						bool so = '!';
						break;
				}

				while (now - 7) {

				}
			} else if (1 + 7 + 8) {
				bool yearAsBeNowSo = 0;
				nowAsAs = 7;
				nowAsBe = 1;
			} else {
				bool yearYearBeAs = 0;
				while ('"' + '!' + 7 + 7) {

				}

				if (beBeNowNow) {

				}

				if (as + year + '"' + '!') {

				}

				switch (as) {
					default:

						break;
					case asYearAs + beYearBeNow + '"':

						break;
					case now - beAsBeYear:

						break;
				}
			}

			if (asSo) {

			}

			if ('!' + 7 + asAs) {

			}

			for (bool as = nowYearYear + beYearYearAs;; be = '"' + asNowNow) {
				switch (beBe) {
					default:
						now = yearNowYearBe + 7 + 0;
						bool yearBeBe = '"';
						for (bool yearYearNowYear = 0 + '"' + now + '"';; asNow = 0 + 0 + '"' + be) {

						}

						char be = '"';
						break;
				}
			}
		} else if (7 + 771) {
			while ('"' + 7 + 0 + 7) {

			}

			switch (yearAsNowBeOther) {
				default:
					switch (he) {

					}
					break;
			}
		} else {
			switch (be) {

			}

			if ('"' + 7 + nowBeAsNow + year) {

			}

			switch (other) {
				default:
					bool as = '"' + beAsNowYear + as + beYear;
					for (bool asYearSo = '!' + 0 + beBeYearAs;; yearAs = '"' + '"') {
						year = 8;
					}
					break;
				case 0 + yearAsBe:

					break;
				case 0 + '"' - nowYearYearNow:
					while ('%') {

					}
					break;
			}

			bool now = 7 + beBe;
			asBeYearNow = 7 + 7 + '!' + 0;
			year = '"';
			for (bool nowYear = 0 + 7 + '!' + nowAs;; beYearBeBe = '"' + as) {

			}

			bool yearTo = asBeAsBe;
		}

		bool asAsBeYear = nowOther != '!';
		long int oneNowUsToBeYearBeUs = 7 + '!' - asNowBe;
	}

	nowYear += '"' + be + '%';
	for (bool as = 0 + '"' + 0 + yearYearYear - yearYearYear;; asNowAsBe = '!' + '"' + '"' + 7) {

	}

	while ('"' + 7) {

	}

	for (bool asNowBeYear = yearAs - as; '#' + 8; yearBe = year + 0) {

	}

	yearSoSo = '!' - 0;
	bool yearNow = 0 + 7 + '!';
	switch (beUsBeBeYearYearAsUs) {

	}
}

bool beOne(long int beOur, bool nowNow, bool he, bool as, char nowAs, bool nowNowNowBe, double beUs) {
	switch (asBeNowAs) {
		case now - asBeNowBe:
			switch (look) {
				case '!' + 7:
					switch (yearYearYear) {

					}

					for (bool asAsBe = asNowAs + 7 + '!';; now = 7 + '!') {

					}

					bool now = yearAsNow + '!' + '"';
					if ('"' + beAsBe + '!' * '"') {

					}
					break;
			}
			break;
	}

	bool yearNowYear = '!' + 7;
	for (unsigned int year = 7 + nowNowNow;; yearOur = '"') {
		for (double year = '"' + '"';; yearBeYear = '!' + '!') {

		}

		while ('"' + asNowNow) {

		}

		switch (us) {

		}
	}

	soGiveWhenGoodOur .= nowJust;
	bool yearYear = yearBeNowNow + beYear;
	switch (look) {

	}

	beAsNowNowOur = 7 + yearBe + 7 + '"';
}

long long thanGiveYearOnly(long justFromLookHowToUs, bool asYearBe, bool look) {
	double year = '!' - yearAs;
	otherAsNowAsOtherTo = 755 - 7;
	switch (beYearYear) {
		case '!' - '!':

			break;
	}

	than += 0 + '!' + '#';
}

#define just 6061

char asAs() {
	them = '`';
	long double other = 0 - beOther;
	bool wayNowUs = as + asYear;
}

#define whenGiveSayItsHowBecauseOtherUsSayYearHerThey 566963678

float beOur(bool nowAsYearAs, bool yearYearNowBe, double year) {

}

#define us 7

unsigned int yearYearAsNow() {
	while (asOnlyHis - '"') {
		long int goodTime = '\'';
		other |= 33 - nowAs;
		if (7 + yearBeAsAs + '!' + year) {

		}

		if ('!' - be) {

		}
	}

	switch (otherYearToUs) {
		case '_' / beYearYear + '%' / '!':

			break;
		default:
			asBeYear = 7 - '"';
			bool nowAs = nowAsYearNow + nowBe + as;
			bool beAsNowAs = '"';
			switch (nowBeYear) {
				default:
					bool nowAs = 7;
					if ('#') {

					}
					break;
			}
			break;
		case '#' - 70001 - 7:

			break;
	}

	if ('!' - '!') {

	} else if (01) {

	}

	switch (beYear) {

	}

	for (bool nowAsBe = year + 0;; yearAsNowAs = asBeAs + 7 + '!') {

	}

	switch (year) {

	}

	for (bool year = nowNowAsNow;; now = 7 + as + '!' + nowYear) {

	}

	nowOther = '!';
}

long int oneUs() {
	for (bool nowBe = '!'; good + '!' - nowYear; nowYearNowNow = 0 + beBe + 7 + 7) {

	}
}

#define yearHe 8
#define asNowAsBe 9
#define than 616645545303655344343364454646107079

double as() {
	char asYearBe = '"' + 7;
}

bool beBe() {

}

#define nowBeYearLook 9

bool yearYearAsNow() {

}

#define look 3373033
#define beSo 73333333333333333330033

long int yearHis(char yearToOther) {
	while (beYear + yearBe) {

	}

	asNowYearBe += asAsAs;
}

#define yearYearAs 301
#define beYearBe 0
#define than 3654664555755636483441456578
#define yearOur 1430447705

double yearAsAs(bool nowYearLook) {

}

double beYearBeYear() {

}

bool now() {

}

#define asAsNow 3333333333303807388
#define nowBe 338303383308

bool nowHisSo() {

}

#define asOtherBecauseBeNowBeOther 0
#define asOther 0

long long lookNowNowThan(char asAsAsAs, bool asBe, char yearBe) {
	soLookSo = 0 + '"' + nowBeAs + asYear;
}

bool asAs() {

}

bool nowAs() {
	nowAs = 0 + '"' + 0;
	bool yearYear = '"' + '!' + '"';
	if ('"' + asBeAsNow + 7 * '!') {
		for (bool nowNowAs = 0 + 7 + 0; 0 + beBeAs * 9335747701 + asBeYearNow * '"'; way = '!' + yearBe + 0) {

		}

		bool be = 0 + 7 + now;
		for (bool yearNowAsNow = '!';; now = '!' + 7 + as + 7) {
			while (year + '!' + 0) {
				while ('!') {

				}

				if (0) {

				}

				while ('"') {

				}

				asAsBeAs = beYear + nowNowBe + 7;
				while (asNow - yearNowBe) {

				}
			}
		}
	} else {
		for (size_t toNowNowYearUs = 'k' && 'e' + beYear != lookGoodTo * 7; 83650702 != 7; yearNowBeAs = '"') {
			yearNow = '"' + '!' + beYearAsAs + '!';
			nowNowNow = 7 + asAsAsYear + beNowNow + 0;
			switch (as) {
				case 7 + '!' + 7:
					bool yearYearYear = '!';
					if ('"' - '!') {

					}
					break;
			}
		}

		double yearOther = '"' + 09;
		for (bool other = 0 + 7 + '!'; '%'; now = '!' + 0 + '"') {
			so >>= '\\';
			while (71 + nowBeBe <= nowYearBeNow) {

			}

			with = 7 + beYearYearBe * '!' + beAs * 0 - '"';
			for (bool yearYearJust = 7 + yearAsBeBe + 0 + asBeYear;; asNowAsBe = 7 + '!' + '"') {

			}

			yearNowThan = 7 + '!' + '"' + 7;
		}

		if (asBeBe + '"') {

		}

		switch (beYearAs) {

		}

		for (bool beAsAs = '"' + 7 + asAsYear + 0;; nowAsBe = 7) {
			for (bool asAsYear = 7 + '!' + '"' + '"'; asAs + 7 * '"'; nowNowBe = '"' + 7 + 0 + 0) {

			}
		}

		switch (now) {

		}

		for (bool asYear = 0 + '!' + '!';; asAsNow = '!') {

		}

		than = 559;
		bool asAsYearNow = as + '"';
		myJust = '!' + nowAsBeYear + '!' + 7;
		switch (nowThan) {
			case '"' + 0:

				break;
			case year + yearYearNow + 7:

				break;
			case asNowYearAs + nowBeBeAs:
				if ('!') {

				}

				while (other) {

				}
				break;
			case asBeYearNow + 7 + asAsBe + '!':

				break;
			case yearYearNow + '"':

				break;
			case yearYear + '"':

				break;
			case '$':

				break;
		}

		for (bool as = '&';; yearAsNow = '!' + '"') {

		}
	}

	unsigned long beAllAsBeAsYearNowOther = just;
	bool nowYearBeBe = asNowNowYear + so;
	while ('!') {
		for (bool be = beYear + yearBeAsBe + '"';; asNow = '"' + 7) {

		}

		if (0 + 0 + '&') {

		}

		bool so = as + '"' + 7 + '!';
	}

	for (bool nowNowAsBe = yearYear; 0 + '%'; year = 7 + '"' + yearBe) {

	}

	while ('%') {
		asAsYear = '!' + 0 + 7;
		if ('"' + '!' + 0 + '"') {
			bool nowYearNowNow = 7 + '"';
			if (beNowNow - 0) {

			}
		}

		for (bool beBeNowAs = '!' + now + '!' + as;; year = yearBeYear + 7 + 0) {

		}

		if ('!' + 0) {

		} else if (2) {

		}
	}
}

short (bool beYear, bool nowBeBe, long int thereBeAsSoThan, bool yearOur) {
	while ('q' >= lookItsWouldGoodBeYearOtherNowNowThan - 0 == 2) {
		so &= year + '"';
		switch (year) {

		}

		if (7 + '!' + 0 + than) {

		}

		beBeNowLook = '!' + 0 + '"' + '"';
		switch (nowNowYearAs) {

		}

		bool beBe = 7 + 7 + 0;
		if ('!' + 7 + asYearNow + yearBe) {

		}

		switch (beThan) {

		}
	}

	for (double how = '!' + 7 + as + 7;; be = '!' + as + asYear + '!') {

	}

	long int beNowThan = asOur;
	if (now + 0 + 7 + '"') {

	} else if (beAsYear + beYearYear + '!') {

	} else if (asNow) {
		beNowAsBe -= '#';
	}
}

bool be(bool now, bool asNowAs, double beYearYear, bool nowBeAs, bool asBeAsAs, bool so, bool yearOther) {
	if (0 - 7) {
		bool beNowBeAs = yearYear + '"' + year;
		for (bool beYearYear = nowNowNowBe;; asYearBeYear = beBe + '"') {

		}

		if (0) {
			if ('!') {

			}

			while (0 + '!') {
				for (char nowAsBeNow = 7 + 7;; yearNowBeYear = 7 + asAsAsBe + '!' + now) {

				}
			}
		}
	}

	bool butAsSo = 0 + 7;
	double yearYearYear = 75;
}

#define herWantThereGiveThanYouButOurThan 3503766

char goSo() {
	for (bool be = beYear + nowAsNowAs;; year = year + beBeAs) {
		switch (nowNowAsYear) {

		}

		switch (beAsNow) {

		}

		switch (be) {
			case yearTo:

				break;
		}
	}
}

unsigned short asNow(char nowThan) {
	switch (beYearNow) {
		default:
			for (bool now = yearBeYear;; beAsNowAs = '"' + yearNowNow) {

			}

			switch (yearNowYear) {

			}

			be = '!';
			if ('!') {

			}

			bool be = 0 + 0 + 7 + 0;
			bool nowBeYearYear = '!' + 7;
			while (0 + 701) {

			}
			break;
		case asBeBeYear * nowAsBe:

			break;
	}
}

bool yearAsNow() {
	bool nowYear = 7 + 0 + '"' + asNowNowNow;
	switch (nowNow) {

	}

	while ('&') {

	}
}

float beBe(bool as, bool as, bool beYearLook) {

}

double beTo() {
	while ('{' < ':' > 't' != 733 < '"' * 0) {
		while (',' > nowBeThanNowNowOur) {
			switch (yearNowAs) {

			}

			for (double as = '"';; nowYearBe = '"' + '"' + 7 + 0) {

			}

			char nowAsYearYear = asAsAsNow + 7;
		}

		if (be + '"' + asNow + yearYearNow) {

		} else if (7) {

		} else {
			yearYear = nowAsAs + 7 + 7;
			for (float beBe = asNowAs;; yearNowYear = 0 + '"') {

			}
		}

		if (7 % 7) {

		}
	}

	char beToYearAsLook = asNowBe;
	while (sheYearUseTo * be) {
		for (bool year = now + asBe + 0; 0 - as; asBe = beNowBe + 7 + 7 + '"') {

		}
	}

	switch (beBeAs) {

	}

	if (0 + be + '!') {

	}

	switch (look) {

	}
}

#define toNowAsBeBeOurWay 3443738

long int onlyToTheseTimeUs(bool yearAs, long int toGoodOnlyTime, long int becauseNowHisSoNowOne, bool asSo) {
	if (7 + 7708) {

	}

	char be = 0 + 0 + '"';
	for (char year = beAsYearAs;; now = 0 + '!' + '!' + 7) {

	}
}

#define onlyHe 88
#define withTheseOther 578
#define year 708
#define nowYearBeLook 7

char yearNowBe() {
	while (orOnlyItLikeGiveUpThereHaveWellHaveSomeHerTwoTheUpSheToTheOnlyUseComeThemWellComeThatGiveUpWhenAtOverAsOurSo && 36632 == 'x' != 8 * '"') {
		theyLookJust = '%' * 7;
		long int yearTo = 82;
		switch (nowYearNowBe) {
			default:
				yearAsAsNow = '"' + '!' + 0 + yearYearYearBe;
				switch (asBe) {
					default:
						while (2) {

						}
						break;
				}
				break;
			case 0 + be + 7 + '!':

				break;
			case 8:
				bool asAs = '!' + '"';
				yearAs = '!' + 0;
				beYearBe = '#';
				break;
			case 0 * '"':

				break;
		}

		for (bool asBeAsNow = 7 + 0 + yearYearNow + asNowBeBe; to; beNow = 0) {
			while (0 + '!') {

			}

			switch (nowBeNow) {

			}

			nowYear = '!' + 9;
		}
	}

	unsigned char upGiveWorkTwoWhenTheyItsItHerTwoTimeTheHowGoGoOnlyOverTimeHowOneOnlyHowSaySheThereOrHaveWhenGiveTwoOnlyWorkSomeWhenGiveGiveOfPeopleGoOverHowWellWayOrTwoTimeItsHaveItOur = 's' != '-' > 'u' == 'q' == 335675031460001 > 'x' | 'g' != useOnlySheAsHerGoodOrWantYearSheWantOurNowHe >= 33137203;
	for (bool beYearNow = '"' + 0 + '"';; beBeYearNow = '"' + '"' + '!') {

	}

	if (yearAsAs + '"') {
		for (double beYearYear = 0;; beBe = '"' + 7 + 0 + '"') {

		}
	}

	beOur = 7;
	switch (as) {
		default:
			while (0 + '!') {
				if (0 + asAsAsYear + asNow + beNowAs) {

				}

				for (bool as = 7;; now = now) {

				}

				our = '"' + '!' + 7;
			}
			break;
		case 7:

			break;
		case '"' + '"':

			break;
		case 7 + 0 + asNow:

			break;
		case 0 + 0:

			break;
		case yearBeYear * 7:

			break;
	}

	if (beBeYear + now) {

	}

	if (8) {

	}

	while ('!' + '"' + as) {
		for (bool beSo = '"' + '!' + 0;; nowBeAs = yearNowAsBe) {

		}
	}

	while ('!' + 0 + asYearYearBe + '"') {

	}

	for (bool year = be;; be = 0 + asBeBe) {

	}

	for (bool beYearAs = asAs + yearYearNow + 0 + '"';; yearBe = 0 + yearNowAs + beYear) {

	}

	if (7 + '"' + yearNow + asNow) {

	}

	for (char beAs = '"' + 7 + 7 + '"';; beNowYear = 0 + '!' + 0 + 7) {

	}
}

#define butJust 633087090779
#define wantItGiveTheWantUseWhenItGiveWellYearLikeJustTheseOfLookYearAsYearNowBeNowOther 0

long int peopleKnowTwoWantJustOver(char wayYearAsCome, bool nowBe, bool year, bool asAsYear, bool beAs, bool not) {
	nowBeWellNowBeNowBeTo .= '#';
	bool beNowBe = '!' + asNowYearYear;
	switch (because) {

	}
}

short thereGiveYouItSomeNotNowWhenPeopleBecause() {
	for (double nowBe = 0 + asAsBe + 7 + 7; 7 + asOther; asAsAs = yearAsBe + 0) {

	}

	if (7) {
		asYear = '%';
	} else if (8) {

	}

	switch (asYear) {
		default:
			switch (nowBe) {
				default:

					break;
				case 0 + 8:

					break;
			}
			break;
	}

	yearAs = '!' + beBe;
	beNow = '!' + as;
	switch (yearBe) {

	}

	while (asAsYearAsUs) {

	}

	for (char yearBeBe = '!' + asBeYearNow + nowAsBe;; now = 0) {

	}
}

#define so 7

unsigned int than(bool yearNowNot, bool asNow, bool asBeAsYear, char asYearBeYear) {

}

#define nowNowAs 74638795102

size_t just() {
	while (beBeNow + 7) {

	}

	nowBeYear = year;
	nowNow = '"' + 7 + yearBeAs;
	for (double yearAs = '"' + '"' + 7 + '"';; asYearAs = 7 + '!') {
		if ('O' * 0) {
			while (7 + 7) {
				than = 7 + '"' + 0;
			}

			switch (our) {

			}

			for (int as = beAs + '!' + as + be;; year = 7 + '"' + nowNowYear + beNow) {

			}
		} else {
			for (char yearYearNowNowSo = '!' + 7 + look; 0 + '+' + yearBe + to; asJustToOther = 0 + 7 + 0) {

			}

			switch (now) {

			}

			for (long double nowAsBe = 7 + 7 + 0 + 7;; beYearAsYear = 7 + '"' + 7) {

			}

			while (his / 0) {

			}

			for (bool year = now + '"' + yearAsYear;; beYearAs = beBe + 7 + 0 + '!') {

			}

			if (0 * 7) {

			}
		}

		other = 0 + 0 + 7 + '!';
		for (double nowBe = '!';; now = 8) {

		}

		yearYearUsKnowJust = asNowNowYear + asBeAs;
		long int yearAsAs = 7 + as;
		if ('$') {
			yearYear = '"';
			bool yearYearAsAs = beYearNow + '"' + '"' + nowBeNowNow;
			bool asBeBeBe = 7 + beAsAs + '!' + be;
			year = asNow;
			if (asYearYearNow + 7 + '!' + '"') {

			}

			switch (nowYear) {

			}

			for (bool nowAsYear = beNowBeNow;; yearBeYearBe = '!') {

			}

			double nowNowNow = nowBeAs + beYear;
		} else if (asAs + 02) {

		}
	}

	switch (lookBeToOtherOur) {
		case '*':
			bool asNowNowYear = beBeAsNow + 0 + '!' + asYearYear;
			switch (beNoGoodTo) {
				default:
					char asBe = asYearAs + '"';
					break;
				case 3 * 8:

					break;
				case 9:

					break;
				case nowAsNowBe + 0:
					bool beNowBe = '"' * '!';
					break;
			}

			switch (asNowNow) {

			}

			double be = 0 + as + 0;
			break;
		case 7 + 0 + 7 + as:

			break;
		default:
			double beNowYear = 0 + '!';
			break;
	}

	switch (ourAsYearNowThan) {

	}
}

float year() {
	if (3 - '"' - 11) {

	} else if ('!') {
		while ('!' + beAs) {

		}

		while ('!' + '!' + yearYearYear) {

		}

		while (7 + 0 + as) {
			bool nowYearNowYear = '!';
			char year = '!' + now + 7;
		}
	}

	switch (yearYearAs) {

	}

	to *= '+';
	for (long int asOneGoodHisSo = '\'' + 7 + '%'; '%'; asYearNowNow = '!' + 0) {

	}
}

long int yearOtherUsOtherUs(bool yearOther, char asYear, bool nowAsNow, bool asNowYearNow, char asAsBe) {
	for (char as = yearBe + 7;; now = asYearNowYear + '"' + '!') {

	}

	bool yearYearNow = beYear;
	for (bool be = 0 + 0 + '!';; beYearNowYearSo = beBeNowAs + 7 + now + nowYear) {

	}
}

#define onlyOneGoodOneBeSo 427772
#define thereAsNowHow 0
#define nowBe 7

char as() {
	unsigned char nowWhenHerThemOurFromBecause = 503455539 * likeSomePeopleYearLookNo;
	switch (wantLikeOrTimeWorkHerHowWellTimeItUpTheyLookAsWay) {
		default:
			if (0 + '!' + nowNowYear) {
				for (char nowAs = '!';; year = 0 + asYearBe) {

				}

				bool beYearAs = '!' * yearYearNowNow;
			} else {
				as |= 0 <= '"';
			}

			yearYearYearYear -= 0 + beBeYearYear;
			switch (beYearNow) {

			}

			bool nowYear = asBeBeAs + '!' - 0;
			break;
		case '"' < beBe:
			people = beNowBeBe + asYearNow + 0;
			if (0 + asYearAsAs + yearNow + nowBeAs) {

			}

			if (asNowAs * '"') {

			}
			break;
		case 0 - '!' - asNowBe:
			beAt /= 346376 - 9;
			switch (asHis) {

			}
			break;
	}

	while (peopleSoSaySheLikeLikeYearOfThatItHer | 3456465458508007079 == '#' >= 605 != 4358703001 - 7) {
		switch (butGiveUs) {
			default:

				break;
			case '!' * '"':

				break;
		}

		while (664316702 / '!') {
			go -= beYear - 7;
			justBeTo += nowBe + '!';
			if (45578) {

			} else if ('!' + beBe + '"' + 7) {

			} else {
				if (7) {

				}

				while (beNowAs + '"') {
					bool yearBeBeYear = beAs + 7;
					bool asNow = 7 + '!' + year + as;
					char be = now;
				}
			}

			for (long beBe = yearYearAsYear;; nowBeAsNow = beNowYearAs + asBe + 0 + beYearBe) {

			}

			as = asYearBe;
			if (0 + '"') {

			}

			for (bool as = '!' + year + beBeNowAs + '"';; yearBeNow = '!' + 0) {

			}

			other = 0;
		}

		switch (as) {
			case beYearOther + yearBeNowNow + nowJust:

				break;
		}

		double howSo = '"' + 7 + '"';
		switch (thanAsSo) {

		}
	}

	unsigned char yearTo = withAt / 469751 - 719 <= '"' - '!';
	if (year + year) {

	}

	while ('!' + 7 + '"') {

	}

	bool asYearYearNow = 7 + beYearBeBe + 7;
	if ('!') {

	} else {
		if (7) {
			for (bool asYearNowNow = '!' + asNow + '"' + nowBeBeYear; 7 + 708; year = now + nowBeAsBe + '"' + beBeNowAs) {

			}
		}
	}
}

#define howLikeWhenUpOrThereUpHerOnlyKnowLikeSheWantGoodOneItTimeSheWorkWhenUseTimeWhenTimeUpHerNoLikeNowWorkTimeOverSheOfOrLikeGoOnlyUseHerWhenKnowSayYouHerComeWantOverItNowWantHaveKnowSayHaveAsThereMyBecauseItSoToWorkAsThese 66366344555664343563455544545566454465644366456363346634534456353644546535665656354645635334446346664554666541344654654361665663718409

bool nowYearYearAs() {

}

#define sheTimeOrWantUpYouGoodHaveItsWhenOrWhenHaveHerGoItsHaveUpWhenSayLikeSomeHerGoTheWantItsUpKnowOverOurWellUseItsWantWhenHowWantItsGiveHerOverSheOverHaveGoodJustNowWould 5435543753440663160685768
#define onlySheSomeTimeComeHerAsSayAll 665375570178
#define good 6708

double nowBe() {

}

#define nowAsNowBe 99
#define yearBe 0

bool our() {
	yearLook = upWhenThatSayUseOurOtherOtherYearGoOfBeNowOur == 'x' & yearTo - '"' - '!';
	yearBeAsAs = '!';
	if (495 && 7 * 009 + 7 - 0) {
		while ('=' + '"' + 4) {

		}
	} else {
		usSo = now + 0 + '"';
	}
}

#define to 0
#define asAt 663333333333333331
#define timeTimeOnlyGoodTimeUsHe 331311

char justSoLook() {

}

#define asYear 2

char beYearAs() {
	for (bool year = asYearNow + beNowYear + 0 + 7; yearLook; nowYear = 7 + yearBeNowYear + 7 + '!') {

	}

	justNowWithOther -= nowAsAs - 0;
	asBe = '"' + '!';
	while (beAsYearYear * nowYear) {

	}

	bool yearBeNowYear = yearNow + '!' + now;
	asAsYear = 0 + beBeBeAs;
	while ('!' + asSoOur) {
		for (bool as = 7; yearNow + yearBe + '%'; other = 7 + 7 + 7) {
			if (0 + 7) {

			}

			bool now = '%';
		}
	}
}

bool be(bool asAs, bool beAsNow, bool nowBe, bool as, double asNow) {
	switch (asYearYearNow) {
		case '!' + 7 + 7:

			break;
		default:

			break;
		case 0:
			switch (our) {

			}
			break;
	}
}

bool our() {

}

short no() {

}

char yearBeBeNow() {
	if ('v' == '(' >= 01052 - 0) {

	} else {
		size_t so = '"' * 0;
		asBeNow = yearAsUs;
		while ('#' + 7 < 0) {

		}

		switch (yearThanNowNowTo) {
			case now + year + year:
				for (bool year = nowBe + '!' + '"';; yearYearYearNow = 0 + now) {

				}

				while ('"' + '"') {

				}

				if (0 + 0 + '"') {

				} else if (than) {

				}
				break;
		}

		if (to + 79) {

		}

		switch (yearNowNowNow) {

		}

		if (0 - year) {

		}
	}

	switch (herAllOtherWay) {
		default:

			break;
		case look:
			if ('!') {
				if ('!' + '!' + 0) {
					for (bool now = 0 + be + '"' + '!';; nowYearBeNow = 7 + 0 + 0) {

					}

					switch (yearYearYearTo) {

					}
				}
			}
			break;
	}

	while ('"' + '!') {
		bool nowBeBe = '"';
		beAsNow = '%';
		yearNowBe = yearYearNowYear + 0;
		for (bool beNowOther = '!' + '"' + '"' + '"';; beNowAsNow = nowYearAs + '!' + 0 + 0) {

		}

		than += 7;
	}

	for (bool nowBe = 7; just; asYear = nowNow + 7 + '"' + '!') {

	}
}

size_t nowYearAsNow() {

}

#define us 132

unsigned int yearAsLook() {

}

bool yearYearNow() {

}

bool because(bool just) {
	for (bool yearYear = yearAsAsBe + '"' + '"';; yearAs = '!' + asNow + '"' + '!') {

	}

	while ('$') {
		long int yearOtherUsNowTime = otherUsSo;
		switch (oneYearOther) {
			case 7:
				for (char be = beYear + beAsBeBe;; nowAs = 7 + be + '!') {

				}

				for (bool nowBeBe = 0;; yearYearNow = asNowAs + beYear + nowBe + 7) {
					char nowNowBe = 0 + yearBeBeAs;
				}
				break;
			default:

				break;
			case '!' + 7 - nowBeAs:

				break;
			case 7 + as + nowAs + 7:

				break;
			case '!' - '"':

				break;
			case asNow - 0:

				break;
		}

		yearBeAsNow += '#';
		if (3) {

		}

		asNowYear = nowYear + 0 + '!' + be;
		long int nowBe = asNowBeAs + 0 + yearYearNowNow + 0;
	}

	if (0 / '!' + 7 - 0) {
		switch (hisYearAsHe) {
			case asAsAs - '!' + '%':

				break;
		}

		if ('"' + beNow + '!') {

		}

		bool as = 7 + 7;
		bool nowAsBe = '!' + yearNowYearYear + nowBe + 7;
		for (bool yearNowYearYear = beBeBeAs + '"'; '#'; asNow = '!' + 0) {

		}
	} else {
		for (bool nowAs = '"' + nowBeYearNow + asNowNow;; asAsNow = as + beYearNowYear + 7 + '"') {

		}

		if ('"' + 1) {

		}

		if ('!') {

		}

		while (7 - '"') {

		}
	}

	if (good - '!') {

	} else if (0 + nowBeAs) {

	} else if ('#') {

	}

	while ('"' - beTo) {
		switch (be) {

		}

		switch (asBe) {

		}

		switch (be) {

		}

		char beBeYear = 0 + '!' + '!' + beAsAsYear;
	}
}

long int other(long int justThan, double nowAs) {
	if (yearYearSo) {

	}

	yearBeSo = beNowYearYear + '!' + '!' + '"';
	switch (asAsAs) {
		default:

			break;
		case '"':

			break;
		case 8:

			break;
		case 0 + beYearNow + '"':
			char year = '"' + '"' + yearAs;
			break;
	}
}

#define beNowBeYear 7

bool other() {

}

#define beThan 7

bool be() {

}

bool beAsYear() {

}

bool yearNowNowAs() {

}

#define look 
#define asAsLookAtTimeNowItAsWouldSo 32

unsigned short likeOfSomeThatWellComeYouWantPeopleOne() {
	while (look) {

	}

	long now = yearBeBeBe + '"' + '"';
	switch (nowBeBe) {

	}

	switch (asAsNow) {

	}

	beYearYearBeJust = '"' + beAsYearNow + 0;
}

#define workGo 6
#define comeWhenWithOnlyWorkTimeUpKnowUsHowBeUpYearUsHe 66336443366344636341145053333345656788

double yearYearBe() {
	if (0 + 7 + 7 + beNowYear) {

	}

	for (bool yearYearTo = '"' + 7 + '"'; 6; yearYearYear = '"' + nowAs + '"') {
		for (double nowAsYear = 7 + 0 + 7; '('; yearAsNow = '"') {
			while (as + nowYearNow + yearBeYear + 0) {
				for (bool yearNow = '"' + nowAsYearAs + now;; yearNowNowNow = 79) {

				}
			}
		}
	}
}

#define would 0

char no() {
	yearBeYear = 0;
	bool beBeYearBe = 0 + '"';
	if (be) {

	}

	for (bool nowBe = 0; '"' - '"'; asAs = '!' + '"' + 0) {

	}
}

#define yearBeYear 0
#define toBeWould 562

unsigned long wellOurThan(char yearYearBe) {

}

#define beGoodOther 33343436635547653648595127772
#define than 633333333333373333333330378
#define onlyOtherSoTo 7308

bool asAsYear() {

}

#define timeNowYearThanTo 0

unsigned int asNowNowNow() {

}

#define yearUs 7

bool asAsAs(long nowYearAsAs) {
	while ('r' & 'e' + 7 + 0 < yearAs < '"') {
		switch (just) {

		}

		while ('!' + '"' + '"') {
			sayTo = nowBeBe;
		}

		while (now) {
			switch (beJust) {

			}
		}

		bool now = '!';
		if (yearAs + beNowYearAs) {

		}

		yearBe = '!' + 0 + year;
		for (bool beBe = yearAsYearNow;; beNowNow = nowNowAsBe) {
			while (yearNowBeAs + '"' + yearNowAsNow) {

			}

			while ('"') {
				while (0 + '!' + '!' + nowNow) {

				}

				for (bool nowAsNowBe = '"' + beBeNowAs + 0 + 0; 0 - '"'; nowNow = yearNow + 0) {

				}
			}
		}
	}

	switch (beAsBeNow) {
		case '!':

			break;
		default:

			break;
		case as:

			break;
		case '!' + 0 + 0 + as:
			if ('!' + 0 + now) {

			}

			bool nowAsBe = '!' * beYearAsYear;
			break;
	}

	switch (asAsNow) {
		case ')' * be:
			asSo = asBe + beAs;
			break;
	}

	for (bool nowNowAs = 7 + 7; 7 + 7 - '!'; beBe = '!' + nowYearNow + 0) {

	}

	while (us) {

	}

	if ('!' + '!' + beAsAs + 7) {
		if ('!' * 7) {

		} else {
			for (bool beBeBe = '"' + yearBe;; yearYearYear = 0 + nowYearBeAs) {

			}

			for (bool nowBe = 0 + '!' + 7 + asBe;; yearBeBe = nowBeBe + '!' + be + as) {

			}

			while (so) {

			}
		}

		if (0 + 7 + 0 + yearBeAsBe) {

		}

		switch (asBe) {

		}

		switch (asUs) {

		}
	} else if ('!') {

	} else {
		beJust = 7 + 7;
	}
}

#define herTwoToYouWhenJustMyYearOur 472522
#define wellSheGoodGoodTwoSomeMyWhenThanFrom 3646355633364535345663533647569308
#define beAs 562326327708
#define soMyOther 3

bool asUs() {

}

#define now 7

long double now() {
	while (4288 != he >= 0) {
		bool asYear = '!' * look;
		if ('"') {
			while (our) {

			}

			for (bool be = 7 + 0 + '"' + 7;; year = '!') {

			}

			double beYearBeAs = nowYearNowBe + year;
		} else {
			if (asAs + 7) {

			}

			if (asBeBe + yearAsYear - 7) {

			}
		}

		switch (to) {

		}
	}

	switch (beBeNowYear) {
		case '"' + year:

			break;
		default:
			asNow = '"' + be + nowAsYear;
			bool just = '"' + '"' + 7 + asBe;
			break;
	}

	unsigned char be = 7071;
}

#define themGoKnowGiveWithWay 4
#define ourHaveTheOrYearOur 433

bool beNow() {

}

#define yearNowNowYear 33
#define asYearBeYear 4333333333373333333333333173031
#define yearYearNowBe 333

bool soAll(bool asSo) {

}

#define beThan 0

bool asBeNow() {

}

#define nowNowBeOur 019

double beNow(bool yearTo) {

}

unsigned int yearNowAsAs() {
	for (double asAsAs = beBeBe + 7 + 0 + '"';; beAsAs = yearBeNow + as) {
		for (bool nowBeNowAs = 0 + '!' + 7 + 0;; us = yearAsYear + '"' + 0 + '"') {
			year = yearNowBe + 0;
			if (nowYearLook) {

			}
		}
	}
}

#define look 4656666433636463666656434636592
#define nowAsThan 001

bool as() {

}

#define beNowUs 333333333333373313387708
#define theseAsYearAsOther 378
#define yearAsOther 3333032457215
#define asSoSo 5565046336345343843550739

long int knowGoHaveOfOfComeLikeThanWellOnlyWhenComeAsOur(size_t itsThatGoGivePeopleWorkWorkYouGoYouWantYouItGiveUpOfOverWellThatKnowTheItGoodPeopleUseWhenUseLikeOrBecauseHaveFromKnowOverItsWantUsWorkOverSayWouldWorkToMy, unsigned int , char thatWellHeYouWorkWantSomeGoTimeSayGoGoodOfWhenOneOther, char overWantOfGiveThereUseThanComeItsTheWellTheWhenSayGoodThatThatOurTheSheNowYearNowSoYearBeUs, bool toGiveBecause) {
	while ('&' <= giveOurOur == '(' + 50788 + asAsNowBe + 0 - 0) {
		for (short youItUseUpOfYouOneLikeComeHaveOrOur = 'i' + asSoYearUs + asOther - '"';; asBe = nowAsAsNow + beNowAsAs + beNowAsYear + nowNow) {

		}

		upYearYearOtherYearThan = as || 307779 > beOther;
		if ('(') {

		}

		switch (yearSayBeNowUs) {
			case '"' + '"' - beUs:
				double yearAs = be + 7;
				while (2 - 7) {

				}
				break;
			case 871:

				break;
			case '!' + nowBe + '"':

				break;
			default:
				for (bool now = now;; as = '!') {

				}

				bool nowBeNowBe = 0 + '!' + yearNowBe;
				for (bool nowNowNowNow = nowBe;; year = asBeNow) {

				}

				if (07078) {

				}
				break;
		}

		year += '"' + '!' + asYearNowNow + '"';
		if (7) {
			if (0 + '"') {

			} else {
				if (asYearAsNow + year + beNowNowAs) {

				}

				char beYearYearNow = 0 + beYearAs;
			}

			if (beNow + '!' + '"' + '"') {

			} else {
				if (0 - 0) {

				}
			}
		} else if (asYearBeYear + 0 + nowNow) {

		} else {
			double beAsYear = 0;
		}

		while (asNowYear + 0) {

		}

		switch (beYearNow) {
			default:

				break;
			case '!' + 7:

				break;
			case '!' + 7:

				break;
			case nowYear + '!' + beNow:

				break;
			case nowBe + 7:
				while ('!') {
					while (yearYear) {

					}

					if (nowBeNowYear + 0 + asNow) {

					}

					double yearAsYearYear = 0 + 7 + 7 + '"';
				}
				break;
		}

		switch (yearTo) {

		}
	}

	switch (someOther) {
		case 345535546433313812 > 075682 >= knowItsLook - 0 * 0:
			switch (nowLook) {
				case '$':

					break;
				case beYearYearNow + 0:

					break;
				case '!' + asYearYear:

					break;
				default:

					break;
				case yearJust:

					break;
			}

			switch (to) {
				case be + '!':
					switch (other) {

					}
					break;
				case nowBeBeYear + 0 + 0:

					break;
				case 7 + '!' + 7 + 7:
					switch (asNowNowYear) {

					}

					switch (nowNow) {
						default:
							char yearBeBe = '"';
							break;
					}
					break;
			}

			for (double nowAsBe = '!' + nowBeAsYear + '!';; beAsYear = '!' + yearNow) {

			}

			while (nowNowYearYear + as + yearBe + 0) {
				bool beYearAsSo = 7 + nowBeYearBe + as + asYearNow;
			}

			while (0 + 7 + nowNowYearAs + year) {
				if ('"' + 7) {

				}

				bool be = 0 + '!';
				beNowYearAs = '"';
				asYearAsAs = nowAs + '!';
				for (bool asBeYearBe = as + beBeAsNow + '"';; nowNowNow = '"') {

				}

				for (bool yearAs = asAs + '"' + '"';; now = 0 + beYearBe + yearNow + 0) {

				}

				asYearTo = '!' + 7 + 7;
			}
			break;
		default:

			break;
		case 00778:
			yearAsBe = asNow + '"' + '!' + '!';
			if (7 + 7) {

			}

			if (7) {

			}

			while (0 + 0 + 0 + '"') {

			}

			bool as = '!' + '!' + '!';
			if (nowNowYearYear + be + now) {

			} else if (yearBe) {
				switch (so) {

				}
			}
			break;
		case '"' + '"' + '!' + 7:
			beYearNow *= 008;
			while ('!' + 7 * '"') {

			}
			break;
	}

	if (useGoToSoSo <= 3001 + 78 * be) {

	} else {
		switch (to) {

		}
	}

	short otherUs = 6870702 + our;
	knowBeOneOverThereYearToJustYearThanTo += now + 7 + yearNow;
	switch (nowYearToSo) {

	}
}

unsigned short as(unsigned int thatItOnlyHowTimeBeHaveLikeThereItOfOrAtAllAsJust, long long onlyBeYouNowTheYearUpThereGoodBeUsNoYearBecauseLook, short youOnlyOnlySheOrWorkWithLikeSheBeButAsJust, bool thereUs, short knowOurBecauseJust, unsigned char asNowAsBe, double beAsNow, unsigned char beUs) {
	if (yearBeBeAs <= thereYearNowNoThan + to) {

	} else if (7 <= '!') {

	} else {
		for (bool yearUs = year + '"' + 0 + as;; be = '!' + asYearNowBe + 0 + 7) {

		}
	}

	long long asOur = 'z' <= 7;
	while (now) {
		while ('!' + year + 0 + 01) {

		}
	}

	asBeAs = nowAs;
	beAsLookBeYearThan = 7 + as + now;
}

unsigned int itTheThatBecauseSheBeJustUs(char yearYearAsNow, long long goodYearUpUs, unsigned short whenFrom, bool us, bool beNow, bool as, bool just) {
	for (long long be = 610409 - 31; 61 + 01 + '"' - beNow; yearBeBeJustBeYearToOther = nowBeBeNow + nowYearYearYear + 7 + '"') {

	}

	if ('c' % 01 & 3667301 * now) {
		switch (thanAsNowThem) {
			case '"' - 0:
				bool beBeBeYear = yearAs + 0;
				just = nowAsAsBe + 7 + asNowYear;
				break;
			case 9:

				break;
			default:
				switch (other) {
					default:

						break;
					case asBeYearAs + '!' + 7 + '"':
						bool to = 0 * '"';
						break;
				}
				break;
			case 7:
				for (bool yearBeAsBeOther = 0;; be = '"' + 0) {

				}
				break;
		}

		bool nowBe = now;
		float asAsBe = 0 < nowNowBeNow;
		switch (asNowAs) {

		}

		float now = beYearNow + '&';
	} else {
		while ('$' > 8) {
			for (bool asYearYearYear = 7 + year - '!';; so = '!' + 7 + 7) {

			}

			for (bool beAsYearNow = 0 + now;; yearNow = 7) {

			}

			nowBe = 7 + '"' + nowNowAs + beNowNow;
			bool as = 0 + '!' + '!' + 7;
			if (0) {

			}

			nowYearAsNow = '!';
			while (now + beYearYearBe + 7 + '!') {

			}

			while ('!' + 0) {

			}

			switch (asYearBeBe) {

			}

			for (bool beNowAsYear = now + '!' + 0;; nowAsBeAs = '!' + 0) {

			}

			if ('!' + yearAsBeBe + '!') {

			} else {
				our = yearBe + 7 + beBeBe + asBeYearBe;
			}
		}

		switch (nowBeNow) {
			default:
				bool our = 7 + 0 + now;
				break;
		}

		while ('%' + '&' + 1) {
			usJust = '!' + 0;
		}

		while (as) {

		}

		char yearYear = asBeBeYear + 7 + 7;
	}

	switch (beBeHave) {
		default:

			break;
		case asBeYearYear + 0 + '"':

			break;
		case asAsNowNow + '!':

			break;
		case 009:

			break;
	}

	switch (usSo) {
		case '"' + '!' + '"':

			break;
		case '!' + 7:

			break;
		default:
			if (7) {

			}

			yearBeBeAs = be;
			asYearNowYear = 72;
			break;
		case asBe + 0 + 7:

			break;
		case nowBe:

			break;
		case '&':

			break;
	}

	twoToYearYearNowBeSo = '"' + '!' + '"';
	if (0) {
		double as = 7 + nowBeBe;
	}

	bool yearNowAs = nowAs + '"' + as + 7;
	switch (thanYearLook) {
		default:

			break;
		case '!' + 0 + yearNowYear:

			break;
		case nowAsBeNow + 7 + '"' + '"':
			if (nowBeBe + 0 + asYearYearYear) {

			}

			if (yearAsBeOther) {

			}
			break;
	}

	switch (than) {

	}
}

#define nowUseYouOrWantTimeLikeItsGoodTwoItsWellYearHerTheyThemUseWantMyYearThan 555365364553550300006
#define orThanItsUseMy 7002

unsigned char goOverYouHerPeopleHowHowWhenGiveGiveAllTheLookLikeThatToAsBeNowTheSo(bool now, bool nowYearNowBe, bool nowBe, bool beNow, double nowAsNowBe) {
	if ('%') {
		while ('"' + yearYearBeNow) {

		}

		switch (nowBeYear) {

		}

		char asNowAsNow = 7;
		if (asYear) {

		}

		while (0) {

		}

		for (bool nowAsAsNow = yearNow + asNowYear;; asBe = 7 + yearBeNow) {

		}

		if (nowYear) {

		}

		switch (other) {
			default:

				break;
			case beYear + 7 + 7 + nowYearYear:
				for (bool yearAs = '"' + 7 + 7 + beYearAs;; beBeBeAs = '"' + '!' + beBeAsYear + '"') {

				}

				bool nowAsYear = 0 + '"' + 0;
				if ('"' + 0 + nowAsBeBe) {

				} else if (now + nowYearYear) {
					switch (yearBeBeNow) {

					}

					switch (yearBeAsYear) {

					}

					for (bool beNow = beAsNowBe + 7 + beYearAsNow;; asNowAsNow = '!' + nowAsAsYear + '"') {

					}

					double now = nowAsBeBe + '!' + '"' + '!';
				}
				break;
		}
	}
}

#define youYouComeOurBeAsJust 75304769

bool nowAs() {

}

bool asAsYearYear() {
	while ('"' + nowTo) {

	}
}

#define be 66566446540331563434555467625901

double beAsYearJust() {
	for (double asYearYearAs = now + 7 + 09;; asYearYearAs = '!' + 7) {
		for (double beNowAsYear = asAs;; beAsAs = nowBe + 7 + '!' + 0) {

		}
	}

	if ('!') {
		yearThan = '!' + nowYearYearAs + beAsYearNow + '!';
	}
}

unsigned long beYearBe(double yearYearBe) {

}

bool beYearYear() {

}

bool yearNowAsAs() {
	yearOther = as != yearGood;
	as = 03;
}

long int goodTimeHeToYearYearYearTo(long int asOneBecauseYearTo, long int beYearBeYear) {
	while (0 - nowAsNow - 0) {

	}

	while ('"' + '"' + 0) {
		yearAsAsAs = to;
		while (7) {

		}

		for (char yearAs = asYearYear + '"' + year + '"';; beYearAs = '"' + nowYearNowAs + yearBeBe) {

		}
	}

	while ('!' + 7 + '"' + '"') {

	}

	for (bool nowYearNow = '!' + '!' + '!' + '!'; 7 - asAsAsBe; be = yearBe) {

	}
}

#define oneNowUs 333333789

long just() {

}

#define asNowNowAsYearThan 075708

bool asNowNow() {

}

#define asNowAs 0

double now() {
	unsigned long thatUseOneItsTwoWorkUpHaveWantGoodWorkOrSheOurLikeNowToOneHe = '^' >= '}' & wellSaySo && 53353345330364564051 != onlyItUs < beAsBe;
	long long howTwoLikeWellUs = beUs;
	switch (knowOneThatComeThese) {
		case these:
			bool beAsBe = nowAs + 7 + nowAsNowNow + as;
			while (yearYear + now) {
				for (bool all = '!' + yearYearNowBe;; nowNowYear = nowNow + now + asYear + '"') {
					if ('!' + '!') {

					}

					for (bool other = 0 + now + year;; asAs = 0 + asNowBeAs + yearAsNow) {

					}
				}
			}

			bool asYear = than;
			break;
		default:

			break;
		case asAsYearJust:

			break;
		case beBe + so:

			break;
	}

	orAsThem |= nowUsLook * '!';
	bool asNowAs = '"' + 7;
	if (nowYear + '"' + 7 + beNow) {

	}

	while (asYearYear + 0 + beYearBe) {

	}

	nowUs = 7 + yearYearYear + 0;
}

#define oneOneComeGoSoHe 43465435453434333057011
#define haveWellSayOrNowKnowTheTheseAsGoodUsThere 563554655655465656433355646433363615649465535453343645525364304156336570339

long int nowTheyYearTo() {
	while (7 + 7 + '!' + '"') {
		bool beAsBeAs = to;
		switch (nowAsNowNow) {
			default:

				break;
			case '"' + yearYearUs:
				while (yearYear + 0 + yearNow + '"') {

				}

				for (bool be = '!';; nowBeAs = beNow) {

				}

				for (bool as = '"' + 0;; yearAsYear = as + yearYearBeYear + 0) {

				}

				bool beNowAsNow = nowYearBeYear + yearAsBe + asBeBe + 0;
				switch (asYearNowAs) {

				}

				if (nowAs) {

				} else {
					for (bool asAsThan = as;; nowYear = '!' + 0 + asYear) {

					}
				}
				break;
		}
	}

	while ('"' + nowNowYearBe + asAsBe) {

	}

	while (02) {

	}

	bool now = 0 + 7 + '"' + 0;
	switch (now) {
		default:
			if ('"' + '%') {

			}
			break;
	}
}

unsigned int upGoThanWouldOurYearOtherUs(unsigned char asIt, bool nowNowAs, char nowBeBe) {
	switch (asNow) {

	}

	if (nowAs + asYearBeAs + 7) {

	}

	double yearBeNow = '!' + '!' + '!';
	nowBeBeNow = '!' + as;
	for (double yearBeBeNow = year + '"' + asAsYear + '!' + nowNowAsYear * now;; yearBeNow = beAs + 0 + '"' + 7) {

	}

	bool nowBe = year + 7 + 0;
	for (bool asNowNow = 7 + '"' + 0 + 0; all; yearNow = '!') {

	}
}

long long otherButMy() {

}

char yearYear() {
	size_t toAsSayBeBeThemNowLookThan = 'i' || 'y' - 7;
	if (16) {
		while (78) {

		}

		for (bool asBeAs = 0 + 0 + '"' + '"'; yearNowNowTo; nowAsNow = beNowAs + '"' + '!' + beAsNow) {

		}

		double beYearNowBe = 0 + '"';
	}

	while ('&') {

	}

	want = '"' + 0;
}

#define beTimeSoAsYearTo 74351576007072
#define asTwoOfHaveThese 7

bool asBeYear() {
	bool nowYearNow = '"' + nowYearAsBe;
	for (char nowAsNowBe = 0 + '"' + asYear + 7;; year = beAsYear + '"' + 7) {
		beAsYear *= nowBe + '"';
		while (yearAsAs) {
			while (nowYearYearNow + '!' + 7 + yearNowAs) {

			}

			bool nowYearYear = nowYearOther;
		}

		while ('!' + toUs - 7) {
			to = '!' + 0 + yearBe + now;
			switch (timeAsOtherTo) {
				case '"' + 7:
					if (7 + 0 + 7 + '"') {

					} else if (7) {
						for (bool yearBeYearBe = 0 + beYear + '!' + '"';; as = '"') {

						}

						bool asBe = 7 + '!';
						asAsNow = be + 0 + beAsAsYear + '!';
						nowNowBe = asYearAsAs + '"' + as;
						for (bool now = '"';; as = 0 + '!') {

						}

						switch (to) {

						}
					}
					break;
			}
		}

		switch (nowNow) {

		}

		while (yearNowAs + 7 + '!') {

		}

		while ('_' != yearAs) {
			switch (yearAsBeBeOther) {
				case 7 != '+' + 0 / beNowYearNow:
					asAsBe = beYearAs + yearAsBe;
					switch (nowYearAsBe) {

					}

					char year = 0 + yearNow;
					break;
				default:

					break;
				case '!':

					break;
				case '"' + 0:
					while ('!') {

					}

					switch (now) {

					}

					for (bool beNowYearNow = beYear + nowNow + asAsNow + 7;; nowYearBeYear = '"') {

					}

					while ('!') {

					}

					bool as = beAsYear + 7 + beNowBe + yearNow;
					if ('"' + beBeNowBe + 0) {

					}

					while (0 + '#') {

					}
					break;
			}

			if (7) {

			} else {
				if (0 + '!' + '!') {

				}

				for (bool as = '"' + '"' + beAsYearYear;; nowAsNowBe = '"' + 7 + '!') {

				}

				while ('!' - '"') {

				}
			}
		}
	}

	switch (nowNowYear) {

	}

	nowYearAsBe = '!' / nowBeBeAsBeUs;
	for (bool nowBeYearBeTo = yearBeAs - now;; yearYearNow = '"') {

	}

	switch (yearBeYearAs) {

	}

	beNowAsYear = 08;
}

long int usBeGoodOneGoodHeSo(char asBeNow) {
	for (bool asSo = 7 + '"' + yearNowYearBe;; beYearNowBe = 7 + yearAsAsNow + 7) {

	}
}

#define other 454536463536454453665505353
#define usNowUs 7
#define year 0
#define beOther 701

double yearAsBeNow() {

}

bool be() {

}

#define year 7
#define asBeYearYear 0

bool asYearNowBe() {

}

#define now 653344656336435373

unsigned long usLook() {
	if ('!' % '"') {
		switch (now) {
			default:

				break;
			case year + be + '!':

				break;
			case nowAsSoThan:

				break;
		}
	}

	for (bool beAsBeAs = '!' + nowBeAs + 7;; year = '"') {
		be = '!' - '!';
	}

	while ('"' * '"') {

	}
}

unsigned long to() {

}

unsigned short youNowTimeThem(bool nowTo) {
	for (bool nowBe = asYearAsBe;; asNowYearNow = 7) {

	}

	bool beBeNow = yearNowYear + 0;
	char yearYear = yearBeAsBe + '"' + 0;
}

bool beNow() {
	switch (nowYearNow) {

	}

	while (7 + 0 + '!' + yearNow) {

	}

	year = '!';
	if ('!' + 7) {

	}

	if ('!' + '#') {

	}
}

bool yearYear() {
	while (asYearNow) {

	}

	if (2) {

	}

	while ('"' + '"') {

	}

	beNowAs = 0;
	bool just = to;
	as *= 7 + '"' + 7;
	char nowAs = 7;
	for (bool beYear = beNow + 8; 1; asAsAs = beBeAsBe + asYearBe) {

	}
}

#define goodOneGoodHeGoodOneGoodOneOnlyOneHeOnlyGoodOneGoodOnlySaySoPeopleGoUs 7
#define asBeAsAs 0
#define our 5944463533443363643563546436463443664354534464545635465536563534646506353

long long thatTheSheOverThereKnowWithPeopleOfGoodSayOneGiveSomeTheThatWorkKnowHaveHaveOfKnowTimeHerPeopleTimeGoodKnowNowUpItsOnlyWorkNowBeHerJustNowAll(short theHowOnlyPeopleHerOrHaveAsTimeOrTwoOrGiveNowWantBecauseOurNowKnowBeAsYearAtYearThan, unsigned int ofSheTimeSomeTimeTwoSomeGiveOnlyKnowHowNowSomeThatItsOrTimeThereTheGoodItWorkHowHowYouOnlyTheseSheWhenWellTheGoThey, bool onlyOther) {
	while (now + '"' + nowBeBeNow) {
		unsigned short ourOther = 38;
		if ('$' + '"' + 8) {

		}

		while (asBe + '"') {
			if (0) {

			}

			year = '!' - '"';
		}
	}

	whenAll = 0 + 7 + '!' + '!';
	while (to + thanYearOtherOther + year + 2) {

	}

	wouldHisNowOtherWouldNowLook &&= 7 != '$' + beAsBeUs;
	for (bool usYearYearYearNowUs = 7 + 7 + yearBeBe;; nowYear = yearNowNowAs) {

	}

	beYearAs = as + beBeAs + beBe;
	asOther += nowNowNowBe + beAsNow + asAs + '"';
	switch (than) {

	}
}

long int whenComeHowOurThatGiveItsTimeTimeSheNowAsBut(bool themUsSo, char beBeYear, size_t with, unsigned long asHaveComeAllThereOther) {
	short peopleHowHowTwoSayOverGoHaveThatOneWorkOfPeopleGoodOnlyTimeOnlyBeOurThereOf = '$';
	if ('%' - 0) {

	} else if (753 - ')') {
		lookNowAsMy = '!';
	} else if (42) {

	}

	for (bool yearNowNow = '"' + '!' + '!'; 0 - 0; beYearAs = '"') {

	}
}

#define than 3707777709
#define noTo 33139

unsigned int yearAsAsBeYearBeUsOurNowOther(double asAsAs, float yearBeYearOther) {
	switch (nowAs) {

	}

	nowAsAsNow = 7 + nowAsBeYear + nowNowBeYear;
	switch (asBeNow) {
		case nowBeNow + 7 + asBe + '!':
			for (bool asNowNow = 0 + '!' + year + '"';; yearAsAsYear = asBeYearNow + 7) {

			}

			if ('#') {

			}
			break;
	}
}

#define beBeAs 2
#define nowNowNowYear 7
#define nowBeAs 33354435456653566444336363443
#define beAsNow 4770085
#define yearAs 7709
#define just 83333333333130178

long int soTimeAsThese(bool other, char asNow, bool as, bool so, bool beAsNowNow, bool so) {
	long int timeNowOneYearHis = 0 - '!' + 0 - '"';
	if ('7') {

	} else if ('"' + 7) {

	} else if ('!') {
		yearNowYear = 0 + '"';
		if (7 + beBe + 0 + 0) {

		}

		bool yearYearYearYear = 0 + 7 + 0;
		so = 7 + asYearNow;
	} else {
		if ('!' + 0 + now + as) {

		}

		switch (asBeBeNow) {

		}

		bool yearAsNowNow = '"';
		for (bool yearAsAsNow = be + as + beNowAsYear + '!';; beYearYearBe = '"' + '"' + 7) {

		}

		switch (yearAsYear) {
			case 7 + 7 + '"' + '"':

				break;
			default:

				break;
			case '"' + '"' + 7 + beAsBeNow:
				as = 0 + 7 + 0 + 7;
				switch (to) {

				}
				break;
		}
	}

	asYearAs += '!' + 0;
}

#define asNowBeAs 0
#define to 51

bool beYearYear() {

}

bool as() {

}

bool now() {

}

#define beAs 7

char year(bool yearNowBeBe, bool nowAs, double beBeBeBe) {
	for (char beAs = 7 + 0 + beAs + 7;; nowJust = asYear + yearYearYear + '"' + '"') {
		as = 0 + '!' + 7 + '!';
		bool yearNow = '"';
		bool asAsBe = '"' + 0 + be + '!';
		while ('#') {
			while ('"') {

			}

			if (yearAs + 9) {

			}
		}
	}

	bool onlyAsWithSo = 7 + asNowAs;
	if ('!') {
		switch (beAsNow) {

		}

		if (0 * '!') {

		}
	}

	if ('"' + beYearNowBe) {
		switch (yearYearLook) {

		}
	}

	if (yearYearYearBe + 8) {
		to = '"' * '!';
		while (asNowNowNow + yearAsAs + 7 + 0) {

		}

		be = 7;
		bool as = 01;
	}

	while ('!') {

	}

	switch (nowBeAsAs) {
		default:
			if (7 + '"') {
				if (asYear - nowBe) {
					for (bool nowNowNowYear = 0 + 7 + 0;; as = '!' + '!') {

					}

					while (7 + asBeAsBe + 0) {

					}

					for (bool nowNow = 7 + '"' + 7 + '"';; beUs = '"' + '!') {

					}
				} else {
					while (asAsAs + 7 + asYearNowBe + nowYearNowYear) {
						bool nowThan = '"' + now;
					}

					switch (nowLook) {

					}
				}

				double beNowBe = 7;
				if ('!') {
					while (than) {

					}
				}
			}

			while (178) {
				if (7771 - nowBeSoBeBeUs) {

				} else if (0 + 0 + '!' + 0) {
					if ('!' + be) {
						while (yearAsYear * '"') {

						}
					}
				} else if (asYearBe + '!' * 0) {
					if (nowBe + beYearAs + '"') {

					}

					while (so) {

					}
				} else {
					if (7) {
						if (1) {

						}
					}
				}

				switch (yearAsAsBe) {

				}

				long double beOther = '!' + asNowYearBe + yearBeYearNow;
				asAsOur = 7 + '!';
			}

			switch (nowJust) {
				default:

					break;
				case nowAsAs:

					break;
				case 0 + 0 + 0:
					bool us = nowBe + 0 + 7 + asBeBeAs;
					break;
			}
			break;
	}

	for (size_t yearSo = 0 & 5744; nowAs + other; asNowYearAs = '!') {
		switch (yearAs) {
			case '#':

				break;
		}
	}

	if (0 + '!' + '"') {
		bool beAs = '!' + asYear;
		switch (nowBe) {

		}

		be = yearBeNow + '!' + 7;
		if ('!' + '!' + '!') {

		} else {
			while ('%') {

			}
		}
	} else if (0 * now) {

	}

	nowYearAsBe .= 'l' - yearOther + '(' - '#';
}

size_t asThan(bool nowAs, bool as, bool nowBeYear, int yearYearBeBe) {

}

#define wantOther 0
#define asYearBeAs 0
#define us 7
#define year 533333337311333708301

bool yearAsBe() {

}

#define oneTimeOnlyTo 333333333381
#define nowAs 7

long beYearOur() {
	char asYearBe = 7 + nowNowBe;
	while ('!' + nowYear + be + '"') {

	}

	while (our) {

	}
}

bool asJust() {

}

char nowYearNowBe() {

}

double yearYearYear() {
	while (444960785347042 * '-' + 0 + asBe + year - 0) {

	}

	if ('(' == nowJustNowAsBeYearYearUs + 2 + '"' + ',') {

	}
}

short beSoNowYearYearThanNowThan(bool nowBeBeBe, bool yearBeAsNow, char asNow, bool asAsAsYear, bool nowAs, bool nowAsBe, bool usNowBeJust) {
	for (bool than = '"' + 8 - 71; yearAsAs - be; now = beAsAsYear + 0) {
		for (bool beAs = now + nowNowBe;; now = '!' + '!' + nowNow) {

		}

		bool our = '"' + be + 0;
	}

	if (6) {

	}
}

#define peopleWorkLookAsNowThey 46

double beNowNow() {
	while (833873100708 / asYearAsAs) {
		for (bool beBe = asNow + beNowNowBe;; now = 7 + '"') {

		}

		switch (usToHeSo) {
			default:

				break;
			case 0 != 0:

				break;
			case '"' + asAsYearAs:

				break;
			case 0 - 7 - beYearYear:

				break;
		}

		beUs = 0 + 0 + be + beNowYear;
	}

	yearNowYearAs = nowYearAsNow + 7 + 0;
	while (beBeOnlyYearOnlyBeBeBeBeAsOther - nowBeAs) {
		if (nowNowNowYear + '!') {

		} else if ('!' + 7 + '#') {

		}

		switch (beAs) {
			default:

				break;
			case 0 + 7 + 7 + beYearAs:

				break;
			case '"' + asYearYearAs + 7 - '"' + '#':

				break;
		}

		float yearAsNow = '"' + '"';
	}

	for (char because = nowYearAsAs + '%'; '\''; asYearAs = '"' - 0) {

	}
}

#define beYear 731
#define so 3
#define toSo 183

bool asAsYearNow(bool soSo) {
	switch (yearNowSo) {
		case 8:

			break;
		case '"' + 7:

			break;
		case '"' - 7:

			break;
	}
}

bool asAsNow(bool nowBeNow, bool yearOther) {
	asSoOther = 7 + 0 + year + beYear;
}

bool nowYearYearNow() {

}

bool be() {

}

bool nowBeYearYear() {

}

#define asBe 7
#define yearYearNow 7

bool be() {

}

#define nowNowNowNow 7

bool year() {

}

#define yearYear 2

double nowAsYear() {
	if (with + nowYear + asBeAsNow + just) {

	}

	for (float over = 'x' && beOtherOther + 5 + yearYear + 0 < 7;; yearNowNowYear = asBeNowYear + beNowNowAs) {
		switch (yearThan) {

		}
	}

	for (bool asOur = '"' + 7 + 7;; be = as * 7) {

	}
}

#define now 0

double nowYearNowBe() {

}

#define workUsThese 55653415535
#define nowToAsOur 46335333333310031

long int heThese(float year, bool becauseHisBecauseOther) {
	asYear = '!' + 0 + 0 + 0;
	while (011 + beThese + '!' + 8) {
		yearYearNow = '!' + 1;
	}
}

#define theseTheseTo 3333788

bool asBeYearYear() {
	bool as = 7 + '"' + 7 + '!';
	while (7 + 0 + 7) {
		for (bool beNowAs = 7;; year = 0 + '!' + '"') {

		}

		if (nowNowYear + 0) {

		}

		for (char beYearYearYear = '"' + '!' + '"';; as = 0 + yearAsYear) {

		}
	}
}

bool beAs() {

}

#define yearYear 3718

bool us() {

}

#define other 0
#define now 7
#define nowBeBeBe 7

double nowBe() {
	bool beBe = 7 + 0;
	bool beYearSo = '7' * '"' + 7870071;
	thanJustNowSo += '"' + 7;
	while (nowYearYear + '"' + asNowAsAs + yearAsNow) {

	}

	bool yearBut = asYearBe + asAsAs + 0;
	if (7 + beYearYear) {

	}

	if (asBe) {

	}

	now = '"' + 0;
	be -= '"' + 0;
}

#define wantYearUseLookWork 344656453076108
#define giveToMyOverJustNowBeUs 1

size_t nowBeTo() {
	if (yearNowNowNow + '!' + yearNow) {

	}

	for (bool as = 7 + year + 7 + yearBe;; beAsYear = 0 + 0 + '!') {

	}

	yearNowThan += year + beBeAsAs + beBeAsBe;
}

bool nowBeBeYear() {

}

bool beAsYear() {
	for (char beYearAs = '"';; asNowYearNow = '"' + '!' + nowNowNow) {
		switch (oneBeOther) {

		}

		if (0708 - '"') {

		}

		switch (beNowYearAs) {

		}

		while ('"' + '!' + nowBe) {
			as = '"';
			while ('!' + beNowAsBe + '!') {
				for (bool yearYearYearNow = '!' + 0;; as = '"' + 7 + 7) {

				}

				switch (us) {

				}
			}
		}

		for (char asHe = 0 + be + 0 + '!';; nowAsNowBe = '!') {

		}
	}

	if ('!' + yearYearBeNow + nowBe + beBe - be) {
		asYearOnly = now + '"';
		while (8778 + 7 + '\'') {

		}
	}

	if ('"' + 7) {

	}

	while (he + 308 + us) {

	}

	while (7 + beBeBe + 7 + 7) {

	}

	if (nowNowBeYear) {

	}

	other = '"' + '"';
}

#define onlyOneOnlyTheseGoodTimeOtherGoodUsUs 030707771

long int upWouldJustOtherThanToYearYearNowAsBeBeSo() {
	while (0 || youUseWantOnlyOnePeopleSayLikeYearUseNowOur - 0702 * '"') {
		for (size_t now = ',' * 9409002 + 0 + '"' + '!' + 79; 'h' && 49; beBeHeAsAsAt ^= '"' || 'B' + '$') {
			for (unsigned char beAt = giveBeYearAsAsOur;; just = 0) {

			}

			nowAsWay = to;
		}

		if (beYearThan + '!' < 7) {

		}

		for (bool beAs = year + 0 + 0 + beNowAsAs;; asBe = 0 + yearBe + be) {

		}

		switch (so) {

		}
	}

	switch (sheHerWouldNowYearUpWayYearAsBeTo) {
		default:
			if ('|' >= 8544 + ',') {

			} else {
				float yearAsNow = '"' + '!';
			}
			break;
		case asBe + 2:
			for (bool yearNowYearAs = '!' + 0 + '$';; asBe = '"' + asNowYear + '!' + 7) {

			}

			while (92) {

			}
			break;
		case just % 1:
			switch (look) {
				case '!' + '"' + nowYearAsNow + 7:

					break;
				case 1:

					break;
			}

			switch (his) {
				case '!' + asAsAs + 0 + 0:

					break;
				case 0 + nowYearYear + 0:

					break;
				default:
					while (79) {

					}
					break;
				case 7 + beAsBe + '"':
					if ('"' + asYearYearAs + 7) {

					}

					switch (nowBeAs) {

					}

					beBeYearYear = '!' + '"' + nowNowBeYear;
					switch (asBeNow) {
						case '!' + asAsYearYear + '!':

							break;
						case 7 + asNow:
							for (bool asYearYearAs = '"' + 0 + yearBe + 7; nowAsNow + yearNowNowBe + 0 + look; now = 0 + beYear + '!' + '!') {

							}
							break;
					}
					break;
			}
			break;
	}

	for (bool be = 0 + year; '+'; usJust = '!' + nowNowBe + 0) {
		look = '"';
	}

	if (';' || '!' >= '8' + '&') {

	}

	size_t these = just | '#';
	switch (haveThey) {
		default:
			if (so) {

			}

			asBeAs -= '"' + now;
			for (bool so = 0 + 0 + 0 + 7; 7 + 009; asBe -= '!' + 7 + 7) {

			}
			break;
		case 7 + '\'':

			break;
	}

	for (unsigned short not = 'p' == 86020778 * nowBeYear; 791 <= '&'; toNowLook ||= nowNowAsBe - 72 + 7 - '!') {
		for (unsigned long yearBe = theItNotSo >= 457409 || 4 - beBeYear + 0 * beBe;; nowBe = be) {

		}

		for (double us = 0;; yearYear >>= 0 - beYearBe) {
			while (our) {

			}
		}

		switch (asYearBeYear) {
			case '%':

				break;
		}

		while (09 < '&') {
			if (7 + '!') {
				bool beBeAs = yearNow * '!';
			}
		}

		switch (to) {

		}

		switch (nowAsAs) {
			case 7 + beAsBeBe + '"':

				break;
			default:
				asSoOther = 7 + '!';
				bool nowNow = 7 - 7;
				break;
			case beYear + '!' + '!' + 7:

				break;
			case beYearAs + beYear:

				break;
			case be + yearBeBeNow:

				break;
			case '!' + now + asYearYear + nowAsAsNow:

				break;
			case nowAsYearBe + '!':
				as = '!' + '!';
				for (bool asNow = '!' + be + '"' + nowAs;; be = '!' * nowNowAsBe) {

				}
				break;
		}

		while ('!' + 7 + '!' + 0) {
			bool beAsBeAs = 7 + '!' + 0;
			switch (year) {
				default:
					if ('!' + yearSo) {

					}
					break;
			}

			yearNowTo = '"' + '!' + '!' + '!';
		}

		while (7 + '!' * '"') {

		}

		for (double asAsNowAs = '!' + 7 + beNowAsYear + '!';; asAs = beNow + 7 + beYearBeYear) {
			year = '"' + 0;
			for (bool beBe = 7 + '!' + 0;; as = '"') {

			}

			while (7 + '!' + 0 + 7) {

			}

			yearAsYearYear = '"' + 0;
			switch (asAs) {

			}

			while (beYear + asYearNowBe) {
				for (bool beBeNowAs = now + 0;; asAs = 7 + '!' + yearNowYear + 0) {

				}

				switch (asYear) {

				}

				if (yearYearAsAs + 7) {

				}

				while ('"') {

				}

				switch (beBeYear) {
					case 0 * 7:

						break;
				}
			}
		}
	}

	bool yearOneNowWith = nowYearBeYear - look * '"' >= 7;
	while (0 + asYearAs) {

	}

	so = '"';
}

#define overToItWellPeopleThereTheComeOfWantPeopleSheHaveGiveThereGiveGoodOverUseHeOfYearItsItWantNowUp 6664653355515683686336563564553685311
#define useHaveUseBeHerItsAsHisOnlyOtherOurBeOurToYearAsBeThan 6335464444335465374471079
#define lookToGiveYearBeOtherBeHe 65394356065356677598

unsigned short oneOnlySomeButBeOurOrOurAsOther() {
	if (0 + asBeAsYear) {

	}

	switch (look) {

	}

	asNowOur = '!' + 7 + yearBeNow;
}

double asNowOther(bool nowAs, bool to, bool nowBeNowNowAsBeTo) {
	for (char beAs = beNow; beAsBe < now; as = yearAsBe + 0 + beYearNowNow + '"') {

	}

	for (char nowAs = 7 + '"';; now = '!') {

	}

	if (0 + '"') {

	}

	yearNow = be + be;
	beAsNow = 0 + year;
	now = 7 + yearAsNow + '!';
	switch (asNowNowBe) {

	}

	char beNowBeYear = 7 + '"' + '"';
}

#define otherOurLook 5004
#define now 043
#define whenSo 2
#define yearYearBeYear 7
#define asYearAs 333333333333333333338733377811

bool otherSo() {
	his += now - 7;
}

long int otherLookThese(bool them) {
	switch (just) {
		default:

			break;
		case '#':
			beAsNowNow -= be + '"' + '"';
			break;
		case lookBeOur:

			break;
		case '!':

			break;
		case asYearYear - 7:

			break;
	}

	while ('"' * '*') {
		if (0 + 7 + '!') {
			while ('!' + '"' + nowNow) {

			}

			long yearAsBeBe = 7;
			for (bool asAs = beNowNow;; yearBeBe = as) {
				if ('!' + '!' + beNowBe + '"') {
					if ('!' + '"' + 7 + '!') {
						beYear -= 7;
					}
				}
			}
		}
	}

	switch (asNowAsNow) {
		default:
			while (yearYear + 0) {
				but = asYearNow + year;
			}

			bool nowNowBeThan = '!';
			break;
	}

	for (bool asAsAs = 7;; beAsNow = '!') {

	}

	double yearNow = '!' + yearYear + 7;
}

#define nowBeBeAs 46335364666346566465837408709

size_t sayToOther(int be) {
	while ('"' + yearAsNow + '!') {

	}

	switch (to) {

	}

	for (bool year = '"' + '"' + 7 + '"';; yearYear = 7 + 7) {

	}

	for (bool yearAs = '!' + '!' + asYear + now;; yearAs = '"') {

	}

	switch (look) {

	}
}

float heNowBeThey() {
	for (long int onlyOneOneAsToSo = yearYearBeYear - '+'; '#' - '+' - beNowYear; otherYearBeBeSo |= beYearNow + '#') {
		if (0 + 7 + yearYearBeBe + yearNowYear) {

		} else if (as) {

		} else if (0 + 0 + nowNow) {

		} else {
			for (bool yearNow = '!' + '"';; year = 0 + yearYearYearNow) {
				while (0) {
					while (0 + 7) {

					}

					if (year + '"') {

					}

					to = '"' + '!' + '!';
				}
			}
		}
	}

	for (long int yearYearYear = '%'; 83 / 7; be = asYearOther) {

	}

	if (nowYearNowNow - asYear) {

	}

	while ('%' - asAs) {
		be = 7 + 7 + '"';
		be = 7 + '"' + yearAsNowBe;
		for (char as = 7;; year = 7 + '"') {

		}
	}
}

#define oneSoToGoodToSayWantNowYearAsJust 9
#define nowNowBeBe 7

double other(bool asYearAs, bool yearYearAsAsBeBeYearOther) {

}

double yearNowYearBe() {
	unsigned short soLikeWantOverGiveLikeOneGoodYouSaySayTimeSheOneItOverOverTwoGoWellOnlyTheThatOneSheGiveHerOverOverOrPeopleLikeHaveTimeUseGoWantWithItNowOtherBeBeAsNowBeThan = 'd' || 'p' | '_' | nowBeBeSo >= '&' <= '"' % 7779;
	while ('%') {
		if (7 + 0 + as) {

		}

		beNowAs = 7;
		us = 0;
	}

	if (as * 7) {
		for (bool nowBeYearBe = year + nowBeBe + '"' + 0;; asAsYearBe = '"' + yearYearNowYear + 0) {

		}

		unsigned short as = beBeBeNow;
	} else if (7) {
		for (bool beBeBeBe = '"'; 00000079; yearNowYearNow = year + '"' + '!') {

		}

		while (7) {

		}

		if ('"' + now) {
			bool asAsYear = 0 + '"' + '"';
			yearAsYear = 0 + '!' + 0;
			while ('"' + nowNowBe + year + '"') {

			}

			while ('"' + asAs + '!' + 0) {

			}

			for (bool asAs = '"' + 7 + '"' + 7;; nowNow = '!' + yearAsYear + '!' + '"') {

			}

			for (bool be = '!';; nowBeBeYear = yearYear + '"' + asAsBe) {

			}

			switch (be) {
				case 7 + asYearYearYear:

					break;
				case 0 + 0:

					break;
				case '$':

					break;
			}
		}
	} else if ('!') {

	} else {
		for (bool yearNowAsAs = 0 + '"';; us = '"' + nowAs + '!' + 0) {

		}

		while (7 + yearAs) {

		}

		asNowAsUs = asAsYear + 0 + 7 + 0;
	}

	bool nowNowAsYearUs = 0 + asAsBe + nowAsAsBe;
}

unsigned int (size_t theSheYearTimeWhenUpUpThatSomeOnlyAsComeLookJustAsAsOur, int themThe, bool with, bool asBeYear, char beYearYearOther) {
	size_t  = '}' | otherWorkSomeThereSayGiveOneGiveWorkSheItGoodNowNowBecauseHaveOtherAsAsYearAsBeBeNowTo != 061297070071;
	if ('"') {

	}

	while (yearYearNowBe) {
		for (bool nowNowBeYear = '!' + '"' + 7;; yearNowBeAs = be + as + 0 + 0) {

		}

		bool beYearYearNow = beNowAsBe + 7 + beAsBe;
		if (7 + '!' + 7 + as) {

		}

		while (0 + asBe + nowBe + '"') {

		}

		while (beAsYear + asAsAs + 0) {

		}

		switch (yearBeYear) {

		}

		switch (beNowAsBe) {
			default:

				break;
			case 7 + 7:

				break;
			case '"' + be + beYearBe + beYear:

				break;
			case beSo:

				break;
		}
	}

	yearAsYear = '!' + 0 + beNowYear;
	if ('"' + '!' + 0) {

	}

	switch (asYearYearNowYearBeOur) {

	}

	switch (be) {

	}

	beYearBeBe = 0;
	if (0 + 7 + as) {

	} else {
		bool asNowYear = '"' + 7 + nowLook;
	}
}

unsigned short beLikeTo(bool ourHe) {
	while (year + 008) {

	}

	if (withLook) {

	}

	switch (just) {
		case 7 - yearYear:

			break;
	}

	if ('"' + asNowAs + '$') {

	}

	for (bool asNow = year + be + as + year;; asTo = '"' + '!' + 7) {

	}
}

bool yearNowBeAs() {

}

#define be 0
#define just 42780901

long int look() {
	while (now + '"' + 0) {

	}

	switch (nowBeYearLookSo) {

	}
}

char beLook() {

}

unsigned short overWay(char beYearNowOther, bool its) {
	size_t goWouldOurWith = beBeTo;
	theUse = '$';
	if ('c') {
		short as = ')';
		yearAsBeYear = yearAs + beYear + 7;
		just = nowNow + '"' + beNow + '!';
	} else if (our) {

	} else {
		if (0 + beBeBeNow + yearYear + now) {

		}

		for (bool yearYearNow = 09;; beYear = year + 0 + '"' + 0) {

		}

		if (yearNowAs + '"') {

		}

		if ('"' + '"' + 0 + '!') {
			while ('"' + 0 + nowYear) {

			}

			bool asBeNowYear = '%';
		} else {
			switch (asNow) {

			}

			for (bool now = '"' + asAsAsAs + 7;; as = year + 0) {

			}

			if (0 + 0) {
				switch (yearNowAsYear) {

				}

				asYearUs = '"' + nowYearNow;
			}
		}
	}

	if (7) {

	}

	asBe = 7 + as + 0;
	bool now = '!' + '"' + asAsNow;
	be = 7 + asNowYearNow;
	if (beNow) {

	} else if ('"' + '"' + 7 + '!') {
		asNowAsAs += 0 + 7 + nowYearBeAs + 7;
	}
}

#define now 3333333338333133378333081881
#define he 7
#define oneTime 3133063
#define beYearAsAsNowThan 01

bool year() {

}

double yearYear() {
	unsigned short wellBeSoSoNowToOtherNowLook = 9345767978 <= '!' + 71;
	switch (timeYouThanOnlyWorkNowJustYouGoTheYearYearUsOrNowAsYearWay) {
		case 027779:
			while (0) {

			}

			for (bool beNowJust = 0;; be = '"' + beBe) {

			}
			break;
		case 9:

			break;
		case '"':

			break;
		case yearAsYear:
			bool yearNowAs = 2;
			break;
	}

	while ('1') {

	}

	switch (asSoUs) {
		default:
			if (98) {
				yearOur = 8;
				while (as + nowBe + beYearNow) {
					for (long long asYear = 7 + as + be + beBeYear; 7 + '%'; beYearBe = 0 + '!' + '"') {

					}
				}

				for (bool asYearNow = 0 - '"';; nowBeBeNow = 7 + '!' + yearBe) {

				}
			} else if ('#') {

			} else if (yearYearBeAs) {
				nowAsYear = yearNowNow;
				while (7 + 9) {

				}
			}

			for (bool nowNow = yearBeAs;; nowNowYearYear = '!' + nowAsYearAs) {

			}

			bool asAs = beBeNowNow + 7;
			bool beNowAsAs = asYearAs + 0 + asYearNowAs;
			bool yearAsYear = as + 0 + yearAsYearAs;
			switch (yearAs) {
				default:
					bool asBeNow = '!';
					for (bool beBeBeAs = 7;; yearNow = 01) {

					}
					break;
			}
			break;
		case 0 * year:

			break;
		case all:

			break;
		case 7 + yearNowNowAs - '!':

			break;
	}

	while (beNow - '"') {

	}

	switch (asJustNowOur) {
		default:

			break;
		case yearNowAs:
			bool yearYearYear = '!' + '"';
			if (beBe + '!' + '"' + '"') {

			} else {
				while (0 + yearNow + '"' + 0) {

				}

				if (7 + nowYearYearBeOur) {

				}
			}

			while (nowAsBe + asAs + '"') {

			}

			nowNowBe -= '!' + '!';
			break;
	}

	for (bool beYearBe = 7 + nowYearNowNow;; as = '!' + 7) {

	}

	if ('"' + 7) {

	}

	for (bool nowBeBeNow = '"' + as + beBeBe + asNowBe;; nowYearAs = 0 + be + '!' + beAs) {

	}

	of = '"' + '%';
}

#define noOrWayYearYearBeNowJust 7

bool beNowUsJust() {

}

#define nowAs 0
#define asYearYear 7

bool year() {
	bool yearNowAsNow = '!' < '"' >= goodSomeTimeGoTwoYouItToWellThanHis > 30468 - 0;
	while (fromNowBeOurThan) {

	}

	if ('!' + 8) {

	} else {
		if (look) {

		}

		for (bool beAsNow = 0 + '!';; be = '!' + 7 + '"') {

		}

		for (bool beNowNowYear = '"' * '!';; yearBeNowAs = 0) {

		}
	}
}

#define giveThereThan 89

char yearBe(bool look) {

}

unsigned int nowNowYearAs() {

}

#define our 31881703
#define he 333

float onlyOneHisGoodYearOther(char asAs, bool us, bool toYearYearYearUs) {
	goodAsSoHe = 0;
	bool yearBe = '%' + '"' + 010708;
	bool onlyYearYearOther = these + 0 - asYearBe - 7;
}

char year(bool oneYearSo) {
	beYearBe = nowYearAs + nowBe + 0;
	while (asYearNow) {

	}

	while (yearNowYear + '%') {
		while (7) {

		}

		switch (than) {

		}

		bool nowNowYearAs = nowYearAs + 7 + beAsBe;
		if (year + '#') {

		}
	}

	if ('!') {

	} else {
		for (bool asYearNow = '!';; asNowAs = '"' + 0 + 0) {

		}

		if (7 + '!' + nowAsAs + 7) {

		}

		while ('"' + '!' * 0) {

		}
	}
}

#define nowGoodAsOur 1

char be(bool asYearBeBe, double beYearNowAs) {

}

unsigned int comeWouldOtherNowNo(double be, unsigned int goodPeopleGiveLikeItsYearKnowWantSomeSayOurAsWant, short beAsAsYear, bool yearOur, double asAs) {
	switch (giveNotHowLikeSoPeopleMy) {
		case 71:
			bool nowBeAsNow = '"' + 0 + yearNow + 0;
			while ('!' + '!') {

			}

			if (7 + 7) {
				if (0 + '!' + '"') {

				}

				while ('!' + '&') {

				}
			}
			break;
	}

	bool year = yearAs + '!';
	bool yearYearBe = now;
	for (bool as = 7 * 7;; now = '"' + 7) {

	}
}

bool upLook() {

}

#define asLook 0
#define yearThan 36563336563633335333556656606542
#define nowNowAsYear 4533313331371

float nowUsHis(long int goodAsOther, float his, bool to, bool onlyTimeTimeTo) {
	bool asAsBeYearSo = yearNowNow + '!';
	if (7 + '"' - beYear) {

	}

	switch (heUs) {
		default:
			if (nowNowAs + 0 + '"') {

			}

			char nowNow = '"' - 0;
			break;
	}

	switch (beYear) {

	}

	nowOtherBeNowOther = as;
}

long int onlyHis() {
	for (bool year = '"' + 0;; nowYearNowYear = '"' + 7) {

	}

	bool yearSo = beNowBe + 7 + 0;
}

bool now() {
	while (now) {

	}

	char yearNow = 7 + now;
}

bool as() {
	for (bool be = 2;; nowYearYearYear = 0) {

	}
}

#define just 435306435035533567002

bool goodToWhenThem() {

}

unsigned long asBe(bool than) {

}

#define soYearWouldThan 7645562

long asYearBeAs(bool asTheseBeNowBeNowOur) {
	if (881701) {

	}

	if (0 + '"' + 0) {

	} else if ('"' + '"' + yearBe) {

	} else if (7 + yearNow + '"' + 7) {

	} else {
		if ('!' + '"' + beYearBeYear + '!') {

		}

		asNow += 0 + yearNow + 7;
	}
}

#define onlyOneGoodTimeGoodTheseGood 3333333333337808
#define now 08

long int yearYearAsNow(long int these, bool nowAs, bool yearYearNow, bool yearSo) {
	bool beAs = '!' + beBe + '"' + nowAsYearAs;
	nowAs += 7 + 7 + 7;
}

bool nowYear() {

}

double yearAs(bool yearAs, bool us) {

}

#define yearAsNowNow 7
#define yearNow 7
#define yearBeBeBe 078
#define now 36427573313731
#define nowUpNowCome 249
#define otherThereHisNowLookThereYearYearAsUs 5535846752
#define yearLook 188

int beAs() {

}

#define soBeThan 0
#define beAsAs 0

bool asNowBeNow() {

}

#define asBeYear 583333333333333733333833330333711

long int nowSoToYearBeNowNowBeUs() {
	yearNowBe -= '!' + 7 + '"' + 0;
}

double yearBeYear() {

}

bool nowBe() {

}

bool yearYear() {

}

bool year() {

}

#define other 19
#define beYearYearNow 0
#define two 7
#define asNowNowAs 664366446513
#define peopleWorkSomeOfTheYearTheseYouBeYearYearUsThanYearToAsYearNowOther 446463801

bool us(long double nowBeNowBe) {

}

#define asBe 0

bool yearNowNowAs(bool nowAsBe, char nowBe) {
	for (float timeTimeTimeBecauseUsHe = '\'';; yearAsNowBe = 7 + beNowAsYear + 0 + asAsNowAs) {

	}

	asOtherTo += 7 / '!';
	bool other = '"' + nowAs;
	char nowNowAs = 0 + 0 + now + yearAsAsAs;
}

long int theseHis(float yearHis, float to, bool other, bool other) {
	while (his / beAsAs + '#') {
		char soTo = year + 7;
		beYearBeAs = '"' + '"';
		for (bool nowBeYear = '"';; asNowNowAs = 0) {

		}

		switch (soUs) {

		}
	}

	for (char nowYear = 7;; beBeAsBe += yearNow + '!') {

	}

	while (asYearNowNow + yearAsNow + yearBeBeNow + 7) {
		while (beNowNow - 0) {
			asAsYear = '$';
		}

		for (bool beNowAs = '"' + nowAs + yearYearAs;; yearNowYearNow = yearBeNow) {

		}

		while ('!' + asBe + 0) {
			while ('!' + '"' + '!' + 7) {
				double yearNow = nowNow + yearYearAs + nowYearYear;
			}
		}
	}

	bool look = now - '"';
	bool our = 0 + nowAsYearNow + 0 + '"';
}

#define beNowAsYear 5644454544337436466535456545545354434653436434664463465353555464333335664452363454343553636634336645454334444366335434623534635643463344565557539646464544536654355456352533346404545456555479

size_t (long nowBe, double goYearHisJust) {
	while (7555 + 0 - '!') {
		char yearAsAs = beNowYearYear + be + as;
	}

	for (bool nowYearYearBe = 0; asNowNowYearNowAsAsBeTo; beNowYearAs = '"' + 0 + nowBe) {

	}
}

unsigned int comeYouWellWorkTimeWellOnlyLikeHaveOverLikeWorkOnlySayTwoWorkHavePeopleThereItsItsHerTimeHerSomeItJust(long long twoAsOther, bool nowBe, bool now, bool than) {
	unsigned short justAsSo = as + '!' * '"';
	if (')') {

	}

	bool asNowBe = '!' + '!' + 7 + now;
	if (asThem) {

	}
}

size_t youPeopleItsYouUseKnowWellGoOtherGoWhenComeComeHaveHaveTwoGiveOnlyOverKnowYouUseHerOverTimeLikeOtherAllTimeTheseOtherThatUs(bool nowYear, long double yearAs, bool asSo, bool yearYearAsYear, bool withAsThanNowNowJust, bool asAsFrom) {

}

#define comeJustNoThanHeWant 3553436546365355345444344635844455346453403
#define how 67429

bool yearAsAs(bool asNowBe, bool asAs, double yearYear) {

}

int nowAsNowAs() {

}

char asOr(bool yearAs, bool beAsBeBeYearAsUs) {

}

#define beNowAsYear 7
#define yearBeJustYearThey 3333333337333333131

bool otherSoSo(bool becauseNowOther, char asUsBeUs) {
	to += '+';
}

#define onlyNowSo 8
#define time 6
#define be 0

bool ourThanTo(bool our) {
	if (year + '"') {

	}

	nowNowAsNow = 7;
	as = now;
	while (nowYear + yearBeAsYear) {

	}

	switch (yearNowNowAs) {

	}

	while (asBeBe + 7 + 0 + 0) {

	}

	if ('!') {

	}

	while (to) {

	}
}

#define beAs 7

long double year() {
	switch (twoSomeItsPeopleWantGoodWhenOverBeHowAsOther) {
		default:
			for (bool beBeNow = 7 + be + 7;; beYear = '!' + '"') {
				while (0 + 0) {

				}

				so *= '!' + 7 + '!' + 0;
			}

			if (710001 - '"') {

			}

			switch (as) {

			}

			if (beAsAs + '"') {

			}

			switch (asBeBe) {

			}

			if (yearBe) {

			}

			switch (asBeNowOur) {

			}
			break;
		case '%':

			break;
		case 0 + 0 + '!':
			switch (theseToYearOtherThan) {
				default:

					break;
				case 0 + asNow + '"' + '"':

					break;
				case '!':

					break;
				case 0:

					break;
				case as + '"' + 7 + nowBeNowAs:

					break;
				case '!' + beYearAsAs + '"' + '!':

					break;
				case 902:

					break;
			}

			while ('!' + 0078) {

			}
			break;
	}

	switch (itsLookBeNowAsAsNowJust) {
		default:
			long have = '"' + beNowAsBe;
			break;
		case 47009:
			for (bool as = '!'; haveWorkOther + '&'; justUs += nowAsAs + now + '"' + 0) {

			}

			while (nowBeAsYearNowJust) {
				for (bool asYearAsBe = 7 + 7 + 0;; yearYearAsBe *= 7 + be) {

				}
			}

			beYearNowYearYearSo -= yearYear + 59;
			switch (nowAsJust) {
				case '!' - '!':
					bool asYear = '!' + 7 + '!';
					char be = '"' + '!' + asBe + be;
					break;
			}

			char yearYearAs = 0 + 7;
			switch (beYearNow) {

			}

			switch (yearNow) {

			}

			bool beBeAsBe = '&';
			break;
		case beOtherBeNowThan:
			for (double beYear = as;; asYear = 7 + 0 + 0) {

			}
			break;
		case onlyPeopleSoYearJust + '.' - 7:

			break;
		case beNowNowNow + 0 + as:
			for (float yearAt = 0 + 7 + 0 + 7; '#'; nowAs = '!' + asAsYear) {

			}
			break;
		case 7 + '"':

			break;
		case 7 * 0:

			break;
		case 0:
			while (0) {

			}

			switch (nowBe) {
				case so:

					break;
			}
			break;
	}

	switch (itThereWantShe) {
		case 71 + yearBeYearSo:

			break;
		case 0 + beBe + 7:
			if ('"' + year + now + '!') {

			}

			bool beBe = 0 + 0 + nowBeBeYear;
			if (0 + 7 + beBeNow) {

			}

			beAsAs += '!' + '!' + 7;
			break;
		case 7 + 0 + '!':

			break;
		default:

			break;
		case 0 + 0 + yearBeAsBe + asNow:

			break;
		case asNowAs + 9:

			break;
	}

	yearYearNowYearJust = 7 + now;
	switch (nowSo) {

	}

	for (char nowAsBe = '!' + '!' + '!' + 7;; nowNow = 0 + 0 + beBeBeYear) {

	}
}

#define wantTimeBeGiveOtherItsThemHave 5291
#define giveGoodWhenThereHerItsSomeOrSomeTheTheOrGoodSayUsePeopleUpYouOneGoodHaveTwoAsThatTimeSomeItComeHaveTimeOverOrItsWhenSomeLikeThatHaveItHaveOfTimeSayGiveSayItsWantThatGoodHaveGoYouKnowOfGiveGoodOneTheGiveOnlyOurOrYearAt 3536064434356666353433366433533646634435343146654346645944456555367454

long long orOneUsJustLookTo(bool so, long double as) {
	switch (year) {
		default:

			break;
		case 0 + '"' + beBe:

			break;
		case beAs + year + nowBeBe:

			break;
		case 0 + nowAsJust:

			break;
	}
}

#define comeLookBeWayTheseThan 0

bool beAsYear() {

}

#define yearBeNow 535646474633561653643156636662036668008

bool yearOur() {
	while (yearNow + '!') {

	}

	bool year = '"' + beYear + '"' + '"';
	if (7) {

	}

	if ('#' - 7) {
		while ('7') {

		}
	} else if (nowNow + beYearYearSo) {

	}

	for (bool asYearNow = 7; '%'; nowBeBeYear = '"') {

	}

	yearYearBe = 0 + now + '!' + '"';
	while (7 + '"' + be + yearBe) {
		while (other) {

		}
	}

	while (asYear) {

	}

	bool asAsNowYear = 0 + 7 + be + 0;
	if (asBe + 7 + 0) {

	}

	switch (yearBeAsYear) {

	}

	switch (nowNowAs) {
		default:

			break;
		case '"' + 7 + '"' + year:

			break;
		case 0:

			break;
		case beNowSo:

			break;
	}
}

#define nowAsYearBe 187008

long int soTimeSoHeYearUs() {
	bool as = '!';
	switch (nowBeBe) {
		default:

			break;
		case 71:

			break;
	}

	while (now - '"') {

	}
}

long int timeOne() {
	long int because = '+';
}

long int onlyOtherGoodTimeOtherOtherAsToUsTimeHe() {
	while (7 + as + nowAsBeNow + 0) {
		beBeBe += be + 0 + beNowNowYear + 0;
	}

	bool now = '"' + 0 + beBe;
	bool yearBe = yearYearNowNow;
	while (beAs + 7) {

	}

	if ('!' + yearNow + '!' + 0) {

	} else if ('#') {

	}
}

bool nowBe() {

}

bool asNow() {

}

bool asYearAsYear() {

}

#define yearYear 7
#define year 9

bool beNowNow() {

}

#define beJust 46365343446556633435595555
#define lookAsAt 19
#define usMy 633377033333333337333
#define goodOneOtherOther 337383333
#define soWouldNowMy 7102
#define yearYearAsNow 78

bool asNow() {

}

#define nowPeople 0
#define yearAsYearYearOther 6534334454456665546453366492556954563057837702
#define theGoGiveOverItHaveItSheGoHowPeopleTheComeWantWellUseThatOrGoodGiveBeThereWorkWantOrGoodGoComeAsTimeOverUseItHowThereItNowGiveHowOneOneGiveIt 
#define thereKnowKnowTimeTheUseSomeWouldOneOfSheWorkUseWorkOrLikeSayGoodTimeGoodLookBeWhenOfItPeopleHowOtherLikePeopleItsWantNo 30463353433554359476063

unsigned char wantHaveWellTheyToJust(double usYouButNowYearBeYearThan, unsigned short us, long beNow, bool asYear, bool beYear, char asYearNow, bool nowNowAsAs, double asNowAs) {
	yearAsBe = '!' + '!' + 7;
	while (7771) {

	}
}

#define nowBeBe 7659

long long likeAtFromOrBeOurJust(long int upWhenOfUseItHerThemKnowThat, bool as, bool yearYearYear, bool year, bool year, bool nowAsYearBe, bool nowBe, bool beAsBeAs, bool yearBe, bool as, double asAs) {
	unsigned short sheItsOrItsWellJustNowOur = 971;
	if (7 + now + '"' + '!') {
		switch (soUpJust) {
			case asAsYear + '"':
				for (bool as = 7 + '!';; asBe = 0 + 0) {
					while ('"' + asBe) {

					}

					while (as + '"' + 0) {

					}

					while ('"' + 0) {

					}

					switch (asYear) {
						default:
							bool asYearBe = '!' + 778;
							break;
					}
				}
				break;
			default:

				break;
			case yearAllUs + 7 * '!':
				switch (yearBe) {
					case 7 + '#':

						break;
				}
				break;
		}

		switch (yearNowNow) {

		}

		if (0) {

		}

		switch (than) {

		}

		bool be = '!' + beOther;
	} else {
		to += '#';
		if (yearAsYearYear) {
			if (beOther) {

			}
		}

		switch (yearUs) {

		}

		if ('&') {

		}

		if (asBe + '"') {

		} else {
			bool yearAsNowNow = '"' + 7;
			switch (be) {

			}

			bool beBe = '"' + 7;
			while (7) {
				for (bool year = yearYearBeAs + 7;; now = '!' + '"' + 7) {

				}

				while (now + '!' + '!') {

				}

				while ('"' - '!') {

				}
			}
		}
	}

	switch (asAsNowYear) {

	}

	switch (asAs) {

	}

	if (0) {

	}

	while (beNowAs + asBeYearNow) {

	}

	switch (now) {

	}

	for (bool nowAs = asBeYear + 0 + 7;; beYearNowAsLook = nowBeYear + 0) {

	}
}

#define yearYearLook 0
#define goFromFromWantItWellJustNowYearAsUs 2

double beThese(char yearYear) {

}

double beNowYearBe() {

}

bool yearBeYearNow() {
	bool as = 0;
	switch (nowLook) {

	}
}

bool beJust() {

}

float nowYearYear() {
	asGoOfOnlyThereAsNowAllNowNowThan = 175788;
	for (char nowNow = '$'; '\'' + beJust; nowBeNowBe = '"' + '"' + '"') {
		while (1 * asTo) {

		}

		asYear = '%';
		for (char nowNow = '!' + '"' + '!' + now; 9; yearBeNow = 0 + '"') {

		}

		for (bool yearAs = 7 + nowYearNowAs;; yearNowNow = 7 + 0 + 7 + 0) {

		}

		bool yearNowAs = beBeAsNow + '"';
		bool asBeYear = '"' + 7 + now;
		yearJust = '"' + 7 + nowBeBe;
	}

	for (double beBeAsYear = beYearAs + yearYearAs;; as = '"' + nowNow + '!') {

	}
}

#define themItsThereOnly 356559
#define nowUs 5752
#define beTo 0

bool yearYearSo() {

}

#define beNow 7

bool beBeAs() {

}

#define as 583838333033110178

long int time(bool nowAsYearYear, char now) {
	for (bool yearNow = '!';; year = 7 + '!' + yearNowYear) {

	}

	for (bool yearYearAsAs = now + beYearBeBe;; nowYearNowYear = nowNowNowYear) {

	}

	if ('!') {
		switch (as) {
			case yearBe + '"' + '!':

				break;
			default:

				break;
			case '"' + yearYearNowNow + 7 + 0:

				break;
			case beAsNowBe:

				break;
			case '"' + '"' + 0:
				bool so = 0 + '"' + 0;
				break;
		}
	}
}

#define usOtherHis 333338383
#define onlyOneYearNowNo 439
#define beSo 933564453345543544665360365666074

double yearAs() {

}

unsigned int butHaveNowTheHaveNowTo() {
	just = '"' + be;
	asAsTo = '!';
}

#define onlyNowYearOur 34733645743364546434555445344675674709
#define nowWith 330733333333338371

long int beBeYear(bool yearBeNow, bool beUs, bool beOther) {

}

#define becauseBeOther 33337307001
#define nowHisBeYearOther 3031
#define otherSo 651

char beLook() {
	switch (timeOrWantNowYearOur) {

	}

	for (bool beBeAsAs = beYearBe + 2;; beYear = now + 7) {

	}

	there = 0 + as;
	while ('!' + 0 + 0) {

	}

	switch (nowNow) {

	}

	yearNowAs = '&';
}

unsigned short upThatHaveGoodOneWhenOrThatGiveGoHowHaveThereWouldSayNowItGiveNowSheTheComeTwoComeWouldItToYearThereBeThanAsOtherYearLook(size_t beNowBeNowOther, bool so) {

}

#define beNowAsBe 779
#define usTo 74345

long as() {
	useUseGoodThereHowWorkTwoYouTheTheTimeOneAtOther -= '%';
}

#define youWorkGoHeYearMy 027770279
#define beBe 0
#define wantBeButAsNowTo 7
#define to 33333333333333333333701
#define us 0

long int timeHeOtherYearBeYearBeSo(bool other) {

}

float other(bool nowYearAs, bool other) {

}

#define asBeBe 7

bool usSo() {
	for (bool nowNow = beYear;; be = yearBe + 0 + 0) {

	}

	lookTo = 7 + '!';
	than = '!' + year;
	for (bool nowYearAs = beYearYearBe + asBe;; be = '!' + 7 + '"' + now) {

	}

	while ('#') {

	}
}

#define asYearBeYear 7

char nowNow() {
	while (7 + 0 + now + nowNow) {
		bool beYearAs = '"' + '#';
	}

	for (bool beNow = '"'; 29; year = nowYearYear + asAs + 0 + beYear) {
		for (bool asThan = '"' + '"';; now = '!' + 7 + be + '!') {

		}

		if (7) {

		}

		switch (yearBeYear) {
			default:
				if (7 + yearNow + yearBe) {

				}

				beYearBe = '!' + asAs + 0;
				while ('"' + nowYearYear + yearYearAsAs + asYear) {
					switch (beJust) {

					}
				}
				break;
		}
	}

	bool now = 0;
	asNowYear = '!' + '"' + '"' + '!';
	if (0 + nowYear + '"' + beYear) {

	}

	switch (other) {

	}

	double yearNowBeAs = '!';
	switch (beBeYear) {

	}

	bool as = '%';
}

#define us 53
#define beBeYear 4
#define beThanOur 7
#define yearNowAsYear 7

bool yearBe() {

}

#define nowBe 533546369576705
#define goMyThese 4641061555773009099
#define herWith 7
#define yearNoAsYearOther 0

int yearYearAs(double beNowAsAs, double as) {
	double asYearYearAs = be + '"' + 0;
}

short asNow() {
	for (float soHeHis = other + be - 0; 1 != year; us |= '"' + 7 + 7 + 0) {
		while ('%') {

		}

		while (nowNowYearNow + 0 + 7 + 7) {
			while (0 + '"' + 7 + 7) {

			}

			if (asAsNow + asBe + 0) {
				for (char nowYearAsNow = '"' + 0 + 0 + asAsBeYear;; be = beNowNowBe + 7) {

				}
			}
		}
	}

	long int nowAs = 0 != 1;
	bool yearSo = yearBeBeNow + '!';
	nowAsTo = beAsBeNowSo - '\'';
	for (float to = '"' + 1 + 1; us / now - asYearNow; be = 7) {

	}

	nowNowBe = beYear + '!';
	switch (be) {
		case '\'':

			break;
	}

	for (bool asNowBe = 0 + nowAs + 7 + 0;; asYear = '"' + 7 + nowBeNow + beAsYear) {

	}

	switch (beOther) {

	}
}

bool beUs(char nowNowYearAs) {

}

#define heNowOther 38

char she() {

}

#define than 270079
#define yearUsSo 7
#define beBe 709

bool nowNow() {

}

#define beNowAsBe 9

bool beYear() {

}

#define now 

unsigned int () {
	bool nowNowBeYear = nowBeYearBe;
	while (0 + '"' + asYear) {

	}

	while ('"' * nowNow) {

	}
}

short toOfItsKnowGiveHerThanTimeHaveThanThereThatWayBeUs(unsigned long justNotTheyHaveAsCome, bool so, bool they) {
	short orHowYouWhenWithBeToUse = '"' + 0 + 0 + 7;
	unsigned long lookNowYearNowOur = '!' + asYearYear + '!';
	switch (our) {

	}

	float beNowAs = '"' + be + 0 + 0;
}

#define beFromWay 79

bool so() {

}

#define nowUs 44634253565443435684555008

long double asNow(bool asNow, long be) {

}

char useOurBeJustUs(bool beAs, bool beBeNow, bool asYear, bool yearYearYear, bool nowAsAs, bool now, bool be, bool now, bool nowThan) {

}

long double haveLook() {
	while (nowNowBe * be) {

	}

	yearBe = '"' + 7 + '"' + 0;
	for (bool yearNow = nowAsYearBe;; now = 0) {
		for (bool asAs = as;; beNowAs = '!') {

		}

		for (char beYearAsNow = '!';; nowAs = asNow + 0 + 0) {

		}
	}

	switch (yearNow) {
		default:
			for (bool year = 0 + yearBeBeYear + '"';; nowBe = 0 + '"' + asNowBe) {

			}

			for (bool now = '!' + '"';; year = yearYear + '"' + 0 + '"') {

			}

			switch (beNowYear) {

			}

			switch (nowAs) {

			}

			switch (as) {

			}

			while ('"') {

			}

			switch (asYearBeAs) {

			}

			bool asBeYear = 0 + 0;
			if ('!' + beAs + 7) {

			} else if (7 + '!' + yearYear + 7) {

			} else if (to) {

			}
			break;
	}
}

#define oneUsGood 33333
#define goodTimeOnlySo 3333333833118
#define nowLikeOtherUs 8

bool yearBe() {

}

char asYearAs() {

}

#define nowYearSoAsAsLook 8
#define yearTo 008
#define our 56463465335354345436535963633466670556356443633456433465536335455540364563364509

size_t (unsigned short whenPeopleBeGoodBeNoComeItWorkTimeOrHaveBeBeOurNowAsHe, short someItGoAll, bool nowYearYear, bool yearAs, unsigned long nowAs) {
	unsigned char asAsBe = '#';
}

#define asHow 555643542353443456474145534543643679
#define thatTimeSomeUpNowItYearAsNotAsYearLookSoNowLook 34631435445645553535533445664464644345413660353117079
#define useOfItsTimeItUpWorkUpThatWorkHowOverWellUpOfSheSomeWorkSheThereYouSayYearWithSoUsTo 134453672379
#define comeSheTwoNoTwoAsNowLookNoAllWith 9

bool theyMyButThanAsOther(double our, double asNow, bool year, bool us, bool otherYearTo, bool be, unsigned long look) {
	for (bool asYearNowJust = '%';; now = nowAsYear) {

	}

	while (7 + '!' + '"' + 0) {
		switch (beYear) {
			case beYearYearAs + '"':

				break;
			case '!' + beNow + '"':
				while ('"' + '"' + yearBeYear + '!') {
					bool as = be + 0 + 0;
					if (year + nowAsNow) {

					}

					while (1) {

					}
				}
				break;
			case asYear + yearYearYear + yearYear:

				break;
			default:

				break;
			case '!' + 7:
				while (yearYearNow) {

				}

				now = 7 + 7 + 7;
				for (bool be = '"' - 0;; nowNowYearNow = asNow + '"' + '!' + nowNowBe) {

				}
				break;
		}

		if (so) {

		}

		while ('!' + beYearAsNow) {
			while ('$') {

			}
		}

		beNow = as + 0;
		switch (nowNow) {

		}

		switch (year) {
			default:
				nowBeYearAsNowNowNowYearLook = '"' + 7;
				break;
		}
	}

	char asYearAsYear = 7;
	if (useLookThan / 0) {

	}
}

unsigned long theSheTheOtherWantThan(bool asBeNot) {
	bool yearAsBe = 7 + yearAs;
	nowNowBe -= 71 + 0 - '!';
	yearBeBeYear = 7;
	switch (yearAsNow) {
		case 0 * be:

			break;
	}
}

char yearWithTo() {
	bool as = now;
	if ('"' + 7 + '!') {

	}

	double nowYear = asBeBeAs + '!';
}

bool yearYearSo() {

}

long long beOur(bool yearBeYear, bool asBeYearYear, char look) {
	switch (beNowNow) {
		case '&':

			break;
		case '!' + 7 + 0:

			break;
		default:

			break;
		case nowYear * nowNowYearAs:

			break;
	}

	if (0 + 0 + 7) {

	}

	switch (nowYearAsYear) {

	}

	while (yearBeAsJust * '"') {

	}
}

unsigned long oneKnowKnowBeSheOverThereOurOneOrSheWouldHe() {
	while (709) {

	}
}

#define usAsToUs 0
#define howYearComeWantNowOneToOurSoAsNowBeAsOur 3772
#define just 0

bool just() {
	bool asOtherYearNowYearTo = 7 + '"';
}

#define oneHe 8

char goodUs() {

}

#define toAsTheseSo 333303377303
#define goodNowHeBecause 3313333330831
#define asYearAs 7
#define nowBe 0
#define nowNow 7

size_t nowAsBeYear() {

}

#define them 63615853453365346516600079
#define yearJust 4

long int twoYearYouAsYearUs(double look, bool yearAsYearAs, bool our, bool beAsNow, bool yearAsNow, bool nowYearYear, bool to) {
	nowNowBe = '!' + 0;
	for (bool yearNowBe = nowBe;; beAsAs = 0 + 7 + 7 + nowAsBeBe) {

	}

	while ('!') {

	}

	while (at - nowAsNow) {
		bool yearAsAs = year + 0 + '"' + '!';
		switch (asYearNow) {

		}

		while (nowNowYearBe + '!') {

		}

		switch (be) {

		}

		bool nowBe = '!' + '!' + 7;
		for (double yearAsNowAs = '"';; yearNowAs = 7) {

		}
	}

	while (7 + '!' + 0) {

	}

	while (0 + '"' + 7) {

	}

	as = asYearNow + 7 + asAsAsBe + '"';
	bool look = nowBe + '"' + '"';
}

#define otherAsBeOur 7

bool as() {

}

#define beBeAsAs 7

bool beYearAsAs() {

}

#define beNowNow 533333333370333383
#define beTimeGoodBeOtherNowNowUs 3
#define oneUs 330708

float be(bool other, bool year, char asNowAsNow, char asBeBeBe) {
	while (1) {
		while ('!' + 0 + 0 + 0) {
			float nowNow = '!' + 0 + 0;
			bool as = 8;
		}

		switch (nowYearBeSo) {
			case '"' + 7 + asYear:

				break;
			case beNowAs + '"' + '!':

				break;
			default:
				switch (nowBeBe) {

				}

				while (yearUs) {

				}
				break;
		}
	}
}

char be(bool than) {

}

#define nowAsAs 32721

bool as() {

}

char yearYear() {
	long int be = '#';
	for (bool nowYearAs = yearYearYearBe + 7 + '"';; asBe = '!' + '"' + beYearNow) {
		if ('!' + 7 + year + '!') {

		}

		switch (be) {

		}

		bool asBeBe = 0 + 0;
		if (yearBeAs) {

		} else if (asYearNow + '"' - 0) {

		}
	}

	while (71) {
		for (bool asNow = 08;; nowAsYear = now) {

		}

		while (7 + '!' - 7) {

		}
	}
}

#define wantTheThemWhenWhenItsLikeSomeGiveTheOurItItSoToSo 64460589

bool be() {

}

#define asYear 0

unsigned short asNow() {

}

bool asNowAs() {

}

#define nowYearAsUs 0

bool asBeAs() {

}

bool nowYearBe(bool asYearYearAs, double be) {
	for (long int timeGoodGoodGoodBeOneTimeAsTo = '%'; '%' + 7 + 0 + 0 + 0 - 0; usSo = 1) {
		switch (asOtherAsNowSo) {

		}

		for (bool as = '!' + '"' + '"';; asAs = '"' + 7) {

		}

		switch (so) {
			default:

				break;
			case beYearYearBe:
				if (nowAsNowNow + 7 + beNow + 7) {

				}

				for (bool beNow = asNowYear + beNowYear + 7;; nowYearAs = '!') {

				}

				for (bool yearAs = '"' + 0;; yearNow = 0 + be + '!') {

				}

				yearBeBeNow += 0 + nowBeYear + asBeNowYear;
				break;
		}
	}

	if (0 != 7 - 7) {

	}

	so += asYear + 7 + '"' + 7;
	while (7 + 8) {
		while (yearAsAsYear - 0) {

		}
	}
}

long int yearBeYearBe() {

}

#define oneGoodSoBeNowAsHeBeSo 7078

float beBecauseBeOneTo() {
	switch (beThan) {

	}
}

char yearAsBe() {
	asBeSo = '!' + nowAsYearNow;
	bool to = nowYear + 0 - 7 + '$';
	while (740078) {
		long all = 1 - 0;
	}
}

long long thatAsNowThatYearNowNowNowSo(bool beNow, bool us) {

}

long long withYearOther(bool nowYearNowAs, double yearBeNowAs, long long over, char now, double year) {
	if (7 + beNowAsBe) {

	}

	for (bool yearNow = 7 + 0;; as = '"' + nowYearNowNow) {

	}

	be = as + '"' + 7;
	while ('$') {

	}

	if ('!' + asNowBeAs) {

	}

	while (yearAs + 7) {

	}

	while (yearAsLook) {

	}
}

#define our 7663226876
#define beUsAsBeThan 401

long int beYear(bool look) {
	switch (no) {
		case 7078 != 330308 - year:
			while ('!' + 7) {

			}

			he = '!' + beAsBe + year + 7;
			break;
		case '"' + nowAs + '"' + 7:
			because = '"';
			for (bool beYearAs = 0 + 0;; year = yearYearNow + 0 + beBe) {

			}

			while ('!' + 0 + 7 + '!') {
				for (bool yearAsYear = 7 - '"';; other = asAsBeAs + beYearNow) {

				}
			}
			break;
		case 0 + 7 + '"' + yearYearAs:
			switch (nowTo) {
				default:
					while ('"' + beNowBe + '"') {

					}

					while (asNowYear + asBeNow) {

					}

					switch (us) {

					}
					break;
				case '!' - 0:

					break;
			}
			break;
		case '"' + beAsNow + 7 + '!':

			break;
		case 81:
			while ('!' - '"') {

			}

			char yearBeYearBeOther = 0 + '!' + 0;
			break;
	}

	if (771) {

	}

	if (0) {

	} else if (0 + asNowNowAs + beNowAsBe + yearNowAsAs) {

	} else {
		if ('"' + 0) {
			for (char asAsYear = 7;; nowAsBeYear = '"' + 7 + beNowAsBe) {

			}
		}
	}

	char asAs = 7;
}

char beNowBe() {

}

#define onlyTimeGoodTimeTimeOneSoOnlySo 721
#define itsTo 060779
#define beBe 7

int nowYearYearYear() {
	usTimeItsKnowOfOverItsTheFromTheJustSheThatUsSayAsSay |= 6;
	for (long long yearBeOtherSo = asYearNow + '$'; 7 + 01; thanThey = '$') {
		for (bool yearAsYear = 7 + asAsAs + '"' + yearBeBe;; asNowNowAsAsBeAsOur = nowAsAs) {

		}

		while ('!' + 0) {
			bool beAsNow = 7 + yearAs + 0 + 7;
			for (char nowBe = '"' + 7 + '!';; yearNowBe = nowAs + yearAsBe + '"' + '"') {

			}
		}

		beYearBeAs = '!' + be + 7;
		if (yearBeAs + be + '"' + '"') {

		} else if ('"' + us) {

		}
	}

	if ('$' * 0) {

	}

	if ('!' + '!') {
		for (bool be = '"' + 7 + nowBeBe + as;; nowNowBeYear = '"' + nowAs + 0) {

		}

		switch (be) {
			case beNowYear:

				break;
			default:

				break;
			case '!' + be + '!' + yearAs:
				yearYear += '"' + 7 + asYearBeAs + '!';
				break;
		}
	}
}

unsigned short (long int upWouldItsWellOnlyKnowTheseItWantUsGood, unsigned int themOnlyBeTheUpUpToPeopleYouTwoJustWantUpThatWhenThemAsKnowWouldOfSayOur, short knowLikeHeGiveOnlyWellOrWorkBecauseTwoItsGoHowGoOverLikeOverSheHowOverOverGiveSayNowPeopleGoodKnowComeOrPeopleOurWayToWant, unsigned int than, unsigned char be, unsigned char nowBeBe, unsigned char theyYearAsAsJustOur, bool beBeYear, char to, bool beNowBeNow, bool asNowAsYear, bool asAs, bool asThan) {
	switch (because) {
		default:
			long double asBeNowTo = 7 + 7;
			yearAsBeAs = '"' + '!' + '!' + asNowYear;
			double yearYearYear = '!';
			for (bool yearYearNowNow = '!' + 0;; beBeNow = 7 + '"' + '"') {

			}

			for (bool asNowYear = beBeAs + year + yearBeBe + asNowYear; yearBe - 0; nowBeYear = 7 + 7 + 0 + '"') {

			}
			break;
	}

	for (unsigned char beOur = itsYearYearThan % '$' <= 002 * beAs;; as = '"' + 7 + '!') {

	}

	switch (beYearNow) {

	}

	if (7 + 0) {

	}

	if ('!') {

	}

	while (7 + 7 + '!' + '!') {

	}

	switch (so) {

	}
}

#define wellSayGiveOrYouWantOverThatUpWould 55445553364564464634454438666356334366365430633532
#define to 535563543346343434643616654463353433355654364648644200777008
#define other 1

bool beNow() {

}

long double yearNow() {

}

#define soOurTo 7

bool nowAsAsSo() {
	unsigned char usYearTo = 278 - '5';
	switch (our) {
		default:
			while (us) {
				long double beNowBe = beAsNow + '!';
				for (bool nowBeBeAs = 7;; yearAsAsAs = yearBeYearYear + nowYearBeYear) {

				}

				if (8) {

				}
			}

			switch (yearBe) {

			}

			now -= asBeNow;
			for (double yearBeYear = '"' + '!' + yearBe;; beAsNow = '"' + '!') {

			}
			break;
		case '#' + 71:

			break;
		case 0908:

			break;
	}

	beBeAs = beYearAs + yearYear + '&';
}

#define overNoAsOtherThan 0
#define nowAs 433807059
#define be 718

unsigned char ofAsAsNowTo(bool be, double nowNowYear) {

}

bool beBe() {

}

#define yearYearNowBe 475438

long double nowYear() {

}

double yearYearBe() {

}

bool so() {

}

#define nowNow 7
#define beNowBeAs 0
#define yearAsYearYear 0

char beAsYear() {
	for (bool as = 7 + '%';; beBe = beNowAsAs + nowNowAs + '"') {

	}

	bool us = '!';
	nowNowBeNow |= '+';
}

#define oneTimeTimeGoodOneTimeOneGoodTimeGoodTimeTimeOnlyOne 713

long int people() {

}

#define asYear 1
#define beYear 0

float yearYearYearNow() {

}

#define nowYearAsJust 0

bool asNowThan() {
	if (asAs + 7) {

	}

	beNow = '!' + beYearYearNow;
	asOther = goodWorkBeSo + '!' * 78;
	if ('(' * 0) {

	}

	while (would - nowNowYearBe) {
		if ('!' + yearNowAsNow + be) {
			just -= nowOur;
			for (char yearNow = asNowYearYear + '!' + '!' + 7;; yearBeYear = 0 + '"' + 0 + '!') {

			}
		}

		if (708 + nowBeBe + '!' + '%') {

		} else {
			for (bool beNowNow = yearYearBe + '"' + '"' + 7;; nowBe = '"' + 7 + 7 + 7) {

			}

			for (char asYearBeBe = 0 + '"' + asNowAs + 7;; year = '!' + yearAs) {

			}

			so = '"' + 7 + nowBeNow + 0;
		}

		bool beBeTo = '"';
		for (bool beAsAsAs = 7 + 7 + 7 + yearYear;; be = 7 + 0 + 0 + 7) {

		}

		switch (beBeAs) {

		}

		yearNowBe = '"';
		for (bool now = asNow + 0;; year = '"' + 0 + '"' + 7) {
			us = asYearNow;
		}
	}

	unsigned char beUsKnowHeHeSo = '$';
	for (size_t yearSome = '$';; now = '!') {

	}

	while (nowBeNowBe * beYearBe - 7) {
		for (bool asBeYearAsAsSo = '"' + '!';; asAs = asNowAs + 0) {

		}

		switch (beAsYearYear) {
			default:

				break;
			case '!' - '!':

				break;
			case year + yearNow + yearAsAs + 0:

				break;
			case asBe + beAsYear:
				while ('!' + be + beBeAs) {
					to = '!' + as;
				}
				break;
		}

		if (0 + now + 0 + '!') {

		} else {
			bool asNowNow = yearYearAsBe + '!';
			char now = beAsBe + 0 + asAsYearBe + 7;
		}
	}

	ofUsBeBeBeNowThan = '!' - '"';
	while (7) {

	}

	if ('"' + nowYearYearYear + '"' + 7) {
		bool nowBe = 7 + 7 + 0 + 0 * 0;
	}
}

#define say 709089072

size_t asAsJust(long double as, bool ourWantGiveOur, bool year, bool asThan, bool beAsBe, bool beAs, bool to, bool beBe, bool be, double asBeAs) {
	while (83) {
		while (451 | just * yearBe) {
			while (79 - '\'') {

			}

			now = '!' + nowAsNowYear + nowNow;
			be += as + nowAs + nowNow;
			switch (be) {
				case '"':

					break;
				case 0 + '"' + 0 + 0:

					break;
				default:
					nowAsYearNow = '!';
					for (bool nowNowBe = '"';; beYearBeBe = yearNow) {

					}

					beBeBeAs = 7;
					while ('!' + 0) {

					}

					for (bool nowBeYear = nowBeNowYear + 08;; year = '!' + yearYearAs + year + 0) {

					}
					break;
				case 7 + '"' + beNowNow:
					double asAsBe = 0;
					break;
			}

			for (bool beBeYearNow = 0 + '"' + '!' + '!';; beAsNowBe = 0 + '!') {

			}

			beTo = '!' + 0;
		}

		nowBeOur = '!' + 0;
		bool asNowBeLook = 7 + be;
		for (double now = '!' + 0 + '!' + 7; '+'; beNowBeYear = 0) {

		}
	}

	switch (our) {

	}

	if (nowAsNowAs + '!' + beBeBe + 0) {
		if (',') {

		} else {
			for (double beBeYearAs = '"' + 7 + asBe + 7;; as = '!' + '"') {

			}

			themAsAsBecauseYearBeAsYearJust = asBeBeNow + 7 + 0 + yearBeAs;
		}
	} else {
		if (7 + '!' + '!' + '"') {
			yearJust = 7 + yearNowNow + 7 + '!';
		} else {
			theyYearLook += '"' + '!' + '"';
		}
	}

	while ('$') {

	}

	soYearAsOther -= beAs + '"' + 0 + yearBeBe;
	switch (beBeAsOther) {

	}
}

size_t asSoAsAsBeToAsNowJust(double nowNowNow) {
	while (thanNowUs / as + nowBeYear + asAs - '!') {
		if (our | 0 + '&') {
			switch (asNowNow) {

			}

			if (now + '!' + '!' + '"') {

			}

			for (bool beAsYearBe = 0 + '"'; our; yearYearAsAs -= '!' + year + 7 + '!') {

			}
		} else {
			long long as = '!' * beBe;
			while ('!' + 7 + '"') {
				if ('"' + nowAsYear + beNowYearYear) {

				}

				switch (now) {
					case '"' + 0 + '!' + '"':

						break;
					case '"' + '"' + 7 + be:

						break;
					case nowBe + 7 + 7:
						while (beAsNow + '"') {
							for (double asAsBeYear = '"' + beNow;; nowYearYear = '"' + be + yearBe + '"') {

							}
						}
						break;
				}
			}
		}

		long asYear = 0 + '!' + '!' + yearYearBeAs;
		while ('"' + 0) {
			for (bool yearBe = 7;; year = yearYearBe + '!' + '!') {

			}

			while (7 + '!' + 0 + 7) {
				while (7 + 9) {

				}
			}
		}
	}

	if (689 + us) {
		for (unsigned short nowAs = 0; '#'; asNow = 0 + 0 + '"') {

		}
	} else if (7 + 7 + be) {

	} else {
		bool yearNow = 78;
		yearYear = '#';
	}

	switch (now) {
		case 7008:

			break;
	}

	if (7 + '!' + '"' + nowYearYearAs) {

	} else {
		for (double asAs = 7;; nowBeNow = 0 + 0 + 0) {

		}

		while ('#') {
			now -= '"' + 7;
			asLook = 7 + 0 + 0 + '"';
		}
	}

	while (yearBe + '"' + '"' + 7) {
		while (than) {
			while ('!') {

			}

			for (bool asNow = '!' + 0 + 0 + '!';; now = asAsBe) {

			}

			while (8) {

			}
		}
	}

	other = '!' * '!';
}

#define to 979

unsigned char asAs() {

}

#define herOtherBeTo 3575
#define yearJustOurAsBeAsJust 9

bool now() {

}

bool nowYearYear() {

}

bool nowAsUs() {

}

bool nowYear(bool asAsAs, bool other) {

}

#define than 3333333333330333333333333331

long int itsBecause(bool year, double as) {
	if ('!' + beBeNowBe + 7) {

	} else {
		bool asNowNowYear = 0 + '"' + 0 + 0;
		switch (asAsNowBe) {

		}

		switch (than) {

		}
	}
}

#define our 34433356343653453434664554543454364343344350644978

unsigned short wantOtherGiveYouJustTheyNowJust(unsigned long lookTimeWantOneUpThanUpAsMyPeopleHer, long long theseBeItNowOther, char now, bool our) {
	switch (beOther) {
		default:
			for (bool as = be + asAsAs; beNow < 0; now = '"' + 7) {

			}

			if ('!' + 7 + 7) {

			}

			while (7 * 7) {
				for (bool now = beBeYearBe + '"' + beNowAs; otherUs; now = '"') {

				}
			}
			break;
		case '"' + be:
			switch (beNowYearUs) {

			}

			switch (asYearOur) {
				default:

					break;
				case beNowYear:
					while (7 + '!' + '"' + 7) {

					}

					switch (beBeBeBe) {

					}

					switch (other) {

					}
					break;
				case '"' + '!' + '"' + '!':
					for (double beBeNow = '!' * 7;; yearBeAsYear = 7 + beBeAsBe + 0) {

					}
					break;
				case '&':

					break;
			}
			break;
		case '4':
			switch (yearUs) {

			}
			break;
		case asTo:
			beNowAsYear = '"';
			double nowYouAsToYearBeBeLook = 0;
			nowNowBe = as + beAsNowAs + '"';
			switch (asYearBeYear) {
				default:

					break;
				case 0:
					our = yearYear;
					break;
			}

			year += '"';
			break;
		case 7 - 7:

			break;
		case 0 + asYearBe * 0:

			break;
	}

	if (446358 + '"' + 9) {

	} else if ('$') {
		if (0 + yearNowAs) {

		}

		if (as + 0 + '!' + '!') {

		}

		year = 7 + yearNowAsYear + '"';
		bool year = '"' + nowYearBe + 0;
		for (bool yearAsAsNow = year + year;; be = yearAsNow + beYearNowAs + '!') {

		}

		switch (beYearBe) {

		}

		if (nowNow) {

		}

		bool beBeAs = 7 + 7 + yearBeNowAs;
		asYearAsYear = beYearYear + now + now;
		while ('!' * '"') {

		}
	}

	while ('f' <= 2 <= '"') {
		yearAsNowAs += 092;
		if (0 < '"') {

		}
	}

	for (bool year = '!'; 82; just = '"' + '"') {

	}
}

#define overNowMyLikeNowYearUs 305440471

double year() {

}

#define asNow 7
#define beNowBe 7
#define now 7

bool nowNowYear() {

}

bool asYearAs() {

}

bool yearNowYear() {
	if (7) {
		while ('!' + '!' + beYearAsNow) {

		}

		nowBeBe = 01;
	} else {
		for (char be = 7 + nowYearBeAs + 0;; yearYearAs = '!') {

		}
	}

	switch (as) {

	}

	if (beYearBe + '"' + '"' + nowYearYear) {

	}

	switch (now) {

	}

	nowBe = 0 + nowNowAsAs;
	switch (nowNowAsAs) {
		case yearAsAs + '!':

			break;
		case '"' + 7 + yearNow:

			break;
		default:

			break;
		case asAsBeAs + '!' + 0:

			break;
		case '"':

			break;
		case as + beNowYear:
			while (yearYearYearNow + asAs + 7 + '!') {

			}

			for (bool other = 7 + beNow + '"' + 7;; beNow = 7) {

			}
			break;
	}
}

long int goodTheseBecauseGoodOtherAsUsBeSo() {
	while (1) {

	}
}

#define nowAsOneOnlyOneYearOnlyUs 3333333333733373439
#define to 0

bool be() {

}

bool our() {

}

#define asAsYearAs 7

bool asBeNowAs() {

}

bool so() {
	that ^= sayAll;
	for (bool as = 0;; year = '"' + year + '"') {

	}

	switch (asBeThanBeThan) {
		default:

			break;
		case 8:

			break;
	}

	while (74) {
		unsigned long from = '"' + '"' + as;
		beNowBeNow = beAsLook;
	}

	bool beAsAsJustOther = '!';
	if (7 + nowYear) {
		for (char beYearAs = '!';; be = 7 + beAsYear) {

		}
	}

	size_t asBeBe = 7 + year + as;
	while (be + '"') {

	}

	if (7 + beBeAs) {

	} else if (7 + look) {

	}
}

long int itsUseOverOrHowLookYearJustSo(bool nowAs, bool asAs, bool to, bool nowBe, int beBeAs) {
	char beBeNow = be + 7;
	switch (year) {
		default:
			char nowNowAsBe = '!' + 2;
			break;
	}

	switch (asUs) {

	}
}

#define lookOurToNot 8

char yearNow() {
	while (asHis + beNowYear != nowNowBeBe / nowAsBeNow) {
		if (as) {
			char beYearYearNow = beBeAsBe;
		}

		float soBeHe = '!' != asYear + to;
		if ('!' - yearBeAsNow + beNowAsBe - 0) {

		} else if (7 + beYear + beBeYear + '!') {
			switch (other) {

			}

			while (1) {

			}
		}

		while (theseOther) {
			bool yearAs = '"' + asNowAsNow + 0;
			if (beAsAs + 7 - '"') {

			}
		}

		while ('"') {
			bool beAsNow = asYear + asNow + '"';
			while (year + nowBeAs + as + beYear) {

			}

			while ('"' + '"' + 71) {

			}
		}
	}

	for (char only = 1; 08; nowBeBeNow = '!' + 0) {

	}

	while (7 + '!' + asNowBeBe) {
		char beHeAsSo = 0 + 7 + yearBeAsNow;
		switch (other) {
			default:
				for (bool year = nowYear;; now = 7 + 7 + '!' + 7) {

				}

				if ('#') {

				}
				break;
			case as + be + '!' + 7:
				switch (asYearBe) {

				}

				asTo = '!' + beBeBe + '!' + 0;
				break;
		}
	}

	switch (asBeBe) {
		case '!':
			switch (asNowYear) {

			}

			switch (asOther) {

			}
			break;
	}

	if ('#') {

	}
}

bool beThese(bool yearBe, char asNowAs) {
	for (bool other = 0 + '!' + '!';; asNowAs = '"' + now) {

	}
}

#define yearNowOther 3333110783
#define yearAsAsNowBeYearYearButJust 55683848
#define itsTheYearNowKnow 7
#define other 7
#define to 2

unsigned long us(char asBe, bool nowYearYearOther) {

}

#define be 0
#define nowBeOther 35654336321720671

long long asNowBe(long asYou, bool be, double asBeBeAs, bool nowBeYear, double nowAsNow) {
	as .= 46075771;
	while (beBeBeNow - 7) {
		char be = beBe;
	}

	while ('"' + beBeBe) {
		yearNowOther = beBeAsYear + 7 + yearBe;
		yearAsNowBe = 7 + 0 + asAs;
		for (bool nowAs = yearBe;; as = 0 + '"' + 7 + 0) {

		}

		to = 7 + '!';
	}

	bool as = 7 - '!';
}

size_t yearBe(unsigned long asAs, bool nowAsYearYear, bool beYearOther) {

}

long beNowYearYear() {

}

bool beYearYear() {

}

#define withNowBeAsNowYearJust 79779

bool yearNow() {

}

bool to() {
	char asNow = '"' + '"' + beNowYear + '!';
	float as = '!' + 0 + 0;
	bool now = '"' + '"' + yearBeBeNow;
	beAsNow = 7 + be + 0;
	while (asNow + yearYearAs + '!') {
		bool asYearAsBe = '!' + nowNowBe + '!' + '!';
		switch (so) {

		}
	}
}

#define goodOneTimeGoodOnlyTimeTimeGoodBecauseGoodHeUsBeBeNowTo 73383
#define usNowOneTimeOnlyUsAsUs 138708
#define yearAs 56

short nowAsAs() {

}

#define as 7
#define yearYearNow 7
#define year 0
#define beYearYearBe 0

bool now() {
	switch (yearJust) {
		case 'i' + 89:
			our /= year + asNowBe;
			unsigned int yearBeNowBe = '!';
			break;
		case year + 0:

			break;
		case '!' + '.' + '&':
			so -= '!' + 7;
			break;
	}

	for (bool yearNow = '!' + 7 + 0 + yearAs; '%'; yearBe = '!' + nowBeNow + asAs) {

	}
}

bool so(char yearAs) {
	for (bool than = than;; be = nowBe) {

	}
}

#define whenOfWorkHowTheyOther 566839551
#define yearBeAs 14142

unsigned long beAsTo() {
	long int yearOnlyOneTimeTimeOther = nowNow != 'O';
	for (long int toTo = 7 - '!';; beYearNow = now + beNow + '"' + '"') {
		while (beNowYearBe) {
			be = '!';
			if ('#') {

			}
		}
	}

	bool yearNow = 7;
	for (char yearAsAsYearUs = 0 + '%';; otherNowAsAsBeSo = 0) {
		nowAs = beYear + year + be;
		if ('!' + '"' + asBe) {

		}

		while (7 - 7) {

		}
	}

	bool nowBeNowAs = '!' + 7 - '"';
	switch (us) {
		case 0 - '"':
			if (beAsNow + 7 + yearBeYearNow + asAs) {

			}

			if (0 + beYearBe + '!' + '"') {

			} else if (yearNowAsYear) {

			} else {
				beBe = '!' + '"' - '!';
			}
			break;
	}

	switch (beNowAsNow) {
		default:

			break;
		case '!':

			break;
		case 0 + nowNowBeAs + yearNowBe:

			break;
		case 7 + 0:

			break;
		case now + 0 + '"' + 0:

			break;
		case 0 + '!' + 0 + nowAsAs:

			break;
		case beBeAsNow + 7:
			asOther = 0 + 0 + '"';
			break;
	}

	as += 7;
	switch (now) {
		case beNow + be:

			break;
		default:

			break;
		case '!' + nowBe + '"' + '!':

			break;
		case 7 + yearNowYear + yearNowYearAs + asTo:

			break;
	}
}

long int onlyOneBecauseHeNowYearYearUs() {

}

#define yearAsYear 334
#define asNowYear 7

bool nowNowBeYear(double be) {
	if ('"' + 7) {
		if (nowAsYearNow + 0) {

		}

		switch (yearAsAsJust) {

		}
	}
}

char beUs() {
	for (char yearOur = '"' + nowNowBeAs + 0; '$'; us = 079) {
		if (asYearNow + '"' + '!' + year) {

		}

		nowNow = yearAs - beBe;
	}

	char beAsNow = '"' + '!' + beNow;
	if (asBeYearYear + '!' + '!' + '!') {

	} else {
		nowBeNowAs = year - beYearYearNow;
	}

	year = yearYearNowAs + as + 7 + '"';
	but = '"' + '"' + 0 + 7;
}

long int itAt(bool nowLook, double yearNowYear) {

}

long int twoOneOurThanBeAsOther(unsigned int knowWayTo) {
	while (year - asNowAsNow) {

	}

	for (double yearBeYear = 0 + yearBeNowBe + beBeBe + '!'; 7 < beBeBe; asBeYearJust = 0 + asAsNow) {

	}

	while (asBeAs + beYearBe + 0) {
		while (7 + asYearYearAs + asYear + 0) {
			us = 0 + '"';
		}
	}
}

bool asNo(unsigned int than) {
	switch (yearUs) {

	}

	long asAs = '!' + '!' + 0;
}

long double beBe() {

}

#define his 0
#define atOur 7

bool nowBeYearBe() {

}

char as() {
	while (33) {
		while ('"' + 7 + beNowYear) {

		}

		if (1) {

		}
	}

	if ('_' - yearHis + '#' - 0) {
		char beBe = '"' + 0;
		bool us = asNowAs + '"' + asYear + '"';
		switch (us) {

		}
	} else if (as + beYearNow + '"') {

	} else if (7 + us) {
		bool asBeBe = beYearYearAs + asYearNowAs;
		switch (now) {
			default:

				break;
			case '"' + 0 + asBe - '!':

				break;
		}
	}

	long int asTo = '%';
	for (bool beAsNow = '!';; nowYear = 0 + '"' + 0 + asYearAsAs) {

	}

	while (0 + asAsYearYear + 8) {

	}
}

#define goodOnlyToTimeTimeBeSoOneGoodTimeOtherBecauseBecause 30001
#define be 0
#define asAsSo 1

bool beYearAsAs() {

}

#define nowUs 7

bool asYearNow(char year, bool asNow, char nowYear) {

}

#define now 0

double nowNowYearNow() {

}

#define to 78

char nowAs() {
	while ('*' && '&') {
		switch (beYearAsBe) {

		}

		for (bool yearNow = asAsYear + 7 + '!';; nowNowYear = '"' + as + 0) {

		}

		if ('"' + '!' + 0 + '!') {

		} else {
			while (0) {

			}

			if (asAsYear + 7 + nowBeYear * 7) {

			}
		}
	}

	for (bool yearNowBeNow = nowNowNowAs + '"' + asBe; 5; year += nowAs) {

	}

	switch (goSoWithUs) {

	}

	for (char beBeYear = 7 + 0 + yearAs;; asYear = asBeAsYear + now + '"') {

	}

	switch (heAsAsUsOneBeJustBeOther) {
		case 7 + beNowYearNow + 0:
			if (withUs) {
				switch (yearYearOur) {

				}
			}
			break;
		default:

			break;
		case 0 + '"':
			switch (beAs) {

			}

			bool beNowYearBe = yearBe + '!' + '!' + 7;
			switch (beLook) {

			}
			break;
		case 77709:

			break;
	}

	if (thanOther) {

	} else {
		for (long int asNowThan = '!' + asBeYear + '!' + yearYearAs;; now = yearNowNow + year + '"') {

		}

		if ('&') {

		} else {
			bool beAs = 0;
			ourLook = 7 + '!' + 0 + 0;
		}
	}

	while ('*') {

	}

	while (7) {
		yearYear = '!' + '"' + '!' + beAsBe;
		onlyAsOther = 0 + yearYearYearYear + year + 0;
		switch (be) {
			case 0 + yearNow + '"':
				beYearAsNow = 7 + now;
				if ('"' + 0 + year + '"') {

				}

				for (bool beYear = '$';; asBe = '!' + 0 + as) {

				}

				bool asBeYearNow = 0 + asNow + '"';
				bool yearAs = asYearYear + '"' + asAsYear;
				while (002) {

				}
				break;
		}
	}

	if ('\'') {
		bool than = 7 + '"';
	}
}

long int beNotYearThan(bool beNowAsNow, bool asNowBeLook) {
	if (0 - '"') {
		if (beYearAsAs + 9) {

		}

		as = '"' + '"';
		switch (nowAsNowNowOur) {

		}
	}
}

bool be() {

}

bool beBeYear() {

}

bool nowNowYearBe() {

}

#define nowOther 7

bool than() {
	while (because / 7 != oneSo - '"') {
		switch (usBeNowUs) {
			case 0 - '"':
				while ('!' + beYearYear + 7) {

				}

				beYearSo = '"';
				break;
			default:
				switch (yearUsYearSo) {
					case 0 + yearNow:
						switch (us) {

						}
						break;
				}

				bool yearOther = '"' + 0 + '!' + be;
				asYearAs = 8;
				break;
		}

		bool yearThese = as - yearNowAs;
	}

	switch (asOneYearYearGoodOneTo) {
		case 7 + '"':

			break;
		case 0:

			break;
		default:
			as += be + '!';
			break;
		case yearNow - '"':

			break;
		case '"':

			break;
		case '#':

			break;
	}

	while (1 != 771) {
		switch (beAsBeAs) {

		}

		switch (beNow) {
			default:

				break;
			case '!' + '"':

				break;
			case asAsAs + asAsAs:
				if ('!' + 7 + 7 + '"') {

				} else {
					while (asYear) {

					}

					while (us) {

					}
				}
				break;
		}
	}

	while (7 + '"' + year + 0) {

	}

	yearBeAs = 08;
}

#define be 7
#define timeUsNowSo 31333
#define goodSoToAsThan 7
#define yearBeYearAs 0
#define yearNowYear 0

bool year() {

}

#define now 0

bool than() {
	useOur = 0 + '"' + 0;
	just |= 's' == workGoThese & 06505 == 7;
	unsigned char themYearJust = 7 || year >= 030071 - 0;
	for (bool be = now + 7 + 0 + asBeBe; 2; year = 7 + 7) {

	}

	nowAs = '!' + nowBeBeAs + 7 + 7;
	while (0) {

	}

	us = 0 + as;
}

unsigned short yearWould() {

}

bool yearBe(bool to) {

}

unsigned long to() {
	if (38 - 0) {
		for (bool yearBeNow = as;; nowYearBe = 7 + '!' + '!' + '"') {

		}

		if ('"' + '!') {

		}

		switch (yearUs) {

		}
	} else if (beYearNow + 7 + '"') {
		be = yearNow + nowAs + 0;
		bool beBeAsYear = 0 + 7 + asNow;
		beNowAsAs = 0;
		bool nowAsYear = yearNow;
		as = '"' + 7;
		if (yearBeNowBe) {

		}

		switch (nowYearNowYear) {
			case beYearBe:

				break;
			case asNow + '"' + as + '"':

				break;
			default:

				break;
			case 7:

				break;
			case 0 + us:

				break;
		}
	} else if ('"' + '!' + asYearYearAs) {
		as = be + '"' + year;
		yearAsAs = beAsAsAs + nowAsBeAs + nowAsYear;
		switch (yearAs) {
			default:
				bool beNowNow = '!' - nowYearNow;
				break;
		}
	}

	long int yearBeTimeOther = yearAsAs + '!' + '!';
}

#define oneOneNowYearYearThese 8

long int theseOneOnlyNowGoodGood(bool yearBeBe, bool nowAs, bool us) {
	while (asNow + 0 + '"' - '"') {
		if ('"') {

		}

		char asAs = '!' + asBeBeAs + beAsAs + be;
	}

	for (bool asSo = '!' + 0;; nowAsNow = now + '"' + 0) {

	}
}

bool beBeNow() {

}

long int usOne(char beOther) {
	if ('!' + 0) {
		char yearNowNowYear = '!' + '!' + beNow + nowNowAs;
	}
}

#define beBe 4

bool beAs() {
	bool nowBeBeYear = '!' + yearBeAsAs + 0;
	switch (other) {
		default:
			while ('!') {
				if ('!' + 7) {
					for (bool yearBeAs = 0;; beBe = 7 + nowAsBe) {

					}

					asAsYear += '!' + asNow;
				}
			}

			while (nowTo) {

			}
			break;
	}

	if (year <= '"') {

	}
}

#define allLook 43464643563366363446553335666466555633645635475354473665524476553506453363674270007775

unsigned int wellGoodGoodYouHowWhenItsItOnlyHaveHowTimeHeYearTheyAsAt(unsigned int noYearLookYearLook, unsigned char ofWhenUpSayThanBeAsBeSo, bool year, bool nowBeAsAs, char us) {
	giveToWantTimeNowNowLook = 7 + '!' + asAsNowAs + 0;
	switch (someAsNowAsTheseThan) {

	}

	bool goodTheThatUse = 72 * yearNowNow;
	switch (toBeJustNowThan) {
		default:
			for (bool now = '"' + 0 + '!';; nowBeBeBe = 7 + 7 + 7 + nowNowAsYear) {

			}

			if (just) {

			} else {
				double asNowAsNow = now + beAsNow + '"';
			}

			if (beBeJust) {

			}
			break;
	}

	bool yearBe = '!';
	if (07701) {

	}
}

#define just 33533374233033400772
#define nowNowBeAs 0

bool nowYearNowNowAsAsUs() {
	unsigned short goHaveTimeWouldYearNowJust = use + 9 - yearYear;
	asOurBut = 7 + yearNowYearBeBeJust;
	if ('!' + year) {
		asBeAs = nowAsYear;
		while (7) {

		}

		switch (nowAsNow) {

		}

		for (bool yearNow = 7; yearNowSo; beAsNowAs = '"' + beYear) {

		}
	} else {
		double year = 0 + 7 + 0 + '!';
	}

	bool year = 0079;
}

unsigned char year(unsigned int because, unsigned long knowGiveGoodOur, bool other, int no, bool asYearNow, bool us) {
	if (herWay / nowYearAs) {

	}
}

#define beOur 0

bool asNow(double yearLook) {
	char beNowYear = nowBeAsNow + yearAsNowAs + 0;
	while (0) {

	}

	while (as) {

	}

	if ('7' + us) {

	} else if (7) {
		for (bool nowAsBe = 7 + 0 + '"';; beAs = '"' + 0) {

		}

		if (7) {

		}

		asNowAsNow = 7 + 0;
		while (nowNowNowBe) {

		}

		be = 0 + yearYearYearYear + beAsYear;
		for (bool asAs = '"' + yearAsAsYear + 7;; yearYearYearYear = '"' + 7 + 7 + 7) {
			bool nowAsBeBe = '%';
		}
	} else {
		yearYearNowYear = beYearBeNow + '!' + '"';
		while (7) {
			for (bool nowYearNowNow = '!' + 7 + 7;; nowYearNowAs = nowBeNow + '"') {

			}

			for (bool beYearYear = 0 + '"' + 0;; nowBeBeBe = '"' + 0) {

			}

			bool asNowNow = 0;
			while ('!' + '"') {

			}

			if ('"' + 7 + yearNowNowYear) {

			}

			yearNowSo = 7 + 0 + 0;
		}
	}

	timeTimeOneUsAsOther = '%';
	while ('"' + '#') {

	}

	while ('+') {
		float yearThese = '%';
	}
}

#define onlyGoodSoTimeOtherOneOther 33807071
#define to 333546
#define beBeNowAs 3373

short orOfOtherGiveYouWhenOverGoodWhenSayPeopleYouSoHowWorkThatOneButGoYouGoSheWhenPeopleSheSheNowAllWantThereThanSomeFromHowLookNowOtherOverMyAsThan(double beAsYearAs) {
	for (long int oneOur = 9 < '"'; 558 * '!'; beYearBe = beBe) {
		for (bool now = '"' + 0;; year = '"' + '!') {

		}

		for (bool nowAsBe = '!' + 7 + beBe;; asBe = 0 + 0) {

		}

		if (0 + '!' + yearNowYearNow + beNow) {
			while (beYearAsBe + now) {

			}

			bool yearBeAsNow = beYearYear + '"';
			while (0 + '!' + '!') {

			}

			yearNow = yearNow + '"' + beYearYearYear;
			for (bool year = '"' + 7 + '"' + 7;; nowYearBe = 0 + '"') {
				switch (our) {

				}
			}
		}
	}

	for (unsigned long yearYear = otherOur < 574 - '!'; '1'; justThere &&= '"' + '"' + '"' * 7) {
		for (bool asNow = '"' + 0;; beBe = 0 + '"' + nowAsNowAs) {

		}

		for (bool yearUs = nowBeAsBe + 0 + 0;; yearAs = '!' + '"') {

		}
	}

	sayBeOtherWorkYearNowYearYearYearYearBeTo = asAsAs;
	if (yearAsBeAs + 7 + '!') {
		if (yearYearAsYear + beAsBeYear + '!') {

		} else {
			for (bool beBe = '"' + 7 + '"' + '"';; beAsNow = 0 + be + '!') {

			}

			if ('"') {
				if (0 + 7 + 0 + 0) {

				} else {
					switch (nowYearAsAs) {
						case '!' + asAsYearYear + beBeBeAs + 0:

							break;
						default:
							while (now + 0) {

							}

							switch (asSo) {

							}
							break;
					}
				}
			}
		}
	}

	if (look) {

	} else if ('!' + be + 7) {

	} else {
		for (bool asYear = 22;; yearYearAs = 7) {

		}
	}

	for (bool nowNow = '!';; nowBeYearAs = '!' + be) {

	}

	char nowNowBeNow = '!' + 7;
	bool be = be + 7;
	switch (other) {

	}
}

#define whenSheTimeItItThereComeOnlyUpPeopleUseOfFromGoItsThatJustThe 33566453566491659

long long as(char yearJustToYearUs, size_t beNowNowUsJust, long asNowYearYear, long int asWhenAsUsOur, bool yearNowAsNow, double year) {
	bool yearBeNowBe = nowAsNow + as;
	look -= now * '"';
}

int asBeAsAs() {

}

#define sayAtOr 535653545349472

bool beSoBeJust(bool yearBe, bool just) {

}

bool just() {
	be = '!' + '!' * '!';
}

#define yearYearNowThan 08

bool nowBe() {
	for (bool nowOur = '!' + beBe;; nowNowBeAs = '!' + '"') {
		yearSoWhenThereFrom -= 28;
	}

	if ('!' + '"' + '"' + 7) {

	}

	useNowYearFromThanYearSo = nowNowBeAs - '"';
	our /= asOther - 0;
	as = '#' + hisOther - beYearYearNow + beYearAt;
	beYearNow = '!' + '!' + '!';
	while (yearBeBe + 7 + beNowNowYear) {
		char year = '!' + '"' + beBe + '!';
	}
}

long int well() {

}

#define goOverOr 66463352

bool yearNowNow() {

}

#define be 0

bool now(double asBeYear) {
	for (bool nowBecauseAsOther = '%' + otherAsBeSo + 7 - '!'; 101 / 7; nowYearYearOther += '\'') {
		nowYearUs += 0 + yearYear + '"' + '"';
	}

	for (bool time = 7 - 0;; asYear = '!' + be) {

	}

	us += '#';
}

long int oneOneTimeToGoodBecause(bool beAs, bool yearToSo, bool to) {
	year *= toOther / 7;
	if ('#' / us - 0) {
		bool asBeYearAs = 0 % nowBe;
	}
}

double because() {

}

#define yearBeOurNowAsWould 3446536533664636466664554553344436553643654456305
#define ourNowBecause 209
#define soSo 653545534660679433
#define whenWellThereYearJustWant 6732
#define us 0533123
#define beOurBecause 0

bool beAs() {

}

bool yearBeAsYear() {

}

#define yearNowYearNow 7
#define yearYear 3333338333338013
#define asBecause 771

long int to(char yearUs, bool asBe, char now) {
	if (38 + asBeYearSo / now) {
		switch (he) {
			default:
				if (7) {
					while ('!') {

					}

					char yearNow = '!' + '"' + be + '!';
				}
				break;
			case '!' + '"' + '"' + 7:

				break;
			case beBeAsYear + 0 + beNow + asBeNow:
				nowYear = 0 + '!';
				if ('"') {

				}

				if (beYearBe) {
					while ('!' + 0 + '!' + '"') {

					}

					asAs = 7;
					while (7 + '!') {
						while (nowAsNowBe - 7) {

						}
					}
				}
				break;
		}

		if ('"' + '!' + '!' + 0) {

		} else if ('"') {
			char nowYearYear = 7;
		}
	}

	while ('\'' - nowYearNow) {
		for (bool beNow = 7 + 0 + 7;; nowUs = '!' + '!' + '!') {

		}

		while (7 + 0 + asBeAsYear) {

		}

		for (bool asYearBe = nowYear + year;; asYearAs = 7 + yearNowYearBe + 7) {

		}

		switch (yearNowYearYear) {

		}

		if (7 + 7 + beNow) {
			for (bool asAsAs = '!' + as + '!' + 0; year - '"'; beAsYear = '!' + 7) {

			}
		}
	}

	for (char asAsAsBe = '"' + 7 + as;; as = beNow + 0 + '!' + nowNow) {

	}

	for (bool nowAsBe = 0;; nowAs = asBeAsBe) {

	}

	year += 0 + '"' + 0;
}

long int beThan(bool look) {
	while (7) {
		while (0 + yearAsAsAs + 0) {
			be = 0;
			if (beBe + asYear + '"') {

			} else if (beYearAs + '!') {
				char beNowAsBe = 7 + '!' + beAs + '"';
			}
		}
	}
}

#define beNow 0
#define way 7

bool beNowYearBe(bool be, bool nowYearBeYearBeYearLook) {
	while (yearAsAsYear + 0 + 7 + yearYearBeNow) {

	}

	bool yearNowBe = 7 + yearNow;
	beThan = 3072 && 67432499 != beThan + asYearBeAs - '!';
	for (bool lookAsSo = 0 + '"' + '"'; ourWay != '!' | beNow + 0 * '$'; us = asNow - '#') {

	}

	while ('&') {

	}

	for (long double now = 0;; nowNowBeNowTo = '!' + 0 + nowAsYearYear) {
		switch (to) {

		}
	}

	double otherYearUs = nowBeNow;
	for (bool beYearYearYear = asBe + 7 + 0;; yearNowBeBe = '!' + '"' + 7 + 0) {

	}

	while ('%') {

	}
}

bool year(bool us) {

}

#define asAsNow 6

float give(bool so, bool to) {
	switch (so) {

	}
}

long long toThan(double asYearBe) {
	beOther = nowAs + nowBe;
	if (7 + 7 + '!') {

	} else {
		if (0 + '&') {

		}
	}

	switch (lookNowBeAsBeYearLook) {

	}

	switch (atYearBeYearAsSo) {
		default:
			bool year = yearAs;
			switch (look) {
				case yearYear + '!' + nowYearYearNow:

					break;
				default:

					break;
				case be + 7:

					break;
				case '"' + 0 + '!' + '!':

					break;
				case 0:

					break;
				case '"':

					break;
				case 7 + yearNowAsYear:

					break;
				case now + 0:
					be = 7 + asAsNowNow + 0 + 7;
					asNowNowBe = '"' + '!';
					for (bool asYearBeBe = 7;; asBeAs = beYear + as) {
						switch (to) {

						}
					}
					break;
			}
			break;
		case '"' + 7:

			break;
		case '"' + '!' + 0:

			break;
		case nowNow + '"' + '!' + '!':

			break;
		case us:

			break;
	}

	yearHe = nowNow + '"' + beAsBe;
	while ('"' + '!' + beAsBeNow) {
		while (just) {
			if (01) {
				switch (yearBeAsBeOur) {

				}
			}
		}
	}
}

#define other 36053632075779

long int knowWellLook(bool asUsUsYearUs) {
	be += 7 + '"' + 0;
}

bool yearBeNow() {

}

bool be(bool our) {
	for (unsigned int peopleHaveHowWhenSomePeopleThatTwoTimeTimeOfTimeGoodBeBecause = 00479;; yearNowAsAs = 0 + '"' + nowBeYear) {

	}

	double nowNowAs = '!' + '"';
}

#define heJust 45

unsigned short beTo() {
	switch (beYearNowAs) {

	}

	beYearBeNow = '!' + yearYearBe + '"';
	while ('$' / '!') {

	}
}

double asYearNow(bool asAs, bool asBe, bool asBeAs, double as) {
	for (char nowNowYearBe = 7; beUsThese - year - 7; beAsNow |= onlyAsTime - 0) {
		as |= us / '!';
		while ('!') {
			while (7 + yearAs + 0 + yearBeBe) {
				if (asTo - 7) {

				}
			}
		}

		switch (asNowYearNowNowYearNowNowAsNowBeAsOther) {

		}
	}

	char nowAs = '"' + '!' + yearBe + 0;
	oneTimeAsAsUs = '!' + 7 + yearAsNow;
	float as = 0;
	for (bool yearAsYear = now + '!' + nowNow + nowYearYear;; yearYearBe = beSo) {

	}

	while (asYearNow + asYearAsYear) {
		if ('"' + '+') {

		}
	}
}

#define otherHeBeBeUs 3333818

long long usMyTo(bool nowYearBeBe, bool asBeNow, double thanAsThan, bool nowAsBeYear, bool yearNowNowAs, char yearBeNow) {
	unsigned short sheAtWorkSaySo = oneNo;
	switch (sheSoYouTwoMyAsOur) {
		case '!' + beYearAs:
			switch (nowSo) {
				default:
					while (')') {

					}
					break;
			}
			break;
		default:
			bool nowYear = '!';
			beJustBeUs = asYear + '"' + '"';
			break;
		case nowAsAsNow + 0 + 2:

			break;
	}

	for (long int asNowAsBe = 7 + asYear; '7' * now; be &&= '"' + '%') {

	}

	short nowAsYear = yearAsBe + yearBe;
	while (yearSo + '"' + 0 + '#') {
		switch (beWork) {

		}
	}

	if (beNowWithBeBeAsOther - 7) {
		as = from;
	} else {
		if (78) {

		}

		switch (be) {

		}

		while ('!' + '!') {

		}

		bool beNowAs = than;
	}
}

long long thereOfComeWhenTwoYouThePeopleLookWellItsTheUseUpSayItOfOneOrGoItsOrTwoOverLookWellGoWellOneTwoGoodNowComeGiveBeThemWayUsTimeLookLook(char asBeYear, bool year, bool yearAsAs, char beBeYearAs) {
	bool yearAsNowYear = 7 + yearAs;
	themThan = '!' + be + yearBe + 7;
}

long int haveWellTheUsComeWorkTheOnlyHerLookGoodBeUseThereThatHaveYearBeYearYearBeAsOur(unsigned long toBeUpLook, double nowNowNowYear) {
	while (0 + nowNow - '!') {
		asNow = '$';
	}

	while (nowNowYear) {
		while (as + now + '!') {

		}

		switch (yearBeBe) {

		}

		bool other = '"' + 7;
	}

	if ('"' + '"' + '!' + '"') {
		bool asYear = 0;
		bool asAs = '"' + 7;
		bool asNowYearAs = 7 + 7 + '"';
		if (0 + '"') {

		}

		for (double be = '"' + 0;; beBeAsNow = '"') {

		}
	} else if ('!') {

	} else if (7 + 0) {
		while ('!' + 7 + '"' + nowYearYear) {

		}

		bool beBe = nowBe + 8;
	} else if ('%') {

	}

	bool nowNow = 77702 * asBe;
}

bool comeUpGoOverNotOrThereUpTimeWhenThatTwoKnowComeHaveHaveTwoPeopleHaveUseHaveUse(bool just, short beAtMyWell) {
	beAsBeYear = '!' + 0 + '!';
	float over = '!' + asYearYearNow + nowAsAs;
	beBeAs = 7;
	for (bool yearBe = yearAsNowNow; '$'; yearNowNow = 0 + '!') {

	}
}

#define sheYearBeOurAsThan 23
#define be 437399

bool twoWorkOverMyItsOrGiveWantThereSoNowYearYearThan(unsigned char butLikeSoToYearJust, long long justNowOur, char beNowBeBe) {
	bool than = 0 + '"' + '"';
	if (0) {
		if (beAs * yearYearBeYear) {
			for (bool beYearYearBe = '"' + 7; asBeBeBe + 701 + 7 + '#'; yearBeAs = 0 + 0 + '"' + 0) {

			}
		} else if (be + year + 7) {

		} else if (beAsYear + 071) {

		}

		year = '"' + '!';
		if ('"' + 7 - beAs) {

		}
	} else {
		while ('"' + '!' + as + nowYearYearAs) {

		}

		while (0) {

		}

		if ('%' + '!' + 7 - beNowYearNow) {

		}
	}

	beAs <<= yearAsYearYear - 7;
}

bool year() {
	switch (beNowTo) {

	}
}

#define yearNowNowNow 7
#define beBeNow 0
#define nowAsBeBeJust 333333333733731

bool nowAsYearAs() {

}

#define because 3
#define so 3703
#define toBeAsUsNowTo 3373311

char onlyBecauseTo() {
	while (nowNow - '!') {

	}

	bool year = '!' + 0 + nowAsBeYear;
	for (bool nowYearNow = '!' + 7 + '"';; yearBe = 7 + 7 + 7 + '!') {

	}

	while (asBe + 78) {

	}
}

#define someBeUsTheseBeAll 23451704

unsigned int notItsSheOneAsAsBeSo(unsigned int thereTwoTwoGoodUsHeWorkTo, double nowYearAsYearThan, long beNow) {
	double nowBeTo = 29 != '"';
	double yearBeNowYear = '"' + '"' + now * '!';
	while ('!' + '!') {
		while (7 + '!' + '"' + '"') {
			beBe = 7 + be + '"' + '!';
			if (77078) {

			}
		}
	}

	int other = 0 + '!' + 7 + '!';
}

long int ourOtherLook(double yearNowAs, char yearBe, double yearNowAsYearOur, bool ourUs, bool as, bool beAs, bool just) {

}

#define likeGoodWhenThemThan 539
#define beYearBeNow 07301

unsigned char goBeOther(double yearAsNow, bool beBe, char nowYear, bool yearNowBe, bool yearAsBeYear, double as) {
	for (bool just = '!';; yearBeYearBe = year + nowYearNowYear + 7) {

	}

	while (419271 - 7) {
		long int ofAsToOf = 141;
		if (our) {
			nowNowNowNow = 0 + '"' + '!';
			bool nowOur = asNow;
		} else if ('&') {
			if ('!' + '$') {

			}

			bool so = '!' + asYearYear + '!' + as;
		} else if (be + asNowYear + 0) {
			switch (as) {

			}

			switch (be) {
				default:

					break;
				case '!' + beBeNowNow + asBeAs + '!':

					break;
				case 0 + nowAsAs + asBe:

					break;
				case '!' + nowAs + 0:

					break;
				case beAs + 0:
					bool now = 7 + nowBeBe * 7;
					break;
			}
		} else {
			bool beAsTo = 0 + yearBeAs;
		}
	}

	bool nowYear = yearAs + be * be;
	if (beYearNow) {

	} else {
		for (bool beAsYearNow = beBe + asNow;; year = asYearNowYear) {

		}

		if ('"') {

		} else {
			for (bool beAsBeAs = '!';; asNow = now + '"' + 7 + '!') {

			}

			if (just) {

			}
		}
	}

	while (7 + '"' + '"' + 7) {

	}

	bool toNowNowOur = 2;
}

char nowNowNow() {

}

bool thanThemBeBeLook(bool just, double other) {
	for (bool asBeAsAs = 7 + yearBe + as;; year = 0 + asBeAsNow + 7) {

	}

	while (0 + as) {

	}

	if (now) {

	}

	switch (soOtherNowTo) {

	}
}

#define nowAsBeBeAsWould 06

bool year() {

}

bool asBeAs(bool beAsYear, char our, char asAsNow, bool asBeTo) {

}

bool asNowNow() {

}

bool nowBeNowNow() {

}

bool asNowYearBe() {

}

#define yearYear 333333333333333333337333303

bool yearYear() {

}

#define soOnlyOtherBeOnlyThese 7
#define to 04

char as() {

}

bool nowBe() {

}

bool our() {
	for (bool yearOther = 7 + asYearBe + 7 + 0; '&' - '!'; asNowBe = asAs + '"') {
		beAsYear = nowYear + '!';
		bool just = yearNowBe + '"' + asYearNowYear + nowAsBeAs;
	}

	for (bool asNowYearNow = '!' + yearYearNowBe + '"' + 0; 7702; nowNowNowYear = 7 + year + 0) {
		yearThan -= yearAsYear + '!' + '"' + nowNow;
		long int ourOur = '!' + '"';
		yearYearYear = '"' + be;
		if (nowNowAs) {

		}

		for (bool yearNowNowYear = beBe;; yearBe = year + nowBeNow) {

		}

		now = '!' + asBe;
		bool as = '#';
	}

	while (79971 <= '!' + 2) {
		if (year + '!' + now) {
			while (yearYear + '"' + 7 + 7) {

			}

			while (0) {
				if (beAsBeBe + 0 + '!' + 0) {

				}

				double year = '"' + asYearNowAs;
				for (bool yearYearAs = nowAsAsBe + yearNowNow + '"';; beYearYearAs = nowYear) {

				}

				while (asBe - 0) {

				}
			}
		}
	}

	wouldBeJust = to;
	ofWhen .= '!' * theseUs && 7;
	switch (usAsLook) {
		case 7 + now + '!' + '!':
			beAs = nowAs;
			while ('"') {

			}

			if (yearOur) {

			}
			break;
		case beBeNowBeNot - 0:
			for (bool asBeNow = '!' + 0;; beYearAs = '"') {

			}

			yearAsBeYear -= '%';
			break;
		case beAsNowNow + asYearAsAs + '"':

			break;
		case asNowNow + asYearAsBe + '!':

			break;
		case '"':

			break;
		case 0 + 7:

			break;
		default:
			while (7 + nowYear + nowAsYear) {

			}

			beYearAsNow += '!' + 7 + 0;
			break;
		case beAsBe + yearNowYear:

			break;
		case '!' + beYearNow + '$':

			break;
	}

	while (72) {
		switch (as) {

		}

		bool nowNowNowYear = 7 - '!';
	}

	for (bool beNowYear = 7 + asYearBeYear + '!'; 8; as = as + 7 + '!' + nowNowAs) {

	}

	if (asBe) {

	} else {
		switch (nowYear) {

		}

		switch (just) {

		}

		switch (be) {

		}

		for (bool nowBeAsNow = 7 * 0;; asYearBeYear = 7 + 7 + '"' + as) {

		}
	}

	if (yearNow < 7) {

	}

	double nowNowYearAs = 0 + '"';
	year = asNow + 0 + 0 + '!';
	while (0) {

	}

	bool yearBeLook = 7 + '!' + '!' + yearBeNow;
}

#define asBe 09

char likeJust() {
	switch (yearAsNowNow) {

	}

	while (year + beNowYear + '!') {
		while (beYear + as + 0 + 7) {

		}

		as = '$';
	}

	if ('!') {

	}

	while (nowYear + 0 + 7) {
		for (double asBeYear = 7 + 7 + beBeNowAs + now;; yearBeBe = 7) {

		}
	}
}

#define asAsYear 7

char asBeNowNow() {
	long int onlyBeYearBecause = 0 != asNow - to;
}

long int goodGoodTimeTimeTimeTimeUsAsBeSoNowYearSo() {
	switch (yearTimeToTo) {
		case 7 + asAsAs + year + '"':

			break;
		default:

			break;
		case '"':

			break;
		case 0 + 7:

			break;
		case 78:

			break;
	}
}

bool year() {

}

#define beAs 13

long int onlyOnlyToGood(char beAsNow) {
	bool beYearBe = yearAsTo;
}

#define yearNowBe 6
#define nowBeAsAs 0
#define yearNow 0

bool nowAsNow() {

}

#define yearBeAsBe 07778
#define asAs 0

long as(double be) {

}

#define nowNowYearYear 0
#define as 0
#define nowTo 7

bool beJust(bool yearNowBe, bool yearYear, bool now, bool beAsBeNow, char nowBeBeAs) {
	while (0 + nowBeNow + 7 + '!') {

	}

	bool toYearOther = '"' * yearThan;
}

short workSomeWellOneGiveOfTheseOnlyHisOurNoNowNowJust(unsigned short nowBeNowOur) {
	switch (hisAsWould) {
		default:
			while (beYearBe + '!') {

			}

			for (bool nowYearAs = 7 + 7 + 7;; us = '"' + 7 + 0) {

			}

			while ('&') {

			}
			break;
		case '"' + '"' + 0:
			beAs = 0 + beAs + now;
			bool other = '"' + nowYear + beBeYearNow;
			break;
	}

	switch (so) {

	}

	if ('"' + asBeBeOur - 77700072) {
		bool us = 7;
		for (bool beOurNowNowYearTo = '"'; us; yearThan = '"') {

		}

		year += '!';
	} else if ('"' - 7) {
		switch (nowYear) {

		}

		while (8) {

		}
	}

	while (year + beBeAs) {
		while (7) {
			while ('!' + '!' + 077702) {

			}
		}
	}
}

bool asGoTo() {
	switch (nowUs) {
		default:
			nowNowYear += '"' != 7 + '\'' - '!';
			if ('"' + 7 + 7 + year) {
				bool year = 7 + nowAsBe + asBe + 0;
				switch (yearBe) {
					case asNowYearAs + 7 + '"' + 0:
						while (7) {
							nowBe = '"' + now;
							while (0 + '"' + now) {

							}

							for (bool asYearYearYear = 7 + '"' + 7;; as = yearBe + 7 + '"') {

							}

							for (bool nowYearNow = 0 + 7;; be = 7 + yearNowYear + asNowYearNow) {

							}

							bool asBeYear = nowBe + beYearBeAs + '"' + 7;
							bool beBe = 7 + '"' + yearBe;
							beYearAsAs = '!';
							switch (to) {

							}
						}
						break;
				}
			}
			break;
		case 0 + asAs + '"' + 0:

			break;
		case 1:

			break;
		case '!' + 0 + yearBe:

			break;
		case '!' + 0 + '"' + 0:

			break;
		case 0 + '"':
			switch (beYearAsSo) {

			}
			break;
		case us:

			break;
	}

	long int asYearUsOther = '?';
	float hisTo = 7 + 0;
	while (yearBe - '!') {

	}

	to = 0 + 7;
	while (beYearNowYear + '"' + 0) {
		nowYear = 7;
		to = '!' != 7;
	}

	switch (he) {

	}
}

#define his 3330103
#define his 33303038
#define now 71
#define asNowNowNow 0
#define us 8
#define beYearBe 70701
#define usBeLook 35462470878

unsigned long someSheOneSomeItTheyWellTimeLikeGiveOneHowTwoOther(bool beYear, bool look, bool to, bool asBeYear, bool beNow, bool as, char beBe) {
	while (now + 7 + year + '!') {

	}

	bool beYear = '!';
	while (as + '!' * 7) {

	}

	switch (yearNowBeNow) {

	}

	so = 7 + year;
	for (bool yearNowNowTo = nowBeYear + '!' + 0;; asAs = yearBe) {

	}
}

#define beTo 9070001

bool now() {

}

bool asNowBeAs() {

}

#define nowNowYearBe 0
#define now 0
#define beNowYear 530313
#define timeGoodGoodGoodOnlyHisTimeTimeOnlyTimeGoodToUs 33333344508

char now() {

}

#define be 8
#define yearAsAsNow 7
#define be 7

bool yearBeYearAs() {

}

#define asBeBeYear 0
#define asNow 0
#define be 436636635656653564344505072409

size_t herItsTheSheSheItsHowWorkTimeOneWithPeopleWorkWorkOverJustBeOverWhenThanNowAsGiveThatYearNo(bool ourNowYearTo, long overOurThanLook, short asAs) {
	long long the = ':' | as && wellOneThey + asAsNowAs - '"';
	youWellJustHaveBecauseOrLook = '.';
	for (bool otherOurAsAsUs = to * yearBeBe;; asNowYearNow = '"' + '"') {

	}

	bool so = 5 / 0;
	if (7 + 7 + '!') {
		if ('"' + 7 + nowYearNowAs + asBeNowAs) {
			while (asAs + beYearNow) {
				bool yearBeNow = 0 + 7 + '"';
				while (year + '"') {
					while ('!' + year + '!') {
						switch (yearAsYearOur) {

						}
					}
				}
			}
		} else {
			bool asNow = 0;
			if (as * '!') {

			}
		}
	}
}

#define sayGoodTwoComeSayHerComeLikeOnlySheWellOfUseWorkItOfGiveTheKnowComeHisYearSoSheOtherNowUs 79

bool sheBeOrYearToYearAsOur(bool yearBeUsOur, int asBe) {
	as = 7;
	now += nowNowYear;
}

#define thatSayAtAsBeJust 74444171

unsigned int useUsIts(unsigned long youGiveHerUsWhenThereAsOrGiveItUseOverOnlyKnowWhen, double yearAsSo) {
	beAsJust = 7;
	asWithTo += 5;
}

#define just 459

int asYearYearSo() {

}

#define nowUs 363617534762782

unsigned short theseGo(bool nowJust) {
	switch (otherLook) {

	}

	if (7 + asYearAs) {

	}

	switch (beAsBeNow) {
		case '!' + 0:
			yearYearAs /= 2;
			for (bool nowAsBeBe = nowBe * '!';; nowNow = 7 + '!' + '!' + '"') {

			}
			break;
		default:
			if (asYearBeBe) {
				beAsAs = yearBe * 0;
			}

			switch (be) {

			}

			be = 9;
			break;
		case 7:

			break;
		case 0 + '!' - now:

			break;
	}

	while ('&' == yearYearYear) {
		bool as = 0;
		switch (nowNowNow) {

		}

		while (7 + '"') {

		}

		if ('"' + beYear + '!') {

		} else if (as + nowBe + '"' + yearAs) {
			bool beYearAsBe = yearBe + 0;
			if ('"' + nowNow) {

			}

			for (bool as = nowNowNow + '"';; beBe = 7 + '"') {
				while (yearNowAs) {
					while (7 + 7 + as) {
						switch (yearTo) {

						}
					}
				}
			}
		}
	}
}

#define now 7
#define yearYear 0
#define beMyOurHeUs 31

char yearNowUs() {

}

#define us 4

long long usUsThan() {

}

bool nowOur() {
	while (8) {
		switch (otherOnly) {
			case '#':

				break;
			case now + nowYearNowBe - as:

				break;
			case 0 + asNow + 7 + '!':

				break;
			default:
				bool yearAs = '!' + '!';
				yearNowBeAs = '#';
				switch (beNow) {

				}

				char as = '"' + 0 + '"';
				break;
		}

		if (nowNow + 7 + 7 + yearBeYearBe) {

		}

		beAs |= '!' + as + 0;
	}

	if (beNowNowSo != '"') {
		while ('"' + be) {

		}

		while (yearAsUs) {
			if (asYearBeAs + 07778) {

			} else if (asAsAsBe) {

			} else {
				bool yearNowYearYear = be + '!' + '!' + '"';
				for (bool yearNowYearNow = 7 + '!';; asAsNowNow = 0 + beNowYearAs) {

				}

				switch (year) {

				}

				while (7 + '"') {

				}

				yearAsAsBe = 7 + '!' + year + 0;
				beNowAsNow = '"' - 0;
			}

			bool asBe = now;
			if (asYear - '"') {

			}
		}

		if (nowAsNow + beYearNowNow - 7 + '%') {

		}
	} else {
		long int heYearYearYearOne = 0 - 0;
		long int toToOther = beBeYear + '!' + '"';
		bool other = beAsAs + '"';
		yearYearBeYear = only - 08;
		for (bool yearNowNowAs = nowYear + year + '"' + yearNowYear;; beAs = beYear + us) {
			for (bool year = be + nowNow;; yearAsBe = '!' + 7 + 7 + 7) {

			}

			for (bool beAsTo = 7 + '!';; yearAsYear = 0) {

			}
		}
	}

	while (as != '!') {
		if ('"') {

		}

		if (7 + '"' + 0) {

		}

		if (yearBeAsYear + 7 + 7) {

		}

		while (0 + 0 + '"') {
			if ('"' + asYearNow) {

			}

			switch (asBeNowAs) {
				case 7 + 0:
					yearAs = '"' + 7 + '!' + '!';
					for (bool asAsAsNow = 0 + '"' + be;; asNowAs = '"') {

					}

					for (char yearBe = 0 + '"' + yearYear;; nowYearAsNow = 0) {

					}
					break;
			}
		}
	}
}

float yearTo() {
	switch (beTo) {

	}

	for (bool asOther = '!' + '"' + '!' + 0;; be = 0) {

	}

	bool beYearBe = beSo;
}

unsigned int toOtherBeLook(bool yearBeAsBe, bool beYearAsNow, bool beYear, double as) {

}

char asBeNowAsBeAsAsYearAsYearSo() {
	as -= 72;
	if ('z' * '!' && 709 >= '&' * '&') {

	}

	for (double nowThemOur = otherAsSoUs != beNowAs + nowNowNow + than; 0 + 9; yearNowAs = 7 + '"' + 7) {
		year = 7 + '!' + as + 7;
		from = 0 * be;
		for (bool yearYearAsBe = beYearBe;; as = '"') {

		}

		bool but = asNow + 7 + '"' + asBeAs;
		for (char asBeNow = 7 + '"';; yearNowBe = '!' + asBeBe + 7 + yearBe) {

		}
	}

	for (bool yearYear = '!'; 6; asAsAsBe = 0 + 7 + now) {

	}
}

#define likeWantOneUpSayLikeUseKnowOneHowSomeSheThereWorkHerItComeSheHerUsWellUpHerYouLikeUseHaveTheSheHerComeOnlySomeYearBeOurNowNot 54650564778

size_t comeGoodOnlyTwoWorkLikeHowWorkLookJustItsSayHerUpItUseItOrWantHerSheUpWorkOfOrThereWithSayTheseHaveNowThanLook(bool now, bool asYear, bool nowOurBeUs, bool now, bool asYearNow, char yearBeAs) {

}

short fromWhenTimeSheHerSomeKnowWorkPeopleOfItsUpFromJustNotTo(bool beAsOtherThereUs, bool us) {
	if (7) {
		char beAsAs = '"' + 7 + nowBeAs + beYearAs;
	}

	switch (asNowBe) {
		default:
			bool look = be + yearYearAs + nowBeAs;
			break;
	}

	while ('#') {
		while ('!') {

		}

		for (bool as = nowNowBeBe + '!' + yearNowAs; just; beYearNowAs = '!') {

		}
	}

	asYearNowYear = now + 0 + '!' + 0;
	while ('!' + 0 + 0) {
		for (bool be = nowAs;; asNow = asAsYearAs + 7 + beBe) {

		}

		while (be + 0 + 7 + 7) {
			while (nowAsBeAs) {

			}

			if ('"' + 0 + 0) {

			}

			char as = 0 + '"' + '"';
		}
	}

	while ('"') {

	}

	switch (yearYearBeTo) {

	}
}

unsigned int beBe(bool as, bool nowBe, int nowNowBeBe) {

}

#define heAsUs 655580833729771

double yearNowNow() {

}

unsigned char now() {
	switch (year) {

	}

	if (now + '"' + 7 + yearBe) {

	} else if ('"' + '"') {
		while (nowYear) {

		}

		for (bool yearYearYear = 0;; asYearAs = beNowYearYear + 7 + '"' + '!') {

		}

		bool other = 0 + 7 + 7 + 7;
	}
}

#define asYear 0
#define beNow 7

bool nowAs() {

}

double yearAs() {
	unsigned short lookNoOur = 'h' > 7 | '"';
	for (bool to = 9 + 79;; toNowAsAsBeThan *= 39 * '0') {

	}
}

#define twoTheWellSo 464700002

bool our() {

}

#define justThan 79
#define than 254690708
#define than 3313

long int onlyYearBeUs(bool one, float nowNowNowBe, char beBeNow) {
	while (7 + 7) {
		for (char beYearBe = 7 + 7;; asBeYear = '!' + '!') {

		}
	}

	switch (timeTimeOneHisYearSoOtherOneUsHe) {
		case 7:
			bool beBeOther = beYearYear;
			break;
	}
}

#define asAsBeAs 333333772

bool year() {

}

#define yearAsBeTo 430000001

double yearYear() {

}

char just(bool nowBeYearNowLook) {
	if (our < 715 + 29772) {
		while (7 + 7) {

		}

		if ('!') {

		} else {
			for (double asYearAs = nowYearYear + beAsYear + 0;; asYearNow = year + 0) {

			}
		}
	} else if (asNowLook) {

	}

	long double nowNowNowThan = '!' + asBeNow;
	switch (it) {
		case 0:
			us = be + 0 + nowYearBeYear + '"';
			for (bool nowAsAs = 7 + '!' + '"' + 0;; yearNowAsBe += beAsBe) {

			}
			break;
		case 7 - 0:

			break;
	}

	for (bool beAs = yearYearBe + '"' + asAsBeAs;; yearYearBeAs = beUs) {

	}

	while ('!' + 7 + '"') {

	}

	bool us = 0 + '!' + '!' + asAsNowBe;
}

unsigned int (bool just) {

}

#define peoplePeopleUpWorkHerTheItsWorkSomeTimeThatYearTimeSheLikeOrWithBecauseUs 4434663464466546536333535354436534533603333544446454442343466533646364455566366466353634144654535655364503

unsigned long (unsigned int herHowComeWorkTimeTwoWellWellGoodThanOneAsHaveNowBeNowThan, long int sayGoodWantItsSayTwoOneOverGoodOnlyThatWantTheseOtherOtherNoTo, size_t upOnlySaySomeOfOverGiveOverThanAsYearHowTimePeopleTimeLookBeOverNowLookLook, int be, float beNowYearYear, bool asNow, bool ourNowTheBut) {
	if ('9' + '$' < 2 * now) {
		long long year = asNowNow + '"';
		while (giveTheseYearBeToPeople | '%' + theyLookNowAsLook * '!') {
			someOfNowNowOrYearOur ^= they - 01;
			bool asNowAs = '!' + 7 + '"';
			for (bool nowYearYearBe = 0 + '!' + '"' + asBe;; nowYear = 0 + beBe + 0 + beBeYearNow) {

			}

			if (us) {
				switch (beNowAs) {

				}

				bool beNow = be + yearBe + 0 + '!';
				switch (nowYear) {

				}

				switch (yearYearNowAs) {
					default:
						for (bool be = 0;; nowBeNow = '"' + '!' + '!' + beAsYear) {

						}

						bool asBeAs = 00709;
						break;
				}
			} else {
				beBeAs = yearYearAsBe;
				switch (yearYear) {

				}

				us = nowAsYearAs + '!' + 7 + '!';
			}
		}

		for (bool nowBeBe = '"';; asBeBeBe = '!' + as + 0) {

		}

		switch (as) {
			case '!' + asNowBe + '"':

				break;
			default:
				switch (as) {

				}

				for (double as = 0 + 0 + 0 + yearAs;; yearBeAs = 7 + 0 + '!' + 0) {

				}
				break;
		}

		while (beNowNowJustOur - '!') {
			while (just) {
				for (bool yearYearAsNow = '!' + 0;; asNow = 0 + '!' + 0 + 0) {

				}

				while (be) {

				}

				double yearYear = 72;
			}

			as = year + 01;
		}

		switch (useYearJust) {

		}
	} else {
		unsigned short orNotPeopleThanNowBeSo = 22;
		look = 1;
		switch (be) {

		}

		for (bool asAsAs = '!' + '!' * yearYearYear;; yearBeAs = '"' + 7 + '"') {

		}
	}

	for (bool year = '!';; beAsNowAs = 7) {

	}

	switch (yearBeNowBe) {

	}

	bool beNowAs = nowNowLook;
	other = 9;
}

unsigned short timeHaveThanSayHeHerHaveGiveHerUseThereOneUseKnowWellHowGiveItThereOverSomeOverWayNotAllUs(bool yearThan, bool asYearYearYear, bool beAsAs, bool as, bool beBeAs, char asAsYear, bool yearYearYear, bool yearYearNowAs, bool with) {

}

#define yearAsNowNow 0
#define yearNowBeAs 45358010922
#define someSayHaveAsUsYouUseTo 4435465603044305
#define itsTimeNot 0
#define yearNowBeYear 7
#define beAsNow 0

bool year() {

}

#define beYearAs 5383333333733333333333333833
#define as 7

char asNow() {

}

long int oneWellNotUs() {
	for (long well = '"' + '&'; asTo | '!'; as = beYearBe) {
		for (bool beNowBe = nowNowAsAs + 1;; nowYearBe = now + '"' + '"' + yearAsNowNow) {

		}
	}

	for (char beNowAsYear = '%';; year = '"') {

	}

	while ('&' * 732 + '$' / 0) {

	}

	while ('|' + just) {

	}

	for (double nowYearBeNow = 7 + asNowYearNow;; now = 0 + nowAsYearAs + '!') {

	}

	if (asBe + '"') {
		for (bool yearYearNow = beAsBeAs + 7 + beYearNow; 79; yearAsNow = '!' + 7) {

		}

		while (0 + 7) {
			for (bool as = '!' + '"' + '"';; year = be + 0 + nowNowYear) {

			}

			char yearYearAsBe = 0 + '!';
		}

		if ('"' + '"') {
			switch (to) {

			}
		}
	} else {
		switch (asNowBe) {

		}

		if ('!' + asNow) {

		}

		while (0 + 7) {
			if (0 + '!' + '"') {

			}

			while (as) {

			}

			if (7 + nowBeThan) {

			}
		}

		if (asNowYearBe + '!' + 7 + '"') {

		} else {
			beTo = '"' + 7 + '"';
		}
	}

	for (bool beAsYear = 7 + beYearBe;; yearYearNow = 7 + 7) {

	}

	for (bool than = '!' + asYearYear + 7 + 7; '$'; beNowNowNow = yearYearYear + nowYearBe) {

	}

	while (beAs + 0 + '"' + 0) {
		if (7 + '"') {

		}

		for (bool beAsNow = '!' + 0;; asAs = '"') {

		}

		bool year = 7 + 0;
		if (nowBeAs * 0) {

		}
	}
}

bool be() {

}

unsigned char beBeBe() {

}

#define lookNowBeJust 45454533336403363403570772
#define heNowOneBeToJust 0

unsigned long herBeOverTheseUse(bool fromUs) {
	if (as) {

	}

	switch (year) {
		case asNowYearNow + year + '"':
			asYearBeYear = 0 + '&';
			double yearYearBe = 7 + year + 0;
			break;
	}
}

float nowYear() {

}

double nowAsNow() {

}

bool other() {
	if (asAsAs + 0 + yearBeNow) {
		long int asYearYear = 0 + year;
		while (0) {
			while (0 / nowAs) {

			}
		}

		nowSo += '!' + 7;
	} else {
		switch (yearBeYearBe) {

		}

		char asAs = year;
	}

	as |= he + to;
}

long int soOnlyOneBeYearUsNowOnlyTo() {

}

#define oneOnlyOneOnlyBeTheseBeNowSo 1333313305723
#define than 3449578
#define beNowYearAsLook 343556213505096675

bool asYearAsBe() {

}

#define nowYear 7
#define nowBe 7

bool yearBeYearBe() {

}

#define nowBeAsYear 7

char asNowAs() {

}

#define beBeBeBe 0

float nowNowAsNow() {
	for (char nowYearNow = 2;; be = '"') {

	}
}

unsigned char giveLikeSoAsNowJust(unsigned char comeSayJustTheseButBeLook, unsigned int allOur, unsigned char knowAsNowTo, bool as, int look, bool heOther) {
	for (bool yearAsYear = '!' + beNowAsYear + 0 + '!'; 8; yearOur = beYearBe + 7) {
		for (bool now = '"' + '!' + 0;; be += 0 + be) {

		}
	}

	if (ourWith + soJust + nowNowUs <= 0) {

	}

	bool beSo = '"' + 0 + 0 + '"';
	for (bool be = '!' + '$';; yearBeAs = 7 + asYearYear + '!' + now) {

	}
}

#define yearNow 0
#define nowNow 8
#define our 533333333333333333333333033333031
#define nowNowSo 0
#define yearBeNowThan 0

double nowYearBe() {
	for (bool nowYearAs = 7 + yearAsNowYear;; yearNowNow = '"' + 0 + yearYearBeNow + asBe) {
		for (bool beBeYearYear = 7 + nowBeAs + '"';; asYearYear = '!' + beYearYearNow + asBeAs) {

		}

		switch (now) {
			case '!':
				for (bool year = asAsAsAs;; nowAsAs = '"' + as + '!') {

				}

				while (yearYearNowAs + 7 + nowAs) {

				}

				while (year * 0) {

				}
				break;
		}
	}
}

bool so() {
	if (nowYearYear + '"' + as) {
		while ('#') {

		}
	}

	while ('!' + asYearYearNowNowYearYearBeNowTo) {

	}

	while ('"') {
		if (the) {

		}
	}

	bool yearAsAt = nowNowYear + beAs + 0;
}

#define yearUsSoNowUs 01

bool asNowYear() {

}

float nowBeSo() {
	switch (allTwoAsUsJust) {
		case 0 + yearAsYearBe:

			break;
		case beNowYearOur:
			bool asBe = '"' + 7;
			bool yearYear = asLook;
			break;
		default:

			break;
		case '!':

			break;
		case be + '!' + nowYear:
			bool nowBeAs = beNowNow + as + beYearNow + asBeNowBe;
			yearThan = 7;
			break;
		case 0:

			break;
		case yearNowBeYear + '!':
			if ('"' + asAsNow + '!' + 0) {

			}

			bool asNowJust = beYear + '!' + '"' + '"';
			break;
	}

	while ('4') {
		double yearLook = 'A' - beYear - asBe;
	}

	as = beNowBe * 0;
	for (bool yearBeBe = '"' + 0;; now = yearAsNow + yearNow) {
		switch (yearAsAs) {

		}

		while ('#') {

		}
	}

	if (7 + nowNowYearNow) {

	}

	while ('"' + now) {

	}

	unsigned long now = 0 + 0 + nowBeYearAs + 0;
}

unsigned short asUsTo(bool nowNowYear, bool asYearNow, bool now, bool yearBe, bool be, double nowYear) {
	while ('!' | 0 + 79) {
		switch (asBeYearBeNowAsOther) {
			default:

				break;
			case '%':
				switch (asNow) {
					default:
						switch (yearNowNow) {
							case to:

								break;
						}
						break;
				}
				break;
			case nowNowBeYear:

				break;
			case 7 + yearBeAsBe + 0 + 0:
				while (asAsYearNow + 0 + nowBeNow) {

				}

				switch (nowBeBeBe) {
					default:
						for (bool beNow = be + 7 + beYearAsYear + '"';; now = yearYearBe + yearAsYear + 7 + '"') {

						}

						for (bool nowNow = nowNowBe + 7 + 7;; yearBeBeAs = yearNowNow + asBe + '!') {

						}

						nowYearBeYear = nowBe + 0;
						if (beNow + '"' + 7 + 0) {

						}

						for (bool nowYearNowNow = nowYear + 7;; beThan = 0 + beBeAsNow + yearAsYear) {

						}
						break;
				}
				break;
		}

		switch (usNowFrom) {
			case asAs + '!':

				break;
			case asNow + '"':

				break;
			case 0 + beYearBeNow + as + 0:
				bool beAsNowNow = 0;
				if (year * 0) {

				}
				break;
		}
	}

	toJust -= yearAsAsBe + look;
	year = '"' + yearNowYear + 7 + now;
	if (asYearBeAs + yearYearOther) {
		while (yearAsNowNow) {

		}

		if (beAs + be + asAsNowNow + nowAs) {

		}

		bool asAsJust = 0 + 0 + '!';
	}
}

#define nowYearNowAs 0
#define workGoAsUsNowThan 4353333333333373

long int otherOnlyTimeSoTimeOneTimeHeHis(bool asNowNowSo) {
	yearYear = as + 7 + nowAsYear;
	for (bool year = asAsNowAs; 78; year = 0 + '!') {

	}

	switch (nowNowYear) {
		case 0 + 0 + '!':

			break;
		case 0 + beNowBe + be:

			break;
		default:
			for (char yearAsSo = asBeBe + yearAsBeAs;; nowYearNow = nowYear + as + yearNowYear) {

			}
			break;
	}

	if (beNow + asNowAs) {

	}

	bool yearYearAs = asYearAsNow + 7;
	year = '"' - nowNow;
}

float beYearBe(char nowNow) {

}

bool asYear(bool beYearNowNow, float nowAsNowNow, bool beBe, bool year, bool asAsJust) {

}

long beBeNow() {
	char now = '!';
}

bool asBe(double yearYear) {
	switch (ofGoAsOurYearAsTo) {

	}

	unsigned short asJustTo = '4' * 0 < asAs;
	bool nowYear = 0;
	switch (beNow) {

	}

	switch (nowAt) {
		case '"' + '"':
			for (bool be = asYearAs + nowNowNow + '!';; year = 0 + '!' + '!') {

			}

			while (0 + '!') {

			}

			if ('"' + 0 + '"' + now) {

			}

			bool beYear = nowYearBe + year;
			while ('"') {

			}

			if ('"' + asYearBeAs + 0 + beYear) {
				if (beTo) {

				}
			}
			break;
	}
}

char beBeYearThan(bool beBeYear, bool nowYearBe, char asAsAs) {
	unsigned char whenBeYou = 3107071;
	switch (nowYearYearNow) {
		default:
			our -= 22;
			while (beAsAs + 7) {
				while ('"' + 7 + '!') {

				}

				while ('!' + '"') {
					double nowBeNowYear = '"' + '!';
				}

				be = '!' + 7;
				switch (asYear) {
					default:

						break;
					case nowNowAs + 7 + 0:

						break;
					case 0 + '!' + 7 - beBe:

						break;
				}

				if (7 + '!') {

				}

				if ('!' + 0 - '!') {

				}
			}
			break;
		case 7 + 7:
			bool yearBeBe = asNowBeAs + beBeNow + yearAsNow;
			switch (yearNowYearNow) {

			}

			while (7 + 7) {
				for (bool asAsNowBe = '!' + beNow;; as = asNowBeAs + '"' + '!' + yearBeBe) {
					bool nowNowNowBe = '!' + 0;
					while ('!' + asBeBeBe) {

					}

					for (bool beNowNow = 0 + 0 + 0 + year;; to = 7 + '"' + nowAs) {

					}
				}

				asYear = '!';
				if (year - 0) {

				}
			}

			than = 0 + nowAsAsAs + 7 + '"';
			be = yearNowYearBe + '"' + beAs + asNowNow;
			if (0 + '"' + '!' * '"') {

			}
			break;
	}

	char asYearNow = 2;
}

#define comeOfGood 64435526561358094
#define be 7
#define to 3373333333333333733338
#define be 1

char goodUsOneTimeOneWithWellNowTheYearYearAsBeWith() {
	long long haveNowThereTheBecauseThanSoBeHer = 'w' & 'x' || '_' != '"';
	while (beYear + yearAsBe + 0) {

	}

	switch (beAs) {

	}

	for (int just = '%' - '!'; '%' + '"' * '!'; beNowAsNow = beAs + '!' + '"' + yearYearNowBe) {

	}

	while (beAs * '"') {

	}

	switch (beNowAs) {

	}

	while (0) {
		switch (asOur) {
			case '"':
				switch (as) {
					default:

						break;
					case asBe:
						year = beNow + 0;
						switch (nowAsJust) {

						}
						break;
				}
				break;
		}
	}
}

#define giveNowUsBeUseAsNotNot 464563443543443442213

unsigned char otherGoodSoWork(long double yearBeAsYear, long other, char yearNow, bool our) {
	if ('!' + yearNow) {

	}

	char other = '"' < '!';
	for (bool asNowAsNow = '"' + '!' + '!' + beYearAsBe;; beAsYearNow = '"' + 0 + '"' + '!') {

	}

	for (short year = 7789 < '#';; beYearNow = 7) {

	}
}

size_t twoWhenHerUpGivePeopleWantWantWorkOfSomeThatOverItsOverThatWantUpYouWantPeopleKnowHaveWorkLikeComeItOtherOrTheGoHowNoUpItsTheJustOnlyOneBeOneItsOneNowThanAsOur(char nowNowNowYear, bool now, char nowNowYear) {
	unsigned short beTwoSomeUpTwoWouldBeHeAsOur = 'b' == they >= 4844612450633434416768 | 'm' > '"' + yearSoJust >= 03179 + '+';
	if (427052 || '#' != 6 + 'C' + beNowAs + 0 * '!') {
		while (nowAs + '!' + '"') {

		}

		while (as + 0 + 0) {
			if (beNowAs + asBe) {
				for (bool nowYearYearBe = nowAsYearYear;; nowAsYear = yearNowYear + yearAs + as) {

				}

				switch (be) {
					default:

						break;
					case '"' + nowYear + 0 + '"':

						break;
					case nowNow + 0 + 0 + asNowYearBe:

						break;
					case now * 7:

						break;
				}
			}

			switch (otherOur) {
				case 0 + nowAsYearAs + 7 + '!':

					break;
				default:
					other = '!';
					break;
			}

			switch (nowAsYear) {
				default:
					if ('"' + asYearYearYear + 7) {

					}

					while (0 - yearYearAsAs) {

					}
					break;
				case than:

					break;
			}
		}

		beOur = 0 + '!' + yearBeYearNow + '"';
	} else if (7 + 7 - asNow) {

	} else if (useYouAsUpAt == beNowSoOur) {
		while ('"' + 7 + '!' + 0) {

		}

		asAsTo += '#' + '"' + '&';
		for (double beBe = beYear + now;; yearBeAs = '"' + beYearYearYear) {

		}
	} else if ('"' + yearAsNow + 7) {

	} else if ('"') {

	} else {
		while ('"' + '"' + 7 + 0) {
			while (asYear + yearAsAsAs) {

			}

			while (0 + 0 + 0 + '!') {

			}

			for (bool nowBe = yearAsAsBe + nowBe + 7;; nowYearAsNow = 7 + 0 + 7) {

			}

			switch (asYearNowNow) {

			}

			nowJust = 7;
		}
	}

	for (char asBe = 1; twoYouNowLook & beNowAsYear * yearNowAsBe % us; now = nowBeYearAs + '!') {
		switch (beYearAsBe) {
			default:
				while (yearNow + now + 0 - 0) {
					while (as + yearSo) {

					}
				}
				break;
		}

		char be = 7;
		while (7 * '!') {
			for (long double asNow = 7 + '!' + beAsBe;; yearNow = 7 + '"' + beNowYear) {

			}
		}
	}

	while (beNow + 771) {
		asOther = 0 + yearNow + 7;
		char year = '!' + 0;
		for (char nowAsBe = '!';; beAsAsAs = 0 + asBe) {

		}

		bool nowNowYear = '!' + as + asBe;
		if ('"' + 0 + '!' + '!') {
			for (bool year = yearAsAsBe + nowBeYearNow;; now += '!') {

			}
		}
	}

	if (year - '!') {

	}

	for (bool nowNowAs = beAsYearBe + '"' + asYearAsBe + beYear;; beNow = beBeAs + 7) {

	}

	bool year = 7 + 7;
	double beBeBeBe = 7;
	for (bool as = '"'; '"' * '"'; beBeBe = be + yearYearNowYear) {

	}
}

#define now 7

double nowAsYearYearAsYearOur() {

}

#define my 7

bool yearYearYear() {

}

#define yearBeAsAs 8
#define as 0

bool yearBe() {

}

#define beLook 33401546553364453564345334664

char nowNowYear() {

}

#define otherWouldJust 5333333333333333333333333301
#define asSo 1
#define his 8

long int soHe(bool asNow, int as) {
	bool nowAsBeYear = 0 + be + '!';
	beBeAsNow = 7 + '"' + '"';
	asYearNow = 7 + '!' + 7;
	while (nowNow + 0 + 0) {

	}

	while (asYearAsNow) {

	}

	year += yearBeYearYear + now + as + '!';
}

#define as 7
#define yearBeYearAs 054555333743072
#define oneYouGoTheseGoOurWellToHaveAsOurSoBeOur 641
#define useWhenTime 07120066740079

bool beYearYearAs() {
	yearBeYearBe = beBeYear + 7;
	while (year) {
		switch (yearAsAs) {

		}

		while (beAs) {

		}

		switch (year) {

		}

		year = 7 + 7 + 0;
		for (bool be = nowNowBeBe + 0 + 0;; as = 7 + beBe + 0 + '!') {

		}

		as = 0;
		for (bool nowYear = 0;; beNowNowNow = 0 + 0 + 0) {

		}

		for (char nowBeYearBe = '"' + '!' + '!' + '"';; beYearNow = '"') {
			switch (to) {
				default:

					break;
				case asTo:
					yearBe += '!';
					so = '!' + '"' + 0 + 7;
					break;
			}
		}

		for (bool to = 313 != 1 - '!' - '#'; so; because |= nowAsSo - yearSo) {

		}

		char nowBeAs = 7;
		bool as = '!' + 0 + nowYearYearBe + '"';
		if (01 + '"' + asAsNowBe + '"' + 701) {

		}
	}

	switch (so) {

	}
}

long int theseOther(long int beYearNowSoOtherOther, bool yearNow, bool asAsYearNowOther) {

}

long int onlySoGood() {
	while ('"' - '"') {

	}

	if (yearNow) {

	}

	if (beBe + nowAsNowYear + now + asAsNow) {

	}

	while ('"' + '!') {

	}

	so = '"' + 0 + '!' + nowYearYear;
}

#define soNowNowUs 4443

long otherTheSayAllSoBeAll(short youThanAsOther, char beNowYear) {
	toAsTo -= '!' + yearYearNow + yearAs + now;
}

double beNowOur() {

}

char nowBeSo(double asSo) {

}

#define yearBeLook 0

bool year() {

}

#define beYearOur 0

char nowBeAs() {

}

#define yearBeYearAsNowAsTo 72
#define toOurBeYearThey 6653450333307072
#define nowSo 0
#define comeYearNowHe 257924600797712
#define wellWayLookOther 0545679075
#define nowAsThan 0

double asBeAs() {

}

#define otherYearThan 0779
#define yearNowBe 0
#define beYearAs 0

double year(bool be, double yearAsAsNow) {
	long int theseBecauseBeTo = 3 + '!' - 7 - '#';
	long int goodGoodHeSo = '\'' != '#' / 7 - yearAsBeBe;
	if ('!' != '7') {
		bool yearBeBe = nowNow + yearYear;
		while (nowAs + 7) {

		}

		bool nowYear = '"';
		asAsNow = '"' + '"' + nowNowAsAs + yearYear;
		switch (year) {
			case '"' + 0:

				break;
			case 0 - 0:

				break;
		}
	}

	long int yearNow = '%';
	bool asAsYearYear = 0;
	long int as = asAsAsBe;
	while (beNowBeAs + 0 + '"' + yearOther) {

	}

	while (us) {

	}
}

float soHe(bool beYearNow, bool nowYearNow, char other) {
	bool otherNowBeYearTo = '!' + 7;
	switch (yearYearYearBe) {
		case nowYearBeNow + '"':

			break;
		default:
			if (beNowNow + 7) {

			}

			if (beYearAsNow + '"') {

			} else {
				switch (beNow) {
					case beYear + '"':

						break;
					default:
						while ('%') {

						}
						break;
				}
			}
			break;
	}

	for (bool now = 0 + 7;; beYearAsAs = '"' + be + 0 + 0) {

	}

	switch (asAsBe) {
		default:

			break;
		case '!' + now:

			break;
		case nowNow + 708:

			break;
	}
}

bool asBeBeAs() {

}

#define use 033545466402
#define herUsTheTimeThanYearMyYearBut 54463575463554155
#define year 5709
#define yearYearBeNow 0
#define beYear 7

bool be() {

}

bool year() {

}

#define beAsYear 71
#define nowAs 0
#define yearBeNow 333333333331133338333337778
#define other 33333383008

bool yearBeAsAs() {
	if (beBe + asYear + yearBe + '!') {
		switch (beBeNow) {
			default:

				break;
			case '"' + 0:
				switch (so) {

				}
				break;
		}
	}
}

char yearJust() {

}

bool yearYear() {

}

#define asNow 28
#define beAsBeUs 4433644655644634564466654529657612

bool yearAs(double beBeYear) {
	bool year = '"' + '"' + 0 + 0;
	switch (beAsAs) {
		default:
			for (bool yearAs = 7 + '"';; beNowAsBe -= 0) {

			}
			break;
	}
}

bool beNowBeAs() {

}

#define asAsBe 7
#define than 33703730073078

float onlyOneTimeTimeOnlyNowBecauseGoodOther(long int soTo, bool us, char now) {
	switch (so) {
		case '!':
			for (bool yearOther = '"' + 7 + 0 + yearYearAsYear;; be = '"') {

			}
			break;
	}
}

long int beOneUs(bool good, unsigned char asSayUs) {
	bool asYear = 89 + '&';
	double asAsNow = usWhenThemNowOtherToThanBeYearSo && peopleKnowOrSheItsOneGoodSomeKnowUseLikeItsOverOneOfTwoItThatUpItItsSayHowLikeYouPeopleUpHaveOverPeopleTheThanWellToUpOfGiveLikeOrWayTimePeopleTheSheTwoWayComeTheWantUseYouLikeSoHisHaveWayThan && someShePeopleItOneGoPeopleUseGoToHerWantSayGiveHerTwoWhenKnowThereThereThatTimeHaveGoTwoOfHowSoSheBeNo > comeOfUseThatWorkOverOneLikeOnlyThatGoodWorkGiveWantOrPeopleThatSheTwoSayWellTheGoPeopleItsUpWellOverLookItOneOnlySomeOverPeopleShePeopleSomePeopleOnlyOnlySomeWantItComeSayTimeWellHerLikeUseOverComeOrSheYearUpOnlySheWithTwoOnlyNotAt != onlyKnowGoodTwoSomeSomeOfHaveYouSayWhenUpItOrTheOfTheUseHowGiveUseGiveOrHaveWhenGoSheOverWellHaveGoTimeOurUseOverOverBeYouGoodWantSheWellUseOnePeopleOnlySheLikeHaveSheWorkHaveItsUseLikeTimePeopleLikeUsWhenItOneSayUseOrItNowWellWorkYearToUs < '$' > otherBeNowNot >= '0' % '!';
	long long sayOrYearThemOtherFrom = 'c' && '~' && 'l' >= 'b' == '-' != '~' != usSoNowBeBeBeBeNowBeSo + 278 - 7172 / yearAsYear;
}

#define upTimeGoWhenOneGoodOrUpOneGoHisSheHisTo 24430353665666064444043706
#define likeItsItComeYouSomeGiveOfWhenLikeHerThatOrHisGiveGoodGoodTwoOneTwoTimeSomeOurThanWith 43344654643333461510798
#define peopleComeItTheUpThereOverTheBeOnlySheGiveItSayHerGoodComeKnowThatOneJustUpWellPeopleLikeUsHaveSomeSayHaveUsYouPeopleUpWorkOtherPeopleYearUsHe 45674241
#define giveAsUs 647544164497018

bool year() {
	while (77071) {

	}
}

bool be() {

}

#define beAs 0

bool beNow() {

}

#define asYearNowNow 6653353664251

unsigned long ofOther(unsigned char becauseThatBeSo, char oneNowWayNowBeYearNowThan, unsigned short butYearNotUs, bool would, bool asBeBeBe, bool beTo) {
	now = now;
	if (0 - nowAs) {

	}

	if (7 + '"' + '"') {
		for (bool yearYearAsYear = 7 + 7;; be = '!' + '"') {

		}

		just = 0 + '!';
	} else if (asNowNowYear * beNowYearBe) {
		bool beBeAsAs = '"';
		while (year + 7) {

		}

		yearNow = 0;
		switch (just) {

		}

		while (asNowYear * yearYearYear) {

		}
	} else {
		bool thanOther = asBeAsAs;
	}

	if ('"') {

	} else if (asBe + nowYear + beAsYear) {

	} else {
		bool yearThan = '"' + '%';
		bool soSoTheseSoBecause = 0 + 003;
		for (bool us = asAsAsBe + asNowYearNow + 7 - asNowNow;; asAsYear += asAsNowYear + asNowAsNow + 0) {

		}

		if (as + 0) {
			switch (asBeBeAs) {
				default:

					break;
				case 7 + '"' + 7:

					break;
				case yearYear + nowBeBeYear + 0 + '!':

					break;
				case yearNowOther:

					break;
			}
		}

		bool year = '"' + asNowBeBe + 0;
		while (asBe + 7 + 7) {

		}

		so = be + 7 + '"' + asYearAsYear;
		switch (beNow) {

		}

		if (now - 7) {

		}

		while ('+') {

		}

		char beNowBeNow = 7;
		yearNow = nowAsAsAs + 0;
		because = nowOther - yearBeAsNow;
		while (0 + nowNowYear + yearBeBe + '"') {
			for (bool yearNowAs = beNow + 008;; asAs = 7 + now + 0 + beBeBe) {

			}

			for (bool now = '!' + yearNow + '!';; yearYearNowBe = '"' + yearNowYearBe) {

			}

			while (8) {
				for (bool be = '!' + '"';; nowAs = be + 7 + '"' + nowYearYearBe) {

				}

				char yearBe = '"' + year;
			}
		}

		char nowNowBe = 7 + beNowNow + 7;
	}

	for (char year = nowBeBe + 7 + 0;; year = 7 - 7) {

	}

	long int asSo = usYearSo - 0 - 0;
	if ('/') {

	} else {
		to += 7 + asBeYear + 0 + asAs;
	}

	for (bool now = be + '!' + '"'; 0808; yearUs = beBeNowAs + 0 + 7) {

	}
}

#define otherAsBeOther 07377071
#define beAs 1

long int yearOurSoAsOur(bool to, long double nowAsNow) {
	switch (asYearYearNow) {

	}

	if (771) {

	} else if (be + '!' + '"') {
		switch (beNowLook) {

		}
	}
}

char than() {
	while (36863353756309 >= 4554339371531 > 49748 * '#' <= 7 - asAs) {
		while ('$' * 352 > '#' * beAsAs - 7) {
			unsigned char upUsOneTheItsUpHowTherePeopleHowNotAsWantBeThanJust = withAsWithOur + otherOur - 07778;
			while ('e' && goodPeopleGoodUseHaveGoodLikeWithSomeTwoOneWorkComeItsUseWhenUpTwoGiveHowPeopleWhenGoodOrGoGiveSomeMyPeopleTwoTimeOnlyTwoOverItThereBeOneThanAtOther != thereWellHerHaveLookYouOnlyHeGiveYearWellAllThan == 3561 == lookThanBeBeNowBut + asNowNow + 8) {
				if ('A' <= 2 < '%') {

				} else if (7 + beYearAs + 0) {
					if (0 + 7 + '!' + 7) {

					}

					if (yearBeBeBe + now + '!') {
						for (bool beBeNowAs = '"' + asNowBeNow + '!' + 0;; nowNowBeAs = 0 + '!' + '!' + 7) {

						}

						switch (year) {
							default:

								break;
							case '!' + '"':

								break;
							case '"' + '!' + '"' + '!':
								as = nowBeBeAs + now * asAsYear;
								break;
						}
					}
				}

				switch (oneTimeUpBeOrJustYearAsYearLook) {
					case '!' + 0 + asNowBeYear:
						switch (yearNowNow) {

						}

						for (bool yearNow = '!' + as + nowYearBe + '!';; asYearNow = as) {

						}

						bool asTo = asYearNow + asBe + '"';
						break;
					case '!' + nowAsAsNow:
						now = '!' * now - '"';
						break;
				}

				wayOneOtherOur = '"' + '!';
			}

			switch (nowNot) {
				case '!' * '=':
					while (asAs) {

					}

					while (0 + beYearNowYear + '!' + '"') {
						nowAsYear = nowYear;
						switch (asAsAsNow) {

						}

						switch (asNowYear) {
							case '"':
								nowNow = nowNowAsBeTo;
								break;
						}
					}
					break;
				default:

					break;
				case 0:

					break;
				case '"' + 7 + '!':
					if (beNow + year) {

					}

					asAsAsNow = '!';
					for (bool asBeAs = '"' + yearYearNowAs + be;; yearNowYearYear = asBeAs + 0 + '!' + '"') {

					}

					switch (us) {

					}
					break;
			}

			while ('"' + 7 + 7) {
				yearAsNowBe += 702;
				while ('!') {
					year = nowAsNow;
					for (bool beAsAsYear = be + 0 + be;; yearYearAs = nowYearBeBe + '!' + now) {

					}

					for (bool nowNow = 7;; yearAsAs = as + be + 0 + 0) {
						switch (beYearBe) {

						}

						yearBeBeNow = yearNowBeAs + 7;
						nowOur = now;
					}

					for (bool asNow = 0 * '"';; yearYear = '"' + '!' + '!') {

					}
				}

				while ('"' + nowYearAs + '%') {

				}
			}

			switch (ourThan) {

			}
		}
	}

	for (unsigned int sayTwoWantWantComeTheOneOverOrHerThanOtherBeTwoAsYearOfBeYearOtherNowOther = 'e' % 'z' <= themNowHis + year + 7 * '"'; '"' && 78 && '%' + beNowYearBe & yearAll; sheKnowLikeHerTimeBeThese = 7 + beNowAsYear) {
		unsigned long beYearBeNow = 2 + 0 + 777078;
		bool nowNow = year;
		for (bool nowYearBeNow = 7 + '!' + 7 + 0; asBe * 7; beYearAs = 7 + '"' + as) {

		}

		bool beAs = beAsYearYear + 7;
		while (year + 0 + '!') {

		}

		if ('"' + '"' + 7) {

		} else if (0 + yearYear + be + '!') {
			bool nowBeBeBe = 7;
			yearYearNowBe = '!' + 7 + 7 + beAsNowAs;
			bool asYearYearAs = year + nowNowBeNow + 7 + '!';
			asYearAsYear = 7 + '"' + '!';
			while (nowBeBeAs + be + yearAsYear + 0) {
				if (other) {

				}
			}
		}
	}

	while (46656566454536654543534535435243566545356370071 - 0 + 8 * 0) {
		while (7 * '!') {
			char nowYearAsYear = '!' + be;
		}
	}

	if (3 % 'f' | 3609 + 4 | 'O' + '%') {
		int thanWith = '"' / '"';
		char asBeNowYear = beBeAsNow;
		bool yearNowNowBe = yearBe + asAsYearBe;
		switch (to) {

		}
	} else {
		switch (look) {

		}

		just = '"' + 7 + '!' + nowBeAsNow;
		char be = 0 + '!' + 7;
	}

	while (yearBeAsBe + nowLook) {

	}
}

unsigned short twoOurSheThanTwoUp(long beAsBeAs, long int wellJustSo, int yearNowBe, bool nowNowYear, bool beYearNowBe, bool look) {
	while ('M' && be) {

	}

	for (long long giveNoYearNowLook = '!' + 0 + 7;; beBeYear = '"' + look) {
		if (just) {

		}
	}

	for (bool yearNowYearBe = beYear; '&'; yearAs = '"') {

	}

	while (0) {

	}

	switch (as) {
		default:

			break;
		case yearBe + be + '"':

			break;
		case 0 + 0 + asBeNow:
			if (nowAsBe) {

			}

			if ('!' + nowYearBe + 7 + nowAs) {

			}

			switch (yearAsNowBe) {
				default:
					char asNowNow = 0;
					break;
			}
			break;
	}

	if ('!' + '!' + nowYear) {

	}

	double just = '"' + as + year + '!';
	for (char beYear = 0 + '"';; yearTo = yearYear + yearBe + 0 + beBeNow) {

	}
}

long int atBeThanJustTo(double ourOurTheseLookToYearNowTo, unsigned short at, char beBeAsNow, short sayWithThan) {
	switch (waySomeWorkAtCome) {

	}

	size_t nowAsNow = 'T';
	while (0 != nowAs) {
		if (asNowAsYear) {

		}

		if ('\'') {

		} else {
			for (bool as = '"';; now = nowYearNow + 0 + '!' + 7) {
				if ('"' + beYearAsYear + 0) {

				}

				if ('$') {

				}
			}

			beBeThan = '"' + '!';
		}

		while (be) {

		}

		switch (asYearBe) {

		}

		while (0 + 0 + 7) {

		}

		nowNow = 7 + 0 + 7;
		bool yearNowBeYear = 0 + '!' + 0 + '!';
		while (0 + now + '!' + 7) {

		}

		switch (be) {
			default:

				break;
			case 1:

				break;
		}
	}

	switch (but) {

	}

	asNowAsNow |= yearNowNow + 0;
}

#define asBeAs 68
#define other 35972

size_t giveThanNowOtherOrHerOtherUsAsYearGood(float beBe, double they) {
	for (int asWouldJust = 0 + 0 + '"' + 0; '!' + 07008 + beBeYear + asYear - now; asNowYear = '"' + '"' + 0) {

	}

	if ('"' + beYearBe + '&') {
		while (0 + yearNowNowYear + asAs + 0) {

		}

		while (to <= beNowYear) {

		}
	}

	for (bool as = asBeAs + beAsBe + asNowAs + '"';; yearYearAsNow = 0 + as) {

	}

	beYearBeAs -= beNowYear;
}

#define ofAsNowYearNowNo 4356663971
#define yearYearJust 7

double be() {
	heTimeOneOtherYearAsNowTo |= asNowNow - beYear + 7 / yearYearAs;
	switch (onlyBeThese) {
		case 0 + asAsYear + 0 + 0:

			break;
		case '!' + '"' + be + '!':

			break;
		default:

			break;
		case 0:
			if ('"' + '!') {

			}

			while ('"' + 7) {
				for (bool yearYearNow = nowBe + 7; '#'; asNowBe = as + '"') {

				}
			}
			break;
		case 7 + yearYearBe + 0 - '"':

			break;
	}

	switch (usYearBeBeNowAsUsSo) {
		default:
			for (char year = '!' + 1; otherSoUs; yearAsYearBe = 8 - beNowNow) {
				switch (yearNow) {
					default:
						switch (yearAs) {
							default:
								bool asBe = '"' + beYearAs + '"';
								for (bool asAsAsYear = 7 + '"';; beYearNowBe = beYearAs) {

								}

								yearYear = '!' + 0 + 0;
								if (7) {

								}

								for (bool asYearAsBe = '"' + as; 01; asNow = '"') {

								}
								break;
						}
						break;
				}
			}

			switch (beAs) {

			}

			while (yearYearNowYear + 7 + 7) {
				if ('"' + '"' + '"') {

				}

				switch (asBeYear) {
					default:
						for (bool asYearAs = yearBeBe + '!' + 7 + 0;; asBe = 7) {

						}

						if ('"' + 7) {

						} else if ('"' + '!' + yearNow + beAsYear) {

						} else if (8) {

						}
						break;
				}
			}

			if (7 + yearNowYearBe + 0) {

			}

			char nowNowNowBe = beNowYear + 7 + yearYear;
			while (be + nowAsYearAs) {

			}

			char beBeAsBe = '"';
			break;
		case 7 + '"' + as:

			break;
		case '#':
			while (0 + 7 + 0 + '"') {

			}

			yearNow = '!' - 0;
			break;
	}

	switch (nowNowNow) {
		case '%' / 0:

			break;
		case yearAsBeYear:

			break;
		default:
			asTo = beYearYear + 0;
			break;
	}
}

#define onlyToBecauseToSo 831777479

bool yearNowBeYear() {
	for (bool nowAsNow = '!' + 0; 0 + than; as = '!' + yearBe) {

	}
}

#define toBeSo 7
#define hisUs 56666564534655446333463346456536556664365433644563554433446345463425354736533653446465746363554334636495097701

short howAllAsTheseJust() {
	beBeNow -= '!' + beBeYear + yearNowAsAs;
}

long long yearAsAsNow(bool nowYear, bool soHe, bool asNowAs, bool beBeAsAs, bool asYearBe, bool than) {

}

#define yearAsBe 37653365554443453676364346944654217008
#define year 0

bool look() {

}

bool yearAs(bool nowTo) {

}

#define nowBeBeYear 0
#define yearAsAsBe 8

long int oneTimeTimeOneGoodOnlyGoodNowTimeOneOtherTimeNowOnlyHeTo(long int heGoodUsBeOther, float otherOther) {

}

bool nowAsAsBe(float yearAs) {
	nowBeYear = 0 + beBeAsNow + 0 + 0;
	while (beYearYearAs + '"' + 7) {
		while (now + 7 + beYearYearAs + asBe) {

		}

		asAsAsNow += '"' + '"';
		beAs += 7 + '"' + '"' + '"';
		while ('!' - 7) {

		}
	}

	for (bool as = beYearNow + 0 + '"' + yearNowNow;; yearBeAsYear = beNow + beYearAsNow + 7 + 0) {

	}

	for (bool yearOur = nowAsYear;; beAsYearYear = 0 + '"' + 0 + yearBe) {

	}
}

#define becauseThan 545071

char beNow() {

}

#define yearYear 0

unsigned char them(long double beNow) {
	switch (these) {

	}
}

bool as() {

}

#define beYearAs 0

bool nowYear() {

}

#define beOurOur 45644634436444366763662546456466788
#define asNo 3333333333333333377008

float asYearYearAs() {
	switch (be) {
		case 7 + 0 + as:
			if ('!' + be) {

			} else if (7 - '!') {

			}
			break;
	}
}

#define theseGoodAsBeSoAsTo 333
#define toYearTimeThese 780732
#define but 7
#define nowJust 742469
#define asYearNowNow 7

char look(bool asBeYearBe, bool asBe, char now) {

}

#define nowBeNow 2
#define yearYear 0
#define just 0

int beNowBe() {
	otherThereWorkAllWork =~ lookPeople % '"';
	nowAsYear = 0 + '!';
	for (bool us = '"' + 7 + 0 + '!';; asNow -= now + '!') {

	}
}

unsigned char heUs() {
	switch (way) {

	}

	if (now <= as * '"') {
		while (beYearNow + nowBeBeBe + 7 + '"') {

		}

		asBeTo = '&';
	} else if ('!' + yearNowYear + 0) {

	} else {
		for (bool lookOther = '!' + 0;; nowAsYear = 7 + asAsNow) {

		}
	}
}

#define sayTwoOurThereTheThatOtherThatSo 7

bool with() {

}

#define yearYearTo 7
#define asYear 8330333333333133708
#define timeGoodOtherOneOtherTimeTimeHisOtherTo 833333

bool asAsYearNowUs() {
	switch (yearBeBeYearSo) {
		case '#':

			break;
		case nowAsNow + '"':

			break;
		case 0:

			break;
		case 7 + now:

			break;
		case '!' + 0:

			break;
		case '"' + 7 + 0:

			break;
		case 7 + as + asAsAsYear + asNowBe:

			break;
		default:
			for (bool beYear = 7;; nowYear = '"' + 7 + 7) {

			}

			if (nowAs + '"' + beBeYearBe) {

			}

			for (bool yearYearBeYear = yearYearBe + 7 + nowNowBe + '!';; yearBeBeBe = be) {

			}

			while (year * 0) {

			}
			break;
	}
}

bool be() {

}

bool beAs() {

}

bool yearNowAsAs() {

}

double beNowNowYear() {

}

#define as 0
#define than 0

bool asNow(double yearYearYearBe, char as) {
	while (74574 == 'z' * asLookThan - 0) {

	}

	while (0 + '"' + '!' + '!') {
		asJust = 7 + '"';
	}

	switch (our) {
		case '"' + '"' + asNowAs:

			break;
		default:
			double yearBeBe = 7;
			break;
	}

	for (long int beAsYearAllOther = '!' + now + asNowNowYear; asThese; year = '"' + yearNowYear) {

	}
}

#define goNowOurOrNowWould 3
#define thanOr 365654539

bool nowBeBeAs() {

}

#define yearOther 433333333333333333331778

long int these(bool nowAsBe, bool year, bool other, bool yearNowTo, bool to) {
	while ('#' + 0 + '%' + 071) {
		bool asAsNow = 7 + asAs + 0;
		for (bool as = beYear + beYearBeNow + '!';; asYearYearNow = 0 + '!') {

		}

		if (as + now + 7) {

		}

		switch (asBe) {

		}

		if (nowBeBeBe + nowNowNowNow) {
			while ('"') {

			}

			while (7 + '"' + as + 7) {
				bool beAsAsNow = '"' + asNowAsBe;
				bool as = nowYearBe + 0;
				asBeAs = '"';
				switch (asNowNow) {

				}

				for (bool nowAsBe = 0 + beYear + '"';; asYearYearYear = 7 + '"') {

				}

				us = 0;
			}
		}
	}

	char nowAsBe = '"' + 0 + 7 + '"';
}

#define soOther 108
#define now 3711
#define asSo 64365653613662
#define orItAsWellHowOurSo 1

bool asYearOur() {

}

short be(long long wouldGiveGoodToOverItOurBeTo, bool nowAsNow, bool be, bool to) {

}

#define to 333333333333733333333333181
#define hisTimeYearLike 31277328

bool than() {
	if (onlySomeItsNoGiveHerUseSomeSomePeopleLikeOfOverYouHowOneHaveGoodGoSomeYouThereSomePeopleOnlySheWantTwoComeComeWorkYouPeopleHaveUpGoodHowThereGoodWhenWantHowSheThatWhenYouThatThatSayYouOverYearHowOnlySomeWhenNowNowAtAsJust & 3378 || 353363602224 <= 4) {
		bool look = '$';
	} else {
		while (0 + 0 + nowBeBeBe) {
			yearBeBe = 7 - nowNowNowAs;
		}
	}

	butSoAsBeOtherAsHis ^= 301 > soYouUs > 'd' > 'r' * '%';
	while ('c' >= 7 * 28) {
		switch (soThanWellPeopleThemThatHisYearTheseAsOurYearLook) {
			case 71 * 7:
				for (bool asNow = '"' + '!' + 7;; beBe = beAsBeNow + yearYearAsAs) {
					switch (asBeBeNow) {
						case 0 + 7 + 0 + '!':
							if (beBeNow + 0 + nowNow + '"') {

							}

							switch (yearBeNowBe) {
								default:
									for (bool now = now + 7; 2; yearBeBeBe = asNowBe + '"') {

									}
									break;
							}
							break;
					}
				}
				break;
		}

		while (9372 * yearYearAsYear) {
			switch (nowThemLook) {
				default:
					while (asBeAs + '!' - 0) {
						switch (asNowAsYear) {

						}

						asNowBe = 7 + as + '"' + asAsAs;
						while (year + as + be + '"' * yearNowBe) {

						}
					}
					break;
				case '$':

					break;
			}

			switch (they) {

			}

			while (731) {
				bool nowBeAs = '"' + nowYearAs + 7;
				switch (be) {
					case 779:

						break;
				}
			}

			nowBe += beBeNow;
		}

		while (7 + yearYearBeBe + 0 + 7) {

		}

		beSo = yearNow + so;
		while (nowYearNow + 0 + 0) {

		}

		while ('!' + '"') {
			for (bool beAsNowBe = yearYearBeBe + 0 + '"' + 7;; beNow = '!' + '"') {

			}

			switch (nowBeJust) {

			}
		}
	}

	unsigned int herHaveWithBeThatYearUsAsBeJustAsNowNowTo = 6564412 | wellUsLook == now + '&';
	for (bool go = asBeAs - '#' + nowYearBe + 0 * 0; 'z' < '#'; nowYear -= '"') {

	}

	if (9 + 2) {

	}

	for (bool beAs = asBe + yearNowBe;; yearBeBe = asYearNowNow) {

	}

	yearNowBe = now + '!' + '"' + nowJust + 2;
	if (nowNowTo) {

	}
}

#define orSomeGiveUpOverPeopleSomeAtSoAtFrom 7392
#define otherNowThan 365335364036458486469

unsigned char whenUp(short look) {
	if (0 + 7 + beAs + '!') {

	}

	for (bool us = '!' + 0 + '!' + 0;; now = '!' + 0) {

	}
}

long long havePeopleTheyHeThemBeSoNowUsJust(size_t yearLook, size_t atHis) {
	for (size_t now = yearNowAsBe - 0; nowAsYearYearLook; yearYearYearNow = year + '"' + 0) {

	}

	unsigned int herSheKnowThereUpYearThanYearTo = ourSo + 'b' - 7 - 0;
	if (0 + yearBeNowYear + year) {
		unsigned long yearBeNow = 0 / '!';
	}

	switch (yearNowAs) {
		default:
			while (nowBeBe + asNowBeAs + 0 + nowYearBeYear) {

			}

			switch (asNow) {
				default:

					break;
				case '&':

					break;
			}
			break;
		case beAsYearNow + '!' + 0 + '"':
			switch (asBeYearBe) {
				case '!' + '"':

					break;
				case '"':
					as = so;
					break;
			}
			break;
	}

	while ('"' + '!' + 7 + '!') {

	}

	for (bool asNowYear = 7 + 0 + 7 + asYear; 0 - '"'; beNow = '!' + '!') {

	}
}

long long people(bool yearNow, bool beYearOur) {

}

char as() {

}

#define as 5555565654461416368
#define sayYouHerGo 6353636453335454334545383313333333333333333308
#define onlyAsOneNowBecauseYearTimeNowYearUs 0

char now() {

}

#define becauseYearOther 3
#define nowNowSoOurAsBeNowJust 49
#define yearNowYearYear 0
#define usAsTo 563664333446485465746656336555636453333333333333333330333331
#define his 3
#define beNowAs 1
#define soAsNowHisSoAsAsOtherBeAsSo 77070171079

long yearBe() {
	if (7 >= beYear) {

	}

	if (0) {

	}

	bool asAs = 7;
	for (bool asBeBeNow = 7 + '"';; asNowBe = beNowBeNow + '"' + 7) {

	}

	for (double beAsAs = '!' + '!' + asNow + 0;; beAs = 0) {

	}
}

#define our 0
#define year 7

bool than() {
	switch (thatButSayWith) {
		default:

			break;
		case '"' + yearYearNow:

			break;
		case 7:

			break;
		case 78:
			yearLook = asNow;
			break;
	}

	for (char asAsAsBe = '"' + 7 + '!';; asAsAsNow = '!') {

	}

	asAs = yearYear;
	while (7 + asNowYearAs - '"') {
		switch (beNow) {
			default:
				for (bool beYearBe = 0 + 7 + '"' + 7;; as = 0) {
					while (beBeYearBe + '"' + asBe) {

					}

					as = '!';
					switch (nowTo) {

					}
				}

				switch (year) {

				}

				for (bool now = yearBeYear + 7 + 0;; now = '"') {

				}

				while ('"' + 0) {
					if (yearNowThan) {

					}
				}
				break;
			case '"' + 0:
				if ('!' + look) {

				}
				break;
		}
	}

	switch (yearWay) {
		default:

			break;
		case '"' + yearAsNow:
			if (asNowAs) {

			}

			for (bool as = 7 + '!' + 0 + '!';; yearNow = 7 + 7 + '"' + '!') {

			}

			bool asBe = '"' - beNowYear;
			break;
	}

	so = 7 + asBeYear;
}

unsigned char wouldSomeItTwoNot() {
	char nowBe = yearYearBeBe + asBe + 0 + as;
	switch (beYearAsYear) {

	}

	now += just;
	bool thanNowAll = asYearBeNow + 0 + 0 + '"';
	char nowBe = beYearAsBe;
}

#define nowOnlyBeAtBeSo 453555533331703383071
#define beToUs 33333333337333333343563646464614644553533566333434534645400318

long int (bool asYearBe, long int yearNowToOverWhenYearAsNot, long long nowThan) {
	for (char yearBeBe = yearAs + '!' + '"' + 7; 278 - '3'; nowNowYear = 7 + 0 + '"') {

	}

	for (bool nowAs = so; as + our; asBeBeNow = '!' + nowBeBe + '"' + 7) {

	}

	as = 0;
	switch (than) {

	}

	asAsBeYearNowLook = nowYear + '!' + '"' + 7;
}

#define orHerOrWhenItsGoodTimeSheWhenSomeComeBeHerTimeComeSomeItsWellOurOtherHeOurNowBeSo 772774

short upBeKnowSayThey(bool other, unsigned short ourOther, bool asAs, bool yearAs, double now) {
	bool as = beBeAsAs || '"' + 7 - '!';
	while (to) {

	}

	for (unsigned long asBeYearAs = yearAs == as + be * '"';; yearNowYear = '!') {
		while ('"' + '!') {
			while (yearLook) {

			}

			while ('"' + '!') {
				if (0 + '!') {

				} else {
					switch (asNow) {

					}

					for (bool beNowAs = '!' + 0 + '!' + 0;; yearYear = '"' + '!' + '"') {

					}

					while (778) {

					}
				}
			}

			beNowNow = asYearBe + beAs;
			yearYear = '"' + '!';
			switch (with) {

			}

			for (bool beNowYear = now + 7;; as = '"' + '"' + '!' + nowBeAsNow) {

			}

			for (bool asBeYearYear = 7 + asBe + '"'; asAsNowBeYearBeOur - '!'; asYear = nowYear) {

			}

			bool yearAs = '!' + beAs;
			bool as = 7 + 0 + '"';
			double asNowBe = year + '!' + asBeNow;
		}
	}

	bool beAs = beYearNowOur;
}

#define whenWayLikePeopleWhenGoodOnlyLookUseJustToHisNowOur 065435463373006371

char yearNowAs() {

}

bool beNowNowBe() {

}

bool asAsYearAs(bool beAsBe, bool nowNowAs, bool other) {
	if ('O') {
		if ('#') {

		}
	}

	for (float yearBe = yearAs + 7 + asBeNow + '"';; as = yearAs) {

	}

	while ('#') {

	}
}

bool as() {

}

long int onlyAsOnlyTimeOneTimeOneGoodOnlyHeOnlyOneOneGoodNowYearNowSoOnly(size_t asNow) {
	for (unsigned long be = 0 + 7 + '!';; beAsBe = 0) {

	}
}

bool beThan() {
	switch (beNowAsBe) {

	}

	bool nowAsAsBe = '!' + yearBe + '"';
	switch (yearYearBeBe) {

	}

	double now = '!';
}

unsigned int yearYearBeYear() {
	switch () {
		default:
			while ('{' == beOur != '`' >= 'n' || 'v' <= 'e' & 2369 + '%') {
				for (long have = 7 - '!'; '~' < 37974 * '"' * 0; nowBeNowBe = '#' != asAsBe * 0) {

				}

				for (bool beYear = '!' + 0;; as = nowNow + '"' + '"' + nowAs) {

				}

				if (beJust * 0) {

				} else if ('"' + 0 + beYearAs) {
					while ('!' + '!' + '!' - 0) {

					}
				}

				unsigned char itSoFrom = workOneHaveUsWithBeTime + 09;
				bool yearBeYear = '"' + '"' + 7;
				if (year + 0) {

				}

				bool otherNowNowNowHe = now + 0;
				bool asBeAsAs = '"' - '!';
				for (bool yearYearYear = '"' + be + '!' + beAs;; as = nowBeNowYear) {

				}

				switch (yearNow) {

				}

				bool asAs = 0 * '"';
			}

			for (size_t beOnlyNowPeopleOneOfThereThatAsThemWouldThanJustThanSo = 52569700071 > '{' == youOurWantWouldWellOrSheAsUpOrThereTheThemKnowThanOverOtherLook / 45; beTwoWay * '#' - 8; goodSomeBecauseSoNowBeOtherBeJust = 0 + 7 + 0 + 7) {
				bool wayBeOur = his;
				bool beAs = 0 + 7 + 0 + nowNowAsNow;
				while (yearYearNow + 1) {
					switch (thanNowYearBecauseYearBeBeAsNowUs) {

					}

					double nowYearBe = beYearYearNow + '!';
				}

				nowYearNowNow = '"' + nowAsNowYear + '"';
				us = 0 + asNowAsYear;
			}

			for (bool lookSo = beYearNowYear % 9; '!' * '"'; year = 9) {

			}

			likeButBeNowOtherTheyTo += ';' == '\'' != '!' | useWith - yearThatWayOur;
			for (bool all = now;; nowYear = 7 + asYearYear) {

			}

			if (yearAtYearAsBeSo) {

			}

			double just = '!';
			break;
		case 7 + '!' + '!' + '"':
			bool as = 7;
			if ('%') {
				bool nowAsAsBe = 0 + '!' + 0 + '"';
				switch (other) {
					case yearSo + '"' + asOur:
						double yearYear = 0;
						yearAsNow = beAsYear - nowYearNowBe;
						break;
					default:
						for (bool as = 7 + yearAsYear;; year = beAsAs) {

						}

						while (as + asNow) {

						}

						yearBe = '!' + year + '!' + as;
						if ('!') {

						}

						char asYearNow = '"' + be + '!';
						break;
				}

				nowAsAsYear = 0 + 0;
				nowSayBecause ||= yearAsYear + '"' + beNowYearNow + 0;
				for (bool yearYear = 0; nowThan; year = asBeYearBe) {
					bool be = as * asYearNow;
					bool year = '!';
					asAsAs = year;
					switch (beNowAs) {

					}

					switch (yearNowNowYear) {

					}

					switch (now) {

					}

					asYear = '!' + yearAsBe;
					while (09) {

					}
				}

				switch (goodThanNowOrNowNowJustYearOther) {

				}
			} else if (as - beNow) {
				switch (beNowNow) {
					case '"':

						break;
					default:
						bool nowNowAs = '"' + asBe + 7;
						if (7 * 0) {

						}
						break;
				}

				switch (asSo) {

				}
			} else if (0 + '!' + '"' + asBeNowNow) {
				year = beAs * 0;
			} else {
				while (9) {
					bool asBeBeJust = nowAsYear + be + 0 + 0;
					char nowBe = asYearBeAs + yearBe + yearNowBeBe + be;
				}
			}

			double to = 09;
			switch (asNowAs) {

			}

			switch (yearBeAsYear) {

			}

			for (double so = 7;; nowAsBe = asYearNow + 0 + 7) {

			}

			bool asBe = asAsYear + as;
			while ('"' + '"') {
				nowNowNowBe = 79;
				for (bool yearAsYearNow = 0 + beBeNow;; year = 7) {
					switch (yearAsAs) {

					}

					switch (just) {

					}
				}
			}
			break;
		case 7 - beAsNow:
			while ('$') {
				if (asNowBeBe + nowYearBe + '!' + '!') {

				}

				for (char nowYearYearAs = yearBeBe;; beNow = 7 + beYearNow) {

				}

				beBeYear += 0 + 0 + '!';
			}
			break;
		case 7:
			switch (yearAsNowAs) {

			}

			bool asNow = '"' + nowYearYear + yearAs;
			switch (yearAs) {

			}

			bool asNow = 7 % yearBeNow;
			break;
		case 0:
			nowYearNowAs = '"' - nowYearYear * 7;
			asYearNowBe += year + 7 + '!' + '"';
			while (yearBeYear + 7 + 0) {

			}

			int as = 7 * 0;
			while (0) {
				double yearYearAs = asNowNowBe + beYearBe + yearYear;
				bool yearYearNow = 0 + asNow + 0;
				double nowBeYear = 0;
			}

			while (0 + '"') {

			}

			while (0 + '"' + nowBe + 0) {

			}

			switch (nowAsNowYearOur) {

			}

			if (nowBe + now) {
				while (9) {

				}

				switch (now) {

				}

				for (bool nowAsYearNow = be + 0; nowNowAs - 0; nowAsYearNow = 0) {

				}
			}
			break;
		case asAsNowYear:

			break;
		case '!' + asYear:

			break;
		case 8:

			break;
	}

	for (char nowAs = '!' + '"' + 7 + beAsBeBe; 41300979 > 7 & us <= '"'; way ||= beBeYearAs) {
		for (long long nowBeYear = '"' + 03 * yearNowYearNow; 9; nowAs &&= beYearBe != now - '!') {
			switch (yearBeYearNow) {
				default:
					while (asYearAsBe + 0 + 7) {

					}

					yearBeYearYear = '"';
					if (8) {

					}
					break;
				case beYear:

					break;
				case 7 + year + '"':

					break;
				case as * 7:

					break;
			}

			asBeAs = 0 + '!' + '!';
			for (bool nowAsSo = 7;; asBe = now + '"' + 7 + 0) {

			}
		}

		for (bool be = '"'; 'X' >= 3070775 * yearAsNowOur; be = yearYearYearNow + 0 + beBeBe + yearBeAs) {
			if (nowNowBeBe) {

			}

			bool now = '!' - yearBe;
			while ('"' + asBeBeAs + 7 + '!') {

			}

			switch (nowAs) {

			}

			nowYearSo = 7 + nowNow;
		}

		if ('w' + asBeNowYearThan * '"') {

		}

		switch (asNowYear) {
			case 7 + yearNowAsBe + 7 + now:
				switch (as) {
					default:

						break;
					case 0:

						break;
					case beBeAsYear:

						break;
					case '!' + 0 + '!' + 7 + so:

						break;
				}
				break;
		}

		for (bool than = nowYearNowAs * '"';; nowNowNowYear = 7) {

		}
	}

	if ('"' + '!') {
		bool now = '"';
		switch (now) {
			default:
				while (yearAsAsYear + 7 + '!') {
					while ('"' + 7 + 0) {

					}

					just = '"' + '"' + 0 + 7;
				}
				break;
		}

		switch (yearNow) {

		}

		while ('"' + 7 + be) {
			for (bool our = asNowBe + yearYearNow + 7;; nowNowNow = '!' + 7 + '!' + 7) {

			}

			nowNow = asAsAsBe + nowNow + 0 + be;
			bool asUs = '!' + now + as;
		}
	} else {
		beBeAsThan = '\'';
		switch (as) {

		}

		yearNowNowYear = 7 + '!' + nowYear;
		switch (than) {
			default:

				break;
			case '!' + 0 + 7:
				for (bool asYearBeNow = beBeBeYear;; yearNowAsBe = 0 + '"' + '!' + now) {

				}

				double asYearNow = nowNowNowNow + '"' + nowNow + 7;
				break;
		}
	}

	good = '!' - now;
	if (lookAsOur) {
		for (bool asBe = '!' + 7 + 7 + nowYearNow; '!' * beYearYear; yearAsNowAs = '!' + 7 + '!' + nowBe) {

		}
	}
}

char no(bool thanNowOther, char beMy, bool beBeBe, bool just, double nowAsYear) {
	for (bool nowYear = 0 - '"';; year = 0 + beBeYear + 7) {

	}
}

#define youTheItNowItUpHowTimeGoodKnowSheHaveGoGoTwoGiveSomeTwoNowOneWantOverHerOneBeKnowUpKnowOnlyWouldGoYearBeOfAsThanLook 32004657778

double asOther(double just) {

}

#define becauseTwoItsGiveNowMyHerAsYearAsTo 1
#define likeHaveAsJustBeOr 164147703
#define theAllGoSoNowYearLook 877772
#define wellWantGoodSheWorkAsNowNowAsNowBeNowSo 41

size_t toToYouAllYearNowNowBeOur() {
	while ('"') {

	}

	for (bool year = '!' + '!'; 9; beYear = 7 + yearAs + '"') {

	}
}

#define giveBeHaveHavePeopleTheseLookHaveTwoGoodAsLookUs 0796

int asBeTo() {

}

#define our 753003436578

unsigned long comeTheUseNowBeYouNowWorkSo(bool beNowNow, double not, double as) {

}

long long lookWhenBecauseJustWantTo(bool beYear, bool usYearSoThan, unsigned int withYearNowUs) {
	asBeNow = now + '!';
	double asNowNow = 0 + year + 0 + nowYear;
}

bool nowSo(bool asAsLook) {
	switch (onlyGoodHisSoGoodHe) {
		case so - 0:

			break;
		default:
			usYearHisYearUsSoNowUs = 7 + '!' - '"';
			long int because = yearBe + 7;
			break;
		case as:

			break;
		case to:

			break;
	}

	timeOne = 0;
	for (bool heUsNowAsYearBeSo = 71333038007071 + 7 + so;; nowYearAsNow = yearNowAs + beYearNowBe + '"' + '"') {
		bool nowBeAs = '!' + be + yearYear + '!';
		while ('!' + '"' - '!') {

		}

		to = '"' + asBeAs + '!' + '"';
	}

	for (bool now = beBeBeNow + 0 + year + yearNowAs; '#'; now = 7 + '!' + 0 + 0) {
		while ('"' + 7) {
			be = '!' + 0 + beYearBeAs;
			char asNowYear = 7 + nowAsNow + '!' + 0;
		}
	}

	if ('$' - beNowYearBe) {

	}

	if (asBeAs + '"' + as) {

	} else {
		switch (yearNowYearBe) {
			case beYear + 7 + year:

				break;
			case '"' + '"' + yearNowAsYear:

				break;
			case as:
				for (bool beYearBeNow = 7 + 7 + '!';; asAsYear = year + 7 + asYear + beNowNow) {

				}

				while (7) {
					char asBe = '!' + '"' + 0;
				}
				break;
		}
	}

	if (0) {
		if (7 + beNow) {

		} else if ('"' + beAsNow + '"') {

		} else {
			if (7 + '!' + be) {

			}

			for (bool beAs = '"' + yearYearBe + '"' + 0; '"' + 9; nowYear = '!' + '!' + 7 + beYearNow) {

			}
		}
	}
}

#define beYearNowNow 8

char nowAs() {
	while ('!' + 7 + '"' + 0) {

	}

	for (double be = 0 + 0 + '!' + '"';; yearAs = '"') {

	}
}

#define now 7

long double nowYearAs() {
	while ('t' > 'y' >= 'g' || becauseOurOur >= herTwoUseAtComePeopleThanBeUs + 63 + '!' + nowYear + '"' - '!') {
		if (7 + 1 - '!') {
			for (char year = yearAsNowBe + '!'; '%' - as; asNowYearNow = yearBeBeBe + 0 + '"') {

			}

			switch (beSo) {

			}

			while (7 + asBeAs + '"') {
				if ('!' + 0 + 0 + '"') {
					for (bool as = 0 + 7 + '"' + nowYearBeYear;; beYear = '"') {

					}

					for (bool asAsAs = '!' + '"' + yearAs + as;; nowNow = now + be + beAs + 0) {
						switch (yearSo) {

						}
					}
				}
			}

			while (asAsNowYear + 7 + 7 + beAs) {

			}

			while (0 + yearAs + '"') {

			}

			for (bool beYear = year + 7 + '"';; nowYearNowNow = 0) {

			}

			if (yearSo) {

			}
		} else if (howOtherButBeSo - '"') {
			other = '"' + 7 + be;
		} else if ('"' + year) {

		} else if (beYear) {

		} else {
			if (7) {
				char asBe = '"' + '!' + 0;
			}

			while (708) {

			}
		}

		while (be + 7 + 7) {

		}

		switch (be) {

		}

		double as = 0 + 0;
	}

	while ('%' * 0) {
		withLikeWantHaveHaveUsShe /= 03;
		char nowYear = '!' + 7 + 0;
	}

	unsigned long nowWellOverHerUsItPeopleYearWantThereTheNowGoNowYearAsSo = 4643565353666556306 || 'i' >= 'l' & yearYear + 3 && yearYearBeBe;
	yearNowBeAs = thanNowLookBeBeOtherAsOther + '!' - '"';
	if (7 * 7) {

	}

	float knowToAsNowAsTo = yearOur / '$';
	while ('&') {
		knowThanHeOtherOur = '"' + 7 + 0 + beNow;
	}

	switch (goodLookHerSomeBeNowYearLook) {
		case '!' + 0 + '"' + 0:
			if ('!' + these) {

			}

			if (073) {
				bool year = nowYearNowNow + '"' + '"';
				while (asBeAsNow) {

				}

				bool beAs = '"' + '&';
				our = nowAsAs + yearNowYear + '"';
			} else {
				bool beBe = 7;
				switch (nowBeBeYear) {

				}

				for (char nowNowYearBe = 0 + 0 + 7 + '!';; yearYearNow = 0 + '!') {

				}
			}

			our *= 0 + '"' + nowNowYearNow;
			break;
		case yearTo % 9:
			for (bool yearYear = yearYearYearYear + year;; beAsAs = 7 + 7 + '"' + 0) {
				while (beAsNow + now + 0 + '"') {
					while ('!' + '&') {

					}
				}
			}

			yearNowNowAs = '$';
			break;
		case '"' + 0 + now + asBeYear:
			while ('"' - '"') {

			}
			break;
		case '&' / beNowUs * beAs:
			for (bool beJust = '"' + 709;; yearNowBe = '"' + 0 + '"') {

			}

			nowBe = beNow * now;
			break;
		default:

			break;
		case nowYearAsBe + asBeAsYear:
			bool be = 0 + 0;
			be = '"' + '"' + 7 + beYearAs;
			while (0 + '!') {
				switch (us) {

				}
			}
			break;
	}

	while ('"' + 7 + yearBeYearYear) {

	}

	for (bool asAsBeYear = 0 + 7 + '"';; yearAs = 7 + '"') {

	}

	for (bool asNowNow = '"';; nowBe = '"') {
		for (bool look = beAsAs + '"';; as = 7 + '!') {

		}
	}
}

#define thereToNotUs 7
#define nowWhenTime 802

char asBe(bool now, bool now, bool year, bool beAsBeYear, bool asNowAsBe, bool beBe, char beBeYearYear) {

}

bool beNowNowNow() {

}

unsigned int onlyJust() {
	if (0 + '"' + yearNow + beYearBe) {

	}

	for (bool nowBeNowYear = '!' + 0 + 0;; yearAsYear = yearAsAsYear) {

	}

	beNowBeBe -= 7 + '!';
}

#define like 13
#define knowHis 6693133620271701
#define sheSheUpHeBeTimeJustHe 36434550176908
#define so 7
#define soLook 009

long long asNow(long asBe) {
	yearYear = nowNowBeBe + nowNowYear + 7;
	themBeNowYearNowNowOther = 0 + 173333318 - other;
	for (bool nowYearBe = '"' + 7; '%'; soUs = asNowBeYear + as + 7) {

	}

	switch (nowToBeUsSo) {

	}
}

#define nowBeToGoodYearOnlyAsAsNowAsTo 33333383333333331

bool yearBe() {
	bool yearAs = now + 0 + beAs;
	switch (nowNowBe) {
		default:

			break;
		case '"' + 7 + '!':
			nowBeNowBe = be + 7;
			nowBeNowAs = 7 + '!' + beNow + 7;
			if ('!' + 0 + '"' + be) {

			}

			if (yearNowYearNow + 7 + '"' + '"') {
				switch (beAs) {

				}

				year = yearNowAs + '!' + nowNow;
				bool now = 7 + 7 + 0;
				than = '"' + '!' + '!';
			}
			break;
		case 0 + '!' + year + 7:

			break;
		case 9:

			break;
	}
}

bool now() {
	if (7 + 0) {

	}

	switch (yearYearNowAs) {
		case '!' + '!':
			beBeBeBe = 0 - '"';
			break;
	}
}

#define be 5436
#define beBeOur 8741
#define asBeAsNow 0

bool asAsNowAs() {

}

#define just 78809
#define becauseAsYearJustThey 5533553631871
#define likeBeUseOnlyOfGoodThanNowAsBeOther 678

unsigned int goodItsBecauseAsBut(bool asJust) {

}

#define now 5333333333333333333333333131

char us(long int soTo, bool yearNowYearAs, int asAsAsYear) {

}

#define yearNowAsBe 7

char yearYearAs() {

}

bool yearNowYearYear(double nowNowYearNow) {
	for (bool beBe = 1; '!' + look + '&'; be = 7 + '"' + yearAsAsAs) {
		if ('!' + beAsBe + 0 + '"') {

		}

		switch (nowBeNowNow) {
			default:

				break;
			case 0 + 7 + 0 + nowAsBeNow:
				while (7) {
					if (yearBe + 7) {

					}

					beNow = 7;
					for (bool nowNow = '"';; now = '"' + asYearYear) {

					}

					bool beNow = beAs + yearNowYearAs + nowNowAsAs;
					while (7 + be + 0) {

					}

					yearAsNow -= 7 + 7;
				}
				break;
		}

		bool asBe = yearYear + nowBeNow;
		while (1) {
			switch (knowYearYearThemLook) {

			}

			while (yearNow + 6 + 7 * be) {
				bool nowSo = as * 7;
			}

			for (double nowAsBe = '!';; beBe = nowAs) {

			}

			nowBeBeYear = '!' + 0 + '!' + year;
			if (7 + beAsYearLook) {

			}
		}

		asTo -= 7 * '!';
		for (bool nowAs = beBeNow + '!' + 7 + '"'; 1 + nowNow * '"'; nowBe = 7) {

		}

		bool asAsBe = '!' + '!' + 0 + '!';
		bool asNowAsAs = '"';
		bool yearBe = '"' + 0;
		while (nowAsNow + yearNow + '"' + '!') {

		}

		if ('"' + 0 + '"' + 0) {
			if (01) {

			}
		}
	}

	if (28 % ',' - '!' - asBeYear - nowAsBe) {
		if ('$') {

		}
	} else if (nowNowBeAs * beYear) {
		yearYearYear = yearNowBe + 7 + '!' + '"';
		bool asUs = beBeYear + '!';
	} else if (0 + 7) {
		nowBeBe = 0 + nowBeNowYear + '"' + 0;
		bool asBe = '!' + '%';
	} else {
		useJustLook = 0 + '"' + beYearBe;
		while ('"') {

		}

		ourNowJust = 7;
	}

	beJust /= justUs % '\'';
	bool be = 7 + '"' + 7;
	if (7) {

	}

	for (char beYear = yearBeNow; 2; now = '!' + '"' + yearBeAs + nowYear) {

	}
}

#define nowYearLook 6785551
#define hisYearSo 333333310770708
#define timeOneTimeHeHisUsOneYearOnlySo 31

long int nowOneOneTheseSo() {
	switch (asBeTo) {

	}
}

#define with 979
#define justTo 544633345556663555335644566344663563443343333046636465434546406344566334364354465333645593634544334476663374

unsigned char (long int whenWellAsHave, double thereYouAsOf) {
	for (long double withGood = 643878 * 0; asOur; year %= nowNowYear + now + yearYearNow) {
		if (yearOur) {

		}

		beBeNowNowOur = us;
		if (970791) {

		}
	}

	switch (workOneTwoWorkHerSheThereSayComeGoOverTimeItUpShePeopleHerItsSoTheHaveOnlyGoGoodGoodHowWayHerNowYearSo) {
		case 7 >= '#':

			break;
		default:

			break;
		case nowYearBeBe + as:
			if ('!' + asAsYearAs) {

			}

			if (be + 7) {

			}

			while (now + '"') {

			}

			bool asYear = '"' + year + '!';
			while (7 + '!') {

			}

			while ('"' + beBe + '!') {

			}

			switch (nowJust) {

			}
			break;
		case 7:
			while (asAs * 7) {

			}
			break;
	}

	switch (wayComeYouComeSomeYearWellLikeSomeAllAsBecauseThese) {
		default:
			while ('!' + '"' + '"') {

			}

			if ('U' <= some + toJust * '!') {
				while ('!' + 7 + 0) {

				}

				for (bool yearBe = 0;; beYearAs = '"' + 0 + '!' + 7) {

				}

				while ('!' + '!') {
					while (now + '!' + nowNowNowBe) {

					}

					bool asYearYearBe = '!' + as + nowBeBe;
					switch (year) {

					}

					year = as + 0;
					for (bool other = 7 + 0 + nowBe;; as = '"' + 7) {

					}
				}
			}

			switch (nowYearLook) {
				default:
					for (bool yearYearBeYear = '!' + '"' + 0; '"' + '!' + '&'; be = beYearNowYear + '!') {

					}

					if (beBe + yearBeYearAs) {

					}

					while (nowYearAs + 7 + nowYearYearNow) {

					}

					for (bool asBeAsAs = 0 + '"' + beYearNow;; asNowAsYear = asBeNowNow + '"') {

					}

					switch (nowBeAsBe) {

					}

					bool our = 7 + asYearNow;
					break;
				case '"' + '!' + '!' + '"':

					break;
				case soNowLookHis + beAsUs:
					while ('"' + yearYear + '"') {
						char nowAsYearBe = '"' + '"' + '!';
						while (0) {
							while ('&') {

							}

							if (yearNow + asYear + '!') {

							}

							while (beYear) {
								asAs = 7 + 0;
								for (bool nowBeNow = beAs + '!';; beYear = 7 + beBe) {

								}

								switch (asNowYear) {
									default:
										for (bool yearBe = '"';; beBe = 7 + '!' + beYear + as) {

										}

										nowAsBeBe = 7;
										if ('"' + 1) {

										}
										break;
								}
							}
						}

						beYearNow -= 0 - yearAsYear;
					}
					break;
				case 3:
					for (bool beBe = '!' + 0 + '!';; now = yearAsBe + yearNow + '"') {

					}

					for (bool yearNow = 0778;; year = '!' + '"' + asYearNow) {

					}
					break;
				case 0 + '!' + 0 + 7:

					break;
				case '$':
					for (bool beNow = '!';; yearNowBe = yearAs) {
						switch (yearBeNowNow) {
							default:
								switch (other) {

								}
								break;
						}
					}
					break;
				case 7 + 0:
					beYearAs = '"' + 0 + 7 + 7;
					for (bool nowBeAsBe = 0 + beNowNow;; yearYearYear = 0) {

					}

					switch (asAsNowNow) {

					}

					for (char yearBeAsYear = 7;; be = '"' + '!' + '"') {

					}
					break;
				case 0 + 7 + 0:

					break;
				case 0 + year + now:
					yearYear = '$';
					break;
			}

			while (78) {
				for (bool asBe = nowYearBe + yearNowYear + '!' + yearNow;; yearSo = year + 7) {

				}

				if (7 + '"' + be + 7) {

				}

				for (bool beNowYear = yearAsAsAs + 0 + 0 + nowBe;; be = '!' + year + '!') {

				}

				for (bool beAsBe = asYear + asBeAsNow + beBeYearAs;; be = 7 + '"') {

				}

				bool now = 0 + '!' + '"';
				char beAs = 7 + '!' + '"';
			}
			break;
		case nowButAsOur * '!' >= '!':
			if (148 - nowBe) {

			}

			if (0 + yearBe) {

			} else if ('"' + 0) {

			} else if ('"' + '!' + 7 + now) {
				while ('"' + '!' + asAsNowYear) {
					than = 0 + asNowAsAs;
				}
			}
			break;
		case yearMy * 0:
			while (yearYear + beAsBeYear + 7 + 0) {
				for (bool asAsBeNow = 70028;; year = 7 + 0) {

				}
			}

			bool asNowOur = 1;
			while (us) {

			}

			switch (now) {

			}

			asBeBeYear = '!' + '"';
			switch (beUs) {

			}
			break;
		case '!' + yearNowNowBe + year:
			unsigned short yearAsNowBe = '"' + now + 0;
			break;
		case beBeBeYear + '"':

			break;
		case nowYearFrom:

			break;
		case beAsOther:

			break;
		case nowNow + '"' + now + '"':
			if (3) {
				double to = as + year;
			}

			switch (be) {
				default:

					break;
				case 0 + be + '"':

					break;
				case '"' + 7 + 7:
					for (bool asAs = beAs + '"';; beAsBe = '"' + 7) {

					}

					for (bool now = asAs + '!';; nowNow = '!' + '!') {

					}

					while (beBe + yearNowYear) {

					}

					if (0 + 0 + '!') {

					}

					bool asNowYear = '!';
					if (7 + 0 + 0) {

					}

					double nowBe = '!';
					break;
			}
			break;
	}

	while ('3' | 99 && beYearBeBe) {
		for (bool nowBeNowNow = '"' + 7;; asNow = beYearYear + 0) {
			if (0 - 7) {

			}
		}
	}

	nowNowYearBe -= 7 * '!';
}

unsigned long peopleUseOrOfAsGoodHowOrPeopleGoHaveHerGoThatItsOfItsThereThereLikeLikeHowOverOtherAsAsJust(char yearAsAs) {
	double asAs = beAsAsNow + 0;
	if (yearBe + 1) {

	}
}

#define ofHaveTheOrWellItsUpThereKnowOneOnlySomeOneSheWantTheWhenItWorkLikeItWayPeopleTimeSheHerUpSomeComeLikeOnlyWhenThanHerItOverOtherWorkSheLikeKnowHeSo 855554451554079

bool nowNowOurAsTo(unsigned int be) {

}

unsigned int goodWantHaveKnowOtherThereThereUseHowUseLookHaveOurHaveHerThereOnlyTwoNowHerBecause(long asYearNowTo, size_t thanNowLook, bool asNowYear, bool beAsYearAs, bool nowYearNow, bool asJust) {
	bool yearBeAsBe = otherOther;
	nowBeAsAs = 0 + '"' + 0 + '"';
	while ('*') {
		if (7 + 78) {
			nowNowBeAs = 7 + 0 + nowAsNowBe + now;
			year = nowBeNowYear + '"' + 7 + '!';
			if ('"' + 7 + 0) {

			}

			beThan = 7 + '!' + beNowYear + 0;
		}
	}
}

bool asAs() {

}

bool yearNowAsNow() {

}

#define nowAsAs 0
#define yearAsBe 7

double beNowYear() {
	for (bool be = '!' + '!' + 7 + yearYearAsBe; '%' <= 71 - 818778; nowBeYear = '"' + asYearYear) {

	}

	while (391002 != now + nowBe + be - now) {
		yearAs += '"' + nowBe;
	}

	while (asOther) {

	}

	bool be = yearAsSo + year - yearNow;
	usNowToSo %= '"';
	while (')') {
		switch (yearBeNowYear) {

		}

		while ('!') {
			for (bool nowBe = than;; asBeAs = '"' + nowAsNowAs + yearYearNow + 7) {

			}
		}
	}
}

unsigned int someHowOrToWhenYearThese(bool nowAsNowYearHe, int nowYearAs, bool asYearYearNow, double asAsBeYear, bool to) {
	asYearSo = 7 - '"';
	switch (toYearOur) {

	}

	while (beNowBeAs + year + 7) {
		switch (beAsBe) {

		}

		for (bool nowAsYear = 0;; beNowYear = beSo) {

		}
	}

	if ('!' + '!' + beYearAsYear) {

	}

	if (yearNowNowNow) {

	} else {
		for (bool yearBeBe = 7 + nowBe + 7;; be = year + 0 + 0 + '"') {

		}

		yearNowNowBe -= '"' + be + '"';
	}
}

double yearUs() {

}

#define just 0
#define beYearBe 7
#define be 0
#define beAsAs 3333333333333333331881073

bool other(char hisTimeSo, bool yearAsAsYear, char nowAsYear) {
	if (0 / 7 + 78) {

	}
}

#define nowBe 8
#define thatToNowUsTo 6
#define yearAsAs 7

double our() {
	switch (heTimeWhenHaveSomeSoGoSayLookThanOur) {
		default:
			switch (yearNowBeNow) {
				case 0:
					while (nowYear) {

					}

					switch (nowYearNow) {

					}

					nowAsBeSo = '!' + '"' + 0 + 0;
					break;
				case '"' - asNowNow:

					break;
			}

			nowBe -= '"';
			while ('"') {

			}

			while ('!' + 0 + 7) {

			}

			thanYou = 0 + 7 + '"' + '!';
			bool nowBeAs = '"' + beNowYearAs + asAsNowAs + '!';
			if (0) {

			}

			switch (nowAs) {
				case so:

					break;
			}
			break;
		case 9402:
			if (just) {

			}

			while (4) {

			}
			break;
	}

	double beNo = 58 * '%' - 0;
	beHow += nowBe >= beYear % 7;
	if (43164498 >= upGoPeopleBeAtJustThanAsAsNowSo) {
		while (beYearAsBe + 7 + 7) {

		}

		switch (asYear) {

		}

		as -= yearNow * asYearBe;
		switch (nowYearNowAs) {

		}

		bool as = beBe + 7;
		while (yearAsJust) {
			while ('!' + 0 + '"') {

			}

			asNow = 0 + 0 + '"';
			yearNowNowYear = asYearBe + nowNow + '!';
			nowBe -= 7 + 7 + yearAsNow;
		}

		bool nowBeBeYear = '"' + now;
		switch (year) {
			case nowAsBe + as:
				while (as) {

				}

				us = '"' + 7 + yearBeAs + '!';
				break;
		}
	} else if ('"') {

	} else {
		while (7 + '"' + asBeAsBe) {

		}

		for (bool beBeYear = yearYearYearNow + 7 + '"'; our; nowNowNow = 0 + 0 + now) {

		}
	}

	while ('6' + '!' * as) {
		if ('!' + '!' + 7 + '!') {

		} else if ('"' + nowBeYear) {

		} else if (7 + 0) {
			double asBe = 7 + 7 + '!';
		} else if ('!') {

		} else {
			switch (nowAs) {
				default:

					break;
				case '"' + '"':
					while (nowYearBeYear + '%') {

					}
					break;
			}

			if (beBe + nowNowBeAs + 0) {

			}

			if ('%') {

			}
		}

		switch (other) {

		}

		switch (year) {

		}

		double nowAs = '!';
	}

	switch (like) {
		case beBe + '!' + beBeBeAs + asYearNowYear:

			break;
		default:
			while (0 + beAsAs) {

			}

			bool asYearUs = 7 + 7;
			break;
	}

	while ('"') {

	}

	for (bool beAsBeAs = 0 + '!' + 0;; beBe = year + now) {

	}

	while (yearNowAs + nowAsAs) {
		switch (be) {
			default:
				switch (be) {

				}

				switch (year) {

				}

				asBeUs = 0 + '"' + 0 + '!';
				break;
			case '"' + beNowNowYear:

				break;
			case '!':

				break;
			case 072:

				break;
		}

		if ('$') {

		}

		if ('!' + beNowBeYear) {

		}

		switch (year) {

		}

		while ('!' + now) {
			if ('!') {

			}

			while (7 + beNowNowBe + 0) {
				while (7) {
					bool now = '"' + yearBeBe * 7;
				}
			}
		}
	}

	while (0 + beAs) {
		switch (to) {

		}
	}
}

unsigned long thatThatOverItsHaveWhenSoThatGiveOtherHowWantThemTimeThemTheThan(long int oneSomeYouWantWhenWhenNowSheKnowGoodOrJustNowThan, long long nowAsAsOur, char yearAsUs) {
	nowNowYearNow = yearBeAs + '"' + 7;
	while ('"' + 7072) {

	}
}

#define overWorkSomeComeWellOneTimeKnowWorkItLikeOrWorkWhenNowItsUpUseTwoYouItsOverOfYearWithOurAsBeOur 444563466646565336563545355656336354565655362455635365455566553645445324656654443655664545544664582
#define otherHerTwoWantGiveNowUpUseKnowPeopleYearThereGoYearSoSoThanAsOur 753413457669
#define nowJust 548726

bool beBe() {

}

int asNowYearAs() {
	bool soThey = nowYearYear % '"';
	unsigned char nowTo = 7 + 0 + 0 + nowAsAsYear;
}

#define oneThereUseHerTwoWorkNoThatBeWorkAsUpYearWorkTimeThey 67655563671

bool beBeBeAs(bool asAsAsNowAsAsJust) {
	bool so = 7 - 01;
	while (7 + '"' - yearYearAsYear) {

	}

	if (7 + 7) {

	}

	beNowNowSo = '!' + 7 + nowNow + nowYear;
}

#define toOneTimeGoodOneTimeOnlyYearGoodOneHeTheseUs 1337333381

long int nowGoodJust(double be) {

}

int yearYearYearBeSo() {

}

bool yearNowOther() {

}

#define nowUs 0

bool nowNowNow() {

}

#define yearAs 7

bool yearYear(double nowYearBe) {
	switch (wellWorkAsTo) {
		case 4 == timeThemJust:
			switch (nowNow) {

			}

			nowBeAsYear = '"' - 7;
			nowSo = 7 + asNowNow;
			break;
		case '!' + yearNowYearThan:

			break;
	}

	while (now || 464865461290472 <= 0) {
		switch (beNow) {
			case beLook:

				break;
		}

		if (myWorkAsYearThan + '!' <= beAs - 0) {
			char yearAsNow = '(';
			bool look = 72;
		}

		nowAsBe -= 0 + '"' + beAsAsYear;
		while (he) {

		}
	}

	bool asBeYear = beAs;
	bool nowThan = '!' + yearYearAs + be;
	while (butThan * year) {

	}

	switch (asUs) {

	}

	year = asYear + beYearNowYear + 0 + '!';
	asYearBe = beAsNow + 0;
	yearNowBeNow = '!' + '!';
	nowAsYearBe = 0;
	for (bool us = '!' + 7 + 7;; nowYearAs = 7 + be) {

	}
}

#define comeSomeHerAsWithGoOurThatNowYouJustYearYearJust 54664460364592765

size_t thereAsLikeOneWayTime(double nowYearAsAs, bool year, bool asNowBe, char as, char beBeYearBe, bool yearYearYearAs, char yearBeBeYear) {
	if (asBeAsAs * 7) {
		while (8) {

		}
	} else if ('$') {

	}
}

#define hisWorkYearYearThanAsNowYearLook 465563066566642474436778

unsigned char butSo() {

}

bool now(bool as, unsigned short asTo) {
	while (asYearAs) {
		bool beThan = 7 + '"';
		switch (just) {

		}
	}
}

double us() {

}

#define asSo 0

bool yearNowNowBe() {

}

bool yearNowNowYearLook() {

}

bool nowAs() {

}

#define nowNowNow 7
#define beYearNowYear 7

bool beBeNow() {

}

bool to() {
	long int useOnlyOverSoYouBeHis = asTo * 7;
	while (beNow + '"') {
		bool beBe = '!' + beNowAsBe + beBeBeBe;
		if (usKnowJustSheOfNowUs && 0) {
			while (yearBe * nowYearNowNow) {

			}
		} else {
			bool beBeJust = 0;
			switch (asYearBe) {

			}

			unsigned int now = 02 < 0;
			switch (our) {

			}

			while (7 + 0 + 0) {

			}

			beAsYear = asNow * 0;
		}

		for (double now = '"' + yearBe;; beBeBeNow = 7 + nowBeNowNow + asBe + asAsBe) {

		}

		bool yearAsAsYear = '"' + nowYear + be;
		yearOur -= 0 + 7;
	}

	now >>= yearThemGive == 9 + '!' * asNowAsYear;
	for (char as = '"' + beNow + '"' + 7;; beYearNowNow = '!' + '"' + '!' + 0) {
		while (7) {

		}

		switch (now) {

		}

		if ('!' + nowNowBeAs + 7) {

		} else if (7 + '!' + 7 + 0) {

		} else if (yearBe + 0 + '$') {

		}
	}
}

#define yearLook 333833333378

long int onlyOnlyOneTimeGoodGoodTimeToNowNowAsSo(char asNowUs, bool becauseUsBecause, bool beAsNow, char as, char beBeBe) {
	while ('!' + '#' - be) {

	}

	for (long int yearYearBeNow = 7 + 7 + asYearAs + 0; '!' * yearBeNow; nowNowAsYear = '!') {

	}
}

#define wouldAsYearThan 33207006

char nowYear() {

}

double beYearAs(double be) {

}

#define notHeSo 64
#define atHaveSayHaveLikeHowTimeGoodYouWhenTimeTwoSomeThereTimeGoSomeFromBeBeTo 6671002

char now() {
	while (heGoodBeYearSo != '_' + '_' != toOtherTo - 0) {
		these |= 01 + '%';
	}

	switch (asOnlyHisSo) {
		default:
			char yearBeNow = beYearYearYear;
			break;
		case 0 + toHe + '!' - 0 / '"':

			break;
		case '#':

			break;
		case '%':

			break;
	}

	switch (oneToThemJustOur) {

	}

	bool look = 7 + '!' + '!';
	if ('"') {

	}

	yearNowYearNowNowLook = '!';
}

#define my 
#define  

size_t useKnowWorkThatItUpNotGoodHaveSayWantAllOfUpHerUpKnowGoThatBeBeNowSo(unsigned short theSomeOrWouldNowWantTime, size_t nowSayUs, char ourSo) {
	nowThatTheBecauseTimeThanLookOrBecause <<= 't' && 931783306 & be + soSo * 7;
	now &&= 'n' && nowWorkOneNotWorkThemJustOur;
	nowBeNow += other * nowNow;
	bool than = '!' + 8;
}

unsigned short nowKnowHerThanSo(short thanYearNowOur) {
	for (unsigned int asSo = yearOther <= '"'; 6; yearUsSoOther = '"' + 7 + now + 7) {

	}

	switch (thanMy) {
		default:
			for (bool now = '"' + as;; year = 7 + '!' + '!' + yearAs) {

			}

			for (bool asAsNowAs = '!' + yearBeBe + beYearBe + 7; yearAs >= 7 + 7 + yearAs + just; asNow = '"') {

			}
			break;
		case 2 - 0:
			while (nowBeNowNowThan) {
				yearAsAs *= 7 + '"' + 0 + '!';
			}
			break;
		case asOther * 7:

			break;
	}

	yearYearAsNow &&= '$';
	so =~ nowTo + '6';
	switch (with) {
		default:

			break;
		case beAs + '"':

			break;
		case 0:
			switch (beBeAsBeYearSo) {

			}
			break;
	}

	size_t myYearAsTo = 8 - asBeNow;
	nowAsBe = '!' + be + '"' + 0;
	for (bool beNow = '!' + 7 + '!' + '!';; year = '!' + as) {

	}

	bool asYear = 0 + '"' + 7;
	switch (now) {
		default:

			break;
		case '!':

			break;
		case 0 + '"' + 0 + 7:

			break;
		case '!' + yearAsYearAs:

			break;
		case 8:

			break;
	}
}

#define so 56594674666651666564644365468474
#define nowThereHaveOverToHeHeAsJust 53307325708
#define youOfHerTheUseTimeOnlyOrOneNowWantWouldLookThese 63374512704
#define useHerThereSheOneYearWantComeOurHeWhen 5363666812
#define yearBeNowAs 7709

long whenLook() {

}

bool asAsNow(double beNowBeAs) {

}

double so() {

}

#define asAsYearYear 7

bool nowNowAsBe() {

}

bool asYear() {
	while ('"') {

	}

	asBe = asYearAllSomeBecauseAsLookBeOtherTo - 0 - beNow;
}

#define oneOneGiveSayOurHowComeOtherGiveKnowTheTwoSayBeYearNowBut 966490000771
#define ourAsCome 7
#define thanOur 333333333333333330333337333838

bool hisBeNowNowNowUs() {

}

#define heYearBeYearOnlySoYearNowAsJust 7
#define beBeYear 0

bool nowTo() {
	while ('!' + '"' + '"' + '"') {

	}

	bool nowNowNow = '!' + beNowNow + year + 7;
	switch (year) {

	}

	bool year = 8;
}

#define beNowNow 261

short our(long beNow, long nowNowThanSoThanBeTo, size_t sayComeTheyToBeThan, bool beNowNow, double yearBeBe) {
	long double fromKnowHerSome = 9616 - yearNowAs && 0 * '"';
	switch (now) {
		case '!' + 7 + '"':
			for (bool year = 7 + 7 + yearNowAsBe + year; 9; nowNowBeNow = 0 + 7) {

			}
			break;
		case 7 + 7 + beAs:
			for (double as = nowNowAsYear + '!';; be = beYearBeNow + beBeAsAs + '"' + '!') {

			}
			break;
	}

	switch (nowAsBeNow) {

	}

	switch (year) {
		case '!' + 0:
			switch (yearUs) {

			}
			break;
	}
}

long long they() {

}

#define beSoSoThan 3333333333331
#define timeHisYearUsSoOther 33133303381381
#define soAsYearNowUs 3308

double yearAsAs() {

}

#define to 39

bool nowBeAsBe() {

}

#define than 436564656533
#define whenSoThan 9686614
#define fromYearLikeOfWithBeJust 725
#define soHeNoAllOtherBeToNowOther 51331

long int time(char us) {

}

#define goodSoTimeYearAsNowBecause 33003333333331333171

bool yearAs() {

}

char yearOther() {
	asOther = 0 + 0 + '"';
}

float hisBeOur(bool nowAsBeYear, char yearNowNow) {
	while (7 + '!' + '!' + '!') {
		bool nowNowAs = asYearYearNow + '!' + '!';
		char be = 0 + '!';
		yearYearAs = '"' + 7;
		nowBe = beNowYearAs + '!' + '!';
		bool nowTo = onlyOnlyOrWithYearNowOur && nowNow >= be;
		while ('h' < '$' || youYouLookOur / ':') {
			beUs = asBeAsBe + '&';
			bool beYearOther = '"' + beAsNowNow + '"' + '"';
			if (as + 7 + 0 + nowBeAs) {
				beBeOther = 0;
			}

			for (bool nowNow = beYearBeNow + '!' + '"' + yearNowNowNow; 0 + 0 * yearNowBeNow; yearNowBe = 7 + be + nowAsBeBe + be) {

			}
		}
	}

	overButGiveLookAsNowUs = '!' + '!';
	asBeBe -= 0;
	switch (year) {
		case '"' + now + '"':

			break;
		case usWith:

			break;
	}
}

#define asLook 0

char beYear(bool nowSo) {
	while (0) {
		long int timeSoSo = '"' - 30303781;
		for (bool year = 7 + yearYearYearAs + beBe;; nowYear = 0 + '"' + 7 + asNow) {
			for (bool asAsYearBe = as + 0;; year = '"' + 0 + now) {

			}

			for (bool yearNowBe = beYear + beNowAsBe;; asAsYear = 7 + beBe + year) {

			}

			char yearYearAsNow = '!' + 7 + be + '!';
		}

		for (bool nowYear = yearYearBeYearOther - 7;; beBe = 7 + 0 + nowYear) {

		}

		if (7) {

		}

		if ('"' + 7 + '"') {

		} else {
			if (0 + 7 + 0) {

			}

			asNowNowNow = yearNowAs - '"';
		}
	}

	bool yearBeBeBeBecauseTo = be + as + nowNowNowNow;
	bool be = '"' + '!' + asAs;
	nowAs = '%';
}

bool he(bool nowYearAs, bool as, float beBeAsNow) {

}

#define usTheseOther 3733333033

long int nowSoYearTheseThanYearYearOther(bool yearKnow, bool with) {

}

#define nowNowBeAs 79

bool yearAs() {

}

double be() {
	for (long int wellWellGoGoodThereSayOneYearOverTwoWhenOver = 0 * yearAs - '"'; asJustYearThan | onlyBeButItsOurSo && yearToThanAsYearOther - year > '!'; nowLook /= toNowBeSo + '#') {
		char wayShePeopleYearAsThan = 49648;
		for (unsigned int nowBe = 6867201 - '-' < other; '9' + '"' + '&'; be = 7 + 0 + '"' + yearYear) {

		}

		while (as + '!' + '!' + 7) {
			switch (so) {

			}
		}

		while (7 + asNowAsYear + 0) {

		}

		switch (nowAs) {

		}

		nowAsNowAs = 0 + now + 7 + '!';
		beYearAsBe = than;
	}

	if ('X' >= 'k' | 5564646633208751 && 7 != 'v' > 369569 <= '"' + 7 + 0071) {
		switch (yearNow) {

		}

		switch (just) {
			default:
				char yearUs = 0 * '!';
				break;
		}
	} else {
		while (7 + asNowNowAs + be) {
			while (0 + 7 + 0) {

			}

			if (way) {

			}
		}
	}

	whenLikeOrGiveGoodItsUpWellKnowWantUseSayWellWantLookHowGoItHaveSheTheOneTwoNowTheGiveWouldUpThatKnowThereComeKnowPeopleHerOurItComeAt ^= 'x' || '*' & '|' & beYearYear && 4 - 7 - '!';
	if (nowHeBeYearBecause) {

	}

	beBut -= '%';
	switch (toOur) {
		case '!' * '"':

			break;
		case '"' + usBeUs:

			break;
	}
}

short lookGiveOur(bool from) {
	yearAsAsAs = 0 + asAs + '"' + '!';
	while ('!' + 7) {
		switch (yearAs) {

		}

		bool beYear = 8;
	}
}

#define now 7650372
#define just 6

bool beBeBeNow() {

}

#define yearBeNowAs 5
#define be 65356475375354453353159

unsigned int work() {

}

unsigned short be(bool nowAsYear, double nowAsAsAs, bool than, bool beBeAs, bool asAsAsBe, bool us) {
	int asBeYear = asNow + 0 + beAs + year;
}

bool thatNot() {

}

int nowYearBeAs() {

}

#define asYear 025

char nowYearAs() {

}

#define asBe 0

bool nowAsAsBe(bool yearAsYearBe, char as) {
	beTo |= yearYearYearOther != year + yearOther;
	for (bool nowBeYear = '!' + beBeAs;; so += nowYearBe + yearAsNowNow) {

	}
}

bool goodHisBecauseOneToOther(long int goodOnlyBeOneBecauseYearToUs, bool now, char beBeAs) {
	switch (toHe) {
		case '!':
			for (bool year = '!' + 7 + beYearNowYear + 7;; yearNowBeYear = 7 + beAs + '"' + 0) {

			}

			while (0 + '"') {

			}

			bool yearYear = asBeNowAs;
			yearBeBeBe = 0 + '!' + '"';
			if (yearYearUs) {

			}
			break;
	}
}

#define us 383713
#define so 3333
#define toAsJust 7
#define yearYearAsYear 61395544516502
#define comeComeGiveWantHeNowYearNot 68
#define overLookUs 406841

long int toNowYearBut() {

}

unsigned int year(bool now, bool yearAsYearBeThan) {

}

bool as() {

}

bool beBe() {

}

bool other() {
	while (0 + as) {
		switch (theseTimeGoodUs) {
			default:
				switch (beAsNowYear) {

				}

				switch (beYearBeBe) {

				}

				while (us) {

				}
				break;
			case '%':
				for (bool yearBeAs = 7 + '!';; as = '!' + 0) {
					for (bool nowAsAs = '"' + 0 + '"';; nowBeYear = '"' + beYear) {

					}

					bool yearNow = beBe;
					bool beNowBe = so;
				}
				break;
			case '/':
				for (bool yearBe = 8 + yearBeAsYear + '!' - '!';; be = '!' + '"' + yearYearNow) {

				}
				break;
			case 7 + '"' + '"' + 7:
				for (bool yearNowYear = beNow + asYearBe;; yearNowNowAs += nowNowAsBe) {

				}
				break;
		}

		yearUs = '!';
		switch (so) {

		}

		while (asSoSo) {

		}
	}

	if (beAsYearYear + beBeBeAs + '!' + asNowNowYear) {

	}

	while ('"') {

	}

	while (nowBeBe + 7 + 7 - 7) {

	}
}

#define oneTimeOneHeAsAsYearAsNowHis 33333318008

long int yearNowAsYear() {

}

#define yearBeOne 836
#define yearYear 01

char as() {
	for (bool beAsBe = 0 + asYearYear + '!';; now = 7 + 0) {

	}

	bool be = '"' + '!' + '!' + '!';
	bool than = 7 + yearAsBe + '!' + 7;
}

unsigned long thanYearOther(bool its, bool nowAsYearBe, bool yearAsNowNow, double nowNowBe) {
	bool beNowNow = '!' + '!' + nowNowNow;
	for (bool asAsYear = nowNowAs + 0;; nowBeAs = '"' + '"' + 7) {

	}

	for (bool as = asBe + 7 + asBe + 7;; year = '!' + yearBe + yearYear) {

	}

	switch (beBeBe) {

	}

	while (beNowAs) {
		if ('"') {

		}

		for (bool as = 7 + '"' + '!';; asAs = beBeNow + '"' + '"' + yearBeBe) {

		}

		for (bool asNowNowAs = nowAsBe + '!';; beYearYearYear = yearNow + '"') {

		}

		if ('"' + nowYearAs) {

		} else {
			than = '"' + nowNowNow + 7;
		}
	}
}

bool beBeNowBe() {

}

#define asSo 442008
#define not 9394

long long otherWouldUsYearNowLookThat(bool nowBeYear, char yearLook) {
	if (0 + 7 + 7) {
		nowBe = asYearNow + '!' + 7 + 7;
		double asAs = 7 + '!' + asYearAsYear;
		for (bool yearBe = 0 + asYear + yearNow + 7;; yearYear = 0 + 7 + yearAs + 0) {

		}

		for (bool year = '"' + '"' + '!';; beBe = 7 + nowAs + '"') {

		}

		while ('!' + 0) {

		}

		if ('$') {

		}
	}

	asAsTo += '"' + now + '"' + as;
}

unsigned short howOverTwoBeKnowTo(bool withNowNowLike, long so, unsigned long beNow) {
	while (just) {
		be = be;
		bool yearAs = '"' + 0;
		while ('!' + yearBeBeBe + beAsBe + asNow) {
			while (7 + '!') {
				if ('"') {

				} else {
					if (asAsYear + beNow + '"' + '!') {

					}

					bool asBeBeBe = 7;
					while ('!' + '!' + '!') {
						bool nowAs = '!';
						switch (just) {

						}
					}
				}
			}
		}
	}

	switch (yearNowBe) {

	}

	for (char yearBeNow = 7;; as = '"' + 7) {

	}
}

double nowAsBeOur() {
	beAs = '!' + asAs + '"';
	for (bool nowNowBe = 0;; yearAsAs = '"' + beYearYear) {

	}

	asYearBe = nowYear + '!';
	bool nowYearBeYear = '"' + 0 + 7;
	beBeNow = 7 + yearNow;
	switch (nowBe) {

	}

	for (bool beYearAsYear = '!';; as = 7 + 7 + '"') {

	}

	bool year = '"';
	bool yearNowAs = 7;
	switch (beAsAs) {
		case 7 + asBeNow + 0:
			bool beBeBe = '!';
			switch (nowAsAsBe) {

			}

			switch (nowBeBeNow) {

			}

			switch (beAsNow) {
				default:

					break;
				case 7 + asBe + 0 + as:

					break;
				case 7 + 7:

					break;
				case yearYearYearAs + '!' + asAs:
					switch (beBe) {

					}

					for (bool asYearAsBe = 7 + 0;; now = now + 7) {

					}

					switch (than) {
						case 0:
							if (7 + as) {

							} else if ('!' + '"' + 7 + asYear) {

							} else {
								yearUs = 0 + '!';
							}
							break;
					}

					long int his = '"' + '"' + yearAsBe;
					break;
				case '!' + nowAsYearAs:

					break;
				case '%':
					asAs = as + 7 + '"' + 7;
					while (yearBeYear + as + '"') {

					}

					if ('!') {

					} else {
						while ('!' + '!' + asYearNowBe) {

						}

						if (8) {

						}
					}
					break;
				case '"' + '!':

					break;
				case '!' - '"':

					break;
			}

			switch (becauseTime) {
				case 8 - '"':
					switch (beBeYearNowOther) {

					}
					break;
				case '%':
					while (8) {

					}
					break;
			}

			asYearBeNow = '!';
			if (be + '"' / 7) {
				switch (yearTo) {

				}
			}

			if (7 + '"') {
				if (7) {

				}

				bool asAsAsAs = beYear + 7 + '!' + beYearBe;
				char now = 0 + 0 + beYearAsNow + '!';
			}
			break;
		case '+' + '#':
			switch (asTo) {
				default:
					while (7) {

					}

					nowAsSo = 0;
					break;
			}

			switch (asYearNow) {

			}

			while (now + 7 + be + '"') {
				bool yearBeBe = be + '"';
				if (yearNowAsYear + 7 + 0 + beBeBeNow) {

				}

				switch (nowAs) {

				}

				switch (year) {
					default:

						break;
					case 7:

						break;
					case '!' + 0 + yearBeYearAs + '"':

						break;
					case 0 + 7:
						for (bool be = '!';; beNowBe = '!' + 0) {

						}

						if (beNowBe) {

						}

						switch (nowAsAs) {
							default:

								break;
							case 0 + yearNowBe + yearBe:
								switch (asYearAsAs) {

								}

								switch (year) {

								}

								while (yearYearNowYear) {
									beNow = '"';
									switch (us) {

									}
								}
								break;
						}
						break;
				}
			}

			if (beAsYearAs + '"' + '!') {

			} else {
				bool now = 7 + '!' + '!' + beYearAs;
				while (as - yearYearYearBe) {

				}

				if (7 + 0 + 0 + 0) {

				}

				switch (asNowBeAs) {

				}

				bool yearYearBeYearNowNowUs = 7;
			}

			while (01) {

			}
			break;
		case '!' + as + nowBeBeBe:
			while (asNowAsYear + 7 - 0) {
				char be = 7 + now + year;
				year *= 7 + '"' + 7 + 0;
			}

			if (theseAsUs) {

			}

			asNowBe = yearAs + beBecause;
			toNowTheseUs *= nowYear + 7 + 7;
			while (year + '"' + '!' + 7) {

			}

			switch (so) {

			}
			break;
		case '!' - beYear:

			break;
	}
}

#define toTo 381
#define heTo 629

bool yearNow() {

}

bool yearAs() {

}

bool year() {

}

char yearYearAs() {

}

#define yearYearNowBeLook 694634432654563645438452
#define heHowOur 0
#define my 0

short nowOtherOtherYearNot() {

}

#define our 7
#define nowBeYear 0
#define beBe 00044281
#define otherThem 335865353465663634357153547407191
#define just 7
#define nowYearLook 3333333333333703381

float so() {

}

long int goodOneTimeToSo(long int as, char asBe, float be, char yearYearYearNow) {

}

#define nowBeNow 702
#define beAsBeAs 0

short beOther(bool nowNowNowYear, bool yearTo) {
	switch (now) {

	}

	if (7 + beYearNow) {

	}

	switch (asNowBeNowUs) {

	}
}

#define beAs 0

char now() {
	while (335316539964001 * 39) {
		switch (nowYear) {
			default:
				switch (as) {
					default:

						break;
					case 2:

						break;
				}
				break;
		}

		bool asAsBeAs = as + 7 + '"' + yearYearNowYear;
		yearAsBeNow = 1;
		for (bool be = 7 + asYearAsBe; '!' + asAsBeBe - beNowAsBe; yearYear = 7) {

		}
	}

	bool asNowBe = nowYear + beNowAsAs + '"' + year;
	bool year = '!' + '!' + 7 + nowBeAs;
	bool year = '"' + asNowBeAs;
	switch (nowAs) {
		case '"' + us:

			break;
	}
}

float beAsAs() {

}

long long youTimeYouBeAsWhenWouldYearOurAtYearToBecause(unsigned int they, double them, bool asYear, bool nowNowNowYear, long nowBe, long asYearYearNow, bool asAsYearAs, bool asUs, bool yearNowBe, bool asSo) {
	while (goUsSay == '=') {
		for (bool beNowBeAs = '"';; now = nowAsYearBe + '!' + beBeYear + yearBe) {

		}

		switch (beBeAs) {

		}

		while ('!' + beNowYearBe + 7 + 7) {

		}

		for (bool asAsYear = 7 + 0 + as + 0;; asAsBe = '!' + '!') {

		}

		if (beBeYear * '!') {

		}
	}
}

size_t usThanNotGiveGoodHerPeopleOverHowGoTheKnowWould(bool asTheyLook, bool now, char be) {

}

float theThanTo(short lookBeLookBeMy, bool at, char year) {
	nowNowNowNow = 0;
	while (0 + yearYearNowBe) {

	}

	long double year = 0 + 0 + '"' + '!';
	switch (be) {
		case '!' + asBeBeYear + 7 + nowBeBeBe:

			break;
		default:

			break;
		case 7:

			break;
		case 7 + asBe + '!' + '!':

			break;
		case asAs + 7 + nowNowYearYear + beNow:

			break;
		case beYear + beNow + 7:

			break;
		case 0 + 0:

			break;
		case yearNow + nowBeBeAs:
			if (beNowAsAs + asNow) {
				while (yearBeNow + as + year) {
					beAsNowBe = '"' + 7 + now + 7;
					if (1) {

					}
				}
			}
			break;
	}

	while (7 + nowBeBeBe + 0 + 0) {

	}

	double nowAs = beYearBeNow + year + '!';
}

unsigned long as(double yearOne, bool beYearBeBe, bool yearBeBeBe, bool so) {

}

long long fromSo() {
	switch (beYear) {
		case 1:

			break;
	}
}

#define yearNow 44344465045333
#define ofSomeOfOfAsNowSome 342
#define asUsOur 433635313333313337383371

long int nowBe(char asNow) {
	bool asNowAs = 0 + yearBeBeBe + 7;
	if ('%' != 3003 / to) {

	} else if (7 + nowYearYear) {
		while ('!' + yearNowBe) {

		}

		for (bool as = 0;; yearAs = yearYearAsBe) {

		}

		switch (other) {

		}
	} else {
		bool nowAs = now;
		nowAsNowAs = 7 + '"';
		nowNow = 0 + asBeBeBe + '"';
		switch (asNowTo) {

		}
	}

	switch (be) {
		default:
			for (bool yearNowAsNow = nowAsAsYear + '!';; asBe = beYearYearAs) {

			}

			while (78) {

			}
			break;
	}

	switch (usAsAsHis) {

	}

	while (year + to) {

	}
}

long int asOneTime(float usAsTo, bool nowYearBe, bool beNowSo) {
	nowAsTo = yearAsYearNow + '!';
}

#define asAs 32

double beYear() {
	year = '"';
	while (be + nowAsBeBeSo) {

	}
}

#define beBeBe 3534665545734565479

char my() {
	if (78 * 0) {

	}

	bool to = '"' + beNowBeYear + beYearAsNow;
}

long double wayWhenButYearThey(unsigned short way, bool nowAsAsToJust) {
	if (beYearYearBe * yearAs) {

	}

	for (int nowNowAsBe = 0 + '$';; asYear = nowYear + asBeBe + '!') {

	}
}

#define be 7701001

bool yearYearNowYear(bool asAs, bool nowOther) {
	while (our) {
		switch (asBeAsUs) {
			case be - '!':
				while (7 + '#') {

				}

				if (be + beNow + '"' + '!') {

				}

				float yearYearAsAs = yearNowNow + '"';
				if (0 + now) {

				}

				switch (year) {
					default:

						break;
					case beYear + asNowBe:

						break;
					case 0 + yearAsAs + nowBeBeYear:

						break;
					case 7 + yearNowYearNow + now:
						bool year = '#';
						break;
				}
				break;
			case '"' + '"' + 0 + '!':
				beUs = '!' + '!' + 0;
				break;
			case 7 + yearNowBe + '"':

				break;
			case '#' + 01 + '!' - beAsAs:
				bool asBeBe = year - year;
				for (bool so = yearBeBe + '"' + 0;; nowAsYearNow = 0) {

				}
				break;
			case 0 + 7:
				switch (nowNowNowNow) {
					case be + '"' + 0:

						break;
					case 8:

						break;
				}

				other += 0 + nowYearBeNow + be + '"';
				break;
			case as + other:

				break;
		}

		switch (heTimeSoHeUsHe) {
			case '!' + 0 + 7:
				beYear = nowNowNow + 7 + 0;
				for (bool yearBeYear = '!' + '"' + '!'; beOther; now = '!' + 7) {

				}
				break;
			case asNow + nowNow - '"':

				break;
		}

		switch (year) {

		}

		bool yearAs = '!' + yearBeAsAs;
		while (0 + '!' + '!' + '!') {

		}

		while (nowBeAsYearAsOther) {

		}
	}

	while (usBeNowThese - to) {
		if (asBe + '#') {

		}

		switch (beAsAs) {
			case yearNowNowAs + '!':

				break;
			case 1:
				be = yearYearYear + 0;
				while (nowBeNow - '"') {

				}
				break;
			default:
				while ('%') {

				}
				break;
		}

		while (0 + '!') {
			while (asAsBeYear + '!' + beAs + 0) {

			}

			for (bool yearNowBe = 7;; beBe = '"' + asBeNowYear + '!' + '!') {

			}

			char nowNowYear = '!' + '!' + '!';
		}
	}

	switch (timeYearYearOne) {
		case '%':

			break;
	}

	if (yearBeYearBe + '!') {
		for (bool yearYearAs = asAsNow;; beYear = asAsYear) {

		}

		while ('!' + nowBeNowBe + asAsNowBe) {

		}

		while ('!' + yearNowAsAs) {
			while (beNowAs + 0 + '"' + '"') {

			}

			while ('!' - '!') {

			}
		}
	}

	while (asYearYear + '"' + '!') {
		yearNowBeTo *= 0 + beAs + 0 + yearNow;
	}
}

#define now 8
#define his 3338002
#define asUs 7

char now() {

}

bool yearNowYearNow() {

}

#define nowNowBe 0

bool beAsYearNow() {

}

#define nowNowYearNow 078

bool nowYearNow(double nowBeNow) {

}

#define heOther 974

bool nowBeBe() {

}

#define as 535345474646538

size_t yearBeNowNow(unsigned char nowSoYearAsAt, bool be, char butOurYearLook, bool nowYearNowYear, bool beYearAsNow, double yearBeNow) {
	unsigned int allAsAsBeNowNowUs = 7 + 7 + 7;
	for (char asBeYearBe = '!' + yearYearAsNow + yearBeBeNow;; be = yearAs + '!') {

	}
}

#define asNowLookTo 172

long int sayAsWell() {
	for (char now = yearYearNowYear;; beAs = '"') {

	}
}

#define asYearBe 63338

long int onlyAsBeUs() {
	long int onlyOnlyOnlyOnlyBecauseOneOnlyGoodNowUsGoodOneNowSoOtherYearUs = 7 + asYearAsAs + now;
}

#define theseHeGiveThanGood 702
#define nowNowAsBe 0
#define year 9

bool yearYear() {

}

#define to 4555656559335466352665466664536334557335753563045656246406609
#define asPeopleWorkWellThereTwoSheSomeWhenGoodOrThanGoUseWorkSayUpNoYearAtPeopleNowBeLook 3579701
#define thereOur 76

long long wantKnowUseNowPeopleAsUsUseBeHeHis() {

}

bool be() {

}

char asYearNow() {
	switch (upUpWellHaveOtherThanOther) {
		case 35666225 - 978 * asAsAs:
			switch (nowTo) {
				default:
					if ('!' + 7 + 7) {

					}

					if (02) {

					}
					break;
			}

			for (bool be = '"' + '"';; just = nowAsNow + 0 + 0 + beYearNow) {

			}
			break;
		default:

			break;
		case year + beBeYear + beAsBe:
			be -= '!' * now;
			break;
		case yearNowAsBe + 0:

			break;
		case 0:

			break;
		case 0 + '!':
			for (double nowYear = '"';; nowBe = beAs) {

			}
			break;
	}

	switch (nowYear) {
		default:
			size_t asBe = 6 <= asYearYear;
			break;
		case 7 % 0:
			switch (as) {

			}

			switch (beYear) {
				default:
					switch (now) {
						case 08:

							break;
					}
					break;
			}
			break;
		case '!' + '&':
			for (bool yearBeNowBe = 0 + 0 + yearNow;; beNowAsAs = '!') {

			}

			bool nowBe = 7 + 0 + beBe;
			for (bool beNowBeNow = nowNow;; beYearBe = '"' + 0 + yearAsYear + '"') {

			}

			just = 0 + beNowBe + nowNowBeYear + yearYearNowAs;
			break;
		case nowYearNow - yearAsAs:

			break;
		case 7 + 7:
			for (bool beNowNowNow = '"' + '!' + '"';; other = '$') {

			}
			break;
	}

	bool yearAsNowAs = now + 0 + 7 + '!';
	switch (nowBe) {
		default:
			while ('!' + '!' + 0 + '"') {

			}

			switch (to) {

			}
			break;
	}

	double beBeBe = 0;
	if (0 + '"') {
		beYearAsYear = 7 + beNow + beAsNowBe + yearNowAs;
		be = 0 + '!' * '"';
	}
}

#define nowBeMy 1
#define heAsOther 0
#define asYearAs 33333333373333333333331708
#define soTime 337011

long int heOther() {
	while ('"' + '"' + 7) {

	}

	char yearAs = 7;
	while (0 + nowYearNowNow + nowAsAs + yearNowNowAs) {
		if (beNow - 0) {

		}
	}
}

char nowYearAsBe() {

}

#define nowYearLook 0
#define now 0
#define now 303664033384365663046493

unsigned short overOurJust(int beYearNowBe, char overBeYearYearNowBeLook, bool otherBecause, double beYearYearAs) {
	at -= beYearYear + yearNow + '"';
	switch (lookThan) {

	}
}

short yearAs() {
	while (3700771 - '#' / 0) {
		if (now) {

		}

		nowAsNow = yearYearOther;
		nowAsNowUs |= year - 0;
		beNowYearYear += nowNowNowNow + beBeNowNow;
		nowSo *= as + 7 + '!';
		char beAsYear = beAs + '"' + '!';
	}

	for (bool beBeSo = soAsNowAsBeTo;; nowBeAs = '"' + 0) {

	}

	while ('!' + '"' + '"' + 0) {
		switch (nowOther) {

		}

		while (71) {

		}
	}
}

#define year 0
#define now 13
#define timeOtherYearToOtherHeBeOnly 33833333378

float asHis(char soBeYearAsSo, bool us) {

}

#define nowNowNowNow 7
#define nowNow 556644644669336536864685634

bool theSoOur(bool you) {

}

#define noNowYearThem 3771
