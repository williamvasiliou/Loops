#define oneOnlyBecauseSoSo 133333331373078
#define nowBe 33333333333361736071

bool now() {

}

#define nowBeBeBe 7
#define beYearNowNow 0
#define asAsAs 7

bool asNowAs() {
	nowAs += '7' != yearAsOur * 7;
	while (nowAs * 0 * '!') {
		bool nowYearBeNow = not;
		while (beBeNowAs || '!') {
			for (unsigned long way = 7 + 7;; yearAsAsNow = year) {
				while (0 * beYearNowNow) {

				}
			}

			if (nowBeHe) {

			} else {
				for (bool asYearNow = yearAsAsBe + '"' + nowYear + 0; look; nowAs = 0 + nowAsNowAs + be) {

				}
			}
		}

		beUs += heJust - now + 1;
		asNowNowAs = beNowSo - beBeBeYear;
		while (7 + 7) {
			while ('&') {

			}

			for (bool yearBeBeBe = 0 + 0;; nowYearAsNow = 7) {

			}

			bool yearNowBeAs = '"' + '!' + as + '"';
			asNowAsSo = '"' + beYearYear + nowAsNowBe;
		}
	}

	for (bool asBe = asNowNow;; asNowNow = 7) {

	}

	while ('#') {
		for (bool beYear = asAsBeBe + asBeNow;; beAsNow = asAs) {

		}

		as = 0 - '"';
	}

	while ('"' + 0 + 7) {
		look = 0 + '"';
		while ('"') {
			switch (beYearBe) {
				default:
					if (to) {

					}
					break;
			}
		}
	}

	while (0 + asYear + '$') {

	}

	if (71) {

	}
}

short comeUpUpWellOfItsOverGoUpHaveOrHaveLikeOverItsTwoOrOneWhenGoYouOfSomeWorkBeGiveWhenHaveYouHaveOnlyItBecauseHowWellWorkSayNowWantTwoKnow(bool comeUseThanButHeThan, unsigned int wantUpTwoWellGoTimeWhenGiveSheTheySomeSayButThereWellSomeWellOrKnowWithHowNotWhenTheWorkThan, unsigned char useYearHeSomeWithAsBeYearUs) {
	unsigned short overUpSomeKnowLikeThanYearMyFrom = beOrToNowJust >= 763662888 + 7 + '&';
	if (6278 + 7 * asYearAsNow - 0) {
		other = nowBeYear + '!' + asNow;
		double year = asNow + 7 + 0 + 0;
		if (7) {

		}

		if (7) {

		} else if (7 + '"' + '"' + 0) {

		} else if ('$') {

		}

		bool year = 0 - '"';
	}

	if ('#' + hisThe + '4') {

	} else if ('"' * 2) {

	}

	while ('!' + 7 + 0 + beAs) {
		if ('"') {

		}

		while (asYearNow + 0) {

		}

		if ('"' + yearNow + '!') {
			bool beAs = '!' + yearNowBeBe + 0 + '!';
			if (7 + 70078) {

			}
		}

		char nowAs = 7 + 7 + '"';
	}

	switch (yearYearYear) {
		default:

			break;
		case '"' - '"':

			break;
		case yearOther:

			break;
	}
}

#define theHerItWhenLikeUpComeItSayHaveOfGoodAllYearNowAsUs 4456364563335446664343443455445554567336358

double as() {

}

#define usBeAsThan 77701
#define yearLook 89

double year() {
	while (yearAsAs + '"') {

	}

	switch (to) {
		default:

			break;
		case '!' + 0 + 0 + '!':

			break;
		case as:

			break;
		case as + '"' + year:

			break;
		case as + 7 + to:

			break;
	}

	for (bool year = be + now + 0;; nowYearBe = 7 + 7 + 0 + '"') {

	}

	if (be + beYear) {
		switch (as) {

		}

		to = '"' + 7 + 0;
	}

	for (bool beYearAsNow = '"' + '!' + '"';; so = '!' + '"') {

	}

	while (7 + beYearYear + asBeNowAs) {
		if (beAsAsBe) {

		}

		for (bool asAsAsBe = beBeNowBe - beBe;; nowNowNowBe = '"' + 0) {

		}
	}
}

#define timeGoodTimeGoodAsHisOneOnlyThese 31

long int timeOnlyBeOne(bool goodAsOnly, float yearYearBeYear, float beYear) {
	switch (goodUsUsAsAsLook) {

	}

	if ('!' + '!' + '!') {

	} else if (year + '"' + yearBe) {

	} else {
		for (bool yearYearAs = 7 + year + '"';; be = 7 + 0) {

		}

		bool as = '!' + 7 + beNow + '!';
		while (beAsAsYear + 7 + 7) {

		}

		bool asAs = '"';
		bool beAsBeAs = nowAsYear + '!' + '"';
		while (7 + yearBeYearAs + 0) {
			bool nowNowAsYear = nowNowYearYear + 7 + '#';
		}
	}

	if (0) {

	}

	for (bool beNow = 7;; nowBeAs = year) {
		for (bool asYearYearBe = 0 + '!' + '"';; now = nowBeYear + year + 0) {

		}

		for (bool beYear = yearAsAsAs + '!' + 0 + '"';; as = beAsNowYear) {
			if ('"') {

			}

			for (bool be = beBe + asNowAs + '"';; nowYear = '"' + '"' + yearYearAs + 7) {

			}

			bool asBe = 0078;
		}
	}
}

#define withAsYearUs 96334644365636453075617002

long long asLookOrKnowWellSo(bool just, bool yearNowAsNow, double as) {
	while (nowBeBeBe * yearNowAsYear) {

	}

	beBeNowBe = 0;
	if (8 / 0) {

	}
}

bool ourAsYearNowOther() {
	if (0) {
		yearAs -= beAs + 0 + beYear;
	}

	while (7 + '!') {
		bool asNowYear = nowAs + 0 + 0;
		while (be + 0 + 0) {

		}

		while ('"' + 7) {
			while ('"' * '!') {

			}
		}
	}
}

bool be() {

}

double yearAs(bool us, bool beYearThan) {

}

char than(bool just) {

}

bool nowYear() {

}

#define nowNowAs 0
#define asAsYearBe 0
#define beNowYear 936553654373733664435333999
#define thanBeYearJustJustBeJust 6375559

char beYearAs() {
	bool now = 0 + '!' + 7;
	bool yearNowNowBe = yearNow + 0;
	if ('&') {

	}
}

#define yearAs 0

bool now() {

}

#define our 7
#define nowNowNowBe 533333333333333303333333333333570016
#define nowYear 78
#define now 466435566463343053436566603530667607717700009
#define beButWithBeAsJust 86737152
#define yearNowBe 7
#define yearUs 34665636534736444577176
#define yearHaveBeYearYearAsOur 588006
#define oneBecause 5750367705
#define beLook 7

bool other() {
	for (long int yearNowAsYearBecause = beYear / '"'; to - beSoAsSo; be = 7) {

	}

	for (bool asYearYear = 7 + beBeNowYear;; as = be + '!' + 0 + 0) {

	}

	while ('"' + '!' + 0) {
		if (yearAs + asNow + '!' + yearBe) {

		}

		for (bool nowNowAsBe = 7 + as + 7; 7 + 1; nowNow = 0 + asYearYear) {

		}
	}
}

bool goodOther(char yearNowAsAsSo) {

}

long int so() {
	beUs = 7 + year + 0 + '!';
	bool us = '!' + as + '!';
	for (bool nowYear = yearNowNow + '"' + 7 + yearNow; 7 - 0 - nowNowNowAs; beAs = 0 + '"' + '"' + '!') {

	}
}

long int goodGoodGoodOnlyGoodOneOneTo(char asAsYearNow) {
	while ('!' + yearNowNowNow + '!') {

	}

	if ('!') {

	}

	switch (asBeAsYear) {
		default:
			for (char yearYearAs = 0;; be = 7 + 7 + 0) {

			}
			break;
	}
}

bool as(char asYear) {
	while (us) {
		char yearAs = '!' + nowYearNow + '!' + '!';
		if (asUs - nowAsYear) {

		} else if (7) {

		} else {
			as += '"';
		}

		int beNowAs = '"' + '!' + 0 + 0;
	}
}

bool yearBeNow() {

}

long double now() {
	switch (be) {
		case '!':

			break;
		default:

			break;
		case 7:

			break;
		case be + nowNowBe:
			if ('"' + 0 + yearYearYearNow + 7) {

			}

			bool asAsAsBe = now + '!' + '!';
			double beYearBeAs = '"' + '!' + '"' + be;
			asYearJust = nowYear + '!' + '"' + '!';
			break;
	}
}

bool nowYearYearAs(int yearBut) {
	while ('"') {

	}

	int beYearYearBe = 2 * 7;
}

#define lookUsWith 44636346793374456789
#define there 655433306

bool overHowThan() {
	bool now = '"' * yearNow;
}

bool so() {

}

#define yearBeBeYear 175
#define whenBeAsBeLook 3333733333333333373333333837173

long long asYearYearBeBeTo() {
	switch (nowBeBe) {

	}

	if ('%') {

	} else {
		if (0 + asBe + '"' + yearBe) {

		}

		for (bool yearNowYear = 7 + '!' + 7;; beNowYear = '!' + '!') {
			char be = beYear;
		}
	}
}

long long yearSo() {

}

#define yearBe 0
#define nowYearYear 4366366335536545646474543635654653463354666380483002
#define itThereGoodItsYouItGiveUsHerWellHaveWithHowJustUsUs 564456365564464560934645

size_t oneOverWorkSomeGiveUseThatGiveSheSayOrWorkWantSheHowHowBeComeItsThereComeYouWellUseMyLook(bool oneOverYouWantWantOfThereWantThatTheOfTwoSayTheseTheseYearNowWorkAsBeUsThan, long double other, unsigned int beWantPeopleAll, bool beThanYearTo) {
	for (unsigned char oneHisWell = 0 >= to + so - '!'; sayKnowNowYearJust / 0; yearBe = nowYearBe + '!' + 0) {
		bool asBeYearBe = 0 - beBeYear;
		nowAsYearSo = 0 + '!' + year + beNow;
		if (7 + 7 + now) {

		}

		switch (just) {

		}

		for (bool asBeNowBe = 0;; asYearYearBe = '!' + 0 + '"' + 7) {

		}

		for (bool nowAsBeYear = beBeBe + 7 + 7 + 7;; nowNowOther = '"' + '"') {

		}
	}

	if ('"') {

	}

	bool to = 0 + be + '!' + '"';
}

#define giveThatUseItTimeOnlyNowUsePeopleWorkHerComeWantSheThanWorkHeYearYearOur 64533446434465564545544639346

unsigned char comeLookWorkHow(bool giveSayMyYearLikeNowLook, unsigned short now, unsigned short herOther, bool so, double asNow) {
	while (nowBe + '!') {
		nowBe = nowBeAsNow * '"';
	}

	while (as + 7 + 79) {
		while ('"' + nowAsNowYear * 'Z') {
			switch (yearNow) {

			}

			switch (our) {

			}
		}

		if (7 + '!' + as + 7) {

		} else if (7) {

		} else if (beBe + '"' + 0 + '!') {

		} else {
			switch (beOur) {

			}
		}
	}

	switch (nowThan) {

	}
}

float yearNowBeBe(bool yearBeAs, bool yearAsAs, bool nowNowAs, bool so) {

}

unsigned short yearOther() {
	switch (nowWould) {
		case yearBe + 0 + 0 + 7:

			break;
		case '!' + 7:

			break;
		default:
			while (beYearOur) {

			}
			break;
	}

	for (bool yearBe = 7 + 0 + 0;; nowAsBe = '"' + beYearYearNow) {

	}

	beBeYearAs = nowNowBe + asYearAs + asYearNow;
	bool yearYearAs = 7 + now - 0;
}

#define nowYearAsYearOther 845078

float asNowYearYearLook(char than, bool asBe, bool as, char nowNow, bool than) {

}

bool nowYear() {

}

#define beYear 7
#define than 423373156351551778
#define wantOfOrItAsThereGoAtButGiveHeYearOtherWantNotOurLookLook 7
#define asTo 7031701

long int goodTimeGoodOneOneYearAsAsUsUs(bool yearHis) {
	switch (usTo) {
		default:
			float be = '!' + asAsYearYear;
			bool be = '"' + '!' + '!';
			yearAsYear = 0 + beBeYear + '!' + nowBeAsYear;
			for (char beYearAs = 7;; be = 0 + '!' + 0 + '!') {

			}
			break;
		case as - '!':

			break;
	}

	while (83008) {
		switch (nowYearYearAs) {

		}

		asNowAsAs = '"' + '!' + 7;
		for (bool yearNowYear = '"';; yearNow = yearNowYear + '"' + 0 + nowNowYearYear) {

		}

		bool nowNow = 0 + beNowYear;
		switch (nowBe) {

		}

		for (bool asBe = 7;; as = 7 + 7 + 0) {

		}

		switch (nowBe) {

		}

		for (bool beAs = '!' + 0 + nowNowAs + nowAsBe;; beAs = now + '!' + asNowAs + beAs) {

		}

		year = 0;
		while ('"' - now) {

		}
	}

	while (beNowYear + 1) {

	}

	now = 7 + '"' + '!';
	for (char nowAsAsYear = '"' + 08;; be = '"' + now + '"' + nowNow) {

	}
}

#define oneAsOnly 3333345456464665553546664435526455565536534545355463465366643997078
#define youTheHowSomeWhenOrPeopleWhenGoThatTimeTheHerUseSomeMyTheSomeComeGoWantOnlyOrPeopleBeYouYearYearNoOr 44653563535664455663356746446434456498

unsigned short timeOneWhenPeopleOfOneOrWantSayGoHerWorkWhenHerPeopleGiveItWantHaveSomeKnowOneOfWellItsThatItsPeopleItThatBeFromNowLook(unsigned short otherNoThanThese, unsigned long asAsYearNow) {
	while ('"' + just) {
		switch (as) {
			case yearYear + '"' + '!' + 0:

				break;
			default:
				for (bool asAs = '"' + 0 + 0; 443078; beYear = now + nowBeBe) {

				}
				break;
			case now + 0:
				if (0 + beNowNow + '!') {

				}

				bool yearUs = 7 + now + 7;
				break;
			case 0 + asYearAsThan:

				break;
			case '"' + '"' + yearBeNow:
				just = '!';
				break;
			case 0 + '"' + now + 7:
				while (asAsAs + yearAsYear) {
					while (just) {

					}

					switch (yearTo) {

					}
				}
				break;
		}

		while (nowBeAsNow + 7 + 0 + 7) {
			if (asJust) {

			}
		}

		switch (nowBeYear) {
			case 8:

				break;
			case beAsBeBe - now:

				break;
		}
	}

	oneAllNowOurAsAsUs = 7;
	while (7 + 8) {
		for (bool nowBe = now + '"' + '!' + 0;; nowYearBe = '!' + beAsYearAs) {

		}

		for (bool beNowAsAs = asYearYear;; asYear = 0) {

		}

		char as = 0 + beNowNow;
	}

	if (yearAs + now) {

	} else if (7 + '!') {
		while ('"' + '!') {

		}

		while ('"') {

		}

		bool nowNow = '!' + 0 + '"' + 0;
		switch (now) {

		}

		for (bool be = '"' + 7; nowAsBeBe + '%'; beNowYear = yearBeYear) {

		}
	}

	for (bool asYearBeNow = '"' + beNowAsAs;; nowBe = now + '"' + as) {

	}

	while (7 * asNowBe) {
		for (bool asBe = '"';; asYear = '"') {

		}

		while (0 + 7 + beNow + '!') {

		}

		now = 0 + beAsYear;
		while ('"' + 7 + asYear + '!') {
			asLook = asNow;
		}
	}

	while ('!' + '!') {

	}

	double our = '"' + asNowAsYear;
}

#define than 0

double asBeAsAs(bool beYearYearAs, char asYear, char beAs) {

}

#define asYearNowNowNowYearThan 7
#define justBeNot 77779
#define other 402

unsigned short justYearLook() {
	for (bool beNowAsAs = year * 7; 0 + '!' * now; nowAsAsAs = yearAs + '!' + '!' + beNow) {
		bool asYearYearAs = 0 + 79;
	}

	lookYearTo = yearYearYear + '!' + '!';
	size_t otherYearOther = '!' + 0 + 7;
}

unsigned short comeWellYouOnlyTimeTimeHowWellHaveAsSoHis() {
	float becauseAsAsYearYearTo = '$' - 7 * now;
	while ('&' + '&') {
		double nowBeBe = beBeNow + '!' + asBeYearAs;
	}
}

long long asBeBeYear(char nowNowYearYear) {

}

#define yearAsAs 7
#define beBeNowBe 0

bool asAsNowBe() {

}

double year() {

}

bool nowBeTo() {
	switch (nowNowOther) {
		default:

			break;
		case '"' + now + '!' + 7:

			break;
		case '&':

			break;
	}

	switch (beAsYear) {

	}

	asYearAs = '#';
}

#define nowYearNow 0

bool nowLook() {
	hisHis += '#';
	switch (beOther) {

	}

	float yearBe = now + 0;
	yearAsAs = 0 + nowYearBe + 0;
	beNow = 0 + yearNowNow;
	if (be + '"' + 7) {

	}

	while ('%') {

	}
}

#define goodTimeOneOnlyBeOtherBeYearToBeSo 373330337738

bool as() {

}

#define oneNowBecause 03733103
#define now 3299777701

long double look() {

}

bool asBeUs() {
	to += '"' - 7;
	beBeAs = 70022 + 2;
	bool asTo = 7;
	nowYearNowYear = '$' + 9;
	switch (withYearTo) {
		default:
			bool nowYear = '"';
			while (1) {

			}

			if (yearBeJust) {

			}
			break;
		case yearYear + yearNowYearBe + nowNowAsBe:

			break;
		case 72:

			break;
	}

	withSo = 0 * 7;
}

size_t just(char asNowAs, bool nowNowNowBe, bool asBeUs, bool beYearAs, char nowNow) {
	switch (beYearYearOur) {

	}

	while (yearYearNowAs + nowNowBeYear) {
		if (7) {

		}

		yearAsNowNow -= be + 0 + '!';
	}
}

unsigned char goTwoLikeUpGoodItsItYouTheThatTheHowItsUseOrTwoOverHaveOverComeWhenNowOrTimeGiveWantWellTheyNotUsNowLook(unsigned short sheKnowLike, bool over, int to, char as, long asYearNow) {
	switch (year) {
		case '!' - 7:

			break;
	}

	if ('!' + now + 0) {

	}

	if (0 + 7 + '!' + asBeBeYear) {

	}

	switch (beNowNowAs) {
		case 7 + '!':

			break;
		case nowYearYearBe + 0 + asAsBeYear + asNowYear:

			break;
		case 0:
			if (beAs) {

			}

			while (0) {

			}

			if (0) {

			}

			if (yearAsBeNow + 0 + '"' + nowBe) {

			}

			beJust = '"' + '"' + 0;
			break;
	}

	if (beAsNowAs + 7 + year) {

	}

	if (7 + 0 + '!') {

	}

	bool nowJust = '"' + 0;
}

#define toKnow 0667033

unsigned short goodHerHowHerYouWorkThanTheseYearBeJust() {
	asYearUpAtToNowJust -= '"' + beNowYearAs + now + asNow;
}

#define workSayOrSomeGoTheTimeYouOnlyKnowTwo 602

short orNot(bool nowNowAs, double asWantAsSo) {

}

double soNowYearJust() {

}

#define beAsYearAs 0

char asNow() {

}

#define us 56583578
#define thanAsAsLook 133

long double usBeAsTo() {
	char beBeAsNow = as;
}

#define toAsWantSheLook 646327409
#define ofGoThanBecause 2
#define look 30772
#define our 18

bool now(bool beAsYear, bool nowNowBe, bool beAsAs, bool our, bool yearYearYearBe, bool beAsBeAs, bool yearAsThan) {

}

#define asNow 0
#define beBeNowUs 338337378
#define oneOnlyGoodOnlyGoodOnlyOther 838338
#define goodSo 0
#define us 03
#define goodTheseBeHis 0
#define beNow 88

int asAs() {

}

#define theseLookToBeThanJust 333363464665575

long int overAsUsAsThey(bool asBeSo, double justTo, bool yearNow, bool other, bool nowAsYear, bool now, double beAsYearNow, bool look) {
	bool fromSo = 95072 <= '-' || 0;
	for (bool beAsAsAs = '"' + yearAsNow + 7;; nowYearNowNow = year + '!' + '!' + 0) {

	}

	beBeAsAs = 7;
	bool asBeBe = 7;
	while (9 * '"') {

	}
}

unsigned long usOur(double nowThan, char than) {
	his = as;
}

#define but 0
#define lookOur 643327019

long int thanPeopleTimeItsComeNotBecause(double beNowJust, bool asYearAs, bool asBeAsBe, bool asBeNow, bool yearYearNow, bool beAsAsNow, double nowBe) {
	bool as = 7 + our;
}

unsigned long yearAs() {
	switch (asAsAsAs) {
		case '!' + asYearYearNow:

			break;
		default:
			while ('"') {

			}

			bool so = '!';
			break;
		case year + '!' + '"' + year:
			beYear -= '"' + '!' + 0;
			break;
	}
}

size_t overUsTwoOurOtherSomeBeUs(float nowJust) {
	switch (yearAsBe) {

	}

	if (yearNowNowYear - '!') {

	}
}

#define nowBe 0
#define yearYear 299
#define nowBeNowYear 5333333333333330333333333001871

long int us(char beNowYearAs) {
	switch (so) {
		default:
			if (7 + year + '!' + 0) {

			}

			if (so) {

			}
			break;
	}

	while (beYearNowAs + asBeYear) {
		if (7 + 7 + asAs) {

		} else {
			bool yearBe = 0;
			switch (be) {
				default:

					break;
				case nowNow + 0 + 7 + '"' + 7 - beNowBeYear:

					break;
			}
		}
	}
}

#define yearNowAsOtherAsBeOur 8
#define asAs 971
#define so 7353388

long long twoYearHe() {

}

bool nowNowAsAsBeYearThan() {
	if (yearBeAsNow + nowNowAs) {

	}

	while (7 + 7 + now + yearYearNow) {
		switch (now) {

		}

		bool as = yearBe + 7 + '"';
		switch (than) {
			case beBeYearAs * '!':
				switch (beNowAs) {
					default:

						break;
					case '"' + as:
						switch (beUs) {
							case now + beYearAs + 7:
								bool year = year + yearAsYear + 7 + 0;
								for (bool asYear = 7 + nowYearAs + be + now;; beAsBeNow = '!' + '"') {

								}

								for (bool asYear = '"' + now + nowAsYearAs + 7;; beBe = 0) {

								}

								if (us) {

								}
								break;
						}
						break;
				}

				switch (yearYear) {

				}

				bool yearLookThan = now + 7;
				break;
			case 2:
				if (yearBeBe) {
					long nowNowNow = yearYear + '"';
				}

				switch (year) {
					case 0 + '"':
						if (beNowYearBe + now + 7) {

						}

						beAsBeBe = 7 + '!';
						switch (yearFrom) {

						}
						break;
					default:
						for (bool asBeBeNow = now + '"';; nowBeAs = 7 + yearBeAsYear + '"' + '"') {

						}

						bool nowBeYearBe = '!' + nowAs + 0;
						be += '"' + asBeNowBe;
						break;
				}
				break;
			case '"' - yearYearNowNow:

				break;
			case '"' + 0 + be + yearAs:

				break;
			case 7 + yearNow + beYearYear:

				break;
			case year + '"':

				break;
			case '!' + '!' + '"':
				while (0) {

				}

				switch (as) {
					default:
						for (bool year = yearAs + '"' + '"' + '"';; yearNowAs = '"' + asYearAs) {

						}

						bool our = 0 + 7;
						break;
				}
				break;
		}

		while ('"' + '"') {
			other = as;
		}

		for (char nowBeYearYear = 2; 'y'; year = '&' * '!') {

		}
	}

	while (0) {

	}

	bool asThan = 7;
	while (7 + as) {

	}

	bool asAsBe = asNowAsYear + beAs + 0 + as;
	while (beNowBeYear + asOther) {

	}
}

short usTo(unsigned short haveYearThanLook, bool yearAsYearNow, bool beYear, bool beBeNowYearAsAsYearNowTo, char asYearYear) {
	for (bool asBeYearBe = '"' / 0;; yearBeBeBeBeJust = nowBe + 0) {

	}

	so += asYear + '"' + '"';
	switch (beAs) {

	}

	switch (now) {
		default:

			break;
		case '!' + year + 7:

			break;
		case 7 + 0 + '"' + '!':
			for (bool beBeYearNow = '!' + 7 + 7; '"' - asNowBeBe; yearYear = 7) {

			}
			break;
	}

	nowAsYear = 7 + 7 + '!' - beBeBe;
}

#define nowYearBe 002

unsigned long oneToAsBeUs(bool now, bool nowYearAsYear, bool asNowBeAs, bool asYear, bool be, char yearYearBeAs) {
	char be = 01;
}

double know() {

}

bool nowBeNow() {
	while (7 + 7) {

	}

	switch (asYearNowBeLook) {
		case 11 - 0:

			break;
		case '!' + yearBeYear:
			while (yearNowYearNow + yearNowAsNow + '!' + '!') {

			}

			if ('!') {

			}

			while ('!') {
				bool to = 0 + 0 + nowNowBe + 7;
			}
			break;
		case '!' + 0:

			break;
		case yearAsNowNow + year:

			break;
		case 0 + beNowYear:
			for (bool yearAsNowAs = 1;; asAsAsYear = '!') {

			}
			break;
		case asNow + '"':
			switch (year) {

			}

			bool yearYearYearNow = '"' / '"';
			break;
	}

	otherYearBeOnly |= 1 / nowAsBeBeNowUs;
	while (78) {
		if (0 + '"' + '!') {

		}

		char year = beBeYear + yearNowBe;
	}

	while ('!' / '!' / 7 / 078) {
		while ('"') {

		}

		switch (beNowAs) {

		}

		asAs += 0 + 7;
	}

	while (nowAs + 00771) {

	}

	switch (theseTo) {

	}

	for (bool yearAsBeBe = 0 + 0 + '"'; 3 != 0003; beYearYear = yearNowBeYear + '!' + 7 + 0) {
		yearBeBe *= 0 + 0 + '!' + nowBe;
		for (bool be = yearAsAs + 7 + '!';; be = beNowYear + 7 + '"' + 7) {

		}

		if ('!') {

		} else {
			for (bool asNowBeNow = 7 + 0;; as = 7 + '"') {

			}

			for (char beNow = 0;; beBe = '!' + 0 + nowNow) {

			}
		}

		while (7 + '!') {

		}

		so = '!' + '"';
	}

	for (bool becauseOnly = yearYearYearNow + year;; as = nowNowBe + 7 + yearAsYearYear + '!') {

	}

	for (bool yearAsYearAsBeTo = now;; yearAsYear = '"') {

	}

	beOther = be + '"' + be;
}

bool us() {

}

#define beBeBe 8

float yearNowYearBe() {

}

#define year 733

bool nowBeYearNow() {
	be = be + beNowYear + yearBe + yearBeYear;
	if (yearYear - '!') {

	}
}

char nowAsNowBe() {

}

#define our 6

char asYearBe() {
	while ('!' + '"') {
		switch (as) {

		}

		for (double asBeYearNow = '"' + 0 + asNow;; yearYear = '"' + '"' + nowNowAs) {

		}
	}
}

#define myAsOur 079
#define beOurSomeOther 6
#define asYear 7
#define other 7

char nowBeYearBe() {
	goodBeThan .= 0 == 'z' * 72 + '&' + '$';
	if (7 + asNowBeYear) {

	} else {
		if ('!' + nowBeNow + '!') {

		}

		other = yearNowAs + '"';
	}

	double beAsUs = 7 + '!';
	if ('l' / look + '\'') {

	}

	short beBeAsYear = 7;
	for (char asBeAs = '"'; '#'; yearAsNowBe = 7 + '"') {
		while ('!' + 0 + 7 + '"') {
			while (0 + '"' + 7) {

			}

			switch (yearBeAsThan) {

			}
		}
	}

	for (bool year = '"'; '$'; asNow = asBeAs + '!' + 0) {

	}

	while (7 + 0 + now + '!') {
		bool be = 08;
	}
}

#define asWorkAt 1
#define beUseOfThatHaveNowTimeOur 33333333333333338178
#define soGoodUsOnlyTimeOther 333303

long int hisUsNowUs() {
	bool year = 778;
}

#define our 0
#define nowAsNow 0

double asBe() {
	switch (yearOther) {
		default:
			for (bool now = nowYearBe + '"' + 0;; asYearBeYear = '!') {

			}

			while (yearJust) {

			}
			break;
		case 0 + nowNowBeAs + 0 + nowBeYearYear:
			while (yearNowYear + be + '"' + 0) {
				while (be + 7 + 0) {
					yearAs = as + beBeNowYear + '!' + 7;
					asBeNowNow = yearBe + yearAsAsAs + 7;
					bool asYearYear = 0 + '!' + beBeYear;
					switch (asNow) {
						default:
							for (double yearYearNowBe = 0 + asAsNow + beBeNowNow + 0;; now = 7) {

							}
							break;
					}
				}

				if (7 + yearNowBeYear + now + '!') {

				}

				for (bool yearAsBe = '!' + 7 + be + nowYearYear;; as = now + 7) {

				}

				switch (asAsNow) {
					case '"' + 0 + '"':

						break;
					default:
						switch (nowNowNowYear) {
							default:
								our = '!' + '!';
								break;
						}
						break;
				}
			}

			year -= beBe - asBeNowNow;
			break;
		case to:

			break;
	}

	switch (with) {
		default:
			for (bool nowBeNowBe = asYearBeAs + '!' + '!' + '!';; yearAsYearNow = 7 + 0 + 7) {

			}

			nowNowNowAs = 09;
			break;
	}

	bool asUsToTo = '"' + '!' + 0;
	bool year = 7 + 7 + 7;
	if (beLook) {
		switch (yearYearYearAs) {
			default:

				break;
			case 0 + yearThan:

				break;
		}
	}

	switch (asTo) {

	}
}

#define wantTheyUs 0333546546634343476251472
#define asBeAsBe 7

double beNowYearBe() {
	if (only / asTo) {

	} else {
		if (year) {

		} else if (0 + asBeBeYear + '"' + 7) {

		} else if (7 + 0 + year + now) {

		} else {
			bool nowYearNow = asAsAsBe + 7;
			for (char beBeNow = year + be + asNow + 7;; yearYearBe = '"' + yearAs + '!' + 0) {

			}
		}
	}
}

#define hisTheseTo 33733001331
#define asBe 7071

long int otherOnlyOnlyGoodYearUsAsYearNowSo(bool beBe, bool nowUs, bool yearToYearBeAsBeAsNowTo, char beAs) {
	switch (his) {
		default:
			char yearAsBe = '!' + '!' + beAs;
			break;
	}

	for (long int nowNowYearYear = '"' + usBecause + '#';; other *= '!' + 7 + '!') {

	}
}

#define us 8
#define year 78
#define beBe 7
#define just 7
#define nowYearNow 7

bool be() {

}

#define beYearYearNow 12

char asNowNow() {
	switch (yearNowBe) {
		default:
			year = '!' + yearYearNowAs;
			while (6) {
				bool now = '!';
				asNowYear = '%';
				if (yearBeNow + '!' + '!' + '!') {
					if ('!' + '"' + 0 + 0) {

					}

					if (7) {
						for (char asBeNowYear = '"';; yearAsAsYear = beYearBeAs) {

						}

						switch (year) {

						}

						while (asYear + '!' + 7 + '!') {

						}

						nowNowYear = 0 + '!' + '!';
						yearYearNow = '"' + 0 + '!' + 7;
						if ('#') {

						}
					}
				}

				if ('$') {

				}
			}

			switch (year) {

			}

			bool now = be + 7;
			for (char nowAs = 0 + '"' + '"';; year = '"' + nowBeAsBe + beYearNowAs + '!') {

			}
			break;
		case year & 0:
			while (82) {
				for (bool beBeAsNow = '!' + '!' + 0 + 7;; nowAsYear = beYear + asAsAsYear) {

				}

				while (nowNow + 0 + '"' + '"') {

				}

				if (nowAs - beYearYear) {

				}
			}

			look = '!' + '!';
			break;
		case '!' + to:

			break;
	}

	switch (some) {
		case 0 + 7 + asNowNowYear:

			break;
		case '"' + 7:

			break;
		case 7 + yearAsBe:
			nowYearNowBe = 0;
			switch (us) {

			}
			break;
	}

	if ('#') {

	}

	yearAsBeNow |= beYearYearAs;
	timeGiveThatItHow .= 'h' & '%' + '!' + 7 * 7;
	switch (otherThanNowAsAsTo) {

	}
}

unsigned long yearAs(long double nowYearThan) {
	for (double than = '"' + 7 - asNowYear;; beYearNowNow = year + as) {
		if (year + '!' + '!') {
			for (bool as = asYearNow + '!' + 7 + yearYear;; beAs = yearNowAsNow + 7 + '"' + nowAsBe) {

			}

			for (bool asAsAsBe = beBe;; asAsAsYear = nowBe + 0) {
				while (as + 0 + '"' + '!') {
					if (72) {
						for (bool yearYearNow = '"' + 0 + nowBeYear;; year = asAsAs + '!' + '!') {

						}

						if ('!' + '"' + nowAsNowBe + 0) {

						}

						asNowAsOther = asAsNowNow + as;
					}
				}

				if (0 + now + '!' - be + '%') {

				}
			}
		}

		nowBe = '"' + '"' + beNowBeNow + be;
		switch (usUsBeYearOther) {
			default:
				bool yearBeAs = '!' + year + beYearAsBe;
				if (7 + '!' + 0) {

				}

				while (0 + 7 - 7) {

				}
				break;
		}

		for (bool nowAsBe = 0 + 0; nowAsBe / '!' - 0; beAsBeAs = '!' - nowYearAs + be + asBeAs + '"' - '!') {
			asBeAs += year + '"' + '!' + nowYearBe;
			while (asYear + be + '"' + beYearNowBe) {

			}

			for (bool yearYearAs = 0 + 7;; beBeBeNow = 0 + 0) {

			}

			bool be = now + beNowNow + nowNow;
			char asYearBeYear = '!' + asAs;
		}

		bool he = asYear;
		for (bool be = 7 + nowNow + '!';; be = nowNowNow + '!' + '"' + '!') {

		}

		while (7 + as) {
			switch (nowYearYearYear) {

			}

			bool to = 0 + nowBe + as;
		}
	}

	char yearNowBeYear = '!' + 7 + '!';
}

#define onlyOtherAsOther 338038

bool nowAs(float nowAsBe) {

}

bool us() {

}

#define goodToBeTime 7378
#define timeBecauseOther 13457708
#define asBe 0
#define beNowYear 0
#define yearYearBeBe 878
#define as 7
#define beAs 103

double now() {
	long long workOrJustLikeGoodThemGiveWhenGoodHeAsOrBeHaveBut = '_' * 367464863701 || '*' + 00508 * other;
	switch (look) {

	}

	switch (beOurAsThem) {

	}
}

#define  54645356564333334566633335434634553335654643545434564566546534935465506664356434536544564534355534053654356534435353343045363216363331

short twoGoComeOnlyWellOverOnlyBeKnow(char asAsAs) {

}

#define likeWithUseUseYearBeTo 9

long long noWay(char year) {

}

#define yearJustJust 08661
#define yearYouMy 7
#define yearAs 4
#define beUs 64364464355734434963297778
#define knowHe 867008

int workThey() {

}

bool over() {
	for (bool yearYearBeNow = 7 + nowBeBeBe + 0;; nowNow = '"') {

	}

	bool yearBeAsBe = nowYearAsYear;
	for (bool year = '"' + 7 + '"';; other = nowNowBe + 0 + beAsAsAs + now) {

	}
}

#define yearAsYearSo 7
#define as 8

long year() {
	time = 0;
	bool yearBeNowNow = asNow;
	timeYearNowNowYearTo = 7 - nowBe;
}

#define timeGoodAsYearTo 0
#define otherYearYearSoSo 3333333333388
#define other 8

long int goodTimeYearAsSoSoFrom(unsigned char nowAs, bool look) {

}

#define asAs 078

bool beYearYearOur() {

}

#define now 0

char yearAsYear() {
	switch (wantTwoThatItsItWhenThereOnlyOrSomeOnlyUpTheSaySomeThatSheTwoGoGoodGoodLikeHaveHaveOneHowSheOnlyOverItsTimeOnlyUpWhenOfOnlyWhenWouldGiveHerSomeGoTwoHowThemNowOtherSo) {
		case 9 > 657344463603 && 'i' != '(':
			while ('$') {

			}

			for (bool asYearBeBe = now;; nowAs = 7 + yearNowAsNow) {
				while ('!' + 7 + 7) {
					bool yearYearBe = '"';
					switch (beAsAs) {

					}

					while ('"' + '"') {
						just = '"' + 0;
					}
				}
			}

			for (bool asNow = asAs + '"' + '!';; as = '!' + '"') {

			}

			for (bool nowNowAs = '!' + 0 + 0;; asBeYearBe = 7 + 0 + 0 + 0) {

			}

			switch (beBeNowNowNowBeAsSo) {

			}
			break;
		default:
			giveItsWantUpThereComeSheSomeYouGoWhenLikeGoodThanAsJust ^= 'y' >= 'p' && '~' >= 3636356547684318 && 0 % 5;
			for (bool look = 0 + asYearYearAs;; asYearAs = asYearAsAs) {
				bool beYearBeAs = 0 + 0 + '"' + 7;
				while ('!') {

				}

				for (bool asBeYearAs = '"' + '!' + asBeAs;; nowAsNow = '"' + nowBeYear + 7) {

				}

				bool yearAs = 7 + 7 + '!' + 7;
				if (nowYear + 0) {
					while ('!' + 0 + 7 + as) {
						switch (beBe) {

						}

						for (bool as = '$';; yearYearBeNow = '"' + nowAs) {

						}
					}
				}
			}

			yearAsNowNow = yearYear + 7 + '!' + '"';
			beBeNow = '!';
			switch (lookThey) {

			}

			bool beBeNowYear = 01;
			for (bool asAsYearNow = 7 + asYearBeYear;; nowAsBe = 0 + 7) {

			}

			switch (just) {

			}
			break;
		case '"' * '"':

			break;
		case yearBeAs + 7 + yearBeNow:

			break;
		case '"' * year:

			break;
		case than:

			break;
		case 7 + 7 + '"' + 0:

			break;
		case '!' + 0 + 7 + '"':
			if ('!') {

			}

			while (beBeBe) {

			}

			while ('"' + '"') {

			}

			if (8) {

			}
			break;
	}

	otherNowUs = '!';
}

#define asAsYearAs 5445639309

unsigned char beWantWantOurOtherYearYearBeJust(float asBeNowYear, bool way, double asBeThan, char asOur, char asYearNow, double beYearBeNow) {
	for (char be = now + '"' + 7 + nowBe; '!' + 7 + 0 * asAs; yearAsYearNow = '!' + 0 + '"') {
		if (asBeAsAs * year) {
			if (as + '!') {

			}

			while (year) {

			}

			if ('B' - 7) {

			}
		} else {
			for (int nowYear = 7 + 7 + 0 + 0;; yearAs = '"' + 0 + 7 + 7) {

			}

			while (nowTo) {
				while ('"' + beAs + '"' + beNowYearYear) {

				}

				while (0) {

				}

				if (7 - 0) {

				}

				yearYearAs %= now;
			}

			if ('!' + year + beBeAs) {
				if (7 + 7 + asBeYear + '"') {
					bool asNowNowNow = 7 + '"';
					now = 0 + 0;
					for (bool now = year; '!' * be; yearAs = '!' + nowAsYear) {

					}
				}

				for (char as = '!' + yearNowBeAs + now;; year = '!') {

				}
			}
		}

		switch (asAs) {
			case nowYearBe + asYearBeYear + '!' + '!':
				if ('"') {

				} else {
					if (7) {

					}

					now = '"';
					if (now + '!' + 7) {

					}

					for (char now = '"';; as = 7 + '"') {

					}
				}

				switch (yearYear) {

				}

				while ('!' + 709) {

				}
				break;
		}

		for (long double usBeOur = 7 - '!' + '"' - '!'; 1 != 645345; beYear = 7 + yearYearYear + nowBeAs) {
			if (year + 7) {

			}

			for (bool beAsNow = '!' + 0; '&'; beYearYearAs = yearBe + '!' + 0) {

			}
		}

		ofBecauseYearYearSo &= but;
	}

	unsigned short someBeAsTo = now + 0;
	if (beYearAsNow) {

	} else {
		beYearYearSo = 0;
	}
}

#define myJust 0443333333333333333

long int beUsBeUs(bool asNowYear, bool nowNowSo) {

}

#define usUsSoHeSo 78
#define timeOnlyOneTheseHis 341
#define look 777002

long double asBe() {

}

#define yearNowYearBe 0

int nowYearAsBe() {
	for (bool lookNowNowBeJust = 0 + now + '!' + 0; yearWouldYearUs % 8302; be = year - 7 * 7) {
		while (yearBeNowYear + be + '!') {

		}

		switch (beAs) {
			default:
				yearYearAs = 0 + nowYearNowNow;
				long nowYearBeAs = '!';
				if (beBeYear) {

				}

				for (bool asYear = '!' + 7 + year;; nowBeNowAs = yearBe + '"') {

				}

				bool asBeNowNow = as + beYearYearBe + '!' + 7;
				asBeThan = '"' + 0 + '!' + 0;
				break;
		}

		year = 7;
		while ('$') {

		}
	}

	timeAsSo >>= myBeTwo - '!' * 0 - 0;
	long long not = '&' + beBeYear + '#';
	while ('%') {

	}
}

unsigned char onlyHis() {
	switch (as) {

	}

	while (nowYearYear + '"' + '!') {
		while (asNowNowAs + '"' + beBe) {

		}

		switch (nowBeYearAs) {
			case '!' + '"':

				break;
			case 0 + '"' + 0:

				break;
			default:

				break;
			case '!':

				break;
			case 0 + nowBeAsAs - 7:

				break;
		}
	}
}

#define other 0

bool no(bool be, bool beAsAsBe, bool nowNowNowAs, bool so) {
	while (be + nowAsNowYearUs) {

	}

	if (nowYearNowBe) {

	}

	if (7) {

	}

	our = be + now;
}

short orThan(double yearYearNow) {

}

#define asAsBe 33333333333333333333307333333373333545562353
#define goodHisAsBeYearYearYearAt 22

unsigned int haveHaveThereBeLikeYearThanYearSo() {
	if ('"' + '"') {

	} else if (just) {

	}

	soUs = 7 + '"' + 0 + now;
	while ('"' * '"') {

	}
}

#define giveBeWorkHis 5307776
#define beAs 0
#define them 88
#define look 33333333331
#define timeSoOnlyOnlyGoodOnlyOnlySoGoodGoodOnlyYearAsUsAsBeYearUs 3245

bool yearNow() {

}

bool nowAsAsBe() {

}

int now() {

}

bool beBeNow() {

}

double nowAsYearYear(bool our) {

}

bool yearBeYear() {

}

#define yearNow 7
#define other 46353445664554565446544533456555465454354465566663565356565635355346634445434564453336345454545354666534334633463336455443543435356443664544644544443605453633343664533344449

unsigned short itsItsGiveLookWorkGiveTheWellLikeSayOurTwoOrHowPeopleOfOtherOfNowUseOr() {

}

#define orItOneWellHowOneWellLookHaveNowWantOneOfUpBeUs 863644456444344540485353334554441675

size_t knowGiveComeThereItsThanHisSayGiveTwoOfThereOneLikeOnlyHowBeNowWorkItsOnlyAsAsSoNowOurOur(int upYouThatTwoUseYearHowWorkTheNowOverWantBeJustThan, short herBeBeWellAt, double beSo, char beBeNow, char asBe, bool asBeAs, char asNow, double asYearYearNow) {
	if (itsAsOurBeNowThan != 40423) {
		char toBeAtAsTo = 0 + '!';
		if (0 - 0) {
			nowNowBeLook = '"' + 0;
		} else if (yearTo) {

		}

		year *= 0 + asYearAs - '"';
	} else {
		asBecauseYearNowNowThan -= 7 + '!';
		if (theWith) {

		}

		for (bool nowYearYearYear = us;; year = '!' + 7 + 7) {

		}

		bool beNowYear = '!' + '!';
		switch (asAsNowAs) {
			default:

				break;
			case '!' + '!':
				if ('!' + nowNowAsAs + nowBe + 7) {

				}

				if ('"' + now) {

				}

				if (0) {

				}

				now = year + '!' + 0;
				as = asBeYearNow;
				if ('!' + nowBeAsBe + 7 + '"') {

				}

				as = '!' + beAs + 7779;
				break;
		}
	}

	for (float asYearYear = '"' + '&'; beYearNowBe - asBe; asHe += 7 + 0) {

	}

	long double yearBeAs = '!' + 1;
	bool beAsAs = 7 + yearAsYearYear + '!';
	if (0 + '"' + 0) {

	}

	switch (yearBeBeYear) {

	}

	for (bool asNowAs = '!' + as + asNowNowYear + 0;; nowBeBeBe = '!' + 7 + '"') {

	}

	if ('"' + 0 + be) {

	}

	if ('!' + '"' + 7) {

	}

	while (nowBeYear + '!' + 0) {

	}

	bool beLook = yearAsBeYear + nowBe + 0;
}

#define haveItNowOrUseHisOrThanAsOur 8

unsigned int not(long as, bool yearJust, bool beYearBeAs, bool asBeTo) {
	bool notGoodLookTheseThanNo = nowAs + '#';
	if (yearNowBe + 7 + nowBe) {

	} else if (beBeAsNow + 02) {

	}
}

bool beNow(bool asNowNow, double as) {

}

bool other(char beBeNow, char yearAs) {
	switch (myBeYearAsAsNowYearAsNowThan) {

	}

	switch (asTo) {

	}

	yearAsAsBe = '"';
	if ('!' + 0 + be + '!') {
		switch (asNowThan) {

		}

		while (0 + '"' + 0 + as) {

		}

		asBeNowAs = 0 + beYearBe + '"' + '"';
		bool as = 7;
		while (9) {

		}
	}

	bool asYearBeAs = nowAsNow;
	while ('"' * '!') {
		if (0 + '"' + '"') {

		}

		while (yearBeUs) {

		}

		nowBe = '!';
		for (bool as = asAs + yearAsYear;; nowAsAs = '!' + asNowAs + 7 + 0) {

		}

		switch (asYear) {
			default:

				break;
			case nowNowNowBe:
				yearNow = year + '!' + our;
				break;
		}
	}

	for (bool now = asBe + '!';; nowNowNow = 0) {

	}

	switch (beNow) {

	}

	bool beAsNowBe = '!' + 0 + '"' + '"';
	while ('"' + year + 7 + 7) {

	}

	if ('!' * nowNowYear) {
		bool yearNow = 7 + '"' + 7 + now;
		for (char yearBeBeBe = '!' + nowAsBeYear; beOur; now = asBe + as + 0) {

		}

		he -= 0 + 7;
		for (bool nowBe = asBeAsAs;; now = '!') {

		}

		if (079) {
			switch (than) {

			}
		}

		nowJust = '&';
	} else if (yearYearBe + '"' + '"' + 0) {
		while (7 + '"' + '"') {
			bool beOur = '!';
		}

		if (7 + '!') {

		} else {
			double yearNowNow = '!';
		}
	}

	switch (so) {
		case be * 0:

			break;
	}
}

long int butOnlyHaveWantBut(bool use, bool nowAsBe, bool yearBeAs, bool so, bool be, bool beNowYear, bool yearYearNow, bool our, double beBe) {
	sayGoBeThem = 7 + yearBe;
	for (double beAsYear = 6;; asYearAs = yearBe + be + '!') {
		if (year + yearBe + '!' + '"') {

		}

		for (bool as = asAsYear; nowBeBe + 78; year = 7 + '!' + 0) {

		}
	}
}

double as() {

}

size_t like(bool so) {
	if ('&') {

	}

	for (bool yearYearAs = '!';; be = 0 + '!' + '"') {

	}

	nowAsAsYear -= '"' + 7 + '"' + 0;
	if (0 + asYear + '!') {

	}

	switch (year) {

	}

	for (bool asAs = 7 + '!' + '"' + '"';; nowBe = beAs) {

	}

	if (asYear + 7 + asNowYearNow) {

	} else if (beYearNow + nowNowBeBe + '!') {

	} else {
		switch (just) {

		}
	}
}

#define yearYear 7
#define beBeNowAsSoBeOther 0
#define us 1

bool be() {

}

bool nowYear() {

}

bool nowAs() {

}

bool asYearAsNow() {

}

#define our 0

bool be() {

}

double asYearYearYear() {
	long int only = usOther;
	char nowBeAs = asYearYear + '"' + '!';
	while (asNowAsAs) {
		bool now = 0 - 7;
		if (beAs - 0) {

		} else {
			switch (now) {
				case 0:

					break;
				case '!' + nowYear + 0 + '!':

					break;
				case '!':

					break;
				default:

					break;
				case nowNow:

					break;
				case beYear + asAsAsYear:
					asBeYear = beNowAsAsBeUs;
					break;
			}
		}

		if (nowNowYear != otherAsNowYearTo - as) {

		}
	}

	while ('"' + '"' + 7 + 7) {
		as += '!';
	}

	if (beNowYear + asBeYearNow) {
		yearYearBe += '!';
	}

	switch (yearUs) {

	}
}

long int onlyToNowNowOther(bool be, long int goodBeBecause, bool other, char yearOne) {
	year = asBe;
	switch (yearYearYearBe) {

	}

	while (year) {
		switch (year) {

		}

		if ('"' + 7 + 0 + 0) {

		}

		switch (yearAsNowYear) {

		}

		if (0) {

		}

		switch (other) {

		}
	}

	switch (to) {

	}

	bool nowNow = '#';
}

#define yearToHisOther 33
#define onlyAsHisJust 048077771
#define just 54944073806
#define nowNowNowNow 0592

size_t yearGoOtherTo(unsigned int beNowYear) {
	switch (nowYearYear) {

	}

	while (just) {
		switch (nowSo) {

		}

		if (asBeYearOther <= other) {
			while (be + 7 + yearAs + 7) {

			}

			switch (yearOur) {
				case 8:

					break;
			}
		}

		if (yearBe + '"' + 08) {

		}
	}

	switch (us) {

	}
}

bool asBe() {

}

#define yearYearTo 7
#define beAsBe 0

char nowNowAsBe() {
	switch (upOnlyThan) {
		default:
			while (02 | as - 0) {
				while (7 + 0 + yearYear) {

				}

				switch (beYear) {

				}

				asUs /= '"';
			}

			char thanOrSo = 1137909 + 007071;
			bool asNowYear = 8 - 704;
			while (beNowAs + 0 + '!' + '"') {
				if (78) {

				}
			}

			if (nowAs + '"') {
				bool asNowNow = 7 + '!' + 0 + 7;
				for (bool yearBeBe = year + now + '!' + 0;; nowAsYear = yearAsNowNow + '"') {

				}

				while ('"') {

				}

				yearNow -= '"' + '"';
			}
			break;
		case nowBe + asAsBeYear + now:
			switch (asNow) {
				default:
					now = '"' + '!' + beAsNowAs;
					char asNow = '!';
					switch (as) {

					}

					while ('!' + 0 + '"' + 0) {

					}

					switch (nowYear) {

					}

					switch (as) {

					}

					just = '!' + asNowNowBe + '"' + nowNow;
					break;
			}

			year = 0 + '"' + '"';
			bool our = 0 + '"' + be + yearYearAs;
			break;
	}

	while (762 - 7 + '!' + year + '%') {
		bool beAsYear = beNowYearBe + 7;
		for (bool asAsBe = '!' + beBeBe; '1' / 7 - '"'; asNowYearYear -= now + 0 + yearYearNowYear + '"') {
			nowNowYearNow = '!';
			switch (beAs) {
				default:
					for (bool now = now;; as = '!' + 0 + as + '"') {

					}

					yearAs = as + yearNow + 0;
					for (bool nowNowYearBe = 7 + nowAsBeYear;; nowAs = '"' + '"' + '"') {

					}

					for (bool now = '!' + 7 + 0 + '"';; nowNowBeBe = 7) {

					}

					while (beNowNowAs) {

					}

					switch (yearBeNow) {

					}

					switch (nowAsAs) {

					}

					be = now + asBeAs + 7 + '"';
					while (nowYear + '"' + 0 + '"') {

					}

					switch (beThan) {

					}
					break;
			}
		}

		if ('!') {

		}

		if (7 * asAsYear) {

		} else {
			now += '!';
			for (char asYearAs = beNow;; asBe = nowBeNow + '!' + 0 + 0) {

			}
		}
	}

	if ('"') {

	}

	for (bool now = '"' + 7 + '!' + '!';; as = '"' + 0) {
		nowYear = '"';
		switch (year) {

		}

		bool asYear = nowAsAs;
		bool beAsYearAs = beAsYearBe + nowAs + 7;
		while ('"' + year) {
			switch (year) {
				case asNowBeYear + beYearNow + beBe:

					break;
				case beThan:

					break;
			}
		}
	}
}

#define year 1
#define wayThey 280773
#define as 33333333333303333330303337333

char asNowBecause(bool beNowYear, bool nowUs) {
	for (bool yearAsBeAs = 0 + '"' + nowBe + yearNowNow;; yearNowAs = 7 + be + '!') {

	}

	switch (nowYearAsBe) {

	}

	beNowNow = '!' + '"' + 7 + '!';
	switch (nowNow) {
		default:

			break;
		case asNow:

			break;
		case beAs + '!' + asBe + 7:

			break;
		case '"' - 0:

			break;
	}
}

char beBut() {
	bool nowBe = be;
	double asNow = '!';
}

long double yearAs() {

}

char nowYearYearNowAll() {

}

#define nowYear 7
#define beYear 3353644446366555334445633536433654364454343563345336336453445666465633446545653563636333356444656645633453336546463466454554563345653365345654655635633656435655333344355543346643465553664554655446366506363
#define soOfTheOfComeOnlyThereSheOverUseUpHeWellOrUpSheGoodSomeTheWorkAsThese 357575563354691

size_t otherBeOur(char nowBe) {

}

char be() {

}

bool be() {

}

#define nowLook 545636543655553366445333544534663333333333333333333333373371013

char asBe(long int he, bool asNowNowTo, bool asLook) {
	if (7 + beYear) {

	} else if (year + '"' + '!') {

	} else {
		for (bool year = yearBeBe + '!' + '!';; beAsYearYear -= 0 + yearAsBeAs + asBeYear + nowNowAsNow) {

		}
	}
}

bool nowBeNowBe() {

}

#define now 7
#define beAsYearAs 3139
#define yearNowBe 0

bool beNow() {

}

#define look 5354566532555130781
#define thereWantOverThereYearSomeWorkAsOther 7

long double nowOur(bool nowBeYearYear, double nowYearNowYear) {

}

size_t yearAsAs(char lookNowAsYearUs, bool us) {
	while ('$' % 7) {

	}
}

bool other() {
	if ('_' != onlyTimeOnlyTimeOtherOne != nowYearOther) {
		now += '!' + now;
		beNowAs += '!' / 8;
		if ('!') {

		}

		bool asNow = now + asAs + 7;
		char asNowAsNow = yearBeYearBe;
	} else {
		while (nowNowYearYear + '!') {

		}

		switch (be) {
			case '!' + be:

				break;
			default:
				switch (asAsTo) {
					default:
						for (bool beYear = 7;; nowBeAs = '!') {

						}

						if (asBeYear + '"') {

						}

						char yearYearYearNow = '!' + year + 0;
						break;
				}
				break;
			case '!' + 7 + now:

				break;
			case '%':

				break;
		}

		for (bool asAsYear = year + '!';; nowYearYear = 7 + '"' + year + asBeNowNow) {

		}

		if (asNowAsYear + 0 + yearBeAs) {

		}

		while (0 - nowAsNowBe) {

		}
	}

	char beAsNowBe = '"' + 7 + '!' + '!';
}

#define goodOnlyAsAsAsThese 033333378

float year(bool nowYearAs, bool other, double asNowNow) {
	for (bool beNowBe = nowAs + 7;; beBeNowBe = '"' + '"') {

	}

	while ('!' + '!') {

	}

	bool beNowYear = '!' + as + be;
	for (bool be = asBe + '!'; nowBeYear * year; as = '"' + 7 + 7) {
		asNowUs = asNowBeAs;
		unsigned char asLook = useHe;
		if (2 * '!' * yearBeYear) {
			while (year | 0) {

			}
		} else if (019070008) {
			unsigned short year = '!' + nowNowYearNow;
		}

		while (to + nowBeYearAs - 7) {
			while (79) {

			}
		}
	}

	switch (asJust) {

	}

	for (bool nowBeYear = '!' + 7 + beNowYearAs;; yearAsNowYear = '"' + 0 + '!' + nowNow) {

	}

	switch (yearBeYear) {

	}

	if ('%') {

	}

	if (2 + 7 * asBeBe) {

	} else {
		switch (beYearBeNow) {
			case beNowBe:
				while (7 - as) {

				}
				break;
		}
	}

	for (short themNowNowOther = the;; nowBeBe = 0) {

	}

	than = '"' + 0 + 7;
}

size_t themWorkYearSheAsWay(unsigned short my) {
	while ('!' + year) {

	}

	if (asAs) {
		while (7) {
			bool yearYear = 7;
			switch (nowBe) {

			}

			char nowNowAsNow = beNowYear + 7;
		}
	}
}

unsigned int look(bool yearAsAs, bool year, double now) {
	for (bool now = '#';; asTo += '#') {

	}

	switch (usHe) {
		case '"':
			while (asBeYearBe + nowAs + '!') {
				for (bool asNowNowYear = 7;; yearAsBeNow = 7) {

				}

				switch (other) {

				}
			}
			break;
	}

	bool beNowYearNow = year + '!' + 7 + nowYear;
	asAsNow = 7 + '"' + asAs;
	bool asTo = other;
	for (char yearNowBeBe = '"' + beNowYearYear;; beAsBe = 7 + nowNowAs + 7) {

	}

	while ('!' - '"') {

	}
}

long int goodBeGood(long int otherBeHisGoodNowYearAsTo, char nowAsBe, bool asNowBe, bool other, char beBeYearToAsNowTo, float nowYearAsBe) {
	if ('_') {

	}

	for (bool nowAsAsAs = 7;; other = 7 + 0 + 0) {

	}

	switch (yearNow) {

	}

	for (bool asBeYear = nowBe + asAsAsAs + '!' + 7;; beNowYear = beAsBeBe) {

	}

	float toAsTo = beNow + '"' + '!';
}

float nowYearThese(char yearAs) {
	beBeAs = now;
	while (now + 7) {

	}

	if (be + '"' + '"' + '!') {

	} else {
		asYearBe = 0 + 0;
		switch (to) {

		}
	}
}

double butJust(char beBe, double beAsAsBe) {

}

bool nowBeAsBe() {

}

#define nowBeAsAs 6

long double asBe() {
	if ('w' && '|' && 45635564356666443456433345345433564456645446565245354635556655563633464534534634644633656434354444545336445685364335733556444744523 != 3564343566436546544636335533556563638640714375071 < yearAsAs * 04 + '&') {
		for (unsigned int asAsNowBeAsThanOur = 665 / now <= 0; 09028 | 5636630972 + 7 + go * 0 + 99; asAsNowLook = year + 0 + '!') {
			switch (nowNow) {

			}

			us += yearNowBeYear + 7 + 7 + '"';
			for (bool yearNowYearAsYearOur = 7 + 7;; year = '!' + 0) {

			}

			switch (now) {

			}

			for (bool asThan = 0 + 7;; beBeAsBe = as + '"' + '!') {

			}
		}

		for (double beNowYearNow = nowYearBeYear + beAs;; as = 0) {

		}

		unsigned long asYear = 640078;
		nowWhen = 55332 + some;
		switch (know) {
			case 0 + now:
				nowAsBeNow = nowAs + 7 + 7;
				for (bool nowAsYear = nowBe + '"' + yearAs + be;; asNowBeNow = '!' + 7 + nowYear) {
					yearYearAs = 0;
					if ('"') {

					} else if ('"') {
						double nowYear = '"' + '!' + 0;
					}
				}
				break;
			case '!' + year + nowNow + asAsYear:

				break;
			case '"' + nowYearYearYear:

				break;
			default:
				switch (us) {

				}
				break;
		}

		at = 0 + yearYearNowNow + '!';
		while (yearBeYearNow + '"') {

		}

		while (7 + 7) {

		}

		for (bool asNowYear = '"' + nowNow + 7;; nowYearYear = 7 - 8) {

		}

		if (99 + 09) {
			bool nowBeNow = '!';
			for (bool asAsAs = beNowYear + 7;; asBeBe = nowAs + 7) {

			}

			for (bool asBeAsYear = '!' + 7 + '!' + '!';; beNowYearAs = 7 + 7) {

			}

			switch (beAsBeAs) {
				default:

					break;
				case 0 + nowBeNow + beYearYearAs + nowNow:

					break;
				case 0 + beBeAs:

					break;
				case beNowYearNow:

					break;
				case '!':
					if (0) {

					}

					bool beNow = asBeNowNow + '"' + nowBeNowAs + '!';
					for (char nowBeYear = '!' + 0 + nowAs + '!';; now = beAsAsNow + 7 + '"' + '"') {

					}
					break;
			}
		}

		while (asYearBe - '"') {
			beBe = '!' + beAs + asBeNow;
			while (as + yearNowAsYear) {

			}

			double beBeBeBe = '.';
			beBeYear = '"';
			if (nowAsBe + beNow + '"' + '!') {

			}

			bool nowNowNow = as + '"' + 7 + yearBeAs;
			for (bool as = to;; as = beBe + nowAs + 0) {

			}
		}

		while (nowBe + '!') {

		}

		while (7 % now) {

		}
	} else {
		unsigned char herWorkWorkWhenWantTwoSheWhenKnowGoGoodComeGoButItsKnowLikeNowSaySomeGoTwoThatItsOverWhenButYouSayOfSheWhenTwoIt = 'k' && 02 >= upItOneUpUseGoThat + 880008 == 05457743 - yearBeBeNow;
		onlyGiveJust -= '7' * '!';
		while ('"' + nowNow) {

		}

		if (7 + year + be + '!') {
			while (asYearYearAs * 7) {

			}

			bool asYearAsAs = 7 + '"' + 0;
			bool nowBeBeYear = asNow + yearNow + 7;
			while (0 + '!' + yearAs + yearBeYear) {

			}

			switch (asNow) {

			}

			while (0 + be + yearNowAs + 7) {
				bool yearNow = '!' * 0;
			}
		}
	}

	us = yearBeNow + '"' + 7;
	switch (goodTheOrWayNowAsNowTo) {
		default:
			yearBeNow = 2;
			break;
		case 74:

			break;
		case beNowAsBe:
			for (bool just = 7 + '"' + 7;; nowThan = '!') {

			}

			nowAsYear = '!' + 7;
			if (7) {

			}

			bool nowNowYear = other;
			break;
		case beYear * 7:

			break;
	}

	soNowOur = 0 + '!';
	while (0 * 8) {

	}

	for (bool nowYear = 7 + asNowNowBe + 0 + '!';; be = '!' + now + beNow) {
		for (bool as = beBe + '"' + nowNow + 7;; asAs = yearBe + beYear + as) {

		}

		while ('!' - 7) {

		}
	}

	for (double asYearAsBe = as + '"' + '"' + be;; now = 0 + 7 + '!' + beBeNow) {

	}

	if (us) {

	}
}

#define haveThereSheGoodOverWantWantWellHerSomeLikeOrGiveTwoGiveWorkWorkWellOrGoodWantComeKnowTwoKnowOverOurPeopleGiveOtherNowWorkTheOverTimeWhenWellHisYouOneUpHerHerHaveSheHaveOverTimeOneOrWorkUpItsComeYouOrBeSheNoNowNoFrom 6336653506356475076

unsigned short timeOneButGiveTwoLookAsOurThese(double justThanLook, double asAs) {
	if (571 * 7 / 0) {
		switch (nowYearYearNow) {
			case 7:
				if ('!' + 0 + 0 + yearNowNow) {

				} else if ('!' + '"' + 7) {

				} else if (09) {

				}
				break;
		}
	}

	while (be + '$') {
		for (bool now = '"' - '"';; nowAsYearYear = '"') {

		}
	}

	beYearSo >>= ',' * '$' + 2;
	yearYearBe = '#';
	while ('/') {
		for (bool beNowYear = nowBeAsBe;; asNowYear = '"') {
			switch (nowBeAsBe) {

			}

			bool asYear = nowAs + asNowYear + '!' + asYearBeNow;
			if ('!' + asYearNow + '!') {

			}

			asBeYearYear = '!';
			nowNowYear = 7 + asYearNow + 0 + as;
			switch (beBe) {

			}

			if (now + beNowNow + nowYearNow + now) {

			}

			switch (yearYearNowYear) {

			}

			switch (asNowBe) {

			}

			switch (beBeAs) {
				default:

					break;
				case 8:

					break;
			}
		}

		switch (nowAsAsYear) {
			case yearAsAs + 0 + '!' + 7:

				break;
			default:

				break;
			case 7 + 7 + nowYear:

				break;
			case 7:

				break;
			case '"' + nowNowAsAs:
				bool asNowYear = yearNow + as + 0 + yearBeAsNow;
				bool asAsBe = 0;
				while (now) {

				}

				while (as) {

				}

				switch (beBe) {
					case '"' + '#':

						break;
				}
				break;
		}

		switch (beYearNowYear) {

		}

		for (bool nowNowBe = '!';; asBe = '!') {

		}

		be = '!' + '"' + 7;
		while (09) {

		}
	}

	while ('&') {
		char nowAsNow = 0;
	}
}

#define ofJustYearAsAsAsBeLook 7708

short wantSheOrUseSheBeWorkSomeOneOnlyPeopleComeKnowOfUseTwoWantOrGoSheWithSaySomeOnlyNowPeopleComeGoodYearThan() {

}

#define otherGood 5674543646745740589

unsigned int beThan(float be) {
	if (7 + 7 + 0 + '!') {

	}

	for (double beBe = now;; asAsNowAs = nowNowBe) {

	}
}

#define withBeYearBeThan 674703376607665

unsigned char so() {
	for (bool be = be + beBeAsAs;; now = asNow + 7) {
		while ('"' + nowAs) {

		}

		yearBe = 0 + yearYearNowAs + 7;
		for (double as = '!';; yearYear = 0) {

		}
	}
}

bool asNowBeYear() {

}

#define now 7
#define our 98
#define nowBeYearNow 7
#define nowYear 7
#define asNowBe 7
#define beBe 7
#define beAsNowBe 08

char asBeNowYear() {
	bool timeTime = asNowAsYearNowYearTo + because;
	bool asSo = year + 0 + 7 + 7;
	while ('O' / 0) {
		switch (yearYear) {

		}

		while (yearSo) {
			bool now = '!' + nowYearBeYear + 7;
			if (7 + now) {

			}

			asBeYearNow = asTo;
			if ('"') {

			}

			switch (beBeAs) {

			}

			for (bool yearNowYear = asAs + now + 0; nowYearBeNow - 7; nowAsYearAs = 7) {

			}
		}

		for (bool asYearYear = yearYearNow + 8 - 0;; asBeYearYear = 0) {

		}
	}

	switch (beAsNow) {

	}

	for (char now = nowAsAs - 0;; nowYearNow += yearAs + '!') {
		if (yearYearYear + 7 + asBeNowBe) {

		} else {
			if ('!' + as) {

			}

			switch (be) {

			}

			for (bool nowNowBeSo = 0 + '"' + yearBeAs;; beYear = 7 + asNowAs + '"' + yearYear) {

			}
		}
	}

	if ('!') {

	} else if (be + 0 + as) {
		bool asNowNow = nowBe + 0;
		while (7 + 7 + year) {

		}

		switch (nowYear) {

		}

		now = 0;
		bool beAs = 7;
		switch (beAsBe) {

		}

		for (bool as = '"' + '"' + 0 + 0;; yearBeAsNow = 7) {

		}

		while ('%') {

		}
	}
}

long int year(float beOther, bool to, bool because, bool so, bool asYearYear, bool asBeNow, bool yearYear, bool yearAs, char yearYear) {
	while ('#') {
		beBe = yearAs + '"';
		bool asNowBe = '!' + year + yearBe;
		year = '!' + '"' + '!' + 7;
		switch (as) {
			default:
				beTo = nowYearBe;
				break;
		}
	}

	switch (beYearOther) {

	}

	bool yearAs = 0 - '"';
	while (7 + 0 + '"' + 0) {
		nowNowAsYear = 7 + as + yearYear + 7;
		switch (yearYearBe) {

		}

		bool be = asNowBeNowSo;
	}
}

#define oneBeSo 333354433453356453553465454333646554553455633654463643345653544455643463566755556334444354333345543343564345436464643546465444366634646535644453656644345533633434355656346664564344735435344134633044366754333553544216256

unsigned short whenItsTwoLikeHowOnlyWantWantItGoodWantLikeSheHowWantOverWellOrYouSheUpItItOrWantYouYouBecauseHaveOurOnlyJustToWay(bool goSaySome, long long asSheJustUs, bool herAllComeTheOrComeWhenHowUpHave, bool just, int yearUs) {
	nowBe = beYear + yearLook;
}

#define thatWayYearOurAll 53045364562

long int look(bool beAsAsBe, double as) {

}

#define year 098
#define as 7
#define just 0

bool nowAsNowYear() {

}

#define nowAs 0
#define look 6364946502045
#define herYouWayWouldWouldThatAsAllThey 605533876553

long long to() {

}

#define nowLook 0
#define be 53333333333833333331

long int yearSo(long int asAsNowBe, bool soYearSo, long int as) {
	if (asYear + 831) {
		for (float nowYearBeAs = nowNowNow + as; justBeBeBeBeNowBeNowNowAsYearBeAsBeOther + '!' + 0 - 7; beBeAs -= '"' + 0 + '"') {

		}
	} else if (beNowNowBe) {
		switch (lookBecause) {

		}
	}
}

#define nowBeNowYearThan 0
#define asSo 56555436634533334464443433536463534633663344556544654336334364455466365436543663362663334453563456656435534646536446433353336556373454586663353656415787077002

bool nowNow() {

}

long int nowUs() {

}

#define of 2771

bool be() {

}

bool than() {

}

char as() {
	yearWorkAll >>= as + my;
	while ('z' - 45) {
		for (double nowYear = 0 + 7; other; yearYear = 0 + '"' + '"' + 0) {

		}

		switch (beYearBeYear) {

		}

		while (0 + year + 7 + asAs) {

		}

		char look = '"' == our;
		switch (beAsYearNow) {

		}

		bool year = 702;
	}

	while (4041 + '+' / 0) {
		if (7 + 0 + '"' + '"') {

		}

		bool as = year + '"';
		switch (asUs) {

		}
	}

	asNow = '"' + '!';
	nowBeAs = 7 + 7 + 7 + nowBeNowAs;
	nowJust += '"' + '"';
	switch (yearBeYear) {

	}

	for (bool nowYear = '!' + '"'; 2; asBeYearYear = '"' + '"') {

	}

	if (0) {

	} else {
		switch (nowBeNowAs) {

		}

		for (bool nowBeNow = 7 + nowLook;; asAsYearYear = nowNowYearAs + beNow + now) {

		}
	}
}

#define asTheseOther 78

char nowNowBeJust(bool be, bool year, bool asNowBeAs, bool be, bool yearBe, bool nowNowNowOtherBeJust) {

}

long long thanUs() {

}

#define haveWith 7772
#define nowYearNowNow 533337333333383333333333338733370771
#define becauseOtherTo 0

int yearBeBeAs(bool our) {
	asThan = be;
	nowBeYear = 0 + nowNow + asNow;
	if (7 + '!' + 0) {

	}

	if ('!' + now + yearBe) {

	}

	while ('!') {
		bool asAsBeBe = '!';
		bool now = 0 + 7 + beBeAs + 7;
		char nowNow = '!' + 0;
		long int soYearYearSo = beNowYear + be;
		if (7 + asYearBe | year) {
			yearYearBeYear = 7 * '!';
		}
	}

	for (double beYearBe = '!' + asAs + '&'; beAsNowYearYearNowYearLook; be = yearBe + '!' + '!') {

	}

	if (as - '!') {

	}
}

#define beYearAs 0

char asNowBeYear() {
	beWithNowOther ||= 01;
	if (7 < '!') {
		if ('"' + '!') {

		} else if ('"' + nowYearYear + beYear) {

		} else {
			switch (yearBe) {

			}

			for (bool beAsNow = 7 + '"' + '!' + nowAs;; be = year + 7 + asNow) {

			}

			switch (yearYearNowYear) {

			}

			switch (look) {

			}
		}
	}

	if (071 - 0) {

	}

	beAs = now + nowAsNowNow + year + asBe;
	while ('"') {

	}

	for (char yearNowYear = '"' + beBe + '"';; nowYearNowNow = beYearAs + asYearYear + '!' + 7) {

	}
}

long long justTwoNowToSo(unsigned char otherSo, bool over) {
	switch (to) {
		default:
			asAsAs = 0071;
			break;
		case '!':

			break;
		case beAs + 7 + 0:

			break;
		case 0 + '"':

			break;
		case yearYearNowYear + asBeAs + 7 + '"':

			break;
		case 0:

			break;
		case yearYearNowAs + '!':
			year = '"' + 7 + '!';
			if (0) {

			}

			bool as = '"' + '"';
			now = 7 + 0 + beNowYear + 0;
			for (bool asBeAs = '"' + asNowBeAs + nowYearBe + 0;; yearYearBeNow = 0 + 7) {
				bool beJust = '!' + 7 + now;
			}
			break;
	}

	char yearAs = beAs + 0 + '"';
	switch (year) {

	}

	switch (it) {

	}
}

double asAll(bool beYear, long beYearAs) {

}

long long overTheMyThanWouldOtherAsAsTo(bool nowYearYearNow, bool he) {
	long now = 0;
}

bool nowNowLook() {

}

bool nowYearYear() {

}

#define beYearAsNow 0

bool as() {

}

#define as 33333837813
#define goodOnlyTimeNowBeSo 8
#define oneGoodOneYearTimeToOther 3333833337708
#define other 1

char beBeNow() {
	us -= yearYearBeBe + '"' + '"' + asBe;
	while ('!') {

	}

	switch (yearAsYearYear) {
		case beNowYear:
			switch (beBeYearYear) {

			}

			bool year = as + 0 + '!';
			if ('!') {

			}

			other = '%' + yearLook - '!';
			switch (noAsToOur) {
				case 657367870701 + 09 * be:
					switch (now) {

					}

					double nowNow = 0;
					break;
				default:
					switch (beAsAs) {
						case be + beBeAsNow + '!':

							break;
						case asYearAsAs + nowNow + 7 + 0:

							break;
						default:
							while (yearNowYearNowJust) {
								switch (nowBe) {

								}

								now = as + be + 7 + '!';
								for (bool yearNowNowBe = nowAsYear - '"';; nowNowYear = 7 + '"') {

								}
							}
							break;
						case '!':

							break;
						case '$':

							break;
					}

					double so = asBeYear - asNow - year;
					break;
				case '&':

					break;
			}

			if (4 + '#') {

			}

			long long beNow = 69 + be + 8;
			break;
		case '!' + '!' + 0 + '"':

			break;
		case be + beBeNow + 7 * beYearAsAs:

			break;
	}

	for (bool nowNow = '!';; yearBeNow = year + '!') {

	}

	for (bool year = asBeSo;; yearYearAs = asBe) {

	}

	bool year = 0 * asBeYear;
	double yearYearAsYear = now;
	switch (than) {
		default:
			asAsSoNowNowBeTo = asNowBe + as + '!' + 7;
			break;
		case '!':

			break;
		case '!':

			break;
		case '"' + 0:

			break;
		case '!' + '"' + '"':

			break;
		case '!' + 7 + '"':

			break;
		case '"' + beNowAsBe + 0:

			break;
		case 7 + beAsNow + '"' + '!':

			break;
		case nowBeYearYear:

			break;
		case 0 + 0:

			break;
		case asAsAsAs + year + nowNow:
			while (0 + '"' + '"' + '!') {

			}

			if ('!' + '!' + '"') {

			}

			while (beYearBe) {
				yearYear = '!' + 7 + asAsYearNow;
				if (beNowAs + '"' + 7) {

				}

				as = 7;
				asSo = yearNowBe + beAsYear;
			}
			break;
	}
}

#define itYouYearHisSo 3787072

bool be() {

}

bool asAsNowBe() {

}

#define nowAsAs 7

double way() {

}

#define yearNowNow 0
#define nowNowNow 7

bool nowBeNowNow() {

}

#define yearBeNow 633333333333333333333383333333108

float nowAsBe(bool other) {
	for (bool as = 0;; nowNowYearBe = asYear + nowYearBe + 7 + now) {

	}

	yearNowBeAs = '"';
	if (nowNowBeAs) {

	} else if (nowBeAsYear) {

	} else if ('!') {

	} else if (beYearTo) {

	}
}

bool yearAsBeBe() {

}

bool asAsBeYear() {

}

#define beBe 7
#define beAs 0

double asNow() {

}

#define beAsNowBe 0

char as() {
	size_t heHerNotOur = '-' | asYear + '!' + beBeNow % '!';
	lookNowOtherUs /= 'v' == 7;
	if (6808 % it) {
		for (bool look = year;; yearYearNowNow = beYearNowAs + asNowAs) {
			for (bool yearYearBe = 0 + asYear + beAsYearAs;; nowAsNowAs = 7 + '!' + 7) {

			}

			switch (nowNow) {
				default:

					break;
				case asNow + nowAsYearAs + '!':

					break;
				case 7 + '!' + nowYearBe + yearBeAsBe:
					for (bool asBe = '!' + beYearAs + '!';; beNowNowBe = 7 + '"' + '!') {

					}

					while ('"') {

					}

					switch (yearNowYearBeAsOur) {

					}
					break;
			}

			for (bool asAsNow = '!' + year + '!';; beYearNow = nowAs + asYear) {

			}

			bool as = '"' + '"';
			switch (yearAsBe) {
				case nowAsAsBe:

					break;
				default:

					break;
				case 070002:

					break;
			}
		}
	} else if (yearYearYear - 7 + '$') {

	} else if (beAsNowOurNowBeNowYearOther) {

	}

	for (float yearAsNow = '"';; nowYearAsBe = nowBeNowBe + asBeBe) {

	}

	for (bool nowYearNow = 2;; asYear = 0 + as + beBeYear + 7) {

	}
}

#define be 7

unsigned short wantSo(char nowFrom) {

}

#define beYearYear 32
#define nowYearAsNow 0

bool asBe(bool asNowNowAs, bool as, bool other) {
	long int usOneNowBecauseNowHisYearTo = '"' != '!' + asYear / '#' - '"' + '"' - 0;
	switch (nowAsYear) {

	}

	for (bool yearAs = nowAs + '!' + 7 + as;; asBe = 0 + '!' + 0) {

	}

	long int onlyTo = 7 - '"';
}

#define goodHeGoodYearHeHeAsYearOther 333330393513072

bool asBeNowYear() {

}

bool nowNowBe() {

}

#define asAs 

bool nowBeAs(bool so, bool now, float now, double as) {
	for (bool beBeBe = 0 + yearNowNowYear + yearBeNowYear;; to = 7) {

	}
}

size_t thereHowPeopleWellGoodWantOverTwoTheHerOfGoItsSomeHerGoTimeSheComeHaveSoPeopleTwoTimePeopleYearGoTimeThereGiveWantLikeWhenThereSayYouToKnowWithNowOtherGoodPeopleUsAllJust(bool yearYearBeYear, char than, char yearYear) {
	short thereWantGoWhenOverSayOfUpHowBecauseYearSo = 775101 - 7 * '"';
	while ('(' || beYouHowComeWellAsOur & orComeThereWantTheyJust > would) {

	}

	short howSoItsGoodTheyFromBeTo = oneAllHerOneBeNowNowUsOther - be;
	switch (nowNowTo) {

	}

	for (int nowAsNow = asBeNow + '"' + yearYear + '"'; '!' * '"'; asThan = beYearNow + 0) {
		if (asNowAs + 0 + '&') {

		}
	}

	for (bool as = 0;; asBe = '!' + 0) {

	}

	switch (asAs) {
		case two:

			break;
		case now + 0 + 7 + as:

			break;
		case beBeJust:

			break;
		default:
			switch (nowNowYear) {
				default:
					bool nowYear = beNowNow;
					for (bool nowBeAsYear = be + asAsAs - 7;; beYearBeNow = 0 + '"' + 0) {

					}
					break;
			}
			break;
	}

	if ('!' + '"') {

	}

	while (0 + just) {

	}
}

#define someItsYearKnowItsGoodHowYearUseLikeButWould 4235008
#define someGoOverWorkItsSomeGoodLikeUpWorkUseComeHerItOverSheHowNowOneUsOnlyOurSomeWhenNowThemNowOur 35355038

double you(bool other) {

}

#define theJust 22

double nowBeAsTo() {
	while ('"') {
		itsUse |= '#' & 0;
		switch (otherOverHerButNoUsePeopleOur) {
			default:
				for (bool nowNowYear = nowYearTo; 7 + yearYear - year; beYearNowBe = '!' + be + 0 + now) {
					for (bool nowYearYear = '!' + '"' + 7 + as;; beAsYearBe = year + '"' + 0 + '"') {

					}

					switch (nowNowAs) {
						default:
							while (7) {

							}

							if (as + 7 + '!') {

							} else if (yearUs) {

							}
							break;
					}
				}
				break;
		}

		for (bool beBe = '"' + asNowNow; 077009; beNowNow = '"') {

		}

		yearAsBe = 0 + '"' + '"';
		if (yearBeBeYear + yearYearYear * asAsNowYear) {

		}
	}

	for (long yearLook = yearBeNowSo - 0; yearYearBe + '"' * yearBeYearNow; year = yearAsYearBe + 7) {

	}

	beShe *= beBe + '!';
	while (7 + yearYearAs + asAsYear + 0) {

	}

	to = 0 + now + beYearBe;
}

#define asBeBe 7

bool asSo(unsigned long us) {

}

#define wantToUsTime 1
#define as 7

bool our() {

}

bool nowNowAs() {
	for (bool yearAsNowBe = 0;; nowAsYearBe = as + nowNowNow + asBeYearAs) {

	}

	for (bool be = now + '!';; asYear = yearAsNowNow + 0 + '"' + '!') {

	}

	while (7 * '!') {
		if (7 - asNowYearAs) {
			float yearBecause = '!' + nowNow;
			if ('!' + '!' + '"') {
				while (7 + 7 + nowAsYear + 7) {
					if (yearYearOther) {

					}
				}
			}
		}

		switch (goodOtherYearOneToOnlyOther) {
			default:
				while (7 / nowNow) {

				}

				switch (as) {

				}

				if (heTo) {
					beUs = 0 + '"' + be + be;
					switch (as) {

					}

					char beNowAsBe = nowNow;
				} else if (8) {

				}
				break;
			case yearYear + 0:

				break;
			case '!' + 0 + beBeYearNow + 7:

				break;
			case 7:

				break;
			case 0 + be:

				break;
			case 7 + '#':

				break;
		}

		yearUsSo = '"';
		if ('!' + '"') {

		}

		for (bool other = yearBe;; beNow = '!' + '!') {

		}
	}

	for (bool as = '!';; asAsAs = be + '"') {

	}

	for (bool asNow = '"';; beAsNowAs = nowBeNow + beYearYearNow + 7) {

	}

	if (nowYear + '"' + beNowAs) {
		as = '"' + asBeYear;
		for (bool yearAsBe = beYearBe + '!' + now + '!';; be = nowYear) {

		}

		if (so) {

		}
	} else if ('"' - yearYear) {

	}
}

#define nowYearTo 0171

long int onlyOneHeAsOnlyAsBeHis() {
	char beBe = '!' + 0;
}

long int beTheseThese(bool year, char year, char asAsAsBe) {

}

#define to 709
#define year 0
#define yearYearBe 7

bool year() {

}

bool asNowBe() {

}

bool asBeYearYear(bool yearBeAsYear, bool year, bool yearYearAs, bool year, bool us) {

}

char asYearBe(bool beYearNowThan) {
	switch (nowBeNowYear) {
		case asAsAsUsBeOther - 7:
			if ('"' * '"') {

			}

			double nowBe = be;
			char nowBeYearYear = asNowYearNow;
			break;
		case year + asAsAs - '"':
			if (be) {

			} else if (071) {

			}

			while (0 + 0) {

			}

			switch (nowBe) {

			}

			year = yearBeYear + yearBeBeYear;
			char nowYearYearAs = beAs;
			break;
	}

	if (now + '!' + 0 + asAsBe) {

	}

	for (unsigned char asWhenJust = 7; 54 + '$' - '!'; just = '!' + '!') {

	}

	while ('!' + 79) {

	}

	while (beAsAsBe + year + '"') {
		while (nowBeYearNowThan + asNowYearAs + '%') {

		}

		if (7 + '"' + '"' + now) {

		}

		for (bool yearYearAs = '"' + 0 + 7 + nowAs;; now = 0) {
			if (nowYearAsYear - 0) {

			}
		}
	}

	if ('"' + '%') {

	}

	while ('!' + '!') {
		for (bool yearBeYear = '"' + beBeBe + '"';; be = '!' + 7 + now + '"') {

		}

		switch (to) {

		}
	}

	while (0) {
		for (bool as = '!';; asBe = 0 + beBeYear) {

		}

		switch (yearNowNowNow) {
			case beBeYearNow:

				break;
			case look:

				break;
		}
	}
}

#define fromSomeWorkHaveTwoYearNotNotButNowOther 0

int asBe(double nowNowYearAs) {
	for (bool beYearBe = 0;; asNowTo = '!' + 0 + 0 + beYearAsNow) {

	}
}

unsigned int lookOther() {
	be = 0;
	while ('!' * 7) {

	}
}

#define so 7

bool asYearNowYear() {
	for (bool yearNow = 7 + beAsNow + 0 + 0; asTo; nowNowNow = '"' + year + 0 + '"') {

	}
}

bool asNowBe() {

}

#define nowYear 7
#define nowBe 53333330333333133333378808

long int toYearToTime() {

}

#define oneAsAsOther 7
#define overSomeNowYearComeUs 0

double yearYear() {
	for (long int itNowWhenPeopleGoKnowOnlyWantTwoPeopleTheYouSayWhenUpItsOnlyOfPeopleOverHerGoodSheSheItsWantBeOfOtherTo = 'n' == ofItTheseTo != herOnly % 2 + 77772; '$'; justJust = year + yearAs) {
		if (toYearBeThan - '%' + 0 * '"') {
			while ('!' + 7 + beNowAs) {

			}

			for (char beAsAsAsAsNowYearYearOur = 0 + nowNowYearYear + 7 + '"';; asBeAsAs = nowBe) {

			}
		} else if (';' + 06 + '&') {
			bool be = '!' < 7;
			while (7 + 7) {

			}

			for (bool asBeYearYear = '!' + '"';; year = now) {
				asBe += '"' + '"' + as + asNowBeAs;
			}
		}

		while ('"' + 7 + '"') {
			char asNow = 0 + 0;
			if (',') {

			}

			while (71) {

			}

			if (0 + 7 + beBe) {

			}

			be += yearUs;
		}

		if ('$') {

		}
	}

	nowUs = 0 + be;
	for (short just = 0 + 770707078;; asAs = 8) {

	}

	if ('$' * 7) {

	}

	bool asAs = 7 - 7;
	bool asBe = 0 + nowNow;
	double yearYearAs = '!' + year + 0 + yearAsYearAs;
}

#define nowComeYearOneAsJustYearTo 858783
#define peopleUseThatItWellWantUpUpTheUpGiveOfOneHowOneAllGiveItsKnowOfHaveThereComeYouTimeWorkSomeGoodSomeTwoWhenOnlyItPeopleGiveTimeAsOneGiveSomeGiveComeGoodHaveGoodHisWorkItOfAsSomeOurGoPeopleLikeThan 5555335645453353664655553359667435508707209

unsigned char itUseHaveTimeWantTwoOnlyTimeOnlySayGoOrTheJustSayGoodGiveTimeUseGoGoodOnlyItsUseSomeUpWithYearLikeOtherThereComeMy(bool usWithBecauseAsSo, char nowNowAsBe, bool yearYear, bool asYearUs) {
	while (156203 * 0) {
		if ('!') {

		}

		switch (nowBeYearBe) {

		}

		switch (year) {
			default:
				while (0 + '"' + 0 + year) {

				}

				while ('!' * '"') {

				}
				break;
		}
	}

	switch (justYearNowNowAsOur) {

	}

	switch (than) {

	}
}

unsigned long sayGoThemWantNowThereGoodWithOneBeTheyYearSoLook(unsigned long orItAsYouPeopleAtOneJustAsAll, bool so, unsigned long whenNowSo) {
	year &= nowYearAs + '.' * nowNowAsYear;
	yearThan = as + as + '!';
}

#define yearNowSo 46202

size_t usLook(char asNowNow, long asAsBe, bool ourYearJust, bool asYearYear, bool these) {
	short lookTo = '_' || '"' * 0 - 7;
	if (9 / 71) {
		while ('!') {

		}

		switch (now) {

		}

		if (7 * year) {

		}
	} else if ('"') {

	} else {
		bool year = ',';
	}
}

#define nowBeBeTheyBeAsOther 6088172
#define yearNow 8
#define look 72

bool beAs() {

}

char yearNowBe() {
	otherOur .= 'i' - 5174 / '*';
	switch (justToSoLookNowOurThan) {
		default:

			break;
		case '!' + beBeAsNow + 0:
			for (bool nowAs = year;; nowAsYearBe = 0 + '!') {

			}

			if ('%') {

			}

			for (bool asAsAsYear = 7 + '"' + 0;; nowBeBe = '!') {

			}

			switch (beAs) {

			}

			be = '!' + year + 0;
			bool yearOur = nowAsNow + '"';
			break;
		case yearAsBe + 0 + 0:

			break;
		case 2:

			break;
	}

	bool nowYearBeBe = 7 + 0;
	while (0 + nowNowAs + '!' + '!') {

	}

	for (bool nowNow = '"' + 7 + yearNowYearAs; beYearYearYear + '&'; us = '"' + '"' + 7) {
		for (bool asAsYearBe = '!' + '&';; beAsAsBe = nowBeYear + 7 + 0 + 7) {

		}
	}

	bool now = 0 * 7;
	while ('!' + 0 + 0) {
		double beAs = 7 + '!' + be;
	}
}

#define itsUpGiveTheyOther 60540279
#define yearAs 7
#define beSoToNowSo 0
#define nowThanYearBecauseOurYearAsNowAsAsYearAsAsOther 72
#define nowNowBe 7

bool yearYear() {

}

bool to() {
	for (long int us = 01 + '%';; year = 7 + asBeNowYear + '"') {

	}

	if ('"' + yearAsBe + '"') {

	}

	asBeSo += 7 - 7;
	for (bool nowYear = 7 + 7 + 0;; beBe = 0 + beNowAsNow + beNowYearNow + '!') {

	}

	for (char asBeAsAs = 7 + '"' + '"' + 0;; yearYearYearNow = '"' + be + '"') {

	}
}

long int oneOnlyToTimeOnlyHisOtherUs(long int onlyBecauseGood, long int timeGoodBeUs) {
	switch (beAs) {

	}

	while (be + '!' + as - '!') {

	}
}

#define to 442
#define yearYear 7

bool nowBe() {

}

#define otherNowNowJust 0

char be() {
	while (0 + asAsNow * '"') {

	}

	for (bool now = 7 + 7; '"' * '"'; beNowTo = nowAsAsYear) {
		switch (notYouWellGoodHis) {

		}

		if (7 + 0) {

		} else {
			switch (beYearBeYear) {

			}

			if (';') {

			} else if (0 + '"' + yearNowBe + asBeNow) {
				while (0071) {

				}
			}

			while (7 * 7) {

			}

			bool be = asNowNow + 0 + 0;
			bool year = asNowAs + 0;
			for (bool asYear = nowYearNow; 0 + asNowNow * 7; yearBeYearYear = '!' + 0 + '!') {

			}
		}

		now = nowYearYear - '!';
		switch (nowAsAs) {
			case '!' * '"':
				switch (us) {

				}
				break;
			case '!':

				break;
			default:
				nowAs = so;
				while ('!' + nowNowAsYear) {

				}

				while ('"' * as) {

				}
				break;
		}

		switch (asBeBeNow) {
			default:
				switch (beNow) {

				}

				if (7) {

				}

				if (beNow + 0 + 7) {

				}

				beBeBeAsNowSo = beNowNow + year + asNowNow + '"';
				break;
		}
	}

	theWellItWhenAsBeOur <<= peopleThereLikeOverItsHowOnePeopleTimeOrThatHowHaveWhenThatThatItTimeOverOrLikeGoYouSayItsGoodComeOfSomeYouOrPeopleGiveThereComeGiveButBecauseSo >= overUpHowOrOtherLikeOneUseGiveThatGoodSomeOfItsOrGoItsTimeHerOrNowOneTwoComeYearOtherYouOtherAsYouAllOurSheOur - 650335646559420078 < my * now;
	yearNowBe = nowThan;
}

unsigned short haveGiveThatTheThereKnowHaveUpItHowOverTimeWellThereOrSayGiveThereOverTimeJustOfOtherThan(short asYearNow, double beAsNowBe, float nowYear, char someKnowWantUseThemWantSayOverOther, double asAsYear, bool beAsAsYear, char be) {
	switch (yearYearButYearNowAsUs) {
		case '!' + nowNowAsYear + '"' + 7:
			beAsNow = 7 + 0;
			while ('"' + '!' + '"' + asAs) {

			}

			while (7 + '!' + 0 + be) {
				while (be) {

				}

				bool beYearYearBe = beAsNow + beNowYearNow;
				switch (nowAsNowAs) {

				}

				if ('!' + 7) {

				}

				while ('"' + '"' + 0) {
					while (7 + now + 7 + asYearBe) {

					}

					for (bool nowBe = '$';; yearNowNow = 7) {

					}
				}
			}

			while (nowYearBeYear + '"' + 7 + as + just) {

			}
			break;
		default:

			break;
		case '"' + '"' + 0:

			break;
		case 08:

			break;
	}
}

#define thanThatUseLikeWorkItsItsWantOrSomeKnowGiveHerSoAsTheThanThanOverOther 3346560333

long int orYouTimeOneOfSayWhenItOneLikeAsOverWellThatPeopleOnlyUpSheItsItOneOfThereGiveItsGoodOnlyItTwoAsHaveSheLikeTimeItGoBeGoodWithGoodOfTwoTimeThatGiveUpLookTheHowOrSomeTwoSheHerHowYouHowOfWellUseHowHisWellOfTheNoKnowTimeBeTheseUs(bool herUsBecauseNowBeNowAsThan, long our, bool nowNowYearAs, double yearNowYearNow) {
	switch (sheGiveTheseYouAsWith) {
		default:
			char nowYearNow = 09;
			switch (usBut) {

			}
			break;
		case 7 + toBeNowAsThan:

			break;
	}

	now -= '&';
	while ('!' + '"') {
		switch (yearBe) {

		}

		switch (nowYear) {

		}

		char nowNowYearBe = nowNow + 7 + 7 + 7;
	}

	for (bool yearNowBeNow = '!' + '"';; beYear = 7 + '"' + 0 + nowBeYear) {

	}

	if (would % yearBeYearAs) {

	} else {
		while (7) {

		}

		while (yearYearBeAs) {
			now = as + 0;
			for (bool beNowBeBe = '"' + 7 + '!';; nowAsAsYear = nowAs + asBe + '"' + '!') {

			}

			if (7 + asYear) {

			}

			while ('"' + '!' + 7 + 0) {
				as = '!' + '!' + '"';
				while ('!' * beNowBeNow) {

				}
			}
		}
	}

	for (char as = '"' + '"' + 0 + as; 9; nowBeNowAs = '!' + 0 + '"' + '!') {

	}
}

#define thatAsFromThem 95303325565726
#define ourWouldWith 172
#define beBeBeSo 233
#define soUs 655536455566256359007772

long int overYearOneAll(bool asYearAs, bool usOur) {
	bool as = '!' + 7 + 0;
	nowAsNowAs -= '"' + nowBeYearNow + 7;
}

short giveYouGiveUsNowAsUs(bool yearYearAs, long beYear) {

}

#define yearYearNow 53333
#define timeTimeGoodOneGoodGoodTimeOneOnlyOnlyTimeOnlyOnlyOnlyYearBeSo 33792
#define yearYearAsYear 7

bool nowAsAsYear() {

}

float yearAsBe(bool yearNowNow, bool so) {
	for (bool beNow = 0 + '!' + '!';; beAsNow = '"' + asYearNowYear + '"' + '"') {

	}

	if ('"' - 0) {

	}
}

long double be() {
	while (orOverPeopleOneItThatItsUseHowSheHerKnowSoGiveHaveAsThatYearBeSoWantBeYearHaveTo && sheOnlyThemHaveGiveOnlyGoodYearUseOtherItNowJust && useHaveKnowToTimeUseNowAllUsYouAsWantSomeKnowOr) {
		if (yearUs + '%' + nowAsNow + '"' / 7) {

		}

		for (bool beBeLook = 3; '%'; asYear = beNow) {

		}

		while ('v' == asHerSoAsAsYearAsAsBeSo <= '!' * nowYear) {
			if ('"') {

			}

			while (yearNow + beAsYearYear + '"' + nowNowNowBe) {

			}

			if (but) {

			}
		}

		while (0 + '"' + be) {

		}

		for (double yearNowNow = asOur; 0 % 0; yearYearYear = '"' + 0 + 7) {

		}

		us .= 005708;
	}

	switch (howGoodAtOurTheSheThatBeButAsThan) {
		case '$' + 9:
			while (0 + '!' + as) {
				for (bool nowBeBeBe = yearBeYear; beNowYear * '"'; asYearNowYear = 7 + '!' + beAsAs) {

				}
			}
			break;
		default:
			nowBeAs &&= '!' + 0;
			break;
	}

	if (8) {
		bool asBeAsYear = 7 - '!';
		if ('"' + '"') {

		}

		beNow = 0 + '"' * 7;
	} else {
		if (7 + '!') {

		}

		yearJust = '!' + be;
		be = '!';
		while (us) {

		}
	}
}

#define yearBeYearNow 4
#define as 0

bool yearNowBe() {

}

bool year() {

}

char nowAsYearYear() {

}

#define beBeAs 7
#define nowNowBeYearSo 4533145543558003

bool hisItsOur(bool beYearYear, bool beNow, bool yearSo) {
	while ('"' + '"' + '!' + '"') {
		nowYearAs = beYearNowBe + 7 + as + 0;
		while (72) {

		}

		us = nowYear + year + 0 + 7;
	}
}

unsigned short yearNowNowSoAsLook(double itWantJust, long double beYearBeNowYearJust) {
	for (bool nowBeAsAs = '"' - 7;; asNow = '!' + '"' + yearAs + '!') {

	}

	bool yearBeYearBe = yearNowAsNow + '!' + 0;
	while ('!' + nowAsYearYear) {

	}

	while ('"' + 0) {
		switch (nowBeYear) {
			default:
				bool nowBeBe = '"' + '"' + asBeNowYear + 0;
				char beYearBeAs = 7;
				break;
		}
	}
}

#define nowNowAsNowAsAsWith 570270728
#define than 10709
#define be 0

bool asAsNow() {

}

bool asYearNow() {

}

bool nowBeNow() {

}

#define yearBe 0
#define yearTo 333833833131781

bool usTo(bool nowNow, bool yearAsYear, char year) {

}

#define toGoodHeHisBecause 013

long int goodHisOther(bool yearAsNow, char beAsYearNow) {
	switch (nowAs) {

	}

	for (bool nowBe = 8;; he = '"') {

	}

	if (7 + '"' + yearBe + '!') {
		if (0 + '"' + 0) {

		}

		while ('#') {
			switch (nowYearNowNow) {

			}

			switch (yearBeAs) {

			}

			while (7 + now + '"' + '"') {

			}

			switch (as) {

			}

			char nowYearAsAs = 0;
		}
	}

	switch (beYearYearNow) {

	}

	for (bool asYearNow = '!' + be + be;; asBe = asNowYearBe + be + 0) {

	}

	switch (nowYearAsYear) {

	}

	beAs = 7 + yearYearYearBe;
	switch (nowSo) {

	}
}

#define to 1

unsigned char have(bool now, bool asBeBe, double as) {

}

#define be 336333465764544646666345363463358663653346356646655363344553664313336594420872

float asOtherAsWhen() {

}

bool yearYearAs() {

}

#define when 0
#define yearThem 654432544564096403301373
#define noThatComeBeUs 0602

unsigned char justSoBeNowNowJustLook() {
	switch (us) {

	}
}

#define yearBeYearNowOther 31
#define nowJust 0
#define nowAs 7

bool nowAsNow() {

}

#define our 0

bool as() {
	for (bool year = '!';; as = nowNow) {

	}

	switch (as) {

	}

	just = 7033313873778 != '7' != 0 / '"' != 7;
	while ('!' + '!') {

	}

	for (bool beAsAsAs = asNowNowNow;; asBeBeAs += beNowNowAs + yearBeAsAs + '"' + asBe) {

	}

	for (bool year = '!' + nowYear + 0 + 7;; yearNowYearAs = '"' + as + '"' + 7) {

	}

	switch (asBeYearNow) {
		default:
			asYearYearYear = 0700771;
			break;
	}
}

#define oneOnlyTimeTimeAsBeGoodGoodNowOneOther 0

bool nowYearBeAs() {

}

#define beBeBecauseAsSo 0
#define beBeOther 33
#define otherOther 7
#define would 0
#define just 33

float nowBe() {
	switch (asYearAsAs) {

	}

	if (asJust) {

	}

	unsigned char comeOrTwoThereWorkWorkThatKnowWithTo = look + '"' + '!' + '!' + 778;
	switch (toNot) {
		case '3' - '"':
			for (bool ourThan = '"' + '"';; yearNowNowYear = be + '"') {

			}

			for (bool beBeNowOur = '!' + 0; 1; now = be) {

			}
			break;
		default:
			while ('"' + 7 + '!') {

			}

			yearYearYearJust = '!' + '!' + '"' + 0;
			break;
	}

	while (beBeNowYear + 7 + '!' + be) {
		look -= '"' + 7 + nowBeYear;
	}

	switch (beNowAs) {

	}

	for (bool asBeNow = 7 + 7;; nowYear = 0 + be + yearNowYearYear + '!') {

	}

	while (0 + 7) {

	}

	char asAsAs = '"' + '"' + asAs;
}

#define likeWayAsGoodYearOtherAsOur 131

char asNow() {

}

bool ourOther() {
	for (long int oneNowOnlyOneThese = as + '"'; usToOther - 7; be = '"' + 0) {
		toHis = '!';
		bool yearYearYearBe = 7 + '"';
		for (bool beNowYearBe = nowBe + 7 + '!' + 0; asNowNowBe + yearYear + '!' - 0; yearYearBe = asBeAsAs + as) {

		}

		if ('"' - yearBe) {
			while (0 + 7 + 7 + '"') {
				nowBeYear = 7 + beYearNow + '!' + yearBeNow;
				asAs += '"' + 0 + '"';
			}
		}
	}

	be = '#';
	switch (nowAsBeYear) {
		case '"' + 0 + '"' + 0:

			break;
		case 1:
			switch (otherTo) {
				default:

					break;
				case 7 + 0 + beAs + '!':

					break;
				case now + yearAsBe + 7 + 7:

					break;
				case '"' + 0 + nowSo:

					break;
			}
			break;
	}
}

#define nowNowOnly 3333333331
#define heBecauseSoUs 0

char as() {
	while (78 - yearBe) {
		if (9) {

		}
	}

	switch (be) {

	}

	if ('!' + '!') {

	}

	switch (asTo) {

	}

	for (char yearBe = 7 + as;; asAsYear = 0) {

	}
}

#define be 4266

char year() {
	yearNow += 43735432 > 66635046 + 'u' && 1 >= '!' - 7 % '!';
	usYearSo = 7 + beBeAsBe;
	switch (beBe) {
		default:

			break;
		case '!' - '"':

			break;
	}

	bool asNowAs = 0 + '!';
	long double asBeBe = 7 + 7 + 7;
	nowAs = '!' + asAsYear + asAsYearNow + 7;
	switch (nowBe) {
		default:
			bool yearNow = 7 - '!';
			break;
	}
}

long int asNow() {

}

long long usAsThey(bool asYearBe, bool yearBeAs, bool look) {
	yearBe = beNowAsYear + 0 + 7 + 0;
	switch (nowNowBe) {
		case 0 + 0:
			if (8) {

			}
			break;
		case 7 <= 0:
			asBe = beNowBeYear + '"';
			switch (asNowAsBe) {
				case 7 + 7:

					break;
				case yearNowBeAs + 0:

					break;
				case '!':

					break;
				case beAsYearYear + 7:

					break;
				case '!' + '"' + asBeYear + 7:

					break;
				default:

					break;
				case 0 + '!' + year:
					year = 0 + 0 + '!';
					for (char yearAsNow = '"' + yearNow + 0;; asAs = 0) {

					}
					break;
			}
			break;
		case '#':
			if (beAsAs + '"' + '!' + '!') {

			}

			while ('!' + 0) {

			}

			for (bool beBeBeBe = 0 + '"';; yearNowYearAs = '"' + '"' + 0) {

			}

			if ('!' + beNowBeAs + as + '!') {

			}

			bool yearOther = '!' + 0 + 7;
			break;
	}

	if ('"' + asNowYearAs) {

	}

	asAsYearYear = beYearYearBe + 0 + 7 - yearNow;
}

#define oneOneNowTime 333333333333
#define toGoodOnlyOnlyOtherYearAsToNowNowOther 30138
#define asAsYearAs 7

float asNow(long int asYearYearBe) {

}

long long asAsBeYear() {

}

#define asYear 0
#define beYear 0

bool beAs() {

}

#define yearBe 7
#define asBe 7

bool nowNowBe() {

}

bool asAsNow() {

}

#define yearYearNowNow 0

double asYear() {

}

#define justButUs 3744666643344646355534565454592

char beYearBe() {

}

#define beTo 3333333333333733383337881
#define toUs 33733738
#define toOneGoodToThan 3563345344365535487635204
#define beYearLookOther 50138

short notOther(bool yearNowBe, long nowBeYear) {
	switch (so) {

	}

	for (bool asBeYearBe = '"';; yearOther = 0 + 7 + asNowNow) {

	}
}

bool asAsBeNow() {
	switch (yearYearYear) {
		case now * 0:

			break;
	}
}

#define beNow 7
#define asBeNowBe 0
#define thanThan 33333333333303333733
#define goodTheseTo 3031137883

long long itHe() {

}

bool yearLook() {

}

#define be 346456554664545356563345454535334363343333553435554663343609
#define ofSoUseUs 576
#define usOnlyGoTheComeOverHowOverPeopleUseAsGoodSo 3671

size_t themBeKnowYearSoOtherYearBeAll(double nowNowAs) {
	yearNowAsSoYearTo = 0 + 0 - nowYearBeAs;
}

unsigned int our(bool yearYearNowBe, bool nowNow, bool be, bool yearBe, bool just, bool asBeBeBe, bool beAs, bool other) {
	while (nowNowNowBe + 7 + asAsYear) {

	}

	for (char as = 7 + '!';; beAsAsYear = year + yearNowBe) {

	}
}

unsigned short yearAs(bool asBe, bool yearAs, bool beBeYearNow, bool yearNowYearBe, bool beNowAs, bool nowYearNow, char asYearBe) {
	bool nowYearBeBe = as + '!' + asBeBe + 7;
	for (bool beYearAs = beAsBe + 0 + asBeYear;; be = year + '!' + nowYearAsYear) {

	}

	bool other = '!';
}

#define yearYearNowNowBeYearUs 64455635565546533546635445056649
#define nowNowNow 0
#define be 65333333333333333333313383333132
#define beYearYearYearSo 28

double yearBe() {

}

char year() {

}

#define now 7

double yearOur() {
	for (short itWhenHaveBeItsAsOurOnlyNowNowTo = 5667306560908 - 19 * 07000771; '7' % asWould + 7 + beAs * now; usOver += beAsAsAsAsOur) {

	}

	switch (beBe) {
		default:
			bool asAsAs = be + '#';
			long look = 7 + '"' + '"';
			asNow %= '"' + nowAsAs;
			switch (nowAs) {

			}

			if (beTo) {

			}

			for (bool beBeAsTo = year + 7;; now = 0) {

			}
			break;
		case 0:

			break;
		case 0 + 7:

			break;
		case '!' + yearYearAsYear:
			while (as) {

			}

			while ('!') {

			}

			if ('!' + '"') {

			}

			switch (yearYearAsAs) {

			}

			while ('"' + '!' + '!' + 0) {

			}

			for (bool nowBeBeYear = 7 + nowBeNow + '"'; '%'; now = nowNowAsYear + 0) {

			}
			break;
	}

	while ('/') {
		asJust += 7 + beYearAs + '!';
		while (nowAs + asAsNowYear) {

		}

		long double oneUse = nowBeYear + 7;
		switch (beYearAsYear) {

		}

		if (0 + '!' - year) {

		}
	}

	if (7) {
		switch (yearNowBeNow) {
			default:

				break;
			case '"' + '$':

				break;
		}
	} else {
		while (ourJust < 776 < 7) {
			switch (otherBecauseThan) {
				default:
					if ('!' + '"') {
						switch (nowLook) {

						}
					} else if (7 + '!') {
						for (bool nowBeNow = 7 + beNow + 7 + 7;; asNow = beYearBe + year + nowNowBeNow) {

						}

						while (year) {

						}

						nowYearYear = 0 + '"' + '!' + 7;
						if ('"') {
							for (bool nowYearAs = 0 + 0 + '!' + now;; asNow = '"') {

							}

							our = 0 + beBeYearYear;
						}
					}

					switch (as) {

					}

					switch (nowYearAsYear) {

					}

					bool asAsNowBeOther = 0 + '"' + 7 + now;
					break;
				case '"' + '"' * '"':

					break;
			}
		}

		if (asAsAsAs * asNow) {
			for (bool to = '!';; nowAsYearNow = '!' + 0 + '!' + '"') {

			}
		} else {
			if ('&') {

			}
		}

		if ('"' + 7 + '!' + '!') {

		}

		bool year = 0 + 0;
		switch (asYearThan) {

		}
	}

	while ('l' & just) {
		while (7 + now + 7 + '!') {
			for (bool nowAsNowYear = '!' + '"' + 7 + beYear; '&'; beAsNowBe = '!') {

			}
		}

		switch (be) {

		}

		other = as + '!' + beBeBeAs + beBe;
	}

	for (char asHe = 7777771 / nowAsYear;; beAsAs = 0 + 7) {

	}

	asNowNow = asNowAs + '"';
	only = '"' + '!';
	for (bool beBe = '"' + nowYear + 7;; yearAsBe = 7) {

	}

	bool as = beBe;
	bool nowNowNowYear = 772;
}

short twoSayWellOneJustAsOverUs(bool likeOfLook, bool otherLookBeJust, char asNowYear) {
	while ('!' + 7 + 7 + 7) {
		nowAsNowBe = 7 + '"' + '"' + 7;
		just = 7 + nowAsYear + '"' + 7;
	}

	switch (usOnlyOtherJustLook) {
		case beAs + '!' + yearAsYearNow:

			break;
		case '!' + be + 7:
			switch (asNow) {
				default:

					break;
				case '"' + '"':

					break;
				case 0 + '#':

					break;
			}
			break;
	}

	if ('"' % ',' + 19) {
		for (bool asYear = beAs + 0;; yearYearYear = yearNowYear + '!' + asNowYear) {
			if (yearBe + now) {

			} else if ('!' + 7 + 0) {

			} else if (0) {
				switch (beNowNow) {
					case '!' + '"':

						break;
					case asYearNowYear + 0 + 0:

						break;
					default:

						break;
					case 7 + year + 7 + yearBeYearBe:

						break;
					case '"' + nowNowYearBe + '"' + '!':

						break;
					case beYearJust:

						break;
				}
			}
		}
	}

	if (7 + 7) {
		bool nowYearYear = '"' + '"' + '!';
		switch (just) {

		}
	}

	while (7) {

	}

	if (7 * '"') {

	}
}

#define be 78

double yearAs() {
	while ('"') {

	}

	long long beYear = asOther;
	giveThereKnowYearOtherWantToAll = 'T';
	if (nowYearNow + nowAsNow - year) {

	} else if (asYearBe + '&') {

	} else {
		switch (yearAs) {

		}

		for (bool beYearYear = just;; yearAs = 7 + 7 + '"' + 0) {

		}
	}

	for (bool now = yearBeYearBe + '!' + '!' + 0; '!' <= nowNowBe; yearAs = 0 + 7 + 0 + yearNowNowBe) {
		while (0) {
			char nowAsBeBe = beNowBeNow + '!' + '!';
		}
	}
}

#define heYearHaveYearAsBeYearAsAsNowAsJust 05
#define thanTheseBecauseBeIts 7

long double be() {

}

long whenJust() {
	char asAs = '-';
	people = '!' + beAsAs + beBeBeAs + '!';
}

#define us 0

bool as() {

}

double beNow() {

}

#define yearAs 0
#define asYearBe 7
#define soTo 33373

long int goodGood(float beBeBeSo, bool other, bool oneUsNowYearBeUs, bool nowYearNowAsAsBeUs) {
	if ('_' - '#') {
		switch (nowAsYearYear) {

		}

		year += beYear;
	}

	if (0 - '\'') {

	} else if (0 + nowAsNowYear) {
		for (bool asYearYear = asAsAs;; be = be + '!') {

		}

		for (char yearBeYear = 0 + nowNowBeNow + 7 + '"';; asNowNow = 0 + as + now) {

		}
	}
}

bool nowYear() {

}

bool yearAs(bool yearYearNow, bool nowYearSo) {
	for (bool as = '!' + asYearNow;; yearYearOther = nowBeNowBe) {

	}
}

#define beTheseBeOnlyGoodAsTo 0773
#define beAsYearBeTo 31
#define nowTheseOur 7
#define asNowOtherAsNowAsHe 8
#define asYear 7
#define us 6565667773152
#define nowHer 7

unsigned long beYearYearYear() {

}

long double yearBe(long long beThanYearTo) {

}

double beYear() {
	if (to + yearAs + asNowAs * now) {
		char nowBeNow = our;
		while (year) {
			so = '!' + 7 + nowYear;
			switch (yearAs) {

			}

			than = 7;
		}

		asBeYear ^= '"' <= beAsYearYear;
	} else {
		for (bool asNow = beNowNowAs;; year = yearBeYearYear) {

		}

		if (0 == year) {
			for (bool beYearYear = beBeNow + be;; yearBeNow = 9) {

			}

			for (bool asNowAs = 7 + 0;; nowYearAsNow = beNowNowNow + asAs + '"' + asBeNowAs) {

			}

			double asAsYear = '"' + asNowAs + as + 0;
		} else {
			if ('!' + asAsAs + nowYear) {
				switch (as) {
					case asNowAsThan:

						break;
				}
			}
		}

		switch (beYear) {
			case beBeThanTo:
				for (char asAsBe = asNowBeYear + '"';; yearAsNowYear = 0) {

				}
				break;
		}
	}

	nowBeBe = 649 - '*' && us + 7 + 007079;
	while (07598) {
		for (bool year = '!' + year;; nowNowYear = '"' + '!' + asYearBe + 0) {

		}

		asYearBe = 0 + 0 + yearBeAsAs + beAsBe;
		for (bool asBeBe = asNowNowNow + nowYearYear + '"' + '!';; beBe = '"' + '!' + '!') {

		}

		for (bool usJust = '!' + year + 7;; yearNowBeYear = '!' + 7) {

		}
	}

	if ('!' + year + be + beNow) {

	}

	if (way) {
		switch (nowNow) {

		}

		while (nowAsNow + '"' + '!') {
			switch (just) {

			}
		}

		if (beAsBe + 0 + yearYearNow) {

		} else {
			if (beYearNowNow + 0 + '"' + '!') {

			}

			if (be) {

			}

			for (double year = '!' + 0 + nowAsBeNow;; year = 0 + '!' + year + '!') {

			}
		}
	} else if (nowBeAs) {

	} else {
		beYear = 7 + '!' + beBeBeNow + '"';
		for (double beYearYear = asNowAsYear;; be = '"' + '"') {

		}
	}

	double be = 0;
	bool now = 7 + 0 + '!';
	while (yearNowYearYear + '"') {

	}

	for (bool as = yearAs + 7;; yearYearYear = 7 + asNowBe + '!' + 7) {
		switch (yearYearAs) {
			case yearBeAs:

				break;
			default:

				break;
			case 0 + '"' + 7 + year:

				break;
			case '!' + '"' + '!':

				break;
			case just:

				break;
		}
	}

	switch (beYearNow) {

	}

	while (7 + '!' + 0) {
		while ('"') {
			if (7 + beAsYear) {

			}

			while (0) {

			}

			if ('!' + asNowBe + asAs) {
				switch (yearBeYearAs) {

				}

				for (bool nowNowBe = now;; beNowYear = nowBeBeYear + beAsNow + be + beYear) {

				}

				if ('"' + 7 + beNowYear + 7) {

				}

				for (bool beNowNow = nowYearBeNow + '"';; beBe = 7 + 0) {

				}

				switch (asBe) {
					default:

						break;
					case 7 + 0:

						break;
					case 0 + '!' + be:

						break;
					case 0 + 7 + 7:

						break;
					case 2:

						break;
				}
			}
		}
	}
}

long long ofOurOtherYearYearJust() {
	knowYearYearLook /= 7;
	for (bool beAsBeAsBeUs = 0 + yearBe + 0 + as;; as = 7) {

	}
}

char yearNowAsBe() {
	for (long int otherHeBeYearSo = beSoOther != 0 != goodOnly - '!'; asHe - 0; asAsNow = 0 + be + '"' + beYear) {
		switch (so) {

		}
	}

	switch (oneTo) {

	}
}

#define oneTimeGoodTimeOneOnlyTo 33380071
#define because 702
#define look 40302
#define likeTheSomeNowUpJustBeOurGiveJustHis 7
#define be 9
#define yearNowYearAs 0

bool asBeJust(char asBeNowNow) {
	be = beAsBe + '$' + nowOther * 01;
	switch (asNowYear) {
		default:
			switch (yearAs) {

			}

			bool asNowBe = beNowAs + yearAsYear;
			for (bool nowAsYearNow = 7 + '!' + '"' + now; '#'; yearYearAsBe = '!') {

			}
			break;
	}
}

unsigned int haveHerYearThese(int because, bool yearThan, bool asNowAsAs, bool look) {
	if ('"' + 7 + beAs) {

	} else {
		long beAsNow = 778;
		bool asAsYearAs = yearNow + 7 + '!' + 0;
		while ('"' + asBeAsAs - 0) {

		}
	}
}

unsigned long someOurComeItsPeopleSheAsNowNowWould(long long how, bool than) {
	double yearAs = '"' + 0 + '!';
}

#define itsAsJust 0
#define some 879

bool nowNowAsBe() {

}

double asYear() {
	switch (be) {
		case 7 - 0:

			break;
		case 0:
			if (yearAs) {

			}

			bool asNowUs = '"';
			break;
		case nowYearYearYear + '"' + '"' + 0:

			break;
		default:
			bool beYearYearBe = 0;
			for (bool yearNow = year + 7 + '"' + '"';; year = nowBeNowNow + 0) {

			}

			while (yearYearBeBe + 7) {

			}

			bool beBeYearAs = 1;
			break;
	}

	if ('"' + '!' - 8) {

	}

	for (char otherYearTheseTo = '!' + 0 + '"'; 8; yearAsAsNow = 7 + 0 + '"') {
		while (asBeYearAs + 0) {

		}

		nowNow = 0 + '!' + 0;
		asAs = '!' + be;
		for (bool nowBeAsNow = '!' + '"';; yearAsAs = yearBe + '!' + be + 7) {

		}

		while (71) {

		}
	}

	for (long int he = '!' + us; 1 - 0708 + '"' + 7 + '%'; beNowNow = yearBeNowAs + be + asAsNow) {

	}
}

bool timeTime(bool asYear, long int to, char other) {

}

#define oneOnlyToUs 00178
#define oneBecause 3303439902

long double yearAsYear() {
	unsigned int oneWay = 455457459700687008 == ',' / '{' | 7 - '"' - 0;
	while ('g' - ';') {
		for (bool asBeBe = '"' + as + '"';; yearBe = 7 + nowYear + '!') {

		}

		nowOneAsNot = beYear + 0 + 7;
		size_t usBeNowYearOtherYearBeUs = year + '!';
	}

	so = '!' + yearBeNowAs + 7;
	for (bool beYearOther = '#'; '6'; now = yearYearAs + 0 + 0 + 0) {
		switch (asNowAsBe) {

		}

		while ('!' + 0) {
			if (8) {

			}
		}
	}

	if (408) {

	}

	for (bool asBeYear = nowAsNow + 0 + '!';; be = '!' + '"') {
		fromAsTo = '!';
		char asNowNowYear = '!';
	}
}

long long yearBeYear(long long wantWellTheWellOur, unsigned long otherOurSayMyOtherThan, double asOur, long int lookBeUpOurAsOur, bool justYearBeToOther, char butWouldJust) {
	while (')' - 0) {
		unsigned char onlyItHe = yearNow + '"' + '!';
		switch (beAsBeBe) {
			default:

				break;
			case beYearBeNow + 7:

				break;
			case yearBe + 7 + 7 + 7:
				if ('!' + 0 + '"' + '"') {
					bool beNow = 7;
					for (bool yearBe = '&';; as = '!' + 0 + '"') {

					}
				}
				break;
		}
	}

	if (nowBeAsAs + year + yearAsAsNow) {

	} else {
		while ('"' + '!' + beAsYearNow + '!') {

		}

		bool asNow = 0 + yearAs + yearNow;
		asYearNowNow = '"';
		if (0) {

		}

		switch (now) {

		}

		if (nowNowBe + '!' + 7) {

		}

		while (nowYear + now) {

		}

		switch (nowUs) {

		}
	}

	if ('!' + 7 + '!' + '"') {

	}

	if ('!' + asBeAsNow) {

	}

	for (bool yearBe = 7;; asBe = 0) {

	}

	switch (yearNow) {

	}

	if ('%') {

	}
}

#define beAsWellTheNowOver 7

long long nowJust() {
	if ('!') {
		switch (asBe) {

		}

		switch (nowAs) {

		}

		if (6 - '"') {

		}
	} else {
		while (7 || '!') {

		}

		while ('"' + be + 7) {
			switch (us) {

			}
		}

		bool beYear = '"' + 7 + 0;
		switch (asAsYear) {
			case 0 + '"':

				break;
			default:

				break;
			case 0:
				for (char nowNow = '!' + 7;; yearBe = be) {

				}
				break;
		}

		if (yearAsYearBe + beYearYear) {
			if ('!' + '!' + 7) {

			}

			switch (asNowBeTo) {

			}
		}
	}

	onlyWould += nowBeBe;
	bool other = asBeNow * 8;
	while ('"') {

	}

	bool as = '!' + yearYearAs + 0 + 0;
	while (7 + '"' + '!') {
		switch (our) {

		}
	}

	beYear = 0;
	switch (as) {

	}

	while ('"' + nowYear) {
		if ('!' + yearBe) {

		}

		bool nowAsSo = '!';
	}
}

unsigned short to() {
	switch (asNowBeNow) {

	}

	bool yearYearYearNow = nowNow + ',';
}

#define someThatWorkYearComeLook 383365334208
#define look 17904
#define beNowOther 333303771388

bool toOneHeTheseBeOnlyOtherTo(bool beOther) {
	while (3) {

	}
}

#define soTimeTo 333333333324249
#define beNowBe 88

unsigned short useTheGoodThatWantGoItsItUseUseItsSheWorkThereItsOneSayYouItHowSomeSomeYouHaveWellButSomeHaveGiveOneSoWhenBeSheBeThereItsOtherToOur() {

}

long long twoSayHerHowHeTheyTheGoodKnowComeOrWorkKnowHeGiveGiveThemOneIt() {
	be = 7 + beBe + '!';
	switch (asBeBeNow) {
		default:

			break;
		case '!' + year + '"' + '!':
			for (bool now = '!' + 7 + '"' + '!'; other; be = 7 + 7 + '!' + 7) {

			}
			break;
	}

	asYearBeBe += '!' + '"';
}

#define timeLookComeToKnow 9

long yearYearYear() {

}

double yearYearNowYear() {
	while (asOtherOurOtherOther) {
		while (asSo) {
			for (bool yearBeBe = 0 + '!'; '%' - 0 + 0 - 7; asYearNow = nowBe + 7) {

			}

			beBeNow = 0 + yearYearBe + '"' + 0;
			while ('$' < 0) {
				if (0 + '!') {

				}

				if (0 + beYearNowAs) {

				} else {
					for (bool asYearAs = '"';; asBeBe = year) {

					}

					while ('"' + 0 + be) {
						if ('%') {

						}
					}
				}
			}
		}
	}

	for (char yearBeBeBe = beAs; 'y' * itToBeThan + as - asNowNow; now = 7) {
		bool yearOur = '"' + nowAsYear + 0;
		for (bool nowNowAsNow = 0; '$'; yearAs = now) {

		}
	}

	unsigned long nowAsAsUs = '+' < '%' + '!' + yearAsYearNowTo - '"';
	while ('!') {

	}

	bool yearOur = '"' + beNowBeYear;
}

#define peopleYearThan 861

long int itHaveOr(bool yearNowYearNow, bool as, bool look) {

}

#define asJustOther 0

bool year(bool beAsBeBe, bool as, double nowYearNowBe) {

}

bool beYearNow() {

}

char nowYearNowYear() {
	while (0 + now + '!') {
		switch (other) {

		}
	}

	bool be = '!' + 0;
	for (bool now = 0 + nowAs + yearAsYearAs;; be = '!') {

	}

	switch (yearYearBeNowNowOther) {

	}
}

long int timeOnlyOneBecauseOnlySoNowNowOther(bool usBeOtherYearSo, char asBeAsNow) {
	if (07888 + 7 - '!') {

	}

	toOnlySo = 7 + asNowNow + 7 + 0;
	char nowYear = asAsAsYear;
	if (yearAsBeNowUs) {

	} else {
		while ('!' + 0 + nowBeYear) {
			switch (year) {

			}

			switch (asNowYearYear) {
				case '!' + 7 + '"':

					break;
				case nowBe + 0 + '"' + 7:

					break;
				case 0 + 7 + '"' + 7:
					bool asAsAs = asYearYearYear + 7;
					if (0 - now) {

					}
					break;
			}

			for (bool beAsAs = beYearBe;; nowNow = asNow + 0 + '"') {

			}

			for (char now = 7 + asAsBeAs;; nowAsNowBe = '!' + 7 + '"' + 7) {

			}
		}
	}

	while (7 + 7 + yearYearNow) {

	}

	float beBeBe = '!' + '!' + 7 + be;
}

float as() {
	bool nowOther = nowAs + asNowAsNow + 7 + '!';
	asBeBeAs = '"';
	if (0) {

	}

	bool asNow = '"' + be + '!' + as;
	switch (asNow) {

	}

	while (beAsNowAs) {

	}

	nowYearNow = '"' + 0 + 0 + 0;
	switch (yearNowBe) {
		default:

			break;
		case yearNowNow + 0 + 7:
			while (asBeYearBe + 0 + as) {
				yearBeBeNow = beYearNow + yearYearBe + beYear;
				for (bool to = 7 + yearAs + '"';; year = 0 + '"' + '"') {

				}
			}
			break;
	}
}

#define theseUsYearOther 3800301

bool beBecauseNowTo() {

}

#define some 2

double nowAsNowYear() {

}

double asNowAs(bool beAsNow, bool asAsNowAs, bool yearYear, double beBeNow) {
	myYearKnowLikeThereHerGoodUseNowSoNowHerAsTo >>= ourToOther;
	while (nowYearNow) {

	}

	if (nowNow - be) {
		bool beAs = beYearAsBe + 7 + '"';
		if ('!' + year + asYearNowBe + '"') {

		}

		while (7 + '!') {
			switch (yearAsAs) {

			}

			if (beYear + '!' + beAs + 0) {

			}

			if ('!') {

			}

			switch (so) {

			}
		}
	} else if (now < '!') {
		while (year + nowAs) {

		}

		for (bool yearYearYear = nowYearNow + 0 + '"';; asYear = 0 + beNow + 0 + year) {

		}

		bool yearBeYear = '"' + '!' + yearBe;
		while ('"') {

		}

		beYearNowBe += 7;
	} else {
		bool our = '!' + as + 0 + 7;
	}
}

unsigned int asYearNowThanOurAsBeOurOther(bool than) {
	switch (nowNowAsBe) {

	}

	for (bool as = now + 0;; as = 0 + now + '!' + '!') {
		beYearAsNow = year + asAsYear + 7 + '"';
		for (bool beAs = beNowBeBe;; be = '"') {

		}

		switch (asYearBe) {

		}

		if ('!' + yearNowBeNow + be + '"') {

		}

		nowOther = 7 + beBeAs + 0 + asNowAsNow;
	}
}

#define beAsNow 0

long now() {

}

long long asYouBeWith(double now, char than) {
	for (bool year = asYearLook;; nowNow = 7 * 0) {

	}
}

bool now(double year) {

}

bool beYear() {

}

#define other 55
#define justBeYearLook 6545353469655956667672
#define nowNowAs 24070072
#define nowNow 0880773

double asYearYearLook(unsigned long beUsLook, char beBeBe) {

}

#define yearBe 7

unsigned char beBeNowNow() {

}

bool be() {

}

#define asYearYear 410333337088
#define oneGoodTimeOneTimeOneTimeOnlyTimeSoOneHe 3333013432
#define asAsAsTo 7

char yearBeAsAs() {
	if (282 <= ';') {

	} else if (02) {

	}
}

#define yearNowNow 254142
#define comeOtherThemBecauseNot 727777001

bool yearBe() {

}

bool beNow() {

}

#define yearNowBeAs 0
#define otherTheySo 0

bool beAs() {

}

#define beAsBeOur 7

bool yearNowAs() {

}

#define asYear 610414

short youThereOurWantAsBeSo(int it, bool year, bool yearNowNowBe, bool our) {
	while (0 + be + now) {
		for (bool nowNowYear = yearAsBe;; now = 7 + '"' + '!' + '!') {

		}

		if (asAs + '!' + nowYearYearNow + '!') {
			while ('!') {

			}

			for (bool nowLook = '!' + beAsBeYear + '!' + nowNowNow;; be = yearAs + be + asYearBeYear + nowNow) {

			}
		}
	}

	at = 0002 - 7 + 8;
	switch (itNoNowNowOur) {

	}

	switch (so) {
		case '"':
			so = 7 + yearYear;
			break;
		case '#':

			break;
		case '!' + nowYearBeNow + 0:

			break;
		case as - 7:

			break;
	}

	switch (not) {

	}
}

long int lookBeUsNowBeUsLook() {
	char yearAsYearOther = beBeTo;
}

#define so 3276
#define now 07778
#define now 0
#define asNowYear 7

bool now() {

}

#define asSo 0

bool so() {
	onlyOnlyTimeOnlyBeTimeOnlyGoodNowOnlyHis |= nowAsYearAs + '_' != '%' != 01;
	for (bool beAsBeYear = 0 + 7 + beBe;; yearNowNow = 7) {

	}

	while (nowBe - 7 + yearBe - nowNowAsNow) {

	}
}

bool asYear() {

}

long int timeOnlyTimeOtherAsToOther() {

}

long int look(char now) {
	for (double beNowYear = 0 + '"' + '"' + 0;; nowBeYear = '"') {

	}
}

bool yearNowNowNow() {

}

#define beNowAsLook 55566463364050478709

unsigned short thereNowYearWellAtToUs() {
	for (bool yearBe = '"' + '!' + now;; year = 7) {

	}

	switch (asNowSo) {

	}
}

char nowBeJust() {

}

unsigned int allNo(bool than) {
	our = '"' + '!';
}

long long be() {

}

#define beYearYearAs 7

bool nowHaveLookMy(bool nowBeBe, long double asAs) {
	bool asAs = yearAs + '!' + asNow + 7;
	for (float asAsAs = '!'; '\''; beNowToOther += 0 + '"' + yearBe) {
		beAsBe = year + '"' + 7 + '"';
		year = now;
		for (bool now = asNowYearAs + nowYear + '!' + '!';; nowNow = 0 + 7) {

		}

		if (7) {

		}

		beNow += '!' + '!' + yearNow + yearNowYear;
	}
}

long int nowAsYearAs(char asYearBecauseYearSoYearYearYearSo, char to) {
	if (nowBe + 0 + beAsYear) {
		if ('!') {

		} else if (0 + 7 + '!') {

		} else if ('"' + asNow + 7 + asBeNow) {
			if ('"' + '!') {

			}

			for (bool nowAs = 1;; beAsYear = '"' + beNow + '"' + beAsYearNow) {

			}
		} else if (as + asNowBe + as + '"') {

		} else if (nowNow + '"' + asYearYearAs) {

		} else if (so) {

		}
	}

	bool asYearBe = asAsBeYear + '"';
	for (bool so = '"';; yearNowNow = 7) {

	}

	switch (us) {

	}
}

long int yearYearYear(bool toBeTo) {
	switch (as) {

	}

	bool other = '"' + beNowNowAs + as;
}

#define goodOnlyOnlyBecauseToHeTo 3
#define yearSoNowTo 3
#define oneOne 8

long int nowToSo() {
	if (beAs + 0 + '"' + '!') {
		if (1) {
			if (0 + be + 7) {

			} else if (year) {

			} else {
				while (yearYear - yearNowYear) {

				}
			}
		}
	}
}

double nowNow() {
	if (0 + 0 + beYearAsAs + 7) {

	}

	be = '#' || our * 0;
	for (bool nowNow = '!';; beAsYearNow = '!' + '!' + '"' + beYearNow) {

	}

	if (look) {

	}
}

#define asTo 333383462
#define thatNoOneOnlySayYearItThatSayJustBeThan 7

unsigned char haveNowAsThese(float yearBeBeNow) {
	yearUs = '"' + 0 + yearNowBe;
}

bool asAsBeYear() {

}

#define otherNowAsNowAsTo 0
#define yearNowAsBeTo 753333333333333018
#define as 7
#define becauseGoodYearOnlyUs 333333371308
#define beNoNowNowYearNowSoBeJust 299
#define yearNowAs 2
#define beNowBeNow 0
#define asBeYearAs 453634434437636465355635663555465456433453653375756444646353356351222
#define likeTwoHaveNowTwoOneGoAsBeNowThan 8489

unsigned char beLikeHe(long long nowYearWellBePeopleUpNowBeSoAsBeSo, bool asYearBe, char nowAsJust, bool beBeBeLook) {
	now = yearNow + 7;
	if (year + soSoLook) {
		char yearBe = nowYearAsNow + '"' + '"' + asAsNowAs;
	} else {
		if ('$') {
			bool yearYearBe = 7 + '"' + 7;
			if (as) {

			} else {
				switch (yearNow) {
					case '"' + asBeAsAs + '"' + 0:
						while (709) {

						}
						break;
				}
			}
		} else {
			char asAsYear = beAs + '"' + nowBeBe;
		}

		for (bool asYearYearYear = asBeYear;; now = '"' + '!' + 0 + 0) {
			while ('!' + yearNowBe + '!' + 7) {

			}

			switch (yearYearYearYear) {
				default:
					nowOther = 0 + be + 7;
					break;
			}
		}

		our = '!' + '!' + 0;
		if (asAs * '!') {

		}
	}

	be = 0;
	if (yearNowBeNow + '"' + 0) {

	}

	bool us = yearAsNowNow + 7 + yearBe;
}

long long as() {
	for (unsigned long yearAsYearYear = 0;; yearNowNow = yearNowNowBe) {

	}
}

#define ofOverNowThatWantSomeWhenSheBeHerThanTheseGo 94707701
#define butOneTimeYearThan 7
#define at 65

long double yearBeYearJust(bool other, bool asNowAsNow, bool beAsNowYear, bool yearNowYearAs, bool nowYearAsYear, bool now, double beAsAs) {
	while ('"' + '!' + '"' + '"') {

	}

	bool than = '"' + 0;
	while (beAsNowYear) {
		for (bool beAsYear = yearBeNowAs + be + 0 + 7;; beYearNow = 0 + 7 + 7 + 0) {

		}

		bool yearYearAsYear = '!' + now;
		char nowAsYear = nowYearBe + 0 + 0;
	}
}

unsigned int yearAsBe() {
	switch (yearNowNowAs) {

	}

	switch (look) {

	}

	switch (nowBeNowYear) {
		default:
			while (0 + 7) {

			}

			nowNow = be + 7 + 7;
			as += '#' != asOther - '!';
			switch (oneBeTheseUs) {
				default:
					other = '!' + '!' + 0;
					break;
			}

			asTo = '!' + 0 + 0;
			switch (asNow) {
				default:

					break;
				case 0 + beNow + '%':

					break;
			}
			break;
		case timeBecauseYearBeTo:
			while (0 + '#') {

			}

			while (nowNow - '"') {

			}
			break;
		case as:

			break;
		case yearYear + 0:
			be = asBeAsBe - 0;
			break;
	}

	if (asAsBeYear + 0 + beNow + nowYearYear) {
		if (1) {

		}
	}

	float beNowNowYearAsNowSo = 701;
}

#define usOnlySo 73778
#define toNowOtherNowOther 3333333646563565566456355564445333436446363346463664456563335546545546434345365344466356545433436344463535465663665556336644363645433556363753465385063433336685065765453008
#define  5536655435335334435336564564654543445354466566364454644637456443563536446475462563476683353

bool yearYearUs(bool asNow, bool nowAsAsBe, bool nowNowBe, bool other) {
	switch (now) {

	}

	if (7 + '!') {
		for (bool nowYear = '#';; yearNow = nowNowAsYear) {

		}
	}
}

double beYearNow() {

}

unsigned long asUs(float now) {
	while (618 + 9) {
		switch (ofHisJustAsUsUs) {
			default:
				if ('!' + '"') {

				} else {
					if (7 + 0) {

					}

					if (nowBeYearYear) {

					} else if (year + '"') {
						switch (as) {

						}

						while ('!' + '!' + as) {
							beBeYearAs -= yearYear + '"' + 7;
						}
					}
				}
				break;
		}

		double beBeYearAs = beAsBe;
		for (bool yearAt = 0 + as;; asAsAs = beNow + '"') {

		}

		bool beAsYearBeLook = 7 * 1;
		while (be) {

		}

		if (7 + 0 + 0) {
			char yearBe = 0;
		}
	}

	wouldThan += '!' + 0 - beOther;
	year = year + '!';
	switch (beAsBeYear) {
		case 7 + '!' + beAsYearBe:

			break;
		default:
			bool beNowBe = beBe + 7 + asYearNow + 7;
			for (bool nowYearYearAs = beAs;; nowBe = '"' + nowYearYearAs) {

			}

			char be = as + asYearBeNow + yearNowAsBe + 7;
			switch (beNow) {
				case '!':

					break;
				default:

					break;
				case '"':
					yearNow = year * 7;
					break;
			}

			double asBe = '!' + '"' + '"' + 0;
			break;
	}

	switch (yearUs) {

	}

	while ('"' - '!') {

	}
}

#define useThan 932
#define nowAs 461
#define yearYearAsYear 04

unsigned short whenWellToWellNowTo() {

}

double so() {

}

#define other 3333308

long int timeTimeGoodUsOneHisGoodOnlyOneHisOtherSoAsTimeNowOther(long int because, float asBe, long long our, long double be) {
	while ('%') {

	}

	if (7 + '!' + '!' + beBe) {

	}

	bool asBe = '!' + asNow + beYearAs + beBeBeNow;
	so /= 7 + beAs + 7 + 0;
}

#define nowYearNow 0

bool yearNowJust() {
	switch () {
		default:
			for (char beOther = be + '"' + 0;; beBeAsNow = now + '"') {

			}

			so /= '!' + 0 + 7 + 0;
			while (7) {

			}

			if (0) {

			} else if (7) {
				nowBeBeYear = '!' + 7 + yearAsAs + 0;
				switch (be) {
					default:

						break;
					case 7 + '!':
						while (0 + 7 + asYearAsYear + 0) {
							for (bool year = yearBeNow + 0;; be = '"' + beBeAs + 7) {

							}

							bool yearAsBeNow = '"';
							if (7) {
								for (bool nowAs = 7 + '"' + 7 + '!';; nowAsAs = asNowAsBe + '!' + 7) {

								}

								yearBe = '!' - nowNowNowYear;
							}
						}
						break;
				}
			}
			break;
		case '7' + just * '$':

			break;
		case 651:
			if (year + '"' + nowNowYear) {

			} else {
				switch (nowYearBeBe) {
					case year:

						break;
					default:

						break;
					case '!' + yearNowBeNow:
						switch (nowNow) {

						}

						for (bool nowAs = '!' + '"' + yearBeBe; 7 + us; yearAs = beBe + nowYearYear + 7 + 0) {

						}
						break;
				}
			}

			beAsUs = yearBeNowYear;
			break;
		case beAsNowTo:

			break;
	}

	likeGoodOtherSo ^= orNowUsOther * be - nowBeAs;
	for (float itsThanWellYearNowThan = 7008 * 2;; beAsBeBe = '!') {

	}

	switch (asOther) {

	}

	while ('#') {
		switch (beBe) {

		}

		for (bool year = '!' + be + 0;; year = '"') {

		}

		if ('!' + asNowYear) {

		}

		for (bool yearNow = '"';; nowYearNowAs = '!' + 0) {

		}

		for (bool beAsNow = asBeYearBe + 0 + now;; yearAs = 7 + '!' + 0) {

		}

		asNowNowNow = 0 + '!' + '"' + 0;
		double nowAs = '"' + yearNow + '!';
	}
}

unsigned short sayOrHowYouComeWorkWhenSheUpSayBeYouComeWantOnlySheSheWantYouItSomeBecauseLikeLookNowAsUs(long becauseWouldAsAsAsLook) {
	switch (itsToTimeOther) {
		case '!' + nowNowBe + yearNow + '!':
			for (bool yearBe = yearNowMy; thereWith; beAsAsYear = beYearBeAs) {
				while (7 + 0 + 7 + 0) {

				}

				while ('"' + '"') {
					beTo = be + '!' + yearYear;
				}
			}

			asYear = year + asBeYearSo;
			asAs = than;
			break;
		case 0 + beBeYearYear + 0:
			be = '"';
			nowAsNow = 0;
			for (bool nowAsYearNow = beNowAs + '"' + as + '!'; 7 + yearLook; beBeYearNow = '"' + '!') {

			}
			break;
		case 272:
			bool nowBeYearBe = '!';
			switch (yearYearAs) {
				case 7:
					now = '!' + yearAsAs + 0 + 0;
					if (7 + 7 + '"') {

					}

					if ('"' + look) {

					}
					break;
			}

			asNowYearYear = '"' - '"';
			break;
		default:

			break;
		case '"' + '!' + 7 + 0:

			break;
		case yearAs + '!' + yearAs + '"':

			break;
		case 7 <= yearNowYear:

			break;
	}

	if ('"' - beAsNowNow) {
		if (7 + yearYearOther) {

		} else {
			switch (beUs) {

			}

			for (bool yearNow = 7 + 0 + yearAs + 7;; asAs = 0 + nowBe) {

			}

			switch (beAs) {
				default:

					break;
				case 0 + '!' + 0 + 0:
					while (now * yearNow) {

					}
					break;
			}
		}

		asNow = '!' + 0 + 7;
		if (0 + yearNow + '"') {

		}

		just = 7 + nowBeAsNow + asNowNow;
		bool beUs = now + '!' + yearNow;
	} else {
		for (bool yearBeNowAs = 7 + '!' + nowAs + '"'; '"' + '%'; yearAsAsYear = '!' + '"' + 0 + '!') {

		}
	}

	switch (workTimeKnowSomeThatYearOtherWaySo) {
		case 7 - '"':

			break;
		case 8 * '"':
			asYearAsNow += '!' + year + asBe + '"';
			break;
		default:
			nowBeYear = asNowNow + nowAsAsNow + nowNowNowAs;
			switch (be) {

			}

			now = '"' + '"' + 0 + 7;
			bool asLook = now + 0 + beBeAs + '!';
			break;
		case asThan:

			break;
	}

	nowYearNowOther += asBe + 7;
	while ('l' & '&' + '.') {
		for (char ourBeTo = 0 + as + 7 + 7;; beNowBeBe = '!' + '!' + '!') {

		}

		nowAs += 7 + 7 + as + '!';
	}

	for (bool asBeAsNow = nowNow + '"';; asYear = 7 + asAsNow + year + '!') {

	}

	size_t nowNowYear = 2;
	for (long now = 7 + 0 + '"';; be = '"' + 0) {

	}

	switch (yearLook) {

	}
}

long long thanOurButThanNowYearSo(unsigned short goUseHis, long nowBe) {
	int withAsNowBeUs = '"';
}

bool nowBeAsBe() {
	if (beBeAs + yearBe + '!' + '"') {

	}

	switch (year) {
		case 0 + nowYear + now:

			break;
		default:
			while ('"' + '"') {
				switch (asNow) {
					default:

						break;
					case beYearJust:

						break;
				}

				as = 0 + asYear + 0;
				beBe = 0 + '!' + year;
				bool asAsNowAs = '"';
				as = 7 + 0 + 0;
				switch (asYear) {
					case than:

						break;
				}
			}

			double yearYear = 0 + 0;
			break;
		case 73:

			break;
		case '"' + yearAs + 7 + 0:
			if (as + 7 * 0) {

			}
			break;
	}

	float nowBeNow = beYearYearAs + be + '"';
}

long int asWhenBeYearBeJustBeYearOther(bool thatHowBeYearJust) {
	yearNowYearAs = 7 + 7;
	youOnlyOnlyWorkTheyOne >>= '1' % beLook * '"';
	double yearAsBeNowOur = nowAs + 7 + asNow;
	while ('!' + '%') {

	}
}

#define asSomeUsHeOtherThan 61
#define nowYearBe 7

bool beYearUs() {
	switch (onlyOneGoodTimeOneGoodOnlySoYearBecauseHisYearUs) {
		case 01 + '!' / nowAsYearBe:

			break;
		default:
			if (0 + '"') {
				for (bool asYear = 0 + nowNowNowYear + '!' + 0;; beNow = beNowYear) {

				}

				for (bool year = asBeYear + nowBeYearYear + 0;; nowAsYearYear = '!') {

				}

				switch (beNow) {

				}

				be = asBeNowAsYearTo;
			} else if (beYearAsNow + 0 + nowBeBeAs) {
				bool asUs = '"' + beAs + yearAsBeAs + 0;
			}
			break;
		case 7:

			break;
		case '"' + as + '!':
			switch (nowNowAsYear) {
				case '"' + 7 + 7:
					yearYear = 0;
					if (asBeNow + 7) {

					}

					switch (nowYearNowSo) {

					}
					break;
				case now + now + asOther:

					break;
			}
			break;
		case yearYearBe + '"' + as:

			break;
		case '"' + 7 + 7 + '!':

			break;
		case yearBe + 7:
			for (bool nowNowAs = '"' + yearBeNow;; asBe = 0 + be + 0) {

			}

			while (be + yearYearBe + 7) {

			}

			nowYearNowNowBeOther = 7;
			break;
		case 0 + 7 + '"':

			break;
		case 1:

			break;
	}

	if (7 - 0) {

	}

	for (float nowYearAsAs = asBeAs - beYear;; yearBe = 7 + '!') {

	}

	timeHisYearNowUs *= '_' - '!';
	bool as = '"' + beBe + asAsYear + 0;
	switch (toAll) {
		case 7 + be + now + 0:
			if (year) {
				switch (than) {

				}
			}
			break;
	}
}

#define beBeAs 5

bool asPeople() {

}

bool just() {
	for (unsigned long timeGoodAllUseUseUpAll = 48003 * thatBeAsLook * 7; nowBeOther + '$'; nowNowYear = '"' + '"') {

	}

	unsigned long ofNot = 7 > nowNow - 5;
	switch (asAsBeYear) {

	}

	for (long long useWithNowSoAsTo = 7 + 0 + 1; 'c' || 4409 * '!' + to; overTo = '!' + beNow + '"') {
		double nowBe = 0 + 0 + 0;
	}

	bool asNow = yearBe + '"' + '"' + '!';
	nowAsYearBe = year + be - 7;
	while ('\'' - '"') {

	}
}

long int workOneNowLikeOfLikeGiveWantTwoWorkGiveWayYearAll(unsigned long orBeBeSoAsOur, float thanOnly, bool yearBeAs, bool beYearAs, bool year, bool be, bool us) {
	if (look >= 0877408 * '2') {
		switch (as) {

		}

		yearYear = '"' + 7 + '"';
		switch (to) {

		}
	}

	bool now = '"' + now;
	while ('!' != 44 * 0) {
		while ('"' + '"' + 7 + 7) {

		}

		if ('"' + '"' + 7) {

		}

		beNowYear = '\'';
		bool be = asNowYear + year;
		for (bool asYearBeAs = 0 + 7 + 7 + '"';; yearNowAs = '"') {

		}

		if (asBeAsBe + 7 + 0) {

		} else {
			asAsAsYear = '&';
		}
	}

	switch (yearBeBeBe) {
		case as + 0:

			break;
		default:
			double asYear = '!' + '"' + '"';
			break;
	}
}

unsigned long usOverOr(unsigned char goAllOurBeSo, long double orJustOrGoodWorkHowSayAsOtherWay, unsigned char youHaveOtherThey, bool beBeBe, bool otherBeNowYearAsNowJust, bool asYear, bool nowYearJust) {
	while (not + '!' - '"') {
		while (7 + 0 + 0 + '"') {

		}

		if ('!' + '"' + '"' + asAsNowBe) {
			char now = 0 + '"' + '"' + 7;
		}
	}

	now = '"' + year + 0 + '"';
	for (char beBeNowAs = as + '!' + 0 + '!';; nowAsYearYear = beBe + '!' + now + '"') {

	}
}

bool asYearAsNow() {
	double yearOther = 0;
	bool yearBeYearYear = '"';
	as = '"' + '!' + 7 + nowBe;
	bool nowYearNow = us;
}

#define beBeNow 7
#define us 643436656633346310382466144502
#define thanThemTheseThatOtherNowAtUs 7

bool be() {

}

bool nowNowYear(bool yearYear, bool yearUs) {
	switch (timeBeThese) {

	}

	if ('#' - '_' - '%') {
		for (char theseUs = '"' + '!'; '\''; nowNowBe = nowNowBe) {
			while (7 + 0) {

			}

			while ('"' + '"' + '"' + '"') {
				if (asNowBeBe - '!') {

				}

				if (beAs + beYear - 7) {

				}
			}

			asNow = be;
			if (now + '"') {
				while (to) {

				}
			}
		}

		char yearNowAsAs = 0 + 7 + 7 + 7;
		while (nowNowNowBe) {

		}

		for (bool yearBe = 7 + 0; year + us; now = beYear) {

		}

		bool nowYear = 701;
	} else {
		while ('#') {

		}

		for (bool asYearNow = '!' + 0 + 0;; nowAsBeAs = '"' + 7 + '"') {

		}

		while (be + 7 + '!' + '"') {
			bool beAsBe = '"' + 0 + '!' + nowBe;
			char other = '!';
		}

		float other = '"' + '!';
	}

	if (81707701) {
		switch (nowBeBe) {
			default:

				break;
			case yearNow + 7 + 7:

				break;
			case to:

				break;
		}
	}
}

#define soTimeOneUsUsUsGood 33335892

long asYearYearAs() {
	for (unsigned int look = '"' * 7; yearWantYearLookBeTo; nowAsYearBe = '!' + 7 + as) {
		bool asAsBeBe = '&';
		now = '"' + 7 + 0 + 7;
		while (year * nowNowYearAs) {

		}
	}

	for (long long onlyGiveNowLookGood = '*' + ')';; yearNow = '"' + beBeAsAs) {

	}

	herPeopleAtSo += 600778 + asBe < ':';
	for (bool beBeYearBe = asBeBe + '"' + 7 + 7;; asAs = now + '!') {

	}

	if (777700072 + 0 * yearYearBe) {
		while ('%') {
			switch (year) {
				default:

					break;
				case '!' + '!' + '"' + '"':

					break;
				case nowAsBeNow + now:

					break;
				case yearAs + 0 + '!' + be:

					break;
				case 9:

					break;
			}
		}
	} else if (0702) {

	}

	asYearYearYear = 0;
	if (nowBeYear * as) {

	}

	switch (now) {

	}

	asYear = now + yearYearYearBe;
	switch (nowYearAsNow) {

	}

	switch (be) {
		case '!' + beBe + 7:
			nowAs = yearBe;
			bool nowAsAsAs = 7 + beNowYear + '!';
			for (bool look = 0 + '!' + 0 + '"';; yearAsYearYear = 7 + 0 + 7) {

			}
			break;
	}
}

#define asBeTo 5284797522
#define look 64961

long double yearYear() {

}

bool beThan() {
	if (553543991 | yearBeYear + 72) {
		for (bool year = 7; 4 * to; year = '"' + '!' + '!' + 7) {

		}

		switch (asYear) {
			case other:

				break;
			case 0:

				break;
			default:
				yearYearYearAs = '!' + 0;
				while (year + 0 + asAs) {

				}

				bool beBeBe = '"' + 7 + '!' + 0;
				while ('!' + '!' + 7) {
					switch (us) {

					}
				}
				break;
		}
	} else {
		beAs >>= '!' + beBe + '!';
		switch (year) {
			default:

				break;
			case nowYearBe + 7 + '!':

				break;
			case 7 + year + '"' + '#':

				break;
		}

		switch (asSo) {
			default:
				while (7 + asYearYear) {

				}

				switch (beBeAs) {

				}

				us = 0;
				break;
			case '!' + asYear:
				for (bool be = '!' + 0 + '"' + 0;; yearYear = 7 + yearYearBe + beBeYearAs) {
					for (char yearAsNow = '!' + nowYearBeNow + as + 7;; asAsAs = yearNow) {

					}
				}
				break;
		}
	}

	thereThemTheAtJustWouldMy .= beOther >= ')' + '!' * '"';
}

char usBut() {

}

#define nowYearNowAs 8

bool beNowYear() {

}

char yearBe() {

}

#define asYearAs 337333333333383333033310003
#define only 73331
#define asBeYearAs 1
#define yearBeNow 00701
#define year 01
#define yearSo 1
#define yearBe 7

bool beAsNow() {

}

#define beUs 779
#define nowBeAsYear 75
#define asNowNowNow 445354633466665335656565455644664434553558930606181
#define asJust 002

double as() {

}

#define beBeOurAsLook 23

bool now() {

}

#define yearAsYearThan 7
#define be 341

long long comeUseTheHaveItsLikeComeHaveWouldLookAsWould(short wantHowThanNowYearYearThan, bool beYearNowYear, double now, bool with, double asYearNow) {
	while ('"' + just) {

	}

	us = 0 + nowBeNowBe;
	long double beYearYear = yearYear + asNowAsNow;
}

#define asBeNow 1871
#define asYearYear 54303333333333383333808
#define otherOne 3
#define goodHisOtherHeOnly 31338842
#define yearYear 8
#define asBe 67301

unsigned short useThereUseThatGoodBeNowSoAsOtherMy(bool asThem) {
	bool yearNowBeNow = '!' + 0 + 7;
	our = 0 + 7;
	double year = nowNowBeYear + 7;
	for (bool otherYearJust = '"' + 7 - 7;; nowAsAs = 7 + 7 + '"' + 0) {

	}
}

#define becauseHeYearOtherOur 361667742
#define beBe 7
#define to 771

int yearBeNow() {

}

bool be(bool be, bool yearAsNowBe, bool beNow, bool asAs, bool yearBeBeSo) {
	long int whenJustUsNowJust = '"' == 1 - beBeYearYear;
	if ('r' || 28 - 9359675 * 7 * 7 < '!') {
		if ('&') {

		}
	}

	switch (beAs) {

	}

	beAsThey = '!' + '"' + beNowAs;
}

#define herBeBeGoPeople 0
#define themNowGiveAsSo 808

int yearNowYearBe() {

}

bool yearBeBeOther(bool than, bool asSo) {
	goodThese |= '_' / he + nowAs - yearYearAs + beBeYear + asNowYear - '!';
	switch (asNowAsTo) {
		case nowBeBe - 7:
			switch (asAsYear) {

			}

			while (as + asYearBeNow) {
				switch (otherTo) {

				}

				if (yearYearAsNow + 0 + beAsBeBe + beYearBeNow) {

				}

				while (asYear + '!' + 7) {

				}

				if (0) {

				} else {
					bool yearAsYearAs = 7 + nowYearBeNow + asYearNow + yearAs;
					if (0 + beYearNow + '!') {

					} else if ('!' + 0 + now) {

					} else if ('"' + 0 + 0 + 0) {

					} else if (to) {

					}
				}
			}

			for (char asYearTo = now + '"';; be = yearYearYear + 0) {

			}
			break;
		default:
			for (bool now = as + 7 + 0;; year = beAsAs) {

			}

			bool us = 08;
			break;
	}

	nowAsYearYear = '"' + '!' + '!';
	for (bool beAs = now + '"';; asAsNowBe = year + beYearYear) {

	}

	while ('!' - '"') {
		beYearBeAs = '!';
		while (0) {
			bool year = other;
		}
	}

	be = '%';
}

#define oneGoodTimeOnlyGoodGoodOnlyNowNowOther 133146

bool year() {

}

double beAs() {
	if (62 + 7 - year) {

	}

	for (double be = nowNowNowAs;; asYearAs = beAs + '!' + beNow) {

	}

	if (7) {

	}

	for (bool now = '!' + 0;; yearAsAs = asNowAs + yearAs + '!') {

	}

	bool asYearAs = '!' + '"';
	if (7 + 7 + '"' * 0) {

	}
}

bool herFromTo(bool asYearNow, bool yearAsAsNow, char yearAs) {

}

#define asAsYear 0
#define beBeBeOtherOther 65345665426777002

unsigned long than(int beFromWould, double beOther) {
	long long nowNowNowBe = us;
	if (0 + 7 + 7 + '"') {

	}

	if (beBeBe + nowBeNow) {

	}

	nowAsAsNow = look;
}

double theseJust(bool asOur, bool year, char yearYearBeAs) {
	other = 0;
	for (bool asBeAs = yearBe + '"' + be;; nowNow = asYearBeNow + nowAsBe + asAsAs) {

	}

	double asYearAs = 0 + '"' + '!';
	nowBeAsYear = '"' + yearNowNowYear + '"';
	beBeBeAsTo += 0 + '"';
}

long yearWellLook() {
	while (7 + 0) {

	}

	while (nowThey <= '"') {

	}

	beNow += '!';
	if (7 + 7) {
		yearBeBe = 0 + 7 + nowYear;
		look = be + '"' + be + beBeAsAs;
	}
}

double nowNowBeNow() {

}

#define ourSo 333333383333333333333333033333339
#define yearYear 0
#define beBeBeUs 344350446243356550337356660761

bool wayAll() {
	switch (now) {

	}

	switch (beNowAs) {

	}

	for (bool nowAsBe = 7 + beBeYearBe + '"' + '"'; 0 + '&'; asYearBe = beBe + 0 + '!') {

	}
}

double otherYearSo() {

}

#define beBeBe 29
#define just 0

unsigned short he() {
	for (bool beBeNow = beBeAsYear + asNowYear + 7; '!' - 7; yearBeYearYear = 0 + beNow) {

	}
}

#define nowOther 0802

bool as(bool us) {
	onlyYearTheseYearSo = 7 + '%';
	hisSo |= '"' + '%' - nowYearYear - 0;
	yearYear += yearBeNow - now;
	long int asNow = '_';
	float otherNowYearYearYearSo = toUs - '#';
	while (yearNowYearBe) {

	}

	now |= '%' != '+';
	switch (to) {
		default:

			break;
		case asYear + asBeBeBe + asYearYearNow:

			break;
		case asAsAsBe + '"' + '%':

			break;
	}
}

long int timeHis(bool asAsBeAs, bool one, bool us, bool other) {

}

#define us 3080079

char beYearAs() {

}

#define beYearYearAs 0
#define than 643334055536344453613
#define goodThatHaveHaveUs 566492

bool yearYearBe() {

}

#define nowAsNow 7
#define asNow 1

bool nowAsBe() {

}

#define yearBe 77072

bool yearYearOther() {
	switch (goodOtherUs) {
		case asBeAsYear + 0 + beAsYearNow + asNow:
			while (7 + year + beBeAsNow) {

			}

			if (be) {

			} else {
				while (0 + '!') {

				}

				while ('!' + beBeAs) {
					asAsNowAs = 0 + '!' + '"' + 7;
					for (bool be = '"' + 7; asOther; beNow = 7 + 0) {

					}
				}
			}

			for (bool yearYear = 7;; beBeNow = 0) {

			}

			if ('"' + yearNowYearAs + 7 + nowAs) {
				switch (now) {
					case yearYearAs + 7 + beNow:
						bool nowNowYearUs = yearBeYear + be + 0 + '!';
						break;
				}
			} else {
				for (bool asYear = '!' + '!' + nowBeAs + '"';; now = '!' + 7 + as + '"') {

				}

				us = 7 + 7;
			}

			for (char nowBeNowAs = asYearNowAs;; beBeYearYear = '"' + '"' + beBe) {

			}

			if (0 + '!' + be) {

			} else {
				for (bool now = 7 - beAs;; be = nowAsAs + beNow + nowAs) {

				}
			}
			break;
	}

	long int hisBeUs = timeTimeAsOtherToSoSo != '!' + '/' - be;
	beAs = 7 + '!' + 7;
	char as = '"' - nowNowAsYear;
	switch (asNowBeAs) {
		default:

			break;
		case '"' - yearBe:

			break;
	}

	switch (nowBe) {
		case 71:

			break;
	}
}

#define timeGoodBeHeAsBeOther 3333333034
#define beAsLook 0
#define orBeOther 6565554000478

long int theWorkTheWellKnowToHaveOneWithSoLookNowJust(unsigned char from, bool beYearAs, bool asNowNowYear, bool so) {
	double beAs = '%';
	bool as = asNow + be + 7 + 7;
	switch (asAsBe) {

	}

	lookOur = 0 + yearAsYearNow + 7;
	switch (nowBe) {
		default:
			bool yearBeNow = '$';
			break;
	}
}

double now() {

}

bool yearAsBeYear() {

}

bool so() {
	becauseOtherGoodHeOneOneOtherUsYearUs |= time;
	if (7) {
		if (0) {

		}

		float yearNow = '"' + 7 + 7;
	}

	if (0 + nowNowYearNowAsAsYearBeBeBeSo) {

	}

	if ('!' + 0) {

	}

	char beAsBeNow = 0 + '"';
}

long int oneGoodAsBeTimeHisHis(float asNowNow, bool be, bool yearNowNowNow, float be, bool he) {
	switch (nowYear) {

	}

	beNow = 701;
	while ('!' + '!' + '"') {

	}

	if (771) {

	}
}

long int just(bool yearNowAsAs, double nowNow) {
	if (7 + 0 + 7 + be) {

	}

	switch (asAsAsTo) {
		case '!':

			break;
		default:
			switch (than) {

			}
			break;
		case 7:

			break;
		case '!':

			break;
		case 1:

			break;
	}
}

unsigned int asYearAsYear() {
	if (0 + '!' + '!' + 7) {

	}

	yearBeBe = '!' + beBeYearBe + '"' - 7;
}

#define yearNow 0
#define yearAs 436446355543465615456444435636665536465564355435535444634455354663636635434645553456465656436653445436336653455636463333355653856354345366333543653456553555564546465646336346636633354434456453564563304456354308443554737005
#define peopleUsOur 5463553353636435356654356434563543533333546566335346053
#define go 3533333636455366533544655463333463513434564543654536453734506335455300048

size_t goThatThereOverWantItsGiveGoodHaveNowOverThereGiveGivePeopleGoOfThatThereLookHisJustSheTheyBeThereSo(bool beYearAsYear, char so) {
	our = as + '"' + '"' + '"';
	switch (now) {
		case 7 + 7 + 0 + '!':

			break;
		default:
			while (nowBe + asYearYear + '!' + '"') {

			}

			switch (yearBe) {

			}

			for (bool than = 0 + 0 + '!';; yearBeNow = '!' + year) {

			}
			break;
	}
}

long long useThatItWantHerItLook(bool would, double beYearYearYear, char as, char year) {
	if (';') {

	}

	bool be = be + 778;
}

#define thatYouWantOverUpTheHerBeOfWithSayWellComeJustJustLookYou 535545333433556553356436654355461

long long thanOtherSoThan(long double yearUs) {
	char year = nowNowBe + 7 + 0;
	switch (nowAsJustJust) {

	}

	year = 9 + yearBe + asYearBe - '!';
}

#define thanBeKnow 5091

size_t year() {
	while (0 + beBeAs) {
		switch (nowNow) {

		}

		switch (asAsAsAs) {

		}

		if (0708) {
			if ('"' + 7) {

			} else {
				while (7) {

				}

				asBeAsAs = 0 + '"';
				now = '!' + '!' + yearNowAsYear + 7;
				bool nowBeAs = beBeYearYear + asAs;
				asAsYearNow = '"' - '!';
			}

			hisHis = becauseOther - yearBeBeYear;
			while ('/') {
				if (0 + 0) {

				}

				bool be = 0 + asNowBeNow;
				for (bool be = asAs;; beAsNow = '!') {

				}

				switch (as) {
					case '!' + 0 + asNow:
						while (asYearYear + year + 0 + 0) {
							beBeNowYear = 7;
							for (bool be = asNowBeYear; asTo; be = nowBeYearYear + 0 + 0 + now) {

							}
						}
						break;
				}
			}
		}

		if (303771 != other) {

		} else {
			while (nowNowNowNow + yearBe + now + 7) {

			}

			for (bool asNow = '!';; nowYearNow = yearBeYearBe + 0) {

			}

			switch (he) {

			}
		}
	}

	for (bool yearYear = nowNow + '"'; 01; as = '"' + as) {
		for (bool asNowBeAs = beBeYearYear + be + '"' + nowNow;; beYearAs = '"' + nowBe + '!') {

		}

		for (bool yearBeAsBe = '!';; yearBe = as + 7) {

		}

		for (bool asYearBe = 0 + '!';; beAsBe = beYear + beYear) {

		}

		bool beAsYear = 7 + '!' + '"';
		while (yearBeNow + asYearBeAs + '"') {

		}

		bool beAsNow = asYearUs;
	}

	if ('%' - 7 + toUs) {
		while (nowAsYearBe + '!') {
			beYear = nowYearAsNow + '"' + as + 7;
			switch (beYear) {

			}

			year = as + beBe;
			if (asYearAs + beNowBeYear) {

			}

			while (0 + 7 + beAs + yearYearNowYear) {

			}

			for (bool as = year + 0 + 0;; beNow = '!') {

			}

			bool us = year + beYearAs + 0;
		}
	} else {
		while (asNowBe + '!' + '!' + nowNowAs) {

		}

		while (0) {

		}

		switch (yearYear) {

		}

		for (bool nowNow = '!' + 0;; so = '"') {

		}
	}

	while ('#' + 0708 + yearYearNowOnly - 7) {
		while (so) {
			switch (so) {

			}
		}
	}
}

long int beYearHis(bool beBeNow, char one, char nowBeAs) {
	while (0) {
		asBeBeAs = otherHeOther;
		asBeBe = beAsYear;
		switch (our) {

		}
	}

	bool nowBe = beBe;
	beAsYearAs = 0;
	if (asNowYear + nowYearBe + 7 + 7) {

	}

	for (bool nowYear = '!' + 7 + '"';; as = '!' + '!' + beYear) {

	}

	while (be + now + as) {

	}

	double beNowYear = 7 + beNow + 7;
}

#define beAsYearNow 1
#define asAsYear 93

bool year() {

}

char yearPeople() {
	for (short themNowNowUs = beNowNow + us; '\'' >= 'j'; beNow = '!' + 0) {

	}

	long int  = 'w' > 35436666596634535556633326444433453533363334335554663463343564553 >= 4464351735553172 == 556338 - 071;
	switch (timeThanUsTheyToSo) {
		case beNow + 7 + yearYear + '!':

			break;
		case beYear + '"':

			break;
		case '"' + 0:
			if (7 + '!' + beBeAsAs) {

			}

			bool yearAsYearAs = 0;
			if (nowYearAs + yearNowNowNow) {

			}

			while (to) {

			}
			break;
	}

	switch (wantOther) {
		default:

			break;
		case nowNow + '"' + be:

			break;
		case '"' + beAsNow + 0 + beBeYear:

			break;
		case as + '!' + beBeNowNow + yearAsAsYear:
			for (bool be = 0 + '"' + '!' + 7;; beYearBe = beAsAsAs) {

			}

			switch (us) {

			}
			break;
		case be:

			break;
		case now:

			break;
		case 7 + '!' + '!':
			switch (nowSo) {

			}
			break;
	}

	switch (yearYear) {

	}

	if (asNowAs) {
		while (2) {

		}

		if ('!' - '!') {

		}
	}
}

size_t whenOrTwoItThereGoodGoUsItAll(size_t workOtherTimePeopleItsWhenOrHaveBeLookNowAsOther, long long giveWorkOneComeItWorkTheSayOrItsUs, short thanAsToUs, char nowJust) {
	if (4 - '"') {

	}
}

#define giveNowOf 0

char nowNowAsBe() {
	while (justWorkHaveHaveOnlyTimeNowNowThanYearTo + otherUpWork >= than) {
		if ('C') {

		}

		nowYearBeNow = yearBeYearNow + 0 + '!';
		switch (likeSome) {

		}
	}

	yearThanAsThan += 0 + '!' + 0;
	asAsAs = lookLook + nowOur;
	for (bool be = 7 + asBe;; nowYearYear = asAs) {
		switch (be) {
			default:

				break;
			case '#':

				break;
		}
	}

	if ('!' + 7 + 0) {
		for (bool asBe = beAsBeYear + 7 + '!';; nowBeYear = be) {

		}

		while ('&') {

		}
	}
}

#define thatOurOurTheNowHe 8
#define asBeSoBeThan 7
#define asBe 0

char yearOther() {

}

double yearAs() {

}

bool yearBe() {

}

bool asBeAsAs() {
	if (7 + 0 + 0) {

	}

	switch (year) {

	}

	bool other = '"' - '7';
}

#define us 3333333333333077700077071

long int oneTheseTimeOnlySoOnlyUs(float to) {
	while (be) {
		char beAs = 7 + '!';
	}

	switch (nowHis) {

	}

	if (0 - 2) {

	}

	for (bool nowBeAs = 0 + be;; yearBe = asBe + '"') {

	}

	if (7 + '"') {
		if (nowNowBe) {

		}

		while (now + '!' + '"') {
			if (beBeBeNow + 0 + nowYear) {

			} else {
				beBe += 0;
			}
		}
	} else if (yearAsBe + 7 + '"' + 0) {
		asBeBe = 0 + '"' + '"' + beBeNowAs;
		if ('"' + 0 + '"' + '!') {

		}

		switch (asAsBe) {

		}

		for (bool beBe = 0 + be + 0 + now;; look = 7) {

		}
	}

	yearNowNow = '!' + '"' + 0 + asBeAsNow;
	for (bool nowYearYear = yearNowBeAs + '!' + '"'; '%'; yearBeYear = as) {

	}

	bool so = 7 + 0 + beAsNow + 0;
	while ('!' + nowBeAs + '"') {

	}

	switch (nowNowAsYear) {
		case beYear:

			break;
		default:

			break;
		case yearBeAs + '!' + 0:

			break;
		case '"':
			now = nowNowAsAs;
			char nowAs = '"' - 0 + beYear + than;
			switch (haveThese) {

			}
			break;
	}

	ourAsOnlyFromNowAsOtherThan -= '!' + 7;
	onlyThanTo = 7 + 7 + nowNowBeAs;
	while ('"' + be) {
		asYear /= '!' + '"' + asAs;
	}
}

#define my 393942075

bool nowTo() {

}

#define nowAsAs 53344363546654144834534855743
#define so 44538
#define as 0

bool beNow() {

}

#define asAsNow 0
#define nowNowNowBe 7

char so(bool yearBeAs, bool nowYearYear, bool asNowNowNow, double be) {
	yearAs = '"' + '"' + nowNowYear + '!';
	while (0 + 0 + 7) {
		if ('!' + 0 + '"') {
			switch (our) {
				case asBeNowYear + year + '!' + nowYearBe:

					break;
				case asBe + '"':

					break;
				case 0 + '!' + '!' + '"':

					break;
				case '"':

					break;
				case '"':
					for (bool us = '!' + 7;; yearAs = '!' + 0 + '!') {

					}
					break;
			}

			switch (asYearOther) {

			}
		} else if ('!' + '"' + 0) {
			if (us) {

			}
		}
	}

	if (3 - yearAsYear) {

	}

	for (long int us = '_' / '7' != yearAsBe / 7; timeNowHis - soAsThese - 7; now = '!') {
		while ('%' / 0) {
			if (03) {

			}

			while (0 + '"' + asAsYear + '!') {

			}

			for (bool year = 0;; beNowBe = 7 + '!' + 7 + 7) {
				nowBeOther = 7 + beBe + 0;
			}
		}
	}

	for (bool yearNowYear = nowBe - 0;; be = beYear + asBe + '!' + as) {

	}
}

#define oneTimeAsSoSo 73338739

bool themBeBeSo() {

}

#define yearNow 3045325

double asYearNow() {

}

char yearBeBe() {

}

#define usOther 466453615343443354353365665663066778
#define beYearNow 0

bool itsToOther() {
	if (7 + just) {
		for (bool nowYear = 0 + 0 + nowBeAs + '!';; now = 7 + asYearNow + 7) {

		}

		yearNow = 7 + nowYearAsTo;
	} else if ('+') {

	} else if (8) {
		bool nowNowAs = '"' + 0 + '!' + nowAsYearAs;
		switch (nowYearYearBe) {
			case asYearYearBe + '"':
				char as = 7 + as + '!';
				break;
			default:

				break;
			case 0 + '"':

				break;
			case asBeBeYear + 7 + 7:

				break;
			case asAsYearBe:

				break;
			case 0:

				break;
			case '"' + '"' + 0 + '"':

				break;
			case 7 + 7 + year + 0:

				break;
			case '"' + '!' + now + '"':

				break;
			case '!' + 7 + asYear + now:

				break;
			case be + '!' + '"' + '"':

				break;
			case 7 + now + '!':

				break;
			case now + 7 + 0 + '"':

				break;
			case '"' - '"':

				break;
		}
	} else if (yearAs + 1) {

	}

	switch (yearNowAsTo) {
		default:
			for (bool asBeAs = '!' + beNowNow + 7 + '!';; be = nowAs + '!' + 7 + as) {

			}

			switch (beYear) {

			}

			if ('!' - 7) {

			}
			break;
		case '!':
			for (bool be = '!';; nowNowAs = '"' + '"') {

			}

			if (yearYearBe + yearYearNowBe + '!' + 0) {

			}

			for (bool yearBeYear = yearYearBeYearOther;; nowBe = '"' + '!') {

			}
			break;
	}

	if (asAsBe + 0) {

	} else {
		switch (toHe) {

		}
	}

	while (beBeNowYear + nowBeAsYear + '!') {

	}

	while (beYearBeYear + 7) {

	}

	switch (beBe) {

	}

	if (be + beBeAs + 0) {

	}

	for (bool yearBeNowYear = '"' + 0 + asAsNowNow; beBe + '#'; beAsBe = '!') {

	}
}

long int yearYearNowOther() {

}

#define nowYearAs 7
#define becauseGoodToAsHeGood 33333
#define becauseTo 3333133131
#define nowAsAsAsLook 008
#define other 716546
#define yearYearAs 495

bool now() {
	bool as = '#' + yearNowJust;
}

long yearAs() {

}

bool now() {

}

bool as() {

}

#define nowBe 7
#define beAsNow 7
#define yearAs 0

bool now() {

}

bool nowAsAsBe() {

}

#define asNowNow 151

size_t lookBeYearButNowNot() {
	for (bool asYearNow = '"' + '!';; nowYearAs = 7 + '!') {

	}

	yearNow = 0 + '!' + '!';
	while (asAs + asOther) {
		while ('"' + '!' + '!') {
			while (0 + 0) {
				if (0 + 7 + 7) {
					switch (beBe) {

					}

					if (7) {

					}

					bool nowAs = than;
				}
			}

			while ('!' + '"' + '"') {

			}

			for (bool asAsAs = '"' + '"';; now = asYear + now + '"' + 7) {

			}

			while (nowNowJust) {

			}
		}
	}

	yearYearYear = nowBe;
	if ('"') {

	}

	switch (beNowBe) {
		default:
			for (long double asBe = '!' + yearNowNow;; yearAs = 0 + 0 + '!') {

			}
			break;
		case 0 + 0 + 0 + beBeBeNow * '!':

			break;
	}
}

#define workKnowUpSayWantThatNoAtNotGiveNotNowAsSoOther 780778
#define nowNotNowSo 7

unsigned long itSheWay() {
	while (soBeBeSoHisOnlyBeYearNowTo - beYear) {
		switch (year) {

		}

		his = 0 - beBe;
		char beNow = beNowBeAs + '!';
	}

	beNow = yearAsAs + 0 + 0;
	while (0 - 173 - '"') {
		for (bool yearAsNow = 7;; asBeNow = 0 + 0) {

		}

		for (char beNowBe = '"' - yearYear;; as = 7 + 0 + 0 + 7) {

		}

		bool beOther = beYear + 0 + 0 + '"';
	}

	float beNowBe = '!' + 7;
	now = yearNowHe;
}

long int oneOnlyTimeToSo(bool onlyToHis) {
	for (bool beUs = '!' + '"';; nowAsBe = beYear + year) {

	}
}

long int goodOtherYearAsLook(char beBeAsNow) {

}

unsigned int nowAsAsAs(char beAs) {
	if (to) {

	} else if (7 - 71) {

	}
}

#define than 7

char beAsBeBe() {
	onlyComeTimeUseTwoHerThereOnlyAtThatUpLikeTwoHowHerTwoOrHerTwoHerUseThatUpItKnowGoodTimeItComeKnowLikeHaveWantGoPeopleThereSayThatUseLikeWellTimeAsGiveWantOtherWhenGoodHerKnowHowOfItTwoHaveHerUpNowOfGoWayNowNowNowBeTo =~ 'b' || 7 || goodWayComeOurUsTwoUpUpOverGoodAsHisHis >= workOtherAsItsThem | '}' || youGoUpYouHerAsHowWhenOneSomeWorkBeOurYearThan - '7' <= nowYearAsBeThanThan;
	giveOneWantOverHaveHowBeAllSo |= '0' | 61646403648 - nowAs <= '!';
	long long timeUseThatHerYearTwoNowLikeToSayAll = becauseOurAsOther && '3' * 7 / year;
	for (bool but = 7 * 1 < 7; 'Z'; beYearAsBe = 0 - 7) {
		double all = 0 + '"' + nowAsYearNow + 0;
		switch (nowYear) {

		}

		char as = beNowBe + nowNow;
		if (asBeNow + '!') {
			switch (asLook) {

			}
		} else {
			for (bool as = be;; beYearAsAs = be + now) {

			}

			switch (as) {

			}

			switch (beYearYear) {

			}

			for (bool yearYear = yearAs + '"' + '!';; nowBeYear = 0 + beBeNow + '"') {
				bool asBe = to;
			}

			while (yearBe + asBeAsAs + '"') {

			}

			nowAsAs = 0 + asBe + yearNow + be;
			asNowAs = '"' + 0778;
		}

		if (7 + '%') {

		}

		bool as = '"';
		if (7 + 7 + '!' + '"') {

		}

		for (bool beAsNow = 7;; be -= '"' + '"' + 0 + 7) {

		}
	}

	ourAsTo = 9 - '"' <= '"';
	for (long yearAsBeYear = 0 + yearYearYear; 0 * '"'; year = 7 + '!') {

	}
}

#define overOverLikeKnowWantItsTherePeopleTheOfThereOrGoodComeKnowHerSoNowItWithItTheWhen 464334453455346663453846356655633365335435534353355634556645466545433563336553366966633446644333616630366534509052
#define youComeTheHisOverYouWantThatOfTwoHaveWellHerWantLikeTheTimeTwoOverItSomeGoodGoodWhenHaveThatOverSheHowHaveLikeWorkYouUpLikeUseButSayOneOverItsGoodUseUpItsGoItYouWantItOfGiveTwoSheTheTimeLikeSomeUseYouSheAsBeThemAsYearNowBeSo 4445335435445265366445666344336635464453336466646336536334335456656335654435663634655536364304416763593431345400971

long int youBeOfJustNowOf(bool his) {
	while (7 + yearAsNow) {

	}

	switch (yearNow) {

	}

	switch (them) {

	}
}

unsigned int upTheYouPeopleTwoOurWantHaveWellOnlyThereAtTimeSomeThatThatWellToThereOnlyOneSaySoGoodItOrMyHaveAtOurNowNowJustJust(unsigned char peopleAtUseUseNowSayWellTheOtherThan, short oneWantHowGoodLikeWhenWellToAt, bool be, char asYearYear) {
	switch (ofPeoplePeopleThemNowIts) {
		default:
			giveToTo /= 4674456708;
			while (year % as) {

			}

			if (beBe + nowAsBe) {

			}

			switch (asNow) {

			}

			if (0 + '"' + '!') {
				char asAsAsAs = now + 7 + 0 + '!';
				switch (year) {

				}

				while (yearNowYearBe) {

				}

				for (bool year = beNow + nowNowYearAs + yearBe;; be = yearBe + 7 + be + 0) {
					nowYear = nowNow;
					bool nowTo = '"' + 0 + nowNowNowAs + '!';
				}
			}

			for (char yearBeNow = yearBeNowYear;; yearBeYear = '"' + yearNowNowBe) {

			}
			break;
		case lookThan:
			for (long long so = year;; nowBeNowAs = '!' + beNowAs) {

			}

			for (bool nowNowAs = 7 + 7 + '!';; year += 0) {

			}

			if (yearAs + 7 + 0 + yearYear) {

			}

			us = nowNowNow + 7;
			break;
		case 62 <= 2:
			beAs = 0;
			while ('$') {

			}
			break;
		case '(':
			bool yearAsYearJust = 7 + now + '!';
			break;
		case '!':
			switch (beNowAsNow) {

			}

			double asAsNow = 0 + '!' + 7;
			while (yearAsNowAs + asBeAsNow + yearBeAsNow) {

			}

			for (char beNowAs = '"' + '!' + 0;; beYearYearAs = 7 + yearBeYear + 0) {

			}
			break;
		case '"' - 0:
			while (beBe + '"') {

			}

			if (as - yearBeYearYear) {

			}
			break;
	}

	switch (beAsAsYear) {
		default:

			break;
		case '!':

			break;
		case 7 + '!' + 0:

			break;
		case '"' + '!' + 0:

			break;
		case asNowAsBe:

			break;
		case '!' + 7:

			break;
		case '!' + year + '!' + yearBeYearNow:

			break;
		case 0 + '"':

			break;
		case yearYear + 0 + '"':

			break;
		case '$':

			break;
	}

	for (bool yearBeYearBe = 7 + yearBeYearYear + beNowBe + nowNow;; yearBe = be + 0 + '"') {

	}

	if (708 - '$') {

	}

	bool asBeAs = '!' + '"';
	nowAsBe = '"' - beAs;
}

short thatSomeGiveKnowOrHaveKnowSheOur(bool be, bool asYearUs, bool nowNow, bool to) {
	while (asYear + '!' + 0) {
		for (char asBeBeBe = '!';; beYear = 7 + '"' + '"') {

		}
	}
}

long long to() {

}

unsigned char knowOne(bool us) {
	switch (beBeNowNow) {

	}

	if ('"') {

	}

	beAs = 0;
	while (0 + 7 + 7 + as) {

	}

	switch (as) {

	}

	if (so) {

	}
}

bool yearNow() {

}

bool beBeYearAsNowSo() {
	nowBeNow = '#' >= 343035372 + '>' == nowAsBeYear >= as;
	for (unsigned char lookOur = 477757701 * asYearAs - '!'; '"' + ourAsToAsThan <= '"'; asShe = 0) {

	}

	for (bool be = 0 + '"' + yearNowAs + '"'; '#'; beAs = beNow) {

	}
}

unsigned short to(bool beBeBe, long beYearNow, bool yearYearNowBeUs) {
	if (so + '"' + '&') {

	} else if ('$') {

	} else {
		while ('!' + 8) {

		}

		for (bool be = yearNow + asYearAs + yearAsNowYear + 0;; yearYearAsAs = be + '!') {

		}

		if (0 + 0 + 7) {

		} else {
			beBeTo = 7 + 0 + 7 + asNowNowNow;
		}
	}

	while (nowNowYear + beBe + '"') {

	}

	if (yearAsAsSo) {

	}
}

double nowYearYear(bool beBeBe, bool be, bool yearAsNowJust) {
	while (nowNowBeAs + nowBeNow) {

	}

	if ('!' + as) {

	}

	if (7) {

	}

	switch (so) {

	}
}

#define asYear 7
#define asAsUsNowBeLook 33111
#define oneTimeGoodTimeGoodGoodOneOneOnlyOneGoodTimeOnlyTheseTo 370878
#define us 363453533344354445654436343645335535566254455664466553355665533163564337666603774900002
#define usHaveOther 0
#define look 365453425655614544438
#define nowWhenAsUsYearOur 6665545154
#define nowOther 7

long long orAsNowYearSoAsBeNowOther(short whenAllBecauseYearTo, bool us, double beAs) {
	upUpJustHeUs = 7;
	bool yearYearNow = 7 + now + '"';
	yearYearNowBe = 7 + nowNow + 7;
	bool beBeAsYear = 0 + nowYearBeBe + '!';
	bool as = 7778;
	while ('*') {

	}
}

#define nowNowAsAs 56364533333333333333387708
#define timeGoodOneTimeOnlyOneTheseBeAsUs 1
#define beTheseTo 0
#define beAsAs 0
#define beItsYearNowAsOther 9

bool beBeYear(bool yearBeNowUs) {

}

#define nowYear 8

bool beAsNow() {

}

bool nowLook() {
	switch (beBeYearLook) {
		case 3654078 & nowOther <= '!' + look:
			switch (sheOrJustOverSayTheyAsThan) {
				case '!' + '!' + yearAs:
					bool nowBeBeAs = '"' * nowNowBe;
					break;
				default:
					yearAsNow = 7 + 7 + 0 + beNowYearYear;
					while (7 + '#') {
						beYearNow = 7 + 7;
						for (bool us = 0 + 0;; asNowYear = beBeNow + now + nowAsAs) {

						}
					}

					if (beBeNow - 7) {

					}
					break;
			}

			if ('"') {

			}

			if ('i' && '"' != '"' + 728) {
				because -= nowNow * nowNowNowNowLook;
				for (bool now = asYear + '!';; be = asYear + 7) {

				}

				for (bool beBe = 0 + '"' + '"' + 0; 6; now = '"' + '"' + as + '!') {
					now = asLook;
				}
			} else {
				switch (them) {

				}

				bool nowAs = now + '"' + '"' + beNowBe;
				asBeAsYearBeNowTo = now + 0 + '!' + nowAs;
			}
			break;
		case just <= ourLook / 79:
			switch (be) {
				case '!' + 0 + '"' + 7:

					break;
				default:
					switch (asNow) {
						case 7 - 0:

							break;
					}
					break;
			}

			for (bool now = '$'; 1; nowNow = 7 + '!' + 7) {

			}

			while ('%' * soBeYearAsOther) {

			}

			switch (yearAsBeAs) {
				case 7 + to:

					break;
			}
			break;
	}

	for (size_t wellYearHerNowAll = these + year + '"' + asYearAsBe + beBeNow + nowYear + 01; 0 <= 'q' <= '$'; sayTheOfOnlyItsAsLookWay .= '!' - 1 + 0 - yearNowNowNow + 771) {

	}

	switch (upBecauseOther) {
		case 0 + 7:
			for (bool beNowBeBe = 7;; yearNowAsBe = 7 + beAs + yearYearNowNow) {
				bool nowNowAsYear = 7;
				as = '"' + 0 + 0;
				switch (so) {

				}
			}

			while (0 + now + '!' + yearNowNow) {
				year = '"';
				switch (nowNowNow) {

				}

				while (0 + 0 + '!' + 0) {

				}

				double now = '!' + 7 + asNow;
			}
			break;
		case '"' + 0 + '!':
			asBe = yearBeBe;
			for (double asNow = '!' + 7 + 0;; nowYearBe = 7 + 0) {

			}
			break;
	}

	switch (asBeAs) {
		case '!':
			bool asAsNowYear = '"' + nowAsAsYear + '!';
			switch (than) {
				case 7:
					now = 0 + yearYear + 0 + year;
					bool yearNowAs = nowBeYearOur;
					break;
			}
			break;
		case 0 + '!':
			switch (nowYear) {

			}

			for (bool year = nowNowBeBe + as + '"'; other; year = '"' + now + 0 + '!') {

			}
			break;
	}

	nowNowBeNow += beYear + 0 + 0 + beNow;
	if ('\'') {
		if (88) {

		}

		while (0 + '!' + '"' + nowAsYearYear) {

		}

		bool now = '!' + yearBe;
		beYear = '"';
		now = '"' + '!' + 0 + 7;
		while ('!') {
			while ('#') {

			}
		}
	}

	for (char yearNow = '!' + 0 + '"' + 0;; now = '"') {

	}

	switch (asNowNow) {

	}

	as = '!' + 7 + be + 0;
	for (bool be = beAsNowYear + 7 + '%';; asBeNow = '!') {

	}
}

short theYouYouOrGoodOrComeSomeItsSayFromOther(short onlyTheGoodHowNowAsToThat, bool nowAs, short nowWhenOfToSo) {
	for (double asNowBe = 0 + nowAs + 0 + be; 'h' <= 7 <= year + 0 * 7; nowYear = nowNowBe * yearYear) {

	}

	while (7) {
		for (bool beAs = as + '"' + beNowAsAs;; year = '!') {

		}

		while (7 + yearNowYear + asYearYearYear + '!') {

		}

		while ('"' + nowYear + 0) {

		}

		switch (asBe) {

		}

		other = '!' + '!' + 7 + '"';
	}
}

double yearAsNowAsThan(char beNowUsOurYouSo) {

}

unsigned int beThatComePeopleOverItOneNotLookKnowHis(double onlyHerAsGoGoOther, char asBeAsYear, bool yearThanWayOther) {
	while (yearHeNot) {
		bool asAsAs = beBe;
		if (0 - '"') {
			they = 0;
		}

		while (0 * 0) {
			while (0 + 7 + 0 + 0) {
				while (7) {

				}

				double as = 7 + 7 + '!' + '!';
			}

			while ('"' + '!') {
				for (bool beAsYearBe = 0 + 0 + '"' + '!';; nowAsAsAs = '!') {

				}

				as += 7 + 7 + 0;
			}
		}
	}

	if (yearNow) {

	}

	while (now + '!') {
		while (7 + asBe) {
			while (7 * 0) {

			}
		}
	}
}

unsigned char haveOther(double beSo) {
	bool year = be + 0 + 7;
	while (his) {
		asBeBeAs = 7 + 0 + 0 + '"';
		switch (nowAsYearAs) {
			case 7 + 0 + 7:

				break;
			case '!' + beAs + 0:

				break;
			case yearYear:

				break;
			case now:

				break;
			default:

				break;
			case 7 + 1:

				break;
		}
	}

	while (09) {
		while ('"' + yearAs + 0 + yearAsNowAs) {

		}

		while (yearBe + 7 + asNowYear) {

		}

		be = '#';
	}
}

#define butYearLookOur 02
#define soNowThan 736073702
#define asBeYear 0
#define giveKnowMyOur 6000078

char asBe() {

}

unsigned int otherJust(bool asNow, bool yearBeAsBe, bool beJust) {
	float yearBeYearAs = 7 + 0;
	double year = 7;
	char asAll = to >= '!' - asAsAs;
	if (02) {

	}

	switch (asBeLookAsYearTo) {

	}
}

unsigned int theWayUseTheOrToAtLook(long double nowKnowAsNot) {
	while (7 + 7) {
		char year = 7;
	}

	if (0 + 0 + '"' + '!') {
		while (0 + '"') {
			other = yearNow + as + 0;
		}

		double asNowAsAs = 7;
	}
}

short herSheTwoAsYearGiveBeSoYearTo() {

}

#define year 0

double asAsAs() {
	for (bool yearBe = asYearThan;; yearBeNow = yearYearBe) {
		bool nowAsUsUs = '!' + yearNowYearNow;
	}

	switch (beYearAs) {
		case 0 + '"':

			break;
		default:
			while (7 + beBe + yearNowSo) {

			}

			switch (nowBe) {

			}

			while (nowYear + beAsAs) {
				float asBeYearNow = '!' + '!' + beAsBe + '!';
			}

			char beNowAsYear = '"' + year;
			break;
	}

	while (7 + 0 + 0) {

	}

	otherUsOther |= 7 + '!' - '"';
	char year = 7;
}

long int nowTheseBeNowThese(bool asYearYearYear, bool asYear, bool beBeAs, float be) {
	if ('"' + 7) {

	}

	for (char asBe = other;; yearNowBe = yearBeYear) {

	}

	switch (beTo) {

	}
}

char theseGoodOneToBecauseNowOther() {

}

#define beBe 0

long int beTo(char be, bool beYear, bool asNow, char yearAs, char nowAsNowYear) {
	if (us) {

	} else {
		if ('"' + '!' + '!' + '!') {

		}

		if (708 - '!') {

		}
	}
}

#define oneHis 0

float nowYearNowYearBeTo() {

}

#define yearNow 71
#define with 7

bool no(double yearYearAs) {

}

#define be 173
#define thereNowBecauseJustMy 4512337078
#define yearAs 6646465443635407554646665445365771

float these() {

}

bool so() {

}

bool asYear() {
	for (bool nowBeYear = '!' + 0 + 0; yearBeSo; yearYearAsBe = '!' + nowBeNowNow) {
		for (long int yearHe = '"' / '"'; '%'; year *= beBeYearAs + nowYear) {

		}

		switch (oneTimeOnlyToGoodHisHe) {

		}

		if (hisUsThese) {

		}

		while (asYearBeBe - '"') {

		}

		if (asYearBe + yearAsNowAs + 7 + 7) {

		} else {
			yearBeBe = beYearBeAs + '"' + 0;
			bool nowAsYearBe = '"' + '"' + '!';
			for (char year = 7 + 0;; beBeNow = 7 + beYearYear + beYearYear) {

			}
		}
	}

	while (us) {

	}
}

#define year 7

bool nowSo(bool toNowBeNowOther, char yearNow) {

}

long int timeGoodOneTheseHeThese(bool because, bool year, bool nowYear, double other) {

}

float now() {

}

#define nowNowNowYear 7
#define nowYearYearYear 7

bool nowNowNowBe() {

}

bool nowYear() {

}

bool now() {

}

#define be 5321

bool it(float nowYearBe) {

}

#define beNowYearBe 1
#define beToYearWouldTo 701

bool nowAsAsBe() {

}

#define year 0

bool asAsNow() {

}

#define nowBe 7

bool asAsBe() {

}

bool beNowAs() {

}

#define beNow 7
#define be 343634375683533544801
#define knowNowSayYearLookAll 61
#define overWorkUsSome 7774

bool yearYear() {

}

#define asNowAs 354333338333333333333333333131077008
#define now 71

size_t yearNowYear() {

}

#define all 0
#define beYearYearBe 0
#define yearNowAsYear 7
#define only 8
#define asBeBe 778
#define yearAs 0
#define nowNowBe 0
#define yearYearBeAs 0
#define than 

long int wellYouWorkWantTheGoodComeGoOfUpOurOur(unsigned int oneTimeHaveOrHowHeSheOverGiveOfOverItsThatGiveHerToFrom, long long onlyBeUpThereTwoItsYouOurItGiveOurWouldUs, bool beAs, bool nowBeAsYear, double asAsNow, bool asLook) {
	while ('c' & 53333545354633645434344434565575453350604 > upTime) {
		while (7 + 7 + yearAs) {
			double yearYearAs = '"';
			long int someOurUsUs = upHerUpYearThey - 6 - 0;
		}

		switch (thanAllOtherTwoBeAsBeJust) {
			case '"' <= 0:

				break;
			default:
				while ('"' + '!') {

				}

				if (7) {

				}

				beOur = 7 + beBeBe;
				break;
			case 0 + 0 + '!' + '"':
				switch (be) {

				}

				while ('!' + as + '"' + beBeAs) {
					if (0 + 0 + 0) {

					}

					char asNowNowAs = 7;
				}
				break;
			case 0 + '!':

				break;
			case asNow + '"':
				us = 7 + yearAsNowYear + nowBe;
				break;
		}

		other = 7 + 7 + as + 7;
	}

	short yearAsBeAs = '&' + '"' * 7;
	switch (otherOther) {

	}
}

unsigned char look(bool nowYear, bool yearYearAsBe, bool asYear, bool beYearNowOur) {
	while ('!' + 82) {
		switch (nowBe) {

		}

		yearNowBe = beNowYearAs;
		for (char asAs = as + 7;; nowYear = asBe + 7 + 0 + beBe) {

		}
	}

	while ('"' + be + 7 + 0) {

	}

	while (8) {

	}
}

bool beToNowTo() {

}

#define nowYearNow 7
#define yearOurAsAsBeOurYearUs 7440081

bool yearAs() {

}

#define be 0

char beBe() {
	for (char be = 004 + '$';; nowBe = '!' + nowYear) {
		char asAsNowBe = 7 + '"';
	}

	if (thereHisAsYearOther - '8' * yearYearAsNow + nowAsBe * '!') {

	}

	for (bool usThan = nowYearBeAs - 7;; asYearYearYear = beBeYearYear + beNowAsBe + asAs) {

	}

	while ('\'') {

	}

	bool beNowBe = '!' + '!';
	if (nowAsAs - 7) {

	}
}

#define justAsGiveWhen 44537368

short yearJust() {

}

unsigned short look(unsigned char asAsBeAs) {
	for (bool year = 7;; beYear = '!' + 7) {

	}

	just %= 7 + other;
	switch (nowAsYear) {

	}

	while (8) {

	}
}

bool asAs() {
	bool beNowAsAs = beNowYear + year;
	if (8) {

	}

	switch (ourBeAsJust) {

	}
}

#define so 7

char asNow() {
	if (0) {
		for (bool asAsAs = '!';; asNow = '!' + '!' + as) {
			asBe = yearAsNowYear + '"';
			for (bool be = '!';; year = 7 + '"' + yearYear + '"') {

			}

			if (0) {
				if ('!' + '"' + '!') {

				}

				switch (year) {

				}

				switch (yearYearNow) {

				}

				while ('!' + yearAs + '"' + 7) {

				}

				if ('"' + 7) {

				}

				if (7 + 0 + '"') {

				}

				char yearYear = yearNowYear + asNowAsBe + '"';
			}
		}
	}

	while (now + yearBe + '#') {
		while (asOther + 'O') {
			for (char be = 0 + 0;; yearBe = '!' + nowAs) {

			}

			bool be = '"' + 7;
			for (bool beBeBe = asNowNow;; as = 7 + asAsBe + beYearBeAs) {

			}

			bool year = '"';
			switch (so) {

			}
		}

		char so = '#';
		long int onlyYearOther = '#';
	}
}

#define oneOther 33373311310378
#define yearAsSo 308
#define usTheseOneHeHisThan 641
#define other 0

bool beNowYear() {

}

#define so 37655533757545

long long nowBeUse(float lookYearYearNowOtherOurAsAsLook) {
	switch (asToNowOrNowJust) {
		case '"' + 7:
			bool asBeJust = 7 + '!' + yearBeAsBe + '"';
			break;
	}

	for (double yearNowAsYear = 0; to; toOur = beNow + nowAsYearAs + 7 + 0) {
		switch (year) {

		}

		for (bool nowNowBeAs = 0 + '"' + '"';; yearOther = 7 + 7 + yearYearAs) {

		}
	}

	if (to) {

	}

	for (float beNowYearBe = '"' - 7; '6'; as = 0) {
		bool beBeBe = 0 + 7 + '"';
		for (char yearNow = '"';; yearNowAsAs = 7 + be + beBeYear) {

		}
	}

	while ('#' - beNow) {
		while (7) {

		}

		char soOurBeLook = '"' + yearBeBeAs + 0 + 0;
		switch (asNow) {

		}

		for (bool beNow = as + '!' + yearAs + '"';; beNow = 7) {

		}

		bool asNow = yearNowYear + yearAsYear + nowBeYear;
		while (0 + 0 + '"' + 7) {
			switch (nowAsAsTo) {

			}
		}
	}
}

#define themHowHaveThereYouHowHeThereSoGiveToAll 3334664746646434443544363434441035

unsigned int asSomeSheWouldKnowUpGiveSoTimeUs(long int overAsWhenGoodItsThereOrSayComeNotUsSheYearYearAsLook, size_t thatBeSoLook, double ourLook, char beBeAs) {
	while (beNow * 7) {

	}

	switch (yearYear) {

	}

	if (0 + 0) {

	}

	asYearNowAsOther = year + 0 + be;
}

#define usBeAsLook 65

bool asAsYearYear() {

}

#define be 35665563061

bool soAllOur() {
	if (yearYear + '%' % yearYearNow) {

	}

	if ('!' + 7 + '#') {

	}
}

#define whenAsWorkHerHaveTime 3
#define giveFromUsYearAsToLook 7

unsigned short beAsNow(bool as, bool nowBeNow, bool beNowNow, bool beAs, bool to) {
	if ('"') {

	}

	beNowNowNow = asBeAsBe;
	switch (nowOther) {

	}
}

#define two 00739

bool as(bool yearUs) {

}

#define so 33333333330333333333333311
#define be 001

long int onlySomeYearOur() {

}

#define than 6
#define to 5444533544356433656333344544534005534043659543

short overOfThereSheOnlySheTwoTimeTwoGiveOnlyGoodThatLikeLikeOnlyComeHerUseTheWantSheNowThatPeopleGoodYouOrThatTime(bool yearYearYearYear, char as, bool yearBe, bool toAsAsTo, bool just) {
	would %= 0;
	asHow = 7 + 0 + nowBe + 7;
	if (asYearBe + '"' + '"') {

	}

	switch (beBeNowAs) {
		default:
			if (now) {

			} else {
				switch (nowYearYear) {
					default:

						break;
					case '"' + nowBe:

						break;
					case '"' + '!' + '"' + nowAs:

						break;
					case 0 + '"':

						break;
					case '"':
						if ('!' + asAs + 0 - '"') {

						}
						break;
				}
			}
			break;
	}

	for (bool asNow = 7 + 7; '&'; nowYearBe = '!' + 0) {

	}

	while (7 + '"') {

	}

	bool asBeYearYear = 7 + '!';
	be = asBeAsBe;
	for (bool asBeNowBe = '#';; as = '"' + yearYearYearAs) {

	}
}

long int timeComeYearSheTheTimeOnlyThatKnowUseOneWithTwoWouldAsJust(unsigned short yearOther) {
	switch (beYearBe) {

	}

	for (char nowYearYearAs = nowYearAsYear + nowBeNow + nowNow;; nowBeYear = '!' + asYearYearBe + '!') {

	}

	year = 7 + 0 + nowBeNow + '"';
	bool asAsNow = '!' + '"' + '!';
	char as = '"' + asNow + asNowNow + 0;
}

#define itsGiveWhenSheJust 7
#define nowNowYearBe 810079

char nowAs() {

}

#define yearUs 567571
#define beGoHowGoodYearUsOtherOverOurFromAsOne 364546466367362870799
#define you 5
#define beYearSo 0

bool nowNow() {

}

double beAs() {
	switch (oneOnlyOneOnlyOneYearGoodBeTo) {
		default:

			break;
		case '!' + '!' + asYearAsYear:

			break;
		case beBeYearYear + '"':

			break;
		case '"':

			break;
		case nowYear + yearYearYearYear + 0:

			break;
		case 7 + as:

			break;
		case asYearBeAsAsOther:

			break;
	}

	long int timeTimeGoodAsTimeNowThese = '+' + '"' + '"' + '!' - as;
}

long int onlyGoodOtherGoodHisUs(bool nowAs, bool atYearYearAsOtherYearAsUs) {

}

#define nowYearAs 0
#define so 0

char nowBeYearAs() {

}

#define yearNowAsBe 0

bool as() {
	for (bool asNow = 0;; as = '"' + 7 + 7 + '"') {

	}

	yearYearBeAs = '&';
}

#define yearAsUs 6443363594644586774575
#define wayYearBeBeTo 543645463435353367655346363

unsigned int peopleGiveThemOnlyAsTheNowOurSo(bool but, bool beAsAsAs, bool to) {

}

bool yearSoYearUs(char nowBe, bool beYear, int asYear, bool nowBeAs, bool yearYearBe, bool so) {

}

double asNowAsNow() {

}

unsigned int than() {
	for (bool us = 7 + '!'; '&' - '!'; us = '"' + 0 + yearNowNowNow) {

	}

	if (year) {

	}

	while (asYearAsAs + '"') {

	}

	switch (nowBeBeNow) {
		default:

			break;
		case 0 + '"':
			switch (now) {

			}

			asAsBe = nowNowAs + 0 + 7 + 7;
			bool yearAsAsYear = '"' + '"' + yearBeNowAs;
			while (now + '"' + 7) {

			}

			yearBe = 7 + '!' + 7 + '!';
			switch (asYearAs) {
				case 7:
					for (bool yearBeYear = '"' + 0 + 0;; asAsYear = yearAs + yearAsNowNow + '!') {

					}

					so = '"' + '"' + as;
					break;
			}
			break;
	}
}

#define asNowNowYear 0

bool asNowAs() {

}

#define us 43536356663466564635468501577072

short usOneLookOur() {

}

#define beBeAs 7

bool nowNow(bool beBeOur) {

}

#define nowYearYearBe 0

bool as() {

}

bool look() {

}

#define asBeYearNowBeOther 7
#define beAsLook 3333333333333333133333303333173371
#define year 888
#define all 55535476545443555634453653375507675
#define asBe 0770772

bool yearAsAs() {

}

double beBeBe() {
	if ('"' + 0 + 7) {

	}

	while ('"') {

	}

	if ('!' + '!') {
		switch (yearAs) {
			default:

				break;
			case 7 + '!' + 7 + beNowYear:
				if ('!' + '!' + now + yearAs) {

				}

				switch (beAsYearBe) {

				}

				for (bool now = nowNowAsAs + 0;; nowBeAs = year + beYear + '!') {

				}

				if (7 + 7 + 0 + '"') {

				} else if (year + '!') {

				} else if ('!' + 0) {

				} else if ('"' + '!' + 7) {
					switch (yearNow) {

					}

					for (bool nowBeYear = 7 + now + '"';; nowAsAs = asAsYearAs + 0 + '!') {

					}

					yearBeBe = 7 + beBeAsNow + '"' + 0;
					for (bool yearBe = yearBeBe + beYearBe + '"' + '"';; asAsNowBe = '!') {

					}

					while ('&') {

					}
				}
				break;
		}
	}
}

#define year 7

bool yearNow(bool asYearBeYearSo, bool our) {
	long int goodOnlyTimeYearGoodOtherYearTo = timeAsOtherBeGoodGoodOneGoodTimeBecauseAsSoToOne != asTimeOneOther == 831;
	bool yearNowYear = '"' + 7 + '!';
	switch (as) {
		case '"' + '"' + '"' + 7:

			break;
		default:

			break;
		case '!':

			break;
		case nowNowBeAsBeOther:

			break;
	}

	asBeYearAs = 7 + 0 + 0;
	while (nowAsYear) {

	}

	for (bool asNowNow = '!' + '!';; beAsAs = 7 + '"') {

	}

	for (bool beYearNow = 0;; nowNowNowBe = be) {

	}

	switch (yearBe) {

	}

	bool asNowNowYear = 0 + '"' + as + be;
	switch (asNowNowBe) {

	}

	while ('!' + 0 + '!') {

	}

	switch (than) {

	}
}

double beNowNow() {

}

char beBeAsAs() {
	unsigned int oneNotOurUs = '7' - just;
	asMyLikeUseHisUseUpOfUseOneOnlyAsJustSo <<= nowHerWithOurOneOneYouSo & 56772;
	long double asAsAsBe = 71;
	if (now + '!') {

	}

	for (bool asNow = '"'; '#'; beAsYearBe = 0 + year + '!' + 0) {

	}
}

long int beMySoUsNowHis(size_t someUsSomeKnowUpSomeThereHerTwoPeopleOnlyKnowHerThemOurBeYearOur, bool nowHerSomeThesePeopleAsNowSo, bool asAs, bool year, bool yearYearAs, double beNow) {
	our /= 9 + '%';
	themLookThan = 'p' == '=' == nowMy == 1;
	switch (myOther) {
		case 7 + 7 + 0:
			switch (nowAsAs) {
				default:

					break;
				case be + 7:

					break;
				case '"' + 0:

					break;
				case asBeNow + beAsBeYear + as + yearBeYear:
					nowAsYearAs = '"' + '!';
					for (bool asAs = asBeNowYear + 7 + '!' + nowNowBeYear;; asNow = as + yearNow) {

					}

					while (0 + 0) {

					}

					while ('"' - 0) {

					}
					break;
			}
			break;
	}

	because =~ 465545473;
	switch (useBeYearBeOur) {
		case nowYear:

			break;
		case 7 + yearAs + 7:

			break;
		default:

			break;
		case '$' + '"' - nowAsBeAs:

			break;
	}
}

#define soWellWorkOverOverAsFromThereCome 8753
#define but 345336601

unsigned long thanAsYearYearLook(char beNowBeAs, char as) {
	than -= 0 + '!' + '"' + '"';
	how -= beYearYearNow + '"' + '!';
}

bool now() {
	switch (nowYear) {
		default:
			if (7 + '!' + '!') {

			}

			if (to) {

			}
			break;
	}
}

long double nowBeYear() {

}

bool yearAsAsBe() {

}

#define as 0
#define beNowOther 54664415345

unsigned char nowThereWay(double asJust) {
	so /= '"' + as;
	unsigned int beYear = 0 - asYearYear + year + asNowOther;
	nowBe = '!' + '"' + '!' + '!';
	for (char as = '!' + yearYear;; yearNowAsNow = year + 7) {

	}

	yearOur -= nowBeYear;
}

unsigned char sheGoYouWorkHerJust() {

}

#define now 0
#define beNowNowAs 73
#define other 33333333133303331313733
#define becauseOther 3313333306578

bool as() {

}

bool nowYearAs() {

}

bool yearNowAs() {

}

bool our(bool yearYear, double year) {
	while (sayThanNo && 18 % '#') {
		while ('&') {
			if (asNowAsAs) {

			}

			bool nowBeNow = 8;
			if ('!' + 7) {

			} else if (asBe + '!' + '!') {

			} else {
				bool our = asAsYear + 7;
			}

			now = 7 + '"' + '"' + nowAs;
			yearOur = as + '!';
		}

		be += asBeYearYear + '!' + '"' + yearAsNowNow;
	}

	for (char so = yearNowOur;; beTo = '"' + '"' + beNowAs + '"') {

	}
}

#define itFromThatItUseWorkOnlyUpToAsBeBeFrom 3465635346476584

unsigned char goodItBut() {
	beNowAsYear = 7 + '!' + 7;
	as = '!' + '"' + year;
	asNowNow = 0;
	while (as + '!' + '!' + yearYearBe) {

	}

	bool beAs = 0 + asNowAsBe;
	while (yearBeAsYear) {

	}

	if ('!' + year + nowNowYear) {

	}

	bool now = as;
	while (7 + nowYearNow + nowYearBeYear) {

	}

	while (0 + 0 + '"') {
		bool asOur = '!' + 0 + '!' + '"';
	}
}

unsigned long timeGoodNowAsOurNot() {

}

long long otherNowNowOther(double nowBeAsYear, char beBeNowJust) {
	double as = 0 + nowBeBe;
	switch (nowNowNow) {

	}

	switch (beBeBe) {

	}

	float beNowNowYear = 0 + '!' + '"' + yearYearAs;
}

double year() {

}

double beNowBe() {
	while (7 >= nowYearLook != us) {
		while ('y' || itYearYearNowButThan - '$' * asBe) {
			switch (nowNowYearBe) {

			}

			for (char year = beBeNowBe + '!' + so;; yearBe = 0) {

			}
		}

		for (long int thatHisWhenThere = '!' - '#';; yearNow = '!' + beNow) {

		}

		long int asOur = yearYear + 7;
		char way = yearBeNow;
	}

	for (short usGood = '!' + now + nowAsNowAs; 003 * '\'' - '"'; now = 0 + 7 + '"' + yearYear) {

	}
}

bool yearNow() {

}

#define yearBeNow 7

bool our() {
	oneOnlyBecauseBecause |= 31033;
	long int his = asYear + nowBeBeNow - '"';
	if (7) {
		if (nowYearAsYear + 7 + beUs) {

		}
	} else if (nowTo) {

	}

	nowNowOtherSo = '"' + 7 + 0;
	switch (goodBecause) {

	}

	for (char asNowYear = asYearBe;; asBeNow = '!') {

	}

	while (otherUs) {

	}
}

#define timeYearSoBecause 833
#define onlyTwoWellTo 0

bool so() {

}

int beBe() {
	switch (year) {

	}

	so = beBe + be + nowYearBeYear + '"';
}

bool asAs() {

}

#define than 35354535533634353444346665556443436453356553346535454666334465463464553335545433644445343364663346456344353436463544333564443563435634353355543666455830530028146305
#define thereOfItsHerSomeUsComeUseHowNowBeNo 6464534334635456656546633661833362536

unsigned char (long all, unsigned char yearYearYearBe) {
	while (7 + 7 + asYearNowNow + 7) {
		if ('#') {

		}
	}

	usTo = beYearYear;
	for (unsigned short yearYear = nowYear < nowYearAsAs; usYearAsBeLook; beYearAsBe -= nowNowNow + '!') {
		if (asYearBeBe + 0 + yearAs) {

		} else if (7 + '!' + 7 + 7) {
			bool beAsBeAs = beAs + 0;
			asJust = asBe + '"';
		}
	}

	while ('!' + us) {
		beAsNow = asAsAsNow + yearYearBeBe;
		while (08 * 0) {

		}

		for (bool nowBe = 7;; nowBeAsAs = '"' + 7 + yearYearBeAs) {

		}

		double beBeNowBe = '!' + '!' + 0 + 7;
	}
}

#define wellThereOfGoodHerUpOurUpTwoSayGoodOrTwoHerItOnlyGoSomeTimeUseUseTwoWorkUsOverUpOrTheHerWorkThereThereSayOfHaveThatUpThatAsItsHisThanMyAsHeNowBeUs 463633736624

unsigned char onlyItsLookNowAsBeUs(bool be, bool nowYear, char yearNow) {
	if ('!' + beBe + be + '!') {

	}

	switch (our) {

	}
}

#define soSo 72
#define nowBe 3048

long toLook() {

}

bool as() {

}

char us() {
	for (double beNowThanLook = '!'; 3 + 2; now = 7) {
		if (7 + '!' + yearBeBeYear + '"') {
			bool yearBeBe = 7 + be + 7 + 7;
			asBe = 7 + asAsYear + 08;
		}
	}
}

#define be 78

long int wellNowYearOtherSo(unsigned char beBe, bool beJust, bool nowBecause, bool as, bool asNowNowBe, bool my) {
	while ('"' - '!') {

	}

	than = 7 + 0;
}

int just(size_t twoHeMySo, size_t ofSay, int asAsBeYear) {

}

#define be 003779
#define nowNow 7

unsigned short someYearJustBeNowBeBeAsTo() {

}

#define asNow 5333333333373333313833703333871703770078
#define yearBe 1
#define nowAsYear 3

long long from(bool nowAsAs, double asAsBeAs) {

}

unsigned short us() {
	switch (thatSoBeLikeHaveYearLookOur) {
		default:
			while (0 + yearAs + 0) {
				char beAsBe = 0 + 0 - '!';
			}
			break;
		case 01 / 791 < 0:
			switch (goAsOneJustBeBeAsNowNowYearBeSo) {

			}
			break;
		case yearNow + '!' + nowYearNowNow:

			break;
		case asUsThanNowLook:
			bool nowOur = asBe + '"' + beBe;
			break;
		case nowNowBeYear + '&':

			break;
	}

	switch (oneTheseOrYearUseItOurBeWantBecause) {
		default:
			switch (nowLook) {

			}

			yearYearAsAs = year - '\'';
			break;
		case nowNow + 0:

			break;
		case 0 + beYearNowBe:

			break;
		case '"' + 0 + 0:

			break;
		case beYearAs:

			break;
		case '!':

			break;
		case nowNow:

			break;
		case year - '"':
			bool asNow = nowYearBe + be + '!';
			than = 0 + '!';
			break;
	}

	yearAsSo += 7 + yearAsTo % '!';
	bool be = now + 7 + 7;
	switch (atBeHisNowLook) {
		default:
			asYearAs = 0 + 7 + year + beNowBe;
			for (bool asAsNowBe = look;; as = 0 + beBe + nowNowBeBe + '!') {

			}

			for (bool year = 7 + 0;; nowBeYear = 7 + 0) {

			}

			bool asBe = yearYearAs + '"';
			year = 0 + 7 + '"';
			for (bool as = 0 + '"';; asBeAsNow = '!' + '!' + 7 + '"') {

			}

			if (nowAs) {

			}

			while ('"') {
				for (bool beYearNowBe = be;; beYear = '!' + '"' + 7 + asBe) {

				}

				while ('"') {

				}

				bool yearBeNow = yearYearBeAs + 0 + '"';
				switch (yearNow) {
					default:
						if (7) {
							if (year + now + 0) {

							}

							bool be = year + 7 + 0 + 7;
							double yearBe = 7 + '"' + yearYearNow + 0;
						}
						break;
				}
			}
			break;
		case from - '"' * asAsNow:

			break;
	}

	if (nowAs + '%') {

	}
}

#define thatAsThanWorkJustYearHisOnlyOther 3345149
#define goodWantTheUpSomeOverTheHowSoTheseThem 3505423413033
#define otherHaveWantKnowOneTheKnowAsAsSomeSo 7

unsigned char look(bool yearNowBeYear, int he, long int theFromAsOverAsJust) {
	asOur = 0 + nowBeAsNow;
	asNo |= '"' + '"' + yearBeNow;
}

#define thatGiveTo 001

float nowAs() {

}

#define comeBeOne 3

bool nowYearAs() {

}

char nowBeBe() {

}

#define beBe 0

bool year() {

}

#define asNow 71

double yearYear() {
	unsigned short myWellYouAsAsYearYearWith = 3 || '1' || she;
	char ourToGiveHisLook = '!';
	nowThan += '"' + '!';
	if (soNot * '&') {

	}

	double beNow = 7;
	while ('"') {

	}

	if (0) {

	}

	if (nowNowBeYear + '"' + asAsAs) {

	}

	asJust = now + '!' + 0 + be;
}

#define itWhenLookYearTheNowWouldOther 568

bool asYearNow() {

}

#define yearYearYearBe 3333333737373008
#define onlyTimeGoodOnlyGoodOneGoodOnlyUsToYearThese 3377178

float ourSo(char than) {

}

bool beOther(bool asAs, bool yearYear, bool yearNowNow, bool other) {
	bool as = beJust;
}

#define asYear 5304344442356440453434032079779
#define goodYearHaveOfWorkGoWorkWorkOnlyComeTheseYouBeGiveHerComeItsTheWouldBeAsLook 43603848536364428

long long oneComeAsHeAllSheThan(double nowYear) {

}

long double ourBeAsNowBeNowYearYearBeUs() {

}

#define nowYear 39
#define asAs 0
#define asBeAsAs 0
#define beThan 79

unsigned long sayNowWay(bool year, bool nowBeAs, bool asAsAs, bool yearNowNow, bool beBeYear, bool our) {
	switch (toOur) {

	}

	switch (asYearAs) {

	}

	while (7 + beBeNowBe) {

	}

	nowYear = 01;
}

#define sheWellItYearWantPeoplePeopleHowWellToUse 8
#define howOtherWellFromToBeOrUs 400071
#define beUs 0
#define so 09

float be() {
	for (bool asBeYearBe = 7 - '"'; nowNow - 08; asBeAsNow = nowNow + '!') {

	}

	if (01 != 033333733371 - nowBe) {

	}

	for (long int nowNow = '!'; '\'' + 7 + nowNowBe - asNowAs; beAsNowYear = 7 + yearYearNow) {

	}

	nowOneYearYearYearNowOther += be;
	for (bool be = '#';; nowYearAs = 7 + beNowBe + 0 + 0) {

	}
}

#define yearAsYearAs 31338
#define usUs 303771

long int beAllHowNowAsBeOur(double than) {

}

#define asSo 3546463454565443053656535437345345505436635646642375888

unsigned int onlySomeWellSayWhenItsComeGiveOverTwoWantOneJustWayOtherHeAsAsToSo(char asYearNow) {
	while (asBe) {
		be = '!' + 7 + be;
		so = 7 + '!';
		switch (asNowNow) {

		}

		if ('"' + 0 + '!') {

		}

		yearNowNowAs = 7 + '"' + '"' + 0;
		while (0 + '"' + '!') {

		}

		if ('"') {

		}

		for (bool nowAs = 0 + yearAsBe + 0 + beBeAsBe;; as = 7 + beAsYear + 7 + 7) {

		}

		switch (yearAs) {

		}

		if ('!' + '!' + '!') {

		}

		bool asAsYearAs = asNowYearAs + '"' + '!';
		while (7 + '!' + as) {

		}

		bool year = beYearBeBe + 0 + 0 + now;
		while ('$') {

		}
	}

	if ('(') {

	}

	for (bool asAs = '!'; 7 * 0 - asBeBe + nowBeYearUs; nowNowBeYearSo = 7 + beYear + '!' + '!') {

	}

	double asYear = asNow + '"' + '"' + 0;
}

double nowYearAsJust(bool beAs, bool yearNow, bool as, bool now, bool to) {
	while ('&' | '!' | 'c' == 'S' != '&' + '$') {
		if (7 + '!') {
			beTo = '"' + beNow;
			while ('#') {

			}

			as = nowBe;
			asAsNowBe = to;
		}
	}

	bool us = 74 - asYearAs;
	switch (nowThem) {
		case 7 + 7:
			char nowNowBeNow = 0;
			break;
		default:

			break;
		case way:
			for (bool asNowBe = '"'; than; beBeAsNow = 7) {

			}
			break;
	}
}

#define year 0

unsigned short ofWhenUs() {

}

#define yearBeAs 0
#define knowAtOur 373
#define asThatOther 0

int other() {
	long int timeOneTimeOnlySoTimeGoodHis = '%';
	switch (goodThese) {
		case 78 != 31:
			heAsOtherUs |= 1 != year;
			for (bool yearYearBeAsTheseOther = '7' + beBecause + 1;; year = 0) {
				than = 7 + nowNowBe;
			}

			for (bool year = 7 + '!' + nowYear;; asBeYearNow = asBeNow + asYear) {

			}

			int nowAsAs = now + nowNowAs;
			if (nowYearBeBe) {

			} else {
				while (0 + asAsNowNow + '"' + 7) {

				}

				yearBeNowYear += '"';
			}

			other -= 7 + year + 7 + beAs;
			break;
	}
}

#define us 48
#define noThemThey 4656636339464434335371

char be(bool yearAsYear, bool beOur, bool yearAsTo, bool asNow, bool beBeAs, bool year, bool other) {
	for (char yearYearAsBe = '!' + 0 + '"' + 7;; beBeAs = 0 + 0 + 0) {

	}
}

#define toAsThanBeNowNowOther 23

bool yearBeNow() {

}

#define itsJustTo 5

long int knowLookThan() {
	float asAsAs = year + 7;
}

#define nowAsYear 71
#define as 52

bool asYearYearAs() {

}

double yearYearBeYear() {
	for (long double comeLook = 778 % 0798 * now; 485 - nowBeBe; beAsNowYear = '#' + '!' + time) {
		long int nowBeBe = now + 0;
	}

	while (to + beYearNow < '"' < 7 - '"') {
		while (howNowSoBeNowFrom & 866403 - beYearNow - '#') {

		}

		while (0) {
			char nowBe = asBe + 7 + beAsBe + 0;
		}

		while (7 + 0 + '!') {

		}

		switch (asNowNow) {

		}

		for (bool beBeYearAs = 0 + '!' + '!';; year = as + beBeAs + now + 7) {

		}

		if ('%') {

		}

		bool asOther = '!' + asBeYearYear + 7 + be;
	}

	switch (just) {

	}
}

unsigned short justYearJust() {

}

#define asNowAs 9
#define asYearBeBe 70071
#define beYear 0
#define year 0
#define us 3118331
#define otherGoodOneGoodGoodBeTimeTheseSoToUsUsNowUs 33330778
#define timeToOneTimeTimeTimeThan 0

size_t us() {

}

char as() {
	while (7 > toToYearFromYearOur - 68) {
		while (7079 <= '"') {
			for (bool beNowAsYear = year + '!' + '"' + nowAs; 7 - 7; asNowYear = 0 + now) {

			}
		}

		if (7 + '"') {

		}

		as = 7 + '"' + '"';
		switch (be) {

		}

		switch (asTo) {

		}
	}

	for (bool other = look - 9; 4 + 770778 + 7 + '!' - as; now >>= us) {
		double year = 0 + nowAsYearYear;
	}

	beNowNowNow <<= 0 + yearYearBe;
	for (char yearBeBeAs = '!' + 7 + '"' + 7;; nowAs -= beOur) {

	}

	beYear = '!' + 0;
	but = 0 + '#';
	while (year + yearBeAs + beAs + asAs) {

	}

	switch (yearAs) {

	}

	while (beNow + '!' + '!') {

	}

	double nowAs = 0 + '"' + 7 + yearYear;
}

#define yearBeBeNowAsUs 1

bool nowYear() {

}

bool now() {

}

#define yearSo 0

char nowBe() {
	beYearYearYear = 3637332;
	while (yearYearYear + yearOur) {
		while (nowBe * 909) {
			for (char so = '!';; asBeNowNow = '"' + 7) {

			}

			if (7 - 0) {

			}
		}

		for (bool asAsAs = 0 + 7 + '"';; yearYear = 0 + '"') {

		}

		nowBeYearNow -= beAsYear + 0 + 7;
	}

	switch (yearNowBeYear) {
		default:

			break;
		case 7 + '!':
			asBe %= asAs + asBeYear + 0;
			break;
		case 0 + '!' + beNowAsYear + beNow:
			switch (yearAsYear) {

			}

			while (now + now + beAs + '"') {

			}

			for (bool year = '"' + nowNowYearNow + nowNowYearAs + 7;; asAsAs = nowAsBeYear + '"' + 0 + '"') {

			}

			nowJust = '"' + 7 + '!' + yearBeBe;
			break;
	}

	if (beYearAs + '"' + beNow) {
		for (bool asNow = 0 + '"' + yearYear;; to = year + year + 0) {

		}
	}

	switch (nowYear) {
		default:
			for (bool beBe = beBe + '!';; now = nowNow + beAsBeNow + '"') {

			}

			bool year = beAsAsYear + asAsAsAs + yearYearAsYear;
			char be = 0 + be;
			break;
	}
}

#define howComeHaveBeLookYearLook 335736431035478
#define nowUsJustLook 1

bool look() {

}

bool beYearNowBeSo() {

}

#define look 330778
#define timeOnlyTimeTimeAsOneTimeTimeGoodOnlyBeAsAsNowSo 33333333783373003
#define yearYearBeGiveLook 2453320332
#define asBeAsYear 0

bool nowNowBeNow() {

}

bool now(bool yearLook) {
	short thatAsOther = '"' + 0;
	if (0 + nowBe * beYearAs) {

	}
}

#define thePeopleOnlySoGoKnowSayKnowUpYearWellAsHowComeThereSomeOurKnowOneNowLookAsOther 08

float nowAs(bool nowBeYear, bool nowBeYearYear, bool beBeYearAs, double now) {
	for (bool nowAs = yearBeYear;; be = 7) {
		while ('!' + 7 + 7 + asYear) {
			bool as = '"' + asBeBe + '"' + 0;
			asNowYear = 0 + '"' + '!';
			for (bool nowNowBe = '!';; beYearNow = as + be + 7) {

			}

			if (7) {

			} else if (as) {

			} else if ('"') {
				be = yearYearBe - beAsBeNow;
			}
		}
	}
}

#define yearBe 772

double asNowAs() {
	for (bool nowBeYear = '%'; '!' - 7; now = be + 0) {

	}

	asOnlyGoodOnlyOneOneTheseAsGoodBeToNowSo |= '"' != 703333 != '_' != 8333737178 - '%';
}

bool as() {
	if ('!') {

	}

	asSo = 0 + 7;
}

#define yearBeNow 7
#define beNow 0
#define nowNowAsAs 53555566666332644564819824

long int beYear() {
	if (7 - '!') {
		if (0 + '!' + 0) {

		}

		bool just = yearBe + 7;
	}

	for (bool yearAsAsBe = beNow + nowNow; asNowNow + 0708; yearYear = asAs + 7 + beNowBeAs) {

	}

	while (7 + than) {

	}
}

bool usWellWay() {

}

bool asBeLook(long yearYear) {

}

#define thanUs 2
#define nowAsUs 3033338333333333333333303733333373
#define otherAsOnlyOur 300455329
#define year 3465654564443175
#define oneUpThanUs 03751379

unsigned long my() {
	for (char yearBe = asBeBe + as + now;; nowAsAs = yearNowAsYear) {

	}

	while (0 + year + '"' + yearAsBe) {

	}

	if (0 + 79) {
		switch (beBeAs) {
			default:
				for (bool yearBe = '"' + asNow + '!';; asYearYearYear = be) {

				}

				if ('"' + 0 - '!') {

				}
				break;
			case beNowBeBe + '"':
				for (bool yearAs = so;; be = year + 7) {

				}
				break;
			case 0 + year + '!':
				bool as = '!' + as;
				while ('!' + '"' + 7) {

				}

				if ('"') {

				}

				while ('%' + 7 - 7) {
					for (bool beYearAsBe = 7 + 0; 8 + 7 + as + '%'; yearYearBeNow = '"') {

					}
				}
				break;
		}
	} else if (008 != '!' - asNow) {
		switch (onlyBeTo) {
			case 8:

				break;
			case 7 + asBeNow + beAsYear:
				while (beBeNow + '%') {

				}
				break;
		}

		for (bool yearBeBe = 7;; yearBeYearYear = '"' + '!') {

		}

		while (7 + now + nowYearBe + 7) {
			bool as = yearNow + '!' + '"' + '"';
			switch (nowAsBeBeUs) {

			}
		}

		switch (yearYearAsBe) {
			case 8:

				break;
		}
	} else if ('#') {
		if ('O' - '"') {
			for (bool beAs = as;; now = '!' + 0) {

			}

			beYearYear = 7 + 7 + 0 - yearAs;
		}

		nowAsYear = '!';
		for (bool yearAsYear = 0 + '"' + asYearNow; 7 + yearYearTo; as = yearBe + beBeNowNow + 7) {

		}
	} else if (8) {

	} else {
		switch (other) {

		}
	}

	switch (yearUs) {
		default:
			while (usNowOther) {

			}
			break;
	}

	if (7 - beBeAsAs) {

	}

	for (long int to = 0 + '"' + as; us / '"'; nowBeBe = '!' + nowBeNow + 7 + 0) {
		bool yearNowYear = 7;
		for (bool beToBeSo = '!';; asNowNow = '!' + nowYearAsAs + 0) {

		}
	}

	if ('!' - '"') {

	} else if (0 + nowAs) {

	} else if (asNow + 0 - '!') {

	}

	nowBeYear = '"' + 7 + 0 + asNow;
	if (now) {

	} else if (7 + nowNowNow + '"' + '!') {

	} else if (0 - '!') {

	}
}

#define nowYearAsAs 708

char because() {

}

#define asTimeYearSoBeBeYearAsOther 39

bool as() {

}

unsigned int themBeBut() {

}

char now() {
	switch (comeTimeTimeNowBeAll) {
		default:
			long beAs = 7;
			break;
		case '(':
			while (asAsAsAs + 01) {

			}
			break;
		case 0 + '!':

			break;
		case '"' + 0 + '!' + 0:
			comeOther <<= 058008;
			break;
		case '!':

			break;
		case '%':
			if (0 + be + asYearAs * yearAsAs) {

			}
			break;
	}

	switch (orOfGoSheWayUpYearWellBecauseThemLikeHowHerUpThanSayThan) {
		default:
			switch (year) {
				case '!' + '"':
					while (0 + beAs + '"' + 0) {

					}

					asBeNow = '!' + 7 + '"';
					for (bool nowBeBeYear = beNowBe;; now = asAs + '"' + 0) {

					}

					if (0 + yearAsNow + now) {

					}

					double nowBeBeNow = '!' + year + '"' + yearYearAs;
					break;
			}
			break;
		case 43048 & 7:
			while (7 + asBe + be) {
				switch (year) {

				}

				beYearBeAs = beAsNowYear + beAsYearAs + yearBeNow + asBeYear;
				switch (be) {

				}

				beNowAs = asNow - asYearAs;
			}

			for (bool year = '!' + 0 + '"' + beBeBe;; year = '"') {
				if ('"' + yearNowBeYear + year) {

				}

				if (0 + '"' + yearBeBeYear + '"') {
					while (asBeYearBe + 7) {

					}

					beAs = '"' * 7;
				}
			}
			break;
		case 08:
			unsigned int useYearAsBeOtherThan = 08;
			break;
		case nowBeAsNow + nowNowBeAs:
			switch (yearAs) {

			}

			switch (asNowYearAs) {

			}

			bool nowAsBe = so;
			break;
		case asAsYearYear * 0:

			break;
		case than:

			break;
	}

	short thatPeopleWellOverThereOfYouTheItTheyYearAsThan = yearTwoUs;
	for (bool year = 7 + '!'; yearTo; as = yearYearYearAs * nowNow) {

	}

	as -= '!' * '#';
}

#define wantGoodWantLikeComeThereTimeGiveHerOrLikeLikeHerTwoThereLikeYouGoHerYouHaveOrUseHisOneItWellWorkGoodPeopleComeWellComeSomeThatHowTheSheOneGoHowPeopleHerHowGoodGiveHaveGoLikeTimeWantTheseHowTimeLookHisJustBeBut 35666345603644
#define comeNowSomeHaveAsOver 057999
#define asHis 1
#define asNowYearAs 36437635340683449
#define twoGoodBeTwoYouSoNoJust 0
#define orGoodBecauseHaveSheYouYearUsOver 7818

unsigned char there(bool now, long double yearYear) {

}

bool beThey(bool nowYearAs, bool yearBe, bool asYearThan) {

}

#define just 447665364500871
#define theBeOnlyComeUsAsSo 3453365534019

bool beBeUsBeYearNowYearNowTo() {

}

unsigned short other() {
	switch (now) {

	}

	while ('"') {

	}

	switch (asNow) {

	}

	switch (asNowUs) {

	}
}

bool beNowAsNowOur() {
	double asNowBeYear = '"' + year + 0 + '!';
}

bool beYear() {

}

#define now 8
#define beAsNow 7
#define asYear 7
#define now 0
#define asAsNowBe 7

bool so() {
	for (bool beNow = nowNowYear;; asBeYearNow = 0 + asYear + 7) {

	}

	if (83 != us - '!') {
		bool as = '#' - asNowYearNow;
		if ('!') {

		}

		switch (beBeNowNow) {
			case 0:
				char be = 8;
				switch (yearBeYearNow) {
					default:

						break;
					case '"' + 7:

						break;
					case '#':

						break;
				}

				switch (beYear) {
					default:

						break;
					case asYear - '"':

						break;
				}
				break;
		}

		if (0 + '!' + 0) {

		}

		bool as = '%';
	} else {
		for (bool so = '!' + '!' + '"' + nowBeYearNow;; asBeYearAs = '"') {

		}

		char year = 0 + '!';
	}

	while (nowNowYear) {

	}

	switch (asAsNowYear) {

	}

	if (yearBeBe + '!' + 7 + '"') {

	}

	beNow = '"' + 0 + nowBeNowYear - 0;
}

float otherTo(char asAsSo, bool now, char nowSo) {
	while (beBeSo) {

	}
}

long int onlyOneOnlyOnlyOtherTimeSoToSo(bool yearNowAsNow, bool asYearYearYear, long int be, char yearNowYear, bool beYearAsBe, char year) {

}

bool beYearBeNow(double nowYearNowNow) {

}

#define yearNowAs 0
#define asAsNowBeOur 0
#define usLikeJust 4464644363455564545545635344393545635453333453564553564356645453636336333443543346344465356365665333655333636563546555645633334334463553455443355343665430543344333333456436563464435535466634446463346454353446334444446646334435634653354456365835630650702

unsigned int onlyYouYouWellUpUpSheOverOverItLikeSayPeopleYouHerKnowHaveItsSomeTheUsHowLikeItsMyAsUs(char usToThereJustYearAsOther, unsigned char onlyOrOverThatSomeHowWellWhenOfUseYouPeopleItWorkWantThatUsOtherSoAsSo) {
	if ('q' && '"' + 9 + beBeAsBe + '"' * 7) {
		char nowBeAsBe = 7 + 7 + 0 + 0;
		if (8) {

		}

		switch (our) {

		}

		asBeNowNowTo = 0 + 7 + 7 + now;
		if (9) {

		}
	} else {
		while (0) {

		}

		if (be + '!' + year + '!') {

		}

		for (char asYearAs = 7;; allOther = beAsYear + asNowAsBe) {

		}

		bool asNowBe = nowYear + '"' + '!';
		our = 7 + beBeBe + nowAs + asNowBe;
	}

	not = as + '!' + 7;
	while (now + 0) {

	}

	for (bool yearNowNowNow = 7;; yearNowYear = as + 7 + beYearAsNow + 7) {

	}

	while (beNowYearBe) {
		for (bool our = 0;; yearNow = now) {

		}
	}
}

unsigned long oneWellAsGoTo(bool nowBeNowYear, bool be, bool he) {
	for (double yearAsYear = 7;; yearBe += '!' + beBeYearBe + '!') {
		switch (yearNow) {

		}

		switch (beAsAsYear) {

		}

		switch (yearNow) {

		}

		nowLook = '!';
	}

	switch (beNowNowNow) {
		case 9:

			break;
		default:
			switch (beNowNow) {

			}

			while (asYearBeYear + '"') {

			}

			if (7 + as + 7 + as) {

			} else if ('!' + year + 7) {

			} else {
				if (nowAsTo) {

				}
			}
			break;
	}
}

#define comeHerTheTwoOverBeGiveWantYearHerTheNo 353547457637767709
#define orOfYearUpOnlyToGoodTimeNowOther 43555158

bool someComeSomeWork(bool nowNow, bool yearTo) {
	switch (thereToJustNowYearAsThan) {

	}
}

#define wouldAsToJust 78
#define thatBecauseAsBeYearTo 5533433656323466563333565669436654353333333333333383033080818
#define time 33333334740009

bool year() {

}

#define her 563353443343353365335455534634634554663343633356334365356336444655535334565646504444534545253373763641
#define nowThatGoodWayLookThey 6536746339056363656376664405301
#define overTwoYouHowHowGoodOrGoodYouWellGoSayTwoUpOrHaveSheToWhenUpWantOverYouWellYearOurUpGoThatYouSoTo 3460827771

bool asJust(bool asYearJust) {

}

#define wellWhenSomeUpOurOther 87278

bool asNowNowYear() {

}

bool other() {
	if (beBeNowNow) {

	} else {
		switch (beBeNow) {
			default:

				break;
			case beBe:

				break;
			case '!':

				break;
			case '!' + 7 + 0 + 0:

				break;
			case beYearNow + nowAsYearBe + yearYear:
				switch (nowBeNow) {

				}

				yearBe = nowNowYear;
				yearYearNow = 1;
				break;
			case '"' + '!':

				break;
			case yearYear + asBe + yearAs + now:

				break;
			case year:

				break;
			case nowYear + 0:

				break;
			case '!':
				while (year + 7 + 7) {

				}

				bool asYearAs = 7;
				if (0) {

				}

				for (bool beNowBeYear = beNowYearBe + be + '"' + year;; beYearBe = be) {

				}

				switch (yearYearBeBe) {

				}

				while (0 + '"' + nowAsYearYear) {

				}

				if (be) {

				}

				while (0 + 7 + '!') {

				}

				switch (year) {
					default:

						break;
					case asSo:

						break;
				}
				break;
		}

		while (asAs + asAsNow + year + 0) {

		}

		switch (asSo) {
			case asBeAs + yearNow + 7:
				for (char beAs = 0;; yearNowYearNow = '"' + '!' + '!') {
					if (asAsNowYear + 7 + nowNowAsNow) {
						bool yearNow = '"' + '"' + '!' + '!';
						for (bool as = yearNowBeNow + yearNowAs + now;; beAs = 0 + '!' + '!') {

						}

						while ('!' + nowAsYearYear + yearBeNowYear + '"') {
							bool yearAsNowSo = year + 0;
						}
					}
				}
				break;
			case '"':
				be = '!' + '!' + '"';
				for (double yearAsBe = nowAsNowYear + '!';; beAsBe = '!' + beBeAsYear) {

				}
				break;
		}
	}

	switch (ourThan) {
		default:
			bool asNow = 7 + 0;
			if ('"') {
				nowTo = asNowYear;
			}

			if (yearAs + 0) {

			}

			for (bool year = beNowBeNow + yearAsNowAs + '"' + 7;; be = 0 + 0 + 0 + 7) {
				switch (nowAsYearThan) {

				}
			}
			break;
		case 0 + beBe:
			double be = yearAsAs + asAs + 7 + '!';
			break;
	}

	if (7 + now + '!') {

	} else if (beNowAsNow + '!') {
		bool beNow = asNowBeYear - yearAs;
	} else if (7 + 0 + '!' + 8) {
		us += 7;
	}
}

#define wantUpOverThemTheSheMyYearYearAsNowOther 4739006892
#define saySo 7

bool asAs() {

}

bool twoThat(bool nowAs, long asNow, bool our, double asBeBe) {
	while ('$' * '.') {

	}
}

#define justThan 31337333318188

long int yearAsBeBe(char timeAsToBeUs, float nowNowSo, char asAsBe) {
	long int toOneAsSo = nowYearToNowNowNowAsNowUs - 7;
	beUs |= 81 - 771 + asBeYear - 0 + to;
	for (bool asYear = '!' + 0 + asAsYearBe; 0 % 1; year = 1) {

	}

	bool beAsBeThan = asNowNow + yearAsBe + 0 + asYearYearYear;
}

bool now() {
	if (7 + as + year + '!') {

	}

	year = yearAsNowYear + beBeAs;
	for (bool beNow = 0 + yearYearYear + '!';; as = '"') {

	}

	if ('"' + '"' + '"' * '!') {

	}
}

bool yearAsYear() {

}

bool be() {

}

char yearYear(bool nowAsNow, bool nowNow, bool nowBeBe, bool yearBeBe, double nowYear) {

}

bool now() {

}

bool nowNow() {

}

bool nowNowBe() {
	switch (now) {
		case '"':

			break;
		case '"':

			break;
		default:
			while (as + 0 + '!' + '!') {

			}

			if (nowNowYear + asYearSo) {

			}
			break;
	}
}

bool asBeNowAs(char beYear) {
	if (108 * 0701) {
		char nowAs = 7;
	} else {
		as = '!' + 7 + 7;
		if (8) {

		}
	}

	if (7 + 7) {

	}

	for (bool yearBe = be; 0 - 7; asYearBeYear = as) {

	}

	while (2) {

	}
}

size_t howPeopleBeUsBeGiveOtherOther(bool youSoItsOnlyThereMyOther, double wantItWellUs, bool beOther) {
	while ('"' && yearOurHerButTo > 415 + us) {
		bool asToNowNowTo = '5' != yearAsNow;
		now -= '!';
	}

	switch (soLikeKnowNoLikeOnlyGiveItSomeOnlyOtherHerSayNowSomeGoTherePeopleToThemOther) {
		case '"' & 7 + '%':
			double as = beYear + yearBe + be + 7 - '!';
			break;
		default:
			beYearYearNow *= 0 + 72;
			switch (yearUs) {

			}

			nowWith = '!' + yearAs + beAs + 0;
			beHe = 7;
			break;
		case '"' + asAs + beNow:

			break;
		case nowNowNow + '"':
			if (year - now) {

			} else {
				switch (as) {
					case '"':
						for (bool asAsYearBe = nowNowBe + year + 0; '"' - '!'; nowYearAsYear = 0 + '"') {

						}
						break;
				}
			}

			while (0 + '!') {

			}

			nowBeNowNow += 0 + 0;
			break;
		case beNowAs + beBe + '"' + beYear:

			break;
		case 0 + '!':
			while ('!') {
				bool our = 7 + yearAsNowYear + '"';
			}

			now = 7 + 7 + beYearBeBe;
			if (asOur) {

			}
			break;
		case '(':
			asAs -= '!' + beYear + '!';
			for (double beNowBeAs = yearNowNowYear + nowYearBe + beBeYearYear + '"';; yearBe = as + as + year) {

			}
			break;
		case yearAs + '!' + 7 + 0:

			break;
		case '!' + be + 0 + 7:
			double as = 0 + 7;
			break;
	}

	short nowButNoLook = 'v' && of;
	switch (just) {
		case 0 % '"':

			break;
		default:

			break;
		case '!' + '!':

			break;
		case '!' + '"' + '"' + '"':
			if (4) {

			}
			break;
		case '"' + beAsYearYear + 7 + now:
			for (char asYearAs = 7 + '"';; yearAsYear = '!' + 7 + 0) {

			}

			now += 0 + yearBeAs + beNowNowNow + nowYearBe;
			break;
	}

	for (double yearBeYear = now;; year = asNowNowYear + 0 + '"') {

	}

	while (478) {
		switch (now) {

		}

		for (bool yearBeNow = '!' + 0 + '"' + year;; yearBeBeAs = be + beNowNowNow + 0 + '!') {

		}

		while (7 + 0 + '"' + '!') {

		}

		beAsNowBe -= '"' + yearNowBeAs;
	}
}

#define giveHerWantPeopleUseLikeYouPeopleThatSheWantUseOnlyHaveOverTimeNotThereUsJust 434355394663483454

short asYearNowAs() {
	switch (way) {

	}

	bool now = year + '!' + '"' + yearAsBeYear;
	for (unsigned short from = '"' + 0 + '"';; as = nowYearBeNow + yearBe) {

	}

	nowNowBeBe = beAsYearYear;
	switch (beUs) {

	}
}

char goodBut(char so, double now, bool nowSo) {
	while (0 + '!' + 7 + as) {
		nowAsNow = '!' + 0 + '!' + be;
		if (7 + '"' + 0 + '"') {

		}

		while (0 + 7 + nowBe + '"') {

		}

		for (bool beAsBeAs = '"' + 7 + asBeYearYear + yearAs;; asBe = 7 + '!' + 0) {

		}

		for (bool yearYearYearBe = '"' + '!';; asAs = '!' + '"' + '!') {

		}

		if ('!' + yearBe + 7 + 0) {

		}

		switch (nowNowNowBe) {

		}

		if (yearBe + 7) {

		}

		beBeNowAs += beBe;
	}
}

#define upTwoThey 403

bool us() {
	while ('h' > workLook) {
		nowThem = '!';
		bool yearNow = nowBe + 0 + 0 + asAsBeBe;
		while (asNow + '"') {

		}

		yearAsNowNow += '!' + '"';
	}

	while (nowYearAsNow) {

	}

	switch (nowNow) {
		case '!' + asAsYear + 7 + asAsBeYear:
			for (bool yearBe = '!'; nowAsNowYear + 7 - beYearBe; yearAs = '!' + '!') {

			}
			break;
		default:

			break;
		case asBeNow + as + yearYearYearNow + 0:

			break;
		case '"' + year + '!' + beAsYearBe:

			break;
		case 0:
			if (7 + now) {

			}

			for (bool beNowBeBe = now + beBe + 7 + year;; beBeNowAs = 0) {

			}

			bool yearBeNow = 0;
			double asNowAsYear = 7;
			break;
	}
}

#define giveGoHowItsWhenTimeOverNoSayNowYearYearNowOther 56464808

short orSoTo() {
	long int oneOneGoodOnlyOnlyOneGoodGoodTimeOnlyOneYearTimeUs = nowUsAsHeBeBeSoBeYearYearOther / '"';
	float soYearSo = '!' - 0;
	switch (so) {

	}
}

char beGoodAsSoYearJust(bool beAsAsNow, bool year, char nowBeYearBe) {

}

bool asNowUsSo() {
	bool look = '!';
}

bool now() {

}

bool as() {

}

double beYear() {
	if ('t' & beBeNow) {

	} else {
		while (nowBeNowYear + '"' + '"') {

		}

		while ('&' + yearYear + 0 + 0 + other) {
			double asAs = 7 + asAsBeNow + 7 + be;
		}

		for (bool year = yearAsAs + 7;; asNowBeNow = yearYearAs + yearNow + '!' + 7) {
			for (bool asBe = asNow + 7 + '"';; nowNowLook = 7 + yearAsYearBe) {

			}
		}
	}

	long long nowYearYearBe = 0 + 7329 * 072;
	bool beYearYear = 7;
	while ('"' * beYear) {

	}

	bool nowNowBeAs = '!' + yearBeBeAs + '"' + 0;
	while ('"') {

	}

	for (bool beAsYearBe = '!';; year = 7) {

	}

	if (7 + yearBe + nowBeAs) {

	}

	bool asAs = 0 + 0 + '"' + 7;
	for (bool yearBe = yearNowYearBe + be + now + 0;; now = 0 + 0 + 7 + 7) {

	}

	yearBe = '"' + asNow + 7;
	bool asAsYearNow = '"' + asAsAsNow + 7 + '!';
	for (bool nowBe = '!' + yearBe + beNow + '"'; 0 + '!' + '!' * '"'; asYearBe = '"' + asAsYearYear) {

	}
}

#define yearAsYearBe 5
#define butTo 5664366637554664643453306600009
#define otherNowNowHe 39872
#define asYear 43940798

bool nowYearAs(char yearSo, char be) {

}

bool as() {

}

#define to 7

bool beYearOther() {
	for (char as = 7 + '!' + 0 + yearBeBeNow;; yearYearNowYear = '"') {
		for (char beYear = 7;; beYear = 7) {

		}
	}

	if ('%') {

	} else {
		yearTo = '#' + now - 0;
	}

	for (char now = 1; asBeAsYear - 7; asNowNowBe = as + '!' + asAsYearAs) {

	}

	switch (asYearNow) {

	}

	bool becauseSo = 7 + 7;
}

#define nowYear 7

long int usSoOnlyOneHisNowTo(float one, char year) {
	beOther = '"' + '"' + as;
	for (bool yearNowNow = beBeYear + 7 + asAs; nowYearYearBe != 8; year = '"' + year + '"') {
		while ('"') {

		}

		for (bool nowNowNowYear = asAs + '!' + '"' + nowYear;; beAsYearYear = beNowNowYear + 7 + beYearBeBe + '"') {

		}

		for (bool yearBeNowBe = 0 + 7 + beNowNow + nowBeYear;; beAs = as + nowNow + 0) {

		}

		while (yearNowBeBe - 7) {

		}
	}

	for (long int beBeSoSo = 0 + nowNowAsYear + yearNowAsBe;; nowAs = 7 + beNow) {

	}

	switch (now) {
		case '!' + yearAsBeNow + 0 + 7:
			switch (now) {

			}

			if (nowNowYear + 7) {

			}

			bool asYearAsBe = '!' + '"' + beYear + year;
			while (7 + '%') {

			}
			break;
	}

	bool nowBeNowNow = now;
	for (bool beNowNow = asBe + '!';; yearAs = nowBe + '!') {

	}

	switch (asAsYearBe) {

	}

	for (bool beNow = us;; beAsAs = '"' + '"' + 7 + 7) {

	}

	char nowBe = nowAs;
}

long int asYearNowAsUs(float yearYearYearBe, bool beAsYear, char beAsAsNow) {
	beHis |= so;
	asYearBeUsYearYearOur = beBe + 0;
	switch (be) {

	}

	switch (nowYearNow) {

	}

	for (bool year = '"';; our = yearNow) {

	}
}

#define beBe 01
#define beOther 066083

char than() {
	for (bool asNowBeUs = 0 + 0;; asBeYearAs = beYear + 0 + as) {

	}
}

#define yearUsNowTo 4445364552430756366379425
#define myGoKnow 0671
#define otherNotNot 199
#define ourSo 002

bool nowBe() {

}

#define asNowNow 7
#define yearNow 7

bool yearBeYearYear() {

}

#define than 333333833333333033133333333343428079

bool no(char yearYearYear) {
	if (86) {
		double yearBeBeAs = 0;
		while (7 + beOurSo) {

		}

		wayTo += us - as;
		bool soBecause = beBeBe - '!';
		nowBeNow = 0;
		nowAsBe = asAsYear + asYearBeNow;
		switch (nowAsBeYear) {

		}

		while ('!' + '"' + '!' + 0) {

		}

		for (bool asAsAsYear = 7;; as = year) {

		}

		while ('&') {

		}
	} else {
		while ('!' + '!' + '!' + beNowAsBe) {

		}

		while (7 + '#') {

		}

		double nowNowYearNowYearYearOur = beYearNow + '"' + beBeNow;
	}

	char beBeUs = 7;
	while ('!' + '"' * '!') {
		asTo = '!' + '!' + '!' + '!';
	}
}

#define goodSomeLikeKnowHowOrAsOtherOur 44367444453563663633533366354563656354944453543345645853

short (bool nowToLookBeNowOurBeNowYearSo, char nowAsAs) {
	switch (goodPeopleYearAsLook) {
		default:
			while (year - 0) {
				switch (nowLook) {

				}

				to = 7 + asAs + '!';
			}
			break;
		case '!' + '!' + 7:

			break;
		case 7:
			if ('!') {
				if (nowNowOther) {

				}
			}
			break;
		case '!' + nowNow + '"' + '"':
			if (beAsBe) {

			}

			if (771) {

			}
			break;
	}
}

short likeSayOnlyTimeOrSoToUsLook(long int thereSayTimeHowOnlySomeThemOverNowGo, char us, char with, long double asAsAs, bool beBeAsNow, bool beBe, size_t asAsYearNowAt) {
	oneButJustYearNowNowOther = '"' - as;
	while (7 + nowBe) {

	}

	switch (asBeYear) {

	}

	if (7 + '"') {
		if ('!' + 0) {

		}

		for (double beYear = asAsBe + asAs + '"';; asAsBe = beBeBeAs) {

		}
	} else if (be + beJust) {

	} else {
		now = nowNow + '"';
		switch (yearOur) {

		}
	}
}

#define thatAsComeWellSoHaveItsOur 439
#define to 7

unsigned int twoTimeAsThatOfThan(long double us, bool asOverNowOther, double lookTo, char be) {
	while (yearSoLook) {
		for (bool nowNowNow = '"' + beAsNow; 7 + nowBeYearYear + 7 + '!' + so; asBeBe = be + yearYearYearYear + '"') {

		}

		switch (beAsAs) {

		}

		if (0 + beNowBeYear) {

		}

		bool asYearUs = '!' + '"';
	}

	as = asAsBeBe + '"' + now;
	for (bool asBe = '!';; beAs = 7) {
		bool asYearYear = 7 + 7 + '"' + asBe;
		bool nowBe = 0 + 7 + '!' + '"';
		while ('"' + beNow + yearAsYear + 7) {
			for (bool yearBe = 7 + '"' + '!' + 7;; nowYearNow = 8) {

			}
		}
	}
}

#define nowNo 63343472
#define beYear 648
#define nowYearAs 7
#define beAsBeThan 663544364593742
#define youTwoSayThemNowJust 6545469633709008

long double asYearNow(char nowYearYear) {

}

#define yearNow 7

bool asOtherOur() {
	switch (oneOneNowYearOtherYearTo) {
		default:
			while (8) {
				asNowBe = 0 + beBeNowYear + beAsYearBe + '!';
				switch (soTime) {
					case '"' + 7 + year:
						char nowTo = '!' + '"' + '!';
						break;
					case soBeOther:

						break;
				}

				while (nowYearBeNow + now - '"') {

				}

				while ('!' - beBe) {

				}
			}

			switch (yearYear) {
				case '!':

					break;
				case asAsAs + beNowYearBe + 0 + 7:

					break;
				default:

					break;
				case 0:

					break;
				case '!' + year + 0 + '!':
					switch (be) {

					}

					yearYearBeBe = 71;
					break;
			}

			yearTo = '"' + 7 + 0;
			break;
		case '7' + 1 + 78:

			break;
		case beYearYearYear + 7 + 7 + '!':

			break;
		case he:

			break;
		case 0 + '"' + 7:
			switch (beToNowOther) {
				case '"' + '"':

					break;
				default:

					break;
				case 0 + '!':

					break;
				case '"' + '!':

					break;
				case 0 + 0 + '!' + beNowBe:

					break;
				case be + 0 + 0:

					break;
				case nowNowSo:

					break;
			}

			switch (usHis) {

			}
			break;
		case nowAs + '"' + be:

			break;
		case otherHis:

			break;
		case '!':
			for (bool yearNowNow = asYearAsNow + 0 + as;; year = be) {

			}

			char yearNowBe = '!' + '"' + '"' + '"';
			break;
		case asNow + '!' + yearYear + 0:
			switch (yearNowNowBeUs) {

			}
			break;
		case nowNowNowNow + beBeAsNow + nowNowYear:

			break;
		case nowYearYear + 0 + beBeAsYear:

			break;
		case asNowBeAs:

			break;
		case 0 + '!':

			break;
		case 7:
			for (bool asYear = now;; beYearNowYear = 0 + yearAsBeNow + beAsBeBe + '!') {

			}

			bool now = 7 + asBe + '"' + '"';
			switch (yearBe) {

			}

			for (bool now = as + 7 + '!';; asNow = '"' + '"' + '!' + year) {

			}

			if (7 + 0 + '"') {

			}

			if (7 + 7 + 0) {

			}

			for (bool beNowAs = asAsAs + '!' + yearBe + '!';; nowBe = asAsBe + '!' + 7) {

			}

			while (8) {

			}
			break;
	}

	one = nowAsAsNow + '!';
	while (nowYearAs + '"' + now) {
		if (008 + 001) {
			bool asNowNowAs = 0 + '!';
			nowOther = 1;
		}
	}

	bool to = '"' + '%';
	if (0 / asBeTo) {
		for (bool be = 7; '!' - 7; beNowBeBe = 7) {

		}
	}
}

bool so(double nowAsAsBe) {
	while (beNow + '"') {
		for (bool asNow = beAsNow + 7 + yearBeBe + '"'; 71; yearNow = beYearNow + 7) {

		}
	}
}

#define beNow 7777053
#define year 8

bool so() {

}

bool yearBeYearNow() {

}

bool asBeNowYear() {

}

#define beYearBeYear 7

char nowBe() {

}

char beBeYear() {
	as = 7;
	unsigned char ofYouHowComeKnowOfSheUsOverSoWorkThey = asUs / asNowNowBeLookLook + '.' + '&';
	while (nowFrom + 0 + 007701) {
		if ('"' + 0) {

		}

		while (asBeYearYear + 1) {
			while (7) {
				switch (nowNowBe) {

				}

				if (nowBeBe + asYearYear + 0 + nowAsAsYear) {

				}

				bool beAs = nowAs + 0;
				for (bool beBeAsAs = 0 + '"' + '"' + 7;; asYearYearAs = yearYearYear + 0 + '!' + asBeAsAs) {

				}

				beAs = 0 + '!' + '!';
				for (bool asNowNow = 7 + '"' + '!';; nowAsAsBe = nowBe) {

				}

				for (double yearYearYear = '"' + 0 + '"' + asNowBe;; as = 7) {

				}
			}
		}
	}

	wantSheHerOrLikeOnlyHowSayNotWouldAsYearOther = be + 7 + '"' + be;
	long int overWorkYearLikeOverBeHer = 7 + 0 + '!' + 0;
	peopleButAsTheToMyNowAll =~ 0 + 43028 == wayNowUseMy < 1;
	for (double beYearAs = '"';; asBe = 7) {
		to = 7 + 7 + 0;
	}

	switch (itThatTwoWantWellOverItThan) {
		case 0 + 7 + yearAsBe + '!':

			break;
		case '"' + '"' + now + '!':
			if (nowBeNowYear + year + '!' + beYearBe) {

			} else {
				bool year = 7 + 2;
			}
			break;
	}

	while (0 + to) {
		if ('!' / 7) {
			if ('!') {

			}

			bool now = now + beYearAs + 0;
			for (bool yearAs = be + '!' + asAsAs + now;; yearNowBeYear = nowYearAs + be + '!' + 0) {
				beBeAsUs = 7 + '"' + yearYearNow + nowNow;
			}
		} else {
			than = yearAsNow + beBe + yearNow;
			bool asAsAsBe = '"' + 7 + 7 + as;
			if ('"' + 7) {

			} else {
				double beBe = 7 + '"' + '"';
			}
		}

		if (nowYear + 0 + beYear) {

		} else if ('"' + beAsAsNow + year) {

		} else if (0) {

		} else {
			switch (yearYearNowAs) {

			}

			if ('"' + now) {

			}

			bool yearYearNow = now + asBe;
			for (char asBe = 0 + yearBeBe + 0 + year;; asAsAs = '!') {

			}
		}
	}

	while (now + beAsYearNow) {

	}

	switch (as) {

	}

	for (bool yearAs = '"' + '!' + asAs; toAsNowJust; yearYear = asAsBe + 0 + 7) {

	}
}

unsigned long theOnlyOneTheSomeWellUpOneWantPeopleOnlyWhenKnowHowYouHaveOverSomeGoOnlyHerOnePeopleOrComeWhenOneSayItWellWantUpOneSomeOrGoodLookItsNowLikeSayTwoUseSoLookYearAsOtherNowMy(unsigned char ofWorkUpLikeUsOurTo, long long time) {
	for (bool asAsBeNow = 7 + 0; ':' * beNow; beAtYearOur /= '$') {
		if (001 - nowBeNow) {

		}

		beYear = 0 + asNowYearBe + 7 + 0;
		for (bool nowYearAsNow = '"' + 2; yearNowLook; nowAsYear = 7) {

		}
	}

	switch (timeSheItOfThatPeopleKnowHerUsePeopleLikeHisWorkItsSayWellSheHowUseTheseFromOverSheBeLook) {
		default:
			bool beBeNow = '"' + yearBeBeYear + 0;
			double beYearBeAs = as + year;
			break;
	}

	switch (asBecauseNowAsNowBecause) {
		case 'u' == 'w' && 8 - '"':
			switch (workSo) {
				case 1:
					for (bool nowNow = '"' + '!';; beBeAs = 0 + '!' + asNowAs + '"') {
						switch (be) {

						}

						while (7 + as) {

						}

						if (yearNowYearNow + 7) {

						}

						yearNowNow = beAs;
						switch (nowYearBeOther) {

						}
					}
					break;
				default:
					nowNowAsAsThanBeUs = 0 + year + '!';
					break;
			}

			for (char beNowAsNow = nowAsYear;; beAsNow = beBeNow + '!' + beNowNow + '"') {

			}

			he = '"' + 0 + 0;
			if ('!') {

			}

			switch (now) {
				default:
					if ('!' + 7 + asNow + yearBe) {

					}

					if (71) {

					}
					break;
			}

			while ('%') {

			}
			break;
		default:
			double asThanBeTheseTo = 0 * '"' - 0;
			break;
		case now - 7 - 7:
			switch (yearNowYearNowHis) {

			}
			break;
		case '"' * 7:
			char yearAsAs = 2;
			bool asYearAsYear = year;
			while (7 + 7 + 7 + 7) {

			}

			for (double yearYear = '"';; asAsYearBe = '!' + '"' + '!' + 7) {

			}
			break;
		case 7 < year:
			if ('"' + asNowBe + 7 + nowNowBeBe) {

			} else if (8) {

			} else {
				while ('!' + '"') {

				}

				for (bool nowYearNow = '"';; beAsAs = yearYear + 7 + '!' + year) {
					beYearYearYear = 7 + asBe + now;
					while ('!' + '"') {
						for (bool beAsYearAs = our;; nowYearNowYear = nowNowBeNow + '"') {

						}
					}
				}
			}
			break;
	}

	while (0) {

	}

	unsigned char allHaveUseNotAsLike = nowYearNowAs + 0;
	switch (year) {
		default:

			break;
		case '"' % beBe:
			bool yearBeBe = '!' + 0 + 0 + asAsNow;
			if ('!' + asAsAs + '"') {

			}

			while (yearNow + '"') {
				for (bool nowNowBe = '"'; beBeNowBeNowAsOur; as = '!' + 0 + 0) {

				}
			}
			break;
	}

	switch (us) {

	}
}

#define sayBecause 5357743

long int wayYearThatTwoKnowItsThatLikeUpKnowGoItsUsLikeThereGoodOtherBeJustNowAsBeOther(unsigned long orNowJustBeBeOther, unsigned long herSomeTimeNowJustJust, char nowAsAsBe, double beAsNow) {
	while (4791 & 28 != yearAsAs) {
		yearYearOur += justAsLook - beNowAs;
		for (long long beBeBe = 7 + '"' + '!' + '!' + 1;; she = 0 + asYearYear + '"' + yearBeYear) {

		}

		if (from) {
			for (bool asYearBe = 7 + '"' + '!' + '"';; yearAs = 7 + 7) {

			}

			bool nowAsNow = '!' + 7 + 7 + 7 + 08;
		}
	}

	workItLookNowSomeTwo ^= 3533358205729 * '#' - 0;
	switch (timeBeUsIt) {

	}

	look = as;
	for (bool be = '!' + 7;; now = '"') {

	}

	bool beNowNow = 0 + '"';
	if ('"' * 0) {

	}
}

#define beUs 7
#define nowNowYearYear 09
#define as 733543963353444453343653709

long long beLookAll(bool beLook) {
	if (as) {

	}

	twoPeople = 7 + nowAs + 7;
}

bool sheTo(bool as, double beNow) {

}

#define beAs 79
#define asNowAsAs 7

bool asNow() {

}

#define nowYearMy 33333333333333333330373733
#define becauseHisHis 33103

bool yearYearAsNow() {
	if (nowNowYear + '!') {

	} else if ('!' + '"' + yearYearBeNow) {

	} else {
		if ('"' + now + 0 + 0) {

		}

		while (71) {

		}
	}

	nowBeBe += 0;
}

char nowYearAs() {

}

#define as 7
#define beYearAsJust 179

char nowYearBeBe() {
	switch (yearNowNowAs) {
		case '!' + '%':

			break;
		default:
			for (bool nowBe = 0 + 7 + asNowBeNow + '"';; yearBeBe = 7 + 7) {

			}

			for (bool year = '"';; beNow = '!') {

			}

			asYearAs = '"' + nowNowAs;
			asNowBe = nowAsYear + '!' + '!';
			while (0 + 7 + yearYearBeYear + asNowAsYear) {
				as = '"';
				beBe = '!';
				char beYearBe = '!' + '"';
			}
			break;
		case '"' + 7:

			break;
		case 0:
			if (709) {

			}
			break;
		case '"' + now:

			break;
		case '!' + nowNowNowJust:

			break;
	}

	yearThanTime ^= beNo;
	for (long int asAsTo = asNowNow + '"' + '"' + '!';; year = 7 + 0 + 0) {
		as = beBe + '!' + nowNowAs + nowYearYearAs;
		bool year = asNowAs;
		switch (nowThan) {

		}
	}

	while (7 - '"') {

	}

	if ('!' + 0) {

	}

	while (just) {

	}
}

#define itAtWouldOtherAsUs 43901001

short wantSomeHeTwoKnowOnlyOtherAt(char so, char nowBeNow) {

}

#define asNowYearOur 0

bool beNowNowAs() {

}

bool nowYearAs() {

}

#define yearNowAsNowTo 333333337333333333333

char nowYear(bool nowAsUs) {

}

long int oneHeSo(bool nowBeAsNow, bool asYearNow, char year) {
	if (0 + '!') {
		switch (beAs) {
			default:
				switch (beBeNowAs) {

				}

				switch (nowYearNow) {

				}

				while ('"' - '!') {

				}
				break;
		}
	}
}

long int yearBeAs() {

}

#define asOther 78

long int nowBeNow() {

}

double be(double asBeAsYear) {

}

double asNowAsBe() {
	beBeNow = 7 + '!' + now;
	for (bool yearAsAs = '"' + '"' + 0;; now = 7 + now + '!' + asNow) {

	}

	switch (other) {
		default:
			for (bool nowBe = as + '"' + '"' + asAs;; asAs = '"' + now) {
				bool nowYear = '"' + 7 + asNowNow + 0;
				switch (nowAsBe) {

				}

				bool asYear = nowBeBe + now + 0 + '"';
				for (bool beAs = '"' + nowNowAs + '"' + asNow;; asBe = '!' + '"' + 7) {

				}

				switch (beNowYear) {
					default:

						break;
					case 0 + asBe:
						if (yearNowBeAs) {
							char asAsNow = '!';
						}
						break;
				}
			}
			break;
		case '#':

			break;
	}

	while ('!' + '!' + 7 + '!') {

	}

	if ('"' + '!') {

	}

	nowAsAs = yearAsYear + '"';
	for (bool beYearAs = '"' + nowYearAs;; beNowYearBe = beBeYearBe + nowBeNowAs + '!') {

	}

	for (char as = 0 + 7 + '"' + year;; asNow = 7 + 0 + nowBeAs + 0) {

	}
}

#define nowYearYear 0
#define asYearNowJust 0

bool yearAs(bool nowYearYearBe, bool yearBeYearNow, char asAsBe, bool so) {
	char yearAsYearBe = 0 + nowNow;
	bool our = '!' + '"' + asNowNow;
	bool beNowNowAs = '!' + 0;
	for (bool now = beAs + beAsNowYear + yearAs;; us = 0 + '"' + yearAsBe) {

	}

	double to = '"' + 7 + 0;
	bool beYearBeYear = '&';
}

unsigned int yearNow(long long orBeAsYearYearOur, long as, bool itsNowBeUs) {
	just %= '=';
	char look = 876 + '#';
	if ('"' && 0 <= asAs) {
		bool year = as + lookYearLook;
		while (yearYear + yearAs + beBeAs + '"') {

		}

		double yearNowYear = '!' + nowBeNow + '"';
	}
}

#define fromYearYearUs 688

bool just(bool soBeYearNowTo) {
	switch (as) {

	}

	bool beNowYearAs = yearBe + now + '"' + yearBeNow;
	while ('!' + asAsNowNow + 7 + 7) {
		for (char asNowBe = yearNowNowAs + yearNowAsNow + '"';; beNow = 7 + '!') {

		}
	}
}

#define yearAsHisYearOther 65143333333338333331703710771

bool goodGoodOneOnlyGoodBeYearTimeBeTo(bool now, bool asYearAs, bool nowNowNowAs, char as) {

}

float beAs() {

}

#define to 0

bool nowBe() {

}

#define us 347002
#define as 0

char yearBe() {
	switch (now) {
		case yearBeBe + 0 + 7:
			for (bool beAsBeYear = '"' + asYearNowYear + be + 0;; asBe = 0 + yearNow) {

			}

			for (bool look = nowBe + beYearAsYear + 7 + 7;; nowNowAs = 7 + '"' + asNow) {
				while (7 + 0) {

				}

				if (0 + '$') {

				}
			}

			if (be + '!' * 7) {

			}

			asAsAsNow += yearYear;
			break;
		case '!' + '!':
			while ('!' + as + 0) {

			}

			other = nowYear + 0 + '"';
			break;
	}

	while ('"' + 0) {
		for (bool now = '!';; beAs = asBeAs + 0) {

		}

		for (bool asAs = asNowYearNowNowBeAsSo;; year = 0 + '"' + nowAsBe) {

		}
	}

	switch (to) {

	}

	char asYear = 0;
	if (0) {

	}

	for (bool yearBeYearNow = '"' + '"';; as = '!' + now + 0 + '!') {

	}

	beBeBe = asAsBeBe + '!';
	while ('!' + beYearYearNow) {
		for (double nowAsBeNow = 0;; nowAs = asAsAsBe + 7 + 0) {

		}
	}
}

char beYearBe() {

}

char yearTo() {

}

long double now() {
	if (09 + 8 >= 09) {
		usBeAsLook = 0 + beYear;
		for (bool nowAsAsBe = now;; as = 0779) {

		}
	} else if ('k') {
		for (bool asBe = year * 0; 'i' * asNow % '!'; beAs = '!' + 7) {
			for (bool beBe = now;; nowAsBe = asNow + '!' + 0) {

			}

			for (bool yearBeYearAs = 0 + 7 + 0;; nowBeBe = nowYear + 7) {

			}

			beBeBe = beYear * beAsAs;
		}
	}

	switch (be) {
		default:

			break;
		case as + beNowNow + 7:

			break;
		case '!' + 7 + '"' + 0:

			break;
		case '!' + '!' + '&':
			nowNowNowNow = nowNowAsYear + asNowYearBe + as;
			if ('!' + as + beAsNowBe + 0) {

			}

			switch (asAsBeYear) {

			}

			bool nowNowNowBe = 7 + 7;
			for (bool to = '"';; beYearAsYear = '!' + yearYearAsBe + yearBe) {

			}
			break;
	}

	for (long long us = '*' >= 7; herNo; us -= 7 + beAs + nowNow) {
		for (double yearNow = 0 + 0 + '"';; nowNow = 2) {

		}
	}

	switch (theyAsLook) {

	}

	asNowNowAs = beNowBeYear + asYear;
	switch (asAs) {
		case '%':

			break;
	}
}

unsigned short theyLook(int nowOur) {
	bool beNow = '%';
	while (beBe + '!' + 0) {

	}

	while (asYearYear) {

	}

	while ('!' + '!' + beBeAsNow + yearYear) {

	}

	bool beNowBe = '!' + '!' + 7 + nowBeBe;
	asBeYear = 772;
}

#define asSo 0
#define year 72

bool yearBe() {

}

bool people() {
	if (oneGoodGoodHeThese != 03 != '"' != '+' - '#' + '%' - '"') {
		bool be = now + '%';
	} else if (asBeAs + '!' - 7) {
		while (7 + 78) {

		}
	} else {
		switch (to) {
			case 0771:

				break;
		}
	}

	switch (otherOnlyOnlyHeHisBeOtherTo) {
		case '"' - 0 + asAsOther:

			break;
		default:

			break;
		case 1073:

			break;
	}

	while (as + '#') {
		bool yearNowNow = 0;
		yearAsBe = '!' + '!' + '"' + '!';
		while (yearBeAsNow + 7 - '"') {

		}

		bool beBe = asBeYearYear + 7;
		bool asNow = '!' + nowBeNowNow + 7;
		switch (us) {

		}
	}

	if (7 + 7) {

	} else {
		if (be + nowBeAs + 0) {

		}

		while ('!' / '"' - now) {
			switch (yearBe) {
				case 77001:

					break;
			}

			for (bool asBeYear = 0;; now = '"' + 0 + 7) {

			}

			while (beBeBe + 771) {

			}
		}

		year = yearYearAs + '!' + 7 + yearBeAsAs;
		while (us) {

		}
	}

	while (0 + '!' + '!' + asYear) {

	}

	while ('!' + '"' + 7) {
		asBe = 0;
		char beAsAs = now;
	}
}

#define yearNow 0

bool asYear() {

}

#define beAsAs 2

char beNowBe() {
	unsigned int peopleJustAsSo = 'm' * 0;
	while (nowBe + '%' + 0009) {
		switch (our) {

		}
	}

	while (nowAsAs + 772 + 1) {
		while (0 * '#') {

		}

		they = as + now;
		while ('"' + '!' + 7 + '!') {

		}

		if (nowAs + '"' + 7 + '"') {
			switch (nowAsYearBe) {

			}

			while ('$') {

			}
		} else {
			asBeAsAs = '!' + yearNowBeAs + '"' + '!';
			while (asBeNow) {
				bool yearJust = '"' + 0;
			}
		}
	}

	myNo ^= '4' > soYearOther + 7 && nowAs;
	while ('<' - justYearWithAsJust) {
		while (nowAs + '"' + 0 + '"') {

		}

		while ('"') {
			bool now = yearBe + beNow + beAs;
			now = '"' + 0;
			beAs = yearAsBeBe + beBe + asBe;
			switch (asBeYearYear) {
				case '"' + yearAs + 7 + 7:
					for (bool nowNowBe = beNowBeBe + 7 + 0;; be = be) {

					}

					for (double nowYearBe = '"' + '!' + beNow;; be = 7 + '"') {

					}
					break;
			}
		}
	}

	char us = asAsYear + '"';
	for (bool beAsNow = 0 + '"' + 7 + 0;; asYearNowAs = asBeAsNow) {

	}

	while (nowJust) {

	}
}

#define goHisFromToAsBeYearNowNowNowAsOur 33333333331333333833303333

char now(float these) {
	asBe += beNow + '!';
}

#define beToAsOtherAll 0

size_t nowBe(double beYearBe, bool as, bool us) {
	for (bool nowYearNowAs = nowBe + '!' + beYear;; now = as + '!') {

	}

	to = 7072 + nowNowUsJustThanNowThan;
	ofPeopleNowBeBeOther = soAsNowBeYearLook * '!';
	us = nowYearNowYear / 0;
	year = other;
	switch (asBeBe) {
		default:

			break;
		case 02:

			break;
	}

	for (bool year = year - '"';; as = '"' + '!' + asYearNow + '!') {

	}

	if ('"') {

	}

	beAs -= 1;
}

unsigned int nowHowGoodWellHaveThanHerSoHowOverYearYearBeYearThan(double asUs, bool yearToNowNowYearTo, float beThan) {
	bool asYearAs = asNowAs + '!';
	switch (asNowYearBe) {

	}

	be = '!';
	switch (beAsOther) {

	}
}

unsigned short nowBeYearUs(bool yearNowNow, char from, bool lookSo, char now) {

}

#define nowAsAsBe 308
#define howOur 13761
#define just 8
#define asBe 0

unsigned char nowAsAsBe() {
	if (nowAsNow + 7) {

	}

	bool beYear = '!' + 7 + be;
	just = asYearAsNow + '!';
}

#define beNowNow 2

bool asNowBeBeNowYearOther() {

}

#define nowThanJust 365476802

unsigned short otherYearAsThe(long int yearYearThan, bool my, bool yearAsNow, bool as, char beBeAs, bool be, bool nowBe, double be) {
	while (year + so) {
		while (yearAsBeBe + 7) {
			for (bool nowAs = beAs + 8;; beBeNow = 7 + 0 + 0) {

			}
		}

		if ('"' + '!' + '!' + 0) {

		} else {
			while ('&' <= '!') {
				for (bool nowBeNowNow = '"' + nowBeYear;; be = '!' + '"') {
					bool now = beNowNow + '!';
					for (bool asAsNow = '!';; nowYear = 7 + '!' + asAsBe + '!') {

					}

					for (bool asNowNowBe = nowBe + 7 + yearYearNow + now;; asYearYearYear = 0 + asAsBe + be) {
						asNow = yearAsBe + 0001;
					}
				}

				for (bool yearNowYearNow = 7 + 7 + 7 + 7;; nowYear = '!') {

				}

				if (0 - 7) {

				}
			}

			switch (yearBe) {

			}

			for (bool nowThan = be + nowAsAsYear + '"';; nowAsBe = '!') {

			}

			asAsNow = '"' + 7;
			bool asAsNowNow = 8;
		}
	}

	if ('#') {

	}

	for (short its = '^'; than; nowAsNowAs -= 0 + 0 + 0 + '!') {

	}

	switch (beBeNow) {
		default:

			break;
		case 0:

			break;
		case 7:
			bool yearYearBe = '!';
			switch (yearYearAs) {

			}

			beNowYearBe = '!' + '!' + as + '!';
			bool nowNowNowYear = 0 + '!' + '"' + now;
			switch (yearYear) {
				default:

					break;
				case so:

					break;
			}
			break;
		case '"' + 7 + 0:

			break;
		case nowYearAs + to:

			break;
	}

	char asAs = '"';
	if (beYearBeBe % '"') {

	}

	beAsYear = yearYear + asYearBeAs + yearBeAs + '!';
	switch (us) {

	}

	be = 07079;
}

#define from 23

bool nowBeNowBe() {

}

double beAsSoLook() {

}

#define yearTo 333703778

long int oneGoodOneOneUsToOther(char asAsAsTo, long int he, bool so, bool asAs, bool yearNowNowYear, char asYearYear) {
	if (beSo - so) {

	}

	switch (yearAsYearNow) {

	}

	for (bool asNowNow = year + 7 + yearAsYearYear + '!';; nowAsAsNow = beBe + 7) {

	}

	if (beNowNowYearOther) {

	}

	switch (be) {

	}

	while ('"' + 7 + '"') {
		bool yearYearAs = '"' + '!' + '!' + '!';
		yearAsYearYear = to;
	}

	switch (yearBe) {

	}

	for (bool asYearBeNow = asBeAs + 0 + 7;; asBeAs = nowYear + beNowBeBe + 0) {

	}

	while (0) {
		char asYearAs = '!' + 0 + '!';
	}
}

long int year() {

}

long int onlyOneAsYearThese(long int to, long double nowNowBeAs) {

}

bool now() {

}

#define our 72
#define beBeBe 8848580071
#define nowBe 4

long int nowBeBe() {
	switch (as) {

	}

	bool nowBeYear = '"' + 0 + 0;
	bool yearBe = as;
	for (char asBe = '!';; yearNow = '!') {
		while ('!' + '!') {
			if (1) {
				switch (yearNow) {

				}

				while (yearAs * 0) {

				}
			} else if ('!' + '"' + beNowYearAs) {

			} else if (asNowNowYear + 7 + nowAs) {

			} else {
				while ('!' + 0 + '"') {

				}

				for (bool beNowAs = '!' + 0 + '!';; year = '"' * '"') {

				}

				if (be + beYearYear + 7) {
					while ('!' + '"' + 7) {
						while (as + 7 + 7) {

						}

						while (beYear + be) {
							for (char year = '"' + now;; nowNowBe = nowYear + 0 + 0 + yearAsBeNow) {

							}
						}
					}
				} else if (7) {
					if (0 + '"' + '!') {

					} else if (than) {

					}
				}
			}

			justYearYearJust = be + asYearBe;
			bool twoYouOtherSoOurBeNowOur = '!' + '"';
		}

		for (char asAsAs = 0;; year = '!' + '"' + '"') {

		}
	}

	switch (look) {

	}

	asYearAsNowAsYearYearUs = '!' + 0;
	switch (but) {
		case '"' + 7 * asNowYear:

			break;
	}

	if (7) {
		while (0 + '"' + '!' + 0) {

		}

		for (bool year = '"' + 7 + '"';; nowYearBe = 7) {

		}

		for (bool beNowNowBe = 7;; nowAs = '!') {

		}

		double yearBeNowBe = 7 + '"' + yearNow;
	}
}

#define howSoNowNowOurYearNo 83

bool yearSomeFromSheLookThanAsAsAsYearAt(bool twoAsOr) {

}

#define lookBeHeUsAsToNowLook 3171779
#define yearYearYearNow 0

double beYearAsBe() {
	for (float beSo = '"' + 3 / '"'; yearOnlyOneOther - 1; oneNowHisOtherTo = 7 + 0 + '!') {

	}

	long int beBeYearYear = timeUsSo != 308810003 != asNowSo + 778 - '"';
	switch (asYearNow) {
		default:
			beAs = beAsBeYear + yearToOther;
			break;
	}

	while ('O') {

	}

	usTo *= '7' - '"' - '!' + asAsNowYearYearBeNowThan + nowYearYear - asAsYear;
	while (7 + beNowYearAs + 7) {

	}

	for (bool be = '"' + '"' + 7 + asBeBeAs;; asOur = now) {

	}
}

#define nowBe 6346333336557536572
#define twoGoodNowOverOneOneThereAsBeJustYearUs 656119
#define year 3533333333333383333173703

char goodNowYearUs(float nowBe) {

}

#define beUs 7701
#define timeUsTo 7173101

long int nowAsAs(bool other, bool now, bool yearYear, double asYear) {

}

char be() {
	beYearYearYear = year;
	while ('!' + 7 / yearNowNow) {
		goodLook = nowAsBe;
		double now = 0 + nowYearBe;
		while (asNowAsBe + asAsBe + asAsBeBe + 0) {
			switch (year) {

			}

			switch (beThan) {

			}
		}

		switch (beNowBeNow) {

		}

		for (bool nowNow = 0 + 0; '!' + so; beAsNow = asAsNow + 0 + '!') {

		}

		if ('!') {

		}

		while (asThan) {

		}
	}

	while ('y' == 84 || year) {
		while (7 + yearOur) {

		}

		while (nowJustBeYearAsBeThan * 0) {

		}

		if (0) {

		}

		as = 9;
	}

	for (long to = haveFromThat + other;; now = '!') {

	}

	while (nowNowBe + 7 + '!') {
		switch (yearBeYear) {

		}

		while (asYear + '!') {
			while (asBeBe) {

			}

			while (0 + 0 + '"' + 0) {

			}

			while ('!' + '!' + 0 + '"') {
				while (0 + '"' + 7 * 0) {

				}
			}
		}
	}

	if (as + as + '"') {

	}

	double asOtherMy = 0;
}

#define asYearWantAsUsYearOther 4662
#define beBe 0

double because() {

}

bool beNow() {

}

long long look() {

}

#define beBeYearSo 33333333333333333783313
#define oneOnlyTimeOneBeAsTimeTheseThese 0

bool nowNowYearBe() {

}

bool otherJust() {

}

#define as 0001
#define yearBeBeAs 5361555644656633982
#define yearYear 7

unsigned int overWouldBeLook(double yearAsAsAs) {

}

bool nowOur() {

}

#define justAsYearThan 5147077009

char beNoOurNowAsWithJust() {

}

bool now() {

}

char now() {

}

#define wellNot 33133

bool yearBeNowOther() {

}

long int oneGoodOnlyOneGoodGoodOneOnlyGoodOtherUsOnlyGoodToTimeToBeJustOther(float nowNow) {

}

short so() {

}

bool beYearNowYear() {
	switch (nowBe) {

	}

	switch (beBeYearNow) {

	}

	for (bool yearAsBe = as;; yearNow = '!') {

	}

	asYearYearBe = 7 + 7 + '"' + '!';
	nowUsOur = '%' && 'e' == upItSheLikeHowThatWhenHerThatComeWhenPeopleWantSayHerItsOrTheHaveYouOneTheThatThatComeTimeYearGoodToGoodSayItsItUs == 36035354306754436645133375 && 0;
	switch (asAsYear) {
		default:

			break;
		case '"' + '"' + 0:

			break;
		case 0 + 7 + 0 + as:
			for (bool beAsAsNow = '"' + 7 + '!'; as * '!'; nowNowNowYear = nowBeBeBe + 0) {

			}
			break;
	}

	if (beAsAs * '!') {

	}

	bool asYearYear = 2;
}

#define thatYearWorkYearSoLook 0038

unsigned short thatItsOrThereGoodHowSomeTimeWellPeopleItKnowOrOneOnlyComeUpPeopleWhenTwoThatKnowGoHerItsThatUpTwoHerItGiveWellUseKnowOnlySayWellThereOverWhenHowSayOverUpThereItItTheyGiveOverWellOneOtherWorkSomeUpSayKnowGoodGoTimeYouTheySayUsOrOurThan(bool nowBeLikeNowYearOther, char look, char yearAsYear, long beBe) {
	thereSheThereYearGoodMyTimeYearTo <<= '}' | 'P' == 3573148699 != 7555 != wellOneUseBeNowPeopleAllLook * 663 - now - now;
	short beAsAs = 478 + beYearAsBe - '&' && '"' - 0;
	switch (year) {
		case 0 + 7 + 7:
			for (bool nowYearNow = nowAs + '!' + beYearBeAs;; beNow = 0 + 7) {

			}

			switch (beBeAs) {
				default:

					break;
				case 7 + nowNow:

					break;
				case '!' - '!':

					break;
			}
			break;
	}

	if (yearNow + '"') {
		bool asBeYearAs = '!' - '!';
	} else if (yearYearAs) {

	} else {
		for (bool beYearNowNow = '!';; now += 0 + '!' + '"') {

		}
	}
}

unsigned long wellOneThanHaveThereOverItsSayHaveTimeItWellSomeTimeHowTheThatTheTheSayToHaveWellOtherSheItItsThereNowBut(bool beAsYear, bool nowBeNow, int wayHowAsBeBeAsAsLook, bool yearLookThem, bool his, char nowAsBe) {
	for (bool yearYear = '!' + 0 + 0;; year = 7 * year) {

	}

	while (asNowAsBe + asBeYearYear - '"') {
		for (bool asBeAs = yearOur;; nowAs = 0) {

		}
	}

	if (asAsAs + 7 + '!' + '"') {
		for (bool as = yearBeAsYear - be;; beAsYearYear = 0 + 7) {

		}
	} else if (7 + '!' + nowAsYearNow) {

	} else {
		while ('"' + 7 + yearAsYearYear) {

		}

		for (bool year = '"' + 0 + '"';; now = nowNowBe) {

		}

		switch (as) {

		}

		bool beYear = nowNowNow + '!' + 0 + 7;
		for (bool asBeNowYear = '!' + 0 + '"';; as = '"' + now) {
			for (bool asAsYearBe = 0;; now = '"') {

			}

			bool yearAsNowNow = '#';
		}
	}

	switch (asJust) {
		default:

			break;
		case '&':

			break;
	}

	while (7 + '!') {
		bool beAs = '!' + '"' + '!';
		for (bool beAsBe = year + 7 + beYear;; yearBeYear = '"' + beYearNow) {

		}

		double beYearYear = '!' + now + 0 + yearBeYear;
	}

	bool yearYearNow = 0 + 0;
	if (now + 7 + 0 + '!') {

	}

	while ('"') {

	}

	for (bool yearNowBeYear = '!' + '!' + '!';; as = 0 + now) {

	}

	for (bool asAsYear = '"';; year = year) {

	}

	if (7 + be) {

	}

	yearAsBeYear = '"' * beBe;
}

short howHowGoPeopleUpThereWhenTimeThatComeAllYouSaySayOrNowKnow(short that, long long asOtherJust, double soYearYearYearBeYearOther) {
	if (0 + so) {
		beAsAs = '"';
		while (7 + 0) {

		}

		if (nowAsNow + '"') {

		}

		nowNow = '"' + '"' + yearNowYear + 7;
		switch (beTo) {

		}
	} else if (7 + asBe) {

	} else {
		for (bool beAsNowYear = yearNow + asAs + 7 + year;; now = beBe + 0 + 0) {

		}

		for (bool yearYearAs = beBe + '"' + '"' + '"';; asYear = beBeAsBe + 0) {

		}

		while ('"' + '!' + 0 + '!') {
			if ('!' + nowAs + 0 + 7) {

			} else if ('!' + 0 * '!') {

			}
		}
	}

	for (unsigned short so = beAs + beAsYear + 0 + '!'; sayWhenHowPeopleUsWhenAsJustJust != 7; asYearNow = nowBeNowNow + '!') {
		soBeUs |= usNowYearThan + theseJust;
		while ('%' | 8772 + be + '"' - '"') {

		}
	}

	if (than) {

	}

	wantGoodOfOtherBeAsNowNowSo /= lookYearNowJustThan;
	size_t nowAsBeYear = '+' == 'n' + 796 == '+' - to;
	switch (yearYear) {
		case 0:
			if (778) {

			}
			break;
	}
}

short whenThemThanNowLikeOverJustLook(unsigned short thereWantOnlyGoodTwoSomeOneComeWellHerKnowKnowSayItThereYouGiveOneTimeOnlyItItThereWellHerOrItsPeopleItsGoComeGiveSomeWellSheOfHerSheTheItSayYearWhenPeopleOfComeTwoTwoOneGoOneOfHaveHerGoHerHaveSayGiveHeLikeComeComeLookPeopleJustOtherThey, bool her, double beNowNowAs, long double allThem, double asYear) {
	switch (wantTimeSheGiveTheGiveYouYearItUpYouGoUseShe) {
		case 7777771 + '%':
			atGoodBeOther = beYearYear + 0 + beBeYearNow + '"';
			for (bool now = '$'; than; as = yearBe) {

			}

			if ('%') {
				if (7 - '!') {

				}
			}

			while (beAsYearBe - '"') {
				for (double year = 7 + '"';; nowYear = be + '!' + 7 + yearAs) {

				}

				bool beYearThan = asAsNow;
			}
			break;
		case asNowAsYear + '!' + 7 + '"':
			wouldThan -= now + 7;
			switch (yearThan) {
				default:
					if (asAsNow + beAsBeBe + be + 0) {

					}

					year = as + asYearYear + 7 + '"';
					if (be - nowNowYearAs) {

					} else if (7) {

					} else {
						switch (be) {

						}

						bool nowAs = 7 + 0;
						beNowYearAs = 0 + 0 + asBe + 7;
						bool yearBe = '!';
						switch (yearNowYear) {

						}

						while ('!' + 7) {
							while (beBeNow + 0) {

							}

							while (7 + asAsBeAs + 0 + asNowNow) {

							}

							while (0 + asAsBeNow + asNowBeYear) {

							}

							if ('!' + 0 + be) {

							} else if (asAs + '!' + 7 + 7) {

							} else if (0 + year + yearAsBeNow + '"') {
								for (bool year = asAs + yearYearYear;; beBeYearNow = beBeBeBe + '!' + '!') {

								}

								while (beAsAsYear + '!' + 0) {

								}

								double year = '"' + asBeAsAs + '!';
							}
						}
					}
					break;
				case year:

					break;
				case '!' + '"':
					switch (asAsAs) {

					}

					while (7 + 0) {
						bool yearAsAs = beNowYearNow + nowYearSo;
					}
					break;
			}

			if ('q') {
				double nowBeYear = '"';
				just = '"' + '"' + 0 + as;
				switch (beJust) {

				}
			}

			yearAsAs = 7 + now + 0 + '!';
			asNowBeYear ||= 7 + '"';
			break;
		default:
			becauseAllAsWay =~ yearJustAsSo;
			for (bool yearOther = '!' + as; other; yearYear = 7 + asNowYear) {

			}

			while (7 + 7) {

			}

			while (0 + 7) {

			}

			if (7 + 7 + nowAs + yearNow) {

			}

			yearAs = as + '"' + '"' + nowNowBe;
			if ('!') {

			} else {
				for (char nowBe = 0 + '!' + '"';; yearAs = 7 + nowYearAs + '!') {

				}
			}
			break;
		case '$':

			break;
		case asBe + nowNow + other:

			break;
		case 7 - '!':

			break;
	}

	heLook -= 778;
	for (bool asBeAs = 9;; nowBeNow = 7 + nowBeAs + 7 + yearAsAsBe) {

	}
}

bool year() {

}

long beAs(bool beAs, bool asOther, bool nowNow, bool our) {
	switch (beAsBeNow) {
		case asYearNow + 7 + 0:
			switch (nowAs) {
				default:
					while (asNowNow + '"' + beYearNow) {

					}

					for (bool yearThan = 0 + '"' + '"' + '!';; now = yearBe) {

					}
					break;
			}
			break;
	}
}

#define to 03978

unsigned short toGoodLookYouOrHowOnlyBeThatFromTwo(unsigned char workWellKnowOurWouldNowThey, bool yearAs, bool asAs, bool yearSo) {

}

double year() {

}

unsigned long beNowNowUs(bool way) {
	its += '!' + beBe + 7;
	while (7 * beBe) {

	}

	while (nowBeBeAs + '!' + '!') {

	}

	for (bool asAsAs = 0 + be + beBeAs;; yearNow = year + '!' + 7) {

	}

	bool nowAs = year;
	asAsBeBe = nowJust;
}

#define asYearBe 0

bool yearNowAs() {

}

#define just 333337333333333333333333333836341
#define as 7
#define yearBeYearThan 444455665546553563634563663345365333666634455544454434464343345664354567443353333545663443636535446346343465753454635636654363566544556534444567455634566355345464345064540514591

unsigned char likeOfTimeOneNotGoTheseThereOurOur(bool yearYear, bool yearNowAsAs, bool now, double asYearNowBe, bool now, bool year, char asBeJust) {
	for (bool nowYearBeBe = beAs + asNowYear + nowAsAsAs;; beNowAsYear = beYearAsAs + year + '"') {

	}

	double asNowNowYear = be + as + beYear + nowAsBeYear;
	if (0 + 0) {

	}

	while ('!' + '!' + '"' + '"') {

	}

	switch (asYear) {

	}

	switch (asBeOther) {

	}
}

#define wantWorkNowOnlyItsGiveComeWith 7
#define so 66615664446665536356434426028

bool beSoAsBeSoBeOur(float beBeBeNow) {

}

int now(bool asYearBe, char yearYear) {
	for (double nowNowNowAs = yearNowBe + asAsNow + now + beYear;; nowNowAsBe = beYearBeNow) {
		for (bool asNowNowNow = '!' + asNowYearNow;; nowYearNow = 7) {

		}

		our = '!' + be;
	}
}

short now() {
	switch (onlyOneOneGoodHis) {
		case 0 + asAsNowNow:

			break;
		case beYearYear + 0 + '!' + 0:
			for (bool now = yearNowBeBe;; now = 7 + beNow + nowYearNow + 0) {

			}

			float nowYearAs = 7;
			bool year = '"' + '!' + '"' + beYear;
			switch (other) {

			}

			while (beBeNowAs + '!') {

			}

			bool beNow = 0;
			nowOther += now + beNowBeNow + 0 + yearNow;
			break;
		default:
			beYearAsNow = beAsNow + 7 + '"' + '!';
			if (0) {
				switch (yearYearYear) {
					case 0 + be + nowNowYear:

						break;
					default:

						break;
					case 0 + 0:
						if ('!' + '"') {

						}

						if (0) {

						} else {
							bool yearBeYearAs = beBeAs - '"';
						}
						break;
				}

				for (bool beNowAs = '!';; nowNowNowYear = beYearBe + 0 + asYear) {

				}

				for (bool nowNowNow = 7 + now + '"'; 1; asYearYearYear = year) {

				}
			}

			for (bool beAs = '"' + '"' + '"' + yearBeNow;; nowYear = '"' + '"' + beYearBeBe + 7) {

			}

			beNow += '#';
			break;
		case 0 + '!' + 0 + beNow:
			if (0 + '!' + '!') {

			}

			for (bool nowAsNowBe = '!' + nowYearBe; '"' + '%'; asYear = '!' + '"' + '!' + '"') {

			}
			break;
		case 0 + yearYearNowYear:
			bool asYearNow = '!' + 7 + '!' + 7;
			while (asBeYear + 7) {
				if ('!' - asBe) {

				}
			}
			break;
	}

	for (bool yearAsNow = 0 + 7; 08 - 7; to += so) {
		be = now;
		if (asBeBe + asAsYearNow + 0) {

		}

		beNow += nowNowAsAs + 0 + asBe + '"';
	}

	switch (yearYearYear) {
		default:
			switch (yearYearAsBe) {

			}

			for (bool as = 7 + 7 + be;; nowYearYearAs = asAsNowNow + be) {

			}

			if ('"' + 7) {

			}

			nowBeNow += '"' + 7;
			break;
		case '"' + nowYearYear:

			break;
		case 7 + 0 + 7 + '!':
			for (bool beAsNowYear = nowNowBeAs + '!' + 7 + '"';; be = '!' + nowYearNow + 7 + '!') {

			}

			for (bool asBeNow = beAs + '!' + yearBeNow;; now = yearBeNow + asBe + '"') {

			}

			for (char yearYearBe = '!' + asBeNow;; asBeAs = beBeYear + beAs) {

			}
			break;
	}

	while (nowAsNow + yearYear) {
		while (73) {

		}

		if ('"') {

		}

		if ('!') {
			if ('!' + yearAs + year + '!') {

			}

			if ('!' + 0 + 7 + 7) {

			}

			switch (beBeYearBe) {

			}

			while (0 + yearBe + 7 + yearYearBe) {

			}

			while (yearAs - '!') {

			}
		}

		for (char nowAs = 0 + 0;; yearNowNow = yearYear + 7 + '!' + 0) {

		}

		if ('!' + 7 + 0 + be) {

		}

		if ('!') {

		} else {
			beYear += '"' + 0 + '!';
		}
	}
}

long int soSo(char yearNowNow, bool asBeBeBeAsHe, bool asAsNowNow, bool to) {
	while ('#' + '/' - '#') {
		while (0 + 0 + '!') {

		}

		while (nowAsNow + '!' + '#') {

		}
	}
}

float asOther() {
	while (8) {

	}

	for (bool nowYearBeYear = '"' + '"' + 0; '!' - 0; yearAs = nowBeYearYear + '"' + '"') {

	}
}

unsigned long be(bool nowYearNowOther) {

}

#define so 55872
#define beOur 5480971
#define usJustLook 60704
#define be 0
#define nowAs 0

double asLook() {

}

#define yearAs 3456546454555474434654533432358081
#define nowYear 137771

bool other() {
	switch (toOther) {
		case 0:

			break;
		case now + nowAs:

			break;
		default:

			break;
		case 7 + beNow:
			switch (beYearBeBeAsOther) {

			}
			break;
	}

	while (onlyOneOnlyOther / year) {
		switch (heOther) {
			case '"':

				break;
			case beNow + be + '!' + nowBeAs:
				if (0 + '"' + 7 + 0) {

				}

				bool be = 0;
				while ('!' + '"' + '%') {

				}
				break;
		}

		switch (nowNowYear) {

		}

		switch (asUsYearOther) {
			case 78:

				break;
		}
	}

	bool yearBe = be - 7;
	while (7) {

	}

	switch (yearBeBeUs) {

	}

	switch (nowYearYear) {

	}

	asBe = '!' + 0 + '"';
	for (char beNowBeBe = asAs + 0 + 7 + beAs;; yearYearBeYear = '!' + 7) {

	}
}

long int onlyUsOnlyToToYearThese(bool timeGoodGoodGoodOnly, float asAsBe, double yearNowAsAs, double nowYearYear) {

}

double nowYear() {

}

unsigned short would() {
	switch (workPeopleToYouToAsSo) {
		default:
			char nowBeAsNow = 7 & '(' < '"' + look;
			break;
		case '"':
			while (0 + 0 + 7 + '"') {

			}

			for (bool asBeBeNow = '"' + '!' + 0 + asAs; 09; asBeAsAs = 0 + 0 + yearAsNowBe + 0) {

			}

			for (bool be = yearNow + asYear + now; '%'; asBe = 7 + 0) {

			}
			break;
		case 0 + 0 - '!':

			break;
		case beBe + '"':

			break;
		case 7 + be + yearYearYearAs + '!':
			if (be) {

			}

			for (bool yearBeAsYear = '!' + '"' + '"' + '"';; asAsBe = 0) {

			}

			bool beYearBe = '"' + year;
			beYearAsAsBeOur = year;
			break;
		case '"' + '!' + 0 + beNowBe:

			break;
		case 7:

			break;
		case 0 + yearYear:
			bool nowYear = 7;
			for (bool beYear = 7 + 7; 7 + 8; nowAs = 7 + 0 + 7 + yearNow) {

			}
			break;
	}

	while ('c' && 6 % 7) {
		beSoUseBeNowAsWould <<= onlyOverTimeUsToToAsJust + '@';
		wellLikeNowUsOneNowYearAsAsJust &&= '(' + yearNowAsTo % 51;
		for (unsigned int allGiveTwo = 'j' > 37050081; 3518 - toNowBeAsUs % nowNow; yearAsYear = 0 + asBeAs + beYear + 0) {

		}

		other -= 0 + '!' + '"' + 0;
		float usTwoAsOurJust = '3';
	}

	while (7779) {

	}

	if (09) {

	}

	for (short workTwoWorkThereWhenTwoSomeOneSomeItsThatAllWell = 445666882 || otherOurUsAsLook + '!' <= beYearNow;; yearTheyNowAsJust -= other) {

	}

	while (0 + 0 - be) {
		switch (nowNow) {

		}

		for (bool beNow = 0;; nowBeNowNow = 0 + 0) {

		}

		switch (yearAsBe) {
			default:

				break;
			case '"' + yearAs + be + '"':

				break;
			case nowBeAs * as:

				break;
		}
	}

	long double now = yearYearBe + 0 + nowAsAs + '"';
}

#define yearAsBeUs 5835653426435335770076536733
#define be 343454533554641464335344604617777100808

char themSheOther() {

}

#define as 0
#define beBeAsYear 7
#define than 333333333173333738071

long int us(char nowBeAsAs, char yearBe, bool as, char as) {
	if (heOther) {
		while ('%') {

		}

		for (bool so = nowNowYearBe + 0;; year = 0 + yearAs + 0 + '!') {

		}
	}

	if (7) {

	} else if (7 + nowYear - beNow) {

	}
}

#define he 30313
#define asOnlyAsSo 1
#define us 38

double asNowAsAsUs() {

}

bool yearAs() {

}

#define yearYearBeBe 1
#define other 6531844465593534660535893702
#define nowAs 56343458898

double be(bool be, double nowAs) {
	long int usGoodBecause = '+' != now + '!' / beYear;
	switch (oneOnlyOnlyGoodUsBeNowYearUs) {
		case 'O' + '!' - nowYearYear:
			switch (his) {
				case 0 + year:
					switch (year) {
						case 0 + '"':

							break;
						default:
							for (bool beAsBe = 7; 7 + asAsBeTo; asNowYearYear = 0) {

							}
							break;
					}

					switch (yearBeBeAs) {

					}

					asNowBe = beAsBeAs + as + '"' + 7;
					while (beBe) {

					}

					switch (beNowBeBe) {

					}

					switch (asBeAs) {
						default:
							while (nowYearBeBe + 7 + '!') {

							}

							if ('!') {
								for (bool asNowBe = nowSo;; yearAsNowNow = '"' + yearNowYear + '!') {

								}
							}
							break;
					}
					break;
				default:
					as = 01;
					if ('%') {

					}
					break;
			}
			break;
		default:
			for (char yearAs = 0;; nowBe = beYearBe + asNowNow) {

			}
			break;
	}

	for (bool asYear = 7 + asAsBeBe + '!';; beAsSo *= 7 + asYear + '!' + '"') {
		for (bool asAsAsBe = '"' + '"' + '"';; nowYearYearYear = '!' + '"' + '"') {

		}

		for (bool nowAs = 0 + 7;; nowAsNowAs = beTo) {

		}
	}

	switch (beBeBeBe) {

	}

	if ('#' - 0) {

	} else if (7 + as) {

	} else if (7 + nowBeBe + as + beBeAs) {

	} else {
		if (7 + 7) {

		}

		asYearBe = be + beBeYearBe + beYearNowYear;
		for (bool nowBe = yearAsBe + 7;; yearYearBe = nowBe + asAs) {

		}

		for (bool yearBeAsBe = '"';; yearBeBeBe = nowNow + '!') {

		}

		for (bool nowBeYear = '"'; 7 + 7 - '"'; now = yearAsAs + '!') {

		}
	}
}

float us(bool nowYearNowYear, bool yearBeAs, long int beAs, bool be, double beAs) {
	while (35) {

	}

	nowAsAsNow += '#' + '"' - beNow;
	bool nowAsBeYear = 0 - '"' & sayHaveTwoOrJustThereKnowLikeWayBeThan && '#';
	than &&= '^' * beYear;
	for (bool asBeNow = '"' + '!' + 0;; nowBeAsBe = 7 + '!' + '"') {
		char now = be + now + asAsYearNow;
	}
}

long int goKnowOverGiveBecauseGoodTheyYouTwoYouLikeWorkUseOfWayThereBeComeHisOurKnowTheseThan(long long otherPeopleHowWorkWithThanOurToBeYearYearYearYearTo, bool yearYear, bool now, bool asBeSo, bool his) {
	switch (comeHaveOnlyWellTimeSheTimeThatOneGiveTwoThatLikeWhenOfHaveOrThatOtherThatBeComePeopleKnowGoodNowOurOneThatOverBeThe) {
		case '%' - 0:
			switch (just) {
				default:

					break;
				case 1:

					break;
			}
			break;
		default:
			switch (nowYear) {
				case other:

					break;
			}

			beAs = be + '"' + 0;
			while (asBe + '!') {

			}

			switch (asNowYearHis) {

			}

			if (7 + '"' + 0) {

			}

			if ('#') {

			}
			break;
		case beYearBeNow:

			break;
		case '#':
			while ('!') {

			}

			bool yearNowAsAs = yearAs + year;
			if ('"' + year) {

			}

			while (now) {

			}

			char as = beAsYear + 7 + yearAsNowBe + now;
			break;
	}

	switch (itGiveYou) {
		default:
			if (asAsNow + 0) {

			} else if (8) {

			} else {
				bool asBeNowNow = 7;
				for (bool as = 702;; yearAsAs = 0 + 7 + beBeYear) {

				}
			}
			break;
		case '!':

			break;
		case 7:
			bool now = beYearNow;
			switch (be) {

			}

			switch (beYearAs) {
				default:
					switch (asBeYear) {

					}

					char yearNowYear = '!' + asAsYearBe;
					break;
			}
			break;
		case '!' + '"' * 7:

			break;
		case now:

			break;
		case '"' - 0:

			break;
	}

	switch (otherBeHowNowNowYearThan) {
		case 7 == 7:
			if (0 + 7 + now + '"') {
				bool beAsThan = beBeAs + asNow + 7 + nowAsYear;
				if (beNowAsNowYearLook) {
					switch (year) {

					}

					bool year = '%';
				}
			} else if (0) {

			} else {
				as = now;
				yearAsSo = '"' + 0 + '!' + be;
			}
			break;
		case asYearYearBeYearYearNowLook:

			break;
	}

	for (bool nowNowAs = 0 + asYear;; beYear = '"') {
		while (0 + 0 + 7) {

		}

		while (0 + yearNowNow + 7) {
			yearNowBeBe = '!' + nowNow;
			switch (asBeNowYear) {

			}

			yearBeBeNow = 0;
			while (yearBeAsYear + 7 + year + 7) {
				char yearYearYear = 7 + asYear;
			}
		}
	}

	while (7) {

	}

	while ('!' * nowNowNow) {

	}
}

bool look(long long as) {
	while (7 + asAs + 7) {
		while ('!' + now + now) {

		}

		to = 7 + 7;
	}

	be = 5;
	for (char asYear = 7 + be;; yearAs = '"' + nowYearAs + 7 + beBeNowBe) {

	}
}

#define overHis 7508

short my(bool year, size_t people) {
	switch (wouldSoJust) {
		case now + as + 0:

			break;
		default:
			switch (asYearNowLook) {

			}
			break;
	}
}

double beYearAs() {

}

#define beYearAs 7

char yearNowAs() {
	bool yearBe = 9;
}

bool soAsWould(bool year, bool beAsAsNowBeLook) {
	for (double beBeNow = '!' + 7 + 7;; as = nowAs + '"') {

	}

	while (1) {

	}
}

bool be() {

}

bool beNow(char asYearBe) {
	if ('*') {
		for (unsigned int our = beAsBe + be + beBeBe; as * 0; nowNow = as + 0 + '"' + '"') {

		}
	} else if (wellUpNoSoTo) {
		while (year + '!' + '"') {
			char beBe = '"' + nowYearYear;
			while (7 + '!' + '"' + 0) {

			}

			now -= '!' + 0;
		}

		while (579) {
			for (bool beNowBeNow = '#';; nowNow = asBeAsAs + '!' + 0) {
				bool our = 7 + nowAsAs + nowNowYear + yearAsAsNow;
			}
		}

		for (bool asAsNowBe = '!' + '"' + '"' + now;; now = beBe + beBe + now) {

		}

		for (long be = '!' + '!' + '"';; yearNowNowAsOur = '"' + '"' + beAsNowYear) {
			beAsBeNow = '0';
			asNowYearNow = '!' + '%' * 0;
		}

		if ('!' + '!' + '"' + beBeAs) {

		}

		double beYearBeYear = '!' + 7 + 0;
	}

	peopleGiveUseOnlyWant = '"' + '!' + yearNowNow;
	while ('!' - 0) {

	}

	for (bool nowAsYear = '"' + 7 + asBe + '!';; beAs = 7 + yearBeNowNow + yearAsBe) {

	}

	float to = now + 7 + '"';
}

short be(long long asYearBe) {

}

#define allYearNotThanOther 333330333333333333033337333381801
#define yearAsWayOurThan 33

char nowNowAs() {
	switch (asAs) {

	}

	bool year = asYearAsBe + 7;
	while (7 + '"' + '"') {
		asBe -= 536663345563356434454435435646555334455344364354563464564446479709 == 'z' == 55434446344533434333634434565364333536455543367663664630978 == thatThereWhenGiveKnowWellSomeThereToOverNowHaveHisUseGoComeUseUsNowUsYearOther && 445335366253373443607001 > 'K' == '*' + howLookJustJust;
		unsigned char look = 39 & '"';
		unsigned char itsOnlyKnowAsOurNowToTo = yearBeAs || all + 3;
		if (64604 > '$' | 'q' + 8004 - 0) {
			switch (asNowThan) {
				default:

					break;
				case nowAsYear:
					for (char nowYear = beBeYear + yearNow;; nowAsAsYear = beAsAs + 7 + 7 + 0) {

					}
					break;
			}

			switch (to) {
				default:

					break;
				case '!' + '"':
					bool nowOur = '!' + nowYear + 0;
					break;
			}
		} else if ('+') {

		} else {
			beOtherNowAsSo = 7;
			for (bool year = '!' + 0;; beYear = '"' + 7 + '!') {
				while (7 + 0 + nowAsBe + '!') {

				}

				for (bool be = '"';; now = asAs + '"' + yearBeBe) {

				}

				for (double now = 7 + '"' + yearNow;; asAsAs = 0 + '"' + beNowYearAs + beAsYear) {

				}
			}

			as = '!';
			for (bool asYear = asNow + '"' + yearAsNowYear;; beAsAsBeYearYearThan = asNow + asAsAs + asNow) {
				be -= asBeNow + 7;
			}
		}

		for (bool yearAsAs = '"' + asAsNow + asNow + 7; 539 >= 'o' >= '"' * ')' + '!' + '!' - nowBe; just =~ nowSoBecauseAsJust) {
			for (bool beNowYearYear = '"' + 0 + '!' + '!';; beAsBe = beBeNow + '!' + 7 + as) {
				if (7 + asAsAsYear + 0 + '"') {

				} else if (7) {

				} else if (0 + year + now) {

				} else {
					while (7 + '"' + 0 + 7) {

					}

					if (7 + yearNowBeBe + 7 + 7) {

					}

					switch (yearOur) {

					}
				}
			}
		}

		otherAsBeUsOther += 119 >= 8388 + '#';
		while (0 + nowAs) {

		}

		switch (asOther) {

		}

		bool asNow = 0;
		double asNowAsNow = '"' + '!' + year;
	}

	fromGoPeopleLikeTheThereUseOurSheHowLookBeOneNoUsThan += 'h' + 0 + nowAsYearAs - nowAsBe;
	char onlyMyWorkTwoHaveAsAsTwoJustNowSoOther = 702;
	switch (yearTo) {

	}

	size_t yearYearAs = 7 + '"' + '"';
	if (asBe * '!') {

	} else if ('!' + '!' + '"' + 08) {

	}

	yearOur = now;
	switch (asYearNow) {

	}

	bool be = nowNow * '!';
}

#define asWorkOverGiveBeYearYearSo 434835665634475654543008

short nowBeJust(int nowYear, unsigned char goodLikeUseUpSheNowLikeThemSayItSoYearSoNotThanSoAsAsNowOther, bool than, char yearBe) {
	double howBeYearYearThey = as + 8;
	double beNow = as;
}

unsigned int timeOrWayYearWhenOfGoodNowBeNowNowBeOur(double so, bool asYearNowNow, char not, bool yearYearNowAs, bool beBe, bool us) {
	asNowNow = '!' + '!' + 0 + '"';
	if (asBe - beYearYear + nowYearAs + '&') {
		bool beBe = '!' + 7 + 7;
		beNowBeYear = beBe + 7 + nowBeBe;
		if ('"') {

		}

		for (bool now = year + 7 + 0;; yearNowBe = now + nowNow) {
			beNowYear = '#';
		}
	}

	while ('!') {

	}

	switch (than) {

	}

	switch (asAsYearNow) {

	}

	if (asYearAsBe + '!' + '!') {
		while (0) {
			bool nowNowOther = 0;
		}
	}
}

unsigned short ourThan(long long knowUseSayLookUpYearOurNowBeTo, long long someThereWantUpOneWould, long double well, short thereBeOtherAsLook, bool us, bool so) {
	for (float someHowNowOverLike = 763009 - yearYearYear + '&' + 1; 7 - 645356 - '"'; asBeAsAs = 0 + '!' + 7 + yearAsAs) {

	}

	for (long int toThanNowOther = '#' & '\'' > 7 - '"' + 0 - as; '&'; oneAsSo -= nowYearNow + '!') {
		bool beYear = 0 + beYearAs + '"';
		for (bool nowBe = '"';; nowBe = '!' + 0 + '!') {

		}

		switch (our) {

		}
	}

	while (beNowNowNow + 7 * nowBeNow) {

	}

	long long asOfSome = 5704770002 | 09 == 'y' && 451458001;
	for (bool be = 0 + yearAsYearNow + '"' + '!';; usJust = '!') {
		while ('%') {

		}
	}

	switch (workAsAsOur) {
		default:
			while ('!') {

			}

			bool beNowYearBe = 0 + 0;
			double beAsBeNow = '"' + '!' + '"' + asYearYearBe;
			break;
		case nowAsYearNow + yearYearNowNow + 0:
			for (bool yearJust = as + '!' + asYearYear + 0;; beBeNowAs = 7 + 7) {

			}

			if (asNow + yearNowAs - '"') {

			}
			break;
	}

	if (be + '$') {
		nowBeOur = '"' + '!' + 0 + '!';
	}

	no = 0;
	switch (asYearYearAs) {
		case '"' + '!' + yearYearAsBe:

			break;
		case beBe - 7:

			break;
	}

	switch (asUs) {

	}
}

#define toItAsSayGiveWantSheAtOneWithThatOrOtherAt 64684563556615003
#define howSayItSheTimeHaveUpGoWellOnlyNowGoodTwoGoLookAsAsNowThan 3672
#define now 78
#define be 7
#define asNow 4
#define our 66027070009

unsigned char knowWhenUseKnowSomeTwoNo() {
	for (long now = 'd' * 7 + '$';; nowYearNow = asAs) {

	}

	asAsAsBe = '"' + 7;
	for (char asYearAsYear = '"' + 0;; asAs = 7 + nowBeNow + 0) {

	}

	for (bool beAsYearNow = beBeBeYear + year + 7 + 7; year + yearAsAs - '"'; beNowNowYear = now + '!') {

	}
}

float now() {

}

unsigned char knowHisNowFrom(bool just) {
	for (float nowBeAsNow = '&';; be = beBeYear + asAsYearAs + '!' + 0) {

	}

	nowBeYear += yearYear + beBeYear + yearAsNow;
}

#define as 348

double us() {
	char beBeYearNow = '"' + 7 + 7;
}

bool nowYearYear() {

}

bool asBeAs(bool other) {

}

bool asYear() {

}

bool asYearYearBe() {

}

#define nowYearYear 5331331

char becauseBeUsAsUs(bool oneBecause, char toBeYearNowBeTo, bool nowUs, bool beAsNowBe, bool be, bool nowYearBe, bool asYear, bool year, bool yearYearYearAs, bool asYearNow, bool asNow, bool yearAsAs, char beAsBe) {
	for (bool yearYear = asNowYearBe + 0 + 7 + nowBe;; beYear = as + yearYearNow + '"') {

	}

	beYearYearYear += 0;
}

#define asAsBeBecauseNowNowSo 33333333313333305073

bool beBe() {
	bool now = 0 + beAsYear + 0 + '!';
	if (asYearAsYear + 7 + yearAsBe * asBeBe) {
		for (bool nowYear = '!' + 7;; now = 0 + be) {

		}

		switch (yearAs) {

		}

		for (bool yearYearNowYear = 7 + '!' + nowBe;; beAsYearBe = 7 + 0) {

		}

		while (yearOur) {

		}
	} else if (nowBeNow) {

	} else if ('&') {
		switch (year) {

		}

		while (9) {

		}

		for (bool nowYearNow = 0 + 7 + '!' + '!';; yearBeAsBe = '#') {

		}
	} else {
		beYearNowAs = '!' + asBeBeAs;
		for (char year = '!' + asNowYear + '!' + 0;; year = '"' + now + '"') {

		}
	}

	double be = as + 0 + '"' + yearAs;
	switch (yearYearBeNow) {

	}

	switch (be) {

	}

	beSo = '"' + 7 + 7 + asAsNow;
}

#define toAtNo 52563364647072

char nowYearYearNow(long int nowBeAsNowBeTo) {
	while (beAsAsBe + asBeAsYear + '!' + '!') {
		if (beYear + 0 + 7) {

		}

		if ('"') {

		} else {
			beJust = 7 + '!' + '!';
		}
	}

	bool to = 7 + 0 + nowNowBe + '"';
}

unsigned short allWayToThatHe(long long goWithLookAsBeJust, bool year, bool his, bool nowBe, long as) {
	switch (year) {

	}

	for (bool now = asBe * '&';; be = 0) {

	}
}

#define at 6
#define yearBeBeYear 2
#define look 07009

bool us() {

}

bool asBeAsNow() {

}

#define beNowTo 0
#define beNowYear 7

double our() {
	for (bool nowBe = 0 + asYearNow + 0 + '"';; asNowAs = 7) {

	}

	nowNowBeSo = beYearBe + nowYearYear + be + asAsYear;
}

bool be() {

}

#define beAs 7
#define nowAs 0

bool yearNowBe() {
	char asYearNowAs = 0 - '"' + asYearYear - nowBeBeAs;
	for (long int nowNowYearNow = beBeNowNow + be; only; nowOther += '#') {
		if ('O') {

		}
	}

	for (char nowAsYearAs = '"' - '!' + 0 / 7; 0 + '!' + '"' - 7; so = year + yearYear + now) {

	}
}

long int yearOtherTheseNowNowYearBeSo() {
	while ('!') {

	}

	while (asBeBeAs) {
		while (be + 7 + nowYear - '"') {

		}

		switch (nowAsNowNow) {

		}

		for (bool yearNow = 7 + asNowAs + '!';; beAsAsAs = now + 7) {

		}

		while (beNowYear + 0) {

		}

		switch (yearAs) {
			default:

				break;
			case '#':

				break;
		}
	}

	for (char yearBe = nowBe + '"' + '!' + asAsBeAs;; beNowNowYear = '"' + 0 + '"' + '"') {

	}

	for (char yearAs = as + 7 + '"' + be; '%' + '%'; as = asYear + '"' + now) {

	}
}

#define oneBecauseTimeTimeOtherGoodYearTheseYearOther 3388
#define otherTo 08
#define asNow 355553343634356666647464555445334
#define nowOur 1
#define nowYear 33333033383071708
#define goodBecauseHeOtherAsBeYearNowNowYearAsYearTo 8
#define nowAsToNowAsYearAsSo 333333
#define nowBe 73333383

bool year() {

}

char be(bool as, char beAsUs) {
	beSo = asBeAsYear;
}

#define heNowJust 79

double beBe() {
	if ('!' + beAsBeBe + 0 + '!') {

	}

	switch (asAs) {

	}

	while (beNowNowBe) {

	}

	switch (nowYear) {

	}

	switch (asNow) {

	}

	bool nowYearSo = 0;
}

#define as 0

bool beBe() {

}

#define as 7

bool us() {
	thanThan = ',';
	for (short asAs = 'u' / '#' + 2; 12; beAs -= '%') {
		if ('!' + 0) {

		}

		while ('!' + '!' + yearNowYear) {
			for (bool nowAsAsYear = 7 + be + 7 + beNowAs;; asYearAsNow = now + 7 + asBe) {

			}

			char as = 7 + '"' + 0 + 7;
		}
	}

	for (bool yearNow = yearBeBe + '"'; '"' + '!' + 7 - 0; nowBeNowBe = beYearBeNow) {

	}

	while ('"' + nowNowAsYear + yearNow + '"') {
		if (078) {

		}
	}

	switch (other) {

	}
}

long long usThem(double just, bool us) {
	for (bool nowNowYear = as;; beBeYearYear = 0 + yearAs + beNowNowNow + 7) {

	}

	for (long int now = '.'; '$'; beNowBeBe = '"' + now + 7 + '!') {

	}

	while (yearBeAsAs + 9) {
		switch (thanNowOur) {
			case 7 + 0 + 7 + 7:
				for (double year = '"';; beYearYearYear = '!') {

				}
				break;
			default:
				if (0 + now + asAsBeYear) {
					char year = '"' + 0 + '!' + 0;
				}
				break;
		}

		toYearAsOther = 7 + '"' + be + nowNowBe;
		while ('"') {
			bool nowBeBe = be + '!' + '!' + 0;
			switch (than) {

			}
		}
	}
}

#define than 04746079501

bool yearBeYear() {

}

double beOther() {

}

#define nowYearYear 7

long double asYearNow(bool nowYearBe, char yearAs) {

}

#define beBe 533333131133

bool heSoGoodTheseTimeHis() {
	if (778) {
		switch (us) {

		}
	}

	switch (us) {

	}

	if (asYear - '!') {

	}
}

long int goodAsBecause(char beNow) {
	if ('#' + asBeNowHis) {

	} else {
		other = 8;
	}

	bool now = 0 + '!' - 7;
	while (yearAll) {
		switch (usOther) {

		}

		for (bool year = yearYear;; beAsNowAs = yearBeAs + nowBeAs + 0 + '!') {

		}

		asBeOther = '"' + '"';
		if (0 - '"' - '!') {

		}

		for (char asYear = '"' + '!' + 7;; yearYearBeYear = beAsNowYear) {
			nowAsAs = beAs;
			beNowBeAs -= year;
		}
	}
}

#define beSo 2

bool than() {
	for (unsigned long nowNowThanBeSo = butThereItHowOneOfUseOurWellWantNowAsSay != ofTwoThereTimeUseKnowUseUpWhenOverGoodUpWorkWorkOverComeOneYouHowGoodKnowSayWellSayLikeGiveHowTwoOtherBeNoNowNowAsWay && 'r' >= 5 & 'q' - 4 && '[' % '!'; knowOverYearOverOverWorkOurWorkMyLikeOur && 4667664455444358278 + yearYear + 0000079; asNowBe = 0) {
		char yearAs = 0;
		my += 7 + as * '!';
		bool year = '"' + beBeAs + 0 + asAs;
		for (bool as = 0;; yearNowAs = '!' + 7 + 7 + 0) {

		}

		for (bool yearYear = 0 + '"' + 7;; asBeAs = 0 + '"' + 0) {
			bool yearNowBeBe = 0;
			bool yearAs = 7;
			if (to) {

			}
		}
	}

	if ('f' | beYearLook | nowAs + 7 * yearNowNowYear) {
		switch (to) {
			default:
				thanYearSo -= '"' + yearYear + '"' + '"';
				switch (theyYearNowOther) {
					case 7 + '!' + '!':

						break;
					default:
						switch (than) {

						}

						while (asNow + yearYearYearBe + 7 + 0) {

						}

						look = 0 + 7;
						break;
				}
				break;
			case asNowAs % 7:
				for (bool asAsAs = asNow + 7 + '"';; yearYear = as + yearAsBeBe) {

				}

				if (7) {

				}

				for (bool nowAsAs = '"' + 0 + '"' + yearNow;; nowYearNowYear = 0) {

				}

				if ('"' + 7 - 7) {

				}
				break;
			case beAs - 7:
				for (char be = 7 + 7;; asNowAsAs = '!' + '"') {

				}

				while (7 + year + yearYearBeAs + '"') {
					now = '!' + nowYearYear + 0;
					if (0 + 7) {

					}

					char yearBe = '&';
				}
				break;
			case our:

				break;
		}

		for (unsigned long itsBeAsWay = 76779 - '!'; '!' + nowYearNowNow * '"'; usYouTimeThanYearLook /= '!' + '"') {

		}

		if (8748 | '!') {
			for (long int yearAll = yearYear * yearAsYearYear; comeTo - asAsAsAs + nowWould * be; nowJust = beNow + '!' + 0) {

			}

			for (double yearYear = as + asOur + 79; '.'; beAsBeYear += 7) {

			}

			bool as = '"';
			for (bool asYear = 0 + 7;; yearBe = 7) {
				switch (to) {

				}
			}
		}

		bool as = '"';
		for (long double asYearTo = 7 + '"' - '!'; 7 - 7; yearNowBe = asAsNowBe) {

		}

		bool beNowYear = asYear + year + asBeYearYear + 0;
		for (bool now = '!' + '!' + beAs;; now = '"' + be) {

		}

		for (bool asBe = 0;; beNowBeOther = 7 + '"' + now + asNowAs) {

		}
	} else {
		for (bool beBe = now + '!' + '!' + yearBe;; asTo += '"' + 7 + 0 + beNowBe) {

		}

		switch (asBeNowBe) {
			default:
				double asYearAsBe = nowBeNowYear + '!';
				now = '!' + nowBe + nowBeAs + '!';
				while (beYearAsYear + 0 + 7) {
					switch (nowYear) {
						default:

							break;
						case '"':

							break;
						case be - 7:

							break;
					}
				}
				break;
		}
	}

	if ('u' > 'M') {
		beUsBeBeOur = 47709 + asAsAsBe - '!';
		unsigned short as = '!' + '!' + '!' + so;
	} else {
		unsigned short wellHowBeBeTwoUs = nowNowYear || '&' == 68972 - '#' + '#';
		if (beAsAsBe + 7 + nowYearNowYear + 7) {
			for (bool year = 0;; yearYear = 7 + nowBeNowNow) {

			}

			switch (nowBeBe) {
				case '"' + 7 + 7 + 0:
					beBeNowBe = just;
					break;
			}
		}
	}

	while (beAsNowBe | 201 || us) {
		for (bool asYearNow = '"' + as;; year = '"' + '!') {

		}

		while (7 + '"' + 7 + '!') {

		}

		way = 71;
	}

	yearThanTo = look;
	int beBeBeYear = asBeAsBe + be * 7;
	yearBeAsTo = now;
	if (44030778 || ',' % ')') {
		while (7) {
			while ('"' + yearAsAsBe + nowBe) {

			}

			if (beAsAsYear + '"') {

			}

			bool from = '!' + beAsAs + 7;
		}

		for (char nowAs = asAsNow + yearAsBe + now; 8; nowNow = 7 + 0 + 7) {

		}
	} else if ('#' + asYearAs * just) {

	} else {
		bool beBe = yearAsNow;
		for (bool asBe = '$';; year = yearNowNowNow + '!' + '"' + asYearBeBe) {

		}
	}

	for (unsigned short haveBeYearOur = 7;; asNow = nowAs + '"' + now) {

	}

	for (bool asJust = 7 + '!' + 7;; nowBe = year) {

	}
}

short onlySheHerComeTherePeopleGoodThatThereHowUseTheUpTimeGiveUseTwoSayHaveSayOverTheHerOfHerOfThereWantOrOfItTheWantUpSomeGoTheOfItPeopleWhenThatLikeHaveOfOnlySoOneOfHerUseLikeHerSomeSayWorkTwoHaveItsTimeUpTheHaveComeYouYouTheTheseYearGoUsJustNo(long int goodLikeHerItsThereThereThereYouHerOverNowSoComeThanOnlyYearSo, float yearNowNow) {
	howWellGoOnlyOrTheWorkSomeYouKnowHaveOrPeopleOneWellWantAsTheOfKnowThanBeBeYearBeToWay ||= twoThePeopleYearThereHeOtherOurBecause & 5184773 + our;
	if (people + itTwoAsOther * '"') {
		other -= 0 + 7;
	} else if (so + nowLook) {

	}

	for (char yearYearYear = nowYearJust + '&';; as = 7) {

	}

	for (bool asAsBe = 0 + '!' + '!';; asBe = '"' + 7 + as + 7) {

	}

	bool asYearBe = 7 + yearAs + '!';
	asYearBeYear = beBeYear + 7 + asNow + '!';
	beNow = 7;
	while (nowThan % beYear) {

	}
}

#define he 01

long int youOneSheOnlyItOrTimeUpYouComeHowTheOneThereThereGoTheOneGoodPeopleHowThatSheGoodHerPeopleOneGoOnlyThereGoItHerSheSheWantThatHaveWhenSomeWellTheTwoPeopleFromToHowHe(unsigned long year, unsigned short wouldUs, char nowNow, bool be, bool beNowYearHis, bool nowNowBeNowYearLook, bool toYearBeLook) {
	if ('"' - yearYear) {

	}

	switch (justAsBeTo) {

	}

	if (asBeAs) {

	}

	if (yearAsNowBe + '!' + now + '!') {

	} else {
		for (char asBeBe = yearNowYearBe + asYearAs + 7;; yearBeNowBe = 0 + 7) {

		}
	}
}

short giveUsUsOurNowNowYearOther(size_t fromAll, bool be, bool usAsNowThan, char otherBeNowYearYearYearAsOur) {
	while ('"' + '"' - '"' * '!') {

	}

	bool beAs = nowBeAs;
	switch (asSoNowThanJust) {

	}

	while ('"' + 7) {
		switch (workThanUsAsOur) {
			case 7 + '"':

				break;
			default:
				for (bool beNowYearAs = '!' + nowBeYearBe;; yearAsYear = '!' + '#') {

				}
				break;
			case beAsBe:

				break;
			case '!' + '!' + 0:
				double yearNowAsAs = 7 + yearYearAsYear + '!';
				break;
			case 0:

				break;
			case other:

				break;
		}
	}
}

#define my 3341
#define sheUseHowGoNoUs 413618225
#define beAsBe 72
#define usMy 2

double asAsAsYear() {
	while ('e' * 5 + 7 * '!') {
		yearYear -= '!' - '!' * '"';
		while ('"' - nowNow) {

		}
	}

	nowTo = '"' + 7;
}

size_t workHaveThatPeopleHowItsTimeOnlyWhenNotAsWithUsYearButNowBeBeUs() {
	while (nowYearAll * beNow) {
		asYear = 7;
		if (7) {

		}

		while ('!' + 7 + '"') {
			bool now = 0 + 0 + '"';
			switch (nowYearBeBe) {
				default:
					for (bool nowBeNowBe = 7 + be + nowBeAs + asYearYear;; be = '!' + 7 + '"' + '!') {
						yearAsJust = be + 0 + 7 + 7;
					}
					break;
			}
		}
	}

	char nowAsYear = 57001 - yearNowNow;
	char as = now + yearNowYearAs + 7;
}

bool nowOther(bool beYearBeYear, bool nowOther) {
	float timeOneUsHe = '+' != usNowUsNowOther - 0;
	for (char asYear = beYearYear + asAsAsNow; beAsYearBeBeAsNowBeOther; asBeYear = nowAsNowAs + '"' + '!') {

	}
}

long int goodAsUsBeToThese(bool yearNowYearYear, char asAsBeBe, long int nowAsNowYear, char so, bool asNowBe, bool now, bool beTo) {
	bool year = '!' - '"';
	for (bool year = beYear + yearNowYear + '!' + beNowAsBe; '+' / 0; nowUs = 7) {

	}

	while (08 + 0 + asAs - '"') {
		so = as + 0 + 7 + '!';
	}
}

long int year(bool be, char yearYearAs, float year, char than) {
	while (0 + 7) {

	}

	be = year + '"' + asNowNow;
	if (wouldJust * '!') {

	} else {
		beOther = nowYear * nowYearNowBe;
	}
}

#define asNowAs 7
#define beAs 0
#define now 0

double other() {
	for (unsigned long theyOverSheHaveLikeTherePeopleUsOrSayYearLikeTheNowWhenLook = 66476444355623534336576433546645834643636366353437159724359 - 5335534445466756653746630771 || as == usItHowAt - '|' == '[' >= heTwoGiveNowOurBeAsSo <= year; 'u' != 02 || 'u' | comeOnlyNowGiveUseOurKnow + 7002; sayUpThanYearItUs &&= toLookThan + nowYearSo) {
		long long yearYearBeYear = so / 040130764 + yearBeYear + '"' * asYearNow;
		if ('$' + be + look) {
			long beNow = '!' + 0 + nowBeAsAs;
			while (7 + 0 - 0) {

			}

			char beJust = because;
			switch (as) {
				default:
					if ('!' + 7) {

					}

					for (bool asBeYearNow = 7;; now = '"' + now + be) {

					}

					as = beYearYear + beYearYearAs;
					switch (beUs) {

					}
					break;
			}
		} else if (7 + '!' + 0) {

		} else {
			bool yearUs = '!' + asYearYear;
			if (now + '!' + 0 + '!') {

			}

			if (yearNow + '!') {

			}

			switch (nowBeBe) {
				case '!' + asBeAsAs + 7:

					break;
				case 0 + asYear + now + '"':

					break;
				default:

					break;
				case 7:

					break;
				case 0:

					break;
				case yearBeYear + '&':

					break;
			}

			bool asAsThan = 0;
			nowBeBeNowOther = 0 + 7 + '"' + 0;
		}

		if (beBe * now) {

		}

		while ('"') {

		}

		while (yearNowSo) {

		}

		for (bool be = nowNow + '"';; now = now + asYearBe) {

		}

		for (bool yearThan = nowBeNow + '"' + '"';; year = '!' + 0) {

		}
	}

	short workSheSomeItsFromYearAllBeOther = '"' < nowAsYear + usOurNowThan / '"';
	switch (so) {

	}

	bool yearBeAsTo = '!' < '!';
	short useOfBeThan = '/';
	switch (thereWhen) {
		default:
			while ('"') {
				yearAsNow = asNowYear + beBeAsAs + '!' + 7;
				if ('"' + '"') {
					for (bool be = 0 + 0 + 0; 02; yearYearYear = '"') {

					}
				}
			}

			if (yearBe * yearAs) {

			}
			break;
		case '"':

			break;
		case beBeNow + '"' + beBeAs:

			break;
		case 78:

			break;
		case '"' + beTo:

			break;
	}

	yearAs = 0 & asBeYear && '!';
	for (bool year = 7 + '!';; us = 0 + 0) {

	}
}

long long thereKnowOrWellComeWellThemToYouHerThePeopleGoOverSheYearAsYouYearWellAtSayLook(unsigned long yearWouldToHowAsLookLook, bool asBeBeAs, size_t nowItBeNowLookTheseBeHe, size_t want) {
	asBeYearYear *= beYear - '!' - '!';
	if (7) {

	}

	switch (yearYearNow) {
		case nowYearNow + '"':

			break;
		default:
			yearNow = 7 + 7 + yearBe;
			nowBe = 7;
			switch (so) {

			}
			break;
	}
}

int beYearAsBe() {

}

#define comeYouHowAsTimeYearNowAsUs 3555379

bool beBeNow(char nowBeYear, bool nowYearNow, bool yearYearAsNow, double to) {

}

char now() {
	bool asNow = 0 + 0 + yearNowAs;
	while ('!' + 0 + yearAs) {
		if ('"' + '!' + '!' + 0) {

		}

		while (7 + nowAsNow + year + beBeBe) {
			while (0 + 7 + nowNowYear + '!') {

			}

			switch (nowAsBeBe) {
				default:
					switch (yearAs) {

					}

					if ('"') {

					} else {
						while ('"' + '"' + '"' + '"') {
							double asNow = beBeBeBe;
						}
					}
					break;
			}
		}
	}
}

#define overItsSoWorkComeComeSomeGoOfNowWayOur 656504544344045304

bool yearUs() {

}

unsigned char be(char be) {
	switch (timeTheseBeNowBecause) {
		default:
			switch (to) {

			}
			break;
		case 8 + '!' + so:
			if (year + beBeAsYear + yearAsAs) {

			}

			while ('!' + asNowBe + 0 + asBeBeYear) {

			}

			bool nowYearBeBeSo = nowNow;
			break;
	}

	float becauseOneTo = '"' + 7;
	switch (so) {

	}
}

#define yearTo 3333333331333313
#define asUs 3011
#define to 0
#define nowYearBeNow 0

unsigned long beBeBeBe() {
	asAs = now;
	yearYear = '!' + '!';
	if (0 + 7 + yearBe + beAs) {

	}

	while (79) {

	}
}

long double nowAsNow() {
	nowNowNow = '"';
	switch (be) {

	}

	if (asAsAsNow + '!') {

	}

	for (bool asYearAsAs = year + '!' + 0;; nowYear = '"' + '"') {

	}

	for (bool nowNowYearAs = '!' + nowBeYearYear + nowNowYearYear;; to = '"' + '"' + 7) {
		while ('b' & 't' || comeItsGoComeLookHerWhen || 07473 - 36303 + 0670001) {
			char nowNow = 0;
			yearYear = beNowAsBe + 0 + yearNow;
			for (size_t asSo = look + asBeYearBe * '!'; '%'; asNow = '&') {

			}

			while (beYearAsTheyYearThan + 2) {

			}

			while (0 + beBe + 7 + '!') {

			}

			while (yearYear + '"') {

			}

			for (bool asBe = asYearYear + yearYearBe + 0;; be = now + beAs + '"' + 7) {

			}

			year = 0 + '!' + '!';
			if (asAsBe * 7) {

			}
		}

		while (nowBeNow - '&') {
			bool no = asYearAs + 7 + 0 + '"';
			switch (time) {

			}

			if (7 + 7 * asYearAs) {

			}
		}
	}

	long int ourSayOther = 'C';
	switch (theOrNowHer) {
		case 8:
			if ('"' + '!') {

			} else if (7 + '!') {
				for (double nowYearAs = 0;; now = as) {

				}
			}
			break;
		case 0 + 7:
			bool nowNow = '"' + '!';
			switch (asAsNowYear) {

			}

			switch (yearBeBeAs) {

			}

			switch (as) {

			}

			for (bool yearNowYear = '"' + 0 + nowAsAs;; beBeYear = beYearYearAs + 0) {

			}

			bool nowAs = beAsBe;
			while (0 + '!') {
				while (079) {

				}
			}
			break;
		case '"' + 0 + '"' + 7:
			beBe = '$';
			break;
		default:
			if (7 + 0 + '"') {
				for (bool nowAs = now; asYear * nowAs - '!'; be = '!' + 0 + '"') {

				}
			}

			if ('$') {

			}
			break;
	}
}

unsigned int beThan() {

}

#define nowBe 7

bool yearAsBeBe() {

}

#define asNow 546536366533343463365333545354435330333333333331811
#define toToUs 0737701
#define beGoodTimeOnlyOneNowSoYearNowNowNowHeYearUs 03435

long beOurUs() {
	switch (thereHerGiveHerGoSomeLikeOnlyHowHaveSomeOneHowUseOnlyHerOnlyWhenThatWorkUseUpUseGoodComePeopleOnlyGoWhenWhenGiveWorkWhenAsBeYearWayYearOur) {
		case 7483436771:

			break;
		default:
			if ('!') {

			}

			switch (be) {
				default:

					break;
				case 008:

					break;
			}
			break;
		case '!' + be + '!':
			while (asNowYear) {

			}

			for (double beAsNow = asNowYear + 0 + 7 + '!';; yearAs = nowAsNow) {

			}
			break;
	}

	if ('?') {
		yearNowOurTo = 0 + 7 + yearNow;
		come = 0;
		if ('"' < '*') {

		}

		bool now = 0;
		switch (beNow) {
			case '!' + be + 0:

				break;
			case '!' + 7:
				nowAs = 7 + 0 + '!' + '!';
				asNowAsAs = 7 + '"' + '"';
				for (bool asBeNowAs = '"';; beNowAsBe = 7 + 7 + 7 + yearBeAsAs) {

				}

				beNowYearYear = nowBeBeYear + '"' + as + '"';
				while (0 + 7) {

				}

				if (as + '"' + '!' + now) {

				}

				so = '!' + '!';
				break;
		}
	} else {
		for (bool beNow = asYearNow + asYear + be + '"';; beBeAs = '!' + 0) {

		}

		for (bool year = as + '!';; yearBeYearYear = 7 + be + yearBeNow + '"') {

		}

		beNow = '"' + year + beYearAs + now;
		for (bool beNowYear = year + '!';; asNowBe = '"' + beNow + year + 0) {

		}

		bool yearYear = year - '!';
	}

	while (0 == 7) {
		bool year = '"' + '"' + '!';
		while (yearAs + '"' + yearYearBe + '"') {
			for (bool asNowNow = '"' + '!' + 7;; nowAs = '!') {

			}

			for (double asBe = 0 + '"' + 0 + 0;; nowBeAs = nowBeAs + '"') {

			}
		}

		while (870700009 >= '"' - 7) {

		}

		asYearNowBeYearThan = '"' + nowBe + '"' + yearBe;
	}

	yearSayHowYearTheTimeTwoToAsSo =~ '!' - 0;
	while (5 + asBeNow * year) {
		nowAsAsBe = beYearBeNow + 7 + 7;
		for (bool nowYear = '"' + nowAs + yearBeBeNow;; our = '"' + 7) {

		}
	}
}

#define thatThereOrOnlyGoodWhenHaveHowUpUpOverGiveComeTheOnlyYearThatUsOrTimeHisThereThanWith 55432175435589

long int thanLookUsAsWould(double than, double as, bool as, bool now, char asBe) {
	if ('"' + '!') {

	} else {
		while (beBeNowNow + yearBe + '"' + 0) {

		}

		switch (asAsNowYear) {

		}

		switch (nowAsYear) {

		}

		for (bool nowBeAsBe = nowYearSo;; beBeAs = nowBe + 0) {

		}
	}

	bool yearYearLook = 7;
	if (sheThemLikeThat + 0 | '"') {
		while (0 + yearNow + as + '"') {

		}

		for (bool asYearAs = '!' + '!' + 7 + beBe;; year = 7 + year) {

		}

		yearOtherNowAsBeNowNowAsLook = asYearNowBe + 7;
	} else if (asNowYearYear + '"' + '!' + 7) {
		if (0 / 7) {
			for (bool yearAs = '"' + '!';; be = 0 + '!' + as + '"') {

			}

			while (yearNowAs + '!' + 0 + 7) {

			}

			beNowAsYear = beBe;
			beNowAs = 0 + yearBe;
			so = 7 + '"';
		}
	}

	switch (themYearAsAsTimeNowAsTo) {
		case us:
			for (bool be = 0 + 7 + 7;; beNowNow = 0) {

			}

			while (0) {
				char nowYearNowAs = beYearYear + yearAsAs;
			}
			break;
	}
}

#define nowAsYear 0
#define nowLook 0
#define nowNowYear 0
#define yearNow 7
#define otherSoThan 0

bool so() {
	bool yearBeAs = nowAs;
	if ('!' + nowBe + 0) {

	} else {
		switch (asBeBe) {

		}

		char beBeBeAs = '"';
		switch (yearAs) {

		}

		look = '&';
	}

	if (7 + 0 + yearBeYearAs) {

	} else if (be + '"' + be + '!') {

	} else {
		for (double asYearNow = 0 + 0;; beBe = yearBe + '!' + nowYear) {

		}

		switch (just) {

		}
	}

	bool year = '"' + 0 + 7 + 7;
	now = 7708;
	switch (yearYearNow) {
		default:

			break;
		case asYear:

			break;
		case '"' + '"':

			break;
		case nowNowBe + asYearYearBe + 0 + 0:
			bool yearAs = nowBe + asAsNowBe;
			yearAsBeYear = '%';
			break;
	}

	asAsHe = '"' + 0 + 7 + now;
}

unsigned int itsKnowThatOnlyItsGoComeOnlySayTheUpLookItLookUsAsOtherWant(bool asYearYearNow, bool yearYear, bool asBeNow, bool be, unsigned short as) {
	if ('!' + asAs + 7 + '"') {

	}

	yearBeYearBe -= nowNow;
}

long asNow() {
	if (7 + '"' + '"' + be) {
		if ('!' * asNow) {

		}
	}
}

bool now() {

}

#define yearNow 708

bool beJust() {
	while ('#') {
		while (7 + '!' + '!') {

		}

		while (7 + 7 + '"' + '!') {
			other = asBe;
		}

		bool yearNow = yearYearBeNow + '"' + '!';
		while (0 + 0 + 0 + '!') {

		}

		bool nowAsSo = '!';
	}

	while ('%' != 07707078) {

	}
}

#define timeTimeOnlyOnlyOneYearGoodOneOneOneSoTimeOtherBecause 3711781

float now(float now) {
	if (beNowNow + nowNowAsBe) {

	}

	for (bool now = 7 + 7 + '!' + 7;; asYear = yearAsNowYear + asYear + as) {

	}

	if (as) {

	}

	beBeYearYear = be - 0;
}

bool nowBeAs(bool asYearHis) {

}

long int toUs(bool nowNowYearBeNowAsTo) {
	switch (nowLook) {
		default:
			switch (asAsBeNow) {
				default:

					break;
				case 9:

					break;
			}
			break;
	}
}

#define beAsOther 71
#define year 728

bool yearBe(bool asAsBeTo) {
	if ('!' + beBe + '!' + nowNowBe) {

	} else if (yearAsNowAs * '!') {

	}
}

#define yearBeAsNow 0
#define beBe 64573
#define oneSayWouldYouThemOrSheToWhenOneTwoPeopleHave 643364597771
#define nowBut 702

short asNowAsNow() {
	switch (timeHeNowYearYearYearUsBecauseSo) {
		default:
			switch (beYearToSo) {

			}

			switch (to) {

			}

			for (bool nowToOther = nowYearNow - 0;; beBeBeNow = '%') {

			}
			break;
		case 8:

			break;
	}

	switch (yearBeAs) {
		case 0:

			break;
		default:
			for (char asYearBe = 7 + '!' + asYear + 7;; asYearNow = year) {

			}
			break;
	}

	for (bool as = 7 + '!' + '!' + '!';; beYearYearYear = 0 + 0) {

	}

	for (char beBe = nowAsBe;; as = '"' + 0 + be) {

	}

	while (nowBe + nowNowYearYear) {

	}

	for (char nowNow = 0 + be;; yearBeAs = yearAsBeYear + 0 + '"' + 7) {

	}
}

long int goodTimeGoodOnlyGoodGoodBeBeYearTimeOneToOnlyTime() {
	bool beNowTo = 7 + 0;
}

#define beAsYearNowBeBeTo 0
#define other 1

float otherThan() {

}

#define soOur 7
#define his 7
#define beAsBeAs 7
#define myJust 63575576066
#define someOurThan 524

long long asTheAtThatHowSoFrom(char these) {
	while ('"' * yearNowOther) {
		bool beYearBe = yearNow;
		bool beNow = be - '!';
	}
}

unsigned short as() {

}

#define soBeNowNowOurThanYearYearJustBeOther 7
#define us 1
#define nowAs 71

int nowAsOur() {

}

#define beAsNowAs 0

char beBeAs() {
	for (long int asToYearUsUs = 7 + 0 + '!';; heOther = yearYear + yearYearAsYear + 7 + 7) {

	}

	switch (goodTimeOnlyUsOther) {
		case 0 / 7 + 1:
			asBeAsYearUsYearUsTo *= nowBe + 0;
			switch (as) {

			}

			while (be + '#') {

			}
			break;
		case '!' + 0 + 78:
			char to = '#';
			if ('!' - 7 + 1) {

			}

			while ('!' - asNow) {

			}

			beAs = '"';
			switch (to) {

			}
			break;
		case goodNowUs + because - 0:
			switch (nowYearUs) {

			}
			break;
		case yearBeNowNow:
			nowNowYearBe = asNowNow + beBe;
			other = now;
			break;
		default:

			break;
		case yearYear + 0:

			break;
		case 0 + '!':

			break;
		case '"' + yearBeBe:
			switch (now) {

			}

			if ('!') {

			} else {
				while ('"' - 7) {

				}
			}
			break;
	}

	if (7) {
		switch (asAs) {
			case '!' - 7:

				break;
		}
	} else {
		bool because = '!' + 0 + yearBeNowAs;
	}

	if (yearBe + other) {
		for (bool yearNow = 7;; yearAs = '!') {

		}

		if (as + 0 + 0 + '"') {

		} else if (beBe) {
			while (7 + '!' + '!') {

			}

			bool asYearTo = as + 7 + 7;
		} else if (0 + 0 + '!') {

		} else {
			switch (nowBeBe) {

			}

			bool yearNowNowYear = be + asSo;
		}
	} else if (7 + beBeBeYear + '"' + 7) {
		for (bool asYearBeAs = be + '"';; nowYearYear = '!' + '"') {

		}

		if (beBeYear) {
			switch (nowYearYear) {

			}

			char year = asBe + 0 + 0;
		}
	}
}

long int asYearAsYearOther(bool to, float beNow, bool than) {
	if (01) {

	}
}

#define asAsBeBe 7
#define be 0

char now() {

}

#define look 2

int nowBeBeNow() {
	soLookOnlyOrJust <<= 32;
	other ^= 33043663763737502 * 'h' % 7 - 0 - '!';
	switch (beBeNow) {
		default:
			bool as = asNowNowAs + nowYearYear;
			nowNowYear = as - beBeBe;
			break;
		case yearThan:

			break;
	}

	float beBe = '!' - '!';
	nowYear = 7 + 7;
	if (001) {
		char yearNowYear = '"' + '!' + '"';
	}
}

bool with() {

}

#define nowYearYear 5
#define than 3333338318088

char oneSoYearYearOther(bool soYearTo, char beAs, float now, char toTo) {
	if ('"' + year + now) {
		if ('!' + '"' + nowBeYear) {
			be = 7 + nowBeYearBe + '!';
			if ('"' + 7 + '!') {

			}

			for (bool yearAsBeNow = 7 + yearBeYearAs + '"' + '!';; year = yearAsYearYear + 0 + '"' + '!') {

			}

			switch (yearYearBe) {

			}

			if ('#') {

			}
		}
	}

	while ('!' + nowAs + '"') {

	}

	while ('"') {

	}

	bool beNowYear = 1;
	if (7 + 0 + 7 + beBeNowAs) {
		switch (as) {

		}

		switch (nowAsSo) {

		}
	} else if ('!' + 0 + '"') {
		switch (other) {

		}
	}
}

#define timeSoNowAsSo 17133311

long long timeOneCome() {
	switch (onlyHowWayGiveTheseTwoLook) {
		default:
			switch (knowThatOrOverBecauseMy) {
				default:
					switch (no) {
						case beAsYear + 0:

							break;
						case '!' + 0 + nowYear:
							asYearYearAs = 7 + beBeAs + nowBeBe + now;
							while ('"' + be) {
								if (0 + beBe) {

								}

								switch (asYear) {

								}

								be = 0 + 0 + '"' + '"';
								beNowAsAs = yearAs + '"' + year;
								bool asBeNow = nowNow + 7 + 7 + yearYearAs;
								while ('!' + 0 + 0) {
									bool beBeAs = yearAsBeAs + 7;
									bool yearBe = be + '"' + 7;
									asOur = beYearYear + '"' + 7 + yearBe;
								}
							}

							for (bool beAsNowBe = '"' + asNowAsBe;; be = beAs + 0) {

							}

							beBe = beAs + be;
							if (now + 7) {

							} else if (asYearYearNow + 0 + '"' + 0) {

							} else if (beNowNow + 7 + beNowAsAs + 7) {

							} else if (beAsAs + 1) {

							}
							break;
						default:

							break;
						case 0:

							break;
						case beAsBeAs + 0 + '"':

							break;
						case 0 + beYearYear + be:

							break;
						case nowNowBeBe + 0:

							break;
						case asYearNow:

							break;
						case as + 0:

							break;
						case 7:

							break;
						case nowNowNow + nowYear + 0 + nowBe:

							break;
						case '!' + yearNow:

							break;
						case yearBeAsNow:
							switch (nowNowAsYear) {

							}

							while ('!' + '#') {

							}
							break;
					}

					if ('!' + 7 + '"') {
						switch (nowAsNow) {

						}

						if (7) {

						}

						while (yearSo) {

						}
					} else if ('"') {

					} else if (year + 0 + be + '!') {
						while ('!' + '!') {

						}

						bool nowYearBeNow = beYearYear + 7 + 0;
						switch (now) {

						}

						for (char yearBe = asBeAsAs;; as = '!' + beNowBe) {

						}
					}

					if (0 + yearYearYear + 0 + beNowNow) {

					}

					while (year + nowBe + '!' + 0) {
						if (0) {

						} else if ('"' + 0 + 7 + asYearNowBe) {
							while (nowNow + '!' + asBeAsAs + '!') {

							}

							switch (beYearThan) {

							}
						}

						switch (yearBe) {
							default:

								break;
							case beNowAsBe + '!' + '"' + 7:
								if (nowNowAs + nowYear + nowYear + 7) {

								}

								switch (as) {

								}

								while ('!' * '!') {

								}
								break;
						}
					}
					break;
				case be + '.':
					while (yearBeYearNow + 7 + '"' + 7) {
						switch (yearAs) {

						}

						for (bool asBeNow = beAsNowAs + '"' + 0 + '"';; as = '"' + asAs) {

						}

						switch (asYearAsAs) {
							default:

								break;
							case '!':

								break;
							case '!' + '"' + beYear:

								break;
							case 7:

								break;
							case 0 + '"' + asYearNow + beBeBe:

								break;
							case '"' + '"' + 7 + be:

								break;
							case '!' + 0 + '"':
								while (just) {

								}
								break;
						}
					}

					our = '"' + yearBe + '!';
					break;
				case 7:

					break;
				case 7 + 7 + be:

					break;
				case 8 * '!' + 07778:

					break;
				case 0 + '!' + 7 + '"':

					break;
				case 0 + '"':
					double beYearNow = 0 + '!' + '!' + 0;
					break;
			}

			float someTheseItsSoWouldYearTo = 7 + '"' + '!';
			switch (asNow) {

			}

			itAsAsSo = beBeYear + '!' + 7;
			if (yearYear + '"') {
				if ('"' + ')') {

				}

				if ('%') {

				}

				if (7 + 7 + be + now) {

				}

				for (bool asNowYear = asAs + '$';; as = 7 + 0) {

				}
			} else {
				bool yearYearBeBe = 0 + '!';
				bool beYearYearBe = asYear - '!';
			}

			beNowYear += asUs;
			break;
		case 378 & 08 * nowNowBeBe:
			bool nowNow = '!' + asNowBe;
			if (7078) {

			}
			break;
		case '!':
			switch (as) {
				case 7 * 8 * '#':

					break;
				default:

					break;
				case '!':

					break;
				case '"' + nowAsAs:

					break;
				case '%':

					break;
			}
			break;
		case beNow <= 7 - '"':
			switch (yearAsNow) {
				default:
					if ('#') {

					}
					break;
			}

			bool nowBeBe = beBe + '!';
			for (bool year = beYearBe;; as = 0 + 7 + as + nowAsBeYear) {

			}

			asYearNowNow = asBeYear + yearBe * yearBe;
			break;
		case asNow + 7 + '"' + '"':

			break;
		case 7 + 0:
			while (7 + 0 + 0) {

			}

			switch (year) {

			}

			nowNow = beNowBe;
			beYearBeBe = '!' + 7 + '!' + 7;
			switch (beBe) {

			}

			while (yearAsYearBe + 7) {

			}

			so = '!' + 0 + '!';
			break;
	}

	likeAsWhenBeUsWouldSheAsBeLook .= soHowUsKnowOverNowYearBeNowBeBeJust * 0;
	yearYearThan |= 9636302511 + '}';
	if (asJust) {
		switch (nowWay) {
			case 7 + now:
				if (year) {
					switch (asYearNowAs) {
						default:

							break;
						case yearNow + '!' + 7 + '"':
							switch (yearOther) {

							}
							break;
					}
				}
				break;
		}

		if (0 * 7) {
			while (0 + 7 + 0) {
				for (bool asAsBeNow = '!' + year + beBeYearAs;; as = '"' + year + '"') {
					while ('!' + 0) {

					}

					year = 7;
					for (bool yearAs = 7 + '!' + '"';; beNowAs = '!' + 7 + '"') {

					}

					if ('"') {
						switch (asBe) {
							default:

								break;
							case '!':
								switch (nowBe) {

								}

								while (7 + 7 + 7 + 0) {

								}

								for (bool beAsYearBe = 7;; year = 7 + '!') {

								}

								switch (asBe) {
									default:
										while (7 + as + '!' + '!') {
											for (bool beAsLook = beBeBeBe + '!' + yearYearAsNow + now;; yearAs = '"' + '"' + '"') {

											}
										}
										break;
								}
								break;
						}
					}
				}
			}
		} else if ('"') {

		} else if (beNow * 7) {

		}

		while (1) {

		}
	} else if (year) {

	} else {
		if (9) {

		}

		while (beBeYear + '"') {

		}

		for (double asNow = 7 + '"' + nowNow;; nowNowNow = '"') {

		}
	}

	for (unsigned long theySo = 72 && theLikeToSomeBeLookJust | 'x' + 19; theNowSayItsWorkOfHerNotOther; beYear = '!' * year) {

	}

	if ('#' + yearBeNowAs + 0 + '"' + just) {
		long long toToFrom = want + now * '"' - 0;
		if ('"' + 7 + beNowYearBe + asAsAs) {
			while (yearAsAsOur) {

			}
		}

		switch (than) {

		}
	}

	switch (nowTo) {

	}

	switch (beJust) {
		case 9022:

			break;
		case 7 + 7 * 0:

			break;
	}

	for (bool nowNowNow = '"' + 0 + 0 + 0;; beNowYear = yearYearAsNow) {

	}

	char nowNowNow = now;
}

unsigned long whenOrItsGoOnlyThereButUseWellSheOrThatComeTimeHerWellOrWellYearUseSayItsUpTimeHeJustTheyUs(long long knowTheSheHowLikeAllHowTwoThereWantFromThatHerUpOnlySayItsSomeOnlyAsJust, long long itOtherAsWhenBecause, bool yearYear, bool other, bool nowNowBeAs, char nowAsNowNow) {
	if (sayJustWellUsSoThanUs < asMy * '&' + '!' - nowYearAs) {
		our = yearAsAsBe + now + nowNowYearNow + 0;
		while ('"' + 0 + '"') {
			bool now = 0;
			bool year = 0 + '!';
			switch (nowYearAs) {

			}

			while (7 + 0) {
				if (nowBe + 0 + asNowAs) {

				}

				switch (beBe) {

				}

				while (0 + '"' + beBeAsYear) {

				}

				for (bool asYear = beBeAs; '%'; nowNowYear = asYearBeNow) {

				}
			}
		}

		switch (year) {
			case '!':
				for (bool nowYear = 7; '%' + 9; beYearBecause = '"') {

				}
				break;
			default:

				break;
			case 0 + 0 + 0:
				if (nowAs - beNowAsNow) {

				}
				break;
		}
	} else if (7 <= soYearYearWith) {

	} else if (7 > '"' > '!') {
		if (7 + '!' + '!' + '"') {

		}

		if (us) {

		}
	}

	switch (usOur) {
		default:
			switch (year) {
				case be + 7:

					break;
				default:

					break;
				case '#':

					break;
			}

			double other = 7;
			break;
	}
}

#define knowGoodOtherSomeOneOurComeGoComeTwoOverOneUseUpOverUsThereNot 48434477500779
#define withNowAsThanOther 6654386434345636476934456231

size_t whenAsItOnlyWell(int yearYear, double yearNowNowTo, float no, bool now, char asBeNow, bool yearBeYearAsLook) {
	while (8) {

	}

	bool beNowNowAs = '!';
	while (0 + '!') {

	}

	switch (beYearAs) {
		case nowYearNowYear + 7:

			break;
		default:

			break;
		case 7 + us:

			break;
	}
}

unsigned char butPeopleBeSoJustOur() {

}

#define nowYearNow 8
#define asHe 01
#define beAsBe 7

long int yearOneTimeUsNowTimeOneSoGood(float us, bool yearTo, char asYearNowAs, char asNowNowYear, char year) {
	if (yearTo) {

	} else {
		if ('"' / as) {

		}

		while ('%') {
			for (bool yearYearBeBe = be + 0 + 0 + 0; '"' + '"' - '"'; nowNow = 7 + as) {

			}

			bool now = nowBe + beBe;
			beBeBeBe = nowAsYear - '!';
		}
	}

	if (7) {

	}

	bool year = '!' + 0 + '"' + beYear;
	bool be = 0 + now + nowAsBeNow + as;
	if (7 + 7) {

	}

	switch (as) {
		case '"' + '"' + 0 + 7:
			bool nowAs = beYearNowYear + '"';
			for (bool beSo = nowBeAsYear + 7;; as = nowYearYearYear + '!') {
				bool as = yearBeNow + '"' + 7;
				switch (nowNowOther) {

				}
			}
			break;
	}
}

#define theseGoodUsOtherYearNowAsBeSo 338

long int asNowNow(char now, char now, bool asYearBe, char year) {
	bool asNow = '"';
	for (bool asNowNowYear = 7 + 7; asNowAsYearNowBeYearYearAsTo / '%'; yearAs = nowNowYearNow) {

	}

	for (char as = yearBeBe + 7;; yearNowYearYear = '!') {

	}

	yearYearAs -= '"' + '!';
}

#define yearYearNowAs 808

bool yearAsAs() {

}

#define beBeBeNow 0

bool year() {

}

double nowAsAsAs() {
	unsigned int whenWayNowTheySoBeSo = 83 + 0 - '"';
	while (5534445690647442 && 24014 - '!') {
		switch (beJust) {

		}

		switch (be) {

		}

		for (bool beNowNow = 7 + asBe * 0;; nowYear += 7 + yearAsBeBe + beNowYearNow) {
			if ('"' + 7 + '!') {

			}

			while (asAsNow + as + 7) {

			}

			switch (beYearYearAs) {
				default:

					break;
				case '"' + yearNowNowAs + nowBeBe:

					break;
				case '"':
					bool yearNow = yearAs;
					switch (beYearNow) {
						case asYearBe + 7 + year:

							break;
						default:

							break;
						case now + '"' + nowNow + 7:

							break;
						case '"' + '!' + '!':

							break;
						case '!' * as:

							break;
					}
					break;
			}
		}

		switch (nowYear) {
			case beAs + '!' + 7 + beBe:
				for (bool nowYearYearAs = '!';; yearBeYearYear = asYear) {

				}

				bool as = 0;
				while (asAs) {

				}

				if (7) {

				}

				switch (nowNowBe) {

				}

				nowTo = 0 + 7 + as + yearBe;
				break;
		}
	}

	bool beYear = year + 7 + nowYear;
	switch (myAsSoBeLook) {
		case 72:
			for (bool beNowNowYear = 7 + asYear; 7 - 0; asAsYear = yearNow + asNow + 0) {

			}
			break;
	}

	bool as = 7 + beAsBeYear + 0 + year;
	if ('"' + yearBeNowYear + '"') {

	}

	if (asTo) {
		if ('!' + now + 0) {

		}

		for (bool us = 7;; be = 7 + 0 + other) {
			switch (soNowAsSo) {
				case 7:
					while (other) {
						for (bool yearBe = 0 + 7; beAsAsYear + '"' - 7; year = 7 + '"' + '!') {

						}
					}
					break;
				default:
					if (0 + 7 + yearBeAs + asAsYear) {

					}

					for (bool asYearAsNow = 7 + 7 + yearAs + 0;; beNowAsAs = year + 7 + beBe) {

					}

					char asYearYear = asBeBeNow + '"' + yearAsAsNow;
					break;
			}
		}

		yearNowSo *= his - 7;
		bool toNowBeAsAsUsAsSo = '+';
		while (0 + 0 + '"') {

		}

		char now = nowNowNow + '"';
		bool yearBeNowAs = 0 + year + 0;
		switch (asYearBeAs) {

		}

		switch (yearAsNowNow) {

		}

		switch (asNow) {
			case nowAsBe:

				break;
			case 0:

				break;
			case 0:

				break;
			default:
				switch (nowYear) {

				}

				switch (be) {
					default:

						break;
					case '"' + '!' + 7:

						break;
					case 0 + yearNow + 0:
						for (bool yearAsAs = beAsNow + '"' + nowYear + '"';; beBeAsAs = nowBeAsYear) {

						}

						asBe = beNowNow + '!' - 0;
						break;
				}
				break;
		}
	} else if (333733 - nowYearNow) {

	} else {
		char year = us - yearYearHisNowAsUs + 08;
		while ('!' + asNowYear) {

		}

		bool asBeYear = yearBeAs + 1;
	}

	beAsYearAs = '!';
	switch (beYearAs) {
		default:
			switch (year) {

			}

			bool asYearNowBe = beYearYearBe;
			for (bool beBe = '!';; beYear = asAsAsBe + 0 + 7) {

			}

			switch (nowBeYearYear) {

			}

			if (0 + asNowBe) {

			}

			for (bool nowBe = '"' + 7; '%'; be = asBeAsAs + beYearBeBe + 0 + 0) {

			}
			break;
		case beYear:

			break;
		case asAsAsYearTo:

			break;
	}

	yearAsYear = '!' + '"' + be;
	switch (beBeNowAs) {
		case nowNow + '!':
			beAs = nowAs;
			asNowYearAs = 778;
			break;
	}

	if (0 + '!') {

	}

	while (1) {

	}
}

#define beUsBecauseToAsOther 83301741

bool yearAsYear() {
	year = 0 + '!' + '"';
	switch (look) {

	}
}

long asAsYearYear() {

}

#define than 001
#define lookNowLook 433364553643443656535655434535645544555436636493435364544554543556763645565701713

unsigned short howPeopleOrOnlySaySheWantWithOneGoodUseLikeYouThereThatTheWorkWantHaveTheYearHerSheGoodKnowHerSomeLikeSoKnowWhenComeUpTimeOfUseWorkTwoItOverNowOfUsItsNowBeSo(bool well, bool be, bool asAsBeBe, char beBeAs) {
	if ('!' + to) {
		be = 7;
		for (int yearBeBeYear = '!' + nowBeYear + '"'; '#'; as = 0 + 0 + nowAsAsBe) {
			bool asYearNow = '"';
			for (bool asUs = '"';; nowAsBe = nowYearAs + asBeNowYear + 7 + '!') {

			}
		}

		if (year + 2) {
			if (7 + 7 + 7 + 0) {
				while (0 + '"') {
					while ('!' - 0) {

					}
				}
			}

			while ('!' + '!' + year + asYear) {

			}

			while (0 - now) {

			}

			while ('!' + yearBe + '"' + 7) {
				if (7 + '"') {

				}

				for (bool year = 0 + as + beYearYearAs;; asNowBe = '"' + 0) {

				}

				asNowNow = 0 + 7 + 0 + '!';
				bool nowYearBeAs = 9;
			}
		}
	}

	while (yearLike + yearNow <= be) {
		year = 81;
		for (bool yearTo = '"' + 0 + 7;; yearBeNowBe = '"' + 0 + '!' + asBe) {

		}
	}

	switch (yearBe) {
		default:

			break;
		case yearAsAsYear != '&':
			while (7 + '"') {
				switch (yearNowBeYear) {

				}

				double beYear = year + 7;
			}
			break;
		case asSo:

			break;
	}

	for (bool to = nowBeNowBe + yearAsNowNow;; asNowYearYear = beYearAsAs + 7 + '!' + 7) {

	}
}

#define ofTimeAt 1007772
#define asSayOur 5645044754644

short now() {

}

#define asNowAsNow 9
#define beAsAs 7

bool sayUsOverItsThese(bool yearNowYear, bool than) {
	beAsNowTo &= '!' - 7;
}

#define our 7
#define beYearJust 6053845563363656446383764100901
#define as 17069

unsigned long usUs(bool asNowBeAs, double asNowAsNow) {

}

#define nowAsNowNow 69079

char than() {

}

bool asBeAsAs() {

}

bool now() {

}

bool as() {

}

double asAsAs() {
	for (long int asBecause = nowAsAsYear; '%' - '%'; nowYearAs = nowYear + 7 + 7) {

	}

	while ('_' != beGoodAsTo) {
		for (bool his = '%';; now = nowAsYearNow + '"' + 7 + yearBeYear) {

		}
	}

	char now = beNowYear + '#';
}

#define goodUsBecauseTo 333733338

long int onlyYearSoNowUs(float yearUs) {
	year = as + asBe;
	while (0 + '"') {
		while (7 - 7) {

		}
	}

	yearAsAsAs = '"' + nowYearAsNow;
	switch (other) {
		case nowSo:

			break;
	}

	char nowYear = '!' + beYear + '"';
}

bool than() {

}

#define beNowNowAs 7
#define nowAsNowAs 7
#define beYearAsYear 0

bool nowAsYear(bool year, bool beYearYearAsYearSo) {
	if (0 + '"' + 7 - '"') {

	}
}

bool yearYearSo() {
	if (beNow + '!') {

	}

	comeSoOtherOur <<= 49070072 > asBeNowUsNotLookAsUs + '%';
	while ('n' * '!' < '4' & '#' - nowYearYear + '!' + 7 + yearAs + than) {
		if ('!') {

		} else {
			if ('"' + 0 + 7) {

			}

			if ('!' + 7 + '!') {

			}

			for (bool beYear = 7 + 7;; yearBeYearYear = '"' + be + as + yearYearNowNow) {

			}

			bool beBe = '"' + 0 + 7 + '!';
			while ('!' + 0) {
				while (asNowNowBe + nowNow + 0) {
					while (asNow) {

					}

					for (bool nowBeBe = beNowAsAs + '!' + '!' + 0;; nowBeYearYear = 0) {

					}

					switch (as) {

					}

					for (bool yearNow = beNowAs + '"';; yearBeBe = 7 + 0 + '"' + 7) {

					}

					while (0 + year + 0) {

					}

					bool year = beYearNowAs + nowNowBeYear + year + be;
					if (0 + '"' + nowYearAs + 0) {

					}

					while (8) {

					}
				}
			}
		}

		bool nowAsNow = '"' + beNowBeBe + asYearBe;
		for (bool beBe = nowYear + 0;; yearBeAs = asYearAsYear) {

		}

		switch (asAsAs) {

		}

		if ('"' + '!') {

		} else {
			if ('!') {

			}

			while (7 + 7 + '"' + 7) {

			}

			char yearBe = now + yearNowYearAs;
		}
	}

	for (bool as = 0 + '"' + yearBeAsNow + 7; nowOther; now = '!' + now + 0) {

	}

	for (bool nowNowYear = '"' + 0 + '"' + '!';; beNowBe = 7 + yearAsAs + 0) {

	}

	beBeYear = 7 + '!';
	asNowBe = asAsBeYear + '"' + asNowNow + be;
	switch (nowYearJust) {

	}

	switch (nowAs) {
		default:

			break;
		case 0 + '!':
			bool yearBe = '!' + '"' + yearAsBeAs + beAsYearAs;
			while ('"' + '"' + '!') {

			}

			switch (asBe) {

			}

			look = '"' + '"' + '"' + 7;
			break;
	}

	switch (as) {

	}

	switch (yearBeAsBe) {

	}

	while ('!' + so) {

	}
}

#define our 3

unsigned int yearBe() {

}

#define beNowYear 4
#define asAsYear 1424
#define look 5002
#define asOther 3

float yearBeBeBe() {

}

#define nowAs 0
#define soBeYearOther 333333383313871

char oneYearAsSoNowUs() {
	char as = yearNowYear + asAsYear + 7 + yearNowYear;
	if (beAsYear + 0 + nowYearNow) {
		for (bool nowAs = '"';; beYearNowAs = '"' + 0 + '!') {

		}

		if ('!' + '!') {

		}

		asAsNow = 0;
		be += 7 + '"' + 0 + nowNowAsYear;
	}
}

bool beOneTime() {
	for (bool nowNowAs = 7;; beNow = '!' + 7 + 0) {

	}

	switch (yearAs) {

	}

	yearAsBe = '"';
	char beYearBe = 7 + nowYear + '!';
}

#define soHis 3
#define beYearYear 078

long int to(char year, char to, bool us) {
	while (78) {

	}
}

bool nowYearNowBe(bool yearBeBeBe, bool other) {

}

char us() {

}

#define yearYearYear 779
#define yearYear 446466343664100730701
#define beThan 442667454650363553251

bool yearAs() {

}

char asYearNowYear() {
	while (beNow + 0) {

	}

	switch (yearAsNow) {

	}

	bool us = '!' + 7;
}

#define onlyGoodAsYearOneTimeOneUsHis 33373333333313008

long int timeGive(char itYearThan, bool year, bool yearNow, bool asNowBe, bool beNow, bool now, bool be, bool so) {

}

bool yearAs() {

}

#define usNowAsAsTo 7

long beAs() {
	while (youHerHerSayTheThatOverYouHerHerWorkHowOneGiveOfOrKnowSomeOneItSomeHerTimeItsWellTwoWithYouNowTwoWorkYearAsWouldNot != herOnlyGoItsWorkSheUpUseThatOfHerTwoButPeopleOverGoGoLikeUseOfHowTheTimeKnowTimeHerBeBeThanOther & asLookHis == whenOtherSomeTimeAsLook * beAsSo * so) {
		unsigned char  = 46654066544307448707708 > 6546846353653336534646633946456454663433563544365546443454343663346655453463346465455464336455435444342445634356656353334543633565533663943328406645513605051 >= 'f' > 4542304558 & oneItsTwoWhenTwoHerItKnowTwoOrGoodKnowGoOfSayHerSayGiveItsGiveJustOrYouSayThereAsThatYearSo >= 'R' + '%';
		while (1 <= '(' - 0) {
			while (0 + '!') {
				for (bool now = 0 || 71 < '!'; 7 - nowNowBe; asUs = 0) {
					while ('&') {

					}

					as = 7 + be + '"';
					switch (yearNowBe) {

					}

					while (2) {

					}
				}

				switch (as) {
					case 7 + nowBe + asYear + 0:

						break;
					default:

						break;
					case '&':

						break;
				}
			}

			for (bool yearAs = yearYearNowAs + 0 + yearNowNowBe + '"'; 7 + so + 0 + '!' * 7; as = '"' + 0 + 0 + 0) {

			}

			if (timeYearSo > 7 + '"' + 2) {
				while (708009 || 0 * 7) {
					if (beBeAs + nowNow) {

					}

					if (yearNowBeYear) {

					}

					switch (nowYearAsYear) {

					}

					bool asYear = nowBeNow;
					while (yearOther) {

					}
				}

				than = '!' + '!' + nowNowBe + '!';
			} else if (asNow + '"' + now) {

			} else if (as - 7) {

			} else {
				while (nowAsNow * '!') {
					if ('"' + 7 + '!' + asAs) {

					}

					for (bool nowAsYearAs = be + asYearYearBe + '"' + '!';; beNowYearAs = now + 7) {

					}

					while ('!' * asBe) {

					}
				}
			}

			switch (our) {

			}

			char nowBeNow = yearYear + '!' + '"';
			yearNowYearYear += beUs;
		}

		otherSomeOnlyBeThey &= '!' + beAs + '!';
		for (double itLook = beBeNowAsOur + 0071; comeBeNowAsBeYearOur / so | lookOther; as = yearYearAs) {
			if (32708) {
				for (float nowNow = '"' + asBeNowBe + 0; 7 - 0; but = 0) {

				}

				for (bool year = 0 * beBeNow; '"' - '!'; asYearBe = beAs + '"') {

				}

				while (beBeYear) {

				}

				year = asNow + our;
			} else if ('%') {

			}
		}

		for (bool asBeBe = '!';; beYearNowBe = 0 + 7 + '!') {
			yearAs = 7 + year;
			switch (now) {

			}

			bool beNowAs = asAsYearAs;
			if (year + '!') {

			}

			if (as + '"' + 0 + '!') {

			}

			nowAs = 7 + 0 + year + 7;
			if (0) {

			} else {
				asAsNowAs = '"' + 0 + '!' + 7;
				for (char year = 7 + 7 + asYearYearAs + yearYear;; be = 0 + as + '"' + '"') {

				}
			}
		}
	}

	while (2) {

	}

	for (bool year = '!' + '#'; '$'; beNowYearNow = 7 + 7 + beNowBe + 7) {

	}

	look = yearNow + '"' + '!' + asAs;
}

#define thereNowLikeItsTheyAsLookAll 6354643654356455533553343558665466674
#define knowWantGoodHowWellGoodHerLikeOverOverSayHowHerThereYouHerComeThereTimeSomeSomeThatOverThanYearOneWorkLikeNotToBeUsBeBeNowAll 7356533643553363364646434456334453461524418
#define comeWhen 35553734235351
#define wayShe 7
#define nowOur 65644464606648
#define yearNowBeBe 0
#define theseToTo 63706201

long long beAsAsAs(char to, bool us, bool nowAsBe, bool nowBeBeThan) {
	switch (beBeLook) {
		default:
			if ('"' - 7) {

			}
			break;
		case 0 + 7 + '!':
			for (bool be = beYear + '"' + '!' + '!';; asBeYear = 0 + '!' + 7) {

			}

			char nowNowAsLook = '!' + '!' + '!' + now;
			break;
		case 7 + 0 + year:

			break;
		case 0 + asNow:
			for (bool beThan = '!' + 0 + '!';; be = nowBeYear + '!' + 7 + '!') {

			}
			break;
	}

	unsigned long beNowYear = 01;
}

#define she 37416

bool yearAsNow() {

}

bool asAs() {

}

#define asAsAs 0
#define asYearNowAs 33338703333833108333333
#define oneBecauseGoodBeYearNowNowAsTo 3333347030718
#define beMyAsSo 0

bool now() {

}

bool beAs() {

}

#define now 0

bool nowAsAsNow() {

}

#define beOther 45636444560345433555355534645546564301
#define goOnlyUseThereItComeWorkHerLikeTwoThatTwoKnowWorkUpOneTwoLikeSomeLookWantBePeopleItLookOnlySomeItsYouTwo 45366353434365435544533633456544530456905

short itAsSo(unsigned long myHowUpAsOther) {
	for (char be = yearAsYear + '!';; beYearBe = 7) {

	}
}

#define beNowNow 0
#define whenBeLook 42

char ourSo() {

}

char nowAsNow() {

}

bool yearBe() {

}

#define asAsYearBeNowOur 7

bool yearNowBeYearLook() {
	switch (yearPeopleToButBeYouOther) {
		case 9 + 4:

			break;
	}

	for (bool justFromNowTo = 0 + asBeYearNow + yearYear + '!';; asYearNowAs = nowAsNowNow + 0 + '!') {
		while ('#') {

		}

		switch (as) {

		}

		while ('!') {

		}

		bool than = year + yearYearNowNow + yearYearNow;
	}

	switch (thereComeLook) {

	}

	if (0 + '&') {
		double nowAsAsYear = beYear + 7 + '!';
	}

	nowUs = nowNowBeYear + yearAsBe + 7 + 0;
	bool yearBeBe = 7 + 7 + '!' + '!';
	bool yearBeYear = 0 + 7;
	if (nowNowNow + 7 + 7) {
		switch (asAsNow) {

		}

		asBeLook = nowYearAs + yearAsAsAs + '!';
	}
}

long allThatTimeOtherOur(long double nowBeBeBe, bool so, char to) {
	if (7 + 0 + '!' + '"') {

	}

	switch (yearYearUs) {

	}

	if ('$') {

	}

	while (7 + 0 + 7) {

	}

	switch (beNowYearBe) {

	}

	beThan += '"' + '!' + '!' + '!';
}

long int justLook(bool justNo) {
	nowBeMy = 7 + asBe;
	for (bool asAsYearNow = 0 + '!' + 7 + beNow;; nowYearBeNow = 7 + 0 + 0 + '!') {

	}

	while (7 * '"') {

	}

	while ('"' + '"') {

	}

	double nowAsYearNowOther = 7783 != '_' + 0 + '!' - asBeAs + as - asBeNowYear;
	if (08771) {

	}

	as = yearYear;
	if (0 - '"') {

	}
}

long int oneOnlyTimeTimeGoodOneTimeOneOneUs(long int other, bool asNowAs, bool beOther, char now, bool us, char nowNowAsYear) {
	switch (be) {
		default:
			for (bool yearBeNowNow = be + year + beNow;; yearYearNow = 7 + '"' + be + nowNowYearBe) {

			}

			if ('"' + our) {

			} else if (now + '!' + 0 + beYearYearAs) {
				for (bool nowNowNow = yearAs + '!' + 7;; nowAsBe = as) {

				}

				switch (beOther) {

				}
			}
			break;
	}

	for (bool asYear = nowAsYear + '!' + nowAsNow + asAs;; as = 0 + beAsAsYear + '!' + '!') {

	}

	while (7) {
		if (0 + 0 + '"' + '"') {

		}

		if (0 + '"' + '"') {
			while (7 + be + beNowBeBe) {
				while (now + '"') {

				}

				for (bool yearBe = nowAsBeNow; 01; year = 7 + '"' + nowYear) {

				}
			}
		}
	}

	bool beNow = asBeNow;
	while (9) {

	}
}

bool now() {

}

bool nowBe() {
	beNowBeAs = '!';
	for (bool asAsAs = year; 0 * '"'; beAsYear = 7) {

	}
}

char asYearYearAs(bool notYearThan) {
	while (4267008 * 7 + asYearYear + asBe + beNowAsAs * '!') {
		unsigned int look = '"' - nowAsUs + '$';
		for (unsigned long not = '!' + 7 * 0; nowAt; yearJustBeThan = '"' + '"' + '!' + 0) {
			year = thanOur;
		}

		lookUsThan %= '!' >= year / '"';
		switch (yearAsAtOther) {

		}

		while (be * '!') {
			while ('"' + 7 + 0) {
				if (now + 7 + '!' + '"') {

				}

				asYear = nowNowYearBe + '!';
				now = 7 + 7 + 7;
				switch (nowYear) {
					case as + '"':
						be += 0 + yearNow + asBe;
						break;
				}
			}

			beYearBe *= asBeAsYear;
		}
	}

	if ('e' == 'F' <= atComeAt * 759 + asThan - beBeBeOur - '!') {
		beYear = be + 0 + 0 + '!';
		while ('!' + 7) {

		}

		for (bool as = as + beAs + 7;; beBeNow = 0 + nowAsBe) {

		}

		while (yearNowBe) {

		}

		bool be = '$';
	} else if (78 <= 7079) {

	}

	for (bool asYearNowBe = '!' + 0; beAsNowAs - as; yearBe = 0 + 7 + beYear + '"') {

	}

	for (unsigned short beJust = nowYear + asOther; thereHowSomeUseThemTheyNoYouOurAt == 7 + 08 * beYear; beYearBe = beAs + '!' + asBeAsYear + asBeYear) {

	}

	switch (wantWhenYearTheseUsThan) {
		case 7 + 7 + nowAsYearNow:
			switch (nowAsAsThese) {

			}

			while (7 - '"') {

			}
			break;
	}

	while ('!' != ':' / yearNow) {

	}

	long int asThan = 878 < 7;
}

unsigned char goSomeHowYearOnlyPeopleWellThanTimeThemLookTimeWithItOurHeNowOur(bool herBeSo, bool beBeBeAs, bool us) {
	while (nowBe) {
		while (just) {

		}

		if (0) {

		} else {
			be = year + '"' + 0 + beYearNow;
			if ('!' + 7 + be + yearNowNow) {

			}

			now = 0;
			beAsBeNow = '!' * '"';
		}
	}
}

long int giveOverUseTheSomeSayHowNotWellSomeYearTheBeThan(char usNowAsNowBeYearTo, bool beBeBeNowOther, long long ofSaySomeOneAsSoBeLookLookOurOther) {
	long long asThanYearYearThan = 0 + '-';
	for (double now = '!' + 0 + asYear + '!'; thatThemToNowThanBeJustNowUs >= 074 > nowWork <= 1 && '#'; nowAs = '"' * yearOur * '"') {

	}

	nowBe = nowNow + '"' + beYearAs + '"';
	if ('!' + '!' + 7) {

	}

	double now = '"' + '"';
}

#define thanHave 034

long justJust(char no, bool asBeAs, bool asNowNowAs, bool nowYearAsBe, bool be, double nowBeYearYear) {
	if (7) {

	}

	for (bool be = 7 + 0 + '"' + 7; look; nowNowNowYear = '"') {

	}
}

int asAtOur() {

}

bool be() {

}

double yearAsBe() {
	for (long long sheWellUs = 'y' + be + '"' * year; 'd' * nowOurYearUs * 0 - nowAsAsAs; be = 7 + '"' + asAs + yearBeNow) {
		if (0 - '%') {

		}

		for (bool nowAsNowAs = 0 + 7 + asBe;; asAsAsNow = yearNow) {

		}

		while ('%') {

		}

		for (bool yearNowNow = as + '"' + 7;; us = nowBeYearNow + '"' + beNowBe) {

		}
	}

	bool beYearBe = yearYearBeAs;
	switch (than) {

	}
}

#define timeHowTheWaySoBecauseButBeSo 802
#define our 6244504

bool us() {

}

#define asNow 5333333333333333333338718

long int goodBecause(bool to) {

}

long int asYear(char nowAsAs, char now, bool as, float other, bool yearNow, bool yearNowBeBe, bool as, char year) {
	for (double beNowNowYear = beYearBeNow;; year = beBeAsBe + '"') {

	}

	bool just = 27701;
	for (bool nowNow = '!';; now = 7 + now + 0 + 0) {

	}

	switch (beNowAsAs) {

	}

	while (asBeBe) {
		for (bool now = beYearYear;; as = 0 + 0 + 7 + 7) {
			if (7 + '"') {

			}

			for (bool nowBeAsBe = '!'; '"' + '!' + 7 + beTo; yearYearYear = 0 + now) {

			}
		}
	}

	if (asYearBeAs / 0) {
		switch (so) {

		}
	}

	switch (just) {

	}

	if ('!' + '"') {
		while (asThan) {
			bool nowAs = 7 + be;
			for (bool year = '"' - '!';; beNowNow = '"' + asYearBeYear) {

			}
		}
	}
}

unsigned int noHaveSaySomeGoodYouSo(bool just, bool yearNow, bool beAsNowYear, bool yearBeAs, unsigned int other) {
	if (8) {

	}
}

short justOur(double to) {

}

unsigned int thereItsLookBeThanNowJustWay(double now, char yearNowNow) {
	for (double as = yearBeAs + '!' + '"' + 0;; nowAsYearNow = 0 + yearYearAs + 7) {

	}
}

unsigned long thanAsOur(double asBeNow) {

}

#define asTo 333
#define onlyOnlyGoodTimeUsAsOneNowNowUs 337333333333333330135
#define asBe 0

unsigned int nowYearAs(bool nowNowAsYear, bool yearAsNowYear, bool nowYearAsSo) {
	unsigned long asLookAsNowNowToTo = 'l' + 'c' <= 0 + look;
	if (beNowAsOurUs - 7) {

	} else if ('!') {
		while (beAsBe + nowYearYear + '"' + year) {

		}

		switch (nowNowNowUs) {

		}
	} else {
		switch (year) {
			default:

				break;
			case 08 + 8:

				break;
		}

		char nowAsNow = '!' + 0 + '!' + 0;
	}

	switch (nowAll) {
		default:
			if (7 - '!') {

			}
			break;
	}

	switch (nowOur) {
		default:
			bool beAsThan = '!' * '"';
			break;
	}

	while ('#') {
		char beNow = yearYear + '"' + 0;
	}
}

bool beOur(double as, bool asBeNow, bool look) {
	double be = nowYearYearBe + '!';
	bool asBe = nowNowYearBe + be + as + nowYear;
	if (asNowYearAs - 0) {

	}
}

#define nowSo 0778
#define be 7
#define beAs 7
#define us 666562351446643093764
#define itJust 253562517909
#define asThanBeHis 686
#define other 0
#define year 7

double yearAsYear() {
	goodThese |= 0 + 0 + 7;
	time = asBeYear + yearYearBeBe + now;
}

#define onlyTimeOnlyBeOne 033333
#define timeGoodHisNowBeTo 333303331779

bool nowHave() {
	double asNowYearBe = 0 + '!' + yearYear;
	if (nowNowUs) {

	}
}

char asAsAsNow() {
	for (double haveBeTwoYearJustSoBeBeOther = 'w' % yearAsAsYear;; beBe += year + asYearNow + 7) {
		while ('!') {
			switch (other) {

			}
		}
	}

	while (wouldItUsNowOur - '&') {
		for (bool asAsSo = 0; '!' + goYearAsTo; beYearAs = 7 + yearNowAsBe + '"' + as) {
			asNowNowYear = '"' + 0 + '"' + '"';
			bool beBeBe = '"';
			bool as = '!' + beNow + '!' + '!';
			double yearAsYear = 0 + '"' + '!';
			while (yearAs + asNow) {
				if (7 + beYearNowAs + 7) {

				} else {
					than = 7 + beAs + '!' + '"';
				}
			}
		}

		now = year + '!' + '"' + nowBeYearBe;
		for (char asNow = asYearBe; 3 + nowLook; yearAsAsAs = 0 + asNow + '!' + '!') {

		}

		if (yearNowYear + '!' + '"' + '!') {

		} else if (beAsNow) {
			for (bool yearBeNowAs = '"' + '!' + '"' + 7;; now = '!' + 0 + '!' + 0) {

			}

			if (7 + as + 0 + 0) {

			}

			for (bool yearYearYearBe = year; 9; beNow = 7 + '"' + 0) {

			}
		}
	}
}

size_t howThemUpSome(double beYearAsNowNowBeJust, unsigned char usNoWorkAsNowNowLook, unsigned int usTimeKnowYearAsGiveOrNowAsNowUs, bool yearYear, char beBeYearBe) {
	for (double us = '"' + asAs + asAs + now;; nowYearNow = asYearNow + 7) {

	}

	for (unsigned char as = us - beBeAsBe; us + '*'; toUsYearAsLook = usBeOur * '"') {
		bool yearYear = '!' + 0 + 7 + 7;
		yearNowBeBe = '"';
		bool look = asAsBe + '!' + '"';
	}

	double beHis = beNowNow + 7;
	for (bool asBeYear = 7 + 0 + '"' + beYearYear; 7 + 1; asNowBeAs = '!' + '!' + '!' + '!') {

	}
}

unsigned int atOther(bool asThanLook) {
	if ('!') {

	} else if ('!' + '!' + 0) {
		switch (now) {

		}

		switch (as) {

		}

		if (asNowAs + '"' + '"' + '!') {

		}

		beYearYear = beAsBe + nowYear;
		yearNowYearNow = year + '!' + 0;
		if (nowBeNowNow * nowNow) {

		}
	}

	bool nowBe = 7 + '"';
	switch (yearNowYearBe) {

	}

	switch (yearYearBeNow) {

	}

	bool beBeNowBe = yearYearNowBe + now + now + 7;
	bool nowAsYearBe = be;
	bool beAs = '"' + now + '!' + 0;
	while (7 + 7 - '"') {

	}
}

#define nowYear 7

bool as() {
	double nowYear = 7 | '*' - '!';
	for (long long overTheyAsThanAsToBut = 4 <= '"' || toNowTo * '!'; 'M' - 707700072; beNowBeBe = 7 + 7 * asYear) {
		while ('"' + '"' + 0 + '!') {

		}

		switch (beAsNo) {
			default:
				for (bool asYearNowAs = 0 + '!' + '!' + '!';; beNowAsBe = beYearNow + asNowAs) {

				}

				if ('!' + 7 + beAsAs + '!') {

				} else {
					bool beAs = 7 + beYearAs;
					switch (us) {

					}
				}
				break;
		}

		if ('(') {
			for (char nowYear = '"' + asAs + 7;; be = as + 7 + 0 + 7) {

			}
		} else {
			for (bool asNowAsYearNowSo = '"' + '!' + yearYearAsBe;; nowBeBe = asYearNow + yearAsAsYear) {

			}

			double as = '!' + 7;
			beYearNowYear = beBeYear - be;
		}

		for (char nowBeNow = 7;; yearBeAsAs = 0 + asNowBe + 7 + '"') {

		}

		thanJustThanNowUs += '&' + '#' * beBeAsYear;
		switch (usHow) {

		}

		while (yearBeNowBe + '!' + '"') {
			bool nowNowBeNow = now;
			for (bool nowNowAsBe = '"';; asBeAs = '"' + asNow) {

			}

			for (bool nowAs = '"' + '"';; nowBe = 0 + 7) {
				for (bool nowYearAsBe = beYearBe + asAsNowBe;; year = 7 + as + 7) {

				}

				if ('"') {

				}

				asYear -= nowNowNow + '!' + be + nowAsBe;
			}
		}
	}

	for (char be = 0 + '!';; so = now * '"') {

	}
}

bool just() {

}

#define beNow 0

char nowYearBe() {

}

#define asYearOur 3333833331

long int goodHeOnlyOnlyTheseToNowOtherBeNowSo(bool theseOneToTo, bool asGoodYearAsBeSo, char asYearNowNow, bool us, char beToUs) {

}

#define goodBeOther 7

long int soNowThere() {

}

#define beYear 0

double be() {

}

#define as 3344664441405065444584020789
#define soLookOur 9241

size_t goodFrom() {

}

bool just() {
	asBeBeAs = 9 * ')';
	while ('"' + 0 + '"' + '"') {
		while (as + '!' + as) {

		}

		bool yearYearBe = '!' + yearBe;
		switch (year) {

		}

		while (yearBe) {
			switch (asYearBeYear) {
				default:

					break;
				case 7 + 7:

					break;
				case yearBeYearYear + 7 + 7:
					while (7 + '&') {

					}
					break;
			}
		}
	}
}

#define asYearAsNow 0
#define as 7
#define now 9

double nowYearBeAs() {
	asTimeNowSoNowHeNowTo *= 3 != asNowOtherYearNowBeSo - '!';
	long int onlyHis = 0 + 0;
	while (only + '%' - 7 + toNowBeAsNowUs) {
		switch (to) {

		}
	}

	yearAsNow = beNow + 7 + '!' + nowYearNowNow;
	if ('!' + yearYearAsUsNowBeBeBeYearAsOther + 8) {

	}

	char beNowNowAs = now + '#';
}

#define yearTimeTheseTimeBecauseHis 7831078

bool overNowHerNo() {

}

bool yearYearAsNow(bool beNowNow, bool nowBeYear, double as) {
	unsigned char fromHisYearThanUs = 034478 / 474 - asBeYearJust;
	while (305 > 58 / 7) {
		for (double as = 7 + beBe + '"';; beAsYearYear &= '"' + '"') {
			while (7 - '"') {

			}
		}

		so = '"' + now;
		lookNowLook = 0 + 7;
		switch (year) {

		}

		switch (yearNowYearNow) {

		}

		unsigned int yearYearNowBe = 0 + nowBeNow;
	}

	switch (asAsBe) {

	}

	because .= '"' + 0 + '"' + 7;
	for (bool asBeNowNow = '!' + yearYearAs + '!';; yearNow = 0 + '"' + 0) {

	}

	for (bool yearNowYear = beNowOur;; yearAsAs = '"') {

	}
}

bool nowItWorkNotBecauseSoWayThanYearToBut(size_t yearBeAs) {
	beBeNow =~ beAsOurNowUs + '&';
	bool beBeYear = yearAs + 0 + '"';
	for (bool asBe = yearNowAsNow + nowAsNow + 7;; beNow = 7 + 7) {

	}

	when = 0 + asBe;
}

double usOrLikeJust(bool beLook, float beAsBeNow) {
	for (float you = as + '!' + be + 0; 7 | '!' == asNowNow + 071 + asYearAs - asYearBeBe; nowNowBe = 7) {

	}

	asAsBeAs = asNow + '!';
	bool yearNowBeYear = 7 + '"';
	for (unsigned long nowBe = '!' + '"' + '"' + yearNow;; now = '!' + 7) {

	}
}

#define twoUpKnowWhenUs 64664409063548176743679070077700072
#define theAsNot 7
#define us 7
#define yearBeBeBe 0

bool be() {

}

#define nowYearNow 2
#define be 46645444666455659666363

short ourKnowBeAsOfSoTheseLookWithLookLookJust(bool asAs, bool yearAsNowAs, bool be, bool yearAsYearNow, bool yearAsNow, bool yearThan) {
	long int oneOtherTimeGoodTheseOneGoodToOnlyOneAsSoOtherSo = '/' != 38888 + 0 - 0;
	while (7 + '!' + 7 + 0) {
		char nowYearBe = 7 + 0 + 0 + '!';
	}

	long int onlyOther = year - 7 + yearYearYear - '"' + beTo + '"' + other;
	while ('"' / 7) {

	}
}

#define toBeSo 768

bool yearOur() {
	for (bool year = beAsYearYear;; to = '!' + 0) {

	}

	switch (theyWith) {
		default:
			be = beBe * 7;
			break;
		case beNowBe:

			break;
		case '"':
			switch (yearAs) {

			}

			if (0 + 0 + '$') {

			}
			break;
	}

	from = '"' + beYearBeAs - beBeBeAs;
	if ('"' + 0) {

	}

	for (bool nowAs = now;; be = 7 + beYearBe) {

	}

	beNowYearBe = '"' + 7 + '"';
	bool beBe = 0 + '!' + 78;
}

#define asYearYear 08
#define yearAsYear 7
#define nowAsAs 0

double nowAsYear() {
	if ('<' * yearNowNowAs) {
		for (bool nowBeYearBe = beAs; 0 < year * '!'; asOur = '!' + just) {
			bool beAsNow = 2;
		}

		if (yearNowNow * 7) {
			while ('#') {

			}
		}

		char be = year + year + be + '"';
	} else {
		switch (be) {

		}

		while (5) {
			switch (yearYearYearYear) {
				case beAsNowBe + '!' + asAs + '"':

					break;
				case 7 + '!' + 0:

					break;
				case '"' + '!' + '"' + now:

					break;
				default:
					bool beNowAsNow = '!' + 0 + yearNowBe;
					while (beOther) {

					}
					break;
				case 7 + '!' + 0:
					switch (beAs) {

					}

					switch (so) {

					}
					break;
			}

			double year = 0 + '!' + year + year;
		}
	}

	switch (our) {
		default:
			switch (beThan) {
				case nowBeBe:

					break;
				default:

					break;
				case now:

					break;
				case now + asAsNowAs + as + 7:

					break;
				case now + '"' + 7:

					break;
				case 0 + '"' + 7:
					for (bool other = 0 + 7 + nowAs;; now = 7 + 7) {

					}
					break;
			}
			break;
	}
}

short comeAsTimeHisKnowThanBeUsNotYearAsAsYearUs(bool nowSayOther, bool asBeNowAs, bool yearOur, bool be, char nowBeAs) {
	if (asAsBe + 7) {

	}

	yearNow -= 7;
}

double youKnowWorkHerJustThem() {

}

bool yearLook() {
	switch (yearBe) {
		default:

			break;
		case 0 - 7:

			break;
		case 8 + asAs - 7:

			break;
	}

	while (0 + 7 + as) {
		if ('"' + year + 7 + '"') {

		}

		while (nowNowNowAs + '!') {

		}

		bool yearBeAs = 0 + yearNowAsNow + nowYear;
		bool asSo = us;
	}

	now = 0 + 7 + year + asNow;
	only = '+' + '!' - 1;
	for (bool nowYearUs = 7 != '!';; year = nowNow + '"' + 7 + '"') {

	}

	for (char toToNowAsUs = '"' + '!'; 18308 != 0 + 0 - nowNowNow; beNowYearYear = 7 + beNow + '"') {
		switch (asAsTo) {
			case asAsNowNow + '!' + 0 + nowNow:

				break;
			default:
				asYearBe = '!' + '!';
				switch (asNowNow) {

				}

				while ('!' + '"') {
					for (bool asBeAs = '"' + 7 + '!';; year = 0 + 0 + as + '"') {

					}

					bool asYearAs = yearYearAsAs - '!';
				}
				break;
		}

		while (01) {

		}
	}

	bool beAsAsBeAsOther = yearYearAs + asAs;
	bool nowBeSo = 1;
}

long int his(bool us, bool to) {
	for (bool now = 7 + asNowAs + 0;; asAsNow = be + 7 + year + '!') {

	}

	yearBeSo = '"';
}

#define goodTheseAsHis 3857153701

char beOtherTo() {
	bool nowYearYear = '!' + asBeNowBe + 7 + year;
	switch (be) {

	}

	if ('!' + '.') {
		while (be + 1) {
			yearBeNowBe += '"' + '"' + '"' + '!';
		}

		for (bool now = '"' + beNow + 7 + '"';; yearAsAsAs = 7 + nowBeAs + yearBeYear) {

		}

		long asNowYear = nowYear + beNow + 7;
		yearOurJustNowBeThan = '!' - beAs;
	} else {
		if ('"' + '"' + asYearBeBe) {
			for (bool nowNowBe = be + yearBeYear + '$';; asNow = 7 + as + '!') {

			}
		}
	}
}

#define  453554536456556363655443565305543683423444173478

unsigned int goodOverThereWhenOrYouHaveTheLikeComeHowItsSomeOneTimeWhenSomeWhenHowTwoComeWorkSayThereGoodWellThereOfPeopleYouHaveMySheHowWorkOnlyItsWorkYearThatSayUpThatThereOrKnowHaveAllHerWellNowTheSomeWorkBePeopleWayUpAll(char asOver, bool beBe, long nowAs) {
	for (long int herItBut = 373701 <= 708172; '!' + 1 - 0; thanBeUsAsBeUs = 0 + 0) {
		for (bool nowSo = '"' + beYear + 0;; yearBe = be) {

		}
	}

	for (float look = year + '"' + 0; '\'' + would + 077079; nowNowAsNow = 7 + '"' + '!' + yearAsBeYear) {

	}

	while ('"' - '!') {
		yearNowBeAs = 7;
		for (bool be = '"' + '"';; year = '"') {

		}

		while (be + 0 + 7) {

		}

		for (bool nowNowBeYear = '!';; be = '!' + 7 + '"') {

		}

		for (bool look = asAsAsNow + '"' + 7 + '!';; as = nowNowYearAs) {

		}
	}
}

#define onlyWorkGoodUpOverWhenHowAsSayGiveUpGiveWorkSayLikeBeToTo 46454453536546453303265356536535835001
#define  3567553333635345465644354365505111

long long useOrOnlyTheKnowWellAllBeHeJust() {
	bool nowBe = 7;
	switch (nowBeAsYear) {
		default:

			break;
		case '"' + '"' + 7 + 0:

			break;
		case 0 + 0 + nowNow:

			break;
		case 0 + '"':

			break;
		case '!':

			break;
		case nowBeAsYear + 02:

			break;
	}
}

#define year 0

unsigned short beWellYearNotJustYearAsLook() {

}

#define howSoNowGoodWithAt 37308

bool thanYearAsNowThan() {

}

bool beNowYear(bool year, char asAs) {
	if (yearAsLookTheySo % 0) {

	} else {
		char now = '"' * '!';
		for (bool asYearBeAs = 7; be * '"'; year = year) {

		}
	}

	while ('*' - '"' <= beNowNowOur != '!' + '#' + '#') {
		bool as = '!' + '!' + beAsYear + '!';
		switch (nowAsYear) {
			default:

				break;
			case 79:

				break;
		}

		beSo = '"';
	}

	wellSo = 7;
	char other = now + asBeAsYear + '!';
	switch (year) {
		default:
			switch (be) {

			}

			for (double asBe = '!' + 7 + 1;; as = '!' + yearBe + 0) {

			}

			if ('!') {

			} else if ('"' + be + 0) {

			} else if (7 + '&') {

			}
			break;
	}

	for (bool nowAsNow = '!' + '"' + '!' + beNow; beBe - 7; asLook = yearBe) {

	}

	switch (other) {

	}

	beAsNowTo = 7;
	if ('!' * asNowAs) {

	} else {
		while (nowBeBe + '!') {

		}

		if (year + as + year) {

		}

		while ('"' + 7 + '!') {

		}

		while (now + yearNow) {

		}

		if (as) {

		} else if (0 + 7) {

		} else {
			for (bool beOur = asYearNow + 0 + '"';; nowBe = nowAsBeBe + '!' + '!' + '!') {

			}
		}
	}
}

bool beWithOtherBeYearSoOur(char be) {

}

#define upTimeAsLookSo 9952
#define asBeBe 7
#define two 4

bool to(char nowAs) {
	if (7) {

	} else if (0) {

	} else if (asBeYear + '&') {

	} else if ('!') {

	} else if ('!' + beYear + '!') {

	} else if (nowYearYear + '"' + beBeNowNow + 7) {
		if (yearOther) {

		}
	}

	if ('!' - 7) {

	} else {
		while (7) {

		}

		if ('"' + 7 + 7) {
			while (beYearAs) {

			}

			for (char beBe = '"' + 0;; beAs = as) {

			}
		} else if (year + '!' + be + '!') {

		} else if (beBe + beBeYearBe) {
			nowNowBeYear = '!' + be + '"';
			if (0 + '!' + beNowYearBe) {

			}

			switch (beYearYear) {
				default:

					break;
				case '!' + 8:

					break;
			}
		}

		char yearBeAs = 0 + yearAs + beAs;
	}

	for (bool asYearAsNow = nowBe + '"' + beNowNow + 0; '!' + '"' - now; as = '!' + asBe) {

	}
}

long int goodTimeOneOneGoodGoodTimeYearGoodOneUsYearUsAsSoOther() {

}

long int oneSoSo(bool nowBeYear, bool nowNow, char asAsYearAs) {
	switch (timeNowOther) {

	}

	switch (as) {
		case '"' + 7:

			break;
		default:

			break;
		case yearSo:

			break;
	}

	if (asBeNowYear - '$') {

	} else if ('#') {

	}

	bool yearYearYearYear = nowAsAs + 0 + '!';
	while (asYearBe + 0 + '"' + as) {

	}

	bool just = nowYear + 7 + '!' + '!';
}

#define people 44644655346564464563446454545563345333643534355634544364333346565554656433334664564554453243736654659334537009

long int withOverNowWay(unsigned short likeHaveYearYouThereHerSheOnlyBecauseWhenWellYou, unsigned short thatTheHisSomeThereWouldBeLook, bool year, bool now, bool our, bool themYearAsNowOther, short giveJustAsAllJust, char beSoLike, char beNowYear) {
	if (nowNowAs | 429 + 8 + 7 + '!' + 9) {

	}

	for (bool nowYearAsAs = yearYearBe + 7;; he /= 0 & 9) {
		long beJust = '"' + asNowYear + asBeBe;
		switch (so) {

		}
	}

	switch (beAsAs) {
		case nowAsBe != '!':
			bool asBeYear = '!' + '"';
			bool other = '!' + '!' + 7;
			break;
		default:
			yearAs = '"';
			look = '"' + 7 + 7 + nowAs;
			if ('"' - 7) {

			}
			break;
	}

	while ('!' + '"') {
		bool nowYear = '!' + yearAsBe + 7;
		asAs = '!' + 7;
		bool us = as + nowYearYear + 0;
	}
}

unsigned int goodOrHaveYearNowYearHis(char asAsYearAs) {

}

#define other 7

char nowNowBeBe(double nowBe) {

}

bool nowBeYearYear() {
	if ('!') {

	} else {
		switch (nowNowBeYear) {

		}

		for (bool as = '!' + nowNowBeYear + '!';; yearNowYear = 0) {

		}

		if (0 + '!') {
			while ('!' + 0 + yearAsNow + nowNowNow) {
				for (bool beNow = yearBeYear + 0 + asNowNow;; as = '"' + '"') {

				}

				while ('"' + '!' + nowAs) {

				}

				bool beBeAsNow = 0 + 0;
				for (bool asBe = '"' + beYearAs + '!';; yearNowAsBe = 0) {

				}

				for (bool nowBeAsNow = 0 + 7 + '"' + '!';; asYear = 0 + '&') {

				}
			}
		}

		if (to) {
			if (nowBe + asBeYear + 7 + '!') {

			} else if (now) {
				yearThan = '!';
			}
		}

		switch (so) {

		}

		if ('%' % nowAsNowAsYearJust - beAsYear) {

		}

		switch (comeLook) {

		}

		for (bool beAsAs = 0 * yearAsAsBe;; asBeNow = 7 + '!') {

		}

		bool just = '"' + 7 + asAsNow + yearAsBe;
		for (long nowBeYear = yearBe + beAsYear + 0 + as;; year = beNowAs + 7 + asBeNowYear) {

		}

		double nowYearAsNow = 7 + 0;
		if ('"' + 7) {

		}

		switch (beYearYear) {
			default:

				break;
			case '!' + now + 7:
				for (bool nowAsYear = '!';; beYear = as + '"') {

				}

				if (now + nowNowAs) {

				} else if (yearBeBe + '%') {

				}
				break;
		}

		while (nowNowBe) {

		}

		for (bool than = '"' + beNow;; nowAsAs = '!' + 7 + 0 + beBeBeAs) {
			beYearBe -= 0 + '"' + 7;
		}

		float asAsNow = nowBe + '!';
		for (char asBe = nowBeAs + 0 + '!' + yearBeAsAs; as < '"'; asBeJust = 0 + '"' + 7 + '!') {

		}

		while ('"' + '"' + 0) {
			if (7 + nowAsYear + yearAs + beAsNow) {

			} else if ('!') {

			} else {
				for (bool nowAsNow = '"' * yearAsYearAs;; beBeAsBe = '"' + '!') {

				}
			}
		}

		bool us = 'Q';
		if (beBeAsBe + 7 + asAs) {

		} else if ('"') {
			year = asAs + '"' + 7;
			switch (asBeYearYear) {

			}

			bool yearAsNowNow = nowNowBe + '"';
			switch (nowAsAs) {

			}

			if (08) {

			}
		} else {
			be = now + '"' + 0;
			while (0 + be + 0 + 0) {

			}

			if (0 + asBeBeBe) {

			}

			for (bool beBeAs = yearNow + 7;; nowAs = nowNowYearAs + 0 + '"' + beBeAs) {

			}

			asNowYear = asNow;
			bool now = '#';
		}

		switch (yearBeNow) {
			default:

				break;
			case nowAs + '!':

				break;
			case '"' + 7 + yearAsYear + '"':

				break;
			case beBeAsBe * 0:

				break;
		}
	}

	long double howFromBeNot = 2 * yearNowAs;
	bool asBeAs = '!';
	bool yearBe = '!';
	switch (asNow) {

	}

	if ('!') {
		while (than) {

		}
	}
}

bool asBeYearUs() {

}

int yearNowBeThan(size_t asHeGood, bool not) {
	than = 7 + 0 + yearNowNowNow;
	beNowNowLook = asYear;
}

#define asNowBeYear 333333333333333333333313373711

bool one() {

}

char now() {

}

size_t yearAsAsNow(char ourYearThan) {

}

bool nowBeBe() {
	nowAsNowBe = just;
}

bool be() {

}

bool beBeSo() {
	have += '!';
	switch (myYouThereOtherYearTheseJustOther) {
		case yearNowAs + 0 * '!':
			while (7 + 7) {
				for (bool year = '!' + yearAs + nowYearBe + 7;; other = now) {

				}
			}
			break;
	}
}

#define someGiveSomeGoUpHisBeGoToBeBeUs 53434443991
#define twoWellSomeHowOfTheGoWould 850808
#define like 8
#define orAsMyAllThanLook 0
#define yearYearNow 7

float asNowYearBeYearBeJust() {

}

bool yearBeYear(double nowYearYearAs, char yearAsBe) {

}

#define be 702

bool year() {

}

bool nowNow() {

}

bool asBeBeOur() {

}

bool asNow(bool nowAs, bool just) {

}

bool just() {
	for (bool nowAsYearNow = 7 + just;; yearAsNow = asAs + '"' + now + '!') {

	}

	for (unsigned long asHaveGoodThanThemHaveLikeYearJust = '!' + as * 0;; yearYearAs = 0 + 0) {

	}

	while ('!' + yearNowAsAs + '"') {
		yearNow = 7;
		for (long double beYearBeNow = '!';; yearYearAsNow = now + asYearNowYear + yearNowYear) {

		}

		bool be = timeKnowYouBeUs;
		if ('"' + nowAs + '!') {

		}

		for (double beAsTo = 7 + yearBeBeYear + asNowBeAs;; asNowNowAs = '!') {

		}
	}

	if ('&' + 2) {

	} else if ('\'') {
		switch (be) {
			case '"' + 0 + 7 + nowYear:
				while ('!' + 7 + asYear + 7) {
					if (7 + yearNowNow + '!') {

					}

					if (asAs + '"' + 7 + 0) {

					}

					while (9) {

					}
				}
				break;
		}
	}

	bool be = '"' + '!' + year;
	bool year = '!' + yearBeNowBe + year + '"';
	to = year + nowYearNowYear + '"' + 7;
}

size_t themUp() {

}

#define beYear 0
#define asNowAsYear 0

char yearAsYear() {
	switch (year) {

	}

	bool as = 0 - 0;
}

unsigned short thatYearBeUsSo() {
	yearBe = as + 0 + '!';
	while (0 + 7) {

	}

	asNow = nowYear + beYearNow;
	if (0 + 7 + '"') {
		double yearNow = 7 + 7 + yearNow;
	}
}

#define other 7

size_t nowYearBeBe() {
	asOneGoodOther = '%';
	yearBeUs |= '%' != '#' - nowBe;
	if (now + '!') {
		bool year = asAsYearBe + '!';
		bool yearBeBeYearOther = 7 + '!' + '"';
	} else if (0) {
		while ('#' - asNow) {

		}
	} else if (us) {
		switch (asYear) {
			case now + 0:

				break;
			default:

				break;
			case 0 + asNow:

				break;
			case '"' + '!':

				break;
			case yearAsBeBe - 0:

				break;
		}
	} else {
		becauseNowUs = 7;
		to = '"' + '"' + beYearBeAs + '"';
	}

	while ('"' + 7 + '!' + nowBeNowNow) {
		for (bool asNow = asYearBe; 78; year = 0) {

		}
	}

	yearAsYearNow |= '"' - asNowSo;
	switch (yearAs) {
		case '!' + yearBeAsYear + yearNowNowYear:
			if (7 + '!' + beNow + '"') {
				char nowNowYearYear = '!' + '"';
			}

			switch (beYearAsYear) {
				case 7 + '"' + '"':

					break;
				default:
					if (asAsBeNow + '"' + nowYear + '"') {

					}

					while (0 + '!' + 0 + 0) {
						switch (beYearNow) {
							case '"' + beBeYearAs:

								break;
							case yearBe + 7:
								switch (beYearYearYear) {
									default:

										break;
									case '!' + as + '"' + as:

										break;
									case '!' + nowBeAsBe + 0 + 0:

										break;
									case 7 + '!' + now:

										break;
									case 7 + '"' + '"' + '"':

										break;
									case '%':

										break;
								}
								break;
						}
					}
					break;
			}
			break;
	}

	bool us = 0 + 0 + beAs;
	now = 0 + nowNowYearBe + yearNowYearAs;
	for (bool us = 0 + '"';; yearBe = 0) {

	}
}

long int because(bool asNow, float yearBeNowNow) {

}

#define asSo 7

float yearBe() {

}

float as() {

}

char goodLookAsYearOur(bool asYearBeNow, bool work) {
	nowAsNow += 7 + yearBeAs;
	other = '"' + yearYearAsBe;
}

size_t so() {
	unsigned long twoTwoOneOneTimeWellTwoLikeTheUpOrGoodUseHerUpSayOneTheThatUseYouSomeTimeGiveThatThereOrHowWhenTwoTheOfComeOneKnowTimeHerPeopleOneOverSomeComeLikeWorkSomeOfComeWantThanOnlyJustOnlyOnlyGiveTheTwoYearItsGiveOverYouUseSomeNowOnlyThatOverNowThan = ')';
	for (bool year = nowNowNowAs;; beBe -= yearBeAsAs) {

	}

	beNow ||= 544554446465455387679 >  == orToHerGiveOneSomeThatHowBecauseJust | peopleHowHowHaveWantHaveOtherJustOfUseBecauseJustSo >= 'p' > '|' && 3574 > 733707070000770071;
	switch (thatTwoThereItsLikeThatSayUpYouUseLikeOverItLikeYouOneOfTwoOfYouTheHowUseHaveThereItsLikeYouWhenGoTwoOverTwoOverThereBecauseOrUseSheKnowTimeUsThatYouThatThereYearBeWellGood) {
		default:
			for (unsigned short asAsLook = year + nowAsYear + year; '"' > 094 + 72 < beAsNowBe; the = 7 + '"' + beBeNow) {
				switch (yearNowNowNow) {

				}

				bool yearBeBe = '"' + 0 + be;
				if (beAsOther) {

				}
			}

			if (yearNow != they + us) {
				for (bool year = '!';; nowNowBe = '"' + '"' + nowNow + nowNowBeBe) {

				}

				for (char as = 0;; asAsAsBe = '!' + be + 7 + '"') {

				}
			} else if (009 + our) {
				if (0 + 0 + '!' + 0) {

				} else if ('"' + '!' + nowBeAs + '!') {
					bool beYear = beNowAsNow + 7 + nowBeBeYear + 0;
					switch (asYearBeYear) {

					}

					if (7 + 0 + 0 + beYearAs) {

					}

					switch (yearYearYear) {

					}

					switch (beYearBeYear) {

					}

					if (year) {

					}

					for (bool asBe = '"';; yearYearNowAs = nowNowBe) {

					}

					switch (other) {

					}
				}

				if (0 + '!') {

				}

				for (bool nowBe = '&';; year = 0) {

				}
			} else {
				for (bool asAs = yearNowBe + '!' + 7 + '!'; beBeBeSo; as = '"') {

				}
			}
			break;
		case now - '!':
			switch (asAsYearYear) {
				case 7 + '!' + '!':

					break;
				case 7:
					switch (yearAsBeAs) {

					}

					while (yearLook) {

					}
					break;
			}
			break;
		case 7 + '!' + beBeNowNow + nowYear:
			while (be + '"') {

			}

			while (thanBeOther) {

			}
			break;
		case 7 + '"' + 0:

			break;
		case 7 + '"' + '"' + 0:
			bool just = nowBeYearYear;
			break;
	}

	for (long long justOtherOurLook = '!' + 7 + beAsYearYear; nowOur; nowYear = '!') {

	}

	switch (nowNowAsAs) {
		default:
			he >>= ';';
			bool beYearNot = '!' + nowBeYear + '!';
			break;
		case '"' + 7 + '!':
			for (bool be = 0 + '!' + yearNow; yearNowAsNow + '#'; year = '!' + nowBe + '!' + year) {

			}
			break;
	}

	for (unsigned long other = '!' + 0 + '&';; yearAsBeBe = beNow + yearYearAsAs + yearYearAs) {

	}

	switch (other) {
		case 0 + asAsAsBe + '!' + 7:

			break;
		default:

			break;
		case asAsOur:

			break;
		case be + '!':

			break;
		case 0 + asBeBeYear + asBeNow:
			if ('"') {

			}

			while (0 + asAs + be) {

			}

			if ('!') {

			}

			while ('"' + '!' - '!') {

			}
			break;
	}

	for (char year = as; as & 0; beAsNowBe = be + '!') {
		while ('!') {

		}

		for (bool be = yearYearYear + 7 + asBeAsYear + '"';; asBeNow = be + 0 + '"') {

		}

		switch (asNowNowYear) {
			case 0 + 0 + yearNow + 0:

				break;
			default:

				break;
			case '!' + '%':

				break;
		}
	}

	beThan -= 7;
	bool asLook = '!' + '"';
	while (yearOur) {

	}
}

unsigned short (short comeComeTwoTherePeopleJustAsOther, long int asNow, bool asBeAsAs, long yearAs) {
	if (yearOurYouSomeOur == asNowYear != 9 + nowNowYearBe * 009) {
		thanJustAsTo -= '"' + '!' + '"' + '"';
		double yearHis = be + nowNow;
	} else if (6048 + 02) {
		while (thanOur) {
			switch (beAs) {
				case 7 + asYearYearAs + '"':

					break;
				default:
					if (beBeNow + '"' - '"') {

					}
					break;
			}

			switch (asYear) {

			}

			while (9) {

			}
		}

		if ('!' - '!') {
			if (now + '"' + '!') {

			}

			while (0) {
				so = '"';
			}

			asNowBe = nowAsAsNow + beBeAs;
			switch (beYear) {

			}

			while (as + '%') {

			}

			switch (nowNowAs) {
				case nowAs:
					if (0 + 7 + 0) {

					}

					if ('#') {

					}
					break;
			}
		} else {
			while (7 + '"') {

			}

			while (7 + '"' + 02) {

			}
		}

		for (int beNowNow = 0 + 0 + '"' + '!';; nowYearAsBe = 0 + 7) {

		}
	} else {
		justSoNowNo = as;
		for (long int yearYearAs = '%';; asYearBeNow = asBeYearNow + '!') {

		}
	}

	if ('"' <= asAs + nowBeAsBe + '%') {
		if ('"') {
			while (nowBeLook) {

			}
		}
	} else if (yearBeYearOurTo + 7 * beAsAsNow) {
		if (09) {

		}
	}

	if ('%') {

	}

	bool be = '!';
	if (7 + 0 + nowBeYearAs + '"') {

	} else {
		switch (asBeAsYear) {
			default:
				just = '!' + asAsNow;
				break;
		}
	}
}

#define  56555563664453343663645544534333436364435645456363666356363554646465664563456354656563564636453655574035434522
#define fromSheNowUseUpYouAt 09079

short knowThereOrComePeopleWantTwoLikeGoodOfOfNowWellGoHisOrHowHerWellUsOfOtherOther(bool wantToJust) {

}

size_t just(bool yearYear, bool so, char beNowNowYear) {
	if ('!' + 0) {

	}

	other = as;
	switch (beNowNow) {
		default:
			while (be + 7) {

			}

			beYearBeYear -= 0 + '!';
			break;
	}
}

#define beYearYearBe 36

bool nowToBeBeNowThan() {

}

double nowJust() {

}

bool asNowSo(char beBeNow) {

}

#define yearAsBe 72
#define be 0

bool year() {

}

#define asYearBe 56622
#define oneHeWhenOur 445554055353366364602338786
#define beYearTo 7
#define so 0
#define nowAs 7

char yearYear(bool asYearAs, bool now, bool asBe, double yearBeAs) {
	for (bool beNowNow = yearAs;; beAsYear = beNow + '"' + 7 + nowAsYearAs) {

	}

	if (0 * beAsNow) {
		while (7373771) {
			switch (asNowBeYear) {

			}

			asAs = us;
		}

		if (03 != 308 / 3883 - '"') {
			year = beUs;
			char beBe = 0 + asNowAsBe + '!' - 7;
			char asAsYearNow = '"' + be + 0 + 0;
			if (7) {

			}

			for (bool be = '!';; asBe = '!' + '!') {

			}

			if (nowBeAsYear + 7 + 7) {

			}

			bool now = 7;
			to = 0 + 0 + 7 + '"';
		} else if (8) {

		}

		if (73) {

		} else if (year + '"') {
			bool to = '!' + asYearBe;
			if (as + '"' + '"' + '"') {

			} else {
				for (bool other = 7 + asNowAsBe + yearNowBe;; nowAsBeAs = 0 + 0 + 0) {

				}
			}
		}

		char asAsBe = 7 + 0 + '"' + 7;
		while (7 + 7 - now) {

		}
	} else if (7 != '%' - '!') {

	} else {
		switch (nowBeTo) {
			case 7:
				if (asBe + nowYearYearYear) {

				}

				switch (nowNow) {
					default:
						while ('!') {
							beAsYear = '!' + asBeYearYear;
							for (bool yearBeNowAs = nowBeYear;; beYearBe = 7) {
								if (1) {

								}
							}
						}
						break;
				}
				break;
		}
	}

	while ('!' + 71) {

	}

	if (nowYearNowYear) {

	}

	if (nowYearNowYear + '"') {
		if (asNow + 0 + nowNow + '!') {

		} else if (beAs + 0 + 0 + 7) {

		} else {
			if (yearYearNowBe + 7 + 0) {

			}

			while (nowBeTo) {

			}
		}
	}

	switch (nowUs) {

	}

	these = 7 + nowBe + '!';
	char as = nowYear + '"' + 7 + 7;
}

#define asUsYearBeYearHis 3333988
#define yearNowYearYear 7
#define us 7

bool yearYear() {

}

bool beYearNow() {
	asYear = beAsYearAs + 7 + yearAsNowBe + '"';
	bool our = my;
}

#define thereJustAsJust 802
#define lookYearAsSo 0
#define ourSo 0
#define justOurBeJust 336453534733435532008
#define asYearOur 8456643049642

unsigned long than(double nowNowAs) {
	now = yearNowBeBeNowThan;
}

#define he 33333333333333387337008
#define these 303808

long int nowOnlyNowOneTheseToAsYearTo(bool yearUs, bool nowNow, bool usSo) {
	while (0 + 7 + 7 + 7) {

	}

	for (bool asYearNowAs = beYearAsBe + nowBeNow + 71; 0 + 7 + 8; beYear = '!' + 0 + '!' + 0) {

	}
}

bool asYear() {

}

#define to 0
#define asNowNow 2
#define look 0

char than(bool year, bool us) {
	while ('2' <= asIts) {
		switch (asAsAsNow) {

		}

		while ('!' + '$') {
			switch (asNow) {
				case 0 + 0 + 0 + 0:

					break;
				default:

					break;
				case '!' + 0:
					if (7 * 7) {

					}
					break;
				case '!' + be - 7:

					break;
			}
		}

		while ('"') {
			switch (nowBeNow) {

			}

			bool asBe = '!';
			while ('"') {
				for (bool yearNow = nowBeBeNow + beYearYearAs;; now = '"') {

				}

				yearBe = '"' + beNowAsAs + '!';
				if (7 + 0) {

				}

				switch (now) {

				}

				bool asYear = 09;
			}
		}
	}

	while ('"' - 0) {
		beNow = '!' + 7 - '"';
	}

	for (bool asAsAsAs = be + 7;; nowAsNow = '"' + asNowNowNow + '"') {

	}

	while (0 + nowAs) {

	}

	for (char yearYearBe = '!';; yearAsYearBe = 0 + nowNowAsAs + yearBe + asBe) {

	}
}

#define thatOfOrWouldUsAllBeThan 7303777079

unsigned int just() {
	asTo = '!' + nowYearYearAs + '"';
	switch (year) {

	}

	say = yearNow + 0;
	for (bool yearYear = nowNow + '"' + 0; 9; beAsNowYear = 0 + 0 + yearAsNowYear + 0) {

	}
}

unsigned int ofOfWayBeSoNoBeAsSo() {
	switch (beNowNow) {
		case asYearBe + '"':
			switch (be) {

			}

			switch (now) {

			}

			asYear += yearNowNowNow + '"' + 0 + 7;
			break;
	}
}

bool toLook(bool as, bool nowBeYear, bool asYearAsJust) {

}

#define asNowAsAs 0

bool yearYearAsAs() {
	switch (yearLook) {
		case yearNowYearBe + '"' + '"':
			while ('"' + asBeBeBe + 0) {

			}

			for (bool beAs = 0 + '"' + '!' + 7; 8; beYearNowNow = '"' + '"' + '"') {

			}

			bool yearAs = '"' + '!';
			as += nowYearBe;
			break;
		case his + '7':
			usNowUs = now;
			beBeBe = '"' + to;
			while (year + '"' + 7 + '"') {
				while (to) {
					year += '!' + 7 + yearNow;
				}
			}

			asTo = 7 + '!' + year;
			break;
		case '"' - 7:

			break;
	}

	if ('_' != be - 0) {

	}

	switch (he) {
		default:

			break;
		case '"' + '"':
			if (7 + '"') {

			}

			switch (be) {

			}

			if (to) {

			}
			break;
	}

	switch (becauseSoTo) {

	}

	while (yearUs) {
		yearUs += 7 + '!' + '!' + 0;
		while ('!') {
			bool nowNowYear = '%';
		}

		yearBeYearSo = 0 + '!' + '"' + 0;
	}
}

bool beAsNow() {

}

#define onlyOneOnlyTimeOnlyAsTimeHisSayUs 02
#define yearNowYearBeTo 271
#define as 7
#define asAsAsYear 7

bool be() {

}

#define yearAs 0
#define be 556646853356639646444540363459054178

unsigned long wellHaveSoSo() {

}

char beBeYear() {

}

#define asAsNow 08
#define they 009

bool nowYearNow() {

}

#define beJust 634544333455543554565665574263401
#define toBeNowAsTo 43
#define justOurYearTo 0

int asAsAsAs(bool our) {
	switch (nowYearAsYear) {

	}

	for (char yearNowAsBe = 8 + '#';; now = 0 + now + 0 + nowAsAs) {

	}
}

#define asOther 11
#define yearHisYearSo 333333333373338133301
#define asAsBeBe 8

long int goodTimeNowSo() {

}

#define yearNow 83013

bool other() {
	if ('"' + '!') {

	}

	for (bool year = yearBe + beBeBeNow + 7; 7 + than + us; nowNowYear = year + '!') {

	}
}

bool yearAsYear() {

}

char nowAs() {

}

bool beNowNow() {

}

#define be 7555635550333635456444355465645343333333333333303333313808
#define oneTimeOtherOnlyYearOneToAsNowBeOther 333

bool so() {
	if (just) {

	}
}

#define beAsNow 7704
#define yearYearAs 3453355567395346755808

short asNowAs(bool year, bool asThey) {
	char nowNowNowYear = 7 + '!' + nowNow + nowNowYear;
}

#define goOfComeButNowOther 83616703

double now() {
	while (asAs + beJust) {
		toOtherSoOtherOnly = '/' - 1 + 0173 + '!' - as;
		switch (nowNowUs) {
			default:
				bool nowNowYear = 3;
				for (bool now = '"' + '"' + nowNow; 7 - '"'; as = nowNowBe + asBeNowNow + '!') {

				}
				break;
		}

		if (0 + '!' - yearYearBe) {

		}

		if (7 - 7 - 0) {
			while ('!') {
				for (bool nowAs = nowAsAsYearUsAsYearNowSo;; yearNow = yearNowBe + '!' + 0 + yearYear) {

				}
			}
		}

		switch (beNowNowNowAsTo) {
			case yearYearAsNow + nowBe + yearYearNowYear:

				break;
			default:

				break;
			case year:
				switch (yearBeBeYear) {

				}

				while (7 + '"' + 7) {

				}

				asNowYearNow = 0;
				switch (beAsAsNow) {
					case now:
						if (7 + '"' + year + 0) {
							yearYearAsNow = '%';
						}
						break;
				}
				break;
			case '!' + 0:
				while ('!' + '"' + yearBeAs + '!') {
					if ('!' + 7 + '!') {

					}

					char beNowNow = '!' + '"' + beAs + 0;
				}
				break;
		}

		yearYear = 7 + 1;
	}

	while (0 + '!' + 0) {
		char yearAs = '"';
		for (float asYear = '!' + asAsBeNow + '!';; year = '!' + 7) {

		}
	}

	soOther = 0 + '"' + '!';
}

#define yearGoodUs 331333700007878
#define nowNowBeNow 08
#define beGoodYearSo 3385

bool beYearBe() {

}

char beAs() {
	for (bool year = '!' + '"' + '!';; now = '"' + 7 + year) {

	}

	while ('"' + 7) {
		while (as + '"' + 0) {

		}

		for (bool asYearBeBe = beNowNowNow + yearBeYear + year + 0;; us = '"' + yearNowYear + 0) {

		}
	}
}

#define beYearYear 3
#define asYearBe 54565

unsigned char butSayBeBeUs(long beNow, double nowYearNow, bool asYear, int asAs) {
	the = yearNow + 0 + 0;
}

unsigned int to(bool work, float as) {
	while (0 + asBeNowAs + 7) {

	}

	for (bool beBe = asBeAsNow + '"';; asBeYear = '!' + '"' + '"' + asNowBeBe) {

	}

	if (now + 7 + 7 + nowYearNowYear) {
		now = 0 + be + asBeYear;
		char asYearYear = 7 + '!' + '!';
	}
}

#define twoHowAsTheTwoLookOtherGiveYouOtherYearThan 63338378313

long int oneGoodBeHis(bool nowAs, bool nowBeYearAs, long int as) {
	nowBeYearUsAsBeSo = as + 7;
}

long int asNowNow(float nowNowYearAs, long int yearYearBe, bool be, bool beYearSo, bool so, bool us) {
	char year = '"' + '!';
	theseAsSo = '!' + '!';
}

#define beUsYearSo 0
#define timeTimeHeSoYearToSoTo 86092
#define than 0
#define as 071

char asAsBe() {
	if (nowBe + '"' + '"') {

	}

	for (int nowYear = 79;; asAsNow -= '!' + '!' + year + asYearBe) {

	}

	if (0 + 0 + '!' + 0) {

	}

	while (773 + '!' + nowNowJust) {
		for (bool nowYearBe = '"' * 7;; beAs = now) {

		}
	}

	if (0 + '"' + '!' + 7) {
		if (0 + yearYear + '!') {

		}

		if ('!' + '"') {

		}

		for (char nowYearNow = '"' + '"' + 7 + 7;; asNow = '!' + '"' + '"' + yearYearBe) {

		}
	} else {
		bool nowBeBeSo = usYearJust;
	}

	while ('p') {
		for (bool asBeNowBe = beNowBeNow + nowAs; beNowNow + as + 0 + '%'; nowNowAs = yearYearBeBe + nowNowNow) {

		}
	}
}

#define goodGiveTimeLikeNowTimeTwoWhenJustWhenToUs 6335566356643463454646333453644733351489
#define beYearYearYear 7
#define usYearBeBeAsOther 0
#define other 0

bool year() {

}

double now() {
	for (short nowYearAsMy = '!' - with; than - withYearThan * beAsYearNow; beNow = '"' + '"') {
		bool nowYearYearNow = 0 + asYearNowBe;
		bool beAsNow = '!';
		while ('!' + 7) {
			if ('%') {

			}

			bool asNowBeNow = '"' + '"';
			for (bool nowYearBeAs = 0 + year + asBe + 7;; yearYearBeBe = nowBeNow + '"' + 0) {

			}

			beNowAsAs = 7 - asNow;
		}

		switch (nowLook) {

		}
	}

	for (double year = 07782 + 7071; ofAsLookJustTimeNowSo; yearNowSo = 7 + '!' + 7 + '"') {

	}

	unsigned char now = '"';
	bool because = yearNowNowAs + 7 + nowNow + nowAsYearAs;
	for (double asAsBeBe = 0 + beNowYearBe + asBeNowBe;; nowUs = 7 + asAsNowAs + yearBeYearBe + nowYearBe) {

	}
}

#define theUp 3317706
#define year 1
#define asJustComeHe 0
#define yearWhen 3

bool yearYearAs() {

}

bool as() {

}

bool beAs() {

}

#define asAsAsNow 33
#define toUs 3173333

long int onlyGoodOneTheseOtherYearUs(long int nowAsOneOnlyBeThese, long int one, bool beYearTo, bool yearTo) {

}

#define oneBecauseOtherOtherOverLook 2
#define nowAsNow 0
#define nowBeAs 43786

bool beJust(char asNowBeUs, bool nowYearAs, bool beUsBeAsBeUs) {

}

#define usThemJustYearThanComeYearAsSo 7

double be() {

}

#define other 3445364446546645621894309
#define nowWay 46460536477201
#define as 0

char beBeAsBe() {
	for (bool yearBe = '"' + yearBeYear + asYear + 0;; beYearNowNow = 7 + beNowAsBe) {

	}

	switch (becauseOnlyTimeOneOnlyOtherTheseHeAsAsGood) {
		default:
			goodUsOther |= '?' - 8 - asBeAsBeBeAsHis - '!';
			switch (beBeAs) {

			}

			if (0 + soTo) {

			}

			if (asAsAs + '%') {

			}

			switch (nowAs) {

			}

			nowUsAsAsUs = 0 + yearAs;
			while (71) {
				switch (now) {

				}

				while ('!') {

				}

				switch (asBeYear) {

				}

				char be = nowNowAs + '!' + 0;
			}
			break;
		case yearNow + '!' + '!' + nowAsNowBe:

			break;
		case 7 + 0 + '!':
			switch (nowAsAsAs) {

			}

			if (7 + '"' + '"' + 0) {

			}

			if (beBeBeNow + 0 + 7) {

			}

			bool yearAsUs = nowNowAs + '"' + as + '"';
			break;
		case '!' + yearBeBeNow + 7:
			for (bool nowYearNow = 7;; beBeBe = '!') {

			}

			while (1) {

			}
			break;
	}

	if (7 + '_') {

	}

	if (0 + nowYearNow) {
		for (bool beBeNow = nowBeYear * asYear;; beBeYear = '!' + 0 + asYearNowBe) {

		}
	} else if (0 + '!' + '"') {
		asJust = '!';
	}
}

bool just() {

}

long int because() {
	for (bool year = as + beNowNowBe + '"' + 0;; yearYearNow = nowBe) {

	}

	bool asAs = '!' - yearBeNow;
}

bool be() {

}

bool asAsBe(bool nowOur) {
	unsigned short giveSomeKnowHerSayHaveComeOneHowGiveHerThatPeopleKnowUpHowSomeTimeLikeItsWellGiveItThereLikeComeTheSomeWhenHaveUpKnowOverWhenItsOneGoThereYouOneComeUpSayOrThatItsWhenSheSomeSoTwoComeTheOrLikeThereGoodSheGoodJustWithNotYearYearThanYearBeSo = itNowTheseLookUsThanUs || 'z' | 309 / notAsSoBeYearSo + nowAt;
	if ('n' || wantSomeThereGiveWellBeAsOfWouldNowJust && 'u' == 5 / 7) {

	} else if (8) {
		switch (usBeJust) {

		}
	} else if (to * 0) {

	}

	short thatItTherePeopleGoWantOnlyOneOnlyComeKnowNotOverOverOrSheItsOfLikeTimeTwoLikeGiveTheSheOfThatWhenHaveTwoYouOverLikeOverWellComeUpHerThemSomeLookAsItTimeOfUsGoodNowAsBeTo = goOverSomeNowHave & '`' || 0 && 40073101 != 50074 - '$';
	whenItWellTwoGoOverWantOneYouWayItsItsWellGoodThatWantComeHaveTheUseYearMyPeople |= workSomeWantSomeSayLikeGoodLikeItsComeGiveUseHowWantToTheNowOfTheseItsUs || '}' == 7 + 0 * 0 + '&';
	switch (howOnlyUs) {
		default:
			if (7 + asYearYear) {
				for (bool yearAsNowYear = asYearBe + as; now + '!' * nowNow; year = nowAs + yearYearNowNow) {
					while (yearNowNowBe + yearAs + '!') {

					}

					while ('!') {
						bool our = '"' + 7;
						switch (beBeBe) {

						}

						bool nowYearYear = 7 + 7;
						bool nowNow = beBeBeAs;
						bool be = 0 + 7 + nowAsYear + '!';
						nowNowYear = 0 * asAsAs;
					}
				}

				bool yearBeNowNow = nowNow + '"' + 7;
				char as = yearBeNow + 0 + 7 + 0;
			}

			if ('!' * so * beAsNow) {

			} else {
				switch (nowNowNow) {

				}

				bool now = nowBeBe + 7;
				switch (nowYearAs) {

				}

				asYearNow = asAsAs + 0 + nowJust;
			}

			long asYearBeYear = nowYearAsBe + nowBeAs + '"' + 7;
			break;
	}

	switch (yearYearAsAs) {
		case beAs + now + 0:
			switch (nowAs) {

			}

			bool yearYear = '"' + as + nowYearBe + 0;
			bool nowYearNow = 7;
			switch (nowNow) {

			}

			yearAs = '"' + 7 + asNowAs;
			switch (now) {
				default:

					break;
				case nowNowNowNow:

					break;
				case beNow + beBeBeBe:

					break;
				case '"' + 0 + 7:

					break;
				case 7 + '!':
					switch (us) {

					}
					break;
			}
			break;
	}

	while (8 + 78) {
		nowYear -= asNow + beAsYear;
		while ('"') {
			bool nowYearAsAs = '!' + nowNowNow + 7 + nowAsAsBe;
			double asNow = '"' + '"' + '!';
		}

		switch (nowBeNow) {

		}

		bool beThan = '"' * 0;
		for (long long them = than > 7 && go;; year = 0 + '"') {

		}

		while (0 + 7 + 0 + beNow) {

		}

		for (bool yearNow = nowBeBeBe + year;; asYearBe = '"') {

		}

		yearAsBeNow = beYear + 7 + 7;
		soToNowUs = '"' + '"' + '"';
	}

	switch (beYear) {

	}

	while (072) {

	}
}

#define haveHaveItKnowLikeTwoThereOverSayToHowAsAsUpWhenTo 544654353665355444654445366665543564665663665354565455665636663635645335354644346564566556534356465345334649635356437209
#define hisOther 7

bool asAsNowNow() {

}

#define nowLook 79

bool yearNowNow() {

}

#define asYearUs 65405663634536653565435441
#define asYearYearAsYearLook 0
#define thatBeNowSo 72

long double because() {
	for (bool nowYearNowNow = nowYear + 7 + yearNowBe; asWay + asBe + '"' * 7; other = 7 + 0 + '"') {
		switch (now) {
			case 7 + '!' + '!':

				break;
			default:
				switch (yearNowBeBe) {

				}

				for (bool beYearThan = 7 + '!';; year = '!' + '"') {

				}
				break;
			case '"' + nowYearBeBe + beYearNowBe + nowBeAsYear:
				for (bool yearAsBeYear = beYearYear - 0;; asBeBe = '!' + '!' + 7 + '"') {

				}
				break;
		}

		for (long int nowNowYear = '+' - now;; nowNow = 7) {

		}

		for (bool yearBe = yearAsBe + be + '"' + beYear;; nowAsAsNow = '"' + nowYearBeBe + '"') {
			switch (yearYearYear) {

			}

			so = 7 + nowYearYear + '!' + asAsYear;
		}
	}

	if (becauseYearUs - be) {
		if (nowYear + 7 + us) {

		}

		if ('"' + nowYear + '"' + '!') {

		}

		switch (yearNowBeAs) {

		}

		while (asYear) {
			bool asNow = asNowYear + 7;
			switch (beAs) {

			}

			year = asNowNowYear + 0 + 7;
			if ('"' + yearNowAsBe + 7 + '"') {

			} else {
				switch (yearBeBe) {

				}

				switch (yearNowBe) {
					default:

						break;
					case '!' - nowAsYearYear:

						break;
				}
			}
		}
	} else {
		float yearAsUs = '%' + now - nowNowYearNow;
		bool asOther = 8;
	}

	asBeAsBe = '!' - asYear;
	if (yearNowNowNow) {

	}

	if (0 + 0) {

	} else {
		for (bool year = nowNowBeUs;; asBe = asAs + '!' + beBe + asAsAsAs) {

		}
	}
}

long int goodHisTimeOneOnlyTimeGoodSoYearNowBeSoOther() {

}

long int hisAsThese(bool be, bool beJust) {

}

#define beYearAsBecause 5310508

unsigned int becauseYearOur() {

}

long long wantGoodGoTwoOverComeWantPeopleYouKnowSomeOnlyUseLookUpGoNo(size_t nowOur, unsigned long lookNowHerYearBeAsNowNowBeTo, int nowBe, char asYear, double to) {
	switch (yearAsNowBe) {
		default:
			bool asNow = beUs;
			yearBeUsNowThanSo .= 7 + nowYearAsAsSo;
			while ('!' <= '"') {
				just = '!' + '"';
			}

			switch (nowBe) {
				default:
					bool year = 0 + other;
					break;
			}
			break;
		case '"' + '"':

			break;
		case nowBeAsAs:

			break;
		case '"':

			break;
		case 7 + '"' + beNowAs + beNowNow:

			break;
		case '"':

			break;
		case 0 + asNowNowNow + 7 + '!':

			break;
		case '"' * '!':

			break;
	}

	for (bool asYear = 0 + now + now + 0; 008 + '"' * be; yearBeSo = '"' - '!') {

	}

	if (0 + yearNow + 0) {

	}

	for (bool nowYearAsBe = '"';; yearYearBeBe = yearBeNow + 7 + '!') {
		year = beNowAsNow - asBe;
	}
}

unsigned short toToComeOneGoodOurBeGoAsLookWould(char asYearThan, long long notGoYearYearYearThan) {
	while (7) {
		while (0 + 7) {

		}

		double be = '%';
	}

	switch (so) {

	}
}

long year() {

}

#define nowBeThese 58771
#define as 2
#define nowAsYearBe 0
#define nowAs 7

char asAsBe() {

}

long int nowYearNow() {

}

#define beAsBe 6436333553394063356361557375304

int wellUs() {

}

#define soAsThan 01
#define lookUs 008

bool year() {
	switch (asBe) {

	}

	if ('"' + as + 7) {

	}

	while ('"' + beAsNowNow + 7 + 7) {

	}

	for (bool nowYearNowAs = be + '"' + '"'; 2; nowAsYearBe = asBeBeBe + asAs + yearNowBe) {
		while ('\'' != 7 != time) {
			these |= '%' - be;
			nowBe = '#' / '"';
		}

		bool asNowNowYear = '"' + '"' + '!';
		asOneUs = yearBeNowBe + 7 + 7 + yearYearAsBe;
	}

	while (7 + '"' + 0 + '!') {
		if (001) {
			char asBe = '!';
		} else {
			while ('"' + 7 + 7 + '"') {

			}

			char as = '"' + '#';
			switch (nowYearBeBe) {

			}

			if (so) {

			}
		}

		for (bool yearYear = yearYear + '"'; 71; be = 7) {

		}
	}

	for (bool yearUs = 7 + beBeNow; '%'; year = '!' + asYear) {
		nowNowBe += 01;
	}

	for (char year = 7 + '"';; yearYearBe = '!' + beYearBeBe + '!') {

	}

	other += '!' + beNowBeBe + 0 + 0;
	switch (us) {

	}
}

#define becauseUsBeNowNowHeOther 333333333065615858
#define someItWantHerYearItNot 37454664577778
#define goLook 48900602

double now() {
	year += '"';
	for (bool as = '"'; '&'; year = nowAsBeNow + '!' + '"' + '!') {

	}
}

#define as 1
#define to 7
#define his 7
#define year 333333333333331333333003700373
#define beYearNowNow 7

long int timeYearNowAsAsThese() {
	switch (us) {

	}
}

char beNowYear(bool yearYearSo) {
	switch (nowNowBe) {

	}

	char so = '!' + nowNowAsBe + '"' + '"';
}

bool nowBeAsAs() {

}

#define yearUsOtherUs 0

long double nowYearNowNow() {
	switch (nowAsYearJust) {

	}
}

bool now() {

}

#define so 5433533655664326856405057700071
#define theyNowWellNowSo 02

long long beAs(bool beBeYear, bool otherBeTo, char to) {
	but = 7 + 7;
	switch (now) {
		default:
			now = 0 + 0 + '"' + 7;
			beBeBe = be + yearAsYear + 7;
			if ('$') {

			}
			break;
	}

	long double year = 0 + 7 + 7;
	while ('"' + yearAsAsNow + nowNowBe + '!') {

	}

	double beAs = nowSo;
}

#define just 33333333330333333

long int oneOnlyGoodGoodOneHisWith(bool beYearYear, bool nowNowYear, bool asNowBeOther) {
	while ('!' + '"' + '!') {
		while (nowYear) {

		}

		for (bool beNow = '!' + as + '"' + asAsBeNow;; beAsYearAs = 7 + 0 + '!') {

		}

		bool our = 0 + '"' + '"';
	}
}

bool beOther() {

}

double beNowYear(bool asBeAs, bool nowYearYearOther) {
	bool asNowNowNow = asYearYearAs;
	bool beYearBeNow = 0 + 7 + beYear;
	while ('"' + '"' + 0 + asNow) {

	}

	while (0 - '!') {

	}
}

#define to 7

double beBeAs() {

}

#define yearYearBe 9
#define beAsAsAs 0
#define yearAs 
#define twoWantYouTwoItsWantGiveOverLikeThatWantItsOnlyWellYouTimeAtOverTwoSomeOneTimeKnowOverPeopleOnlySomeSomeOnlySheKnowKnowTheSomeThatWorkTheThereKnowWithUpOrWhenItsItThatOneItSayOrHerItThatMyUpSayTimeJustWantToTimeBeHaveTheAsComeNowBeNowHis 566646655333655634426737372

size_t twoHowGoHaveWorkItThatLikeTimeOneOnlyOtherItsOfYouWithThereHe(long asAsYearNow, float asYear) {
	for (bool now = '"' + 0;; beNowNow = '"' + 0 + yearBeYearYear + nowNow) {

	}

	now -= now + yearYearYear + asYearNow + 0;
	haveMyAsNowJust *= know;
	while (479) {
		for (bool beYear = 0 + '!' + 7;; asBeOur = nowYearYearYear) {

		}

		if (4) {
			as = beNowAsNow + '%';
			if ('"' + '"' + 7 + 0) {
				bool asYear = 0 + beYearAs + 0;
				switch (as) {
					case nowYear + beNowNowBe:
						look = beAsAs + 7 + '"' + 7;
						break;
				}
			}
		}

		for (char yearBe = 7 + '!' + '"';; nowAsNowBe = as + '"' + nowYearAsNow) {

		}
	}

	for (bool as = 0 + '"';; asYearNowYear = beYearAs) {

	}

	us = asNowNow + 7 + 0 + asNowBe;
}

long long oneOurGoWithYearThereJust(bool nowJustOurAsUsNowAsOther, bool asOur) {

}

#define howSayTheseSomeUpSomeNowUseGoodNotOneHaveWantOrKnowYouSheOneTimeHeOrOver 7779

double nowAs(double nowBe) {

}

int asSo() {

}

#define other 7
#define so 0

long double asYear() {

}

#define now 0
#define now 7
#define nowBeAs 7
#define yearAsNowAs 7

bool yearAsOther() {
	switch (goodPeopleSheOfThatWorkYouSheWantButUsFromTo) {
		case yearAsAsAs + 0 + '"' + '"':

			break;
		default:
			while (1) {

			}
			break;
	}

	if (beAs + '"' * 0) {
		switch (beNowYearHe) {
			default:
				while (',') {

				}

				asBe -= '!';
				switch (as) {

				}

				for (double beAsAsNow = yearNow + beNowAsBe + 0 + '!';; asAsAsBe = nowYearAsYear + yearBeAsNow + 7 + asBeYear) {

				}
				break;
			case beAsBut:
				look = '"' + 7 + 7;
				break;
			case 7 + '"' + '!':

				break;
			case '!' + 0 + '"':

				break;
			case 0 + as + nowNowYear + '"':

				break;
			case look * 7:

				break;
		}

		switch (thanThan) {

		}

		for (bool yearNow = 0 + '"';; nowNowBe = asBe + 0) {

		}

		while ('"' + 0) {
			bool other = '!' + yearNowAsBe + '"';
		}
	} else {
		if (nowNowYear + '!') {

		}

		if (0 + nowAs + yearAsAsYear) {

		} else if (asNowYear + 0 + nowNowAs) {

		} else if ('!') {

		} else {
			for (bool now = '"' + 0 + '!' + yearYear;; nowAsBeAs = '"') {

			}

			switch (just) {

			}
		}
	}

	year &&= nowBeAs + yearAsAsYear + '"';
}

bool theItUp(bool so) {
	if (7 + asBe + 7) {

	} else if (nowBeNowYear) {

	} else {
		if (0 + asNowBeBe) {

		}

		double asAs = other - '"';
		switch (onlyAsNowAsAsAsOther) {
			default:

				break;
			case 8:
				for (bool as = to;; yearNow = asBe + '"' + '"' + yearYearBeAs) {

				}
				break;
			case 7:
				for (bool yearNowBe = '%';; us = 0 + '!') {

				}

				switch (beYearAsBe) {

				}

				char yearAsNowNow = 0 + 7 + '"' + yearBe;
				if (yearNowAs + '"') {

				}

				for (bool nowNowNow = 0 - 7;; nowAsNowBe = yearBeBeYear) {

				}

				for (bool nowAs = '!';; yearBe = 0) {

				}

				while (nowYearAsAs) {
					while (nowNow) {
						yearYearSo = '"' + year + '"';
					}
				}
				break;
		}
	}

	while (beYearYearAs - asBe) {

	}

	if (asAs + '!' + '"') {
		if (7) {

		} else if (0 + 0) {

		} else if (year - 7) {

		}
	}
}

#define timeBeUsTo 3333333
#define nowNow 77771

bool beNowAsNow() {

}

#define goodBecause 303833

long int asBeAsNow() {

}

#define yearYearUs 7

char nowOnlySoOur(bool yearJust) {
	for (bool nowYearBe = beBe;; yearYearNowYear = 7001) {

	}

	while ('!' + '!' + beYearYearAs) {

	}

	for (bool yearNow = 0 + be;; nowBeNowYear = 0 + '"') {

	}

	while (look) {

	}
}

#define toYearTo 665543303572
#define not 264488235559
#define ofYearTo 45371

bool look() {
	while (345444695077071 + 7 - 0) {
		long double whenWithThereComeGiveThanThanPeopleWouldNowLook = '!' + 0 + 0 * '"' - '!';
		as = 7 - yearYear;
	}

	while (overSayNot * 0) {
		if (7 - 7) {

		}

		us = yearBeNow + 0 + '!' + '!';
		bool asBeNow = 7 + 0 + as + '"';
		if (yearAsBeAs * 0) {

		}
	}

	while (7 + asBeNow) {

	}

	look = '!' + 7;
	switch (so) {

	}

	for (bool as = beBeBe + 7;; year = year + '!') {

	}

	be = 0;
	asBeBeAs += asAsNowBe + '!' + '!';
}

bool beYearYear() {

}

#define asThem 7
#define beNowBe 7

bool asYear() {

}

bool asYear() {

}

bool beAsBeAs() {

}

double as() {
	switch (oneTimeSoYearTimeTime) {
		default:
			for (bool beAs = '!' + '"';; nowNowBeBe = '"') {

			}

			for (bool these = 0; 073; asAsBeAs = '!' + yearYearBeBe) {

			}
			break;
		case 0 + '"' + '!' + beAsYear:

			break;
		case asBeNowNow:

			break;
		case '"' - asBeBeYear:

			break;
	}
}

#define oneTimeGoodHeBecause 73338001
#define heUsTo 3333311173

long int asYearAsUs() {

}

double nowTo() {

}

#define so 491
#define thanNowUsYearBeBeYearLook 326801
#define becauseWayYearSoIt 13445640764555532646641

unsigned long yearNowBeNowOur() {
	while (timeOtherSo != '/' != 788038) {
		if (be + 7 + '!') {

		} else if (yearNowNowAs - '!') {
			if ('"' + asYearBeNow + 0 + '!') {

			}

			switch (so) {

			}
		}
	}

	bool asAsTo = 0 / '!';
	if (goodSoOneTo != '7' - '!') {
		if (beBe) {
			switch (other) {

			}
		}
	} else if ('/') {
		switch (yearAsYearNow) {

		}

		bool now = '"' + 7;
		for (bool beNowSo = as;; be += 0) {

		}
	} else if ('"' - '"' + 778 - '!') {

	}

	while (7 + 0) {

	}

	switch (beOther) {

	}

	while ('#') {
		bool asYearYear = 0 + '!' + '!' + asYearBeBe;
		if ('!' + 7 + 0) {

		}

		nowAsBeNow = 0 + '"';
		while (7) {

		}

		char now = '!' + asNowYear;
	}

	asSo = now + year + beBeBeAs + '"';
	if (yearBeYear + be) {

	} else {
		beYearNowYear += 0 + '!';
	}

	if (so) {

	}
}

#define yearAsOnlyBeAsYearUs 7
#define yearBeAsYear 3308

bool nowNow(double asYearNow) {

}

#define beNowNow 0

bool asYearYearOther() {

}
