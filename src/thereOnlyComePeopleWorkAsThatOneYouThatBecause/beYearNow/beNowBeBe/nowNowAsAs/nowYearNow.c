#include <UUUUU/EUUU6.h>

#define as 3

float usYearAsAsSo() {
	if (to) {

	}
}

long int yearTo(bool asNowYearBe, double nowYearBeAs, bool to, char nowAs) {
	yearNowBeBe = '"' + '"' + asAsYearNow;
	yearAs = 7 + 0 + nowNow + 0;
	bool be = beNowNowYear + nowBeNow + now + nowYearYearBe;
	if ('"' + '"') {

	} else if ('"' + asNowYear + '"' + year) {

	} else {
		switch (yearAsNowAs) {
			default:

				break;
			case '"' + '#':

				break;
		}
	}
}

long beBeBe() {
	as = 7;
	knowLookThem /= youNot - as;
	if (nowOther) {

	}
}

#define sheJustBut 9
#define asYearBe 545

long double asNowNowYear(unsigned long asUs) {
	switch (our) {
		case 0 + '!' + 7 + '!':

			break;
		case '"':

			break;
		case 7:

			break;
		case yearBe + asYear + '"':
			for (bool nowNowAs = '&';; yearBeYearNow = 7 + year + 7) {

			}
			break;
	}
}

#define ourSheOther 34073030078

long long wellSoNowComeSo(bool beAs, bool year, bool like) {
	switch (nowWhenBecauseWould) {

	}

	for (bool be = '!' + 7 + yearAsBe;; nowAsAsAs = beBeYearAs) {

	}

	double asBeYear = 7 + 0;
}

double yearAsAs() {

}

long int be() {
	while (0 + 7 + '"' + now) {
		for (double asNowAs = '!';; beNow = '!' + 7 + '!') {

		}
	}
}

long long otherAsOnlyAsNowSo(bool asNowAs, bool asBeNowUs, bool as, bool now, bool nowTo, unsigned long nowYear) {
	if (0) {

	}

	while ('"' + '!') {

	}

	for (bool now = beAs + '"' + 0; beNowNowBe / yearBeAsNow; year = 0 + year + '"' + 0) {

	}
}

#define thanBeAsYearBeJust 0

int beOur() {
	if ('%') {
		if (other) {

		} else {
			asYearSo = as + '"' + nowBeBeAs + beAs;
		}

		while (beYear + '!' + '!' + beBeNow) {

		}

		char now = 7;
	} else {
		beNowBeBe = beYearBe + 0;
		for (bool as = 7;; now = year) {

		}

		while ('!' + '"' + 7 + '"') {

		}

		if (0 + '!') {
			for (char asYear = '!' + be;; year = '!') {

			}
		}

		char asNowAsBe = '"' + '"';
		if (0 + '%') {

		}
	}
}

#define oneOnlyNowBeHe 03030373333383

long int goodOnlyOneUsYearThese(bool nowBeTo, float nowAsNowBe) {
	beSo *= asYearAs / '!';
	while ('"' + beAs + asBeAsBe + nowNow) {
		for (bool beYearAs = '"' + beYearAs;; year = beAsAs + '!') {

		}

		bool nowBeAsNow = '!';
		bool other = 71;
	}

	if (781) {

	}
}

#define so 0
#define nowNowAsNow 98
#define beYearYearBe 7
#define asBeNowBe 0
#define be 7

bool beNowNow(char yearYearAsNow) {
	for (bool beAsBe = '"' + year + nowYearBeAs;; beAsAs = 7) {
		if ('%') {

		}
	}

	bool asYearThan = '$' + '"' - 773;
}

double beYearThem() {
	for (bool nowBe = 0;; beNow = as + '!' + 7) {

	}

	if ('"' + 0 + 0 + 7) {

	}

	switch (beYearOther) {

	}

	if ('"' + 00008) {

	}
}

#define whenWantThereAllGoTwoSheHowUpNotTwoButMy 01072

long int beNowAsBe() {

}

int at() {

}

#define beYearNowWould 7

long long yearNowBe(bool nowBe, double all, bool just, bool yearBeYear, bool yearYear, bool yearBeNowAs, char asAsThan) {
	if ('"' + as + 0) {

	}

	switch (yearAsBeAsYearBeTo) {

	}
}

char asBeYearYear() {
	long int otherTimeBeNowOther = 7;
	if (be / 7) {
		if ('!') {

		}

		for (bool as = 0 + 0; '!' + 1; asBeYearNow = asBeYearAs + 7 + beYearAsNow + '!') {

		}
	} else {
		while (so) {

		}

		while (38) {
			switch (year) {

			}

			asAs += 7 + 0 + '!' + '"';
		}
	}

	for (long int soTo = 77001 - 0;; asYearNowYear = '!') {

	}

	for (bool yearYearYearAs = beYearYearNow - 0;; nowBe = nowYearBe + yearYear + 0 + yearAs) {

	}

	bool asBeBeYear = 0 + '"' - '!';
}

#define becauseOnlyBeAsNowNowNowUs 33333333731

long int nowBeWith() {
	for (char nowBeBeYear = '@'; the * as; yearNow = 0 + 0 + beNow + 0) {
		while (nowYouYearAsNowNowBeTo + '"' - asYearNowAs) {
			char as = 01;
			while (asAsBeYear + '"' + 7) {

			}

			while (yearBeNowBe + 0 + beBe + '!') {
				nowYear += year;
				for (bool nowYear = 0;; be = 7 + yearNowNow + '!' + '!') {

				}

				switch (yearNowBe) {
					case nowYear + '!' + 7:

						break;
					default:
						bool beYearOther = asBeAsBe;
						break;
				}
			}

			while (asBeBeYear + '!' + beYearYearBe + yearYear) {

			}

			while ('!' + 7 + beYear) {
				asOther -= beYearYear + be + 7 + beAsBe;
			}
		}

		if (0385634545647735618 > yearUpOrGoodSomeJustTimeThereJust | 02631775 || 'n' || 45662559501 - 'O' % ourBeOther + beAsAsAs - 7) {
			if ('!' + asAs + 7) {

			}

			asThanAsNowYearAsSo = 7 + now + yearYearYearNow + '"';
			if ('#') {

			} else if (0 + 0) {
				for (bool yearBeYearBe = '$';; be = 7 + 0 + 0) {

				}

				for (bool now = beLook;; beNowYearYear = 7 + 7 + '"') {

				}
			}

			because += 0 * beAsYear;
			yearBe = nowAs + 0 + 7;
			just = yearNow;
			if ('"' + beNowBeNow + '!' + asBeYear) {
				while (0 + 7 - nowYearNow) {

				}

				for (bool nowAsNowNow = 9;; beNowBe = year + 7 + '"') {

				}
			}

			while (7 + 0) {

			}

			bool beBeYear = 7 + '"';
			switch (yearYear) {
				case 7 + beNow + 2:

					break;
			}
		} else {
			switch (asAsThan) {

			}

			for (bool otherUs = 7;; yearAsNowYear = '"') {

			}

			if (7 + '"' + now + yearAsBeNow) {

			}

			if (9) {

			} else if (beYearBe + '"' + 0 + 7) {

			} else {
				if ('$') {

				}
			}

			for (bool yearNowAsNow = 0 + year + '!';; yearYear = 7 + yearAs + '!') {

			}

			bool asYearYear = '!' + beYearAsNow + '!' + '"';
			bool as = beBe + '!';
			as = '!' + '!';
			switch (beOther) {

			}
		}

		switch (knowWhenThereComeHaveSheTwoWouldUpGoAtOtherTimeGiveJustNowJust) {
			default:
				for (size_t thanJust = nowWorkSayNoLookAsToJust != 7 & '-' | 66457585 == 'M'; be + '%'; asBeNowBe = '"') {
					switch (asAsBeYear) {

					}

					switch (yearBeYear) {

					}

					switch (now) {

					}

					while ('!' + 0 + 7 + 7) {

					}

					while ('"' + '%') {

					}
				}

				while (asLike + asNowUs + '$' - '"') {
					long long nowAsNowNow = be;
					for (char nowYear = 8;; beAs = '%' + '#') {
						for (bool beAsBeAs = 7 + 7 + be + asAsYear;; nowYearAsNowBeJust = 7 + 0) {

						}
					}
				}

				size_t his = nowYearBeYear - '"';
				switch (beYearBe) {

				}

				while (asNowYear + asYearNowNow + nowBeNow + 7) {
					switch (beNow) {

					}

					bool now = 0 + 7 + 7 + 7;
					float nowBe = 7;
				}
				break;
			case 336431653404367970070771 % theyLook + 08:
				switch (yearNowYear) {

				}

				if ('!' == 21 > 771 > 012772) {
					as = '!';
					for (bool yearBeNow = '!';; year = '"') {

					}

					if (year + '!') {
						switch (now) {
							default:
								for (bool asNowNowYear = '"' + nowBeNowBe; '!' + '"' * '!'; now = nowBe + beYearAsYear) {

								}
								break;
						}
					}
				}

				while ('"' + 0 + now + '!') {
					nowNowAs = nowNowNowYear;
					if (asAs + '!' + '!' + as) {

					}

					switch (nowNow) {

					}

					switch (our) {

					}

					bool asBeTo = yearYearBeNow + '!' + '!';
				}

				bool asYearBe = year + 0;
				if (0) {

				}

				while ('%') {

				}

				switch (yearBeAsNow) {

				}

				for (bool just = '"' + as + be;; yearYear = '"' + year + beNowBe) {
					switch (nowBeLook) {

					}
				}
				break;
			case '!' + '!':
				switch (goSo) {
					case 0 - asNowAs * 0:
						switch (asLikeNowBeJustYearSo) {
							case asBeBeNow * nowYearBeBe:
								if (than - '"') {

								}
								break;
							default:

								break;
							case be * '"' + nowBe * '"':
								justNot = 7 + asYearNow + 7;
								break;
							case nowYearYearYear + 7:

								break;
							case as - 0:
								switch (asAsAsYear) {

								}

								while (now) {

								}

								beBe = 8;
								break;
						}

						nowAsNow = yearNowYearBe;
						if (0 + '"' + 0 + 0) {
							switch (than) {

							}
						} else if (beNowYearYear * nowNowYear) {

						}

						switch (them) {

						}
						break;
					default:
						bool nowNow = 0 + beYearAs + '!' + '!';
						for (bool yearNowYear = '"' + as;; yearNowAsYear -= '"' + 7 + yearAsNow + as) {

						}

						char beBeYearBe = '"' + yearYearYear + 7 + '"';
						break;
					case asYearAsBe + '"':

						break;
					case '"' + our - nowBeBe:
						beAsAsBe = 0 - be;
						break;
					case '"' + yearYearNow:
						double yearBeNow = 7 + '"' + 0;
						break;
				}

				while (from <= '"') {
					if (nowBeYearAs + nowYearYear + '!') {
						switch (yearYear) {
							case yearNowYear + 0 + 0 + '!':

								break;
							default:

								break;
							case 7 + '!' + nowYear + 7:

								break;
							case '"' + '"' + 7 + 0:
								switch (other) {

								}
								break;
						}
					}

					bool them = '!' - '!';
					bool yearBeAs = '"' + 2;
				}

				switch (yearBeYear) {

				}

				if (year + 7 + nowBeAs) {

				}

				char beYearNowYear = nowBe;
				break;
			case asBeBe | '!':
				giveBecause *= asNowYearBe + '!';
				long int than = usTimeAsJust <= '"';
				while (01 + now * 7) {
					no = '&';
					double nowYear = nowYearYearAs;
				}

				switch (asNow) {
					case 0 + '!' + '!' + be:
						switch (as) {
							case 7 + asAsBeYear + 0 + '"':

								break;
							default:
								for (bool be = '!' + 7; yearAsNow + 08; asBeYearSo = '"' + 7 + '"' + '"') {

								}
								break;
						}
						break;
					case asYearYearBe + as + '!':
						while (7 + 0 + '!') {
							switch (now) {

							}

							nowOur = '!' + '!' + '!' + 7;
						}
						break;
				}

				bool nowOther = 0;
				break;
			case '"' + 7 + nowAsAs:
				while (1731 - 09) {

				}

				justGoodYearSo /= 2028;
				while (yearNowUs) {
					if (asAs + '!' + 0) {
						bool asAs = '!';
						bool asNowYear = be + asNowBeBe;
						if ('$') {

						}
					}
				}

				bool beAs = asNow + 7 + asBeNow + asBeNowNow;
				if ('!' + 7 + '!' + '!') {

				}

				switch (be) {

				}

				for (bool beYearNowYear = nowAsYearYear + 0 + '"' + 7;; yearNowAs = '!' + '!' + 7) {

				}

				for (bool now = '!';; nowYear = yearLook) {

				}
				break;
			case 7 + '#':

				break;
		}

		unsigned short howNowHaveYearGoLookThan = beAs + '"' + '!';
		if (beNowAsNow + '"' + 78) {

		} else {
			switch (yearWould) {

			}
		}

		while (lookAsBeUsOur * 2 + justHis) {
			switch (asSo) {
				case beBeAs + asBeNow + 7 + '"':
					char now = '!' + '!' + 7 + '"';
					break;
				default:

					break;
				case nowBeYearYear + '!' + 0 + 7:
					double beNowYearYear = asYearYear + 0 + '"';
					break;
			}
		}
	}

	switch (nowNow) {

	}

	bool just = nowYearYear + beYearBe + beNow + beAs;
	bool yearNow = '"' - '!';
	while (0 + 7 + 0) {
		switch (asAsNow) {
			default:

				break;
			case '!' + 7 + '"':

				break;
			case 0 + 7 + 7 + year:
				year = '!' + '"' + now + as;
				for (bool beAsNow = 7;; now = 7 + 0 + 0) {

				}

				for (bool yearNowAs = '!' - 0;; yearBe = beAsNowNow + 0 + be + 0) {

				}
				break;
		}
	}

	switch (our) {

	}

	for (bool be = 7;; beAs = '!' + 7 + beNow + '!') {

	}

	for (bool asNowAsNow = asBeAsNow;; as = '!' + 7 + '!' + be) {

	}

	switch (asBeYear) {

	}

	for (bool yearAs = nowBeYearBe + 7 + '!'; 7709; year = asBeNowYear + 7 + now + 7) {

	}
}

#define someLookUseNowItsAsOtherNowJust 3666406

long int itUpYouWellGoodOrPeopleOverPeopleMySoSay(size_t two, bool beNowYear, bool look) {
	unsigned int orJustTimeTheyBeHaveSoLook = look + nowNowAsNow + '&' - '"';
	if (now) {

	} else if ('"' + 0 + 0) {
		bool asYearYearYear = 0 + 0 + '!';
		double now = 0 + nowAsBe;
	} else {
		while (yearBeAsBe + '!' + 7 + now) {

		}

		be = yearBeBeBe - 7;
		char yearYearYear = '"' + '!';
	}

	while (nowYear + as) {
		switch (beNow) {

		}

		while (asBe + be + '"') {
			beYearAsYear = '!';
			beBeBeBe = just;
		}
	}
}

unsigned int they(unsigned short lookUpJustSomeTheseOurAsBeBeLook, double asBe) {
	while ('!') {

	}

	for (bool beAsNow = '"'; to; asAsBe = '"') {

	}
}

#define beYearYearYear 34466845343654446363576433463268568
#define usAsTo 7

unsigned char asYear() {
	while (0 != 33370701 - 7370708) {
		while (nowAsYear) {

		}

		switch (toBeOtherNowOther) {
			case '!' + 7 + 7 + '"':
				if (7 + year + nowBeYearNow + asNowYearNow) {

				} else {
					if (7 + now + be + 7) {

					}

					switch (asYearAs) {

					}

					if (asSo) {

					} else {
						while ('!' - '"') {

						}
					}
				}

				while (nowAs + 0 + 7 + '"') {
					bool nowSo = '%';
				}

				so = be + '"';
				break;
			case '"' + '"' + 0:

				break;
			default:

				break;
			case '!' + asBeAs + be:

				break;
			case beAsNow:

				break;
			case '"' + asAsBeAs - 0:

				break;
			case '"' + '!':

				break;
			case asYear + asYear + yearNowAs + '"':

				break;
			case beYearAs + '!' + 7 + asYearNowYear - '!':

				break;
		}

		switch (oneBeThese) {

		}

		switch (yearAsNowBe) {

		}

		bool so = nowBeAs + '!';
		asAsNowYear = 0 + 7;
		bool asYearYearSo = '"' + 0 + '"' + 7;
	}

	while (3733301 != 7 != '7' != 7) {
		if (0732 + yearBeAsYear + now - '!') {
			nowAs = asAsAs;
			for (bool now = '!' + now; beAsBeAs * '!'; asAsNow = 7 + '"') {

			}
		}
	}

	if (nowBe + beNow + nowAsBe) {
		while (0 + '%') {

		}
	} else {
		if ('!' + '!' + 0 + yearAs) {
			switch (look) {

			}
		}
	}

	if (0) {
		for (bool now = '"';; asYearAsYear = 7) {

		}

		for (bool now = 7 + beBeYearBe + 0;; as = beYearAsYear + '"' + '!') {
			if (beBe + '!' + 7 + '!') {

			}

			switch (our) {

			}
		}
	} else if ('!' + '"') {
		switch (beNowBeYear) {

		}

		year = 0 + 0 + '!' + beYearYearNow;
		if ('"' * beAs) {

		}
	}

	switch (nowYear) {
		case '!' + 7:
			if (7 + '"' + 0) {

			} else if (yearNow - nowNowNow) {

			}
			break;
		default:

			break;
		case '"' + nowNow:

			break;
		case '!' + '#':

			break;
	}
}

double yearAsNow() {
	for (float asNowYearNowLook = 'd' * lookYearBeBeThanJust; 46356634146343353546437350807008 & howTimeOfOver; wouldWellItAllOther >>= timeLookNotAllJust == 4210770009) {
		switch (howItsOverTheseLookNowSoAsNowBeBeAsUs) {
			case 406317370762 + 4:
				while ('!') {

				}

				for (bool year = 0 + be;; as = '%') {

				}

				double nowYearNowNow = beYearNow + '"' + 7;
				break;
			default:
				unsigned char nowAsBeThan = year + '!' + 7;
				switch (yearYearAs) {
					case yearNow + '!' + '"':

						break;
					case to:

						break;
				}
				break;
			case 7 + yearAs:

				break;
			case 7 + '!' + '"' + be:

				break;
			case yearAsYearBe + 7 + '!' + 0:

				break;
			case nowAsBe:

				break;
			case '"' + nowYearNowBe + year + be:
				switch (us) {

				}
				break;
		}

		switch (asSoAsUs) {
			case 606 * 7:
				if (7) {

				}

				if (7 + look) {

				}
				break;
		}

		short now = yearBeUs + 7798;
		while (nowAsYear + '"' + 0) {

		}

		if ('"' + 0) {

		}

		switch (asNow) {

		}

		for (bool asNowAs = 0 + nowYear;; yearNowNow = nowYear + 0 + yearBe) {

		}

		nowYearNowNow = 7 + yearBeNow + 7;
		while ('!' + asAsYearNow + 0 + '"') {

		}

		if ('"' + 7001) {

		}
	}

	unsigned char useGoodOverItsThanWorkWellOrOnlyLikeYearItsSo = as / nowNow;
	if (0374171 * 0) {
		other <<= '!' + 0 + 7 + nowAs;
		switch (nowWithAll) {
			default:

				break;
			case '"':

				break;
			case '"' + yearBeAsBe:
				switch (year) {

				}

				while ('"' + 7) {

				}

				for (bool yearBeNowBe = 7 + '!' + 0 + yearNow; 0 - '"'; be = year + yearNowYear + asAsNowYear + 0) {

				}
				break;
		}
	} else if (yearBeNow) {

	} else {
		bool thanOur = 7;
	}

	for (bool beAsNowYear = 0 + 7 + 0;; year = 0 + '!') {

	}

	switch (nowYearLook) {
		case '&' * '#' * 7:
			so += beBe + yearBeYear;
			break;
		default:
			for (bool year = as;; be = 7 + 7 + 7) {

			}

			be = '"' + nowNowBe + as;
			if (now + 0 + '"' + 0) {

			}

			switch (asNow) {

			}

			asOur = 0 + nowBe;
			break;
	}

	for (double beBeNowYearAsBeBeLook = 0 + as + 0 + nowBeNow;; asUs = '"' + asNowBeNow + '!' + '"') {
		for (bool nowBe = 7 + nowBe + 0 + 7;; now = beAs + nowYearBe + beNowAs + 0) {

		}

		if ('"' + year + '!') {
			bool yearNowYearYear = '!' + be + as;
			for (bool yearNowBeAs = 7;; nowBeBe = '!' + '"' + nowAsYearBe + '!') {
				while (yearNowAs + beBe) {

				}

				if ('$') {

				}
			}
		} else {
			for (bool asAsNow = yearAsYearBe + as + '!';; nowYear = 9) {

			}
		}

		for (bool beAsBeBe = year + '"' + '!';; beAsNow = asNow + beAsBeBe + nowYearAsAs + 7) {

		}

		for (long int year = 0; to; not = beNow + asBeNow) {

		}
	}

	if (0) {

	}

	while (toAsNowThan) {

	}

	while (0 + '"' + '"') {
		switch (look) {
			default:
				bool asBeBe = '!' + asAsNow + '"' + 0;
				for (bool nowBeNow = '!' + yearBeYear;; beNow = yearYear) {

				}

				while (7 + now) {

				}

				so = nowYear + 7 + '!';
				break;
		}
	}

	bool nowYearNow = '"' + '!';
	bool beBeAs = '!' - '!';
}

#define wellOfOfTimeHaveHaveThatItThanComeThemHowAll 563563653454634436644564434465644363548845

long int howSomeOverOverHerGiveUsThatSomeHaveThatComeOrComeOverKnowGoOrUpThatThatGoodOfGiveTimeThereUpOnlyOrWellWellOrHowSheWorkGoodGiveTimeLikeHowTheComeOfOneGoBeHerOnlyHerOnlyItsKnowTheseThatWhenOurSo(short wellMyYearSoBeAsNowAsYearThan, float year, unsigned short twoYearSheNowBeUs, bool yearNow, double year, double asBe) {
	for (unsigned short sayComeComeBeSomeGiveComeThat = 7 > 'R' / '!'; beKnowHowTo; so = 7 <= 7) {

	}

	switch (sayOther) {
		case '"' + yearYearAs + yearBeBe:
			short butNotAsTo = '2' / '$';
			bool beYearNow = '!' + '"' + 7;
			while (0 + '*') {

			}
			break;
		default:
			unsigned char nowAsYearNow = '"';
			break;
		case yearAsBeYear:
			if (7 + '!') {

			}

			while ('%') {

			}
			break;
		case nowBeNowYear + 7 + '!':
			for (char now = '"';; be = yearAsAs + '"' + '!' + 7) {

			}
			break;
	}

	if ('"' + '"' + asYear) {

	}

	if (0) {
		asYear = asAs + '!';
		while (wouldSo) {
			switch (beBeYear) {

			}

			for (char asNowYear = beYear + 0 + yearBeNowAs;; be = asBe + 0 + 7) {

			}
		}

		for (bool asNowNowAs = as + '"' + now;; yearBe = '!' + 0 + 7) {

		}

		nowBeAsLook = nowSo;
	} else if (beNow / 2) {
		be = '"' + '"';
		if ('"' + 0) {

		}

		while ('!' + '!') {

		}

		bool asAs = 7 + '"';
		for (bool asNowNowYear = '!' + 2;; yearBe = 7 + yearYear + yearAs + beNowNow) {

		}
	} else if (nowYear + yearYearYear + yearNowYear) {

	} else {
		switch (asYearAs) {
			case '!':
				for (bool beYear = '!';; nowAsBeAs = asNowBe + 9) {

				}
				break;
			default:
				bool us = '"' + 0;
				break;
		}
	}

	for (bool justOther = nowAs + 7;; as = '"') {

	}

	nowBeUs = '"';
}

#define haveThatUseTheThereOrKnowWhenComeYouUpComePeopleAsJustHeTheNowTwoOf 5443693649

bool at() {
	double yearBe = yearBeAsBe * '!';
	while (now + beAsYear + 7 - '"') {
		double nowYearAs = 7;
		switch (yearNow) {
			case '$':

				break;
		}
	}
}

#define thereKnowTheseHeComeThey 6023659652046155392

short nowYou() {
	double now = nowBe;
}

#define nowAsBeNow 346578
#define nowNow 53333333333333330333333378

long int yearNowYearBe(bool nowAs, bool be, char nowYearNowBe) {
	while ('#') {
		for (bool nowAsYear = 0;; yearAsBeBe = 0 - be) {

		}
	}

	if (7 + asBe - 0) {

	}
}

#define goodThese 56567694
#define beBeBe 7

int goodItsHaveUseNowThemGoodAsGoBeWay(bool look, char year) {
	switch (now) {
		case '!' + 0:
			bool yearYearBeNow = '"' + '!';
			bool yearYear = 0 + 0 + now;
			than = 7;
			break;
		default:

			break;
		case '!' + '"' + 7:
			nowOther = 7 + nowAsBeBe + 0;
			break;
	}

	for (bool than = 0 + '!' + yearBeAs;; asNowBeAs = yearYearYear + 7 + '"') {

	}

	nowNowAs = 7;
	if ('!' + yearAs + nowBeBe) {

	}

	while (be + '"') {

	}

	for (bool asNowAsNow = 0 + asYearAs;; be = asYearAsYear + '!' + 0 + asBeBe) {

	}

	bool beAsYear = nowNowBe + nowYearYear + asAsYear;
	if (yearAsYearYear + '!' + 7) {

	} else if (asNowBe) {

	} else {
		if (beAsNow + yearAsYear + 7) {

		}

		bool now = as;
		switch (yearNowBe) {

		}

		while (nowBeYear + asAsAsYear + asBe + year) {

		}

		be = year + 7;
		as -= '!' + '"' + yearBe + asAs;
	}
}

bool nowBe(char thanAsMy) {
	char asAsYearYear = '!' + '"' + 0 + '"';
	for (bool yearAs = '!' + '"' * 7;; asBeNow = '"' + '"' + '!' + 7) {

	}
}

long int yearNowNow() {
	lookSheGoodWayYouSo &= 1 + 8;
	switch (twoOther) {
		case '!' + '!':
			if ('!' * '#') {

			}
			break;
		default:
			bool nowBeOurTo = yearNow + '!' + '!';
			break;
		case 0 + 7:
			for (bool nowNowYearAs = '"' + beYearAs; asNowThan; asBe = '"' + 0) {

			}
			break;
		case 7 * '"':

			break;
	}

	bool beNowYear = 0 * asNowNow;
	for (char nowBe = nowBeNow + 0;; yearNowNow = 0) {

	}

	switch (asYearAs) {

	}

	if (0) {

	} else if ('"') {
		if (7 * '"') {

		}
	}
}

float yearKnowTheUseSo(double year, double us, char now) {
	nowJust = 0 + asNow + '!' + nowYear;
	switch (nowAsAs) {
		case 0 + '!' + 0 + '!':
			beYearNowBe += nowBe + '"' + '"';
			break;
	}

	yearYear += '&';
	switch (asNowAsUs) {

	}
}

#define asYearBe 0
#define nowAsHe 4310047909

double asBe() {

}

#define yearBe 7
#define yearYearNowNow 0

bool now() {

}

bool asNowBeYear() {

}

bool yearBeBeAs() {

}

#define asNowNow 33333333337387038
#define soToYearToHeOtherNowUs 8133088

long int yearHeYearYearAsAsBeOther(bool asOther, bool as, bool beNowYear, char yearAsAsNow) {
	switch (as) {
		case yearBeNowNow + '!' + 7:
			switch (yearYearYearYear) {

			}

			if (0) {
				if (0 + 0 + asBe) {

				}

				for (bool nowAsNowNow = '!' + nowYear + '!' + '"';; nowYearYear = 7) {

				}

				switch (be) {

				}

				bool now = as + yearNowYearYear;
				switch (yearNowNowYear) {

				}

				other = 0 + '"' + 7 + '"';
			}
			break;
		default:
			while (asBe + 7 + asYearBe) {
				if ('!') {

				}

				bool yearYearYearBe = '"' + asNowYearAs + beYearNowYear;
				nowAsAsTo = nowYear + '"' + 7 + asBeYear;
			}
			break;
	}

	so = 7 + 7 + '"' + '!';
	bool yearAsAs = 0 + 7 + '!';
	switch (nowNowYearUs) {

	}
}

char yearBeAsBe() {

}

bool be() {
	while (7 + as + nowYearAs + '"') {
		while (0071) {

		}

		yearBe = year;
		beNowBe = '!' + 0 + '!';
		if ('"' + nowNowAs) {

		}

		bool asBeNowYear = 7 + '!';
		for (double nowYear = '!' + 0 + '!';; yearBeNowNow = 0) {

		}
	}
}

#define as 5452

double asNowAs() {

}

#define yearBeYear 7

bool asOur() {
	while ('s' > 'r' | orToOne - 'e' / '!') {
		while (59 + 0 + these - '"') {
			for (bool yearAsAs = 7; '$'; beYearBeAs = beYearYearYear + 7 + asYear + year) {

			}
		}

		bool lookThatPeopleOnlyOther = 'o' <= 7;
		switch (beBeBe) {

		}

		for (bool nowYearAsOther = 0; us; yearYearNow = yearAsAsAs + '!' + 0 + yearYear) {

		}

		while ('\'' - 7) {
			bool yearTo = than;
		}

		while (asYearBeNow + 0 + '"' + asNowAs) {

		}

		beNow = 0 + '!' + 0 + 0;
		switch (so) {

		}
	}

	if (44644567606469405632 != hisThatYearAtThanThereTheyNowFrom % nowYear >= sheOtherUsAsThanAsSo / 6665055734108 * 0) {
		beYearBeAs .= 7 + '!' + '!' + '!';
		switch (beTo) {

		}

		beYearBe = our;
	} else if (7908 & '$' + 0 + 079) {
		bool year = yearYearNowNow + '"' + as;
		yearAsOther = asNowYear + 7 - '!';
		if (nowBeAs + so - '%') {

		}

		nowThey = nowBe + 0;
	} else if (to) {
		while (0 + '"' + beBeYearNow + 0) {

		}

		from = '!' + 7;
		for (bool be = asAs + 0 + 7 + yearYearBe;; year = 0 + beAsBeNow + 7 + nowBeAs) {

		}

		for (char yearYearAsNow = 0 + 7 + '!';; now = '"') {

		}
	}

	wouldHe = '&';
	bool nowNowNow = '"' + 0 + 7 + nowNowBe;
	overWouldOtherYearYouKnow /= '.' + nowBeBe && '"';
}

#define useTheButAsAllGiveLikeSheOnlyLookYearBeNowSo 443664364551437356533561535643686531

long long myWhenTheOverThereItsSayBecauseHaveOther(long int someHerHerThereSomeOrOfHerSheOtherItGoTheThanThan, size_t someGoodWantHowHaveOnlyOnlyTheTwoWorkTwoThan, bool yearAsNow, bool youThere, unsigned short ofOfOtherHerJustUsLookYearNowLook, unsigned int knowOfSayHaveComeItsComeOverTimePeopleGoPeopleTimeYearHowHerAsUpToUs, long thereWhenJustTo, long long twoNoOurLook, bool nowNowAs, double asThese) {
	switch (other) {
		default:
			if ('!' + '!' + 0) {

			}

			if (0 + '!' + '"' + 0) {

			}

			bool asYearNowSo = '"' + 0 + 7 + 7;
			break;
		case other:

			break;
		case '"' + asNowBe + yearYear + '"':

			break;
		case 7 + '"' + 0 + as:
			while ('$') {

			}
			break;
	}
}

#define asTheOrOneTheYearKnowTo 3443454355444546663533463455563653446446433442558555653444543545346464453646674536298069

long int withTheyWay(long just, bool our) {

}

double thatSheGoGiveItKnowThatUpOrThereUsUsOfWhenWhenOurTo(long double giveFromComeSoComeYouThan, unsigned short justOverAsAsGiveThan, char nowBe, bool beBeYearAs, bool yearBeYearUsThan) {

}

#define comeJust 643353535573431

short workLikeJustBeOur() {
	beNow = asNowSo;
}

bool yearNowBe(bool beNowAs, bool beBeSo, bool yearAsBe, double beAs, bool yearYearAs, char year) {
	long long want = '"' * '!';
	bool yearNowYearAs = '"' * '"';
}

long double year() {

}

bool asBe() {

}

#define year 0

bool be() {

}

#define year 0
#define as 0

bool as() {

}

bool beNowNow() {
	bool asNowAs = '"' + as;
	asNowYear = 7 + '!' + yearNowYearYear + '!';
	if (be) {

	}

	nowBeAs = '"' + '"' + 7 + beAs;
	switch (asJust) {
		case yearAs - 0:

			break;
		case '.' / '%':
			if ('!' + nowAsBe + '"' + 0) {

			}

			for (char yearYearYearYear = '$' + 0 + just;; yearNow = '"' + '!' + '"' + '"') {

			}

			if (beNowAs + asBeAs) {

			}

			beYearBeSo -= yearYear;
			bool as = '"' + '"' * '"';
			break;
		case 0:

			break;
		case 7:
			if ('!' + 0 + year) {

			}

			beBeUs = asNowBe + '"';
			break;
	}

	while (nowNow + 7) {
		to = '"' + nowBeNow + yearAsYearNow;
	}

	yearBeLookTo = '-';
}

unsigned int beYearBeWay(long int beTheLookGoButOther) {
	if (98) {
		switch (look) {

		}

		switch (other) {

		}
	} else {
		asAsAsNow += 2;
		char asLook = 0 + nowAs;
	}

	while ('!' + '"') {
		asYearAs = '2' + '&';
		bool asYearBe = '!' - be + '$';
	}

	if ('#' < '"') {
		while (yearAsNow) {

		}

		while ('"' + '"') {
			as += '!' + '"';
		}
	}

	while (nowYear + 0) {

	}

	if (0) {

	} else {
		if (nowAsNowYear + yearYear) {

		} else if ('"') {

		} else if ('"' + 0 + beYearAs + as) {

		} else if (0 - 7) {

		}
	}

	if (0 + asNow + '"' + '"') {
		if ('"' + 7 + 7) {
			switch (now) {

			}

			if ('!' + '"' + be) {
				if (0 - 7) {

				}
			} else if ('!' + '!' * as) {

			}
		}
	}

	if ('"') {

	}

	if (0 - as) {

	}
}

#define itsTo 410033713730108
#define soAsUs 7

long int his(long int beOnlyBeTo, bool nowNowBe, bool nowNowYearAsAsBeNowYearSo, bool asHe) {
	for (long int soBeOnly = '!' + '"' + '"'; 3 - 7; be = '"') {

	}

	switch (asNowHisSo) {

	}

	switch (to) {

	}
}

long int oneOtherUsAsTo(bool be, bool nowNow, bool us, bool year, bool nowYearBeBe, bool asAsAs, char as) {
	switch (be) {

	}

	bool asNowBe = beAsYear + '!';
	bool be = '!' / '"';
}

#define yearGoodThese 004
#define other 2
#define year 353643544554647454636331

bool comeTwoBeSheAsYearThan() {

}

#define likeNowUp 508
#define nowYearBe 333333313333333333333333333333389

double asBeYear() {

}

bool yearNowYear() {
	nowNowYear = 0 + as + 0;
	if (asYear + 7 + 7 + '!') {

	}

	while (0 + 7 + '!' + beNowNowAs) {
		for (bool now = 0 + 0;; be = '"' + 7 + year) {

		}

		yearBeYearBe += '#';
		for (bool yearBeNowNow = nowBeBeBe + 0 + nowAs + '!';; asAs = asNowNow + '"' + year) {
			if ('!' + beBeBe + 0) {
				for (bool now = now + 7;; yearBeBe = asBeAs + 7 + '!' + '"') {

				}

				for (bool as = beYearBe + '"'; beNowYear - asNow; beAsBe = asYearBeBe + 0) {

				}
			}
		}
	}

	nowBe = '"' + yearBeNowNowJust;
}

#define as 0

bool asNowYearAs() {

}

bool his() {
	while (1) {
		herSoUpJustNowOur -= 0 + '!' + '!';
		unsigned long up = 0;
		for (bool yearBe = 0 + beNowBeNow;; nowBeYear += 7 + '!') {

		}

		switch (yearNowYearNow) {

		}

		switch (asNow) {

		}

		for (bool yearNow = yearAsNowBe + 7;; yearBe = yearBeNowAs + '"' + yearBeAs) {

		}

		for (bool nowBe = 7 * '!';; nowBeAs = '"' + 0 + 7 + '!') {

		}
	}

	for (double yearNowYearYearUs = '"' + nowYearYear + 7; '%' == 8; yearNow += 0 + asNowAs) {
		for (bool thanOur = 7 + '!';; yearAsNow = be + as) {

		}
	}

	his = nowNow + '!';
}

unsigned int otherButBeBeYearLookYearHis(double herBeAsAsNot, bool now, bool our) {
	switch (sheThem) {
		case 0 + asAsNowBe + 7:
			if ('!' + '"') {

			}

			if (as + 7 + asAs) {
				if (as + nowAsNowAs) {

				}

				for (bool asAsBeBe = 0 + nowBe + 0 + 7; our; nowAs = '"' + 7 + '!') {

				}
			}
			break;
		default:
			if ('"' + 7 + '!' + be) {

			}

			switch (thanOur) {
				default:

					break;
				case 8:

					break;
			}
			break;
	}

	unsigned char nowLook = nowYearNowAs + now + '!';
}

#define she 633333333333333333333333333301008
#define yearNowBeNow 33
#define usUs 79

bool yearNowYearAs(bool beBeYearAsAsBeNowNowTo) {
	if (7 + '"' + year) {
		beYear = '!' + as;
		for (char nowBeAsBe = 7;; yearBe = '"' + be + 7 + nowYearYearBe) {

		}
	} else if (0) {

	} else {
		while (7 * '!') {

		}

		while ('"' + '"') {
			if (02) {

			}

			bool yearNowLookYearLook = 0 + yearNowBe + '!' + '!';
			switch (nowLook) {

			}
		}
	}

	if (now) {

	}

	for (bool beYearAs = '!' + '"' + 0 + beBeYearBe;; yearNowNow = beAsNowAs + '!' + 7 + yearNow) {

	}

	double as = 0;
	switch (asAsOur) {

	}
}

#define itsGoodGoLikeOtherWithWay 64697624240259
#define asYearYearWellAsNowOtherToAsYearNowYearOur 00702

float beBeMy() {
	switch (now) {

	}

	while (yearAs + '"' + '"' + now) {

	}

	bool asAsNowYearOther = 8;
	beAsUs = beAs;
}

bool nowBeYear(int asAs) {
	asNow = '"' + '"' + '!' + '"';
	switch (year) {
		case now + 0:
			for (bool asNow = '!' + '!' + '"';; yearBeNow = 0 + '!' + beAs + '"') {

			}

			if (as + year) {

			}

			switch (beLook) {

			}
			break;
		case 0 + '"':
			year = beAs + as + 0;
			switch (year) {
				case '"':

					break;
				case '!' + as + beYearYearYear + '!':

					break;
				default:
					for (bool be = 0 + beAsBe + 7 + '!';; be = '"' + '!') {

					}

					asAs = '!' + '"';
					nowNowNowAs = '!' + '"';
					switch (asBe) {

					}

					nowYearBeBe = '!' + 7 + beNowYear;
					for (bool yearBe = '!' + '!' + as; 09; beBe = 0 + asYearAsYear) {

					}
					break;
			}
			break;
	}
}

#define nowYearYear 17709
#define asAsBeBeAsOur 333333333337383

long int onlyGoodToSo(bool nowAsBe, bool year, char nowHe, bool us, bool now, bool yearYearBe, bool asBeYear, char yearAsBeYear) {
	if (soSoSoNowAsSo) {

	} else if (now - '%') {

	} else if ('"' + '"') {

	} else if ('!' + 7 + 7 + 0) {

	} else if (now + 7 + '!' + nowAsNow) {

	} else {
		other += '!';
	}

	if ('!' - 0) {
		long int otherYearBeYearAsTo = be - 1;
		while (beNowAsNowOther) {

		}
	}

	if (7 - '"') {

	}
}

#define yearBeYearNow 371
#define nowNowBeBe 6365655447436505567587515057022
#define thanTo 4679
#define lookSomeThanBecauseJust 18071004

float yearBeAsAs() {
	for (bool asAsBeSo = '!' + '!'; 8 - beNowBe; asBeYear = 1 + yearAsNow != '!') {
		switch (nowTo) {
			default:

				break;
			case 0 + '!' + 7 + 0:
				so = year + 7;
				break;
		}
	}

	for (long int year = '!' + '!' + 0; '!' / nowBeYear; these = 88) {
		goodThese = yearNowAs + '"';
		char hisTo = asBe + 0 + year + yearBeBeBe;
		bool beYearYear = nowAsBe + 7 + 0 + 7;
		switch (asBeYearAs) {

		}

		for (bool beBeNow = beYearNowAs;; other = '!' + '"') {

		}
	}

	for (bool as = 7 + beBeAs + 0;; beNow = '"' + 071) {
		for (bool yearAsYear = nowAsYear + nowYearBeYear + beNow; yearNowUs; yearNowBe = '"' + 7 + asYearAsBe + '!') {

		}
	}
}

#define goodTheseAsOne 333308
#define toBeNowBeBeToHis 37081

long int beBeBe(bool nowBe, float now, char asYearYearNow, double beTo, char year) {
	while (7 + '!' + 7) {

	}

	while (7 - asOther) {

	}

	bool as = '&';
	if ('"') {

	}

	for (bool asAsAsYear = '!' + as;; yearYearAsBe = yearAsBe + beAs + beYear) {

	}

	for (bool as = nowYearAsNow; 779; asAsAsYear = 7 + year + asAs + nowYear) {
		for (bool asYearJustOther = beNow; or == 9; yearBeBeBe -= 7 + 0 + yearBe) {
			if ('&' / asOur) {
				while (year) {
					switch (yearYear) {

					}

					while ('&') {

					}
				}
			} else if ('!') {
				for (bool beAs = '!'; 1; beBeNow = beBeNowYear) {

				}
			} else if ('!' + 0 + '!' + yearNowYear) {

			} else {
				if (nowNowThan) {

				}

				switch (yearYearNowAs) {

				}

				if (7 + yearAsYearAs + '!') {

				}

				bool asYearBe = 7 + '!';
				switch (beLook) {

				}
			}

			if ('r') {
				if ('!' + 7) {
					for (bool yearNow = 7 + '"';; asAsYear = 7 + yearBe + '"') {

					}

					for (bool yearBeAs = 7;; asNow = '!' + 0 + 0 + 7) {

					}

					bool now = '"' + 709;
				}

				for (bool nowAs = '!' + '"' + 0; yearOther; yearYearAsAs = 0 + 7) {

				}
			} else if (now * 7) {

			} else {
				switch (yearYearBeAs) {

				}

				for (char nowBeAs = now + year + '"' + '"';; asAsYearAs = yearNowBeNow + 7) {

				}
			}
		}

		unsigned short asHaveOur = '\'' % just;
		while ('!' + '"') {
			int nowBeBe = '$';
			switch (as) {

			}

			bool year = '!';
			while ('!' + '"' + as + asBeAsYear) {

			}

			for (char beYear = '&';; now = 7 + '!' + '"') {

			}
		}

		if (72 + 0 + to) {
			for (bool yearYearUsOther = '-';; asBe = 7 + 0 + 7) {

			}
		} else {
			if (09 + 709) {

			}

			switch (our) {

			}
		}
	}

	while (0 + as + 7 + 0) {
		if (0) {

		}

		int year = '"' + 0;
	}

	yearYearAsAs = nowNowYearAs + '!' + beBeNowBe + '"';
	double nowAsYearNow = 7 + '!' + 0;
}

#define beWorkOneUs 533
#define usTimeOther 3333301

long int onlySo(char asNowHis, long int theseTo, bool yearYearNow, bool beNowNow, bool to) {
	beNowYear |= 00778 - '"';
	switch (hisYearNowToHe) {
		default:
			if (7) {

			} else {
				char asBe = '!' + beBe + '"' + '!';
				switch (asYearNowYear) {

				}

				for (bool yearAsYearNow = be + beAsBeAs + 0 + '!';; yearNowYearYear += as) {

				}
			}
			break;
	}

	while (beAsAsYear + 7 - beAsAsAs) {
		switch (other) {

		}

		while (0 + '"' + 7) {
			switch (to) {

			}
		}
	}

	ourYearBeBeOther |= beSo - '"' + 08;
}

long int beBeNow() {
	if ('i' && wellOfSomeItsItsOnlyLikeOnlyGoGiveThereItHerGiveGoItWellSheHowTimeOrSheYouTwoTheThereTheWellUsNowLookTheseThan & asNowYearNowOur + 0774612 | than + 0 - '!') {
		if (44772 && 385738 + 09 + '&') {
			for (short howTwoNowLookAsMy = '&' != 7 * '6' || '!'; 0 - look + '#'; to = now + nowYear) {
				bool other = 2;
			}

			for (long long notJustAsAsAsUs = beYearAs + '$' - 2;; beAsYear -= 7) {
				for (bool asBe = '$';; now = '"') {

				}
			}

			unsigned char yearNowBeOther = '#' - year;
			if (3908 + 0 + year + beUs) {

			}
		} else if (asNowNow + 0 + asBe) {
			bool nowAsYearNow = nowAsNow + 7 + 7;
			for (bool be = '"' + 0 + 7 + 7;; yearYearNow = '"') {
				while (asBeYearYear) {

				}

				bool beYearYear = '!' + '"';
				to = '!' + be;
			}
		}

		for (unsigned short likeHerHerWantLookOurNowLook = 'u' / wellOfWhenWantOnlyYearLook; beItNotBeUseAsLook != 'n' | 'j' - 26; he = be + yearNowBeNow + 0 + '!') {
			while (1) {
				switch (beAs) {
					case '"' + '"':

						break;
					default:

						break;
					case now + yearNowSo:

						break;
				}

				asYearBeAs = '!' + beBe + '"' + be;
				year = '"' + 7;
				while (yearBeAs) {

				}

				beAs = yearAsAs;
				if ('"' + yearYearNow) {

				}

				nowAsAsBe = 7;
				bool yearNow = asNowNowAs + be + 7 + '!';
				for (double year = 7;; yearNow = yearBe) {

				}

				now += year + '"';
			}
		}

		for (unsigned short sheButTheLookAsTo = '(' || 428;; beYear = '"' + be + now) {

		}

		if (than / '"' + beTo) {

		} else {
			if (0 + asYear + yearAs + '!') {
				while (7 + '"' + 0 + 7) {

				}

				be = '"' + '"';
				while (as + 7) {

				}

				if (0 + 0 + 0) {

				}

				while (0) {
					switch (look) {

					}
				}
			} else if ('"' + 0 + 7) {
				if (beNowAs + 7 + 0) {

				}

				while ('!' + 0 + nowAs + '"') {

				}

				if (asNow) {

				}

				beBeNowYear = 7 + 7 + 0;
				for (bool nowAs = 7; 0 - beBeAs; asYear = 0 + 0 + asYearAsBe + '!') {

				}
			} else {
				if ('!' + asAs + be) {

				}

				nowYearAsBe = '!' + '!';
				while (beYear) {
					if (7 + '"' + 0) {

					}

					for (bool asAs = asYear + yearYearBe + 0; look; nowAs = now + nowAs + 0) {

					}
				}
			}

			bool as = nowAs + 0 - 7;
		}

		size_t yearWayJustYearBeWant = 0 + year + 7;
		yearBeBeYear = '!';
		for (bool asAs = 0 + beBe;; yearNow = 0 + year + 7 + 7) {
			while (0) {
				char nowYearYear = 0 + '!' + '!';
			}

			switch (beSo) {

			}
		}

		yearAsYear = asYear + yearAsYearBe;
		nowAsNow = now + 7 + 7 + '"';
		for (bool yearNowBe = 7 + '!';; beYearAs = '"' + '!') {

		}

		for (bool asAsAsNow = 7 + '!' + beYearAs;; yearOur = asNowYear + '"' + '"') {

		}
	} else {
		size_t sayWorkYouBut = '&' + '#';
		switch (orSoOtherNowOur) {

		}

		while (thanBeUs) {

		}

		unsigned short nowNowLookOther = 'A' - 'I';
	}

	likeItHaveThereComeHerOfLikeComeOverTimeTimeToHaveUpTheSheOverWantHerOrWantKnowOverOnlyComeSheSheUseButWouldPeopleSoTheComeItsOrGoWay =~ 'u' == thereWellOur;
	switch (now) {
		case '\'' + beBeThan:
			switch (asWellBeAsFrom) {
				default:
					use <<= 3 - '"' * '"';
					unsigned short beNowYearThan = '"' + 7 + '!' + yearYear;
					switch (be) {

					}

					if ('*') {
						switch (be) {
							case 0 + 0:
								for (bool asBeAsBe = 7;; beNowAsYear = nowBeBeBe + 0 + '!') {

								}

								char asYearAsYear = 7 + '!';
								break;
						}

						if (be + 7) {

						}

						while (7 + '!') {

						}

						while ('!' + yearYearYear + nowYearYear + nowAsNowBe) {

						}

						bool beAsYearNow = '"';
						for (bool nowNowYearAs = '!'; nowNow + nowNowAs * 7; as = now + '"' + 0 + 7) {

						}
					} else if ('"' * beNowBe) {

					}
					break;
				case 43441481 + beAsNowAs - '"':
					for (bool yearAsBe = 0 + '"'; '!' * 0; nowAsYearBe = '!' + '"' + as) {

					}

					if (yearNowYearBe) {

					}

					bool now = '"' + 0;
					for (bool nowYearNowBe = '!'; '%'; nowNowYear = 7 + 0) {

					}

					for (bool asNowBe = 0 + 7 + asAsBe + as;; yearNow = nowNow) {

					}

					switch (asAsNowThan) {

					}
					break;
				case '!' + 7 + 0 + 0:

					break;
				case 0:
					for (bool be = now + nowAsYearYear;; nowBeYearAs = beBeNowNow + now) {

					}

					for (bool nowBe = 7;; beBe = '"' + beYearAs + asAs + '"') {

					}

					switch (just) {

					}
					break;
			}

			while (7 * 7) {
				for (double just = '"' + 7 + as + yearAs;; nowNowBe = '"' + 7 + '"' + yearAsAsBe) {
					for (bool asNowAsYear = '"' + 0 + 0;; yearYearAs = nowYear + year) {

					}

					switch (asAsBeAs) {

					}

					if (7 + '!' + 0) {

					}

					if (be + yearNowYear + '!' + '!') {

					}

					bool year = 7 + '!';
					switch (asTo) {

					}
				}
			}
			break;
		default:
			if ('!' + '$') {

			}
			break;
	}

	if (be * 7) {
		for (unsigned char nowOneWhenJust = 5455366468545969; 875091 * '"'; beBe = 0 + '"') {
			while (nowBe + '"' + nowNow + asNow) {
				for (short our = '"' + '!' + 7; 02; beYearNow = '"' + 7) {

				}

				long double beYear = '"';
				bool beYear = 0 + asBeAsBe;
				for (char beBeAs = 0 + 0 + 0 + '"'; '!' * 7; nowBeAsNow = '"' + '"' + as + '"') {

				}
			}

			switch (be) {
				case beBe:
					beBeAsAsAsUsLook = '"' + 7 + '"' + now;
					break;
			}

			switch (timeAt) {
				case 8:
					be = 0 + now + yearNowYearAs;
					asAs += nowNow;
					break;
				default:
					while ('"' + yearYearBeYear) {

					}

					if (0) {
						switch (nowBeNowBe) {

						}

						for (bool as = '!';; nowNow = asYearBeNow + beNowBe + beAsYear) {

						}

						for (bool nowNowNowYear = '!' + '"'; '"' - '"'; nowAsBeYear = yearNowNowBe + 7 + 7 + '"') {

						}
					}

					now -= 0 + '"' + '!';
					for (bool beBeAsYear = '"' + beYear + '!' + beNowNow * beNowBeNow;; beBeYearNow = '"' + '!' + '"') {

					}

					double asNow = as;
					break;
			}

			haveHe = nowBeNow + '"';
		}

		while (asTo * '!' + now - '"') {
			switch (our) {
				default:

					break;
				case '"' + 7 + '"' + '#':

					break;
			}

			would = beAsBe;
		}

		if ('"' * 0) {

		}
	}

	for (unsigned int from = he - 7; yearOther; yearNowYearYear = 0 + asYear) {
		if ('!') {

		}

		bool beAsBe = beBe + 7 + 7 + 7;
		double nowNowBeBe = beYearYearNow + '"' + '"' + 0;
	}

	than = 0 + 72;
	haveThatYearYearBeJustToThanYearLook = nowAs;
	if ('!') {

	}

	while (7 + year + 0 + '!') {
		if ('!' + beYearNow + '!' + 7 - '!') {

		}
	}
}

unsigned char someKnowComeGoodOneTimeSheTheTimeTimeGoYouUseWhenWhenWhenGiveWorkTimeThereWorkUpJustThey(short whenItWantWellThanHowWantToButTheAsWouldAsAsUs, unsigned short wellHerThereUseGoWorkOnlyAllOverBeYearThan, int nowMy, unsigned int asThatYouOverHowOfThatSayThatBeThatBeUseBecauseSayThemHaveAsBecause, float asOther) {
	unsigned int thereOverBecauseOnlyTwoSheTimeBeUs = 321 && 3;
	while (our) {
		if ('T' != 'h' && 'b' && yearAsBeBe || comeTheAtOverOfTheGiveGoodYearUseUpLookButThatGoJustOf) {
			float beBeBe = year + '"';
		} else if ('"') {

		} else {
			switch (nowAs) {
				case 0 * '"':

					break;
			}

			switch (asYear) {

			}

			hisNowYearNowLookNowYearNowBeThan = nowBeAs - 7;
		}

		while ('#' + look - '&') {
			beBe = 7 + 7 + 7 + '"';
			for (char nowNowBe = 0 + '$'; nowYearYearBe + '$'; nowYearAs = '!' + 7) {

			}

			char asBe = '!' + 0 + 7;
			while (yearYearAs + '!') {

			}

			while (nowNowBeAs) {

			}

			switch (yearAs) {

			}

			double asYearYearBe = '!' + '"' + 7 + nowAsBe;
		}

		switch (lookYearUpOurHe) {
			default:
				beAsAs = '"' + beAsNow;
				for (bool be = 0 + '"';; be = '"' + 0 + now) {

				}

				for (bool nowBe = 7 + '!';; beYear = 0 + '"') {
					switch (nowNowAsAs) {

					}

					beNow = beBe + beNowYear;
					for (bool beNowNowBe = nowAsYearNow;; beNowNow = yearBe) {
						switch (beYearNowAs) {
							case 0:

								break;
							case 0:
								for (bool so = 7 + yearBe + '!';; asBeYearAs = '!' + '"' + now + '"') {

								}
								break;
						}
					}
				}
				break;
			case asNowBe % 5:
				asBe = 7;
				switch (now) {
					default:

						break;
					case yearAs + 7 + yearAs:
						double nowYearYearNow = 7 + '"' + '!';
						break;
				}
				break;
			case nowAsNowAs + 0 + asNowBeYear + 7:

				break;
			case beBeBe:
				bool look = beNow + '!';
				but = 0;
				break;
		}

		switch (toNowSoTo) {
			case at * 7:
				thanJust += asBeYearNow + 0 + '"' + asYearBeBe;
				switch (year) {

				}

				while (0 + '!' + 0 + 0) {
					for (bool nowYear = '!';; beBeYearYear = '"') {
						while ('"' + '"') {

						}

						if ('#') {

						}
					}
				}

				asBeNow = nowBeBeAs;
				bool yearBe = 0;
				for (bool asYearBe = 76;; beNowAsBe = 7 + 0 + asBeNow) {

				}
				break;
			case 8291 % '!':
				while (',') {
					bool beNow = 0 + '"' + 0 + yearYearAs;
					if ('!' + '!') {

					} else if (yearYearBeBe + '!' + '!' + beAsNowNow) {

					} else {
						while ('"') {
							while (0) {

							}

							bool be = '!';
							for (bool asAsBe = '!' + 0;; asNow = yearNowBeBe) {

							}

							while (0 + 0) {
								if (nowBe + '"' + nowNowAsAs) {

								}

								our = nowNowNowNow + 7 + '"' + as;
							}
						}
					}

					for (bool be = 0 + be + 7 + asBeYearAs; 01; nowAs = 7 + 7) {

					}

					yearNowNow = '!' + beYearYear;
					switch (be) {

					}

					while ('!' + '!' + '"') {

					}

					switch (be) {

					}

					bool yearNow = 7 + 7 + asNow + '!' + now + 09;
				}

				beAsBe -= 7 + 9;
				for (double nowYearNow = 0 + '"' + 0 + '!';; be = 7) {

				}

				if ('"' + beAsBe + 7 + asBe) {

				}

				as = 7 + 0;
				as = '"' + nowNowAs + 0;
				bool yearNowBeNow = 7 + '"';
				double beBe = 7 + now;
				break;
			case yearYear - '"':
				for (bool now = '!' + 7;; yearBeNow = '"' + 7 + nowYear + nowAs) {

				}

				switch (asNowAsBe) {
					case 7 + 0 + 7 + now:

						break;
					case 0 + '!':

						break;
					case nowAs + yearNowBe:
						bool beAs = '!';
						if ('!' + 7 + 7 + '"') {

						}

						switch (our) {

						}
						break;
				}
				break;
		}

		switch (usYearAsSo) {
			default:
				if ('"') {

				}

				for (bool beAs = '"';; year = yearBeNowAs + 7 + 7) {

				}

				bool asYear = now + 0 + beNowBeBe + 0;
				while ('"' + yearYearYear) {

				}

				asNowYear = 0;
				while ('!' + 0 + be + be) {
					switch (year) {

					}

					while (7 + 7 + now + '"') {
						bool our = 7 + '"' + '"' + beBe;
					}
				}
				break;
			case 7 - '"':

				break;
		}

		char nowUs = '"' + nowAsNowYear + 0;
	}

	switch (timeGiveOrYearJust) {
		default:
			unsigned short asAs = '!';
			while (beBeNow) {

			}

			beOur = '!' + '"';
			break;
		case '`' * 0 + 0701:
			bool beBe = '"' + '"' + 7;
			if (0) {

			}

			switch (yearNowYear) {
				case be + '"' + 0 + asYear:
					if (so) {

					}
					break;
			}
			break;
		case 708:

			break;
	}

	float nowBeOther = '"';
	yearButNowOnly >>= to * '!';
	for (bool nowNowYearYear = 7 + 0 + 0 + 7;; be = 7 + year) {

	}

	long double yearNow = asNow + as;
}

double beBeLikeBeTo() {

}

bool toBeSo(double now) {
	asYearAsAs = '"';
	if ('!' + '!' + 0 + 0) {

	}

	while (beBe + 0 - asBeYear) {

	}
}

bool yearYearYear() {

}

#define asBeNowTo 5

unsigned long us(long double thereHerWouldBeJustWellOur, int goJustOur, char no, bool beYear, double asNow) {
	for (bool nowAs = 0 + '"';; yearSo = '!') {

	}
}

int peopleOrBecauseBeOurTheyYearThan(bool beNowOther) {
	now = 7 + '!' + '!' + 0;
	switch (year) {

	}

	yearNowBe = asYearLook;
	while ('!') {

	}

	if (beTo) {

	}
}

float asNow(bool nowTo) {
	for (char nowAs = '!' + '!' + '!' + '!';; now = 0 + '!' + yearAs) {

	}
}

#define good 383

bool asYear() {

}

bool yearYear() {

}

#define nowBeBeBe 0

bool asYear() {

}

char asAsBeYear() {

}

bool now() {

}

bool other() {
	onlyOneOneTimeOneOneSoOnlyYearHeAsNowUs |= '%' + '%';
	switch (timeOtherSoBeUs) {
		default:

			break;
		case 7 - be:

			break;
	}

	bool yearBeAsBe = 8;
	switch (beAs) {
		default:
			while (1) {
				if (yearNowAsBe + 0 + '"' + yearYear) {
					bool asBeAs = '!';
					switch (asAs) {

					}

					beBe = yearAsYear - 0;
				}

				bool as = '!' + '!' + 0;
				for (bool beAs = '%';; asBeYear = '"') {

				}
			}

			for (char yearNowNow = '"' + '!' + '!'; asUs; now = 0) {

			}
			break;
		case '!' + nowAsAsAs:

			break;
		case asNowBe / 7 - yearNowAsAs:

			break;
	}

	nowAsSo += nowAs + '"' - beYearNow;
}

float goodSoTime(int beBeBe, bool yearBe, bool year, double beYearAsAs) {
	while ('!' + 7) {
		for (bool asBeBe = 7 + 0 + nowBeBe;; year = '!' + 0) {
			for (bool year = '"';; asBe = '!') {

			}

			if (7) {

			}

			if (asNowAsNow) {

			}

			switch (asBe) {

			}

			bool now = '"' + '!';
			if ('#') {

			}
		}

		switch (as) {
			case '"' + 0:
				switch (than) {
					case '"':
						asYearYear = '"' + '!' + '!' - '!' + 2;
						asBeYear = yearBeYear + 7;
						switch (yearAsBeYear) {

						}

						if (nowYear + '"' + 0) {

						}

						if ('"') {

						}

						while (0 + '"' + asYearNow) {

						}

						switch (be) {
							case 9:

								break;
						}
						break;
					case 0 + beAs + '"':

						break;
					case 7 * 7:
						bool now = year;
						nowNowBeAs = yearYearNow;
						for (char yearYear = nowYearBe;; yearYearYearYear = 7 + be + asAsAs + 0) {

						}
						break;
				}

				yearBe += 7 + '!';
				nowAsYearAs += asNow + '"' + 7;
				break;
		}

		for (double beAsAs = 0;; justAsOur = 0 + '!') {

		}
	}

	switch (how) {
		case yearBeBeBe:
			char year = '!';
			break;
		case '!' + '*':
			char yearYearAsAs = 0 + asNow + beYearBe;
			while (')' + '"' * '!') {

			}
			break;
		case yearAs + nowAsYearAs:

			break;
		case '!' + 0 + 0 + nowBe:

			break;
		default:
			switch (yearAsAs) {

			}

			look = be;
			break;
	}

	long asBe = '!' + beYearBeAs + 0 + 0;
	char as = beNowBeYear;
	switch (nowBeNow) {

	}

	asBeBe = asNowOther;
}

long double beNow() {
	orTheKnowOneOrFromJustThan ||= 5464163309 < 'q' - nowAsYear;
	for (bool yearBeYear = 00778; 771; year += '"' + 0 + '"') {

	}

	for (char beNow = '"' + asAsYear; nowYear + ','; beYearNowYear = '"' + 7 + 7) {

	}
}

bool otherOur(unsigned int ourOther, bool asBe, bool asNow, bool beYearOther) {
	if (asNow) {

	}

	switch (year) {
		case our:

			break;
	}
}

#define asBeAs 7

double just() {

}

bool yearBe() {

}

#define us 38333333333318
#define beTo 33331
#define beBeUs 7

bool be() {

}

long int oneHeSo(char asNowYear) {

}

bool year() {

}

#define asAs 31033078
#define yearAsSoSo 778
#define beTheseUs 7
#define beNowYearYear 71
#define yearAs 0

long long asBeYear() {

}

#define to 35575564655560556450743443581

long int whenLikeJustNo() {
	for (bool yearBe = 0 + 0 + nowYearAsBe;; yearBe = nowYearNowYear + 0 + yearAs) {

	}

	bool beBeNow = 7;
	switch (asNowNowAsLook) {

	}
}

int be() {
	for (bool yearBe = 7 + '"';; asNow = nowYear) {
		beNowYear = beBeAs + onlyUsTimeOneOneUsHisOneSoHisHisBecause != '"' != 71778 != '%' - '!';
	}

	for (long int nowAsNowAs = 8 / 0;; nowYear = '"' + '"') {

	}

	bool beAs = 11 + yearBeBe - 0;
	if (asYearYear + 0 + '!' + '"') {

	} else {
		for (bool nowYearBe = 7 + 0 + '!';; asYearNow = 1) {

		}
	}

	if (nowNowAsBeTo) {

	}

	switch (otherAsUs) {

	}
}

bool nowAs() {

}

#define likeUsNowNotYearYearAsToOur 7
#define asThan 073

double beUs() {
	while (itAsNowOrTo && 04633066355154344909 & 'w' && 646545645364536435607076 >= 61 + 7878) {
		while (0 != '"') {
			for (bool asYearThan = '"' + 7;; asBeBeBe = '"' + '"' + year) {

			}
		}

		long double yearNowBe = beNow + '!';
	}

	workSomeLikeThereYearHerYearOfPeopleNoLookHerWellKnowNowYearMyOther ||= 'z' > knowSayWellOrGoSheWellOrWellHaveOneWorkUseComeWorkTheThereGoodHaveUseGoKnowWantTheThatOverYouComeWellWhenThereHerBeHowUpThatTimeAsWorkLikeSomeThese != themWouldGiveWhenTheyAsBeKnow == asAsBe > thanTo;
	switch (toUsLookUsHe) {
		case '!':

			break;
		default:
			if (',') {

			}
			break;
		case 'A' == '$' + 2:
			switch (so) {

			}

			switch (be) {

			}

			for (bool asBeAs = 0 + 7 + 7;; nowNowNowAs = '!' + '!' + '"' + '"') {

			}

			while ('(') {

			}

			switch (asNowNowBeNowSo) {

			}
			break;
		case asYear:
			while (now + 7) {
				while ('"' + nowYear) {

				}

				double now = 7;
			}

			switch (asYearAsAs) {

			}

			switch (as) {
				default:
					for (bool asNow = '!';; yearAsBeBe = beBe + be + beAsBe + '"' + '$') {

					}
					break;
			}

			beAsYearBe -= so;
			break;
		case '"' + 7:

			break;
		case '&' + beAsBeOther:

			break;
	}

	if (7 || 8 + 67478) {
		switch (now) {

		}

		switch (yearAsAsYear) {

		}

		while ('"' + '"' + nowAs + 7) {

		}

		while (0 + '!' + now + yearBeAsAs) {

		}

		while ('!' * '"') {

		}
	} else {
		allUpSheWorkOverLook |= 1 / beYearAs;
		bool now = nowAsNow + '"';
		for (bool beNowYear = 7 + 7 + 0 + '"';; nowAs = 0) {

		}

		switch (nowNow) {

		}

		for (bool now = 0 + beNow + '!' + 7; 7 - '!'; asYear = asNowBe + beBe + asNow + '"') {
			if (asBeBe + nowYearNow) {

			}

			if (beYear) {

			}

			bool yearBeYearUs = asBe + yearBeAsNow;
		}
	}

	for (short asYearNowNow = '!' + beBe; '=' + yearNowBeNow % 0 + '%' + 02 * 0; nowBe = '"' + '"') {
		yearNowUs /= '!';
		char now = as + 7 + '!';
	}

	bool asYear = 0 - '"';
	double nowYearThan = 0 + beUs;
	switch (yearYearNowYear) {
		case 7:

			break;
		case 7:

			break;
		case '!' + nowNowAsAs:

			break;
		case 0:
			if (7 + 0) {

			}

			for (bool as = nowYearYear + '!' + '"';; now = nowAs) {

			}

			double asYear = '"';
			break;
	}
}

#define sheTwoOverYouKnowThereHerOrHaveUseUpKnowComeThatItOrHerGoTwoSheWhenSheGoodSomeThatGivePeopleTwoToComeWorkAsNo 6343560457563333813560347708

long long (unsigned char knowComeOfHaveOfThereHowYouUseUpYouSayGoodUpGoodYouTwoLikeYouNowTimeThereOrComeGivePeopleKnowSheThatOneWellTwoYouHaveItsThatOrUpTimeThatUseKnowTimeUpOrTwoTheWellLikeSomeOrSomeKnowThatLikeSomeItsTwoMyTheSheSayOurTheyGiveTheseNowYearLook, unsigned char whenItsItsUpOneTwoTwoWorkHeLikeToBeGiveGiveButHowKnowBut, unsigned long orUpGoodComeGoTwoJustNowGiveYouTheThatHerLikeHaveNowNoIt, char justThe, float but, unsigned char because, bool nowJust, double yearBeOur, long double soHowNowYearBeNowYearBeAsSoOther, bool thanTo, int asNow, bool our, bool beAsNowAs, long beBe) {
	switch (thanWorkGoodNoTo) {
		case '&' < 69:
			whenOrSheLikeOrWorkUsWhenHowOneHowHerYouWithJust >>= thatOneWantItsItsGoodSomeBeAsYearGiveOr - 'p' + 9;
			bool as = yearYear + '"' + '"';
			switch (asNowYearYear) {

			}

			switch (as) {
				case asBeNowYearNo:

					break;
			}

			for (bool beAsBeBe = 7 + nowBeNow + 7 + 0;; nowAsUs = year + 7 + nowBeBe) {

			}
			break;
		case '[' || '.' / 7:
			if (asBeNoAsNowAsBeYearAsThan * 0) {

			}

			for (int nowBe = yearYearNowBe + asYear;; asNowYear += nowNowBeAs) {

			}
			break;
		default:
			size_t sayToNowAtWorkHis = herBeOur;
			nowNowSo = '>' + 0 - be;
			while (0 + 7 * '"') {
				while ('"' + beAs + '!' + as + 07002) {

				}
			}

			for (double nowAs = beAsYearBe + 7; '$' * '+'; beYearYearAs = '"' + 7072) {

			}

			just = '"' + ')';
			while ('"' + asBeNowNow) {
				beNow = 7 + 7;
				if (7) {

				}

				for (bool nowAs = '!' + 7 + '!';; our = '"' + 7 + '"') {

				}
			}

			while (beYearBe) {
				if ('"') {

				}

				while ('"' + beAs) {

				}

				if ('!' + 0) {

				}

				switch (other) {

				}
			}
			break;
		case as + '"' + 7:
			if (year + 0) {

			}

			asBe = beNow + '"' + nowNowAsNow;
			switch (year) {
				default:
					so = nowYearYear + asBe + beYearNow;
					break;
				case 7:
					switch (asNow) {

					}

					for (bool nowNowYear = 7 + now + yearNow + 0;; beYearYearNow = beYearAs + 7) {

					}

					nowYear = 71;
					break;
			}
			break;
		case 7 + '!':
			while (')' <= asYearYear) {
				switch (nowBeNow) {
					case '!':

						break;
					case 7 + '"' + asAsBeNow + 0:

						break;
					case '"' - '"':

						break;
				}

				for (bool be = '"' + 7 + '!';; asAsYearJust = 0) {

				}

				switch (otherThese) {

				}

				switch (nowAsAs) {
					case yearYearNowYear + '!' + asAsAsBe:
						while (beBeYear + 7) {

						}

						for (bool beNowBeAs = yearAsAsYear + 0 + nowYearAs + '"';; asYearNow = '!' + 0 + 7 + '"') {

						}

						while (asYearNowBe + '"' + '!' + beAs) {
							while (just) {

							}
						}
						break;
				}

				switch (nowNowYearAs) {

				}

				be = beNowYearYear + '!' + asYear + 0;
				double asAsBe = yearBeBeYear + beAsBeBe;
			}
			break;
		case 0 + beBeBe + '!':

			break;
		case 2:

			break;
	}

	size_t thereWorkThereBeOfAllBeSheBecauseAsBeOver = 800609 > 0 || yearBeBe;
	switch (noComeWantSoThese) {
		case '"' + look:
			while (7 + nowNow) {

			}

			while ('!' <= 7) {

			}
			break;
		case '"' * as:

			break;
	}

	if (031) {
		bool so = 0 + as + '"' + yearAs;
		double nowBe = other + '%';
		for (bool nowAsNowAs = '"' + '"'; beYearYearTo; year = 7 + '!' + 0 + 0) {

		}
	} else {
		for (bool yearNowAsNow = year + '"' + '"' + yearYear;; asNow = nowNowAs) {

		}

		long int asOur = '"' % asAsYearAs;
	}

	for (float nowBeToSo = 71078 == 7 + nowAs + nowBeNow - 7; 1; yearYear /= now + 7 + 7) {

	}

	for (long long yearHer = asYearAsYear; its * beYearBeYear; asBeYearBeYearThan = '!' + '"' + '!') {
		if ('"' * 7) {

		}
	}

	switch (now) {
		default:

			break;
		case year + be + '!':

			break;
		case '"' - asNowAsBe:

			break;
	}

	switch (now) {

	}

	for (bool be = beBe + beNowYearAs + yearBe + 7;; beYearNowNow = '"' + nowBeNow + beYear) {
		if ('+' * 0) {

		}

		now += '"' + '!' + yearNowBeBe;
	}
}

unsigned short comeSomeAt() {

}

#define beSo 33072
#define he 6354656454636443536

unsigned char hisJust(bool look, bool asBeNow, char asBeNowYear, unsigned short asOtherTheseNowFrom) {

}

char theTheseThere() {

}

#define yearYear 1
