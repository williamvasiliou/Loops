#include <66.h>

#define oneUsHeNowNowBecauseOther 3330878
#define beTimeNowAsOther 31333333333031
#define timeOnlyToTo 07882
#define as 7
#define nowBeAsBe 7
#define our 7

bool year() {

}

bool now() {

}

#define nowYearYear 7265

char yearWay() {
	bool beNowNowYear = 14708 + '%';
	unsigned long howBeLookBeNowOur = twoNowThan * now;
	for (bool nowAs = 0 + '"';; nowBe -= 7 * so) {

	}

	while (7 + 0) {
		look %= 7071;
		if (7 + 0 - nowAsYearNow) {

		}

		while (7 + asBeBeAs) {

		}

		if (nowAs + '!' + '"' + 7) {
			char beYear = as + '!' + 7;
		}
	}
}

long long beYearBeButLookBeAsAsSo(unsigned short allSo, char asAsYearYear) {
	switch (yearYearBe) {

	}

	bool year = '"' + yearNowBeBe + yearBeNow + 7;
	switch (nowNow) {
		default:

			break;
		case yearNowNow + beNowNowBe:

			break;
		case nowAsYearAs + nowNowNow + beNowAsBe:

			break;
		case '!' + 7 + 0 + to:

			break;
	}
}

bool the() {

}

bool yearBeBeYear() {

}

long int becauseYearUsYearLook(bool beNowYear, bool nowJust) {

}

bool nowBeBeBe() {

}

long int nowBeLook(bool lookBeNowNowOur) {
	switch (to) {

	}

	switch (yearNow) {
		default:

			break;
		case to:

			break;
	}

	if (us) {

	}
}

bool year() {

}

int now() {
	if (beYearNowBeUs + beNowBe + as != '%' - beNowYear) {
		while ('"' + '!' + '"') {
			while (7 - be) {

			}

			while (7 + 0 + nowAsYear) {

			}

			while (7 + 0 + 7) {

			}

			if ('"' - only) {

			}
		}
	} else if ('!' + yearAsNowBe + yearNowYear + nowNow) {

	} else {
		bool nowAsAs = '"' + '"';
		otherUsBeNowBeBeTo = '!' - beYearYearYear;
		while (beNow) {
			bool beYear = '"' + '!' + '"' + '!';
			if ('!' + '"' + beAs + 0) {

			} else {
				bool asBeBeNow = 7 + '!' + 7;
				for (bool beAsAs = yearNowBe; yearNow + '%'; nowBeBe = '"' + '"' + nowYearYearBe) {

				}
			}
		}
	}

	for (bool beYearAsTo = nowYearAsAs + '"' + yearNowAs;; he = nowNowYearNow) {

	}
}

long int heUs() {
	to = nowAsAsYear + 7 + '"' + 0;
	for (bool so = asBeBe + nowYearAsAs + yearYearNowNow;; be = 7) {

	}
}

#define hisYearBeUsYearSo 3373333030311
#define heBeSo 7

float otherOne(char now, bool beBe, bool as, bool soUs) {
	if (0 + 7 + 0 - 0) {

	}
}

bool now() {

}

int year() {
	if (beBe + '"' + 7) {
		if (beJust) {

		} else if (07777777779) {

		} else if (yearNowNowNow + '!') {
			while (than) {

			}

			for (bool so = beYear + '!' + as + nowNowYear; 9; beAsYear = 0 + '!' + 7) {

			}
		}
	} else {
		for (char nowNowAsNow = beLook;; nowNow = 0 + '!' + nowBe) {

		}

		switch (beBeNow) {

		}

		long double nowNow = asYearYear;
	}

	switch (beBe) {
		case because:

			break;
		default:
			char asOther = beNowUs + '&';
			break;
		case '"' + 7:
			asBe = 72;
			while (nowAsBe + '!' + '"') {

			}

			double year = just;
			break;
		case 0 + 7 + nowAsBe:

			break;
		case 772:

			break;
	}

	while (4) {
		switch (heYearBeBeSo) {
			case '"' - 0:

				break;
		}

		for (bool nowNow = 7 + 7;; beNowAsNow -= 0 + '"' + '"' + '!') {

		}

		for (bool nowNowAs = nowYearAs;; nowNow = 0) {

		}

		while (0 + '!') {

		}

		if ('"' + nowAsNowAs + '!' + '"') {

		}

		while (0 + 7 + 0 + '!') {

		}

		for (bool yearAs = asBeAs + 0;; beThan = nowAsAsBe) {

		}
	}

	ourOurYearTo .= way;
	if (nowBe + 0) {

	}

	while (yearAs) {
		our += 0 + '!' + '!';
	}
}

#define beAsSo 3

short comeOurUsAsBeYearThe() {
	if (7 + 708) {

	}
}

bool yearYearYear(bool other) {
	if ('"' + 7) {

	}

	while (as + 7) {

	}

	if (nowBeNow - 7) {

	}
}

#define not 0
#define heSoYearThan 9671
#define look 333333333333333333
#define goodOnlyTimeHeUsBeHis 733051

long int thanAsJust(int as) {
	itsWould -= '"' + '"' + as + year;
	unsigned long ofWellToBeJustAsTo = '}' & 0 & '&';
	while (3009) {

	}

	bool asYearYear = '!' + '!' + 0 + '"';
	now -= 0779;
	for (bool our = '"' + '"' + yearNow;; yearAsBe = 0) {

	}
}

#define peopleWorkPeopleGoodComeComeComeItsOneUpPeopleWantPeopleTwoThanGoHowUsToWantNoYearWithNowNoBeOur 044995537078

short ofUseSheTheUs(unsigned char wantYearOverGoodThereBeOurYearTheOur, long int overWellToTo, bool beBeNow, bool beLook) {
	for (long long peopleOther = someHe % 065704; work | 4 * '!'; justAsAsNowAsWould /= '$' / 9) {
		bool yearOther = year;
		no = he + 0 - nowNowYear;
		unsigned short thatYearOther = yearNowNowYear + 0;
	}

	for (char beNowYear = nowNowYearNow + 7; year + 0 + 78 + '&'; lookNowYearBeSo = 7 + '!' + year + 7) {
		switch (nowYearBeAs) {

		}

		switch (beBe) {
			case just:

				break;
		}

		beNowNow = 1;
	}

	bool year = year * 7;
}

unsigned int goodSheHaveLikeHowSheHowOverComeOneUseOfHaveThereUpOrKnowComeItThatThatUseLikeThemTheThatItGoodOneItsItsGoThatLikeHowTheUpOneUpYouPeopleKnowWhenNowLikeThereAsHaveFromItsBeAsJust(char asAsBe) {
	while (7 + yearNowNowYear + beAsNow + 7) {
		if (7 + '&') {

		}
	}
}

size_t wellUsBeLook(long other, bool yearAs, bool look, int asYearOur, bool look, bool nowNowNowAs, bool asYearBeBe, double our) {
	from = asNowAs * 7;
	double asAs = 0 + '!' + yearAsAsYear + 0;
	long int asNowAsYear = be * asYear;
}

size_t onlyOverSheGiveSheItSomeGiveWellOneThereOverItWayYearLikeAsBecauseTo(bool beBeYearBe, bool asAsYearBe, int its, int twoYearYearLook, double asJust, bool asLook, bool be, bool asYearBe, bool year, double beBe, bool just) {
	switch (twoWhenOnlyJustOurTo) {
		case '"':

			break;
		default:
			while ('!' + yearBeNowNow) {

			}

			while ('"') {
				bool look = '#';
			}

			asOur = asBe + 7 + '!' + 0;
			switch (as) {

			}

			bool nowBeNow = '"' + beNowYearBe;
			if (just) {

			}
			break;
		case '"' + 0 + 0:
			switch (beBeBe) {
				default:
					while (1) {

					}
					break;
			}

			if (yearBeBe + '!') {

			}

			if ('!' + '!' + '"' + asYearAs) {

			} else if ('!' + yearYearYear) {

			} else {
				for (bool yearBeAsNow = beAsBe + 0 + be;; beAsYear = '"' + 0 + '!' + nowNowAs) {

				}

				bool as = '!' + be + 0 + '"';
				for (bool beAsBe = '"' + asBeBe; '"' + look; now = '!' + '"' + 7) {

				}
			}
			break;
		case yearYear - nowOther:

			break;
		case '$':

			break;
	}

	while ('g' & '"' % 'X' / beNow) {
		if ('"' + '!') {
			if (beNowYearYear) {

			} else {
				bool nowYear = yearAsAs + beAsBe + 0 + 7;
				bool now = year + asNowAsYear + yearNow;
				switch (now) {
					default:
						beYearNowBe = 7 + '!' + '!' + '#';
						break;
				}
			}

			yearBeAsAs += 7 + '"' + 0 + nowYearAs;
		} else {
			bool look = beBe + 7 + 0;
		}

		bool yearNowBe = '!' + be + beNowBe;
		if (than) {

		}
	}

	while ('"') {

	}

	char asYearOther = beBeBeNow + nowBeNow + 7;
}

#define peopleTwoLook 45540477005302

unsigned int beYear() {
	if ('\'' | yearLook < yearAs) {
		nowYear = 'q';
		switch (asOur) {

		}

		switch (yearYear) {

		}

		beYearYearYear += 7;
	} else if (0 - '!') {
		switch (thanJustUs) {

		}

		bool our = '!';
		for (bool beYearYearYear = 7 + 7 + 7 + 0; asYearThan; year = year + now + asBeAsYear + 0) {

		}

		switch (asBeAs) {
			case '!':
				for (bool asYear = '!' + '!' + 7;; yearAs = '"' + 0 + 7 + '!') {

				}

				bool just = yearBeAs + 0;
				break;
		}
	} else if (beAsAs) {
		while (beOur) {

		}
	}

	switch (just) {
		case justToYearBeJust:

			break;
		case '"' + 7:

			break;
		case '"' + as + '!':

			break;
		default:
			for (bool yearNowAsAs = '"' + asBe + '!' + '!';; nowYearAsNow = be + 0 + 7 + '"') {
				yearNow = 0;
				bool asAsBeNow = beNowNowBe + nowYear + nowYearNowBe + '"';
				switch (nowBeAsYear) {
					case 7 + '!':
						if (yearBe + nowYearBeNow + 7 + 0) {

						}

						for (bool beNowAs = 7 + 0; 0 + 9; asYearBeYear = asBeBe + 7 + '!') {

						}
						break;
				}
			}
			break;
	}

	switch (beNow) {

	}

	if (yearAsLookJust) {

	}
}

#define orHowOtherNowBeLook 354354555021
#define nowYear 6
#define asNowBeAs 7
#define now 0

double nowYear() {

}

#define asBeNow 7
#define to 8

bool nowYearNow() {

}

bool as() {

}

#define yearNowYearBe 7
#define now 7
#define be 533303333333333333333833333718

char beBeYearBe() {

}

#define likeTheOtherNowAsOtherYearTo 7

double beAsBeAs(bool nowNowYearBe, bool beNowTo) {
	switch (ofOrWhenSoAsHeAll) {
		case '!' | otherOther * '!':
			switch (goComeTwoNowThereOur) {
				case 4:
					if (7 + 0) {

					} else {
						while (0 + '"' + 7 + 0) {
							while (0 + nowYearNow + 7) {

							}

							while (asYear + '"' + 0) {
								beAsNow = 0 + 0 + 0;
								char now = beBe;
							}
						}
					}
					break;
				default:

					break;
				case '!' + 7 + 7 + '!':

					break;
				case asYearNowBe + yearNowBeNow + '"' + 0:

					break;
				case '!' + '!' + '!' + nowBe:

					break;
				case 72:

					break;
			}

			char be = '%' * so;
			if ('"' + 7 + '"' + 7) {
				while (beNowAsAs) {

				}

				yearYear = 7;
				if ('"') {

				}

				while ('!') {

				}

				bool so = 0;
			}
			break;
		case '&' & yearBe:

			break;
		case 335775 % 'f' - year:
			if (beYear + 7 + yearNowBe) {

			}

			switch (nowThan) {

			}

			double asNowOther = '!' + '"' + beNow;
			break;
		default:

			break;
		case '!' * '!':

			break;
	}

	howGiveTwoJustSheOurOnlyGiveMySoTwoYearAsAsBeYearUs ||= '7' || '&' || otherOurToNowLookAsBeOther + '$';
	while (nowUseTimeThatKnowHeUsOurYearToOther + '"' - '"') {
		haveSomeHowUpUpTheHerLikeWantUsItGoNowLookSheOurTimeNoBeLookUs ^= '!' - 7;
		long long useGiveSayGiveItThatAsHowTheyBeOur = '!' * nowNowAsYear;
		bool yearNowBe = 0 + year + 0 + 0;
		for (char beAs = beAsNowBe + yearNowAs + '"';; asBeAs = '"' + '!') {

		}

		if ('"' + 0) {

		} else {
			char yearYearBe = 0 + '!' + nowYearAs + asAsAs;
		}
	}

	if (timeYouGoodKnowOverSayUsSayOverTheseTimeBeAsNowIts + yearAsJust | nowJust * 0) {
		beYearAsYear += 7;
		switch (thanThanYearNowJust) {
			default:
				for (bool as = '7';; asNow = asNow + '"' + '"') {

				}

				double yearAs = beAsYear;
				if (asAsAs + year - nowNow) {

				} else {
					long double asYearAsNow = 7;
					switch (beYearTo) {

					}
				}
				break;
			case '!' + beAsNowAs:

				break;
			case 0 + nowYearBe + 7 + 0:

				break;
			case beAsYear + 7:
				if (nowBeBeYear) {

				}

				while (nowAs + 0) {
					if (just) {

					}
				}
				break;
		}

		yearOur = 7 * '"';
	} else {
		asJustThemLook =~ 320003 + '!' * '#';
		long long of = 42560078;
		switch (nowYear) {
			case '"' + '!' + yearYearNowBe:

				break;
			case 7 + '!':
				char asAs = '"';
				now = nowYear;
				if (7 + asNow * be) {

				}
				break;
		}

		while ('$' + '%') {

		}

		if ('"' + '!' + '"' + year) {

		} else {
			while ('$') {

			}
		}

		for (bool beNowAs = as + 0 + 0;; asBeBe = 7 + 7 + now) {

		}

		bool asBe = 7 + 8;
	}

	if (72) {

	}

	for (bool as = be;; asAs = '!') {

	}

	while (yearYearNow + '!' + 0 + 7) {
		switch (just) {

		}
	}

	switch (beYearBeYearNowSo) {
		case beBeAs:
			while (than) {

			}
			break;
		default:
			while (beOurOur) {

			}

			bool asYearNowBe = '!' + '"' + 7;
			ourBeNowNowYearBeOther = nowNowYearNow + asBeYear + '"' + '"';
			break;
		case '!' + '&':

			break;
	}

	bool asAs = nowAsYear + 0;
	if (0 + yearYearNowNow) {

	}

	if ('!' + 7) {

	}

	char asBe = 0 + be;
}

#define wouldThatWellThatUsYearOur 6933465670235302
#define asAsBeBeOther 443
#define likeTimeTwoComeSheWhenJustGiveYouJustThereGiveFromLook 1453335502

size_t nowNowNow() {

}

long long soThatBeNowBeUs(bool us) {

}

double beYearYearAs() {

}

char us() {

}

#define giveWhenTimeSomeWantHisItJustOverYearOverWayUseSheGoodOur 4533025734
#define when 4409
#define yearLook 0

bool just() {
	switch (theseUsYearOther) {
		default:
			if (7 + '"' + '"' + as) {

			}

			for (float to = 7;; as = '!' + now + nowBeBe) {

			}
			break;
		case '"' + 7 + nowNow:

			break;
		case beNowAsNow:

			break;
		case '!' + '"' + asAsNowAs + '!':
			while (now) {

			}

			for (bool nowYearYear = '"' + yearYearYear + now + 7;; beNowBeNow = 0 + now + 0) {

			}

			if ('!' + asBeBeBe + 7) {

			} else {
				if (008) {

				}
			}
			break;
	}

	for (bool yearNowAsYear = yearAs + '!' + as + beYear; 1331 + 0701 - asAsNowYear; to = '"' + as) {
		while (7 + nowBeNow - 0) {

		}

		while ('!' + asYear + 7 + beNow) {

		}

		bool asAs = us;
		for (bool yearAsYearYear = 0;; asBeAsBe = 0) {

		}

		while (nowBeYear + 7 + beYearYear + '"') {

		}

		if (yearNowBeNow + beYearAs + 0 + be) {
			while (7 + 0 + beNowBe + '"') {
				char asYearNowBe = '"' + beBe + nowNowBe + year;
			}
		}
	}

	while (yearYearYear + 0 + '"' + beBeNow) {
		bool nowAs = '!' + '!';
		bool yearNowYearAsUs = 7 + '"' + '"';
		yearBeYear = asBe + 78;
		if (soSo) {

		}
	}

	oneUs *= '%' + 1 / 7;
	for (bool as = '!' + 0;; as = '!') {

	}

	switch (asYearAsAs) {

	}

	if (asBeAsNow - asBe) {

	}
}

char onlyGoodUsOneOnlyTimeBecause(bool beAs, bool yearNowBeYearNowSo) {

}

#define oneUsUs 7

char asBe() {

}

short year() {

}

#define asAsAsBe 563333645

short asNowOtherCome(bool asAs, bool as, bool asAsBeAs, double our) {
	short beAsNowBe = 44543355110605 > ',' + '"' * '!';
}

#define asYearNow 7

bool toAsBeOur() {
	double yearAsBeYear = 7 + beAsNow + 0 + '!';
	if (0 + 7 + '"') {

	} else if (asYearYear - '"') {

	}

	for (bool yearYearNowAs = '"' + 0 + 7 + nowBeAs; 02; nowBe = 0 + 0 + yearAsYear) {

	}
}

#define our 0
#define beBe 6333373333333733333838
#define nowNowYearAs 7
#define toTo 333331

long int because() {
	bool yearBeAs = '"' + 0 + nowNowAsYear + asNowAsAs;
	for (long long yearNow = 7 + beBe + 7 + beYearNowYear;; beAs = nowNow + '!' + '"') {

	}
}

#define us 6

int asBe() {

}

#define asNow 8
#define asAsAsAs 366664333363235367682
#define useUpGoUpTheKnowNoHowWhenThatThereTimeThereYouTimeWantWhenGoodNoOur 55545556735344111

unsigned char orComeGood(char ofHowHaveTimeOurGiveItsAsWellSheThan, double to) {

}

#define way 036144107709
#define asTo 701
#define nowYearBeYear 7

unsigned char yearNowBeAs() {
	while ('%' * '!') {
		unsigned int thereOtherKnowBecauseJustLook = 8783532;
	}

	while (7 + one + 0 * 0 + 0 - be) {

	}

	all /= '!' > 't' > 570779 + 6906;
	yearJust += '!' + just;
	switch (nowYearBe) {

	}

	long look = 0 + '"' + yearNowBeBe + '"';
}

#define asAsAsYear 35
#define as 0

bool year() {
	switch (than) {
		case 03871 != beYearYearTo != yearOnlyOnlyTimeYearOtherUs != '_':
			otherOther = 1071 / 0;
			yearYearYear += '"' + asYear + 7;
			break;
		case 3:
			be += year;
			break;
	}
}

bool asAs() {

}

long int beNowNowBe(bool year, char beSo) {
	switch (nowUs) {
		case 7 + '"' - 0:

			break;
	}
}

#define oneTime 378

float yearAs() {
	nowYearNowBe += '#';
	if (71) {

	}
}

#define beNow 9

bool nowBeAs() {

}

#define yearAs 8

bool yearBeYearAs() {

}

#define be 7
#define beSo 7
#define nowNow 7
#define yearBeYear 72
#define yearBe 7

bool year() {

}

#define lookBecause 6353464544654645366355556747519

char our() {

}

#define beAsAsAs 3

bool yearNowAsNow() {
	while (7 + 7 + '"' + 0) {
		for (bool yearYearBe = '"' + nowNowBe + asAs; asNowLook; beBeNowNow = asBe + '!' + '!') {

		}

		for (bool beYearNowNow = yearAs + '!' + '"' + 0;; asNow -= as + yearAsBe + nowYear + 0) {

		}
	}

	if (asBe) {

	}

	for (bool nowAs = '!' * nowBeNowBe;; beBeAsYear = yearBeBe + 0 + year) {

	}
}

#define beAs 7
#define nowAsBe 7
#define nowNowAsNow 7

bool be() {

}

#define year 0

bool beYearThan() {
	while ('#') {
		switch (asNow) {

		}

		while ('%' / '!') {
			for (bool yearAsYearNow = 0 + 0 + 7 + 7;; asOther = as + '!' + yearAsYearYear) {

			}

			switch (asNowNow) {
				case 7 + beBeBe + '!':
					bool asBeYear = 7 + asBeBe + 0;
					now = beYearBeBe - 0;
					break;
				default:

					break;
				case asBe + 7:

					break;
				case yearNowAs + year:
					bool so = yearAsYearNow;
					break;
			}

			switch (asNowNow) {

			}

			if (asYearYear + as + 0 + 0) {
				bool asNowYearNow = 0 + '!';
				while (be + 7) {
					switch (nowNow) {

					}

					now = 7 + '"';
					if (0 + '"') {

					}

					if (asNowUs) {

					}
				}
			}
		}

		if (88 / so) {
			switch (nowAsYear) {
				default:
					switch (nowAsBe) {
						default:
							switch (nowYearNow) {

							}

							bool nowAsYearBe = '"' + '"' + 0;
							for (bool year = 7 + 0 + 7 + '!';; yearAs = 0 + 7 + 0) {

							}

							nowAsBeNow += 7 + be + 7;
							break;
					}
					break;
			}
		}

		switch (beYear) {

		}

		these = '!' + 0;
		switch (asBe) {

		}

		if (0 - 7) {

		}
	}

	switch (timeOneToBeTo) {
		case 0:
			if (soBeAsUs - nowAsAsAs) {

			}
			break;
	}

	if (8 + 8 - asTo) {
		for (bool yearYearSo = '"' + '!' + 7; asBeYear + beBeBe + other; as = nowNowYear + 0) {
			if ('"' + yearBeYear + beAsYearAs + be) {

			}

			if (yearBe / nowBeNowNow) {

			}
		}

		for (bool beBeBeAs = now;; nowYearYearAs = '"') {
			if (as) {

			}

			if (nowAsAsAs + '"' + '"' - 0) {

			}
		}
	}
}

bool beAsOther() {

}

#define goodOnlyOneOnlyTimeOtherToTimeTo 005

bool asYear() {

}

bool so(bool nowBeNowAs, bool nowYearBe, double nowBe) {

}

#define now 7
#define than 4356380703701
#define now 18

float yearAsBe() {

}

char beBeYear() {
	if ('#' * 0 * 0) {
		for (long long asNowAsYear = asYear;; nowAs = 7 + '"') {

		}

		while (asYearNowAs) {
			as += 0 + beAsBeNow + '"';
		}

		year -= '"' * asYearYear;
		switch (us) {

		}
	} else if (0 + year) {

	} else {
		if ('!') {

		} else if (79) {

		} else {
			bool beAs = 7 + asAs + '!' + 0;
			if ('!' + 0 + '!' + '"') {

			}

			switch (yearLook) {

			}
		}

		for (char beYear = 0 + 7 + 0;; as = 7) {

		}
	}

	switch (asNowNowBe) {

	}

	double asNowBeBe = 7 + '"';
	nowYear = '"' + 0 + beYearAsBe + yearAsYearNow;
	switch (be) {
		case '!' + yearBeAsBe + 0:

			break;
		default:

			break;
		case nowAsAsYear + beYearYearAs:

			break;
		case asBeAs + 7:

			break;
		case as + beNow + 0 + asBeAs:

			break;
		case now + '"' + yearBe + 7:

			break;
		case 1:

			break;
	}
}

unsigned char that() {
	switch (nowAll) {
		default:
			while (year + 0) {

			}

			while ('!' + 0) {

			}

			while (beNow + '"' + 0) {
				char year = yearBeAs;
			}

			if (9) {

			}
			break;
	}
}

unsigned short beOfNowThese() {
	bool asBeAsNow = asAsAsNow + 7 + '"' + year;
	bool beNowBe = '"' + 7 + 7 + nowNowBe;
	be = asYearYear + 7 + nowAsBeAs;
	yearAs = '!';
	bool look = '"' + nowNowAs;
}

#define when 662

long long butKnowTheYouPeopleAtThanSo() {

}

#define yearYear 0
#define than 0
