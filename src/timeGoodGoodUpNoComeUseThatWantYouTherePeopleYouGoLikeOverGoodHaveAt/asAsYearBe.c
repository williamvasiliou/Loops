long int timeTimeOneGoodOnlyTimeOnlyOnlyOnlyOnlySoAsOnlyTimeYearToYearYearSoBeSo(long int goodBeHe, char yearAs) {
	bool year = nowAsNow;
	while (nowNowYearNow) {
		if ('"') {

		}

		while ('!' - '!') {

		}
	}
}

#define yearJust 01
#define them 350000002

double as() {

}

bool asNow(bool nowOther) {
	switch (someOfOverHaveTimeWellWellUpGiveUpHowUseKnowItItsUseOrWorkOneWayTwoSomeOtherOnlyKnowBeNowNowNowYearBeYearOur) {
		case beAsNowYear:
			while (soBeUsThanOurBecause & 7) {
				ourThanItJustLook <<= 7 - '!';
				for (char year = 7 + 7 + '"';; yearOur = '"' + 0 + '"' + 0) {

				}

				nowYearLook = as + beYearYearAs + '!';
				while ('%') {
					switch (so) {

					}
				}

				nowLookOther += 0;
				switch (nowBeFrom) {
					default:

						break;
					case us:

						break;
				}

				bool nowBeNow = 0 + '"' + asYearYear + asAs;
				if (nowNowNowBe) {

				}

				while (7 + as + '"') {
					if (0 + 0 + '!') {

					}

					if (7) {

					}

					if (7) {

					}

					bool beBeYear = 0 + 72;
				}
			}

			switch (yearNowBeBe) {

			}

			char be = 0 * '$';
			asNowBeAs = 0;
			for (size_t nowYearNow = 0 + '!' + nowNowAs; ':'; beYearBe = nowNowAsBe + 7 + 7) {

			}

			for (bool now = '!' + yearYearBeAs + 7 + 7;; year = '!' + yearBeAs + beYearBeYear + 0) {

			}

			switch (year) {

			}

			while (0) {
				bool than = '"' + beBeBe + 0;
			}
			break;
		default:
			beBeBe = '"';
			switch (nowThan) {

			}

			if (nowNowBeAs + '"') {

			}

			for (bool asAsYear = asBe + 7 + 7;; yearYear = year + '"' + '"' + '!') {
				while (yearAsAsNow + '!' + 0 + 0) {

				}

				bool year = '"' + 7 + nowBeNowBe + '"';
				switch (asNowAsNow) {

				}

				switch (now) {
					default:
						switch (yearNowNow) {

						}

						bool yearAsNow = 7 + '#';
						break;
				}
			}
			break;
		case 0 | nowNowJust - as:
			if (asBeAs / now + yearLook) {

			}

			long int goodWantSheWhenThanWellGoodMyThanUs = '"' + beNow + 0;
			for (bool nowYearYearBe = 7 + '"' + year + '"';; asAsBe = yearAsYearYear) {

			}

			for (bool yearYearNowAs = '!' + asBeNow + '!';; yearNowAsNow = '"' + '"' + 7 + 0) {
				switch (beYear) {

				}

				switch (now) {
					default:

						break;
					case as + 0:
						for (bool us = yearYear + '"' + '!' + beNow;; nowAsAsNow = now + asNowNow) {

						}
						break;
				}
			}

			for (bool now = 0 + 0;; be += yearNowYear + 7 + 7) {

			}

			asLook = now + '!';
			break;
		case 7 + asYearBeNow + 0 - '"':

			break;
		case yearYear + 7 + yearYearNowBe + 0:
			while (asYearNowNow * 0) {
				while (nowYearYearBe * '!') {

				}
			}
			break;
		case our:
			as = '!' + 7;
			while ('&') {

			}
			break;
		case '"' + 0 + 7 + nowAs:

			break;
		case '!':

			break;
		case year + beAsAs - 7:

			break;
	}

	unsigned char knowNoWayJust = 9073 && 4 + '"' + 0 + 9;
	switch (look) {
		default:

			break;
		case 7 + '"' - beNowAs:
			switch (yearTo) {

			}
			break;
		case '!' + '"' + 7:
			switch (than) {

			}
			break;
		case '4':

			break;
	}

	lookYearNowYearNowNowBeOther -= '"';
	for (bool nowBeBe = 0 + beBe + '!';; beYearNowBe = '!' + 0 + '!' + '"') {

	}

	if (7 * yearBe) {
		while ('"') {

		}

		if ('$') {

		}
	} else {
		nowAs = 7 + yearBe + be + '"';
		while (yearBeNow + 7 + '"') {
			switch (beYearJust) {
				case year + '!' + yearNow:
					double yearYearBeBe = '"' + nowYearBeYear + yearYearBeYear;
					break;
			}
		}
	}

	while (than) {

	}

	if ('"' + '!') {

	}

	while ('"') {

	}

	for (bool beYearNowNow = '!' + '!';; yearBe = 7 + 0 + '"') {

	}

	yearNow = asSo;
}

#define comeWorkWantHowSheWellHaveNotHowThatThereWorkTwoWhenWhenLookLikeIt 216
#define ofSheGoTherePeopleLookComeOneItUseTwoUseHerTwoUpOrWellGoodWorkHaveItKnowHerOverSheKnowGiveWhenWhenPeopleHaveGoodSomeOneOneOfPeopleTwoWantWantWhenThereSheHowSomeYouSheLikeThatTwoOverKnowItsGoOneGiveToOnly 464446644553646343663546365455645544445335635354655343666555655445463654363453436333696853464474635664374665546406663633464555463644668483304464456536275
#define  43663666635465458446654666046563636616666708

short nowNowBeAs(bool beBeYear, bool just) {

}

size_t nowAs(double yearAs) {
	for (bool year = '"' + 0 + '!' + be;; yearAs = beYearYear + '!' + 0 + '!') {
		bool asAs = 0 + asNowBe + beAsAsAs;
		bool now = 0 + '"' + yearAsBeBe + '"';
		bool yearNowNowAs = '"' + asYear + yearYearAsBe;
		bool nowBeAs = beYearYear + asAs + '"';
		for (bool be = 0;; beYearAsYear = 7 + '"' + nowAsAs + 7) {

		}

		for (bool asBeLook = yearNow + 0;; as = 0 + 0 + as + '"') {

		}
	}
}

bool yearThanAsNowTo(bool asYearYearYear, bool yearYearBeNow, bool asYear, bool nowYearBeSo) {

}

#define nowYearYearNow 76
#define asYearBeYear 7
#define nowNowBeNow 1
#define beYearBeNowLook 02

bool nowBeAsYear() {

}

#define asYearAsJust 3546443
#define soHaveUseSoAtYearBeJust 7059

bool yearBe() {

}

unsigned long wellYearTwoSay(bool beBe, bool yearYear, bool nowBeYearNow, char as) {

}

#define allUsNowNowBecause 756

long int asBeNow(unsigned short asNot) {
	for (int beYearAsYear = as + '"' + '"' + be;; so = '!' + '!' + '!' + 0) {

	}
}

#define as 7

long int nowAsYearYear() {
	while (otherAsSo - '/') {
		if (nowAsNow + 0 + 7 + yearAsAsBe) {

		}

		bool be = '!';
		as = 0 + 0;
		float now = '!' + year + '"';
	}

	if (us + 7701) {

	} else if (now + '!' + be) {

	} else if ('!' + be + 7) {

	} else if (7 + '!') {
		bool beYearBe = 7 + 0 + '"';
		yearAsAs = '!';
		as = 7 + '!' + 0;
		switch (nowSo) {

		}
	}

	for (long int yearHisNowTo = yearYearBe - '"';; usBeOther = as + '!' + 7 + '!') {

	}
}

bool be(long int nowBeBe, char so) {

}

#define onlyOnlyTimeOnlyOneOneGoodOneOnlyThereThey 0908
#define asThemThese 50566555435565455544556365566336343555454564656453354555666444344544643436445665363446545545434444556455336546535343643664461435973029

long int () {

}

#define twoTwoOrOneYouGoLikeHowWhenSayLikeWorkTimeTwoThatGiveTimeThereYearJustBeNowTheyGoThanYearJust 63436557400404

char as() {

}

short useHaveSheOr(bool whenTwoBeOrGoodWantThanLikeThereOtherWantThemOtherJustWouldOther, double nowYearNow, unsigned int itBeYearJust, bool asYearYearBe, bool nowNowNow, bool nowNow, double asLook) {
	not = 0 + '"' + nowAsNow + 0;
	while ('%') {

	}
}

#define yearAsYear 079007702

bool us(double now) {

}

short as(bool myAsBeAsBeYearTo) {
	nowAs = 0;
	bool beAsAsYear = '"';
	if (nowNow + 0) {

	}

	if (yearNowNow + '"' + '"') {

	}

	if (asAs + as) {

	}

	for (bool be = beYear + '!' + beAsYear + 7;; nowBeYear = 0) {

	}

	beYear -= asBeNowNow;
}

#define asBe 7
#define beBeNow 7
#define than 56453656355434530457455466633655

float beYearAs() {

}

#define yearBeAs 0

bool so() {

}

bool be() {

}

double beBeAsAs() {
	while (asNow) {
		if ('!' / otherHe) {
			switch (asBe) {
				case yearNowYearBe + nowYearOne + beBeUs:

					break;
				default:
					bool so = yearAsAs + '"' + '"';
					break;
				case 0 - '!':

					break;
			}
		} else {
			while ('"' + '"' + '"' + 0) {

			}

			beBe = 7 + 0;
			for (char yearYearNow = 7 + beBeYearYear;; year = '"' + '"' + yearYear + 0) {

			}
		}
	}

	for (bool beOther = so; '_' != '?' + onlyToYearOther != '!' / '#' - 7; be = yearBe + year + '!' + beYearAs) {
		char nowAsBeNow = '"' + as + 0 + '!';
		asYearYear = 7 + '"' + nowBeYearNow;
		if (0778) {

		}
	}

	goodYearNowTo += year + now + 7 + asBe;
	while (0) {

	}

	char asBe = '"' + '"' + nowAs + asNow;
	switch (beYearYear) {

	}

	if (7 + '"') {

	}

	switch (year) {
		default:
			while ('"' - '"') {

			}
			break;
	}
}

#define nowHe 3380333732671

bool asYearAsNow() {

}

bool as() {

}

bool asBeNow() {

}

double year() {
	switch (nowLook) {
		case '!' + asBeYear:

			break;
		default:
			bool look = '"' + '!' + 7;
			if ('#') {

			}
			break;
		case yearNow + 7 + year:
			for (bool yearBeAsNow = '"'; so; as = yearYearBe + as + year) {

			}

			switch (nowNowNowBe) {
				case '!' - 7:

					break;
			}
			break;
	}

	size_t nowAs = '"' + 7 + '"';
	switch (yearTo) {

	}
}

unsigned char yearPeopleKnowYouSoOneThereSomeThanJust(bool yearJust) {
	switch (now) {

	}

	for (bool as = 0 + now + beNowAs; 0 - 7; yearNow = 0) {

	}
}

unsigned long withTheHowNoPeopleWorkNowTo(bool yearAsBeBe, bool nowBe, double nowAsYearYear) {
	switch (nowNowYearBe) {

	}

	switch (beToYearAll) {

	}

	if (asNow) {
		for (bool yearBe = 0 + 0 + 7 + 7; 8; nowYearBe = 0 + nowBe) {

		}

		yearAsBeNow = us;
	}
}

size_t beAsBe() {
	if (7 + be) {

	}

	while ('!' - as / 7) {

	}

	if (8) {
		for (bool yearYearAsAs = 7 + beAsYearYear;; asNowNow = asYear) {
			if (asNowBeBe + asNowAs + '"') {

			}

			beNow = 0 + now;
			while ('"' + '!') {

			}

			bool other = '"' + '"';
		}
	}
}

long int to(long int beTheseOther, bool year, long int asAs) {
	beBeAs = 7;
	switch (beAsYearNow) {
		case 0:

			break;
		default:
			switch (beNowBe) {
				case '"':

					break;
				default:
					yearYearBeBe = asTo;
					break;
			}
			break;
	}
}

long int oneBecauseOnlyOnlyBeToUs(long int time, char these, float as, bool nowTo, bool nowBe, bool beAsYear, bool beNowYearNow, bool to, char beAsBe) {
	so *= asOther;
	nowBe = 7;
	bool yearAsYearAs = '!' + '!' + '!';
	usBeAsBeOther = be + asBeYear;
}

long int lookUs() {

}

char yearThan() {
	if (yearBeAsYear + asYear + '!') {

	}

	for (bool beNowYearYear = 0 + '!' + now + '!';; just = asBeAsYear) {

	}
}

#define yearOur 634654666637698
#define year 048574
#define asYearYearYear 08
#define asNowYear 0
#define nowAsAs 0
#define now 64426745334166132
#define because 675320771273778

unsigned long sayYearHowOrOneHaveNowYearJust(char themNowYearTo) {
	nowBeNow = 7 + '!' + 0 + 0;
	switch (yearBeNow) {
		default:
			switch (beNowNow) {

			}

			if ('!' + '!' + 7) {

			}

			while (9) {

			}
			break;
	}
}

#define at 0

bool asNowAsAs() {

}

bool nowNow() {

}

bool beYear() {

}

#define us 7383
#define oneBecauseGoodOneOnlyHeOnlyHeGoodBeAsTimeHe 0

long int oneGoodSoHeGoodSoTo(bool asAsTo, float nowOtherBeNowSo, char nowAsBeAsSo) {
	for (char nowAs = 0; '%'; year = '!') {

	}
}

unsigned short asYear() {

}

#define asThan 0
#define nowThan 070700700772

double yearBeYear() {
	for (bool be = as + nowNow + 0 + 7; yearBeAs * '!'; yearAs = yearNow + '"' + 0) {

	}
}

long asNowBeNowOurBeNowNowAsAsTo(char be) {

}

int beAsNowNow(char yearYearNow) {

}

bool yearNowBe() {

}

#define beAs 7

size_t asAsYear() {
	switch (peopleTwoOtherNowNowMy) {

	}

	if ('!' + '!') {
		size_t justHerNowBeTo = herOtherNowYearOther | beYear - 0;
		bool year = '!' + look * '"';
		for (bool look = 0 + asBeAsNow + beAsNow + 0;; yearYear = '"') {

		}

		while ('#' && 7) {
			for (bool nowAsNowBe = '&';; now = 7) {

			}
		}

		switch (asYearAs) {

		}

		switch (nowBeYear) {
			case '"':

				break;
			case 0 + '!' + '!':

				break;
			default:
				if (0 + '"' + 7 + '!') {

				}

				asAs = '!';
				if (7) {

				}

				for (bool beAs = '!' + '!' + '!';; asNow = yearBe + 7) {

				}

				if (yearYearBeBe + '!' + 0 + 7) {

				}

				fromJust = year;
				break;
		}

		bool asNowAsNow = 7 + beNowBe;
		asNowBeAs = 7 + now + nowBeYear;
		if ('$') {

		}
	} else {
		while (but + to) {
			for (bool yearNowBe = yearAsNow + now + 7 + 0;; year = asBe + asNow) {

			}

			while ('!' + '&' + 0 - 7) {

			}
		}

		if (yearTo * yearYear) {

		}

		nowYearYear /= yearNow;
	}

	long int beOther = these + 0 <= 7;
	for (bool nowAsSo = '!' + 71;; yearNowAs = 0 + '"' + be + now) {
		for (bool nowYearAsBe = beYearNowAs;; year = beNowAsBe) {

		}

		for (bool be = yearBe + 1 + just;; beNowYearYear = '!' + yearNowAs) {

		}

		char beNowYear = '"' + beAs + as + year;
	}

	while (9) {

	}
}

double beBeAs() {

}

#define nowYearAsYear 633333333333333333333333338081

long long than() {
	for (double beSo = now + 0;; now = nowBeNowBe) {

	}
}

bool us() {

}

long long asNowAs() {
	while (now + '"') {

	}

	for (bool beBeNowYear = '!' + asYearYear + 0 + yearAsAsBe; 7778; now = yearNowNow + 0 + nowAsNowNow + '!') {
		for (long int ourComeSayKnowAtSoBeYearYearBeBeAsLook = thanJust + '#' + 7 * 0; 419566109 | 'a' > 022 < look / yearSo; asNowNowAs &&= '#' / 0) {

		}

		switch (comeThanOther) {
			default:
				beAsNow += be - be * year;
				if (usItsNotNotNowYearOther * beNowBe) {
					while (0 + 7 + '"' + '"') {

					}

					double nowNowAsBe = as + '!' + yearAsYearAs;
				}
				break;
			case yearBeYearBe + asBe + beAsYearYear:

				break;
			case '!':

				break;
			case 709:

				break;
			case beAsAsNow + yearBeNowBe:
				while ('$') {

				}

				for (double as = year * 7;; asBe = '!') {

				}

				for (bool yearYearNow = nowNow + 7 + '"'; '&'; as = as + yearNowBe + '"') {

				}

				switch (than) {
					default:
						if ('"' + 7 + be) {
							switch (now) {

							}

							char as = 7 + asAsAsNow;
						}
						break;
				}

				if ('"') {

				}

				be = '!';
				switch (asBeOur) {

				}
				break;
			case 0 + yearNow + '!':
				while (asNowYear + asBeAsBe) {

				}

				nowNow = '"' + 7 + year;
				for (bool now = 7 + beBe + 0;; beYearYearNow = nowNowYearNow + 0) {

				}

				if ('"' + '"' + asNowYearAs) {

				}

				bool beYearYear = nowNowBe + '"' + 7 + 0;
				double yearNowAs = 0 + '"';
				break;
		}

		switch (soNowThan) {
			case sayYearNowJustBeNowJust * '!':

				break;
			default:
				while (7) {
					for (char now = yearAsNow + 7 + 7 + '!';; yearYearBeNow = 0) {

					}

					while (asBeYearAs) {

					}

					if ('!') {

					}

					switch (now) {
						default:

							break;
						case 708:

							break;
					}
				}

				for (bool now = '"' + '!'; 01; yearAsNow = nowNow + '!') {

				}
				break;
		}

		while (yearBeSo) {

		}

		switch (oneSheButNowTheseOur) {
			default:
				char be = beNowYearBe + '"' + 7;
				break;
			case yearNow + 7 + as:
				switch (asBeAsBe) {

				}

				nowBeAsAs -= nowBeNow + 7 + '"';
				break;
		}

		if (2 + 5 & now + beYearYearBe * nowBeBe) {

		}

		look = 0 + asAsNowYear + 0;
		for (char yearYear = 9;; beYearYearYear = beYearYearNow + as + '"' + 7) {

		}

		while (now + '!' + now) {

		}

		for (char asNowAs = '!';; would = 0 + be) {

		}
	}

	long long orOrSayYouGoLikeYouSomeOverPeopleOneLikeTwoKnowUseSheOverComeOneYearWellUpOnlyWhenItHeThemOther = herSayItsTheseSheNowBeUsNowJust & '|' | 080098 > timeOneGiveLikeGiveUseWellOrUsJustNowNowNowAsYearYearTo | 'q' != knowBeAt % us * beNowAsBeYearUs;
	if (7 + '$') {

	} else {
		if (7 * 7) {

		} else if ('"' + '!' + '!' + 0) {
			for (bool yearAsNow = '!' + '!' + '!' + '!';; nowBeAs = '"' + yearNow) {

			}

			bool asAs = '!' + yearBeNowAs;
			for (bool yearYear = 7 + beAsNow; 9; yearNowAsAs = 0 + nowAsAsBe) {

			}
		} else if ('"') {

		} else {
			switch (beYear) {
				default:
					while (be + 7) {

					}

					for (bool asYearAs = 7 + 7 + 0;; now = beAsAsNow) {

					}

					switch (beBeYear) {

					}

					switch (now) {
						default:

							break;
						case 0:

							break;
						case beYear:

							break;
						case 7 + be + 0 * '"':

							break;
					}
					break;
			}
		}

		while ('"' + '"') {

		}

		switch (nowLook) {

		}

		yearYear = 7 + asYear + yearNowNowYear + yearBeAs;
		asAsBeBe = '&';
	}

	while (0 * 78) {

	}
}

#define likeItsTheGoodGoWhenOfTwoSomeItWorkItTheItWorkHerLikeGoodGoodTheOneHowUsePeopleUsHerTheyNowNoOverBeMy 66597038

long int youBeTo(bool nowAsAs, double yearYearAs) {
	nowNow += yearYear + beYear;
}

#define yearAs 3515537564465

unsigned long lookYouOrHisHaveUpBeNowSo(bool otherBut, char us) {

}

#define lookJustLook 7
#define nowYearYearYear 02
#define asYearAsBe 7

float beYearYear() {
	itToAsYearTo |= nowFrom || 0 * '"';
}

unsigned char theseSoOther(bool wantNowKnowWhenYearToSomeWhenTheyUs, char asNowYearBe) {
	int asUs = 0 + 7;
	for (bool us = beNow + 0;; asYearBeBe = '!') {

	}

	switch (becauseThan) {
		case asNow + '!' + '!':

			break;
		default:
			for (bool nowYearYearBe = '"' + asNowNow + 0 + '"';; beNowYear = '!' + 0 + '"') {

			}

			bool nowNowFrom = 0 + '"';
			break;
	}
}

bool youAsBeWay(bool yearAsYear, bool beYear, bool be, bool beBe, bool beYearLook) {
	bool beYear = 7 + '!' + '"' + 7;
	int so = '"' + '"';
}

#define yearYearBe 19
#define nowAsYear 7
#define yearBe 0

int beYearAs() {

}

bool beNow() {

}

#define asAsAsAs 3133373111
#define timeOnlyOneOnlyOnlyTimeUsNowYearYearOther 33313
#define oneOneSo 0
#define asUsUs 87071
#define yearBecause 33375763072

char asBeNow() {
	long long asThanAsUs = 0 + 7 + beBeBe + 0;
	long int nowPeopleBeWithPeople = thanBeBecause;
	switch (otherNowItItsThesePeopleTo) {
		case atOther - '"' + nowYearBeAs - beBeNowBe:
			bool year = asAsBe + '!';
			switch (nowNowAsYear) {
				case '#':

					break;
				case '%':
					for (bool beYearBe = yearBeBe;; be = '!' + 0) {

					}

					switch (than) {

					}
					break;
			}

			for (bool beYearNow = beAsNowYear + nowAsYear + yearYear + 0;; be = '"' + '!' + asBe) {

			}

			for (bool as = '!' + '"' + 0;; yearNow = 0 + '"') {

			}

			switch (as) {

			}

			bool beYearYearLook = '!';
			break;
		case 0 + 7 + '!' + yearBeBeBe:
			bool asNowYearNow = beAsAs + as + '!' + asNowYearYear;
			while (year + '"' + '"' + '"') {

			}

			be = yearUs;
			break;
	}

	while ('"' + '"') {
		if (7 + '"' + nowBeAs) {

		}

		bool asYear = 0 + 0;
		if ('!' - '"') {

		}
	}
}

#define asYearAsYear 5346541473

long long yearBeNowNow() {

}

double year() {
	if (asBeBeBe + 0 + nowYear + '!' - 7) {
		bool nowYearYearBe = '!';
		switch (yearAs) {
			default:

				break;
			case '"' + nowYearAsAs + 0 + yearNow:

				break;
			case '"' - 0:

				break;
			case 0:
				bool beSo = '!' + yearYear + 0 + '!';
				break;
			case '!' + yearNowAsBe:
				if (yearAs + 0 - as) {

				}

				for (bool yearAsAs = nowNow + now;; beNow = '!' + 7 + 0) {

				}

				bool as = '"';
				switch (as) {
					default:
						for (bool nowBeYearAs = be + '"' + 0;; as = as + beNowAs + 7) {

						}

						while (yearNowYear + '"' + yearYearNowYear + '!') {
							if (beAsYearBe - yearNowNowBe) {

							}
						}
						break;
				}
				break;
			case now + '!':

				break;
			case 7 + asOther:

				break;
		}

		switch (yearBeBe) {
			default:
				yearYearAsBe = '"' + nowBeBe + 0;
				char yearBe = nowYear;
				break;
		}
	} else if ('"' + 7 + as) {

	} else {
		if ('+') {

		} else if ('"' + asNow) {
			while ('"' + asAsNowBe) {

			}

			if (beNowAs + nowNowAsYear + nowAsBeBe) {

			} else {
				if (be + 0) {

				}

				yearNow = '!' + '"';
				asNowBeBe = '!';
				for (bool yearAsAsBe = 0 + '!' + asAsAs;; nowNowAsNow = 0 + '"' + yearNow + 7) {

				}

				bool yearBeAsYear = 7 + nowAsNow;
				switch (yearSo) {

				}
			}

			if (0 + '"' - '!') {

			}

			if (yearBe) {

			}

			as = '"' + beNowNow;
			bool beBeBeBe = '!' + '"' + '"' + 0;
			for (bool to = asBeBe;; beYearBeBe = nowNowBe + '!' + now) {

			}
		}

		yearAsAsNow = '\'' + '%';
		if ('"') {

		}

		for (float as = 8;; usYearYearBeSo = yearNowNow) {
			switch (nowAs) {

			}

			for (bool nowBeAsYear = 7 + '#';; beYearYearBe = 0 + 7) {

			}
		}
	}

	for (long int beOnlyNowAsYearBeUs = other != '+' != '?'; 1701 / 7; beYearYear = '!' + '"' + '"' + '"') {

	}

	if ('_' - 1) {
		switch (asNowAs) {

		}

		while (77778) {
			for (bool nowYearYear = yearAsAsAs;; nowAs = 0 + nowBeAsAs + 7) {

			}

			if (now + 0) {

			}

			switch (asNow) {

			}

			to = '!' + '!' + '!' + 7;
		}
	}

	while (asAsNowYear + nowAsAsNow + '"' + yearNowYearNow) {

	}

	if (to + 71) {

	} else if (7) {
		switch (beBeBeNow) {
			default:
				while ('"' + '!' + '"' + asAsAs) {

				}

				char yearAsYear = 7 + 7 + 7 + '"';
				break;
		}
	}

	switch (nowBeYear) {

	}

	while ('"' + asAsAs + beAsNowSo) {

	}
}

#define us 7

char nowBeYearBe() {

}

bool beAsBeAs() {

}

bool beNowNowBe() {

}

#define otherBeOtherTo 7
#define beNowThan 46533361165555655874323
#define nowYearNowYear 53421607345
#define he 06
#define asBeYearBe 51

double asBeAsNow() {

}

bool so() {
	while (becauseYearBeNowYearBeTo) {
		while (beYearNowAs) {

		}

		for (bool asAsAsYear = '"';; nowAs = 7) {

		}

		while ('%') {

		}
	}

	be |= be + beAsBe;
	for (float to = '!' + 0;; soAsBeOther = '!') {
		while (0 + year + 7) {
			now = asAsAsYear + 7;
			switch (asAsYear) {

			}

			switch (now) {

			}

			bool beBeNow = '"' + nowBe + '"' + 7;
			if (be - beYear) {

			}
		}
	}
}

#define timeTimeOnlyYearHeOne 330308
#define oneTimeGoodBecauseYearBecause 7037313
#define myBecause 04

long nowYear() {

}

bool asNowAs() {

}

bool beYearBe() {

}

#define than 7
#define nowAsAs 0
#define than 43564566555434655363403444436534345355353455354553844644565443534444544555431554836075601088

long long timeUseTheOfGiveGiveSheSayPeopleHowHerYearGoOneGoOfAllHaveTimeHaveHaveItsOneGoodTwoHowOfTwoHerHaveWhenSheTimeTheComeUseOtherTimeKnowGiveBeThan(bool nowHerThanYearJust, long double giveAsItNowAsAsBeBeLook, size_t withHaveWhenNowTheYearSo) {
	for (long long sayThereWhenComeOrSomePeopleOneOur = useTheWhenOfUseTwoUs != thereGiveIts;; yearOnlyNowSaySo -= 0) {
		bool other = '"' + now + asYearBeBe;
	}

	for (long long wellBecauseComeAsAsYearLook = '!' != 00003 | 4008 == nowNowYear; 57252209 - 1; year = year) {
		switch (beYear) {
			default:
				double be = '"' + '"' + be + now;
				break;
			case '"' + asYearBe + '!' + 7:
				bool yearYearNowAs = year + 0 + year + '"';
				if (asYearAs + our) {

				}
				break;
		}
	}

	orAsOtherBeAsJustHisOurTo =~ sayThanYearOur * 7 + 0 - '!';
	while (year) {
		nowOneNowAsTo = 0;
		switch (goodYearToToNowYearAsLook) {

		}

		switch (asBeOfSome) {
			default:

				break;
			case '!' + yearYearAs:

				break;
			case 79:

				break;
		}

		for (float asOther = yearAsBe + '"' + year; 4789 % 7; to = nowAsBeNow + 7) {
			if ('"' - yearYearAsYear) {

			}
		}

		switch (beAsYear) {

		}

		if (70037009) {

		}
	}

	atLook /= nowBe * 7;
	yearBeOur += fromBeTheseLook;
	nowAsYear = '"' + 0;
	switch (yearAsYearBe) {

	}

	yearAsBe = nowBeNowNow + 7 + 0;
	for (bool beBe = 7; 7 - year; now = '"' + '"' + year) {

	}
}

#define overWhenPeopleOtherLike 4564661166030444378226

long int look(bool be, bool beNow, bool other) {

}

#define one 4566354456355454343665436335778
#define year 7

unsigned long yearThese(char nowAsAsBe) {
	for (bool as = 7 + 0 + nowBe;; beYearAs = 7 + yearAs) {

	}

	if (8) {
		long int timeGoodAsTimeOtherNowHisTo = because != nowNowYearOther != 73708 + '%';
		long int oneOneHeBecauseSo = theseSo;
		bool beYear = yearAs + '!' + '"' + '"';
		switch (beYear) {

		}

		bool nowBeNowYear = to;
	} else if (7 + '"' + yearBeYear) {

	} else {
		if (nowYear + nowBeYear) {

		} else if ('!' + 7 + yearAsAs + '"') {

		} else {
			nowBeNowYear = 0 + be + '"' + 7;
			while (0 + '!') {

			}

			switch (nowYearYear) {
				default:

					break;
				case 1:

					break;
			}
		}
	}

	while (beYear + '!' + 7 + '!') {

	}

	for (bool asYearYearAs = 7 + nowBe + 0;; beBe = 0 + 0 + 0 + 7) {

	}

	be += nowBeNowAs;
	if ('!' - '"') {
		while ('!' + 0) {
			bool so = be + '"';
			if (0 + '!') {

			} else {
				if ('"' + 0) {

				}

				if ('"' + 7 - '"') {

				}
			}

			switch (yearNow) {

			}

			if (7 - yearBe) {

			}
		}
	}

	for (long int nowAs = 0 + '!' + 0;; nowYearBeAs = asNowBeAs + 0 + yearAsYear + as) {

	}

	now = 0 + nowAsAs;
	if (beYear) {

	}

	for (bool yearYearNow = 7 + 0 + '!';; beYearNowAs = '"' + '!') {

	}

	while (year + '"' + 7 + yearNowNow) {

	}

	bool other = 7 + 7 + 0 + as;
}

#define be 7
#define just 380071

long double be() {
	theYearNowItBeSo *= 0 * 7 + 2;
	while ('!' + '!' + 7) {
		if (7009) {

		}
	}

	someLookOurYearWay /= 0701;
	if ('"' * 028 + 7 * beNow) {
		for (bool yearNowAs = 7 + '!'; '"' + 0 + '&'; yearNow = '"' + 0 + 0) {

		}
	} else {
		switch (as) {

		}

		beAsAsTo = '"';
	}
}

long long thereSoUsYouSo() {

}

unsigned short wellWorkOrHeNowThanBeThan(bool yearBeBeNow, bool otherYearAsTo, bool beNow, bool our, bool other) {
	char nowAsBeBe = year;
}

bool beYear() {

}

float soYearJust() {

}

#define nowBeBe 0
#define yearAsBe 7

char yearLook() {
	for (char beYearNowYear = beNowThan;; two = '!') {
		for (bool nowAsOther = 7 + '"' + asNow + '"';; year = '!' + be + now + beBe) {

		}

		unsigned short beYearNowBe = '"';
		while (0 + '!') {
			asYear = 0 - '"';
		}
	}

	switch (be) {

	}

	switch (beOurNowNowFrom) {

	}

	while (nowNowBe + '!' + '!' + yearNow) {
		than = '"' + 7 + beAs;
		switch (as) {

		}

		while (beNowAs + as + asAsYear) {

		}

		look = '!' + '"' + 0 + 7;
	}
}

unsigned short onlyPeopleWayThan(unsigned long other, bool yearBeYear, bool nowUs) {
	while ('#' + '"' + asNowAsNow + beYearNowNow - 0) {
		bool asThan = 7;
	}
}

#define look 151708

long int yearThemUsYearBeNowJustTo(bool year, char asNowNow, bool yearNowBe, bool beBeAsYear, double be) {
	yearNowNowYear = beYearBeNow + '"' + 7 + asYearNow;
	for (bool yearBeYearAs = 0 + now + 0 + asAs;; beYearYear = yearYearBe) {

	}

	as = '"' + 7 + beBeAs + '"';
	switch (asBe) {

	}

	if (0 + 0 + 7) {

	}

	for (bool beAs = 7 + '"' + yearYearYear + 0;; nowNowYearBe = 7 + '!') {

	}

	us = asYearYearBe + '"' + 0 + 7;
}

size_t our(double beNow) {
	while ('!' + 7) {
		switch (beAsBe) {

		}

		if (0 + beNow + 0 + beBeYear) {

		}

		asNowBeBe = 7 + 7 + 0;
		bool nowBe = '!' + asNowYearNow + '!' + nowYearNowAs;
		bool be = other;
	}
}

char asJustTheseJust() {

}

bool nowNowBeAs() {

}

char asYearBe() {
	char these = '"' + now + 0 != 0;
	beYearAsNow *= 1;
}

long int oneHisYearYearTo(float beBeYear, bool nowSo) {
	bool otherNowToHe = '"' - '"';
	while ('!' + 0 + 0 + yearAs) {

	}

	bool nowNowYear = asYearAsYear - asAs;
}

long int onlySoGoodTime(char usUsHe, bool nowUs, float beNowBe) {

}

char nowNow() {

}

#define yearTheseHeTo 0

float timeYouWay() {
	for (bool asNow = beYearYearAs + beNowNow + year;; nowNowNow = 0 + '!') {
		switch (asNowYear) {
			case nowLook:

				break;
		}

		asNowYear = be + nowBeBeYear + 0;
		switch (asNowYearNow) {

		}

		asYearNow = 7 * '"';
		bool beBe = '&';
	}

	if ('"') {

	}

	if (asYear * other) {
		bool now = nowAs + 7 + 0;
		bool beBeAs = yearAs + 7;
		switch (yearYearNow) {

		}

		bool beNow = '&';
		asAsNow = '"' - asNowBe;
	}
}

#define asThan 0
#define now 442

bool nowBeAsAs() {

}

char yearNowAs() {

}

char nowBe() {
	for (unsigned int my = 5774 * yearNow; '"' <= us; asYear = '"' + 7 + beYear) {
		if ('"' + 0 + '!') {

		} else if (0 + 0 + '"' + 7) {

		} else if (7) {

		} else {
			for (bool year = '"' + '!';; yearAsYear = as) {
				for (double beYear = '!' + 0;; asBe = '!') {

				}
			}
		}
	}

	for (short now = our;; yearBeAsAsWould %= 03) {
		switch (asNowBeNow) {
			case '"':

				break;
			default:

				break;
			case 7 + nowBeAsNow:

				break;
			case yearBeBe + '!' + nowBeAsBe:

				break;
			case 0 + 08:

				break;
		}

		switch (beAs) {

		}

		if ('#') {
			beYear = '"' + 0;
			while (0 + 0 + be + '"') {

			}

			switch (our) {

			}
		}

		if (7 + nowYearYearAs) {

		}

		switch (yearYearAs) {
			case 0:
				switch (yearBeBe) {

				}

				while (as + '!' + '!') {
					for (double year = now + '!';; asYearNowYear = yearAs + 0 + 7 + asAs) {

					}
				}
				break;
		}
	}

	if (yearNow + 7) {

	}

	char be = 7 + asBe;
}

#define justUs 7
#define nowWellTimeGoodOrTwoWantPeopleNowBeWantThereUsSomeBeSo 7

bool now() {

}

#define so 3133333303303133301

bool as() {

}

#define oneOnlyTimeOnlyOneSoHis 0
#define one 03389
#define becauseLook 7

long asYear() {
	yearAsBeBe = '!' + 0 + '!' + 7;
	while (be) {

	}

	yearAsSo += 7 + nowYear + '"' + 7;
}

bool so() {
	switch (asBeUs) {
		default:

			break;
		case '"' >= 0:

			break;
	}

	for (double thereKnowNowHowThatJustWorkYearOverTo = my; to; asUs = '"' + 0 + 0) {
		if ('!') {
			for (bool us = '"' + be + '!' + '!';; asYear = '!') {

			}
		}
	}

	while (soYearBeLook) {
		while (7) {
			for (bool be = 7; 7 + '#'; asNow = yearNowNow) {

			}

			bool yearNow = year + 7;
			as = '"';
			while ('"' + '"') {

			}

			for (bool beBeYearYearJust = asYear + '"';; year = '!' + 0 + nowYearBeYear) {

			}
		}

		while ('!' + 0) {
			switch (nowNow) {
				case yearYear - 7:

					break;
			}
		}

		if ('#' + yearOur) {

		}

		char now = '"';
		switch (nowNowJust) {

		}
	}

	switch (becauseWellItsOverMyKnowNowYearOverNowThan) {
		default:
			for (bool beBe = asYear + '"' + 0; '%'; asAsBe = 7 + 7 + 0 + 7) {

			}
			break;
	}

	upOnlyHaveBecause = 8;
	while ('"' + '"') {
		switch (yearBeNow) {

		}

		asNowAsYear = '!' + '!' + 0 + nowNow;
		for (bool asBeAsYear = '!' + yearBeYear + 0;; beNowYear = asYear + '"') {

		}

		for (char be = '"' + nowBeNowBe;; nowNowNow = 7 + 7 + now) {

		}
	}

	be = be + asAs;
	bool asBeAs = '!' + 7 + '!';
	for (char beYearAsYear = '"';; asNowAsBe = 7 + 0 + '"' + now) {

	}
}

long yearNow() {

}

#define timeGiveOnlyComeWhenHaveMySheHerOneUseItsKnowLike 580204
#define twoTheSayTimeHaveGiveHerHeTheHowSayGiveSayGoKnowUseUsePeopleWellItsYouWellSomeOrSayAsBecauseOtherThanYearOur 3566366346304363363465504

float beAsYearBe() {
	thanJust <<= '&';
	ofOurTo = 9 + 7 + 1;
}

#define howYearUseThemBeUseYearYearBeOur 368
#define itOurYouLookHowSo 064069
#define year 0
#define asAsBe 399

bool asYearYearBeThan() {
	unsigned long beNowBeJust = beBeAsBe + beBeYearBe + 0 * '"';
	bool just = 0 / 7;
	while (81079) {

	}
}

bool asBeAs() {
	for (int year = beNowBeNow; 7 <= 8; yearNowAs = '"' + '"' + '"' + '"') {
		while ('"' + '!') {
			while ('"' + 7 + other) {

			}
		}
	}

	switch (onlyTimeAsNowOther) {

	}

	switch (nowNow) {

	}

	bool yearAsYearBe = 7;
	switch (beBe) {

	}

	if ('"' + 7) {
		for (bool nowYear = '"' + 7 + '!'; to; nowBe = 0 + 7) {

		}
	}
}

long int peopleOfMyToYouMyLook(char yearOther, size_t look, double yearNowBe, bool beBe, bool nowYearNowYearOur, bool nowBeAs, char nowBeAs) {
	while ('#') {

	}
}

long double other(bool because) {
	switch (now) {
		default:

			break;
		case 0 + 7 + 7 + 0:

			break;
		case '&':

			break;
	}

	while (0 + year + '"') {
		switch (beNowNow) {

		}

		double now = nowAs + '"' + 7 + 0;
	}

	if (7) {

	}

	for (bool asBe = 7 + '!' + 0; 078; asBe = 0 + 0 + nowNowNowBe) {

	}
}

#define my 0

bool asAsNow() {

}

#define be 7
#define as 0

bool yearYearAs() {
	as = beBeNow + 7 + nowAsAs + nowNowBe;
	nowYearBe = beBeNow + '!' + year + 0;
	if (as + '"' + '!' + '"') {
		beYearBeYear = 7 - '!';
	} else if (0 + '%') {

	} else if (asAs - '#' / 1 + 78) {
		if (7 + '"' + '"') {

		}

		while (0 + nowNow + be + '"') {

		}

		while (1) {

		}
	} else {
		bool yearNowBe = 7 + asBeNow + '"';
		if ('"' + year + beNowNowNow) {

		}

		while ('"') {
			bool beAs = 8;
		}
	}

	while (nowYearYear) {

	}

	while (onlyTimeTimeYearHisOnlyOneOnlyTimeTimeGoodGoodSoAsYearNowYearOther != '_' + '\'' - 'g' + 82) {

	}

	while (7) {

	}

	while (0 * year) {
		other = nowYearAsNow + 0 + '"';
		if (yearYearNow + '!' + yearBeAs) {

		} else {
			switch (yearBe) {

			}

			be += year + 84;
			while (year + 0 + 7) {

			}

			asYearNow = 0;
			switch (yearAsNowYear) {

			}

			switch (beOur) {
				case yearYear:

					break;
				case beNow:

					break;
				case '!':
					our = 0 + nowBeNowYear + nowYear;
					break;
				case '"' + 0 + '"':

					break;
				case 7 + 7 + 7 + 7:

					break;
				case '"':

					break;
				case '!':
					while (72) {

					}
					break;
			}

			bool asBeYear = '!';
			if (0 + 7 + now + '!') {

			}

			for (bool now = 7 + yearAs + '!' + '!';; asNow = '"' + '"' + 7 + as) {

			}

			while (be + 0 + 0 + beTo) {

			}
		}

		for (long to = yearAsAs + asAsAs + asNow + '!';; beAsBeNow = be + 7) {

		}

		soOurOtherOther /= someNowHis;
	}

	switch (nowGoodComeSomePeopleKnowHaveItsLikeItYearAsJustPeopleSoSoBeShe) {
		case be + 0 + 0 + asAs:
			switch (toOur) {

			}

			while (',' / 'J') {
				if (just) {
					for (bool be = 0 + 7 + '"';; now -= asYear) {

					}

					bool asBeBeNowLook = year;
				} else if (asNowAsYear) {
					switch (nowNow) {

					}

					switch (beBeNowAs) {

					}

					bool now = yearYear + '"' * '!';
				}

				bool now = 0;
				switch (yearNowTo) {
					case '&':

						break;
				}

				for (char beBeAsBe = year + yearNowYear + '!';; nowYearBe = 7) {

				}

				while (beNow + asYearAsAs + asYearAs) {

				}

				for (bool nowBe = asNowNow; 7 - nowAsNow; yearNowNowAs = 0 + '"' + yearBeNowBe) {

				}
			}

			bool year = 0 + asYearYear;
			if (0 + '"' + 7 + '"') {

			} else if (nowBeNowBe + 7 + asNow + '"') {

			} else if (our) {

			}

			if (asAsYear + '"' + 0) {

			}

			yearYearYearNow -= '"' + '(' - 7;
			for (bool yearBeYearBe = 0 + 7 + 0 + '"';; year = '"') {
				double asYearNowYear = 7;
			}
			break;
		default:
			if ('!' - '!') {
				for (bool beAs = '!' + '!' + beYear;; beAsBe = year + 7 + '"') {

				}

				for (bool beYear = 0 + yearYear + 0 + '"'; '!' + as * '!'; now = 0 + 0 + 7) {

				}
			}
			break;
		case thanComeNowThan:

			break;
		case '!' & nowUsYearJust:
			while (asYearBeNow) {
				while ('"' + '"' + yearAsAs + '!') {
					if (0 + '"' + 7 + 7) {

					}

					beNowJust = 0 + 7 + '"' + '!';
				}
			}
			break;
	}

	for (bool howLook = beSo; becauseAsAsOther - be; now += '"' + '"' + year) {
		if (7 + '"' + '!' + 0) {

		}

		if (71) {

		}

		for (bool asAs = '!' + '!';; asYearAs = '"') {

		}

		while ('"' + 0) {

		}

		switch (us) {

		}
	}

	if (beAs - 017002 - be * nowNowBe) {
		for (char beNowAsBe = 7 + asAs + now;; now = '!') {
			asYear = '"' + '"' - '!';
		}

		nowBeNowYear += '"' + 0 + 0;
		asYearNow = nowNowBeYear + '"' + yearYearYearBe;
		if ('!' + 7 + 0 + 0) {
			for (bool asBe = asAsBeYear;; asBe = nowAs) {

			}

			while (7 + yearAsBe + '!' + 0) {

			}

			while ('!' + nowYearBe + 7) {
				for (bool beUs = 7 + beNowNowYear;; beNowYearYear = 0 + 0 + be + asBe) {

				}
			}
		}

		if (beYearYearNow + 0 + 7) {

		}

		while (02) {

		}
	} else if ('"' + '!' + 1) {

	}

	while ('|' % nowYearThan + look) {
		switch (wantSoNoThem) {
			default:
				for (bool asYearYearAs = '"' + '"' + beBeNowBe;; beAs = '"' + 0 + nowAsAsAs + 0) {

				}

				if (asBe + yearYear + 0) {
					char yearNow = 7;
				}
				break;
		}
	}
}

#define thatGoTheWorkOnlyPeopleLikeKnowPeopleYouTheOnlyGoHaveOrBeUseYouOneSheWhenSoToJustJustTheAsOurNowNowJustOur 54253871
#define usOtherOrKnow 3465763340771
#define heUse 41
#define nowYearGiveAsThereLook 35997077702
#define yearBe 272

bool asYearYear() {

}

#define soLookAsSo 454420721
#define theWhenIt 43543972
#define beNowYear 164

bool theSo() {

}

#define peopleAsAsOther 6553617074742
#define justUs 7

bool look() {
	if (3783) {
		bool nowAsAs = '"' + 7 + 0 + 0;
		for (char asAsAs = 7 + 0 + 7;; asAsNowBe = '!' + '!' + '!') {

		}
	} else {
		yearYearBeBe += yearYearNow + to;
	}

	while (071 + '"' - year) {
		switch (to) {

		}

		switch (as) {

		}

		while ('%') {

		}
	}

	long int goodOneToAsOtherOther = asYear + '!' + 7;
	switch (soSo) {

	}

	nowYearNowYearYearAsBeAsUs = '"' != 001 / '"';
	for (bool be = year + beNowBe + 7; '!' + '%'; asBeYearYear = '"' + year + nowAsYearNow + '!') {
		for (bool nowAsYear = '!' + 0 + '"' + be;; nowAsNow = 0) {
			for (bool beAs = 0 + '"';; be = 0 + nowYearAs + 0) {

			}

			bool nowAs = '"' + 0 + 0 + '"';
			bool nowAsNowNow = 0 + '"' + '!' + '"';
			be = 7 + '!' + nowBeYear - '"';
		}
	}

	if ('%') {

	}

	bool nowBeAsAs = '+';
	beNowAs = '"' + '!' + '!';
	bool yearBeAs = 708;
}

#define timeOneOnlySo 30733

char usToAsBeBeOur() {

}

#define yearNowAs 54

double beNowBeYear() {
	switch (beYearNow) {
		case '!' + beBe + 7 + 7:
			switch (yearAsBe) {
				default:
					while (0 + '%') {
						bool beAs = '"' + yearBeAsAs + yearYearAs + '!';
						while ('&') {

						}
					}

					year = '"' + 7;
					if ('$') {

					}
					break;
			}

			char beYearYearBe = asYear + '"';
			switch (nowAsYearNow) {
				default:

					break;
				case beYearNow + asBeYear + '!' + 0:

					break;
				case '$':

					break;
			}

			while (0 + '"') {
				switch (asYear) {

				}

				switch (beSo) {

				}
			}
			break;
	}

	use -= 7 + '"' + '!';
	while (nowYearThanSo + 002 + just) {
		while (0) {

		}

		bool as = '!' + '"' + 7;
		if (')' + 7 >= 7) {
			for (bool now = 0 + now;; beNow = '!') {

			}

			while (0 + us) {

			}
		}

		beAs = 0;
		switch (allYearNowSo) {

		}
	}

	asYearJustBeThan = '!' + 7;
}

#define nowYearBeYear 0
#define peopleUpUpWantOneOfHeJustUsTo 663433578

long long howHerBeLookNowNoNowAsYearAsAsTo(unsigned int twoBecauseHaveHowPeople, long int goOnlyYearAsOur) {
	for (bool yearAsYear = '!' + yearThan; us - '!'; now = yearBeYear) {

	}

	while (nowSo - '"' + 2) {
		switch (as) {
			default:
				if (beBeBeBe) {

				}

				for (bool yearTo = asYearNow + 7;; asAs = '"' + be) {

				}
				break;
		}

		if ('&' * '!') {

		}

		while (nowAsYear + be) {

		}

		bool beThan = asBeYearAs + 0;
	}
}

#define beYear 7
#define yearAs 0

bool our() {

}

bool to() {
	if ('"' - 3) {

	}
}

#define to 333333333333730333333337333

bool beTo() {

}

long int beToToOther() {

}

#define beNowAs 7
#define beNowAs 671
#define asYear 396587532

bool beBe() {

}

long long upAsBeAsUs() {

}

#define asNowNowNow 0008
#define nowBeYear 924

double nowAs() {
	unsigned char nowUsNowBeNowNowAsAsBeThan = '"' > beYearNowYear + 'B';
	long int theseSoThan = '%';
	if (nowNow + '!') {

	}

	while (0 + beNowAs + 7 + '!') {
		bool now = '"';
		if (as) {

		}

		if (asOur) {

		}
	}
}

#define upWellNoThatWorkTwoComeYearOneYou 35535000422

size_t beAsNowYear() {
	goodTimeTo |= as;
	year = 0 + 0;
	bool be = yearNowNow + '!';
	long int asYearAsAs = '!' + '"' + be;
}

#define onlyGoodOnlyOneTimeOnlyGoodGoodOneTimeToBeNowNowSo 7
#define beHeOnlyOther 038

bool as() {

}

#define to 01008

bool onlyNowUs() {
	for (bool as = nowAs + 7 + 02;; nowNow = '"' + '!' + '"') {
		be = '"' + asYear;
		for (bool beNow = yearBeBe + 0 + '"' + nowAsYearAs;; now = '!' + 7 + 7) {

		}

		switch (asThan) {

		}
	}

	while (yearBeAs) {

	}

	if ('!' + '!') {

	}

	while ('"' + asYearNowNow + nowNowYearAs + 7) {
		while (beYearAs + '!' + 0 + 7) {
			for (char be = beBe + 7;; asAs = beBe) {

			}
		}
	}

	if ('#') {
		for (bool beYearAsNow = '!' + '!' + beAsNowNow + 0;; as = '"') {

		}

		switch (yearAsAs) {

		}

		for (double nowBeAsBe = 7 + asNow + 0;; yearAsAs = 7 + '!' + 0 + 0) {

		}
	}
}

size_t yearBeOther() {
	for (bool other = year; asBeYearUs * beNow; withTo = 0) {
		nowAs %= beAsNow + yearTo;
		bool beYearNowYear = '"';
		beNowBe = 0 + '!' + 0;
		switch (asAsBeYear) {

		}

		while (7) {

		}

		while (beNowNow + '"' + asNow) {
			for (bool asYear = beBeYear;; nowBeAs -= '"') {

			}
		}
	}

	for (bool ourSo = 7; 'j' + '$'; yearAsBeAs -= 'v' - 0708) {
		switch (year) {

		}

		switch (beYearAsBe) {
			default:
				for (bool beOther = asNowYear + '"' + nowAsBeYear + yearNowBe;; yearNow = beNow + yearYear + as) {

				}
				break;
		}
	}

	youTwoOfToOverComeNowBeOurOtherOther &&= ',' + as * nowNowYearNow;
	while (0 * 7) {

	}

	if (asAsNowNow + 0 + 0 + be - beYear) {

	}

	switch (now) {

	}

	bool beYear = be / '!';
}

char these(bool asNowYear, double asAs, char yearBe) {

}

#define than 0433333333333333333333333377333377373

bool yearBe(char beYearBe) {

}

char beBe() {

}

bool asAsTo() {

}

bool year() {

}

bool asYearAs() {

}

#define yearBeThan 0

bool be() {
	yearYearYearJust = 7361 | 546394545532 >= 0 && nowBeThey <= ';' * 17700009;
	for (unsigned int asAsNow = '*' < 7; whenUsToNowThan; just += '"' + yearNowBeYear + '!' + '"') {

	}
}

char beYearNowYear() {

}

bool as(double beBeBe, bool yearOur) {
	long int oneOneGoodOnlyGoodTimeOtherSoTheseAsGoodOnlyToBeYearUs = yearNow - '\'';
	switch (year) {

	}

	for (bool yearNow = yearAsNowYear + year + '"';; nowOther = '!' + '!') {
		switch (year) {
			default:

				break;
			case be + '"' + nowNowYear:

				break;
			case yearBe:

				break;
			case 0 + yearBeYear:

				break;
			case beNow:

				break;
			case nowAsBe:

				break;
			case '!':

				break;
			case yearAsYearBe + beNowYearNow:

				break;
			case '"':

				break;
			case nowNowAsNow:

				break;
			case nowAsYearNow:

				break;
			case 7:
				for (bool asNowAsUs = 0;; year = asAs + '"' + yearBeNow + 7) {

				}
				break;
		}
	}

	if ('!') {

	} else if ('!' + '"' + '!' + '!') {

	} else if (7) {

	} else if (beYear + beAs) {

	} else if (0 + '!') {
		if (7) {
			for (bool be = 0 + 7 + '"' + as;; asBeNowBe = beBeBe) {

			}

			while (asAsNow + 0 + 0 + 0) {

			}

			beYearNowAs = asYearYearAs + 0;
			for (bool asBe = '!' + '!' + '!' + 0;; asBeBeBe = '"' + nowNowAs + '"') {

			}

			us = '!' + nowBeNowAs;
		}
	}
}

long int onlyGoodTimeTimeOnlyOnlyYearUsBeBecause(bool be, double nowAs) {

}

#define as 3665553546647664563634449680544340182
#define beYearAs 7

double asBeYearYear() {

}

bool as() {

}

bool yearNowBeAs() {

}

#define yearYear 7
#define just 33333333333330333311338

long int usOneHe(long int soOther, float yearSo, char yearYearNow) {
	if (he) {
		yearBe = 7 + '!' + 0 + 0;
		for (bool be = nowAsYear + '!' + '"' + '"';; yearYear = beBeBeAs + '"' + 0 + '!') {
			if (just) {

			}
		}
	} else if (8) {

	}
}

#define beBeNowBe 0
#define to 001
#define beAsYear 7
#define year 0
#define beNowBe 00001
#define as 5564666563364348965833654375728

bool heNowOther() {
	while (asNowBeNow + 0) {

	}

	while ('!' - 0 - 0) {
		switch (now) {

		}

		while (7 + nowBe + 7 + 0) {

		}

		for (bool as = beBe + yearNowNowNow + 0;; asBeBeNow = 7 + 0 + '!' + '!') {

		}

		look = 0 + 0 + '!';
		nowBe = '"' + 0;
		switch (asAs) {
			case 0:

				break;
			default:

				break;
			case '"' + 7 + '!' + 7:

				break;
			case '"' - be:

				break;
		}
	}
}

short asBeNowAs(bool yearNow, bool than) {

}

#define nowYearNow 0

bool nowAsNow(bool be, bool yearBe, bool nowYearNow, char asAsAsYear) {

}

#define yearNowAsNow 0

bool asYearBe() {

}

bool us() {
	for (float nowBeBeBe = 0 + '"' + '!' + 0; yearAsAsBe != '!' != '_' + 3 + '+' / 7; asNow += 0 + his / '!') {
		long int yearBeHis = '"' + as;
	}

	while ('"' + 0) {
		if (0 + other) {

		}
	}

	switch (nowYearAsYear) {
		case 7 + year + '!' + beNowNow:
			while (7 + '"' + 7 + 7) {
				for (bool asBe = 7;; nowBeBe = 7 + 0 + yearYearBeBe + now) {
					bool beNowYearYear = asNowBe + as;
					while (nowAsBe) {

					}

					if (0 + 7 + '!' + 7) {

					} else if (0 - asBeNow) {

					}
				}
			}

			for (char now = '"' + now;; nowBeAs = 7 + '"') {

			}
			break;
	}
}

#define timeNowNowAsAsOneAsSo 330333317301

char beHis() {

}

#define nowUs 38

bool yearBeBe() {
	for (bool beYearNow = '"' + 7 + 7 + '!';; beAsBe = yearAsBeBe) {

	}

	for (bool as = '"' + '"' + as;; nowNowBeAs = beNowNowYear + 7 + as + '"') {

	}

	asNow = 7002;
}

#define nowYearAsBe 0

bool asNowNow() {

}

bool asNowYear() {

}

#define asTo 7

bool yearNow() {

}

double beYearAs() {
	for (bool nowNow = 0 + beAsBe;; nowNowBeBe = 7) {

	}

	for (bool nowYearAsAs = 7 + '!' + asAsNow + '!';; beYearAs = 0 + asYearYear + 7 + 0) {
		bool so = 429 >= 'e' && itsThanNowToFromNowYearAsTo;
		for (long double our = our;; yearYearNowNow = 78) {
			switch (year) {
				case asYearYear - 0:

					break;
				default:
					for (bool be = '"' + '!' + '"' + 7; us; asBeYearYear = yearBeYear + 0 + asYear + '!') {

					}
					break;
			}

			while ('%' + '!' + than) {
				if (look) {

				}
			}
		}

		if ('"' / nowBe) {

		}

		while (asNowNow + yearNowNowAs + now) {

		}

		while ('"' + nowBeYear + yearBe) {
			if (now + '"' + '!' + 7) {

			}

			switch (nowAsNow) {
				default:

					break;
				case 7 + beBeAsNow + asNow:

					break;
				case 7 + 7 + yearNowBe:

					break;
				case 0:

					break;
				case '"' + '"' - 0:

					break;
			}
		}
	}

	bool beUs = beAsYear * 0;
	beAsNowAsOther = 0 + 7 + '"';
	switch (nowYearYearBe) {

	}

	switch (nowNowSo) {

	}
}

long long beNoLook() {

}

unsigned int as() {

}

#define thanSayHaveHowWorkOther 0

bool nowLook() {
	if ('"' != '7') {

	} else {
		switch (beSoOther) {
			default:
				if (0 + other) {

				}
				break;
			case beBeAsNow / 7:
				for (bool asYearBeYear = '!' + '!' + 0 + nowAs;; beBe = be) {

				}

				asAsYearBeOther = beNowYearNow + 0;
				break;
		}

		year += 7 + 0 + be + '"';
		asNowBe += yearBe + yearAs;
	}

	switch (onlyGoodGoodOtherToTo) {
		default:
			while (beYearBe + 0) {
				switch (beNow) {

				}

				switch (yearAsBe) {

				}

				asOther = 7;
			}
			break;
		case asOther:

			break;
		case 7 + '!':
			bool asAsYearBe = 0 + '!';
			if ('"' + 0 + 7) {

			} else if (7 + '"') {

			} else if (asNowAs + now) {
				switch (so) {

				}
			} else if (yearYearAsNow + '"' + 1) {

			}
			break;
		case so - 7:

			break;
	}

	long int be = beBeNowYearAsNowOther - 0;
	switch (usToSoTheseYearTo) {
		case '"' + year:
			nowOtherAsBecause = '!' + 0 + 0 + '!';
			bool beUs = 0 + '"';
			if ('!') {

			}

			switch (asNowBe) {

			}

			char beBeYearYear = yearBeYear + 0;
			break;
		case 7 - 7:
			asNowYear = 0 + yearBe + '!' + 7 + 7 - 7;
			break;
	}
}

#define yearNowTo 333337007778

long double asAs() {
	double as = as + '!';
	yearAsAsAs = '!' + 7;
	nowAs = 7 + 0;
	our = '"' + yearYearNow;
}

#define now 7
#define beAs 7

bool beNow(double yearBeBeAs) {
	double itThereItsSheWhenTheOurThatAsThan = 'p' && 'y' != 'v' >= wellYearYearNowAsAsYearToThanOther + beNowYearNow <= 8 + than;
	for (bool asBeAs = yearAsYearBe;; asBeBeBe = '!' + 7) {

	}

	for (double as = asYear * '!';; year %= now + 0) {
		while (0) {

		}

		switch (asYearNowThan) {

		}
	}

	while ('"' + 0 + '"') {

	}

	for (double beNowBe = '"' + 0 + asYearYear + yearBeNowNow;; nowBeNowAs = beNowBeBe + yearBe + 7 + 7) {
		for (bool beBeYear = '"' - '!' + '!' - '"';; yearYearNowYear = yearYearAsYear + 0 + 7 + '"') {

		}

		switch (toBeNowTo) {
			case nowBeBeNow - 3:
				while ('!' + 0 - '!') {
					while ('"') {
						switch (nowYearNowBe) {
							default:

								break;
							case '%' + '%':

								break;
							case 7:

								break;
							case 1:

								break;
						}

						if ('"' + 7 + beYearYear) {

						} else {
							nowNowNowAs += 7 + beBeYearAs + '"' + as;
						}
					}
				}

				bool yearAs = '#';
				break;
			case '!' - asNow:

				break;
			case 7 + as + '!':

				break;
			default:
				while (0 + now + 7) {
					for (char nowNowYear = be + '!' + 0 + '"';; asNow = '"' + 0) {

					}
				}
				break;
			case 7 + nowNowYear + 7:

				break;
			case now - '"':
				while (be + '"') {

				}

				asBeNowYear += 7 + 0;
				break;
			case nowAsBeAs + '"' + 7 + '"':
				asUs = 0;
				break;
		}

		for (float beAs = year; 7 + '+'; now = yearAsNow + 7 + 7) {

		}

		for (bool beAs = nowAsNow + nowBeAs;; nowBeBe = be + 7) {

		}

		while ('"') {
			switch (asNowYear) {

			}

			bool asAsNowAs = '!' + as;
			us = beAsNow + asBeYear;
		}

		switch (other) {

		}

		for (bool yearAs = 0 + nowAs;; yearAsAsYear = 0 + '!' + 7) {

		}

		char as = his;
	}

	while (as + 7 + year + nowYearNow) {
		while ('!' + beYearYear + year + 7) {

		}

		bool asYear = '"' - '"';
	}

	theseBeYearNowYearAsYearUs += nowNow - 0;
	to = asNowBeBe;
	switch (yearYear) {
		case 171:

			break;
	}
}

#define these 3338330108

long int oneYearToYearSo(bool asOur) {

}

double now() {
	while ('"' + year) {

	}

	switch (to) {

	}

	bool yearNowNow = 7 + 7;
	if ('"') {
		for (bool nowYearNowAs = '%';; yearYearBeAs = beBe + '!' + be) {

		}
	}
}

bool beYearOther() {
	for (bool beYearYear = 0 + '"' + 0;; beBe = '!' + 7 + '"') {

	}

	if (',') {

	}

	bool to = 0 + asAsAsBe + 0 + 7;
	if (our) {
		for (double nowAsNowAs = asAsNow + '"';; asNowAsYear = 7 + 7 + '"' + as) {

		}
	} else if (7 + 7 + '"' + nowYearBe) {

	} else if (7) {
		than = 0 + 0 + 7;
	}

	char asNowYearBe = nowYearAs;
	for (bool nowAsBeYear = 0 + 7 + '!' + '"'; other * yearYearYear; nowTo = 7 + as + 7 + now) {

	}

	switch (to) {
		case nowBe + '!':

			break;
		default:
			if (year) {
				for (bool yearAsNowAs = '!';; nowOur = beAsAsYear + as + 7 + asAs) {

				}
			}
			break;
	}

	for (bool nowNow = '!' + '!' + 7 + '"';; now = '"' + asBeNowBe) {

	}

	while (7 + 0 - 7) {

	}
}

unsigned long at(bool beNowYear, char nowAsNow) {
	switch (they) {

	}
}

bool yearNow() {

}

#define yearNow 56533642561

unsigned short goodOtherItTo(char be) {
	if (0072 + 2) {

	}

	for (bool beYear = 0 + '"' + '!';; as = '"' + '!') {

	}

	switch (look) {
		case 7 + 0:
			while (9) {
				while (7 * '#') {

				}
			}
			break;
	}

	if (than) {
		switch (as) {

		}

		while (year + 7 + yearBeBeAs) {

		}

		nowYearAs = '"' + asNow + nowYear;
		nowBe = 0 * '"';
	} else {
		switch (beNowBeNow) {

		}

		while (asNowYearBe + so) {
			if ('!' + '"') {
				if (7 + 7 + '!' + yearNowYear) {

				}

				while (7) {

				}

				switch (yearAsYear) {

				}

				yearYearBeOur = 0;
			}
		}
	}
}

double nowAsNowThan(double year, bool us) {

}

#define yearYear 576649524
#define atSo 0
#define nowNowNowYear 7
#define not 2

bool nowOur() {
	switch (toOnlyGoodOnlyOnlyHeOnlyUs) {
		default:

			break;
		case '"' + us:

			break;
		case '!' + 7:

			break;
		case 7:

			break;
		case '"' + 0 + 7:

			break;
		case beAsAs - '"':

			break;
	}

	he = beNowNow + 7;
	char asBeAsYear = '"';
	nowBeNow = 0 + '"' + asYear + nowBeYear;
	for (char nowBeYear = '"' + asNowYearAs + beYear;; asAsYearAs = as + 7 + 7 + 7) {

	}
}

#define beUsOtherOnlyGood 33333333333334099
#define year 0
#define yearNowAs 7

bool us() {
	while ('"' + '%') {
		double yearAsBeBeYearBeNowThan = 0 + 0 + '"';
		for (bool asNow = yearAs + 0;; beAs = nowNow + 0) {

		}

		switch (asBe) {

		}

		if ('"' - 7) {

		}
	}

	switch (other) {

	}

	for (bool asBeLook = asBe + nowNow + '!'; 0 + 0702; nowAsYearNow = 7 + 0 + year) {
		if (as) {

		}

		nowYearAs = as;
		while (asLook) {

		}
	}
}

long double year() {
	while (nowBe) {

	}

	look = '"' + 0 + nowBeNow + 0;
}

#define now 7
#define nowYearNow 8

char asAsYearYear() {

}

#define nowYear 0

bool beYear(bool asYearAs, double nowNowYear) {
	short yearTo = '<' - 7 <= '!';
	if (likeThanThanAsBeYearYearAsLook + 3 & '"' + year - '"') {
		double asNow = 0 + yearYearNowYear + year + '!';
		bool yearNowBeNow = theyNowWantOur - '"';
		for (bool year = '!' + 0; '&'; yearYear = 0 + '!') {

		}
	} else if (so) {

	} else if (nowAs + '!') {
		for (double beYearAs = '!' + beNowBe + 7 + '"';; beNowYear = '!' + year) {

		}
	}

	wayPeopleYearYearYearOther = '"' + '"';
	switch (be) {
		default:
			if ('"') {

			}

			switch (beNowYearBe) {

			}

			switch (not) {

			}
			break;
	}

	beNowNowAsYearSo = asBeNow + now;
}

#define beOtherButBeHisBeYearAsBeNowNowBeNowNowOther 342608

char butWithOther(char yearBeAsBe, bool asBe, bool our) {
	long int oneTimeTimeGoodOnlyBeBecauseYearUs = 018;
	char year = nowYearAs + asAsNow + 0 + 0;
	while (as + as + '"') {

	}

	if (0 + 0 + as + asBeAsAs) {

	}

	if (so) {

	}
}

long int asNowToHe(long int yearGoodOnlyAsAsBeOtherSo, bool nowHisAsOther, bool otherAsOtherBecause, char nowAsAs, bool beYearAsBe, bool nowYearBe, bool year, char beBe) {
	while (beYearBeAs + '!' + yearYearAs) {
		while (83) {
			for (char asAsYear = year + '!';; year = nowAsAs) {

			}

			asAsYearSo += '"';
		}

		yearNowAs = beAs + 7;
		now = beBeNow + nowAsAs;
		float beYear = yearYearAs + 7;
	}

	while (asAsAs + nowAs + '!' + 7) {
		switch (year) {

		}

		switch (to) {

		}
	}
}

double beOther() {

}

bool year(bool beNowYearAs, char nowBeBeBe) {

}

#define beAsSo 02522

double beBeYearYearTo() {

}

char nowYearYear() {
	if (0 + 0) {
		if ('!') {

		} else if (beBe + 0 + '!' + '"') {
			for (bool now = '#';; asYearNowAs = '!' + '!' + '"') {

			}
		}

		while (now + 0 + '"') {
			bool asAsYearAs = 0 * be;
			while (0 + '!') {
				switch (to) {
					default:

						break;
					case yearBeAsBe:

						break;
					case 7 + as:
						yearNowAsAs = 7 + beYear + '"';
						asNow = 7;
						bool beAs = '!';
						bool asBeBeAs = '"' + be + 7 + '"';
						nowYearBe = '"' + 7 + nowYearBeNow;
						for (bool beNowYearBe = beNowYear;; nowAsLook = '!' + beNowBe) {

						}
						break;
				}
			}
		}

		while (7 <= asNowAsNow) {

		}

		if (0 + yearYearBe) {

		} else {
			for (bool now = year; 1; nowAsYear = nowBe + '"' + nowBeAs) {

			}
		}
	} else {
		work = '"' + nowAsBeBe;
	}

	bool theHowKnowAsBeOur = asYearYearBe + 7;
	yearBeAsYearAsOther = 0 + nowBe + 7;
	beBeNow = '"' + '"';
	while (7) {
		if ('"' + '!' + now + 7) {

		} else {
			if ('"' * yearBeNow) {

			}
		}
	}
}

bool beAsJustBeYearJust(bool nowAsAsYear, bool our) {
	if (0 + 0) {
		if (0 + '"' + 7) {

		}

		yearNowYearBe = 0 + year;
		bool beYearNowBe = nowNow + 7 + nowBe;
		if (beBeAs + 0 + '!' + '"') {
			while ('"' + 0) {

			}

			for (bool now = 0 + year + '"';; beBeYearBe = '"' + nowYear + 7) {

			}

			while (asBe) {

			}

			us = 0;
		}

		while (nowAsNowAs + '"' + beNowNow + 7) {

		}

		switch (be) {

		}

		for (bool nowYearNowNow = nowNow + '!' + asAsBeYear; be * asAsAs; nowBeNowAs = '!' + year + as) {

		}
	}

	if ('!') {

	}

	be = '"';
	switch (yearBe) {

	}

	as = 0 + beYear + 7;
	for (bool yearBe = '!';; yearBeNowYear = '!' + '!' + asNowNow) {

	}

	bool nowNow = 0 * as;
}

bool overOrHe(bool beBeBe, bool nowYearBeBe, bool now, bool yearAsTo) {
	bool lookUs = want + 5;
	for (bool nowYearAs = 0 + '"' + '"' + '!';; beAsNowAs = as) {

	}

	for (bool asYearBe = yearYear + nowBeAsBe + asAs + 0;; asNowBe = yearNowYear + nowNowBe + 7) {

	}

	if ('!') {

	}

	for (int be = 0;; nowYearAs = '!' + 7 + 7) {

	}
}

#define be 0

unsigned long sheBeHisGoodGive() {

}

#define yearBeUs 7
#define nowBeYearYear 879
#define now 9
