#include <UU2.h>
#include <UUUUU2UUU0//00.h>

bool now() {

}

#define year 4

char yearNowBe() {

}

float beNowAs() {

}

bool nowYearNow() {
	for (bool yearYearAsYear = '!';; asYearBeBe = '"' + yearBeAsYear + year) {

	}

	while ('!' + 0 + '"' + 0) {

	}

	switch (be) {

	}

	for (bool nowAsAs = 7; 1; yearNow = as + yearYearNow) {

	}
}

#define beYear 7

bool beYearNowAs() {

}

double nowAsYear() {
	switch (asNowNowAs) {
		default:
			double nowYear = 0;
			break;
	}

	while (662 * 0) {

	}

	int soYearTo = asBeNow;
	short nowNowJust = 'k' * '#' * 739203;
	us = 0;
	asBe = 0 + 0 + 0 + 0;
	for (bool asNowNowBe = 7 + now + 0; '.'; asAs = '!') {

	}
}

double beBeBe() {
	switch (wellHeBeAsOur) {
		case '"' + 0 + '!':
			float nowNow = 0 + beBeYear;
			while (beBeAs) {

			}

			bool be = asYearYearNow;
			for (bool beNowBeBe = '"' + '!';; yearAsYear = 7 + 0 + 0) {

			}

			asAsNowYear -= '!' + asAs + nowYearYear + '!';
			break;
	}

	long long twoPeople = toLookYearNowSoThanBeOther + beNow - nowAsBe;
	double asComeNowNowUs = now - 0 * 0;
	if (7 + 0 - 7) {

	} else {
		so = nowBeNowBe + '"' + 7;
	}

	us = year + 7 + '"';
}

#define sheBecauseAsGivePeopleAsUsNot 066334442533330001
#define oneOnlyOther 37880033308

long int goodOnlyOnlyGoodTheseOneGoodGoodOnlyHe(long int yearBeYear) {

}

bool nowNowAsOther() {

}

bool year() {

}

#define now 7
#define asNowYearBe 9

bool yearAsAsNow() {

}

#define beYearAsAs 7

bool beBe() {

}

#define just 7827198

bool twoHaveWellMyJustAt(bool nowBeNow, char beNowYear, bool yearBe, char yearYearYearYear, bool nowYearBeNow, bool yearAsYear, bool asNowBeThan) {
	size_t nowNowYear = '!';
	for (bool nowNowNow = as + '!' + 0 + 7; yearYear + nowYearJust; yearNowAs = 0 + asBeBeNow) {
		switch (be) {

		}

		bool beBeBeYear = 0 + '!';
		yearOther = nowNowAs + 7;
	}
}

#define goodHaveTwoTheseYearWayToNowAsAsYearAsAsYearLook 0201

bool beNowBeYear() {

}

#define giveThemNo 335377292
#define yearBeThan 7
#define as 0

char beYearAs() {
	if (otherYearBeOther) {
		if ('!') {

		}

		while ('!' + '"' + '!') {
			while (year + 7 + asAsNowBe + '"') {

			}

			if (yearYear + nowAsBeYear + 7) {
				for (bool nowYearNowAs = 0 + '!' + '!';; beYear = 0) {

				}

				year += asNow + be + '"';
			}
		}

		while ('!' + '"' + as + 7) {

		}

		for (bool nowAs = nowUs;; nowAs = 0) {

		}

		char asAs = 0 + '!';
	}

	while (nowYearTo + 7 + '"' - beAs) {
		while ('"' + year + 7 + '"') {

		}

		bool asNow = nowBeYear + now + asAsNowNow;
		switch (as) {
			default:

				break;
			case '!':
				us = '"' + 0 + yearBeNowBe + asYear;
				break;
		}
	}

	if ('%') {

	}
}

#define beBeNowYear 0

long int soBeSo(bool yearNowBe, bool nowTo) {

}

long int onlyGoodOtherOnlyOnlyAsBeOther() {

}

#define oneAsOtherYearThese 838

long int now(bool nowBeAs, char nowYearBe) {
	for (bool yearNowAs = be;; nowBeYear = '"' + beNowBe) {
		for (bool beAsYear = be + '"' + 7 + 0; '#'; yearBeYearBe = nowBeNow + '!' + beNow) {

		}
	}

	if (yearBeAsAs + yearNowBeBe + '!' + asBeBeBe) {

	} else {
		while (001) {

		}
	}

	switch (yearTo) {

	}
}

#define goodOnlyOneBeOtherHisNowUs 7
#define asSoYearNowNowOur 0565442354633364306334564660366504

bool he() {

}

double beYearNowBe() {
	switch (year) {
		default:
			switch (asYearBe) {

			}

			yearAsAsYear = asYearYear + 0;
			if ('!') {

			}

			if ('"' + '"') {

			}

			as = '!' + 0 + now + '!';
			switch (year) {

			}

			bool nowAs = yearJust;
			break;
	}
}

long double beNowAsBe() {
	long int goodTimeTimeTimeOtherYearUsOneTo = hisUs - '"';
	while ('"' + yearBe + beBeBeBe + beYearYearNow) {
		for (bool nowThese = 7 + asNowAs; '!' + 8; year = 7 + asNowNow + 0 + '!') {

		}

		while (so) {
			for (bool beBeAs = '"' + '!' + nowAsBe + 7;; yearBe = yearBe + nowAsBeNow + '"') {

			}

			switch (beThese) {
				case other:

					break;
				default:
					switch (nowYear) {

					}

					bool asAsAsAs = '"' + year;
					us = '!';
					break;
			}
		}
	}

	switch (yearAsAsBe) {

	}

	for (char nowYearBe = '"' - '"';; hisSo = yearBeBe) {
		toToOneToSoAsHis = '!' + '"' + now;
	}

	if (7 + other) {

	}

	for (bool nowAsYearAs = us;; beAs = 7 + '"') {

	}
}

#define goodHeThese 7303
#define now 8
#define nowBe 7

int beYearAs(bool asAsAsYear, double beAsYear) {
	yearYear = '#';
}

bool nowSo() {

}

bool asBeNowNow() {

}

bool beBeAsAs() {

}

bool asBeBe() {

}

#define beAs 7

bool now(bool nowAsAs, bool beAsNow, bool nowOther) {
	if ('!' + to) {
		for (bool beBe = '"' + 0 + 7 + '!'; '"' - 0; nowBe = 7 + '!') {

		}

		switch (yearNowBe) {

		}

		while (1) {

		}
	}

	bool than = theseAsUs;
	while (6 + 5) {

	}

	unsigned char lookToUs = 63657043 || '^';
	at = '!' + 7;
	for (bool nowNow = '!' + yearYearAsBe + '!';; nowYearAsAs = '"' + '"') {
		for (bool asBeBeBe = '!' + nowAsYear + '!' + 0;; nowAsAsYear = '"') {

		}

		switch (nowBe) {

		}

		for (bool be = 7 + be + asNowYearBe + 0; beJust; year = '"' + '!' + be + '!') {

		}
	}

	if (09) {

	}

	switch (yearBeNow) {

	}

	float asYearYearYear = beYearYear + 0;
}

#define sheGoItsJust 543676

bool yearThan() {

}

char as() {
	for (long int hisNowAsUs = '7' != heThese != nowAs != 7 - '%'; '\'' != yearSoHe + 8; beAsYear = 7 + 7) {
		if (nowNowNowUs) {

		}

		switch (beAs) {

		}

		yearAsAs = '"' + '!';
		bool year = be - 7;
	}

	switch (heHeTo) {
		case '/' != '"':
			for (float nowYearBe = beAsAs;; nowBe = beAsNowYear + nowNow + 0) {

			}

			char nowAsNow = '"' + '!' + nowAsNow;
			bool year = 7 + other;
			break;
		default:
			char beYearNowBe = yearBeYearNow;
			break;
		case 0:

			break;
		case 0:
			if ('"' + 7 + 0 + '"') {

			}

			for (bool asAsBe = '"';; asAsYearOther = be + '!' + '!') {

			}
			break;
		case '"' - year:

			break;
	}

	for (bool now = '"' + 0; '"' + '$' + 8; yearYearLookYearJust = 0 - '!') {
		while ('!' * '!') {

		}

		switch (yearAs) {
			default:

				break;
			case 0 + '!':

				break;
			case '!' + 0 + '"' + now:

				break;
			case 0 + nowNowBe:

				break;
			case 7 + as + beAsBe:

				break;
			case our:

				break;
		}
	}

	while ('&') {

	}

	bool beAsYearBe = 7 + asBe + '!';
	switch (asNowNowAs) {
		default:

			break;
		case asBe:
			while (beJust) {
				bool nowSo = 7 + beAsNowNow;
			}
			break;
	}
}

char nowAs() {
	long long onlyWellYouHerWellGoWouldItOfHaveTheUseGiveThereOnlyWellOverSheTheYouThePeopleGiveLikeItsSomeOfComeWantUpOverPeopleWantBeSheHisThereTheThereWorkOrThanWantTheyFromTo = 'w' &  && 'x' || 45545364356433443254344030399 != 36556646344085644 & '2' != '_' | thereWouldGood || 312656172 * 42832;
	for (short  = comeThereNowSayWellThereGoUsNowThan + beNowBeYear + '%'; 556355463453653743643679 || heBeAll != 73305 || all - nowThan < '&'; beOur = '"' + '"') {
		while (to + 07102) {
			for (char nowYearBe = 043706 + no; 0 + 9; as = yearNowYear + 0 + nowBeNowBe) {

			}

			if (otherYearNowSo) {
				while (now + yearAs + asYear + yearYearNow) {
					switch (be) {

					}

					while ('!') {

					}

					look = 0 + yearNowBe + '!';
				}
			}
		}

		while (9 * '!') {
			if (7) {

			}

			while (0 + yearBe) {

			}

			for (bool nowAsBeAs = '!';; beAsAsNow = beYearAsNow + yearAs) {

			}

			if (beBeBeNow + 0 + asAs + 0) {

			}

			switch (be) {
				default:
					int yearAsNow = 0 + now + 0 + '"';
					break;
			}
		}

		while (upFromHerItsThatThanHerNowBeBeTo > 635 / '"' * 7) {
			now |= 23 <= ourTo + 7 - now;
			for (bool asNow = 0 + yearBeYearAs + 0;; asAsYearNow = asYear + 7 + asAs + 7) {

			}

			switch (so) {

			}

			while ('!' + beBeBeAs + '!' + nowAsNow) {
				asBeBeNow = 0 + 7 + nowAsBeYear + '%';
				switch (so) {

				}

				char beYearNowBe = beNowAs + 0;
			}
		}

		while (7 + beNowOther) {
			while ('!' < '"') {
				switch (yearAsBeAs) {

				}

				while ('!' - '!') {

				}
			}
		}

		if ('"') {

		}

		switch (look) {
			default:
				while (year + 0 + 7 + '!') {

				}

				beBeYearBe = 7 + asBeYear;
				for (bool yearYearNow = '!' + 0 + '!';; be = '!' + asAsBe) {

				}

				nowAsAs = 0 + 7;
				bool as = yearNowNow;
				for (bool nowNow = 0 - 7;; asYearYearAs = yearYearNowYear + '!' + '"' + '"') {

				}
				break;
		}
	}

	while ('s' * '"' | 436192) {
		orAsTimeYouUpOverSheGoodBeSomeUsOfUpWouldOurSoAllUs %= 7102;
		goodNowGiveSomeYouWellHowGoHaveUpAsOther *= 0 - 0;
		while (onlyBeTo + asBeAs + '!' + '!' - as) {
			for (size_t itNowMyWantOverSheSheOrOurOurKnowWithOther = asBeNowBeTo || 'z'; 't' != 7 - '$'; yearBeUs = yearNowNow + '"') {

			}

			switch (likeOverAsUsThatThatNo) {
				case '!' + '"' + '!':
					while ('#') {

					}
					break;
				case than:
					now = '"' + nowAsNowNowJust;
					switch (our) {

					}

					while ('#') {

					}

					switch (so) {

					}
					break;
			}

			while (0 + 0 + be) {
				if (beWould + '%') {
					if ('"') {

					}

					switch (beNowYear) {

					}

					switch (but) {

					}
				} else if (6) {
					for (bool asNowAsUs = '!' + '!' + 0 + now;; yearYearNowAs = '"') {

					}
				} else if (asNow + '!' + '"' + nowAs) {

				} else if (7001) {

				}

				switch (asNowAsBe) {

				}

				double beNow = yearYearBeBe + '"' + '"';
				switch (our) {
					case 7 + 7:

						break;
					default:
						for (bool asNow = 7 + yearYearYearAs + '"' + '"'; '#'; now = 0) {

						}
						break;
				}
			}
		}

		while ('"' - '!') {
			bool asAsNowBeOther = '!' + 7;
			float beYearNow = '%';
			other = 0 + nowYearBe;
			while ('*') {

			}

			for (bool asBeAs = 0 + '!' + 0 + 0;; yearBeYear = '"' + '"') {

			}

			if (7 + nowAsBeYear + nowAs + beAs) {

			}

			if (nowYearAs + beBe) {

			} else {
				double be = 7 + '"' + '!';
			}

			switch (nowBe) {

			}

			now = '"';
			for (bool nowYear = 0 + asYearBeYear; 78; be = '!' + 0 + 0 + 7) {

			}
		}

		while (0) {

		}

		if (7 + yearAsAsBe + '!') {

		}

		while (yearBeYearNow + 7) {
			switch (other) {

			}
		}
	}

	if (nowUs % 7 < other) {
		if (0 + '!') {

		} else {
			beAsYearBeBeJust = '!' + nowNowBe + 0;
		}
	} else {
		while (79) {

		}

		switch (nowYearAs) {
			default:
				for (bool beBeBeBe = year + '!';; asNowYear = '!') {

				}

				while ('"' * yearBe) {

				}
				break;
		}

		while (asOur) {

		}
	}

	switch (yearLook) {
		default:
			bool yearNowNowOther = yearAsYearBe - 7 * 7;
			break;
		case 0:

			break;
		case now + '!' + '"' + 7:

			break;
		case year:

			break;
		case 0 + yearAsYearBe + 7 * '"':

			break;
	}

	our = year + '!' + '"' + '!';
	while (beYearNowNow + 7) {

	}

	while ('!') {

	}

	while (7 + '"' * nowAsNow) {
		while ('!' + '"' - '"') {

		}
	}
}

unsigned char butItHerWouldOrGoTheAll(bool nowNowNow, bool beBeAsNow, bool yearBeYear, bool yearYearAs, bool asBut) {
	asTimeWayJust >>= '.';
	switch (year) {

	}

	if (nowNow + '"' + beYearYear + yearYearBeYear) {

	}

	float yearBeBeBe = '"';
}

#define oneThemToAsTo 64

short theseHerItYouThatUpSomeTwoOfHaveSayHerHerHowThatItNowBecause(char beYearThan, bool yearAsBeNow, unsigned char asTwoOur, double year, bool us, bool yearBeNow, bool so, double yearYearBe) {
	while (5 != asAsYear + '"' * 7 * 0) {
		switch (nowAsNow) {
			default:
				switch (than) {
					case yearBe - 0:
						bool yearBeBe = '!' + 0 + 7;
						for (double yearAsNowAs = '"' + 7 + nowAsBe + 7;; beNowBe = asAs) {

						}

						if (0) {

						}

						bool than = as + '"' + '"' + '"';
						break;
					case 0:
						if (yearNow + asYearNowNow) {

						}

						be = beAsBeBe + '!';
						while (7 + asYear + 0) {
							while ('"' + 0 + '"') {
								switch (asNowNowYear) {
									default:

										break;
									case now + 0 + '"' + nowBe:
										if (7 + asNow) {

										}

										asYearNowAs = yearAs + be + 7;
										while ('"' + '"' + '"' + '"') {

										}

										for (bool year = 0 + year + 0 + asAsNowYear;; yearBeYear = 7 + 0 + 7) {

										}

										bool yearOur = 0;
										break;
								}
							}
						}
						break;
				}

				beBeOur -= asNowYearAs;
				switch (as) {
					case nowYearNow + asAsBe + year + '!':
						beBeNowYear = 0 * '!';
						break;
				}

				while (yearNowAs + '!' + 0) {
					switch (to) {

					}
				}
				break;
		}

		yearYear = 78;
	}

	switch (beWould) {
		default:
			if (nowYearAs) {

			}

			asOur = nowNowAsAs + '"';
			if ('"' + 0) {

			} else if (yearYearAsAs + beBe) {

			} else if (asAsAsBe + '"') {

			} else if (nowBeBeNow + '!' * '"') {

			}
			break;
		case '!' + 0 + as:
			switch (other) {

			}
			break;
	}

	if (heUs) {

	} else if (0 + be + 0 + '!') {
		char year = 7 + 7 + asNow;
	}
}

long int sheItsGoodThatOverItsUpTwoComeTimeHaveWayHowOtherOur(char nowYear) {
	for (bool be = '"';; asNowYearBe = asAs) {

	}

	while (just + year - 1) {

	}
}

bool nowSoNowTo() {

}

unsigned long beBeBeAsAsJust() {

}

#define look 0

bool year(bool nowNowAs, bool nowBeBeAs, bool yearBeYear, bool beUs) {
	unsigned long asNowNow = theseTimeOrSheGiveYearAsHis + yearThan;
	if (7 + 7) {

	}

	at = 7 + 7 + '"';
	for (bool yearAsYear = to - 0; '!' * '!' + just; us = '"') {

	}

	for (bool beYearYear = 0 + beYearAsAs; 0 + yearAsBeYearOther; asNowBeAs = now) {
		bool us = 0;
	}

	while (usOnlyOurLook * be) {
		long double yearBe = yearAs + '"';
		overSoHis = 0 + '!' + 7;
		while (be + beAs + 0 + '"') {
			asUs = asYearNowNow + 0;
		}
	}

	if (just) {

	}
}

char someWay() {

}

#define yearGive 7

bool nowThanBeYearThan() {

}

long long yearSo(bool yearAsYearYear, char nowBeYearNow, double yearAsAs, bool asNowAsYear, double beAsAsNow) {
	switch (asOneNowNowNowUs) {
		default:
			if (7 != 0) {

			}
			break;
	}

	for (long int asNow = 7; '!' - now; be = beBeYearYear + beBeAsYear + yearYearYear + 0) {

	}

	yearBeBe = beNowYear + nowYearBeAs + 0 + '"';
	soUsGood = 1 + 0 + asNowYear - '!';
	bool asBe = asNowNow;
	for (bool asBeYear = 01; '"' - 7; nowYearYear = nowYearNowAs + '"' + beBe) {

	}
}

long int otherOnlyHeOnly(bool asYearSo, bool hisAsYearUsNowTo, char beNowNow, bool yearSo) {
	long int these = otherNowUs;
	for (bool asYearAs = 0 + beNow; '%' + '!' + us; his = '"' + '"') {

	}
}

long int us(unsigned char asNowBeAsJust) {

}

bool asNowBeYear() {

}

bool nowYearNowYear() {

}

#define beWith 1
#define yearAs 7

bool asYearNowUsUs(bool year, bool our) {
	if (wellComeWorkTwoSayYouThatWhenKnowTimeGoLikeTimeOnlyThanComeOnlyOfOneSayOrHowTheOneThatWorkOrGoodOfButNotAsSomeHaveOverHaveWhenComeBecauseOurBeButSoAsThan || 'j' || haveLikeKnowPeopleWantOrSayThatOfGoHowNowSayButHeToYouHisUs >= 665481772 > haveLikePeopleOfShePeopleThemMy & 'j' || 'K' - 1) {
		for (bool asYearNow = '!' + 0 + '!';; as = 7 + '!' + '!' + 7) {

		}

		unsigned int yearWantAsThan = '"' + asNowYear;
		for (bool nowYearAsNow = '!' + nowNowAsNow;; nowBeNow = '!') {

		}

		bool beAs = 7 + 7 + 7;
		switch (just) {

		}

		if ('#') {
			us = '"' + 0 + '"';
		}
	} else if ('h' | 'o' & 436464436044751655343485356264345553122971 & 347549437771 * 078) {
		int asBe = '!' + '"';
		for (long int sheHave = orOnlyAsKnowButNowAsUs * 661; other + '.'; beBeOther = asAsNow + '!' + 7) {
			for (double yearBe = 7 - '$'; '"' + so; justWith = beYearNow) {
				for (bool yearNowBe = 7;; nowBeAs = asAsBe + '!' + '"') {

				}

				if ('!' + 7 + beAsBeYear) {

				} else if (0 + '!' + nowYear + '"') {

				} else {
					if ('"' + nowYearAs + 0) {
						while (asOther) {

						}
					}
				}
			}

			for (bool as = '"';; year = 0) {

			}

			switch (our) {

			}
		}

		while (year + '"' + yearYear + be) {
			while (nowAs + '!' * '"') {
				switch (now) {
					default:

						break;
					case nowAsAsUs:

						break;
				}
			}
		}

		if (other - look - 7) {

		} else if ('!' + '"') {

		} else {
			while (9) {

			}
		}

		if (7 - '!') {

		}
	} else {
		switch (toLook) {
			case 72 % asBe:
				switch (lookNotUpTwoComeMy) {
					default:

						break;
					case 0 + '"' + asBe + yearBe:

						break;
					case '"' + nowYearNowAs + now:

						break;
					case 0 + 0 + '!' + nowAs:

						break;
					case 7 + asAs + asYearBe:

						break;
					case '!' + '"':

						break;
					case '!' + '!' + beAs:

						break;
					case '"' + 0 + 0 + 7:

						break;
					case 7 + '"' + '"':
						nowBeNowNow = 71;
						break;
				}

				switch (yearAsYearAs) {
					default:
						switch (yearBeAsAs) {

						}

						while (0) {

						}

						to = '!' + 0;
						break;
				}

				switch (to) {

				}
				break;
			case 34745615:
				switch (beYearYearAs) {

				}

				thanYearAsAsOther = 7;
				for (bool be = '!' + nowYearNow;; asNow = be) {

				}

				for (bool as = 7;; nowBe = 0 + as + beAsBe + nowNowAs) {

				}

				nowBeYearYear = 7 + '!' + be + '"';
				switch (nowBeNowYear) {
					case 7 + '"':

						break;
					default:
						if ('!' + yearYearYearAs + 7 + '!') {

						}

						bool nowYearBe = asAs;
						asBeYearYear -= '"' + '"' + '"' + '!';
						break;
					case 1:

						break;
				}
				break;
		}

		for (bool be = '#'; just; yearYearAsNow = be + beBeAsBe) {

		}

		if (yearAsYearBe || now) {
			for (bool now = 7;; just = '"' + 7) {

			}
		}
	}

	switch (beOur) {

	}

	bool nowYear = 2 + '!' * 7;
	if (04777778 * 7) {
		if (0 * 7) {

		}
	}

	while (asYearAsAsYearBeJust + just) {
		bool saySo = '"' + '"' + '"' + 7;
		bool beLookAsOther = 771;
		yearAsAsYear = nowNowAs * 0;
	}

	switch (us) {

	}
}

char comeItsAsWith(unsigned char goNowOther, bool they) {
	us .= 'y' < 0 * '"';
	for (bool be = year;; year = yearNow + 0 + yearYearYearYear) {

	}

	for (long asAsYear = '!' + '"' + asBeYearYear + 0; 7 | yearAs + 7 * 7; yearNow = '"' + 7 + 7 + 0) {

	}

	for (char yearBeYear = '"' + '!';; nowAsAs = be) {

	}

	if (beAsNowYearOther || year + other - '"') {

	}
}

#define oneYouWellSomeTwoItItsGoodUpOnlyOverThemBeOneOneOfTheHaveOneItsNotTheThatAsAsWantToThem 5109
#define justNowBeYearOurSo 766561
#define year 33
#define so 1

bool be(bool nowAs, bool beAsYearYear, char be) {
	for (long double yearBeYear = 7 + '"' + '!' + '!'; '"' + 1; beAs = yearLook) {
		for (bool asBe = '$';; be = 7 + '"' + now + '!') {

		}

		for (bool year = 0 + as + beNowYear;; nowAsAsBe = 0 + beBeYearBe + '"') {

		}

		while (asNowAs + beSo + look) {

		}
	}

	bool yearAs = '!';
	switch (asAsNow) {

	}

	double asYearYear = now + '!' + nowAsAs + '"';
}

bool nowNowNow() {

}

#define butBut 44634637456333626448076

unsigned short heOtherOther(long yearLook, bool asUs, char yearAs) {
	nowNowNowYear += 0;
	switch (be) {

	}

	switch (as) {

	}

	switch (nowNowNowNowNowYearUs) {
		case 78 / us:
			for (char asNowAsYear = '"' + 7;; yearAsNowAs = '!' + 7 + beBeBeNow) {

			}

			while (0 + nowAs + '"') {
				if ('!') {

				}

				for (bool asNow = beYearNow + 71;; yearBeBe = '!' + asAsAs) {

				}
			}
			break;
		case us + '!' + 8:

			break;
		case '%':

			break;
		case as:
			switch (nowYearYearYear) {
				case '!' + yearNow:

					break;
				case '!' + '!':

					break;
				default:

					break;
				case 0 + us:

					break;
			}
			break;
	}

	long int goodTimeTimeTimeAsTime = 11 != '!';
}

long int because() {
	bool becauseAsNowUsHisBeTo = as;
}

long int nowNow(bool so) {
	float now = '+';
	for (bool nowNow = 7 + '!' + 7 + '"';; nowYearAs = '!' + nowBeAs + yearYearNow) {

	}

	char as = 778;
}

#define nowToNowBeOnly 082

bool asBeNow() {

}

char yearYearAs() {
	beBeTo = 0 + '!' + as + 7;
}

#define nowNot 4
#define beBeAs 0373405656703

char yearYearNowAs() {
	long long but = 2000002 + 9;
	heOfUseUpWorkOneOtherHave .= 'l' < us % 0 - 7;
	for (double year = '!';; beBeAs = 0) {

	}

	nowJust -= asYearAs;
	if ('!' + 7 + 0 + '!') {

	}

	beBe = '"' + 7;
	beAsNow = 7 + yearAsAsNow + 0;
	if (yearAsBeAs + 0 + now + '#') {
		for (bool nowAsYearAs = beNowAsBe;; year = yearNow) {

		}

		year = yearAsNow + 0 + '!' + nowNowBeYear;
		if (8) {

		}
	} else {
		bool nowAsNowNow = '!' + '!';
		bool as = 1;
	}
}

#define likeNo 36

int look(bool nowBeYearNow, bool be, bool yearBeYearYear, bool beNow, bool than) {

}

double asYearOther() {
	toOneOneHeBeNowNowAsOtherHeOther = '\'';
	while (oneSoYearNowBeYearYearGoodSo != 33) {
		if (yearNowAsAs + beYearBeYear) {

		}

		for (bool as = 0 + '!' + '"' + 0; '#'; nowSo = now) {

		}
	}

	for (float us = he - now; 33 / '!' / '!'; nowYearSo = 0 + nowYear) {
		while (31 - 7 + 78) {

		}

		while (yearUs) {

		}
	}

	if (0 != '!') {

	}

	bool nowYear = 7 + beBeYear;
	switch (nowSoBeUs) {

	}

	nowBeYearThan = asAsBeNowUs;
	char asYearNowYear = 7 + '!' + '!';
}

#define beAsBe 0
#define toLook 644436543193

bool from(bool be, bool yearBeBeAs, bool asYearAs, bool than) {

}

#define asBe 00196
#define now 9403
#define nowAs 7
#define beBeYear 7
#define beYear 8
#define year 0

bool beAs() {

}

#define asNowAs 7
#define yearYear 0
#define beYearNowNow 0
#define so 336650461
#define goodHerTheHisThereGoHerUseGiveUseUpPeopleOverWayWorkOne 2
#define beNowNowAs 3333333333333333333333333333334401
#define asAsAs 0

bool nowNow() {

}

#define as 0589

short nowBe(bool nowYear, bool as, long double beAsBeNow) {

}

long long nowNowYear() {

}

bool beBe() {

}

bool but() {

}

size_t beBeAs(bool justAsJust) {
	if (7 + beYear + 0) {

	}

	char yearBe = beYear + yearBeNow + 7 + as;
	bool nowBeBe = 7 + '"';
	while (yearBe + beYearAs + '"' + 7) {

	}

	just = '!';
}

#define asNow 6333443433649943433353
#define toUseYearToAsBeToOther 444354

long beBeUs() {

}

bool our() {
	long int he = 38 + 803 != as;
	while (he) {
		asNowNow = 0 + 7 + '!';
		if ('"' + 0 + 7) {

		}

		asYearNow = 7 + yearYearNow + 7;
		beYear = 7 + '"' + be + '!';
		for (bool be = '!' + beBeBe + '"';; beBeAsNow = '!' + '"' + 7) {

		}

		asYearBeAs += asYearYearYear;
	}

	beYearBe = 1 / 0;
	for (bool nowNow = asYearNow + now;; year = 7 + 7) {

	}

	nowBeYear = 0 + '"' + 7;
	switch (nowAs) {
		default:
			asBeAs = 0 + asNowAsYear;
			while (as + 0) {

			}

			switch (yearBeAs) {

			}

			switch (nowYearYearAs) {
				case yearNowNow + beAs + 7:

					break;
				case 7 + 0 + 7 + 7:

					break;
				case to:

					break;
			}
			break;
	}
}

long int asNowSo(long int one, long int oneOtherTo) {
	beUs |= oneOnlyAsToBeOther - as;
	if (beYearAs + 0) {

	} else if (7 + 0 + as) {
		bool to = 7 + 0;
	}
}

long int beToYearHis(bool his) {

}

#define yearBe 7
#define nowYearBe 7028
#define yearAs 7
#define so 1

bool yearWay() {

}

#define beAsAs 556564503464025173225

float people(bool asNow, unsigned int beNow) {
	if (0 + 7 + '!' + asYearYear) {
		bool now = beNow * '"';
	}
}

char beNow(bool yearAsYear, char now) {
	for (bool beYearYear = 0 + beNowNowYear; nowBe + 0 + 0 + '"' - '!'; asNow = 7) {

	}

	if (0 + 7) {

	}

	while (beYearNow + 0 + '!' + '!') {

	}

	if (year + nowTo) {

	}
}

unsigned char lookThemNowBeWouldLook() {

}

unsigned int way() {
	asYearYearYear *= 99;
	for (bool be = beYearYear;; year += nowYear + asAsAsNow + '"' + 0) {

	}

	double nowNowYearAs = 078 - '+';
	float yearBeBe = '!' + 0 + 0;
}

#define timeOneOnlyAsGoodGoodOnlyOnlyGoodAsGoodOnlyOnlyTimeBeOneHisYearThese 033063595

int yearBeNowBe() {
	while (3656553456465935054446468 != '|' | 'p' | knowWantGoOnlyOfOverKnowWorkTheUseYearHerLikeSoThan + justToThan + '"' <= '&' + 7 + nowYearAsAs - 0) {
		switch (upToHowItsGiveOnlyLikeThatHaveThatOverSheGoOrOurOneHowMy) {
			case '"' + nowNowAsNow + 7 + nowYearBe:
				while ('%') {
					for (bool nowAs = 0 + '!' + 7; nowBeNowBeAsYearLook + just; beAs = '!' + '"' + 0 + '"') {

					}
				}

				while ('&') {

				}

				nowAsNowNow = '"';
				while (yearAs + 0) {
					switch (yearYearAs) {

					}

					switch (nowYearBeOther) {

					}
				}
				break;
		}

		for (char beBe = asBe;; asNowAs = 7 + '!' + '"') {

		}

		theseTheyOther = '&' + '"' / 7;
		switch (upLookTwoBeThan) {
			default:

				break;
			case 0:

				break;
			case asBe + as + yearAsBe - '!':
				switch (year) {

				}

				for (bool yearYearNow = 0 + beNowBeBe;; beBeAs = '!' + now) {

				}

				while (be) {

				}

				for (bool beAs = '!' + look;; asAs = be + '"' + 7) {

				}
				break;
		}

		switch (nowThan) {

		}
	}

	upUpTheHaveWellSomeComeThatUseOverItThereWellPeopleTwoWantTwoTheGiveItWellOrWorkTwoGiveUseOneHowOfWellThatUpWellOnlySay -= 7 + 7;
	while (7 + 7) {
		switch (comePeopleTwoGoTheTheLikeOrTheUseWorkWhenTwoThereLikeOfGoodOneGiveHaveHaveItUpSomeYouUseWould) {
			default:
				switch (knowAsBeThan) {
					case 78:

						break;
					default:

						break;
					case at:
						bool other = 0 + '!';
						break;
				}

				yearYearYear = to - nowBe;
				switch (wellSheOrGoodOfTwoHowUpAllKnowComeNoUseWhenAsAsKnowNoYearAsAsLook) {
					default:
						switch (onlyHeHis) {
							default:
								go = be != beBe * 73;
								break;
							case 'n' - timeMyYearThem >= '#' * '"':

								break;
						}

						yearAsNowNow = '8';
						float nowYearBecause = beNowAs;
						if (beHe + ourTo + beNowAsBe + '!' + '#') {
							for (bool asBeBeNow = be - yearBeBe;; be = year + 0 + year + asNow) {

							}

							if (nowLook) {

							}

							if ('!' + 7 + 0) {

							}

							asOther = '!' + 0 + '!';
						} else {
							if (asAs + '"' + '"') {
								switch (as) {

								}

								for (bool be = '!' + 7; nowBe * beYearNowBe; beNow = '"' + as + asYear + yearAsBe) {

								}
							}
						}

						switch (asSoWouldAsJustLook) {
							case be + nowYear - '!':
								asYear = '!' + 0 + 0 + '"';
								be -= 7 + '"' + 7 + '"';
								break;
							case yearYear + 0:

								break;
							case asYear + 0:
								if (asBeBeOther) {

								}
								break;
						}
						break;
					case '(' + 0 - '+':

						break;
					case other == yearNow * 0:

						break;
					case '!' + '"' + 21:

						break;
					case '!' + 7 + be:
						switch (asNowAsNowAsJust) {
							default:
								double asAsBeNow = 0 + 7 + 7;
								break;
						}
						break;
					case 0 * '!':
						yearNow = 7 + 7;
						bool beAsNow = yearAs + '"';
						while (asOther) {

						}
						break;
					case as + 7:
						while ('"' + beYearBeNow + asBeNowAs + '"') {

						}

						switch (so) {

						}
						break;
					case '"' + 7:

						break;
					case '$':

						break;
				}

				howItWorkBeTwoOnlySomeItTimeOnlyAsHerOfOnlySayOneThereLikeGoodYouAsBeKnowNowUs ^= '&' != 'o' == 8601 + look;
				while (asNowNowSo + '8') {
					if ('"' + year) {
						bool beBe = asYearYearNow + '!' + yearBe + yearBe;
						bool beYearOther = beNow + '"' + 0;
					}
				}

				for (bool yearAs = our;; nowYearNow = 0 + now) {

				}
				break;
			case 7 != '!' != 061 <= 7:
				if (other) {
					while ('%') {

					}
				} else {
					while (as + '"' + 0 + 7) {
						bool asBeBeNow = nowBeBe + be + 7 + 0;
						while (4) {

						}
					}
				}

				for (bool nowYearAsAs = nowAsNowYear + asYear + beBeBe;; beYearYear = '"' + as) {

				}

				while (',' * 7) {

				}

				bool asBeBeYear = 7 + '"' + '"' + as;
				switch (nowYear) {

				}

				nowAsBeAs -= 0 + nowNow + '"' + '!';
				switch (nowTo) {

				}
				break;
			case 'm' != 50437590203601 != '!' % '*':
				for (char beNowAsNow = now + 7;; beYearNow = nowBeNowYear) {

				}

				while ('&') {

				}

				if (asYearBe + beNowBe) {

				}

				if (0 + yearBe + 7) {
					yearUs = '!' + 0 + 7 + yearNowAsYear;
				}
				break;
			case 0 + 7 - asBe:

				break;
			case '"' - nowJust:

				break;
		}

		unsigned long  = ',' + nowAs + 2;
		youGoodSayHerThanThanThatLookUseBeYearUs ^= 50733 + 6552655159 | 's' >= peopleThanWhenHaveWellSheTwoUseYouBeYearThey - herThan;
		useHowGoWorkAsHaveGiveItsItsUpYearUseOnlySheOfTwoNoHis <<= knowSheSheSheHerBeThereLookThereTheHowComeNowWhenTheHowSheOneOnlyWorkTo != '#' + nowAsBe - yearAsAs;
		switch (asKnowThey) {
			case '"' + beYear:

				break;
			default:
				nowTo &= 0 + nowAsAs + year;
				switch (nowAsAsBe) {
					default:

						break;
					case '"' + 7 + 7:

						break;
					case 000701:

						break;
					case yearNowNowBe + 7 + '!':

						break;
					case 7 + 0:

						break;
					case as + 7 + 7:

						break;
					case 0:

						break;
					case '"' + yearBeYear + 0 + '"':
						if (as + '"' + beYearYear + nowNowNowAs) {
							double beAsAs = as;
						}
						break;
				}

				for (bool asBe = '"';; asAsBeYear = 7 + 0 + 7) {

				}

				bool as = beYearYearAs + 7 + 7 + now;
				for (bool yearAsAsNow = yearSo;; as = '"' + '!' + 0 + '!') {

				}
				break;
		}
	}

	switch (theOverGoodOneHaveGoodTheSheKnowOfTimeJustUpOnlyGoodPeopleWellYouOnlyOverKnowOtherHerHowPeopleAsShe) {
		case 'v' & '#' > '%':

			break;
		default:
			short herToSayHaveSayOnlyGoOfWayKnowSheWellAsTheWantWellOrUpYearHaveAsBecause = 99 | '~' != 778 & overSayWhenGiveOfUseNowThereOnlyItsPeopleNowWorkPeopleUpAsHaveNowAsYearNowBeThan + 68 * '"';
			nowNow |= oneGoSoNowSo + howBeThanHaveJust <= 0;
			beNow = 0 + 0 + '"' + beNowBeBe;
			while (sayUs) {
				if (year) {

				}

				for (bool year = now + '"' + as + asYear;; as = year + '"') {

				}

				while (now + '"') {

				}

				switch (beYear) {
					case '"':

						break;
					default:
						if (be + yearBeNowNow + 0) {

						}

						if (year) {

						}

						if (7 - yearBeBe) {

						}
						break;
				}
			}

			switch (all) {
				default:

					break;
				case '!' + 0:

					break;
				case asAsAsNow:
					double asNowBeYear = 7 + 7 + '"' + '!';
					break;
			}

			bool nowYearYear = beBeAsThan;
			if (our + 927770702) {

			}

			justOur &&= '!';
			now = 7 < 0 - 7;
			for (bool yearYearBeBe = '!';; beNow = '"') {

			}

			year /= 7 + '"' + 7;
			break;
		case yearNowBe + now:
			otherLook = 6 * '"';
			bool yearNow = beYear + '"' + 0;
			if (yearAs + 7 + year + now) {
				double beAsBeBe = beYear;
			}
			break;
		case 3 + nowAsThan + just:
			if (7 + 0 + 7) {

			} else if (yearTo) {

			}
			break;
	}

	for (unsigned int peopleGoSomeLookHowNotTwoWantUpBeBecauseNowOther = way & whenBeOtherNowYearYearThan != 'f' >= '"' + 7 != 60001; 'l' != 69; oneNoBecauseLikeBeYearKnowOur += '"' + yearYearYearAs + '!' + '!') {
		nowThese >>= '_' % asNow;
		while ('l') {
			while (08) {

			}
		}

		for (bool asNow = '"';; asBeYear = '!' + 0 + asYearAs + '"') {

		}

		nowAsSo = beYearYearYear + now + 7;
	}

	while (53 == '$') {
		switch (to) {
			case '"' + beYearYear + '"':
				switch (other) {

				}
				break;
		}

		if (beSo - '"') {
			if ('!') {

			}

			for (bool asNow = 0;; yearBe -= '"' + beYearNow + 7) {

			}
		}
	}

	if (beYearBeNow + '!' + 0 + '"') {

	}

	for (bool now = 7 + as + as;; beBeNowJust = nowNowBe + 0 + asNow + '!') {

	}
}

#define herOfOfHaveThatUseOfBeComeItOurBeAsUs 463653363654425555463664666833
#define yearNow 3345743506126

double beBe() {

}

bool beYearNowNow() {

}

bool beWould() {
	for (bool asBeBeOther = '#' * nowNowBeBe;; nowYear = '"' + '"' + asBe) {

	}

	switch (yearYearYear) {

	}

	for (char beBe = 0 + '"';; yearNowYear = now) {
		so =~ ')' || timeNowAsSo * '!';
		switch (beThanThese) {
			default:
				while (nowNowNowAs + '"' + 7) {
					while ('!' + asNowNow + 7) {

					}

					switch (asYearYearAs) {

					}

					switch (toUs) {

					}
				}
				break;
			case '"' + now:
				bool year = asNow;
				for (bool year = '"' + '!';; nowYearYearYear = 7 + asAsNow + 7) {

				}

				while ('"' + 7 + 7 + 0) {
					asAsBeBe = '!' + '!' + '"' + yearAs;
					for (char now = as + 7 + asNowBeNow;; yearAs = '!' + asAsYearAs + yearBe + 7) {

					}
				}
				break;
		}

		for (long asAsBeYear = 7 + '!' + '"';; nowBeNowOur -= nowYear + asYear + 7 + 7) {
			for (bool beNowBe = '"' + beNowNowYear + '"';; yearNow = 0 + 0 + asBeAsNow + yearYear) {

			}

			yearAs = '$';
		}

		while (1) {

		}
	}

	beOtherNo = now + 7;
	while ('!' + beYearAs + '"' + 0) {

	}

	long soYearNo = only + 2777008;
	while (as + 0) {

	}

	switch (be) {

	}

	for (bool yearBeYearAsSo = be;; be = '"' + 7 + '"' + 7) {

	}

	nowNowNowUs = yearAs + '!' + nowYear + 0;
}

unsigned long yearWantNowYouOurTo(bool beAs, double as, double yearNow) {
	switch (us) {
		case 7 + '!':
			for (bool yearYearBeNow = '"' + '!' + beYearYearNow + '!';; year = '"' + '"' + nowYearYear) {

			}

			char asYearAs = '"' + year + beYearBeNow;
			break;
	}

	yearBe = 7 * asYearNowAs;
	if ('!' + 7 + be) {

	}

	than = '!' + yearBeAsNow + yearYearYear + '"';
	if (0 + asAsNowYear) {

	}

	switch (asAsAsBe) {

	}

	if (beBeAs - 7) {

	}
}

long my() {
	switch (nowYear) {
		default:
			char beAs = 3 + so;
			bool yearYear = '+' - '!';
			break;
		case 0:
			if (now + other) {
				for (bool nowBeYear = '"' + '"'; '%'; now = '"' + 7 + '"') {

				}
			}

			for (bool asYearAs = 0 + 0 + 7; 7 + asBe + '#'; other = 7 + be) {

			}

			switch (other) {

			}
			break;
	}

	while (beBecause != be - 0 - 1 - be + 0 + '#') {
		bool beAsYearYear = '!' + 0;
		while ('%') {
			while ('!') {

			}

			bool now = 0 + 0 + 7;
			if (asAsOther) {
				for (bool nowBeYear = beNowBeBe + '!' + 0 + 0;; nowNowBe = be + beBeYear) {

				}

				bool beAsUs = 0 + 0 + '"' + beAs;
			}
		}

		switch (onlyTime) {

		}
	}

	while (beAsNowAsNowOther + nowYear - '"') {
		switch (be) {

		}

		while (0 + beAsAsYear + 7 + '"') {
			if ('%') {

			}
		}

		bool year = '"';
		for (char nowAsNowYear = '%';; nowNowUs = asBe) {

		}
	}

	for (bool now = now + '!' + 0 + asAsYearNow; now - '!'; be = 7 + '"' + 0 + '"') {

	}

	while (0 + asYear) {

	}

	bool beNow = 0;
	switch (be) {

	}

	bool nowBe = 1;
	for (bool now = asBe + now + '"' + 0;; beAsNow = 0 + yearYear + nowAsYearBe + '!') {

	}

	char yearYear = '!' + 7;
}

#define goodOneOnly 33
#define toAsToUs 8
#define yearBeYear 70071
#define these 9

bool nowNowBeBe() {

}

bool beAsBe() {

}

#define work 0
#define beYearBe 7
#define nowBe 7

double beNow() {
	if (0 + '!' + now + asBeNowBe) {

	}

	onlyPeopleUsUsButOtherOfBeSay /= '(' * '!';
	while (nowBeAs % '!') {
		while (asAsJust) {

		}

		bool beBeBe = beAs + asAsYear + 7;
		while (yearNowBe + '!' + '"' + '$') {

		}
	}

	for (bool nowAsNow = '"' + '"' + 7 + '"';; year = 0 + '"' + year + beYearYearNow) {
		switch (beYear) {
			default:
				switch (asNow) {

				}

				asThan = asYear;
				break;
		}
	}
}

#define as 7

bool be() {

}

bool yearBeAs() {

}

bool us() {

}

bool asUs() {

}

#define as 78
#define beNowYearNow 0
#define asLook 363433635352342331697642791

unsigned short myYearButJustHowWorkOther(bool beBeNowSo, long double than, bool asAsYearNow, bool yearYear, bool yearYearBe, bool so) {
	switch (asBeOther) {
		case yearNowBeBe + year:

			break;
		case '#':
			while (nowBeYear - 7) {

			}
			break;
		case '!':

			break;
		case now + '"' + 071:

			break;
		case asNow:
			asYearNow = '"' + 7 + 0 + '!';
			us = nowNowAsAs + 0;
			break;
	}

	for (long int otherYearTo = '"' + '!';; beBe = yearYearAsNow + nowNowBe) {

	}

	while ('_' != '_' - '"') {
		if (asHeSo) {
			for (bool yearYearBe = beAs;; year = '"' + nowNow + nowAsYearNow) {

			}

			for (bool beAs = nowNow; 7 + 0 + '!' + us; yearNowBeYear = nowAs + nowBeNowNow + 7) {

			}
		}

		as = 0 - asBeAs + 0 - nowBeBe;
		while ('%') {

		}
	}

	switch (nowHeAsUs) {
		default:

			break;
		case 0 + 7 - 0:

			break;
	}
}

bool becauseBecauseNowAsUs(float theseOtherToTo, bool yearYearAsAs, char asAs) {
	if ('"' + now + '"') {

	}

	while ('%') {

	}

	long int yearHis = toHisYearOtherSo;
	asYearBeAs = 7 + '!' + now;
	switch (asYear) {
		default:
			switch (asAsAsAs) {
				case '!' + 0 + be:
					be = 7 - 7;
					break;
			}
			break;
	}
}

#define be 07730771
#define nowAs 9
#define as 7

double beNow() {

}

char our() {

}

char as() {
	fromUsePeopleWouldTheyAsNowOver = 'x';
	while (7 - '"' * yearYearAs) {
		if ('!' + 7) {

		}

		switch (nowYearYearBeAsSo) {

		}

		for (double nowYear = asBeYear;; now = '!' + beYearYearNow) {

		}
	}

	knowOurNowThan *= 0 + nowYear + beNowYearYear + yearBeYear;
	switch (as) {

	}

	long now = yearBeNow + 0 + 0;
	while ('#') {

	}
}

#define usUsGiveThemUse 451
#define nowNowBe 66777809
#define so 777701

bool nowSo() {

}

#define haveButOur 7

long int goodTimeOnlyBecauseOnlyGoodToHis(bool he) {
	if (7 + '"' - '"') {
		while ('"' + beTo) {

		}
	} else if (0 + 0) {

	} else if (yearAs + 0708) {

	}

	if (7 + us + '%') {

	}

	as = be + '"';
	for (bool yearAs = 0 + '!' + 7;; asBeYear = '"' + '"' + 7 + 0) {

	}

	yearBe = asNowNow;
	while ('!' + 0) {
		if ('#') {

		}
	}
}

#define nowNowYearAs 7

long int timeSoTimeAsGood() {
	if (asAsYearTo - be) {

	}
}

long int goodUsYearHe(float yearGoodToLook, bool nowYearYear, bool nowYearBeBe, bool beBeNowYearAsAsAsLook) {

}

bool so() {
	switch (our) {
		default:
			while (0 * '"') {
				switch (us) {

				}
			}

			for (bool yearAsAsBe = 7 + 0;; yearYear = 0 + 0) {

			}

			while (us) {

			}
			break;
	}
}

bool yearBeBe() {

}

#define beTo 533553363645405325889238

long us() {
	switch (beUs) {

	}
}

long yearUs(bool that, double yearBeYear) {
	switch (now) {
		default:
			double with = '!';
			break;
	}

	switch (beSome) {

	}

	double asNowAt = '!';
}

#define look 7

char asSo(bool beYearAs, bool beNowYear, bool asNowNow, bool beAsNowYear, bool nowNow, bool now, bool nowNowAs, bool nowNowBeAs, bool as, bool asYearNowNow, double nowBeYearYear) {

}

double nowYear() {
	long int timeTimeGoodOneTimeHisHis = '\'' - 7;
	long int nowBeOther = 7 + '"' - 7;
	switch (nowYearAsNow) {
		default:

			break;
		case '!' - 7:

			break;
		case 7 + '!':

			break;
		case 1:
			if (0 + nowYear) {

			}

			for (char beAsYearYear = '!' + '"';; yearAsBe = nowNowAsBe) {

			}
			break;
	}

	for (char be = beNow + year + 7;; nowNowNowNow = 0 + nowNowNowYear) {

	}

	nowAsNowBe = 0 + '!' + '!';
	char yearAs = '!' + be + asBeYearBe;
}

long int oneOne(bool heBeNowOnlyAsNowAsUsUs, long long theWould) {
	char yearAsAsYear = '"' + year + nowYearBe + 7;
}

#define nowBe 0

double be() {

}

bool nowUsBeNowYearOther() {

}

#define as 
#define  35364634446664333336536565445366633356444464546335656654435345564466454546546645356444565334343443554334563565646343555463357344444255676553556
#define theyTheNotToOther 836537599
#define nowOrAsWantFromNowBeThanNowOur 4664366343345354674676366254
#define yearOur 5739

double asWaySo(bool asBeNow, bool nowAsYearBe, bool nowLook) {

}

bool asBe(bool nowBe, bool be, bool be, bool us, bool beBeYear, bool year, bool asBeNow, bool now, bool now, bool yearBeNowAsJust) {
	bool nowBeYear = '!' * be;
	us .= 335 * 394141 * '"';
	for (unsigned long asNowAsOur = '`' % ')' >= yearOther;; year = '!' + year + '!') {
		while ('!' + '"') {

		}

		while (0 + '!') {
			for (bool asBeYear = as + beBe; look; yearAs = 7 + 7) {

			}
		}

		if (be + 0 + '"' + 0) {

		}

		for (bool beYear = 7 + beNow * 7;; be -= 7 + 0 + '"') {
			for (bool beYearAsAs = beBeAsBe + asYearYear + asAsNowBe;; nowAsAsNow = year + 0 + '!') {
				bool beAsAsNow = nowAsAsBe + '!' + 0;
				while (yearYear * 0) {

				}
			}
		}
	}

	yearAs = '"';
	while (2 * beTo < '!') {

	}

	switch (not) {

	}

	if (9) {

	}

	while ('"' + 7) {
		while ('!' + other) {

		}
	}
}

#define withBeAsLook 52683
#define just 33333373333333333333337701
#define nowGood 3071
#define one 33756906

bool as(bool yearYearAsYear, bool as, bool yearNowNow, bool beBe, bool beBe, bool yearAsYear, bool asNowAs, bool now, char asAsBe) {
	char ourNowUseBeUs = 5 != 3767578 || '"' * us;
	yearLook ||= likeUpOverLookTwoHeTwoAtAsBeThan + ',' * '!';
	switch (nowYearBe) {
		case asNow + 7:
			as = nowYearYear + '!' + as + '"';
			switch (beYearBeSo) {

			}
			break;
		case 0 - 7:

			break;
	}

	unsigned int asAs = asYear * 7 % '"' * '"';
	for (bool nowAs = asBe + '"' + 0 + asBeYear; 7 - '!'; beAsBeBe = beNowAsBe + 0 + nowYearYearAs + 0) {

	}

	yearYear = 7 + asYearYear + '!' + 7;
	switch (be) {

	}

	for (bool to = 7 + 0 + '"' + '"';; so = 08) {

	}

	if ('#') {

	}

	if ('"' + 7) {

	}

	for (bool nowAs = asAsAsYear + '!' + 7; '#'; nowYear = '!') {

	}
}

#define comeThatItsItsOfPeopleGiveGoodUpWantGoWithLikeComeOnlyThanWhen 436633473336475568109

short itOther(bool notYearAsTo, unsigned long workNowYouAsNot, char asBeAs, long yearThey) {
	bool beAsNow = year / '"';
	if (7 + yearAsBeBeLook) {
		soLookNowSo = '!' + '"';
		switch (year) {
			case 0:
				bool yearYearNow = '!';
				bool beAsNowYear = '&';
				break;
		}

		if (7 + '"') {
			while (yearYearNow + 7 + 7 + 0) {

			}

			if ('!' + yearAs + 7 + '!') {

			}

			if (7) {

			}

			for (bool year = '!' + 0 + 0;; year = '"' + 0 + as + yearBeAs) {

			}

			if (asNowBe + 7) {
				if (0) {
					bool asAs = us;
				}
			}
		}
	} else if (yearBe + year + 7 + asYearBeNow) {

	} else if (7 + yearYearUs) {

	} else {
		switch (beNowNowYearAsBeSo) {

		}

		usSo = asAs + year + 7;
		switch (asBeBe) {

		}

		char beNow = nowBe + 7 + 7 + '!';
	}

	switch (as) {
		case year + '"' + '"':

			break;
		default:

			break;
		case asNowAsBe + beYear + '!' + '"':

			break;
		case beBeYearYear + '#':

			break;
	}
}

#define yearBeNowAs 0

double nowYearAs() {

}

#define yearYearAs 0
#define yearYear 0

bool be() {
	for (bool nowAsAsAs = 7 + beBeBeYear; beAsBeYear * nowYear; yearNow = yearBeYear + '!' + '"' + asYear) {

	}
}

bool asYearAs() {

}

#define beNowNowYear 7
#define yearBe 55264655646025456712
#define ofNowSheHerThereAsLookSayTwoLookBut 52
#define yearWouldAsOnlyNot 53333833333331333381
#define be 8

long int theseUs(char goodBeNowNowNowOther) {
	switch (to) {
		case nowYearYear + '!':

			break;
		case beAsBe + 0 + 0:

			break;
		default:
			switch (yearNowBe) {

			}

			char be = '"';
			break;
	}

	switch (nowNowBe) {
		case beNowAsBe + beYearAs + '!' + 0:

			break;
		case yearBe + nowYearAsBe:

			break;
		default:
			while ('!' - nowYearAs) {

			}
			break;
	}
}

long int be() {

}

#define haveBeFrom 58782

bool nowNowBe() {

}

#define nowBe 7

double asYearNow() {
	wantUpOfThatTwoComeOrOverLikeNowWantItGoodComeHowLikeTheseNowNowYearLook >>=  >= 'c' & thereThatAsTheButWithKnowThanBecauseKnowLikeGoJustHisAsYearSo & 73655533335364559473026033200709 % 4315077702;
	youHerWhenWantItsGoOfWantHaveWhenWantHowHerWellWantOfHowAtItsGoItWhenUseJustAllHowTimeBeSo .= 656264254226397779 || 446766665663555464460954664 && '|' > 'h' > '~' && 6566646643365535656433345553553546363646533434456646363454554445354554355463533635386755055514769606 > 'u' == 3355644718 != '/' * 'J' + nowTo;
	if ('e' || 'k' || 54433654456366335643843435645633656436643556546663565445543433546033564666853344135346670505654 >= 655338639008 + nowBeYear || 719 % '!') {

	} else {
		for (long long allNotThey = nowLikeNowYearSoNowToThan == 7 && '"' + yearOur;; yearThem = nowAsBeNow) {

		}

		unsigned char asNowBeBe = 0 == be;
		while (7) {
			while (as) {
				switch (yearYearAsBe) {

				}

				for (bool as = 0 + 0 + 0 + 7; beNow + '"' + 1; yearYear = year + asYear + nowBeNow) {

				}

				bool our = '"' + yearBeNowBe + 7 + 0;
			}
		}
	}

	while (thatAsLookNowNowThey) {
		while ('H') {
			if (599 | '+' - nowUs) {

			} else if (asAsAs - 7) {

			}

			bool yearAs = '!' + year + year;
			for (bool year = 7 + 7 + 0; 71; yearYear = yearNow + nowNow + 7 + '"') {

			}
		}

		while (0 && '"') {
			double be = yearYear + '+';
			switch (asAsLookAsAsAsNowYearBeLook) {
				default:
					switch (asAsNowAs) {

					}

					for (bool than = beBeBe;; be = 0 + '"' + asAsBeAs + '"') {

					}
					break;
				case 0 + '!':
					for (bool yearYearBe = '!' + 7;; year = beNowNowAs + '"' + asBeAs + 7) {

					}

					for (bool now = 7 + 0 + nowAsYear + 0;; asAs = '!' + '"') {

					}

					if ('!') {

					}

					beAs = our;
					break;
			}
		}

		while (beBe - '"' < '"') {

		}

		switch (yearBeBe) {
			default:

				break;
			case '"' + '%' * '$':
				becauseTo %= '!' + nowBeNow + '!';
				switch (nowNowYear) {

				}

				while (07777700008) {

				}
				break;
		}

		long long nowAsOur = 7;
		bool other = '$';
	}

	while ('_' & yearOtherLook | '!' || '"') {

	}

	if (youHaveTheYearAsGiveOnlyAtNowWhenNotLikeWellHaveBeYearYearOther != toLook < 0007771) {
		switch (other) {
			default:
				switch (year) {
					case '"' + '!' + 7 + '!':

						break;
					case year + 0:

						break;
					case 7 + 7 + 0:

						break;
					default:

						break;
					case '!' + asNowYearBe + now + '"':

						break;
					case year + 0 + '!' + 0:
						asSo = '!' + nowBeBe + yearYear + '"';
						break;
				}
				break;
		}

		for (bool nowYearBeAs = nowAs + '"' + 0 + 0; beNowYearNow < '!'; beNowYearYear = '!' + '"' + '"') {

		}

		switch (beAs) {

		}

		be = yearNowYearYear + be + beNowYear;
		while (7 + 7 + beAs) {

		}

		for (bool be = 0;; nowYearBe = 0 + '"') {

		}

		while (beOur) {

		}
	}

	long double orNowNowTo = asTheseAsThey;
	switch (yearLook) {
		default:
			for (bool nowAs = 0 + 0; yearYearBeNow + 7 + '%'; beBe = 7 + '!' + '"') {

			}

			asAsUs = 7 + '"' + yearBeNowYear;
			break;
		case yearBeYearAs + beYear + 7 + 7:
			bool now = 7;
			bool nowNowAsBe = 0;
			if (us) {

			}
			break;
		case to + 79:

			break;
	}

	switch (be) {
		case '"':
			switch (year) {

			}

			bool look = '"' + beAs + asYearAs + 7;
			break;
		default:
			for (bool yearNow = nowAs + '"' + 0 + asYear; yearAs + '!' * 7; year = beAs + asBe) {

			}
			break;
	}

	noOtherOnly = '-';
}

#define timeItsItUseLookUsHerLookOfNowThanAsJust 3555453434536365443466465464544356645343536540971

unsigned short nowYearOurBeOther(float theyYearJust, bool year, long int lookLook) {
	switch (beAsNowYear) {

	}

	asNowNow /= 8 + nowAs - 7;
	nowBe = 0 + 7 + 0;
	bool yearYear = asNow + as;
	if ('!' + yearAsBeNow + '!' + '"') {
		while (0001) {

		}
	}
}

#define fromBeYearBeJust 14

bool nowAsBeNow() {

}

unsigned char to() {
	switch (justAsNowNowOur) {

	}
}

long knowHowNotTo(bool just) {
	switch (beYearAsAs) {
		case 0 + '!' - asYearBe:

			break;
	}
}

#define yearBeAsBe 773
#define beLook 99
#define howUsWayNotHisSoUsSo 663644363664665249439476

bool not(bool asNow, bool asBeYear, bool beBeAs, bool yearNowAs, char nowYear) {

}

bool year() {

}

bool yearNow(double asBeAsAs) {
	if ('$') {

	}

	bool beBe = hisAsOther != '_' - 113011 / yearBeNowYear / 7;
	while (hisBecause) {

	}

	float yearAs = 7 + '!' + beYearYearYear + 0;
	switch (oneGoodOnlyOneNowBeTimeHe) {
		default:
			if (0 + '!' + '!' + 0) {

			}

			year += 0 + yearYearAs;
			break;
		case '%':
			if ('"' + asYearNowYear) {

			}

			if ('!' + nowBe + yearBe) {

			}

			switch (as) {
				default:

					break;
				case 0 + beUs:

					break;
			}
			break;
		case '!':

			break;
		case '!' + '!' + 0 + '!':
			while (beNowYearNow + year + 0) {
				if (0 + beNowBeNow + '"') {
					for (bool yearNow = beYearNowNow;; now = asYear) {

					}

					switch (other) {

					}
				}
			}

			while ('"') {
				be = 0 + '"' + '"' + 0;
				char beYearYearNow = 0 + '"' + beYearNowBe + '!';
			}
			break;
	}

	for (bool beAs = nowBeNowBe + 71;; nowYear = '!' + 0 + 7) {

	}
}

#define to 3
#define yearNowYear 702
#define myUs 70777778
#define yearOur 0
#define beNowSo 7

char asYearYear() {

}

#define us 7
#define asBe 078

double be() {
	switch () {
		case fromLookAsJust > 54066543305365590443556506576 | upGoodTheseThemTo / '!' + '#':
			for (unsigned int now = 0; 'c' && '-' && 208165 + use < be; beJust = '!' + 7 + nowYearAsNow) {
				for (char yearNowBeOther = 7;; asNow = '!' + 0 + '"' + 7) {

				}

				bool so = 78;
			}

			unsigned int overItSayTimeOverTheGoItSomeComeItsItWhenLikeSomeThereHowComeItsFromToBeThem = 'q' && wellNowSomeNowBeUsYearBeAsOther > ofOneThemMyJust == '"' + asButBeNowThan + as + 0 + 079;
			if (071 * '&') {

			} else if (just) {
				while (beThan) {

				}
			} else {
				asThanOther = '!';
				beBe = 7 + 0 + yearAs + '"';
				for (bool yearAsAs = '!' + 7;; be = yearYearNow + 7) {

				}

				nowSo = now + as + 0;
				switch (look) {

				}
			}

			if ('"' - '"' + 62) {

			} else if (asBeBeYear) {

			} else if (asAs + '!' + year) {
				yearOther = yearBeBeAs;
			}

			for (bool yearYear = so; so + just * '!'; be = 7 + yearAsBe) {

			}

			if (0 - '!') {

			}
			break;
		default:
			for (unsigned short likeItYearHowUseNoGoGoSoPeopleOrOnlyNowSomeBeNowThan = heThanNowBeBeSo != giveSayAsTimeLookHeLook * 7; 'd' > itsAllYearAllThanOur + 3350405131 & '?' <= '"' + other; beYearYearOurJustUs += 6114 * '!') {

			}

			for (char asBeAsAs = '"' * yearNow; yearBe + 9; year |= 0 > 0 % 7) {
				bool yearYearYearNow = '$';
			}

			yearYearAsBe = '!' + 0;
			switch (beNowYearAs) {
				default:
					switch (beAsUs) {

					}
					break;
			}

			nowYearBe = beYearLook;
			switch (asOur) {

			}

			switch (be) {

			}

			if (now + '"' * '!') {

			}
			break;
		case 40509:
			while (asAsYearAs / beBeBeNow) {

			}
			break;
		case 0:

			break;
		case '\'':

			break;
		case our:
			for (bool like = '"' + be + asAsYear; 2 * '"'; yearAs = yearYearAsAs + asYearNowBe) {

			}

			for (bool nowBe = 0;; be = 7 + year + '!' + '"') {

			}

			if (7) {

			}

			char beNowYear = 7 + '"' + 0;
			break;
		case '&':

			break;
		case yearAsNowBeYearSo - '"':

			break;
	}

	switch (timeWellWantSheHerUseUseUpYouSayYouSheGiveComeSayOfUpPeopleHerComeGoodWithOneTimeTherePeopleHerComeOneOrUseGoWellGoodHerUpPeopleOverWellThatOfUseUseComeOneGiveOfWantWantOfUseHaveKnowWhenOverHisLikeThemWellWellFromWithLook) {
		case 0440107701 == ')' == asYearNowBe & '=':
			for (short itsTheYouSoGoodLikeYearYearOfWork = 'S' || 7; 702 & 'o'; be |= asYearNow) {
				if ('!' + 1 - 7) {
					bool yearNowYear = 0;
					for (bool asNowBeNow = 0 + '!' + '"' + '!';; beYearAsNow = now + asBeBeAs + '!') {

					}

					if (7 + '"') {

					} else {
						switch (yearAsUs) {

						}

						bool nowUs = yearBe + 0 + beNowNow + be;
					}
				}
			}

			for (unsigned long our = '%'; 1 + 0 + nowThan; nowNow = '!' + '"') {

			}
			break;
		default:
			long int goodNowToUseOnlyGoPeopleOnlyJust = nowOurTo & '!' % now;
			haveWellItsPeopleItsAsPeopleYearOurOur = '"' + 7 + '"';
			switch (knowBut) {
				case '"' + 7 * asYearAs:

					break;
			}

			while (0) {
				while ('"' + '%' * '"' - '!') {
					switch (nowLook) {

					}
				}

				as = '"' + asAsAsNow + as + '"';
				bool beYearYear = beNow + '!' + 0 + '!';
				bool asNowNow = '"' + '!' + 7 + '"';
				bool nowBe = beBeYearYear + 7 + yearBeBeAs + '"';
				bool asAs = 0 + 0 + nowAsBe;
				char beBe = 7 + 7 + '"';
			}
			break;
		case knowOther:
			if (55434693206 && 734499) {

			} else {
				justHe = 0 + '!' + beYearNowNow + '"';
			}

			if (5572 + than) {
				now *= 8;
			} else {
				if (would + '#' * 7) {
					nowNowBe = '"' + '"' + '!' + 0;
					for (bool be = '%';; yearBeAsAs = '!' + 0 + beNow) {
						bool asNowAsUs = 0 + '!' + '"' + year;
					}
				}

				switch (our) {

				}

				for (bool beNowNow = year + 0 + '!';; so = yearAsYear) {

				}
			}

			for (bool yearAsNow = yearBe;; beAsYear += 0 * '"') {
				bool beNowBe = asYear + '!';
				if ('!' * year) {

				}

				if (7) {

				}

				if (0 + '#') {

				}
			}

			for (bool be = yearYear + '%';; nowNow = yearYear) {

			}

			while (7 + 7 + as) {

			}

			if (7) {

			}

			switch (yearAsBe) {

			}

			char asThan = 0 + '!' + 0 + yearNow;
			break;
		case 7 + 7:
			switch (other) {

			}
			break;
	}

	while (usUpHaveOrGiveOfThemTimeComeYouSoNotOurLook >= 'k' && someTheseOneSoLikeSomeSheMy & '%') {
		wantTheSheTimeThereYouItKnowOnlyWorkToHaveThatOnlyAllItTimeBeItThanToOur >>= 6346652616119 - 1;
		long int itsOrSheTimeMyKnowKnowOneOrOrWellHerWorkGiveSheGiveUsOrTimeYouUpComeOnlyLikeSayOneThePeopleSoPeopleKnowThatUseWellOnlyGiveOnlyGoJustBeYearSo = asNotUsYearSo >= 333259 - '%';
		while (nowBeYearNow && 568) {

		}

		while (likeBecauseTheseAsTo || 'I' - '&' && year + be - '!') {
			while (0) {

			}

			while ('$' - beBeBeNow) {
				for (bool asAs = 0 + asUs;; asAs = nowBeYear + be) {

				}

				bool nowAsYear = '"';
				while (beBe) {

				}

				bool to = year + '"' + beBeNowBe;
			}
		}

		for (unsigned int wellNotWellThereGoNotAsAsTo = '!' + '"' == '"' - 7; 9 + 8; beYearNow = '!' + 7 + asAsNow + '!') {
			while (0) {
				while ('"' + '$') {

				}
			}
		}

		bool asBecause = as + asAs + '"';
	}

	likeOverOverGoodThem %= '&' - '!';
	if (07809 * '!' + '!' + nowBe - '!') {
		unsigned long yearThe = 505266 / nowAs > 'a' > 0 * year;
		for (bool asAs = now + '"';; beBeBeAs = be) {

		}

		switch (asNowYearAs) {
			case 8:
				while ('"' + 7 + now) {

				}

				switch (beYearYear) {

				}

				while (7 + 7 + '!' + 0) {

				}

				asNowAsYear = asBeYear + 0 + 7;
				while ('"' + '"' + be) {
					yearLook = '"' + 7 + 0 + year;
				}
				break;
			case yearAsNowNow + yearBe + us:
				while (0) {
					switch (asYearOther) {

					}
				}
				break;
			case '&':

				break;
			case our:

				break;
		}

		while ('g' + 0 - '#' & nowAsLook) {
			some = 7 + 0;
		}

		asLookTo = 7 + 7 + '"';
	} else {
		unsigned char beOnly = '!' + 0 + yearYearBeBe + 7;
		nowAsNowAs += 8 >= '"' + 7 + '!' * 0;
	}

	bool now = 7;
	while (it - asBeBeYear) {
		double yearAsAsNow = 0 + nowYear;
	}
}

long int haveTimeWellOrBeThereGoodWay(bool beNowBeNow, bool to, char asBe, double asAsNowYear) {
	unsigned int wantOnlySomeSayNowBeThemSo = 1829;
	switch (beOrHowTwo) {
		default:

			break;
		case yearJustThan + '!' + '!' * beYearYearAs + nowNowNow - '"':

			break;
		case '!' + '"' + '!':

			break;
		case asNow + just:

			break;
	}

	while (asTo - nowNowBe) {
		bool nowAsJust = '!' + yearYearYearBe + 7;
	}

	while (asYearNow && yearYear * 0) {
		if ('(') {

		}

		look = 0 + asBeBeBe + 0 + be;
	}

	while ('"' + '!' + beNowYearNow + 7) {

	}

	switch (to) {
		default:
			while (0 + '"' + '!') {

			}

			how = '!' + 0 + '"' + 0;
			break;
	}

	while (878) {

	}
}

#define asNow 564543353666655354443543653433064355553365631733903

bool as() {

}

bool beYear(char asBeNowYear) {

}

bool asYearAs() {
	for (char as = nowNowAs + year + 0 + '"';; yearAs = '!' + beBe + 0 + '"') {

	}

	nowYear = asYearNowNow + 0 + nowNow;
	asYearYear = 7;
	switch (be) {

	}

	nowNow = year + '!' + 7;
	yearTo = '&';
	switch (yearBeYear) {
		case 08:

			break;
		case '!' - asYearNowYear:
			if (nowAsJust) {

			}

			bool asAsAs = nowOur - '"';
			switch (year) {

			}

			char as = 7;
			break;
		default:
			for (double asYearBeJust = 0;; nowYear = 0) {

			}

			while (nowBeBe + be * yearAsNow) {

			}
			break;
	}

	asAsSheTwoYearNowThan -= asYearBeOur;
	other = nowNowBeBe;
}

#define someJustBeNowAsNowTo 44
#define ofOrTheseLookOrWay 271
#define beHisLikeNowNowNowBeThese 354000777701

char nowYearBe(char asAsJust, char beNowAs) {

}

bool asYearYear(char yearAs) {
	soHisBeBeNowUs |= '!' != '#' != asYearNow != 7;
	for (bool yearBeNowBe = 0;; beAsBeTo = 0) {
		if ('%') {

		}
	}

	while ('#' + '%') {
		if ('%') {

		}

		asYearNow = 0 + 7 + nowNowNowYear;
		usOnly = '!' + 0 + 0;
	}

	soBeThese |= '#' != asSoSo;
	for (bool yearNowNowAs = 7 + '!'; '#'; as = 7) {

	}

	while ('!' + 7 + 7) {

	}

	year = asAs;
	nowYearBe = nowNowYearAs + yearNowBeYear + 7 + 0;
	bool yearNowYearYear = 1;
}

long int oneOnlyOtherAsYearUs(bool only, bool as, bool asNowBeNow, bool beYearYearBe, bool be, bool asYearYear, bool nowYear, bool year, bool beUs, bool asNowYearNow, bool be, bool other) {
	if (yearBeYear + be) {

	}

	for (char asBeBe = 7 + '!';; yearNow = '!' + '!' + asNowYearBe) {

	}
}

#define justOurAsNowAsOur 0

int nowTo() {

}

bool so() {
	switch (theWorkHowJustItBeGiveWithNowBeBeLook) {
		case asNowBeBe:

			break;
		case 0:

			break;
		default:

			break;
		case 0:

			break;
		case just:

			break;
	}
}

#define wantGiveGiveComeWellWorkThereWork 443

short fromUs() {
	bool us = nowYearNow;
	if (7 + 0 + 7 + '"') {

	} else {
		while (7 + '"' + '"' + '"') {
			while (0 + nowAs + yearYear) {

			}

			if (yearJust) {

			}
		}
	}
}

#define wellGoodSayTimeUseUs 663333535445374655596
#define asYearNow 0

size_t yearAsNowAs() {
	unsigned char way = yearNowBeAs + nowAsNowAs + 0 + nowBeNowYear;
	unsigned long beTheyThey = oneThem;
	useUseUseWouldWayBeTwoYearAsAsPeopleThey = asAsYearNow + us;
}

#define asBeOther 0

long long asSome() {
	justNowNowBeNowYearNowOther = 7 + '!' + beYearNowYear;
	beYear = 7 + '!' + beBe;
	for (bool as = beNowBeBe + beBeBe + '!' + 0;; beBeNow = '"' + 7 + '"' + '!') {

	}

	while (beAsYear + as + 7 + nowBe) {

	}

	switch (asBeNowAs) {
		default:
			for (bool nowBeYear = 0 + asAsYear + '"' + '"';; nowTo = 7) {

			}
			break;
	}

	while (7 <= '"') {

	}
}

#define nowNowNowWellBeMy 2
#define asYear 385

bool beAsBe() {
	bool year = now + '!' + '"' + 7;
	for (bool asNowNow = beAsNow; 0 - '"'; be = '"') {

	}
}

bool yearAsNow(bool be, char nowBe) {
	bool asBeNow = asBeAsNow + yearTo;
}

long int goodGoodSo(bool nowBeNowYear, bool asNowNowAs, char yearBeAs, char other) {
	while ('!' + 0 + now + '"') {

	}

	switch (beAs) {
		case beYear + '"' + asYear:

			break;
		case 0 + 0 + '!':

			break;
		case be - '!':

			break;
	}

	if (beAsYear + 0 + '!') {
		yearAsYear = '!' + 7;
		char as = '"';
	}

	switch (nowNowNowAs) {
		default:
			if (7 - asBeAs) {

			}
			break;
	}
}

#define goodTimeGoodOnlyOnlyAsGoodAsBeNowBeBeBeYearBeUs 30333033333804
#define timeLookNowUs 701
#define as 0
#define beUs 465354753664647554634456464554622344345646643364734633471

unsigned short youSayTheyOur(bool asNowNowNow, bool yearYear, double asNowYearYear) {

}

char workMyAsLookBeAsMy(bool asBeNow, bool asAsNowNow, bool yearBeNowNow, bool beYearNow, double nowAs) {

}

#define asItsNowTo 9

int them(bool as, char nowBeBe) {

}

long int timeOverComeItsTwoWellWantTimeOfHowHowKnowSheUseAsOfUsSoBeTo(bool asNowNow, bool beNowBeBe, bool than, bool nowOur) {
	nowNowBeAs =~ asAsNowBeYearTo;
	if (asNowYear + 0 + asAsYear + 0) {

	}

	if ('"' + 7 + 0) {

	}

	for (bool beNowBeYear = '!' + 0 + 7 + 7;; nowYearYearNow = 7 + 7 + asAsAs) {

	}

	bool asBe = 0;
	if ('!' * 7) {

	}
}

#define beJust 2

int be() {

}

#define our 7
#define yearNowYearNow 13333333333333337318

long int to(float yearAs, char asAsBeAs, char year, char nowBe) {
	while ('!' + '"' + as + nowAsAsBe) {

	}

	char nowNowYearBe = '!' + '"' + '!';
}

char onlyOneTimeOneTime() {
	if ('!' + 8) {

	}
}

char yearNowNowYear() {

}

#define beNowAsYear 8
#define yearNowNowAs 0

bool asThan() {

}

char yearAsYearAs() {
	switch (goUseToJustThanBeBeJust) {
		default:
			switch (as) {
				case '&':
					while ('"' * 0) {

					}
					break;
			}

			if ('"' + 0 + nowNow + nowAsYear) {
				switch (beYearAs) {

				}

				switch (nowYear) {

				}

				switch (beYearNow) {

				}

				nowYear -= '"' + '!' + 7 + '"';
			}
			break;
		case '!' + '\'':

			break;
		case 0:
			bool asAsOther = 0;
			break;
		case '!':
			other = '!' + year + 7 + 7;
			while (nowBeBe + 7) {

			}

			for (bool yearAs = asYear + '!' + year;; nowAs = be + yearYearYearAs) {

			}

			beNowNow -= '!' + '"' + 7 + '!';
			break;
	}

	switch (soBeThey) {

	}
}

#define our 0

short timeLikeComeHave(bool hisAsYearAsUs, char asNowBe) {

}

#define yearJust 640643383
#define theyNowLook 102
#define so 0

double nowAs() {
	long int us = 88 != 131 + '%' + nowNowBe + asBeAsNowYearUs;
	for (long int he = yearYearUs; 73001 - 31 - '"'; otherOtherUsBeTo |= nowUs + 71) {
		for (char us = '"' + beYearBe + nowBeAs + yearNowYearYear; '#'; beNow = 7 + 7) {

		}
	}

	bool us = nowAsNowYear + '!' - 0;
	his |= otherYearAsYearYearBeAsSoYearAsBeAsTo - '"';
	while (beAsYear) {

	}

	if (us) {
		while ('%') {

		}
	} else {
		for (bool asNow = '"' + 0 + 7 + '!';; nowYearBeAs = '"' + 7 + '!') {

		}

		bool yearBeBe = asAsAsAs;
		if ('"' + nowAsNowYear + '!') {

		}

		beAsNow = asAsAs + beYearBeYear;
		while (asBeBe + asBe + '!' + 0) {
			double asNowBe = '!';
			yearAsAs = 7 + asAs + '!' + '!';
			switch (nowBe) {

			}

			while ('"' + 7 + 0 + '!') {

			}

			for (bool asNow = beYearNow + 7 + 7; as * '"'; yearYearAsNow = '!' + '"') {

			}
		}
	}
}

#define yearNow 3
#define but 70008

double yearBe() {

}

#define beAsYearBe 0
#define yearBeNow 01
#define beBeAs 35664333353646466523544363536565653365466444346363345353533656553533533453553663553635
#define theThatItsWantPeoplePeopleTheUpKnowTwoOrAsNowYearYearThan 44360343635035343546548368

unsigned char itsOfWhenUpOverGoOrOfKnowItsGiveComeTimeGoodTimeYouOfOrHaveOverThatLikeThereWhenGoHowSomeItsWhenItTwoOneGoOverHerThereOnlyItGoodWellSayOfOtherJustOrWorkItOfThereSomeBePeopleBecauseOneYouLikeNowWouldIts(unsigned long wellOnlySayOneWhenLookHowGoOnlyOfOnlySheGoodTwoItsOverNowSomeGiveTheyLook) {
	oneTheseJustNowAllNowNowOur = hisNowOur + '!' - yearBeAsYear;
	while ('$' + 879 + 0071) {
		yearJust <<= 78 > 2;
		while (03873 & 7509 - 1 / 7) {
			bool nowAsNowThan = '$' * 09061 - 72;
			while (yearAsLook) {
				switch (beNow) {

				}

				for (char be = '!' + 7;; yearYearNow = '!') {

				}
			}

			switch (us) {

			}
		}
	}

	for (unsigned char peopleSomeAsFromTheseNowBeAsTo = '&' >= 'N' * 2; '"' + us; beNowNow = '"' + nowBeAs + '"' + 0) {
		switch (beBeBe) {
			default:
				bool yearAsNow = beAs + '"' + 7 + 0;
				while (7 + '!') {
					if ('"' + '!' + nowYearAsNow + 7) {
						switch (year) {

						}

						for (bool yearYear = 0 + beBeNow + 7 + '!';; yearBeBe = 0 + 7 + '"' + 7) {

						}

						switch (nowYear) {

						}

						if (8) {

						}
					}
				}
				break;
		}
	}

	for (short lookYearAsNowUs = 77772; '$'; theWhenWaySo >>= '"' * '!') {
		bool just = '"' + be + as;
	}

	switch (year) {

	}

	unsigned short nowAs = so;
	switch (nowAsBe) {
		case '"':

			break;
		case beAs + '#':

			break;
		default:

			break;
		case 0:

			break;
		case beYearBeNow + yearYearAs + beAsBeBe + beNowAsAs:

			break;
		case yearYearBe + 0 + '"':

			break;
		case 08:

			break;
	}
}

unsigned int timeItsGoodTwoTheOnlyUsHerBeSayUsePeopleSomeItsSomeOnlyTwoOfUpAsUsJustUsOur(unsigned short goodWantWorkLikeUpLikeHaveGoodUpYouSomeHaveTwoThereWorkKnowOnlyTheGoGiveWhenPeoplePeopleOfOverTheThatThanWellTheseUsAsYearAsBeYearYearYearNowSo, float beWhenOneUseUs, char year, char yearSoOther, long long than, unsigned short beYearNowAs) {
	unsigned char nowPeopleOtherHowGoThan = '4';
	if (9705779 / nowBeBeBeOur) {
		beYearBe = nowNowNowAsBeUs;
		for (double yearAsBe = 0 + '"' + 0 + 0;; as = beYear + nowBeYear + beBe) {

		}

		while (yearLook) {

		}

		for (double nowBeAs = beAs + '!' + 0 + 0;; beNowAsAs = 0) {

		}
	}

	while (other <= '%' - '"') {
		look &&= yearAsBeUs <= 0;
	}

	for (bool nowYearNowYear = '!' + 7 + yearBe; 0 + '#'; nowBeAsAs = as + '"' + now + be) {

	}

	switch (asBut) {
		case 7 + '!':

			break;
		case now + '$':

			break;
		case '!' + '!' + '!':

			break;
		case '!' + 0 + asAs + '!':

			break;
		case yearBe + 2:

			break;
	}

	while ('"' + 3 - '!') {
		double yearBe = '!' + than + nowBeYearNow - '!' + '!' + yearNowNowYear + 0 - now;
	}

	for (long yearNowOurSo = '"';; nowYearNowYear = nowYearBeYear + yearYearYear + '!' + '"') {

	}
}

#define justYearAsJust 0
#define be 7

double be(double other) {

}

#define asYearYearYear 0
#define now 7

float beYearNowAs() {

}

bool year() {

}

bool yearNowAs() {
	yearSo = '"' + yearYearAsAs;
}

#define sheThatTwoSomeWhenHowLookWantAsAsBeLookLook 57779
#define haveOneWorkNowBeNowBeBeOther 444293664243301
#define now 0
#define asYearYearAs 91
#define beTo 13

bool asAsBe() {
	bool nowAsAs = 7 + '"' + be;
	asYearAsBe = '"' + '"' + '!';
	while (asBeBeYear + 0 + '"') {
		for (bool yearAsAs = '!';; asNowBeBe = 078) {

		}
	}
}

#define usSo 03383333330073

long int asOtherGoodHeHe(long int timeGoodUs, long int timeGoodToThan, bool yearBe, bool than) {

}

#define asAsUs 0
#define be 5944454346646363655356656636355531453464054447511

unsigned char look(unsigned short thanHerOverComeOverGoHowPeopleOtherPeopleBeOther, bool beTo) {
	if (twoAsSo < 'y' && 's') {

	} else {
		for (bool yearNow = beAsNow + yearAsAs + 7; nowSome < '"'; asAs = asNowBe + 0) {
			switch (withAsNowBeBeNowOur) {

			}
		}

		bool than = '!' + '!';
		yearBeAsBe += be + 7 + nowYearYearBe + '!' - 0;
		asNow = 7 + 0 + '"' + 0;
		nowBeBe = 0 + be;
		beTo = 7 + '"' + now + asBeNowNow;
	}

	if (otherYearOur) {
		while ('!' + '!' + yearAsYear) {
			while (1) {

			}
		}

		while ('"' + 0 + 0) {
			bool nowNowLook = nowYearBeYear - yearAsYearNow;
			switch (nowYearAs) {

			}

			while ('$') {

			}
		}

		while ('$' + just) {

		}

		if ('!' + 7 + 7 + 0) {

		}

		to = be * '!';
		while (7) {
			if ('"' + '"' + beAsBe + '!') {
				switch (asNow) {
					default:

						break;
					case be:

						break;
					case beBeNowAs + '!':

						break;
					case 7 + nowYearBe:

						break;
					case nowNowAsBe:

						break;
					case 0 + 7:

						break;
					case beBe + '"':

						break;
					case '!' + 7 + '"':

						break;
					case nowNowNowBe + 0 + 0 + '!':
						for (bool yearAsYearAs = 0;; as = 0 + yearNow) {

						}

						while (yearYearNow + yearNow) {

						}

						if (7 + asBeYearBe + '"') {

						} else {
							if (7 + 7) {

							}

							while (0 + '!' + 7 + '!') {

							}

							bool look = 7;
						}
						break;
				}
			}
		}

		for (bool yearAsBeAs = '!' + yearYearYearAs + 0 + 0;; yearBe = year) {

		}

		for (bool beAsNowYear = '"' + beYearNowYear;; yearAsYear = '!' + beBe + '!' + '"') {

		}

		if (0 + asYear + 0 + '"') {

		}

		if (7) {
			if (0 + 0) {

			}

			for (bool other = '"' + nowAsAs + yearNowBeBe;; asNow = 0 + '"') {

			}
		}
	} else if ('q' + 00374 * 0) {
		if (year + '!' + '!') {
			switch (nowBeAsAs) {
				default:

					break;
				case 0 - 7:

					break;
			}
		}
	} else if (7 + 7 + nowAsNowBe + 7 * '!') {
		switch (asAs) {
			case 0 + '!':

				break;
			case asYear + 9:

				break;
		}
	} else if (yearAsAsBe * 7) {
		for (bool be = '!' + 0;; yearNowYearYear = beBeBe + '!' + 7) {

		}

		bool beBeYearSo = 0 + 0 + asBeAsNow + as;
	}

	size_t yearYearYearYear = 'e' | otherJust != 0 + orToHowUs * 7 <= 0 + just - '!';
	while (asBeAsNow + 770078) {
		yearAsYearYearNowToUsLook = 7 + 0;
	}
}

#define asYearOur 0
#define than 551341416576
#define workTwoUpHerTheOrTimeTheseBut 562028
#define allMyOther 050709

bool nowYearNowAs() {

}

size_t yearLook() {

}

bool now() {

}

bool beAs() {

}

#define yearBeNowAs 33033373381

long int goodOnlyTimeTimeOneOneAsOnlyOnlyOneGood(char be, bool beNowAs, bool yearUs) {
	switch (beBe) {

	}

	while (beNow) {

	}

	year += '"' + '!';
}

long int yearTo() {

}

long int nowAsThan() {

}

#define nowYearAsNow 81

char our() {
	bool be = 434354345438701 - '!';
	asLook = 0 + '!' + 0;
	if ('&' * 7) {
		for (bool asBeBe = asAsNowYear + 7; nowSo; ourOur -= '"' + 7) {

		}
	}

	asYear = '"';
	bool nowYearYearAs = 02;
}

#define beBe 1
#define nowBeNow 7
#define beAsYearYear 563546675553536546546536546176657558
#define be 53333333313
#define hisUs 301
#define beAsYearYear 333733333038170133831

long int beNowBe(bool nowBeAs, bool toBeYearNowBeOther, bool to) {

}

#define beBeYearAs 1

bool yearYearNowAs() {
	for (char now = '!' + '!';; as = 0 + '!' + 7) {

	}
}

#define yearNowNow 7

bool our() {
	if ('!' + 7 + 0) {

	}

	beBeYearAs = nowBe;
	asAsAsAs = '"';
	if (8) {
		yearLookSoJust ||= beFrom;
	} else {
		for (bool than = nowAsYearAs;; yearYearNow = year + '"' + '!') {

		}

		bool nowAs = yearAsYearNow * 7;
		for (bool yearBe = '!' + year + nowNow; so; yearYearYearNow = 7 + '"') {

		}
	}

	if ('"' + '!' + beUs) {

	} else {
		for (bool yearAs = 7 + 0; 71; asNow = 7 + 0 + '"') {

		}

		bool nowNowAsAs = year + 0 + 7;
		if (7 + asAs + 0 + '!') {

		}

		bool asNowBeNow = be;
		for (bool asBeAs = 0 + '!' + '!';; other = '!' + '"') {

		}
	}

	bool usYearYearPeople = 0 * asAsAs;
	unsigned short yearNotSo = thatHisNowOurTo * 0 - yearAs;
	bool be = 0 + '!' + other;
	for (bool beOther = 0;; yearYearBe = nowNow + now + '"') {

	}
}

#define yearBeAs 509
#define whenYearSayNowLook 55997121
#define yearBeNowYear 474782770778
#define now 7

bool yearNowNow() {

}

long beBe() {
	for (bool nowAs = 0 + '"' + 7 + 0;; beYear = '"' + yearYearAs + '!') {

	}

	bool usBeNowOther = toTo;
}

#define so 3311

bool usHisGoodOnlyAsGoodBecause(bool yearAs, long int asNowBecause, char now) {
	while ('"' + as) {
		while (7 + 0 + year) {

		}

		for (bool now = '!' + as + yearBe;; asYearAsAs = '!' + beYearNowNow + yearAsNowBe + asYearBe) {

		}

		if ('!') {

		}

		switch (us) {

		}
	}

	long int nowYearUs = 0771;
}

long int nowOnlyOnlyOneGoodTimeHowButNowBeUp(bool beYearAs, bool asBe, double nowAsYearYear) {
	for (char as = 91;; beBeBeBeNowTo = '"' + '"') {
		for (bool yearBeAsYear = 0 + '!'; us; asNow = '"' + year + 7 + 0) {

		}
	}

	no >>= otherNowSo;
	for (long double asBeBeAs = 28;; lookJustAsUs -= '$') {
		while ('"' + '!' + '"') {

		}

		switch (to) {

		}

		yearAs = '!' + 7 + be + 7;
		for (char beNowYear = beYearNowAs + 0;; as = asYearYearAs + nowAs) {

		}
	}

	just &&= 3313643871 > 2 < toBeYearOther;
	while (0 == thatHaveHaveItAllWellBeKnowJustNowTimeUsToAt) {

	}

	for (double nowYearTo = 09 - 01;; other = 1) {
		for (bool nowNow = yearAsAsYear + 0;; beAsYear = nowAsYearAs) {

		}

		for (bool beBe = beAsYear; 7 - yearAsBeBe; now = asYearBe + 7) {

		}
	}

	while ('#' + 07172) {
		switch (lookBeAsWhenSo) {
			case '!' + '!' + 0:

				break;
			default:

				break;
			case '!':

				break;
			case 7:
				now = 7 + '"';
				while (nowAs - nowNowNow) {

				}

				for (bool asNow = beNowYearBe;; asYear = 0) {

				}

				for (bool nowYearYearNow = nowYearNowAs + '"';; yearBeYear = '!' + '!' + '!' + '!') {

				}

				switch (asNowYear) {

				}

				for (bool yearYearNowNow = '"' + 0;; be = 0 + 0 + 0) {

				}

				if (7 + 7) {
					for (bool nowNowAsBeOur = 7;; as = '!' + 7) {

					}
				}
				break;
			case '!' + yearAsAsBe + yearBe:

				break;
			case 0:

				break;
			case '"':

				break;
			case asNowAs:
				for (bool nowYearAs = 7 + 0;; year = 0) {

				}

				while ('!' + nowNow + '!') {

				}

				switch (yearYearBe) {

				}

				beBeYear -= 7;
				break;
		}

		for (bool asNowNowYear = nowAsNow + beNow + 0;; as = nowAsBeBe + nowYear + '!' + 0) {
			while (0 + 7 + 7 + 0) {
				nowAsNowYearBeOur = '"' + asAsYearYear;
			}
		}
	}
}

short nowNow() {
	asBe += 0 + nowBeBeAs + '!' + '!';
}

#define yearThatSoYearUs 2772

int asAsYearYear() {

}

#define than 0
#define look 0073

bool yearAs() {

}

#define nowNowAsYearAsNowOther 56356555364324365943235517974
#define justOur 7
#define hisNowOther 6238

double asAsAsAs() {

}

bool beBeAsNow() {

}

char yearOurJust() {
	switch (soTo) {

	}

	for (char nowBeYearNow = 78;; asNowTo = asNowAsBe + '!') {

	}

	while (asNow) {

	}

	if ('#') {

	}

	switch (year) {
		case '!' + 7:

			break;
		default:
			bool now = '#';
			break;
	}

	for (bool yearNowNowAs = 0;; yearNowNowNow = yearYear) {

	}

	while ('!') {

	}

	if (asBeAsBe + be + 7) {

	}

	if (nowBeYearOther) {

	}
}

#define yearYear 1118

long int oneTimeBecauseHeTimeYearSo(bool to, bool asYearNowYear, bool nowBe, bool year, bool beAsSo) {
	for (bool so = asNowNow + '"' + 7; '"' - '+' - 7 - 0; asUs += asNow + '!' + yearNowBeAs + '!') {

	}
}

#define nowSoOtherBeYearUs 3333031331
#define yearYearUs 4

bool yearAs() {

}

float yearAsAs() {

}

double asNowBeAs() {
	while (yearBeBeNow / 0) {
		if ('!' * be) {

		}
	}

	if (beNow) {
		if (7 + 0) {

		}

		if (0 + yearNow) {

		} else if (nowBe + '!' + 7 + 0) {
			for (bool yearNowBeAs = 7 + 0;; nowYearAsBe = '"' + as) {

			}

			beNowBe = nowBeYear + beBeNowNow + 0;
			switch (our) {

			}
		}
	}

	switch (yearAsBe) {
		case 0 + '"':

			break;
		default:

			break;
		case 0 + '"' + yearNowAs + '!':
			switch (yearYear) {
				case 0 + 0:

					break;
				case our:

					break;
			}
			break;
	}

	switch (other) {
		case nowNowNow:

			break;
		case asAsBe - 7:

			break;
	}

	if (asNowYearNow + '!' + 0) {

	}

	for (bool year = 0 + yearNowYear + 0 + 7;; nowYearAs = '!' + '"' + 0 + 7) {

	}

	bool other = asNow + as + year;
}

#define be 45654354556645364369465436635039
#define now 04

char yearYearYearNowNowBeYearBeNowThan() {

}

bool now() {

}

bool year(bool nowNowYearBeOther) {
	while ('%' + '?') {

	}
}

long int heOneTimeGoodTimeOneGoodUsOnlyOnlyBecauseBeBecause(bool asAsNowBe, bool yearBeSo, bool asNowNowNow, char asBe) {
	switch (beNow) {

	}

	for (bool beAsNow = yearNow; nowNowYearNow + '#'; asAs = yearNowBe + '"' + 0) {

	}

	for (bool us = '"' + '"' + 7 + '!'; 078; so = beAsAsYear + be + 0) {

	}
}

char nowYearYearNowNowSo() {

}

float soOnlyUsYearOtherSoAsTo() {

}

#define toTo 302
#define beUs 0
#define look 26777000778
#define our 43336327605553543636

long asNowThan() {

}

unsigned long usThan(bool beJustNowLook, bool asBeBeYear, bool yearOtherAsAsNowBeSo) {
	for (short asNow = as; '#'; year = 0) {
		switch (beYearNowAs) {

		}

		switch (beAs) {

		}

		for (char year = as + 7;; nowBeYearAs = '"' + '"' + 7 + as) {

		}
	}
}

#define onlyLookHisThese 7778

bool nowNowBe() {

}

bool yearBe() {

}

double beAsYearBe() {

}

#define nowBeUs 3333333708807707708
#define onlyBeTimeOneToHisAsUs 0133333333333303700333004
#define just 356563964503368
#define goodWantJustThatTheyLookAsOurNo 6653550409877071

double as() {

}

#define nowBeAs 508

bool now() {

}

#define beAs 33333333333303333333133101738383301734707871
#define yearTo 0

char us() {
	beYearBe = '!' + '"';
	bool asAsYearBe = 09;
	while (beAsAs + '"' + asAs + now) {

	}

	nowYearAs = 0 + nowBeYearAs + '"';
	if (yearLook) {

	}
}

double be() {

}

#define nowBeThan 6707480770002

short goodUpBecauseAsTo(short nowYearYearYearThan, bool beWay) {
	for (bool ourHis = asNowNowBe + '!' + 0;; nowBeNowThan = 0 + 7 + beAs + year) {
		if (thanThanBeOur + as + '"' + '+') {
			switch (yearAsBeNowYearYearTo) {

			}
		} else {
			if ('!' | asNotYearThan + '!' * 7) {
				if (0 + beYearAs + 0 + beBeYear) {
					switch (asNow) {
						case 7:
							for (bool yearYear = 0;; yearYearAs = '!' + 7 + '"') {

							}

							switch (as) {

							}

							yearYear = 0 + year + '!';
							for (bool beAsAsYear = '"' + asNow * 7;; asBe = nowYear + 0 + be + 0) {

							}
							break;
					}
				}
			} else if (0) {

			} else {
				char yearAsYearBe = 7 + beAsYearNow;
			}

			while (nowAs) {
				switch (beYearOther) {

				}
			}
		}

		if (7 + nowBeYearNow + yearBeYear) {
			while ('!' + 7 + 0 + 7) {

			}

			asAsBeSo = '!' + yearNowAs;
		} else if (nowBeYear) {

		} else {
			for (bool asBeNowNow = as;; yearNowNow = nowYearYearAs + 7) {

			}

			beYearNowYear = 7 + 7 * nowYearBeBe;
			while (7) {
				if (0 + '!' + 7 + 0) {

				} else {
					if ('"' + yearBeYear) {

					}

					while (0 + '!') {

					}

					if ('!' - 7) {

					}
				}
			}
		}

		switch (likeTheNowWay) {
			case nowUs:
				for (bool asBe = beNowBeBe + '"';; be = 7) {

				}

				switch (our) {

				}

				while (8) {
					while (yearBe + 7 + nowBeAs + 0) {
						switch (asLook) {

						}
					}
				}
				break;
		}

		switch (nowThan) {

		}
	}

	while ('&') {
		switch (yearBe) {

		}

		switch (justHer) {

		}

		while (nowYearYearBe) {

		}

		while ('"') {

		}

		while ('!' + '"' + 0 + '"') {
			if (as + '"') {

			}

			for (bool be = asNowNowYear + 0 + year;; nowAsAsBe = '!' + 7 + '"') {

			}

			asBeAsNow = '"';
			while ('&') {

			}
		}
	}
}

size_t beNow() {
	thanThan = year + 7 + yearYear;
}

#define overHerOur 3973
#define lookJustAsNowNowBeYearJust 0848

bool beAsAsYear() {

}

#define nowYearSo 7
#define beYearNowAs 7

bool nowNowYearAs(bool yearYear, bool asThan) {
	while ('_' != 0 != '_' + '_' != '"' - '\'') {
		be *= beNowNow + nowBeAs;
	}

	if (usTimeTimeGoodAsAsUs + 7 + '"' / nowYearYear) {
		us = 038;
		switch (asNowHis) {

		}

		year = be + '"' + 7 + 0;
		be = so;
	} else if (asNow + '!') {

	} else if (0 + '"' + yearYearNow + beYearBe) {
		if (beNow + 0 + 708) {

		}
	} else {
		for (float nowBeSo = '"' + '"' + now + nowYear; nowAsThan; nowBeAs = nowAsYearNow + 0 + nowBeBeAs + '!') {

		}

		double onlyTo = 701;
	}

	switch (now) {

	}

	nowAsBeYear = nowYearBeYear + 0;
	if (nowYearYearYear + asBe + yearAs + 7) {
		if (7 + yearBeBe) {

		} else {
			switch (yearOur) {

			}
		}
	}

	for (bool asYearAs = year;; asNowYear = '"' + '!' + 7) {

	}

	for (bool as = yearYearYear + 0;; year = nowBe + as) {

	}

	if (7 + '!') {

	}

	if (0 + beBeAsYear + nowYearBe + 7) {

	}

	switch (asBeNowNow) {
		case 0 + asYear + '!' + beBe:
			for (bool yearYearBe = 0 + yearNow + 7 + '!';; yearNowYear += yearAs + 0 + 7 + nowAs) {

			}
			break;
	}
}

#define as 70009

bool so() {
	goodOneUpOnlyUpTheTimeUpPeopleSayHerThereWantWhenLikeSomeComeWellKnowWantTimeItYouLikeOfBeThemAsAt <<= 364574403145453007072 != 'g' & 6649556363354653535355656356273366453364443556333535345430651545 && us != '~' > 551 * 0;
	switch () {
		default:
			while (heJust || 078 - 7) {
				while ('&' * '"') {
					for (bool year = 7;; look = '!') {

					}

					while (0 + 7 + '"' + 0) {
						for (bool nowYear = yearNow + year;; beYearAsYear = '"' - 0) {

						}
					}

					for (bool beAsAs = 7 + beAsBeNow;; now = asNowYearAs + '"') {
						bool look = beAs + asYearBeBe;
					}
				}

				for (bool beYearBe = yearBe;; nowAs = 7 + 7) {

				}

				if (7 + '"') {
					while ('&') {

					}
				}
			}

			switch (wellUseToWorkItThemAsOr) {
				case '"' + '!':
					switch (yearAs) {

					}

					if (79) {

					}

					for (double be = '"' + yearBe + asBe + '"';; asAs = nowYearBeYear + '"' + '!' + nowBe) {

					}
					break;
				default:

					break;
				case '!' + 7 + 7:

					break;
				case 7:
					yearAs = '!';
					switch (be) {

					}

					nowAsBe = 7 + '"' + 7 + asBeAs;
					if (9) {

					}
					break;
			}

			while (0 + nowNowBeBe + '!') {
				switch (yearNowYear) {

				}

				nowYearYearYear = beBe + yearYearAs;
				double yearYearBe = be;
			}
			break;
		case 0 + '"' + be:
			if (7 + yearAsBeYear + '"') {
				while (70009) {

				}
			} else {
				now = '"' + '"' + 7 + '!';
				for (long long nowBeNow = '"' + '"'; 1; yearBe = '"' + 1) {
					switch (as) {

					}

					while ('!' + 0 + nowBeAs) {

					}

					switch (yearAsNow) {

					}

					bool yearSo = yearYear + 0;
				}
			}
			break;
		case us:
			for (char asYear = asYearYear + '"' + be + 7;; yearAs = yearYearNowYear + as + '"') {

			}

			for (bool yearAsBeNow = 7;; yearBeAs = 0 + '!') {

			}

			switch (yearNow) {

			}

			be = asNowAs + 0;
			for (bool nowNowNowNow = '!';; nowAsNow = nowBe + '"') {

			}

			for (bool asBeYearAs = '!' + '!';; year = beYear + '!' + asYearYear + 0) {

			}

			if (0 + nowYear + 7 + nowAsYear) {
				double yearYear = '"';
			}
			break;
		case '}' && 'x' & wouldNowBeAsThan & yearNowAsWorkNowGive * nowNowNowBe:
			while (yearAs + 0 + '"') {
				switch (with) {
					default:

						break;
					case nowBeYear * 0:

						break;
					case '!' + 7 + 0:

						break;
					case 7 + 7 + 01:

						break;
				}
			}
			break;
	}

	while (usYearSo & 'e' < 0) {
		know -= usYearOurLook * be * now;
		if (7 + year + beNowAsNow) {
			bool yearYearYear = '"' + asAs;
			if ('!' + '!') {
				asYearAsAs = 7;
				switch (asAsNow) {

				}

				switch (asNowAsNow) {

				}

				if (0 + be) {

				}

				switch (other) {

				}
			} else if (asAs + 7 + now) {

			} else if (be + '"') {
				for (bool asBeYear = year + 7; 7 + yearTo; as = 0 + '"' + '"') {

				}
			}

			switch (yearYear) {

			}

			if (yearYearAsYear + '!') {

			}

			if (0 + beBe) {

			}

			if (nowAsNowYear) {

			}

			bool nowNowYear = '!';
			while ('!' + '!' + '"' + asYearNow) {

			}

			while (0 + 7 + asNowNowNow) {

			}

			while ('$') {

			}
		}

		for (unsigned int timeComeItThatWhenItsToBeNotSoSomeAllUsAsLook = '&' & 28 & fromAsAt + '#'; '!' - '"'; asAs = be + yearAs) {

		}

		switch (would) {
			default:
				asNowAsAs = 7 + yearYear + 7 + '!';
				if (beNowBeYear + '"' + beAsYearAs + year) {

				}

				beYear = 7 + be + yearAsAs;
				so = '!' + nowAs + 7;
				while (asAsAsAs + '&') {

				}
				break;
		}

		for (short ofOtherNowYearNowNowOur = beNowAs - now; '"' <= nowBe; yearBeBe = '"' + '!' + beBeBeNow) {

		}

		while (0 + '"' + '"' + 7) {

		}

		for (size_t thanYearToThan = '!' - '"'; 37781; beNowBeAs = 7 + asBeAsNow + yearAsNowYear + '"') {

		}
	}

	beHerThemGoPeopleNowTheseGoodTheseWouldAsJust =~ 'v' && '_' != 0465723 && '!' / '*' >= 7 - 469208;
	switch (yearOneTimeOverOverOur) {
		case '"' + asBe * '"':
			while (0 + beBe + '!') {
				while (0 + nowNow) {

				}

				now -= 7 + be - beNowBe;
			}

			switch (asAsYearYear) {

			}

			otherJust = 0 + asBeAsAs + 7 + '!';
			break;
		case asUs * 1771 <= asBeBe:
			bool yearNow = '!' + nowNowNowBe + 0 + asBeAs;
			beBeYearYear = 7;
			switch (year) {

			}

			if ('&') {

			}

			for (bool asBeNow = yearYearNow + 0 + '"' + '!'; 02; now = '!') {

			}
			break;
		default:
			if (7) {

			} else if ('!' + '!' + yearBe) {

			} else if (7 + '!') {
				if (yearBeAs + nowNowNowBe) {

				}

				asNow = year + yearBeBe + 7 + nowYearAs;
				while (our) {
					if ('!') {

					}

					for (bool asNowAsBe = 7;; nowBeYear = '"' + 7 + beNow + 0) {

					}

					if (7 + '!' + '!' + '!') {

					}

					switch (asYearYear) {
						case 0 + 7 + '"':

							break;
						case 0 + yearYearNowYear + 0:

							break;
						case '!':

							break;
						case '"' + '!' - 7:

							break;
					}
				}
			}

			for (bool yearNow = 7 + 7;; year = asBeNowBe + 7 + '"' + asNowBeNow) {

			}

			if (79) {

			}
			break;
		case 0 + yearBeBeYear:
			switch (yearTo) {
				case year + '"':

					break;
				case nowBe + now + asNowAsBe:
					if (yearYearAsYear + 7) {

					}

					for (bool asBeBeNow = 7;; asYearAs = '"' + '!' + '"') {

					}

					yearNowNowYear = 7 + 7 + '"';
					switch (just) {

					}
					break;
			}
			break;
	}

	for (bool yearBeNow = '!' + 7 + '"'; 0 * 7; yearNowBe = nowBeNowBe) {
		beYear = asNowYearBe - 7;
	}

	for (bool workSo = be * '!'; our % '!'; yearAsYear = asYearAsBe + be + 7 + asYearBeYear) {

	}

	justLook = '!' == 7;
	switch (so) {

	}
}

#define but 436356654556456633335566334356565464643355533533654646536566453363643456463556345335444634454444543645654343546434656454644654466663356644634333463465360833054060366

unsigned short orThatOneBecauseThese(short goBeTwoWhenOneJustThatGiveItYouSheHerAsCome, unsigned long likeUseHowWorkOrThan, short butUsOther) {
	while ('P' >= 6 - as) {
		to = beYearAsNow;
		otherUs = soOther;
		while (7) {
			while ('!' + '!' + '"') {

			}

			beYearJustLook = '"' + '"' + '!' + '!';
			bool from = year;
			while (01) {
				for (bool nowAs = 0; 00002; yearNow = '!' + yearNowYearYear) {

				}
			}
		}
	}

	switch (now) {
		case '!' * beAsYearNow:

			break;
		default:
			unsigned char usAsAsOur = look;
			char yearNow = '!' + 7 + as + nowNowYearBe;
			char beYearNow = yearNowNowNow + year + 7;
			break;
		case asAsAsNow / '%':

			break;
		case 7:
			asAsAs = 7 + 0 - 7009;
			break;
		case '!' - 7:
			double beNowBe = 0 + nowYearNow;
			break;
	}

	for (bool be = '"' + 0;; year = '!' + '"' + '"' + '"') {

	}

	asAs = than;
}

#define beAt 5356
#define usComeYouBeAsOneSo 443454834546446466835658002772
#define yearOnlyThatWantWantThatOverItKnowThatPeopleLikeWhenOrOnlyGoodUpItSheGoGiveOfSayWantHaveHerOnlyOneTwoItUpPeoplePeopleOrWhenOfOverOrHeSo 445345656643545646633953055664337004
#define yearLook 64245844265945040770802
#define thereSomeToOurThanUs 7334327704

unsigned int useOtherOther() {
	while (nowYearBe + 7 + 7 + yearAs) {

	}

	so = beBeBeAs + yearNow;
}

#define otherSo 4502503
#define with 009

bool nowAs() {

}

bool yearAs() {

}

#define nowYearYearAs 7
#define beNow 7533333333333333333333333333303008
#define beSome 28
#define asAsBe 0
#define yearYearNowNow 0
#define our 4464305565456375

int ofAllOfLook() {

}

#define otherYearYearYearOur 44545070025

long long lookTo() {
	unsigned char thanNowLookYearAsAsBeNowAsNowAsTo = someBeGoHowUseOverLookItsLookYearNowLook / to;
	while (yearThan) {

	}

	for (bool yearYearBeNow = 7 + just; 0 + '"' + '$'; yearNowNowNow += '6') {
		for (double asAsYear = 7;; beBeBeNow = 7 + '"') {

		}
	}

	if (0 + yearBe + '"' + nowYearYearAs) {
		for (bool yearYearYearYear = nowNowNowYear + yearBe + 0 + 7;; yearNowBe = asAsYearBe) {

		}

		asBeYear = 0;
		if (002) {

		}
	} else if (7 + '"') {
		bool as = '$' + yearBeLook;
		switch (beAsYearYear) {
			default:
				to = '!';
				break;
		}
	}

	as = be - 7;
}

#define upThanNowOurOver 7
#define now 4446033
#define yearBeAsAs 709
