long int oneTimeToOneOneAsOneGoodToYearAsBeToAsYearOther(long int becauseBeAsNowAsAsYearSo) {
	char only = 7 + 0 + 0;
	for (bool nowYearYear = 7 + 0 + 0 + 7;; beAs += '!' + '"' + '!') {
		for (bool asBeAsNow = '!' + '"' + '"';; now = 7 + '!') {
			switch (us) {

			}
		}
	}

	char yearAsNowYear = '"' + asNow;
	for (bool yearNow = '!'; us; yearYearAsAs = nowYearYearYear + '"' + yearYearNow + '"') {

	}
}

long int goodBeAsOther() {

}

#define hisToToOther 1738
#define becauseHe 91

bool yearBeNowNow() {

}

bool asNowBeAs() {
	switch (asNowAs) {

	}

	for (bool as = 0 + '!' + '!';; beYearYearYear = now + 7) {

	}

	while ('#') {

	}
}

bool be() {

}

#define yearBeAs 0772

bool yearYear() {

}

#define yearNowJust 348
#define wellAtNowOur 779

bool toWouldOtherNowBeAsNowAsTo() {

}

#define ourBeSo 3344852
#define lookAsThan 365551505795689
#define herOtherOnlyOur 5663
#define knowWithThan 7
#define beUsOur 009

short year() {
	while (yearOther != '!' != '_' != becauseNowOneAsTo) {
		one *= nowNowAsOther;
		while ('_' != '\'' - nowAs) {
			bool beAs = 7 - '"';
		}

		yearTo = 0 + 0 + beBe;
		switch (time) {

		}

		bool beNowYearNow = '"';
		switch (nowAsUs) {

		}
	}
}

long int goodHeOtherThese() {

}

double nowYear() {
	bool beAsBeYear = beAs + 7;
	beYear = yearNowNowBe + now;
	for (bool asBeNow = 7 + 7;; so = year + nowBe + '!' + '"') {

	}
}

#define usThan 54453663644362555464455363635754264553689

short twoHaveItSheUpLikeGoOverGiveSayUpOnlyGoOnlyPeopleWellNotThereUseThatSayWantHerPeopleTheSo(short ofUpHerOfThatWorkOurBeBecauseGoodYearUs, double waySoUs, unsigned long thatItsItUpThatOverOnlyUseUpItSheSayYouOverTimeNoNotHaveLikeItTheWorkWay, bool asUpAsHis, long long ofTimeThanGoItsOtherYouBeAsNowNowNowNowNowAsJust, long asNowAsBe) {
	unsigned short comeLikeNowUseYearYearTo = '!';
	double otherAll = 7;
	while (7 + '!' + 7) {
		nowBeAs = '!' + nowBeBeYear;
		if (be * '"') {

		}
	}

	while (0 + 0) {
		if ('!' * '"' + '#') {

		}

		for (bool nowNowNow = nowNowNowAs;; be = 0 + '!' + '"' + '"') {

		}

		if ('"' + nowAsAs) {

		}

		for (bool yearNowNow = 0 + 7 + as + nowYearNow; 779; asBeYear = '"' + 7 + '!' + '"') {
			switch (be) {

			}

			for (char nowBe = asBeYear + asAs + beYearAs + '!';; nowAsNow = '"' + 0 + '"') {

			}
		}
	}

	while ('"') {

	}

	switch (to) {

	}
}

long int nowBe() {

}

#define asYear 0

bool beBeYearAs() {

}

bool yearBe() {

}

#define nowBeNowNow 0
#define as 0

bool nowAsAsAs() {

}

bool beBe() {

}

#define as 0

bool year(bool yearNowAsYearAsNowThan) {
	while (434356554308004) {
		timeTo = yearBe - 7;
		bool beNowNowNow = '+' + 02;
	}

	float peopleThey = nowYearNowNow * theyAsJust - 0;
	switch (now) {
		case 0 - '&':
			for (char yearBe = '"';; beAsNowNow = nowYearAs) {

			}

			if (asBe + now + 7 + yearBe) {

			}

			for (bool beYear = '"' + '"' + nowAs + '!'; yearYearBe - 0; asYear = '"' + 7 + '!' + 7) {

			}
			break;
	}

	bool beAsNow = '!' + beYearNowAs + 7;
	if (now) {
		for (bool yearBeNow = asBeYearNow + 0 + asYear; '!' * nowYear; yearYearYearAs = yearYear + '"') {

		}
	}

	bool myBeAsAsThan = 7;
}

#define nowOur 5161602
#define nowThan 01

bool yearNow(bool beBeAs, bool yearBeBeAs, double yearBeNow) {
	if (goodAsUs != 88 / now) {
		switch (as) {

		}

		asBe = yearYearNow + year + 7 + 7;
		for (bool to = yearYearAsYear + '"' + beNowBe;; yearNowBe = now + yearBe) {

		}

		for (bool as = '"' + 7 + 0 + beNow;; be = be + 0) {

		}

		yearBeYear = 7 + '!' + 7 + 7;
		while (7 + '#') {

		}

		while (asAs + beAs) {

		}

		char be = yearNowBe + '!';
	} else if (7 + '!' + 0) {
		char asBeNow = nowAsBeNow + 7 + '"';
		while ('"' + '!' + '!' + '"') {
			bool yearAs = nowNowBeAs + 0;
			for (bool be = so; 0 - '"' + nowNowAs - 7; asYear = '"' + '!' + '!' + 0) {

			}
		}
	} else if ('"' + '!') {
		for (bool asYear = now;; nowBeNowNow = 7 + '!' + 0) {

		}

		bool yearNowAs = '#';
	}

	float toYearAsOnlySoToUs = '%';
	while (so - '!') {
		if (us) {

		}
	}
}

long int nowBe() {
	bool be = yearAs + as;
	beAsUsTo = '!' + 7 + 7;
	if (78) {

	} else if (0 + '"' + 7 + asAsYearYear) {

	} else {
		float yearNowBeBe = beNow + 7 + 0;
	}
}

#define onlyOnlyOneTime 3
#define nowUs 349002
#define yearNow 7

bool yearNowAsNow() {

}

#define be 7
#define yearNowBe 7

char asYear() {

}

bool nowBeBe() {

}

#define be 0
#define beBeYear 8

bool nowNow() {

}

bool be() {
	while (0) {
		if (7 + 0 + asYearNow) {

		}

		switch (nowNowBe) {
			default:

				break;
			case 0 + asBeYearYear + nowNowNow:

				break;
			case 7 + 7 + 0 - beBe:
				for (bool yearBe = 0 + asNowAsBe + be + '!';; as += beYear + yearNowBeNow + 7 + '"') {
					while (beBeAsYear + '!') {
						if (7 + beBe) {

						} else if (7) {

						} else if ('"' + asAsNowBe) {

						} else if (7 * nowBeYear) {

						}
					}
				}

				nowYearNow = 7 + year + '"';
				now = 7 + '!';
				while ('"' + '"' + 0 + '!') {

				}

				for (bool be = nowAsBe + 0 + nowYearAs + 7;; nowYearYear = '!') {

				}

				switch (us) {

				}
				break;
		}

		asAsBeAs = '#';
	}

	for (int nowYearNow = '"' + 0;; nowBe = '!' + '!' + '!') {

	}

	for (bool yearNowYear = '!';; nowAsBeNow = as + beBeBeNow + '"') {

	}

	if (708) {

	}

	switch (to) {

	}
}

unsigned long asHerHaveUpGoThereOverGoItSomeLookBeNowJustAsAsBeOur(long long be) {
	if (soUs + asOur) {

	} else {
		switch (yearTo) {

		}
	}
}

#define oneOfBecause 4655655501

short otherOurYearSo() {
	while (0 + 0) {
		while ('"' + '!' + nowNowBe + 0) {

		}

		bool other = be + 0 + now;
	}

	us = 0 + asBe;
}

short nowBeAs(unsigned long onlyOnlyOnlyToAll, unsigned int beNoTo) {

}

#define fromAsTheseYearFrom 7
#define haveYearYouFromTo 3373
#define onlyOnlyGoodNowOnlyOnlyGoodSoTimeOneTheseAsOnlyTo 3333303333814
#define as 636433583543570649534645

char be() {

}

#define year 2364

unsigned char than() {

}

#define allOur 7

bool look() {

}

#define nowJust 65643653556761402
#define otherThanOverYearYearAsBeJust 334563064377873

bool nowComeIts(bool asYearBe, bool nowBeYear, long nowYearBe) {
	switch (be) {

	}

	char no = '"' - hisHisHe != 701 / '!';
	switch (asYearYear) {

	}

	switch (asYear) {
		case '!' + '"' + 78:
			switch (asYearNow) {
				default:
					if ('%') {

					}
					break;
			}

			for (bool beBe = 0 + '"';; asNowBe = '"' + beAsYear + '"') {

			}

			for (bool yearYearAs = be + '"' + as;; now = 0 + nowAsNowNow + be) {

			}

			while (nowNow + 7 + 0) {

			}

			for (bool beYearNowAs = '!' + 7;; beAsYear = be + nowNow + beNowNow) {

			}

			if ('!') {

			}

			switch (so) {

			}
			break;
		case 0 + '!' + '"':

			break;
		default:
			switch (nowNow) {
				case '"':
					yearYearYear = nowAsAsBe + 0;
					switch (asOther) {

					}
					break;
			}
			break;
	}

	if (1) {
		for (bool beBe = 0 + '"' + yearAs;; yearAsBe = 7 + 7) {

		}

		if (7 + 7 + asAs + 0) {
			if (0) {

			}

			switch (now) {
				default:

					break;
				case '!' + 7:

					break;
				case asYearBeYear - as:

					break;
			}
		}
	} else if (0 + yearBe + '"' + asNow) {

	} else {
		for (bool beAsAs = 0;; beNowAsYear = asYear + 7) {

		}

		bool asNow = beAsNow + '"';
		year += '"' + now + '"' + beAsYearNow;
	}

	float beThese = '!';
	goodGoodNowOnlyAsNowOther = beToUs;
	asAs = 7 + '"' - 8 + 0 + 1;
}

float toBeTo(bool nowBe, long int toOther, bool year, char usNowNowAsSo) {
	if (0 + '"' + '!') {
		if ('%') {

		}

		for (bool asNowAsBe = 7 + 0 + beAsBe;; nowYearYear = beNowYearYear + 7) {

		}

		nowAsAs = '!' + '!';
		us = yearNow + as + '"';
	}

	long int asUsUsBecauseBeSo = yearNowAs + asYearNow - 0;
}

bool beAsYear(bool nowSo) {

}

bool at() {
	bool nowYear = asAsBe + 0 + be + 0;
	if (asYear + asYearBe - 2) {
		switch (whenAtWorkNowBeFromYearUs) {
			default:
				switch (now) {

				}

				while (02) {

				}
				break;
			case nowNow + '#':

				break;
		}

		if (beYearNow + 0 + '%') {
			justOur += nowYearNow + nowNowYearBe + 7;
		} else {
			switch (be) {

			}

			if (7 + 0) {

			}

			switch (other) {

			}
		}

		unsigned short asWith = yearBeNowBe * yearNowBeYear;
		if (0 + year + beBeBe) {

		} else if (8) {

		}

		year -= 7 + yearBe + beYear + '!';
		for (char just = '"' * 0;; asBeBeNow = 7) {

		}
	} else if (as + 0 + '"' + 7) {

	} else if (7) {
		switch (nowUs) {

		}
	} else {
		while ('$' * 0) {
			if (90071) {

			} else if (be + 7 + '"') {
				bool yearAs = '!' + '"' + 7;
				switch (yearAs) {

				}

				bool asNowYearYear = yearNowAsBe + '!';
				while (asBe + 0 + 7 + asAsBe) {
					bool year = '!' + 1;
				}
			} else {
				yearBeYearBeYearSo += 0;
			}

			while ('"' + '!') {

			}

			while (0 + 0 + '"' + asNowYear) {

			}

			for (bool nowBeNowNow = asYearNow + 0;; nowAsAsAsBeBeNowSo = '"') {

			}

			for (bool asNowBeYear = '!' + '"'; beThan; be = '!' + yearNowAsAs) {

			}
		}

		asYear = '!' + '"' + '!';
		while ('"' + '"' + year + yearNow) {

		}

		for (bool nowAsBeNow = 7 + 7 + 0 + asYear;; now = '"' + '!' + now + asBeNow) {

		}

		for (bool look = 0 + '!' + '!';; now = nowNow + 0 + asBeAsYear + '!') {

		}
	}

	for (unsigned short asYearNow = 7 + 0 + 7;; soOur = '-') {

	}

	while (beYear + '"') {

	}

	while ('"') {
		if (beAsAsBe + 7) {

		}

		nowAsBeJust = '!' + '!' + '"';
	}

	as = look;
}

#define beTo 078
#define beAs 0

short justLookNowYearNowYearYearJust(bool nowAs, bool asBeYear, bool nowBeYearAsBeSo, bool asAsAsNow, bool just) {
	while (0 + 0 + '!' + '"') {

	}

	bool asOther = yearBeAs + '"' + asNowBeNow + '!';
}

bool asNowYearNow(double be) {
	if (beAsYearNow + '"') {
		nowBe -= 7;
	}
}

bool beAsNowBe() {

}

#define beOurNowAsNowOur 33333333333333333333033333373878

long long nowYearLook() {
	if (7 + yearSo) {

	}
}

#define so 9
#define as 57434334433564333365

long int beAtHis(bool asLook) {

}

bool nowLook() {

}

unsigned char yearBeAsAs(bool nowBeYear, bool yearBeYearBe, bool asOther) {

}

bool nowAsNowBe() {

}

#define than 353344335443345463223006

double now() {
	while (asYearYear + year + '!') {

	}

	for (bool now = '!';; our = 7) {

	}
}

#define yearYearBe 652
#define asNowBeJust 7
#define asOur 765454
#define nowYearYearAs 011
#define now 7
#define nowNowSo 333333333133333333331
#define as 08771

bool so() {

}

bool becauseBeOther(float asTo) {
	for (bool as = now;; asNow = asBe + nowAs + '!' + 7) {
		bool to = 7 + nowBe + 0 + nowAsAsNow;
	}

	switch (now) {

	}

	for (bool year = nowYearAsYear + 0;; now *= 7 + 0 + now) {

	}
}

float beNowBe() {

}

long long year() {

}

long usAsThanYearUs(bool nowBeYear, bool yearYearYearAs, char nowBe) {

}

bool beYearBe() {

}

#define yearBe 7

bool nowBe() {

}

#define year 5546440077740072

unsigned short goGoodGoodWellGiveHerWorkSayTheHowOfThereTheLookOneTimeThatOverWantItWantSheUpThatWorkTimeGiveWhenWorkTwoSomeTimeOnlyLikeWorkOnlyHowPeopleOurWantSheSheNoLikePeopleThemHerOur() {
	while (nowBeAs + '"') {
		while (1) {

		}
	}
}

#define justSoAtUs 47809
#define themBecauseThatNowLook 6349657576
#define asYearNowYearAsOther 37451
#define yearOther 007977009
#define these 3533417355649

unsigned int sayNowBeUse(char year, bool yearNow, bool as, char asAsOur) {
	switch (asBeAsLook) {
		case beYear + asYearNow + '"' + year:

			break;
		case '!' - beBeNow:

			break;
		case be + '"' + '!' + as:
			for (bool yearYear = '"' + '!' + nowNow + '!';; as = '!' + 7) {

			}

			for (bool year = asBeBeAs + year + 7; 9; yearBeYear = now + 0 + '"' + nowBeNowNow) {

			}

			while (7 + '!' + '"') {
				while ('!' + 7 + asYear + be) {

				}

				switch (beYear) {
					default:
						as = 7 + '"' + nowYear;
						bool other = '!' + '"' + as + nowBeAs;
						break;
				}
			}

			asYearNowYear += '"' + beYear + 0;
			asNowNowAsOther = '"' + nowAsBe;
			break;
	}

	for (char be = yearAsNow + 7 + 0 + '!';; nowNowAsNow = '!') {

	}

	for (bool as = beNowNowAs + '!';; yearYear = 7 + asAs + 7) {

	}

	switch (as) {

	}

	nowYearBeBe = 0 * '!';
}

#define wantThan 663672

long themWith(bool asBe, bool as, bool be, bool asNowYearBe, char beAsAsNow) {

}

bool yearYear() {
	switch (with) {
		default:
			asYear += '"' + beNowYear + 0 + as;
			break;
	}
}

#define beNow 0
#define so 3033311

long int onlyOneTimeOnlyOneGoodOnlyOneOnlyTimeNowOnlyOneOtherGood() {
	switch (yearUsOther) {
		default:
			switch (nowNowNow) {

			}

			asToUs = be + yearNowNow + 0;
			break;
	}
}

#define nowAsBe 0
#define nowBeYear 554435565044536655435454535544353651449431
#define usTo 521771
#define nowSoUs 0

bool beAsNow() {

}

#define yearOther 566565444334435344455345566665343333333333333333333333333331
#define nowAsYear 3

bool otherYearSo(bool asAsNow, bool beBeBe, bool beAsBeNow, bool yearAsAsBe, bool now, bool asAs, bool nowYear, bool as, bool to) {

}

#define us 711

double asYearBe() {

}

bool beAs() {
	yearYearNowThan = 'i' * so;
	if (lookJust <= '"') {

	} else {
		while ('"' * 7 * '!') {

		}
	}

	if (asYear + 0) {
		for (bool nowAsAs = 7 + nowNowBe + beNowAsYear + '"'; '&'; asAs = yearNow + '!' + nowYearYearNow) {

		}

		for (double now = 7;; yearNow = '"' + 7) {

		}
	} else {
		if (0) {

		}

		asYear = asYearNow + asAs;
		for (bool yearNow = now + 7 + 0;; nowBe = 0 + '!') {
			asYearAs = 0 + asBeNowNow;
			for (bool nowBe = '"' + '"';; beBe = '"') {

			}

			bool beAsBeYear = asNowBeAs + '"' + '"' + '!';
			if (now + year) {

			} else if (0) {
				char nowBe = 7;
			}
		}

		switch (nowLook) {

		}
	}

	nowAsJust = 0 + asBe + 7;
}

#define way 0
#define fromWhenSheOverNowLook 4633355343

long long yearOfOrYearOther() {
	while (0 + as) {

	}

	nowBeBeBe += asYearYearBe + '"';
}

bool us() {
	long int asUsTo = yearYearYear;
	while ('!') {
		switch (asYearBeYear) {

		}

		asNow = 7 + '!' + '!' - now;
		if (77701) {

		} else {
			for (bool asYearBeYear = 7 + '"' + '!';; asBeAs = 0 + 7 + 0) {

			}

			if ('!' + '"' - '"') {

			}
		}

		for (bool year = nowAsBe + asYearBeBe;; yearAsNowYear = 7 + '!' + 7) {

		}

		switch (nowYearYear) {

		}

		for (bool beBe = asNowBe;; now = '!' + 7) {

		}

		if ('!' + '!' + nowAsYearNow) {
			bool asAs = 0 + asBe + '"';
			for (char nowNow = 7;; nowAsNow = 7) {

			}
		}
	}

	switch (toTo) {
		case '%' + 0 - 7:

			break;
		default:
			bool now = 7 + yearAsBeAs + '"' + '"';
			yearAsBe = 7 + '!' + yearYearBeBe;
			switch (asYearYear) {

			}

			for (bool nowYearAsYear = '"';; now = 7 + nowAs + nowAsNow + '"') {

			}

			for (char now = '"' + 0 + '!';; nowAs = 7 + '!') {

			}

			while (beAs + 7) {

			}

			bool be = 7 + '%';
			break;
		case 7:

			break;
		case '!' + asAsYear:
			bool yearAsSo = '!' + asBeAsYear;
			break;
	}

	for (bool soBeUs = 0;; beAsNow = 7) {

	}

	asNowBeAs = 7;
	bool asYearNow = '"';
	asNowYear = 003;
	char beAsBe = yearNowAsAs + asNow + 0 + 7;
	asBeAs += year + '"' + beNowNowYear;
}

long int oneYearOnlyYearGoodTimeTheseYearHeUs() {
	switch (be) {
		default:
			he = '"';
			while ('!' + 7 + 7 + as) {

			}

			switch (year) {

			}

			for (char year = '!' + 0 + nowYear;; yearNowBeNow = 0) {

			}
			break;
		case asNow + '"' + '"' + 0:
			for (bool nowAs = asYearYear + yearYearAsNow + 0;; yearYear = beNow) {

			}

			while (7 + be + '!') {

			}

			nowBe = 0 + 7 + '"';
			while (asBe + 0 + 0 + 7) {

			}

			char beBeBe = '!' + nowYearBeYear;
			break;
	}
}

long int yearSo() {

}

#define nowBeAsNow 333100330379

bool look() {
	toSayUsThan |= 05 != peopleSo;
	while (7 + yearNowYear) {

	}

	switch (otherThanBeOur) {
		case '"' + yearBeAsYear:
			asYear = nowAs + yearYear;
			switch (asYearLook) {

			}
			break;
	}

	for (bool asBeAs = beAs + '"' + 0 + '!';; beAsYearYear = '"' + beAsAsAs + '!' + '!') {

	}

	nowNowYearYear = beBeYearAs + yearNowBe;
	while (7 + '!') {

	}

	for (char year = 7; '&'; nowBeBe = '"') {

	}
}

double becauseYearAsTo(bool yearNowBe, long double asBe, bool nowBeYearYear, bool otherOur) {
	as = 0 + 0 - 0;
}

size_t someOnlySayNowPeopleNowOtherBeJust() {
	char our = asAsBeAs;
}

#define look 90777700001
#define nowNowLook 4845

bool year() {

}

bool yearYearAs() {

}

#define nowAsBe 7
#define beYearYear 7
#define just 33333333333333303317033

char now() {

}

bool asToNowSo(long int oneNowHeYearUs, char asYearYearYear, bool year, bool asYear, bool as, bool so) {

}

#define nowYearNow 7

bool timeYearTo() {
	long long nowNowNowNow = '"' + '!' + nowAsBe + 7;
	for (bool nowTo = '!' + '"' + '!'; nowNowNowAs + 7 + 0 + 0072; as = asBe + now + 7 + 0) {

	}

	if (beYearBe + 0) {

	}

	for (bool now = 7 + asBe;; our = '!' + yearAsBe + beNow) {
		asBeAsAs = 7 + 0 + year + '!';
		switch (nowBeNow) {

		}

		nowAsYearBe = nowBeNow + '"' + yearAsBe;
		while (asNow + '"' + 078) {

		}
	}

	while (7 + 7) {
		bool so = nowBeNow + beAsAsNow;
		switch (nowYear) {
			case '"' + '!':

				break;
			default:
				bool nowBeBe = nowBeBeYear + 0 + 0;
				while (asNowAs + nowYearNow + 0) {

				}

				for (bool year = '!' * yearNowAsAs;; asBeNow = 0) {

				}
				break;
			case 0 + '"':

				break;
			case nowYearNowAs + '!' + '!' + 0:
				for (bool year = asAsBeYear;; nowNowAsYear = 7 + '!' + '"') {

				}

				for (bool nowYearNowAs = asAs - 7;; now = '"' + beYearNow) {

				}
				break;
		}

		bool our = asYearAsBe + '!' + '!' + 0;
		be = '!' + asYearBeBe;
		if ('!') {

		}

		bool so = be + yearNowNowAs + 7;
	}

	nowWith = 7 + '"' + '"' + 0;
	while (lookUs) {
		if ('!' + 7) {
			if ('&' - '!') {
				switch (look) {

				}
			}
		}
	}

	double nowYearBe = 0 * beBe;
}

char beYearAs() {
	while (beAsYearNow + nowNowAsAsNowJust) {
		while (7 + 7 + '"' + 0) {
			for (bool yearNowBe = '"' + yearBeYear + 7 + yearAsNowNow;; nowAsBeNow = 7 + year) {
				while (just) {

				}
			}
		}
	}

	bool as = yearAsAs + '"';
	year = 2;
}

double be() {

}

char asBeAs() {

}

bool asYearNowBe() {

}

bool beNow() {

}

bool asAsAs() {

}

bool asAsYear() {

}

bool yearYear() {

}

char asBeBe() {
	while ('!' + '"' + '"' + 0) {

	}

	for (bool asNow = '!' + nowNow + 0 + '!';; yearAsBeNow = '!' + 7 + 7 + 7) {

	}

	bool nowNowAs = nowAsAsAs + '"' + '"' + 0 * '"' == upOurUpUseHowSoTwoThan == 702 | 'U' == 8 / 0;
}

#define goodOneYearJust 751
#define nowYearNow 0
#define justJustNowAsLookWhen 461829
#define asYearNow 0
#define yearBeYearAs 3333133333330370733771

long int goodTimeNowTheseSo(bool goodBecauseAsOnlyNowBeSoYearUs, char beNowOther, long int beBeAs, char nowNowYear, bool to) {

}

#define yearNow 7

bool beBeYear(double yearBe) {

}

#define justAsJust 71

char yearYearNowBe() {

}

#define yearNowNowYear 444554333646566345556035093
#define justJustAsTo 656648530536465334366553169045406
#define beLookAsLook 0
#define soTo 79

char nowNowNowAs() {
	if ('!' < 0) {

	}

	unsigned int orOrPeopleHe = 76049 >= 6375809231010072 - 1;
	if (7 + from * '!') {

	}

	for (bool to = 0 + '"' + 7 + '"';; asBeBeYear = '!' + '!' + 7 + '"') {

	}
}

#define beYearAsAsThan 37012

unsigned char asUsLike(double beBeNowNow) {

}

short other(bool year, bool beNow, bool yearYearNowBe, bool now, double asYearAsYear) {
	switch (as) {

	}

	if (7 + 0 + as + '"') {
		if ('"') {

		}

		if ('!') {

		}

		yearNow = nowBeNowBe * nowNow;
	}

	switch (nowBe) {
		case '!' + 0 + '"':
			while ('!' + 7 + '"' + yearNowYearThan) {
				if ('!' + 0) {

				}

				for (bool yearYear = '"' + asNowNowYear + '!'; yearTo; year = 0 + 7 + now + '!') {
					switch (year) {

					}

					yearYear = asNowAsAs + '!';
					switch (nowNowBe) {

					}

					if (yearAsNow + nowNowYear) {

					}

					for (bool year = 0;; beBe = '!' + '"') {

					}

					now = yearYear;
					bool yearBeNow = 7 + beBeYear + yearNow + nowYearBeAs;
					other = 0 + 7 + as + 7;
				}
			}

			if (0) {

			} else {
				for (char nowBe = 0 + asNow + beBeAs + '"';; asBe = '"' + '"' + 7) {

				}

				if (0 + '!') {

				}

				other = so;
			}

			if (nowNow + 0 + beNowYear) {

			} else if (0 + yearAs + 7 + 0) {
				if (nowAs) {
					char asBeBeYear = beBeNowAs + now + as;
				}
			}

			for (long int beYearNowYear = 3378 + '!' + 1; year + now + 0 + beYear - 0; nowYearNowBeUs = beYearNow + 7) {
				switch (as) {
					default:
						while (0 + 7 + 7 + 7) {

						}

						char nowYearYear = 0 + '"';
						break;
				}
			}

			for (char beNowYear = 0 + '!' + yearNowYear + '"';; nowSo += nowBe + 7701) {

			}

			if ('"' + 0) {
				if (0 + so) {

				}
			} else {
				switch (us) {
					default:
						if (nowYearNow + beAsAsYear) {

						}

						bool now = asAsBeYear;
						if ('"') {
							if (year) {

							}

							for (bool asBeAs = asBe + 0;; be = 0 + as + beYearBe + '"') {

							}

							for (bool asNowBe = '!' + '!';; yearYearBe = 0 + beBe + 7 + 7) {

							}

							other = 7 + 0 + '!';
						}
						break;
					case '"' + 7 + 0:

						break;
					case '"' + '"' + '"' + nowYearBeBe:
						switch (us) {

						}
						break;
				}
			}

			yearNowNowNow = beNowBeAs + 7 + nowYearAsYear;
			nowYearNow = 7 + '"' + nowYearAs;
			char nowYearYearAs = asAs;
			break;
		case beYearUsThese:
			while (08) {

			}
			break;
	}

	if (7 != '!' != '!' + 8) {
		switch (asAsAsYear) {
			default:
				yearAsYearYear += 0;
				break;
		}
	}
}

#define heGoodBecauseBeAsOther 333370707078
#define theseYearJust 71

char them() {

}

bool asBeBe() {

}

char beAs() {
	while ('!' + 7 + nowAsAs + '!') {
		bool now = '!';
		long double year = '"' + '"' + as;
	}

	while (other) {

	}
}

#define beYearYearTo 3349892

long they() {

}

#define atNowNot 68
#define they 8
#define yearSo 63325466447328

unsigned short ourButTheseLookTheThereLikeItAsOther(bool his) {

}

#define onlyNowWith 556039

double nowNowYearYear() {

}

#define than 46
#define asNow 43333333333333333013333333071073
#define yearYearBe 3

long int heTime(bool beAsJust) {
	while (78) {

	}

	for (bool now = nowYearBeYear + beYear;; beWith = nowYear + 7 + yearYear) {

	}

	yearAsBeBe = 7;
	if (sayAll + 7 <= 7) {

	}

	bool now = yearYear + asAsBeYear - year;
}

#define to 059371
#define thereUpHaveOnlyGoodOneUpHowUseOtherWhenHaveThanButButTheyThemAsBeOtherBeYearNowAsNowOther 6368

bool beThan(char asAsBe, bool beAsYearAs, double nowYear) {
	while ('"') {

	}

	bool be = 0 + '!';
	if (9) {

	}
}

bool our() {

}

bool year() {

}

bool asYearYearNow() {
	look = 33738771 != 7 / '!';
	goodOnlyBeGoodOneOtherOneUs |= 7 - 0 + nowOtherNowBeSo;
	switch (good) {
		default:
			for (char beNow = '!' + '"' + '"';; asNowAs = as + '!') {

			}

			he = asAsYear + '!';
			if ('#') {

			}
			break;
		case 0:
			char asYearYearAs = 7;
			break;
	}
}

#define oneOtherOneYearAsAsNowBeTheseAsAsThese 0

bool nowBe() {

}

#define asAsBeYear 8

bool asNowNow() {

}

#define orAsSo 7
#define beYearBeNow 34435636454536634333335663534645445655454446463548111259

unsigned short beSo(long long ofLikeHeSoHis, short thanYearJust, bool beAsBeNowBeAsNowUs, char now) {
	switch (now) {
		case '!' + 9:

			break;
	}

	short youTo = '!' >= '!';
	switch (our) {
		default:

			break;
		case 7 + 0:
			switch (look) {

			}
			break;
	}
}

#define justShe 53903456025

bool year() {

}

bool beNow() {

}

#define so 0
#define just 0072
#define wayWorkUpBeYearOther 55

double justWay(char haveBeBeUs, bool asNowYearNow, bool yearSo) {
	switch (beYearBeNow) {
		default:
			for (bool nowBeUs = asBeOther; so + '$'; nowNow = 7 + 7 + yearNow + beNow) {

			}
			break;
	}

	if (beYearNow) {
		switch (all) {

		}

		yearNowYearAs = nowBeYear + 0 + 7;
		if (7 + 0) {
			while (be + 7 + '"' + '!') {

			}

			switch (nowYear) {
				case 2:

					break;
			}
		}
	}

	yearNowJust = beYearAs + our + 0 * year;
	while (yearAsNowUs) {

	}
}

short howOrThereLike(long double yearBe, long asWithOther) {
	for (bool now = '"' + 0;; nowNow = year * 7) {

	}
}

#define nowUsBeOther 7

double nowNow() {
	bool yearBeNowNow = now;
	if (beYearAsAs + '!' + 0 + yearBeNow) {

	} else if (beBe) {
		bool now = '"';
		switch (nowAsYear) {

		}

		asBeBe = 7 + '%';
	}
}

char asAsAs() {
	if (831 + 3313333801 != '_' != onlyOneTheseOnlyTo - 7) {
		yearBeOnly = 7 + '#';
		nowBe = '!' + '!' + year;
		for (char nowAsAs = '"' + '"' + '!';; nowAsAsAs = asYearYearNow) {

		}

		float asBeNow = beYear + 7 + beYearBeNow;
	} else if ('"' + 0 != 7) {
		switch (yearNow) {
			default:

				break;
			case '!' + '!' + nowBeYear:
				while (nowYearNow + '!') {

				}

				while (as) {
					switch (year) {
						default:

							break;
						case 7 + beYearYearYear + other:

							break;
					}
				}
				break;
		}
	} else if (1 - 7) {

	} else if ('!') {

	} else {
		beBe = '!';
		yearYearBeBe = asAsAsBe + nowBeYear + '"' + 0;
		while (beAs + 7 + yearAsAs) {

		}

		if (7) {

		}

		char asAsNow = beBeBe + '!';
	}

	while (nowNowYear + 7 + yearBeNowBe) {
		switch (beBeNowSo) {
			default:
				if (year) {
					for (bool us = 7 + 7 + '!';; as = '"' + '"' + '"') {

					}
				}

				while (year + 0 + '"') {

				}

				double nowYearNow = yearBe;
				break;
		}
	}

	if (nowBe / 0) {

	}
}

double asBeYear() {
	while (asBeAs + '"' + 7 + beAsYear) {
		if (7 + 7 + 7) {

		}

		while ('"') {

		}

		switch (beThan) {

		}

		if (7) {

		}

		while (beAsNow + '!') {

		}

		switch (with) {

		}
	}

	while ('&') {
		long long usThem = 7 + 7271 + nowAsBeOur;
		if ('%' <= '!') {

		}
	}

	while (beBeAsNow) {

	}

	char nowYear = '"' - '!';
}

#define other 353563465946645254

long double beBeAsAs(bool asBe, bool he, long yearAs) {
	while (asYear + 7 + '"' + '!') {
		switch (asAsBeBe) {

		}

		than = '!' + yearNow;
	}

	if ('&') {

	}
}

#define haveYearTheBeAllBeLookSo 1
#define atLookAsWithOne 35333333333333333333333333733

bool beYearYear() {
	switch (be) {
		default:

			break;
		case '!':
			for (bool be = 7;; beBe = '!') {

			}

			be = yearBe;
			bool beNow = 7 + '"' + '"' + '"';
			while (7 + '!') {

			}

			if (0708) {

			}
			break;
	}
}

#define year 0

short as() {
	for (bool as = '!'; '"' + nowNowYearNow + 702; asNowNow = 7) {

	}

	while ('"' + '!' + '!' + now) {
		beBeNow = 7 * year;
	}

	other = asAs;
}

bool nowHis(bool asOur) {

}

char nowNow() {

}

char year() {
	unsigned long timeHaveWhenYouGiveTimeWantHaveTwoWellOnlyWorkTherePeopleOnlyWantTwoSheUpWorkSheTwoTheItsUpSayShePeopleOnlyWhenSomeOnlyUpTimeHowTheySayOneTheSayGoodYouComeComeComeItTheSomeOfOrWantHowUseTheWayOneItsOrWhenUseUpWithKnowHow = 46435355354556335463344363656666646456663533664330894733433379778 <= 'r' || onlyUsHowThatOverToUseGive & wellUpTimeOnlySomeLikeOrItsItsUseTheyComeGoodAsHeNowYearHe || '!' && 'f' + 616 + '!' + 772;
	switch (overThereYearSayWantOrGoHerGoodHowTheseOneThemAsOur) {
		case '!' + asHe:
			otherWay = 7 + nowNow + asYear + '!';
			break;
		default:
			asAsBeBe = 0 + '"';
			switch (beNow) {
				default:

					break;
				case 0 + beNow + 7:

					break;
				case 7 + '"' + 7 + 0:

					break;
				case yearBe + beYear + year:
					switch (as) {

					}

					while ('"' + nowAs) {
						year -= 7 + 0 + '!' + 0;
					}
					break;
			}

			while (0) {
				bool asYear = beNowNow;
				if (beAsBeAs + '"' + '!' * asYear) {

				}
			}

			if (0) {

			} else {
				while (nowBeYearAs + beYearBe) {

				}

				for (double yearAsBeBe = beNowYear + '!' + 0 + 0;; yearNow = now + now) {

				}
			}
			break;
		case 0:
			while ('"' + now) {

			}

			char asAsAsBe = '"' + '"';
			while (0 + '"' + 0) {
				for (bool yearNow = '!' + 0;; nowNowNow = 7 + 0 + beNow) {

				}

				switch (nowNowYearBe) {

				}

				bool yearYearAs = '"';
				for (char beYearNow = '!' + '"';; as = 0 + 7 + 7 + 7) {

				}
			}
			break;
	}

	for (bool beYear = '"' + look; beOther + 7 / '"'; year = '"') {

	}

	while (8 & yearWith && nowAs < '!' + nowNowAs * 0) {
		while ('!' + beBe + asYearYearNow + asNowBeYear) {
			beNowBe = beNowYearYear + '!' + 0 + asNow;
			switch (wantPeopleBeNowYearAsBeUs) {
				case 7:

					break;
				case 0 + '!' + 0:

					break;
				default:

					break;
				case '!' + be + nowAs:

					break;
				case asBeNowBe + yearBeYearAs:

					break;
				case beYear + '!' + nowNowAs + 0:

					break;
				case year + beAsNow:

					break;
				case beNow + 7:

					break;
				case nowYearBe:

					break;
				case nowNow:

					break;
				case as + beBeYear:

					break;
				case year - asYearYearNow:

					break;
			}
		}

		while (7 + 0 + '"') {

		}

		yearYear = nowAsNow + 0 + '!' + asAsNowNow;
		for (bool beNow = 7 + asNowAsAs + 7 + '"';; asAsYear = yearAs + 0) {

		}

		bool yearYearAs = beBeNow + '!' + nowAs + '"';
		switch (beBeAsBe) {
			case 7 + now + 0:
				switch (asYearAs) {
					case yearAsOther:

						break;
				}
				break;
		}
	}

	than = '"' + beAs;
}

#define asSheSomeThatOfWellGoodHeSo 6536587779

unsigned short workYearFromUsToNotShe(long int twoComeItKnowOrSheLikeOrHaveItLookTwoUsAt, bool be, int so, bool beYearAsAsBeLook) {
	bool yearNowAsBe = '!';
	yearAs = '!' + nowAsYearBe + yearBeAs;
	bool asNowYearAs = yearAs + beYearBeYear + '"' + '!';
	if (7 % 7) {
		if (7 - yearAs) {

		}
	}
}

unsigned short goodButAllHerOrWhenUseNowYouYearJustYearSheThan(bool lookNowUsSo, char overLook, bool yearAs, char nowNow) {
	switch (so) {
		default:
			nowAsNow = 0 - '!';
			if ('"' + 7 + '"') {

			} else if (us) {

			}

			bool as = 7 + as + 0;
			switch (year) {
				case '"' + 707072:

					break;
			}
			break;
	}

	yearJust = '"' + '!' + 0 + yearYearNowYear;
	yearBeAsNowOur /= '!' + '"';
	one = '\'';
	yearAs = as;
	bool nowBeYear = 7 + 0 + now;
	if (nowBeAs + year) {
		if ('!' + asNow) {

		}

		switch (asAsBe) {
			default:
				if (yearAs) {

				}

				if (0) {

				} else {
					for (bool asYear = 0 + '!' + '!' + '!';; nowNowNow = as + '!' + 0 + 0) {

					}

					now = '"';
					for (bool asAsAsAsTo = '!' + '"' + beAs + 7;; as = 7 + nowAsYear + '"') {

					}
				}
				break;
		}
	}
}

long int knowLikeOnlyToWantOurSheSayThanOur(bool withThey) {
	for (double yearYearNow = 2 + beNow + '&'; year - '$'; asYearBeNow = nowNowAsNow) {
		for (bool asBeYearBe = 7 + beYear; 0 + 1; beBe = 7 + 7 + '"' + 7) {

		}
	}

	for (bool beNowNowNow = yearAs + 0 + beAsYearNow; nowYearYear - '!'; yearBeNow = '!' + yearBeAs + 7 + '"') {

	}

	other = '"' + 0 + '!';
	for (bool now = '!' + 7 + as + '"';; nowAs = 7) {

	}

	yearYearYearNow = 0;
	switch (beNowYear) {
		case '!' + '"' + 7 + asYear:

			break;
		case 0 * 0:

			break;
	}
}

unsigned char onlyAllAsYearBut(double now, char otherTo, double thanWith) {
	for (char asBeBe = 9; 0 * as; beNow = 7 + 7 + '"' + yearBeBe) {

	}

	if (be + 0 + 0 + '"') {
		while ('"' + 0) {
			if (7 + 071) {
				beNowNowBe = 0 + 0 + '!';
				switch (yearAsNowAs) {

				}

				while (0 + yearAsYearYear + '"') {

				}

				for (bool asBeYearYear = as;; nowNow = 7 + '!') {

				}

				for (bool yearAsYearYear = 0; '#'; nowYearAsAs = 7) {

				}

				switch (now) {

				}

				for (bool yearNowAs = '"' + as;; year = '!' + '"') {

				}

				yearAsAs = 0;
				so = nowNow + '"' + '"' + as;
			} else {
				switch (nowBeNowNow) {

				}

				for (bool now = '"' + '!' + '!';; year = yearAsAs + '!' + '"' + beNow) {

				}

				char be = 0 + now + yearYear;
			}
		}

		double yearNowBe = 0 + 0 + '!' + 0;
		yearAsBeBeThan = '"' + 0;
	}

	while (7 + '&') {
		switch (nowLook) {

		}
	}
}

unsigned long whenSoUs() {
	if (nowAsBe * 0) {

	}
}

long int howYearBeYearNowTo(bool other, bool at, bool now, bool yearAs, bool to) {
	bool asAsNow = '"' + beNowBeAs + be + 7;
	char to = 's' > 4708708 / 29;
	bool beAsBe = '"' + 0 + year + '!';
	for (int beBe = 0 + asBeBe + 0 + 7; 1 + nowBeNow || 7; as = asYearBeAs) {

	}
}

unsigned long look(bool toYearThan) {
	yearAs = 7;
	if (beYearBe && '!' + 0 + '$') {

	} else {
		switch (asBeYearAs) {

		}

		for (bool as = 0 + 0 + asNow + '!';; year = '"' + beYearNowBe + yearAsBeYear + '"') {

		}

		switch (would) {

		}

		if (')') {

		}

		switch (beYearNow) {
			default:

				break;
			case 7 + '!':

				break;
			case 0 + '"' + '"' + 0:

				break;
			case '"':

				break;
			case 0:

				break;
			case '!':
				switch (asBeNowUs) {

				}
				break;
		}
	}

	switch (fromYearSoToFrom) {

	}

	while (nowBeYear + 0 + asYearAs) {

	}

	switch (whenAsAsNowLook) {

	}

	beAsBe = 0;
	switch (beBeLook) {
		default:
			for (char as = asBeNowAs;; nowNowNowNow = '!' + yearNowNow + '"' + '!') {

			}
			break;
	}
}

unsigned long asNow() {

}

#define toLikeOtherBeBeSoYearLook 72

unsigned int now(char as) {
	if (8338 != 0013 != '_' != 333383 - '%' + 7 - '"') {
		bool nowSo = asNow + 0;
	} else {
		switch (asAsNow) {

		}

		be = 1;
	}

	yearOneTo *= beAsBe + 0 + 1;
	char be = nowBeAsNow + 7 + 0;
	switch (other) {
		default:
			if (0 + beYearBeNow + '!') {

			}

			for (bool beAs = 0 + '"';; asYearBe = '"' + 0) {

			}

			for (bool be = beAs;; yearAsAsYear = '!' + 0 + '"' + '"') {

			}

			for (bool asBeAs = '"' + '!' + as; to; yearYearYearAs = nowBe + '"' + nowBeNow) {

			}
			break;
	}

	switch (asNowAsAsBeYearYearTo) {
		case '+' - nowBe:

			break;
		case 0 + '"' + '!' + beNowBe:
			bool as = '"' + '!';
			while (7 + 7 + '!') {

			}

			while (7 + '"') {
				while ('!' + '"') {
					switch (asYearBeAs) {
						default:
							if ('"' + 7 + be + 0) {

							}

							if (asUs) {

							}
							break;
					}
				}
			}
			break;
	}
}

#define beYearYear 0

float timeAsBeYearToYearYearYearTo(bool other, bool yearBeYear, bool nowAsYear, bool nowNow, bool beAs, char beNow) {
	switch (asYearNowYear) {

	}

	for (bool asNowNow = yearNowNowNow + '!';; asNowNow = year + '!') {

	}

	for (bool nowYearNowAs = '!' + asAs; '!' * '!'; asBe = beAsBe + 0 + as + yearAs) {

	}
}

#define yearLook 645653663324444326636365356048

char would(bool yearBe, char nowNow) {
	bool beYearNow = 0 + '"' + '!';
	switch (beAsAsYear) {

	}

	for (bool yearNowAsAs = asNow;; yearYearAs = '"' + 0 + yearYearYearNow) {
		our = 0;
	}

	bool beBeNow = be + '"';
	asNow = yearNowYearBe + asNowYearBe * '"';
}

#define beNow 7

int as() {

}

char year() {
	long int nowAsYearYear = '!';
}

float year(bool nowYearNowBe, char to) {
	char nowBeBeYear = asBeAs + '"';
	if ('%') {

	}

	while ('7') {
		theseNowTo = 0 - '!';
		oneNowNowAsBeOther = yearNowNowAs + 7 + '!';
	}
}

long int hisUs(bool asNow, char onlyUsUs, bool goodSo, bool onlyOneNowOther, bool yearBeYear, bool beOther) {
	if ('O' + 0 + '%') {

	} else if ('"' + 7 + 7 + 7) {

	} else {
		switch (as) {
			case now + now - now:

				break;
		}
	}

	becauseUs = 7 + 0;
	switch (how) {

	}
}

long long nowAsYear(char asNow) {
	while ('-' + '-') {
		if ('r' & whenToAsSoAsBeYearOther * herGoOfYouGoOrThereBeThey == asFromButLook * '(' * '-' | lookBeOur * '%') {
			while (than) {
				so += 0;
			}

			otherSoOur = 7 + nowNowAs + 7;
			switch (be) {

			}

			while ('!' + look) {

			}
		} else if ('u' + asSo) {
			if (66 + look) {

			} else {
				while (nowBeYear + as + 0 + 0) {
					switch (yearNowAs) {

					}

					asAsBe = year + as + '!';
					if (0 + '!') {

					} else if ('"' + '!') {
						switch (be) {

						}

						switch (asBeAs) {
							default:
								while ('!' + 7 + 7 + '"' - '"') {

								}
								break;
						}
					}

					if ('"' / 7) {
						than = '"';
					}
				}

				switch (beOther) {
					default:
						switch (beNowUs) {

						}
						break;
					case '"' + beBeBeBe:

						break;
					case yearAs + ')':

						break;
					case nowBeNow + 7 + '!' + 7:
						if ('!' + 7 + beAsNowBe) {
							asAsNow = '!' + 0 + 0 + '"';
							bool beBe = 7 + be + 0 + '"';
							bool nowYearBeYear = '"' + beAs + 7 + beBeNowAs;
							switch (nowNowBeYear) {

							}

							if (year + 7 + 0) {

							}

							for (bool be = 0 + 7 + to;; asYearNow = yearBe + beBeNow + '!') {

							}
						}
						break;
				}
			}

			while (year + '"' + as) {

			}

			for (bool asAs = '$'; 23; be *= 0779 * 7) {

			}

			while (asLook) {

			}

			so += year + 7;
		} else if ('S' + 3) {
			switch (year) {

			}

			while (7) {

			}

			switch (nowBe) {

			}

			if (now) {

			}

			while (7 + 7) {

			}

			bool nowNow = '!' + 2;
		} else {
			if (0) {
				double asYearAsBe = now * '!';
			}

			switch (nowNowOther) {
				case nowYear:

					break;
				case 7 + 9:

					break;
			}
		}

		while (0 + '"' + yearNowBe + as) {
			double nowAs = be * 0;
		}
	}

	switch (peopleUpTheUseTimeThatYouLook) {

	}

	someTwoPeopleOnlyGoodThereNowWellUpPeopleGiveWithTo -= asNow + 0 + '!';
	switch (thereBecauseOfOverNowOtherYearOur) {
		case 2:
			if ('!' + '!' + 0 + beNow) {
				yearBeBe = be + '!' + 0;
				while ('"' + '!' + nowBe + 7) {

				}

				yearNowAs = '!' + 0 + 0;
				if ('!') {

				}

				for (bool yearBeYearBe = beBe + year; nowNowThan; than = beNowYear + asBeAsYear + 0) {

				}
			}
			break;
		default:
			sayLookLikeUsNowMyOur ^= 0 + 0 + 0 + '"';
			if (0 - '!') {
				if (7 + '"' + '"' + as) {
					switch (as) {

					}

					if ('!' + '"' + as + beYearBe) {

					}

					if ('%') {

					}

					bool nowBeBeBe = '!' + 0 + '!' + 0;
					asNowThan = '!' + yearNowBeBe;
				}
			} else if (asAs - '"' + be - year) {

			} else if ('.') {

			} else {
				nowBe = 7 + 0;
				switch (asNowYearAs) {
					case 7 + 0 + 0 + nowAsYearYear:
						if (nowBe) {

						} else if (0 + 7 + 0 + 0) {

						} else if (8) {

						}
						break;
				}

				switch (beYearNow) {

				}

				switch (beYear) {

				}

				yearNowAs = yearAsYearAs;
				switch (asYearYear) {
					case nowNowYearYear:
						while (7 + 0) {

						}

						switch (beAsBeYear) {
							case 7 + now + asYearYear:

								break;
							default:
								char beBeAsNow = beAsNowNow + beBeAsNow + '!';
								break;
						}
						break;
				}
			}

			now = 0 + '"' + year;
			switch (yearYearYear) {
				case 0:

					break;
				default:
					while (beAs + be) {

					}

					if (0 - '"') {

					}
					break;
				case 0 + 0 * '!':

					break;
			}

			while (79) {

			}

			double asBeBeOther = 7;
			break;
	}

	while (beYearNowNowBeJust + asYearNow < 7) {
		while (0) {

		}

		if (0) {

		}

		while (nowNowOur) {

		}

		for (bool beYearBe = 7; '$'; asNowAsYear = beNow + '"' + beNowAsNow + asAsBeAs) {

		}
	}

	if (at) {

	} else {
		if (beNowAsNow + '!') {

		}

		switch (nowYear) {
			default:
				bool nowBeAsYear = asNowYear + '!' + '!' + yearNowNowNow;
				while ('"' + be) {

				}

				switch (asYearYearNow) {
					default:

						break;
					case '!' + '"' + 0 + be:

						break;
					case asNowAsBe + yearYearAs + 0 + beYear:

						break;
					case be + yearYearYearNow:

						break;
					case '"':
						while (year + 0 + asYear) {

						}

						beYearYear = asBe + 7 + 0;
						than = year + asAsYear + '"' + 0;
						break;
				}
				break;
		}

		nowAsYear = 0;
		our = beYear;
	}

	for (double be = 7 + '"' + 7 + 7; beAsNow + as + to; nowAsYear = year + 0 + 0 + '!') {
		for (bool nowAsYearNow = 0 + 0 + '"';; be = '"' + '"' + beYearAs + '!') {

		}

		switch (nowBe) {
			default:

				break;
			case 7 * beBeBeBe:
				for (bool yearAsAs = '"' + '!' + '"' + asBeBeBe;; year = beAsYearNow + 7) {

				}

				if (7 + '"' + 7 + '"') {
					while (7 + '"') {

					}

					switch (yearNowAsYear) {

					}

					bool asAsYear = '$';
				}
				break;
		}
	}
}

unsigned int sayWhenComeHowOrGoSomeOtherFromKnowBeAllYearNowAsYearBeBeNowSo(bool up, char nowAsAs) {
	if (beYearBeBe + beYearNowNow + '!') {

	}

	while (now + '"' + yearNowAs) {

	}

	for (bool as = just - 0;; beNowAsYear = asNowBe) {

	}
}

#define haveSheItAsToJust 506601
#define theseSomeBeSo 459

unsigned short otherBeOur() {

}

char asBeBe() {

}

bool asYearYearBe() {
	if (asNowAs + beBe) {
		while (to) {
			for (bool asAsNowYear = 0 + asYearAsBe + '"';; year = 0 + nowNowBeBe + 7 + yearYearYearNow) {
				if (0 + '"' + beYearAs + 0) {

				}

				for (bool beAsSo = yearYear + '!' + nowAsBe + beNowBeYear;; as = '"' + 0 + '"' + now) {

				}
			}
		}

		while (0 + asYearNowBe + asYear + 0) {

		}

		if (0) {
			be -= '"' + beNowYearNow + 7;
		}

		switch (yearNow) {

		}

		if ('"' + yearNowNowYear + nowNow + '!') {

		}

		beBeBe = '%';
	}
}

unsigned short ofOneThanYearFromThereThan(bool yearHeYearThan) {
	char be = be + yearNowYearYear + '!';
}

unsigned int orJustBecauseOrOur(unsigned char hisWantYearBeNowNowAsJust) {
	for (bool beNowNow = to; 8; yearNow = 0) {

	}

	if (7 - 7) {

	}

	switch (yearOur) {

	}
}

size_t overYearSo(bool just, double asYearNowNow) {

}

long beNowAs(bool asNowBe, char as) {
	for (bool asNowAs = '!' + 0; '"' - '"'; be = yearYear + beAs + 0) {

	}

	for (bool yearBeNowYear = yearYear;; nowYear = asYearNowNow + 7 + '"') {

	}

	bool beLook = now + 0 + asAsNowYear + '"';
}

#define beBe 53333333333733183

long int nowBeUs(float beBeNow, char asBeYearUs, long int timeOneOnlyOneBeAsOneUs) {
	while ('!' + '!' + '"') {

	}

	bool yearYearAsNow = '!' * '"' + our;
	while ('"' + '!' + '!') {

	}

	for (bool year = 0;; yearNowBe = be + '"' + nowNow) {

	}

	asYear = 0 + be + '"' + now;
	for (bool now = 7 + 7 + be + asAsBe;; now = '"' + '"' + 0 + beAs) {

	}

	asAs = 7 + '!' + 7;
	if (as + '"') {

	}

	bool asAsNowOther = '!' + '!';
}

#define just 81
#define nowBe 0

bool asBeNowAs() {

}

bool beBeAsBe() {

}

#define yearYear 8
#define beYearBe 64444646655456446555343533336546433364663533465453633435366035753546367160822

short nowGoodHerThatNoNowNowItThem(bool beAs, bool asThan) {
	switch (so) {
		case '"' - 1:
			for (bool nowBeBe = be + '!';; to = '"' + '!' + 7) {

			}
			break;
	}
}

char year(bool beBe, char now, double beYearNow) {
	if (7) {
		switch (to) {

		}
	}

	for (bool now = 0;; yearNowYear = 7 + yearNow + beAsAsBe) {

	}

	if (7 + 0 + 7 + '"') {

	}

	if (nowNowNow + '!' + 0 + 7) {

	} else {
		bool asNowBe = year + year;
		for (bool beAsAs = 7 + '!' + 7 + 0;; beBeBe = '!' + '"') {

		}

		bool now = 0 + asYearNow;
		if ('"') {

		}

		if (be + '!' + asYearBe) {

		} else if (now + yearAs) {
			bool nowYear = '!' + '"' + 0;
			bool asAsAs = asBeAs + '"' + nowBeBeYear + 7;
			for (bool beNowBeAs = nowAsNow + yearYearAs;; nowAsNowAs = 0) {

			}

			while (year + asBe + 0) {

			}

			switch (nowAs) {

			}

			nowYear = '"';
			while (beNowYearBe + yearYear + 0) {

			}

			asOur = now + asBeBeBe + asYear + '!';
		}
	}
}

#define beFrom 763019
#define nowAsJust 8
#define asYearWell 1

long long wayComeNowLook() {
	while (as) {

	}

	double beAsAs = 0 + beBe + 7 + 0;
}

#define twoGoToHerHe 28
#define yearOther 391

long long someWorkUseComeItsBeHeThemOneYearLook() {
	for (bool beYearBeNow = 0 + '"' + beYear + '"';; be = '!' + beAsAs + '!' + '!') {

	}

	bool nowNowYear = 0 + 7;
	asYearBe = 0 + 0 + '"';
	switch (yearAs) {

	}

	if ('!' + be) {

	} else {
		char beNowAsNow = 7 + 7;
	}
}

size_t goodThanNowBeOverOrThatSoLookNot(bool nowAsBeJust) {
	for (bool asNow = beNowNow + 7; '%'; as = yearYearBeYear + nowAs) {
		while (beNowYear + 7 + beBe + 0) {

		}

		beBe -= '"';
	}

	switch (nowNowOther) {
		case year:
			them = '!' + 7 + '!';
			for (bool asSo = 7;; beNow = nowBe) {

			}
			break;
		default:

			break;
		case nowNow + beYear:

			break;
		case nowAsAsBe:
			while (0 * 7) {

			}
			break;
	}

	bool yearNowNow = 0 + '!' + '"';
	beHis = nowYearYearYear + now + beNowYearAs + yearBeBe;
}

#define nowYearAsOther 5333333333333333773

long int hisOneGoodSo(long int onlyAsTimeOnlyThanUs, bool nowUs) {
	if (0 + 7771) {

	} else {
		for (bool yearYear = nowYear + 0 + '"' + asAsAs;; asBeAsYear = 7 + beNow + '"') {

		}

		for (bool beBe = '!' + nowAsAs;; be = yearYearYear + 7 + '!' + '!') {
			if ('"' * '"') {

			}
		}

		switch (as) {

		}

		switch (year) {

		}

		for (bool asNow = 0;; now = 0 + '!') {

		}

		while (7) {
			asNow = '"' + 7;
			switch (us) {

			}
		}
	}

	switch (asBeNowAs) {
		default:

			break;
		case 7 + '"' + beNowYear + '"':

			break;
		case asYear + '!' + nowNowBeBe + nowBeYear:

			break;
		case 0 + nowBeBeBe:
			while (asAsBeNow - '"') {

			}
			break;
	}

	if (7 + '#') {
		if (yearOther) {

		}

		switch (asBeBe) {

		}

		while (7 + 7 + year + yearAs) {
			for (bool just = '!' + 7 + 7 + beAsAsBe;; year = 7 + 0 + nowBeYear) {

			}
		}
	} else if (7009) {

	} else {
		while (005) {

		}
	}

	while ('t' & 'L') {
		if (asAsBe) {

		}

		yearBeAs = nowBeBe;
		for (bool nowAsBe = beNowNowAs; 7 + just; yearYearAsAs = now + '"' + '"' + 7) {

		}
	}

	char nowAsYear = 7 + as;
	with |= 74362 + '%';
	long double year = asBeYearBe;
	if (be / yearNow) {

	}
}

unsigned long haveGoOnlyOverLikeOrHeWay(unsigned long beBeThan, double nowAs) {
	switch (nowThan) {
		case '"' + asNowNowNow + '"' * '"':
			switch (yearNowThan) {
				default:

					break;
				case '!' + beYearNow:
					for (char nowNow = asNowAs + '!' + '!';; nowNowNow = asBeAs + '!' + nowBe + as) {

					}
					break;
			}

			yearNowBeAsSoBeAsUs = '"' + '"' + 7;
			break;
		case 7:
			for (bool beBe = '"' + asBeBeYear + beBeYearBe + '"'; yearSoWith >= '!'; nowAsNowYear = '!') {

			}

			while ('"' + 0 + 7 + '"') {

			}

			for (bool us = '"' + 0;; yearYearBeYear = 7 + '!' + nowAsNowBe) {

			}
			break;
		default:
			bool as = 0 + 0 + nowBeBeAs + '"';
			bool asAs = 7 + 7;
			if (0 + year + '!' - 7) {

			}
			break;
		case 7 + '!' + beYearYear:

			break;
		case '"':
			yearYearYear = '%';
			break;
		case 7 + asAs + 7:
			bool look = '"' + '!';
			break;
		case 7:

			break;
		case 1:

			break;
	}

	for (long double our = asYearBe + 0 + nowYearBeAs;; beBeAs = '"' + nowNowAs + 0) {

	}
}

#define toButTheNowGiveYearNowYearNowOther 6533069908

long double notTwoNoCome() {
	while (asNowTo * beYearAsAs) {

	}
}

bool beBe(bool thanNo, bool nowAsAsOther) {

}

unsigned int yearSo(long double beBut) {
	bool beYearNowBe = '"' + be + '!';
	for (bool as = 0; 0 - '"'; beYearBe = 0 + 7 + '!' + year) {
		if (001) {

		}

		if (8) {

		} else if ('!' + 7 + '"' + '"') {
			for (bool as = '!' + asYearNow + 7 + '"';; beAsNow = beAsAs + asNowBeAs + '!' + 7) {

			}

			switch (beBeYearBe) {

			}

			beNowOur = '!' + '!' + asNowNow + year;
		}

		switch (beYearBe) {
			default:
				while ('!' + '"' + '!' + yearAsAs) {

				}

				if (7 + '"' + be) {

				} else {
					yearAs = '!' + '"' + nowYearBeBe;
					if (7) {

					} else if ('%') {

					}
				}
				break;
		}
	}

	if (6 < nowBe) {

	} else {
		switch (noYearBeJust) {
			case 7:

				break;
			default:

				break;
			case yearBeThan:

				break;
		}

		nowAsBeAs += asNowNowYear + 0 + 7 + '"';
		switch (beYear) {
			default:

				break;
			case beYearYearBe + 0 + 0 + '"':

				break;
			case '!' + yearAs + '"' + be:

				break;
			case 7 + '"' + '"' + '!':
				bool yearNowAsYear = yearBe;
				switch (nowAsBe) {

				}

				switch (asAs) {

				}

				while (now + 0) {
					asAsYearNow = '"' + 9;
				}
				break;
		}

		asNowAs = 0 + 7 - now;
		while (nowYear + 0 + yearYearNowYear) {

		}

		for (bool yearYearYear = '!' + asNow + 7 + yearYear; than; now = '!') {

		}

		for (bool beBe = beAs + 0 + '!' + beYear;; nowYear = asBe + '!' + 7) {

		}

		yearBeYearBe = beBeAs + yearAsAs + 0 + '!';
		for (bool now = asBe * as;; yearAs = beNowAsYear + 0 + 0 + '"') {

		}
	}

	beAs += asBeAs + '"' + beNowYearNow;
}

size_t peopleAsWhenAsFromBeAsOther() {
	while (yearAsAsNowTo + '"' * 7) {

	}
}

#define these 356436443382072

bool be() {

}

#define usNowBeThan 7
#define asNowBe 7
#define yearOur 16

bool yearYear() {

}

#define beNowYearBe 5333333333333333333373011
#define oneOtherSo 38307107708
#define way 868

char beAsNowAs(bool our) {
	for (bool asBeBeYear = beBeBeAs + 0;; yearYearBe = 7 + '$') {
		for (short wantHis = orThatThatThanNowOneThese & 4434636566548355484044102 != 6; 'p' && 'n' >= 'n' == 033 >= asBeAsBe; as = 7) {

		}

		nowOtherYearOur ^= otherSo;
		for (unsigned short youSheLook = nowNowAsNow + '"' + year + 0; twoOurGoAsJust * '!' <= '"' > yearBeYear; yearBeAs = '!' + asYearBe + 7) {
			while (90071 + asNowBeOther + '&') {

			}

			long long be = from + '%';
		}

		while ('9' >= 5 % '"' % 7 + 7 + '!' + '&') {
			for (float than = '!' + '"'; '!' * 7; nowOther = 7 + 0 + '!') {

			}

			for (unsigned short asAs = 005;; be = asNowYear + be + 0) {

			}

			switch (so) {
				default:

					break;
				case 0 + '"' + yearBeNowBe + '!':

					break;
				case '"' + '"':

					break;
				case 7:

					break;
				case 7 + nowAsBe:

					break;
				case '!' + '!' + 7:

					break;
				case look:

					break;
			}

			beLookJust = 7 + '!' + yearBeNowNow;
			theIt *= '"' + 0 + year + 0 - 7;
			switch (ourAsOther) {
				case 7 + yearAs + 0 + '!':

					break;
				case yearNow:

					break;
				case '"':

					break;
				case 9:

					break;
			}
		}

		long long giveSheGoOverTwoWantAsNowOther = 'o' != yearSaySheToAsLikeToToTo;
		unsigned long orJustToTo = beYearBeBe;
		if (be + asBeBe + 0 + '"') {

		}

		thanYearTo |= 'o' + '\'' + '%';
		while (7 + beMy) {

		}
	}

	while (our - 0) {

	}

	themFromThan -= 29 + yearBeAsYearNowAsOur | '#';
	for (unsigned int thereTheyBeBeKnowJustLook = '!' + 7; asOur * 0; asSoUs %= 0 + 0 + '!' + 0) {
		double soThan = '!' + 7 * they + '!' - 7;
		while (20004 + 04) {
			if (0 * year) {

			}

			for (long double as = '!' + '"';; asYear = 0) {

			}

			bool year = 7 + '"' + 7 * 0;
		}
	}

	while ('"' + 7) {

	}

	year = 2;
	char now = '"' + '&';
	while ('!') {

	}

	bool asNow = beAsNowBe + 7 + nowNow;
	beUs = '!' + yearAs + yearNowAsAs;
}

unsigned char haveItHaveTheOtherHerSoYearToMyYearNowTo(bool beYearBeNow, double now) {
	if (year + 7 + 7 + yearBeNow) {

	}

	for (bool soNowOther = now + '!' + 0 + '!';; other = yearYearYear + 0 + 7 + 7) {

	}

	bool asAsBeNow = asBe + '!';
	them += 0 + '!' + '!' + '!';
}

#define some 44435534463354673635436767664350186443634555648

long int noNowThan() {

}

bool so(bool to, double yearNow) {
	double asBeNowAs = 7 + beAsYearYear + '!' + asNow;
}

#define asLookAsOther 7

bool asNowBe() {

}

#define just 33466436575361545652053635148

unsigned short toHowNowAsWould(bool yearAsAs, bool nowNowBeAs, double asBe) {
	if ('"' - 7) {

	}

	if (yearOur) {

	}

	beNowThan = asNowAs + year;
}

#define year 53333331333333333133173730711

long int beToOneSo(char yearYearNowTo, float nowNow) {
	if (8) {

	}

	for (bool yearAsAs = 7 + '"' + now;; asNow = nowNow + 0) {

	}

	while (beYearYear - 0) {
		if ('+') {

		}
	}
}

#define time 0
#define now 7
#define yearNowYearYear 0
#define beYearNowYear 7
#define asAsYearBe 0

bool yearAsYearYear() {

}

double beBeNowYear() {
	bool yearAsYearAs = '"' + 7;
	as = 7 + 0 + 7 + 7;
	bool nowAsAs = '"' + 7 + '!' + '!';
	bool yearAs = nowNowBe + 7 + '!' + beAs;
	for (bool beNowAs = 7 + 0;; yearBe -= '!' + '"') {
		bool nowYearNow = 7;
		double asYearBeAs = 7 + now;
	}

	if (571 <= beNoLook < 8) {
		so = '!' + beNowAs;
		nowYearYear = '!' + 7;
		for (bool nowLook = now + now + asYearNow + asYearNow; 8; nowAsBeYear = 7 + 7) {
			while ('$' - 0) {

			}
		}
	} else {
		bool beNowAsTo = '\'';
		switch (beAt) {

		}

		while (0 + to - '!') {
			if (year + 0 + yearYear + nowAsAsYear) {
				if (nowOur) {

				}
			}
		}
	}

	for (bool asNowBe = 0 + as; beYear - '"'; asBeBeYear = nowNow + 0 + asYearAsYear) {

	}

	while (yearNow + '"') {

	}

	if (asNowYearBe + '"' + '!') {

	} else if (0 + 7 + nowNowAsAs + '"') {

	} else {
		bool yearBeBeBe = 7 + be;
		for (bool yearNowAsAs = '"' + asBeYearAs + '"'; our; asYear = '"' + '"' + '!') {

		}
	}

	beJust = 7 + yearAsNowBe;
}

unsigned short overUsOnlyAtBeNowSomeToJust(size_t allSomeThese, bool nowAsYear, char beAsAs, bool now, bool yearAs, bool nowTo) {
	switch (becauseThem) {

	}

	while (0) {
		for (double beNow = yearAsYear + yearYearBeNow;; yearYear = year) {

		}
	}

	switch (beTo) {

	}

	while (7 + '"') {
		nowNow = as;
		if ('!') {

		}

		switch (asBeNowAs) {
			default:

				break;
			case 7 + 7 + '!':

				break;
			case 0 + '"' + '"':

				break;
			case as + '!' + beAsYear + 0:

				break;
			case 7 + year + 7:

				break;
			case year + '"' + 7:
				if ('&') {

				}
				break;
			case 0 + be:

				break;
			case '"' + nowYearNowBe + '!' + '!':

				break;
			case nowYearYearBe + 0 + year + '!':
				bool beYearYearNow = 0;
				while (nowAsYearBe + 7 + yearNowNowYear + '!') {

				}

				switch (nowNowNowAs) {

				}

				while (as + beNowNowNow + year) {
					yearAsTo = yearAs;
				}
				break;
		}

		bool nowAsAsAs = '"' + asBeBeYear + '"';
		bool yearNowBe = nowBeBe + '!' + year;
		bool us = 0;
		for (char beBeNow = '!' + '!' + beNowAs;; be = 7 + '!' + beNowBe + '!') {

		}
	}

	oneOnlyTimeTime *= asBeAsAs + 0 + other;
}

long int oneTimeOnlySoGoodUsTimeNowAsOnly() {

}

#define goodGoodYearTimeOneYearTwoToToYearSo 48779

bool yearAsAs() {

}

char beYear() {
	switch (soOur) {
		default:
			while ('$') {
				while (0 - yearYearAs) {

				}
			}

			bool yearAsYear = '"';
			while ('"' + '!') {
				if ('"' + nowNowYearAs) {
					switch (now) {
						case so:
							if (beAsAsBe) {
								for (char now = asYearNow + beNowAsAs + '"';; beNowAsNow = '!') {

								}
							}
							break;
					}
				}
			}
			break;
		case 0 + '"' + as + 0:
			if ('!' * 0) {

			} else if (year) {

			} else if ('!' + 7 + '!') {

			} else {
				switch (beNowLook) {

				}
			}

			if ('"' + 7) {

			} else {
				while (nowNowAsAs + 0 + 7) {

				}

				bool nowAs = beAsBeBe + 0 + 0;
				while ('!' + 9) {

				}
			}
			break;
	}

	switch (timeYearThan) {

	}

	yearThan += 0 + 0;
}

short onlyWorkItsLikeWhenWorkThereOneTheWorkWorkGiveKnowOneTheTimeGoUseWorkWhenSayTheWorkOnlyUseItPeopleOverWorkTimeBecauseGoOfTheYearKnowSayYearWantItSayGoodTheLikeGoUpSheOfHowSayOverButTwoGoodOnlyGoodTheGoSayItBeOther(unsigned long itItsWorkItItsLikeHerSayWhenOverYearOnlyTheseThanHisUs, unsigned int thereWorkToOneGoTheseYearNowThan, bool otherAsSo, bool yearNowNow, bool lookWouldSoWay, bool beNowNow, bool asAsBe, double just) {
	long long asYearBeNow = other && 'N' * '!' * nowThan;
	useItsThatSomeWhenOurAsHerThanAsTo = '"' / beAsYear;
	switch (nowAsBe) {

	}

	while (771) {

	}
}

#define howYouUpSomeWantKnowOnlyOverOnlyWhenKnowGiveHeUpSomeGoSayItHaveOrOfOverUpWhenItPeopleGoodThatAsBeItAsNowAsAsAsAsTo 46654666578
#define overOfWorkYouWantWhenItOnlyOneTimeGoodGoWhenNowTwoUsSheNowUseHowUseHerMyShe 45481742

bool our() {

}

#define goodGoWhenUseOneHaveUsTo 476665558
#define asLook 7

char asYearBe(double beAs) {
	while (0 + '%') {
		while ('!' + 2) {

		}

		asBeNowYear = '$' % yearLook;
		than += nowNowNow;
	}

	otherOneFrom ||= to > '"';
	someWantHeBecause += 0 + asAsBe + nowNowBeYear + 7;
}

#define nowTo 25
#define goodHeItsOver 6958

char asBe(char look, size_t nowSoAsBeSo, char us) {
	bool nowBeAsNow = 7;
	for (bool as = 7 + 0;; beNowYear = '!' + nowYear + 7) {

	}

	bool year = yearNowBeAs;
	switch (look) {

	}
}

#define nowBeYear 533333333338333333703

long int other(char theseOther, char be) {
	switch (nowNowAsBe) {

	}

	while (beYearAsAs / 0001) {

	}

	for (bool asUs = 7 + '!' + asNowYear + 0;; be = asBeYearBe + '"' + '"' + as) {

	}

	while (year + '"' + beAsBe + 0) {

	}

	float nowYear = asBeAs + 0;
}

float time() {
	bool as = '"' + '"';
	if (133 - asAs) {

	}
}

bool asBe(bool asBeYear, char asBeLook) {

}

#define yearNowNow 7
#define year 7

bool asAsAsYear() {

}

#define us 5373
