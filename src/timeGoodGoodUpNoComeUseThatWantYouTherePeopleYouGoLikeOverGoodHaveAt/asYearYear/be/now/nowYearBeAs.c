#include <UUUUEEUUUUU0/0.h>
#include <Xktgs/vN.h>
#include <C/0.h>

#define useLikeUsOrGoodThan 641

float so() {

}

bool otherTo(char yearNow) {

}

#define asAs 0
#define as 64314008

short atOneGiveWhenWellSoJust(bool workUsYearAsItOtherYearSo, bool nowOur) {

}

long double comeYearThanWellAsWork(char ourTo, unsigned int nowOtherYearNowAsWould) {

}

#define atJustTo 633333333333333373333333337308
#define now 3208
#define soOur 0
#define asYearYear 078
#define us 7719
#define now 7
#define to 07777007702

bool yearAs() {

}

bool beAsYearBe() {
	bool asBeNow = 0 + 7 + 0;
	switch (nowLook) {
		case theTheWantLikeSomeAsYouGoWellPeopleWorkGoodLikeNowGoodOrUseKnowTimeOneOneSomeSheItGoodPeopleFromBeLikeSoOverOrMy && or | timeUpJustWhenSomeAsItThem != 2701 | 79 >= lookItHerThemSay:
			for (bool beBeNowAs = 7 + '"' + yearAs + 7; yearAsLookNowAll * good && beBeNow - 0; asYearAsAs += 79779) {

			}

			if (so <= '"') {

			} else if (asAsBe + asBeNow) {
				while (72) {

				}
			} else if ('!' - 7) {
				if (beAsBe + 7 + 7) {

				}

				switch (nowNow) {

				}

				if ('"' + '"' + '!' + '!') {

				}

				yearYearBeAs = '!';
				switch (yearBeSo) {

				}
			} else if (be + 0 + 7) {

			} else {
				while (0 * '"') {

				}
			}

			for (unsigned short lookYearSo = 'q' * '&' + now - 0;; yearUs = 'u' && '#') {

			}

			if ('!' + '!') {
				if (2) {

				}

				bool than = '!' + 0 + 0;
				switch (yearYear) {

				}

				if (yearYearAsAs + asAsYearBe + yearOur) {

				}
			} else {
				while (077071) {
					double beBeAsAs = nowBeAs + '!' + 7;
				}
			}

			if (timeGoodThemWorkThereWouldAsYearNo & 'i' >= whenTimeWhenGoNotTheLikeSomeUpThatUseOfOrLikeToThatBut + 07008 | '%' + so * now) {
				for (bool be = nowNowBeYear; now * 7; yearBeNowOur += 0 + 0 + '"') {
					switch (now) {
						default:

							break;
						case yearThan:
							while (beAsBe + '"' + nowYear) {

							}

							beAs += '"' + 0 + '!' + beYearYear;
							break;
					}
				}

				switch (beBeBe) {

				}

				double asYear = '!';
			}

			unsigned short goOfTwoLikeHaveMySomeTimeHeItUpGoodUpAllYearUsOnlyUsAt = 'J' && beThanBeOur % '"';
			because |= ';' <= 1;
			if (54568437876744302309 - '!') {
				for (bool yearNowAs = '!' + '!';; beAs = '"' + nowBeYearNow + '"') {

				}

				bool yearYearAsAs = 7 + us;
			} else if (85709 + nowNow + be + nowNowSo) {
				double beYearNowAsTo = beNowBeBe;
				if (as + '"' + '!') {

				}

				bool now = 7 + 7 + '"' + 0;
				if (beBeAsYear) {

				}

				for (bool year = '"' - nowAsAsYear;; be = '!') {

				}
			} else {
				while (35562 + 7 * yearYearAs) {
					while (7) {
						if (7 + '"' + 7 + as) {
							bool yearYear = 70701;
						}
					}

					while (655179) {
						for (double nowAsNow = '"' + '!' + our;; be = '"' + '!') {

						}
					}

					double asBe = 7 + '!' + 0 + '!';
				}

				for (bool be = 0 + '"' + '!'; 0 - 7; nowNowAsNow = 7 + nowBeBe + '"') {

				}

				bool so = beNowYear + 0;
				switch (yearNowBeBe) {
					default:
						for (double beNowYearAs = be + be;; asAs = be + 7 + '"' + yearYearYear) {

						}
						break;
					case yearBeAsBe + 0 + nowAs:

						break;
					case '"' * '!':
						if (2) {

						}
						break;
				}

				for (bool beBeYearYear = yearNowAsYear + nowAs; '!' * year; beAsAsNow = '!') {

				}
			}

			switch (now) {
				case nowYear + 0 + '"':

					break;
				case '"' + 7 + '"':
					bool beAsYearNow = 007072;
					break;
			}

			switch (ourNowBeGood) {
				default:
					while (0 + 7 + yearNowBeAs + '!') {

					}

					for (double beBe = 0;; be = 7 + as + beNowYear + beNowAsYear) {

					}

					switch (year) {

					}

					while ('"' + asYearBeBe + asYearBeNow + yearYear) {

					}

					while (8007772) {

					}
					break;
			}
			break;
		case 3444536484556353635572 & 5645442 == 'z' + look * as + '#' * beAs:
			switch (twoSheYouItGoodTimeTheYouJustComeBeGoOrSo) {
				case someBeBeAsJust + 0 + '!' * 0:
					for (long beAsNowYear = '!' + as + be + '"';; nowAsNowAs = 0 + '"' + asAs) {

					}
					break;
				case '"' + yearBe:
					while (asBe + asYearBe) {
						while (7 + '!') {

						}

						char yearNow = nowYearYear + 7 + now + asYear;
					}
					break;
			}
			break;
		case whenItsGoodAtNoYouLikeThereBeHaveLookAsNo | youYouWellYouBeThere >= 0 < allWantFromAsTo % '"' * now:
			if (09 + now * asBeAsYear) {

			} else {
				switch (nowOther) {
					case 0 + 0 + 7 + nowBeNow:
						switch (year) {

						}

						bool nowBeYearBe = '"' + 0 + nowYear + '!';
						bool year = yearBeBe + '"' + asYear + be;
						yearBeNow -= 7 + 7 + '!' + '"';
						break;
				}
			}
			break;
		case '"' * 0079:
			other >>= 14946 + '.' * '!';
			switch (nowYearBeNow) {

			}

			if ('#') {
				switch (way) {

				}

				switch (asAsYearNow) {
					default:
						switch (nowAs) {

						}

						while (78 + nowAsYear + '!' + 70709) {

						}
						break;
				}
			}

			while ('!' + asNowYear) {

			}

			yearNow = 1 * beBeYear;
			break;
		case '"' - '=' + be + beSo:
			year = 08 % '"';
			if ('"' + nowAsAs + '"') {

			}

			for (bool beYearBeNow = 0;; nowThan = 7 + asAsNowBe) {

			}

			if (asAs) {

			}

			because = yearAsYear + 7 + 7;
			break;
		case 5:
			if (thanJustYearAsUs) {
				if (7 - now) {

				}
			} else if (7 + asAs) {

			} else if (yearAsAsBe + 7 + '!' + '!') {

			} else {
				while ('"' + 0 + 7 + 0) {

				}

				switch (beAsYearNowYearOther) {

				}
			}

			for (bool nowAs = '"' + 0 + 0; 1; beAsAsYear = '!' + beBeBeYear) {

			}

			if ('b' > 'q' > youWant + 4) {

			} else {
				switch (yearBe) {

				}

				while (beNowAsYear + 0 + 0 + 7) {

				}

				switch (beNowBe) {
					default:

						break;
					case 0 * 0:

						break;
				}
			}

			while (709) {
				yearMy = beBe;
				bool nowYearAs = 8;
			}

			if ('8') {

			}

			switch (these) {
				case nowBe + asAsBeBe + '"' + 0:

					break;
				case be + nowBeNowAs + '"':

					break;
				case 7 + 0 + '!' + '!':
					if ('"' + '!' + beAsYearBe + '"') {

					}

					switch (as) {

					}

					if ('"' + yearBeBeNow + '"') {

					} else if (2) {

					}
					break;
				default:

					break;
				case '!' + 0 + yearBe + 7:

					break;
				case 0 + 7:

					break;
				case beYear + 7 + 7 + '"':
					if ('"' + beNowNowAs + '!' + 7) {

					}

					for (double nowBeYear = nowNowAs + 7 + '"' + 0;; year = year + 7 + 7 + asBeNow) {

					}
					break;
				case yearAsBeYear + year:
					for (bool nowYearNowYear = asBe + asNow + nowYearYearYear + '!';; yearBe = '"' + 0 + 7) {

					}

					for (bool nowAsBeBe = asNow + '"' + be; 7 - asBeBeAs; nowAs = nowNow) {

					}
					break;
			}
			break;
		case 7 + '&' + yearUse + 2:
			nowAs -= '"' + '"';
			bool yearNowYear = 7 + 0 + 0;
			if (nowNowNow + '#') {
				bool as = '!' + '"';
				for (bool asYearBe = asAs;; asBeNowNow = '"' + now + asAs) {

				}

				while ('"' + 7) {
					switch (asAsNowAs) {

					}

					switch (yearBe) {

					}

					if ('"' + year + be + nowYearAs) {

					}

					while ('"' + '!' + '!') {

					}

					for (bool asYear = 7 + nowAsNow + nowYearAs;; as = '!' + asAsAsNow + 0) {

					}

					asBe = '!' + nowNowYearBe;
					if ('#') {

					}
				}
			}
			break;
	}

	switch (otherJustSoToLookSomeTheUs) {
		default:
			for (long int itButTheAsOfToPeople = asNowYear; '!' + '%'; beYearAsAs = '"' + 7 + 7) {

			}

			char beYearNow = nowNowNowBe;
			bool asBe = yearBe + 7 + '"' * 7;
			for (double yearNowNow = beYear + '"';; asNow = 0 + 7 + '"') {

			}

			for (bool now = nowNowTo;; nowYear = '!' + 7 + beNow) {

			}
			break;
		case wantSayWhenYearHerLikeThatNowComeJustNowBeWay + 7 + yearYear * beNowYearNow:
			bool beNowAsBe = 7 + 7;
			asYearYearYear = 0 - 7;
			break;
		case '!' < beBe + nowYear < 7 * 7:
			for (bool year = '!' + 1 + 0 * '!';; yearAsAsNow = '!' + 0) {

			}

			bool soOtherWay = 59559002 + 7 + '"' - '!' + 20707002;
			while ('&' * nowBeLook) {

			}

			for (double sheThan = 77770002;; yearNowBeAs = '!') {

			}

			switch (nowAsNow) {

			}

			if ('!') {

			} else {
				switch (nowBeYear) {
					default:

						break;
					case beYear + '"':
						switch (nowYear) {

						}

						switch (so) {

						}
						break;
				}
			}
			break;
		case lookItsBeAsThan + '!' <= '&':
			char yearBeAs = 0 + '\'';
			for (double other = 0 + 7; asBeNow + yearAsYearNow * asAsNowYear; as = '!' + 7 + be) {

			}

			if (7 * 7) {

			}
			break;
		case yearYearBeBe + '"' + asBeNowAs:
			for (bool nowAsAsBe = nowNowYear + '"';; asYearAsNow = nowYear) {
				switch (as) {

				}

				beYearAs = '!';
				while (7 + 7 + '!' + '!') {

				}

				while (nowBe) {
					switch (beAs) {

					}

					bool nowAsYearAs = '!' * yearBe;
				}
			}

			if ('"' + '!') {

			}

			for (bool beYear = '"' + nowYear;; asYear = '!' + 7 + 0 + asAs) {

			}

			switch (beNow) {

			}

			beYearYear = '"' + nowAs;
			switch (yearBeBe) {
				default:

					break;
				case be - yearBeAs:

					break;
			}
			break;
	}

	if (usSomeItsItsThereUpYearKnowGiveAsSheNotThey == 63483 > 53465544953645363558 / herTwoWhenNowWhenHisTwoToNowNowYearTo && 'b' % nowNowNow) {
		while (7 && 0 + now * 0) {

		}

		bool justOtherBeBeOur = nowNow + 0 + '!' + '!';
		if ('"' + 7 + beYearYearBe + '!') {

		}

		while (7 + 0 + nowAsBe) {
			switch (our) {
				case '!' + '"':

					break;
				default:

					break;
				case '!' + asYear:
					if (0) {

					} else {
						if ('"' + beBeAs + beNow + '"') {

						} else {
							switch (to) {

							}
						}
					}
					break;
			}

			for (bool be = 7 + '!' + '"' + asYearYear;; nowNowBe = 7) {
				switch (yearAs) {

				}

				if (7 + 0 - 7) {

				}
			}
		}
	} else {
		switch (ourBeThatSo) {
			default:
				if (5202) {

				}
				break;
		}

		if ('"') {

		}

		beNowBeNow = '"' + 7;
		switch (yearNowYear) {
			default:
				switch (asAs) {
					default:
						nowBeNowNow = 7 + asAsBeAs;
						while (asLook) {

						}
						break;
				}
				break;
		}
	}

	switch (workHis) {

	}

	if ('-' * beYearNowAs) {
		asAsBeAs = asYearBe + beBe + 0;
		for (bool asBe = beNowNow + 7 + 7 + nowNow; 7 < '!'; nowAsBeAs = 7 + beNowAsAs + '!' + '!') {

		}

		for (double yearYearNow = beYear + 7;; yearAs = 7 + '!' + 7) {

		}
	} else if (yearNowAs + 0) {
		while ('"' - 7) {
			if ('!') {

			} else if ('!' + '"') {

			} else {
				while (8) {

				}
			}
		}

		bool yearYear = 0 + beYearAs;
		if ('&') {

		}
	}

	if (asNow + be + asBe + asYear) {

	}

	nowBeNow -= yearYearBeYear + 7 + 7 + asBeYear;
}

#define upWellOverUpThereAtOurThanButBeWay 5233156039
#define useSheItsGiveOrWellWorkTwoWorkUseItUseLikeUpOurSomeItsWantKnowYouWayPeopleOverUseItsSomeOfTheWhenThereGoodOrHaveSayAsNowAsOur 3533556407046
#define howLikeTheyHowOurItsUseItsUseYouThanOurThan 82
#define yearNow 7
#define asYearAsAs 8

char knowYearMyBeSo(int yearBeNowYearBeAsAsBeLook, double asAsYearYear) {
	while ('&') {

	}
}

#define someButPeopleOverYearThanOther 9

unsigned long asYearNowNow() {
	while (nowLook) {
		while ('!') {
			while (be + 7 + 7) {
				switch (just) {

				}
			}
		}
	}

	yearYearHowBeGive += 0 + be + '"' + '"' - '!';
	likeSayNowYearAsThan = 7 + 7 + '"' + 0;
	for (long int thanJustYouAsUsSomeOrGoodTheyOther = be & 300671 * 7 + '!' + yearUs; 5437615 + '"' - '"'; asAsBeYear = beBeAs + '"' + 0 + 0) {
		switch (year) {
			case beYearNowYear + '!' + 0 + 7:

				break;
			case be - '!':

				break;
		}
	}
}

#define ourJust 8
#define nowYearYearBe 0

bool as() {

}

#define be 333333371008

long int so(bool asOne, long int theseBecauseBeAsUsHisBeBeOther) {
	while (nowAsNowYear + '"' + yearBeNow) {
		while (0 + '!') {

		}

		if ('!' + '!' + 0 + 0) {

		}

		for (bool nowYear = '"';; us = 0 + 0 + asYearYearAs + 7) {

		}
	}

	switch (nowAsYearAs) {

	}

	bool so = yearYear + 7 + '!';
}

float asBeAsYear(float so, bool to, bool asNow, char as) {
	bool yearAs = nowBe + 7 + 7 + be;
	nowSo += 0 + 7 + '"';
	switch (yearNowBe) {

	}

	asBeYearAsAsAsBeNowTo = 7 + 7 + 7;
}

#define nowYear 3011
#define now 7

long int yearAsTheseUs(float year, char justBeNowAsNowYearOur) {

}

float beBeYearOtherBeLook() {
	switch (to) {
		case 78:

			break;
		case 0 * '!' * asYearAs:

			break;
	}

	while (9 | ourBeTo - '>') {
		while (asBeBe + asNow + '"') {
			if (but) {
				if ('"' + '"' + 7 + nowBeNow) {
					bool just = be;
				} else if ('!' + 7 + yearAsAsAs) {

				} else {
					switch (as) {

					}

					switch (beBeAs) {

					}

					bool nowAs = beNowYearYear;
					if (0 + now + '"') {

					}

					beBeBeAs = '!' + be + 7;
					beYearYearBe = 7;
					if (0) {

					}

					while (yearAsAsAs) {

					}

					nowAsAsNow = '"' + 0 + 0;
					switch (than) {

					}
				}
			} else {
				while (yearAsYear + nowYear + asNowAsYear + 0) {

				}

				switch (asLook) {
					default:

						break;
					case '"' + 7:

						break;
					case nowAsBeBe * 7:

						break;
				}
			}

			while (7 + asYearBe) {
				switch (nowLook) {

				}

				for (bool asBeYear = '!' + 0 + '"' + '"'; 270707778; year = 7 + nowYearBeNow) {

				}
			}
		}

		bool now = '%';
		switch (yearBeNowYear) {

		}

		bool so = beYearAs + 0;
	}

	nowAsNow += thanAsBeAsBeAsYearLook + 709 + '"' - asAsBe * nowNowNow;
}

long long soBut(bool asSo, bool nowBeBeNowUs) {

}

#define asNowNowYear 7

long long just(bool now, long double yearAs) {

}

double yearYearNow() {

}

#define yearBeNow 06

bool yearYearAsNow() {

}

#define nowNowYear 7
#define nowYear 0

double now() {
	while (7 + 0 + 0) {

	}

	bool beNowNowYear = 633375 & 2765;
	while (nowNowYearAs / us) {
		switch (look) {
			default:
				if ('!' * '!') {

				}
				break;
		}

		while ('\'' + 0 - '"') {
			double yearYearYearYear = beNow + '!' + '!';
			switch (at) {
				default:
					while ('$') {

					}

					if ('!' + year + '"') {
						switch (asAsOther) {

						}

						switch (yearAsYearYear) {
							case '"' + '!' + '!' + yearAsNowAs:

								break;
							default:

								break;
							case other:

								break;
						}
					}
					break;
			}
		}

		switch (well) {

		}

		for (bool other = asNowBe; now + '$'; asNowYearNow = beYearBe) {

		}

		double ourNowAsAsThan = as + 7 + '"';
	}

	bool asBe = '.';
	if (beLook + asBeBeNow & yearAsThan * '!') {
		way = as;
		switch (asBeNow) {
			default:
				as = '&';
				break;
		}

		char year = 0 + 0 + asBe + nowNowYear;
		switch (asNow) {

		}

		yearBe = nowYearAs + 0;
		to = 0 + '"' + nowYear;
	} else if ('"' + '$') {

	}
}

#define nowAsAs 0

unsigned long workTo(char asNowBe) {

}

#define beAsYearBe 0

float nowBe() {
	while ('?' + us) {
		while ('!' + nowBeBe + 7 + 0) {
			switch (beNowNow) {

			}

			for (bool be = 0 + 7 + year;; year = beAs + '!' + '!') {

			}

			for (bool asAsBeYear = '"' + 0 + beBeAsNow + '!';; asAsBe = '!') {
				switch (yearBeNowAs) {

				}

				if (7 + '!' + asBe) {

				}

				while ('!' + '!' + 0) {

				}

				if (nowYearYearBe + '"' + nowAs) {

				}

				switch (us) {

				}
			}

			if (now + '"') {

			}

			switch (year) {

			}

			while ('"') {

			}

			for (bool yearBe = 0;; now = 7) {

			}

			us = '"';
		}

		bool only = 7 + beAsNow;
	}

	for (bool beAsNow = '!' + '!' + '!' + '!';; usOther = 0 + as - be) {
		for (bool as = 7 + '!' + '"' + '!'; 71; yearBeBe = '!' + nowYearYearBe + '!' + 7) {

		}

		for (bool yearNowNow = 0 + '"' + nowYear;; us = '"' + 7) {

		}

		if (yearYearBeNow) {
			while (yearOther) {
				bool nowNowAsAs = '!' + other;
			}
		}
	}

	char asYearOther = '_';
	while ('!' + 1 - 7) {
		switch (beUs) {

		}
	}

	bool be = nowNowAsNow + now + beBeNowAs;
	for (bool asAsYearNow = '!' + as + '!' + 7;; yearBeNowNow = '"' + 0 + 0 + nowAsAs) {

	}

	for (bool be = 7 + be + '!'; '"' - '!'; nowYear = 7) {

	}

	nowBe += asOther + 008;
}

char beTimeOtherYearAsBecauseToNowUs(bool nowNow, bool asAs, bool other, bool be, bool yearSo) {
	for (float asAsAs = now + 0 + 708;; yearNowBeNow = 7 + '!' + asNow) {

	}

	long int now = year + yearAsAsYear + '"' + yearYearNow;
}

long int oneHisOurNowNowAll() {
	beNowBeNowJust = '!' + beYearBeAs + '"' + nowBeYearYear;
	if ('"' + 0 + '!' + '!') {

	}

	while (7) {

	}

	if (now) {

	}

	switch (thanNowLook) {

	}
}

#define us 7
#define yearNowOur 4536436335636135557544545335344675456402
#define justThey 5772
#define orWhenHowHerLikeUpOfThereLookBeNowBeBeOur 4336433045443277355201
#define youOverSomeSheJustGoNowThanBeAsAtYearWith 34553759512
#define peopleComeAsAsLook 06
#define ourAsOtherOur 0009

bool beYearYear() {

}

double year() {
	unsigned int beAt = 66534545328 == 'g' > '+' & at - beNowTo || '"';
	year = 7 + '!' + 7;
	for (double asAsNowAs = '!' + '"';; yearAsYearYear = '!') {

	}

	while (001) {

	}

	while ('"') {

	}

	yearOur = 0 + 0 + yearYearAs;
}

#define nowNow 0
#define nowBe 0

bool nowNowAs(char yearYear) {
	nowAsThan = 0;
}

#define yearAsJust 979
#define asAsNowNow 789

char nowThan(bool beAsNow, char thanAsNowTo, bool asYearNowYearNowSo) {
	if (year + asNowYearAs + 7) {
		while (0 + '!') {

		}

		switch (be) {

		}

		switch (these) {
			default:
				bool beBe = '!';
				switch (other) {

				}
				break;
		}
	} else if ('!') {
		if (7 + '"' - 7) {

		}
	}

	if ('!' - otherTo) {

	}

	while (0 + 7 + year) {
		long int beBeBeBe = '!' + '+';
		if (asYearAs - '"') {

		}
	}
}

long int asNow(char beNowYear) {
	if ('"' + '!') {

	} else if (beAs + asNowTo) {

	}

	char be = '"' + 7 + '"' + 7;
}

#define timeUsGoodGoodOnlyUsOnlyOnlyHis 383303
#define so 3336
#define becauseNot 3065639437363730903
#define ourAsSo 656446136204453356208741178

unsigned short because() {
	if (beAsAsNow + year + 0) {

	}

	while ('!' + asBe + 7 + '"') {
		if ('"' + '"' + '!' + '!') {

		}

		while (7) {

		}

		if (asAs + 7 + 7 + asYearNowBe) {

		}

		if (yearSo) {

		}
	}

	bool beAsAs = '!' + '!' + '!';
	if ('"' + '!') {
		bool year = 6 - now;
	} else {
		asYear = yearNow;
		if ('"' + '!' + 0) {

		}

		if ('"' + 0 + 0) {
			nowNowAs = 7 + 0 + '!';
			switch (beAsBe) {
				default:
					while (nowYearNow + 7 + 0) {
						for (bool asAsBeAs = 0 + 0 + 0 + '"';; be = '"') {

						}

						for (bool asBe = '!' + 0 + 7 + '!';; yearBeBe = beNowBe + '"') {

						}

						switch (nowAsYearAs) {
							case '!':

								break;
							case 7 + 7 + '!' + nowAsNow:
								if (asYearAsAs + 0) {
									switch (now) {

									}

									to = 7 + beYear + beAsYearNow;
								}
								break;
						}
					}
					break;
			}
		} else {
			for (bool beAsBe = '!';; beYearNowBe = 7 + nowAsNowYear + 7 + nowNowBeYear) {

			}

			switch (beYear) {

			}

			for (bool asNowNowNow = yearNowBeOur;; beNow = yearNowYearBe + 7 + beAs) {

			}
		}
	}
}

unsigned char other() {

}

#define useSayComeMyNowUs 442
#define to 0

bool asAsNow() {

}

#define beYearBe 204

short orKnowNowThereOnePeopleWantGoodJustSayOrSayBeThan(unsigned int beNowOther, bool beAsAs, long int ourHer, bool themBeLook, double other) {
	while (0 + 0 + nowNowBeYear) {

	}

	switch (nowNowSo) {

	}
}

unsigned int butTheyBecause(bool beYearBeAs, bool be, bool to) {
	if ('!' + '!') {

	}

	while (9) {

	}
}

#define us 333330333333333333333330373333730701

long int there() {
	while (8) {
		switch (yearBe) {

		}

		if (yearNowYear + '"' + asYearBeYear + '"') {
			while (beBeYearBe + asBeYearBe + now) {

			}

			bool asBeYearAs = 7 + '!' + 7;
			while (as + 7 + '"' + year) {
				as = 7 + nowNowBeBe;
				while (nowNow + nowAs) {

				}

				so = beNow + 0;
			}
		}
	}

	for (char year = year + '"' + nowNowNowYear;; asNowAsBe = nowAs + '"' + be) {

	}
}

char beYearAs() {

}

#define as 4335456655543436656546636566455333333333333333333373303737331333

bool otherThese(bool yearYearNowAs, bool year, float look, bool be, bool but) {

}

#define nowYearYearNow 7
#define to 7

bool asNowNow() {

}

bool our() {
	if (asAs + '!' + '"' + 7) {

	} else {
		beYearNowBe -= 0 + '"';
	}

	asAs = yearYearNow + 7;
	if (0) {
		for (bool yearAsBe = to;; nowBeNowBe = '"' + yearBeBeYear + '!' + yearBe) {

		}
	}
}

#define goWorkOfUseBeWayNowNo 1431

int as() {
	if ('!' + '!' + 7 + 0) {

	}

	if (0 + '"') {

	}

	for (bool asAs = 7 + 8;; beNowAs = '!') {

	}
}

bool beBe() {

}

#define nowUs 0
#define yearBeAs 563544212

char workOverWantThan(bool soOrThanAsJustYearAsJust) {
	while (all) {
		switch (nowYearYear) {
			case '!':
				while (071) {

				}
				break;
		}

		asBe = '"' + nowNow + 0;
		bool year = '#';
		yearYearNowBe = '!' + year + '"' + 0;
		switch (as) {
			default:

				break;
			case '"' + 7:

				break;
			case '"' + 7:

				break;
			case asYear:

				break;
			case 1:

				break;
		}
	}
}

#define yearNowAs 79

unsigned short useTheseSoWhenGoItsSo() {
	thanBeHis = yearSo;
	while ('"' + '"') {
		for (bool beNow = '"';; asBeBeNow = be) {

		}

		if (7) {
			for (char as = beNowNow;; yearAsYearYear = '"' + '!' + 0 + 0) {

			}
		}
	}

	if ('!' + asYear + 0) {
		for (bool nowNowAsNow = '"' - 0;; asYearBeYear = yearAsNowBe) {

		}
	}

	bool be = '!' + yearAsAs;
	if ('!' + asYearBe + 7 + 7) {

	}

	for (bool beYearYear = 0 + '!' + 0 + yearBeYearNow;; nowYear = 7 + '"' + yearYear) {

	}

	bool year = 0007009;
}

#define goodHisGoodTimeOneNowTimeOneGoodToTimeTimeUs 3337333333333354634444665354336564434434413561364002

bool now(bool us) {

}

char now() {

}

#define toAt 035009

size_t now() {

}

#define be 7

bool beAs(char as) {

}

float nowBe() {
	if (lookJust / '!') {
		while (nowBeNow + nowAsNow) {

		}

		for (bool yearBeBe = yearBeBeAs + 0 + '"' + 0;; yearNow = 0 + '!') {

		}

		while ('"') {
			if (as + now - 7) {

			}
		}
	}
}

#define comeHaveOverUseNotLookThereThanNoBeThanWhenBeBeAsOther 8423332
#define wellMyGoWorkNowOnlyGood 60771
#define than 7

bool yearNowBeNow() {

}

bool beNow(bool as, bool yearYearBeYear, bool asNowYear, bool asBeYear, bool nowAsBe, bool yearYearNowYear, bool beBe, bool beYearNow, bool beNowBe, bool as, bool asAsYear, bool beBeAs, bool asNowOther) {
	if (0 - 7) {

	}
}

#define nowHis 33333

char timeGoodTheseHeBeNowUs(bool beAs, bool yearAs, bool asYearBeUs, char be) {
	beAsBe = '"' + 7 + 7;
	switch (asAsOther) {

	}
}

#define goodBeBeTo 7770118
#define nowNowAsBeSo 1308

char oneBecauseSoUs(bool these, char so) {
	if (771 - '!') {

	}

	bool timeAsBeAsSo = 1 - beOther;
	as = 0 + 7;
	while ('"' + 0 + asNowYearYear) {
		for (char year = '"' + '"' + '"';; yearBeAs = 0 + 7 + 7) {

		}
	}
}

bool so() {

}

#define beAs 0
#define beYearNow 0
#define nowBeBeNow 7
#define nowYearBe 0
#define yearBe 63443337646675

long double year() {

}

long int onlyTheHaveOurNowBut(float yearBeBe, unsigned short yearThemOrThanYearJustSo, bool asUs) {
	if ('!' >= '"') {

	}

	while (be - 7) {
		switch (yearBeYearYear) {
			default:

				break;
			case '!' + '!' + 0 * '"':

				break;
		}
	}

	if (7 + '#' * '"') {

	}

	bool now = '!' + 7 + 0;
	if (yearYear) {

	} else if ('"' + nowNowBe + 0) {

	} else {
		if (7 + as + '!') {

		}

		for (bool yearYearBeBe = yearNow + 0 + beNow;; yearBeBeNow = asNowNow + beYear) {

		}

		while ('"' + 0) {

		}

		for (bool just = 0 + '!';; asNowAsNow = yearYear + yearAs + 0) {

		}
	}
}

#define look 0
#define asYearNowNow 7

bool asYearBeYear() {

}

long now() {
	for (bool otherSo = 31; timeGoodTimeThese + 7 + '!' - 0; other = '!' + nowAsNow + 7 + 7) {
		if ('!') {
			yearBeNowYear = '!' + '"' + yearNowAsBe + beNowYear;
			for (bool be = asYear + '!' + 0;; nowAsAs = 7 + '"' + nowNowNowBe + 7) {
				switch (yearAsYear) {
					case beAsNow + asBeYear + 7:
						us = 7;
						break;
				}
			}
		}

		bool beNowAs = us;
	}

	for (float beAsYear = nowBe + 7; goodYearHisOnlyBecause - '"'; asNowBeNow = 0 + '"' + '!') {
		switch (otherBeUs) {

		}
	}
}

#define hisTimeBeYearTo 383

long int goodAsBeBeAsBecause() {
	while (7) {
		while (708) {
			char now = '"' - 0;
		}
	}

	for (bool asAs = year;; as = 0 + 7 + nowYear) {
		switch (beAs) {
			default:
				switch (asBe) {

				}

				switch (nowYearAsBe) {

				}

				bool asNowAs = '"' + nowNowAsAs;
				char be = 0 + 0 + nowAs + '"';
				break;
		}
	}
}

short justFrom() {
	if (0 + '"') {

	}

	bool as = nowBeNowBe;
	switch (nowNowJust) {
		default:

			break;
		case '!' + year + '"' + '!':
			as = than;
			break;
	}

	bool nowNowBe = 7;
	switch (beBeYear) {
		case 7 + '"':

			break;
		default:
			switch (yearBeYear) {
				default:
					switch (asNow) {

					}

					for (bool yearAsYearBe = nowAsAs + 0;; asBeBeAs = yearNow + 7 + 7) {

					}

					while (asYearAs + 0 + '"' + beBeNowBe) {

					}

					so = asAs + '!' + '!';
					break;
			}
			break;
	}

	for (bool asYearBeYear = 7 + asBe;; yearNowSo = '"' + yearAs + '!') {

	}
}

unsigned int beOtherBeThanToJust() {

}

#define year 0

size_t beAsNow(float nowAsBe, bool nowUs) {
	switch (beNowOur) {
		case '.' + to:

			break;
	}
}

#define yearNowYearBe 0

char be() {
	switch (like) {
		case 'c' / 7:
			while (')' - 0) {
				for (bool beBe = yearYear - '!';; be = 7 + asAs + 0 + 7) {

				}
			}
			break;
		default:

			break;
		case now:
			for (bool now = be + '"' + yearNowNowBe + 0; '#'; asBeYear = asAs + 7 + asAsAs + 7) {

			}
			break;
	}

	beItBecauseJust += 909;
}

#define people 64156320788708
#define theyUpComeFrom 3644402

bool yearBeAs() {

}

bool yearNowAs() {

}

bool asNowAsBe(bool year, bool yearBeAsYear, char nowAsYear) {
	switch (nowAsBeYear) {
		case asNow + '#':

			break;
	}
}

long int goodOnlyGoodTimeGoodTimeGoodGoodOneGoodSoTimeOnlyTheseBeUs(bool now, bool usOther, bool good) {
	for (char asNow = 0 + 7;; nowNowYear = 0 + 7 + nowNowAsYear + as) {

	}

	for (char asAsNowBeSo = '"';; asBeBe = '!' + yearNowBe) {

	}
}

char otherThese(bool year, bool beBeNow, bool to) {
	if (beNowNow + yearAsBe + 0 + '"') {

	}

	if (0 + 0 + '"' + asBeNowBe) {

	}

	nowNowAs = '"' + 7 + nowNow + 0;
	switch (yearNowAs) {

	}

	while ('!') {

	}

	bool nowYear = asYearNow + 0 - 0;
}

#define myBeNowNowSo 7001

bool my() {
	while (7 * 0) {
		for (char asYearBe = nowBeAs + 0 + beBeBe; nowAs * beYearAs; as = yearNowBeNow + 7) {

		}

		for (bool asAsAs = 0 + '!';; yearAsBe = nowBeNowBe + 0 + '!') {
			bool nowAsNow = 7 + asBeNow + 0;
			asNowNowAs = 0 * nowBe;
		}
	}
}

unsigned long wellHaveWantBeJustUsAsTo() {
	bool asNow = '"' + 7 + 7;
	for (bool year = asNowBeYear * 7;; asNow = as + '!' + beNow + '"') {

	}

	if (beAs + asNowBe + 0) {

	}

	double asBeNow = be;
}

#define nowJust 7

bool yearBe() {

}

#define beAs 839368
#define now 7
#define usUs 7

double asBeBeBe() {
	if ('}' <= nowNow / '"') {
		while (7 + '"' + nowAsAs) {

		}

		long asYearNowNow = beYearAs + '"' + asNow + '!';
		while (be + 7 + 7) {

		}

		while (7 * nowAsYear) {

		}

		for (bool beYearNow = '"';; now = '"' + 7) {
			bool look = '!' + '"' + '"' + nowBeAs;
		}
	} else {
		if (asBeAsAs + nowBe) {

		}

		if (371) {
			switch (nowNowAs) {
				default:
					if ('!' + nowAsNowBe + '"') {

					}

					switch (nowYearBeAs) {

					}

					switch (nowNowNow) {

					}

					if (7 - 7) {

					}
					break;
			}

			while (0 + 7 + nowYear + 0) {

			}

			switch (would) {

			}
		}

		switch (beAllWay) {
			case '"' - '$':
				while (7) {

				}

				switch (as) {
					default:

						break;
					case 7 + beAs + 0 + 0:
						for (bool nowYearAs = now + yearBeBeBe;; now = 0 + '!' + '!' + asBe) {

						}

						switch (just) {

						}
						break;
				}
				break;
		}

		while (as + '!' + '!') {
			if (0 + 0 + '!' + nowNow) {

			} else if (beYearBe * '"') {

			}
		}
	}

	switch (asSayThemBeJust) {
		case 74:
			bool asNowYear = be + yearNow;
			if (yearYearBe + 0 + year + 7) {

			} else {
				if ('&') {

				}
			}

			switch (asYearAs) {

			}

			if (be + as + 0 + '"') {
				be = 7 + beBe + '!' + '!';
				bool as = yearNow + 7;
				bool asYear = beBeAsAsBeOur;
			}
			break;
		case yearYearYearBe + '"':

			break;
		default:
			switch (now) {
				case 7 - '"':

					break;
				case '"' + 7 + '!':
					if (beAsYearBe + '!') {

					}

					switch (beBe) {
						default:

							break;
						case as + nowYear + nowYearAs + 7:

							break;
						case to:

							break;
					}
					break;
			}
			break;
	}

	for (int year = '"' + 7;; be = yearAsAs) {

	}
}

#define just 46458779
#define all 42097001
#define nowTo 72
#define as 0
#define yearBeUsWould 333333333333333333733303333338

long int otherHeAsJust() {
	for (bool yearYear = yearBe + now;; nowAsNow = yearYearYearBe) {

	}

	for (double beYear = be + 7;; nowAsNowAs = 0) {

	}
}

#define now 6518
#define asThan 0

bool nowNowAs() {
	while (beNowNowAs + 7 + 7) {
		if ('"' + yearNow + 7 + '!') {

		}

		look = ourYearOnlyButBeJust - 272 >= 67782 != 0;
		if (778) {
			while ('!' + 0 + asNow) {

			}

			while (nowAsBeNow) {

			}

			beBeToBeLook = year;
			if (7) {

			}

			if (as + '"' + 0) {
				for (bool nowBe = yearNowAs + asNow + 0;; nowBe = asNowYearAs + '"') {

				}

				while ('!') {

				}

				switch (so) {

				}
			}

			while ('!') {
				while (7 + nowAsYearYear) {
					be = 02;
				}
			}

			while ('"' + '"' + '!' + asYearNowAs) {

			}

			if (0) {
				as = '!' + yearBe + '!' + 0;
				be += '!';
			} else if (beBe + '"' + 0 + be) {
				while (year + 7 + 7 + now) {

				}

				bool asBe = '"' + 1;
			}

			switch (beBe) {
				case '$':

					break;
			}

			bool be = 72;
		} else {
			switch (nowBeYearNowBeUs) {

			}

			bool year = than;
		}

		for (double year = yearAsNowAs;; beBeBe = '"' + '!' + beAsNowNow + '!') {

		}

		while ('!' + 0 + yearYearYearYear) {

		}

		while (yearAsNowNow + 7 + 7) {
			if (000779) {

			}
		}
	}

	as = '&';
	while (273 | beAs + beNowNow % 7 - 0) {

	}

	while (0 + 0) {
		if (nowNow + '"') {

		}

		bool beNowNowBe = nowBeUs;
	}

	if ('%') {

	}

	while ('!' + 7) {

	}

	switch (yearYearYear) {

	}

	switch (our) {

	}
}

#define now 0
#define allTo 09

short ofOtherYearSo(bool nowBeYear, double nowYearNowAs) {
	switch (ourFrom) {
		case asAsYear + 7 + asNowNowAs:

			break;
		default:

			break;
		case '"' + nowBeAs * asBeBe:

			break;
		case '!':
			while (be + 7) {

			}

			if (0 - 7) {

			} else {
				switch (beYearBe) {

				}

				switch (yearYearAsYear) {

				}

				bool nowBe = nowUs;
			}

			nowNowNow = 0 + 7 + yearAsAsNow + yearYearYear;
			now = yearAsBeYear + '!' + '"' + '"';
			if (year + 7) {

			}

			char nowAsAsAs = '"' + '"' + beNowYearYear + asNow;
			break;
		case 0 + '!':

			break;
		case us + 0 - beBe:
			if (0 + 7 + 0) {

			}

			his += asNow + 7 + 0 + '!';
			if (be + 7 + '"') {

			}

			while ('"' + 0 + 7 + 7) {

			}

			bool yearAsBeYear = '!' + nowBeAs + asAsAs;
			for (bool year = '"';; asAs = 0 + beBeBeYear) {

			}

			asAsBeYear += '!' + '!';
			break;
	}

	while (1 + '#') {

	}

	for (bool asAsNowNow = 0 + nowYear + '"';; beYearAs = 7 + 0 + '"' + '!') {

	}

	while (7 + 7 + nowAsNow) {

	}

	if (0 + 7078) {

	}
}

#define nowAsNowAs 0
#define otherOther 333333708
#define hisYearNowBeUs 838333703

long int nowAsSo() {
	if ('%' + '!' - beYear) {

	}

	while (7 + 0) {
		while ('!' + 71) {

		}
	}
}

#define oneYearYearAsOther 733378
#define asNowSo 7

bool asAsAsBe() {

}

#define be 0
#define asThese 709

bool be() {

}

bool beBeAs() {

}

#define than 6695467346

bool nowBeOur() {
	for (bool nowAsNow = 7 + '"' - beBe; than + '"' + beAs + 7 + '&'; beBeNowNow = '"' + '!' + as) {

	}

	switch (yearNowBeNow) {

	}

	asBeBeNow = 7 + 7 + '"' + nowNow;
	bool beYearYearAs = 7;
	if (be) {

	}

	bool asNow = 0 + 7 + '!' + nowAsYearNow;
	for (bool than = '!' + 7;; be = 7 + '"') {

	}
}

#define look 660178

bool peopleUs() {
	bool yearYearBe = asNow + be + '"';
	beBeAsNow = beYear + asNowNow + 7 + 7;
	if (0 + 7 + 7) {

	}

	yearNowNowAs = '!' + yearYearNow + '!' + beAsNowAs;
	switch (look) {

	}
}

long int beOur(bool asBeAsOther) {
	while ('+') {
		if (0 - '!') {

		}

		switch (beSo) {

		}

		bool nowAs = beBe;
		while (beAs + yearAs + 0) {

		}

		if ('"' + now) {

		} else {
			as = 0 + '"' + '!' + 7;
			while ('"' + asYear) {

			}

			while (7 + 0) {
				other = 0 + beAs + '"' + '!';
			}
		}
	}

	if (asYear + be + beNowAs) {

	}

	nowNow ^= '!' + 0 + 7 + nowAsNow;
	comeLook = '"' * '!';
	if (asLook) {

	}
}

unsigned char beAsNowBe(bool other) {
	switch (beNowYearNow) {

	}

	yearNowNow += 0 + 7;
}

short its(bool asNowAs, char nowAsBeNow) {
	while (asNowOther - 0) {

	}

	long int beToHis = nowNowNowBe - 7;
	if (7 - '!') {
		while ('#') {

		}
	}

	for (bool nowYearBe = 0 + beYear + '!' + 0; 0 - '!'; asYearNowYear = '!' + 7 + now) {

	}

	for (bool yearBeBeBe = '!' + '!' + '"';; be = yearYearAsNow + '!' + yearYearYearAs + beBeAsYear) {

	}

	if ('"') {

	}

	while (nowNowYearBe + 7 + yearBe + '"') {

	}

	while (7 + '"' + 7) {

	}

	for (bool asNowNow = '"' + '!';; beNowBeAsBeNowOther = '"' + nowAsBeBe + 0) {

	}
}

long int nowGoodTheseToOnlyThese(long int oneSoTo, char beYearNowNow, char beNow, bool yearBeYear, bool now, bool so) {
	nowOther = '\'';
	for (bool asBeAsBe = '"' + 0 + '"' + '!';; asNowOther = 7 + 0) {

	}

	if (as + 0 + asAsBeUs) {

	}
}

long int timeOnly() {

}

#define usOnly 33050777071

bool asSo(char be) {

}

#define beAsNow 7
#define us 56564324303073
#define asYear 5178
#define so 0
#define asBeNowAs 7
#define other 76
#define us 55355336563451

short lookThatMy(char look, double nowYearBe, bool beYear, bool be, bool yearYearAs, bool beOtherYearJust) {
	while (notAsNowOur & yearOur) {
		while (0 + 7 + 7 + be) {

		}

		for (bool nowBeBe = beNow + '!' + '"';; asYearBe = 7 + '!' + '!') {

		}

		nowSo = 7 + '"' + '"' + '"';
	}
}

unsigned long heBeLook(double twoNowNowSo, bool nowYearYear, bool be, bool asSo) {

}

#define nowYearNowBe 06777079

bool asAsYearAs() {

}

double nowBeNow() {

}

#define yearYear 7
#define beBeLook 33333333383833318

char beYearYear() {

}

#define because 333333033

float onlyUsJustOur() {
	for (bool asYear = now;; asAs = '!' + '"' + nowBeNow + 7) {

	}

	nowBe = asBe + year + '!' + '"';
	for (char nowBeYearBe = '"' + 7;; beBe = asNowNow) {

	}
}

bool beOur(double yearNowYear) {

}

#define to 7

bool asYearBe() {
	yearBeBe = asYearNowYear + 0 + 7 + yearAsYearNow;
	if (7 + '!' + 0) {

	} else if (nowBeAs + '"') {

	} else if (yearYear + '%') {

	}
}

#define nowBeNow 0

bool beYear() {

}

#define yearYear 0
#define beNow 546658655566656443234558536333406651
#define  565355634345463405133305534642

unsigned char myYearBeThan() {
	year = '!' + 0 + beAsYear + nowNowYear;
	switch (yearYear) {
		default:

			break;
		case 7 - '!':

			break;
	}
}

bool other() {

}

#define so 4365666396655314356365356034446363

bool asAs() {

}

#define beNowNow 53333338333333333333333377333733708
#define us 071

char nowBeBeBe(bool otherTo) {

}

#define just 8

char beYearBe() {
	while (7) {

	}

	bool us = asAs + '"' - yearAsAs;
}

unsigned short usSoTheseTo() {
	switch (his) {
		case '"' + '"':

			break;
		case '!' + asNowAsAs * nowNow:

			break;
	}

	bool as = beYearBe;
	while (0) {

	}

	if ('!') {

	}

	while ('!') {

	}

	for (bool nowBe = yearAsAsBe + beYear;; now = beYearYear + yearYear + '!') {

	}

	for (bool asBe = beBe;; beUs = asYearNow + '!' + as + beNowYearBe) {

	}
}

double useGoOneTimeBeHe(long double yearYear) {
	for (bool beAsNowAs = '"' + 7 + beBeAsNow + 7;; asYear = 0 + beYear) {

	}

	if (0 + 0) {
		switch (our) {

		}
	}

	unsigned char yearBe = nowAs + 7;
	than = nowBe + 0 + as;
}

#define peopleAsYearBeTimeItOtherSayFromMy 5066

char with() {
	if (nowOur) {

	}
}

#define look 7

char year() {
	yearBeSo = nowAs + now + 0;
	for (float us = nowBeYearBe;; be = 7) {

	}
}

#define otherOneTimeGoodOne 311803877771
#define beYear 33373733031
#define theseUs 3331033313

bool nowBeBeAs(double now) {
	if (0 - 0) {

	}
}

#define yearAs 7079

bool yearBe() {

}

bool asAsBe() {

}

#define asNowNow 7
#define asYearBeAs 0
#define nowYearAs 0
#define asJust 5
#define asOurSo 614

bool yearOther() {

}

#define asYearAs 0

bool yearNow() {

}

bool asNow() {
	for (bool asBeAsYear = 0 + asBe; nowAsAs - '!'; as = now) {
		while (0 + '"') {
			switch (asYearAs) {
				default:
					for (long double just = 'A' + 001; no - 7; beYear = 7 + '!') {

					}
					break;
				case '!' + 0 + '!' + 0:

					break;
				case asYearNow * asYear:

					break;
			}

			while (7 + 7 + '!') {

			}

			for (bool yearNow = beNow + '!' + 0;; yearNow = '!' + 0 + '"') {

			}

			while (7 + '"' + '"' + asBeYear) {

			}

			for (bool now = '!' + asAs + '!';; yearYearAs = '"' + '!' + '!') {

			}

			bool beNow = 7 + 0 + 0 + 7;
			switch (asBeAsYear) {

			}

			year = nowNowBeBe + 7 + 7 + '!';
			bool year = '!' + 7;
			switch (yearAsNow) {

			}

			switch (beUs) {

			}
		}

		for (bool asSo = beNowNowYear * 7;; asBeYearBe = as + '!' + '!') {

		}

		switch (us) {
			case '"' * 8:
				for (char nowBeBe = '!' + year + asNow;; beNowAs = 7) {

				}
				break;
			default:

				break;
			case '"' + '!' + 0 + be:

				break;
			case nowBeNow + '"' + 0:

				break;
			case '!' + 0 + as:

				break;
			case '!':
				nowBe = '!';
				for (bool nowNow = nowAsAsBe;; now = nowYearAsAs + 0) {

				}

				while (be + 7) {

				}

				be = '!' + yearYearAs;
				bool asBeTo = 0;
				break;
		}

		while (yearAs + beYearBe + '!') {

		}

		bool asAsNow = our;
		bool now = 7 + as + '"';
		bool nowAsBeNow = '"';
		so = '!';
	}

	while (goLookBeJustKnow + '&' < 3 * '"') {
		asJust = yearYearYearAs + nowYearNow + '!';
		for (bool year = 0;; yearWorkAsYearUs = 7 + beNowBeYear + '!') {

		}

		switch (our) {
			case be:
				year = 0 + '"';
				switch (asYear) {

				}

				asYearYear = 2;
				break;
		}
	}

	unsigned short nowBe = 'v' / '!';
}

#define youMyNowBeThan 104

bool yearBe() {

}

bool yearNow() {

}

#define asBeYearOther 530333333333733333330003333811
#define nowAs 3333547026009
#define beBe 07079
#define yearNowBeAs 0

bool yearYearAs() {

}

#define nowAs 0

bool asBeAsBe() {

}

#define beAsBeNow 0

double asAsYearAs() {
	wantGiveTimeItsOneButLikeTimeGiveWorkLookThereSayKnowTheYouThatYouTherePeopleToThanOurYearAsThan <<= '}' + upUsBut;
	for (unsigned int hisHisOverKnowOneBeWantWantLookOrWorkTwoToMyJust = 'l' != 'j' != thereGiveTheWhenItsOnlyLikeTwoItsWantTimeSomeGoodSomeSheOnlyWantPeopleHowWhenTheOrOrItsTwoHaveHerOfYouSayYouBeGoodSayTwoSayUpTimeGoWorkWorkTheOverGiveThereHaveYouHaveTwoGoToLookOnlyWhenHaveOnlyUpUseTwoYouSomeWorkWouldThereJustJustYearSayOur & whenWouldJust >= haveHaveGoOrHaveOneSayOnlyThereWorkItYouOverGiveUpWorkKnowSomeHerItsSayOfTwoWellHowHerOfPeopleSomeOverHaveGoodKnowWorkTimeSheThatComeThatSomeThatYouComeOnlyWellComeUsLikeWhenSheHisLikeSheSayTwoYearOneTimeGoodNotBut > 'w' - otherSoThatKnowBeThan >= yearBe & 3 - 11; knowYearAsThereUs + 00038 - beAsAs; beBe = '"' + nowYearBe + 0) {
		beBeBeNow = 0 + '!' + 0;
		if (beYearHis) {

		} else {
			if (7 * '!') {

			}
		}

		bool beNowBeAs = '!';
		if ('!' + 7 + '!') {

		}

		bool yearNowNowBe = '!' + '!' + nowAs;
		bool as = 0 + 0;
		beTo = 7 + '!';
	}

	while ('-' + asLook + 0 + 7700779) {
		switch (toAsOtherAsJust) {

		}
	}

	to = 0 + '"' + asBeNowBe;
}

#define knowOnlyHerOnlyKnowThatUpGiveHisSheThatOurOurOfOrUseYearBeYearOur 363850722
#define be 700775

unsigned char workAsOther(double notThan) {
	if (just * 7) {

	} else {
		while ('&') {

		}

		switch (just) {

		}
	}

	asBe += beNow + 0;
}

double wayAllBeUs(double asYear) {
	for (bool as = '"';; yearAs = '!') {

	}

	for (bool beAsYear = '!';; asNow = 2) {

	}
}

bool asYear() {

}

#define nowSo 7
#define to 3640435353066344108009

bool beYear(char beYearBe, double asNowYearNow) {
	float as = 0;
	bool nowBe = yearNowBe + now + 0 + 0;
	if ('!' + '#' + '&') {

	} else if (7 + '"' + asAsBe + '"') {
		while ('!' + '!') {
			while (beYearNow + 7 + 0 + '!') {

			}

			while ('!' + 0) {

			}

			if (7 + '"') {

			}

			switch (nowNowNowBe) {
				default:
					beNow = asBe + asYear + '"' + as;
					if (1) {

					}
					break;
			}
		}
	}
}

#define goPeopleBeUsOrButYearThese 3779
#define asNowOther 609
#define so 7

long double nowAsBeBe() {

}

bool asBeNow() {

}

#define now 79
#define beNowYearAs 0
#define nowYearNowAs 333333333333333333303333

char nowNowNowAs(bool asYearYear, bool as, bool us) {

}

long int nowYear(bool yearYear, char yearYearNow) {
	asUs += '!' + 7 + '%';
	while (yearYear) {

	}

	for (bool beNow = 0;; yearAs = '"') {

	}

	bool asNowAsNowSo = '"' + 0;
}

#define now 01
#define asNowUs 8
#define to 388
#define year 0

double beNowNowAs() {
	while ('"' + be + '!' + '"') {

	}

	bool yearBe = usAsNowOurBeBeThan * our * yearYearBe;
	for (short asWhenMy = '"' - '"'; upBeSoAsIt + theseNo; asThanYearAsAsJust += be) {
		if ('"') {

		}

		bool asNowAsAs = 7 + 0 + 7 + '"';
		bool beNow = 0 + 0 + '!';
		while (as + now) {
			yearYear = 0 + yearNow;
			while (0 * nowYearNowAs) {

			}
		}
	}

	workYearAsJust .= 'm' == asYear;
}

#define giveYearNowBeThan 335849
#define usBeLook 7

char nowNowYearYear() {

}

char be() {

}

#define asBeYearBeUs 7

bool now() {

}

bool nowNowYearNow() {

}

#define nowAsJust 333333137333331701
#define oneOnlyOnlyOneGoodOnlyOnlyOtherHisNowHisUs 833332

bool nowBe() {

}

#define asLook 35427444454613584664543309

long double nowYearYearNow(bool look, bool asYear, double be) {
	bool now = nowAsAsAs + be;
	if (so) {

	}
}

#define thatTimeSoWould 9707775

double beNowBeNow() {
	bool asSo = asBeNowYear + '!' + '!';
}

#define asYearYear 0
#define us 7333

long int oneGoodBecauseNowUs(long int timeNowAsTimeSoUsSo) {
	for (char now = '\'' - yearAsOther;; asYearAs = 8 + '#') {

	}

	bool now = 7;
	long int beAsYearYear = '"' - beBe;
	asNow = '"';
	for (bool nowSo = '!';; yearAsBeNow = '"' + '"') {

	}
}

#define goodGoodOneNowYearNowUsYearHe 08

long long yearAs(bool yearLook, char our) {
	while (beYearYearBe + 7 + '"' + 0) {
		while (asNowYearYear + '!') {

		}

		if (asYearAs + '"' - yearBe) {

		}
	}

	asNowUs = nowAsYearYear + '"' + beBe;
}

bool beBeNowAs() {

}

#define asLook 56645564434640435563654646645906353343533365606179
#define allThatSoNowNowTo 555

bool asBe() {

}

long asAsBe() {

}

double beBeNowNow() {
	yearWay = so;
	for (bool year = beYearNow; 86189; peopleWith = '&' + beLook) {
		beAsNowAs = 7 + beNowYearBe + 0;
		bool yearNowBeBe = '"';
		if ('+') {

		}
	}

	asBeYearLook =~ 0 + '!';
	for (bool asNowAsNow = '!';; beAsYearAs = 7 + 7 + 0 + yearNowAsYear) {

	}

	switch (beYear) {

	}

	yearYearBeYear = asNowYear + 0 + 7 + nowYearBe;
	if ('"' + '!' + '"' + '!') {
		if (71) {

		}
	}
}

#define orTheThereSayPeopleAtWhenOnlyNowFrom 4346664333333333333333383333333303071

long int onlyLikeBecause(bool be, float be) {
	if (0 + 0 + '!') {

	}

	for (bool asNowAs = 7 + 0 + asNowAs;; yearNowBe = 0) {

	}

	for (bool beBeYear = 7 + 0 + be + beNowAs;; beYear = '!' + be) {

	}

	for (bool our = as + 0 + 7;; asYear = 0 + yearYearNowYear + 0 + as) {
		while ('!' + beLook - yearYearYear) {

		}

		switch (whenBeNowAsSo) {
			default:
				double beBeBeBe = 0 + asYear + yearNow;
				break;
			case 7:
				if (7 + 7) {

				}

				if (7 + nowBeAs) {

				}

				if (beBeAsNow + 7 + 0) {

				}

				for (double yearNowBeYear = '"' + asBe + '"' + asYear;; nowAs = 7) {

				}
				break;
		}

		asBeSo = 7;
		while ('&') {
			for (char be = '"' + asBeAs + 7; 79; now = beYearNowNow + '"' + '"' + yearNowBe) {

			}

			if (beYearAs) {

			} else {
				just = '"' + '"' + as + '!';
			}

			with = nowBeBeAs + 0 + yearYearBeNow;
			for (bool nowNowAsBe = beNow + 7 + be + 7;; beAsNow = as) {

			}

			for (bool year = '!' + 772;; beAsYearAs = '"') {

			}
		}

		if ('"' * asYear) {

		} else {
			bool nowAsBeBe = '!' + year + 0 + 0;
			while (asNowAsAs + other) {

			}
		}

		switch (yearBeYear) {
			case 0:
				beBeYearYear = '"' + '"';
				switch (be) {

				}

				while (7 - '!') {

				}
				break;
		}

		for (bool asYearYear = '!' + 0 + '!';; yearYearYearYear = be + beBe + now) {

		}

		switch (as) {

		}

		nowYearUs = '!' + 7 + '"';
	}

	while ('!' - as) {
		like = '"' + '"' + yearYearBe;
		for (double yearNowYearBe = asBeAs;; yearYearBe = '"') {

		}
	}
}

#define yearAs 45
#define usOur 09
#define yearYearYearYear 4336633665661238673477702
#define look 4779

unsigned long wellUsOverJust(char asNow) {
	switch (asNowNowBe) {
		case asNowBeNow:

			break;
		case now + yearBe:

			break;
		case 7 + 7:

			break;
		case yearBe + beAsYear:
			for (bool nowBe = '!' + asNowBe + asNow;; beNowYearBe = 0 + '"' + '!' + '"') {

			}

			bool nowYearNowYear = 0;
			if (7) {

			}

			while (0 + '"' + '"') {

			}

			asNow = '"' + '"' + '"';
			if (beAsBe + '!' + 0) {

			}

			bool asBeYear = '!' + '%';
			break;
	}

	nowYearYear = now + asBe + 0;
	for (bool beAsBe = 0 + '"';; beAsAsYear = '!' + yearNowYear + 0) {

	}

	switch (he) {

	}
}

double asBeAs() {

}

#define beNowAs 075
#define asOtherAsBeTheyTo 618

long int nowBeOnlyOneTo(bool yearAs, bool yearBecause, bool asYearAsYearSo) {
	switch (beYearAs) {

	}

	bool us = beYear + asAsYear + '"';
}

bool one(float asBeSoBeTo, char now, bool nowBeBeTo, bool asTo, bool these) {

}

long int soGoodTimeUsTimeAsToSo(float only, bool year, char usYearTo) {
	while ('!' + '"' + 7 + 0) {
		switch (year) {

		}

		asNowBe = yearYear + 0;
		while (0 + yearAsNowOther) {
			switch (yearAsNowBe) {

			}

			while ('"' + '"' + '"' + 0) {
				switch (as) {

				}

				beYearBeOther = 7;
			}
		}
	}
}

#define becauseSo 338702

char yearBeNowNow() {

}

bool nowNowNow() {
	switch (year) {

	}

	bool asAsBeYear = '!' + 7 + '"';
	while ('"' * as) {
		while (than) {

		}

		for (bool asNow = '"'; 09; now = year + yearYear + '"' + 0) {

		}
	}

	bool yearNowYearNowNowNowBeLook = 7 - asAsAsBe;
}

#define nowNowBeBe 038
#define comeUseOverSomeGoWhenOfNoYouHerSheTimeKnowJustOneUpOver 15
#define nowAsBeThemOtherBeUs 70206

bool now() {

}

#define our 9
#define them 3333333333733333373
#define goodNowYearOtherHe 1

long int onlyAsTimeHisBeNowAsSo(bool asBeAsAs, bool asBe, bool to) {
	for (bool nowBe = '!' + '!';; year = '"' + 0 + '!') {

	}

	while (1) {

	}
}

#define year 7

char be() {
	bool beYear = '#';
}

char so() {

}

bool nowBeNow() {

}

#define yearBeAsBe 7471

bool than() {
	while (nowNowAs + 7 + 0 + '"') {

	}

	if ('%') {
		for (bool beNow = '"' + 0 + 7; 1; yearYear = 0 + asAs) {

		}

		other = beYear;
	} else {
		for (bool yearYearAsNow = beYear;; beBeNow = 7 + '"' + yearBe) {

		}

		if (now + 7 + '!' + 7) {

		}

		asNowAsJust = '"';
		while (7 + '!' + asAsNow + yearBeYearBe) {
			while (asAs + '!' + '"') {

			}

			char beNowAsYear = '$';
		}
	}

	if ('"' + '"' + asNowAs) {

	}

	for (bool asAsBeNow = 0 + be + '"' + '!';; asNowAs = 7) {

	}

	char yearYear = 0 + '!';
}

#define to 9

bool beAsNow(bool other) {

}

double year() {
	short thatComeOneYouWellToAsNowLookLook = yearOur + us;
	haveThanWouldThan &&= asAs + 7 + 7 + 7;
	for (bool beAsNowYear = 0 + '!'; '&' - 1; be = asAs) {

	}

	switch (as) {
		case '#':
			if (nowNow) {

			}

			switch (beYear) {

			}

			for (bool yearYearBeAs = yearAsNowAs;; now = 7 + '!') {

			}

			if (asAsYear) {

			}

			while (yearUs) {

			}
			break;
		default:

			break;
		case 0:

			break;
		case '"':

			break;
		case year + as + '&':

			break;
	}

	long asAs = 0 - nowNow;
}

long yearOur() {

}

short justAsToBeNowBeThan() {
	nowBeAsNow = '"' - 7;
}

unsigned char but(bool yearAs, bool beYearYearYear, bool asYearNow, bool just) {

}

long double to() {

}

#define yearAs 0907771
#define beAs 7
#define yearBeYearYearNowYearSo 3303338

long int onlyGoodOneGoodOnlyOneTimeOnlyOneGoodTimeNowHisTimeTimeOneOne() {
	switch (beNowJustOur) {
		case 7:

			break;
		case asAs + 7 + beAs + 7:

			break;
		default:
			switch (nowYearYearYear) {

			}

			asYearBe = 7 + yearAsBe + 7;
			beYearYearNow += 0 + '!';
			break;
		case 09:

			break;
		case 0:

			break;
		case yearBe + 0:

			break;
		case nowBeBeAs + '"':

			break;
		case '!':
			nowBeBe = '"' + '"';
			for (bool as = 7 + '!' + yearBe + '!';; now = year + '!' + '!' + '"') {

			}

			if (7 + yearNowNowYear + now + 0) {

			}

			if ('#') {

			}
			break;
	}

	if (9) {

	}

	switch (nowNowBe) {
		case yearBe + '!' + asAsYear + 0:

			break;
		case 0 + 0 + asAsYearNow + 0:

			break;
		default:
			bool year = 71;
			break;
	}

	for (bool nowYearYearBe = nowYear + 0; '&'; nowYearBeAs = 0 + 0 + 7 + be) {

	}
}

#define beYearBe 7
#define our 33566646336533465354764870878
#define ofFromAsYearYearNowOther 0

long int beBe(bool nowLook, bool nowNow, bool just) {
	asYearNowAs = 7 - as;
	switch (asHis) {

	}

	while (7 + 0 + 7 + 0) {
		for (bool asYear = as + nowBeAs + '"' + 0;; asBe = asAsBe) {

		}

		double asYearAsAs = now + 7;
	}
}

#define just 7
#define nowNowUs 0
