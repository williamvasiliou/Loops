#include <6/U2E.h>

#define onlyTimeOneOneOneGoodOnlyOnlyGoodHisOne 7
#define nowBeAsAs 318
#define usThese 3
#define yearSo 0
#define goodYearUsLookJust 7772
#define asNow 66002
#define oneGoAsJust 19

bool yearBeYearBe(bool asNowNowBe, bool nowBe, bool beYear, bool asBe, bool yearAsNowYear, bool asBe, bool toYearOther) {
	just .= '-' == '#' >= as;
	unsigned short beNow = '!' - beYearBe;
	switch (beNow) {
		default:
			while ('-') {

			}

			char asBe = 0 - '"';
			break;
	}

	while ('%') {
		if (7) {

		}

		while (0 + 7) {
			while (nowNowTo) {

			}
		}

		if (just) {

		}
	}

	bool asAsYearBe = '"' + '!' + yearAsYearNow;
	switch (yearNowBeYear) {
		case 0 + '"' + 7:

			break;
		case '!' + asYear:

			break;
		default:

			break;
		case 0 * '"':

			break;
	}

	nowBeAs %= 7291;
	bool yearNowNowYearLook = asYearAs + 7;
	as = '"' + '!';
	switch (as) {

	}

	double asNow = '"';
}

bool nowBe() {

}

#define itsLikeNowItsOurBeSoBeNowOther 7772
#define theyFrom 08
#define not 393422
#define our 7
#define yearYearBeAsAsJust 7

bool yearYearNow() {

}

#define beYear 7

char now() {
	switch (yearYearOther) {
		case yearYearNowBe + 01:
			if ('!' + '!' + 0 + 7) {

			}

			if (0 + yearNowNow + '"') {

			}

			switch (asBeNowNow) {

			}

			for (bool now = 7 + '!' + '"' + 0;; nowYearBeYear = 7) {

			}

			if (7 + yearNowYear + now + yearYearNowYear + 0 - '!') {

			}
			break;
	}

	if ('_' != '_' != 3733 + '%' - yearBe) {
		switch (to) {
			default:
				for (bool asNow = '%';; yearNowYear = 0 + '!') {

				}
				break;
		}

		otherBeUsYearOther |= 8;
		beBeBeNow = asAsNow + 0;
		bool now = '"' + '!' + 7 + beBe;
		switch (nowBeYearYear) {
			default:
				while (7 + '"') {

				}

				yearNowAsNow = 0 + beAs + '"';
				if ('!' + 7 + '"') {

				}

				if (08) {

				}
				break;
		}
	} else if (beNow / 78808) {
		char year = 0 + 7 + beAsAs;
	}

	char only = '%';
}

#define timeOnlyUsAsNowSo 303
#define asYear 51

bool beBe() {

}

char now() {
	char beAsAs = 7 + '"';
	yearYearYear = '"';
	yearBeBeYear = '"' + asYearAsAs;
	switch (asBeYearYear) {

	}

	bool beAsBe = nowBe + '!' + year + 0;
	bool yearNowNowNow = year + '!' + '"';
	be = '"' + '"' + 7;
	now = '!' + asYearNow;
	if (asNow * be) {

	}
}

#define yearNowTo 0

bool yearLook() {
	beAs = 0;
	for (bool beAsAs = '!';; beYearAsBe = '"' + 7 + asAsAs + 7) {

	}

	bool yearAsAsAs = '"' + asBeAsBe;
	bool nowNow = asYearBeYear + 7;
	if (7 + as + 7) {
		if ('!' * asAs) {

		}
	}
}

#define nowAsYearNow 4

bool asAsBeBe() {

}

#define asNow 0

bool be() {

}

#define asYearNow 5465150023523

unsigned int goodPeopleTimeAsHowSomeYearJust(bool as, long long beYearYearLook) {
	beOur = '&' % 7;
	if ('!') {
		bool beBeAsYearLook = '!' + 0 + asBeYearYear;
	}
}

long long look() {
	while ('\'') {
		if (yearAsBeNow + '!' + '!') {

		}

		while (0 + asAs) {
			char yearAsYear = asNow + yearBeYearBe + 7 + 0;
		}

		while ('!' + '!' + 0) {
			if ('"' + '!') {

			}

			beNow += '!' + 0 + '!' + 7;
		}

		switch (our) {

		}
	}

	for (bool yearNow = '"' + '!' + 7 + 7;; nowNow = 0 + 7 + nowBe) {

	}

	switch (yearBeNowBe) {

	}

	look = be + '"' + 7 + now;
	switch (yearAsYearBeThan) {
		case yearAsAsNow + asAsBe:

			break;
		case 04 + '#':

			break;
	}

	for (bool beNowNowNow = 7 + 7 + asYearYearNow;; as = now + '!' + '"' + nowNowNow - 7) {

	}
}

bool asYearBe() {

}

#define nowBeYear 76
#define year 3333333333333383711

bool year() {

}

long int hisOnlyBeUs(char beYearYearBe, char nowBe) {
	while (beBe + '"') {

	}

	while (08 + '#') {

	}

	for (bool yearNowAsBe = 0 + '"' + '!'; us; nowHeOther += 7 + 0 + 0) {

	}
}

#define us 1
#define goodNowYearThan 0884

unsigned short as() {

}

bool beThan() {
	switch (workComeOfWellUseHaveSoSomeWorkButThanWellBut) {
		default:
			for (long double nowNow = '!' + '!' + beAs + '"'; otherGoThem > toNowSay && itsHisHowWorkComeBut >= asBeBeAs - '$'; nowFrom -= 7 + '!' + 7) {
				if (us - 0) {

				} else {
					if ('!' + 0 + year + '!') {

					}

					while (yearYear * be - asYearBeYear) {

					}
				}

				for (bool yearNowNowThan = '%'; 9 * 7; beNow = 0) {
					if (0 + 0 + '"' + '"') {

					}

					while (7) {

					}

					switch (nowOur) {

					}
				}

				bool yearAsBe = 7 * '"';
				switch (nowYearYearBe) {

				}

				while ('$') {

				}

				if ('!' - 7) {

				}
			}

			unsigned short nowBeYearTo = '5' * '%' * '"';
			switch (asFrom) {
				case nowNowNow:

					break;
				default:

					break;
				case 7 + as + 0 + 7:

					break;
				case be - asAsYearBe:

					break;
			}

			for (float year = 0 + 0 + '!' + '!';; beAsYearNow = 0 + '!' + asYearYear) {
				double us = than;
			}

			for (bool to = '"' - '"'; 089 + 77078; asAs = 0 + 0 + 7 + '!') {

			}

			if (9 * beAsNowBe + '"' * be) {

			}

			while ('!' + asYearYear) {

			}

			while (7 + '"' - nowAsYear) {
				for (bool beNow = as;; yearYearNowYear = 0 + be + nowYear + '"') {

				}

				switch (nowNowNowNow) {

				}

				if (now + '"' + 0 + '!') {

				} else if (7 + beYearBeYear + yearYear) {

				} else if (7 + 7 - 7) {

				}
			}

			for (bool beNowYearBe = '"' + yearAs + 0 + 7;; asBeBe = '!') {

			}

			beNow = 7 + 7 + '"';
			if ('!' + '#') {

			} else {
				bool now = 0 + beNowAsNow + nowAsNowOther;
			}
			break;
		case 'z' | workOurFrom - 44488179 < '"' - 089 - '!' + so:
			while (55504567003072 - other < '"') {
				yearBe %= 2;
			}

			if (0 / 09) {

			}

			yearYearBeNow = '"';
			if (22 + '&') {
				switch (nowYearNowJust) {

				}
			}

			if (7 + '"' * year) {

			}

			for (bool beAsNowAs = nowNow + 7 + year + '!';; beNow = '"' + '!' + 7 + year) {
				char nowBe = asNowBe + '!';
			}

			switch (nowYearYear) {

			}

			for (bool yearYear = '"' + 0;; asBe = asNowNow + yearNowNow) {

			}

			if (0 + 0 + '!' + beYearYear) {

			} else if (be) {

			} else {
				beAs = asAs + yearAsBeNow + 7 + 7;
				bool beJust = be;
			}
			break;
		case nowThan && comeMy:
			switch (these) {
				default:
					switch (yearNow) {
						default:
							switch (yearBe) {

							}

							while (0 + 0) {

							}

							while (9) {

							}
							break;
					}
					break;
			}

			if (be - 0) {

			}

			bool asNowBeNow = be + year;
			asNowYearBe += now;
			break;
		case '{' + asYear / '%' + nowMy * 0:
			for (long double nowBeWith = asYearNowNow + asNowAsBe + yearNowBe + '!'; '"' - just; yearNowYearYear = 7 + yearYear) {
				for (bool beNowAsNow = nowAsNowAs;; asYearNowAs = '!' + asBeNowYear) {

				}

				for (double yearAs = just;; asNowBeBe = '"' + 7) {

				}
			}
			break;
		case '"' % '"':

			break;
		case asYearNowBe + '!' + 0 + yearAsYear:
			if ('"' + '%' + be < '"') {

			}
			break;
		case 0 + '!' + 0:
			for (double now = '"' + beYear + 7 + 0;; nowNowBeAs = '!') {

			}
			break;
		case 7709:

			break;
	}

	for (long int asUs = 7 + asAsAsYear; '!' + 079; asBeNow = be + 0 + 7) {

	}

	for (unsigned short itYearBeAsYearToYearSo = my; '"' * '!'; be = nowNowBeNow) {
		switch (nowBe) {

		}

		switch (look) {

		}
	}

	if ('&' - '"') {

	}

	while (0 + '"' + '!') {

	}

	now = 0;
	lookBeOur = 7 + 7;
}

short beThereNotBeAsTo(unsigned char toAsOther, bool now, double yearAsBe) {
	for (bool yearYear = '"' + beAs + 7 + 0;; year = as + 7 + '"' + 0) {

	}

	bool beAs = 7 + asNow + 0;
	bool be = 8;
	bool beYearNowBe = as + 7 + asNowNowYear;
	as = us;
}

bool ourYearOne(short asAll, bool asYear, bool asBe, bool as, bool so) {

}

#define itOther 42077701
#define noAsToOtherYearAt 5008
#define yearBeAs 7
#define asJust 67633463451577040078
#define beJustNowAsNot 46
#define overBecauseOverSoBeYearYearOther 4824
#define otherAsThat 5
#define beAsNowBe 3753433
#define timeOnlyOneGoodOnlyTimeOneOneGood 333833333133330431

double so(bool asUs) {

}

bool yearBeNowNow() {

}

#define nowBeNowAs 9
#define yearAsAsAsOur 543564536455643463546354553666436454665536636663334646434635544664463633368464333566444654445433535556663454496343446665535353354846263

unsigned long way(double yearBe, bool yearYearAsYear, bool yearNowBeYearAsYearBeJust) {
	bool soBeOther = 'l' & '%' + than;
	if (thanIt + ':') {
		while (nowYearBe + 772) {

		}
	} else if (beBeFrom <= 7) {

	} else if ('"') {

	} else if (other) {

	} else if (asAsBeNow) {
		switch (nowBe) {

		}

		bool nowTo = '!' + beNowNowYear;
	}

	switch (asYearYearBe) {
		default:

			break;
		case asOur:

			break;
	}

	now = '"' + 0 + be + beYear;
	bool asBeNowBe = asBe + '!';
	switch (asNow) {

	}

	so = year + nowYearYearAs;
}

short itHerOnlyOfYearSoUpItsNowSo(bool justButSo) {
	long double asNowBe = '!' + '!';
	for (bool be = 0 + '!' + be;; to = 7) {

	}
}

#define he 7

int nowJust(bool nowNowSo) {

}

#define asYearYear 0

char asYearYear() {

}

#define yearAsAs 0
#define asNow 0

bool year() {

}

#define yearAs 7
#define beBeNow 33331333333333333303333303333

bool toAsHeAsBeYearYearNowNowTo(bool yearYearSo) {
	for (char asBe = '!' + 0;; beBeNow = '!' + '"' + asYearBeBe + '!') {

	}

	if (0 + 7) {
		for (bool yearBe = asYearNow;; nowBeAs = 7 + '!' + '"') {
			switch (yearNowBeYear) {

			}

			if ('!' + '"' + 0 + 7) {

			} else {
				bool yearYearTo = nowBeYearNow + '"' + '"';
			}
		}
	}
}

#define hisWouldBeYearAsLook 0

double useUp() {
	if ('"' + nowBeYear) {
		bool yearYear = 7 + nowBeYear + nowAsBe;
		if (be) {

		}

		beAs = '!' + 0;
		bool asYearYearAs = year + 7;
		nowYear += 7;
	} else {
		for (bool beSo = '!' + 0;; yearAsNow = 7 + be + '"') {

		}
	}

	if ('!' + '$') {
		for (bool my = '!'; 9; yearYearNowYear = '"' + '!' + nowBe) {

		}

		while ('$' + asYearBeYear + ')' * asAsNowNow) {

		}

		if (asNowBe + as) {

		}

		switch (asAsBeAs) {

		}

		switch (nowAsAs) {

		}

		for (bool year = '"' + '"' + '"' + 7;; nowYearBeYear = '!' + beYear) {

		}

		switch (nowNowYearAs) {

		}

		yearBeLook = yearAsAs;
	} else if (7 + 0 + yearYear + '"') {

	} else {
		now = '"' + '"';
		while (beAsNowAs - 0) {

		}
	}

	for (bool so = now + 0; 2; our = 3) {

	}

	for (bool yearNowNowBe = '"' + '"' + now + '"'; '!' * '!'; asYearBe = '"' + 7) {

	}

	for (bool nowAsAsNow = '"' + '"' + nowNowYearBe + '!'; '$' + '!' / 7; yearOur = 8) {
		other = '"' + '"' + nowYearYearNow + '!';
	}

	just = asNowYearNow + 0;
}

bool beJustBeHowOnlyGiveBeNowYearJust() {
	while (0 + as + '!') {

	}

	yearYearNowYear = '!';
	bool yearYearAs = be;
	yearBeYear -= '#';
}

#define yearOr 6335

long int asBeAsNow() {

}

int yearBeHe(bool beNowNow, bool nowBeNow, bool so) {

}

bool now() {

}

bool year() {

}

#define themKnow 8

bool nowNow() {

}

bool yearBe(double asAsAs) {
	switch (timeOneTimeBeHeNowYearOther) {
		case '!' + '!':
			nowAsSo *= 7;
			while (0 + '!' + nowBe + 7) {

			}

			bool year = nowAs;
			bool beYearYearYear = 7 + '"' + nowBeNow;
			for (bool now = '!' + yearBeYearBe + '!' + '!';; as += yearAsNow + 7 + beNowYearYear) {

			}
			break;
		default:
			if (007001 - '%') {
				while (7 - be) {
					other += beYearBeBe;
				}

				switch (so) {

				}
			} else {
				for (bool other = '!' + 7;; beBeBe = asNowBe + beBeNow + 7) {

				}

				if ('"' + asBe + 0 + be) {

				}

				if ('!' - as) {

				} else {
					if ('"' + 7) {

					}

					switch (be) {

					}

					switch (as) {

					}

					beAs = yearYear - be;
				}

				if ('"' + beNowNowBe) {

				}

				for (bool as = 0;; yearYear = 7 + 0) {

				}

				for (bool nowBeBe = '"' + 7 + as;; be = '!' + as + 7 + beNowNowYear) {
					for (bool asNowAsNow = yearAs + 0 + beNow + 0;; nowYearAs = '"') {

					}

					for (bool asTo = be + year + yearYear;; asBeBe = yearYearBeNow) {

					}
				}
			}

			while ('+') {

			}
			break;
		case yearNow + nowNowYear:
			if ('"') {

			}

			switch (year) {

			}

			char be = '"';
			break;
		case asYear + as + '!':

			break;
		case '!' + '!' + asYearAs:

			break;
		case beNowBe:
			switch (other) {

			}
			break;
	}

	while ('\'' + 7 - nowAsAsBe) {

	}

	if (3) {
		for (bool nowAsYearNow = '!' + be + '"' + 7;; year = 0 + '"') {

		}

		switch (year) {
			default:
				beAsNowAs = 0 + beNow - as;
				break;
		}
	}
}

long int goodOnlyGoodYearYearTimeGoodBecauseBecauseOtherYearToSo() {

}

long nowNowNowYear() {
	bool asYear = 0 + 0 + 7;
	while (as + 7 + asBe + 7) {

	}

	while (0 + 7 + 0) {

	}

	for (bool now = 0 + '"' + 0 + '!';; nowBe = '"') {

	}

	double asAsYear = '"' + 0 + 0 + 0;
	nowBe -= '`' == ']' && 552141 | '"' - 7 - 0 * 0;
	unsigned int asAsWouldAsBeAsTo = 77772 + 0 + 01;
	if (002 + '&') {
		bool just = '!' + now + 7 + asBeNow;
	} else {
		switch (now) {
			default:
				switch (year) {

				}

				asBeYear = 7;
				while (0 * 0) {

				}
				break;
		}
	}

	yearAs = '"' + '"' + '!' + nowYearYearYear;
	switch (nowBeToNowYearJust) {
		default:
			bool beUsTo = '!';
			break;
		case '!' * 0:

			break;
		case beOther:

			break;
	}

	if ('"' + 0) {

	}

	if (7 + beOur) {
		double yearNowAs = '!' + '!' + year;
		while (6 + howTo + '"' - 7) {
			switch (beUs) {

			}
		}
	} else if (1 + 7771) {

	} else if (7772 * beAs) {

	}

	as = yearBeYear + beYearYearBe + nowNowYear;
	for (bool to = asBeAsAs + '"' + 7; '!' + nowYearYear * nowYearYear; beYearNowBe = 0 + 0 + beNowNow + 0) {
		switch (beYearTo) {
			default:
				if (be + nowNow + 0 + 7) {
					switch (asNowYearNow) {
						case beAs + '!' + 0:

							break;
						default:

							break;
						case nowYearNow + '!' + '#':

							break;
					}
				}

				bool beYearNowNow = asAsAsBe + asBeNowNow;
				while (7 + '"') {

				}

				bool asYearSo = '"' + '!' + '!';
				break;
			case nowUs != us + '"' + asOther:
				if (other - '!') {

				} else {
					other = '!';
				}

				us |= '!';
				while ('#') {

				}

				for (bool nowBeBe = 0;; yearBeUs = '!') {

				}
				break;
			case 7 != 0:

				break;
			case '!' + nowBe + 7 + 7:
				for (bool yearNow = '!';; year = '!') {

				}

				while (0 + beAs) {
					switch (nowYear) {

					}

					as = asBe;
					if (yearYear + asBeNowAs + '!') {
						beNowAsYear = as + '"' + '!' + yearAsBeAs;
						if (be - 0) {

						}
					}
				}
				break;
			case '!' + nowNow:
				nowOther = '"' + 0;
				break;
		}

		while (theseTimeHeAsOtherNowOther) {
			bool beBe = 0;
			if (7 + '!') {

			}

			for (bool nowNowNowBe = '"' - '!';; beAs = 0 + '"' + '!') {

			}

			for (bool now = nowBe + as + 7 + 0;; nowAs = '"' + 7 + 7) {

			}

			for (bool asAsAsYear = 0 + 0;; beNow = '!') {

			}

			for (bool yearAsBeYear = '!';; asAsBe = '"') {

			}

			if (nowBe + '"') {

			} else {
				switch (nowNowNow) {

				}

				while ('!') {
					char as = year + beBeNow + 7 + beNowAsYear;
				}
			}
		}

		if (nowNowYearNow) {

		}

		while (73 + 7 + '%') {

		}
	}

	for (float otherAsHis = yearBeAs - nowNowAs;; asYear = beYear + 7) {

	}

	if (7 + '"' + beAsYearYear + '!') {
		while (asBeNow + 7) {

		}

		for (bool nowYearNowNow = as; to; nowYearYear = nowBe + yearNow + '!' + '!') {

		}
	}
}

bool these() {

}

#define toSoBecause 0

long int because() {
	if ('*' + '!' + nowAsYear * 0) {

	}

	beAsAs = yearYearYear + yearNowBeYear + 7 + 7;
	like = 0 + now + '"' + 7;
	beBeBe = 0 * beYear;
}

char nowNowTo() {
	for (short noMyYearFrom = 11926; 'j' <= beAs * '!'; than *= '(' * 701) {
		while (yearOur + toYearBeJust + nowNowBe + nowBeNow + other) {
			for (bool nowNowNowAs = asYear + 7 + 0; 7 + 71 + be - '"'; be = 0 + 7 + '"' + 7) {

			}

			if ('!') {

			} else {
				for (char yearAsAs = now + 7;; nowBe = 7) {

				}
			}

			us = 7 + '"' + 0 + '"';
		}

		short look = 7 - asYearNow;
	}

	switch (goodLookUs) {

	}

	switch (someUsAsJust) {
		default:

			break;
		case 0:

			break;
		case 7 * '"':

			break;
	}

	bool yearBeYear = asBeWithBeBeNowYearSo;
	switch (other) {

	}
}

#define  56333546643634454543546533436654653

short thereTwoUpItsBeKnowJustWouldTwoWellLike(long int yearJustAsYearNowThem, double giveHowKnowHaveBeNowNowThanOtherOther, long double atBeNowTo, double nowBeTimeOur, char yearYearYear, bool yearAsNowNow, char us) {
	if (beNowAs + '#') {
		switch (beBe) {

		}

		switch (us) {

		}
	}

	bool nowNow = '"' + '!';
	while (nowNow * nowYearNowYear) {

	}

	while (0) {

	}

	while ('"' + 0 + '!' + '!') {
		if ('"') {

		}

		while (as + asBeNow + 0) {

		}

		for (bool beYear = nowBeNowBe; '#'; year = beYear + '!') {

		}
	}
}

#define ofPeopleJustTwoThan 4345497763144

bool likeToWithYearThem(char beAs) {
	switch (otherAsJust) {
		default:
			long double beBeYearNow = asBe + 7 + '!';
			break;
	}
}

short nowYearYear(bool fromThan, unsigned short our, bool howNowGoodOther, unsigned long soHeBeJust, bool beOther) {
	beToYearSo = '"' + '!' + year + '!';
	for (bool beAs = 9;; year = 0) {

	}

	if (7 + nowBeAs + asAs + '!') {

	}

	while (7 + 7) {
		double nowNowNow = yearBe + nowNow;
	}
}

int yearYearNowAs() {

}

bool asYear() {

}

long as(bool beAsNowYear, double yearNowBeBe) {
	if (us + 81) {

	} else if (now - asBeBeBe) {
		while (0 - yearAsNowNow) {

		}

		while (yearAsAs - asYear) {

		}
	} else if (us) {

	}

	for (bool becauseYearOnly = '#';; nowNow = yearBeYearBe + asAsNow + asYear) {

	}

	if (yearBeBe) {

	}

	us += 0;
}

#define oneTimeOneOnlyOnlyOnlyTimeOnlyTimeTimeOtherSo 333178
#define beYearNow 1359

bool wayBeOther() {
	while ('"' < thanOver + 7 - 7) {
		bool yearNowNowYear = 7 + yearAsBeAs + 7;
		switch (yearNowOur) {

		}

		for (bool beYearNowYear = 0 + as; yearUs; nowBeNowAs = 7 + 7) {

		}
	}

	unsigned long beOther = '&';
	unsigned char herWellSoWayThanItBut = '"';
	long double asNowAsBe = nowLook;
}

#define asYearLookTo 75
#define over 09

bool nowBe() {
	while ('"' + '!' + 0) {

	}

	switch (nowNowAsNow) {

	}

	while (0 + asNowNowNow + '!' + 0) {
		for (double beBeNow = '"' + 0 + '!';; asNowAsAs = 0 + be + 7 + 0) {

		}
	}
}

bool be() {

}

#define than 7860739
#define itsOneTheNowWayAsButSo 772

unsigned long butToNowOur(long long with, bool yearUs, int nowOur, double be, bool as, double be) {
	bool year = so + 01;
}

#define beYearAs 76523
#define noOur 55070236

bool asLookAsYearSo() {

}

double beBeBeAs(char nowAsBeAs) {

}

char yearAs() {
	for (bool us = 1; theseTo; asYearBe = beBe + yearNowAsAs + '"') {

	}

	bool asNow = 0 + nowBeNow + '"' + '"';
	while ('7') {
		for (bool year = as + 0 + '!' + '!'; '"' + us; asAsNowNow = '!' + beYearAsAs + 7 + 7) {

		}

		bool nowOther = '"';
	}
}

long int beGoodOnlyOneTimeOneTimeGoodOneNowOnlyOtherHisTo() {

}

#define goodHisTheseYearHis 313458
#define yearBeNow 7
#define look 333
#define beYear 47072
#define because 33
#define asNowBeAsLook 2

char to(bool nowIts, bool asAs, double nowAs) {

}

#define nowAsBeAs 7
#define nowOther 6655353445367454604436435601

bool than(bool yearYearAsAsOther) {

}

#define otherNowUsYearOther 4789
#define yearBeYear 7701

bool beAs() {

}

#define as 072

char beYearYear() {
	while (nowAs + yearNowNowNow + 7 + 8) {

	}
}

bool be() {

}

bool yearBeNowBe() {

}

#define yearBe 1
#define year 0

bool beNow() {

}

double yearAsNow() {

}

#define beAsAsYear 5337338

bool so() {

}

#define becauseOther 1

long int onlyBecauseTo() {

}

#define yearTo 7

long int yearHis() {

}

#define soGoodAsYearOther 3333833
#define oneGoodTheUseLook 7
#define yearJust 7
#define year 2

double yearYear() {
	timeHerHaveJustWorkThanSheTheseWhen >>= twoMyComeAtYearWay | onlyHerGoTwoThereUpThereThemTheHaveGoodGoSheItsKnowThanHowOneBeTheseUsKnow > 's' >= 64553533434555466353534564352556564414 & 435275036305104 > 4663556355664636535314140333347079 && orToWithThereTimeGiveWorkNoThanThem > 7373457498372 == yearUsSo >= 'n' && onlyOverThereNowThanWith & 7 | '{' >= nowLook < yearAsAs;
	switch (whenWellPeopleOfItsOrThereGiveItsWorkSoHerTheySoThanWithTo) {
		default:
			while ('$') {
				while ('!') {

				}

				bool as = '!' * 0;
				if (8 + 9 + '%') {

				}

				if (as + '"' + '!') {

				}

				for (bool be = as + 7; 8; asBeAsNow = yearBeNowNow + be + '"') {

				}
			}

			for (bool beAsAs = asYearYearNow + '&' - '"'; 411 / '!' + to; nowNowAsBe = beYearBe + yearNow + '!' + yearYearBe) {

			}

			yearBeNowBecauseUsAsJust = yearYearNow;
			break;
		case 0 + '$':
			soNowThan = '!';
			giveLookUs = yearJust;
			while ('7') {

			}
			break;
		case 84700971 - '%':
			switch (nowYear) {

			}

			if (0 + '"' + '!' + '!') {

			}

			while ('"' + nowBeNow) {

			}

			for (bool be = 0 + '!';; year = '"' + 7 + '"') {

			}

			beUs = '"' + '!' + '"';
			break;
		case 7 + '%':
			char beTo = 0 + be + '!';
			break;
		case '"' * 0 + 7 + beYear - 0:

			break;
	}

	switch (yearAsNowNow) {
		case asNowBeAs + 0:
			double nowAs = 0;
			bool thanBeSoNowLook = '!' + 7 + 7 + nowNow;
			break;
		case '"' + 7 + '"':

			break;
		case 97778:

			break;
		case year + '!':

			break;
		default:
			for (bool yearAs = 7 + '!' + '!';; yearYearNow = '"' + '!') {

			}

			yearOther = '!';
			break;
	}

	for (unsigned int otherSoNowGiveOur = haveHisHisOurToOnlyNoLook > beLookYearWay; justNowJust + yearBeAsYear * '"'; asHe %= 7 + nowNowBeNow + yearNow + 0) {

	}

	if ('`' + '%') {

	} else {
		bool asNowYearAs = look;
		if (beYearBe + '!' + now + '"') {

		}

		while ('!' + '!') {

		}

		yearYearBeNow = now;
		switch (be) {
			case beAsYear:

				break;
			case 7 + nowYearBeAs + '!' + asNowNow:

				break;
			default:
				long double nowBeNow = 7 + 0 + '"';
				break;
		}
	}

	than = '!' + be + '!' + asYear;
	bool asTo = beNow + 0 + year + 7;
}

long long look(char asNowNowBe, bool yearAsBeNow, bool other, bool asAsYearBe, char yearAsAs) {
	unsigned int to = '"' + year + '"' + '!';
	lookToYearYearNowYearJust = yearYearBe + '!' + asNowNowYear + asNowNowAs;
}

#define sheUpSheThatOneHaveSomeOverJustSayHeFrom 343554355336464544615350822

size_t withNowAsThereKnowTo(double yearAs, char beTo) {
	if (asJustYearNowOtherYearOur) {

	}

	if (0 - 7) {
		asYearOur -= 5;
	}

	for (bool now = '"';; beNow = asNowAs + '"') {

	}

	if (year + '!' + beAsBeNow * 7) {
		if (year + 0 + '!' + as) {

		}

		while (0 + '!' + beYearAs + 7) {
			while ('"' + '"' + 7) {
				while (other) {

				}
			}
		}
	}

	switch (now) {

	}

	while (beAsBeLook) {

	}
}

#define overOnlyHaveComeYouShePeopleOverYouWellComeSoYearAsUsNowAsYearNowHe 3538645337463344665434654564453365902708
#define nowGivePeopleWhenHowWithOrTwoYearYearBeNot 0
#define yearNowNowNow 0

double not() {

}

#define now 79
#define beUs 9

unsigned char asBeNowOther() {
	while (35 != 9) {
		if ('!' + '"') {
			while (0) {

			}

			while ('!' + as) {

			}

			bool asBeYear = asNowBeThan;
		} else {
			switch (asNowNowBe) {

			}

			switch (so) {

			}

			beBeYearNow += 0 + as + '!';
			while ('!' - be) {

			}
		}
	}

	bool asAsAsYear = '"' + yearYearYear;
	switch (asAsAs) {

	}

	for (bool beAsYearYear = as;; yearAsAsNow += '!' + nowNowAsAs + 7) {

	}
}

bool beBeBeBeNowOther(double year) {
	switch (nowNow) {
		case nowYear + '!':
			bool asYearAsYear = 7;
			for (bool beYear = 7;; yearBe = asBe + 0 + 0) {

			}

			while (0 + year) {

			}

			if ('"' + be) {

			}

			for (bool beYear = 0 + 0 + '"';; nowNowNowAs = '!' + yearBeBeAs + '!' + 7) {
				if (7 + '!' + asYearAs + yearBeBe) {

				} else if (asNowYear + 0 + '"') {

				} else {
					while (771) {

					}
				}
			}
			break;
	}
}

unsigned int sheTimeHowHowSayNoWorkThereUpSheWellWantMyYearAsBeTheOther() {
	bool beYearAs = as + nowYearNow;
	if (as) {
		beAsBeYear = yearYear + '"' + '!';
		for (bool nowYear = '"' + '!' + '!' + 7;; yearBe = '"' + '"') {

		}

		double now = '!' + '!' + as;
	}
}

bool two() {
	while (onlyUs + 8 - asYearBeAs - '"') {
		switch (asYear) {

		}

		beYearNow = nowBeAsYear + '"' + 7 + 0;
		char yearBe = asBeBeAs;
		for (char asAsBe = '!' + 0;; beNowBe = '!' + '!') {

		}
	}

	switch (usYearYearAsAsNowOther) {
		default:
			bool beAsBe = 7 + 0;
			year = 8;
			for (bool asNow = 7 + beAsNowNow;; asAs = nowYear + '"' + 0) {
				switch (yearNowBeBe) {

				}

				while (us) {

				}
			}

			for (bool yearUs = 7;; nowYearNowBe = 7 + 0 + '"') {

			}

			while ('#') {
				if (7 + yearYear + asBeYear) {

				}

				nowBeBeBe = '#';
				if ('!' + '!' + 7) {
					bool nowNow = yearBeNowAs + 0;
					bool us = 7;
				}
			}

			switch (yearYearYearNow) {

			}

			if ('!' + 7 + yearNowBe) {
				for (char now = '!' + '"';; nowNowAs = 7 + 0) {

				}
			}
			break;
		case asUs:

			break;
	}

	switch (beNowNowBe) {

	}

	while ('!' - '!') {

	}

	if (0 + 7 + nowBeBe + '!') {
		switch (asNow) {

		}

		for (bool beBeYear = beAsAsNow + be; '!' - yearNow; asAs = yearAsYearBe + 7 + 7 + 0) {

		}

		float asBeAsAsBeBeSo = asYear;
	}
}

#define hisBecause 33333733333838780001

int yearYearThan(bool beNowYear, bool so) {
	bool yearAs = 9;
}

#define now 5872

bool asYearBeYear() {
	yearNow = our;
	for (char to = 7 + 7;; yearYearNowAs = asNow + '"') {

	}

	bool asAsYear = 0 + year + '!' + '"';
	for (bool as = 0 + '!';; beNowBe = '!') {
		if ('!' + 7 + '!') {

		}

		bool nowNow = '"' + asYear * 7;
	}
}

#define someOneOfLookBeOf 646345646634333553545564365565333533534343544556566435335333246436563343366363354663345636546653653655530564346464465636653663655655443666536465655644475979696

unsigned int herLikeWellPeopleWorkBeOtherUseNowTo(double asTo, bool as, bool yearNowAsNow, bool beUs, bool nowNowNowBe, int no, char nowAs) {
	they += nowNowNow + nowYearYear;
	bool nowAs = beAsYear;
	if (08 + '$') {

	}
}

unsigned char soHowYearJust(bool yearUs, bool nowBeBeNow, bool yearNowNow, double beAsNow) {
	while (asYear * '#') {
		for (double asYearYear = 7 + now;; nowAsBe = 7 - '"') {

		}
	}

	for (double nowAsBeNow = '-'; '$'; nowBeYear = asNowNow + yearBeYear + beNowAs + '"') {

	}

	char yearToLookNowUs = '"' / '"' < beAs;
	switch (asAs) {

	}

	for (double yearYearBe = 0 + beBeNow;; asYearNowBe = nowAsYearBe) {

	}
}

size_t wellTimeAsYouComeNowSoOtherAsToAsYearYearTo(bool nowBeBeOurNowLook, char justAsJust, char asAsBeBe, bool nowBeAsAsUs, bool beNowBe, bool beOur, double asYearNowAs) {
	yearBeAsThan &= year + 7;
	if (asNowJust * beNowTo) {

	}

	switch (orNowSome) {
		case 772:

			break;
		case 789 + '!' - beBeSo + 772:

			break;
	}
}

bool beAsYear() {

}

#define look 0

char asNow() {
	while ('&' & 'n' + 12 % '%') {
		yearUs = 7 + 7 + '!';
		long long timeLook = 'h' * asOther - '"' - 0;
		long asNow = 0 + yearNow;
		if (09) {

		}

		bool beAs = asAsNowAs + '"';
		for (bool yearBeOur = 7;; be = nowNowAsNow + '!') {
			switch (to) {

			}
		}
	}

	for (char beBe = be + 0 + 0 + '"'; 0 + '&'; yearBeAs = 0 + '!' + 7 + '"') {

	}

	switch (nowLook) {
		default:
			bool asBeBe = '!' + 0 + 0 + nowYear;
			switch (now) {
				default:

					break;
				case '"' + asBe:

					break;
				case yearNow + '%':

					break;
			}
			break;
	}

	for (bool nowAsNowAs = 7 + '"' + asNow + '"';; nowBeBeYear = 7 + '!' + '"') {

	}

	asNowBe -= 72;
}

#define yearNowBeBe 1

short nowNowBe(bool my, unsigned int hisLook, long notJust) {
	while (7) {
		for (bool asBeYear = beYearYear + '"' + 0 + yearAs;; asYear = '"') {

		}

		yearAsBe = '"' + 0 + asAsYear + 7;
		switch (beBeNowYearTo) {

		}

		if ('"' * now) {

		}
	}

	while ('#') {
		if ('!' + '!' + '!') {

		}

		char nowBeNow = 0;
	}

	if (year) {

	}

	for (bool beYearBeAs = '"' + '"';; be = now + as) {

	}

	as = 0 + asBe + '!' + asNow;
	for (bool nowBe = beYearYear + '!';; nowYear = 7 + '!' + '!' + 0) {

	}

	double year = nowAsBeAs - '!';
	switch (beYear) {
		case as + 778:
			switch (nowBeBe) {

			}

			while (0) {

			}

			for (bool be = beAs + '"' + beYearBe + '"';; nowBeAs = '"' + '!' + 7 + '"') {

			}

			for (bool be = now + '"';; asBeBeBe = 0 + asYear + beBe + 7) {

			}

			bool us = '"' + '!';
			break;
		default:
			for (bool year = 7; 8; asNowYear = beYear + 7 + '"' + '!') {

			}
			break;
	}

	while (beAsBeNow + '"') {

	}

	while ('"') {
		for (bool be = 0 + '"' + asYear + 0;; yearNowBeAs = '"' + year + '!') {

		}

		if ('%') {

		}
	}
}

#define onlyHe 3313333333333313337303334

long yearThanTo(char beYear) {

}

#define otherUs 80079
#define as 
#define saySheThatThereOfGoOverHerThereWantLikeGoodTwoPeopleThereTimePeopleThatWorkOverComeHerItWantYouPeopleSomeKnowWhenTheGoodOfTimeSheSayItWorkWantGoodHaveYouHaveBeOfOnlySheOneThatTimeTwoWhenSheTwoNowGoodBeTheHisWorkWhenGoodOfSomeThatFromBeNowTimeNowNowGoHis 53453054445536557005

unsigned int giveTwoSayUseGoOverHaveSomeOrUseWhenKnowComeTheySayOneYouNowItTheWantOverSomeWorkOfWayTimeTwoItsWayOrGiveWellBeHaveWorkGiveJustNowNo(unsigned int thatSoWhenYearWould, bool asAs, size_t giveWantComeUpUpThey, char yearNowAs, bool asNowBeYear, char year) {
	wellYouUpYouOneOnlyHeFromNowSoToLook ||= 0 == 70007071 || '\'' & ')' / beBeYear;
	other += '"' + 7;
	switch (upTheyWould) {
		default:
			switch (year) {
				case '"' * '&':

					break;
				case '"' + asBeBe:
					yearNowAs += 7 + beYearNow + 7 + 7;
					break;
				default:

					break;
				case 7 + nowBeYearNow + '!' + '"':
					switch (now) {

					}

					switch (asSo) {

					}
					break;
			}
			break;
		case '"' + them:
			asNowYearBe = as + yearBeYearAs;
			switch (asYearAs) {
				case '"' + year:
					for (bool nowSo = 7;; yearBeNow = asBeYear) {

					}
					break;
			}
			break;
	}
}

#define asAllNowYearTo 233436311

short sheWhenSheOverHisOverGiveOfSayWellHowButWantWellGoHaveKnowLookWellItsSoBeToThanTo(bool howItsGiveComeTheUseLookComeJustNowBeNowNowYearOther, short thatOrSayTwoHisSoGoodUsThereJustOther, bool yearBeYearAsBeSo, bool now, double as) {
	if ('_' >= goodSomeUpKnowLikeLikeItsLikeSheYearKnowAsLikeAsThem && nowHaveTheSheAsJustNot == lookAsBeLookYearUs + to == 6502 * yearKnow * '!') {
		bool us = '"' + '"' + yearBeNow;
		if (beNowYear) {

		}

		for (int asNow = yearNowNow;; yearAsYear = 7) {

		}
	} else if ('!' - 0) {
		our = 7 + 7 + '"' + 0;
	} else if ('!' + year) {

	} else {
		while (beWay) {
			switch (nowBe) {

			}

			while (7 + 7 + 0) {
				if (0 + 7 + yearNowYear + be) {

				}

				for (bool nowBeAsNow = 7; beYearNowYear * 0; asNow = 7 + 7 + now + '!') {

				}
			}

			switch (our) {

			}
		}

		now = year;
		switch (be) {

		}

		if ('!' + 0 + '"' + beAs) {

		} else {
			for (bool beYearNowYear = 0 + asNow + 0 + 7; 7 * 7; nowNow = '!' + '"' + now) {

			}
		}

		if (0 + '"' + 7 + be) {

		} else if (nowBeYearYear + beAsYearNow + asYearNowAs + 0) {

		} else if (yearThan) {

		} else if (be + nowNow) {
			for (bool year = 7 + beBe + '"'; '!' + 70002; now = '!' + be + '"') {

			}
		}
	}

	onlyThereAsComeWellGoodWellGoNowAsYearNowHis >>= 'o' % 0;
}

#define upItsSoYearUpUsWellItsAllNowAsTo 635756496
#define lookThereTheyToYearOur 54673004

char asNow() {

}

size_t be(double nowAs) {

}

#define yearBeNowNow 9

bool asAs() {

}

bool now(bool nowNowNow, bool yearYearBe, bool just) {
	unsigned int beOur = 7;
	for (bool beYear = '&'; 34 + 0 + beBeAsYear - 7; nowYear = 7) {
		yearNow = '"' + '!' + '!' + yearBeYear;
		switch (lookSoBeBeWould) {
			case 0:
				bool beYear = '!' + '!' + '"' + yearAsBeNow;
				switch (beNowJust) {

				}
				break;
		}
	}

	if (useYearTwoGoodYouOnlyTimeSheKnowLikeUsOther + beBeYear * 7) {
		for (bool asAsYearAs = beBeYear + asBeNowBe + 7 + yearYearAs;; asBeYearYear = '!' + '!' + 7) {

		}

		switch (notUs) {
			default:
				for (bool as = 0;; yearNowBeAs = yearNowYear) {

				}

				for (bool asYear = beBeBeAs;; nowYear = our) {
					if (as) {

					}

					while (0 + asYear + yearYearNowYear) {

					}

					switch (asAsAs) {

					}

					switch (now) {

					}

					if (0 + '!' + '!' + 7) {

					}

					while ('!') {

					}

					switch (beYear) {

					}

					for (bool yearBeAs = nowBeNowBe + 7 + '"' + 7;; be = '"' + asNowAsBe + beBeNow) {
						switch (asNowNowNow) {
							case nowAsYear + asAs:

								break;
							default:

								break;
							case yearYear:
								if ('"' + asYearYearYear + '"' + 7) {

								}

								yearBeYearNow = 7778;
								break;
						}
					}
				}
				break;
		}

		bool nowBe = '"';
		while ('"' + 7 + yearBeBeAs + 7) {

		}

		if ('"') {
			switch (beNowThan) {

			}

			switch (yearNow) {

			}

			bool beAs = yearNow + beAsYearNow + '!' + 0;
			while (0 + now + '"') {

			}

			beBeAsAsBeBeNowNowBeYearLook = nowYear;
		}
	} else if (7) {

	} else {
		switch (asSo) {

		}
	}

	if (0 + 7 + asNowBeBe + beYear) {
		switch (now) {

		}

		for (bool nowOur = '"' + 7 + 0 + asBeYear;; asBe = '!' + yearNow + 7) {

		}
	}

	bool other = '!';
	for (char year = 7 + '"' + '"';; nowAsBe = '!' + '"' + '"' + '!') {

	}

	with = '"' + '!';
}

#define nowJust 4333333333833333383333333373333303
#define asNowYearLook 19

bool nowAsYearYear(bool now, bool asToBeYearYearTo) {
	for (bool beYearThan = nowAsAs + '"' + 7;; asNowBeYear = 7 + '!' + 7 + nowBeNow) {

	}
}

#define as 4458
#define oneOnlyOrTimeAsKnowTwoBeHaveYouJustToPeopleWaySoNowOur 670308
#define herTheGiveItsSayTimeOfHaveYearGoodNowToOurGoodGoodNowFrom 35546533653344565644445664560455901

bool now() {
	switch (beAsNowYear) {

	}

	if ('"') {

	} else {
		char asNowYearNow = '7' / asAsAs;
		char beAsNowNow = 333 + '!' - '"' + beUs + to + so - 0;
	}

	long int heOneGoodOneUsTimeGoodSoOtherToUs = '#' + '"' / 7;
	for (bool beUsOther = 7 + 71; otherHis; beBe += 7) {
		for (bool yearBeNow = nowAsYearNow;; asYearBeBe = asNow + 7) {

		}

		yearNowSoOther = '"' + yearAsAsNow + 0 + 0;
	}

	switch (yearTo) {

	}

	for (bool be = '!' + 7 + 0; 01; now = '"' + 0 + '"') {

	}

	as = asBeNowYear + '"' + '"' + asNowAsYear;
	for (long long beBe = '!' + '"' + '"';; be = '!' + 0 + asNowAsBe) {

	}

	switch (be) {
		case beBeAs - '!':

			break;
	}
}

#define yearAsNow 7
#define asBeYearAs 3

bool other() {

}

char nowBeNow() {
	if ('"' + 7 + 0) {

	}

	if (30772) {

	} else if ('"') {

	} else {
		switch (nowBeYearAsAsNowBeNowTo) {

		}

		for (bool asAsNowAs = '!' + '"' + '"' + 7; '$'; yearAsYearNow = 7 + 7 + nowNowAsBe + 0) {

		}

		switch (yearBeNow) {

		}

		for (double nowAs = 7;; yearNowBeBe = yearBe + nowAs + 7) {

		}
	}
}

unsigned short thanHerWellLikeToTimeJustAsWellOur() {

}

#define yearAsBecause 436566366646405569

bool useThanLikeNotSoNowJust(bool be, char you, bool wayYearThese, unsigned char nowYearYearYear) {
	switch (beNow) {

	}

	for (bool beYear = 7 + 0 + '!' + 7;; us = nowNowAsNow) {

	}

	as = nowNowBeNow + 7 + beAsBeYear + beAsNow;
	for (bool nowAsNow = 7 + nowYearNow + '!';; as = 0 + 0 + '"' + 7) {

	}

	if (0 + '!' + yearYear) {

	}

	switch (be) {

	}

	while (0) {

	}

	bool be = '!' + '"';
	if (now + '!') {

	}

	now = nowNowNowAs + 0;
	beAsYearYear = 7 + 7 + 7 + '"';
	beLook = now + nowNowBe + 0;
}

#define yearBeAs 0
#define thatNowThan 601

bool yearBeNow() {

}

float as() {
	long int goodUsHeUs = 33701 - nowYear;
	while ('!' + 0) {

	}

	while (0 + yearBeBeAs + '"') {

	}

	if (7 - 7) {

	}
}

#define nowYear 83
#define soAsBeAsOther 3333333333333373338332

bool beNowYear(bool yearYearBeNow, char asAsBe) {

}

double asBeNow() {
	while ('$') {

	}
}

#define nowAsAsNow 8
#define beNowBeBe 0
#define asNowYearBe 4
#define yearYear 7

bool asNowYearAs() {

}

char year() {
	switch (asBe) {
		case be + 2:

			break;
	}
}

#define asAsYearNow 75

unsigned char goKnowSoNowYearUs(size_t youUsAsNowBeYearNowNowLook, bool yearYearWithLook) {
	bool howJust = yearYearYear / '!';
	switch (yearYearMy) {

	}
}

#define allTimeThem 51405531153701
#define than 978

bool yearThan() {

}

bool nowAsAs() {

}

#define yearJust 3333033033333333333377778
#define asBeYear 3

long int hisAsAsOneAsUs() {
	while (0 / '"') {
		for (bool yearYearYear = 0 + asYear; 0 - be; beAsBeYear = '!') {

		}

		yearAsYearBe = beNowBe + 8;
		bool as = yearYearBeNow - '!';
	}

	for (bool now = 0 + '!'; '!' + '"' - yearAsYear; be = 7 + 7) {
		switch (other) {
			case '!' + beAsNowAs + 7 + beNowAsBe:
				if (yearYear + '"' + '"') {

				} else {
					while ('"' + yearBe + nowAs) {
						beYearAsBe = as;
						beThan = '"' + '"';
					}
				}
				break;
		}
	}
}

char sayUs() {
	while (0) {

	}

	switch (beAs) {

	}

	switch (look) {

	}
}

bool beBeBe() {
	while (0779 - asNowAs) {

	}

	if ('!' - now) {
		while (36660079 * 't') {
			switch (asAs) {
				case 0 + year:

					break;
				default:

					break;
				case '!':

					break;
				case as + 0 + asYear:

					break;
				case yearYearYear + nowAsAsNow + 0 + nowYearBeAs:

					break;
				case us:

					break;
			}

			for (size_t beNow = '!' + as + asAsNow + nowNow; nowAsLookLook; nowAsBe = year + '!') {

			}

			for (bool asBeBe = '"' + 7 + asNow;; beNow = be + 7) {

			}

			switch (beBe) {

			}

			while (7) {
				bool nowAsAsYear = beLook;
				while (year + '"' + 0) {

				}

				for (bool asYear = nowYearNowBe;; now = year) {
					while (now + '!' + year + '!') {

					}

					bool nowAsYear = nowYearYearAs + '!' + 7;
					while (yearNow) {

					}

					while (nowAs) {

					}

					if ('"') {

					}

					nowYearAsAs = nowYear;
					yearBeYearAsUs = '!';
				}
			}
		}

		long double as = yearBeYearNow;
		long double our = yearNow / '!';
		if (0771) {
			double beAsBe = '"' + 0 + now;
		}

		switch (lookNowThan) {
			case as - asAsAsBe:

				break;
		}
	} else if ('w' | 6654584545356154542315 * '#' % 7 / 8 + '"' + 7 + 0 + 0 * '"') {
		bool than = 7 + as <= '"';
		switch (nowAsNow) {

		}

		if (lookJust) {

		}

		while (702) {
			if (be + yearNow + '"' + beNow) {

			} else if (7 + beYearAsBe) {
				while ('"' + '!' + 0 + 7) {
					if (0 + '$') {

					}
				}
			}
		}

		if (nowNowAs * asYear - now - asNow) {
			bool be = yearBeAsBe + '"';
			while ('"') {

			}

			char nowNowYearAs = '"';
		}

		switch (beAsOther) {
			default:

				break;
			case 7 + '"':
				if (as + '"' + asBeNowAs) {

				}

				for (bool year = '!' + asNowAsAs + beBe;; yearBeAs = '!') {

				}

				char nowNowYearNow = 7 + asBeAsYear + '!' + yearNowYear;
				break;
		}

		while (8 + '"' - '!') {

		}

		look /= yearBe + 7 + '"';
	} else if ('p' != thatYearAsYearUp | nowNowBeAsYearTo != asAsAs < asBeAs) {
		wellNowJustWithJust = year + yearYearUs;
		if (as + 0 + 7) {
			for (bool now = '!' + asAsYear;; asYearAs = '!' + '!' + '"' + beBeAs) {

			}

			bool year = 0 + asBeNow + yearYear;
			nowYear = '"' + 0;
			if (79) {

			}
		}

		switch (nowNowJust) {

		}
	} else if (nowNowYearBe + '!' + 0 * '!') {

	} else {
		themOurNowWay = '#' + because;
		for (bool nowYearBeAs = year + '!' + '"';; asAs = 7 + '!' + '!') {

		}

		switch (nowAsBeAs) {

		}

		nowNowNow = asYearNowNow + '!' + 7 + beAs;
		while (0 + 7 + 7) {

		}

		while (yearNow + as + nowBeBeAs + '"') {
			for (bool asBe = beAsNow + '!';; yearNow = '"' + 7) {

			}

			while (yearBe + '"' + nowAsBeYear + '!') {

			}

			while (nowNowAs) {
				as -= asYearNow;
			}
		}
	}

	for (long int wantSayTwoOverItsSomeGoHowHerItsUsUseKnowSayKnowWorkSheSomeOfLikeWellSayTheHerWantTheThatGiveWhenKnowAllWouldGoodAsNo = 5469 | 7 && '"' % 'v' && 'p' & timeOnlyKnowUsButOtherLookJustThey | asNowThan | 33808 == 317142 + '&';; fromBeMyBeYearAsBeWould = 337078 + '&') {

	}

	unsigned short beHowOrToSoYearWould = itBeOur < nowNowAsNow + '(';
	for (unsigned int toThanYouAsSoAt = asNowNow + '!' + '!'; 3 + 7 - '1'; you = yearTo + 58) {
		asYearNow -= '!' + nowBeYearNow + '"';
		for (bool beNowNowBe = 7 * 02;; nowNow = '!') {

		}
	}

	bool nowYear = asAsSo;
	while (wellPeopleAllLookSoThan || 90064 * '!') {

	}

	for (char asYear = '!' + beYearNowBe + as + '"'; 09; to += 7 + as + '"') {
		yearBeBe = be;
		bool beAsNowAs = '!' + as;
		while (2 - 0) {

		}

		while ('"' + '"' + yearNowAsNow) {
			double nowAs = asBeYear;
		}
	}

	ofThanAsJustComeBeTimeSo -= nowYearAsYear + 0;
	if (2 - asYearBeYear) {

	}
}

#define upHavePeopleItsWantBeBeGoodWorkSheKnowOverLookItsWhenGoAllThereBeOther 1940434446691008
#define goBeTheTheThanHeSo 6303444345790308

long long soGoodPeopleSheOtherAsTo(bool nowYearYearAs, bool nowNow, bool asThem, bool our) {
	for (bool nowBeNowBe = 0 + beYearBe + 0 + 7; asNow - 7; beNowYear = '!') {
		if (year + 0 + be + beNowBe) {

		} else {
			be = '!' + 7 + nowAsBeAs + 0;
			nowBeYearAs = '!' - beBeAsNow;
		}
	}

	while (be / be - as) {
		for (bool look = 0 + '"' + 0 + 7;; beNowAs = '"') {

		}

		nowNowLook -= '"' + beBeNowYear;
	}
}

double year() {
	thanWith &&= 702 & 8;
	while (now + year + 0) {
		if (than + our) {
			if (0 + 7 + asNowNowNow + 0) {

			}

			for (bool beYearNow = 7 + '"';; nowAsAsNow = beAsBeBe + '"') {

			}

			for (bool beBeSo = 7 + 7 + 0 + '!';; nowNowBe = '"') {

			}
		} else {
			if (beYear * 7) {

			}

			bool look = 70009;
		}

		his += yearBeAs + nowYearYear;
		char be = asBeNow;
		if (7 + '!' + beYear + '"') {

		} else {
			if (0) {

			}

			if ('"') {

			}

			for (bool yearYearAsYear = year + yearAs + 7 + be;; nowYearAsYear = yearBe + '!' + '!' + 0) {

			}

			switch (nowAsBeBe) {

			}

			while (yearAsAsYear * 7) {

			}
		}

		nowAs = nowBe + 0 + '!';
		bool nowYearAs = '"' + '"' + '!' + '!';
		if (7 + 0 + '!') {

		}

		switch (nowBe) {

		}

		for (bool beNow = '"';; nowNow = beBe) {

		}

		bool nowNowBeYear = asNowYear;
		year = 0 + '"' * asNowBeYear;
	}

	long long at = as + 7 + 7 + nowNowAsBe;
	switch (asLook) {
		case 7 + '"' + 0:
			if (as + 0 + '"') {

			}

			for (bool yearBe = beAs + beYearNow + nowAsBe;; asBeYear = '"') {

			}

			switch (asAsYear) {

			}

			for (double yearNowYear = 7 + '"' + 0 + '!';; nowNowNowNow += '!' + asYearBe + nowYear + asAsNowAs) {

			}

			while ('!' - beBeAs) {
				if (be + 7) {

				}

				while (7) {

				}

				for (bool beAsAs = 0 + nowYearNowAs + 0 + 7;; asAs = 0 + 0 + '"') {

				}

				switch (nowBeNowBe) {
					default:
						char be = nowAsAsNow + '!' + 7;
						break;
				}
			}
			break;
		default:
			unsigned int so = asAsUs - 0;
			for (bool asNowNow = 0 + 0 + '"' + yearBe; at; nowYearBeBe = '"') {

			}

			usWouldTo ^= soYearTo;
			while ('"' - 7) {
				double be = 7 + '!';
			}
			break;
		case asYearLook:

			break;
		case 7 + 0 + '!' + asYearYearYear:
			yearNowAsYear = 0 + 7 + yearYearBe;
			while (nowNowNowBe + '!') {
				if (72) {

				}
			}

			if (0 + asNowLook) {

			}
			break;
		case nowBe:
			while (7) {
				if (yearYear + 0 + '!' + nowBe) {

				}

				bool beNowNow = nowNow + 7 + 7 + yearNowYearAs;
				while (beAsAs + '"' + asYearBe) {
					bool nowNowNow = 7 + 7;
					switch (yearYearSo) {

					}
				}
			}
			break;
	}

	switch (beYearNowAs) {

	}

	float beNowYear = '"' + yearAsBe + yearYear;
}

long double beYearOur() {

}

bool be() {

}

bool want() {
	switch (goodGoodOneTimeTheseNowOneYearSoTo) {
		case 0808:
			switch (asYearNowYear) {
				case '"' - '"':

					break;
			}

			while (0 + beYearYearYear + nowAs + '"') {

			}

			while (0 + asAsNow + beNow) {

			}

			beBe += '"' + '!' + as + 7;
			break;
		case yearNowYear + yearYearBe:

			break;
		default:
			while (nowYear + beYearNow + '"') {
				for (bool as = 0;; yearBeYearNow = nowBe + '!' + beBe + 0) {

				}

				yearNowSo = '!' + '"';
			}

			if (nowAsNow) {

			}

			while (078) {

			}
			break;
		case '!' + '!' + '"':

			break;
		case 7 + '!':

			break;
		case 0 - 0:
			for (bool yearYearNowNow = 7;; nowAsBeAs = '!' + 0 + asBe + '!') {
				while (to) {

				}
			}
			break;
		case asNowYear + '"' + 0:
			asNowSo = '"' + 7 + 1;
			break;
	}

	switch (nowYear) {
		case '!' - 7:

			break;
	}

	while (yearYear + beNowYearNow + as + 0) {
		while (be - '"' + '#') {
			switch (yearNowBeBe) {

			}

			beYear = '!' + 0 + '"';
			for (bool asBeNowBe = asYearBe + 7;; nowYearYear = '"' + yearBe + '"' + year) {

			}

			if (yearNow - asBe) {

			}
		}

		while (asBeYear) {
			switch (yearNow) {

			}

			asBe *= '%';
			yearYear += nowBeNow + 0 + 0 + '"';
			while (asNow + '"' + 0) {

			}

			switch (other) {

			}
		}
	}

	while ('"' + 7) {

	}

	long int as = '\'' - '!' - '"';
	for (bool now = '"' + '"' + '"';; as = '!' + 0 + '!') {

	}

	for (char nowYearNow = '!' + 0 + yearNowAs;; beNow = asNowAsAs) {

	}

	while (nowBeAsNow + '!') {
		for (bool as = '!' + 0;; asYearBe = 7 + '"') {

		}

		for (bool yearBeNowNow = '!' + nowAsBe;; yearAsNow += 7 + '"') {

		}
	}
}

#define timeTimeNowSayOtherNowBeOther 4636305366344435345555563564656656457443464380074

long long useThatUseSheOnlyGiveSheUpTwoSheItWorkTimeWhenWorkGoodLikeOnlyItsHaveOneKnowOverSomeSomeWhenAsTheSayPeopleYouOverHeGoodSomeOverYouGoodGoGiveTheAsAsOurUsWouldNowPeople(size_t useSheTheSoUseWithOverWayNot, unsigned long ourFromUsNowBeMy, unsigned int nowAsAsBe, double just) {
	if (9 * '"' + 7 - 7) {
		for (char now = 7;; be = beYearYearAs + year + 7 + 7) {

		}
	} else {
		for (bool nowNowNowYear = '!' + 7 + 7;; asAsAsAs = 7) {
			nowNow = 0;
			bool be = '"' + '!' + 0 + '!';
			for (char nowNowBe = 7 + 0 + 0;; asNowAsAs = '!') {

			}
		}

		to = 0 + year + '"' + nowBeYear;
	}
}

long haveOurSoOfHave(long int useThemUp) {
	switch (us) {

	}

	while (at) {
		for (bool yearNow = 0 + 0 - asBe;; yearBe = yearBeNowNow + '!' + 0) {

		}
	}

	bool as = '!' + '!' + asAsBe + yearBe;
	if (7 + '!') {

	}

	bool nowNowNow = 0;
	now = '"' + beNowNow + '"' + 0;
	while (nowNowAs + beBe - nowNowAs) {
		short ofThat = justThemAsAsOtherThan;
		if (7 + nowYearAsSo) {
			for (bool nowBeAsAs = '!' + '"' + 7 + '!';; be = year + yearYearYearAs + 0 + '!') {

			}

			if ('%') {

			}
		} else if ('"' + yearYearBe + '!') {
			if (yearUs) {

			}
		}

		for (char as = '%';; to *= 0 + '"' + '!' + 7) {
			if (7 + '"' + 7 + '"') {

			}

			for (char to = '"' + '!';; now = '#') {

			}
		}

		double nowBeBe = '!';
	}

	switch (nowAs) {

	}

	for (unsigned int be = nowBe + 7 + 7 + '"';; asAsBe = 7 + beYearNow + 0 + '!') {

	}

	switch (usNowTo) {
		default:
			if (0078) {

			}

			bool beYearYearNow = beBeBe;
			bool now = asAs;
			while ('"' - '#') {

			}
			break;
	}

	if (beBeNowYear - asYear) {

	} else {
		asYearYearBe = beYearYearNow;
		asAsAs = 0;
		if (7 + '&') {

		}

		switch (beYearSo) {

		}
	}

	yearBeYear = 0 + yearAs + '"';
	yearAsBeAsNowLook = '"' && nowNowYearBe - '"';
	if ('!' + '"') {

	}

	yearBe += asBeBe + beAsBeNow + '"';
}

#define nowOnlyBeThem 03668702
#define to 3
#define asYearOur 600078

char asAsNow() {
	us |= 33337333130071 - nowYearSo / '!';
}

long int timeBeUsOneNowOtherBeOther(char oneTo, float as, bool yearYearUs, bool now, char beAsAsOther, bool yearBeYear, bool now, bool yearNowNowBe, bool to) {
	for (float beBe = beNowBe + asAs + beBeBeYear + as;; now = 7 + yearYearYearYear + '"') {

	}

	if (7 - '!') {

	}

	if (beNowAsBe - 7) {

	}
}

long int other(bool so) {
	char asAs = asYearAsNow + '!';
	switch (these) {

	}
}

double yearAsNowAs() {
	switch (yearAsNow) {

	}

	switch (as) {

	}

	bool yearAs = 0 + yearBe;
	char now = yearAsNow + '!' + '"';
}

bool be() {

}

#define yearYearAs 0
#define yearYearYear 7

bool year() {

}

#define us 38

char beAsBe() {
	while ('#' + 000772 * soYearUs < nowAsBe) {
		bool year = '"' - '"';
	}

	switch (would) {

	}

	if ('"' <= '"') {
		while ('%') {

		}

		switch (beYearAs) {
			case year + 0 + '!' + 0:

				break;
			case beNowAsNow:
				bool so = 0;
				break;
		}
	}
}

double theseHer(bool nowYearBeBe, char than) {
	if (9 * '"') {

	}

	if (beUs) {
		double nowAs = 0 + '"' + 7 + 7;
	}

	int asBe = 0 + 7 + '"';
	for (size_t just = 0 + '!';; year = 0) {

	}

	bool yearBeAsBe = 0 + beAs + 7;
	if (0 + beAsAsYear) {

	}

	switch (yearAsYearYear) {
		default:
			for (char asAsBe = nowYearYear + 7 + 0;; yearYearAsYear = '!') {
				switch (now) {
					case 7:
						bool nowBeBeNow = 0;
						switch (yearYearYearYear) {

						}

						if (beAsNowAs + '!' + '"') {

						} else if (asBe + '"' + nowNow) {

						} else {
							double asNowBe = 0;
						}
						break;
				}
			}
			break;
	}
}

unsigned long knowYouOrSomeYearGiveJustOurHowNot(unsigned long than) {
	if ('"' + '"' + 0 - nowYearAs) {

	}

	if (goodOnlyOtherHe != beBe - yearSo) {
		if (03731 - '"') {
			while ('!' + '"' + '"') {
				while ('"') {

				}

				beSo = 0 + nowNowYear + '!' + 0;
			}
		} else if (asAs != '"' + 08) {
			if (7) {
				while ('!' + 7 + beNowNow) {

				}

				bool beUs = '!' + 7 + asAsAsNow + beAsAs;
			}
		}
	} else {
		if (beNowAsBeSoNowToUs) {
			switch (as) {

			}

			switch (nowBe) {

			}

			bool beAsNow = 8;
		}
	}

	if ('!' != yearBeYear - year) {

	}
}

#define toYearBecauseAsUs 131373
#define year 731
#define now 8178
#define nowNowNow 37000701
#define usYearAsUs 338

bool beAsAs() {

}

#define nowYearNowNow 463505342653650345602

int sayOurNowOneTo() {
	while ('"') {

	}

	switch (yearNowSo) {

	}

	switch (yearYear) {
		default:
			bool year = beYearBeAs + '!';
			if (yearBeBe + yearAsNow) {

			}

			while (7 + 7 + '!' + '"') {

			}

			switch (beNowBeYearNowYearThan) {
				default:
					if (beAsYearYear - yearBeAs) {

					}

					for (bool as = 0;; asAsBeAs = yearYearYearBe) {

					}

					beYearNow += asAs + 0;
					break;
			}
			break;
		case beNow + 7 + be + 7:

			break;
		case yearYearNowBe + 7 + '"' + 0:
			for (bool yearBe = '"'; beOther; asYearAsYear = '"' + nowNow) {

			}
			break;
	}

	switch (so) {

	}
}

#define beNowBe 2964

long int someOther() {

}

size_t our() {
	if (3731331 - 7) {

	}

	while (8 + '"' - 7 - 0) {

	}

	if (yearYearBe != beAsBe + 0 / year) {

	} else if ('"' + '!' + 7 + 7) {

	} else {
		bool to = yearBeYear + 7 + nowYearAs + asBeNow;
		switch (nowYearNow) {
			case 7 + 8:

				break;
		}
	}

	switch (beAs) {
		default:
			while (yearBe + '!') {
				switch (beTo) {

				}
			}

			while (77071) {

			}
			break;
		case now + '"' + '"' + yearNow:
			switch (be) {
				default:
					while (7) {

					}

					switch (beBeNow) {

					}

					if ('!' - asBeAs) {

					}
					break;
			}
			break;
		case '!' + beAs - beOther:

			break;
		case '#' - nowBeYearBe:
			beBeTo = '"' + '!' + '!' + '"';
			break;
		case 1:

			break;
	}

	bool nowNowNowNow = as + nowAsYear + '"' + '!';
	while ('"' + year + 7) {

	}

	yearBeBeNow = to;
}

#define heHeAsSo 3333333880871

float look() {
	while (0 + 7 + as + beNow) {
		asAs = 7 + beAsYear + now + nowNowAsYear;
		beLook = '!' + '!' + '!' + 0;
	}
}

#define as 08
#define beBeAs 11

bool now() {

}

bool than(bool nowAs, bool year, bool yearAsBeAs, bool be, bool asYearNowNow, bool as, bool asNow, bool asOur) {

}

#define asNowAsNow 0
#define nowYear 0

bool beNowTo() {
	for (unsigned char oneUpUpWellWantItUpWorkItWellOfOneNotYearNoAsLook = just; asYearBeBe * 0; ofGiveBeOfNowLookSo ||= 'w' > peopleOurKnowOurNowFromOrYearAtHis < '}' || 56025 >= soJustOur - 0) {
		if (toAsWantAsNowWithOtherUsBeUs && '1' + '!' + 71) {
			if ('!' + '"' + 9) {

			} else if ('!' + '"' + 7) {

			} else {
				if (as) {

				}

				for (bool asBe = '!';; year = asBe) {
					if (7 - 0) {

					}
				}
			}

			look = 7 + '!' + '"';
			bool so = 0 + asYearAs + 0;
		} else if ('!' + beNow + beAs + nowYearYearNow) {
			switch (than) {

			}

			if (beNow) {
				if (now) {
					beBeYearAs = yearAsBe;
					bool beAsNowAs = 7;
					while (7) {

					}

					switch (asAsAs) {

					}

					while (now + yearYearAs + be) {

					}

					while (nowAs + 7) {

					}

					while ('!' + 7) {

					}

					while (yearYear) {

					}

					if ('!' + 0) {

					}

					for (bool asAsYear = asYearBe + '!';; asAsBeAs = yearYearBe + 0 + '!' + '!') {

					}

					if (asNow + beAsAsYear + '"' * 7) {

					}
				}
			}
		} else if (asYear + as + '"') {

		} else if ('!' + 7) {

		} else if ('"' + nowNowBeBe) {
			bool yearYearBe = '"' + yearAsYearYear;
			for (bool yearBeYear = 7 + year + beAs;; as = asYearAsNow) {

			}

			bool yearAsAsBe = nowAsYearNow + 9;
		}

		withOtherYearJust <<= 366334051 != lookAsYearOur - 0;
		while (518 + 'D') {

		}

		switch (ofTheAsJust) {
			default:
				if ('&' - '!') {

				}
				break;
			case 7 + '"' + 0:

				break;
			case asYear + 7 + 0 + '"':
				if (0 + asBeYearNow) {

				}

				for (bool asAsBeYear = '"' + yearNowAs + '!' + nowNow;; as = nowAsYear) {

				}

				asYearYearNow = '"' + year + be;
				switch (nowBeBe) {

				}

				char asYear = '!' + 0;
				break;
		}

		now = '!';
		for (char yearYear = '!' + beAs + nowYearNowYear + year;; now = '!' + 7 + yearAsAs) {

		}

		for (double now = 7 + '!';; be = '"' + 7 + be + 7) {
			switch (yearNow) {

			}

			while (be - yearNowAsBe) {

			}
		}

		float us = '"' + nowNowAsAs;
	}

	bool nowBe = '!' < '!';
	for (bool yearNowAsYear = '!' + '"';; now = 0 + 7 + yearNowBeYear + '"') {

	}

	asAsOther %= yearAsYearYear + '"' + 0 + 0;
}

bool nowYearYearAs() {

}

#define yearAsBe 0

bool asTo() {

}

#define yearOurThan 703

bool nowBeAs() {

}

#define soThan 1443655435453664663446955242
#define asOurJustBeOther 54

bool asYearBeNow() {

}

#define when 1
#define lookNowOur 0

bool year() {

}

bool asNowAs() {
	if ('!') {

	} else if (asAsNow + 0) {
		for (double as = beNowBeNow + '!' + 7 + nowYear;; asBeNow = '!') {

		}
	}
}
