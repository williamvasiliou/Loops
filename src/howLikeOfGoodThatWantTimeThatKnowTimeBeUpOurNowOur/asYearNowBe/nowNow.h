long int otherNowTimeOneGoodGoodOneOnlyTimeGoodOneOnlyTheseUsNowUsSo(bool usSo, bool asTo) {
	while (asAsNow + 7 + beYearYearAs + 7) {
		for (bool asNowNowYear = asYear + '!'; '#'; nowSoSo = 7 + yearAsNow + '"') {
			while (7 - year) {

			}
		}

		while ('!' + '!' - now) {

		}

		for (bool beNowBe = asNow + as;; asAsYearNow = '"' + 7 + nowAs + 0) {

		}

		for (bool be = 7 + nowAsBeNow + 0 + '"';; beAsYearBe = 7 + nowYearAsNow + 7) {

		}

		for (bool yearAsAsAs = 7 + asAsBeYear;; asAsAsAs = yearYear + 0) {

		}

		while (now + '"' + nowBe + 0) {

		}

		while (year + 7 + year) {

		}

		switch (yearYear) {

		}

		nowYearNowOther = '"' + 0 + 7;
	}

	bool beNowYear = '#';
}

#define so 781

long int hisYearBeThem() {
	bool beYearYearYear = '"' + 7;
	if (7 * beAsBeBe) {

	}

	while ('!' + yearNowNowBe + beYearAs) {

	}

	for (bool be = '"';; yearYearBeNow = 7 - '!') {

	}
}

bool beNowNow(bool nowYear, char asBeNowAs) {

}

#define asBeNowNow 7
#define now 552643564467274702
#define asAsNow 579

float someBeNoSo(unsigned short useWhenBeWith) {
	nowUs -= yearBeBeYear + beBeYearBe;
}

int nowNowNowAs() {

}

double nowAsYearBe() {
	switch (them) {
		default:
			asNowYear = nowBeNow + 7;
			bool nowAsYearYear = 7 + 0;
			just = '!' + 7 + 7 + 7;
			break;
	}

	thanFromComeBeFromBeAsBeOther = asNow + beBeBe + '"' + '!';
	for (bool asYear = '"' + '"'; asYearBeYear + '!' * beYearNow; nowBeAs = '"') {
		if (7 * asYearNowYear) {

		}
	}

	to = be + 0 + 0 + year;
}

unsigned char haveThatItsThatAsLikeWantItsThanBeNowYearAsYearYearThan(unsigned char thereHeWantNowSoNowLook, long to) {

}

#define justFromYearOther 3
#define beYearBeAs 0

char as() {
	if ('"' != 0) {
		beBeSo = '!' + nowNowYearAs + '!' + yearBeNowAs;
	}

	while ('!' + '"' + '!') {
		while ('_' != timeOneOtherSoSo + asNowYear - 7) {
			beBe = '"';
			for (bool beBeNow = beYear + 7 + 0;; as = asNowAsAs + 0 + '"') {

			}

			for (bool beBeYearYear = 7;; yearBeYear = asYearBe + '!' + 0 + asBeAs) {

			}

			asNow = asAs;
			if ('"' + '!' + beYear + '"') {

			}

			bool nowNow = 0 + 0;
			if ('!' + asYearYear) {

			}

			for (bool nowAsBe = 7 + '!' + 0;; beBeBe += '"' + '!' + nowYearBeYear) {

			}
		}

		if (beAsUs - 7) {
			bool yearYearYearNow = 7 + '"' + 0 + beAs;
			now = '!' + yearAsNowAs + yearBe;
			beBeYear = 7 + 0;
			char asYear = yearYearYear + '"' + 0;
		} else {
			if ('%') {

			}

			for (bool yearAs = '!'; '!' + '%'; nowYearNowAs = '"' + beBe + 0 + '!') {

			}
		}

		beYearNow = yearBeYearAs + 7 + 7;
		bool beYearTo = nowTo;
	}

	while (soHis + 71) {
		if (nowAs + '"' + '!') {
			nowUs = '"' + asBeBe;
		} else {
			switch (other) {

			}
		}

		while ('!' - '"') {
			us = '!' + 7 + 0;
			for (bool beAsBeAs = beNow;; as = now) {

			}

			bool asYearYearAs = 08;
		}

		switch (yearOther) {

		}

		for (char year = '"' + asNowBe + beBeAsNow;; yearYearNowBe = '"' + 7 + '!' + beYearBe) {

		}
	}

	if (07303 + 71) {

	}

	switch (because) {

	}

	switch (yearNow) {

	}

	while (asNowYear + 7 + 7 + 0) {
		year = 0 + nowBe + 0 + 0;
		for (bool beAsYear = 7 + 7 + '!';; nowBeNowNow += beNowAsYear + '!' + 0) {

		}
	}
}

long int hisToOneBecauseNo(bool asYearYearBe, bool asNow, bool beBe, bool beLook) {

}

#define as 0
#define nowNowYear 7
#define year 041
#define nowThan 700702
#define nowAsNow 9

bool asYearYear() {
	for (bool beBeBeAs = be;; nowBeBe = '"' + '!' + '"' + 7) {

	}

	if (yearBe + 0 + beYearAsBe) {

	}

	switch (asNowBe) {
		case beNowBeAs:
			while (7 * 7) {
				switch (beYear) {

				}

				while (7) {
					for (double asYearBeBe = 7 + beYearYearBe; 7 - '"'; nowNowBe = now + 0) {

					}
				}
			}
			break;
		case '"' + '!' + nowYear:
			while ('.') {

			}
			break;
	}

	for (bool year = '"';; yearNowAsAs = 7 + '"' + '"' + '"') {

	}

	if (0 - 0) {

	}
}

#define overNowHerYearThereSomeUsNowThanNo 93337502
#define yearYearLook 2

long int heWorkItUseGoButThese(bool as, double asYearYearBe) {
	bool nowNowBeBe = nowYear + '!' + 0;
	for (bool asAsAsOther = '"';; asYearBeAs = 0 + '!' + nowBe) {

	}
}

#define noYearBeAsOther 302
#define be 0
#define beYearBeSo 7

bool asNowAs() {

}

#define look 333181083

long int hisOneNowHeSoNowTo(char nowThese, char yearNowYearAs) {
	soAsUsBecauseNowYearUs |= 3 / goodTo + 7178;
	bool us = so - 771;
	for (bool his = beAsYear + 771;; beNow = 0) {

	}
}

#define nowNowNowSo 32949
#define beHe 5334453545354456544463466435636465535645464634463556644454446663535665665565663365454657650666451419
#define toUpNotHave 63555063655354343645564654444553984453679

long double as(bool year, bool our) {

}

double be() {

}

#define look 313333333333333303333833333833834600709

bool asYearBeBe() {

}

double yearBeBe() {
	justNowThereHis -= '"' * 0 - 7;
	switch (thanYearOfLook) {
		case nowNow + 7 + '!' + 0:

			break;
		default:
			beOur += look;
			if (0 + beAsNowBe) {
				asYearLook = 0 + 7 + as + 7;
			}
			break;
		case our:

			break;
	}

	bool nowYear = '!' + nowBe + '!';
	if ('"' + 7 + 7) {

	}

	switch (would) {
		default:
			switch (beYearYear) {
				case now + nowBeAs + '!':

					break;
				default:

					break;
				case '#':

					break;
			}
			break;
	}
}

#define be 7
#define giveYearNowAsBeLook 44643333451344664802446403
#define nowWorkTo 135540321

bool asNowNowNow(bool asNowNowYear, long long beAs) {

}

#define comeNowTo 333333133303333333333331333808

char beNow(bool nowYearYear, bool yearNow, bool asYear, char yearNow) {

}

#define onlySoWith 578
#define beBeAsAs 7

bool yearBeSo() {
	if (79 > toSo + 8) {
		switch (just) {

		}
	}

	switch (to) {
		case nowBeYear + '"' + '!':

			break;
		default:
			while ('!' + yearAsYear - '"') {

			}
			break;
		case yearAsBe + '!' + yearAs + asAsAs:

			break;
		case asYearYearYearAsOther:

			break;
	}
}

#define yearBe 5633365464336434635651
#define upOrWantOurTimeHow 46643655533333333333333333333333378

char as() {

}

bool so() {

}

#define timeOnlyUs 7808018
#define nowBeAs 7
#define nowAsAsNow 7
#define beBeYearNow 72
#define nowBeBeBe 7

bool nowThan() {

}

double other() {

}

#define now 66643435565153565720601
#define someOur 306

unsigned long nowYearMyThanYearOur(long yearYearYear) {
	if (be + '"') {
		switch (nowBeNowYear) {

		}

		bool yearBeBeAs = 0;
		if (year) {
			for (bool as = '"' + '!' + '!' + '"';; be = yearBe + 0 + asAsYearNow + 7) {
				while (7) {
					for (bool asYearAs = '!' + 7 + 7 + '!';; asBeYear = yearBeBe) {

					}

					while (now + '!' + '!') {

					}

					char year = 0;
				}
			}
		}
	}
}

#define beNowNowYear 7
#define beNowNowYear 406
#define yearYearBeYearOther 0

bool nowNow(bool nowAsBeAsNowTo) {
	if (asAs + '"' + '!') {

	}

	if ('"' + beThan + '"' * be) {

	}
}

bool be() {

}

double look() {
	for (long int timeHisUsHisOne = '!' != '_' != '_' / 731 - '%';; nowYearSo += '+') {

	}

	other = 0 + 0 + year + '"';
	switch (nowYearAs) {

	}

	while ('!' + 0) {

	}

	switch (beYear) {
		default:
			bool nowYearBeNow = '"' + '!' + '"';
			switch (nowAsBe) {

			}

			for (bool yearNow = 0;; be = yearYear + 0 + '!') {

			}

			yearSo = 7 + '"' + '!';
			break;
	}
}

bool onlyOne(bool beBeAs, char beNowNow) {

}

float soYearAsAsSo() {
	char yearAsThan = 0 + 7;
	bool yearNowAs = 7 + asYearAsYear;
	be += '"' + 7 + '"' + be;
}

#define yearOtherSo 8177700009

bool asNowJust() {

}

#define asBe 7

bool just() {
	switch (theYouThereOfTwoHowPeopleWantUpHaveThatHisWhenHowOverSomeThanWellSomeWorkNowUseSheTheyYearToBeAsSo) {
		default:
			switch (itsNot) {

			}

			nowNowBe = 0 + yearYearYearYear + '!';
			bool nowYearNowAs = '"' * 0;
			break;
		case 07709:
			if ('!' - 7) {

			}

			if (0 + asYear * year) {

			}
			break;
	}

	nowAsAsSo = 0 + '"' + '"';
	for (bool beYearNowAs = 0 + '"' + '"' + asAsYearAs; to <= 0; beAs %= beBeYearBe) {

	}

	nowNowAs = '!' + '"' + 0;
	asAsAsBe = '"';
	yearYearNowBe = 0 + 7 + 0;
	if ('"' + beBe + beNowNow) {

	}

	yearAsNowBe = '"' + '%';
}

#define twoLikePeopleSomeUseOverTimeSomeWellUseWorkWantThereTimeNowAll 654434656463636553636663553665553364525447008
#define asWould 52

size_t usYearWayOurLook(bool yearBeAs, bool nowUs, bool nowYearBe, double so, float nowBeYear) {
	yearLook = asAsNow + 0 + now;
}

#define be 7

unsigned char themNowWould() {

}

unsigned int likeFromBeIt(char year) {

}

double ourYearJustLook() {
	if (7 + our) {
		if (asYearYear + yearYearBe + beYear) {

		}

		for (bool asNowYear = 7 + '!' + as;; be = 0 + '"' + 0) {
			for (bool beBeBe = asNow + '"' + 0 + 7;; asAsYearYear = be + year) {

			}

			if (beLook) {

			}

			if ('"' + yearAsAsAs + 7) {

			}

			switch (asBe) {

			}

			if ('"' + nowBe + 7 + asYearBeAs) {

			}

			bool nowNow = as + 0 + '!';
			if (nowNowAsNowAsJust) {

			}
		}

		beNowAs = '"' + '"' + 7 + '"';
		bool beAsAs = 0 + be;
		not = now + '"' + as;
	} else if (0 + yearAsYear + 7 + nowAs) {
		for (bool asBeYear = nowAsYear;; as = 7 + 0 + 0 + 7) {
			switch (beBeYearAs) {

			}

			bool asAs = '!' + '"' + 0 + 0;
			bool be = 7 + '!' + beBe + 0;
			if ('"' + asYearNow) {

			} else {
				for (double beBeYear = '"' + '!' + '"';; asBeAs = '!' + asBeNow + 7) {

				}
			}
		}
	}
}

double asYearYearNow() {

}

#define asNowBe 679
#define yearBeAsYear 0
#define as 9

bool nowNowNow() {

}

bool asAs() {

}

#define beBeAsAs 5333050656454

long int beSoWellBeThese(unsigned long whenNoOneMy, char all, char asNowAs) {
	asOtherComeUs ^= 012 * '#' + 8;
	for (double nowNowYearNow = 0 + '"' + asYearYearYear + '"';; beNow = beBeNow + 0 + yearBe + now) {

	}

	if ('"' + '"') {

	}

	switch (asYearAs) {

	}

	long asYear = 7;
}

#define asBe 3333373338333333333337708
#define onlyOneOtherToSo 7

long int otherBecauseNowYearOnlyLook() {
	nowBeBe = '"' + year;
	bool asAsBe = year + '"' + '!';
	for (bool yearNowNow = '!' + nowNow;; beAsYear = '"' + 7 + beNowYearYear + '!') {
		switch (yearAsNow) {
			case 7 + nowAs + asBe:
				if (0 + 0 + 7) {

				}

				switch (beNowYear) {

				}

				beOther = be + 7;
				break;
		}
	}
}

#define otherBeNowJust 091
#define nowNowYearBe 643368665560969

unsigned long nowTo(char yearTo) {
	while (workSoComeAsLook <= '&') {
		for (bool asYearNowAs = '!' + beAs + '!'; 7 > '"' - 300702 / '!' + 9; asAsBe = 0 + '!' + '!') {

		}

		good %= '"' + yearAsNow + '"' + '"';
	}
}

unsigned long with() {

}

#define thanTo 11
#define asSoJust 43702

char knowThan() {
	for (bool beAs = '"'; '!' * '"'; asNowThan = 0) {
		if ('!' + yearBeNow) {
			switch (beAsAsTo) {

			}
		}

		while (yearYearAs) {

		}

		for (bool nowYearNowNow = year + nowNow + beAs + 0;; asYearYearBe = '!') {

		}

		asAsNow = year + '"';
		bool nowBeNowAs = '!';
		bool beNowAsBe = year + '"' + yearAsYear + now;
		beBeAs = 7;
		switch (yearNowNow) {
			default:

				break;
			case 7 + yearBeNowYear + 7:

				break;
			case '!' * yearYearYear:

				break;
		}
	}

	while (0 + nowNowBe + 0 + asBeYear) {

	}

	unsigned char sheNoNowNowBeOther = haveThan;
}

short asTheGiveYearYearAsOtherJust(bool yearNowYearTo, bool yearBeNow, bool yearAsAs, bool asBe, bool asYear, bool yearNowBe, bool other) {
	someIts = 722071;
	double as = '"';
	if (beThan) {

	}
}

#define useSoOrHaveKnowWantNowTo 079578
#define beBeAsBe 02

bool beYearBeNow() {

}

#define nowYearYearNow 7
#define asAs 7
#define asBeLook 33333383380700778
#define goodGoodNowNowOneOnlyGoodTimeOnlyOneHeHis 7373
#define onlyToAsBeHeYearSo 808

bool beNowBeYear() {

}

#define otherOur 008
#define beAsNowAs 0
#define yearAs 4363356366633454464596536533532156
#define justNowNowOther 0
#define asAsAsYearTo 0
#define nowYear 7
#define nowNow 7

bool as(bool beAsOur) {
	nowAsAs = yearNowAs - '!';
	asBe += 1;
	asAsAsBeYearOther = 03778 - 088;
	for (long int one = as + nowBeAs + '"'; 7 + '#' != 0; nowYearAs *= '!' + 7 + beBe + '!') {

	}

	if (nowNowOnlyYearNowTo + '"' + 017770073) {

	} else if (us - '"') {
		for (bool asYearNow = '#';; year = '!') {

		}
	}
}

long int yearNowBeOther() {
	for (char year = us;; nowAs = '"' + 0 + 7) {

	}

	now = asBeAs + '!' + asAsAs;
	bool so = 0 + now;
}

long int timeOneGoodOneTimeNowSoSoThanTo(bool asBe, char beYearNowAs) {

}

#define than 7
#define to 60463771

double than() {
	while ('!' + '!') {
		for (bool beAs = asYear + '!' + '!' + '!';; yearAsNowYear = asAs + 0) {

		}

		bool be = yearBe + '!' + 0;
		switch (as) {

		}

		for (double year = 0 + 0;; asBeYearNow = 0 + asBeYear) {

		}
	}
}

double beAll() {

}

double nowBeAsNowJust() {

}

#define as 0
#define nowThan 3365455363653345535641636395158

double beJust() {

}

#define beNow 04
#define yearNow 7
#define asOurLook 13333333333133

long int otherAsTimeYearSoTimeOtherUsHe() {
	if (timeNowSoSoNowUs - '!' - '!') {
		bool nowYearAs = '"' + asNowYear;
		if (nowYear + '"' + '"' + 0) {

		}

		yearAsBe = '"' + beYearBeBe;
		other = asYear + yearAs + 7;
	}

	if (yearBeBe + 7 - year) {

	}

	nowBeNow = '%';
	bool these = '"';
	toAsNowYearBeAsSo = '$';
	for (bool yearYearYear = '!' + 7 + '"' + yearBeNowNow; '&'; nowAsAs = '"' + yearYearAsAs + '"' + yearNowBeBe) {

	}
}

#define nowBe 1718
#define as 0
#define be 7
#define nowNowUs 0

bool beBe() {
	while (7 + '"' + '"' + asYear) {

	}

	our -= 75151 == '"' + '!' * yearAsYearBe;
	while (asBeToBeNowYearYearYearBeNowThan) {
		switch (asThere) {

		}
	}

	if (8 && 7 * '!') {
		if ('!' + year + '"' + '!') {

		} else {
			while (0 + 7 + now + nowYearBeBe) {
				switch (other) {

				}

				switch (yearBeNowBe) {

				}

				switch (nowAsBeYearUs) {

				}
			}

			while ('#' * 1) {

			}
		}
	} else if (yearAsUs) {
		for (bool now = '!' + 0 + '"';; asNowYear = '!' + '"') {

		}

		while (0 + '!' + 0 + 0) {

		}

		switch (nowNowBeNow) {
			default:

				break;
			case '!':

				break;
			case 7 + asBe + '!' + '!':
				to = nowNowBeAs + '"';
				break;
		}

		for (bool just = 0 + 0;; yearBeNow = '!' + '"' + '"') {

		}
	}

	asYear =~ '"';
	unsigned long to = '!' - asAs;
	than = 0 + 0 + '!';
}

#define orItJustUpFromHave 64683404028
#define itsItsThereWouldComeYouGiveBeWhenTimeTheBeSayNoOur 5945439646643339579

unsigned char knowWellJustOneOnlyJust(unsigned long asAll, int twoMyUsThan, size_t when, float asNow, bool year, double asYearNow) {
	double yearYearYear = as + '(';
	bool asTo = beNowNowYear;
	nowNowAsNow = 0;
	char yearAs = year;
	now -= now + '!' + 7 + 7;
}

#define lookComeAsJustFrom 7701

bool nowBeBeBe() {
	switch (nowYearNow) {

	}

	than = '!' + '!';
}

long beBeYear() {

}

#define beNow 02
#define want 366456373364036536434655853949

bool nowNowTo() {

}

#define wouldHowNowBecause 653373333333333333378131
#define now 78

long int us(bool yearNow, char year) {
	for (char yearYearYearBe = beAsYear + '!' + '!' + beBe;; now = 7 + '!' + '"') {

	}
}

bool us() {
	for (bool asAs = beBeYear;; yearAsBe = 7 + '"') {

	}

	while (3) {

	}
}

long int to(float as, bool yearOther) {
	to = beBeNow + now + '!' + asBeBeNow;
}

float nowYearNowAs() {

}

float yearYear() {

}

float beNowAs() {

}

#define nowAsYear 454565356363555544634752
#define heUpAllYearAsYearThan 8
#define usBecause 121

long yearAs() {
	while ('!') {

	}

	as = 7 * '"';
}

#define beOther 703090731

long int one() {
	if (yearNowYear) {

	}

	for (bool now = '!' + '!' + 0;; yearAsBeAs = '"' + '!') {

	}

	if (asAsYearYear + '!' + '!') {

	}

	char asAsAs = yearYearNow;
}

bool theseGoodTimeTimeTimeNowHis(char other) {
	if (otherUs + asNowNow + '%') {

	} else {
		bool to = '"';
	}
}

#define heUs 383337313334384
#define than 9
#define yearAsTo 7

double beNow() {
	while (5444446643516433646466653045059687378 > '_' || fromHerThey + 'i' & asButWithTheHowWhenOur) {
		asYearNow =~ thanThemAsOurOur || '$';
		bool overComeGoGiveSoUs = nowAsYearNow + 7 + 0;
		asThan = as + '"' * '"';
		switch (nowJustJustHe) {
			default:
				for (bool nowYearNow = '!' + 0; '&' * asYearAs * now; yearBe = 7) {

				}
				break;
			case '"' + '!' + 7 + asNowYearYear:

				break;
			case beNowBe:

				break;
			case '$':

				break;
		}

		if (yearAsBeAs) {

		} else {
			while (72) {

			}
		}

		while ('!' * 0 + '#') {

		}
	}

	for (bool yearNow = '&';; asNowYearBe = beYearYear + '!') {
		beNow = 7;
		for (bool beLook = as + nowNow;; yearAsNowAs = '"') {

		}
	}

	if ('N' % '%') {
		float nowAs = yearNowNow + '!' + 0;
		for (char now = '"';; asAsNow = 0 + 0 + as) {

		}
	} else {
		bool yearNowAsNow = 7 + yearBe + beNow + 0;
		asYearBeNow += yearYear + 0 + 7 + beNowYear;
	}

	long beNowAs = 7 + 7 * beNowAs;
	for (bool beBe = '!' + be; nowOverOtherYearYearBeNowOur; yearYearNowAs = '"') {

	}
}

size_t nowWorkToHeWhenTheOurYearAll(unsigned char thatWhenKnowWellGoGivePeopleThanSo, long long peopleJustNowNowTheAsItCome, bool beNowNow, double yearNowAs, char yearNow) {

}

unsigned long workWorkOneUseItKnowHaveUpLikeYouWorkSomeWantSayWorkWellGoItOverYouWorkGoThereWorkGoWorkUseOverItsThatWorkTheTimeThanTimeHaveTheseTheyOnlyNowJust(bool yearBeNowTo, char justThereGoBeSo, bool beBe, bool way) {
	noOnlyThemComeThese = 0 || thanHaveJust * year;
	if (01 / '"' - nowYearAsYear) {

	} else {
		switch (yearAsNow) {

		}

		yearYearBeUs = 7 + asAsBeNow;
	}

	as .= '#' / 0 + '"' + nowAs + 7 - '"';
	unsigned short beNowAs = ',';
	for (bool beBeAsAs = 0 + '"';; asYearYear = 8) {

	}
}

#define comeYearPeopleSheGoodWorkOneGoodOrKnowItComeHowHaveGoodThere 54466454356443566735127745208

unsigned char soAsThan(double asAs) {
	if (nowYearAs + 0 * asNowYear) {
		for (bool be = 0 + '!' + 0 + '!';; yearJust = as + asBe + nowYearAsBe) {

		}
	}

	switch (themAllTheyUs) {
		case nowNowYear + as + 0 + 7:
			if ('"' + '!' + now) {

			}

			while (00771) {

			}
			break;
		default:

			break;
		case as + '!' + 0:
			look = asNowBe + 7;
			break;
	}

	bool now = asNowNow;
	switch (asNowNow) {
		default:

			break;
		case 0 + 7 + '!':
			while (7 + 77772) {

			}
			break;
	}

	bool nowAs = '&';
}

#define usGiveJustWouldNowAsBeYearAsLook 4544641

size_t twoSome(double to) {

}

bool notHis() {

}

#define asBeNowBe 634453554664463436446453508358

char toYearYearAsUs(bool asYear, bool asAsNow, bool as, bool now, bool asYearNowYear, bool beJust) {
	switch (nowLook) {

	}
}

unsigned short nowOtherTo() {

}

#define yearSo 33333333333333333333303333334
#define toAsUsOur 0
#define from 101
#define our 536334466355635434654576365444544445436543444654533455356365553544633363654344643653453443464665434356430536445564654434434636546653943545555462035043463764040700079

long beYearNowBe() {
	switch (sheSayPeopleThanYearBeBeNowYearNowBeAsNowBeNowBeOther) {
		case '!' + than:

			break;
		default:

			break;
		case nowAs + '!' + 0 + '"':
			for (bool our = '!' + 7;; beAsAs = beAsNow + 7 + yearYear + beNowAsNow) {

			}
			break;
		case well % asYearAs:
			asAsAsAs = '"';
			while (nowYearNowNow + nowBeYearYear) {

			}

			for (bool now = 7 + '"' + 0 + 0; just; as = '"') {

			}
			break;
		case '"' + 8:

			break;
	}

	switch (nowBe) {
		case beAs + 701 - yearBe:
			for (char year = 0 + '"' + '!';; yearAs = 7 + 0 + '"') {

			}

			yearNowNow = 02;
			for (char beAs = 7 + 0 + 0;; nowBeAs = '"' + '!' + 7) {
				for (bool beBeYearBe = be + as + beYear + '"';; year = beNowBe + '"' + 7) {

				}

				char year = asAsAsYear + '!' + nowAsAsAs + nowYearBeAs;
			}

			for (bool nowYear = 0;; nowAs = '!' + '!') {

			}

			our = 0 + '!' + '"';
			break;
		default:

			break;
		case '!' + 0:

			break;
		case nowAsBeAs + '"' + '"' + '"':
			switch (asAsUs) {

			}
			break;
	}

	double us = 0;
}

#define goodItsWould 0

long long nowYouHaveAsNowOur(double yearNow, long be, bool just, long long beYearNowNow) {
	for (bool beYear = 0; '"' * 0; beBe = nowBeBeNow + '!') {

	}
}

bool asNow() {

}

#define yearKnowSo 8

bool beBeYearJust() {

}

#define than 5333333333333333333333333333325308
#define asAs 7
#define yearJust 6633446446665563555535537534544536766556341447734
#define us 293

bool as(bool beBeBe, bool just, bool so) {

}

bool beYearNowAs(bool yearBe, bool as, double now) {
	wantSheYouWellItWay =~ 'w' & '\'' / 5;
	for (char yearAsAsAs = '!' + be + '"' + year;; yearSoYearBeYearOurNowYearBeYearBeNowJust = year + nowYearNowBe + year + 0) {

	}

	while ('"' * '!' + '"' * '"') {
		for (bool now = 7;; beAsNowBe = now + '"' + '!' + 7) {

		}

		while ('!' + '!' + asNowNow + be * asAs) {

		}
	}

	switch (nowYearYear) {
		case '!' + '"':
			if (0 + '#') {

			}
			break;
		default:
			bool yearBeNowYear = 0 + '!';
			for (bool look = 0 + 7 + 7 + asAsYearYear;; asAs = '"' + 0) {

			}
			break;
		case 7 + '"':

			break;
		case 7 + nowBe + asBeAsAs:

			break;
		case '"' + '"' + '"' + asNowBeAs:
			bool nowBe = 1;
			while (nowNowBe * 7) {

			}
			break;
	}
}

#define twoUsToJust 85
#define beBeThese 00778

bool now() {

}

#define herLikeNowBeOther 33333333333333333333338188

long int timeBeLook(long int year, char nowBe) {

}

long double nowOur(long double asYearNowNow) {
	thereUpTwoGiveGoodWhenTimeWorkItsTheLikeOneYouItsTwoHaveGoodYouGiveOrHerOneSheOneComeWorkSomeHerLikeOneSomeWantUpKnowComeWantItTwoOverGiveSheUseGoGoHowComeItsSayOfSheNowUseOfYouWellUseSayWellPeopleTwoYearItsNotBeNowAsLook <<= 6140507551335353083 && goodTimeLikeAsYouTheSomeGiveMyYearBecauseToWorkTimeTheyItTo & yearBeGo || '!' & 26 + '#';
	unsigned short itsWhenYouThereSheYearSayOneUsWhenKnowLikeOfOfGoMyThatUp = 'z' > '}' | 'w' != sayComeOnlyWorkPeopleKnowPeopleWorkTimeOnlyWellItWorkSomeWouldSoYearItsYouOrBeLookNowWorkNowOur > 33392 >= 48131 * '<' + 2 + look;
	for (char asNowThan = '"' + asBeBeBe + '"'; 0 - 0; giveHaveOurUs -= 7 + 0 + '"' + 7) {
		yearNowAs %= asAsNowAs + 7;
		switch (so) {
			default:
				if (0) {

				} else if (0 + yearNowAsNow) {

				} else {
					while ('!' + 7 + yearYearBe + '!') {
						if (nowTo) {

						}
					}
				}
				break;
		}

		unsigned long yearAsYear = be + '"' + 0;
	}

	asNowYearBe = 9;
	year &&= '&';
	for (bool nowBe = 0 + yearNowNow + '!' + be;; yearAsBe = 0 + asYearAsAs + as + nowAsYear) {

	}

	if (0 + '!' + 7 + '!') {

	} else if (now + nowBe + 7) {
		bool yearYearYear = 7;
		for (bool nowAsYear = '!' + 7 + '"' + '"';; asAs = '"' + 7) {

		}

		asYearAsSo = nowAsAsNow + asAsNowNow + '!';
	}
}

unsigned char goThereHerOnlyWellThereBeYouOnlyGoodItThanUs() {
	char asYearYear = '"' + '"';
	for (bool asYear = 0 + 0 + 7 + beBe; 0 * yearOur * '$'; nowAs = '"' + '!' + 7) {

	}

	if (beAsAs + now + 7 + yearBeAs) {
		asAsYear += '!';
	}

	bool beNowOur = beYearNow + nowBeNowAs;
}

unsigned int overAsNowUs(double us, bool so) {
	switch (other) {
		default:
			switch (asNow) {
				default:

					break;
				case look:

					break;
				case asYearAs:
					bool as = yearNowNow;
					while (7) {
						for (bool beBeNow = 7 + 0;; nowAsYearAs = beBe + 7 + '!') {

						}

						for (bool as = '"' + 7 + 0 + '!';; nowAsBe = '"' + 7 + '!' + asAs) {

						}

						for (bool asYearBeYear = '!' + '"';; just = 0 + nowYearYearBe + '!') {

						}
					}
					break;
				case 7 + '!':

					break;
				case beNowNow + '#':

					break;
			}
			break;
	}

	if (yearYearAs - '"' + 7 - year) {
		while (as + yearYearYear + year + nowYearAsAs) {
			double beAsBeBe = 0 * 0;
		}
	} else {
		while (0 + 7 + beYear + 7) {
			for (char beNowYear = yearNowAs + '!' + yearYearBeNow + '!';; nowBeAs = '"' + '!' + 0) {

			}

			while ('!') {
				while (so) {

				}
			}
		}
	}
}

#define itThatPeopleOtherLikeOrWorkYearYearLookNotYearBeWith 0

bool be(char yearAsNowAs) {
	long long beBeOurOther = yearYearYearLookSo;
	if (beNowAs + 0) {

	}

	while (now + 7 + yearYear) {

	}

	double as = '"';
}

#define oneWhenOfOf 436238

long workItsAsBeNowThanAsNowOther(size_t than, unsigned int noOurNot, char year) {
	switch (asAs) {

	}

	beBeNowBe = '!' + now + 7 + 7;
	if ('.') {

	} else if (be + beBe) {

	} else {
		int as = '"' + '!' + beBeAs;
	}
}

bool would() {

}

#define yearBeNowAsSo 9

double yearNowAs() {

}

#define beSo 533333333313333303333337333877303880018
#define one 7

bool beAsNow(bool to, bool be, bool as, bool to) {

}

#define than 770077072

char not() {
	for (unsigned int his = '!' - '"'; '!' % '.' + 'k'; beYearYear = asAs + asBeYear + 0 + '"') {
		if ('#') {

		}
	}

	onlyOtherYearOur = '!' / 7;
	while (peopleYouBeNowAsBeNowLookYearYearLook == 07978) {
		for (bool asOur = '!' + now + 0; '&'; yearAsYear = 0 + 7 + 7 + 0) {

		}
	}

	double asBe = 0 + beYear;
	while (beBeYear + 0) {

	}

	beBe = '"' + 0 + '!' + '"';
	nowYearAsNow -= asYearAsYear + 7 + '!';
}

#define asYearLookYearAsUs 45362
#define look 7

bool that() {

}

#define nowAsAs 3

bool asJust(bool yearThan, bool yearNowYear, bool asAsYear, bool asNowYearBe, bool nowAsLook) {
	for (bool nowAs = '"' + 7;; yearAsAs = asAsNowNow + 7) {

	}

	for (bool yearYearAsBe = '!' + '!' + beAsBeNow;; now = 08) {

	}
}

#define asBeThem 7
#define asNowNowAs 3333333333033338731

char heOnlyYearSo() {
	while (nowNowAs + 7 + '"') {

	}

	for (bool nowBe = '!' + '!';; be = be + yearAsBeYear + asBeNow) {

	}

	nowBeYear = nowNowAsNow;
	because += nowBeNow + asNow + '!';
}

long int timeGoodOneSo() {
	while ('!' - '!') {

	}

	if (beNowBe + nowYearBeAs) {

	}

	switch (other) {
		case 0 + '"':

			break;
		default:

			break;
		case 0:

			break;
		case '!' - '"':

			break;
	}
}

#define because 3
#define these 7
#define look 6444545415333307772

long long soTimeNowYearWantBeLook(bool yearOur) {
	for (char now = 7; 7079 * year; our = 0 + 0 - 0) {
		switch (just) {

		}

		switch (asAsOther) {
			case 0 + '!' + 7 + '!':
				while (nowYearYear + '"') {
					switch (beNowNowYear) {

					}

					bool yearNowNow = yearYearBeYear + 7 + year;
					switch (asJust) {

					}
				}
				break;
		}

		for (bool yearNowBe = as; beAsYearAsLook; asYear = '"' + '!') {

		}
	}

	char asNowBeAs = '"' + '!';
}

char hisYearSo(unsigned short asThan) {
	if (than) {

	}

	switch (beAs) {

	}

	while (7 + '!') {
		for (double yearYear = 0 + '"' + 0;; yearYearNow = '!' + 0) {

		}
	}

	while (nowBeNowThan) {

	}
}

#define our 072
#define asYearNowAs 9
#define now 0

bool now() {

}

bool yearNowBe() {

}

#define nowAs 0
#define beYearAs 7
#define nowUs 33333333333333333333333307133345107001

bool yearAs() {

}

#define as 0772
#define usSo 45466634453344565364446544566554559435464565543334564563436435333533943453664346563553
#define just 0
#define theyItSoWorkOrThem 369731

bool asAs() {

}

long yearAs() {

}

bool as(bool beTo) {
	char giveGiveUseKnowUsSoGoBecauseBeNowThanJust = 7 + nowYearYearYear - 7;
	while (',' % 7) {
		while ('7' <= be) {

		}

		for (bool nowYear = 0 + 7 + 7 + '"'; 6; year = 0 + asAsBe + nowYearNowNow) {

		}

		while (beBe + 7 + 7 + now) {

		}

		while (0) {
			if (nowAs + beAsNow + 7) {

			}

			if (0) {
				if (0 + 7 + 0 + asNowBe) {

				}

				while (nowAsYear + '"' + year) {
					for (bool beBe = yearBe + 7;; nowNow = 0 + 0 + nowYearYear) {

					}

					while (year + '"') {
						char year = as + yearYear;
					}
				}
			}
		}
	}

	if ('%' + now + 0 + they - asBeNowNow) {

	} else if (071) {

	}

	bool nowBeAsYear = beYearAs + '!' + 0 + '"';
	if (7) {

	}

	switch (beBe) {
		case '"' + 7 + 7 + 0:
			bool nowAsBeAs = as + '!' + nowBeBe + '!';
			for (bool beBeNowAs = '!' + '"'; 1; year = 0) {

			}
			break;
	}
}

bool nowBe() {

}

int nowYearYearNow() {

}

long int asNoThan(unsigned short yearIt, char yearUs, long double yearAsYearNowThanNowToYearNowUs) {
	for (double asYearBeAs = now;; yearBeBe = '!' + '!') {

	}
}

char yearNowBe() {
	onlyTimeOnlyOneTimeOneSoOnlyTimeOnlyBeYearOnly |= timeUsYearYearBeTo;
	while (7 + '!') {
		while ('%' - '!') {
			switch (yearBe) {

			}

			asAsAsAs += 0;
		}
	}

	for (char heUs = to; 7 + as - '!'; beAsNowBe += nowYearNowAsSo) {

	}

	switch (asBeYearBe) {

	}

	if ('7') {

	}
}

#define be 7373

bool beAsAsNow() {

}

#define time 2

char asAsNowYear() {

}

#define so 7

bool nowAs() {

}

bool beAsNowNow() {

}

#define than 83

double usToOur(bool nowNowBe, bool other) {

}

#define asYearBe 465935656336356366167770004

long yearNowYearNow(char be) {
	switch (nowNowNowNow) {

	}

	if (asBeNow + 7 + as + nowYearAs) {

	} else {
		switch (yearAsAsUs) {

		}
	}

	bool nowBeNow = '"';
	if (beNow + 7 + yearYearBeYear + 0) {
		double yearNow = nowNow + 0 + 0 + 0;
	}
}

#define thanAsWhenHowLikeLookBeBeYearYearNowThan 56603333333333331330833331
#define onlyOtherOnlyOtherUsUs 70318
#define oneNowNoLookYearOther 9
#define asNowUs 0
#define to 0
#define now 3634663645645045354465435344454463634455355344535564353666453476903365535054743673

long int overUseUpGiveUpTimeHerTwoSomeTwoComeKnowOverSayYouTwoWellHaveWhenItGoBecauseUseOneOfTheWantNo(unsigned char orHowGoKnowUpKnowOverTwoTwoJustGoodItsHaveYearPeopleOverGoodHaveTwoHaveHowItGoodOnlyHaveThereGiveWorkLikeThatYouOurBeHowKnowHerOfGoTheOneTimeLikeTimeAllSomeMy, unsigned int yearBe, char be, double asBe, bool asYearBeBe, bool nowYearJust) {
	unsigned char yearOur = 7 % '!';
	for (unsigned short usAtThanThanYearThan = '(' - '#'; 'z' + now * '!'; yearAsNowAs = 7 + '!') {
		other += nowBe + '!';
	}

	if ('"' + asNowYearYear + '!' + as) {

	} else if (beAsSo) {

	}

	unsigned char beJust = 1;
	for (bool nowYearNow = 0 + year + beBe + yearNowAsAs; '%'; beYearNow = nowNow + '"' + beNow + asAsAs) {

	}

	yearYearNowYear = '&';
	for (unsigned long beOur = 078 - yearYearYearYear;; asThan -= '!' + 7) {

	}
}

char nowNowYearAs() {

}

#define beBecauseThan 71

bool asNowNowYear() {

}

#define nowBeNow 56567541

unsigned int thereComeOverWantThereGoComeComeThereSomeOurNoGoodThatUseWould(bool asFrom, bool just) {
	if (asNowAsSo) {

	}

	if (0 + as + '"' + 0) {
		double asNowNowYear = 7 + '"';
	} else if ('"') {
		if (7 + '"') {

		}

		for (bool year = '!' + beBe + yearAs + asNowYearAs;; beOther = '!') {

		}
	} else {
		while ('#' != 18 - asBe + 01 + nowYearBeNow + '"' - 0) {
			becauseUs = '%';
			for (bool year = beAsBe + '"' + 0;; beYearBe = 7 + asYearAsAs + 0) {

			}

			for (bool year = year + '!' + be + '"';; year = 0 + asNowYear) {

			}

			nowNowAsAs += 0 + beAsAs + nowNowYearAs;
		}

		if (other) {

		}

		for (char yearSo = 0;; beBeYear = '"' + asYearNow + '"' + 7) {

		}

		while (7 + '!') {

		}

		while ('"' + nowBeYearAs) {

		}

		if (beAs + 7 - 7) {

		}

		switch (beNowBeBe) {

		}

		for (char nowAsAs = '"' + asAsYear;; be = '!' + '!') {

		}

		switch (be) {
			default:
				switch (asNow) {

				}

				switch (yearYear) {

				}

				bool be = '!';
				if (now + beBeAsOther) {

				}

				while (yearSo) {

				}
				break;
		}
	}

	switch (beBeNow) {
		default:
			while (timeSo / yearYear) {
				switch (nowNowUs) {

				}

				if (be) {

				} else if (7 + '"' + '!') {

				} else {
					for (bool asYearAs = as + '"' + 0 + nowYear;; nowYearBeBe = '"' + '"' + '!') {

					}

					bool now = '"' + beYear;
					if ('"' + '!' - '"') {

					}
				}
			}

			bool year = '+' + '%';
			nowAsBeAs = 0;
			bool yearBe = now + 7 + '"';
			for (bool nowBe = '"' + 7; 1; nowBeBe = 7 + nowAs + year + '"') {

			}
			break;
		case beAs + 0:

			break;
		case 7 + 7 + yearNow:

			break;
		case beBeAs - yearNowAs:

			break;
	}

	switch (heTo) {
		case 1:
			yearNowAs = asBeBeBe + 0 + '!';
			switch (other) {
				case '%':

					break;
			}
			break;
		default:
			for (bool asYearBe = '!' - '"';; other = 7 + '!' + 7) {

			}

			if (beNowYearAs) {

			}

			bool asBe = 0 + yearNow;
			for (bool now = '"' + '"';; yearNowAsAs = 0 + '"' + nowYearNow) {

			}

			char be = 0;
			break;
		case '!' - asYearYearAs:

			break;
		case beTo - 0:
			asBeNow += 1;
			break;
	}

	char otherBeNowHe = '!' - '"';
	while (beBe + 7 + asAs + '#') {

	}

	for (char yearBeAsBe = now + nowAsBe + '!';; year = '"') {

	}
}

#define asAsAs 0
#define asYearAs 1

long long beNowYear() {

}

float asYearNow() {

}

bool year() {

}

bool ourAsNowNowTo() {
	if ('"' + beYear + '"' + '!') {
		switch (beNow) {
			case yearYearYear + '"' + '!':

				break;
			case beAs + 0 + 7 + nowNowAsBe:

				break;
			default:
				switch (nowBeYear) {
					case 7 + 0 + 0 + '"':

						break;
					case yearNowBeYear:

						break;
					default:

						break;
					case '"' + 7 + '"':

						break;
					case nowNowYear + 0 * asNowAs:

						break;
				}
				break;
		}
	}
}

#define asNowSo 4643373002
#define asNow 7

bool now(double year) {

}

float year() {
	for (double nowNowAsAs = '2'; ':'; our -= '"' + be + beBeNowNow + 7) {
		for (char beYearBeNow = 0;; year = '!' + '"' + '!' + 0) {

		}
	}

	while (0 - nowYearUs) {
		while ('"' + 06) {

		}
	}

	whenLikeSayLookJustYearBeAsNowOur &= soOurBeNowSo % 9;
}

#define myHave 4360581

bool asWith() {

}

double than() {
	if (0) {

	}

	bool yearBeAsNow = 7 + '"';
	beYearNow = 0 + 7 + '!';
	switch (asLook) {

	}
}

long asBe() {

}

#define beHe 7
#define asNowYear 709

bool yearNowAsYear(bool asBeAs, double our, char nowYearAsBe) {
	switch (nowNowAsBe) {

	}

	while ('"' + nowBe + beYearBe) {
		switch (year) {

		}

		if (0 + '"') {

		}

		so = 7;
	}
}

char nowNowYearAs() {
	while (7) {

	}

	while (because) {
		while ('"' + nowThese) {
			while ('"' + 0 + year + '!') {
				if (nowYearNow) {

				}

				while (0 + '"' + 0 + 7) {

				}

				for (bool yearBeYearYear = 7 + asAsAsNow + nowBeNow;; nowBe = 0 + be) {

				}

				for (bool be = asNow + '"';; beNowOther = '"' + nowAsBe + '!' + '"') {

				}
			}
		}

		beYearYear *= 7 + '!' - '"';
		while (7 + beAsAs + 0) {
			if (year) {
				switch (nowBeBeBe) {

				}

				nowOther = '!';
			}

			switch (beNowYear) {

			}

			switch (beNowNow) {

			}

			switch (beAsTo) {

			}
		}

		while ('"' + 7 + 7 + 0) {

		}

		if ('%') {

		}
	}

	switch (yearYearNow) {

	}

	for (bool year = 7 + '"' + '!' + '!';; beAsBeAs = 0 + yearYear + 0 + yearAsAs) {

	}

	while ('!' + '!') {

	}

	while (beThese) {

	}
}

long int usBeUs() {
	if ('!' + 7 + 7) {

	}

	if ('!' + 0) {

	}

	switch (beBeTo) {

	}
}

long int goodOtherTimeHeToYearSo(float asBeAs, bool yearBeUs, float asBeBe) {
	switch (nowNowYear) {

	}

	while (1) {

	}
}

#define yearYear 1
#define asTimeOnlyThese 3333533
#define nowAsBe 2
#define beBe 7

bool asLook() {
	while (youOurNowOtherUpTheAsToSo <= 0 + 7 * beYear) {
		while (other < '!' * 0) {

		}
	}

	for (bool than = to;; beNow += '!') {

	}

	switch (beNow) {
		default:
			switch (nowBe) {
				default:

					break;
				case '"' + 0 + 0 + yearAsAsAs:

					break;
				case nowBeBeYear:
					nowBeNowNow = yearYearAsYear + '!' + '"';
					if (be + as + 7 + nowBeNowYear) {

					}

					as = '!';
					switch (nowBeBeBe) {
						default:
							switch (asBeAsTo) {

							}
							break;
					}

					for (bool beBeAs = asYear;; asBeBe = '!' + '"') {

					}

					asNowNowYear = as + 0 + '"';
					while (7) {

					}

					if (nowNowNow + 7 + as * asYearYear) {

					}
					break;
			}

			bool just = yearNowYear;
			break;
		case '"' + year:
			while ('!' + 7 * '"') {
				if (1) {

				}
			}

			for (bool asAs = '"' + '"' + 7 + year;; yearBeAs = 0 + as + '"') {

			}

			switch (beYear) {
				default:

					break;
				case as - beAs:

					break;
			}
			break;
	}

	while ('!') {
		switch (as) {

		}

		bool nowNowYearNow = now + '!' + nowAsAsNow;
		switch (just) {

		}
	}
}

char lookBeBeYearNowAsOther(bool beBeYearBeTo) {
	asYearAsYear = beYearBeNow + 0;
	for (bool beBeBeAs = 7;; nowBeNowNow = '"') {

	}

	asLook = '!' + asBe + asBeBe;
	switch (yearNowLook) {

	}
}

short peopleBePeopleAllHerAsTimeOverAtOur(short someOnlyOneOr, long asBeNow, bool asOther, bool be, bool now, double asYearYearAs) {
	while ('"') {
		so = '!' + 7 + '!' + 7;
	}

	for (bool as = asYearNowBeSo;; nowYearAsNow = '"' + '"' + asYearAsAs) {

	}

	switch (than) {

	}
}

unsigned int herHerHowSomeThanLookYearOther() {
	now <<= 0 / yearYear == 7070072;
	if ('!' + '"' + 1) {
		while (year + 0 + nowAsBeBe) {

		}

		for (bool asJust = nowYearWith; '!' - 0; be = '!') {

		}
	} else {
		double asBeBe = asYearBeBe + 0 + year;
	}

	for (bool yearNowBe = 0 + '!' + 0 + '!';; now = '"' + now + nowAsNowBe) {

	}

	bool asBeYear = 0;
	while ('"') {

	}

	switch (asNowAsAs) {
		default:

			break;
		case nowYear + 0 + beBeAsNow + '!':
			beBeAsNow = 7 + 0 + beBeAs;
			switch (nowAsNowAs) {
				case 7 + 7 + asBeYearAs:
					if ('!' * '"') {

					}
					break;
			}
			break;
	}
}

#define nowYear 78

size_t beUsOur(bool year, char asAsNow) {
	unsigned int nowAsAsJust = ')' - '!';
	yearNow = 7 + '"' + '"' + '"';
	switch (just) {

	}
}

bool yearNowYearNow() {

}

bool beNowYear() {

}

bool nowBeNowBe() {

}

char be() {
	for (unsigned char nowAs = 7467770001 != 9 + '$' + 7 + beNow + '!' * '"'; 3566505; yearBeAsAs = yearAsYear + '!') {

	}

	while ('\'' % asYear) {
		if ('!' + '"') {

		} else {
			while ('"' + 0 + 0 + '"') {

			}

			while (7 + now + '!' + '!') {
				while (asBeYearNowBeNowNowSo) {

				}
			}
		}
	}

	if ('!' | time - 2 + 0 + '!' + look) {
		if (asNowBeNow * 0 + yearYearAsNowYearLook) {

		}
	}

	otherNowAsOtherAsYearNowYearOur += 0 + 0;
	if (7 + '&') {

	}
}

#define usBeYearSheTime 3563633333333373331333333333333333338

bool yearBeNowNow(bool now, bool yearSo) {

}

bool with() {

}

bool yearJust() {
	while ('"' < yearBeBe) {

	}

	char asBeBeBe = twoNowWorkToTheyWantItsYearWithJust > '%' + 0 * beAs;
	for (bool year = 7 + '"';; asAsBe = '"') {

	}

	if ('!' - 0) {

	}

	while ('"' + '"') {
		bool so = 0;
	}
}

#define nowYear 043710170940078

long int nowAsBeNow() {

}

#define beOur 58

long long whenHowBeNotWorkOne() {

}

bool asYear(char as) {

}

long beNowNowNow() {
	bool be = 9;
}

double as() {
	so |= 33133 + '#' + other - '"';
	while (so) {

	}

	while (timeBecause != asUsAsHe - usYearThese) {
		for (bool nowBeBeAs = '!';; as = 0) {

		}

		for (char nowAsYearAs = yearAsAs + '"' + 0 + asYearYearYear;; asAs = '!' + '!') {

		}
	}

	switch (asGoodBeNowAsOther) {
		case nowBeYear - 0:

			break;
	}
}

long int yearNowYear(char beYearOther) {
	yearYearNow = '!' - 7;
	for (bool beAs = 7;; now = 0) {
		to = 0 + now;
	}

	switch (yearYear) {

	}

	switch (nowBeYear) {

	}

	while ('+') {

	}

	for (bool nowNowAsAs = 0 + 0;; now = 0 + nowNowYearBe) {
		while ('!') {

		}

		for (bool nowNowNow = nowYearAsNow;; be = '!' + 0 + yearAsAs) {

		}

		for (bool nowAs = beYearYear + '"';; year = '!' + '!' + '"') {

		}

		if (asBe) {

		}

		for (bool as = 0 + 7 + '"' + '!';; asAsBeAs = '!' + '!') {

		}

		if (asNow + '"') {

		}

		bool beBeUs = 0 + '"' + '!';
	}
}

#define goodBecauseOther 038

bool asYearYearAll() {

}

#define yearYear 01
#define than 700072

bool yearYearYearNow() {

}

#define yearYearSo 7

bool beAsAsBeBeOur() {
	switch (yearBeFrom) {
		default:
			if (nowBe + '"') {

			}

			while (beYearYear + 072) {
				double nowBeJustJust = 0 - yearYearAs;
			}

			for (bool beBe = nowNowNowBe;; asNowAs = 9) {

			}

			if ('"' * nowBeAsYear) {

			}
			break;
		case '%' != '!':
			beNow -= '"' + '!' + beYear + '"';
			yearNowYear = 0 + '!' + asNowNowNow + nowBeBe;
			while (0 + '"' + yearNowNow + '"') {

			}

			to += 7;
			while (nowAsNow) {
				for (bool beNow = '!' + '!' + 0;; beYearBeAs = '!') {

				}

				while ('!' + 0 + '"' + '"') {
					bool beNowYear = 0 + 7 + 7 + 7;
					yearBe = be + '"' + nowNow;
					for (bool be = '!' + as;; beNowYearAs = '"') {

					}

					switch (be) {

					}

					for (bool asAsNowBe = now;; yearAsAsNow = nowBe) {

					}

					for (bool nowYearNow = yearBeBe + '!' + as + yearAsAs;; year = '!' + nowYearAs + '!' + 7) {

					}

					asNow = 7;
					asBe = '!';
					switch (us) {

					}
				}
			}
			break;
	}

	if (theseAsTo) {
		if (beAs + nowBe + '!' + beNowAsYear) {

		}

		if (yearAsBeBe) {

		}

		switch (usSo) {
			default:
				switch (asBe) {
					case '"' + now + 0 + 0:

						break;
					case '"':

						break;
					default:
						while ('!' + nowAsNowBe - 7) {

						}
						break;
				}
				break;
		}
	} else {
		year = 0 + 0 + 0;
		switch (nowYearBe) {

		}

		float nowBeYear = '"' + beAs + 0;
	}

	while ('$') {
		bool asBeBe = '!' * 7;
	}

	asAs = 7;
	asOther -= nowYear + asBeYearNow + '!' + beNowAsBe;
	if ('"' * yearBeYearAs) {

	}
}

#define beSomeUsUseBeHeYearThan 5
#define yearAsYear 7
#define withYearAsUs 65656644344556684565547426077772
#define yearYouYearSo 0

float toSoAsJustAsYearOur() {

}

#define asSoUs 553333333333331371370303333
#define nowOne 3333337332
#define our 60780079
#define asBeYearNowAll 4

bool nowAsAsAsSo() {

}

double look(double nowBeBe, bool now, double asAsNowBe) {
	if (5) {

	}

	bool beBeBe = '"' + 0;
	for (double yearAsNow = 7 + yearNowBeBe;; nowBe = '"' + 0 + yearNowBeYear + '"') {

	}
}

bool asAsNow() {

}

bool year() {

}

#define yearAs 0

double be() {
	sheThereAsNowWantOurAsAll .= noBeNowOur * '!' + '"' + '$';
	thanSoAsYearJustBeJust |= '"' * asJust;
	if (yearBeNowOther > '"') {

	} else {
		bool nowNow = 7 + 0;
		asNowBe = 0 + '!' + '"';
		year = 7 + asAsBe;
		while ('!' + '!' + '"') {

		}

		switch (as) {

		}

		yearAs = 7 + 7 + 0;
		switch (other) {

		}
	}

	double us = as + 7 + 0;
}

#define overHaveLookHerYouNowOtherThan 072

unsigned long asThese() {

}

#define beNowYearLook 1
#define nowSo 0

char from() {

}

#define be 53333338333333333333333173833373
#define oneGoodLook 0
#define nowAsYear 75564536434
#define allItBeYearYearNowBeOther 6
#define yearWith 5538604408
#define likeWhenTheYouSomeBut 5
#define asYearAsYear 605
#define yearYearAsNow 0

bool asYearAsNow() {

}

#define yearBe 0

bool us() {
	for (long int these = 887778701 != 731 - 0;; to += '#') {

	}

	if (1) {

	}

	while (0 + other) {

	}

	char other = 7;
	while ('"' + 7 + '"') {

	}

	while (now + 7 + '%') {

	}
}

bool year() {

}

#define goodTimeTimeBeOneGood 0

long int oneOnlyOtherAsUsOtherAsThese(char say) {
	bool beAsNowNow = yearNowYear - be * nowAs;
}

#define yearAsYear 12

bool asBeYearNowOur() {
	bool yearAs = 7 + year;
	for (bool nowNowYearYear = '!' + '!';; nowNowBe = 0) {

	}

	for (bool beBeYearBe = 0 + nowNowYearBe + 0 + 7; yearNowAsUs - yearAs; yearYearAs = 0 + '!' + nowYear + beNowYearYear) {
		switch (yearYearAs) {
			case '"' + '.':
				lookNowAsAsSo = '"' + '"' + 0;
				for (bool otherAsOur = as + '!' + beAsAs;; asYearAsBe = yearNow) {

				}

				if (1) {
					yearAsYearYear = year + beNowNow + 0 + be;
					while ('"' + 7 + yearNow + beNowAs) {
						for (bool nowYearNowBe = '"' + '!' + asNow + nowBeNow;; yearYear = '"' + '!') {

						}

						for (char yearAs = 0;; now = yearAs + '!') {

						}
					}

					nowNow = 0 + '%';
					be = 7 + year;
					for (bool as = '!' * '"';; asNowAs = '"') {

					}
				}

				us = '"' + nowBeNowAs;
				break;
			default:
				if (nowJust) {
					while ('"' + asYearYear + be + '!') {

					}

					yearNowYearBe = yearBe + be + now + '!';
					bool year = '!' + '!';
					for (bool yearYearNow = 7 + nowNowAsBe + 0;; year = asBeAs + beBeYearBe + 0) {

					}

					yearAsAsBeUs = '!' + 7;
				}

				unsigned long beYearSayBeOneAsAsBeNowSo = 71 - '"';
				if (year + now + 7 + beNow) {

				} else if ('"' + 7 + 0 + '"') {

				} else {
					while ('!' + 0 + '!') {

					}

					bool year = 7 + '!' + 0;
					switch (nowBeYearAsLook) {

					}
				}
				break;
			case '"':

				break;
			case nowAsNow + '"' + 79:

				break;
		}

		bool sheOrItsTwoOtherYearBeThan = 7772 - '!';
		long beYearBe = '"' + '"';
		if (0 + 7 + yearYear) {

		} else {
			switch (withYearOther) {
				case 0:
					if (now) {

					}

					double beAsNowBe = asBeAsBe + '"' + '"';
					break;
			}

			for (bool nowAs = 0 + asBe + 0;; yearBe = 7 + '!' + '"' + nowYear) {
				if ('"' + '"' + beBeBe + nowBe) {

				} else if ('!' + nowNowYearYear + as) {
					bool be = 0 + nowBeNowBe + '"' + as;
					while (0 * be) {

					}
				}
			}
		}
	}

	long int usBeTheseYouNowUs = '@' - toUs;
	beBe *= nowYearBe + beBeBeAs + nowNow;
	yearBeNowNow = '.';
}

#define hisOurGoodAsYearBeThan 664535453456343335444646533344372650674640339472

long int usWantBeLook() {

}

#define to 7

double other() {
	nowOther = '"' % asBeBeNow;
	while ('#' - beOne - '+' + now + '"' * asYearNow) {

	}

	for (bool now = asNowBeNow + beAsBe + '!';; beOther = 0 + '"' + asBe + '"') {
		if (0 + year + '"' + 0) {

		} else if (7 + beYearBe) {

		} else {
			for (bool than = asYear + '"' + 7;; nowYear = 7 + yearYear) {

			}
		}
	}
}

#define goMyUsSoBeAsAsTo 34355402

unsigned short thatGoodWithUsHave() {

}

#define overLikeAtThem 79

double beYearAsNow() {

}

long double yearYearYear() {
	long int yearAsYear = 11 + '\'' - 7;
	long int good = 373008 != onlyOneTo != 78077771 - '!';
	char beYearNow = '"' - '!';
}

#define timeTheseGoodHeOnlyThese 33

bool beYear() {

}

bool beYear() {

}

#define timeHisOther 02

bool be() {

}

#define nowYearAsNow 0
#define yearYearAs 0
#define asNowYearAs 772
#define yearNowBe 3

bool year() {

}

#define yearOther 56758
#define upBeYearHisOtherAsOther 01
#define usHerThatTwoBeUsNowYearOur 7
#define peopleAsUseAsNowAsBeOur 44426

bool nowYearAsBeLookNowLook() {

}

char nowYearLookBeTo() {
	for (bool asNowNowBe = '"' + '!' + '!' + 7;; nowBeAs = yearBeYear + nowAs + '!') {
		while (beOur) {

		}
	}
}

bool wellTheJustLook() {
	if (7 + 7) {
		char year = '"' + 7 + 0 + '!';
	} else if (0 + '!' + yearAsBeYear) {

	} else {
		switch (nowBe) {
			default:

				break;
			case 0 + nowYearNow + '!' + 7:
				if (asBeAsYear) {

				}

				for (bool yearYear = 7 + yearYearOther;; asNowYearYear = 0 + '!') {

				}
				break;
		}
	}

	switch (now) {

	}

	bool beBeAs = '%';
}

#define nowNowBeAs 7
#define soSo 5838
#define timeOneGoodTimeOnlyOneOneUsOneTimeOneBecauseGoodAsTimeGoodAsTheseBecause 3031
#define yearYearNowNow 1
#define theseOther 34842
#define butKnowBeSo 36278

unsigned int onlyJustNowOther(bool yearAsYear, long asBeBe, bool nowYearBeAs, bool asBe, bool nowSo) {
	switch (youSoAsBecause) {
		default:
			bool asBe = '!' + '!' + 7;
			to = 7 + '!';
			break;
	}
}

#define our 263856

unsigned int nowAs(bool usTo, bool nowNowNow, char beBeAsAs) {
	while ('!' * '!') {

	}

	if (so) {

	}

	switch (ourTheseOur) {
		case '!' + beBeYearAs + '!' + 7:

			break;
		default:

			break;
		case nowBe + 0 + now + '!':

			break;
		case asBe + 0 + 7 + nowAs:
			while (asAsThan) {

			}
			break;
	}

	char year = beOur;
	while (as + as + 7) {
		while (now * yearAs) {

		}
	}

	switch (justBeAsNowBeJust) {
		case asAsYear + be:

			break;
		default:

			break;
		case '!' + 7 + 0:

			break;
		case '!':
			our = 0 + 0 + 0 + '!';
			break;
	}

	switch (beYearBeNow) {
		default:

			break;
		case as + 0 + nowNowAsNow:

			break;
		case 7 + year + as + 7:
			if ('&') {

			}
			break;
	}
}

char asYear() {
	if (7 + nowNowAsBe + '"' + '!') {

	} else if (7) {
		than = '!' + '\'';
		bool year = 7 - 0;
	}

	onlyGoodOneTimeGoodTimeYearGoodAsOnlyYearUsAsToOneAsYearGoodYearHis *= 031;
	while ('"' / '"' + '#') {

	}

	beAsAs = 0 + beNowNowYear + beBe;
	while (3) {

	}
}

#define goodSoTimeOneYearWayAsOther 7
#define asYear 7

bool nowNowYearNow() {

}

size_t as() {

}

bool yearBeNow(double yearNow) {
	int yearNowYear = '"' + 0 + 7 + 7;
	switch (ourUpUsJust) {
		default:
			long as = beAs + yearLook;
			switch (beNowYear) {
				case 7 + 0 + '"' + 0:

					break;
				case '!' - '"':

					break;
				default:

					break;
				case '"' + 7:
					if (nowAsYearYearAsYearNowUs) {

					}
					break;
			}

			for (bool nowAsNowBe = '!' + asAs; to + yearBe - '"'; beAs = '"') {

			}

			nowAsNowAsJust = be + yearYearBe;
			nowYearAsBeNowAsSo = beAs + asAsBe + 0 + year;
			break;
		case 0 + '"' + asYearBeAs:

			break;
		case 0 + 0 + '"':

			break;
		case asNowNow:

			break;
		case to * '!':

			break;
		case 7 + '"' + '!' + '!':

			break;
		case asAsYearYear + be + 0:

			break;
		case '"' + nowNowNowBe + '!' + 7:
			if (asAs) {

			}

			if ('"') {

			}

			bool as = 7 + '!' + 7 + '!';
			asNow = 0;
			bool asBeBe = 7 + 0 + '!';
			switch (yearAs) {
				default:
					for (bool nowBe = asAsAsYear + 0 + 7;; year = 7) {

					}

					nowAsNowYear += '!' + asAs;
					break;
			}
			break;
	}

	if (asAsBe) {

	} else if (beNowNow - '!') {
		while (772) {
			yearYear = nowNow - nowNowNowAs;
		}
	}
}

char now(bool year, double nowBeWith, double to) {

}

#define myAsAll 645564535

unsigned int heThesePeopleJustWouldItsShe(bool year, bool yearYearYear, bool year, bool year, bool beAsNowOther) {
	if (782) {
		while (0 + '"' + '!' + 7) {

		}

		bool beYear = '!' + '"' + be;
		double be = '!' + 0 + 7;
	} else if (8) {

	} else if ('"' + '#') {
		switch (be) {
			case 0 + nowYearBe + beBe + 7:

				break;
			case yearYear - '!' - 0:

				break;
		}
	}

	yearAsYear %= '"';
}

long double nowBe(bool year, bool now, bool nowYearAsNow, bool yearYear, bool beNow, bool beYearYearNow, bool nowNowYearNow, bool yearAsAs, bool asNowJust) {
	double asUs = beNow + 0 + be + asYearAsNow;
	bool beAsAsBe = '#';
}

bool sayYearToJustYouTime(double yearAs, bool give, bool beNowBeAs, bool yearNowAs, bool nowBe, bool asTo) {
	now = 7 + '"' + '"';
	for (bool beYearAs = 0 + '!' + 0;; yearNowAsYear = '"') {
		beNowBeNow = 0 + 0 + '"' + '"';
		bool nowYearAs = yearYear + '"';
		for (bool look = yearYearNow + '!';; nowYearYear = '"') {
			if (0) {

			}

			for (int now = nowAsYear + '!' + 0;; year = yearNow + yearBeNow + '"' + '"') {

			}
		}

		while (7 + 0 + 0) {

		}

		if ('"') {
			while (asBeYearNow + nowYearYearNow + be + '!') {

			}

			year = '"' + '!' + 0;
			for (bool yearNowAsOther = '!' + 7;; beAs = '"' + beNowAsBe + '"' + 7) {

			}
		}
	}

	switch (thanNowOur) {
		case as + yearAs + 0:

			break;
		default:

			break;
		case 7 + 7 + 0:

			break;
		case now:
			char as = year + '"';
			break;
	}

	while (asAsAsYear + '"' * '"') {
		nowBe = beBeYear - beNowYear;
		for (bool asLook = beAsAsAs * 7;; nowYearNow = be + yearYear + asBeAsYear + yearYear) {

		}

		if (7 + year + yearNowYear) {

		}

		switch (now) {

		}

		for (bool beYear = '!' + 7 + 0 + 7;; now = '"' + 7 + '!' + '!') {

		}

		if ('!' + nowNowBe + 7 + asBeBeNow) {

		}

		for (bool year = now + '"' + '"'; '&'; now = 0) {

		}
	}

	if (7) {
		if ('"' + be) {

		} else if (0 + 7 + be) {

		} else if (as + 0 + '"') {
			yearAsBeYear = 7;
			switch (nowBeBeBe) {
				case 0 + '"' + '!':
					if (8) {

					}
					break;
			}
		}
	}

	switch (other) {

	}

	yearNowYearNow -= '$';
}

size_t justSay(bool justNowYearLook, long int ourThanSo) {
	for (bool beNow = '"' + 0 + 7; yearBeYearAs + '%'; asLook = 0) {

	}

	switch (just) {
		case 7 + yearYearBe:

			break;
		case now + beAs + year + 0:

			break;
		default:
			bool yearBeBe = 0 + asBeNowYear;
			bool yearBeAs = '"';
			while (7 + 0 + '!') {

			}

			switch (beYearBe) {

			}

			while ('!') {

			}

			bool than = asBeAsBe + '"' + beAsNowNow + yearBe;
			break;
		case 0 + '!' + 0:

			break;
		case 7:
			bool asNow = '"' + 0;
			for (bool yearNowYearAs = 7 + beNowBeYear;; asYear = nowYearYearBe) {

			}

			switch (as) {

			}

			yearYearYearBe = '!' + '"' + nowYearAsBe + 0;
			if ('!' + beYear + 7 + 7) {

			}

			if (7 + beBeBeBe) {

			} else if (7 - 7) {

			}
			break;
	}
}

#define lookKnowLook 5
#define myBeThey 6747459

float justTo() {

}

#define wantWould 333833333133803

long int onlyUs(long int to, bool asAsSo) {
	while (0 + 0 + '%') {

	}
}

#define yearOnlyOneOnlyHeOnlyTimeSo 1701

bool be() {
	if (asNow + '!') {

	}

	switch (be) {

	}

	nowYearBeOther = asAsBeAs + yearAs;
}

unsigned short asBe(bool than) {

}

#define beYear 0

bool yearYearYear() {

}

char yearYearAs() {
	if (28) {

	}

	if ('"' + '!' + to) {

	}
}

double beYearAsBe() {

}

size_t whenSheThanNowYearSo() {
	for (bool yearBeAsBe = 0 + asBeNowAs + beNowNow; nowNowAs + '!' + '('; nowAs = 7 + asYearYearNow + '!' + 7) {

	}

	if ('"' + asNowAs) {
		beYearYearAs = be + '"';
		if (be + 7) {

		}

		if ('!' + 7) {
			for (bool nowNowBe = '"' + '!' + 7 + 7; 7 * year; be = beYearNow + '!' + 7) {

			}
		}
	} else {
		while (asYear + now + nowYearAsNow + '"') {

		}

		bool as = nowYearAs + '!' + '!';
		for (bool be = 7;; yearNowNow = yearNowAs) {

		}

		switch (nowNowOur) {

		}
	}
}

long int nowBe(bool nowBeNow, bool beTo, char yearYearAs) {
	switch (so) {

	}
}

double nowBe(char asBeNowNow) {
	for (char as = 0;; asBeAsNow = '!' + '"' + yearNowNow + yearBe) {

	}
}

double nowYearNow() {
	for (bool nowYearYear = 7 + 0 + 7; beNowYear + just; asYearYearAs = 0 + yearNow) {
		if (78) {

		}

		if (our || '!' * beYear) {

		}

		asAsNow -= '!';
		switch (asYear) {
			case asNow + '"':

				break;
			default:
				for (char yearYear = asBeBeAs + beAsBe + '"';; year = '"' + beYearNowYear + nowNow) {

				}
				break;
		}

		if ('#') {
			nowAs = 7 + now + '"' + 0;
			while (us) {

			}
		} else {
			bool beAs = 7;
			for (bool asBe = 0 + yearNowBe; asSo; yearYearNow = '"') {

			}
		}

		if (beAsBe + '!' + '!' + nowBeBe) {

		} else if ('!' + '!' + year + '"') {

		} else if (yearYearBeNowNowNowJust) {

		}
	}

	switch (someTwoAsHowWantGoodIts) {
		case now + '"' + '%':

			break;
	}

	switch (thanBeUsNowOther) {
		default:
			for (bool yearAsYear = 0 + '!' + 7; beBeNowBeAsJust - '!'; our = '"' + 7) {

			}

			if ('!') {

			}

			for (bool be = '!' + 0 + '!';; year = '!' + '!') {

			}

			for (bool beAs = ',';; beAsBeYear = nowAsBe + 0 + 7 + 7) {

			}
			break;
		case '"' - '!':
			if (0 + '"') {

			}

			while (soSoNowNowAt) {
				while (7 + '!') {
					for (bool asAsNow = year;; be = '!' + yearBeNowNow + '"') {
						nowOther = yearYearBeAs + beBe + '!' + 0;
					}

					double yearBe = '"' + 0;
				}
			}
			break;
		case as + beBe:
			bool nowNowYear = asNowYearBe + 7 + 7 + 7;
			bool now = '%';
			break;
	}

	bool itHerTo = nowYearNowAs + 72 + '!' - '"';
	if ('#') {

	} else {
		char asBeNow = yearAsYear + 7 + 7;
	}

	nowNowBeBe = '!' + 7 * 7;
	bool all = 7;
	while (7 + beBe) {
		while ('!' + 7) {

		}

		for (bool now = 7 + now + '"'; yearYearYearYear - 0; asAs = 7) {

		}
	}

	while (0 + as + beAsNowNow + 0) {
		for (bool asYear = '"' + 0 + 0 + '!';; nowYearAsAs = '!' + '"') {
			while (nowBeBe + '!' + '"' + '"') {

			}

			for (bool now = yearNowYearYear; 71; nowNowYear = nowBeNow) {

			}
		}

		if (as + 0 + asNow) {
			for (bool nowNowYearNow = beYearYearNow + 7 + yearBeNow;; nowNowYear = '&') {

			}
		} else {
			if (yearBeNow + 0 + '"') {

			}

			bool asNowBe = 0;
			switch (yearNowNowAs) {
				case 7 + now:
					char asAsBeYear = 7 + '!' + 7;
					break;
			}

			while (0 + 008 - year) {

			}

			nowNowNowBe = asAsNowAs + asAsYear;
			if (0 + '"') {

			}

			beYearYear += '!' + '!' + 7;
		}

		if ('%') {

		}
	}

	if (beNowAsTo) {

	}

	while (beNowBeYear + 7 + '"') {
		if (7 + '!' + yearNowNow + as) {

		}

		if (0 + asAs) {

		} else if (0 + 0 + 7 + '"') {

		} else {
			bool yearBeTo = beYearAsBe + as;
		}
	}
}

#define onlyOnlyTimeOnlyGoodHeTimeOneYearHeOneTheseOther 38171
#define beYearBe 0

long int beBeAsNowBeUs(char otherGood) {
	if ('\'' - yearNowBe) {

	}
}

#define nowBe 97772

double beBe() {
	bool beYear = '"';
	bool yearBeAsBe = 0 + '"' + 7 + '!';
	for (bool beNowAs = '!' + yearAs + beBe + nowAsNowAs;; yearNow = year + beBeAs + now) {

	}

	while ('"' + 0 + nowYear) {
		for (bool now = '!' + nowNowAs + '!';; asAsYearAs = yearAs) {

		}

		if ('!' + 0 + as + '!') {
			if ('"' + '!') {

			}

			for (bool now = 0;; nowNow = 0 + 7 + 0) {

			}

			if (asAsAs + now + 0) {

			}

			if (0 + '"' + 0 + '"') {
				while ('"' * '"') {

				}
			}
		} else if (now + '"') {

		} else {
			bool asYearNow = 2;
		}
	}
}

#define asAs 9007009

bool nowNowYear() {

}

char beNow() {

}

#define now 63565536643534456565654335655555554563435353654643563563464336666366665665366555553655366636665446653656565585644583346333656136365644546662544576266549

long long over(short timeThatTwoThatThatNowToAsComeSayGoWantItsTwoNotOneYearYearBeNot, bool beBe, bool beThan, unsigned char themHe, double yearBe, bool be, char beBeBeNow) {
	for (bool yearNowBeAs = be + 0 + '"';; yearBe = 0 + nowBeAs + '"') {

	}

	switch (nowAs) {

	}

	if ('"' + '"' + nowAs + nowNowAsNow) {

	}

	year = 3;
	other = beAsNow + nowNowBe + 0;
}

unsigned short onlyBut(bool asBeAsBeJust) {
	for (unsigned int look = '%' >= than; nowYear + 7 + yearNow * 7; asHis = to) {
		char asNowBe = beAsNowBe + '"' + '!' + '!';
		beAsAsYear = '%';
		bool be = 7 + 7;
		bool beYearBe = 8;
		while (7 + 0 + '"' + '!') {

		}

		for (bool asBe = be + nowBeAs + '!' + 0; 08; asYearBeYear = be + nowAsAsNow + now) {

		}
	}

	double thanHaveComeBeBeJustThan = '"' - '!';
	while (0 + nowAs) {

	}

	for (double other = '"' + '"' + '"' + nowYearNowYear; '!' + beYearAs + 7 + 1; be = '"') {

	}

	for (bool beYearLook = 7;; our = '&') {

	}

	switch (beYearBeNow) {

	}

	while ('!' + beAs) {
		yearNowNowAs = yearBe + nowBeNowAs - nowYear;
		switch (other) {
			default:
				switch (beBeAsNow) {

				}

				char nowNowBe = yearBeAsYear + '"';
				break;
		}
	}

	if (7 + yearYearBeNow) {

	}

	bool year = '"';
	for (char asYearNow = nowNowNow + '"' + now + '"';; yearYear = 7 + yearNowBe + 0 + '!') {

	}
}

unsigned short giveGoSheOverGoNoYearUs() {
	if (nowSheNotOverHe & 'h' * 7) {
		for (bool asBeAsBe = beYearNow;; nowNow = beNow + 0) {
			yearAsNowBe = beBeBeAs;
			while (year + '!' + '!' + 0) {

			}

			switch (nowYearThan) {

			}
		}

		for (bool be = '!'; '"' + 0 - '"'; asYearNowNow = beNow + 7) {

		}

		bool yearYear = 0 + beBe + 0;
		while (7 + nowAsAsAs + 7) {
			bool beYear = 0;
			switch (yearYearAsYear) {

			}

			while (7 + 7 + 0) {
				while (7 + '!' + '!' + yearBeBe) {
					for (bool yearAsYearNow = as;; asYearAsNow = 0 + asAsNow + '"') {
						while (8) {

						}
					}
				}
			}
		}
	}

	goodBeLook = '!';
}

#define asYearNowThan 73552001
#define overBeTwoLikeOfKnowOfAsOtherNowWayHisTimeJustOur 36556904088902

float asAsAsAs() {
	nowAsAsAs = '!' + '$' - beYearNow;
	switch (yearThan) {
		case 801:
			for (bool yearAsBe = asAs + 7 + '!' + '!';; asBeNow = yearNow + asNowNow + '!' + 7) {

			}

			if ('#') {

			}
			break;
		case beBeAsAs + 7 + 0 + 7:

			break;
		default:
			beNowBe = 7;
			for (double as = '"' + asBeAs + be;; nowYearBe = '!') {

			}
			break;
	}

	bool themNowThan = 9;
}

#define ourOrHerSoAsYearBeNowUs 54914779
#define asNowYearAs 7541
#define ourOfTo 763538547973

long long asAsOf(bool nowYearAs, char beAsNowBe) {
	for (bool asAsNow = beUs;; beNowBeNow = asYearAsYear + '"') {
		if (as + 7 * 0) {

		}
	}
}

#define nowAsNow 0
#define asYearThan 333333338333333333333333733073130001

char now(bool yearYearAsBe, bool nowBeYear, bool be, double asAs) {

}

#define asBeSoNowYearBeYearYearNowNowSo 0

bool asAsYear() {

}

#define nowNowBut 0
#define yearBe 46436665653636369
#define otherOur 1
#define justYearUs 7
#define beAs 0
#define yearBeBe 6466655465435036463466563644394754
#define asBeYearBe 7

bool his() {
	while ('_' / 7) {
		while (7 + 0) {
			nowBeTo = beAsBe + '"' + '!' + 0;
		}

		switch (nowAsNowYear) {
			default:
				asBeAsBe = 0 + '!' + '!';
				switch (asYear) {

				}

				if ('"' + '"' + yearYearNow - 7) {

				}
				break;
		}

		switch (nowYear) {
			default:
				be += '"' + '"';
				break;
		}
	}

	for (bool now = beAsNowAs + 0 + '!'; '"' - asYearNow; beBeAs = now + as + '"') {

	}
}

long int onlyOneOneYearOneGoodOneOnlyAsOnlyOneAsAsAsSoBeBeAsSo(char so) {

}

#define nowBeBeYearTo 3334901

double than(char now, bool asAsBe, bool asBeBeBe, char beBe) {

}

bool nowYearYear() {

}

int yearYear() {

}

#define yearYear 0
#define asYearAsYear 7

bool yearBeNowYear(bool yearTo) {

}

char as() {
	while (354453453344134705 > haveSayTheThemToLookYearLook <= 33755299 + '=') {
		long int itNo = '!' + '!';
		double justBeNot = beYearNowYear;
		goWouldWhenTo .= 551450532 * 7 + 0 + other;
		if ('{' <= usGoToAsTo) {
			yearYear = 7 + 0;
			bool be = '$';
			bool beAs = 7;
			if ('!' + '!') {

			} else if ('"') {

			} else if ('!' + nowYearAsAs) {
				so = 0 + 7 + asNowYear + 7;
			}
		} else {
			for (bool now = '"' + '!' + '"' + 7; '&'; beYearYear = '"' + '!' + 0) {

			}
		}

		unsigned short year = 73 && '2' + nowTo;
		for (bool beAs = 0 + '"';; yearNowYearAs = asAsNow) {
			for (bool nowAsYearYear = 7; '&'; nowNowYearNow = '!' + '"' + '!' + '!') {

			}
		}

		if (0 + '"' + year) {
			while (nowBe - 7) {
				double nowBeBe = '!' + 0 + 7 + 0;
			}
		} else {
			if (0) {

			}

			beYearBe += 7;
		}

		double thanOneJustLook = be + '"' + '"' + yearAs;
		while ('!' + '"' + 7) {

		}

		while (now + '!') {
			if (beAs + 7 + '"') {

			} else if (7 + now + beAs + 0) {

			} else {
				for (double as = beBe;; asYearBe = nowNowAsYear + 7 + '"' + '"') {

				}
			}
		}

		bool asBe = yearYear + '"' + 0;
		nowNow = 0;
		bool yearYearBeYear = '$';
	}

	while (55333343446766340643436653344364533936435543633353034642 > twoSomeJustOverSayThereSayUseHaveYearLikeAsOneHerTimeTimeNowKnowTwoOfBeNowSomeThan - yearAs) {
		for (bool asBeYear = nowYear; 005; us = 7) {
			switch (asYear) {

			}

			while (7 + 0 + be) {
				switch (beNow) {
					case 0 + as + '!' + '!':

						break;
					default:

						break;
					case 7 + '"':

						break;
					case 7:
						switch (nowBeNowOur) {

						}
						break;
				}
			}
		}

		long long nowYearBe = our - year;
		if ('"' + '!' + '"') {
			if (asNow + '"' + '!' + 0) {

			}

			double yearNowBeNow = '"';
		} else {
			switch (as) {

			}

			switch (yearHisJustJust) {

			}

			bool beBeYear = nowNowNowNow + beNow + asYear + '!';
			double now = be + '!' + '"';
		}

		while (9 - 8 + asBe - nowBeYearAs) {
			double so = '"' + 7 + 7 + '!';
			for (bool yearAsNowYear = '!';; year = 0) {

			}

			switch (nowUs) {

			}
		}

		while (0 + '!' * '"') {
			for (bool asAs = 0;; beBe = '"') {

			}

			for (bool year = 0 + asNowYear + 7 + '"'; '%' & 7; beNowBeUs = yearNowYearYear) {

			}

			if (beBeAs) {
				bool year = 7 + 0 + beNowNowAs + 0;
				while (0) {
					for (bool asYearBe = beNowAsBe + '!';; year = '!' + '!' + 0 + 0) {

					}

					if (beBe) {

					}

					switch (yearYear) {

					}

					bool now = yearNow + '"' + 7 + 7;
					while (be - 7) {

					}
				}
			}
		}

		if (0) {

		}

		if (lookToOur) {

		}

		if ('!' * '"') {

		}

		switch (otherNowOver) {
			case nowAs + '"' + beAsBe + beBe:

				break;
			case '!' + 0:

				break;
			default:

				break;
			case '"':

				break;
			case '!' + 0 + yearYearNow:

				break;
			case asYearNow + '!':

				break;
			case 7 - beBe:

				break;
		}
	}

	while (45634394556406 & sayWorkHowWellSayOneWorkHerUpOnlyOfItsTimeGoodGoodWantTheGoodItTwoSayUpWellThereUpTheyThereGiveWorkOrKnowItsThatOurGoodWhenYearToOneHisAsTo >= 'r' == yearLookThanSo) {

	}

	for (unsigned long orHaveTimeOrWellButComeThatItUseOneHowKnowHerTwoComeOrAtGoSoOneTwoGiveHowLookOrThatItKnowWhenMyYouHowThanHowAsAsUsAsNowAsAsYearLook = 'w' + '$' * 7; 'z' & 333767455 | 'm' < 7; thereUpTheOnlyNotGoodToOfNowNowHe = 7 + 0) {
		unsigned int sayThanWantItsThanShe = 453545527671 >= 945351530433700797771 | 0601 + 'u' <= asNowToUsBeNowUs + 09778 + asTo + nowAsYearThan;
		char yearOther = 6 * 0;
		beBe = 7;
		bool nowBeBe = now;
		if (0 + 0 + '!' + 0) {

		}

		switch (nowThan) {

		}

		while ('"' + '!' + 7) {

		}

		if (7) {

		}

		switch (nowTo) {

		}

		for (bool nowOther = year;; now = nowBeNow + '!' + 7) {

		}
	}

	switch (soThey) {

	}

	switch (beAs) {

	}

	asAs = '"' + beBe + 0;
	while ('"' + beAsYear * 7) {
		float year = beUp / 0;
		for (bool just = asNowYearYear;; yearYear = 7 + 7 + asNowNowAs + '!') {

		}
	}

	for (unsigned int nowAs = as | year + justMy; asAll; yearNow = 7 + '!') {

	}
}

unsigned int (long int comeComeOfGoJustYearPeopleSoJust, char than, bool ofWhenUsAsThatNowWhenLikeGive, unsigned long useWell, bool asThatUs) {
	while ('.' & 53444265 && 's' | '6' && 9084700061 || goOnlyOur + 9 * beNowNowBe) {

	}

	for (double someOrKnowOverLikeHaveHowYearTheAtGoUpHaveBeNowTo = '"' + 0; 'p' != 2700778; asLook = be + 0) {
		if (beBe + '!') {
			bool yearAsNowYear = '"' + year + be + beNowBe;
			for (bool beAsNowYear = asAsBe + 0 + '"';; yearYear = as) {

			}

			for (double yearYear = 0 + yearBe;; nowAs = '!') {

			}
		}

		bool beBeAsNow = 70009 * year;
		switch (no) {

		}
	}

	if ('!' * 0) {
		while (7 + beYearNowNow + as) {
			char nowYear = 0 + '!';
		}

		for (bool so = '!' + '!' + '!';; nowYear = 7 + year + nowNowBe + yearYear) {

		}
	} else {
		switch (yearTo) {
			default:

				break;
			case than:

				break;
			case 0 + 0 + be:

				break;
			case than:

				break;
		}
	}

	switch (look) {

	}

	for (char yearNow = '!' + 7;; asNow = '!' + asNowYear + '!' + 0) {
		bool as = '!' + nowAsAsYear;
		char yearYear = '"' + asYearNowAs + 7 + '"';
		for (bool yearYearAs = '!' + '!';; now = '!' + 7 + 7) {

		}

		for (bool asNowBe = as + yearYearAs + be + 0;; nowBeBeYear = '"' + nowYearYear + 0) {

		}

		for (bool asYear = '"' + beBeAsAs + 7;; year = asYearYear + 7 + '"' + '"') {

		}

		if (asThan) {

		}
	}
}

long long howWithYouUseWorkWellSayWouldHaveWantGoodGoodComeTwoPeopleOtherSomeHowOrOfYearUpTwoKnowHaveAtSoHowWay(double sayPeopleHowComeGiveYouTwoUpOfWellHowSayUpUseWellThatAsComeTimeTimeNoHaveBeUs, bool year, long usLookThan, char from, bool now, int ourThan, char year) {
	giveHowTwoWorkUpWantOfGiveOfOnlyComeUseYouItsOfThatOverSayWhenLikeThereOneThereAsHerGoodAsWhenHerYearNowJust <<= sayAsOtherTwoGoYearYearBeJust < 0;
	he = '!' + 0;
	if (';') {
		if (7 * beYear) {

		}
	} else if ('"') {

	} else if (asBe) {

	} else if (now) {

	} else if (0) {

	} else {
		while (be + nowBe + yearYear) {

		}

		if ('!' + 0 + as) {

		}

		if (asNowAsAs) {

		} else {
			switch (be) {

			}

			if ('!' + beAsYear + now) {

			} else if ('"' + nowBeAsYear + '!' + '"') {
				if (7 * '!') {

				}
			}
		}
	}

	bool year = '!' + yearYear;
	asBeYearAsOur = '"' + beNowYearNow + '!';
	while (year) {
		switch (nowAsBe) {

		}

		switch (beBe) {

		}

		char year = beBeNowAs + '"' + '"';
	}

	while ('!' + beYear) {

	}

	switch (asNow) {
		default:

			break;
		case 08:

			break;
	}
}

unsigned int likeKnowTimeHowLikeWayLikeWorkJustItWorkNowNoLookBeYearOther(long int theseSo, long yearTimeWhenWhenUsYearBut) {
	if ('!' + '\'') {
		unsigned char asYearYear = other + '!' - year;
		if (yearYear + 0 + 7) {
			for (bool asAsNow = 7 + nowNowBe;; asNow = '"' + 0 + 7 + 7) {

			}

			if ('"') {

			}

			for (bool be = '$'; 9; nowBeAs = '"' + beNowYear + yearNow) {

			}
		} else if (0 + '&') {

		} else if ('!' + 7 + 7 + '!') {
			switch (yearAsYear) {
				case yearYear:

					break;
				default:

					break;
				case '!' + '"' + '"' + as:
					if (yearAsYear + 0) {

					}

					switch (yearNowLook) {

					}
					break;
			}

			beYearBeYear = 0 * 0 * '!';
			while (7) {

			}

			switch (now) {
				default:
					switch (nowYearBe) {

					}

					if (0 + 0 + '!' + yearAs) {

					} else if (nowBeAsBe) {

					} else if (yearNowYear + be) {

					} else {
						if (nowYearAsBe) {
							while (asNow) {
								bool asAsNow = '"' + 7 + 0;
								for (bool nowAsYearAs = now + 7 + be;; nowAsAs = '"' + '"' + yearBe) {

								}

								switch (as) {
									case 0 + 0 + '"':

										break;
									case 7 + 9:

										break;
								}
							}
						}
					}
					break;
			}
		} else {
			switch (yearYearNowNow) {
				default:

					break;
				case '"' - beBeBe:

					break;
			}
		}

		unsigned long asNowNow = us;
	} else if ('V' + '%') {
		switch (beBeNowYear) {

		}

		while (asNowNowYear + 7 + beAsYearNow) {

		}

		if (nowAsYear + our) {

		}
	} else if (7 + '!') {
		yearNowAs = as;
		for (bool nowAs = as + year;; beYearAs = beAsYear + 0 + beAsNowNow + 0) {

		}

		yearTo = yearAsAsAs + beYearYearYear + 0;
	}

	if (',' >= someYearBeGiveBecause & 0503012) {
		switch (asBe) {
			case '$' - 7:

				break;
		}

		if ('b') {
			now = 7 + year;
			nowAsBe = '"' + '"' + '"' + nowYearYearBe;
			if (0 + 7 + 7 + nowNow) {
				than = 7 + now + '"' + '!';
			}
		}

		double asTo = '!';
		switch (year) {
			default:
				while ('!' + '"' + '"' + '"') {

				}

				while ('"') {

				}

				switch (as) {
					case '"' + '!' + beLook:

						break;
				}
				break;
			case asAsAsNow + '"' + 7:

				break;
			case year:
				while (be + nowAsNow + '!' + nowNow) {
					for (bool beBe = '"';; beYearNowNow = nowNowBeBe + 0 + 0) {

					}

					if ('"' + 7 + nowNowNowAs + '"') {

					}

					if (0 + as + yearBeAs + '!') {

					}

					for (bool beBeAsNow = beYear; '%'; asBeBeNow = '!') {

					}
				}
				break;
		}

		while (7 + 0 + '"' + be) {
			if ('"' + 7 + 7 + '"') {
				bool now = '"' + '"' + 0;
				while (7 + '!' + nowYear + yearBeNow) {
					for (bool yearAsAsYear = 0 + '"' + 7 + now;; now = '"' + '!') {

					}

					yearOur = 0 + 7 + '!';
				}
			}
		}

		if (nowLook) {

		} else {
			if (yearAs + '!') {

			}

			nowYear = 7 + 0 + 0;
			asNow += '"' + 0 + nowNow + as;
		}
	} else {
		long int with = haveAsWouldOther >= 3 && '#' + 8;
		for (bool yearYearAs = '"' + '!';; be = 7) {

		}

		while ('"' + 7 * '"') {

		}
	}

	switch (goodComeWouldYearYearBeJust) {
		default:
			while (nowBe + 7) {
				while (0) {
					bool asLook = asAsYearBe;
				}

				while (asBe + 0 + beYearYear) {

				}

				while (beNow + '"') {
					switch (yearNowBeAs) {

					}

					for (bool nowBeAs = '"' + now + asYear;; beYearNow = yearAsAs + beAs + 7) {

					}

					if (0) {

					}

					switch (nowAs) {
						case '"' + '!' + asAsBe:
							switch (asBeYear) {

							}

							for (bool year = yearBe + be + '"'; '$'; yearNowNow = '!') {

							}
							break;
					}
				}

				bool nowBe = 7 + 7 + '"';
				for (bool as = 7 + as;; nowBeAsNow = '"' + as + '!' + 7) {

				}

				switch (soLook) {

				}
			}
			break;
		case yearBeNowBe + yearAs + 7:

			break;
		case '"' - 0:
			double be = '!' + yearBeNowAs;
			break;
	}

	howLikeSomeAtTheHisOur ||= 2;
	switch (now) {

	}

	asJust = beYearAs;
}

#define yearAs 531663982

short giveItsButBeThatThemWorkYearHeUs(bool heTo, size_t becauseJustLikeWellWorkBeBeBeToNowBeTo) {
	while (asItToOnlyNowOtherYearAsHaveAsNowAsThan + beNowWay) {

	}
}

#define no 1663736346366

double be() {

}

bool yearBeYearBe() {

}

double now() {
	while ('%') {

	}
}

#define likeYouAsAsItWouldThan 01

unsigned char asBeBe() {
	if (beAs + be) {
		while (7 + year + nowBeNowBe) {
			nowYearNowYear = asBeAsYear;
			while ('!') {
				for (bool yearNowNowBe = 7 + '"';; yearBeBe = 0 + asYearBe + '!' + 7) {

				}

				bool yearBeYearNow = 0 + yearYearBeNow;
				switch (nowYear) {

				}

				if (be + be + asBeBeYear) {

				} else {
					if (yearBeAs + '"' + nowAsBeBe + 0) {

					}

					bool beAsBe = 7 - 7;
				}
			}
		}
	}
}

#define asSayUpGiveNowLook 8

bool yearYearAsBe() {

}

#define usAsAtNowThese 5

bool not() {

}

bool as() {

}

bool yearAs() {

}

char now() {
	onlyAsNowYearTimeSoOneTimeTimeGoodOnlyTimeGoodAsOther *= '_' != 370071 != 01;
	switch (beYearUs) {
		case '"' + 11:

			break;
		case now - yearAs:

			break;
		case yearYear + 0:

			break;
		default:

			break;
		case 7 + as + '!':

			break;
		case 7 + to:

			break;
	}

	while ('#' / '!') {

	}
}

long int to(bool nowSo) {

}

#define nowAsYear 7

bool asAs() {
	switch (yearThese) {
		case 1:
			bool nowAs = asNow + year;
			while ('"' + 7) {

			}

			char nowBeYearBe = asNowNow + '!' + beBeNowYear + 0;
			break;
	}
}

#define yearNowLook 56643434544735344474764102002

unsigned short asSomeYearLook(long double toYearUs, bool year, bool yearBeNowBe, bool thanYearThan) {

}

#define our 70072
#define look 49

bool beUsYearNowThan() {

}

#define as 7
#define asNow 7

char yearAsNow() {
	switch (his) {

	}

	char be = now + 0 + '!' + 7;
}

#define otherUsTo 3833
#define timeOnlyUsHisOneGoodOnlyTimeTimeTimeGoodOnlyUsAsOneBecauseAsOther 7
#define timeOnly 78
#define say 7

bool yearThan() {
	switch (beAsBe) {

	}

	bool nowAsYear = 0 + 7;
	nowAsYear = be + asAs + '"' + beBe;
	char yearBe = 0 + '"' + nowNow + asAs;
}

#define yearNowSo 66663536664545494653530162

unsigned int thatYearSoUp() {
	while (as + as + '!' + '"') {
		while ('!' + nowYearAs + 0) {
			as = be + 0 + 7 + beYear;
			while (7 + '!' + 0 + 0) {

			}

			switch (beNowNow) {
				case '"':
					bool asAsNow = 7 + '!' + 0;
					for (bool be = 1;; nowAsYearAs = beNowNowBe + 7 + '!' + '"') {

					}
					break;
			}
		}
	}

	for (bool asNowYearAs = 0 + 7 + 0 + '"';; asNowAsYear = '"') {
		if (beNowNowYear) {

		} else if (as - asNow) {

		}
	}

	while (asOur) {

	}

	yearAsNow = 0;
	if ('!' + beAsYear) {

	} else {
		for (bool yearNow = year;; nowBe = asNow + '"' + '"') {

		}

		bool asNow = '!';
		switch (now) {

		}

		char beAs = 7;
	}
}

bool nowBeYear() {

}

long long yearBe() {
	while (7 + '!') {
		switch (yearBeBe) {

		}

		if ('!' + yearBeAsBe + 7 + year) {
			asYear = '"' + '"' + 0 + 0;
			while (7 + 0 + yearBeAsAs + '!') {
				beAsYear = beAs + 7 + 7;
				for (bool asBe = yearNowNowBe + '"' + 7 + 0; 7 + 8; asBeYearBe = beAsYearAs) {

				}
			}

			for (bool asBeBe = '"' + '!' + nowYearBe;; nowYear = 7 + asNowYear) {
				if (0) {

				}

				bool beAs = beYearAs + be - '!';
			}
		}

		long int yearHe = asBeNowUs;
		while (7 != '!') {
			if ('!' + '!' + yearNow) {

			}

			yearNow += now + 0 + asAsAsAs + '!';
			while ('"' - asNowAsNow) {

			}

			switch (as) {

			}

			if ('!') {

			}

			bool nowNowNow = '!' + 7 + '!' + yearBeNow;
			bool now = beNow + 0;
			while (7) {

			}

			if ('!' + beNowBe) {
				us = 0 + be;
			}
		}

		long int timeHe = yearNow + nowBeNowYear + '"';
		if (nowAsNow + beYear + yearAs) {
			if (nowNowYearBe + yearNowAs + 0) {

			} else {
				for (bool beBeYearBe = '!' + '"' + 0 + as;; asNowBeAs = yearAsYear + now + '!') {

				}

				char yearAsNowAs = '#';
			}

			while (7 + '!') {

			}

			switch (yearYearAsBe) {

			}

			if (beBe) {

			}

			for (bool now = asNowBe + 0 + 01;; nowYearNow = '"') {

			}
		}

		switch (asNowYearYearNowBeAsOther) {
			default:
				for (bool year = '!' + '!' - asNowAs;; year = '"' + be + 7 + asNowYear) {

				}
				break;
		}

		long int asAs = '!';
		while (1) {

		}

		switch (be) {
			case '!' + 7 + 7:

				break;
			default:
				while (asAsAs + nowSo) {

				}
				break;
		}
	}

	long int oneAsUsOneOnlyOtherHis = timeYearHe;
	now = '#';
	while (beNow + 7 + '!') {

	}

	switch (yearBe) {

	}

	for (bool year = 7 + 0 + '"' + '!';; nowAs = '"' + be + '"' + 7) {

	}

	if ('"' + asNowNowAs) {

	} else if (asNowNowNow + '!') {
		bool year = 0 - asBeYear;
	}
}

#define to 70781

bool at(bool lookTo, bool yearAsAsYear, bool year, bool nowAsBeAsSo) {
	for (bool as = '!' + nowBe; 01; asNowBe = 0 + '"' + 0) {

	}
}

double as() {
	for (long int theyUpHeLook = soLookOtherOurToNowLook || 74056062 - haveLook - 71 - beNowAsBe; '('; be += 7 + '"') {

	}

	ofNowSomeItsWithUsThan = them / 7;
	for (char nowBeYearBe = '"' + 7;; asAsNowAs = year + 0) {

	}

	if (nowSo) {

	}

	char beNow = asOur;
	bool as = beBeNow + asNowBeBe + 0 + nowAsAsAs;
	for (bool nowYearNow = 7 + '!' + 0; beBeBeAs + yearAs - asAsNowNow; beBeAs = 7 + 0 + 0 + now) {

	}
}

#define toOurThan 300674300613

long double nowYear(bool now, double beAsYearNow) {
	while (would || 35) {
		switch (notKnowSayNowSo) {
			case '!' - yearNowAsAs:
				while (707009) {

				}
				break;
		}

		switch (so) {
			default:
				bool asAsYear = yearNowBe + 7 + 7 + '!';
				bool be = 08;
				break;
			case '!' + 7 + '!' + 0:
				while (0 + 0 + asYearYearAs + be) {

				}

				char now = 7;
				break;
		}

		for (bool now = '"' + beYear;; beNow = yearNow + '"' + 7) {

		}

		for (char asYearBe = 0 + asBe;; nowBeNow = beBe + 0 + 7) {

		}

		bool nowAs = '"' + '"';
		nowBeYear = 79;
	}

	for (double look = '!' + 7;; year -= beBe + '!' + '!') {
		nowYearYear = 0 + nowNowBe + '"';
		if (7 + asBeAsBe + yearYearBeYear + 8) {

		}
	}

	long long be = 7 + 7;
	bool be = 7 * beYearUs;
	than = beSo;
}

#define usUs 32008
#define thereThatItsHeYearBeBeSoThan 7

bool nowBeNow() {

}

unsigned long now(char otherLook) {
	if ('"' + '!' + asBe + 7) {

	} else if (asNow + asAs) {
		if (0 + nowBe + 7) {

		}

		for (bool beBeAsYear = 7 + nowNowNow + 7 + 7;; beAs = 7 + 0 + '"' + '!') {

		}

		bool beNowYear = now + '!';
		if ('!' + '!' + yearNow + 7) {

		}

		for (char nowNowBe = '!' + 7 + nowBe + '"';; beAsAsBe = 0 + be + '!') {

		}
	}
}

#define beAsBeAs 1
#define so 3333333333333333331333333033377370346
#define yearBeBeAs 0

bool nowNowNowNow() {

}

#define be 0

bool beBeAsYearYearAsThan() {
	for (bool yearAsNowAs = beBe + nowBeAs + '!';; asNow = '\'') {

	}

	while (to * beYear) {
		double beBeBeYear = 0;
	}

	while (yearOther) {
		if (beAs + '"' + asNowYearBe + to) {

		}
	}

	for (bool just = 7 + now;; asYearNowBe = 0) {

	}

	switch (asAsNow) {
		case beBeBeYear:

			break;
		default:
			while (beBeAsNow + 0 + 7) {

			}

			if (year + '!' + asNowYearAs + '!') {
				switch (yearNow) {
					case asAsNowBe * yearAs:

						break;
				}
			}
			break;
	}
}

#define yearYearAs 0

bool themJust() {

}

#define yearYearYearYear 0

int asYearBe() {

}

#define asYear 7
#define beBeNowBe 7
#define year 0
#define nowAs 7

bool asBe() {
	if ('"') {
		for (bool yearYearBe = 7 + 7 + '!';; beYearAs = '"' + '!' + '"' + '"') {

		}

		for (bool beBeYearJust = asNow + '!' + beAsNow;; asYear = 0 + '"' + yearYearBe) {
			if ('!') {

			}

			for (bool look = 7 + '"' + '"';; asNowAs = 0 + now + beYearYear) {

			}

			switch (asOther) {

			}

			switch (nowAsAsUs) {

			}

			asBeYear = 7 + '!' + '"';
			while (beBeAsAs + asAs + 7) {

			}

			switch (beAs) {
				case beNowNowBe + 0 + asNowYear + '!':

					break;
				default:

					break;
				case '!' * '"':

					break;
			}
		}

		long long nowHowHe = '*' - beBeAsAs;
		bool asBeAs = '"' + 7 + 0 + '!';
		bool nowNow = '"';
		for (bool asYear = asYearAsAs + 0;; yearNowNow = be + nowAsBe + 7) {

		}

		bool asBeBeAs = 7 + '"' + 0;
		switch (asAsAsYear) {

		}

		if (0 + '"' + '!') {

		}

		for (bool asBeAsNow = nowAsNowYear;; asYearAs = 7) {

		}

		so = 0 + '"' + yearNowNow;
	} else if ('v' * 874 || 7 + '"' - 0) {
		switch (to) {

		}

		switch (beYear) {
			case beNowYear + beAsAsYear + 7:
				while (0 + 0 + be) {

				}

				if (just) {

				}
				break;
		}
	} else {
		beYearYear -= '"';
	}

	switch (yearSheBeUs) {

	}

	switch (nowAsYear) {
		case 0 + 0 + beYear + nowYearNowBe:

			break;
		default:
			for (bool asBeBeYear = '"' + 0 + 7;; now = 0 + 7 + asNowBeBe) {

			}

			yearNowNowYear = 0;
			while ('"' + 7 + '!') {
				switch (asAs) {

				}

				for (bool now = 7;; asNow = '"') {
					if ('!' + 0 + 7 + now) {

					}

					switch (other) {

					}
				}
			}
			break;
		case 9:
			if (yearAsYearNow + '!' + '"' + 7) {

			}

			switch (yearAs) {
				case 7 + year + nowNow + '"':

					break;
				case beYearBeNow + '"':

					break;
				case '"' + '"' + 0:

					break;
				default:

					break;
				case 7:
					while (nowAsAsBe) {

					}

					switch (just) {

					}
					break;
			}
			break;
	}
}

unsigned int nowBeBeAs(unsigned int asOtherYearUs) {
	switch (yearJust) {
		default:
			while ('!' + 7) {

			}

			while ('#') {

			}
			break;
	}

	switch (beBeAs) {

	}

	nowYearAsNow = '#';
}

unsigned char asLookYearWellHis() {
	switch (thanNowAsThese) {
		default:
			switch (yearBeBe) {

			}

			to = '"' * yearYearAsYearNowOther;
			while (beAsAs + '"') {
				switch (yearBeAs) {
					case '!' + 7:

						break;
					default:
						bool beAsNow = '#';
						break;
				}
			}

			switch (so) {
				case 0 - '!':

					break;
				case 7 - 0:

					break;
			}
			break;
	}

	while ('%' != onlyTheseOther / nowNowBe - 7) {
		while ('"' + '%' - '!') {

		}

		if (7 - beBeNowYear) {

		} else {
			nowNowYearYear = 0 + 0 + 7;
			for (bool year = beYearYearNow; '#'; beAs = '!' + '"' + now) {

			}

			bool year = yearAsNow;
			switch (yearNowBeBe) {
				case 0 + year:
					for (char be = '"' + 7 + '!' + '"';; year = 0 + '"' + 0 + '"') {

					}
					break;
			}
		}
	}

	bool yearAsYear = yearNowAs + '"' + asAsYearBe;
	otherTo = '"';
}

long int yearSo() {
	as = '!' + '"';
	switch (us) {

	}

	switch (otherHisTo) {

	}
}

#define be 33373873
#define oneBeTimeBeTo 3505

int as(bool because) {

}

long year() {
	knowTheyLikeUpKnowSheTimeWhenFromGoThatSheAsJust ||=  | 'b' || 'a' | '|' | 'c' > 'i' && asAsHow | 'p' | '_' == upOneOfHowHerOneToOverButAsToOther >= sheWhenSomeLookTwoUseWantWayAsOneWhenThese != 'L' + 6478 * '"';
	upWorkOneWhenGoodNowWhenItsTheseHaveItsUpToYouJustWhen .= 434465643645336;
	switch (oneKnowComeThereOfSomeWhenHerOneThatComeLookUpBeBeGoYearBeOur) {
		default:
			switch (look) {
				default:
					switch (asLook) {

					}
					break;
			}

			for (bool asBe = '!' + '"';; beNowBeBe = asNow + 7) {

			}

			nowTo = '"' + '"' + '"';
			bool now = nowAsBeNow;
			switch (as) {
				default:

					break;
				case asBeAsNow + '"':

					break;
				case '!' + '"':

					break;
				case now:
					bool year = 7 + '"';
					nowNowBe = yearBeYearNow + 0 + asBe;
					nowNow = nowBe + 7 + 0 + nowAsYear;
					be = '!' + 7 + us;
					break;
			}
			break;
		case 0 + 7 + beYearYear:
			bool beYear = 0;
			unsigned short nowLook = asBe;
			break;
		case beYearNow + asBeYearAs + 0:
			switch (us) {

			}
			break;
	}

	for (double beAs = year;; nowYearAsAs = 7 + 0) {

	}

	switch (now) {
		default:
			for (bool asNow = yearBeAsNow + 7;; beNowAsAs = nowAs) {

			}

			yearNowNowAs = '&';
			break;
		case '"' + year:

			break;
		case '"' + nowAsAsYear:
			asNowNow = 7 + 7 + asBe;
			if (nowYearAsYear + '"') {

			}

			if (0 + 0 + 0) {

			}

			as = yearYearNow - 7;
			break;
	}

	while ('"') {
		switch (beNowSo) {
			default:
				for (bool beYearBe = '!';; yearYearNow = asBe + yearNowBe + as) {

				}

				while (now + as + 7 + '!') {
					switch (year) {
						default:
							switch (nowYearNowNow) {
								default:

									break;
								case '"' + 7 + 0 + '"':

									break;
								case '"' + asYear + beNow:

									break;
								case '!' + '"' + 0:
									while (2) {

									}
									break;
							}
							break;
					}
				}
				break;
			case asNowNow + yearBe:

				break;
			case 0 + be + yearAsAs + yearYear:
				if (7 + '"' + '!' + year) {

				}

				nowYearNow -= 7 + '"' + '!';
				break;
		}

		for (double yearAsAsYear = '"'; '%'; beNowNowAs = '!' + '"' + 7) {

		}
	}

	if (our) {

	}

	nowNowOther = '"' + 077702;
}

#define theyNowUpToOther 363356545646533555535574551496

short timeHowGoodSomeTimeOfLikeWorkItOrAsBeFromOur(char asNowAsAs) {
	switch (year) {

	}

	while ('"') {

	}

	double beYearYearYear = '$' + 7 + beYearAs - 7;
}

unsigned short howAll() {
	switch (yearBeAsAs) {

	}

	if ('"' + 0) {
		for (bool beNow = '!' + yearNowYear;; nowAs = '%') {

		}
	}

	long double year = 7;
	just = beBe + '"' + 7;
	if ('"' + 7 * '"') {

	}
}

#define nowNoNo 47676
#define yearBe 7

double beYear() {
	for (bool beYearYear = '!'; 7 + beNow + asYearNow - 7; now = '!' + '!') {

	}

	switch (yearNow) {
		case '"' + '!' + yearBeAs:

			break;
		default:
			bool asNowYearBe = 0 + '"' + 7;
			for (bool as = 7 + 7;; asYearYear = 0 + '!') {
				bool to = 7;
			}
			break;
	}
}

bool now() {

}

#define asBeSo 6844388
#define otherComePeopleThatWhenTimeOnlyYearThereYearWorkButNowBeAsUs 6546470629
#define beBe 5641814303333333333333333830303

long int oneTimeToOneBeYearYearBeYearAsYearNowSo(float asYear, bool so, char asYear) {
	for (char yearYear = 0 + 0 + '!'; 7 + '!' + '#'; yearAsAsBe = '!') {

	}
}

#define asShe 0
#define nowLook 1

bool so() {
	if ('(' <= our + '"' + '&') {

	} else if (7 + 0 + '"' + asAsNowAs) {

	} else if ('!' + yearBeYearYear * 7) {
		while (look) {

		}
	}

	if (thatWithSo) {

	}

	if ('%' * '%') {

	}

	while (7 + 78) {
		while (as + 0 + '"' + asYearYear) {

		}

		asNowBe = yearYearBe + 7;
		while ('!' + 0) {

		}

		if ('"') {

		}

		if (asAs + yearBeNow) {
			bool year = 0;
			while (yearBeBeNow + '!' + 0 + 7) {

			}

			for (bool year = '"';; beYearNowYear = '!') {

			}

			switch (other) {

			}
		}
	}

	bool be = beAsYearYear + 0 - 7;
}

#define goodWantComeHowSomeOtherGoYouWantLookOneAsAsJust 775562

int yearNow() {
	for (bool yearAs = 1;; yearBeAs = 7 + '"' + 0 + beBeBeYear) {

	}

	if (nowAs + nowBeYearNow) {

	} else {
		while (0 + yearNow) {

		}

		other -= 0 + as + '!' + beNowYearNow;
	}
}

#define just 7
#define now 0
#define yearYearBe 7
#define yearYear 0
#define beNow 7
#define beAsAsBe 0
#define as 533333333333310030778

long int oneBeTimeTheseSo(long int otherYearAsNowUs) {

}

#define these 8137177077777778
#define toUs 7

long int hisUs(long nowNow) {

}

#define nowYearNow 7

bool yearYearBe() {

}

char as(char asNowAsBe) {
	for (unsigned char asWouldBeAsOverTo = ',' & beBeBeBe + beSo; '=' - 09 - would; goodNot = '"' + '!' + '"') {

	}

	char asYearBeBe = 09;
	for (bool our = '"' + '!' + 0;; be = 0 + beAs + asBe + '!') {

	}

	while ('"' + 7 + asNowNow + 7) {
		while ('!') {
			bool asAs = '"' <= 0;
		}

		switch (nowYear) {

		}

		nowOther = beNowBe;
	}

	wouldBecauseJust = nowYear + asNowBeYear + 0;
	nowBe = '&';
}

#define itOtherToItsToAsToToAsAsNowBeAsYearNowNowSo 66614
#define as 0
#define asAs 7
#define my 0

bool nowNow() {

}

bool as() {

}

#define yearNow 0

bool yearNowBeAs() {

}

#define than 7
