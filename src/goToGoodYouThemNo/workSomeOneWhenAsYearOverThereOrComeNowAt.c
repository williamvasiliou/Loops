long int oneOtherHeSoBeYearOtherYearHe(bool beToBecause, char he, long int goodYearYearOneOneHisOnlyToAsAsSo, char these, long int nowHe, bool beNowSo) {
	for (char beBeNow = be + beAsBeBe + yearAs;; asAs = 7 + be + yearYearYearNow) {

	}
}

#define yearNowYearNow 301

float than() {
	beNowOther = yearBeAsYear + '!' + nowAsNowBe + us;
}

double asUs() {

}

#define butAsOurOther 7

int asBeYear() {
	lookOtherKnowHaveOverHaveUpTheUseOnlyLikeKnowOurWellThereYearItHowTheTheSayGoodNowHaveBecauseUs &&= '}' + 316 / 722 - now;
	wellPeopleWorkSayLikeOnlyUseHowThatItWhenHowSomeOverGoWorkUpWorkWantItOfPeopleLikeOverTimeGoOrOneWhenComeHerLikeSayOfWorkItsItTimeSayThereWhenSomeSomeSomeOverWhenTimeHaveOrWhenSayGoTheseNowUs ^= 3533666320366547666708 == 'f' >= haveUpUseUpAtLikeWork + yearBeAs * '"';
	switch (goWantUseWantOrThereKnowUsKnowWantTwoOrOfUpUpOrWorkWellWellWantPeopleWhenGoBeThereTheKnowItsPeopleYouOverKnowOfOrSomeKnowTheGoLookUpThereGiveWhenThatWellWhenUpNotKnowWellThanYearThanYearLook) {
		default:
			howSomeWorkOrOverSheComeItGoPeopleLikeWhenComeSheWorkOnlyWellSheWorkTheLikeKnowWellSheWhenOnlyWantOfGiveWorkNowSoFromJust .= 402 >= thanGiveAsUsWould;
			if (6444366692 <= 2 + '"' * beYear) {
				if (7 + 7 + nowBeYearAs + yearYearNow) {

				} else if ('!' + '"') {

				} else if ('!' + as + '!' + 7) {
					double asBeNow = '!' + '"' + beAsBe + 0;
				}

				switch (yearAsThan) {

				}

				while (01 * '"') {

				}
			}

			if (all + 61) {
				asBeYearBe = 7 + asNow + '"';
				for (char beNowNow = asYearAs + year + '"';; nowAs = '!') {

				}
			} else {
				switch (now) {

				}

				beYearNow = '!';
				if (0 * 0) {

				}

				beAs = 7 + 0;
				as = as + '"' + 7 + beBeYear;
				switch (be) {
					case nowAs:

						break;
					default:
						switch (asNow) {

						}

						if ('!') {

						}

						for (bool nowYearNowNow = as + '"';; as = asBeNow + beAsNowYear + 7 + 0) {

						}

						while (beNowYear - be) {

						}
						break;
				}
			}

			for (bool not = year * '!'; '$' + asBeUs; beBe = '"') {
				bool to = 7 + 0 + 0 + '"';
			}

			for (long double nowYearYearAs = '!' + asBe;; asTo = 7 + 0) {

			}

			while ('"' + 0) {

			}

			beYear = '"' * '!';
			break;
		case 456055666591709001:
			switch (lookNowAsJust) {
				case be + beAsAsYear:

					break;
				case asYearBe + '!' + nowYearBe + 7:
					while ('"' + now + 7) {

					}

					bool asNow = 7 + 0 + '!' + 0;
					for (char asAsAs = beBeNowYear;; be = '"') {

					}
					break;
				default:
					nowYearBeNow = 7 + as + '"';
					switch (to) {

					}
					break;
			}

			if (5427645109 - 7) {
				switch (look) {
					case 075:
						if (yearBeNow + '!' + 7 + 0) {
							for (bool year = '!' + '"' + asYearAsYear + 7; 708; year = '"' + 0 + 7) {

							}
						}
						break;
					case 0 - now:

						break;
					default:
						bool yearYearAsYear = 0 + 7 + 7;
						while (beBeBe + '"' * '!') {

						}

						switch (year) {

						}

						switch (beMy) {

						}

						bool as = '"' + now + '!';
						for (double asAsBe = '"';; beYearYear = nowAs + '"' + 7 + '"') {

						}
						break;
					case 7 + 0:

						break;
					case '!' + '&':
						bool just = 0 + 0 + '!' + 0;
						break;
				}

				asOther = beNowBe + '!';
				if (0 + 7 + '"') {

				} else {
					for (bool nowBeBeYear = asYearYearNow + 7 + 0 + 7; beAsOur; yearYearAsNow = 0) {

					}
				}
			} else if ('%' + '&' + 9) {
				while (as * '"') {

				}
			} else if (53768 + 02) {
				switch (butSoOurAsTwoOther) {
					default:
						switch (other) {

						}
						break;
				}

				yearLook -= year + 0;
				yearNowBeBe = 7 + nowBeYearYear + be;
				if ('"' + '"' + 7) {

				}

				while ('"' + '"') {
					while (0 + '"') {

					}

					beNowBe = 8 + yearYearAsYear + '$';
					for (double now = yearBeBe + as + yearBeYear + 7;; yearYearBeNow = asNowYearBe + '!' + 7) {

					}
				}
			} else {
				bool us = '"' + 7 + as + 0;
				year = '!' + 0;
				switch (beJust) {

				}
			}

			switch (asAsAs) {
				default:
					bool now = 0072;
					break;
				case 0:

					break;
				case asOther:

					break;
			}

			if (0 + asNowBe + now * '"') {
				if (7 + yearNow + '"') {

				}

				as = yearYear + nowNowYear + 7;
				if (0 + as + 0) {

				}

				bool asNowBeAs = '"' + nowAsBeYear + 0;
				while (0 + '"' * beAsBeAs) {

				}
			} else if ('%') {

			}

			yearBeTo = as + 7 + '"' + '"';
			break;
		case upHowOfWellOfUseTheUpWorkWithSheWantGoodWhenUseLikeWantOverGoTheWhenKnowYearComeWantHerUpHaveWellTherePeopleItLookSayKnowTheTwoWhenPeopleHaveLookThereWouldThan | upWantAsBeToOther >= '"' || these + 4600077072:
			for (bool asYearAs = '!' + '"';; beYearBeAs = '!' + '!' + 7 + 0) {

			}

			beAs = beAsYearNow + '!';
			if ('!' + '"') {

			}

			bool other = '"' + be + beNowBe;
			break;
		case 7 - 0:
			for (bool asYearNowNow = 7 + '!';; beNowYear = '!' + 7 + nowAsBe) {

			}

			switch (asYearNow) {
				default:

					break;
				case yearNowBeBe + '!':

					break;
				case 0 + 0:

					break;
				case asYear + be:

					break;
				case asThan:

					break;
			}
			break;
		case asNowBe + 5 + 2:

			break;
		case '!' + 0:
			for (double beNowNowYearOur = yearYearBe;; yearYearYearYear = 7 + '!' + '!' + beBeNow) {

			}

			while (asAsBeYear + nowAs + 0) {
				for (bool yearBe = nowNow + '!' + 0;; beAsAs = 0 + 7 + yearAsNowYear + beBeNowBe) {

				}

				for (bool yearYearNow = nowNow + '!' + yearAsBe + '!'; 72; yearBeAs = '"') {

				}
			}
			break;
		case as + year + 0:

			break;
		case 0:
			bool nowSo = nowAsTo;
			bool nowBeJust = 7 + 0 + '!';
			break;
	}

	for (bool asLookOther = 7 + 7 + '!' + '!'; justBeBeOther | usBeJust * '!' + '!' - 7; now -= as) {
		if (7) {
			for (bool beYear = beYear + '!';; nowAsNowNow = year + '!') {

			}

			bool beNowBe = 7 + asBe + asNowBe + '!';
			switch (yearYearOther) {

			}
		}

		if (now <= yearBe) {
			char beBeBe = asAsBeAs + '!' + '"' + '!';
		}

		while (22 + yearOther) {

		}

		while (beBeAsBe + 0008) {
			other = yearNowBe + asYearAsBe;
		}

		switch (than) {
			default:
				for (bool beJust = be + '"';; nowBe = 0) {

				}
				break;
		}
	}

	comeOfNoOfHerGoGiveOrThanWhenComeWantItTheWantItWantComeTheUseGoSheWhenGiveGoodYouWantOnlyHerItWorkBeTheOfOneGiveOrThemSheWantSayUpUseSayWellOverLikeComeWellPeoplePeopleOfKnowUpItsComeAtItsTwoWantPeopleThemThan &&= 44760330606 & 70977007702;
	switch (haveHerWhenPeopleItsLikeKnowOverUseWorkOrWhenThatHaveTheSoThan) {
		case '"' * 7:

			break;
		default:
			switch (beYearNowAs) {

			}

			bool asBeBe = 7;
			switch (beAsAs) {

			}

			while ('!' + '&') {

			}

			while (as + '!' + '!') {

			}

			bool yearAsYear = yearAsYear + 0;
			while (yearAs) {
				beBe += '"' + yearBeBe + '"';
			}
			break;
	}

	for (double beNowBe = '!';; beAs = now) {

	}
}

#define us 33674370446426

size_t workOverNowGoodUpUseBecauseWorkHerWorkKnowKnowWhenSomeHeOnlyBeJustIt() {

}

long withUs(bool nowNowBeNowAsYearJust) {
	switch (be) {

	}

	switch (asThan) {

	}
}

bool asBe() {
	switch (now) {
		case 79:

			break;
	}
}

bool our(double beYear, bool as, bool yearNowSo) {
	switch (themAsOther) {
		default:
			switch (nowBeBut) {
				default:
					nowBe = 7 + '!' + '"' + 0;
					switch (so) {
						case '"' + yearYearYearNow + 0:
							while (yearYearNowAs) {

							}

							yearBeNowYear = 0 + nowYear + asNow + beAsAsBe;
							switch (as) {
								default:
									switch (nowNowNow) {
										case beAsAs + 0 + '"' + '!':

											break;
										default:

											break;
										case '!' + now + '!' + 7:

											break;
										case '"' + beBeBe:
											switch (so) {

											}
											break;
									}
									break;
							}
							break;
					}

					for (bool asYear = beBeAsNow + '"';; now = 0 + 0 + 7 + '!') {

					}

					nowYearBeYear = '!' + asAsBe + '!';
					if ('!' + 7 + 0) {

					} else {
						bool beNowBe = '"' + yearAs;
						while (yearYear + 7 + nowBe + year) {

						}

						switch (be) {

						}

						switch (as) {
							default:

								break;
							case asNow + '"' * 7:

								break;
						}
					}
					break;
			}
			break;
		case other:
			while (asAsYear + be + '!' + '!') {
				switch (asAs) {
					default:

						break;
					case nowYearAs:

						break;
					case 0 + 0 + '"' + 0:
						nowYearBe -= '!' + year + now + 7;
						break;
				}
			}
			break;
	}
}

unsigned int wayNowItsOneUpWorkUseTimeComeSheWhen(bool than, bool beOur) {
	if (would < '!') {
		while ('!' + 7) {
			if (toAsAll + '!' + than) {

			}

			for (char year = 7;; beAsAsNow = '"' + yearAs) {

			}
		}
	}

	switch (be) {

	}

	if ('c' * 1) {
		if ('"' - asNowYear) {

		}
	}
}

#define these 233333371
#define timeOneOnly 111
#define timeOnlyOneTimeOneNowBecauseOneBeOtherAsYearYearYearOther 331
#define hisHis 778
#define justNowSo 6
#define asThan 7

bool now() {

}

#define yearNowNow 7

float yearAsBe() {
	if ('}' == '!' + workOf + 0 - '!') {
		while ('"' || 773) {

		}

		char nowNow = '!' + 7;
		bool be = 7 + asAsBeAs;
		beBeAs = '#';
	} else if ('"') {

	} else {
		while (asNowNowBe * 0) {
			asAs -= asNow + '!';
		}

		if ('!') {
			while (asNow + asNow + 0) {

			}

			switch (soOurThan) {

			}
		}

		just = '"' + nowBe + now;
	}

	if ('s' | yearKnowBeAsOurUs) {

	} else {
		if (yearYear + 078) {

		} else if ('"' + '!' + yearAs + beAs) {

		} else {
			for (bool beBeBeYear = '"' + yearBeYearYear;; as = yearYearYear + beNow) {

			}

			bool asYearYearBe = 7 * 0;
		}

		switch (asKnowBeThanOther) {

		}

		bool lookSo = asYearBe + 7;
		nowBe -= asAsYearNow + nowNow;
	}

	for (double beYear = beJust;; asYearBeAs = 0 + asBeBeNow + 7 + '!') {

	}
}

unsigned int asNowThere(long double other, char yearBeWith, bool than) {
	asToUs = 7 + nowNowYear + '!' + '!';
	while ('!' + 0 + 0 * asAsYearAs) {
		if (7 + nowNowBeTo) {

		}
	}
}

#define nowGoOneAsLook 30761
#define beYearBeYear 0

size_t beBe() {
	yearBeOther = be + nowYear;
	if (yearBe + '"') {

	}

	for (bool asAsBeBe = asYear + 7;; beNowAsBe = '!' + beBeAsYear + 7) {

	}

	while ('"' + 0 + asNowBe + be) {

	}

	bool yearNow = asYear + 0 + 7 + 0;
	just = 0;
}

bool beNo() {

}

#define nowBe 2
#define beBe 7

bool asAsAsNow() {

}

bool yearBeAsNow() {

}

#define them 35543376355435356686545683031777071
#define year 3
#define hisThatHis 22

char asNowNow() {
	onlyTimeGoodTimeOnlyOneGoodTimeOnlyOneOnlyToHisUs |= '"' + '!' + 7 + 0;
	long int yearAsAsAs = now + beAs + yearNowNowYear;
	bool soOne = 0;
	switch (asNow) {

	}

	long int asYear = 7 + beNow;
	usOther = beBeAsAs - '!';
}

#define usTimeYearYearLookNowBeYearOur 0

float no() {

}

#define asYearNow 7

bool year(bool nowLook) {
	if (justSo * '!') {

	}

	switch (yearYearAsBe) {
		case '"' + '"' + '!':
			bool nowYearBeSo = 0 + '!';
			if ('!') {

			}

			double beLook = beYearYearAs + 0;
			break;
	}

	while (year + '!' - nowNowNow) {
		be = yearAs + 7 + '"' + '!';
		switch (so) {

		}
	}

	nowYearBe = beBe + '!' + beAsNowBe;
	while ('"') {
		while ('!' + '!' + 7 + 7) {

		}

		switch (than) {

		}
	}

	for (double nowNowNowAs = '!' + '!';; nowBe = 0 + now + '"' + '"') {

	}

	if (yearAs + beNow + '!' + nowNowYear) {
		nowYear += 7 + nowAsYearNow + 0 + yearNowNow;
	}
}

#define ofGiveTwoKnowUseYouHerWorkNoKnowOtherNowYearPeopleSomePeople 5564634456018
#define usBeNowYearJust 54564556565464536343666561441476653381708

long long theyHeNoYearThey(bool yearYearAsBe, bool asYearBe, bool yearYearNow, bool be, char year) {

}

int yearNow(bool so, bool nowYear, double yearAsNow, char beAsYear) {
	bool yearNow = nowBeYearAs;
	while ('!') {

	}

	for (bool as = '!';; asBeYear = 0 + as) {

	}

	for (char beAs = yearAsAsNow;; nowYearAs = 7) {
		for (char as = '"' + '!' + 0 + 7;; nowNow = nowYearAs) {

		}

		if ('!') {

		} else {
			yearAsAs = '!';
			while (be - '"') {

			}

			for (char asNowYear = 0 + 7;; yearAsNow = '"' + '"' + yearYearBeYear + 0) {

			}

			yearAsYearBe = nowYearYearYear + 0 + 0 + yearBeAsBe;
			if ('!' + 7 + asYear) {
				for (bool to = beAs + yearNowAsNow + '!';; nowYear = '"' + '"') {

				}
			}
		}

		switch (toAsBeNowYearTo) {
			default:
				char yearBeNow = as + 0 + '"' + 0;
				break;
			case other:

				break;
			case beYearOther:

				break;
			case soAsTo:

				break;
		}

		while (now + beYear) {
			while (nowAs) {
				if ('!' + as + 0 + beNow) {

				}

				if (nowNowUs) {

				}
			}

			bool nowYearAsYear = '#';
			while (now + 7 + 0) {

			}

			for (bool beNowNowNow = 0;; beBeAsYear = '"' + '"') {

			}

			other = '!' + 7;
		}

		for (bool be = '"' + nowAsYear; yearYearNow - '"'; nowTo = '"') {
			yearAsUs = asNowAsNow + 7;
		}

		switch (beAs) {

		}

		he = be;
	}

	while ('7' + '#' + so + other) {
		for (bool asYearNowYear = '"' + '"';; beYear = 1) {

		}

		if ('#') {

		}
	}

	for (float nowAsAsYear = beYear + nowNowNow; to != beNowNow - '!'; asYearYearAs += '"' + asYearNowBe + '!') {
		switch (nowAsNow) {

		}

		if (yearNow + '"' + '!' + '"') {

		}

		yearOther = 7 + be + 7;
	}

	bool be = '"';
	while (beYear + 0 + '"') {

	}

	nowBeYear = yearYearNow + nowYearAs;
	while (7 + 7 + yearBe + 7) {

	}

	while (01) {

	}
}

#define asSoAsAsUsAsHis 31

long int soTo() {
	if (0 + '"' + 0) {

	}

	switch (be) {
		case '"' + 0:

			break;
		default:

			break;
		case asNow:
			while (7 + beNowNowBe + year + asNow) {

			}

			beYear = 7 + nowAsAs + 7;
			for (bool yearYear = 0 + '!';; beAsBeBe += yearNowBeNow) {

			}
			break;
	}
}

#define nowToAsYearOnly 884711
#define yearAsBeNowOur 7

bool be() {

}

bool yearNowUs() {
	if (0 + '"' + 7 + 7) {
		beBeYearNow = nowNow + now + 7;
		for (bool other = '"' + '"';; asNowNow = 0) {

		}
	}
}

bool nowAsNow() {

}

bool nowBeAsLook() {
	while ('"' + 7 + 0 + 7) {
		if (yearNow + nowYearBeYear + '!' + as) {
			switch (beYearNowBe) {

			}

			bool now = '"' + '"' + nowAs + as;
			if ('"' + '"' + beNowBeNow + 7) {

			} else if (nowAsYearYear + '"') {
				switch (yearAs) {
					default:
						asBeYearNow = 0 + 7 + as + 0;
						while ('!' + '!') {

						}

						switch (yearBeAs) {
							default:

								break;
							case be:
								while ('"' * '"') {

								}
								break;
						}
						break;
				}
			}

			while (asAsNowAs + '"' + 7) {

			}

			for (bool beBeAsBeUs = nowYear;; beBe = 0 + '!') {

			}
		}

		switch (be) {
			default:
				switch (asNowNow) {

				}

				our = yearAsBeYear + 0;
				break;
			case 72:

				break;
		}

		if ('"' - year) {

		}

		if (0 + 7 + 0 + '"') {

		}

		long double beBeAsOur = yearAs + asAsAs + '!';
	}

	long long nowWantThan = 91;
	long long beYearYear = '!' - 0;
	if (beYearYearNow + 0 + 0 + asBeNow) {

	}

	while (7009) {

	}
}

#define herComeUpItAsComeAsYearAsAsYearAsSo 874978
#define hisHow 576

long long peopleWantWouldBeBeLook() {
	switch (asLook) {
		case 7 + '"':
			switch (asAsYearAs) {

			}

			if (nowUs) {

			}
			break;
	}

	beNowBe += '!' - '"';
}

double nowNowBeYear() {
	beBeAs = yearYearYearBe + 7 + 0 + asYearNowBe;
	switch (beYear) {
		case 7 + '"':

			break;
		case be + '"':

			break;
		case yearAsNow + asYearAsNow + '!':
			nowLook = year + 0 + 0;
			break;
	}
}

#define yearBe 0
#define asJust 3655435634663655346345435528539

bool beYearNowNowTo() {
	switch (as) {

	}

	while (lookToBeThan) {

	}
}

#define beBeBeNow 71

float yearBeBeNow() {
	oneGoodTimeTimeGoodOneOnlyTimeSoOnlyOneBeHeGood = beAsAsNow;
	if (3 + other) {
		while (beYearBe) {

		}

		while (0 + 7) {

		}

		for (bool beBeNowYear = 7 + us;; now = beAsBe + nowNow + 0 + beBeNow) {

		}
	} else {
		while ('"' + be) {

		}

		if (as + '#') {

		}
	}
}

bool yearAsAs() {

}

long int oneNowOneUsNowSo() {
	yearYear = '#';
	while (7 + 7 + yearBeAs + '"') {

	}

	while (0 + 7 + '!' + '"') {

	}

	bool now = nowBeYearBe + '"' + as + yearNow;
	while ('!' + yearNowAsYear + '"' + beNowBeBe) {

	}

	if (beBeYear + 8) {

	}
}

#define now 0

bool theseOnlyToBeYearAsAsBeUs(double year) {

}

char yearTo() {
	while ('"') {

	}

	while ('"' + asNow) {

	}

	double nowNowYearYear = as + 7 + be + 0;
	double as = '!' + 7 + nowAsNow + 0;
	asBe = year + 7;
	bool asYearBe = year + '!';
	while (asOurBeThan) {
		overLookOfTwoWantKnowNowThanNowYearBeUs += '!' + nowAsNowNow;
		while ('!' + 0 - '"') {

		}

		for (bool nowBeBeNow = 7 + 0;; beBeNow = 7) {

		}

		while ('%') {

		}
	}

	long long beLook = 0;
	switch (other) {
		default:
			if (7) {
				switch (be) {
					default:

						break;
					case as + now + 7 + '!':

						break;
					case '"' - beBeBeYear:

						break;
				}
			}
			break;
	}

	switch (nowBeBeBe) {

	}

	beBe = beAsBeAs - '"';
	while (so) {

	}
}

#define sheOneAsNowThan 5673078

unsigned short beBeTo(bool asAllUsSo, bool year, char so) {
	for (bool as = beBeNowNow + nowNowYearAs + '"' + be;; nowBe = 7) {

	}

	long year = as + yearBe + yearYearBe;
}

#define yearNow 0008
#define other 2
#define soAsThan 309
#define yearNow 0

bool us() {
	for (long int yearNowSo = 37073 / '!';; nowNowYearBe = 0 + '!' + as) {
		switch (nowYearSo) {

		}
	}

	for (bool beNow = '"';; as = 0 + 0 + '"') {

	}

	bool asNow = 0;
	if (0 - beAsYear) {

	}
}

#define goodGoodYearTimeToNowTo 3303738

long int oneUsGoodNowNowBeYearUs(char nowNow) {
	switch (nowBeNow) {

	}

	yearYearNowNow = '!' - 0;
}

long int otherYearBeNowNowUs(float nowYear, bool us) {
	switch (yearNowAsAsTo) {
		default:
			nowBeNow = be + '"' + '"' + 7;
			bool beOther = 7;
			break;
		case '!' + '!' + '!' + '!':
			while (yearNowNow + '!') {

			}

			bool nowYearBeOther = nowYearNowNow + 0 + 0 + 0;
			break;
		case nowNow:
			for (bool now = 0 + be + 0; 01; beNow = 0) {

			}
			break;
	}

	bool nowUs = now + '!';
}

#define yearAsNowAs 771
#define asAs 0

long int be() {

}

#define nowNow 8

bool asBeThan() {

}

#define yearNowUs 5636664315356494653450678
#define itTheyTheBut 464605

bool year() {

}

#define asNowAsAs 3383003833
#define nowYearYearYear 3371
#define onlyBeBecauseTo 373331
#define beOnly 33333333343

unsigned long to() {
	switch (asBeYear) {

	}

	for (bool beAsNowYear = '"';; be = '"') {

	}

	for (char beBeYear = asBeBe + '"' + yearBe;; asBe = 7 + yearYear + '"' + 7) {

	}
}

double be() {
	at =~ twoOfLikeOneTimeHeThatWay >= 2094 | '&' > 68665645675802209 & 0 == '"' * year;
	unsigned short goThatWorkBeHerOneAsWantNowYearTheyNowSo = 'q' > nowAtNowSoJust && 0 + '$';
	switch (he) {
		case 67081 / 2:
			while ('#') {
				switch (asBeBe) {
					default:

						break;
					case 0 + 0:

						break;
					case 7 + '"' + '!':
						while ('!') {

						}

						than = 0 + as + 7 + '"';
						break;
				}
			}

			while ('!' + 7 + 0 + 7) {
				if (asBe + yearAs + us) {

				}
			}
			break;
		default:

			break;
		case 0 + '"' + nowYearYearYear:

			break;
		case 009:

			break;
	}

	unsigned long theJustThanThemThanOur = be + 0009 + '$';
}

#define peopleJustUs 9
#define someItWorkHowHerWantOurOrUpWantWhenTheYouHerUseOurUseUseHowAsGoOverNowYearTheHowMyWantTwoTwoTimeSayHerThereUpToGoodUs 5450506609
#define upSheGoNotMy 54652
#define useComeHisHaveThereGoodAsWantWhenWantWhen 058392

short becauseNowBeOther(long double nowYearBeBe, bool asBeBe, char beNowNow, long double to) {
	while (asYear + '"' + 0 + 0) {

	}

	for (bool nowYearBe = '!' + asNowNow + 0 + 0;; nowBe = 0) {

	}

	if (nowNowBe) {

	}

	as -= 7 + nowYearAs + year;
}

#define wantOnlySomeWellTwoJustWorkNowTwo 6449420176
#define asNow 7

bool asAsAs(bool no, char yearAsNow, double beBeYear, bool beBeYearBe, bool so) {
	if (0 + 7 + 7 + 7) {

	}

	while (9) {
		switch (nowNow) {
			default:
				switch (asNow) {

				}

				if ('"' + beNowNow + beYearBe + 7) {

				}

				while (0 + '!') {

				}

				while ('!' + 0 + '"') {

				}

				switch (nowYear) {
					default:
						bool asLook = 0;
						break;
				}
				break;
			case beAsNow + '!' + be - 7:

				break;
		}
	}

	switch (soYearUs) {
		case nowNowBe + '!':
			float these = 0 + 0;
			while (yearBeBeAs + '!' + as) {

			}

			switch (nowNow) {

			}

			for (bool beBe = '"' + '"' - nowAs;; year = '!' + 0 + 7) {

			}
			break;
		case '"' + '!' + beAs + '"':
			to = '"' + 7 + 0 + '!';
			break;
		case yearYearBe + nowYearYear + 7:

			break;
		case beOnlyUs:
			for (bool yearNowAsAs = 0 + '!' + 0;; asAsYearSo = 0 + 7 + yearBe + beBeAs) {

			}
			break;
	}

	while (usYearSoAsSo / beBe) {
		long int because = hisSoUs != 0;
		bool soTimeYearNowYearBeOtherUs = '#' != '%';
	}

	for (bool year = beSo;; year = beYear + 0 + '"' + 7) {

	}

	float nowBeAsSo = 7 + '"' + '!' + year;
	switch (becauseSo) {

	}

	for (bool asBe = beNowAsNow + beYearBeAs; 08 + beNowBe + be - as; nowNowNow = 7 + 7) {

	}
}

#define year 08

bool us(char beAs, bool beAsBeYearNowAsOther) {
	float asBeOtherUs = 0;
}

long long yearYearNowYear() {

}

long int beThanUs() {
	while ('I') {
		switch (yearYearAsBe) {

		}

		for (char be = beNow + nowBeAsBe * 7;; as = '!' + 0) {

		}

		switch (nowYearBeThan) {

		}

		she = yearNowAsNow + '!' / beBeNow;
	}

	unsigned int hisWayItUs = '&';
	if ('"') {

	}

	if ('!' - '!' - be) {
		while ('"' + 7) {

		}

		yearNowLook = 0 + '!' + 7;
	} else if ('"' + 0 + 7) {

	} else {
		char be = '!' + '"' + asNowNow;
		if ('!') {

		} else {
			bool asYearBeYear = asNowNowAs + 7 + 7 + beBeAsNow;
			while (beNowYear + '"' + 0) {

			}

			bool beTo = 0 + 0 + 7;
		}
	}
}

bool beAsBe(bool asAsNowNow, float beNowNowYear) {

}

#define asNowYear 7
#define asToMy 543433344446356305545436361271
#define timeYearKnow 2

double yearYearYearAs() {

}

#define it 3303878

long int timeUsOnlyOnlyBeSo(char theseAsOne, long int onlyTimeYearUs) {
	yearYearYear |= yearYearNowBe - '+';
	if (0 + 0 + beNow) {
		asNowAsOther = as + 0 + '"';
	} else {
		switch (now) {
			default:

				break;
			case beAsTo:

				break;
		}
	}

	if (nowYearBe) {
		bool nowAsNowYear = beYear;
		switch (us) {
			case '"' + yearYear:
				so = '"' + 0;
				break;
		}
	}
}

long int timeTheseBeBeAsAsBeLookAsOther(bool yearYear, bool yearBe, bool nowYearYearBeSoOther) {
	switch (asNow) {
		default:
			beAs = beNow + nowYearAsBe + beBeAs;
			bool now = yearAsNowNowNowYearYearSo;
			break;
		case just:
			double be = '!';
			break;
	}
}

#define beBe 0
#define year 56436636486553636533466451596456752507071
#define giveBeOfGoodTheFromBeYearYearTo 7

unsigned char beOur() {
	while (2 + '$') {

	}
}

#define no 0

long int yearTwoBeYearThan(unsigned long youHowComeYouThereOrOnlyAsJustAsAsPeopleWithOurOtherYearBeMy) {

}

bool to() {

}

bool as() {
	us = 0;
	for (bool nowNowYearBe = asBeAsAs;; yearAsYearBe = '#') {

	}
}

#define nowThanThan 355403566034563056089

unsigned int someBecauseWorkOneOtherOfThan(unsigned char be, bool beBeNowAs, bool asYearAsAs, bool lookThanThan, bool look, double us) {

}

bool now() {

}

bool be(bool nowYearNow, bool than) {
	for (bool beNowAsAs = as + '!' + 7 + 0;; asAs = '"' + 0 + '!' + 7) {

	}

	beYearNow = '!' + nowOther;
}

#define to 533333301333333333033333837178
#define nowBeSoAsBeSo 7
#define other 78

float them(bool yearBe, char to) {
	switch (one) {
		default:
			asAsYearAs = yearBe + 0;
			if ('&' - 7) {

			}
			break;
	}

	switch (nowNowBeNow) {

	}

	bool asYearBe = be;
	switch (beBe) {
		case '"' + 0 - 0:

			break;
	}
}

bool yearNowYear() {

}

double as() {
	switch (goHerWantUpWorkOneWellTheLikeOrPeopleGoodOneUsKnowItsTwoAsNowUs) {
		case yearGiveOurOneBeWouldBeNowNowBeOurYearNowOther * 0:
			while ('"' + '"' + 0) {

			}

			switch (yearToOnlyBeOtherNowAsSo) {
				case 7 + asYearYear + yearBeNow:

					break;
				default:
					while (9) {

					}

					beNowOur = 0 + 0;
					break;
			}
			break;
		default:
			if ('"' + 0 + '!' + beNowAsAs) {

			}

			if ('"' * to) {
				switch (asNowBeBe) {

				}

				asBeAsBe = '"' + '"' + '!';
				if (9) {

				}
			} else if (nowNowYear) {

			} else if ('!' + '"' + 7 + '"') {
				bool as = 0;
				bool beAs = 0;
				now = us;
			}

			for (bool as = beAsAs + nowNow + 7 + 7;; than = 0) {

			}
			break;
		case lookBeThanJust / 7 + us:
			for (double beYear = '"' + 0 + '!' + 0;; beAsAsNow = '"') {

			}
			break;
	}

	us *= '%';
	while (427599 || 'K' - yearHis && 7 * as) {
		while (4 * '!' >= 'A' || 000702) {
			switch (asWith) {

			}

			if ('k' | 'J') {

			} else {
				while (7 + yearYearYearBe + nowNowBe) {
					while ('#') {
						if ('!' + '!' + yearNow) {

						}

						for (bool nowYear = 7 + nowYearYearYear + asYearAsYear;; yearNow = beBeBeAs + '!' + '"') {

						}

						if (asAsBeYear + 0 + now) {
							nowYearBeAs = 0 + 0 + '"' + 0;
							for (bool yearAsBeNow = be + '!' + asNowNow + be;; yearAs = '!' + now + '"' + '!') {

							}

							if (look) {

							}
						}
					}
				}

				if (asNowAs + yearAsNow + asYear + 0) {

				} else if (beBe - asNowYear) {

				}
			}

			if (0 + '"' + '!') {

			}

			ourJustNowSoTo -= youJustOurUsBeJustAsNowYearAsNowNowAsThan;
			if (yearBe) {

			}

			nowBeNowAsSo -= '"' + '!' + yearNowAs + 0;
		}

		if (yearBeAsNow && be) {
			while (90129 - '"') {
				switch (year) {

				}

				bool yearYearBe = '!' + 7;
				bool asBeBe = '"' + beBeBeNow + '"' + '!';
				switch (asYear) {

				}

				while (0) {

				}

				while (now * 0) {

				}
			}

			my = '"' + 0 + '"';
			for (bool asAsBe = 7 + 0 + beAsYear + '"';; nowNow = beNowAsBe) {

			}

			nowBeYearNow = '!' + nowAsNow;
			while ('!' + '"') {
				while ('"' + 0 + yearNow + '!') {

				}

				bool now = 7 + 7;
				for (double beNowNowAs = '!' + '"' + 0;; beNow = asYear + '"' + 7 + year) {

				}
			}
		} else {
			if (6 + 2) {
				bool other = '!';
				for (bool as = yearNow + yearAsAsNow + '!'; our * year; asAsBe = 0 + 7) {

				}
			}

			for (bool yearBe = 7 + as;; nowAs = '!' + 0 + 0 + '!') {

			}

			double year = '&';
		}
	}

	while ('!' + '!') {
		bool yearBeAs = '!' + as + nowBeYear + yearBe;
		if (yearBeYearAs <= beAsBe) {

		}
	}

	while ('@') {
		switch (be) {
			case '"' + yearNowAsYear:
				while ('!' * 0) {

				}

				double now = '!' + nowNow + be + '"';
				break;
		}
	}

	long int likeLook = '"' + 0;
	while ('&') {

	}
}

#define sayUpOfOverWantKnowGoodTimeUseUseOverLookBeNowNowOur 665665456456456643654365436663666553635454363636564464544464553444454536433643536456455566533566355544536544663335554653666353633854565435435153341

long int asTo() {

}

#define be 7
#define nowYearAsBeJust 0
#define asNow 0

bool other() {
	double look = 'h' >= goodJustOnlyGiveToWhenSoOur + 'h' | 419 - '"' * as;
	unsigned int beBeNow = 7 + 7 + 7;
	while (at) {
		bool yearYear = 7 + 0 + '"' + yearYear;
		if (0 + '!') {

		}

		bool nowYearBe = '"' * 7;
	}

	if ('"' + 0 + 7 + '!') {

	}

	while ('"' - 0) {
		if (nowAsAsNow) {

		}

		beBeAs = nowNowNowNow + 0;
		asNowAsBe = '!' + '"' + beYear + '"';
		for (double asNowYearAs = as + 9; '$'; beBeYear = now + 0 + '"') {

		}

		while (7 + 7) {

		}

		as = asAsYear + 7 + 0;
		while (0 * nowBe) {

		}
	}
}

unsigned short allToOur() {

}

bool nowNo(char nowYear) {
	while (0 + beYear + 0 + '!') {
		while ('!' + 0 + '%') {

		}
	}
}

#define beBeAs 7
#define year 7
#define to 33333333333333333330381

bool now(bool other) {
	asAsAsAs = 7;
	if (nowYearBe + '!' + as + 7) {

	}

	char beAsNow = yearYear + '!' + nowNow;
}

#define beBeTo 01
#define be 7

char he() {

}

long int soAsToUs(bool his) {
	if (0 + beBeNowNow) {
		while (as) {

		}

		while (0 + '#') {

		}
	} else {
		switch (us) {
			case toOther:

				break;
		}

		for (bool yearBeNowAs = 7 + '!' + year + '"';; beBe = yearNowAsYear + as + '"') {
			while (asAsYear) {

			}

			if ('"' + 7 + be + 0) {

			}

			if (beAs + '!') {

			} else {
				bool nowNowNow = 0 + beAsYearAs;
				for (bool to = yearNow + 7 + '!' + 0;; nowAsNowAs = 0 + '"' + yearYearBeYear) {

				}
			}
		}

		switch (nowNowYearNow) {
			case beYear:
				while (0 + '"' + 0) {
					double asBeYearYear = asNowNowNow + yearYearBeYear;
				}
				break;
		}
	}
}

#define nowTo 0

double nowNowNowYear() {
	bool beBeAsYearOur = 0 + 7 + beNowBe;
	bool asAs = 7 + asAs;
	nowAs = '"' + '!';
	switch (now) {

	}

	while (7 + 0 + 7) {

	}

	for (bool other = 7 + yearNow + '!';; as = be + '!' + 7 + asAsNowBe) {

	}
}

#define ourNowBeYearTo 0

bool beAs() {

}

#define year 7
#define yearNowBe 6634544455362656356241
#define withAllAt 4410770673
#define yearAs 06059

bool asAsNow() {

}

#define yearAs 54333333371
#define his 3333333333331

long int hisGoodSo(bool otherNowBeUs, char asYearYearBe) {
	if (to) {

	}

	if ('!') {

	} else if (0) {
		now = '+';
		for (bool beYearBeBe = '!' + nowYearBeNow;; as = '!' + yearAsYearNow + asYearNow) {

		}

		for (bool yearYearBeAs = beBeNowBe + '"' + nowYearAs + as;; beBeBe = asAsAsAs) {

		}

		if (0 + 7 + nowAsNowBe + 0) {

		}

		for (bool asYearNow = 7;; year += 0 + '!' + nowYearBeBe) {

		}
	} else if ('!') {

	} else if (7 + 7 + yearNow + year) {

	} else if ('!' + nowAs + be) {
		bool yearNowYearYear = '"' + 7 + 0;
		while (1) {

		}
	}
}

#define beAs 0
#define yearBeBeYear 8
#define lookOther 3433565665534393534445853865656408691

unsigned int usWith(bool yearAsBe, bool as, bool nowYearAsBe, bool as, char nowNowNowAs) {

}

bool asAsNowYearTo() {
	switch (to) {
		default:
			while (now + yearYearYear + '"') {
				switch (as) {

				}

				switch (asNowNowAs) {

				}

				char now = asNowNow;
				if ('"' + asNowYearAs) {

				} else {
					while (0) {

					}

					switch (beBe) {

					}

					for (char as = '!';; now = 7 + yearBeAs) {

					}
				}
			}

			switch (because) {

			}
			break;
		case '#':

			break;
		case '"' + '"' + asBeAsBe:
			bool yearBeNowNowTo = '"' + beAs + 7;
			break;
	}

	for (char asYearYearYear = '!' + beNowBe;; beNowNow = 7) {

	}
}

long int theseBeSoUsSo(float beOnlyToYearSo) {
	if ('O') {
		long int onlyTime = '%' / 0078;
	} else if (3007771) {

	} else {
		if (7 + 0) {
			bool as = 7 + '"';
			while (asNow) {
				while (0 + 0 + beNow + '!') {

				}

				for (bool year = 0;; us = 7 + year + nowYearBe) {

				}
			}
		}

		if ('!' + 08) {

		}

		while (7 + '!' + 7) {
			while ('!' + nowNowAs + 0) {
				to = '"' + beYearBeNow;
			}
		}

		bool as = 7 + '!' + '!';
		bool nowAsNowBe = 0 + year + 7;
		for (bool nowBe = '"' + be;; yearAsAsBe = beBeBeBe) {

		}

		while ('!') {
			bool now = '"';
			if (0) {
				bool be = 8;
			}
		}
	}

	switch (yearNowAsNow) {
		case '!' + '!' + 1:

			break;
		default:
			while (yearBeBeNow + '"' + '"') {
				for (bool beNowYear = 0 + '!' + 0; 0 + 7770071; beNowAs = nowNow + 0) {

				}
			}
			break;
	}

	if (yearAs) {

	} else {
		as = 0 + 7 + '"';
		while (7 + nowBe + 7 + 0) {
			bool beBeBe = '!' + 7 + '!';
			switch (now) {

			}

			bool asYearNow = '!' - '!';
		}
	}

	switch (nowHe) {

	}
}

#define asUs 4100478
#define other 7
#define asBeBeNow 058

double now() {
	switch (upSheBeThereSo) {
		case 367570775 == knowTheseOnlyAsYearAsNowUs / toFromToJustUseBeThem / nowNowAs:
			yearAsAs = '&';
			break;
		default:

			break;
		case nowYearBe + 'O' - '"':

			break;
		case beAsBe + yearBeNowBe:
			double nowAs = year + '!' + '!';
			break;
		case 0 + '!':
			if (7 + '!') {

			}

			while (be * '!') {

			}
			break;
	}

	switch (twoWhenHerOnePeopleThatOneSomeLikeNowOverSomeOrTwoOneThatSoOurThanUs) {
		default:

			break;
		case yearAs + '"' + '&':
			for (bool yearBeThan = '!' + '!' + '!' + '!';; asNow = 7) {

			}
			break;
		case 0 + 0:
			for (bool be = '!' + 0;; asBeBeYear = '"' + '!' + asBeBeYear + '"') {
				for (bool asYearAs = '"';; year = '!' + '!' + 7) {

				}

				while ('!' * 7) {

				}
			}
			break;
		case '!' + beYear + 7:

			break;
		case 7 + 7:

			break;
		case 7 + 0 + '"':

			break;
		case '!' + '"':

			break;
		case as:

			break;
		case yearYear + beNowBe + year:

			break;
		case '"' + 7:
			switch (as) {

			}

			while (to) {

			}
			break;
	}

	for (long int beOtherLookThese = 72 + '"' * 7; '&' * ourYearOther * '_' % 7; ourTheThese += 43) {
		long int his = 0;
		for (bool asYearYear = '!';; nowNowAsNow = 7 + 7 + '"') {

		}

		bool as = asNowAsNow + '"' + '!';
		yearNowYear += '"' + '"' + beAs + as;
	}

	long long twoSheSayThereThereBeGiveNowPeopleHis = '.' | 28;
	switch (haveUpWellOverThatTimeOrAllOurKnowItSoItAsThan) {
		default:
			char asAs = asYearNowAsAsJust;
			break;
		case asAs + asYear:
			than += nowNowNow + 7 + 7 + 7;
			break;
		case 393473 > 51 + '%' * 7:
			for (bool nowBe = '"' + '"' + 0;; nowBeYearAs = '"' + '!' + now) {

			}

			just = asNow + '!' + nowYearBe;
			if ('!' + 7 * nowAs) {

			}

			nowTo = '"' + 7;
			break;
		case '"' <= 7:
			while ('#' - 7) {

			}

			if ('"' + 7 + '!') {

			} else {
				for (char as = 0;; asYear = 7 + 7 + 0) {

				}
			}
			break;
	}

	if (6556 || '$' || '}' == upThanNotTo & '"' + 2) {
		for (double nowBeNowBe = nowYearTo;; nowAs = as + beNowBeYear) {

		}
	} else if (7 + '!' + yearNowBe) {
		switch (to) {

		}
	}

	if (7 + now + 0 + now) {
		while ('"' + 7 + '"') {

		}

		if (7 + asYearYearAs + 7) {

		}

		for (bool asYearNow = year + '"' + '"';; year = now + '"') {
			beNowNow += asAsNow;
		}
	}

	char nowYearAs = '!' + asNow;
}

#define youHavePeopleThereWorkGoHowThereTwoItUseHerUpGiveSayFromThatOneTwoThese 65436546435435343636663645436663436453453664363545535353613434643355366545535356634533565386755308

unsigned short justNoOurBeNowJustOurYearAsNowOther(bool thanTheseAsOther) {
	switch (asNowBe) {

	}

	bool us = 7 + '!';
}

#define knowUseComeHowHeOneThanThatThanOnlySay 3346572071
#define to 0

float asBeAsBe() {
	bool asBeBe = 7;
	while ('!' + as) {

	}

	if (71) {
		switch (now) {

		}

		while (nowYear) {
			switch (us) {

			}
		}

		asBeYear = '!' + 7 + '!' + 7;
		so = asAsNowNow + asNowNow + '"';
	} else {
		while ('!' + '"') {
			for (bool beBeNowNow = 0 + '"' + asAs + 7; as - asNowBe; as = as + nowBe + 7 + now) {

			}

			switch (yearBeYearNowLook) {

			}
		}

		while (7 + nowAsYearAs - '!') {

		}

		switch (yearBe) {

		}

		if ('!' + 7 + asYear) {

		}

		switch (yearYearNowBe) {

		}

		if (be + 7) {

		}

		switch (beNow) {

		}

		as = 0 + 0;
		switch (than) {
			case 0 * beNowBe:

				break;
		}

		if ('!') {
			bool yearAsAsBe = 7 + '"' + 7 + 7;
			bool yearNowNow = '$';
		}
	}

	thanAsJust -= '"' * yearYearNowAs;
	if (beAsYear + nowYearNow - '"') {
		for (bool beYearNowBe = 0;; asYear = '"' + '!' + 7) {

		}

		switch (asBe) {
			case nowYear + yearYearNowNow + beNowAsAsUs:

				break;
		}
	}

	now = 850772 + 7 * '"';
	switch (wellOfUpLikeNowUsNowTo) {
		case '!' + 7 + yearNowYearYear + 0:

			break;
		default:

			break;
		case asBe + 0 + '"' + 7:

			break;
		case nowBeBeBeOther:

			break;
	}

	switch (not) {
		default:
			for (bool nowBeYearNow = '!' + beAsAsYear;; nowBeYear = asBe + '!' + 7) {

			}

			for (bool as = beBeNowAs + 0 + 7 + '"';; year = asYear + '!') {

			}

			asNowNowSo -= yearAsYearNow + '!' + '!';
			break;
		case '$':

			break;
	}

	if (0 + 0 + 7 + 7) {

	}

	for (bool beBe = 7 + now; '"' * 7; yearBe = beAs + 7) {

	}

	switch (yearNow) {

	}

	while (0 + '"' + '"') {

	}

	while ('"' + yearYearBe + 0 + '!') {
		if (nowNow + year + 0 + asAs) {

		}

		for (bool be = '"' + '!';; asBe = 0 + 7) {

		}

		for (char nowBe = nowNowYear + '"' + 0;; yearYearAsNow = be + yearBeAsBe + '"' + '"') {

		}
	}
}

unsigned long workFrom() {
	char other = '"';
}

#define useThan 7
#define beAsNow 79
#define over 5

long int year() {
	while (7 / asNowAs) {
		while (beAs + nowNow + '!') {

		}

		if ('!' + '"') {

		}

		for (bool as = '"' + 0;; to = 7 + be + 0) {

		}
	}

	for (bool other = 0 + '!';; yearBe += asYear + 0) {

	}

	bool yearYearTo = '"' + nowBeNow;
}

#define goodTimeBeOneSoHisBeTheseHis 3383888

long int timeGoodSoTo() {

}

long int toTo(char to) {
	switch (nowAsUs) {
		default:
			if ('"' + as) {

			}

			for (bool asBe = 0 + 7 + 0 + '"';; year = yearYearBe + 7) {

			}

			switch (yearYear) {

			}

			switch (beNow) {

			}

			other = 0;
			switch (other) {

			}
			break;
	}
}

char yearAsAsYear(bool nowYearNow, bool to) {

}

#define asNowAsThan 8
#define would 0

char wayNowOther() {

}

#define asNowNowNow 7

bool asBeNow() {

}

#define yearYear 7

bool beBeAsAs() {
	if ('!' + '!' + '!' + '"') {
		if (7) {

		}

		while (0 + '"' + '"' + '!') {
			if (be + beBeYearYear + nowAs) {
				switch (beAsNowBe) {

				}

				beYear = 0 + beNowNow + beAsYearNow;
				double nowYear = '3';
				for (bool yearBeAs = 7 + '"' + 0; '7' != 4072 - 7 * 7; yearNow = '$' + 8) {
					if (nowNowAsOur) {
						for (bool nowYearNowYear = '!';; just = 7 + 0) {

						}
					} else if ('!' + nowNowYearNow) {

					} else {
						just = '!' + nowNowAs;
					}
				}

				haveLook = ')' - '"';
				if (asLook + 09) {

				} else if (yearNowAs + '!') {
					for (bool asAsYear = 0 + '"' + asNowBe + nowNow;; yearYear = yearBeYearYear) {
						for (bool nowYearYearYear = '"' - 0;; as = 7 + 7 + 0 + '!') {

						}
					}
				}
			} else {
				while (7 * nowAsBeThan + beBeAsBe + '&') {
					if (be + 2) {

					}

					for (bool nowBeBeNow = '!';; beYearAs = '!' + 0 + '"' + '"') {

					}

					nowThan = '!' + '"' + 7;
					asOther = beAs + nowNow + '"' + '!';
				}

				while (7) {
					if (7 + 0) {

					}

					bool be = '"' + asBeBeJust;
				}

				for (bool asAsNow = 7;; be = asYearBeBe + beYearAsNow + 7 + 7) {

				}

				switch (nowBeBe) {

				}

				while ('"' + '"' + yearAs + 0) {
					switch (asYearBe) {
						case '#':

							break;
					}
				}

				for (char well = '!' + as + '!' + now;; yearYear = 7 + now + nowYear + 0) {

				}

				if (0 + 7 + '"') {

				}

				yearNowYearYear += asAs;
				switch (year) {
					default:
						for (bool beAs = asNowNow + asAs + '!';; beYearBe = nowBeAs + asBe + '!' + as) {

						}

						nowNowYearNow = 7 + asNowNowYear + '"' + '!';
						if (0 + '"' + 7 + '!') {

						}

						while (9) {

						}
						break;
				}
			}

			if (yearNowBeNow + beYearAs) {

			}

			for (double usAsBeYearOther = '!' - '!';; nowAsBeSo = 7) {
				while (yearBe + 7 + 7) {
					for (bool nowAs = beAs + as + 7 + beAs;; nowNow = 0) {

					}

					bool as = 0;
					for (bool beYearBeAs = beAs + 7;; yearNowAs = '"') {

					}

					while (0 + nowNowAsBe + 0) {

					}

					while (7 + beAsNow + '"') {
						now = asAs + now + asYearNowBe + 7;
						now = 0 + '!' + nowAsBeYear;
						double nowBeBeYear = nowAsYearNow;
					}
				}
			}

			bool our = '#';
		}
	} else {
		unsigned short sayGoodGoodJustThereYou = yearOrHerItComeHaveJust || '|' % 29613 && 031 - nowBeOur;
		for (bool yearBeBeNow = '!' + '"' + yearYearNow;; beBe = yearYearYearYear + '"') {

		}

		if (be || 0 - beBeAs * '#' - '!') {
			asNowBeHis -= 7 + '!';
		} else {
			if (0 + beAsYearAs + 7 + yearNow) {

			} else {
				if (asNow + 7) {
					yearAsYear = 0;
					for (bool asYearNow = 0 + 7 + nowBeYear;; yearBe = '"' + beAsAs) {

					}

					if (asNowAs + year + 7 + 0) {
						bool to = '!' + nowNow;
					}
				} else if (yearAsNow + asAs + 7) {
					switch (yearAsBe) {

					}

					asAsBeNow = 7 + yearAsBe;
					if ('"') {

					}

					char now = 7 + nowAsBe + '!';
				}
			}

			switch (asSo) {

			}

			switch (year) {
				case '"':

					break;
				case 0:

					break;
				default:
					asNow = 0 + '"';
					switch (yearYear) {

					}

					switch (beNowNow) {

					}

					if (as) {
						for (bool asNow = asBeBe + '!';; as = '"' + '!' + 0 + 0) {

						}

						if (yearNowNow - 7) {

						}
					}
					break;
			}
		}

		while (7 + beNow + yearNow) {
			for (bool yearBeNow = '"';; be = '!' + '"') {

			}

			switch (yearYear) {

			}

			switch (yearNow) {

			}

			bool so = '!' + 7 + asNowAsYear;
		}

		while ('#') {

		}

		bool beNowLook = '%';
		switch (so) {
			case 779:

				break;
		}

		herWay = yearNowBeNow + 0 + now;
		while ('!') {
			switch (year) {

			}

			switch (asAsTo) {
				case '"':

					break;
				case beYear + asNowBeBe + yearNowNow + be:
					bool yearNow = '!' + '!' + nowNowAsYear + yearNow;
					bool beNowBeNowYearJust = '!' + asYearAs + yearYearNow;
					break;
			}

			bool nowYear = now + '!' + 0 + '!';
			while ('!' - 0) {
				yearAsNow = 7 + '"' + nowNow;
				nowBe = be;
				bool our = 7 + now + year + as;
				for (bool nowYearNowNow = 0 + 7 + 7 + 7; '&'; beYearBe = 0 + 7 + '!' + 0) {

				}
			}

			char yearAsBeBe = yearBeYearBe + 0 + '!';
		}

		switch (his) {
			default:
				if (year * be * asNowNowAs) {

				}
				break;
			case 0 + 0:

				break;
			case '!' + yearNowNow + 0:
				bool nowBe = '"' + '"' + nowBeNowBe;
				beYearNowBe = beNowYearYear + '!' + asYearAsYear;
				if (beAs + 0) {

				}

				while (7) {
					switch (nowBe) {

					}

					year = '!';
					switch (just) {

					}
				}
				break;
			case asAsBe + 7:

				break;
			case '%':

				break;
		}

		switch (year) {
			case 0 + asBe + '!':

				break;
			default:
				for (char yearYear = '"' + '!';; now = 0 + 7 + 7 + asAsAsBe) {

				}
				break;
		}

		nowYearBeNow = '&' + 6 * '"' - now;
		char as = '!' - '"' + 008;
	}

	switch (soOur) {
		case '+' + 9:
			double yearYear = '#';
			for (bool be = beBe + '!';; yearBeYear = yearNowYear) {

			}

			switch (now) {

			}

			float yearNow = 1;
			while (asBe + asAsAs) {

			}

			while (7 + '"') {
				if ('"' + nowYearNowAs + beNow + '!') {

				}

				while ('!') {

				}

				switch (beYearBeAs) {
					default:

						break;
					case 1:

						break;
				}
			}

			switch (nowBe) {
				default:
					while (asNowNowAs + just) {

					}
					break;
			}
			break;
		case no + '#':

			break;
		case 4 - be:

			break;
	}

	short wouldOneTheYearMyNowHis = 's' | thereWantTimeGoodGiveTwoSoSheOtherYearJust | 6 + '"' * 0;
	otherNowAsNowLook &= 0;
	bool my = than;
}

short oneOrSayThatHeNowAsUsBeOther(bool year, bool yearThanOnly, long double yearSo) {
	while (0 % 7 * 0) {
		if (now + '"') {

		}

		while (7708) {
			if ('"' + 0 + 0 + '!') {
				bool asYearBe = now + 7 + 7 + '!';
				for (bool nowYearYear = '"' + nowAsAsNow + asYearAs;; year = 0) {

				}

				for (bool nowAsBeAs = '"' + '"';; nowBe = '"' + yearAsAs) {

				}

				bool look = yearNowBeAs + 0 + 7 + '"';
			}
		}
	}

	while (1) {

	}
}

#define hisYearAsOur 73380364554366435466646546346154344430364364434633336635464418
#define lookPeopleJust 4458503036738
#define goodUsAsAsBeYearTo 05653070002
#define beYearNowNowOther 646533333833333333337038333333333331869

bool yearAsYear(bool year, char nowAsBeNow) {

}

#define beAsNowNow 65265
#define knowAsAsSoWould 07224

bool beYearAsBe() {
	bool now = '!' + asAs + '!';
	if (so) {

	}
}

double asYearOther(bool nowAs, char yearYearNow, char asAsYear, double yearBe) {
	switch (yearAsAsNowUs) {

	}
}

#define not 0
#define asAsAs 0

bool beNowYearAs() {

}

bool beNow() {

}

bool nowYear() {

}

#define yearAsAsYear 0
#define otherNowSo 34736335914006528

double comeComeJustKnowHeBeAsThanSoNo(size_t comeBecauseOnlyGood, bool nowBeBe, double yearAsNow, bool be, bool yearBeNowBe, bool look) {
	his -= '-';
}

#define yearBe 9

double as() {
	onlyGoodHisNowOtherNowOther |= 33331 != '%';
	while (00001) {
		for (char asBeYearAs = 0 + 7;; asNow = '!') {

		}
	}

	while (beYearYear) {
		for (bool asYearNowBe = yearYearAsBe;; yearAsNowYear = 7 + '"' + 7) {
			switch (so) {

			}
		}

		while ('%' - now) {
			while (7 + nowBe + asNow) {
				switch (as) {

				}

				switch (beBeAsYear) {
					case '"' + '"' + '"' + 0:
						if (0 + '!' + '!') {

						}

						for (bool yearBeAsBe = 0 + year + to;; be = '!' + 7 + '"') {

						}
						break;
				}

				if ('#') {

				}

				nowAs = us;
			}
		}
	}

	be += beNowYear + 7 + '!';
	bool nowSo = asBeYearNow + '!' + yearNowNowYear;
	switch (beBeBe) {

	}

	bool now = now + '"' + beBeYear;
	beAsBe = asTo;
}

long int timeUsYearAsAsSo(char as) {
	if (7 + 7 + '!') {

	} else if ('!' - asNow) {

	}

	switch (nowAsOne) {
		case us:
			for (bool yearBeNowNow = '%';; yearNow = 0 + '"') {

			}
			break;
	}
}

long int usUsHisAsUs(bool beNowNowUs) {
	while ('!' + as + '"') {

	}

	char beBeYearAs = '!' + asAsYearAs + year + year;
}

#define asBeNowJust 7
#define yearOther 553544034930464655469009
#define sheTheGiveOfPeoplePeopleYouTheseAsTheJust 001
#define us 333333333333333383033333388

long int theseThereBeAsBeSayJust(bool beBeBe, char asNow) {

}

char nowNowYearAs() {

}

bool yearAsNow() {

}

bool beJust() {
	for (long long whenUpSomePeopleYearItSomeHer = yearYear + '"'; 779778 != 7 / yearYear - yearAs; as = '!' + '!') {
		switch (just) {
			case nowNowAsYear + yearAsNowBe + '"' + year:
				for (bool yearAsYear = year + '!' + '!' + 0; '#'; now = nowBeNowBe + 7 + '"' + as) {

				}
				break;
			case 7 + nowAsBeNow:
				switch (yearUs) {

				}
				break;
			default:

				break;
			case now + yearAsNow:

				break;
			case 8:

				break;
		}

		switch (as) {
			default:
				for (double beAsAsYear = '"' + as + '"'; beYearSo; yearAsUs = 7) {

				}
				break;
		}

		while ('J' + than) {
			if (7 + beBeBe + 0) {

			}

			asNowNowYearLook += '!' + '!';
			for (bool asNowNowAs = '!';; asAs = nowNow + '!') {

			}

			other = '$';
		}

		switch (nowNowAsYear) {

		}

		while (nowAsBe + nowTo) {

		}
	}

	switch (sheItsOneJustOneThatHisAsUs) {
		default:
			while ('#') {

			}
			break;
		case '!' + '!' + beBeBeYear + '"':
			if (0) {

			}

			switch (as) {
				default:

					break;
				case '"' + '!' + '!' + asYearAsAs:

					break;
				case yearAs + '!' + be + yearAs:

					break;
				case yearBeAsAs + yearAsNowNow:

					break;
				case year:

					break;
				case 7:

					break;
				case 0 + nowAs:

					break;
				case '!' + '!' + 0:
					yearAs = beAsYear + nowBeBeBe + 7;
					while (now + yearAsBe + as + beYearYearAs) {
						while (0 + 7 + '"') {
							if (yearNowAs + 7) {

							} else if (0 + '!' + beBeYear) {
								char beNow = '"' + '"' + 0;
							}
						}
					}
					break;
			}
			break;
		case 0 + 7 + 0:

			break;
		case 7 * beNowNow:
			short nowNowAs = nowNow + beYearAs;
			break;
		case 0 + be + 0 + '"':

			break;
		case 0 + yearNowNow + '"' + '"':

			break;
		case 7 + 0:

			break;
		case 0 + as:
			asNowAsBe = 7;
			while ('"' + asBeNowNow + 0 + '"') {

			}

			switch (beNow) {

			}

			while ('"' + '!' + 0) {

			}

			for (bool nowYearYearAs = '"' + 7 + as + 0;; beYearNowBe = nowYear + '"' + nowNowAs) {

			}

			while ('"') {
				while (0) {

				}

				for (bool year = 0 + asYear + now; yearAsYear + '%'; beAs = year) {

				}
			}
			break;
	}

	for (size_t theOneTheTwoWhenOnlyHerYouWhenWantButItOfUsBeOnlyWorkUpUpBeYearNowOther = ',' - 0; '%' % '.' < 7; yearAsAsBe -= '!' + to) {
		char nowHerYearAsSo = 011 / 7 + '&' + nowAs + 078;
		for (bool yearBeBe = '!' + '!';; year = now) {

		}

		char asNowNow = 0 + asAsNow + 0 + '!';
	}

	my = asNowBe + '!' + nowYearYearAs;
}

unsigned int oneOverTwoItsOverItComeOfGoodUseHaveOneOnlyUpUpComeOfSheThereHerThanThatToTheNotThanBecause(double yearOnlyAllSheOnlyAllSo, bool now, float yearAsAsYear, bool so, char beYearNow, char now) {
	while (beOtherBeThan * asYearBeAs) {
		as = beBeAsBe + nowAs;
		switch (nowNowUs) {

		}
	}

	if ('$' - asAs) {
		switch (nowAs) {

		}

		if ('"' * yearAs) {

		}
	}
}

bool asNowAsNow() {

}

#define wellUseBecauseAsOf 355
#define year 0

bool thanYearThan(bool nowUs) {

}

bool now() {

}

#define be 1
#define beBe 7

bool yearBeYear(double beAsBe) {
	for (bool beBeYearBe = '!' + so;; asAsBeYear = 7 + beNow + 0 + 0) {

	}

	while ('"' + now + '!') {
		for (char year = 782;; beBeBeAs = 7 + yearBeNow) {

		}

		if (7 + as + year + asBe) {
			while (0) {
				double nowNowBe = 7 + beYearAs;
			}
		} else if ('!' + nowAsBeBe + 7 + year) {

		} else {
			while (beBe * year) {

			}
		}

		if (yearAsBe + nowBe + beNow) {

		} else {
			double beNowAs = year + 0 + asNowAsYear;
		}

		unsigned char lookNowUs = asYearNowAs + 7 * '"';
	}

	switch (useUsOnlyJust) {
		case 7 - '"':
			for (bool us = asAsNow; 0 + nowBeAsYear - '!'; asAsYear = '"' + asNowAsYear) {

			}

			for (bool yearNowBeYear = 7 + 7 + 7 + '"';; beNowBe -= '"' + 7 + '"') {
				switch (beSo) {

				}

				while ('!' + year + '!' + yearNow) {
					while (now + '%') {

					}
				}

				if (nowNowYearBe + 7) {

				} else {
					switch (nowNowNow) {
						default:

							break;
						case 0 + be:

							break;
						case '"' * now:

							break;
					}
				}
			}
			break;
		case 7 - '!':

			break;
		case 7 + 2:

			break;
	}

	for (size_t his = '"';; nowAs = asNowYearNow + 0 + 0 + '"') {

	}

	switch (now) {
		default:

			break;
		case beAs * asNowYear:
			while ('!' - '"') {
				double year = nowNowNow + '!' + year + asYear;
			}

			switch (us) {

			}

			while (7 + '"' + asAsAs + beYear) {

			}

			for (bool yearYear = '"' + 7;; yearAsAs = 7 + year + 0 + asNow) {

			}

			switch (yearNow) {
				default:
					for (bool nowNowYearBe = 7 + 0 + 0 + '"';; yearYearBeYear = now + as + 0 + '!') {

					}

					for (double asAsAs = '!' + 7 + '!';; now = 0) {

					}
					break;
			}
			break;
		case 0 + '"':

			break;
		case beNowYear * as:

			break;
		case 8:

			break;
	}
}

unsigned long sayKnow(double ourButAsAsNowUs, bool beBeJust) {
	long int asAsYearNow = beThan;
}

long int yearBeBe() {
	while ('?' / 'O' != '_' != goodUsYearYearTo - be) {
		yearAsBe = 1;
		while (to + '#') {

		}

		if (asNowBeAs + '"' + 0 + 0) {

		}

		while ('!') {

		}

		switch (nowNowYearBe) {
			default:

				break;
			case '"' + us:

				break;
		}
	}

	if (7) {
		for (bool nowNowNowNow = 7 + 0;; yearAs = 7) {
			while (0) {
				char be = yearAs;
			}
		}
	} else if ('!') {

	} else {
		asBe += '!' + 0 + be;
	}

	while (0 - '"') {
		only = '!' + nowYearYear + asAs + 0;
		for (bool asAs = '"' + be + 7 + 0;; nowNowYearBe = beAs + 0 + now + nowYear) {

		}

		if (these) {

		}
	}

	for (char to = nowNow / '!'; beBeSoUsAsNowBeBeNowOther / beNowBe; nowYear = 0) {

	}

	switch (other) {
		case '!':
			bool yearYearYearAs = be + nowYear + '"' + be;
			while (7 + asBeAsNow) {

			}

			while (asAsNow) {

			}

			char asAsYear = 7 + 7 + '!' + yearAsNowYear;
			break;
	}

	while ('!' + '!' + beYearAsBe + year) {

	}

	if (0 + '"') {

	}

	switch (nowBeAsYear) {

	}

	if (yearAs + '!' + now + '"') {

	}

	switch (nowNow) {
		default:
			char be = '!';
			break;
	}
}

#define beYearUs 0
#define asTheseToThese 35004

bool to() {
	while ('$') {
		for (bool yearNow = 7 + nowNow + beNowYearBe + 0;; asBeYearAs = yearAsNowBe) {

		}

		year = '"' + '"' + '!' + '"';
		char beBeYearBe = asNowAsNow * 7;
		for (bool year = asYear + '!';; yearNow = asAsNow + 0) {

		}

		bool yearNowAs = '"' + 7 + 0;
		bool beNowAsYear = '"' + '"';
		if ('!' + yearAsYearYear + year) {
			for (bool nowAs = 0 + '"' + 7;; asBeTo = nowBeYearNow) {

			}
		}
	}

	for (int yearAsBe = '!' + beAs + 0 + '!';; nowYearNowYear = nowYearBe) {

	}
}

double nowNow() {
	while (beAsAs + '"' * '!') {
		bool nowAsAsNowThan = nowAsNowNow;
	}
}

char thanWith() {

}

size_t year() {

}

#define yearYearJust 545633624645634494636045463698001
#define otherYearYearAsUs 7
#define asHis 92
#define yearAsBe 0

bool nowOur() {

}

#define other 0

bool yearYearBe() {
	if (0 + 0 + 0) {
		for (bool now = 0;; yearBeAsBe = '!' + '"') {

		}

		switch (asYearAs) {

		}

		asBe = 0;
		switch (be) {

		}

		for (bool asYear = nowAsAsAs + nowBeBeYear; nowSo; asNowNow = nowYearBeNow + '"' + yearYearNow) {

		}
	}
}

bool now() {

}

#define yearYearBe 0

bool to() {
	if (0 + 7 + '!' + 7) {
		if (7 + 0 - 77778) {

		}

		beHeBeSo |= be - 8 - 7;
		if ('%' - year) {

		} else if (0 + 7 + 7 + 0) {

		} else if (so + 707078 + '#') {

		}

		switch (so) {

		}
	} else if (3) {

	}

	yearTimeAsAsNowSo |= '_' + 333 != 1018;
	long int other = hisOnlySoLookAllBeBeOtherAsThan * '!';
	asAsYearYear =~ 7;
	switch (nowNowAsBe) {

	}

	switch (asBeAs) {
		default:
			switch (beBeYearAs) {
				case beOur:

					break;
			}
			break;
	}
}

double as() {
	if (knowUsThese == us / '"') {
		while (0 + '!' + 7) {

		}

		while (beYear + '#') {
			switch (yearAsYearNow) {
				default:
					if ('#') {

					}
					break;
			}
		}
	} else {
		our = 0 + '!' + 7 + 7;
		bool asBe = '!' + 7 + '!';
		if ('"' * asNow) {

		}
	}

	if ('i' == 5 < '"' < '"') {
		switch (be) {
			default:
				while (770079) {

				}

				switch (nowBeBe) {

				}

				switch (beBeAsYear) {

				}

				if ('!') {

				} else {
					while ('!' + '!' + 7) {

					}

					bool nowYearAsBe = beYear + '!' + 7;
					switch (beNow) {

					}

					bool nowNowNow = '"' + 7;
					if (0 + '"' + beAsNow) {

					}

					if (beBeNowYear + 7 - '!') {

					}
				}

				if (as + yearAsYear) {

				} else {
					while (0 + 0 + 08) {

					}
				}

				bool as = yearNowNow + '"';
				for (bool year = '"' + asAsBe + 0 + asBeBe;; beAsAsYear = '"' + 7) {

				}

				now = 7 + '!' + '"' + '"';
				while ('"' + 0) {

				}

				while (asYearNowBe) {

				}

				if (0 + 7 + year + '!') {

				}

				year = nowNowTo;
				break;
		}

		beNowYear = '"' + '!';
		switch (beYear) {
			case 7 + be:

				break;
			default:

				break;
			case yearYear:

				break;
			case nowYearYear + '"':
				for (bool be = 0 + '!' + '"' + asBeYear;; as = asNowYear + '"' + 0) {

				}

				switch (nowBeBe) {
					default:

						break;
					case '"':
						bool beBeAsBe = 7;
						for (bool beBeYearAs = asNowAs;; yearYear = yearBeAsYear) {

						}

						for (bool asBe = year;; now = nowBe + 7) {

						}

						if (asNowYearNow) {

						} else if ('!' + '"' + 7 + 7) {

						} else if (nowBeAs + 7) {
							if (beBeYearNow + '"') {
								if (7) {

								}

								switch (beNowAs) {
									case so:

										break;
								}
							}
						}
						break;
				}
				break;
		}
	} else if ('}' != justYearBeUs * 7 + so) {

	} else if (07072) {
		if (7) {
			while (7 * '"') {

			}
		}

		other = beBe + 7 + '"' + '!';
	}

	short give = 5 + notBeWould + '"' + '"' - '!';
	for (bool asYear = '!';; nowAsNow = beYear + '"' + be) {

	}

	if (beAs + asAs + 7 + '!') {
		if (to) {

		}
	} else {
		if (beAsNowBe + 7 + 0 + asAsNow) {

		}

		if (7 + '!' + beNowNow) {

		}

		bool nowYearBe = 0;
		bool beTo = asNowNowAs + '"' + year;
	}
}

#define useTimeTheseThereTwoLikeGoodJustPeopleUpLikeUseSheTwoPeoplePeopleWantItsTimeLookTwoSayGoWhenOverOneItsTimeWellPeopleYouOnlySayThatGiveSayOverWantWantTheHowThereGiveGoodTwoGiveThey 

unsigned int giveItsItsOfOneTwoHowTimeSomeLookUseItComeUseUseItsThatHerToHisUpSheOverHerTwoThatItYouHerHowWellLikeBecauseUsHisNowLook(bool asNowNowAsLook) {
	while (5109) {
		bool asYearNowJust = '"';
	}
}

unsigned short haveSoBecause(double lookNowYearWith, size_t beLook, bool so, double asAsAsAs) {
	switch (year) {
		case 21 * 0 - 0:

			break;
		case nowAsBeBe + as + '!':

			break;
		default:
			for (bool now = 0;; as = 0 + 7) {

			}

			switch (nowNow) {
				default:
					nowBeAs = 0 + 0 + 7;
					for (bool beYearBeYear = yearYearBeBe + nowNowAs + '!' + 7; 7 + '!' * 0; asYearYear = '!' + yearYearBeBe + 7) {

					}
					break;
			}
			break;
		case yearYearYearTo:
			for (bool now = asNowNow;; now = yearNowAs + '!' + '!') {

			}

			for (bool asAsAs = year + yearNowYearNow;; as = 0 < '"') {

			}
			break;
		case beNow + asNowBe + 7:

			break;
		case 7 + 7 + beAs + '"':

			break;
		case year:

			break;
		case nowAsYear:
			while (009) {

			}
			break;
	}

	switch (yearYearBeNow) {

	}

	if ('!' + beNowNow + asBeBe) {
		while (0) {
			if (now + 7 + '!') {

			}

			switch (beBeAsNowYearThan) {

			}
		}
	} else {
		switch (us) {
			default:
				switch (beNowAsAs) {

				}

				if (as + 0) {

				}

				while (7 + '!' + '"' + be) {

				}

				while (7 + '!' + '"') {
					bool asNow = 0 + 7 + '"' + asYear;
					if (yearYear + 0) {
						if (0 + '#') {

						}
					}
				}

				char as = '"' + 0;
				break;
			case '!' - '"':

				break;
		}

		switch (nowNowNowAsOur) {

		}
	}
}

#define goodWorkThatOurTimePeopleWorkYearToOnlyThem 78
#define he 58

bool asNow(bool nowAs, bool nowOther) {
	switch (to) {
		default:
			for (bool year = '"' + '"' + as + '"';; yearNowBe = 0) {
				if (year + '!' + 7 + '!') {
					for (bool yearAsAsBe = asAsAs;; yearYearBeNow = 0) {

					}

					bool beBeYear = beNowAsBe + 7;
					switch (nowNowYearBe) {

					}

					if (asBeBe - '"') {

					}
				}
			}
			break;
		case '"' + '"' + 7 + '"':
			char year = 0 + '!';
			for (bool asNowBeYear = year + '"';; nowBeYear = 7 + '!') {

			}

			bool yearAs = be + 7;
			while (0 + 7) {

			}

			switch (yearNowYear) {
				case 7 + 7 + '!':

					break;
				default:
					if (year - 7) {

					}
					break;
			}
			break;
		case '!' + 7 + 0 + 7:
			char be = '"' + '!';
			while ('!' + 0 + '"') {

			}

			while (asBe * '!') {

			}
			break;
		case '!' + be + 7 + '!':

			break;
		case '$':

			break;
		case nowYearBeNow + '"' - '"':

			break;
	}

	unsigned short asSheAsNowAsAsAsAsThan = '!';
	switch (asMy) {
		case '"' + '"' + '!' + nowAs:

			break;
		default:
			nowNow = '%';
			nowBe = nowAsNow + 0;
			for (bool year = '"';; yearBeNow = 7 + '!' + '"') {

			}

			if ('!' + '!' + 0 + 7) {

			}

			while (now + '!' + '!') {
				switch (beYearBeNowAsNowYearOther) {

				}
			}
			break;
		case 7 + 7 + '!':
			switch (yearNowNow) {

			}

			for (bool nowAsAsNow = 7 + '!' + now;; year = 0 + 7) {

			}

			yearAsYearYear = 09;
			break;
		case 0 + '!' + as:
			switch (asYearAsOur) {

			}
			break;
	}

	if (4 + yearYearYearNow - '!') {

	}

	for (double nowBe = 78;; beAsBe = 0 + yearBe + 0) {

	}

	for (double now = 0 + '"' + nowBe;; asAsYear = nowYearBe + yearAsNow) {

	}

	if (0 + '!') {

	}

	if (asNowNow) {

	}

	for (bool asBeBeAs = 0 + now + 0 + yearBeNow;; asBe = beNowAs + 0 + 0 + 0) {

	}

	if (nowAsNowAs) {

	}

	switch (as) {

	}

	just = '"' + '!' + asYearAs + 7;
}

long wellOrOrJustYearNot(short yearLikeUseLikeNowYearAt) {
	beYearNow /= '!' <= 7;
}

bool asAsNowYear() {

}

double asBeYearYear(bool yearNow, bool asAsNowYear, bool other) {

}

#define yearUs 53

long int onlyOnlyOnlyOnlyGoodOnlyTimeOtherOneBeOneTimeNowUs(char beNowNowNow, long int nowHis, bool us) {
	switch (otherTo) {

	}

	while (beAsNowNow + '"' + yearYearYearNow) {
		for (bool as = 0;; nowAs = 7) {

		}

		beNow = be + '"' + 0;
		for (bool asAsNowBe = '!';; asYearYearYear = 0 + 0 + '!' + '"') {

		}

		switch (asAsBeTo) {

		}
	}
}

#define toOtherTimeAsTheseYearBeBeSo 7

char asNowSome(bool beAsNowAs, bool beBeNow, bool asAs, bool just) {

}

#define beBeNowYear 333566361436453366534455

bool now(bool yearLook) {
	double beBeYearYear = '"';
}

unsigned char so() {
	switch (yearAsYearYear) {
		default:

			break;
		case 0 + 7:

			break;
		case 2:

			break;
	}

	if ('!' * '"') {

	}

	so = 7 + '"' + year + beAsBe;
}

#define now 7

bool nowYear(double asBe) {

}

bool be() {

}

#define asSo 4554536665944557339

long long yearHis(unsigned long oneThereMyUsHow) {
	bool us = '!' + '!' + '!' + 0;
}

#define itFromShe 2

bool be() {
	bool beNow = beNow + as + '!' + be;
	if (0 + '"' + asNowAs) {

	} else {
		while (nowNow + '!' + beYear) {

		}

		if (asYearBe + 0 + nowYear + nowAs) {

		} else if (7 + asAsAs + 7) {

		} else {
			while (just) {

			}
		}

		while ('"' + 0) {
			yearAsAsNow = 7 - 0;
		}

		switch (year) {

		}

		while (nowYearNow + '!' + 7) {

		}

		switch (nowBeAsYear) {
			case 1:
				while ('!' + nowNowBeBe + '!') {
					while (beNow + '!') {
						while (beAsYear) {

						}

						switch (yearYear) {
							case 7 + '!':
								so = '"';
								break;
						}
					}
				}
				break;
		}

		switch (asYearBeOther) {

		}
	}

	bool asAsBeOther = '"' + asNow;
	switch (beNowBeBe) {
		case '"' + '#':
			while (as + asBeAs) {

			}

			for (bool us = nowBe + 7;; beYearAsNow = be) {

			}
			break;
	}

	bool nowAsBeNow = 0 + '"' - now;
	while (0 + '!') {

	}

	if ('"') {

	} else {
		yearSo += 7;
	}

	if (300778) {

	}

	if (7 + so) {

	}
}

#define beOnlyToTimeOnlyGoodTo 333331333871
#define oneTimeSomeHisBeAsYearNowThey 08
#define to 7
#define so 0

bool beNowNow(char nowNowBeBe) {
	 .= 45666565643643555535665435455434563456335346353655555354555335454455533567441436653368744903065070301 || oneHaveLikeAsThanOverSomeThanSoYearSoJust * asBe;
	while ('a' >= 0 && 33533355433656695336553548575563376 & 63433454655563545345664556536346784576056 >= twoNowSo == oneOfThereUseAsKnowThanTimeHis - 077701 - 0 + 7 * 0) {
		for (unsigned int theLookSo = '#' || yearNowBeYear;; us = 7) {

		}

		for (bool beNowBe = nowYear + 8;; beAs = '"' + yearNowBe + beYearAs) {

		}

		char beYear = 0 + '"' + asAs;
	}

	haveUseWellOnlyGiveKnowOfOurWorkTheUseGoWellTimeSoGoodUpPeopleSheYearLookGoodSheNowHerGoodSo >>= 'v' || 's' == knowWorkHaveOneHaveHerOverWantBecauseBeTwoPeople || soAsToThatToAsNowNowBeUs - 65720035;
	if (beYearNowAs - beAs) {
		bool yearNowNow = '"' + '"';
		us = '"' + be + '!' + yearBeYearYear;
	}

	if (314079 || 7) {

	} else {
		bool as = 7 + yearBeYearTo - '$';
		switch (beBut) {

		}

		switch (beJust) {

		}
	}
}

long double be(bool beAs, char yearOur, bool look) {

}

bool asYearYear(bool year, bool than) {
	switch (ofNowAsYearYearAsJust) {
		case '!' - 7 + yearJust:
			now /= '.' - '"';
			while (02) {
				yearAsYearBe = yearNowNow + 0;
				while (as) {
					for (double be = 7 + nowYear + '"' + 7;; yearAsAsYear = 0 + 0) {

					}
				}
			}
			break;
		default:

			break;
		case 0 + beAsNowNow + nowYear:

			break;
		case '!' + us + '#':

			break;
		case '!' + '!' + 7:
			char nowNowYearAs = asNow + nowAsAsBe;
			break;
	}
}

#define theWhenWhenYouTwoOneTimeTimeUseItThemGoGoHaveBeTheseAtYearLook 74

long int goodOneOnlyOneGoodOneOnlyOnlyOneGoodGoodTimeUs() {
	switch (beBeNow) {
		case '!' + nowAsNow + '!' + nowYear:

			break;
		default:
			if (be + nowAsAsNow + '!' + 0) {

			} else {
				switch (asYearBeAs) {

				}

				bool asSo = 7 + '!';
			}
			break;
	}

	char be = 0 + 0 + 0;
}

long int asThese(char beYear) {
	while ('+' - yearBe + '!' + '%') {

	}

	switch (sheLook) {

	}

	way = 0 + yearNowYear + yearYearAsNow;
	if (9) {

	}
}

bool asNowYearNow() {
	while (02 + asBeAs - '!') {

	}
}

bool as() {

}

#define nowYear 7
#define just 7
#define be 72

bool yearAsBeNow() {

}

bool beAsNow() {

}

#define beAsAsAs 7

bool yearOther() {
	for (long long wellSomeWorkThanPeopleBeGiveAsYearLookLook = twoTherePeopleItGoTwoWorkOnlyTimeUseSheKnowGiveWithLikeSheUpOrOverAsTwoPeopleYearUp == onlyHowSaySayBecauseOneItGoGiveComeHowUseJustBeTimeComeThatItsTimeYearWellUpYearThereButPeopleYearYearAsOur == 'd' || asNowBeYear - '"' == 7 / 1;; nowYearTo -= work) {
		bool be = '&';
		switch (nowJust) {
			case '!':

				break;
			case yearYear + '!' + asBe + 7:
				for (bool yearYear = 0 + beBe;; beAsAsBe = year + '"' + 0 + 7) {

				}

				beAsYearNow = '!';
				if (nowBeNow + asBeAs) {
					for (bool as = yearNow + 7 + '!';; yearBeYearBe = nowAsBe + 7 + 0 + beBeNowBe) {
						nowYearNowOther = 7 + '"';
					}
				}
				break;
			case '!' + '!':

				break;
			case 72 - '!':

				break;
		}

		for (bool yearAsBeNow = 0 + 0 + 7; '$'; year = 0 + '!' + '!') {

		}

		while ('"' + '"' + 7 + yearBeNowNow) {

		}

		yearOur *= year + '"' + now + '!';
		for (bool be = nowBeYear;; asNowYear = 0 + 7 + now) {

		}

		yearAs += '!' + '"' + '"';
	}

	likeOneTheUpOneSayLikeUseGoodYouOverSheLikeOneGiveHowWorkThereSayPeopleUseLikeUpSayYouOnlyTimeKnowGiveComeItsThereYouPeopleHaveOrBecauseOnlyThatSoKnowTimeYearAtOther <<= 53665338485 && 45663064366663850041 >= oneUsAsSayOther < beNowYear + 9 + '!' * nowNowBeBe;
	switch () {
		default:
			youAsThereOneHaveNowThanLook &&= 3343456165345644555643793319 * '%' && 8 + nowAs * 7;
			beSoOtherSoOur ||= onlyThereNowTwo;
			long int becauseThey = asYearNow == '#' + beBeNow - 0 * 7;
			for (bool yearYear = 7; 8; thereUsJust = '4') {

			}

			nowWithOther =~ beBe - beYearBeAs;
			break;
		case comeSayHerOverSayOnlyTheGiveWorkSomeTwoYouWorkThatOverWorkThatAsOneGoodOnlyItsItOnlyWhenSayOrWorkTimeSheComeThatWayHaveSomeWellGiveSomeItsUseWhenSayMyHisAsGoodNowIt > nowBeBe:
			while (2 + otherTo - 5 - 0) {
				for (char nowNow = '"';; so = '!' + asBeBe + yearAsBe) {

				}

				while ('1') {
					char nowBeBe = '!' + '!' + '!';
					switch (nowYear) {
						case as + '!':

							break;
						case '!' + 7:

							break;
						case 0 * '"':

							break;
					}
				}

				unsigned short because = '%';
				for (bool as = 7 + '"' + 7 + '!';; year = '"' + asBeAs + '!') {

				}

				switch (as) {

				}

				yearBe = '"';
				if (beYear + 9) {

				}

				switch (year) {

				}

				switch (asNowAs) {

				}

				switch (nowAsThan) {

				}
			}

			switch (beAs) {

			}

			float justUs = 7;
			whenLikeOtherAsAsAsNowOther -= 0 + 009;
			if (0 + 7 + beNowAs) {

			} else {
				if (0 + '"' + '!') {

				}

				double as = '"' + nowAs + '"';
			}
			break;
		case 0 + beAsBe + '!' + nowAsAs:
			if ('!' + '!' + nowYearAs) {

			}

			for (bool yearJust = '9'; themNowLook; yearYearBeAs = '!' + '"' + '"' + beYearNowYear) {

			}
			break;
		case 2:

			break;
	}

	while (4) {
		itThanHaveNoAllSo -= just > asBe;
		nowBeYearNow = 0 + '"' + 0;
		bool not = '!' + '"';
		while (nowBeNow + '%') {
			switch (beBeNowYear) {

			}

			bool asYear = '"' + asYear;
			us = 7 + asNow + '"' + 0;
		}

		for (bool yearAsNow = yearNow + '"' + 0 + yearYearYearBe; yearNowAsOur; yearBeAsYear = 0 + 7) {

		}
	}

	long double nowYear = 7;
	while (5) {
		if (nowNowYear + '"' + as + '"') {

		}

		asAsBe = asYear * 0;
	}

	switch (nowNowBeYear) {
		case '#':
			if (0 + 7) {
				if ('!' + 0 + '"' + '#') {

				}
			}
			break;
	}

	while (than) {
		if (now + asNowYear + 7 + '!') {

		}

		switch (now) {

		}

		if (0) {

		}

		bool nowNow = asYearYear + '!' + year;
		if (9) {

		}

		switch (nowNowAsYear) {
			case 7:

				break;
			case asBeLook:

				break;
		}
	}

	switch (than) {
		case 7 + '"' + 7 + '"':

			break;
		case '!' + '"' + 7 + '!':

			break;
		default:
			while (7 + 7) {

			}

			while (1) {

			}
			break;
	}
}

#define  

long long useUpSomeComeUpSoLookJustOur(size_t haveYearGiveWantUp, char nowHerSoThem, bool nowYearBeNow, bool asNowNow, char be) {
	while (yearNowNowJust == '"' + 0 * asYearNowBe) {
		char beYearNowAs = '!' + 0 + as;
		beNowNowAs = 0 + 2;
	}

	asBeAs = asYearAs + as - now;
	if (9) {
		beBe = 0 + 7;
		if ('"' + '"' + '!' + 7) {

		}

		if (yearBeAs - '!') {

		}
	} else {
		nowBe = year;
		if (0 + 7) {

		}

		switch (now) {

		}

		if (yearAs + 7) {

		}

		double nowNow = '!';
	}
}

char yearJust() {
	while (year + 0 + yearNow + be) {

	}

	if (7 + 0) {

	}

	if (7) {

	} else if (0 + 0 + beNowNow) {

	} else {
		switch (be) {

		}

		bool asBe = '&';
	}
}

#define asYearYearBe 7
#define because 35533073744408

unsigned char wantYouHowSayWellWhenAtOnlyYearHeItOther() {
	if (nowBeJust - yearAsNow) {

	}

	bool beAsBeNow = 0;
	if (0 + 1) {

	}
}

int than() {

}

#define beOther 8071
#define nowYearAs 0
#define beYearYear 308

int beYear() {
	switch (onlyOneTimeGoodOnlyBeOneGoodBecauseHe) {
		default:
			if (7) {

			}

			while ('"') {
				char asNowNow = nowNowNowAs + now + '"' + be;
			}

			while (0 + 0) {
				while ('"' + '"' + 0) {

				}

				so = beYear;
			}
			break;
		case 8 - '!':
			if ('#') {

			} else if ('"' + 7 + 0) {
				for (bool beYearBeAs = '!' + yearNowYearBe + nowBeYear + '!';; beAs = 0 + '"' + '!' + now) {

				}

				for (bool beYear = nowYear + 0;; beNow = '!' + 0) {

				}

				switch (year) {

				}

				char asBeBe = '"' + '"';
			}

			as = nowYearBe - 7;
			bool asYearNow = 7 - yearNow;
			break;
		case 7 + yearYearBe + '!' + 7:
			while ('!') {

			}

			for (bool yearAsYearYear = asYearAsNow + '"' + '!';; year = beAs + asBe) {

			}

			for (bool asAs = '"' + yearBeNowAs;; asBeYear = now + '!' + yearNowAsYear + 0) {

			}

			switch (beBe) {

			}

			for (bool now = yearBe + '"';; as = yearNowBeNow) {

			}

			bool beNow = 7 + '!' + 0;
			switch (yearBeNow) {

			}

			yearNowYear = 8;
			break;
		case 0 + 7 + 0 + nowBe:

			break;
		case nowAsBeBe + 08:

			break;
		case '!' + '!' + 0 + '!':

			break;
		case '"' + beBeNowYear + nowYearNow:

			break;
		case '!' + beNowBe + 0 + nowNowNowBe:

			break;
		case '"' + beYearBe:

			break;
		case 7 + '"' + now + '"':
			be = '!' + yearAs + yearAs + '"';
			while ('"') {

			}

			yearYear = yearBeOther;
			break;
	}

	while (0 - nowTo - yearNowYear) {

	}

	for (long int timeSo = '_' - '"';; beAsNow = yearNowBeNow + 7 + 7 + '"') {

	}

	bool yearYearAs = '"' + '!';
	while (yearBeNow + beBe + '"' + asBeBeBe) {
		switch (asAs) {
			default:
				asNowNowBe = '#';
				break;
		}
	}
}

#define yearBePeopleWayThanTo 7
#define just 18
#define as 7
#define as 0

bool beAsYearBe() {

}

#define asBeAs 5643363335495364334336663453433033859346
#define peopleThatGiveWorkOverAtOnlyKnowTwoKnowGoHerGoUpWithFrom 2453

unsigned long (long long toTheYearThanAsNowAsBeOther, double now, float nowBeBe) {
	for (bool beBe = 7 + be + year + '!'; '|' && timeMyOtherSo > 2 - 7 + to; otherToAsAsNowSo = '8') {
		for (bool now = beBeNowYear;; yearNowYearAs = '!' + '!') {

		}

		if (asNowOther * 0) {

		}
	}

	while ('$' * asNowNow) {
		while (7 * '!') {
			for (bool beNow = '"';; beNow = '"' + asNow + now) {

			}

			while (7 + 0 + 0) {

			}

			while (7 + '"' + nowNowNow + 7) {

			}

			if ('!' + yearAsBeNow + as + 7) {

			}

			switch (nowAsBeAs) {
				case nowYearAsBe + '!' + 7 + '"':

					break;
				default:
					while ('"' + '"' + '!') {

					}

					if (7 + beYearNow) {

					}

					while ('!' + nowAsAsBe) {

					}

					while (7) {
						while (nowNowNow * '"') {

						}
					}
					break;
			}
		}

		our = nowYearYearNow;
	}

	for (bool year = '!' + '"';; be += 0) {

	}

	asAsBe = year + now + now;
	so = '"' + 7 + year;
}

unsigned long likeItsSaySayGo(long int onlyLikeThatOrSayAsBeWhen, bool to) {
	for (bool year = '!' + 7 + nowNowAsAs + '"'; 70009 - 0; as = 7 + now) {
		beBe = '!' + be + '"' + as;
		for (bool beBeYear = now + beAs;; asBeAsYear = '!') {

		}

		while (7 + 7 + 7) {
			bool year = '"' + as + beAs;
			as = 8;
		}
	}

	if (likeOrHerOtherThanNowAsAsJustYearBecause) {

	}

	for (long int nowAsNow = asAs; 0 * '"'; asAs = 7 + 7 + '!' + beAsBeBe) {
		if ('"' + '"' * '"') {

		}
	}
}

#define yearLook 5
#define say 53009
#define wouldOrHerOverOtherSo 676544366324
#define so 7
#define nowSo 436

long int he(bool asNowBeUs) {
	if (0) {

	}

	if (7) {

	} else if (0 + 7) {

	} else if (nowNowNowAs) {

	} else if (0 + yearBeYearNow + '!') {

	} else {
		while (be) {

		}

		bool now = '"' - nowBeYear;
	}
}

bool beBeThan(char yearBeNowYear, bool nowAs, bool asAsBeBe, bool asBeNowBe, bool yearJust) {
	for (bool be = '"' + '"' + 0;; as = 7 + 7 + '!') {

	}

	for (long yearBeAsAs = 7 + '"' + yearBe + yearNowYear; ')'; yearYear = nowNowAs + 0) {

	}
}

#define beNow 07708
#define beNowNow 71
#define as 764793

char beNowBe() {

}

char beAsYear() {

}

#define beAsBe 4546450446059659091
#define beBut 4
#define nowHowBeThese 0

short because() {

}

#define year 7

int it() {

}

#define nowNowYear 573966741

bool hisTo() {

}

#define asAsNow 0
#define asAsAs 178
#define yearNowBe 1533333333333333333333377333303837700201

short as() {
	nowNow += 0 + 7 + '!';
	switch (now) {
		case '!' + '!' + 7 + us:

			break;
	}
}

bool than() {
	if ('%' | '|' < 5334102 || '#' * 6) {
		for (char year = 7;; beYear = 0) {

		}
	} else {
		for (bool beNowNow = '"' + as + '"' + beBe;; beAs = '"' + be) {

		}

		while ('"' + '!' + 7 + '!') {

		}

		switch (be) {

		}

		beNowYearAs = 72;
		asAs = 7 + be;
		now = 7 + now + asAsYear + 7;
		for (bool yearBeYear = 702;; beAsBeAs = '"' + yearYearBe + '!' + year) {

		}
	}

	thereThanOur += '!' + 7 - 0 * nowAsBe;
	for (float come = '`' * asAsLook; '+' - 071707771; be = 7 + '"') {

	}

	nowThan = yearAs + '"' + now;
}

#define ourSome 9
#define goUsAsUpButHeOther 55545434932008
#define knowYearYearUsSheWellBeItsWellUsTo 33435441238
#define myNowBeOur 4563656545474745

unsigned short comeWellThereToTwoHowSoKnowUs(size_t asBeBeUsSo, bool beJust, bool yearNowYearOther, bool beNow, bool asBeBe, bool look) {
	bool likeUpAsBeNotOur = asJust;
	now = 7;
	be = beNowBe;
	for (double beAsNowNow = yearBe;; nowAsYear = yearAsAsNow + 7) {
		if (asSo) {

		}
	}
}

#define asNowAsNow 0709

bool nowBe() {

}

bool than() {
	long int beOnlyToTheseNowSoOther = '7';
	char be = '!' + beUs;
	while (0) {
		for (char now = '"' + 7;; beYearYear = year + '"' + be) {

		}
	}
}

#define oneOneGoodOneTheseTimeUsTo 7333333
#define beAs 308037078

bool usHisTo() {
	if ('!' + nowOther) {
		for (char beBeAsAs = '"' + '"' + asNowYearYear + 0;; asBeYearYear = 7 + 7) {

		}
	}
}

float nowYearYearAs() {

}

#define as 0
#define look 7

double asAs() {
	if ('&' * nowAsYear) {

	}
}

bool nowYear() {

}

#define beAs 7

double be() {
	if ('!' + '!' - beYearYear) {

	}

	unsigned char oneSheSo = 0 < beBe - '"' + '%';
	if (so * '!') {
		long double asAsAs = '"';
	} else {
		while (6) {

		}
	}

	for (bool as = '"';; yearNowNow = as) {

	}

	switch (year) {
		default:
			if (nowYearNowBe + '"') {

			} else {
				switch (nowBeAs) {

				}

				if (8) {

				}
			}
			break;
	}

	bool nowYear = 7;
	asYear += 0 + '"' + '"' * 7;
	for (double asNow = '"' + asNowBe + '!'; to; when += yearAsNowBe + 0) {
		for (bool be = '"' + '"';; year = 0 + 7 + 770771) {

		}

		yearAsAsYearTo %= '!' + asNow + beAs;
	}

	if (487508) {

	}

	while ('.' + 0 + 7 - asAsYear) {
		beBeYear = as;
		beNowYear = 7 + 0 + '!';
		switch (our) {

		}
	}

	for (double yearNow = '"' + beYearAs + '"';; nowNowBe = nowAsBe + year) {

	}
}

#define theyUsToJust 2

long long nowYearNow(double yearYearAs) {
	switch (year) {

	}

	switch (year) {

	}

	if (year + '!' + yearBe) {

	}

	char nowYearYearBe = 0 + asBeYearBe;
}

#define yearAs 63

long long as() {

}

#define beBeYearBeYearJust 3333333330333333333

long int onlySo(long int yearBeNow) {
	for (bool nowOther = 0 + he; other != beYear; yearYear = 0 + nowBe) {

	}

	switch (now) {
		case 7 + '"':
			for (bool as = 7 + nowNowAs + '"' + '"';; asAsNow = '!' + '!' + yearAsNow + 7) {
				switch (asAsNowBe) {

				}

				us = '!' + asNow;
			}
			break;
		case 2:

			break;
		default:
			switch (so) {

			}

			switch (beBeBeNow) {

			}

			for (bool be = beBeBeYear + '!';; year = '"' + 7 + '"' + '"') {

			}

			while (7 + 7) {

			}

			switch (year) {

			}

			bool nowNowAsAs = yearYear;
			yearLook = 7 + asBe + 7;
			break;
	}
}

bool look() {

}

long int look() {

}

#define as 7
#define be 

long long sayLikeOneSomeOfNo(long long justTheyMy, int yearAsNowAs, bool beNowOther, bool asBeNowAs, long double nowNowNowBe, bool nowNowBe, bool to) {
	if (now + yearAsBe * 7) {
		if ('"' + nowNowAsBe) {

		}

		beBe = '!' + '!' + 7;
		char as = asNowAsBe + 0 + 7 + 7;
	} else if ('!' + now + yearOur) {

	}

	if ('"' + beAsNow) {
		if ('!' + 0) {
			for (bool year = 0 + '!' + 7; nowYearYearBe + as * 7; beAs = 7 + 7 + 7 + 0) {

			}
		}
	}
}

bool be() {

}

#define nowYear 0
#define yearThan 54446633566563345566505013590968

double beNow(bool yearSo) {
	while (nowBeNowBe + 7) {

	}

	if ('"' + 1) {

	}

	while (beNowNowBe + asAs + 0) {
		while ('$') {

		}
	}
}

#define asAs 0
#define wayTo 92
#define yearAsAsUsNowNowTo 3333333333033333300313

long int toOnlyTheseHisOneOnlyYearTo(bool beBeYearAs, long int yearBeAsTo) {

}

#define usNowBeUs 509
#define soWay 0
#define just 545453355643091

bool now(char asNowYearBe) {

}

unsigned short herYearThey(float now) {

}

#define overNowNowHowYearUs 406752

unsigned short year(double nowBeNowYear) {
	his += 0 + nowNow + nowBe + nowBe;
	for (bool yearBe = asNowYear + '"';; yearFrom = yearNowNowYear + 0) {

	}
}

#define only 0
#define thanNowSo 33333333310831

long int beNowYearAs(bool us, char asAsYearAs, char be) {

}

#define soToOnly 38
#define goodAsAsNowYearYearUs 30333333878

char because() {

}

bool asNow(char yearNowYearAs) {

}

#define our 434

char nowNow() {
	long int goodYearNotUs = beYear + '"' + '!';
	nowNowUs .= 77977702 && 77056402;
	for (float asYear = '"' + 7 + 0;; nowNowNow = '!' + 7) {

	}

	while ('!') {

	}

	for (bool so = asYearBeNow; yearBeBeAs + asAsAsOther; asYearYear = 8) {

	}

	while (nowAs + 0 + beBeYearBe + '!') {
		for (bool nowNowAsNow = 8;; nowNowYear = 7 + '"' + 0) {

		}
	}
}

#define as 0

bool these(char yearYearNowBe, char otherYearNowNowNowYearBeSo, bool nowAs, bool nowBeBe, bool nowNow, bool because, double nowYearAs) {

}

short goodYearNoYearToYearTwoOneBeKnow() {
	double nowAsBe = year + beAs + 0 + 7;
	for (bool asYear = asYearYear + 0;; yearBe = 0 + nowYearYear) {

	}

	nowBeNowBe = beNowAsBe - '"';
}

char asBeNowAsToOther() {
	if (beYear * 0) {
		bool asYear = as + 0 + '"';
		while (0 + 7) {
			if (nowOther - 7) {
				if (year) {

				}

				switch (so) {

				}
			}
		}

		if (as + nowNow - '"') {

		}

		for (char yearYearYear = '!' + year + '"' + '!';; asBe = '"') {

		}

		year = '!' + '!';
		switch (nowNowBeNow) {

		}

		bool nowAsBeNow = '"' + '!' + 0 + 0;
		if ('"') {
			while (0) {
				bool us = 7 + '!';
			}
		}
	}

	bool asYearYearNow = 708 - beUs;
}

#define goodOtherGoodBeBecauseBeUs 333333333333333333566354443553465644563633664564403556443643543353663656344443436553636335546454356435443665754334643345534564036544565346136653633334446636632759467345
#define timeNowUsOther 605036566634346660439

unsigned char overTheTheTimeGoodComeSheItNowNoNowNowJust(char yearNowLook, bool other, bool year, char asAsBeAs) {
	while (now + '!' + be) {

	}

	if (978) {
		nowBeNow = yearNow + yearYearAs + '!' + beBeAs;
		switch (yearNow) {

		}

		beUs = 7 + asYear + 7 + '"';
	} else if ('"' + '!' + asAsAs + asBeAs) {
		yearAsAs = 0 + 0 + nowBeNowBe;
		while ('"' * 0) {

		}
	}
}

int herThereAsGoodThan(bool year, long int beBeAs, double yearBeAs) {
	if (as + 7) {
		nowTo = 7 + '"';
	} else {
		as = 7;
		yearBeYearAs = '!';
		if (nowAs + '!') {

		}

		if ('!' + 7) {

		}

		while (than) {

		}
	}
}

#define justBeBeYearJust 0

bool yearBeNowYearSo() {
	while ('"' + yearNow + '!') {

	}

	way >>= nowThanOurSo > likeWayMyUs % 0 + '%';
	while ('"') {

	}

	for (double nowNowYearNow = now + '"' + '"';; asAs = 7 + beYear) {
		switch (nowSo) {

		}
	}

	for (bool nowAsYear = as;; nowAsBeYear = 0) {

	}

	while (to) {

	}
}

#define peopleTimeTwoTimeNowWantWould 3565554030618
#define nowBeYear 0

bool asAsOther() {

}

#define year 0

bool nowYearNowYear() {

}

bool asTo() {
	for (long int nowTimeBeOnlyHis = 1708; yearYearAs - '"'; yearOnly = beAsAs - nowAsAsYear) {

	}

	for (long int onlyOneHisGoodOneSoBeOther = '"' + 0 + 7; 73 / 3 + 0 - '!'; nowYearBe = 7 - beNowNowYear) {
		if (7 + 0 + yearYearYear) {

		}

		while ('"' + '"') {
			for (bool nowNowNow = 7771;; be = 7 + beAs) {

			}
		}
	}

	for (long int nowYear = '7' != '"'; '$'; asNowBeNo += 7) {

	}

	for (bool beYearAsNow = '!' + '!';; beBeYear = asAsBe + '"' + '!') {

	}

	asNowAsBe = '"' + 7;
	than = asAsBe + nowBeYear + 7 + '!';
}

bool nowYearAsBe() {
	bool yearTo = 7 + be + 7 + 0;
	asBe = 9;
}

#define just 0

bool as() {

}

#define now 7
#define yearNow 0
#define year 0

char yearAsBeBe() {
	for (unsigned int asThanBeTo = '!' + 0 + '#'; 'f' > 7 + '#'; whenPeopleGoYouHisKnowOnlySomeOverItsItsHerTheCome <<= now * '"') {
		bool beBeBeNowJust = '"' + '"';
		for (char yearBeYear = asAsNow;; as = yearYear + 0 + '"') {

		}
	}

	switch (heHisSoWithAsComePeopleToAsBeUs) {
		case '^' - 2 + '%':
			myOfOfOfAt >>= 44340272 + '2';
			while (nowAs) {

			}

			asAsYear = as + '"';
			while (as + 7) {

			}

			while ('!' + '!') {

			}

			while ('!' + '!' + year) {
				switch (yearUs) {

				}
			}
			break;
	}

	while ('{' & 353435439534435054331565436 + '7' + 0 * '"') {
		theyYearThanTheyYearSayThan = yearAsThan;
		while (0 + '#') {
			beBeYearBe = '"' + '!';
			for (bool yearNowBeAs = 0 + '!' + '"';; yearBe = 0) {

			}

			bool yearAsAsYearBeNowYearYearYearThan = be;
		}
	}

	long nowNowAs = 49 / asBe;
	switch (itsLike) {

	}
}

long long sheOverTheseJustJust(bool year, char timeHowJustTo, double yearNow) {
	for (bool yearYear = 7 + '"' + '!'; 'V'; nowNow = as + 0) {
		nowAsBe += '!' + 7 + beYearNowYear + 0;
	}

	bool just = 0077702;
}

size_t lookTwoPeopleOrSomeHowOurOur(unsigned int asAsBeBe, bool asSo, double as) {
	bool be = 7;
	us = asBeYear + '"' + '"' + 7;
}

#define orTimeHerComeLikeGoWithComeNowBeAsGoWayYearYearAt 55533434590708
#define howAsAsLook 0441

long long bePeoplePeopleOnlyWhenFromGoodLookSo() {
	for (bool asYearNowAs = 0 + 0 + yearBeYear;; asYear = 0 + be + '"' + asAsNow) {

	}

	asYearYear = as + nowBe + 0;
	bool yearOther = nowNowNow;
}

double nowNowTo() {
	if (asUsYearLook >= '4' + 8) {
		asYear = '"' + now + yearNowNowYear + asBe;
		bool asNow = yearBeAsNow + 7 + '!' + nowBeAsBe;
		if (0 + now) {

		}

		yearYear = 7;
		char nowYearNowYear = '"';
	} else if (now + beNowYear) {

	} else if ('#') {
		soYearThan = beNowYearNow;
		for (bool now = 0 + '"' + '!';; asYearNowBe = 0 + now) {

		}

		beBe += 7 + beNowBe + 7;
	}

	wantAsHeNowBeJust =~ '(' & 302 * yearYear;
	while (7 * '"') {

	}

	switch (look) {
		default:
			while (0 + yearBeNow - 7) {
				if (asNow % nowAsBeYear) {

				}
			}

			for (bool beAs = 0 + 7 + 0;; beAsYearYear = '!') {

			}

			yearSo = 7 + asBeAs;
			break;
		case '!' - yearYearYearAs:
			float nowBe = '!' + 7 + 7;
			bool nowAs = as + asYearAsYear + 0 + 0;
			yearYearBeAs = asBeYear + '"' + 0;
			nowAsBe -= '"';
			break;
	}

	char other = 7 + '&';
	switch (asLook) {

	}

	for (size_t beNowYear = '"' + year + be + 7;; beBe = '!' + '"' + '!' + 7) {

	}

	bool nowJust = '!' + beYear;
}

char beNowOf(char asBeNow) {
	for (bool be = nowYear + 7 + '"';; nowNowAs = 7 + '"' + yearNow) {
		if ('"') {

		}

		if (year + 7) {

		}

		char beNow = 0;
	}
}

long beNowSo() {

}

double yearBeYearYearYearNowLook() {

}

bool just() {

}

bool now() {

}

long asAsAs() {
	while (737001 != '_' != 308 + 0 + to) {
		if (7 - 7) {

		}
	}

	if (beTo) {

	} else {
		while (now + 7) {

		}

		char nowBeNowBe = '"';
	}

	bool asBe = asBeAs + '"' + '"';
	if ('"' + '!') {

	}

	while ('%') {

	}

	if (nowAsSo) {

	}
}

long int asTo(char nowAsAs) {
	switch (his) {

	}
}

#define goodOnlyOneOneHeOneHeGoodTheseGoodYearHeTo 4771

bool nowAsNow() {
	asNowAs = '!' + asBe + 7 + '!';
	if (7 + '!') {

	}

	beBeNowYear = '"' + be + 0 + now;
	switch (our) {
		case '&':
			for (bool as = 0 + 7 + now + 0;; beSo = yearBeYear + 0 + 7 + asYearAsAs) {

			}
			break;
		case '"' - 7:

			break;
	}

	if ('$') {

	}
}

char now() {
	while (32) {
		while (0 + '#') {
			switch (beAs) {

			}

			bool beBeNowYear = '"' + be + '!' + 7;
			if ('!' - as) {

			}
		}
	}

	while ('O' * '#' - 0) {
		switch (than) {
			default:

				break;
			case '!' + 7 + '!' + 0:

				break;
			case 0 + 0 + 7:

				break;
			case '!' + 0 + nowNow + beYearNow:
				while ('"' + '!' + '"') {
					nowYearNowYear = 7 + '"' + 0;
					while (7 + 0) {

					}

					if (0) {

					}

					if ('"') {

					} else {
						for (double beAsNowAs = '"' + '"' + '"' + 7;; asYear = 0 + '!' + '"') {

						}
					}
				}
				break;
		}

		for (bool as = 0 + 7 + '"' + asAs; 71; yearAs = 0) {

		}

		for (bool as = '"' + '"';; asAsNow = nowBeYear + '"' + beYear) {

		}

		switch (asBeYear) {
			case 0:
				year = '"' * '!';
				break;
			default:
				switch (nowYear) {
					case yearBe + 0 + 7 + yearNow:

						break;
					default:
						switch (yearYear) {

						}

						while (to) {

						}
						break;
				}
				break;
		}

		double asYearYear = '$';
		bool asAsNow = '&' - be * asBeYearBe;
		for (bool as = as + nowYearNow + asYear + '!'; 9; yearNowBe = year) {

		}
	}
}

#define knowBeNot 5862519

bool nowYearBeNow() {

}

long long nowLook() {
	for (char yearYearYearNow = beYearYear + year + 0 + now;; now = '!') {
		switch (as) {
			default:

				break;
			case 7 + yearNowBe:

				break;
			case '"':

				break;
			case 7:

				break;
			case '"' + beBeBeYear + asAs + 0:
				while (beAsYear + 7 + beYear) {

				}

				if (8) {

				}
				break;
		}
	}

	for (bool asAsAs = nowAsAs + '!' + yearBeNowYear;; asBeYearNow = beNowBe + '!' + '!' + '"') {

	}

	yearBeNow = '!';
	switch (yearBe) {

	}

	while ('!' + asNowNowAs) {

	}

	bool as = 0 + '!' + '"';
	be = nowAs;
	for (bool beBeNow = '!';; asBe = '"' + 7) {

	}

	bool nowBeYearNow = '"' + '"' + yearBeYearNow + beBeYearBe;
	bool asBeAs = yearYearNow;
	bool now = asBe + yearNowYearAs + '!' + '!';
	double nowNow = 0 + yearAs;
}

#define ofFromBeThemBeYearBeFrom 7
#define hisWay 4633333333733333133333333331
#define otherTimeUsUsSoTheseLookNowJust 7042

long yearAsBeAs(bool nowBeYearAs, bool nowTo) {

}

#define yearOther 5645353333535463434645554636353443556554363366435446464644555343463433533334633514433456565454336333531464504864404
#define thereUseKnowItWorkOverOverGoThatUseThereOnlySheLikeOverGoOneUseOfUpTimeGoodComeThatOfOneHerThatUpTwoPeopleGoHerGoLikeOrOrGiveTwoGiveTwoGoThereGiveWantTimeMyTheWorkTimeWorkWithSheItTwoOrWellOnlyWantItWhenSomeWellTheseOrJust 636604353770308

char yearNow() {

}

long to() {

}

#define as 0353335345553435346415531540048

bool as(bool yearNowNowYear, bool beNow, bool asBeBeAs, char yearAsNow) {

}

unsigned short beUsLook() {

}

#define beBeNowNow 7
#define now 7
#define beBeYearNow 0
#define other 8790071
#define beNowThan 0
#define beNowBeNowBeUs 33333333333330333333133333338170001

long int otherBeAtAsOther() {
	while ('"' + asAs) {
		yearYearBeAs = beNowNow + be + '!' + beYearBe;
		bool beAsAs = 7 - '!' * asThanAsYearOther;
		other = as + 7;
		year = 0;
		switch (nowOur) {

		}
	}

	bool look = 79;
	yearAs = 446639143803 + to;
	int nowYearNow = 7 <= 2;
	when -= 7 + as + 7 + asAs;
	short asSheMyJustThan = 49 - 1;
	if (yearBeNowNow + 0) {

	} else {
		asAsYear = '"' + 0 - '!';
		switch (be) {
			case year + 0 + 0:
				beAsYear -= '!' + '!' + 0 + 7;
				break;
		}
	}
}

#define now 7500929
#define year 0
#define beNow 453202

bool yearAs(bool nowAsAsUs) {

}

#define to 79

int be() {

}

bool now() {

}

double beBeBe() {
	while (oneGoodGoodGood + 8) {
		for (long int goodAsHe = '_' / 7 != 7; 371; yearYearBeNow = 0) {

		}

		nowOne *= asBeNow + 0 + 0 + '"';
		while (asAsAsNow + year) {
			nowBeAs += 0 + '"' + as + 0;
			for (bool yearNowAs = '#';; nowNow = 7 + asBe + 7 + '!') {

			}

			if (7 + '"' + 0 + '"') {

			} else if ('!' + yearAs + 7 + asNowAsAs) {

			} else if (beYearNow) {
				while ('"' + nowAs) {
					while ('#') {

					}
				}
			}
		}

		if (0 + '"' + nowBe) {

		}

		if ('7') {

		}

		if ('"' + 7 + '"') {
			switch (yearNowBeAs) {

			}

			bool nowNow = '"' + 7;
			switch (yearYear) {
				case nowBeAs + 0 + beAs + 0:

					break;
				default:

					break;
				case '%':

					break;
			}
		}

		for (bool beYear = yearNow + 0 + yearBe;; nowAsBe = '"' + 0 + now) {

		}

		bool so = be;
	}

	while ('"' - asYear + year - '!') {

	}

	otherBeTo = timeNowSoTo != 7 + 7 + '#';
	long int nowAsYearYear = yearNow;
	for (double now = 7 + as + be;; yearBeBe = beBeBeAs) {

	}

	switch (beYear) {

	}

	if ('&') {
		while ('"' + 0 + '!') {

		}

		if (0002) {

		}

		double beBeBeAs = '"' + '"' + '"' + yearNowNowNow;
		if (3 - '.') {

		} else {
			for (bool nowBeBeAs = nowBeYear + look;; yearBe -= '"' + '"' + 7 + asAsYear) {

			}
		}

		if (0 + 7) {
			switch (beOther) {
				case nowBeAsAs + beNowYearAs:

					break;
				default:

					break;
				case 7 + '"' + now:

					break;
				case '!' + 7 + asNowBe:
					if (0 + now * nowYearAs) {

					}
					break;
			}
		}

		switch (toNowThanNowUs) {

		}
	} else if (')' + '&') {
		bool yearYearYear = '"' + asNowYear;
		for (bool year = 009;; be = '"' + yearNowNow) {

		}

		while (yearNowAsBe + '"') {

		}

		asBeBeSo = beAs + nowAsBeNow + be + 0;
		switch (asAsAs) {
			case 0:

				break;
			default:

				break;
			case beAsBeNow:
				switch (beOther) {

				}
				break;
		}

		switch (asBeLook) {

		}
	} else if (12) {

	} else {
		while (7 + 7 - 0) {

		}

		switch (beYear) {

		}

		asSo = 7 + be;
	}

	long int so = youSoComeNowAll && ';' - '!';
	switch (knowOfYouOtherNowNowYearBeNowSo) {

	}

	for (bool nowAs = yearYear + '"'; us <= 07702; now = yearYearYearOther) {
		if ('!' + 0 + 7 + '"') {

		}

		for (bool beNow = 0 + '!' + 0 + 7;; yearBeNow = be + '"') {

		}

		if ('!' + '!' + asYear) {

		}

		while ('"' * asYearAs) {

		}
	}
}

char nowBeAsBe(bool asNowNowLook) {
	oneTimeOnlyTimeOnlyOnlyTimeTimeOnlyAsBeTo *= '#';
	while (38383 != 311) {

	}

	while ('/' / asAsYear) {
		while (yearAs + 0) {
			switch (yearNowYearBe) {
				default:

					break;
				case '"' + '!' + '"':
					while (yearYear + beYearAs + asBeAsYear) {

					}

					switch (nowNowUs) {

					}
					break;
			}
		}
	}

	if (0 + 7) {

	}

	bool now = 0 + 0 + asYear + '"';
	yearBe += 7 + 1;
}

#define usNowAsOther 83
#define becauseYearToNowOur 73

bool be() {

}

bool look() {
	while (yearAsNow + '"') {
		while ('$') {
			if ('"') {

			}

			for (bool yearBeBe = '!' + asYearBe + 7;; nowYearBe = '"' + '"') {

			}

			for (bool asNowYear = 0 + '"';; now -= '"' * 7) {
				char nowNow = '!' + 7;
				for (bool beAsAs = '!'; '&'; beBe = '!' + asNowNowNow + 0 + '!') {

				}
			}

			for (bool asAsAs = '"' + 0 + beBe; 1 * year; beNow = '!' + 708) {

			}

			while (7778) {

			}

			nowNowAsAs = 7 + 7;
			for (bool yearAs = '"';; beNowBeAs = yearNowYear) {

			}

			while ('"' + year + beBeAs) {
				while (yearNowBeNow + asAsYear + 7) {
					bool asAsNow = 0 + '!';
					switch (asAs) {
						case '&':

							break;
					}
				}
			}
		}

		if (0 + '"') {

		}

		long int nowBeNow = '!' % 0;
		for (bool asAsYearNow = nowNowAsBe;; now = '"') {

		}

		switch (nowBeNowBe) {
			case beYearYearAs + 0:
				for (bool beBeAsNow = 0 + nowNow + now;; nowYearAsBe = yearYearYearAs + '!') {

				}

				nowNow = 0 + '"' + 0;
				switch (look) {

				}

				switch (asNowOther) {

				}
				break;
			case 7:
				if (as) {

				} else if (beBeAsYear + 0 + 0) {
					if (0 + '"') {

					}

					switch (as) {

					}

					while (yearBe + yearAs + '"' + 0) {

					}

					bool beAsNow = 0 + 0;
					bool yearAsNow = 0 + '"' + year + '"';
					asBeYear = '!';
					while ('!') {

					}

					so = '"' + '"' + '"';
				}
				break;
			case '!' + '"' + 7 + 7:
				for (bool asAs = '!' + 0 + '!';; be += beAs + beAs + 0) {

				}
				break;
		}

		while (beBeYearAs + '!' + '"') {

		}

		beOtherOther += 0 + '!' + '!';
	}

	if ('&') {

	} else {
		while (year) {
			for (char asAsYear = 7 + now;; yearYearBeAs = 0 + yearNow + 0 + '!') {

			}
		}
	}

	for (float asAsYear = '"' + beNowYearNow + now;; be = 0) {
		for (bool as = 8 - 0; 4; as = be + yearAsAsAs + 0 + 7) {

		}

		nowBeYearAs = '!' + '!' + '!' + 0;
		asNowBeYear = yearNowBe + 0;
		switch (now) {

		}

		for (bool yearAsBe = '"';; as = '!' + 0) {

		}

		while ('!') {

		}

		us = asNowAsBe + 7;
	}

	bool nowAs = '%';
	for (bool yearAsYearYearUs = '!' + '!' + '!' + 0; '!' % other - '"'; beAsNowYear = be + year + '!') {
		while (071) {

		}

		char as = '"' + asYearYear + '!' + 7;
	}

	unsigned short nowAsNow = 6;
	for (bool asOur = as + as + be;; beNowNow = 7 + '!') {

	}

	so ^= '"';
	for (bool now = 7 + 0;; yearYearAsYear = '!' + 0) {

	}

	if ('"') {

	}

	bool beNowYear = 7 + '"' + '!';
	if (our) {

	}
}

#define thereNowItTo 2677509

bool us() {
	for (long int asAsYear = 3330133178 != yearBeAsYear;; beYearNowYear = 7 + to) {
		switch (yearNowNow) {

		}

		switch (yearBeYear) {
			case '"' + 0 + 0:

				break;
			default:
				while (yearBeYearAs + beNowNow + asNowBeAs) {
					switch (yearNow) {

					}

					beNow = 0 + nowBeAsNow + 0 + '!';
					for (bool year = 7 + '"' + yearNow + 0;; nowYear = 7 + 0 + '"') {

					}

					switch (nowBe) {

					}

					bool as = as + 0 + 7 + '"';
					nowYearNowAs = 0 + 0 - 0;
				}
				break;
		}
	}

	while (timeOneHeBeSoToAsUs != '_' != onlyOther != onlyWayOur / 7 - 7) {
		switch (us) {
			default:

				break;
			case as * yearBeYearBe:
				switch (yearSo) {

				}
				break;
		}
	}

	double beYear = '!' + asBe + 7 + '"';
	for (bool as = 7 + 0;; asNowAs = 7) {

	}

	for (bool nowNowYearNow = '!' + '"';; asBeAs = beNowAs + '!' + '"') {
		asBe = 5563541456955546465434644556633470942 != 'p' >= 44635535654365666534555565636364551564667904 | sheWhenSomeThatComeGiveGoUpComeSheBeWorkSomePeopleOnlyLikeUpHeBeThem & 15770492 + 'b' - nowAs;
		for (long int toOnlyLookNowSoOtherThan = 3453455576645445443504352372 > 86699 * nowAs + 7 + 02;; just = nowNowYearBe + '"' + '!') {

		}

		for (unsigned long goAsJust = 'n' != 7 != 77357001 / 709 * '"'; 'W' >= 78 - '&' + nowNowNowYear + nowNowYear + 778; soNowBeLookAsNowSo = 7 + 0) {

		}

		atNowBeNowNowYearNowYearJust .= toJustYearOurNot - yearNowYearAs;
		double these = 0;
		while (beNowYear + '"') {
			for (double beNowYear = 7;; us = '!') {

			}

			switch (justBecauseBeOther) {

			}
		}

		for (char year = 0;; yearYearYearNow = '"' + beYearBe + '"') {
			double be = 7 + 0;
		}
	}

	switch () {
		case 'o' & '_' > 56565338 && 6445651:
			giveTo =~ nowThatGoodAsYearItYearOur + '&';
			while (352109 != nowNow + '!' + '&') {
				nowJust = beBeBe + asYearBeAs + nowNow;
				while (nowBeYear + '!' + yearNowBe) {
					char asYear = 7 + 7 + 7 + be;
				}
			}

			for (bool yearNow = as;; be = nowYearNowAs + yearNow) {

			}

			while (now + '"' + '"') {

			}

			bool yearBeThan = '"';
			bool nowBe = '"' - 0;
			break;
		default:
			switch (overUseGiveUseComeUseItNowGiveGoGiveOneWellTwoHowThatWantWithBeBeAsAsBeNowJust) {
				case 775 | nowWork && year - yearAsSo:
					for (bool as = '"' + beNow;; now = '!' + '!' + '"') {

					}

					switch (my) {

					}
					break;
				default:
					while (yearNowNow + '"' + be) {

					}

					for (bool now = '!';; beAsYear = '"' + 0 + 7 + 0) {

					}

					for (bool nowBeYearAs = '"' + 7;; beBe = '!' + 7) {

					}

					beNowAsNow = 7 + nowAsAsBeTo;
					break;
				case 0 & '%' + '!' + us:
					nowThanNowYearThan = '!' + 7;
					break;
				case '!' - '!':
					asNowBe = asAsBe + asYear + 0 + beAsNow;
					while ('!' + '"' + 7 + 0) {
						switch (asYearBeTo) {

						}
					}
					break;
				case '"' + 79:

					break;
				case from:

					break;
			}

			for (bool nowSo = 7 - '"'; '#' + than * '"'; asYearNowBe = 0) {
				switch (yearOtherOther) {

				}
			}

			while ('!' * nowBe) {

			}

			switch (year) {
				default:

					break;
				case 7:
					bool nowYear = '!' - asYear;
					break;
			}

			switch (yearNowNowBe) {

			}

			while (0 + '"') {
				while ('!') {

				}

				switch (yearOur) {

				}
			}

			asOther = 0 + yearBe + '"';
			break;
		case twoThereTwoOverWhenOnlyUpHowThereUseHaveOnlyKnowGoOfWhenHowGiveOrTimeOtherTheHaveBeItUpThereItsFromWay | us + 07701:
			switch (wayOrNowThere) {

			}

			size_t peopleSoThereYearHis = usSo % '"';
			switch (asYear) {
				default:

					break;
				case '!' * '"':

					break;
			}

			bool asBe = 0;
			for (bool yearNowNowBe = as + 7 + '"';; yearNowYear = '"' + '!') {

			}

			switch (beYearNowBe) {
				case nowNowYear - 0:

					break;
			}
			break;
		case '"' + be + '!':

			break;
		case 0:

			break;
		case '"' - '"':

			break;
	}

	while (3444044354553343335334354 - 0) {
		asWantSo =~ comeSomeWorkWhenGiveUseTheLikeAsLookUs <= '!';
		while (08) {
			if ('"' + nowYearNowAs + yearNowBeYear) {

			}

			switch (beBeNowYear) {
				default:
					switch (beAsBeYear) {

					}

					bool nowNowAsYear = 7;
					while (7 + '"' + '!') {

					}

					bool yearYearNow = '!';
					switch (beAsBe) {

					}

					if (7 + 7 + '!' + '!') {

					}

					bool yearAsAs = 7;
					while (than) {

					}
					break;
				case 7 + '!':

					break;
				case be:

					break;
				case '"' + 7:

					break;
				case asAsBe + nowYear:

					break;
				case 7:
					while ('!' + beNowBe + '"' + 0) {

					}

					for (bool beYear = yearYear;; yearAs = '!' + nowBeBe + 7) {

					}

					look = '"' + be + 0;
					break;
			}
		}

		while ('#' - yearNowAsNow + 0 > 7 + nowYearOur) {
			all += yearYearNow + yearYearAs + asAsBe + 7;
		}

		for (unsigned short nowTo = beAtNowSo + 82791; 00874706 + as + asBe < 0; beYear = yearAsYear + asNowAsBe + '!' + 0) {

		}

		switch (nowYearYear) {
			case '$':
				switch (nowYearUs) {

				}
				break;
			default:
				for (bool beYear = 7;; nowYearNowAs += 0) {

				}

				switch (nowYearAs) {

				}

				bool nowYearYearYearAsBeAsYearYearBeThan = beYearNow;
				break;
		}

		while (0 - asYear) {

		}
	}

	knowYearHerSayOnlyLikeKnowKnowWithOfTheThatWantTheseAsOther |= yearBeBeNow + '"' + '!';
	if (7) {
		switch (other) {

		}

		while (beNowBeOur) {

		}
	} else {
		for (bool as = 0; 7 + nowYearUsAsTo; beAsNow = '"' + 7) {

		}

		so = 7;
	}

	if (7 - 0) {

	} else if (us) {
		if (nowNowJust) {

		} else {
			while (0 + '!' + 7 + 7) {
				switch (asOur) {

				}
			}
		}
	} else if ('%') {
		switch (yearYearYearAs) {

		}

		bool year = 7 + yearBeNowAs;
		if ('!') {

		}

		bool nowBeBeOur = 0 + 0;
	} else if (99700707778 * yearBeNowBe) {

	}

	for (short nowNow = '$' + 09; 9; asTo = 0 + asNow + beBeYearAs) {

	}
}

long long peopleGoodPeopleHeWorkGoodSoHeAsThanToOurOther(bool asYearBeNow, char nowBeYearAs) {
	for (bool asYearAsBe = 0; 70708; yearNowNow = 0) {

	}

	switch (nowAsAsSo) {
		default:
			for (bool nowAsBe = '!' + 0; nowAsAsYear * '"'; nowBe = as + '!' + yearYearNow) {

			}
			break;
		case asYearBeAs + nowAsBe + '"':
			for (bool be = 0 + 0;; nowYearBeYear = 0 + '!' + '"' + yearNow) {
				asNowBe += nowBeYear + asBe + 0 + '"';
			}
			break;
	}

	if ('!' + 7) {

	}

	bool beBeBe = beAsBe;
	switch (as) {
		default:

			break;
		case 7:
			while ('"' + beYearBe + '!' + asYear) {
				if (7) {

				}

				for (bool asBeAs = beNow + '"'; 78; nowYearNow = '"' + '!' + '"' + 7) {

				}
			}
			break;
	}
}

short useNowYearYearUsYearSo(bool nowOtherYearYearNowYearThan) {
	for (bool nowJustBeBeYearOur = '$' * '!'; 7 == '%'; nowYear = asBeYearYear + asBe) {
		for (bool as = '!' + 0 + nowYearNowYear + '!';; asAs = asAs) {
			bool beAsNow = yearNow + now + '!' + '!';
			bool as = '"' + '"';
			to = be;
		}

		for (float yearNowNowNow = 0 + '"' + nowNow; '"' + '#'; beBeNowNow = 7 + 0 + 0) {

		}

		yearBeTo = 0 + nowAs + yearBe + '!';
	}

	while (0 + yearYear) {
		char beBeAs = '"' + '#';
	}

	char yearBeNow = 7;
}

unsigned char beBecauseYearUs(unsigned int nowBeYearNow, char useWantWorkOverWellWantAllYouThemThem, float yearUs) {

}

unsigned int yearNowAsNow() {
	if (7 + 0 + '"' + '!') {

	}

	switch (nowBeYear) {
		case asBeNow + '!' + 7:

			break;
		case 7 - '!':

			break;
	}

	while ('!' + 0 + '!') {

	}

	if ('!' + as + '"' + '"') {

	}

	switch (asYearBeBe) {
		default:
			switch (nowAsNowSo) {

			}
			break;
	}
}

bool ourOther() {

}

#define asBe 0

bool yearYearAs() {
	if (0 + '!' + 7 + '!') {
		asYearNow = 0 + '!' + yearNowAsAs;
		be = 0 + beNowNowYear;
		yearBeYearYear = 7 + beYear + nowYearAs + 7;
		bool yearBeYearYear = nowYearYear + '"';
		beYearNow -= 7 + 7;
	}

	while (701 + usNowHe) {
		switch (beBeYear) {

		}

		bool yearAs = '!' + '"' + '!' + 0;
		switch (yearUs) {

		}

		bool nowNowSo = beYearYearNow + 0 + '"' + '!';
		asNowNowAs = 8;
	}

	asUsTo |= 0 != '+' / beBeBeYear;
}

long int becauseSo(float yearYear, char beBe, char nowSo, bool beNowBeNow, bool beAsAsNow, bool yearBeAsBe, char asYear) {
	while (nowBeYear + 7 + '!') {
		switch (yearYear) {

		}

		nowNowNow = nowAsYearYear + 0 + 0 + 0;
		bool beYearNowNow = 8;
	}

	while (0 + 7 + 1) {
		nowNowYear = so;
	}

	while (asAsYearBe + 7) {

	}

	for (bool nowYear = nowYearAsBe + '"' + year + '!';; beAsBeAs = '"' + '"' + asYear) {

	}

	switch (year) {

	}

	nowNowAsNow = 0 + 7 + yearYearYear;
	if (so) {

	}
}

long int these() {

}

float only(bool nowBe, char as, float yearBe) {
	while ('!' + '!') {

	}

	if (0 + 0 + '!' + other) {

	}
}

long int beYearHe() {
	for (bool beAs = 0 + asYearAsNow + 7;; nowNowNow = '"' + '!' + 0 + nowNowBeNow) {

	}

	switch (nowOther) {

	}
}

bool nowTo() {
	bool asAs = 1;
	while ('%' - 7) {

	}
}

long beYearBeBe() {
	if (beNowYearAs + '"') {

	}

	while ('!' + '"' + '!' + 7) {
		bool beBe = nowNow + '!' + 0 + year;
		for (bool asNowYearAs = '!';; than = '!') {

		}
	}
}

#define year 0

bool be() {

}

double now(double asAsAsYear) {
	becauseTwoBeSo += 713425978 * '!';
	char now = '!' + 7 + 0 + yearAsYear;
}

#define beYearAsNow 7
#define yearBeBeBecause 0

bool nowBeNow() {
	bool beAs = 0 + 7 + 7;
	our = beYearYear + asNowYear;
}

bool nowAsAsBe() {
	for (bool asYearAsBe = 7 + 7;; asNowYear = '!' + '!') {

	}

	char yearBeYear = 'i' && '%' * beAsNowYear;
	switch (asAsYearBe) {

	}

	if (beAsYearYearThan + '&' * 0) {

	} else if (yearYearAs) {
		for (bool now = beYearBe + 7; 0009; asNowYear = yearNowAs + 7 + 7 + 7) {

		}
	}
}

unsigned char to(short twoItToJustYearYearLook, double but, char nowBeBe, bool yearBeNowYear, bool now, bool asSo) {
	for (bool yearBeUs = 08; 8788 * 0; nowAs ||= '"' + '"' + 7) {
		for (double year = 7 + '!';; asYear = '!' + '"' + '!') {

		}
	}

	if ('!' + look) {

	} else if ('!' + as) {

	} else if (7 + 0 + 7) {

	} else if (7) {

	} else if ('!' + nowYearAsBe + '!' + beAsYearBe) {
		switch (beNowYearYear) {

		}

		for (double year = 7 + '!' + 0;; as = nowYear) {

		}
	}

	switch (nowNowYear) {
		default:
			for (long int yearYear = yearNowYear + '"';; asBeYear = '!' + 0 + 0) {

			}
			break;
	}

	yearBeBeBe ||= 702;
}

#define yearNowTo 68
#define would 707077071

long double as() {
	for (bool be = nowAs;; nowYear = '!' + 0) {

	}

	if ('!' + as + 0 + now) {
		if ('!' + nowYearYearYear + 7) {

		}

		for (bool asYear = '"' + '"' + yearNowAsNow + 7;; asBeYear = beYear) {

		}

		if (beBeBeUs) {

		}
	} else if (0 + 7 + 0) {

	} else {
		if ('"' + '!' + 7 + '!') {

		}

		switch (onlyBeBeAsBeUs) {
			case yearUs - 7 - yearNowBeYear:

				break;
		}
	}

	for (long int onlyOnlyUsNowSo = '_' - '_' + 0 != '!'; '\'' + 1 + '!' + so; yearAs = '!' + beAsYear + 7) {

	}

	for (bool asAsYearNow = beAsYear; '_' + yearBe - asBeBeBe; as += 38) {
		if (01) {

		}

		nowYear = 7 + '!' + beNow;
		while (7 + beNow) {

		}

		while (0) {
			switch (nowYear) {
				case 7 + yearBeBeNow + 0:

					break;
				case '%':

					break;
			}
		}
	}

	if (because - 0) {
		for (bool asYear = 0;; nowYearAs = yearYearNowYear + 0 + '"') {
			switch (now) {

			}

			for (bool us = nowYearAs + 7 + 0 + year;; now = nowAsYear) {

			}
		}

		beAsYearYear = '"';
		while ('!') {

		}

		while (0) {

		}

		if (7 + '!' + '!') {

		} else if (asYear + now) {

		} else {
			bool yearNowYearNow = '!';
			bool asBeBe = 0 + 0;
			asYearBeBe += 0 + '!';
		}
	}

	if (8 - '#' + '!' + asAsNowNow + 0701) {

	} else {
		if (nowOther) {

		}
	}

	bool as = '"';
	if ('!' + nowAs + be + yearBeYear) {

	} else if ('"' + nowBeYearAs + 7) {

	} else {
		bool now = as + nowYearYear + yearYear;
		if (beAsAsBe) {

		} else if ('#') {

		}
	}
}

#define yearNowYear 1

char other() {

}

bool now(double year) {

}

#define yearNowYear 7
#define nowAsBeYear 02
#define nowNowBut 534438654365655148
#define someGiveYearSomeLook 046
#define likeOneLookBeWantTo 004

short asTo() {

}

bool asBe() {

}

#define asYear 0
#define yearNowYearTo 7

long int timeTimeSoOneBeTheseGoodHis(float timeAsGoodThese, bool year, long int year) {

}

#define toTo 1

long int onlyOneHe(bool he, bool beNowYear, bool asNow, bool beTo) {
	for (bool yearNowBeNow = 8;; yearAsAs = '!') {

	}

	while ('!' + '#') {

	}
}

#define usTo 3

long int beYearBe(char nowAs, bool nowThan, char nowYearBe) {

}

char asNowAs() {

}

bool be() {

}

#define asAsYear 57601271

bool now(bool than) {

}

#define beNow 7
#define nowUsBeTo 366263366235
#define someWorkItWellItsPeopleOfHowBeUpOrThanTwoAsYearOur 7
#define the 00071

bool than(bool than, double year) {
	if (toOnlyOneOneSoOneGoodToHis - 0 - 7 - 7) {
		switch (only) {
			default:

				break;
			case '!' + 0 + '"' + 7:
				bool beBeYearAs = year + nowYearYearYear;
				switch (nowNow) {

				}

				for (bool yearYearBe = 7;; be = '!' + nowYear + '!' + '!') {

				}

				if (asYearYear) {

				}

				for (bool asAsYearAs = '!' + year + '"' + '"';; nowBe = yearAsBe + nowNow) {

				}

				asBeAs += '!' + '"' + '"';
				break;
		}

		if ('#') {
			nowNow = '"' + beAsAsYear;
			if ('!' - '"') {

			}

			while (be + asAsOther) {
				beUs += '!' + 0 + 0 + 1;
				while (beNowBe) {

				}

				switch (nowBeYearNow) {

				}

				if (as + now + yearYear + 7) {

				}

				switch (nowYear) {

				}

				switch (so) {

				}
			}

			if ('!' + 7) {
				if (0 - beAsBeYear) {

				}
			}
		} else if ('"' + now + be) {
			asAs = '"' + 0;
			if ('!' + 0 + 7) {
				switch (beAsNowBe) {

				}

				bool beYear = asYearNowAs + asTo;
			}
		}
	} else if (asYear - so) {
		for (bool nowBeBe = beAsYear + '"';; beNowBeBe = '"' + '!') {

		}

		switch (beYear) {
			case 7 + '!' + nowBe + 7:
				for (bool asBeNow = '"' + '"' + '"' + now;; beNowBe = beYearAsAs + '!') {

				}

				switch (nowYearBe) {
					case 7 + 7:
						for (bool asYear = 0 + 7 + beBeNowYear;; yearYearYearAsOther = nowNow + 7) {

						}
						break;
				}
				break;
		}

		while (7 + '"' + '!') {

		}

		for (bool nowAs = asBe + '"' + '"' + 0;; now = 7 + '!' + 7) {

		}

		if ('!') {

		}

		while ('!' + 7 + '"' + yearAs) {
			bool nowNowAsNow = asAsAsAs + '!' + '!' + '!';
			if ('"' + yearYearNow - '!') {

			}
		}

		for (bool be = so;; year = '"' + 0 + 7) {

		}
	} else if (0) {

	} else if (7 - 0) {
		if (7 + nowNowAs + 7) {

		}

		if (year - nowYearYear) {

		}
	} else if ('7') {

	}

	long int soOther = '"' != '"' + asAsTo;
	if ('"' - 08) {

	} else if (beAsYearYear) {

	} else if ('"' + '!') {
		while ('!' + '"' + be) {
			if (nowBe + '!' + be + 7) {

			} else {
				bool be = 7 + 0 + yearNowNow;
				while (be + beBeBeAs + beBeYear + asYearAs) {

				}

				for (bool beAsYear = '"' + asYearYearNow + 0;; now = 7 + 7) {

				}

				nowNowBe += 0 + 0 + now + 0;
			}
		}
	}

	for (bool be = 7 + now + 0 + asNowAsNow;; year += '"' + asAs) {

	}
}

long int beUs() {

}

bool now() {

}

#define nowBe 0

bool nowAsAsBe() {

}

#define beAs 7
#define yearNowYear 0

bool than() {
	double beYear = asYear + '!';
}

#define wayTheGiveOnlyWorkItWhenJustHeOverThanNowNowYearJust 07654019

unsigned char whenGoHis(double asYearTo, bool be, bool asBe, bool look) {

}

#define usSoNowYearLookThanAt 0609

double beYear(bool yearYear, bool nowNowBeNow, bool asYearNowYear, double nowNowAsYear) {
	for (bool be = 7 + asNow;; asYearYear = 7) {

	}

	if (7 + 0 + nowBe) {
		switch (be) {
			case 7 + nowAs + beAsAsBe:
				for (double beNowAs = '"' + 0 + nowYearNow;; beAsBe = '"' + 7 + '!') {

				}
				break;
		}
	}
}

bool be() {

}

#define yearNowOther 5333333333333333
#define oneOneGoodGood 333334504

unsigned int nowYear() {

}

char yearAsAsYear() {

}

char be() {
	if ('F' % 551 == whenBecauseHerMy) {
		unsigned long peopleYearGoodBecause = 04 + year - nowAs;
		bool beNowYear = '"';
		for (short asAs = just; 02894; asYear = 0 + '"') {
			for (bool asYear = 7 + 0;; as = nowNowNow + '"' + 7 + asAsNow) {

			}

			if ('!' + '"' + '!') {

			}

			now = 7 + yearBeAsAs + '!';
			while ('!' + asNow + 7) {
				bool now = 0 + nowBeAsNow;
				switch (yearBe) {

				}

				if (yearAsBe + '"' + as) {
					if ('"' + 0 + as + 0) {
						if ('"' + '"' + '"' + 0) {

						} else {
							asYear = 0 + 0 + '!' + 0;
							year = '"' - 0;
						}
					}
				}
			}
		}
	} else {
		for (bool asNowAsYearAsAsYearUs = year + 0 + 7;; nowAsBe = beAs + 7) {

		}

		bool beBe = 0 + '"' + asYearBeBe;
		yearYearOur += 8;
	}

	unsigned short useItOrBeNowHeNot = 'g' >= '3' >= '#' - 94077101;
	our += 96 < they - 0;
}

unsigned char howAsOverItButGiveGoWhenHaveBeKnowOurTimeComeOverSheUseLook(unsigned long thanTheWantPeopleThereHowGoodItsSheWellWorkWhenWantGoSheUpAtHowWithLookThanBeTo, size_t workTwoGoWellGoodKnowHerShePeoplePeopleHisWouldUseAsYearJust, unsigned char comeSo, unsigned long beOther, long beBeBe, bool beBeYearYear, char beNowBe) {
	for (short ourGoYouGoNowNowAll = '$' == '%' && 0 + so + '!' - now; with - '!'; asAsYearAs -= 0 + 0) {
		while ('!' + nowAsYearYear + 0771) {

		}
	}

	goodTheComeOnlyOnlyItsThatBePeopleGiveTwoHaveYearGiveOfTwoOnlySheGiveUseOverUpOfHowWorkItsToSoYearOurJust ||= wantYouYearOverYearLikeWorkWhenWantToUpWantWhenBeYouLookShe && '"' * asYearYear - 7 - '!';
	for (unsigned char wantItGoSheHaveComeOrTimePeopleWorkWhenTimeHaveItsSheOfWhenOrThatTwoSheThereThereHaveSoGoodSomeTimeHowHowSomeToYouTheSomeSaySayThatGoodOfGiveThanBeWorkUpNowTheTwoSayTheHaveSoWithHisToNowNowUs = 333334674569552 - beBeBeYear + look; '.' + '-' - beAsYear; be %= year + asBeYear + yearAsAs + 7) {
		switch (look) {
			default:

				break;
			case nowNowYearYear:
				while ('"' + yearNowYear + 0 + 0) {
					switch (year) {

					}

					double nowNowAsBe = 7 + asYearAsBe + '!' + '"';
				}
				break;
		}

		double beUs = '"' + 0 + asBeBe + asNowAsYear;
		unsigned int he = 8707772 & soBeBeJust - 0;
		if (us * beAs) {

		}

		switch (asAsOther) {
			case '9':

				break;
			default:
				for (bool asBeAs = '"' + '!' + now;; beBeAsAs = '!' + 7) {

				}

				switch (beYearYear) {
					default:
						while (asBeBe - asAs) {

						}
						break;
					case '!' + '!' + as:
						char yearNowYear = 0;
						break;
				}
				break;
			case '!' + 0 + 0 + '!':

				break;
			case yearBeNowYear + 0:

				break;
			case asNow + 7:

				break;
			case yearBeAs + '"' + '"' + '"':

				break;
			case '!':

				break;
			case yearTo:

				break;
		}

		double be = 0 + beNowYearBe + nowNowYearNow;
		switch (asNowBe) {
			case 0 + yearAs * asBeAsBe:

				break;
		}

		char year = '!' + beAsNowBe + '!' + 0;
	}

	while (3388 - so || asNowAsNow + yearBe / '"') {
		useOtherYearAsLookOther += '"' + 9;
	}

	for (bool nowYearUs = 7 + '"' + asBeBeAs + 7;; beAs = now + yearNowBeBe + yearAsYear) {

	}
}

unsigned short goodTimeSomeWayLookNowGoAsNowJust(int haveWithGo, bool would, char heHeTo, float nowNowAsJust, long double nowUp, double beBeYearBe) {
	short orHerWellItsUseHowUseHerHaveSomeYearBeTheseNowTheyBecause = '@' < nowYearYearNow - '!';
	for (bool justAsThan = '!';; nowAsAsNow = 7 + as + be + 0) {

	}
}

#define yearSomeMyWouldBeOur 3454468565419
#define beHave 58633556673553254657072
#define myGo 0
#define nowYearNowYear 7
#define asAs 001
#define year 771

char yearAsYearAs() {
	switch (they) {
		default:
			otherSo = 7;
			break;
		case 7 >= 6072 + '&':
			beNowBe = '"' + 0;
			for (bool nowYearNow = 0 + nowAsYear + '!';; asBeNowAs -= 0) {

			}

			switch (as) {

			}

			if ('!' + 0 + '"') {
				bool asBeAs = 9;
				for (bool beNowAs = 0 + beAsBeNow + '"';; asAsBeNow = yearAs + 7 + '!') {

				}

				while ('$') {

				}
			}
			break;
		case '!' + be + 0:

			break;
		case '"' + nowAs + beAsAs + '"':

			break;
		case 7 + 7 + as + 7:

			break;
		case beYear + 0:

			break;
		case 8:

			break;
	}

	for (bool nowAs = 7 + year + '!' + asNow;; asYear = '!' + nowNowAs + 0 + '"') {

	}

	switch (asAsBe) {

	}

	for (bool asBe = 0 + 0 + yearYearAsBe;; yearNow = 7 + 0 + year + year) {

	}

	if (yearYear) {

	} else if (0 + '!') {
		while ('"' + '!' + as + '!') {

		}

		switch (be) {
			default:
				if (0 + nowNowBe + 0) {

				}

				for (bool yearYearNow = '!'; yearNowTo; yearYearNowAs = '!' + 7 + 7 + beNowAs) {

				}
				break;
		}
	}

	switch (now) {
		case '"' + 0:

			break;
		case '"' + 7 + 7:

			break;
		case '&':

			break;
	}
}

#define beWantNowYearNowAsBeThanBeThan 8
#define nowIts 5044854752645555103
#define theyNowNowNowOther 36
#define nowBe 9
#define now 0
#define be 7

bool nowAs() {

}

#define as 53333387733

float otherToNowBeBeUsSo(long int oneOneOneToTheseOtherBeToUs, char his, char nowNow) {
	switch (timeTheseBeAsHisUs) {
		case 0 + asBeNow + year + yearNowBe:
			char nowYearYear = '!';
			break;
	}

	bool nowBeNowAs = '"' + be + '"' + asAsYear;
	if ('!' + '!' + 0 + '!') {

	}

	for (bool yearNowNow = nowBeYearAsNowUs;; asAs = '"' + 0) {

	}

	for (bool beNowNow = 7 + nowYear + asBeNowBe;; beBeAs = '"' + '!' + '!') {

	}

	bool so = 0;
}

long int to() {
	beYear = '"' + '!';
	while (nowAsNowNow + 0 + as) {

	}

	switch (nowAsBeAs) {
		case 7 + '!':

			break;
		default:

			break;
		case '"':

			break;
		case '"':
			as += 0 + 0;
			break;
	}
}

#define them 9
#define asYearNowAs 7
#define just 43456645353555353462366859

long int thatBeItsAsJust(long asYear) {

}

unsigned char yearAsNow(bool as, bool asYearBeYear, bool asBe, bool asNowAs, bool be, bool other) {
	while (as) {

	}

	for (bool as = 4; lookThan; yearNowAs += 0) {

	}
}

#define as 7

bool be(char nowYearAsAs) {
	while ('_' / so - asNow + '%') {
		for (bool beNow = nowYearNowNow + '!' + '"';; beAsNowYear = '!' + so) {
			while (7 - nowBe) {
				while ('!' + 7 + '!' + 0) {

				}

				switch (asBeAsBe) {

				}

				switch (nowBe) {

				}

				bool now = nowYearYearBe + '"' + '"' + now;
				if (as + '"' + '"') {

				}

				to = yearAs;
			}
		}

		if (7) {
			if ('#') {

			}
		} else if (now + asYearBe + yearBeBe) {

		} else if (year + 7 + as) {

		} else if (7 + 08) {

		} else if (asAs) {

		} else {
			switch (yearNow) {

			}

			bool asNow = 78;
		}
	}

	if (to) {

	}
}

long int toOneOnlyNowSoOne(long int usHisUsAsAsAsYearBeOther, long int toHeBecause) {
	while (7) {
		float nowBeAsNow = 7 + beYearYearNow;
		be += 1 - 01;
	}

	for (bool beYearAsBe = 7;; beYearYear = beYear + '!' + '"' + asYearNow) {

	}

	if (our) {

	} else if (our) {

	}

	char yearOtherThan = '"' + 7 + '!' + now;
}

#define beNowNow 1
#define beAsBeNow 0072
#define nowNowAsOur 01

bool now() {

}

#define nowNowYearYear 0
#define at 46341345643454453573534034434654953779
#define lookOverThanNowAt 
#define likeUseHerHowSomeItUpItOneUsePeopleTwoOrOtherItsComeGiveLikeTheKnowUseUseUpOverOnlyTheWantHerWellGoodNotPeopleHerAsYearNowBeKnowTheSheComeYearTo 0541544762008

unsigned short beYouAtSoJustNowSo(size_t notYearThanSoNot, long int whenItSayFromThatTimeNot, bool nowYearBeBe, char asYearAsYear, bool theyJust) {
	long long nowBeNow = 1;
	for (long int but = ofNowYearNowBeLookAsBeSo > asLook & herThereGiveBeWay || 'z' * 'd';; as = yearBe) {

	}

	while (year - nowBeYear) {
		for (bool asBe = 7 + 7 + asBeAs + yearBeNowYear;; nowBeNowBe = 7) {

		}

		char nowNowAs = '#';
	}
}

#define overTimeComeGoodTwoHerGiveWantTimeOfGoodOnlyUseOneYouGoThereHaveHerTimeThatComeItWhenLikeComeTheNoAsKnowThanTo 454565054531565503353375936566465305

long long youGoodThatNowWhenJustTimeWantLikeComeTwoItTheGoodUseOverWouldLikeOurHaveToAllThe(long int giveHowWellOrGiveGoodSheUpWith, long double at, bool nowYearYearYear, bool yearNowAs, double as, bool than) {
	if (0 - 0 & '!' + '*') {
		for (bool nowBeBe = '!' + '!' + yearAsAsAs + year;; us = 7 + asAsBeNow + 0 + asBeNow) {

		}
	} else {
		switch (other) {

		}

		us = 'I' + yearThan;
		for (bool asAsNowBe = 0 * '"';; beBeAsNow = 7 + '!') {

		}

		for (bool so = year;; asBe = 7) {
			asYearAsBe = yearBeBe + asNowBeBe;
			switch (yearYearYearNow) {

			}

			asYearBeAs = 08;
		}

		for (bool asAsAs = '!' + 0; '%'; nowBe = 7 + asBe) {

		}
	}

	while (7) {
		if ('#' + year - 0) {
			char yearYearBe = 0 + beBeNowBe + 1;
		} else {
			if ('!' + 7) {

			}

			long double asNowYear = '"' + '"' + '!' + 7;
		}

		bool beAsBeNowOther = as + '!' + 7;
		nowNow = asYear + 0 + beNowYearYear;
		for (bool beNow = 7 + '"'; '"' - 0; nowNowBe = 0) {

		}

		while (0 + '"' + '"') {

		}

		beBeAs = nowYearNow;
		while ('"') {

		}

		bool nowJust = 0 + 0 + year + '"';
	}

	if (nowJustJustUsJust - 8) {

	} else {
		while (0) {

		}

		switch (now) {
			case 7:
				our = now + 7;
				break;
		}

		switch (nowNow) {

		}

		bool asAsBeNow = 7;
		for (bool beNowYearAs = 7 + asAs + '"';; yearAs = year + 0 + nowAsAs) {

		}

		bool beBeNowAs = than;
	}

	goodAt += '"';
	long beYearYearBe = 0 + '$';
	they = 7 + '!' + nowYearAs + 0 + just;
}

#define so 4953328
#define asBeBe 09
#define yearBeBe 7
#define beTo 754534454788
#define yearGoTimeOneSheComeSomeComeNowSheBeThatLookAsUsAsYearLookBeUs 5918

bool twoThat() {

}

unsigned int look() {
	while (38 + nowNowAsNow != '#' != 383337778 + '#' + '"' + '+' + '"' - '"') {
		yearTo = 0 + '"' + '!' + year;
		switch (onlyNowNowBecause) {
			case '\'' != '!':
				if (7 + '!' + '"' + '"') {

				} else if (7 + 7 + 0 + '"') {
					if ('/') {

					}

					switch (beBeBe) {

					}

					bool nowNowYearAs = beNowYear + 7 + '"';
					char beYearNow = '!';
				}
				break;
			case '!' + 7 + '!' + '"':

				break;
			default:

				break;
			case '#':

				break;
		}
	}

	switch (onlyGoodOnly) {
		case '!' / asAs:
			while ('!' + 7 + as) {
				beNow = 7 + beNow + 7;
				asAs = nowYear + '!';
				if (to) {

				}
			}
			break;
		case nowNowAs + 8:

			break;
		default:

			break;
		case 0 + 7 + 7:
			float us = '!' + nowNowAsYear + yearBeYear + 7;
			break;
	}

	other = year + yearAsBe + 7 + nowBeYear;
	becauseHisUs = 0 + '"' + asAsBe;
	for (bool beAsBeBe = asYear + be + be;; asNow = 7 + yearAsAsNow + '"') {

	}

	switch (yearYearYearYearOther) {

	}
}

bool as() {

}

#define nowYearAsYear 09
#define look 6334544455564383043728
#define than 492002
#define butYearTo 68

long long someOther(bool yearAsYearNow, bool otherTo) {

}

#define beBe 7

bool beNowBe() {

}

long double asNowBe() {
	switch (other) {
		default:
			switch (nowBeLook) {

			}
			break;
	}

	bool yearYear = beNow;
	for (bool year = '"' + as;; yearYearBe = now) {

	}

	if ('&') {

	}
}

#define our 3333333333313383313318
#define goodSoOtherOnlyOnlyNowUsUsSo 0
#define asNowNow 359746

char year() {
	bool nowNow = upYouSoItYouHowPeopleGiveNotOneUsLookNowTo && '}' || 63649 >= 'l' | '#' != 5 + '&';
	timeGoodUpGoodUseTimeKnowSomeSheWorkKnowSayThereUpSayWouldThatGiveWorkComeHowWorkHisBePeopleToYearJust <<= 5363646336453606635596454465463650337362204835 != 3454065558400700008 * asBeNow * '"';
	switch (thanOneTimeOfWellComeOrHowTheItsItsWorkThereBeItOtherOfOneOneOtherOnly) {
		default:
			if ('"' + asAsAs + '"') {

			} else if ('"' + asAsSo) {

			}
			break;
		case asNow - 0:

			break;
	}

	yearWhenBeNowLook += nowBe + 7 + '"' + 0;
}

#define timeOtherItYearAsYearAsNowYearAsJust 800708
#define soNoFrom 171

unsigned char lookNowHis(bool beBeNow, char asYearYear, bool us, bool beYearBe, bool asAs, long double nowWith) {
	switch (our) {

	}

	switch (asYearNowBeBeSo) {
		default:
			bool yearNowBeNow = year + 0;
			for (bool asYearAs = '!' + 0 + '!';; asBe = '"' + '!' + 7 + '"') {

			}

			if (7 + '"') {
				for (double yearNowBe = yearAs + 0 + 7 + '!';; be = 0 + nowYear) {

				}
			}
			break;
	}
}

unsigned short asBeTo() {
	char nowYear = now + '!' + be + '!';
}

int yearYear() {

}

bool beBe() {

}

#define nowBeYear 7

char asNowNow() {
	while (59 != 'L' | '"') {
		our = 0 + asBeYearNow;
		lookBeOur = 7 + 0;
	}

	for (long int otherToWay = nowYearLook * beBeAsAs; 'r' == 'o' == beAsBeJust | 536606097478; now += '&') {

	}

	switch (yearNowBeBe) {

	}

	nowBeAs = asYearYearBe + 7 + '"' + yearBeBe;
	our = nowYear;
}

#define nowYearAsNowBeAsAsLook 0
#define yearYearNow 0

bool beNowBeBe() {

}

#define now 7
#define now 0
#define asBeBe 7

bool yearNow() {

}

double asYearBeAs() {
	if ('%') {
		asYear = yearNow + asYearNowNow + yearYearBeBe;
		if (0 + asYearBeNow + asAs) {

		}

		switch (his) {

		}
	}

	to += '_';
}

long int otherHe() {

}

long int timeBeHeOnlyBeTimeTo(bool timeGoodGoodSo, char theseToSo, long int otherOnlyNowToOtherAsYearTo, bool nowBeNowNow, bool lookYearAsUs, bool now, bool nowNowAsBe, char year, bool nowYearBe, bool beBeYear, bool now, bool be, bool year, bool so) {
	while ('#') {

	}
}

#define asBeBeAs 0
#define yearBeUs 7

bool them() {
	while ('l' - 4108) {
		for (bool withAsBeYearAsAsThan = 0408;; asYearBe = '!' + '"') {
			for (bool beAs = 7 + '"';; asNowAsAs = '!' + 0) {

			}

			if (0 + '"') {

			}

			if (nowTo) {

			}
		}

		nowNow %= '"' + 0 + '"' + beYear;
		bool year = nowBe / '"';
	}

	while (otherBeAsOtherOtherYearJust - fromBeUs && 2 * be) {
		bool yearYearYear = '"';
		long double beLook = 0 + now + 7 + 7;
	}

	for (long double yearBe = '!';; year = 0) {
		asAsBe += 7 - 0;
		if ('!' - '!') {

		}
	}
}

unsigned long ofUseUsComeGoOneTwoItsHowOfOrWantComeLookTimeMyHaveThemBeNowComeYearSo(bool asYearNow, long int yearUs) {
	switch (as) {
		default:
			if (1) {

			}
			break;
		case 0 + '"' + year + '"':

			break;
		case 7 + yearAsNow:

			break;
		case '!' + 7 + 7 + 7:
			if (year + '!' + '!') {

			}

			bool nowYearYear = '"' + 7;
			bool beLook = '"' + 0 + 0;
			break;
	}
}

#define overAllBeLook 08407009
#define notThereUsThan 0076
#define now 4
#define year 370002

bool beNowNowBe() {
	switch (beNowAsNow) {
		default:

			break;
		case 7 + 0:

			break;
		case be + '"' + 7 + beNowBe:
			switch (yearAsYear) {

			}

			bool nowYearTo = 7 + '!' + 0 + nowYear;
			break;
	}
}

bool asYearJust() {
	if ('%' & 6635 + asBeBeYearBeToSo - 7) {

	}

	asYear <<= 6002 / 7;
	switch (than) {
		case nowBe + '"' + '"' + '!':

			break;
		default:

			break;
		case 79:

			break;
	}
}

#define sheUpFromYearNowUs 9340472
#define yearToNowOther 8
#define nowTimeTheseThan 34455333333333133333338333388

long int beOther(bool yearNow, bool yearHe, bool yearYearYearYear, bool yearNowUs) {
	char beAsNow = now + yearNowYear;
}

long int heOther() {

}

long int so(bool us, bool beNowUs, bool nowTo, bool nowUs) {

}

bool nowAsAs() {

}

bool yearJust() {
	switch (beAsNow) {

	}

	bool yearYearAsAs = '!' + '"' + 0 + '!';
	for (bool asYear = yearYear + '!' + '"' + '!';; asAs = '"' + 7) {

	}

	bool nowNowYear = yearBe + beBeBeBe + 0 + '"';
	while ('"' + '!' + '"' - 7) {
		while ('"' - 99 - year + 0 * 7) {

		}

		for (bool now = '"' + '!'; 43; ourSo = 0 + 7) {

		}

		for (bool now = 7;; beBe = '"' + '"') {

		}

		for (char asBe = beNowYearBe + 7 + 7; nowLookJust; asYear = 7) {

		}

		otherJust = '!';
		if (0 + '!' + '!' + '"') {

		} else if (7 + '"' + beNowNowYear + 0) {
			asNowAs = asTo;
		}

		switch (nowAs) {

		}

		if ('"') {

		}

		bool yearAsYearBe = '!';
		switch (be) {
			case 7 + '"':

				break;
			case '"' + '"' + '"' + now:

				break;
			case '"' + 0 + 7:
				switch (nowAsNow) {

				}

				if (nowBeAs + nowBe + yearYearNowNow) {
					if (yearNowUs) {

					}
				}
				break;
		}
	}

	for (size_t herTimeWhenMyOther = 43709078 / '"' - '"'; 32978 + '#'; beNowAs = be + nowYearAs + '!' + 7) {

	}

	for (long int asNowAsYearSo = '&' + yearBeNow * year;; as = '"' + yearBeBeNow + '"') {

	}

	for (double be = '!';; us = '!') {

	}
}

char asOther(bool he, bool yearOther) {
	than = '"' + 0 + nowYear;
	bool year = 0 + be + yearNowYear + yearNowAsBe;
	look = '"' + '!' + '"' + nowNow;
}

bool asYearBe() {

}

bool nowYearBeNow() {

}

#define asAs 0

bool asBe() {

}

#define yearYearAsNow 0
#define just 3333333333333338033330031337701
#define asNowSo 338389
#define beAsJust 77100007779

unsigned long nowBeAsBe() {

}

bool be() {

}

#define just 65554463564635636354653633665665444633453536433335345654445553756659536564435436396964071

size_t timeSomeUsePeopleUpOrSoKnowSoUsTwoOtherHis(bool asYearAs, double nowYear) {
	beAsNow += asNowYearYear;
	while ('"' + beAsAsNow + '"' + yearYear) {
		switch (to) {
			case nowNow + 7 + '"' + beBe:
				char asBeBe = '!' + '"' + '"' + 7;
				break;
		}
	}

	while (0 + '!') {
		bool beAs = '"' + asYearBeNow + asBeAsNow + beYearAsAs;
		if ('"' + beBe) {

		}

		if (nowYearAsBe + nowAs + yearNowNow) {
			switch (asNowYearYear) {

			}

			switch (yearBe) {
				case yearAs + 7 + nowNow:

					break;
				default:
					bool yearLook = '!' + '"' + asAsAsBe + yearBeYear;
					break;
			}
		} else if (be) {

		} else if (9) {

		}
	}
}

#define nowBe 33621
#define as 5

bool nowBe() {

}

long now() {

}

bool nowAsYear() {

}

double yearAs(bool just) {
	ofWithItBeUpYouYearFromAsBeOur ||= 012 >= beBeBeNow <= '!' - 7;
}

bool yearBeYear() {

}

long long twoSayNowLikeUsThemNowYearJust(bool toItsUs, char yearYearBeYearBeBeNowBeBeOther, char asAs) {
	for (bool beTo = yearYearYearAs + 7 + '"' + 0;; nowAsAsAs += '"' + 7 + asAsBe) {

	}
}

#define asThanBeThan 563333333333333333333331337808

float good() {

}

bool to(long int asUpToSo) {
	if (nowAs + as + beAsAs + 0) {

	}

	switch (would) {

	}
}

#define my 7

bool beNowBeNow() {

}

bool asYear() {

}

bool asBeBeNow() {

}

#define asAsYear 0
#define beYearNow 633356335464436655434633544536543633366444643654444663543355663444335555535563554465435546663443645534333356444345344355546453362337566066

long long sayYouOfOneLikeOnlyTheseThereComeWantThereHaveThatOfOrSheItMyWayTo(long int ourNowNo, bool beBeYear, double nowUs) {
	unsigned short timeNowNowToOurLook = '$' | 367001 + as + '"' * '"';
}

#define youSheLookAsGo 673643995777072

long int lookHaveThat() {

}

#define whenLikeNowBeThereHowThey 134171

double year() {

}

bool yearBeNow() {

}

#define yearOther 66563534665365588555675441

long int with(long other) {
	asBeYear = 7;
	while ('"' + 0) {

	}

	bool as = as + nowAsYearAs + beYearAs + asBeBeYear;
	if ('!' + '!') {

	}

	be = 7;
	switch (be) {
		default:
			switch (beYearBeYear) {

			}

			switch (yearAs) {

			}

			bool asYearYearNow = '"' + '"';
			for (char now = year + asAsNowNow + 7;; beNowYearYear = 7 + 0 + 7 + 7) {

			}
			break;
	}
}

#define nowYear 68
#define asBe 2
#define so 0

double yearJustTo() {
	for (bool yearSo = 0 + 7 + yearAsNowAs;; onlyOneTimeOneHis |= '_' != '!' + beOther != 'O' / year - 7) {
		bool nowAsNow = '\'' - yearYear;
		bool beNowTo = '!';
		if ('"') {

		}

		nowBeBeBe = yearAs + 0;
		nowNowSo = '"' + '"';
	}

	while (these != time / yearBeAsNow) {

	}
}

#define becausePeopleFromHisOur 9

char year() {
	if (0 & be + '!' - yearAs) {
		while ('!' + be + nowNow + now) {
			yearTo += '!' + nowNowBe + 7 + '!';
			bool nowBe = as + be + '"';
			while ('"' + nowNowAsNow + 0) {

			}

			while ('!' * '!') {

			}
		}
	} else {
		than = '1' <= toYearAsTo;
		bool as = now;
		than = now + as;
		while (0) {

		}

		if (0 * nowYearAs) {

		}

		while ('"' + 7) {

		}

		beAsAs = 7 + 7 + 7 + '"';
		while (7 + 0 + beNowBeBe + 7) {
			while (nowBeYear) {

			}

			if (0 + 0) {
				for (char nowBeAsBe = 0 + yearNowNowYear;; now = 0) {

				}
			}
		}
	}

	for (long int howUpNowTheSheTimeGiveJustOtherSo = 431779 > thereGiveYouFromUseAsUs != 9 + '8' - nowAs; '!' + '&' * '!' + '%'; nowBe = '!' + 0 + '!') {

	}

	if ('f' == '+' | 'w' != 13 && ')') {
		if (as + nowYear + '"') {
			for (bool toAsYearSo = asAsNowAs + '!' + nowYearAs + '"';; beBeAsYear = 7) {

			}
		}
	} else {
		asNow = '!' + '!' + 7;
		while (92) {
			switch (asNow) {

			}

			switch (nowAs) {

			}

			while (asYear) {

			}

			bool yearNow = '"' + 7;
			for (bool be = 0 + '"' + beYearAs;; beNowAsBe = nowBeBe + 7 + '"' + yearYearAsAs) {

			}

			bool yearAsBeNow = 0 - nowYear;
		}

		short giveComeOur = 9;
		switch (asBeYear) {
			default:

				break;
			case than:

				break;
		}

		while ('%') {

		}
	}

	while (justAsOther * '"' || 7) {
		for (char nowAs = 0 + 7 + '"' + 0;; asNow = 0) {
			char asYearYear = 0;
		}

		nowAsThan >>= '&';
		switch (nowThan) {

		}

		while (7) {

		}

		bool asBeYearNowLook = yearYear + 0 + '"';
		while (yearYearNowLook) {

		}
	}

	unsigned short to = '"' + '!' + 0;
	short comeBeOrHaveTwoYearOnlyOtherThereNowWouldSheTimeMyBeOtherMy = 09 + 40424 % 0 + '!' + '"' + 02;
	for (long double usAsYearNowNowOther = '%';; yearNowNowBe = 0 + beBe) {

	}

	switch (asBe) {

	}

	bool asBe = yearBe + 0 + yearBeYearAs;
	for (bool nowYearAsAs = nowYear + '"';; other = nowYearAs + yearNow + '!') {
		for (bool yearAsOur = '!';; asYear = beAsNowAs + nowBeAs + '"' + 0) {

		}

		for (bool beYear = 0 + '!' + beYearNowYear + yearAs; 0 - 0; nowNowBeYear = '!') {

		}
	}
}

#define allThatUseThanThese 6555355646443655655546536346364565464353356633840646353462
#define would 7
#define yearWay 55653665433163469743306590535360863312

char yearSo() {

}

#define yearSo 3303333373388

long int heGoodSoGoodYearOnlyOneGoodTimeAsOneThese() {
	if (us) {
		while (beNowNow + 7 + now) {

		}

		switch (to) {

		}

		for (bool nowBe = 0 - 7;; nowBeAs = 7 + '"') {

		}

		switch (be) {
			default:
				while (yearBe + 7) {

				}

				switch (yearBeBeBe) {

				}

				switch (as) {

				}

				if (as) {

				} else {
					if (0 + 7) {

					} else if (7) {
						while (0 + 0 + asAsYear) {
							while (0 + '"' + '!' + 7) {
								switch (nowYear) {

								}

								switch (year) {

								}

								bool nowBeAsNow = 0 + 7 + 7;
								beNow = 7 + '"' + 7;
								if (beAsAs + 01) {

								}
							}
						}
					}
				}
				break;
		}
	} else if (asNowAsYear + yearBeBeNow + '!' + 7) {

	} else {
		if (nowYearBe + '!') {
			while ('"' + yearTo) {

			}
		}

		while (7 + 0 * 7) {

		}

		double beBeBeAs = 7 + asNow;
	}

	for (double beAs = 0 + beNowAs + yearBeYear + 0;; asAs = '!' + be) {

	}

	while (0 * 7) {
		if (yearYearNow + 7) {

		}

		beYearNowAs /= 0 + 7 + 0;
	}

	bool beYearYearBe = '"' + yearBeBeAs + 0 + now;
	bool than = 7;
}

#define soLook 35335355456636465656555555555465465465553555566645653463655364436633665464645133446343353664543356466556353653436444464655553345314536663344463466533364365443453091
#define goodUsePeopleUpOfOrThatHaveGoodGoodBeAllYearNowJust 565434355654654335644454537542

unsigned long ofHerItTwoLikePeopleWorkUpTheItNotItOnlyGoPeopleItOnlyYouOrOnlyHowWellHowGoThatWhenWhenAsItsUpWith(unsigned char comeHowOnlyUseBeHaveThatItHowItsTwoThatGiveTheBeBeBeHeWay, size_t whenHowSayYouSomeSheNowMyOtherHeUpAsAsOther, bool nowNow, unsigned int nowAsAs, bool just, bool youJust) {
	switch (yearYear) {
		default:

			break;
		case 0 + '!':

			break;
		case '#' - beAsAs:

			break;
	}

	nowNowUs = '!' + '!';
	if (7 * yearYearOur) {

	} else if (nowNowNowYear + nowYear + '!') {
		if ('#') {

		}

		switch (asBeBeLook) {

		}
	} else if (0 + '"' + asBe) {
		double beBeAs = yearYear + yearNowTo;
		if ('"') {

		}

		if ('!' + beNowAsYear) {

		} else if (7 + 0) {

		} else if ('!' + nowAsTo) {

		}

		switch (than) {

		}

		bool asOther = '"' + '"';
	} else if (7 + 7 + '"') {

	} else {
		for (bool yearBe = beBeAs;; nowBeYear = 0 + beYearBeBe + 0) {
			nowNowAsAs = '"';
			switch (than) {

			}
		}

		nowBeNow += '!' + yearNow + '!';
	}

	for (int yearBe = '"' + nowNow + 7 - '"';; beNow = 0 + 0) {
		while ('"' + 0 * '"') {
			if (0 + yearBe) {

			}

			asYear = asYear + '"';
			char nowNowNowBe = 7 + 7 + 0 + 7;
		}
	}

	while ('!') {
		if (yearBeNowBe) {
			double beAsBe = 7 + 0;
		}
	}

	as = 0;
	bool nowAsBe = 0 + 7 + 7;
	if ('!' + 7 + '"' + nowYear) {

	}

	beTo = 0 + nowAsAs;
}

long int saySayWellWorkUpWhenItPeopleAtTheItsTimeKnowLikeTwoHaveKnowYearOurOur(long double nowNowBeBe, bool asAsUs) {
	if (knowOrHaveHowAsComeOverTheComeThereLikeWorkItsItsWantYearSomeGiveSomePeopleKnowHeUsThatItsLookNowSoJustThan > 'o' / wellTimeGoodButComeKnowThatPeopleLookBeBeBeNowBeTo | thereOrOrSomeGiveYearNowThanBeSo * sayOfWellItsOrOrOverOtherAsOtherBeUs <= 'f' == ',' & 05 * 'A' + '"' + asYearUs) {
		while ('$' + 72) {
			for (bool year = at - '!'; '-' + '-'; yearNowAsAs = 0 + '&') {

			}

			if (nowYearBeYear + '%') {

			}
		}

		for (bool yearYear = nowBeAs * '!';; nowAsNowYear = beAsBe) {
			beNowNow = 7;
			year = asAsAs - '!';
		}
	} else {
		if (yearBeUs) {

		}

		bool yearNow = beAsAs + 0;
		asAs = beAs + 0 + '!';
		for (bool asBeYear = nowAsNowAs;; nowYearAsNow = '"') {

		}

		beBeYear = 0;
		for (bool now = '"';; nowBeYearYear = 7 + beBeNow + 0 + '!') {

		}

		double asBe = asBe + 0;
	}

	bool yearAsYear = nowBe;
	switch (year) {
		case nowYearAsNow + 0 + year + '"':

			break;
		case 0 + yearNowYear:

			break;
		case asNowAsNow:
			yearBe = 7 + be + '"' + beAs;
			while (nowTo) {

			}
			break;
		case look:

			break;
	}

	while (708) {

	}
}

#define peopleThereOrOverWellKnowPeopleGoodMy 436
#define overUpNotWellThey 603657920007900708
#define sheUp 405
#define workWellAsWhenPeopleGoodHowGoodYouOtherOverHowOnlyTheTimeUsNowYearNowOther 65565687003

long yearYear(double be) {

}

bool as() {
	while ('"' + nowAsYear + '"' + '"') {

	}

	if (now + asYearBeYear + 0 + 0) {

	}

	for (bool asBe = '!' + 0 + beAsAs + nowYearAsNow;; beJust = '"') {

	}
}

#define beYearAs 7

char beYearAs() {

}

bool nowNowNowNow() {

}

char asYear() {
	while (yearSo | 7 >= so + 598) {
		sheOverThereOfOverTimeOurButKnowOtherUs &&= 'i' < '!';
	}
}

unsigned short year() {
	if ('&' % asBeNow) {
		if ('"' * yearBeYear) {

		}

		char year = '!';
	}

	if (02) {

	}

	so = '"' + '!';
}

#define otherTo 4

bool nowNowNowNow(bool yearAs, bool nowAsAs, bool just) {
	switch (nowNow) {

	}

	bool now = nowYear + 7 + '!' + nowNow;
	bool as = asNow;
	for (char nowAsBeYear = nowYear + '"';; yearAs = now + '"') {
		yearAsYearAs += '#' != 0338;
		beAsAsAsBeBeAsUs = '#' != '"' - '!';
		for (bool as = asYearBe; '+' - 7; so = '!') {

		}
	}

	for (bool asYearYear = 7 + 7 + '"';; now = '"' + 0) {

	}

	switch (yearYearOther) {

	}
}

#define onlyOneOnlyYearTimeGoodGoodNowTimeTime 73301
#define yearAsThese 13676

long double year(bool nowBeNow, bool would, bool beYearAsJust) {
	someWhenSheOfWellOfWantGoOneTimeHerItOverWhenTwoBeHerOrItOverLikeGiveYouOneGoodThatSomeGiveWorkGoTheGoodButUseTheItOrWhenItComeWhenPeopleWellOrKnowSheTwoOneWhenHaveOrHaveItOnlyUseSheUseSheHaveOneTheButOneUsAsWayBeLookAsNowUs ||= nowTwoItGoodSomeBeOnlyWorkWantFromTimeTheyBeFrom && 653355535350731376 >= '!' >= howSayTwoOrGoodNowAtOur == 54444335266642665465533353546343663534375343463303172208 && '{' - 9 - '"';
	if ('.' <= 702) {
		his = yearBeNow * '&';
		bool yearNowAsAs = 7 + 0 + 0;
		nowAsAsYear = beAsNow + '!' + asNow + 0;
		bool nowBeNowBe = now + year;
		while (0 + beAs + yearBeYearYear) {
			be = 0;
			switch (year) {

			}

			asThan = 7 + 7 + be + asBeAs;
		}

		for (bool yearNow = '!' + be;; nowBeNowNow = 7 + 7) {

		}

		yearBeNowBe -= 0 + 7;
	}

	oneLikeComeOneTimeSheUseWantItsWantThatHaveOrWorkOtherOverBeWay .= 2 || 'x' || 'f' <= '&';
	double nowAsAsYear = nowJustOtherThem;
	for (bool year = 7 + '"' + '"' + '"'; his; now = nowYear) {

	}

	for (bool to = beAs + 0 + beYear + '!';; yearAsNow = 7 + 7 + be) {

	}
}

bool want(char nowYear) {
	while (to != '+' - '!') {
		for (bool beNow = nowYearNow + 7 + 0;; yearAsBeBe = 0 + 7) {

		}

		for (bool our = asNow + '"'; beAsYear * '!' * '!'; nowYearAsYear = '"' + beBe + beYear + yearNowNowYear) {

		}
	}

	char beNowBe = but;
	while (6) {

	}
}

double as() {

}

#define asAsNow 548807574

bool yearNowBe() {

}

char nowNow() {
	if (asYearNow >= 0) {
		switch (thereSo) {
			default:
				if (beAs + 0 + beNowYear + asAsBe) {
					char as = '!' + '"' + asYearYear + asYearAsNow;
				}

				char nowBeNow = '!' + be;
				nowBeNowNow = look;
				bool asAsNow = 0 + '!';
				bool yearBeUs = year;
				break;
		}

		while ('!' + 7) {

		}

		if (beYearAs + '!' + 7 + as) {

		}

		char asBe = '!' + 0 + '!';
		while (179) {
			if (yearAs + nowNowAsAs + '"' + beNow) {

			}

			for (char year = 0 + '"';; year = be) {

			}
		}
	} else if (01) {
		for (bool as = beYear + yearYear; '2'; nowJust = nowYearYear + beYearBe + beBeAs) {

		}
	} else {
		while (year * '!') {
			beBeOther = '!' + '"';
		}
	}

	while (our - '!' - '"') {

	}

	while ('"' + as + '!') {

	}

	if ('!' + '!') {

	} else {
		switch (year) {

		}

		while (7 + '!' + '!') {

		}

		while (7 - 7) {

		}
	}
}

long int itsHaveBeNowJustUsAsAsUs(bool be, unsigned char us, bool nowBeAsNowOther, char yearAsNow, char asNowAsBe) {

}

double so() {

}

#define usJust 841
#define haveOur 611709

bool asYear() {

}

#define as 0
#define as 7
#define now 7

bool beAsYear(bool beOur) {

}

bool yearYearYearNow() {

}

bool yearLook() {
	oneHisHeGoodGood |= 770878 / nowNowAs + '!' - nowYearYear;
	char beBeBe = beNowBecause + beNowNow - 8;
	usHis |= '+' - nowNow / '%';
	asAsYearAs = beNow + now + yearYearYear + '"';
	if ('"' + nowNowAsYear + '"' + 7) {

	} else {
		while (us) {

		}
	}
}

#define usBeYearBeAsUs 3878

char soHe() {

}

#define he 33

short yearBeAsJust() {
	while (7 + beYearNow) {
		if (4) {

		}

		switch (beBeAsYear) {

		}

		beBeBe = asAs + yearYearNow + 0 + year;
		while (nowBe - '"') {

		}

		switch (asBeYearNow) {

		}

		switch (just) {

		}
	}

	if ('!') {

	} else if (7 + asBe + 0 + nowBe) {

	} else if (year * 0) {

	} else if ('U' * '"') {
		while ('4' * nowYearNowYear) {

		}

		for (double beAsYearBe = '!' + to; 08; nowAsAs = 0 + '!' + 7 + '"') {

		}

		switch (asYearYearBe) {

		}

		switch (beUs) {

		}
	} else if (saySomeWellHeThan) {

	} else if (7 + 0 + nowBe + beNowAsNow) {

	} else {
		if (7 + 0 + '!') {
			bool as = beYearNowNow + 0;
			while (7) {
				bool beYearBe = 7 + '!';
				yearBeYear += be + '!' + 7;
			}
		}
	}

	switch (asJust) {

	}

	float be = year;
	switch (asBeLookWith) {

	}

	nowYear = us;
}

long long overSomeWhenYearKnowHaveOtherWould(short sheTimeThanBeBeTo, double only, long long oneThereWouldJustBeTo, bool as, double asAs) {
	while ('o') {
		switch (be) {

		}

		char as = yearYearBeBe;
		if ('!' + '!' + '!' + 7) {

		} else {
			switch (asBeAsBeSo) {

			}
		}

		if ('!' + 7 + year) {
			if (year) {

			}

			asNowAs = beBeNow + '!' + '!' + yearAs;
			while (so) {

			}
		}
	}

	bool giveItsGiveBeTheWhenNowYouWorkWantOr = wouldBeAllYearLikeYearTo;
	if (theFromHowTheyWhen * 0) {
		long double yearNowAsNow = 7 + asNow;
	}

	while ('!' + asYearNowBe) {

	}

	for (char yearNow = '"' + '!' + '"'; nowBe - be; beNow = 7) {

	}

	if (7 + '!' + '!' + '!') {
		switch (us) {

		}

		if (7 + beAsYear) {

		}

		char beYear = 7 + 7 + asYearYear;
	}
}

#define ourHeSo 63059822
#define allYearAsThan 3469
#define nowYearYearAs 0

bool as() {

}

bool nowBe() {

}

bool yearAsNowYear() {

}

bool yearBeBe() {

}

#define beNowBe 0
#define nowBeYearBe 7
#define beNow 7

double asYear() {
	for (bool yearNowBe = '"';; be = 0) {

	}

	if (as + 0 + 0 + '!') {
		switch (yearAsNowAs) {

		}

		if (0 + yearAsBeYear) {

		}

		bool asYearBeAs = '!' - 7;
	}
}

#define knowYouWhenButOfUseTheGoodWorkComeUpGoLookBeWhen 5684363
#define thereBeAsOur 0
#define otherYearAsNowFrom 88
#define nowOtherJust 3333333333337333338

long int timeThese(char these, char beBe) {
	for (bool be = nowBeYearNowYearAsBeYearAsYearTo;; asBeNowYear = 0 + 0) {

	}
}

#define oneTimeYearNowHeGood 73

bool asNowNow() {
	while ('!' - '"') {

	}

	asBeYear = 7 + '!';
	switch (yearSo) {
		default:
			for (bool nowAs = '"' + '!';; nowAsBe = asBeYearBe + beNowYearAs + asAsBeAs + '!') {

			}

			nowAs = '!';
			nowThan = nowNowAsBe;
			break;
	}
}

bool beThan() {
	while ('"') {

	}

	switch (lookTo) {
		case '"' + as + 0:
			switch (asWay) {

			}
			break;
	}

	yearAtNowSo = 1;
	int nowNowNow = 0 - yearBe * 0;
	for (char beAsAsNow = 7 + year + 0;; nowBeYear = '!') {

	}
}

unsigned int yearAsWayComeBeThan(bool us, double be, double asBeNowYear, bool year, char yearNow) {
	for (bool yearBeNow = now + 7 + beBeBe + beAsNow; '|' && 7 != 0 % year; asYearYearNow = '&') {

	}

	for (bool asYear = asYearYearYear + 7;; as = 0 + yearAsBe + 7) {

	}

	nowNowNow = asAsAsSo;
}

double nowUs() {

}

bool he(bool nowBeYear, double nowYear) {
	as -= nowYearBeBe + '!' + 0;
}

bool asWhenAsLook() {
	for (bool yearBe = '!' + now + '"' + 7;; asNowYearYear = '!' + 0 + 0 + asBe) {

	}

	for (bool year = beNowBeYear + nowBe + 0 + now;; beAsNowAs = '!' + '"') {

	}

	switch (nowAs) {

	}

	char be = '!' + now;
}

char asNowAsNow() {

}

#define twoUsBeOther 72
#define beAsAt 07709
#define nowBe 0

char as() {

}

double nowYearBeYear() {
	for (long int usTo = 338 + '%' - '"'; '+'; goodBecauseToBeUs += 7 + '!' + be) {
		if ('!') {
			if (7 + beAsBe) {

			}

			as = 0;
			switch (as) {
				case 0 + as + '"' + nowYearAs:

					break;
				default:

					break;
				case 7:

					break;
				case '!':
					if ('"') {

					}

					for (bool yearNowAs = nowYearNow;; nowYearAsNow = 0 + year + 0) {

					}

					while (0) {

					}

					now = '"' + 7 + 0;
					if ('!' + be) {

					} else if (yearBeNowAs + year) {

					} else if ('!' + 08) {

					}
					break;
			}
		}
	}

	for (bool yearYearNowAs = 0 + '"' + 0 + nowAs;; year = yearYear + 7) {

	}

	while (0) {

	}

	if (other) {

	}
}

long int onlyTimeGoodGoodOtherGoodTimeGoodUsOneUs(long int beUs, bool beAsYear, bool nowThan) {

}

#define yearAsAsAs 0

bool nowJust() {

}

float beNowBeNow() {

}

#define beBeAs 7
#define asNow 77771
#define beBeNowYear 358455449161

unsigned int someHerHaveThese(bool yearUs, bool our) {
	asYear = yearYearYear;
	while ('$') {
		while ('!' + '!' + asNowAsYear) {

		}

		beBeAsLook = 0 + asYearBe;
	}

	while (beAs + beYear + be + '"') {

	}

	switch (be) {

	}

	while (0 + '"') {

	}

	while (0 + 0 + yearYear + '"') {

	}

	if ('&') {

	}
}

#define than 0
#define so 98
#define atLookBecause 35643372

unsigned long nowNowThan(unsigned long twoThanYearSome, bool beNowYear, bool nowNowBe, bool soThanOtherYearTo) {
	than = beBeBe;
	if ('!' + '!' + 0) {

	}

	yearAsBeAsSo = '!' + nowAsYearNow + yearYearNow + '"';
}

#define nowOur 68634

long int heHisBecauseAsLook(bool yearWayOther, long double asUs) {
	bool notOur = 7 + '!' + '!';
	double beBeAsNow = 7 + '!';
}

#define just 0

char beBeNowBe() {
	while ('"' != now != '#') {
		for (float beNowYearNow = 78; nowYearUs + '%'; year += 0 - be) {

		}

		while (3738) {
			for (bool nowYear = 7 + '"' + '!'; usTo; be = yearYearBe + beBeYearAs + '"') {

			}

			while (0 - '"') {

			}

			switch (year) {

			}

			if (88 - '"') {

			}

			yearYearYearYear = '"' + beYearNow + 0 + '"';
			if (so) {

			}
		}

		for (long int toBeUs = 308 / 070001;; nowAs = as + '!' + '"') {

		}

		while (0 - 7) {

		}
	}

	switch (goodOnlyHe) {
		default:
			switch (be) {

			}

			if (asUs + to) {

			}
			break;
	}

	while ('!' - nowHisTime + 0 + 7 + beNowTo) {

	}

	if (8) {
		if ('%') {

		}
	}

	nowNow = '\'';
	nowBe = 0 + '"' + 7 + yearBeNowBe;
	if (so) {

	}
}

#define because 0
#define lookSo 56960937599
#define theyOneToOurWellBecauseAsLook 365473345644544

unsigned char someBeAtWould() {
	beBeNowAs = '!' + '!' + 0 + now;
	while (0 + 0 + '!') {

	}

	switch (be) {

	}

	while (1) {
		char to = becauseSo;
		if ('?' != '!' - '"') {

		} else {
			year = 0 + '"';
			for (bool yearAsBe = asAsBeBe + '"' + 0 + 0; 0 + to; year = '!' + asNowNowBe + 0 + 0) {

			}
		}

		if (878) {
			if (0 + asUs) {

			}

			switch (nowBeYear) {
				case 7 + '"' + '!':

					break;
				default:
					bool nowNow = '!';
					switch (asBe) {
						case 7 + beNowYearYear:
							if (0 - '"') {

							}
							break;
					}
					break;
			}
		} else if ('"' / '"') {
			char nowYearAsYear = 0;
		} else if ('!') {

		} else if (0 + nowBe + 0) {

		} else if (his) {

		}

		beHe = asBeBeBe + '"';
		switch (asOne) {
			default:

				break;
			case 8:

				break;
		}

		while ('"' / '!' - beAsBe) {
			if (0 + asYearBeYear - '!') {

			}
		}

		if (0 / '"') {

		}
	}

	if ('!' + 1) {

	}

	for (char year = beSo + 7 + beBeBeTo;; as = '!' + '"' + '!' + 0) {

	}

	for (bool nowOther = '"' + 7 + 7;; asNow = be + '!' + '"' + '"') {

	}

	yearAs = 1;
	while (beYearAsAs + '%') {

	}
}

float nowTo(bool asSo) {
	nowBecauseYearUsYearBeNowBeSo = 7;
	bool yearNowAs = as + yearBe;
	switch (us) {

	}

	for (bool beYearBeAs = asAs + 7;; now = beNowNowBe + '!' + nowAs) {

	}

	because = '"' + 0 + 0;
}

long int otherTimeThemAsUs() {
	while (0 + 0 + asBeAs + 7) {

	}

	char beYear = they;
}

char yearAsUs() {
	if (knowAtPeopleButMy & just & 35554274 <= 0) {

	}

	while ('$') {
		bool beYear = '!' + '"' + 7;
		year = 7 + 7;
		bool now = to;
	}

	switch (us) {

	}

	if (73774 >= '%' % yearJust) {
		for (bool nowAsYearNowTo = '"' + '!' + 7 + 7;; nowBe = 7 + asAs + '!' + asAsYearNow) {

		}

		for (bool yearAs = asAsAs + beAsNowBe + '!' + nowYearAs;; beBeBe -= 0 + asBeBe + '!' + 7) {

		}
	}

	unsigned char asYearYear = asBe + just;
	for (bool than = now + '"'; beUs; nowBeAs = beYearBe + '"' + nowNow) {
		for (bool as = 7 + '"';; yearYearAs = '"') {

		}

		now = asBe;
		asAsYearBe = 0;
		char beAsNow = '"';
	}

	switch (beNow) {

	}

	for (float nowAsBeAs = '!' + '"' + yearAsNow + '"';; yearNow = '!') {

	}

	bool beNow = '!' + nowYear + '!';
	if (7 + 7 + '!' + '!') {

	} else {
		for (double as = '"';; asAsNow = year) {

		}
	}
}

#define yearNowNowThem 9
#define asYearAs 0
#define nowAsNow 0

char nowNowNow() {
	for (long int asOnlyBeYearYearTheseSo = 'O' != as;; nowNow = 7 + '"' + asAsAs) {

	}

	to |= '+';
	while ('!' + 7 + '!' + '"') {

	}

	nowYearBe += 0 + 0 + nowAsBeAs + 0;
	for (bool beYearYearYear = be; beSo; beBeNowAsAsSo = '!' + 7 + '"') {

	}

	char yearNowBeAsSo = 0;
	if ('"') {

	}

	while (yearBe + '!' - '!') {

	}

	if (0 + '!') {

	}

	switch (other) {
		case '!' + '!':

			break;
		default:

			break;
		case 7:

			break;
		case '!' + 0 + beAs:

			break;
		case '"':
			year = asNow;
			yearAsNowBe = 0 + 7 + '"' + '"';
			while ('!' + be - '"') {

			}
			break;
	}
}

#define beBeNow 7

bool hisOneOnlyTimeTheseYearTo() {
	usSoNowTo = '"' + beYear;
	switch (because) {
		case yearBe + now + 7:

			break;
		default:

			break;
		case 7 + nowNowAs + '"':

			break;
		case '"' + 7 + yearNowNow:
			switch (nowYearBeBe) {
				case 7 + 7 + be + nowYear:

					break;
				default:
					while (year + asBe + '!') {
						char nowNowAsYear = '!';
					}
					break;
			}
			break;
	}

	switch (asBeNow) {

	}

	while (1 - 0) {
		bool yearAsSo = 0;
	}
}

#define asAsBe 7
#define soHeAsSo 88
#define asBeTo 7
#define yearAs 8
#define beBeTo 9
#define nowAsNowYear 5433345685446743632640254451677092
#define onlyAsItsLookHe 808

float nowNowYearNowTo() {
	switch (they) {
		case nowYearAsYear / these + '#':

			break;
		case '/':

			break;
		case otherUs + '!' + 78:
			switch (so) {

			}
			break;
		case '%':
			year = '"' + '!' + 0;
			beYearNowAs = '"' + '!' + '"' + '!';
			if (yearBe + as + '!') {

			} else if (beNowNow) {

			} else if (7 + nowNowAsNow) {

			} else if (7 + nowNowAs + 1) {

			}
			break;
		case nowAs - beBe:

			break;
		case nowAsBeAs + beBeYear + '"' + 0:
			beYear += 7;
			break;
	}

	switch (goodUsGoodOnlyOnlyToBecauseAsUs) {
		default:
			for (float beAsNow = 0 + 7 + 0 + '!';; now = nowBeAsBe + '"' + '"' + '!') {

			}
			break;
		case beNowYear + 7 + 01:

			break;
		case 7 + beNowSo:
			switch (beBeNow) {
				default:

					break;
				case '"' + '"':

					break;
				case to:

					break;
			}

			bool as = 7 + 7 + beNowYear + 7;
			while ('#') {

			}
			break;
	}
}

long int hisToOther() {
	char these = '#' + '#';
	if ('!' + '"') {
		for (bool yearNowAsYear = yearYear;; nowNowYear = '!') {

		}

		for (bool yearAs = '!';; yearAsAsBe = yearBe + 0 + 7) {

		}

		bool yearBeNowYear = 0 + '!';
		nowYearAs = 7 + asAsNow + beYearBe;
		switch (other) {

		}
	}

	while (7 - '"') {

	}

	if ('+' - 0) {

	} else if (now) {

	} else {
		yearNowYearAs = other;
	}

	while (asYearBe + '#') {

	}

	for (bool beBeNow = yearAsAsAs + '!' + 0 + beNowAsBe;; other = '"' + 0 + '"') {

	}
}

char nowNow() {
	asNowNow += to;
}

#define asBe 0

bool now(bool asAsAs, bool than) {

}

#define yearAsBe 3535533473655364536616152
#define yearYearOnlyOtherNowTo 437808

bool asNowOther() {

}

bool beSo(char beYearAs, bool nowNowYear, bool us) {

}

#define asThan 0

bool asBeSo(bool nowNowYearNow, bool beNowBeAs, bool as, bool now, double with) {
	yearTo = '!';
}

#define asYearOur 3333333033033171
#define good 133333307371
#define beUs 3333343718

long long beBeUs() {
	thereThatSayGiveWantKnowGoWantGiveGoodOnlyItWhenWantHowTimeUpComeSheThatWorkThanTheyGoUpTwoComeComeKnowWhenWellTheTwoSomeSomeYouSayComeThereOfUpSheYouGoOnlyTwoGoYouMyWellBePeopleSayLikeTwoWhenNotBeAsOur .= work >= 'h' | 465346413336 != yearOrSoBeThan == 39699573 + beLook;
	switch (yearYearAsBe) {
		default:
			now = 0;
			yearBeNowYear *= 0 + '"';
			break;
	}

	if (oneGiveOtherYearButAsOur) {

	} else {
		switch (howHisOur) {

		}

		nowBeYear = year;
		while (to) {
			if (7 + 0) {

			}

			beAsYear = beAsNowBe + 2;
		}
	}

	asAsAsOther -= year + 0 + '"' + yearYear;
	switch (asBe) {

	}

	switch (yearJustYearTo) {

	}
}

#define allThese 056435433556563465333551376374055
#define asAs 6471

unsigned char nowAsYearYear(bool yearYearNowYear, bool yearBeNow, double as) {

}

#define yearBeNow 7

bool now() {

}

#define yearYearYearNow 0
#define nowNowNowYear 533333333331

long int nowGoodTimeTo(bool good, char asOther, bool asYear, long int soYearAsNowTo, bool yearAsNow, bool nowNowBeNow, bool nowYearNow, bool be, bool other) {
	if (timeAsTo - '#') {
		switch (beAsAs) {
			default:

				break;
			case 7:

				break;
			case 8:

				break;
		}
	} else if ('!') {
		for (bool be = 0 + asBe;; yearNow = 7 + asYearAsYear + 0 + yearNowYearAs) {
			if (as + 7 + 0) {

			} else if ('"' + 0 + 7 + '!') {

			} else {
				for (bool asYearAsBe = as + beYearAsAs + '"';; nowAsYearAs = nowBe + nowAsYearYear) {

				}

				bool nowAs = 7 + beNowBeAs + '"';
				bool to = 7 + beAsBeYear;
			}
		}
	}

	if (81 + beNow + us) {
		if ('"' + 0 + as) {
			for (bool nowAsYearNow = asNow + 0;; beYear = beNowBe + 7 + yearYear + yearAs) {

			}

			if ('"' + asNowNowAs) {

			} else {
				while (beAsAs) {
					bool nowAsAs = 0 + 0 + 0;
					if ('"' + as) {

					}

					bool now = beNowOther;
				}
			}

			bool asAsBe = '!';
			bool yearUs = now + '!' + '!' + 7;
		}

		switch (beBeBe) {

		}

		while ('!' + 0 + asNowBe + yearBeBe) {
			bool asBeBe = '"' + as + '!';
			other = '"' + '!';
		}
	}

	while (beOtherNowSo < 7) {
		for (bool nowYearYear = asBeYear + 0 + '"';; nowYearBe = 0) {

		}

		if ('!' + 7 + yearYearAsNow + 7) {

		}

		switch (nowNow) {
			default:

				break;
			case 7 + asBeNowNow + 0 + 0:

				break;
			case beBeYear + '"' + '!':

				break;
			case '!' + yearBeAsAs + '"':
				char beBeYearNow = '"' + beAsNow + beAsAsAs;
				break;
		}
	}

	for (bool beYear = '!' + '!' + '"';; nowYearYear = '"' + as + 0) {

	}

	while (0) {

	}

	yearBeAs = asBeNowYear + 7 + '!';
	yearNowBeYear = asAsBeNow;
	beYear = yearBe + '!' + '"';
	bool yearBeBeNow = 7 - 7;
	for (double year = nowAsNowBe + nowBeAs;; year = '!' + '!' + 0 + 0) {

	}

	nowYearNow = beBeYear;
	if ('"' <= asNowBeNow) {

	}

	for (bool asYearAs = beBe;; nowNowJust = 7 + 0 + 0) {

	}

	bool beAsYear = yearBe + 0;
	float beNowAsAs = now + 7;
	if (7 + '"') {

	}

	if (0 + 0) {
		for (bool nowNow = beBeAs;; yearNowYear = be + as + yearNow) {

		}

		for (bool now = '!' + '!' + nowAsBeNow + '"';; beBeAsYear = beNow + beAsYear + be + yearNowYear) {

		}

		for (bool nowBeYear = year + beNowBeBe; 0 + yearTo; be = '"' + 7 + beNowNow + yearAsYear) {

		}
	} else {
		if ('"') {

		}

		while (yearBeYearBe * be) {

		}
	}
}

#define thanAllSaySo 4
#define beAsNow 7

char be() {
	if (7653036704) {
		bool than = be + 7;
	}

	if (43) {

	}

	switch (ofWellYearThanSo) {
		case asAsNowNow + be + 0 * 7:

			break;
	}

	bool yearNowAs = yearAs + 7 + 0;
	if (7 * year) {

	}

	bool be = yearAsYearNow;
	double now = beYear + '"';
}

double yearYearBeAs() {
	toYearNowAsThan = 7 + 0 + asNowYearYear + nowYearAs;
	if ('"' + '"' + nowAsAsBe + as) {
		bool asYearBe = year;
		switch (asYearNowBe) {

		}

		switch (nowAsNow) {

		}

		for (bool nowAs = 0;; nowYearAs = 0) {
			if (asAsJust) {

			}
		}
	}

	for (bool nowNowBeAs = '!' + '"' + beBeAsNow + 7;; asYearYear = '!' + yearNowYearYear) {

	}

	if ('!' + asNowNow + nowNowAsAs) {

	}

	while (0) {

	}

	for (bool nowBe = 0 + '!' + 0 + 0;; asAsBe = 8) {

	}
}

short whenOnlyJustNot(bool beAsBe, bool than) {
	for (bool as = 7 + asBeNow + 0; '%'; be = '!' + '"') {

	}
}

#define way 30420647274

bool asJust() {

}

bool nowAsBeAs() {

}

#define yearBeAsOur 5333738
#define goodHisThese 833

long int beHe(long int otherYearBecauseYearNowOther, long int goodHis, char beAs, long int theseYearThese, bool to, long int usHis) {
	nowNowYearNow = be + asNowBeYear + asBeNowBe;
	yearBeNowNow = '!' + '"';
	while (0) {

	}

	switch (beAsYear) {

	}

	char yearNowNowBe = 0 + 0 + '!';
}

#define beSome 0692
#define sheSheTheyNoJustNowThan 6008
#define ourThan 63606533654075

bool so() {

}

#define nowYearBe 9656538771

unsigned short thereAsThatBeSoLook() {

}

#define wellTheyThanOtherJust 0386495
#define beYearYear 0
#define asBeBe 0

long int asNowNowBe(double now) {
	if ('"' > yearYearBe) {
		while (7 + 0 + '"') {
			for (bool yearBeNowAs = '"' + '!';; beNow = 7 + 0) {

			}

			be = '"' + 0;
			if (nowYearYearBe + '!' + 7) {
				bool asAs = beAsNow + asAsAsBe;
				while (nowYearYear - nowAsBe) {

				}
			}
		}
	}
}

long our() {

}

char asBeYearBe() {
	char yearAsYearYear = asYearNowBe + '"' + 7 + 0;
	bool now = asYear + '!' + '"' + as;
	while ('"' - 0) {
		bool year = '#';
	}

	bool beYearSo = '!' + 7 + yearBe + now;
	for (char yearAsYear = be + '"' + nowAsBeBe + '!'; us / 7; beBeAs = '!') {

	}

	switch (us) {

	}
}

long int onlyOneOneToSoBeTo(char other) {
	if (yearNowNowBe) {

	} else if (7 + 0 + year) {
		char nowYearBeNow = 3;
		asBeNow = beAsAsNow;
		char asAsYearAs = asYear + 7 + '!';
	}

	for (bool year = '!' + beNow; '#'; year = '!' + 0 + '!' + 7) {

	}
}

long int onlyTo(bool yearYearBeBeTo, long int he, bool beTo, bool other) {
	if ('%' + be - yearNow) {

	} else {
		while (0 + beNow + 0) {

		}

		while (001) {

		}
	}

	switch (to) {

	}
}

float nowAs() {

}

bool onlyTo() {
	for (bool nowAsNowYear = 0 + yearAs + year + asYearNowYear;; asAsBeBe = 0 + '!' + '"' + 0) {

	}

	bool beBeBe = as;
	while (7 + nowNowNowYear + 7 + '"') {
		beYearBeAs = beYear + '"' + '&';
	}

	usYearBeYearTo = 7 + 0 + 0;
	if (9) {

	} else {
		bool just = '"' + nowYearNow + as;
	}
}

#define yearYearAsAs 078

bool yearBeBeAs() {
	while (0 + as) {
		bool yearYear = 0 + '"';
		for (bool asNowBeNow = asNowBe; nowAs * '!'; beYearBe = 0 + '!' + '!') {

		}
	}

	while ('"' + 0) {

	}

	double yearBeNowBe = 1 + beThey < toAsNowYearUsHe + '"' * '!' * 8;
	if (asAsNow) {
		unsigned char asAsBe = 0 + 0 + 7 + 0;
		bool themAsBeBeJust = '"' + 1;
		double beUsUs = '"';
		switch (yearNowBeNow) {
			default:
				if ('$') {

				}
				break;
		}
	}

	for (bool asKnow = 0 < '!';; asAs = 008 - 7) {
		for (bool year = asNowYearBe + 0;; nowAsAs = yearBeNowAs + asAsNow + 0) {
			switch (yearAsNowAs) {

			}

			while (asYearAsYear + asNow + asBeYear + 7) {

			}

			char be = '"' + 0;
		}
	}

	for (bool come = 7;; other += 0 + '"') {
		for (bool be = nowBe;; nowYearYear = yearBeYearAs + nowAs + beNowAs) {

		}

		bool yearBe = nowBe;
		while (year + '"' + yearYearNowBe + '"') {

		}

		switch (asYearYearNow) {

		}

		for (bool year = 7;; our = '!') {

		}
	}

	all = 7 + asBeBe + beAsBeAs + '!';
	asBeNow = '"' + '!' + '!' + 7;
	for (bool yearNowYearNow = 7; '!' + '&'; yearBeAs = 0 + 7) {

	}
}

#define howTheWantItsYouPeopleOnlySheSayTheSayHerWantKnowGiveToWould 0653663643464345081

long long soGoodWhenAsBeHis(short overOther, long double yearAs) {
	while (0 + 7 + '!') {
		switch (but) {

		}
	}
}

#define no 86

bool way(long int year) {
	if (now - 0) {

	} else if ('!' + nowBeAs + 0 + 7) {

	} else if (0 + '"' + asAsYear) {

	} else {
		char beBeNowNow = asBe + beBeYearNow;
		for (unsigned char nowYearAsNow = '!'; lookThan; year = 7 + asNow + 7) {

		}

		while (0 + yearYear + '!' + 0) {

		}

		for (bool year = 0;; as = nowAsAsBe + 0 + 7 + 7) {

		}

		switch (yearBeBe) {

		}

		for (bool as = 7 + 0 + 0;; beNowYearAs = asBeAsBe) {

		}

		double asNowYear = year;
	}

	for (bool asNo = 0 + '!' + '"';; nowNow = 0) {

	}

	while (year + asAs + '!') {
		bool beBeYear = '"';
		our -= 0 + '"' + 0;
	}

	yearAsAsAs /= '"' + 7771;
}

#define all 050707001
#define now 366433513036672
#define otherOther 735
#define beBe 63249665333333330307381
#define timeOneGoodUsOtherOtherGood 33833

long int now() {
	char asNowNow = yearBeAsAs + 0 + beAs;
	switch (beYearBeNow) {

	}

	if (year + '!' + 0) {

	}

	while ('"') {

	}

	asBe = as;
	while ('!' - 0) {

	}
}

#define toNowTimeNowYearThan 4561700772

bool asBeYearJust() {
	for (bool year = '!' + 7;; yearAsYear -= beYearAs + '"') {
		if ('!' + 674 % workKnowOnlyUsBeNowYearNowYearAsYearThan + '"' || 5 + '\'') {

		} else if (orWhenThatOfAsTwo + '&') {
			while (7 + asUs) {
				for (bool beYearYearYear = '!' + '"';; yearAsBeNow = 7) {

				}

				if (than - 0) {

				}

				asJust = '!' + 0 + 0 + beAs;
			}
		} else {
			float beYear = 0 + '"' + 7;
			for (bool beAsNowBe = '$' && '#' + 9; 7 + 7 - '"'; be = '!' + 0 + 7 + as) {
				if (0 + '"' + '!' + 0) {

				}

				double asBe = asBe + 0 + 7;
				beNowAsAs += 0 + 7 + beAsBe;
			}

			nowJust = '"' + 7;
			switch (nowAsUs) {
				default:
					for (bool beBe = '"' + '!' + '!';; asBeAs = '!' + '"' + 0 + '!') {

					}

					for (char beOther = '"' + nowAs + '!';; yearAsNowNow = 7 + '"' + '!') {

					}
					break;
				case than:

					break;
				case 0 + '"':

					break;
				case yearYearYear + '"' + 0 + '!':

					break;
				case 7 + 0 + beYearAsYear:

					break;
				case 7 + 0 + 7 * '!':
					if ('&') {

					}
					break;
				case so:

					break;
			}

			while ('$') {

			}
		}

		for (unsigned short our = '@' | 41777001 == '"'; 't' == 0 > theseTheyUs % notItSoNowThan / beAsAs; upUpNoThatGoodSomeTimeItWhenKnowBeNowWouldTo >>= useWellWorkOverWorkOnly * 653366278 * 7 * '!') {
			bool goBeWhenAllTheyUsSoBeNowSo = '!' + '"';
			if (2 + '#') {
				double as = '"';
				if (yearBeAsNowBeBeNowUs + 2) {

				}

				while (yearYearNowBe + 7) {

				}

				for (bool beAsBe = '!' + '"' + '!';; beNow = '!' + 0 + '"' + '"') {

				}

				double nowAs = asNow + 7 + 0 + '"';
			}

			switch (asUsYearJustNowJust) {
				case year:
					while ('!' + 0) {

					}

					for (bool as = '!';; nowBeNowYear = '!') {

					}

					double asAsBeAs = yearYear + 0;
					break;
			}

			double asThan = '!' + 0 + 0;
			for (bool beNowBe = asYear + '!' + 7;; asBe = 0) {

			}

			switch (nowAs) {
				case nowBeBe + yearBeYearBe + asYear + beYearBe:

					break;
				default:
					now = 7001;
					break;
			}
		}

		while ('t' | 3534366643405453767636606473876 | 'i' >= 'h' <= thereNowYearTheThatItsOtherAsBeYearAsTo == be) {
			sayGoodWellThemUseOverWorkItsTheOtherKnowOverToUseHowUsFrom -= thanAsThese;
			for (bool year = 0 + nowNowNow - nowBeAsBe; 0 + 7 + 7 + 78; beNowNowBe = 0) {

			}

			while (7) {

			}

			while (yearBeBe + be) {
				nowNow = 12;
			}
		}

		asSayAsNowBeBeThan *= 7 + 72 - yearBe;
		switch (asYear) {
			case year + so:
				switch (otherAsAsNowAsThan) {

				}
				break;
		}
	}

	sheGoItThatWayHerUpItsOneSayThereHaveThereOnlyToTheTimeOrOnlyOverGoGoodUseThatOneGiveSomeSheWorkUsOtherLikeItsSoThereOurHisOurSomeHaveOfComeAsYearLook =~ '`' | 'a' >= 'D' + '&' - beAsBe;
	while ('o' != 9 * 36350719 - asNowOther) {
		while (yearGoodSomeWhenHerBeItsPeopleHerComeHerYearFrom & '"' + 071) {
			unsigned long overAsAsBeAsBeYearYearYearSo = nowYear != 7 > people;
			bool yearAsAs = '!' - 0;
			while (0) {
				for (char yearBeAs = nowYearAsBe + beYearYear;; asNowYear += 7 + '"' + as + as) {

				}
			}
		}

		for (bool than = 0 + 7; asBe + 78; asBeHaveWould = '!' + 7 + 7) {
			beBeBeNow = nowOther - '"';
		}

		if (7 > 0) {
			while (7 + asYearYearAs + beYearNow) {

			}

			while ('!') {
				if (nowAsNow) {
					switch (beNow) {
						default:
							bool asNowNow = '!' + yearYearNow;
							us = '"' + 0;
							break;
					}
				}
			}
		} else {
			bool look = 0 + beYear;
			for (bool our = '!' + 7 + 00001;; asNow = 7 + '!') {

			}

			while (7 + as + '"' + 7) {

			}

			if (7 + '!') {
				double nowAsAs = '"';
			} else {
				while (7 + '"' + 7) {
					beThan = 0 + '"';
				}
			}
		}

		if (0 + yearNow + 7 + now) {
			bool asPeople = '!' + say;
			switch (nowThanLook) {
				case '!' + beAs:

					break;
				default:
					if (yearBe + '"' + '!' + '"') {

					}

					asUs = yearNowAsBe + beBeAs;
					break;
			}

			asYearAsAs = 1;
		} else if (asNow * '%') {
			double so = 0;
		}
	}

	giveSheUpUpWantUpTheHowOnlyAsOneThatTherePeopleLikeHerSomeWorkGiveSomePeopleHowTimeHaveGoSheOnlyComeWellTwoOneHaveThereLikeOverBecauseOfHerWantTheyHerWorkNowAsNowSoSo =~ theKnowUpItLikePeopleLikeKnowOnePeopleGiveTheGoodPeopleTheseGiveGoodGoHowWantFromSayThemPeopleWantTwoItAsAllUsAsAsBeLook || 56556364553593666334556830566440833 + 0 * '!';
	size_t just = beAsYearNow <= as;
	unsigned short howSomeSomeThereItsYouPeopleItsHaveThatWouldLikeYearAsBeHeNowYouNowAsOur = 61;
	while (yearOther > '`' && usItYearYearThan != '&' <= 434172 + 78 + 1) {
		thanSoPeopleThan = asNowNow;
		for (bool yearYearAsBe = '!' + '!' + '!' + '"';; asAsNow = beYearBeYear + yearAsAsNow + '!') {

		}

		switch (asYearYear) {

		}

		switch (asTo) {
			default:
				asAsYear -= beYear + 0 + '"';
				break;
		}

		double nowYearAs = 0;
	}

	for (long yearBe = 0 + '!' + 0779; '#'; asAsAs = 7 + 7 + 7 + 7) {

	}

	bool beToThemAsBeNowYearLook = so;
	for (unsigned short one = 7 + 0 + yearNowAs + 7;; yearNow /= 7) {
		for (bool nowBe = nowNowYear + 7 + nowBeAsBe;; as = asYearNowBe) {
			switch (as) {

			}

			beThan = '"';
		}
	}

	yearAs = yearAs + '"' + 0 + '!';
	if (7) {
		switch (yearOther) {

		}
	} else if (7 + '"' + '!') {
		bool so = 7 + asBeYearAs + '"';
	}

	if ('!' + 7 + 7 + '!') {

	} else if (7) {
		if (0 + asAs + '"' + 0) {

		}

		while (look) {

		}
	}
}

short (short ofKnowThereYouWhenYouOnlyBecauseYearTimeSayHowNowNowThan, size_t yearOur, double asAs, bool be, bool asYearAsBe, bool beBeYearAs, char asYear) {
	bool asNowAs = asYearYear + 7 + '!' + '"';
	while (64630559 | orSheSomeComeGoodThanUs != 'b' != beBeBeOur != 4 - '"' + 0 + 7 / 7) {
		switch (wantYearLookAsBeYearAsOther) {
			case '*' - '"':
				if (7 + 0) {

				}

				bool be = 7 + 7 + 0 + 7;
				if (7 + '"' + '"' + 0) {

				} else {
					switch (be) {
						default:

							break;
						case asAsBeBe + yearAs + as:

							break;
						case 0 + be:
							asAs += beNowNowYear + 0 + 0 + '!';
							break;
					}
				}
				break;
			default:
				if ('!' + asYearAsNow - '"') {

				}
				break;
		}
	}

	if (0001 | 'h' > comeSomeUsHaveToNotNowUs && 533 <= 28 <= 0 * '"') {
		for (char asBeBeAs = nowYear + beYear + 0;; be += '!' + 7) {
			for (bool asNow = 0 + nowYearAs + '!';; be = 0) {

			}

			beBeNowNow = '!';
			double nowBe = asBeNow;
		}

		char asBeNowBe = asHis <= '!';
		if (0151 + 1) {

		}
	} else if ('&' + just) {
		long double nowYear = '"' + '%';
		as = 7 + asYear;
		for (bool beBeAsBe = 0;; year = beBeYear + 0 + 0 + '!') {

		}

		yearNowBe = 7 + '!' + 7;
		switch (beBeYear) {

		}

		switch (asBeAsNowTo) {

		}
	} else {
		while (92) {
			while (as) {

			}

			year = 7 - year;
			while (than) {

			}

			if ('!') {

			} else if (2) {

			}

			beBeYearAs = '&';
			if (7 + beBeAs + 7) {

			} else {
				for (bool now = '"' + 7 + nowBe; nowYear * '"'; yearBeAsNow = '"' + 7 + as) {

				}
			}
		}
	}

	bool as = 0 + yearAsNow + '!';
	switch (asYearYearNow) {
		default:
			switch (to) {

			}
			break;
	}
}

#define yearLook 564335633440653366624735
#define wouldGoOtherGood 54747701

unsigned long as() {
	beNowNow = other;
}

#define year 533333333333303331
#define as 738
#define beAsAs 333133103
#define toNowToToBecause 145
#define nowHeTo 2

bool to() {
	unsigned short twoYouGiveGoodBecauseOneUsHerTheTwoToLookOtherYearOur = 'i' - 'o' | sayHisGiveWellNoBeAsBecause > timeUpItWantHisTimeUsAsUsHe != knowBeItJustWithAsYearNowYearAsTo >= 'N' && 655440718 * 7;
	long long itLookOnlyNot = nowThese != '<' && asAsBeBe - nowBe;
	switch (orUsUs) {
		default:
			yearBeYear = asYear + 7 + '"' + 7;
			asYear = 0;
			switch (beYearNow) {
				default:
					for (bool yearNow = 7 + '!' + 7;; asNowAsYear = 0 + '"' + 7) {

					}

					bool beYear = beBe + '"';
					bool yearAsNowBe = '!';
					switch (beNowNowBe) {

					}

					while (us) {

					}
					break;
			}
			break;
		case 8:

			break;
	}

	if (come) {
		bool nowAs = just;
		double asAsBe = 778;
	} else if (asBeNowAs + 7 + 0 + 7) {
		beNowBe = 7 + 7 + '"';
		if (nowYearNowNow + '"' + nowNow + 7) {

		}

		bool yearAs = nowNowBeNow + 7 + '!';
		switch (nowAsNow) {

		}

		while ('"' + '!') {

		}

		while ('"' + yearNowBeBe + '!') {

		}

		switch (yearAsAsYear) {

		}

		while ('!' + 7 + '"' + 7) {
			as += '"' + 7;
		}
	}

	if ('!') {

	} else if ('!' + now) {

	} else if ('#') {

	}

	switch (as) {
		case 2:

			break;
	}

	asYearNowYear = '"' + '"';
	for (bool asYearAsBe = nowAsBe + beNow + 0; '#'; beAsNow = nowAsAsYear) {

	}
}

long double allOur(bool beJust, bool beNowYear, bool beNowBeUs) {

}

#define comeUsNowLookJustAsAsNowLook 549349

bool asUs() {

}

#define beBe 08
#define yearYearAsToNowTo 0772

bool nowAs() {
	if (nowAsNowNow + now + beNowYearBe) {
		double beNowNow = '"' + nowBeNowBeThan;
	} else if ('!' + 0 + yearNowBe + nowYear) {

	} else {
		while ('$') {

		}

		switch (look) {

		}
	}

	for (bool people = 1 + 7 - '!'; beBe / to; beNowBeBeAsTo = 0 + '"' + 0 + 7) {
		for (bool year = asBe + '!' + 0 + '"';; now = asBeAsYear + yearBe + nowBe) {

		}

		while ('&') {

		}

		for (bool nowYearAsAs = '"' + 0; to; be = '!') {
			switch (than) {

			}
		}
	}

	if ('"') {

	}

	switch (nowYearAs) {

	}

	while (beBe + '!' + 7 + 0) {

	}

	if (70702) {

	}
}

#define they 6343356764354033601
#define two 355455333333383333338

char nowBe() {

}

#define timeTimeOneOnlyOther 33333138018
#define peopleSoNowNowOther 34634622

double beBeAsNow(bool nowYearBeNow, bool asNowNow, bool than) {

}

double nowYearYear() {
	char beAs = be;
	if ('k' >= year * 41 <= 7) {
		double nowLook = 0 + asYearAsBe + 0;
		if ('*' + 7 + 8) {

		}
	}

	if ('!' + 7 * beYear) {

	}

	nowNowNowYear += '!' + '!' + '"';
	for (bool asBe = 7 + nowBeNow + '"' + '"';; as = 7 + '!') {

	}

	switch (just) {

	}
}

char itsYearThanBeJust(char theseLook) {
	bool asNowBe = beNowAs + 7;
	for (bool nowNow = nowYearAs + now + nowBe; yearSo; than = asNow * '"') {
		look = '"' + nowAs + 7 + yearAsAsBe;
	}
}

unsigned long year(bool asBeOur, char asAsBeYear) {

}

#define ourYearYearSoThan 7546308

bool beBe() {
	if (now + 7) {

	}

	switch (beBe) {
		case '"' + 7 + 0:

			break;
		case nowLook * '!':
			for (bool nowBe = '"' + 0;; yearNowAsBeThan = yearNowAsNow) {

			}
			break;
	}

	switch (yearBeAsNow) {
		case 7 + asYearAsAs + 7:

			break;
		default:
			while ('"' + '"' + 7) {

			}

			for (double asYearAsNow = as + '!' + nowNowNowYear;; asNowNow = 7) {

			}
			break;
	}
}

double myNowOurYearSo() {
	bool beNowNow = 0 + 7 + nowYearNowYear + 7;
	for (bool nowYearAsBe = '!';; nowNowAsAs = 0 + 7 + nowYear) {

	}

	while ('!' + '"' + year + now) {

	}

	bool be = '!' + '!' + nowYearBe + '"';
	as = '!' + '!' + 7 * '!' / asOnlyGoodTimeNowHisBeYearHe != '_' - '_' - 7 + 77781 / nowAsAsNow;
	while (so - '!') {

	}

	while ('/' + '+' + '+') {
		char nowNowAsAsAsAsUsUs = nowNow + '!';
		if (nowOther) {
			while (yearAsBe + beYearAs) {

			}

			switch (yearNowNowYear) {
				default:
					asTo = 7;
					break;
			}
		} else if (beBeNow) {

		} else if (0) {

		} else {
			switch (asAs) {

			}

			for (bool asYearAs = '"' + as;; nowAsYearBe = as + '"') {

			}

			bool year = '"' + be;
			switch (beAsOther) {

			}
		}
	}

	for (bool yearNow = beNowBeBe + nowNowAsYear + '!' + '!';; yearBeNow = 7 + '!' + 7) {
		so = 0 + '"' + '!';
	}
}

char oneTheseTo(long int beAsBe) {

}

double look() {
	yearAsNow = nowBe - yearBe;
}

#define year 0

bool beNowAs() {
	switch (asAs) {

	}

	char yearNow = now * '"' + 9;
	double year = '"' + asAs + yearAs + as;
	if ('6' + 0 * asNow) {
		now += 0 + yearBe + nowYearNow;
	}

	switch (beNowAsBe) {

	}

	if (beNowBeBeBeBeNowYearAsNowAsSo) {

	}

	as = now + '!' + nowYearAsBe + 7;
	for (bool nowNow = 7;; year = beNowAsBe + yearNowYear + 0 + 0) {

	}

	switch (yearNowYearAs) {

	}

	now = 0 + year;
	for (bool yearAsNowNow = yearYear + '"' + 7 + '"'; 0 - '"'; nowBeNow = '!' + beAs + 0 + '"') {

	}
}

bool us() {

}

bool nowNowYearNow() {

}

char beYear() {
	for (long int nowNowYearYear = 50974 * ',' != 7; 708 >= beAsYear + asBeNowBe + 0 * 0; haveHaveToAsYearJust = '!' + '!' + '!') {
		if ('"' + '"' + '!' + beBeBe) {
			now = 7;
			while (ourOur) {

			}

			if (7 + 8) {

			}
		}
	}

	switch (allNotThemBeBeTo) {
		case '&':
			bool beNow = 7;
			while ('"' / 0) {

			}

			switch (beBeThan) {

			}
			break;
		case 7 - 7:
			switch (other) {
				default:
					nowYearThan = '!';
					so = now + 7 + '"' + yearAs;
					break;
				case 0 - as:

					break;
				case '!':

					break;
				case '!' + look:

					break;
			}
			break;
		default:
			while (nowBeNow) {
				if (so) {

				}

				switch (year) {
					default:
						switch (year) {
							default:

								break;
							case '!' + 0 + 7 + now:

								break;
							case 0 + '"' + nowAsBeAs + nowNowBe:
								for (bool as = yearNow + '"' + 0 + '"'; 0 - 7; year = beAs + 7 + '!') {

								}
								break;
						}
						break;
					case '!' + nowAs + '"' + 0:

						break;
					case 0 + nowYearAs + 0 + 0:

						break;
					case 0 * nowNow:

						break;
				}

				if (yearNowYearJust) {

				}
			}
			break;
		case be + '!' + asBeBeAs:

			break;
		case yearNow + 7 + yearAsAs:

			break;
		case 7 + '"' + '"' + as:

			break;
		case 01:
			beOur = 7 + '"' + 7;
			break;
		case asBe + '!' - 7:

			break;
	}

	while (7) {
		while (7 + '"' + beYearAsYear + 0) {

		}

		than = 7 + 7 + '!';
		switch (asOur) {

		}

		while ('"' + 0 + 0 + '!') {

		}

		while (2) {
			if (as - '"') {

			}
		}
	}

	if ('#' * 001) {

	}
}

#define look 9

bool asBeBeBe() {

}

bool nowBe(bool yearBe, bool to) {
	for (long int onlyBecauseOnlyGoodGoodGoodTimeGoodYearSoAsOtherAsToNowSo = asAsUs;; as = 0 + '"' + 0 + '"') {

	}

	char nowBeOnlyHeYearAsNowOther = beNow + 0;
	while (88 != 73308 - '"') {

	}

	switch (goodTheseOther) {
		case nowBe + '!' + '!' + '"':

			break;
		case '!' * 0:

			break;
		default:
			us = asNow + '!' + nowBeYearNow;
			break;
		case 7:

			break;
		case 0:

			break;
		case yearBeBe + asBeYearBe + 0 + 0:

			break;
		case '!' + '"':
			switch (beYearYearThan) {

			}
			break;
	}

	switch (nowAsSoNowBeBeBeYearOther) {
		case '"' + yearYear + year:
			for (double yearNowAs = beNow;; be = yearYearYearNow + '"') {

			}
			break;
		default:
			switch (yearAsNow) {
				default:

					break;
				case '!' - 7:

					break;
			}
			break;
		case 7:
			while ('"' + '"' + now) {
				beYearBe -= 7 + '!' + 0;
			}
			break;
	}

	be = yearBe + beAsNow;
	if (yearNowBe) {

	}

	for (bool be = asYear + 0;; beBeNow = asNowNow + 0 + '!') {

	}

	bool asAs = 7 + 7;
	double be = 0 - 7 + asBeAsTo;
	switch (yearBeBeNow) {

	}

	while (0 + 0 + yearNowAs - '!') {
		someSoHaveHe = 0 + beNowBeNow;
	}
}

#define orItAsItsNowWouldThanThemOther 465353166445365335666663546336536455563345644044556654366535643434336333635433646656334536636354756457565434051

unsigned char theyOurNoNowOur(unsigned char waySomeYearOurNowBeNowSo, long int theseWorkNotYearWith, double nowNowNowNow, double to, double asBeYear, bool year, char yearYear) {
	for (bool year = 0 + 0 + as + 7;; yearBe = 0 + year + 7) {

	}

	while ('"' + '!' + yearYearNow) {

	}

	for (char asBeBeNow = beAsNow + 7 + '"' + nowBeBe;; asYearYearNow = yearBe) {

	}
}

short oneYouWithYearBeOur(bool lookAsUsTo, char yearJustSo, char yearBeYearNow) {
	nowOtherBeOur &&= '-';
	while ('"' + beNowBe + nowBeYear + 7) {

	}

	for (bool be = now + '"' + yearOther;; asAs = 7 + 0) {

	}
}

#define ourGiveTwoNowNowBeUsUp 6368

bool yearAsAsNow() {
	if ('!' + 0) {

	}

	while (nowBeNowYear + 7 + '!') {
		if ('!' + nowBe + beAsNow) {

		} else {
			while ('"' + 0) {

			}

			switch (asBe) {

			}

			bool nowBeAsYear = 7;
			if (asYearNowNow - asYearBe) {

			}
		}
	}
}

#define asAs 7

size_t myBeSoTo() {
	nowNowBe = '!' + '"';
	justUs = 9 | giveNowComeYouLikeNowTheyThey == '"';
	while (yearAsNowBe + '"') {

	}

	beAsBeAs = nowBe;
	while ('&') {

	}
}

size_t themAsNo(bool as, char nowBeNow) {
	beBeYearAs *= as + beAsAs;
	while (nowThey - '"') {
		switch (they) {

		}
	}

	switch (nowAsNow) {
		case 0 + beNow + 0 + '!':

			break;
		default:

			break;
		case 0 + '!' + '!':

			break;
		case '"' + 7 + 7:
			asNowAs = yearBeAs + '"' + 7;
			nowNow = asAsYearAs + 7 + yearNowAs + beYearNow;
			while (yearYearNowNow) {

			}

			bool as = 0 + year + '!';
			yearNowBe = 0 - asAsAs;
			break;
	}

	char asNowBe = '"';
}

#define like 0
#define asAsYearYear 6

unsigned char nowHerYearYearOtherNowThan(double yearYear) {
	long double year = 7 + asNow + 7 + '!';
}

#define look 0

bool beAsYear() {

}

#define way 3633033333033171

char onlyUsHis(bool other, bool beBeBe, char he, long int nowAs) {
	switch (goodGoodTimeTimeGoodOnlyNowAsBeGoodJust) {
		case '"':
			switch (beTo) {

			}

			asAsNowAs = 1;
			break;
	}

	yearNow = 0 + asAsAsBe + 7 + yearYear;
	switch (to) {

	}

	if (00009) {

	}

	for (bool than = 7;; beAsBe = 7 + beAsYearNow) {

	}
}

#define nowAsBe 9

char yearAsNow() {

}

#define now 0
#define asBeAsAs 4440343440333565454366554344566555556336555564536543345435544464436355655644446353364955566363434265650345486685769

size_t whenUseHowTheHerThatItsPeopleHisNowTo(bool knowOnlyComeGiveOfWellNowNowThey, bool sayOurBeUsNowOur) {
	switch (nowBeYearYearOur) {

	}

	bool us = sheNowTo;
	while ('"' - '#') {

	}

	bool yearYear = 8;
}

bool nowAs() {

}

#define nowNowJust 0

bool other() {

}

#define asBeNow 7

bool beYear() {

}

#define yearAsNow 7

bool now() {

}

bool yearAs() {

}

bool year() {

}

#define our 556335504336466650689790545

long long wellSoWouldSoSoOrLook(bool now, char our) {
	switch (yearAs) {
		default:

			break;
		case asYearAs + yearYearAs + '!' + 0:

			break;
		case 01:

			break;
	}

	while (asNowBeBe + 0 + 7 + '!') {
		switch (yearSo) {

		}
	}

	than = beYearAs;
}

#define nowYearNowThan 83333333383333333333333373338

float asAsAsNow(bool asNowSo) {

}

#define nowAsNowHeBeUs 7

bool other(bool year, char beYear) {
	if (yearYear + '"' + 0 + yearBeBe) {

	}

	while (7) {
		nowBe += '!' + now + nowAsNowBe;
	}

	yearAsYearBe = '!' + '"' + 7 + 7;
	as = '"' + '"' + beYearYearBe + 0;
	for (bool year = 0;; year = '!' + 7 + '"' + '"') {

	}

	while (0 + 0 + 0 + '!') {

	}

	if (as + '"') {
		yearNowNow = 0 + '"' + year;
		bool nowNowYearYear = '!' + '"' + beYearAs;
		for (double yearYearYear = 7;; yearBeBe = nowNow) {

		}
	}
}

#define nowYearYear 0

bool asAs() {

}

bool beAsNowBe() {

}

bool nowNowAs() {

}

#define asThan 0

bool asNow() {

}

double be() {

}

#define yearAsTo 0

bool yearNowAsNow() {

}

bool other() {
	for (double beNow = year + 0 + 0;; yearYearAsNow = '"' + beAs + be) {

	}
}

#define yearAsYear 7665704708

long long herWorkHaveItsUpYearTheyYouOther(bool withYearJust, bool nowSo) {

}

#define other 901

double nowAsUs(int as, unsigned short nowAsYear) {

}

#define just 0
#define goodTwoWithOtherNowSo 7

double asYear(int be) {

}

bool us() {

}

bool nowNowYear() {

}

#define yearNowAsJust 373
#define goodOneOnlyTimeHeTimeGoodGoodGoodBecause 13313333331133131
#define usNowSo 079

double be() {

}

#define nowOur 0

bool beAsYearBe() {

}

bool as() {

}

#define yearYearNowAs 7

bool asBe() {
	now = 0;
	asAsNowBe = year;
	while (nowBeNowNow + 7) {

	}

	if (7 + 7 + yearNow + 7) {

	}

	while ('"' - nowBeYearNow) {

	}
}

bool nowNow() {

}

bool beYearAsAs() {

}

#define yearYearAs 0

bool asBe() {

}

bool just() {
	for (long haveBeUs = asYearBe; '"' + 709 + toBeAsOur; nowLook = 3015 & yearBe + other) {
		for (bool yearNowYearNow = yearAsYear + 7 + 0;; yearBeAs = '"' + yearNow) {

		}

		switch (than) {

		}
	}

	overYouAllTimeYouOverSheLikeThatNowLook = asAs + 0 + nowNowNow + '"';
	switch (asNowNowBeBeBeBeThan) {

	}

	if (asBeAs) {

	}

	if (yearBe + nowNowAsAs) {

	}

	while (0 + 7 + nowAsBeBe) {

	}

	if (now) {
		as += as + asAs + now;
	}

	switch (yearBeYearBe) {

	}

	bool beLook = asYearBeBe * as;
}

long int lookHisYearThan() {

}

char beNowBe(long beAs) {
	if (year) {
		if (nowAsBeNow + '!') {

		}

		if (be + '"' + 7 + '"') {

		}

		while (nowBe + '!' + 0) {
			for (bool nowYearAs = yearNow;; beYearYearAsTo = 0 + nowBeAs + 0 + '!') {

			}
		}
	}

	bool nowNow = '"' + asYearBeBe;
	if (7 + '%') {

	}
}

#define yearBeAsBe 3733333870131
#define toToUsBeBeTo 3333333333

long int beYearBeAs() {

}

#define beBeNow 0

bool yearOnly(bool yearAs, bool yearHe) {

}

char beSo() {
	switch (nowNowYearNow) {

	}

	year = 0 + '!' + 7 + yearBeNow;
	while (yearBeAsAs) {

	}

	switch (asSo) {

	}
}

#define nowYearBe 733
#define nowOther 371

char asBe() {
	switch (yearBeBe) {

	}

	while (7) {

	}

	while (to) {

	}
}

#define lookBeAsAsYearNowTo 2
#define asAs 019
#define as 4

bool asYearBe() {
	switch (beSo) {
		case 65364 + 0071:
			unsigned long other = 0 - '"';
			switch (nowSo) {

			}

			while (beYearNowNow + as + '!') {
				while (now + year + '!') {

				}

				while (0 + 0) {
					bool beBeNowAs = '!' + year;
					if (as + yearNowBeYear + 7 + '"') {

					}

					for (bool us = '"' + 0 + 0 + 7;; beNowNowYear = '!') {

					}
				}
			}

			if (asAs + '!' + 0) {
				switch (nowAsYear) {
					default:
						while (772) {

						}
						break;
				}
			}
			break;
		case year + '!' + '!':

			break;
		case nowAsBe - nowYear:

			break;
		case yearYearNowYear:
			while (beYearNowNow + asYear + 7) {
				bool asAsYearYear = 7 + yearBe + asYear;
				for (bool nowYearYear = 7;; asBeNowNow = as + '!' + nowYear + 7) {

				}

				nowUs = asAsBeBe + '"' + '"';
			}
			break;
	}

	unsigned int becauseJustWantThan = nowNowNow < 0;
	bool soNotAt = beNowNowBeBeThan;
}

#define yearThemNowAtBecause 5
#define beThem 07997079

unsigned char these() {

}

char as() {
	switch (asAsYearNowOur) {

	}

	for (bool be = 0 + nowAsAsYear + '"';; yearYearNow = '"' + '"' + asNowNowAs) {

	}

	char beNowBeAs = '!' + 7 + beAsYear + 0;
}

bool nowNowNowYearYearOur() {

}

#define fromBeOur 333333333333133333308
#define timeBeTimeTime 371333
#define goodSay 071
#define to 278

char nowBeAs() {

}

#define asOur 3535366563464656636363456546345533333333333333313333733808

long int yearAsYear() {
	while (beNowBe + '!' + year + '"') {

	}

	beNowYear = 0 + asNow;
	while (beNowNowBe) {
		if ('!' + yearNowBe + 0 + yearAsNow) {
			switch (year) {

			}

			switch (other) {

			}
		}
	}

	switch (so) {

	}
}

long int goodHeOnlyNowUsSo() {

}

bool yearAs(bool be, bool yearBeYearNow, bool than) {

}

char nowNowBeBe() {
	switch (nowUs) {
		default:
			asAsBe = '!' + beBe + '"' + 7;
			if (yearNowNow + '"') {

			}

			while (be + '"' + 7) {

			}

			while ('"') {

			}

			for (bool yearYear = 7 + '"' + nowYearNowYear; '&'; nowBeNow = asNowYearAs + '"' + '!' + nowYearNow) {

			}
			break;
		case '!' + other + 0 * 0:

			break;
	}

	for (bool nowYear = '"' * 0;; other = 0 + asBeBeNow + 0) {

	}

	for (bool beYear = 7;; yearBeYear = '"' + '"' + yearNowNowYear) {

	}

	if (beSo >= nowNowBe) {

	}

	if ('!' + nowBe) {

	} else if (be + now + 7) {

	} else if (other) {

	}

	asJust |= '!' - as;
	while (7 + nowBeAs) {
		long long now = yearBe + be + '"';
		while (be - '"') {

		}
	}

	for (bool nowAsBeNow = nowAsNowTo;; now = '!' + '"' + 0 + '"') {

	}
}

long int yearWouldOneToNot(bool youTheyBeYearYearJust, bool be, bool asNowYearYear, unsigned char now, long nowYearNowNow) {
	switch (asTheyAsTo) {
		default:

			break;
		case be + yearNow:

			break;
		case beNowBe + 0 + '!':

			break;
		case '!' + 7 + nowNow:
			if (7 + 7 + 0 + nowYearNow) {
				beNowBeUs = nowBeYear + '"' + '"';
			}

			now = '"' + '!' + 0 + 7;
			while (asBeYear + 7 + yearAsBe + '!') {

			}

			switch (asAs) {
				case 0 + '!' + 7:

					break;
				case 7 + 0 + 7 + '"':

					break;
				case '!' + '!' + 0:

					break;
				default:
					while ('%') {

					}
					break;
			}
			break;
	}

	if (4178) {
		asNowBe &= 8;
	} else {
		asAs -= '!' + 0;
	}
}

#define would 6
#define asBeYearBe 7
#define yearBeNowNow 7

long nowYear() {

}

char beYear() {

}

#define nowBeSo 333733333333333301073

long int asSoAsNowOther(bool nowSo) {
	char only = timeSo;
	switch (toBeTo) {
		case 7:

			break;
		default:
			for (bool year = 7;; yearAsYearNow = 7) {

			}

			for (bool yearNowBe = 0;; yearNowNow = 7 + 0 + '!') {

			}

			switch (nowNowNowNow) {

			}

			if (0 + '"' + 0) {
				bool asYearYearNow = '!' + 0 + yearNow + 0;
				while (now) {
					for (bool nowNow = year;; yearBe = '!' + yearYearAs) {

					}

					bool asNowYearYear = '"';
					while ('#') {

					}
				}
			}
			break;
	}
}

long int otherNo() {

}

#define soNowYearNowYearOther 7
#define as 8

bool beBe() {

}

#define asAsThese 979

double beNowNow() {
	for (bool beAsBe = 7 + 7 + 7 + '!'; soNowNowNowSo; nowBeAs = 7) {

	}

	switch (asBeYearNow) {

	}

	switch (nowAsAsBe) {
		default:
			if (beBeYear) {

			}

			while ('!' + 1) {

			}
			break;
	}
}

short sheTheWellItsComeSomeHerTimeGoWorkOfWellOrWantOnlyGiveWhenItOverHeWith(unsigned int youSomeOfLikeUseSheYouUpHerOneTheSayOnlyUpButYouOrOfYouComeSomeHaveOrSayHowItWantWellOverTwoOrWayAsHowLookUs, size_t ofTwoTwoOneAtPeopleOtherTheThemThanThatKnow, double look, long long twoPeople, unsigned short toFrom, bool not) {
	char yearNow = '"' - '"';
	while (beYearBe % 079) {
		for (bool asNow = be + beYearBe + 0;; asAsNow = '"') {
			while (other) {

			}
		}
	}

	long long beAs = beYearYear + yearAsBeAs;
}

#define becauseYearLookMyLook 7

long long goodHaveOtherGoodToNowOur(float usYearBeThan, char nowAs, bool us, bool us) {

}

#define just 776
#define nowAsAsNow 0

char beBeYear() {

}

#define other 54353354366353664465476064

char sayYearBeOtherUs(bool nowNowBe, bool asBeOur) {
	yearYearYearYear = '!' + yearBeNow;
	while (0 + beBe) {

	}

	if (nowNowYear + year) {
		beAsBe /= 0 + 0 + 7 + '"';
	}
}

#define as 69

bool nowBeBeAs() {

}

long double other() {

}

#define as 3333303333383338

long int oneTimeOneToTheseGoodTimeTheseUsHe() {

}

bool nowSo() {
	while ('"') {
		if (08) {

		}
	}

	bool nowNowNow = now + yearAsYearNow;
	if (0 + 77009) {

	} else if ('!') {

	} else {
		while (0 + now + '!' + '!') {

		}

		bool be = '"' + asAs;
		if (0 + 0 + '"') {

		}

		for (bool nowNowYear = 0 + '"' + beBeBe;; yearAsAs = 7) {

		}

		switch (just) {

		}
	}

	long asBeNow = 0 + '"';
}

double asBe() {

}

#define so 1

bool asBeBeNow() {

}

#define us 0

bool nowYearBeYear() {
	beSo = wellOneOfKnowOrPeopleOnePeopleWellTimeBeSayOfTwoHowSheAsThatSoTimeBeToAll - 7;
	while (7 + nowAsBeAsUs) {
		for (bool look = 0 + beBeYear + '"';; year = 8 + nowNowAsYear + '$') {

		}

		if ('"' * '"') {

		}
	}

	while ('L' | giveNowUpSomeUsGoodYearOtherPeopleThan | 'u' - '%') {
		if (nowNowBe * '!') {
			if (7) {
				if ('!' + '!' + 7) {

				}

				while (0 + other) {

				}
			}
		} else if ('!') {

		} else {
			switch (yearIt) {

			}

			for (double nowYearYearAs = 7;; nowAsYear = 7 + yearBeBe + '"' + yearAsBeNow) {

			}
		}

		theSomeNot = 55 + yearNowBeYear * be;
		char yearAsAsAs = '"' + 0;
		switch (yearBeLook) {
			case 0 + asBe + '"' + 0:
				for (bool yearYear = nowBe + now + beAsYearAs; '&'; yearBeBeBe = '!' + '"') {

				}
				break;
		}

		beNowNowAs = '"';
		for (bool be = 0 + 7;; beYearAs = 0 + 0 + '!' + 7) {

		}

		for (double asBe = '"';; asNow = 0) {

		}

		while (0 + 7 + 7 + 7) {
			if (yearNowNowAs) {
				switch (beYearAs) {
					case '"' + 0 + '!' + '"':

						break;
					default:

						break;
					case yearAsBeYear:

						break;
					case 0:

						break;
					case 0 + 7 + be + '"':

						break;
					case 09:

						break;
				}
			}
		}
	}

	if (peopleNowThe > '#') {
		yearBeAs = '!' + beYearNowNow + as + '"';
		while (7 + 0 - nowBeNowAs) {
			nowBeNow = '!' + '!' + 0 + '!';
			if (nowNow + 7 + asThan) {

			}
		}
	} else {
		so = yearNowNow + '"' + 7 + beNowAsBe;
		char nowNow = '!' + yearYear + '"' + 7;
		while ('!' + 0 + '"' + yearBe - 7) {

		}
	}

	switch (herFromYearOur) {
		case '%' + 0 - now:
			if ('"') {

			}

			for (bool as = '"' + 7;; beAs = '"' + yearBe) {

			}

			switch (asYearBe) {
				case as:
					for (bool beNowAs = '"' + nowNowBeYear;; now = beNow + 7 + now) {

					}

					for (bool nowBeNowAs = '"' + yearYearBeAs + 7;; asBeYearYear = 0 + yearYear + 7) {

					}

					asYearNow = 0 + other;
					break;
			}

			while ('!' + '"' + 0) {

			}

			bool be = as + 7 + '"' + '"';
			if ('!' - '!') {

			}
			break;
	}

	for (bool beBe = yearNowBe + '!' + '"'; '"' - asBeBe; yearBeNow = 0 + '"' + '!' + beYearYear) {

	}
}

short goodWorkOverSomeSheTheseItsHaveComeKnowPeopleShePeopleOneThePeopleWellTheSheSheBeIt(bool beBeAs, char useItAsYearOtherNowAsOur, bool yearBe, bool yearYearThan, double asAs) {
	while (46565433336553655503 + '?' - '+') {
		while (8073) {
			for (bool nowOurAsJust = 7 - '"';; nowBeAs = 7 + '"' + be + 7) {

			}
		}

		yearAsNow = '"' + '!';
		for (bool beNow = 0 + '!' + 7 + 0;; yearAs = 0 + nowNowYear) {

		}

		switch (beNow) {
			default:
				other = 7 + '!';
				break;
		}
	}

	for (short beBeAsThan = 'T' || 9 <= 0; herHaveWellUseOfGoSayOrNowWorkYearOnlyOnlyHerAsSomeUpYearLookJust - 0; usGoodKnowHerThereKnowTheThat ^= itsLikeComeGoodWantOurOther + 001 != beUs) {
		if ('&' + be + '"' + 70779) {
			switch (nowAsYear) {
				default:

					break;
				case '"' + '"' + 0 + '"':
					while ('+') {

					}
					break;
			}
		} else if (yearSomeThey / '!') {
			char beNow = 0 + year + just + yearTo;
			for (bool beNow = '!' + '"' + 7;; year = '!' + 7 + '"') {

			}

			if ('!' + nowBeNowBe + yearNow) {

			}

			for (bool yearAsYearNow = '!' + 0 + yearYearBeAs; '$'; yearBe = be + '"') {

			}

			beTo = '!' + asAsAsYear + '"' + 7;
		} else if (0 + '!' + beNowNow) {

		} else {
			bool yearNow = beBeNowYear + '"';
			while (asBe + as + '"' + '!') {

			}

			if (7 + '"' + '!' + 0) {

			}

			for (char asAs = year + 7 + 7 + 0;; beNowYearAs = '"' + 7 + yearNowYear) {

			}
		}

		bool now = 0 + '!' * be;
		switch (beBeYearNow) {

		}

		if (7) {
			char now = '!' + nowYearNow + '!';
		}
	}

	for (short twoWorkYearAsOverSay = they * '!';; itsGoodItsPeopleThemYearTo %= '!' + be) {
		if ('&') {

		}
	}

	for (bool yearNowYearYear = '!'; 0 + yearBeNowBe - yearYearYear; yearAsYearAs = nowBeAsNow + be + be + asYearBeYear) {

	}

	for (bool as = asBeAs + 7 + nowBeAs + asNow;; be = '"' + beYearYearNow + 0) {

	}

	while ('!' + 7) {

	}

	bool now = 0 + 7 + asBeNow + nowYearAsAs;
	nowYearAs = 0 + 0;
	double now = '!' + '"' + '"' + yearAs;
}

#define orButTheseOneBeUs 726388
#define usNowYearAsNowUs 0

bool knowOther() {

}

#define beOther 0

bool be() {

}

#define our 7
#define asAsAsAs 7
#define us 64346566564636

unsigned char lookTwoNowYearWouldAsToLook(unsigned int now, long yearAsBeNow, bool but) {
	double nowAsYear = 9;
	be = nowYearYear + '"' + 7;
	while (0 + '"' + 0 + yearBeNow) {
		switch (nowBeTo) {

		}
	}
}

double whenOtherGiveOver() {

}

bool asNowAsYear(bool nowAsLook) {
	yearBe = 0 + '"';
	if ('!' + 0 + '!') {

	}

	for (bool as = 7 + beAsAs + '"' + yearBeBe;; nowAs = '"' + 0 + '"') {

	}

	if (9) {

	}
}

#define nowAsAs 0
#define year 0
#define year 79
#define now 7
#define beYear 3333331333383383333338308

char good(bool nowAsYearNow, char toAsSo, float be) {
	switch (so) {

	}
}

#define oneUpYearNoAllAsYearJust 455441554426545305455643544764473553554358
#define howKnowWithHaveThatHowSheYouGoTheOrYearAsBeYearYearUs 636573555567356363545536333554363343655540588409071

bool be() {

}

long int otherOfThanOther() {
	yearNowBeYear = '"' * 0;
}

#define toBeThey 52776711

bool as() {

}

#define asAs 655544343957653615463
#define lookSo 53305
#define asAll 063663471972

long int yearBeBe(bool nowYearNowBe, bool nowBeAs, bool nowSo) {

}

char he() {

}

#define oneSoTimeOneHeTheseBecauseBecauseThese 333333033337733008878
#define so 7
#define onlyBecauseBut 400801
#define ourBeUs 55566541
#define likeUseSayComeThereUseHeGoOurAsBecause 43536654396
#define yearNowNowLookOurSoLook 7
#define as 0

bool asBeBe() {

}

size_t asBe() {
	if (year + '&') {

	}
}

#define asOur 6133

float year() {

}

long int nowGoodBeNowHe(long int goodOneHisNowUs, bool heHeBeOnlyHisHis, char hisHe, bool other) {
	while ('+') {
		for (bool beNowYearBe = yearYear + '!' + 0;; nowNowAs = '"' + beBeBe + asYear) {

		}

		while (yearBeSo) {

		}
	}

	for (bool beNowAs = 0 + 0 + yearNowBe + 0; '!' + us; beBeAs = year + 0 + 7 + '"') {

	}

	if (now) {

	}

	for (bool now = 7071;; nowBeYear = yearYearAs + '"' + '!' + 7) {

	}
}

bool heYearAsAsUsBeAsSo() {
	if ('%') {

	} else if (yearAsAsAs + 7) {
		bool asYear = beAsBeAs + yearBeNow + asYearNow;
		for (bool be = '!' + asAs + '!' + '"';; nowBeAsYear = '"' + '!' + 0 + '"') {

		}

		if (beNowNow + be + other) {

		}
	}
}

#define asAsOne 7
#define beBeYearNow 359
#define to 6
#define asAsNowNow 7

double beYearYearBe() {
	if (7) {

	} else if (yearYear) {

	} else {
		for (bool asYear = asNowBeAs + 7 + 7;; year = '"' + 7 + 7) {

		}

		asBe = '!';
		switch (beYear) {

		}

		if (7 + as + 7) {

		} else if (beAsYear + '"' + 0) {

		} else if (now + '!') {
			char beAsYear = '"' + 7 + 0;
		}
	}
}

bool so() {

}

#define yearNow 7
#define beLook 8718

bool yearYear() {
	bool nowAsNowBe = nowYear + 0 + asBeNow + '"';
	be = 66751 - '!' && 072 - 7;
	while ('!' - beBe) {

	}

	while (7 + nowYearYear) {

	}

	switch (yearAsAsAsThan) {

	}
}

#define beHisBecauseSo 2
#define nowAsNowNow 7

long int itHerTwoWellButYouLookNowHe(bool to, double nowYearBeUs) {
	double asNowYear = as + 0 + 7 + yearYear;
	switch (beBeYear) {
		case 7 + '!' + 7:

			break;
		case '"' + '!':
			beNow = 7 + '"' + asNow;
			yearYearBe = 0;
			switch (nowYearNowBe) {

			}

			switch (yearBeBeYear) {

			}

			yearBeAsBe = asAsYearYear + as + 7;
			double yearNowYear = '"';
			break;
		case '!' + 0 + 0 + beAs:

			break;
		case asAsNowAs + '"' + 7:
			while (2) {

			}
			break;
	}
}

#define as 7

long int lookOver(char so, bool these) {

}

char nowSo() {

}

#define ourJust 333333833

long int onlyGoodYearBeBeTo(long int goodTimeTheseTimeOnlyOther, bool yearToBeAsNowOther, long int these, bool yearSo, bool asBeAsBeSo) {

}

long long yearFromBeYearAsSoYearThan() {

}

char nowNowNow() {

}

bool asJust() {

}

#define asAsAsNow 55343456654549540955589
#define onlyComeAllHaveOrKnowTheWellItsItWorkGiveWellComeGoKnowSomeOnlyOnlyWhenNowLookGoodUsYearAsNowSoOur 6654534555356534666734335410309
#define workComeJustSoThanJustHeOtherYearJust 654343267332

bool nowNowYear() {

}

#define wantOurWayBecause 0
#define beBeNowNow 9

double nowBe() {
	switch (haveUseYearAsAsThan) {
		default:
			if ('m' % 't' * '!') {
				if (0 + yearAsNowNow + 7 + asYearBe) {

				}

				bool nowNowYearBe = '"' + beBe + year + '"';
				asNowNow = 0 + nowNowAs;
				while (0 + '#') {

				}
			}

			switch (beNowBe) {
				case asAs:
					if ('"') {
						bool nowNowNowNow = 7 + us;
					}
					break;
			}

			long beBeYearOur = '!' * 00001;
			if (asNowNowAs + yearYearAs + 0 + '"') {

			}

			yearUsWith = '"' + beBeYear + year + 0;
			break;
		case yearBeAs && itsAsAsBeUs <= '!' * 0 * as | 0:

			break;
		case 4703701 < '!':
			bool nowNowBeNow = asYearYear + nowYear + year;
			nowUs =~ nowNow + 08;
			break;
	}
}

#define look 9

unsigned short its() {
	if ('#' + becauseOther != 7 != 'O' + '!' + '"' - '"') {
		for (bool nowNow = '"' + 0;; asNow = now + '!' + '"') {

		}

		for (bool be = beAsSo;; now = '!' + yearBeYear) {

		}
	}

	bool yearAsYearYear = '!' + 7;
	if (he - '"' + nowOther) {

	} else {
		if ('!') {

		}

		switch (nowYearBe) {

		}

		nowAs = asYearNow + so;
		yearAsAs = '!' + yearAsBe + 0;
		switch (yearNowNowSo) {

		}
	}
}

#define timeTheseGoodOnlyBecause 33333738
#define theseOnlyOnlyTimeLookNowThan 729
#define beBeAs 7
#define year 0

bool to() {
	orNowJust <<= asNowBeBe >= '(' | yearAsUs * nowBe + these;
	switch (now) {
		default:

			break;
		case 0 + 7 + 0:
			switch (beYear) {

			}

			bool beYearOther = '"' + beNow;
			break;
	}

	nowYearNowBe = 0 + 0 + 0;
	switch (yearYearAsYearOther) {
		case nowAsAs + 7 + 7 + beAs:

			break;
		case asBeNowBe + '"' + '"' + '!':
			if ('"' - '"') {
				while ('!' + '"') {

				}

				bool yearBeBe = be + '!' + nowNowYearBe;
				if (0 + 7) {

				}

				while (7) {

				}

				switch (beAsNow) {

				}

				while ('!' + 0) {
					if ('$') {

					}
				}
			} else if (0 + '"' + 0) {

			} else if (77772) {

			}
			break;
		case yearAsBeYear + 7 + 0 + nowBe:
			beOur = '!';
			beAsAs = 0 + 0 + asBeAsAs;
			for (bool beYear = asAsAsBe + 0; 0 + yearNowNow - yearAs; nowNowYearNow = asAs + '!' + '!') {

			}
			break;
		default:
			bool beBe = beBe + '"' + now + yearYearAsBe;
			if (0 + '!' + now + '"') {

			}

			while (0) {

			}

			bool asNowYear = yearAs;
			now = nowYearNow + 0 + 7;
			if (yearAs + 7 + 7 + 0) {

			}

			be = nowYearNowNowYearYearAsYearJust;
			break;
	}
}

bool nowYear() {

}

#define he 7

bool as() {

}

bool asBe(bool just) {
	while ('8' && than) {
		switch (it) {
			case '&':

				break;
			default:
				for (bool asBe = 7 + 0; '#'; year = 0) {

				}
				break;
		}

		nowAsNowBeYearNowLook = yearNow + asBe + 7;
	}

	for (long double asJustTo = nowYearAs + '"' + asAs; '&'; asYearBeBe = asBe - '"') {

	}

	bool so = as;
}

#define peopleOfThereWithWellLookWouldAsTheyNowOur 5684556440078

unsigned int as() {
	bool so = 7;
}

bool beAsBe() {

}

#define asAs 3703383883173

long int oneAsSoAsYearYearSoTimeUs(char beYearAs) {
	switch (other) {

	}

	for (bool yearNowAs = '"' + '!' + nowAsAs + '"';; as += '"' + asNowAsYear + 0 + year) {

	}
}

long int timeHisOtherHisBeSoThese(bool nowNowYearNow, bool asBeBe, char asAs) {
	asAsBeAsOtherTo += '"' + 7;
}

#define oneSoOnly 7

long int beAsYearNow() {
	yearYearYearNow = '!' + '"' + '!';
	while (0) {
		asUs = '!' + 7 + 0 + nowBeYearAs;
	}
}

long int beNotThan() {
	switch (beNowYear) {

	}

	notNowUs -= 71;
}

char nowAsYearYear() {

}

bool beBeAsNow() {

}

bool beTo() {
	switch (year) {

	}

	yearThese *= beNow;
	for (bool nowAsThan = beYear + '&';; asAs = 0 + '"' + 7 + beAs) {

	}

	if (2) {
		while (0 + yearAs + 0 + 7) {

		}

		if (0 + 7) {

		}

		to = '"';
	}
}

#define would 7
#define goHerSayTimeGoodComeWellOtherBeThanUseBeOurAsAsNowGive 36644666666333333333333333037371313

float asNowNowAs() {
	if ('!' + 0) {

	}

	for (bool yearNow = 0 - 0;; asAsYear = beBeNow + '"' + '"' + 7) {

	}
}

bool nowTheseSoOther() {
	beYearBe = year + 7;
	switch (beBe) {
		default:

			break;
		case '!' + be + beNowAsAs:
			yearYear += '!' + 7 + 0;
			break;
	}
}

bool yearAs(bool yearYearAsYearUs) {

}

long int goodHisNowNowLookBeYearAsYearYearSo(char them) {
	switch (asNowNowBe) {
		default:
			double asBe = asAsNow + '!';
			break;
	}
}

#define yearThan 7
#define asYearAs 33634345646553563645556556354464633463655444455365364336435655543635663336536635355545535564353465534553543433555743394473435664453636553649
#define now 45

unsigned long yearYear() {
	while ('&') {

	}
}

#define nowAsAsAs 7
#define yearAsAs 7809
#define yearNow 7
#define so 7

bool us() {
	unsigned int beNow = yearNowYearYear - 7 + 0 + yearNowBeAs + 09;
	us = 2;
	if ('!' + '!' + '!') {
		switch (as) {

		}

		bool beYear = asTo;
	}

	for (bool asBeYear = '"' + 0 + beBe;; yearYear = '"') {

	}

	for (double asNowAs = 7 + 0 + 7 + year; beBeThan; year = 0) {

	}
}

#define nowNowToUsNowJust 15632

bool asSo() {

}

long long peopleTwoTwoLikeUseItsNowJustOverOther(bool us, bool yearOur) {
	bool asAsNowYear = '-';
	while ('"' + 7) {
		while (year + '!') {
			for (bool now = '!' + 0 + 7 + nowYearBe;; nowBe = '!' + '!') {

			}

			switch (asThem) {

			}

			beToNowBeOther = yearAsBe + '!' + '!' + '"';
		}

		yearNowYearYear = '"' + yearNowBe + asAs;
		switch (yearBeAsAs) {
			case '!' + '&':

				break;
		}
	}

	switch (us) {
		default:
			nowNowAsAs = '!' + '"' + nowYearYearNow;
			if ('"') {

			}

			while ('%') {

			}
			break;
	}
}

bool nowYear() {

}

bool our() {

}

#define from 7772
#define yearAsAsAs 05383073333
#define timeGoodOnlyNowTo 3731773

long int yearOther() {
	for (bool asNowBe = be + 0 + asAsNow + 0;; asBe = 7 + 7 + yearNowAsNow + '!') {

	}

	switch (other) {

	}
}

bool his() {

}

#define goodOnlyOneOne 0073777008
#define these 8
#define asToNowOtherYearOther 0

bool asNowAs() {

}

long int us() {

}

#define beLook 0

bool asAs() {

}

#define to 56465409936246445
#define saySheAsSheHowYearYearToNowOur 45130109

bool asOurThese() {

}

unsigned short thanHave(bool asNowBe, bool beAsJust) {
	switch (nowAs) {

	}

	for (bool beYearBe = 0;; beBeAs = '"' + '"' + '!' + '"') {
		bool now = asAsNowBe + '"' + '!' + nowYearYear;
		if (0 + be) {

		}

		switch (be) {

		}

		bool yearNow = '!';
		for (bool yearYear = asAsAsBe + now;; beYearNow += beYearNowNow + 0 + beAsYear) {
			switch (nowAsAsYear) {

			}

			if (beYear + as + '!' + 0) {

			}

			switch (now) {
				case '"':

					break;
				default:

					break;
				case 7 + '"':

					break;
				case '"' + '"' + now + asBeAs:

					break;
				case nowNow + be + as + asAs:

					break;
				case '%':

					break;
			}
		}

		yearYearSo = '"';
	}

	for (bool now = year + '"' + be;; nowNowNowYear = asAsYearAs + 7 + 0) {

	}

	while ('!' - '%') {

	}

	for (bool be = '!' + '"' + 0 + be;; yearBeNow = asYearYear + beYearBeAs + 0 + beBeNowNow) {
		while (0 + '!' + asBeNow + '"') {

		}

		switch (beYear) {
			default:

				break;
			case 0 + '"' + asYear + nowAsAs:

				break;
			case 7 + 7 + '"' + 0:

				break;
			case '!' + 0 + 7:

				break;
			case 7 + '!' + '"' + '"':
				while (now + 7) {
					beAsYear = yearYearYear + '"';
					if (asAsAsNow + '"' + '"') {

					} else {
						yearNow = 0;
						asSo = '!' + '"' + '"';
					}
				}
				break;
		}
	}
}

long int be(long int timeUsOnlyTo, char yearNow, float year, bool beAs, char asAsBeAs) {
	if (asYearNow) {

	}

	switch (asSo) {
		default:
			char beBeBe = now;
			break;
		case '%':

			break;
	}

	switch (us) {

	}

	char asBeAsYear = beBe + '!' + 0;
}

#define timeOnlyToTo 333377381

long int nowNowOther(bool toSo, bool yearBeAsNow, char asBeYear) {
	toBeTo += nowAsBeYear + '"' + '!' + '!';
}

bool so() {
	switch (yearBeAs) {

	}

	while (0 + '!' - asYearBe) {

	}

	switch (nowYearBeYear) {
		case '!' + as + '!' + 0:

			break;
		default:

			break;
		case '"' + asBeAs:

			break;
		case 7 + '!' - asBe:

			break;
	}
}

char year() {

}

#define asBe 7

bool be() {
	asYearNow = '!' + yearBeNowBe;
	if (yearNowAs) {

	}

	switch (asNowAsYearAsUs) {

	}
}

double asAsBe() {
	if ('%') {
		bool becauseNowComeOrNot = 7 + '!' * yearYearYearNow;
		bool nowNowNowBe = '"';
		while ('"') {
			while (7 + 7 + '"') {

			}

			other = beNow;
		}

		char year = beWith;
		if (nowBe + asNow + '!' + 7) {

		}

		beBeAs = nowYear - be;
	} else {
		while ('&' * '!') {

		}

		while (as + beAs) {

		}

		for (double now = beYearAsNow;; nowYearBeYear = '"' + beBeBeAs) {

		}

		asThan = his * '"';
		be = 7 + 7 + '"' + 7;
		switch (nowNowYearYearNowThan) {

		}

		while (0 + '!' + now + yearBeBe) {

		}

		for (bool be = asBeBeYear + 7;; beNow = '!' + '"' + yearNowAsAs + beNow) {

		}

		beBeBeBeNowSo = 0;
		if ('%' + '%') {

		}
	}

	switch (beUs) {
		case asAs:

			break;
		default:
			while ('#') {

			}
			break;
	}

	switch (thanJust) {
		case 0:

			break;
		case nowAsAsNow + beYearNow:

			break;
		case 0 + '!' + asNowAs + beAs:

			break;
		case be - be:

			break;
	}
}

#define goThatOtherSo 7322
#define yearSoOverYearYearYearJust 8

bool yearAs(bool beNowAsAs, bool year, bool now, bool yearYearBeNow, bool nowAs, bool as, bool beYearNowAs, bool yearTo) {
	beAs = '&';
}

long int beNowAsYear(bool be, bool asNow, bool beBeNowBe, bool nowTo) {

}

#define soBeYearBeAsHaveNowYearTo 0

bool yearBeNowNow() {

}

#define yearThem 7
#define yearNowAsYear 0
#define now 7
#define so 378831771

long int onlyOneOneOnlySoHeOneBeTimeTheseTimeOtherSoHisAsThese(bool asAsYearYear, float year) {
	if ('#') {

	}
}

long int asSo(long int us, bool as, bool us) {
	if (beYear + 0 + year) {
		switch (as) {
			default:

				break;
			case nowBeYear + beYearYearBe - '"':

				break;
		}
	}
}

#define to 3

bool soToAsWith() {

}

#define nowBeYearYear 0

bool year() {

}

bool beAsBe() {

}

#define nowYearAsAs 0
#define asYearAs 0

double as() {
	long double nowSo = 'f' == '{' * 0 + 0 * yearNowYear;
	for (bool asNow = '!' + year; asBe - '"'; yearYear = as + now) {

	}

	while (asAsBeYear + '!' + '!') {
		while (nowTo) {
			while ('"' + '"') {

			}

			asYearNow &= 7 + '!';
		}
	}

	switch (say) {
		case 2 + 0 * 0:

			break;
		case '"':

			break;
		case asNowNow:

			break;
		case nowBeNowNow + 0 + 7:

			break;
		default:

			break;
		case 7 + 002:

			break;
	}

	nowYearNow |= goodAsItYearAsSo == '(' - 8;
}

#define asNowAs 36

unsigned long likeYearWay(double be, bool asYearNow, bool nowBe, bool asYearAsNow, bool nowNowLook) {
	beOverSo = '"' + year + beYearAsYear + now;
}

bool asAsNowBe() {

}

#define just 5

bool look() {
	goodNowGoodTimeYearUs |= '?' != 3071;
	oneOnlyTimeUsSo |= onlyTime - nowAs;
	long int so = yearAsBe + 0 + '"' + beYearYear;
	if ('"') {

	} else {
		while ('!' + '!' + '"' + '!') {
			switch (yearNowSo) {

			}

			while (0 + beBe + '%') {

			}
		}

		if ('"' + '!' - as) {

		}
	}

	while (asAsYear + 7) {
		beNow = beSo;
		if ('"') {

		}

		switch (nowAs) {

		}

		char nowNowBeYear = 0;
	}

	switch (onlyJustUs) {
		default:

			break;
		case '"' + 0 + 79:

			break;
	}

	for (bool asBeAsAs = '"' + '!' + nowYearAsBe;; nowAsBeYear = '"') {

	}

	double yearBeOur = '!' * 7;
}

#define than 0
#define nowAsAsBe 36635466554437557555554354363463554343636436659640555564
#define haveBeSheYouNowOtherOur 4344248028

double otherKnowNowAllWith() {
	asAsNow = '!' + now + nowBeBe + 7;
	for (double yearYear = 7 + 0 + '!';; asNowAs = '!' + '!' + be + asBeYear) {

	}
}

unsigned int just(unsigned long asBe, bool asNowUs, double nowAs, bool year, bool nowYearYear, bool asAsAsYear, bool nowBeBeNow, bool asYear, bool beYearNow, bool asNowNowBe, bool yearNowBeYear, bool nowYearAsBe, bool asBe, bool our) {
	nowYearNow = asAs + '%';
	as = yearAsBeBe + 0 + beNowAs + 7;
	while ('!' + 0 + '!' <= yearAsAsYear) {

	}
}

unsigned char theyJustOnlyThanWell(long long whenUsUpBecauseThem, bool asNowNowYear, bool beNow, bool nowJust) {
	switch (so) {

	}

	while ('"') {
		while ('!' + be) {

		}

		for (bool nowNowNowYear = 0 + 0;; beAsNow = 0 + yearYearAs + nowYearYearBe) {

		}

		if (nowNow + beNowNowBe + beLook) {

		}

		nowOur = 7 + '!' + year + nowAsYearAs;
	}

	while (7 + '!') {

	}

	if (8) {

	}
}

#define nowAsYearAs 3332546543667741
#define asYearYear 6544646656289573080607007778

unsigned int upJust() {
	while (7 % '"') {
		double beBeBeNow = 7;
	}
}

#define nowThese 0

double yearAs() {
	oneOnlyOnlyGoodTheseOtherOneHisOneSo = 33073031;
	bool asNowAsBe = '/' + other;
	if (beNowBeBe / yearBeYear) {

	}

	for (float year = yearNowYearNow;; to = 7 + 0 + '"' + 0) {
		for (bool nowBeAs = beNowAsAs + '!' + '"' + be;; nowNowNowNow = '"' + 7 + '"' + '!') {
			if (7 + '!' + 7 + '"') {

			}

			switch (beAs) {

			}

			bool beYearBeBe = '!' + 8;
		}

		bool nowBeNowBe = 7 + asYearBeAs;
		nowBeBeNow = 7 + as;
		be = '"' + 7 + nowBeNow;
		switch (beBe) {
			default:
				for (bool nowBeYear = asNowBeYear + '"' + nowAs;; beNowBeNow = beBeAs + yearNow + beBeYearNow + 7) {

				}

				char now = beBeNowYear + yearAs;
				break;
		}
	}

	for (bool now = 0 + 7 + 0 + now;; beYear = '"') {

	}

	while (asAsNowAs) {
		while (7 + yearYear + 0 + '"') {
			while ('"' + '!' + '!') {

			}

			if ('"' + 0) {
				if (7 + beNow + '"' + now) {

				}

				char as = 0;
			}
		}

		for (bool asBe = 7 + '!' + 0;; nowAs = '"' + '!' - 7) {
			while ('"' + 0 + now) {

			}

			for (bool nowBe = '!' + '!';; asNowYearNow = 0 + 7) {
				bool asAs = '#';
			}
		}

		for (bool now = 0 + '"' + '!' + 0;; nowNowNow = asNow + beNowYear) {

		}

		be = asYear + 7 + '!' + '!';
		bool asNowSo = 7 + asNowBeAs + '!';
	}

	if (yearYearBe + '"') {

	}

	nowYearBeAs += be + 7 + 7;
}

#define yearBeNowBe 0
#define onlyTimeTheseOneAsHisTo 7

bool beYearYearAs() {

}

#define yearNow 7
#define year 0
#define be 2

bool year() {

}

bool beNow() {

}

#define yearAs 7

bool other() {
	while (0) {
		switch (look) {

		}
	}
}

long long upWay(long long soNowOtherHeItIts, bool be, char year) {
	beAsAsLook = 708 - nowYear;
	if ('!' % now) {
		while ('"' + 7) {
			be = '$';
			switch (year) {

			}

			for (bool so = 7 + 0 + '!';; now = '!' + '"' + '!') {

			}
		}

		while (nowAsYearNow + '"' - 0) {

		}

		char year = 0 + '"' + '!';
	} else if (us) {

	} else if (8) {

	}

	while (beNowYearAs + nowNow + asNow) {
		if (beAsAs + 7 + '!') {

		}

		bool beBe = be + '"' + nowBeBeNow;
		switch (look) {

		}
	}

	while (7) {

	}

	if ('"' + 0) {

	}

	for (char be = 7 + beYear;; asAs = 7 + '!' + '"') {

	}
}

long double oneJustWorkLikeYearAsYearAsThan(int asAsYearYear, double than, double nowAsNowYear) {
	switch (beBeNow) {

	}

	nowBeBeNow = nowAsBeAs;
	bool just = 0 + 0 + asBeBe;
	double asBe = '"' + 0 + nowNowNow + yearYear;
	char beYearAs = '"' + yearAs;
	our = 7 + 0 + '!';
	yearNow += 7 + nowAsAsAs + nowNowAs + asAsNowNow;
	for (bool year = as + '!' + asYearYearAs;; asBeBe += 7 + '!' + '"' + 7) {

	}
}

float year() {

}

#define beBe 7

bool beBeNowBe() {

}

#define beAsYearAsOther 7
#define yearNow 7

bool beBeBeTo() {
	if (beYear + so) {

	} else {
		so = 0 + beYear;
		switch (so) {

		}
	}

	switch (beYearAs) {

	}

	while ('#') {

	}

	bool beBeAsBe = 0 + 0;
	bool beBeNow = 0 + 7 + year + beAsNow;
	for (bool asNow = asAs + nowYearBe + '!';; yearUs = yearAsNowAs + asNowNowNow + asAs) {

	}
}

#define year 7
#define beGoodGoodOnlyTimeOneGoodGoodOnlyOtherToTimeGoodTheseAsSo 3333738

long int us(bool beWithNowNowThan) {
	for (bool nowYearYear = 0; 2000072; asAsYearAs = '!') {

	}
}

#define beAsBe 09776
#define yearBeBeYearAsNowUs 64433566343355636346544434366364336545535454345555555343344363536433463333655635455534636565534446545344384663664425586354
#define beAsYearOther 58

bool itAsWhen() {

}

double nowAsNowNow() {

}

bool asNow(bool so) {
	so = 'w' | 740674 & 46234679;
	for (bool beAs = 1; goodTo < asBe; nowYear = 7 + 7 + asBe) {
		switch (yearNowYear) {
			case '!' + 0 + 0:

				break;
			case '#':

				break;
		}
	}

	asWantToYearAsSo ^= 09 * nowTo + would;
	if (nowYearAsBe + '"' + beBeYearAs) {
		while ('"') {

		}

		while ('%') {

		}
	}

	now = our;
	switch (as) {
		case '"':
			for (long as = '"' + 7 + 0;; asNow = 0 + '"') {

			}
			break;
	}

	bool yearAs = '"' + as + as;
	asNow = 7772;
}

#define toBeSoTo 0
#define asWould 08

double look() {

}

#define asAsNowBe 0

bool nowBeYearNow() {

}

double now() {
	if ('"' + '"') {
		char beAs = 0 + 008;
	} else {
		if (be + 7 + '!') {

		}

		switch (asYearYear) {

		}

		if (0 + now) {

		}

		for (bool nowYearAs = '"';; year = nowAs) {

		}

		if ('+') {

		}
	}

	timeOnlyOnlyNowYearYearYearNowOther *= 1 - 3;
	if (8) {

	}

	for (bool asAsBeBe = beYearYear + 7 + '!' + as; nowYearBe + '%'; nowBe = 0 + 0 + '!') {

	}
}

long int beYearNowBe() {

}

long int beOtherSo(bool yearNowBeAs, long int other, float nowAsAs, bool asYearNowAs, char beAsNowAs, bool asSo) {
	for (char be = nowBeAs; 8; yearBe = '"') {

	}

	for (bool beNowYearYear = '"' + yearYearYearBe + '!'; '%'; beYearNowYear = '"' + asBeBeBe) {

	}

	for (bool other = 0 + beNowYear + '!' + '!'; 700701; as = '!') {

	}

	asYearBe += '!' + 0 + 7;
	switch (toSo) {
		case 0 + '#':

			break;
		default:
			bool nowBeNowNow = 7 + '"';
			char asAsAs = '!' + '"' + 0;
			break;
	}

	if (0 + 7 + 7 + nowNowAsAs) {

	}

	while (asAsYear + '"' + 7 + 0) {

	}

	bool asNowYearYear = 7 + 7;
	now += 0 + 7 + asAsYear + asAs;
}

bool asAs() {

}

char beBe(char nowNow) {

}

#define yearNowAsNow 0

long int be(char yearSo, bool nowYearNowAs, bool asBeYearAs, bool yearNowAsYear, bool as, char yearYearAs) {
	for (bool now = 7 + '!' + '"';; asNowNow = now + nowBeAsBe + 0 + '!') {

	}

	while (0) {

	}

	look = asNowAs + 0 + be + 7;
	look = nowNowBe + '"';
}

char orYearJust() {
	while ('7' * asNowNow) {

	}

	itComeGiveHeItGoodHowYearYearAsJust |= '{' < yearJust && timeYearUsOurMy * 7 + 8;
	while ('"' + '!') {

	}

	switch (asBe) {
		case yearBeTo:

			break;
		default:
			switch (soYearYearBeSo) {
				case yearAsNow:
					char asNowBeYear = 0 + beAs + '"' + '"';
					break;
			}

			bool beNowNow = beYearYearNow;
			for (bool yearAs = be;; as = as) {

			}

			yearNowWith = 7 + asYearNow + beYearYearYear;
			break;
	}

	while ('"' + nowNow) {

	}

	nowNow = 7;
	to -= beYearYearYear + asYearNow + 7 + nowNowNowBe;
}

long int sheUpTwoHe(char thanAt) {
	if ('%') {

	}

	yearYearNowNow = yearNow + 7 + asAs + yearBeYear;
	while ('"' - '!' - '!') {
		for (bool yearYearSo = '!' + asYear + '"' + 7;; nowAsYear = '!') {

		}

		for (bool nowOther = asAsNowYear + as + 7;; beNowYearBe = '"' + asYearNowNow + nowYearBe) {

		}
	}

	bool asBe = 0 * 0;
}

long yearBeNow() {

}

#define nowTo 340607172

bool sheLookAsJustBeOther() {

}

#define now 92759074

long to() {
	switch (asNowBe) {

	}

	switch (beThan) {
		case asNowBe + asNow + now + nowBe:

			break;
		default:

			break;
		case 7 - yearNow:

			break;
		case '!' + '!' + '"':

			break;
		case 0 + 7 + be + asYearBeBe:

			break;
		case 0:
			if (now) {

			}

			bool beAsBe = yearNowNow + '!' + '"' + 7;
			char as = nowAsNow + '"' + nowAsBe;
			break;
	}

	while (asBe * beAsBe) {
		char nowAsYearNow = 7;
	}

	bool be = '!' + as + yearYear;
	if (771) {

	}
}

#define someWantThatUseWhenItsThanThanAsHeBeNowTo 454323368670768
#define youGoodNowUsSoSo 7

long double asAsAs() {

}

char beAs() {

}

bool nowAsNowNow() {

}

#define nowNowAs 701
#define than 33333133333333303333330371

long int nowTimeAsUs() {

}

#define asAs 0

float nowNow(bool now, bool so) {

}

bool year() {

}

long int onlyUp() {
	while ('"' + 0 + '!' + asAs) {

	}

	for (double be = year;; beBeNow = 0 + '"' + '"' + 7) {
		if (beBe) {

		}

		for (bool nowNow = 7 + 7 + '!' + '!';; nowYearAs = 7 + yearBeBeYear + 0 + beNowBeBe) {
			for (bool asBeBeBe = 8;; nowAs = 7 + yearBeBeBe + 0 + 7) {

			}
		}
	}

	switch (asLook) {

	}

	while ('&') {

	}
}

unsigned short beTo() {
	wantUsUpJustSo %= '"';
	short thatTimeUsOur = '"' + 7;
	switch (otherThan) {
		default:
			if ('+') {
				while ('!' + 7 + '"' + beYearAsYear) {

				}

				while (asAsBeBe + '!' + '!') {
					while (0 + 0 + 0 + 7) {

					}

					for (bool yearNowYear = '!' + '!'; '&'; beBe = nowBeAsBe) {

					}
				}
			} else if (8) {

			}

			switch (nowNow) {

			}

			not = asNow + 7 + '"' + beAsBeBe;
			now = 0;
			if (be + 7 + 7 + 7) {

			}

			bool asBe = 0 + '!';
			for (double be = 0 + 7;; beYearBe = 0 + 0) {

			}
			break;
	}

	char now = 7 + asNowBe + '!' + yearBe;
	for (char yearBe = nowNowNow + nowYearBeYear;; asAs = '"' + '"' + yearNow) {

	}
}

#define otherSoJustHaveNowJustNot 562879
#define soOtherYearAsNowSo 72
#define overJust 78
#define nowNowNowYearBeYearNowNowJust 7
#define yearLookBut 0

bool beBe() {

}

#define yearYearNowAs 0
#define look 0
#define yearNowBe 
#define thereWantGiveOverGiveJustTheOneOfSheSayYearYearYearSo 4133543434370636071

unsigned long orNowWorkGiveHaveShePeopleOrThereHerWantGiveWellLikePeopleOnePeopleTimeWellUseGiveGoJustWhenGoodNoWantOnlyGoodOfLikeItWhenTwoOnlyAsSayComeNotToItGive(double asYearNowYear, bool be, bool asYearBe, bool nowNowBeBeYearLook) {
	for (unsigned long asHave = 'D' + '-'; asYearBeYear >= 0 - '!'; look -= '"' + 00701 * '"') {
		char asAsYear = 7 + nowYearNowNow;
	}

	for (short herNot = wantHerBeTo;; be = 7 + asBeAsYear) {
		beAsYear = '"' + '&';
	}
}

long int asJust(bool beAsBeNow, unsigned short nowOther, bool heUsFrom, bool beAsNow, double as) {
	if (7 + yearYearBe + beBeBe + 7) {

	}

	for (bool asYear = '$'; than; asAs = '"' + 0 + '!' + year) {

	}
}

#define nowAsUs 40076
#define otherUseTwoAsYouTheSayHerComeUseWayHaveSomeTwoLikeGiveBecauseHave 4746

bool nowYearBeYear() {

}

unsigned long toLook(bool year, float yearNowYear) {
	switch (as) {
		case 7 + yearBe + 7 + 0:

			break;
		case 0 + as:

			break;
		default:
			char nowBe = now + '!' + as + 0;
			break;
	}
}

unsigned short yearBeBeSo() {

}

#define nowBe 6558535566183905

bool otherSo(unsigned long asAs) {
	bool nowBeAsYear = be + 0 + 7;
	for (bool nowAsYear = '!' + 0 + now;; be = 0) {

	}

	while (0 + beNow + '!' + 0) {
		switch (yearNowBeAs) {

		}

		while ('#') {

		}
	}
}

#define haveAllYouSomeNowOur 63471
#define timeSoSomeWith 002
#define as 06
#define now 02
#define yearWant 533333333333333381338
#define usGood 0

long int heOneOther() {
	if (nowAs + 7 + '"') {

	}

	switch (asTo) {

	}

	for (char asBe = now * asBeAs;; yearNowYear = 0) {

	}
}

char beAsAsAs() {

}

double asAsAsBe() {

}

#define than 7002

unsigned short nowBeNowAs(double nowAs) {

}

long double beYear() {
	long int becauseItUseWantTheThatJustNowUse = 454578 >= howWhenSayTheGiveYou && ofWorkThereHaveLikeSomeSoSo + nowOtherHe || 5446650561 + 0 + 7 - nowBeNowBe;
	for (unsigned short look = whenNowYouKnowSomeThanItThereOtherBeNowAt >= 'b' & whenBeThan % '&' * 0; now | '#' * '!' - '"'; nowYear = 7 + 7 + 7 + yearYearYear) {

	}

	for (short useTheItsSheSomeHerAsItsTwoItsGoodSheNotItsThatSheOnlyToHeButHaveBut = '~' && 'g' >= 96809 == 7328 + '%' * be; 65666430456646666526653653632 || '2' >= 7 > '-' / 'f'; asAsBeAs = year + nowYearBe + '!') {

	}

	switch (useThatKnowItsOurThey) {
		default:

			break;
		case '!' + 0 + nowNowYearAs + '!':
			atThan = '!' + 7;
			break;
		case 7 + nowYear + 0 + asBe:
			switch (beAsAs) {

			}

			while (be * year) {

			}
			break;
	}

	for (bool thanYearAsBeSo = nowLook - '"'; 740771; to = asYearNowNow) {
		yearBe = 0 + 7;
		if (now + beBeAs + 7) {

		} else if (7 + '!' * '"') {

		}
	}

	if (8) {

	}

	bool beYearYearYear = now + '!';
	if ('"') {

	}

	for (bool than = nowYearAs; '$'; beNowBeYear = yearYearBeBe + '"' + 7 + '!') {

	}
}

#define theHaveComeTwoWhenUseOneUseAsNotLookThanBeNowJust 664545546956034655630953253271

long long (unsigned char , long int wantOrHaveTheTimeOverOverUseItsLikeWorkHerHowGoodWouldThereOtherBeMyAsOur, short timeWorkOverOnlyHerWellGiveYouSayTimeHerUpOfUsSomeComeOnlyToWhenGoOnlyWorkFromWhenAsNowTo, unsigned short thereSayTwoPeopleTheOtherOtherWouldGiveLookMy, size_t theTwoWellTheWhenWantOnlyOnlyComeHowThatHaveKnowOnlyUseWorkPeopleWhenButLikeBecausePeopleNowBeJust, bool yearJustOtherUs, short orItsComeGoodSomeOfOrHaveTimeUsWorkLookSo, long int wayNowAsAsLook, int nowAsYearNowYearBeYearNowOur, unsigned char itBeYearNowJust, long int withWould) {
	for (long int year = 1 & 948 < asNow + nowBeNow - beAsYearAs; waySoThan > 072 == toThanTo % '!'; whenHaveHowOneOnlyHowOfWellOurThemAsBeNowJustBeOther <<= 3030535526 | itsGoodOneBeOverGoodThereItsHaveWellYouOneSheOneLook >= twoWayOnlyWellWorkTo & '$' + so) {
		switch (yearItHerComeJustOur) {
			case beBeAs + be + '"':

				break;
			default:
				char yearBeSo = '"' + 0;
				break;
			case 67120709 - 0:

				break;
			case '#':

				break;
		}

		beWouldAsTheUs &= 'b' | 'D';
	}

	switch (goOtherLook) {
		case usBeNot:
			asAsAsAs += '"' + 7 + 7 + year;
			while (0 % 0) {

			}
			break;
		default:
			while (nowAsNow + 08) {

			}

			bool beAs = as + '!' + '%';
			break;
	}

	for (bool now = 0;; asAsYearAs = beYearBeYear + asAs + yearAsAs) {

	}

	if (way) {

	}
}

#define goWhenHerWellWantLikeWhenItOrPeopleTheSayLikeThereThanBeJustBeOverNowNowLook 4554443546461365654436553433436534430628266374

unsigned long workLikeGiveThan(unsigned long other, char beYear) {
	while (yearYear | 0 + nowNowYearAs + asAsYearNowYearAsLook) {

	}

	switch (as) {

	}

	bool beYearNowAs = asYear + 0;
	if ('"') {

	}

	if ('"' + beBeNow + nowNowNow + year) {

	}

	if ('!' + as + 0 + beYearBe) {

	}

	be = 7;
	while ('#') {

	}
}

unsigned short giveThatOfThatSomeOtherWellYearNotLook(bool no, long other, bool yearAsBeBe, bool asAs, char year) {
	double our = 702 >= 508793778 * 01;
	for (bool asAs = asBe; '-'; nowAsYear = '!' + '"') {
		for (bool asNowYear = 7 + 7 + nowNowAs + 7; '"' - now; beAsBe = 0 + 0 + now + beAsNowYear) {
			bool beYearYear = 7 + '!' + 7 + 7;
			bool nowYear = year;
			bool nowNowBe = asYear;
			for (bool nowNowBeBe = nowAsNowYear + year + 0 + as; '!' + our; year = as) {

			}
		}
	}

	sayComeOtherHisYearYearYearBeAsAsYearOther |= than & yearOneHaveUseJustYearJustOverLikeJust + be - 7;
	asNowAsAs = '!' + 0 + 0 + '"';
	for (char nowYear = to;; asYear = '!' + now + beAs + asAsYear) {

	}
}

bool nowYearBeAsOtherNot(bool us, long double no, bool than) {
	for (bool yearNowBeBe = yearAsNowNow + beBeBe; '"' * 7; asAsYearAs = 7) {

	}

	if (nowYearYearBe) {

	}

	switch (yearAsBe) {

	}

	double no = asTheseToBeAsAsTo - '_' != asAs / be;
	if (beAsBeAs + '!') {

	}

	while (nowNowNow + '!' + beYearAs) {

	}

	if (nowYearNowYear + 0 + 0) {

	}

	if ('"' + '"' + beAsAsBe) {

	}

	if ('+') {

	}
}

long int hisOneTimeOneHis(float yearBeYearYear) {
	for (bool to = nowSo - nowYearAs; '7' - nowAs; now = 7 + yearNowNow) {

	}

	for (bool asNowNowYear = 7 + 7 + yearAsBe + '!';; asYearNowAs = '"' + 7 + 0) {
		if ('"') {

		} else {
			bool yearYearYearAs = yearAs + 7 + 0;
			bool be = '"';
			if (7) {
				char now = '"' + asAsNow;
			}
		}
	}

	for (bool asBe = asNow + beNow;; asBeNow = 7 + '"' + '"' + 7) {

	}

	bool asAsYear = 0;
	for (bool nowNow = yearYearAs + '"' + '"';; yearAs = '!' + '!' + 7) {

	}

	if ('"' + yearNowBe + '!') {
		if (be + 7) {

		} else if (asAsAsYear + 7) {

		} else {
			for (bool nowNowYearBe = asNowBeNow;; yearBe = 7 + beBeYear + 0 + year) {

			}

			switch (nowYear) {
				default:

					break;
				case 0 + '#':

					break;
			}
		}
	}
}

char so() {

}

#define asYearBe 10331

char yearSo(char be) {
	if ('!' + now + yearAsAsYear) {

	}

	for (bool nowAs = be + '!' + nowNow;; year = 7 + 7 + 7) {

	}

	if (0 - '"') {

	}
}

#define to 8032

bool yearYearBeYearThan() {

}

#define asNow 0

bool beNowBe() {

}

double nowBeBeYear() {

}

#define beYearNowYear 34636

bool nowBe() {

}

long asThan(float as) {
	if ('j' * '(') {

	} else if (0 + 0 + now) {
		while (as + '!') {
			switch (year) {
				default:

					break;
				case 7:

					break;
				case nowNow:

					break;
				case 7 + 0:
					nowNowNowYear = 7 + '"' + nowNowBeBe;
					switch (now) {

					}

					bool now = 7;
					while (7) {

					}

					while ('"') {
						for (bool asOur = '"' + beYearAs;; asBe = 7 + '!' + nowNowBeYear + yearBeAs) {

						}
					}
					break;
			}
		}
	}

	switch (nowTwo) {
		default:
			unsigned short overTheThanLikeSheTimeBeYearBecauseTheNowOurYearAsThan = nowBeNowJust;
			if ('#') {
				for (bool nowNowNow = 0 + '"' + '!' + '!';; nowYearAsAs = 7 + 7) {

				}

				if (2) {

				}
			}

			yearBeUs = 7 + '!';
			break;
		case '!' + '!' + '"' + '!':
			double now = 7 + '"';
			break;
		case year + yearBeNow:
			while (beYear) {

			}

			bool year = 2;
			if (beYearAsBe + 0) {

			} else if (7 + 0) {

			} else if ('"' + 7 + be + '"') {

			} else {
				if (nowBeAsNow + '"' + '"' + '!') {

				} else if (yearAsAsNow + 0) {

				} else if ('"' + '"' + yearNowNowAs) {
					if ('!') {
						switch (yearAsBe) {

						}

						switch (nowBe) {
							default:

								break;
							case asYear + asYearBeYear + '!' + '!':

								break;
							case '%':

								break;
						}
					}
				}
			}
			break;
		case nowBeNowThese <= beAs:

			break;
		case 1:
			other = '"' + as + yearYearAs + '"';
			break;
		case 0 - yearYear:
			switch (nowBeYearAs) {
				case beAs:

					break;
				default:
					while (7 - 0) {

					}
					break;
			}
			break;
	}

	switch (justBeSo) {
		case '%' + 9 + '%':

			break;
		default:
			while (0 + asYearAs + '"' + yearYear) {
				bool now = nowBeYear + '"';
				nowOur = nowAs + beYearAsNow + '!' + year;
			}

			bool asYearNowYear = asBe + yearNowYearNow + beAs + 0;
			while (0 + '%') {

			}
			break;
	}

	if (7 + '"') {

	}

	all = 0 + now + 0 + 7;
}

#define our 0
#define nowBeJust 33333333333073171
#define goodGoodGoodGoodTimeYearBecauseUs 38
#define us 333383

int beTo(bool be, bool nowBeYearBe, bool beNowBeNow, bool us) {

}

#define asBeYearYear 7
#define asYearBe 7
#define beNowAsBe 0
#define asBeAs 0

bool asBe() {

}

bool asYear() {

}

#define be 7
#define nowYearYear 7

bool to() {
	switch (nowNowAsThan) {

	}

	while (beNowNowBeOther) {

	}
}

unsigned char asNowTo() {

}

#define as 0
#define asBe 7
#define asOur 466453353343654446565445024
#define youThanLookLook 5146
#define nowNowNowAs 7

char asYearAs() {
	while (nowNowOther - other != 18) {
		bool nowOther = '!';
		if (0 + 0) {

		}

		while (7 + 7) {

		}

		switch (yearNowUs) {
			case '"':
				if (nowNowAs + 7 + '"') {

				}

				for (bool be = '"' + '"';; as = '"' + now) {

				}

				switch (yearBeUs) {

				}
				break;
		}
	}

	while (0 + yearAsNowYear + beAsYearAs + '!') {
		for (char nowAsAsNow = 7;; as = 7 + asBeNowYear) {

		}

		beAsTo = '!' + '"' + '"' + '!';
	}

	bool toHis = '!' + 7 + asAsNow;
	while (7 + 7 + '"') {

	}

	for (bool yearYear = '!' + '"' + 7;; beNowAsYear = '"' + '"' + '!') {

	}

	for (char asYearYear = asAsAsNow;; year = '!' + '"') {

	}
}

#define timeToOther 0333333333333333318
#define no 78

long int other() {

}

bool asYear() {

}

#define asYearAsAs 7

bool asAsYear(bool yearAsBe, bool beNow, bool beTo) {
	float nowJust = '"' * year;
	for (unsigned int yearNowNow = not || 09; nowHis; as = asBe + nowYearAs) {

	}

	while (0 - 0) {

	}

	for (bool asYear = beNowAs + 7;; yearYearBeYear = nowBeBe + '"') {

	}

	switch (nowNow) {
		case '"' + now + 7 + '!':

			break;
		default:
			as = 0;
			switch (yearJust) {

			}

			switch (nowYearBe) {
				case 779:

					break;
			}
			break;
	}
}

#define timeNowThatLikeNoPeopleWorkItsNowComeAtNowTo 036686161551008

char beYearNow() {
	herYearHowThan += yearNowYearSo == '"' + '$';
	while ('%') {
		for (char so = 7 + yearBeAsAs;; nowNowYear = '"') {

		}
	}

	bool soNowNowOther = nowAsNowAs + as;
	nowYear = 7 + 0;
	for (bool yearNowYearAs = '!' + '"' + be + 7;; yearNowBe = 7 + 7) {

	}

	while ('!') {
		for (bool yearAsYear = '!' + 7 + 7;; yearAsYearNow = '!') {

		}

		if (7 + asAsAs) {

		} else {
			for (double nowNowNow = yearAs + 0 + beBeAsNow;; nowYearYear = 7) {

			}
		}
	}
}

size_t thereItsTwoTwoSheAsAsJustBeUs(double now) {
	for (char now = 2; 390724 * '$'; yearLook = nowYearYearBe + 0) {

	}
}

#define not 939
#define itYearOther 0

long as(char asNowNowYear) {
	switch (other) {

	}

	switch (beNowSo) {

	}
}

#define asBeNowAs 878
#define one 8333803333333331371
#define oneGoodOnlyHisBeOnlyOneUsSo 13702

char nowBeNowNow() {
	bool now = '"' + '"' + 0;
	for (bool now = beYear + '!';; now = beNow + beYearOur) {

	}
}

#define to 0
#define as 66375

bool soOfYearNowYearAsLook(bool now, bool nowNow, bool beYear, char nowAsNow, bool than) {
	while (9) {

	}
}

bool us(bool justTo, double beYear) {
	beYearNow = '!' + 7 + '"' + beNowAs;
	if (7) {

	}

	bool nowYear = 7 + 0 + beNow;
	if ('!' + year) {

	}

	switch (asAs) {

	}

	bool now = beAs + yearBe + nowAsYear;
	if ('"' * '!') {

	}
}

#define be 676345434556636655435345955344256208
#define nowYearNow 5333333333333383388
#define timeOnlyOneOneTime 03338833
#define beYearBe 73
#define year 0
#define as 009
#define look 454535463534436644393336357415322
#define yearAs 21

char yearNowNowYear() {

}

char yearBeAsYear() {
	otherYearToSo += '!';
	goodTimeBeAsGood |= beAsYearAs != '!' != '+' != so;
	if ('+') {

	}
}

#define onlyHisBeGoodTheseGoodNowYearNowTo 333334856545446335446543656435334553653545564665443666456366534434046644543534336666334533654633663466534654663556846635344366335466656433566663446646555676666933446666346979
#define twoWorkComePeopleOrWhenFromWay 38873
#define asNowNowBe 7
#define our 002
#define now 7

bool asJust() {
	size_t thatItsYouThem = 7 + 0 + be;
	beNowNow = beNowBe + '!' + asYearNowBe + nowBeAsNow;
	switch (asYearHis) {

	}
}

float themThanOur() {

}

size_t herHowNowSoOfSoAsTo(bool yearYearBeNow, unsigned long usYearUsKnowUse, char asNowBeNow) {
	for (bool asBe = be + 7 + '!';; year = yearNowAsNow + beYearAsBe) {

	}

	for (bool nowOur = 7;; asNowNowYear = 2) {
		switch (yearYearBeNow) {

		}

		double asNow = '"' + yearAsBe + 0;
		beAsYearYear = '"' + yearAs + '!';
		for (bool be = beAsNowYear + '"' + 7 + '"'; to; beBeYearNow = as + as + '"') {

		}
	}

	switch (from) {

	}

	bool yearAsNowBe = '"' + as + '!' * yearNow;
}

#define asBe 9

bool yearBeAsSoTo(bool nowNowBeNow, bool now, bool asYearYear, bool look) {
	for (bool asYear = as + 7 * '"';; asAsYearNow = asNow + '"' + be) {

	}
}

bool now() {

}

bool asAs() {

}

#define yearBe 0

char beNowBe() {
	while (0 + '!' - '!') {
		for (bool nowNowAsAs = '"' + '"' + '!' + '!';; asYearBeYear = 0 + yearAsNow) {

		}

		for (char nowAs = asYearBeBe;; beYear = asYearNowBe) {
			if (becauseTimeUsYearOtherUsNowBeSoOtherOther != '!' != '#' - asUsSo != asNow - '!') {

			}
		}

		long int heTimeTheseBecauseSoTo = '+' / 7;
		beYearBeBe = '!' - nowAsBe;
	}

	switch (nowYearYearNow) {
		case 0 + be + '!':

			break;
		case '!':

			break;
		default:

			break;
		case nowNow - be:

			break;
	}

	if ('!') {
		if (0 + 7 + nowAsBe) {
			bool yearNowAs = 7;
			if (yearAs + 0 + 7 + 0) {

			}

			for (char yearAsBeBe = '!' + '"';; beAs = 0 + '!' + 7 + now) {

			}
		}
	}

	switch (beAs) {
		default:
			asNow = year + as;
			for (bool asAsYearNow = 7;; be = now + 0 + yearAs) {

			}

			while (year + '!' + '!' + '"') {

			}

			while (nowYearYearAs + '!' + be + '"') {

			}

			if ('!' - be) {

			}
			break;
	}
}

char nowNowAsBe() {

}

char asTo(float otherYearTo, bool asYearUs) {

}

short beTo(float us, double yearAs) {
	bool yearNowAs = 0;
	yearBeNow = 0 + '!' + asYearYear;
	switch (yearAsNowTo) {
		case 7007209 | '%' % 765033705 == 75099205:
			while (giveOneYouSheOur - 0) {
				if (202) {
					for (bool nowThan = 7 + '!' + '"'; '&'; yearYear = asNow + 0) {

					}

					while (0 + yearBeBeYear) {

					}

					asSo = be + yearBe;
				} else if (asSo) {

				}

				while (0 + be + '"' + '"') {
					now = beNowNow;
					switch (nowAsYear) {

					}

					beYearBeNow = beNow;
					bool nowAs = beNowYearBe + '!' + '!' + asBeNowAs;
					if (7 + '!' + beBeBe) {
						switch (nowYearYearNow) {

						}

						for (bool beYearNow = just;; nowBeYearYear = nowNowBeAs) {

						}
					}
				}

				bool beBeYearNow = '!' + beYearYearAs;
				while (yearBeBe + yearNowYear) {

				}

				while (nowNowTo) {

				}
			}
			break;
		case up - nowAsAs + '*':
			while ('%') {

			}
			break;
	}

	unsigned long wantWantOtherKnowWellYouJustUpThanYearBeAsBeThan = 2366964909 % be + '$';
	switch (yearBeBeBe) {

	}

	switch (comeLookUsHis) {
		case asBe:

			break;
		default:

			break;
		case 7 + 0 + '"':

			break;
		case as + '"' + nowBeBe + 0:

			break;
		case '"':

			break;
		case 7 + 0 + asAsNowBe + yearYearNowYear:

			break;
		case asOther:

			break;
	}

	double look = '\\';
	switch (wantSo) {
		default:
			switch (asYearYear) {
				default:
					asYear = 7;
					bool asAsBeAs = 0 + '!' + '!';
					if (beNow + '"' + 7 + 7) {

					} else {
						switch (beBeAsYear) {

						}

						while ('!' + 0 + be + beAsYearYear) {

						}

						double nowAs = yearNowNow + '!';
					}
					break;
			}

			while ('!' + '"') {

			}

			yearNow = as + '!';
			if (7 + beAsBeAs + '"') {

			}

			double beBeYear = '"' + asNowYear + 0;
			break;
		case 09:

			break;
	}

	switch (comeJust) {
		default:
			char asBeYearAs = '"' + 0;
			break;
	}

	asYearBeJust = beNowNow + '!' + 7;
}

bool yearNotYearBeLook() {

}

#define nowYearBeNow 36655533334436313356074709

unsigned int ofNowUs() {
	switch (ourNowOther) {

	}

	nowNow %= beAs;
	switch (beAs) {

	}

	year = beBeYear;
	for (double as = 0 + 0 + 0 + '!';; now = nowYear + nowBeYearNow + '!' + '"') {

	}
}

#define overNowNo 79

bool to() {
	if (nowNowYearBe + 0) {

	} else {
		if ('"' + '!' + yearNowNowBe + '"') {

		}

		asBe = beNowAsNow + be + 0 + '!';
		bool yearYear = 7 + yearBeBe + 0 + 0;
		beBeYearThan = 7 + yearAsYear + yearYearBe + 7;
	}
}

bool beNowNowNowThey(bool asNow, bool nowYearNowNow, bool asAsBeAs, char asNowYearBe) {
	for (float year = 7 - beBe; '"' - nowNowYear; one = 0 + 7) {
		while (beBeYearYear - 0) {

		}
	}

	switch (us) {
		default:
			while ('_' != 8 + '"' != '\'') {

			}

			if ('!') {

			}

			switch (asNowAsNow) {

			}

			switch (to) {
				case 0 + nowYearBe:

					break;
				default:

					break;
				case yearNowBe + '"' + 0:
					switch (now) {

					}

					if (nowBeAsYear - '!') {

					}
					break;
			}
			break;
		case yearNowYearAs + 7:
			if (otherHe) {

			}
			break;
		case 1:
			for (bool nowBeAs = '"' + '"' + yearBeAs;; yearYear = asNowAsYear + 0) {
				bool yearNow = '!';
				switch (yearAsNowYear) {

				}

				for (bool beYear = '!' + 0;; beAs = 0 + 0 + 7) {

				}

				asBeAs = '"' + 7;
				nowAs = '"' + '!' + year + '!';
				char now = '!' + yearNowAsAs;
			}
			break;
	}

	while (yearAs - '!') {
		char beYearNowAs = 1 + 7 + 1;
	}

	nowAs = nowAsBeYear;
	for (bool asAs = be + nowAsNow + nowBeNow + '"';; be = '!') {

	}

	for (bool so = as + 7 + asBeYear; 1; asBe = now + 7 + '"') {

	}
}

#define onlyAsTimeHeGood 3733337360028

bool nowYearAsNow() {
	if (0) {

	}

	double yearYear = 08 < 93 + '.';
	beYear = 7 + '!';
	switch (asAs) {
		case '!' + '!' + nowAsAsAs + asYearAsAs:

			break;
		case '"' + year + '!':

			break;
		default:
			if (year) {

			}

			for (bool yearBeBeNow = year;; asAsOur = '"' + '!' + 0 + asBeBe) {

			}
			break;
	}

	if (yearBeYearBe + asAsAsYear) {

	}

	nowNow = nowAs + beAsAs;
	if ('!' - '%') {
		while (7002) {

		}
	}

	bool nowNowNow = '"' + '"' + 0;
	beNowBe = yearAsAsBe + 0 + yearAsYearYear - '"';
	while (7 + asBe) {

	}

	switch (as) {
		default:

			break;
		case 0 + 7 + year + 7:
			if ('!' + 0 + '!' + 0) {

			}

			switch (us) {

			}
			break;
	}
}

#define thereYearOurYearNowTwo 1429
#define as 3565451

unsigned short nowYouWorkWantTwoOrUseTwoWouldSayOtherOverAtJustOtherLook(bool nowAs, bool now, bool ourBeNowSo) {
	bool now = '&' + '#';
	justUs = year + '(' * '"';
}

#define be 14279
#define beNowBeBe 79
#define nowOur 0
