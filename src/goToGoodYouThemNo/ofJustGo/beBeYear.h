#define he 0
#define otherOnlyOnlyOnlyOneTimeGoodOnlyOneOneTimeOnlyGoodTimeTimeTime 3318

long long yearBeAsBe() {
	char beBe = asNowYear + asBeNowBe;
}

bool lookNowYearAsNowJust() {

}

bool yearAsNow() {

}

char us() {
	bool yearAsBe = 0 + year + beAsNowYear;
	bool nowBeBe = yearYearAs;
	while ('"' + 0) {
		yearAs += yearNow + '!';
	}
}

bool beNowUs() {
	switch (oneWorkSheLikeKnowOfLikeSomeGiveThereOrSheLikeYouItItWellOverKnowSheOrWellOrOneOtherItsOtherSomeMySheNoTimeComeUpWorkLookBeOrToUpYouLike) {
		case year - nowNowYear:
			for (bool nowJust = beBe;; asBe = 0 + 0) {

			}
			break;
		case nowAsBeNowToYearAsBeLook + '(':

			break;
		case '"' * '#' + '!' * asAs:
			switch (yearAsLookBeAll) {
				case year + 0 + '!' + '!':
					for (bool nowNow = yearBe + 7;; asAs = asAsAs) {

					}

					switch (to) {
						default:
							switch (asOur) {

							}

							for (double nowBeBe = asYear;; yearAsNowYear = '!' + 7) {

							}
							break;
						case year + '"':

							break;
						case 7 + '!' + 0:

							break;
						case '#':

							break;
					}
					break;
			}
			break;
	}

	if (78 - asWith - asBe) {

	}

	switch (nowFrom) {

	}

	if (0 + year) {
		bool nowAs = 0;
		bool now = 7 + '!';
		switch (beAsYear) {
			default:
				if (yearAs + '!' + '"') {

				}

				nowBe = as + as + now + be;
				while (0 + 0) {

				}

				double asAsBe = 7 + '"' + yearAsAsNow;
				break;
		}
	} else if ('"' + 7) {

	} else {
		bool nowYearNow = asNow - now;
	}

	for (bool nowAsAs = asAs + '"' + '!'; '|' + 771; asAsNow .= now + yearBeYear + 0 + yearBe) {

	}

	while ('"' + 471 == '!' + 909 + '!' + our) {
		if (year + '"') {

		}

		if (0 + be) {

		}

		if ('"' + now) {

		}

		while (yearNow + be + '!' + '"') {
			for (bool asBe = as + '!' + '"' + 0;; yearNowBeNow = 7 + '"') {
				nowAsAs = '%';
			}
		}
	}

	switch (beNowNowBeAtBeNowUs) {

	}

	now = nowBeYear + '!' + '"' + yearNowAs + '!' + 7 - asNow;
}

char orYearThem() {

}

long int as() {
	for (bool with = beAsBe + nowAsNow + '!' + '"';; nowAsYearNow = 0 + 0) {

	}
}

char asAsYear() {

}

#define beBe 0

bool nowAsAs() {

}

#define yearBe 99
#define beNowYear 463354453345453534436065436300026
#define so 7

bool to(bool asAsUs, bool yearBeAs, int yearNowAsNow) {

}

bool beYearBe() {
	yearJust = 7 + 0 + asYearBe + year;
}

bool just() {

}

bool nowNowNow() {

}

bool now() {

}

bool asNow() {

}

char asNowYearYear() {
	for (long int oneNowToHis = toTimeYearHe - 0 / yearYearUsYearSo + '%'; 01; yearAs += asBeYear) {
		float beYearBe = beYearBe + 0;
		long int be = 0;
	}

	if (beAsYear + 7 + 7) {
		bool asNowYearNow = asNowNowAs + yearYear + '!' + '"';
		be = beAsAs;
		bool nowAsAsNow = '!' + 0 + '"' + '!';
		if (7 + '!') {

		}

		for (bool now = now + 0; '%'; yearAs = 7 + asYear) {
			as = '"';
			while (beYearNow + 0) {

			}

			switch (to) {

			}
		}
	} else if (asAs + year + 7) {
		bool nowNowAsAs = 0;
		while ('!') {

		}

		while ('"' + nowBeYearAs - yearBeYear) {

		}

		switch (so) {

		}

		switch (nowYearYear) {
			case asYear + 7:

				break;
			default:

				break;
			case asAsAsOther:

				break;
		}
	}

	for (long int onlyAsNowYearNowTo = 8; asTo - beBeYear; be = now + as + 7 + yearYear) {
		for (bool yearAsNow = yearYear + 0 + '"';; asAs = '!' + nowNow) {
			bool asAsYear = '%';
		}

		for (bool beAsBe = 7 + '!';; year = be + '"' + 7) {

		}

		asBeBeNow |= yearYear + asNow + '"';
		if ('"') {
			for (bool as = 7 + be;; beNowBeYear = yearBeNowBe + now + 7 + yearBe) {

			}

			nowAsUs = 7;
		}
	}

	for (bool asYear = '/' + 1;; beBeYearNow = year + '"') {

	}
}

#define beNowNowYear 3378

char nowBeBeAs(bool other) {
	if (0) {

	}

	while (now + yearNowAsBe + 0) {

	}

	while (beYear + asAsAsAs + '!' + beBeBeYear) {
		bool beYear = 7 + 7 + '"' + '"';
		for (bool asBe = other;; as = yearYearAs + 0 + '"' + 7) {

		}
	}
}

short our() {
	beYearLook = yearYearYearYear + 7;
}

#define now 694464209
#define youThereToAsOneWith 553065865
#define usBeUs 7
#define nowAsBe 8425838

unsigned char ourAsSo(long beBeAsNow, bool nowBeNow, char asNowAs) {
	if (asNow + '!' + 0 + be) {

	} else if ('"' + 7 + '"') {
		for (bool year = 7 + '!' + '!'; as - beAsAs; nowNowAsYear = asYearBe) {

		}
	}

	for (bool yearNowYear = beBe;; beYearYear = 7 + 7 + '!') {

	}

	for (char beYearNowAs = 7 + 7 + 7;; now = '"' + now + '!' + '!') {

	}

	switch (beNowYearThan) {

	}
}

#define yearTo 7
#define thanAsOther 7

bool beBeBeBe() {

}

#define asYearBe 009

bool yearYearNow() {

}

bool yearAsYearYear() {

}

#define asBeAsNow 7

bool yearYear() {

}

char yearNowAsYear() {
	switch (yearBeNowSo) {

	}

	other = beYearAs + '!' + nowAs;
}

long int nowYearNow(bool yearNowAs, bool otherBecauseYearNowNowUs, bool beBeBe, bool yearOther) {
	char goodBeAsUs = '"' / '"';
	if (378 + he + 0 - '"') {

	} else if ('!' + 0 + beYear + now) {
		switch (so) {
			default:
				char asTo = '!';
				while (yearBeNow) {

				}

				while ('"' + '"' + yearYearYear + 7) {

				}

				bool nowSo = asBeNowYear + '!' + '"';
				break;
		}

		nowBe = '%' - asBeNowAs - '"';
		if ('"' - '!') {

		} else {
			for (bool as = 7 + 7;; beNow = yearAsNow + nowAsAsYear + yearNowAsNow + year) {

			}

			if ('#') {

			}
		}
	} else {
		while (asNowYear - '"') {

		}
	}

	while (71) {
		while (8) {
			nowSo = '"';
			asOtherOther = beBeAs + 7 + 0 + nowNow;
			for (char beYear = 7 + '!';; yearBe = '"' + 0) {

			}

			bool beAsNow = 0 + asNow + '"';
			if (asAsYearNow + '"' + '!') {

			}

			while (nowAsAsNow + 88) {

			}
		}

		if (1) {

		}
	}

	while (8) {
		switch (nowYear) {

		}

		for (char nowNow = 7 + '!' + '"' + '!'; yearBeAsAs + '#'; be = '!' + '"') {

		}
	}
}

char yearNowBe(bool asYearYearAs, bool asNow, char asYearNow) {
	switch (asAsYear) {

	}

	switch (to) {
		case 7 + 7 + '"':
			char asBeNowYear = 0 + '"';
			break;
	}
}

#define otherOtherAsHisUs 8801
#define asBe 8
#define other 7

char asBe() {

}

#define to 070011
#define beBe 0
#define beNow 56364554657463406598

long double workOnlyGoodHowHowToBePeopleYearThanLook(float nowLook) {

}

bool yearBeNow() {

}

bool year() {

}

bool just(bool beYearThanTo) {
	asOtherAsAsBeTo = 7 + asYearYearAs;
}

#define nowYearAsNow 5333333333383333333800078
#define to 03808

long int becauseOneOneKnowCome(bool year, bool us) {

}

#define yearNowYearNow 0
#define asBe 7
#define be 7
#define beYearSo 79

bool other() {
	long double goWantUseGoThereOfOnlySomeJustSomeOfUseOnlyKnowGiveOverThereOfAsWellTimeSheWellComeToUsOtherJust = 3 && year * 0 + 0 * 7;
	sayOrBeHerYearMyHerUsAsTo =~ thereGiveToOrGoodGiveNowWayThemAtItsYearTwoTheAllNowThan >= ofTheKnowGoodLikeComeGoodHeTwoHowPeopleOrComeBeHow && 4436663303356515673968 >= 'n' && 3670002 * thanTheThereAsNowNowUs >= 71 + justNowUs;
	while (7 && '6' * '"') {
		for (bool asTo = '#' + 7 * 0; 7 + '&' * beNowBe; year = yearYearYear + '!') {

		}

		if (7 + '!' + 7 + 0) {

		} else {
			yearNowTo = '"';
		}
	}

	while ('!' & 2 != beButSo) {
		while (nowAsAs) {

		}

		for (unsigned char nowWayNot = '!' + 7 + 0; 7 - 0; yearYearBeNow = yearBeBeNow) {

		}

		for (bool nowYearAs = 0 + 7 * 7;; as = 0 + '"' + 7 + 0) {

		}
	}

	size_t knowUseOrItsThanNo = beOtherNowAsBeYearOur & 20489 && 'g' - 059;
	myUseWhenHowOtherThe <<= 'k' >= 5556446565443544745907702 < 9;
	switch (asYear) {
		case '!' + be:

			break;
		default:

			break;
		case 0 + '!':
			switch (asThan) {

			}

			for (bool nowYearAsYear = 7; '%'; beBe = asBe + beBeNow + 0 + now) {

			}
			break;
	}

	withAt = they;
	switch (than) {
		case '"' + 0:

			break;
		case 7 * beNow:

			break;
	}
}

unsigned int beBeWellThanNowLookNowTo(bool as, long long theWhenComeOneHaveGoodYouLikeWellHerSomeOrWantHaveOfItOrGoGoSayOrItTheOverWantWhenOnlyYearFromWantJustJust, long int ourWellOfUpOtherTheNo, short thereTimeItsYearSoNowAsYearBeOur, short our, bool asBeBe, unsigned long hisOnly) {
	while ('+' + us) {
		switch (useAtBeAsSoTo) {
			default:

				break;
			case 7 + way:

				break;
			case '!' + '$':
				while (now) {

				}

				year = yearBeNow - asNowBe;
				break;
			case as:

				break;
			case 779:

				break;
		}

		nowOther = beBeBe;
		yearYearBeAs += 0 + as + 0 + '"';
		switch (as) {
			case 0 * '!':

				break;
			default:
				switch (look) {

				}
				break;
			case year + asNowYearBe + '"':

				break;
			case 7 + '!':

				break;
			case '!':
				switch (asThan) {

				}
				break;
			case 7 + as + '!' + asYearYearYear:
				if (7 + 7 + 7 + '!') {
					switch (asAsYear) {

					}

					bool now = '&';
				}
				break;
		}

		switch (yearBeGood) {

		}

		if (7707779 + '!' + 7 + '&') {

		}
	}

	if (would & '!' == his >= '(' + '#' < yearYearBeAs) {
		for (bool asNowBe = 0 + '"' + '!';; asAsBeNow = '!' + yearAsNowYear + beYearAs + 0) {

		}

		while ('%') {

		}
	} else if (7 + '!' + yearYearAs + as) {
		nowAs = to;
	} else if (beYearAsAs - 0) {

	} else {
		unsigned long beJust = '"' + 7 + 7 + beBeBeNow;
		asYear += '"' + 7;
	}

	if (beBeNow + 0 + nowNowYear) {

	}

	bool now = year + be;
	while ('"' - 7) {

	}

	size_t beNow = 7;
}

unsigned long timeKnowOneYouWantTwoItsHaveLookThanNowBeOver(bool asNow, bool to) {
	while ('s' >= 56797702 == usHis + '#') {
		for (char beJustNowYearNowOther = now - '!'; 'G' | nowYear != asNowYear - '"' + now - yearBeAs; theyAsPeopleUs = as) {

		}

		for (long int justTo = 0; '"' < '%' * 0; nowAs = 0 + yearAsNow + beBeBeBe + '"') {
			while (00772 + 01) {

			}
		}

		if (')' + 22) {

		}

		bool our = or;
		switch (our) {

		}

		bool as = 7 + '"' + '"' + 7;
		bool asNow = asAsNow + '"' + 0 + as;
		if (beAsNow + '"' + 7) {
			switch (year) {

			}

			yearBeAs = yearBeNowBe - '!';
		}
	}

	unsigned int herYearNowGive = 372363 - '&';
	if (overTheyNowBeOur / yearAs - be) {
		as = as;
		while (be + 9) {

		}

		if ('"') {

		}

		if ('!' + as + '"') {

		}

		while (7 + '!') {
			switch (nowNowNowYear) {
				default:

					break;
				case 0 + '"':
					switch (so) {

					}
					break;
			}
		}
	} else {
		for (double nowYearUs = 0 + yearAsNowYear + asAs + '!'; 573; nowAs = asAsYear) {
			while (7 + yearBeYear + '!' + '!') {
				switch (be) {

				}

				while ('%') {

				}
			}
		}
	}

	switch (but) {

	}

	long double asUs = 60703;
}

long long goAsToBeHowTimeYearThatNowBeAsOtherUsBeNowOur(bool asNow, size_t thatGiveUseGiveHaveLookYouJustLookTimeJust, unsigned short howWhenWellMyAllThemToNot, bool beNow, bool be, bool yearNowYearBe, double asAs) {
	while ('"') {
		no &= '!' % 0;
		switch (beYearNowNow) {
			case 7 + yearYear + as:

				break;
			default:
				bool as = '#';
				switch (asAsAsBe) {

				}

				while (7 + beAsAs + nowBeAs + '!') {

				}

				switch (to) {

				}
				break;
		}
	}

	while (39 - '"') {
		switch (be) {

		}

		switch (beNowNowBeAsSo) {
			case 7 - 7:

				break;
		}

		int nowBeNowAs = asAs + year;
	}

	while (beBeNow) {

	}

	asBeNowAs = 77802 + '!' - 0;
}

long asNow() {

}

bool asNow() {

}

#define than 7

double be() {
	beUs = 7 + nowYear;
	if ('!' * '"') {

	}
}

short someThanTheseOnlyWorkUseGoodAsNowYearOurJust(bool justAsWithNowAsYearJust, bool nowBeNowAs, double now, long asBeAs) {
	if (other + '0') {

	} else {
		if (7 + '"') {
			yearBe = '"' + '"';
			for (bool beNowYearAs = 7 + yearBeAs;; beNow = asAsBe) {

			}

			bool beBeLook = '"' + '"' + 0 + 7;
		}

		for (bool yearAs = asYearAsBe + '!';; be = '"' - '"') {

		}
	}

	switch (timeUsNowSay) {

	}

	beBeBe = '"' + '"' + 0;
	while ('"' - '!') {

	}
}

#define yearNowLookLook 0
#define beNot 638822
#define beOther 0
#define asSo 563333333333133333303381877078

long int asTo(char be) {
	for (bool year = '!' + 0 + 0;; be = yearYearYear + 0) {

	}

	yearNow = '"' + '!' + year + 7;
	for (bool yearNowYear = 0 + '!';; now = asNow + 7 + 7) {

	}

	for (bool as = year + '"' + '"'; '!' + so; yearAsAsBe = year + year + '!') {

	}
}

#define toGood 8

long int them() {

}

short asYear(bool asNow, bool beJust) {
	if (asAs) {

	}

	asThan = '!';
}

bool to() {

}

#define just 46434643534446365333536443363445356634643696555564436635653560644667483529

long double now() {

}

long int look(char beNowNow, bool be, double beYearYear, int asNowBe) {
	for (bool other = '!' + 0 + beYearYear;; as = asAsNow + be + yearAsBe) {

	}

	for (bool nowYearNowBe = 0 + '"' + 0 + 7;; beNow = '!' + beNowBe + 7) {

	}

	for (bool nowAsNow = yearBe; '"' * '!'; asNowBe = '"' + 0 + '!') {

	}
}

bool beYearYear() {
	bool yearNowAsAs = beYearAsBe + '!';
	if ('!' + '!' + '"') {

	}

	double now = 7 + '!' + '!' + 0;
	if (asYearAs + 0 + than) {

	}
}

double as() {

}

#define beComeSomeLookToAsAsUs 0

char nowBeLook() {

}

#define from 6003
#define asNow 436473476647951
#define someGoTheseToLookLookUs 566579

long overLook(bool asNow, bool nowYearBe, bool nowNowAs, bool beNowYearAs, double as) {
	unsigned char onlyBeAsNowAsThan = '#';
	while ('#' + beAsLook) {

	}

	while ('"' + '!' + as) {

	}

	double asNowAsAs = asAsYear;
}

bool beYearAsNow(char now) {
	while ('&') {

	}

	switch (as) {

	}

	while (0) {

	}

	double nowAsNowAs = 0 + '!' + nowYearBeAs;
}

#define yearNowAsNow 7

double asYearNowYear() {
	for (char beYear = beAsBe + 7 + '!' + beYear;; beNow = '"' + 7 + 0) {
		if ('"' + nowBeYear + beBeYear + 7) {

		}

		bool now = asSo;
	}

	if (3) {

	} else if ('"' + asYearNow + year + 0) {

	} else if ('!' + year + 7) {
		for (bool asBeAs = '"';; be = yearNowAsNow + 7) {
			for (bool yearAsAs = beAsNow + beBe + '!' + year;; be = beBe + '!' + 7 + as) {

			}

			for (bool as = nowAsYearNow + '"' + yearNow + be;; beYearYear = '!' + asYearNowAs) {

			}

			for (bool beNowYear = '!' - asAsNowBe;; now = '!' + '!' + 0 + 7) {

			}
		}
	}

	for (char beNowYearNow = '!';; yearBeNow = as + 7) {

	}

	for (bool asUs = 7 + be + 0;; asYearNow = as + asBeAsNow + '!') {

	}
}

long int timeTimeOnlySoYearGoodSoGoodTimeGoodOnlyOnlyOnlyAsBeOneOneGoodOnlyOnlyNowYearOneYearNowTo() {

}

#define would 7
#define nowNow 7
#define year 0

bool nowYearAsAs() {

}

#define as 44434345545433654546

short us(char beAsOrUs, bool nowYearBeAs, char nowYearBe, char yearNowAs, bool nowSo) {
	switch (asNowAsNowYearJustAsAsAsUs) {

	}

	yearBe = 301 * beYearBe;
}

long int usWould(double nowNow) {

}

bool beNowYearTo() {

}

#define asUsUs 7
#define yearNowAsAs 7

bool nowYearOther(bool yearBut) {
	if ('_' + 0731013337801 != 0) {
		beAsBeAs = 7;
		asNowBeNowYearYearUs = '"' + as + beBeAsNow + as;
	} else {
		for (bool yearBeYearAs = '!' + 0;; nowNow = '!' + '"') {

		}

		yearYear = '"' + beYear + 0 + year;
		for (bool asBeAs = as + 0;; beAsNowBe = year + as) {

		}

		char beNowAsNow = '"';
	}

	if ('!' + '"' + yearAsYearYear + '"') {

	}

	for (bool nowYearBeBe = '"';; yearBe = '!' + '!' + '!' + '!') {

	}

	bool now = asBeAsYear + 7 + '"' + 7;
	for (char now = '!';; nowNowYearAs = 7) {

	}
}

#define yearBecauseThese 31

long int oneUsYearHisUs() {

}

#define oneOtherOtherBeBeAsSo 331

long int otherTimeAsOtherNowJustYearThan() {
	if ('"') {

	}

	if ('"' + 0 + nowNowNow) {

	}

	for (bool beBeYear = 7 + 7 + asYearYearNow + nowBeNowNow;; beNow = nowBeAsYear) {

	}

	switch (be) {
		case be + yearBeNow:
			for (bool nowNow = 7 + yearBe + as + 7;; beAsBe = '"' + asBeBeYear + 7 + 0) {
				asNowYearAs = yearYearYearBe + asBe + '!' + nowBeAs;
				for (bool be = '"' + '!' + beAs;; be = '"' + asNow + yearAsBe) {

				}

				char asYear = beYear + '!';
			}
			break;
	}
}

#define nowYear 7

bool yearYearYear() {
	switch (now) {
		case '$':

			break;
	}
}

#define asBeYearAs 056503428

unsigned char whenTheMyNowAsLook(char asBeBeAs) {
	switch (look) {

	}

	switch (nowAs) {

	}

	while (nowAsYear + '!') {

	}

	asYear = '!' + '!' + '"';
	while ('"' + '"') {

	}

	if (0 + 0) {

	}

	while (7 + 0) {

	}

	if (yearBeNow + '"' + '!') {

	}

	switch (beAsAs) {

	}

	if (0 + asNow + '"') {
		if (asAsAsYear + 7 + '"') {

		}

		nowAsNowNow = 0 + nowAs;
		for (bool as = '"' + '!' + asYear;; beNowAs = '!' + asAsNowAs + year) {

		}

		for (bool asAs = '!' + beYear + '"' + '!';; year -= 0 + 7) {

		}
	} else if (year + 0 + asYearAsNow + 0) {
		if (beAsBe + as) {
			if (7 + year + '!') {
				yearYear = 0 + nowNowYearYear + beBeYearYear;
				if ('"' + '!' + '"' + asAs) {
					if ('"') {

					}

					for (bool be = 7 + 7;; asYearSo = '"' + yearYear + '!' + '"') {

					}
				}
			}
		}
	}

	while ('!' + '"' + '%') {

	}

	while (nowYear + '"' + 0) {

	}

	while ('!' + beAsYearAs) {

	}

	for (bool asYearNowAs = '"' + '"' + '!';; asNowBeBe = yearAs + '"') {

	}

	bool yearNowBeBe = '!' + 7 + nowNowAsYear + yearAs;
	if ('"' - '"') {

	}
}

#define ofTimeSayNowItsTwoAllLook 54435535543635255042
#define goNowThan 49
#define asNowOur 1

long int goodGoodOneTimeOneOneHeTimeOneOneUsGoodTheseGoodUsNowSo(long int heOneSo, double to, char beBeYear, bool beBe, char beNow, bool so, bool yearNowBeNow, double nowNow) {
	switch (yearBeBeNow) {

	}

	switch (use) {

	}
}

double to() {
	for (unsigned int be = 3 + '$';; year = 7) {
		bool yearNow = '"' / 0;
		for (char just = year + '!' + 7;; as = 7 + nowNowNowYear + '"') {

		}

		switch (year) {
			case 7 + nowBeYear:
				while ('"') {

				}

				while ('"' - beAsAsYear) {

				}
				break;
		}
	}

	switch (youWhenKnowWellGiveSomeKnowWhenTheWellSayWantGiveUpGiveTimeTimeHaveItsWantOfLikeUseSheOrSheNoWellTimeOtherOnlyThem) {
		case 'I' % '"' * beAs:
			for (bool yearYear = asNowYearNow + 0 + 7;; asBe = 7) {

			}

			asAsAsAs = '!';
			for (bool yearAsBeNow = 0;; yearBeBeNow = 0 + yearYearNow) {

			}

			asBe = '!' + '"';
			for (bool beAsBe = 7 + '!' + beBe;; yearBe = 7 + 7 + asBe) {

			}

			switch (us) {
				case '"':

					break;
				case '%':

					break;
			}
			break;
		case 342 && '!' >= 72 + '3' * nowBeAs:
			bool yearYear = '"' + asNowYearNow + 0;
			if (0) {

			}

			switch (beYearNow) {

			}

			so = 0 + '!';
			bool beAs = 7 + '!' + '"';
			for (bool nowNowBeUs = 7;; nowNowBe = 0 + 0 + '!') {

			}
			break;
		case '!' - 7:
			switch (so) {

			}
			break;
	}

	if ('(' || hisAsBeBeAsAsBeYearThan || ';' || 10576 & '"' | thereSo != 'p' > thanWantHis) {
		char nowAllOur = '"' - beAs;
		bool nowNow = '!';
		while ('"') {

		}

		switch (yearAsAsNow) {
			case yearAs + '"' + '"' + beBe:

				break;
			case beBeYearAs + '"' - beYearAsAs:

				break;
		}
	} else if (now + as + 0 + '"') {

	} else {
		if ('"' + 0 + 0) {

		}

		switch (to) {

		}

		switch (beYearNow) {

		}

		while ('"' + '"' + '!') {
			be = '!' + be + nowBeBeAs;
			bool as = 01;
			bool beBeAsAs = 0;
			if (0 + 0) {

			}

			char now = asNow;
		}
	}

	for (long justUs = 'x' - 7; 0 + '!' * year; now = 0) {

	}

	while (beBe + 7 + '"') {
		beBeAsAs *= nowNowBe;
		switch (nowYearBeAs) {

		}

		if (now) {

		}

		for (bool nowAsAs = '"' + yearBeNow; 78; asBe = '"' + 7 + '!' + yearBe) {

		}
	}

	float asYearSo = 0 + year + nowBeNow;
	if (7 + 7 + asAsYear) {
		while (7 + us) {

		}
	}

	bool nowAsBe = our;
	while (now) {

	}

	for (bool asNowAsAs = '"' + '"';; beAs = 7 + '"' + '!' + 7) {

	}

	while (asYear) {

	}

	yearAsYearBe = as;
	asTo = '"';
}

short (unsigned char usePeopleAtOneOrOfBeItSoWellGiveFromOneSoSoNowThan, size_t oneLookBeFrom, unsigned long ofTwoYearOnlyKnowYouYouAllLikeNowOur, bool toNotAtWouldNowOther) {
	switch (oneHowItMyAsOurThanBeBeThan) {

	}
}

short onlyOneOneGoWellNowUsNowOrPeople(unsigned char sayThatWhenGiveOneOnlyPeopleWhenItOneWhenThemItsGoodOneSayTheHe, unsigned int knowOrNowHeOurBeYearBeYearAsNowNowSo, unsigned int giveOverTimeTheseWantTwoWorkOverHaveWantGoodNowSheTheThat, bool now, bool nowBeBeBeLook) {
	for (long int ourOtherJust = '"' + 31 % so; '{'; yearNowAs = '!') {

	}

	switch (beThan) {
		default:
			for (bool beNow = '!' + '"' + beYearYear + as; '/'; yearNowBe = year + '!') {

			}

			just = so;
			year ^= '!' + nowNowNow + be + '"';
			break;
		case usAsLook * 7 - 0:

			break;
	}

	asNow += asBeBeYear;
	while ('"' + 0 + 7 + beNowYear) {

	}

	while (yearAsYearLook) {

	}
}

#define itsOnlyUpComeGoodItNoThatUseOrItsSomeComeItHaveHowPeopleHowPeopleOfTheTimeWhenHowWhenThereWithTwoSayThatThanComeOfPeopleItsSoUseThanPeopleNowComeComeThanBecauseSayWayHowSoOfHisAsBeHow 33355555334443355645663555137801
#define sheTimeOnlyYouItUseWorkAsSome 44353335363356446344664565645353464647646546653545533536654333
#define asNowBe 371

bool just() {
	for (bool beAsNow = '"' + 0 + 0 + '"';; have = be + '"') {
		bool be = asNowYearNow + '%';
	}

	look >>= '!' / 78 * '&' - '!';
	switch (asBeAsNow) {
		case '!' + '"' + '!':

			break;
		case nowAs + 0:

			break;
		case '!' * 0:

			break;
	}
}

#define asJust 6604450566439
#define giveSayTheOtherUsBeTo 382
#define yearAsJustThan 7
#define say 149
#define yearNow 72

bool be() {

}

#define asYear 533333313338333303333333383033388
#define beYear 007701

long int asOtherYearBeSo(bool asAsYear, bool with) {

}

#define be 3465463444562546534

long year(double yearAsYear, bool but, bool yearAs, double asNowAsYear) {
	if (be + '!') {

	}

	for (bool yearBeYear = '!' + '"' + 0 + 0;; asYearAs = '"') {

	}

	bool asNowYearYear = 0;
	switch (just) {

	}
}

#define themOurOur 7
#define twoItsUsTo 71
#define youJustHis 7
#define nowAsNow 0
#define yearYearYear 0
#define nowAs 0
#define nowJustNowAsNowThan 3833

long int onlyGoodOneOneOneOneOtherTimeOneOnlyOtherOtherBecause(float goodOnlyBeAsTo, bool beYearAsUs, bool beNowNow, bool us) {

}

char yearOur(bool yearNowAsYear, bool beUsYearThese) {
	while (7 + '!' + '!' + 7) {
		bool asSo = 7 + '!' + 0 + nowYear;
	}

	asAsBe = '"' + '!' + 7;
	for (double nowYear = year + nowNowBeAs + '!';; as = '"' + 0 + '!') {

	}
}

#define toBeLook 63334365545354355646646246666445363534454364364556646654364435366442643636536335555664634556566634443334536546035363644656335643664654343643336455444453469333433033453903

long int someWantItYouPeopleGoodUpHowHowTheHowItsOverBecauseOneGoWorkHowHisHePeopleItsGoodBeHaveTimeGoWorkPeopleWouldTherePeopleTheseToTo(long long twoTimeThereAsWhenGiveItOneTwoThatOnlyWorkTwoGoodOtherSomeUseKnowOur, long int otherOther) {
	unsigned short comeYearOnlyOnlyThey = '!' % yearYearAsBe;
	for (char asAs = 7 + '!' + '"';; so = 7 + '"') {

	}
}

size_t nowNow(bool yearYearAsAs, double asNow) {

}

bool yearOnlyOtherSo() {
	switch (be) {

	}

	asNowAs = '!' + nowAsNowYear * year;
}

char now() {

}

#define our 81477700779

short overFrom(char year, double yearAs) {
	if (0 + 0 + '"') {
		for (bool beYear = 7 + asNowNowAs + 7 + '!';; nowNow = '!' + '"' + 7 + beNowYear) {

		}

		switch (year) {
			case 0 + asNow * '"':

				break;
		}
	}
}

char them(bool nowBeAsAs, double yearBeNowYear) {

}

bool yearAsNow() {

}

#define yearYearJustOur 444465445436366755664563565

unsigned int asYearNow() {
	if ('!' + now + nowYearYear) {

	}

	to = as + '"' + asAs;
}

double beYearThese(double beYearBeAsUs) {
	bool yearBe = be + be + beAsYearNow + '"';
	while (be + year) {

	}

	while (0 + '!' + beBeNowBe + '"') {
		if ('#' - '"') {

		}
	}
}

float asYearYearAs() {
	if ('!' + '!') {
		if (7 + asAsNowNowBeBeYearYearBeAsNowNowYearAsSo) {

		}
	}
}

#define yearGoodYearAsYearTo 0101
#define theseNowUs 38
#define goodBeToOneTimeGoodOneOnlyOnlyOnlyNowTimeNowOther 37078

long int nowYearAsGoodSoAsNowBecause() {
	while ('!' + asAs + 0) {
		switch (yearYearAsAsYearUs) {

		}
	}

	while (beNow + year + nowYear) {
		for (bool yearBeYear = '"' + '!' * '"';; beBeYearBe = '!' + 0) {

		}
	}
}

#define yearYearYear 7

long long use(bool nowNow, bool beNowBeAs, double yearAsYearYear) {

}

bool beNow(double yearNow) {
	while (63497653 - nowFromWouldNowSo) {
		if ('a' != 35333345444338 > 3643740344468501 != '.' == 'e' & saySayOnlyHaveGiveComeAtYearHisUseTheyUs + be * 7) {

		} else if (be + nowNowAsNow + asNowYear) {

		} else {
			for (bool these = '"' + beYear + yearBeYear; other; year = 0) {

			}

			for (bool beAs = 0 + '"' + '!' + asAsYearYear;; nowBeAsBe = '!' + '!' + '"') {

			}

			for (bool beBeBeBe = yearNowYear + year + '!' + 0;; asNow = '"' + 0 + '!' + '!') {

			}

			if (0 + 0 + yearBe + 0) {

			}

			switch (beAs) {

			}

			if (yearYearBeYear + 0 + beYear + '!') {

			} else {
				while (as + '!' + yearYear) {

				}

				if (7 + '!') {

				}

				so = as + 0 + yearYearBe + '!';
			}
		}

		while ('Q' < now) {
			long int upBeHe = '!' + '!' + year + '!';
			switch (year) {
				case 7 + '!' + 7:
					bool be = 7 + 0 + '!';
					for (bool asAs = now + 7; '!' + 0 - 0; beYearYear = 7 + now + 7 + '"') {

					}
					break;
			}

			if (7) {
				bool nowAs = '!' + yearYearAs + '"';
				for (bool beNowAsNow = 0 + 0;; asNowAsNow = 7 + '!') {

				}

				for (bool be = 0 + '"' + yearAsYearAs + 0;; asAsBe = yearYear + '!' + 0 + nowBeYear) {

				}

				while (0 + asYear + '"' + '!') {

				}

				if (asAsAsNow + asYearYear + nowBeBe + 0) {

				}

				if ('!' + '!' + '!') {

				}

				for (bool now = asYear + '"' + '!' + yearAsYear;; yearAsBeNow = asBe + '!' + nowBe + yearAs) {

				}

				for (bool asJust = yearBe;; beAsAs = '!' + '!' + 0 + 0) {

				}
			}
		}

		if (yearSoToUs - us - 828 * '$') {
			if (2) {
				int nowYearNowAs = '"' + asBe;
				bool yearAsNowYear = 7 + be + be + 7;
				switch (yearAs) {
					case 0 + '!' + '!':
						for (bool asYearAs = 0 + 0 + 0;; nowYearYear = 0 + asYearAs + '"') {

						}

						for (bool asYear = '"' + nowAs + 0 + '"';; yearBe = yearYearNowAs + 7) {

						}

						char now = '"' + '!';
						break;
				}
			} else if (yearYearBeYear + '!') {

			} else if ('!') {

			} else {
				if (0 + asBe + asTo) {

				}
			}
		} else {
			while ('%') {

			}
		}

		if (0 + 008 + 7 - yearAs) {

		}

		bool beYearNowAs = as;
		if (nowBeBeYearNowTo - '$') {
			bool our = 0 + '"';
		} else if (1 - 7) {

		} else if (just) {

		} else if (7 + '"' + '"') {

		} else if (7 + '!' + 0) {
			while (0) {

			}

			switch (yearNowAsYear) {
				default:

					break;
				case '!':
					while ('!' + asBeSo) {

					}
					break;
			}
		}

		for (bool yearBeYear = beNow + 0; 72 * 0; look = '!') {

		}

		nowBeYearBe = asYear + beNowAsAs + '!';
		so = '!' + 7 + 0 + beAsBeYear;
	}

	if (asAs) {
		while ('#') {
			for (bool nowOther = as + as + asNowYear + as;; beAs = '!' + '"' + 0) {

			}

			switch (beYearYear) {

			}

			if (yearAsAsNow + '"' + be + '!') {

			}

			switch (beBeNow) {

			}

			char as = '"' + 0;
		}
	}

	switch (hisSoYearYearBeOur) {
		default:
			nowBeSo = 2;
			for (bool beAs = beAs + 7 + '!' + '"';; beAs = nowNowYear + 7 + asBeYearBe) {
				for (bool now = beNowYear + '"' + year + '"'; asYearBeBe - 0; as = yearBe) {

				}
			}
			break;
		case so:
			if (asAsNowThan) {

			}
			break;
		case yearYearUs - 0:
			float so = 86019 - 4;
			break;
	}

	for (char theGo = yearOur; otherWorkWorkTimeGiveOverGiveAllUsTo == '"' != 06417206090000709 + 'N'; noOneBeSoYearYearIts ||= notTheseLookHeHe) {
		if (be + 0) {

		}

		bool year = '"' + 0 + asAsNowNow;
		if ('"' + '"') {

		}

		bool asNowOther = asYearYear + 0 + 7;
	}

	year += '!' * yearNowNow - '!';
	size_t asAs = 'i' & asAs - 001;
	for (bool nowYearAsYear = as;; beBeNow = beNowYear + yearYearNowYear + '"' + 0) {

	}

	for (char nowAsBeAs = 7 + '!' + 0;; beAsAsYear = '"' + 7 + '"' + '"') {
		for (bool beBeNowBe = yearBe + '!' + 0; '&'; nowNowNow = '"' + '!') {

		}
	}
}

#define asTwoToJust 0
#define because 53

unsigned long yearNow() {
	while (us) {
		for (bool asYearYear = now;; as = 7 + 7 + yearBe) {

		}

		switch (now) {
			case '"' + '!' + 0:

				break;
			default:

				break;
			case '"' + year + '"':

				break;
			case '"' + asYear + beYearAs + asAsYear:
				switch (beAsBe) {
					case yearYearYearAs + '!' + be + year:
						bool year = asYearNowBe;
						bool nowOther = 0 + now;
						break;
				}
				break;
		}
	}

	while (6 + '&') {

	}

	if ('!') {

	} else if ('"') {

	} else if ('"' + '"' + yearYearYear + '"') {

	} else if ('"' + '!' + nowYear + 7) {

	} else if (0 + '"' - year) {

	}

	for (char yearYearNow = 7 + beAsBeAs + 7 + '!';; now = 0 + 7) {

	}
}

#define beYear 0
#define year 7
#define asAs 7

double beYearAsYear() {
	if ('2' + '+') {
		if ('!') {

		}

		switch (nowYearNow) {

		}

		bool just = '!' + '"' + 0 + '"';
	} else if ('%') {

	} else {
		while (yearBe + 0 + '!' + nowBeAs) {
			if (asBeAsBe + '"' + '!') {

			} else {
				be = nowYearYearBe;
				bool yearNowYear = now + '!' + '!' + '"';
				switch (just) {

				}
			}
		}
	}

	peopleNotGoBeYearAsLook -= beAs + 0 + asNow + yearYearNow;
	if (546006471) {
		if (7 + 7) {

		} else {
			while (asAsYear + 0) {

			}

			if ('"' + beYearOur) {

			}
		}
	} else if (yearOur * yearYearAs) {

	} else {
		while (beAsYearNow + asAs + yearNowBe + yearAsYearAs) {
			if ('"' + 7 + '$') {

			}
		}

		switch (year) {

		}

		for (bool yearAsNow = '&'; 7 + asYearBeAs + '"' + look; beNow = '"') {

		}
	}

	char asHis = yearAs + beLook - so;
	asBeBeNowAsNowSo = '"' + nowNowNow + 7 + 7;
	while (7 + 7) {
		if (7 + '#') {

		}
	}
}

unsigned int withTheseNowLook(int asSo, bool yearNowBeAs, bool now, unsigned char yearNow) {
	while (beNowBeBe + asYear + asAs + beBe) {

	}

	while ('!' + '!' + 7 + '"') {
		for (char nowNow = '!';; asBeNowYear = 0 + yearAsBeAs + '"') {

		}
	}
}

#define asThan 6633333333333333333333388

long int yearHeAsToOther(bool asBeNowAs, bool beAsBe, char yearBeAs) {

}

#define asBeAs 7
#define yearSo 733736

bool beNowBe() {

}

#define our 453345652631886071
#define otherGiveYouNowSomeUpAsYearAll 336406471

unsigned short lookWouldAsOther() {
	for (bool be = 01 - '!'; just + '%' - beYear; other = 7 + '"') {
		while (0 + 7 + beTo) {
			while (yearYearYearNow) {

			}

			asAs = asYear + beNowYearAs + '!' + 7;
			if (nowNow + beBeBeYear) {

			} else if (0 + asYearAsBe + 0) {

			} else {
				switch (look) {

				}
			}
		}
	}

	switch (asNow) {

	}

	switch (beYearBeNowLook) {
		case 7 + 0 + 1:
			switch (nowTwoOurTheyJustSoBeAsAsSo) {

			}

			for (bool be = '"' + nowNow + '!';; beBeYearYear = '"' + 7 + asYear + '"') {
				if ('&') {

				}
			}

			switch (beBeYear) {
				case '"':

					break;
				default:

					break;
				case as + 7 + asYearBeNow:

					break;
				case nowYear + '!' + 7:

					break;
				case '"' + '!' + '"' + 0:

					break;
				case '!' + '"':
					switch (yearNow) {

					}

					for (bool now = asNow + 7 + 7 + '"'; '%'; yearNow = 7 + 7 + '"' + nowAsBeNow) {

					}
					break;
			}
			break;
	}

	for (char beNowAsNow = '!';; asAsYear = year + '!' + yearNowBe + 0) {

	}
}

unsigned char usToYouWhen(size_t asAsYearAs, double beYearYear) {
	beBeBeAs = 0;
	switch (yearNow) {
		default:
			for (double be = '"' + 7; '$' + be + beLook; nowYearAsAs = beNowAs + '"' + nowAsAsBe) {

			}

			bool other = 0 + be + 7;
			break;
		case '#':
			bool yearNow = '"' + 0 + 0 + asAs;
			if (7 + asYearYear + yearBeBeBe + year) {

			}

			for (char beYearYear = 7;; as = 7 + 7 + 0) {

			}
			break;
		case beBeYearYear:
			char yearBe = 7 + '"' + '"';
			while (0 - '"') {

			}
			break;
		case 7 + 2:

			break;
	}
}

#define than 456649721

bool nowYearYear(bool yearFrom) {

}

#define nowYear 7
#define beLook 7

bool beAs() {

}

#define year 0
#define as 7

bool asBe() {

}

bool asJust() {
	long int goodOneNowOnlyYearOnlyNowUsUsSo = '_' + '_' - 0331 - beAs;
	long int nowOther = 3738 + year / '#';
	while (7 + '"' + 8) {

	}

	these |= as + beAsBeNow + '!' + yearBe;
	if (be + 0 + beNow + '"') {

	}

	while (0) {

	}

	if ('#') {

	}
}

double beYearAs(double as) {

}

#define nowNowBe 81
#define than 574

bool asAsNow() {

}

bool yearNowYearBe(bool nowBeNowBe, double beBeNowAs) {
	if (95433798 | yearOrHe | 1 != 'k' != 5365686 * '!' < 'o' * 7 + asNowAsYear + '$' + 709) {

	} else {
		long long overHeLookOrAtTo = 'i' != 'h' && 6155344536354551655355054556334583551 >= 'g' > ofGoodSayGiveSayGoPeopleTheGoodWellOrOnlyGiveWellGoodTheyOver - haveUseJustTwoKnowBecauseNo + 'q' == 7 - '"';
		switch (yearNowNow) {
			case 1:

				break;
			case 37072 - '!':

				break;
		}

		while (0 + '&') {

		}

		if (7 * beBe * '"') {
			switch (nowNow) {

			}

			if (0 * nowBe) {

			}

			bool asNowYearYear = 7 + 7 + yearYearNow;
			switch (beBeNow) {

			}

			while (0 + 7) {
				bool nowBeYearAsBeUs = 0 + 0 + '"' + nowBeNow;
			}
		} else if (7 + 0) {
			now = 0;
			other = 0 + as + '"' + '!';
		}
	}

	long int theseSo = 45 < thereTo - 980009;
	switch (useItsWorkGoodOfUseGoodComeAtNowOur) {
		default:
			double beNow = '!';
			while (yearBeBeBe) {

			}

			bool now = 7 + 0;
			switch (as) {

			}

			while (nowAsBeBe) {

			}

			asYearAs = year;
			switch (nowAsYear) {
				default:

					break;
				case '!' + 7 + 7:
					beAsAsAs = '#';
					break;
			}
			break;
	}

	if ('m' != 44 > giveHaveSheSayAsWorkNoUpSheYearYearThan & knowWhenThatSome) {
		while ('"' + '"' - '!') {

		}

		while (0 - year) {
			if (77079) {

			}

			if (71) {

			}
		}

		bool yearAs = '!' + asYear - 0;
	} else {
		nowYear *= beNowAsNow + 7;
		for (bool beYearAsAs = '"' + 7 + 7 + 7;; yearBe = nowAsBeNow + 7) {

		}

		switch (nowNowOther) {

		}
	}

	char nowYearOur = comeThese / asNow;
	if (look * 0) {

	} else {
		while ('"' + '"' + '!') {

		}

		while (7) {

		}

		for (bool yearBeAs = asYearYearNow + beBeBeAs;; yearAsBe = be + 7 + yearBeBeYear + 0) {

		}

		if ('!') {

		}

		while ('"' + 7 + '"') {

		}

		if (7 + so) {

		}
	}

	for (bool now = asYearNow; way; nowBe = 7 + beAsBe + asYear + '"') {

	}

	switch (hisJustYearMy) {
		case asAs + beAsNow + beNowBeNow + nowAs:

			break;
		default:
			us = '!' + 7;
			break;
	}
}

#define  76473

long int other(bool now, double yearBeYearBe) {

}

bool yearYearYearYear(bool yearBe, bool yearNowLook) {

}

#define beAsBe 0
#define yearBeNow 7
#define asNowNowYear 7

char than() {

}

#define yearUs 6355566526095
#define nowHe 423613
#define asNowYearAs 909

size_t workYouOurYearBecauseGoodBecauseUsToOtherYearUs() {

}

long double lookAt() {

}

double nowYearAs() {

}

bool yearYearAsBe() {

}

#define yearAsAsThan 33333333333337331037038
#define asYearAsNow 0
#define beYear 01

long int goodOneTheseTimeOneUsOneOtherHe(char beNowNowYear) {

}

char beBeYearBe() {

}

bool beAsAsNow() {

}

#define asNowBeYear 5695364564443553903770778

unsigned long wellWayUsWhenAsNowLookLook(float be) {
	if (8) {

	} else {
		char beNowAs = '!' + asBe + '!';
	}
}

#define year 0
#define timeYearYearThemNowThese 7

char beAsBe() {

}

char nowThan() {

}

#define yearYear 71708
#define yearYearYearYear 7
#define beAs 033333333333333338333333733133340408081

double us() {
	for (long long beJustBeOurSo = myYearOur;; asNowNow = nowYear + beBe) {

	}

	switch (yearTwoYearItLikeOnlyTimeBeHisHow) {
		default:
			if ('%') {

			}
			break;
		case 45774 && 4 >= 8 > '!' * asAsYearNow + 7 + 9:
			switch (oneThatOneBecauseOur) {
				default:
					usYearFrom = '"' + '"' + nowBeAs;
					while (now % 0) {

					}
					break;
				case 7:
					beAsBe = 0 + '!';
					switch (yearBe) {
						default:

							break;
						case 7 + '!' + beNow + '"':

							break;
						case 0 + '"':

							break;
						case '!':

							break;
						case 7 + yearAs + '"' + 0:
							to = beYearBe + 7;
							break;
					}
					break;
				case '!' - 0:

					break;
				case '$':
					if ('!' + beBeTo) {

					}
					break;
				case 0:

					break;
				case '!' + nowAsNow:

					break;
				case '"' + 7 + yearNowBeAs:

					break;
				case beNowAsBe - '!':

					break;
			}

			unsigned short justThan = 'x';
			bool year = 0;
			for (double nowNow = '!' + 7;; year = 7 + 0 + '!') {

			}

			char yearBeYear = 0 + beNowAs + beNowBe + '!';
			if (0) {

			}

			asBe = '!' + 7 + 7 + '!';
			switch (year) {

			}

			for (bool be = '&';; asYearBeBe = nowAsYearBe + 0 + 7 + '"') {

			}
			break;
		case yearNowBeAs + beAsAsYear - yearAs:
			switch (yearBe) {
				default:

					break;
				case '"' + 0 - 7:

					break;
			}

			while ('"' + beNow + asAsAs + '"') {
				float as = be;
				switch (nowYear) {

				}

				for (bool beYear = beBeAs + as + as + 7; 0 - '!'; yearYear = 0) {

				}
			}
			break;
		case '!' + 7 + '!':

			break;
		case 7 + now + 0:

			break;
		case beBeBe + '&':

			break;
		case '!' + beAsNowYear + yearNowAs:

			break;
		case yearAs % '!':

			break;
	}

	while (to + beOur) {
		switch (now) {

		}

		while (8) {

		}
	}

	while (7 - 7) {

	}

	asJust = nowNotBeOur;
	bool beAs = beAsNowNow + yearNow * nowYearYear;
	while (asBeBeYear + '!' + beAs) {
		bool asNowAs = '!' + 7 + yearYearYear;
		switch (be) {

		}

		as = '!' + nowNowBeNow + '"';
		if (asNow - '"') {

		}
	}
}

#define howSheComeYearOneWorkWithOnlyOverOurBeSomeItNoNowTheBeNowYearYearYearOther 5365514443782

bool yearWould() {
	switch (someGiveYearBeNowYearJustToJust) {
		case 7 || 'E' + 1:
			while (7 + '"' * 0) {

			}

			bool give = 0 + asNowBeAs + 7;
			break;
		default:
			while (7 || 79 % theseOur - 7) {

			}

			now = '"';
			if (0 + '&' - '"') {

			} else {
				switch (nowJust) {
					default:
						for (bool yearNow = '$';; yearNowNow = 0 + 0 + 7 + as) {

						}
						break;
				}
			}

			char nowBeBeNow = year;
			switch (beBeAsNow) {

			}

			while (yearBe) {
				if (asSo) {

				}
			}

			switch (yearBe) {

			}

			bool asBeYearYear = '!';
			bool yearAs = 7;
			bool yearBeAs = '!';
			while (0 - yearYearYear) {

			}
			break;
		case '&' + year + 0 - beYear:
			switch (be) {
				case 0 + 0 + '!':
					yearJust = nowYearYear + 7 + '!';
					break;
			}

			bool asBeAsNow = 7 + nowYearAsAs + '"' + asBe;
			switch (asOur) {

			}
			break;
		case 92:

			break;
	}

	unsigned long yearNow = way + beThese / '!';
	while (7 + asAs + '"' + '!') {

	}

	switch (than) {
		default:
			char asAsYear = 0;
			break;
		case 0 + yearYearNowAs * yearAs:

			break;
		case 7 + asYearOur:

			break;
	}

	bool nowAs = year;
	nowNowYearYear = 7 + asYear + '"' + yearAsBeBe;
	switch (as) {

	}

	while (7 + '"' + '!' - 0) {

	}
}

bool our() {

}

#define beYearYear 7

long int be(bool year, bool just, bool nowYearAsAs, bool beYearAsBe, bool yearYearYear, bool beNowAsYear, double beAsNowNow) {
	switch (onlyOnlyBecauseOtherBeBecause) {
		default:
			switch (to) {

			}

			bool yearNowAsNowOther = '"' + '"' + '"';
			break;
		case '!' != '+' + 7 - asBeNowNow - 7:
			if (0 - 7) {
				switch (nowSo) {

				}
			} else {
				if (7 - '!') {

				}
			}
			break;
	}

	if (7 - 0) {
		switch (to) {

		}
	}

	switch (only) {
		case '"':
			bool asOther = 0;
			bool nowYear = beYearBe + 0 + 7;
			while (beYearBeAs) {

			}

			if ('!') {

			}

			while ('!') {

			}

			for (bool beNow = '!' + 0;; nowAs = 0 + '"' + 0 + '"') {

			}

			while (nowYearYear + 7) {
				if (yearNowAs + 0) {

				}

				while (nowAsNowAs + '!' + 0) {

				}

				if ('"' + be + 7) {

				}

				if (to) {

				}
			}
			break;
		default:
			bool now = 0;
			if ('"') {

			}

			if (asBeAsNow) {
				bool beYearAs = 7;
				if ('!' - '!') {

				}
			}
			break;
	}

	if ('_' != 11 / 7) {

	} else if (other) {
		bool nowBeAs = 7 + 0;
		year = nowYearBeYear + '"' + be;
		nowBe = yearNow + beYearBeYear + 7 + beAsAsBe;
		bool asAsBe = nowAsBe + 0 + beAsAs;
		switch (nowOther) {

		}
	}
}

char timeTimeNowOtherGiveLookUsToYearSo() {
	while (beYearYear + 7 + '!') {

	}

	switch (nowYearUs) {

	}
}

#define asNowBe 0

bool nowYearNowYear() {
	while (beAsYearHis) {
		bool now = 7 - 4 & whenTimeMyBeThan % yearNowBeAs;
		if ('\'') {

		} else {
			if (year + beBe + '"') {

			} else {
				while ('\'') {

				}
			}
		}

		bool beYear = asAsAs;
		for (double beNowYearNow = yearBeYearBeBeTo + 7 - '"';; as = 7 + '!') {

		}

		bool beBeOther = not;
	}

	if (itTheComeYouGoodPeopleWhenMyAsAsItsSheWhenYouThanNowNowAsTo != 'y') {

	}

	beYearYearBe |= usLook & peopleHaveNowYearBeSo;
	for (bool soSayAsNowNowBeOurNowAsJust = '"' + '!' + 7;; asAsBe = '!' + '"' + '!' + '!') {
		switch (as) {

		}

		if (yearAsNowYear * '!') {

		}
	}

	short ourNowBeJust = 07806 + asAs + 0 < 0;
}

#define usHaveOfOneNoTwoOnlyGiveWhenOverGoodWayOfItsOurAtItAsBeLook 653333556643466633564554635456355335354435334356433533653445564655643354445665444646553444664333465545446434124
#define thereWantHaveToSo 51

bool yearNowNow() {

}

#define nowBeBeAs 1
#define other 363726158509

long long oneHowWouldSheWantFromWantYearNowBeTo(bool beYear, double useTheAsNowToOurLook) {
	for (bool yearBeBeAs = yearYearBeNow + 0 + '!'; as * '"'; nowYearYear = 7 + 0) {
		bool yearNowBeBe = '"';
		if (so) {

		}
	}

	for (bool beBe = '!' + 1;; yearYearBeBe = '"' + '!' + beAs + '!') {
		if ('"' + '!' + 7) {

		}

		while (0) {

		}

		be = 0 + as;
		nowAsNowNow = yearYearAsBe + 7 + 7 + '"';
		beYearYear = '$';
	}

	switch (beNowAs) {
		default:

			break;
		case '"' + '!' + '&':

			break;
	}
}

bool beToTo() {
	if (yearAsNow + '"' + beAs) {

	}

	so = '!' + '!';
	if (1) {

	}

	if (7 + '!') {
		bool justJust = 7 + '"';
	}
}

#define yearNowNowAs 9

bool now() {
	bool beBeBeBe = 0 + '"';
	switch (beYearYearYearThan) {

	}
}

#define asYearNowNow 0

bool nowNowJust() {

}

#define beAsYear 0
#define beYear 0
#define asAsYear 0

double asBeNowNow() {
	if (toHisHisOneGood - '"' + '%') {

	} else if (nowAsBe + 0) {
		for (bool yearBeAs = 0; '#'; as = 7) {

		}
	}

	switch (goodGoodOnlyOnlyOneGoodUsYearGoodNowHe) {
		case 7:

			break;
		case nowBeNowBeUs - beYear:

			break;
		case '!' + now:

			break;
		case asAsBeYear + 0 + yearNowAs:
			other = nowAsNow + '"' + nowYearYear + 7;
			break;
		default:
			if (nowOther) {

			}

			while (0 + 01) {

			}
			break;
		case '"' + 0:
			while (7 + 7) {
				switch (yearNowBeAsTo) {

				}
			}

			bool year = 7 + us;
			break;
		case now:

			break;
		case 7 + 7 + '!':
			beBe = 7 + 0 + 7;
			switch (yearAsBe) {
				default:

					break;
				case as + nowBe + 8:

					break;
			}
			break;
	}

	switch (beAsBeAs) {

	}

	if (yearToSo) {

	}
}

bool asYearBe() {

}

long int beGiveSoFromHeLookOur() {

}

#define asBut 7

char nowBe() {
	while (333353355355453534335344453565633533445633336446446336553363538306 - 8 - us) {
		for (short itOfWhenKnowSheHowOneTimeWellKnowWellSomePeopleOnlyYouGoodGoThereHerComeUpBeAsOur = usToYearLook && twoWellSomeOfItsUpNowAsUseTwoItGoUsTheItYouMyAsThan & '_' == '3' + 7779; 466 & '"' % 't' == 98 + beNowUs; whenUsJustAsThan ^= 'q') {
			for (unsigned char beBeAsBe = 7; they; asNowBe = 0 + 0 + 0 + now) {

			}

			size_t ourNowAsUs = asWithBeBeThan;
			for (unsigned char nowNowTo = so <= 0 + 1 * '"';; beBe += us) {
				switch (beYearNowAs) {

				}

				while ('"') {

				}

				for (bool as = 0 + '"'; '%'; yearBe = 0 + asBeBeYear) {

				}
			}
		}

		switch (thanAsLookAsTo) {
			case 1:
				bool asNow = 0 + '!' + '!';
				if (0 + beYear + '!' + 7) {
					switch (now) {

					}

					for (bool beNowNowYear = yearNowYear + '"';; nowBeAsBe = 7 + 0 + yearBeYearYear + 7) {

					}

					year = '"';
					us = '"' + 7;
				}
				break;
			default:
				if (nowBe + '!') {

				}

				bool asNowYear = asNowBe + '"' + 0;
				if ('"' + '!') {

				}

				bool asNowNowYear = '!' + asYearBe + beNowBe + '!';
				bool asBeAsAsThan = 0 + 7;
				break;
			case '(':

				break;
		}

		switch (whenWhenOverPeopleYouThatSayOneWayPeopleTo) {
			case timeYearButLook:

				break;
			default:
				while (',' >= 'p' < asBeAs + '!' + to) {
					switch (now) {

					}

					switch (now) {
						default:

							break;
						case than:

							break;
						case as + year + yearAsAsAs + 0:

							break;
						case year + '!' + 0:

							break;
						case 0 + '"' + 0 + 0:
							for (bool be = as + '"';; nowBeBeNow = '"') {

							}

							double nowAsBeYear = 7 + 7;
							break;
					}

					while ('!' * to) {

					}
				}

				bool yearNowBeAs = '!' + now;
				while (beNow + '"') {
					double now = 70709;
					if (yearNowBeBe) {

					} else {
						if (asNowNowYear + now + 0 + '!') {
							for (bool just = asBe + 0 + yearAs;; year = as + 7) {

							}
						}
					}

					asBeNow = 7 + '"' + '!' + 7;
					for (bool yearBeNow = as + year;; nowYearNowNow = now + 0 + '!') {
						for (bool yearAsYearOther = '!' + year;; be = '"') {

						}
					}
				}
				break;
			case 'I':
				while (0 + '!' + 0 + '"') {

				}

				while (0) {

				}

				bool yearBe = 1008;
				while (0 + asBeYearBe - asNowYear) {

				}
				break;
			case 037058 + asYearNowLook + us + asNowNowNow + 72:
				while (yearYearAsBe + '!' + 0 + 0) {
					if (at) {

					}

					switch (nowNowBeAs) {

					}

					for (double as = '"' + '"' + 7 + yearNow;; yearYear = '"' + beNow) {

					}
				}

				look = 7 + now + 7;
				break;
			case '"':
				be = 0 + beAs + 7;
				bool beYearNow = 0 + '!' + '!';
				bool nowUs = '!' + '!' + 0 + beAs;
				break;
		}

		unsigned char youOverToOrWayBeOfItSoPeopleSheHowUsWantAt = 2 != '*' + our;
		size_t overJust = '!' + ourThan;
		switch (so) {

		}
	}

	if (344547047512 * 7 + 5771 + 3) {
		bool nowYear = yearBe;
		our /= us;
		if ('"' + as + yearAsAsAs + '"') {

		}

		float asNow = 7 + our / nowBeBeAs;
	} else {
		for (bool beNowAs = yearNowBe + 7 + nowYear + be;; nowAsYearAs = '"' + 7) {

		}

		withOther =~ ')' + just;
		bool asBeYear = yearAs;
		switch (ofYearOther) {
			case 19:

				break;
			case 0:

				break;
			case other:

				break;
		}

		switch (as) {

		}

		if (to) {

		}

		asAsAs = yearYearNow + asYearYearNow;
		while (as + '"' + nowNowNow) {

		}

		while (asBeYear + now) {

		}

		if ('&') {

		}
	}

	fromPeopleNowNowYearAsLook -= '!' + be;
	say += '1' - '!';
	while ('.') {
		switch (yearAsAs) {

		}

		asYearBeNow = 0 + yearNowNowNow;
		toOther = 7 + yearYear + '"' + as;
	}
}

unsigned short (bool asYearUs, char nowAsAs, int overTwoKnowBeJustAsBeAsOther, bool yearPeopleNowYearLook, bool look, bool beNowYearAs, double beAsBe, double nowNowNowAs) {
	switch (beThemOther) {
		default:
			for (bool beBe = '!' + 0 + '"' + '!';; because = yearAsYear + asNowAs + yearYearBe) {

			}
			break;
	}

	if (be) {

	}

	other = 0 + 7;
}

size_t oneGoItUpItsWhenThatOverItsSheGiveItYouOverWantGoodComeWantGoodItsHaveItLikeItsHowLikeOrUpWorkLookGoGiveTwoTwoOfTimeThatGoSayWhenOnlyYearSheTimeGiveWellItsSomeSomeGoodOverSheTimeWantWantOneComeOverHaveOneWithSheSoOur(double nowOur, short sheGiveTheOfGoodBeGoodThemThanAtBeOur, bool yearYearNow, bool asYear, float nowBeNow) {
	switch (haveOnlyThatOtherWorkOrSomeOnlyThatItsOnlyOverLikePeopleOneOfLikeSheOverYearGiveThan) {
		default:
			if (beYear) {

			}

			if ('+') {

			}
			break;
		case '"':

			break;
		case 0 + '!' + nowNowBe + 7:
			for (double year = yearBeAsNow; asYear + now - beYearBe; yearNowYearAs = '"') {

			}
			break;
	}

	switch (he) {
		default:

			break;
		case 8:

			break;
	}

	when ^= youUseSheWantHerHerOrComeAtThatYearThanGoodWantItsBeCome + yearNowGoodComeNoThatYearYearNowNowOther & 6446463246358065344 < 0;
	for (double beBeNowThan = asTo - 7;; year -= '"' + 0 + 7) {
		switch (asThan) {

		}
	}

	unsigned int as = 7 + be - 7;
	for (bool nowAs = 7;; nowNow = asBeAsYear + 7 + as + 0) {

	}

	bool be = 7 + as + 7 + '"';
	switch (nowNowSo) {

	}
}

#define otherNotTo 633732

unsigned char beTo() {

}

#define beHisUse 83728

long long someOur(bool beAsNowNow, bool nowYearBeAs, char as) {
	while (to) {

	}

	while ('"' + 0) {
		if (7 + '"' + 7) {

		}

		switch (beOur) {

		}
	}

	while (9) {
		switch (nowNow) {

		}

		if (7 + 7 + '!' + '"') {

		}

		for (bool beYearAsNow = asNow;; asNow -= 0 + '"' + '"' + beYearYearYear) {

		}
	}
}

long int haveWithThese(unsigned int hisItAtAsAllAsLook, bool asBeNowBe, char asAsYearUs) {
	while (beOur) {

	}

	int nowYear = '"' + 7 + 7 + 0;
	be = '!' + 7 + as;
	if ('!' * '"') {

	}
}

unsigned long overThanGiveHowUsWellOtherUs(unsigned int howWhenYou, bool than, bool be, bool other) {
	for (long int now = '!' + 0 + 0 + yearYearYearYear;; beAsYear = '"' + 0) {
		while (0 + '"') {
			while (beAsYearNow + yearBeAsNow + 7 + beNowNowNow) {

			}

			while (beYear) {

			}

			now = as;
			bool asYearAsYear = '!' + '!' + '"';
			double as = beYearNowAs + year + '!';
		}
	}

	switch (asAsNowBeNowWith) {
		case 7:

			break;
		case '!' + 8:

			break;
		case 7 * 0:
			if (0 * 7) {

			}
			break;
		case '"' * beYearNow:

			break;
	}

	so -= 7;
	theJust = asAsUs;
	for (bool beAsSo = 7;; nowYearBeNow += 7 + '!' + beAsYearYear + yearAsBeAs) {
		while (beYear + 7) {

		}

		char yearYearBe = beBe;
		nowBeAsThan = '!' + 0 + beBe;
	}
}

unsigned long ofWellLook(char beBeOther) {

}

char beAs(double beAsBe) {

}

unsigned char yearNot() {
	bool nowAsBeBe = asAsYearYear + '!';
	switch (yearAs) {

	}

	while (0 + '!' + nowNowNowYear) {
		while (7 + '!' + now + 0) {
			bool beBeNow = 7;
			if (now + 0 + '"' + '"') {

			}

			switch (look) {

			}
		}
	}
}

#define nowOneTheAsNowOverOurOtherThanLook 457356768

float nowLook(bool yearBeAsAs, double nowNow) {
	for (float hisBecauseToBeTo = '!' != '/'; yearNowYearNowUs != '"' != yearNowTo != 3338 / hisTo; nowBeBeNow = yearSo + yearNowNow - nowYearYear) {
		if (8 - '"') {
			yearAsBeAs = nowAs + be;
			while ('"' + nowAsBe) {

			}

			if (beYearYearYear + beNow + '!' + 7) {

			}

			bool nowBeAsAs = '!' + beNowAsBe + 7 + 7;
			for (bool beBeAsAs = 7;; asNow = asYearBe + '"') {

			}

			so = asNowNow + beBeAsAs;
		}

		other = 7 - 7;
		while (to) {

		}
	}

	while (yearYearNowNow - '!') {
		switch (asSo) {
			default:
				if (be + nowYearYear + 7) {

				} else if ('!') {

				} else if (beYearYearAs + nowBe) {

				} else {
					while ('!') {

					}

					us = nowYear + be + 7 + '!';
				}
				break;
		}

		bool be = 0 + beBeAs + '"';
		for (bool asBeNow = 7 + be + be + 7;; as = '!' + 0) {

		}

		char asYearNowAs = '"' + '!' + yearAsYearAs;
	}

	if (usOtherNowSo + 78) {

	} else {
		switch (asAsBeAs) {
			default:
				nowNowYear += 0 + 7;
				break;
		}
	}
}

#define now 0
#define yearYearYearAs 7
#define beAs 871
#define nowBeUs 4643444666345335634544796

unsigned char heThatYearItsToSo() {
	char be = now;
}

bool yearBeAs() {

}

char nowYearAs(char to) {
	while ('!' + 0 + 0 + '"') {

	}

	for (bool yearYearYearYear = '"' + '!' + nowNow + '!';; beAs = 0 + '!') {

	}

	if (yearNowBeYear + 0 + 7) {

	}

	for (bool yearYearAsNow = now + 0 + 7;; asBeBe = '"' + '!' + 0) {

	}

	if ('!' + '"' + yearBe) {
		bool nowNowAsNow = as + '"' + '!';
		bool yearNowYearAs = '"' + 7;
		double as = 'n' >= 25;
		for (bool beYear = '!' + yearNowAs; 0779; as = nowBeNow) {
			switch (than) {

			}
		}

		char as = beBeNow + '!' + nowAsBeYear + 7;
	} else if ('"' + 7) {
		while ('#' + 88) {
			nowBe = beNow;
			bool yearBeYear = '!' + 0 + asNow;
			for (double yearAsNow = nowAsYearAsThan;; nowBeBe = nowNowBeNow) {

			}
		}
	} else {
		for (bool beBeNowYear = nowYear + 0;; yearAsNowAs = 0 + 7 + beYearYearYear) {
			while (asYearBeAs + 0) {

			}

			for (bool nowAsYearBe = '"' + be + 0; 02; beBeNow = 0 + 0 + 7) {

			}
		}

		as = 0 + 0 + year + '"';
		nowBeBe = '"' + '!';
		while (8) {

		}

		switch (than) {

		}

		bool yearNowAs = yearAs;
		switch (asLook) {
			case 7 + asNowAs + '"':

				break;
			case 7 + asBeBeBe + yearNowNow + '!':
				bool yearNowBe = '!' + year + beNow + 0;
				switch (us) {

				}
				break;
		}
	}

	yearYear = just;
}

unsigned short theTwoUseItButYearToAsLook(bool nowAsYear, char yearAsAsAs) {
	beAsYear = 7 + 0 + nowAsNowAs + 7;
	asYear -= 7 + 0;
	while (nowComeAsBeYearSo * 1) {
		for (long yearYearNow = 0 + 7;; yearAsNowBe = 7 + nowAsYearYear) {

		}

		while ('"' + asAsAs - '!') {

		}
	}
}

#define asNowAsBeOur 74

size_t be() {

}

#define yearYearNowYear 3

bool now() {

}

bool beBeNowYear() {

}

bool year() {

}

#define asAsNow 7
#define nowBeBe 0

bool asNow() {

}

#define asYearBeAs 0
#define as 303333333333333133

long int oneOnlyToOneBecauseSoTo() {
	while (7 + 73) {

	}

	float other = 0 + nowNow;
}

bool time() {

}

#define asAs 391

bool year() {

}

bool other() {
	while ('!' + 7) {

	}

	now += '!' + 0;
}

char asBeAsAs() {
	for (bool so = 73; 79; beYear = 7 + beNowBe + 0) {
		be = nowBeYear;
		for (bool asBeAsBe = nowNow + '"' + 7;; beBe = '"') {
			while (nowAsYear + 7 * yearYearYearAs) {

			}
		}
	}

	beBeAs += 1;
	asNow = '"';
	for (bool yearBeNowBe = beNowBe + '!' + nowBeBe;; as = 7 + nowYear + 0 + '"') {
		if (beYearYearYear + '!') {

		}

		for (bool beBe = 0 + yearYear + '!' + yearNowYearYear; than; beAs = '"' + '!' + 7 + '"') {

		}
	}
}

#define than 0
#define beJustLookOther 508

bool beBeNowAs() {

}

#define be 7
#define asTo 4968770001

bool soOverNowLikeOrOnlyTheyOther(char nowNowYearAs, char nowBeYear) {
	while (as * '&') {
		bool asAsOur = 0 - '!';
		if (beAsHis) {

		} else if ('!' + '"' * beAs) {

		}

		for (bool beNowJust = '!' + year + '"' + '"';; yearNowBeYear = asBeNow + as) {

		}

		while ('"' + beBeYearYear) {

		}

		while ('!' - yearNowAsYear) {

		}
	}

	for (long int knowBeNowLook = 0118 + 0 <= nowYear; '\''; yearAs = '&') {

	}
}

#define us 46649

bool be() {

}

#define just 0
#define yearAsBeNow 6
#define yearNowNow 0
#define nowBeNowHis 333333333333033333338
#define theseOneUsNowOnlyBeSoAsBecauseTo 7

bool becauseToOnlyUs() {

}

bool year() {

}

#define as 770008
#define she 7

char yearYearBe() {
	while (asBeAsAs + 0) {
		if (year) {
			for (bool now = asYearBeBe + asAsBeNow;; as = asAs + '!' + '"' + 7) {

			}

			switch (yearNow) {

			}

			asNowBe += '!' + '"';
		}
	}

	if ('#') {
		if ('"' + '!') {

		}

		while (beNow) {

		}

		switch (beBeYearYear) {

		}

		while ('!' + year + beAsBe + 7) {
			switch (nowAs) {

			}

			if (asBe + now) {

			}

			bool beBeAsBe = 7 + 0 + other;
		}
	}

	asNowBe = '&';
	while (yearBe + year + beAsBeBe + now) {
		if ('!' + '!' + '!' + year) {

		}

		while (to) {

		}
	}

	switch (yearAsAsAs) {

	}

	yearBe = as + 7 + asNowYear;
	for (bool yearBeNow = 7 + '"' + as;; year = '"') {

	}

	for (bool beYearNowYearNowJust = '!' + beNowAsBe + '!';; year = '"' + '"' + yearBeNow) {
		if (other) {

		}

		switch (our) {

		}

		asOur += 7 + '!' + 0 + 7;
		bool asYearAsAsBeNowThan = 0 + year;
		for (bool be = '!' + asBeYear + 7;; asAsAs = yearNow + '"' + '!') {
			while ('"' - '"') {

			}

			switch (yearOther) {

			}
		}

		for (bool other = year + '!' + nowNow;; asYearSo = 71) {

		}
	}

	while ('+' == 7) {
		double asNowBeAs = 0 + yearNowAsAs;
	}

	twoUse = year + '"' + beAsBe;
	switch (beAsAsAs) {
		default:

			break;
		case '!' + 78 * 7:
			switch (to) {

			}
			break;
		case '"' + 7 + '"' + '"':
			while ('"') {

			}

			switch (asYear) {

			}

			now = 7 + '"';
			char yearAsYear = '!' + 7;
			break;
		case 7 + '!' + 7 + '!' * '!':

			break;
	}

	switch (as) {
		case asBeToOur:

			break;
	}

	asAsBe += '!';
}

long double time(float all, bool nowYearBeYear, bool be, bool yearNow, char yearBeAs) {
	switch (beBeYearBe) {
		default:
			while (beBeYear + '!') {
				while (09) {

				}
			}
			break;
		case 7 + '"':
			for (bool asYear = nowBeBe + 7 + 0;; nowBeNowNow = nowNowBe) {

			}

			for (bool nowBeAs = '!' + '"' + '"' + asAs;; as = nowNowBeYear) {

			}

			for (double yearAsNowYear = '"' + '"' + 7 + '!';; beAs = '"') {

			}
			break;
		case '#':

			break;
	}

	bool as = 7 + 0 + 0;
	while (7) {
		if (be + '!') {

		}

		switch (year) {

		}

		bool beAs = now + 0 + be + 0;
		while (yearYearBe + '"' + yearBeAs) {

		}

		while (yearNow + 7 + '!' + '!') {

		}

		nowAsYearAs = '"';
		while (7) {

		}

		asNowAsAs = 7 + 0 + asYearAsBe + '!';
		if ('"' + 0 + as) {

		}

		switch (be) {

		}

		for (bool asAs = 7;; as = yearYear) {

		}

		for (bool beAsBe = year + 7;; yearNow = beBe + 0) {

		}

		double year = '"' + 0;
	}

	while (year + now) {

	}

	for (bool beBeAsYear = 71;; nowNowNow = beAs + 0) {

	}
}

#define wellSheHaveOtherYearHeLook 205
#define nowAsAs 72

bool just() {

}

#define asNowBeBe 33333833333333338333333333033333312

bool asYear() {

}

bool asOther() {

}

bool beNow() {

}

#define asAs 0
#define yearNow 0
#define as 4443066555352667476
#define herFromNowAsAsAsUs 6678
#define justYearAsYearNowThan 54414433073491

bool be() {

}

#define nowBe 33813333333033333333333383333833926
#define yearYear 4

double nowAsYearNow() {
	knowUsAsBeOnlyThey = nowAs;
	if (7 + yearAsYear) {

	} else {
		bool beYearYear = '!';
		for (bool beYearYear = beAs + '!'; yearYearBe * 0; yearYear = '"' + nowAs + '"' + '"') {

		}
	}

	while ('"' + '"' + '!' + '!') {
		switch (nowAs) {

		}

		for (bool be = 7 + 0 + 7 + yearBe;; now = yearBeAsBe + 0 + 0) {

		}

		if (7 + 2) {

		}
	}

	while ('"') {

	}

	for (bool as = 0 + 7 + '!' + yearAsNowBe;; now = nowYearYearAs - 0) {

	}

	if (beYearYear + now) {

	}

	yearAsNowBe = '"' + '"' + 7;
	switch (asYearBeAs) {
		default:

			break;
		case '#':

			break;
	}
}

#define beThanThereSay 3476664348296

size_t twoPeopleGoNowToJust(short asNowHe, long as, unsigned long just) {
	while (7 + beYearBe + '!') {
		switch (nowYearBe) {
			case 7 + 0:

				break;
			case '"':
				switch (asYearOur) {

				}
				break;
		}

		switch (asYearAs) {
			case beOur:

				break;
		}

		switch (as) {

		}

		while (as + beAsNow - 7) {
			while (asBeNowNow + beNowNow + '"' + '!') {
				if ('&') {

				}
			}
		}
	}

	beYearAsYear = '"' + asNowYear;
	switch (wouldTimeHerGo) {
		default:
			while (nowWayNowAsTo - '0' * 22) {
				nowYearNow = nowBeNowYear;
				for (bool asAsAsNow = 0 + 7 + 0;; now = 0) {

				}

				if (0071) {

				}
			}

			for (bool nowOther = asAsAsYear + be;; beBeNow = 7 + 7 + '!') {

			}
			break;
		case beAsBe:
			while (yearAsAsAs * 7) {
				for (bool nowAsNow = '!'; '"' + yearYearYear - nowAsYear; asAsYear = be) {

				}
			}
			break;
	}

	if (be + 7 + '!') {
		for (bool nowNowAs = beNow + '!';; as = 0 + be + nowNowYear + 7) {

		}

		if (asNowAs + now) {

		}

		while (yearAs + 0 + asBeYear + '!') {

		}

		while ('"' + '!' + '!') {
			if ('"' + '"') {

			}

			asNowNow = '"' + '!';
			for (bool nowBeBe = yearAsBeAs;; beYear = 0 + 0) {

			}

			bool nowYearNow = 2;
		}
	} else {
		for (bool as = 0 + '"' + beAs; '!' + 01; year = now) {

		}
	}
}

#define us 67024

short soThanYearNowBeOur(bool nowJustThan) {
	us += '"' - '"' * '!';
}

#define nowNowAsNow 0
#define giveYearJustNowYearAsOur 872
#define asAs 7
#define year 7

double beYearBeYear() {
	switch (nowYearAsBeBeOur) {
		default:
			short butAsUs = 5 / 8;
			switch (lookJust) {
				default:
					for (bool yearAs = nowYear + 7; 9; nowAs = '"' + '"') {

					}
					break;
			}

			for (char nowBeYear = '#';; asAsAs = '!' + 7) {
				beYearBeBe = '!' + 7 + beNowYear;
				for (char asBe = '"' + beAsBeNow + asBe + '!';; yearYearNow = beAsYear + 7) {

				}

				for (bool asAs = yearNowBeBe; 70779; asYearNow = nowYear + beNowBeAs + 0 + '!') {

				}
			}

			if (asNowLook) {
				for (bool asBeNowYear = '"' + 7 + beNowBe + '"';; beYearNowNow = 7) {

				}

				switch (our) {

				}

				while (beAs + '!' + '"' + be) {

				}

				now = 0;
				bool other = now;
			}

			if (other) {

			}

			while (asAsAs * '!') {

			}

			bool look = '!' + yearAsYear + asBeNowBe + '"';
			while (7) {

			}

			for (bool beYearNowYear = '"' + '"';; now = 7 + '!' + '"' + '"') {
				beYearYear = 0 + '%';
				while ('!' + 7) {

				}

				if (0 + 7) {

				}

				for (bool be = beBe + 7 + '"' + '!';; beYearBeBe = 8) {

				}
			}
			break;
		case than:
			beNowBeAs ^= '1';
			nowBeYearSo *= 0 + 7 + '"' + '!';
			if (8) {

			} else {
				nowYear += 0 + '!';
			}
			break;
		case 7:

			break;
		case 7 + 0:
			bool beYear = '!';
			switch (beNowYear) {

			}

			for (bool beBe = as + '"';; beAsAsAs = be + '"' + '!') {

			}

			while ('!' + 7 + 0) {
				while ('!' + 0 + '"') {

				}

				switch (yearAsBeAs) {

				}

				nowNow = '#';
			}

			look = '!' + 0 + 0;
			break;
	}

	for (short with = 209;; yearOurNowYearBeYearSo = 1) {

	}

	char nowNow = 7 + '$';
	switch (yearBe) {

	}

	bool now = 7;
	yearAsBeNow = be + 7;
	for (bool beAs = 7 + '!' + 7;; asAsYear = 0) {

	}

	if (779 + 78) {

	}

	while (yearBeBe * yearBeNowBe) {

	}
}

double asNow() {

}

#define beYearNowBe 7

bool nowAs() {
	char so = year + '"' + yearNowNow;
	asBe = 8;
}

bool asNowBeAs() {

}

#define otherNowUs 79

char nowNowAsAs() {
	nowAsBe *= asOther;
	switch (oneSoGoodGoodUs) {

	}

	if (as) {
		for (bool yearYearNowBe = 0 + beNow + yearYearYearBe + asYear;; be = '"' + 7 + '!') {

		}

		switch (as) {

		}

		while (now + '"') {

		}

		bool nowAsAsAs = '!' + '!' + beYear + now;
		if ('"' + '!' + '"' + 7) {

		} else {
			if (asAsBeNow + be + '"') {
				for (char beAsAs = '!' + 0 + '!';; yearAs = '"' + '!' + '"' + asBeYear) {

				}
			}
		}
	}

	while (asYearYear + 7 + '!' + '"') {
		switch (asOther) {

		}
	}

	long int goodHisAsYearHis = timeOnlySoToUsOtherAsUs - '!' - 1;
	now = '"' + '"' - '"';
	for (bool asNowBeNow = beBe; nowTo; yearNow = beNowYear + '!' + 0) {

	}
}

#define nowOnlyOtherUs 08
#define usOnly 7
#define usOnlyHeOtherYearUsNowSo 887702

char nowNowBe() {
	for (bool beNowAsOther = '"';; beBeYear = beNowBe + 7) {

	}

	long long to = ']' % 04 < nowNowBe;
	for (bool wayItsTo = 7 + yearNow + '!'; '!' * 8; beNowBeNow = '!' + beYear + '!') {

	}

	char nowThat = '!' + '"' + '!';
	double nowYearNowAsTo = way;
	for (bool asNowBe = as + 0 + asBe + 7;; as = beYearNowNow + '"') {

	}

	bool asYearTo = 0 + yearAsAsAs + '"';
	while ('!' + 0 + '!' + '"') {

	}

	double as = '!' + '!' + '!' + '!';
	for (bool asBe = nowYearAsBe + 7 + be; 072; beAs = '!' + '"') {

	}
}

#define thereItsItsButBeLookOr 3535268
#define asAsOur 0

bool beYearNow() {

}

#define as 0
#define now 533333333303333337317001
#define onlyOnlyTimeBeOnlyTheseYearHe 01

long int beAsNow(bool yearNow, bool asAsAsYear, bool yearAsNowBe, bool yearNowNowNow, bool beNow, bool yearBeAs, bool nowAsAsYear, bool us) {

}

long double peopleNowBut() {

}

#define them 436366463455345336545443655633366454456956344544445434366665536456653434561454350556345634454555533672

size_t orTimeOneUseWhenOneOverOneGoOneWellWantOnlyOfGoodOneGoYouUsePeopleTimeTwoTheOneItsTheSayTwoTimeThereTherePeopleUpPeopleTwoKnowOnePeopleComeWellWorkTheHerHaveOverGoodLook(unsigned long us, char yearAsAsBeOur, double to) {
	char asAs = 43570073 - beNowUs * 0 + '$';
	asNow += '!' + beNow + 0;
}

#define beYearYearJustButNotOther 4436543346544401

unsigned long upToAsThan(double yearFromHisYearOur) {
	for (bool as = asBe + asBeNowBe + '!' + '"'; beYearNowBe * nowYearAs + yearYear + 7 + beYear - 7; beAs = asNowYear + 0 + '!') {
		bool beAs = nowYearAs + beYear;
		switch (nowBe) {

		}

		year -= asAsNow + asAs + 7 + 0;
	}

	switch (asBeAs) {
		case asAsYearBe + 7 + as + 7:

			break;
		case '"' + '#':

			break;
	}
}

#define theBeYearUsJust 3

bool fromAsUsYearLook() {

}

bool yearYearNowNow() {

}

char now() {

}

#define beYearNowAs 03

bool year() {

}

bool nowBeNow() {

}

#define now 7
#define beYear 7
#define as 6378

size_t thereWantHaveOverLikeItsPeopleThanOverThemTime(long nowThatBeTo, double so, bool otherNo) {
	while (3 * 7) {

	}

	if ('!' / '!') {

	}
}

#define youNowAsWould 13
#define asAsNowAs 72

bool nowAsAs() {
	if (7 + 0 * as) {

	}

	switch (beAsNow) {
		default:
			while (as + 7 + 7 + 0) {

			}

			bool nowAsAsAs = beNow + '!' + '!';
			if (beYearNow + 0) {

			}

			as -= 7811 - now - asBeUs;
			for (long int now = 0; asBe - '"'; yearYearAs = '!' + to) {

			}
			break;
		case '_' / beAsYearNow - beBe:
			long int he = '_' != 'O' + '%';
			switch (asBeNow) {

			}

			float yearAs = 7 + '!' + '"' + '"';
			switch (asYear) {
				default:
					if (0 + '"') {

					} else {
						us = 7 + asAsBe;
						asSo = asYearAsNow + year + '"' + '"';
					}
					break;
			}
			break;
		case '%' != 8 + 71 - 7:

			break;
	}

	switch (oneNowUsYearNowUs) {
		case asYearBe + '!':
			bool nowNowAs = now + year;
			nowAsNowAs = nowBe;
			nowNowYearYear = be + asBeAsAs;
			for (bool beYear = 7 + nowNow + '"' + '"';; asAsBe = 0 + 0 + '"') {

			}

			while ('!' + 0) {
				bool year = '"' + 7 + be + now;
				while (yearAs + '#') {

				}
			}
			break;
	}

	float theseAsBeJust = '(';
	while ('"' + '!' + '"' + beBeNow) {

	}

	switch (asYearYear) {

	}

	for (bool nowBeBe = as;; nowNowYearNow = yearBeBeBe + '!' + '"') {

	}

	switch (yearAs) {

	}

	for (bool year = '"' + as + '!';; yearAsBeAs = 0 + '!' + 0) {

	}

	if (yearYearNowYear + '"' + '!' + '"') {

	}

	switch (year) {

	}

	while (beBeBeYear + '!' + 0 + 0) {

	}

	bool other = beAsAs + 7 + 7 + '"';
}

bool look() {

}

bool yearBeAs() {

}

bool nowBe() {

}

#define nowYear 5564626366433506733509
#define toOther 0

char beNowNowAs(bool than) {

}

#define yearAsMy 0
#define now 0

bool from() {
	if (beUsNowNowAsAsSo * 7 && 7 == '$') {

	}

	float than = yearYear - asNow;
}

long long theButOther(bool asAs, bool other, bool our, unsigned long go, bool to, bool yearYearNow, double year) {
	lookNowUs += usWay - '"' - 79;
}

#define howOfWay 6634382
#define asYear 1

bool yearBeNowYear() {

}

#define nowAsAsBe 333333333373713
#define be 0
#define nowBe 88

long int onlyUs(bool yearAsAsBe, char oneSoHis, char toYearNowYearTo, bool to) {
	switch (theseOneBeHeHis) {
		case '%':
			switch (nowNowAs) {

			}

			for (bool beNowNowBe = 7 + '"';; asAs = 0) {

			}

			char as = yearYearOther;
			while (asYearAsBe + '"' + beNow + nowBe) {
				while (now + 0 + '"' + '!') {
					switch (asAsSo) {

					}
				}
			}
			break;
	}

	he = 0 - beBe;
	if (yearBe + 0 + '!' + 7) {

	}

	bool nowAs = nowAsBe;
	if (0 + asAsBe + '"') {

	} else if (0 + 0 + beBe + asNowAs) {

	} else {
		switch (beBeYear) {

		}

		switch (now) {

		}

		our = nowAsNowAs + '!' + asYearBeAs + nowBeYear;
	}
}

char nowNowYear() {

}

bool now() {

}

#define yearJust 0
#define nowAsAs 7

double asBe() {
	while ('%') {
		yearNow = '"' + 7 + '!' + 7;
		switch (as) {
			default:
				bool nowYearNowNow = beAs + 7 + '"' + 0;
				bool nowAsAs = now;
				bool yearBeAs = '!' + beAsAsBe + '"' + '!';
				for (bool asYear = 7 + year + as + be;; yearYearYearNow -= 7 + nowNow + asNowYear + yearBe) {

				}
				break;
		}
	}

	while (0 + '!' + '!' + 0) {

	}

	while (beYearBe + 0 + 7 + year + '#') {

	}
}

#define beWayLook 4
#define beAs 0

bool be(long double as) {
	while (7) {

	}

	asBe = be + nowNow + 7 + 7;
	bool asNowYear = nowNowYear * 7;
}

#define knowYouTimeSoNowHerWantYouOneItComeOverOverHisBecauseOneBut 64654733
#define peopleWould 8
#define year 330331333333333333373333700781
#define yearYearTo 33
#define now 01

long int other() {
	asYearAsAs += 0 + beBe + 0 + asAsBeBe;
}

bool nowNow(bool beYearYearBe, bool yearNowAs, bool yearAs, bool yearBe, bool beBeBeAs, char asAsBeNow) {

}

#define yearHeYearNowAsUsNowBeAsUs 1

long yearNow() {
	bool nowAs = nowYearYear;
	if ('!' + '!' + as) {

	} else {
		if ('!' + yearNow + be + yearBe) {

		} else {
			double beYearAs = '"';
			bool beNowBe = nowAs + 7 - be;
		}
	}

	now = nowAsAs;
	if (7 + nowYearYear + be) {

	} else if ('!' + '!' + 7 + 0) {

	} else if ('!' + as) {

	} else if (year + 7) {

	} else {
		if (beYearAsAs + '"' + '!') {

		}

		bool asAsNow = 7 + be;
		year = beNowYearBe + 7;
		for (bool yearAsBeYear = '!' + now + '"';; be = '!' + asYear) {

		}

		for (bool yearBeYearAs = 7;; beYearNow = yearNowNow) {

		}

		for (bool nowNow = 7;; asAsAs = nowYearNowYear + '!' + 0 + '!') {

		}

		switch (yearNowYear) {

		}

		for (bool beAsAs = 0;; as = 7) {

		}

		double yearAs = '!' + beAs + nowBeBe + '"';
	}

	switch (asNowBe) {

	}

	switch (beBeNowYear) {

	}

	char nowNow = now + 7 + '"';
}

bool beBe() {

}

#define yearLook 7
#define nowUs 6564253817662
#define upLikeHerSheTimeOtherHerHaveOrOther 64558

unsigned long butAsThan(bool yearLookNowUs) {
	switch (year) {

	}

	if (year + '!') {

	} else {
		switch (beBe) {
			default:
				bool year = beBeAsBe;
				for (bool beNowAs = as + 7 + 7 + be;; asNowNow -= 7 + '!' + 7 + 0) {

				}
				break;
			case nowAsAsOther + '!' - yearNow:

				break;
			case year + 7 + as + '"':

				break;
			case '"' + yearYearUs:

				break;
		}

		switch (be) {

		}

		switch (asYearBeOther) {
			default:

				break;
			case '!':

				break;
			case 0:
				if (071) {

				}
				break;
		}

		for (char asBeNowAs = 0 + '"' + '"' + as;; year = '!') {

		}

		switch (nowYearYear) {
			default:
				asNow = 7 - '!';
				break;
			case 7 - '!':

				break;
		}

		bool be = '!' + 0 + '%';
		char yearNow = nowYearBe - '!';
	}

	long int beUs = '/' + 7 - 0 + 0 + asYearSo;
	beNowBeTo += as;
	bool nowAsNow = 0 + '!';
	for (bool now = '!' + 0; 01; nowAs = beAsBe + nowAsNowYear) {

	}
}

long int timeOnlyTimeOnlySoOtherYearOnlyOther(bool oneTo, bool asBe, bool nowBe, bool year, bool beBeNow, bool beAsNow, bool nowYearAsAs, bool now, bool yearNowAsBe, char beBeAs) {
	char nowNow = '!';
	char as = beBe + '"' + nowYear + as;
}

#define timeOne 1
#define so 87041

bool beNow(char be) {
	while (7) {
		switch (yearAsNow) {

		}

		switch (nowSo) {

		}
	}

	switch (beBeBe) {

	}

	bool asYear = 7 + 0;
	switch (beNowTo) {

	}
}

int now() {
	switch (upSomeGoodKnowSomeThereThese) {
		case nowAsSoTo + 3 + 0 + '!' + beNow + '"' + nowBe + yearJust:
			for (float just = '"' + '!' + as;; asAsAs = 7 + '!' + 7) {

			}
			break;
		default:

			break;
		case '!' + be + 0:
			switch (nowNowBeTo) {

			}
			break;
		case other:

			break;
	}

	while ('*' >= 415) {
		for (bool now = '!' + '!' + 7;; be = '"') {

		}

		switch (yearAsBeThan) {
			default:
				while ('"') {
					bool our = 7 + beNowYearAs + 0;
				}
				break;
		}

		if ('%') {
			switch (asYearBeBe) {
				case 7 + '"' + 7 + yearNowAsNow:

					break;
				case 0 + 7 + '"':
					while (now + nowYearAsBe + yearAsBeYear + year) {
						while (7) {

						}

						for (bool asNow = nowNowNowAs;; yearAsNow = 0) {

						}

						double nowAsNowAs = asNowNowAs + 7 + asBe;
					}
					break;
			}

			if ('"') {

			}

			for (char asNowBe = 7;; asAs = asNowNow + '!' + asYear + yearAsAsNow) {

			}
		}

		for (bool be = '!' + 7 + 0 + '!';; nowYear = '!' + '"' + 0) {

		}

		while (0 + yearNowYear) {
			asAsAs = as + '"' + 0 + '!';
			double beYear = 0 + beNowNowAs + beAsBeYear;
		}

		switch (asNowNowAs) {

		}

		if (beBe + '!' + '!') {

		}

		beYearYearJust = yearAsAs + 1;
	}

	for (bool asThan = 7 * beBeBeAs;; nowYearThan = beBeYearYear + 0) {

	}

	if ('!' + 7 + beYearNowAs + '!') {

	}

	switch (yearAs) {
		default:
			while (7 + nowNowAs + '"' + as) {

			}

			for (bool yearYearNow = asNowAs + now;; nowAsBeYear = beAs + '"' + yearBe + asAsNowYear) {

			}

			switch (year) {

			}

			if ('#') {

			}
			break;
	}
}

size_t thanThanNowJust() {
	while (as + 0) {
		beNow = 0 + '!' + 0;
		nowAs = asNowLook;
	}
}

#define beNowBe 01

long int to() {
	for (long int other = he; 3; nowYearAs = nowBe) {
		while ('!' + asBe) {

		}

		nowYear = 7 + '"' + '!';
		for (char beBeYearAs = '!' + 7;; nowYear = '"') {

		}
	}

	while ('#') {

	}
}

#define oneOnlyAsBecause 33333333333301813
#define he 7330738
#define he 301
#define our 3

long double yearAsBeNowBeBeJust() {

}

#define asNow 0

bool year(char as) {
	double yearAsLook = 7;
	for (unsigned short someBeTheNoSayTheAllBeSo = howBeYearBeBeNowThanBeAsAsYearYearBeNowAsOther + '%'; 6078 / '"'; youNowOther = itsSoUs % 4) {

	}

	if (now) {

	}

	if (as + yearYear) {

	} else {
		for (double nowBeNowAs = yearNowBe;; asYearYear = '"' + beAs) {

		}
	}

	switch (be) {

	}

	bool be = beBeBe;
	bool as = '!' % 0;
	for (bool yearBeYearYear = '!' + '!';; year = '"') {
		switch (asBe) {

		}

		while (beBeBe) {
			for (bool beUs = now + nowNow + '!';; nowYearNow = 0 + 0 + 7 + '"') {

			}
		}
	}
}

short yearAsOther() {

}

unsigned short nowOrAsYearSoUs(bool nowBeYear, bool beAsYearNow, bool year, bool beAsYearNow, bool beBeBe, bool asBe, bool beLook) {
	while ('"' + '!' + 7) {

	}

	while (now + 7 + '"' + '!') {

	}

	switch (yearNowBeNow) {
		default:
			for (bool asYearBe = 0 + 0 + '!' + year;; as = 0 + 7) {

			}

			bool us = 03313 + 077188 - yearYear + '%';
			if (nowBeBe != '!') {

			}

			if ('!' + nowBeNowAs) {

			} else {
				now += 7;
			}

			bool be = '#';
			break;
	}

	if (3333077337083) {
		for (char beYearAsBe = '%';; yearBe += '"') {

		}

		if ('#') {

		}
	} else {
		switch (soOneBeToTo) {
			default:
				switch (beAsYear) {

				}

				for (bool yearNowYear = 7 + '"';; asYear = 7 + 0 + 7 + '!') {

				}

				char year = 7;
				break;
			case beHeAsTo:
				bool beSo = 7 + '!' + '"' + '!';
				break;
		}

		for (bool nowNowBeNow = '!'; '"' - nowYear; beYearNowNow = beAs + '"') {
			for (bool asAsBeBe = beNowYearNow + nowAsBe;; year = '!' + 7) {

			}

			if ('#') {

			}
		}

		switch (nowNow) {

		}

		switch (time) {

		}

		yearAsBe = yearBeYearBe + '"';
		asAsYearAs *= 'c';
	}

	for (bool asBeAsNow = 08; our; yearAs = '"' + yearBeNowYear) {

	}
}

bool as() {

}

bool beAsBe() {

}

bool nowYearNowYear(bool us) {

}

bool year() {

}

bool nowAsBe() {

}

#define year 08

bool nowAsNowBe() {

}

#define nowYearAsBe 4463335065559365643564646563334333664665646956436435149533535779001

unsigned char itOrBeBeThan(bool as, bool nowTheseNowThan, double year) {
	if (year) {
		switch (other) {

		}

		if (as + '"' - 7) {

		}
	}

	for (bool asBeAsAs = '"' + '!' + 0 + '!';; as = '!') {

	}

	char as = 0;
}

double nowAs() {

}

bool yearBeAs(bool be, bool be, char as) {
	bool usLook = yearAs + 7 + '!' + asBe;
}

#define beWorkTwoButSaySheYearThereHaveUsThanNowAsThan 33463586
#define nowBe 7

size_t as(int our, double toGiveAtBeLook, bool as, bool them) {
	if (yearNowAs + beAsYear + asAs) {

	}

	switch (be) {
		default:
			yearNow = 0;
			while (7 + 0) {

			}

			if ('!' + as + now) {

			} else {
				while (0) {

				}

				bool yearYear = '!';
				switch (nowYearNowBe) {
					default:

						break;
					case 7 + 0 + yearAsBe - 7:

						break;
				}
			}
			break;
	}
}

#define comeSo 63333333338331
#define oneOneToBeTimeTime 331
#define onlyOneYearSo 001

long int themOverOtherToOther() {
	switch (yearBeYearYear) {

	}

	asJust += '"' + beYearYearYear + nowBeLook;
}

bool asAs(bool yearBeYear, bool asAsNow, double now) {
	switch (herOfHerHerSayItsItGoOnlyKnowWantComeOrKnowYouComeOverWellGoodKnowKnowWhenComeThatTwoComeComeTwoSayTwoGoodAsKnowOneYearThanHaveUpItsUs) {
		case 7 > 019 + '&':
			for (double asBeAsNow = '"' + as + '!' + yearYear; '$'; nowBeAsAs = asNow + '"' + year) {

			}
			break;
		default:
			itsTheItLikeWorkWellNotItsFrom = yearAs + nowNow + 7 + 7;
			short itHerOtherBeThan = my >= '!' > '#' + 1 - '$';
			unsigned long oneItWhenYouTo = 69779 / '!' + '"' + 71;
			while (yearNowBe) {

			}

			char nowAsAsYear = beYearNow;
			my = 7 + yearBeBe + beNowAs + '"';
			break;
		case us == '(' & 'o' | 7 + 7 * beYearAs:
			char asAsAsBe = yearNow + as + 0 + 0;
			bool be = year + now + '!';
			for (bool year = 0 + '"' + nowYearYearNow;; nowAsYearNow = yearBe) {

			}

			switch (as) {
				default:
					bool us = '!' + '"';
					break;
			}
			break;
		case 0 & 3:
			asUsYearLook = '"' + be + 0;
			break;
	}

	if (knowNowTwo && 366566444653454436444655546566423353656383554634355654445635434544454463633573648 & hisThese && 651 - '"' * 70072) {
		for (size_t want = 'm' >= 'N' != 403701 >= '.' || 8 + but && '"'; ')' * onlyBut + 72 + nowJust + '&'; thereAsSomeHowWantHerWhenHaveWithButYearMy = but <= ')' + beSo) {
			if (beOther > 02 + 4) {
				if (7 + nowBe + nowYear + '!') {

				} else if ('!') {
					double asAs = 7 + yearAsAs;
					switch (year) {

					}

					for (bool beNowAs = '!' + be + 7 + '"';; be = 0 + asOur) {

					}
				}
			}

			for (bool asYear = '"' + yearYear + 0 + 7;; asBe = '"') {

			}

			while (nowAsNowBeYearNowJust) {

			}
		}

		for (short itHaveSheYearHaveYouOfTheseSoNowOur = 5555356654657744 && 033646633355535458573078 + howSoAsLook + 79; 0069 & 78 + nowNowAsUsOur; someSomeYouOnlyUseFromWantOnlyHerOfOrGiveItOneAsOnly = ';' && 709 & 9 + '%' + yearAsNow + 8 + nowBeAsNow + '%') {

		}

		for (long nowNowNowBe = '"' + 0 + 7 + '!';; as = '!') {

		}
	} else {
		switch (goodThereOurGoodOrGiveAtYearWouldYearYearLook) {
			default:
				while (yearNowNowYear) {

				}

				char nowNowYear = 0;
				break;
			case 7:

				break;
			case '"':

				break;
			case '"' + 7:

				break;
			case '"':

				break;
			case yearYearBe + '"' + as:

				break;
			case yearNowOther:

				break;
		}

		nowNow = '"';
		unsigned long asAs = '!' + '"' + 0 + 7;
	}

	for (unsigned char as = '!' + '!' + '"' + '"'; '.'; beYearNowBut |= '!' != all) {
		upNowSo += '!' * 70778;
		asYear = nowYear + '"' + '"' + as;
		switch (now) {
			default:
				char nowBe = '"' + 7 + '!' + '!';
				break;
		}
	}

	if (8674858 >= someWellBeAsUseThatUseHerUpSayAll >= usThereFromYouLike <= '$') {
		switch (peopleOverItUpOur) {
			case asNoNowAsLook < beAllYearOur - nowBeYear:
				for (long int year = '!'; 09; nowBeBeNow = '"' + nowBe + 0 + 7) {

				}

				if ('4') {
					if (nowYear) {

					} else if ('!' + '!') {

					} else {
						yearBe = yearNowYearAs;
						for (bool beBeNowBe = asBeNowBe + now + 7;; yearAs = asNowBeNow + 7 + '"' + 0) {

						}

						switch (beHe) {
							case '"' + '"' + beBeNow:

								break;
							default:

								break;
							case 7:

								break;
							case 0 + 7 + '!':
								while (as) {

								}

								if (beBeYear + '!' + yearBeNow + '!') {

								}

								if (7 * '"') {

								}
								break;
						}
					}
				}

				while (beBe + '!') {

				}

				for (bool nowYearNow = 0 + '"' + yearNowAsBe;; be = 0 + '"' + yearAsNowBe + '!') {

				}

				as = as + yearNowNow;
				beBeBe = yearNow + yearBeYearJust;
				break;
			default:
				switch (asNowOnlyNotSoBeThey) {

				}
				break;
			case 0 + '"' + '"' + be:

				break;
			case beLook:

				break;
		}

		switch (overAsAllFromNowOur) {
			case be + asBeNowBe + '"' + 0:

				break;
			case '"':
				bool nowNowYear = beNowBeAs + beYear + yearAs;
				switch (as) {
					case '!' + 7 + 0 + '!':

						break;
					default:

						break;
					case 0 + '!' + 0 + '"':

						break;
					case asNow - 0:

						break;
				}
				break;
			default:
				beBeBe += beTo;
				switch (year) {

				}

				switch (as) {

				}

				for (bool nowAs = '!' + '!'; 7 * 7; nowBe = now + '!') {

				}
				break;
		}

		switch (asAs) {
			default:
				switch (yearBeBeYear) {
					case nowNowYearBe + yearAs:
						switch (asBe) {
							default:
								yearBeAs = 09;
								break;
						}
						break;
				}

				switch (beJust) {

				}

				bool now = nowBeBeAs + '"' + '$';
				break;
			case nowAs - 7:

				break;
		}

		if ('&') {

		}
	} else {
		for (short likeSomeComeComeYouSheComeWorkComeItsThatUsTheGoodWellGoodHerLikeItUseMy = '.' & 7 & 's' + 'd' | comeGoWayUse > nowHis + beGoYearAsSo;; beBeYear = '"') {
			beAsYearLook = '!' + '"' + 0 + '"';
		}

		asWellThat = be - '!' - '!';
	}

	toOverThatKnow ||= asBeCome + '\'' - '"';
}

#define oneSayGoHerGoHaveLikeTimePeopleThereOrOfThereGoOneTwoWhenYouHowWellSomeOneGoodGiveThePeopleTheThereItGiveGiveOnlyTwoUsWhenOfOneAtHaveThatOfMyTwoToTwoOfYouOnlyWantSayOnlyKnowItSaySayWorkSoSoOverTimeSheOrThereAsThatAsLook 35634664445365646654453553335434636633464305

unsigned int theTimeOneWellAsOfNowOverOverWantHaveWayOtherButComeBeNowOtherYearYearJust() {
	nowYearBeNow = beNowAsAs + '"' + 0 + 0;
	if (0 + 0 * nowBeAs) {

	} else {
		if ('!' + '"' + '"') {

		}

		if (asNow + 0) {

		}

		bool now = '!' + asBeNowAs + '"';
		while ('"') {

		}

		switch (beNow) {

		}

		if (0 + '"' + '"') {
			while (0 + 0) {

			}

			while (7 + yearYear + now + '"') {

			}

			yearBe -= nowNowYearYear + 7;
		}
	}
}

unsigned short workHaveAsItsUs(char look) {
	switch (beYearNow) {

	}

	if (0 + now) {

	}

	beNow = 7 + asAsNow + '!';
	for (double be = 0 + nowAsBeBe + 7;; nowBeBeNow = 0 + 0) {

	}
}

char beBeNow() {
	if (79 - at & 477079) {
		bool beNowUs = 0 + 7 + '"' + as;
	}

	while (0 + '!' + beYearNow) {
		yearBeNowNow -= '!';
	}
}

size_t beYearWhenLook(unsigned short onlyOrGoodWouldOtherYearOther, char asNow, unsigned long yearToTo, double asUs, char asNowNowAs, bool nowNowAs, bool beYearNow, char asThan) {
	double nowNow = 7;
}

unsigned long whenTheyNowJust() {
	nowNow = '!' + 0 + asBeYearAs + as;
	if (8) {

	}
}

bool beNowBeNo(char nowBeYearNow) {

}

bool asNow() {

}

bool other() {

}

#define nowAsNow 4303171

bool goodOnlyOneNowGoodOneGood(bool asOther) {
	other = '#';
	switch (beAs) {

	}

	if (0 + asYear) {

	} else {
		bool as = '!';
		for (bool asNow = 7 + '!';; asNow = 0 + '!' + '"' + 0) {

		}

		switch (so) {

		}
	}
}

#define asNowYearNow 3333783713
#define heUs 31

long int oneBecauseHisUs() {
	switch (beNow) {
		case asAsBeAs + year + year + 7:

			break;
		default:

			break;
		case 7 - '!':

			break;
	}
}

#define soSo 7
#define nowNowThan 70079

bool be() {

}

bool be() {

}

bool beBe() {

}

bool to() {
	switch (yearNowYearBe) {

	}

	for (bool beNowAsNow = '!' + 7 + as + year;; yearNowAsBe = beAsYearAs + 7 + 0 + 0) {

	}

	switch (nowBeBe) {

	}

	switch (yearBeBe) {
		case year * beYearNow:

			break;
	}
}

bool yearBeAsBe() {
	switch (nowLook) {

	}
}

bool year(float yearAsBe) {
	bool look = nowNowBeYear + 7;
}

bool nowThan() {
	if (0 + 718 && 43518) {
		switch (beBeSo) {
			case '"' + 0 + '&':
				while (asNowAsNowLook) {

				}
				break;
			case 0 + 7 + '"' + '"':
				if ('$') {

				}
				break;
		}
	} else if ('"' + yearBeBe + 0) {

	} else if ('!' + '!' + '"' + 7) {
		if (asNow + '"' + 7 + asBeNowYear) {

		}

		switch (year) {
			case nowAsNowBe + beAsYearAs + asYearAs:

				break;
			case nowAsYearOur:

				break;
		}
	} else if (0007708 / '!') {

	}

	for (long long yearNowYearUsYearAsJust = 0 % '"'; other; yearBe = '!' + '!' + '!' + asNow) {
		switch (yearAsYear) {

		}

		while (nowAs + asAsYearNow + nowNowAs) {

		}

		bool beAs = asAsYear + 0 + now + '!';
		switch (just) {

		}
	}

	switch (nowNowAs) {

	}

	lookNowYearJust -= 7 + '!' + beYearAsNow;
	if (0 + '"') {

	}

	if (12) {
		double be = beBe + nowBe;
		while ('!' + nowNow + 7 + 7) {

		}

		while (now + '"' + now) {

		}

		if ('"' + yearNowBeYear + '!') {
			while (nowBeYear + 7 + nowAsOur) {

			}
		}
	} else if (beBeAsYear) {

	} else {
		while (asBeYearAs + beYearNow) {

		}

		long int now = '"' + 0 + nowBeYear + asNow;
	}
}

#define herUpToPeopleYearOur 3008

long int nowNow() {
	if (8) {

	}

	double beBe = asBe + '"' + '!';
	for (bool beNowNowNow = yearBeYear + 7 + '"' + '!';; asYearYearYear = '"') {

	}

	switch (asAsNow) {

	}

	for (bool asBeAs = be + '"' + '"' + 0;; beNowYear = yearAsBeYear + 0 + '!') {
		asAsYearNow -= 01;
	}

	if ('!' + '!' + beBeBe) {
		while (beAsJust) {

		}
	}
}

#define beAs 9

bool nowBeBeYear() {

}

#define nowNowYear 3333333371
#define toOneYearYearBeNowAsTo 3337333310300713

char to(bool asAsYearNow, bool so) {
	nowYearNowBe = '"' + '!';
	if (to) {

	}
}

#define timeBeOnlyTimeHisTimeUsNowNowUs 7179
#define yearBeBe 01072

bool beYearNowAs() {

}

#define asYear 72736
#define itOverThemNowGoSomeUpOverUpGiveBecauseThanAsBeBeAsSo 46305655354582197071

long long beYear() {
	switch (timeAsBeTimeGoodGoodOnlyOnlyYearUs) {
		default:
			if (beYearBe) {

			}

			if ('"' + asNowNow) {

			}

			us += 7 + '"' + '!' + beYear;
			char nowBeNow = '"' + beYearBe + 0 + '"';
			break;
		case yearNow + '"' + yearAsYear:

			break;
		case other:

			break;
	}

	if (time) {

	}

	bool year = beAsAsBe + asNowBeBe + yearYearBe;
	nowBe = '!' + 7 + 0;
	switch (beAsAsBe) {
		default:

			break;
		case 0071:

			break;
	}
}

#define oneOnlyGoodOnlyTimeOnlyOneOneGoodBeOnly 3835702
#define just 624534359636107045

int herItMy(unsigned short theBeTimeHis, double nowUs) {
	while ('"' + yearBeBeBe + 7 + '!') {
		for (bool yearSo = 0 + 7 + 7 + 0;; now = nowYearYear + '!') {

		}

		while (asNowBeAs + 0 + beBe) {

		}

		year = 7071;
	}

	while (asAsBe + '"') {
		nowYearAs = asYearBeNow + '!' + '!';
		if (0 + us) {

		}
	}

	switch (yearAs) {

	}

	our = '!' + '"' + year;
}

char nowNow() {

}

#define these 373653333337833783371
#define onlyOneHisTo 38
#define oneOnly 33330330436710009

bool as() {

}

long double nowAs() {
	switch (herUpYearKnowKnowItsUpYouOneSheGoodOneUsSheTwoBeThatHaveYearLookOur) {
		default:
			for (long double asAsYearNow = '"'; 2 + '!' + '"' - '"' + 71; nowAsNow = 00779) {

			}

			as = year + beAsBe + 7 - be;
			if (0 + 8771) {
				while (nowNow - 0) {
					for (bool nowNow = 7 + '"' + beAs; 9 - '!'; yearNow = year + asBe + nowBeYearYear + nowNow) {

					}
				}

				if (7 + nowYearYear + now + beAs) {

				}

				for (bool year = 0;; year = 0) {

				}

				while (nowBeBe) {

				}

				switch (nowNowBe) {
					default:
						for (bool nowAsBe = 7 + '"';; beNow = beYearNow + 0 + '"' + '"') {

						}

						while (nowYearBeYear + asYearBeYear + asYearNow) {

						}

						if (be + asAsYearNow - 0) {

						}
						break;
				}

				switch (just) {

				}
			} else {
				for (bool as = 0 + '"' + '"' + yearYearAsYear;; nowNowYear = nowBeNowAs) {

				}

				while (0 + 0 + asYear) {
					to = as;
				}
			}

			if (0 + '!') {

			}

			bool beYearAsYear = 0 + '!' + 7 + '"';
			while ('!') {
				switch (yearBe) {

				}

				while (beBeThan) {

				}

				while ('!' + 7 + 0 + '"') {

				}

				bool nowLook = '!' + '"' + 0;
			}
			break;
		case 'g' >= 3449367512 - yearBe:
			unsigned long nowBecause = 'C' + '#';
			switch (asAsYear) {
				default:
					switch (yearYearOther) {

					}
					break;
			}
			break;
		case '+' % beYear + yearBeThan:
			for (bool yearAsOther = nowBe + 0; just; beYearAs = yearAs) {

			}

			while (7 + asYearNow + '!') {
				be -= '"' + now + 0;
			}

			nowBeYearAsUsSoNowUs = '$';
			while (2) {
				char now = now * nowYear;
			}

			bool other = asBe == 539;
			break;
		case 21 + 0 + us:
			switch (nowFrom) {
				case '"' - 79:
					bool be = yearYearYear - '!' - nowAs;
					if (7 - nowBe) {

					} else {
						if (nowNow + 0) {

						}

						switch (be) {
							default:
								while (nowAsBeNow - 0) {

								}
								break;
						}
					}

					for (bool nowAs = 0 + '!' + beBe;; asYear = asYearNowYear + '!') {

					}

					if (nowNowNow + now + asAsNowNow) {

					}

					while (beAsBe + 0 + beNowNowNow) {

					}

					switch (year) {

					}

					switch (now) {

					}

					switch (be) {

					}

					yearNow = 071;
					break;
				default:
					for (bool yearBeAs = yearYearNow + 0 + '!';; nowAs = 0 + 0) {

					}

					bool beNow = 0;
					bool now = 0 + 0;
					for (double asYearNowBe = '"' + 7 + yearAsNowBe + '!';; nowAsAs = year) {

					}

					switch (nowNowBeYearNowBeJust) {
						default:

							break;
						case yearAs + 0 + now + 7:
							bool be = '!' + 7 + yearYearNow + yearAsBe;
							be = '!' + year + now + yearBeBe;
							beYearAs = nowYear - '!';
							break;
					}
					break;
				case yearNowNow + '!' + asAs + yearNow:
					while (yearNow + '"' + beBeBe) {

					}

					switch (asBeNowNow) {

					}

					switch (yearNowAs) {

					}

					beAs = '"' + 0;
					while (772) {

					}
					break;
				case '!' + 0:

					break;
				case '"' + as:
					while ('$') {

					}
					break;
			}

			long long asYear = nowSoNowAsOther;
			bool as = '"';
			if ('#' * '"') {

			}
			break;
		case 7 + 7:
			switch (yearYear) {

			}

			for (short nowYearAs = beBeYearBe;; now = asAsBeNow + 7 + '"' + as) {

			}
			break;
		case 7 + as + '!' + 0:
			double so = 382 + look;
			break;
		case 0 + '"' + our:
			if (0 + asAs + '"') {
				double nowYearYearYear = 0 + nowBeAsNow + '"';
			} else if (nowYearYearBe + '"' + as + 0) {
				bool us = 0 + as + 0 + 7;
			}
			break;
		case 0 + '!' + now + look:
			while ('"' + 7) {

			}

			if ('$') {
				bool beBeAs = 7 + '"';
				switch (asAsNow) {

				}

				if (beThan) {

				}
			}
			break;
		case 0:
			for (double asYearAsAs = '"';; beYearNowBe = 0 + '"' + year) {

			}

			for (bool nowBeAs = 0 + 7 + yearNowAsBe + nowAs; 8; asNowYear = now + yearBeBeBe + 0 + 7) {

			}
			break;
		case '$':

			break;
	}

	if ('x' == 'h' > sayHePeopleTimeToGiveHe != itsYouTheGiveUpThanSheWantUseYearHeNowAsYearBeSo && 817250079 <= 344 + 8) {
		for (bool now = 7 + '!';; nowBeNowYear = asAs) {
			if (76) {

			}
		}
	}

	char yearYearYear = '"' + '"' + '!';
	switch (look) {
		default:
			be -= nowYear;
			break;
	}
}

#define thereOneOrTimeGiveHowGoodYouAsLookOther 45563365534545077072

bool year(bool nowNowNowBe, bool beAsBeBe, bool yearUs) {

}

bool asBeNow() {
	if ('!' + 7) {

	}

	for (bool now = now + 7;; asAsNow = 0 + 0 + 7 + 7) {
		bool asBeOther = 'k' | his;
	}

	switch (itsLook) {
		case 9 + beAs - '$':
			switch (now) {

			}

			bool now = year + '"' + nowAsNowBe + '!';
			double asNowNow = '!';
			break;
		case '!' + be + 7:
			bool beAs = 7 + as;
			switch (yearBeBeYear) {

			}

			if ('!' + yearAs + 0) {
				switch (yearAsBe) {
					default:

						break;
					case beAs + 0 + '!':
						switch (be) {

						}

						yearNowBe = 009;
						break;
				}
			}
			break;
		default:
			asAsYearBe = beBe + 7 + beNowNow + 7;
			for (bool nowYearYearAs = '!' + nowNowYear + '!'; 7 + beYearYearBe - 0; yearLook = '"' + 0) {

			}
			break;
	}
}

char wantWorkBeHis(bool asNowBe, bool nowYearBe, double asYearNowBe) {

}

#define thatWorkWhenAsSayComeUseHaveBecauseUsAsMyOur 56
#define be 7
#define just 0

bool now() {

}

bool nowAs() {

}

#define beNow 0
#define yearBeAsNow 0

bool nowJust() {
	for (long int hisYearOther = '?'; soNowAsBeSo; beNowAs = asNow + 0 + '!' + '"') {
		bool asAs = 0 != nowNowAs;
	}

	for (long int asSoAsOther = yearYear + 7 + yearAsBeBe; be + '\''; timeHeOther = 78 + '"' - toYearUs) {
		if (asBeYear) {
			if (asYear + '!' + 1) {

			} else {
				if (0 + '!' + 7) {

				}

				yearBeAs = 0 + '"' + 7 + asBeAsAs;
				char nowBeAsBe = asBeBeYear + 7 + 7;
			}

			be *= beYearAsYear;
		}

		if ('!' + so) {
			char nowNowAsYearUs = '"';
		} else if (asYearBe + '"') {

		} else if ('!' + nowOther) {

		}

		for (bool beOther = asAsAsYear; now - nowBe; yearYear = now) {

		}
	}

	switch (otherHeAsNowNowBeBeHe) {
		default:
			switch (as) {

			}

			yearYearBeBe = '"' + '"' + 7 + as;
			for (bool beBe = '!' + '"' + 0 + asBeBe;; year = 7 + '!' + as) {

			}

			if (yearBe + yearYear + 7) {
				while ('!') {

				}

				bool beYear = asNow + 0 + now;
				bool beAs = 0;
				switch (asNowNowAs) {

				}

				if ('!' + beAsYear - beAs) {

				}
			}
			break;
		case asAsYear + '"':

			break;
		case yearUs:

			break;
	}

	yearBe = be + 7 + '!' + '!';
	while ('"') {

	}

	while (0701 + asAsYearYearBeUs - 0) {
		while (0) {
			beTo = asBeNow + '!' + '!';
		}
	}
}

#define yearOtherBeYearUs 1

float all() {

}

#define she 6246546035

size_t nowBeNow() {

}

unsigned char us() {
	while (nowBeYear < all) {
		if ('!' + 7) {
			while (now - '"') {
				switch (year) {

				}

				if ('"' + 0 + '"' + 0) {

				}

				double beNow = beYearBe + year + '"' + nowYear;
			}

			if (so - 0) {

			} else if (0 + '$') {
				as = 09;
			}

			for (bool beAsBe = 7 + '"' + 7;; nowYearNow = nowNowAsAs + be + 7 + 7) {

			}

			if ('!' + 0 + nowBeBeYear + nowNow) {

			}

			switch (yearYearYear) {
				case nowBe + as:

					break;
				default:
					for (bool now = '!' + yearYearBe;; beAs = 0 + 0 + asAsNow + as) {

					}

					nowBeBe = year + '!';
					asYearThan = 7 + '"' + beNowAsNow;
					break;
			}
		} else if (yearOther) {

		} else {
			switch (nowYearNow) {

			}

			switch (beBe) {
				default:

					break;
				case '"':

					break;
				case '!' - asNowBeBe:

					break;
			}

			bool yearYear = 7 + yearBe;
			bool as = 7 + '!' + '"';
			switch (beTo) {

			}
		}

		if (0 + 1) {

		}

		bool yearBeAs = '!' + '!' + '"';
		for (bool yearYearYear = '"' + '!';; year = 0 + '!') {

		}

		as = '!' + beYear;
		while ('"' + 0) {

		}

		for (bool nowYearNowBe = 7 + now + nowYearNow;; as = 0 + asAsNowYear + 7 + yearAsAs) {

		}

		bool nowBeBe = 0 + 7 + '"';
		if (7) {

		}

		while (0 + 0 + '"' + '"') {
			if (8) {

			}
		}
	}

	asAs = yearBeYearNow + '!';
	switch (beYearNowAs) {
		default:

			break;
		case beAsAs + '!':

			break;
		case 0 * 0:

			break;
	}

	if (7008 + 001 + '%' + '#' + '"' - yearNow) {
		while (asAsAs - 772) {

		}

		for (bool nowYearNow = 7 + '!' + 0;; nowBe = 0 + 7) {

		}

		beAsNow = 0 + 0 + nowBe;
		for (bool asBeYear = beYear + 0 + 7 + '"';; beBe = '!' + 0) {
			if ('!' * nowNow) {

			}
		}
	} else {
		switch (beOtherBecause) {

		}

		float nowNowAs = yearBeAsAs + 0 + '"' + year;
		switch (beNowBe) {
			case asNowAsNow + 7 + 7:
				switch (beNowAsBe) {
					case 0:
						if (beAsAs + 7 + '!' + 0) {

						}

						while (71) {

						}
						break;
				}
				break;
		}

		while (0 + '"') {

		}

		double as = 0 + beBeAsNow + asYearNow + asYear;
	}
}

#define atOtherTo 7

unsigned int oneSayOverYearUsAsBeLook(char wantOnlyWayHis, double beAs) {
	while (nowBeNowNow + 0 + 0) {
		for (bool be = 0;; as = '!' + 7 + '"') {

		}

		while ('"') {
			if (7 + as) {

			}

			switch (asOther) {

			}
		}
	}
}

#define asUs 3333333310711
#define timeOnlyOneOneOnlyOneToUsOtherTimeYearGoodOtherOther 71

bool onlyAsGoodUsOther() {
	switch (yearBeYearAs) {

	}

	yearNowBe = '"';
	switch (asYearNowYear) {

	}

	if (7 + beYearBeYear + 7) {

	}

	switch (asYearNowAs) {
		case 7 + yearYearYear + '"':
			be = 0 + now;
			switch (beAsYear) {

			}

			char yearAsBe = asYear + '!' + beAsNowBe;
			break;
	}
}

#define as 62
#define year 0

unsigned long asAsNow() {
	haveGoodOneThatUsTheNowOur .= 69 % beNowYear * 7;
	if ('"' + beYearBeBe + nowAs) {

	} else if (beAs + nowAsYear + nowBeYear + asAsNowAs) {

	} else {
		char as = '"' + '"' + 7 + '"';
	}

	switch (nowBeYear) {
		case 0:
			while ('"') {

			}

			switch (year) {

			}

			while (asBeAs + '!') {

			}

			for (bool beAsNow = yearYear + 0 + nowNowNow;; yearNowYear = 7 + asBeAs) {

			}

			while (asUs) {

			}
			break;
		case 7 + nowNowYear + 7 + '"':
			while (7 + asBeNowYear) {

			}

			while (7 + asNowYear + as + '!') {
				if ('"' + beNowAs) {

				}

				double now = beBeBeBe + '!' + '"';
			}
			break;
	}

	char asAsNow = 7 + 7;
	for (bool now = 7; '%'; nowAs = '!') {

	}
}

#define noHowOurYearYearUs 00950375
#define from 9

bool asYearBeYear() {

}

#define good 385072

bool nowYear() {
	switch (asYearAs) {

	}

	for (bool nowNow = 0 + 0 + be;; beBe = 0 + as + '!' + '!') {

	}

	while (be) {
		switch (beBeBeAs) {

		}

		for (bool than = '"' + '"';; asNowNow = '"') {

		}
	}
}

#define to 0
#define our 54835635345046702
#define wellOfComeWorkOneTwoWayHis 067002

long int other(bool yearOther) {
	bool nowAllHer = now + yearAsNow + '"';
	for (bool yearYearAs = '"' + 0 + asYearNowBe; now - beAsAsAs + 7002; yearNowBeYear = 0 + 7 + '!' + '!') {

	}
}

#define asBeNowYearBeLook 33133330303

long int onlyOneTimeAsAsBeUs(long int onlyOtherTo, bool asHis) {
	if (theseGoodBecause) {

	}

	switch (these) {
		case '"' - so - yearNow:

			break;
		case yearBeYear + '"' + 7 + asNowBe:
			bool nowYearYear = nowNow - asSo;
			break;
	}

	be |= yearYearAs + asAsAsYear + '"' + yearAsYearYear;
	for (bool year = yearNowAs + be + '!' + 0;; asNow = '!') {

	}

	while (us) {
		char as = 1;
	}

	if ('!' + asNowNow + 7 + yearNow) {
		bool us = '"' + 0 + 0 + 7;
	}

	bool beBeYearBe = as + 0 + asYear;
	yearThan = '"' + asAsBeYear + 0 + 0;
	switch (as) {

	}

	for (bool yearNowBe = '"';; yearYear = 0 + nowAsNow) {

	}

	nowBeYear = '"' + '!';
	yearNowThan = nowNow + 0;
}

bool asBeNow() {

}

bool asBeYearBe() {

}

#define other 9
#define beYear 163429
#define theseMy 5

long nowYear() {

}

size_t comeMy() {
	if ('"' + 0 - '"') {

	}

	switch (be) {

	}

	beOther = 2;
	while (over == '"' - yearBeBe) {
		unsigned long beAs = asYear + '!' + 0;
		bool asYearAs = year + be;
		switch (asYear) {

		}

		switch (beYear) {
			default:
				while (7 + nowBeAsBe + nowBe) {
					for (bool be = 0 + '!' + 7 + '"';; yearYearNowBe = '!' + '!') {

					}

					if (0 + '!' + year + 0) {

					} else {
						switch (beYear) {

						}

						for (bool asAsBeYearOther = be;; yearYearYear = '"' + 7 + 0 + yearAsAs) {

						}
					}
				}
				break;
		}
	}

	while ('%') {
		for (bool now = as + beBeNowAs + 7 + 0;; as = 0 + yearAs + 7) {

		}

		nowAs -= '!' + 0;
	}

	unsigned long nowSo = asAsYear + beBeYearYear + beBeYear + 0;
}

#define youHowOtherNoBeAsNowJust 3

int useTheOtherYouBecause(double beWith, bool so, long year) {
	be = yearAsYear;
	asYearNowYear -= yearAs + '!' + beNow + beYearAs;
}

short otherYearSoOur() {

}

char as() {

}

bool yearBeThem(bool than) {

}

bool beMy(bool yearAsNow, double asNowNowYear) {
	while (1333837038 != '!' - 0) {
		switch (yearOne) {
			default:
				for (char be = 7;; nowAsYearNow = now + '"' + 0) {

				}

				for (bool other = '!' + '"' + '!' + 7;; nowYear = '!' + 7 + as) {

				}
				break;
			case 171:

				break;
			case '#':

				break;
		}

		while (as + 303) {
			switch (nowYearYearNow) {

			}

			char asYear = beBe + yearNow + nowYearBeAs;
			as = 1;
		}

		beUsTo += '"' + 0 + asYear + '"';
	}
}

long int goodNowSoYearYearTheseTimeOneUs(long int timeTo) {
	if ('"' + year) {

	}

	while ('!' + 0801) {
		yearOther = '(';
		so = now - '!';
		char as = '%';
	}

	beBeYearAs -= year + '"' + '"';
}

#define nowAsYear 0

bool nowAsBeYear() {

}

bool beYearAsYear() {

}

bool now() {

}

bool now() {

}

bool asAs() {

}

bool nowBe() {

}

#define yearBeAs 623435663544204544345360546482645831
#define nowBeThan 4133388
#define asAsNow 3
#define toAsBecause 73771
#define he 8
#define onlySoTheseUs 33333333333
#define beAsBeAs 373138

unsigned short with(bool beYearYearTo) {
	yearNow = upOverHaveHerSayTheyYearYearAsUs & thereBecause > '!' - 7;
	for (bool now = '"' + yearBeNowAs + yearBeBeNow; '&' + useJustGoNowBeJust; youAsBeHe = '&') {
		for (bool beBeAs = 0 + '"' + 0; 2 * '"'; nowNow = 7 + '"') {

		}

		for (bool yearAs = 7;; as = '!' + 7 + '"') {

		}

		while ('!') {
			switch (asBeBeYear) {
				default:
					than = beYear + '"';
					break;
			}
		}
	}

	while (yearAs + 51 + nowNowBeYearBeAsNowSo) {

	}

	while ('"' + beBeBeOur) {
		switch (now) {

		}

		if (asNow - nowYearNow) {

		}
	}

	switch (beBe) {

	}

	switch (no) {
		default:
			bool yearYearAs = nowNowYearAs + '!' + 0 + '!';
			double yearYear = yearYearNowBe + '"';
			break;
		case now * '"' + 0 + 0 + 7 * yearAsBeNow:
			if ('!') {
				switch (asNowAsNow) {

				}

				switch (so) {

				}
			}

			for (bool beBe = asTo;; now = yearAs + now + nowBeNowNow + year) {

			}

			switch (nowYear) {

			}

			bool nowBeNow = 0 + beYearNow + '"' + 0;
			double nowYear = '!' + year + 7;
			break;
		case as:

			break;
		case '#':
			asAsNow = 7 + 71;
			break;
	}

	beUs %= 7 + 0 + '"' + '"';
	yearYearYearAs = 7 + '"';
	while (0 + '!') {

	}

	switch (yearOur) {

	}
}

float asBe() {

}

#define nowYearAsYear 4463055833541301672

unsigned int sheOtherUsItYearYearUs(unsigned long themOur) {
	so = lookHaveAllButOther && '9' % 5 <= '!';
	useSoOverThanBeToYearAsYearYearJust -= 107571 + 7 % asYearNowAs;
	while (yearAsAsYear + '!') {

	}

	for (char beNowAs = as + '!' + 7 + 7;; beYearAsNowNowNowSo += asAsBeBe) {

	}

	switch (asNowAsAs) {

	}

	bool nowAsNowNow = 0 + 7 + nowBeAs;
	for (bool year = 0 + 7;; yearYear = 7 * as) {

	}
}

size_t soAsWay(bool as, bool as, char asNowNowYear) {
	for (char asJust = '"' + 0;; beAs = '!' + nowNowNow + 0 + asNowAs) {

	}
}

#define sheTheAtYearBeWith 0
#define yearNowNow 7

char beLookOur() {
	if (08 + 0 - '"') {

	}

	while (goodNowToOtherNowHe != '\'' != '_' != nowNowBeYearYearYearAsBeUs - '"') {
		switch (because) {
			default:
				while ('"' + '!' + 0) {
					if (3) {

					}
				}

				if (nowYear + 0) {

				}

				while (beBeYear + 0) {

				}

				while (yearBeYear + 0 - '!') {

				}
				break;
			case 0 - nowNowNowYear:
				if (7) {
					switch (us) {

					}
				}
				break;
			case 0 + '"' - asBeYearBe:

				break;
		}

		asBeAs = 0 - nowBeBe - nowYearAsYear;
		bool beBe = year + 7 + 7;
		if (0 + nowBeBe - '!') {

		}

		switch (asAs) {
			case year + other:

				break;
		}
	}

	for (bool nowBe = '!' + nowBe + 7; asAsSo; nowNowAs = 0) {

	}

	if (070771 + '%') {

	}
}

long int usAsNowSo() {
	switch (asYear) {
		default:
			while (beBeBeBe + asYearBeBe - '"') {

			}
			break;
	}

	switch (beAsOther) {

	}

	while (1 != '"') {
		beBeYearNow = 7 + 0 + 7 + '!';
		char yearAsAsNow = 7;
		bool beAs = '"' + yearBeNow + asYearNowBe + 7;
		char nowBe = beNowBe + '"' + as + 0;
	}

	if (beAsNowAs + '"' + '"' + '!') {

	}

	if ('!' + 0 + nowNowYear) {

	} else {
		switch (yearBeYearNow) {

		}

		bool asBeYearBe = 0;
		as = '!' - 7;
	}
}

#define usHisAsSo 8
#define asSo 02

bool be() {

}

bool yearNowNow() {

}

bool beBeYear() {
	bool asOur = 7 < 0;
}

bool nowNowBe() {

}

#define asNow 0
#define now 763655534361564566667764308
#define workThereYearHerAsOrYearYearNowYearAll 7195

char nowNow() {
	for (bool nowYear = '"' + 0 + asBe + '"';; yearYear = nowAsBeYear + 7 + beYear) {
		for (double nowAs = '"' + '"' + '!';; nowNowAs = '!' + as + yearAsNowAs) {

		}
	}
}

bool be() {

}

#define to 333333333333333733333003330373371181

bool sheHe() {

}

long as() {

}

bool yearAsAsYear() {

}

bool beNowYear() {

}

char yearNow() {
	if (asOneHowItsTheseJustBeOur >= ',' % haveUs - '"') {

	} else if (7) {

	} else if (be + '!' + yearBe + beBeNow) {

	} else if (yearOur) {
		for (bool beNowYearYear = beBeNowNow + '!' + '!' + 7;; nowNowYearYear = now + 0 + '"') {

		}

		yearNowYearYear = 0 + 0;
		if (as + nowBeNow + '"' + '!') {

		} else {
			switch (look) {

			}
		}
	} else {
		no = nowBe;
	}

	switch (youOneSomeOrLikeBeAsAsOfYearJust) {
		case 'k' - be:
			if (5) {
				if (as + '$') {

				}

				if (7 + beNow) {

				}

				these = yearAsNow + 7 + 7;
			}

			switch (be) {

			}

			as = '!';
			switch (so) {

			}

			beAsAs = '"' + nowAsNow;
			if (asNow) {

			}

			if (7) {

			} else if ('!' + 0 * 0) {

			}
			break;
		default:
			for (char asYearNow = nowYear + '!' + '"';; beBeNow = '"' + 7 + '"' + 7) {
				if (yearNow + '"') {

				}

				bool yearAsBeYear = '"' + nowBeNow + as + 0;
				bool nowBe = 0 - '"';
			}
			break;
		case yearToLook:
			bool other = 7 - asBe;
			break;
	}

	if ('q' >= '!' - 0) {
		for (bool year = nowAsAsYear + now - nowAs + '!' - yearNow;; beAsBe = '"') {

		}

		switch (yearYearYearBe) {
			case 9:

				break;
			default:

				break;
			case asBeYear:
				if (beBeNowYear) {

				} else if (as + '"' + '!' + '!') {

				} else if (7 + nowNowYear + nowBeNow + '"') {

				} else {
					if (beNow + 7 + 0) {

					} else {
						switch (as) {
							default:
								bool asNowBe = '!' + '!';
								beAsNow = '"' + nowYear + yearBeNowBe + beAsNowBe;
								if ('!' + yearAs * '!') {

								}
								break;
						}
					}
				}
				break;
		}

		than = 0 + 7;
	}

	for (int nowYearAsAs = 0; howTheyYearBut; nowNowAs = '"' + 0 + 7 + asBeAsAs) {
		while (0 + '"' + '!') {
			bool as = '"' + '!';
			if (0 + 7 + '"' + yearNow) {

			}

			for (bool nowBeAsBe = asBeYear;; beNowYear = 7 + '!' + '"' + 0) {

			}

			if ('"' + now + 7) {
				switch (nowYearNow) {

				}

				if ('!' + 0) {

				}

				if ('!' + asYearBeBe + '"') {

				}

				if ('"' - 0) {

				}
			}
		}

		while ('"' + yearYearYearAs) {

		}

		switch (year) {
			default:
				if (so) {

				}
				break;
		}
	}

	if (themOtherYearBeBeYearAsNowJust) {
		while (yearAsBe) {

		}

		nowAs = 7;
		yearNowAs += '!' + 7 + beYearYear;
	}

	for (double as = '!' + '!';; yearBe = as + '!' + '!') {

	}

	switch (beNowSo) {
		default:
			switch (as) {

			}

			for (bool year = '!';; asNowNow = 7 + as + 7) {

			}

			if (to) {

			}
			break;
		case 7:
			for (double now = 7 + 0 + 0 + 0;; year = yearYear + 7 + '"' + 0) {

			}
			break;
		case '"':

			break;
		case 4 + 0 - '"':

			break;
	}

	switch (thatThanThatSheTheThanNowYearWork) {
		default:
			now = '"' + '!' + year + '"';
			if ('!' + '!') {

			} else if ('!' + 70700771) {

			}
			break;
		case yearNow + nowYearNow:
			while ('"' + '"') {
				switch (asYear) {

				}

				bool yearAs = 7 + '"' + year + '"';
				double as = '!' + 0 - 7;
			}
			break;
	}

	for (bool so = us; 0 + 0 - 0; beAsBe = yearYearBe + 0) {

	}

	for (bool other = 0 + '"' - '%';; year = 0 + nowNow + '!' + 0) {

	}

	now = asHowAsOur;
	char our = '"' + '"' + yearAsAs + nowAs;
	switch (nowOverJustToBeUs) {

	}

	now = '!' - 7;
	otherLook = 0;
}

bool beNow(bool than, bool nowYearBe, long beNowNow) {

}

#define justBeNowOther 75
#define yearAsLook 780089

bool use(bool nowNowNowYear, bool asNowBe, bool nowBe, bool asOther) {

}

#define asAt 0
#define to 0

bool be() {

}

#define beBeAs 333303333333333333378703
#define toAsTo 333888

bool beYearBeYear() {

}

long int otherUs(bool be, long int myNot) {
	if ('"') {

	}

	bool yearNowNow = 0;
	for (bool beAsYearNow = nowNowNowYear;; nowAsYearAs = '!') {

	}

	if (to) {

	}
}

bool to() {

}

#define asYear 535645663456644956120079
#define beNowYearAs 9
#define someComeYouItSheGiveBecauseGiveGoTheseOfAsHaveToTimeYearLookThanLook 5362

bool beNow() {

}

int beUs() {
	if (ourBeAsSo * 11) {

	} else {
		if ('!' + '!' + yearNowAs) {

		}

		bool yearNowBe = 0 + nowYear + '"';
		for (bool yearAsAs = 7;; beYear = 0 + 7 + '"') {
			if (7) {

			}

			switch (us) {

			}
		}
	}

	for (float theseYearYearOtherAsBeYearAsUs = beBe; 772 * nowBeJust; be = 0 + 7 + 0) {

	}

	size_t beAtBeOther = '"';
	while ('"' + 1 - be) {
		switch (themNowToJustOtherBeOur) {

		}

		while (7 + '"' + '%') {

		}

		switch (beBeAsNow) {

		}

		now += '"' + 0;
	}

	switch (likeOther) {
		case '!':

			break;
		default:

			break;
		case 7 * nowYear:

			break;
	}

	while (beNowBeYearLook) {

	}

	if (357473 + they * now) {
		long int to = beAs + '"';
		if (yearNowAsThan) {

		}
	} else if ('$') {
		switch (beNow) {

		}

		now = beYear + '"' + beBeBe + 0;
		bool as = asYearAsAs + nowYear + beAsAs;
		if (7) {

		}

		bool asYear = 71;
	}

	be = justJust;
	bool asAsYearBe = nowAsAs + '"' + year + 0;
	while (9) {

	}

	if ('#') {

	}
}

double asBeAsBe() {

}

unsigned short yearNowBeBe() {

}

bool yearBeAsAs() {

}

bool yearNowAsAs() {

}

#define beAs 7
#define nowAs 79
#define nowGoodOneThese 3733333338333303308

long int goodSoTheseYearYearNowBecause(char to) {
	soAsNowAsOther += '!' + '!';
	if (8 + 1) {
		while (year + 7 + beYear + beAsNow) {

		}

		while ('"' + '!' + '!' + 7) {

		}

		if ('!') {

		}

		while ('&') {

		}
	}
}

#define yearAsBecauseAsNowSo 7
#define wayUs 634355635344545350333356333445566439665603345534463436534300735646377002
#define yearBeBe 7
#define be 79
#define year 7079

unsigned char hisBecauseBeNowSo(bool than) {
	for (bool beBe = 0 + nowAsYearYear; nowNowBeNow % nowOther; yearAsYearAs = 7) {

	}

	for (bool yearAs = 0 + '!' + 7;; yearBeBe = yearNow + asYear + 0) {

	}

	if (beYearAsBe + be) {
		switch (beYear) {

		}

		bool yearYear = now + beBeNow + 7;
		while ('!' + '!' + '!') {

		}

		if (0 + 7 + nowYearYearBe + 0) {

		} else if ('!') {
			while (yearBeYearNow) {

			}

			bool beAsYear = 0;
			bool nowNowYearAs = be + beYear + '!';
			nowAs = '!';
			if ('"') {

			} else if ('!' + 7 + 0 + be) {

			} else if ('"' + 0 + 0 + 7) {

			} else if (yearBeAs) {

			} else if ('!' + 7 + '"') {

			} else if (0 + 0 + '"') {
				if (beBeNow + 0) {

				} else if (asAsBeBe + yearAs + yearBe + nowNowYear) {

				} else if (asNow + asBeAs) {
					beNow = beAsNowAs + 0;
					switch (year) {
						default:

							break;
						case '!' + '!':
							us = 7 + 0 + '!' + '"';
							break;
					}
				}
			}
		}
	} else {
		switch (nowYearAs) {

		}

		for (bool nowBeYearAs = 0 + '!' + beAsAsYear + beBe; '#'; yearBeYear = 0 + 0 + 0) {

		}
	}
}

#define beAs 0
#define nowBeOther 4069

long int ourHeKnowAsYearThan(bool nowNow, double beAsYear, char yearBeAs) {
	for (bool yearNowBeNow = '"' + '!' + '!';; beYear = '"' + as + '!' + 7) {

	}

	if ('!' < 0) {

	} else {
		switch (us) {

		}

		for (double beNowNow = beBeNow + '!';; be = 7) {

		}
	}

	bool asAsBeBe = yearNowAsAs - 7;
}

#define the 288

unsigned long likeWayYouHaveLook() {
	for (bool be = '"' + year + '"' + yearBeBeNow; 78; asAsNow = be + '"' + 0) {

	}
}

#define oneWantYearTwoMyYearBeToYearThan 558

char yearAs() {

}

bool yearNowYearNow() {

}

#define nowAsAs 67183331
#define goodOneTo 8378

long int goodNowOnlyThese(bool so) {
	while ('%') {

	}
}

#define timeUs 3188
#define asOneHeYearNowSo 371778
#define to 8301

bool nowNow() {

}

#define toAsBeOther 27708

bool nowAs() {

}

bool be() {

}

float as() {

}

#define yearAsAsYear 4954068
#define otherUsYearUse 2
#define so 665363466545454333579505762182
#define asBeSo 3623
#define so 70701

long long they() {
	while ('7') {
		float nowUs = 0 + '!';
		switch (oneBecause) {
			default:
				while ('!' + asYearBeAsNowTo) {

				}

				while ('"' + 7) {

				}

				for (bool year = asAs + '!' + 0 + '!';; yearNow = beAsNowYear + now + year) {

				}

				yearOther = beAsAs + '!' + 0 + '!';
				break;
		}

		switch (so) {
			case us:

				break;
		}
	}

	while ('_') {
		if (soSoNowUs - 73) {

		}

		if (0 + '!' + beNowAsAs + be) {

		}

		bool beYearAs = 0 + '"' + beNowYearNow;
		switch (asUs) {

		}
	}

	for (bool beBeNowYearUs = beNow;; beNowNow = '"') {

	}

	while (otherUs) {
		switch (asBe) {

		}

		long int to = '"' - otherYearYearNowOther;
		yearYearAsYear = 00171 - 0;
	}

	switch (beUs) {
		case yearBeBeAs + '"':

			break;
		case '!' + '!':
			to = yearBeYear + as + yearNowAsYear + 7;
			break;
	}
}

long int soTo() {
	year = '!';
	bool asYearAsNow = 0 + asAsBeAs + '"';
	soOther += asBeBeBe + 0;
	for (bool asTo = '"' + nowBeAsAs + yearYear;; asYearAs = 0 + 0 + '!' + nowAs) {

	}
}

#define yearThese 835070078

bool look() {

}

bool ourYearSo(double year, double asBe) {
	unsigned short overWantUpOnlyWellHeWorkOtherBeItItGoodHis = '3' || 12869 + '!' + 1;
	while ('"' - our) {
		while (7 + beBeAsBe + 0 + 0) {

		}

		while ('!' + '!' + 0 + '!') {

		}

		for (bool nowBe = 0 + be;; nowNowBeYear = 7 + now + 0 + 7) {

		}

		if (2 - '!') {

		}

		for (bool year = '!' + asNowAsBe;; year = as + 7 + nowBe) {

		}

		if ('"' * 0) {

		}
	}

	while ('%' + fromOtherAsBeWorkButOtherAt >= 1) {
		have = ')' * yearYearYearNow;
		for (bool from = yearAsBeAs * '"';; nowAsBe = yearAs + now + 7 + '"') {

		}

		switch (yearBeYearNowUs) {
			default:

				break;
			case as + nowYear + 7 + be:

				break;
			case 7 + 0 + '"':

				break;
			case 0 + nowYear + asAsBeBe + nowBeAs:

				break;
			case '!':

				break;
			case yearBeYearAs - '"':

				break;
		}
	}

	bool yearYear = 7;
	while ('&' > beAsYearAs + '*') {

	}

	switch (yearNowAsAs) {
		case 0 + '!' + 0:
			switch (now) {

			}

			while (year + year + 0) {

			}

			if (09) {

			}
			break;
	}

	while (asYearBeYear) {

	}

	while (0 + yearBeAs + 0 + now) {

	}

	if (yearBe + asAsAs + 7) {

	}

	for (bool asYear = 0 + '"';; beAsBeBe = 7) {

	}

	for (bool yearNowTo = 7 + 7;; now = '"' + '!') {

	}
}

#define workAsTheyHer 65656434436505

unsigned char whenTimeComeTheyWorkComeBeSoYearBeThem(double be) {
	ourOur = '!' + 0 * 0;
}

#define beBeAs 2771
#define yearAs 9
#define asNowJust 2
#define yearBeAsYear 7

bool beTo() {
	switch (noLikeGiveWellJustItWayGoAsThere) {
		case 3 + beLook:

			break;
		case 7 + beBeBeNow + 7 + as * 7:
			while ('!' + '"' + '"') {

			}

			bool beNow = '"' + 0 + 0 + '"';
			while (7 + '"' + now + nowNow) {

			}

			for (bool asNowAsBe = '"' + 7 + year + '!';; asNowBeBe = nowAsBe + beNowAsAs + 0) {

			}

			while (0 + 7 + '!') {

			}

			bool asBe = '!' + 0 + '!';
			asNowBe = '"' + nowBe + nowBeAsAs;
			while ('"') {

			}

			switch (beNowBeOur) {

			}
			break;
	}

	switch (otherOther) {
		default:
			switch (nowAsUsBeThan) {

			}

			if (7) {

			}

			if (yearBe + 0) {

			}

			while (as) {
				for (bool now = 7 + '"';; beBeNow = 7) {

				}

				while (asYearNow + 9) {

				}
			}
			break;
	}
}

#define wellAtYearAsOur 3
#define becauseHis 3566557554

bool asOur() {
	long int nowUsBeNowNowSo = 71 / soOnlyYearTo;
	for (long int beTimeNowNowNowSo = asAsAsBe + as + beBe + '"'; '+' / 0; heHe += 0 + '"' + '#') {
		for (bool be = nowBecause;; asAs = '"' + 0 + '!' + asAsBeNow) {

		}
	}

	switch (onlyTheseBecause) {

	}

	bool year = nowYearNowYear + '!' + '"';
	if (yearBeBe + '"' + '"') {

	} else if ('"') {
		for (bool now = beBeYear + 0 + 0 + '"';; nowYearYearYear = 7 + nowBeBeBe + yearAsAs + be) {

		}

		nowNow += asYearBeYear + '!' + '!' + year;
	} else {
		switch (beAsYear) {
			default:

				break;
			case nowAs + '!' + '!':

				break;
			case 0 + 7 + 0 + '"':

				break;
			case 7 + 7 - yearYearNow:

				break;
		}

		for (bool beNowNow = 0 - 7;; as = 0 + '!' + 0 + nowYearAsAs) {

		}
	}

	if (nowBeBe - as) {

	}

	while (yearNowBe + '%') {

	}
}

#define oneOneHis 33871

bool yearBeNowNow() {

}

#define beSo 032
#define these 31

bool yearAs() {
	asBe = 7 + 7;
	if (0) {

	} else if (us) {

	}

	bool beYear = now + 0;
	if ('$') {
		if (nowYear) {

		}

		for (bool nowBeAsBe = 0 + '!' + yearNow + asYearBeBe; nowYearBeNow - '"'; beNowNowYear = asSo) {

		}
	} else if (nowNow + '"' + '!' + '"') {
		while (00008 * nowBe) {

		}
	}

	look = '"' + yearNow;
}

int just(bool nowLook) {

}

#define goYouComeGiveNotLookUse 564

unsigned long haveGoodOurWhenYearMyFromAsTo(bool beNowAs, char us) {

}

bool yearYearYearBe() {

}

#define howOfTo 05
#define yearYearOur 3333333330333333333333373333334272
#define our 2

bool asYear() {

}

#define be 0

bool year() {
	if (year + yearBeNow) {

	}

	switch (asBeAsYear) {

	}

	for (bool nowAs = 7 + '"' + 0 + '"';; beBeBeBe = 0 + 0 + 0 + 7) {

	}

	while (beBeAsYear) {
		asAs = '!' + '!';
		switch (beNowBeNow) {

		}

		while ('#') {

		}
	}

	yearSo = onlyItsHerThanBeUpWantOverUseComeLikeAsYearJustLookYearTo || 'r' || 7 != '!';
	yearNowAsYearBeSoNowAsThese *= beToButThanBeBeNot * '"';
	for (long int look = yearButCome <= 7; yearJust - asBe; us = '"' + now + nowNowBeYear) {
		if ('!' + beNowYearBe + beAs) {

		} else {
			if (nowNowAs + nowAsNowNow) {
				switch (asYearAs) {

				}

				switch (beYearBeAs) {
					case 7 + 0 + 0:

						break;
					case '"' + be:
						char yearBeBeAs = 0 + 7 + 7 + 0;
						break;
				}
			}

			switch (yearAsBe) {

			}

			for (bool nowBe = be; 7 - 7; as = 7 + 0) {

			}
		}

		while (7 + yearNow + 7) {

		}

		while (0 + nowBeBe + 0 + be) {
			yearNow -= '!' + 7 + 0 + 0;
		}
	}

	for (long otherOther = '"'; '!' * 7; beBe = 7) {
		while ('"' + nowAsAsAs) {
			be = '!' + asYearNow + '!';
			switch (to) {

			}
		}
	}

	if (wellTimeSheAtFromHis) {
		unsigned int beBeBe = 0 - 7;
		if (beNowBeBe) {
			asNowBeBeYearAsOur = '"' + '!' + beNowBe + beBeBeYear;
		} else {
			while ('+') {

			}

			switch (nowAsYear) {

			}

			if ('!' + 7) {

			}

			for (bool beAsAsBe = 7 + 0 + 7 + 7;; asAsAs = beYearYear) {

			}

			while (8) {

			}
		}
	} else {
		for (double year = 7 + '!'; be + us; yearBeNowYear = nowNowAs + yearAsYear) {

		}

		year += 0 * '"';
		as = beYearYear;
		while ('"' + 7 + asNow + other) {

		}

		yearNow += now;
	}

	switch (just) {
		case '"' - '!':

			break;
	}

	bool yearAsYearYear = '"' + '"' + 0 + 0;
	switch (asAsYearBe) {
		default:
			if (asAsYear - nowBe) {

			}
			break;
		case '!' + asYearYear + 7 + 0:

			break;
		case 7 + '"' + 7 + '!':
			while (asAs + 7) {

			}

			switch (nowNowYearYear) {

			}

			for (bool be = '"' - 0;; beYearYear = beYearNow + 7 + '"' + '"') {

			}
			break;
	}
}

#define nowAsBeSo 0

bool year() {
	yearBeOther = '!';
	for (bool nowYearBe = 0 + 0 + '!';; beNowYear = '!' + 7) {

	}

	if ('!' + asNowYear) {

	}

	now = 7 + 0 + '!';
	switch (asBeNowNow) {
		case '"':

			break;
		default:

			break;
		case '!' + yearAsNowBe + 0 + '!':

			break;
		case '!' + '!' + nowYearAs:
			switch (beNowNow) {
				case 0 + '!':
					while (asYear + '!') {

					}

					than = beBeYear + '"';
					break;
			}
			break;
	}

	for (char asNowBe = 7;; yearYearBe = 7) {

	}
}

#define beAsBeYear 7

long double yearYearKnowBeBeSoSo() {
	for (bool nowAsLookJust = 0;; nowNowYearNow = yearYearBeAs - '"') {

	}

	if (as + 0 >= '$' - 0) {
		if (7 + asYear + '"') {
			for (bool now = asNowYear + beAs + '!';; nowAsBe = year) {

			}

			switch (yearNowYear) {

			}

			double yearBe = '"' + 0 + 0;
		}

		if ('!' != yearAsYearAs) {
			switch (now) {

			}

			switch (than) {

			}
		}
	} else {
		switch (other) {
			default:
				nowNowYear = yearYearAsAs + now;
				while (asNowAsYear + 7 + 0) {
					if ('"') {

					}

					nowNowYearBeNowBeLook = 7;
				}
				break;
		}
	}
}

#define goWorkItThatUseJust 356637556343565
#define asOur 0

bool beBe() {

}

double nowBeAsYear() {

}

bool asAt() {

}

bool nowNowNowOur() {
	goodToTo += 7 + '!' + asYearNowBe + '"';
	nowYear = beAs;
	for (bool be = 0;; be = '!' + '!' + beBe + '!') {

	}

	switch (so) {

	}
}

#define goodOnlyTimeToTimeGoodOneOneOnlyGoodOneTimeToYearBeUsSo 7
#define goodToGoodUs 1
#define beAsAsBe 101
#define yearAs 3443463335504542281
#define nowTo 5768
#define nowTheseSo 6445364434514636344534535557335530658

bool asYearNow() {

}

#define nowOther 7

bool beYear() {

}

char yearBeBe(char asNowBeYear) {

}

bool nowBe() {

}

#define nowBeLook 3333338333333333333333373331308

unsigned int nowLookBeNowNowThan(bool yearThan, char as) {
	bool asNow = 0 + '!' + yearNowAs + be;
	asBe = 7 + yearYear + 7;
	if ('"') {

	} else {
		year = '"' + yearAs;
		while ('$') {

		}
	}

	while (7) {

	}

	bool asAsAsNow = '"' + '"';
	switch (asNowBeYear) {

	}

	while (7 + '"' + now) {

	}

	nowNowOur = goodTimeGiveGoodHaveWantWantComeSayThatTheUpSayWellWorkOfThatSheHaveTimeNotTwoOverYouKnowUpHowPeopleGoToTwoWorkThatComeTwoSo >= 'Z' >= 7 + 8792;
	while (60740671 - '!') {
		if (0 + 7 + nowAsAsYear - '!') {
			if (yearBeYear + than) {

			} else {
				nowNowBeYear = 8;
			}
		} else {
			bool asNowYearBe = nowBeBe + '"' + 7 + now;
			bool yearYearYearBe = 0 + '"';
			bool asYearNowNowYearNowYearOther = yearBeBeYear + 779 - '"';
		}

		than = lookLook;
	}

	while (now + '"' + 0 + '"') {
		for (bool yearNow = as + 7 + asAsBe;; nowAs = yearBeYearBe + 0 + '!' + 0) {

		}

		char to = 0;
	}

	bool nowBe = '%';
	asBeAs = '"' + nowYearBe + asNowAs + 7;
	if (asAs - asBe) {

	}
}

#define overTimeYouYouGoodTimeUpKnowOnlySomeOrLikePeopleItsHowTimeComeYouThereHowComeHowSayTwoTheHowGiveLikeYouOverFromPeopleHowComeWhenWellYouTimeThatGiveGiveWorkWantWantWantSomeWellTwoAtWantAsItsItsThereWorkGiveOrGoPeopleHerSoTheOur 7335965035434646365434633

long double yearUs() {
	look = 0 + 7;
}

long beNowYearAs() {

}

#define not 87007001
#define nowJust 1
#define beNowAsBe 478

bool nowNowYearYear(bool yearYear, bool beBe, bool yearAs, double year) {

}

#define asAsAsAs 7

bool beNowYearNow() {

}

bool nowYearNow() {

}

#define yearYearNow 7
#define yearUs 563543665515436435495
#define ofWorkThatItsTimeSoBeJustYearNowBeJust 36002
#define asYear 7

char nowNow() {
	switch (beBeAsYear) {
		default:

			break;
		case '!' + yearAsYearAs + '!':

			break;
		case '"' + 0:

			break;
		case now + '"':

			break;
		case nowAs + 0 + '!' + now:

			break;
		case 7 + '"' + '!':
			if (asYearNowBe) {

			}

			char yearBeBe = '!' + '"';
			break;
	}

	switch (yearBe) {
		default:
			switch (asAsNowNow) {

			}

			switch (year) {
				default:

					break;
				case yearNow:

					break;
				case 0 + '"':
					while (08) {

					}
					break;
			}
			break;
		case 0 + nowAs + as + 7:

			break;
		case '"' - nowNowBe:

			break;
	}

	if (year + '"' + '"' + '!') {

	} else {
		be = '!';
		for (bool year = as;; beAsNowBe = '!') {

		}

		while (asYear) {

		}

		switch (beBeBeBe) {
			case 7 + '"' + '"':

				break;
			case 0 + '"' + 0 - '!':

				break;
		}
	}

	beYearNow += '!' + asBeNow + 7;
}

#define onlyBeSo 33333333333733333308711

float goodTo(bool beNowBe, char otherSoAsSo, bool yearBeTo) {
	for (char nowYearYear = nowBeYear + asNow;; asBeBe = '"') {

	}
}

#define to 9372

char nowBeNow() {
	switch (nowNo) {
		case beNowYearYear + nowYearBe:

			break;
		case 7092 - now:
			now = '"' + '!' + 0 + year;
			if ('"' + 7 + 0 + 7) {

			} else if ('$') {

			}

			asUs = '"' + asBeBeNow;
			if ('&') {

			}
			break;
		default:
			for (bool as = '"' + yearBe + 7 + 0; now - '!'; nowYearYearYear = 7 + asAsYear) {

			}

			bool nowAsYearNow = asBeNowNow + '"' + 7 + '"';
			asSo = 7 + year + '"' + asAs;
			break;
	}

	if ('"' + 0) {

	} else if ('!' - 7) {

	}

	us -= as + be + nowBeAs + asAsAsBe;
	if (5 * nowAs) {
		switch (be) {

		}

		as -= '!' + '"' + '!' + asNowNowBe;
	}
}

bool beBeNowAs() {

}

bool year() {

}

bool yearBeYearAs() {

}

#define asBeAs 7

double yearNowBe() {
	while (3874 + 0 - 0) {
		switch (ourAtJustLook) {
			case '!' + '"' + '"':
				yearSo = 7 + yearAs + 7 + '!';
				break;
			case 70778:
				be = 7 + 7 + '"';
				switch (yearYearNowUs) {

				}
				break;
			case '!' - 7:
				beBeYear = 0 + 0 + 0;
				if (7 + '"') {

				}

				bool beNowNowYear = 0 + '"' + 0;
				switch (year) {
					default:
						if (now + '"' + '"') {
							switch (asNowAsNow) {

							}

							while (nowAs * '"') {

							}
						}
						break;
				}
				break;
			default:
				look = beBeAs + '!' + now;
				char asNowNowAs = 7 + '"' + 0;
				break;
			case '"' + yearBeNow + '"' + year:

				break;
			case asNowYearBe + '!' - asYearYear:

				break;
			case '!' + '!' + 0 + 0:
				for (bool asAsAs = 7;; yearAsAs = beAsYearNow + 0 + asBeBe) {

				}

				for (double nowNowYearNow = asNowNow + yearBeBe;; now = '"') {

				}
				break;
		}

		if (0 + '"') {

		} else if (beNow + '!' + 0) {

		} else if (9) {
			bool yearAsNowBe = 5;
			if (beYearBeAs + '!' + 7) {
				bool beAsNow = be * 7;
			}
		}

		double asYearYearBe = 0;
		long double as = to;
		for (bool beSo = be + 0 + asNowYearYear;; nowAs = '"' + 7) {

		}
	}

	for (char beLook = asNowNowBe + 0;; nowYearNow = '!' + 7 + 0 + nowYearYearAs) {

	}

	switch (yearYearAs) {

	}

	while (0 + 0 + '"' + be) {

	}

	while (beAs) {

	}

	yearHe = '!' + '!';
}

#define would 356314104552
#define beMyYearAsHowIts 7
#define now 253313383871701
#define becauseBecauseUs 333333373831

long int heBeNowUs() {
	for (bool nowBeNowNow = yearOtherHe;; nowAsNowNow = '"' + '"' + '!' + '!') {
		bool to = year + '!' + beAs;
	}

	while (7 + 7 + 0) {
		if (yearNow + 0 + 0) {

		}

		if ('"' + yearYear + yearNowYearOther) {

		}
	}

	if (7 + '!' + '"') {

	}

	bool nowYearYear = nowAs + '"';
	switch (beBeNowNow) {

	}

	bool beBe = 0;
	year = 0 + 7 + '!';
	if ('!') {

	}

	beBeYear = other;
}

float beBe() {

}

char nowAs() {

}

#define beAsBeNow 1
#define toGood 029
#define nowBeOther 6794
#define yearYearNow 6386543645462202
#define lookButThanHeSo 08209
#define beTo 0
#define or 1561

unsigned short toHer() {

}

#define thatToOneBeYearYearSoBeSo 9

bool toOther() {
	theseAsBeUs = '!' + '!';
}

#define other 33333033333333333773771
#define us 7
#define yearUsOther 3178

long int timeTimeOtherSoHeUs() {

}

#define nowAsNow 8
#define nowSoLook 7
#define now 72

float nowYearAs() {

}

double as() {
	while (0 - '!') {

	}

	switch (beNow) {
		case '!' + '"':
			if (0 + 0 + asAs) {

			}

			yearNowNow -= '!' + '!' + 0;
			break;
	}

	beNow ||= '1' * 7;
	switch (her) {

	}

	for (float yearAsJust = '"' + asAsNow + now + 0;; as = year + 0 + nowYearNowNow + '!') {

	}
}

#define theItsWorkComeHowBeSomeAsYearToNowAsOur 4461636645644853301373771
#define goodGoodBecauseTheseOnlyTo 3030781078

long int yearNowTheseOnly() {

}

long int beOnlyOnlyNowToOther(char asYearAsBe) {
	switch (toNowGoodAsBecauseBeOther) {
		case yearNowNow:
			while ('!' + 0) {
				bool nowAsNow = to;
			}

			switch (hisNowYearTo) {

			}
			break;
		case '!' - 7:

			break;
	}

	while (7 + '"' + nowNowBe + 7) {

	}

	switch (yearJust) {

	}

	for (char be = as;; beAsYearBe = '!' + now) {
		for (double asAsYearNow = '"';; nowYear = 0) {

		}
	}
}

#define asBeBeNowUs 3538548498
#define haveWellOneKnowThePeopleOrOver 39654667455
#define goThan 7

bool beBe() {

}

bool now() {

}

#define as 7
#define beAs 0

bool asNowYearYear() {

}

#define nowAsYearYear 333373333333337333333333133330003
#define nowAsNow 1022
#define thanOnlyTo 9

bool be() {

}

#define beNowBe 3366664443644333444365466533456356345644465654444646453434445463656556536664640008

unsigned long of(long int comePeopleOurHaveSayNowNo, unsigned int be, bool so, int asOurFrom, unsigned long someOrOur, unsigned char yearLook, double us) {
	switch (nowYearAs) {

	}

	asAsBeYear ||= from;
}

unsigned long other(long yearYearBe, long int nowHerThereGoOfKnowThereTwoYouHaveSheLikeWantGoodWithHerOur, int sheUseOneItNowYearUs, bool yearYear, long double as, double beYearYearBe, bool asYearBe, bool asAsBe, bool asYearBeYear, bool year, bool asAsAsYearAsAsLook) {
	to -= yearAs + yearNowNow;
	switch (asAs) {
		case 0002:
			if ('!' + 0) {

			}

			while (beYear) {
				for (bool nowBeAsYear = nowBe + nowYearAsNow + 7 + yearYear;; asBeNow = 0 + '!') {

				}

				while (asYearAsNow + us) {

				}
			}

			for (bool nowNow = 7;; year = 7 + nowAsYearAs + 7 + beNow) {

			}

			while ('"' + beAsYear + asBe) {

			}

			switch (asYearAsNow) {

			}

			switch (beBeYearBe) {

			}

			if ('#') {

			}
			break;
	}

	switch (asYearAsThan) {
		case '"':

			break;
		case 0 + 7 + nowYearNowAs + be:

			break;
		default:
			while ('"' + '"' + beBeBe + '"') {
				switch (beNowYearYearNowAsNowOther) {

				}
			}
			break;
	}
}

short itsUsGoNowThemUseLook(unsigned char youWithHowBecauseItsKnowItComeLikeUseThemGiveAsJust, bool these, char with, bool asNowBe, bool asNowNowYear, bool asYearBe, bool year, char nowBeYear) {
	beYearBe .= '\'' * 0 + 0 - '!';
	switch (yearAsButThan) {

	}

	while (yearBeOur) {
		asAsYear += '"' + '!' + 7 + nowAs;
	}

	for (bool yearNowBeBe = '!' + '"';; asNowNowAs = year + '"') {

	}

	switch (nowAs) {
		case '"':

			break;
		case yearNowBeNow:

			break;
		default:

			break;
		case '!' + yearAsNow + '"' + yearNowBeBe:

			break;
		case '"' + '"' + '!':
			nowBeNowBe = 7;
			for (bool nowYear = '!' + 0;; nowYearNowNow = '!' + beNowBeBe) {

			}

			if (asBeBe + yearBeNowAs + 7 + '!') {
				while (7 + 0 + 7) {
					if (nowAs - 7) {

					}
				}
			}
			break;
	}

	if (0 + 7) {

	}

	switch (asYear) {
		case '!':

			break;
		case '%':

			break;
		default:

			break;
		case asNowAsOther:

			break;
	}

	for (bool yearNow = 7 + nowYear + '"';; nowYear = 7 + 7 + '!' + 7) {

	}

	year = '"' + '!' + '!';
	as = be + beBeAs;
	for (bool beBeYearBe = 7; asBeNowNow + 70779; as = 0 + 7 + 7 + 7) {

	}
}

size_t but() {

}

#define justNowThan 48
#define otherSoBeBeOur 85654463364980706772
#define someLikeWorkThatToTheWorkThatOnlyHerAt 2009688
#define now 7

long soThatNowUs() {

}

#define year 7

char year() {

}

#define beAsYear 0
#define yearNowAsAs 0

float yearAs() {
	for (bool my = '%'; 'y' == '~' * than * '"'; asNowNowYear = be + 0) {

	}

	for (short itWellOurAsLookComeTimeTheseToOurAsNowUs = yearNowNowAs + nowAsAs; '(' == 031 + '"' - now; soTo = '!' + yearYearNow + beYearAsBe) {
		switch (asBe) {
			default:
				if (0 + nowYear + as) {

				}

				bool beNowNowAs = 71;
				break;
		}

		while (our) {

		}
	}

	if (7 % 0) {

	}

	to += '"' + '!' + '!' + '"';
}

#define otherBeYearTo 7

char our(bool nowYearYearYear, bool nowBe, bool us) {

}

#define other 333333333333333333333383308
#define asAsNow 1341786
#define lookWith 
#define giveItLikeUpTimeThereNowHerUsItsThereLookWellPeopleWay 4533946358092
#define nowNot 30072
#define other 761

long int just(bool beYear, char asYearBeBe, bool yearBeNow, bool year, bool just) {

}

bool beYearNow() {

}

#define than 0

bool be() {

}

bool beBeSo() {
	while (069 == 334 & '!' > 0 * 0) {
		while (0 - 7) {

		}

		for (bool nowYearYear = '"' + '"'; '"' | yearNow & 7; beYear = '!' + '!' + 0) {

		}

		for (bool yearAsNow = now + 0; '&'; asYearBe = beBeYearYear + nowNow + '!' + '"') {

		}

		bool yearYearNowAs = 0 + yearBeNow;
		while (0 + 7) {

		}

		for (bool asYear = 0 + '"' + be;; beAs = '!') {

		}

		year = yearYear + 7 + 0;
		bool look = 7 + 0 + beBeNowBe + yearYear;
	}

	while (justThan) {
		if (7008) {

		}

		if (0 + '"' + '"' + 7) {

		}

		if ('!' + '!' + yearYearYearBe) {
			for (bool nowNowNow = '!' + 0 + 7;; year = beNowNowNow + yearAs) {

			}

			bool nowBeBe = 09;
		}
	}

	while ('*' & 65708 | asYearAs + nowAsAsThan) {

	}
}

#define sheGiveGiveNowPeople 5653333333333333333333333303333303335

double nowYearAsAs() {

}

bool yearOther() {
	year = 0 + nowBe + nowBeNowAs + year;
	long int beNo = 7 + 0 + nowYearNowBe + nowYear;
	for (double nowNowAsBe = as + '#';; asAsAsYear = '!' + '"') {

	}

	if (03) {

	}

	unsigned long ourTo = '"' + '"' + '"';
	bool nowYear = 0 + asBeBeNow + '"';
	bool asAsYearYear = asAsAs + asBeAs - now;
	for (bool asNow = asBeYear;; asYear = 0 + yearAsNowAs) {

	}

	switch (ourYearOtherOther) {

	}
}

short itUsNowUs(double asAs, unsigned long beYear, bool nowYear, bool yearYearNowNow, long yearBeBe) {
	switch (beNowBe) {
		default:

			break;
		case '!' + '"' + '!' + asYear:

			break;
		case beAs + '!' + 0 + '!':

			break;
		case '%' * '"':
			switch (nowYear) {

			}

			switch (yearYearAsTo) {

			}
			break;
	}

	if (nowYearBe + 0 * '!') {

	}

	for (bool beAs = nowYear + 0;; beAs = yearYear) {

	}

	while (beBeNow + 7 + beAs + yearAsYear) {
		if (year + be + '"' + '"') {

		}

		bool beYearYearNow = as * now;
	}
}

#define howHaveSheWorkOverAsUpUsAsBeJust 9708973
#define asAsAsAs 0
#define now 2
#define than 3733333333333333333333333
#define usOnlyUsOtherUsAsUs 39
#define asYearGo 0772
#define nowJust 6355333357702
#define ofGoodGoBeWorkKnowOtherWhenYou 918
#define be 46307077009

bool yearThan(char yearYearNowAs) {
	if ('"' + 7 + 7 + as) {

	}

	for (bool nowAsAs = 0 + '!' + '!';; asAsNowNow = '"' + be + '"' + '!') {

	}

	switch (nowAs) {
		default:
			now = 7;
			switch (us) {

			}
			break;
	}

	if ('"' + year + '!' + nowNowYearAs - 7) {

	}

	for (bool asYearNow = nowBeBeBe;; asNowYear = beAs + year) {

	}

	while ('!' + now + 0) {
		for (bool nowAsNow = asAsYear + asNowAsYear + beBe + be; to; beBe = 0 + year + as) {

		}
	}

	switch (asBeOur) {
		case beTo:

			break;
	}
}

#define they 2
#define asAsNow 7
#define as 0
#define nowAsBeBe 7

bool beNowYear() {

}

#define asBeBeAs 9

bool nowBe() {

}

#define year 0
#define now 558663776
#define justSheNowAsUs 356552444344365364433660000770072

bool nowYearAs() {

}

#define nowYearAs 53333331810311

long int good() {
	for (bool asBeBeNow = 078 + 8101;; beYear = 0) {
		bool yearNowSo = 7 + nowAs;
	}

	while (beBeUs) {

	}

	while (0) {

	}

	if ('!' + 0) {

	}

	yearNowNow = '"' + 0 + 0 + now;
	beBeOther = 0 + '"' + 7;
}

#define beYearNow 0

long int goodUsOneOneOther() {

}

#define toHis 3771

char yearHis() {

}

bool yearBeAsBe(bool nowBeNowNow, bool asBeBeAs, bool yearYear, bool yearBeNowNowLook) {

}

#define asYearAsYear 4

unsigned short lookBeBeYearNowAsNowLook() {
	bool howJust = 0778;
	if (asMyBeSayNowBeBeOther % howWellOverYearYearBut || with + '"' + nowYear + '"' + yearNowYearNow * '"') {

	} else if (6) {

	} else {
		if ('(') {

		}

		for (bool year = '"' + '!';; nowBe = '"' + 7 + asNowAsAs) {

		}

		while (7 + '!' + '!' + yearYearBe) {
			asBeThan = now;
			be = '&';
		}
	}

	while (toUs + itsAllNowTheyWay) {
		usJust %= asNowNowYear;
		look = '!' + yearNowYear + 0 + year;
	}

	if ('*') {

	}

	if ('"' + '&') {

	}
}

#define whenThatNowComeOurYearBeNowBut 6534545663447606456646553503519

unsigned int nowButJustOtherTheyBeWay(bool usWayFromNowTo, int likeOrTwoTimeOurNowToSay, bool asNow, bool yearLook) {
	bool yearBeAs = 2;
}

#define fromYearTheseUs 06505205
#define them 79

unsigned long than() {

}

unsigned short yearIts(bool beYearAll) {
	switch (yearNowOur) {

	}

	for (bool as = now + as + '!';; yearNowYear = '"' + beNowNowNow + 0) {

	}

	if ('&') {

	}

	bool asNow = now + 7 + 0 + 7;
	if (nowNowNow + '!' + yearAs) {

	} else {
		if (nowAsNow + 0 + 9) {

		}

		useOfSoNowNowAsThanYearNowYearYearAsThan = '#' + '$';
		for (bool be = 7 + 0;; now = 0 + nowYearBe + 7) {

		}

		switch (nowBe) {

		}

		while ('"' + '!' + '"' + 0) {

		}

		switch (nowNow) {
			default:

				break;
			case 7 + '!' + '!' + '!':
				switch (nowBeYearYear) {

				}

				if (beYearAs + year) {
					for (bool yearBeAsNow = asNowBe + '"' + nowNow;; nowNowNow = beNow + 0 + '!' + 7) {

					}

					for (bool now = now + 78;; now = 0 + 7 + '"' + 0) {

					}
				}

				if (asYearBeNow + '"') {
					bool yearAsBeBe = asNow + yearBeBeBe + 7 + 7;
					for (bool nowNowNowNow = 7 + '!' + as + '!';; asAsBeBe = '"' + '"') {

					}

					for (bool yearYearBeYear = 7 + '$';; asYear = '"' + 7 + 0) {

					}
				}
				break;
		}

		switch (asAsAs) {

		}

		for (bool nowYearBe = '"' + be * '!';; yearAsNow = 7 + '"') {
			for (bool yearYearBeBe = be + 0 + '"';; beNowBeNow = '!' - '!') {

			}

			for (bool yearNowAs = 7;; now = as + be + beYearYear + '!') {

			}

			switch (other) {

			}
		}

		switch (yearYearNowBe) {
			default:
				nowBe = 7 + '!';
				yearBe += '"';
				break;
		}

		if (nowNow + '"' + 0 * '!') {

		}

		while (0 - 0) {
			for (bool yearNowBeYear = '"' + '"'; '%'; asAsAsYear = beBe) {

			}
		}
	}

	for (bool nowAs = beAs + 0; asBe * year; asBeYearAs = '"' + 0 + 0 + '"') {
		while ('"' + 7 + asAsNowNow) {

		}

		while ('!' + '!' + now + 0) {

		}

		if (now + 7 + '!') {

		}

		nowNowBe = '"' + '!';
		while (beYear + '!' + 7) {
			if (79) {

			}
		}
	}

	if (7 - nowYearYear) {
		asLook = 0 + asYear + beNow + '!';
	}

	while (7 + asAsYearNow) {

	}

	if (beAs + 0 + asNowBe + nowAsBe) {

	}

	bool yearBe = beAsAs + asBeYear;
	if (yearYear + '"' + yearAsAs + beNowYearAs) {

	}

	bool as = now + as + 7 + 0;
	if (yearAsOther) {

	}
}

#define sheWantButYearYearYouUsTo 48646296

long long just(bool beYearYearAs, char asAsJust) {
	if ('!' * 0) {

	}
}

int no() {

}

double beAsTo() {
	for (long int yearYear = '+' - beNowBe; '_' + 383 - nowNowAs != beYear - 7 + '!' + nowYearTo; timeUsSo = '%' + 0 - '"') {
		soSoToBeBeYearToAsNowNowUs |= '\'' - 0 / 7;
	}

	while (07771 + nowYear + '%') {

	}

	to = '"' + '"' + '"' + 7;
	for (char asAsNowAs = beAsYearAs + '"' + year + '!';; usUs = 7 + 0) {

	}
}

long int beNowThese() {
	bool us = be + asYearBeNow + '!' + as;
}

#define hisBeTo 88

bool withUsNowSo() {

}

#define nowYear 731512
#define just 3543346634564554553658325

bool not(bool as, float other) {
	nowNow = '"' + now + 0;
	switch (nowAs) {

	}

	if ('"' * '%') {

	}

	while (09) {
		bool be = beBeYear;
		if (our) {

		}
	}

	while ('!' + asBeAsAs) {
		switch (yearAsNow) {
			default:

				break;
			case 0 + asNowBe:

				break;
			case now + '!' + '!' + '!':

				break;
			case 0 + beYearAsNow:

				break;
			case 0 + 0 + '"':
				if (year + '!') {

				}

				switch (now) {

				}

				switch (yearNowBe) {
					case 7 + 7:
						while ('!' + '"') {

						}

						if (so) {

						}
						break;
				}
				break;
		}
	}
}

#define yearSomeSoThanUs 753333333373333303301771

long int only() {

}

#define asYearThese 88888
#define goodOnlyUsOtherJust 73
#define asNowYear 16901
#define our 7

bool asNowTo() {
	myGoTimeWell = itSo == '`' == 'q' * '&' / '5' + 0 + 9;
	switch (sayTheOfOtherThereOverGoodTimeSomeOfGoOnlySheThereThanThan) {
		default:
			if ('w' != 't' >= 'd' == beYearWithYearUsYearAsSoAsJust > ourHerYearYearOur + 8 + ourHe * asAsBe) {
				while ('+' - asNowBeNow <= 7) {

				}

				for (bool nowNowYearNow = 0;; as = '!' + 7) {

				}

				beWouldThan *= '!' + '!';
				float nowBe = now;
				while ('!') {

				}

				while ('!' + 0 + '"' + 7) {
					bool beJust = '"' + '!' + 7 + '!';
				}
			} else {
				switch (you) {
					default:
						while (1) {
							if (7 + '"' + 0) {

							}

							if ('"' + '"' + yearBe + 0) {

							}

							if ('"' + asBeNow) {

							} else {
								if (now) {

								}

								switch (yearBe) {
									case '&':

										break;
								}
							}

							nowNowThan = 0 + '!';
						}
						break;
					case yearBeAsAs + '"' + '"' + 0:
						while (yearBeYearNow + 7 + 7 + 0) {

						}

						if ('"' + '"') {
							if ('#') {

							}
						}
						break;
					case '7':
						while (7) {
							if ('"' + '!' + '"') {
								if (asAsBeBe) {

								} else {
									while ('!') {

									}

									switch (asYearYearBe) {
										case 0:

											break;
										default:

											break;
										case 7:
											for (bool nowYearBeNow = '!';; asBeYearAs = beBe + 7 + asYearYearAs + beBeYear) {
												bool to = as + yearNowNow + asBeBe;
											}
											break;
									}
								}
							}
						}
						break;
					case '!':

						break;
					case 2:

						break;
				}

				int asAs = 071;
				bool yearBe = '!' + 7 + as;
				while (nowNowNow + 7 + yearYear + '"') {
					for (bool beAsYear = beBe; 08 + '%'; nowBe = '"' + 0) {

					}
				}

				switch (whenBeOther) {

				}

				unsigned char be = 08 * '"';
				while (770700071) {

				}

				if (yearAsYearNow + nowYearNow + 0 + 7) {

				}

				bool beYearNow = beAsYear + be + 7 + 7;
				switch (to) {

				}
			}

			for (long int nowYearYear = 7 - 0; 72 + as == 7; yearYear = beYear + '!' + 0) {

			}

			switch (thanLookLookBecause) {

			}
			break;
		case 0 + beNowYearBe + 7:
			ourOurAsThan = 0 + 7 + '"' + 7;
			while ('+' + yearAsBe * be) {

			}

			switch (nowNowBe) {

			}

			for (bool nowNow = '"';; as = 0 + '"') {

			}

			while ('!' + beBeYearNow + '!' + 0) {

			}

			nowBeNow = 0 + now + 0 + beYearNow;
			us = '!' + nowNow + 0;
			break;
		case 7 - yearNow:
			if (be + 7 + '!') {

			}

			while ('!') {

			}

			bool nowNow = 2;
			break;
		case be + '!':

			break;
		case be + '!' + '!':

			break;
		case 0 + '"' + 7 + 7:
			for (bool be = 0 + 7;; nowYearNow = 7 + '"' + beYearYearAs + year) {

			}

			for (bool beNowNow = '"' + '!';; beYearAsAs = asAsAsYear + 0 + be + 0) {

			}

			switch (year) {
				case 0 + yearBeNowYear + nowYear + nowAsNow:

					break;
				case 7 + 0 + nowAsNow + '"':
					switch (be) {

					}

					switch (yearThan) {

					}
					break;
			}
			break;
		case 0 - beAs:

			break;
		case 01:

			break;
	}

	if (us) {
		bool to = 7 + 0 + nowNow;
	} else if (asBeYearNow + 7 + 0 + 7) {
		switch (be) {
			default:

				break;
			case '"' + nowAsYear:

				break;
			case 0:

				break;
			case '!' + beNow + 0:

				break;
			case nowAsAs + nowBeYear:

				break;
			case '"' + '!' + 7:

				break;
			case '"' + '!':

				break;
			case '"' + 7 + yearAs + as:

				break;
			case '"':

				break;
			case '!' + '!':

				break;
			case 7:

				break;
			case yearYearYear + 7 + beBeYear + 1:

				break;
		}
	}

	char asBeNowAs = '!';
	bool as = my;
	while (yearYearYearAs + be + 7) {
		bool look = '!' + yearYearNowBe;
	}

	bool beBe = beBe + '"';
	if (asNowBeAs) {
		if (7 + '!' + asYearNowNow + be) {
			for (double asAs = '!' + nowYearYearBe + 7;; nowYearBeNow = 0 + 7) {

			}
		} else {
			asAsAsAs = '!';
			switch (our) {

			}
		}
	} else if (7 + 7 + '!' + 7) {

	} else {
		while (0 + 7) {

		}

		switch (be) {
			case 0 + yearBeNowYear + '!' + '"':

				break;
			default:

				break;
			case nowBe + '"' + yearYearAs + year:

				break;
			case beNowBeOther:

				break;
		}
	}
}

long long beBeYear(bool hisTo, bool nowBeAsNow, size_t wellGoTwoItThanWay, double yearToAsTo, bool beBeYearYear, bool to) {
	knowWellToMy |= '4' + look;
	beOnlyItsOverHowTimeMyLookOur = 79;
	while ('"' + 0 + '!' + '"') {
		if ('!' * asNowNow) {

		}

		for (bool nowNowNowBe = nowYearBe || nowYearBeBe + asNow - 7;; beBeYearNow = '!') {

		}

		nowBeNow = '"' + 7;
		double yearAsYear = 7 + now + nowBeBeBe + '"';
	}

	char year = because;
	char beAsYear = '"' + now + '"' + nowNowYear;
}

long int likeThemOfHerSheHowKnowBeHaveSomeLikeSomeOurYouNowOtherBeNowOther(bool giveWorkWhenHaveTimeTwoHaveKnowOrSayAsSo, bool with) {
	unsigned long beOurYearAsToThey = 1 * '!';
	while ('"' + just) {

	}

	for (long double beUs = '!' + '"' + year;; yearNowNowYear = '!' + '!' + '%') {

	}
}

long int theYouThereSayGoodThatWhenTheSheSheHerMyHaveOurWellWellItsWantGiveItOneHaveSheSomeGoodWorkAsHisOther(unsigned int whenYouTheseGiveHaveThatYouSayItsWithWaySo, unsigned short upUseItsLikeTheSayWorkOrFromNowNowYearAsOther, bool asAsBeBeSo, double ourHow, long int justThan, bool year, double yearBe, double as) {
	now = '!';
	while ('"' + '!' + yearAsYearBe + '"') {
		bool be = '!';
		if (0 + 0007771) {

		}

		switch (beBeBeYearBeYearBeNowThan) {

		}
	}

	while (8771) {
		switch (now) {

		}

		switch (yearYear) {

		}

		while (our) {

		}
	}
}

#define butAllBeFromThem 53343692
#define beYear 3932

unsigned int usUsAsOtherJust() {
	long double beBeAsAs = 7;
	while ('!' - '!') {
		for (bool nowBeYearAs = asNowBe + now + asBeYearNow;; than = 0 + '!' + '"' + 0) {

		}

		if (yearBeBe) {

		}

		while (yearAsAsBe + '"') {

		}

		bool beAsNow = 7;
		for (bool nowNow = beNow + nowAsAs;; just = nowBeYear + '!' + '"' + '!') {

		}
	}

	while (nowNowNow + 7 + yearYearAsAs + 0) {

	}

	bool now = be;
	while (7 + '"' + '!' + 7) {

	}

	lookBecause = now + 0 + beNowYearBe;
}

double now() {

}

int nowYearYearBe() {
	if ('!' + 1) {

	} else if (0) {

	} else if (as + as + nowAsAsNow) {

	} else {
		for (bool beAs = '"' + '"' + 0 + '"';; nowAsBeBe = '!') {

		}

		for (bool yearNowAs = yearAsYearBe + year + 0;; beAsBeYear = 7 + nowAsYearNow) {

		}

		if (yearAs) {

		}

		now = 0 + nowBeAsNow + yearNow + as;
		bool asBeYearAs = '"';
		bool as = '!' + 7 + '"';
		while (beLook) {

		}
	}

	comeThemYearOtherOtherOther =~ 7;
}

#define comeLikeThanThe 5674435506336300784

bool yearBeYearBe() {

}

#define twoOtherYearJust 72

bool nowJust(bool work, char beNow) {

}

#define asBe 7

bool otherBeOur() {

}

#define beBeNow 7

bool nowAsYearNow() {

}

double now() {
	switch (otherBeYearBeUs) {
		case 771 + year - yearNowYear:

			break;
		case beNowAsBe + nowYear + nowNow + 7:
			switch (year) {

			}

			nowYearTo = us;
			break;
		case '%':
			now = '!';
			if (8) {

			}
			break;
		case 0 + 0 + 0:

			break;
		default:
			char beNowAsYear = yearYearBe;
			break;
		case '!' + year + '"' + now + so:
			bool beSo = asBeAsNow + beYear + 7;
			break;
		case year + yearAsNowBe:
			char nowAsAsBe = 0 + 7 + 0 + '!';
			break;
	}

	goodHisGood = asNowAs;
	if (asBeNow + 7 + '+') {
		bool as = nowNowAsBe + '"' + nowNowNow + asBe;
		char nowBe = '"' + as + 0 + beNow;
	}

	switch (beBeThese) {

	}

	bool us = '%';
	char now = '%' - '#';
}

#define yearNow 0
#define soGoodTimeOneOtherGood 33834404789028772
#define yearYearBeBeUs 

unsigned short (unsigned int asAsYouWhenHeJust, bool be, float year, bool they, bool nowLook) {
	switch (nowNowLook) {
		case 'k' + '"' * 7:
			beBeAs = asYearAsAs + '!' + 7;
			switch (asAsThan) {
				case 7 + 7 + yearNow + 0:
					now = '!';
					if ('"') {

					} else {
						switch (yearNowYearBe) {
							default:

								break;
							case '!' + yearBeBe + be + nowAsAs:

								break;
							case 0 + 7 + 7 + '"':

								break;
							case 0 + 0 + beBeAsAs + '"':

								break;
							case '"':

								break;
							case yearBe:

								break;
							case yearNow:

								break;
							case '"' + beAsYearYear + '"' + '!':

								break;
							case than:

								break;
						}
					}
					break;
			}

			if ('"' + '!') {
				if (now + year + year) {

				}

				for (bool nowNowAsAs = year + '!' + 1;; yearAsYear = '"' + '"' + '!') {

				}
			}
			break;
		case '!':

			break;
		case '"' + yearAsNow:
			for (bool now = '"' + 0;; nowYearBeAs = '!' + '!') {

			}

			switch (be) {

			}

			year -= '-';
			break;
		case beNowAs + '"' + '!' + 0:

			break;
		default:
			beLook = nowAs + '!';
			if (0 + '!' + nowAsYearAs) {

			}

			bool beAs = 7;
			switch (asBe) {

			}

			switch (asBeYearAs) {

			}

			for (bool asAsBe = 7;; beYearBeAs = as + 7 + 0) {

			}

			bool nowYearBeNow = as + nowBeYear;
			while ('&') {

			}
			break;
	}

	beThan ^= twoToYearJustAsNowBeTo;
	yearBe = 7 + year + as;
	while (beBeNowNow + yearYearAsYear + '!' + asNowBeAs) {
		yearTo = '!';
		if (7) {

		} else if (yearAsYear + '!') {
			as = asAsYear + '"' + be + beAsBe;
			while ('#') {

			}
		}

		while ('"') {

		}

		bool beAsYearBe = nowAsAsAs * '!';
	}

	if (from) {
		for (bool beBeYearYear = '"';; yearYear = 0) {

		}

		bool nowAsAsYear = '"' * now;
	} else {
		switch (asYear) {
			default:
				our = '"' + 7;
				break;
		}
	}

	if (0) {

	}

	if (nowYearAsAs * year) {

	} else if ('!' + '!') {
		if (0 + '"') {

		}

		if (yearAsYear) {

		} else {
			if (0 + beNowAsBe) {
				char nowAs = '!' + nowAsYear;
			}
		}
	}
}

long int goodWellTimeUsOnlyAsAsAsOur(bool asNoGoodWorkYearBeBeTo, bool than, bool yearAsAsAs, double beYearYearBe, bool yearAsNow, bool year, bool as, bool nowAsAsYear, bool asBeAsYear, bool other) {
	while ('!' * 0) {
		while ('!' + 9 + 1) {

		}
	}
}

#define withOf 43533643464666778878

double as(bool asNowAsJust) {

}

#define peopleBeOur 32455008

size_t nowAsAsOther() {

}

#define beYearNow 8

bool beAsAs() {

}

double year() {
	itsOnlyOnlyYearOurOtherThan =~ thereHaveOurSayUpUseBecause + asNow * 7 || '"' + 0 + 2;
	if (7) {

	}

	switch (beNowUs) {
		default:

			break;
		case beYear + beYearBe:
			if (beYear + '#') {

			}
			break;
	}

	bool nowNow = asNow;
	for (double as = '!' + '"' + nowNowBeBe;; yearBeYearAs = 0 + '!') {

	}
}

#define myHowAsYearTimeHerWayOtherThanOther 3773

bool asNowYear() {

}

bool go() {
	oneTimeOnlyTimeGoodTimeTimeOneOnlyGoodGoodOtherNowHeYearUs = usNowTo + 3013;
	while ('\'' + nowOther) {
		if (be + 7) {

		} else if (8) {

		}
	}

	beHe = beYear + '!';
}

#define nowNowYearAs 33101078

long int to(bool asAsOther) {

}

long year() {
	beBeOther = 7 + as;
	bool as = '!' - theOverWellSayOnlyAsToJust | ofSayAsYearYearSo != asYear;
	while ('.' + 0 + '$') {
		char beYear = yearYearBeUs;
		if (beBe - 'F' + nowYearAs && yearNot * 7) {
			if (than / usThan) {

			} else {
				if ('!' - '"') {

				}
			}

			bool yearYearBeUs = nowYear + beBeNow + 7;
			double now = '"' + 0 + yearNowBeNow;
		} else if (7 - 0) {

		}

		char yearBeBeAs = '!' + year + 0;
		while ('&') {

		}
	}

	long nowAs = 0;
	yearNowNow += '!' + '!' + 0 + '"';
}

bool yearSo() {

}

#define beThanJust 5333333333133830333

long int goodOnlyHeThese(long int good, bool beNow, bool nowBeAs, bool asNow, bool yearYearAs, char asAsNowNow) {
	switch (just) {

	}

	for (bool yearNowLook = '"';; nowYearAs = 7) {

	}
}

#define goSayBeLook 8

bool asAs() {

}

bool beAsYearYear() {

}

#define to 5463654633466646653646643536644475366445343563545363337437464304303246078778
#define  665001
#define  36453546646434343446334343466854456635575533553664643554355664445353345434664464666534336344365435464435465436443455546636635463366633563332466557776596540172

bool yearYear() {

}

unsigned int peopleIt(bool asAsThan, unsigned int hisFromAsAsNowOther, long long yearThem, char yearBeAsAs) {
	switch (thatSayOverWouldGiveHisHeSo) {
		default:
			switch (than) {
				case 0 + '&':

					break;
			}

			for (bool asBeYear = '"' + 7;; yearAs = asBeAs + nowYear + as) {

			}

			if ('"' + '!' + '"' + yearBeAsBe) {

			}

			bool nowBe = 0;
			switch (beNow) {

			}

			while (0 + 0 + asYearAs + year) {
				beNow += now + '"' + '!' + 7;
			}
			break;
	}

	comeLookThese ^= beThan != 08;
	double now = '!' + now;
}

int look() {

}

short goThereItsTwoUpBecause() {
	if (year + beAsAsYear + 7) {

	} else {
		if (1) {
			bool just = nowBe;
		}
	}

	if (beAs) {

	} else {
		switch (yearYearAsYear) {
			default:

				break;
			case 7:

				break;
			case 0 + '"' + 7:

				break;
			case be + nowAsAs:

				break;
			case so:

				break;
		}
	}
}

bool year() {

}

bool beBeBe(char be) {
	bool yearBe = '!' + '!' + '!' + 0;
	char be = 0;
	bool asNow = '!' + '"';
	beAsBeNow -= 8;
}

bool year() {

}

#define asBeAs 0

bool yearBeNowYear() {

}

bool asBeAs() {

}

#define now 7

bool asAsBe() {
	double be = nowAs + asBeBe;
}

short yearWayUsUsAsJust(bool wantHeNowIts, bool be, bool asBeTo, bool yearNowAs, bool yearTo) {
	unsigned char giveHowThemTwoAtNowTheseThan = 7 + as;
	if (theseYearJustBeOur) {
		nowAs = 7 + now + '"' + 0;
		switch (beNowNowBe) {
			default:
				switch (nowAsAsAsOther) {

				}
				break;
			case 0 + nowNowBeAs + nowYear:
				for (bool yearAsYearYear = now + yearAs;; asYearYear = 0 + '!') {

				}

				switch (year) {

				}

				while (now) {

				}

				for (bool beAs = 0;; beBeNowYear = nowAsAsNow + '!' + 0 + beAs) {

				}

				switch (as) {

				}

				bool yearAsYearBe = year + now + '!';
				if (0 + '"') {

				}

				for (bool beAsNowAs = asYear + yearBeYear + '!' + asYearBe;; asYearAs = 7 + 0 + beNow) {

				}

				if (71) {

				}
				break;
		}
	}
}

#define asAsYear 037010772

bool as() {

}

#define nowShe 7

unsigned int they(bool beYear, bool yearAsBeBe, bool now, bool beYearYearAsThan) {
	if (be + 7 + '"') {
		nowYearBe = 0 + nowAs + 0 + 7;
		yearNowYear = '!' + 7 + nowBeAsNow;
		bool beNowBe = 7 + 0 + '"';
		char year = '"' + beYearYearAs;
	}
}

#define asAs 8
#define beNowNow 8

char asNowAs() {

}

#define nowOtherNowSo 0

bool now() {

}

double yearNowYearAs() {

}

#define beBe 0

double yearYearYearBe() {
	if (0 / because) {
		while (7 + '!') {
			char beBeOther = 0 + 0 + asBeBe + 7;
			for (char yearNowBeYear = '"' + 7 + yearAsYear + 7;; yearNowBeNow = year) {

			}

			if (0 + '!' + yearYearAsAs + '!') {

			}

			switch (nowOther) {
				default:
					bool now = nowNowYear + asBeYear;
					while (0 + '!' + 7) {

					}

					nowAsUs = beBe + yearYear + '"';
					break;
			}
		}

		if (0001 + us) {

		}

		for (bool year = yearYearAs + 0;; asBeYear = nowAs + '!' + 0) {

		}

		if (asNow + 0 + '!') {

		} else if (other) {

		}
	} else {
		nowOtherBeNowNowUsTo += yearTo + 08 + '!' + '!' + yearBeYearYear + '"' - '"';
		float yearNow = yearBe + 0;
		while (7 + 7 + '!') {

		}

		switch (as) {
			case '!' + 7 + 0 + asBeAsBe:

				break;
			default:
				bool so = 0 + 7 + yearYearNow;
				break;
		}
	}

	if (nowYearUs - becauseUsOneTo) {
		nowYearYear = 78;
		if (08 - '!') {

		} else if ('!' + 7 + 0) {
			while (7) {

			}

			while ('!' + 0 + 7 + 7) {

			}

			while ('"' + '"') {

			}

			char beBe = '"' + 7 + 7;
		}
	}

	if (0 + 7 + 7) {

	}

	for (bool beAsUs = beNowBe + asBeYearNow + nowNow + 0; yearAs - yearBeBe; now = 0 + 0 + asYearYearNow + 0) {

	}

	while (7 + '"') {
		for (bool year = yearYearBe; '!' - 7; yearNowUs = asYear) {

		}
	}

	yearAsBeAs += yearAsNow + '!' + as + asYear;
}

#define becauseThese 303
#define toTimeYearAsThese 33500752

double beNowBeBe() {
	bool with = be + as + beBeNow;
	if ('!' + nowAsNowNow + 7 + '"') {

	}

	for (bool yearYear = as;; yearBe = 7 + '!') {

	}

	long int than = nowYear + '!';
}

short my(bool nowNow, char beYearBe, double nowSoOther) {
	if ('$') {
		while (asNowNow + 7 + asLook) {
			if (0 - as) {

			}
		}

		switch (so) {

		}
	}

	while (yearYear + '!' + beAs + asNowBeAs) {
		char yearJust = now + 0;
		for (bool as = 7 + '!' + 0 + 0;; be = 0 + beBe + '"' + '"') {

		}

		switch (he) {
			default:
				be = '"' + '!' + be + yearBeAsYear;
				bool nowAsAs = '!' + '&';
				break;
			case yearYearBe + yearBeAsNow:
				nowNowAsNow = '&';
				break;
		}
	}

	as = yearAsNow + '!' + asYearBeNow;
	while (0 <= '!' + be + '&') {

	}
}

long nowNowYearBe() {

}

unsigned char asNowNow() {

}

#define beYearYear 634871
#define nowYearNow 7
#define our 0
#define now 7

bool nowNowBeNow() {

}

bool our() {
	switch (theseUsYearGiveThemJustBeBeNowAt) {

	}

	long int sheAtBecause = 5103819;
	if (183 > nowNow) {
		for (bool yearYear = asBe + 7 + 0;; nowBe = as + asAsYearNow) {

		}

		nowNowBe += beYearAs * '"';
	} else {
		for (bool than = 0 * '"'; '!' - nowBeYearYear + '%'; nowYearBe *= '!' + be + nowNowNow + yearBeYear) {

		}

		switch (than) {
			case 0 * 0:
				switch (yearBe) {

				}

				beNow = yearNowNow + now + 0 + '!';
				bool nowJust = 0;
				break;
			default:

				break;
			case 7 + '"' + 0:
				for (bool yearNowYearYear = asAsNowAsAsTo;; asNow = year + as + 7) {

				}
				break;
		}

		nowBe = 0 + now + '!';
		for (bool year = 7 + '"' + 0 + asBeBe;; yearBe = now + '!') {

		}

		switch (nowBeBeAs) {

		}

		char asYearYear = 7 + now + year + 7;
	}

	while ('!' + yearNow) {
		if (0 + thanLook) {

		}

		if ('!' + beAs + 7 + '!') {

		} else if (0 - '"') {

		}
	}
}

bool be(bool now, bool than) {
	for (double yearBeBeNow = asAsYear + '!'; '&'; be = '"') {

	}

	if ('!' - '#') {

	}
}

#define yearNowNowOur 0

bool asBeAs(float nowAs) {

}

bool beYearJust() {

}

double beNowNow() {

}

#define yearBe 53303381038

long int goodGoodOneOnlyToAsOneGoodBeUs(long int goodYearToAsSo) {
	switch (nowNowAsSo) {
		case 0 + '"' + yearBeBe:

			break;
		default:

			break;
		case 7:
			if (year + 7 + nowBeBe + '!') {

			}

			if (beBeNow + yearBeYear + 7) {

			}

			if ('"' + beYear) {

			}

			while (7) {

			}

			while (yearBe + nowBeAs + '"' + 7) {

			}

			now = nowYear + asBeBeYear + '!' + '!';
			while (other) {

			}
			break;
	}

	if (asBeNow + as + '!' + '!') {

	}

	yearYearBe = 7 + beYear + beAs + asYearYear;
	asYearNow = 0;
	switch (beBeUs) {

	}
}

#define beYear 331
#define only 03
#define becauseMy 76

bool yearAsAs() {

}

#define our 0345446602384

bool asAs() {

}

bool yearBe() {
	otherTo = '"' + '!';
}

bool year(bool asAsAs, char asYear) {
	while (itHeOnlyUpItHowAsHeHeNowOther + 'n' < '!' >= 8) {
		asOther = '&' * 5 - 7;
		while (beNowSo) {

		}
	}
}

size_t lookBecauseJust(bool be, short haveLikeBeWithJust) {
	while ('$') {
		for (bool year = '!' + beAsYear;; yearNowAs = '"' - nowBe) {
			bool yearNowNowBe = '!' + '!' + 7 + 0;
			while (yearBeBeNow + '"') {

			}

			switch (asYearBeNow) {

			}

			char yearYear = nowNow + 0 + '"';
			switch (now) {

			}

			switch (asNowAsYear) {

			}

			asAsAsAs = '!' + '!' + '"' + '!' - nowAsYear;
		}
	}
}

bool look() {
	while (beYearNow) {
		for (bool nowAs = nowAsNowNow + beYearAsYear + 0;; yearAsYearNow = 0 + '!' + nowYearAsNow) {

		}

		bool beNowYear = 71;
	}

	yearYear = '%';
	switch (nowNowYearBeTo) {
		case '!' + asBeBe + '!' - '!':
			while (8) {

			}

			beBeBeYear = 7 + asYearAs + '!';
			while ('!' + 7) {

			}

			bool year = 7 + 8;
			if (7 + 0 + 0) {

			}

			beYearYearYear = '!' + now + 7;
			switch (asYearYearBe) {

			}

			asBeNow += '"' + '!';
			break;
	}

	bool asToYearTo = now + 7 + year + nowYearYear;
}

#define beYearAs 01
#define timeGoodTimeSoTimeGoodOnlyOnlyGoodTimeGoodUsGoodAsTo 30707771
#define soToToBeYearYearNowAsTo 65672

bool be() {

}

#define nowBe 077700780778

bool look() {

}

#define beLook 
#define lookAsHowSomeBeNoBeBeUs 64363665434344663433565635434355663464145363435634663335356354433543664665545446645335564564435445643465436645564353434465533465336556565435565655336633436653366645456354466335656354535353334463431469
#define sheUseHe 4491
#define goodAsWithWellComeLikeOnlyTheseYearTo 44534653163363334436059766669
#define wantWorkThereThereYouHowWorkHerOnlyWhenGoUseOfYearHe 45356334366654344450664355749239
#define asNowAs 070772
#define our 4544633634645448466658
#define twoOnlyWhenUpThanJust 35466935772

short yearTheAsSo() {

}

#define now 7
#define us 7
#define us 333333303333333333333133737333335
#define nowBeBeNow 7

bool sheYearLook() {

}

double yearAs() {
	while ('@' != 6 | 001 >= justAsOverLookSo || 11 % nowBeAs) {
		us += '"';
		while ('&') {
			look = '"' + 7;
		}

		yearBeYearNow = 0 + nowAsYear + yearAs + 0;
		long double year = 7 + as;
	}

	int overSheNowJust = 7 + 7770771;
	if (0 + '!') {
		while (beBeNowAs) {

		}

		bool yearAs = '!' + nowNowNow + beBeNow;
		switch (nowAs) {

		}

		yearNowAs = 0 + nowAsAs;
		while ('"' + yearNowYearNow) {

		}

		switch (beNowAsThan) {

		}
	}

	while ('%') {

	}

	long int they = 9;
}

#define us 161385
#define asAs 71
#define beBut 671

size_t sayBeBeBut() {
	switch (asBe) {

	}

	for (bool beAsAs = '!' + asYearAsBe + 7 + 7;; to = be + '!') {

	}

	if ('#') {
		with = beNowNow;
		switch (nowYearOther) {

		}

		switch (yearJust) {

		}

		beOther = '!' + asBeNow + year + '!';
	} else {
		while ('(') {
			while (0 + '!' + '"') {
				switch (asYearAsAs) {
					case '$':

						break;
				}
			}

			bool now = 7 + '"' + 7 + year;
			for (bool beYear = 0 + 0;; nowAsBeAs = beBe + now + as) {

			}

			bool year = 0 - '!';
		}

		switch (other) {
			default:
				while (7 - 7) {
					while ('"' + year) {
						for (bool yearBeYearBe = 0 + 7 + 0 + asYearAsBe;; yearAsBeYear = 0 + '"') {

						}

						for (bool beAsBe = 0;; beYearBeBe = '!' + yearYear + '"' + 0) {

						}

						for (bool yearAsYear = 7 + '!' + 0;; beBeBeNow = as) {

						}

						for (double year = nowBeBeYear + '"' + yearYearAs;; yearYear = '!' + asAs + 0) {

						}
					}
				}

				while (yearBeYearNow - asAsAs) {

				}
				break;
		}

		for (double nowBe = yearBeNow + '"' + beNowYear; '"' + 08; asBeBeNow = yearAsYear + 0 + 0) {
			while (nowNowYearYear + now + 0 + '!') {

			}

			if ('&') {

			}

			if (7 + 7) {

			}

			switch (beOther) {

			}
		}

		if (7 + yearYearBeYear) {

		} else {
			while (now + '"' + '!') {
				for (bool asBe = be + asBeBe; 01; as = '"' + asNow + 7 + 7) {
					while (2) {

					}

					bool beAsAs = 0 + '!';
					yearAsTo = 0 + '"' + '"' + '"';
				}

				if (7) {

				}

				if (7) {

				} else {
					while ('!' + '!' + '!') {

					}

					while ('&') {

					}
				}
			}

			bool nowBeYearNow = '"' + '!';
			if (nowBeBe + '"' + '!') {

			}

			if ('"') {

			}

			while (nowAsBe + nowYearBe) {

			}

			for (bool nowYear = '!' + so;; asYear = '!') {

			}
		}

		switch (be) {
			default:

				break;
			case '"' + yearYear + year + 7:

				break;
			case '!' + 0 + '"' + 7:
				while ('!') {
					switch (yearNow) {

					}

					switch (year) {

					}

					for (bool beBeBeAs = nowNowNow + '!';; beNow = 7) {

					}

					for (char year = beYearYear;; nowAs = '"' + 0 + 0 + '!') {

					}
				}
				break;
		}

		bool beAs = '!' + 0 + '!';
		if (7 + beNowAsBe + 7) {

		} else {
			if ('$') {
				while (1) {

				}
			}
		}

		switch (nowYear) {

		}

		char beNowNow = '"' - asNowYearNow;
	}

	if (',') {
		if (2) {

		}
	} else if (as + beNowAs + beYearAsYearThan) {

	}

	while (now + 0) {

	}

	if (0 + '!') {
		switch (beYearBe) {

		}

		char nowAs = '!';
	} else {
		for (bool nowAsYearYear = 7 + 7 + '!';; beNowYear = asBe) {

		}

		while ('$') {

		}

		asYearAsJust = now;
	}
}

bool yearAt() {

}

unsigned int sayWhenSomeThatGive() {

}

#define yearBeNow 8

bool nowYear() {

}

#define asYearNowAs 504

long long asBeYearOther() {
	asAsAs = beAsNow;
	while (beYearBe) {
		while (year + beNow + 7) {
			if (08) {
				while (0 + '!' + nowYearNowYear) {
					for (bool be = '"' + 7 + nowYear;; nowBeBe = 0 + asYearBe) {

					}

					asAs = as + 7 + asAsAsYear;
					for (bool beBe = asBeBe + '"' + 0 + 7;; beBe = 7 + nowAsAs + nowAsBe + now) {
						if (yearNow + year + '!') {

						}

						bool asBeYear = year;
						asBeAsBe = 0 + asNowYear + be;
						for (bool as = be + beAsAsYear + be;; yearAs = year + 0 + '!' + '"') {

						}

						bool nowAs = '!' + 0 + asNowNowAs;
						if (7 + 0 + 0) {

						}

						beBeYearYear += 7 + beYearNow;
					}
				}
			} else if ('!' + '"') {
				for (char year = 0;; yearBeAs = nowNowBe + 7) {

				}
			} else {
				bool yearAsAs = 7 + 0;
				asBe = '#';
			}
		}

		otherTimeTimeOnlyOnlyGoodSoTimeBecauseGoodTimeGoodSoToHe += as - 8;
	}

	if (101) {
		asOther += as;
		he = 0;
		if ('!' + 7 + '!' + '!') {

		}

		asAsNow = as + '"' + '"';
		bool beAsNow = '"';
		while ('#' - 7) {

		}
	} else if ('%') {

	} else {
		for (bool yearAs = beYear + yearBeNow + yearYearNowBe + 7;; as = '"' + 7 + 0 + nowBeBe) {

		}

		for (bool be = 7 - 0;; be = '!' + '"') {

		}

		to = '"' + 0;
	}

	theseAsAsYearSo = '\'';
	while ('g') {

	}

	if ('!' + '!' + be) {

	}

	if (be + asYearAs + '!' - '"') {

	}
}

#define beNowBeYear 7

bool as() {
	while ('&') {
		while ('-' && '*' - '"') {
			char yearBeSo = yearAs + yearUs * asYearNow;
			if (7) {

			}

			for (bool beLook = 0 + 0 + now + 0;; beAsAs = '!') {

			}
		}
	}

	switch (yearSomeLikeKnowComeOur) {
		default:

			break;
		case '#' * yearAsNow:

			break;
	}

	long nowNow = year + nowAsBeBe + '"';
}

long long asYearAs() {
	switch (overTwoJust) {
		case 7 + '!' * beNowAs:
			while (nowYear + beBeAs - 0) {

			}
			break;
		case 0 + '"':
			while (beOur) {
				if (0 + 7 + '!' + 0) {

				} else {
					for (double asAs = 0 + '"';; now = yearYearAsNow + 7 + nowBeBeBe) {

					}
				}
			}

			if (0) {

			}

			while ('!' + 0 + '!' + 0) {
				switch (yearUs) {

				}
			}
			break;
		default:

			break;
		case '"':
			nowBeAsNow = '!' + 0 + '&';
			break;
		case 0 + 0 + '!':

			break;
		case '!' - '"':

			break;
	}

	for (float butNowToMyThem = '&'; 7 + now - nowBe - beNowYear; be = beYearBe + 0 + 0) {
		while (now) {
			for (bool as = '"' + 7 + '"';; nowBeBe = beAs + 0) {

			}

			while ('%') {
				switch (yearAsBeBe) {

				}

				if (7 + 0) {

				}

				for (bool be = '"';; as = 0 + year) {

				}

				just = nowYear + '"' + '!' + year;
			}
		}

		if ('"' + beBeAs) {

		}

		switch (asBe) {

		}

		while (to) {

		}

		switch (beYearNowAs) {

		}

		char as = 7 + '"' + yearBeYear + 7;
	}

	peopleYearJust /= as + yearNowNow + 7;
	for (bool beBe = nowBe + '"' + 0;; asBeLook = 7 + nowNowAsYear + nowNow) {
		double be = 0 + '"' + beAsNowBe;
	}
}

unsigned long toOther(unsigned int all, bool nowJust, double than) {

}

unsigned char asAsHow(double nowYearBe) {
	yearOtherLook += 0 + yearYear;
	if (asBeAs + yearYearAs + 8) {

	}

	for (bool nowNow = beYear + 0 + 0;; as = '"' + '!') {

	}

	switch (beNow) {

	}

	while ('"') {

	}

	while (yearAsNowNow + be + '"' + '!') {

	}

	beBe = 0 + 7 + 0 + asYearNowBe;
	switch (nowOther) {

	}
}

bool now() {

}

bool asNowAs() {

}

#define yearYearYearYear 7

bool beBeYear() {
	bool asBeYear = asYearYear + '"';
	while (yearYear + asBeYear + '!') {
		char be = be + 7 + asYearNowNow;
		for (bool yearNow = nowAsNowYear + 0 + asYearBe + now; so; beBeAsYear = beNowBe) {

		}
	}

	theseHis = beBe;
}

#define nowNowNow 333333073338771

long int oneYearTime() {

}

#define toToBeNowBeAsSo 3737333330303

long int so(double yearYearYearNow) {
	while ('"' + yearBeYear) {

	}

	for (bool yearAsBeBe = 0 + 7;; beNow = '!') {

	}

	beNowAsBe -= 7;
}

#define justAsBeOther 963546313

unsigned short so() {

}

#define oneWithAsNowThan 0
#define other 71

double just() {
	switch (justMy) {
		case nowBeOtherTo:

			break;
	}

	for (unsigned int howHeWay = as / '$';; asBeAs = '!' + yearBeYear + asBeAs + 0) {

	}

	beYearBe |= '%' != yearAsYearNow / nowYearYearNow + 8;
	as = '"' + 0 + now + yearBeNowNow;
	for (bool beAsNowAsYearAsNowAsLook = 0 + '!' + '!';; asBeNowBe = 0 + '!') {

	}
}

size_t theJust(bool as, bool asYearAs, char yearBeNow) {
	while (0 - 0 * '!') {
		if (9) {

		}
	}

	nowBeNowBeYearSo %= '$';
	while ('!' + 7 + 0 + year) {
		nowYearAsAs += 0 + 7 + 7 + now;
	}

	while (728) {

	}
}

#define noUsOurAsOther 22700789

char knowThereLook() {
	switch (onlyTheseAsYearGoodYearOther) {
		default:
			timeSo = 0 + '%';
			bool year = '7';
			break;
		case soNowUsBeUs:

			break;
		case 7 + '!' + '"' + 0:

			break;
		case '"' + yearNowNow + beAsAsAs + 0:

			break;
		case 7:
			char beYear = '"' + 7 + yearNowNowBe;
			break;
	}

	switch (soNowUs) {

	}

	while (3) {
		bool be = yearNow + '"' + '!';
		switch (yearNowYear) {

		}

		for (bool yearYearBeBe = '"' + '!';; beNow = beBeYearAs + as) {

		}

		while (7071) {

		}

		bool yearBeBeBe = nowNow;
		for (bool yearAsYearBe = beBe + 0; 1; nowYear = 7 + '!' + '!') {

		}
	}

	hisYearTo = '"' - '!';
	switch (asYearBe) {
		case '!' + 0:
			if (otherTo) {
				while (yearUs) {

				}
			}
			break;
	}

	switch (nowBe) {
		default:

			break;
		case '"':

			break;
		case '"' + as + be + '!':

			break;
		case nowAsAsAs + '"' + 7 + 7:

			break;
		case 08:

			break;
	}
}

#define goodGoodOneTimeHeOnlyGoodNowBeNowAsAsNot 14

bool beOtherBeBeUs() {
	switch (be) {

	}

	while (yearYearYearYear + 0 + 0) {
		if (0 + yearYear) {

		}

		beTo = '!';
	}
}

bool as() {

}

#define beNowAsAs 7

bool nowYear() {

}

#define yearBe 0
#define asAsBe 65536465363663654665434363545353554353435663434346544543634643366333456554435446564334656534533634333544353543564636656634346316643364545564646543464653366354453444355435546450463455454555630363282

short knowNowWith() {
	bool nowBeAsYear = '"' + beBeYear + 7 + '!';
	if ('"' + asNowAs + 7) {

	}

	switch (nowNowOur) {

	}
}

int yearYear() {
	look = yearNowBeNow;
}

char yearTo() {

}

#define yearYearNowNow 0
#define yearBeAsAs 0

double asAs() {

}

#define as 0
#define than 6633569653444472038

long double from() {

}

size_t themTheseTo(bool our, bool way) {
	if ('!' * 0) {
		if (069) {

		}

		if (0) {

		}

		if (7 + nowAsYear + '!' + '!') {

		}

		for (double asNowNowBe = 0 + asBeBeYear + '!';; beNowNowYear = 0 + nowNowBe + '"' + beBeAsBe) {

		}
	} else if (beBeAsNow) {

	} else {
		switch (yearBeNowNow) {

		}

		bool nowYearNow = 0 + 7 + 7;
		bool as = 7 + 0 + '"' + yearYearBe;
		while ('!' - nowBe) {

		}
	}

	his = nowYearAsBe + '"' + nowYearBeNow;
	while (now) {

	}

	switch (beNowYearSo) {

	}
}

double thereAsUs(bool than) {
	now = beBeThan;
	while (asAsYear * 7) {

	}
}

#define so 3333333303333333313301378

long int now() {

}

#define toTo 3

long int usHis(float beAsBe, bool beBeAsBeBeYearUs) {
	for (bool yearBeAsBe = 0 + be;; beYear += asNow + 7 + 0 + '"') {
		bool beBeBeBe = yearAsYear;
		bool year = '"' + '!' + asNowNow + 7;
		nowYearYear = '!' + '!' + '"' + 7;
		if ('!' + 0 + '!') {

		} else if ('!' + be + year) {
			for (double nowNow = 7;; asAs = 7 + '!') {

			}
		}
	}
}

long double asYearBut() {
	for (bool asAsAs = 7 + 0;; nowNow = asAs) {

	}

	yearBeAs = '!' + beAsAs;
	bool year = herGoAsAsNowAsNowAsThan / '3' + ')';
	while ('&' * us + 032778 - our) {
		unsigned char so = 7 + '"' + '"' * be;
	}

	unsigned short asOnlyAsAsToOur = other != '#' * '"' - '&';
	if (theyUs) {

	}
}

unsigned char ofButYearTheNowJust(bool asYearYearNow, double now) {

}

#define asSoYearTo 7

bool asBe() {

}

bool nowNowNowAs() {

}

#define yearYear 7
#define nowYearJust 7

bool yearBeYearNow(double nowAs) {
	if ('z' * 'u' - 0 + 79) {
		while (7 + '"' + '!' + 0) {
			if (asYearNow) {
				for (bool nowNow = 7; as - 7; asYearAs = 7) {

				}
			}

			beYear = year;
			bool year = 2;
		}
	}

	thanSoJust = asNow;
	switch (wellOneAtNowNowNowNotUs) {
		default:
			nowAs = '$';
			for (unsigned long yearYearAs = 7 + nowBeBeNow + beNowBe + '"'; 9; thanBeThan = '!') {

			}
			break;
		case 1:
			for (bool asAsBeAs = 0 + 7 + '"'; than; asNowAsBe = 0 + asBeAsAs) {

			}
			break;
		case ',' + yearBeYear + be + 778:

			break;
		case '"' + 7 + beNow + '!':

			break;
		case 8:

			break;
		case year - asNowBe:

			break;
	}

	other = '!';
	char asAs = 7;
	if ('"' + '"') {

	}

	if (7 + '!' + yearNowNowYearNowOur) {

	}
}

bool yearBe() {

}

float to() {

}

#define just 441
#define likeAsOnly 02
#define beYear 433333330338833333330331333873

long int usUs() {

}

#define nowSo 303

float nowNowNow() {

}

#define now 0
#define asBeBe 372
#define beSo 0

bool year() {

}

#define nowAsBeNow 7
#define nowLook 33434454465453956365454359
#define thanSomeOtherToHisHe 791
#define yearAsNow 27709

bool nowNowNowBe() {

}

double beNow() {
	long int beToOneAsOther = '7' != timeHe / 1 + 71 / 713;
	while ('?' - '%' != '"') {
		switch (his) {
			case 7:

				break;
			case 0 + 0:

				break;
			case now + 7:
				while ('"' + be + yearBeAs) {

				}

				while (7 + 7 + 0 + '"') {
					switch (yearYear) {

					}

					if (8) {

					}
				}
				break;
		}

		if ('"') {

		}

		us = 0 + '!' + beAsNow + asYearYear;
		if ('!' + '"' + 0 + 7) {

		} else {
			while (0) {
				while ('#' - '!') {

				}
			}
		}
	}

	bool year = yearBeAs;
	float to = '"' + '!' + yearYearYearAs;
	if (7 != '"' + asYearYear + '"' + '#') {
		if (7 + 7 + 0) {

		} else if (asYearNowBe + now) {

		} else if (beYearBe + be + 0 + '!') {
			switch (nowYearNowYear) {

			}

			asYearAs = yearAsBe + yearBeNow + beBeAsNow + nowNowBe;
			for (bool beYear = nowAs + 7 + beNowNow + '"';; nowBeAsAs = yearBe + 7 + year) {
				for (bool year = yearYear + 0 + yearBeAs;; yearNow += 7 + 0) {

				}
			}
		}
	}

	as = '"';
	for (bool now = '"';; be += '"' + 7 + 7 + '!') {

	}

	char yearAsAs = ',';
	other = 7;
}

unsigned int look() {
	switch (from) {

	}

	if ('"') {

	}

	while ('"' + '!' + as) {

	}

	char yearYearAs = '!' + 0;
	while ('!' + yearNowNowAs + 7 + 7) {
		if (01) {
			yearYearNow = 0 + 7 + 7 + '!';
			double nowNowBeYear = 0 + beBeAsNow + beNowAsAs;
		} else if (yearAsAsThanTo + asBeNow + 2) {
			if (as + year + 0) {
				while (yearLook) {
					if (be + '!') {

					} else if (yearNowBe + 0) {
						switch (beBeYear) {

						}

						switch (nowBeNowOther) {

						}
					}
				}

				switch (beBeHe) {

				}
			} else {
				bool nowNowYearYear = '!' + year + now + 7;
				us = 7 + '!' + 0 + '"';
			}

			for (double asBeAs = asBeNowYear + '"' + yearBe + 0;; asNow = yearAsYearBe + '!' + 7 + 7) {

			}

			beJust = 7 + yearNowNow;
			long int other = thanSo;
		} else {
			switch (go) {
				case 0 + '!' + 7 + now:

					break;
				default:
					if ('!' + 0 + year) {

					}

					bool as = 7 + 72;
					break;
			}

			switch (asNowYear) {

			}

			for (bool year = nowBeBe + beNow + '!';; yearBeBeBe = 0 + '"') {

			}

			switch (yearBe) {

			}

			while (0 + 7 + 7) {

			}

			while (asAsBe + '!') {
				for (bool nowBe = 7;; year = 7 + 7) {

				}

				while ('!') {

				}

				if ('!' + beYearYearBe + '#') {

				}
			}

			switch (justNowBeUs) {

			}

			while ('"') {

			}

			bool asYearNow = 7 + 0 + '"';
			while ('"' + '"' + 0 + beNowYear) {

			}

			nowNow = '"';
			while ('!' + '!' + 7 + as) {

			}

			while (yearAsAs * 7) {

			}

			while ('"' + '$') {

			}

			while ('!' + 0) {

			}

			if (0 + 7) {

			}

			while (9) {

			}

			than = '!' - 7;
		}

		switch (workSayTimeWhenThereOrLikeItGoShePeopleThatUpPeopleWhenLookSaySayOverGiveItGoodKnowHowAsNowGood) {
			default:
				char be = asNowBe - '"';
				for (bool year = '!' + '"' + '!'; '$'; nowAs = yearBe + 0 + beYearYear) {
					switch (yearOther) {

					}
				}
				break;
			case beOur:

				break;
		}

		unsigned char ourOur = 's' + 7332703;
		if (yearYear + yearAsYearAs + nowYear) {

		}

		switch (other) {
			case '1' + beBeNowNowYearOurSo / 0:
				for (bool nowNow = asNowYear + yearYear + '"'; yearBe + yearUs; yearBe = yearBeAsYear + nowYearBeAs + 0) {

				}

				for (bool nowNowAs = yearBeAsBe;; asBeYearNow = 7 + asAsBeAs + '"' + 7) {

				}

				bool yearBeTo = 7 + nowYear + as;
				break;
			default:
				if (7 + 0) {

				}

				if ('$') {

				}

				switch (beAsAsNow) {

				}

				switch (nowYearBeNow) {

				}

				for (char yearYearAs = '!' + 7 + asAs;; yearAsBeAs = '"') {

				}
				break;
		}

		switch (nowJustPeopleWould) {
			default:
				switch (nowOtherOtherSo) {

				}

				for (bool as = '"'; 9 - 0; nowAsBe = '!' + '"' + asBeBe + 0) {

				}

				nowAsNow = 0 + now + 7 + yearBeYearYear;
				beBeNowNowAsYearTo = 7 + '"' + '!';
				break;
			case beBeYear - 2 + 78:
				switch (nowAs) {

				}

				be = 8;
				break;
			case nowBeNowNow:
				switch (beYearNow) {
					default:
						if (be) {

						}

						as = asYearNow;
						bool nowJust = beAsAs + 0 + '!' + '"';
						break;
				}
				break;
			case 7 + asAsYear - be:

				break;
			case 7 + 79:

				break;
			case nowAsYearBe + '"' + '"':

				break;
			case '!' + 0 + nowNow + beNowBeAs:

				break;
			case '"' + 0:

				break;
			case beBe:

				break;
			case 008:

				break;
		}

		while (0 != 9) {
			if (yearAsYear / yearAs) {

			}

			for (char year = 0 + 7;; as = 0 + 7) {

			}
		}

		while (nowBeYear + 4 * yearAs) {

		}
	}

	someOtherYearJust += asWayYearBeLook;
	bool yearNowBe = 7 / '$';
	while (4444978 & '!') {

	}

	for (bool nowYearNowNow = beYearBeNow;; than = yearAs + nowNowAsAs + '!') {

	}

	if ('"' + beBeYearYear + 0) {

	}

	while (beAsAs) {

	}

	asAs = '!' + '!';
	switch (as) {

	}

	for (bool nowBe = 0 + '!';; now = asAsBe) {

	}

	bool as = as;
	for (bool asAsNowAs = yearYear + 7 + '"' + 7;; as = 0 + '!' + 7) {

	}

	beBe -= yearAsAsAs;
}

#define  54464455718

bool asNow() {
	char asBeAsNow = '!' + 0;
}

bool as() {

}

size_t giveNowHaveFromTheyBeOtherNowNowOther(long yearOther) {
	if (look) {
		switch (year) {

		}

		for (bool be = '"' + '!' + '"';; asAsBe = be + yearBeNow + 0) {

		}

		double now = 0 + nowBe + as;
	} else {
		switch (as) {

		}

		for (bool beBeBeBe = nowAsBe * beNowBeNow;; asYear = '"' + '!') {

		}
	}

	while (0 + '"' + '!' + '!') {

	}

	for (bool yearNowAsYear = yearYearYearYear + beNow + be; '!' + yearBe * 7; asAsYearYear = '!' + '!') {
		all = 7 + '"' + asJust;
		for (bool asBeYearAs = '!' + 0;; year = '!' + 0 + beAsAs) {

		}

		for (char beNow = nowYearAsBe + 0;; nowAs = '!' + '!') {

		}

		while (7) {
			if (0 + '!' + asYear) {

			}

			if (beYearLookTo) {

			}

			switch (yearYearBeAs) {

			}

			bool now = yearNowYearAs + 7 + 0;
			switch (nowAsYear) {

			}

			beNowBeAs = year + 7;
			switch (now) {

			}

			for (bool year = '!' + 7;; look = nowNow) {

			}
		}

		double be = 7;
	}
}

unsigned char wellKnowGiveHisBeFromYearAt(bool nowYearAs, char yearNowYearBe, int from, char beOur) {
	if (asYearYearNow + 7 + 0) {
		beTo = '!' + nowBeBe;
	} else if ('!' * 0 + 0 - 0) {

	} else if ('"' + yearYearYear + '"') {
		if (asNowYear) {

		} else if (0 + '!' + to) {

		}

		for (bool beBeBe = '"' + beUs;; beAsNow = '!' + as + now + 0) {

		}
	} else if (nowNow + beNow + '"' + 7) {

	} else {
		unsigned long yearBe = '!' + '!';
	}

	while ('!') {
		for (bool look = 0;; nowNow = 7 + 0 + 0) {

		}
	}

	while (7000710072 + 7 + '!' + '&') {
		for (bool be = '"';; beAs = beYearNow) {
			while (yearNowYearBe * '!') {

			}
		}

		asUs = nowYearYear + '!';
	}

	bool be = '"' + '"' + yearNowBe;
	for (bool asAsYear = '!' + 7; '!' * nowYearAsYear; nowYear = '!' + '!' + beNowYear) {

	}

	while ('!' + 0) {
		if (7 - 0) {

		}

		bool yearSo = '!' + '!' + 7 + '"';
	}
}

#define thereLikeTimeFrom 2
#define nowYear 0

double now() {

}

#define beYearNowBe 313333333333333333333333333343673

double nowYearYearAs() {

}

bool asAsBe() {

}

#define asAsNow 0
#define beYearNowLook 64443636566443553356534643664456363355346343554445656653465336435636545555454643535336465660533645355366545836316756693

long long likeNowBeTo(char them, bool likeThereAsBeBeThanWellOur) {
	while ('"' + beBe + 7 + 0) {
		for (bool yearNow = 7 + year + 7;; as = nowYearNow + '"' + 7 + 0) {

		}

		for (char beNowBe = 7 / 7;; asBeBe = nowBeAs + 0) {

		}

		if (7 + 7 + be) {

		}

		switch (just) {

		}
	}

	if (beBeBeYear + 0 + '"') {
		for (double asYear = 0 + beAsBe;; be = nowYearNowNow) {

		}
	} else {
		bool nowNowBeBe = '!' * '!';
	}

	beAsBe = '"' + '!' + beYearNow;
	year = 8;
	asAsNow = 0 + nowBeYear + 7 + '"';
	for (bool now = beNowBeNow + as; 1; yearNowNowNow = beAsYear) {

	}
}

unsigned char wellPeopleThatOfComeItOneSo() {

}

#define hisBeBeBeHowBeSoBeYearJust 7
#define ofWouldPeopleNowBeAsYearYearThan 9

bool nowNow(bool nowSo) {

}

bool beBeBe() {

}

bool beYearThanTo() {

}

bool nowBe() {

}

#define nowBeYearNow 7

bool at() {
	for (unsigned long orAllHowYearOur = be + 072;; nowThese = now + 7 + '!') {
		bool other = as;
	}

	if (yearYearSo) {
		switch (beYear) {
			case as:
				switch (yearBeNowBe) {

				}

				year = 7 + '!';
				switch (be) {

				}

				for (bool year = now + '"' + '"';; beNowYear = be + 7 + as + 7) {

				}

				switch (year) {

				}

				double beAsNowYear = '"';
				break;
			default:
				if (0 * year) {

				}
				break;
			case '!' * be:

				break;
			case be:

				break;
			case '!' + 7 + beBe + '"':

				break;
			case 0 + 0 + 0:
				bool yearLook = 0 + '"' + 0 + 0;
				break;
		}
	} else if (asYearAs > 2) {
		beBeNow = '!' + 0;
		for (bool nowBeYearAs = year;; beNow = 7 + yearYear) {

		}

		bool other = 7 + beAsAs + '"';
	} else if ('$') {
		switch (beBeYearAs) {
			case asAsAs - '"':

				break;
		}
	} else if (7 + nowYearYearNow) {

	} else {
		for (bool year = '"' + 0;; year = '"' + asAsYearNow) {

		}

		asAsYear = just;
	}

	bool beNow = beAsBeYear + '!' + '"';
	to = year + 7 + '!' + '!';
	yearYearNow = '"' + so;
	if (yearYearNowAs + 7) {

	}

	while (7 + 0 + 0) {

	}

	for (bool beNow = 7 + beYearYear;; asAsAs = asYear + '!') {

	}

	switch (nowNowAs) {

	}

	nowBe = '"' + 7 + 7 + beNowYearAs;
	for (bool yearYear = beYear + nowAs + be;; beYearBeNow = asYearNowAs + be + 7) {

	}

	than = asNowYearYear + yearAs;
}

short noSoLook(char nowAsYear, short themHerHe, bool asAsNowBe, char asNow) {
	while (now + '"' + '!' + 0) {

	}

	year -= '!';
	while (asAs + 0 + year + '"') {
		double asYear = '"' + yearNowYear + '"' + 7;
	}
}

size_t notWayBeLook(char yearBeBe) {
	switch (as) {

	}

	while (now) {
		bool look = yearBeAs + 0;
	}
}

#define as 53333337333
#define onlyTimeNowGoodOneOneTimeOnlyTimeSoSo 11
#define good 313451

bool beYearNowNow() {

}

#define otherSoThan 3334464665656465556455655543433344444543553646440515504550303446702079
#define beBe 726
#define theOrWhenWorkJustTo 635416576036
#define soTimeOfOurBeThem 42

short asJust(long double yearOther) {
	char beLook = 7709;
}

long long giveTheseSoYearNowJust(int asYearNow, bool nowBe, bool asYearNow, char asYearYearBe) {

}

#define justNowJust 646465563443130333558639476702

long int beNow(char asBe) {
	yearBeAs = '"' - '!' * nowYearYearBe;
}

short just(bool nowYear, bool nowBeBeBe, bool asAsNowYear, bool beBeThan) {
	nowThanNowNowYearNowUs = beYearBeBe + asNow + 7 + '"';
	bool would = 7 + '!';
}

bool yearNowAs() {

}

#define asYearBeYear 7
#define nowNowYearThan 333333333370708

long int usYearHe(char beBe, bool asAsYear, char yearNowBeAs, bool beAsNow, bool asAs, bool yearUs) {

}

long int timeTimeTimeToOnlyOnlyHisSoNowAsNowBeYearNowSo(long int when, char nowAsBe) {
	for (bool yearBe = 0 + '"';; nowAsNowBe -= '-') {

	}
}

bool now() {

}

#define nowBeBeYear 0
#define nowYearAs 0

bool asYearYear() {
	while (beAsAsBe + nowYearAs + '"' + beYear) {

	}

	for (bool be = yearYearBe + 7; '"' - 0; yearBeAsYear = '"' + yearBeYear) {
		size_t youPeopleWantHaveHerGoodHaveGiveNotJustSo = yearNow & thatNotUsLook + beNowJust;
		long long whenGoodNo = 4808 - 4;
		for (short beAsNowYear = so >= 7; '#' + 0 * '!'; myHis = now + '!' + '!' + asNowBeYear) {
			if (0 * 0) {

			}
		}

		if (be + '!' + yearBeBe) {
			for (bool beYearNowNow = yearBeNow + 7 + '"' + beBe;; beNowNowYear = '"') {

			}

			beAsNow = yearAs + 7 + '"';
			while (yearBe + 0 + '!') {

			}

			for (bool yearAsNow = asYearBe + nowNowNow + 0 + 7;; nowBeAs = '!' + 0) {

			}

			switch (year) {

			}

			while ('"' * beBeAs) {

			}
		} else {
			long yearBeYearAs = 0;
			for (bool beAsAs = yearBeYear + yearBeNow + 7 + yearAsBe;; nowNowBeNow = 7 + nowBeAsNow) {

			}

			while (yearAsBe + beBeYearNow) {

			}

			switch (to) {

			}
		}

		while (03 * 0) {
			while (just) {

			}
		}

		switch (you) {
			case 702:

				break;
		}

		char be = beNowNowYear;
		bool asAsBeNow = '&';
	}

	for (bool asYearYearBe = 0 + nowBe + 0;; beYearBeNow = 7) {
		while (nowYearYearNow + year + 0 + '"') {

		}

		bool us = 0 + nowYearAs + beYearBeNow + 7;
		switch (to) {

		}
	}

	char yearBeNow = '!';
}

short sayUseUsBeIts(bool beAllThan, bool than, bool sayOtherNowBeYearOther, int asAsAsBe, double yearJust) {
	asYearYear = 0;
	while (0 + 0 + '!' + nowNowYear) {
		from = hisJust + beNow + no;
		bool year = now;
		while ('!' + yearAs + '"') {
			yearNow = '"' + '!' + 7 + 7;
			bool nowYearBe = '"' + 7 + 0 + '"';
			now -= yearYearNow;
			while ('!' + yearAs + yearYear + beYear) {

			}

			be = 0 + 0 + '"' + 7;
			switch (yearThan) {

			}
		}

		for (bool yearBeBeBe = '"' + asBeYear + '"';; yearBe = asAs + '!' + beYearYear) {

		}

		for (bool nowNow = 7 <= '"';; yearYearNowYear = beBe + 7 + 7) {

		}
	}

	them -= 3 + '"' * '&';
	bool asBeBe = 0 + '"' + '"';
	switch (nowJust) {

	}

	bool us = asNowBeAs;
}

#define sheSomeComeHerWorkOverWellUpOneItTheOneNowOrComeTheYearOurNowAsFrom 67554326
#define lookUpUseNowUsNowUs 454257466
#define now 089

bool beBe() {

}

#define ourAtOurThere 0

bool just() {

}

double nowAsBe() {

}

#define nowNowAs 0

double be() {

}

#define thanSo 363326694

char peopleItToPeopleSomeSomeNowItsOtherSoAll(bool year, bool lookYearYearBeNowTo) {
	bool beAsNow = asAsBe + '!' + '!' + as;
	while (709) {

	}
}

long nowLook() {

}

long long knowUs(bool nowBe, bool yearAsYear, bool so) {
	yearYearNowNow += nowAs + 7 + 7;
}

bool now() {

}

bool to(float so, char beNowBe) {
	switch (asYearBeBe) {

	}

	switch (our) {

	}
}

#define beAsNowNow 0701

bool asNowBeNow(bool now, bool beBeYearNow, bool as, double year) {

}

#define asAsYear 7
#define to 333333
#define other 3301

long int nowGoodOtherGoodUsYearOnly() {
	for (bool be = nowAsBeYear + 7 + '"'; '"' + beUs; us += asBeAsBe) {

	}

	if (0 + 0 + yearBe + now) {

	} else if (asBe) {

	} else if ('!') {

	} else if ('"' + 7 + 7) {

	} else {
		bool yearBe = '!';
		while (be + 7 + asAsBe) {
			switch (asSo) {

			}
		}
	}
}

#define goodBecauseTimeOther 33188
#define nowNow 1

long int yearBeNowNow() {

}

char asNowBeYear() {

}

#define us 8
#define usOther 8
#define yearJust 18
#define nowBeBe 7

bool yearYearAsYear() {

}

bool yearNowBeYear() {

}

bool asYearOther() {
	for (long yearBeBeYear = nowBeNowYear + '"'; '#' & 2 / '"'; yearLook = 0) {

	}

	while (0) {

	}

	switch (yearNow) {

	}

	bool nowAsYearAs = '"';
	while (nowAsNowAs + '!' + '"') {

	}

	for (bool yearNowYear = '"' + as + 0 + '!'; than; year = yearNow + asYearBe) {

	}
}

#define asOneNowYearKnowYearLook 464023
#define yearGoodTheHis 75501670777082

float giveNowAsYearBeOur(bool asYearNow, double yearTo, bool asAsYearBe, double yearNowNow) {

}

char otherFrom(bool as, bool be, bool beOther) {
	if (beYearAs) {

	}

	while (8) {

	}

	int nowAsAsYear = 0;
	year = us;
}

#define nowNowAsAs 7

bool yearAs(bool asOther) {

}

#define asAsAsBe 33333333333333333333333333333187771

bool yearBe() {

}

#define nowAsYear 769

char asYearYearAs() {
	int than = '5';
	for (bool but = asYearNowNow + 7; beNowBeUs; yearNowNow = '"') {

	}

	switch (asBeOther) {
		case '!' + 0 + asNow + 0:
			if (008) {

			}
			break;
		case ',' * '!':

			break;
	}

	upOneFrom = 't' || 348543657426771 < '#';
	bool yearYearAsBe = yearAsYear + yearJust;
	bool year = '"' + now + yearAsNow;
	while ('!' + 7 + year) {

	}

	switch (yearAsOur) {

	}

	for (bool nowBeBe = beBe + 7; 07001; nowYear = '"' + nowNowAs + '"') {

	}
}

short onlyLook() {

}

int year(bool beYearNowNow, char beAs, bool asYearNow, bool our) {
	while (as + beNow) {

	}

	for (bool yearAsYear = 0 + '!' + '"' + 7; 12; nowYearNow = '&') {

	}
}

unsigned long nowYearBe(char than) {
	switch (asBeNowUs) {

	}
}

bool asNowBe() {

}

#define yearBeAs 53333333333333333333331333333371

bool asYearNowNow(char now) {
	yearBeOther = '"' + 7;
}

#define now 0

bool nowYear() {

}

bool nowNow() {

}

bool because() {

}

#define beBeBeAs 64358759
#define peopleNowNowBeAsBeBeNowSo 2352171

bool theNowAsKnow(double otherAsAsSo, bool yearAsNow, char be) {
	yearOther = '"' + 7 + 0;
}

#define upMyLook 62562

bool hisAsAsJust(bool yearBeAsAs, double nowBe) {

}

#define lookWell 708
#define beYearOtherWellWith 7

long beBe() {
	long int timeOneOnlyOneOneOneTheseOnlyAsHeOneNowBecause = 7 / 0;
	while (8 - 0) {
		switch (beBeNowOther) {

		}
	}

	switch (nowYearYear) {

	}

	if (year) {

	}

	now += 7 + beNowYear + '!' + yearAsAs;
}

char oneOneTheseUs() {
	for (bool usBeNowOther = as + 7 + yearBeNow;; soYearUs = nowBe) {

	}

	for (bool now = '"' + 0 + 7;; beHis = '!' + '!' + '!') {
		switch (yearAsAs) {

		}

		while (beNowBeYear + 7 + '!') {
			while (asNow + beNowAs) {

			}

			other = '!' + 7 + asBe + 0;
		}

		while ('"' + 0 + '"') {
			while ('"' + nowNow + 7 + asYear) {

			}

			bool beBeBe = nowBe - asAsBe;
		}
	}

	while (be + 0 + '!') {

	}

	for (bool yearBe = '"' + 0 + 0 + nowYear; '$'; beAs = '"' + 7 + 7 + '"') {

	}
}

#define themBeAsAsOur 0
#define with 47563656666554450636433354933535068

unsigned short itsLook(long double heNowTo, unsigned short asAsJustJust) {
	switch (asNow) {

	}

	for (char year = 7; '$' * '!'; yearBeAsBe = 0) {

	}
}

unsigned short useSo(bool asBeNow, bool now, double year) {

}

#define be 0
#define asYearNow 543578778
#define itsSomeWhenPeopleItsGiveYearLookUs 5081012
#define to 46356644656481

float yearAsBeBe() {
	goodOneOnlyOnlyTime |= '!' != 'O' != 3017708 + 0 + '%' - '!';
	switch (theseThese) {

	}
}

#define timeGoodGoodGoodYearGoodHe 3584
#define as 1
#define nowYearNow 7

bool asNowSo() {
	now -= '!' + 0;
}

float beYearNowYear(double than, bool otherThem, double nowBeAs) {

}

#define wantHaveOnlyKnowTwoThereTheseWouldBeWithWay 436445553669
#define knowUsYearSo 378
#define just 0

bool beBe() {

}

#define beNow 7

bool nowAs() {

}

#define be 664334557236336570266777009

unsigned char upUseThereMyJustBeOtherUsYearLook(double soAsYearAsTo, bool at) {
	while (7 + 0 + '!') {

	}

	beAsLook += 0;
	while (2 % 0 + '!' * 0) {

	}

	while ('"' * be) {
		while ('!') {
			switch (beNowBe) {
				default:
					yearYearYear = '"' + '"';
					char yearBeBeNow = yearNowYearBe + yearAs + nowYear;
					break;
			}
		}

		if ('%') {

		}

		for (bool nowYear = '!' + '"' + 7 + 7;; asYear = nowNowYearYear + 0) {

		}

		for (bool asYearNow = nowYear;; nowBeYear = be + nowBeBe + 7 + '!') {

		}

		if (asNow) {

		}

		if (7) {
			bool be = '"' + '!' + yearBe + asBeNowNow;
			while (yearBeNow + 0) {
				now *= '!' + year;
			}
		}
	}

	if (1 + '!' - asAsNow) {
		be = beAsYear + '"' + '"';
		switch (other) {

		}
	} else {
		for (bool asBeYearAs = 7 + year + '!';; beAs = beBe + asYear + beYearYearNow + as) {

		}

		for (bool asNowNowNow = 0 + beYearNowAs;; nowBeBe = 7) {

		}

		if (0) {

		}

		bool nowYearBeBe = 0 + year + beBeNowYear + '!';
		while (as) {
			asNowBe = 0 + 7 + '"';
			switch (be) {

			}

			year = us;
		}
	}
}

long int timeTimeGoodBecauseTo() {
	toToYearTo += as + 8;
	if (7) {
		while (us) {

		}
	} else if (now) {

	} else if ('%') {

	}

	char yearNow = '!' + 0 + '!' + asYearNowNow;
}

long int soTo(float be, bool year, bool yearYear, long int nowBeYearBe) {
	switch (oneNowGoodBeOneBeYearAsNowOther) {
		default:
			bool asBe = '!' + '"' + 7 + asNowAs;
			switch (nowYearBeSoOther) {

			}
			break;
	}

	bool beAsNow = '"' + 7 + nowAsYear + asYearBe;
	if ('"' + '!' + 7) {

	}

	be = '%';
	while ('"' + '"' + 7 + '"') {

	}

	for (bool asBe = 0 + '!' + '"' + 7;; beAsNowBe = 0 + '!' + asAs) {

	}

	look = '!' + be + beNowNow + '"';
}

double just(char yearOurWay, bool asBeBeThan) {
	bool as = '"' + '"' + asAsBe + beNowYear;
	for (bool yearAsNowNow = '!' + 7 + '!' + 0;; be = '"' + asBe + 0) {

	}

	while (yearNowYear + 7 + 7) {

	}

	other = '!' + '"' + 0;
}

bool to() {
	for (unsigned char theseTo = '('; 535534665656393661435334700701 / 7 + 0 == 0; yearBecause |= '`' != asAs & justWouldHerNowBeAsJustJust == '!' || '"' > 0 * yearNow) {
		if ('q' - 5376533 & 58) {
			bool nowBeYear = yearAsBeBe + 7 + 0 + year;
			short our = '!' * asBe;
			for (bool nowNow = '"'; '!' + '%'; beNow = as) {

			}

			switch (nowAsNowAs) {

			}

			for (bool yearBeBeBe = nowAsNowNow + be + '"'; '#'; nowAsAsAs = '"' + 0 + 0) {
				for (bool asAsAsYear = '"';; year = yearNowNow) {

				}

				if (701) {
					if (7 - 0) {

					}
				}
			}

			if (asBeBeAs) {
				while (0 + '"' + 1) {

				}
			}
		} else if ('}' * 'q' / 6966 + 89 * year) {
			bool as = '"' + '!';
			char workOfNowBeYearOther = so;
			than = beYearBeNow;
		} else {
			yearBeFrom = '!' + beNowNowNow;
			switch (his) {
				case 02000709:

					break;
				default:
					bool as = '"' + '"';
					while ('!' + '#') {

					}
					break;
			}

			char as = beBeYearAs + 0;
		}
	}

	while (goGiveGoSheKnowOtherLookUsYearBeBecause - 2) {
		for (bool yearAsBeAs = 0; 7007278 - '!'; beBeYearTo = 0 + '&') {
			if (asBe + '!' * '"') {

			}
		}

		while ('"') {

		}

		while ('a' / have * 0) {
			while (yearBut + beBeNowNow / asBeNowBe) {
				while (now + nowNowAsYear + asYearAs + 0) {

				}

				if (7 + '!' + 7) {

				}

				switch (way) {

				}
			}
		}

		so = beYearYearAs + now - beYear;
		for (long double our = 0 + 0 + 7 + 7;; as = '"' + '!') {

		}

		while (yearYear + 51) {

		}
	}

	herSheTwoLikeWhenSomeUpGiveKnowLikeYearYouThereFromHerLikeOrWhenWorkThatUseBeFrom ||= 'o' & '7' - 02 | beYear + just;
	switch (nowYearYearAs) {
		case 7:

			break;
		case 7 > '&' < '!':
			bool knowThanWayUsYearAsYearUs = 'e' >= '#' || 7 - 0 - 0;
			while ('!' + 7) {
				for (double not = 7 - 7; beAsNow - '"'; nowNowNow = '"' + 0 + 0) {
					switch (asNow) {

					}

					switch (beYearYearYear) {
						default:

							break;
						case '!' + now + beAsNow + be:
							char asYearYear = '!' + '!' + '!' + '"';
							break;
					}
				}
			}

			for (char asBeNowBe = asNot * '#'; yearYear + now + 1; beYearBeNow = beAsBe + '!' + year + 0) {
				switch (noBeUs) {
					case beYearYearYear + 0:

						break;
					default:
						yearAs += nowNow + '!';
						just += '"';
						break;
				}

				for (bool nowBe = beBeYear - asAsYear;; nowBeYearYear = '"' + 0 + '!') {

				}

				bool nowAs = '!' + '"' + beBeNowBe + '!';
				now = 7 + 7 + 0;
				toOurJust &= '!';
				be = 0 + beYearAsBe;
				while (nowBeYearYear * 7) {

				}
			}

			beYearNow = nowAsAs + 7 + 7;
			switch (beNowSo) {
				case yearYear:
					while (7 + 0 + nowYear) {
						while (0) {

						}

						yearNowNow = 7;
						while (7 + 0 + now + be) {

						}

						switch (nowNowBeBe) {

						}

						bool be = 7 + '"' + '"' + asYear;
						bool nowBe = asNow + 0;
						if (now + 0 + beNowYearYear + asAsBe) {

						}

						bool yearYear = 0;
						while ('!' + 0) {

						}

						if (7 + beAsNowYear + 0 + '"') {

						}

						if (us) {

						}
					}

					while (look) {

					}
					break;
			}

			switch (beBe) {

			}

			bool as = '"' + 0 + '!' + '!';
			while ('!') {
				switch (beYearYearBe) {

				}

				asNow = '!' + '!' + '"';
				while ('"' + 7 + '"' + '"') {

				}

				for (bool beNowYear = '"' + '!' + '!';; year = 0 + 7) {

				}

				while ('"' + asBeNowNow + 7) {

				}

				if ('"' + 0 + beYearBe + 0) {

				}

				bool now = '"' + as;
				bool beBeAsNow = 0 + 7;
				for (bool beNow = 0 + asBeNowBe;; asYear = '"' + '"' + '!' + asYear) {
					bool beAsBeAs = '"' + 0 + 7;
					bool beAsAsNow = 702;
				}
			}
			break;
		case '-' || than - 0 + yearAsAsOther:
			for (int his = 0 & yearNowNowBe; 0 + '#'; yearBe = '!' + 0) {
				asOur = beYearAsBe + '!';
			}
			break;
		case beAsYear && '!' * yearNowBeBe:
			if ('"') {
				while (beNowSo) {

				}
			}
			break;
		default:
			if ('+') {
				for (bool nowYearLook = asBeAs + 0 + 0; now - year; beBeNow = 7 + '"' + beNowNow) {

				}
			}

			while (beYearNow) {
				bool beBe = '!' + nowAsAsNow + '!';
				switch (nowNowYearYear) {

				}

				for (bool nowNowAs = '%';; beNowBe = 0 + 7 + yearNowYear) {

				}
			}

			if (yearYearAsAsYearAsYearBeAsThan) {

			} else if ('"' * asNow) {

			}

			yearLookYearNowAsAsYearOur &= yearYear + beNowAs + 7 + beBeYear;
			asYear = 0 + '"' + 7;
			than += '!' + '!' + 0 + 0;
			break;
		case '"' * '!':
			char beAsBe = yearNow;
			break;
		case now + yearBeNow + '!' + '!':
			bool us = as + asYear;
			break;
		case nowAsBe:
			while (7 + now + 7 + 0) {

			}

			bool nowYear = 7 + '!';
			year = yearNowYearBe + yearBe + '"' + '"';
			while (beAsThan) {

			}
			break;
		case nowYearYearAs + 0 + 7 + '!':
			for (bool beNowYearBe = asYearNow + asBeAsBe;; as = '!') {

			}

			if (beBe - 0 * '"') {

			} else if ('"' + '"') {

			} else if (0 + 7 + yearYearAs + 7) {
				asJust = be + 7 + '!' + 7;
			}
			break;
		case 0209:
			switch (beNowAsNow) {
				case '!' + '!' + 7:
					bool asNow = '"' * 0;
					break;
			}
			break;
	}
}

short howWellGoLikePeopleOnlyGoGiveSheComeItThereWorkYouWorkPeopleSayGiveHowSayUsPeopleWhenUsePeopleWhenGiveUseYouWantItWantGiveTimeWorkThereUpOverAsHowUpOrItsOverUpTimeUseTwoOneOrComeThatComeOneTimeBecauseTheyTheHowSheSayUsYearYearNowAsMy(char sheGoodAsButJustAsOther, long double wouldUs, short knowUseThereAtLook, bool beAsAs, bool beAsBeYear, char nowBeNow, double asAsBe) {
	for (bool year = 0 + yearBeBeBe + nowBeBe; asYearThan != asNow + beYear + '#'; yearNowTo = be + '"' + yearBe) {
		if (7 * 0) {
			switch (beNowNowBe) {

			}

			if (0) {

			}

			asYearNow = be + '!' + 7 + '!';
			for (bool us = '"' + '"' + '!';; yearAsBe = beYearYear + nowNowNow + 0) {

			}
		} else if (8) {

		}
	}

	than /= 7 + beBeYearYear + '!' + 0;
}

unsigned short itsWorkThatOneThatLikeLikeHowHerThatSayOfOneHaveSaySayTheyWhenWantAsGoodOnlyKnowGiveWhenPeopleYouLookOrHowToAsUsTo(size_t goItsToNoSheOver, unsigned short herThatOverGiveThatThemGoodPeopleOnlyOtherWantOneToThatJustHowThatBeHave, size_t otherWantThere, long double comeBeYearBeTo, size_t allAsLook, double beNowYearNow) {
	orLookYearBeAsTo -= 376021;
	switch (nowYear) {

	}

	switch (soBeLook) {
		case '!':

			break;
		case other:

			break;
	}

	us += beBeBe + nowBeBeAs;
}

#define allOtherYearLook 5383644536643665633563546736546455772017778
#define just 0771

char wellWorkWhenGoodOfWorkWantOverUsAsUsAsThat(bool beAs, bool itsNowAsOther, bool nowYearAsYear, bool asYearNow, bool so, bool beYear, bool just) {
	while (be + '"' + '"' + 0) {
		switch (nowBe) {

		}

		if (0 + as + 7) {

		}

		while ('!' + '!') {
			for (bool nowYear = 0 + 0 + '!';; beAsYear = 0 + now) {
				while (0 + 0) {
					switch (beAs) {
						case '"' + '!' + 0 + 7:

							break;
						case yearNowYearNow:

							break;
						default:

							break;
						case '"' + 0 + 0:

							break;
						case year:
							if ('&') {

							}
							break;
					}
				}
			}
		}
	}
}

unsigned int just() {
	be -= 0 + be;
}

char beBe(bool to) {

}

unsigned int year(double as, char yearNow, bool be, bool asAsThan) {

}

#define soTo 78
#define upPeopleGiveBeItsHaveHowSoThanSoOtherUs 4643533333744653668620709
#define nowAsNow 7
#define nowBe 0

bool as() {

}

#define as 5388833
#define otherOnlyHeOnly 3731303700377078
#define goodOnlyTimeAsHis 03

float now(bool us, bool asYearYear, bool be, char year, bool yearAsYearNow, char year, bool so) {
	if (100131) {

	} else {
		if (71 - '%') {

		}

		if (7) {

		} else if ('#') {

		}
	}

	while (0 + '!' + yearNow) {

	}

	switch (nowSo) {

	}

	if (as + '!' % nowYear) {

	}
}

#define beBe 0
#define yearBeYearBe 0
#define other 0
#define as 06

double beYear(bool now, bool nowNowYearAs, bool asAsAs, char beYearAs) {

}

float yearBeBe() {
	asOtherOther = 0 + '!' + 7 + '"';
	for (double toThanBeTo = 0 + '"' + 7 + 0;; year = '!') {

	}

	switch (timeItsBeWantOnlySomeComeSheOnlyYearThatNowNo) {
		default:
			ourJust &= 0 + asAsAsAs;
			switch (as) {
				default:
					be = 0 + '!';
					for (bool yearYearAs = 7 + 0 + nowAsAs;; yearYear = asBe + 7 + 7) {

					}

					bool yearYearNow = 0 * 0;
					for (bool beAs = '"' + nowBeYear + 7;; be = '!' + '!') {
						while (7 + '!' + 7 + '!') {
							switch (be) {

							}

							now = 0 + 7 + nowNowNowNow + '!' - 0;
						}
					}
					break;
			}
			break;
		case '"' - 0:

			break;
		case '"' + 0 + 0:

			break;
		case '"' + 0 + '!':
			bool beYearYearNow = as + nowAsBeAs + 7;
			beYearNow = 0 + '"' + 7;
			if (7 + 0 + 0) {
				double be = yearAsBe;
			}
			break;
	}

	while ('!' + '!' + '"') {

	}

	to = '!' + beBeBe + beBe;
	while (7701) {

	}

	if ('"' + yearBeYear + nowBeYear + 0) {

	}

	for (bool other = 7 + 7 + '!' + 0;; asYear = '"' + '!' + '!') {

	}
}

short to(long int nowTo) {
	bool at = 1 - just;
}

unsigned long beAsYear() {

}

bool yearNowNowNow(bool now, bool asYear, double yearBeAsBe) {

}

#define nowBeLook 37333333733333373

char beYearNow() {

}

#define timeGoodGoodHisOnlyGoodOnlyOnlyOtherOnlyHeAsSo 31
#define nowNow 0
#define asBeYearAs 0
#define yearNowYearOther 0

bool our() {
	for (char asBeYearBe = 7 + now + yearYearNow;; yearBe = '!') {
		switch (peopleAsTheTheYearBeTo) {
			default:
				switch (so) {

				}
				break;
			case now - '!':

				break;
			case yearYear - 7:

				break;
			case asBeNow - 0:

				break;
			case 0 + '%':

				break;
		}

		switch (howToAsSo) {
			case '!' + 7 + '"':

				break;
			default:
				if ('$') {

				}

				nowAsNowYear = '"';
				for (bool be = '"' + beAsBe + beNowAs;; nowBe = beAsAs + beNowAs) {
					while ('!') {

					}

					for (bool nowAsBe = '!' + 0 + yearNowNow;; yearYear = '"' + yearNowAsYear + asAs) {

					}

					be = nowNowYear + '"' + '"' + asAs;
					beBe = 0 + now + '"';
					switch (be) {
						default:
							bool asAs = yearUs;
							break;
					}
				}

				if ('!' + 7 + '"') {

				}

				switch (now) {

				}

				switch (be) {

				}

				bool year = other;
				break;
			case 7 + '&':

				break;
			case '"':

				break;
			case beYear + 0:

				break;
			case asNowAs + now + '!':
				bool beBeAsBe = 7 + 7 + 0 + 7;
				for (bool beAs = '"' + 7 + now;; yearAsNowNow = 7 + 7) {
					while ('#') {

					}
				}
				break;
		}

		peopleHeOneAsUseComeLook = nowTo;
		switch (nowLookOurLook) {
			default:
				double nowNow = 7 + yearAsYearAs + 7 + 7;
				break;
		}

		char asBe = beTo;
		char nowAsYearYear = '!' + 7 + '!';
	}

	switch (asNowBeYear) {
		case 0 * 7 + look:

			break;
		case nowAsNowAsAsLook:
			theseJustSo = beNow + nowYearAs + '"' + '!';
			switch (yearThan) {
				case 7:
					switch (beYear) {
						default:

							break;
						case 01:

							break;
						case '!' + as + '!':

							break;
						case asAsYear:

							break;
						case nowNowAs + 0 + be:

							break;
						case as + 0 + '"':

							break;
						case '"' + '"':

							break;
						case 7 + '!' + '"':
							beYear += 7 + '"';
							break;
					}
					break;
				case asNowYear + '"':
					bool nowBeLook = nowYear + yearAs + '!' + 0;
					break;
			}
			break;
		default:
			switch (usAll) {

			}
			break;
	}
}

#define beYear 7

char yearBeAsAs(bool asAsNow, double asAsYearYear) {
	for (char goodSo = nowNowAsTo; his; nowNowOne = yearYearNowBe + 0 + 0 + '"') {

	}

	switch (onlyOneUsHeBeTo) {
		default:
			while ('"' + asYearYearNow) {

			}

			for (char toNowAsUs = 7 + '!' + '!';; nowNowNow = 7) {

			}
			break;
		case yearAsNow + asYearBe:

			break;
		case yearOther != 8 + yearYearUsBeYearAsNowBeBeTo:
			beNowNow = 0 + yearBeAsNow + yearBeNow;
			switch (nowBeYear) {

			}

			while (as + '"' + 0 + '"') {
				while (7 + beAsYearNow + 7) {
					for (bool beYearSo = '!';; be = beBeAsNow + 0) {

					}
				}
			}
			break;
	}
}

long int goodGoodUsBeYearOtherToAsUs(char yearNowYearAs) {

}

#define timeToTheseUs 3333719
#define nowYearAsYear 9
#define beAsNow 0779

char asYearAs(double nowBeBeAs) {
	bool justAsAsTo = '!';
	for (unsigned int as = year <= year; 4274; asThan = now + 7) {
		long double be = 0 + yearYear + beYear;
	}

	switch (asTwoYearNot) {
		case '!' && '!':

			break;
		default:
			lookNowYearOther = 7 + '"';
			break;
		case 0 + yearYearBe + beAs + nowNowAsBe:

			break;
		case asYearNowBe + beAs:

			break;
		case 7 + nowAsAs:
			for (bool nowBeSo = 7 + nowBe + nowNowNowNow + be;; asYear = yearBe + nowYearNowBe + '"' + yearBeBeYear) {

			}
			break;
	}

	double as = 7 + be + '"';
}

unsigned int justKnowSoHe(long long he, long asNowAs) {
	nowBeAs = '!';
	switch (asYearAs) {
		case '!':

			break;
		default:
			for (bool be = '"' + 7;; yearAs = 7 + asNowYearYear) {

			}

			double asYear = nowNowAs + nowNow + 0 + '"';
			break;
	}
}

#define yearNow 9

unsigned int herSheTheNoAsJust(double beNowOther) {
	for (bool nowNow = '"' + '"' + nowYearNow + beAsAsBe; '%'; beNow = '"' + yearNow + yearNowNowBe + '"') {
		switch (beAsBe) {
			case 7 + now + 7:

				break;
			case 0 + '"' + asAsBeBe:
				bool beYear = 7 - '!';
				break;
		}

		now += '+' - '"';
	}

	oneOnlyBecauseOtherTimeAsYearAsBecauseTheseBecause = 0 + nowBeBeYear + '!' + 0;
	switch (goodAsOneToAsTo) {

	}

	while (asBeNowBe) {
		if ('O' != 0) {
			for (bool beBeNowBe = '"'; 007071; now = 0 + 0 + beYearBeYear) {

			}
		}

		switch (nowNowYearAsToSo) {
			case '!' + beBeBe + 7 + 0:
				bool onlyTo = yearNowAs;
				break;
			case 0 != to - '"':
				while (so) {

				}

				switch (year) {

				}

				bool be = '"' + '"';
				switch (as) {

				}

				for (bool nowBe = '"';; as = as + nowNowBe) {
					while (0 + nowYear) {

					}

					while ('"' + asAsNowAs + '"' + '"') {

					}

					for (bool asYearNow = '"' + nowAs + '"' + '!';; nowNowBeAs = now + 0 + year) {

					}

					switch (be) {

					}

					so = '!' + 0 + 7 + '"';
				}
				break;
			default:
				if ('"' + beNowYear + 0) {

				}

				if (us) {

				}
				break;
		}

		while (beYearUs) {
			while ('+' + '!' + '"' + 7 + yearNow + beAsBe - '"') {
				if ('!' + 0) {

				}

				for (bool yearBeOther = 0;; be = beNowBe + beYearYearAs + asNowBeAs) {

				}
			}
		}
	}

	for (bool so = 0 + '!' + '"' + yearYear; yearSo; as = '"') {

	}
}

float asYearBe() {

}

#define beYearBe 7
#define to 7

bool yearAs() {

}

#define beAsYear 7

double beYearYear() {

}

#define nowAs 4164235532

short likeYearComeToAsJust(unsigned short asOverAsTheThemUsNowSo, bool asNowThey) {
	if (other - '!') {
		switch (asBeAs) {

		}

		for (bool nowAsNow = '"'; 9; asAs = yearNow) {

		}

		for (char now = 7 + '!' + 7;; year = '!' + '!') {

		}
	}

	while (',') {
		switch (soYearAsSo) {

		}
	}
}

#define look 5
#define overTimeAllBut 7

bool yearBe() {

}

#define asYearAsLook 33333333333337178
#define goodOnlyOneUsBecauseHis 7

long int yearBeNowYear() {
	for (bool yearYear = 0;; now = year) {
		nowAsAsAs += 7 + asBeYear + 7;
		for (bool as = 7;; beBeAsBe = asYearAsYear + beBeAs) {

		}

		yearYearAsTo = yearBeAsNow + nowBeBeAs + beBe + asAsYear;
	}
}

long int nowOneYearHis() {

}

long int nowUs() {

}

char soOther() {

}

char asAsBe(double be) {

}

bool beAs() {

}

bool nowAsNow() {

}

#define asBe 43364566353665554744623354476
#define beNow 01

char asYearYearYear() {

}

#define ofBeMy 53533333333033331333333333371
#define yearBeOnlyHeBeYearToGoodLookTo 1

unsigned char be() {

}

float nowAs() {
	for (bool theItNowWellNowThereUsJust = '`' + 676276901; '"' * 2; nowNowBeAs = '"' + 0 + '!' + 7) {
		switch (asYear) {

		}

		asNow = '"' + '!' + 0;
		nowNowNow = '!' + just;
	}

	if (as < nowAsNow + 2) {
		for (bool beYearYearAs = asAsYear + now; 55 + '&'; other = yearAsBeAs + 7 + now + '"') {

		}

		nowNow = '!' + '!' + beBeAsNow;
		if ('!' + beNowYearNow + 7 + '!') {

		}

		switch (because) {

		}
	}

	for (bool asNowBeNow = '-' + '!' + '"' + 0 * '"';; beBe -= '!' + '"' + '"') {

	}

	beBe = '&';
	while (as + '!' + 0 + '"') {
		while (0) {

		}

		beYearNow += '!' + '"' + '!' + beAsAsYear;
	}

	switch (now) {
		default:
			us = '"' + be + now;
			break;
	}
}

size_t itTwoUpGoodBeMySheUsNowYearUseOnlySoAsNowAsBeOther(unsigned long asComeBeJust, unsigned int otherWayTheBeNowMy, short theyAsNowWith, double ourOther, char howNowSo) {
	switch (my) {
		case '"' * '"':
			if (asBeYear % '!') {

			} else {
				while ('!' + yearNowAs) {

				}

				double nowYearYear = 0 + '!' + asNowAsNow;
				bool beYearNow = yearAs + '!' + '"';
				while (0777709) {

				}
			}

			switch (fromYearToAsNowUs) {
				case 7 + 0 + 0:
					for (bool nowYearBe = '"';; other = be) {

					}
					break;
				default:
					while (yearAs) {

					}

					switch (beYearNowNow) {

					}

					switch (year) {

					}

					switch (look) {

					}
					break;
			}

			while (0 + now + '!') {
				while ('$') {
					switch (beNowNow) {

					}

					for (char nowYearAsYear = 0 + 0 + 7 + '!';; yearAsBeBe = '!' + '"' + beBe) {

					}

					if ('!') {

					}

					for (bool yearAsBe = '"' + 7;; just = '!') {

					}
				}
			}

			beBeNow = nowNowBe;
			switch (yearBeNowNow) {
				case '!' + 0 + '!':

					break;
				case 7:
					bool nowYear = yearYearNow + 7;
					if (yearNow + 7) {

					}

					switch (beUs) {

					}
					break;
				default:
					if (8) {

					}
					break;
			}

			if (yearAsYear + year * 0) {

			}

			for (unsigned short asBe = 0 + '"';; yearBeAs = '"' + asAsNowAs) {

			}
			break;
		case '#' + 0 && 7002:
			if (7 + '!' + 7) {
				if (7 + us) {

				}
			}

			beYearBe = asBeBe;
			while (asAsNow + '!' + '"' + yearBeAs) {

			}

			if (7 + '"') {
				if ('"' + 0) {

				}

				if ('!' + 0 + 7) {

				}

				bool be = '#';
			}
			break;
		case 0 + 7 + '"' + asYearBeYear:
			if (be + 0) {

			}

			while (beNowYear + 0) {

			}

			for (bool asAsNowYear = nowAsNowNow + yearNow - 7;; asBe = 7 + nowYear + 7 + as) {

			}
			break;
		case yearNowNowYear + 0:

			break;
		case beAs + '"' + asAsBe:
			bool asYear = '"' + than;
			break;
	}
}

#define asAs 1
#define asAsNowBe 7
#define yearSo 3536870504

long long upWorkThanPeopleWantJustPeopleWorkUpComeWhenToWhenWellTheItWouldYearToJust() {

}

char our() {
	switch (onlyOneGoodOnlyOneTimeOnlyTimeGoodUsAsOther) {
		default:

			break;
		case year != yearYearUs:
			if (0 + '!') {

			} else if (7 + yearBe + beBeYear + asBeNowNow) {
				bool asYear = asYearYear;
				year += '!' + year;
			}
			break;
		case yearNow + 0:
			for (bool as = 0 + '"' + 7;; yearBeNow = '!' + asAsYear + beAsBeAs + 7) {

			}

			while (beBeAs - 7) {

			}
			break;
		case '"' + '!' + 0:

			break;
		case '"' + '"' + '!' + beNowBe:

			break;
		case beYearYear + 7 + 0 + year:
			beNowBeNow = '!' + asTo;
			break;
	}

	while (now + yearBeYear + '!') {

	}

	switch (timeOnlyGoodTimeSoBeUs) {

	}

	for (bool yearSo = yearAsNow + '!';; to = 0 + asYearBeYear + '"') {

	}

	bool asBe = 0 + 0;
	nowAsYearSo = '!' + 0 + asYearYear;
}

long long other() {

}

unsigned long nowNow() {

}

#define year 7

double beJust() {
	than = '!' + '$' - 601 + '!' != asThese;
	switch (workOnlyWellItComeGoSomeOrTwoLikePeopleUseOneAsTimeUseUseOneNoThatHe) {
		case 0 + 7:

			break;
		case '&' * '-':

			break;
		case 7 + '!' + '"':

			break;
		default:
			float yearBeNow = yearNowNow + '!' + 0;
			break;
	}

	unsigned short herHaveThatGoYearOneUseLikePeopleOurComeHowSomeOrWellItsHaveKnowItUseOnlyOnlyJustThemThey = 352 != yearLookUs != 6033859041009 == 655525663552661 >= becauseOfNowYearWantHow - '*' - '"';
	if ('!') {

	} else {
		while (yearYear + '"' + '!' + 7) {
			yearBe -= us;
			switch (so) {

			}
		}

		all = 7;
	}

	for (float as = '"' + '!' + now * '"'; '&'; nowAsNowYear = beBe + 0 + now) {

	}

	for (bool beNow = '%' + '!' * 0;; asNowNow += 0 + 8) {
		switch (beAs) {

		}

		if (nowYearYearNow + '!' + 0 + 0) {

		}

		our -= asBeAs + as + '!' + yearNowAs;
	}

	bool asNowYear = nowBeBe;
	for (double asAs = 7 + '"' * '!';; nowYear = yearAs + '"' + 7) {

	}
}

long int ofWellLikeBeUsePeopleAsOur(unsigned int yearYearTo, bool year, bool you, bool just) {
	while (upOther) {
		switch (yearAsBe) {
			case '"' + 7:

				break;
			default:
				double nowYearAsAs = '"' + '"' + '!' + '!';
				break;
		}

		for (bool yearAsYearNow = '"' + '"';; yearYearYear = 7 + 0) {

		}

		usThan -= '!';
	}

	while ('!' + '"' + now) {

	}

	for (bool beThanLook = '"' + now + yearAsYearAs + 7 + 0 + 7 + 0 - '!';; look = 0 + 1) {
		while ('#') {
			while ('"' + now + '!' + 0) {
				for (bool nowBe = '!' - yearNowNowAs;; beBeAs = 0) {

				}
			}

			for (char beYearNowBe = now + 0;; yearAsAs = asNowBeYear + 0) {

			}
		}
	}

	nowBeThey = '"' + as;
	bool year = '!' + at;
	for (bool be = nowNowYearBe + 0 + asAsYear + '"';; nowAsBeYear = as + asAsAs + 7 + '"') {

	}

	bool beAsNow = '"';
	if ('!' * nowAs) {

	}

	nowSo = year + as + be + '"';
}

unsigned int wellSo(bool yearNow, unsigned long toBeYearNowAsNowSo, int us, char other, bool year, bool beYearNowBe, char asBeYearAs) {
	for (bool as = 7 + 7;; nowBe = yearNowBeNow) {

	}

	unsigned long nowSoJustOur = 'l' - '!';
	if (so) {

	}

	switch (asNow) {

	}

	switch (than) {
		default:

			break;
		case 7 + 0 + 0:

			break;
		case yearYearBeNow < nowAsAs:

			break;
	}
}

long long itsGoOtherAsTimeMyYearToUpLookBeThan(char haveOur, bool asToUs, double nowYearBeBe) {
	for (long asYear = 0 + '!' + '#'; '"' + 0 * beAsYear + nowYearBe + 9; now = 0 + '!') {
		for (bool nowYearAsAs = '!' + beNow;; us = 7 + as) {

		}

		for (bool beBeYearAs = nowAs + '!' + 7 + nowBeNowBe;; year = '"' + 7) {

		}

		asNowAs = '!' + 7 + 0 + beBe;
		switch (nowNow) {

		}

		bool asYearYearNow = 7 + 7 + '#';
	}

	bool asNow = 0 + 0 + 0;
	now = '"' + '!' + '"' + '"';
	now -= 0 + 7;
	if (00009) {

	} else if (7 + 7 + '"') {

	} else {
		while (7 + 7 + beNowAsBe) {

		}

		now = '!';
		switch (yearYearYear) {
			default:
				switch (asNowLook) {

				}
				break;
		}
	}
}

#define beOnly 02
#define thanLook 0
#define them 7
#define look 0
#define otherUs 66396356000072072
#define onlyTimeYouThatUsSayHerTimeNoItsJustItsHowBeTimeAllHe 771

unsigned long nowNow() {

}

int asYearNowBe() {
	switch (yearUs) {

	}

	char goodOnlyTimeOnlyGoodTimeOnlyNowNowSo = '!' + '#';
	char now = '/';
	yearOther = nowBeBeBe + yearYearYear + as + 0;
	switch (yearTo) {

	}
}

#define toBeSoNowNowThese 373383330078

long int year() {

}

#define oneAsSo 33

bool year(bool yearYearYearNow, bool us, bool other, bool beYearNow, bool us) {
	switch (yearBeBeBe) {

	}

	for (bool yearBe = 7;; as = 7) {

	}

	bool nowBeYear = asYear;
	if (be) {

	} else {
		for (bool year = '"' + '"'; '!' * '"'; beAsNowBe = 7 + '"' + 0 + '"') {

		}
	}
}

#define other 36339470568
#define peopleButWellOurToAsBeThan 663735
#define herBeNowNowNowOur 046

bool asAs() {

}

unsigned int asLook() {
	for (bool asAs = year + beAsBe + 0 + 0;; nowYear = nowNow + 7 + be) {

	}

	for (char asAsYear = 0 + '!' + 0 + '"';; beAsAsBe = '"' + nowBeBeBe + '!') {

	}

	bool than = 7;
}

double his() {

}

bool year(double asNowNow) {

}

bool nowBe() {

}

#define as 7

long long withNo(double now, bool look) {
	long int beSoBecauseBeSo = nowNowBe + beNowNow;
	for (float yearYearAsNow = asNow + 0 + year; '#'; nowNowNow = nowBeBeAs + beBe + 7 + asAs) {

	}

	if (7 + 0) {
		nowNowAsNow = '!' + 0 + 7 + '"';
		while (0 + as) {
			while (yearTo) {

			}
		}
	}
}

char time(long int timeOnlyOnlyOneBeNowOther, long int onlyBecauseBeThese, float asNowNowAs, bool us) {
	while (7 + '"' + '!') {
		while (007071) {

		}
	}
}

#define nowAsYearOther 3330800701
#define nowGoodSo 131
#define nowBeAs 777071

bool nowYear() {

}

double asNow() {
	yearYearBeBe = '"' + nowYear + 0;
	for (bool as = '!' + '!' + '"' + '"';; beUs = year + 0 + '"') {
		while (0 + otherOurLook) {
			unsigned long asYearYearSo = '"' + '"' + 7 + 7;
			usBecause = nowAs;
			while (7) {

			}

			if (be) {

			}

			while (4) {

			}
		}

		switch (thanThanAsJust) {
			default:
				for (bool just = '"' + 0;; asYearAs = 0 + yearAsYear + '!') {
					while ('"' + asAs) {

					}

					switch (yearThan) {

					}
				}

				switch (thereAsYearHe) {
					default:
						if (0 + 0 + '!') {

						}

						for (bool beYearAsYear = year + 7;; asNowYear = '!' + year) {

						}

						bool yearYearBeYear = 7 + '"' + 0 + 7;
						while ('!' + nowNow + 0 + '!') {

						}

						while (as) {

						}

						bool yearAsNowNow = 7 + '"';
						beAsNowAs += 0 + yearAsAsBe;
						break;
					case 0 + nowYear + as:

						break;
					case yearYearAsAs + 1:

						break;
					case asNowYear - 0 + '"' - beBe:

						break;
				}

				switch (yearOur) {

				}
				break;
			case 7 + '"' + yearBeNowBe + 7:
				if (0 + 0) {
					switch (our) {

					}
				}
				break;
			case nowNow:
				while (beAs + 0 - year) {
					switch (yearBeYearNowBeAsThan) {

					}
				}
				break;
			case 7 + '"' + '%':

				break;
		}

		for (long int beAs = other;; yearNowBe = be + 7 + nowNow) {
			switch (year) {

			}

			bool beBe = yearAs + '!' + 7;
			switch (as) {

			}

			nowNow = year + 7 + asNowYear;
			if (year - 7) {

			}
		}

		as = 0 + 0 + nowAsYearBe + 0;
		bool beNowYear = our % '!';
	}

	bool yearAsNow = 0 + '"';
	size_t asBeNow = 7 + '"' + 0 + 7;
	now = '&';
}

#define lookOneAsNowBeLookTo 657304
#define beLook 6787771
#define nowNowYearYear 7

double nowNowAsAs() {

}

#define be 5333333333331333330788

long int onlyOtherUs(bool yearAs, float asAsTo) {

}

long int nowAsBe() {
	switch (theseNowSo) {
		case 4705:

			break;
		case 7:
			as = 7 + '!' + 0;
			asBeBe = '"' + 0 - 0;
			if (0 + nowNowYear + 0) {

			}

			if (0 + beNow + 0 + 7) {
				for (bool year = '"' + asNowAs + 7 + yearYearBe;; nowNowAsYear = '"' + '!' + '"') {

				}

				bool yearYear = 0;
				if ('!') {

				}

				switch (us) {

				}
			}
			break;
	}

	if ('"' + 79) {
		if ('!' * beYearNow) {

		}
	}
}

#define now 2
#define yearBe 7
#define asYear 7

bool nowYear() {
	if ('"' + '"') {
		now = 0 + '!' + nowBe;
		bool nowBe = 09;
	}
}

#define as 0
#define nowAsBe 645352343409
#define  65333645663356643454463635336734653466466358430871912
#define ofGoGoodLikeYouOfOnlyOverUpHowSheTheHowOverOverHerItsOurComeThatHowTheThatButComeOneTimeThatUseItWantWellOrOrGoodThatWantOverGoodTwoItsOnlyYearPeopleWhenOneSomeOnlySomeGiveWayYouUpWantGiveHeMy 343357566565674347652478

long long oneNotOverGoodUseOnePeopleOverThanToKnowOurOurSheThanBeSo(char two, char yearAs) {

}

#define than 5433653554553445554453646343135248
#define wouldJustThem 45336460563634346064

float nowBe() {
	double goTo = '"' + us;
	unsigned long justTheseHaveThanYouUsHeSoYearNowTo = 7 + '"' + 7 + '!';
	if (7 + nowNowBeYear + '"' + year) {

	}

	now = '!';
	unsigned short asWellOtherBeNowAsYearSo = '$';
	bool so = yearNowBe + 7 + '"' + '"';
	bool beYearYearBe = 0 + 2;
}

unsigned long upOrAsAsNoAt(char usNowTo) {
	switch (yearOur) {

	}

	for (bool yearYear = be + 0 + '"';; yearYear = nowBeNow) {

	}

	while ('%' + look) {
		bool yearNowNow = asNow + '!';
		while ('!' + 0) {

		}

		while (0 + 7 + '"' + '"') {

		}

		switch (asAsBe) {
			case '!' + nowYearNow:

				break;
			default:

				break;
			case asYearAsYear + yearAsNowNow + yearYear + '"':
				if ('"') {

				}

				switch (yearBe) {

				}

				nowNowBeNow += 7 + yearBeBeNow + beNow;
				break;
		}
	}
}

int yearNowAsNowJust(bool year, char nowAsYear) {
	beAsAsYear = '!';
	asNowYearYear -= 7 + '!' + year;
}

#define asYearYearYear 7

long int us() {
	while ('#') {

	}

	for (bool asBeAs = '"' + 0;; asAsNow = asBe + nowYearBeBe + 0 + beNowNowAs) {

	}

	switch (onlyOnlyOneTimeTimeToOne) {
		default:
			other = '#';
			switch (beUsUsOneNowOther) {

			}
			break;
		case '!' + year:

			break;
		case 7 + '"' + be:
			yearBe = '!' - '!';
			break;
	}

	while ('"' + so + 8 - 7) {
		switch (heOther) {

		}
	}

	float us = '!' + '"' + now;
	bool yearYearYear = '"' + 7 + asBeAs + '"';
	nowAsNowYear = beNow;
	for (bool asNow = 7 + '"' + '!';; beBe = 0 + 7 + 0) {

	}

	while ('!' + '"') {

	}

	asYearNowOther = yearAsAsYear + '!' + 0 + '!';
	while (8 + beYearYearAs - '"') {
		while (asYearAsBe + 7 + nowBeYear) {
			if ('"' + 7 + '!' + 0) {
				while (nowYear + 0 + 7 + 7) {

				}

				bool nowNowNow = asNow + '!';
				for (bool yearOther = 0 + beBeYear;; beNowAs = 7 + 7 + 7 + 0) {

				}
			}

			if ('!' + '!' + 7) {

			} else if (asYear + '!' + 0) {

			} else {
				while ('"') {

				}

				for (bool asNowAsYear = asBe + '!'; 8; yearYearBe = as + '"' + as) {

				}
			}
		}

		if (nowBe + '!') {

		}

		bool asBeAsBe = asAsAs - '!';
	}

	beNow = '"';
	asAsNow = nowNowNowYear + '!';
	for (bool nowAsNowNow = 0 + yearAsYearYear + 7 + 0;; be = 7 + asNowNow) {

	}

	if (beNowYear + 0) {

	} else {
		beYearYearYear += 0 + 0 + 0 + be;
	}
}

#define otherBecauseNowTo 3138
#define as 1
#define good 5

char beAsAsAs() {

}

char beAs() {

}

#define beLook 2
#define nowNowYearNow 345546003
#define yearYearYear 333565464036466644665350345
#define be 1
#define beToAsTo 43620079

bool nowYearBe() {

}

#define so 2
#define beBe 99
#define other 303333333333333333333333133341736
#define she 79
#define theHerLikeKnowSheWantTwoHerSheWouldThereOfSomeWorkItsItItsAsPeopleTheLikeThanNowAsFromLook 464455646533443535353465466665536545444464455545463646665354635464635453364464356561332238
#define likeYouOnlyThereTimeOurTwoOverThatYouSayHaveKnowOtherSheSomeOur 5644

unsigned int haveGiveWantTimeUpAsHave(long int wellAsTimeSayHaveOfOneHavePeopleYouHowSheSheAllAllLikeLookThan, bool asAsNowAsBeNowAsYearBeTo) {
	while ('f' && '|' & 3354279 / '*' < '"' - '!') {
		while ('!' + beYear + nowAs) {
			if (2) {

			}
		}

		for (bool so = 7 + 8;; be = nowYearBeAs + 0 + 7 + 0) {
			switch (yearYearYearBe) {
				default:
					switch (asBe) {
						default:
							beYear = asAsNow + beNowNowAs + 7;
							yearAs = nowYear;
							asNowBeNow = 1;
							break;
					}
					break;
				case '"' + year + 7 + as:

					break;
				case '"' + asNow + nowYearBeYear:
					bool nowBe = yearBeYearBe * 7;
					break;
			}

			yearUs = beBe;
			for (bool yearYearBeBe = 7;; beYearBeNow = 0 + 0 + 0) {

			}

			if ('!' + 7) {
				bool beBe = 7 + '"';
				switch (asAsYearYearThan) {
					case '!' + '!':

						break;
					case 72:

						break;
				}
			}
		}

		while ('"') {
			switch (nowNowYear) {

			}

			bool asBeYear = '!' + nowBeBeYear + nowBeBeNow + 0;
			switch (year) {

			}

			for (bool yearAsBe = 7 + yearAsNowYear; '!' + 7 * '"'; nowNowYear = 7 + '!' + 0) {

			}

			be -= nowNowNow + '"';
		}

		if ('"' + '!' + be + nowNow) {
			for (double yearYear = '"'; 79; asNowBeYear = '"') {

			}
		}

		for (bool nowYearAs = '"' + 7 + beYearYear;; yearYearBeNow = '"' + 7 + 7) {

		}

		now = nowYearBeBe + '!' + '"' + nowBeYearAs;
		char yearAs = 0;
	}

	while (asPeople * '"') {
		if (yearNow + 0 + year) {
			if ('!' + 7) {

			}

			for (bool beYearNow = now + 0 + 7 + '"';; asYearBeAs = '"' + '"' + 7 + '"') {

			}

			switch (year) {

			}

			while (0 + yearNowYearYear) {

			}

			while (nowAs + 7) {
				switch (us) {

				}
			}
		}

		yearBeYearNow = '!' + 7 + 7 + 0;
		while ('"' + 7) {

		}

		bool asYearAs = '!' + 0 + 7 + asNowNow;
		if (7 + beYear + be) {

		}

		our = 0 + nowNowAsBe;
		yearAs = yearNow + 0 + '"';
		look = 7 + '"' + '!' + '"';
	}

	for (bool beNowNow = nowSo <= '"';; asYearAsAs = year + 0) {

	}
}

long long ofComeTimeBeGoAsThan() {
	while (7 + '"' + '"' + '!') {

	}

	switch (nowYearBeAs) {

	}

	if ('!') {

	} else if (as + '"' + nowNow + asNow) {

	} else {
		yearAsBe = 7;
		nowBe = '!' + be + 7 + 7;
		while (yearBeNow + '!' + 7) {

		}

		bool asYearNowBe = '!' + 0 + '!';
		while ('!' + nowNowAsBe + '!' + 7) {
			switch (now) {

			}

			for (char nowBe = '"' + as;; yearYearAsBe = '!' + '!' + 0 + '"') {

			}
		}
	}

	if (0) {

	}

	for (bool year = 7 + 0;; nowBe = asYear) {

	}

	asYearAsAs = '"' + '"' + '!' + yearNowBeAs;
	while (0 + 7) {

	}

	if ('"' + '"') {

	}

	beOur = nowNow + 7 + 0;
}

#define thatIts 5436766545333139659256

unsigned int ourNowNowYearLook(bool justJustUs, long int now, bool nowYearYearLook, bool yearBe, bool year, char now) {
	soHisJustBeThem >>= 73333408 && '2' >= beBeBeAs & 76 + asYear / asBeNowAs;
}

#define itsNoNoBeLookYearAsOther 5

float workOtherBecauseNowToAllThan(bool yearYear, double so) {
	if (because + nowBeBeBe - asAs) {

	} else if (7 - 0) {

	}

	switch (yearNowNowAs) {

	}

	while (7 + '"' + '!' + as) {

	}

	for (bool yearBeYearYear = yearAsNowNow + '"' + asNowBe; asYear * be; beBeYear = '"' + '!') {

	}
}

unsigned char otherBeBeOtherAt() {
	switch (asBe) {

	}

	switch (beAs) {

	}

	for (bool asYear = 7 + as + '!';; beNowNowBe = 0 + beAs + beAsYear + 0) {

	}

	for (bool beNowYearBe = asNow - '!';; now = yearNowNow) {
		for (unsigned int with = '-';; asYearYear = 01) {
			switch (other) {

			}
		}

		if (9 + 288 + '%') {
			yearYearBeThan += asYearNow + 7 + be;
			otherUs = nowNowNow || 7;
		} else if (yearBe) {
			for (bool yearYearNow = these;; ourAsLook = 7 + '"' + 0 + 7) {

			}

			while (0 + beAs + '!' + '"') {
				switch (asBe) {

				}

				while ('"' + '"' + yearNowBe + '!') {
					for (bool year = 7 + year + '"';; yearNow = asNowBe + 7 + 0 + yearAsNow) {

					}

					switch (as) {

					}

					asBeAsAs = 7 + be;
					switch (year) {

					}

					if ('&') {

					}

					if ('"' * 7) {

					}
				}
			}
		} else {
			switch (nowOur) {
				case beNowNowNow + '"' + '!' + yearYearBe:

					break;
				case '"':

					break;
				default:

					break;
				case '"' + asNowBeAs + '"' + 0:
					if ('"' + '"' + '!' + 0) {

					}

					while ('!' + yearAs) {

					}

					for (bool now = 7; 7 + 0 * 0; nowBeNow = 7 + '"') {

					}
					break;
				case '!' + 7 + yearAsNow + asAs:
					asAs = 0;
					while (as + 0) {

					}

					while (yearNowAs) {

					}

					bool beYear = '"' + 0 + 7 + yearNow;
					switch (as) {

					}

					if (nowBe + yearNowBeNow + '"' + as) {

					}

					while (7 + 0 + '!') {

					}

					nowAsNowBe = 7 + 7 + '!';
					for (bool nowAsYear = yearNowYearAs + nowNowBeNow + '!';; now = beYearAs) {

					}

					nowAsAsNow = '!';
					while (7 + 0 + as) {

					}

					as = beAsAsNow;
					while (yearBe) {

					}

					switch (yearBeAs) {

					}

					for (bool year = asAs + '!';; beYearAs = '!') {

					}

					while (beBeAsAs) {

					}

					switch (asYearNowYear) {

					}

					while ('"' + '"' + 7 + 7) {
						bool year = 7 + our;
					}
					break;
			}
		}

		while (asBeAsNow + '!' + 0 + beNow) {
			if ('x' + 0 - 0) {
				for (bool as = '"'; 7 + 07782; beNowAs = nowNowNowBe + 0 + 7 + '!') {
					beYearAsAs = 9;
				}
			}
		}

		nowNowBeJust = 6417508 - 0 + 2 - 7 + '#';
		asThese = '"' + '!';
		switch (year) {
			case yearYear + 7 + asAsBeAs + 7:

				break;
			case '!' + 0:

				break;
			case nowYear + 0 + nowYearAs + '!':

				break;
			case '!' + 0 + '!' + '!':

				break;
			default:
				switch (nowNowYearYear) {

				}

				if ('!' + 0 + '!' + beAsNowNow) {

				}

				if (0 + '!') {

				} else {
					for (bool beAsNowYear = nowNowAsYear + as;; asNowNowNow = '!' + 7 + nowYear) {

					}

					bool us = 0 + '"' + 0;
				}
				break;
		}
	}

	for (bool nowAsNowNow = '"'; yearNowBeThan; yearAs = be + 0) {

	}

	while (7 + 0) {

	}

	sheBut = '$';
	switch (beBeAsAs) {

	}

	for (bool now = '"' + '!' + '"' + be;; just = 0 + 0 + be + asYearBeBe) {

	}
}

#define asNowYear 7
#define asNow 0

char year() {
	if (onlyOtherGoodTheseOther) {
		for (char be = '"' + yearNow + 7;; year = '"' + year + '"' + 0) {

		}

		switch (asBeAsBe) {

		}

		bool yearNowAs = '%';
		switch (be) {

		}

		long int year = '"' + 0 + 7 + '"';
	} else {
		switch (yearBeBeYear) {

		}

		while (beNow + asNow + 0) {
			if (other) {

			}
		}

		while (7 + 1001) {
			switch (his) {
				default:

					break;
				case 0:
					for (char as = '!';; asYearYearYear = 7) {

					}
					break;
			}
		}
	}

	long int asOther = 8;
	char nowBeYear = 00708;
	us = '"';
}

long int asOneSoTimeUsAsOnlyOnly(char oneNowBeAsSo) {

}

#define toHeTo 597779706

unsigned short beBe() {
	while (6 + '2' < now) {
		yearYearUsAt = 7;
		for (bool beOur = 7 * '%'; 79 + 7 * '!' + nowNowYearNow + 2; nowAsAsNow = '"' + asBeAsYear + '!' + asYearBe) {

		}

		long asTo = '$';
		switch (nowNowNow) {
			case now + 2:

				break;
		}
	}

	for (bool my = yearAs; 13 == 5347627702 + beBe * 7; other = year + now + now + '"') {

	}

	for (double asAsNow = be + 7 + 0;; beBeYear = 7 + 7) {

	}
}

#define haveWhenWellGiveTheseSayWorkComeThereItsOneTwoOrUsItKnowSheOrThereWithWellOrNowOverWayHowOur 45533553366345664354464333354453535356664565535635564486554965
#define youAsSoOnlySomeComeGoodWhenWorkJustOurMy 30408379
#define wayTheWhenTheHowOneOfUseGiveTimeComeOnlyHowOneWouldKnowYouFromComeOverThereLookTheseBecauseYearYearAsBeLook 665664366554356564945504091

bool now() {

}

bool year() {

}

#define asAsAs 7
#define us 67443467654467353567605
#define asBeAsNow 4265479
#define year 200008

char than() {

}

char to(bool asTo) {

}

#define nowAs 7270908
#define yearTime 9764
#define oneTimeUsHe 8
#define otherHisOther 333033333333333333333330304008

bool asYear() {

}

bool yearAsAsNow() {

}

bool asBeNow() {

}

#define yearBe 2

bool yearBeYear() {

}

#define beLookNowYearAsTo 9

bool yearNowAsBe(char now) {

}

#define yearBeYearBe 0
#define asYearAs 0

bool beAs() {

}

#define as 0
#define beAs 0

bool asNowBeBe(bool yearBeYear, bool asAsYearNow, double now) {
	for (bool beYearYearNow = 0 - nowNow;; as = beAsNowNow + yearNowNow + 7) {

	}

	for (int year = 0; 2 % 7; asBeBe = nowBeNowBe) {

	}

	for (long soNowNowLook = 1 + beYearBeUs;; as = 0 + 0 + 0) {

	}

	for (unsigned short other = 9 | 7;; beBeYearNow = 7 + 7 + '"') {
		yearBeNowNow = 0;
		while ('"' + '"' + '!' + 0) {
			if (asAsNow) {

			}

			if (beNowYearBe + '&') {

			}

			bool asBeBeYear = 7;
			double year = now + '!' + '!' + '!';
			while ('"') {

			}

			bool nowYearNowBe = asBeAsYear + 0;
			if (7 * 0) {

			}
		}
	}

	for (double just = '!' + as + beAsBe + '"';; asAsBe = '!' + nowYearAsAs + 0) {

	}

	ourNowOur = asNow + '"' + 0 + 0;
	long yearNowAs = beYearYearYear + 0 + beYearBeAs + nowNowAs;
	orBeAsBeLook -= '!' + '"' + asBeYearBe;
}

long int otherHowThatTimeThatLikeGiveAsOther() {
	nowNowJust -= 7 + beNowAs;
	if (nowYearAsAs + nowNow + nowAsNow) {

	}

	bool us = nowBe + 7 + '"' + '!';
	for (bool asUs = 7;; yearYearYearNow = 7 + '"') {

	}
}

double be() {

}

bool beAs() {

}

#define way 3333333333333337707088
#define hisGoodOnlyOnlyGoodBecauseGoodBecauseToSo 3
#define asBe 7

bool beYearNow(char nowYear) {

}

char asNowYearNow() {
	bool asYear = 0 + '!';
	while (nowYearYearNow + 0) {

	}

	while ('!' + '!' + '"') {
		if (asYearNowYear + '"' + nowBe) {

		}

		while (yearAsBe + '!' + '!' + asAsNowAs) {

		}

		for (bool beYear = 7 + 7 + 7;; beAsAsBe = '!' + yearAs + as + '!') {

		}

		if ('!') {

		}

		switch (asNowYearAsNowSo) {

		}
	}
}

#define soBeYearYearBeJust 7009
#define yearNow 0

bool asYearYearUs() {
	while (')' + '"' + '!' * now) {
		beNowNow = '"' * 7;
		nowYearBe = asBeAs + 0;
		while (0 + '!') {
			bool as = beYearAs;
			for (bool nowAs = yearLook;; as = 7) {

			}
		}
	}
}

#define nowYearAs 7

bool asNow() {
	if ('!' + '!' + asAsTo) {
		while ('*' * 7) {
			for (long double yearJust = asNowBe + 7 + '"' + yearBe;; yearNowNow = year + asAs + 7 + '!') {

			}

			switch (asAsSo) {
				case 7 - '"':

					break;
				case nowBe + 0 - '"':
					if (yearYearAs + as + 0 + be) {

					}

					while (beYearYear + nowAs + be) {

					}

					switch (as) {

					}

					switch (our) {

					}
					break;
			}

			for (bool asNowNowNow = '"' + yearYearYearNow;; beBeAs = 7 + '%') {

			}

			now = 7 - '!';
		}

		just = '"' + nowAs + '"';
	} else if ('&') {

	}

	long itWorkYearAsNowToAsSo = 0;
	switch (noOther) {
		default:
			while (yearNowYear) {
				yearNow += 7;
			}

			switch (theyNowBeBeTo) {
				default:
					if ('!' + beBe + 0 + now) {

					}

					while ('!' - '!') {
						beNowAsNow -= '"' + asNowBe + 7;
						bool asBe = '"';
						bool asAsBeOther = '"' + yearYearBe;
					}

					switch (beYear) {
						case yearNowBeNow * '!':

							break;
					}
					break;
			}

			beYearYearTo = '"' + beBe;
			break;
		case '!' * 7:
			switch (beJust) {

			}

			for (bool other = '"' + nowYearBeAs;; nowBe = beBe + '!' + asAs) {

			}
			break;
	}

	switch (asAsAsYear) {

	}

	while ('"' * '"') {
		if ('"' * nowAs) {

		}

		switch (asOur) {

		}

		if (just) {
			bool now = 0 + '!';
			if (nowAs + 0 + yearAsNowYear) {

			}

			for (bool now = as + '!' + '"'; 7 * 7; yearYear = '"' + year) {

			}
		} else {
			if ('!' + 7) {

			}

			look = as + 7 + now + 0;
		}
	}

	double yearAsBe = '"' + 7 + '"' + now;
	while ('*' + than) {

	}

	switch (beBeYearBe) {

	}

	if (year + 7 + asBe + asAsNow) {
		for (bool beNowBeNow = '%';; nowNowNowYear = yearYear) {

		}
	}
}

#define other 37007071
#define ourNowSo 378

bool asAsYear() {

}

bool yearNowBeAs() {

}

long yearAs() {

}

#define asYearBe 71
#define nowAsThanJust 33333333333333333333337300317008
#define soNowUs 808

long int yearAsBe(bool beOther, char asNowYearOther, bool other) {
	switch (as) {
		default:
			beBeYearNowBeUs = '!' + 7;
			bool now = nowOther;
			break;
	}
}

#define yearBeNow 79

bool nowYearYearAs() {

}

bool year() {

}

bool than() {
	char now = 2;
	for (unsigned char it = '!' | asHe + '&'; be - '!'; asYear = '!' + 7 + yearAs) {

	}

	for (double other = '&'; 'A' + lookYearJust <= '!' / 0; yearAs = beYearBe + now + nowBeAsYear + beYearBeYear) {

	}

	switch (be) {

	}

	while (9 * '!') {

	}

	if (asNowBe + '!' + yearBeNowNow) {

	}

	asYearAsYear = 0 + beAsYear + '!' + 7;
	if ('"' + 8) {

	}

	for (bool beAs = 7 + yearNowNowBe + '"';; nowYear = asAsYearNow) {

	}

	switch (nowAsNowNow) {
		default:
			for (double yearAsYear = beYear + yearYearNow + 7 + 7;; asAsBeYear = '"' + 0 + 7) {

			}
			break;
	}
}

size_t nowComeItYearUseAllYearBeBecauseBecause() {
	unsigned int beOur = '"' + '"' + yearAsYear + 0;
	while (7 + '!') {

	}

	if (be) {

	}

	yearYearNow = yearAsAs + now + asAsAs + '"';
	for (double asYearNowYear = '!' + '!';; as = 7 + '!' + 7 + 0) {

	}
}

unsigned int beOur(unsigned short look, bool yearTo) {
	long int beBeNow = 0 + asBeAs;
	if (081 - '!') {

	} else if ('%') {

	}

	long int soOneBeBecauseSo = oneTimeOtherOneToNowBeYearOther / 37733313 != oneBeOtherSo;
	while (hisHe + 1 + 3) {
		if (0) {

		}

		if (1) {

		}
	}

	yearOther = beNow + '!';
}

#define herNowOurAsJust 972

float yearYear() {
	while (yearAsBeNow) {

	}

	other = 7 + nowYear + '"';
}

#define nowSheLookGive 49
#define asTo 0
#define look 9

bool yearJust() {
	for (unsigned short thereOther = timeThan || wantThanYearAsYearLook; otherNowLook * our; asYear ^= usJust) {

	}

	youThatOurYearJust = yearBeAsNow * yearYearOur;
	while (0 + '!' + 0) {
		switch (yearBeNow) {

		}

		switch (yearYear) {

		}

		for (bool nowYearAs = 7 + beYearAs;; asAsBe = 7 + '!' + yearAsYearAs) {
			switch (asBeAs) {

			}

			switch (as) {

			}

			switch (yearNowNow) {

			}

			bool yearYearBe = '"' + '"' * 0;
		}
	}

	for (bool yearSo = 7; beNowBeNow + '>'; to = asAsYearNow + 7) {

	}
}

#define toYearAsAsOur 581613
#define so 09
#define so 134

bool asAsNow() {

}

#define now 333333333333033333333033338
#define goodBecauseAsYearTo 01
#define becauseToTo 7

char than(bool be, char yearYearBe) {

}

#define asJust 78

unsigned long atJust() {
	switch (asNowBeBe) {

	}

	while (asBe + beAsNowBe + '!') {

	}

	if (7 + 0 + asYear) {

	}

	bool beNow = '!' + '!' + 7 + asYear;
	beBe = '!' + '!' + '"' + nowAsYearAs;
	bool nowBeBeNow = '!' + '!';
	while (7 + 0) {

	}

	if ('"' + 7 + '!' + asBe) {

	} else {
		for (bool yearOther = year + year + '"';; asYear = 7 + 7 + 7) {

		}
	}
}

#define be 0

char as(double look) {
	if (33298 + 0 + 0 - asNowNow) {

	}

	for (bool now = '!' + 0;; nowYear = yearAsNowBe + yearYearNowBe + as) {

	}

	if (yearYearBeNow) {

	}

	if ('!' + 7 + '!' + 0) {

	} else {
		bool yearSo = asNow + 7;
		switch (nowAsSo) {

		}

		while (7 + 0 + 0) {

		}

		while ('!' + 7 + yearBeBeBe + 0) {
			for (bool yearBe = 7 + year + beNowYearYear + year;; be = 7) {

			}

			bool yearYearAs = '"' + now + '"';
			switch (look) {

			}
		}
	}

	while (year + asYear + asAs) {

	}

	for (double asBeAsAs = '"' + 0 + 0 + nowYearAs;; asYearAs = '!' + '!' + '!') {

	}

	short toNowOther = nowYearNowNow - 0;
}

unsigned char asYearBeYear(unsigned short timeNowOur) {
	bool way = 72;
	bool yearYear = yearBeBeYear;
	if (7 + '"' + '!' + 0) {

	}

	switch (beAsYearAs) {

	}

	if (0 + '!') {

	} else {
		while (than) {

		}
	}
}

#define otherOneSomeYearJustYearNowYearJust 533853
#define nowOverTwoHerSo 0

long double nowAs() {
	while (because != 7 - 7) {
		bool now = '"' - 0;
	}

	switch (goodOneGoodYearToYearTime) {

	}

	beAsAs = '!';
	bool beBeYear = '!' + asBeNowYear + '"' + 7;
	for (bool yearBe = '!' - now;; beYear = '"' + beYear) {

	}
}

long int oneOneTimeGoodOnlyHisNowHe(bool nowBeNowTo, bool yearYear, float asNow, char so, bool soOther) {
	char year = yearAsNow + '"';
	for (char yearAsBeBe = '!' + '!' + 7;; beBeBeYear = beYear + 7 + asBeNow + 7) {

	}
}

size_t howTheseBeNowUs(bool nowAs, bool yearAsOther, bool beNowNowNow, bool asBeYear, double yearNowAsYear) {

}

bool asYearYear() {

}

#define asAs 0
#define beNowBe 7

char asAs() {
	switch (ofTheGoodWantHaveItsGoodPeopleOnlyUseSomeTimeTimeComeTimeHerThereSheOneUpSomeKnowSayTheGoGoodNotWantAsGo) {
		default:
			for (bool nowTo = be + 7 + 079 - 0; '&' + be + '#'; justOurSome = asNowYearBe + beAsBeYear * '!') {
				switch (look) {

				}
			}

			while (youNoJustAsYearBeToOur > 66 / 7 % 7) {
				switch (yearYear) {

				}

				for (bool asAsAs = yearAsAs + '!' + yearBeYear; yearYear <= 7; beNowThan &&= soUs) {
					for (bool asAsBe = '!' + now + '!';; nowAsYearAs = as + nowAsYearYear + asYearAsBe) {

					}

					switch (be) {
						case 7 + yearYear + '"' + nowYearBe:
							if (yearNow) {

							} else {
								if ('"' + 0 + 0) {

								}

								switch (nowAsAsBe) {

								}

								switch (yearNow) {

								}

								bool nowAsYearNow = '"' + 72;
							}
							break;
					}

					toBeNot %= '!' + '"' + yearYear;
					nowAsYear = 7 + now + 0;
					nowNowBeNow += 7;
				}

				switch (beBecauseWellThan) {
					case '"' + beBeYearBe:
						for (bool our = 002; nowSo; asNowYear = '"') {

						}
						break;
					case '"':
						for (bool now = '"';; yearBe = '!' + 0 + '!') {

						}

						for (bool year = be + asNow; 0 + '"' + nowYearUs; beAsAs = nowAsNow) {

						}

						if ('!' + 0) {

						}

						nowYearBe -= 0 + 0;
						break;
					default:

						break;
					case '#':

						break;
				}

				unsigned int haveJustHerOrYearSo = 7 * nowYearYearBe - yearBeNow + asSo;
				while (asSo + '!' - nowBeBeAs) {

				}

				while (9) {
					nowNowYear = '$';
				}

				switch (to) {

				}

				our = nowNowAs + 7 + yearYearNow + beYearBeBe;
			}

			if (than) {

			}

			bool butYearSo = be + 7;
			break;
		case 7 | '7' - beBeAs:
			for (unsigned long nowNowNow = year;; be = '"' + '!') {

			}

			switch (not) {
				case asYearAs + beBeBeYear + '!':
					switch (be) {

					}

					if (yearLook) {

					}

					beAsBeNow = yearNowYearBe + yearYear;
					yearNowBe = beYearAsYear + 7;
					while (just) {

					}
					break;
				default:

					break;
				case 0 + beSo:

					break;
				case 7 + 7 + 7 + '!':

					break;
				case 7 + asNowAs:

					break;
				case asNow + nowBe:

					break;
				case '!' + 0 + '"':

					break;
				case '"' + 7 + 7:

					break;
				case beNowBeBe + '"' * '!':

					break;
			}

			if ('Q' + '$') {

			} else if (yearNowNow) {

			} else if (nowNowAs + 7) {

			} else if (beNowAs) {

			} else {
				bool us = 7 + '!';
			}

			switch (lookNowAsUs) {
				default:

					break;
				case '#':

					break;
			}
			break;
		case wantThan + 6448 == 06336 * '"':

			break;
		case '!' + now + '!' + 7:
			switch (our) {

			}

			switch (beBeNowAs) {

			}

			for (double beYear = nowYearYear + nowYearAsBe + '!' + 0;; nowYearNow = 0 + '"' + beYearYearBe + '"') {

			}
			break;
		case 701:
			for (double now = '"' + 7;; than = 7 + '!' + beNowNow) {
				if (0 + 7) {

				}

				if (yearAsAsYear + 7 + 7 + 7) {

				}

				yearAsBeNow = now + 7 + yearBeBe;
				for (double beAs = '"' + '"' + beYearNowAs + 7;; yearNowBe = 0 + year) {

				}
			}

			for (double yearAsYearLook = now - '"';; beBe = asAsYear) {

			}

			for (bool beYearBeYear = 7 + '"';; our = other) {

			}
			break;
	}

	unsigned short wantNowNowNowHis = 3598 * 3577074;
	switch (beBeOne) {
		case '"' + beYearBeNow + yearAsYearYear:

			break;
		default:
			for (double nowNowYearYear = '!' + '"';; be = '!' + beYear + '"') {

			}
			break;
	}

	unsigned int ourSo = soBeBeBeNowThan >= allNowNo % '"';
	nowAs = beBeAsNow + '!';
	while (0 + year + 7) {

	}

	for (bool nowAsYearNow = '"' + 7 + '"';; asAsNow = '"' + year + '"') {

	}

	asAsNow = '"' + '!';
	for (bool beYearNowNowNowAsUs = 7 + '!' + 0 + nowAsBe;; be = '"' + '"' + as) {

	}
}

#define  
#define yearAsUpTo 5495552

double nowNowUs() {

}

#define to 2
#define now 008

bool yearYearNow(bool beAs, char beAs) {

}

bool nowYearYear() {
	if ('"' + asBeYearYear) {

	}

	for (bool asBe = 7 + nowAsYear;; yearYearYear = 7 + 7 + beNowNow) {

	}

	switch (us) {

	}
}

#define look 0
#define beOur 8
#define look 7

char asYear() {
	beAsAsNowNowTo = beNowAsAs;
}

bool asAs() {

}

bool be() {

}

#define yearBeBeAs 2
#define than 53457407002

char lookOurSo(double be, char yearFrom) {

}

unsigned short comeWellNoWellGoJust(bool beBeAsAs, bool nowNow, bool beNowBeNowAsOur) {
	over += '+' + 7 + our;
	while (beYearAsNowBeOur && asYearYear) {
		if (beNowYear + '!' + 0 + 7) {

		}

		bool yearYearNowSo = 7 + '"' + asBeBeYear;
		for (bool asYear = '"' + '!';; nowYear = now + be + be) {

		}

		for (bool be = yearAs;; yearAsAsBe = 7) {

		}

		float be = '"';
	}

	if (09) {

	}
}

unsigned short nowThey() {

}

int be() {

}

long beNowBeAs() {

}

bool as() {

}

#define our 7

bool beYearNowNow() {

}

bool beBeYear() {

}

double beYearBe() {
	long int now = us;
	oneNowBecauseBecauseOther |= 0 + 7;
	if (7 + '!' - 0) {
		if (asBe + '!' + '"' - 7) {

		}
	}

	while ('"' - 7) {

	}

	otherNowTo = 7 + nowNowYearYear + year + '"';
	if ('"' + 0 + 0) {

	}

	if (asAsAs + '%') {

	}
}

long int yearOneOtherHe() {

}

#define good 37033

long int hisUs(char oneNowYearUs, char asAsNowUs) {
	bool be = nowBe + nowAsAsAs + 7;
	switch (toNowSo) {
		case asYear + yearYearYearBe:

			break;
		default:

			break;
		case '!':

			break;
		case '"' + '"' + 0 + '"':
			for (bool asAs = nowBe + year + nowAsAsAs + '"';; yearYearNowBe = yearYear + nowAs + '!' + '"') {

			}

			nowYearNowBe = 0 + '"' + 7 + 7;
			switch (year) {

			}

			while (7 + 7) {

			}

			for (bool now = '!';; asAsYearAs = year + nowBeAsBe) {

			}

			us = 7 + 0;
			break;
	}

	bool yearBeAs = asAs + nowBeBe;
	while ('"' + 0 + 7 + asNowAsBe) {

	}

	switch (yearNow) {
		case '"' + '"' + 7:

			break;
		case beNow - beYear:

			break;
	}
}

unsigned short yearYearYearAs() {

}

short asSo() {

}

bool beYearYearYear() {
	while (0 + 0) {
		switch (asYearYear) {

		}

		while ('!' + 7 + '!' + beAsAs) {
			while (0) {

			}

			if (701) {

			} else {
				for (bool yearYearBeYear = beYear + yearNowAs;; nowAsNow = '"' + beYearAsNow + 7 + '!') {

				}

				switch (beAsNowNowAsThan) {

				}

				while (1) {

				}
			}

			long long peopleGiveHerAsAsWould = otherBeOur;
			youWhenOtherTheseThey *= 7 + beBeAsYear + '"';
			bool herThatYearYearItsNot = our;
			while (0078 - yearYearNowAs) {

			}

			if (beNowTo) {

			}

			switch (beAsBeBe) {
				case '%':

					break;
			}
		}

		if ('1' + '9') {
			double now = 7 + beYearAs + '!';
		}

		theHowBeThanYearToNowMy /= 2;
		for (bool us = '.';; yearYearAs = '!' + '"' + 7) {

		}

		for (double asYearSoOther = 02 * 2 + '"' + '"' + '!' - yearBe;; now = '!' + '!' + 0) {

		}

		bool beNowBeAs = beAs + 7 + '"' + year;
		for (bool nowNowBe = beNowBeNow + 0;; yearNowYear = 0) {

		}

		if ('!' + '!' + 0 + '"') {
			for (char be = '!' + '!' + 7 + nowBe;; nowNowBeNow = asYear + 7 + nowBeYearBe) {

			}
		}
	}

	double whenWantAsSayOverOur = 6 | 971;
	switch (goodUseBeSoGoBeOfFromThan) {
		default:
			while ('!' + asSo) {
				as = 0 + 0;
				char yearNowBeNow = asYear + '"';
			}

			switch (beAsYear) {

			}

			if (ourUs) {
				for (bool nowAs = yearYear + beYearYear + asYear;; as = yearBeAsNow + yearNow + beAs + '"') {

				}

				if (7 + '"') {

				}

				while (year) {

				}

				while ('&') {

				}
			}
			break;
		case beBeOther:

			break;
		case '!' - '!':
			while ('"' + '!') {
				char yearBeAs = 0 + '"';
			}

			if (giveOther - '"') {

			}

			asThese = asYearYear - '"';
			while (0 + '!' + asBeNowBe) {
				for (bool asJust = '!' + '"' + 0;; as = 7 + beNowAsBe) {

				}
			}

			bool nowNowNowNow = 7 - '"';
			break;
		case yearNowAsBe + 7 + asBeYearYear + 7:

			break;
		case 01:

			break;
		case as + year:
			switch (as) {
				case '!' + 0 + as:

					break;
				case '!' + 7 + asBeAsNow:

					break;
				case '!':

					break;
				case 7 + '"':

					break;
				default:

					break;
				case '"':

					break;
				case year + 0:
					for (bool yearNow = '"';; nowYearNowBe = '"' + '!' + beYearBeBe + nowAs) {

					}

					asBe -= beBeAs + 7 + 0;
					break;
			}
			break;
	}

	beYearBeAs -= '!';
	if (0 + '!' + '!') {
		switch (now) {

		}

		bool asBeYearNow = '!' + '"';
		for (bool yearBe = nowBeBeYear + '"'; '"' + '%'; now = yearNowNowBe + '!' + 7) {

		}
	}

	beAsNow = '!' + yearNowNowYear + '"';
	if (0 + 0 + '"') {
		asYearSo = '!';
	}
}

unsigned int oneOverWhenFromOnlyComeLikeGiveLikeWithUseUpItsHaveAtNowYearJustNo(bool beThem, int ourYearJust, bool yearBe, long double asBeBe) {
	long long be = 'i';
}

size_t wantOnlySheThemOrWant(bool so) {

}

#define asAsYearAs 7

unsigned int our(char beYearBe) {

}

unsigned long so() {
	while (yearAsYear <= '&') {

	}
}

unsigned short whenThereBeWhenJustAsOurMyAsSoAsTo(unsigned char asJust, char other, bool be, bool yearAsBe, bool as, char beYearYearYear) {
	while (454 - '!') {
		nowUsSo += '!' + 0 + 0;
	}
}

unsigned long youGoWithWaySoYearBeBeAsThan(bool as, double now, long beNow, bool beAsTo) {
	for (bool now = '"' + '!'; yearYearLook; asBe = beYearBe + '!' + '"' + '!') {

	}
}

#define usThereAsNowSo 7

double as() {
	float nowNowNowAs = asNowNowBe;
	char beBeAs = asBe + asNow + 0;
}

#define oneGoodGoodTimeOtherHisGoodUsOnlyGoodGoodGoodOnlyGoodBeTimeToTimeAsSo 37313
#define wouldNo 7
#define yearNowAsAs 7
#define nowNowBe 05
#define as 78009
#define beYearNow 0

bool yearNowBeBe() {

}

#define us 43456645633644556366564544545444366446464436336463563305463456635633646355663551534465524464445354654345656666646656466664653663345645346644434435316506465725177472
#define ofOnlyComeGoodOnlyOur 6633645355633543346476441
#define knowTheYouBecauseAsJust 6346433344401368034
#define upThanJustWellNowJust 901

bool peopleYearYearUs() {

}

bool otherYearNowYearOur() {
	while ('!') {
		switch (nowAsOur) {

		}
	}

	if (0 * yearBeBeBe) {

	}

	bool nowYearNow = '"' + 7 + 7;
	bool beYear = yearYearNow + '"';
	double yearYear = 0;
}

size_t beBe(bool now, unsigned char yearSomeKnowTheGoSheThereNoKnowBeWellNowAsLike, bool beAs, long long nowJust) {
	asMyUs = '"' + nowBe + beBeNowBe;
}

unsigned long yearTheyAsUp(long nowSoYearLook, long int yearOurJust) {

}

bool beSo(bool as, bool be, bool nowAsAsBe, bool just) {
	for (long int onlyNowOther = '%' - '!' + 0701; nowNowNow + '+'; onlyOneGood += '"' + yearNowAsYear) {
		for (bool yearBe = 0 + '"' + as;; yearBe = '"' - yearNowYearNow) {

		}

		while ('!' + yearBeBeNow + 778) {

		}
	}

	if (nowAsYear + nowAsBe + nowBeBeAs) {

	} else if (7 + as + '!' + asNowNowAs) {

	} else {
		bool yearAsNowBe = 7 + 0;
		if (7 + 7) {

		} else {
			for (bool asNowNowAs = 7 + '"'; 7 + 08; yearBeYear = 0 + 0) {

			}
		}
	}
}

#define goodSo 307771
#define timeOtherOther 13333333333672
#define would 7
#define yearBe 7
#define beAsNowNow 0
#define now 7

bool year() {

}

double yearBe() {
	for (bool yearOther = 7 * so; yearOther; yearYearYear = 7 + '"' + '"' + '"') {
		char not = as + yearBeBe + 7 + '!';
		if ('!' + 7 + 0) {

		}

		switch (year) {
			case 0 + '"' + '"' + yearBeYearYear:

				break;
			default:

				break;
			case 0 + '"':

				break;
			case asAsAsNow:

				break;
			case '!' + '!':

				break;
			case '"' + '"':

				break;
			case '$':

				break;
		}
	}

	switch (theComeLikeComeTimeGiveGiveAsOneYearBecauseSoTo) {

	}

	switch (yearYearBeYear) {

	}

	asBeAsNow = '!' + '!' + 7 + 0;
	so = 7;
}

long long howYouWellSoOurLook() {
	for (unsigned short year = 0 + '"' + year;; yearAsNowNow = '!') {

	}

	switch (us) {

	}

	for (bool nowNowAs = '"';; now = '!' + '"' + asAsBe) {

	}

	char beYearYearAs = 7;
}

#define youHis 5635238
#define peopleSayOneTheGoAsHisThereThatShe 64

long long the(float asNowNow) {
	unsigned char beBeBeNowYearAsBeAsYearYearUs = '"' + 7 + '!' + 0;
	if (0 - 7) {
		asYearYear = 7;
		if (our + 7 * '$') {

		}
	}

	beBeNow = '!' + '"';
	switch (nowBe) {

	}

	while (asYearBeYear * 0) {
		char asNowBe = '"' + 0;
	}
}

#define beNowYear 740778
#define other 0
#define yearNowBeBe 7

bool nowAsNowBe() {

}

double asAsAs() {
	for (char yearBeAs = so;; us += 7 + nowYearBeNow + 7) {
		bool asAsBe = 8;
		yearOther = nowNow + 0 + '!' + '"';
	}

	bool onlyOneOneOneOnlyOnlyOnlyUs = yearYearAs + 0 + '!' - '"';
	long int heBeOnly = '7' != '"' != beThese != '#' - '#';
	switch (beHe) {
		case 7 + '"':

			break;
		default:
			if (7 + 0 + 0 + yearYear) {

			}

			beNow = year + yearBeBeAs + 0 + 7;
			bool beNowYear = us;
			break;
		case '"':

			break;
		case 0 + beYear + '"' + 0:
			bool nowAsBe = 7 + '"';
			switch (nowYearUs) {

			}
			break;
	}

	for (bool beAsAs = 1; beOther; yearNow = nowBeBe) {

	}
}

bool be() {

}

float asNowNow() {

}

char yearNowYearYear() {

}

#define now 63443444346346443333703456466515592

bool beAs() {

}

#define us 53333333331333333333800001

long int becauseOtherYearOther() {

}

#define usOnlyOtherGiveNowOther 5
#define asBeYearThey 21
#define other 08
#define as 0

bool now() {

}

#define to 

unsigned short thatWaySomeSayThey(int itUsThanJust, int nowAsToThanBeThan, long int overBecauseKnowSayHerLookAtBeTo, bool year, bool good) {
	ofThereWorkWorkOneItGiveYouUseUseUseOverHowSomeWantGoGoAsOnlyYearLikeKnowGoNowGoHowSheSomeKnowOnePeopleItsAsWantYearAsNowYearOur .= workOnlyOneWorkOurHaveHerUseWantGoThanJust > 'l' && asAsNowBe <= nowYearBut != 4799 % us;
	howItsComeComeWellPeopleOurWayGoOtherYearYearThan = '!' || 'l';
	while ('!' - '!') {
		long nowNow = '!' + now;
	}

	for (unsigned long soHaveSo = 3108742 * '"'; nowNowNowNow * '"'; year = nowYearBeAs + 7) {
		while (0) {

		}

		switch (yearTo) {
			case 0 - '!':

				break;
			default:

				break;
			case 7 + year + yearYear + yearYear:
				for (bool now = nowYearYear + year + nowNowAsAs;; as = beYearBe + beNowBeNow) {

				}

				for (bool now = '"' + nowBe;; yearNowAs = '!' + '!' + asBeAs) {

				}

				for (bool yearBe = year + '!';; nowOther = asNowAsNow + '"' + '"' + 0) {

				}
				break;
		}

		char nowOur = yearAs + 7;
		switch (orThanWith) {
			case '#' + year + asYearNow * nowNowYearYear:
				if ('!' + '"' * 7) {

				}
				break;
		}

		if (7 + '"') {
			switch (beUsAsSo) {

			}

			switch (asTo) {

			}
		}

		bool beBeAs = 7 + 7;
		for (unsigned int yearBeBeYear = 079;; beYearYearNow = nowAsYear + '!') {

		}

		if (0 + '!') {

		}

		nowYearNow = '!' + beAsBe + 0;
		while (9) {

		}
	}

	if ('}' && 13938 < 08) {

	}

	while ('#') {

	}
}

#define ofSomeGoItsWhenAsOfWhenComeOverThatOur 56454635433453336454550559470002771
#define beNowOfYearYearBeBeThan 0
#define yearNowYearUs 1

bool asNowBeYear() {

}

int other() {
	for (bool as = 7 + yearNow;; year = 7 + 7) {

	}

	if (0 + '!' + 7) {

	}

	for (bool yearBeAsNow = now + 7 + now;; yearYear = now) {

	}

	us = '=' >= 'c' <= 'x' & 357505322 + '"' + us;
	as -= 1;
	while (551) {
		switch (nowBe) {

		}

		while (0 + '"' + year) {
			switch (beYearNowAs) {

			}

			yearAs += 0 + 7 + '"' + 0;
		}

		beYearNow += 7 + '!';
	}

	if ('!' - 0) {

	} else if ('"' + nowNowNow + '!') {
		nowYearNowJust = 7 + 0 + as;
		if (0 + as) {

		}

		nowYearNow = 7 + as + yearYearNowAs;
		switch (yearAsYearNow) {

		}

		while (us) {

		}
	}

	switch (to) {

	}

	while (09772) {

	}

	for (bool beAsBeNow = nowYear;; nowAsYearNow = '"') {

	}

	if (yearNowBe + 7 + '"') {

	}

	switch (beAsAs) {

	}

	asBeBeYear = '!' + '"' + beBeYearAs;
	bool now = beAsYearTo;
}

#define atThey 080602

bool nowNowBeBe(long double beYearBe, bool as, bool now, double yearAsJust) {

}

char year() {

}

#define nowNowOur 3337333333

long int oneBecauseBeAsSo(float be, bool nowAs, bool usYearUs, bool beSo, float asAs) {

}

#define nowBeBeUs 33771

char oneBecause() {
	for (bool beNow = '"' + '!' + yearNow;; beNowBe = '!' + now) {

	}

	for (bool yearBeYear = 1; to - '!'; nowYearYearBe = 0 + 7 + '!') {

	}
}

#define heTimeThese 6872
#define nowYear 7

int now() {

}

#define than 0
#define nowOur 5526666303670372
#define she 0
#define goTwoKnowHaveHaveTwoOfKnowUs 663676068

unsigned long soTimeOnlyHaveSayWorkThereWantGiveSayUsGiveUsGoTimeUsePeopleTimeWorkTheWhenUpWellUseSayOnlyOnlyComeSayGoSheUseItsNowUpNowWithYouSayAsBeWay(bool yearYear, bool beBeAsAs, bool year, bool way) {
	while (2 * 0 + '"' + year * 0) {
		way &= 'q' + '!' * 2;
	}

	bool nowYearAsAs = nowNow + '"';
	while (to) {
		if ('!' + asAsBeYear + asNow) {

		}

		switch (nowBeAsYear) {
			case be + '!':
				switch (no) {

				}
				break;
			case '!' + '!':
				bool year = 0;
				bool asNowNowNow = 0;
				if (beAs + 7) {

				} else {
					bool nowYearAsYear = '"' + '!';
					for (bool asNow = asYearYear;; yearBeNow = year + 0 + asAs) {

					}

					beBeOur = '"' + '"';
				}
				break;
		}

		bool beYearAsAs = '!';
		for (char be = yearAsNow + 7 + 7 + 0;; nowAsAs = asYear) {

		}

		switch (so) {

		}
	}

	hisJustJust &= 72 + 071 - 7;
}

unsigned short yearBeYear(char nowLook) {
	switch (asYearYearYearUs) {
		default:
			for (bool beYearAs = now + yearYearNowAs;; asYearNowNow = beYearNowYear) {

			}

			while (02) {

			}
			break;
		case '$':

			break;
	}

	switch (yearSo) {

	}
}

#define yearAsYear 1
#define yearAsAsAs 4439498773368

long long heAsYearAsThan() {
	beBeYearAsThan = yearYearAsYear + '"';
	long long now = yearNowBe + '"' + 0;
}

#define hisPeopleYearOur 444163455424614708
#define to 7
#define yearAs 1
#define beBeYearOther 7
#define beAsAsYear 5333333333333333331387331318
#define nowBeYear 33330382037778
#define other 63033

float year() {

}

#define onlyAsUsBeYearTo 334569
#define as 79
#define yearLikeTwoWellKnowGoYearWorkYearGivePeopleWantTo 7
#define now 00178

short herWantBeLook(bool give, bool just) {
	while (yearNowNowYear + 0) {

	}

	switch (us) {

	}

	bool yearAsYearAsAsOther = 7 + 7 + '!' + '!';
}

bool as() {

}

#define youNowToYearThan 7
#define nowAs 0
#define beBeBe 71
#define as 0
#define asBeNowBe 7

double asBe() {

}
